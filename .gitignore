.DS_Store

# 정은지 선수 기준 모션 원본 영상 — S3(sunity-motion-pilot-videos)로 업로드. git 추적 금지.
정은지님 영상/
*.mp4
*.mov

# AWS access key 다운로드 파일 — 절대 commit 금지
sunity-api_accessKeys.csv
*_accessKeys.csv
*.pem

# Firebase 서비스 계정 키 — Firestore Admin 권한 전체. 절대 commit 금지.
*firebase-adminsdk*.json
firebase-sa.json
firebase-service-account.json

# MotionBERT 사전학습 가중치 — 용량 크고 Apache 2.0 redistribution 조건. git 추적 금지.
# scp 로 Pod 에 직접 배치 (runpod_inference/README.md "MotionBERT 가중치 scp 절차" 참조).
best_epoch.bin
**/FT_MB_lite_MB_ft_h36m_global_lite/

# Ad-hoc diagnostic scripts (Phase 17 development) — gitignored. 향후 재사용 시 정식 파일로 rename.
app/scripts/_*.mjs

# GSD framework + tooling — 로컬 환경 박힘 박힘 박힘. 다른 머신에서 다시 install.
.claude/
.codex/
.planning/WAITING.json

# Reference keypoint/angles JSON — Pod 추출 결과 박힘 박힘. Firestore seed 후 git 추적 X (재추출 가능).
reference-keypoint-reports*.json
new6-angles.json
new6-body-profiles.json

# Diagnostic visualization scripts (Pod 박힘 박힘 / debug 용도)
app/scripts/_viz_*.py

# Phase 14 pre-seed active-pose snapshot (hashes only in run log; raw snapshot not committed)
.planning/phases/14-reference-motion-registration/14-PRESEED-SNAPSHOT.json
