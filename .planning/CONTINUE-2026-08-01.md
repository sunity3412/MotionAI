# 이어서 하는 곳 — 2026-08-01 (belle 외출로 중단)

## 지금 어디서 멈췄나

**#1 (감점마다 측정 순간 기록) 계획까지 나왔고, 계획 검증 직전에 멈췄다.**
계획은 커밋돼 있다 — `.planning/quick/260801-gbk-record-atframeidx-criterion/260801-gbk-PLAN.md` (`1001edb6`).
코드 변경은 아직 **0**.

**재개 첫 명령** = 계획 검증(gsd-plan-checker) → 실행(gsd-executor, worktree) → 검증(gsd-verifier).
검증자에게 넘길 집중 검사 4가지는 이 파일 맨 아래에 적어뒀다.

---

## 오늘 belle 확인으로 드러난 가장 큰 것

**belle 기기가 07-30 doc 을 보고 있었다.** 07-31 §C-4 재산출본은 **시뮬레이터 계정**
(`fvcNXzEqKjgqVxRPVSj1iwFnIpn2`) 아래에만 있었고 belle 계정(`csKWYvI3WCPYPysNQ9KkWecaUvq1`)에는
안 닿았다. 문서 ID 는 같은데 계정이 달랐다. → 4건 복사 + 음성 키 교정 완료(반영됨).

**앱 OTA 는 정상이었다.** 낡은 것은 앱이 아니라 분석 데이터였다.

---

## 오늘 끝난 것

| 건 | 결과 |
|---|---|
| belle 계정 반영 | 4 doc 복사 (60/79/100/63), 이미지 12장 생존(206), 음성 키 정합 OK |
| **#2 음성 큐 재생 복구** | `ff07be2e` 코드 · `027f900d` 문서. 만료 인지 캐시 + 로드 실패 감시 + 15초 정지 제거. **실기기 미검증** |
| 각도 미표시 원인 | 관절 결측 아님 — `conf < 0.5` 게이트. 12관절 다 있음(hand 포함) → §C-4 유효 |
| 마커 왼쪽 몰림 원인 | UI 아님 — 모든 감점이 한 프레임 (#1 의 본체) |
| 가로 자막 없음 원인 | 자막 렌더(1542행)가 전체화면 Modal(1722~1822) **밖** |

---

## 남은 11건 (belle 이 순차 진행 지시)

TaskList 에 살아 있다. 요지:

| # | 건 | 막는 것 |
|---|---|---|
| **1** | 감점 단일 프레임 몰림 | **진행 중 — 계획 검증부터** |
| 2 | 재생 2회차 무음 | ✅ 완료 (belle 실기기 확인 대기) |
| 3 | 음성 후 영상 어긋남 | #1 이후 재평가 |
| 4 | 저신뢰 0.49 인데 63점 확정 | belle 결정 |
| 5 | 엘보 기준 패널 전 카드 무표시 | #4 와 한 뿌리 |
| 6 | 파워스핀 다리 카드 2장 동일 + 사이각 없음 | belle 결정 불요 — 바로 가능 |
| 7 | 문구가 목표를 말함 + 어깨·팔꿈치 통합 | belle 설계 |
| 8 | 가로 자막·마커 누락 | 표현 |
| 9 | 어깨 −0.8 미세 대응 | belle 결정 |
| 10 | 실 doc parity 배율 **미측정** | 정직하게 미완결 |
| 11 | 이미지 URL **08-07 만료** / Pod 상태 | 운영 |

---

## belle 이 나중에 한 번에 볼 것 (OTA 발행 후)

#2 수리분 4항목:
1. 앱을 **1시간 이상** 띄운 뒤 재진입 → 음성이 들리는가
2. **기내모드**로 실패시켰을 때 영상이 6초 안에 재개되는가 (15초 정지 없음)
3. 실패한 큐 **다음 큐**가 정상 발화하는가
4. 분석 A → B 이동 시 A 음성이 새어나오지 않는가

---

## #1 재개 시 검증자에게 넘길 집중 검사 4가지

1. **채점 무접촉이 구조로 보장되는가** — D-gbk-04 는 `_attach_translation_emission`
   (`app.py:4890-4914`)에서 tally **뒤** 각인하므로 `deduction_engine.py` diff 0 이라고 주장한다.
   그 함수가 정말 tally 뒤에 불리는지, setdefault 로만 만지는지, out-param 재료가 도달하는지.
2. **대표 프레임 산출식이 record 보고값과 정합하는가** — 특히 silent 경로가
   `per_joint_deviation` 의 `diffs` 를 얻으려 시그니처를 바꾸는데, 그 함수는 **KISMAM 채점 입력**이다.
   호출처가 깨지거나 점수가 움직이지 않는지.
3. **fail-closed 가 실제로 폴백하는가** — 필드가 비면 `worst_seconds` 경로로 되돌아가
   현행과 동일 산출이 되는지. 폴백이 깨지면 회귀다.
4. **게이트가 거짓 통과/실패하지 않는가** — 주석 속 동작명 리터럴 37회,
   `frames_fps=9.0` 2회 pre-existing 를 걸러낸 grep 이 현 트리에서 통과하는지 직접 실행.
   pytest 는 `backend/.venv/bin/python` 고정(인터프리터가 다르면 58건 기준선 대조가 무의미).

## 안 건드릴 것 (이미 소스로 반증된 오진)

- `expo-audio` 네이티브는 정상이다 — `play()` 가 종료 옵저버를 매번 재등록하고(`AudioPlayer.swift:63-65`),
  `replaceCurrentItem` 은 새 아이템을 만들어 0 에서 시작하며, `isPlaying` 은 실시간 조회다.
- 대표 프레임에 **argmax 금지** — median 을 쓰는 이유가 RTMW jitter(p99 35~50°)다.
  argmax 는 정확히 그 jitter 프레임을 "여기가 감점 부분"으로 확대한다.
- `extension_deviation` 의 시간축 집계는 max 가 아니라 **window mean** 이다(오케스트레이터 오기, 계획이 교정).

---

# 2026-08-02 OTA 발행 (belle 지시) — 검증은 08-03 시작

runtime **1.1.0** (앱 버전 동일 — 재빌드 불필요). **앱 완전 종료 2회째 적용.**

| 채널 | update group |
|---|---|
| production | `00b29a73-b2be-4383-ba93-addfb7ddbd8b` |
| preview | `b5af8d13-2617-453c-ba25-26675d552ceb` |
| development | `8bad6318-812e-4317-b415-b904952f81db` |

**롤백 (1분)** — `cd app && npx eas update:republish --group <직전 group>`
· production 직전 = `d1b97758-6007-44e9-9f1d-fc226637d588` (07-31 33 §9)
· preview 직전 = `385bfa4d-a523-4fc7-b4c3-e3c6517d0c18`
· development 직전 = `6d95fa4f-23ed-43bb-827c-9957a015f735`

⚠ **시뮬레이터 렌더 확인을 못 했다** — 시뮬에 앱 미설치, 개발 빌드부터 필요.
`npm run typecheck` 는 통과했으나 렌더 크래시는 못 잡는다([[verify-ui-on-simulator-before-ota]]).

## 이 OTA 로 보이는 것 / 안 보이는 것

**보임(앱 코드)**: 음성 캐시 만료 인지·실패 복구·15초 정지 제거 / 자막이 결함부터 말함(14/14) /
감점 표시 병합(단 실 doc 발동 0건) / 기준 패널 무표시 안내 줄

**안 보임(백엔드 재분석 필요)**: **점수 이동**(elbow-twist 63→73 · power-spin 62→67 · kip-up 99→100) /
감점별 제 순간(마커 위치·큐 분리) / 프레임 신뢰도 tie-break / 저신뢰 게이트 계측
→ **Pod 필요**. 현재 프록시 빈 404(꺼짐/삭제 미확인). SAM 배포도 미실시.

## belle 결정 대기 3건
1. 100점인데 헤드라인이 "오늘은 이 부분에 집중해봐요" 가 맞는가
2. 감점 0 인데 확대카드가 뜨는 게 맞는가 (kip-up 1→2)
3. 역립 동작 촬영 안내를 만들 것인가

---

# 2026-08-06 새 Pod 재분석 + 회귀 수리 + OTA

**Pod**: 238o5d6rfok4g6 (RTX 4090, 기존 볼륨). SSM v25 + Lambda URL 동기화.
서버 시작 정본 = `start_server.sh` (p15 판은 인버전 OFF 함정 — /health envFlags 로 발견).

**재분석** (시뮬 계정 4건, `ra.sh --reprocess-direct`, 백업 `docs_before_0806/`):
power-spin 60→60(캡 −40 절사 지배) · elbow-twist 63→**72**(억제 2 + 재측정 소멸 2) ·
pdshape 100→100 · kip-up 79→**80**. noise-floor 억제(260802-nse) 실 파이프라인 작동 실증.
**belle 계정 반영** = belle 이 `_copy_to_belle_0806.py` 직접 실행 → 검증 PASS
(음성 키 전건 belle uid, S3 객체 존재, 이미지 URL 08-13 만료로 갱신).
belle 기존 doc 백업 = Pod `docs_belle_before_0806/`.

**belle 실기기 1·2·3 확인 결과**: ① 72 OK·팔꿈치 OK·어깨 미표시 = **IN-01 저신뢰 강등의
의도 동작**(역립 visibility 0.489<0.5, 감점 -10 은 점수에 반영, 미결 #4 와 한 뿌리)
② 80 OK ③ **내 영상 미표시 = 회귀** → 260806-sjt 수리 (STATE.md 표 참조).

## 08-06 OTA 발행 (260806-sjt 가드)

runtime 1.1.0 (재빌드 불필요). **앱 완전 종료 2회째 적용.**

| 채널 | update group |
|---|---|
| production | `2904b667-698c-49d0-ae3f-fc5c4ffeff01` |
| preview | `0f294446-ca7d-46e1-8927-d33f88dfb4af` |
| development | `1de9e449-cc2f-4729-89f4-93eda6248825` |

**롤백 (1분)** — `cd app && npx eas update:republish --group <직전 group>`
· production 직전 = `00b29a73-b2be-4383-ba93-addfb7ddbd8b` (08-02)
· preview 직전 = `b5af8d13-2617-453c-ba25-26675d552ceb`
· development 직전 = `8bad6318-812e-4317-b415-b904952f81db`

⚠ 시뮬 렌더 확인 못 함(개발 빌드 부재) — 08-02 와 동일 제약. typecheck 만 GREEN.
belle 재확인 3항목: ① 내 영상 재생 ② 음성 큐 발화 ③ 최근 실업로드 분석 영상 정상(회귀 체크).

## 08-06 밤 belle 재확인 결과 → 다음 세션 = 재생·음성 큐 영역 debug 사이클

가드 OTA 후 **내 영상 재생 자체는 복구됨** (belle 스크린샷: 엘보 양 패널 렌더,
duration 0:17 정상). 그러나 음성·영상이 동시에 살아 돌아간 첫날이 되자
**재생·음성 큐 영역의 결함이 연쇄로 드러남** — belle: "영상 오류가 지금 상당할 수 있어".

| # | 증상 (belle 실기기, 08-06 밤) | 아는 것 |
|---|---|---|
| V-1 | **음성 종료 후 내 영상만 미재개** — 선수 영상만 돌아감 (엘보, 내 영상 4.9s 에 동결) | 재개 코드는 양쪽 `play()` (VideoCompare 728-730). 실기기에서 왼쪽만 실효 없음. 후보 = 재개 직후 왼쪽 재-pause 잔존 경로(33-16 F-1 계열) / 학생 영상 92MB 재버퍼 스톨(기준 6MB 는 즉시). **미결 #3 의 재현 조건 확보** |
| V-2 | **시작하자마자 음성 발화** (킵업) — belle: 심각한 문제 | 큐 앵커 0.889s(frame 16)는 old/new doc **동일**(08-06 재분석이 만든 것 아님, 실측 대조). split_angle 이 잰 프레임 없어 legacy 단일 시각 폴백 = **미결 #1 의 gbk 미해결분**(gbk SUMMARY 가 "킵업 split 안 풀린다" 명기). 데이터(백엔드) + 시작 직후 발화 억제(앱 설계, belle 결정) 양쪽 경로 |
| V-3+ | belle "다른 것도 몇 개 있었다" — **목록 미확보** | 다음 세션 시작 시 belle 에게 전체 목록 요청 (08-01 의 11건 목록 방식) |

**선행 조건 = 재현 수단.** 개발 빌드가 없어 시뮬 재현·검증이 불가한 것이 "눈감고
OTA" 반복의 구조적 원인 ([[verify-ui-on-simulator-before-ota]]). 다음 세션 순서:
① 개발 빌드(시뮬용) 확보 ② V-1 재현 → 원인 확정(추측 수리 금지, F-6 원칙)
③ V-2 는 앱측 억제안을 belle 결정으로 올리고 데이터측은 #1 후속으로
④ 수리 → 시뮬 검증 → OTA. **Pod 238o5d6rfok4g6 = 08-06 밤 belle 이 Terminate**
(볼륨 보존 — 재생성 시 부트스트랩+URL 동기화 ~10분, 절차는 `memory/current-pod-238o5d6rfok4g6.md`).
V-1~2 는 앱 문제라 Pod 불요. belle 확인은 08-06 복사해 둔 belle 계정 doc 4건으로 계속 가능
(이미지 URL 08-13 만료). Pod 종료 제안을 먼저 하지 말 것
([[feedback-pod-keep-running-no-terminate-nagging]]).

단서 (belle 미확인): V-1 동결 상태에서 하단 재생 버튼을 누르면 다시 도는지 —
돌면 상태머신 문제, 안 돌면 플레이어/버퍼 문제로 갈린다.

---

# 08-07 시작 — V-A "자막-음성 불일치" (밤 2회 수리 후에도 실기기 재현)

**착수 = 보드 아티팩트 + 이 절.** 어젯밤 경과: usc(불변식) → belle 2차 관측 →
wj3(플레이어 재생성·틱 표시·nudge, OTA `747ee98f`) → **belle: "음성이 자막과 여전히
안 맞음"**. 앱측 오재생 경로(스테일 replace)를 없앴는데 증상 불변 = **원인이 앱 재생
로직 밖일 가능성이 커졌다.**

## 자정 실측 (S3 직접 조회 — 이게 출발점)

belle 계정 엘보 prefix `results/csKW…/elbowtwistsisterFault1785373695/` 에 **mp3 12개**:
현세대 4(r00 right_elbow / r01 right_shoulder / r02 left_hip / r03 right_knee) +
**07-31 구세대 8(r00~r07, 옛 8-record 명명)** 공존. 그리고 구세대 안에서
`r02:left_shoulder.mp3 md5 == r03:right_shoulder.mp3 md5` — **좌/우 어깨 음성이
바이트 동일 = 합성 파이프라인이 잘못된 내용을 쓸 수 있음이 실증됨.**
(스크래치에 12개 전부 다운로드해 둠 — 세션 바뀌면 재다운로드 1분.)

## 용의점 (우선순위순 — 하나씩 죽일 것)

| # | 용의점 | 판별 방법 (기기 불요) |
|---|---|---|
| S-1 | **현세대 mp3 4개의 내용이 cueLine 과 다르게 합성** (구세대 어깨 중복이 전례) | whisper(faster-whisper small)로 4개 전사 → record cueLine 과 대조. **10분, 결정적** |
| S-2 | **OTA 미적용** — belle 재확인이 발행 직후라 옛 번들(ac79c72e 이전)일 수 있음 | belle 앱 완전 종료 2회 후 재확인 + `eas update:list` 대조. 단 S-1 이 양성이면 이건 부차 |
| S-3 | **자막-음성 조인 어긋남** — cueWindows 가 시간순 정렬인데 recordId 부여가 doc 순서 인덱스면 짝이 밀린다 | result.tsx 의 cueWindows/audioCues 빌드 지점에서 text·recordId 가 **같은 record 객체**에서 나오는지 코드 판독 + 조인 단위 테스트 |
| S-4 | 합성 시점 원본: 어제 재분석 중 "Cerebras 코칭 실패 → 수치 폴백" 2건 — 폴백 경로가 cueLine-음성 짝을 깨는지 | `_synthesize_coach_audio_items`(pipeline app.py ~3866) 판독 — 텍스트 소스가 record.cueLine 인지, 인덱스 재사용인지 |

## 정리 대상 (원인 확정 후)

- S3 구세대 mp3 잔재 삭제 (belle·sim·신시뮬 3계정 prefix 전부) — playback-url 이
  doc 저장 키와 exact 비교라 **재생엔 무해**하지만 세대 혼합은 다음 진단을 오염시킨다.
- 원인이 S-1/S-4(백엔드)면: 합성 수리 → 4건 재합성·재업로드는 Pod 없이도 가능
  (Polly 는 AWS — 로컬 boto3 로 합성 가능한지 확인. 불가면 새 Pod 1회).

## 어젯밤 확정 성과 (재조사 금지)

- 재생 불변식(usc): belle 실기기에서 **작동 실증** — 큐1 재개 정상, 실패 시 대칭 정지.
- 틱 표시(wj3): 시뮬 확인 — 단 **4개가 한 지점 뭉침**(틱 빌더가 record atFrameIdx
  미사용, gbk 미배선 잔재) = 별도 배선 거리.
- 시뮬 재현 루프: 개발 빌드 #29 + Metro + 시뮬 계정 `Wm5KTg0OsObIHchuMmfMr1hDSxy2`
  (doc 4건 + S3 복사 완료) — 그대로 재사용 가능.

---

# 08-07 결과 — V-A 원인 확정·수리 완료 (belle 실기기 확인 대기)

**원인 (S-1~S-4 가 아니라 다섯째)**: 08-02 mrg 가 **자막**을 결함-선행(statusLine+행동절)으로
재조립했는데 **음성 합성**은 cueLine 전문(목표 도입문 포함) 그대로 → 08-02 이후 모든 큐에서
음성≠자막이 구조적으로 성립. 한 분석의 전 큐가 같은 목표문으로 발화를 시작해 "같은/다른 음성"
지각을 이중 유발. mrg SUMMARY 미검증 #4("안 들었다")로 박제돼 있던 그 리스크가 F-6 해소로 표면화.
S-1(mp3==cueLine 정합)·S-3(recordId 원자 조인)·S-4(합성 짝 정상) 전부 whisper·코드 실측으로 소거.

**수리**: 합성 텍스트 = 자막 조립식 미러(`_coach_audio_speech_text`, pipeline/app.py) + 미러 핀
테스트 + deductionSheet.ts lockstep 주석. 기존 데이터 = 유저 대면 3계정 27키 전부 재합성
(엘보 12 + 킵업/파워스핀/실업로드 071df9f8… 15 — 엘보 매몰 갭을 전수 스캔으로 잡음),
whisper 전수 검증(목표문 시작 0건). 구세대 고아 42키 삭제(참조 키 11/8/8 전건 보존).
**OTA·재설치 불요** — 앱 코드 무변경, S3 데이터만 교체. eval 계정(phase25e…)은 재생처 없어 제외.

**belle 확인 (재생만 하면 됨)**: 엘보·파워스핀·킵업·실업로드에서 ①각 큐 음성이 자막과 같은
문장으로 시작 ②큐마다 서로 다른 문장(종전엔 전부 "목표는 거꾸로…" 시작) ③"목표는" 도입문 안 들림.
확인되면 `/gsd-debug continue va-subtitle-audio-mismatch` (resolved 아카이브).
반려(음성이 자막과 **다른 관절**)면 같은 명령으로 S-2 분기 재조사.
세션 정본 = `.planning/debug/va-subtitle-audio-mismatch.md`.

## 08-07 낮 — belle 킵업 반려 2건 → 2차 수리 + OTA (실기기 확인 대기)

**반려**: ① 킵업 음성이 포인트가 아닌 시작 직후 발화 ② 마침표 자리를 그대로 이어 읽음.
② = 1차 수리의 결함(합성·자막 둘 다 공백 결합 — whisper 검증이 문장부호를 지워 못 잡음).
① = V-2 로 알려져 있던 미인증 앵커(킵업 split 은 측정 순간이 없어 0.889s 가짜 폴백)가
실사용 차단급으로 확정된 것.

**2차 수리** (`35eb03d3` + `ae398c51`, push 됨): 결함문 뒤 마침표 경계(자막·합성 lockstep,
27키 재재합성·whisper 재전수) + **큐·틱을 record 인증 순간(atVideoSec/atFrameIdx)으로** —
미인증 record(킵업 split, 파워스핀 r01, 실업로드 3건)는 순간을 지어내지 않고 자동 발화 억제
(안내문은 카드 존치, Pod 재분석이 순간을 만들면 자동 복귀 = V-2 데이터측 잔여).
틱은 record 별 분리(엘보 4개 분산 — 뭉침 잔재 해소).

**시뮬 실측**(개발빌드 #29): 킵업 무발화 완주 / 엘보 4큐 제 순간(4.9→7.4→10.1→11.1s)·
"…있어요. 위아래로…" 마침표 자막·정지-재개 대칭 / 파워스핀 인증 2큐만(구 2.11s 발화 없음).
**OTA 3채널**: production `e096f9f2` / preview `4441c980` / development `f3db8421`
(롤백 = 직전 production `747ee98f`). 앱 테스트 78 · coach_audio 27 · tsc GREEN.

**belle 확인 (이번엔 OTA 수신 필요 — 앱 완전 종료 2회)**: ① 킵업 시작 직후 무발화
② 엘보 큐 4개 제 순간·상이 문장·마침표 쉼 ③ 파워스핀 2큐만 ④ 음성==자막.

---

# 08-07 마감 — 다음 세션 착수점 (belle /clear 예고)

하루 요약: V-A 종결(belle 확인) + 앱 수리 4사이클(fpw 번호 시간순·재개 백오프·체이닝·부위
빨강 / iwp 기준 패널 짝 스냅·드리프트 히스테리시스·마커 강조 / k70 가로 자막 #8·말하는
지점만 표기·재재생 settle / m63 드래그 발화 게이트·체인 겹침 전용). OTA 최신 prod
`421b4d7b`. 전 사이클 시뮬 실증 후 발행, STATE.md Quick Tasks 표에 4행 기록됨.

**belle 결정**: "이대로 가면 끝이 없다 — 일단 대략 됐다 치고, 미세조정·정확성·부드러움은
Pod 전제로." → 앱측 미세 루프 **동결**. 돌파 3수 승인:
1. **서버측 정렬 합성 비교 영상** (본명 돌파, 새 세션 1순위) — Pod 에서 두 패널을 DTW warp
   로 프레임 정렬해 나란히 붙이고 감점 정지·음성·자막 타이밍까지 구운 **단일 mp4** 를 렌더.
   앱은 그것만 재생 — 동기·스냅·재개·드리프트 버그 계열이 원리적으로 소멸. 기존 Pod
   트랙(정렬 개선·자세 거리 짝 재선정·split 측정 순간·부드러움)은 전부 이 렌더의 입력으로 흡수.
   **첫 단계 = 전 동작 프로토타입 배치 → belle 느낌 평가** (belle 08-07: "왜 엘보 1건? 다른
   데도 마찬가지일 것" — 한 동작 매몰 금지 원칙 적용). 렌더러는 동작 무관 단일 코드(동작별
   규칙 0), 배치 = 엘보(대략 정렬·저신뢰 최악 케이스)·킵업(측정 순간 없는 split)·파워스핀
   (인접 큐 겹침)·pdshape(감점 0 순수 재생)·belle 실업로드(순간 데이터 전무) — 이 다섯이
   오늘 버그들의 원인 케이스를 전부 커버하는 일반화 매트릭스. 채택 시 라이브 동기 기계는
   폴백 강등.
2. **경험 계약서 + 자동 판정 리그** — 오늘 수작업(녹화→타임라인 추출→정지 횟수·시각·자막
   문장 판독·whisper 전사 대조)을 스크립트화, "전 항목 PASS 아니면 OTA 없음".
3. **파일럿 기준선 한 장** — 남은 항목을 파일럿-차단/이후로 belle 과 한 번에 가르기.

**새 세션 착수 순서**:
1. belle Pod 생성(**RTX 4090, EU-RO-1 우선, 기존 Network Volume 연결**) → Pod ID 수신
2. 부트스트랩 ~10분 (절차 = memory/current-pod-238o5d6rfok4g6.md: bootstrap_full.sh + awscli
   + __pycache__ 청소 + **start_server.sh 정본**(p15 판 = 인버전 OFF 함정) + SSM v25 갱신
   + Lambda RUNPOD_ANALYZE_URL 동기화). push 는 돼 있음(`eea172af`).
3. 합성 영상 프로토타입 = GSD 정식 사이클(/gsd-quick --discuss 이상 권장 — 렌더 스펙:
   레이아웃/fps/음성 트랙/자막 굽기 vs 앱 오버레이/스크럽 UX)
4. 병행 로컬(GPU 불요): 짝 재선정·split 순간 알고리즘(하네스 저장 좌표), 리그 스크립트화

정본 = 보드 아티팩트(URL·갱신법 = memory feedback-status-board-artifact-is-canonical) +
memory/RESUME-2026-08-07-va-root-cause-fixed.md.
