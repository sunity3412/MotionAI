# 2026-08-14 이어서 하기 — 발굴 반영 확인 → 발굴 일반화 스윕

> 착수점. 이 파일만 읽으면 이어진다. 현황 정본 = 보드
> https://claude.ai/code/artifact/f8630d0f-c07f-4d82-943a-0fa272900b5f

## 어제(08-13~14 심야) — 11사이클 체인, 전부 push·검증 완료

1. **선 문법 종결** (ebd→fxx→hlv→ivs→l0u→m0k→nh4): belle 번복(스포트라이트 철회·선
   채택) → 골반 P3·관절 위 V 운영 배선 + 표시 좌표·V 선분 = freeze align 단일 출처(B
   스펙) → Pod 실증(점수 60 재현) → 승인 5동작 스윕 → 판정 전건 종결(반려 2건 회복
   완결 — 왼무릎 짝 = B 현행 유지, override 불필요). 카드 커버리지 13건 중 10.
2. **초 라벨 수리** (u8i, 검증 7/7): ÷9.0 잔존 청산 — label_fps 실효 fps 환산, Pod
   실측 5.9→5.3s. ★상류 의제 = peterpan freeze 초가 클립 밖(구 타임베이스 잔재).
3. **왼무릎 신규 발굴** (wif): 좌표 무입력 게이트 스캔이 kpo 인증 홀드 재발견 —
   **belle 판정 cand13b(12.9/12.4s) 채택, 사전 박제 일치 1/1 = 승격 실적 장부 개시**.
   cand02b 반려(사진상 판독 불가 — 전달력 의제).
4. **영상 반영 실증** (0p2): fresh pdshape 영상에 발굴 정지 삽입 로컬 재렌더 — 리그
   ALL PASS(discover 라벨 명기 면제 1건, 사칭 0) + diff 3층 국한 + **상속 카드가
   belle 채택 카드와 md5 byte-동일**. ★S3 업로드 보류.

## 오늘 순서

1. **belle 실물 확인**: /Users/Shared/sunity-freeze-inject-260814/ (재렌더 영상
   `재렌더영상_신규정지포함.mp4` + 스틸 13장 + 안내.md). 보드에도 게시됨.
   ★확인 항목 1건: 재사용된 r04 캡션("무릎을 펴는 게 아니라, 깊게 접은 다리 모양
   그대로...")이 belle 결함 서사(다리 펴고 회전 후 걸기)와 정합하는지 — 어긋나면
   반영 사이클에서 캡션 수정.
2. 확인 통과 시 → **반영 사이클**: S3 같은 키 업로드 + doc renderedCompare 갱신 +
   backend 정식 배선(freeze 주입 레이어 + H2 `discover` 라벨 — 필요 변경 목록 =
   0p2 `evidence/SUPPORT-SURFACE.md` §5) + 검증 사다리 + Pod 실증(새 Pod 필요).
3. **발굴 일반화 스윕** (belle 질문 "pdshape에서만 한겨?" 대응): 승인 5동작 전체에
   발굴 하네스를 돌려 동작별 발굴/침묵 시트 — 운영 자동화 전 관문.
4. 대기열: 승인 정지 2건 처분(pdshapefault r01·peterpan r00) · 미세조정 = 실증하며
   그때그때 · 일러스트(뒤로 — 재개 시 "생성 후 코드로 부위 표시" 경로부터) ·
   ★실기기 통합 검증 = 파트 검수 전부 합쳐진 뒤 (belle 방침).

## 확정 사항 (재질문 금지)

- 문법: 골반 = P3 하이브리드 · 그 외 = 관절 위 V · 좌표·V 선분 = 게이트 freeze
  align 단일 출처(fail-closed) · 라벨 = 실효 fps · 마크 길이·위치 튜닝은 실증하며.
- 카드 = 영상 freeze 상속 (발굴 순간도 영상에 먼저 들어가고 카드가 상속).
- 발굴 승격 경로 = 사전 박제 장부 일치 실적 (현재 1/1). 자동화는 실적 누적 후.
- 짝 선정 = 기술 요소 정체성 우선, 포즈 유사도는 보조 (라운드 5 교훈).

## Pod

- **터미네이트됨** (08-14 심야, belle). 반영 사이클·일반화 스윕의 Pod 실증 때 새 Pod
  필요 — 재진입 6단계 = memory `current-pod-mddy6gsqmt24ud` (주소 변경 → SSM+Lambda
  URL 재동기 필수). 앱 업로드 분석은 새 Pod 까지 불가 상태.

## 데이터 좌표

- fresh doc: uid `fvcNXzEqKjgqVxRPVSj1iwFnIpn2` / `p34fresh1786628533` (60점,
  ref-pdshape). 발굴 장부 = wif `DISCOVERY-LEDGER.md`. 판정 장부 = xa1 `JUDGMENT.md`.
- 하네스: wif `discover_knee.py`(발굴) · 0p2 `inject_freeze.py`(주입 재렌더) ·
  u8i `verify_label.py` · nh4 `verify_port.py` (전부 원본 무수정 사본 체계).
- 눈 원장 90건+ (Phase 22 씨앗). 승격 실적 장부 1/1.

## 규율 리마인드

- 이미지 전달 = 보드 embed 정본 · 사전 박제 → belle 판정 대조 → 장부 append ·
  frames-before-numbers · 완료 주장은 실행 로그로 · LLM 학습 영향 항상 언급
