# 2026-08-14 이어서 하기 — 스윕 판정 대기 + Pod 실증

> 착수점. 이 파일만 읽으면 이어진다. 현황 정본 = 보드
> https://claude.ai/code/artifact/f8630d0f-c07f-4d82-943a-0fa272900b5f

## 오늘(08-14) 한 것 — 4사이클, 전부 push 완료

1. **캡션 교정** (chd): belle 판정("영상은 괜찮음, 캡션만") 반영 — 발굴 정지의
   캡션·음성을 결함 서사 정합 신규 문장으로 교체(Polly 재합성, 정지 9.8→11.2s).
   원본 r04 정지(10.5s)는 무변경(H3 문자 일치가 기계 증명). 카드 3장 md5 불변.
   → **belle 승인**: "좋다 다음 단계로 넘어가자. 다른 영상들도 이런식으로".
2. **발굴 반영 배선 + 프로덕션 반영** (di7): `result.discovery` 일반 스키마 +
   `build_timeline` 주입 레이어 + verify H1~H4 **doc-anchored fail-closed**
   (blanket 면제 거부 — 사칭 구멍 차단). **사본 delta(monkeypatch) 청산**.
   프로덕션 실행 = 승인본 mp4 md5 사전 대조 후 canonical 키 byte-보존 업로드 +
   discover mp3 업로드 + doc discovery/renderedCompare 기입 → live 왕복 검증.
3. **발굴 일반화 스윕** (ehz): 승인 5동작 13 record 전수. 소스 게이트 5/5 PASS
   (Pod 불요) → 88버킷 → 압축 37 → 육안 29/8 → 눈 58회 → **PASS 5 / 기각 24**.
   **발굴 2동작**(pdshapefault 4 · powerspin 1) / **침묵 3**(elbow·kipup·peterpan).
   ★cand13B 카드 = belle 채택 wif 카드 **byte-동일**(같은 영상·다른 경로 재생산).
   ★elbow 침묵 실측 = 트랙이 5~7도 굽힘 주장한 순간을 눈이 전부 "폄"으로 뒤집음.
4. **운영 재렌더 소실 수리** (ghs): di7 배선이 **운영 경로에선 안 돌던** 갭 —
   in-memory result 에 discovery 미탑재 → 흔적 0 소실 + 리그 ALL PASS.
   RED 재현 후 수리(조달 fail-open + mp3 회수 + 회계 로그). pytest 59 IDENTICAL /
   4211 passed. 상세 교훈 = memory `partial-field-writes-invisible-to-inmemory-doc`.

5. **데이터 플라이휠 첫 가동** (j24 → l5i): 눈 원장(운영 분석마다 쌓이는데 소비자 0)을
   학습 4번째 트랙 `eye` 로 잇고, belle 판정 2건 반영 — "정은지 계정만 먼저"로
   watch 러너 **최초 실행**(신규 4편), "앱 계정은 우리거·미오픈"으로 **동의 축 해제**
   (게이트 제거 아님 — 자사 계정 sha16 화이트리스트 + 만료 조건 + 명단 밖 보호 테스트).
   균등 축 오적용 수리(동작 → 관절x관측)로 방출 7→35행. 상세 = l5i SUMMARY.

## Pod (현재 가동 중)

- **`4tayd9c172qh53`** (RTX 4090, belle 제공 08-14). 재진입 6단계 완료 —
  코드 `cf23bbd7` 동기 · start_server.sh md5 정본 일치 · /health 4항목 PASS ·
  SSM v32 + Lambda `RUNPOD_ANALYZE_URL` 새 proxy 재동기(나머지 3키 보존).
  SSH = `ssh root@213.173.109.73 -p 11655 -i ~/.ssh/id_ed25519`.
  ★아직 실증 미착수(belle 이 플라이휠로 우선순위 전환) — 켜둔 상태.

## 오늘 순서

1. **belle 판정 대기 — 스윕 추천 2건**: 재료 =
   `/Users/Shared/sunity-discovery-sweep-260814/` (카드·전신짝 9장 + 안내.md),
   보드에도 embed. 판정할 것 = ① 피디쉐입 왼팔꿈치 16.5초 채택/반려
   ② 파워스핀 왼어깨 0.5초 채택/반려(★방향 반대 — 학생이 폈고 기준이 접음.
   초반 0.5초라 wif 반려분 cand02b(1.5초)와 프로필이 닮음) ③ 동반 2장 처분
   ④ elbow·peterpan 침묵이 옳은 침묵인지 ⑤ 벌림각(split) 발굴을 과제로 세울지.
   판정 결과 → DISCOVERY-LEDGER 승격 실적 장부 append (현재 1/1, 이번 5행 대기).
2. **학습 1바퀴** (플라이휠 완성): 선행 = 크롭 PNG 를 `training/phase22/eye/` 로
   업로드(현재 `--upload --with-eye` fail-closed 차단 중, 크롭 원본은 이미 같은
   버킷 `results/*/*/eye/` 에 있음 — 버킷 내 이동). 그 뒤
   `backend/training/sft/run_retrain_cycle.sh` (Qwen3-VL-8B QLoRA 4bit, 4090 적합).
   ★GPU 수 시간 작업 — belle 지시 확인 후 착수.
3. **Pod 실증** (미착수, Pod 은 이미 가동 중): ghs 수리가 운영 경로에서 실제로
   도는지 확인. 확인 항목 = `discovery 조달 n=…` + `[discover] rid=…` 실행 로그 ·
   리그 `[discover]` PASS 라인 · 영상/카드 실물. ★함정 = `rerun_compare_stage.py`
   금지(운영 경로 결함 못 잡음) · 넘기는 result 에서 `discovery` 를 **지우고** 호출
   (안 지우면 실증 무효) · S3 put/Firestore write 는 스텁(승인본 덮어쓰기 금지).
4. 스윕 채택 시 → 반영(di7 일반 경로 그대로 — 이제 재분석에서 살아남음).
4. 대기열: split 축 발굴(눈에게 물을 새 질문 형식 필요 — 단일 마크 좌표 부재) ·
   압축 상한 4/record(88중 37만 눈까지) · peterpan 승인 freeze 6.444s 가 align
   클립 6.067s 밖(u8i 상류 의제) · 승인 정지 2건 처분 · 일러스트(뒤로) ·
   ★실기기 통합 검증 = 파트 검수 전부 합쳐진 뒤 (belle 방침).

## 확정 사항 (재질문 금지)

- 문법: 골반 = P3 하이브리드 · 그 외 = 관절 위 V · 좌표·V 선분 = 게이트 freeze
  align 단일 출처(fail-closed) · 라벨 = 실효 fps · 기준측 초 표기는 회전류
  (`stamp_ref = category=="spin"`)만 — belle amendment, 정상 동작.
- 카드 = 영상 freeze 상속. 발굴 순간도 영상에 먼저 들어가고 카드가 상속.
- 발굴 승격 경로 = 사전 박제 장부 일치 실적. 자동화는 실적 누적 후.
- 짝 선정 = 기술 요소 정체성 우선, 포즈 유사도는 보조.
- 발굴 채택 반영 경로 = di7 배선 (doc `result.discovery` → 주입 레이어 →
  fail-closed verify). 사본 delta 는 청산됐다 — 새로 만들지 말 것.

## Pod

- **터미네이트 상태**. 앱 업로드 분석은 새 Pod 까지 불가.
- RunPod API 키가 로컬에 없어 **Pod 생성은 belle 콘솔 작업**. 나머지(재진입·
  기동·URL 재동기·재분석)는 내가 실행.

## 데이터 좌표

- fresh doc: uid `fvcNXzEqKjgqVxRPVSj1iwFnIpn2` / `p34fresh1786628533` (60점,
  ref-pdshape, discovery 반영 완료). 스윕 데이터 = `.planning/phases/
  35-server-rendered-comparison-video/data/{motion}/{doc,align}.json` (5동작
  전건 로컬 replay 가능) + 폴 `260811-ii0.../sweep_out/poles.json`.
- 장부 = wif `DISCOVERY-LEDGER.md` (ehz 사전 박제 append 완료, 판정란 공란).
  시트 = ehz `DISCOVERY-SHEET.md`. 판정 장부 = xa1 `JUDGMENT.md`.
- 하네스: ehz `discover_sweep.py`(일반화 발굴) · chd `inject_freeze.py`(주입
  재렌더) · di7 `wire_discover.py`(배선 재현) — 전부 원본 무수정 사본 체계.
- 눈 원장 90건+ + ehz 58건 (Phase 22 씨앗).

## 규율 리마인드

- 이미지 전달 = 보드 embed 정본 · 사전 박제 → belle 판정 대조 → 장부 append ·
  frames-before-numbers · 완료 주장은 실행 로그로 · LLM 학습 영향 항상 언급 ·
  ★리그 PASS 를 무결성 증거로 읽지 말 것(회계 집합 밖은 구조적으로 못 잡는다).
