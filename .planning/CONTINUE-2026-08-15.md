# 2026-08-15 이어서 하기 — 학습 결과 확인 + belle 판정 2건

> 착수점. 이 파일만 읽으면 이어진다. 현황 정본 = 보드
> https://claude.ai/code/artifact/f8630d0f-c07f-4d82-943a-0fa272900b5f

## 지금 돌고 있는 것 (belle 취침 시각 기준)

**Pod `gvbqbkstfp4jcd` (RTX 5090 32GB) 에서 SFT 학습 84% 진행 중** — 18분 뒤 학습
종료, 이어서 게이트(병합 + vLLM 평가) → 승격까지 체인 자동. 실행 래퍼 =
`/workspace/_run_train.sh`, 로그 = `/workspace/_train.log`.

- 산출 = `/workspace/phase22_sft_out/v27-20260814-140946/checkpoint-{17,34,51,...}`
  (에포크마다 저장, **네트워크 볼륨이라 Pod 를 지워도 남는다**).
- ★belle 이 밤중에 Pod 를 터미네이트했을 수 있다 → 그러면 학습은 checkpoint-51
  까지 남아 있다. 재개 = 새 Pod + `pod_doctor.sh` + `--resume_from_checkpoint`.
- SSH = `ssh root@213.173.103.5 -p 27227 -i ~/.ssh/id_ed25519` (Pod 생존 시)

## 첫 할 일

1. **학습 결과 확인** — 체인이 끝났으면 `[chain] 전 단계 완료` / 게이트 exit /
   `promotion_ledger.json` 의 `current` 확인. **게이트 통과 여부가 핵심**
   (7월 v4~v13 은 한 번도 통과 못 했다 — current=null). 통과·실패 어느 쪽이든
   **이유와 함께** 보고할 것.
2. **Pod 정리** — 결과 회수 후 belle 에게 터미네이트 안내(스톱 말고 터미네이트).

## belle 판정 대기 2건 (오늘 것)

1. **캡션 원인 절** — 자막 3줄 제한에 맞추느라 문구를 줄였다(62→51자 / 50→34자).
   **의미가 상했는지 belle 판정 필요**. 재료 = `/Users/Shared/sunity-caption-cause-260814/`
   (카드 5장 + 안내.md), 판정란 = rcz `CAPTION-SHEET.md` §4.
2. **각도 표기 억제** — 반려하신 파워스핀 카드에서 V 마크가 사라지고 원 마커만
   남는 게 의도대로인지. 같은 재료 폴더 3번 파일.

## 2026-08-14 에 끝낸 것 (요지)

- **발굴 판정 반영**: 캡션 3절화(증상→원인→행동, 원인 없으면 기존과 byte-동일) +
  각도 표기 적용 조건(A3 양 패널 사이각 차, 임계 99.6도 — 승인 10/10 md5 동일,
  반려분 1장만 V 소멸). 상세 = quick-260814-rcz.
- **눈 원장 → 학습 유입 개통**: eye 트랙 신설 + 수확기 + 크롭 반출. 학습셋
  canonical **262행**(distill 79 / eye 35 / perturb 118 / text 30). quick j24 + l5i.
- **동의 축 hold 해제**: belle "앱 계정은 그냥 우리거야 아직 앱이 오픈되지도 않음"
  → 자사 계정 sha16 화이트리스트(명단 밖은 게이트 유지, 회귀 테스트로 못박음).
- **플라이휠 자동화**: `flywheel_cycle.sh` + launchd `com.sunity.flywheel`(월 10:07)
  + `.planning/FLYWHEEL-LOG.md`. **1회 실행 검증 완료**(로그가 스스로 커밋됨).
  ★줄이 안 늘면 자동화가 죽은 것.
- **포드 재현성**: `pod_doctor.sh`(7영역 진단, import 로 판정) ·
  `bootstrap_full.sh` 리포 박제 · `setup_train_venv.sh` 레시피.

## 학습 환경 — 오늘 실측으로 확정된 것

- **4090 24GB 불가**(5설정 전부 OOM). **5090 32GB 가능** — 단 `use_liger_kernel`
  필수. 로짓(시퀀스 x 어휘 15만)이 8.57GiB 스파이크를 내는데 liger 가 그걸 없앤다.
  ★프레임을 줄이는 방향은 **틀렸다** — 길이를 지배하는 건 영상이 아니라 3만~5만 자
  텍스트 타깃이다(프레임 절반으로 줄였더니 로짓이 오히려 커졌다).
- liger 켜면 **22-07 검증값 그대로**(32프레임·rank64·ViT 학습·32K) 돌아간다.
- venv 함정: `--system-site-packages` 로 만들면 Pod 이 바뀔 때 깨진다(python 버전
  불일치 · 베이스 cudnn 충돌 · 의존성 구멍). **python 버전만 맞으면 링크 수정으로
  30~40분 재설치를 피할 수 있다**(오늘 실증).

## 규율 리마인드

- 이미지 전달 = 보드 embed 정본 · 사전 박제 → belle 판정 대조 → 장부 append ·
  frames-before-numbers · 완료 주장은 실행 로그로 · LLM 학습 영향 항상 언급 ·
  ★설치 성공 != import 성공(진단은 실제로 불러봐야 한다) ·
  ★"OOM → 설정 축소" 전에 데이터 크기부터 재라.
