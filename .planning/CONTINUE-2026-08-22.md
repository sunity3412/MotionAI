# 2026-08-22 이어서 하기 — 08-21 은 수확의 날

> 착수점. 이 파일만 읽으면 이어진다. **인계 규율**: 아래는 관측과 belle 판정 원문.
> 내 해석은 "내 해석:"으로 표시. 이월된 belle 판정 대기 항목은 belle 이 먼저 꺼내지
> 않는 한 재노출 금지 (메모리 `climb-question-closed-do-not-reask`).

## 한 줄

**일러스트 "어떻게" 첫 에셋이 승인→배선→OTA까지 나갔고, 발굴 채택 3건이 운영 영상에
반영·실증됐고, 재학습 v32 가 A100 에서 완주했다. 남은 큰 것 = v32 gates(다음 Pod) 와
발전 캡션.**

---

## ★내일 먼저 볼 것

### 1. Pod — 밤사이 없음 (belle 08-21 밤 전부 Terminate 계획, 내일 5090 재개)

- belle 계획: 오늘 Pod(A100 w340kaemere1po·4090 a9lta40b3p58z5) 터미네이트, **내일 5090
  (32GB, CUDA 12.8+ 필터)으로 재개**. **SSM `pod-expected=down` 전환 완료(v5)** — 감시 조용.
- 내일 5090 뜨면: bootstrap_full.sh → pod_doctor.sh → start_server.sh → SSM
  `runpod-analyze-url` + Lambda env 갱신 → **`pod-expected=up`** → **v32 gates 재발사**
  (병합본 17G 볼륨에 있어 서빙+판정만 — run_retrain_cycle.sh gates, rp=1.05).
- 08-21 마지막 SSM 상태: url v37 = 4090(종료 예정 주소) — 내일 갱신 필요. 산출물·venv 전부
  네트워크 볼륨이라 안전. 4090 복구 절차 선례 = current-pod-a9lta40b3p58z5 메모리.

### 2. 재학습 v32 — train 완주 + 병합 완료, gates 는 32GB+ Pod 대기

```
[train]   완주  68/68 step · 4 epoch · train_loss 1.139 · eval_loss 1.107(ep1) · wall 2h54m
          산출 = /workspace/phase22_sft_out/v32-20260821-091049/checkpoint-68 (볼륨 존속)
          venv = /workspace/train_venv_cu124 (transformers 5.11.0 핀 — v30 사인 해소 입증)
[merge]   완주  checkpoint-68-merged **17G bf16** (볼륨 존속 — 재작업 불요)
[gates]   ★STOP 4090 24GB 로는 서빙 불가가 측정 확정 (가중치 16.65+오버헤드 2.9+KV 4.5
          ≈ 24.1 GiB > 23.52). max_model_len 축소는 비교성 훼손이라 기각.
          → **VRAM 32GB+ AND 드라이버 570+** Pod (5090/A6000/L40S, CUDA 12.8+ 필터)에서
          gates 만 재발사 — 병합 스킵이라 짧다. 넘어야 할 선 = v29 성적
          (빈 골격 9/29 · faults 2 · 4동작 중 1동작 · 게이트 FAIL). 디코딩 rp=1.05.
[promote] gates PASS 후 (fail-closed — cycle_gate_exit.txt 는 08-15 스테일 FAIL 그대로)
```

### 3. Cerebras 402 — **해소됨** (belle 08-21 밤 충전 완료)

- tg6 실증 중 402 payment_required 2회 관측(Gemini cross-fill 로 분석은 done) →
  belle 이 cloud.cerebras.ai 에서 충전 완료. 키는 SSM 그대로라 다음 분석부터 자동 복구.
- 내일 첫 분석에서 Cerebras 200 인지 로그로 한번 확인하면 완전 종결 (메모리
  `cerebras-billing-console`).

### 4. 후속 재료 (수리 별건, 관측만 박제됨)

- 운영 마킹 payload 의 `:discover` rid suffix 소실 (pipeline/app.py:4400-4403,
  D-di7-05 계약 불일치 — tg6 SUMMARY. no-write 라 실 doc 무영향이었음)
- climbfault 조목이 무릎 1건뿐인 게 충분한가 — 일부러 넣은 실수 목록은 belle 만 안다.
  belle 이 꺼내면 카드로 확인.

---

## 08-21 에 끝난 것 (11 quick tasks, 전부 push)

| 무엇 | 결과 | 어디 |
|---|---|---|
| 일러스트 "어떻게" 3단계 잔상 | belle: 40·60 절대 불가, 20 종류 채택 + 화살표 필요 | exq |
| 화살표 A/B | A(그림에 굽기) 결함 재현 — 생성 모델이 출발점 못 지킴. B(앱 오버레이) 채택 | fe9 |
| 수치 표기 | belle: "수치 = 발전 추적(20°→18°), 캡션에 '저번보다 더 벌어졌어요'" — 수치 문장 하단 중앙 채택 | fe9 |
| **킵업 배선 + OTA** | 20-1(잔상 baked) 에셋 + 앱 오버레이, 시뮬 실증 후 **OTA 발행** (preview, 714e9439) | gb7 |
| 발굴 판정 | 08-14 스윕 대기분: 추천 2건(피디쉐입 팔꿈치·파워스핀 어깨) **채택**, 동반 2건 보류 | 판정 보드 |
| **발굴 3건 운영 반영** | di7 경로 재사용, 왼무릎 보존 bit-exact, 쓰기 8건 왕복 검증 | kgq |
| climb·combo 스윕 | 3동작 침묵 → climbfault 는 **하네스 veto OFF 가 원인**으로 판명 | ls0→pnp |
| climbfault 코퍼스 | veto-ON doc 교체(record 1건 = 오른무릎 2.4s), 재스윕 = 눈 전건 기각(정직한 침묵), 하네스 경고 수리 | pnp |
| Pod 복구 ×2 (A100 → 4090) | bootstrap→doctor→서버 up→SSM v36→v37·Lambda 동기→pod-expected=up | (직접) |
| **v32 train 완주** | cu124 venv + transformers 5.11.0 — v30 사인 해소 | o3m |
| E2E + D-di7-03 종결 | 신선 분석 done·발굴 정지 결정론 재현·discover mp3 운영 회수 증거 | tg6 |
| v32 병합 + gates 시도 | 병합 17G 성공 / 서빙은 24GB 한계 측정 확정 → 32GB+ Pod 이월. 부산물 = gemini __init__ lazy 수리(141 tests) | umc |

## 대기열 (Pod 무관)

- **발전 캡션** ("저번보다 더 벌어졌어요") — 같은 동작 직전 분석 조회 + 노이즈 문턱 측정.
  belle 채택 근거는 메모리 `kipup-how-asset-approved-20-1`.
- 일러스트 "어떻게" 확산 — 나머지 12짝 (킵업 어깨 포함, 잠금 계열 문법 번역은 짝별 belle 승인).
  생산 경로는 검증됨: 그림 생성 → belle 실물 승인 → 좌표 메타 → 앱 오버레이.
- 판정 보드 아티팩트 2개: 킵업(50b723cd…) · 발굴(7f9796bc…) — 기록 상태로 갱신 완료.

## 08-21 에 틀린 것 (다음 세션이 같은 실수 안 하게)

- 이월된 belle 판정 대기 항목(클라임)을 다시 물어 "뭘 자꾸 묻냐" — 닫힌 건은 재노출 금지.
- 확대비교 발굴 반영 상태를 중간 기록(chd)만 보고 "보류 중"으로 오보 — 뒤 사이클(di7)이
  이미 반영했었다. 체인 끝까지 본 뒤 단정할 것.
- veto-ON doc 의 analysisId 를 no-veto 실행 ID 로 오귀속해 플랜에 핀 — 실행자 STOP 게이트가
  잡았다. 산출물 ID 는 산출물에서 직접 읽어 박제할 것.
