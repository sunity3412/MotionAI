# 2026-08-23 이어서 하기 — 08-22 는 판정과 원인의 날

> 착수점. 이 파일만 읽으면 이어진다. **인계 규율**: 아래는 관측과 belle 판정 원문.
> 내 해석은 "내 해석:"으로 표시. 이월된 belle 판정 대기 항목은 belle 이 먼저 꺼내지
> 않는 한 재노출 금지 (메모리 `climb-question-closed-do-not-reask`).

## 한 줄

**v32 gates 가 5090 에서 완주해 FAIL 로 확정됐고(형식 축 전부 해소·내용 축 전멸),
침묵의 원인이 실측으로 갈렸다(암기 42%·일반화 0·베이스 프라이어 후퇴). 발전 캡션이
배선·실증됐고 문구는 belle 이 "저번보다 나아졌어요"로 갱신 — OTA 만 대기.**

---

## ★내일 먼저 볼 것

### 1. Pod — 없음 (belle 08-22 밤 terminate, `pod-expected=down` v7 전환 완료)

- 다음 Pod 뜨면: bootstrap_full.sh → pod_doctor.sh → start_server.sh → SSM
  `runpod-analyze-url`(현재 v38 = 종료된 wvonetv6g3hu66 주소) + Lambda env 갱신 →
  `pod-expected=up`. 선례 = 메모리 `current-pod-wvonetv6g3hu66` (08-22 복구 그대로).
- **Pod 규율 (belle 08-22 원문): "포드 과금 낭비하게 두면 절대 안돼" — Pod 가 켜져
  있으면 Pod 필요한 작업(GPU 서빙·학습·실측)부터 처리하고, Pod 무관 작업은 뒤로.**

### 2. belle 결정 대기 2건 (재료 전부 준비됨 — 묻기만 하면 됨)

- **OTA 발행**: 발전 캡션 신문구 "저번보다 나아졌어요" 실렌더 캡처
  (`.planning/quick/260822-oe1-progress-caption/sim_progress_caption_v2.png`) 확인 후
  "걸어도 된다" 한마디면 발행. 시뮬·Metro 는 08-22 밤 기준 켜져 있었음.
- **split_angle 문턱 21° 전환**: 실측 근거 제시됨 — 같은-영상 재분석 페어(n=55)에서
  20° 플립이 **36.4%**(순수 측정 요동), 48h 실사용 페어(n=48)에선 플립 0.
  권장 = split_angle 만 criterion 별 문턱 21°(한 스텝 제외), 나머지는 전역 12° 유지.
  대가: 킵업 다리 캡션은 30° 이상 개선에서만 뜬다(뜨면 진짜).

### 3. v32 사이클 마감 상태 (다음 재학습의 출발점)

```
[gates]   FAIL 확정 (base=1 require_pass=1) — 4 게이트 동작 전건 "fault 결함 0건"
          형식 축 전부 해소: 파싱실패 5/29→0 · 폭주 13/37→0 · run1↔run2 bit-exact
          내용 축 전멸: 빈 골격 12/29→29/29 · faults 2→0
[원인]    실측 완료 (원장 = Pod 볼륨 /workspace/heldout_results.json — 볼륨 존속):
          학습 JSONL 원문 15행 투입 → held-out 0/3 · train 5/12 발화(2건은 개수까지 일치)
          발화 = 펜스 없음+실코칭(학습 타깃 양식) / 침묵 = ```json 펜스+빈 coaching
          — 학습 타깃 272행 전부 펜스 없음이므로 침묵 = 베이스 모델 프라이어 후퇴
          내 해석: 학습 전무 아님(암기 42%), 일반화 0. 코퍼스 결함 행 45%라 결핍도 아님.
          안 잰 축(다음 처방을 가를 것) = 데이터 규모·다양성 vs 학습 강도(LoRA 설정)
[promote] 미실행 — CONTINUE 계약(PASS 후)대로 스킵. FAIL attempt 원장 기록은 v28·v29
          관례인데 실행이 권한 분류기에 차단됨 — belle 이 원하면 Pod 에서
          `bash training/sft/run_retrain_cycle.sh promote` 재시도 (기록 전용, 승격 없음)
```

### 4. 소소한 이월

- Cerebras 402→충전 완료(08-21) 후 **아직 실분석이 없어 200 확인 미완** — 다음 첫
  분석 로그에서 확인하면 종결 (메모리 `cerebras-billing-console`).
- `.planning/TRAINING-DUE.md` 존속 — v32 사이클이 그 응답이었고 FAIL 이라 파일 유지.
  다음 재학습은 §3 의 "안 잰 축" 측정 후.

---

## 08-22 에 끝난 것

| 무엇 | 결과 | 어디 |
|---|---|---|
| Pod 복구 (5090 wvonetv6g3hu66) | bootstrap→doctor→서버 200→SSM v38→Lambda→up | (직접) |
| v32 gates 재발사 | 완주 **FAIL** — rp=1.05·GATES_PORT=8100·ab_venv_cu129 | cycle_gates_260822.log |
| v29 대비 성적표 | 같은 자(bakeoff 원장)로 재측정 — 형식↑ 내용↓ | §3 표 |
| 침묵 원인 실측 (b) | held-out 0/3 · train 5/12 · 베이스 프라이어 후퇴 | heldout_results.json |
| **발전 캡션 배선** | measure-first(문턱 12 실측: 페어 282·표본 1017·P95 11.60) + 순수 lib TDD + 시뮬 양면 실증 | quick-260822-oe1 |
| 캡션 문구 갱신 | belle: "저번보다 나아졌어요 정도가 괜찮을 것 같은데" → 교체·재실증 | 9ff16c7f |
| split_angle 교차표 | 같은-영상 플립 36.4% / 48h 플립 0 — 문턱 21° 권장 근거 | (§2) |
| phrasebook 어색 문장 발굴 | belle 이 "반동을 다리로 만들어보세요" 지적 — 출처 = `backend/data/phrasebook.json` ref-kip-up.split_angle cueLine 둘째 문장. **수정 방향 belle 미정 — 묻지 말고 belle 이 꺼내면 처리** | (미착수) |

## 08-22 에 틀린 것 (다음 세션이 같은 실수 안 하게)

- **Pod 를 켜 둔 채 Pod 무관 작업(캡션 시뮬)을 먼저 했다** — belle 지적. Pod 가 살아
  있으면 GPU 필요한 작업부터 소진할 것 (§1 규율).
- 시뮬 시드 doc 에서 `result.comparison.mode` 를 빠뜨려 캡션 미표시를 앱 결함으로
  오인할 뻔 — **시드는 소비 코드(findPreviousComparable 등)가 읽는 필드를 코드에서
  역산해 만들 것**. 실물이 안 뜨면 시드부터 의심.
- 이 인계 직전까지 promote 차단 건을 "belle 승인 대기"로 두 번 노출 — 결정 대기
  항목은 §2 처럼 한 곳에 모아 한 번만.
