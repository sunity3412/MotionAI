# 2026-08-25 이어서 하기 — 08-24 는 대전환의 날

> 착수점. 이 파일만 읽으면 이어진다. 인계 규율: 관측과 belle 판정 원문만,
> 내 해석은 "내 해석:" 표시. 이월된 belle 판정 대기 항목은 belle 이 먼저
> 꺼내지 않는 한 재노출 금지.

## 한 줄

**일러스트 기능이 belle 최종 결정으로 전면 제거됐고(-2,892줄, 하루 8+라운드
기각의 귀결), 확대비교 사진 7일 만료 결함이 수리·배포·소급 실증됐다.
RunPod 일반 API 키 확보로 Pod 을 이제 Claude 가 직접 만들고 끈다.**

---

## ★내일 먼저 볼 것

### 1. belle 한마디 대기 1건 — **일러스트 제거 OTA**

- 08-24 아침 OTA(97088038, preview)에 발전 캡션이 실려 나갔는데 그날 저녁
  기능 전체가 제거됨 → 실기기에서 지우려면 제거본 OTA 1회 필요.
  코드·게이트·시뮬 회귀 전부 완료 상태 — "걸어도 된다" 한마디면 발행.
- **일러스트 재도입 제안 금지** (belle: "아예 빼자. 확대비교랑 모션 분석에
  집중" — 메모리 illustration-feature-removed-focus-zoom-analysis).

### 2. 집중 방향 = 확대비교 + 모션 분석 (belle 08-24 지시)

- 확대비교 1호(사진 만료)는 완료 — 다음 후보는 belle 이 정한다.
- **모션 분석 = 재학습 사이클(v33)**: 시작 전 v32 침묵의 "안 잰 축"
  (데이터 규모·다양성 vs LoRA 학습 강도) 측정부터. admit 325 로 v32(272행)
  대비 재료가 늘어 있음 (TRAINING-DUE.md 08-24 갱신: 영상 353/400·admit 325/60).
- **Pod 은 이제 내가 만든다**: `/sunity/motion/runpod-api-key` (일반 키,
  Read/Write, GraphQL 검증 완료). 규율 유지 — 작업 끝나면 즉시 Terminate,
  `pod-expected` 동기화. ★잔액 $10.47 — 재학습 전 충전 필요 여부 belle 확인.

### 3. 별건 부채 (등재만 — 착수는 belle 판단)

- **라이브 SAM 스택 낙후**: 배포된 템플릿 117줄/함수 2 vs git 777줄/함수 8.
  실제 배포 관행 = 함수 zip·레이어 직접 갱신 (08-08 이력, 08-24 도 동일).
  `sam deploy` 를 지금 돌리면 수개월치 미배포 인프라가 통째로 나간다 — 하지 말 것.
  풀 배포하려면 템플릿 파라미터 5종(31-10 visual 게이트, 의도적 no-default)
  값 결정 필요.
- 레이어 상태: playback-url = v19 (v17 + s3keys·models 두 파일만 HEAD 교체).
  firestore_admin 등 나머지 모듈은 v17(08-08) 상태 — 의도적 최소 반경.
- Cerebras 402→충전(08-21) 후 200 확인 미완 — 다음 실분석 로그에서 종결.

---

## 08-24 에 끝난 것

| 무엇 | 결과 | 어디 |
|---|---|---|
| RunPod 일반 API 키 | SSM `/sunity/motion/runpod-api-key`, GraphQL 검증 | 메모리 runpod-general-api-key-in-ssm |
| split 문턱 criterion화 | 기본 12 + split 21 (규칙 `max(12, ceil(P95_c)+1)`), 교차표 원장 박제(구두 36.4% 재현 안 됨 — 52.7%, 문턱 재료는 일치) | quick-260824-bxf |
| 캡션+문턱 OTA | 발행 완료 97088038 (그날 저녁 기능 제거로 사문) | bxf |
| 일러스트 8+라운드 | 잔상 생성·편집·스켈레톤·rotate 전건 기각 → **기능 전면 제거 -2,892줄**, 테스트 208/208 실패 0 | gt1 · jw4 · pqc |
| 확대비교 사진 만료 수리 | playback-url asset 'faultZoom' + 앱 재발급 훅, **외과 배포(레이어 v19)**, 7-30 회색 패널 부활 실렌더 | quick-260824-q6p |
| 플라이휠 월요 알람 | 정상 동작 확인 (belle 의 다른 프로젝트 알람과 구분: Sunityfunding daily-refresh 08:00) | com.sunity.flywheel |

## 08-24 에 틀린 것 (다음 세션이 같은 실수 안 하게)

- **일러스트에 하루를 태웠다** — 같은 수리 반복(치환 8라운드)을 3회째에 끊는
  규율이 있었는데 8회까지 갔다. belle 이 구조를 끊어줬다("아예 빼자").
- 외과 배포 1차에서 **의존 파일 누락**(app.py 가 참조하는 models.py 상수를
  레이어에 안 실음) — 함수 코드를 갈면 그 코드의 import 대상 전부를 diff 로
  확인할 것. 서버 로그(AttributeError)로 잡아 v19 보강.
- 분류기 차단(프로덕션 Lambda write) 2회 — r7k 선례대로 우회 없이 스크립트
  작성 → belle `!` 실행 분담으로 해결. 이 프로토콜이 표준.

## 세션 환경 관측

- 시뮬(iPhone 16 Pro 873D7CB3)·Metro(:8081) 켜진 채 종료 — belle 확인용.
- Anthropic API 529 폭주 시간대 있었음(서브에이전트 4연속 사망) — 인라인
  전환으로 돌파. 재발 시 백오프 후 재시도, 안 되면 인라인.
