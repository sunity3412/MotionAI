# 2026-08-30 이어서 하기 — Phase 36 계정 시스템 (36-01 완료)

> 착수점. 인계 규율: 관측과 belle 판정 원문만, 내 해석은 "내 해석:" 표시.

## 한 줄

**계정 시스템을 로드맵에 Phase 36 으로 올리고 첫 겹(화면 3종 + 라우팅)을 실행했다.
게스트 동선 회귀 0 을 시뮬레이터로 실증했고, 인트로 배경을 폴스포츠로 교체해 belle 승인까지
받았다. provider 배선은 아직 0 — 거기가 다음 겹이다.**

## ★내일 먼저 할 것

**36-02 Google 로그인** — Firebase iOS 앱 등록(내가 실행 가능) → 네이티브 구글 로그인 →
`linkWithCredential` 게스트 승계. 여기서 로그인 표시 이름을 받아 인사말
`{name}님, 오늘도 한 발 더!` 에 물린다(belle 08-30 결정).
belle 판정 대기는 2건뿐이고 둘 다 36-02 를 막지 않는다.

## belle 결정 (2026-08-30, 락인)

- **범위 = 소셜 4종만.** 이메일 가입(STEP01/02)·비밀번호 찾기·Face ID 는 이번 범위 밖.
- **인트로는 Figma 레이아웃 그대로.** "시작하기" = 게스트 진입(현행 동작 유지),
  "이미 계정이 있으신가요? 로그인하기" = 로그인 화면.
- **인트로 배경 = Figma 를 따르되 사진만 폴스포츠로 교체.** 후보 6장을 실제 앱 화면에
  걸어 비교 → **C 확정**(커밋 1f124579, 출처는 36-ASSETS.md).
- **로그인 배경 = Figma 그대로** (`#FFEDEA`). 구현이 이미 그러해 변경 0.
- **인사말 "희연" 자리 = 가입한 실명 또는 아이디.** 템플릿은 그대로 두고 36-02 에서
  로그인 표시 이름을 물려 넣는다.

(08-28 결정 4건은 `.planning/phases/36-account-system/36-CONTEXT.md` D-01 에 원문 박제)

## 오늘 한 일 (전부 실행으로 검증함)

### Phase 36 등재 + 계획
`.planning/phases/36-account-system/` — `36-CONTEXT.md`(D-01~D-08) + `36-01-PLAN.md`.
ROADMAP Phase 36 항목 = Goal·belle 결정·범위·Success Criteria 5개. Depends on 없음
(분석 트랙 34·35 와 독립).

### 36-01 실행 (커밋 f957bad9)
- `app/src/app/index.tsx` 개편 (Figma 1:142) — 사진 배경 + 로고/구분선/슬로건 +
  투명 CTA + "로그인하기" 링크. **CTA 동작은 손대지 않았다** (`signInAnonymously`).
- `app/src/app/auth/login.tsx` 신설 (1:550), `auth/signup.tsx` 신설 (1:961).
- `app/src/constants/authCopy.ts` — 문구 단일 출처. `socialProviders.ts` — 색·치수.
- `app/src/components/SunityWordmark.tsx` / `SocialIcon.tsx` — Figma 벡터 인라인.
- 토큰 2개 신설 (`authLoginBg` #FFEDEA, `introCtaBorder`). **brand #FF4B33 무접촉.**

**검증 (실행함)**: `tsc --noEmit` 통과 · iOS 시뮬레이터(iPhone 17 Pro, iOS 26.5)에서
인트로 → 로그인 → 가입 왕복 후 뒤로 → **"시작하기" → 익명 로그인 → 튜토리얼 진입**
스크린샷 확인 · 화면 파일 hex 리터럴 0 / UI 한국어 리터럴 0 (grep).

## 오늘 정정한 것 (인계 노트 오류)

| 08-29 인계 노트 | 실측 |
|---|---|
| "Figma 계정 화면 = 인트로·가입·STEP01·STEP02 4개" | **로그인 3화면(1:550/1:581/1:605) + 비밀번호찾기 3화면 + 가입 검증 5상태**가 더 있다. 노드 전수 = 36-CONTEXT D-02 |
| "콘솔 전이라도 Google·Apple 까지는 착수 가능" | **부분 오류.** Firebase 에 **iOS 앱이 미등록**(웹 앱 1개뿐, MCP `firebase_list_apps` 실조회). iOS 앱 등록 + Auth provider 활성화가 선행. 다만 이 둘은 내가 할 수 있어 belle 대기 항목은 Apple Developer capability 하나 |
| "가입 후 프로필/레벨 입력 단계는 없다" | **맞다** (가입 섹션 한정). 레벨 `1:611`·종목선택 `1:646` 은 별도 섹션 = 온보딩, 계정 범위 밖 |

## 약관 — belle 지시로 새로 씀 (커밋 833334d5)

belle: "펀딩과 커뮤니티에 집중된 약관이거든, 기존에 있는 건" / "참고해서 모션분석,
AI 서비스에 맞게 조정이 필요해보여" → 기존 문서 실측 후 Motion 전용 초안 작성.
격차 6건 근거 = `36-LEGAL-GAP.md`. 가입 화면 링크가 이제 앱 내 문서로 간다.

**남은 결정 2건 (belle):**
- **배치** — A-1 Motion 전용 문서 유지(권장, 파일럿을 funding 배포에 안 묶음) vs
  A-2 회사 방침 1개에 Motion 절 추가(원칙적으로 더 맞으나 funding 저장소·배포 필요).
- **법무 검토** — 민감정보(통증부위)·국외이전(RunPod 루마니아)·AI 학습 3건은 내가 문안을
  확정할 수 없다. `COMPANY` 상수의 사업자 정보·보호책임자도 belle 이 채워야 한다.

★출시 게이트: App Store Connect 는 **공개 URL** 을 요구한다. 파일럿은 앱 내 화면으로
충분하지만 정식 출시 전에 정적 페이지로 내보내야 한다.

## belle 판정 대기 (1건 잔여)

1. **나머지 카피** — 가입 부제 "3초만에 가입하고…"(마케팅 톤) vs 현재 앱 기대설정 톤.
   전량 `authCopy.ts` 한 곳이라 배선 후 한 번에 봐도 비용 0.

★**튜토리얼 화면의 다크 배경 충돌은 여전히 별건으로 남아 있다** — 인트로는 belle 이
Figma 우선으로 판정했지만, 그 판정을 튜토리얼까지 확장해도 되는지는 안 물었다.

## belle 콘솔 작업

**완료 (2026-08-30)**
- Firebase iOS·Android 앱 등록 — 내가 실행.
- Firebase Auth **Google provider 켜짐** — belle 이 콘솔에서 토글.
  OAuth 클라이언트 자동 생성 확인(iOS `CLIENT_ID` + web clientId 확보).

**남음**
- **Firebase Auth Apple provider** — 새 제공업체 추가 → Apple → 사용 설정.
  입력란은 비워도 된다(네이티브 iOS 흐름). 36-03 선행.
- **Apple Developer** — App ID `com.sunity.aicoach` 에 Sign in with Apple 활성화.
  ★Firebase 토글만으로는 부족하다. 둘 다 필요.
- **카카오** — 기존 앱에 iOS 플랫폼 추가(번들 ID + 커스텀 스킴). Android 는 **키 해시**도.
- **네이버** — 기존 앱에 모바일(iOS/Android) 등록.
- **Android SHA-1** — Play 앱 서명 또는 EAS 릴리스 키스토어 지문. 확보되면 내가
  `firebase_create_android_sha` 로 넣는다. belle: "베타테스터 거치긴할거지만" →
  Play Console 배포 시점에 지문이 생기므로 Android 구글 로그인은 그 이후가 자연스럽다.

## 남은 계획 (36-02 ~ 36-06)

| plan | 내용 | 선행 |
|---|---|---|
| 36-02 | Google 로그인 + 게스트 승계(`linkWithCredential`) | Firebase iOS 앱 등록(내가) |
| 36-03 | Apple 로그인 (`expo-apple-authentication`) | belle Apple capability |
| 36-04 | Lambda `POST /auth/social` — 카카오/네이버 토큰 검증 → `createCustomToken` | — |
| 36-05 | 카카오·네이버 앱 배선 | belle 콘솔 + 36-04 |
| 36-06 | 마이 탭 계정 섹션 + 게스트 승계 E2E 실증 | 36-02~05 |

**36-05 첫 태스크 = 미검증 갈래 해소**: 카카오·네이버가 커스텀 스킴(`sunityaicoach://`)을
Redirect URI 로 받는지. 안 되면 (a) https 중계 페이지 또는 (b) 네이티브 SDK
(`@react-native-seoul/*`)로 우회. 이걸 먼저 재고 배선한다.

## 박제할 함정 (오늘 실측)

**Figma 프레임을 PNG 로 export 하면 프레임 배경이 같이 구워진다.** 로고 `1:147`·`1:573`,
소셜 아이콘 `1:974`/`1:979`/`1:981` 이 전부 frame 이라 @4x PNG 에 `#CCCCCC` 배경이
붙어 나왔고, **시뮬레이터에서 회색 상자로 발각**됐다. typecheck 도 리뷰도 못 잡는 계열.
해법 = `svgAssets` 를 `SvgXml` 로 인라인(신규 의존성 0). 원본이 래스터인 Google 만
`rawImages` 에서 crop. 메모리 `figma-frame-export-bakes-background` 에 박제.

## 상태 (그대로 두고 감)

```
Pod 0대 · 잔액 $4.61 (시연 전 충전 필요) — 08-29 과 동일, 오늘 손 안 댐
Lambda RUNPOD_ANALYZE_URL = 죽은 주소 — 시연 때 Pod 띄우며 갱신
SSM pod-expected = down · launchd podhunt/cyclewatch = unload
Metro + iOS 시뮬레이터(iPhone 17 Pro) = 이 세션에서 띄워둠. 남아 있으면
  `pkill -f "expo start"` 로 정리.
미커밋 = 세션 시작 시점과 동일 + 오늘 문서(plan.md / CONTINUE / 메모리)
```

## 별건 (불변)

- 라이브 SAM 스택 낙후 — `sam deploy` 금지, 함수 직접 갱신. **36-04 에서 Lambda 를
  새로 만들 때 이 규율에 걸린다 — 신규 함수 추가 방식을 그때 정할 것.**
- 일러스트 재도입 제안 금지.
- Phase 22 v34 FAIL — belle: 데이터 더 쌓고 재시도.
- RTMW clean weight — 출시 hard gate (파일럿·TestFlight 는 범위 밖).
