# 2026-08-31 이어서 하기 — 두 목적 축으로 재정렬

> 착수점 (유일본). 인계 규율: 관측과 belle 판정 원문만, 내 해석은 "내 해석:" 표시.
> 이 파일 하나 읽고 시작하면 된다. 이전 착수점은 전부 `.planning/archive/`.

## 목적 (belle 2026-08-30 원문 — 모든 판단의 기준)

> "목적은 단순해. 고객이 정은지 선수와 비교하기 위해 그 동작을 올렸을 때 잘
> 분석되는가(Mode1), 내 동작을 전 후로 올렸을때 비교해서 발전을 체크할수있는가
> (Mode3). 그렇게 되게끔 일단 정리를 해줘."

**두 목적에 기여하지 않는 일은 하지 않는다.** 파킹 목록의 재론은 belle 이 먼저 꺼낼 때만.

## ★belle 에게 보고하는 형식 (2026-08-30 합의)

- **판정 먼저**: 된다 / 반쪽 / 안 된다 + 한 줄. 수치는 내가 근거로 쥐고, belle 이 "왜?" 물을 때만.
- **판정을 구할 땐 사진으로**: "이 팔꿈치 펴져야 하나요? ○/×". 수치표 들이밀지 말 것
  ("정확한 수치는 나한테 보여줘도 햇깔려서 몰라"). [[frames-before-numbers-gate]] 실전 적용.
- **권할 땐 하나만**: "A를 권합니다, 왜냐면 B" — 선택지 나열 금지.

---

## Mode 1 — 정은지와 비교. 판정: **조건부 된다**

**되는 것** (실사용자 분석 363건 실측, 평가배치 609건 제외 후):
- 관절 8곳 지적·코칭·부상위험·보완운동·음성 = 빠짐없이 나온다
- 확대 비교·합성 비교 영상 = 대체로 나온다 (최근 90%)
- 점수 변별 = 된다 (중앙 76, 40~100 고르게 분포. "60점 뭉침"은 평가배치 오염 착시였음 — 정정 완료)
- 등재 밖 동작도 **분석이 막히지 않는다** — criteria 없으면 line 축만 빠지고
  angle+stability+코칭+확대비교 전부 나온다 (loader FileNotFoundError → BENT_OK 폴백 실측)

**조건** (이게 무너지면 전부 실패):
- ★**GPU Pod 이 켜져 있어야 한다. 폴백 없음.** 현재 Pod 0대, Lambda `RUNPOD_ANALYZE_URL`
  = 죽은 주소. 기동 5분 절차 = memory [[demo-only-pod-bring-up-procedure]].
  E2E 는 앱 순서(upload-url → Firestore 문서 먼저 → S3 PUT) — 틀리면 조용히 mode3 로 떨어짐.

**약점 (우선순위순)**:
1. ~~belle 이 실제로 보는 축이 코드에 없다~~ → **08-31 해소 (quick/260831-bjj)**: 상체
   꼿꼿함·머리-척추 1자 순수 함수 + mode1 코칭 배선 완료, 피터팬 방향 검증 PASS
   ([[belle-readings-20260817-discovery]] 해소 절 참조). 벌림 대각선 = split_angle_series
   기존 존재. ★Pod 뜨는 날 실코칭 출력에서 문구 확인 남음.
2. ~~코칭이 1줄로 떨어지는 비율 31%~~ → **08-31 재측정 PASS** — 75초 영상 코칭 완전
   섹션 3/3 + 실전 잘림 발생 시 salvage 작동 실증 (아래 Pod 검증 섹션 참조).
3. ~~line(신전) 축이 사실상 파워스핀에서만 켜진다~~ → **08-31 검토 결론: 이건 약점이
   아니라 belle 06-27 실측 결정이었다** (정타 폼이 무릎을 굽혀 EXTEND 가 false fault —
   ref-peter-pan.yaml 헤더 참조). 08-17 판독이 가리키는 것은 델타 축(08-31 구현)과
   reference_relative — line 재추가는 하지 않는다. 남은 동작 새 판정은 belle 이 꺼낼 때
   사진으로 소량씩. (내일 첫 작업 #3 결론과 동일)

## Mode 3 — 전후 비교로 발전 확인. 판정: **된다, 실증이 얇다**

- 실사용자의 **17.9%가 실제로 쓴다** (65건). "아무도 안 쓴다"는 내 오진이었음(평가배치 오염).
- 첫 분석 = 절대평가, 이후 = 최근 mode3 와 자동 delta 비교. 발전 캡션("저번보다
  나아졌어요") 배선돼 있음(08-22).
- 내 해석: mode3 는 **학생-학생 비교라 같은 파이프라인끼리**다 — 기준 기질 불일치(33-07)의
  영향이 mode1 보다 작다.
- ★체크 필요: **mode3 전후 페어 E2E 를 한 번도 최근에 안 돌렸다.** Pod 뜨는 날 1회
  (발전 캡션·성장 카드까지 눈으로).

---

## 오늘 버린 것 (확정 — 다시 줍지 말 것)

| 버림 | 왜 |
|---|---|
| `svg_spec` 게이트 (커밋 b340a4e3) | 일러스트(08-24 제거)용 필드. 소비처 0인데 승급을 막고 있었다. 복원 순서는 코드 주석에 |
| `reference_loader` 배선 계획 | 고쳐도 소비처 없음. 게이트와 함께 사문 |
| "재촬영 문턱" 질문 (Phase 34 원안) | 관절 0개 분석이 972건 중 0건 — 그런 영상은 실제로 안 온다 |
| EXTEND ○/× 수치 판정지 (아티팩트) | belle "수치는 봐도 햇깔려" + 08-17 에 이미 부분 답 있음. 남은 동작은 나중에 **사진으로 소량씩** |
| `.planning` 루트 24개 낡은 문서 | archive/ 로 이동 (bbb9179a). 루트 = 살아있는 8개만 |

## 파킹 (두 목적에 지금 기여 없음 — belle 이 꺼낼 때만 재론)

- **Phase 22 자체 모델 학습** — belle: 데이터 더 쌓고 재시도. v34 FAIL 의 한 축(svg_spec)은 오늘 사라짐
- **Phase 35 서버 렌더 비교 영상** — 승인은 있으나 코어(위 약점) 먼저
- **계정 36-03~06** (Apple·카카오·네이버·마이탭) — belle: "중요한건 로그인이니 게스트보단 분석"
- **33-07 기질 flip** — belle 보류 유지 + Pod 필요. mode1 정확도에 닿는 건이라 파킹이지 폐기 아님
- **31-12 배포 게이트** — 참고코너 인프라. 두 모드 직결 아님
- **미실행 꼬리 — 처리 완료 (2026-08-31, quick/260831-c3l)**: 실측 6건이었다("5건"은 오기 — 괄호 내역 01×2·12_5×1·20×1·24×1·25×1 합이 6). 사문 2(01-24·12.5-01) / 실질완료 3(01-25·24-03·25-04) / 파킹 1(**20-04 — SCORE-09 는 별도 PENDING 잔류**, belle 이 꺼낼 때만 재론). 스텁 = `phases/*/{01-24,01-25,12.5-01,20-04,24-03,25-04}-SUMMARY.md` (각 스텁에 확인 커맨드+결과 박제)

## 내일 첫 작업 (belle 결정 불필요, Pod 불필요)

1. ~~belle 08-17 축 구현~~ → **완료 2026-08-31, quick/260831-bjj** (커밋 7aed6ff7~c5b2fe6c).
   features.py 순수 함수 2종(`torso_uprightness_series` 0°=꼿꼿·제품 사용은 델타만,
   `head_spine_alignment_series` 180°=1자) + `posture_axis_summary`(nanmedian, significant≥5°)
   → mode1 `_build_coach_context` postureAxes → 양 writer 공유 인과형 렌더(학생 열위·significant
   만 발화, 미발화 시 프롬프트 byte-불변). **완료 판정 PASS**: 피터팬 align.json 사전 박제 검증
   ref 10.75° < user 20.59°(delta +9.84°) = belle 원문 방향 일치. 테스트 4528/0.
   ★남은 것: Pod 뜨는 날 실코칭 출력에서 이 축 문구 확인.
2. ~~미실행 꼬리 삼진 분류~~ → **완료 2026-08-31, quick/260831-c3l** (실측 6건 — 사문 2·실질완료 3·파킹 1. 상세는 위 파킹 목록의 처리 완료 라인).
3. ~~line criteria 반영 검토~~ → **검토 완료 2026-08-31, 결론 = 반영하지 않음.**
   피터팬·엘보 criteria 가 빈 것은 방치가 아니라 **belle 06-27 실측 결정**이었다
   (ref-peter-pan.yaml 헤더: 정타 폼이 무릎 굽힘 ~116°, EXTEND 유지 시 정타 false fault —
   "굽힘 form 에 신전기준 강요 금지"). 08-17 판독("오른다리 접힘"·"상체 꼿꼿함")은 절대
   기준(line)이 아니라 **기준-학생 델타 축**을 가리킨다 — 꼿꼿함·머리-척추는 작업 1로
   구현됐고, 다리 접힘은 기존 reference_relative 가 처리. line 재추가는 06-27 결정과 충돌.
   남은 동작(pdshape·kip-up·climb) 새 판정은 belle 이 꺼낼 때 **사진으로 소량씩**.

## ~~Pod 뜨는 날 같이 할 것~~ → **08-31 검증 완료 (belle "켜서 하자" 승인, L4 20분, $0.17)**

4런 전부 앱 동일 경로 E2E (`backend/scripts/e2e_app_path.py` — 이날 실증본 영구화):

- **코칭 수리(810eb959) 재측정 = PASS.** 75초 영상(52s+17s 연결)에서 코칭 완전 섹션
  3/3(관절별 causes 3+coachNote+fix, 수치 폴백 0). 수리 전 실패 모드는 62초 3/3 실패.
  보너스 실증: 다른 런에서 잘림이 실제 발생 — 로그 "Cerebras 응답 잘림(finish_reason=
  length, 2016자) — 완결 항목 2개만 사용" → salvage 경로가 실전 작동, dual-track 조립
  cerebras_ok 유지.
- **mode3 전후 페어 E2E = PASS(데이터 면).** 피터팬 잘못된예시→잘된예시 같은 계정 연속
  업로드: 2번째 doc 에 isFirst:false·previousAnalysisId 정확·deltaFromPrevious 생성.
  단 이 페어는 delta 0 (양쪽 stability 98 — 절대차원이 이 페어를 못 가름). 발전 캡션
  육안은 08-22 시뮬 실증이 있고 오늘은 데이터 면 실증 — 캡션 재육안은 다음 시뮬 세션.
- **postureAxes(260831-bjj) 실코칭 = PASS.** mode1 피터팬 잘못된예시 vs ref-peter-pan:
  코칭 원인 항목에 "상체가 기준보다 6도 정도 더 기울어지면서 흉추의 바른 정렬이 무너지고"
  — 새 축 프롬프트 라인이 LLM 코칭에 인과형으로 반영, 방향 정확(학생이 더 기울어짐).
  라이브 6°(joints3d) vs 오프라인 9.8°(2D align) = 소스 차이, 방향 일치.
- mode1 종합 sanity: overall 80(fault 시연 영상), 관절 8곳·확대비교 done·not_pole 미발화.
- 관측(결함 아님): GET /reference Lambda 는 10초 타임아웃으로 전 호출 500 — 단 앱은
  Firestore 직독(referenceMotions.ts)이라 **소비처 0**. 14일간 호출 이력도 0.
- GPU 실사용 확증: RTMW 75초 영상 25.5초, VRAM 1.2GB. 절차/함정 갱신은
  [[demo-only-pod-bring-up-procedure]] 해소 절 참조 (미푸시 커밋은 git bundle 로 반입).

## 6동작 페어 스윕 (08-31 2차 기동, belle "돌려라" — L4 ~30분 $0.27)

정은지 잘못된예시 vs 잘된예시, mode1 앱 동일 경로 12런 (피터팬 fault 는 오전 Run A 재사용).
결과 JSONL = quick 디렉터리 예정. 판정 기준: fault overall < correct overall.

| 동작 | fault | correct | 방향 |
|---|---|---|---|
| peter-pan | 80 | 100 | PASS |
| power-spin | 60 | 80 | PASS (단 ★아래) |
| climb | 92 | 100 | PASS |
| elbow-twist-sister | 82 | 100 | PASS |
| pdshape | 63 | 100 | PASS |
| kip-up | 100 | 100 | **FAIL — 틀린 영상 감점 0** |

- **5/6 PASS.** correct 쪽 100·100·100·100·80 — 엘리트 위양성(정은지 41점 계열)은 파워스핀
  1건 의심 외 없음.
- **kip-up FAIL → 같은 날 수리 완료 (belle "1번 수리하자", quick/260831-isk)**: 당시
  해석("감점 입구 둘 다 닫힘")은 절반만 맞았다 — 진단 확정본은 **vision 이 결함을 여전히
  변별하는데(로컬 재현: fault=split 20° 지지 1건, correct=0건) tol 20° 재적용이
  경계(dev==tol)에서 over=0 으로 지운 것** (6월엔 같은 영상 50° 추정 → -20. 모델 세대
  드리프트). 수리 = vision-sourced 편차 tol 우회(over=dev, `_criterion_deduction`),
  geometry byte-불변. fault 80 < correct 100 방향 복원, 4537/0. 상세 =
  quick/260831-kipup-diagnosis/DIAGNOSIS.md. ★라이브 E2E 재확인 = 다음 Pod 때 킵업 페어 1회.
- **power-spin correct -20 → belle × 판정 → 같은 날 수리 완료 (quick/260831-gyk)**:
  뿌리는 원근이 아니라 **창 오염**이었다 — Gemini hold ±2초 창(54,90)이 스핀 진입
  전환부(무릎 55~145°)를 평균에 섞어 135.81° 를 만듦(감점 record 와 소수점 일치 역산).
  수리 = `_select_window` 가 힌트 창 내부에서 분산-최소 부창 재선택(4줄, 튜닝 상수 0,
  동작 분기 0). 실데이터 사전 박제 4/4 PASS — correct 감점 소멸(line 0→93)·fault 방향
  보존. 4532/0. 예측 장부: "측정 오류다" 는 적중(+1승), 기전(원근 단축)은 빗나감 —
  장부에 반반으로 박제. fps 9.0 리터럴은 후속 후보로 보류.
- 부수 실측: 익명 가입 연속 생성 시 Firebase 400(남용 방지) — E2E 는 --uid 재사용으로 우회.
  엘보 잘된예시 원본 107MB > 100MB 한도로 앱 경로 정상 거절 — 1080p 재인코딩(2.7MB)으로 실행.

## belle 몫 (급하지 않음 — 분석 우선이라 뒤로)

- 콘솔 3건: Apple Developer capability · 카카오 iOS 플랫폼 · 네이버 모바일 등록
- 약관 초안 법무 검토 (민감정보·국외이전·AI학습)
- 미푸시 커밋 ~29개 — 푸시 여부 (두 번 물었으나 답 없음)

## 상태

```
★빌드 1.2.0 발행(08-31): iOS 빌드 31 = EAS FINISHED + TestFlight 자동 제출(1차 빌드 30은 EAS 서버 업로드 오류로 재시도), Android APK = https://expo.dev/artifacts/eas/2FwKCMAR-TR9upUYaYsIaMO1mHazSMwZkdAG-t8-UxE.apk (preview-android, 설치 링크). belle 실기기 확인 대기(구글 로그인 + 결과 화면 재구성 육안)
백엔드 테스트: 4537 passed / 0 failed (.venv/bin/python -m pytest tests — 시스템 python3 금지)
Pod 0대 · 잔액 $10.41 (충전됨) · SSM pod-expected=down · Lambda RUNPOD_ANALYZE_URL=종료된 h9jds14b0ryuaj (시연 전 재기동 필수)
★OTA 발행 금지 — 네이티브 모듈(google-signin) 들어감. 새 빌드 시 앱 버전 1.2.0 으로 런타임 가를 것
Metro/시뮬레이터 이 세션에서 띄움 — 남아 있으면 pkill -f "expo start"
계정 시스템: 화면 3종+Google 로그인 배선까지 완료(로컬 빌드 검증). uid 승계 실증은 belle 구글 로그인 필요
약관: Motion 전용 초안 앱 내 걸림(법무 검토 전 배너). 출시 전 sunity.ai 공개 URL 필요
```

## 함정 (이번 주 실측 — 반복 금지)

- **Firestore `analyses` 는 평가배치가 75%** (phase24eval·phase25eval·eval18·e2everify·devdiag).
  사용자 행동 분석 시 반드시 제외. limit 만 걸고 "최근"이라 부르지 말 것 — 전수 count 먼저.
- GSD STATE.md 는 날짜만 최신이고 내용이 낡을 수 있다 — phases/ 디렉터리 실측(PLAN vs SUMMARY)이 정본.
- Figma 프레임 PNG export = 프레임 배경 구워짐 → 벡터 인라인 ([[figma-frame-export-bakes-background]]).
- 새 섹션 착수 시 정리 한 바퀴 ([[section-start-housekeeping]]) — 이 파일이 그 규약의 산물.
