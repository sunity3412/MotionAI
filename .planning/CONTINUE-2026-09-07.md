# 2026-09-07 이어서 하기

> 착수점 (유일본). 인계 규율: 관측과 belle 판정 원문만, 내 해석은 "내 해석:" 표시.
> 이전 착수점 = `.planning/CONTINUE-2026-09-06.md`.

---

# ★첫 일 — 관절 좌표 학습 라인 (belle 09-06 "내일 그것부터 시작하자")

**belle 원문:** "그러면 그 학습 라인도 만드는게 좋겠네. 이따 인수인계써에 남겨줘 내일 그것부터 시작하자"

## 왜 이것인가 (09-06 에 확정된 사실)

| 학습이 쌓이면 좋아지는 것 | 안 좋아지는 것 |
|---|---|
| 결함 짚는 눈 (Qwen3-VL 8B, `backend/training/sft/run_sft.sh`) | **관절 좌표** |

관절 좌표는 **RTMW(`rtmw-x-384.onnx`) 고정 사전학습 모델**이 낸다. 플라이휠 경로에 없다.
그런데 09-06 실측에서 확대 사진이 틀리는 바닥 원인이 그 좌표다 — 도립·접힌 자세에 몰려 있다.

belle 질문("Gemma 로 학습시키면?")에 대한 답: **VLM 을 세우는 것보다 RTMW 파인튜닝이 작다.**
좌표 학습에는 "이 프레임에서 왼어깨가 여기 픽셀" 라벨이 필요한데 우리 라벨은 전부 결함 유무다.
RTMW 는 Apache-2.0 이고 파인튜닝이 되며, **틀린 프레임만 골라 고쳐주면** 된다.

## ★Step 0 — 학습 전에 두 가지를 먼저 잰다 (Pod 불필요, 오늘 데이터로 가능)

**내 해석:** 안 재고 학습부터 하면 헛일 위험이 크다. 둘 중 하나라도 통과하면 학습이 아예 필요 없다.
belle 이 "그냥 학습부터"라고 하면 Step 1 로 건너뛰면 된다.

1. **다른 기성 자세 모델 대조** — 09-06 에 "좌표 틀림"으로 나온 프레임을 다른 모델에 넣는다.
   걔도 틀리면 "어려운 프레임"이고, 걔가 맞히면 "우리 모델 문제"다. 판정이 갈린다.
2. **다중 프레임 다수결** — 같은 관절을 앞뒤 프레임 여러 장에서 잡아 다수결로 뽑는다.
   이것만으로 고쳐지면 학습 없이 끝난다.

재료 = 09-06 라이브 6문서 (uid `NdVZrpbmUbPMNMjASFwUgy8Fj9p1`, aid 는 아래 §오늘 한 것),
틀린 면 목록 = `quick/260906-n2j-stage-1-advisory/evidence/panel_center_v2_live.json` 의 `final=mismatch`.

## Step 1 — 라벨 파이프라인

틀린 프레임만 골라 사람이 관절을 찍는 경로. 전부 라벨링하지 않는다.
기존 플라이휠(`backend/training/datagen/harvest_eye.py`)과 **별개 트랙**이다 — 라벨 종류가 다르다.

## Step 2 — RTMW 파인튜닝

mmpose 계열. 게이트는 **09-06 실측 대비**로 세운다 (사전 박제할 것):
지금 v2 계기로 36패널 중 mismatch 8 · not_center_anchored 12 · ok 16.

---

# belle 09-06 판정·지시 원문

- **"근데 이렇게 판정해주는거 자체가 짜맞추는거 아냐? 그렇게 개발되고 있다는 느낌이 드는데..."**
  → 맞았다. 08-17 에 세운 규율([[belle-eye-is-the-answer-key-predict-before-asking]])을 어깨 건에
  내가 안 지켰다. 재발이라 메모리 장부에 박았고, 부족했던 지점 4개를 추가했다
  (기준을 커밋으로 박제 + 계기 하나는 모델 안 씀 + 대조군 필수 + belle 에게 고르라 하지 말고 재서 가져감).
- "걱정은 전문가가 하나 영상을 올리고 사용자가 이걸 따라서 영상을 올려서 비교를 했을 떄.... 제대로
  나올까..? 지금도 이렇게 몇 번씩 맞냐 틀리냐를 너가 물어보고 앉았는데..."
- "영상은 뭔가 그래도 잡는거 같은데 확대비교가 더 어렵냐 어째..."
- "그럼 우리 phase22로 학습이 잘 쌓이면 훗날에는 그것도 잘 되것지..?"
- **"그럼 너 말대로 하고 이런거 어때?"** → 내 권고(자동 사진은 게이트 확인분만) **승인**.
- ★**"영상에서 음성 나올 때 멈춘단 말이지? 그때 캡처하기 버튼 또는 손가락으로 영상을 멈추고
  확대할 수 있게 넣어주는건...?"** → belle 설계 제안. 아래 §승인된 방향 참조.
- "omni flash 광고를 보면 테니스 자세도 막 분석을 해주고 그러더라고.. 광고긴 하겠지만"
- "omni flash와 같은 계열인 gemma?를 통해 학습 시키는 방법은 없나?"
- **"보는 눈은 지금 그대로 하면 되는데"** → 결함 짚는 눈(플라이휠)은 손대지 않는다.
- "그러면 그 학습 라인도 만드는게 좋겠네. 이따 인수인계써에 남겨줘 내일 그것부터 시작하자"

---

# 승인된 방향 — 확대비교를 두 갈래로

belle 승인(위 "그럼 너 말대로 하고") + belle 제안(멈춤·확대·캡처)을 겹친다.

1. **자동 사진은 게이트가 부위를 확인한 것만** 내보낸다. 못 확인한 자리는 사진 대신 각도 문장.
2. **음성이 짚는 그 순간에 멈춤 + 확대 + 캡처를 사용자 손에 준다.**

**내 해석 — 2번이 강한 이유:** 지금 실패 사슬은 다섯 칸(좌표 → 프레임 선택 → 정은지 대응
프레임 → 크롭 중심 → 표시 위치)이고 다섯이 다 맞아야 사진 한 장이 맞다. belle 안은 **뒤 세 칸을
없앤다.** 남는 앞 두 칸은 점수·코칭이 쓰는 평균 기반이라 지금도 잘 되는 쪽이다.
덤 = 캡처를 강사에게 보낼 수 있다 (현장 리서치: 강사가 도입 결정권자).

**못 없애는 칸 하나:** Mode 1 은 정은지 쪽 같은 순간을 옆에 띄워야 하고 그 대응은 우리가 계산한다.
belle 09-03 "초가 다른 거 같은데"가 여기 남는다. 다만 한 장으로 못 박아 배달하는 게 아니라
사용자가 스크럽해 맞출 수 있으니 실패해도 회복 가능하다.

★**짓기 전에 잴 것:** 지금 6동작에서 **게이트 확인분만 남기면 사진이 몇 장 남는가.**
0장 되는 동작이 있으면 "사진 없는 결과 화면"이 생긴다 — 짓기 전에 알아야 한다. (09-06 미측정)

---

# 09-06 에 한 것

## 1. n2j 라이브 실증 — 판정: 된다

**판정문 = `quick/260906-n2j-stage-1-advisory/evidence/N2J-LIVE-VERDICT.md`**

사전 박제한 기준 두 개(POD-RUNBOOK §3) 다 통과:

| 기준 | 결과 |
|---|---|
| 게이트가 없던 경로가 잡히는가 | **잡힌다** — stage-1·advisory 전 경로 실행, 신규 경로에서 표시 **8건 억제** |
| 오판(정상인데 지움) 0 유지 | **유지. 오판 0** (정당 3 · 판정불가 5 · 오판 0) |

부수: gated 경로에서 **`moved=1`** 첫 사례 (j8g 라이브는 15면 중 0).
비용 실측 = **문서당 눈 호출 15.5회**, 6문서 분석 644초.

```
uid NdVZrpbmUbPMNMjASFwUgy8Fj9p1 (mode1, 09-03 과 같은 6동작 잘못된예시)
pdshape 4e232c4a73ec40a6a8deb07cbf6aa03f 67 · climb a559705f06784505bf266afe99cf2822 78
elbow   d213622a3e754d0f851a436e8bc9f208 94 · powerspin 5ae210fabec349698e1d35912b8f22e1 70
kipup   ea765def661e480292f95c5df57092a1 100 · peterpan 21b1b7ed3bc84d829d201334e64acc5b 80
```

## 2. ★계기를 먼저 고쳤다 — j8g 판정에 쓴 v1 이 결함 계기였다

`panel_center_eye_measure.py`(v1) 결함 3건, 전부 09-06 라이브 로그로 확정:

| 결함 | 근거 |
|---|---|
| 크롭 비율 `0.42` 리터럴 | 실측 `shared_frac` = **0.4000 / 0.4048 / 0.5324 / 0.5500**. 0.55 카드에서 v1 창 154px vs 게이트 창 118px = **면적 1.7배** |
| 표시(브랜드색 링) 그려진 그림을 무표시 전용 질문에 넣음 | 링만 지우자 **36면 중 4면 판정 뒤집힘** |
| "패널 정중앙 = 앵커" 가정 | 18카드 중 **10카드가 `vertex_centered=False`** — 설계상 중심이 앵커가 아니다 |

정본 계기 = `panel_center_eye_measure_v2.py` (리포 커밋됨).
**v2 결과 36패널: ok 16 · mismatch 8 · not_center_anchored 12.** (v1 은 mismatch 15 라고 했다)

## 3. quick-260906-vho — 감사 계기 결함 2건 수리

`7583e7ca` 외 6커밋. **pytest 4724 / 0 failed / 20 skipped** (기준선 4717 + 신규 7).

- advisory·legacy 카드도 `userMarked` 방출 (refMarked 는 contract §11.9 정책대로 criterion 카드만)
- 감사 `marked_flags` per-key — 방출값 우선, 없는 측만 폴백
- `card_photo_audit.panel_error_kind` 순수 함수 + 마지막 줄 `mark_errors=/center_errors=` 두 열
- 라이브 replay = `total=18 mismatched=6 unresolved=0 mark_errors=1 center_errors=5`
- `_CROP_FRAC`(0.42)·마커 반지름 `int(_OUT*0.16)` **byte 무변경** (belle 판정 대기)

## 4. 어깨 각도 문법 시험 — 사전 박제 후 측정

기준 사전 박제 = `SHOULDER-GRAMMAR-PREREGISTRATION.md` (커밋 `2906b272`, 14:04:32Z).
측정은 그 뒤에만. 계기 둘 중 M1 은 **모델을 안 쓴다**.

**M1 기하 결과 — 내 기전 주장이 깨졌다:**

| 동작 | `beyond_hip` 학생 | 기준 |
|---|---|---|
| 파워스핀 | 0.884 | 0.883 |
| pdshape | 0.748 | 0.674 |
| 피터팬 | 0.683 | 0.670 |
| 클라임 | 0.656 | 0.628 |

**한 면도 1.0 을 안 넘는다** = 선이 골반을 지나치지 않는다. 파워스핀 선 끝점의 최근접 관절도
무릎·발목이 아니라 **골반**(다리까지 145px). 판정지에 쓴 기전 "고정 길이 선이 엉덩이를 지나
다리로 넘어간다"는 **belle 이 본 그 프레임에서는 거짓**이다. 어제 조사의 "25%·19%"는
클립 전체 프레임 평균이었고 카드가 쓴 프레임은 그 안에 없었다.

**안 B 는 A 와 수치가 한 자리도 다르지 않다 — B 는 졌다.** (사전 박제에 예측해 둔 대로)

★심판(M2 + 사전 기준 적용) 결과는 `SHOULDER-GRAMMAR-TRIAL.md` 참조.
**belle 판정지 아티팩트는 기전이 틀렸으므로 고쳐 다시 올려야 한다** —
https://claude.ai/code/artifact/30f7da02-169d-4b44-a203-319e86a9db55

---

# 남은 것

- **belle 판정지 정정 배포** — 위 M1 결과로 기전 문장과 안 B 를 고칠 것. (미완)
- **게이트 확인분만 남겼을 때 동작별 사진 수** — 승인된 방향 1번을 짓기 전 필수. (미측정)
- **게이트가 통과시킨 불일치 4면** — 그 중 2면은 `vertex_centered=True` 라 두 계기가 같은 자리를
  봐야 하는데 답이 다르다. 남은 차이는 픽셀 원천(원본 프레임 vs 배달 JPEG 재압축)과 창 좌표
  클램프. **원인 미상 — 단정 금지.**
- **크롭 폭 `_CROP_FRAC=0.42` / 마커 반지름** — belle 판정 대기. 09-06 검증에서
  "원이 이웃을 덮어 오판을 만든다"는 인과는 **미입증**으로 판명. 원 크기 vs 크롭 폭을 가르는
  계기는 아직 아무도 안 돌렸다 (저장 좌표로 두 벌 렌더해 같은 눈에 묻는 것이 그 계기).
- **양다리 벌림 축** — `required_split_deg` 가 항상 None 이라 기하 경로 사문.
  belle 이 "파워스핀 가장 문제"라고 한 축.
- **엘보 트위스트 belle 지적**("팔이랑 다리랑 잘못 잰 거") 이 어깨 카드인지 골반 카드인지 미확정.

# belle 판정 대기 (먼저 꺼내지 말 것)

- `.planning/research/pdshape-report-trim-2026-09-03.md` 본문 1~13 + 시트 14~20 + 보완 운동 축소
- 피그마 node 235-506 "예상 부위 클릭 시 시트" 캡처 대조

---

# 상태 (2026-09-06 마감 실측)

```
게이트: backend pytest 4724 passed / 0 failed / 20 skipped (내가 직접 실행)
        앱 tsc 0 · node --test 219/0 (09-03 이후 앱 코드 무변경)
Pod:    0대 · pod-expected=down (ssjrcw9fynu9ja terminate 완료)
        ★Lambda RUNPOD_ANALYZE_URL 은 종료된 ssjrcw9fynu9ja — 다음 기동 시 동기화 필수
앱:     1.2.4 두 플랫폼. main 에 1.2.4 이후 앱 커밋 1개(7016e8c3) → 1.2.5 대기. OTA 금지
Gemini: Flash 정본 = gemini-3.8-flash. 판정·코칭은 3.1-pro-preview
학습:   TRAINING-DUE 발화 상태 (수집 371/400, admit 325/60) — belle 보고됨, 미실행
```

## Pod 기동 정본

`quick/260906-n2j-stage-1-advisory/evidence/POD-RUNBOOK-2026-09-06.md` §1.
09-06 에 그대로 완주했다: 번들 반입 → `pip install -r backend/runpod_inference/requirements.txt`
+ awscli google-genai rtmlib opencv-python-headless cerebras-cloud-sdk →
`pip uninstall -y onnxruntime && pip install onnxruntime-gpu==1.22` →
`source /workspace/aws_env.sh && bash /workspace/start_server.sh` → Lambda/SSM 동기화.
★로컬 감사 도구 실행에는 `FIREBASE_SA_PATH=<repo>/firebase-sa.json` + `AWS_PROFILE=sunity-motion`
+ `GEMINI_API_KEY`(SSM) 셋 다 필요하다.
