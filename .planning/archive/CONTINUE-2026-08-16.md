# 2026-08-16 이어서 하기 — 학습 선순환 완성, 다음은 재료 + 앱

> 착수점. 이 파일만 읽으면 이어진다.

## 한 줄

**학습 파이프라인이 처음으로 끝까지 돌았고, 게이트도 처음으로 통과 가능한 구조가
됐다. 판정은 FAIL 이지만 v28 의 붕괴(29/29 빈 골격)에서 탈출했다.** 남은 병목은
설계가 아니라 **재료**다.

## v29 판정 — 실측 대조

| | v28 (08-15 새벽) | **v29 (08-15 저녁)** | v13 (7월 최고) |
|---|---|---|---|
| 빈 골격 | **29/29** | **9** | 7 |
| 내용 있는 산출 | 0 | **5** | 6 |
| **faults(결함 짚기)** | **0** | **2** | 1 |
| coaching | 0자 | 1,392자 | 2,082자 |
| 산출 평균 | 130byte | **18,733byte** | 20,309 |

★결함 짚기 2건 = **역대 최고**(7월 최고 v13 이 1건). 산출 크기 144배.

**아직 FAIL 인 이유**
```
- [eval18] power-spin: 페어 레코드 파싱 불가 (fault=ok, correct=missing)  ← 결함은 짚었다
- [eval18] peter-pan / elbow-twist-sister / pdshape: fault 멤버 결함 0건
- [svg_spec] 결함 리포트 1건 중 wellformed 0건 (0.00 < 0.5)
- [determinism] real-kipup-correct: run1/run2 판정 상이
· SKIPPED (perturb 트랙 0행 — 좌표 보정 descope)   ← 오늘 수리가 작동한 증거
```
통과하려면 **4동작 전부**에서 결함을 짚어야 하는데 지금은 사실상 1동작이다.

**관측된 탄성 (belle 이 물은 "얼마나 더 필요한가"의 첫 숫자)**
```
재료 distill 79 → 171 (2.2배)  →  빈 골격 29→9 (69%↓),  faults 0→2
```

## 오늘 고친 설계 결함 (전부 커밋·푸시됨)

| 결함 | 수리 | 커밋 |
|---|---|---|
| **게이트가 안 가르친 능력을 요구** — synthetic_holdout 이 corrected_coords 를 요구하는데 그걸 가르치는 perturb 는 belle C1 로 descope. **어떤 모델도 통과 불가였다** | 학습셋 `track_counts.perturb==0` 이면 SKIP. perturb 되살리면 게이트도 복귀 | b6c5034c |
| 게이트 FAIL 을 exit=0(PASS)으로 기록 — **실패 모델 승격 직전이었다**(막은 건 무관한 예외) | 스크립트가 require-pass 코드로 종료 + 러너가 선언값과 교차검증(fail-closed) | cf97daf9 · 5756b40f |
| 운영 분석 962건이 학습에 **0행** 유입 | report 트랙 신설(harvest_reports + build_jsonl + 플라이휠) | a125fa87 |
| 학생 서술 원인 절이 전 유저에게 전파 | causeSubject 화이트리스트, 기준 서술만 고정 | 9a8f6fdc |
| perturb 118행이 결함 신호 익사 | belle C1 대로 기본 off (`SFT_WITH_PERTURB=1` 로 복귀) | b6c5034c |
| 수집이 정은지 IG 하나만 (매주 신규 0) | `--only eunji` 제거 → 유튜브 17채널. 영상 243→**303** | 2c48ec5e |
| venv 가 베이스 이미지에 결혼 (Pod 바뀌면 파손) | python 3.12 **격리** 재생성. flashinfer 근본 해소 | 95c930b9 |
| liger/boto3/imageio 레시피 누락 | 레시피 + pod_doctor 에 박제 | 69e25761 |

## ★다음 세션 우선순위

### 1. 재료 — 가장 큰 레버부터
| 레버 | 예상 수확 | 상태 |
|---|---|---|
| **기존 303편 재채굴**(1영상=1행 → 순간 단위) | 수백 행 | **미착수. 새 촬영 0으로 몇 배** |
| 채널 확대 | 채널당 20~40편 | belle 계정 지정 or 발굴 후 승인 |
| 정은지 촬영 | 기준 모션 + **앵글 짝** | 일정 미정 |
| 베타 유저 분석 | 분석마다 1행 | 관은 뚫림(동의 게이트 작동 실증) |

★주간 자동 수집은 이제 **신규 업로드만** 건진다(채널을 한 번 훑었으므로). 물이
콸콸이 아니라 똑똑 떨어진다 — 재채굴이 지금 유일하게 큰 물줄기다.

### 2. 앱 — 서비스 재개 시 반드시 선행
**앱 분석이 지금 죽어 있다.** SSM `/sunity/motion/runpod-analyze-url` 과 Lambda env 가
어제 Pod(`4tayd9c172qh53`)을 가리키는데 그 Pod 은 없다. 서비스 Pod 을 띄우면:
```
1. Pod 에서 추론 서버 기동:  cd /workspace && source aws_env.sh && bash start_server.sh
2. SSM 갱신:   aws ssm put-parameter --name /sunity/motion/runpod-analyze-url \
                 --value https://<POD_ID>-8000.proxy.runpod.net/analyze --overwrite
3. Lambda env 갱신 (pipeline 함수 RUNPOD_ANALYZE_URL)
```

### 3. 미착수로 남은 것
- **22-08 서빙 교체** — 게이트 통과 모델이 생겨도 앱은 안 바뀐다. 이게 있어야 고리가 닫힌다
- **카메라 앵글 축** — 학습 데이터에 각도 라벨이 없다. 정은지 촬영 때 짝을 확보 안 하면 되돌아간다
- **판정이 영상을 안 본다** — `judge_report` 는 리포트 텍스트만 받는다. 교사가 없는 결함을
  지어내도 글이 매끄러우면 통과. 대조 실험 준비 완료:
  `backend/research/spikes/spike_judge_sees_video.py` (라벨링과 동시 실행 금지 — 429)
- **kip-up 커버리지 1건** — report 트랙이 균등 게이트에서 25→11 로 트림된 이유

## 환경 상태

- **Pod `ydlljgv6amjtr7`** (RTX 5090 32GB, EU-RO-1). belle 이 정리 예정.
  - `/workspace/train_venv312` — python 3.12 격리, 검증 완료(11모듈 + vLLM 진입점 + CUDA)
  - `/workspace/train_venv` (구 3.11) **삭제됨** — 레시피로 재생성:
    `TRAIN_VENV_ISOLATED=1 bash backend/training/sft/setup_train_venv.sh`
  - 볼륨 **97G/150G** (153G 에서 56G 확보: v28 병합본 17G · 구 venv 23G · GEN3C conda 33G)
- **플라이휠**: launchd `com.sunity.flywheel` 매주 월 10:07. 수집 전체 + 눈 수확 + **분석 수확** 포함
- 승격 원장: entries 2 / **current=null** (아직 통과 모델 없음)

## 오늘 비용

Gemini 호출 112회 · 신규 라벨 56건 · 학습 8,501초 · Pod 1대 종일

## 규율 리마인드

- 진행 시간은 **시각이 아니라 남은 길이**로 말할 것 ("16:00" → "약 1시간")
- 단계별로 쪼개서 추정할 것 — 학습 앞뒤(S3 동기화 20분·병합 10분)를 빼먹어 계속 짧았다
- **결정은 실행자가, 허락이 필요한 것만 물을 것** (belle: "할까요 말까요 결정을 물어보냐")
- 게이트 판정 전 **병합본 크기 확인** — 쿼터 초과로 config 2개(980K)만 남은 껍데기를
  판정할 뻔했다(정상 17G)
