# 2026-08-17 이어서 하기 — belle 판정 3건 대기 + 일러스트 남은 5장

> 착수점. 이 파일만 읽으면 이어진다. 어제(08-16) 커밋 28개.

## 한 줄

**앱 분석을 살렸고(죽어 있었다), 발굴 구조를 뒤집었고(눈이 후보→수치가 검증), 일러스트
실패 원인이 "모델 운"이 아니라 배선 누락이었음을 밝혀 9/19 → 14/19 로 올렸다.**
지금 막혀 있는 것은 전부 **belle 눈 판정**이다.

---

## ★belle 이 볼 것 3건 (전부 준비 완료, 재촉 금지)

| # | 무엇을 판정하나 | 재료 |
|---|---|---|
| 1 | **일러스트 재생성분** — 이전/이후 대조 20장. 미통과 5장은 사유(자세 충실) 명시됨 | `/Users/Shared/sunity-illustration-260816-ill2/` + `판정요청.md`<br>보드 = `.planning/quick/260816-ill2-illustration-framing-wiring/board.html` |
| 2 | **눈-우선 발굴 승격 8건** — 특히 엘보 `r00cand04`(서술 3개 전부 수치 확인됨) | `/Users/Shared/sunity-pole-eyefirst-260816/` (짝 사진 20장 + 판정요청.md) |
| 3 | **두 클라임이 같은 동작인가** — belle "나중에 물어볼게" 라 한 것 | `/Users/Shared/sunity-climb-compare-260816/두_클라임_비교_위=05-22기준_아래=06-17.jpg` |

침묵 3동작 확인(`/Users/Shared/sunity-silence-check-260816/`)은 **닫힌 것으로 본다** —
belle 이 실제 영상으로 더 정확히 판독해줬고(엘보 = 다리 방향·오른팔, 피터팬 = 버티기 힘·
다리가 폴에 붙음) 그게 구조 변경으로 이어졌다.

---

## 어제 확정된 것

### 1. 앱 분석 경로 복구 — 죽어 있었다
Pod 종료 후 SSM·Lambda 가 없어진 주소를 가리켜 **분석이 전량 실패**하고 있었다
(`_delegate_to_runpod` 는 위임 실패 시 **폴백이 없다** — `pipeline/app.py:184`).
새 Pod → SSM·Lambda 재동기 → 운영 경로 그대로 영상 1건 태워 **1분 43초 status=done** 실증.

### 2. v29 판독 — 08-15 인계 표를 정정해야 한다
"산출 130byte→18,733byte(144배)"는 **개선이 아니라 폭주**였다(토큰 상한까지 반복 생성).
같은 잣대로 재니 **v29 ≈ v13(7월)**: 폭주 16→15 · 빈 골격 7→9 · 결함 1→2.
**재료 2.2배가 사실상 제자리.** v28(전부 빈 골격)은 이상치라 기준선으로 쓰면 안 된다.
→ 게이트 파서 수리 완료(잘린 산출의 안쪽 조각을 리포트로 인정하던 결함, 24/5 → **14/15**).

### 3. 발굴 구조 뒤집기 (belle 승인)
```
전:  수치가 주장 → 눈이 검증      (축이 굽힘/폄 하나, 좌표 깨지면 무력)
후:  눈이 후보를 내고 → 수치가 검증
```
근거: 엘보 좌표 품질 최악(모순 7.8%·저신뢰 22%)에서 후보 12/12 탈락. 좌표 없이 짝 이미지만
물으니 **belle 사전 판독 3/3 일치**. 실행 결과 서술 55건 → 승격 8 / 기각 8 / **수치화불가 39(71%)**.
★71% 가 수치로 못 재는 것 = 뒤집은 구조의 약한 고리. belle 판정으로만 걸러진다.

### 4. ref-climb 교체 — 짝이 어긋나 있었다
기준 11개가 **두 배치**다(Firestore createTime): 05-22 극초반 5개(따뜻한 스튜디오) / 06-11 세트 6개(흰 "TA" 스튜디오).
climb 만 기준은 1차, 학생 영상은 06-17 촬영이라 **다른 클라임**이었다.
```
교체 전  climb correct angle 26(차단 임계 25) · climb fault angle 0 → NotPoleMotionError 차단
교체 후  climb correct angle 100            · climb fault angle 86 → 차단 해소
```
다른 기준 10개 SHA-256·영상 무변경. **구 자산 전량 복구 가능**(아래 롤백 참조).

### 5. 일러스트 — 진단이 바뀌었다
19장 전수 9항목 선별: **9 통과 / 10 반려, 반려는 전부 ②자세·구도 하나에서만.**
나머지 8항목은 19/19 통과(08-09 수리가 겨냥한 민머리·상의 없음·폴 2개·목·기형은 실제로 잡혔다).
**원인 = 배선 누락** — `cropBox`·`cropNote`·`orientation` 이 모델에 전달 안 됨. 대신
고정 문구 `The figure must FILL the frame` 이 장별 의도("head is outside the frame")와 **정면 모순**.
배선 잇고 재생성 → **5 통과 / 5 미통과**, 전체 **14/19**.
★`app/assets/illustrations/` 는 **아직 교체 안 함** — belle 판정 후 별도 사이클.

### 6. 그 외
- **RunPod S3 API 개통** — 볼륨을 Pod 없이 읽는다(v28·v29 원문을 GPU 0원에 회수·박제)
- climb·combo P35 편입 → 발굴 대상 **5 → 7**
- P35 스크립트 **캐시 기본값 fresh** (원본 바뀌어도 옛 캐시 물던 구멍)
- 메모리 8건 신규/갱신 + MEMORY.md 압축(21.3 → 18.7KB, 링크 195개 보존)

---

## 미해결 (착수 가능)

| 항목 | 상태 |
|---|---|
| **긴 영상에서 코칭이 수치 폴백** | `coach_writer.py:298` JSON 미종결. 18초 0건 vs 62초 포함 **3/3 실패**. 폴백이 조용히 나가 발생률을 아무도 모른다. **미수리** |
| **일러스트 미통과 5장** | `combo--leg`·`kip-up--leg`·`kip-up--shoulder`·`pdshape--arm`·`pdshape--leg`. 배선으론 안 풀리는 자세 문제. 후보 다장 생성 후 게이트로 거르기 / 자세 조건부 생성 |
| **반복 페널티(repetition_penalty) A/B** | v29 의 지배적 실패가 **반복 폭주**다(`real-kipup-fault` 가 `svg_*` 키 43종을 **64번 반복**하다 토큰 상한에서 잘림 = 39KB). 반복 페널티는 이미 쓴 토큰에 벌점을 줘 그 현상을 막는 표준 노브이고, `run_sft_gates.sh` 에 `REPETITION_PENALTY` 로 **이미 있는데 1.0(꺼짐) 고정**이다. **같은 조건에서 1.0 vs 1.05 두 번 돌려 폭주 15건이 사라지는지** 본다. 사라지면 한 달간 '재료 부족'으로 본 진단이 사실은 디코딩 설정 하나였다는 뜻이고, 안 사라지면 학습 쪽(199행·64스텝 과적합)으로 내려간다. **드라이버 580+ Pod 필요**(현 Pod 550, 볼륨 venv torch 는 cu130). **다음 재학습 직전 필수 선행** |
| 킵업 벌림각 | 표시할 단일 관절 좌표가 없어 눈에게 물어보지도 못함. 새 질문 형식 필요 |
| 22-08 서빙 교체 | 승격 모델이 없어 지금 해도 실익 없음 |
| 앵글 축 | 학습 데이터에 각도 라벨 없음. 정은지 촬영 때 짝 확보 안 하면 되돌아간다 |
| 촬영 필요 | `foxtop`·`foxtop-split`·`invert`·`sideway-spin` — **학생 영상이 아예 없다**(기준만). 발굴 불가 |

---

## 학습 트랙 (08-16 에서 이월 — 어제는 손대지 않았다)

어제는 앱 복구·발굴·일러스트를 했고 **학습 쪽은 파서 수리 말고는 진척 없음.** 아래는 08-16
인계에서 그대로 넘어온 것이라 계속 이월한다. 없애지 말 것.

### 재료 레버 (병목이 여기라고 08-15 에 판단했으나 — 아래 ★주의)

| 레버 | 예상 수확 | 상태 |
|---|---|---|
| **기존 303편 재채굴**(1영상=1행 → 순간 단위) | 수백 행 | **미착수. 새 촬영 0으로 몇 배 — 가장 큰 레버** |
| 채널 확대 | 채널당 20~40편 | belle 계정 지정 or 발굴 후 승인 |
| 정은지 촬영 | 기준 모션 + **앵글 짝** | 일정 미정 |
| 베타 유저 분석 | 분석마다 1행 | 관은 뚫림(동의 게이트 작동 실증) |

★주간 자동 수집은 이제 **신규 업로드만** 건진다(채널을 한 번 훑었으므로). 물이 콸콸이 아니라
똑똑 떨어진다 — **재채굴이 지금 유일하게 큰 물줄기**다.

★★**단, "병목 = 재료"라는 08-15 판단은 08-16 실측으로 흔들렸다.** v29 ≈ v13 이고 재료를
2.2배 늘린 효과가 사실상 없었다. 지배적 실패는 **반복 폭주**(15~16/29)이고 한 달째 안 변했다.
**재료를 더 붓기 전에 반복 페널티 A/B 를 먼저 재는 것이 맞다**(위 미해결 표). 안 그러면
수집·라벨링·GPU 를 태우고 같은 자리에서 FAIL 한다.

### 학습 파이프라인 미착수 항목

- **판정이 영상을 안 본다** — `judge_report` 는 교사가 만든 **리포트 텍스트만** 받는다
  (`contents=[prompt]`, 영상 없음). 교사가 없는 결함을 지어내도 **글이 매끄러우면 통과**한다.
  대조 실험 준비 완료: `backend/research/spikes/spike_judge_sees_video.py`
  (텍스트 판정 vs 영상 판정으로 같은 리포트를 태워 갈리는 건수를 본다).
  ★**라벨링과 동시 실행 금지 — 429 로 진행 중 사이클을 죽인다.**
- **kip-up 커버리지 1건** — report 트랙이 균등 게이트에서 **25 → 11 로 트림**된 이유.
  동작 편중을 막는 게이트가 kip-up 을 1건까지 깎았다.
- **22-08 서빙 교체** — 게이트 통과 모델이 생겨도 앱은 안 바뀐다. 이게 있어야 고리가 닫힌다.
- **카메라 앵글 축** — 학습 데이터에 각도 라벨이 없다. 정은지 촬영 때 짝을 확보 안 하면 되돌아간다.

### 학습 데이터 현황

- S3 학습셋(`training/phase22/jsonl/`)은 **v29 학습 이후로 이미 넘어가 있다** — v29 는
  distill 171 + text 28 로 돌았고, 그 뒤 **report 트랙 11행**이 붙어 현재 **245행**
  (distill 171 · text 28 · eye 35 · report 11, perturb 0).
- report 트랙 = 운영 분석 리포트를 학습 재료로 끌어오는 경로(08-15 신설, `harvest_reports`
  + `build_jsonl` + 플라이휠). 그 전까지 운영 분석 962건이 학습에 **0행** 유입이었다.
- perturb 는 belle C1 대로 **기본 off**(`SFT_WITH_PERTURB=1` 로 복귀). 게이트의
  synthetic_holdout 도 `track_counts.perturb==0` 이면 SKIP — 되살리면 게이트도 복귀한다.
- 승격 원장 `backend/training/sft/promotion_ledger.json` — entries 2(v28·v29), **current=null**.

---

## 환경

- **Pod `t4i1p9k2hnvb0q`** (RTX 4090 24GB, EU-RO-1, 드라이버 550) — **앱 서비스 중, 추론 서버 PID 4823. 재기동 금지.**
  `ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -i ~/.ssh/id_ed25519 -p 15167 root@213.173.102.172`
- 앱 분석 배선: SSM `/sunity/motion/runpod-analyze-url` + Lambda `sunity-motion-pilot-pipeline` env 둘 다 이 Pod 을 가리킴
- **플라이휠**: launchd `com.sunity.flywheel` **매주 월 10:07 → 오늘(08-17) 10:07 자동 실행**. 빠뜨린 회차 없음
- 재학습: **아직 때 아님**(영상 303/400, 분석 admit 25/60). `.planning/TRAINING-DUE.md` 생기면 belle 에게 즉시 알릴 것
- 승격 원장 `current=null` (통과 모델 없음)
- pytest 기준선 = **4398 passed / 59 failed / 26 skipped**. "59"는 통과 수가 아니라 **기존 무관 실패 수**

### ref-climb 롤백 (필요 시)
```
S3        aws s3 cp s3://sunity-motion-pilot-videos/reference/_archive/ref-climb-pre-quick260816r7k.mp4 \
                    s3://sunity-motion-pilot-videos/reference/ref-climb.mp4
Firestore .planning/debug/backups/reference-11-preC-20260816-170029.json 으로 복원
          (S3 사본: s3://sunity-motion-pilot-videos/backups/ 같은 이름)
```

---

## ★어제 데인 함정 (전부 실측, 반복 금지)

- **`start_server.sh` 35행이 `pkill -f "uvicorn.*server:app"`** — env 소싱은 **3~34행만**. 넘기면 앱 추론 서버가 죽는다
- 소싱 전 `export LD_LIBRARY_PATH="${LD_LIBRARY_PATH:-}"` — env 블록이 `$LD_LIBRARY_PATH` 를 이어붙여서 `set -u` 면 unbound 로 죽는다
- **`| tail` 로 파이프하면 종료코드가 tail 것**이 된다. 실패가 rc=0 으로 보이고, 백그라운드 완료 알림도 실제 종료와 어긋난다. **프로세스 소멸을 조건으로 걸 것**(`until ! ps -p PID; do sleep 10; done`)
- **캐시가 원본보다 오래 산다** — `ref.mp4` 만 지우고 추출 프레임(`rf15`)을 안 지워 climb align 이 옛 기준으로 정렬됐다(refFrames 256 vs 119). P35 는 이제 fresh 기본값이지만 다른 곳에도 같은 형태가 있을 수 있다
- **파일이 쓰이는 중에 읽지 말 것** — 판정 파일을 두 번 읽었는데 값이 달랐다. 병행 실행이 원인
- **서브에이전트가 폴링 대기로 턴을 끝내는 일이 잦다** — 완료 보고인 줄 알고 넘어가지 말고 산출물로 실제 상태를 재라. Pod 실행은 스크립트를 scratchpad 에 쓰게 하고 내가 돌리는 분담이 잘 돌아갔다
- **거부된 호출(권한 분류기)은 다시 밀어붙이지 말 것.** belle 이 모바일 원격이라 긴 명령은 따옴표가 깨진다 — Pod 작업은 내가 직접 돌린다

---

## 규율 리마인드

- 진행 시간은 시각이 아니라 **남은 길이**로
- **결정은 실행자가, 허락이 필요한 것만 물을 것**
- 판정 재료는 **원본 크기로** 볼 것 — 축소본 검수가 일러스트 실패의 뿌리였다
- **게이트가 요구하는 것이 우리가 가르친 것인지** 매번 확인. 어제 v29 에서 고친 그 잘못을 일러스트 게이트에서 똑같이 재현했다(크롭을 벌함)
