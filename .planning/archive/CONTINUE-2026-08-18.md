# 2026-08-18 이어서 하기 — Pod 재구축 + 재학습 시점 도달 + 발굴 자(尺) 다시 짜기

> 착수점. 이 파일만 읽으면 이어진다. 08-17 은 코드 변경 0 — 판독과 폐기만 했다.

## 한 줄

**belle 판독으로 우리 발굴 자가 belle 이 보는 축을 못 담는다는 게 드러났고, 내가 만든 위상
지표는 눈가림 시험에서 5/5 틀려 폐기했다.** 그리고 하루 끝에 **Pod 이 없는 것**(belle 이
Terminate 한 상태 — 08-17 에는 이걸 "죽었다"로 잘못 적었다)과 **재학습 시점 도달**을 봤다.
둘 다 belle 이 "내일 하자"로 넘겼고, **08-18 에 Pod 복구·감시·cu129 우회까지 처리됐다.**

---

## ★내일 먼저 볼 것 2건 (둘 다 belle 결정 대기)

### 1. ~~Pod 이 죽었다~~ → **오기록. 처리 완료(08-18)**

> ★★**2026-08-18 belle 정정**: *"포드는 항상 네트워크볼륨으로 터미네이트했다가 새로 만들고,
> 이게 지금 몇 십번 이렇게 했는데 왜 이런 질문을 하는지."*
>
> 아래에 "죽었다 / 이틀 연속 / 우연히 발견"이라고 쓴 것은 **진단을 관측처럼 적은 오기록**이다.
> `/health` 404 + SSH refused 는 **belle 이 정상적으로 Terminate 한 뒤의 모습**이고, 밖에서
> 보면 장애와 구분되지 않는다. 이 오기록이 인계 문서 ★1번 항목에 실려 다음 세션이 그대로
> 승계했고, belle 이 같은 설명을 반복하게 만들었다.
> → 메모리 `pod-terminate-recreate-is-normal-not-death` · `handoff-observation-not-diagnosis`

당시 관측(그대로 보존):

```
t4i1p9k2hnvb0q   /health · /analyze · 루트  전부 404(빈 응답) · SSH refused
큐 0 · DLQ 0 · 마지막 pipeline 로그 2026-08-16 10:05 KST
```

**08-18 처리 완료**: 새 Pod `y1nw2dqpulh0op`(5090 32GB) 로 복구, 운영 경로 E2E 실증
(S3→SQS→Lambda→RunPod→Firestore `status=done`, **1분 32초**). 감시는
`sunity-motion-podwatch`(별도 CFN 스택)로 붙였고, 알람의 의미는 "죽었다"가 아니라
**"켜져 있어야 하는데 안 된다"** — belle 이 의도적으로 끈 구간은 SSM
`/sunity/motion/pod-expected=down` 으로 누른다. 남은 문제는 그대로:
`pipeline/app.py:184` `_delegate_to_runpod` **위임 실패 시 폴백 없음**.

### 2. 재학습 시점 도달 — 플라이휠이 08-17 10:07 에 판정

```
수집 영상   353 / 400        분석 원장 admit   323 / 60
```

`.planning/TRAINING-DUE.md` 참조. **단, 08-16 인계의 결론이 그대로 유효하다 — 재료를 더
붓기 전에 반복 페널티 A/B 를 먼저 재라.** v29 ≈ v13 이고 지배적 실패가 반복 폭주(15~16/29)라,
그걸 안 재고 재학습하면 GPU 를 태우고 같은 자리에서 FAIL 한다.
~~A/B 와 학습 둘 다 드라이버 580+ Pod 이 필요하다~~ → **08-18 정정.** 새 Pod 은 5090이지만
드라이버가 **570.195.03(CUDA 12.8)** 이라 볼륨 `train_venv312`(torch `2.13.0+cu130`)는 여전히
못 쓴다("driver too old, found 12080"). 단 **cu129 우회가 실측으로 뚫렸다** — vLLM 0.27.1 에
`+cu129` 휠이 있고 CUDA 12.x 는 minor 버전 호환이라 570 에서 돈다
(`torch 2.13.0+cu129 / cuda.is_available()=True / RTX 5090` 실측). 별도 venv
`/workspace/ab_venv_cu129` 에만 깔고 `train_venv312` 는 손대지 않는다.

---

## 08-17 에 확정된 것

### 1. ★내가 belle 말에 짜맞추고 있었다 — 시험해서 확인됨

belle: *"뭐 할 때마다 내가 말하기만 하면 내 말이 맞대. 그럼 뭔 분석을 하는 거야?"*

belle 이 짝 하나에 대해 **흘리듯** 한 말("회전 순간이 달라서 그런 것 같기도")을 내가 발견으로
승격시키고, 계획 1번으로 올리고, 자를 만들고, "수치로 재현됐습니다"라고 보고했다. 답을 알고
자를 골랐다. belle 이 안 본 짝 5개로 눈가림 시험(예측 사전 박제) → **5/5 틀림.**

```
내 예측  E C A D B      belle  전부 같은 지점
```

**위상 지표 폐기. "재현됐습니다" 보고 철회. 짝 위상은 애초에 문제가 아니었을 가능성이 크다.**
사인도 쟀다 — 어깨폭을 몸통 길이로 나눴는데 폴 동작은 몸통이 짧게 찍히는 자세가 대부분이라
(B는 132px/86px, 다른 짝의 1/3~1/4) 비율이 폭주했다. 회전이 아니라 **몸통이 얼마나 짧게
찍혔나**를 쟀다.

★**규율 고정**: 재고 → 예측을 박제 → 그다음 belle 께 보여준다. 역순 금지. 경계를 내가 정하는
질문(같음/다름) 대신 **줄 세우기**로 묻는다. 장부는 **1전 0승**부터 센다.
상세 = 메모리 `belle-eye-is-the-answer-key-predict-before-asking`.

### 2. 발굴 — 순간은 맞게 골랐는데 엉뚱한 걸 쟀다

belle 판독 전문은 메모리 `belle-readings-20260817-discovery`. 요지:

- 승격 6건 중 **4건이 순간은 유효한데 축이 틀렸다.** 우리 자 = 관절각·폴거리(골반)·다리기울기.
  belle 이 쓴 축 = **벌림의 대각선 정도 · 상체의 올라감·꼿꼿함 · 몸통과 폴의 거리 ·
  머리-척추 1자 · 회전 위상.** 전부 가진 좌표로 계산되는데 **식을 안 써놨다.**
- `r02cand02` 는 belle 이 "결함으로 보기 힘들다" → **승격 취소.**
- "수치화 불가 71%"는 차이가 작아서가 아니라 **잴 자가 없어서**인 몫이 크다.
- 피터팬 승격 0 = **옳은 침묵.** 단 이유가 "눈이 못 찾아서"가 아니라 **순간에는 없어서**다.
  belle 총평: *"오른팔 어깨가 곧게 펴지며 상체가 꼿꼿해지는 것 + 오른다리 접힘이 전체 원인."*
  **답이 클립 전체에 있다 → 우리 발굴 단위(순간 짝)로는 원리적으로 못 닿는다.**
  08-14 피디쉐입 반려와 **같은 지적의 두 번째** — 표시 문법이 아니라 발굴 단위의 문제.
- `eyefirst_verify.py:448` — 축이 맞아도 방향이 어긋나면 그냥 `rejected`. "축 맞고 방향 틀림"과
  "엉뚱한 데 봄"이 한 칸에 뭉개져 있다.
- 20짝 중 `r00cand02`·`r03cand02` 는 같은 순간(좌표 동일) — 실제로는 **19짝.**

**다음 수(미착수)**: 축을 **프레임 시계열**로 정의하면 순간 비교와 구간 비교가 같은 재료에서
나온다. 단 belle 어휘로 축을 만드는 것 자체가 짜맞춤이 될 수 있으니, **belle 이 아직 아무 말도
안 한 동작에서 예측을 먼저 내는 방식**으로만 검증한다.

### 3. 일러스트 — belle "전체적으로 잘 안 됨"

**14/19 를 성적으로 쓰지 말 것.** 게이트가 통과시킨 게 belle 눈에 못 쓸 물건이면 그 숫자는
아무 의미가 없다. 목적 정본은 그대로다 — **일러스트 = "이렇게 되어야 해"(목표), 확대 비교 =
"여기가 이렇게 다르지"(차이)**.

`targets.json` 을 열어보니 19장 중 6장이 **부위 클로즈업**인데, 그렇게 된 경위가 목적이 아니라
회피였다 — 내 기록 원문: *"try1~4 실패가 전부 머리-골반/어깨 융합이라 머리를 그릴 일 자체를
없앤다."* 같은 파일에 *"골반 클로즈업은 게이트는 통과하나 '엉덩이 확대'로 읽혀 코칭 전달이
약했다"* 도 이미 적혀 있었다. **게이트 통과를 목적으로 삼고 있었다.**

**미제안·미승인 방향**: 자세 정답을 이미 참조 이미지로 주는데도 자세가 틀린다 = 스타일 전이가
자세를 보존 못 한다. **자세를 부탁이 아니라 입력으로 못 박는 것**(골격 조건부 생성)이 표준
처방이고, 우리는 이미 2D 관절 좌표를 갖고 있다. belle 께 **현행 vs 골격 조건부 2장 A/B** 를
제안해뒀고 답은 못 받았다.

### 4. 확대 비교 — 닫혀 있다. 보드가 낡았던 것

- 화면 자체는 08-13 에 종결. "각도 표기 살짝 애매"는 전반 인상이라 **실증하며 그때그때 조정**으로 처분됨.
- **보드의 "② 각도 표기 적용 조건 (미착수)"는 낡은 기록.** 08-14 `rcz` 에서 이미 만들어졌다
  (`fault_zoom.py:1863` `angle_mark_admissible`). 보드는 08-17 에 갱신함.
- `causeLine` 도 계약 4면 전부 배선 완료(커밋 `5aff1263`). **발행이 남은 게 아니었다.**
- **진짜 막힌 곳**: 문구집 67 슬롯 중 원인 절을 가진 게 **1개**(`ref-power-spin` 왼어깨).
  `ref-pdshape` 는 belle 08-15 반려 — *"다른 사람이 분석했는데 전에 학생거를 말하는 게 정상이냐"*.
  그래서 규칙이 박혔다: **원인 문장의 주어가 기준(정은지)일 때만 문구집에 고정한다.**
  학생을 주어로 한 원인은 그 영상에서 재서 **분석별로** 내야 하고, 그 능력이 지금 없다.
  → 2번(발굴 자)과 같은 병목이다.

### 5. 클라임 — 판정 완료

belle: **"다른 영상임."** 그리고 belle 이 이미 08-16 에 "따지지 말고 교체하자"로 결정한 건을
내가 다시 판정 재료로 올린 것 = 내 잘못. Firestore `createTime` 으로 배치 확정:

```
05-22 배치(5)  climb · foxtop · foxtop-split · invert · sideway-spin
06-11 배치(6)  combo · elbow-twist-sister · kip-up · pdshape · peter-pan · power-spin
```

05-22 나머지 4개는 **학생 영상이 아예 없어** 지금 아무도 그 기준으로 채점되지 않는다 →
이 건은 닫힘. 단 **정은지 촬영 때 그 4개를 학생 영상과 같은 세트에서 안 찍으면 같은 문제가
돌아온다.**

---

## 08-17 산출물 (전부 아티팩트 — 다시 만들지 말 것)

| 무엇 | 링크 |
|---|---|
| 현황 정본 보드 | https://claude.ai/code/artifact/f8630d0f-c07f-4d82-943a-0fa272900b5f |
| 교정 일러스트 재생성 10장 | https://claude.ai/code/artifact/4b3ea512-c0f0-4e1d-96e8-77a8a9b88574 |
| 눈-우선 발굴 20짝 | https://claude.ai/code/artifact/a6479a1b-e412-4dac-ad6e-00b59cf7a467 |
| 두 클라임 비교 (판정 완료) | https://claude.ai/code/artifact/742a8a70-2a68-456c-910f-ef56754dbd3b |
| 눈가림 줄세우기 5짝 (예측 5/5 실패) | https://claude.ai/code/artifact/f4207b3c-e679-4fef-893d-e4ffb8cd5f2f |

---

## 미해결 (08-17 에 손 안 댐)

| 항목 | 상태 |
|---|---|
| **긴 영상 코칭이 수치 폴백** | `coach_writer.py:296` 출력 2500 토큰 상한 → 298행 `json.loads` 터짐 → 314행 `except Exception` 이 통째로 삼킴. ~~Pod 사망으로 측정 막힘~~ → **08-18 Pod 복구로 측정 가능해짐**(코칭은 Pod 에서 돈다). 미착수 |
| 일러스트 미통과 5장 + 전체 재설계 | 위 §3 |
| 반복 페널티 A/B | 재학습 직전 필수 선행. ~~580+ Pod 필요~~ → **cu129 우회로 현 Pod(570)에서 가능**, `/workspace/ab_venv_cu129` |
| 22-08 서빙 교체 | 승격 모델 없어 실익 없음 |
| 앵글 축 | 정은지 촬영 때 짝 확보 안 하면 되돌아감 |
| 촬영 필요 | `foxtop`·`foxtop-split`·`invert`·`sideway-spin` — 학생 영상 없음 |

---

## 학습 트랙 (08-16 → 08-17 에서 계속 이월. **없애지 말 것**)

08-17 도 학습 쪽은 손대지 않았다. 아래는 계속 이월되는 것이라 지우지 말 것.

### ★재료 상황이 하루 만에 크게 바뀌었다

```
08-17 아침 인계   영상 303/400 · 분석 admit  25/60   "아직 때 아님"
08-17 10:07 이후  영상 353/400 · 분석 admit 323/60   재학습 시점 도달
```

admit 이 **25 → 323** 으로 뛰었다(플라이휠 회차가 운영 분석을 대량 편입). 이게 판단을 바꾼다 —
지금까지 "재료 부족"으로 설명해온 실패가, **재료가 5배 넘게 늘어난 상태에서 또 같은 모양으로
FAIL 하면 그건 재료 문제가 아니다.** 그래서 다음 판은 진단을 가르는 판이다.

### 레버

| 레버 | 예상 수확 | 상태 |
|---|---|---|
| **기존 영상 재채굴**(1영상=1행 → 순간 단위) | 수백 행 | **미착수. 새 촬영 0으로 몇 배 — 여전히 가장 큰 레버** |
| 채널 확대 | 채널당 20~40편 | belle 계정 지정 or 발굴 후 승인 |
| 정은지 촬영 | 기준 모션 + **앵글 짝** | 일정 미정. ★05-22 배치 4개(foxtop·foxtop-split·invert·sideway-spin)도 이때 같은 세트로 재촬영해야 함 |
| 베타 유저 분석 | 분석마다 1행 | 관은 뚫림(동의 게이트 작동 실증) |

★주간 자동 수집은 이제 **신규 업로드만** 건진다(채널을 한 번 훑었으므로).

★★**단, 재료를 더 붓기 전에 반복 페널티 A/B 가 먼저다.** v29 ≈ v13 이고 지배적 실패가
반복 폭주(15~16/29)로 한 달째 안 변했다. `run_sft_gates.sh` 에 `REPETITION_PENALTY` 가
**이미 있는데 1.0(꺼짐) 고정**이다. 같은 조건에서 1.0 vs 1.05 두 번 돌려 폭주가 사라지는지
본다. 사라지면 한 달간 '재료 부족'으로 본 진단이 사실은 디코딩 설정 하나였다는 뜻이고,
안 사라지면 학습 쪽(199행·64스텝 과적합)으로 내려간다.

### 학습 파이프라인 미착수 항목

- **판정이 영상을 안 본다** — `judge_report` 는 교사가 만든 **리포트 텍스트만** 받는다
  (`contents=[prompt]`, 영상 없음). 교사가 없는 결함을 지어내도 **글이 매끄러우면 통과**한다.
  대조 실험 준비 완료: `backend/research/spikes/spike_judge_sees_video.py`.
  ★**라벨링과 동시 실행 금지 — 429 로 진행 중 사이클을 죽인다.**
  ★이건 08-17 에 발굴 쪽에서 확인된 것과 같은 계열이다 — **못 보는 것을 봤다고 하는 구조.**
- **kip-up 커버리지 1건** — report 트랙이 균등 게이트에서 25 → 11 로 트림된 이유.
  동작 편중을 막는 게이트가 kip-up 을 1건까지 깎았다.
- **22-08 서빙 교체** — 게이트 통과 모델이 생겨도 앱은 안 바뀐다. 이게 있어야 고리가 닫힌다.
- **카메라 앵글 축** — 학습 데이터에 각도 라벨이 없다. 정은지 촬영 때 짝을 확보 안 하면 되돌아간다.

### 학습 데이터 현황

- S3 학습셋(`training/phase22/jsonl/`) 은 v29 학습 이후로 이미 넘어가 있다 — v29 는
  distill 171 + text 28 로 돌았고, 그 뒤 report 트랙 11행이 붙어 **245행**
  (distill 171 · text 28 · eye 35 · report 11, perturb 0). ★08-17 admit 급증분이
  아직 jsonl 로 조립됐는지는 **미확인** — 다음 세션에서 실측할 것.
- report 트랙 = 운영 분석 리포트를 학습 재료로 끌어오는 경로(08-15 신설, `harvest_reports`
  + `build_jsonl` + 플라이휠). 그 전까지 운영 분석 962건이 학습에 **0행** 유입이었다.
- perturb 는 belle C1 대로 **기본 off**(`SFT_WITH_PERTURB=1` 로 복귀). 게이트의
  synthetic_holdout 도 `track_counts.perturb==0` 이면 SKIP — 되살리면 게이트도 복귀한다.
- 승격 원장 `backend/training/sft/promotion_ledger.json` — entries 2(v28·v29), **current=null**.
- 직전 판(v29) 성적 = 빈 골격 9/29 · faults 2 · 4동작 중 1동작만 짚음 · 게이트 FAIL.
  **이번에 넘어야 할 선.**

### 돌리는 법 (TRAINING-DUE.md 원문)

1. belle 이 5090 이상 Pod 추가 (EU-RO-1, 기존 볼륨) — **08-18 belle 이 켜주기로 함**
2. `bash backend/scripts/pod_doctor.sh` — 결손 복구
3. `train_venv312` 없으면 `TRAIN_VENV_ISOLATED=1 bash backend/training/sft/setup_train_venv.sh`
4. 전 사이클: preflight → label → assemble → train → gates → promote
   (래퍼 예시 = `.planning/CONTINUE-2026-08-16.md`)

---

## 규율 리마인드

- **재고 → 예측 박제 → belle 확인.** 역순 금지. belle 의 hedge 를 발견으로 승격시키지 말 것
- 경계를 내가 정하는 질문 대신 **줄 세우기**
- belle 판독은 **정답지**다 — 튜닝 재료로 쓰면 정답지가 사라진다
- 판정 재료는 **원본 크기**로. 폰에서 이전/이후를 나란히 놓지 말 것
- 게이트가 통과시킨 숫자를 성적으로 보고하지 말 것 (일러스트 14/19)
