# 2026-08-19 이어서 하기 — 일러스트 "어떻게" 확정 + 재학습 train 단계 재개

> 착수점. 이 파일만 읽으면 이어진다. 08-18 은 긴 하루였다 — Pod 2대, A/B, 감시, Gemini 갱신,
> 일러스트 방향 전환. **인계 규율**: 아래는 관측과 belle 판정 원문. 내 해석은 "내 해석:" 으로 표시.

## 한 줄

**일러스트의 역할이 바뀌었다(belle: "어떻게"). 첫 실물이 문법까지는 통과했고 그림은 미승인.
재학습은 라벨·조립까지 끝나고 train 단계에서 환경 오류로 죽었다 — 라벨 재과금 없이 이어 돌리면 된다.**

---

## ★내일 먼저 볼 것

### 1. Pod — 없다 (belle 08-18 밤 Terminate). 볼륨 존속

- 새 Pod 뜨면 복구 절차 그대로: `bootstrap_full.sh` → `pod_doctor.sh` → `start_server.sh`
  → SSM `/sunity/motion/runpod-analyze-url` + Lambda env 둘 다 갱신 → **SSM `pod-expected=up`**.
- ★Pod 드라이버가 CUDA 13.0(580+)이면 볼륨 `train_venv312` 그대로 돈다. 12.x(570)면
  `run_sft_gates.sh` 의 자동 가드(cu129 우회 3종)가 걸린다 — 학습(swift)은 여전히 불가.
- 감시: `sunity-motion-podwatch` 가동 중, 수신 sunity3412@gmail.com (실증 완료). 지금은
  `pod-expected=down` 이라 조용하다. **새 Pod 띄우면 up 으로 바꿀 것** — 안 바꾸면 감시가 없는 것과 같다.

### 2. 재학습 v30 — train 단계에서 죽음 (환경 오류, 코드 아님)

```
[label]     완료  351/351 · 신규 53편 · accepted 286 (10,961s ≈ 3h, Gemini 과금은 여기)
[assemble]  완료  canonical 교체 + S3 upload (training/phase22/jsonl/)
[train]     ★죽음  ValueError: You cannot perform fine-tuning on purely quantized models.
                   Please attach trainable adapters on top of the quantized model
```

- 로그: `s3://sunity-motion-pilot-videos/training/phase22/cycle_logs/cycle_260818.log`
- **내 해석**: 이 Pod 에서 처음 학습을 돌렸는데 `train_venv312` 의 transformers(5.12)가
  swift 의 QLoRA 어댑터 부착 순서와 어긋난다. v29(08-15)는 다른 venv(train_venv)로 돌았다.
  transformers `validate_quantization_for_training` 이 어댑터 없이 4bit 모델을 막는다.
- **다음 수**: `bash training/sft/run_retrain_cycle.sh train` 부터(라벨·조립 스킵, 과금 0).
  단 그 전에 `run_sft.sh` 의 swift 인자에 LoRA 어댑터가 실제로 붙는지(`--train_type lora`
  또는 `--lora_rank`) 확인. 안 붙으면 이 venv 의 swift/transformers 조합을 맞춰야 한다.
- 재학습 재개 전 확인: 볼륨 `phase22_sft_out/v30-20260818-122707/` 는 args.json 만 있는 빈 껍데기.

### 3. 일러스트 — 방향은 확정, 그림은 미승인

**belle 판정 원문(08-18)**:
- 역할: "지금처럼 확대 비교에서 확인할 수 있는 장면을 그림으로 다시 표현하는 게 맞을까?" → A(어떻게)로 결정
- 킵업 "어떻게" 4장 중 **"가"(잔상 + 화살표) 통과** — 장부 3전 1승(첫 승)
- 문법: 화살표는 잔상 발에서 시작 / "50° 정도 더 벌리세요"(방향) / "좁다" 금지 / 잔상 = 학생 각도
- 앱 실물(정은지 그림 + 회전 복사 잔상): **"그림이 이게 아닌데... 아까 잔상 잘 그렸었는데"** → 미승인, 원복

**지금 상태**: 앱은 08-09 그림 그대로(`325c4ac8`). 오버레이 코드·앵커는 남아 있고 장면 표만
가리키지 않는다(fail-closed). 승인되면 `illustrationScene.ts` 의 kip-up leg asset 한 줄.

**열린 질문 (belle 답 대기)**:
- "잘 그렸었다"는 잔상이 **"가"(화살표 있던 것)** 인지 **"3번"(화살표 없던 ghost-noarrow2)** 인지
- 두 길 중 어느 것: ① 모델 잔상 1장 고정(모든 학생 같은 잔상) / ② 모델 잔상을 20°/40°/60°
  3단계로 만들어 학생 값에 가장 가까운 것을 앱이 고름(내 권고 ②)
- 그림 재생성은 Gemini 라벨링과 겹치지 않을 때

**★규율(오늘 박음)**: 문법 승인 ≠ 그림 승인. 그림은 belle 이 그 물건을 보고 "이거다" 한 뒤에만 배선.
메모리 `grammar-approval-is-not-asset-approval`.

---

## 08-18 에 끝난 것

| 무엇 | 결과 | 어디 |
|---|---|---|
| Pod 복구 ×2 (y1nw2dqpulh0op → 잔액 소진 → mssm8k9bcw9sft) | 앱 E2E 1분 32초 / 1분 34초 | 메모리 `current-pod-mssm8k9bcw9sft` (미작성 — 아래 참조) |
| Pod 헬스 감시 | `sunity-motion-podwatch` 배포, 알람→SNS→수신 실증, `pod-expected` 스위치 | `backend/infra/podwatch.yaml`, README |
| 반복 페널티 A/B | rp 1.0→1.05: 폭주 5/37→0, 파싱실패 11/29→0, **게이트 FAIL 동일**(4동작 결함 0건). 원인은 디코딩 아니라 학습 쪽 | `s3://…/training/phase22/ab_rp_260818/` |
| Gemini Flash 3.5→3.7 | 중앙 config + 하드코딩 9곳 제거 + 3.5 화이트리스트 제외(fail-loud). Pod 실호출 로그로 확인 | `359b9de5` |
| 드라이버-툴체인 불일치 자동 가드 3종 | Triton ptxas / FA2 PTX / FlashInfer — 불일치 시만 발동 | `run_sft_gates.sh` |
| 일러스트 목표 카드 (그림+문장 결합) | 시뮬 실렌더 확인. OTA 미발행 | `87ffe458` |
| 일러스트 "어떻게" 오버레이 코드 | 잔상(회전 복사)·화살표·표기·방법 캡션. 실렌더 확인. **에셋은 원복** | `52d88c95` + `325c4ac8` |

## 08-18 에 틀린 것 (다음 세션이 같은 실수 안 하게)

- 인계 문서의 "Pod 이 죽었다"를 검증 없이 승계 → belle 정상 운영(Terminate)을 장애로 취급.
  세 번째 원인(잔액 소진)도 있었다. → `pod-terminate-recreate-is-normal-not-death`, `handoff-observation-not-diagnosis`
- 알림 메일을 belle6466(git 이메일)로 → 운영 계정은 sunity3412
- 예측 장부: 일러스트 belle 눈 **4전 1승** (pdshape A/B 틀림, "나" 대신 "가", 1번 대신 3번)
- SVG 시안을 안 열어보고 발행 → 잔상 안 보임·화살표 포물선. 이후 이미지 합성으로 눈 확인 후 발행
- 승인 안 난 그림을 앱 에셋에 걸고 커밋 → 원복

## 메모리 미작성 1건 (다음 세션이 쓸 것)
- `current-pod-mssm8k9bcw9sft` — 5090, 드라이버 580.126.20, 213.173.103.4:44986, 08-18 밤 Terminate. 위 §1 절차로 대체 가능.
