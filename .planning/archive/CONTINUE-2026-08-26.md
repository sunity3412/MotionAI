# 2026-08-26 이어서 하기 — v33 재학습 완주·판정의 날

> 착수점. 인계 규율: 관측과 belle 판정 원문만, 내 해석은 "내 해석:" 표시.

## 한 줄

**v33 재학습이 관문 4개(눈 크롭 fail-closed → 0-trainable resume → 볼륨 쿼터 →
gates 무출력 즉사)를 뚫고 완주했고, 판정은 gates 양 모드 FAIL — 내용 침묵(결함
짚기 0/29)은 v32 와 동일, 형식 축은 오히려 후퇴(exact 1.0→0.62). NOT PROMOTED.
Pod 은 종료했고 원장·수리 3건 전부 push 됨.**

## v33 실측 결과 (원장 = promotion_ledger afa4073 · bakeoff json 은 볼륨 eval_out)

| 축 | v32 | v33 | 판정 |
|---|---|---|---|
| 결함 짚기 (faults>0 / 29) | 0 | **0** | 침묵 그대로 |
| json exact_match | 1.0 | **0.62** (18/29) | 후퇴 |
| json_cer_mean | 0.0 | 0.38 | 후퇴 |
| temporal_acc | 1.0 | 1.0 | 유지 |
| eval18 페어 | — | 4동작 correct-영상 파싱 불가 (power-spin·peter-pan·elbow-twist-sister·pdshape) | FAIL 사유 |

- 학습셋 279행 = distill 197 + text 32 + **eye 37(첫 탑승)** + report 13, perturb 0.
  train 완주 9264s(72스텝, A100 SXM 80GB), 어댑터 3.8148% Trainable(v32 동일).
- **데이터 가설 판정**: belle 프레임 "22번 실패이유 = 데이터 부족" — 이번 증분
  (admit 286 accepted, eye 트랙 신규)으로는 침묵이 안 풀렸다. v32↔v33 차이는
  데이터뿐(하이퍼파라미터·프롬프트 동일)이라 형식 후퇴도 데이터 변화에 귀속된다.
  내 해석: eye/report 트랙 혼입이 형식 출력을 흔들었을 가능성 — A/B(--with-eye
  제외) 없이는 단정 불가. 다음 처방 후보: (a) eye 제외 A/B 로 후퇴 원인 분리,
  (b) LoRA 강도/에폭 축(08-23 침묵 원인 실측이 가리킨 미측정 축), (c) 재료 계속 증식.

## 오늘 뚫은 관문 4개 (수리 커밋 포함 — 다음 사이클은 안 막힘)

1. **eye 크롭 fail-closed**: `harvest_eye --upload-media --run` 이 해제 유일 경로
   (S3 results/ 해시 대조 포함). admit 46건 전부 반출 완료, manifest uploaded 세움
   (afa4073). 다음 사이클부터 이 관문 없음.
2. **0-trainable 즉사**: 원인은 venv 가 아니라 `SFT_RESUME=auto` 가 어댑터 없는
   `checkpoint-*-merged` 를 집는 것 (08-18 v30 사망도 같은 병 — 로그 대조 실증).
   수리 = glob 에서 -merged 제외 (0e48d663). v33 은 SFT_RESUME=none 으로 완주.
3. **볼륨 쿼터 초과(150GB)**: step 36 저장 중 사망. 미승격 11개 디렉터리 삭제(belle
   `!` 실행)로 63G→22G 확보. v32 완주본은 보존. 현재 볼륨 ~115G/150G.
4. **gates 무출력 즉사**: ptxas-blackwell 프로브가 venv 에 없는 바이너리 실행 →
   pipefail+set -e 가 stderr 억제 상태로 죽임. cuda-devel 이미지에서만 발화.
   수리 = `|| true` (920f229).

## ★venv 지형 (다음 Pod 필독)

- **train = `/workspace/train_venv_cu124`** (python3.11, torch 2.6.0+cu124) — 완주 실증.
  이미지 `runpod/pytorch:2.4.0-py3.11-cuda12.4.1-devel` 과 정합.
- **gates(vLLM) = `/workspace/train_venv312`** (vllm 0.27.1, torch 2.13+cu130) —
  **드라이버 CUDA 13.0 필요** (이번 pod 580.159.03 OK). vllm 은 이 venv 에만 있다.
- venv312 의 train 은 미실증 (0-trainable 관측은 resume 원인으로 판명 — venv 탓 아님).
- ab_venv_cu129 는 인터프리터 깨짐(구 pod 잔재).

## 상태

- Pod: **없음** (cw9lz0wi1wornk terminate 확인, pods=[]). `pod-expected=down` 그대로.
- RunPod 잔액 **$7.02** (사이클 비용 ~$12.3). v34 돌리려면 충전 필요.
- Cerebras 200 확인 여전 미완 — 다음 실분석(서빙 Pod) 때 종결.
- SSM runpod-analyze-url 은 여전히 죽은 주소(v38) — 다음 서빙 Pod 때 갱신.
- 별건 부채(불변): 라이브 SAM 스택 낙후 — `sam deploy` 금지.
