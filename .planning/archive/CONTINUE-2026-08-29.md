# 2026-08-29 이어서 하기 — 계정 시스템(Figma) 착수

> 착수점. 인계 규율: 관측과 belle 판정 원문만, 내 해석은 "내 해석:" 표시.

## 한 줄

**v34(표적 데이터 첫 학습)는 완주했으나 gates FAIL — belle 결정 "학습은 데이터 더
쌓고 재시도". 대신 실분석을 복구·실증했고, 낡은 코드/테스트를 전수 정리해 백엔드가
0 failed 가 됐다. 다음 선두 과제는 belle 이 지적한 **계정 시스템(Figma 설계 미구현)**.**

## ★내일 먼저 할 것 = 로그인/가입 (Figma 따라)

belle 결정 (2026-08-28):
- **"게스트 유지 + 로그인 추가"** — 게스트로 바로 쓰는 경로는 그대로, 원하는 사람만 연동.
- **"디자인은 피그마를 따라줘"** — 카피(텍스트)는 **belle 과 논의 후** 확정. 그때 한 곳만
  고치면 되도록 문구를 상수로 뺄 것.
- **"간편로그인은 같은 dev 폴더 funding 쪽에서 이미 하고 있어서 설계는 편할 거야"**
- **"아이디는 같은 아이디를 쓰면 돼, 비즈니스 계정이니까"** (= 별도 심사 불필요, 플랫폼만 추가)

### Figma 실측 (fileKey `jrdI7kp245HkPfLB0nclsz`, 미구현 화면)

```
인트로 1:142      "시작하기" + "이미 계정이 있으신가요? 로그인하기"
가입   1:961      카카오 / 네이버 / Google / Apple + 이메일로 가입하기 + 약관 안내
STEP01 1:987      이메일 + 비밀번호 (8자 이상, 영문+숫자+특수문자)
STEP02 1:1072     약관 동의 — [필수] 만14세·이용약관·개인정보 수집
                              [선택] 마케팅 수신·위치정보
그 외   찾기 1:374 · 알림 1:522 · 입문 1:612 · 중급 1:629 · 신규 1:675 · 이력 1:694
```
★**가입 후 프로필/레벨 입력 단계는 없다**(확인함) — 범위가 생각보다 작다.

### 재사용 가능 / 불가 (funding `/Users/kimtaesung/Dev/Sunityfunding`)

| | 내용 |
|---|---|
| **가능** | 콘솔 등록이 이미 있다(비즈니스 계정). `Auth.java` 의 `provider`/`providerId`/`authType`/`isMarketingAgreed` 계약 + `AuthController`/`AuthService` 검증 흐름 = 참고본 |
| **불가** | funding = Next.js 웹 · `window.opener.postMessage` 팝업 · Spring Boot(EC2). Motion = RN/Expo · 딥링크 · Lambda · Firebase Auth. **코드 이식 안 된다.** CLAUDE.md §3 상 funding 서버 호출도 금지 |

### 설계 (내 해석 — belle 승인 전)

```
Google / Apple  → Firebase Auth 기본 제공, 서버 불필요
카카오 / 네이버  → expo-auth-session 딥링크로 code → 신규 Lambda POST /auth/social
                 → provider 토큰 검증 → Firebase createCustomToken → signInWithCustomToken
게스트였으면     → linkWithCredential 로 익명 계정 승계 (기록 유지)
```

### belle 콘솔 작업 (내가 대신 못 함, 심사 없음)

기존 등록은 전부 **웹(sunity.ai)** 기준 — 같은 앱에 모바일 플랫폼만 추가하면 된다.
- 카카오: 플랫폼 iOS 등록 (번들 `com.sunity.aicoach` + 커스텀 스킴)
- 네이버: 모바일 앱 등록 (iOS URL 스킴)
- Apple: **App ID 에 Sign in with Apple 활성화** (웹 Service ID 와 별개)
- Google: Firebase 콘솔 iOS 앱 등록 시 대부분 자동

★콘솔 전이라도 **Figma 화면(인트로/가입/약관) + Google·Apple 까지는 착수 가능**.

## 08-28 한 일 (전부 실행으로 검증함)

### v34 학습 — NOT PROMOTED (커밋 4e0ade41)
라벨 6/6 accepted(전부 fault 시연) · train 완주(2h49m, LR 1e-4) · **gates FAIL**.
겨냥한 침묵 4동작(power-spin·peter-pan·elbow-twist-sister·pdshape)이 **여전히 결함 0건** —
"잔여 침묵 = 표적 데이터 공백" 가설이 이번 실측으로 지지받지 못했다.
새 축: 결함 리포트 8건은 나오는데 svg_spec wellformed 0/8(`target_angle_deg`=None).
상세·다음 측정 후보 3건 = `.planning/quick/260828-v34-targeted-data/SUMMARY.md`.

### 실분석 복구 + E2E 실증 (Pod 은 이후 종료)
08-22 부터 **6일간 앱 분석이 죽어 있었다** — `RUNPOD_ANALYZE_URL` 이 죽은 Pod 주소.
L4($0.49/hr)로 복구 → 앱과 동일 경로 E2E:
`comparison.mode=mode1` · `scoringBasis=reference_motion` · "정은지 측정 각도 기준 비교" ·
similarity 100 · overall 100 · dims `{angle:100, stability:69}`.
★**Pod 없으면 분석은 실패한다(실질 폴백 없음)** — 기동 절차는 메모리
[[demo-only-pod-bring-up-procedure]] 에 5분 절차로 박제.

### 코드 수리 (커밋 6765ad93 / 82d23333 / 875bd3fa / 13e21717)
- **폴 검출기 OpenCV 5.0 호환** — `HoughLinesP` 반환이 4.x `(N,1,4)` → 5.0 `(N,4)` 로
  바뀌어 언팩이 터졌다. 치명 아니라 `vertical_fallback` 으로 **조용히 degrade** 중이었고,
  그 바람에 폴 x위치(`midpoints_x_norm`)가 유실됐다 — belle 이 08-17 판독에서 지목한
  "몸통-폴 거리"·"엘보 폴 근접도"의 입력이다.
- **금지 모델 폴백 + raw 모델 문자열 전수 제거** — `gemini_moment_extractor` 가
  `gemini-2.5-pro`(08-18 이후 ALLOWED_MODELS 밖)에 굳어 있었다. `technique_cache` 는
  `gemini-3.1-pro`(suffix 누락 금지형)를 **캐시 키**로 써서 **모델을 올려도 캐시가
  무효화되지 않았다**. 전 상수를 `gemini/config.py` 승계로 교체.

### 테스트 전수 정리 (커밋 2704a32d / d42d1e14 / e8664140 / 3300222d / 24a64d8c)
```
세션 시작: 4192 passed / 62 failed / 8 수집오류
현재:      4491 passed /  0 failed / 0 수집오류
```
프로덕션 로직 변경 0. 원인 세 갈래 — (1) 결정이 코드엔 반영되고 테스트엔 안 됨
(2) 이름 모호성(`conftest`/`fixtures` 중복, 상대 경로) (3) `importlib.reload` 미복구.
★reload 는 모듈 객체를 두고 **안의 이름만 새 객체로 재바인딩**한다 — `sys.modules`
복구로는 안 되고 `__dict__` 스냅샷을 되돌려야 한다.

## 락인된 실측 (재론 불필요)

| 사실 | 근거 |
|---|---|
| 로드맵 미완 페이즈 = Phase 22 하나 | ROADMAP 전수. 앱은 화면 13개 완비·TODO 0건 |
| ~~앱 개발 남은 것 없음~~ **틀림** | belle 지적 — **Figma 계정 시스템 미구현**. 내가 Figma(=UI 정본)를 안 보고 답했다 |
| omni 는 비전 판정에 못 쓴다 | `generateContent` 400 "only supports Interactions API". **영상 생성 모델**이다 |
| omni 1.1 flash 는 도달 가능(08-28 릴리스) | `v1beta/interactions` `{model,input}` → `completed` 실증. **카메라 앵글 트랙 차단 해소** — belle: "앱 안정적일 때 나중에" |
| Flash 정본 = `gemini-3.7-flash` | ListModels 실조회. 3.5 는 화이트리스트에서 빠짐 |
| SFT peak 31.04GiB → 학습 GPU 하한 48GB | 08-26 LR 실험 로그 |
| 서빙 VRAM 1.3GB → L4 로 충분 | 08-28 실측 |

## 미해결 (정직)

- ~~`visionVeto: skipped_error`~~ — **08-28 해소 (커밋 2125798e). Pod 없이 로컬로 닫음.**
  ★원인은 제품 결함이 아니라 **내 E2E 가 Firestore 문서를 S3 PUT 뒤에 써서 doc 에
  mode 가 없던 것**이었다. 정상 경로 실측 = `candidate_verdict`, 3/3 호출,
  samplingComplete=True. 다만 진짜 약점이 드러나 수리함 — mode 가 비면 세 가드를
  전부 비껴가 `skipped_error` 로 뭉쳐져 "어댑터 장애"와 구분이 안 됐다.
  `unknown_mode` 상태 신설 + 가드 + 경고 로그 + 게이트 블록리스트 반영.
- **Phase 22 v34 FAIL** — belle: 데이터 더 쌓고 재시도.
- **RTMW clean weight** — 출시 hard gate. **파일럿·TestFlight 는 범위 밖**(belle 2026-06-02
  조건부 승급). 과금·공개 출시 전 필요. (a) mmpose 공식 상업 가능 가중치 문의 우선.
  ※08-28 웹 검색으로는 새 가중치 근거 못 찾음(약한 확인 — 확실한 경로는 OpenMMLab 직접 문의).
- **튜토리얼 카피 충돌** — Figma "AI가 함께하는 폼 코칭!"(마케팅) vs 현재 "강사님을
  대신하진 않아요"(기대설정, 현장 리서치 반영). 코드 주석은 "Figma 카피 우선"이라 적혀
  있지만 현재 카피는 **강사 도입 저항 방어** 목적. 배경도 충돌(Figma 다크레드 vs
  CLAUDE.md "다크 배경 금지"). **belle 판단 필요**.

## 상태 (그대로 두고 감)

```
Pod 0대 · 잔액 $4.61 (시연 전 충전 필요)
Lambda RUNPOD_ANALYZE_URL = 죽은 주소(8rny5z90npl1kg) — 시연 때 Pod 띄우며 갱신
SSM pod-expected = down · launchd podhunt/cyclewatch = unload (자동 생성 없음)
미커밋 = 세션 시작 시점과 동일 (내 작업물 전부 커밋)
```

## 별건 (불변)

- 라이브 SAM 스택 낙후 — `sam deploy` 금지, 함수 직접 갱신.
- 일러스트 재도입 제안 금지.
- E2E 는 앱 순서를 지킬 것: upload-url → **Firestore 문서 먼저** → S3 PUT.
  순서 틀리면 조용히 mode3 로 떨어진다(08-28 에 두 번 오진).
