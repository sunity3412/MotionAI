# 2026-09-06 이어서 하기

> 착수점 (유일본). 인계 규율: 관측과 belle 판정 원문만, 내 해석은 "내 해석:" 표시.
> 이전 착수점 = `.planning/archive/CONTINUE-2026-09-04.md`.

---

# ★첫 일 (belle 09-06 저녁 "pod 켜서 해") — Pod 은 켜져 있다, 기동 마저 하고 도립 붕괴 조사

**런북 = `.planning/quick/260906-n2j-stage-1-advisory/evidence/POD-RUNBOOK-2026-09-06.md`**
→ **§0-1 부터 읽을 것.** Pod 을 새로 만들지 말 것.

```
Pod ssjrcw9fynu9ja · RTX 4090 · $0.74/hr · ssh -p 10642 root@213.173.98.96
https://ssjrcw9fynu9ja-8000.proxy.runpod.net · pod-expected=up
반입 완료 /workspace/delta.bundle  · 남은 것 = 부트스트랩·checkout·서버기동·Lambda 동기화
```

**중단 이유 (belle 에게 보고함):** Pod 문제가 아니다. **내 로컬 셸**이
`fork failed: resource temporarily unavailable` 로 죽었다 — 09-06 오후에 워크플로 2개
(이미지 판정 에이전트 8개) + 배경 python 을 겹쳐 돌려 맥이 프로세스 한도에 걸린 것.
전경·배경·서브에이전트 셸 전부 동일, TaskStop 으로도 복구 안 됨. Read/Write 는 정상.
**교훈: 동시 에이전트 수를 줄일 것.**

---

## ★★★ 오후 정정 — "좌표가 틀리다"는 절반만 맞았다

**정정본 = `.planning/quick/260906-n2j-stage-1-advisory/evidence/PHOTO-ROOT-CAUSE.md`**

카드가 쓴 프레임에 **저장 좌표 12개를 이름표와 함께** 그려 3중 독립 판정한 결과:

| 학생 패널 불일치 5장 | 판정 |
|---|---|
| pdshape 왼어깨 (**belle 이 지적한 그 카드**) | **좌표 맞음** (3/3 만장일치, 정규상관 정합 ncc 0.61 로 카드=이 프레임 42% 크롭 확인) |
| 엘보 오른어깨 / 파워스핀 왼팔꿈치 | **좌표 맞음** |
| pdshape 오른무릎 / 오른팔꿈치 | 좌표 틀림 (도립 자세에서 사지 사슬이 몸통으로 붕괴) |

**얼굴 사진으로 보이는 진짜 이유 (좌표 아님):**
- 참고·legacy 카드 크롭이 **고정 0.42**(`fault_zoom.py:50`) — 감점 카드에만 부위 크기
  파생 밴드(0.40~0.55)가 걸려 있다. belle 카드는 **참고 카드**다.
- 표시 동그라미 반지름이 **`int(_OUT*0.16)` = 57px 고정**(`fault_zoom.py:1761`) —
  **패널 폭의 32%**. 도립 자세에서 어깨 옆이 얼굴이라 원 안에 얼굴이 통째로 들어온다.

**내 오전 계기가 틀린 이유:** 18% 좁은 크롭은 맥락이 없어 좌표가 맞아도 옆 부위가 화면을
채우면 "틀림"으로 읽힌다. 아래 오전 표(36패널 중 14 틀림)는 **과다 계상이다.**

**버린 것:** "눈에게 관절 위치를 물어 사진 중심을 옮기기" — 틀린 8장 중 2장만 고치고
**멀쩡한 대조군 3장 중 1장을 망가뜨렸다.** 채택 안 함 (`scratchpad/eye_locate.log`).

---

## (오전 기록 — 위 정정본이 우선한다) 관절 좌표 자체가 틀린다

착수점이 적었던 "신뢰도 미달 좌표로 crop"도, 내가 그 다음에 찾은 "붕괴 프레임"도 **원인이 아니었다.**

**실측 (09-06 라이브 6문서 36패널).** 카드 크롭(짧은 변 42%)이 아니라 **짧은 변 18%** 로 관절 좌표에
바짝 붙여 자른 뒤 기계 눈에 "여기 무슨 부위냐"를 물었다:

| | 패널 |
|---|---|
| 좌표 OK | 15 |
| **좌표 틀림** | **14** |
| 못 읽음 | 7 |

**신뢰도도 붕괴 검사도 이걸 예측하지 못한다** — conf 0.872 판독불가 / 0.749 무릎 자리에 폴 잡은 **손** /
0.743 팔꿈치 자리에 **허벅지** / 0.637 어깨 자리에 거꾸로 된 **얼굴**. 뒤의 셋은 육안으로도 명백하다
(`quick/260906-f4j-.../evidence/keypoint_truth_eyecheck.png`).

**이것이 설명하는 것:**
- f4j(붕괴 프레임 배제)가 라이브에서 제대로 돌았는데도(moved 11·앵커 붕괴 14/42→4/36) **눈 불일치가
  안 줄었다** — 감점 카드 4장 → 4장. 프레임을 옮겨도 그 프레임 좌표도 틀리기 때문.
- 카드 감사가 `ok` 로 통과시킨 카드에도 틀린 좌표가 있다(클라임 오른무릎: 양쪽 다 팔).
  **넓은 크롭(42%)이 틀린 좌표를 가린다.**
- belle 이 며칠째 같은 걸 보는 이유. 프레임 선택·붕괴 배제·크롭 폭·마커 위치는 전부 좌표 위의 층이고,
  바닥이 40% 틀리면 위층을 고쳐도 안 낫는다.

원본: `.planning/quick/260906-f4j-gated-card-collapse-frame-skip/evidence/ROOT-CAUSE.md`

## 오늘 들어간 수리 2건

| quick | 내용 | 커밋 |
|---|---|---|
| 260906-f4j | 멈춤 상속(gated) 경로에 붕괴 프레임 배제 — 앵커 붕괴 시 ±4 안 성한 프레임으로 이동, fail-open, 로그 1행 | `bf250cc6` |
| 260906-j8g | **앵커 부위 확인 게이트** — 표시 전 양측 앵커를 좁은 크롭(18%)으로 눈에 확인, 실패 시 최대 2프레임 재확인, 그래도 실패면 **표시만 생략** | `e58a2edb` |
| 260906-n2j | 같은 게이트를 **stage-1·advisory** 두 경로에도 — 카드별 `anchor_check` 콜백 + 공용 팩토리 1벌, 재확인 프레임 미사용 | `105fa15e` |

셋 다 **카드 삭제 권한 없음** (belle 09-03 규칙 1 "멈춤 구간마다 사진 1장, 검사는 표시만 정한다").
게이트: backend pytest **4717 passed / 0 failed / 20 skipped** (내가 직접 실행).

## j8g 라이브 실증 — 끝났다 (09-06 오후)

Pod 기동 → 09-03 4영상 재분석(`verifyj8g0906`) → 게이트 로그 회수 → Pod 종료.

**게이트 로그 실측** (3문서, 카드 9, 면 15, 눈 46회):

```
ee577422 (pdshape) cards=4 sides=7 eye_calls=24 pass=5 moved=0 suppressed=2
f6f6ea51 (climb)   cards=1 sides=2 eye_calls=9  pass=1 moved=0 suppressed=1
c4f944f0 (elbow)   cards=4 sides=6 eye_calls=13 pass=6 moved=0 suppressed=0
```

`moved = 0 / 15` — **재확인 프레임이 카드를 구제한 적이 한 번도 없다.** (n2j 는 그래서 재확인을 안 쓴다.)

**판정은 세 번째 계기로 했다.** 감사(`audit_card_photos`, 카드 전체 42%)는 불일치 6/14 로
거의 안 움직였는데, 감사와 게이트가 서로 다른 답을 내는 면이 있었다. 그래서 **배달된 카드
패널을 게이트와 같은 시야로 다시 잘라**(패널 정중앙 42.9% = 원본 짧은변 18%) 5회 최빈으로
물었다 — 계측기 `quick/260906-n2j-.../evidence/panel_center_eye_measure.py`.

| 28패널 | |
|---|---|
| 중심이 제목 부위 | 18 |
| **중심이 딴 부위** | **9** |
| 못 읽음 | 1 |

불일치 9장을 게이트 처분별로:

| 게이트 처분 | 불일치 | 뜻 |
|---|---|---|
| `suppressed` | **3** | 잡았다 — 틀린 표시가 배달 안 됨 |
| `pass` | **3** | 놓쳤다 |
| 미실행 | **3** | 게이트가 그 경로에 없었다 → n2j 가 닫음 |

**게이트가 표시를 지운 3면은 전부 실제 불일치 — 정상인데 지운 것 0.**
belle 09-04 "얼굴에 동그라미"(pdshape 오른팔꿈치 학생)는 두 계기 모두 `head` 판독,
라이브에서 표시가 사라졌다.

**내 해석:** 실패 방향이 안전한 쪽(맞는 표시를 안 지우고 놓칠 뿐)이라 게이트를 남기고
범위를 넓혔다. 다만 **게이트가 고치는 것은 "틀린 표시"이지 "틀린 사진"이 아니다** —
사진을 맞게 하려면 맞는 좌표가 필요하고 그것은 자세 모델의 일이다. 그래서 감사 지표
(불일치 수)는 안 내려간다(`mark_elsewhere` → `no_mark_and_center_elsewhere` 로 바뀔 뿐).

원본: `quick/260906-n2j-stage-1-advisory/evidence/GATE-VERDICT.md`

## ★다음 Pod 때 첫 일 = n2j 라이브 실증 (같은 절차)

1. 09-03 6영상 재분석 (uid 새로) — **n2j 는 아직 라이브 미검증**(다음 Pod 번들에 실린다)
2. 로그 회수: `fault_zoom_anchor_check path=stage1|advisory` + `..._summary scope=render`
3. 배달 패널 정중앙 재측정 (`panel_center_eye_measure.py <dump-dir> <out.json> 5`)
   → **판정 기준: 미실행 3장이 잡히는가, 오판(정상인데 지움) 0 이 유지되는가**
4. 지연 실측 — 게이트가 카드 3경로 전부에 붙어 문서당 눈 호출이 는다 (j8g 만으로 문서당 13~24회)

## 남은 것 (범위 자르지 말 것 — belle 09-06 "뭐가 또 수리 범위 밖이여")

- **어깨 각도 문법** — 파워스핀 왼어깨가 정은지 패널에서 `knee` 로 읽힌다(앵커는 성함).
  꼭짓점이 어깨가 아니라 **겨드랑이**(`_ARMPIT_T=0.15`, 어깨→골반 15%)이고 두 선이
  `ANGLE_BAKE_MAP["shoulder"] = ("elbow", "hip")` — 팔 하나, **골반 하나**. belle 09-05
  "겨드랑이랑 다리를 잘못 재고 있어"와 구조가 일치. 승인 문법(7R#2)이라 바꾸려면 belle 판정 필요.
- ~~**참고(advisory) 카드**~~ — n2j 로 게이트 부착 완료(라이브 미검증). 09-06 실측에서 참고 카드
  불일치 3장이 전부 게이트 미도달이었다.
- **게이트가 통과시킨 불일치 3** — 원본 프레임에선 허용 부위, 배달 패널에선 아님
  (`c4f944f0` 왼골반/기준·오른어깨/학생, `ee577422` 오른무릎/기준). 같은 좌표·다른 픽셀
  (원본 vs 축소 크롭)인지 눈의 경계 불안정(armpit↔abdomen)인지 **아직 모른다 — 단정 금지.**
  판정법 = 다음 Pod 에서 배달 패널로 다시 재기.
- **감사에 '표시 오류 / 중심 오류' 열 분리** — 지금은 둘 다 불일치로 세서 표시 게이트의
  효과가 지표에 안 잡힌다.
- **크롭 폭** — `_CROP_FRAC=0.42` 는 몸 절반이 들어와 "확대"가 아니다. 감사도 이 때문에 틀린 좌표를
  통과시킨다. 아직 안 건드림.

## 자가검출 (belle 09-05 "안 맞는게 들어갔습니다를 먼저 말해라")

- 기준 리포트를 `db.document('referenceMotions/…')` 로 읽으면 빈 dict 가 오고 `is_collapsed_frame` 이
  fail-open(False) 이라 **"정은지 붕괴 0"이라는 가짜 통과**가 났었다. 정본은
  `fa.get_reference_motion(rid)['referenceKeypointReport']`. 고친 뒤 실제 값은 4/18.
- 09-03 표본으로 "advisory 경로 붕괴 0"이라고 적었는데 **표본 특성이지 구조가 아니다** —
  09-06 라이브에서 advisory 2장이 정은지 붕괴 앵커를 가졌다.

## 어제(09-05) belle 판정·지시 원문

- "3.8로" — Gemini Flash 정본 갱신 승인
- "이렇게 사진 보여줘, 반영해줘 할 때라도 다시 점검을 해서 '안 맞는게 들어갔습니다' 뭐 이런 식으로
  알아서 좀 해야 나중에 고객이 분석해도 잘 잡아주겠거니 신뢰가 간다는거지"
- "내가 원하는거는 인제 이렇게 나랑 티키타카 하는게 아니고 너가 분석을해서 발견해내는걸 보여줘야
  신뢰가 가는거지"
- "omni flash 모델은 되어야 하는데" (belle 이 맞았다 — 아래 참조)
- "파워 스핀의 경우에 양 다리 벌림이 가장 문제인데 그거는 안잡고 어꺠를 잡아 놨네. … 왼쪽 어깨를
  잡을 때 다리를 각도 재는걸로 착각해서 겨드랑이랑 다리를 잘못 재고 있어"
- "엘보트위스트에서는 이게 정은지 선수거는 엉덩이 장면도 아니고 팔이랑 다리랑 잘못 잰거 같은데"
- "피터팬은 정은지쪽 장면이 문제 부위를 알리는 장면들이 대부분 아닌 거 같긴 하다"
- "영상은 내 기억에 잘 맞았던거 같은 기억은 좀 있긴한데… 다른 동작들 영상은 잘 맞췄는데 그 부분을
  캡처를 못해서 우리가 애를 좀 먹었을겨"

## 09-05 관측 (판정 아님)

- **21장 중 7장이 제목과 다른 부위를 보여준다.** 라이브 2런 카드 판정 21/21 일치.
  마지막 줄 `card_photo_audit total=21 mismatched=7 unresolved=0`.
- **양다리 벌림 축이 Gemini 단독 의존이다.** `TechniqueProfile.required_split_deg` 가 모든 생성
  지점에서 하드코딩 `None`(인식기 4곳 포함) → 기하 split seed 가 발화하지 않는다. 코드 주석이
  스스로 2곳에서 "게이트가 항상 False 라 사문" 이라고 적어뒀다. 09-03 라이브 6동작 중
  **5동작이 `fallback=gemini_silent`**, 말한 킵업만 split −20 이 잡혔다. 침묵하면 감점·억제·
  커버리지갭 어디에도 흔적이 없다. 측정 함수 `features.split_angle_series` 는 IPSF 정의대로
  구현돼 있고 테스트 8건 통과 + 파이프라인이 import 까지 한다 — 게이트만 안 열린다.
- **spotCheck 가 상대 항목을 판정하지 못한다.** 6동작 거의 전 항목이 `uncertain`, 사유가 동일:
  "비교 측정 항목이므로 프레임만으로 판단할 수 없습니다". 제대로 판정된 유일한 건 파워스핀
  `leg_extension`(절대 기준 항목) — `match` + 근거 문장.
- **Omni Flash 는 된다.** `generateContent` 는 400 이지만 `POST /v1beta/interactions`
  `{model, input:[{type:text},{type:image,data,mime_type}], response_format:{type:text}}` 로
  이미지 판정이 온다. 다만 반복 안정성이 3.8-flash 보다 나쁘고(3카드 5회 비교) 카드당 $0.017 로
  최고가라 판정용으로는 안 쓴다. 영상 생성 트랙([[camera-angle-ai-single-view-synth]])은 별건.
- **09-04 착수점의 "belle 폰 pdshape 5장 확인"** 은 belle 이 폰 대신 대장 아티팩트를 보는 것으로
  대체됐다. 폰 확인 보고는 없었다.

## belle 판정 대기 (먼저 꺼내지 말 것)

- `.planning/research/pdshape-report-trim-2026-09-03.md` 본문 1~13 + 시트 14~20 + 보완 운동 축소
- 피그마 node 235-506 "예상 부위 클릭 시 시트" 캡처 대조 (09-03 밤 fetch 는 belle 이 막음)

## 상태 (2026-09-06 실측)

```
게이트: backend pytest 4717 passed / 0 failed / 20 skipped (09-06 n2j 이후, 내가 직접 실행)
        앱 tsc 0 · node --test 219/0 (09-03 이후 앱 코드 무변경)
미푸시: origin/main = 31a0f1cd, HEAD = 73b28ba6 — 로컬 65 커밋 앞
Pod: 0대 · pod-expected=down · Lambda RUNPOD_ANALYZE_URL 은 종료된 sgl4muagt7xlb9 (기동 시 동기화 필수)
앱: 1.2.4 두 플랫폼. main 에 1.2.4 이후 앱 커밋 1개(7016e8c3 시트 한 줄) → 1.2.5 대기. OTA 금지
Gemini: Flash 정본 = gemini-3.8-flash (ALLOWED_MODELS 에서 3.7 제거됨). 판정·코칭은 3.1-pro-preview 유지
        Pod 는 재기동해야 3.8 승계 (start_server.sh 가 config 에서 읽음)
확대 사진 대장(자체 점검 포함): https://claude.ai/code/artifact/9261104b-743a-4014-b318-edbf8d5d2b63
카드 패널 사진(리포 밖): /Users/Shared/sunity-card-audit-260905{,-ota}/
```

## 09-05 에 들어간 것

| quick | 내용 | 커밋 |
|---|---|---|
| 260905-mbd | Gemini Flash 정본 3.7 → 3.8, 화이트리스트에서 3.7 제거 | cd765baf |
| 260905-mvm | 카드 사진 감사 도구 (`part` claim + 순수 대조 + `backend/scripts/audit_card_photos.py`) | 62656643 |
| 260905-ota | 감사 2단화 (`mark_part` claim + `adjudicate` + `--replay`), 허용 집합 무변경 | 593fd19e |

감사 돌리는 법:
`backend/.venv/bin/python backend/scripts/audit_card_photos.py --uid <uid> --analysis-id <aid>`
(모델 = `resolve_model("C")`, 키 = env `GEMINI_API_KEY` → SSM. 마지막 줄이 불변식)

## 다음 후보 (첫 일 다음)

1. **양다리 벌림 축 살리기** — `required_split_deg` 를 인식기가 채우게 하거나, 게이트를 profile 이
   아니라 "두 허벅지를 잴 수 있는가"로 바꾼다. belle 이 "가장 문제"라고 한 축이다.
2. **감사를 통과 기준으로 승격** — 09-03 "6/6 PASS" 는 장수만 센다. 장수 + 부위 정합 둘 다로.
   분석 시점에 붙이면 카드당 눈 호출이 는다(21장 2단에 17~21분) — 비용·처분 결정 필요.
3. **LLM 단독 의존 축의 침묵을 기록하기** — 지금은 흔적이 0이라 사후 발견이 불가능하다.
