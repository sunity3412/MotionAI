---
phase: 01-poseengine-mediapipe-nlf-r-d
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/shared/python/sunity_shared/analysis/pose_frame.py
  - backend/shared/python/sunity_shared/models.py
  - app/src/types/analysis.ts
  - docs/contract.md
  - backend/tests/fixtures/__init__.py
  - backend/tests/fixtures/synthetic_frames.py
  - backend/tests/fixtures/mediapipe_landmarks.py
  - backend/tests/test_pose_frame_contract.py
autonomous: true
requirements:
  - POSE-01
  - POSE-02
must_haves:
  truths:
    - "PoseFrame Python dataclass와 TS interface가 동일한 필드 set을 노출한다"
    - "PoleAxis 타입이 source='hough'|'vertical_fallback' literal union을 강제한다"
    - "PoseFrame.empty() 생성자가 미감지 프레임용 sentinel을 반환한다"
    - "테스트 픽스처가 MediaPipe 33 landmark mock + synthetic 폴 영상 frame을 제공한다"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/pose_frame.py"
      provides: "PoseFrame + PoleAxis dataclass 정의"
      contains: "@dataclass(frozen=True)\nclass PoseFrame"
    - path: "app/src/types/analysis.ts"
      provides: "TS PoseFrame + PoleAxis interface (lockstep)"
      contains: "export interface PoseFrame"
    - path: "backend/tests/test_pose_frame_contract.py"
      provides: "PoseFrame 필드 contract 단위 테스트"
    - path: "backend/tests/fixtures/synthetic_frames.py"
      provides: "Hough 검출용 합성 프레임 generator"
  key_links:
    - from: "backend/shared/python/sunity_shared/analysis/pose_frame.py"
      to: "app/src/types/analysis.ts"
      via: "필드명 lockstep (camelCase TS ↔ snake_case Python)"
      pattern: "PoseFrame|PoleAxis"
    - from: "docs/contract.md"
      to: "PoseFrame + PoleAxis 섹션"
      via: "Cross-cutting lockstep 룰"
      pattern: "## PoseFrame|## PoleAxis"
---

<objective>
PoseFrame + PoleAxis 데이터 계약을 3-way lockstep으로 정의(Python dataclass + TS interface + docs/contract.md)하고, Wave 1·2·3·4가 의존하는 테스트 픽스처를 만든다. 이 plan은 모든 후속 plan의 기반 — 파일 자체는 가벼우나 계약이 명확해야 다운스트림이 흔들리지 않는다.

Purpose: D-04(원본 33 + COCO-17 + 폴 확장)·D-05(confidence 정책)·D-12(raw + pole-aligned 둘 다 저장) 결정을 데이터 계약으로 박제. CLAUDE.md Cross-cutting "single source of truth for the API contract" 룰 준수. Wave 0 테스트 인프라 확보로 후속 wave의 RED→GREEN 사이클 가능.

Output: pose_frame.py (Python dataclass) + analysis.ts (TS interface) + contract.md 갱신 + 합성 픽스처 2개 + 계약 contract 테스트.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-VALIDATION.md
@CLAUDE.md
@backend/shared/python/sunity_shared/models.py
@backend/shared/python/sunity_shared/analysis/interfaces.py
@backend/shared/python/sunity_shared/analysis/skeleton.py
@app/src/types/analysis.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PoseFrame + PoleAxis Python dataclass 정의</name>
  <files>backend/shared/python/sunity_shared/analysis/pose_frame.py, backend/shared/python/sunity_shared/models.py, backend/tests/test_pose_frame_contract.py</files>
  <read_first>
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md (D-04, D-05, D-12 결정 박스 — 정확한 필드/정책 인용)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md §Pattern 2 (PoseFrame 데이터 계약 — TS/Python 동등 정의)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md §Pattern 3 (33→COCO-17 매핑 표 + 폴 확장 landmark 표)
    - backend/shared/python/sunity_shared/models.py (기존 상수 패턴 — frozen dataclass 위치)
    - backend/shared/python/sunity_shared/analysis/skeleton.py (KEYPOINT_NAMES, JOINT_KEYS — 재사용 대상)
    - backend/shared/python/sunity_shared/analysis/interfaces.py (NoHumanError, Protocol 패턴)
    - backend/tests/test_validation.py (frozen dataclass 테스트 패턴 참고)
  </read_first>
  <behavior>
    - Test 1 (test_pose_frame_empty_sentinel): PoseFrame.empty(frame_index=5, timestamp_ms=555) 반환 → frame_index==5, timestamp_ms==555, raw_landmarks_33=={}, keypoints_3d=={}, keypoints_3d_pole_aligned=={}
    - Test 2 (test_confidence_conversion_formula): D-05 공식 검증. visibility=0.8, presence=0.9 → confidence == 0.72 ± 1e-6 + uncertainty_proxy == 0.28 ± 1e-6. visibility만 있을 때(presence=None) confidence == visibility
    - Test 3 (test_pole_axis_source_literal): PoleAxis(direction_3d=(0,0,1), confidence=0.9, source='hough') 생성 성공. source='invalid'는 typing 위반 (mypy/런타임 validator)
    - Test 4 (test_pose_frame_lockstep_fields): PoseFrame dataclass의 fields() 결과 set이 TS PoseFrame interface 필드 set과 명칭 매핑(snake_case→camelCase)으로 일치 — 기대 set = {frame_index, timestamp_ms, raw_landmarks_33, keypoints_3d, keypoints_3d_pole_aligned, keypoints_2d, pole_extension_landmarks}
    - Test 5 (test_confidence_range_validation): confidence 또는 visibility가 0~1 범위 밖이면 ValueError (PoseFrame 생성 시점 또는 도우미 함수)
  </behavior>
  <action>
    `backend/shared/python/sunity_shared/analysis/pose_frame.py` 신규 생성.
    - `from __future__ import annotations` + module docstring (D-04, D-05, D-12 결정 명시).
    - `Landmark3D` TypedDict 또는 dataclass(frozen=True): fields = `x: float, y: float, z: float, visibility: float, presence: float`. CONTEXT.md D-05의 raw_visibility/raw_presence/confidence/uncertainty_proxy 모두 별도 필드.
    - `Keypoint3D` dataclass(frozen=True): `x: float, y: float, z: float, confidence: float, uncertainty_proxy: float`. confidence = visibility × presence (둘 다 있을 때) 또는 visibility (presence=None일 때). uncertainty_proxy = 1 - confidence.
    - `Keypoint3DAligned` dataclass(frozen=True): `x: float, y: float, z: float` only (D-12 pole-aligned은 신뢰도 metadata 별도 저장 불필요 — keypoints_3d의 confidence를 그대로 참조).
    - `Keypoint2D` dataclass(frozen=True): `x: float, y: float, visibility: float` (normalized 0~1, D-03 image landmarks UI 오버레이용).
    - `PoseExtensionLandmark` dataclass(frozen=True): `x: float, y: float, z: float, confidence: float` (toe/heel/grip — D-04).
    - `PoseFrame` dataclass(frozen=True):
      필드 (전부 키워드 인자):
      - `frame_index: int`
      - `timestamp_ms: int`
      - `raw_landmarks_33: dict[str, Landmark3D]` (default factory dict — MediaPipe 33 landmark name → Landmark3D)
      - `keypoints_3d: dict[str, Keypoint3D]` (COCO-17 keypoint name → Keypoint3D, D-04 스코어링 계약)
      - `keypoints_3d_pole_aligned: dict[str, Keypoint3DAligned]` (D-12 pole-aligned, 분석에 사용)
      - `keypoints_2d: dict[str, Keypoint2D] | None` (D-03 normalized image — UI 오버레이용, optional)
      - `pole_extension_landmarks: dict[str, PoseExtensionLandmark] | None` (D-04 폴 확장, optional)
      classmethod `empty(cls, frame_index: int, timestamp_ms: int) -> 'PoseFrame'`: 미감지 프레임 sentinel. 모든 dict 필드 empty `{}`.
      staticmethod `compute_confidence(visibility: float, presence: float | None) -> tuple[float, float]`: D-05 공식 적용 + 0~1 범위 validator. presence=None이면 confidence=visibility. 반환 `(confidence, uncertainty_proxy)`.
    - `PoleAxis` dataclass(frozen=True):
      - `direction_3d: tuple[float, float, float]` (단위 벡터 — 정규화는 호출자 책임이나 validator 권장)
      - `confidence: float` (0~1)
      - `source: Literal['hough', 'vertical_fallback']`
      `__post_init__`에서 confidence 범위 검증 + source literal 검증.
    - `to_coco17_array(pose_frames: list[PoseFrame]) -> np.ndarray`: helper. `skeleton.KEYPOINT_NAMES` 17개 순서로 keypoints_3d_pole_aligned에서 추출해 `(T, 17, 4)` 배열 생성 (x, y, z, uncertainty_proxy). 빈 sentinel은 NaN 채움 + uncertainty_proxy=1.0. 기존 `compute_joint_angles`가 그대로 받을 수 있는 형태. RESEARCH §Pattern 1 Last paragraph 참조.

    `backend/shared/python/sunity_shared/models.py` 수정:
    - 파일 끝에 추가: `from .analysis.pose_frame import PoseFrame, PoleAxis, Keypoint3D, Landmark3D  # re-export (lockstep)` (단, circular import 주의 — pose_frame.py는 models.py를 import하지 않음).
    - 파일 상단 docstring에 한 줄 추가: "PoseFrame/PoleAxis는 analysis/pose_frame.py에 정의 (lockstep with app/src/types/analysis.ts)".

    `backend/tests/test_pose_frame_contract.py` 신규 생성:
    - module docstring: "Phase 1 D-04, D-05, D-12 계약 검증. lockstep with TS interface."
    - `from sunity_shared.analysis.pose_frame import PoseFrame, PoleAxis` import.
    - Test 1~5 위 behavior 그대로 구현. pytest 단위 테스트 (mark·fixture 불필요, 순수 단위).
    - Test 4의 expected_snake_set은 모듈 상단 모듈 상수로 선언 (실패 시 diff가 명확). camelCase ↔ snake_case 매핑은 단순 변환 (frame_index↔frameIndex). 매핑 함수도 helper로 도출.
    - 어떤 외부 fixture도 import하지 말 것 (Wave 0 가장 먼저 도는 테스트).

    `(D-04, D-05, D-12 per CONTEXT)` 주석을 dataclass 정의 위에 명시.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_pose_frame_contract.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `pytest backend/tests/test_pose_frame_contract.py -x` exits 0
    - `python -c "from sunity_shared.analysis.pose_frame import PoseFrame, PoleAxis; print(PoseFrame.empty(0, 0))"` runs without error
    - `grep -n "@dataclass(frozen=True)" backend/shared/python/sunity_shared/analysis/pose_frame.py | wc -l` >= 6 (Landmark3D, Keypoint3D, Keypoint3DAligned, Keypoint2D, PoseExtensionLandmark, PoseFrame, PoleAxis)
    - `grep -n "compute_confidence" backend/shared/python/sunity_shared/analysis/pose_frame.py` matches at least one staticmethod
    - `grep -n "to_coco17_array" backend/shared/python/sunity_shared/analysis/pose_frame.py` matches one function returning np.ndarray
    - Test 2 numerical assertion: visibility=0.8, presence=0.9 → confidence ≈ 0.72 ± 1e-6
    - `grep -n "PoseFrame\|PoleAxis" backend/shared/python/sunity_shared/models.py` matches at least one line (re-export 또는 docstring)
    - No fenced code block in this action body (deep_work_rules compliance)
  </acceptance_criteria>
  <done>PoseFrame + PoleAxis Python dataclass가 D-04·D-05·D-12 정확히 반영하고 5개 contract 테스트 모두 통과. models.py에 re-export 추가.</done>
</task>

<task type="auto">
  <name>Task 2: TS PoseFrame + PoleAxis interface + docs/contract.md lockstep</name>
  <files>app/src/types/analysis.ts, docs/contract.md</files>
  <read_first>
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md §Pattern 2 (TS 정의 정확한 형태 인용)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md (D-04, D-05, D-12, D-11, D-10 모두 — TS doc-comment에 반영)
    - app/src/types/analysis.ts (기존 export 패턴, doc-comment 스타일, lockstep 주석 위치)
    - backend/shared/python/sunity_shared/analysis/pose_frame.py (Task 1에서 생성 — 필드 매핑 source of truth)
    - docs/contract.md if exists (기존 섹션 구조)
    - CLAUDE.md Cross-cutting (single source of truth 룰)
  </read_first>
  <action>
    `app/src/types/analysis.ts` 수정:
    - 파일 끝(현재 PROGRESS_SEQUENCE 다음)에 새 섹션 `// ── Pose Engine 데이터 계약 (Phase 1, D-04/D-05/D-12) ──`을 한국어 doc-comment와 함께 추가.
    - `Landmark3D` interface: fields `x: number; y: number; z: number; visibility: number; presence: number;`. 주석: "MediaPipe 33 raw landmark — visibility = 보임 여부, presence = 사람 있을 확률 (D-05)".
    - `Keypoint3D` interface: `x: number; y: number; z: number; confidence: number; uncertaintyProxy: number;`. 주석: "COCO-17 keypoint. confidence = visibility × presence (D-05). uncertaintyProxy = 1 - confidence".
    - `Keypoint3DAligned` interface: `x: number; y: number; z: number;`. 주석: "폴 축 정렬된 좌표 (D-12). 신뢰도는 keypoints3D의 같은 키로 lookup".
    - `Keypoint2D` interface: `x: number; y: number; visibility: number;`. 주석: "image normalized 0~1 (D-03 image landmarks — UI 오버레이용)".
    - `PoleExtensionLandmark` interface: `x: number; y: number; z: number; confidence: number;`. 주석: "폴 확장 — toe/heel/grip (D-04, 분석 보조)".
    - `PoseFrame` interface (camelCase 필드):
      - `frameIndex: number;`
      - `timestampMs: number;`
      - `rawLandmarks33?: Record<string, Landmark3D>;` (D-04 원본 보존)
      - `keypoints3D: Record<string, Keypoint3D>;` (COCO-17, 스코어링 계약)
      - `keypoints3DPoleAligned: Record<string, Keypoint3DAligned>;` (D-12, 분석에 사용)
      - `keypoints2D?: Record<string, Keypoint2D>;` (D-03)
      - `poleExtensionLandmarks?: Record<string, PoleExtensionLandmark>;` (D-04)
    - `PoleAxis` interface:
      - `direction3D: { x: number; y: number; z: number };` (단위 벡터)
      - `confidence: number;` (0~1)
      - `source: 'hough' | 'vertical_fallback';` (D-09, D-11)
    - 새 섹션 상단에 lockstep 주석: "// PoseFrame/PoleAxis는 backend/shared/python/sunity_shared/analysis/pose_frame.py 의 dataclass와 lockstep (CLAUDE.md Cross-cutting). 변경 시 양쪽 + docs/contract.md 모두 갱신."
    - AnalysisDoc, ReferenceMotion 등 기존 export는 건드리지 않음 (Pattern 2 결정 — Firestore 저장은 별 phase에서 결정. 본 phase는 타입 정의만).

    `docs/contract.md` 수정 (또는 신규 생성 시 적절한 위치 — 기존 §3~5 다음에 추가):
    - 새 섹션 `## §6. PoseFrame + PoleAxis (Phase 1 신규)` 추가.
    - 한국어 설명 + 두 언어 정의 표 (필드명·TS 타입·Python 타입·설명·해당 D 결정 ID).
    - D-04, D-05, D-12 결정 박스를 인라인으로 인용 (CONTEXT.md에서 복사 — 박스 출처 명시).
    - Firestore 저장 정책 명시 (RESEARCH §Pattern 2 last paragraph): "본 phase에서 PoseFrame은 RunPod 메모리 전용. Firestore 저장은 기존 angles flat 패턴 유지. 키포인트 오버레이 저장은 Phase 12 결정."
    - "변경 시 lockstep" 경고 박스: 3 파일 동시 변경 (TS + Python + 이 문서).

    contract.md가 존재하지 않으면 minimal 형태로 생성 — frontmatter + `# 분석 데이터 계약 (Sunity AI Coach)` + `> 단일 진실: 양쪽 (TS/Python) + 이 문서` + §6만 생성. 기존 sections이 있으면 보존.
  </action>
  <verify>
    <automated>grep -c "export interface PoseFrame" app/src/types/analysis.ts | grep -v "^0$" && grep -c "export interface PoleAxis" app/src/types/analysis.ts | grep -v "^0$" && cd app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export interface PoseFrame" app/src/types/analysis.ts` matches exactly one line
    - `grep -n "export interface PoleAxis" app/src/types/analysis.ts` matches exactly one line
    - `grep -nE "rawLandmarks33|keypoints3D|keypoints3DPoleAligned|keypoints2D|poleExtensionLandmarks" app/src/types/analysis.ts` matches >= 5 distinct field references
    - `grep -nE "source: 'hough' \\| 'vertical_fallback'" app/src/types/analysis.ts` matches exactly one line
    - `cd app && npm run typecheck` exits 0 (no TS errors)
    - `grep -nE "^## .*PoseFrame.*PoleAxis" docs/contract.md | wc -l` >= 1 (new §6 section exists)
    - `grep -n "D-04\|D-05\|D-12" docs/contract.md` matches at least 3 lines (decision IDs cited)
    - `grep -n "lockstep" docs/contract.md` matches at least 1 line
  </acceptance_criteria>
  <done>TS PoseFrame + PoleAxis interface 추가, npm run typecheck 통과, docs/contract.md §6 section 추가 + D-04/D-05/D-12 인용 + lockstep 경고. Python dataclass 필드와 1:1 매핑 (snake_case↔camelCase).</done>
</task>

<task type="auto">
  <name>Task 3: 테스트 픽스처 (synthetic frames + MediaPipe mock landmarks)</name>
  <files>backend/tests/fixtures/__init__.py, backend/tests/fixtures/synthetic_frames.py, backend/tests/fixtures/mediapipe_landmarks.py</files>
  <read_first>
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-VALIDATION.md (Wave 0 Requirements 섹션 — 픽스처가 어떤 테스트들에 쓰이는지)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md §Pattern 3 (MediaPipe 33 인덱스 매핑 + 폴 확장 매핑 — mock landmark 생성에 필요)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md §Pattern 4 (Hough 검출 입력 영상 특성 — 수직 폴 띠 + 수평선 배경)
    - backend/tests/conftest.py (기존 fixture 패턴)
    - backend/shared/python/sunity_shared/analysis/pose_frame.py (Task 1 출력 — Landmark3D 형태)
    - backend/shared/python/sunity_shared/analysis/skeleton.py (COCO-17 KEYPOINT_NAMES 순서)
  </read_first>
  <action>
    `backend/tests/fixtures/__init__.py` 신규 생성:
    - 빈 파일 + 한 줄 docstring: "Phase 1 test fixtures — synthetic frames + MediaPipe mock landmarks. Wave 1·2·3 단위 테스트가 import."

    `backend/tests/fixtures/synthetic_frames.py` 신규 생성:
    - module docstring: "Hough 폴 검출 + 어댑터 단위 테스트용 합성 프레임 (T,H,W,3) RGB uint8. 무거운 의존성 없음 — numpy only."
    - `import numpy as np`
    - `def synthetic_vertical_pole_frames(num_frames: int = 3, height: int = 128, width: int = 64, pole_x: int | None = None) -> np.ndarray`:
      입력 width/2를 기본 pole_x로 사용. (T,H,W,3) uint8 배열 생성: 배경 흰색(255), 수직 폴 띠(좌우 5px 너비, 그레이 80)를 모든 프레임 같은 x에. RGB 채널 동일값. dtype=np.uint8.
      Hough 검출 시 vertical line 명확히 감지되도록 충분한 contrast.
    - `def synthetic_no_pole_frames(num_frames: int = 3, height: int = 128, width: int = 64) -> np.ndarray`:
      균일 흰색(255) 영상 — Hough 검출 실패 케이스 (D-11 vertical_fallback 테스트용).
    - `def synthetic_tilted_pole_frames(num_frames: int = 3, height: int = 128, width: int = 64, tilt_deg: float = 30.0) -> np.ndarray`:
      기울어진 폴 (vertical_tolerance 5° 밖) → 검출 실패 케이스. (선택 — Wave 1 Task 안에서 분기 테스트에 사용).
    - 모든 함수에서 return 직전 `assert arr.dtype == np.uint8 and arr.ndim == 4 and arr.shape[3] == 3` (sanity).

    `backend/tests/fixtures/mediapipe_landmarks.py` 신규 생성:
    - module docstring: "MediaPipe PoseLandmarker mock result. detect_for_video 호출을 mock — 실 mediapipe library 부재 환경(로컬 macOS)에서도 어댑터 단위 테스트 가능."
    - `from dataclasses import dataclass` + `from typing import Sequence`.
    - `@dataclass\nclass _MockLandmark`: fields `x: float, y: float, z: float, visibility: float, presence: float`. MediaPipe NormalizedLandmark/Landmark의 duck-typed 대체.
    - `def make_mock_world_landmarks_33(visible_indices: Sequence[int] | None = None, default_visibility: float = 0.9, default_presence: float = 0.9) -> list`:
      33개 `_MockLandmark` 리스트 반환. world 좌표: 골반 midpoint origin (D-05/Pitfall 3). visible_indices 명시되면 그 인덱스만 visibility/presence를 default, 나머지는 visibility=0.1/presence=0.1로. 각 landmark의 (x,y,z)는 deterministic 합성값 (`x = idx * 0.01, y = (idx % 5) * 0.02, z = 0.0`).
    - `def make_mock_image_landmarks_33(default_visibility: float = 0.9) -> list`:
      normalized 0~1 좌표. (x = idx/33, y = (idx%10)/10, z=0, visibility=0.9, presence=0.9).
    - `def make_mock_detection_result(world_landmarks_33=None, image_landmarks_33=None) -> object`:
      MediaPipe DetectionResult duck-type 객체 반환. attributes: `pose_landmarks: list[list]` (외부 list = num_poses, 내부 = landmarks), `pose_world_landmarks: list[list]`. 둘 다 None이면 empty list 반환 (미감지 케이스).
    - `def make_mock_no_human_result() -> object`: pose_landmarks=[], pose_world_landmarks=[] (NoHumanError 트리거 케이스).

    이 fixtures는 Wave 1·2·3 모든 단위 테스트에서 import. Task 1 contract 테스트는 fixture import 안 함 (선후 순서 명확).
  </action>
  <verify>
    <automated>cd backend && python -c "from tests.fixtures.synthetic_frames import synthetic_vertical_pole_frames, synthetic_no_pole_frames; import numpy as np; arr = synthetic_vertical_pole_frames(); assert arr.shape == (3, 128, 64, 3) and arr.dtype == np.uint8; print('synthetic OK')" && cd backend && python -c "from tests.fixtures.mediapipe_landmarks import make_mock_world_landmarks_33, make_mock_no_human_result; lm = make_mock_world_landmarks_33(); assert len(lm) == 33; r = make_mock_no_human_result(); assert r.pose_landmarks == []; print('mock OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from tests.fixtures.synthetic_frames import synthetic_vertical_pole_frames; print(synthetic_vertical_pole_frames().shape)"` outputs `(3, 128, 64, 3)`
    - `python -c "from tests.fixtures.mediapipe_landmarks import make_mock_world_landmarks_33; print(len(make_mock_world_landmarks_33()))"` outputs `33`
    - `grep -nE "def synthetic_vertical_pole_frames|def synthetic_no_pole_frames|def synthetic_tilted_pole_frames" backend/tests/fixtures/synthetic_frames.py` matches 3 functions
    - `grep -nE "def make_mock_world_landmarks_33|def make_mock_image_landmarks_33|def make_mock_detection_result|def make_mock_no_human_result" backend/tests/fixtures/mediapipe_landmarks.py` matches 4 functions
    - `python -c "from tests.fixtures.mediapipe_landmarks import make_mock_no_human_result; r = make_mock_no_human_result(); assert r.pose_landmarks == [] and r.pose_world_landmarks == []"` exits 0
    - All synthetic functions return uint8 ndarrays with shape (T,H,W,3) — assertions in module pass on import
  </acceptance_criteria>
  <done>Wave 1·2·3가 import할 fixture 2개 모듈 + __init__.py 작성. 합성 frame 3종(vertical, no_pole, tilted) + MediaPipe mock 4종 함수. 모듈 import 자체가 sanity check 통과.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 데이터 계약 ↔ 다운스트림 | TS/Python/contract.md lockstep 위반 시 silent 미스매치 발생 (Pitfall 7). 본 plan이 lockstep 강제 |
| 테스트 픽스처 ↔ 실 데이터 | 합성 fixture가 실 MediaPipe 출력 형태를 정확히 모사하지 못하면 Wave 1 단위 테스트가 실 행동을 보장 못함 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-04 | Tampering | PoseFrame 계약 (TS/Python 분기) | mitigate | docs/contract.md lockstep 강제 + Task 2 acceptance에 양쪽 grep 검증 포함 |
| T-1-05 | Information disclosure | Confidence 값 0~1 범위 위반 | mitigate | `PoseFrame.compute_confidence` 정적 validator + dataclass `__post_init__`에서 ValueError |
| T-1-06 | Spoofing | source literal ('hough'|'vertical_fallback') 외 값 주입 | mitigate | Literal 타입 + `PoleAxis.__post_init__`에서 set membership 검증 |
</threat_model>

<verification>
- `pytest backend/tests/test_pose_frame_contract.py -x` exits 0 (5 tests pass)
- `cd app && npm run typecheck` exits 0
- Fixture import sanity: synthetic_frames + mediapipe_landmarks 둘 다 module-level import 시 ImportError 없음
- 3-way lockstep: TS interface 필드 set ↔ Python dataclass field set ↔ docs/contract.md §6 표 — 7개 PoseFrame 필드 + 3개 PoleAxis 필드 동일
</verification>

<success_criteria>
- PoseFrame + PoleAxis 데이터 계약 3-way lockstep 완성 (Python + TS + docs)
- Wave 1·2·3가 의존하는 합성 fixture 2개 모듈 작성
- 5개 contract 테스트 통과 (frozen sentinel, confidence 공식, source literal, lockstep field set, 범위 validator)
- D-04, D-05, D-12 결정이 코드 + 주석에 명시적으로 인용됨
- 어떤 외부 무거운 라이브러리(mediapipe, opencv, scipy) 의존성 도입 없음 — numpy + dataclasses + typing만
</success_criteria>

<output>
Create `.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-01-SUMMARY.md` when done. SUMMARY 내용:
- 생성된 파일 목록 + 라인 수
- 5개 contract 테스트 결과 (PASS/FAIL)
- TS typecheck 결과
- Wave 1·2·3에 노출되는 public API (PoseFrame, PoleAxis, fixtures functions)
- belle 확정 보류 항목: A1 (MediaPipe→COCO-17 매핑 공식 검증) — Wave 1 Task 1에서 수동 검증할 것임을 명시
</output>
