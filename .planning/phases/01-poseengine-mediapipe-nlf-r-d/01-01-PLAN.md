---
phase: 01-poseengine-mediapipe-nlf-r-d
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/shared/python/sunity_shared/analysis/pose_frame.py
  - backend/shared/python/sunity_shared/models.py
  - app/src/types/analysis.ts
  - docs/contract.md
  - backend/tests/fixtures/__init__.py
  - backend/tests/fixtures/synthetic_frames.py
  - backend/tests/fixtures/mediapipe_landmarks.py
  - backend/tests/test_pose_frame_contract.py
  - backend/shared/python/sunity_shared/analysis/reliability.py
  - backend/tests/test_reliability_gate.py
autonomous: true
requirements:
  - POSE-01
  - POSE-02
must_haves:
  truths:
    - "PoseFrame Python dataclass와 TS interface가 동일한 필드 set을 노출한다 (raw_visibility, raw_presence, confidence, uncertainty_proxy 4개 별도 필드 per D-05)"
    - "PoseFrame.pole_axis: PoleAxis | None 필드가 D-12를 박제한다 — 모든 frame이 video-level pole_axis 메타를 함께 보존"
    - "PoseFrame.reliability: ReliabilityLevel literal 'high' | 'medium' | 'low'가 frame-level 신뢰도 게이트를 닫는다 (D-05 + Success #4)"
    - "PoleAxis.confidence_level: 'high' | 'medium' | 'low' enum이 D-11 confidence='low' 박제 + UI/리포트가 안전하게 분기 가능"
    - "compute_angle_with_reliability(...) wrapper가 keypoint 저신뢰 시 low_reliability=True 마킹 (Success #4 게이트)"
    - "docs/contract.md의 angles 필드가 flat number[] + anglesJointKeys + anglesFrames로 정정되어 TS/Firestore 실제 저장과 lockstep (M-5)"
    - "테스트 픽스처가 MediaPipe 33 landmark mock + synthetic 폴 영상 frame을 제공한다"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/pose_frame.py"
      provides: "PoseFrame + PoleAxis + ReliabilityLevel + ConfidenceLevel dataclass 정의 (D-04/D-05/D-11/D-12)"
      contains: "@dataclass(frozen=True)\nclass PoseFrame"
    - path: "backend/shared/python/sunity_shared/analysis/reliability.py"
      provides: "compute_angle_with_reliability — low-reliability 게이트 stub (Success #4)"
    - path: "app/src/types/analysis.ts"
      provides: "TS PoseFrame + PoleAxis + ReliabilityLevel + ConfidenceLevel interface (lockstep)"
      contains: "export interface PoseFrame"
    - path: "backend/tests/test_pose_frame_contract.py"
      provides: "PoseFrame + reliability + pole_axis 계약 단위 테스트"
    - path: "backend/tests/test_reliability_gate.py"
      provides: "compute_angle_with_reliability 게이트 단위 테스트 (Success #4)"
    - path: "backend/tests/fixtures/synthetic_frames.py"
      provides: "Hough 검출용 합성 프레임 generator"
    - path: "docs/contract.md"
      provides: "§6 PoseFrame/PoleAxis + §3 angles 필드 정정 + low-reliability 단정 금지 정책 NOTE (H-4)"
  key_links:
    - from: "backend/shared/python/sunity_shared/analysis/pose_frame.py"
      to: "app/src/types/analysis.ts"
      via: "필드명 lockstep (camelCase TS ↔ snake_case Python)"
      pattern: "PoseFrame|PoleAxis|ReliabilityLevel|ConfidenceLevel"
    - from: "docs/contract.md"
      to: "PoseFrame + PoleAxis + 정정된 angles 섹션"
      via: "Cross-cutting lockstep 룰"
      pattern: "## PoseFrame|## PoleAxis|angles\\?: number\\[\\]"
---

<objective>
PoseFrame + PoleAxis 데이터 계약을 3-way lockstep으로 정의(Python dataclass + TS interface + docs/contract.md)하고, Wave 1·2·3가 의존하는 테스트 픽스처를 만든다. 본 plan은 REVIEWS.md H-3 (PoseFrame.pole_axis 누락) / H-4 (reliability 게이트 미존재) / M-1 (raw_visibility/raw_presence 정확 명명) / M-2 (confidence_level enum) / M-5 (contract.md angles 불일치) 5건을 모두 해소한다.

Purpose: D-04(원본 33 + COCO-17 + 폴 확장)·D-05(confidence 정책 — raw_visibility/raw_presence 별도)·D-11(PoleAxis confidence_level='low' enum)·D-12(raw + pole-aligned + PoleAxis 메타 모두 저장) 결정을 데이터 계약으로 박제. ROADMAP Success #3 / #4 게이트를 PoseFrame.pole_axis 필드 + PoseFrame.reliability 필드 + compute_angle_with_reliability wrapper로 닫는다. CLAUDE.md Cross-cutting "single source of truth for the API contract" 룰 준수. Wave 0 테스트 인프라 확보로 후속 wave의 RED→GREEN 사이클 가능.

Output: pose_frame.py (Python dataclass) + reliability.py (게이트 stub) + analysis.ts (TS interface) + contract.md 갱신 (§6 신규 + §3 angles 정정 + low-reliability 단정 금지 NOTE) + 합성 픽스처 2개 + 계약 contract 테스트 + reliability 게이트 테스트.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-VALIDATION.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-REVIEWS.md
@CLAUDE.md
@backend/shared/python/sunity_shared/models.py
@backend/shared/python/sunity_shared/analysis/interfaces.py
@backend/shared/python/sunity_shared/analysis/skeleton.py
@app/src/types/analysis.ts
@docs/contract.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PoseFrame + PoleAxis + Reliability Python dataclass (raw_visibility/raw_presence + pole_axis + reliability 필드 모두 박제)</name>
  <files>backend/shared/python/sunity_shared/analysis/pose_frame.py, backend/shared/python/sunity_shared/models.py, backend/tests/test_pose_frame_contract.py</files>
  <read_first>
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md (D-04, D-05, D-11, D-12 결정 박스 — 정확한 필드명과 정책 인용)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-REVIEWS.md H-1 ~ H-4, M-1, M-2 (필드명·enum·게이트 fix 방향)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-VALIDATION.md (PoseFrame contract 테스트 요구)
    - backend/shared/python/sunity_shared/models.py (기존 상수 패턴 — frozen dataclass 위치)
    - backend/shared/python/sunity_shared/analysis/skeleton.py (KEYPOINT_NAMES, JOINT_KEYS — 재사용 대상)
    - backend/shared/python/sunity_shared/analysis/interfaces.py (NoHumanError, Protocol 패턴)
    - backend/tests/test_validation.py (frozen dataclass 테스트 패턴 참고)
  </read_first>
  <behavior>
    - Test 1 (test_pose_frame_empty_sentinel): PoseFrame.empty(frame_index=5, timestamp_ms=555, pole_axis=None) 반환 → frame_index==5, timestamp_ms==555, raw_landmarks_33=={}, keypoints_3d=={}, keypoints_3d_pole_aligned=={}, pole_axis is None, reliability == 'low'
    - Test 2 (test_confidence_conversion_formula): D-05 공식. visibility=0.8, presence=0.9 → confidence == 0.72 ± 1e-6 + uncertainty_proxy == 0.28 ± 1e-6. visibility만 있을 때(presence=None) confidence == visibility
    - Test 3 (test_landmark3d_raw_field_names): Landmark3D 인스턴스가 raw_visibility, raw_presence 두 필드를 가지며 visibility/presence라는 단축명은 없다 (M-1 D-05 박제). `set(fields(Landmark3D))`에 raw_visibility, raw_presence 존재, visibility/presence 부재 검증
    - Test 4 (test_pole_axis_confidence_level_enum): PoleAxis(axis_vector=(0,0,1), confidence_level='low', source='vertical_fallback') 생성 성공. confidence_level='invalid'는 ValueError. source='invalid'도 ValueError. (M-2 + D-11)
    - Test 5 (test_pose_frame_has_pole_axis_field): PoseFrame dataclass의 fields() 결과 set에 pole_axis 존재 (H-3 D-12 박제). 비어있는 sentinel도 pole_axis=None을 받아 frozen 인스턴스로 저장
    - Test 6 (test_pose_frame_reliability_field_and_threshold): compute_frame_reliability(mean_visibility=0.85) == 'high', 0.55 == 'medium', 0.3 == 'low' (H-4 + D-05). PoseFrame.reliability 필드 존재 + literal 'high' | 'medium' | 'low'만 허용 + 그 외 ValueError
    - Test 7 (test_pose_frame_lockstep_fields): PoseFrame dataclass의 fields() 결과 set이 TS PoseFrame interface 필드 set과 명칭 매핑(snake_case→camelCase)으로 일치 — 기대 set = {frame_index, timestamp_ms, raw_landmarks_33, keypoints_3d, keypoints_3d_pole_aligned, keypoints_2d, pole_extension_landmarks, pole_axis, reliability}
    - Test 8 (test_confidence_range_validation): confidence 또는 visibility가 0~1 범위 밖이면 ValueError (PoseFrame 생성 시점 또는 compute_confidence 함수)
  </behavior>
  <action>
    `backend/shared/python/sunity_shared/analysis/pose_frame.py` 신규 생성:
    - `from __future__ import annotations` + module docstring (D-04, D-05, D-11, D-12 결정 명시 + H-3/H-4/M-1/M-2 인용).
    - `from typing import Literal, Optional` import.
    - 타입 alias 두 개를 module-level로 노출:
      - `ConfidenceLevel = Literal['high', 'medium', 'low']` (M-2 D-11 박제 — PoleAxis와 reliability 둘 다 같은 enum 사용)
      - `ReliabilityLevel = Literal['high', 'medium', 'low']` (H-4 — 의미 분리, 같은 값 도메인이지만 의도 분리)
      - `PoleAxisSource = Literal['detected', 'vertical_fallback']` (H-3 H-4 reviewer 명세 — D-09/D-11). 호환을 위해 'hough'도 추가 가능하나 reviewer가 'detected'를 권장하므로 'detected' | 'vertical_fallback'.
    - 임계값 상수: `RELIABILITY_HIGH_THRESHOLD: float = 0.7`, `RELIABILITY_MEDIUM_THRESHOLD: float = 0.4` (H-4: mean visibility >= 0.7 high, 0.4~0.7 medium, <0.4 low — defensible default. CONTEXT.md D-05 기반).
    - `Landmark3D` dataclass(frozen=True): fields = `x: float, y: float, z: float, raw_visibility: float, raw_presence: float` (M-1 정확 명명). `__post_init__`에서 raw_visibility, raw_presence ∈ [0,1] 검증.
    - `Keypoint3D` dataclass(frozen=True): `x: float, y: float, z: float, confidence: float, uncertainty_proxy: float`. confidence = raw_visibility × raw_presence (둘 다 있을 때) 또는 raw_visibility (raw_presence=None일 때). uncertainty_proxy = 1 - confidence.
    - `Keypoint3DAligned` dataclass(frozen=True): `x: float, y: float, z: float` only (D-12 pole-aligned은 신뢰도 metadata 별도 저장 불필요 — keypoints_3d의 confidence를 그대로 참조).
    - `Keypoint2D` dataclass(frozen=True): `x: float, y: float, visibility: float` (normalized 0~1, D-03 image landmarks UI 오버레이용 — image landmark는 raw_presence가 mediapipe API에서 별도로 제공되지 않을 수 있어 visibility 한 필드만 유지. raw_ prefix는 PoseFrame 레벨 정책이지만 image-landmark에서는 의미 없음 — 주석으로 명시).
    - `PoseExtensionLandmark` dataclass(frozen=True): `x: float, y: float, z: float, confidence: float` (toe/heel/grip — D-04).
    - `PoleAxis` dataclass(frozen=True): (H-3 reviewer 명세 정확 반영)
      - `axis_vector: tuple[float, float, float]` (3D 단위 벡터)
      - `confidence_level: ConfidenceLevel` (M-2 D-11 enum — 'low' | 'medium' | 'high')
      - `source: PoleAxisSource` ('detected' | 'vertical_fallback')
      - `frame_index: int | None = None` (None이면 video-level pole_axis. D-10에 따라 보통 None — 모든 frame 동일 axis.)
      `__post_init__`에서 confidence_level literal 검증 + source literal 검증 + axis_vector norm ≈ 1 (atol=1e-3 — 부동소수 허용) 검증.
    - `PoseFrame` dataclass(frozen=True): (H-3 + H-4 박제)
      필드 (전부 키워드 인자, 순서 = 매개변수 우선순위):
      - `frame_index: int`
      - `timestamp_ms: int`
      - `raw_landmarks_33: dict[str, Landmark3D]` (default factory dict)
      - `keypoints_3d: dict[str, Keypoint3D]`
      - `keypoints_3d_pole_aligned: dict[str, Keypoint3DAligned]`
      - `keypoints_2d: dict[str, Keypoint2D] | None`
      - `pole_extension_landmarks: dict[str, PoseExtensionLandmark] | None`
      - `pole_axis: PoleAxis | None` (H-3 — D-12 박제, 모든 frame이 video-level pole_axis 메타를 함께 보존. D-10에 의해 보통 모든 frame이 같은 instance 또는 None)
      - `reliability: ReliabilityLevel` (H-4 — frame-level 게이트, 다운스트림 단정 금지 게이트)
      classmethod `empty(cls, frame_index: int, timestamp_ms: int, pole_axis: PoleAxis | None = None) -> 'PoseFrame'`: 미감지 프레임 sentinel. 모든 dict 필드 empty `{}`, reliability='low', pole_axis는 인자 그대로 전달.
      staticmethod `compute_confidence(raw_visibility: float, raw_presence: float | None) -> tuple[float, float]`: D-05 공식 적용 + 0~1 범위 validator. raw_presence=None이면 confidence=raw_visibility. 반환 `(confidence, uncertainty_proxy)`.
    - module-level 함수 `def compute_frame_reliability(mean_visibility: float) -> ReliabilityLevel`: H-4 임계값 매핑. >=RELIABILITY_HIGH_THRESHOLD(0.7) → 'high', >=RELIABILITY_MEDIUM_THRESHOLD(0.4) → 'medium', else → 'low'. 0~1 범위 검증.
    - module-level 함수 `def to_coco17_array(pose_frames: list[PoseFrame]) -> np.ndarray`: helper. `skeleton.KEYPOINT_NAMES` 17개 순서로 keypoints_3d_pole_aligned에서 추출해 `(T, 17, 4)` 배열 생성 (x, y, z, uncertainty_proxy). 빈 sentinel은 NaN 채움 + uncertainty_proxy=1.0. 기존 `compute_joint_angles`가 그대로 받을 수 있는 형태.

    `backend/shared/python/sunity_shared/models.py` 수정:
    - 파일 끝에 추가: `from .analysis.pose_frame import PoseFrame, PoleAxis, Keypoint3D, Landmark3D, ConfidenceLevel, ReliabilityLevel, PoleAxisSource  # re-export (lockstep)`.
    - 파일 상단 docstring에 한 줄 추가: "PoseFrame/PoleAxis는 analysis/pose_frame.py에 정의 (lockstep with app/src/types/analysis.ts)".

    `backend/tests/test_pose_frame_contract.py` 신규 생성:
    - module docstring: "Phase 1 D-04, D-05, D-11, D-12 계약 검증 + REVIEWS H-3/H-4/M-1/M-2 박제 확인. lockstep with TS interface."
    - `from sunity_shared.analysis.pose_frame import PoseFrame, PoleAxis, Landmark3D, compute_frame_reliability, ConfidenceLevel, ReliabilityLevel, PoleAxisSource` import.
    - Test 1~8 위 behavior 그대로 구현. pytest 단위 테스트 (mark·fixture 불필요).
    - Test 7의 expected_snake_set 모듈 상단 상수 선언 — 정확히 9개 필드: frame_index, timestamp_ms, raw_landmarks_33, keypoints_3d, keypoints_3d_pole_aligned, keypoints_2d, pole_extension_landmarks, pole_axis, reliability. camelCase↔snake_case 변환 helper도 작성.
    - 외부 fixture import 없음.

    주석에 (D-04, D-05, D-11, D-12 per CONTEXT) + (REVIEWS H-1~H-4, M-1, M-2 박제) 명시.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_pose_frame_contract.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `pytest backend/tests/test_pose_frame_contract.py -x` exits 0 (8 tests pass)
    - `python -c "from sunity_shared.analysis.pose_frame import PoseFrame, PoleAxis, ConfidenceLevel, ReliabilityLevel; print(PoseFrame.empty(0, 0))"` runs without error
    - `grep -nE "raw_visibility: float" backend/shared/python/sunity_shared/analysis/pose_frame.py` matches at least 1 line (M-1)
    - `grep -nE "raw_presence: float" backend/shared/python/sunity_shared/analysis/pose_frame.py` matches at least 1 line (M-1)
    - `grep -nE "^[[:space:]]*visibility: float" backend/shared/python/sunity_shared/analysis/pose_frame.py | grep -v "Keypoint2D\|image" | wc -l` outputs `0` (bare visibility 없음 — Keypoint2D image landmark 예외 제외)
    - `grep -nE "ConfidenceLevel = Literal\\['high', 'medium', 'low'\\]" backend/shared/python/sunity_shared/analysis/pose_frame.py` matches exactly 1 line (M-2)
    - `grep -nE "PoleAxisSource = Literal\\['detected', 'vertical_fallback'\\]" backend/shared/python/sunity_shared/analysis/pose_frame.py` matches exactly 1 line (H-3 reviewer 명세)
    - `grep -nE "pole_axis: PoleAxis \\| None" backend/shared/python/sunity_shared/analysis/pose_frame.py` matches at least 1 line (H-3 PoseFrame 필드)
    - `grep -nE "reliability: ReliabilityLevel" backend/shared/python/sunity_shared/analysis/pose_frame.py` matches at least 1 line (H-4 PoseFrame 필드)
    - `grep -nE "def compute_frame_reliability" backend/shared/python/sunity_shared/analysis/pose_frame.py` matches exactly 1 line (H-4 임계값 wrapper)
    - `grep -nE "RELIABILITY_HIGH_THRESHOLD.*0\\.7|RELIABILITY_MEDIUM_THRESHOLD.*0\\.4" backend/shared/python/sunity_shared/analysis/pose_frame.py | wc -l` outputs at least `2` (defensible threshold 박제)
    - `grep -n "@dataclass(frozen=True)" backend/shared/python/sunity_shared/analysis/pose_frame.py | wc -l` >= 6 (Landmark3D, Keypoint3D, Keypoint3DAligned, Keypoint2D, PoseExtensionLandmark, PoseFrame, PoleAxis)
    - `grep -n "compute_confidence" backend/shared/python/sunity_shared/analysis/pose_frame.py` matches at least one staticmethod
    - `grep -n "to_coco17_array" backend/shared/python/sunity_shared/analysis/pose_frame.py` matches one function returning np.ndarray
    - Test 2 numerical assertion: raw_visibility=0.8, raw_presence=0.9 → confidence ≈ 0.72 ± 1e-6
    - `grep -nE "PoseFrame|PoleAxis|ReliabilityLevel|ConfidenceLevel" backend/shared/python/sunity_shared/models.py` matches at least one line (re-export)
    - No fenced code block in this action body (deep_work_rules compliance)
  </acceptance_criteria>
  <done>PoseFrame + PoleAxis + ReliabilityLevel + ConfidenceLevel Python dataclass가 D-04·D-05·D-11·D-12 + REVIEWS H-3/H-4/M-1/M-2 정확히 반영. 8개 contract 테스트 통과. raw_visibility/raw_presence/confidence/uncertainty_proxy 4개 별도 필드 + pole_axis 필드 + reliability 필드 모두 PoseFrame에 박제.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: compute_angle_with_reliability 게이트 stub + docs/contract.md 갱신 (H-4 + L-1 + M-5)</name>
  <files>backend/shared/python/sunity_shared/analysis/reliability.py, backend/tests/test_reliability_gate.py, docs/contract.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-REVIEWS.md H-4 (reliability 게이트 + low_reliability 마킹 필요), M-5 (contract.md angles flat 정정), L-1 (ROADMAP Success #3 wording 정정)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md D-05 (각도 low reliability 마킹 정책 + 고객 리포트 기술 용어 노출 금지)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md D-10 (영상 전체 평균 축 1개)
    - backend/shared/python/sunity_shared/analysis/pose_frame.py (Task 1 — PoseFrame.reliability, Keypoint3D.confidence)
    - backend/shared/python/sunity_shared/analysis/features.py (기존 각도 계산 진입점 — 향후 wrapper가 호출될 위치, 본 plan에서 import만 확인)
    - docs/contract.md (line 123 — angles?: number[][] 잘못된 시그니처 + line 195 — flat 명시되어 있지만 §3 표가 불일치)
    - app/src/types/analysis.ts (line 152~156 — angles?: number[] flat 실제 시그니처)
    - .planning/ROADMAP.md (line 62~63 — Phase 1 Success #3 wording 정정 대상)
  </read_first>
  <behavior>
    - Test 1 (test_compute_angle_with_reliability_high): 모든 필수 keypoint confidence >= 0.7 → 결과 dict에 low_reliability=False, value: float
    - Test 2 (test_compute_angle_with_reliability_low): 필수 keypoint 1개 confidence < 0.4 → 결과 dict에 low_reliability=True. value는 None 또는 계산값 (정책: 계산은 하되 마킹) — 본 게이트는 "마킹"이 목적
    - Test 3 (test_required_keypoints_per_angle_table): ANGLE_REQUIRED_KEYPOINTS 상수 dict가 최소 5개 entry (left_knee_flex, right_knee_flex, left_elbow_flex, right_elbow_flex, hip_axis 등) + 각 entry는 list[str] keypoint name
    - Test 4 (test_reliability_threshold_uses_pose_frame_constants): reliability.py가 pose_frame.RELIABILITY_HIGH_THRESHOLD 등을 재사용 (단일 진실)
  </behavior>
  <action>
    `backend/shared/python/sunity_shared/analysis/reliability.py` 신규 생성:
    - module docstring: "Phase 1 H-4 게이트 — Success #4 '추정 표기 + 후속 단정 금지'를 코드로 닫는 wrapper. 각도(angle) 계산 시 필수 keypoint가 저신뢰면 low_reliability=True 마킹. 다운스트림(coach_writer, dimensions) 단정형 코멘트 금지의 신호. D-05 박제."
    - `from __future__ import annotations`
    - import: `from .pose_frame import Keypoint3D, RELIABILITY_HIGH_THRESHOLD, RELIABILITY_MEDIUM_THRESHOLD, ReliabilityLevel`
    - 상수 `ANGLE_REQUIRED_KEYPOINTS: dict[str, list[str]]`: 각도 키 → 필요한 COCO-17 keypoint name list. 최소 5개 entry (예: 'left_knee_flex': ['left_hip', 'left_knee', 'left_ankle'], 'right_knee_flex': [...], 'left_elbow_flex': ['left_shoulder', 'left_elbow', 'left_wrist'], 'right_elbow_flex': [...], 'hip_axis': ['left_hip', 'right_hip']). 정확한 매핑은 skeleton.JOINT_ANGLES와 일치하는 방향으로 작성.
    - 함수 `def compute_angle_with_reliability(angle_key: str, keypoints: dict[str, Keypoint3D], compute_fn) -> dict`:
      1) angle_key가 ANGLE_REQUIRED_KEYPOINTS에 없으면 ValueError
      2) required = ANGLE_REQUIRED_KEYPOINTS[angle_key]; 모든 required keypoint가 keypoints에 존재하지 않으면 `{'value': None, 'low_reliability': True, 'reason': 'missing_keypoints'}` 반환
      3) min_confidence = min(keypoints[k].confidence for k in required)
      4) low_reliability flag = min_confidence < RELIABILITY_MEDIUM_THRESHOLD (0.4)
      5) value = compute_fn(...) 호출 결과 (compute_fn은 의존성 주입 — 본 stub은 게이트만 책임)
      6) 반환 `{'value': value, 'low_reliability': bool, 'min_confidence': float}`
    - 함수가 TODO 주석을 포함: "TODO: 후속 phase에서 features.py 각도 계산이 본 wrapper를 호출하도록 wiring. Phase 1 종료 시 wrapper 자체는 stub — 다운스트림 phase에서 enforce."

    `backend/tests/test_reliability_gate.py` 신규 생성:
    - module docstring: "Phase 1 H-4 게이트 단위 테스트 — Success #4 박제."
    - imports: pytest, dataclasses, `from sunity_shared.analysis.pose_frame import Keypoint3D, RELIABILITY_HIGH_THRESHOLD, RELIABILITY_MEDIUM_THRESHOLD`, `from sunity_shared.analysis.reliability import compute_angle_with_reliability, ANGLE_REQUIRED_KEYPOINTS`
    - Test 1~4 behavior 그대로 구현.
    - compute_fn은 mock (lambda kps: 90.0).
    - Keypoint3D 인스턴스 직접 생성 (uncertainty_proxy = 1 - confidence)

    `docs/contract.md` 갱신:
    - **§3 Firestore 문서 영역 angles 정정 (M-5):** line ~123 부근의 `angles? number[][]  (T, 8) 시퀀스` 표 entry를 다음 세 줄로 교체:
      `angles?            number[]               flat 시퀀스 — Firestore nested array 금지 회피. 길이 = anglesFrames × anglesJointKeys.length. 읽는 쪽이 reshape (T, J).`
      `anglesJointKeys?   string[]               angles의 J 축 순서 (방어적 명시, backend skeleton.JOINT_KEYS와 일치)`
      `anglesFrames?      number                 angles의 T (검증·디버깅용)`
      기존 §4의 두 번째 angles 언급(line 195)은 이미 flat 명시되어 있으므로 그대로 둠.
    - **§6 신규 섹션 (H-3 + H-4 + M-2 박제):** 파일 끝(또는 §5 다음)에 `## §6. PoseFrame + PoleAxis (Phase 1 신규 — D-04/D-05/D-11/D-12)` 헤더 추가.
      - 한국어 설명 + 필드 표 (한 표에 9 필드 — PoseFrame, 4 필드 — PoleAxis, 2 type alias — ConfidenceLevel/ReliabilityLevel)
      - 표 컬럼: 필드명 · TS 타입 · Python 타입 · D-NN · 설명
      - PoseFrame 필드: frameIndex/frame_index, timestampMs/timestamp_ms, rawLandmarks33/raw_landmarks_33 (D-04), keypoints3D/keypoints_3d, keypoints3DPoleAligned/keypoints_3d_pole_aligned (D-12), keypoints2D/keypoints_2d (D-03), poleExtensionLandmarks/pole_extension_landmarks (D-04), **poleAxis/pole_axis (D-12, H-3)**, **reliability (D-05, H-4)**
      - PoleAxis 필드: axisVector/axis_vector, **confidenceLevel/confidence_level: 'low'|'medium'|'high' (M-2 D-11)**, source: 'detected'|'vertical_fallback' (D-09/D-11), frameIndex/frame_index
      - Landmark3D 필드 표: raw_visibility, raw_presence (M-1 정확 명명 — D-05 박제)
      - D-04 / D-05 / D-11 / D-12 결정 박스 인용 (CONTEXT.md에서 복사 — 출처 명시)
      - "변경 시 lockstep 경고": 3 파일(TS analysis.ts + Python pose_frame.py + 이 문서) 동시 변경.
      - **NOTE (H-4 D-05 박제):** "**reliability='low' 또는 low_reliability=True 마킹된 각도는 coach_writer/dimensions 단정형 코멘트 금지.** 후속 phase가 본 정책을 import해 enforce — 본 phase는 NOTE로 명시." 박스 표시.
      - Firestore 저장 정책: "본 phase에서 PoseFrame은 RunPod 메모리 전용. Firestore 저장은 기존 angles flat 패턴 유지. 키포인트 오버레이 저장은 Phase 12 결정."

    contract.md가 존재 — 기존 sections 보존하고 §6만 추가. §3 angles 표는 인-플레이스 정정.

    `.planning/ROADMAP.md` 갱신 (L-1):
    - line ~62 부근 Phase 1 Success #3 wording을 다음으로 교체:
      `3. 영상에서 폴 축이 자동 검출되고 video-level **PoleAxis가 1개 산출되어 모든 frame의 PoseFrame.pole_axis에 적용되고**, 모든 키포인트 좌표가 폴 기준 좌표계로 변환된 결과를 반환한다 (D-10 — 일반 폴 가정, 스피닝 폴은 v1.5)`
    - 기존 "frame별 PoleAxis가 산출되며" → "video-level PoleAxis가 1개 산출되어 모든 frame의 PoseFrame.pole_axis에 적용되고" 로 정정.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_reliability_gate.py -x -v && grep -nE "angles\\?:\\s*number\\[\\]" /Users/kimtaesung/Dev/SunityMotion/docs/contract.md && grep -n "anglesJointKeys" /Users/kimtaesung/Dev/SunityMotion/docs/contract.md && grep -n "video-level.*PoleAxis.*모든 frame\\|모든 frame.*PoseFrame.pole_axis" /Users/kimtaesung/Dev/SunityMotion/.planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `pytest backend/tests/test_reliability_gate.py -x` exits 0 (4 tests pass)
    - `grep -n "def compute_angle_with_reliability" backend/shared/python/sunity_shared/analysis/reliability.py` matches exactly 1 line
    - `grep -nE "ANGLE_REQUIRED_KEYPOINTS:.*dict" backend/shared/python/sunity_shared/analysis/reliability.py` matches exactly 1 line
    - `grep -nE "low_reliability" backend/shared/python/sunity_shared/analysis/reliability.py | wc -l` outputs at least `2` (게이트 마킹 박제)
    - `grep -n "TODO.*features\\.py.*wiring\\|후속 phase.*enforce" backend/shared/python/sunity_shared/analysis/reliability.py` matches at least 1 line (stub 명시)
    - `grep -nE "angles\\?\\s+number\\[\\]\\s+flat" docs/contract.md | wc -l` outputs at least `1` (M-5 — flat 시퀀스로 정정됨)
    - `grep -cE "number\\[\\]\\[\\]" docs/contract.md` outputs `0` (nested 표기 0)
    - `grep -nE "^## .*§6.*PoseFrame.*PoleAxis|^## §6\\.\\s*PoseFrame" docs/contract.md` matches at least 1 line (H-3 §6 추가됨)
    - `grep -nE "poleAxis|pole_axis" docs/contract.md | wc -l` outputs at least `2` (H-3 PoseFrame 필드 인용)
    - `grep -nE "reliability\\s*[:\\=].*\\(?\\s*'?high'?.*'?medium'?.*'?low'?" docs/contract.md` matches at least 1 line (H-4 enum 명시)
    - `grep -nE "confidenceLevel|confidence_level" docs/contract.md | wc -l` outputs at least `1` (M-2)
    - `grep -nE "raw_visibility|rawVisibility" docs/contract.md | wc -l` outputs at least `1` (M-1)
    - `grep -nE "D-04|D-05|D-11|D-12" docs/contract.md | wc -l` outputs at least `4` (4 decisions cited)
    - `grep -nE "단정형 코멘트 금지|단정 금지" docs/contract.md` matches at least 1 line (H-4 정책 NOTE)
    - `grep -nE "video-level.*PoleAxis|모든 frame.*pole_axis" .planning/ROADMAP.md` matches at least 1 line (L-1 wording 정정됨)
    - `! grep -n "frame별 PoleAxis가 산출" .planning/ROADMAP.md` exits 0 (옛 wording 제거)
  </acceptance_criteria>
  <done>compute_angle_with_reliability stub + ANGLE_REQUIRED_KEYPOINTS 박제. docs/contract.md §3 angles flat 정정 (M-5) + §6 PoseFrame/PoleAxis/reliability 추가 (H-3 H-4 M-1 M-2). ROADMAP Success #3 wording을 video-level PoleAxis로 정정 (L-1).</done>
</task>

<task type="auto">
  <name>Task 3: TS PoseFrame + PoleAxis interface + 테스트 픽스처 (synthetic frames + MediaPipe mock landmarks)</name>
  <files>app/src/types/analysis.ts, backend/tests/fixtures/__init__.py, backend/tests/fixtures/synthetic_frames.py, backend/tests/fixtures/mediapipe_landmarks.py</files>
  <read_first>
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-REVIEWS.md H-3 (poleAxis TS 필드), H-4 (reliability TS 필드), M-1 (rawVisibility/rawPresence TS 명명), M-2 (confidenceLevel enum)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md §Pattern 2 (TS 정의 정확한 형태 인용)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-VALIDATION.md (Wave 0 Requirements 섹션 — 픽스처 의존성)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md (D-04, D-05, D-12, D-11, D-10 — TS doc-comment에 반영)
    - app/src/types/analysis.ts (기존 export 패턴, doc-comment 스타일, lockstep 주석 위치)
    - backend/shared/python/sunity_shared/analysis/pose_frame.py (Task 1에서 생성 — 필드 매핑 source of truth)
    - backend/tests/conftest.py (기존 fixture 패턴)
    - backend/shared/python/sunity_shared/analysis/skeleton.py (COCO-17 KEYPOINT_NAMES 순서)
    - CLAUDE.md Cross-cutting (single source of truth 룰)
  </read_first>
  <action>
    `app/src/types/analysis.ts` 수정:
    - 파일 끝(현재 PROGRESS_SEQUENCE 다음)에 새 섹션 `// ── Pose Engine 데이터 계약 (Phase 1, D-04/D-05/D-11/D-12) ──` 한국어 doc-comment 추가.
    - 새 섹션 상단에 lockstep 주석: "// PoseFrame/PoleAxis는 backend/shared/python/sunity_shared/analysis/pose_frame.py 의 dataclass와 lockstep (CLAUDE.md Cross-cutting). 변경 시 양쪽 + docs/contract.md 모두 갱신. M-1: rawVisibility/rawPresence는 별도 필드 (D-05). H-3: PoseFrame.poleAxis 필수. H-4: PoseFrame.reliability 필수."
    - type alias:
      - `export type ConfidenceLevel = 'high' | 'medium' | 'low';` (M-2 D-11)
      - `export type ReliabilityLevel = 'high' | 'medium' | 'low';` (H-4)
      - `export type PoleAxisSource = 'detected' | 'vertical_fallback';` (D-09/D-11)
    - `Landmark3D` interface: fields `x: number; y: number; z: number; rawVisibility: number; rawPresence: number;` (M-1). 주석: "MediaPipe 33 raw landmark — D-05: raw_visibility/raw_presence 별도 필드".
    - `Keypoint3D` interface: `x: number; y: number; z: number; confidence: number; uncertaintyProxy: number;`. 주석: "COCO-17 keypoint. confidence = rawVisibility × rawPresence (D-05). uncertaintyProxy = 1 - confidence".
    - `Keypoint3DAligned` interface: `x: number; y: number; z: number;`. 주석: "폴 축 정렬된 좌표 (D-12). 신뢰도는 keypoints3D의 같은 키로 lookup".
    - `Keypoint2D` interface: `x: number; y: number; visibility: number;`. 주석: "image normalized 0~1 (D-03)".
    - `PoleExtensionLandmark` interface: `x: number; y: number; z: number; confidence: number;`. 주석: "폴 확장 — toe/heel/grip (D-04)".
    - `PoleAxis` interface: (H-3 + M-2)
      - `axisVector: { x: number; y: number; z: number };` (단위 벡터)
      - `confidenceLevel: ConfidenceLevel;` (M-2 D-11)
      - `source: PoleAxisSource;` (D-09/D-11)
      - `frameIndex?: number | null;` (D-10 — null이면 video-level)
    - `PoseFrame` interface (camelCase 필드 — Python snake_case와 1:1 매핑):
      - `frameIndex: number;`
      - `timestampMs: number;`
      - `rawLandmarks33?: Record<string, Landmark3D>;` (D-04 원본 보존)
      - `keypoints3D: Record<string, Keypoint3D>;` (COCO-17, 스코어링 계약)
      - `keypoints3DPoleAligned: Record<string, Keypoint3DAligned>;` (D-12, 분석에 사용)
      - `keypoints2D?: Record<string, Keypoint2D>;` (D-03)
      - `poleExtensionLandmarks?: Record<string, PoleExtensionLandmark>;` (D-04)
      - `poleAxis: PoleAxis | null;` (H-3 D-12 — frame-level metadata, video-level pole_axis 공유)
      - `reliability: ReliabilityLevel;` (H-4 D-05 — frame-level 게이트)
    - `AnalysisDoc`, `ReferenceMotion` 등 기존 export는 건드리지 않음 (Pattern 2 결정).

    `backend/tests/fixtures/__init__.py` 신규 생성:
    - 빈 파일 + 한 줄 docstring: "Phase 1 test fixtures — synthetic frames + MediaPipe mock landmarks. Wave 1·2 단위 테스트가 import."

    `backend/tests/fixtures/synthetic_frames.py` 신규 생성:
    - module docstring: "Hough 폴 검출 + 어댑터 단위 테스트용 합성 프레임 (T,H,W,3) RGB uint8. 무거운 의존성 없음 — numpy only."
    - `import numpy as np`
    - `def synthetic_vertical_pole_frames(num_frames: int = 3, height: int = 128, width: int = 64, pole_x: int | None = None) -> np.ndarray`: 배경 흰색(255), 수직 폴 띠(좌우 5px, 그레이 80) — Hough 검출 명확.
    - `def synthetic_no_pole_frames(num_frames: int = 3, height: int = 128, width: int = 64) -> np.ndarray`: 균일 흰색 — Hough 검출 실패 케이스.
    - `def synthetic_tilted_pole_frames(num_frames: int = 3, height: int = 128, width: int = 64, tilt_deg: float = 30.0) -> np.ndarray`: 기울어진 폴 — vertical_tolerance 5° 밖.
    - 모든 함수에서 return 직전 `assert arr.dtype == np.uint8 and arr.ndim == 4 and arr.shape[3] == 3`.

    `backend/tests/fixtures/mediapipe_landmarks.py` 신규 생성:
    - module docstring: "MediaPipe PoseLandmarker mock result. detect_for_video 호출 mock — 로컬 macOS(mediapipe wheel 부재)에서도 어댑터 단위 테스트 가능."
    - `from dataclasses import dataclass` + `from typing import Sequence`.
    - `@dataclass\nclass _MockLandmark`: fields `x: float, y: float, z: float, visibility: float, presence: float`. (mediapipe library의 raw landmark 이름은 visibility/presence이고 어댑터 변환 단계에서 raw_visibility/raw_presence로 정정 매핑 — fixture는 library API 형태 유지)
    - `def make_mock_world_landmarks_33(visible_indices: Sequence[int] | None = None, default_visibility: float = 0.9, default_presence: float = 0.9) -> list`: 33개 mock landmark. visible_indices 명시 시 그 외는 visibility/presence=0.1.
    - `def make_mock_image_landmarks_33(default_visibility: float = 0.9) -> list`: normalized 0~1.
    - `def make_mock_detection_result(world_landmarks_33=None, image_landmarks_33=None) -> object`: MediaPipe DetectionResult duck-type 객체.
    - `def make_mock_no_human_result() -> object`: pose_landmarks=[], pose_world_landmarks=[] (NoHumanError 트리거).

    이 fixtures는 Wave 1·2 모든 단위 테스트에서 import.
  </action>
  <verify>
    <automated>cd app && npm run typecheck && cd ../backend && python -c "from tests.fixtures.synthetic_frames import synthetic_vertical_pole_frames, synthetic_no_pole_frames; import numpy as np; arr = synthetic_vertical_pole_frames(); assert arr.shape == (3, 128, 64, 3) and arr.dtype == np.uint8; print('synthetic OK')" && python -c "from tests.fixtures.mediapipe_landmarks import make_mock_world_landmarks_33, make_mock_no_human_result; lm = make_mock_world_landmarks_33(); assert len(lm) == 33; r = make_mock_no_human_result(); assert r.pose_landmarks == []; print('mock OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export interface PoseFrame" app/src/types/analysis.ts` matches exactly one line
    - `grep -n "export interface PoleAxis" app/src/types/analysis.ts` matches exactly one line
    - `grep -nE "export type ConfidenceLevel\\s*=\\s*'high'\\s*\\|\\s*'medium'\\s*\\|\\s*'low'" app/src/types/analysis.ts` matches exactly 1 line (M-2)
    - `grep -nE "export type ReliabilityLevel\\s*=\\s*'high'\\s*\\|\\s*'medium'\\s*\\|\\s*'low'" app/src/types/analysis.ts` matches exactly 1 line (H-4)
    - `grep -nE "export type PoleAxisSource\\s*=\\s*'detected'\\s*\\|\\s*'vertical_fallback'" app/src/types/analysis.ts` matches exactly 1 line (H-3 reviewer 명세)
    - `grep -nE "rawVisibility:\\s*number" app/src/types/analysis.ts` matches at least 1 line (M-1)
    - `grep -nE "rawPresence:\\s*number" app/src/types/analysis.ts` matches at least 1 line (M-1)
    - `grep -nE "poleAxis:\\s*PoleAxis\\s*\\|\\s*null" app/src/types/analysis.ts` matches at least 1 line (H-3)
    - `grep -nE "reliability:\\s*ReliabilityLevel" app/src/types/analysis.ts` matches at least 1 line (H-4)
    - `grep -nE "confidenceLevel:\\s*ConfidenceLevel" app/src/types/analysis.ts` matches at least 1 line (M-2 PoleAxis 필드)
    - `grep -nE "rawLandmarks33|keypoints3D|keypoints3DPoleAligned|keypoints2D|poleExtensionLandmarks" app/src/types/analysis.ts | wc -l` outputs at least `5` (5 distinct field references)
    - `cd app && npm run typecheck` exits 0 (no TS errors)
    - `python -c "from tests.fixtures.synthetic_frames import synthetic_vertical_pole_frames; print(synthetic_vertical_pole_frames().shape)"` outputs `(3, 128, 64, 3)`
    - `python -c "from tests.fixtures.mediapipe_landmarks import make_mock_world_landmarks_33; print(len(make_mock_world_landmarks_33()))"` outputs `33`
    - `grep -nE "def synthetic_vertical_pole_frames|def synthetic_no_pole_frames|def synthetic_tilted_pole_frames" backend/tests/fixtures/synthetic_frames.py` matches 3 functions
    - `grep -nE "def make_mock_world_landmarks_33|def make_mock_image_landmarks_33|def make_mock_detection_result|def make_mock_no_human_result" backend/tests/fixtures/mediapipe_landmarks.py` matches 4 functions
  </acceptance_criteria>
  <done>TS PoseFrame + PoleAxis + ConfidenceLevel + ReliabilityLevel + PoleAxisSource interface 추가. npm run typecheck 통과. Python dataclass 필드와 1:1 매핑 (snake_case↔camelCase). M-1/M-2/H-3/H-4 모두 박제. Wave 1·2가 import할 fixture 2개 모듈 + __init__.py 작성. 모듈 import 자체가 sanity check 통과.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 데이터 계약 ↔ 다운스트림 | TS/Python/contract.md lockstep 위반 시 silent 미스매치 발생. 본 plan이 lockstep 강제 |
| 테스트 픽스처 ↔ 실 데이터 | 합성 fixture가 실 MediaPipe 출력 형태를 정확히 모사하지 못하면 Wave 1 단위 테스트가 실 행동을 보장 못함 |
| 다운스트림 단정 게이트 | reliability='low' 마킹된 frame이 coach_writer/dimensions에서 단정형 코멘트 생성 시 위양성 위험. 본 plan의 reliability 필드 + NOTE가 일관 적용 기반 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-04 | Tampering | PoseFrame 계약 (TS/Python 분기) | mitigate | docs/contract.md lockstep 강제 + Task 3 acceptance에 양쪽 grep 검증 + Task 1 lockstep field set 테스트 |
| T-1-05 | Information disclosure | Confidence 값 0~1 범위 위반 | mitigate | `PoseFrame.compute_confidence` 정적 validator + dataclass `__post_init__`에서 ValueError |
| T-1-06 | Spoofing | source / confidence_level / reliability literal 외 값 주입 | mitigate | Literal 타입 + `PoleAxis.__post_init__` / PoseFrame `__post_init__`에서 set membership 검증 |
| T-1-19 | Information disclosure | low-reliability frame이 단정형 코멘트로 사용자에게 전달 (D-05 정책 위반) | mitigate | PoseFrame.reliability 필드 + compute_angle_with_reliability stub + contract.md NOTE — 후속 phase가 enforce |
</threat_model>

<verification>
- `pytest backend/tests/test_pose_frame_contract.py backend/tests/test_reliability_gate.py -x` exits 0 (12 tests pass)
- `cd app && npm run typecheck` exits 0
- Fixture import sanity: synthetic_frames + mediapipe_landmarks 둘 다 module-level import 시 ImportError 없음
- 3-way lockstep: TS interface 필드 set ↔ Python dataclass field set ↔ docs/contract.md §6 표 — 9개 PoseFrame 필드 + 4개 PoleAxis 필드 + ConfidenceLevel/ReliabilityLevel enum 동일
- ROADMAP Success #3 wording이 D-10 (video-level) 반영 (L-1)
- contract.md §3 angles 시그니처가 flat number[] (M-5)
</verification>

<success_criteria>
- PoseFrame + PoleAxis + ReliabilityLevel + ConfidenceLevel 데이터 계약 3-way lockstep 완성
- raw_visibility / raw_presence 정확 명명 (M-1)
- PoleAxis.confidence_level enum (M-2)
- PoseFrame.pole_axis 필드 (H-3 D-12)
- PoseFrame.reliability 필드 + compute_angle_with_reliability stub (H-4 Success #4)
- docs/contract.md §3 angles flat 정정 (M-5)
- docs/contract.md §6 신규 + low-reliability 단정 금지 NOTE (H-4)
- ROADMAP Success #3 video-level PoleAxis wording (L-1)
- Wave 1·2가 의존하는 합성 fixture 2개 모듈
- 12개 contract+gate 테스트 통과
- D-04, D-05, D-11, D-12 결정이 코드 + 주석에 명시적으로 인용
- 외부 무거운 라이브러리(mediapipe, opencv, scipy) 의존성 0
</success_criteria>

<output>
Create `.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-01-SUMMARY.md` when done. SUMMARY 내용:
- 생성된 파일 목록 + 라인 수
- 12개 contract+gate 테스트 결과 (PASS/FAIL)
- TS typecheck 결과
- Wave 1·2에 노출되는 public API (PoseFrame, PoleAxis, ConfidenceLevel, ReliabilityLevel, compute_angle_with_reliability, fixtures functions)
- REVIEWS H-3/H-4/M-1/M-2/M-5/L-1 박제 확인 grep 결과
- belle 확정 보류 항목: A1 (MediaPipe→COCO-17 매핑 공식 검증) — Wave 1 Task 1에서 수동 검증할 것임을 명시
- reliability threshold (HIGH=0.7, MEDIUM=0.4)는 defensible default — belle 검토 후 갱신 가능
</output>
