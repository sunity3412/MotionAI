---
phase: 01-poseengine-mediapipe-nlf-r-d
plan: 02
type: execute
wave: 1
depends_on:
  - 01-01
files_modified:
  - backend/shared/python/sunity_shared/analysis/pose_engines/__init__.py
  - backend/shared/python/sunity_shared/analysis/pose_engines/mediapipe_engine.py
  - backend/shared/python/sunity_shared/analysis/adapters/__init__.py
  - backend/shared/python/sunity_shared/analysis/adapters/mediapipe_to_coco17.py
  - backend/shared/python/sunity_shared/analysis/interfaces.py
  - backend/tests/test_pose_engine_interface.py
  - backend/tests/test_adapter_mediapipe_to_coco17.py
autonomous: true
requirements:
  - POSE-01
must_haves:
  truths:
    - "PoseEngine Protocol이 estimate(frames, pole_axis) -> list[PoseFrame] 시그니처를 강제한다"
    - "MediaPipePoseEngine이 mock된 MediaPipe Tasks API 호출을 받아 PoseFrame 리스트를 반환한다 (D-01/D-02/D-03)"
    - "MediaPipe33ToCOCO17Adapter가 33 landmark에서 17 COCO + 폴 확장 (toe/heel + pole_grip_left/pole_grip_right per M-3 D-04)을 정확한 인덱스로 추출한다"
    - "어댑터가 mediapipe library의 raw visibility/presence를 PoseFrame Landmark3D.raw_visibility/raw_presence로 정확히 매핑한다 (M-1)"
    - "어댑터가 frame-level reliability를 mean visibility 기반으로 산출해 PoseFrame.reliability에 설정한다 (H-4)"
    - "어댑터가 pole_axis 인자를 받아 PoseFrame.pole_axis에 보존한다 (H-3 D-12)"
    - "전 프레임 미감지 시 NoHumanError 발생, contract no_human 매핑 보존"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/pose_engines/__init__.py"
      provides: "PoseEngine Protocol export"
    - path: "backend/shared/python/sunity_shared/analysis/pose_engines/mediapipe_engine.py"
      provides: "MediaPipePoseEngine 어댑터 (lazy import mediapipe)"
      contains: "class MediaPipePoseEngine"
    - path: "backend/shared/python/sunity_shared/analysis/adapters/mediapipe_to_coco17.py"
      provides: "33→COCO-17 + 폴 확장 (toe/heel + pole_grip_left/pole_grip_right) 매핑"
      contains: "MEDIAPIPE_33_TO_COCO17"
  key_links:
    - from: "pose_engines/mediapipe_engine.py"
      to: "adapters/mediapipe_to_coco17.py"
      via: "convert_landmarks_to_coco17_and_pole_ext 호출 + pole_axis 인자 전달 (H-3)"
      pattern: "from ..adapters.mediapipe_to_coco17"
    - from: "interfaces.py"
      to: "pose_engines"
      via: "PoseEngine Protocol 정의 (PoseEstimator는 deprecated 표시)"
      pattern: "class PoseEngine"
---

<objective>
PoseEngine Protocol(estimate→list[PoseFrame])을 interfaces.py에 추가하고, MediaPipePoseEngine 어댑터 + MediaPipe33→COCO17 변환 어댑터를 구현한다. mediapipe 라이브러리는 lazy import — 로컬 테스트는 mock으로 진행. 본 plan은 REVIEWS M-1 (raw_visibility/raw_presence 어댑터 매핑) + M-3 (pole_grip_left/pole_grip_right 추가) + H-3 (pole_axis 어댑터 보존) + H-4 (reliability 어댑터 계산)를 박제. Plan 03(PoleDetector)과 병렬 실행 가능 (파일 겹침 없음).

Purpose: D-01(Heavy), D-02(Tasks API), D-03(world + image), D-04(33 원본 보존 + COCO-17 + grip 확장 — M-3), D-05(confidence + raw_visibility/raw_presence + reliability 정책)을 코드로 박제. RESEARCH §Pattern 1·3 + Code Example 1 구현. A1 (MediaPipe→COCO-17 + grip 매핑) belle 확정 필요 — 본 plan에서 명시적 표 + 단위 테스트로 박제 + 향후 belle 확정 후 갱신 절차 주석.

Output: PoseEngine Protocol + MediaPipePoseEngine + 어댑터 + 단위 테스트 2개 모듈.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-VALIDATION.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-REVIEWS.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-01-SUMMARY.md
@backend/shared/python/sunity_shared/analysis/interfaces.py
@backend/shared/python/sunity_shared/analysis/pose_frame.py
@backend/shared/python/sunity_shared/analysis/skeleton.py
@backend/tests/fixtures/mediapipe_landmarks.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PoseEngine Protocol + 33→COCO-17 + grip 확장 어댑터 (M-3 D-04 박제)</name>
  <files>backend/shared/python/sunity_shared/analysis/interfaces.py, backend/shared/python/sunity_shared/analysis/adapters/__init__.py, backend/shared/python/sunity_shared/analysis/adapters/mediapipe_to_coco17.py, backend/tests/test_adapter_mediapipe_to_coco17.py</files>
  <read_first>
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-REVIEWS.md M-1 (어댑터 raw_visibility/raw_presence 명명), M-3 (pole_grip_left/pole_grip_right 추가), H-3 (어댑터가 pole_axis 보존), H-4 (어댑터가 reliability 산출)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md §Pattern 1 (PoseEngine 시그니처 권장 — estimate 반환 list[PoseFrame], pole_axis 인자)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md §Pattern 3 (33→COCO-17 매핑 표 17개 + 폴 확장 매핑 표)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md D-04, D-05 (변환식 + 폴 확장 인덱스 — A1 belle 확정 필요 명시)
    - backend/shared/python/sunity_shared/analysis/interfaces.py (기존 PoseEstimator Protocol + NoHumanError — 보존하되 deprecated 마킹)
    - backend/shared/python/sunity_shared/analysis/skeleton.py (KEYPOINT_NAMES 17개 정확한 순서)
    - backend/shared/python/sunity_shared/analysis/pose_frame.py (Landmark3D with raw_visibility/raw_presence, Keypoint3D, PoseFrame.compute_confidence, compute_frame_reliability)
    - backend/tests/fixtures/mediapipe_landmarks.py (Wave 0 fixture)
  </read_first>
  <behavior>
    - Test 1 (test_coco17_index_mapping_exact): MEDIAPIPE_33_TO_COCO17 dict 정확히 17 엔트리. 매핑 = {nose:0, left_eye:2, right_eye:5, left_ear:7, right_ear:8, left_shoulder:11, right_shoulder:12, left_elbow:13, right_elbow:14, left_wrist:15, right_wrist:16, left_hip:23, right_hip:24, left_knee:25, right_knee:26, left_ankle:27, right_ankle:28}
    - Test 2 (test_pole_extension_includes_toe_heel_grip): POLE_EXTENSION_MAP 6 엔트리 = {left_heel:29, right_heel:30, left_foot_index:31, right_foot_index:32, pole_grip_left: derived, pole_grip_right: derived}. left_heel/right_heel/left_foot_index/right_foot_index는 raw MediaPipe 인덱스. pole_grip_left/pole_grip_right는 derived landmark (M-3 D-04)
    - Test 3 (test_pole_grip_derived_from_wrist_pinky_thumb): make_mock_world_landmarks_33 입력 → convert 후 pose_extension_landmarks['pole_grip_left'] 위치가 left_wrist(15) + left_pinky(17) + left_thumb(21) 좌표 평균. 정확한 인덱스: GRIP_LEFT_SOURCE_INDICES = [15, 17, 21], GRIP_RIGHT_SOURCE_INDICES = [16, 18, 22]
    - Test 4 (test_convert_landmarks_basic): mock 33 landmarks (visible_indices=range(33)) → convert_landmarks_to_coco17_and_pole_ext 호출 → PoseFrame 반환. keypoints_3d에 정확히 17 키, raw_landmarks_33에 33 키. 각 Keypoint3D.confidence == raw_visibility × raw_presence = 0.81 (default 0.9 × 0.9).
    - Test 5 (test_convert_raw_visibility_presence_named_correctly): mock landmark의 visibility=0.8, presence=0.7 → PoseFrame.raw_landmarks_33[name].raw_visibility == 0.8, raw_presence == 0.7. bare visibility/presence 속성 없음 (M-1 박제).
    - Test 6 (test_convert_pole_extension_present): visible_indices = {29, 30, 31, 32, 15, 17, 21, 16, 18, 22} (heel + toe + grip source 모두) → pose_extension_landmarks에 6개 키 존재 (left_heel, right_heel, left_foot_index, right_foot_index, pole_grip_left, pole_grip_right), 각 confidence > 0.
    - Test 7 (test_convert_low_confidence_keypoint_marked): visible_indices=[5,12,14,16,24,26,28] → keypoints_3d[left_elbow].uncertainty_proxy > 0.9 (low confidence).
    - Test 8 (test_pole_axis_preserved_in_pose_frame): pole_axis = PoleAxis((0,0,1), confidence_level='high', source='detected') 주입 → 결과 PoseFrame.pole_axis is 그 PoleAxis 인스턴스 (또는 같은 axis_vector/confidence_level/source) (H-3 박제)
    - Test 9 (test_reliability_computed_from_mean_visibility): visible_indices=range(33) (모두 0.9) → PoseFrame.reliability == 'high'. visible_indices=[0,1,2] (나머지 0.1) → reliability == 'low'. (H-4 박제)
    - Test 10 (test_pole_alignment_applied_when_aligner_available): pole_axis=PoleAxis((0,0,1), 'high', 'detected') 주입 시 keypoints_3d_pole_aligned가 비어있지 않음 + identity 회전이면 raw와 동일. pole/aligner 부재 시 pytest.importorskip로 skip.
  </behavior>
  <action>
    `backend/shared/python/sunity_shared/analysis/interfaces.py` 수정:
    - 기존 PoseEstimator Protocol 보존. 그 위에 `# DEPRECATED: PoseEngine으로 마이그레이션 중 (Phase 1 D-04). 신규 코드는 PoseEngine 사용.` 한 줄 주석 추가.
    - 파일 끝에 `PoseEngine` Protocol 추가:
      `class PoseEngine(Protocol):` docstring + `def estimate(self, frames: np.ndarray, pole_axis: 'PoleAxis') -> list['PoseFrame']: ...`
      `from __future__ import annotations` + TYPE_CHECKING 블록으로 PoseFrame/PoleAxis import (circular 회피).
    - NoHumanError, NotPoleMotionError, FallbackCoachWriter 그대로 보존.

    `backend/shared/python/sunity_shared/analysis/adapters/__init__.py` 신규 생성:
    - module docstring: "스키마 변환 어댑터 (33→COCO-17, SMPL-X→COCO-17 등). RESEARCH §Pattern 3 / Phase 1 D-04 / REVIEWS M-3 grip 확장."
    - `from .mediapipe_to_coco17 import convert_landmarks_to_coco17_and_pole_ext, MEDIAPIPE_33_TO_COCO17, POLE_EXTENSION_MAP, GRIP_LEFT_SOURCE_INDICES, GRIP_RIGHT_SOURCE_INDICES`

    `backend/shared/python/sunity_shared/analysis/adapters/mediapipe_to_coco17.py` 신규 생성:
    - module docstring: "MediaPipe 33 landmark → COCO-17 + 폴 확장 (toe/heel/grip) 변환 (D-04 + REVIEWS M-3 grip). A1: 매핑 공식 belle 확정 보류 — 향후 갱신."
    - `from __future__ import annotations`, `from typing import Any`, `import numpy as np`.
    - import: `from ..pose_frame import PoseFrame, Landmark3D, Keypoint3D, Keypoint3DAligned, Keypoint2D, PoseExtensionLandmark, PoleAxis, compute_frame_reliability`, `from ..skeleton import KEYPOINT_NAMES`. pole/aligner import는 lazy (호출 시점에 import, try/except 가드).

    상수:
    - `MEDIAPIPE_33_TO_COCO17: dict[str, int]` — 17 엔트리, Test 1 명시 매핑
    - `GRIP_LEFT_SOURCE_INDICES: tuple[int, ...] = (15, 17, 21)` (left_wrist, left_pinky, left_thumb — M-3 / A2 belle 확정 필요)
    - `GRIP_RIGHT_SOURCE_INDICES: tuple[int, ...] = (16, 18, 22)` (right_wrist, right_pinky, right_thumb)
    - `POLE_EXTENSION_MAP: dict[str, int | tuple[int, ...]]` — 6 엔트리. raw landmark 직접 인덱스: {'left_heel': 29, 'right_heel': 30, 'left_foot_index': 31, 'right_foot_index': 32}. derived: {'pole_grip_left': GRIP_LEFT_SOURCE_INDICES, 'pole_grip_right': GRIP_RIGHT_SOURCE_INDICES} (tuple 값은 derived 표시). 주석: "// TODO belle confirm grip derivation — A2 항목, 정확한 source 인덱스는 추후 갱신."
    - `MEDIAPIPE_33_LANDMARK_NAMES: tuple[str, ...]` — 33개 명칭. 출처 주석: "RESEARCH §Pattern 3 + ai.google.dev/edge/mediapipe/solutions/vision/pose_landmarker — A1 belle 확정 권장".

    함수:
    - `def _landmark_to_dict(lm: Any) -> Landmark3D`: MediaPipe NormalizedLandmark/Landmark duck-typed 객체에서 x, y, z, visibility(→raw_visibility), presence(→raw_presence) 추출. presence 부재 시 raw_presence=visibility로 폴백 (D-05 폴백). (M-1 박제: PoseFrame Landmark3D는 raw_visibility/raw_presence 명명, mediapipe library는 visibility/presence 명명 — 본 어댑터에서 정확히 변환).
    - `def _build_keypoint3d(lm: Any) -> Keypoint3D`: confidence = raw_visibility × raw_presence + uncertainty_proxy = 1 - confidence + xyz 그대로. `PoseFrame.compute_confidence` 사용.
    - `def _build_pole_extension_raw(lm: Any) -> PoseExtensionLandmark`: confidence = raw_visibility × raw_presence (raw landmark 직접 인덱스 케이스).
    - `def _build_pole_grip(landmarks: list, source_indices: tuple[int, ...]) -> PoseExtensionLandmark`: source 인덱스의 좌표 평균 + confidence는 source confidence들의 평균 (raw_visibility × raw_presence each).
    - `def _compute_mean_visibility(world_landmarks: list) -> float`: 33 landmark의 raw_visibility 평균 (frame-level reliability 산출 input — H-4).
    - `def convert_landmarks_to_coco17_and_pole_ext(frame_index: int, timestamp_ms: int, world_landmarks: list, image_landmarks: list | None, pole_axis: PoleAxis) -> PoseFrame`:
      1) `raw_landmarks_33`: enumerate(world_landmarks, name=MEDIAPIPE_33_LANDMARK_NAMES[idx]) → Landmark3D dict
      2) `keypoints_3d`: for coco_name, mp_idx in MEDIAPIPE_33_TO_COCO17 → world_landmarks[mp_idx] → Keypoint3D
      3) `pole_extension_landmarks`: POLE_EXTENSION_MAP iteration. int 값(=raw 인덱스)은 _build_pole_extension_raw 호출. tuple 값(=derived)은 _build_pole_grip 호출.
      4) `keypoints_2d` (image_landmarks 주어졌을 때): for coco_name, mp_idx in MEDIAPIPE_33_TO_COCO17 → Keypoint2D (x, y, visibility) — z는 image에서 무시
      5) `keypoints_3d_pole_aligned`: lazy import pole.aligner. import 가능하면 compute_alignment_matrix(pole_axis.axis_vector) → apply_alignment(keypoints_3d, R) → dict. import 불가(Plan 03 stub 단계)이면 keypoints_3d를 그대로 복사해 Keypoint3DAligned로 변환 (xyz만, fallback).
      6) `reliability`: compute_frame_reliability(_compute_mean_visibility(world_landmarks)) (H-4 박제).
      7) `pole_axis`: 인자 그대로 PoseFrame에 보존 (H-3 박제 — D-12).
      8) PoseFrame(...)를 반환.

    `backend/tests/test_adapter_mediapipe_to_coco17.py` 신규 생성:
    - `from tests.fixtures.mediapipe_landmarks import make_mock_world_landmarks_33, make_mock_image_landmarks_33`
    - `from sunity_shared.analysis.adapters.mediapipe_to_coco17 import convert_landmarks_to_coco17_and_pole_ext, MEDIAPIPE_33_TO_COCO17, POLE_EXTENSION_MAP, GRIP_LEFT_SOURCE_INDICES, GRIP_RIGHT_SOURCE_INDICES`
    - `from sunity_shared.analysis.pose_frame import PoleAxis, Keypoint3D`
    - `from sunity_shared.analysis.skeleton import KEYPOINT_NAMES`
    - Test 1~10 위 behavior 그대로 구현. Test 10은 `pytest.importorskip("sunity_shared.analysis.pole.aligner")` 가드.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_adapter_mediapipe_to_coco17.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `pytest backend/tests/test_adapter_mediapipe_to_coco17.py -x` exits 0 (10 tests pass, Test 10은 pole.aligner 부재 시 skip)
    - `python -c "from sunity_shared.analysis.adapters.mediapipe_to_coco17 import MEDIAPIPE_33_TO_COCO17; assert len(MEDIAPIPE_33_TO_COCO17) == 17 and MEDIAPIPE_33_TO_COCO17['nose'] == 0 and MEDIAPIPE_33_TO_COCO17['right_ankle'] == 28"` exits 0
    - `python -c "from sunity_shared.analysis.adapters.mediapipe_to_coco17 import POLE_EXTENSION_MAP; assert 'pole_grip_left' in POLE_EXTENSION_MAP and 'pole_grip_right' in POLE_EXTENSION_MAP and POLE_EXTENSION_MAP['left_heel'] == 29 and POLE_EXTENSION_MAP['right_foot_index'] == 32"` exits 0 (M-3 grip 박제)
    - `python -c "from sunity_shared.analysis.adapters.mediapipe_to_coco17 import GRIP_LEFT_SOURCE_INDICES, GRIP_RIGHT_SOURCE_INDICES; assert GRIP_LEFT_SOURCE_INDICES == (15, 17, 21) and GRIP_RIGHT_SOURCE_INDICES == (16, 18, 22)"` exits 0
    - `python -c "from sunity_shared.analysis.interfaces import PoseEngine; import inspect; sig = inspect.signature(PoseEngine.estimate); assert 'frames' in sig.parameters and 'pole_axis' in sig.parameters"` exits 0
    - `grep -nE "raw_visibility.*=|raw_presence.*=" backend/shared/python/sunity_shared/analysis/adapters/mediapipe_to_coco17.py | wc -l` outputs at least `2` (M-1 — 어댑터가 raw_ prefix로 변환)
    - `grep -n "pole_grip_left\\|pole_grip_right" backend/shared/python/sunity_shared/analysis/adapters/mediapipe_to_coco17.py | wc -l` outputs at least `2` (M-3)
    - `grep -nE "MEDIAPIPE_33_LANDMARK_NAMES.*=" backend/shared/python/sunity_shared/analysis/adapters/mediapipe_to_coco17.py` matches one tuple/list definition with 33 entries
    - `grep -nE "A1.*belle 확정|belle 확정.*A1|A2.*belle confirm|belle confirm.*A2" backend/shared/python/sunity_shared/analysis/adapters/mediapipe_to_coco17.py | wc -l` outputs at least `2` (A1 + A2 limitation 명시)
    - `grep -nE "compute_frame_reliability|reliability=" backend/shared/python/sunity_shared/analysis/adapters/mediapipe_to_coco17.py | wc -l` outputs at least `1` (H-4)
    - `grep -nE "pole_axis=pole_axis|pole_axis=" backend/shared/python/sunity_shared/analysis/adapters/mediapipe_to_coco17.py | wc -l` outputs at least `1` (H-3)
  </acceptance_criteria>
  <done>PoseEngine Protocol + MediaPipe 33→COCO-17 + 폴 확장 (toe/heel + pole_grip_left/pole_grip_right) 매핑 박제. 10개 단위 테스트 통과 (Test 10은 Plan 03 완료 후 통과). M-1 raw_visibility/raw_presence 어댑터 매핑, M-3 grip derived, H-3 pole_axis 보존, H-4 reliability 산출 모두 박제.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MediaPipePoseEngine 어댑터 (lazy import + factory + mock 테스트)</name>
  <files>backend/shared/python/sunity_shared/analysis/pose_engines/__init__.py, backend/shared/python/sunity_shared/analysis/pose_engines/mediapipe_engine.py, backend/tests/test_pose_engine_interface.py</files>
  <read_first>
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-REVIEWS.md H-2 (모듈 상단 import 금지 — class __init__ 내부 lazy import)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md §Code Examples Example 1 (MediaPipePoseEngine 골격)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md §Pitfall 1 (Lambda ARM64 미지원 — fail-fast)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md §Pitfall 2 (timestamp monotonic)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md §Pitfall 4 (protobuf 충돌 — try/except import)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md D-01, D-02, D-03 (Heavy, Tasks API, world+image)
    - backend/shared/python/sunity_shared/analysis/pose_frame.py (PoseFrame.empty 사용 + pole_axis 인자)
    - backend/shared/python/sunity_shared/analysis/adapters/mediapipe_to_coco17.py (Task 1 산출물)
    - backend/tests/fixtures/mediapipe_landmarks.py (mock DetectionResult)
    - backend/shared/python/sunity_shared/analysis/interfaces.py (Task 1에서 PoseEngine Protocol 추가)
  </read_first>
  <behavior>
    - Test 1 (test_engine_estimate_returns_pose_frames): mock PoseLandmarker (detect_for_video → make_mock_detection_result with 33 visible) 주입 → 3 frame 입력 → list[PoseFrame] 길이 3 반환. frame_index = 0,1,2. timestamp_ms = 0, 111, 222 (target_fps=9 → 1000/9 ≈ 111.11 → int).
    - Test 2 (test_engine_propagates_pole_axis_to_pose_frame): estimate(frames, pole_axis=PoleAxis((0,0,1), 'high', 'detected')) → 모든 반환 PoseFrame.pole_axis가 같은 PoleAxis 인스턴스 (H-3)
    - Test 3 (test_engine_no_human_error): 모든 detect_for_video 호출이 make_mock_no_human_result() → NoHumanError 발생 + message에 "MediaPipe" 포함.
    - Test 4 (test_engine_partial_detection_sentinel): frame 0 = detection, frame 1 = no_human, frame 2 = detection → list 길이 3 (PoseFrame.empty for frame 1 — pole_axis 인자 전달) + NoHumanError 미발생.
    - Test 5 (test_engine_timestamp_monotonic): 10 frame estimate → timestamps = [0,111,222,333,444,555,666,777,888,1000]. monotonic_increasing.
    - Test 6 (test_engine_model_path_missing): MEDIAPIPE_POSE_MODEL_PATH 없는 경로 + model_path=None → RuntimeError + message에 "pose_landmarker_heavy.task" 포함.
    - Test 7 (test_engine_lazy_import_no_mediapipe): mediapipe 라이브러리 미설치 환경 시뮬레이션 → MediaPipePoseEngine() 생성 시 RuntimeError (또는 ImportError 매핑) + 메시지에 "RunPod" 또는 "mediapipe" 안내. (H-2 박제)
    - Test 8 (test_module_import_without_mediapipe): `import sunity_shared.analysis.pose_engines.mediapipe_engine`만 (인스턴스화 없이)으로는 mediapipe import 시도하지 않음 — module load 성공 (H-2 박제)
    - Test 9 (test_confidence_conversion_in_engine): mock landmark visibility=0.8, presence=0.7 → engine 호출 후 frame.keypoints_3d['nose'].confidence ≈ 0.56 ± 1e-6 (D-05 + 어댑터 통합).
    - Test 10 (test_reliability_set_in_engine): mock 33 visible → engine 호출 결과 PoseFrame.reliability == 'high'. (H-4 통합)
  </behavior>
  <action>
    `backend/shared/python/sunity_shared/analysis/pose_engines/__init__.py` 신규 생성:
    - module docstring: "포즈 엔진 어댑터 모음 (MediaPipe 제품, NLF는 R&D 격리 — backend/research에 별도). Phase 1 D-06/D-07."
    - `from ..interfaces import PoseEngine, NoHumanError` (re-export)
    - `MediaPipePoseEngine`은 lazy export — `def __getattr__(name: str)` 패턴으로 접근 시 import (lazy module load — H-2)

    `backend/shared/python/sunity_shared/analysis/pose_engines/mediapipe_engine.py` 신규 생성:
    - RESEARCH §Code Example 1 골격 + H-2 박제 (NO module-level heavy imports):
      - `from __future__ import annotations` + module docstring (D-01, D-02, D-03, D-04, D-05 + Pitfall 1·2·4 + REVIEWS H-2 인용)
      - module-level imports — light only: `import os`, `from pathlib import Path`, `import numpy as np`, `from ..interfaces import NoHumanError`, `from ..pose_frame import PoseFrame, PoleAxis`, `from ..adapters.mediapipe_to_coco17 import convert_landmarks_to_coco17_and_pole_ext`. mediapipe, cv2, scipy 등 heavy library는 module level에서 import 금지 (H-2 박제). 
      - class `MediaPipePoseEngine`:
        - `__init__(self, model_path: str | None = None, target_fps: float = 9.0)`:
          - mediapipe library는 본 __init__ 내부에서만 lazy import: `try: import mediapipe as mp except ImportError as e: raise RuntimeError("mediapipe library 미설치 — RunPod 서버(x86_64)에서만 동작. Lambda ARM64 미지원 (Pitfall 1 / REVIEWS H-2). 모듈 import는 깨끗하지만 인스턴스화는 mediapipe 필요") from e`
          - 모델 경로 검증 — missing 시 RuntimeError("pose_landmarker_heavy.task 모델 파일 없음: {path}. MEDIAPIPE_POSE_MODEL_PATH env 또는 명시적 경로 필요.")
          - PoseLandmarkerOptions: VIDEO mode, num_poses=1, min_pose_detection_confidence=0.5, min_pose_presence_confidence=0.5, min_tracking_confidence=0.5, output_segmentation_masks=False
          - target_fps 저장.
        - `def estimate(self, frames: np.ndarray, pole_axis: PoleAxis) -> list[PoseFrame]`:
          - 빈 frames(T=0) 시 NoHumanError 또는 빈 list 처리 (정책: 빈 list).
          - frame별 detect_for_video 호출. timestamp_ms = int(t * 1000 / self._target_fps) (Pitfall 2).
          - 미감지 frame은 `PoseFrame.empty(frame_index=t, timestamp_ms=ts, pole_axis=pole_axis)` 추가 (H-3 — empty도 pole_axis 보존).
          - 감지 frame은 convert_landmarks_to_coco17_and_pole_ext 호출 (pole_axis 인자 전달).
          - 전 프레임 미감지면 `NoHumanError("MediaPipe 전 프레임 미감지")` raise.
        - factory `classmethod create_with_landmarker(cls, landmarker, target_fps: float = 9.0) -> 'MediaPipePoseEngine'`: dependency injection — 단위 테스트가 mock landmarker 주입. mediapipe import skip.
      - 모듈 끝에 `__all__ = ['MediaPipePoseEngine']`

    `backend/tests/test_pose_engine_interface.py` 신규 생성:
    - `from tests.fixtures.mediapipe_landmarks import make_mock_detection_result, make_mock_no_human_result, make_mock_world_landmarks_33, make_mock_image_landmarks_33`
    - `from sunity_shared.analysis.pose_engines.mediapipe_engine import MediaPipePoseEngine`
    - `from sunity_shared.analysis.pose_frame import PoleAxis, PoseFrame`
    - `from sunity_shared.analysis.interfaces import NoHumanError`
    - mock landmarker class: `_MockLandmarker` with `detect_for_video(image, timestamp)` method returns from queue.
    - Test 1~10 위 behavior 그대로 구현. Test 7은 `sys.modules` 패치 + `importlib.reload`. Test 8은 단순 `import` 호출만으로 sanity.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_pose_engine_interface.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `pytest backend/tests/test_pose_engine_interface.py -x` exits 0 (10 tests pass)
    - `grep -n "class MediaPipePoseEngine" backend/shared/python/sunity_shared/analysis/pose_engines/mediapipe_engine.py` matches exactly one class
    - `grep -nE "^import mediapipe|^from mediapipe" backend/shared/python/sunity_shared/analysis/pose_engines/mediapipe_engine.py | wc -l` outputs `0` (H-2 박제 — module-level mediapipe import 없음)
    - `grep -nE "^[[:space:]]+import mediapipe|^[[:space:]]+from mediapipe" backend/shared/python/sunity_shared/analysis/pose_engines/mediapipe_engine.py | wc -l` outputs at least `1` (lazy import inside method)
    - `grep -nE "int\\(t \\* 1000 / self\\._target_fps\\)|int\\(t\\*1000/self\\._target_fps\\)" backend/shared/python/sunity_shared/analysis/pose_engines/mediapipe_engine.py | wc -l` outputs at least `1` (Pitfall 2 timestamp 단조성)
    - `grep -nE "NoHumanError\\(.*MediaPipe" backend/shared/python/sunity_shared/analysis/pose_engines/mediapipe_engine.py | wc -l` outputs at least `1`
    - `grep -nE "create_with_landmarker|@classmethod" backend/shared/python/sunity_shared/analysis/pose_engines/mediapipe_engine.py | wc -l` outputs at least `1` (factory)
    - `grep -nE "Pitfall 1|ARM64|H-2" backend/shared/python/sunity_shared/analysis/pose_engines/mediapipe_engine.py | wc -l` outputs at least `1`
    - Lazy-load sanity (H-2 박제): `pip uninstall -y mediapipe opencv-python 2>/dev/null; python -c "from sunity_shared.analysis.pose_engines.mediapipe_engine import MediaPipePoseEngine; print('module loaded without mediapipe')"` exits 0 (모듈 로드는 mediapipe 부재 환경에서도 성공)
  </acceptance_criteria>
  <done>MediaPipePoseEngine 어댑터 구현 + lazy mediapipe import (H-2 박제: module-level import 0, 인스턴스화 시점에만 import) + factory classmethod + 10개 mock 단위 테스트 통과. Lambda ARM64 미지원 fail-fast.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MediaPipe library ↔ Adapter | 외부 라이브러리 출력 형태 변경 시 변환 깨질 수 있음 — version pin 0.10.35 (Plan 05) |
| Mock fixtures ↔ Real MediaPipe | mock 결과가 실 MediaPipe DetectionResult 형태와 다르면 단위 테스트 false positive. Plan 06 회귀 검증으로 보완 |
| Lambda module import ↔ heavy library 부재 | H-2 — module-level heavy import 시 Lambda fail-fast가 깨짐. lazy import로 mitigate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-07 | Spoofing | MediaPipe wheel hijack (typosquat) | mitigate | requirements.txt에 `mediapipe==0.10.35` exact pin (Plan 05). slopcheck [OK] |
| T-1-08 | Tampering | A1/A2 매핑 표 (33→COCO-17 + grip) 오류 → 분석 정확도 저하 | mitigate | belle 검증 필요 표시 + Plan 06 회귀 검증에서 정은지 영상 5개 검증 |
| T-1-09 | DoS | Adversarial input video → MediaPipe DoS | mitigate | 영상 100MB 한도 + Lambda 폴백 fail-fast (Plan 05) |
| T-1-20 | DoS | module-level mediapipe import가 Lambda fail-fast를 깨뜨림 (H-2) | mitigate | 본 plan에서 lazy import 박제 + Plan 05 acceptance가 import 검증 |
</threat_model>

<verification>
- `pytest backend/tests/test_pose_engine_interface.py backend/tests/test_adapter_mediapipe_to_coco17.py -x` exits 0 (20 tests pass)
- 모든 매핑 상수가 RESEARCH §Pattern 3 표 + REVIEWS M-3 grip과 정확히 일치
- mediapipe library가 import되지 않는 환경(macOS local, ARM64 Lambda 시뮬레이션)에서 module load만으로 import error 없음 (H-2 박제)
- PoseEngine Protocol 시그니처가 RESEARCH §Pattern 1 권장 그대로 (estimate 시그니처 inspection)
- 어댑터가 raw_visibility/raw_presence 정확 매핑 (M-1) + grip derived (M-3) + pole_axis 보존 (H-3) + reliability 산출 (H-4)
</verification>

<success_criteria>
- PoseEngine Protocol이 interfaces.py에 추가 (PoseEstimator는 deprecated)
- MediaPipePoseEngine 어댑터 + factory classmethod
- MediaPipe33ToCOCO17Adapter 17 키 + 폴 확장 6 키 (toe/heel + grip derived) 매핑
- 20개 단위 테스트 모두 통과 (adapter 10 + engine 10)
- A1 (33→COCO-17 매핑) + A2 (grip derived) belle 확정 보류 사항 코드 + 주석에 명시
- mediapipe import는 lazy (Lambda fail-fast 안전 — H-2)
- 새 외부 의존성: 본 plan 코드 자체는 mediapipe import만 (RunPod 환경에서만 활성화). requirements.txt 추가는 Plan 05에서.
</success_criteria>

<output>
Create `.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-02-SUMMARY.md` when done. SUMMARY 내용:
- 추가된 module list + line count
- PoseEngine Protocol 시그니처
- MediaPipe 33→COCO-17 + grip 매핑 표 (verbatim)
- 20개 단위 테스트 결과 + Test 10 (adapter) skip 여부
- belle 보류 항목 재확인: A1 (33→COCO-17 매핑) + A2 (grip derived) — Plan 06 회귀 검증 후 갱신 약속
- 다음 wave (Plan 03 PoleDetector) 의존성: pole.aligner 모듈 — Plan 03 완성 시 Test 10 통과
- H-2 박제 확인 grep 결과 (module-level mediapipe import 0)
</output>
