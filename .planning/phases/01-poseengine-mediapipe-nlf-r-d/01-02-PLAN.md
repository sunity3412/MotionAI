---
phase: 01-poseengine-mediapipe-nlf-r-d
plan: 02
type: execute
wave: 1
depends_on:
  - 01-01
files_modified:
  - backend/shared/python/sunity_shared/analysis/pose_engines/__init__.py
  - backend/shared/python/sunity_shared/analysis/pose_engines/mediapipe_engine.py
  - backend/shared/python/sunity_shared/analysis/adapters/__init__.py
  - backend/shared/python/sunity_shared/analysis/adapters/mediapipe_to_coco17.py
  - backend/shared/python/sunity_shared/analysis/interfaces.py
  - backend/tests/test_pose_engine_interface.py
  - backend/tests/test_adapter_mediapipe_to_coco17.py
autonomous: true
requirements:
  - POSE-01
must_haves:
  truths:
    - "PoseEngine Protocol이 estimate(frames, pole_axis) -> list[PoseFrame] 시그니처를 강제한다"
    - "MediaPipePoseEngine이 mock된 MediaPipe Tasks API 호출을 받아 PoseFrame 리스트를 반환한다"
    - "MediaPipe33ToCOCO17Adapter가 33 landmark에서 17 COCO + 폴 확장 8개(toe/heel/finger)를 정확한 인덱스로 추출한다"
    - "전 프레임 미감지 시 NoHumanError 발생, contract no_human 매핑 보존"
    - "confidence = visibility × presence + uncertainty_proxy = 1 - confidence 어댑터 변환 검증"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/pose_engines/__init__.py"
      provides: "PoseEngine Protocol export"
    - path: "backend/shared/python/sunity_shared/analysis/pose_engines/mediapipe_engine.py"
      provides: "MediaPipePoseEngine 어댑터 (lazy import mediapipe)"
      contains: "class MediaPipePoseEngine"
    - path: "backend/shared/python/sunity_shared/analysis/adapters/mediapipe_to_coco17.py"
      provides: "33→COCO-17 + 폴 확장 매핑"
      contains: "MEDIAPIPE_33_TO_COCO17"
  key_links:
    - from: "pose_engines/mediapipe_engine.py"
      to: "adapters/mediapipe_to_coco17.py"
      via: "convert_landmarks_to_coco17_and_pole_ext 호출"
      pattern: "from ..adapters.mediapipe_to_coco17"
    - from: "interfaces.py"
      to: "pose_engines"
      via: "PoseEngine Protocol 정의 (PoseEstimator는 deprecated 표시)"
      pattern: "class PoseEngine"
---

<objective>
PoseEngine Protocol(estimate→list[PoseFrame])을 interfaces.py에 추가하고, MediaPipePoseEngine 어댑터 + MediaPipe33→COCO17 변환 어댑터를 구현한다. mediapipe 라이브러리는 lazy import — 로컬 테스트는 mock으로 진행. 이 plan은 Plan 03(PoleDetector)과 병렬 실행 가능 (파일 겹침 없음).

Purpose: D-01(Heavy), D-02(Tasks API), D-03(world + image), D-04(33 원본 보존 + COCO-17 변환), D-05(confidence 공식) 결정을 코드로 박제. Pattern 1·3(RESEARCH) 구현. A1 (MediaPipe→COCO-17 매핑) belle 확정 필요 — 본 plan에서 명시적 표 + 단위 테스트로 박제 + RESEARCH §Tertiary "공식 매핑 표 미발견" 한계 명시 + 향후 belle 확정 후 갱신 절차 주석.

Output: PoseEngine Protocol + MediaPipePoseEngine + 어댑터 + 단위 테스트 2개 모듈.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-VALIDATION.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-01-SUMMARY.md
@backend/shared/python/sunity_shared/analysis/interfaces.py
@backend/shared/python/sunity_shared/analysis/pose_frame.py
@backend/shared/python/sunity_shared/analysis/skeleton.py
@backend/shared/python/sunity_shared/analysis/pose_estimator.py
@backend/tests/fixtures/mediapipe_landmarks.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PoseEngine Protocol + 33→COCO-17 어댑터</name>
  <files>backend/shared/python/sunity_shared/analysis/interfaces.py, backend/shared/python/sunity_shared/analysis/adapters/__init__.py, backend/shared/python/sunity_shared/analysis/adapters/mediapipe_to_coco17.py, backend/tests/test_adapter_mediapipe_to_coco17.py</files>
  <read_first>
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md §Pattern 1 (PoseEngine 시그니처 권장 — estimate 반환 list[PoseFrame], pole_axis 인자)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md §Pattern 3 (33→COCO-17 매핑 표 17개 + 폴 확장 매핑 표 8개)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md D-04, D-05 (변환식 + 폴 확장 인덱스 — A1 belle 확정 필요 명시)
    - backend/shared/python/sunity_shared/analysis/interfaces.py (기존 PoseEstimator Protocol + NoHumanError — 보존하되 deprecated 마킹)
    - backend/shared/python/sunity_shared/analysis/skeleton.py (KEYPOINT_NAMES 17개 정확한 순서 — adapter 출력이 따라야 함)
    - backend/shared/python/sunity_shared/analysis/pose_frame.py (Landmark3D, Keypoint3D, PoseFrame.compute_confidence)
    - backend/tests/fixtures/mediapipe_landmarks.py (Wave 0 fixture — 단위 테스트 입력)
  </read_first>
  <behavior>
    - Test 1 (test_coco17_index_mapping_exact): MEDIAPIPE_33_TO_COCO17 dict이 정확히 17 엔트리 + 매핑 = {nose:0, left_eye:2, right_eye:5, left_ear:7, right_ear:8, left_shoulder:11, right_shoulder:12, left_elbow:13, right_elbow:14, left_wrist:15, right_wrist:16, left_hip:23, right_hip:24, left_knee:25, right_knee:26, left_ankle:27, right_ankle:28}
    - Test 2 (test_pole_extension_index_mapping_exact): POLE_EXTENSION_MAP dict 8 엔트리 = {left_heel:29, right_heel:30, left_foot_index:31, right_foot_index:32, left_pinky:17, right_pinky:18, left_thumb:21, right_thumb:22}. (left_index/right_index는 grip 보조 — A1 belle 확정 보류 — 일단 매핑은 인덱스 19,20도 등록하되 conservative 처리. 총 10개로 확장 가능 시그니처). 본 테스트는 최소 8개 매핑 통과 + 모든 인덱스 < 33.
    - Test 3 (test_convert_landmarks_basic): mock 33 landmarks (visible_indices=range(33)) → convert_landmarks_to_coco17_and_pole_ext 호출 → PoseFrame 반환. keypoints_3d에 정확히 17 키 (skeleton.KEYPOINT_NAMES), raw_landmarks_33에 33 키 (MediaPipe 인덱스 string 또는 명칭). 각 Keypoint3D.confidence == visibility × presence = 0.81 (default 0.9 × 0.9).
    - Test 4 (test_convert_pole_extension_present): visible_indices = {29, 30, 31, 32} (heel + toe만) → pose_extension_landmarks에 4개 키 (left_heel, right_heel, left_foot_index, right_foot_index) 존재, confidence > 0. visible 안 된 grip(17,18,21,22)는 confidence=0.01 (낮은 visibility/presence 곱).
    - Test 5 (test_convert_low_confidence_keypoint_marked): visible_indices=[5,12,14,16,24,26,28] (오른쪽 절반만) → keypoints_3d[left_elbow].uncertainty_proxy > 0.9 (low confidence + 1 - confidence). uncertainty 임계값 검증.
    - Test 6 (test_no_pole_alignment_applied_in_adapter): pole_axis=PoleAxis((0,0,1), 0.9, 'hough') 주입 시 keypoints_3d_pole_aligned가 비어있지 않음 + identity 회전 (Z+축 일치)이면 raw와 동일.
  </behavior>
  <action>
    `backend/shared/python/sunity_shared/analysis/interfaces.py` 수정:
    - 기존 PoseEstimator Protocol 보존 (RunPod 호환 + 점진 마이그레이션). 그 위에 `# DEPRECATED: PoseEngine으로 마이그레이션 중 (Phase 1 D-04). 신규 코드는 PoseEngine 사용.` 한 줄 주석 추가.
    - 파일 끝에 `PoseEngine` Protocol 추가 (RESEARCH §Pattern 1 시그니처 그대로):
      `class PoseEngine(Protocol):` docstring + `def estimate(self, frames: np.ndarray, pole_axis: 'PoleAxis') -> list['PoseFrame']: ...`
      `from __future__ import annotations` + TYPE_CHECKING 블록으로 PoseFrame/PoleAxis import (circular 회피).
    - NoHumanError, NotPoleMotionError, FallbackCoachWriter 모두 그대로 보존.

    `backend/shared/python/sunity_shared/analysis/adapters/__init__.py` 신규 생성:
    - module docstring: "스키마 변환 어댑터 (33→COCO-17, SMPL-X→COCO-17 등). RESEARCH §Pattern 3 / Phase 1 D-04."
    - `from .mediapipe_to_coco17 import convert_landmarks_to_coco17_and_pole_ext, MEDIAPIPE_33_TO_COCO17, POLE_EXTENSION_MAP`

    `backend/shared/python/sunity_shared/analysis/adapters/mediapipe_to_coco17.py` 신규 생성:
    - module docstring: "MediaPipe 33 landmark → COCO-17 + 폴 확장 변환 (D-04). RESEARCH §Pattern 3 매핑 표 박제. A1: 공식 매핑 미발견 — belle 확정 후 갱신."
    - `from __future__ import annotations`, `from typing import Any`, `import numpy as np`.
    - import: `from ..pose_frame import PoseFrame, Landmark3D, Keypoint3D, Keypoint3DAligned, PoseExtensionLandmark, PoleAxis`, `from ..skeleton import KEYPOINT_NAMES`, `from ..pole.aligner import compute_alignment_matrix, apply_alignment` (Plan 03 산출물 — Plan 03이 wave 1에서 병렬이지만 같은 wave 끝나면 사용 가능. 단 Task 1 단독으로는 Plan 03 fixture가 없으므로 `compute_alignment_matrix`/`apply_alignment`는 lazy import 또는 try/except 가드. 권장: 호출 시점에 import).

    상수:
    - `MEDIAPIPE_33_TO_COCO17: dict[str, int]` — 17 엔트리 매핑 (skeleton.KEYPOINT_NAMES 키 → MediaPipe 33 인덱스). 정확한 값은 behavior Test 1 명시.
    - `POLE_EXTENSION_MAP: dict[str, int]` — 8 엔트리 (또는 10 — A1 보류 항목). behavior Test 2 명시.
    - `MEDIAPIPE_33_LANDMARK_NAMES: tuple[str, ...]` — 33개 명칭 (developers.google.com/mediapipe — BlazePose 33 landmark name list). raw_landmarks_33 key 명명에 사용 (인덱스 0~32). 출처 주석: "RESEARCH §Pattern 3 표 + ai.google.dev/edge/mediapipe/solutions/vision/pose_landmarker — A1 belle 확정 권장".

    함수:
    - `def _landmark_to_dict(lm: Any) -> Landmark3D`: MediaPipe NormalizedLandmark/Landmark duck-typed 객체에서 x, y, z, visibility, presence 추출. presence 부재 시 (legacy) visibility만 — Landmark3D.presence = visibility로 폴백 (D-05 단서: "둘 다 있으면..., visibility만 있으면 confidence = visibility").
    - `def _build_keypoint3d(lm: Any) -> Keypoint3D`: confidence = visibility × presence + uncertainty_proxy = 1 - confidence + xyz 그대로. `PoseFrame.compute_confidence` 사용.
    - `def _build_pole_extension(lm: Any) -> PoseExtensionLandmark`: confidence = visibility × presence만 (uncertainty 비포함 — 확장 landmark는 게이트만).
    - `def convert_landmarks_to_coco17_and_pole_ext(frame_index: int, timestamp_ms: int, world_landmarks: list, image_landmarks: list | None, pole_axis: PoleAxis) -> PoseFrame`:
      1) `raw_landmarks_33`: enumerate(world_landmarks, name=MEDIAPIPE_33_LANDMARK_NAMES[idx]) → Landmark3D dict
      2) `keypoints_3d`: for coco_name, mp_idx in MEDIAPIPE_33_TO_COCO17 → world_landmarks[mp_idx] → Keypoint3D
      3) `pole_extension_landmarks`: for ext_name, mp_idx in POLE_EXTENSION_MAP → PoseExtensionLandmark
      4) `keypoints_2d` (image_landmarks 주어졌을 때): for coco_name, mp_idx in MEDIAPIPE_33_TO_COCO17 → Keypoint2D (x, y, visibility) — z는 image landmarks에서 무시
      5) `keypoints_3d_pole_aligned`: pole.aligner.compute_alignment_matrix(pole_axis.direction_3d) → apply_alignment(keypoints_3d, R) → Keypoint3DAligned dict
      6) PoseFrame(...)를 반환.

    `backend/tests/test_adapter_mediapipe_to_coco17.py` 신규 생성:
    - `from tests.fixtures.mediapipe_landmarks import make_mock_world_landmarks_33, make_mock_image_landmarks_33`
    - `from sunity_shared.analysis.adapters.mediapipe_to_coco17 import convert_landmarks_to_coco17_and_pole_ext, MEDIAPIPE_33_TO_COCO17, POLE_EXTENSION_MAP`
    - `from sunity_shared.analysis.pose_frame import PoleAxis`
    - `from sunity_shared.analysis.skeleton import KEYPOINT_NAMES`
    - Test 1~6 위 behavior 그대로 구현. Plan 03이 Task 3까지 완료 안 됐으면 keypoints_3d_pole_aligned 검증 부분(Test 6)에서 ImportError 발생 가능 — `pytest.importorskip("sunity_shared.analysis.pole.aligner")` 가드. Plan 03 완성 후 Wave 2에서 통합 verification 재실행 시 통과해야 함.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_adapter_mediapipe_to_coco17.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `pytest backend/tests/test_adapter_mediapipe_to_coco17.py -x` exits 0 (6 tests pass, Test 6는 pole.aligner 부재 시 skip)
    - `grep -n "MEDIAPIPE_33_TO_COCO17" backend/shared/python/sunity_shared/analysis/adapters/mediapipe_to_coco17.py | wc -l` >= 2 (정의 1줄 + __init__.py re-export 또는 docstring 인용)
    - `python -c "from sunity_shared.analysis.adapters.mediapipe_to_coco17 import MEDIAPIPE_33_TO_COCO17; assert len(MEDIAPIPE_33_TO_COCO17) == 17 and MEDIAPIPE_33_TO_COCO17['nose'] == 0 and MEDIAPIPE_33_TO_COCO17['right_ankle'] == 28"` exits 0
    - `python -c "from sunity_shared.analysis.adapters.mediapipe_to_coco17 import POLE_EXTENSION_MAP; assert POLE_EXTENSION_MAP['left_heel'] == 29 and POLE_EXTENSION_MAP['right_foot_index'] == 32"` exits 0
    - `python -c "from sunity_shared.analysis.interfaces import PoseEngine; import inspect; sig = inspect.signature(PoseEngine.estimate); assert 'frames' in sig.parameters and 'pole_axis' in sig.parameters"` exits 0
    - `grep -nE "MEDIAPIPE_33_LANDMARK_NAMES.*=" backend/shared/python/sunity_shared/analysis/adapters/mediapipe_to_coco17.py` matches one tuple/list definition with 33 entries
    - `grep -n "A1.*belle 확정\|belle 확정.*A1" backend/shared/python/sunity_shared/analysis/adapters/mediapipe_to_coco17.py` matches at least 1 line (research limitation 명시)
  </acceptance_criteria>
  <done>PoseEngine Protocol + MediaPipe 33→COCO-17 adapter + 폴 확장 매핑 박제. 6개 단위 테스트 통과 (Test 6은 Plan 03 완료 후 통과). A1 belle 확정 보류 표시 + 출처 인용.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MediaPipePoseEngine 어댑터 (mock-based 단위 테스트)</name>
  <files>backend/shared/python/sunity_shared/analysis/pose_engines/__init__.py, backend/shared/python/sunity_shared/analysis/pose_engines/mediapipe_engine.py, backend/tests/test_pose_engine_interface.py</files>
  <read_first>
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md §Code Examples Example 1 (MediaPipePoseEngine 완전 골격 — 그대로 인용)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md §Pitfall 2 (timestamp monotonic 강제 — int(t * 1000 / target_fps))
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md §Pitfall 4 (protobuf 충돌 — try/except import)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md D-01, D-02, D-03 (Heavy, Tasks API, world+image)
    - backend/shared/python/sunity_shared/analysis/pose_estimator.py (기존 lazy import 패턴 — 그대로 차용)
    - backend/shared/python/sunity_shared/analysis/pose_frame.py (PoseFrame.empty 사용 — 미감지 sentinel)
    - backend/shared/python/sunity_shared/analysis/adapters/mediapipe_to_coco17.py (Task 1 산출물 — convert_landmarks_to_coco17_and_pole_ext 호출)
    - backend/tests/fixtures/mediapipe_landmarks.py (mock DetectionResult)
    - backend/shared/python/sunity_shared/analysis/interfaces.py (Task 1에서 PoseEngine Protocol 추가 + NoHumanError)
  </read_first>
  <behavior>
    - Test 1 (test_engine_estimate_returns_pose_frames): mock PoseLandmarker (`detect_for_video` → make_mock_detection_result with 33 visible) 주입 → 3 frame 입력 → list[PoseFrame] 길이 3 반환. 각 frame의 frame_index = 0,1,2. timestamp_ms = 0, 111, 222 (target_fps=9 → 1000/9 ≈ 111.11 → int = 111).
    - Test 2 (test_engine_no_human_error): 모든 detect_for_video 호출이 make_mock_no_human_result() 반환 → engine.estimate() → NoHumanError 발생 + message에 "MediaPipe" 포함.
    - Test 3 (test_engine_partial_detection_sentinel): frame 0 = detection result, frame 1 = no_human_result, frame 2 = detection result → list 길이 3 (PoseFrame.empty for frame 1) + NoHumanError 미발생 (any_detection=True).
    - Test 4 (test_engine_timestamp_monotonic): 10 frame estimate → timestamps = [int(t*1000/9) for t in range(10)] = [0,111,222,333,444,555,666,777,888,1000]. monotonic_increasing.
    - Test 5 (test_engine_model_path_missing): MEDIAPIPE_POSE_MODEL_PATH 없는 경로 + model_path=None → RuntimeError 발생 (Code Example 1의 ValidationError) + message에 "pose_landmarker_heavy.task" 포함.
    - Test 6 (test_engine_lazy_import_no_mediapipe): mediapipe 라이브러리 미설치 환경 시뮬레이션 (sys.modules['mediapipe'] = None → import 실패 분기) → MediaPipePoseEngine() 생성 시 ImportError + 메시지에 "RunPod" 또는 "mediapipe" 안내. (Lambda ARM64 미지원 Pitfall 1 대응)
  </behavior>
  <action>
    `backend/shared/python/sunity_shared/analysis/pose_engines/__init__.py` 신규 생성:
    - module docstring: "포즈 엔진 어댑터 모음 (MediaPipe 제품, NLF는 R&D 격리 — backend/research에 별도). Phase 1 D-06/D-07."
    - `from ..interfaces import PoseEngine, NoHumanError` (re-export)
    - mediapipe_engine은 lazy export 권장 — `def __getattr__(name: str)` 패턴으로 `MediaPipePoseEngine` 접근 시 import (lazy)

    `backend/shared/python/sunity_shared/analysis/pose_engines/mediapipe_engine.py` 신규 생성:
    - RESEARCH §Code Example 1 골격 그대로 구현. 추가/변경 사항:
      - `from __future__ import annotations` + module docstring (D-01, D-02, D-03, D-04, D-05 모두 인용 + RESEARCH §Pitfall 1·2·4 인용)
      - import: `os`, `Path`, `numpy as np`, `..interfaces.NoHumanError`, `..pose_frame.PoseFrame, PoleAxis`, `..adapters.mediapipe_to_coco17.convert_landmarks_to_coco17_and_pole_ext`
      - class `MediaPipePoseEngine`:
        - `__init__(self, model_path: str | None = None, target_fps: float = 9.0)`: lazy import (`import mediapipe as mp`). mediapipe import ImportError 시 명확한 메시지로 `RuntimeError`("mediapipe library 미설치 — RunPod 서버(x86_64)에서만 동작. Lambda ARM64 미지원 Pitfall 1 참조") 발생. 모델 경로 missing 시 `RuntimeError`("pose_landmarker_heavy.task 모델 파일 없음: {path}. MEDIAPIPE_POSE_MODEL_PATH env 또는 명시적 경로 필요."). `PoseLandmarkerOptions`: VIDEO mode, num_poses=1, min_pose_detection_confidence=0.5, min_pose_presence_confidence=0.5, min_tracking_confidence=0.5, output_segmentation_masks=False (RESEARCH Example 1). target_fps 저장.
        - `def estimate(self, frames: np.ndarray, pole_axis: PoleAxis) -> list[PoseFrame]`: RESEARCH Example 1 그대로. timestamp_ms = int(t * 1000 / self._target_fps) (Pitfall 2). NoHumanError raise 조건: any_detection=False. 미감지 frame은 PoseFrame.empty 추가.
        - testability: `self._mp` + `self._landmarker`을 dependency injection 가능하게 — `MediaPipePoseEngine.create_with_landmarker(landmarker, target_fps)` factory classmethod 추가 (단위 테스트가 mock landmarker 주입 가능). __init__의 무거운 부분은 헬퍼 `_load_landmarker(model_path)`로 분리.
      - 모듈 끝에 `__all__ = ['MediaPipePoseEngine']`

    `backend/tests/test_pose_engine_interface.py` 신규 생성:
    - `from tests.fixtures.mediapipe_landmarks import make_mock_detection_result, make_mock_no_human_result, make_mock_world_landmarks_33, make_mock_image_landmarks_33`
    - `from sunity_shared.analysis.pose_engines.mediapipe_engine import MediaPipePoseEngine`
    - `from sunity_shared.analysis.pose_frame import PoleAxis`
    - `from sunity_shared.analysis.interfaces import NoHumanError`
    - mock landmarker class: `_MockLandmarker` with `detect_for_video(image, timestamp)` method that returns from a queue (test가 frame별 결과 주입).
    - Test 1~6 위 behavior 그대로 구현. Test 5는 `monkeypatch.setenv` + tmpdir 사용. Test 6은 `sys.modules` 패치 + `importlib.reload`로 ImportError 강제.
    - 추가 Test 7 (test_confidence_conversion_in_engine): visibility=0.8, presence=0.7 mock landmark → engine 호출 후 frame.keypoints_3d['nose'].confidence ≈ 0.56 ± 1e-6 (D-05 검증, 어댑터 통합 동작).
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_pose_engine_interface.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `pytest backend/tests/test_pose_engine_interface.py -x` exits 0 (7 tests pass)
    - `grep -n "class MediaPipePoseEngine" backend/shared/python/sunity_shared/analysis/pose_engines/mediapipe_engine.py` matches exactly one class
    - `grep -nE "import mediapipe|from mediapipe" backend/shared/python/sunity_shared/analysis/pose_engines/mediapipe_engine.py | wc -l` >= 1 (lazy import 위치) AND `grep -n "def __init__" backend/shared/python/sunity_shared/analysis/pose_engines/mediapipe_engine.py | head -1 | xargs -I{} echo {}` — mediapipe import는 __init__ 또는 _load_landmarker 내부
    - `grep -nE "int\\(t \\* 1000 / self\\._target_fps\\)|int\\(t\\*1000/self\\._target_fps\\)" backend/shared/python/sunity_shared/analysis/pose_engines/mediapipe_engine.py` matches at least 1 line (Pitfall 2 timestamp 단조성)
    - `grep -nE "NoHumanError\\(.*MediaPipe" backend/shared/python/sunity_shared/analysis/pose_engines/mediapipe_engine.py` matches at least 1 line (에러 메시지에 엔진명)
    - `grep -nE "create_with_landmarker|factory" backend/shared/python/sunity_shared/analysis/pose_engines/mediapipe_engine.py` matches at least 1 (testability)
    - `grep -n "Pitfall 1\|ARM64" backend/shared/python/sunity_shared/analysis/pose_engines/mediapipe_engine.py` matches at least 1 (Lambda 환경 경고)
    - Module load test: `python -c "from sunity_shared.analysis.pose_engines import PoseEngine, MediaPipePoseEngine; print('ok')"` exits 0 (lazy __getattr__ 경유, 단 mediapipe import는 안 일어남)
  </acceptance_criteria>
  <done>MediaPipePoseEngine 어댑터 구현 + lazy mediapipe import + factory classmethod + 7개 mock-based 단위 테스트 통과. Lambda ARM64 미지원 경고 코드 + 주석에 명시.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MediaPipe library ↔ Adapter | 외부 라이브러리 출력 형태 변경(라이브러리 업그레이드) 시 변환 깨질 수 있음 — version pin 0.10.35로 mitigate |
| Mock fixtures ↔ Real MediaPipe | mock 결과가 실 MediaPipe DetectionResult 형태와 다르면 단위 테스트 false positive. RunPod 통합 테스트로 보완 (Wave 4) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-07 | Spoofing | MediaPipe wheel hijack (typosquat) | mitigate | requirements.txt에 `mediapipe==0.10.35` exact pin (Plan 05에서). slopcheck [OK] 결과 RESEARCH §Package Legitimacy Audit |
| T-1-08 | Tampering | A1 매핑 표 (33→COCO-17) 오류 → 분석 정확도 저하 | mitigate | belle 검증 필요 표시 + Wave 4 회귀 검증에서 정은지 영상 5개 검증 + 발견 시 매핑 수정 |
| T-1-09 | DoS | Adversarial input video → MediaPipe DoS | mitigate | 영상 100MB 한도 (이미 enforce) + Lambda 폴백 fail-fast (Plan 05) |
</threat_model>

<verification>
- `pytest backend/tests/test_pose_engine_interface.py backend/tests/test_adapter_mediapipe_to_coco17.py -x` exits 0
- 모든 매핑 상수가 RESEARCH §Pattern 3 표와 정확히 일치 (grep으로 검증)
- mediapipe library가 import되지 않는 환경(macOS local)에서 모듈 로드만으로 import error 없음 (lazy import 검증)
- PoseEngine Protocol 시그니처가 RESEARCH §Pattern 1 권장 그대로 (estimate 시그니처 inspection)
</verification>

<success_criteria>
- PoseEngine Protocol이 interfaces.py에 추가됨 (PoseEstimator는 deprecated 마킹 유지)
- MediaPipePoseEngine 어댑터 작성 + factory classmethod로 testability 확보
- MediaPipe33ToCOCO17Adapter 17 키 + 폴 확장 8 키 매핑 박제
- 13개 단위 테스트 모두 통과 (adapter 6 + engine 7) — Plan 03 완성 후 adapter Test 6도 통과
- A1 belle 확정 보류 사항이 코드 + 주석에 명시 (33 landmark 명칭 출처 + 매핑 검증 필요)
- 새 외부 의존성: 본 plan 코드 자체는 mediapipe import만 (RunPod 환경에서만 활성화). requirements.txt 추가는 Plan 05에서.
</success_criteria>

<output>
Create `.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-02-SUMMARY.md` when done. SUMMARY 내용:
- 추가된 module list + line count
- PoseEngine Protocol 시그니처
- MediaPipe 33→COCO-17 매핑 표 (verbatim — 검증 가능)
- 13개 단위 테스트 결과 + Test 6 skip 여부
- belle 보류 항목 재확인: A1 매핑 — Wave 4 회귀 검증 후 갱신 약속
- 다음 wave (Plan 03 PoleDetector) 의존성: convert_landmarks_to_coco17_and_pole_ext가 pole.aligner를 호출 → Plan 03 완성 필수
</output>
