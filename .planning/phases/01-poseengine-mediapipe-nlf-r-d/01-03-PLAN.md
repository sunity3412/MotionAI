---
phase: 01-poseengine-mediapipe-nlf-r-d
plan: 03
type: execute
wave: 1
depends_on:
  - 01-01
files_modified:
  - backend/shared/python/sunity_shared/analysis/pole/__init__.py
  - backend/shared/python/sunity_shared/analysis/pole/detector.py
  - backend/shared/python/sunity_shared/analysis/pole/aligner.py
  - backend/tests/test_pole_detector.py
  - backend/tests/test_pole_aligner.py
autonomous: true
requirements:
  - POSE-02
must_haves:
  truths:
    - "HoughPoleDetector.detect()가 수직 폴 영상에서 source='detected' + confidence_level='high' 또는 'medium' PoleAxis 반환 (D-09)"
    - "검출 실패 영상에서 source='vertical_fallback' + confidence_level='low' PoleAxis 반환 (D-11 + M-2 enum)"
    - "video-level PoleAxis 1개 산출 — 모든 frame에 동일 axis 적용 (D-10 + L-1)"
    - "PoleAxisAligner가 폴 축을 Z 축에 정렬하는 회전행렬을 산출하고 identity 케이스를 안전 처리 (D-12)"
    - "cv2, scipy import는 module-level이 아닌 class __init__ 또는 첫 호출 시점에 lazy — Lambda module import 안전 (H-2)"
    - "스피닝 폴 영상은 v1.5로 명시 — Phase 1 범위 밖"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/pole/detector.py"
      provides: "HoughPoleDetector (OpenCV Canny + HoughLinesP, lazy cv2 import per H-2)"
      contains: "class HoughPoleDetector"
    - path: "backend/shared/python/sunity_shared/analysis/pole/aligner.py"
      provides: "PoleAxisAligner (scipy align_vectors, lazy scipy import per H-2)"
      contains: "compute_alignment_matrix"
  key_links:
    - from: "pole/detector.py"
      to: "PoleAxis (pose_frame)"
      via: "return PoleAxis(axis_vector, confidence_level, source)"
      pattern: "from ..pose_frame import PoleAxis"
    - from: "pole/aligner.py"
      to: "adapters/mediapipe_to_coco17.py"
      via: "compute_alignment_matrix + apply_alignment 호출 (Plan 02 Task 1)"
      pattern: "scipy.spatial.transform"
---

<objective>
폴 축 자동 검출(HoughPoleDetector) + 좌표계 정렬(PoleAxisAligner) 모듈을 구현한다. Plan 02와 병렬 실행 (파일 겹침 없음). 본 plan은 REVIEWS H-2 (cv2/scipy module-level import 금지) + M-2 (confidence_level enum 사용 — numeric 0.3 대신 'low' literal) + D-10 (video-level 1개 axis) 박제.

Purpose: D-09(Hough Line Transform + 수직 prior) · D-10(video-level 평균 축 1개) · D-11(검출 실패 시 수직 가정 + confidence_level='low' — M-2 enum) · D-12(raw + pole-aligned 둘 다 저장) 박제. RESEARCH §Pattern 4·5 + Code Example 2·3 구현. H-2 박제 — cv2, scipy import는 class __init__ 내부 (또는 첫 호출 시점) lazy로 — Lambda module import는 cv2/scipy 부재 환경에서도 성공해야 함.

Output: pole/__init__.py + detector.py + aligner.py + 단위 테스트 2개 모듈.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-VALIDATION.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-REVIEWS.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-01-SUMMARY.md
@backend/shared/python/sunity_shared/analysis/pose_frame.py
@backend/tests/fixtures/synthetic_frames.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HoughPoleDetector (Canny + HoughLinesP, lazy cv2 import per H-2)</name>
  <files>backend/shared/python/sunity_shared/analysis/pole/__init__.py, backend/shared/python/sunity_shared/analysis/pole/detector.py, backend/tests/test_pole_detector.py</files>
  <read_first>
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-REVIEWS.md H-2 (lazy cv2 import — module-level 금지), M-2 (confidence_level enum 'low'/'medium'/'high')
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md §Code Examples Example 2 (HoughPoleDetector 골격)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md §Pattern 4 + Hough 파라미터
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md D-09, D-10, D-11
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md §Pitfall 3 (world landmarks 원점 hip)
    - backend/shared/python/sunity_shared/analysis/pose_frame.py (PoleAxis with axis_vector + confidence_level + source — Task 01-01 산출)
    - backend/tests/fixtures/synthetic_frames.py (synthetic_vertical/no/tilted_pole_frames)
  </read_first>
  <behavior>
    - Test 1 (test_module_import_without_cv2): `import sunity_shared.analysis.pole.detector` (인스턴스화 없이)으로는 cv2 import 시도하지 않음 — module load 성공 (H-2 박제). `sys.modules`에서 cv2 부재 확인하며 import.
    - Test 2 (test_detect_vertical_pole_returns_detected): synthetic_vertical_pole_frames(3, 256, 128) → detector.detect(frames) → PoleAxis(source='detected', confidence_level in {'high','medium'}). axis_vector[1] (y component) > 0.9 (수직성).
    - Test 3 (test_detect_no_pole_fallback_low_confidence): synthetic_no_pole_frames(3, 128, 64) → PoleAxis(source='vertical_fallback', confidence_level='low', axis_vector ≈ (0,1,0)). (M-2 + D-11)
    - Test 4 (test_detect_tilted_pole_fallback): synthetic_tilted_pole_frames(3, 128, 64, tilt_deg=30) → 수직 필터(±5°) 통과 못함 → source='vertical_fallback', confidence_level='low'.
    - Test 5 (test_video_level_single_axis): 5 frame 입력 → 결과 PoleAxis는 단일 instance (D-10 + L-1). frame_index가 None (또는 -1 — video-level 표시) 또는 None.
    - Test 6 (test_pole_axis_returns_unit_vector): axis_vector의 norm = 1.0 ± 1e-3.
    - Test 7 (test_detector_empty_frames_handles_gracefully): 빈 (0, H, W, 3) 배열 → vertical_fallback + confidence_level='low' 반환 (raise 안 함).
    - Test 8 (test_confidence_level_threshold_mapping): 내부 numeric confidence가 >=0.7면 'high', 0.4~0.7면 'medium', <0.4면 'low'로 매핑됨 (M-2 enum + pose_frame.RELIABILITY_*_THRESHOLD 재사용)
  </behavior>
  <action>
    `backend/shared/python/sunity_shared/analysis/pole/__init__.py` 신규 생성:
    - module docstring: "폴 축 자동 검출 + 좌표계 정렬. Phase 1 D-09/D-10/D-11/D-12 + REVIEWS H-2 lazy import / M-2 confidence_level enum. 스피닝 폴은 v1.5."
    - `from .detector import HoughPoleDetector`
    - `from .aligner import compute_alignment_matrix, apply_alignment`

    `backend/shared/python/sunity_shared/analysis/pole/detector.py` 신규 생성:
    - RESEARCH §Code Example 2 골격 + H-2 박제 + M-2 박제:
      - `from __future__ import annotations` + module docstring (D-09, D-10, D-11 인용 + Pitfall 3 + REVIEWS H-2 module-level cv2 import 금지 + M-2 confidence_level enum).
      - module-level imports — light only: `import math`, `import numpy as np`, `from ..pose_frame import PoleAxis, RELIABILITY_HIGH_THRESHOLD, RELIABILITY_MEDIUM_THRESHOLD`. **cv2 module-level import 금지 (H-2)**. cv2는 class __init__ 또는 detect() 첫 호출 시점에 lazy import.
      - 상수 (RESEARCH 권장값, module-level):
        - VERTICAL_TOLERANCE_DEG = 5.0
        - CANNY_LOW = 50
        - CANNY_HIGH = 200
        - CANNY_APERTURE = 3
        - HOUGH_RHO = 1
        - HOUGH_THETA_DEG = 1.0 (radian 계산은 detect 내부에서 math.pi/180)
        - HOUGH_THRESHOLD = 80
        - HOUGH_MIN_LINE_LENGTH = 100
        - HOUGH_MAX_LINE_GAP = 20
      - class `HoughPoleDetector`:
        - `__init__(self, vertical_tolerance_deg: float = VERTICAL_TOLERANCE_DEG, **hough_overrides)`:
          - 모든 상수를 instance attribute로 저장. hough_overrides로 belle 튜닝 가능.
          - cv2 lazy import — H-2 박제: `__init__`에서 `try: import cv2; self._cv2 = cv2 except ImportError as e: raise RuntimeError("opencv-python-headless 미설치 — HoughPoleDetector는 RunPod 또는 cv2 설치 환경에서만 동작") from e`
        - `def detect(self, frames: np.ndarray) -> PoleAxis`:
          - 빈 frames(T=0) 시 vertical_fallback 반환: `PoleAxis(axis_vector=(0.0, 1.0, 0.0), confidence_level='low', source='vertical_fallback', frame_index=None)` (D-10 video-level, frame_index=None)
          - frame별 처리: 그레이 → Canny(self._cv2.Canny) → HoughLinesP(self._cv2.HoughLinesP).
          - 수직 선분 필터: 각도가 90° ± vertical_tolerance_deg 이내. 통과 안 하면 frame skip.
          - 누적 vertical 선분 list. 비어있으면 vertical_fallback 반환 (D-11).
          - 결과 axis_vector 계산:
            1) confidence 가중평균으로 평균 2D direction vector → 정규화
            2) 3D 변환: `(avg_dir[0], avg_dir[1], 0.0)` (image 평면 가정 — Pitfall 3 명시 주석)
            3) numeric_confidence = min(weights.mean() / SOME_NORMALIZER, 1.0) (자세한 정규화 식은 RESEARCH Example 2 따름)
            4) `confidence_level = _map_to_level(numeric_confidence)` — H-4 임계값 재사용: >=RELIABILITY_HIGH_THRESHOLD(0.7) → 'high', >=RELIABILITY_MEDIUM_THRESHOLD(0.4) → 'medium', else → 'low' (M-2 박제)
            5) `PoleAxis(axis_vector=(x, y, 0.0), confidence_level=confidence_level, source='detected', frame_index=None)` 반환 (D-10 video-level — frame_index=None)
        - module-level helper `def _map_numeric_to_confidence_level(numeric: float) -> ConfidenceLevel`: 임계값 매핑 함수 (테스트 8에서 import).

    `backend/tests/test_pole_detector.py` 신규 생성:
    - imports: pytest, numpy as np, sys, importlib, `from tests.fixtures.synthetic_frames import synthetic_vertical_pole_frames, synthetic_no_pole_frames, synthetic_tilted_pole_frames`
    - Test 1: H-2 박제 검증. `sys.modules`에서 cv2 제거 후 `importlib.import_module("sunity_shared.analysis.pole.detector")` 호출 → no ImportError (모듈 load는 cv2 부재에서도 성공).
    - Test 2~8 위 behavior 그대로 구현.
    - Test 4 fixture: frame별 폴 띠 너비 다르게 (frame 0 = 2px, frame 1 = 8px, frame 2 = 4px) — numpy 직접.
    - opencv 의존 — local 환경에 opencv-python-headless 또는 opencv-python 설치 필요. Test 2~7는 `pytest.importorskip("cv2")` 가드. Test 1과 Test 8은 cv2 없이 통과 가능 (module load + 함수 매핑만).
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_pole_detector.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `pytest backend/tests/test_pole_detector.py -x` exits 0 (8 tests pass, Test 2~7는 cv2 부재 시 skip — local에서는 통과)
    - `grep -n "class HoughPoleDetector" backend/shared/python/sunity_shared/analysis/pole/detector.py` matches exactly one class
    - `grep -nE "^import cv2|^from cv2" backend/shared/python/sunity_shared/analysis/pole/detector.py | wc -l` outputs `0` (H-2 박제 — module-level cv2 import 0)
    - `grep -nE "^[[:space:]]+import cv2|^[[:space:]]+from cv2" backend/shared/python/sunity_shared/analysis/pole/detector.py | wc -l` outputs at least `1` (lazy cv2 import inside __init__ or method)
    - `grep -nE "source='detected'|source=\"detected\"" backend/shared/python/sunity_shared/analysis/pole/detector.py | wc -l` outputs at least `1` (검출 성공 시 source — reviewer 명세)
    - `grep -nE "source='vertical_fallback'|source=\"vertical_fallback\"" backend/shared/python/sunity_shared/analysis/pole/detector.py | wc -l` outputs at least `1` (D-11 폴백)
    - `grep -nE "confidence_level='low'|confidence_level=\"low\"|confidence_level=confidence_level|_map_numeric_to_confidence_level" backend/shared/python/sunity_shared/analysis/pole/detector.py | wc -l` outputs at least `2` (M-2 enum 사용)
    - `grep -nE "VERTICAL_TOLERANCE_DEG\\s*=\\s*5\\.0|HOUGH_THRESHOLD\\s*=\\s*80|HOUGH_MIN_LINE_LENGTH\\s*=\\s*100" backend/shared/python/sunity_shared/analysis/pole/detector.py | wc -l` outputs at least `3` (RESEARCH 권장값)
    - `grep -nE "Pitfall 3|hip midpoint|카메라 roll" backend/shared/python/sunity_shared/analysis/pole/detector.py | wc -l` outputs at least `1`
    - `grep -nE "frame_index=None" backend/shared/python/sunity_shared/analysis/pole/detector.py | wc -l` outputs at least `1` (D-10 video-level)
    - Lazy-load sanity (H-2 박제): `pip uninstall -y opencv-python opencv-python-headless 2>/dev/null; python -c "import sunity_shared.analysis.pole.detector; print('module loaded without cv2')"` exits 0
    - `grep -nE "numeric.*>=.*RELIABILITY_HIGH_THRESHOLD|>=.*0\\.7" backend/shared/python/sunity_shared/analysis/pole/detector.py | wc -l` outputs at least `1` (M-2 enum 임계값 매핑)
  </acceptance_criteria>
  <done>HoughPoleDetector 구현 + RESEARCH 권장 파라미터 박제 + cv2 lazy import (H-2) + 8개 단위 테스트 통과. video-level confidence 가중평균 + vertical_fallback (D-11) + confidence_level enum (M-2) + Pitfall 3 좌표계 가정 명시.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PoleAxisAligner (scipy align_vectors, lazy scipy import per H-2)</name>
  <files>backend/shared/python/sunity_shared/analysis/pole/aligner.py, backend/tests/test_pole_aligner.py</files>
  <read_first>
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-REVIEWS.md H-2 (lazy scipy import — module-level 금지)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md §Code Examples Example 3 (PoleAxisAligner 골격)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md §Pattern 5 (scipy align_vectors edge case)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md D-12
    - backend/shared/python/sunity_shared/analysis/pose_frame.py (Keypoint3D, Keypoint3DAligned)
  </read_first>
  <behavior>
    - Test 1 (test_module_import_without_scipy): `import sunity_shared.analysis.pole.aligner` 호출 시 scipy import 안 일어남 (H-2 박제).
    - Test 2 (test_identity_alignment): pole_axis_direction = (0, 0, 1) → 회전행렬 = identity (3x3 단위행렬, max diff < 1e-6).
    - Test 3 (test_alignment_y_to_z): pole_axis_direction = (0, 1, 0) → 회전행렬 적용 후 (0,1,0) → (0,0,1).
    - Test 4 (test_alignment_anti_parallel): pole_axis_direction = (0, 0, -1) → 회전행렬 적용 후 (0,0,-1) → (0,0,1) (180° rotation).
    - Test 5 (test_apply_alignment_preserves_keys): keypoints_3d (dict 5 키) + R → output dict 5 키 (동일 set).
    - Test 6 (test_apply_alignment_numerical): identity R + keypoints_3d → output 좌표 = input 좌표 (소수점 6자리).
    - Test 7 (test_compute_alignment_matrix_returns_3x3): 어떤 pole_axis 입력이든 (3,3) ndarray 반환 + determinant ≈ ±1.
    - Test 8 (test_compute_alignment_matrix_zero_vector_raises): zero vector → ValueError.
  </behavior>
  <action>
    `backend/shared/python/sunity_shared/analysis/pole/aligner.py` 신규 생성:
    - RESEARCH §Code Example 3 골격 + H-2 박제:
      - `from __future__ import annotations` + module docstring (D-12 인용 + scipy align_vectors edge case + REVIEWS H-2 lazy scipy)
      - module-level imports — light only: `import numpy as np`, `from typing import TYPE_CHECKING`. `if TYPE_CHECKING: from ..pose_frame import Keypoint3D, Keypoint3DAligned`. **scipy module-level import 금지 (H-2)**.
      - 상수 `TARGET_AXIS = np.array([0.0, 0.0, 1.0])`
      - `def compute_alignment_matrix(pole_axis_direction: tuple[float, float, float] | np.ndarray) -> np.ndarray`:
        - 함수 내부 lazy import: `from scipy.spatial.transform import Rotation` (H-2 박제 — module-level 금지)
        - pole_axis_direction이 zero vector(norm < 1e-9)이면 ValueError raise.
        - 정규화 후 Rotation.align_vectors([TARGET_AXIS], [pole]) → as_matrix() 반환. shape (3, 3).
      - `def apply_alignment(keypoints: dict, R: np.ndarray) -> dict`:
        - scipy import 없음 — numpy 행렬 곱만 사용.
        - keypoints가 dict[str, Keypoint3D] 또는 dict[str, dict] 모두 허용 (duck-typed).
        - 각 key별로 x,y,z 추출 → R @ vector → Keypoint3DAligned(x, y, z) 인스턴스화. Keypoint3DAligned import도 함수 내부에서 lazy: `from ..pose_frame import Keypoint3DAligned`.
        - 입력 keypoints dict의 key set과 output key set 동일.

    `backend/tests/test_pole_aligner.py` 신규 생성:
    - imports: pytest, numpy as np, sys, importlib.
    - Test 1: H-2 박제 검증. `sys.modules` 에서 scipy 제거 → `importlib.import_module("sunity_shared.analysis.pole.aligner")` 성공.
    - Test 2~8 위 behavior 그대로 구현. numerical: `np.allclose(actual, expected, atol=1e-6)`.
    - Test 5 fixture: keypoints fixture 단순 dict ({'nose': Keypoint3D(0,0,0,0.9,0.1), 'left_shoulder': Keypoint3D(1,0,0,0.9,0.1), ...} 5개) — `from sunity_shared.analysis.pose_frame import Keypoint3D`.
    - Test 2~8은 scipy 의존 — `pytest.importorskip("scipy")` 가드.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_pole_aligner.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `pytest backend/tests/test_pole_aligner.py -x` exits 0 (8 tests pass, scipy 부재 시 Test 2~8 skip)
    - `grep -n "def compute_alignment_matrix" backend/shared/python/sunity_shared/analysis/pole/aligner.py` matches exactly 1
    - `grep -n "def apply_alignment" backend/shared/python/sunity_shared/analysis/pole/aligner.py` matches exactly 1
    - `grep -nE "^from scipy|^import scipy" backend/shared/python/sunity_shared/analysis/pole/aligner.py | wc -l` outputs `0` (H-2 박제 — module-level scipy import 0)
    - `grep -nE "^[[:space:]]+from scipy|^[[:space:]]+import scipy" backend/shared/python/sunity_shared/analysis/pole/aligner.py | wc -l` outputs at least `1` (lazy import inside function)
    - `grep -nE "TARGET_AXIS\\s*=\\s*np\\.array" backend/shared/python/sunity_shared/analysis/pole/aligner.py` matches 1 line
    - Lazy-load sanity: `pip uninstall -y scipy 2>/dev/null; python -c "import sunity_shared.analysis.pole.aligner; print('module loaded without scipy')"` exits 0
    - Plan 02 Task 1 Test 10 (test_pole_alignment_applied_when_aligner_available) re-run: 본 plan 완료 후 `pytest backend/tests/test_adapter_mediapipe_to_coco17.py::test_pole_alignment_applied_when_aligner_available -x` should exit 0
  </acceptance_criteria>
  <done>PoleAxisAligner 구현 + scipy lazy import (H-2 박제) + 8개 단위 테스트 통과 + Plan 02 adapter Test 10 통합 통과.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OpenCV HoughLinesP ↔ 폴 검출 결과 | adversarial 영상 (거울, 창문 등 수직 선) 시 오검출 가능. D-11 폴백 + Plan 06 회귀 검증 |
| scipy align_vectors ↔ Rotation | scipy 버전 변경 시 numerical noise — version pin 1.17.1 (Plan 05) |
| Lambda module import ↔ cv2/scipy 부재 | H-2 — module-level import 시 Lambda fail-fast 깨짐. lazy import로 mitigate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-02 | Information disclosure | 폴 축 검출 실패 시 기술 용어 노출 | mitigate | source='vertical_fallback' 내부 enum만 — 사용자 카피는 D-11 안내문으로 매핑 (Plan 05/Phase 12) |
| T-1-10 | Spoofing | 사용자 영상 조작 → 잘못된 pole axis | mitigate | confidence_level='low' 표기 + 사용자 안내 (D-11) + 다운스트림이 confidence_level enum으로 분기 (M-2) |
| T-1-11 | Tampering | OpenCV CVE | mitigate | opencv-python-headless 4.13.0.92 (Plan 05 requirements.txt) |
| T-1-21 | DoS | module-level cv2/scipy import가 Lambda fail-fast 깨뜨림 (H-2) | mitigate | 본 plan에서 lazy import 박제 + module load sanity test |
</threat_model>

<verification>
- `pytest backend/tests/test_pole_detector.py backend/tests/test_pole_aligner.py -x` exits 0
- Plan 02 의존성 해소: Plan 02 adapter Test 10이 본 plan 후 통과 (importorskip 통과)
- 모듈 load는 cv2/scipy 부재 환경에서도 성공 (H-2 박제)
- Pitfall 3 (image 2D → 3D 카메라 roll=0 가정) 주석 명시
- M-2 confidence_level enum 사용 — numeric 0.3 대체
</verification>

<success_criteria>
- HoughPoleDetector + PoleAxisAligner 모듈
- 16개 단위 테스트 통과 (detector 8 + aligner 8)
- Plan 02 adapter Test 10 (pole alignment) 통과
- D-09, D-10, D-11, D-12 + M-2 + H-2 박제
- 외부 의존성: opencv-python-headless + scipy. Plan 05 requirements.txt에 추가 예정 — 본 plan 코드만
- module-level cv2/scipy import 0 (H-2 박제)
</success_criteria>

<output>
Create `.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-03-SUMMARY.md` when done. SUMMARY 내용:
- 추가된 module list + line count
- Hough 파라미터 (verbatim)
- 16개 단위 테스트 결과 + skip 케이스
- D-11 폴백 시 사용자 카피 (Plan 05/Phase 12 입력)
- 외부 의존성 (opencv-python-headless 4.13.0.92, scipy 1.17.1)
- Plan 02 adapter 통합 검증 결과 (Test 10 pass)
- H-2 박제 grep 결과 (module-level cv2/scipy import 0)
- M-2 박제 grep 결과 (confidence_level enum 사용)
</output>
