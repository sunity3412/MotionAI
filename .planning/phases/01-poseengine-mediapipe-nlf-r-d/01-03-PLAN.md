---
phase: 01-poseengine-mediapipe-nlf-r-d
plan: 03
type: execute
wave: 1
depends_on:
  - 01-01
files_modified:
  - backend/shared/python/sunity_shared/analysis/pole/__init__.py
  - backend/shared/python/sunity_shared/analysis/pole/detector.py
  - backend/shared/python/sunity_shared/analysis/pole/aligner.py
  - backend/tests/test_pole_detector.py
  - backend/tests/test_pole_aligner.py
autonomous: true
requirements:
  - POSE-02
must_haves:
  truths:
    - "HoughPoleDetector.detect()가 수직 폴 영상에서 source='hough' + confidence>0 PoleAxis 반환"
    - "검출 실패 영상에서 source='vertical_fallback' + confidence=낮음 PoleAxis 반환 (D-11)"
    - "PoleAxisAligner가 폴 축을 Z 축에 정렬하는 회전행렬을 산출하고 identity 케이스를 안전 처리"
    - "스피닝 폴 영상은 v1.5로 명시 — Phase 1 범위 밖"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/pole/detector.py"
      provides: "HoughPoleDetector (OpenCV Canny + HoughLinesP)"
      contains: "class HoughPoleDetector"
    - path: "backend/shared/python/sunity_shared/analysis/pole/aligner.py"
      provides: "PoleAxisAligner (scipy align_vectors)"
      contains: "compute_alignment_matrix"
  key_links:
    - from: "pole/detector.py"
      to: "PoleAxis (pose_frame)"
      via: "return PoleAxis(direction_3d, confidence, source)"
      pattern: "from ..pose_frame import PoleAxis"
    - from: "pole/aligner.py"
      to: "adapters/mediapipe_to_coco17.py"
      via: "compute_alignment_matrix + apply_alignment 호출 (Plan 02 Task 1에서 사용)"
      pattern: "scipy.spatial.transform"
---

<objective>
폴 축 자동 검출(HoughPoleDetector) + 좌표계 정렬(PoleAxisAligner) 모듈을 구현한다. Plan 02와 병렬 실행 (파일 겹침 없음). Plan 02 어댑터가 keypoints_3d_pole_aligned 계산 시 본 plan의 compute_alignment_matrix/apply_alignment를 호출.

Purpose: D-09(Hough Line Transform + 수직 prior) · D-10(영상 전체 평균 축 1개) · D-11(검출 실패 시 수직 가정 + confidence='low') · D-12(raw + pole-aligned 둘 다 저장) 결정 박제. RESEARCH §Pattern 4·5 + Code Example 2·3 구현. Pitfall 3(world landmarks 좌표 원점 = hip midpoint) 인지하여 image 좌표 → 3D 변환은 카메라 roll=0 가정 명시.

Output: pole/__init__.py + detector.py + aligner.py + 단위 테스트 2개 모듈.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-VALIDATION.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-01-SUMMARY.md
@backend/shared/python/sunity_shared/analysis/pose_frame.py
@backend/tests/fixtures/synthetic_frames.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HoughPoleDetector (Canny + HoughLinesP + 수직 prior)</name>
  <files>backend/shared/python/sunity_shared/analysis/pole/__init__.py, backend/shared/python/sunity_shared/analysis/pole/detector.py, backend/tests/test_pole_detector.py</files>
  <read_first>
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md §Code Examples Example 2 (HoughPoleDetector 완전 골격)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md §Pattern 4 + 권장 Hough 파라미터 (정확한 초기값)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md D-09, D-10, D-11 (검출 알고리즘, 시간 안정 평균, 폴백 정책)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md §Pitfall 3 (world landmarks 원점 hip — image 2D 폴 방향과 별도)
    - backend/shared/python/sunity_shared/analysis/pose_frame.py (PoleAxis import — Task 1 산출)
    - backend/tests/fixtures/synthetic_frames.py (synthetic_vertical_pole_frames, synthetic_no_pole_frames, synthetic_tilted_pole_frames)
  </read_first>
  <behavior>
    - Test 1 (test_detect_vertical_pole_hough): synthetic_vertical_pole_frames(3, 256, 128) → detector.detect(frames) → PoleAxis(source='hough', confidence > 0.3). direction_3d[1] (y component) > 0.9 (수직성).
    - Test 2 (test_detect_no_pole_fallback): synthetic_no_pole_frames(3, 128, 64) → PoleAxis(source='vertical_fallback', confidence ≈ 0.3, direction_3d=(0,1,0)).
    - Test 3 (test_detect_tilted_pole_fallback): synthetic_tilted_pole_frames(3, 128, 64, tilt_deg=30) → 수직 필터(±5°) 통과 못함 → source='vertical_fallback'.
    - Test 4 (test_detect_confidence_weighted_average): 5 frame에서 line length가 frame별로 다른 영상 — 긴 line 가중치가 더 큼 → 결과 direction_3d가 긴 line frame 방향에 더 가깝게.
    - Test 5 (test_pole_axis_returns_unit_vector): direction_3d의 norm = 1.0 ± 1e-6 (모든 결과).
    - Test 6 (test_detector_empty_frames_handles_gracefully): 빈 (0, H, W, 3) 배열 입력 → vertical_fallback 반환 (raise 안 함).
  </behavior>
  <action>
    `backend/shared/python/sunity_shared/analysis/pole/__init__.py` 신규 생성:
    - module docstring: "폴 축 자동 검출 + 좌표계 정렬. Phase 1 D-09/D-10/D-11/D-12. 스피닝 폴은 v1.5 (out of scope)."
    - `from .detector import HoughPoleDetector`
    - `from .aligner import compute_alignment_matrix, apply_alignment` (Task 2 산출 — 같은 plan 내 후속)

    `backend/shared/python/sunity_shared/analysis/pole/detector.py` 신규 생성:
    - RESEARCH §Code Example 2 골격 그대로 + 추가 사항:
      - `from __future__ import annotations` + module docstring (D-09, D-10, D-11 인용 + Pitfall 3 인용: "image 2D 방향만 측정 — world keypoints 변환은 카메라 roll≈0 가정 (A6)")
      - imports: `math`, `numpy as np`, `cv2` (lazy 안 함 — opencv는 ARM64 wheel 있음 → Lambda·RunPod 모두 OK), `..pose_frame.PoleAxis`
      - 상수 (RESEARCH 권장값):
        VERTICAL_TOLERANCE_DEG = 5.0
        CANNY_LOW = 50
        CANNY_HIGH = 200
        CANNY_APERTURE = 3
        HOUGH_RHO = 1
        HOUGH_THETA = math.pi / 180 (또는 np.pi/180)
        HOUGH_THRESHOLD = 80
        HOUGH_MIN_LINE_LENGTH = 100
        HOUGH_MAX_LINE_GAP = 20
        FALLBACK_CONFIDENCE = 0.3
      - class `HoughPoleDetector`:
        - `__init__(self, vertical_tolerance_deg: float = VERTICAL_TOLERANCE_DEG, **hough_overrides)`: 모든 상수를 instance attribute로. hough_overrides로 검출 실패 시 belle 튜닝 가능.
        - `def detect(self, frames: np.ndarray) -> PoleAxis`: RESEARCH Example 2 그대로. shape (T,H,W,3) RGB uint8. 빈 frames (T=0) 시 vertical_fallback 반환.
        - 결과 direction_3d 계산:
          1) frame별 vertical 선분 list 누적 + length 가중
          2) 누적 비어있으면 vertical_fallback `PoleAxis(direction_3d=(0.0, 1.0, 0.0), confidence=FALLBACK_CONFIDENCE, source='vertical_fallback')`
          3) confidence 가중평균으로 평균 2D direction vector → 정규화
          4) 3D 변환: `(avg_dir[0], avg_dir[1], 0.0)` (image 평면 가정 — Pitfall 3 명시 주석)
          5) confidence = min(weights.mean(), 1.0)
          6) `PoleAxis(direction_3d=(x, y, 0.0), confidence=float, source='hough')` 반환
        - 결과 direction_3d는 단위 벡터로 정규화 후 반환 (Test 5 보장).

    `backend/tests/test_pole_detector.py` 신규 생성:
    - `from tests.fixtures.synthetic_frames import synthetic_vertical_pole_frames, synthetic_no_pole_frames, synthetic_tilted_pole_frames`
    - `from sunity_shared.analysis.pole.detector import HoughPoleDetector`
    - `from sunity_shared.analysis.pose_frame import PoleAxis`
    - `import numpy as np`
    - Test 1~6 위 behavior 그대로 구현.
    - Test 4 fixture: 합성 frame별로 폴 띠 너비를 다르게 (frame 0 = 2px, frame 1 = 8px, frame 2 = 4px) — 직접 numpy로 생성 (fixture function 재사용 + 후처리).
    - opencv 의존 — local 환경에 opencv-python-headless 또는 opencv-python 설치 필요. `pytest.importorskip("cv2")` 가드 (CI에 opencv 없으면 skip).
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_pole_detector.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `pytest backend/tests/test_pole_detector.py -x` exits 0 (6 tests pass, cv2 부재 시 모두 skip — local에서는 통과해야 함)
    - `grep -n "class HoughPoleDetector" backend/shared/python/sunity_shared/analysis/pole/detector.py` matches exactly one class
    - `grep -nE "VERTICAL_TOLERANCE_DEG\\s*=\\s*5\\.0|VERTICAL_TOLERANCE_DEG = 5\\.0" backend/shared/python/sunity_shared/analysis/pole/detector.py` matches 1 line
    - `grep -nE "HOUGH_THRESHOLD\\s*=\\s*80|HOUGH_MIN_LINE_LENGTH\\s*=\\s*100|HOUGH_MAX_LINE_GAP\\s*=\\s*20" backend/shared/python/sunity_shared/analysis/pole/detector.py` matches 3 lines (RESEARCH 권장값 박제)
    - `grep -n "source='vertical_fallback'\\|source=\"vertical_fallback\"" backend/shared/python/sunity_shared/analysis/pole/detector.py` matches at least 1 line (D-11 폴백)
    - `grep -nE "Pitfall 3|hip midpoint|카메라 roll" backend/shared/python/sunity_shared/analysis/pole/detector.py` matches at least 1 line (좌표계 가정 주석)
    - `python -c "import sys; sys.path.insert(0, 'backend/shared/python'); import cv2; from sunity_shared.analysis.pole.detector import HoughPoleDetector; d = HoughPoleDetector(); print('ok')" || echo "cv2 missing (acceptable for local — RunPod has it)"` 출력 'ok' (cv2 설치 시) 또는 missing 메시지 (acceptable)
  </acceptance_criteria>
  <done>HoughPoleDetector 구현 + RESEARCH 권장 파라미터 박제 + 6개 단위 테스트 통과. 영상 전체 confidence 가중평균 + vertical_fallback (D-11) + 단위 벡터 정규화 + Pitfall 3 좌표계 가정 명시.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PoleAxisAligner (scipy align_vectors)</name>
  <files>backend/shared/python/sunity_shared/analysis/pole/aligner.py, backend/tests/test_pole_aligner.py</files>
  <read_first>
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md §Code Examples Example 3 (PoleAxisAligner 완전 골격)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md §Pattern 5 (scipy align_vectors edge case)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md D-12 (raw + pole-aligned 둘 다 저장)
    - backend/shared/python/sunity_shared/analysis/pose_frame.py (Keypoint3D, Keypoint3DAligned 형태)
  </read_first>
  <behavior>
    - Test 1 (test_identity_alignment): pole_axis = (0, 0, 1) (이미 Z 축) → 회전행렬 = identity (3x3 단위행렬, max diff < 1e-6).
    - Test 2 (test_alignment_y_to_z): pole_axis = (0, 1, 0) → 회전행렬 적용 후 (0,1,0) → (0,0,1) 변환 확인.
    - Test 3 (test_alignment_anti_parallel): pole_axis = (0, 0, -1) (반대 방향) → 회전행렬 적용 후 (0,0,-1) → (0,0,1) (180° rotation). scipy align_vectors는 안전 처리.
    - Test 4 (test_apply_alignment_preserves_keys): keypoints_3d (dict 5 키) + R → output dict 5 키 (동일 set). 각 value는 Keypoint3DAligned 인스턴스 또는 dict {x,y,z}.
    - Test 5 (test_apply_alignment_numerical): identity R + keypoints_3d (단순 fixture) → output 좌표 = input 좌표 (소수점 6자리).
    - Test 6 (test_compute_alignment_matrix_returns_3x3): 어떤 pole_axis 입력이든 (3,3) ndarray 반환 + determinant ≈ ±1 (proper rotation).
  </behavior>
  <action>
    `backend/shared/python/sunity_shared/analysis/pole/aligner.py` 신규 생성:
    - RESEARCH §Code Example 3 골격 + 보강:
      - `from __future__ import annotations` + module docstring (D-12 인용 + scipy align_vectors edge case 처리 명시)
      - imports: `numpy as np`, `from scipy.spatial.transform import Rotation`, `from ..pose_frame import Keypoint3D, Keypoint3DAligned` (단, Keypoint3D는 input 타입 hint용 — runtime은 dict 또는 dataclass 모두 허용)
      - 상수: `TARGET_AXIS = np.array([0.0, 0.0, 1.0])`
      - `def compute_alignment_matrix(pole_axis_direction: tuple[float, float, float]) -> np.ndarray`:
        RESEARCH Example 3 그대로. pole_axis_direction이 zero vector(norm < 1e-9)이면 ValueError raise (방어적). 정규화 후 scipy.align_vectors([TARGET_AXIS], [pole]) → as_matrix() 반환. shape (3, 3).
      - `def apply_alignment(keypoints: dict, R: np.ndarray) -> dict`:
        keypoints가 dict[str, Keypoint3D] 또는 dict[str, dict] 모두 허용 (duck-typed). 각 key별로 x,y,z 추출 → R @ vector → Keypoint3DAligned(x, y, z) dict 반환. 입력 keypoints dict의 key set과 output key set 동일.

    `backend/tests/test_pole_aligner.py` 신규 생성:
    - `import numpy as np`
    - `from sunity_shared.analysis.pole.aligner import compute_alignment_matrix, apply_alignment, TARGET_AXIS`
    - Test 1~6 위 behavior 그대로 구현. numerical assertion은 `np.allclose(actual, expected, atol=1e-6)`.
    - Test 3 (anti-parallel): scipy 1.17.x에서 안전 처리 보장 — `Rotation.align_vectors([target], [(0,0,-1)])` 호출이 deprecated warning 또는 valid Rotation 반환. test에서 assert `np.allclose(R @ np.array([0,0,-1]), [0,0,1], atol=1e-6)`.
    - Test 4 (apply_alignment): keypoints fixture로 단순 dict ({'nose': Keypoint3D(0,0,0,0.9,0.1), 'left_shoulder': Keypoint3D(1,0,0,0.9,0.1), ...} 5개) 사용. output dict의 keys() == input dict의 keys().
    - scipy 의존 — local pip 환경에 scipy 없으면 `pytest.importorskip("scipy")` 가드.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_pole_aligner.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `pytest backend/tests/test_pole_aligner.py -x` exits 0 (6 tests pass, scipy 부재 시 skip — local에 scipy 있으면 통과해야 함)
    - `grep -n "def compute_alignment_matrix" backend/shared/python/sunity_shared/analysis/pole/aligner.py` matches exactly 1
    - `grep -n "def apply_alignment" backend/shared/python/sunity_shared/analysis/pole/aligner.py` matches exactly 1
    - `grep -n "from scipy.spatial.transform import Rotation" backend/shared/python/sunity_shared/analysis/pole/aligner.py` matches exactly 1 line
    - `grep -nE "TARGET_AXIS\\s*=\\s*np\\.array" backend/shared/python/sunity_shared/analysis/pole/aligner.py` matches 1 line (`[0.0, 0.0, 1.0]`)
    - `python -c "import sys; sys.path.insert(0, 'backend/shared/python'); from sunity_shared.analysis.pole.aligner import compute_alignment_matrix; import numpy as np; R = compute_alignment_matrix((0, 0, 1)); assert R.shape == (3, 3) and np.allclose(R, np.eye(3), atol=1e-6); print('identity OK')"` outputs 'identity OK'
    - Plan 02 Task 1 Test 6 (test_no_pole_alignment_applied_in_adapter) re-run: 본 plan 완료 후 `pytest backend/tests/test_adapter_mediapipe_to_coco17.py::test_no_pole_alignment_applied_in_adapter -x` should exit 0 (importorskip 통과)
  </acceptance_criteria>
  <done>PoleAxisAligner 구현 (scipy align_vectors) + 6개 단위 테스트 통과 + Plan 02 Task 1의 pole.aligner 의존 테스트가 importorskip을 통과해서 실행 + Plan 02 어댑터가 keypoints_3d_pole_aligned 계산 가능.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OpenCV HoughLinesP ↔ 폴 검출 결과 | adversarial 영상(폴 외 다른 수직 선분 — 거울, 창문) 시 오검출 가능. D-11 폴백으로 graceful + Wave 4 회귀 검증으로 확인 |
| scipy align_vectors ↔ Rotation | scipy 버전 변경 시 numerical noise 변화 — version pin 1.17.1로 mitigate (Plan 05 requirements.txt) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-02 | Information disclosure | 폴 축 검출 실패 시 기술 용어 노출 ("vertical_fallback" 등) | mitigate | source='vertical_fallback' 내부 enum만 — 사용자 카피는 D-11 "카메라 기울어져 세부 각도 해석에 주의"로 매핑 (Plan 05 또는 Phase 12에서 결과 화면) |
| T-1-10 | Spoofing | 사용자 영상 조작 → 잘못된 pole axis | mitigate | confidence='low' 표기 + 사용자 안내 카피 (D-11) + 다운스트림이 axis confidence를 가중치로 사용 |
| T-1-11 | Tampering | OpenCV CVE | mitigate | opencv-python-headless 4.13.0.92 (최신, Plan 05 requirements.txt) |
</threat_model>

<verification>
- `pytest backend/tests/test_pole_detector.py backend/tests/test_pole_aligner.py -x` exits 0 (또는 cv2/scipy 부재 시 skip — local에서는 둘 다 있어야 함)
- Plan 02 의존성 해소: `pytest backend/tests/test_adapter_mediapipe_to_coco17.py -x` 모든 테스트 pass (importorskip 통과)
- 단위 벡터 정규화 + identity/anti-parallel edge case 안전 처리
- Pitfall 3 (image 2D → 3D 카메라 roll=0 가정) 주석 명시 — 향후 정확도 개선 phase에서 review point
</verification>

<success_criteria>
- HoughPoleDetector + PoleAxisAligner 모듈 작성
- 12개 단위 테스트 통과 (detector 6 + aligner 6)
- Plan 02 adapter Test 6 (pole alignment 통합)가 본 plan 후 통과
- D-09, D-10, D-11, D-12 결정이 코드 + 주석에 명시적으로 인용
- 외부 의존성: opencv-python-headless + scipy. Plan 05 requirements.txt에 추가 예정 — 본 plan은 코드만
</success_criteria>

<output>
Create `.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-03-SUMMARY.md` when done. SUMMARY 내용:
- 추가된 module list + line count
- Hough 파라미터 (verbatim — 회귀 검증에서 belle가 sweep할 base값)
- 12개 단위 테스트 결과 + skip 케이스
- D-11 폴백 시 사용자 카피 (Plan 05/Phase 12 결과 화면 입력)
- 외부 의존성 요약 (opencv-python-headless 4.13.0.92, scipy 1.17.1 — Plan 05에서 requirements.txt 추가)
- Plan 02 adapter 통합 검증 결과 (Test 6 pass)
</output>
