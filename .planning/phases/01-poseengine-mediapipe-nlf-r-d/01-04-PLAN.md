---
phase: 01-poseengine-mediapipe-nlf-r-d
plan: 04
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - backend/research/__init__.py
  - backend/research/README.md
  - backend/research/pose_engines/__init__.py
  - backend/research/pose_engines/nlf/__init__.py
  - backend/research/pose_engines/nlf/nlf_engine.py
  - backend/research/pose_engines/nlf/models/.gitkeep
  - backend/research/evaluations/__init__.py
  - backend/research/evaluations/verify_nlf_pipeline.py
  - backend/research/evaluations/verify_self_comparison_nlf.py
  - backend/research/evaluations/extract_reference_angles_nlf.py
  - backend/shared/python/sunity_shared/analysis/pose_estimator.py
  - backend/scripts/nlf_l_multi.torchscript
  - backend/yolo11n.pt
  - backend/scripts/verify_nlf_pipeline.py
  - backend/scripts/verify_self_comparison.py
  - backend/scripts/extract_reference_angles.py
  - .samignore
  - backend/tests/test_nlf_isolation.py
autonomous: true
requirements:
  - POSE-01
must_haves:
  truths:
    - "NLF 코드(NlfPoseEstimator, nlf_l_multi.torchscript, yolo11n.pt)가 backend/research/ 외부 import 경로에서 제거됨"
    - "import sunity_shared.analysis.pose_estimator 시 ImportError 또는 module not found 발생"
    - "backend/research가 .samignore에 명시되어 SAM 빌드 결과물에 포함 안 됨"
    - "기존 NLF 검증 스크립트(verify_nlf_pipeline, verify_self_comparison)가 backend/research/evaluations/에서 동작"
  artifacts:
    - path: "backend/research/README.md"
      provides: "R&D 비교군 전용 사용 정책 명시 (PS:License 1.0, 제품 import 금지)"
    - path: "backend/research/pose_engines/nlf/nlf_engine.py"
      provides: "기존 NlfPoseEstimator를 새 위치로 이전 + PoseEngine Protocol 구현으로 진화"
    - path: ".samignore"
      provides: "research/ 경로 SAM 빌드 차단"
    - path: "backend/tests/test_nlf_isolation.py"
      provides: "ImportError 보장 + research 격리 단위 검증"
  key_links:
    - from: "sunity_shared.analysis"
      to: "backend/research/pose_engines/nlf"
      via: "물리 경로 분리 (D-06, D-07) — import 경로 단절"
      pattern: "ImportError"
    - from: ".samignore"
      to: "Lambda layer 빌드"
      via: "research/ 디렉터리 제외"
      pattern: "research/"
---

<objective>
NLF 관련 코드 + 모델 파일 + 검증 스크립트를 모두 `backend/research/`로 이동(D-06, D-07). `sunity_shared/analysis/pose_estimator.py`와 NLF 의존 scripts는 제품 import 경로에서 완전히 사라진다. 동시에 본 plan은 atomic swap(Plan 05) 직전 단계 — 격리만 하고 pipeline 교체는 안 한다.

Purpose: 제품 코드의 라이선스 리스크 0 보장 (PS:License 1.0 비상업이 sunity_shared·Lambda layer에 부재 = 라이선스 위반 0). T-1-01 위협(NLF 격리 우회) 차단. backend/research는 사내 R&D 비교군 전용 — Plan 06 회귀 검증에서만 호출.

Output: backend/research/ 디렉터리 구조 + 파일 이동 + README + .samignore + 단위 테스트 (ImportError 검증).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-VALIDATION.md
@backend/shared/python/sunity_shared/analysis/pose_estimator.py
@backend/scripts/verify_nlf_pipeline.py
@backend/scripts/verify_self_comparison.py
@backend/scripts/extract_reference_angles.py
@backend/template.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: backend/research/ 구조 + README + .samignore + 파일 이동</name>
  <files>backend/research/__init__.py, backend/research/README.md, backend/research/pose_engines/__init__.py, backend/research/pose_engines/nlf/__init__.py, backend/research/pose_engines/nlf/nlf_engine.py, backend/research/pose_engines/nlf/models/.gitkeep, backend/research/evaluations/__init__.py, backend/research/evaluations/verify_nlf_pipeline.py, backend/research/evaluations/verify_self_comparison_nlf.py, backend/research/evaluations/extract_reference_angles_nlf.py, backend/shared/python/sunity_shared/analysis/pose_estimator.py (deletion), backend/scripts/nlf_l_multi.torchscript (move), backend/yolo11n.pt (move), backend/scripts/verify_nlf_pipeline.py (move), backend/scripts/verify_self_comparison.py (move + rename), backend/scripts/extract_reference_angles.py (move + rename), .samignore</files>
  <read_first>
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md D-06, D-07, D-08 (격리 방식 + atomic swap)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md §NLF 격리 — atomic swap 대상 코드 라인 (11개 대상 표)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md §Recommended Project Structure (디렉터리 구조 박제)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md §Open Questions Q1 (.samignore 명시적 차단 권장)
    - backend/shared/python/sunity_shared/analysis/pose_estimator.py (전체 — 어디로 이동할지 파악)
    - backend/scripts/verify_nlf_pipeline.py, verify_self_comparison.py, extract_reference_angles.py (전체 — 이동 대상)
    - backend/template.yaml line 60-70 (ContentUri: shared/ — research/는 자연 제외 확인)
    - CLAUDE.md (라이선스 박스 + R&D 비교군 격리 정책)
  </read_first>
  <action>
    1) 디렉터리 + __init__.py 생성:
       - `backend/research/__init__.py` (빈 파일 + docstring: "R&D 비교군 전용 — 제품 코드에서 import 금지. PS:License 1.0(비상업) NLF/SMPL-X 평가용.")
       - `backend/research/pose_engines/__init__.py` (빈 + 동일 경고)
       - `backend/research/pose_engines/nlf/__init__.py` (`from .nlf_engine import NlfPoseEstimator, NlfPoseEngine` re-export — 이름 변경 옵션)
       - `backend/research/pose_engines/nlf/models/.gitkeep` (빈 파일 — git track용)
       - `backend/research/evaluations/__init__.py` (빈)

    2) `backend/research/README.md` 신규 생성:
       - 헤더: "# backend/research — R&D 비교군 전용"
       - 정책 박스 (한국어): "이 디렉터리는 NLF/SMPL-X 등 PS:License 1.0(비상업) 라이브러리 사용 코드 전용. **제품 코드(sunity_shared, functions/, runpod_inference/server.py)에서 import 금지**. Lambda 레이어 빌드(`ContentUri: shared/`)에 포함되지 않음 + .samignore로 명시적 차단."
       - "사용처: belle 사내 평가 — MediaPipe vs NLF 정확도 갭 측정, Phase 1 회귀 검증(D-13~D-16)"
       - "출시 게이트: 향후 NLF/SMPL-X를 제품 코드에 도입하려면 Max Planck Innovation 상업 라이선스 클리어 필수 — info@max-planck-innovation.de"
       - "디렉터리 구조": pose_engines/nlf/ + evaluations/ 표
       - "실행": `python -m backend.research.evaluations.compare_engines --motions ...` (Plan 06 산출물)
       - "라이선스 출처": https://is.mpg.de/ps/code, https://smpl-x.is.tue.mpg.de
       - 마지막에 "마지막 갱신: 2026-05-31, Phase 1"

    3) 파일 이동 (mv — git mv):
       - `backend/shared/python/sunity_shared/analysis/pose_estimator.py` → `backend/research/pose_engines/nlf/nlf_engine.py`
         + 파일 내부 docstring 추가 (top): "원본 위치 backend/shared/python/sunity_shared/analysis/pose_estimator.py — Phase 1 D-06/D-07로 R&D 격리. 제품 import 금지."
         + class 이름은 `NlfPoseEstimator` 유지 (R&D 측 코드와 호환). 필요 시 `NlfPoseEngine` alias 추가 (PoseEngine Protocol 구현 — list[PoseFrame] 반환 wrapper) — 단 본 plan에선 alias만, 실 구현은 Plan 06 회귀 검증 시 진행. 일단은 wrapper 시그니처 stub `def estimate_as_pose_frames(...) -> list[PoseFrame]: raise NotImplementedError("Plan 06에서 구현")`.
         + import 경로 갱신: `from sunity_shared.analysis.skeleton` 등 sunity_shared 의존은 유지 (역방향 import는 허용 — research → sunity_shared만, sunity_shared → research 금지).
       - `backend/scripts/nlf_l_multi.torchscript` → `backend/research/pose_engines/nlf/models/nlf_l_multi.torchscript`
       - `backend/yolo11n.pt` → `backend/research/pose_engines/nlf/models/yolo11n.pt`
       - `backend/scripts/verify_nlf_pipeline.py` → `backend/research/evaluations/verify_nlf_pipeline.py` (이름 유지 — 명확히 NLF)
       - `backend/scripts/verify_self_comparison.py` → `backend/research/evaluations/verify_self_comparison_nlf.py` (이름 변경 — NLF임 명시. MediaPipe 버전은 Plan 06에서 새로 작성 또는 본래 위치에 새 verify_self_comparison.py 생성)
       - `backend/scripts/extract_reference_angles.py` → `backend/research/evaluations/extract_reference_angles_nlf.py` (이름 변경 — NLF임 명시. MediaPipe 버전 추출 스크립트는 Plan 06 또는 후속 phase에서 생성)
       - 이동된 스크립트 내부 import 경로 수정: `from sunity_shared.analysis.pose_estimator import NlfPoseEstimator` → `from backend.research.pose_engines.nlf import NlfPoseEstimator` (또는 상대 경로 `from ..pose_engines.nlf import ...`). 모델 파일 경로 상수도 새 위치로 (`backend/research/pose_engines/nlf/models/nlf_l_multi.torchscript`).
       - 이동된 스크립트 docstring에 추가: "이전 위치 backend/scripts/ — Phase 1 D-06으로 backend/research/evaluations/로 이동. R&D 비교군 전용."
       - `backend/scripts/verify_nlf_overlay.py`, `verify_nlf_pipeline.py`, `extract_reference_angles.py`, `verify_self_comparison.py`, `_nlf_smoke.py`는 모두 이동 또는 삭제. 단순 NLF 의존만이라면 모두 backend/research/evaluations/로. `_nlf_smoke.py`도 마찬가지. 단 reference-angles.json·overlay_* 디렉터리 (결과 데이터)는 backend/scripts에 유지 (제품 데이터 — 이동하면 다운스트림 영향).

    4) `.samignore` 신규 생성 또는 추가 (Open Question Q1 권장):
       - 파일이 없으면 신규 생성, 있으면 append.
       - 추가 라인:
         ```
         research/
         backend/research/
         **/research/**
         ```
       - 주석: `# Phase 1 D-06/D-07: NLF/SMPL-X R&D 비교군 격리 — Lambda 레이어에 포함 금지`
       - 추가: `backend/scripts/nlf_*` `backend/scripts/verify_nlf_*` `backend/scripts/_nlf_smoke.py` `backend/yolo11n.pt` 패턴 (파일 이동 후 backend/scripts에 안 남아있어야 정상이지만 방어적 차단)
       
    5) `backend/shared/python/sunity_shared/analysis/pose_estimator.py` 파일 자체 삭제 (git rm 또는 mv 후 origin 자동 삭제). 파일 삭제만 — 다른 import 참조(pipeline/app.py)는 Plan 05에서 교체.

    6) commit 권장 message: `refactor(01-04): NLF R&D 격리 — backend/research로 이동 (D-06, D-07)`.

    **격리 안전 검증 (atomic swap 전 — 여기서 일부 import 깨짐을 의도적으로 받아들임):**
    - 본 plan 완료 시점에 `pipeline/app.py:131`은 여전히 `from sunity_shared.analysis.pose_estimator import NlfPoseEstimator`라 ImportError가 발생함. **이것은 의도된 중간 상태** — Plan 05 atomic swap이 같은 wave(2) 직후(wave 3)에 즉시 교체. backend/tests/test_pipeline_dispatch.py는 mock 처리되어 있으므로 ImportError 없을 가능성 — 확인 후 mock 보강 필요.
    - 단위 테스트(`pytest -x`)가 Plan 04 단독 시점에 ImportError로 깨지지 않도록 — Plan 04 acceptance criteria에 격리 단위 테스트(test_nlf_isolation.py)는 Task 2에서 추가 + pipeline_dispatch.py에서 NlfPoseEstimator import는 lazy 처리 또는 mock으로 대체. _ensure_adapters 호출 시점만 import되므로 module load는 깨지지 않음 — pytest 수집 시 ImportError 발생 안 함. 안전.
  </action>
  <verify>
    <automated>test -d backend/research/pose_engines/nlf && test -f backend/research/README.md && test -f .samignore && ! test -f backend/shared/python/sunity_shared/analysis/pose_estimator.py && ! test -f backend/scripts/nlf_l_multi.torchscript && grep -q "research/" .samignore && grep -q "제품 코드에서 import 금지" backend/research/README.md && echo "isolation structure OK"</automated>
  </verify>
  <acceptance_criteria>
    - `test -d backend/research/pose_engines/nlf` exits 0 (디렉터리 존재)
    - `test -f backend/research/README.md` exits 0
    - `test -f backend/research/pose_engines/nlf/nlf_engine.py` exits 0
    - `test -f backend/research/pose_engines/nlf/models/nlf_l_multi.torchscript` exits 0
    - `test -f backend/research/pose_engines/nlf/models/yolo11n.pt` exits 0
    - `test -f backend/research/evaluations/verify_nlf_pipeline.py` exits 0
    - `test -f backend/research/evaluations/verify_self_comparison_nlf.py` exits 0
    - `test -f backend/research/evaluations/extract_reference_angles_nlf.py` exits 0
    - `! test -f backend/shared/python/sunity_shared/analysis/pose_estimator.py` exits 0 (파일 삭제됨)
    - `! test -f backend/scripts/nlf_l_multi.torchscript` exits 0 (이동됨)
    - `! test -f backend/yolo11n.pt` exits 0 (이동됨)
    - `! test -f backend/scripts/verify_nlf_pipeline.py` exits 0 (이동됨)
    - `grep -nE "^research/$|^backend/research/$" .samignore` matches at least 1 line
    - `grep -nE "PS:License 1\\.0|Max Planck|info@max-planck-innovation" backend/research/README.md` matches at least 1 line (라이선스 정책 명시)
    - `grep -nE "제품 코드에서 import 금지|제품 import 금지" backend/research/README.md` matches at least 1 line (D-07 박제)
    - `git status --short` shows expected mv operations (renames detected by git)
  </acceptance_criteria>
  <done>NLF 코드 + 모델 파일 + 검증 스크립트 모두 backend/research/로 이동. .samignore에 research/ 차단. backend/research/README.md에 R&D 사용 정책 + PS:License 1.0 + Max Planck 채널 명시. pose_estimator.py 파일 자체 삭제 (제품 패키지에서 사라짐). pipeline/app.py:131의 import는 여전히 깨진 상태 — Plan 05 atomic swap으로 wave 3에서 교체.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NLF 격리 단위 테스트 + import 경로 차단 검증</name>
  <files>backend/tests/test_nlf_isolation.py</files>
  <read_first>
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md D-06, D-07 (물리 경로 분리 = 격리 방식)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-VALIDATION.md (NLF isolation test 요구)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md §Security Domain T-1-01
    - Task 1 결과 (backend/research/ 구조)
    - backend/tests/conftest.py (test infrastructure)
  </read_first>
  <behavior>
    - Test 1 (test_pose_estimator_module_not_in_sunity_shared): `import sunity_shared.analysis.pose_estimator` → ModuleNotFoundError 발생.
    - Test 2 (test_nlf_engine_importable_from_research): `from backend.research.pose_engines.nlf import NlfPoseEstimator` → 성공 (격리 위치에서는 정상 import). 단 lazy import이므로 torch/ultralytics 부재 시에도 module load 성공.
    - Test 3 (test_research_dir_excluded_from_sam_layer): SAM `ContentUri: shared/` 빌드 시 research/ 부재 — `template.yaml`의 ContentUri 값이 `shared/`임을 grep으로 검증 + research/는 shared/ 하위에 없음을 path check.
    - Test 4 (test_samignore_blocks_research): `.samignore` 파일에 research/ 패턴 존재. `grep -E "^research/|^backend/research/|\\*\\*/research/\\*\\*" .samignore` 매치 >= 1.
    - Test 5 (test_no_nlf_imports_in_sunity_shared): `grep -rE "import nlf|from .*nlf|NlfPoseEstimator|NLF" backend/shared/python/sunity_shared/` → 0 매치 (또는 docstring/주석만 — 코드 import 0).
    - Test 6 (test_no_nlf_model_files_in_backend_scripts): `nlf_l_multi.torchscript`, `yolo11n.pt`, `_nlf_smoke.py` 모두 backend/scripts/와 backend/ 루트에 부재.
  </behavior>
  <action>
    `backend/tests/test_nlf_isolation.py` 신규 생성:
    - module docstring: "Phase 1 D-06/D-07 NLF 격리 검증. T-1-01 위협 차단."
    - imports: `import pytest`, `import subprocess`, `import os`, `from pathlib import Path`.
    - Test 1 (test_pose_estimator_module_not_in_sunity_shared):
      `with pytest.raises(ModuleNotFoundError): import importlib; importlib.import_module("sunity_shared.analysis.pose_estimator")`
    - Test 2 (test_nlf_engine_importable_from_research):
      backend가 sys.path에 있는 가정. `import importlib; mod = importlib.import_module("backend.research.pose_engines.nlf"); assert hasattr(mod, 'NlfPoseEstimator')` — 단 backend가 package가 아니면 sys.path.insert로 backend/ 부모를 추가 + research를 package로 인식. 또는 직접 spec from file. 가장 robust한 방법: `importlib.util.spec_from_file_location` + Path("backend/research/pose_engines/nlf/__init__.py"). 단순화: `subprocess.run(["python", "-c", "import sys; sys.path.insert(0, '.'); from backend.research.pose_engines.nlf import NlfPoseEstimator"], check=True)` — return code 0 검증.
    - Test 3 (test_research_dir_excluded_from_sam_layer):
      `tpl = Path("backend/template.yaml").read_text(); assert "ContentUri: shared/" in tpl; assert "ContentUri: research/" not in tpl`
    - Test 4 (test_samignore_blocks_research):
      `samignore = Path(".samignore").read_text(); patterns = ["research/", "backend/research/", "**/research/**"]; assert any(p in samignore for p in patterns)`
    - Test 5 (test_no_nlf_imports_in_sunity_shared):
      `result = subprocess.run(["grep", "-rE", "import nlf|from .*nlf|NlfPoseEstimator", "backend/shared/python/sunity_shared/", "--include=*.py"], capture_output=True, text=True); assert result.stdout.strip() == "" or all("#" in line or '"""' in line for line in result.stdout.strip().split("\\n"))` — 정확히 0 매치 또는 주석 라인만.
    - Test 6 (test_no_nlf_model_files_in_backend_scripts):
      forbidden = [Path("backend/scripts/nlf_l_multi.torchscript"), Path("backend/yolo11n.pt"), Path("backend/scripts/_nlf_smoke.py"), Path("backend/scripts/verify_nlf_pipeline.py"), Path("backend/scripts/extract_reference_angles.py")]; for p in forbidden: assert not p.exists(), f"{p} should be moved to backend/research/"

    각 테스트는 from cwd = 프로젝트 루트(/Users/kimtaesung/Dev/SunityMotion) 가정 (`backend/tests/conftest.py` 또는 pytest rootdir 설정). 필요 시 monkeypatch.chdir로 보정.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && pytest backend/tests/test_nlf_isolation.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `pytest backend/tests/test_nlf_isolation.py -x` exits 0 (6 tests pass)
    - `python -c "import sunity_shared.analysis.pose_estimator"` exits with ModuleNotFoundError (Plan 04 Task 1 완료 후 보장)
    - `grep -rE "NlfPoseEstimator|from .*\\.pose_estimator import" backend/shared/python/sunity_shared/ --include="*.py" | grep -v "#" | wc -l` outputs `0`
    - `find backend/scripts -name "nlf_*" -o -name "*_nlf_*"` returns empty
    - `find backend -maxdepth 2 -name "yolo11n.pt"` returns empty (이동 완료)
    - `grep -E "^(backend/)?research/|\\*\\*/research/\\*\\*" .samignore` matches at least 1 line
    - 격리 단위 테스트가 backend/tests/ 안에 있되 backend/research/ 를 import 검증 — cross-directory 의존이지만 단방향 (test → research) 허용
  </acceptance_criteria>
  <done>6개 단위 테스트로 NLF 격리 강제 검증. ImportError 보장 + .samignore 확인 + grep으로 sunity_shared에 NLF 참조 0 보장 + 모델 파일 부재 확인. T-1-01 위협 mitigation 완료.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sunity_shared 패키지 ↔ backend/research/ | 단방향 (research → sunity_shared 허용, 역방향 금지). 물리 경로 분리만으로 강제 (D-07) |
| Lambda 레이어 빌드 ↔ research/ | SAM ContentUri: shared/ + .samignore research/ — 이중 차단 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | License violation (Compliance — STRIDE outside scope이나 belle 명시 위협) | NLF/torch/ultralytics가 제품 코드에 포함 | mitigate | 본 plan에서 물리 격리 + .samignore + 단위 테스트 6개로 강제. PS:License 1.0(비상업) 위반 0 |
| T-1-12 | Repudiation | git mv 누락 시 파일 유실 | mitigate | git mv 사용 + Task 1 acceptance에서 양쪽 위치 모두 확인 (old 부재 + new 존재) |
| T-1-13 | Tampering | 누군가 sunity_shared에 NLF import 재추가 | mitigate | Test 5 (grep 검증)가 CI에서 강제 — Wave 0~3 모든 단계에서 pytest 수행 |
</threat_model>

<verification>
- `pytest backend/tests/test_nlf_isolation.py -x` exits 0 (6 tests pass)
- `git status` shows expected file moves (pose_estimator.py, scripts/*.py, models, etc.)
- `find backend/shared/python/sunity_shared -name "pose_estimator.py"` returns empty
- `cat backend/research/README.md` 출력에 PS:License 1.0 + Max Planck 채널 명시
- Plan 04 종료 시점 임시 inconsistency: `pipeline/app.py:131`의 import가 실제 호출 시(Lambda _ensure_adapters) ImportError 발생할 수 있음 — Plan 05(같은 wave 다음)에서 즉시 swap으로 해소
</verification>

<success_criteria>
- backend/research/ 디렉터리 + 파일 구조 완성 (Task 1)
- 11개 atomic swap 대상 중 격리 부분(8개 파일/디렉터리 이동) 완료
- pose_estimator.py 모듈이 제품 패키지에서 완전 사라짐 (D-06)
- .samignore에 research/ 차단 (D-07 + Open Question Q1)
- T-1-01 라이선스 위협 mitigation
- 6개 단위 테스트로 격리 강제 검증
</success_criteria>

<output>
Create `.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-04-SUMMARY.md` when done. SUMMARY 내용:
- 이동된 파일 11개 list (old path → new path)
- backend/research/README.md 핵심 정책 인용
- .samignore 추가 패턴
- 6개 격리 테스트 결과
- 임시 inconsistency 경고: pipeline/app.py:131이 여전히 NLF 참조 — Plan 05에서 즉시 교체 필수
- belle 확인 사항: backend/research/pose_engines/nlf/nlf_engine.py의 PoseEngine Protocol 구현 wrapper(`estimate_as_pose_frames`)는 stub — Plan 06 회귀 검증에서 구현 (NLF → list[PoseFrame] 변환)
</output>
