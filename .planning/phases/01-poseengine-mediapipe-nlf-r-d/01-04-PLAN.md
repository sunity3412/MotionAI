---
phase: 01-poseengine-mediapipe-nlf-r-d
plan: 04
type: execute
wave: 3
depends_on:
  - 01-06
files_modified:
  - backend/research/__init__.py
  - backend/research/README.md
  - backend/research/pose_engines/__init__.py
  - backend/research/pose_engines/nlf/__init__.py
  - backend/research/pose_engines/nlf/nlf_engine.py
  - backend/research/pose_engines/nlf/models/.gitkeep
  - backend/research/evaluations/verify_nlf_pipeline.py
  - backend/research/evaluations/verify_self_comparison_nlf.py
  - backend/research/evaluations/extract_reference_angles_nlf.py
  - backend/research/evaluations/_nlf_smoke.py
  - backend/research/evaluations/verify_nlf_overlay.py
  - backend/shared/python/sunity_shared/analysis/pose_estimator.py
  - backend/scripts/nlf_l_multi.torchscript
  - backend/yolo11n.pt
  - backend/scripts/verify_nlf_pipeline.py
  - backend/scripts/verify_self_comparison.py
  - backend/scripts/extract_reference_angles.py
  - backend/scripts/_nlf_smoke.py
  - backend/scripts/verify_nlf_overlay.py
  - .samignore
  - backend/tests/test_nlf_isolation.py
autonomous: true
requirements:
  - POSE-01
must_haves:
  truths:
    - "NLF 코드(NlfPoseEstimator, nlf_l_multi.torchscript, yolo11n.pt)가 backend/research/ 외부 import 경로에서 제거됨"
    - "import sunity_shared.analysis.pose_estimator 시 ModuleNotFoundError 발생 (D-06 박제)"
    - "backend/research가 .samignore에 명시되어 SAM 빌드 결과물에 포함 안 됨 (D-07 박제)"
    - "기존 NLF 검증 스크립트 6개 (verify_nlf_pipeline, verify_self_comparison, extract_reference_angles, _nlf_smoke, verify_nlf_overlay) 모두 backend/research/evaluations/로 이동 — backend/scripts에 NLF/YOLO 잔여 0 (H-5 박제)"
    - "본 plan은 Wave 3 — Plan 06 belle 회귀 검증 승인 후에만 진행 (H-1 박제)"
  artifacts:
    - path: "backend/research/README.md"
      provides: "R&D 비교군 전용 사용 정책 명시 (PS:License 1.0, 제품 import 금지)"
    - path: "backend/research/pose_engines/nlf/nlf_engine.py"
      provides: "기존 NlfPoseEstimator를 새 위치로 이전"
    - path: ".samignore"
      provides: "research/ 경로 SAM 빌드 차단"
    - path: "backend/tests/test_nlf_isolation.py"
      provides: "ImportError 보장 + research 격리 단위 검증 + H-5 강제 검증"
  key_links:
    - from: "sunity_shared.analysis"
      to: "backend/research/pose_engines/nlf"
      via: "물리 경로 분리 (D-06, D-07) — import 경로 단절"
      pattern: "ModuleNotFoundError"
    - from: ".samignore"
      to: "Lambda layer 빌드"
      via: "research/ 디렉터리 제외"
      pattern: "research/"
---

<objective>
**Plan 06 belle checkpoint 승인 후** NLF 관련 코드 + 모델 파일 + 검증 스크립트를 모두 `backend/research/`로 이동(D-06, D-07). `sunity_shared/analysis/pose_estimator.py`와 NLF 의존 scripts는 제품 import 경로에서 완전히 사라진다. 본 plan과 Plan 05는 같은 wave (3)에서 atomic하게 진행 — 한 번에 격리 + swap.

Purpose: 제품 코드의 라이선스 리스크 0 보장 (PS:License 1.0 비상업이 sunity_shared·Lambda layer에 부재 = 라이선스 위반 0). T-1-01 위협(NLF 격리 우회) 차단. REVIEWS H-1 (Wave 2 belle gate 통과 후 진행) + H-5 (files_modified에 _nlf_smoke.py, verify_nlf_overlay.py 명시 + acceptance grep 강제) 박제.

Output: backend/research/ 디렉터리 구조 + 파일 이동 (8개 NLF 관련 파일) + README + .samignore + 단위 테스트 (ImportError 검증 + H-5 grep 검증).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-VALIDATION.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-REVIEWS.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-06-SUMMARY.md
@backend/shared/python/sunity_shared/analysis/pose_estimator.py
@backend/scripts/verify_nlf_pipeline.py
@backend/scripts/verify_self_comparison.py
@backend/scripts/extract_reference_angles.py
@backend/template.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: backend/research/ 구조 + README + .samignore + NLF 파일 이동 (H-5 박제 — 8개 파일 모두)</name>
  <files>backend/research/__init__.py, backend/research/README.md, backend/research/pose_engines/__init__.py, backend/research/pose_engines/nlf/__init__.py, backend/research/pose_engines/nlf/nlf_engine.py, backend/research/pose_engines/nlf/models/.gitkeep, backend/research/evaluations/verify_nlf_pipeline.py, backend/research/evaluations/verify_self_comparison_nlf.py, backend/research/evaluations/extract_reference_angles_nlf.py, backend/research/evaluations/_nlf_smoke.py, backend/research/evaluations/verify_nlf_overlay.py, backend/shared/python/sunity_shared/analysis/pose_estimator.py (deletion), backend/scripts/nlf_l_multi.torchscript (move), backend/yolo11n.pt (move), backend/scripts/verify_nlf_pipeline.py (move), backend/scripts/verify_self_comparison.py (move + rename), backend/scripts/extract_reference_angles.py (move + rename), backend/scripts/_nlf_smoke.py (move), backend/scripts/verify_nlf_overlay.py (move), .samignore</files>
  <read_first>
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-REVIEWS.md H-5 (files_modified에 _nlf_smoke.py + verify_nlf_overlay.py 명시 + acceptance grep 강제) + H-1 (Wave 3에 위치 — Plan 06 후)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md D-06, D-07, D-08 (격리 방식 + atomic swap)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md §NLF 격리 — atomic swap 대상 라인
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md §Recommended Project Structure
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md §Open Questions Q1 (.samignore 차단)
    - backend/shared/python/sunity_shared/analysis/pose_estimator.py (전체 — 어디로 이동할지 파악)
    - backend/scripts/verify_nlf_pipeline.py, verify_self_comparison.py, extract_reference_angles.py, _nlf_smoke.py, verify_nlf_overlay.py (전체 — 이동 대상)
    - backend/template.yaml line 60-70 (ContentUri: shared/)
    - backend/research/__init__.py (Plan 06 Task 1에서 이미 생성됨 — 재사용)
    - backend/research/evaluations/__init__.py (Plan 06 Task 1에서 이미 생성됨 — 본 plan에서 verify_* import 추가)
    - CLAUDE.md (라이선스 박스 + R&D 격리 정책)
  </read_first>
  <action>
    **참고**: backend/research/ + backend/research/evaluations/ + .gitkeep + compare_engines.py 등은 Plan 06 Task 1에서 이미 생성됨. 본 plan은 추가로 nlf/ 서브디렉터리 + verify_* 이동만 수행.

    1) 신규 서브디렉터리 + __init__.py:
       - `backend/research/pose_engines/__init__.py` (빈 + docstring: "R&D 비교군 — 제품 코드 import 금지. PS:License 1.0(비상업).")
       - `backend/research/pose_engines/nlf/__init__.py` (`from .nlf_engine import NlfPoseEstimator` re-export)
       - `backend/research/pose_engines/nlf/models/.gitkeep` (빈)
       - `backend/research/__init__.py` docstring 갱신 (Plan 06 Task 1의 초안에 NLF 격리 추가 명시).
       - `backend/research/evaluations/__init__.py` 갱신: `from .compare_engines import run_engine, EngineResult, AVG_CONFIDENCE_THRESHOLD` (Plan 06 산출 유지) + `# verify_* / extract_reference_angles_nlf / _nlf_smoke 모듈은 직접 실행 — re-export 안 함`

    2) `backend/research/README.md` 신규 생성:
       - 헤더: "# backend/research — R&D 비교군 전용"
       - 정책 박스 (한국어): "이 디렉터리는 NLF/SMPL-X 등 PS:License 1.0(비상업) 라이브러리 사용 코드 전용. **제품 코드(sunity_shared, functions/, runpod_inference/server.py)에서 import 금지**. Lambda 레이어 빌드(`ContentUri: shared/`)에 포함되지 않음 + .samignore로 명시적 차단."
       - "사용처: belle 사내 평가 — MediaPipe vs NLF 정확도 갭 측정, Phase 1 회귀 검증(D-13~D-16, Plan 06 Task 2)"
       - "출시 게이트: NLF/SMPL-X 제품 도입 시 Max Planck Innovation 상업 라이선스 — info@max-planck-innovation.de"
       - "디렉터리 구조": pose_engines/nlf/ + evaluations/ 표 + 8개 NLF 관련 파일 목록 (H-5 박제)
       - "실행": `python -m backend.research.evaluations.compare_engines --motions ...` + `python -m backend.research.evaluations.verify_nlf_pipeline ...` 등
       - "라이선스 출처": https://is.mpg.de/ps/code, https://smpl-x.is.tue.mpg.de
       - "마지막 갱신: 2026-05-31, Phase 1 Wave 3"

    3) 파일 이동 (git mv) — H-5 박제: **8개 파일 모두 명시**:
       a) `backend/shared/python/sunity_shared/analysis/pose_estimator.py` → `backend/research/pose_engines/nlf/nlf_engine.py`
          + 파일 내부 docstring 추가 (top): "원본 위치 backend/shared/python/sunity_shared/analysis/pose_estimator.py — Phase 1 D-06/D-07로 R&D 격리. 제품 import 금지."
          + class 이름은 `NlfPoseEstimator` 유지
          + import 경로 갱신: `from sunity_shared.analysis.skeleton` 등 sunity_shared 의존은 유지 (역방향 import 허용 — research → sunity_shared만, 반대는 금지).
       b) `backend/scripts/nlf_l_multi.torchscript` → `backend/research/pose_engines/nlf/models/nlf_l_multi.torchscript`
       c) `backend/yolo11n.pt` → `backend/research/pose_engines/nlf/models/yolo11n.pt`
       d) `backend/scripts/verify_nlf_pipeline.py` → `backend/research/evaluations/verify_nlf_pipeline.py` (이름 유지)
       e) `backend/scripts/verify_self_comparison.py` → `backend/research/evaluations/verify_self_comparison_nlf.py` (rename — NLF 명시)
       f) `backend/scripts/extract_reference_angles.py` → `backend/research/evaluations/extract_reference_angles_nlf.py` (rename)
       g) `backend/scripts/_nlf_smoke.py` → `backend/research/evaluations/_nlf_smoke.py` (H-5 박제 — 명시적 이동)
       h) `backend/scripts/verify_nlf_overlay.py` → `backend/research/evaluations/verify_nlf_overlay.py` (H-5 박제 — 명시적 이동)
       - 이동된 스크립트 내부 import 경로 수정: `from sunity_shared.analysis.pose_estimator import NlfPoseEstimator` → `from backend.research.pose_engines.nlf import NlfPoseEstimator` (또는 `from ..pose_engines.nlf import ...` 상대). 모델 파일 경로 상수도 새 위치(`backend/research/pose_engines/nlf/models/`).
       - 이동된 스크립트 docstring에 추가: "이전 위치 backend/scripts/ — Phase 1 D-06 + REVIEWS H-5로 backend/research/evaluations/로 이동. R&D 비교군 전용."
       - reference-angles.json 등 결과 데이터는 backend/scripts에 유지 (제품 데이터 — 다운스트림 영향).

    4) `.samignore` 신규 생성 또는 추가:
       - 파일 없으면 신규, 있으면 append.
       - 추가 라인:
         `research/`
         `backend/research/`
         `**/research/**`
       - 주석: `# Phase 1 D-06/D-07: NLF/SMPL-X R&D 비교군 격리 — Lambda 레이어 포함 금지 (H-1 박제: Wave 2 belle 승인 후 진행)`
       - 추가 패턴 (방어적): `backend/scripts/nlf_*` `backend/scripts/verify_nlf_*` `backend/scripts/_nlf_smoke.py` `backend/yolo11n.pt`

    5) `backend/shared/python/sunity_shared/analysis/pose_estimator.py` 삭제 (git rm).

    6) commit message: `refactor(01-04): NLF R&D 격리 — backend/research로 이동 (D-06, D-07, H-5 박제)`.

    **격리 안전 검증 (atomic swap 동반 — 본 plan 단독 종료 시 중간 상태 — Plan 05가 같은 wave에서 즉시 swap)**:
    - 본 plan 완료 시점에 `pipeline/app.py:131`은 여전히 `from sunity_shared.analysis.pose_estimator import NlfPoseEstimator`라 ImportError 발생. Plan 05가 같은 wave에서 즉시 swap.
    - Plan 05를 본 plan과 같은 wave에서 atomic 진행 — 두 plan을 같은 wave에서 순서대로 (04 먼저, 05 직후) 진행.
  </action>
  <verify>
    <automated>test -d backend/research/pose_engines/nlf && test -f backend/research/README.md && test -f .samignore && ! test -f backend/shared/python/sunity_shared/analysis/pose_estimator.py && ! test -f backend/scripts/nlf_l_multi.torchscript && ! test -f backend/scripts/_nlf_smoke.py && ! test -f backend/scripts/verify_nlf_overlay.py && grep -q "research/" .samignore && grep -q "제품 코드에서 import 금지" backend/research/README.md && echo "isolation structure OK"</automated>
  </verify>
  <acceptance_criteria>
    - `test -d backend/research/pose_engines/nlf` exits 0
    - `test -f backend/research/README.md` exits 0
    - `test -f backend/research/pose_engines/nlf/nlf_engine.py` exits 0
    - `test -f backend/research/pose_engines/nlf/models/nlf_l_multi.torchscript` exits 0
    - `test -f backend/research/pose_engines/nlf/models/yolo11n.pt` exits 0
    - `test -f backend/research/evaluations/verify_nlf_pipeline.py` exits 0
    - `test -f backend/research/evaluations/verify_self_comparison_nlf.py` exits 0
    - `test -f backend/research/evaluations/extract_reference_angles_nlf.py` exits 0
    - `test -f backend/research/evaluations/_nlf_smoke.py` exits 0 (H-5 박제)
    - `test -f backend/research/evaluations/verify_nlf_overlay.py` exits 0 (H-5 박제)
    - `! test -f backend/shared/python/sunity_shared/analysis/pose_estimator.py` exits 0 (파일 삭제됨)
    - `! test -f backend/scripts/nlf_l_multi.torchscript` exits 0
    - `! test -f backend/yolo11n.pt` exits 0
    - `! test -f backend/scripts/verify_nlf_pipeline.py` exits 0
    - `! test -f backend/scripts/verify_self_comparison.py` exits 0
    - `! test -f backend/scripts/extract_reference_angles.py` exits 0
    - `! test -f backend/scripts/_nlf_smoke.py` exits 0 (H-5 박제)
    - `! test -f backend/scripts/verify_nlf_overlay.py` exits 0 (H-5 박제)
    - `! ls backend/scripts/ 2>/dev/null | grep -iE 'nlf|yolo'` exits 0 (H-5 박제 — backend/scripts에 NLF/YOLO 잔여 0; grep 매치 시 exit 1)
    - `! grep -rE 'from sunity_shared\\.analysis\\.pose_estimator|import.*NLF' backend/functions/pipeline/ backend/runpod_inference/ 2>/dev/null` exits 0 (제품 코드 경로에 NLF 참조 0 — Plan 05 atomic swap 완료 가정. 본 plan 단독 시점에서는 pipeline/app.py에 NLF 참조가 남아 있을 수 있어 본 acceptance는 Plan 05 완료 후 검증)
    - `grep -nE "^research/|^backend/research/|\\*\\*/research/\\*\\*" .samignore | wc -l` outputs at least `1`
    - `grep -nE "PS:License 1\\.0|Max Planck|info@max-planck-innovation" backend/research/README.md | wc -l` outputs at least `1`
    - `grep -nE "제품 코드에서 import 금지|제품 import 금지" backend/research/README.md | wc -l` outputs at least `1`
    - `git status --short | grep -E "^R " | wc -l` outputs at least `5` (8 git renames detected — at least 5 detected as renames)
  </acceptance_criteria>
  <done>8개 NLF 관련 파일 모두 backend/research/로 이동 (pose_estimator.py + 2 model files + 5 verify/script files). .samignore에 research/ 차단. backend/research/README.md에 R&D 사용 정책 + PS:License 1.0 + Max Planck 채널 명시. pose_estimator.py 파일 자체 삭제. H-5 박제 — backend/scripts에 nlf/yolo 잔여 0 grep 검증 + 8개 파일 명시. pipeline/app.py:131의 import는 Plan 05 atomic swap에서 즉시 교체 (같은 wave 3).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NLF 격리 단위 테스트 + H-5 grep 검증</name>
  <files>backend/tests/test_nlf_isolation.py</files>
  <read_first>
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-REVIEWS.md H-5 (grep으로 강제 검증 — backend/scripts에 nlf/yolo 잔여 0)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md D-06, D-07
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-VALIDATION.md (NLF isolation test 요구)
    - Task 1 결과 (backend/research/ 구조)
    - backend/tests/conftest.py
  </read_first>
  <behavior>
    - Test 1 (test_pose_estimator_module_not_in_sunity_shared): `import sunity_shared.analysis.pose_estimator` → ModuleNotFoundError.
    - Test 2 (test_nlf_engine_importable_from_research): backend.research.pose_engines.nlf import NlfPoseEstimator 성공 (lazy 의존).
    - Test 3 (test_research_dir_excluded_from_sam_layer): template.yaml의 ContentUri = `shared/` + research 부재.
    - Test 4 (test_samignore_blocks_research): .samignore에 research/ 패턴 존재.
    - Test 5 (test_no_nlf_imports_in_sunity_shared): grep으로 sunity_shared/ 안 NLF 참조 0.
    - Test 6 (test_no_nlf_model_files_in_backend_scripts): nlf_l_multi.torchscript, yolo11n.pt, _nlf_smoke.py, verify_nlf_pipeline.py, verify_nlf_overlay.py, verify_self_comparison.py, extract_reference_angles.py 모두 부재 (H-5 박제).
    - Test 7 (test_backend_scripts_no_nlf_yolo): subprocess `ls backend/scripts/ | grep -iE 'nlf|yolo'` empty (H-5 박제).
    - Test 8 (test_pipeline_runpod_no_nlf_imports): subprocess `grep -rE 'from sunity_shared\\.analysis\\.pose_estimator|import.*NLF' backend/functions/pipeline/ backend/runpod_inference/` empty (H-5 박제 — Plan 05 swap 후).
  </behavior>
  <action>
    `backend/tests/test_nlf_isolation.py` 신규 생성:
    - module docstring: "Phase 1 D-06/D-07 NLF 격리 + REVIEWS H-5 강제 검증. T-1-01 위협 차단."
    - imports: pytest, subprocess, os, pathlib.
    - Test 1: `with pytest.raises(ModuleNotFoundError): import importlib; importlib.import_module("sunity_shared.analysis.pose_estimator")`
    - Test 2: `subprocess.run(["python", "-c", "import sys; sys.path.insert(0, '.'); from backend.research.pose_engines.nlf import NlfPoseEstimator"], check=True)` — return code 0
    - Test 3: `tpl = Path("backend/template.yaml").read_text(); assert "ContentUri: shared/" in tpl and "ContentUri: research/" not in tpl`
    - Test 4: `samignore = Path(".samignore").read_text(); patterns = ["research/", "backend/research/", "**/research/**"]; assert any(p in samignore for p in patterns)`
    - Test 5: `result = subprocess.run(["grep", "-rE", "import nlf|from .*nlf|NlfPoseEstimator", "backend/shared/python/sunity_shared/", "--include=*.py"], capture_output=True, text=True); meaningful_matches = [l for l in result.stdout.strip().split("\\n") if l and "#" not in l.split(":", 2)[-1][:5]]; assert len(meaningful_matches) == 0, meaningful_matches`
    - Test 6 (H-5 박제): forbidden = [Path("backend/scripts/nlf_l_multi.torchscript"), Path("backend/yolo11n.pt"), Path("backend/scripts/_nlf_smoke.py"), Path("backend/scripts/verify_nlf_pipeline.py"), Path("backend/scripts/extract_reference_angles.py"), Path("backend/scripts/verify_self_comparison.py"), Path("backend/scripts/verify_nlf_overlay.py")]; for p in forbidden: assert not p.exists(), f"{p} should be moved to backend/research/"
    - Test 7 (H-5 박제): `result = subprocess.run(["bash", "-c", "ls backend/scripts/ 2>/dev/null | grep -iE 'nlf|yolo' || true"], capture_output=True, text=True); assert result.stdout.strip() == "", f"backend/scripts에 NLF/YOLO 잔여 파일: {result.stdout}"`
    - Test 8 (H-5 박제 — Plan 05 완료 후 검증): `result = subprocess.run(["grep", "-rE", "from sunity_shared\\.analysis\\.pose_estimator|NlfPoseEstimator", "backend/functions/pipeline/", "backend/runpod_inference/", "--include=*.py"], capture_output=True, text=True); meaningful = [l for l in result.stdout.strip().split("\\n") if l and "#" not in l.split(":", 2)[-1][:5]]; assert len(meaningful) == 0, meaningful`

    각 테스트는 cwd = 프로젝트 루트(/Users/kimtaesung/Dev/SunityMotion) 가정. 필요 시 monkeypatch.chdir.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && pytest backend/tests/test_nlf_isolation.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `pytest backend/tests/test_nlf_isolation.py -x` exits 0 (8 tests pass — Plan 05 완료 가정. 본 plan 단독에서 Test 8은 일시적 FAIL — Plan 05 wave 3 같은 wave에서 즉시 통과)
    - `python -c "import sunity_shared.analysis.pose_estimator"` exits with ModuleNotFoundError
    - `grep -rE "NlfPoseEstimator|from .*\\.pose_estimator import" backend/shared/python/sunity_shared/ --include="*.py" | grep -v "^[[:space:]]*#" | wc -l` outputs `0`
    - `find backend/scripts -name "nlf_*" -o -name "*_nlf_*"` returns empty (H-5 박제)
    - `find backend -maxdepth 2 -name "yolo11n.pt"` returns empty (H-5 박제)
    - `bash -c "ls backend/scripts/ | grep -iE 'nlf|yolo' || true"` outputs empty (H-5 박제)
    - `grep -E "^(backend/)?research/|\\*\\*/research/\\*\\*" .samignore` matches at least 1 line
  </acceptance_criteria>
  <done>8개 단위 테스트로 NLF 격리 강제 (D-06/D-07/H-5). ImportError + .samignore + grep으로 sunity_shared NLF 0 + backend/scripts NLF/YOLO 잔여 0 (H-5) + pipeline/runpod NLF 0 (H-5, Plan 05 후).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sunity_shared 패키지 ↔ backend/research/ | 단방향 (research → sunity_shared 허용, 역방향 금지). 물리 경로 분리만으로 강제 (D-07) |
| Lambda 레이어 빌드 ↔ research/ | SAM ContentUri: shared/ + .samignore research/ — 이중 차단 |
| Wave 2 → Wave 3 gate | Plan 06 belle checkpoint approved 신호 없이 본 plan 진행 금지 (H-1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | License violation | NLF/torch/ultralytics가 제품 코드 포함 | mitigate | 본 plan 물리 격리 + .samignore + 8개 단위 테스트 (H-5 박제) |
| T-1-12 | Repudiation | git mv 누락 시 파일 유실 | mitigate | git mv 사용 + Task 1 acceptance에서 양쪽 위치 모두 확인 |
| T-1-13 | Tampering | 누군가 sunity_shared에 NLF import 재추가 | mitigate | Test 5 (grep 검증) CI 강제 |
| T-1-22 | Spoofing | H-5 우회 — _nlf_smoke.py / verify_nlf_overlay.py 이동 누락 | mitigate | files_modified 명시 + Test 6 + Test 7 grep 검증 |
</threat_model>

<verification>
- `pytest backend/tests/test_nlf_isolation.py -x` exits 0 (8 tests, Test 8은 Plan 05 후)
- `git status` shows expected file moves (pose_estimator.py, scripts/*.py, models)
- `find backend/shared/python/sunity_shared -name "pose_estimator.py"` returns empty
- `cat backend/research/README.md` 출력에 PS:License 1.0 + Max Planck 채널 명시
- Plan 04 + Plan 05 같은 wave 3 — atomic 진행. Plan 04 단독 시점 일시 inconsistency는 Plan 05 즉시 해소.
- H-5 박제: backend/scripts에 NLF/YOLO 잔여 0, files_modified에 8개 파일 명시.
</verification>

<success_criteria>
- backend/research/ 디렉터리 + 8개 NLF 관련 파일 이동 완료 (H-5 박제)
- pose_estimator.py 모듈이 제품 패키지에서 완전 사라짐 (D-06)
- .samignore에 research/ 차단 (D-07)
- T-1-01 라이선스 위협 mitigation
- 8개 단위 테스트로 격리 + H-5 강제 검증
- Wave 2 belle 승인 후 진행 (H-1 박제)
</success_criteria>

<output>
Create `.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-04-SUMMARY.md` when done. SUMMARY 내용:
- 이동된 파일 8개 list (old path → new path) — H-5 박제 확인
- backend/research/README.md 핵심 정책 인용
- .samignore 추가 패턴
- 8개 격리 테스트 결과
- 임시 inconsistency: Plan 05 atomic swap 직전 (같은 wave) — 즉시 해소
- belle 확인: backend/research/pose_engines/nlf/nlf_engine.py가 NLF estimate 시그니처 유지 (compare_engines.py 호환 — Plan 06이 NLF를 옛 위치에서 import한 시점에 이미 검증됨)
</output>
