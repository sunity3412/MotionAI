---
phase: 01-poseengine-mediapipe-nlf-r-d
plan: 05
type: execute
wave: 3
depends_on:
  - 01-02
  - 01-03
  - 01-04
files_modified:
  - backend/functions/pipeline/app.py
  - backend/runpod_inference/requirements.txt
  - backend/runpod_inference/server.py
  - backend/runpod_inference/setup.sh
  - backend/tests/test_pipeline_dispatch.py
autonomous: true
requirements:
  - POSE-01
  - POSE-02
must_haves:
  truths:
    - "pipeline/app.py가 MediaPipePoseEngine + HoughPoleDetector로 동작 (NLF 참조 0)"
    - "RunPod requirements.txt에 mediapipe, opencv-python-headless, scipy 추가 + ultralytics 제거"
    - "Lambda 폴백 경로가 ConfigurationError로 fail-fast (RunPod 위임 미설정 시 명확한 에러)"
    - "pose_landmarker_heavy.task 모델 파일이 setup.sh에서 다운로드되고 MEDIAPIPE_POSE_MODEL_PATH env로 주입"
    - "pipeline_dispatch 통합 테스트가 MediaPipe mock 경로로 동작"
  artifacts:
    - path: "backend/functions/pipeline/app.py"
      provides: "MediaPipe + 폴 축 정렬 파이프라인 (NLF 호출 0)"
    - path: "backend/runpod_inference/requirements.txt"
      provides: "mediapipe + opencv-python-headless + scipy 의존"
    - path: "backend/runpod_inference/setup.sh"
      provides: "pose_landmarker_heavy.task 다운로드 + 환경 변수 설정"
  key_links:
    - from: "pipeline/app.py"
      to: "MediaPipePoseEngine + HoughPoleDetector"
      via: "_ensure_adapters() lazy load + _angles_from_video() 새 흐름"
      pattern: "MediaPipePoseEngine|HoughPoleDetector"
    - from: "RunPod setup.sh"
      to: "Google CDN pose_landmarker_heavy.task"
      via: "wget + 환경 변수 export"
      pattern: "storage.googleapis.com/mediapipe-models"
---

<objective>
Plan 02/03/04 결과를 활용해 atomic swap을 수행한다 — `pipeline/app.py:131-132`의 NLF 참조를 MediaPipePoseEngine + HoughPoleDetector + PoleAxisAligner 흐름으로 교체하고, RunPod requirements.txt + setup.sh를 갱신한다. Lambda 폴백은 ConfigurationError로 fail-fast (Pitfall 1 ARM64 wheel 부재 — D-08).

Purpose: D-08(atomic swap, 중간 상태 없음) 완수. NLF 격리(Plan 04) + 새 모듈(Plan 02·03)이 모두 wired up. Plan 06 회귀 검증 직전 — 양쪽 엔진을 동일 다운스트림으로 비교 가능한 상태 확보.

Output: pipeline/app.py 교체 + RunPod requirements.txt 갱신 + setup.sh 모델 다운로드 + pipeline_dispatch 통합 테스트 갱신.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-VALIDATION.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-02-SUMMARY.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-03-SUMMARY.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-04-SUMMARY.md
@backend/functions/pipeline/app.py
@backend/runpod_inference/server.py
@backend/runpod_inference/requirements.txt
@backend/tests/test_pipeline_dispatch.py
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: pipeline/app.py atomic swap (NLF → MediaPipe + 폴 축)</name>
  <files>backend/functions/pipeline/app.py, backend/tests/test_pipeline_dispatch.py</files>
  <read_first>
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md D-08 (atomic swap, 중간 상태 없음)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md §NLF 격리 — atomic swap 대상 코드 라인 (pipeline/app.py 라인 131-132, 146-153)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md §Pitfall 1 (Lambda ARM64 fail-fast — 권장)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md §System Architecture Diagram (새 흐름 1~10단계)
    - backend/functions/pipeline/app.py (전체 — 현 흐름 파악 + 어디에 폴 축 검출 삽입할지 확인)
    - backend/shared/python/sunity_shared/analysis/pose_engines/mediapipe_engine.py (Plan 02 Task 2)
    - backend/shared/python/sunity_shared/analysis/pole/detector.py (Plan 03 Task 1)
    - backend/shared/python/sunity_shared/analysis/pose_frame.py (to_coco17_array helper)
    - backend/tests/test_pipeline_dispatch.py (기존 mock 구조 — MediaPipe case 추가)
  </read_first>
  <action>
    `backend/functions/pipeline/app.py` 수정 (atomic swap).

    1) 모듈 상단 import 섹션에 다음 import 추가: `to_coco17_array` 및 `PoleAxis` from `sunity_shared.analysis.pose_frame`, `MediaPipePoseEngine` from `sunity_shared.analysis.pose_engines.mediapipe_engine`, `HoughPoleDetector` from `sunity_shared.analysis.pole.detector`. mediapipe·cv2·scipy 실 라이브러리 import는 lazy (모듈 안 __init__에서 발생) — 본 파일 module-level은 깨끗.

    2) 전역 변수 변경: `_POSE_ESTIMATOR` 제거, `_POSE_ENGINE: MediaPipePoseEngine | None = None` 추가, `_POLE_DETECTOR: HoughPoleDetector | None = None` 추가.

    3) 모듈 상단에 `class ConfigurationError(Exception)` 정의 + docstring "Lambda 폴백 환경에서 MediaPipe wheel 부재(Pitfall 1) 등 환경 설정 오류 — 즉시 fail-fast".

    4) `_ensure_adapters()` 함수 수정 (line 123-135 부근):
       - `_FRAME_EXTRACTOR` lazy 초기화 유지.
       - `_POSE_ESTIMATOR` 블록 제거, 대신:
         _POSE_ENGINE가 None이면 try 블록 안에서 `MediaPipePoseEngine(model_path=os.environ.get("MEDIAPIPE_POSE_MODEL_PATH"), target_fps=9.0)` 인스턴스화. except `(RuntimeError, ImportError)` 잡아서 `ConfigurationError("MediaPipe 엔진 초기화 실패 — Lambda 폴백 환경에서는 mediapipe wheel 없음 (Pitfall 1 ARM64). RunPod 위임이 정상 동작하는지 확인.")` raise from exc.
       - _POLE_DETECTOR가 None이면 `HoughPoleDetector()` 인스턴스화 (단순 — opencv import만, ARM64 OK).
       - `_COACH_WRITER` lazy 초기화 유지 (CerebrasCoachWriter — 변경 없음).

    5) `_angles_from_video(bucket, key)` 함수 교체 (line 146-153):
       기존 흐름 = S3 download → frame_extractor.extract → pose_estimator.estimate(frames) → compute_joint_angles → temporal_fill.
       
       새 흐름 (RESEARCH §System Architecture Diagram 1~10):
       - tempfile.NamedTemporaryFile로 S3 download (변경 없음)
       - `frames = _FRAME_EXTRACTOR.extract(tmp.name)` (재사용)
       - 신규 라인: `pole_axis = _POLE_DETECTOR.detect(frames)` (변수 타입 PoseAxis, 폴 검출 — Plan 03)
       - 신규 라인: `pose_frames = _POSE_ENGINE.estimate(frames, pole_axis)` (PoseEngine.estimate — Plan 02)
       - 신규 라인: `keypoints_array = to_coco17_array(pose_frames)` ((T,17,4) — Plan 01 helper)
       - `angles = compute_joint_angles(keypoints_array)` (재사용 — skeleton.py JOINT_ANGLES 그대로)
       - `return temporal_fill(angles, joint_uncertainty(keypoints_array))` (재사용)
       
       반환 타입은 그대로 np.ndarray (angles, T×J). 다운스트림 _process는 변경 없음.

    6) RunPod 위임 경로(line 200+ 부근, `_dispatch_to_runpod` 함수) 변경 없음 — RunPod 서버 측은 server.py에서 같은 pipeline 모듈을 import해 _process 사용. 본 swap이 자동 반영됨.

    7) ImportError 가드 — cv2가 import 안 되는 환경(예상치 못한 ARM64 + opencv build 누락)에서도 _POLE_DETECTOR 초기화 시 `ConfigurationError`로 매핑.

    8) `backend/tests/test_pipeline_dispatch.py` 갱신:
       - 기존 NLF mock 케이스 제거 또는 MediaPipe mock 케이스로 교체. `_POSE_ENGINE`을 `monkeypatch.setattr(app, "_POSE_ENGINE", mock_engine)`로 패치. `_POLE_DETECTOR`도 같은 방식.
       - 신규 테스트 3개:
         - `test_pipeline_with_mediapipe_engine_mock`: mock MediaPipePoseEngine + mock HoughPoleDetector (return PoleAxis((0,0,1), 0.9, 'hough')) 주입 → 합성 mp4 fixture 또는 S3 mock → _process → status 'done' + dimension_scores 산출.
         - `test_pipeline_lambda_fallback_fail_fast`: `_ensure_adapters` 호출 시 `MediaPipePoseEngine()` 가 RuntimeError raise (mediapipe import fail mock) → `ConfigurationError` raise. _process는 server_error로 fail (기존 error mapping).
         - `test_pipeline_pole_detection_fallback`: HoughPoleDetector가 vertical_fallback 반환 → _process 정상 완료 + 결과 doc에 pole confidence='low' 표기는 아직 미구현 (deferred — Phase 12 사용자 카피). 본 테스트는 흐름이 끊기지 않음만 검증.
       - 기존 test_pipeline_mode3 등 unrelated 테스트 변경 없음.

    9) Pitfall 1 인용 주석을 ConfigurationError class 위 + _ensure_adapters except 블록에 명시.

    commit message: `feat(01-05): atomic swap NLF→MediaPipe + 폴 축 (D-08, Pitfall 1 fail-fast)`.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_pipeline_dispatch.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "NlfPoseEstimator|pose_estimator import" backend/functions/pipeline/app.py | grep -v "^[[:space:]]*#"` returns empty (NLF 참조 0, 주석 제외)
    - `grep -nE "MediaPipePoseEngine|HoughPoleDetector" backend/functions/pipeline/app.py` matches >= 2 lines
    - `grep -n "to_coco17_array" backend/functions/pipeline/app.py` matches at least 1 line
    - `grep -nE "class ConfigurationError" backend/functions/pipeline/app.py` matches exactly 1 line
    - `grep -nE "Pitfall 1|ARM64|RunPod 위임" backend/functions/pipeline/app.py` matches at least 1 line (Pitfall 1 인용)
    - `grep -nE "_POLE_DETECTOR|_POSE_ENGINE" backend/functions/pipeline/app.py` matches >= 4 lines
    - `pytest backend/tests/test_pipeline_dispatch.py -x` exits 0
    - `grep -n "def test_pipeline_with_mediapipe_engine_mock\\|def test_pipeline_lambda_fallback_fail_fast\\|def test_pipeline_pole_detection_fallback" backend/tests/test_pipeline_dispatch.py` matches 3 lines
    - `grep -rnE "NlfPoseEstimator" backend/functions/ backend/shared/python/sunity_shared/ | grep -v "^[[:space:]]*#"` returns empty
    - Other existing test suites still pass: `pytest backend/tests/ -x --ignore=backend/tests/test_nlf_isolation.py` exits 0
  </acceptance_criteria>
  <done>pipeline/app.py가 MediaPipe + 폴 축 정렬 흐름으로 atomic swap 완료. NLF 참조 0. Lambda 폴백 ConfigurationError fail-fast. pipeline_dispatch 3개 신규 테스트 통과. D-08 atomic swap + D-07 격리 + Pitfall 1 mitigation 모두 박제.</done>
</task>

<task type="auto">
  <name>Task 2: RunPod requirements.txt + setup.sh + server.py 검토</name>
  <files>backend/runpod_inference/requirements.txt, backend/runpod_inference/setup.sh, backend/runpod_inference/server.py</files>
  <read_first>
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md §Standard Stack (정확한 버전 pin: mediapipe 0.10.35, opencv-python-headless 4.13.0.92, scipy 1.17.1)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md §Pitfall 4 (protobuf 충돌 가능성)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md §모델 파일 배포 권장 (setup.sh wget 권장)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md D-01, D-02 (Heavy + Tasks API)
    - backend/runpod_inference/requirements.txt (현재 deps)
    - backend/runpod_inference/server.py (변경 거의 없음 확인 — pipeline 모듈 그대로 import)
  </read_first>
  <action>
    1) `backend/runpod_inference/requirements.txt` 갱신:
       - 제거: `ultralytics>=8.2` (NLF 의존 — backend/research 측 평가 환경에서 별도 설치)
       - 추가 (정확한 pin, 주석에 Phase 1 D 결정 ID + 라이선스 명시):
         - `mediapipe==0.10.35` (D-01, D-02, Apache 2.0)
         - `opencv-python-headless==4.13.0.92` (D-09, Apache 2.0)
         - `scipy>=1.17,<2.0` (D-12, BSD-3)
         - `protobuf>=4.25,<5` (Pitfall 4 — mediapipe + firebase-admin 충돌 방어)
       - 유지: fastapi, uvicorn, pydantic, boto3, firebase-admin, imageio, imageio-ffmpeg, numpy.
       - 변경 없음: torch/torchvision (RunPod base image 제공).

    2) `backend/runpod_inference/setup.sh` 신규 생성 (없으면) 또는 갱신:
       - Shebang: `#!/usr/bin/env bash`
       - `set -euo pipefail`
       - module header 주석: Phase 1 D-01, D-02 + RESEARCH §모델 파일 배포 권장.
       - 단계 1) `pip install -r requirements.txt`
       - 단계 2) `pip check` — Pitfall 4 의존성 충돌 검증 (실패 시 명확한 에러로 종료)
       - 단계 3) MediaPipe 모델 다운로드:
         - `MODEL_DIR=/workspace/models`
         - `mkdir -p $MODEL_DIR`
         - `MEDIAPIPE_MODEL_URL="https://storage.googleapis.com/mediapipe-models/pose_landmarker/pose_landmarker_heavy/float16/latest/pose_landmarker_heavy.task"`
         - `wget -O $MODEL_DIR/pose_landmarker_heavy.task "$MEDIAPIPE_MODEL_URL"`
         - (선택) `echo "$EXPECTED_SHA256  $MODEL_DIR/pose_landmarker_heavy.task" | sha256sum --check` — Google 공식 hash가 있으면 검증 (없으면 wget HTTPS 신뢰 + skip with WARN)
         - 환경 변수 영구화: `echo "export MEDIAPIPE_POSE_MODEL_PATH=$MODEL_DIR/pose_landmarker_heavy.task" >> ~/.bashrc`
         - 현 셸 export: `export MEDIAPIPE_POSE_MODEL_PATH=$MODEL_DIR/pose_landmarker_heavy.task`
       - 단계 4) sanity: `python -c "from sunity_shared.analysis.pose_engines.mediapipe_engine import MediaPipePoseEngine; print('engine module loadable')"`
       - 단계 5) `echo "Setup complete — MediaPipe Heavy installed at $MODEL_DIR (size ~26MB, Apache 2.0)"`
       - 주석: "Pod 재생성 시마다 실행 필수 (생명주기 수동 — STATE.md blockers)"
       - 파일 권한: 실행 가능 (`chmod +x`) — git commit 시 file mode 100755.

    3) `backend/runpod_inference/server.py` 검토:
       - grep으로 NLF 참조 검증: `grep -nE "NlfPoseEstimator|pose_estimator import" backend/runpod_inference/server.py` → empty여야 함. 발견 시 제거.
       - 환경 변수 처리(`RUNPOD_AUTH_TOKEN` 등) 변경 없음.
       - `MEDIAPIPE_POSE_MODEL_PATH` env는 setup.sh에서 주입 + pipeline.app.py의 `_ensure_adapters`에서 `os.environ.get`로 읽음.
       - server.py가 pipeline._process를 그대로 import → swap 자동 반영. server.py 자체 코드 변경 없을 가능성 높음 — 변경했다면 명시.

    commit message: `chore(01-05): RunPod deps + setup.sh 갱신 (mediapipe + opencv + scipy + heavy model download)`.
  </action>
  <verify>
    <automated>grep -E "mediapipe==0\.10\.35" backend/runpod_inference/requirements.txt && grep -E "opencv-python-headless==4\.13\.0\.92" backend/runpod_inference/requirements.txt && grep -E "scipy>=1\.17" backend/runpod_inference/requirements.txt && ! grep -E "^ultralytics" backend/runpod_inference/requirements.txt && grep -nE "pose_landmarker_heavy|storage\.googleapis\.com/mediapipe-models" backend/runpod_inference/setup.sh && ! grep -nE "NlfPoseEstimator|from .*pose_estimator import" backend/runpod_inference/server.py && echo "RunPod config OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "mediapipe==0.10.35" backend/runpod_inference/requirements.txt` outputs `1`
    - `grep -c "opencv-python-headless==4.13.0.92" backend/runpod_inference/requirements.txt` outputs `1`
    - `grep -cE "scipy>=1\\.17" backend/runpod_inference/requirements.txt` outputs `1`
    - `grep -cE "protobuf>=4\\.25" backend/runpod_inference/requirements.txt` outputs `1`
    - `grep -E "^ultralytics" backend/runpod_inference/requirements.txt` returns empty (제거됨)
    - `test -f backend/runpod_inference/setup.sh && test -x backend/runpod_inference/setup.sh` exits 0 (실행 가능)
    - `grep -n "pose_landmarker_heavy.task" backend/runpod_inference/setup.sh` matches at least 1 line
    - `grep -n "storage.googleapis.com/mediapipe-models" backend/runpod_inference/setup.sh` matches at least 1 line
    - `grep -n "MEDIAPIPE_POSE_MODEL_PATH" backend/runpod_inference/setup.sh` matches at least 1 line
    - `grep -n "pip check" backend/runpod_inference/setup.sh` matches at least 1 line (Pitfall 4 mitigation)
    - `grep -nE "NlfPoseEstimator|pose_estimator import" backend/runpod_inference/server.py | grep -v "^[[:space:]]*#"` returns empty
    - `bash -n backend/runpod_inference/setup.sh` exits 0 (shellcheck syntax 통과 — setup.sh 자체 실행은 RunPod Pod에서만)
  </acceptance_criteria>
  <done>RunPod deps 갱신 (mediapipe 0.10.35 + opencv-python-headless 4.13.0.92 + scipy + protobuf 방어) + ultralytics 제거. setup.sh가 Google CDN에서 pose_landmarker_heavy.task 다운로드 + env 주입 + pip check 검증. server.py에 NLF 참조 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Google CDN ↔ pose_landmarker_heavy.task | HTTPS 다운로드 — typosquat URL 방어 필요 (정확한 storage.googleapis.com URL) |
| Lambda 폴백 ↔ MediaPipe wheel | ARM64 미지원 — fail-fast로 silent error 방지 |
| RunPod env ↔ pipeline._process | 같은 pipeline 모듈을 RunPod에서 import → swap이 양쪽 동시 반영 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-14 | Tampering | 악성 모델 파일 (poison pose_landmarker_heavy.task) | mitigate | 공식 Google CDN URL만 (storage.googleapis.com/mediapipe-models). SHA256 hash 검증 권장 (Google 공식 hash 있으면 setup.sh에 추가) |
| T-1-15 | Spoofing | MediaPipe wheel typosquat (`medipipe`) | mitigate | requirements.txt에 exact name `mediapipe==0.10.35` (Package Legitimacy Audit [OK]) |
| T-1-16 | Configuration | MEDIAPIPE_POSE_MODEL_PATH env 미설정 → silent failure | mitigate | Plan 02 MediaPipePoseEngine.__init__에서 RuntimeError raise + Plan 05 _ensure_adapters에서 ConfigurationError 매핑 (fail-fast) |
| T-1-01 | License violation | NLF import 잔존 | mitigate | Task 1 acceptance에 grep 검증 0 강제 (Plan 04 격리 + Plan 05 atomic swap 합산) |
</threat_model>

<verification>
- `pytest backend/tests/ -x` exits 0 (전체 swap 후 모든 단위 + 통합 테스트 통과)
- `grep -rE "NlfPoseEstimator|from .*pose_estimator import" backend/functions/ backend/shared/python/sunity_shared/ backend/runpod_inference/server.py | grep -v "^[[:space:]]*#"` returns empty (NLF 참조 0)
- RunPod requirements.txt에 mediapipe·opencv·scipy 정확한 버전 + ultralytics 제거 검증
- setup.sh가 Google CDN URL로 모델 다운로드 + env 주입
- Lambda 폴백 ConfigurationError fail-fast 동작
- 회귀 검증(Plan 06)에 입력될 wired-up 시스템 완성
</verification>

<success_criteria>
- D-08 atomic swap 완수 — NLF → MediaPipe 1:1 교체 + 중간 상태 없음
- D-07 격리 검증 — sunity_shared, functions, runpod_inference/server.py에 NLF 참조 0
- D-09/D-10/D-11/D-12 — pipeline이 HoughPoleDetector + PoleAxisAligner 흐름 사용
- Pitfall 1 mitigation — Lambda 폴백 ConfigurationError fail-fast
- Pitfall 4 mitigation — pip check + protobuf 버전 방어
- RunPod deployment ready — setup.sh로 Pod 재생성 시 자동 셋업
- pipeline_dispatch 통합 테스트 3개 통과
- Plan 06 회귀 검증 입력 준비 완료 (compare_engines.py가 호출할 새 MediaPipe 흐름)
</success_criteria>

<output>
Create `.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-05-SUMMARY.md` when done. SUMMARY 내용:
- pipeline/app.py 변경 diff 요약 (라인별)
- RunPod requirements.txt 변경 (추가/제거)
- setup.sh 실행 단계 + 모델 size 명시
- pipeline_dispatch 3개 신규 테스트 결과
- atomic swap 후 grep 검증 결과 (NLF 참조 0)
- belle 작업 사항: RunPod Pod에서 `bash backend/runpod_inference/setup.sh` 1회 실행 + 결과 확인 (Plan 06 회귀 검증 직전 필수)
</output>
