---
phase: 01-poseengine-mediapipe-nlf-r-d
plan: 05
type: execute
wave: 3
depends_on:
  - 01-04
  - 01-06
files_modified:
  - backend/functions/pipeline/app.py
  - backend/runpod_inference/requirements.txt
  - backend/runpod_inference/server.py
  - backend/runpod_inference/setup.sh
  - backend/runpod_inference/README.md
  - backend/tests/test_pipeline_dispatch.py
autonomous: true
requirements:
  - POSE-01
  - POSE-02
must_haves:
  truths:
    - "D-08 박제: atomic swap — pipeline/app.py가 MediaPipePoseEngine + HoughPoleDetector로 1:1 교체 (NLF 참조 0, 중간 상태 없음, config 플래그 없음)"
    - "RunPod requirements.txt에 mediapipe, opencv-python-headless, scipy 추가 + ultralytics 제거"
    - "Lambda 폴백 경로가 ConfigurationError로 fail-fast (Pitfall 1 + H-2 박제)"
    - "pipeline/app.py에 mediapipe, cv2, scipy 등 heavy library의 module-level import 0 — 모든 heavy import는 _ensure_adapters() 내부 lazy (H-2 박제)"
    - "분석 결과 doc (Firestore + RunPod result)에 poleAxis 메타 (axis_vector + confidence_level + source) 보존 — D-12 + H-3 박제"
    - "RunPod README가 MediaPipe 셋업으로 갱신 (NLF_MODEL_PATH/NLF 운영 메모 0 — M-4 박제)"
    - "본 plan은 Wave 3 — Plan 06 belle 회귀 검증 + Plan 04 NLF 격리 후 진행 (H-1 박제, D-08 atomic swap이 회귀 검증 통과 뒤에만 실행되도록 wave 순서 정정)"
  artifacts:
    - path: "backend/functions/pipeline/app.py"
      provides: "MediaPipe + 폴 축 정렬 파이프라인 (NLF 호출 0, heavy import lazy per H-2)"
    - path: "backend/runpod_inference/requirements.txt"
      provides: "mediapipe + opencv-python-headless + scipy 의존"
    - path: "backend/runpod_inference/setup.sh"
      provides: "pose_landmarker_heavy.task 다운로드 + 환경 변수 설정"
    - path: "backend/runpod_inference/README.md"
      provides: "MediaPipe Pod 운영 가이드 (NLF 참조 0 — M-4)"
  key_links:
    - from: "pipeline/app.py"
      to: "MediaPipePoseEngine + HoughPoleDetector"
      via: "_ensure_adapters() lazy load (H-2 박제) + _angles_from_video() 새 흐름"
      pattern: "MediaPipePoseEngine|HoughPoleDetector"
    - from: "RunPod setup.sh"
      to: "Google CDN pose_landmarker_heavy.task"
      via: "wget + 환경 변수 export"
      pattern: "storage.googleapis.com/mediapipe-models"
---

<objective>
Plan 02/03/04 결과를 활용해 atomic swap을 수행 — pipeline/app.py의 NLF 참조를 MediaPipePoseEngine + HoughPoleDetector + PoleAxisAligner 흐름으로 교체하고, RunPod requirements.txt + setup.sh + README를 갱신한다. **모든 heavy library import (mediapipe, cv2, scipy, MediaPipePoseEngine, HoughPoleDetector, PoleAxisAligner)는 module-level이 아닌 `_ensure_adapters()` 내부 lazy로** — Lambda module load는 mediapipe/cv2/scipy 부재 환경에서도 성공 (H-2 박제). 결과 doc에 poleAxis 메타 보존 (H-3 D-12 박제). README에 NLF 잔여 0 (M-4 박제).

Purpose: D-08(atomic swap, 중간 상태 없음) + H-2 (Lambda fail-fast가 module import에서 깨지지 않음) + H-3 (분석 결과에 poleAxis 보존) + M-4 (README 갱신) 박제. Plan 06 belle 회귀 검증 + Plan 04 NLF 격리 통과 후 진행 (H-1).

Output: pipeline/app.py 교체 + RunPod requirements.txt + setup.sh + README 갱신 + pipeline_dispatch 통합 테스트.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-VALIDATION.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-REVIEWS.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-02-SUMMARY.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-03-SUMMARY.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-04-SUMMARY.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-06-SUMMARY.md
@backend/functions/pipeline/app.py
@backend/runpod_inference/server.py
@backend/runpod_inference/requirements.txt
@backend/runpod_inference/README.md
@backend/tests/test_pipeline_dispatch.py
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: pipeline/app.py atomic swap (NLF → MediaPipe + 폴 축, ALL heavy imports lazy per H-2, poleAxis 결과 보존 per H-3)</name>
  <files>backend/functions/pipeline/app.py, backend/tests/test_pipeline_dispatch.py</files>
  <read_first>
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-REVIEWS.md H-2 (module-level heavy import 금지 — `python -c "from backend.functions.pipeline.app import lambda_handler"`이 mediapipe/cv2/scipy 부재 환경에서도 성공해야 함), H-3 (결과 doc에 poleAxis 보존)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md D-08 (atomic swap, 중간 상태 없음)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md §NLF 격리 — atomic swap 대상 라인 (pipeline/app.py 131-132, 146-153)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md §Pitfall 1 (Lambda ARM64 fail-fast)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md §System Architecture Diagram (새 흐름 1~10)
    - backend/functions/pipeline/app.py (전체 — 현 흐름)
    - backend/shared/python/sunity_shared/analysis/pose_engines/mediapipe_engine.py (Plan 02 Task 2)
    - backend/shared/python/sunity_shared/analysis/pole/detector.py (Plan 03 Task 1)
    - backend/shared/python/sunity_shared/analysis/pose_frame.py (to_coco17_array, PoleAxis)
    - backend/shared/python/sunity_shared/firestore_admin.py (complete_analysis — 결과 doc 저장)
    - backend/tests/test_pipeline_dispatch.py (기존 mock 구조)
  </read_first>
  <action>
    `backend/functions/pipeline/app.py` 수정 (atomic swap + H-2 박제 + H-3 박제):

    1) Module-level import 정책 (H-2 박제):
       - `to_coco17_array` 및 `PoleAxis`는 `from sunity_shared.analysis.pose_frame import to_coco17_array, PoleAxis` — module-level 허용 (pose_frame.py는 numpy + dataclasses만, heavy 의존 없음).
       - MediaPipePoseEngine, HoughPoleDetector, PoleAxisAligner 등 heavy import는 module-level 금지. 모두 _ensure_adapters() 내부 lazy import.
       - mediapipe, cv2, scipy 등 라이브러리는 본 파일에서 직접 import 안 함 — Plan 02/03 모듈이 자체적으로 lazy 처리.

    2) 전역 변수 변경: _POSE_ESTIMATOR 제거, _POSE_ENGINE = None 추가, _POLE_DETECTOR = None 추가.

    3) 모듈 상단에 class ConfigurationError(Exception) 정의 + docstring "Lambda 폴백 환경에서 MediaPipe wheel 부재(Pitfall 1) 등 환경 설정 오류 — fail-fast (H-2)".

    4) _ensure_adapters() 함수 수정:
       - _FRAME_EXTRACTOR lazy 유지.
       - _POSE_ESTIMATOR 블록 제거, 대신 _POSE_ENGINE가 None이면 try 블록 안에서 `from sunity_shared.analysis.pose_engines.mediapipe_engine import MediaPipePoseEngine` (lazy import, H-2) → MediaPipePoseEngine(model_path=os.environ.get("MEDIAPIPE_POSE_MODEL_PATH"), target_fps=9.0) 인스턴스화. except (RuntimeError, ImportError) → ConfigurationError("MediaPipe 엔진 초기화 실패 — Lambda 폴백 환경에서는 mediapipe wheel 없음 (Pitfall 1 ARM64 / H-2). RunPod 위임이 정상 동작하는지 확인.") raise from exc.
       - _POLE_DETECTOR가 None이면 try 블록 안에서 `from sunity_shared.analysis.pole.detector import HoughPoleDetector` (lazy import, H-2) → HoughPoleDetector() 인스턴스화. except (RuntimeError, ImportError) → ConfigurationError("HoughPoleDetector 초기화 실패 — cv2 부재 (Pitfall 1 / H-2).") raise from exc.
       - _COACH_WRITER lazy 유지 (변경 없음).

    5) _angles_from_video(bucket, key) 함수 교체 (line ~146-153):
       - tempfile S3 download (재사용)
       - frames = _FRAME_EXTRACTOR.extract(tmp.name)
       - pole_axis = _POLE_DETECTOR.detect(frames) (Plan 03)
       - pose_frames = _POSE_ENGINE.estimate(frames, pole_axis) (Plan 02 + H-3 박제 — pose_frames의 모든 frame.pole_axis가 보존)
       - keypoints_array = to_coco17_array(pose_frames)
       - angles = compute_joint_angles(keypoints_array)
       - 반환 signature 변경: tuple (angles, pole_axis). `return angles, pole_axis` — 호출자(_process)가 pole_axis를 결과 doc에 저장 (H-3 박제).

    6) _process(message) 함수 변경 (H-3 박제):
       - _angles_from_video 호출 결과 (angles, pole_axis) 모두 받기.
       - firestore_admin.complete_analysis(...) 호출 시 새 인자 pole_axis_meta 전달: `{"axisVector": list(pole_axis.axis_vector), "confidenceLevel": pole_axis.confidence_level, "source": pole_axis.source}` (H-3 D-12 박제 — 결과 doc의 result.poleAxis 필드).
       - firestore_admin.complete_analysis 시그니처도 동시 갱신 (별도 task가 아닌 본 task에서 — pole_axis_meta 인자 추가). 변경 후 grep으로 검증.

    7) RunPod 위임 경로 (_dispatch_to_runpod): 변경 없음 — RunPod 서버 측 server.py가 _process를 import해 pipeline 사용 → swap 자동 반영.

    8) backend/tests/test_pipeline_dispatch.py 갱신:
       - 기존 NLF mock 케이스 제거. `monkeypatch.setattr(app, "_POSE_ENGINE", mock_engine)`, `_POLE_DETECTOR`도 동일.
       - 신규 테스트 5개:
         - test_pipeline_module_imports_without_mediapipe_or_cv2_or_scipy (H-2 박제): monkeypatch sys.modules에 mediapipe/cv2/scipy를 None으로 설정 → importlib.reload(app) → lambda_handler attribute 존재. 또는 subprocess로 가상환경 격리.
         - test_pipeline_with_mediapipe_engine_mock: mock MediaPipe + mock Hough → _process → status 'done' + dimension_scores 산출.
         - test_pipeline_lambda_fallback_fail_fast: _ensure_adapters 호출 시 MediaPipePoseEngine() RuntimeError mock → ConfigurationError raise → _process server_error.
         - test_pipeline_pole_axis_preserved_in_result (H-3 박제): _process 완료 후 firestore_admin.complete_analysis 호출 인자에 pole_axis_meta dict 포함 + axisVector/confidenceLevel/source 키 모두 존재.
         - test_pipeline_pole_detection_fallback: HoughPoleDetector가 vertical_fallback (confidence_level='low') → _process 정상 완료 + 결과 doc의 result.poleAxis.confidenceLevel == 'low'.

    9) Pitfall 1 + H-2 인용 주석을 ConfigurationError class 위 + _ensure_adapters except 블록에 명시.

    commit message: `feat(01-05): atomic swap NLF→MediaPipe + 폴 축 + poleAxis 결과 보존 (D-08, H-2 lazy import, H-3 D-12)`.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_pipeline_dispatch.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "NlfPoseEstimator|pose_estimator import" backend/functions/pipeline/app.py | grep -v "^[[:space:]]*#"` returns empty (NLF 참조 0)
    - `grep -nE "MediaPipePoseEngine|HoughPoleDetector" backend/functions/pipeline/app.py | wc -l` outputs at least `2`
    - `grep -n "to_coco17_array" backend/functions/pipeline/app.py | wc -l` outputs at least `1`
    - `grep -nE "class ConfigurationError" backend/functions/pipeline/app.py` matches exactly 1 line
    - `grep -nE "Pitfall 1|ARM64|H-2|RunPod 위임" backend/functions/pipeline/app.py | wc -l` outputs at least `1`
    - `grep -nE "_POLE_DETECTOR|_POSE_ENGINE" backend/functions/pipeline/app.py | wc -l` outputs at least `4`
    - H-2 박제: `grep -nE "^from sunity_shared\\.analysis\\.pose_engines|^from sunity_shared\\.analysis\\.pole" backend/functions/pipeline/app.py | wc -l` outputs `0` (module-level heavy import 0)
    - H-2 박제: `grep -nE "^[[:space:]]+from sunity_shared\\.analysis\\.pose_engines\\.mediapipe_engine|^[[:space:]]+from sunity_shared\\.analysis\\.pole\\.detector" backend/functions/pipeline/app.py | wc -l` outputs at least `2` (lazy import inside function)
    - H-2 박제 critical test (Lambda 시뮬레이션): `python -c "import sys; sys.modules['mediapipe'] = None; sys.modules['cv2'] = None; sys.modules['scipy'] = None; from backend.functions.pipeline.app import lambda_handler; print('ok')"` outputs `ok`
    - H-2 박제 critical test (실 uninstall 시뮬레이션): `bash -c "pip uninstall -y mediapipe opencv-python opencv-python-headless scipy 2>/dev/null; python -c 'from backend.functions.pipeline.app import lambda_handler; print(\"ok\")'"` outputs `ok` (실 환경 uninstall — Lambda ARM64와 동일한 wheel 부재 환경)
    - H-3 박제: `grep -nE "pole_axis_meta|poleAxis|axisVector|confidenceLevel" backend/functions/pipeline/app.py | wc -l` outputs at least `2` (분석 결과에 pole_axis 메타 보존)
    - `pytest backend/tests/test_pipeline_dispatch.py -x` exits 0
    - `grep -nE "def test_pipeline_module_imports_without_mediapipe_or_cv2_or_scipy|def test_pipeline_with_mediapipe_engine_mock|def test_pipeline_lambda_fallback_fail_fast|def test_pipeline_pole_axis_preserved_in_result|def test_pipeline_pole_detection_fallback" backend/tests/test_pipeline_dispatch.py | wc -l` outputs at least `5`
    - `grep -rnE "NlfPoseEstimator" backend/functions/ backend/shared/python/sunity_shared/ | grep -v "^[[:space:]]*#"` returns empty
    - Other existing test suites still pass: `pytest backend/tests/ -x --ignore=backend/tests/test_nlf_isolation.py` exits 0
  </acceptance_criteria>
  <done>pipeline/app.py가 MediaPipe + 폴 축 정렬 흐름으로 atomic swap. NLF 참조 0. heavy library module-level import 0 (H-2 박제). ConfigurationError fail-fast. 결과 doc에 poleAxis 메타 보존 (H-3 D-12). 5개 신규 테스트 통과 — Lambda module import sanity 포함.</done>
</task>

<task type="auto">
  <name>Task 2: RunPod requirements.txt + setup.sh + server.py 검토 + README 갱신 (M-4 박제)</name>
  <files>backend/runpod_inference/requirements.txt, backend/runpod_inference/setup.sh, backend/runpod_inference/server.py, backend/runpod_inference/README.md</files>
  <read_first>
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-REVIEWS.md M-4 (README의 NLF_MODEL_PATH/NLF 운영 메모 0 + mediapipe 명시), L-2 (working directory 통일)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md §Standard Stack (정확한 버전: mediapipe 0.10.35, opencv-python-headless 4.13.0.92, scipy 1.17.1)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md §Pitfall 4 (protobuf 충돌)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md §모델 파일 배포 (setup.sh wget)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md D-01, D-02
    - backend/runpod_inference/requirements.txt (현재 deps)
    - backend/runpod_inference/server.py (변경 없음 확인)
    - backend/runpod_inference/README.md (현재 — NLF_MODEL_PATH 참조 등 정리 대상)
  </read_first>
  <action>
    1) `backend/runpod_inference/requirements.txt` 갱신:
       - 제거: `ultralytics>=8.2`
       - 추가 (정확한 pin):
         - `mediapipe==0.10.35` (D-01, D-02, Apache 2.0)
         - `opencv-python-headless==4.13.0.92` (D-09, Apache 2.0)
         - `scipy>=1.17,<2.0` (D-12, BSD-3)
         - `protobuf>=4.25,<5` (Pitfall 4)
       - 유지: fastapi, uvicorn, pydantic, boto3, firebase-admin, imageio, imageio-ffmpeg, numpy.
       - 변경 없음: torch/torchvision (RunPod base image).

    2) `backend/runpod_inference/setup.sh` 신규 또는 갱신:
       - Shebang: `#!/usr/bin/env bash`
       - `set -euo pipefail`
       - module header 주석: Phase 1 D-01, D-02 + RESEARCH §모델 파일 배포.
       - 단계 1) `pip install -r requirements.txt`
       - 단계 2) `pip check` (Pitfall 4)
       - 단계 3) MediaPipe 모델 다운로드:
         - `MODEL_DIR=/workspace/models`
         - `mkdir -p $MODEL_DIR`
         - `MEDIAPIPE_MODEL_URL="https://storage.googleapis.com/mediapipe-models/pose_landmarker/pose_landmarker_heavy/float16/latest/pose_landmarker_heavy.task"`
         - `wget -O $MODEL_DIR/pose_landmarker_heavy.task "$MEDIAPIPE_MODEL_URL"`
         - 환경 변수 영구화: `echo "export MEDIAPIPE_POSE_MODEL_PATH=$MODEL_DIR/pose_landmarker_heavy.task" >> ~/.bashrc`
         - 현 셸 export: `export MEDIAPIPE_POSE_MODEL_PATH=$MODEL_DIR/pose_landmarker_heavy.task`
       - 단계 4) sanity: `python -c "from sunity_shared.analysis.pose_engines.mediapipe_engine import MediaPipePoseEngine; print('engine module loadable')"`
       - 단계 5) `echo "Setup complete — MediaPipe Heavy installed at $MODEL_DIR (size ~26MB, Apache 2.0)"`
       - 주석: "Pod 재생성 시마다 실행 — STATE.md blockers. 작업 경로: /workspace/SunityMotion (L-2 박제)"
       - chmod +x.

    3) `backend/runpod_inference/server.py` 검토:
       - grep으로 NLF 참조 검증: `grep -nE "NlfPoseEstimator|pose_estimator import" backend/runpod_inference/server.py` → empty여야 함. 발견 시 제거.
       - 환경 변수 변경 없음.
       - server.py가 pipeline._process를 그대로 import → swap 자동 반영.

    4) `backend/runpod_inference/README.md` 갱신 (M-4 박제):
       - 기존 NLF_MODEL_PATH, NLF 운영 섹션 모두 제거.
       - 새 섹션:
         - `# RunPod Inference — MediaPipe Pose Engine (Phase 1 D-01/D-02)`
         - "환경 셋업": `bash backend/runpod_inference/setup.sh` (Pod 재생성 후마다)
         - "필수 환경 변수": MEDIAPIPE_POSE_MODEL_PATH, RUNPOD_AUTH_TOKEN, AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, AWS_DEFAULT_REGION, FIREBASE_SA_JSON (또는 FIREBASE_SA_PATH)
         - "라이선스": MediaPipe Pose = Apache 2.0 (라이선스 리스크 0)
         - "작업 경로": /workspace/SunityMotion (L-2 박제)
         - "운영 메모": "Pod 생명주기 수동. 재생성 시 proxy URL 변경 → Lambda env(RunpodAnalyzeUrl) 동기화 필수."
         - "NLF 운영 메모는 제거됨 — NLF/SMPL-X는 R&D 비교군으로 격리 (backend/research/, M-4 박제)"
         - 마지막에 "마지막 갱신: 2026-05-31, Phase 1 atomic swap (Plan 05)"

    commit message: `chore(01-05): RunPod deps + setup.sh + README MediaPipe 갱신 (M-4 NLF 잔여 0)`.
  </action>
  <verify>
    <automated>grep -E "mediapipe==0\.10\.35" backend/runpod_inference/requirements.txt && grep -E "opencv-python-headless==4\.13\.0\.92" backend/runpod_inference/requirements.txt && grep -E "scipy>=1\.17" backend/runpod_inference/requirements.txt && ! grep -E "^ultralytics" backend/runpod_inference/requirements.txt && grep -nE "pose_landmarker_heavy|storage\.googleapis\.com/mediapipe-models" backend/runpod_inference/setup.sh && ! grep -nE "NlfPoseEstimator|from .*pose_estimator import" backend/runpod_inference/server.py && ! grep -niE "NLF_MODEL_PATH|NLF model" backend/runpod_inference/README.md && grep -ni "mediapipe" backend/runpod_inference/README.md && echo "RunPod config OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "mediapipe==0.10.35" backend/runpod_inference/requirements.txt` outputs `1`
    - `grep -c "opencv-python-headless==4.13.0.92" backend/runpod_inference/requirements.txt` outputs `1`
    - `grep -cE "scipy>=1\.17" backend/runpod_inference/requirements.txt` outputs `1`
    - `grep -cE "protobuf>=4\.25" backend/runpod_inference/requirements.txt` outputs `1`
    - `grep -E "^ultralytics" backend/runpod_inference/requirements.txt` returns empty
    - `test -f backend/runpod_inference/setup.sh && test -x backend/runpod_inference/setup.sh` exits 0
    - `grep -n "pose_landmarker_heavy.task" backend/runpod_inference/setup.sh | wc -l` outputs at least `1`
    - `grep -n "storage.googleapis.com/mediapipe-models" backend/runpod_inference/setup.sh | wc -l` outputs at least `1`
    - `grep -n "MEDIAPIPE_POSE_MODEL_PATH" backend/runpod_inference/setup.sh | wc -l` outputs at least `1`
    - `grep -n "pip check" backend/runpod_inference/setup.sh | wc -l` outputs at least `1` (Pitfall 4)
    - `grep -n "/workspace/SunityMotion" backend/runpod_inference/setup.sh | wc -l` outputs at least `1` (L-2 박제)
    - `grep -nE "NlfPoseEstimator|pose_estimator import" backend/runpod_inference/server.py | grep -v "^[[:space:]]*#"` returns empty
    - `bash -n backend/runpod_inference/setup.sh` exits 0 (syntax 통과)
    - M-4 박제: `! grep -niE "NLF_MODEL_PATH|NLF model" backend/runpod_inference/README.md` exits 0 (NLF 참조 0)
    - M-4 박제: `grep -ni "mediapipe" backend/runpod_inference/README.md | wc -l` outputs at least `1` (MediaPipe 명시)
    - M-4 박제: `grep -ni "MEDIAPIPE_POSE_MODEL_PATH" backend/runpod_inference/README.md | wc -l` outputs at least `1` (env 명시)
  </acceptance_criteria>
  <done>RunPod deps 갱신 (mediapipe 0.10.35 + opencv-python-headless 4.13.0.92 + scipy + protobuf) + ultralytics 제거. setup.sh가 Google CDN에서 모델 다운로드 + env 주입 + pip check 검증. server.py에 NLF 참조 0. README가 MediaPipe로 갱신 — NLF 참조 0 (M-4 박제).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Google CDN ↔ pose_landmarker_heavy.task | HTTPS 다운로드 — 정확한 storage.googleapis.com URL |
| Lambda 폴백 ↔ MediaPipe wheel | ARM64 미지원 — H-2 lazy import + fail-fast |
| RunPod env ↔ pipeline._process | 같은 pipeline 모듈을 RunPod에서 import → swap이 양쪽 동시 반영 |
| Module load ↔ heavy library 부재 | H-2 박제 — module-level heavy import 0, Lambda fail-fast 보장 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-14 | Tampering | 악성 모델 파일 (poison pose_landmarker_heavy.task) | mitigate | 공식 Google CDN URL만 (storage.googleapis.com/mediapipe-models). |
| T-1-15 | Spoofing | MediaPipe wheel typosquat | mitigate | requirements.txt에 exact name `mediapipe==0.10.35` (Package Legitimacy Audit [OK]) |
| T-1-16 | Configuration | MEDIAPIPE_POSE_MODEL_PATH env 미설정 → silent failure | mitigate | Plan 02 MediaPipePoseEngine.__init__에서 RuntimeError + Plan 05 _ensure_adapters에서 ConfigurationError |
| T-1-01 | License violation | NLF import 잔존 | mitigate | Task 1 acceptance에 grep 검증 0 강제 (Plan 04 격리 + Plan 05 atomic swap) |
| T-1-20 | DoS | module-level mediapipe/cv2/scipy import가 Lambda fail-fast 깨뜨림 (H-2) | mitigate | 본 plan acceptance에 module load 시뮬레이션 + grep 강제 |
| T-1-23 | Configuration | README 잘못된 NLF 셋업 — 운영자 혼란 (M-4) | mitigate | README NLF 잔여 0 grep 검증 + mediapipe 명시 |
</threat_model>

<verification>
- `pytest backend/tests/ -x` exits 0 (전체 swap 후 모든 테스트)
- `grep -rE "NlfPoseEstimator|from .*pose_estimator import" backend/functions/ backend/shared/python/sunity_shared/ backend/runpod_inference/server.py | grep -v "^[[:space:]]*#"` returns empty
- RunPod requirements.txt + setup.sh + README가 MediaPipe로 갱신, NLF 잔여 0 (M-4)
- Lambda 폴백 ConfigurationError fail-fast 동작 + module load는 mediapipe/cv2/scipy 부재 환경에서 성공 (H-2)
- 분석 결과에 poleAxis 메타 보존 (H-3 D-12)
- Plan 06 회귀 검증에 입력된 wired-up 시스템과 동일 동작
</verification>

<success_criteria>
- D-08 atomic swap 완수 — NLF → MediaPipe 1:1 교체 + 중간 상태 없음
- D-07 격리 검증 — sunity_shared, functions, runpod_inference/server.py에 NLF 참조 0
- D-09/D-10/D-11/D-12 — pipeline이 HoughPoleDetector + PoleAxisAligner 흐름 사용
- H-2 박제 — Lambda module load는 heavy library 부재 환경에서도 성공
- H-3 박제 — 분석 결과 doc에 poleAxis 메타 (axisVector + confidenceLevel + source) 보존
- M-4 박제 — RunPod README NLF 참조 0 + mediapipe 명시
- Pitfall 1 mitigation — Lambda 폴백 ConfigurationError fail-fast
- Pitfall 4 mitigation — pip check + protobuf 버전 방어
- RunPod deployment ready — setup.sh로 Pod 재생성 시 자동 셋업
- pipeline_dispatch 통합 테스트 5개 통과
</success_criteria>

<output>
Create `.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-05-SUMMARY.md` when done. SUMMARY 내용:
- pipeline/app.py 변경 diff 요약 (라인별)
- RunPod requirements.txt 변경 (추가/제거)
- setup.sh 실행 단계 + 모델 size 명시
- README MediaPipe 갱신 요약 + NLF 참조 0 확인
- pipeline_dispatch 5개 신규 테스트 결과
- atomic swap 후 grep 검증 결과 (NLF 참조 0)
- H-2 박제: Lambda module load 시뮬레이션 결과 (sys.modules None + uninstall 둘 다)
- H-3 박제: 결과 doc poleAxis 메타 grep 결과
- M-4 박제: README NLF 잔여 0 + mediapipe 명시 grep 결과
- belle 작업 사항: RunPod Pod에서 `bash backend/runpod_inference/setup.sh` 1회 실행 (Plan 06 Task 2 belle checkpoint에서 이미 수동 셋업했을 가능성 — 본 setup.sh 자동화 확인 후 commit 권장)
</output>
