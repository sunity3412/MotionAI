---
phase: 01-poseengine-mediapipe-nlf-r-d
plan: 06
type: execute
wave: 4
depends_on:
  - 01-05
files_modified:
  - backend/research/evaluations/__init__.py
  - backend/research/evaluations/compare_engines.py
  - backend/research/evaluations/reports/.gitkeep
  - backend/research/pose_engines/nlf/nlf_engine.py
  - backend/tests/test_compare_engines_smoke.py
autonomous: false
requirements:
  - POSE-01
  - POSE-02
must_haves:
  truths:
    - "compare_engines.py가 MediaPipe vs NLF를 동일 영상 N개에서 실행하고 비교 보고서를 JSON + Markdown으로 출력한다"
    - "회귀 검증 보고서에 D-13~D-16 게이트 4개(within_tolerance_5pt, mediapipe_score_ge_70, top3_overlap_2_of_3, avg_confidence_ok)가 motion별로 표기된다"
    - "summary.phase1_ready_to_swap이 모든 게이트 PASS여야 true"
    - "belle 시각 검토 checkpoint가 회귀 보고서 + 정은지 5영상 결과를 함께 보고 atomic swap 승인 또는 D-16 보류 결정"
    - "NlfPoseEstimator의 PoseEngine wrapper(estimate_as_pose_frames)가 R&D 비교군 측에서 구현되어 같은 다운스트림 호출 가능"
  artifacts:
    - path: "backend/research/evaluations/compare_engines.py"
      provides: "MediaPipe vs NLF 회귀 검증 스크립트 (D-13~D-16)"
      contains: "phase1_ready_to_swap"
    - path: "backend/research/pose_engines/nlf/nlf_engine.py"
      provides: "NlfPoseEstimator의 PoseEngine wrapper 구현 (estimate_as_pose_frames)"
    - path: "backend/research/evaluations/reports/"
      provides: "compare_engines.py 출력 디렉터리 (JSON + Markdown)"
  key_links:
    - from: "compare_engines.py"
      to: "MediaPipePoseEngine + NlfPoseEstimator + sunity_shared 다운스트림"
      via: "두 엔진을 동일 _process 입력으로 사용 → 같은 dimensions/kismam"
      pattern: "run_engine"
    - from: "회귀 보고서"
      to: "belle 검토 + atomic swap 게이트"
      via: "checkpoint:human-verify (gate=blocking)"
      pattern: "phase1_ready_to_swap"
---

<objective>
MediaPipe vs NLF 회귀 검증 스크립트(compare_engines.py)를 작성하고 정은지 영상 5개에서 실행해 점수 갭(±5점)·고수 위양성(≥70점)·Top-3 일치율(≥2/3)·평균 confidence를 비교 보고서로 출력한다. belle가 보고서를 검토하고 atomic swap을 승인 또는 D-16에 따라 보류한다. 본 plan은 belle checkpoint가 필요 — autonomous=false.

Purpose: D-13(검증 영상 5개) · D-14(±5점 허용) · D-15(4 지표) · D-16(검증 실패 시 보류) 결정 박제. 신뢰도 게이트 — atomic swap의 결과가 실제로 분석 정확도를 보존하는지 belle 인지 + 승인.

Output: compare_engines.py + NlfPoseEstimator PoseEngine wrapper + 회귀 보고서 디렉터리 + smoke 테스트 + belle 검토 checkpoint.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-VALIDATION.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-04-SUMMARY.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-05-SUMMARY.md
@backend/functions/pipeline/app.py
@backend/research/pose_engines/nlf/nlf_engine.py
@backend/shared/python/sunity_shared/analysis/pose_engines/mediapipe_engine.py
@backend/shared/python/sunity_shared/analysis/pose_frame.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: NlfPoseEstimator PoseEngine wrapper + compare_engines.py 스크립트</name>
  <files>backend/research/pose_engines/nlf/nlf_engine.py, backend/research/evaluations/__init__.py, backend/research/evaluations/compare_engines.py, backend/research/evaluations/reports/.gitkeep, backend/tests/test_compare_engines_smoke.py</files>
  <read_first>
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md §Code Examples Example 4 (compare_engines.py 골격 — 그대로 인용)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md D-13, D-14, D-15, D-16 (검증 영상·임계값·실패 정책)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md §회귀 보고서 포맷 권장 (JSON + Markdown)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md §Pitfall 6 (NLF 격리 후 기존 reference angles 무효화 — 같은 엔진끼리 비교 권장)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-VALIDATION.md (Manual-Only Verifications — 회귀 검증 실행 방법)
    - backend/research/pose_engines/nlf/nlf_engine.py (Plan 04 이동 결과 — PoseEngine wrapper stub 존재)
    - backend/functions/pipeline/app.py (_process 함수 — compare_engines가 호출할 다운스트림 entry point)
    - backend/shared/python/sunity_shared/analysis/pose_frame.py (to_coco17_array, PoleAxis)
    - backend/shared/python/sunity_shared/analysis/pole/detector.py (HoughPoleDetector)
  </read_first>
  <action>
    1) `backend/research/pose_engines/nlf/nlf_engine.py` 보강 (Plan 04에서 이동 + stub 추가됨):
       - 기존 NlfPoseEstimator 클래스 보존 (기존 `estimate(frames) -> (T,17,4)` 시그니처).
       - 신규 메서드 `estimate_as_pose_frames(self, frames: np.ndarray, pole_axis: PoleAxis) -> list[PoseFrame]`:
         - 내부에서 `keypoints_array = self.estimate(frames)` 호출 (T, 17, 4 ndarray)
         - skeleton.KEYPOINT_NAMES 17개 순서로 각 frame index t에 대해 PoseFrame 인스턴스 구성
         - keypoints_3d: 17 키 + confidence는 1 - uncertainty (NLF의 4번째 채널 = uncertainty)
         - raw_landmarks_33: {} (NLF는 SMPL 형태 — 33 BlazePose 매핑 없음. 빈 dict 또는 SMPL 17만)
         - keypoints_3d_pole_aligned: pole.aligner.compute_alignment_matrix(pole_axis.direction_3d) → apply_alignment 호출
         - pole_extension_landmarks, keypoints_2d: None (NLF는 미제공)
       - 본 wrapper는 compare_engines.py가 호출 — 같은 PoseEngine Protocol 시그니처로 두 엔진을 동일 다운스트림(pipeline._process 또는 그 분해)에 주입 가능.

    2) `backend/research/evaluations/__init__.py` (Plan 04에서 생성됨) 보강:
       - `from .compare_engines import run_engine, EngineResult` re-export 추가.

    3) `backend/research/evaluations/reports/.gitkeep` 신규 (빈 파일) — 결과 디렉터리 보장.

    4) `backend/research/evaluations/compare_engines.py` 신규 생성 (RESEARCH §Code Example 4 골격 기반):
       - module docstring: "MediaPipe vs NLF 회귀 검증 (D-13, D-14, D-15, D-16). R&D 비교군 전용 — 제품 코드 비호출. 사용: python -m backend.research.evaluations.compare_engines --motions ref-... --out reports/compare_YYYYMMDD.json"
       - `from __future__ import annotations`, imports: argparse, json, time, tempfile, pathlib, dataclasses, numpy as np, boto3.
       - `@dataclass\nclass EngineResult`: overall_score, dimension_scores (dict), top3_failures (list[str] — joint key 또는 finding key), avg_keypoint_confidence (float), ms_per_frame (float).
       - `def run_engine(engine_name: Literal['mediapipe', 'nlf'], video_path: str) -> EngineResult`:
         - 임시 파일로 S3 영상 다운로드 (s3:// URL 파싱 + boto3.client('s3').download_file)
         - frame_extractor.extract (재사용)
         - HoughPoleDetector.detect → pole_axis
         - engine_name=='mediapipe'면 MediaPipePoseEngine + .estimate(frames, pole_axis); 'nlf'면 NlfPoseEstimator + .estimate_as_pose_frames(frames, pole_axis)
         - 추론 시간 측정 (time.perf_counter 전후, ms_per_frame = elapsed_ms / num_frames)
         - to_coco17_array → compute_joint_angles → temporal_fill → kismam + dimensions 호출 → overall_score, dimension_scores, top3_failures
         - avg_keypoint_confidence: pose_frames의 모든 keypoints_3d.confidence 평균
         - EngineResult 반환
       - `def compare_engines(motion_ids: list[str], bucket: str = "sunity-motion-pilot-videos") -> dict`: 각 motion_id에서 두 엔진 run_engine → 결과 dict 구성 (RESEARCH Example 4 그대로):
         - score_gap, within_tolerance_5pt (≤5), mediapipe_score_ge_70 (≥70), top3_overlap_2_of_3 (≥2), avg_confidence_ok (≥AVG_CONFIDENCE_THRESHOLD), ms_per_frame
       - `AVG_CONFIDENCE_THRESHOLD = 0.5` (A9 belle 보류 — module-level 상수로 상단에 배치 + 주석 "belle 확정 필요 — placeholder")
       - `def generate_markdown_report(payload: dict) -> str`: 사람용 Markdown 표 생성:
         - 헤더: "# Phase 1 회귀 검증 보고서 — MediaPipe vs NLF"
         - 생성 시각 + criteria 박스 (D-14 ±5점, D-15 70점·2/3·confidence)
         - motion별 표 (motion_id, MediaPipe score, NLF score, gap, top3_overlap, avg_conf, ms/frame)
         - summary 표 (all gates PASS/FAIL)
         - 최종 결정 박스: phase1_ready_to_swap=true면 "atomic swap 진행 (D-08)", false면 "D-16에 따라 보류 — 원인 분석 후 재시도"
       - `def main()`:
         - argparse: --motions (nargs+, required, default ['ref-ballerina-spin', 'ref-front-hook-spin', 'ref-plank-spin', 'ref-invert-butterfly-combo', 'ref-gemini-to-ayesha-combo'] — D-13 정은지 5영상 default)
         - --out (Path, default `reports/compare_<timestamp>.json`)
         - --bucket (default 'sunity-motion-pilot-videos')
         - compare_engines 호출 → payload 구성 (criteria + results + summary + phase1_ready_to_swap)
         - JSON 출력 + Markdown 동시 출력 (args.out.with_suffix('.md'))
       - `if __name__ == '__main__': main()`.

    5) `backend/tests/test_compare_engines_smoke.py` 신규 생성:
       - `from backend.research.evaluations.compare_engines import EngineResult, generate_markdown_report, AVG_CONFIDENCE_THRESHOLD`
       - Test 1 (test_engine_result_dataclass): EngineResult(overall_score=85, dimension_scores={'angle': 80, 'line': 90, 'stability': 85}, top3_failures=['left_knee', 'right_hip', 'left_shoulder'], avg_keypoint_confidence=0.85, ms_per_frame=200.0) 생성 성공.
       - Test 2 (test_generate_markdown_report_basic): synthetic payload (motion 1개 + summary) → markdown 문자열 출력 → 헤더 "# Phase 1 회귀 검증 보고서" 포함 + motion_id row 포함 + summary 표 포함.
       - Test 3 (test_phase1_ready_to_swap_logic): synthetic results 1개 모두 PASS → summary.phase1_ready_to_swap == True. 하나 FAIL → False.
       - Test 4 (test_avg_confidence_threshold_belle_review): `AVG_CONFIDENCE_THRESHOLD == 0.5` (A9 placeholder 검증 + belle 확정 후 갱신 reminder).
       - run_engine 자체는 S3·MediaPipe·NLF 다 필요 — smoke 테스트 범위 밖. mock 또는 skip (`@pytest.mark.skipif(not has_runpod, reason="full engine 실행은 RunPod Pod 필요")` 사용 가능).
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_compare_engines_smoke.py -x -v && python -c "from backend.research.evaluations.compare_engines import compare_engines, generate_markdown_report, EngineResult, AVG_CONFIDENCE_THRESHOLD; print('import OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f backend/research/evaluations/compare_engines.py` exits 0
    - `test -d backend/research/evaluations/reports` exits 0 (.gitkeep 존재)
    - `grep -n "def run_engine\|def compare_engines\|def generate_markdown_report\|def main" backend/research/evaluations/compare_engines.py | wc -l` >= 4
    - `grep -nE "within_tolerance_5pt|mediapipe_score_ge_70|top3_overlap_2_of_3|avg_confidence_ok" backend/research/evaluations/compare_engines.py | wc -l` >= 4 (D-14 + D-15 4 게이트)
    - `grep -n "phase1_ready_to_swap" backend/research/evaluations/compare_engines.py` matches at least 2 lines (compute + summary 포함)
    - `grep -n "AVG_CONFIDENCE_THRESHOLD = 0.5\|AVG_CONFIDENCE_THRESHOLD=0.5" backend/research/evaluations/compare_engines.py` matches exactly 1 line (A9 placeholder)
    - `grep -nE "A9|belle 확정|placeholder" backend/research/evaluations/compare_engines.py` matches at least 1 line (belle 검토 reminder)
    - `grep -n "estimate_as_pose_frames" backend/research/pose_engines/nlf/nlf_engine.py` matches at least 1 method definition
    - `pytest backend/tests/test_compare_engines_smoke.py -x` exits 0 (4 tests pass)
    - `python -c "from backend.research.evaluations.compare_engines import compare_engines; print('importable')"` outputs 'importable' (RunPod 부재 환경에서도 import만은 OK — mediapipe·NLF lazy)
    - Default motions list = 5개 (D-13 정은지 5영상)
  </acceptance_criteria>
  <done>compare_engines.py 스크립트 + NlfPoseEstimator wrapper + smoke 테스트 4개 통과. D-13~D-16 게이트 4개 모두 박제. A9 (AVG_CONFIDENCE_THRESHOLD placeholder) 명시 + belle 확정 필요 reminder. Plan 06 Task 2의 belle checkpoint에서 실제 실행 + 검토.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: belle 회귀 검증 실행 + atomic swap 최종 승인 (CHECKPOINT)</name>
  <what-built>
    Plan 02~05 완료 — atomic swap 완료된 MediaPipe + 폴 축 파이프라인 + Plan 06 Task 1 compare_engines.py 스크립트.
    이제 belle가 RunPod Pod에서 회귀 검증을 실행하고 보고서를 검토해 atomic swap의 결과(MediaPipe가 NLF 대비 분석 정확도를 유지하는지)를 승인 또는 D-16에 따라 보류한다.
  </what-built>
  <how-to-verify>
    1) RunPod Pod에 SSH 접속 (sunity3412 계정, [MEMORY: RunPod GPU dev env])
    2) Pod에서 setup.sh 실행 (Pod 재생성 후거나 처음 셋업 시):
       `cd /workspace && bash backend/runpod_inference/setup.sh`
       - mediapipe 0.10.35 + opencv + scipy 설치 확인
       - pip check 통과 확인 (protobuf 충돌 없음 — Pitfall 4)
       - pose_landmarker_heavy.task 다운로드 (~26MB, /workspace/models/)
       - MEDIAPIPE_POSE_MODEL_PATH env 영구화 확인
       - sanity import 통과
    3) 정은지 5영상 reference motion id 확인 (Firestore 또는 backend/scripts/reference-angles.json — D-13 default):
       - ref-ballerina-spin
       - ref-front-hook-spin
       - ref-plank-spin
       - ref-invert-butterfly-combo
       - ref-gemini-to-ayesha-combo
       (영상 부재 시 belle가 다른 5영상 motion_id 제공)
    4) 회귀 검증 실행:
       `cd /workspace && python -m backend.research.evaluations.compare_engines --motions ref-ballerina-spin ref-front-hook-spin ref-plank-spin ref-invert-butterfly-combo ref-gemini-to-ayesha-combo --out backend/research/evaluations/reports/compare_$(date +%Y%m%d).json`
    5) 출력 검토:
       a) JSON 파일: `cat backend/research/evaluations/reports/compare_*.json | python -m json.tool | less`
       b) Markdown: `cat backend/research/evaluations/reports/compare_*.md`
    6) D-14, D-15 게이트 검토 (보고서 summary):
       - all_within_tolerance (D-14): MediaPipe vs NLF 점수 갭 ±5점 이내?
       - all_mediapipe_ge_70 (D-15 ①): 모든 정은지 영상이 MediaPipe에서 ≥70점? (위양성 0)
       - all_top3_overlap_ok (D-15 ②): Top-3 실패 원인이 ≥2/3 겹침?
       - 각 motion별 avg_confidence_ok (D-15 ③): MediaPipe 평균 keypoint confidence ≥0.5 (A9 placeholder — belle가 임계값 확정)
       - mediapipe_ms_per_frame (D-15 ④): 추론 속도 측정 — SLA 적합성 (200~400ms/frame 예상, Lambda CPU SLA 부적합 시 deferred 결정)
    7) `summary.phase1_ready_to_swap` 값 확인:
       - **true (모든 게이트 PASS)**: atomic swap 결과 신뢰 가능 → reference 5개 MediaPipe 재추출 권장(Pitfall 6) + 사용자 분석 모두 MediaPipe로 진행 + Phase 1 완료.
       - **false (게이트 FAIL)**: D-16에 따라 atomic swap 보류 (swap은 이미 코드 레벨에선 됨 — git revert 또는 NLF 임시 복귀). 원인 분석 → Hough 파라미터·매핑 표·confidence 계산 튜닝 → 재실행.
    8) A9 임계값(AVG_CONFIDENCE_THRESHOLD) belle 확정:
       - 5영상 결과의 avg_keypoint_confidence 분포 확인 → belle가 임계값 결정 (e.g., 0.5? 0.6? 0.7?)
       - compare_engines.py의 `AVG_CONFIDENCE_THRESHOLD` 값을 확정값으로 갱신 (Plan 06 Task 1에선 placeholder)
    9) 25개 다양성 세트 요청 재확인:
       - 5영상만으로는 다양성 부족 — belle가 다양한 경력/체형 10 + 고의 평이 5 + 정은지 10 (총 25개)를 지속 확보 약속 (D-13)
       - 향후 phase에서 25개 세트로 재검증 시 본 compare_engines.py 재사용
  </how-to-verify>
  <resume-signal>
    다음 중 하나로 응답:
    - "approved" + AVG_CONFIDENCE_THRESHOLD 확정값 (e.g., "approved, threshold=0.55") → Phase 1 완료, ROADMAP Phase 2로 진행
    - "hold: <원인>" → D-16에 따라 보류, 원인 분석 + 재시도 plan 추가 필요 (e.g., "hold: ref-invert에서 MediaPipe 41점 위양성 발생 — Hough 튜닝 필요")
    - "issues: <상세>" → 회귀 보고서에서 발견한 이슈 상세 보고
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RunPod Pod ↔ S3 reference 영상 | personal video 처리 시 RunPod Pod에서만 + 결과 보고서에는 motion_id만 (영상 경로 노출 금지) |
| 회귀 보고서 ↔ belle 검토 | belle가 게이트 결과를 직접 확인 — auto-advance 금지 (D-16 보류 결정권자) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-03 | Information disclosure | compare_engines.py가 정은지 영상 처리 시 personal video 캐시 → 보안 저장소 외 누출 | mitigate | RunPod Pod에서만 실행 + 결과 보고서에는 motion_id만. 임시 다운로드 영상은 tempfile (자동 cleanup) |
| T-1-17 | Repudiation | belle 승인 없이 swap 진행 | mitigate | Task 2 = blocking checkpoint, autonomous=false. resume-signal 필수 |
| T-1-18 | Configuration | A9 threshold 미확정으로 swap 진행 → 후속 phase 신뢰도 부정확 | mitigate | resume-signal에 threshold 확정값 요구 |
</threat_model>

<verification>
- compare_engines.py 실행이 RunPod Pod에서 정은지 5영상에 대해 JSON + Markdown 출력
- belle가 보고서를 검토하고 명시적으로 승인/보류 결정
- A9 (AVG_CONFIDENCE_THRESHOLD) belle 확정값으로 갱신
- atomic swap 결과가 분석 정확도를 보존 (위양성 0, Top-3 일치, 점수 갭 ±5점 이내)
- 보고서가 `backend/research/evaluations/reports/`에 저장되어 향후 referenceable
</verification>

<success_criteria>
- compare_engines.py 스크립트 작성 + smoke 테스트 통과
- NlfPoseEstimator의 PoseEngine wrapper(`estimate_as_pose_frames`) 구현
- 정은지 5영상 회귀 검증 실행 + JSON + Markdown 보고서 생성
- belle가 보고서를 검토 + atomic swap 승인 또는 D-16 보류 결정
- AVG_CONFIDENCE_THRESHOLD belle 확정 (A9 해소)
- 25개 다양성 세트 belle 인지 유지 (지속 요청)
- Phase 1 Success Criteria 6개 모두 만족 — PoseEngine 인터페이스·MediaPipe 어댑터·NLF 격리·폴 축·confidence 게이트·PoseFrame lockstep·MediaPipe vs NLF 회귀 보고서
</success_criteria>

<output>
Create `.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-06-SUMMARY.md` when done. SUMMARY 내용:
- compare_engines.py 구현 요약 (라인 수, 게이트 4개 박제 확인)
- NlfPoseEstimator.estimate_as_pose_frames 구현 요약
- smoke 테스트 4개 결과
- belle checkpoint 결과 (approved + threshold 확정 또는 hold + 원인)
- 회귀 보고서 위치 (`backend/research/evaluations/reports/compare_YYYYMMDD.json + .md`)
- Phase 1 종료 게이트 통과 여부 (Success Criteria 6개 모두 만족)
- Phase 2 진입 전 belle 작업 사항:
  - reference 5개 MediaPipe 재추출 (Pitfall 6) — `backend/scripts/extract_reference_angles.py` MediaPipe 버전 필요 (별 phase 또는 후속 todo)
  - 25개 다양성 세트 belle 지속 확보 reminder
  - RunPod Pod 처분 검토 (deferred — MediaPipe가 Lambda CPU 적합한지 SLA 측정 후 결정)
</output>
