---
phase: 01-poseengine-mediapipe-nlf-r-d
plan: 06
type: execute
wave: 2
depends_on:
  - 01-02
  - 01-03
files_modified:
  - backend/research/__init__.py
  - backend/research/evaluations/__init__.py
  - backend/research/evaluations/compare_engines.py
  - backend/research/evaluations/reports/.gitkeep
  - backend/tests/test_compare_engines_smoke.py
autonomous: false
requirements:
  - POSE-01
  - POSE-02
must_haves:
  truths:
    - "compare_engines.py가 MediaPipePoseEngine과 NlfPoseEstimator를 동일 영상 N개에서 실행하고 비교 보고서를 JSON + Markdown으로 출력한다 (D-13 검증 영상 5개)"
    - "본 plan은 Wave 2 — NLF는 아직 sunity_shared/analysis/pose_estimator.py (옛 위치)에 존재. compare_engines.py가 그 import 경로를 사용한다 (H-1 박제)"
    - "D-14 박제: 점수 갭 허용 ±5점 이내(within_tolerance_5pt). 갭이 ±5점 초과면 D-16에 따라 atomic swap 보류"
    - "D-15 박제: 추가 검증 지표 4개를 motion별로 표기 — (1) 정은지 영상이 MediaPipe에서 ≥70점 (mediapipe_score_ge_70, 고수 위양성 0, SCORE-04 직결), (2) Top-3 실패 원인 ≥2/3 겹침 (top3_overlap_2_of_3), (3) MediaPipe 평균 keypoint confidence ≥ threshold (avg_confidence_ok), (4) MediaPipe Heavy ms/frame (mediapipe_ms_per_frame)"
    - "summary.phase1_ready_to_swap이 D-14 + D-15 게이트 모두 PASS여야 true"
    - "belle 시각 검토 checkpoint가 회귀 보고서 + 정은지 5영상 결과를 함께 보고 atomic swap 승인 또는 D-16에 따라 보류 결정 — swap (Plan 04/05)은 본 checkpoint 승인 후에만 진행 (H-1 박제, D-08 atomic swap의 회귀 검증 선행 조건)"
  artifacts:
    - path: "backend/research/evaluations/compare_engines.py"
      provides: "MediaPipe vs NLF 회귀 검증 스크립트 (D-13~D-16)"
      contains: "phase1_ready_to_swap"
    - path: "backend/research/evaluations/reports/"
      provides: "compare_engines.py 출력 디렉터리 (JSON + Markdown)"
  key_links:
    - from: "compare_engines.py"
      to: "MediaPipePoseEngine (Plan 02) + NlfPoseEstimator (sunity_shared/analysis/pose_estimator.py — 옛 위치, Wave 2 시점)"
      via: "두 엔진을 동일 다운스트림 (compute_joint_angles, dimensions) 입력으로 사용"
      pattern: "run_engine"
    - from: "회귀 보고서"
      to: "belle 검토 + Wave 3 (Plan 04 + Plan 05) gate"
      via: "checkpoint:human-verify (gate=blocking)"
      pattern: "phase1_ready_to_swap"
---

<objective>
MediaPipe vs NLF 회귀 검증 스크립트(compare_engines.py)를 작성하고 정은지 영상 5개에서 실행해 점수 갭(±5점)·고수 위양성(≥70점)·Top-3 일치율(≥2/3)·평균 confidence를 비교 보고서로 출력한다. **본 plan은 Wave 2에서 실행** — NLF는 아직 격리되지 않은 상태(`backend/shared/python/sunity_shared/analysis/pose_estimator.py` 옛 위치)이므로 두 엔진을 모두 import해 비교 가능. belle가 보고서를 검토하고 **atomic swap (Plan 04 NLF 격리 + Plan 05 pipeline swap)을 승인 또는 D-16에 따라 보류**. 본 plan의 belle checkpoint가 통과해야만 Wave 3가 시작된다 (H-1 박제 — D-08/D-16 wave 순서 정정).

Purpose: D-13(검증 영상 5개) · D-14(±5점 허용) · D-15(4 지표) · D-16(검증 실패 시 보류) 박제. ROADMAP Success #5 (R&D 평가 스크립트). 신뢰도 게이트 — 회귀 검증이 통과해야 atomic swap이 정당화됨 (D-08 locked decision).

Output: compare_engines.py + 회귀 보고서 디렉터리 + smoke 테스트 + belle 검토 checkpoint (Wave 3 gate).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-VALIDATION.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-REVIEWS.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-01-SUMMARY.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-02-SUMMARY.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-03-SUMMARY.md
@backend/shared/python/sunity_shared/analysis/pose_estimator.py
@backend/shared/python/sunity_shared/analysis/pose_engines/mediapipe_engine.py
@backend/shared/python/sunity_shared/analysis/pose_frame.py
@backend/shared/python/sunity_shared/analysis/pole/detector.py
@backend/functions/pipeline/app.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: backend/research/ minimal 스캐폴드 + compare_engines.py (NLF 옛 위치 import — Wave 2 시점)</name>
  <files>backend/research/__init__.py, backend/research/evaluations/__init__.py, backend/research/evaluations/compare_engines.py, backend/research/evaluations/reports/.gitkeep, backend/tests/test_compare_engines_smoke.py</files>
  <read_first>
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-REVIEWS.md H-1 (wave order — compare BEFORE swap), L-2 (working directory 경로 통일)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md §Code Examples Example 4 (compare_engines.py 골격)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md D-13, D-14, D-15, D-16
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md §회귀 보고서 포맷 권장 (JSON + Markdown)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-VALIDATION.md (회귀 검증 실행 방법)
    - backend/shared/python/sunity_shared/analysis/pose_estimator.py (Wave 2 시점에 NLF가 여전히 옛 위치에 존재 — compare_engines가 여기서 import. 아직 R&D 격리 전)
    - backend/shared/python/sunity_shared/analysis/pose_engines/mediapipe_engine.py (Plan 02 산출 — MediaPipePoseEngine)
    - backend/shared/python/sunity_shared/analysis/pole/detector.py (Plan 03 산출 — HoughPoleDetector)
    - backend/shared/python/sunity_shared/analysis/pose_frame.py (to_coco17_array, PoleAxis)
  </read_first>
  <action>
    1) `backend/research/__init__.py` 신규 (빈 파일 + docstring): "R&D 비교군 평가 디렉터리. Phase 1 Wave 2에서 compare_engines.py만 — NLF 격리 (Plan 04)는 Wave 3에서 진행."
    2) `backend/research/evaluations/__init__.py` 신규 (빈 + docstring): `from .compare_engines import run_engine, EngineResult, AVG_CONFIDENCE_THRESHOLD` re-export.
    3) `backend/research/evaluations/reports/.gitkeep` 신규 (빈 파일) — 결과 디렉터리 보장.

    4) `backend/research/evaluations/compare_engines.py` 신규 생성 (RESEARCH §Code Example 4 골격 기반):
       - module docstring: "MediaPipe vs NLF 회귀 검증 (D-13, D-14, D-15, D-16). **Wave 2 시점** — NLF는 아직 sunity_shared.analysis.pose_estimator (옛 위치)에서 import. R&D 격리 (Plan 04)는 본 회귀 검증 belle 승인 후에 진행. 사용: python -m backend.research.evaluations.compare_engines --motions ref-... --out reports/compare_YYYYMMDD.json. **실행 경로**: /workspace/SunityMotion (L-2 박제)"
       - `from __future__ import annotations`, imports: argparse, json, time, tempfile, pathlib, dataclasses, typing.Literal, numpy as np, boto3.
       - `@dataclass\nclass EngineResult`: overall_score (float), dimension_scores (dict), top3_failures (list[str]), avg_keypoint_confidence (float), ms_per_frame (float).
       - 상수: `AVG_CONFIDENCE_THRESHOLD: float = 0.5` — module-level + 주석 "A9 belle 보류 — placeholder. Plan 06 Task 2 회귀 결과 후 belle가 확정값으로 갱신".
       - `DEFAULT_MOTIONS: tuple[str, ...]` = 5개 (D-13 정은지 5영상 default): 'ref-ballerina-spin', 'ref-front-hook-spin', 'ref-plank-spin', 'ref-invert-butterfly-combo', 'ref-gemini-to-ayesha-combo'.
       - `def _download_video(s3_key: str, bucket: str, dest: pathlib.Path) -> None`: boto3 download_file.
       - `def _extract_frames(video_path: str) -> np.ndarray`: lazy import frame_extractor — `from sunity_shared.analysis.frame_extractor import FrameExtractor`. extractor.extract.
       - `def _run_mediapipe(frames: np.ndarray, pole_axis) -> tuple[list, dict]`: lazy import MediaPipePoseEngine → estimate → to_coco17_array → compute_joint_angles → temporal_fill → 점수. 반환 (pose_frames, score_payload).
       - `def _run_nlf(frames: np.ndarray, pole_axis) -> tuple[list, dict]`: lazy import — `from sunity_shared.analysis.pose_estimator import NlfPoseEstimator` (Wave 2 시점 — 옛 위치). estimate → (T, 17, 4) keypoints array → compute_joint_angles → temporal_fill → 점수. NLF는 PoseFrame을 반환하지 않으므로 keypoints array를 직접 사용 (변환 wrapper 불필요).
       - `def run_engine(engine_name: Literal['mediapipe', 'nlf'], video_s3_key: str, bucket: str) -> EngineResult`:
         1) tempfile로 영상 download
         2) frames extract
         3) HoughPoleDetector(lazy) → pole_axis
         4) start = time.perf_counter()
         5) engine_name=='mediapipe' → _run_mediapipe; 'nlf' → _run_nlf
         6) elapsed_ms = (time.perf_counter() - start) * 1000
         7) ms_per_frame = elapsed_ms / len(frames)
         8) overall_score, dimension_scores, top3_failures, avg_keypoint_confidence 추출 (score_payload에서)
         9) EngineResult 반환
       - `def compare_engines(motion_ids: list[str], bucket: str = "sunity-motion-pilot-videos") -> dict`:
         - 각 motion_id에 대해 두 엔진 run_engine → 결과 dict 구성:
           - score_gap = abs(mediapipe.overall - nlf.overall)
           - within_tolerance_5pt = score_gap <= 5.0 (D-14)
           - mediapipe_score_ge_70 = mediapipe.overall_score >= 70 (D-15 ①)
           - top3_overlap_2_of_3 = len(set(mp.top3_failures) & set(nlf.top3_failures)) >= 2 (D-15 ②)
           - avg_confidence_ok = mediapipe.avg_keypoint_confidence >= AVG_CONFIDENCE_THRESHOLD (D-15 ③)
           - mediapipe_ms_per_frame = mediapipe.ms_per_frame (D-15 ④)
         - summary: all_within_tolerance, all_mediapipe_ge_70, all_top3_overlap_ok, all_avg_confidence_ok = all(per-motion 게이트), phase1_ready_to_swap = all 4
       - `def generate_markdown_report(payload: dict) -> str`:
         - 헤더: "# Phase 1 회귀 검증 보고서 — MediaPipe vs NLF (Wave 2, atomic swap 직전 게이트)"
         - 생성 시각 + criteria 박스 (D-14 ±5점, D-15 70/2/conf/ms)
         - motion별 표 (motion_id, MP score, NLF score, gap, top3_overlap, avg_conf, ms/frame, 각 게이트 PASS/FAIL)
         - summary 표 (all gates PASS/FAIL)
         - 최종 결정 박스: phase1_ready_to_swap=true면 "Wave 3 진행 가능 (Plan 04 격리 + Plan 05 atomic swap)", false면 "D-16에 따라 Wave 3 보류 — Hough 파라미터·매핑·confidence 튜닝 후 본 plan 재실행"
       - `def main()`:
         - argparse: --motions (nargs+, default DEFAULT_MOTIONS), --out (Path, default reports/compare_<timestamp>.json), --bucket (default 'sunity-motion-pilot-videos')
         - compare_engines 호출 → payload 구성 (criteria + results + summary + phase1_ready_to_swap)
         - JSON 출력 + Markdown 동시 (args.out.with_suffix('.md'))
       - `if __name__ == '__main__': main()`.

    5) `backend/tests/test_compare_engines_smoke.py` 신규 생성:
       - `from backend.research.evaluations.compare_engines import EngineResult, generate_markdown_report, AVG_CONFIDENCE_THRESHOLD, DEFAULT_MOTIONS`
       - Test 1 (test_engine_result_dataclass): EngineResult 생성.
       - Test 2 (test_generate_markdown_report_basic): synthetic payload → markdown 출력 → 헤더 + motion_id row + summary 표 포함.
       - Test 3 (test_phase1_ready_to_swap_logic): synthetic results 1개 모두 PASS → summary.phase1_ready_to_swap == True. 하나 FAIL → False.
       - Test 4 (test_avg_confidence_threshold_placeholder): `AVG_CONFIDENCE_THRESHOLD == 0.5` (A9 placeholder 검증).
       - Test 5 (test_default_motions_count): len(DEFAULT_MOTIONS) == 5 (D-13).
       - run_engine 자체는 S3·MediaPipe·NLF 다 필요 — smoke 범위 밖. mock 또는 skip.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_compare_engines_smoke.py -x -v && python -c "from backend.research.evaluations.compare_engines import compare_engines, generate_markdown_report, EngineResult, AVG_CONFIDENCE_THRESHOLD, DEFAULT_MOTIONS; print('import OK, motions=', len(DEFAULT_MOTIONS))"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f backend/research/evaluations/compare_engines.py` exits 0
    - `test -d backend/research/evaluations/reports` exits 0
    - `grep -n "def run_engine\\|def compare_engines\\|def generate_markdown_report\\|def main" backend/research/evaluations/compare_engines.py | wc -l` outputs at least `4`
    - `grep -nE "within_tolerance_5pt|mediapipe_score_ge_70|top3_overlap_2_of_3|avg_confidence_ok" backend/research/evaluations/compare_engines.py | wc -l` outputs at least `4` (D-14 + D-15)
    - `grep -n "phase1_ready_to_swap" backend/research/evaluations/compare_engines.py | wc -l` outputs at least `2`
    - `grep -nE "AVG_CONFIDENCE_THRESHOLD\\s*=\\s*0\\.5" backend/research/evaluations/compare_engines.py | wc -l` outputs `1` (A9 placeholder)
    - `grep -nE "A9|belle 확정|placeholder" backend/research/evaluations/compare_engines.py | wc -l` outputs at least `1`
    - `grep -nE "from sunity_shared\\.analysis\\.pose_estimator import NlfPoseEstimator" backend/research/evaluations/compare_engines.py | wc -l` outputs at least `1` (Wave 2 — 옛 위치 import, H-1 박제)
    - `grep -nE "Wave 2|atomic swap 직전|R&D 격리.*Wave 3" backend/research/evaluations/compare_engines.py | wc -l` outputs at least `1` (wave 순서 인용)
    - `grep -nE "/workspace/SunityMotion" backend/research/evaluations/compare_engines.py | wc -l` outputs at least `1` (L-2 박제)
    - `pytest backend/tests/test_compare_engines_smoke.py -x` exits 0 (5 tests pass)
    - `python -c "from backend.research.evaluations.compare_engines import compare_engines; print('importable')"` outputs 'importable'
    - `python -c "from backend.research.evaluations.compare_engines import DEFAULT_MOTIONS; assert len(DEFAULT_MOTIONS) == 5"` exits 0
  </acceptance_criteria>
  <done>compare_engines.py 스크립트 + smoke 5개 테스트 통과. Wave 2 시점 (NLF 옛 위치 import) — H-1 박제. D-13~D-16 게이트 4개 박제. A9 placeholder + belle 확정 reminder. L-2 working directory /workspace/SunityMotion 박제. Plan 06 Task 2 belle checkpoint에서 실제 실행 + 검토.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: belle 회귀 검증 실행 + Wave 3 (atomic swap) 최종 승인 (CHECKPOINT — Wave 3 GATE)</name>
  <what-built>
    Wave 0 (Plan 01) + Wave 1 (Plan 02 MediaPipe 어댑터 + Plan 03 PoleDetector/Aligner) 완료 + Wave 2 (Plan 06 Task 1 compare_engines.py 스크립트).

    **중요**: 본 시점에는 NLF가 아직 격리되지 않음 (`backend/shared/python/sunity_shared/analysis/pose_estimator.py` 옛 위치에 그대로 존재). pipeline/app.py도 여전히 NLF 호출 중. **MediaPipe 어댑터는 모듈만 존재하고 pipeline에 연결 안 됨** — Wave 3 (Plan 04 + 05)가 belle 승인 후 진행.

    이제 belle가 RunPod Pod에서 compare_engines.py를 실행해 두 엔진 결과를 비교하고, **Wave 3 (atomic swap) 진행 여부를 결정** — D-08/D-16 locked decision 박제.
  </what-built>
  <how-to-verify>
    1) RunPod Pod에 SSH 접속 (sunity3412 계정, [MEMORY: RunPod GPU dev env]).
    2) Pod에서 MediaPipe 환경 셋업 (Plan 05 setup.sh는 아직 미작성 — 본 checkpoint에선 belle 수동 셋업):
       a) `cd /workspace/SunityMotion` (L-2 박제)
       b) `pip install mediapipe==0.10.35 opencv-python-headless==4.13.0.92 scipy>=1.17,<2.0 protobuf>=4.25,<5`
       c) `pip check` 통과 확인 (Pitfall 4)
       d) `mkdir -p /workspace/models && wget -O /workspace/models/pose_landmarker_heavy.task "https://storage.googleapis.com/mediapipe-models/pose_landmarker/pose_landmarker_heavy/float16/latest/pose_landmarker_heavy.task"`
       e) `export MEDIAPIPE_POSE_MODEL_PATH=/workspace/models/pose_landmarker_heavy.task`
       f) NLF 환경 유지 (옛 위치 그대로 — 격리 전)
       g) sanity: `python -c "from sunity_shared.analysis.pose_engines.mediapipe_engine import MediaPipePoseEngine; from sunity_shared.analysis.pose_estimator import NlfPoseEstimator; print('both engines loadable')"`
    3) 정은지 5영상 reference motion id 확인 (Firestore 또는 backend/scripts/reference-angles.json — D-13 default):
       - ref-ballerina-spin
       - ref-front-hook-spin
       - ref-plank-spin
       - ref-invert-butterfly-combo
       - ref-gemini-to-ayesha-combo
       (영상 부재 시 belle가 다른 5영상 motion_id 제공)
    4) 회귀 검증 실행:
       `cd /workspace/SunityMotion && python -m backend.research.evaluations.compare_engines --motions ref-ballerina-spin ref-front-hook-spin ref-plank-spin ref-invert-butterfly-combo ref-gemini-to-ayesha-combo --out backend/research/evaluations/reports/compare_$(date +%Y%m%d).json`
    5) 출력 검토:
       a) JSON: `cat backend/research/evaluations/reports/compare_*.json | python -m json.tool | less`
       b) Markdown: `cat backend/research/evaluations/reports/compare_*.md`
    6) D-14, D-15 게이트 검토:
       - all_within_tolerance (D-14): MediaPipe vs NLF 점수 갭 ±5점 이내?
       - all_mediapipe_ge_70 (D-15 ①): 모든 정은지 영상이 MediaPipe에서 ≥70점? (위양성 0)
       - all_top3_overlap_ok (D-15 ②): Top-3 실패 원인이 ≥2/3 겹침?
       - all_avg_confidence_ok (D-15 ③): MediaPipe 평균 keypoint confidence ≥0.5 (A9 placeholder)
       - mediapipe_ms_per_frame (D-15 ④): 추론 속도 측정
    7) `summary.phase1_ready_to_swap` 값 확인:
       - **true (모든 게이트 PASS)**: Wave 3 진행 가능 — Plan 04 NLF R&D 격리 + Plan 05 atomic swap (D-08 박제).
       - **false (게이트 FAIL)**: D-16에 따라 atomic swap 보류 — Hough 파라미터·매핑 표·confidence 튜닝 후 본 plan 재실행. **Wave 3 시작 금지** (H-1 박제 — D-08/D-16 wave 순서 정정).
    8) A9 임계값(AVG_CONFIDENCE_THRESHOLD) belle 확정:
       - 5영상 결과의 avg_keypoint_confidence 분포 확인 → belle가 임계값 결정 (0.5? 0.55? 0.6?)
       - compare_engines.py의 `AVG_CONFIDENCE_THRESHOLD`를 확정값으로 갱신 (Plan 06 Task 1에선 placeholder)
       - reliability.py의 RELIABILITY_*_THRESHOLD도 동일 검토 (Plan 01에서 placeholder)
    9) 25개 다양성 세트 요청 재확인 (D-13 belle 지속 확보 약속).
  </how-to-verify>
  <resume-signal>
    다음 중 하나로 응답:
    - "approved" + AVG_CONFIDENCE_THRESHOLD 확정값 (e.g., "approved, threshold=0.55") → Wave 3 진행 (Plan 04 + Plan 05). Phase 1 Wave 3 시작.
    - "hold: <원인>" → D-16에 따라 보류, 원인 분석 + Plan 06 Task 1 튜닝 + 재실행 plan 추가 필요 (e.g., "hold: ref-invert에서 MediaPipe 41점 위양성 — Hough 튜닝 필요")
    - "issues: <상세>" → 회귀 보고서에서 발견한 이슈 상세 보고
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RunPod Pod ↔ S3 reference 영상 | personal video 처리 시 RunPod Pod에서만 + 결과 보고서에는 motion_id만 |
| 회귀 보고서 ↔ belle 검토 | belle가 게이트 결과를 직접 확인 — auto-advance 금지 (D-16 보류 결정권자) |
| Wave 2 → Wave 3 gate | belle 명시 승인 없이 Plan 04/05 진행 금지 (H-1 박제 — D-08/D-16 wave 순서) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-03 | Information disclosure | compare_engines.py가 정은지 영상 처리 시 personal video 캐시 | mitigate | RunPod Pod에서만 실행 + 결과 보고서에는 motion_id만. 임시 다운로드 영상은 tempfile (자동 cleanup) |
| T-1-17 | Repudiation | belle 승인 없이 swap 진행 (H-1 위반) | mitigate | Task 2 = blocking checkpoint, autonomous=false. resume-signal 필수. Wave 3 plans depends_on에 본 plan 명시 |
| T-1-18 | Configuration | A9 threshold 미확정으로 swap 진행 → 후속 신뢰도 부정확 | mitigate | resume-signal에 threshold 확정값 요구 |
</threat_model>

<verification>
- compare_engines.py가 RunPod Pod에서 정은지 5영상에 대해 JSON + Markdown 출력
- belle가 보고서를 검토하고 명시적으로 승인 또는 보류 결정
- A9 (AVG_CONFIDENCE_THRESHOLD) belle 확정값으로 Plan 04/05/Plan 01 갱신 대상 명시
- atomic swap 결과가 분석 정확도를 보존 (위양성 0, Top-3 일치, 점수 갭 ±5점 이내) — Wave 3 진행의 게이트
- 보고서가 `backend/research/evaluations/reports/`에 저장 (향후 referenceable)
- Wave 3 (Plan 04 + Plan 05) 시작은 본 checkpoint의 approved resume-signal 이후에만 (H-1 박제)
</verification>

<success_criteria>
- compare_engines.py + smoke 5개 테스트
- Wave 2 시점 NLF 옛 위치 import (H-1 박제)
- 정은지 5영상 회귀 검증 실행 + JSON + Markdown 보고서
- belle가 보고서 검토 + Wave 3 (Plan 04 + Plan 05) 승인 또는 D-16 보류
- AVG_CONFIDENCE_THRESHOLD belle 확정
- 25개 다양성 세트 belle 인지 유지
- L-2 working directory /workspace/SunityMotion 통일
</success_criteria>

<output>
Create `.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-06-SUMMARY.md` when done. SUMMARY 내용:
- compare_engines.py 구현 요약 + 게이트 4개 박제 확인
- smoke 테스트 5개 결과
- belle checkpoint 결과 (approved + threshold 또는 hold + 원인)
- 회귀 보고서 위치 (`backend/research/evaluations/reports/compare_YYYYMMDD.json + .md`)
- Wave 3 진행 게이트 통과 여부 (Plan 04 + Plan 05 진행 신호)
- Wave 3 진입 전 belle 작업 사항:
  - AVG_CONFIDENCE_THRESHOLD 확정값을 Plan 01 RELIABILITY_*_THRESHOLD + Plan 06 AVG_CONFIDENCE_THRESHOLD에 동일 적용
  - 25개 다양성 세트 belle 지속 확보 reminder
  - Plan 04는 본 checkpoint approved 후에만 진행 가능 (H-1)
</output>
