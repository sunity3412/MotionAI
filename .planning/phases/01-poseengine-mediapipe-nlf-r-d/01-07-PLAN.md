---
phase: 01-poseengine-mediapipe-nlf-r-d
plan: 07
type: spike
wave: 2
depends_on:
  - 01-02
  - 01-03
  - 01-06
files_modified:
  - backend/research/spikes/__init__.py
  - backend/research/spikes/spike_motionbert.py
  - backend/research/spikes/mediapipe_to_h36m17.py
  - backend/research/spikes/README.md
autonomous: false
gap_closure: true
parent_plan: 01-06
parent_verdict: "D-16 보류 (회귀 게이트 4개 중 3개 FAIL — stability 28~50점 갭)"
requirements:
  - POSE-01
  - POSE-02
must_haves:
  truths:
    - "Plan 01-06 belle 검증 결과(D-16 보류, Path B-with-spike 확정) 직속 후속 plan — Plan 08 (lifter 본 통합) 진입 전 위험 검증"
    - "1개 lifter (MotionBERT primary, HybrIK backup) + 1개 영상 (ref-foxtop-split — Wave 2에서 가장 큰 갭 59점) 으로 최소 비용 검증"
    - "spike 통과 시 Plan 08 (full integration) 신설, 실패 시 belle 재결정 (다른 lifter 시도 vs Path A 회귀 vs Path D 임시 회귀)"
    - "spike 코드는 backend/research/spikes/ 로 격리 (production import 경로 비포함) — 운영 코드와 분리 (D-06/D-07 NLF 격리 패턴 따름)"
    - "MotionBERT 라이선스 Apache 2.0 확인 후 진입 — README 에 라이선스 출처 + 가중치 다운로드 경로 명시"
  artifacts:
    - path: "backend/research/spikes/spike_motionbert.py"
      provides: "MediaPipe 2D → MotionBERT 3D lift → 관절각 → stability/line 점수 출력 + NLF baseline 비교 (단일 영상)"
      contains: "def run_spike(video_path, motion_id)"
    - path: "backend/research/spikes/mediapipe_to_h36m17.py"
      provides: "MediaPipe 33 landmark → H3.6M 17 joint 매핑 어댑터 (MotionBERT 입력 형식)"
      contains: "MP33_TO_H36M17"
    - path: "backend/research/spikes/README.md"
      provides: "spike 실행 절차 + 가중치 다운로드 + MotionBERT 라이선스 출처 + 통과/실패 판정 기준"
  key_links:
    - from: "backend/research/spikes/spike_motionbert.py"
      to: "backend/shared/python/sunity_shared/analysis/pose_engines/mediapipe_engine.py"
      via: "MediaPipe 2D 단계 재사용 (3D lift만 신규)"
      pattern: "MediaPipePoseEngine|world_landmarks|landmarks_2d"
    - from: "backend/research/spikes/spike_motionbert.py"
      to: "backend/research/evaluations/compare_engines.py"
      via: "scoring 체인 동일 (compute_joint_angles → absolute_dimension_scores → overall_from_dimensions)"
      pattern: "_score_from_angles|absolute_dimension_scores"
---

<objective>
MediaPipe 2D + MotionBERT (Apache 2.0 라이선스 lifter) 조합을 ref-foxtop-split 1개 영상으로 spike. NLF baseline 대비 stability/line 차원 점수 회복 정도를 측정하고, Plan 08 (full integration) 진입 또는 다른 경로 회귀를 belle 가 결정할 수 있도록 정량 데이터 제공.

Purpose: Plan 01-06 belle 검증에서 MediaPipe stability 가 NLF 대비 28~50점 깎이는 구조 한계 확인 (z 추정 노이즈, 인버트 자세에서 frame-to-frame 일관성 무너짐). MediaPipe 의 2D 키포인트는 정확(avg_conf 0.79~0.89)하므로, 2D 키포인트만 채용 + 별도 학습된 3D lifter 로 z 를 재구성하면 stability 회복이 가능하다는 가설을 최소 비용으로 검증.

Output: spike_motionbert.py 단일 스크립트 (실행 → 표준 출력에 motion 별 score + per-dim 점수 + NLF baseline 갭) + MediaPipe→H3.6M 17-joint 매핑 어댑터 + README (Pod 실행 절차 + 라이선스 + 판정 기준). 통과 시 Plan 08 신설용 정량 근거 확보.
</objective>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-RESEARCH.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-REVIEWS.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-06-SUMMARY.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-02-SUMMARY.md
@CLAUDE.md

## Plan 01-06 핵심 발견 (재인용)

- MediaPipe avg_conf 0.79~0.89 (D-15③ PASS) — 키포인트 검출 자체는 정확
- 모든 5영상에서 stability 차원이 NLF 대비 28~50점 깎임 (ref-foxtop-split: MP 3 vs NLF 53)
- world_landmarks 의 z 추정이 인버트/측면/폴 폐색 자세에서 노이즈 큼 → angle 시간 분산 → stability 무너짐
- belle 결정: D-16 보류 + Path B-with-spike (이 plan 이 spike, Plan 08 이 본 통합)

## MotionBERT 선정 근거

- 라이선스: **Apache 2.0** (https://github.com/Walter0807/MotionBERT)
- 논문: ICCV 2023 — H3.6M, AMASS pretrained
- 시간축 transformer — frame-to-frame 일관성에 강함 (stability 문제와 정렬)
- 추론 속도: GPU 약 30ms/frame
- 백업 후보: HybrIK (MIT, https://github.com/Jeff-sjtu/HybrIK), GAST-Net (MIT). spike 가 MotionBERT 에서 실패 시 backup 으로 재실행.

## Pod 잔존물 (재사용)

`01-06-SUMMARY.md` "Pod 환경 잔존물" 섹션 참조. MediaPipe + NLF + AWS 키 + PYTHONPATH 모두 살아있음. spike 는 MotionBERT 가중치 다운로드 + repo clone 만 추가.

## H3.6M 17-joint vs COCO-17 매핑

MotionBERT 입력은 H3.6M 17-joint (Hip 중심, hip-spine-thorax-neck-nose 척추 5점 포함). MediaPipe 33 의 어떤 joint 가 H3.6M 17 의 어떤 인덱스에 대응되는지 매핑이 spike 의 핵심 어댑터. 표준 매핑 + 일부 derive (hip = (l_hip+r_hip)/2 등) 필요.
</context>

<tasks>

## Task 1 — MotionBERT 라이선스 + 가중치 + 어댑터 (T-1)

**T-1-1**: MotionBERT 라이선스 확인 → README 에 출처 명시 (Apache 2.0 https://github.com/Walter0807/MotionBERT/blob/main/LICENSE)
**T-1-2**: MediaPipe 33 → H3.6M 17 매핑 테이블 `MP33_TO_H36M17` 작성 (`mediapipe_to_h36m17.py`). hip / spine / thorax 는 derive 규칙 명시.
**T-1-3**: 매핑 unit 테스트 (가짜 33-landmark 입력 → 17-joint 출력 shape + 핵심 인덱스 매칭 확인)
**T-1-4**: `backend/research/spikes/__init__.py` 빈 진입점 + `README.md` 초안 (Pod 실행 절차 + MotionBERT clone/weights/install + 통과 판정 기준)

## Task 2 — spike_motionbert.py 작성 (T-2)

**T-2-1**: `run_spike(video_path, motion_id)` 함수
  - MediaPipe 2D keypoints 추출 (기존 MediaPipePoseEngine 재사용)
  - MP 33 → H3.6M 17 변환
  - MotionBERT 추론 (T-frame batch)
  - H3.6M 17 → COCO-17 역변환 (compute_joint_angles 입력 호환)
  - compute_joint_angles → temporal_fill → absolute_dimension_scores → overall_from_dimensions
  - NLF baseline 동일 영상에 대해 별도 실행 (compare_engines.py 의 _run_nlf 재사용)
**T-2-2**: CLI 인자 (video local path 또는 S3 key + bucket) + 표준 출력 포맷 (JSON + Markdown)
**T-2-3**: 출력에 포함:
  - lifter (MP+MotionBERT) overall + per-dim 점수
  - NLF baseline overall + per-dim 점수
  - 갭 + 각 차원별 갭
  - 추론 시간 (ms/frame)
**T-2-4**: spike 실행 (Pod 에서) — ref-foxtop-split 1개 영상

## Task 3 — 결과 분석 + belle checkpoint (T-3) — autonomous: false

**T-3-1**: spike 결과 보고서 정리 (`backend/research/spikes/reports/spike_motionbert_<date>.md`):
  - lifter overall vs NLF overall + 갭
  - per-dimension 회복분 (stability 가 핵심)
  - 추론 시간 차이
  - 통과/실패 판정
**T-3-2**: belle checkpoint — 다음 중 하나 응답:
  - **"approved, proceed to Plan 08"** — strong pass (stability ≥55, overall ≥60). Plan 08 본 통합 작성 + Wave 3 진입 준비
  - **"try HybrIK"** — weak signal (stability 40~55). HybrIK 으로 spike 재실행 (T-2 재실행, T-1 어댑터만 교체)
  - **"hold + reconsider path A"** — spike 실패 (stability <40). Path A (튜닝) 또는 Path D (NLF 유지) 로 재고
  - **"hold + commercial license"** — Path D 전환. Max Planck Innovation 컨택 (D-08 백업)

## 판정 기준 (T-3 belle 결정용)

| 결과 | stability | overall | 의미 |
|---|---|---|---|
| Strong pass | ≥55 | ≥60 | MotionBERT 충분, Plan 08 진입 |
| Weak signal | 40~55 | 45~60 | HybrIK 등 백업 lifter 시도 |
| 실패 | <40 | <45 | lifter 만으로는 안 됨, Path A/D 재고 |

(현재 MP 단독: stability 3, overall 3 — 어떤 회복이든 의미 있음)

## Pod 실행 절차 (README 에 동일 내용 박제)

```bash
# 1) repo pull
cd /workspace/SunityMotion
git pull --ff-only origin main

# 2) MotionBERT clone + 가중치 다운로드 (1회)
cd /workspace
git clone https://github.com/Walter0807/MotionBERT.git
cd MotionBERT

# 사전학습 가중치 (H3.6M)
mkdir -p checkpoint/pose3d/MB_train_h36m
# Google Drive 링크: https://drive.google.com/uc?id=1ALFJ... (MotionBERT README 참조)
# wget 가능한 곳에 미러된 가중치 또는 belle 가 직접 업로드

# 3) MotionBERT 의존성 설치
pip install -r requirements.txt
# 핵심: torch, einops, timm 등

# 4) spike 실행
cd /workspace/SunityMotion
python3 -m backend.research.spikes.spike_motionbert \
  --motion ref-foxtop-split \
  --bucket sunity-motion-pilot-videos \
  --motionbert-root /workspace/MotionBERT \
  --motionbert-weights /workspace/MotionBERT/checkpoint/pose3d/MB_train_h36m/best_epoch.bin \
  --out backend/research/spikes/reports/spike_motionbert_$(date +%Y%m%d).json
```

</tasks>

<success_criteria>
- [ ] MotionBERT 라이선스 Apache 2.0 확인 + README 출처 명시
- [ ] MediaPipe 33 → H3.6M 17 매핑 어댑터 (unit 테스트 포함)
- [ ] spike_motionbert.py: MP 2D → MotionBERT → 3D → 점수 + NLF baseline 비교 (단일 영상)
- [ ] Pod 에서 ref-foxtop-split 1영상 실행 → JSON + Markdown 보고서 산출
- [ ] belle checkpoint 응답 받음 (approved / try HybrIK / hold)
- [ ] 모든 spike 코드는 `backend/research/spikes/` 격리 (제품 import 경로 침범 없음)
- [ ] 추론 시간 측정 (MP + MotionBERT 합산 ms/frame)
- [ ] 가중치 파일은 git 추적 안 함 (.gitignore 또는 별도 path)
- [ ] No emojis anywhere
</success_criteria>

<scope_limits>
- 5영상 전체 회귀 검증은 본 plan 의 범위 밖 — Plan 08 의 책임.
- HybrIK 등 백업 lifter 평가는 spike 가 "weak signal" 일 때만 진입.
- production pipeline (functions/pipeline/app.py) 수정 금지 — spike 는 별도 격리.
- Plan 06 의 compare_engines.py 수정 금지 — Plan 08 에서 변경.
</scope_limits>
