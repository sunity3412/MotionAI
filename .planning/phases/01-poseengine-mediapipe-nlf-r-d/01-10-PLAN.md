---
phase: 01-poseengine-mediapipe-nlf-r-d
plan: 10
type: spike
wave: 2
depends_on:
  - 01-07
  - 01-08
  - 01-09
files_modified:
  - backend/research/spikes/__init__.py
  - backend/research/spikes/rtmpose_to_h36m17.py
  - backend/research/spikes/spike_rtmpose.py
  - backend/research/spikes/README.md
  - backend/tests/test_spike_rtmpose_to_h36m17.py
autonomous: false
gap_closure: true
parent_plan: 01-09
parent_verdict: "Plan 09 license_blocked at T-1-1 — AlphaPose Noncommercial Only. belle 결정 (2026-06-01) = option-b-1 RTMPose (Apache 2.0, OpenMMLab) 으로 대체. 측면 자세 보강 재시도."
requirements:
  - POSE-01
  - POSE-02
must_haves:
  truths:
    - "Plan 01-08 5영상 회귀에서 ref-sideway-spin 만 64점(목표 70). MotionBERT 가 H3.6M (대부분 정면 walking/sitting) 학습이라 측면 자세 z 복원이 약함. 1차 가설: 2D detector (MediaPipe) 가 측면에서 keypoint 분포가 좁아져 lift 가 어려워짐"
    - "RTMPose-l (MMPose, OpenMMLab) 은 COCO 데이터셋 학습 (정면+측면+occlusion 분포 더 균등). 2D detector 단계만 MediaPipe→RTMPose 교체, MotionBERT lift 단계는 그대로 유지. Plan 08 production 코드 (pose_lifters/) 무수정"
    - "RTMPose 라이선스 Apache 2.0 확인 후 진입 (MMPose: https://github.com/open-mmlab/mmpose/blob/main/LICENSE). 모델 가중치 라이선스도 Apache 2.0 호환 여부 확인. 안 맞으면 spike 중단 + belle 재검토 (Plan 09 hard rule 재발동)"
    - "ref-sideway-spin 1영상 spike — overall ≥ 70 회복 시 Plan 11 (5영상 sweep + 게이트 룰 재정의 + Wave 3 진입) 작성"
    - "spike fail (overall < 60) 면 — D-15① 4/5 통과 + sideway-spin known limitation 수용 + Wave 3 진입 (게이트 룰 재정의 path) 또는 다른 lifter checkpoint (HybrIK MIT) 시도. AlphaPose 는 라이선스 차단으로 영구 후보 제외 (.claude memory 박제됨)"
  artifacts:
    - path: "backend/research/spikes/rtmpose_to_h36m17.py"
      provides: "RTMPose COCO-17 출력 → MotionBERT H3.6M 17-joint 입력 형식 매핑. Plan 08 mediapipe_to_h36m17.py 의 derive 패턴 재사용 (hip/spine/thorax/neck_nose/head 5개 보간)"
    - path: "backend/research/spikes/spike_rtmpose.py"
      provides: "RTMPose 2D 추출 + COCO→H36M 변환 + MotionBERT lift + 점수 계산 (ref-sideway-spin 1영상). Plan 07 spike_motionbert.py 패턴 그대로 — 2D detector 만 교체"
    - path: "backend/research/spikes/README.md"
      provides: "RTMPose spike 절차 추가 (기존 Plan 07/08 MotionBERT 절차 보존, Plan 10 RTMPose 섹션 신규)"
    - path: "backend/tests/test_spike_rtmpose_to_h36m17.py"
      provides: "COCO-17 → H3.6M 17 매핑 + derive 보간 단위 테스트 (mock RTMPose 출력)"
---

<objective>
Plan 09 라이선스 차단으로 AlphaPose 대신 **RTMPose-l (Apache 2.0)** 를 채택. Plan 01-08 에서 ref-sideway-spin 만 64점 (목표 70) 으로 fail 한 측면 자세 약점을 보강하기 위해, RTMPose-l 2D 키포인트 + COCO-17→H3.6M 17 변환 + 기존 MotionBERT lifter 조합을 spike. ref-sideway-spin 1개 영상에서 overall ≥ 70 회복 여부 확인.

Purpose: Plan 08 belle 검증에서 4/5 영상 PASS 했으나 ref-sideway-spin (옆모습) 만 64점. MediaPipe avg_conf 0.85 (검출 OK) 인데도 점수 깎인 것은 — MediaPipe 가 정면 데이터 편향이라 측면 keypoint 분포 좁음 + world_landmarks z 노이즈가 누적된 것이 1차 가설. RTMPose-l 은 COCO 학습 (정면+측면+occlusion 더 균등) + score-based regression 으로 측면 자세에서도 일관된 keypoint 를 제공할 가능성. 2D 단계만 교체하면 Plan 08 production lifter (MotionBERT) 가 그대로 들어가므로 ablation 이 깨끗함.

Output: spike_rtmpose.py + RTMPose COCO-17→H3.6M 17 어댑터 + 단위 테스트 + README 업데이트 + ref-sideway-spin 1영상 결과 → belle 판정 후 Plan 11 분기 결정.
</objective>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-07-SUMMARY.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-08-SUMMARY.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-09-SUMMARY.md
@CLAUDE.md

**참조 핵심**: `01-09-SUMMARY.md` `## 측면 자세 보강 대안 후보 → Option B-1 (MMPose RTMPose)`. belle 결정 (2026-06-01) 로 Option B-1 채택 확정.

## Plan 01-08 5영상 회귀 결과 (재인용)

| 모션 | MP+MotionBERT | NLF | D-15① ≥70 |
|---|---|---|---|
| ref-climb | 85 | 58 | PASS |
| ref-foxtop-split | 75 | 62 | PASS |
| ref-foxtop | 90 | 64 | PASS |
| ref-invert | 92 | 65 | PASS |
| **ref-sideway-spin** | **64** | 81 | **FAIL** |

belle 결정 (2026-06-01): **option-b-1 RTMPose** — Apache 2.0 + 2D detector 교체 + MotionBERT lift 유지.

## RTMPose 사전 research 필요 (executor 가 T-1 에서 수행)

1. **라이선스 확인** — MMPose 전체 스택 (mmpose, mmengine, mmcv) 이 Apache 2.0 인지 (https://github.com/open-mmlab/mmpose/blob/main/LICENSE). RTMPose-l 모델 가중치도 Apache 2.0 호환 여부 (가중치는 ModelScope/OpenXLab 호스팅 — 라이선스 분리 가능).
2. **출력 형식** — RTMPose-l 은 COCO-17 출력. COCO-17 → H3.6M 17 매핑 어댑터 필요 (Plan 08 `mediapipe_to_h36m17.py` 의 5개 derive 패턴 재사용 — hip/spine/thorax/neck_nose/head).
3. **Pretrained checkpoint** — RTMPose-l (COCO + body) checkpoint URL + 가중치 크기. 권장: `rtmpose-l_8xb256-420e_coco-256x192` 또는 384x288 해상도. 가중치 ~150MB 예상.
4. **MotionBERT 입력 매핑** — MotionBERT 가 기대하는 H3.6M 17-joint 순서·정규화. Plan 08 `pose_lifters/mediapipe_to_h36m17.py::convert_mp33_to_h36m17` 와 동일 출력 형식 `(T, 17, 2)` 정규화 좌표 (또는 confidence 포함 시 `(T, 17, 3)`).
5. **Pod install 절차** — Plan 08 setup.sh 상태 위에 `pip install mmpose mmengine "mmcv>=2.0"` (정확한 버전은 T-1-4 에서 박제). checkpoint 다운로드 명령 (wget URL 또는 `mim download` CLI).

## 판정 기준 (Plan 07/09 패턴 따름)

| 결과 | overall | 다음 행동 |
|---|---|---|
| Strong pass | ≥ 70 | Plan 11 작성 (5영상 sweep + 게이트 룰 재정의 + Wave 3 진입) |
| Weak signal | 60~70 | RTMPose-x (larger) / 384x288 해상도 / 다른 checkpoint 시도. 또는 HybrIK (Plan 09 SUMMARY Option A, MIT) 로 path 전환 |
| 실패 | < 60 | 측면 보강 포기 → D-15① 4/5 수용 (게이트 룰 재정의 path, Plan 11 별경로) |
</context>

<tasks>

## Task 1 — RTMPose research + 라이선스 + Pod install 명령 박제 (T-1)

**T-1-1**: MMPose 스택 (mmpose + mmengine + mmcv) 라이선스 확인 Apache 2.0 (https://github.com/open-mmlab/mmpose/blob/main/LICENSE).
  - **HALT 조건**: GPL/AGPL/Noncommercial 발견 시 spike 코드 작성 STOP + belle 보고. Plan 09 hard rule 재발동.
**T-1-2**: RTMPose-l (COCO 데이터셋) 모델 가중치 라이선스 확인. ModelScope/OpenXLab 호스팅 시 별도 라이선스 분리 여부 확인. Apache 2.0 호환 아니면 RTMPose-m / 다른 체크포인트 검토.
**T-1-3**: RTMPose 단일 영상 추론 API research — `MMPoseInferencer` 또는 `inference_topdown` 패턴. 입력 (BGR frame array), 출력 (COCO-17 keypoints + scores) 형식 박제.
**T-1-4**: Pod install 절차 = Plan 08 setup.sh 상태 위에 추가 명령만 정리:
  - `pip install -U openmim`
  - `mim install mmengine mmcv mmdet mmpose` (정확한 버전 박제 — mmcv 2.x 와 PyTorch 2.4 호환 확인)
  - 가중치 다운로드: `mim download mmpose --config rtmpose-l_8xb256-420e_coco-256x192 --dest /workspace/rtmpose_weights/` (또는 wget 직링크)
  - PYTHONPATH / CUDA_VISIBLE_DEVICES 는 Plan 08 와 동일

## Task 2 — `rtmpose_to_h36m17.py` 어댑터 (T-2)

**T-2-1**: RTMPose COCO-17 출력 → H3.6M 17-joint 매핑 함수 작성. Plan 08 `mediapipe_to_h36m17.py::convert_mp33_to_h36m17` 패턴 재사용:

  | H3.6M idx | H3.6M joint | source |
  |---|---|---|
  | 0 | hip | derive: (COCO 11 + COCO 12) / 2 |
  | 1 | r_hip | COCO 12 |
  | 2 | r_knee | COCO 14 |
  | 3 | r_foot | COCO 16 (right ankle) |
  | 4 | l_hip | COCO 11 |
  | 5 | l_knee | COCO 13 |
  | 6 | l_foot | COCO 15 (left ankle) |
  | 7 | spine | derive: (hip[0] + thorax[8]) / 2 |
  | 8 | thorax | derive: (COCO 5 + COCO 6) / 2 |
  | 9 | neck_nose | derive: (thorax[8] + COCO 0 nose) / 2 |
  | 10 | head | COCO 0 (nose proxy) |
  | 11 | l_shoulder | COCO 5 |
  | 12 | l_elbow | COCO 7 |
  | 13 | l_wrist | COCO 9 |
  | 14 | r_shoulder | COCO 6 |
  | 15 | r_elbow | COCO 8 |
  | 16 | r_wrist | COCO 10 |

**T-2-2**: 정규화 방식 결정 — RTMPose 는 absolute pixel 좌표 출력 → `(T, 17, 2)` 형식으로 image width/height 로 정규화 ([0,1]). MotionBERT 가 기대하는 형식과 일치 확인 (Plan 08 mediapipe_to_h36m17 의 정규화 방식과 동일).
**T-2-3**: confidence 처리 — RTMPose 는 keypoint 별 score 출력. low-confidence keypoint 는 NaN 처리 후 lift 단계에서 temporal_fill 보간 (Plan 07 spike 패턴 동일). threshold 는 spike CLI args 로 노출 (기본 0.3).
**T-2-4**: 단위 테스트 (`backend/tests/test_spike_rtmpose_to_h36m17.py`):
  - mock RTMPose COCO-17 출력 `(T=10, 17, 3)` (x, y, score) → 변환 후 shape `(T=10, 17, 2)` 검증
  - 핵심 joint 직접 매핑 검증 (l_shoulder, r_knee, head 등)
  - derive 5개 joint (hip, spine, thorax, neck_nose, head) 보간 정확도 (known input → known output)
  - low-confidence (score < threshold) → NaN 처리 검증

## Task 3 — `spike_rtmpose.py` (T-3)

**T-3-1**: RTMPose-l 모델 로드 — `mmpose.apis.MMPoseInferencer` 또는 `inference_topdown` 패턴. config + checkpoint 경로는 CLI args.
**T-3-2**: 단일 영상 frame 추출 — Plan 08 `frame_extractor.py` 패턴 재사용 (9 fps, 640px downsample).
**T-3-3**: RTMPose 2D 키포인트 추출 (per-frame) → `(T, 17, 3)` (x, y, score).
**T-3-4**: `rtmpose_to_h36m17` 변환 → `(T, 17, 2)` 정규화 H3.6M.
**T-3-5**: MotionBERT lift — 기존 `pose_lifters/motionbert_lifter.py::MotionBertLifter` 재사용 (Plan 08 production code, 수정 금지).
**T-3-6**: 점수 계산 chain — Plan 07 spike 와 동일 (`compute_joint_angles` → `temporal_fill` → `absolute_dimension_scores` → `overall_from_dimensions`).
**T-3-7**: CLI args:
  - `--motion ref-sideway-spin` (기본)
  - `--bucket sunity-motion-pilot-videos`
  - `--rtmpose-config <path-or-name>` (예: `rtmpose-l_8xb256-420e_coco-256x192`)
  - `--rtmpose-checkpoint <path>` (mim download 받은 .pth)
  - `--motionbert-root /workspace/MotionBERT`
  - `--motionbert-weights /workspace/MotionBERT/checkpoint/pose3d/FT_MB_lite_MB_ft_h36m_global_lite/best_epoch.bin`
  - `--score-threshold 0.3` (low-confidence keypoint NaN 임계값)
  - `--out backend/research/spikes/reports/spike_rtmpose_$(date +%Y%m%d_%H%M).json`
**T-3-8**: 보고서 출력 (JSON + Markdown) — Plan 07 spike_motionbert.py 보고서 형식 그대로 + 추가 필드 `detector: "rtmpose-l"`, `rtmpose_config: "..."`.

## Task 4 — README 업데이트 (T-4)

**T-4-1**: `backend/research/spikes/README.md` 에 RTMPose 섹션 추가 (Plan 07/08 MotionBERT 섹션 보존 — append, 덮어쓰기 금지):
  - 라이선스 표 RTMPose 행 추가 (Apache 2.0)
  - "Plan 10 — RTMPose 측면 보강 spike" 섹션 — 배경 + 가설 + 판정 기준
  - Pod 실행 절차 — Plan 08 setup 상태에서 시작 가정 (`bash runpod_inference/setup.sh` 는 이미 실행됨)
  - 신규 install 명령 + checkpoint 다운로드 + spike 실행 명령

## Task 5 — belle Pod 실행 + 판정 (T-5) — autonomous: false

**T-5-1**: belle Pod (RTX 3090, Plan 08 setup 그대로) 에서 다음 순서로 실행:
  ```bash
  cd /workspace/SunityMotion && git pull --ff-only origin main
  pip install -U openmim
  mim install mmengine "mmcv>=2.0" mmdet mmpose  # 정확한 install 명령은 T-1-4 에서 확정
  mim download mmpose --config <rtmpose-l-config> --dest /workspace/rtmpose_weights/
  python3 -m backend.research.spikes.spike_rtmpose \
    --motion ref-sideway-spin \
    --bucket sunity-motion-pilot-videos \
    --rtmpose-config <config-name> \
    --rtmpose-checkpoint /workspace/rtmpose_weights/<file>.pth \
    --motionbert-root /workspace/MotionBERT \
    --motionbert-weights /workspace/MotionBERT/checkpoint/pose3d/FT_MB_lite_MB_ft_h36m_global_lite/best_epoch.bin \
    --out backend/research/spikes/reports/spike_rtmpose_$(date +%Y%m%d_%H%M).json
  ```
**T-5-2**: 결과 보고서 (`spike_rtmpose_*.json` + `.md`) 검토.
**T-5-3**: belle 응답:
  - **"approved, proceed to Plan 11"** — overall ≥ 70 → 5영상 sweep + 게이트 룰 재정의 + Wave 3 진입
  - **"try other checkpoint"** — 60~70 → RTMPose-x 또는 384x288 시도
  - **"try HybrIK"** — RTMPose 측면 약함 확인 시 path 전환 (Option A, MIT)
  - **"accept limitation, proceed to Plan 11 with 4/5 rule"** — sideway-spin 보강 포기, D-15① 4/5 수용

</tasks>

<success_criteria>
- [ ] MMPose / mmengine / mmcv 라이선스 Apache 2.0 확인 + RTMPose-l 가중치 라이선스 호환 확인 (출처 URL 박제)
- [ ] `rtmpose_to_h36m17.py` 어댑터 — COCO-17 → H3.6M 17 매핑 (직접 12개) + derive 5개 + confidence threshold NaN 처리
- [ ] `test_spike_rtmpose_to_h36m17.py` — shape + 핵심 joint + derive + NaN 처리 검증, mock 입력으로 mmpose 의존 없이 로컬 실행 가능
- [ ] `spike_rtmpose.py` 단일 영상 실행 가능 (CLI args 8개: motion/bucket/rtmpose-config/rtmpose-checkpoint/motionbert-root/motionbert-weights/score-threshold/out)
- [ ] README RTMPose 섹션 추가 — Plan 07/08 MotionBERT 섹션 보존, append 만
- [ ] `01-10-SUMMARY.md` 작성 (T-1~T-4 executor 결과) + belle Pod checkpoint payload (정확한 install 명령 + spike 실행 명령)
- [ ] RTMPose 가중치 (.pth) git 추적 금지 — `/workspace/rtmpose_weights/` Pod 경로 only
- [ ] No fabricated numbers — 1영상 실행은 belle Pod 에서만, executor 는 spike 코드만 작성
- [ ] No emojis
</success_criteria>

<scope_limits>
- 5영상 sweep 은 본 plan 범위 밖 (Plan 11 책임)
- Wave 3 (NLF 격리 + atomic swap) 은 Plan 11 결과 보고 진입
- 운영 코드 (`functions/pipeline/app.py`, `runpod_inference/server.py`) 수정 금지
- production `pose_lifters/` 패키지 (motionbert_lifter, mediapipe_to_h36m17) 수정 금지 — Plan 08 production code 안정성 유지
- Plan 10 spike 디렉터리 (`backend/research/spikes/`) 안에서만 신규 파일 추가. 기존 `mediapipe_to_h36m17.py` / `spike_motionbert.py` 보존 (5영상 sweep 시 비교군으로 재사용)
- HybrIK / 다중 시점 / 4/5 수용은 Plan 10 fail 시의 대안 경로 — 본 plan 에서 작업 금지
- AlphaPose 는 라이선스 차단 (Noncommercial) 로 영구 후보 제외 — 어떤 task 에서도 도입 시도 금지 (memory `license-blocklist-pose.md` 박제됨)
- mmpose / mmcv install 은 Pod 전용. Lambda 배포 금지 (Plan 08 RunPod 격리 원칙 유지)
</scope_limits>
