---
phase: 01-poseengine-mediapipe-nlf-r-d
plan: 11
type: spike
wave: 2
depends_on:
  - 01-08  # MP+MotionBERT 4/5 PASS, ref-sideway-spin fail
  - 01-10  # RTMPose-l STRONG_PASS (ref-sideway-spin 72.0)
files_modified:
  - backend/research/spikes/sweep_rtmpose.py
  - backend/research/spikes/debug_dimensions.py
  - backend/research/spikes/README.md
  - backend/tests/test_sweep_rtmpose_smoke.py
autonomous: false
gap_closure: true
parent_plan: 01-10
parent_verdict: "Plan 10 STRONG_PASS (ref-sideway-spin overall 72.0). belle approved C scope = 5영상 sweep + line/angle root cause + 게이트 룰 검토. Gemini 통합은 Phase 5 별 phase."
requirements:
  - POSE-01
  - POSE-02
must_haves:
  truths:
    - "Plan 10 = ref-sideway-spin 단일 영상 STRONG_PASS (72.0). Plan 08 다른 4영상 (ref-climb 85, ref-foxtop-split 75, ref-foxtop 90, ref-invert 92) 는 MediaPipe+MotionBERT 기준이고 RTMPose+MotionBERT 회귀 미실행 — Plan 11 5영상 sweep 으로 RTMPose 가 4영상에서도 PASS 유지 또는 회귀 발생 확인 필요"
    - "Plan 10 결과 line / angle 차원 N/A — production overall 신뢰도 결정적 약점 (PROJECT.md 핵심 블로커). FallbackRecognizer (technique.py) 가 EXTEND/BENT 프로파일 결정 못 하면 dimensions.absolute_dimension_scores 가 None 반환. 5영상 모두 N/A 인지 / 일부만 N/A 인지 박제 + root cause 정확히 분석"
    - "Gemini 기술 인식기 통합 (Phase 5) 은 본 plan scope 밖 — belle Gemini API 키 발급 중 (Parameter Store /sunity/motion/gemini-api-key SecureString 박제 완료, 2026-06-01). line/angle 완전 회복은 Phase 5 진입 후"
    - "게이트 룰 D-15① (overall ≥70) production 우선. D-14 (NLF gap ≤5) 는 Plan 10 -9 양보. Plan 11 5영상 sweep 결과로 게이트 룰 적정성 재검토 + Wave 3 진입 조건 명시"
    - "5영상 sweep PASS = Wave 3 진입 게이트 = Plan 04 (NLF R&D 격리) + Plan 05 (atomic swap) 진입 조건. all PASS (5/5) 또는 4/5 + sideway-spin known limitation 수용 시 진입"
  artifacts:
    - path: "backend/research/spikes/sweep_rtmpose.py"
      provides: "5영상 batch sweep harness — Plan 10 spike_rtmpose 단일 영상 logic 을 list iteration 으로 wrapping. RTMPose+MB vs NLF baseline 비교. 영상당 overall / stability / line / angle / avg_score / ms_per_frame + 5영상 종합표"
    - path: "backend/research/spikes/debug_dimensions.py"
      provides: "line / angle N/A root cause 박제 스크립트 — dimensions.absolute_dimension_scores trace + FallbackRecognizer (technique.FallbackRecognizer) EXTEND/BENT 프로파일 출력 dump. 영상별로 어떤 관절이 None 인지 / 어떤 조건에서 N/A 인지 명시"
    - path: "backend/research/spikes/README.md"
      provides: "Plan 11 sweep 절차 + debug 절차 append (Plan 07/08/10 섹션 보존)"
    - path: "backend/tests/test_sweep_rtmpose_smoke.py"
      provides: "sweep_rtmpose CLI 파싱 + 결과 JSON 형식 + 5영상 list iteration smoke 테스트 (mock spike_rtmpose 결과, mmpose 의존 없이 로컬)"
---

<objective>
Plan 10 RTMPose-l STRONG_PASS (ref-sideway-spin 72.0) 후속 — 정은지 5영상 전체에 RTMPose+MotionBERT 회귀 + line/angle N/A 차원 root cause 박제 + 게이트 룰 검토 + Wave 3 진입 게이트 조건 명시. Plan 04/05 (NLF R&D 격리 + atomic swap) 진입 조건 확정.

Purpose: Plan 10 은 단일 영상 spike — 측면 자세 보강 가능성 검증 완료. Plan 11 = 5영상 sweep 으로 production 진입 신뢰도 확보 + line/angle N/A 정확한 원인 (FallbackRecognizer 한계 패턴) 박제 + 게이트 룰 적정성 재검토. line/angle 완전 회복은 Phase 5 Gemini 기술 인식기 통합 시점이지만, 본 plan 에서는 Wave 2 마무리 + Wave 3 진입 가능 여부 확정.

Output: sweep_rtmpose.py (5영상 batch) + debug_dimensions.py (root cause trace) + 5영상 결과 보고서 + Wave 3 진입 게이트 verdict.
</objective>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-08-SUMMARY.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-10-SUMMARY.md
@backend/research/spikes/spike_rtmpose.py
@backend/shared/python/sunity_shared/analysis/dimensions.py
@backend/shared/python/sunity_shared/analysis/technique.py
@CLAUDE.md

## Plan 10 결과 (재인용)

| 항목 | RTMPose+MB (ref-sideway-spin) | NLF | 갭 | 게이트 |
|---|---|---|---|---|
| overall | 72.0 | 81.0 | -9.0 | D-15① PASS (≥70) |
| stability | 72.0 | 81.0 | -9.0 | — |
| line | N/A | N/A | N/A | **본 plan T-2 root cause** |
| angle | N/A | N/A | N/A | **본 plan T-2 root cause** |
| ms/frame | 37 | 665 | — | 18x faster |
| avg_rtm_score | 0.4382 | — | — | D-15③ 0.5 약간 미만 |

## Plan 08 5영상 결과 (MediaPipe+MotionBERT 기준 — RTMPose+MB 비교군)

| 모션 | MP+MotionBERT | NLF | D-15① ≥70 |
|---|---|---|---|
| ref-climb | 85 | 58 | PASS |
| ref-foxtop-split | 75 | 62 | PASS |
| ref-foxtop | 90 | 64 | PASS |
| ref-invert | 92 | 65 | PASS |
| ref-sideway-spin | 64 | 81 | FAIL → Plan 10 RTMPose 로 72 회복 |

Plan 11 5영상 sweep 은 RTMPose+MB 로 위 표를 재실행. 기대:
- ref-sideway-spin: 72 (Plan 10 검증)
- 나머지 4영상: 70 이상 유지 또는 회귀 (RTMPose 가 측면 강하지만 정면 자세에서 MediaPipe 보다 약할 가능성)

## line / angle N/A 원인 가설

dimensions.absolute_dimension_scores 가 N/A 반환하는 조건:
1. FallbackRecognizer (technique.py) 가 EXTEND/BENT 프로파일 결정 못 함 — TechniqueProfile 의 expected_extend / expected_bent 가 empty → line 채점 대상 0
2. compute_joint_angles 결과가 충분한 관절에서 NaN — temporal_fill 보간 후에도 NaN 남으면 angle 계산 fail
3. score_threshold 0.3 컷에서 너무 많은 keypoint NaN → 시간축 전체에서 의미있는 데이터 부족

본 plan T-2 에서 정확한 trace 로 박제.

## 판정 기준 (Wave 3 진입 게이트)

| 결과 | 5영상 PASS 수 | line/angle 회복 | 다음 행동 |
|---|---|---|---|
| Strong pass | 5/5 (overall ≥70) | line 또는 angle 일부 영상 회복 | "approved, proceed to Wave 3" → Plan 04/05 진입 |
| Conditional pass | 4/5 | line/angle 모두 N/A | "accept limitation, proceed to Wave 3 with known gap" → Plan 04/05 진입 + Phase 5 (Gemini) 우선순위 명시 |
| Regression | 3/5 이하 | — | "regression in RTMPose, evaluate path" → Plan 12 추가 검토 (HybrIK 또는 MediaPipe+MotionBERT 유지) |
| line/angle 완전 N/A 가 production 차단 | — | 모두 N/A | "Wave 3 진입 보류, Phase 5 Gemini 통합 선행" → Plan 04/05 차단, Phase 5 진입 |
</context>

<tasks>

## Task 1 — `sweep_rtmpose.py` 5영상 batch sweep (T-1)

**T-1-1**: Plan 10 `spike_rtmpose.py::run_spike` 를 5영상 list iteration 으로 wrapping. CLI: `--motions ref-climb ref-foxtop-split ref-foxtop ref-invert ref-sideway-spin` (Plan 08 5영상 그대로).
**T-1-2**: 영상별 결과 dict 수집 — `{motion: {overall, stability, line, angle, avg_score, ms_per_frame}}`. NLF baseline 도 함께 (Plan 08 패턴 따라 — compare_engines 와 일관성).
**T-1-3**: 5영상 종합표 출력 (Plan 08 SUMMARY 표 패턴 그대로):
  ```
  | 모션 | RTMPose+MB | NLF | D-15① ≥70 | line | angle |
  ```
**T-1-4**: 보고서 dump (JSON + Markdown). Markdown 에 게이트 룰 verdict 자동 계산 (5/5, 4/5, 3/5 분류).
**T-1-5**: CLI args:
  - `--motions <list>` (default = Plan 08 5영상)
  - `--bucket sunity-motion-pilot-videos`
  - `--rtmpose-config <path>` / `--rtmpose-checkpoint <path>` (Plan 10 과 동일)
  - `--motionbert-root /workspace/MotionBERT` / `--motionbert-weights <path>`
  - `--score-threshold 0.3`
  - `--det-model none` (single-person 우회 default — Plan 10 detector 우회 패치 활용)
  - `--out backend/research/spikes/reports/sweep_rtmpose_$(date +%Y%m%d_%H%M).json`

## Task 2 — `debug_dimensions.py` line / angle N/A root cause 박제 (T-2)

**T-2-1**: Plan 10 sweep 결과의 영상별 raw 데이터 (frames + RTMPose keypoints + MotionBERT 3D + FallbackRecognizer 출력) 를 그대로 받아서 dimensions 계산 단계별 trace 출력.
**T-2-2**: FallbackRecognizer.recognize() 의 TechniqueProfile 출력 dump — `expected_extend`, `expected_bent`, `applicable_joints`. 5영상별로 어떤 관절이 비어있는지 확인.
**T-2-3**: dimensions.absolute_dimension_scores 의 line / angle 차원 계산 trace:
  - `line_score` 가 None 반환하는 조건 (expected_extend empty? 또는 compute_joint_angles NaN?)
  - `angle_score` 가 None 반환하는 조건
**T-2-4**: 영상별 root cause table 출력:
  ```
  | 모션 | line N/A 원인 | angle N/A 원인 | FallbackRecognizer profile |
  ```
**T-2-5**: 가능한 fix 후보 박제 (Phase 5 Gemini 진입 전 임시 회복):
  - score_threshold 조정 (0.3 → 0.2) 시 line/angle 회복 가능성
  - FallbackRecognizer 의 default profile 확장 (모든 관절 EXTEND/BENT 가정)
  - Plan 11 에서 즉시 적용은 X — 가능성만 박제, 실제 변경은 belle 결정 후

## Task 3 — 게이트 룰 검토 (T-3)

**T-3-1**: D-14 (NLF gap ≤5) 적정성 재검토 — Plan 10 -9 양보 가능했고 production 진입에 결정적 아님. 5영상 sweep 결과로 D-14 가 모든 영상에서 -10 이상이면 보조 게이트로 강등, D-15 우선.
**T-3-2**: D-15① (overall ≥70) threshold 적정 — Plan 06/07/08/10 모두 이 기준으로 검증됨. 변경 없음 권장.
**T-3-3**: D-15② (Top-3 failures overlap ≥2/3 vs NLF) — Plan 11 sweep 결과로 RTMPose+MB 와 NLF top-3 failure 가 얼마나 겹치는지 확인.
**T-3-4**: D-15③ (avg_confidence ≥0.5) — Plan 10 ref-sideway-spin 0.4382 약간 미달. 5영상 sweep 결과로 평균 0.5 이상이면 통과, 영상별로 0.4 미만 영상은 known limitation 박제.
**T-3-5**: 5/5 PASS 기준 (production threshold) vs 4/5 PASS 기준 (Plan 08 belle 결정 path) — Plan 11 결과로 5/5 이면 5/5 유지, 4/5 면 D-15① 80% threshold 재정의 검토.

## Task 4 — Wave 3 진입 게이트 조건 명시 (T-4)

**T-4-1**: Plan 04 (NLF R&D 격리) 진입 조건 박제:
  - 5영상 sweep PASS (5/5 또는 4/5)
  - line/angle N/A 가 Wave 3 차단 사유인지 결정 (수용 vs 차단)
  - Plan 11 SUMMARY 에 진입 조건 명시 → Plan 04 execute 시 게이트 확인
**T-4-2**: Plan 05 (atomic swap) 진입 조건 박제 — Plan 04 완료 후 자동 진입 (depends_on: 01-04, 01-06)
**T-4-3**: Phase 5 (Gemini 기술 인식기) 진입 조건 명시:
  - belle Gemini API 키 발급 완료 ✓ (Parameter Store /sunity/motion/gemini-api-key SecureString, 2026-06-01)
  - Lambda env wiring + RunPod env wiring 필요 (Phase 5 시작 시점)
  - line/angle N/A 가 5영상 sweep 에서도 모두 N/A 면 Phase 5 우선순위 ↑
**T-4-4**: Phase 1 완료 조건 — Plan 11 PASS + Plan 04 (NLF 격리) + Plan 05 (atomic swap) 완료. Phase 2+ 진입.

## Task 5 — belle Pod 실행 + 5영상 판정 (T-5) — autonomous: false

**T-5-1**: belle Pod (RTX 3090, Plan 10 환경 유지 — mmpose / numpy / detector 우회 모두 설정됨) 에서 sweep 실행:
  ```bash
  cd /workspace/SunityMotion && git pull --ff-only origin main
  python3 -m backend.research.spikes.sweep_rtmpose \
    --motions ref-climb ref-foxtop-split ref-foxtop ref-invert ref-sideway-spin \
    --bucket sunity-motion-pilot-videos \
    --rtmpose-config /workspace/rtmpose_weights/rtmpose-l_8xb256-420e_coco-256x192.py \
    --rtmpose-checkpoint /workspace/rtmpose_weights/rtmpose-l_simcc-coco_pt-aic-coco_420e-256x192-1352a4d2_20230127.pth \
    --motionbert-root /workspace/MotionBERT \
    --motionbert-weights /workspace/MotionBERT/checkpoint/pose3d/FT_MB_lite_MB_ft_h36m_global_lite/best_epoch.bin \
    --score-threshold 0.3 \
    --det-model none \
    --out backend/research/spikes/reports/sweep_rtmpose_$(date +%Y%m%d_%H%M).json
  ```
  예상 소요: 5영상 × ~2분/영상 = ~10분.
**T-5-2**: 결과 .md 검토.
**T-5-3**: belle 응답:
  - **"approved, proceed to Wave 3"** — 5/5 (overall ≥70) → Plan 04 / Plan 05 진입
  - **"accept limitation, proceed to Wave 3 with known gap"** — 4/5 + line/angle 모두 N/A → Plan 04/05 진입 + Phase 5 우선순위 ↑
  - **"regression in RTMPose, evaluate path"** — 3/5 이하 → Plan 12 추가 검토 (HybrIK 또는 MediaPipe+MotionBERT 유지 path)
  - **"Wave 3 보류, Phase 5 선행"** — line/angle N/A 가 Wave 3 차단 사유로 판단되면 Phase 5 Gemini 통합 우선

</tasks>

<success_criteria>
- [ ] `sweep_rtmpose.py` 5영상 batch 실행 가능 (CLI args 9개, single-person 우회 default)
- [ ] `debug_dimensions.py` line/angle N/A root cause trace 출력 — FallbackRecognizer profile + dimensions 단계별 None 발생 지점 박제
- [ ] `test_sweep_rtmpose_smoke.py` — CLI 파싱 + 결과 JSON 형식 + 5영상 list iteration smoke (mmpose 의존 없이 로컬)
- [ ] README Plan 11 섹션 append (sweep 절차 + debug 절차, Plan 07/08/10 섹션 보존)
- [ ] `01-11-SUMMARY.md` 작성 — 5영상 결과 표 + line/angle root cause + 게이트 룰 verdict + Wave 3 진입 조건
- [ ] belle Pod 5영상 sweep 결과 보고서 dump (json + md)
- [ ] No fabricated numbers — 5영상 실행은 belle Pod 에서만
- [ ] No emojis
</success_criteria>

<scope_limits>
- Wave 3 (NLF 격리 + atomic swap) 실 진입은 Plan 04 / Plan 05 책임 — 본 plan 은 게이트 조건 명시만
- Gemini 기술 인식기 (Phase 5) 통합은 본 plan 범위 밖 — line/angle 완전 회복은 Phase 5 진입 후. 본 plan 은 root cause 박제 + fix 가능성만 검토
- 운영 코드 (`functions/`, `runpod_inference/`, `shared/pose_lifters/`) 수정 금지 — Plan 08 / Plan 10 production 안정성 유지
- spike 디렉터리 (`backend/research/spikes/`) 안에서만 신규 파일 추가. Plan 07/08/10 기존 파일 (mediapipe_to_h36m17, spike_motionbert, rtmpose_to_h36m17, spike_rtmpose) 보존
- AlphaPose 는 라이선스 차단 (Noncommercial) 로 영구 후보 제외 — 어떤 task 에서도 도입 시도 금지 (memory `license-blocklist-pose.md`)
- 다중 시점 / HybrIK 은 Plan 11 fail 시의 대안 경로 — 본 plan 에서 작업 금지
- mmpose / numpy 추가 install 금지 — Plan 10 환경 그대로 (mmpose 1.3.2 + numpy 1.26.4 + detector 우회)
- FallbackRecognizer 코드 수정 금지 — root cause 박제만, 실제 fix 는 Phase 5 (Gemini 어댑터) 진입 시
</scope_limits>
