---
phase: 01-poseengine-mediapipe-nlf-r-d
plan: 13
type: spike
wave: 2
depends_on:
  - 01-11  # gap_too_wide_blocked sweep verdict — line/angle 5/5 N/A 회복 path 1차 진입
  - 01-12  # 5가설 final verdict (4 strong, b weak) — dominant (a) frame-mean + (e) 3D 분포. Plan 13 path 권고 박제
  - 01-15  # GeometricCriterion frozen dataclass + load_grouped_criteria + 5영상 1차 박제 (commit 861fb3a / 4264001)
files_modified:
  # 신규 — Gemini 어댑터 + moment-list dimensions + spike CLI + 테스트 + SUMMARY
  - backend/shared/python/sunity_shared/judging/gemini_moment_extractor.py
  - backend/shared/python/sunity_shared/judging/__init__.py     # gemini_moment_extractor 재노출 1줄만 추가
  - backend/shared/python/sunity_shared/judging/moment_dimensions.py
  - backend/research/spikes/spike_gemini_moment.py
  - backend/research/spikes/README.md                            # Plan 13 섹션 append (Plan 07/08/10/11/12 보존)
  - backend/tests/test_gemini_moment_extractor.py
  - backend/tests/test_moment_dimensions.py
  - backend/tests/test_spike_gemini_moment_smoke.py
  - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-13-SUMMARY.md
autonomous: false
parent_plan: 01-12
parent_verdict: "Plan 12 final verdict (2026-06-01) — 5가설 4 strong / 1 weak. dominant = (a) frame-mean (45-52° disagree) + (e) 두 엔진 3D 분포 (root-relative 220+). Plan 13 path = standard + nlf_re-spike (별 plan) + rtmpose_to_h36m17_correction (별 plan) + multi_engine_averaging (별 plan). 본 plan = standard 만 (Gemini key moment + IPSF criteria lookup)."
requirements:
  - POSE-01  # Phase 1 핵심 — Success Criteria #7 (IPSF tolerance + line/angle PASS) 달성 1단계
  - SCORE-01 # Phase 5 책임이지만 본 plan 의 Gemini key moment spike 가 SCORE-01 "Gemini 는 분류·자연어 번역만 (좌표·판단·점수 출력 금지)" 정책 준수 1차 검증
must_haves:
  truths:
    - "Plan 12 verdict 적재 (2026-06-01) — 4/5 strong, dominant (a) frame-mean + (e) 두 엔진 3D 분포 차이. (b) RTMPose headdown weak (dominant 아님). 본 plan = 권고 path 중 standard 만 (Gemini key moment + IPSF criteria lookup). NLF re-spike / rtmpose_to_h36m17 derive 보정 / multi_engine_averaging 은 별 plan."
    - "입력 데이터 = Plan 15 GeometricCriterion (commit 861fb3a/4264001 1차 박제, status=pending_belle_labeling 갱신 대기). ref-invert hold_moment left/right_shoulder + left/right_hip + right_knee 5 entries 채워짐. moment_key enum = sunity_shared.judging.geometric_criterion.VALID_MOMENT_KEYS (setup/hold/peak/release) 고정."
    - "Gemini 응답에 좌표·점수·판단 출력 금지 (SCORE-01 + D-05 + memory `analysis-objectivity-no-human-scores.md` 박제). 응답 = moment_key + frame_idx + 자연어 근거(rationale) 3 필드만. 좌표/점수/판단 키워드 정규식 차단 가드 검증."
    - "moment-list 기반 dimensions sampling — 운영 dimensions.py / technique.py / FallbackRecognizer 무수정. 신규 모듈 `judging.moment_dimensions` 가 측정 각도 (T, J=8) + GeometricCriterion + moment frame_idx 입력으로 measured_angle / gap / pass_tolerance / fail_minimum / deduction 산출. 기존 frame-mean 경로 (hold_window + line_score) 보존."
    - "8 angle joints (skeleton.JOINT_KEYS) 만 measured_angle 추출 — derive joint (hip/spine/thorax/neck_nose/head) 미사용. Plan 12 (d) strong (rtmpose_to_h36m17 H36M ordering vs NLF COCO ordering 17/17 다름) 회피. derive joint 보정 spike 는 별 plan."
    - "NLF 호출 0 — R&D 격리 유지 (D-06 backend/research/pose_engines/nlf/ 격리, D-07 sunity_shared 밖). 본 plan 의 측정 엔진 = Plan 10/11 RTMPose+MotionBERT path 만 (spike_rtmpose._run_rtmpose_2d / _load_motionbert / _run_motionbert_inference / _h36m17_to_coco17_subset 호출, spike_rtmpose 무수정)."
    - "Phase 5 정식 Gemini 통합 X — 본 plan = spike. 운영 코드 (functions / pipeline / runpod_inference / shared/analysis/recognizers) 무수정. Phase 5 가 정식 GeminiTechniqueRecognizer 어댑터 + FallbackRecognizer 합성 + Cerebras 코칭 통합 책임."
    - "Pod 환경 보존 (STATE.md 2026-06-01 22:00 박제) — torch 2.4.1+cu124 / numpy 1.26.4 / mmpose 1.3.2 / mmcv 2.1.0. Gemini SDK 추가 install 최소화 — stdlib `urllib.request` + JSON 응답 파싱으로 외부 의존성 0 (어쩔 수 없는 경우만 google-generativeai 추가). 추가 install 시 backend/requirements-dev.txt 에만 (Lambda 런타임 미사용)."
    - "Gemini API 키 = AWS Parameter Store `/sunity/motion/gemini-api-key` (SecureString, STATE.md 박제 2026-06-01). Pod env 주입 = T-1 책임. `.env` 하드코딩 영구 금지 (CLAUDE.md §3 + memory `aws-keys-and-bucket`)."
    - "5영상 batch sweep 미실행 — Plan 14 책임. 본 plan 의 live mode (T-6) 는 1~2영상 시범 (ref-invert 우선, Plan 15 1차 박제 5 entries 보유) → 결과 적재 → Plan 14 5영상 sweep 진입 게이트."
    - "Phase 1 Success Criteria #7 (ROADMAP:66) baseline 변경 박제 — 측정 각도 ↔ IPSF GeometricCriterion `angle_target` 갭 ≤ `tolerance_full` (기본 ±20°). 본 plan = 이 baseline 산출 함수 + 단일 영상 검증. Plan 14 = 5영상 sweep + line/angle 5/5 PASS 게이트."
  artifacts:
    - path: "backend/shared/python/sunity_shared/judging/gemini_moment_extractor.py"
      provides: "GeminiMomentExtractor 어댑터 (Phase 5 정식 아님, spike 레벨). Parameter Store fetch + Gemini 2.5 Pro multimodal video API 호출 + 응답 스키마 검증 (moment_key ∈ VALID_MOMENT_KEYS, frame_idx int ≥ 0, rationale str non-empty) + 좌표/점수/판단 키워드 정규식 차단 가드. KeyMoment frozen dataclass (motion / moment_key / frame_idx / rationale / source='gemini-2.5-pro')."
    - path: "backend/shared/python/sunity_shared/judging/moment_dimensions.py"
      provides: "moment-list 기반 dimensions sampling — measure_moment_angles(angles_tj, key_moments, window) → dict[moment_key, dict[joint_key, measured_angle_deg]]. compute_criteria_gaps(measured, grouped_criteria) → list[MomentGapResult] (motion / moment_key / joint_key / measured / angle_target / gap / tolerance_full / pass_tolerance: bool / fail_minimum: bool / deduction). 운영 dimensions.py 무수정 — 신규 함수 only."
    - path: "backend/research/spikes/spike_gemini_moment.py"
      provides: "단일 영상 spike CLI — S3 영상 다운로드 + spike_rtmpose 경로로 (T, J=8) angles 산출 + GeminiMomentExtractor 호출 + Plan 15 load_grouped_criteria 입력 + compute_criteria_gaps. 출력 = backend/research/spikes/reports/spike_gemini_moment_<UTC>.json + sibling .md. report-only mode (Gemini stub 응답 fixture) + live mode (Pod / Gemini API)."
    - path: "backend/tests/test_gemini_moment_extractor.py"
      provides: "응답 스키마 검증 + 좌표/점수/판단 키워드 가드 정규식 + Parameter Store fetch mock + VALID_MOMENT_KEYS enum 위반 차단. mmpose/torch/NLF/Gemini import 0 (로컬 실행, stub HTTP)."
    - path: "backend/tests/test_moment_dimensions.py"
      provides: "measure_moment_angles window 산출 정확성 + compute_criteria_gaps (pass_tolerance / fail_minimum / deduction 식) 단위 테스트. ref-invert hold_moment 5 entries fixture (Plan 15 1차 박제 그대로) 입력 → measured 각도 vs angle_target 갭 계산 결과 박제."
    - path: "backend/tests/test_spike_gemini_moment_smoke.py"
      provides: "CLI parse + report-only mode JSON shape + Gemini stub 응답 → criteria gap 산출 end-to-end smoke. Pod 미진입 (live mode 는 T-6 belle 책임)."
    - path: ".planning/phases/01-poseengine-mediapipe-nlf-r-d/01-13-SUMMARY.md"
      provides: "T-1 ~ T-5 executor 완료 박제 + T-6 belle Pod live mode 대기 status. Plan 14 진입 게이트 박제 (IPSF tolerance baseline). Plan 15 status `pending_belle_labeling` → `partial_labeled` 갱신 가능 (ref-invert 5 entries 확인)."
  key_links:
    - from: "backend/research/spikes/spike_gemini_moment.py"
      to: "backend/research/spikes/spike_rtmpose.py"
      via: "함수 import — _run_rtmpose_2d / _load_motionbert / _run_motionbert_inference / _h36m17_to_coco17_subset. spike_rtmpose 무수정 호출만."
      pattern: "from backend.research.spikes.spike_rtmpose import"
    - from: "backend/research/spikes/spike_gemini_moment.py"
      to: "backend/shared/python/sunity_shared/judging/loader.py::load_grouped_criteria"
      via: "Plan 15 데이터 입력 — load_grouped_criteria(motion) → dict[moment_key, list[GeometricCriterion]]"
      pattern: "load_grouped_criteria"
    - from: "backend/shared/python/sunity_shared/judging/gemini_moment_extractor.py"
      to: "AWS Parameter Store /sunity/motion/gemini-api-key"
      via: "boto3 ssm get_parameter(Name=..., WithDecryption=True). Pod env 주입 fallback (env GEMINI_API_KEY 사용 가능)."
      pattern: "ssm.get_parameter|GEMINI_API_KEY"
---

<objective>
**Plan 12 final verdict 후속 — dominant (a) frame-mean 한계 + (e) 두 엔진 3D 분포 차이 회복 1차 path.** Plan 11 5영상 sweep `gap_too_wide_blocked` (line/angle 5/5 N/A + |gap| 평균 14점) 의 line/angle 차원 복구 + IPSF tolerance baseline 산출 함수 도입.

Purpose: Phase 1 Success Criteria #7 (ROADMAP:66) — 5영상 sweep 재실행 시 (a) IPSF tolerance 안 + (b) line/angle 5/5 PASS — 두 게이트 모두 통과해야 Wave 3 (Plan 04/05) 진입. baseline = IPSF Code of Points 객관 임계값 (Plan 12 (c) strong → NLF 갭 baseline 영구 폐기 + Plan 15 GeometricCriterion 데이터 도입). 본 plan = key moment 시점 추출 (Gemini 2.5 Pro multimodal) → 시점의 측정 각도 추출 → Plan 15 IPSF target 과 갭 계산.

Output: spike 레벨 GeminiMomentExtractor + moment-list dimensions + 단일 영상 CLI + Pod live mode 결과 1~2영상. Plan 14 가 본 plan 결과 적용 후 5영상 batch sweep 실행.

본 plan 의 결과는 Plan 14 (5영상 재검증 sweep) 의 IPSF tolerance baseline 측정 entry point — Wave 3 (Plan 04 / Plan 05) 진입 결정 게이트.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-11-SUMMARY.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-12-SUMMARY.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-15-SUMMARY.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-15-PLAN.md
@backend/shared/python/sunity_shared/judging/geometric_criterion.py
@backend/shared/python/sunity_shared/judging/loader.py
@backend/judging_data/criteria/ref-invert.yaml
@backend/research/spikes/spike_rtmpose.py
@backend/research/spikes/sweep_rtmpose.py
@backend/shared/python/sunity_shared/analysis/dimensions.py
@backend/shared/python/sunity_shared/analysis/skeleton.py
@docs/research/폴스포츠-지식.md
@CLAUDE.md

## Plan 12 verdict 적재 (2026-06-01) — 본 plan path 결정 근거

| 가설 | report-only | live | final | dominant? |
|---|---|---|---|---|
| (a) frame-mean 한계 (45-52° disagree) | inconclusive | **strong** | **strong** | **yes** |
| (b) RTMPose headdown 약점 | inconclusive | weak | **weak** | no |
| (c) NLF baseline 편차 (span 23, var 60.56) | strong | strong | **strong** | (→ Plan 15 baseline 교체) |
| (d) keypoint chain 17/17 다름 | strong | strong | **strong** | (→ derive joint 회피) |
| (e) 두 엔진 3D 분포 (root-rel 220+) | inconclusive | **strong** | **strong** | **yes** |

본 plan path = `standard` 만 (Plan 12 권고 4개 중 1개). nlf_re-spike / rtmpose_to_h36m17_correction / multi_engine_averaging 은 별 plan (Plan 14 통과 미달 시 후속).

## Plan 15 데이터 입력 (commit 861fb3a / 4264001)

ref-invert hold_moment 5 entries 1차 박제 완료:

| joint | angle_target | tolerance_full | deduction_per_step | minimum_requirement |
|---|---|---|---|---|
| left_shoulder | 180° | 20° | 0.2 | 160° |
| right_shoulder | 180° | 20° | 0.2 | 160° |
| left_hip | 180° | 20° | 0.2 | 160° |
| right_hip | 180° | 20° | 0.2 | 160° |
| right_knee | 180° | 20° | 0.2 | 160° |

다른 4영상 (ref-climb / ref-foxtop-split / ref-foxtop / ref-sideway-spin) 은 빈 템플릿 — belle 후속 라벨링 (Plan 15 T-5). 본 plan 의 live mode 는 ref-invert 우선 시범. Plan 14 5영상 sweep 진입 전 belle 4영상 추가 라벨링 필요.

## Pod 환경 (STATE.md 2026-06-01 22:00 박제) — 추가 install 최소화

| 항목 | 상태 |
|---|---|
| GPU / Container | RTX 3090 / RunPod PyTorch 2.4 / Python 3.11 |
| torch / numpy | 2.4.1+cu124 / 1.26.4 |
| mmcv / mmengine / mmdet / mmpose | 2.1.0 / 0.10.7 / 3.3.0 / 1.3.2 |
| MotionBERT | /workspace/MotionBERT/ + best_epoch.bin |
| RTMPose-l weights | /workspace/rtmpose_weights/ |
| AWS 자격증명 | env 박제 (Plan 08 이래 유지) |
| Gemini API 키 | Parameter Store `/sunity/motion/gemini-api-key` (SecureString, 2026-06-01 박제) — Pod env 주입은 T-1 책임 |

## 본 plan 이 풀지 않는 것 (스코프 외)

- NLF 호출 (R&D 격리 유지 — D-06 / D-07). Plan 12 verdict 이후 NLF 갭 baseline 영구 폐기.
- Phase 5 정식 Gemini 통합 (운영 코드 functions / pipeline / runpod_inference / shared/analysis/recognizers/gemini_recognizer.py 본 plan 무수정).
- multi_engine_averaging — Plan 12 (e) strong 후속 spike, 별 plan.
- rtmpose_to_h36m17 derive joint 보정 — Plan 12 (d) strong 후속, 별 plan. 본 plan 은 8 angle joints (JOINT_KEYS) 만 사용해 회피.
- 5영상 batch sweep — Plan 14 책임.
- belle 추가 IPSF 라벨링 (4영상) — Plan 15 T-5 책임.
- HybrIK 비교군 — Plan 12 (b) weak verdict, 시급성 낮음.
- 사람 점수 라벨링 영구 금지 (어떤 형식이든) — memory `analysis-objectivity-no-human-scores.md`.
</context>

<scope_limits>
- 운영 코드 0줄 수정 — functions/* / runpod_inference/* / shared/analysis/dimensions.py / shared/analysis/technique.py / shared/analysis/FallbackRecognizer / shared/analysis/pose_estimator.py / shared/analysis/pose_lifters/ 무수정. spike_gemini_moment 의 measure 함수가 dimensions.py 의 frame-mean 경로를 호출하지 않고 신규 moment_dimensions 모듈만 사용.
- 기존 spike 0줄 수정 — spike_rtmpose.py / sweep_rtmpose.py / debug_dimensions.py / debug_gap_root_cause.py / rtmpose_to_h36m17.py / mediapipe_to_h36m17.py 무수정. 본 plan 의 spike_gemini_moment 는 spike_rtmpose 의 함수만 import 호출.
- Plan 15 judging 모듈 (geometric_criterion.py / loader.py / __init__.py 본문) 0줄 수정. __init__.py 는 gemini_moment_extractor + moment_dimensions 재노출 1~2줄만 추가 가능 (append-only, 기존 export 보존).
- NLF / SMPL-X import 0 (R&D 격리, D-06/D-07).
- 라이선스 = Gemini 2.5 Pro API only (Google AI Studio, belle 본인 계정 키). 다른 LLM 제공자 도입 금지.
- Lambda function 별 requirements.txt 무수정 — Gemini SDK 추가 시 backend/requirements-dev.txt 에만 (Lambda 런타임 미배포). 가능하면 stdlib `urllib.request` 로 외부 의존성 0.
- 시크릿 = AWS Parameter Store 만 (CLAUDE.md §3). `.env` / 하드코딩 / 로그 출력 영구 금지.
- 사람 점수 라벨링 (belle / 강사 / 심사자) ground truth 영구 금지. Gemini 응답에 점수 / 좌표 / 판단 출력 금지.
- 이모지 금지 / 슬롭 코드 금지 (CLAUDE.md §7).
- 8 angle joints (JOINT_KEYS) 만 measured 추출 — derive joint (hip/spine/thorax/neck_nose/head) 미사용 (Plan 12 (d) 회피).
- 사람 점수 또는 NLF 값 ground truth path 영구 금지.
- mmpose / torch / Gemini SDK 추가 install 최소화. 추가 시 backend/requirements-dev.txt 에만.
</scope_limits>

<tasks>

<task type="auto">
  <name>Task 1: GeminiMomentExtractor 어댑터 — Parameter Store + 응답 스키마 가드</name>
  <files>backend/shared/python/sunity_shared/judging/gemini_moment_extractor.py, backend/shared/python/sunity_shared/judging/__init__.py</files>
  <read_first>
    - backend/shared/python/sunity_shared/judging/geometric_criterion.py (VALID_MOMENT_KEYS = (setup, hold, peak, release) 재사용)
    - backend/shared/python/sunity_shared/judging/__init__.py (기존 재노출 보존)
    - backend/shared/python/sunity_shared/firestore_admin.py (Parameter Store fetch 패턴 참조 — boto3 ssm.get_parameter WithDecryption=True)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md (D-05 confidence 정책, 좌표·점수·판단 금지)
    - .planning/STATE.md (Gemini API 키 Parameter Store path /sunity/motion/gemini-api-key 박제)
    - CLAUDE.md §3 (시크릿 정책 — `.env` 하드코딩 금지)
  </read_first>
  <action>
KeyMoment frozen dataclass — motion: str, moment_key: str, frame_idx: int, rationale: str, source: str (default "gemini-2.5-pro"). validate() 메서드 6항목 가드 — moment_key ∈ VALID_MOMENT_KEYS (geometric_criterion 모듈에서 import), frame_idx >= 0 정수, rationale.strip() 비어있지 않음, source 비어있지 않음, motion 비어있지 않음, rationale 에 좌표/점수/판단 키워드 정규식 차단 (`r"(?i)\b(x|y|z|score|점수|coordinate|좌표|판단|verdict|pass|fail|deduction|감점|degree|도|°|180|160|140|120)\b"` 일치 시 ValueError "Gemini rationale 에 좌표/점수/판단 키워드 포함 — SCORE-01 위반").

GeminiMomentExtractor 클래스 — 생성자에서 Parameter Store path (`/sunity/motion/gemini-api-key`) + env fallback (`GEMINI_API_KEY`) 둘 다 시도. 우선순위 = env > Parameter Store (Pod 운영 편의). 키 미발견 시 ImportError 아닌 RuntimeError ("Gemini API 키 미설정 — Parameter Store /sunity/motion/gemini-api-key 또는 env GEMINI_API_KEY 필요. CLAUDE.md §3 — `.env` 하드코딩 금지"). __init__ 에서 boto3.client('ssm') lazy import (모듈 로드 시점 boto3 import 0).

extract_moments(video_uri: str, motion: str, expected_moment_keys: tuple[str, ...] = VALID_MOMENT_KEYS) -> list[KeyMoment] 메서드 — Gemini 2.5 Pro multimodal video API 호출. HTTP 경로 = stdlib `urllib.request` 우선 시도, Gemini SDK (`google.generativeai`) 미설치 시 stdlib path 만 사용. 프롬프트 핵심:
  - 입력 = S3 영상 URI (Gemini 가 직접 fetch 못 하면 presigned URL 또는 base64 frames 전송 — T-1-3 helper 로 분리)
  - 출력 강제 JSON 스키마 = `{"moments": [{"moment_key": "setup|hold|peak|release", "frame_idx": int, "rationale": str}]}` — 다른 키 출력 시 무시.
  - 프롬프트에 "좌표/점수/판단/도/각도 수치/PASS/FAIL 출력 금지. moment_key 시점 + 자연어 근거만." 강제 명령 박제 (시스템 instruction).
  - rationale 자연어 = 한국어 1~2문장, 100자 이내 권장.

Gemini 응답 파싱:
  1. JSON 추출 시도 (top-level 또는 ```json 블록).
  2. moments list 가 아니면 ValueError ("Gemini 응답이 moments list 아님: ...").
  3. 각 entry → KeyMoment(motion, moment_key, frame_idx, rationale) + validate() 호출. validate 실패 entry 는 ValueError 박제 + 어느 entry 인지 메시지.
  4. expected_moment_keys 와 교집합 0 이면 ValueError ("Gemini 응답에 VALID_MOMENT_KEYS 중 하나도 없음").
  5. 성공 시 list[KeyMoment] 반환.

응답 캐싱 — extract_moments 결과를 `backend/research/spikes/reports/gemini_moments_<motion>_<UTC>.json` 에 dump (재호출 방지 + 디버그). live mode CLI 가 cache 옵션 사용.

`__init__.py` append-only 추가 — `from .gemini_moment_extractor import KeyMoment, GeminiMomentExtractor`. 기존 GeometricCriterion / load_criteria / load_grouped_criteria export 보존.

운영 코드 0줄 수정 — gemini_moment_extractor.py 는 신규 파일. Lambda 런타임에는 import 안 됨 (Plan 13 = spike, Phase 5 가 운영 통합).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && /Users/kimtaesung/Dev/SunityMotion/backend/.venv/bin/python3 -m pytest backend/tests/test_gemini_moment_extractor.py -v</automated>
  </verify>
  <done>
    - gemini_moment_extractor.py 존재 (KeyMoment frozen dataclass + GeminiMomentExtractor 클래스)
    - validate() 6항목 가드 PASS — 좌표/점수/판단 키워드 정규식 차단 검증됨
    - Parameter Store fetch + env fallback 두 경로 모두 mock 으로 검증됨 (boto3 mock 또는 monkeypatch)
    - VALID_MOMENT_KEYS enum 강제 (geometric_criterion 재사용)
    - 응답 캐시 path 박제됨
    - __init__.py 재노출 1줄 추가, 기존 export 보존
    - 단위 테스트 mmpose/torch/NLF/Gemini SDK import 0
  </done>
</task>

<task type="auto">
  <name>Task 2: moment_dimensions 모듈 — measure_moment_angles + compute_criteria_gaps</name>
  <files>backend/shared/python/sunity_shared/judging/moment_dimensions.py, backend/shared/python/sunity_shared/judging/__init__.py</files>
  <read_first>
    - backend/shared/python/sunity_shared/analysis/dimensions.py (frame-mean hold_window / line_score 기존 path 파악 — 운영 코드 무수정 확인용)
    - backend/shared/python/sunity_shared/analysis/skeleton.py (JOINT_KEYS = 8 tuple, JOINT_ANGLES dict)
    - backend/shared/python/sunity_shared/judging/geometric_criterion.py (GeometricCriterion 필드 / VALID_MOMENT_KEYS)
    - backend/shared/python/sunity_shared/judging/loader.py (load_grouped_criteria 인터페이스 — dict[moment_key, list[GeometricCriterion]] 반환)
    - backend/judging_data/criteria/ref-invert.yaml (Plan 15 1차 박제 5 entries — angle_target 180° / tolerance 20° / minimum 160°)
    - backend/shared/python/sunity_shared/judging/gemini_moment_extractor.py (KeyMoment frozen dataclass, T-1)
  </read_first>
  <action>
MomentGapResult frozen dataclass — motion: str, moment_key: str, joint_key: str, measured_angle_deg: float, angle_target: float, gap_deg: float (signed: measured - target), tolerance_full: float, pass_tolerance: bool (|gap| <= tolerance_full), fail_minimum: bool (measured < minimum_requirement), deduction: float (max(0, |gap| - tolerance_full) * deduction_per_step), source_ref: str (Plan 15 source_ref 그대로 전달).

measure_moment_angles(angles_tj: np.ndarray, key_moments: list[KeyMoment], window: int = 5) -> dict[str, dict[str, float]] —
  - angles_tj shape 검증 = (T, len(JOINT_KEYS)) = (T, 8). 다르면 ValueError "moment_dimensions 는 8 angle joints (JOINT_KEYS) 만 지원" (Plan 12 (d) derive joint 회피 박제).
  - 각 key_moment 마다 frame_idx ± window/2 범위 평균 (NaN-safe `np.nanmean`). frame_idx 가 T 범위 밖이면 clamp + 경고 (RuntimeWarning, ValueError 아님).
  - window 가 짝수면 [frame_idx - window//2, frame_idx + window//2] 슬라이스, 홀수면 +1 보정.
  - 반환 = dict[moment_key, dict[joint_key (JOINT_KEYS 8개 그대로), measured_angle_deg]]. 같은 moment_key 가 여러 KeyMoment 로 들어오면 마지막 entry 가 덮어쓰기 (또는 frame_idx 최소값 우선 — 옵션 정책, T-2 결정).
  - 8 joint 만 추출 — derive joint 미사용. JOINT_KEYS 순서대로 ordered dict 권장.

compute_criteria_gaps(measured: dict[str, dict[str, float]], grouped_criteria: dict[str, list[GeometricCriterion]]) -> list[MomentGapResult] —
  - 입력 grouped_criteria = Plan 15 load_grouped_criteria 반환값 그대로.
  - 각 moment_key 별 GeometricCriterion list 순회 — measured[moment_key][criterion.joint_key] lookup. measured 에 없으면 skip + 경고 (criterion.joint_key 가 JOINT_KEYS 8개 외 = ValueError; JOINT_KEYS 안인데 measured 에 없으면 skip + 경고).
  - gap_deg = measured - angle_target (signed). pass_tolerance = |gap_deg| <= tolerance_full. fail_minimum = measured < minimum_requirement. deduction = max(0, |gap_deg| - tolerance_full) * deduction_per_step.
  - 반환 = list[MomentGapResult] flat (moment_key 별 그룹 X — 호출자가 그룹화 가능).

compute_line_score_from_moments(gaps: list[MomentGapResult]) -> int | None —
  - Plan 11 line N/A 회복 path. dimensions.line_score 의 expected_extend 누락 우회 — Plan 15 GeometricCriterion 가 IPSF EXTEND 자체이므로 angle_target >= 150° entry 만 line 차원에 사용 (또는 별도 line_mask 필드 도입 후속).
  - 빈 list → None (기존 dimensions.line_score 와 동일 시그니처).
  - 그 외 → kismam.score_from_deviation(mean(|gap_deg|), tolerance_full mean) 식 또는 단순 평균 deduction 합 → 100 - sum(deduction). 정확한 식은 T-2 결정 (테스트로 검증). 기존 line_score 의 가우시안 스코어와 호환되도록 0~100 정수 반환.

compute_angle_score_from_moments(gaps: list[MomentGapResult]) -> int | None —
  - Plan 11 angle N/A 회복 path. 기존 dimensions.absolute_dimension_scores 는 angle 키를 reference 필요로 함 (mode1=정은지 / mode3=이전). 본 plan 의 IPSF baseline 은 reference 무관 — 측정 ↔ angle_target 만으로 산출 가능.
  - 빈 list → None. 그 외 → 100 - sum(deduction) 또는 평균 deduction 식 (T-2 결정).

운영 dimensions.py 무수정 — 본 모듈은 신규. 호출 chain = spike_gemini_moment → moment_dimensions → judging.loader.load_grouped_criteria. Phase 5 정식 통합 시 dimensions.py 의 absolute_dimension_scores 가 본 모듈 호출하도록 확장 (Phase 5 책임, 본 plan 무관).

`__init__.py` append-only — `from .moment_dimensions import MomentGapResult, measure_moment_angles, compute_criteria_gaps, compute_line_score_from_moments, compute_angle_score_from_moments` 재노출. 기존 export 보존.

테스트 fixture — ref-invert.yaml 5 entries 그대로 입력 + 가상 measured_angle (예: left_shoulder=175°, right_shoulder=170°, left_hip=185°, right_hip=178°, right_knee=140°) → gap / pass_tolerance / fail_minimum / deduction 정확 산출 검증.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && /Users/kimtaesung/Dev/SunityMotion/backend/.venv/bin/python3 -m pytest backend/tests/test_moment_dimensions.py -v</automated>
  </verify>
  <done>
    - moment_dimensions.py 존재 (MomentGapResult + 5 함수)
    - measure_moment_angles 8 joint shape 검증 + window 산출 정확성 검증
    - compute_criteria_gaps gap / pass_tolerance / fail_minimum / deduction 식 검증
    - compute_line_score_from_moments / compute_angle_score_from_moments 0~100 정수 반환 + 빈 list → None
    - ref-invert.yaml 1차 박제 5 entries fixture 입력 → 가상 measured 로 결과 박제됨
    - 운영 dimensions.py / technique.py / skeleton.py 무수정 확인
    - 단위 테스트 mmpose/torch/NLF import 0 (numpy 만 사용, Pod 의존성 X)
  </done>
</task>

<task type="auto">
  <name>Task 3: spike_gemini_moment CLI — 단일 영상 end-to-end</name>
  <files>backend/research/spikes/spike_gemini_moment.py, backend/tests/test_spike_gemini_moment_smoke.py</files>
  <read_first>
    - backend/research/spikes/spike_rtmpose.py (단일 영상 spike 패턴 + S3 다운로드 / _run_rtmpose_2d / _load_motionbert / _run_motionbert_inference / _h36m17_to_coco17_subset / build_arg_parser)
    - backend/research/spikes/sweep_rtmpose.py (sweep 패턴 — generate_markdown / .json + .md sibling 출력)
    - backend/research/spikes/debug_gap_root_cause.py (12 CLI args + report-only vs live mode 분리 패턴 참조)
    - backend/shared/python/sunity_shared/judging/gemini_moment_extractor.py (T-1)
    - backend/shared/python/sunity_shared/judging/moment_dimensions.py (T-2)
    - backend/shared/python/sunity_shared/judging/loader.py (load_grouped_criteria)
    - backend/shared/python/sunity_shared/analysis/temporal.py (NaN fill — spike_rtmpose 가 호출하는 패턴 그대로)
  </read_first>
  <action>
CLI 인자 (10~12개, Plan 10/11/12 spike 패턴 호환):
  - `--mode` default `report-only` — `report-only` | `live`
  - `--motion` (required) — 분석 motion_id (ref-invert / ref-climb / ...)
  - `--bucket` default `sunity-motion-pilot-videos` — S3 버킷
  - `--rtmpose-config` (live mode 필수) — RTMPose .py
  - `--rtmpose-checkpoint` (live mode 필수) — RTMPose .pth
  - `--motionbert-root` default `/workspace/MotionBERT`
  - `--motionbert-weights` (auto)
  - `--score-threshold` default `0.3`
  - `--det-model` default `none`
  - `--gemini-stub` default None — report-only mode 에서 stub 응답 JSON 경로 (테스트 / fixture)
  - `--criteria-dir` default None — Plan 15 base_dir override (테스트)
  - `--out` (auto) — `backend/research/spikes/reports/spike_gemini_moment_<UTC>.json`

run_spike(motion, mode, ...) 흐름:
  1. Plan 15 load_grouped_criteria(motion, base_dir=criteria_dir) 호출. 누락 motion → FileNotFoundError 명확 메시지 (belle 라벨링 안내).
  2. grouped_criteria 의 빈 moment_key 비율 확인 — 모두 빈 list 면 RuntimeError "Plan 15 IPSF 라벨링 미진입 motion={motion}. belle T-5 필요." (ref-invert 외 4영상은 1차 박제 시점 비어있음).
  3. mode=live: S3 영상 다운로드 (spike_rtmpose 의 _download_video_from_s3 import + 호출, spike_rtmpose 무수정). spike_rtmpose._run_rtmpose_2d → _h36m17_to_coco17_subset → _load_motionbert → _run_motionbert_inference → angles_tj (T, 8) 산출. spike_rtmpose 의 함수 호출 chain 그대로 — 본 plan 은 호출만, 코드 1벌. NaN fill 은 temporal.temporal_fill 호출 (spike_rtmpose 와 동일).
  4. mode=live: GeminiMomentExtractor(API key from Parameter Store/env) → extract_moments(video_uri, motion) → list[KeyMoment]. 응답 캐시 = `backend/research/spikes/reports/gemini_moments_<motion>_<UTC>.json`.
  5. mode=report-only: --gemini-stub 경로의 JSON 로드 → KeyMoment list 생성 (validate() 호출 — 키워드 가드 적용). angles_tj 는 fixture 또는 zero matrix (테스트용).
  6. measure_moment_angles(angles_tj, key_moments, window=5) → measured dict.
  7. compute_criteria_gaps(measured, grouped_criteria) → gaps list.
  8. compute_line_score_from_moments(gaps) + compute_angle_score_from_moments(gaps) 산출.
  9. JSON + Markdown 출력 — top-level: generated_at, motion, mode, rtmpose_path / motionbert_path (live), num_moments, num_gaps, line_score, angle_score, gaps[] (moment_key / joint_key / measured / target / gap / pass_tolerance / fail_minimum / deduction / source_ref), key_moments[] (moment_key / frame_idx / rationale).
  10. Markdown 표 — moment_key × joint 별 measured / target / gap / pass / fail / deduction.

generate_markdown 출력 형식:
  ```
  # Plan 13 spike — Gemini key moment + IPSF criteria
  motion: ref-invert
  mode: live
  line_score: 78
  angle_score: 82
  key_moments: 4 (setup / hold / peak / release)
  
  ## hold_moment
  | joint | measured | target | gap | pass(±20) | fail<minimum | deduction |
  | --- | --- | --- | --- | --- | --- | --- |
  | left_shoulder | 175.0 | 180.0 | -5.0 | PASS | FAIL | 0.0 |
  ...
  ```
  (이모지/체크마크 금지 — `pass(±20)` / `fail<minimum` 컬럼은 `PASS` / `FAIL` 문자열만. CLAUDE.md §7).

테스트 (test_spike_gemini_moment_smoke.py):
  - TestCliParsing — 3~5 케이스 (mode default / motion required / out default path / stub 경로 인식)
  - TestReportOnlyMode — Gemini stub fixture (ref-invert 4 moment_key entries) + 가상 angles_tj zero matrix (T=200, J=8) → 정상 JSON + .md sibling 생성. line_score / angle_score 산출 정확성.
  - TestStubValidationGuard — Gemini stub 에 좌표/점수 키워드 포함 시 ValueError (T-1 guard 검증 e2e).
  - TestEmptyCriteria — ref-climb (빈 템플릿) → RuntimeError "Plan 15 IPSF 라벨링 미진입".
  - TestJsonShape — top-level 필수 키 + gaps[] 스키마.
  
모든 테스트 mmpose/torch/Gemini SDK import 0 (HTTP / GPU stub).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && /Users/kimtaesung/Dev/SunityMotion/backend/.venv/bin/python3 -m pytest backend/tests/test_spike_gemini_moment_smoke.py -v</automated>
  </verify>
  <done>
    - spike_gemini_moment.py 존재 + build_arg_parser + run_spike + generate_markdown + main
    - report-only mode 가 mmpose/torch/Gemini SDK 없이 로컬 실행 가능
    - live mode 가 spike_rtmpose 함수 호출만 (spike_rtmpose 무수정)
    - JSON + Markdown sibling 출력 형식 박제
    - smoke 테스트 ≥ 8 PASS (CLI / report-only e2e / stub guard / empty criteria / JSON shape)
    - 이모지 0건 + 사람 점수 0건
    - 응답 캐시 path 박제됨 (gemini_moments_<motion>_<UTC>.json)
  </done>
</task>

<task type="auto">
  <name>Task 4: README append + 5영상 IPSF 4영상 미라벨링 가드 박제</name>
  <files>backend/research/spikes/README.md</files>
  <read_first>
    - backend/research/spikes/README.md (Plan 07/08/10/11/12 섹션 보존 확인)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-12-SUMMARY.md (verdict + recommendation 박제 패턴)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-15-SUMMARY.md (status pending_belle_labeling 박제 패턴)
    - backend/judging_data/criteria/ref-invert.yaml (1차 박제, 다른 4 yaml 빈 템플릿)
  </read_first>
  <action>
Plan 13 섹션 append (기존 Plan 07/08/10/11/12 보존). 섹션 구성:

1. **목적** — Plan 12 dominant (a) + (e) 후속 standard path. IPSF tolerance baseline 산출 + line/angle 회복.

2. **모듈 / CLI 요약** —
   - `sunity_shared.judging.gemini_moment_extractor.GeminiMomentExtractor` — Parameter Store + Gemini 2.5 Pro multimodal video API
   - `sunity_shared.judging.moment_dimensions` — measure_moment_angles + compute_criteria_gaps + compute_line/angle_score
   - `backend.research.spikes.spike_gemini_moment` — 단일 영상 CLI

3. **실행 예시** — report-only mode + live mode (Pod) 둘 다 copy-paste 가능 명령.
   - report-only:
     ```
     python3 -m backend.research.spikes.spike_gemini_moment \
       --mode report-only --motion ref-invert \
       --gemini-stub backend/research/spikes/fixtures/gemini_stub_ref_invert.json
     ```
   - live (Pod):
     ```
     cd /workspace/SunityMotion && git pull --ff-only origin main
     python3 -m backend.research.spikes.spike_gemini_moment \
       --mode live --motion ref-invert \
       --bucket sunity-motion-pilot-videos \
       --rtmpose-config /workspace/rtmpose_weights/rtmpose-l_8xb256-420e_coco-256x192.py \
       --rtmpose-checkpoint /workspace/rtmpose_weights/rtmpose-l_simcc-coco_pt-aic-coco_420e-256x192-1352a4d2_20230127.pth \
       --motionbert-root /workspace/MotionBERT \
       --motionbert-weights /workspace/MotionBERT/checkpoint/pose3d/FT_MB_lite_MB_ft_h36m_global_lite/best_epoch.bin \
       --out backend/research/spikes/reports/spike_gemini_moment_$(date +%Y%m%d_%H%M).json
     ```
   - 사전 조건 = Pod env `GEMINI_API_KEY` 또는 AWS Parameter Store `/sunity/motion/gemini-api-key` 접근 가능.

4. **5영상 IPSF 라벨링 상태 박제** —
   | motion | Plan 15 status | 본 plan live mode 가능 |
   |---|---|---|
   | ref-invert | 1차 박제 (5 entries, commit 861fb3a/4264001) | **YES (우선 시범)** |
   | ref-climb | 빈 템플릿 (belle T-5 대기) | NO |
   | ref-foxtop-split | 빈 템플릿 | NO |
   | ref-foxtop | 빈 템플릿 | NO |
   | ref-sideway-spin | 빈 템플릿 | NO |

   → 본 plan live mode = ref-invert 단독 시범. Plan 14 5영상 sweep 진입 전 belle 4영상 추가 라벨링 필요.

5. **주의사항** —
   - 운영 코드 무수정 (dimensions.py / technique.py / FallbackRecognizer / functions / runpod_inference)
   - 기존 spike 무수정 (spike_rtmpose / sweep_rtmpose / debug_dimensions / debug_gap_root_cause)
   - NLF 호출 0 (Plan 12 (c) verdict 후 영구 폐기)
   - Gemini 응답 좌표/점수/판단 금지 가드
   - 8 angle joints (JOINT_KEYS) 만 사용 — derive joint 회피 (Plan 12 (d))

6. **Plan 14 진입 게이트 박제** —
   - 본 plan ref-invert live mode 결과 line/angle 모두 산출됨 (None 아님)
   - tolerance ±20° 안 pass_tolerance 비율 50% 이상 (1차 신호 — 절대 통과 게이트 아님, Plan 14 5영상 baseline)
   - 위 두 조건 통과 시 → belle 4영상 IPSF 라벨링 (Plan 15 T-5) → Plan 14 PLAN 작성 (5영상 batch sweep + IPSF tolerance baseline)
   - 미통과 시 → Plan 12 권고 후속 spike (nlf_re-spike / rtmpose_to_h36m17_correction / multi_engine_averaging) 별 plan 신설
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && grep -c "^## Plan 1[3]" backend/research/spikes/README.md | grep -q "^1$" && grep -c "^## Plan 1[2]" backend/research/spikes/README.md | grep -q "^1$" && grep -c "^## Plan 1[1]" backend/research/spikes/README.md | grep -q "^1$" && grep -c "^## Plan 10" backend/research/spikes/README.md | grep -q "^1$"</automated>
  </verify>
  <done>
    - README Plan 13 섹션 append 완료
    - Plan 07/08/10/11/12 섹션 모두 보존 (헤더 count 검증)
    - 5영상 IPSF 라벨링 상태표 박제 (ref-invert 1차 박제, 나머지 4영상 belle T-5 대기)
    - Plan 14 진입 게이트 명시
    - report-only + live mode 실행 명령 copy-paste 가능
  </done>
</task>

<task type="auto">
  <name>Task 5: SUMMARY 작성 + STATE.md / ROADMAP.md 갱신 (executor 완료 박제)</name>
  <files>.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-13-SUMMARY.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-12-SUMMARY.md (frontmatter / TL;DR / decisions / Self-Check 패턴 참조)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-15-SUMMARY.md (status pending_belle_* 패턴 참조)
    - .planning/STATE.md (Current Position / progress.completed_plans 갱신 위치 파악)
    - .planning/ROADMAP.md (Plan 13 행 위치 + Phase 1 진행률 표 위치)
  </read_first>
  <action>
01-13-SUMMARY.md frontmatter 박제:
  - phase: 01-poseengine-mediapipe-nlf-r-d
  - plan: "13"
  - subsystem: judging-spike
  - tags: [spike, gemini, key-moment, ipsf, line-angle-recovery, pending_belle_live, apache-2.0]
  - dependency_graph.requires: [01-11, 01-12, 01-15]
  - dependency_graph.provides: [
      "sunity_shared.judging.gemini_moment_extractor (KeyMoment + GeminiMomentExtractor + 좌표·점수·판단 금지 가드)",
      "sunity_shared.judging.moment_dimensions (MomentGapResult + measure_moment_angles + compute_criteria_gaps + compute_line/angle_score_from_moments)",
      "backend/research/spikes/spike_gemini_moment.py (단일 영상 spike CLI, report-only + live mode)",
      "IPSF tolerance baseline 산출 entry point — Plan 14 5영상 sweep 진입 게이트"
    ]
  - dependency_graph.affects: [01-14, "Wave 3 (Plan 04/05) 진입 게이트"]
  - tech_stack.added: ["(가능하면 0) — stdlib urllib.request 우선. 어쩔 수 없는 경우 google-generativeai >= 0.8 backend/requirements-dev.txt 만 추가 (Lambda 런타임 미배포)"]
  - tech_stack.patterns: [
      "Plan 12 standard path 만 선택 — nlf_re-spike / rtmpose_to_h36m17_correction / multi_engine_averaging 은 별 plan",
      "8 angle joints (JOINT_KEYS) 만 사용 — Plan 12 (d) derive joint 회피",
      "Gemini 응답 = moment_key + frame_idx + rationale 3 필드만, 좌표·점수·판단 정규식 차단 가드",
      "Parameter Store /sunity/motion/gemini-api-key + env GEMINI_API_KEY fallback",
      "spike_rtmpose 함수 호출만 (코드 1벌 분기 0) — spike_rtmpose 무수정"
    ]
  - key_files.created: [
      "backend/shared/python/sunity_shared/judging/gemini_moment_extractor.py",
      "backend/shared/python/sunity_shared/judging/moment_dimensions.py",
      "backend/research/spikes/spike_gemini_moment.py",
      "backend/tests/test_gemini_moment_extractor.py",
      "backend/tests/test_moment_dimensions.py",
      "backend/tests/test_spike_gemini_moment_smoke.py",
      ".planning/phases/01-poseengine-mediapipe-nlf-r-d/01-13-SUMMARY.md"
    ]
  - key_files.modified: [
      "backend/shared/python/sunity_shared/judging/__init__.py (재노출 1~2줄 append-only)",
      "backend/research/spikes/README.md (Plan 13 섹션 append)",
      ".planning/STATE.md (last_activity / progress.completed_plans / Current Position 갱신)",
      ".planning/ROADMAP.md (Plan 13 행 / Phase 1 진행률 갱신)"
    ]
  - decisions: [
      "Plan 12 final verdict 적재 후 path 중 standard 만 선택 — 다른 3 path (nlf_re-spike / rtmpose_to_h36m17_correction / multi_engine_averaging) 은 별 plan. Plan 14 통과 미달 시 후속.",
      "Plan 15 데이터 입력 = commit 861fb3a/4264001 ref-invert 1차 박제 (5 entries) 그대로. 다른 4영상은 belle T-5 라벨링 대기 — 본 plan live mode 는 ref-invert 단독 시범.",
      "Gemini 응답 좌표·점수·판단 키워드 금지 — SCORE-01 + memory analysis-objectivity-no-human-scores. 정규식 가드 (x/y/z/score/점수/coordinate/좌표/판단/verdict/pass/fail/deduction/감점/degree/도/°/숫자) 박제.",
      "8 angle joints (skeleton.JOINT_KEYS) 만 measured 추출 — derive joint (hip/spine/thorax/neck_nose/head) 미사용. Plan 12 (d) strong (17/17 chain 다름) 회피.",
      "NLF 호출 0 — R&D 격리 유지 (D-06/D-07). Plan 12 (c) verdict 후 NLF 갭 baseline 영구 폐기.",
      "Phase 5 정식 Gemini 통합 X — 본 plan = spike 레벨. 운영 코드 (functions / pipeline / runpod_inference / shared/analysis/recognizers/) 무수정.",
      "Pod 환경 보존 — torch 2.4.1+cu124 / numpy 1.26.4 / mmpose 1.3.2. Gemini SDK 추가 install 최소화 (stdlib urllib 우선).",
      "Gemini API 키 = AWS Parameter Store /sunity/motion/gemini-api-key SecureString + env GEMINI_API_KEY fallback. .env 하드코딩 영구 금지."
    ]
  - requirements_completed: []  # POSE-01 / SCORE-01 = Plan 14 통과 후 평가, 본 plan 은 entry point 만
  - metrics.duration: "~75 min executor (T-1 ~ T-5 + smoke 테스트 + README append + SUMMARY)"
  - metrics.completed_date: "pending_belle_live (T-6 belle Pod live mode 대기)"
  - metrics.tasks_completed: 5
  - metrics.tasks_total: 6
  - metrics.files_created: 7
  - metrics.files_modified: 4

본문 섹션 (필수):
  1. TL;DR 표 (Verdict / 단계 / scope / 신규 파일 / 단위 테스트 / 커밋 / 운영 코드 수정 / 기존 spike 수정 / 다음 행동)
  2. T-1 ~ T-4 executor 완료 박제 (각 task 핵심 산출물 + 가드 표 + 테스트 PASS 수)
  3. T-5 (본 SUMMARY + STATE/ROADMAP 갱신)
  4. T-6 belle Pod live mode 대기 — autonomous: false 명령 + 응답 옵션 4개 + 응답 후 SUMMARY 갱신 path
  5. 핵심 결정 박제 (frontmatter decisions 와 중복, 본문 명시)
  6. Plan 14 / Wave 3 진입 게이트 갱신
  7. Deviations from Plan (있으면)
  8. Known Stubs (Gemini API live 미실행 — T-6)
  9. Threat Flags (신규 네트워크 endpoint = Gemini API, auth = Parameter Store + 정규식 가드 박제)
  10. Open Questions / Next Step
  11. Self-Check (파일 존재 / 운영 코드 무수정 / 기존 spike 무수정 / 테스트 PASS / 커밋 존재)

STATE.md 갱신:
  - last_updated: 갱신
  - last_activity: "Plan 13 executor T-1~T-5 완료 (Gemini key moment + moment_dimensions + spike CLI + 23+ smoke 테스트). T-6 belle Pod live mode 대기. Plan 14 진입 게이트 박제."
  - progress.completed_plans: 9 → 10 (Plan 13 executor 완료, live mode 미실행이지만 작업 완료로 카운트)
  - Current Position 섹션 — Plan 13 verdict 박제 + 다음 행동 (T-6 belle Pod live mode 또는 Plan 14 진입 게이트)

ROADMAP.md 갱신:
  - Phase 1 Plans 행 Plan 13 체크박스 (`[~]` executor 완료, live mode 대기) 갱신
  - "Phase Details" Phase 1 Plans 목록 Plan 13 행 — 작성 완료 + executor 박제
  - Progress 표 Phase 1 Plans Complete 카운트 갱신

본 task 의 executor 부분 commit 메시지 예시:
  - `docs(01-13): SUMMARY (pending_belle_live) + STATE/ROADMAP 갱신`
  - 또는 plan-phase orchestrator 가 자동 commit 처리
  </action>
  <verify>
    <automated>test -f .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-13-SUMMARY.md && grep -q "^phase: 01-poseengine-mediapipe-nlf-r-d$" .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-13-SUMMARY.md && grep -q "pending_belle_live" .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-13-SUMMARY.md && grep -q "01-13" .planning/STATE.md && grep -q "01-13-PLAN.md" .planning/ROADMAP.md</automated>
  </verify>
  <done>
    - 01-13-SUMMARY.md frontmatter (phase / plan / subsystem / tags / dependency_graph / decisions / requirements_completed / metrics) 박제
    - 본문 11 섹션 모두 작성 (TL;DR / T-1~T-6 / 결정 / 게이트 / Deviations / Known Stubs / Threat Flags / Open Questions / Self-Check)
    - STATE.md Current Position + progress.completed_plans 갱신
    - ROADMAP.md Plan 13 행 + Phase 1 진행률 갱신
    - T-6 belle Pod live mode 대기 status 명시 (autonomous: false)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: belle Pod live mode 실행 + 결과 박제 (autonomous: false)</name>
  <what-built>
    - Plan 13 T-1~T-5 executor 완료 — Gemini key moment 어댑터 + moment_dimensions + spike CLI + 단위 테스트 + README + SUMMARY (status pending_belle_live)
    - 본 task = belle Pod 에서 live mode 1차 실행 (ref-invert 단독 시범, Plan 15 1차 박제 5 entries 보유)
    - 결과 = line_score / angle_score 산출 여부 + tolerance ±20° 안 pass_tolerance 비율 + Gemini 응답 좌표·점수·판단 가드 작동 여부
  </what-built>
  <how-to-verify>
    **사전 조건**:
    1. Pod 살아있음 (RunPod 종료 X). 종료 시 belle 재생성 → STATE.md Pod 환경 동기화 필요.
    2. Gemini API 키 = AWS Parameter Store `/sunity/motion/gemini-api-key` 접근 가능 (또는 Pod env `GEMINI_API_KEY` 주입).
    3. Plan 15 ref-invert.yaml 5 entries 보유 (commit 861fb3a/4264001 확인됨).

    **belle Pod 실행 명령** (copy-paste):
    ```bash
    cd /workspace/SunityMotion && git pull --ff-only origin main

    # Parameter Store 에서 Gemini 키 가져오기 (또는 env 사전 export)
    export GEMINI_API_KEY=$(aws ssm get-parameter \
      --name /sunity/motion/gemini-api-key \
      --with-decryption \
      --query Parameter.Value --output text \
      --region ap-northeast-2)

    python3 -m backend.research.spikes.spike_gemini_moment \
      --mode live \
      --motion ref-invert \
      --bucket sunity-motion-pilot-videos \
      --rtmpose-config /workspace/rtmpose_weights/rtmpose-l_8xb256-420e_coco-256x192.py \
      --rtmpose-checkpoint /workspace/rtmpose_weights/rtmpose-l_simcc-coco_pt-aic-coco_420e-256x192-1352a4d2_20230127.pth \
      --motionbert-root /workspace/MotionBERT \
      --motionbert-weights /workspace/MotionBERT/checkpoint/pose3d/FT_MB_lite_MB_ft_h36m_global_lite/best_epoch.bin \
      --out backend/research/spikes/reports/spike_gemini_moment_$(date +%Y%m%d_%H%M).json
    ```

    예상 소요: 1영상 (ref-invert) × ~3분 (RTMPose+MB 1.5분 + Gemini API 1.5분) = **~3분**.

    **결과 파일**:
    - `backend/research/spikes/reports/spike_gemini_moment_<UTC>.json`
    - `backend/research/spikes/reports/spike_gemini_moment_<UTC>.md` (sibling)
    - `backend/research/spikes/reports/gemini_moments_ref-invert_<UTC>.json` (Gemini 응답 캐시)

    **belle 검토 항목**:
    1. Gemini 응답이 정상 JSON 으로 파싱됨? `moments` list 가 4 moment_key 중 최소 1개 포함?
    2. Gemini 응답에 좌표·점수·판단 키워드 없음? (가드 작동 확인 — 키워드 발견 시 ValueError 로 차단)
    3. moment-list 기반 measured 각도가 5 joint 모두 산출됨? (NaN 비율 < 50%)
    4. compute_criteria_gaps 결과 — ref-invert hold_moment 5 entries 중 pass_tolerance ≥ 50% (절대 통과 게이트 아님, Plan 14 5영상 baseline)?
    5. line_score / angle_score 모두 None 이 아닌 0~100 정수로 산출됨? (Plan 11 line/angle N/A 회복 1차 신호)

    **belle 응답 옵션**:
    | 응답 | 조건 | 다음 행동 |
    |---|---|---|
    | `proceed to Plan 14 sweep` | 5 검토 항목 모두 PASS + line/angle 둘 다 산출 | belle 4영상 IPSF 라벨링 (Plan 15 T-5) → Plan 14 PLAN 작성 |
    | `partial pass, need 1 more spike` | 4/5 PASS (Gemini 응답 형식 문제 등 minor) | spike_gemini_moment 의 응답 파싱 보강 + 재실행 |
    | `gemini quality insufficient, retry with different prompt` | Gemini 응답이 moment 시점 잘못 식별 | 프롬프트 보강 + 재실행 또는 belle 가 frame_idx 직접 입력하는 fallback path 추가 (별 task) |
    | `Plan 12 후속 spike 필요` | line/angle 둘 중 하나라도 None | nlf_re-spike / rtmpose_to_h36m17_correction / multi_engine_averaging 중 belle 가 우선순위 결정 → 별 plan |

    **응답 후 박제**:
    - 01-13-SUMMARY.md status `pending_belle_live` → `live_passed` 또는 `live_blocked` 갱신
    - frontmatter metrics.completed_date / tags 갱신
    - STATE.md Current Position Plan 13 verdict 명시
    - Plan 14 진입 게이트 갱신 또는 후속 plan 신설
  </how-to-verify>
  <resume-signal>belle 응답 4 옵션 중 1 + 결과 .md 공유 (또는 직접 SUMMARY 갱신 후 "labeled" 신호)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (belle Pod) → Gemini API | Gemini 2.5 Pro multimodal API HTTPS 호출. video frames 또는 presigned URL 전송 — Google AI Studio 트래픽. |
| client (belle Pod) → AWS Parameter Store | boto3 ssm.get_parameter (SecureString 복호화). IAM 자격증명 Pod env 박제. |
| Gemini 응답 → moment_dimensions | 응답 JSON 신뢰 경계 — 좌표·점수·판단 키워드 정규식 차단 가드 + VALID_MOMENT_KEYS enum 검증. |
| spike output → 결과 JSON 파일 | 로컬 디스크 저장. PII / 민감 정보 0 (motion_id + frame_idx + 자연어 rationale + 측정 각도 수치). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-13-01 | Information Disclosure | Gemini API 키 노출 | mitigate | Parameter Store SecureString + env fallback. `.env` 하드코딩 영구 금지 (CLAUDE.md §3). 로그 출력 0 (api_key 변수 명시적 redact). |
| T-01-13-02 | Tampering | Gemini 응답에 좌표/점수/판단 강제 출력 (prompt injection) | mitigate | 정규식 차단 가드 (x/y/z/score/점수/coordinate/좌표/판단/verdict/pass/fail/deduction/감점/degree/도/°/숫자 키워드) + VALID_MOMENT_KEYS enum 강제. 위반 entry 는 ValueError 로 즉시 차단. |
| T-01-13-03 | Repudiation | Gemini 응답 ground truth 위장 | mitigate | source='gemini-2.5-pro' 박제 + rationale 자연어 출처 명시. 사람 점수 라벨링 영구 금지 원칙 (memory `analysis-objectivity-no-human-scores`) + Plan 15 IPSF 객관 임계값만이 정식 baseline. |
| T-01-13-04 | Denial of Service | Gemini API rate limit | accept | spike 레벨 — 1영상 단발 호출. 재호출 시 응답 캐시 사용 (gemini_moments_<motion>_<UTC>.json). Phase 5 정식 통합 시 rate limit handling 별도. |
| T-01-13-05 | Elevation of Privilege | spike_gemini_moment 가 운영 dimensions.py / technique.py 직접 수정 | mitigate | 운영 코드 무수정 게이트 — moment_dimensions 는 신규 모듈. spike_rtmpose 함수 호출만 (spike 무수정). 단위 테스트로 회귀 확인. |
| T-01-13-SC | Tampering | npm/pip/cargo installs | mitigate | google-generativeai 추가 시 backend/requirements-dev.txt 만 (Lambda 런타임 미배포). 가능하면 stdlib urllib 만 사용하여 외부 install 0. RESEARCH.md Package Legitimacy Audit 미작성 — 본 plan = spike, 후속 plan (Phase 5) 에서 Package Legitimacy Gate 통과 후 운영 통합. |
</threat_model>

<verification>
## 단위 테스트

```bash
cd /Users/kimtaesung/Dev/SunityMotion
/Users/kimtaesung/Dev/SunityMotion/backend/.venv/bin/python3 -m pytest \
  backend/tests/test_gemini_moment_extractor.py \
  backend/tests/test_moment_dimensions.py \
  backend/tests/test_spike_gemini_moment_smoke.py -v
```

예상 결과: 25+ PASS (Gemini extractor 가드 ~8 + moment_dimensions ~10 + spike smoke ~8). 모두 mmpose / torch / NLF / Gemini SDK import 0 (로컬 실행 가능).

## 기존 테스트 회귀 없음

```bash
/Users/kimtaesung/Dev/SunityMotion/backend/.venv/bin/python3 -m pytest backend/tests/ -v --ignore=backend/tests/test_gemini_moment_extractor.py --ignore=backend/tests/test_moment_dimensions.py --ignore=backend/tests/test_spike_gemini_moment_smoke.py
```

Plan 15 23 PASS (schema 11 + loader 12) + Plan 12 11 PASS (debug_gap smoke) + Plan 11 12 PASS (sweep_rtmpose smoke) + 기존 backend 테스트 321 PASS 유지.

## 운영 코드 무수정 검증

```bash
git diff --name-only main HEAD -- backend/functions/ backend/runpod_inference/ backend/shared/python/sunity_shared/analysis/ | head
```

empty 결과 확인 (운영 코드 0줄 수정). `backend/shared/python/sunity_shared/judging/__init__.py` 만 1~2줄 append (재노출).

## 기존 spike 무수정 검증

```bash
git diff --name-only main HEAD -- backend/research/spikes/spike_rtmpose.py backend/research/spikes/sweep_rtmpose.py backend/research/spikes/debug_dimensions.py backend/research/spikes/debug_gap_root_cause.py backend/research/spikes/rtmpose_to_h36m17.py backend/research/spikes/mediapipe_to_h36m17.py
```

empty 결과 확인.

## live mode (Pod, T-6 belle 책임)

ref-invert 단독 시범 — 5 검토 항목 (Gemini 응답 파싱 / 가드 작동 / measured 산출 / pass_tolerance ≥ 50% / line/angle 둘 다 산출) belle 응답 박제.
</verification>

<success_criteria>
## Executor (T-1 ~ T-5) 완료 게이트

- [ ] `gemini_moment_extractor.py` 존재 — KeyMoment frozen dataclass + GeminiMomentExtractor + 좌표/점수/판단 정규식 가드 + Parameter Store fetch + env fallback
- [ ] `moment_dimensions.py` 존재 — MomentGapResult + measure_moment_angles (8 joint shape 검증) + compute_criteria_gaps + compute_line/angle_score_from_moments
- [ ] `spike_gemini_moment.py` 존재 — build_arg_parser (10~12 args) + run_spike + generate_markdown + main. report-only + live mode 분리
- [ ] `judging/__init__.py` append-only 재노출 (기존 export 보존)
- [ ] `research/spikes/README.md` Plan 13 섹션 append (Plan 07/08/10/11/12 보존)
- [ ] 단위 테스트 25+ PASS (mmpose/torch/NLF/Gemini SDK import 0)
- [ ] 기존 backend 테스트 회귀 없음 (Plan 15 23 + Plan 12 11 + Plan 11 12 + 321 기존 PASS 유지)
- [ ] 운영 코드 0줄 수정 (functions / runpod_inference / shared/analysis/)
- [ ] 기존 spike 0줄 수정 (spike_rtmpose / sweep_rtmpose / debug_dimensions / debug_gap_root_cause / rtmpose_to_h36m17 / mediapipe_to_h36m17)
- [ ] NLF import 0
- [ ] Gemini 응답 좌표/점수/판단 가드 단위 테스트 PASS
- [ ] 8 angle joints (JOINT_KEYS) 만 사용 — derive joint 미사용 검증
- [ ] Parameter Store path `/sunity/motion/gemini-api-key` 박제 + env GEMINI_API_KEY fallback
- [ ] 이모지 0건 / 사람 점수 0건 / 슬롭 코드 0건
- [ ] 01-13-SUMMARY.md status `pending_belle_live` + STATE/ROADMAP 갱신

## live mode (T-6, belle) 완료 게이트

- [ ] belle Pod 실행 명령 1회 PASS (1영상 ref-invert, ~3분)
- [ ] Gemini API 응답 정상 파싱 (`moments` list ≥ 1 entry, moment_key ∈ VALID_MOMENT_KEYS)
- [ ] 좌표/점수/판단 가드 작동 (응답에 키워드 없거나, 발견 시 ValueError 차단)
- [ ] measured 각도 5 joint 모두 산출 (NaN 비율 < 50%)
- [ ] line_score / angle_score 모두 None 이 아닌 0~100 정수
- [ ] pass_tolerance 비율 ≥ 50% (1차 신호 — Plan 14 5영상 baseline)
- [ ] SUMMARY status `pending_belle_live` → `live_passed` 또는 `live_blocked` 갱신

## Wave 3 진입 게이트 (Plan 14 통과 후, 본 plan 직접 책임 아님)

- [ ] belle 4영상 (ref-climb / ref-foxtop-split / ref-foxtop / ref-sideway-spin) IPSF 라벨링 (Plan 15 T-5)
- [ ] Plan 14 5영상 batch sweep 실행 — 측정 각도 ↔ IPSF angle_target 갭 ≤ tolerance_full 5/5 PASS
- [ ] Plan 14 line/angle 5/5 PASS (None 아님)
- [ ] 위 두 조건 통과 → Plan 04 (NLF R&D 격리) + Plan 05 (atomic swap) 진입
</success_criteria>

<output>
Create `.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-13-SUMMARY.md` when T-1 ~ T-5 done (executor 부분). T-6 belle Pod live mode 완료 후 SUMMARY status 갱신.
</output>

---

## 판정 기준 / Wave 3 진입 게이트

### Plan 13 단독 verdict

| 항목 | PASS 조건 | FAIL 조건 |
|---|---|---|
| executor 부분 (T-1~T-5) | 단위 테스트 25+ PASS + 운영/spike 무수정 | 회귀 발생 또는 가드 미작동 |
| live mode (T-6) | ref-invert 5 검토 항목 PASS | Gemini 응답 quality 부족 또는 line/angle None |

### Plan 14 (5영상 sweep) 진입 게이트

본 plan T-6 PASS → belle 4영상 IPSF 라벨링 (Plan 15 T-5) → Plan 14 PLAN 작성. Plan 14 게이트:

- 5영상 measured 각도 ↔ IPSF angle_target 갭 5/5 ≤ tolerance_full
- 5영상 line/angle 5/5 PASS (None 아님)
- pass_tolerance 비율 ≥ 80% (Plan 14 5영상 baseline)

### Wave 3 (Plan 04 / 05) 진입 게이트

Plan 14 통과 → Plan 04 (NLF R&D 격리) + Plan 05 (atomic swap) 진입. POSE-01 충족.

### Plan 12 후속 spike 신설 조건 (본 plan 미통과 시)

| Plan 12 권고 후속 | 조건 | 별 plan |
|---|---|---|
| nlf_re-spike | (c) NLF baseline 후속 — 사실상 Plan 15 IPSF baseline 으로 대체됨, 추가 신설 불필요 | — |
| rtmpose_to_h36m17_correction | (d) derive joint 점수 영향 확인 시 | 별 plan (Plan 16?) |
| multi_engine_averaging | (e) 두 엔진 3D 분포 strong, lift path 통일 필요 시 | 별 plan |

---

## Risk

| Risk | Mitigation |
|---|---|
| Gemini API 응답 quality 부족 — 잘못된 moment 시점 식별 | 응답 캐시 + belle 검토 + 프롬프트 보강 path (별 task). 최악 시 belle frame_idx 직접 입력 fallback path 추가. |
| Gemini rate limit / API 비용 | spike 레벨 1영상 호출 — 비용 0.01 USD 이하 예상. Phase 5 정식 통합 시 rate limit handling 별도. |
| moment_key enum mismatch (Gemini 응답이 setup/hold/peak/release 외 값) | validate() 6번 가드로 즉시 차단 + 응답 재시도 path. |
| 8 angle joints 만 사용하는 제약이 일부 동작에 부적합 (예: 머리 위치 평가 필요한 자세) | Plan 14 5영상 sweep 결과로 확인. 부적합 시 derive joint 보정 spike 별 plan. |
| Plan 15 다른 4영상 라벨링 지연 (belle T-5 미진입) | 본 plan = ref-invert 단독 시범 OK. Plan 14 진입 게이트가 belle 4영상 라벨링 — belle 일정 의존. |
| Pod 종료 시 환경 재구축 비용 | RunPod Pod Stop 금지 (memory `runpod-gpu-env.md`). 살아있음 유지. 종료 시 STATE.md Pod 환경 동기화 필요. |

## Open Questions

1. **Gemini 2.5 Pro multimodal video API 가 S3 URL 직접 fetch 가능?** 불가능 시 presigned URL 또는 base64 frames 전송 — T-1 구현 시 확인. 후자 = 영상 frames 추출 후 sampled frames 전송 (예: 10 fps).
2. **moment_key 추출 정확성** — Gemini 가 ref-invert hold_moment 시점을 얼마나 정확히 식별? belle T-6 검토 결과로 확정.
3. **window 사이즈 (5 frame default)** — moment frame_idx ± 2~3 평균이 적절한지 단위 테스트 + live 결과로 결정.
4. **line_score / angle_score 산출 식** — 100 - sum(deduction) vs 가우시안 평균 vs 다른 식? T-2 결정 + 테스트 fixture 로 검증. Plan 14 5영상 baseline 통과 확률에 영향.

## Next Step

```
executor T-1~T-5 실행 (~75 분)
  → 단위 테스트 25+ PASS + 운영/spike 무수정 검증
  → 01-13-SUMMARY.md (pending_belle_live) + STATE/ROADMAP 갱신 + commit
  → belle Pod live mode (T-6, ~3분)
  → 결과 .md + .json 공유
  → SUMMARY status 갱신 (live_passed / live_blocked)
  → belle 응답 4 옵션 중 1
  → belle 4영상 IPSF 라벨링 (Plan 15 T-5) → /gsd:plan-phase 1 --plan 14 진입
  또는 Plan 12 후속 spike 신설 (rtmpose_to_h36m17_correction / multi_engine_averaging)
```
