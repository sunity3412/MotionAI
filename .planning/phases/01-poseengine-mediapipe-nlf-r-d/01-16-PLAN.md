---
phase: 01-poseengine-mediapipe-nlf-r-d
plan: 16
type: spike
wave: 2
depends_on:
  - 01-08  # MP+MB 5영상 baseline — ref-invert 92 (Plan 08 SUMMARY 행 4) frame-mean 재현 비교군
  - 01-11  # RTMPose+MB 5영상 sweep — ref-invert 70 frame-mean (sweep_rtmpose) 비교군
  - 01-12  # 5가설 final verdict — (a) frame-mean strong / (e) 두 엔진 3D 분포 strong (root-rel 220+) 박제
  - 01-13  # Plan 13 verdict measurement_unreliable_blocked — frame 88 단일 frame 5/5 fail + 인체학적 비정상 (right_shoulder 18.2°) + 4 가설 (a/b/c/d) trace 요청
files_modified:
  - backend/research/spikes/spike_measurement_trace.py
  - backend/research/spikes/README.md
  - backend/tests/test_spike_measurement_trace.py
  - backend/tests/test_spike_measurement_trace_smoke.py
  - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-16-SUMMARY.md
autonomous: false
parent_plan: 01-13
parent_verdict: "Plan 13 verdict measurement_unreliable_blocked (2026-06-01) — ref-invert hold frame 88 측정 5/5 minimum fail + 인체학적 비정상 (right_shoulder 18.2 / left_hip 54.9 / right_knee 73 도) + Plan 08 MP+MB 92 / Plan 11 RTMPose+MB 70 / Plan 13 단일 frame 5/5 fail cross-engine inconsistency 4-5배 차이. 각도 컨벤션 (compute_joint_angles inner angle 180=완전 신전) IPSF target 일치 확인됨 — 측정값 자체가 root cause. 4 가설 (a) Gemini frame_idx 정확도 / (b) RTMPose+MB 단일 frame 좌우 noise / (c) lifter occlusion 좌우 헷갈림 / (d) RTMPose+MB lift 자체 invert family 부적합 → 본 plan trace 책임."
requirements:
  - POSE-01  # Phase 1 Success Criteria #7 entry gate — 측정 신뢰도 확보 미달 시 IPSF tolerance + line/angle 게이트 통과 불가

must_haves:
  truths:
    - "측정 신뢰도 root cause 4 가설 (a/b/c/d) 각각 strong / weak / rejected 단계 박제 — 임계값 모듈 상수로 박제 (Plan 12 verdict 패턴 동일). dominant 가설 1~2개 식별."
    - "다각도 촬영 (multi-view) 옵션 영구 자동 제외 — memory `single-camera-first-multi-view-last.md` 박제 (2026-06-01). 본 plan 의 권고 / 후속 plan 옵션 / 코드 / 문서 어디서도 multi-view 언급 0건. belle 명시 지시 시에만 재진입."
    - "NLF 호출 0 — Plan 12 (c) verdict 후 영구 폐기 (memory `license-blocklist-pose.md` + Plan 13 박제). 본 plan 의 측정 비교군 = RTMPose+MB + MediaPipe+MB 두 path (둘 다 Apache 2.0). NLF lift 호출 0."
    - "8 angle joints (skeleton.JOINT_KEYS = left/right × elbow/shoulder/hip/knee) 만 trace — derive joint (hip center/spine/thorax/neck_nose/head) 미사용. Plan 12 (d) verdict (RTMPose H36M ordering vs NLF COCO ordering 17/17 다름) 회피 유지."
    - "운영 코드 / 기존 spike / Plan 13 모듈 / Plan 15 데이터 전부 무수정 — git diff HEAD 가 backend/functions/** / backend/runpod_inference/** / backend/shared/python/sunity_shared/analysis/** / backend/shared/python/sunity_shared/judging/** / backend/judging_data/** / 기존 spike 8개 (spike_rtmpose / sweep_rtmpose / debug_dimensions / debug_gap_root_cause / spike_motionbert / rtmpose_to_h36m17 / mediapipe_to_h36m17 / spike_gemini_moment) 모두 empty. 본 plan = 신규 spike + 신규 테스트 + SUMMARY + README append-only."
    - "Plan 13 모듈 무수정 — gemini_moment_extractor.py / moment_dimensions.py / spike_gemini_moment.py 호출 0건. 본 plan 의 frame_idx 입력은 Plan 13 spike 결과 박제 (hold frame 88) 상수만 사용 (Gemini 재호출 0)."
    - "사람 점수 라벨링 0건 (memory `analysis-objectivity-no-human-scores.md` 영구 금지). Gemini API 호출 0건 (본 plan 은 측정 trace, Gemini moment 추출은 Plan 13 책임). 좌표 / 점수 / 판단 키워드 출력 0건."
    - "Pod 환경 보존 (STATE.md 2026-06-01 박제) — torch 2.4.1+cu124 / numpy 1.26.4 / mmpose 1.3.2 / mmcv 2.1.0 / mediapipe 0.10.x. 추가 install 0 (MP / MotionBERT / RTMPose 모두 Plan 08/10/13 시 install 완료 박제). 라이선스: MediaPipe (Apache 2.0) + MotionBERT (MIT) + RTMPose/MMPose (Apache 2.0)."
    - "Plan 14 진입 게이트 = 본 plan dominant 가설 박제 + 후속 plan path 권고 (path B multi-engine averaging / path D Gemini 직접 EXTEND/BENT / path combined) 산출. 본 plan 자체가 Plan 14 통과를 보장하지 않음 — 후속 plan 결과로만 Plan 14 진입 가능. multi-view 옵션 0건 박제."
    - "verdict 임계 (모듈 상수, rationale 주석): (a) frame 88 vs hold_window (88±5) 평균 disagreement ≥ 30도 → strong. (b) RTMPose+MB 의 frame 별 |L-R| 평균 ≥ 40도 → strong. (c) RTMPose+MB 와 MP+MB 의 좌우 매핑 disagreement (좌우 swap 감지) frame 비율 ≥ 0.30 → strong. (d) RTMPose+MB 와 MP+MB 의 frame-by-frame 각도 disagreement 평균 ≥ 30도 → strong."

  artifacts:
    - path: "backend/research/spikes/spike_measurement_trace.py"
      provides: "측정 신뢰도 frame-by-frame trace CLI — ref-invert 단독 (default) 영상 + RTMPose+MB / MP+MB 두 lift path 동시 실행 + Plan 13 박제 frame 88 단일값 vs hold_window 평균 vs 두 엔진 cross-engine 비교. 4 가설 verdict (a/b/c/d) 자동 분기 + 후속 plan path 권고 (multi-view 옵션 0건). JSON + sibling Markdown 출력 (backend/research/spikes/reports/spike_measurement_trace_<UTC>.{json,md}). report-only mode (stub angles fixture) + live mode (Pod, 두 lift path 직렬 실행 ~5분)."
    - path: "backend/tests/test_spike_measurement_trace.py"
      provides: "trace 함수 단위 테스트 — frame-by-frame 8 joint angle 산출 / 좌우 비대칭 (4 pair) 계산 / hold_window (88±5) 평균 산출 / cross-engine joint별 disagreement / 좌우 swap detection / 4 가설 verdict 임계 분기. mmpose/torch/mediapipe import 0 (모두 stub angles fixture)."
    - path: "backend/tests/test_spike_measurement_trace_smoke.py"
      provides: "CLI argparse + report-only mode end-to-end JSON shape + sibling Markdown 생성 + verdict 4 분기 (각 가설 dominant 시나리오) smoke. mmpose import 0."
    - path: "backend/research/spikes/README.md"
      provides: "Plan 16 섹션 append-only — 목적 / 라이선스 (MP Apache 2.0 + MotionBERT MIT + RTMPose Apache 2.0) / report-only 실행 예시 / live mode belle Pod 절차 / 4 가설 verdict 매트릭스 / 주의사항 (multi-view 옵션 0건 박제 / 운영 코드 무수정 / Plan 13 모듈 무수정). Plan 07/08/10/11/12/13 섹션 보존."
    - path: ".planning/phases/01-poseengine-mediapipe-nlf-r-d/01-16-SUMMARY.md"
      provides: "T-1 ~ T-5 executor 완료 + T-6 belle Pod live mode 결과 박제. dominant 가설 1~2개 + 후속 plan path 권고. Plan 14 진입 결정 (본 plan 단독으로는 통과 보장 X, 후속 plan 결과 후)."

  key_links:
    - from: "backend/research/spikes/spike_measurement_trace.py"
      to: "backend/research/spikes/spike_rtmpose.py"
      via: "함수 import only — _resolve_video / _extract_frames / _run_rtmpose_2d / _load_motionbert / _run_motionbert_inference / _h36m17_to_coco17_subset. spike_rtmpose 무수정 호출."
      pattern: "from backend.research.spikes.spike_rtmpose import"
    - from: "backend/research/spikes/spike_measurement_trace.py"
      to: "backend/research/spikes/mediapipe_to_h36m17.py"
      via: "MP 33 keypoint → H36M 17 변환 + spike_motionbert _load_motionbert / _run_motionbert_inference 재사용 (Plan 08 path 동일)"
      pattern: "from backend.research.spikes.mediapipe_to_h36m17 import|from backend.research.spikes.spike_motionbert import"
    - from: "backend/research/spikes/spike_measurement_trace.py"
      to: "backend/shared/python/sunity_shared/analysis/features.py::compute_joint_angles"
      via: "8 angle joints (skeleton.JOINT_KEYS) inner angle 산출 — derive joint 미호출"
      pattern: "compute_joint_angles"
    - from: "backend/research/spikes/spike_measurement_trace.py"
      to: "backend/shared/python/sunity_shared/analysis/temporal.py::temporal_fill"
      via: "frame별 NaN-aware fill (Plan 13 spike 와 동일)"
      pattern: "temporal_fill"
---

<objective>
**Plan 13 verdict `measurement_unreliable_blocked` 후속 — path A (frame-by-frame trace + cross-engine reproduction) 우선 spike (POSE-01).**

Plan 13 (2026-06-01) ref-invert hold frame 88 단일 frame 측정 결과 = 정은지 (폴스포츠 세계챔피언) invert split peak hold 자세에서 right_shoulder 18.2도 / left_hip 54.9도 / right_knee 73도 같은 인체학적 비정상 + 5/5 minimum fail. 동일 영상에서 Plan 08 (MP+MB) frame-mean overall 92 / Plan 11 (RTMPose+MB) frame-mean overall 70 / Plan 13 (RTMPose+MB) hold frame 88 5/5 fail = 4-5배 cross-engine inconsistency. 각도 컨벤션 (compute_joint_angles inner angle 0-180도, 180도 = 완전 신전) IPSF target 일치 확인됨 — 컨벤션 미스매치 아님. 측정값 자체가 root cause.

Purpose: belle 결정 (2026-06-01, memory `feedback-analysis-first.md` + `gap-and-line-angle-mandatory-gates.md` 일관) — "분석 정확도 최우선, 다른 기술 반영해서라도 잡아야 한다". 단, **다각도 촬영 (multi-view) 옵션은 영구 자동 제외** (memory `single-camera-first-multi-view-last.md` 박제). 본 plan = 4 가설 (a) Gemini frame_idx 정확도 / (b) RTMPose+MB 단일 frame 좌우 noise / (c) lifter occlusion 좌우 헷갈림 / (d) RTMPose+MB lift 자체 부적합 → frame-by-frame trace + cross-engine 재현으로 dominant 가설 박제.

본 plan 은 POSE-01 의 Phase 1 Success Criteria #7 entry gate — 측정 신뢰도 확보 미달 시 IPSF tolerance + line/angle 게이트 통과 불가.

Output: spike 레벨 측정 trace CLI + 단위/스모크 테스트 + ref-invert 단독 belle Pod live mode 결과 + dominant 가설 박제 + 후속 plan path 권고 (path B multi-engine averaging / path D Gemini 직접 EXTEND/BENT / combined). multi-view 옵션 0건.

본 plan 결과는 Plan 14 (5영상 재검증 sweep) 진입 게이트의 entry condition — 본 plan 통과 후 후속 plan 작업, 후속 plan 결과 후 Plan 14 진입.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-08-SUMMARY.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-11-SUMMARY.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-12-SUMMARY.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-13-SUMMARY.md
@backend/research/spikes/spike_rtmpose.py
@backend/research/spikes/spike_motionbert.py
@backend/research/spikes/mediapipe_to_h36m17.py
@backend/research/spikes/debug_gap_root_cause.py
@backend/shared/python/sunity_shared/analysis/features.py
@backend/shared/python/sunity_shared/analysis/temporal.py
@backend/shared/python/sunity_shared/analysis/skeleton.py
@CLAUDE.md

## Plan 13 verdict 적재 (2026-06-01) — 본 plan trace 책임 4 가설

ref-invert hold frame 88 단일 frame 측정 표 (Plan 13 SUMMARY 박제):

| joint | measured | target | gap | minimum_met |
|---|---|---|---|---|
| left_shoulder | 88.2도 | 180도 | 91.8도 | False |
| right_shoulder | **18.2도** | 180도 | **161.8도** | False |
| left_hip | 54.9도 | 180도 | 125.1도 | False |
| right_hip | 115.5도 | 180도 | 64.5도 | False |
| right_knee | 73.0도 | 180도 | 107.0도 | False |

Cross-engine inconsistency (동일 ref-invert 영상):

| measure | Plan 08 (MP+MB) | Plan 11 (RTMPose+MB) | Plan 13 (RTMPose+MB) |
|---|---|---|---|
| sampling | frame-mean | frame-mean | hold frame 88 단일 |
| overall | **92** | 70 | 5/5 minimum fail |

4 가설 (Plan 13 SUMMARY 박제) — 본 plan trace 분기:

| # | 가설 | 본 plan trace path |
|---|---|---|
| (a) | Gemini frame_idx=88 정확도 — hold 정점 아닌 transition 분류 | hold_window (88±5) 평균 vs frame 88 단일 비교 |
| (b) | RTMPose+MB lift 단일 frame 좌우 noise 폭주 (Plan 12 (e) 직접 연결) | RTMPose+MB frame 별 |L-R| 비대칭 trace, 영상 평균 baseline |
| (c) | lifter occlusion 자세 (거꾸로 매달림) 좌우 keypoint 헷갈림 | RTMPose+MB vs MP+MB 좌우 매핑 swap detection |
| (d) | RTMPose+MB lift 자체가 invert family 부적합 — multi-engine averaging 필요 | RTMPose+MB vs MP+MB frame-by-frame 각도 disagreement |

## 본 plan trace 핵심 4 비교

1. **Plan 08 reproduction sanity check** — 동일 ref-invert 영상 MP+MB path 재현 시 frame-mean overall 92 회복 여부. 회복 안 되면 reference angles / reference data 의존 가능성 (별 plan 후보), 회복되면 RTMPose+MB lift 가 root cause 박제 강화.
2. **Plan 13 frame 88 reproduction** — RTMPose+MB 단일 lift 가 frame 88 에서 Plan 13 측정값 (right_shoulder 18.2도 등) 재현되는지 확인. 재현되면 측정 자체 박제, 안 재현되면 spike pipeline 버그.
3. **hold_window 평균 vs 단일 frame** — frame 88±5 (11 frames) 평균 vs frame 88 단일. 평균이 정상 범위 (>=160도) 회복되면 단일 frame sampling 이 root cause = (a) dominant.
4. **Cross-engine disagreement** — RTMPose+MB vs MP+MB 두 lift path 의 frame 별 8 joint 평균 disagreement + 좌우 swap detection. >=30도 면 (d) strong, swap frame >=30% 면 (c) strong.

## 4 가설 verdict 임계 (모듈 상수, rationale 주석)

- `HYPOTHESIS_A_FRAME_VS_WINDOW_STRONG_DEG = 30.0` — frame 88 단일과 hold_window (88±5) 11 frame 평균 의 joint 별 평균 절대 차이. >=30도 면 단일 frame sampling = Gemini 시점 노이즈가 폭주의 직접 원인.
- `HYPOTHESIS_B_LR_ASYMMETRY_STRONG_DEG = 40.0` — RTMPose+MB 의 frame 별 4 pair (elbow / shoulder / hip / knee) |L - R| 평균. >=40도 면 단일 lift path 자체가 좌우 noise 폭주 (Plan 13 frame 88 의 left vs right delta 60-70도 와 직접 일치).
- `HYPOTHESIS_C_LR_SWAP_FRAME_RATIO_STRONG = 0.30` — RTMPose+MB 와 MP+MB 두 path 에서 좌/우 joint 의 부호 (L > R vs L < R) 가 반대인 frame 비율. >=0.30 (30%) 면 RTMPose+MB lift 가 occlusion 자세에서 좌우 매핑 swap 빈발.
- `HYPOTHESIS_D_CROSS_ENGINE_DISAGREEMENT_STRONG_DEG = 30.0` — RTMPose+MB 와 MP+MB 두 path 의 frame 별 8 joint 평균 disagreement. >=30도 면 두 lift path 자체가 invert family 에서 일치하지 않음 (Plan 12 (e) verdict 두 엔진 3D 분포 strong distance 220+ 와 직접 연결).
- `PLAN_13_FRAME_INDEX = 88` / `PLAN_13_HOLD_WINDOW = 5` (±5 = 11 frame)
- `PLAN_08_MP_MB_REF_INVERT_OVERALL = 92.0` / `PLAN_11_RTMPOSE_MB_REF_INVERT_OVERALL = 70.0`

## 후속 plan path 권고 분기 (verdict 합산)

| dominant | 후속 plan path 권고 | rationale |
|---|---|---|
| (a) strong 단독 | path D 조기 진입 권고 = Gemini moment 추출 prompt 재설계 (frame 정확도) 또는 path B-light = moment_dimensions 의 window 평균 채택 | 단일 frame sampling 만이 dominant 면 Plan 13 모듈 수정 (window=5 평균) 또는 Gemini frame 선택 정확도 개선으로 회복 |
| (b) strong | path B = multi-engine averaging 진입 권고 | RTMPose+MB 단일 lift 좌우 noise → MP+MB / RTMPose+MB 두 lift 평균 또는 voting |
| (c) strong | 좌우 매핑 sanity check 추가 권고 + path B 검토 | lifter occlusion swap detect → RTMPose+MB lift 의 좌우 sign correction layer 추가, 또는 MP+MB 단독 path 전환 검토 |
| (d) strong | path B = multi-engine averaging 진입 강제 | 단일 lift path 자체가 invert family 부적합 — 두 lift 평균 / voting / ensemble 필수. Plan 12 (e) verdict 와 일치. |
| (a)+(b) 동시 | path B + path D combined | 단일 frame + 단일 lift 둘 다 noise → multi-engine averaging + Gemini frame 정확도 동시 개선 |
| 모두 weak / rejected | path D 정식 진입 (Phase 5 조기) = Gemini 직접 EXTEND/BENT 분류 | 측정 path 자체 신뢰 불가 → Gemini 분류 결과로 측정 우회 (SCORE-01 정식 path 조기) |

**multi-view (다각도 촬영) 옵션은 모든 분기에서 0건 박제** — memory `single-camera-first-multi-view-last.md` 박제 (2026-06-01).
</context>

<scope_limits>
**In scope (본 plan, path A 우선):**

- 신규 spike CLI `backend/research/spikes/spike_measurement_trace.py` (T-1 ~ T-5)
- 두 lift path (RTMPose+MB / MP+MB) 동시 실행 + frame-by-frame angle trace + 좌우 비대칭 + cross-engine disagreement + 좌우 swap detection
- 4 가설 verdict 자동 분기 + 후속 plan path 권고
- 단위/스모크 테스트 (mmpose/torch/mediapipe import 0, stub angles fixture)
- belle Pod live mode (T-6, ref-invert 단독, ~5분)
- JSON + sibling Markdown 출력 (`backend/research/spikes/reports/spike_measurement_trace_<UTC>.{json,md}`)
- README append-only (Plan 16 섹션)

**Out of scope (영구 / 다른 plan / Phase):**

- **다각도 촬영 (multi-view) — 영구 자동 제외** (memory `single-camera-first-multi-view-last.md` 2026-06-01 박제). 본 plan 권고 / 후속 plan 옵션 / 코드 / 문서 / verdict 분기 어디서도 multi-view 언급 0건. belle 명시 지시 시에만 재진입.
- **path B (multi-engine averaging) 구현** — 본 plan verdict (b)/(c)/(d) strong 시 권고만, 별 plan.
- **path D (Gemini 직접 EXTEND/BENT) 정식 진입** — Phase 5 정식 path, 본 plan verdict 결과로 조기 진입 권고 가능. 구현은 별 plan.
- **5영상 sweep** — Plan 14 책임. 본 plan = ref-invert 단독.
- **NLF lift path** — Plan 12 (c) verdict 후 영구 폐기. 본 plan 호출 0.
- **Plan 13 모듈 호출** — gemini_moment_extractor / moment_dimensions / spike_gemini_moment 호출 0. frame_idx=88 박제 상수만 사용.
- **운영 코드 수정** — dimensions.py / functions / runpod_inference / shared/analysis/* / shared/judging/* 전부 0줄.
- **Plan 15 데이터 수정** — geometric_criterion.py / loader.py / 5영상 YAML 0줄.
- **기존 spike 수정** — spike_rtmpose / sweep_rtmpose / debug_dimensions / debug_gap_root_cause / spike_motionbert / rtmpose_to_h36m17 / mediapipe_to_h36m17 / spike_gemini_moment 0줄. 호출 only.
- **사람 점수 라벨링 / Gemini 호출** — 영구 금지 (memory `analysis-objectivity-no-human-scores.md`).
- **HybrIK / AlphaPose / NLF / SMPL-X / VideoPose3D 비교군** — 라이선스 차단 (memory `license-blocklist-pose.md`). 본 plan 비교군 = MP+MB (Apache 2.0) + RTMPose+MB (Apache 2.0) 만.
- **이모지** — 0건 (CLAUDE.md §7).
</scope_limits>

<tasks>

<task type="auto">
  <name>T-1: spike_measurement_trace.py 골격 + CLI args + 두 lift path 분기 + 모듈 상수 박제 (POSE-01)</name>
  <files>backend/research/spikes/spike_measurement_trace.py</files>
  <read_first>
    - backend/research/spikes/spike_rtmpose.py (RTMPose+MB pipeline 본체 — _resolve_video / _extract_frames / _run_rtmpose_2d / _load_motionbert / _run_motionbert_inference / _h36m17_to_coco17_subset 함수 시그너처 무수정 import 확인)
    - backend/research/spikes/spike_motionbert.py (MP+MB lift path — _load_motionbert / _run_motionbert_inference 재사용 패턴 확인)
    - backend/research/spikes/mediapipe_to_h36m17.py (MP 33 → H36M 17 변환 함수 시그너처 확인, 무수정 import)
    - backend/research/spikes/debug_gap_root_cause.py (Plan 12 5가설 trace 모듈 패턴 — argparse / verdict 임계 상수 / lazy import 패턴 참조)
    - backend/research/spikes/spike_gemini_moment.py (Plan 13 spike CLI 패턴 — 두 mode 분기 / JSON+MD sibling 출력 패턴 참조, 호출 0)
    - backend/shared/python/sunity_shared/analysis/skeleton.py (JOINT_KEYS = 8 joint, JOINT_ANGLES dict, KEYPOINT_NAMES — 본 spike 가 8 joint 한정 박제 확인)
    - backend/shared/python/sunity_shared/analysis/features.py (compute_joint_angles 시그너처 — inner angle 0-180도 박제 확인)
  </read_first>
  <action>
모듈 상단 docstring 에 본 plan 목적 + Plan 13 verdict 인용 + 4 가설 매트릭스 + multi-view 영구 제외 박제 명시 (memory `single-camera-first-multi-view-last.md` 인용) + 라이선스 박제 (MediaPipe Apache 2.0 + MotionBERT MIT + RTMPose Apache 2.0) + NLF 호출 0 박제.

모듈 상수 박제 (rationale 주석 필수): HYPOTHESIS_A_FRAME_VS_WINDOW_STRONG_DEG=30.0, HYPOTHESIS_A_WEAK_DEG=10.0, HYPOTHESIS_B_LR_ASYMMETRY_STRONG_DEG=40.0, HYPOTHESIS_B_WEAK_DEG=15.0, HYPOTHESIS_C_LR_SWAP_FRAME_RATIO_STRONG=0.30, HYPOTHESIS_C_WEAK_RATIO=0.10, HYPOTHESIS_D_CROSS_ENGINE_DISAGREEMENT_STRONG_DEG=30.0, HYPOTHESIS_D_WEAK_DEG=10.0, PLAN_13_FRAME_INDEX=88, PLAN_13_HOLD_WINDOW=5, PLAN_08_MP_MB_REF_INVERT_OVERALL=92.0, PLAN_11_RTMPOSE_MB_REF_INVERT_OVERALL=70.0, DEFAULT_MOTION="ref-invert", ENGINE_RTMPOSE_MB="rtmpose_mb", ENGINE_MEDIAPIPE_MB="mediapipe_mb".

4 LR pair tuple 박제 — (left_elbow, right_elbow), (left_shoulder, right_shoulder), (left_hip, right_hip), (left_knee, right_knee). skeleton.JOINT_KEYS 8 joint 와 매핑 정합 module-load assertion 추가 — 정합 안 맞으면 RuntimeError fail-loud.

`build_arg_parser()` 함수 — CLI args 10개 박제: --mode (default `report-only`, choices `report-only` / `live`) / --motion (default `ref-invert`) / --frame-index (default 88) / --hold-window (default 5) / --bucket (default `sunity-motion-pilot-videos`) / --rtmpose-config (default None, live 필수) / --rtmpose-checkpoint (default None, live 필수) / --motionbert-root (default `/workspace/MotionBERT`) / --motionbert-weights (default None, live 필수) / --out (default 자동 `backend/research/spikes/reports/spike_measurement_trace_<UTC ISO 8601>.json`).

`run_trace(...) -> dict` 함수 골격 — 두 lift path 분기만 박제, helper 호출은 T-2 / T-3 / T-4 가 채움. live mode 시 spike_rtmpose / mediapipe_to_h36m17 / spike_motionbert lazy import (모듈 로드 시점 mmpose / torch / mediapipe import 0).

`main()` 진입점 — argparse + run_trace + JSON dump + sibling Markdown 출력. report-only mode 는 stub angles fixture (T=20, J=8, frame_index=10) 으로 함수 검증만.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && PYTHONPATH=backend/shared/python:. backend/.venv/bin/python3 -c "import importlib, sys; m = importlib.import_module('backend.research.spikes.spike_measurement_trace'); assert m.HYPOTHESIS_A_FRAME_VS_WINDOW_STRONG_DEG == 30.0; assert m.PLAN_13_FRAME_INDEX == 88; assert m.PLAN_13_HOLD_WINDOW == 5; assert m.PLAN_08_MP_MB_REF_INVERT_OVERALL == 92.0; assert 'mmpose' not in sys.modules; assert 'torch' not in sys.modules; assert 'mediapipe' not in sys.modules; p = m.build_arg_parser(); ns = p.parse_args(['--motion', 'ref-invert']); assert ns.mode == 'report-only'; assert ns.frame_index == 88; assert ns.hold_window == 5; print('T-1 OK')"</automated>
  </verify>
  <acceptance_criteria>
    - 모듈 로드 시점 mmpose / torch / mediapipe / cv2 / boto3 import 0 (live helper lazy import 박제)
    - 모듈 상수 14개 박제 + 값 검증 통과
    - `build_arg_parser()` 10 args 박제 + default 값 정확
    - 모듈 docstring 에 (1) Plan 13 verdict 인용 (2) 4 가설 표 (3) multi-view 영구 제외 박제 (memory 인용) (4) 라이선스 박제 4 줄 포함
    - LR pair tuple 4개 + skeleton.JOINT_KEYS 와 정합 module-load assertion 통과
    - `git diff` HEAD 가 backend/functions/** / backend/runpod_inference/** / backend/shared/python/sunity_shared/analysis/** / backend/shared/python/sunity_shared/judging/** / backend/judging_data/** / 기존 spike 8개 모두 empty
    - 모듈 / docstring 내 `multi-view` / `다각도` / `multi camera` 키워드 0건 — `grep -v '^#'` 적용 후 0건 (영구 제외 박제 인용은 `single-camera-first-multi-view-last` 키워드만 사용)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>T-2: frame-by-frame trace 함수 (양 엔진 T x 8 + 좌우 비대칭 + hold_window 평균) + 단위 테스트</name>
  <files>backend/research/spikes/spike_measurement_trace.py, backend/tests/test_spike_measurement_trace.py</files>
  <read_first>
    - backend/shared/python/sunity_shared/analysis/features.py (compute_joint_angles — input (T, 17, 4) keypoints, output (T, J=len(JOINT_KEYS)) angles 시그너처, NaN-safe 검증)
    - backend/shared/python/sunity_shared/analysis/temporal.py (temporal_fill — NaN-aware fill, Plan 13 spike 호출 패턴 참조)
    - backend/shared/python/sunity_shared/analysis/skeleton.py (JOINT_KEYS 정확 8 joint 순서 확인)
    - backend/research/spikes/spike_rtmpose.py (_h36m17_to_coco17_subset 의 output shape (T, 17, 4) — xyz + score 확인)
    - backend/tests/test_spike_gemini_moment_smoke.py (Plan 13 smoke 테스트 패턴 — stub angles fixture 작성법 참조)
  </read_first>
  <action>
spike_measurement_trace.py 에 함수 3개 추가 (모두 numpy-only, mmpose/torch import 0):

`trace_angles_per_frame(angles_tj, frame_index, hold_window) -> dict` — output dict keys: frame_single (dict joint_key → angle), frame_window_mean, frame_window_min, frame_window_max, window_indices (list[int]), frame_vs_window_disagreement_per_joint (dict joint_key → abs_diff), mean_disagreement_deg (float). frame_index 범위 초과 ValueError. window = [max(0, frame_index - hold_window), min(T, frame_index + hold_window + 1)]. NaN-aware (np.nanmean / nanmin / nanmax). mean_disagreement = 8 joint 의 abs(frame_single - frame_window_mean) 의 nanmean.

`compute_lr_asymmetry(angles_tj) -> dict` — output dict keys: per_frame (np.ndarray shape (T, 4)), per_pair (dict pair_name → mean_abs_diff), overall_mean_abs_lr_deg (float). 4 LR pair frame 별 |L - R| 산출. per_pair = 영상 전체 평균 |L - R| (pair 별). overall_mean_abs_lr = 4 pair 의 영상 전체 평균.

`compute_hold_window_lr(angles_tj, frame_index, hold_window) -> dict` — frame_index 주변 hold_window 의 |L - R| 평균 (frame 88 단일 vs hold_window 평균 비교용). output dict keys: frame_single_lr (dict pair_name → abs_diff), frame_window_mean_lr (dict pair_name → mean_abs_diff).

각 함수에 NaN-safe + shape assertion + docstring 1 줄 rationale.

테스트 (`backend/tests/test_spike_measurement_trace.py` 신규) 3 클래스 12 tests:
- `TestTraceAnglesPerFrame` (5 tests): stub (T=20, J=8) all 90도 → disagreement 0 / frame 88 단일 ≠ window 평균 시 disagreement > 0 / frame_index 범위 초과 ValueError / hold_window window 경계 (frame=0, frame=T-1) / NaN frame 포함 시 nanmean 동작
- `TestComputeLrAsymmetry` (4 tests): all 90도 → |L-R| 모두 0 / 한 pair 만 L=180/R=90 → per_pair 해당 pair 90도 나머지 0도 / (T, 4) shape / NaN frame nanmean
- `TestComputeHoldWindowLr` (3 tests): frame 단일 vs window 평균 분리 / shape / 경계 frame

테스트는 numpy-only. mmpose/torch import 0.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && PYTHONPATH=backend/shared/python:. backend/.venv/bin/python3 -m pytest backend/tests/test_spike_measurement_trace.py::TestTraceAnglesPerFrame backend/tests/test_spike_measurement_trace.py::TestComputeLrAsymmetry backend/tests/test_spike_measurement_trace.py::TestComputeHoldWindowLr -v 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - 12 tests PASS, 실행 시간 < 1s
    - 함수 3개 모두 NaN-aware + shape assertion + docstring rationale
    - 테스트 import 에 mmpose / torch / mediapipe 0건 (`grep -v '^#' backend/tests/test_spike_measurement_trace.py | grep -E '^import|^from' | grep -E 'mmpose|torch|mediapipe' | wc -l` 결과 0)
    - LR pair 매핑이 skeleton.JOINT_KEYS 와 정합 (테스트 내 assertion)
    - 기존 spike 8개 무수정 (git diff empty)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>T-3: Cross-engine disagreement + 좌우 swap detection + 가설 verdict 함수 (4 가설 분기) + 단위 테스트</name>
  <files>backend/research/spikes/spike_measurement_trace.py, backend/tests/test_spike_measurement_trace.py</files>
  <read_first>
    - backend/research/spikes/debug_gap_root_cause.py (Plan 12 가설 verdict 함수 패턴 — strong / weak / rejected / inconclusive 4 단계 + 임계 상수 사용)
    - backend/research/spikes/spike_measurement_trace.py (T-1/T-2 박제 상수 + 3 함수 시그너처 확인)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-13-SUMMARY.md (4 가설 매트릭스 인용)
  </read_first>
  <action>
spike_measurement_trace.py 에 함수 4개 추가:

`compute_cross_engine_disagreement(rtmpose_angles, mediapipe_angles) -> dict` — input: 두 (T, 8) angle matrix. output dict keys: per_frame_mean_disagreement (np.ndarray (T,)), per_joint_mean_disagreement (dict joint_key → mean_abs_deg), overall_mean_disagreement_deg (float), peak_disagreement_frame_idx (int). 두 matrix 의 T 가 다르면 짧은 쪽 truncate. NaN-aware.

`detect_lr_swap(rtmpose_angles, mediapipe_angles, lr_pairs) -> dict` — input: 두 (T, 8) + 4 LR pair tuple. output dict keys: swap_frames_per_pair (dict pair_name → list[int]), swap_frame_ratio (float, 0~1), swap_frame_ratio_per_pair (dict pair_name → float). 각 frame 별로 rtmpose 의 sign(L - R) 과 mediapipe 의 sign(L - R) 비교 — 부호 반대인 frame = swap frame. NaN frame 은 swap 판정에서 제외.

`verdict_hypothesis(measure_value, strong_threshold, weak_threshold) -> str` — utility. measure >= strong → "strong", measure >= weak → "weak", else → "rejected". NaN → "inconclusive".

`aggregate_verdicts(trace_a, trace_b, trace_c, trace_d) -> dict` — input: 4 가설 trace 결과 (T-2/T-3 함수 결과). output dict keys: hypotheses ({a: {value, verdict}, b: {...}, c: {...}, d: {...}}), dominant (list[str] of "strong" hypotheses), next_path_recommendation (str). next_path_recommendation 분기 룰 (`<context>` 표 참조):
- (a) strong 단독 → "path D 조기 진입 또는 path B-light (window 평균)"
- (b) strong → "path B (multi-engine averaging) 진입"
- (c) strong → "좌우 매핑 sanity check 추가 + path B 검토"
- (d) strong → "path B (multi-engine averaging) 진입 강제"
- (a)+(b) 동시 → "path B + path D combined"
- 모두 weak / rejected → "path D 정식 진입 (Phase 5 조기) — Gemini 직접 EXTEND/BENT"

multi-view 키워드는 next_path_recommendation 의 어느 분기에도 0건 박제 (assertion 으로 검증 가능).

테스트 4 클래스 추가:
- `TestComputeCrossEngineDisagreement` (4 tests): 두 동일 matrix → 0 / 30도 차이 stub → 30도 / NaN frame 처리 / T 다름 truncate
- `TestDetectLrSwap` (4 tests): 두 matrix 부호 동일 → swap 0 / 부호 반대 50% frame → swap ratio 0.5 / pair 별 분리 / NaN 제외
- `TestVerdictHypothesis` (4 tests): strong / weak / rejected / NaN inconclusive
- `TestAggregateVerdicts` (6 tests): (a) only / (b) only / (c) only / (d) only / (a)+(b) / 모두 weak — 각 분기 next_path_recommendation 문자열 매칭 + multi-view 키워드 0건 assertion
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && PYTHONPATH=backend/shared/python:. backend/.venv/bin/python3 -m pytest backend/tests/test_spike_measurement_trace.py::TestComputeCrossEngineDisagreement backend/tests/test_spike_measurement_trace.py::TestDetectLrSwap backend/tests/test_spike_measurement_trace.py::TestVerdictHypothesis backend/tests/test_spike_measurement_trace.py::TestAggregateVerdicts -v 2>&1 | tail -25 && grep -v '^[[:space:]]*#' /Users/kimtaesung/Dev/SunityMotion/backend/research/spikes/spike_measurement_trace.py | grep -iE 'multi.view|multi.camera|다각도' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - 18 tests PASS (12 from T-2 + 6 신규 클래스 = 18 + 12 = 신규 18 추가 총 30 PASS)
    - 함수 4개 추가 + NaN-aware + docstring rationale
    - next_path_recommendation 6 분기 모두 multi-view / 다각도 / multi camera 키워드 0건 (grep 결과 0)
    - TestAggregateVerdicts 모든 6 분기 assert next_path_recommendation 내 "multi-view" 0건 명시 검증
    - 기존 spike 8개 / 운영 코드 / Plan 13 모듈 / Plan 15 데이터 전부 무수정 (git diff empty)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>T-4: JSON+Markdown 출력 + report-only mode end-to-end + 스모크 테스트</name>
  <files>backend/research/spikes/spike_measurement_trace.py, backend/tests/test_spike_measurement_trace_smoke.py</files>
  <read_first>
    - backend/research/spikes/spike_gemini_moment.py (Plan 13 spike — JSON+MD sibling 출력 패턴 / generate_markdown 시그너처 참조)
    - backend/research/spikes/debug_gap_root_cause.py (Plan 12 spike — report-only mode JSON shape 패턴)
    - backend/research/spikes/spike_measurement_trace.py (T-1/T-2/T-3 박제 함수 호출 통합)
    - backend/tests/test_spike_gemini_moment_smoke.py (Plan 13 smoke 패턴)
  </read_first>
  <action>
spike_measurement_trace.py 에 함수 2개 + main 완성:

`generate_markdown(trace_result) -> str` — JSON 결과 → Markdown sibling 생성. 섹션: (1) 메타 (mode / motion / frame_index / hold_window / generated_at), (2) RTMPose+MB trace (frame 88 단일 vs window 평균 표, 8 joint), (3) MP+MB trace (동일 형식), (4) 좌우 비대칭 (4 pair, 양 엔진 비교), (5) cross-engine disagreement (joint 별 평균), (6) 좌우 swap detection (pair 별 swap frame 비율), (7) 4 가설 verdict 매트릭스, (8) next_path_recommendation, (9) Plan 08 / Plan 11 / Plan 13 cross-engine inconsistency baseline 표.

`run_trace(...)` 함수 완성 — report-only mode 시 stub angles fixture (T=20, J=8) 두 set (RTMPose+MB stub / MP+MB stub) 생성 → 7 함수 호출 → JSON dict 반환. live mode 시 lazy import + 두 lift path 직렬 실행 (RTMPose+MB 먼저 ~3분, MP+MB 다음 ~2분) → 동일 함수 chain.

`main()` 완성 — argparse + run_trace + json.dump (default=str for numpy) + generate_markdown → sibling .md 파일.

JSON top-level keys: generated_at, mode, motion, frame_index, hold_window, plan_13_inputs (Plan 13 5/5 fail 측정 표 박제 reference), engines (dict ENGINE_RTMPOSE_MB / ENGINE_MEDIAPIPE_MB → trace_angles_per_frame + compute_lr_asymmetry 결과), cross_engine (compute_cross_engine_disagreement + detect_lr_swap), verdicts (aggregate_verdicts), baseline_inconsistency (Plan 08 92 / Plan 11 70 / Plan 13 5/5 fail 박제 표).

스모크 테스트 (`backend/tests/test_spike_measurement_trace_smoke.py` 신규) 5 클래스 11 tests:
- `TestCli` (4 tests): mode default / motion default / frame_index default / hold_window default
- `TestDefaultOutPath` (1 test): reports/ 아래 + UTC ISO 8601 형식
- `TestReportOnlyMode` (2 tests): stub angles → JSON shape 검증 (top-level keys 8개) + sibling .md 파일 실제 생성
- `TestVerdictBranches` (3 tests): (a) dominant stub / (b) dominant stub / 모두 rejected stub — next_path_recommendation 문자열 매칭
- `TestLiveModeGuards` (1 test): live mode + rtmpose-config 누락 → ValueError

모든 테스트 mmpose / torch / mediapipe import 0.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && PYTHONPATH=backend/shared/python:. backend/.venv/bin/python3 -m pytest backend/tests/test_spike_measurement_trace_smoke.py -v 2>&1 | tail -25 && PYTHONPATH=backend/shared/python:. backend/.venv/bin/python3 -m backend.research.spikes.spike_measurement_trace --mode report-only --motion ref-invert --out /tmp/spike_measurement_trace_test.json && python3 -c "import json; d = json.load(open('/tmp/spike_measurement_trace_test.json')); assert 'verdicts' in d; assert 'cross_engine' in d; assert 'engines' in d; assert 'baseline_inconsistency' in d; import os; assert os.path.exists('/tmp/spike_measurement_trace_test.md'); print('T-4 OK')"</automated>
  </verify>
  <acceptance_criteria>
    - 11 smoke tests PASS, 실행 시간 < 1s
    - report-only mode end-to-end 실행 시 JSON + sibling .md 둘 다 생성
    - JSON top-level keys 8개 모두 존재 (generated_at / mode / motion / frame_index / hold_window / engines / cross_engine / verdicts / baseline_inconsistency)
    - generate_markdown 출력에 multi-view / 다각도 / multi camera 키워드 0건
    - Markdown sibling 의 9 섹션 모두 존재
    - 운영 코드 / Plan 13 모듈 / Plan 15 데이터 / 기존 spike 8개 무수정 (git diff empty)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>T-5: README append-only (Plan 16 섹션) + SUMMARY 박제 (status pending_belle_live)</name>
  <files>backend/research/spikes/README.md, .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-16-SUMMARY.md</files>
  <read_first>
    - backend/research/spikes/README.md (Plan 07/08/10/11/12/13 섹션 보존 확인, append-only 패턴)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-13-SUMMARY.md (Plan 13 SUMMARY 구조 — TL;DR 표 / Self-Check / Verdict 요약 / belle Pod 결과 섹션 패턴)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-12-SUMMARY.md (Plan 12 SUMMARY — 5가설 verdict 매트릭스 패턴, dominant 표기, next path 권고 패턴)
    - @/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
  </read_first>
  <action>
README.md append-only — Plan 16 섹션 추가 (Plan 07/08/10/11/12/13 섹션 모두 보존). 섹션 구성:
- 목적 (Plan 13 verdict measurement_unreliable_blocked 후속, path A 우선)
- 라이선스 표 2 줄 (MediaPipe Apache 2.0 / MotionBERT MIT / RTMPose Apache 2.0). NLF 호출 0 박제.
- 추가 파일 4개 (spike_measurement_trace.py + 테스트 2 + SUMMARY)
- report-only mode 로컬 실행 예시 (`python3 -m backend.research.spikes.spike_measurement_trace --mode report-only --motion ref-invert`)
- live mode belle Pod 5단계 절차 (pull / 환경 확인 / spike 실행 / 결과 공유 / SUMMARY 갱신)
- 4 가설 verdict 매트릭스 (a/b/c/d 임계 + dominant 분기 권고)
- 주의사항 8항목: (1) multi-view 옵션 영구 0건 박제 (2) NLF 호출 0 (3) 8 joint 한정 (4) Plan 13 모듈 호출 0 (5) Plan 15 데이터 무수정 (6) 기존 spike 8개 무수정 (7) 사람 점수 0건 (8) Gemini 호출 0 (본 plan 은 측정 trace 만)

git diff README.md 가 18 (Plan 07/08/10/11/12/13) + Plan 16 새 # heading 들만 추가 — 0 deletion 확인.

SUMMARY 작성 (`.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-16-SUMMARY.md`) — frontmatter status `pending_belle_live`. 본문 구성:
- TL;DR 표 (verdict pending_belle_live / scope path A trace / 신규 파일 4 / 단위+스모크 테스트 ~41 PASS / 운영 코드 무수정 0 / Plan 13 모듈 무수정 0 / 기존 spike 무수정 0 / NLF 호출 0 / multi-view 0건 / 이모지 0건 / 사람 점수 0건)
- Plan 13 verdict 인용 (measurement_unreliable_blocked + 5/5 fail 측정 표)
- 4 가설 매트릭스 (a/b/c/d 임계 + trace path) — T-6 결과 적재 placeholder
- 모듈 구조 박제 (T-1~T-4 함수 9개 + 모듈 상수 14개)
- 단위/스모크 테스트 표 (~41 PASS, 클래스 별)
- T-6 belle Pod live mode 실행 절차 (5단계)
- belle 응답 옵션 6개 (Plan 13 SUMMARY 패턴) — dominant (a) / dominant (b) / dominant (c) / dominant (d) / 다중 dominant combined / 모두 weak path D
- 핵심 결정 박제 8항목 (must_haves.truths 8개 본문 명시)
- Deviations from Plan (executor 실 구현 차이 있으면)
- Self-Check (파일 존재 / 운영 코드 무수정 / 기존 spike 무수정 / Plan 13 모듈 무수정 / Plan 15 데이터 무수정 / multi-view 키워드 0건 / 사람 점수 0건)
- Verdict 요약 (verdict pending_belle_live / next action belle Pod live mode)
- belle Pod live mode 결과 placeholder (T-6 적재 대기)
  </action>
  <verify>
    <automated>grep -c '^# Plan' backend/research/spikes/README.md && grep -v '^[[:space:]]*#' backend/research/spikes/README.md | grep -ciE 'multi.view|다각도|multi camera' && test -f .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-16-SUMMARY.md && head -5 .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-16-SUMMARY.md | grep -c 'pending_belle_live'</automated>
  </verify>
  <acceptance_criteria>
    - README.md `grep -c '^# Plan'` >= 7 (Plan 07/08/10/11/12/13 + Plan 16) — 기존 섹션 보존 확인
    - README.md 본문에 multi-view / 다각도 / multi camera 키워드 (주석 제외) 0건
    - SUMMARY.md 존재 + frontmatter status `pending_belle_live` 박제
    - SUMMARY 본문 12 섹션 모두 존재 (TL;DR / Plan 13 인용 / 4 가설 / 모듈 구조 / 테스트 / T-6 절차 / belle 응답 옵션 / 핵심 결정 / Deviations / Self-Check / Verdict / belle 결과 placeholder)
    - 운영 코드 / Plan 13 모듈 / Plan 15 데이터 / 기존 spike 8개 무수정 (git diff empty)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>T-6: belle Pod live mode 실행 (ref-invert 단독, ~5분) + 결과 적재 + SUMMARY 갱신</name>
  <what-built>
T-1 ~ T-5 executor 가 spike_measurement_trace.py + 테스트 + README + SUMMARY placeholder 박제 완료. 본 task = belle Pod 에서 live mode 1회 실행 + 결과 .json/.md Claude 공유 + SUMMARY frontmatter status + 본문 결과 갱신.

ref-invert 단독 (Plan 15 1차 박제 + Plan 13 verdict 측정 영상). 두 lift path (RTMPose+MB + MediaPipe+MB) 직렬 실행, 약 5분 소요 (RTMPose+MB ~3분 + MP+MB ~2분).
  </what-built>
  <how-to-verify>
**belle Pod 실행 절차 (5단계)**:

1. 코드 최신화:
   ```
   cd /workspace/SunityMotion
   git pull --ff-only origin main
   ```

2. 환경 확인 (Pod 환경 보존, 추가 install 0 — STATE.md 2026-06-01 박제):
   ```
   python3 -c "import torch; import mmpose; import mediapipe; print(torch.__version__, mmpose.__version__, mediapipe.__version__)"
   ```
   기대값: torch 2.4.1+cu124, mmpose 1.3.2, mediapipe 0.10.x. 정합 안 맞으면 STOP.

3. spike 실행 (ref-invert 단독, ~5분):
   ```
   python3 -m backend.research.spikes.spike_measurement_trace \
     --mode live \
     --motion ref-invert \
     --frame-index 88 \
     --hold-window 5 \
     --bucket sunity-motion-pilot-videos \
     --rtmpose-config /workspace/rtmpose_weights/rtmpose-l_8xb256-420e_coco-256x192.py \
     --rtmpose-checkpoint /workspace/rtmpose_weights/rtmpose-l_simcc-coco_pt-aic-coco_420e-256x192-1352a4d2_20230127.pth \
     --motionbert-root /workspace/MotionBERT \
     --motionbert-weights /workspace/MotionBERT/checkpoint/pose3d/FT_MB_lite_MB_ft_h36m_global_lite/best_epoch.bin \
     --out backend/research/spikes/reports/spike_measurement_trace_live_$(date +%Y%m%d_%H%M).json
   ```

4. 결과 파일 확인 — `.json` + `.md` 둘 다 존재 + JSON top-level keys 8개 모두 있는지 확인 + verdicts 의 4 가설 모두 strong/weak/rejected/inconclusive 한 값 박제 + multi-view 키워드 0건.

5. 결과 Claude 공유 — `.json` + `.md` 내용 그대로 전달. Claude 가 SUMMARY 갱신:
   - frontmatter status `pending_belle_live` → `belle_live_complete` (dominant 가설별 verdict tag 추가)
   - 본문 `belle Pod live mode 결과` 섹션 신설 — verdict 표 + dominant 가설 + next_path_recommendation + Plan 14 진입 결정 박제
   - STATE.md Current Position + Last activity 갱신
   - ROADMAP.md Plan 16 진행률 행 [~] → [x] + Plan 14 entry 갱신 (본 plan 통과 후 후속 plan path)

**검증 임계 (Claude 가 결과 적재 시 확인)**:
- JSON / Markdown 에 multi-view / 다각도 / multi camera 키워드 0건 (`grep -v '^[[:space:]]*#'` 적용 후 0)
- 좌표 / 점수 / 판단 키워드 0건 (사람 점수 라벨링 0건 확인)
- 4 가설 verdict 모두 산출 (strong/weak/rejected/inconclusive 4 단계 중 하나)
- next_path_recommendation 6 분기 중 1개 산출
- RTMPose+MB frame 88 single 측정값이 Plan 13 측정값 (right_shoulder 18.2도 등) 과 1도 이내로 일치하는지 별도 박제 (재현 검증)
- MP+MB frame 88 single 측정값 별도 박제 (cross-engine 비교)
  </how-to-verify>
  <resume-signal>belle 가 `.json` + `.md` 결과 + 재현 검증 결과 (Plan 13 frame 88 측정값 재현 여부) Claude 공유 후 "Plan 16 결과 적재"</resume-signal>
</task>

</tasks>

<verification>

## Plan 16 통과 게이트 (Plan 14 entry condition)

- [ ] T-1 ~ T-5 executor 통과 (단위/스모크 테스트 ~41 PASS)
- [ ] T-6 belle Pod live mode 실행 완료 + 결과 적재
- [ ] 4 가설 verdict (a/b/c/d) 모두 strong/weak/rejected/inconclusive 4 단계 중 1개 산출
- [ ] dominant 가설 1~2개 박제
- [ ] next_path_recommendation 6 분기 중 1개 산출 (multi-view 옵션 0건 자동 확인)
- [ ] Plan 13 frame 88 측정값 재현 검증 (1도 이내) — 재현 안 되면 spike pipeline 버그 (별 task)
- [ ] 운영 코드 / Plan 13 모듈 / Plan 15 데이터 / 기존 spike 8개 전부 무수정 (git diff empty)
- [ ] multi-view / 다각도 / multi camera 키워드 0건 (코드 + 테스트 + README + SUMMARY + JSON + MD 모두)
- [ ] 사람 점수 라벨링 0건 / Gemini 호출 0건
- [ ] 이모지 0건

## 판정 기준 — Plan 14 진입 결정

| 본 plan 결과 | Plan 14 진입 |
|---|---|
| dominant (a) 만 — Plan 13 모듈 window 평균 수정 또는 Gemini frame 정확도 개선 | 후속 plan (Plan 17 후보) 통과 후 Plan 14 |
| dominant (b)/(c)/(d) — multi-engine averaging 필요 | path B 별 plan 작업 후 Plan 14 |
| 다중 dominant combined | path B + path D 별 plan 둘 다 작업 후 Plan 14 |
| 모두 weak/rejected — 측정 path 자체 신뢰 불가 | path D 정식 진입 (Phase 5 조기) — Gemini 직접 EXTEND/BENT 분류 path |

본 plan 단독으로는 Plan 14 진입 보장 X — 후속 plan path 결과 후 진입.

</verification>

<success_criteria>

본 plan 완료 시점 = belle Pod live mode 결과 적재 + dominant 가설 박제 + 후속 plan path 권고 합의:

1. spike_measurement_trace.py + 테스트 2개 + README append + SUMMARY 5 파일 박제 완료
2. 단위+스모크 테스트 ~41 PASS (mmpose/torch/mediapipe import 0)
3. report-only mode end-to-end JSON + sibling Markdown 생성 검증 통과
4. belle Pod live mode (ref-invert 단독) 실행 + JSON+MD 결과 적재
5. 4 가설 verdict 산출 + dominant 1~2개 박제 + next_path_recommendation 1개 산출
6. Plan 13 frame 88 측정값 재현 검증 (RTMPose+MB 단일 path, 1도 이내)
7. multi-view 옵션 0건 / 사람 점수 0건 / 이모지 0건 / NLF 호출 0건 박제
8. 운영 코드 / Plan 13 모듈 / Plan 15 데이터 / 기존 spike 8개 무수정 (git diff empty)
9. SUMMARY 갱신 + STATE.md + ROADMAP.md 진행률 갱신
10. 후속 plan path 권고 박제 — 본 plan 단독 Plan 14 진입 보장 X, 후속 plan 결과 후

</success_criteria>

<risk>

## Risk 1: belle Pod 환경 변화

Pod 환경이 STATE.md 2026-06-01 박제 (torch 2.4.1+cu124 / mmpose 1.3.2 / mediapipe 0.10.x) 와 다르면 spike 실패. T-6 절차 2단계가 환경 확인 게이트 — 정합 안 맞으면 STOP + belle 에 알림.

## Risk 2: MP+MB lift path 재현 실패

Plan 08 (MP+MB) frame-mean 92 가 동일 영상에서 재현 안 되면 reference angles / reference data 의존 가능성 — 별 plan 후보 (본 plan scope 외, but SUMMARY 에 박제).

## Risk 3: Plan 13 frame 88 측정값 재현 실패

RTMPose+MB 단일 lift 가 frame 88 에서 Plan 13 측정값 (right_shoulder 18.2도 등) 1도 이내로 재현 안 되면 spike pipeline 버그 가능성 — Plan 13 spike 와 본 plan spike 의 chain 차이 확인 필요 (별 task).

## Risk 4: 4 가설 모두 inconclusive

stub angles fixture 가 verdict 함수의 임계를 모두 넘기지 못해 inconclusive 만 나오면 단위 테스트 통과 의미 약함. T-3 의 TestAggregateVerdicts 가 6 분기 모두 강제 stub 으로 검증 → 임계 분기 자체는 검증됨.

## Risk 5: 다른 motion 으로 cross-engine inconsistency 비교 부재

본 plan = ref-invert 단독. 다른 4 motion (ref-climb / ref-foxtop-split / ref-foxtop / ref-sideway-spin) 에서도 동일 cross-engine 4-5배 차이가 나는지 확인 X — Plan 14 5영상 sweep 책임. 본 plan 결과만으로 일반화 불가.

</risk>

<open_questions>

1. **Plan 13 frame 88 측정값 재현 결과** — RTMPose+MB 단일 lift 가 1도 이내 재현되는가? 재현 안 되면 spike pipeline 차이 별 task 필요.
2. **MP+MB ref-invert frame-mean 92 재현 결과** — Plan 08 결과가 본 plan 의 MP+MB path 에서 재현되는가? 재현 안 되면 reference data 의존 가능성 별 plan.
3. **dominant 가설** — 4 가설 중 어느 것이 strong 인가? 권고 path B / D / combined 결정 근거.
4. **Plan 13 모듈 수정 여부** — (a) dominant 시 moment_dimensions 의 window 평균 채택 = 운영 dimensions.py 수정 vs Plan 13 모듈 수정 vs 신규 모듈 — 별 plan 책임.

</open_questions>

<next_step>

```
T-1 ~ T-5 executor 실행 (단위/스모크 테스트 ~41 PASS)
  → README append + SUMMARY placeholder 박제
  → belle Pod live mode (T-6, ref-invert 단독, ~5분)
  → 결과 .json + .md Claude 공유
  → Plan 13 frame 88 재현 검증
  → SUMMARY 갱신 (status pending_belle_live → belle_live_complete)
  → dominant 가설 + next_path_recommendation 박제
  → STATE.md + ROADMAP.md 갱신
  → belle 응답 (6 옵션 중 1)
  → 후속 plan (path B / path D / combined) PLAN 작성
  → 후속 plan 통과 후 Plan 14 5영상 sweep PLAN 작성
```

multi-view 옵션은 모든 분기에서 0건 박제 — memory `single-camera-first-multi-view-last.md` (2026-06-01 박제) 유지.

</next_step>
