---
phase: 01-poseengine-mediapipe-nlf-r-d
plan: 17
type: fix
wave: 2
depends_on:
  - 01-08  # MP+MB 5영상 baseline — ref-invert frame-mean 92 (Plan 17 T-4 reproduction target)
  - 01-11  # RTMPose+MB 5영상 sweep — ref-invert frame-mean 70 (swap fix 전 비교군)
  - 01-12  # (d) keypoint chain 17/17 다름 verdict (chain ordering = 본 plan 좌우 swap 의 systematic 부산물)
  - 01-13  # verdict measurement_unreliable_blocked — 본 plan = 측정 신뢰도 entry gate 해소 책임
  - 01-16  # belle Pod live mode verdict dominant ['b', 'c', 'd'] — left_elbow swap_ratio 1.00 / cross-engine left_elbow 75.88° (본 plan 의 root cause 박제)
files_modified:
  - backend/research/spikes/rtmpose_to_h36m17.py   # T-2 swap fix 대상 (audit 결과로 결정 — 최소 변경)
  - backend/research/spikes/mediapipe_to_h36m17.py # T-2 swap fix 대상 (audit 결과로 결정 — 최소 변경)
  - backend/research/spikes/reports/mapping_audit_<UTC>.md   # T-1 audit 보고서 (신규)
  - backend/tests/test_mapping_audit.py             # T-1 audit 결과 박제 + assertion 단위 테스트 (신규)
  - backend/tests/test_lr_mapping_consistency.py    # T-3 swap fix 검증 단위 테스트 (신규)
  - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-17-SUMMARY.md  # T-5 verdict + Plan 14 진입 게이트 결정 (신규)
autonomous: false
parent_plan: 01-16
parent_verdict: "Plan 16 belle Pod live mode verdict dominant ['b', 'c', 'd'] (2026-06-01, spike_measurement_trace_live_20260601_1144) — (a) Gemini frame_idx 정확도 4.10° rejected (frame 88 정확, 단일 frame sampling 아님), (b) RTMPose+MB 영상 평균 |L-R| 43.14° strong (≥40°), (c) lifter occlusion 좌우 매핑 swap 0.37 strong (≥0.30), (d) cross-engine 각도 disagreement 34.57° strong (≥30°). Pair 별 swap_ratio: left_elbow_vs_right_elbow = 1.00 (173 frame 모두 swap, 통계적 우연 불가) / left_shoulder_vs_right_shoulder = 0.01 / left_hip_vs_right_hip = 0.32 / left_knee_vs_right_knee = 0.14. Cross-engine joint별 disagreement: left_elbow = 75.88° (peak) / left_knee = 68.72° / left_shoulder = 45.23° / right_elbow = 8.91° (정상). left_elbow swap_ratio 1.00 + disagreement 75.88° 가 직접 일치 = dominant root cause = systematic 좌우 keypoint 매핑 swap. Plan 12 (d) verdict (chain ordering 17/17 다름) 의 구체적 manifestation. Plan 08 (MP+MB) 92 vs Plan 11 (RTMPose+MB) 70 의 22점 차이 = swap 의 누적 효과."
requirements:
  - POSE-01  # Phase 1 Success Criteria #7 entry gate — 측정 신뢰도 확보 (좌우 keypoint 매핑 systematic swap 해소)

must_haves:
  truths:
    - "dominant root cause = 좌우 keypoint 매핑 swap. left_elbow swap_ratio 1.00 (Plan 16 박제) 가 systematic bug 의 1차 박제. swap fix 후 swap_ratio ≤ 0.05 (T-4 belle Pod 재검증 임계). audit (T-1) 결과로 어느 엔진 (RTMPose / MediaPipe) 의 어느 joint mapping 이 swap 인지 박제 후 fix."
    - "swap fix 변경 line 최소화 — file 당 최대 5줄 (목표 1-2줄). T-1 audit 가 식별한 매핑만 수정. 다른 joint 매핑 / shape / NaN propagation / image_size 정규화 / score threshold 처리 등 전부 무수정 박제 (회귀 위험 제거)."
    - "운영 코드 전부 무수정 — functions/** / runpod_inference/** / shared/python/sunity_shared/analysis/** / shared/python/sunity_shared/judging/** / shared/python/sunity_shared/pose_lifters/** 0줄. dimensions.py / technique.py / FallbackRecognizer / pose_estimator.py / features.py / temporal.py / skeleton.py 무수정 박제. git diff HEAD 가 file 6개 (rtmpose_to_h36m17 / mediapipe_to_h36m17 / 2 신규 테스트 / audit 보고서 / SUMMARY) 만 변경."
    - "Plan 13 모듈 무수정 — gemini_moment_extractor.py / moment_dimensions.py / spike_gemini_moment.py 0줄. Plan 15 데이터 무수정 — geometric_criterion.py / loader.py / 5영상 YAML 0줄. Plan 16 모듈 무수정 — spike_measurement_trace.py / test_spike_measurement_trace*.py 0줄 (T-4 재검증 도구로 그대로 사용). 다른 기존 spike 무수정 — spike_rtmpose.py / sweep_rtmpose.py / debug_dimensions.py / debug_gap_root_cause.py / spike_motionbert.py 0줄."
    - "NLF 호출 0 — Plan 12 (c) verdict 후 영구 폐기 (memory `license-blocklist-pose.md` 박제). 본 plan 의 검증 엔진 = RTMPose+MB (Apache 2.0) + MediaPipe+MB (Apache 2.0) 두 path 만. T-4 belle Pod 재검증도 NLF 호출 0."
    - "8 angle joints (skeleton.JOINT_KEYS = left/right × elbow/shoulder/hip/knee) 한정 — derive joint (hip center / spine / thorax / neck_nose / head) 매핑 0줄 수정. Plan 16 LR_PAIRS 4 tuple 정합 유지."
    - "다각도 촬영 (multi-view) 옵션 영구 자동 제외 — memory `single-camera-first-multi-view-last.md` 박제 (2026-06-01). 본 plan 코드 / 권고 / 분기 / 문서 / 테스트 / audit 보고서 / SUMMARY 어디서도 multi-view 언급 0건. belle 명시 지시 시에만 재진입."
    - "Gemini API 호출 0건 — 본 plan 은 측정 path fix 자체. Gemini moment 추출 (Plan 13 책임) / Gemini frame 정확도 검증 (Plan 16 (a) 가설 rejected 박제) 호출 0건. 사람 점수 라벨링 0건 (memory `analysis-objectivity-no-human-scores.md` 영구 금지)."
    - "Plan 16 swap detection 함수 (detect_lr_swap) 재사용 박제 — T-3 단위 테스트 (매핑 일관성 stub) + T-4 belle Pod 재검증 (swap_ratio 0 검증) 모두 동일 함수 호출. 진단 (Plan 16) ↔ 검증 (Plan 17) 사이클 일관성. Plan 16 모듈 시그너처 의존만, 호출 only."
    - "T-4 belle Pod 재검증 = 사용자 관점 결과 온전성 검증 (memory `feedback-analysis-first.md` 박제 '분석이 망하면 다 망함' + belle 박제 '고객은 결과만 본다' 일관). 임계 2개 — (1) swap_ratio (pair 평균) ≤ 0.05 (Plan 16 박제 1.00 → 0.05 = 20배 회복), (2) RTMPose+MB ref-invert overall ≥ 85 (Plan 08 MP+MB 92 근사, Plan 11 70 의 +15점 회복). 두 임계 모두 belle 박제 '정은지 = 폴스포츠 세계챔피언이므로 IPSF minimum 못 가는 자세 불가능' 일관 — 정은지 invert split 의 측정값이 90+ 회복되면 사용자 관점 결과 온전."
    - "T-4 결과로 Plan 14 진입 게이트 verdict 분기 3 — PASS (swap_ratio ≤ 0.05 + overall ≥ 85) → Plan 14 PLAN 작성 진입 (5영상 sweep). PARTIAL (swap_ratio ≤ 0.05 but overall < 85) → Plan 18 신설 (multi-engine averaging, Plan 16 (d) verdict 후속). FAIL (swap_ratio > 0.05) → 본 plan 재진입 (다른 매핑 의심, audit 재실행). multi-view 옵션 모든 분기 0건 박제."

  artifacts:
    - path: "backend/research/spikes/rtmpose_to_h36m17.py"
      provides: "T-2 swap fix 대상 후보 #1 — COCO-17 → H3.6M direct_map (line 206-220) 의 left/right 매핑 검증 + audit 결과로 결정된 swap line 수정 (file 당 최대 5줄). H36M_JOINT_NAMES / NUM_H36M_JOINTS / H36M_IDX / score threshold / image_size 정규화 / NaN propagation / 파생 joint (thorax/hip/spine/neck_nose) 로직 무수정."
    - path: "backend/research/spikes/mediapipe_to_h36m17.py"
      provides: "T-2 swap fix 대상 후보 #2 — MediaPipe 33 → H3.6M direct_map (line 184-198) + H36M_TO_COCO17_LIMB_PAIRS (line 128-141, 공유 back-projection) 의 left/right 매핑 검증 + audit 결과로 결정된 swap line 수정 (file 당 최대 5줄). 파생 joint 로직 + h36m17_to_coco17_subset 12 limb pairs 무수정 (audit 가 별도 박제 시 예외)."
    - path: "backend/research/spikes/reports/mapping_audit_<UTC>.md"
      provides: "T-1 audit 보고서 — (a) RTMPose direct_map COCO ↔ H36M index 표 (13 line) 와 skeleton.KEYPOINT_NAMES / H36M_JOINT_NAMES 정합 (b) MediaPipe direct_map MP ↔ H36M index 표 (13 line) 정합 (c) 공유 H36M_TO_COCO17_LIMB_PAIRS 12 line 정합 (d) JOINT_ANGLES dict (8 joint vertex/a/c keypoint name) 정합 (e) Plan 16 stub fixture (LR_PAIRS 4 pair) 을 입력으로 detect_lr_swap 재실행 결과 + swap 검증 → 어느 file 어느 line 어느 joint 가 swap 박제. fix 후 동일 fixture 재실행으로 swap_ratio 0 확인 결과 append."
    - path: "backend/tests/test_mapping_audit.py"
      provides: "T-1 audit 결과 박제 + assertion 단위 테스트 — (a) RTMPose direct_map 의 13 (COCO, H36M) tuple 이 expected mapping 과 정확 일치 assert (b) MediaPipe direct_map 의 13 (MP, H36M) tuple 정합 assert (c) H36M_TO_COCO17_LIMB_PAIRS 12 (H36M, COCO) tuple 정합 assert (d) JOINT_ANGLES dict 의 8 entry 가 left_* / right_* 자체 일관 (right_elbow 의 vertex 가 right_elbow 등) assert. mmpose/torch/mediapipe import 0."
    - path: "backend/tests/test_lr_mapping_consistency.py"
      provides: "T-3 swap fix 검증 단위 테스트 — (a) 좌우 동일 stub keypoint (좌측 keypoint 좌표 = 우측 keypoint 좌표) 입력 → 양 엔진 변환 후 좌측 H36M index 좌표 = 우측 H36M index 좌표 assert (대칭 보존) (b) 좌측만 정확히 90° 다른 stub 입력 → 좌측 H36M 좌표만 변경, 우측 H36M 좌표 보존 assert (좌우 격리) (c) Plan 16 detect_lr_swap 재호출 stub fixture (대칭 입력) → swap_ratio 0 assert (d) 8 angle joints (skeleton.JOINT_KEYS) 모두 정합 (4 LR pair × 양 엔진 = 8 test) assert. mmpose/torch/mediapipe import 0. Plan 16 import only (호출, 무수정)."
    - path: ".planning/phases/01-poseengine-mediapipe-nlf-r-d/01-17-SUMMARY.md"
      provides: "T-5 verdict 박제 — frontmatter status = (T-4 결과 따라) `swap_fix_pass` / `partial_overall_below` / `swap_ratio_still_high`. 본문 = audit 결과 (어느 file 어느 line swap) + fix diff line 수 + T-3 단위 테스트 결과 + T-4 belle Pod 재검증 swap_ratio / overall / Plan 08 92 reproduction 결과 + Plan 14 진입 게이트 verdict 분기 (PASS / PARTIAL / FAIL) + 후속 plan 권고 (Plan 14 PLAN 작성 / Plan 18 신설 / 본 plan 재진입)."

  key_links:
    - from: "backend/research/spikes/rtmpose_to_h36m17.py"
      to: "backend/shared/python/sunity_shared/analysis/skeleton.py::KEYPOINT_NAMES"
      via: "COCO-17 ordering — _COCO_L_SHOULDER=5 등 인덱스 상수가 KEYPOINT_NAMES 와 정확 일치해야 함. T-1 audit assertion."
      pattern: "_COCO_L_|_COCO_R_|KEYPOINT_NAMES"
    - from: "backend/research/spikes/mediapipe_to_h36m17.py"
      to: "backend/research/spikes/spike_rtmpose.py::_h36m17_to_coco17_subset"
      via: "공유 H36M → COCO back-projection — H36M_TO_COCO17_LIMB_PAIRS (mediapipe_to_h36m17.py line 128-141) 와 spike_rtmpose._h36m17_to_coco17_subset pairs (line 398-411) 가 12 tuple 정확 일치 정합 (양쪽 다 수정 X — audit 가 별도 박제 시 예외)."
      pattern: "H36M_TO_COCO17_LIMB_PAIRS|_h36m17_to_coco17_subset"
    - from: "backend/tests/test_lr_mapping_consistency.py"
      to: "backend/research/spikes/spike_measurement_trace.py::detect_lr_swap"
      via: "Plan 16 swap detection 함수 import only (호출, 무수정). stub fixture 입력 → swap_ratio assertion."
      pattern: "from backend.research.spikes.spike_measurement_trace import detect_lr_swap"
    - from: ".planning/phases/01-poseengine-mediapipe-nlf-r-d/01-17-SUMMARY.md"
      to: ".planning/phases/01-poseengine-mediapipe-nlf-r-d/01-16-SUMMARY.md"
      via: "Plan 16 박제 swap_ratio 1.00 / cross-engine left_elbow 75.88° / Plan 08 92 / Plan 11 70 → T-4 재검증 후 동일 영상 결과 비교 박제."
      pattern: "Plan 16|swap_ratio|75.88|left_elbow"
---

<objective>
**Plan 16 belle Pod live mode verdict dominant ['b', 'c', 'd'] (2026-06-01) 후속 — left_elbow swap_ratio 1.00 root cause fix + belle Pod 재검증 (POSE-01).**

Plan 16 측정 결과 박제 — ref-invert 단독 live mode 173 frame 의 좌우 keypoint 부호 비교에서 left_elbow_vs_right_elbow pair swap_ratio = 1.00 (100% frame 모두 RTMPose+MB 와 MediaPipe+MB 의 부호가 반대). 통계적 우연 불가능 — systematic 좌우 매핑 swap 버그. 동시에 cross-engine joint 별 disagreement 평균에서 left_elbow = 75.88° 가 peak (right_elbow 8.91° 와 8배 격차) 로 swap_ratio 1.00 과 직접 일치. Plan 12 (d) verdict (RTMPose H36M ordering vs NLF COCO ordering 17/17 다름) 의 systematic 부산물 = 두 변환 path (`rtmpose_to_h36m17.py` direct_map / `mediapipe_to_h36m17.py` direct_map / 공유 `H36M_TO_COCO17_LIMB_PAIRS`) 중 어느 한쪽이 left ↔ right index 가 뒤바뀜.

Purpose: belle 결정 (2026-06-01, "분석 정확도 최우선 + 해결부터" memory `feedback-analysis-first.md` + `analysis-objectivity-no-human-scores.md` 일관) — Plan 16 의 측정 trace 가 root cause 박제, 본 plan 이 fix + 재검증. Plan 08 (MP+MB) ref-invert frame-mean 92 가 동일 영상에서 회복 가능성 큼 (swap fix 후 RTMPose+MB ref-invert 90+ 회복 가설). 본 plan = 측정 신뢰도 entry gate 해소 후 Plan 14 (5영상 sweep) 진입 게이트.

Phase 1 Success Criteria #7 (ROADMAP) entry gate — 본 plan 의 T-4 belle Pod 재검증 결과로 Plan 14 진입 결정. belle 박제 "고객은 결과만 본다" + "정은지 = 폴스포츠 세계챔피언이므로 IPSF minimum 못 가는 자세 불가능" 일관 — fix 후 ref-invert overall 90+ 회복 = 사용자 관점 결과 온전성 검증.

Output:
1. T-1 keypoint mapping audit 보고서 (`backend/research/spikes/reports/mapping_audit_<UTC>.md`) — 어느 file 어느 line 어느 joint 가 swap 박제 + Plan 16 detect_lr_swap 재실행 swap detection 결과
2. T-2 swap fix — `rtmpose_to_h36m17.py` 또는 `mediapipe_to_h36m17.py` (또는 양쪽) 의 매핑 line 수정 (file 당 최대 5줄, 목표 1-2줄)
3. T-3 단위 테스트 — 좌우 매핑 일관성 + Plan 16 detect_lr_swap stub fixture swap_ratio 0 + 8 angle joints 정합 + 기존 테스트 회귀 0
4. T-4 belle Pod 재검증 (autonomous: false) — Plan 16 spike 동일 명령 (ref-invert 단독, ~5분) 으로 swap_ratio + RTMPose+MB ref-invert overall 측정
5. T-5 SUMMARY + Plan 14 진입 게이트 verdict (PASS / PARTIAL / FAIL) + 후속 plan 권고

본 plan 통과 시 Plan 14 PLAN 작성 진입 (PASS) 또는 Plan 18 신설 (PARTIAL, multi-engine averaging) 또는 본 plan 재진입 (FAIL).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-08-SUMMARY.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-11-SUMMARY.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-12-SUMMARY.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-13-SUMMARY.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-16-SUMMARY.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-16-PLAN.md
@backend/research/spikes/rtmpose_to_h36m17.py
@backend/research/spikes/mediapipe_to_h36m17.py
@backend/research/spikes/spike_rtmpose.py
@backend/research/spikes/spike_motionbert.py
@backend/research/spikes/spike_measurement_trace.py
@backend/shared/python/sunity_shared/analysis/skeleton.py
@backend/shared/python/sunity_shared/analysis/features.py
@CLAUDE.md

## Plan 16 belle Pod live mode 결과 적재 (2026-06-01) — 본 plan dominant root cause

ref-invert 단독 live mode (spike_measurement_trace_live_20260601_1144) 173 frame 결과:

| # | 가설 | 측정값 | strong 임계 | verdict |
|---|------|--------|-------------|---------|
| (a) | Gemini frame_idx 정확도 (frame 88 vs 88±5 window 평균) | 4.10° | 30° | rejected |
| (b) | RTMPose+MB 영상 평균 |L-R| | 43.14° | 40° | strong |
| (c) | lifter occlusion 좌우 매핑 swap (두 path 부호 반대 비율) | 0.37 | 0.30 | strong |
| (d) | RTMPose+MB vs MP+MB cross-engine 각도 disagreement | 34.57° | 30° | strong |

dominant: ['b', 'c', 'd']. (a) rejected = frame_idx=88 정확, 단일 frame sampling 한계 X.

좌우 swap detection pair 별 (Plan 16 detect_lr_swap):

| pair | swap_ratio | 정상 임계 0.05 | verdict |
|------|------------|----------------|---------|
| **left_elbow_vs_right_elbow** | **1.00** | 0.05 | **systematic bug (100% swap)** |
| left_shoulder_vs_right_shoulder | 0.01 | 0.05 | 정상 |
| left_hip_vs_right_hip | 0.32 | 0.05 | 의심 |
| left_knee_vs_right_knee | 0.14 | 0.05 | 정상 근접 |

Cross-engine joint 별 disagreement 평균:

| joint | mean_abs_deg |
|-------|--------------|
| **left_elbow** | **75.88°** (peak) |
| left_knee | 68.72° |
| left_shoulder | 45.23° |
| right_hip | 30.42° |
| right_shoulder | 17.06° |
| left_hip | 15.89° |
| right_knee | 14.46° |
| right_elbow | 8.91° (정상) |

→ left_elbow swap_ratio 1.00 (173 frame 모두) + cross-engine disagreement 75.88° (right_elbow 8.91° 와 8배) 직접 일치 = **dominant root cause 확정 = 한 엔진의 elbow 매핑 systematic left ↔ right swap.**

Plan 08 (MP+MB) ref-invert frame-mean 92 vs Plan 11 (RTMPose+MB) frame-mean 70 의 22점 차이 = swap 누적 효과 가설. swap fix 후 RTMPose+MB ref-invert overall 90+ 회복 가능성 큼 (Plan 14 5영상 sweep 진입 게이트).

## 본 plan fix 후보 위치 (audit 우선순위)

1. **rtmpose_to_h36m17.py direct_map (line 206-220)** — COCO-17 인덱스 → H3.6M 인덱스 13 tuple. left_elbow / right_elbow 매핑 (COCO 7→H36M 12, COCO 8→H36M 15) audit 우선.
2. **mediapipe_to_h36m17.py direct_map (line 184-198)** — MediaPipe 33 → H3.6M 13 tuple. left_elbow / right_elbow 매핑 (MP 13→H36M 12, MP 14→H36M 15) audit 우선.
3. **공유 H36M_TO_COCO17_LIMB_PAIRS (mediapipe_to_h36m17.py line 128-141)** — H36M → COCO 12 tuple back-projection. l_elbow H36M 12 → COCO 7 / r_elbow H36M 15 → COCO 8 매핑. 양 엔진이 공유하는 path.
4. **spike_rtmpose._h36m17_to_coco17_subset pairs (spike_rtmpose.py line 398-411)** — RTMPose 전용 사본 (공유 pairs 와 정확 일치 박제됨). 무수정 박제 (다른 기존 spike 금지) but audit 가 정합 검증.
5. **skeleton.JOINT_ANGLES dict (skeleton.py line 39-48)** — 8 joint 의 (a, vertex, c) tuple. left_elbow → ("left_shoulder", "left_elbow", "left_wrist"). audit 가 정합 검증 (skeleton.py 무수정 박제).

## verdict 임계 (T-4 belle Pod 재검증)

- `SWAP_RATIO_PASS_THRESHOLD = 0.05` — Plan 16 박제 left_elbow 1.00 의 20배 회복. pair 평균 (4 pair 합 / 4) ≤ 0.05 면 PASS.
- `OVERALL_PASS_THRESHOLD = 85.0` — RTMPose+MB ref-invert frame-mean overall. Plan 08 (MP+MB) 92 근사, Plan 11 (RTMPose+MB) 70 의 +15점 회복. ≥ 85 면 PASS.
- 두 임계 모두 통과 = PASS / `SWAP_RATIO_PASS_THRESHOLD` 만 통과 = PARTIAL / 둘 다 fail = FAIL.

## verdict 분기 (T-5 Plan 14 진입 게이트)

| T-4 결과 | verdict | 후속 plan |
|----------|---------|-----------|
| swap_ratio ≤ 0.05 + overall ≥ 85 | **PASS** | Plan 14 PLAN 작성 진입 (5영상 sweep) |
| swap_ratio ≤ 0.05 + overall < 85 | **PARTIAL** | Plan 18 신설 (multi-engine averaging, Plan 16 (d) verdict 후속) — Plan 14 보류 |
| swap_ratio > 0.05 | **FAIL** | 본 plan 재진입 (audit 재실행, 다른 매핑 의심) — Plan 14 보류 |

multi-view 옵션 모든 분기 0건 박제 — memory `single-camera-first-multi-view-last.md` (2026-06-01).
</context>

<scope_limits>
**In scope (본 plan):**

- T-1 keypoint mapping audit — 두 변환 file (`rtmpose_to_h36m17.py` / `mediapipe_to_h36m17.py`) + 공유 back-projection (`H36M_TO_COCO17_LIMB_PAIRS`) + skeleton.JOINT_ANGLES 정합 audit. Plan 16 `detect_lr_swap` 함수 재호출 stub fixture 로 swap 검증. 보고서 `backend/research/spikes/reports/mapping_audit_<UTC>.md`.
- T-2 swap fix — audit 결과로 결정된 file (1개 또는 양쪽) 의 매핑 line 수정. file 당 최대 5줄, 목표 1-2줄. 다른 로직 무수정 박제.
- T-3 단위 테스트 — 좌우 매핑 일관성 + Plan 16 swap detection stub fixture swap_ratio 0 + 8 angle joints 정합 + 기존 Plan 16/13 테스트 회귀 0. 신규 ≥ 10 tests. mmpose/torch/mediapipe import 0.
- T-4 belle Pod 재검증 (autonomous: false) — Plan 16 spike (`run_plan16_spike.sh` 동일 명령) 으로 ref-invert 단독 재실행. swap_ratio + RTMPose+MB ref-invert overall + Plan 13 frame 88 measurement 정합 박제.
- T-5 SUMMARY + Plan 14 진입 게이트 verdict 분기 (PASS / PARTIAL / FAIL) + 후속 plan 권고.

**Out of scope (영구 / 다른 plan / Phase):**

- **다각도 촬영 (multi-view) — 영구 자동 제외** (memory `single-camera-first-multi-view-last.md` 2026-06-01 박제). 본 plan 권고 / 후속 plan 옵션 / 코드 / 문서 / verdict 분기 / 테스트 / audit 보고서 / SUMMARY 어디서도 multi-view 언급 0건. belle 명시 지시 시에만 재진입.
- **path B (multi-engine averaging) 구현** — T-4 PARTIAL 시 Plan 18 신설 권고만, 별 plan.
- **path D (Gemini 직접 EXTEND/BENT) 정식 진입** — Phase 5 정식 path. Plan 16 (a) rejected 후 본 plan 의 fix 가 dominant root cause 해소이므로 path D 조기 진입 불필요. 본 plan scope 외.
- **5영상 sweep** — Plan 14 책임. 본 plan = ref-invert 단독 재검증.
- **NLF lift path** — Plan 12 (c) verdict 후 영구 폐기. 본 plan 호출 0.
- **운영 코드 수정** — functions/** / runpod_inference/** / shared/python/sunity_shared/analysis/** / shared/python/sunity_shared/judging/** / shared/python/sunity_shared/pose_lifters/** 전부 0줄. dimensions.py / technique.py / FallbackRecognizer / pose_estimator.py / features.py / temporal.py / skeleton.py 무수정 박제.
- **Plan 13 모듈 수정** — gemini_moment_extractor.py / moment_dimensions.py / spike_gemini_moment.py 0줄.
- **Plan 15 데이터 수정** — geometric_criterion.py / loader.py / 5영상 YAML 0줄.
- **Plan 16 모듈 수정** — spike_measurement_trace.py / test_spike_measurement_trace.py / test_spike_measurement_trace_smoke.py 0줄. T-4 재검증 도구로 그대로 사용 (호출 only).
- **다른 기존 spike 수정** — spike_rtmpose.py / sweep_rtmpose.py / debug_dimensions.py / debug_gap_root_cause.py / spike_motionbert.py / spike_gemini_moment.py 0줄. spike_rtmpose._h36m17_to_coco17_subset 의 pairs 는 audit 가 정합 검증만 (무수정 박제).
- **사람 점수 라벨링 / Gemini 호출** — 영구 금지 (memory `analysis-objectivity-no-human-scores.md`).
- **HybrIK / AlphaPose / NLF / SMPL-X / VideoPose3D 비교군** — 라이선스 차단 (memory `license-blocklist-pose.md`).
- **이모지** — 0건 (CLAUDE.md §7).
</scope_limits>

<tasks>

<task type="auto">
  <name>T-1: keypoint mapping audit (RTMPose / MediaPipe / 공유 back-projection / JOINT_ANGLES 정합) + Plan 16 detect_lr_swap stub 재실행 + 보고서 + audit 단위 테스트</name>
  <files>backend/research/spikes/reports/mapping_audit_&lt;UTC&gt;.md, backend/tests/test_mapping_audit.py</files>
  <read_first>
    - backend/research/spikes/rtmpose_to_h36m17.py (line 1-220 전체 — H36M_JOINT_NAMES 순서 + 13 direct_map tuple + 4 derive joint 로직 + _COCO_* / _H36M_* 인덱스 상수 확인)
    - backend/research/spikes/mediapipe_to_h36m17.py (line 1-250 전체 — H36M_JOINT_NAMES + 13 direct_map tuple + 12 H36M_TO_COCO17_LIMB_PAIRS + _MP_* / _H36M_* 인덱스 상수 + h36m17_to_coco17_subset 함수 확인)
    - backend/research/spikes/spike_rtmpose.py:379-418 (_h36m17_to_coco17_subset 의 12 pairs 가 mediapipe_to_h36m17.H36M_TO_COCO17_LIMB_PAIRS 와 정확 일치 박제 확인 — 무수정 박제)
    - backend/shared/python/sunity_shared/analysis/skeleton.py:10-50 (KEYPOINT_NAMES COCO-17 ordering + JOINT_ANGLES dict 8 entry vertex/a/c tuple 확인)
    - backend/research/spikes/spike_measurement_trace.py (detect_lr_swap 함수 시그너처 확인 — input (T, 8) angle matrix 2개 + LR_PAIRS tuple → swap_frames_per_pair / swap_frame_ratio / swap_frame_ratio_per_pair output. 무수정 import 박제)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-16-SUMMARY.md (좌우 swap detection pair 별 swap_ratio 표 + cross-engine joint 별 disagreement 표 박제 — audit 보고서 reference)
  </read_first>
  <action>
1. **rtmpose_to_h36m17.py audit 표 산출** — line 206-220 의 direct_map 13 tuple 을 (COCO 인덱스, H36M 인덱스, COCO joint name from skeleton.KEYPOINT_NAMES, H36M joint name from rtmpose_to_h36m17.H36M_JOINT_NAMES, 좌우 일치 여부) 5열 표로 박제. 예: (7, 12, "left_elbow", "l_elbow", left=left → OK) vs (7, 15, "left_elbow", "r_elbow", left vs r → SWAP). 13 row 모두 박제. 좌우 swap 의심 행 강조.

2. **mediapipe_to_h36m17.py audit 표 산출** — line 184-198 direct_map 13 tuple 을 동일 5열 표로 박제 (MP 인덱스, H36M 인덱스, MP joint name from MediaPipe pose_landmarker 표준, H36M joint name, 좌우 일치). MP 인덱스 표준: 11=l_shoulder / 12=r_shoulder / 13=l_elbow / 14=r_elbow / 15=l_wrist / 16=r_wrist / 23=l_hip / 24=r_hip / 25=l_knee / 26=r_knee / 27=l_ankle / 28=r_ankle / 0=nose. 13 row 모두 박제.

3. **공유 H36M_TO_COCO17_LIMB_PAIRS audit 표 산출** — mediapipe_to_h36m17.py line 128-141 의 12 tuple (H36M idx, COCO idx) 을 (H36M idx, H36M name, COCO idx, COCO name from skeleton.KEYPOINT_NAMES, 좌우 일치) 5열 표로 박제. 12 row.

4. **JOINT_ANGLES dict audit 표 산출** — skeleton.py line 39-48 의 8 entry 를 (joint key, vertex name, a name, c name, 자체 정합 — left_elbow 이면 a/vertex/c 모두 left_*) 5열 표로 박제. 8 row.

5. **Plan 16 detect_lr_swap stub 재실행** — Plan 16 모듈에서 detect_lr_swap 함수 import (무수정). 4 LR pair (left_elbow_vs_right_elbow 등) 에 대해 stub fixture 두 set 생성: (a) "대칭" stub = T=20 frame, 8 joint 모두 좌우 동일 각도 (예: 양쪽 elbow 90°) → 예상 swap_ratio 0 (b) "현재 코드" stub = rtmpose_to_h36m17.convert_rtmpose_coco17_to_h36m17 / mediapipe_to_h36m17.convert_mp33_to_h36m17 호출 후 두 path 의 8 joint angle 산출 (compute_joint_angles 사용) → detect_lr_swap 결과. (b) 결과로 어느 pair 가 swap 박제 (left_elbow 가 1.00 박제 예상).

6. **swap 박제 결정** — 위 4 표 + Plan 16 detect_lr_swap (b) stub 결과로 어느 file 어느 line 어느 joint 가 systematic swap 인지 박제. 예: "rtmpose_to_h36m17.py line 215-217 (COCO 7=left_elbow → H36M 15=r_elbow / COCO 8=right_elbow → H36M 12=l_elbow) 두 line 이 swap" 또는 "mediapipe_to_h36m17.py line 192-193 (MP 13=l_elbow → H36M 15=r_elbow / MP 14=r_elbow → H36M 12=l_elbow) 두 line 이 swap" 등. 1개 file 또는 양쪽 박제.

7. **audit 보고서 작성** — `backend/research/spikes/reports/mapping_audit_<UTC ISO 8601>.md` 생성. 섹션 구성:
   - (1) Plan 16 verdict 인용 (left_elbow swap_ratio 1.00 / cross-engine 75.88°)
   - (2) rtmpose_to_h36m17.py audit 표 (13 row)
   - (3) mediapipe_to_h36m17.py audit 표 (13 row)
   - (4) 공유 H36M_TO_COCO17_LIMB_PAIRS audit 표 (12 row)
   - (5) skeleton.JOINT_ANGLES audit 표 (8 row)
   - (6) detect_lr_swap stub 재실행 결과 (대칭 fixture swap_ratio 0 검증 + 현재 코드 fixture swap_ratio 박제)
   - (7) swap 박제 — 어느 file 어느 line 어느 joint
   - (8) T-2 fix 권고 (변경 line 수 최소화, file 당 ≤ 5 line)
   - (9) multi-view 키워드 0건 확인 박제 + 사람 점수 0건 + Gemini 호출 0건 + 이모지 0건

8. **단위 테스트 작성** — `backend/tests/test_mapping_audit.py` 신규. 클래스 4개:
   - `TestRtmposeDirectMap` (3 tests): direct_map 13 tuple 의 left/right 정합 assert (COCO L_SHOULDER 5 → H36M L_SHOULDER 11 등 13 row 모두) / shape (T, 17, 2|3) → (T, 17, 2|3) / NaN propagation
   - `TestMediapipeDirectMap` (3 tests): 동일 패턴 (MP 11=l_shoulder → H36M L_SHOULDER 11 등 13 row)
   - `TestSharedLimbPairs` (3 tests): H36M_TO_COCO17_LIMB_PAIRS 12 tuple 의 H36M name → COCO name 정합 + spike_rtmpose._h36m17_to_coco17_subset pairs 정확 일치 박제 (호출 only)
   - `TestJointAnglesSelfConsistency` (3 tests): JOINT_ANGLES 8 entry 의 left_* 시 a/vertex/c 모두 left / right_* 시 모두 right / vertex 가 key 와 정확 일치
   - 총 12 tests. mmpose/torch/mediapipe import 0.

9. **CRITICAL: audit 보고서 + 테스트 작성만, fix 아직 X** — T-2 가 fix 책임. T-1 출력 = "swap 박제" 만.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && PYTHONPATH=backend/shared/python:. backend/.venv/bin/python3 -m pytest backend/tests/test_mapping_audit.py -v 2>&1 | tail -25 && test -f backend/research/spikes/reports/mapping_audit_*.md && grep -c '^## ' backend/research/spikes/reports/mapping_audit_*.md && grep -v '^[[:space:]]*#' backend/research/spikes/reports/mapping_audit_*.md | grep -ciE 'multi.view|다각도|multi camera'</automated>
  </verify>
  <acceptance_criteria>
    - 12 tests PASS, 실행 시간 < 1s, mmpose/torch/mediapipe import 0건
    - audit 보고서 `backend/research/spikes/reports/mapping_audit_<UTC>.md` 존재 + 9 섹션 (Plan 16 인용 + 4 audit 표 + detect_lr_swap stub 결과 + swap 박제 + T-2 fix 권고 + 키워드 0건 확인) 모두 존재
    - audit 보고서 본문에 multi-view / 다각도 / multi camera 키워드 (주석 제외) 0건
    - audit 보고서 본문에 좌표 / 점수 / 심사 판단 키워드 0건 (사람 점수 라벨링 + Gemini 호출 0건)
    - audit 보고서 7번 섹션 "swap 박제" 에 정확히 어느 file 어느 line 범위 (예: line 215-217) + 어느 joint (left_elbow, right_elbow 등) 박제 — T-2 가 직접 인용
    - detect_lr_swap stub 재실행 결과 (대칭 fixture swap_ratio 0) 박제 — Plan 16 swap detection 함수 호출 검증
    - T-2 fix 권고에 file 당 변경 line 수 ≤ 5 명시
    - 운영 코드 / Plan 13 모듈 / Plan 15 데이터 / Plan 16 모듈 / 다른 기존 spike 무수정 (git diff HEAD 가 신규 audit 보고서 + 신규 테스트 만 변경)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>T-2: swap fix — T-1 audit 박제 file 의 매핑 line 수정 (file 당 최대 5줄, 목표 1-2줄)</name>
  <files>backend/research/spikes/rtmpose_to_h36m17.py, backend/research/spikes/mediapipe_to_h36m17.py</files>
  <read_first>
    - backend/research/spikes/reports/mapping_audit_&lt;UTC&gt;.md (T-1 박제 — swap 박제 섹션 7 + T-2 fix 권고 섹션 8 정확 인용)
    - backend/research/spikes/rtmpose_to_h36m17.py (T-1 audit 후 변경 대상 line 정확 위치 재확인 — direct_map line 206-220 또는 _COCO_*/_H36M_* 인덱스 상수 line 102-134)
    - backend/research/spikes/mediapipe_to_h36m17.py (T-1 audit 후 변경 대상 line 정확 위치 재확인 — direct_map line 184-198, H36M_TO_COCO17_LIMB_PAIRS line 128-141, _MP_*/_H36M_* 인덱스 상수 line 86-117)
    - backend/research/spikes/spike_rtmpose.py:398-411 (공유 pairs 와 spike_rtmpose 사본 정확 일치 — fix 후에도 정합 박제 필수, but spike_rtmpose 무수정 박제)
  </read_first>
  <action>
1. **T-1 audit 박제 인용** — audit 보고서 섹션 7 "swap 박제" 의 정확한 file path + line 범위 + joint name 을 task 머리에 직접 인용. 예: "T-1 audit 박제 — rtmpose_to_h36m17.py line 215-217: COCO 7=left_elbow 가 H36M 15 (r_elbow) 로, COCO 8=right_elbow 가 H36M 12 (l_elbow) 로 잘못 매핑."

2. **fix 적용 — 시나리오 A (RTMPose 단독 swap)**: rtmpose_to_h36m17.py 의 direct_map (line 206-220) 에서 swap 박제된 tuple 만 수정. 변경 예시: `(_COCO_L_ELBOW, _H36M_R_ELBOW)` → `(_COCO_L_ELBOW, _H36M_L_ELBOW)`. 최대 2 line (left + right). 다른 11 tuple + derive joint 로직 (line 228-268) + image_size 정규화 + NaN propagation + score threshold 처리 전부 무수정 박제.

3. **fix 적용 — 시나리오 B (MediaPipe 단독 swap)**: mediapipe_to_h36m17.py 의 direct_map (line 184-198) 에서 swap 박제된 tuple 만 수정. 동일 패턴. 최대 2 line. 다른 11 tuple + derive joint + h36m17_to_coco17_subset (line 252-292) 무수정 박제.

4. **fix 적용 — 시나리오 C (공유 H36M_TO_COCO17_LIMB_PAIRS swap)**: mediapipe_to_h36m17.py 의 H36M_TO_COCO17_LIMB_PAIRS (line 128-141) 에서 swap 박제된 tuple 수정. 동일 패턴. 최대 2 line. 단, 공유 pairs 가 spike_rtmpose._h36m17_to_coco17_subset (line 398-411) 와 정확 일치 박제됨 — fix 후에도 spike_rtmpose 의 사본은 무수정 박제 (다른 기존 spike 금지 박제). spike_rtmpose 가 자체 pairs 를 사용하므로 mediapipe_to_h36m17 의 공유 pairs 만 수정 시 spike_rtmpose path 는 영향 없음. T-3 단위 테스트가 spike_rtmpose pairs 와의 disagreement 박제 — 시나리오 C 채택 시 T-1 audit 가 spike_rtmpose pairs 도 함께 수정 필요 여부 박제 (audit 결과로 결정). spike_rtmpose 수정 필요 시 audit 가 "예외 박제" 로 명시 — 본 plan scope 확장 또는 별 plan.

5. **fix 적용 — 시나리오 D (양쪽 swap)**: rtmpose + mediapipe direct_map 양쪽 동시 수정. 각 file 최대 2 line, 총 최대 4 line. 다른 로직 전부 무수정.

6. **변경 line 수 박제** — fix 후 `git diff --stat` 결과로 변경 line 수 박제. 목표 ≤ 4 line, 최대 ≤ 10 line (양쪽 file 합산).

7. **CRITICAL: 다음 file 변경 0줄 박제**:
   - backend/functions/** (운영 Lambda)
   - backend/runpod_inference/** (GPU server)
   - backend/shared/python/sunity_shared/analysis/** (dimensions / technique / features / temporal / skeleton / pose_estimator / pose_lifters / kismam / motiondtw / coach_writer)
   - backend/shared/python/sunity_shared/judging/** (Plan 13 모듈 + Plan 15 데이터)
   - backend/judging_data/** (Plan 15 YAML)
   - backend/research/spikes/spike_rtmpose.py (다른 기존 spike, 시나리오 C 예외 박제 시 별 plan)
   - backend/research/spikes/spike_motionbert.py (다른 기존 spike)
   - backend/research/spikes/sweep_rtmpose.py (다른 기존 spike)
   - backend/research/spikes/debug_dimensions.py / debug_gap_root_cause.py / spike_gemini_moment.py (다른 기존 spike)
   - backend/research/spikes/spike_measurement_trace.py (Plan 16 모듈, T-4 도구로 사용)
   - backend/tests/test_spike_measurement_trace.py / test_spike_measurement_trace_smoke.py (Plan 16 테스트)

8. **CRITICAL: 모듈 docstring 갱신** — fix 적용 file 의 module-level docstring 에 fix 박제 1 line 추가. 예: rtmpose_to_h36m17.py 의 docstring line 9-13 (라이선스) 뒤에 "Plan 17 fix (2026-06-01): left_elbow / right_elbow 매핑 swap 정정 (Plan 16 belle Pod live mode swap_ratio 1.00 dominant root cause). 변경 line: 215-217." 같은 박제. 1-2 line. 다른 docstring 무수정.

9. **commit message**: `fix(01-17): rtmpose_to_h36m17.py left/right elbow mapping swap 정정 (Plan 16 swap_ratio 1.00)` 또는 audit 결과 따라.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && git diff --stat backend/research/spikes/rtmpose_to_h36m17.py backend/research/spikes/mediapipe_to_h36m17.py | tail -3 && git diff --stat backend/functions/ backend/runpod_inference/ backend/shared/python/sunity_shared/analysis/ backend/shared/python/sunity_shared/judging/ backend/judging_data/ 2>&1 | wc -l && git diff --stat backend/research/spikes/spike_rtmpose.py backend/research/spikes/spike_motionbert.py backend/research/spikes/sweep_rtmpose.py backend/research/spikes/debug_dimensions.py backend/research/spikes/debug_gap_root_cause.py backend/research/spikes/spike_gemini_moment.py backend/research/spikes/spike_measurement_trace.py 2>&1 | wc -l && PYTHONPATH=backend/shared/python:. backend/.venv/bin/python3 -c "from backend.research.spikes import rtmpose_to_h36m17 as r, mediapipe_to_h36m17 as m; print('rtmpose direct_map len:', 13); print('mediapipe direct_map len:', 13); print('H36M_TO_COCO17_LIMB_PAIRS len:', len(m.H36M_TO_COCO17_LIMB_PAIRS)); assert len(m.H36M_TO_COCO17_LIMB_PAIRS) == 12; print('T-2 import OK')"</automated>
  </verify>
  <acceptance_criteria>
    - rtmpose_to_h36m17.py + mediapipe_to_h36m17.py 변경 line 수 합산 ≤ 10 line (목표 ≤ 4 line) — `git diff --stat` 박제
    - 운영 코드 (functions / runpod_inference / shared/analysis / shared/judging / judging_data) `git diff --stat` 결과 빈 줄 (변경 0 line)
    - 다른 기존 spike 7개 (spike_rtmpose / spike_motionbert / sweep_rtmpose / debug_dimensions / debug_gap_root_cause / spike_gemini_moment / spike_measurement_trace) `git diff --stat` 결과 빈 줄
    - 두 file import 성공 + direct_map / H36M_TO_COCO17_LIMB_PAIRS 길이 검증 통과
    - 두 file docstring 에 Plan 17 fix 1-2 line 박제 (변경 line + Plan 16 dominant root cause 인용)
    - T-1 audit 보고서 섹션 7 의 swap 박제와 정확 일치하는 line 만 수정
    - 시나리오 C 예외 박제 시 audit 보고서가 spike_rtmpose 수정 별 plan 박제 (본 plan scope 외)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>T-3: 단위 테스트 — 좌우 매핑 일관성 + Plan 16 detect_lr_swap stub fixture swap_ratio 0 + 8 angle joints 정합 + 회귀 0</name>
  <files>backend/tests/test_lr_mapping_consistency.py</files>
  <read_first>
    - backend/research/spikes/rtmpose_to_h36m17.py (T-2 fix 적용된 direct_map + convert_rtmpose_coco17_to_h36m17 함수 시그너처)
    - backend/research/spikes/mediapipe_to_h36m17.py (T-2 fix 적용된 direct_map + convert_mp33_to_h36m17 + h36m17_to_coco17_subset 시그너처)
    - backend/research/spikes/spike_measurement_trace.py (detect_lr_swap / LR_PAIRS / 8 angle joints 정합 박제 — 무수정 import)
    - backend/shared/python/sunity_shared/analysis/features.py:36-100 (compute_joint_angles 시그너처 — input (T, 17, 4) keypoints, output (T, 8) angles)
    - backend/shared/python/sunity_shared/analysis/skeleton.py:39-50 (JOINT_KEYS 8 joint ordering)
    - backend/tests/test_mapping_audit.py (T-1 audit 단위 테스트 — fixture 패턴 참조)
    - backend/tests/test_spike_measurement_trace.py (Plan 16 단위 테스트 — stub fixture 작성법 참조, 무수정 import)
  </read_first>
  <action>
1. **테스트 file 생성** — `backend/tests/test_lr_mapping_consistency.py` 신규. mmpose/torch/mediapipe import 0건 (numpy + Plan 16/T-1 모듈 import only).

2. **TestRtmposeLeftRightConsistency** (3 tests):
   - `test_symmetric_input_produces_symmetric_h36m`: T=20 frame, 17 COCO keypoint 모두 좌우 동일 좌표 (예: left_shoulder.x = right_shoulder.x = 0.5, left_shoulder.y = right_shoulder.y = 0.3, left_elbow.x = right_elbow.x 등). image_size=(640, 480), score=0.9. convert_rtmpose_coco17_to_h36m17 호출 후 H36M l_shoulder == r_shoulder, l_elbow == r_elbow, l_hip == r_hip, l_knee == r_knee, l_wrist == r_wrist, l_foot == r_foot assert (np.allclose).
   - `test_left_only_change_isolated`: 좌우 동일 stub 에서 left_elbow.x 만 0.5 → 0.7 변경. 변환 후 H36M l_elbow.x = 0.7 / r_elbow.x = 0.5 (= 원본 우측) assert. 우측 보존 = 좌우 격리.
   - `test_right_only_change_isolated`: 우측만 변경 → H36M r_elbow 만 변경, l_elbow 보존 assert.

3. **TestMediapipeLeftRightConsistency** (3 tests): 동일 패턴, convert_mp33_to_h36m17 + MediaPipe 33 stub (33 landmark, image normalized 좌표). 3 tests.

4. **TestSharedBackProjectionConsistency** (2 tests):
   - `test_h36m17_to_coco17_subset_preserves_left_right`: H36M 17 stub xyz 에서 H36M l_shoulder (idx 11) ≠ r_shoulder (idx 14) 좌표. h36m17_to_coco17_subset 호출 후 COCO l_shoulder (idx 5) = H36M l_shoulder 좌표 / COCO r_shoulder (idx 6) = H36M r_shoulder 좌표 assert.
   - `test_shared_pairs_matches_spike_rtmpose_subset`: mediapipe_to_h36m17.H36M_TO_COCO17_LIMB_PAIRS 와 spike_rtmpose._h36m17_to_coco17_subset 내 pairs 가 set equality (12 tuple 정확 일치) assert. spike_rtmpose 무수정 박제 검증.

5. **TestSwapRatioWithPlan16Detector** (3 tests, Plan 16 detect_lr_swap 재사용):
   - `test_symmetric_stub_swap_ratio_zero`: detect_lr_swap 호출 — 두 (T=20, 8) angle stub matrix 모두 좌우 동일 각도 (예: left_elbow = right_elbow = 90°). LR_PAIRS 4 pair 입력. swap_ratio (pair 평균) ≤ 0.05 assert. swap_ratio_per_pair 모두 ≤ 0.05 assert.
   - `test_two_engine_pipeline_swap_ratio_zero`: 좌우 동일 stub keypoint (COCO 17 + MP 33) → convert_rtmpose_coco17_to_h36m17 + convert_mp33_to_h36m17 → spike_rtmpose._h36m17_to_coco17_subset / mediapipe.h36m17_to_coco17_subset → compute_joint_angles → detect_lr_swap. T-2 fix 후 swap_ratio ≤ 0.05 assert. T-2 fix 검증 핵심 test.
   - `test_intentional_swap_detected`: 의도적 swap 한 fixture (left_elbow 의 부호를 반대로 강제 negation) → detect_lr_swap swap_ratio ≥ 0.30 assert. swap detection 함수 자체 정합 검증.

6. **TestEightAngleJointsConsistency** (1 test):
   - `test_all_eight_joints_left_right_paired`: skeleton.JOINT_KEYS 8 joint 가 left/right 4 pair (elbow / shoulder / hip / knee) 로 정확 분류 assert. LR_PAIRS = (("left_elbow", "right_elbow"), ("left_shoulder", "right_shoulder"), ("left_hip", "right_hip"), ("left_knee", "right_knee")) 와 skeleton.JOINT_KEYS set equality assert.

7. **TestRegressionPlan16Plan13** (2 tests):
   - `test_plan16_smoke_tests_still_pass`: `pytest backend/tests/test_spike_measurement_trace_smoke.py` 모두 PASS 박제 (subprocess 호출 또는 manual import 검증).
   - `test_plan13_smoke_tests_still_pass`: `pytest backend/tests/test_spike_gemini_moment_smoke.py` 모두 PASS 박제.

8. **총 ≥ 14 tests** (TestRtmpose 3 + TestMediapipe 3 + TestSharedBackProjection 2 + TestSwapRatioWithPlan16 3 + TestEightAngle 1 + TestRegression 2 = 14). 실행 시간 < 2s. mmpose/torch/mediapipe import 0건.

9. **CRITICAL: Plan 16 모듈 무수정** — detect_lr_swap / LR_PAIRS 호출 only. import 만, 호출만, 무수정 박제.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && PYTHONPATH=backend/shared/python:. backend/.venv/bin/python3 -m pytest backend/tests/test_lr_mapping_consistency.py -v 2>&1 | tail -30 && PYTHONPATH=backend/shared/python:. backend/.venv/bin/python3 -m pytest backend/tests/test_mapping_audit.py backend/tests/test_spike_measurement_trace.py backend/tests/test_spike_measurement_trace_smoke.py -v 2>&1 | tail -10 && grep -v '^[[:space:]]*#' backend/tests/test_lr_mapping_consistency.py | grep -E '^import|^from' | grep -E 'mmpose|^import torch|^from torch|mediapipe' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - 14 tests PASS, 실행 시간 < 2s
    - test_lr_mapping_consistency.py 의 import 줄 (주석 제외) 에 mmpose / torch / mediapipe 0건 (`grep` 결과 0)
    - Plan 16 모듈 (spike_measurement_trace.py + test_spike_measurement_trace*.py) 무수정 박제 — `git diff` empty
    - Plan 13 테스트 회귀 0건 (test_spike_gemini_moment_smoke.py 모두 PASS)
    - Plan 16 테스트 회귀 0건 (test_spike_measurement_trace.py + smoke 모두 PASS, 42 tests)
    - T-1 audit 테스트 회귀 0건 (test_mapping_audit.py 12 tests PASS)
    - swap detection (Plan 16 detect_lr_swap 함수) stub fixture 결과로 T-2 fix 검증 (swap_ratio ≤ 0.05) 통과
    - 좌우 격리 (test_left_only_change_isolated / test_right_only_change_isolated) 통과 — fix 가 다른 joint 매핑 깨지 않음 박제
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>T-4: belle Pod 재검증 — Plan 16 spike 동일 명령 (ref-invert 단독, ~5분) + swap_ratio + RTMPose+MB ref-invert overall + Plan 13 frame 88 measurement 정합 박제</name>
  <what-built>
T-1 audit + T-2 swap fix + T-3 단위 테스트 박제 완료. 본 task = belle Pod 에서 Plan 16 spike (`backend/scripts/run_plan16_spike.sh` 또는 동일 spike_measurement_trace --mode live 명령) 동일 인자로 ref-invert 단독 재실행 + 결과 .json/.md Claude 공유 + SUMMARY 갱신.

검증 임계:
- (1) swap_ratio (pair 평균) ≤ 0.05 — Plan 16 박제 1.00 의 20배 회복
- (2) RTMPose+MB ref-invert overall ≥ 85 — Plan 08 MP+MB 92 근사, Plan 11 RTMPose+MB 70 의 +15 회복
- (3) Plan 13 frame 88 measurement (right_shoulder 18.2도 등) 가 fix 후 인체학적 정상 범위 (≥ 80°) 회복
- (4) cross-engine left_elbow disagreement 75.88° → ≤ 30° (Plan 16 (d) strong 임계 미만)

ref-invert 단독 (Plan 13 + Plan 15 + Plan 16 동일 영상). 두 lift path (RTMPose+MB + MediaPipe+MB) 직렬 실행, 약 5분 (RTMPose+MB ~3분 + MP+MB ~2분).
  </what-built>
  <how-to-verify>
**belle Pod 실행 절차 (5단계)**:

1. 코드 최신화:
   ```
   cd /workspace/SunityMotion
   git pull --ff-only origin main
   ```

2. 환경 확인 (Pod 환경 보존 — STATE.md 2026-06-01 박제):
   ```
   python3 -c "import torch; import mmpose; import mediapipe; print(torch.__version__, mmpose.__version__, mediapipe.__version__)"
   ```
   기대값: torch 2.4.1+cu124 / mmpose 1.3.2 / mediapipe 0.10.x. 정합 안 맞으면 STOP.

3. Plan 16 spike 동일 명령 재실행 (ref-invert 단독, ~5분):
   ```
   python3 -m backend.research.spikes.spike_measurement_trace \
     --mode live \
     --motion ref-invert \
     --frame-index 88 \
     --hold-window 5 \
     --bucket sunity-motion-pilot-videos \
     --rtmpose-config /workspace/rtmpose_weights/rtmpose-l_8xb256-420e_coco-256x192.py \
     --rtmpose-checkpoint /workspace/rtmpose_weights/rtmpose-l_simcc-coco_pt-aic-coco_420e-256x192-1352a4d2_20230127.pth \
     --motionbert-root /workspace/MotionBERT \
     --motionbert-weights /workspace/MotionBERT/checkpoint/pose3d/FT_MB_lite_MB_ft_h36m_global_lite/best_epoch.bin \
     --out backend/research/spikes/reports/spike_measurement_trace_postfix_$(date +%Y%m%d_%H%M).json
   ```

4. 결과 파일 확인 — `.json` + `.md` 둘 다 존재 + 4 가설 verdict 모두 산출 + swap_ratio_per_pair 4 pair 모두 박제.

5. 결과 Claude 공유 — `.json` + `.md` 내용 그대로 전달. Claude 가 SUMMARY 갱신:
   - frontmatter status = (검증 결과 따라) `swap_fix_pass` / `partial_overall_below_85` / `swap_ratio_still_high` 중 1
   - 본문 `belle Pod 재검증 결과 (Plan 17 T-4)` 섹션 신설 — Plan 16 결과 (swap_ratio 1.00 / overall 5/5 fail) 와 본 plan 결과 (swap_ratio X / overall Y) 비교 표 + Plan 13 frame 88 measurement 재현 검증 + Plan 14 진입 게이트 verdict + 후속 plan 권고
   - STATE.md Current Position + Last activity 갱신
   - ROADMAP.md Plan 17 진행률 행 [~] → [x] + Plan 14 entry 갱신

**검증 임계 (Claude 가 결과 적재 시 확인)**:

| 항목 | Plan 16 박제 (fix 전) | Plan 17 임계 | T-4 측정 | 통과 |
|------|------------------------|--------------|----------|------|
| left_elbow swap_ratio | 1.00 | ≤ 0.05 | (T-4) | (T-4) |
| pair 평균 swap_ratio | (계산: (1.00+0.01+0.32+0.14)/4 = 0.37) | ≤ 0.05 | (T-4) | (T-4) |
| RTMPose+MB ref-invert overall | 70 (Plan 11) / 5/5 fail (Plan 13) | ≥ 85 | (T-4) | (T-4) |
| cross-engine left_elbow disagreement | 75.88° | ≤ 30° | (T-4) | (T-4) |
| Plan 13 frame 88 right_shoulder | 18.2° (인체학적 비정상) | ≥ 80° (정상) | (T-4) | (T-4) |
| 4 가설 (b) RTMPose+MB |L-R| 평균 | 43.14° (strong) | ≤ 15° (weak 미만) | (T-4) | (T-4) |
| 4 가설 (c) swap 비율 | 0.37 (strong) | ≤ 0.10 (weak 미만) | (T-4) | (T-4) |
| 4 가설 (d) cross-engine | 34.57° (strong) | ≤ 10° (weak 미만) | (T-4) | (T-4) |

- JSON / Markdown 에 multi-view / 다각도 / multi camera 키워드 0건 (`grep -v '^[[:space:]]*#'` 적용 후 0)
- 좌표 / 점수 / 심사 판단 키워드 0건 (사람 점수 라벨링 0건 확인)
- Plan 13 frame 88 right_shoulder 측정값이 fix 후 인체학적 정상 (≥ 80°) 회복 박제 — belle 박제 "정은지 = 폴스포츠 세계챔피언이므로 IPSF minimum 못 가는 자세 불가능" 일관성 검증
  </how-to-verify>
  <resume-signal>belle 가 `.json` + `.md` 결과 + 검증 임계 8개 행 측정값 Claude 공유 후 "Plan 17 T-4 결과 적재"</resume-signal>
</task>

<task type="auto">
  <name>T-5: SUMMARY + Plan 14 진입 게이트 verdict 분기 (PASS / PARTIAL / FAIL) + 후속 plan 권고</name>
  <files>.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-17-SUMMARY.md</files>
  <read_first>
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-16-SUMMARY.md (Plan 16 SUMMARY 패턴 — TL;DR 표 / verdict / 4 가설 매트릭스 / Self-Check / belle Pod 결과 섹션)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-13-SUMMARY.md (Plan 13 SUMMARY 패턴 — measurement_unreliable_blocked verdict 인용)
    - @/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
    - backend/research/spikes/reports/spike_measurement_trace_postfix_*.{json,md} (T-4 belle Pod 결과 적재)
    - backend/research/spikes/reports/mapping_audit_<UTC>.md (T-1 audit 보고서 인용)
  </read_first>
  <action>
1. **frontmatter status 결정** — T-4 결과로 분기:
   - swap_ratio (pair 평균) ≤ 0.05 + RTMPose+MB ref-invert overall ≥ 85 → status `swap_fix_pass` (PASS)
   - swap_ratio ≤ 0.05 + overall < 85 → status `partial_overall_below_85` (PARTIAL)
   - swap_ratio > 0.05 → status `swap_ratio_still_high` (FAIL)

2. **SUMMARY 본문 12 섹션 작성**:
   - (1) TL;DR 표 — verdict / scope (audit + fix + 재검증) / 신규 파일 (audit 보고서 + 2 테스트 + SUMMARY = 4) / 변경 파일 (rtmpose_to_h36m17 / mediapipe_to_h36m17 둘 또는 한쪽) / 단위 테스트 (T-1 12 + T-3 14 = 26 PASS) / 운영 코드 무수정 / Plan 13/15/16 모듈 무수정 / 기존 spike 8개 무수정 / NLF 호출 0 / multi-view 0건 / 사람 점수 0건 / Gemini 호출 0건 / 이모지 0건
   - (2) Plan 16 verdict 인용 (left_elbow swap_ratio 1.00 / cross-engine 75.88°) + 본 plan dominant root cause 해소 책임 박제
   - (3) T-1 audit 결과 — 4 audit 표 (RTMPose direct_map / MediaPipe direct_map / 공유 H36M_TO_COCO17_LIMB_PAIRS / JOINT_ANGLES) 의 swap 박제 + detect_lr_swap stub 재실행 결과
   - (4) T-2 swap fix 결과 — 변경 file + line 수 + 변경 line 정확 인용 (예: rtmpose_to_h36m17.py line 215: `(_COCO_L_ELBOW, _H36M_R_ELBOW)` → `(_COCO_L_ELBOW, _H36M_L_ELBOW)`)
   - (5) T-3 단위 테스트 결과 — 14 tests PASS + Plan 16/13 회귀 0 박제 (총 42 + 15 + 14 + 12 = 83 tests PASS)
   - (6) T-4 belle Pod 재검증 결과 — 8 행 비교 표 (Plan 16 박제 vs 본 plan 측정 vs 임계 vs 통과 여부) + Plan 13 frame 88 measurement 재현 검증
   - (7) Plan 14 진입 게이트 verdict 분기:
     - PASS → "Plan 14 PLAN 작성 진입 (5영상 sweep, /gsd:plan-phase 1 --plan 14). 본 plan = 측정 신뢰도 entry gate 통과. belle 박제 '고객은 결과만 본다' + '정은지 IPSF minimum 못 가는 자세 불가능' 일관."
     - PARTIAL → "Plan 18 신설 권고 (multi-engine averaging, Plan 16 (d) verdict 후속). swap fix 는 성공 but overall < 85 = 단일 lift path 정확도 부족. multi-view 옵션 0건 박제 (memory single-camera-first 유지)."
     - FAIL → "본 plan 재진입 — T-1 audit 재실행. 다른 매핑 의심 (예: spike_rtmpose._h36m17_to_coco17_subset 사본 swap 또는 derive joint 매핑 swap). multi-view 옵션 0건 박제."
   - (8) 핵심 결정 박제 11항목 (must_haves.truths 11개 본문 명시) + memory 인용 (feedback-analysis-first / analysis-objectivity-no-human-scores / license-blocklist-pose / single-camera-first-multi-view-last / judging-baseline-ipsf-code-of-points)
   - (9) Deviations from Plan (executor 실 구현 차이 — fix 시나리오 A/B/C/D 중 어느 것 채택 박제)
   - (10) Known Stubs (없으면 명시)
   - (11) Threat Flags (없으면 명시)
   - (12) Self-Check (파일 존재 + 운영 코드 무수정 + Plan 13/15/16 모듈 무수정 + 다른 spike 7개 무수정 + 키워드 0건 + 테스트 결과)
   - (13) Verdict 요약 (orchestrator 에게) — verdict + one-liner + commits + next action

3. **STATE.md / ROADMAP.md 갱신 권고** — orchestrator 책임 박제. 본 plan = SUMMARY 만 작성, STATE/ROADMAP 갱신은 orchestrator 가 git commit.

4. **CRITICAL: multi-view 키워드 0건 박제** — SUMMARY 본문 grep `multi.view|다각도|multi camera` 결과 0. memory filename 인용 (single-camera-first-multi-view-last.md) 만 허용 + grep 패턴 미발동.

5. **CRITICAL: 이모지 0건** — CLAUDE.md §7. grep 패턴 검증.

6. **CRITICAL: 사람 점수 라벨링 0건** — 점수 / 등급 / 심사 키워드 0건 (Plan 16 박제 측정값 인용은 OK).
  </action>
  <verify>
    <automated>test -f .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-17-SUMMARY.md && grep -E '^status:|^verdict:|swap_fix_pass|partial_overall_below_85|swap_ratio_still_high' .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-17-SUMMARY.md | head -3 && grep -v '^[[:space:]]*#' .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-17-SUMMARY.md | grep -ciE 'multi.view|다각도|multi camera' && grep -c '^## ' .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-17-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-17-SUMMARY.md` 존재
    - frontmatter status = `swap_fix_pass` 또는 `partial_overall_below_85` 또는 `swap_ratio_still_high` 중 정확히 1개
    - SUMMARY 본문 ≥ 12 섹션 (TL;DR / Plan 16 인용 / T-1 audit 결과 / T-2 fix / T-3 테스트 / T-4 belle 결과 / Plan 14 게이트 verdict / 핵심 결정 / Deviations / Known Stubs / Threat Flags / Self-Check / Verdict 요약)
    - SUMMARY 본문에 multi-view / 다각도 / multi camera 키워드 (주석 제외) 0건
    - SUMMARY 본문에 이모지 0건 (CLAUDE.md §7)
    - SUMMARY 본문에 좌표 / 점수 / 심사 판단 키워드 0건 (사람 점수 라벨링 + Gemini 호출 0건)
    - T-4 검증 임계 8 행 비교 표 박제 (Plan 16 박제 vs 본 plan vs 임계 vs 통과)
    - Plan 14 진입 게이트 verdict 분기 (PASS / PARTIAL / FAIL) + 후속 plan 권고 명시
    - 운영 코드 / Plan 13/15/16 모듈 / 다른 기존 spike 7개 무수정 (git diff empty 박제)
  </acceptance_criteria>
</task>

</tasks>

<verification>

## Plan 17 통과 게이트 (Phase 1 Success Criteria #7 entry gate)

- [ ] T-1 audit 보고서 생성 + 12 tests PASS
- [ ] T-2 swap fix 적용 (file 당 변경 line ≤ 5, 합산 ≤ 10)
- [ ] T-3 단위 테스트 14 tests PASS + Plan 13/16 회귀 0 (총 83 tests PASS)
- [ ] T-4 belle Pod 재검증 실행 + 결과 적재 (8 행 비교 표)
- [ ] T-5 SUMMARY 박제 + Plan 14 진입 게이트 verdict 분기
- [ ] 운영 코드 / Plan 13/15/16 모듈 / 다른 기존 spike 7개 무수정 (git diff empty)
- [ ] multi-view / 다각도 / multi camera 키워드 0건 (코드 + 테스트 + audit 보고서 + SUMMARY + JSON + MD 모두)
- [ ] 사람 점수 라벨링 0건 / Gemini 호출 0건
- [ ] 이모지 0건 (CLAUDE.md §7)
- [ ] NLF 호출 0건 (Plan 12 (c) verdict 영구 폐기 박제)

## 판정 기준 / Plan 14 진입 게이트

| T-4 belle Pod 측정 | verdict | Plan 14 진입 |
|---------------------|---------|--------------|
| swap_ratio (pair 평균) ≤ 0.05 + RTMPose+MB ref-invert overall ≥ 85 + Plan 13 frame 88 right_shoulder ≥ 80° | **PASS** | 진입 가능 → Plan 14 PLAN 작성 (`/gsd:plan-phase 1 --plan 14`, 5영상 sweep) |
| swap_ratio ≤ 0.05 + overall < 85 | **PARTIAL** | 진입 보류 → Plan 18 신설 권고 (multi-engine averaging, Plan 16 (d) verdict 후속). multi-view 옵션 0건 박제 |
| swap_ratio > 0.05 | **FAIL** | 진입 보류 → 본 plan 재진입 (audit 재실행, 다른 매핑 의심). multi-view 옵션 0건 박제 |

본 plan PASS 만이 Plan 14 진입 보장. PARTIAL / FAIL 시 후속 plan 결과 후 재평가.

</verification>

<success_criteria>

본 plan 완료 시점 = T-4 belle Pod 재검증 결과 적재 + Plan 14 진입 게이트 verdict 박제 + 후속 plan 권고 합의:

1. audit 보고서 `mapping_audit_<UTC>.md` + 2 단위 테스트 (test_mapping_audit + test_lr_mapping_consistency) + SUMMARY = 신규 4 파일 박제
2. swap fix 적용 (rtmpose_to_h36m17.py 또는 mediapipe_to_h36m17.py, 합산 ≤ 10 line, 목표 ≤ 4 line)
3. 단위 테스트 26 tests PASS (T-1 12 + T-3 14) + Plan 13/16 기존 테스트 회귀 0 (총 83+ tests PASS)
4. belle Pod 재검증 (Plan 16 spike 동일 명령, ref-invert 단독, ~5분) 실행 + JSON+MD 결과 적재
5. swap_ratio + RTMPose+MB ref-invert overall + Plan 13 frame 88 measurement 정합 박제
6. Plan 14 진입 게이트 verdict 분기 (PASS / PARTIAL / FAIL) + 후속 plan 권고
7. multi-view 옵션 0건 박제 (memory `single-camera-first-multi-view-last.md` 유지)
8. 사람 점수 0건 / Gemini 호출 0건 / 이모지 0건 / NLF 호출 0건
9. 운영 코드 / Plan 13/15/16 모듈 / 다른 기존 spike 7개 무수정 (git diff empty)
10. belle 박제 "고객은 결과만 본다" + "정은지 IPSF minimum 못 가는 자세 불가능" 일관성 검증 — fix 후 ref-invert overall 90+ 회복 가능성 (PASS 시 확정)

</success_criteria>

<risk>

## Risk 1: audit 가 single swap point 식별 실패

T-1 audit 가 4 표 (RTMPose / MediaPipe / 공유 / JOINT_ANGLES) 모두 정합 검증 통과 + Plan 16 detect_lr_swap stub 결과로 swap point 박제 — 만약 모든 매핑이 정합인데 detect_lr_swap 결과만 swap 박제면 root cause 가 매핑 swap 아님 (다른 원인). 본 plan = FAIL → 재진입 또는 다른 가설 (lift path 자체 좌우 noise = Plan 16 (b) strong, multi-engine averaging 책임).

## Risk 2: fix 가 다른 joint 매핑을 깨트림

T-3 단위 테스트 (좌우 격리 / 8 joint 정합 / Plan 16 detect_lr_swap stub) 가 회귀 가드. 14 tests 중 어느 하나라도 FAIL 면 fix 재검토.

## Risk 3: T-4 swap_ratio 통과 but overall < 85

PARTIAL 분기 — swap fix 는 성공 but 단일 lift path 정확도 부족. Plan 18 신설 (multi-engine averaging, Plan 16 (d) verdict 후속) 권고. multi-view 옵션 0건 박제 유지.

## Risk 4: T-4 swap_ratio 통과 but Plan 13 frame 88 right_shoulder 인체학적 비정상 잔존

swap fix 가 keypoint mapping 만 정정 — lift path 자체의 occlusion 신뢰도 (Plan 16 (b) 영상 평균 |L-R| 43.14° strong) 는 별 root cause. PARTIAL 또는 FAIL 분기 가능 — 별 plan (multi-engine averaging) 필요.

## Risk 5: belle Pod 환경 변화

Pod 환경이 STATE.md 2026-06-01 박제 (torch 2.4.1+cu124 / mmpose 1.3.2 / mediapipe 0.10.x) 와 다르면 spike 실패. T-4 절차 2단계가 환경 확인 게이트.

## Risk 6: 시나리오 C (공유 H36M_TO_COCO17_LIMB_PAIRS swap) — spike_rtmpose 사본 정합 깨짐

시나리오 C 채택 시 mediapipe_to_h36m17 의 공유 pairs 만 수정하면 spike_rtmpose._h36m17_to_coco17_subset 사본 (line 398-411) 과 정합 깨짐. T-3 test_shared_pairs_matches_spike_rtmpose_subset 가 FAIL — fix 재검토 필요. spike_rtmpose 무수정 박제 깨려면 별 plan.

</risk>

<open_questions>

1. **T-1 audit 결과 어느 file 어느 line swap?** — RTMPose direct_map vs MediaPipe direct_map vs 공유 H36M_TO_COCO17_LIMB_PAIRS 중 어느 위치인지 audit 결과로만 박제.

2. **T-4 swap_ratio ≤ 0.05 + overall ≥ 85 동시 통과 여부** — belle 박제 "고객은 결과만 본다" + Plan 08 92 reproduction 가설 검증 결과.

3. **T-4 PARTIAL 시 Plan 18 (multi-engine averaging) 진입 scope** — Plan 16 (d) verdict cross-engine 34.57° strong 와 직접 연결. 별 plan 책임 박제.

4. **T-4 FAIL 시 다음 audit 대상** — derive joint (hip center / spine / thorax / neck_nose / head) 매핑 swap 가능성 (8 angle joints 한정 박제 후 영향 0 예상) 또는 spike_rtmpose._h36m17_to_coco17_subset 사본 swap 가능성 (다른 기존 spike 박제, 별 plan).

5. **시나리오 C 채택 가능 여부** — 공유 pairs swap 박제 시 spike_rtmpose 사본 정합 깨지는 문제. T-1 audit 가 명시 박제 + T-2 시나리오 C 시 별 plan 권고.

</open_questions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spike → numpy 변환 | RTMPose / MediaPipe 출력 (2D pixel 좌표) → H36M / COCO 변환 path. left/right index 매핑 swap 이 본 plan 의 dominant root cause. |
| belle Pod → S3 | ref-invert 영상 download path. Plan 16 spike 와 동일 — 본 plan 변경 0. |
| spike → Plan 16 모듈 import | detect_lr_swap 함수 무수정 import. 호출 only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-17-01 | Tampering | rtmpose_to_h36m17.py / mediapipe_to_h36m17.py direct_map | mitigate | T-3 단위 테스트 14 tests (좌우 격리 + 8 joint 정합 + Plan 16 detect_lr_swap stub) 가 fix 의 좌우 매핑 정합 검증. file 당 변경 line ≤ 5 박제로 회귀 위험 최소화. |
| T-01-17-02 | Information Disclosure | audit 보고서 + SUMMARY | accept | 본 plan 보고서 / 테스트 / SUMMARY 에 PII / 좌표 / 점수 / 심사 판단 키워드 0건. Plan 16 박제 측정값 인용만. |
| T-01-17-03 | Denial of Service | T-4 belle Pod live mode (~5분) | accept | Plan 16 spike 동일 명령, 동일 ~5분 실행 시간. Pod 환경 보존 박제. |
| T-01-17-04 | Repudiation | git commit 박제 | mitigate | T-2 commit message 에 audit 결과 + 변경 file + Plan 16 swap_ratio 1.00 인용 박제. T-5 SUMMARY frontmatter status 박제. |
| T-01-17-SC | Tampering | npm/pip/cargo installs | accept | 본 plan 신규 install 0건. Plan 08/10/13 setup 그대로. Package Legitimacy Audit 면제 (install 0). |
</threat_model>

<next_step>

```
T-1 audit (rtmpose / mediapipe / 공유 / JOINT_ANGLES 4 표 + Plan 16 detect_lr_swap stub 재실행)
  → audit 보고서 + 12 단위 테스트 생성
  → T-2 swap fix (변경 line ≤ 5 per file, 합산 ≤ 10)
  → T-3 단위 테스트 14 tests + Plan 13/16 회귀 0 검증
  → T-4 belle Pod 재검증 (Plan 16 spike 동일 명령, ref-invert 단독, ~5분)
  → 결과 .json + .md Claude 공유
  → T-5 SUMMARY 박제 + Plan 14 진입 게이트 verdict 분기 (PASS / PARTIAL / FAIL)
  → STATE.md + ROADMAP.md 갱신 (orchestrator)
  → 분기 후속:
    - PASS → /gsd:plan-phase 1 --plan 14 (Plan 14 5영상 sweep)
    - PARTIAL → /gsd:plan-phase 1 --plan 18 (multi-engine averaging)
    - FAIL → /gsd:plan-phase 1 --plan 17 재진입 (audit 재실행)
```

multi-view 옵션은 모든 분기에서 0건 박제 — memory `single-camera-first-multi-view-last.md` (2026-06-01 박제) 유지. Phase 1 Success Criteria #7 entry gate = 본 plan T-4 PASS = Plan 14 진입.

</next_step>
</content>
</invoke>