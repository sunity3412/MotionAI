---
phase: 01-poseengine-mediapipe-nlf-r-d
plan: 20
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/licenses/rtmw-weights-audit.md
  - backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/__init__.py
  - backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/weights_manifest.json
  - backend/tests/test_rtmw_weights_manifest.py
autonomous: false
gap_closure: true
requirements:
  - POSE-01
must_haves:
  truths:
    - "RTMW 운영 후보 가중치 (rtmlib RTMW-l 256x192 + RTMW-x 384x288 + RTMW3D 후보) 각각의 학습 데이터셋 + 상업 사용 가능 여부가 audit 문서에 박제됨 (D-25)"
    - "각 후보 가중치 entry 는 weights_manifest.json 에 sha256 + license_status (commercial_ok / restricted / unknown) + source_url + audit_date 가 박제된다"
    - "license_status='commercial_ok' 가중치만 production_eligible=true 로 표기 — plan 21 이 이 manifest 만 참조 가능 (manifest 가 plan 21 의 가중치 선택 게이트)"
    - "belle 확인 checkpoint = audit 결과 + 선택한 production 가중치 1개 + 폴백 가중치 1개를 belle 가 검토 (사람 점수 라벨링 X, 라이선스 결정 O)"
    - "memory `license-blocklist-pose.md` 의 RTMPose OK 기록과 정합 — 차단 목록 위반 0"
  artifacts:
    - path: "docs/licenses/rtmw-weights-audit.md"
      provides: "RTMW 후보 가중치별 학습 데이터 + 라이선스 상태 + 상업 사용 결정 박제 (D-25)"
    - path: "backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/weights_manifest.json"
      provides: "production 가능 RTMW 가중치 매니페스트 — plan 21 의 가중치 선택 입력"
    - path: "backend/tests/test_rtmw_weights_manifest.py"
      provides: "manifest 스키마 + license_status 필드 강제 검증"
  key_links:
    - from: "weights_manifest.json"
      to: "plan 21 (RTMW 통합)"
      via: "production_eligible=true 인 가중치만 로드 가능 (plan 21 의 RTMWPoseEngine __init__ 가드)"
      pattern: "production_eligible"
    - from: "docs/licenses/rtmw-weights-audit.md"
      to: "memory license-blocklist-pose"
      via: "차단 목록 cross-reference"
      pattern: "license-blocklist-pose"
---

<objective>
RTMW 운영 가중치의 학습 데이터 + 상업 사용 가능 여부를 audit 하고 (D-25), production-eligible 가중치를 manifest 로 박제한다. belle 명시 (개발 지시 §5): "rtmlib/RTMPose 코드는 Apache-2.0 이지만, 모델 가중치의 학습 데이터 상업 사용 가능 여부는 모델별로 반드시 확인". 본 plan 통과 후 plan 21 이 audit 통과 가중치만 사용 가능 (라이선스 위반 0 강제).

Purpose: D-25 박제 + memory license-blocklist-pose 와의 정합. plan 19 (interface 박제) 와 평행 진행 가능 (file overlap 0). plan 21 (RTMW 통합) 의 입력 게이트.

Output: rtmw-weights-audit.md 문서 + weights_manifest.json + 스키마 테스트 + belle 검토 checkpoint.

MVP 슬라이스 정합: 본 plan 은 라이선스 게이트로 단독 user-visible 슬라이스를 만들지 않는다. plan 21 이 manifest 의 production_eligible 가중치를 로드해서 end-to-end 슬라이스를 만든다.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md
@/Users/kimtaesung/Downloads/Sunity_v1_개발지시_RTMW무료스택.md
@.planning/ROADMAP.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: rtmlib RTMW 후보 가중치 audit + weights_manifest.json + 스키마 테스트</name>
  <files>docs/licenses/rtmw-weights-audit.md, backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/__init__.py, backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/weights_manifest.json, backend/tests/test_rtmw_weights_manifest.py</files>
  <read_first>
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md D-17 (rtmlib RTMW 133 wholebody Apache-2.0) + D-25 (가중치 라이선스 위생)
    - /Users/kimtaesung/Downloads/Sunity_v1_개발지시_RTMW무료스택.md §2 (포즈 엔진 무료 스택) + §5 (라이선스 위생)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-10-SUMMARY.md (RTMPose-l 단일 영상 STRONG_PASS — Apache 2.0 + dataset 정합 기록)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-11-SUMMARY.md (RTMPose 5영상 sweep)
  </read_first>
  <action>
    docs/licenses/rtmw-weights-audit.md: 신설. 한국어 본문. 섹션:
    - "## 1. Audit 범위" — rtmlib RTMW 후보 가중치 = (a) RTMW-l 256x192, (b) RTMW-l 384x288, (c) RTMW-x 384x288, (d) RTMW3D-l 단일 카메라용 (plan 22 후보). 각각의 mmpose / rtmlib 공식 weight URL.
    - "## 2. 후보별 학습 데이터셋" — 표 형식. 컬럼 = 가중치명, 학습 데이터셋 (COCO, COCO-WholeBody, AIC, Halpe, Body-7 등), 데이터셋 라이선스, 상업 사용 가능 여부 (yes/no/unknown), 출처 URL. rtmlib README + mmpose model zoo 참조.
    - "## 3. 의사결정 매트릭스" — 가중치별 production_eligible 판단 근거. 'commercial_ok' = 학습 데이터 모두 상업 사용 허용 + 가중치 자체 Apache-2.0. 'restricted' = 학습 데이터 비상업 (예: AIC 같은 dataset). 'unknown' = 라이선스 명시 부재 — belle 확인 필요.
    - "## 4. belle 결정" — Task 2 checkpoint 산출 (initial empty placeholder + 후속 업데이트).
    - "## 5. 차단 목록 cross-reference" — memory `license-blocklist-pose.md` 에 RTMPose OK 박제됨, AlphaPose/NLF/SMPL-X/VideoPose3D Noncommercial 차단 — RTMW 후보 중 차단 목록 항목 0 확인.
    - "## 6. Plan 21 가중치 선택 게이트" — production_eligible=true 만 weights_manifest.json 에 등재. plan 21 의 RTMWPoseEngine __init__ 가 이 manifest 만 로드.

    backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/__init__.py: 신설 (빈 placeholder). docstring 만 — "RTMW 어댑터 패키지 (plan 21 에서 본격 구현). 본 plan 20 은 weights_manifest.json 만 박제."

    backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/weights_manifest.json: 신설. 스키마:
    {
      "manifest_version": "1.0",
      "audit_date": "2026-06-02",
      "weights": [
        {
          "name": "rtmw-l-256x192",
          "url": "<rtmlib 공식 URL>",
          "sha256": "<TBD or null until download>",
          "training_data": ["COCO", "COCO-WholeBody", ...],
          "license_status": "commercial_ok" | "restricted" | "unknown",
          "production_eligible": true | false,
          "source_audit_ref": "docs/licenses/rtmw-weights-audit.md#3"
        },
        ... (4 entries)
      ]
    }

    초기 모든 entry 의 production_eligible=false (belle 검토 전). sha256 = null 또는 "pending_download". 후속 Task 2 belle checkpoint 통과 후 plan 21 시작 시점에 belle 가 production_eligible=true 로 1개 이상 승급.

    backend/tests/test_rtmw_weights_manifest.py: 단위 테스트:
    - test_manifest_loads_as_valid_json: json.load 성공
    - test_manifest_required_keys: 최상위 = {manifest_version, audit_date, weights}
    - test_each_weight_required_keys: 각 entry = {name, url, sha256, training_data, license_status, production_eligible, source_audit_ref}
    - test_license_status_enum: license_status ∈ {'commercial_ok', 'restricted', 'unknown'}
    - test_production_eligible_implies_commercial_ok: production_eligible=true 인 entry 는 license_status='commercial_ok' 필수 (라이선스 위반 차단 게이트)
    - test_at_least_one_candidate_weight: weights 배열 길이 ≥ 1
    - test_no_blocklisted_weights: 모든 entry 의 name 에 'alphapose', 'nlf', 'smplx', 'videopose3d' 부분문자열 0건 (memory license-blocklist-pose 정합)
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_rtmw_weights_manifest.py -x -q && test -f docs/licenses/rtmw-weights-audit.md</automated>
  </verify>
  <acceptance_criteria>
    - 7개 테스트 모두 PASS
    - grep -c "^## " docs/licenses/rtmw-weights-audit.md ≥ 6 (섹션 6개)
    - python -c "import json; m = json.load(open('backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/weights_manifest.json')); print(len(m['weights']))" ≥ 4
    - audit 문서에 "Apache-2.0", "COCO-WholeBody", "license-blocklist-pose" 키워드 모두 등장
  </acceptance_criteria>
  <done>
    RTMW 후보 가중치 audit 박제 완료. manifest 가 plan 21 의 가중치 선택 게이트로 기능. belle 검토 (Task 2) 전까지 production_eligible 가중치 0개 — plan 21 진입 불가 (라이선스 위반 차단).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: belle 라이선스 검토 checkpoint — production 가중치 1개 + 폴백 가중치 1개 승인</name>
  <what-built>
    Task 1 산출 — rtmw-weights-audit.md 의 의사결정 매트릭스 + weights_manifest.json 의 4개 후보 가중치. 각 후보의 학습 데이터셋 + 라이선스 상태가 명시되어 있다. 모든 entry 는 production_eligible=false 초기 상태.
  </what-built>
  <how-to-verify>
    1. cat docs/licenses/rtmw-weights-audit.md — §2 "후보별 학습 데이터셋" 표를 belle 가 직접 읽음
    2. §3 "의사결정 매트릭스" 의 license_status 판정을 검토
    3. belle 결정 = (a) production 가중치 1개 선택 (license_status='commercial_ok' 인 것 중) (b) 폴백 가중치 1개 선택 (또는 single backbone 으로 충분)
    4. 결정 결과를 audit 문서 §4 "belle 결정" 섹션에 박제 + weights_manifest.json 의 해당 entry production_eligible=true 로 갱신
    5. belle 가 'license_status=unknown' 인 가중치를 production 승인하려는 경우 차단 (Task 1 의 test_production_eligible_implies_commercial_ok 가 실패 → belle 가 명시적으로 license 확인 후 commercial_ok 로 승급 필요)
    6. 만약 4개 후보 모두 license_status != 'commercial_ok' 인 경우 → plan 21 진입 차단. belle 가 추가 가중치 후보 발굴 또는 mmpose 공식 채널 문의 → Task 1 재실행
  </how-to-verify>
  <resume-signal>
    belle 가 "approved: production=<name>, fallback=<name>" 또는 "blocked: <reason>" 으로 응답. blocked 인 경우 plan 21 진입 차단. approved 시 weights_manifest.json + audit §4 갱신 commit 후 plan 21 진입 가능.
  </resume-signal>
  <acceptance_criteria>
    - belle 응답 수신
    - approved 인 경우: weights_manifest.json 에 production_eligible=true 인 entry ≥ 1 + audit §4 에 belle 결정 + 승인 일자 박제
    - blocked 인 경우: plan 21 진입 차단 + 후속 audit 작업 별 plan 또는 issue 박제
  </acceptance_criteria>
  <done>
    belle 검토 응답 박제. license-blocklist-pose memory 와 정합 확인. plan 21 진입 게이트 명확화.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 모델 가중치 ↔ 운영 코드 | 가중치 라이선스 위반은 belle 사업 리스크 — manifest 가 hard gate |
| audit 문서 ↔ belle 결정 | 문서 갱신 없이 manifest 만 수정 시 추적 단절 — Task 2 checkpoint 강제 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering | weights_manifest.json 의 production_eligible 무단 승급 (audit 없이) | mitigate | Task 1 의 test_production_eligible_implies_commercial_ok 가 license_status 동시 확인 강제 |
| T-20-02 | Repudiation | belle 결정 박제 누락 → plan 21 진입 시 라이선스 근거 불명 | mitigate | Task 2 checkpoint 가 audit §4 갱신 + commit 강제 |
| T-20-SC | Tampering | rtmlib 가중치 무결성 (sha256 검증) | mitigate | manifest sha256 필드 — plan 21 가 가중치 다운로드 시 검증 (plan 21 task 에서 acceptance 박제) |
</threat_model>

<verification>
- audit 문서가 RTMW 후보 가중치 (RTMW-l 256x192, RTMW-l 384x288, RTMW-x 384x288, RTMW3D-l) 4개를 모두 다룸
- 차단 목록 (AlphaPose, NLF, SMPL-X, VideoPose3D) 위반 0
- belle 검토 응답 박제 완료
- plan 21 진입 게이트 명확
</verification>

<success_criteria>
- RTMW 가중치 audit 박제됨 (D-25 충족)
- weights_manifest.json 이 plan 21 의 가중치 선택 게이트로 기능
- belle 검토 응답 수신 (approved 또는 blocked)
- license-blocklist-pose memory 와 정합
</success_criteria>

<output>
Create `.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-20-SUMMARY.md` when done. SUMMARY 에 belle 결정 인용 + plan 21 진입 가능 여부 명시.
</output>
