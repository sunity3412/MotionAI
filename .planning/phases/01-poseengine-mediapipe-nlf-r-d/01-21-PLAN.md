---
phase: 01-poseengine-mediapipe-nlf-r-d
plan: 21
type: execute
wave: 2
depends_on:
  - 01-19
  - 01-20
files_modified:
  - backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/__init__.py
  - backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/rtmw_engine.py
  - backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/wholebody_keypoints.py
  - backend/shared/python/sunity_shared/analysis/pose_engines/__init__.py
  - backend/shared/python/sunity_shared/analysis/adapters/rtmw_133_to_coco17.py
  - backend/shared/python/sunity_shared/analysis/adapters/__init__.py
  - backend/shared/python/sunity_shared/config.py
  - backend/tests/test_rtmw_engine.py
  - backend/tests/test_rtmw_133_to_coco17_adapter.py
  - backend/tests/fixtures/rtmw_keypoints.py
autonomous: true
gap_closure: true
requirements:
  - POSE-01
must_haves:
  truths:
    - "RTMWPoseEngine 클래스가 plan 19 의 PoseEngine Protocol 을 구현하고 estimate(frames, pole_axis) -> list[PoseFrame] 시그니처를 만족한다 (D-24)"
    - "RTMWPoseEngine.__init__ 가 plan 20 의 weights_manifest.json 을 로드하고 production_eligible=true 인 가중치만 사용 — 그 외 시도 시 LicenseViolationError (D-25)"
    - "RTMW133ToCOCO17Adapter 가 rtmlib RTMW 133 키포인트에서 COCO-17 + 폴 확장 (toe/heel/grip) 을 정확한 인덱스로 추출한다 (D-20)"
    - "RTMW 의 단일 keypoint score 가 PoseFrame.keypoints_3d[*].confidence 로 매핑되고 uncertainty_proxy = 1 - confidence (D-22)"
    - "POSE_ENGINE 환경 변수 = 'RTMW' (기본) 또는 'NLF_SMPLX' (R&D 전용). config.py 의 get_pose_engine() 가 환경 변수 읽어 적절한 엔진 instance 반환 (D-21)"
    - "rtmlib / mmpose / mmcv 의 module-level import 0 — 모두 RTMWPoseEngine.__init__ 내부 lazy import (Lambda fail-fast 안전, H-2 기존 패턴 정합)"
    - "PoseFrame.body_shape = None 으로 설정 (RTMW 는 SMPL-X β 없음, D-19/D-21)"
    - "기존 MediaPipePoseEngine 코드 (plan 02 산출) 는 본 plan 에서 변경 없음 — plan 24 가 R&D 격리 담당"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/rtmw_engine.py"
      provides: "RTMWPoseEngine 어댑터 (rtmlib RTMW 133 wholebody)"
      contains: "class RTMWPoseEngine"
    - path: "backend/shared/python/sunity_shared/analysis/adapters/rtmw_133_to_coco17.py"
      provides: "RTMW133ToCOCO17Adapter — 133 → COCO-17 + 폴 확장 매핑"
      contains: "RTMW_133_TO_COCO17"
    - path: "backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/wholebody_keypoints.py"
      provides: "rtmlib RTMW 133 키포인트 인덱스 표 (body 17 + foot 6 + face 68 + hand 42)"
      contains: "RTMW_KEYPOINT_INDICES"
    - path: "backend/shared/python/sunity_shared/config.py"
      provides: "POSE_ENGINE 환경 변수 + get_pose_engine() factory"
      contains: "get_pose_engine"
  key_links:
    - from: "rtmw_engine.py"
      to: "rtmw_133_to_coco17.py + plan 19 PoseEngine Protocol"
      via: "어댑터 호출 + Protocol 구현"
      pattern: "from ..adapters.rtmw_133_to_coco17|class RTMWPoseEngine"
    - from: "config.py get_pose_engine()"
      to: "POSE_ENGINE 환경 변수"
      via: "환경 변수 분기 (RTMW 기본)"
      pattern: "POSE_ENGINE"
    - from: "rtmw_engine.py __init__"
      to: "weights_manifest.json"
      via: "production_eligible 가중치만 로드 (라이선스 게이트)"
      pattern: "weights_manifest"
---

<objective>
rtmlib RTMW 133 wholebody (Apache-2.0) 백본을 PoseEngine Protocol 구현체로 통합한다 (D-17/D-20/D-21/D-22/D-24/D-25). 본 plan 은 RTMW pivot 의 **end-to-end 슬라이스 첫 단계** — frames + pole_axis → RTMW 추론 → 133 키포인트 → COCO-17 + 폴 확장 변환 → PoseFrame 리스트. plan 22 가 3D path (RTMW3D 또는 lifter) 통합, plan 23 이 5영상 회귀 검증, plan 25 가 pipeline atomic swap.

Purpose: 운영 백본 마이그레이션의 핵심 코드. plan 19 의 contract 와 plan 20 의 라이선스 게이트를 모두 만족.

Output: RTMWPoseEngine + RTMW133ToCOCO17Adapter + RTMW 키포인트 인덱스 표 + POSE_ENGINE config 플래그 + 단위 테스트 3개 + fixture.

MVP 슬라이스 정합: 본 plan + plan 22 + plan 25 가 합쳐져 RTMW end-to-end 슬라이스를 완성한다. 본 plan 단독으로는 pipeline 통합 안 됨 (plan 25 가 swap). 하지만 본 plan 의 RTMWPoseEngine 은 plan 19 Protocol 을 만족하므로 단위 테스트로 frames → PoseFrame 리스트 변환이 동작 검증된다.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-19-SUMMARY.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-20-SUMMARY.md
@/Users/kimtaesung/Downloads/Sunity_v1_개발지시_RTMW무료스택.md
@backend/shared/python/sunity_shared/analysis/interfaces.py
@backend/shared/python/sunity_shared/analysis/pose_frame.py
@backend/shared/python/sunity_shared/analysis/pose_engines/__init__.py
@backend/shared/python/sunity_shared/analysis/pose_engines/mediapipe_engine.py
@backend/shared/python/sunity_shared/analysis/adapters/mediapipe_to_coco17.py
@backend/shared/python/sunity_shared/analysis/skeleton.py
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RTMW 133 키포인트 인덱스 표 + RTMW133ToCOCO17Adapter + 폴 확장 (toe/heel/grip) 추출 (D-20)</name>
  <files>backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/wholebody_keypoints.py, backend/shared/python/sunity_shared/analysis/adapters/rtmw_133_to_coco17.py, backend/shared/python/sunity_shared/analysis/adapters/__init__.py, backend/tests/test_rtmw_133_to_coco17_adapter.py, backend/tests/fixtures/rtmw_keypoints.py</files>
  <read_first>
    - backend/shared/python/sunity_shared/analysis/adapters/mediapipe_to_coco17.py (기존 MediaPipe33ToCOCO17Adapter 패턴 — convert_landmarks_to_coco17_and_pole_ext 함수 시그니처 + return 형식 + grip derived 계산)
    - backend/shared/python/sunity_shared/analysis/skeleton.py (KEYPOINT_NAMES 17 COCO 순서 — RTMW 매핑 표 작성 시 동일 순서 강제)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md D-20 (RTMW 133 원본 보존 + COCO-17 + 폴 확장)
    - https://github.com/Tau-J/rtmlib (RTMW Wholebody 133 keypoint layout 공식 표 — COCO-WholeBody 표준: body 17 + foot 6 + face 68 + hand 42 = 133)
  </read_first>
  <behavior>
    - Test 1 (test_rtmw_keypoint_indices_total_133): RTMW_KEYPOINT_INDICES dict 의 총 entry 수 = 133. body 17 + foot 6 + face 68 + hand 42.
    - Test 2 (test_rtmw_body_17_indices_match_coco): body 17 부분 매핑 = {nose:0, left_eye:1, right_eye:2, left_ear:3, right_ear:4, left_shoulder:5, right_shoulder:6, left_elbow:7, right_elbow:8, left_wrist:9, right_wrist:10, left_hip:11, right_hip:12, left_knee:13, right_knee:14, left_ankle:15, right_ankle:16}. COCO-17 표준 순서.
    - Test 3 (test_rtmw_foot_6_indices_present): foot 6 keypoint = {left_big_toe:17, left_small_toe:18, left_heel:19, right_big_toe:20, right_small_toe:21, right_heel:22} (COCO-WholeBody 표준).
    - Test 4 (test_rtmw_hand_indices_present): left_hand_root (wrist 대체) = 91, right_hand_root = 112. 손가락 keypoint (예: left_index_finger_tip = 95 등) 일부 검증.
    - Test 5 (test_rtmw133_to_coco17_mapping_exact): RTMW_133_TO_COCO17 dict 정확히 17 entry. 매핑 = body 17 인덱스 그대로 (COCO 표준이므로 1:1).
    - Test 6 (test_rtmw_pole_extension_includes_toe_heel_grip): POLE_EXTENSION_MAP 6 entry = {left_heel:19, right_heel:22, left_foot_index:17, right_foot_index:20, pole_grip_left: derived, pole_grip_right: derived}. left_foot_index = left_big_toe RTMW 인덱스 17. right_foot_index = right_big_toe RTMW 인덱스 20.
    - Test 7 (test_pole_grip_derived_from_hand_root): make_mock_rtmw_keypoints_133 입력 → convert 후 pose_extension_landmarks['pole_grip_left'] 위치 = RTMW hand root 평균 (left_hand_root 91 + left_index_finger_mcp 92 + left_thumb_mcp 95 같은 grip-relevant 키포인트 평균). 정확한 인덱스는 rtmlib COCO-WholeBody hand layout 문서 따라 GRIP_LEFT_SOURCE_INDICES / GRIP_RIGHT_SOURCE_INDICES 상수로 박제.
    - Test 8 (test_convert_keypoints_basic): mock 133 keypoints (모두 score=0.9) → convert_rtmw_keypoints_to_coco17_and_pole_ext 호출 → PoseFrame 반환. keypoints_3d 에 정확히 17 키, raw_landmarks_133 또는 raw_keypoints_133 에 133 키 (D-20 원본 보존). 각 Keypoint3D.confidence == 0.9 (D-22 rtmw_score 직접 매핑).
    - Test 9 (test_convert_low_score_keypoint_marked): 일부 keypoint score=0.1 → 해당 Keypoint3D.uncertainty_proxy > 0.85 (low confidence).
    - Test 10 (test_convert_pole_axis_preserved): pole_axis 인자 주입 → 결과 PoseFrame.pole_axis 동일 보존 (H-3 기존 패턴 정합).
    - Test 11 (test_convert_reliability_from_mean_score): 모든 keypoint score=0.9 → PoseFrame.reliability='high'. score=0.1 → reliability='low' (H-4 기존 패턴 정합).
    - Test 12 (test_convert_body_shape_none): PoseFrame.body_shape == None (D-21 — RTMW 에서는 SMPL-X β 없음).
  </behavior>
  <action>
    pose_engines/rtmw/wholebody_keypoints.py: 신설. RTMW_KEYPOINT_INDICES = {keypoint_name: index} dict (133 entry). 섹션 = body 17 + foot 6 + face 68 + hand 42 + comments. rtmlib README + mmpose COCO-WholeBody 표 인용. RTMW_133 = 133 상수.

    adapters/rtmw_133_to_coco17.py: 신설. 함수 시그니처 = convert_rtmw_keypoints_to_coco17_and_pole_ext(keypoints_133, scores_133, image_width, image_height, frame_index, timestamp_ms, pole_axis) -> PoseFrame. 내부:
    - RTMW_133_TO_COCO17 dict 로 17 키 추출
    - POLE_EXTENSION_MAP 으로 toe/heel 추출 + grip derived (wrist + finger MCP 평균)
    - confidence = score (D-22 단일 score), uncertainty_proxy = 1 - confidence (D-22)
    - raw_keypoints_133 에 133 원본 보존 (D-20)
    - PoseFrame.body_shape = None (D-21)
    - reliability = compute_frame_reliability(mean(scores)) (기존 H-4 함수 재사용)
    - pole_axis 인자 그대로 PoseFrame.pole_axis 에 전달 (H-3 정합)

    adapters/__init__.py: __all__ 에 convert_rtmw_keypoints_to_coco17_and_pole_ext + RTMW_133_TO_COCO17 + POLE_EXTENSION_MAP 추가. 기존 mediapipe 어댑터 export 변경 없음.

    tests/fixtures/rtmw_keypoints.py: 신설. make_mock_rtmw_keypoints_133(scores=None, visible_indices=None) 헬퍼 — numpy (133, 3) 좌표 배열 + (133,) score 배열. default scores=0.9.

    tests/test_rtmw_133_to_coco17_adapter.py: 위 12개 테스트.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_rtmw_133_to_coco17_adapter.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - 12개 테스트 모두 PASS
    - python -c "from sunity_shared.analysis.pose_engines.rtmw.wholebody_keypoints import RTMW_KEYPOINT_INDICES; print(len(RTMW_KEYPOINT_INDICES))" 출력 = 133
    - python -c "from sunity_shared.analysis.adapters.rtmw_133_to_coco17 import RTMW_133_TO_COCO17; print(len(RTMW_133_TO_COCO17))" 출력 = 17
    - grep -c "from rtmlib\|import rtmlib\|from mmpose\|import mmpose" backend/shared/python/sunity_shared/analysis/adapters/rtmw_133_to_coco17.py 결과 0 (어댑터는 backend-agnostic)
  </acceptance_criteria>
  <done>
    RTMW 133 → COCO-17 + 폴 확장 매핑 박제. 어댑터가 backend-agnostic (rtmlib import 0) — Task 2 의 RTMWPoseEngine 이 rtmlib 호출 + 어댑터 호출 분리. plan 22 (3D path) 가 본 어댑터 재사용 가능.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RTMWPoseEngine 어댑터 + weights_manifest 라이선스 게이트 + POSE_ENGINE config 플래그</name>
  <files>backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/__init__.py, backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/rtmw_engine.py, backend/shared/python/sunity_shared/analysis/pose_engines/__init__.py, backend/shared/python/sunity_shared/config.py, backend/tests/test_rtmw_engine.py</files>
  <read_first>
    - backend/shared/python/sunity_shared/analysis/interfaces.py (PoseEngine Protocol — plan 19 보강 후 시그니처)
    - backend/shared/python/sunity_shared/analysis/pose_engines/mediapipe_engine.py (기존 MediaPipePoseEngine 패턴 — lazy import + factory + estimate() 흐름)
    - backend/shared/python/sunity_shared/analysis/pose_engines/__init__.py (lazy export 패턴)
    - backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/weights_manifest.json (plan 20 산출 — production_eligible 가중치)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md D-21 (POSE_ENGINE config) + D-22 (confidence 매핑) + D-25 (라이선스 위생)
    - Task 1 산출 (rtmw_133_to_coco17.py)
  </read_first>
  <behavior>
    - Test 1 (test_rtmw_engine_implements_pose_engine_protocol): RTMWPoseEngine 인스턴스가 isinstance check 또는 hasattr('estimate') + signature 검증으로 PoseEngine Protocol 만족 (Python Protocol 은 structural typing — duck typing 으로 검증).
    - Test 2 (test_rtmw_engine_estimate_returns_list_pose_frame): mock rtmlib wholebody inferencer (test fixture) 주입 → engine.estimate(frames=mock_frames, pole_axis=mock_pole_axis) → list[PoseFrame] 반환. len 일치 + 각 element 가 PoseFrame.
    - Test 3 (test_rtmw_engine_loads_manifest_on_init): 정상 manifest (production_eligible=true 가중치 1개 포함) → RTMWPoseEngine() 초기화 성공.
    - Test 4 (test_rtmw_engine_rejects_non_eligible_weights): 모든 가중치 production_eligible=false 인 manifest → RTMWPoseEngine() 초기화 시 LicenseViolationError raise (D-25 게이트).
    - Test 5 (test_rtmw_engine_module_level_no_rtmlib_import): ast.parse(rtmw_engine.py) → ImportFrom/Import 노드 검사 → rtmlib / mmpose / mmcv module-level import 0 (H-2 기존 패턴 정합 — Lambda fail-fast 안전).
    - Test 6 (test_rtmw_engine_no_human_raises_nohumanerror): mock inferencer 가 빈 결과 반환 → estimate() 가 NoHumanError raise (기존 contract 보존).
    - Test 7 (test_pose_engines_init_lazy_exports_rtmw): from sunity_shared.analysis.pose_engines import RTMWPoseEngine 성공. __all__ 에 'RTMWPoseEngine' 포함.
    - Test 8 (test_config_get_pose_engine_returns_rtmw_by_default): POSE_ENGINE env unset → get_pose_engine() 가 'RTMW' string 반환 (D-21 기본값).
    - Test 9 (test_config_get_pose_engine_respects_env): POSE_ENGINE='NLF_SMPLX' env set → get_pose_engine() 가 'NLF_SMPLX' string 반환.
    - Test 10 (test_config_invalid_pose_engine_raises): POSE_ENGINE='INVALID' env set → ValueError 또는 ConfigurationError raise.
  </behavior>
  <action>
    pose_engines/rtmw/__init__.py: docstring 갱신 + __all__ = ['RTMWPoseEngine']. lazy export: __getattr__ 에 RTMWPoseEngine 추가 (mediapipe_engine 패턴 그대로).

    pose_engines/rtmw/rtmw_engine.py: 신설. RTMWPoseEngine 클래스.
    - __init__(self, weights_manifest_path: Path | None = None, target_fps: int = 30): manifest 로드 → production_eligible 가중치 1개 선택 → 가중치 sha256 검증 (plan 20 manifest 의 sha256 필드 — 다운로드 후 검증). production_eligible=true 인 entry 0개면 LicenseViolationError raise.
    - estimate(self, frames: np.ndarray, pole_axis: PoleAxis) -> list[PoseFrame]: lazy import rtmlib → wholebody inferencer 호출 → 각 프레임의 keypoints_133 + scores_133 → convert_rtmw_keypoints_to_coco17_and_pole_ext 호출 → list[PoseFrame] 반환. 전 프레임 사람 미감지 시 NoHumanError.
    - LicenseViolationError 정의 (또는 interfaces.py 의 NoHumanError 와 동일 패턴으로 sunity_shared.errors 에 정의).
    - 모든 heavy import (rtmlib, mmpose, mmcv, torch) 는 __init__ 또는 estimate() 내부 lazy.
    - factory: create_with_inferencer(inferencer, manifest_path, target_fps) — DI 패턴, 단위 테스트용 (rtmlib mock 주입).

    pose_engines/__init__.py: __all__ 에 'RTMWPoseEngine' + 'LicenseViolationError' 추가. lazy __getattr__ 에 RTMWPoseEngine 분기 추가.

    config.py: 신설 또는 기존 파일 확장. get_pose_engine() -> Literal['RTMW', 'NLF_SMPLX'] 함수. os.getenv('POSE_ENGINE', 'RTMW') 후 검증.

    tests/test_rtmw_engine.py: 위 10개 테스트. rtmlib mock 은 unittest.mock.MagicMock 으로 inferencer DI.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_rtmw_engine.py tests/test_rtmw_133_to_coco17_adapter.py tests/test_rtmw_weights_manifest.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - 10개 테스트 모두 PASS (Task 2) + Task 1 12개 + plan 20 7개 = 29개 PASS
    - python -c "from sunity_shared.analysis.pose_engines import RTMWPoseEngine; print(RTMWPoseEngine.__name__)" 출력 = RTMWPoseEngine
    - grep -cE "^(from rtmlib|import rtmlib|from mmpose|import mmpose|from mmcv|import mmcv)" backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/rtmw_engine.py 결과 0 (module-level 부재)
    - python -c "from sunity_shared.config import get_pose_engine; print(get_pose_engine())" 출력 = RTMW
  </acceptance_criteria>
  <done>
    RTMWPoseEngine 이 PoseEngine Protocol 구현체로 박제. weights_manifest 라이선스 게이트 작동. POSE_ENGINE config 플래그 박제. plan 22 (3D path) 가 본 엔진 위에 lifter 또는 RTMW3D 변형 추가 가능.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rtmlib API ↔ 어댑터 | rtmlib 가중치 출력 형식 가정 위반 시 silent corruption |
| weights_manifest ↔ RTMWPoseEngine | manifest 우회로 비-eligible 가중치 로드 시 라이선스 위반 |
| POSE_ENGINE env ↔ pipeline | 'NLF_SMPLX' 환경 변수가 운영에 잘못 설정 시 R&D 엔진 운영 사용 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Tampering | weights_manifest 우회 (직접 가중치 다운로드) | mitigate | Task 2 RTMWPoseEngine.__init__ 가 manifest 필수 + sha256 검증 |
| T-21-02 | Spoofing | rtmlib 모듈 module-level import 가 Lambda 에서 silent fail | mitigate | Task 2 test_rtmw_engine_module_level_no_rtmlib_import 강제 |
| T-21-03 | Tampering | POSE_ENGINE=NLF_SMPLX 운영 환경 설정 (라이선스 위반) | mitigate | plan 25 의 pipeline atomic swap 이 POSE_ENGINE=RTMW 강제 (운영 deployment template) |
| T-21-SC | Tampering | rtmlib 가중치 무결성 (sha256 mismatch) | mitigate | RTMWPoseEngine.__init__ 가 sha256 검증 |
</threat_model>

<verification>
- 본 plan 산출 RTMWPoseEngine 이 plan 22 의 3D path 에 직접 주입 가능 (factory 패턴)
- 본 plan 단독으로는 pipeline 통합 안 됨 — plan 25 가 atomic swap 담당
- 라이선스 게이트 작동 (LicenseViolationError)
- rtmlib lazy import 정합 (Lambda fail-fast 안전)
</verification>

<success_criteria>
- RTMWPoseEngine 이 PoseEngine Protocol 구현체로 박제
- RTMW133ToCOCO17Adapter 가 133 → COCO-17 + 폴 확장 변환
- POSE_ENGINE config 플래그 박제 (기본 RTMW, R&D NLF_SMPLX)
- weights_manifest 라이선스 게이트 작동
- 다운스트림 변경 0 (D-24 약속 검증)
</success_criteria>

<output>
Create `.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-21-SUMMARY.md` when done. SUMMARY 에 RTMW 통합 완료 + plan 22 (3D path) 진입 가능 명시.
</output>
