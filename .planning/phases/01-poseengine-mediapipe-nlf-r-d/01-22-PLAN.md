---
phase: 01-poseengine-mediapipe-nlf-r-d
plan: 22
type: execute
wave: 3
depends_on:
  - 01-21
files_modified:
  - backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/rtmw3d_engine.py
  - backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/__init__.py
  - backend/shared/python/sunity_shared/analysis/pose_engines/__init__.py
  - backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/lifter_pipeline.py
  - backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/three_d_path_decision.md
  - backend/tests/test_rtmw_3d_path.py
  - backend/tests/test_rtmw_3d_path_selected.py
autonomous: false
gap_closure: true
requirements:
  - POSE-01
must_haves:
  truths:
    - "단일 카메라 3D path 가 RTMW3D 직접 (옵션 A) 또는 RTMW 2D + MotionBERT lifter (옵션 B) 두 가지 후보 중 belle 선택으로 박제된다 (D-18)"
    - "선택된 3D path 가 PoseEngine Protocol 구현체로 wrapping 되어 frames + pole_axis → list[PoseFrame] (z 좌표 채움) 흐름이 동작한다"
    - "Pose2Sim (멀티 카메라) 는 본 plan scope 외 — Phase 4 (다중 시점) 에서 통합. 본 plan 은 단일 카메라 우선 ([[single-camera-first-multi-view-last]] 정합)"
    - "3D path 의 출력이 기존 plan 07 (MotionBERT-lite spike) 또는 plan 17 (keypoint mapping audit) 의 mapping fix 와 정합 — 좌우 swap 0 (lift path swap_ratio 1.00 버그 회귀 방지)"
    - "rtmlib RTMW3D weights 가 plan 20 의 weights_manifest 에 등재되어 있고 production_eligible=true (RTMW3D 옵션 선택 시) 또는 MotionBERT lifter weights 의 라이선스 = MIT 박제 (옵션 B 선택 시)"
    - "본 plan 의 belle checkpoint = 옵션 A/B 선택 + 선택 근거 박제. 두 옵션 모두 가능하나 단일 path 만 운영 (config 분기 없음)"
    - "Task 2 checkpoint 가 종료되기 전 선택된 어댑터의 estimate() 가 NotImplementedError 미발생 + 자동 pytest 게이트 PASS (backend/tests/test_rtmw_3d_path_selected.py) — plan 23 의 sweep 진입 전 contract 검증 완료"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/three_d_path_decision.md"
      provides: "옵션 A (RTMW3D) vs 옵션 B (RTMW 2D + MotionBERT lifter) 비교 + belle 결정 박제"
    - path: "backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/rtmw3d_engine.py"
      provides: "옵션 A 어댑터 — RTMW3D 직접 사용 (선택 시 활성화, 비선택 시 stub)"
    - path: "backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/lifter_pipeline.py"
      provides: "옵션 B 어댑터 — RTMW 2D + MotionBERT lifter 결합 (plan 07 lifter 재사용, 선택 시 활성화)"
    - path: "backend/tests/test_rtmw_3d_path.py"
      provides: "두 어댑터 stub 의 Protocol 만족 + 결정 전 NotImplementedError 검증 (Task 1)"
    - path: "backend/tests/test_rtmw_3d_path_selected.py"
      provides: "belle 가 선택한 1개 어댑터의 z-coord 채움 + 좌우 정합 (swap_ratio 0) + PoseEngine Protocol contract 검증 (Task 2 게이트)"
  key_links:
    - from: "선택된 3D path"
      to: "RTMWPoseEngine (plan 21)"
      via: "factory 패턴 또는 wrapping — RTMWPoseEngine 의 estimate() 가 2D 키포인트 산출 → lifter 가 z 채움 (옵션 B) 또는 RTMW3D 가 직접 z 산출 (옵션 A)"
      pattern: "z_coord|3d_path"
    - from: "선택된 3D path"
      to: "plan 17 keypoint mapping audit"
      via: "좌우 swap 회귀 방지 — Cycle 3 audit fix 정합"
      pattern: "swap_ratio"
    - from: "Task 2 선택된 어댑터 실 구현"
      to: "backend/tests/test_rtmw_3d_path_selected.py"
      via: "automated pytest 게이트 (NotImplementedError 부재 + z 채움 + swap_ratio == 0 + Protocol)"
      pattern: "test_selected_engine_"
---

<objective>
RTMW pivot 의 3D path 를 결정 + 박제 (D-18). 단일 카메라 (폰) 환경에서 z 좌표 산출 방법:
- **옵션 A**: RTMW3D 직접 사용 (rtmlib 의 3D wholebody 변형)
- **옵션 B**: RTMW 2D + MotionBERT lifter (plan 07 lifter 재사용)

두 옵션 모두 plan 21 의 RTMWPoseEngine 위에 wrap. belle 가 옵션 선택 → 선택된 path 만 운영 (config 분기 없음 — atomic decision per D-08 정신).

Purpose: plan 21 의 2D keypoints 만으로는 downstream 분석 (kismam, dimensions) 의 3D 각도 계산 불가. 3D path 가 추가되어야 end-to-end 슬라이스 완성. Pose2Sim (멀티 카메라) 는 Phase 4 scope ([[single-camera-first-multi-view-last]] 정합).

Output: 옵션 A/B 비교 문서 + 두 어댑터 stub (선택 시 활성화) + belle 결정 checkpoint + 단위 테스트 2벌 (Task 1 stub-level, Task 2 selected-path 실 contract).

MVP 슬라이스 정합: 본 plan + plan 21 + plan 25 가 합쳐져 RTMW end-to-end 슬라이스 완성. 본 plan 의 belle 결정 후 선택된 path 만 plan 23 (회귀 검증) 의 입력으로 사용. 옵션 A 선택 시 plan 07 MotionBERT lifter 코드 는 R&D 격리 대상 (plan 24).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-21-SUMMARY.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-07-SUMMARY.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-17-SUMMARY.md
@/Users/kimtaesung/Downloads/Sunity_v1_개발지시_RTMW무료스택.md
@backend/shared/python/sunity_shared/analysis/pose_lifters/motionbert_lifter.py
@backend/shared/python/sunity_shared/analysis/pose_engines/mediapipe_lifter_engine.py
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 옵션 A (RTMW3D) vs 옵션 B (RTMW + lifter) 비교 문서 + 두 어댑터 stub 박제</name>
  <files>backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/three_d_path_decision.md, backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/rtmw3d_engine.py, backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/lifter_pipeline.py, backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/__init__.py, backend/shared/python/sunity_shared/analysis/pose_engines/__init__.py, backend/tests/test_rtmw_3d_path.py</files>
  <read_first>
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md D-18 (3D path — 단일 카메라 RTMW3D 또는 monocular 리프팅)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-07-SUMMARY.md (MotionBERT-lite lifter spike 결과 — MIT 라이선스 박제)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-17-SUMMARY.md (Cycle 3 keypoint mapping audit PASS — lift path 좌우 swap 약점 root cause)
    - backend/shared/python/sunity_shared/analysis/pose_lifters/motionbert_lifter.py (기존 MotionBERT lifter 구현 — 옵션 B 재사용 후보)
    - backend/shared/python/sunity_shared/analysis/pose_engines/mediapipe_lifter_engine.py (MediaPipe + lifter 결합 패턴 — 옵션 B 모델로 재사용)
    - /Users/kimtaesung/Downloads/Sunity_v1_개발지시_RTMW무료스택.md §2 (3D 산출 — RTMW3D 또는 모노큘러 리프팅 + 모노큘러 3D 정확도 한계 인지)
    - https://github.com/Tau-J/rtmlib (RTMW3D 변형 가용 가중치 확인)
  </read_first>
  <action>
    three_d_path_decision.md: 신설. 한국어 본문. 섹션:
    - "## 1. 배경" — RTMW pivot 후 단일 카메라 3D path 결정 필요. Pose2Sim (멀티) 은 Phase 4.
    - "## 2. 옵션 A: RTMW3D 직접 사용"
      - Pro: 단일 추론으로 3D 직접 산출, lift path 좌우 swap 약점 (plan 17 audit) 부재, latency 낮음 (단일 모델)
      - Con: rtmlib RTMW3D 가중치 가용성 + 라이선스 확정 필요 (plan 20 audit 표 §3), 정확도 = monocular 3D 한계 (개발 지시 §2 인용)
      - 가중치 후보 URL + sha256 (plan 20 audit 결과)
    - "## 3. 옵션 B: RTMW 2D + MotionBERT lifter (plan 07 재사용)"
      - Pro: MotionBERT MIT 라이선스 박제됨 (plan 07), Plan 17 keypoint mapping fix 적용됨, 분리 가능 (2D 와 3D 독립 디버깅)
      - Con: 2 단계 추론 latency, lift path swap_ratio 1.00 약점 (plan 17 audit 박제 — Cycle 3 audit fix 후 회귀 검증 필요), MotionBERT 가중치 파일 추가
    - "## 4. 결정 기준" — (a) 정확도 (plan 23 회귀 검증 결과), (b) latency (plan 23 ms/frame), (c) 라이선스 (plan 20 audit), (d) plan 17 swap_ratio 회귀 위험.
    - "## 5. belle 결정" — Task 2 checkpoint 산출. 결정 마커는 `selected: option_a` 또는 `selected: option_b` 정확 문자열로 박제 (plan 24 acceptance grep 의존).
    - "## 6. 선택 후 격리 대상" — 옵션 A 선택 시 plan 07 MotionBERT lifter 는 plan 24 에서 R&D 격리. 옵션 B 선택 시 그대로 운영 코드 유지.

    rtmw3d_engine.py: 옵션 A 어댑터 stub. RTMW3DPoseEngine 클래스 (PoseEngine Protocol 구현). __init__ 가 weights_manifest 의 RTMW3D entry 확인 후 LicenseViolationError 가드. estimate() 는 lazy import rtmlib RTMW3D 변형 → 3D keypoints 산출 → 어댑터로 PoseFrame 변환. belle 옵션 A 선택 시에만 실 구현 진행, 그 외에는 NotImplementedError raise + docstring 에 "옵션 A 미선택 — stub" 명시.

    lifter_pipeline.py: 옵션 B 어댑터 stub. RTMWLifterPoseEngine 클래스 (PoseEngine Protocol 구현). __init__ 가 RTMWPoseEngine (plan 21) + MotionBERT lifter (plan 07) 둘 다 instantiate. estimate() = RTMW 2D → lifter → 3D 채움 → PoseFrame. plan 17 mapping fix 적용 (좌우 swap_ratio 회귀 방지 — adapter import 시 fix 적용된 module 사용 명시). belle 옵션 B 선택 시에만 실 구현 완성, 그 외에는 NotImplementedError + docstring.

    pose_engines/rtmw/__init__.py: lazy export 에 RTMW3DPoseEngine + RTMWLifterPoseEngine 추가.

    pose_engines/__init__.py: __all__ 에 RTMW3DPoseEngine + RTMWLifterPoseEngine 추가.

    test_rtmw_3d_path.py: 단위 테스트:
    - test_three_d_path_decision_doc_exists: three_d_path_decision.md 존재 + 5개 섹션 (## 1~## 6 중 belle 결정 §5 + 격리 §6 검증 항목 포함)
    - test_rtmw3d_engine_implements_protocol: RTMW3DPoseEngine 이 PoseEngine Protocol 만족 (hasattr estimate)
    - test_rtmw_lifter_engine_implements_protocol: RTMWLifterPoseEngine 이 PoseEngine Protocol 만족
    - test_both_engines_no_module_level_heavy_import: ast.parse → rtmlib / mmpose / torch module-level import 0
    - test_both_engines_unselected_raises_not_implemented: belle 결정 전 두 엔진 모두 estimate() 호출 시 NotImplementedError (belle 결정 후 선택된 1개만 실 구현 — Task 2 이후 또는 별 plan)
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_rtmw_3d_path.py -x -q && test -f backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/three_d_path_decision.md</automated>
  </verify>
  <acceptance_criteria>
    - 5개 테스트 PASS
    - grep -c "^## " backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/three_d_path_decision.md ≥ 6
    - 문서에 "RTMW3D", "MotionBERT", "MIT", "Pose2Sim", "single-camera-first-multi-view-last" 키워드 모두 등장
    - python -c "from sunity_shared.analysis.pose_engines import RTMW3DPoseEngine, RTMWLifterPoseEngine; print('ok')" 출력 = ok
  </acceptance_criteria>
  <done>
    옵션 A/B 비교 문서 박제. 두 어댑터 stub 박제 (PoseEngine Protocol 만족). belle 결정 (Task 2) 후 선택된 1개만 실 구현.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: belle 3D path 결정 checkpoint + 선택된 path 실 구현 + selected-path contract pytest 게이트</name>
  <files>backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/three_d_path_decision.md, backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/rtmw3d_engine.py, backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/lifter_pipeline.py, backend/tests/test_rtmw_3d_path_selected.py</files>
  <what-built>
    Task 1 산출 — three_d_path_decision.md 의 옵션 A/B 비교표 + 두 어댑터 stub (RTMW3DPoseEngine, RTMWLifterPoseEngine). belle 결정 미실행 상태이므로 estimate() 호출 시 NotImplementedError.
  </what-built>
  <how-to-verify>
    1. cat backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/three_d_path_decision.md
    2. plan 20 의 docs/licenses/rtmw-weights-audit.md §2 에서 RTMW3D 가중치 라이선스 상태 확인 (commercial_ok 인 경우만 옵션 A 후보)
    3. plan 17 SUMMARY 에서 lift path 좌우 swap_ratio 1.00 결과 확인 (옵션 B 위험)
    4. belle 결정 = "옵션 A (RTMW3D)" 또는 "옵션 B (RTMW + MotionBERT lifter)" 중 1개
    5. 결정 박제: three_d_path_decision.md §5 에 belle 응답 + 결정 일자 + 근거 (정확도/latency/라이선스/swap 위험 중 핵심) 추가. 마커 라인 1개 = `selected: option_a` 또는 `selected: option_b` (정확 문자열, plan 24 grep 의존).
    6. 선택된 어댑터의 estimate() 실 구현 진행 — NotImplementedError 제거 + 실 추론 흐름 코드. 비선택 어댑터는 NotImplementedError 유지 (R&D 격리는 plan 24).
    7. backend/tests/test_rtmw_3d_path_selected.py 신설 — 선택된 1개 어댑터에 대해 다음 3개 테스트 작성 (mock inferencer 주입, GPU 의존 없음, < 60s):
       - test_selected_engine_no_not_implemented: 선택된 어댑터의 estimate() 가 NotImplementedError 미발생 (mock frames + PoleAxis 주입 시 list[PoseFrame] 반환)
       - test_selected_engine_z_coord_filled: 반환된 모든 PoseFrame 의 keypoints_3d.z 가 numpy.nan 아님 + 분산 > 0 (스칼라 0 채움 회귀 방지)
       - test_selected_engine_no_left_right_swap: plan 17 mapping fix 정합 — 어깨/엉덩이/무릎/발목 좌우 인덱스에 대해 합성 mock 입력 (좌=+x, 우=-x) 주입 → 출력의 swap_ratio = 0 (즉 모든 프레임에서 좌 keypoint 의 x > 우 keypoint 의 x)
    8. 위 3개 테스트 cd backend && python -m pytest tests/test_rtmw_3d_path_selected.py -x -q 실행 → PASS 확인
    9. commit + push 후 plan 23 (회귀 검증) 진입 가능.
  </how-to-verify>
  <resume-signal>
    belle 가 "approved: option_a" 또는 "approved: option_b" 로 응답. 응답 후 선택된 어댑터의 실 구현 + selected-path 테스트 작성 → pytest PASS 확인 → commit. 작업 commit 후 plan 23 (회귀 검증) 진입 가능.
  </resume-signal>
  <acceptance_criteria>
    - belle 응답 수신 + three_d_path_decision.md §5 에 `selected: option_a` 또는 `selected: option_b` 마커 라인 박제: grep -E "^selected: option_(a|b)$" backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/three_d_path_decision.md 매치 = 1
    - 선택된 어댑터의 estimate() 가 NotImplementedError 미발생: python -c "import importlib, inspect; ... " — 다음 명령 둘 중 1개 (선택된 옵션) PASS:
      - 옵션 A 선택 시: grep -c "raise NotImplementedError" backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/rtmw3d_engine.py 결과 = 0
      - 옵션 B 선택 시: grep -c "raise NotImplementedError" backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/lifter_pipeline.py 결과 = 0
    - 비선택 어댑터는 NotImplementedError 유지 (반대 옵션 파일에 raise NotImplementedError ≥ 1)
    - backend/tests/test_rtmw_3d_path_selected.py 존재: test -f backend/tests/test_rtmw_3d_path_selected.py
    - 선택된 path contract 테스트 3개 PASS: <automated>cd backend && python -m pytest tests/test_rtmw_3d_path_selected.py -x -q</automated>
    - plan 23 진입 게이트 통과
  </acceptance_criteria>
  <done>
    3D path 결정 박제 + 선택된 어댑터 실 구현 + 자동 pytest contract 게이트 PASS. plan 23 (RTMW vs IPSF 회귀 검증) 진입 가능.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 옵션 A RTMW3D ↔ 가중치 라이선스 | RTMW3D 가중치 commercial_ok 미확정 시 선택 불가 |
| 옵션 B lift path ↔ plan 17 swap 약점 | lifter swap_ratio 회귀 위험 — 회귀 검증 필수 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Information Disclosure | RTMW3D 가중치 라이선스 미확정 → 옵션 A 선택 위반 | mitigate | Task 2 가 plan 20 audit cross-reference 강제 |
| T-22-02 | Tampering | 옵션 B 선택 시 lifter swap_ratio 1.00 회귀 | mitigate | Task 2 acceptance — test_selected_engine_no_left_right_swap pytest 게이트 |
| T-22-03 | Repudiation | belle 결정 박제 누락 | mitigate | Task 2 가 §5 갱신 강제 + `selected: option_(a|b)` 마커 grep 검증 |
| T-22-04 | Tampering | 선택된 어댑터가 실 구현 없이 stub 만으로 plan 23 진입 | mitigate | Task 2 acceptance — selected-path pytest (test_rtmw_3d_path_selected.py) 3개 통과 강제 |
</threat_model>

<verification>
- 선택된 path 의 z 좌표 채움 동작 검증 (test_selected_engine_z_coord_filled)
- swap_ratio 회귀 없음 (test_selected_engine_no_left_right_swap — 옵션 A/B 무관 강제)
- 비선택 path 의 stub NotImplementedError 유지 (plan 24 R&D 격리 입력)
- selected 마커 박제 (plan 24 의 conditional 격리 acceptance 가 의존)
</verification>

<success_criteria>
- 단일 카메라 3D path 결정 박제 (D-18 충족)
- 선택된 어댑터가 PoseEngine Protocol 구현체로 동작 + 자동 pytest 게이트 PASS
- plan 23 (회귀 검증) 진입 가능
</success_criteria>

<output>
Create `.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-22-SUMMARY.md` when done. SUMMARY 에 belle 옵션 결정 + 비선택 path 의 격리 대상 명시 (plan 24 입력) + selected-path pytest 통과 결과 박제.
</output>
</content>
</invoke>