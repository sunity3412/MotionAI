---
phase: 01-poseengine-mediapipe-nlf-r-d
plan: 23
type: execute
wave: 4
depends_on:
  - 01-21
  - 01-22
  - 01-15
files_modified:
  - backend/research/evaluations/compare_rtmw_vs_ipsf.py
  - backend/research/evaluations/reports/.gitkeep
  - backend/tests/test_compare_rtmw_vs_ipsf_smoke.py
  - backend/tests/test_compare_rtmw_vs_ipsf_pole_axis.py
autonomous: false
gap_closure: true
supersedes_notes:
  - "본 plan 은 기존 plan 01-14 (5영상 재검증 sweep — RTMPose+MB vs IPSF 게이트) 를 SUPERSEDE 한다. plan 14 는 RTMPose+MB+lifter 백본을 가정했으나 2026-06-02 RTMW pivot 후 백본이 RTMW 단일 wholebody 로 변경. baseline 도 NLF 갭이 아닌 IPSF GeometricCriterion (plan 15 산출) 로 영구 변경. plan 14 는 미실행 상태로 SUPERSEDED 마킹 (코드 회귀 없음, ROADMAP 갱신 별도). plan 14 의 게이트 정의 (갭 ≤ tolerance + line/angle 5/5 PASS) 는 본 plan 이 그대로 계승."
requirements:
  - POSE-01
  - POSE-02
must_haves:
  truths:
    - "compare_rtmw_vs_ipsf.py 가 plan 21 의 RTMWPoseEngine + plan 22 의 선택된 3D path 를 정은지 5영상 (ref-climb / ref-foxtop-split / ref-foxtop / ref-invert / ref-sideway-spin) 에서 실행한다 (D-13 검증 영상 세트 유지)"
    - "baseline = plan 15 의 IPSF GeometricCriterion (사람 점수 라벨링 영구 금지, IPSF Code of Points 단일 기준 — memory analysis-objectivity-no-human-scores + judging-baseline-ipsf-code-of-points 정합)"
    - "Wave 3 진입 게이트 = (a) RTMW 측정 각도 vs IPSF targetValue 갭 ≤ toleranceFull (5/5 PASS) + (b) line/angle 차원 5/5 PASS — 둘 다 강제. NLF 갭 baseline 영구 폐기 (belle 2026-06-01 결정 박제). 한쪽만 PASS 인 경우 보류 (D-16 정신 계승)."
    - "summary.phase1_ready_to_swap 이 두 게이트 모두 PASS 여야 true. plan 25 (atomic swap) 진입 게이트."
    - "추가 보고 지표 = (1) RTMW 단일 score 평균 분포 (D-22 confidence 매핑 합리성 검증) + (2) ms/frame (RTMW + 선택된 3D path) + (3) plan 17 keypoint mapping audit swap_ratio 회귀 (좌우 정합 0)"
    - "belle 시각 검토 checkpoint — 5영상 결과 + 두 게이트 PASS/FAIL + 추가 지표를 belle 가 검토하고 plan 25 (atomic swap) 승인 또는 D-16 따라 보류"
    - "POSE-02 강제: compare_rtmw_vs_ipsf.py 가 HoughPoleDetector 를 import + 영상별 video-level PoleAxis 1개 산출 + 모든 frame.pole_axis 에 적용 + 보고서에 pole_axis 블록 (axis_vector + confidence) 박제 + 저신뢰 폴백 (D-10/D-11) 시 confidence='low' 박제 + 비크래시 — 자동 pytest (test_compare_rtmw_vs_ipsf_pole_axis.py) 로 검증"
  artifacts:
    - path: "backend/research/evaluations/compare_rtmw_vs_ipsf.py"
      provides: "RTMW vs IPSF GeometricCriterion 회귀 검증 스크립트 (D-13~D-16 계승, baseline 변경) + POSE-02 pole-axis 적용"
      contains: "phase1_ready_to_swap"
    - path: "backend/research/evaluations/reports/"
      provides: "회귀 검증 보고서 출력 디렉터리 (기존 plan 06 디렉터리 재사용)"
    - path: "backend/tests/test_compare_rtmw_vs_ipsf_smoke.py"
      provides: "compare_rtmw_vs_ipsf smoke 테스트 — fixture 데이터로 게이트 로직 검증"
    - path: "backend/tests/test_compare_rtmw_vs_ipsf_pole_axis.py"
      provides: "POSE-02 wiring 검증 — HoughPoleDetector import + 보고서 pole_axis 블록 + 저신뢰 폴백 비크래시 (3개 어서션)"
  key_links:
    - from: "compare_rtmw_vs_ipsf.py"
      to: "RTMWPoseEngine + 선택된 3D path (plan 21/22) + IPSF GeometricCriterion loader (plan 15)"
      via: "두 엔진 동일 다운스트림 (compute_joint_angles, dimensions) 입력 + IPSF tolerance 비교"
      pattern: "load_criteria|RTMWPoseEngine"
    - from: "compare_rtmw_vs_ipsf.py"
      to: "HoughPoleDetector (plan 01-01 산출, D-09/D-10/D-11 정합)"
      via: "영상별 video-level PoleAxis 1개 산출 → 모든 frame.pole_axis 에 주입 → 보고서 pole_axis 블록 박제"
      pattern: "HoughPoleDetector|pole_axis"
    - from: "회귀 보고서"
      to: "belle 검토 + Wave 5 (plan 24 + plan 25) gate"
      via: "checkpoint:human-verify (gate=blocking)"
      pattern: "phase1_ready_to_swap"
---

<objective>
RTMW pivot 의 회귀 검증 (Wave 3 entry gate). plan 21 RTMWPoseEngine + plan 22 선택된 3D path 를 정은지 5영상에서 실행 → plan 15 IPSF GeometricCriterion 과 비교 → Wave 3 atomic swap (plan 25) 진입 여부 결정.

본 plan 은 기존 plan 14 (5영상 재검증 sweep, RTMPose+MB+lifter 백본 가정) 를 SUPERSEDE — RTMW pivot 후 백본 + baseline 모두 변경. plan 14 의 게이트 정의 (갭 ≤ tolerance + line/angle 5/5 PASS) 는 그대로 계승. belle 결정 (2026-06-01) = NLF 갭 baseline 영구 폐기, IPSF tolerance baseline 으로 영구 전환.

Purpose: D-13 (검증 영상 5개), D-14 supersede (baseline = IPSF), D-15 (추가 지표), D-16 (검증 실패 시 보류). ROADMAP Success #7 (5영상 sweep IPSF tolerance + line/angle 5/5 PASS). 사람 점수 라벨링 영구 금지 ([[analysis-objectivity-no-human-scores]] 정합).

POSE-02 강제: 본 plan 은 POSE-01 + POSE-02 둘 다 requirement 로 가진다. POSE-02 success criterion #3 (video-level PoleAxis 1개 산출 + 모든 frame.pole_axis 에 적용) 은 자동 pytest (Task 3) 로 검증한다 — sweep 이 pole-axis 없이 "tolerance PASS" 만 박제하고 axis-unaware 각도로 plan 25 게이트 통과하는 anti-pattern 차단.

Output: compare_rtmw_vs_ipsf.py + 회귀 보고서 + smoke 테스트 + pole-axis wiring/report/fallback 테스트 + belle 검토 checkpoint (plan 25 atomic swap gate).

MVP 슬라이스 정합: 본 plan 은 검증 게이트로 단독 user-visible 슬라이스 없음. RTMW end-to-end 슬라이스 (plan 21+22) 가 정은지 영상 5개에서 동작하는지 객관 측정 — 이 검증이 PASS 해야 plan 25 가 운영 pipeline 에 RTMW 를 atomic swap 한다.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-21-SUMMARY.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-22-SUMMARY.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-15-SUMMARY.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-11-SUMMARY.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-12-SUMMARY.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-17-SUMMARY.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-06-PLAN.md
@backend/shared/python/sunity_shared/judging/geometric_criterion.py
@backend/shared/python/sunity_shared/judging/loader.py
@backend/judging_data/criteria/ref-climb.yaml
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: compare_rtmw_vs_ipsf.py + 5영상 sweep + IPSF tolerance 갭 + line/angle 게이트 + pole-axis 적용 + 보고서 출력</name>
  <files>backend/research/evaluations/compare_rtmw_vs_ipsf.py, backend/research/evaluations/reports/.gitkeep, backend/tests/test_compare_rtmw_vs_ipsf_smoke.py</files>
  <read_first>
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-06-PLAN.md (기존 compare_engines.py 골격 — Wave 2 plan 06 산출. 본 plan 은 RTMW vs IPSF 로 baseline 변경 후 재구성)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-15-SUMMARY.md (IPSF GeometricCriterion 스키마 + 로더 + 5영상 YAML 데이터 박제 상태)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-11-SUMMARY.md (Plan 11 sweep 결과 — D-14 4/5 FAIL, line/angle 5/5 N/A 한계 박제)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-17-SUMMARY.md (Cycle 3 keypoint mapping audit PASS — lift path swap_ratio 1.00 약점)
    - backend/shared/python/sunity_shared/judging/geometric_criterion.py + loader.py (plan 15 산출 — load_criteria(motion) API)
    - backend/judging_data/criteria/*.yaml (belle 라벨링 5영상 IPSF 임계값)
    - backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/rtmw_engine.py (plan 21 산출)
    - backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/three_d_path_decision.md (plan 22 belle 결정 — 옵션 A 또는 B)
    - backend/shared/python/sunity_shared/analysis/pole_axis.py (plan 01-01 산출 — HoughPoleDetector + PoleAxis dataclass, D-09/D-10/D-11)
  </read_first>
  <action>
    backend/research/evaluations/compare_rtmw_vs_ipsf.py: 신설. 한국어 docstring + 영어 함수명.
    - main() 진입점: argparse 로 --videos (5영상 path 리스트) + --output-dir + --pose-engine (기본 RTMW + 3D path) + --criteria-dir (기본 backend/judging_data/criteria/)
    - **POSE-02 — pole-axis 적용 (반드시 명시 import + 호출):**
      - 모듈 상단에서 `from sunity_shared.analysis.pole_axis import HoughPoleDetector, PoleAxis` 정확 import (test_compare_rtmw_vs_ipsf_pole_axis.py 가 grep 으로 검증)
      - detect_pole_axis(video) 함수: 영상 첫 N프레임 (예: 30) 에 HoughPoleDetector 적용 → confidence 가중평균으로 video-level PoleAxis 1개 산출 (D-10). 검출 실패/저신뢰 시 PoleAxis(axis_vector=(0,1,0), confidence='low') 반환 (D-11 수직 가정 폴백). 크래시 금지.
      - run_rtmw(video, pole_axis) 함수: frames 추출 → RTMWPoseEngine.estimate(frames, pole_axis=pole_axis) (plan 22 선택된 3D path 통합) → list[PoseFrame] (모든 frame.pole_axis 가 video-level PoleAxis 와 동일 객체 참조) → compute_joint_angles → joint_angles (T, 17) → key moment frame indices (plan 13 의 Gemini extractor 또는 fallback hold detection) 에서 각도 추출
    - compare_to_ipsf(motion_name, measured_angles) 함수: load_criteria(motion) → 각 GeometricCriterion 별 (targetValue, toleranceFull, jointKey, momentKey) → measured_angles 의 해당 joint+moment 각도 추출 → 갭 계산 → within_tolerance (|measured - target| ≤ toleranceFull) bool
    - compute_line_angle_gates(joint_angles, pole_axis) 함수: dimensions.py 의 line_score + angle_score 호출 (pole_axis 인자 전달 — axis-aware) → 5/5 PASS 여부
    - write_report(results, output_dir) 함수: JSON + Markdown 두 형식. JSON 스키마 = {motion: {pole_axis: {axis_vector: [x,y,z], confidence: "high|low"}, ipsf_gaps: [{joint, moment, target, measured, gap, within_tolerance}], line_pass: bool, angle_pass: bool, ms_per_frame: float, rtmw_mean_score: float, lift_swap_ratio: float | null}}. pole_axis 블록 모든 motion 에 필수 (axis_vector null 금지, confidence "high" 또는 "low"). Markdown = 표 형식.
    - summary 함수: phase1_ready_to_swap = (모든 모션의 ipsf_gaps within_tolerance) AND (모든 모션의 line_pass) AND (모든 모션의 angle_pass).

    backend/research/evaluations/reports/.gitkeep: 신설 (디렉터리 보장).

    backend/tests/test_compare_rtmw_vs_ipsf_smoke.py:
    - test_compare_to_ipsf_within_tolerance: GeometricCriterion (target=180, tolerance=20) + measured=175 → within_tolerance=true, gap=5
    - test_compare_to_ipsf_exceeds_tolerance: 같은 criterion + measured=150 → within_tolerance=false, gap=30
    - test_phase1_ready_to_swap_all_pass: 5영상 모두 within_tolerance + line/angle PASS 결과 → ready=true
    - test_phase1_ready_to_swap_partial_fail: 1영상 within_tolerance=false → ready=false
    - test_phase1_ready_to_swap_line_fail: 모든 ipsf within_tolerance + 1영상 line_pass=false → ready=false (둘 다 게이트)
    - test_report_writes_json_and_markdown: write_report → output_dir 에 .json + .md 파일 생성
    - test_smoke_main_with_fixtures: argparse 로 fixture 영상 (또는 mock PoseEngine) 주입 → main() 실행 → output_dir 에 보고서 생성 + exit code 0
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_compare_rtmw_vs_ipsf_smoke.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - 7개 smoke 테스트 PASS
    - python backend/research/evaluations/compare_rtmw_vs_ipsf.py --help 출력에 --videos, --output-dir, --pose-engine, --criteria-dir 4개 인자 노출
    - grep -c "phase1_ready_to_swap" backend/research/evaluations/compare_rtmw_vs_ipsf.py ≥ 2 (정의 + 출력)
    - grep -c "within_tolerance\|toleranceFull" backend/research/evaluations/compare_rtmw_vs_ipsf.py ≥ 3
    - grep -c "load_criteria" backend/research/evaluations/compare_rtmw_vs_ipsf.py ≥ 1 (plan 15 IPSF loader 사용)
    - grep -c "HoughPoleDetector" backend/research/evaluations/compare_rtmw_vs_ipsf.py ≥ 1 (POSE-02 wiring)
    - grep -c "pole_axis" backend/research/evaluations/compare_rtmw_vs_ipsf.py ≥ 3 (detect + run_rtmw 인자 + report 블록)
  </acceptance_criteria>
  <done>
    compare_rtmw_vs_ipsf.py 박제 완료 (POSE-01 + POSE-02 wiring). fixture smoke 테스트 PASS. Task 3 pole-axis 검증 + belle Pod 실 영상 실행 (Task 2) 준비 완료.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: POSE-02 pole-axis wiring + report + 저신뢰 폴백 자동 검증</name>
  <files>backend/tests/test_compare_rtmw_vs_ipsf_pole_axis.py</files>
  <read_first>
    - Task 1 산출 (compare_rtmw_vs_ipsf.py — HoughPoleDetector import + detect_pole_axis + run_rtmw pole_axis 인자 + write_report pole_axis 블록)
    - backend/shared/python/sunity_shared/analysis/pole_axis.py (HoughPoleDetector + PoleAxis dataclass)
    - .planning/REQUIREMENTS.md POSE-02 (success criterion #3 — video-level PoleAxis 1개 산출 + 모든 frame.pole_axis 에 적용)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md D-10/D-11 (시간 안정 + 검출 실패 폴백)
  </read_first>
  <behavior>
    - test_compare_script_imports_hough_pole_detector: 소스 grep 으로 `from sunity_shared.analysis.pole_axis import HoughPoleDetector` 또는 등가 import 라인 존재 검증. 정확 명령: `assert "HoughPoleDetector" in open("backend/research/evaluations/compare_rtmw_vs_ipsf.py").read()`. 또한 importlib 으로 모듈 import 후 `assert hasattr(compare_module, "detect_pole_axis")` (함수 노출 검증).
    - test_report_contains_pole_axis_block_per_motion: fixture mock PoseEngine + mock HoughPoleDetector (confidence='high', axis_vector=(0,1,0)) 주입 → main() 실행 → 결과 JSON 로드 → 모든 motion entry 에 `pole_axis` 블록 존재 + `axis_vector` 가 None 아님 + length=3 list/tuple + `confidence` 가 "high" 또는 "low" 값. 정확 어서션: `assert all('pole_axis' in m and m['pole_axis']['axis_vector'] is not None and len(m['pole_axis']['axis_vector']) == 3 and m['pole_axis']['confidence'] in ('high', 'low') for m in r['motions'])`
    - test_low_confidence_fallback_no_crash: HoughPoleDetector mock 이 빈 결과 또는 ValueError raise → detect_pole_axis 가 PoleAxis(axis_vector=(0,1,0), confidence='low') 반환 + main() 비크래시 (exit code 0) + 결과 JSON 의 해당 motion `pole_axis.confidence == "low"` (D-10/D-11 정합)
    - test_frame_level_pole_axis_matches_video_level: run_rtmw 의 mock 반환 PoseFrame 들의 frame.pole_axis 모두 video-level PoseAxis 와 동일 (axis_vector + confidence) — frame 별 재계산이 아니라 video-level 1개 박제 (POSE-02 #3, D-10 정합)
  </behavior>
  <action>
    backend/tests/test_compare_rtmw_vs_ipsf_pole_axis.py: 신설. pytest + unittest.mock + pathlib + json 만 사용. 외부 의존 0. GPU 의존 0. 60초 안에 완료.

    구조:
    - 모듈 상단 `import json, pathlib, importlib, sys; from unittest.mock import patch, MagicMock`
    - fixture: tmp_path 사용해 output_dir 생성. mock PoseEngine 은 frames 입력 → PoseFrame 5개 (각각 keypoints_3d + pole_axis 채움) 반환. mock HoughPoleDetector 는 detect() → PoleAxis(axis_vector=(0,1,0), confidence='high') 반환 (또는 ValueError/빈 결과로 fallback 분기 테스트).
    - test 1 (test_compare_script_imports_hough_pole_detector): `src = pathlib.Path("backend/research/evaluations/compare_rtmw_vs_ipsf.py").read_text()` → `assert "HoughPoleDetector" in src` AND `assert "from sunity_shared.analysis.pole_axis" in src`. 추가: `mod = importlib.import_module("research.evaluations.compare_rtmw_vs_ipsf")` (sys.path 조정 필요 시 `sys.path.insert(0, "backend")`) → `assert hasattr(mod, "detect_pole_axis")`.
    - test 2 (test_report_contains_pole_axis_block_per_motion): `with patch("research.evaluations.compare_rtmw_vs_ipsf.HoughPoleDetector") as mock_hough, patch("research.evaluations.compare_rtmw_vs_ipsf.RTMWPoseEngine") as mock_rtmw: ...` → 5영상 fixture 입력 → main(...) → output JSON load → assert 위 behavior 항목 그대로.
    - test 3 (test_low_confidence_fallback_no_crash): mock_hough.return_value.detect.side_effect = ValueError("low signal") → main(...) → exit code 0 + JSON 의 모든 motion `pole_axis.confidence == "low"` + axis_vector == [0, 1, 0] (수직 가정 D-11).
    - test 4 (test_frame_level_pole_axis_matches_video_level): run_rtmw 직접 호출 → 반환 list[PoseFrame] 의 모든 frame.pole_axis 가 video-level PoseAxis 와 동일 인스턴스 (또는 동일 axis_vector + confidence).

    헤더 주석: POSE-02 success criterion #3 + D-10/D-11 인용. 4개 어서션 모두 명확.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_compare_rtmw_vs_ipsf_pole_axis.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - 4개 pole-axis 테스트 모두 PASS
    - test 1 (wiring): grep 으로 HoughPoleDetector import + detect_pole_axis 함수 노출 검증
    - test 2 (report): mock 주입 → 보고서 JSON 의 모든 motion 에 pole_axis 블록 (axis_vector + confidence)
    - test 3 (fallback): HoughPoleDetector 저신뢰/예외 → confidence='low' 박제 + 비크래시
    - test 4 (per-frame consistency): video-level PoleAxis 가 모든 frame 에 동일 적용
    - python -c "import pathlib; src = pathlib.Path('backend/research/evaluations/compare_rtmw_vs_ipsf.py').read_text(); assert 'HoughPoleDetector' in src and 'pole_axis' in src" 통과
  </acceptance_criteria>
  <done>
    POSE-02 wiring/report/fallback 자동 검증 박제. sweep 이 pole-axis 없이 PASS 박제하는 anti-pattern 영구 차단. Task 2 belle 검토 진입 가능.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: belle Pod 5영상 실 실행 + 회귀 보고서 검토 + plan 25 atomic swap 승인 또는 D-16 보류</name>
  <what-built>
    Task 1 산출 — compare_rtmw_vs_ipsf.py (POSE-01 + POSE-02 wiring). Task 3 자동 pole-axis 검증 PASS 상태. belle Pod 환경 (RunPod GPU, plan 10 11 setup 그대로 유지) 에서 5영상 실 실행 후 보고서 산출.
  </what-built>
  <how-to-verify>
    1. belle Pod git pull (gsd-pod-work-push-first 정합 — 본 plan commit + push 후 Pod pull)
    2. Pod 환경 변수 = POSE_ENGINE=RTMW. rtmlib 설치 (plan 21 가중치 manifest 의 production_eligible 가중치 다운로드).
    3. plan 22 선택된 3D path (옵션 A 또는 B) 의 의존성 (RTMW3D 가중치 또는 MotionBERT lifter) 설치.
    4. cd /workspace/SunityMotion && python backend/research/evaluations/compare_rtmw_vs_ipsf.py --videos <ref-climb,ref-foxtop-split,ref-foxtop,ref-invert,ref-sideway-spin> --output-dir backend/research/evaluations/reports/sweep_rtmw_<timestamp>/ --pose-engine rtmw --criteria-dir backend/judging_data/criteria/
    5. 출력 보고서 .md 파일 belle 검토. 표 컬럼 = 모션 / pole_axis (axis_vector + confidence) / IPSF 갭 (joint × moment) / line PASS / angle PASS / ms/frame / rtmw_mean_score / swap_ratio.
    6. 결정 path:
       - **(a) 둘 다 PASS (ipsf within_tolerance 5/5 + line/angle 5/5)** → phase1_ready_to_swap=true → belle 승인 "approved: proceed to plan 25" → Wave 5 atomic swap (plan 24 + plan 25) 진입
       - **(b) 한쪽이라도 FAIL** → D-16 보류 → belle 가 root cause 후속 plan 결정 (예: RTMW 가중치 변경 / 3D path 옵션 재선택 / IPSF criteria 갱신 / 추가 spike plan 신설)
       - **(c) 측정값 자체 의심 (plan 13 의 measurement_unreliable_blocked 정신 — 인체학적 비정상)** → plan 16 의 keypoint reliability trace 재실행 또는 별 spike plan
    7. belle 결정 박제: 보고서 디렉터리에 belle_decision.md 추가 + sweep 결과 요약 + 다음 plan 명시.
  </how-to-verify>
  <resume-signal>
    belle 가 "approved: proceed to plan 25" 또는 "blocked: <reason> + next plan = <plan ID or new plan needed>" 으로 응답. approved 시 plan 24 + plan 25 진입. blocked 시 D-16 따라 후속 plan 결정.
  </resume-signal>
  <acceptance_criteria>
    - belle Pod 5영상 sweep 완료 + 보고서 .json + .md 생성
    - 보고서 JSON 모든 motion 에 pole_axis 블록 존재 (axis_vector + confidence) — POSE-02 #3 검증
    - belle 검토 응답 박제 (보고서 디렉터리 + plan 23 SUMMARY)
    - approved 인 경우 phase1_ready_to_swap=true 명시 + plan 24 + plan 25 진입 게이트 통과
    - blocked 인 경우 root cause + 다음 plan 박제 (Wave 5 진입 차단)
  </acceptance_criteria>
  <done>
    RTMW vs IPSF 회귀 검증 결과 박제 (pole-axis 정합 포함). plan 25 (atomic swap) 진입 여부 결정.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RTMW 측정 ↔ IPSF tolerance | 측정값 noise 가 tolerance 안에 들지 못하면 swap 차단 |
| line/angle 차원 ↔ FallbackRecognizer | EXTEND/BENT 인식 한계 (plan 10 박제) — 게이트 PASS 위해 기술 인식기 (Phase 5) 필요 가능 |
| sweep 게이트 ↔ pole-axis 적용 | pole-axis 없이 axis-unaware 각도가 tolerance PASS 박제 → plan 25 swap 후 운영 회귀 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Tampering | belle 결정 우회 (sweep 미실행 후 plan 25 진입) | mitigate | Task 2 checkpoint blocking-human + belle_decision.md 박제 강제 |
| T-23-02 | Information Disclosure | RTMW 측정값 인체학적 비정상 (plan 16 reliability trace 정신) | mitigate | 보고서에 swap_ratio + rtmw_mean_score 표기 + belle 검토 |
| T-23-03 | Tampering | line/angle 차원 N/A (FallbackRecognizer 한계) 를 PASS 로 우회 | mitigate | Task 1 의 phase1_ready_to_swap 가 N/A 를 PASS 로 카운트 금지 (line_pass / angle_pass = bool, None 거부) |
| T-23-04 | Tampering | sweep 이 pole-axis 없이 (HoughPoleDetector 호출 누락) tolerance PASS 박제 → POSE-02 미충족인데 plan 25 진입 | mitigate | Task 3 자동 pytest (test_compare_rtmw_vs_ipsf_pole_axis.py) — wiring + report + fallback 3개 어서션 강제 |
</threat_model>

<verification>
- 5영상 sweep 보고서 (JSON + Markdown) 생성 검증
- 두 게이트 (IPSF tolerance + line/angle 5/5) 모두 강제 — N/A 우회 금지
- POSE-02 pole-axis 적용 자동 검증 (Task 3 4개 어서션)
- belle 결정 박제 (approved 또는 blocked + 다음 plan)
- plan 25 진입 게이트 명확
</verification>

<success_criteria>
- RTMW vs IPSF 회귀 검증 스크립트 박제 (POSE-01)
- POSE-02 pole-axis wiring/report/fallback 자동 검증 박제
- 5영상 실 실행 결과 보고서 산출 + pole_axis 블록 모든 motion 박제
- 두 게이트 PASS 여부 belle 검토
- plan 25 (atomic swap) 진입 또는 D-16 보류 결정 박제
</success_criteria>

<output>
Create `.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-23-SUMMARY.md` when done. SUMMARY 에 belle 결정 + 두 게이트 PASS/FAIL + pole-axis 적용 결과 + plan 25 진입 여부 명시.
</output>
</content>
</invoke>