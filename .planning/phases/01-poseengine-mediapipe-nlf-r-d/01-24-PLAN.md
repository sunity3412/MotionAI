---
phase: 01-poseengine-mediapipe-nlf-r-d
plan: 24
type: execute
wave: 5
depends_on:
  - 01-23
files_modified:
  - backend/research/__init__.py
  - backend/research/README.md
  - backend/research/pose_engines/__init__.py
  - backend/research/pose_engines/nlf/__init__.py
  - backend/research/pose_engines/nlf/nlf_engine.py
  - backend/research/pose_engines/nlf/models/.gitkeep
  - backend/research/pose_engines/mediapipe/__init__.py
  - backend/research/pose_engines/mediapipe/mediapipe_engine.py
  - backend/research/pose_engines/mediapipe/mediapipe_lifter_engine.py
  - backend/research/pose_engines/mediapipe/mediapipe_to_coco17.py
  - backend/research/pose_engines/mediapipe/mediapipe_to_h36m17.py
  - backend/research/pose_engines/mediapipe/motionbert_lifter.py
  - backend/research/evaluations/verify_nlf_pipeline.py
  - backend/research/evaluations/verify_self_comparison_nlf.py
  - backend/research/evaluations/extract_reference_angles_nlf.py
  - backend/research/evaluations/_nlf_smoke.py
  - backend/research/evaluations/verify_nlf_overlay.py
  - backend/shared/python/sunity_shared/analysis/pose_estimator.py
  - backend/shared/python/sunity_shared/analysis/pose_engines/mediapipe_engine.py
  - backend/shared/python/sunity_shared/analysis/pose_engines/mediapipe_lifter_engine.py
  - backend/shared/python/sunity_shared/analysis/pose_engines/__init__.py
  - backend/shared/python/sunity_shared/analysis/adapters/mediapipe_to_coco17.py
  - backend/shared/python/sunity_shared/analysis/adapters/__init__.py
  - backend/shared/python/sunity_shared/analysis/pose_lifters/motionbert_lifter.py
  - backend/shared/python/sunity_shared/analysis/pose_lifters/mediapipe_to_h36m17.py
  - backend/shared/python/sunity_shared/analysis/pose_lifters/__init__.py
  - backend/scripts/nlf_l_multi.torchscript
  - backend/yolo11n.pt
  - backend/scripts/verify_nlf_pipeline.py
  - backend/scripts/verify_self_comparison.py
  - backend/scripts/extract_reference_angles.py
  - backend/scripts/_nlf_smoke.py
  - backend/scripts/verify_nlf_overlay.py
  - .samignore
  - backend/tests/test_nlf_isolation.py
  - backend/tests/test_mediapipe_isolation.py
autonomous: true
gap_closure: true
supersedes_notes:
  - "본 plan 은 기존 plan 01-04 (NLF R&D 격리) 를 SUPERSEDE 하고 확장한다. 01-04 는 NLF 만 격리 대상이었으나 RTMW pivot 후 MediaPipe 운영 코드 (plan 02/03 산출) + (plan 22 비선택 시) MotionBERT lifter 도 격리 대상 (D-23). 01-04 의 D-06/D-07 (NLF 위치 + import 차단 + .samignore) 정책은 그대로 계승. plan 04 는 미실행 상태로 SUPERSEDED 마킹."
requirements:
  - POSE-01
must_haves:
  truths:
    - "NLF 코드 (NlfPoseEstimator, nlf_l_multi.torchscript, yolo11n.pt) 가 backend/research/ 외부 import 경로에서 제거됨 (D-23 NLF 격리 유지)"
    - "MediaPipe 운영 코드 (MediaPipePoseEngine, MediaPipeWithLifterEngine, MediaPipe33ToCOCO17Adapter, MediaPipeToH36M17Adapter) 가 backend/research/pose_engines/mediapipe/ 로 이동됨 — sunity_shared 운영 import 경로 0 (D-23 MediaPipe 운영 제외)"
    - "plan 22 비선택 path 의 코드 (옵션 A 선택 시 MotionBERTLifter + h36m17 adapter, 옵션 B 선택 시 RTMW3D stub) 도 R&D 격리 — 운영 import 경로 0. 선택된 path 의 코드는 운영 경로 유지."
    - "import sunity_shared.analysis.pose_estimator 시 ModuleNotFoundError (NLF 격리)"
    - "from sunity_shared.analysis.pose_engines import MediaPipePoseEngine 시 ImportError 또는 AttributeError (MediaPipe 운영 제외)"
    - "backend/research 가 .samignore 에 명시되어 SAM 빌드 결과물에 포함 안 됨 (D-07 → D-23 계승)"
    - "본 plan 은 Wave 5 — plan 23 belle 회귀 검증 승인 후에만 진행 (plan 23 checkpoint 결과 phase1_ready_to_swap=true 필수)"
    - "RTMW 코드 (plan 21/22 산출, sunity_shared/analysis/pose_engines/rtmw/) 는 운영 경로 유지 — 격리 대상 아님 (D-23 명시)"
    - "조건부 격리 정책의 분기 결정 단일 출처 = backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/three_d_path_decision.md §5 의 `selected: option_a|option_b` 마커. acceptance 가 grep 으로 분기 판정"
  artifacts:
    - path: "backend/research/README.md"
      provides: "R&D 비교군 전용 사용 정책 명시 (NLF PS:License 1.0 + MediaPipe 미사용 사유 + MotionBERT 옵션 미사용 사유)"
    - path: "backend/research/pose_engines/mediapipe/"
      provides: "MediaPipe 운영 코드 격리 위치 (plan 02/03 산출 이동)"
    - path: ".samignore"
      provides: "research/ 경로 SAM 빌드 차단"
    - path: "backend/tests/test_nlf_isolation.py"
      provides: "NLF ImportError 강제 검증"
    - path: "backend/tests/test_mediapipe_isolation.py"
      provides: "MediaPipe 운영 import 0 강제 검증 (D-23 MediaPipe 제외)"
  key_links:
    - from: "sunity_shared.analysis"
      to: "backend/research/pose_engines/{nlf,mediapipe}"
      via: "물리 경로 분리 (D-23) — import 경로 단절"
      pattern: "ModuleNotFoundError|ImportError"
    - from: ".samignore"
      to: "Lambda layer 빌드"
      via: "research/ 디렉터리 제외"
      pattern: "^research/$|^backend/research/$"
    - from: "plan 22 three_d_path_decision.md `selected:` 마커"
      to: "Task 1 §C 조건부 이동 + acceptance branched assertions"
      via: "grep 으로 옵션 A/B 분기 → MotionBERT lifter 격리 여부 결정"
      pattern: "selected: option_(a|b)"
---

<objective>
plan 23 belle 회귀 검증 승인 후 (phase1_ready_to_swap=true) NLF + MediaPipe + plan 22 비선택 path 코드를 모두 backend/research/ 로 격리 (D-23). 본 plan 과 plan 25 는 같은 wave (5) 에서 atomic 하게 진행 — file overlap 0 으로 병렬 실행 가능.

본 plan 은 기존 plan 04 (NLF 격리만) 를 SUPERSEDE — RTMW pivot 후 MediaPipe 운영 코드 (plan 02/03 산출) 도 격리 대상이 됨. D-23 명시 — "MediaPipe 코드 (Plan 02·03 결과물) 는 폐기 또는 R&D 격리 — 운영 import 경로에서 제거".

Purpose: 운영 코드의 라이선스 리스크 0 (NLF PS:License 1.0 비상업 격리) + 사용 안 하는 백본 코드 운영 경로 부재 (D-23). plan 25 의 atomic swap 과 병렬 진행.

조건부 격리 정책: plan 22 의 belle 결정 (옵션 A vs B) 에 따라 MotionBERT lifter 와 h36m17 adapter 의 격리 여부가 달라진다. 분기 결정은 `three_d_path_decision.md §5` 의 `selected: option_a|option_b` 마커를 grep 으로 판정 (단일 출처).

Output: backend/research/pose_engines/{nlf,mediapipe}/ 디렉터리 구조 + 파일 이동 + README + .samignore + ImportError 강제 단위 테스트 2개.

MVP 슬라이스 정합: 본 plan 은 코드 이동이므로 user-visible 슬라이스 없음. plan 25 와 합쳐져 운영 pipeline 의 RTMW atomic swap 완성.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-23-SUMMARY.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-22-SUMMARY.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-04-PLAN.md
@backend/shared/python/sunity_shared/analysis/pose_estimator.py
@backend/shared/python/sunity_shared/analysis/pose_engines/mediapipe_engine.py
@backend/shared/python/sunity_shared/analysis/pose_engines/mediapipe_lifter_engine.py
@backend/shared/python/sunity_shared/analysis/pose_engines/__init__.py
@backend/shared/python/sunity_shared/analysis/adapters/mediapipe_to_coco17.py
@backend/shared/python/sunity_shared/analysis/pose_lifters/motionbert_lifter.py
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: NLF + MediaPipe 코드 + plan 22 비선택 path 코드 backend/research/ 로 이동 + .samignore + 운영 경로 정리</name>
  <files>backend/research/__init__.py, backend/research/README.md, backend/research/pose_engines/__init__.py, backend/research/pose_engines/nlf/__init__.py, backend/research/pose_engines/nlf/nlf_engine.py, backend/research/pose_engines/nlf/models/.gitkeep, backend/research/pose_engines/mediapipe/__init__.py, backend/research/pose_engines/mediapipe/mediapipe_engine.py, backend/research/pose_engines/mediapipe/mediapipe_lifter_engine.py, backend/research/pose_engines/mediapipe/mediapipe_to_coco17.py, backend/research/pose_engines/mediapipe/mediapipe_to_h36m17.py, backend/research/pose_engines/mediapipe/motionbert_lifter.py, backend/research/evaluations/verify_nlf_pipeline.py, backend/research/evaluations/verify_self_comparison_nlf.py, backend/research/evaluations/extract_reference_angles_nlf.py, backend/research/evaluations/_nlf_smoke.py, backend/research/evaluations/verify_nlf_overlay.py, backend/shared/python/sunity_shared/analysis/pose_estimator.py, backend/shared/python/sunity_shared/analysis/pose_engines/mediapipe_engine.py, backend/shared/python/sunity_shared/analysis/pose_engines/mediapipe_lifter_engine.py, backend/shared/python/sunity_shared/analysis/pose_engines/__init__.py, backend/shared/python/sunity_shared/analysis/adapters/mediapipe_to_coco17.py, backend/shared/python/sunity_shared/analysis/adapters/__init__.py, backend/shared/python/sunity_shared/analysis/pose_lifters/motionbert_lifter.py, backend/shared/python/sunity_shared/analysis/pose_lifters/mediapipe_to_h36m17.py, backend/shared/python/sunity_shared/analysis/pose_lifters/__init__.py, backend/scripts/nlf_l_multi.torchscript, backend/yolo11n.pt, backend/scripts/verify_nlf_pipeline.py, backend/scripts/verify_self_comparison.py, backend/scripts/extract_reference_angles.py, backend/scripts/_nlf_smoke.py, backend/scripts/verify_nlf_overlay.py, .samignore</files>
  <read_first>
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md D-23 (NLF 격리 유지 + MediaPipe 운영 경로 제거)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-04-PLAN.md (기존 NLF 격리 패턴 — backend/research/pose_engines/nlf/ + .samignore)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-22-SUMMARY.md (옵션 A/B 결정 — 비선택 path 의 격리 대상 명시)
    - backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/three_d_path_decision.md §5 (selected 마커 — 본 plan 의 조건부 격리 분기 결정 단일 출처)
    - backend/shared/python/sunity_shared/analysis/pose_engines/__init__.py (lazy export 패턴 — MediaPipe export 제거 + RTMW export 유지)
    - backend/shared/python/sunity_shared/analysis/pose_engines/mediapipe_engine.py (이동 대상 1)
    - backend/shared/python/sunity_shared/analysis/pose_estimator.py (이동 대상 — NLF NlfPoseEstimator)
    - CLAUDE.md (.samignore + sam build --use-container 정책)
  </read_first>
  <action>
    **분기 결정 (반드시 가장 먼저 실행):**

    DECISION 변수 산출 — backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/three_d_path_decision.md §5 의 마커 라인을 grep 하여 옵션 A/B 판정. 정확 명령:
    ```
    DECISION=$(grep -E "^selected: option_(a|b)$" backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/three_d_path_decision.md | head -1 | awk '{print $2}')
    ```
    DECISION 값 = `option_a` 또는 `option_b`. 미박제 시 본 plan 진입 차단 (plan 22 Task 2 미완료 → plan 23 진입 불가 → plan 24 진입 불가, 본 점검은 fail-fast 안전망).

    **이동 작업 (git mv 권장 — 히스토리 보존):**

    A. NLF (기존 plan 04 계획 그대로, 무조건):
    - backend/shared/python/sunity_shared/analysis/pose_estimator.py → backend/research/pose_engines/nlf/nlf_engine.py
    - backend/scripts/nlf_l_multi.torchscript → backend/research/pose_engines/nlf/models/nlf_l_multi.torchscript
    - backend/yolo11n.pt → backend/research/pose_engines/nlf/models/yolo11n.pt
    - backend/scripts/verify_nlf_pipeline.py → backend/research/evaluations/verify_nlf_pipeline.py
    - backend/scripts/verify_self_comparison.py → backend/research/evaluations/verify_self_comparison_nlf.py
    - backend/scripts/extract_reference_angles.py → backend/research/evaluations/extract_reference_angles_nlf.py
    - backend/scripts/_nlf_smoke.py → backend/research/evaluations/_nlf_smoke.py
    - backend/scripts/verify_nlf_overlay.py → backend/research/evaluations/verify_nlf_overlay.py

    B. MediaPipe (D-23 추가 — 무조건, MediaPipe 운영 백본 영구 제외):
    - backend/shared/python/sunity_shared/analysis/pose_engines/mediapipe_engine.py → backend/research/pose_engines/mediapipe/mediapipe_engine.py
    - backend/shared/python/sunity_shared/analysis/pose_engines/mediapipe_lifter_engine.py → backend/research/pose_engines/mediapipe/mediapipe_lifter_engine.py
    - backend/shared/python/sunity_shared/analysis/adapters/mediapipe_to_coco17.py → backend/research/pose_engines/mediapipe/mediapipe_to_coco17.py

    C. plan 22 비선택 path (조건부 — DECISION 분기):

    **C-1. DECISION == option_a (RTMW3D 직접 선택)** → MotionBERT lifter + h36m17 adapter 격리 + RTMW3D stub 운영 유지:
    - backend/shared/python/sunity_shared/analysis/pose_lifters/motionbert_lifter.py → backend/research/pose_engines/mediapipe/motionbert_lifter.py
    - backend/shared/python/sunity_shared/analysis/pose_lifters/mediapipe_to_h36m17.py → backend/research/pose_engines/mediapipe/mediapipe_to_h36m17.py
    - backend/shared/python/sunity_shared/analysis/pose_lifters/__init__.py 삭제 (디렉터리 비움)
    - backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/lifter_pipeline.py 는 stub 그대로 유지 (NotImplementedError) — 운영 경로 잔존하나 호출 없음 (선택은 plan 25 wiring 에서)
    - backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/rtmw3d_engine.py 는 운영 경로 유지 (실 구현 완료, plan 22 Task 2 산출)

    **C-2. DECISION == option_b (RTMW + MotionBERT lifter 선택)** → MotionBERT lifter + h36m17 adapter 운영 유지 + RTMW3D stub 격리는 선택 (코드량 작아 운영 잔존 허용, 단 acceptance 가 lifter 경로 보존 확인):
    - backend/shared/python/sunity_shared/analysis/pose_lifters/motionbert_lifter.py 유지 (운영 경로)
    - backend/shared/python/sunity_shared/analysis/pose_lifters/mediapipe_to_h36m17.py 유지 (운영 경로 — lifter 가 H3.6M 17 입력 필요)
    - backend/shared/python/sunity_shared/analysis/pose_lifters/__init__.py 유지
    - backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/lifter_pipeline.py 는 실 구현 (plan 22 Task 2 산출) 유지
    - backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/rtmw3d_engine.py 는 stub 그대로 유지 (NotImplementedError) — 운영 경로 잔존하나 호출 없음

    D. 운영 경로 정리:
    - backend/shared/python/sunity_shared/analysis/pose_engines/__init__.py: lazy __getattr__ 에서 MediaPipePoseEngine + MediaPipeWithLifterEngine 분기 제거. __all__ 에서도 제거. RTMWPoseEngine + (RTMW3DPoseEngine, RTMWLifterPoseEngine 둘 다 export 유지 — stub 호출 없으나 import 가능 상태).
    - backend/shared/python/sunity_shared/analysis/adapters/__init__.py: MediaPipe33ToCOCO17Adapter export 제거. RTMW133ToCOCO17Adapter 만 export.

    E. .samignore: 신설 (없는 경우) 또는 갱신. 라인:
    ```
    backend/research/
    docs/research/
    docs/adr/
    ```

    F. backend/research/README.md: 신설. 한국어 본문. 섹션:
    - "## 1. 목적" — R&D 비교군 전용 (NLF, MediaPipe, 비선택 3D path). 운영 코드 import 금지.
    - "## 2. NLF (PS:License 1.0 비상업)" — 사용 환경 = 사내 R&D 노트북/RunPod, 공개 베타·유료 파일럿·고객 영상 처리 금지. 출시 전 Max Planck Innovation 상업 라이선스 클리어 필수.
    - "## 3. MediaPipe (운영 미사용 사유)" — Apache 2.0 안전하지만 2026-06-02 RTMW pivot 후 RTMW 단일 백본 채택. MediaPipe 는 R&D 비교군으로만 유지.
    - "## 4. plan 22 비선택 3D path" — belle DECISION 박제 + 격리 위치 + 사유 (DECISION 변수 값에 따라 motionbert_lifter / mediapipe_to_h36m17 격리 여부 명시).
    - "## 5. .samignore 정합" — SAM 빌드 결과물 에서 본 디렉터리 전체 제외.

    G. import 경로 정리 — sunity_shared 안 모든 .py 파일 grep 으로 MediaPipe / NLF import 잔여 0 확인 후 정리:
    - grep -RIn "from sunity_shared.analysis.pose_estimator\|from .pose_estimator\|MediaPipePoseEngine\|MediaPipeWithLifterEngine\|MediaPipe33ToCOCO17Adapter" backend/shared/python/sunity_shared/ — 결과 0

    DECISION == option_a 의 경우 추가:
    - grep -RIn "from sunity_shared.analysis.pose_lifters\|from .pose_lifters\|MotionBERTLifter\|MediaPipeToH36M17Adapter" backend/shared/python/sunity_shared/ — 결과 0 (lifter 운영 경로 단절 확인)
  </action>
  <verify>
    <automated>cd backend && python -c "import sys; sys.path.insert(0, 'shared/python'); import sunity_shared.analysis.pose_estimator" 2>&1 | grep -q "ModuleNotFoundError\|No module" && cd backend && python -c "import sys; sys.path.insert(0, 'shared/python'); from sunity_shared.analysis.pose_engines import MediaPipePoseEngine" 2>&1 | grep -q "AttributeError\|ImportError\|cannot import" && grep -cE "^backend/research/$|^research/$" .samignore</automated>
  </verify>
  <acceptance_criteria>
    **무조건 acceptance (옵션 A/B 무관):**
    - python -c "from sunity_shared.analysis.pose_estimator import NlfPoseEstimator" → ModuleNotFoundError
    - python -c "from sunity_shared.analysis.pose_engines import MediaPipePoseEngine" → AttributeError 또는 ImportError
    - python -c "from sunity_shared.analysis.pose_engines import RTMWPoseEngine; print('ok')" → ok 출력
    - grep -RIn "MediaPipePoseEngine\|MediaPipeWithLifterEngine\|MediaPipe33ToCOCO17Adapter\|NlfPoseEstimator" backend/shared/python/sunity_shared/ | grep -v '^#' | wc -l 결과 0
    - grep -cE "^backend/research/$|^research/$" .samignore 매치 ≥ 1
    - ls backend/research/pose_engines/nlf/nlf_engine.py 존재
    - ls backend/research/pose_engines/mediapipe/mediapipe_engine.py 존재
    - ls backend/research/README.md 존재
    - test ! -f backend/shared/python/sunity_shared/analysis/pose_estimator.py
    - test ! -f backend/shared/python/sunity_shared/analysis/pose_engines/mediapipe_engine.py
    - grep -E "^selected: option_(a|b)$" backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/three_d_path_decision.md | wc -l 결과 = 1 (DECISION 박제 사전 검증)

    **분기 acceptance (옵션 A 선택 시 — DECISION=="option_a"):**
    아래 명령은 옵션 A 일 때만 실행/평가. shell 형식:
    ```
    DECISION=$(grep -E "^selected: option_(a|b)$" backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/three_d_path_decision.md | head -1 | awk '{print $2}')
    if [ "$DECISION" = "option_a" ]; then
      test ! -f backend/shared/python/sunity_shared/analysis/pose_lifters/motionbert_lifter.py    # 격리됨
      test ! -f backend/shared/python/sunity_shared/analysis/pose_lifters/mediapipe_to_h36m17.py  # 격리됨
      test -f backend/research/pose_engines/mediapipe/motionbert_lifter.py                        # 격리 위치 존재
      test -f backend/research/pose_engines/mediapipe/mediapipe_to_h36m17.py                      # 격리 위치 존재
      grep -RIn "from sunity_shared.analysis.pose_lifters\|MotionBERTLifter\|MediaPipeToH36M17Adapter" backend/shared/python/sunity_shared/ | grep -v '^#' | wc -l   # 결과 0
    fi
    ```

    **분기 acceptance (옵션 B 선택 시 — DECISION=="option_b"):**
    ```
    DECISION=$(grep -E "^selected: option_(a|b)$" backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/three_d_path_decision.md | head -1 | awk '{print $2}')
    if [ "$DECISION" = "option_b" ]; then
      test -f backend/shared/python/sunity_shared/analysis/pose_lifters/motionbert_lifter.py     # 운영 경로 보존
      test -f backend/shared/python/sunity_shared/analysis/pose_lifters/mediapipe_to_h36m17.py   # 운영 경로 보존
      test -f backend/shared/python/sunity_shared/analysis/pose_lifters/__init__.py              # 패키지 보존
      test ! -f backend/research/pose_engines/mediapipe/motionbert_lifter.py                     # R&D 측 미존재 (격리 안 함)
      test ! -f backend/research/pose_engines/mediapipe/mediapipe_to_h36m17.py                   # R&D 측 미존재 (격리 안 함)
      python -c "import sys; sys.path.insert(0, 'backend/shared/python'); from sunity_shared.analysis.pose_lifters.motionbert_lifter import MotionBERTLifter; print('ok')"   # 출력 = ok (lifter import 가능)
    fi
    ```

    위 분기 블록은 SHELL 에 `set -e` 와 함께 실행한다. 어느 branch 에서든 명령 1개라도 실패 시 plan 24 acceptance FAIL → plan 25 진입 차단.
  </acceptance_criteria>
  <done>
    NLF + MediaPipe + (DECISION 에 따라) 비선택 3D path 코드가 backend/research/ 로 격리됨. 운영 경로 import 0. .samignore 갱신. RTMW + 선택된 3D path 의 운영 경로만 유지. 분기 acceptance 가 옵션 A/B 양쪽 모두 명시적으로 검증.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 격리 검증 단위 테스트 (NLF + MediaPipe ImportError 강제) + grep 회귀 가드</name>
  <files>backend/tests/test_nlf_isolation.py, backend/tests/test_mediapipe_isolation.py</files>
  <read_first>
    - backend/shared/python/sunity_shared/analysis/pose_engines/__init__.py (Task 1 정리 후 — RTMW 만 export 확인)
    - Task 1 산출 (이동 결과)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-04-PLAN.md (기존 H-5 grep 강제 패턴 — backend/scripts 의 NLF/YOLO 잔여 0)
    - backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/three_d_path_decision.md §5 (selected 마커 — option_a/b 에 따라 lifter 테스트 분기 필요 시 참조)
  </read_first>
  <behavior>
    - test_nlf_pose_estimator_unimportable: importlib.import_module('sunity_shared.analysis.pose_estimator') → ModuleNotFoundError raise
    - test_nlf_torchscript_not_in_scripts: pathlib.Path('backend/scripts/nlf_l_multi.torchscript').exists() == False
    - test_yolo_weights_not_in_backend: pathlib.Path('backend/yolo11n.pt').exists() == False
    - test_nlf_verify_scripts_not_in_scripts: backend/scripts/verify_nlf_pipeline.py + verify_self_comparison.py + extract_reference_angles.py + _nlf_smoke.py + verify_nlf_overlay.py 모두 부재 (H-5 강제)
    - test_nlf_files_present_in_research: backend/research/pose_engines/nlf/nlf_engine.py 존재
    - test_mediapipe_pose_engine_unimportable: from sunity_shared.analysis.pose_engines import MediaPipePoseEngine → AttributeError 또는 ImportError
    - test_mediapipe_with_lifter_engine_unimportable: from sunity_shared.analysis.pose_engines import MediaPipeWithLifterEngine → AttributeError 또는 ImportError
    - test_mediapipe_adapter_unimportable: from sunity_shared.analysis.adapters.mediapipe_to_coco17 import convert_landmarks_to_coco17_and_pole_ext → ModuleNotFoundError
    - test_mediapipe_files_present_in_research: backend/research/pose_engines/mediapipe/mediapipe_engine.py 존재
    - test_rtmw_pose_engine_importable: from sunity_shared.analysis.pose_engines import RTMWPoseEngine 성공 (RTMW 운영 유지 확인)
    - test_no_mediapipe_import_in_sunity_shared: subprocess grep -RIn "from sunity_shared.analysis.pose_engines import MediaPipe\|from sunity_shared.analysis.adapters.mediapipe" backend/shared/python/sunity_shared/ 결과 라인 수 = 0 (주석 라인 제외 grep -v '^#')
    - test_no_nlf_import_in_sunity_shared: subprocess grep -RIn "from sunity_shared.analysis.pose_estimator\|NlfPoseEstimator" backend/shared/python/sunity_shared/ 결과 라인 수 = 0
    - test_lifter_isolation_matches_decision: three_d_path_decision.md §5 의 `selected:` 마커 grep → option_a 면 `backend/shared/python/sunity_shared/analysis/pose_lifters/motionbert_lifter.py` 부재 + `backend/research/pose_engines/mediapipe/motionbert_lifter.py` 존재. option_b 면 lifter 운영 경로 존재 + R&D 측 미존재. (Task 1 §C 분기 acceptance 의 pytest mirror — 분기 위반 시 즉시 FAIL)
  </behavior>
  <action>
    test_nlf_isolation.py: 기존 plan 04 계획 의 단위 테스트 (있는 경우) 를 기반으로 위 테스트 1~5 작성. import 차단 + 파일 부재 + 이동 후 위치 확인. subprocess grep gate 는 grep -v '^#' | grep -c 패턴 (header 주석 carbon-invalidating 방지 — gsd 규정).

    test_mediapipe_isolation.py: 신설. 위 테스트 6~13 작성. test_lifter_isolation_matches_decision 은 pathlib + 단일 grep 으로 DECISION 판정 후 분기 — pytest.skip 금지 (양쪽 분기 모두 어서션 강제).

    두 테스트 파일 모두 pathlib + importlib + subprocess (grep) 조합. 외부 의존 0.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_nlf_isolation.py tests/test_mediapipe_isolation.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - 13개 테스트 모두 PASS (NLF 5 + MediaPipe 7 + lifter 분기 1)
    - Task 1 acceptance 의 grep 검증 결과 0 유지
    - Task 1 §C 분기 acceptance (옵션 A/B) 와 test_lifter_isolation_matches_decision pytest 가 동일 분기 정책을 강제 (이중 게이트)
  </acceptance_criteria>
  <done>
    NLF + MediaPipe 격리 단위 테스트 박제. import 회귀 시 즉시 FAIL. lifter 격리도 분기 별 검증. plan 25 atomic swap 과 병렬 진행 가능.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 운영 import 경로 ↔ R&D 코드 | grep 우회 import (예: importlib 동적 import) 차단 |
| .samignore ↔ SAM 빌드 | .samignore 미설정 시 R&D 코드가 Lambda 패키지에 포함 |
| Task 1 §C 분기 ↔ DECISION 마커 | DECISION 박제 누락 또는 잘못된 분기 적용 시 lifter 경로 깨짐 → 운영 ImportError |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Tampering | 격리 후 NLF 코드를 운영 경로로 재import | mitigate | Task 2 test_nlf_pose_estimator_unimportable 강제 |
| T-24-02 | Tampering | MediaPipe 코드 재import | mitigate | Task 2 test_mediapipe_pose_engine_unimportable 강제 |
| T-24-03 | Information Disclosure | .samignore 미설정 → Lambda 패키지에 NLF 가중치 포함 (PS:License 위반) | mitigate | Task 1 의 .samignore 갱신 + SAM 빌드 검증 (plan 25 task 에서) |
| T-24-04 | Tampering | DECISION 마커 무시하고 lifter 잘못 격리 (옵션 B 인데 lifter 격리, 또는 옵션 A 인데 lifter 잔존) → 운영 ImportError 또는 미사용 코드 잔존 | mitigate | Task 1 §C 분기 acceptance + Task 2 test_lifter_isolation_matches_decision 이중 게이트 |
</threat_model>

<verification>
- 13개 격리 테스트 PASS
- grep 회귀 가드 작동
- backend/research/README.md 박제
- .samignore 갱신
- Task 1 §C 분기 acceptance 가 옵션 A/B 양쪽 모두 명시적으로 어서션
</verification>

<success_criteria>
- NLF + MediaPipe + (DECISION 기반) 비선택 3D path 코드 격리 완료
- 운영 import 경로 0 (D-23 충족)
- RTMW 운영 경로 유지
- DECISION 분기가 acceptance + pytest 양쪽에서 강제됨 (이중 게이트)
- plan 25 atomic swap 과 병렬 진행 (file overlap 0)
</success_criteria>

<output>
Create `.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-24-SUMMARY.md` when done. SUMMARY 에 격리 통계 (이동된 파일 수 + 운영 경로 잔여 import 0 + DECISION 값 + 분기 acceptance 결과) 명시.
</output>
</content>
</invoke>