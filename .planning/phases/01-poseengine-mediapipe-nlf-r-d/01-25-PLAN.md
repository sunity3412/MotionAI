---
phase: 01-poseengine-mediapipe-nlf-r-d
plan: 25
type: execute
wave: 6
depends_on:
  - 01-23
  - 01-24
files_modified:
  - backend/functions/pipeline/app.py
  - backend/runpod_inference/requirements.txt
  - backend/runpod_inference/server.py
  - backend/runpod_inference/setup.sh
  - backend/runpod_inference/README.md
  - backend/tests/test_pipeline_dispatch.py
autonomous: false
gap_closure: true
supersedes_notes:
  - "본 plan 은 기존 plan 01-05 (pipeline atomic swap NLF -> MediaPipe) 를 SUPERSEDE 한다. swap target 이 MediaPipe 가 아닌 RTMW + plan 22 선택된 3D path 로 변경 (D-23/D-24). D-08 atomic swap 정신 (중간 상태 없음, config 플래그 점진 롤아웃 없음) 그대로 계승. H-2 (lazy import) / H-3 (poleAxis 보존) / M-4 (README NLF 잔여 0) 박제도 그대로 계승. plan 05 는 미실행 상태로 SUPERSEDED 마킹."
requirements:
  - POSE-01
  - POSE-02
must_haves:
  truths:
    - "D-08 박제: atomic swap — pipeline/app.py 의 NLF 참조가 RTMWPoseEngine + 선택된 3D path + HoughPoleDetector + PoleAxisAligner 흐름으로 1:1 교체 (NLF 참조 0, MediaPipe 참조 0, 중간 상태 없음, config 플래그 없음)"
    - "RunPod requirements.txt 에 rtmlib + mmpose + mmcv + opencv-python-headless + scipy 추가, ultralytics + mediapipe 제거 (또는 MotionBERT 의존 추가 시 plan 22 선택 정합)"
    - "RunPod server.py 가 RTMWPoseEngine 로 swap + setup.sh 가 RTMW 가중치 (plan 20 weights_manifest production_eligible) 다운로드 + sha256 검증"
    - "Lambda 폴백 경로가 ConfigurationError 또는 RuntimeError 로 fail-fast (rtmlib 미설치 환경에서 silent 분석 진행 차단, H-2 정신 계승)"
    - "pipeline/app.py 에 rtmlib, mmpose, cv2, scipy 등 heavy library module-level import 0 — 모든 heavy import 는 _ensure_adapters() 내부 lazy (H-2 박제)"
    - "분석 결과 doc (Firestore + RunPod result) 에 poleAxis 메타 (axis_vector + confidence_level + source) 보존 — D-12 + H-3 박제"
    - "RunPod README 가 RTMW 셋업으로 갱신 (NLF_MODEL_PATH / NLF 운영 메모 0 + MediaPipe 운영 메모 0 — M-4 계승)"
    - "POSE_ENGINE=RTMW 환경 변수가 Lambda + RunPod 양쪽 deployment template 에 박제됨 (D-21 운영 기본값 강제)"
    - "본 plan 은 Wave 6 — plan 23 belle 회귀 검증 승인 + plan 24 R&D 격리 통과 후 진행"
  artifacts:
    - path: "backend/functions/pipeline/app.py"
      provides: "RTMW + 3D path + 폴 축 정렬 파이프라인 (NLF / MediaPipe 호출 0, heavy import lazy)"
    - path: "backend/runpod_inference/requirements.txt"
      provides: "rtmlib + mmpose + mmcv + opencv-python-headless + scipy 의존"
    - path: "backend/runpod_inference/server.py"
      provides: "RunPod GPU 추론 = RTMWPoseEngine + 선택된 3D path"
    - path: "backend/runpod_inference/setup.sh"
      provides: "RTMW 가중치 다운로드 + sha256 검증 + 환경 변수 설정"
    - path: "backend/runpod_inference/README.md"
      provides: "RunPod RTMW 운영 가이드 (NLF / MediaPipe 메모 0)"
  key_links:
    - from: "pipeline/app.py"
      to: "RTMWPoseEngine + 3D path (옵션 A 또는 B) + HoughPoleDetector"
      via: "_ensure_adapters() lazy load + _angles_from_video() 새 흐름"
      pattern: "RTMWPoseEngine|HoughPoleDetector"
    - from: "RunPod setup.sh"
      to: "rtmlib 가중치 + plan 20 weights_manifest"
      via: "wget + sha256 검증 + 환경 변수 export"
      pattern: "rtmlib|weights_manifest"
    - from: "POSE_ENGINE 환경 변수"
      to: "Lambda / RunPod deployment template"
      via: "기본값 RTMW 강제 — NLF_SMPLX 운영 사용 차단"
      pattern: "POSE_ENGINE=RTMW"
---

<objective>
plan 21 (RTMWPoseEngine) + plan 22 (3D path) + plan 23 (회귀 검증 PASS) + plan 24 (R&D 격리) 결과를 활용해 atomic swap 수행 — pipeline/app.py 의 NLF 참조를 RTMWPoseEngine + 선택된 3D path + HoughPoleDetector + PoleAxisAligner 흐름으로 교체. RunPod requirements.txt + server.py + setup.sh + README 갱신.

본 plan 은 기존 plan 05 (NLF -> MediaPipe swap) 를 SUPERSEDE — swap target 이 RTMW 로 변경. D-08 / H-2 / H-3 / M-4 박제 그대로 계승.

모든 heavy library import (rtmlib, mmpose, mmcv, cv2, scipy, RTMWPoseEngine, HoughPoleDetector, PoleAxisAligner) 는 module-level 이 아닌 _ensure_adapters() 내부 lazy — Lambda module load 는 rtmlib 부재 환경에서도 성공 (H-2 박제). 결과 doc 에 poleAxis 메타 보존 (H-3 D-12 박제). README 에 NLF / MediaPipe 잔여 0 (M-4 박제).

Purpose: D-08 (atomic swap) + H-2 (Lambda fail-fast 가 module import 에서 깨지지 않음) + H-3 (분석 결과에 poleAxis 보존) + M-4 (README 갱신) + D-21 (POSE_ENGINE=RTMW 운영 강제) 박제.

Output: pipeline/app.py 교체 + RunPod requirements.txt + server.py + setup.sh + README 갱신 + pipeline_dispatch 통합 테스트.

MVP 슬라이스 정합: 본 plan 통과 시 RTMW pivot 의 end-to-end 운영 슬라이스 완성 — Mode 1/3 영상 업로드 -> S3 -> SQS -> Lambda -> RunPod RTMW + 3D path -> Firestore. 사용자 (수강생) 시점에서는 분석 결과가 변하지 않으나 백본은 라이선스 비용 0 RTMW 로 swap 완료.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-21-SUMMARY.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-22-SUMMARY.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-23-SUMMARY.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-24-SUMMARY.md
@.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-05-PLAN.md
@backend/functions/pipeline/app.py
@backend/runpod_inference/server.py
@backend/runpod_inference/requirements.txt
@backend/runpod_inference/README.md
@backend/tests/test_pipeline_dispatch.py
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pipeline/app.py atomic swap (NLF -> RTMW + 3D path) + heavy import lazy + poleAxis 메타 보존 + Lambda fail-fast</name>
  <files>backend/functions/pipeline/app.py, backend/tests/test_pipeline_dispatch.py</files>
  <read_first>
    - backend/functions/pipeline/app.py (기존 NLF 직접 import line 131-132 + _ensure_adapters + _angles_from_video + Firestore complete_analysis)
    - backend/shared/python/sunity_shared/analysis/pose_engines/__init__.py (plan 24 정리 후 — RTMW export 만)
    - backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/rtmw_engine.py (plan 21 산출)
    - backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/three_d_path_decision.md (plan 22 선택된 3D path)
    - backend/shared/python/sunity_shared/analysis/pole/detector.py (HoughPoleDetector — plan 03 산출)
    - backend/shared/python/sunity_shared/analysis/pole/aligner.py (PoleAxisAligner — plan 03 산출)
    - backend/shared/python/sunity_shared/firestore_admin.py (complete_analysis 함수 — poleAxis 메타 저장)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-05-PLAN.md (기존 swap 패턴 — Task 1 ~ 4 구조 참조)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md D-08 / D-12 / D-21 / D-23 / D-24
  </read_first>
  <behavior>
    - test_pipeline_no_module_level_heavy_import: ast.parse(pipeline/app.py) -> ImportFrom/Import 노드 검사 -> rtmlib / mmpose / mmcv / cv2 / scipy / torch / RTMWPoseEngine / HoughPoleDetector / PoleAxisAligner module-level import 0 (H-2 박제). 단, sunity_shared.analysis.pose_engines (lazy 모듈) import 는 허용.
    - test_pipeline_no_nlf_reference: grep -RIn "NlfPoseEstimator\|pose_estimator import\|nlf_l_multi" backend/functions/pipeline/app.py 결과 0 (atomic swap)
    - test_pipeline_no_mediapipe_reference: grep -RIn "MediaPipePoseEngine\|MediaPipeWithLifterEngine\|mediapipe_to_coco17" backend/functions/pipeline/app.py 결과 0
    - test_pipeline_uses_rtmw_engine: grep -RIn "RTMWPoseEngine\|sunity_shared.analysis.pose_engines" backend/functions/pipeline/app.py 결과 1 이상
    - test_pipeline_uses_pole_detector: grep -RIn "HoughPoleDetector\|sunity_shared.analysis.pole" backend/functions/pipeline/app.py 결과 1 이상
    - test_pipeline_dispatch_lambda_fallback_fail_fast: rtmlib 미설치 mock 환경에서 _ensure_adapters() 호출 시 ConfigurationError 또는 RuntimeError raise (silent 분석 진행 차단)
    - test_pipeline_dispatch_runpod_delegate_path: RUNPOD_ANALYZE_URL env set + mock RunPod HTTP 응답 -> pipeline 이 RunPod 호출 path 사용 + 응답 정상 처리
    - test_pipeline_complete_analysis_preserves_pole_axis: mock RunPod 결과에 pole_axis 메타 포함 -> firestore_admin.complete_analysis 호출 시 pole_axis 필드 보존 (H-3 박제 — Firestore doc 에 axis_vector + confidence_level + source)
    - test_pose_engine_env_default_rtmw: get_pose_engine() 호출 (POSE_ENGINE env unset) -> 'RTMW' 반환 (D-21)
  </behavior>
  <action>
    pipeline/app.py 변경 항목:
    A. module-level import 에서 NLF 관련 (from sunity_shared.analysis.pose_estimator import NlfPoseEstimator 등) 전부 제거. rtmlib / mmpose / mmcv 류 import 신규 추가 금지 (lazy).
    B. _RTMW_POSE_ENGINE / _POLE_DETECTOR / _POLE_ALIGNER 모듈-level 싱글톤 None 초기화.
    C. _ensure_adapters() 함수: lazy import. POSE_ENGINE env 확인 (get_pose_engine()) -> 'RTMW' 인 경우 from sunity_shared.analysis.pose_engines import RTMWPoseEngine + plan 22 선택된 3D path engine (RTMW3DPoseEngine 또는 RTMWLifterPoseEngine) -> __init__ -> 싱글톤. 'NLF_SMPLX' 인 경우 -> ConfigurationError raise ("R&D only — 운영 사용 금지").
    D. _angles_from_video(video_path, pole_axis) 함수: frames 추출 (기존 FrameExtractor 재사용) -> HoughPoleDetector.detect(frames) -> pole_axis 산출 -> 선택된 3D path engine.estimate(frames, pole_axis) -> list[PoseFrame] -> to_coco17_array(pose_frames) -> (T, 17, 4) numpy -> 기존 compute_joint_angles + dimensions 흐름 그대로.
    E. lambda_handler 의 RunPod delegate path 정리: RunPod 응답 dict 에 pole_axis 메타 포함 시 firestore_admin.complete_analysis 호출에 그대로 전달.
    F. Lambda fallback path (RUNPOD_ANALYZE_URL 미설정) 는 ConfigurationError 즉시 raise — silent 분석 진행 차단 (Pitfall 1 + H-2 정신).

    test_pipeline_dispatch.py: 위 9개 테스트. unittest.mock 으로 RTMWPoseEngine + HoughPoleDetector + firestore_admin DI.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_pipeline_dispatch.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - 9개 테스트 PASS
    - grep -c "NlfPoseEstimator\|MediaPipePoseEngine\|mediapipe_to_coco17\|nlf_l_multi" backend/functions/pipeline/app.py 결과 0
    - grep -c "RTMWPoseEngine\|sunity_shared.analysis.pose_engines.rtmw" backend/functions/pipeline/app.py 결과 1 이상
    - grep -c "HoughPoleDetector\|PoleAxisAligner" backend/functions/pipeline/app.py 결과 1 이상
  </acceptance_criteria>
  <done>
    pipeline/app.py atomic swap 완료. NLF / MediaPipe 참조 0. RTMW + 3D path 운영 진입. heavy import lazy. poleAxis 메타 보존. Lambda fail-fast 작동.
  </done>
</task>

<task type="auto">
  <name>Task 2: RunPod requirements.txt + server.py + setup.sh + README atomic swap (rtmlib + 가중치 sha256 + NLF/MediaPipe 잔여 0)</name>
  <files>backend/runpod_inference/requirements.txt, backend/runpod_inference/server.py, backend/runpod_inference/setup.sh, backend/runpod_inference/README.md</files>
  <read_first>
    - backend/runpod_inference/server.py (기존 NLF 호출 path + _pipeline_module 재import — RTMW 로 변경)
    - backend/runpod_inference/requirements.txt (기존 의존 — ultralytics / torch 제거 검토)
    - backend/runpod_inference/setup.sh (기존 NLF 가중치 다운로드 — RTMW 로 변경)
    - backend/runpod_inference/README.md (기존 NLF 운영 가이드 — RTMW 로 갱신)
    - backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/weights_manifest.json (plan 20 산출 — production_eligible 가중치 URL + sha256)
    - .planning/phases/01-poseengine-mediapipe-nlf-r-d/01-CONTEXT.md D-21 / D-25
  </read_first>
  <action>
    backend/runpod_inference/requirements.txt:
    - 제거: ultralytics, mediapipe, NLF 의존 (torch/torchvision 은 PyTorch base image 제공 — 명시적 pin 금지)
    - 추가: rtmlib >=0.0.13,<1.0, mmpose >=1.3,<2.0, mmcv >=2.0,<2.2, mmengine >=0.10,<1.0, opencv-python-headless >=4.10,<5.0, scipy >=1.13,<2.0, fastapi >=0.115,<1.0, uvicorn[standard] >=0.30,<1.0, boto3 >=1.34,<2.0, firebase-admin >=6,<7, pydantic >=2.5,<3.0
    - plan 22 옵션 B 선택 시 MotionBERT lifter 의존 추가 (chumpy 등 — plan 07 SUMMARY 참조)

    backend/runpod_inference/server.py:
    - module-level import 에서 NLF / pose_estimator 제거. rtmlib / mmpose / mmcv import 신규 추가 금지 (lazy module load 패턴 유지).
    - _load_pipeline_module() 또는 _process 재import path 가 plan 25 Task 1 의 새 pipeline/app.py 를 그대로 가져온다 (단일 코드 path 정신 — RunPod = pipeline _process 재사용).
    - _ensure_adapters() 시점에 RTMWPoseEngine + 선택된 3D path engine instantiate.
    - /analyze endpoint 응답에 pole_axis 메타 포함 (H-3 박제).

    backend/runpod_inference/setup.sh:
    - 제거: NLF torchscript 다운로드, YOLO 가중치 다운로드
    - 추가: rtmlib + mmpose + mmcv install (--no-build-isolation 권장 — plan 10 의 Pod 함정 박제 정합), RTMW 가중치 다운로드 (plan 20 weights_manifest production_eligible entry 의 URL + sha256 검증), 환경 변수 export (RUNPOD_AUTH_TOKEN, AWS_*, FIREBASE_SA_PATH, POSE_ENGINE=RTMW)

    backend/runpod_inference/README.md:
    - 제거: NLF_MODEL_PATH, YOLO_WEIGHTS_PATH, NLF 운영 메모 전부 (M-4 박제)
    - 추가: RTMW 가중치 path 환경 변수, plan 20 weights_manifest 사용 가이드, plan 22 선택된 3D path 의 setup 절차, sha256 검증 단계, POSE_ENGINE=RTMW 운영 기본값 명시, license-blocklist-pose cross-reference.

    grep 검증 — README + setup.sh + server.py 모두 "NLF" / "MediaPipe" / "nlf_l_multi" / "ultralytics" 키워드 부재 (M-4 박제 + D-23 정합).
  </action>
  <verify>
    <automated>bash -c 'cd backend/runpod_inference && grep -v "^#" server.py setup.sh README.md requirements.txt | grep -cE "NLF|nlf_l_multi|ultralytics|MediaPipe"' && bash -c 'cd backend/runpod_inference && grep -cE "rtmlib|mmpose|RTMW" server.py setup.sh README.md requirements.txt'</automated>
  </verify>
  <acceptance_criteria>
    - server.py / setup.sh / README.md / requirements.txt 4개 파일 합산 "NLF" / "nlf_l_multi" / "ultralytics" / "MediaPipe" 키워드 grep -v 주석 후 0건
    - 4개 파일 중 "rtmlib" 키워드 등장 ≥ 2개 파일 (setup.sh + requirements.txt 최소)
    - 4개 파일 중 "mmpose" 키워드 등장 ≥ 1개
    - 4개 파일 중 "RTMW" 키워드 등장 ≥ 2개 파일 (README + setup.sh 최소)
    - setup.sh 에 sha256 검증 명령 (sha256sum 또는 shasum) 등장
    - setup.sh 에 "POSE_ENGINE=RTMW" 환경 변수 export 라인 등장
  </acceptance_criteria>
  <done>
    RunPod RTMW 셋업 박제 완료. NLF / MediaPipe 잔여 0. plan 23 belle 회귀 검증 결과를 운영 배포로 atomic 전환 완료.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: belle Pod RTMW 배포 + 정은지 1영상 end-to-end 검증 (실제 운영 슬라이스 통과 확인)</name>
  <what-built>
    Task 1 + 2 산출 — pipeline/app.py + RunPod 셋업이 RTMW + 3D path 로 atomic swap 완료. belle 가 Pod 에 실 배포 후 정은지 영상 1개를 Mode 1 로 분석해 end-to-end 슬라이스 통과 확인 (사용자 시점 결과 출력 확인).
  </what-built>
  <how-to-verify>
    1. belle Pod git pull (plan 25 commit 까지)
    2. cd /workspace/SunityMotion/backend/runpod_inference && bash setup.sh (rtmlib install + RTMW 가중치 다운로드 + sha256 검증)
    3. uvicorn runpod_inference.server:app --host 0.0.0.0 --port 8000 --workers 1 (background or new shell)
    4. SAM deploy 또는 sam local invoke pipeline 으로 Lambda 의 RTMW 분기 동작 확인 (RUNPOD_ANALYZE_URL set + POSE_ENGINE=RTMW)
    5. 앱 또는 직접 S3 PUT 으로 정은지 영상 1개 업로드 (예: ref-invert.mp4 — plan 23 sweep 의 첫 영상)
    6. Firestore analyses doc 확인 — status='complete', overall ≥ 70 (정은지 위양성 없음 — SCORE-04 정합), pole_axis 메타 (axis_vector + confidence_level + source) 보존 확인
    7. RunPod log 확인 — RTMW inference ms/frame + 사용된 가중치 path + 라이선스 게이트 (production_eligible=true) 통과 확인
    8. belle 응답 = "approved: phase 01 complete, RTMW 운영 시작" 또는 "blocked: <reason>"
  </how-to-verify>
  <resume-signal>
    belle 가 "approved: phase 01 complete" 응답 + Firestore doc + RunPod log 박제. Phase 01 종료 + Phase 02 (BodyNormalizationProfile MediaPipe segment 기반 — RTMW segment 로 재정의 필요) 진입 게이트 통과.
  </resume-signal>
  <acceptance_criteria>
    - belle 응답 수신 (approved 또는 blocked)
    - approved 인 경우: 정은지 영상 1개 분석 결과 Firestore doc 존재 + overall ≥ 70 + pole_axis 메타 보존
    - approved 인 경우: RunPod log 에 라이선스 게이트 통과 확인 (production_eligible=true 가중치 로드)
    - blocked 인 경우: root cause + 후속 plan 박제 (Phase 01 미완료)
  </acceptance_criteria>
  <done>
    RTMW pivot end-to-end 운영 진입 검증. Phase 01 완료 게이트 통과 (또는 차단 시 후속 plan 명시).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline/app.py ↔ RTMW 코드 | atomic swap 중 NLF/MediaPipe 코드 잔여 시 silent fallback 위험 |
| RunPod setup.sh ↔ 가중치 무결성 | sha256 미검증 시 supply chain 공격 위험 |
| POSE_ENGINE env ↔ 운영 | NLF_SMPLX env 가 운영에 잘못 설정 시 R&D 엔진 운영 사용 (라이선스 위반) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Tampering | atomic swap 중간 상태 (config 플래그 분기) — D-08 위반 | mitigate | Task 1 의 grep 게이트 (NLF + MediaPipe 0) + lazy import 강제 |
| T-25-02 | Tampering | rtmlib 가중치 무결성 (sha256 mismatch) | mitigate | Task 2 setup.sh 의 sha256sum 검증 |
| T-25-03 | Repudiation | belle 운영 진입 결정 박제 누락 | mitigate | Task 3 checkpoint blocking-human + Firestore doc 박제 강제 |
| T-25-04 | Information Disclosure | POSE_ENGINE=NLF_SMPLX 운영 사용 (PS:License 위반) | mitigate | Task 1 의 _ensure_adapters() 가 NLF_SMPLX 분기에서 ConfigurationError raise |
| T-25-SC | Tampering | rtmlib / mmpose 가중치 supply chain (npm/pypi 의존성 변조) | mitigate | requirements.txt pin (>=,<) + sha256 검증 (가중치는 plan 20 manifest 기반) |
</threat_model>

<verification>
- pipeline/app.py atomic swap 완료 (NLF + MediaPipe 참조 0)
- RunPod RTMW 셋업 박제 완료
- belle Pod end-to-end 검증 통과
- Phase 01 완료 게이트 명확
</verification>

<success_criteria>
- pipeline/app.py + RunPod 셋업이 RTMW + 3D path 로 atomic swap 완료
- heavy import lazy 정합 (Lambda fail-fast 안전)
- poleAxis 메타 보존 (D-12 + H-3)
- POSE_ENGINE=RTMW 운영 기본값 강제 (D-21)
- belle end-to-end 검증 통과
- Phase 01 완료 (또는 차단 시 후속 plan 명시)
</success_criteria>

<output>
Create `.planning/phases/01-poseengine-mediapipe-nlf-r-d/01-25-SUMMARY.md` when done. SUMMARY 에 belle 검증 결과 + Phase 01 완료 여부 + Phase 02 진입 게이트 명시.
</output>
