---
phase: 02-bodynormalizationprofile-rtmw-segment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/contract.md
  - app/src/types/analysis.ts
  - backend/shared/python/sunity_shared/analysis/body_normalization.py
  - backend/shared/python/sunity_shared/analysis/body_normalization_measurer.py
  - backend/tests/fixtures/body_normalization/__init__.py
  - backend/tests/fixtures/body_normalization/_factory.py
  - backend/tests/fixtures/body_normalization/_generate.py
  - backend/tests/fixtures/body_normalization/normal_pose_pose_frames.json
  - backend/tests/fixtures/body_normalization/inverted_pose_pose_frames.json
  - backend/tests/fixtures/body_normalization/occluded_leg_pose_frames.json
  - backend/tests/fixtures/body_normalization/short_clip_pose_frames.json
  - backend/tests/fixtures/body_normalization/asymmetric_pose_frames.json
  - backend/tests/fixtures/body_normalization/test_fixtures_loadable.py
  - backend/tests/test_body_normalization_measurer.py
  - backend/tests/test_pipeline_body_profile_injection.py
  - backend/tests/test_research_import_isolation.py
  - backend/tests/test_body_normalization_lockstep.py
  - backend/functions/pipeline/app.py
  - backend/shared/python/sunity_shared/firestore_admin.py
  - backend/research/evaluations/compare_body_profile.py
  - .planning/REQUIREMENTS.md
autonomous: false
requirements:
  - BODY-01
user_setup:
  - service: runpod-gpu-pod
    why: "ROADMAP success criterion 4 (R&D NLF→SMPL-X β BodyNormalizationProfile 갭 보고서) 충족은 belle 사내 RunPod GPU pod 에서 NLF/SMPL-X 실데이터 실행이 필요하다. Lambda/scaffold 단독으로 closure 불가."
    env_vars:
      - name: RUNPOD_AUTH_TOKEN
        source: "belle pod env (이미 박제)"
      - name: SMPLX_MODEL_PATH
        source: "belle 사내 NLF→SMPL-X R&D weights (PS:License 1.0 비상업)"
    dashboard_config:
      - task: "Pod 에서 5영상 (ref-foxtop / ref-foxtop-split / ref-invert / ref-sideway-spin / ref-climb) 으로 compare_body_profile.py 실행 → reports/body_profile_gap_<date>.json 산출 → git commit"
        location: "belle RunPod pod 1ablelgbtrzcgb (현 활성)"

must_haves:
  truths:
    - "RTMW PoseFrame[]을 측정기에 넘기면 시간 평균·MAD 스무딩된 segment 길이가 산출된다"
    - "BodyNormalizationProfile이 estimatedHeightScale·armScale·legScale·torsoScale·shoulderHipRatio·confidence·warnings 7필드 모두 채워서 반환된다"
    - "낮은 confidence/occlusion/insufficient frames/asymmetric/inverted 5종 warning이 정확한 조건에서 발화된다"
    - "BodyNormalizationProfile이 _angles_from_video 호출 경계 너머로 propagate되어 Phase 6 consumer가 접근 가능하다 (per HIGH-1 — Option A: estimate() 시그너처 tuple 반환 + Firestore 저장)"
    - "5종 fixture JSON이 PoseFrame dataclass 시그너처 (timestamp_ms / Keypoint3D.uncertainty_proxy / PoleAxis 4-field axis_vector+confidence_level+source+frame_index) 와 정확히 일치하며 test factory helper로 load 가능하다 (per HIGH-2)"
    - "AST import isolation은 backend.research / research / nlf / smplx / smpl_x 만 차단하며 mediapipe는 본 phase scope 외로 명시 제외된다 (per HIGH-3 — TODO 마커 박제)"
    - "ROADMAP success criterion 4 충족은 belle Pod 실행 산출 (`reports/body_profile_gap_<date>.json`) 체크인까지 보류되며 scaffold만으로 충족 표시하지 않는다 (per HIGH-4 — user_setup 게이트)"
    - "pose_too_inverted 판정은 raw y 비교가 아닌 PoleAxis-relative 토르소 벡터 부호로 계산한다 (per M2)"
    - "estimatedHeightScale은 절대 키가 아닌 torso-relative proportion heuristic 임이 contract.md / analysis.ts / body_normalization.py 3 파일에 동시 명시된다 (per M3)"
    - "lockstep 테스트가 5종 warning enum 문자열을 3 파일에서 AST/regex parse 한 결과로 자동 검증한다 (per L1)"
    - "_RTMWNlfCompat가 모든 PoseFrame에 동일한 video-level body_shape를 박제하고 호출 caller에게 profile 객체 자체도 함께 반환한다"
    - "R&D 비교 harness가 backend/research/에 격리되며 sunity_shared가 backend.research / nlf / smplx 를 import하지 않는다 (AST 단위테스트 강제)"
    - "contract.md §7 warnings 행이 5종 enum으로 갱신되고 TS/Python 주석이 3-way lockstep으로 동시 변경된다"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/body_normalization_measurer.py"
      provides: "measure_body_profile(pose_frames) -> BodyNormalizationProfile 순수 함수"
      contains: "measure_body_profile"
    - path: "backend/tests/fixtures/body_normalization/_factory.py"
      provides: "pose_frame_from_dict(raw: dict) -> PoseFrame test helper — PoseFrame.from_dict 부재 우회 (per HIGH-2)"
      contains: "pose_frame_from_dict"
    - path: "backend/tests/fixtures/body_normalization/_generate.py"
      provides: "seed=42 멱등 fixture 생성기 + 5종 JSON (실제 dataclass 시그너처 정합)"
      contains: "RandomState(42)"
    - path: "backend/tests/fixtures/body_normalization/test_fixtures_loadable.py"
      provides: "5 fixture × pose_frame_from_dict() smoke load (HIGH-2 게이트)"
    - path: "backend/tests/test_body_normalization_measurer.py"
      provides: "5 fixture × profile assertion 단위 테스트 + PoleAxis-relative inversion 검증 (M2)"
    - path: "backend/tests/test_pipeline_body_profile_injection.py"
      provides: "_RTMWNlfCompat tuple-return contract + caller-side propagation 회귀 테스트 (HIGH-1)"
    - path: "backend/tests/test_research_import_isolation.py"
      provides: "AST 스캔으로 sunity_shared → backend.research/nlf/smplx 차단 (mediapipe 제외 — HIGH-3 TODO 박제)"
    - path: "backend/research/evaluations/compare_body_profile.py"
      provides: "R&D NLF→SMPL-X β vs RTMW segment gap 보고서 CLI (scaffold + smoke 가능, ROADMAP §4 실 보고서는 belle Pod 실행 필요 — HIGH-4)"
  key_links:
    - from: "backend/functions/pipeline/app.py::_RTMWNlfCompat.estimate"
      to: "sunity_shared.analysis.body_normalization_measurer.measure_body_profile"
      via: "video-level 1회 호출 + tuple (np.ndarray, BodyNormalizationProfile) 반환 (per HIGH-1 Option A)"
      pattern: "measure_body_profile\\(pose_frames\\)"
    - from: "backend/functions/pipeline/app.py::_angles_from_video / _angles_and_video_path_from_video"
      to: "Firestore via firestore_admin.complete_analysis(bodyNormalizationProfile=profile)"
      via: "tuple unpack + complete_analysis kw 추가 — Phase 6 consumer 경계 (HIGH-1)"
      pattern: "complete_analysis\\(.*bodyNormalizationProfile|complete_analysis\\(.*body_normalization_profile"
    - from: "docs/contract.md §7 warnings"
      to: "app/src/types/analysis.ts BodyNormalizationProfile.warnings + body_normalization.py docstring"
      via: "3-way lockstep (single atomic commit) + lockstep test AST 검증 (L1)"
      pattern: "low_keypoint_confidence|occluded_endpoint|insufficient_frames|asymmetric_landmark_count|pose_too_inverted"
    - from: "backend/tests/fixtures/body_normalization/*.json"
      to: "backend/tests/fixtures/body_normalization/_factory.py::pose_frame_from_dict"
      via: "test factory helper (PoseFrame.from_dict 부재 우회 — HIGH-2)"
      pattern: "pose_frame_from_dict"
---

## v2 Replan Changelog

본 plan은 `02-REVIEWS.md` (Codex 리뷰) 의 4 HIGH + 3 MEDIUM + 1 LOW concern 을 모두 fix 한 v2 이다. 각 concern → 영향받은 v1 task → v2 fix → orchestrator 가 검증한 file/line 증거 매핑.

| Concern | Severity | v1 task | v2 fix | Evidence (orchestrator verified) |
|---|---|---|---|---|
| HIGH-1: `body_shape` injection discarded — `to_coco17_array` 가 `np.ndarray` 만 반환해 `body_shape` 가 downstream 으로 propagate 안 됨. ROADMAP success #2 ("두 엔진 공유 입력") 미충족 | HIGH | v1 Task 4 (pipeline integ) | **Option A 채택**: `_RTMWNlfCompat.estimate()` 시그너처를 `tuple[np.ndarray, BodyNormalizationProfile]` 로 변경. caller (`_angles_from_video` line 255 + `_angles_and_video_path_from_video` line 271) 가 tuple unpack. profile 을 `firestore_admin.complete_analysis(bodyNormalizationProfile=...)` 로 저장 → Phase 6 가 Firestore 에서 읽음. Phase-6-boundary 단위 테스트 신설 — Firestore payload 에 `bodyNormalizationProfile` 키 존재 검증 | `backend/functions/pipeline/app.py:222-226` `_RTMWNlfCompat.estimate` 가 `to_coco17_array(pose_frames)` 만 반환 (np.ndarray). `:255` `_angles_from_video` 가 `_POSE_ESTIMATOR.estimate(frames)` 호출 후 `compute_joint_angles(keypoints)` 만 사용. `:493` `firestore_admin.complete_analysis(...)` 가 `angles` 만 저장 — `bodyNormalizationProfile` 키 없음 |
| HIGH-2: Fixture plan 이 존재하지 않는 PoseFrame API 와 잘못된 필드 (`timestamp_seconds` / `Keypoint3D.uncertainty_proxy` 누락 / `PoleAxis.origin` 환상) 참조 | HIGH | v1 Task 2 (fixture) | (a) `PoseFrame.from_dict` 가 코드베이스에 **없음** 확인 — Codex 정정 후 박제. test factory helper `pose_frame_from_dict()` 신설 (`backend/tests/fixtures/body_normalization/_factory.py`). (b) fixture JSON 스키마를 실제 dataclass 시그너처로 재작성: `timestamp_ms: int` (not `timestamp_seconds`), `Keypoint3D` 가 `uncertainty_proxy: float = 1 - confidence` 포함, `PoleAxis` 가 정확히 4 필드 (`axis_vector`/`confidence_level`/`source`/`frame_index`), **`origin` 필드 박멸**. (c) smoke load 테스트 신설 — measurer 테스트보다 먼저 실행 | `backend/shared/python/sunity_shared/analysis/pose_frame.py:228-246` `PoseFrame` 에 `timestamp_ms: int` (camelCase=`timestampMs`), `PoseFrame.from_dict` 메서드 없음 (grep 0 hits). `:103-116` `Keypoint3D` 가 `uncertainty_proxy: float` 필수. `:167-200` `PoleAxis` 가 `axis_vector` / `confidence_level` / `source` / `frame_index=None` 4 필드 — `origin` 필드 없음 |
| HIGH-3: Import-isolation rule 이 legacy mediapipe code 와 충돌 — Task 6 가 자동 fail | HIGH | v1 Task 6 (AST 차단) | AST scan 차단 대상을 narrow: `backend.research` / `research` / `nlf` / `smplx` / `smpl_x` 만. **`mediapipe` 차단 제외** + `# TODO(post-mediapipe-removal): RTMW pivot 후 mediapipe quarantine 별도 phase 책임` 박제. MediaPipe 정리는 본 phase scope 밖 ([[rtmw-free-stack-pivot]] 정합) | `backend/shared/python/sunity_shared/analysis/pose_engines/__init__.py` 가 `MediaPipePoseEngine` lazy 박제. `pose_engines/mediapipe_engine.py:74-76, 197` 가 `import mediapipe` 사용 (live code) |
| HIGH-4: R&D `NotImplementedError` stub 으로 ROADMAP success criterion 4 충족 표시 — scaffold ≠ gap report | HIGH | v1 Task 5 (R&D harness) | **Task 5 분할**: Task 5a (scaffold + smoke + math, in-scope, executor 가능) + Task 5b (belle Pod 실행 + `reports/body_profile_gap_<date>.json` 체크인, `user_setup` 게이트, executor 불가). must_haves 가 Task 5b 가 완료되어야만 ROADMAP #4 closure 표시. autonomous=false 갱신 | ROADMAP.md:116 success criterion #4 verbatim "R&D 비교군: 동일 영상에서 NLF→SMPL-X β로 추출한 BodyNormalizationProfile 과의 갭을 보고서로 출력 (제품 코드 비호출, 평가 전용)". v1 Task 5 은 `NotImplementedError` stub. |
| M1: Synthetic-only tests 가 RTMW 좌표/naming drift 탐지 불가 | MED | v1 Task 2 | Phase 1 sweep keypoint dump 가 실제로 디스크에 **없음** (확인: `backend/research/evaluations/reports/sweep_rtmw_20260603_1409/` 에 `report.json` + `report.md` 만 존재, `.npz`/`.json` keypoint dump 없음). 옵션 (c) 채택: 5 synthetic fixture 유지 (warning 커버리지) + **documented TODO** + Task 2b 신설 (RTMWPoseEngine 호출 가능 환경에서만 동작하는 sanity-check 테스트 — pytest skip if RTMW model unavailable) | `ls backend/research/evaluations/reports/sweep_rtmw_20260603_1409/` → only `report.json` + `report.md`, no `.npz` |
| M2: `pose_too_inverted` 가 raw `y` 비교 → coordinate convention dependent | MED | v1 Task 3 (measurer warnings) | warning 로직을 PoleAxis-relative torso projection 로 변경: `torso_vec = mid_shoulder - mid_hip` 을 `pose_frame.pole_axis.axis_vector` 에 projection → 부호 < 0 시 "inverted" 판정. unit test 2개 신설: (a) 정상 자세 + raw y axis 반전 시 동일 결과, (b) 인버트 fixture 가 PoleAxis-relative 판정 검증 | M2 |
| M3: armScale/legScale/estimatedHeightScale semantics 미명세 → Phase 6 오해 가능 | MED | v1 Task 1 (contract) | Task 1 의 3-way lockstep 에 명시 문구 추가: "torso-relative proportion heuristic — 절대 키가 아니라 torso 길이 대비 사지 비율. Phase-6 coarse normalizer hint 로만 사용". `estimatedHeightScale = (arm_scale + leg_scale + 1.0) / 3` 공식 + "average torso-relative proportion" 코멘트 박제 (3 파일 동일 문구) | M3 |
| L1: Lockstep warning enum 만 grep 검증 → 자동화 부족 | LOW | v1 Task 1 verify | `test_body_normalization_lockstep.py` 를 확장 — 5 warning enum 문자열을 AST/regex parse 로 3 파일에서 자동 검증 (`test_warnings_enum_lockstep_across_3_files`). grep-only 보조 검증 폐기 | Phase 1 Plan 01-19 산출 `backend/tests/test_body_normalization_lockstep.py` 에 5 함수, warnings enum 검증은 없음 |

이 v2 plan 은 v1 의 6 task 구조를 유지하되 (Task 5 만 5a/5b 로 split) Task 별 내부 fix 를 박제 한다. 총 atomic commit 수는 7 (Task 5b 는 belle Pod 체크인 = +1 commit).

<objective>
RTMW 133 wholebody 키포인트로부터 신체 segment 길이·비율·confidence·warnings 를 자동 산출하는 `measure_body_profile()` 측정기를 박제하고, `_RTMWNlfCompat` propagation contract 를 통해 모든 PoseFrame 에 video-level `BodyNormalizationProfile` 이 채워질 뿐 아니라 **Phase 6 consumer 경계** (Firestore) 까지 propagate 되도록 한다. R&D 비교군 (NLF→SMPL-X β) harness 는 `backend/research/` 에 격리하고 AST import 차단 테스트로 운영 코드 leak 을 영구 방지한다. ROADMAP success criterion 4 (실 영상 갭 보고서) 는 belle Pod 실행 게이트 (Task 5b, user_setup) 로 closure.

Per D-02-01, D-02-02, D-02-03, D-02-04, D-02-05, D-02-06.

Purpose: Phase 6 (체형 정규화 비교) / Phase 7 (차이 분류) / Phase 8·9 (힘 패턴) 가 공유하는 입력 schema 를 채운다. v1 = 측정기 본체 + 호출 경계 propagation 검증, **Firestore 저장은 본 phase 에 포함** (HIGH-1 fix 의 일부 — Phase 6 가 읽을 곳).

Output:
- `body_normalization_measurer.py` 신설 (순수 numpy, scipy/외부 의존 0) — M2 PoleAxis-relative inversion 박제
- 5 synthetic fixture + `_factory.py` test helper + smoke load + 5 단위 테스트 (HIGH-2 schema 정합)
- `_RTMWNlfCompat.estimate` 시그너처 변경 (tuple 반환) + 두 caller 모두 갱신 + Firestore 저장 — HIGH-1 propagation closure
- contract.md §7 warnings enum 5종 + estimatedHeightScale 의미 박제 (3-way lockstep 단일 commit) — M3 정합
- `backend/research/evaluations/compare_body_profile.py` R&D 격리 harness (scaffold + smoke — Task 5a)
- belle Pod 실행 게이트 (Task 5b — user_setup, ROADMAP §4 closure)
- AST import isolation test (mediapipe 제외, TODO 박제) — HIGH-3
- 확장 lockstep 테스트 (warning enum AST 자동 검증) — L1
- REQUIREMENTS.md BODY-01 "MediaPipe" → "RTMW" 정합 갱신
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-bodynormalizationprofile-rtmw-segment/02-CONTEXT.md
@.planning/phases/02-bodynormalizationprofile-rtmw-segment/02-RESEARCH.md
@.planning/phases/02-bodynormalizationprofile-rtmw-segment/02-REVIEWS.md
@docs/contract.md
@backend/shared/python/sunity_shared/analysis/body_normalization.py
@backend/shared/python/sunity_shared/analysis/pose_frame.py
@backend/shared/python/sunity_shared/analysis/temporal.py
@backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/wholebody_keypoints.py
@backend/shared/python/sunity_shared/analysis/adapters/rtmw_133_to_coco17.py
@backend/shared/python/sunity_shared/firestore_admin.py
@backend/functions/pipeline/app.py
@app/src/types/analysis.ts
@backend/tests/test_body_normalization_lockstep.py
@backend/tests/test_body_normalization_profile.py
@backend/research/evaluations/compare_engines.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: contract.md §7 warnings enum 5종 + estimatedHeightScale 의미 박제 + lockstep AST 자동 검증 (3-way lockstep, 단일 atomic commit)</name>
  <read_first>
    backend/shared/python/sunity_shared/analysis/body_normalization.py (현 docstring + warnings 필드 코멘트),
    app/src/types/analysis.ts (lines 364-388 — 현 BodyNormalizationProfile interface + 필드 주석),
    docs/contract.md (§7 — 현 BodyNormalizationProfile 명세, line 342 warnings),
    backend/tests/test_body_normalization_lockstep.py (확장 대상 — 현 5 함수)
  </read_first>
  <action>
    Goal: D-02-02 lockstep 정신 정합 — `warnings` 필드의 허용 enum 5종 + scale 필드 의미 (M3) 를 3 파일에 동시 박제. lockstep 테스트를 AST 자동 검증으로 확장 (L1).

    구현 노트:
    1. **5종 warning enum (per HIGH-2 + REVIEWS.md "Warnings enum")**: `low_keypoint_confidence`, `occluded_endpoint`, `insufficient_frames`, `asymmetric_landmark_count`, `pose_too_inverted`. 3 파일에 정확히 같은 문자열로 박제.

    2. **docs/contract.md §7 갱신 (line 342 근처)**:
       - 현 placeholder `"측정 품질 이슈 (예: 'short_arm_clip', 'occluded_torso'). 기본값 []."` 제거.
       - 5 enum 명시 + 각 의미 1줄. 예: `low_keypoint_confidence — profile.confidence < 0.4`, `occluded_endpoint — 60% 초과 frame 에서 endpoint conf<0.5`, `insufficient_frames — T<30`, `asymmetric_landmark_count — 좌우 valid frame 차이 >30%`, `pose_too_inverted — PoleAxis-relative torso projection<0 in 50%+ frames`.
       - **M3 박제 추가** — `estimatedHeightScale` / `armScale` / `legScale` 필드 설명에 다음 문구 (정확히 동일하게 3 파일 박제): "torso-relative proportion heuristic — 절대 키가 아니라 torso 길이 대비 사지 비율. Phase 6 coarse normalizer hint 로만 사용." 그리고 `estimatedHeightScale = (armScale + legScale + 1.0) / 3` 산식 명시 + "average torso-relative proportion" 코멘트.

    3. **app/src/types/analysis.ts BodyNormalizationProfile interface (lines 373-388)**:
       - `warnings: string[]` JSDoc 위에 5 enum 한국어/영문 매핑 nested list 추가. "Phase 11 coach_writer 가 한국어 카피로 번역" 노트.
       - `estimatedHeightScale` JSDoc 갱신 — "torso-relative proportion heuristic" 문구 (M3 박제).
       - `armScale` / `legScale` JSDoc 도 동일 문구 박제.
       - TS 타입은 `string[]` 유지 (literal union 은 Phase 6+ 결정).

    4. **backend/shared/python/sunity_shared/analysis/body_normalization.py**:
       - 클래스 docstring 의 `warnings 의미` 블록을 5 enum + 각 의미 명시로 교체.
       - `estimated_height_scale` / `arm_scale` / `leg_scale` 필드 위 코멘트 박제 — "torso-relative proportion heuristic" 문구.
       - `__post_init__` 의 enum membership 검증은 v1 에 추가 X (CONTEXT D-02-03 정합 — 다른 모듈이 새 warning 추가 가능성 열어둠).

    5. **L1 — lockstep AST 자동 검증 확장 (`backend/tests/test_body_normalization_lockstep.py`)**:
       - 기존 5 함수 유지.
       - **신설 함수 `test_warnings_enum_lockstep_across_3_files`**:
         - 5 enum 상수를 module-level frozenset 박제 (`_EXPECTED_WARNINGS`).
         - `docs/contract.md` 텍스트에서 각 enum 문자열이 모두 등장하는지 substring 검사.
         - `app/src/types/analysis.ts` 도 동일.
         - `body_normalization.py` 도 동일.
         - assert 메시지에 어느 파일에서 누락됐는지 명시.
       - **신설 함수 `test_estimated_height_scale_semantics_lockstep`**:
         - "torso-relative proportion heuristic" 문구가 3 파일에 모두 등장 (M3 박제).
         - 누락 파일 명시.
       - regex parse 권장 (substring 검색 — AST 는 비실용적, 박제된 문자열만 검증).
       - 기존 5 함수 회귀 0.

    6. **단일 commit 정합**: TS + Python + contract.md + test 동시 변경 (D-02-02).

    참조: REVIEWS.md HIGH-2 / M3 / L1 + RESEARCH.md "Confidence + Warnings Spec" + "BodyNormalizationProfile Schema (3-way)".

    Implementation deferred to execution — per D-02-02 / D-02-03. No code in this plan.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_body_normalization_lockstep.py -x -v</automated>
    추가: `grep -c "low_keypoint_confidence" docs/contract.md app/src/types/analysis.ts backend/shared/python/sunity_shared/analysis/body_normalization.py` 각 ≥1.
    추가 M3: `grep -c "torso-relative proportion heuristic" docs/contract.md app/src/types/analysis.ts backend/shared/python/sunity_shared/analysis/body_normalization.py` 각 ≥1.
  </verify>
  <acceptance_criteria>
    - 5 warning enum 문자열이 3 파일 모두에 존재 (lockstep AST 테스트 PASS)
    - "torso-relative proportion heuristic" 문구가 3 파일 모두에 존재 (M3 박제)
    - 기존 `short_arm_clip` / `occluded_torso` placeholder 제거됨
    - `test_warnings_enum_lockstep_across_3_files` + `test_estimated_height_scale_semantics_lockstep` 신설 PASS
    - 기존 5 lockstep 함수 회귀 0
    - 단일 commit 에 docs/contract.md + analysis.ts + body_normalization.py + test 동시 변경
  </acceptance_criteria>
  <done>
    위 acceptance_criteria 모두 충족.
  </done>
  <commit-template>
    feat(02): lock warnings enum 5-way + scale semantics for BodyNormalizationProfile (3-way lockstep + AST gate)
  </commit-template>
  <depends-on>None — Wave 1 진입점</depends-on>
</task>

<task type="auto">
  <name>Task 2: PoseFrame test factory helper + Synthetic fixture 생성기 + 5종 fixture JSON + smoke load 테스트 (HIGH-2 fix)</name>
  <read_first>
    backend/shared/python/sunity_shared/analysis/pose_frame.py (실제 dataclass 시그너처 — PoseFrame / Keypoint3D / PoleAxis 4-field / Landmark3D 모두 확인. PoseFrame.from_dict 부재 박제),
    backend/shared/python/sunity_shared/analysis/body_normalization.py (BodyNormalizationProfile dataclass),
    backend/shared/python/sunity_shared/analysis/skeleton.py (KEYPOINT_NAMES 17 — COCO-17 인덱스 0~16),
    backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/wholebody_keypoints.py (RTMW 133 인덱스)
  </read_first>
  <action>
    Goal (per HIGH-2 fix): `PoseFrame.from_dict` 가 **코드베이스에 없음** 확인됨 (orchestrator verified). 따라서 (a) test factory helper `pose_frame_from_dict(raw: dict) -> PoseFrame` 신설, (b) fixture JSON 스키마를 **실제 dataclass 시그너처** 로 재작성, (c) smoke load 테스트 신설.

    구현 노트:

    1. **`backend/tests/fixtures/body_normalization/_factory.py` 신설 — test helper**:
       - `pose_frame_from_dict(raw: dict) -> PoseFrame` — raw dict → `PoseFrame` 인스턴스화.
       - 처리해야 할 필드 (실제 dataclass 시그너처):
         - `frame_index: int`
         - `timestamp_ms: int` (NOT `timestamp_seconds` — HIGH-2 fix)
         - `keypoints_3d: dict[str, Keypoint3D]` — 각 entry 는 `{x, y, z, confidence, uncertainty_proxy}` 모두 포함 (HIGH-2 fix: `uncertainty_proxy = 1 - confidence` 명시).
         - `keypoints_3d_pole_aligned: dict[str, Keypoint3DAligned]` — `{x, y, z}` 만.
         - `raw_keypoints_133: dict[str, Keypoint3D] | None` — RTMW path 박제, body 17 채우고 117 슬롯은 conf=0.0 + uncertainty_proxy=1.0.
         - `pole_axis: PoleAxis` — **정확히 4 필드**: `axis_vector: tuple[float, float, float]` (norm=1.0 검증 필수), `confidence_level: 'high'|'medium'|'low'`, `source: 'detected'|'vertical_fallback'`, `frame_index: int | None`. **`origin` 필드 없음** (HIGH-2 fix).
         - `reliability: 'high'|'medium'|'low'`.
         - `body_shape: None` (Phase 2 측정기가 채울 예정 — fixture 는 None).
       - 누락 필드는 default factory 사용 (`raw_landmarks_33={}` 등).
       - JSON tuple 은 list 로 deserialize 되므로 `tuple(...)` 변환 박제.
       - 단위 테스트는 별도 (Task 2 smoke).
       - **Important**: `pose_frame.py` Production 코드는 수정 X — helper 는 `backend/tests/fixtures/body_normalization/` 안에 격리.

    2. **`backend/tests/fixtures/body_normalization/_generate.py` 신설 — 멱등 생성기**:
       - 헤더: `numpy.random.RandomState(seed=42)` 박제. CLI `--check` 옵션 (재실행 멱등성 검증).
       - 모든 fixture JSON 은 `pose_frame_from_dict` 통과 가능.
       - body 17 keypoint (COCO-17 인덱스 0~16, `KEYPOINT_NAMES` 그대로) 만 채움. `raw_keypoints_133` 도 같은 17 슬롯 박제 (RTMW path 시뮬).
       - PoleAxis default: `axis_vector=(0.0, 1.0, 0.0)`, `confidence_level='medium'`, `source='vertical_fallback'`, `frame_index=None`. **`origin` 필드 박제 금지** (HIGH-2 fix).
       - `Keypoint3D` 생성 시 `uncertainty_proxy = 1.0 - confidence` 박제 (HIGH-2 fix).
       - 모든 timestamp 는 `timestamp_ms: int` (NOT `timestamp_seconds`).

    3. **5 fixture JSON 스키마 (per HIGH-2 정합 dataclass 시그너처)**:
       - **normal_pose** — T=60 frame, 정상 자세 (PoleAxis-relative torso projection > 0). 모든 keypoint conf 0.85~0.95 ±2% gaussian. 인체 평균 비율 (torso=200px, upper_arm=120px, forearm=110px, thigh=180px, shank=160px, shoulder=140px, hip=100px → shoulder_hip_ratio≈1.4).
       - **inverted_pose** — T=60, PoleAxis-relative torso projection < 0 in 80% frames (M2 박제 — 인버트 자세). conf 0.6~0.8.
       - **occluded_leg** — T=60, `left_knee`/`right_knee` conf<0.3 in 70% frames. 나머지 정상.
       - **short_clip** — T=20 (MIN_VALID_FRAMES=5 통과하지만 T<30 임계값 미달). 정상 자세.
       - **asymmetric_pose** — T=60, 좌측 keypoint conf>0.9 in 90% frames, 우측 keypoint conf>0.9 in 50% frames 만 (frame 수 차이 40%).
       - JSON 파일 평균 size 200KB 이내.

    4. **`backend/tests/fixtures/body_normalization/test_fixtures_loadable.py` 신설 (smoke)**:
       - `test_all_fixtures_load_via_factory` — 5 fixture JSON 을 `pose_frame_from_dict` 로 load → `PoseFrame` 인스턴스 (raise X). 각 fixture 의 첫 frame `frame_index=0`, `timestamp_ms` 가 `int`.
       - `test_polestaxis_no_origin_field` — 5 fixture 의 PoleAxis JSON 에 `origin` 키 등장 0 (HIGH-2 박제).
       - `test_keypoint3d_has_uncertainty_proxy` — 5 fixture 의 keypoints_3d entry 가 `uncertainty_proxy` 키 보유 (HIGH-2 박제).
       - 본 smoke 가 PASS 해야만 Task 3 measurer 테스트가 의미 있음.

    5. **M1 — Task 2b (Real-RTMW sanity check, optional skip)**:
       - `backend/tests/fixtures/body_normalization/test_real_rtmw_sanity.py` 신설.
       - `@pytest.mark.skipif(not _rtmw_available(), reason="RTMW ONNX 미설치 — Pod 환경 외 skip")`.
       - 짧은 embedded video clip (또는 Pod 환경에서만 실행) 로 RTMWPoseEngine 호출 → 첫 frame 의 `keypoints_3d` 가 COCO-17 keypoint 이름 17개 정확히 포함 + `uncertainty_proxy` 범위 [0,1].
       - 통과 X 면 skip — CI 환경에서 안전. Pod 환경에선 실행 (M1 mitigation).

    6. **TODO 마커**: `_generate.py` 상단에 `# TODO(post-pilot): Phase 1 sweep keypoint dump 재추출 후 실 영상 fixture 1종 추가 — REVIEWS.md M1 정합`.

    참조: REVIEWS.md HIGH-2 + M1 + RESEARCH.md "Test Fixture Plan".

    No code in this plan.
  </action>
  <verify>
    <automated>cd backend && python -m tests.fixtures.body_normalization._generate --check && pytest tests/fixtures/body_normalization/test_fixtures_loadable.py -x -v</automated>
    추가: `ls backend/tests/fixtures/body_normalization/*.json | wc -l` → 5.
    멱등성: `_generate.py --check` 두번 실행해도 byte-identical JSON.
    HIGH-2 박제: `grep -l "timestamp_seconds" backend/tests/fixtures/body_normalization/ 2>/dev/null` → 0 (timestamp_seconds 없음 확인).
    `grep -l '"origin"' backend/tests/fixtures/body_normalization/*.json | wc -l` → 0 (origin 없음 확인).
    `grep -c "uncertainty_proxy" backend/tests/fixtures/body_normalization/*.json | grep -v ':0$' | wc -l` → 5 (5 fixture 모두 uncertainty_proxy 박제).
  </verify>
  <acceptance_criteria>
    - 5 fixture JSON 파일 존재
    - `pose_frame_from_dict` helper 가 5 fixture 모두 load 성공 (smoke PASS)
    - `_generate.py --check` 멱등성 (재실행 시 byte-identical output)
    - HIGH-2 fix 검증: `timestamp_ms` 사용, `Keypoint3D.uncertainty_proxy` 박제, PoleAxis 에 `origin` 없음 (4 필드만)
    - 각 fixture JSON size < 200KB
    - Task 2b RTMW sanity test 존재 (skip OK in CI, Pod 에서 실행 가능)
  </acceptance_criteria>
  <done>
    위 acceptance_criteria 모두 충족.
  </done>
  <commit-template>
    test(02): add PoseFrame factory helper + synthetic fixtures (5 cases) + RTMW sanity skip (HIGH-2 schema fix, M1 fallback)
  </commit-template>
  <depends-on>None — Task 1 과 병렬 가능. 그러나 commit 순서는 Task 1 → Task 2 권장 (lockstep 박제 먼저).</depends-on>
</task>

<task type="auto">
  <name>Task 3: body_normalization_measurer.py 본체 + 단위 테스트 (5 fixture + PoleAxis-relative inversion + M2/M3 박제)</name>
  <read_first>
    backend/shared/python/sunity_shared/analysis/pose_frame.py (PoseFrame / Keypoint3D / PoleAxis 실제 시그너처 — Task 2 의 helper 와 1:1 정합),
    backend/shared/python/sunity_shared/analysis/temporal.py (MAD outlier `_column_outliers` 패턴 — DEFAULT_OUTLIER_K=3.0),
    backend/shared/python/sunity_shared/analysis/body_normalization.py (BodyNormalizationProfile dataclass — 7 필드 + __post_init__ validator),
    backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/wholebody_keypoints.py (RTMW_KEYPOINT_INDICES — body 17 인덱스),
    backend/tests/fixtures/body_normalization/_factory.py (Task 2 산출 — helper 사용)
  </read_first>
  <action>
    Goal: `measure_body_profile(pose_frames: list[PoseFrame], *, conf_gate=0.5, mad_k=3.0, min_valid_frames=5, low_conf_gate=0.4) -> BodyNormalizationProfile` 순수 함수 박제. numpy only (scipy 의존 0). Phase 1 `temporal.py` MAD 패턴 재사용. **M2 박제** — `pose_too_inverted` 가 PoleAxis-relative 판정.

    구현 노트:

    1. **상수 박제 (module top)**:
       - `SEGMENT_PAIRS: dict[str, tuple[str, str]]` (RESEARCH.md `RTMW 133 Keypoint Segment Mapping` 표 그대로 — 좌우 8 segment + shoulder_width + hip_width = 10 entry).
       - `TORSO_TOP_KPS = ("left_shoulder", "right_shoulder")`, `TORSO_BOTTOM_KPS = ("left_hip", "right_hip")`.
       - `WARNING_CODES = frozenset({"low_keypoint_confidence", "occluded_endpoint", "insufficient_frames", "asymmetric_landmark_count", "pose_too_inverted"})` — Task 1 박제 5 enum 정합.
       - 임계값 module 상수: `CONF_GATE_PER_FRAME=0.5`, `MIN_VALID_FRAMES=5`, `MAX_BAD_FRAME_RATIO=0.6`, `MAD_K=3.0`, `LOW_CONFIDENCE_GATE=0.4`, `INVERTED_FRAME_RATIO=0.5`, `ASYMMETRIC_FRAME_RATIO=0.3`.

    2. **`_measure_segment_per_frame(pose_frames, kp_start, kp_end) -> (lengths: np.ndarray, confs: np.ndarray)`**:
       - per-frame `sqrt(dx² + dy²)` 만 사용 (z 무시 — RESEARCH Pitfall 2 + Risk 3 mitigation).
       - confidence = `min(start.confidence, end.confidence)`.
       - 미감지 frame → NaN + conf=0.

    3. **`_measure_torso(pose_frames) -> (lengths, confs)`**:
       - `mid_shoulder = (left_shoulder + right_shoulder) / 2` (좌표 평균).
       - `mid_hip = (left_hip + right_hip) / 2`.
       - 길이 = `sqrt((mid_shoulder.x - mid_hip.x)² + (mid_shoulder.y - mid_hip.y)²)`.
       - conf = min of 4 endpoint confidences.

    4. **`_robust_median(lengths, confs, conf_threshold, mad_k) -> (median: float, segment_conf: float)`**:
       - RESEARCH.md 코드 sketch 그대로. MAD k=3 rejection + median + `(kept_ratio × mean_conf)` 산출.
       - NaN 안전: valid frame < `MIN_VALID_FRAMES` 시 `(nan, 0.0)` 반환.

    5. **M2 박제 — `_is_inverted_frame(frame: PoseFrame) -> bool | None`**:
       - frame 의 mid_shoulder / mid_hip 좌표 (3D — x, y, z) 추출 (없으면 None).
       - `torso_vec = (mid_shoulder.x - mid_hip.x, mid_shoulder.y - mid_hip.y, mid_shoulder.z - mid_hip.z)`.
       - `pole_axis = frame.pole_axis` — None 이면 None 반환 (판정 불가).
       - `proj = dot(torso_vec, pole_axis.axis_vector)` — scalar projection.
       - **인버트 정의 (M2 박제)**: `proj < 0` (torso 가 폴축 방향과 반대로 향함 = inverted).
       - 본 함수가 `_check_pose_too_inverted_warning(pose_frames) -> bool` 에서 사용:
         - 모든 frame 의 `_is_inverted_frame` 결과 중 True 비율 > `INVERTED_FRAME_RATIO` (0.5) → warning 발화.
         - None 반환 frame 은 분모에서 제외.

    6. **`measure_body_profile(pose_frames, *, conf_gate=0.5, mad_k=3.0, min_valid_frames=5, low_conf_gate=0.4) -> BodyNormalizationProfile`**:
       - 모든 SEGMENT_PAIRS + torso 길이 측정.
       - 좌우 평균 → upper_arm_median, forearm_median, thigh_median, shank_median.
       - `torso_length = _measure_torso(pose_frames)` 의 robust median.
       - `arm_scale = (upper_arm_median + forearm_median) / torso_length`.
       - `leg_scale = (thigh_median + shank_median) / torso_length`.
       - `torso_scale = 1.0` (self-reference 정의 — M3 박제: "torso 대비 사지 비율").
       - `shoulder_hip_ratio = shoulder_width_median / hip_width_median`.
       - **M3 박제** — `estimated_height_scale = (arm_scale + leg_scale + 1.0) / 3` (평균 torso-relative proportion heuristic, placeholder).
       - `profile.confidence = mean of 11 segment confidences`.
       - **Warnings 판정**:
         - `low_keypoint_confidence`: profile.confidence < `LOW_CONFIDENCE_GATE`.
         - `occluded_endpoint`: 임의 segment 가 endpoint conf < `CONF_GATE_PER_FRAME` in > `MAX_BAD_FRAME_RATIO` (60%) frames.
         - `insufficient_frames`: T < 30.
         - `asymmetric_landmark_count`: 좌측 valid frame 수 vs 우측 valid frame 수 차이 > `ASYMMETRIC_FRAME_RATIO` (30%).
         - `pose_too_inverted`: `_check_pose_too_inverted_warning(pose_frames)` 결과 (M2 박제 — PoleAxis-relative).
       - **NaN 방어 (T-02-03 mitigation)**: torso_length == NaN 이거나 valid frames < `MIN_VALID_FRAMES` → 모든 scale=1.0 + confidence=0 + warnings 에 `insufficient_frames` 포함.
       - finite 강제 (`__post_init__` 통과): NaN/inf → 1.0 폴백.

    7. **Logging**: `from __future__ import annotations` 박제. stdlib `logging.getLogger(__name__)`. `log.info("body_profile arm_scale=%.3f leg_scale=%.3f conf=%.3f warnings=%s", ...)`. Secret 없음.

    8. **단위 테스트 (`backend/tests/test_body_normalization_measurer.py`)** — 5 fixture × per-case + M2 + 회귀:
       - `test_normal_pose_profile_valid`: 정상 fixture → profile.confidence > 0.7, warnings == [], arm_scale ∈ [0.8, 1.4], leg_scale ∈ [1.0, 1.8], shoulder_hip_ratio ∈ [1.1, 1.6].
       - `test_inverted_pose_warning`: inverted fixture → "pose_too_inverted" in warnings.
       - **`test_inverted_warning_is_pole_axis_relative_not_raw_y` (M2 박제)**:
         - 정상 fixture 의 모든 frame 의 keypoints_3d.y 좌표를 (1.0 - y) 로 flip 변환 (raw y 만 반전).
         - 같은 PoleAxis (vertical (0,1,0)) 유지.
         - `measure_body_profile` 결과 `pose_too_inverted` warning 등장 **여부가 정상 fixture 결과와 동일** (raw y 만 바뀌어도 PoleAxis-relative 판정은 불변).
       - **`test_inverted_warning_fires_when_torso_opposes_pole_axis` (M2 박제)**:
         - 정상 fixture 의 mid_shoulder 와 mid_hip 좌표를 swap → torso_vec 부호 반전 → projection<0.
         - `pose_too_inverted` warning 발화 확인.
       - `test_occluded_leg_warning`: occluded fixture → "occluded_endpoint" in warnings.
       - `test_short_clip_warning`: short fixture → "insufficient_frames" in warnings.
       - `test_asymmetric_warning`: asymmetric fixture → "asymmetric_landmark_count" in warnings.
       - `test_z_axis_ignored_when_zero`: 정상 fixture 의 모든 z=0 으로 override 후 측정해도 arm_scale 동일 ± 1% (Pitfall 2 검증).
       - `test_profile_validator_passes`: 5 fixture × `BodyNormalizationProfile.__post_init__` 통과 (NaN 출력 X).
       - `test_idempotent`: 동일 fixture 2번 측정 → byte-identical output (deterministic).
       - 모든 테스트는 Task 2 의 `pose_frame_from_dict` helper 로 fixture load.

    참조: RESEARCH.md "Smoothing 알고리즘" + "Length Normalization Unit" + REVIEWS.md M2 / M3 + Phase 1 `temporal.py:_column_outliers`.

    No code in this plan.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_body_normalization_measurer.py -x -v</automated>
    추가: `python -c "from sunity_shared.analysis.body_normalization_measurer import measure_body_profile; help(measure_body_profile)"` (import + docstring).
    회귀: `pytest tests/test_body_normalization_profile.py tests/test_body_normalization_lockstep.py -x` (Phase 1 회귀 0).
    의존성: `grep -r "import scipy\|from scipy" backend/shared/python/sunity_shared/analysis/body_normalization_measurer.py | wc -l` → 0.
    M2 박제: 위 `test_inverted_warning_is_pole_axis_relative_not_raw_y` PASS — PoleAxis-relative 판정 검증.
  </verify>
  <acceptance_criteria>
    - 10개 단위 테스트 모두 PASS (5 fixture × per-case + M2 2개 + z=0 + validator + idempotent)
    - `measure_body_profile` 가 5 fixture 에서 BodyNormalizationProfile 반환 (NaN/inf 출력 X)
    - 정상 fixture 에서 warnings == []
    - 4 비정상 fixture 가 각자 1개 이상 정확한 warning 발화
    - M2 박제: PoleAxis-relative inversion 판정 (raw y flip 에 둔감)
    - scipy import 0 (numpy only)
    - Phase 1 기존 테스트 전부 PASS (회귀 0)
  </acceptance_criteria>
  <done>
    위 acceptance_criteria 모두 충족.
  </done>
  <commit-template>
    feat(02): add measure_body_profile RTMW segment measurer with MAD smoothing + PoleAxis-relative inversion (BODY-01, M2 fix)
  </commit-template>
  <depends-on>Task 1 (warnings enum 박제), Task 2 (5 fixture + pose_frame_from_dict helper)</depends-on>
</task>

<task type="auto">
  <name>Task 4: Pipeline 통합 — `_RTMWNlfCompat.estimate()` tuple 반환 + 두 caller 갱신 + Firestore 저장 (HIGH-1 Option A fix)</name>
  <read_first>
    backend/functions/pipeline/app.py (lines 200-310 — `_RTMWNlfCompat` 정의, `_ensure_adapters`, `_angles_from_video` line 255, `_angles_and_video_path_from_video` line 271, `_process` 호출 경로),
    backend/functions/pipeline/app.py (lines 480-510 — `firestore_admin.complete_analysis` 호출 site),
    backend/shared/python/sunity_shared/firestore_admin.py (lines 45-72 — `complete_analysis` 시그너처),
    backend/shared/python/sunity_shared/analysis/pose_frame.py (PoseFrame 시그너처 — dataclasses.replace 정합),
    backend/shared/python/sunity_shared/analysis/body_normalization.py (BodyNormalizationProfile dataclass — Firestore serialize 시 사용)
  </read_first>
  <action>
    Goal (per HIGH-1 fix, Option A 채택): `_RTMWNlfCompat.estimate()` 시그너처를 `tuple[np.ndarray, BodyNormalizationProfile]` 로 변경하여 video-level profile 을 **caller 가 직접 받을 수 있게** 한다. 두 caller (`_angles_from_video` line 255 + `_angles_and_video_path_from_video` line 271 부근) 가 tuple unpack → `_process` 가 profile 을 `firestore_admin.complete_analysis(bodyNormalizationProfile=...)` 로 저장. 이로써 Phase 6 가 Firestore 에서 `bodyNormalizationProfile` 을 읽을 수 있다.

    구현 노트:

    1. **`_RTMWNlfCompat.estimate()` 변경 (`backend/functions/pipeline/app.py:222-226`)**:
       - 시그너처: `def estimate(self, frames) -> tuple[np.ndarray, "BodyNormalizationProfile"]:`.
       - 본체:
         - `pose_frames = self._engine.estimate(frames, self._default_pole)` (기존).
         - `from sunity_shared.analysis.body_normalization_measurer import measure_body_profile` (lazy import — numpy only 이므로 top-level 도 OK).
         - `profile = measure_body_profile(pose_frames)`.
         - `pose_frames_with_profile = [dataclasses.replace(pf, body_shape=profile) for pf in pose_frames]` (Pitfall 3 정합 — frozen dataclass).
         - `import dataclasses` 가 module top-level 에 이미 있는지 확인 (없으면 추가).
         - `from sunity_shared.analysis.pose_frame import to_coco17_array`.
         - `coco_array = to_coco17_array(pose_frames_with_profile)`.
         - `return coco_array, profile`.
       - 기존 호환성 깨뜨림 — 단일 caller 만 있는 사설 클래스이므로 안전.

    2. **`_angles_from_video` (`backend/functions/pipeline/app.py:255`) 갱신**:
       - 현 `keypoints = _POSE_ESTIMATOR.estimate(frames)` 를 `keypoints, body_profile = _POSE_ESTIMATOR.estimate(frames)` 로 변경.
       - 반환 시그너처도 변경:
         - `def _angles_from_video(bucket: str, key: str) -> tuple[np.ndarray, "BodyNormalizationProfile"]:`.
         - 본문 끝 `return temporal_fill(angles, joint_uncertainty(keypoints)), body_profile`.
       - 호출처 (`_process` 안) 도 tuple unpack 으로 갱신.

    3. **`_angles_and_video_path_from_video` (`backend/functions/pipeline/app.py:271`) 갱신**:
       - 시그너처: `def _angles_and_video_path_from_video(bucket: str, key: str) -> tuple[np.ndarray, str, "BodyNormalizationProfile"]:`.
       - 내부 `keypoints = _POSE_ESTIMATOR.estimate(frames)` 도 tuple unpack.
       - 반환에 `body_profile` 추가.
       - 호출처 (`_process` 안 Gemini 분기) 도 tuple unpack 으로 갱신.

    4. **`_process` 안 (`backend/functions/pipeline/app.py:300~500` 영역) 갱신**:
       - `_angles_from_video` / `_angles_and_video_path_from_video` 호출 결과를 tuple unpack 으로 받아 `profile` (= `body_profile`) 변수 박제.
       - `firestore_admin.complete_analysis(...)` 호출 (line ~493) 에 `body_normalization_profile=profile` kw 추가.

    5. **`firestore_admin.complete_analysis` 시그너처 확장 (`backend/shared/python/sunity_shared/firestore_admin.py:45`)**:
       - 시그너처: `def complete_analysis(uid: str, analysis_id: str, result: dict, *, angles=None, angles_joint_keys=None, angles_frames=None, body_normalization_profile: "BodyNormalizationProfile | None" = None) -> None:`.
       - 본체:
         - 기존 payload 빌드 유지.
         - `if body_normalization_profile is not None:` `payload["bodyNormalizationProfile"] = {"estimatedHeightScale": p.estimated_height_scale, "armScale": p.arm_scale, "legScale": p.leg_scale, "torsoScale": p.torso_scale, "shoulderHipRatio": p.shoulder_hip_ratio, "confidence": p.confidence, "warnings": list(p.warnings)}` (TS camelCase 정합).
       - **Firestore nested-array 제약 (RESEARCH Pitfall 4 + [[firestore-nested-array-flat]] 정합)**: `warnings: list[str]` 은 flat string list → nested array 아님 → 안전. 단위 테스트로 검증.

    6. **TS AnalysisDoc lockstep (`app/src/types/analysis.ts`)**:
       - `AnalysisDoc` interface 에 `bodyNormalizationProfile?: BodyNormalizationProfile;` optional 필드 추가.
       - 주석 박제: "Phase 2 BODY-01 propagation 박제 — Phase 6 가 읽음".
       - 본 task 의 atomic commit 에 포함 (3-way lockstep — Firestore schema 변경).

    7. **통합 테스트 (`backend/tests/test_pipeline_body_profile_injection.py`) 신설**:
       - `test_rtmwnlfcompat_returns_tuple_with_profile`:
         - fake `_engine` (3 PoseFrame return) → `_RTMWNlfCompat.estimate(frames)` 호출 → 반환값이 tuple (np.ndarray, BodyNormalizationProfile).
         - tuple[0].shape == (3, 17, 4).
         - tuple[1] 이 `BodyNormalizationProfile` instance + `__post_init__` 통과.
       - `test_estimate_injects_body_shape_into_all_frames`:
         - 위와 같은 setup 후 `_engine.estimate` 결과의 `pose_frames` 의 body_shape 가 모두 동일 profile (`id()` 또는 equality).
         - (테스트 환경에서 직접 검증 가능하도록 helper 또는 monkeypatch 박제.)
       - `test_short_frames_graceful_degrade`:
         - T=3 frame (MIN_VALID_FRAMES 미달) → measure_body_profile 이 confidence=0 + insufficient_frames warning 폴백, exception 발생 X, tuple 정상 반환.
       - `test_complete_analysis_persists_body_profile_for_phase6` (Phase-6 boundary 박제 — HIGH-1 fix 의 핵심):
         - monkeypatch `firestore_admin._doc(...).set(...)` 로 payload capture.
         - `firestore_admin.complete_analysis(uid="u1", analysis_id="a1", result={}, body_normalization_profile=profile)` 호출.
         - captured payload 에 `bodyNormalizationProfile` 키 존재 + 7 camelCase 필드 (estimatedHeightScale 등) 모두 박제.
         - `warnings` 가 `list[str]` (flat — Firestore nested-array 안전).
       - `test_pipeline_callers_propagate_profile`:
         - `_angles_from_video` 의 반환 시그너처가 tuple[np.ndarray, BodyNormalizationProfile] (signature inspect).
         - `_angles_and_video_path_from_video` 도 tuple[np.ndarray, str, BodyNormalizationProfile].
       - `test_to_coco17_array_unaffected`:
         - body_shape 주입 전후 `to_coco17_array` 결과 shape (T,17,4) 동일.

    참조: REVIEWS.md HIGH-1 Option A + RESEARCH.md "Pipeline Integration Plan" + firestore-nested-array-flat memory.

    No code in this plan.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_pipeline_body_profile_injection.py -x -v && pytest tests/ -x</automated>
    회귀: 기존 pipeline 테스트 전부 PASS.
    propagation grep: `grep -n "bodyNormalizationProfile\|body_normalization_profile" backend/functions/pipeline/app.py backend/shared/python/sunity_shared/firestore_admin.py app/src/types/analysis.ts | wc -l` ≥ 3 (각 파일 박제).
  </verify>
  <acceptance_criteria>
    - 6개 통합 테스트 모두 PASS
    - 기존 `tests/` suite 전체 회귀 0
    - `_RTMWNlfCompat.estimate` 가 tuple (np.ndarray, BodyNormalizationProfile) 반환
    - `_angles_from_video` 시그너처가 tuple 반환으로 확장
    - `_angles_and_video_path_from_video` 도 tuple 확장
    - `firestore_admin.complete_analysis` 가 `body_normalization_profile` kw 받음 + `bodyNormalizationProfile` 키로 Firestore 저장
    - TS `AnalysisDoc` 에 `bodyNormalizationProfile?` optional 필드 박제
    - Phase-6 boundary 테스트 PASS — payload capture 검증
    - lazy import 또는 top-level import 정합 (numpy only — top-level OK)
  </acceptance_criteria>
  <done>
    위 acceptance_criteria 모두 충족.
  </done>
  <commit-template>
    feat(02): propagate BodyNormalizationProfile from _RTMWNlfCompat to Firestore via tuple return + complete_analysis kw (HIGH-1 Option A — Phase 6 boundary closure)
  </commit-template>
  <depends-on>Task 3 (measure_body_profile 함수)</depends-on>
</task>

<task type="auto">
  <name>Task 5a: R&D 비교 harness compare_body_profile.py scaffold + smoke test (HIGH-4 분할 in-scope)</name>
  <read_first>
    backend/research/evaluations/compare_engines.py (R&D harness 패턴 — argparse + reporting 구조),
    backend/shared/python/sunity_shared/analysis/body_normalization.py (BodyNormalizationProfile dataclass — 갭 계산 대상),
    backend/shared/python/sunity_shared/analysis/body_normalization_measurer.py (Task 3 산출 — RTMW path 측정기 호출)
  </read_first>
  <action>
    Goal: NLF→SMPL-X β path 와 RTMW segment path 의 BodyNormalizationProfile 갭을 보고하는 CLI scaffold. **`backend/research/` 격리** — `sunity_shared` 무관. D-02-04 정합 (SMPL-X 상업 라이선스 X — R&D 만).

    **HIGH-4 분할 박제**: 본 task (5a) 는 scaffold + smoke test + math 만 — executor 가능. **belle Pod 실행 + 실 보고서 체크인은 Task 5b (user_setup gate)** 로 분리. must_haves 에 명시 — Task 5b 완료 전까지 ROADMAP success criterion 4 closure 표시 X.

    구현 노트:

    1. **파일 헤더 docstring (`backend/research/evaluations/compare_body_profile.py`)**:
       ```
       """RTMW segment vs NLF→SMPL-X β BodyNormalizationProfile 갭 보고.

       D-02-04 박제: NLF/SMPL-X 모듈 import 는 본 파일에서만. sunity_shared 무관.
       [CITED: license-blocklist-pose memory — SMPL-X 상업 불가 / R&D 만]
       실행 환경: belle 사내 RunPod GPU pod (Lambda/RunPod 운영 path X).

       HIGH-4 분할 박제 (REVIEWS.md):
         - Task 5a (본 PR scope): scaffold + smoke + gap 산식
         - Task 5b (belle Pod 실행, user_setup): 실 NLF→SMPL-X β 호출 + reports/body_profile_gap_<date>.json 체크인
       """
       ```

    2. **CLI argparse**:
       - `--rtmw-keypoints-dir` (Phase 1 sweep dump 경로, 향후 재추출).
       - `--nlf-keypoints-dir` (R&D NLF→SMPL-X path 출력).
       - `--videos` (영상 리스트, default = `ref-foxtop ref-foxtop-split ref-invert ref-sideway-spin ref-climb`).
       - `--output` (default `backend/research/evaluations/reports/body_profile_gap_{date}.json`).

    3. **함수 구조**:
       - `load_rtmw_keypoints(path: Path) -> list[PoseFrame]` — Phase 1 sweep dump 또는 Pod 재실행 결과 load.
       - `load_nlf_smplx_keypoints(path: Path) -> list[PoseFrame]` — NLF→SMPL-X β path 출력 load. **본 PR (5a) 에서는 stub + TODO** — placeholder 가 `NotImplementedError("belle Pod 에서 NLF→SMPL-X β 실데이터로 교체 — Task 5b user_setup 박제")` raise. import-time stub 박제 — `nlf` / `smplx` import 는 함수 body 안에 lazy 박제.
       - `compute_profile_gap(rtmw_profile: BodyNormalizationProfile, smplx_profile: BodyNormalizationProfile) -> dict` — abs diff per field. **본 PR 의 수학은 완전 구현** (smoke 가능):
         - `arm_scale_abs_diff = abs(rtmw.arm_scale - smplx.arm_scale)`
         - 6 numeric 필드 모두 (warnings 제외).
         - `verdict` = `"within_5pct_tolerance"` if all diffs < 0.05 else `"gap_too_wide"`.
       - `main()` → JSON + markdown 보고서 산출 (compare_engines.py 패턴).

    4. **운영 코드 import**:
       - `from sunity_shared.analysis.body_normalization_measurer import measure_body_profile` 는 OK (RTMW path 가 운영 측정기 호출).
       - `from sunity_shared.analysis.body_normalization import BodyNormalizationProfile` 도 OK (dataclass type).
       - NLF / SMPL-X import 는 본 파일 안의 함수 body 에서만 lazy 박제 — module-top import 금지.

    5. **Smoke test (`backend/tests/test_compare_body_profile_smoke.py`)** — NLF/SMPL-X 실호출 X:
       - `test_argparse_help`: `--help` 실행 시 sys.exit 0, "rtmw-keypoints-dir" 문자열 등장.
       - `test_compute_profile_gap_zero`: 동일 profile 2개 입력 → 모든 diff == 0, verdict == "within_5pct_tolerance".
       - `test_compute_profile_gap_nonzero`: 다른 profile (arm_scale 차이 0.10) → abs diff 0.10, verdict == "gap_too_wide".
       - `test_load_rtmw_empty_dir`: 빈 디렉터리 → graceful empty list (raise X).
       - `test_load_nlf_smplx_raises_stub`: `load_nlf_smplx_keypoints(...)` 가 `NotImplementedError` raise (Task 5b 안전망 — 실 구현 전까지 운영 path 가 사용 시도하면 즉시 fail-loud).

    6. **TODO 박제 (파일 하단)**:
       - `# TODO(belle-pod, Task 5b): NLF→SMPL-X β path 실 구현 + Pod 에서 5영상 실행 → reports/body_profile_gap_<date>.json 산출 → git commit`.
       - `# 본 TODO 해소 = ROADMAP success criterion 4 closure (HIGH-4 게이트).`

    참조: REVIEWS.md HIGH-4 + RESEARCH.md "R&D 비교 Harness" + `backend/research/evaluations/compare_engines.py`.

    No code in this plan.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_compare_body_profile_smoke.py -x -v && python -m backend.research.evaluations.compare_body_profile --help 2>&1 | grep -q "rtmw-keypoints-dir"</automated>
  </verify>
  <acceptance_criteria>
    - 5개 smoke test 모두 PASS
    - `--help` 실행 시 CLI 인자 노출
    - NLF/SMPL-X import 가 본 파일 안의 함수 body 에서만 (Task 6 의 AST 테스트로 강제)
    - 파일 헤더에 D-02-04 + license-blocklist-pose memory 인용
    - HIGH-4 게이트 박제: `load_nlf_smplx_keypoints` 가 `NotImplementedError` stub
    - gap 산식 완전 구현 (5a scope) — Task 5b 가 실 입력만 채우면 됨
    - 파일 하단 TODO(belle-pod, Task 5b) 박제
  </acceptance_criteria>
  <done>
    위 acceptance_criteria 모두 충족.
  </done>
  <commit-template>
    chore(02): scaffold R&D compare_body_profile harness with gap math + NLF stub (HIGH-4 5a — Pod execution deferred to Task 5b)
  </commit-template>
  <depends-on>Task 3 (BodyNormalizationProfile 측정기 — gap 계산 대상)</depends-on>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5b: belle Pod 실 NLF→SMPL-X β 실행 + body_profile_gap_&lt;date&gt;.json 체크인 (HIGH-4 ROADMAP §4 closure gate)</name>
  <files>
    backend/research/evaluations/reports/body_profile_gap_&lt;YYYYMMDD&gt;.json (create — belle Pod 산출),
    backend/research/evaluations/reports/body_profile_gap_&lt;YYYYMMDD&gt;.md (create — belle Pod 산출)
  </files>
  <read_first>
    .planning/phases/02-bodynormalizationprofile-rtmw-segment/02-RESEARCH.md (R&D 격리 + NLF→SMPL-X β 변환 경로 + Architectural Responsibility Map),
    backend/research/evaluations/compare_body_profile.py (Task 5a 산출 scaffold — load_nlf_smplx_keypoints stub 위치 + gap math),
    backend/research/evaluations/compare_engines.py (R&D harness 패턴 — 보고서 산출 형식 참조),
    docs/licenses/rtmw-weights-audit.md (라이선스 게이트 — SMPL-X 비상업 박제 확인),
    backend/shared/python/sunity_shared/analysis/body_normalization.py (BodyNormalizationProfile dataclass — RTMW 측정값과 비교할 target schema)
  </read_first>
  <action>
    Goal (per HIGH-4 게이트): ROADMAP success criterion 4 ("R&D 비교군: 동일 영상에서 NLF→SMPL-X β로 추출한 BodyNormalizationProfile 과의 갭을 보고서로 출력") 의 실제 closure. executor (자동화 path) 가 수행 불가 — belle 의 RunPod GPU pod 에서 NLF/SMPL-X 모델 weights + 실 영상 access 가 필요.

    **HIGH-4 박제**: scaffold (Task 5a) 만으로는 ROADMAP §4 closure 표시 X. 본 task 가 완료되어야만 phase verifier 가 success criterion 4 PASS 처리.
  </action>
  <what-built>
    Task 5a 의 `compare_body_profile.py` scaffold + smoke (executor 가 commit). `load_nlf_smplx_keypoints` 는 `NotImplementedError` stub.
  </what-built>
  <how-to-verify>
    belle 가 수행:

    1. **belle Pod (1ablelgbtrzcgb 또는 후속) ssh**:
       ```
       ssh -p &lt;port&gt; -i ~/.ssh/id_ed25519 root@&lt;pod-ip&gt;
       cd /workspace/SunityMotion
       git pull origin main  # Task 5a scaffold 받음
       ```

    2. **NLF→SMPL-X β path 구현**:
       - `load_nlf_smplx_keypoints` 의 `NotImplementedError` stub 을 실 NLF 호출로 교체 (belle Pod 환경 — NLF weights 이미 박제).
       - 5영상 (`ref-foxtop ref-foxtop-split ref-invert ref-sideway-spin ref-climb`) 의 raw frames → NLF 3D keypoints → SMPL-X β fit → segment 비율 변환 → `BodyNormalizationProfile`.

    3. **gap 보고서 산출**:
       ```
       python -m backend.research.evaluations.compare_body_profile \
         --rtmw-keypoints-dir backend/research/evaluations/reports/sweep_rtmw_$(date +%Y%m%d)/keypoints/ \
         --nlf-keypoints-dir backend/research/evaluations/reports/nlf_smplx_$(date +%Y%m%d)/ \
         --output backend/research/evaluations/reports/body_profile_gap_$(date +%Y%m%d).json
       ```

    4. **결과 검증 + commit**:
       - JSON + Markdown 보고서가 5영상 모두에 대해 RTMW vs SMPL-X β BodyNormalizationProfile gap 표시.
       - `verdict` 가 belle 검토 — within tolerance 또는 gap_too_wide.
       - `git add backend/research/evaluations/reports/body_profile_gap_*.{json,md}` 후 commit.

    5. **본 plan 의 task 5b 종료 신호**:
       - belle 가 SUMMARY 에 산출 파일 경로 박제 후 "approved" 라고 응답.
       - 만약 NLF→SMPL-X β path 가 belle Pod 에서 구현 어려우면 ("not feasible") — Task 5b 박제 보류 + ROADMAP §4 를 v1.5 로 deferred 박제 (belle 결정).
  </how-to-verify>
  <resume-signal>belle 가 "approved" + `body_profile_gap_<date>.json` 파일 경로 응답, 또는 "not feasible — defer to v1.5" 응답 시 다음 task 진행</resume-signal>
  <acceptance_criteria>
    - `backend/research/evaluations/reports/body_profile_gap_<date>.json` 파일이 5영상 데이터 포함하여 체크인됨 (approved path)
    - 또는 belle "not feasible" 박제 + ROADMAP §4 v1.5 defer 박제 (alternative path)
    - 본 task 가 closed 되어야 phase verifier 가 ROADMAP success criterion 4 PASS 처리
  </acceptance_criteria>
  <done>
    위 둘 중 하나 충족.
  </done>
  <commit-template>
    data(02): add body_profile_gap_<date>.{json,md} from belle Pod 5-video run (HIGH-4 ROADMAP §4 closure)
  </commit-template>
  <depends-on>Task 5a (scaffold)</depends-on>
</task>

<task type="auto">
  <name>Task 6: AST import isolation 테스트 (mediapipe 제외, HIGH-3 narrowed) + REQUIREMENTS.md BODY-01 RTMW 정합 갱신</name>
  <read_first>
    backend/shared/python/sunity_shared/analysis/pose_engines/__init__.py (lazy mediapipe import 박제 — HIGH-3 root cause),
    backend/shared/python/sunity_shared/analysis/pose_engines/mediapipe_engine.py (live mediapipe import lines 74-76, 197),
    .planning/REQUIREMENTS.md (line 30 BODY-01 — MediaPipe 언급),
    backend/research/evaluations/compare_body_profile.py (Task 5a 산출 — import 차단 의미 부여)
  </read_first>
  <action>
    Goal:
    (1) AST 스캔으로 `sunity_shared/**/*.py` 가 운영-격리 대상 모듈 (`backend.research` / `research` / `nlf` / `smplx` / `smpl_x`) 을 import 시 fail 하는 자동 차단 테스트 박제. **HIGH-3 fix — `mediapipe` 차단 제외** (legacy code 살아있음, RTMW pivot post-cleanup 별 phase).
    (2) REQUIREMENTS.md BODY-01 의 "MediaPipe" → "RTMW" 정합 갱신.

    구현 노트:

    **(1) `backend/tests/test_research_import_isolation.py` 신설**:

    - `ast.parse` 로 `backend/shared/python/sunity_shared/` 하위 모든 `.py` 파일 스캔.
    - 금지 패턴 (HIGH-3 narrowed list):
      - `import research`
      - `import backend.research`
      - `from research import ...`
      - `from research.X import ...`
      - `from backend.research import ...`
      - `from backend.research.X import ...`
      - `import nlf`
      - `from nlf import ...`
      - `from nlf.X import ...`
      - `import smplx`
      - `from smplx import ...`
      - `from smplx.X import ...`
      - `import smpl_x`
      - `from smpl_x import ...`
    - **`mediapipe` 차단 제외 (HIGH-3 박제)**:
      - 본 phase 는 mediapipe 차단 책임 X.
      - 파일 상단에 큰 TODO 코멘트 박제:
        ```
        # TODO(post-mediapipe-removal): RTMW pivot (commit 2a8aa72) 후속 작업 —
        # mediapipe quarantine 은 별도 phase 책임 ([[rtmw-free-stack-pivot]] 정합).
        # 본 phase (Phase 2) 가 mediapipe import 차단까지 떠안으면 sunity_shared/pose_engines/
        # mediapipe_engine.py 와 mediapipe_lifter_engine.py 가 즉시 violations 으로 잡힘
        # (현재 legacy live code). REVIEWS.md HIGH-3 박제 — narrowed 범위.
        ```
    - 위반 검출 시 `assert False, f"sunity_shared module {path} illegally imports {forbidden}: {node.lineno}"`.
    - 테스트 함수 2개:
      - `test_sunity_shared_does_not_import_research`: backend.research / research 차단 검증.
      - `test_sunity_shared_does_not_import_nlf_or_smplx`: nlf / smplx / smpl_x 차단 검증.
    - **운영 path 만 검증** — `backend/tests/` / `backend/research/` / `backend/functions/` 는 검증 대상 X.
    - pytest 실행 시 < 1초 (순수 AST).

    **(2) `.planning/REQUIREMENTS.md` line 30 갱신**:
    - 현 텍스트: `"**BODY-01**: MediaPipe 키포인트로부터 신체 segment 길이·비율·좌우 비대칭이 자동 추출되어 ...`
    - 갱신: `"**BODY-01**: RTMW 133 wholebody 키포인트로부터 ...`.
    - 뒷부분 `SMPL-X β 비교군은 R&D 평가 스크립트에서만 갭 보고 (제품 코드 비호출)` 유지.
    - footer note 추가 (REQUIREMENTS.md 하단 update 로그):
      - `*Updated 2026-06-07 — Phase 2 plan 01 RTMW pivot 정합 (BODY-01 MediaPipe → RTMW 갱신).*`.
    - line 27 POSE-01 도 MediaPipe 언급 있음 — **본 task scope X** (Phase 1 책임, 이미 RTMW swap 완료).
    - 본 task 는 BODY-01 1줄만 변경 (atomic + scoped).

    참조: REVIEWS.md HIGH-3 + RESEARCH.md "R&D 비교 Harness" + Plan 01-24 정합.

    No code in this plan.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_research_import_isolation.py -x -v</automated>
    추가: `grep -n "RTMW" .planning/REQUIREMENTS.md | grep "BODY-01"` → 1.
    추가: `grep "MediaPipe" .planning/REQUIREMENTS.md | grep "BODY-01"` → 0 (BODY-01 에서 MediaPipe 제거).
    HIGH-3 박제: `grep -c "mediapipe" backend/tests/test_research_import_isolation.py` ≥ 1 (TODO 코멘트 + 박제 사유 존재).
  </verify>
  <acceptance_criteria>
    - 2개 AST 테스트 PASS
    - sunity_shared 의 backend.research / NLF / SMPL-X import 0 확인
    - mediapipe 차단 코드 **없음** (TODO 박제만 존재 — HIGH-3 fix)
    - REQUIREMENTS.md BODY-01 (line 30) 이 RTMW 로 갱신, MediaPipe 제거
    - REQUIREMENTS.md footer note (2026-06-07 RTMW pivot 정합) 추가
  </acceptance_criteria>
  <done>
    위 acceptance_criteria 모두 충족.
  </done>
  <commit-template>
    chore(02): enforce sunity_shared R&D import isolation via AST (mediapipe excluded — HIGH-3) + align BODY-01 to RTMW
  </commit-template>
  <depends-on>Task 5a (R&D harness 존재해야 import isolation 의 의미 있음)</depends-on>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RunPod Pod ↔ Lambda | RTMW pose_frames 가 RunPod 에서 측정 후 Lambda 가 결과 받음 — Phase 2 는 내부 호출만 (외부 입력 X) |
| sunity_shared (운영) ↔ backend.research (R&D) | 라이선스 격리 (SMPL-X 상업 불가) — import leak 시 법적 리스크 |
| `_RTMWNlfCompat.estimate` (Pod) ↔ Firestore (Lambda complete_analysis) | BodyNormalizationProfile propagation 경계 — Phase 6 consumer 가 읽는 곳 (HIGH-1 박제) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | BodyNormalizationProfile confidence 값이 [0, 1] 범위 벗어남 | mitigate | dataclass `__post_init__` validator 박제 (Phase 1 산출). 측정기 출력 `min(1.0, ...)` 강제. NaN → 0.0 폴백 |
| T-02-02 | Repudiation / 법적 | sunity_shared 가 backend.research 또는 SMPL-X / NLF 모듈 import (라이선스 위반) | mitigate | Task 6 AST 단위테스트 자동 차단 (HIGH-3 narrowed — mediapipe 제외). CI gate |
| T-02-03 | Denial of Service | 인버트/짧은 영상에서 NaN profile 출력 → 다운스트림 (Phase 6) crash | mitigate | `insufficient_frames` warning + confidence=0 + scale=1.0 폴백. `__post_init__` 가 finite 강제. Task 3 `test_short_frames_graceful_degrade` 검증 |
| T-02-04 | Information Disclosure | R&D 보고서 (`body_profile_gap_*.json`) 에 사용자 영상 메타데이터 leak | accept | belle Pod 환경 (내부) 만 실행. 보고서는 `backend/research/evaluations/reports/` (private repo). 외부 노출 path 없음 |
| T-02-05 | Tampering | `_RTMWNlfCompat.estimate` tuple 시그너처 변경이 caller breakage | mitigate | Task 4 통합 테스트 `test_pipeline_callers_propagate_profile` 가 signature inspect 로 검증 + 기존 pipeline 회귀 0 |
| T-02-06 | Information Disclosure | Firestore `bodyNormalizationProfile` 필드가 nested array 위반 | mitigate | warnings = flat string list (`firestore-nested-array-flat` memory). Task 4 `test_complete_analysis_persists_body_profile_for_phase6` 가 payload shape 검증 |

> 본 phase 는 외부 입력 X (Lambda HTTP API 없음, 모두 Pod/Lambda 내부 함수). V2/V3/V4/V6 적용 X.
</threat_model>

<verification>
## Phase-level 검증 (전 task 완료 후)

1. **단위 테스트 전체**: `cd backend && pytest tests/ -x` — 회귀 0.
2. **3-way lockstep 자동 검증** (L1 박제 포함): `pytest tests/test_body_normalization_lockstep.py -x` PASS (7 함수 — 기존 5 + L1 신설 2).
3. **Fixture smoke load (HIGH-2)**: `pytest tests/fixtures/body_normalization/test_fixtures_loadable.py -x` PASS.
4. **Measurer 5 fixture + M2**: `pytest tests/test_body_normalization_measurer.py -x` PASS (10 case).
5. **Pipeline 통합 + Phase-6 boundary (HIGH-1)**: `pytest tests/test_pipeline_body_profile_injection.py -x` PASS (6 case).
6. **R&D 격리 (HIGH-3 narrowed)**: `pytest tests/test_research_import_isolation.py -x` PASS (2 case).
7. **R&D harness smoke (HIGH-4 5a)**: `pytest tests/test_compare_body_profile_smoke.py -x` PASS (5 case).
8. **HIGH-4 5b 게이트**: `ls backend/research/evaluations/reports/body_profile_gap_*.json 2>/dev/null` ≥ 1 — belle Pod 산출 체크인 완료 (또는 v1.5 defer 박제).
9. **REQUIREMENTS.md 정합**: BODY-01 텍스트 grep — "RTMW" 등장 + "MediaPipe" 미등장.
10. **의존성 검증**: `grep -r "import scipy" backend/shared/python/sunity_shared/analysis/body_normalization_measurer.py` → 0 (numpy only).
11. **Propagation 검증**: `grep -n "bodyNormalizationProfile\|body_normalization_profile" backend/functions/pipeline/app.py backend/shared/python/sunity_shared/firestore_admin.py app/src/types/analysis.ts | wc -l` ≥ 3 (HIGH-1 박제).
12. **HIGH-3 박제**: `grep -c "mediapipe" backend/tests/test_research_import_isolation.py` ≥ 1 (TODO 코멘트).
</verification>

<success_criteria>
## ROADMAP Success Criteria 매핑 (v2 박제)

| Criterion | Task | 검증 | 본 phase closure 가능? |
|-----------|------|------|--|
| 1. RTMW 키포인트에서 segment 길이가 시간 평균으로 안정적으로 추출 (jitter 스무딩) | Task 3 (MAD smoothing) | `test_normal_pose_profile_valid` | 예 |
| 2. BodyNormalizationProfile 7필드 자동 산출 | Task 3 | `test_profile_validator_passes` | 예 |
| 3. 낮은 confidence 시 warnings 표기 (단정 X) | Task 3 (5종 warning + M2 박제) | `test_inverted_pose_warning`, `test_occluded_leg_warning`, `test_short_clip_warning`, `test_asymmetric_warning`, `test_inverted_warning_is_pole_axis_relative_not_raw_y` | 예 |
| 4. R&D 비교군 NLF→SMPL-X β 갭 보고서 (제품 코드 비호출) | Task 5a (scaffold) **+ Task 5b (belle Pod 실 보고서 체크인)** | scaffold smoke + 실 보고서 ls 검증 | **Task 5b 완료 시에만 PASS — HIGH-4 게이트** |
| 5. 데이터 계약 3-way lockstep | Task 1 | `test_body_normalization_lockstep` (확장 7 함수) | 예 |

**v1 Done (executor scope)**: criterion 1, 2, 3, 5 PASS + criterion 4 의 5a scaffold PASS + Phase 1 회귀 0 + Pipeline 통합 verified + REQUIREMENTS.md BODY-01 RTMW 정합.

**ROADMAP §4 closure**: Task 5b 완료 (belle Pod 실행 산출 체크인) 시. autonomous=false 박제 — verifier 가 자동 close 금지.
</success_criteria>

<source_audit>
## Multi-Source Coverage Audit (v2 박제 — Codex review 정합)

### GOAL (ROADMAP)
| Item | Plan | Task |
|------|------|------|
| RTMW 키포인트 → BodyNormalizationProfile 자동 출력 | 02-01 | Task 3 |
| 두 엔진 (체형 보정 + 힘 패턴) 의 공유 입력 | 02-01 | Task 4 (pipeline propagation + Firestore 저장 — HIGH-1 fix) |
| SMPL-X β R&D 비교군만 | 02-01 | Task 5a (scaffold), Task 5b (실 실행), Task 6 (import 차단) |

### REQ (REQUIREMENTS.md)
| Req ID | Plan | Task |
|--------|------|------|
| BODY-01 | 02-01 | Task 3 (측정기) + Task 4 (통합 + Firestore) + Task 6 (텍스트 RTMW 정합) |

### RESEARCH (02-RESEARCH.md)
| Feature | Plan | Task |
|---------|------|------|
| body_normalization_measurer.py (순수 numpy) | 02-01 | Task 3 |
| MAD outlier + robust median smoothing | 02-01 | Task 3 |
| torso self-reference normalize | 02-01 | Task 3 |
| 5종 warnings enum | 02-01 | Task 1 (3-way + L1 박제) + Task 3 (판정 + M2 박제) |
| 5 synthetic fixture (seed=42) | 02-01 | Task 2 (HIGH-2 schema 정합) |
| `_RTMWNlfCompat` body_shape 주입 + propagation | 02-01 | Task 4 (HIGH-1 tuple 반환 + Firestore) |
| R&D 비교 harness compare_body_profile.py | 02-01 | Task 5a (scaffold), Task 5b (실 실행) |
| AST import isolation (HIGH-3 narrowed) | 02-01 | Task 6 |
| Phase 1 sweep keypoint dump 재추출 (M1) | DEFERRED to v1.5 | Task 2 TODO 박제 + Task 2b sanity skip |
| Firestore 저장 (HIGH-1 fix 의 일부) | 02-01 (v1 scope 갱신) | Task 4 — Phase 6 boundary 박제 |

### CONTEXT (02-CONTEXT.md Locked Decisions)
| Decision ID | Plan | Task |
|-------------|------|------|
| D-02-01 (RTMW 운영 백본, MediaPipe X) | 02-01 | Task 3 (RTMW 만 측정), Task 6 (REQUIREMENTS RTMW 갱신) |
| D-02-02 (3-way lockstep) | 02-01 | Task 1 (단일 atomic commit + L1 AST 박제) |
| D-02-03 (confidence/warnings 항상 출력) | 02-01 | Task 3 (warnings 판정 + 폴백) |
| D-02-04 (SMPL-X R&D 만) | 02-01 | Task 5a (R&D 격리), Task 5b (실 실행 — Pod), Task 6 (import 차단) |
| D-02-05 (자동 측정 우선) | 02-01 | Task 3 (자동 측정기 본체) |
| D-02-06 (실 영상 BodyProfile 만 v1) | 02-01 | Task 2 (synthetic fixture), Task 2b (실 RTMW sanity skip) |

### REVIEWS (02-REVIEWS.md — Codex)
| Concern | Severity | v2 Task | Notes |
|---|---|---|---|
| HIGH-1: body_shape propagation discarded | HIGH | Task 4 (Option A — tuple 반환 + Firestore) | Phase-6 boundary 테스트 박제 |
| HIGH-2: fixture API/schema 환상 | HIGH | Task 1 + Task 2 (factory helper + 실 dataclass 정합) | PoseFrame.from_dict 부재 박제 |
| HIGH-3: import isolation legacy code 충돌 | HIGH | Task 6 (mediapipe 제외, TODO 박제) | mediapipe 별 phase |
| HIGH-4: scaffold ≠ gap report | HIGH | Task 5a + 5b 분할 (user_setup 게이트) | autonomous=false 박제 |
| M1: synthetic-only 한계 | MED | Task 2b (RTMW sanity skip) | Pod 환경에서만 실행 |
| M2: pose_too_inverted raw y | MED | Task 3 (PoleAxis-relative + 2 unit test) | y flip 둔감 검증 |
| M3: scale 의미 미명세 | MED | Task 1 (3-way 박제 "torso-relative proportion heuristic") | 3 파일 동일 문구 |
| L1: lockstep grep-only | LOW | Task 1 (lockstep AST 자동 검증 확장) | 5 enum 자동 검증 |

### Out of Scope (CONTEXT §1)
- Phase 3 자가입력 → Phase 3
- Phase 6 다운스트림 활용 → Phase 6
- 좌우 비대칭 활용 → Phase 7 (v1 은 warning 만)
- 다각도 입력 → Phase 4
- SMPL-X 본격 도입 → 영구 R&D

### Gaps Found

**HIGH-4 명시 박제**: ROADMAP success criterion 4 ("R&D 비교군: 동일 영상에서 NLF→SMPL-X β로 추출한 BodyNormalizationProfile 과의 갭을 보고서로 출력") 의 실 closure 는 **Task 5b 가 user_setup 게이트** — executor 자동화 불가. belle Pod 실 실행 + `reports/body_profile_gap_<date>.json` 체크인 완료 시까지 ROADMAP §4 PASS 표시 X. 본 plan 의 `autonomous: false` 박제 정합.

이외 GOAL/REQ/RESEARCH/CONTEXT 모든 항목이 task 에 매핑됨.

**HIGH-2 박제 위치 (Codex 정정)**: 본 plan 은 `PoseFrame.from_dict` 가 **코드베이스에 없음** 을 인정 — 따라서 test factory helper (`pose_frame_from_dict` in `backend/tests/fixtures/body_normalization/_factory.py`) 박제로 우회. Production 코드 수정 X.
</source_audit>

<output>
Phase 2 가 완료되면 다음 산출:

1. `backend/shared/python/sunity_shared/analysis/body_normalization_measurer.py` — 순수 함수 측정기 + M2 PoleAxis-relative inversion (Phase 6/7/8/9 의 공유 입력)
2. `backend/tests/fixtures/body_normalization/_factory.py` — `pose_frame_from_dict` test helper (HIGH-2 — PoseFrame.from_dict 부재 우회)
3. `backend/tests/fixtures/body_normalization/_generate.py` + 5 fixture JSON — HIGH-2 schema 정합 (timestamp_ms / uncertainty_proxy / PoleAxis 4-field)
4. `backend/tests/fixtures/body_normalization/test_fixtures_loadable.py` — smoke load 게이트
5. `backend/tests/test_body_normalization_measurer.py` — 10개 단위 테스트 (M2 박제 2개 포함)
6. `backend/tests/test_pipeline_body_profile_injection.py` — 6개 통합 테스트 (HIGH-1 Phase-6 boundary 박제)
7. `backend/tests/test_research_import_isolation.py` — AST 자동 차단 2개 테스트 (HIGH-3 narrowed)
8. `backend/research/evaluations/compare_body_profile.py` — R&D 비교 harness scaffold (HIGH-4 Task 5a)
9. `backend/tests/test_compare_body_profile_smoke.py` — 5개 smoke
10. `backend/research/evaluations/reports/body_profile_gap_<date>.json` — belle Pod 산출 (HIGH-4 Task 5b user_setup)
11. 수정: `docs/contract.md` §7 warnings 5 enum + M3 "torso-relative proportion heuristic" + `app/src/types/analysis.ts` 주석 + `body_normalization.py` docstring (3-way lockstep) + `test_body_normalization_lockstep.py` L1 AST 박제
12. 수정: `backend/functions/pipeline/app.py` `_RTMWNlfCompat.estimate` tuple 반환 + 두 caller 갱신 + `_process` complete_analysis kw (HIGH-1)
13. 수정: `backend/shared/python/sunity_shared/firestore_admin.py` `complete_analysis(body_normalization_profile=...)` kw 박제 + `bodyNormalizationProfile` Firestore 필드 (HIGH-1)
14. 수정: `app/src/types/analysis.ts` `AnalysisDoc.bodyNormalizationProfile?` optional (HIGH-1 propagation lockstep)
15. 수정: `.planning/REQUIREMENTS.md` BODY-01 "MediaPipe" → "RTMW"

완료 시 `.planning/phases/02-bodynormalizationprofile-rtmw-segment/02-01-SUMMARY.md` 생성.

다음 Phase: Phase 3 (자가입력 BodyProfileInput) — BODY-01 완료 후 unblock.
</output>

---

## Total Atomic Commits 요약 (v2 — Task 5 split 박제)

| # | Task | Commit Template | Files Changed | Type | Concerns fixed |
|---|------|----------------|----------------|------|---|
| 1 | contract.md §7 warnings enum 5종 + M3 의미 + L1 lockstep AST | `feat(02): lock warnings enum 5-way + scale semantics for BodyNormalizationProfile (3-way lockstep + AST gate)` | 4 files | feat | HIGH-2 (enum scope) / M3 / L1 |
| 2 | PoseFrame factory helper + Synthetic fixture + smoke + RTMW sanity skip | `test(02): add PoseFrame factory helper + synthetic fixtures (5 cases) + RTMW sanity skip (HIGH-2 schema fix, M1 fallback)` | 9 files (factory + 5 JSON + generator + 2 test) | test | HIGH-2 / M1 |
| 3 | body_normalization_measurer.py 본체 + 단위 테스트 + M2 박제 | `feat(02): add measure_body_profile RTMW segment measurer with MAD smoothing + PoleAxis-relative inversion (BODY-01, M2 fix)` | 2 files | feat | M2 |
| 4 | Pipeline 통합 — tuple 반환 + Firestore 저장 + AnalysisDoc lockstep | `feat(02): propagate BodyNormalizationProfile from _RTMWNlfCompat to Firestore via tuple return + complete_analysis kw (HIGH-1 Option A — Phase 6 boundary closure)` | 4 files | feat | **HIGH-1** |
| 5a | R&D 비교 harness scaffold + smoke + gap math | `chore(02): scaffold R&D compare_body_profile harness with gap math + NLF stub (HIGH-4 5a — Pod execution deferred to Task 5b)` | 2 files | chore | HIGH-4 (in-scope half) |
| 5b | belle Pod 실 NLF→SMPL-X β 실행 + body_profile_gap_<date>.json 체크인 | `data(02): add body_profile_gap_<date>.{json,md} from belle Pod 5-video run (HIGH-4 ROADMAP §4 closure)` | 2 files (Pod 산출) | data | **HIGH-4 게이트** |
| 6 | AST import isolation (mediapipe 제외, HIGH-3) + REQUIREMENTS BODY-01 RTMW | `chore(02): enforce sunity_shared R&D import isolation via AST (mediapipe excluded — HIGH-3) + align BODY-01 to RTMW` | 2 files | chore | HIGH-3 |

**총 7개 atomic commit** (Task 5 분할 = +1).
**의존 chain**: `Task 1 + Task 2 (병렬) → Task 3 → Task 4 + Task 5a (병렬) → Task 5b (belle Pod user_setup, blocking) → Task 6`.
**autonomous: false** 박제 (Task 5b 가 user_setup checkpoint).
