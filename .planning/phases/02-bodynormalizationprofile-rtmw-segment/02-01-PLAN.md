---
phase: 02-bodynormalizationprofile-rtmw-segment
plan: 01
type: execute
wave: 1
plan_revision: v5
depends_on: []
files_modified:
  - docs/contract.md
  - app/src/types/analysis.ts
  - backend/shared/python/sunity_shared/analysis/body_normalization.py
  - backend/shared/python/sunity_shared/analysis/body_normalization_measurer.py
  - backend/tests/fixtures/body_normalization/__init__.py
  - backend/tests/fixtures/body_normalization/_factory.py
  - backend/tests/fixtures/body_normalization/_generate.py
  - backend/tests/fixtures/body_normalization/normal_pose_pose_frames.json
  - backend/tests/fixtures/body_normalization/inverted_pose_pose_frames.json
  - backend/tests/fixtures/body_normalization/occluded_leg_pose_frames.json
  - backend/tests/fixtures/body_normalization/short_clip_pose_frames.json
  - backend/tests/fixtures/body_normalization/asymmetric_pose_frames.json
  - backend/tests/fixtures/body_normalization/test_fixtures_loadable.py
  - backend/tests/test_body_normalization_measurer.py
  - backend/tests/test_pipeline_body_profile_injection.py
  - backend/tests/test_research_import_isolation.py
  - backend/tests/test_estimated_height_scale_consumer_semantics.py
  - backend/tests/test_body_normalization_lockstep.py
  - backend/tests/test_body_normalization_profile.py
  - backend/functions/pipeline/app.py
  - backend/research/evaluations/compare_body_profile.py
  - backend/research/evaluations/extract_rtmw_body_profile_keypoints.py
  - backend/research/evaluations/smplx_joints_to_body_profile.py
  - backend/research/evaluations/extract_smplx_joints_from_video.py
  - backend/research/evaluations/run_body_profile_gap_report.py
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - BODY-01
user_setup:
  - service: runpod-gpu-pod
    why: "ROADMAP success criterion 4 (Phase 6 통합 path 의 R&D 비교군 — NLF→SMPL-X β fit → β→model→joints → joints→BodyNormalizationProfile 변환 + RTMW segment 측정과의 갭 보고서) 충족은 belle 사내 RunPod GPU pod 의 실 NLF + SMPL-X 추론을 요구. v5 박제: belle 가 실행할 명령은 단 두 줄 — (1) `python -m backend.research.evaluations.run_body_profile_gap_report --videos ... --date ...` (single audited orchestrator, HIGH-2 v5), (2) `git add ... && git commit ...`. 4단계 console 작업 폐기 ([[user-beginner-stepwise]] 정합)."
    env_vars:
      - name: RUNPOD_AUTH_TOKEN
        source: "belle pod env (이미 박제)"
      - name: RTMW_ONNX_PATH
        source: "belle pod env (Pod 의 rtmw end2end.onnx 절대 경로)"
      - name: YOLOX_ONNX_PATH
        source: "belle pod env (rtmlib detector 의존)"
      - name: NLF_MODEL_PATH
        source: "belle pod env (Pod 의 nlf_l_multi.torchscript)"
      - name: SMPLX_MODEL_PATH
        source: "belle 사내 NLF→SMPL-X R&D weights (PS:License 1.0 비상업)"
    dashboard_config:
      - task: "v5 — belle Pod 실행 두 줄. Step 1: `cd /workspace/SunityMotion && python -m backend.research.evaluations.run_body_profile_gap_report --videos ref-foxtop ref-foxtop-split ref-invert ref-sideway-spin ref-climb --date $(date +%Y%m%d)`. Step 2: `git add backend/research/evaluations/reports/body_profile_gap_$(date +%Y%m%d).* && git commit -m 'data(02): SMPL-X vs RTMW body profile gap report (Phase 2 ROADMAP §4)'`. 설계/구현 결정 0. 환경 manipulation 0 ([[runpod-gpu-env]] 외)."
        location: "belle RunPod pod 1ablelgbtrzcgb (현 활성)"

must_haves:
  truths:
    - "신규 helper `_angles_and_body_profile_from_video(bucket, key) -> tuple[np.ndarray, BodyNormalizationProfile | None]` 가 박제되며 사이드카 mutable state 0 (race-safe). B8 박제 (_angles_from_video, _angles_and_video_path_from_video 시그너처) 무변경. (HIGH-1 v4 유지)"
    - "동시 background task 2개가 barrier 로 인터리브되어도 각각 자기 frame 의 profile 만 받는다 (concurrency 회귀 테스트, HIGH-1 v4 유지)"
    - "estimate 가 예외를 던지면 helper 가 예외를 전파하며 직전 성공 호출의 profile 이 leak 되지 않는다 (stale-failure 테스트, HIGH-1 v4 유지)"
    - "Phase 2 v1 scope — Firestore 저장은 Phase 6 plan 책임. AnalysisDoc / firestore_admin 변경 0. ROADMAP success criterion 2/4 가 'measurer + helper ready' (Phase 2) 와 'product analysis document 가 profile 포함' (Phase 6) 을 acceptance text 에서 명시 구분한다 (MEDIUM-1 v4 유지 + MEDIUM-1 v5 wording 갱신)"
    - "SMPL-X 비교 path 는 weights-free fake 가 아닌 `smplx_joints_to_body_profile(joints: np.ndarray) -> BodyNormalizationProfile` 로 구현되며 joints 는 NLF→SMPL-X β→model→joints 의 실 Pod 산출이다. CI 는 synthetic 직립 joint array 로 테스트 가능. β-only pure-math 변환 폐기 (HIGH-1 v5)"
    - "Task 5b 의 belle Pod 실행은 단일 명령 `python -m backend.research.evaluations.run_body_profile_gap_report ...` + 단일 `git commit` 두 줄로 축약된다 — 멀티스텝 console 작업 0 ([[user-beginner-stepwise]] + HIGH-2 v5)"
    - "BodyNormalizationProfile.__post_init__ 가 5개 numeric scale 필드 (estimated_height_scale / arm_scale / leg_scale / torso_scale / shoulder_hip_ratio) 의 finite (not NaN, not ±inf) + strictly positive (> 0) 를 contract 경계에서 강제하며 NaN/inf/0/negative 입력은 ValueError 로 거절된다 (MEDIUM-2 v5)"
    - "pose_too_inverted 판정은 image y-order 직접 (mean(mid_shoulder.y) vs mean(mid_hip.y) in y-down image coords) 로 PoleAxis.axis_vector sign 과 독립적이다 — 임의 pole_axis 부호에서도 동일 결과 (MEDIUM-3 v5)"
    - "estimatedHeightScale consumer semantic guard 는 4 dir 스캔한다: backend/shared/python/sunity_shared/**/*.py + backend/functions/**/*.py + backend/runpod_inference/**/*.py + app/src/**/*.ts(x). 제외: backend/research/ + backend/tests/ + app/scripts/ (LOW-1 v5)"
    - "AnalysisDoc 필드 부재 검증 + BodyNormalizationProfile interface 존재 검증은 `grep -c` 대신 awk block-extraction 또는 python AST/regex block-scoped snippet 으로 한다 (LOW-2 v5)"
    - "Task 6 AST scanner 가 importlib.import_module + __import__ + spec_from_file_location 의 string literal 인자 모두를 차단하며 비-literal 동적 import 는 잔여 위험으로 명시 박제된다 (LOW-2 v4 유지)"
    - "lockstep 테스트가 5종 warning enum 문자열을 3 파일에서 string/regex parse 한 결과로 자동 검증한다 (LOW-1 v4 호칭 갱신 유지)"
    - "RTMW 실 출력 coverage 는 v1.5 deferral — ROADMAP 성공 기준 1 의 acceptance text 가 'synthetic + adapter-path validation; real RTMW output validation deferred to v1.5 sweep run' 으로 갱신된다 (MEDIUM-2 v4 유지, v5 reaffirm)"
    - "5종 fixture JSON이 PoseFrame dataclass 시그너처 (timestamp_ms / Keypoint3D.uncertainty_proxy / PoleAxis 4-field axis_vector+confidence_level+source+frame_index) 와 정확히 일치하며 test factory helper로 load 가능하다 (HIGH-2 v1→v2 유지)"
    - "AST import isolation 은 sunity_shared 뿐 아니라 backend/functions 와 backend/runpod_inference 까지 스캔하며 importlib.import_module / __import__ / spec_from_file_location 모두 차단한다 (MEDIUM-1 v3 + LOW-2 v4 유지)"
    - "estimatedHeightScale은 절대 키가 아닌 torso-relative proportion heuristic 임이 docs/contract.md / app/src/types/analysis.ts / backend/.../body_normalization.py 3 파일에 동시 명시된다 (v1→v2 M3 유지)"
    - "Task 5b 산출 (`reports/body_profile_gap_<date>.json`) 체크인 또는 belle 명시 v1.5 defer 까지 ROADMAP §4 closure 표시 X"
    - "Task 6 (AST isolation + REQUIREMENTS RTMW 정합 + ROADMAP §1+§2+§4 wording) 는 Task 5a 만 의존하며 Task 5b human gate 와 평행 진행 가능하다 (MEDIUM-3 v3 유지)"
    - "`python -m backend.research.evaluations.{...}` 는 모두 repo root (`/workspace/SunityMotion`) 에서 실행되며 plan 의 모든 verify 블록과 commit 메시지가 이 convention 을 박제한다 (LOW-1 v3 유지)"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/body_normalization.py"
      provides: "BodyNormalizationProfile dataclass + __post_init__ extended validator (5 numeric scale finite + strictly positive, MEDIUM-2 v5)"
      contains: "estimated_height_scale"
    - path: "backend/shared/python/sunity_shared/analysis/body_normalization_measurer.py"
      provides: "measure_body_profile(pose_frames) -> BodyNormalizationProfile 순수 함수 (MEDIUM-3 v5 image-y-order inversion + MEDIUM-2 v5 fallback emits 1.0 not 0.0)"
      contains: "measure_body_profile"
    - path: "backend/tests/fixtures/body_normalization/_factory.py"
      provides: "pose_frame_from_dict(raw: dict) -> PoseFrame test helper (HIGH-2 v1→v2)"
      contains: "pose_frame_from_dict"
    - path: "backend/tests/fixtures/body_normalization/_generate.py"
      provides: "seed=42 멱등 fixture 생성기 + 5종 JSON. inverted fixture 는 image-y-order 로 mid_shoulder.y > mid_hip.y 보장 (MEDIUM-3 v5)"
      contains: "RandomState(42)"
    - path: "backend/tests/fixtures/body_normalization/test_fixtures_loadable.py"
      provides: "5 fixture × pose_frame_from_dict() smoke load + image y-order convention guard (MEDIUM-3 v5)"
    - path: "backend/tests/test_body_normalization_measurer.py"
      provides: "5 fixture × profile assertion 단위 + image-y-order inversion (pole_axis-sign-independent) 검증 (MEDIUM-3 v5)"
    - path: "backend/tests/test_pipeline_body_profile_injection.py"
      provides: "신규 helper `_angles_and_body_profile_from_video` 단위 + concurrency barrier + stale-failure + B8 박제 회귀 (HIGH-1 v4 유지)"
    - path: "backend/tests/test_research_import_isolation.py"
      provides: "AST 스캔 (sunity_shared + backend/functions + backend/runpod_inference → backend.research/nlf/smplx 차단, importlib/__import__/spec_from_file_location literal 3종, MEDIUM-1 v3 + LOW-2 v4)"
    - path: "backend/tests/test_estimated_height_scale_consumer_semantics.py"
      provides: "4 dir consumer lint scan (sunity_shared + backend/functions + backend/runpod_inference + app/src TS, LOW-1 v5). 현재 consumer 0 → vacuous PASS, Phase 6+ load-bearing"
    - path: "backend/tests/test_body_normalization_profile.py"
      provides: "BodyNormalizationProfile.__post_init__ validator unit (MEDIUM-2 v5 — 5 numeric NaN/inf/0/negative 거절 + 정상 값 통과)"
    - path: "backend/research/evaluations/compare_body_profile.py"
      provides: "두 profile dict gap 계산 + 보고서 CLI (HIGH-2 v5 — joints-side path 갱신)"
    - path: "backend/research/evaluations/extract_rtmw_body_profile_keypoints.py"
      provides: "belle Pod 에서 5영상 → RTMW keypoint dump CLI (HIGH-4 v3 유지)"
    - path: "backend/research/evaluations/smplx_joints_to_body_profile.py"
      provides: "smplx_joints_to_body_profile(joints) pure NumPy 변환 + load_smplx_joints(path) loader + CLI. weights-free fake 폐기. 실 fitted SMPL-X joint 입력 (HIGH-1 v5 핵심)"
      contains: "smplx_joints_to_body_profile"
    - path: "backend/research/evaluations/extract_smplx_joints_from_video.py"
      provides: "Pod-only NLF→SMPL-X β fitting + β→model→joints rendering CLI (HIGH-1 v5 — lazy NLF/SMPL-X import, CI 환경에선 weights 없으면 graceful exit)"
      contains: "extract_smplx_joints"
    - path: "backend/research/evaluations/run_body_profile_gap_report.py"
      provides: "단일 audited orchestrator — extract RTMW + extract SMPL-X joints + joints→profile + compare → JSON/MD 보고서. belle 의 한 줄 명령 (HIGH-2 v5 핵심)"
      contains: "run_body_profile_gap_report"
  key_links:
    - from: "backend/functions/pipeline/app.py::_angles_and_body_profile_from_video"
      to: "backend/functions/pipeline/app.py::_RTMWNlfCompat.estimate_with_profile"
      via: "local-return tuple (np.ndarray, BodyNormalizationProfile | None) — 사이드카 mutable state 0 (HIGH-1 v4)"
      pattern: "_angles_and_body_profile_from_video\\("
    - from: "backend/functions/pipeline/app.py::_RTMWNlfCompat.estimate_with_profile"
      to: "sunity_shared.analysis.body_normalization_measurer.measure_body_profile"
      via: "video-level 1회 호출 + tuple return — 시그너처 (frames) -> tuple[np.ndarray, BodyNormalizationProfile | None] (HIGH-1 v4)"
      pattern: "estimate_with_profile"
    - from: "docs/contract.md §7 warnings + estimatedHeightScale 의미"
      to: "app/src/types/analysis.ts BodyNormalizationProfile.warnings + body_normalization.py docstring"
      via: "3-way lockstep (single atomic commit) + lockstep test string/regex 검증 (MEDIUM-1 v4 revert — AnalysisDoc 변경 없음, Phase 6 scope)"
      pattern: "low_keypoint_confidence|occluded_endpoint|insufficient_frames|asymmetric_landmark_count|pose_too_inverted"
    - from: "backend/tests/fixtures/body_normalization/*.json"
      to: "backend/tests/fixtures/body_normalization/_factory.py::pose_frame_from_dict"
      via: "test factory helper (PoseFrame.from_dict 부재 우회 — HIGH-2 v1→v2)"
      pattern: "pose_frame_from_dict"
    - from: "backend/research/evaluations/extract_smplx_joints_from_video.py"
      to: "backend/research/evaluations/smplx_joints_to_body_profile.py::smplx_joints_to_body_profile"
      via: "joints (np.ndarray (NJ,3) 또는 (T,NJ,3)) → BodyNormalizationProfile. fake β path 폐기 (HIGH-1 v5)"
      pattern: "smplx_joints_to_body_profile\\("
    - from: "backend/research/evaluations/run_body_profile_gap_report.py"
      to: "extract_rtmw_body_profile_keypoints + extract_smplx_joints_from_video + smplx_joints_to_body_profile + compare_body_profile"
      via: "single CLI orchestrator — belle 가 실행할 한 줄 명령 (HIGH-2 v5)"
      pattern: "run_body_profile_gap_report"
---

## v1 → v2 Replan Changelog (preserved — audit history)

본 plan 은 `02-REVIEWS.md` (Codex 1차 리뷰) 의 4 HIGH + 3 MEDIUM + 1 LOW concern 을 fix 한 v2 를 기반으로 한다. v2 → v3 → v4 → v5 에서 일부 결정이 재조정되었지만 **v1 → v2 의 다음 fix 는 그대로 보존된다**:

| Concern | Severity | v1 task | v2 fix (이후 버전에서 유지) | Evidence (orchestrator verified) |
|---|---|---|---|---|
| HIGH-2: Fixture plan 이 존재하지 않는 PoseFrame API 와 잘못된 필드 (`timestamp_seconds` / `Keypoint3D.uncertainty_proxy` 누락 / `PoleAxis.origin` 환상) 참조 | HIGH | v1 Task 2 (fixture) | (a) `PoseFrame.from_dict` 가 코드베이스에 **없음** 확인 → test factory helper `pose_frame_from_dict()` 신설. (b) fixture JSON 스키마: `timestamp_ms: int`, `Keypoint3D.uncertainty_proxy: float = 1 - confidence`, `PoleAxis` 4 필드 (`axis_vector`/`confidence_level`/`source`/`frame_index`), `origin` 박멸. (c) smoke load 테스트 신설 | `backend/shared/python/sunity_shared/analysis/pose_frame.py:228-246` PoseFrame, `:103-116` Keypoint3D, `:167-200` PoleAxis 4 필드 |
| HIGH-3: Import-isolation rule 이 legacy mediapipe code 와 충돌 | HIGH | v1 Task 6 (AST 차단) | AST scan 차단 대상을 narrow: `backend.research` / `research` / `nlf` / `smplx` / `smpl_x` 만. `mediapipe` 차단 제외 + TODO 마커 박제 | `backend/shared/python/sunity_shared/analysis/pose_engines/__init__.py` lazy mediapipe + `pose_engines/mediapipe_engine.py:74-76, 197` live import |
| HIGH-4 (v2 정의): R&D `NotImplementedError` stub 으로 ROADMAP §4 충족 표시 — scaffold ≠ gap report | HIGH | v1 Task 5 (R&D harness) | Task 5 분할: Task 5a (scaffold, executor 가능) + Task 5b (belle Pod 실행, user_setup gate). autonomous=false. ROADMAP §4 closure 는 5b 완료 시에만 | ROADMAP.md success criterion #4 — scaffold 만으론 closure X |
| M1: Synthetic-only tests 가 RTMW 좌표/naming drift 탐지 불가 | MED | v1 Task 2 | 5 synthetic fixture 유지 + Task 2b RTMW sanity skip (Pod 환경에서만 실행) | `backend/research/evaluations/reports/sweep_rtmw_20260603_1409/` 에 keypoint dump 없음 |
| M3: armScale/legScale/estimatedHeightScale semantics 미명세 | MED | v1 Task 1 (contract) | 3-way 박제: "torso-relative proportion heuristic — 절대 키가 아니라 torso 길이 대비 사지 비율. Phase-6 coarse normalizer hint 로만 사용". `estimatedHeightScale = (armScale + legScale + 1.0) / 3` 산식 박제 | M3 |
| L1: Lockstep warning enum 만 grep 검증 → 자동화 부족 | LOW | v1 Task 1 verify | `test_body_normalization_lockstep.py` 확장 — 5 warning enum 문자열을 3 파일에서 자동 검증 | Phase 1 산출 lockstep 파일에 warnings enum 검증 없음 |

## v2 → v3 Replan Changelog (preserved — audit history)

Codex 2차 리뷰 가 v2 의 4 HIGH + 3 MEDIUM + 1 LOW 신규 concern 을 보고. v3 fix 중 **HIGH-1 (사이드카) 와 MEDIUM-2 (Firestore lockstep) 는 v4 에서 reverted** (아래 v3→v4 changelog 참조). 나머지는 보존:

| Concern (v2 → v3) | Severity | Affected v2 task | v3 fix (현재 status) | Evidence (orchestrator verified) |
|---|---|---|---|---|
| HIGH-1 (v3): tuple-return propagation 이 B8 fix 박제 위반 | HIGH | Task 4 (전체 재작성) | **v4 에서 폐기** — 사이드카 → race-safe local-return helper 로 교체 | `backend/tests/test_pipeline_recognizer_switch.py:225-238` |
| HIGH-2 (v3): `_is_inverted_frame` 부호가 RTMW image y-down 에서 반전 | HIGH | Task 2 (fixture), Task 3 (measurer) | **v5 에서 superseded** — image-y-order 직접 비교 (MEDIUM-3 v5). pole_axis 부호 독립. v3 의 y-down convention 박제 정신은 유지 | `backend/.../adapters/rtmw_133_to_coco17.py:184-189` |
| HIGH-3 (v3): NotImplementedError raise 가 5b landing 시 회귀 | HIGH | Task 5a (smoke), Task 5b (Pod 실행) | **유지** — graceful-empty 패턴 (반환형 v5 에서 `dict[str, BodyNormalizationProfile]` 유지) | v2 plan Task 5a action 5번째 bullet |
| HIGH-4 (v3): 5b 가 RTMW keypoint dump 부재로 실행 불가 | HIGH | Task 5a (helper 추가), Task 5b (pre-step 박제) | **유지** — extract_rtmw_body_profile_keypoints.py 신설 + v5 에서 run_body_profile_gap_report.py 가 내부 호출 | `ls .../sweep_rtmw_20260603_1409/` keypoint 없음 |
| MEDIUM-1 (v3): AST 스캔이 sunity_shared 만 | MED | Task 6 | **유지 + LOW-2 v4 확장** — 3 디렉터리 + importlib + `__import__` + `spec_from_file_location` 모두 차단 | `pipeline/app.py:454-461`, `runpod_inference/server.py` |
| MEDIUM-2 (v3): contract.md AnalysisDoc 갱신 누락 | MED | Task 4 (4-way lockstep) | **v4 에서 폐기 (revert)** — Firestore 저장은 Phase 6 scope. AnalysisDoc 변경 0 | `docs/contract.md:82-97` — Phase 6 가 박제할 곳 |
| MEDIUM-3 (v3): Task 6 가 Task 5b 뒤로 묶임 | MED | Task 6 depends-on | **유지** — Task 6 의 depends-on 은 Task 5a 만. Task 5b 와 평행 | v2 plan 의존 chain |
| LOW-1 (v3): `cd backend && python -m backend.research...` 가 module resolution 실패 | LOW | Task 5a verify, Task 5b dashboard | **유지** — 모든 `python -m` 은 repo root 실행. pytest 는 `cd backend && pytest` 유지 | `backend/research/evaluations/compare_engines.py:28-34` |

## v3 → v4 Replan Changelog (preserved — audit history)

Codex 3차 리뷰 (02-REVIEWS.md plan_revision: v3) 의 신규 concern 을 fix 한 v4. v5 에서 추가 갱신:

| Concern (v3 → v4) | Severity | v3 mechanism | v4 fix (현재 status) | File/line evidence (orchestrator verified) |
|---|---|---|---|---|
| **HIGH-1 (v4)**: `_RTMWNlfCompat.last_body_profile` 사이드카 mutable state 가 RunPod FastAPI BackgroundTasks 환경에서 race / leak 가능 | HIGH | `last_body_profile` instance attribute | **유지 v5** — 사이드카 폐기 → race-safe local-return helper. `estimate_with_profile(frames) -> tuple[np.ndarray, BodyNormalizationProfile \| None]` + `_angles_and_body_profile_from_video(bucket, key)` helper. B8 박제 무변경. concurrency barrier + stale-failure 테스트. | `backend/functions/pipeline/app.py:110-115` + `backend/runpod_inference/server.py:39, 198, 213` BackgroundTasks |
| **HIGH-2 (v4)**: Task 5b 가 belle 에게 NLF→SMPL-X β loader 실 구현 + ... 요구 → 설계/구현 숨김 + 반환형 mismatch | HIGH | Task 5a scaffold + Task 5b 가 본체 구현 | **v5 에서 superseded** — `beta_to_body_profile(beta)` 박제 폐기 (HIGH-1 v5 weights-free fake 문제). 대신 `smplx_joints_to_body_profile(joints)` + 별도 Pod-only `extract_smplx_joints_from_video.py` + 단일 orchestrator `run_body_profile_gap_report.py` (HIGH-2 v5) | v3 Task 5b how-to-verify Step (b) |
| **MEDIUM-1 (v4)**: Firestore 저장 + AnalysisDoc lockstep 이 CONTEXT.md §1 / §8 v1 scope 박제 위반 | MED | Task 4 4-way lockstep | **유지 v5** — Firestore + AnalysisDoc 변경 0. v5 MEDIUM-1 에서 ROADMAP success criterion 2/4 wording 만 추가로 갱신해 "Phase 2 closure vs Phase 6 closure" 경계 명확화 | CONTEXT.md §1 + §8. RESEARCH.md "Pipeline Integration Plan" Firestore 권장 X |
| **MEDIUM-2 (v4)**: 실 RTMW network output coverage v1.5 deferred | MED | Task 2b skip-if-no-rtmw | **유지 v5 reaffirm** — Codex v5 review 가 "honest" 로 인정. v1.5 deferral 문구 유지 | ROADMAP.md:113 |
| **MEDIUM-3 (v4)**: `estimatedHeightScale` 필드명이 absolute height 처럼 보임 | MED | Task 1 3-way docstring lockstep | **유지 + v5 확장** — Task 7 consumer semantic guard lint test. v5 에서 스캔 범위 4 dir (sunity_shared + backend/functions + backend/runpod_inference + app/src TS) 로 확장 (LOW-1 v5) | `backend/shared/python/sunity_shared/analysis/body_normalization.py:30-82` |
| **LOW-1 (v4)**: "AST lockstep" 호칭 mismatch | LOW | Task 1 string/regex 호칭 | **유지** — Task 6 만 진짜 AST | v3 plan Task 1 |
| **LOW-2 (v4)**: AST scanner 가 importlib literal 만 catch | LOW | Task 6 scanner | **유지** — `__import__(literal)` + `spec_from_file_location(name, literal_path)` 모두 추가 catch. 비-literal residual TODO | `backend/runpod_inference/server.py:31, 73` legitimate spec_from_file_location 사용 |

## v4 → v5 Replan Changelog (THIS revision)

Codex 4차 리뷰 (02-REVIEWS.md plan_revision: v4, reviewed_at: 2026-06-07T13:12:33Z) 의 신규 concern 을 fix. v4 core 아키텍처 (race-safe helper + Firestore revert + R&D 격리) 는 무변경. 변경은 R&D 과학적 정합성 (HIGH-1) / belle 실행 ergonomics (HIGH-2) / 데이터 계약 robustness (MEDIUM-2) / inversion 부호 독립성 (MEDIUM-3) / scan + verify scope (LOW-1/2) 에 집중:

| Concern (v4 → v5) | Severity | v4 mechanism | v5 fix | File/line evidence (orchestrator verified) |
|---|---|---|---|---|
| **HIGH-1 (v5)**: `beta_to_body_profile(beta) -> BodyNormalizationProfile` (pure NumPy, weights-free) 가 과학적으로 무의미 — SMPL-X β 는 shapedirs + joint regressor + template mesh 없이 body geometry 산출 불가. CI 는 통과해도 ROADMAP §4 "NLF→SMPL-X β BodyNormalizationProfile 갭" 가 fake 로 박제됨 | HIGH | Task 5a `nlf_smplx_body_profile_loader.py` 의 `beta_to_body_profile(beta)` pure-NumPy 변환 | **β-only 변환 폐기 → joints 기반 변환으로 교체**: (1) 신규 모듈 `backend/research/evaluations/smplx_joints_to_body_profile.py` — `smplx_joints_to_body_profile(joints: np.ndarray) -> BodyNormalizationProfile` (joints shape `(NJ, 3)` 또는 `(T, NJ, 3)`). pure NumPy: segment-pair distance (SMPL-X joint mapping 기반) → torso-relative ratios → 7 필드. RTMW measurer 와 동일 math (입력만 SMPL-X joints). CI 단위 테스트: 합성 직립 joint array (e.g., 일정한 hip-shoulder-elbow-wrist 거리) → 알려진 ratio 검증. (2) `load_smplx_joints(path: Path) -> dict[str, np.ndarray]` — Pod 산출 joint dump JSON load. (3) 신규 Pod-only 스크립트 `backend/research/evaluations/extract_smplx_joints_from_video.py` — NLF → SMPL-X β fit → SMPL-X model render → joints `(T, NJ, 3)` JSON 산출. SMPL-X model weights (PS:License 1.0) 필요. CI 환경 weights 없으면 graceful exit. (4) 기존 `nlf_smplx_body_profile_loader.py` 의 `beta_to_body_profile` 영구 폐기. compare_body_profile.py 의 `load_nlf_smplx_profiles` 는 joint dump path 를 받아 `load_smplx_joints` → `smplx_joints_to_body_profile` 체인으로 dict[video, Profile] 산출. (5) ROADMAP §4 acceptance text 갱신 (Task 6) — "NLF→SMPL-X β fitting + β→joints rendering + joints→Profile 변환 + RTMW segment 측정과의 갭 보고서. β-only pure-math 변환은 fake 이므로 ROADMAP §4 만족 불가 — 실 joint 산출이 closure 조건" 명시. (6) Smoke test 갱신: 합성 직립 joint → 알려진 ratio assert + nonzero variation. β-pure NumPy 테스트 폐기. | SMPL-X β 는 numpy-only 로 body geometry 산출 불가 (scipy 도 아님; SMPL-X model 파일 자체 필요). v4 plan Task 5a "(pure NumPy, weights 무관, CI 가능)" 문구가 과학적으로 무의미. |
| **HIGH-2 (v5)**: Task 5b 의 4-command console workflow ([[user-beginner-stepwise]] 위반) — belle 가 extract → loader → compare → commit 4단계 수동 실행 | HIGH | Task 5b dashboard_config 4 step | **단일 audited orchestrator 로 축약**: (1) 신규 모듈 `backend/research/evaluations/run_body_profile_gap_report.py` — Task 5a 가 박제. CLI: `python -m backend.research.evaluations.run_body_profile_gap_report --videos ref-foxtop ref-foxtop-split ref-invert ref-sideway-spin ref-climb --date YYYYMMDD`. 내부 phase: (a) extract RTMW keypoints (extract_rtmw_body_profile_keypoints 호출), (b) extract SMPL-X joints (extract_smplx_joints_from_video subprocess 호출 — Pod env 만), (c) load_smplx_joints + smplx_joints_to_body_profile 변환, (d) compare_body_profile.compute_profile_gap → JSON + MD 보고서 산출 `reports/body_profile_gap_<date>.json/.md`. stdout 에 각 phase 진행 로깅. 실패 시 actionable Korean error (e.g., "RUNPOD_AUTH_TOKEN 미설정"). exit 0 / non-zero. (2) Task 5b dashboard_config 정확히 두 줄: `python -m backend.research.evaluations.run_body_profile_gap_report --videos ... --date ...` + `git add ... && git commit ...`. 중간 결정 0. (3) must_haves.truths 에 "Task 5b 2 commands 축약" 박제. | [[user-beginner-stepwise]] memory. v4 Task 5b dashboard_config 4 단계 (Step a/b/c/d). |
| **MEDIUM-1 (v5)**: ROADMAP success criterion 2 ("BodyNormalizationProfile이 ... 산출된다") 가 "product path outputs it now" 로 오독 가능 — Phase 2 는 measurer + helper 만 박제, _process / Firestore 통합은 Phase 6 책임 | MED | Phase 2 v1 scope (MEDIUM-1 v4) | **ROADMAP wording 박제**: (Task 6 commit 에 포함) ROADMAP Phase 2 success criterion 2 acceptance text 갱신: 기존 "BodyNormalizationProfile이 ... 산출된다" → "측정기 함수 `measure_body_profile` + helper `_angles_and_body_profile_from_video` 가 박제되고 단위 테스트가 BodyNormalizationProfile 7필드 산출을 검증한다. _process / Firestore 통합은 Phase 6 plan 책임 — production analysis document (Firestore AnalysisDoc) 가 bodyNormalizationProfile 을 포함하는 시점이 Phase 6 closure 조건". 성공 기준 4 acceptance text 도 갱신: "NLF→SMPL-X β fitting + β→joints rendering + joints→Profile 변환 + RTMW segment 측정과의 갭 보고서 산출. β-only pure-math 변환은 fake (HIGH-1 v5) 이므로 closure 불충족 — 실 joint 산출이 closure 조건". plan-level `<verification>` 에 `closure_boundary` 노트 추가. | v4 plan ROADMAP §2 wording 가 product-path 출력으로 오독 가능. CONTEXT.md §1 + §8. |
| **MEDIUM-2 (v5)**: `BodyNormalizationProfile.__post_init__` (body_normalization.py:64-81) 가 confidence 범위 + warnings 타입만 검증. 5개 numeric scale 필드 (estimated_height_scale/arm_scale/leg_scale/torso_scale/shoulder_hip_ratio) 가 NaN/inf/0/negative 허용 → contract 경계 robustness 결핍 | MED | measurer 가 NaN 회피 의존 | **`__post_init__` 확장 (Task 1 commit 에 포함)**: 5 필드 모두 (a) `math.isfinite(v)` (NaN/+inf/-inf 거절), (b) `v > 0` (strictly positive — 0 거절. 신체 segment 비율은 본질적으로 양수). 위반 시 `ValueError(f"<field> must be finite and strictly positive, got {v}")`. body_normalization.py 코드 변경 (Task 1 의 3-way 박제 + validator 확장 = 단일 atomic commit). `backend/tests/test_body_normalization_profile.py` 갱신: 5 필드 × {NaN, +inf, -inf, 0.0, -1.0} = 25 case → ValueError 검증 + 정상 값 통과 case. Task 3 measurer 의 fallback path 가 0.0 대신 1.0 emit (validator 통과 보장). | `backend/shared/python/sunity_shared/analysis/body_normalization.py:64-81` 현재 `__post_init__` confidence + warnings 만 검증. v4 plan Task 3 fallback "scale=1.0" 박제는 이미 strictly positive 정합 — 추가 변경 minimal. |
| **MEDIUM-3 (v5)**: `pose_too_inverted` 가 `PoleAxis.axis_vector` 부호 의존. `pole/detector.py:186-193` 가 production path 에서 y+ 정규화 (canonical) 하지만 본질적으로 fragile — 부호 canonicalization 깨지면 warning 임의 flip. | MED | dot(mid_hip - mid_shoulder, pole_axis.axis_vector) < 0 → inverted (HIGH-2 v3) | **image-y-order 직접 비교 (Option A)**: pole_axis 의존 폐기. y-down image coords 에서 직립 iff `mid_shoulder.y < mid_hip.y` (어깨가 엉덩이보다 위쪽 픽셀 = y 가 작음). 인버트 iff `mid_shoulder.y > mid_hip.y`. 공식: `mean(shoulder_y) > mean(hip_y)` 인 frame ratio > INVERTED_FRAME_RATIO. `_is_inverted_frame(frame)` 가 pole_axis 인자 폐기 — keypoints_3d 의 left_shoulder/right_shoulder/left_hip/right_hip 의 y 만 사용. Task 2 fixture: y 좌표만 swap (axis_vector 무관). Task 3 measurer 갱신. 신규 단위 테스트: `test_inversion_independent_of_pole_axis_sign` — `axis_vector=(0,1,0)` 와 `axis_vector=(0,-1,0)` 두 fixture 산출 결과 동일 verdict. | `backend/shared/python/sunity_shared/analysis/pose_frame.py:166-200` PoleAxis (axis_vector 정의). `pole/detector.py:186-193` y+ canonicalization production code — 외부 변경 시 깨질 risk. Codex v5 review concern. |
| **LOW-1 (v5)**: `estimatedHeightScale` consumer semantic guard 가 sunity_shared 만 스캔. 미래 consumer (backend/functions, backend/runpod_inference, app/src TS) 누락 | LOW | Task 7 1 dir scan | **4 dir 확장**: backend/shared/python/sunity_shared/**/*.py + backend/functions/**/*.py + backend/runpod_inference/**/*.py + app/src/**/*.ts(x). 제외: backend/research/ (R&D throwaway) + backend/tests/ + app/scripts/. Python 측은 "torso-relative proportion heuristic" substring 검사. TS 측은 `estimatedHeightScale` identifier 등장 line 의 인접 (±3 라인) 에 "torso-relative" 또는 "torso-relative proportion heuristic" 주석/JSDoc substring 검사. Phase 2 시점 4 dir 모두 consumer 0 → vacuous PASS. | v4 Task 7 scan root `Path("backend/shared/python/sunity_shared")` 만. |
| **LOW-2 (v5)**: `grep -c "bodyNormalizationProfile" file -> N` 류 verify block 이 fragile — line 위치/주변 context 검증 못 함. AnalysisDoc 블록 안 vs 다른 곳 구별 X | LOW | v4 verify block `grep -c "..." file → N` | **scope-anchored 검증**: AnalysisDoc interface 안 `bodyNormalizationProfile?` 부재 검증 + BodyNormalizationProfile interface 존재 검증을 awk block-extraction (`awk '/interface AnalysisDoc \{/,/^\}/' analysis.ts \| grep -c bodyNormalizationProfile`) 또는 python snippet 으로 교체. Task 1/4/6 의 brittle grep 모두 scope-anchored 로 갱신. | v4 plan Task 4 verify `grep -c "bodyNormalizationProfile" app/src/types/analysis.ts → 1`. |

v5 plan 의 v4 → v5 변경 summary:

- **Task 1**: MEDIUM-2 v5 박제 — `body_normalization.py` `__post_init__` validator 확장 (5 numeric finite + strictly positive). 3-way 박제 + validator 확장 = 단일 atomic commit. `test_body_normalization_profile.py` 25 case 갱신. LOW-2 v5 박제 — verify scope-anchored.
- **Task 2**: MEDIUM-3 v5 박제 — inverted fixture 가 image-y-order 만 사용. axis_vector 무관. RTMW adapter 경유 fixture 유지 하되 inversion verdict 는 y-order 직접.
- **Task 3**: MEDIUM-3 v5 박제 — `_is_inverted_frame` 가 pole_axis 인자 폐기, y-order 직접. 신규 테스트 `test_inversion_independent_of_pole_axis_sign`. MEDIUM-2 v5 박제 — fallback path 가 scale=1.0 (validator 통과). 박제된 `_is_inverted_frame` docstring 도 "image y-order, pole_axis-sign-independent" 로 갱신.
- **Task 4**: 변경 0 (HIGH-1 v4 race-safe 아키텍처 유지).
- **Task 5a**: HIGH-1 v5 박제 — `beta_to_body_profile` 영구 폐기. 신규 모듈 `smplx_joints_to_body_profile.py` + `extract_smplx_joints_from_video.py` + `run_body_profile_gap_report.py`. `nlf_smplx_body_profile_loader.py` (v4 박제) 폐기. compare_body_profile.py 의 `load_nlf_smplx_profiles` 가 joints dump dir → joints → Profile 체인. HIGH-2 v5 박제 — run_body_profile_gap_report.py orchestrator 가 belle 의 한 줄 명령.
- **Task 5b**: HIGH-2 v5 박제 — execution two-line. dashboard_config = `run_body_profile_gap_report` 한 줄 + `git commit` 한 줄.
- **Task 6**: MEDIUM-1 v5 박제 — ROADMAP success criterion 2/4 acceptance text 갱신 (Phase 2 closure vs Phase 6 closure 경계 명시). LOW-2 v5 박제 — verify scope-anchored. v4 의 §1 v1.5 deferral 유지.
- **Task 7**: LOW-1 v5 박제 — 4 dir scan (sunity_shared + backend/functions + backend/runpod_inference + app/src TS). TS 측 scan 추가.

<objective>
RTMW 133 wholebody 키포인트로부터 신체 segment 길이·비율·confidence·warnings 를 자동 산출하는 `measure_body_profile()` 측정기를 박제하고, `_RTMWNlfCompat.estimate_with_profile()` + 신규 helper `_angles_and_body_profile_from_video()` 의 **race-safe local-return tuple** 을 통해 video-level `BodyNormalizationProfile` 이 caller 가 사이드카 mutable state 없이 받을 수 있도록 한다. **B8 fix 시그너처 박제** (`_angles_from_video`, `_angles_and_video_path_from_video`, `_RTMWNlfCompat.estimate`) 무변경. **Firestore 저장 + AnalysisDoc 박제는 Phase 6 plan 책임** — CONTEXT.md §1 + §8 정합. ROADMAP success criterion 2/4 acceptance text 가 "Phase 2 closure (measurer + helper ready) vs Phase 6 closure (production analysis document 가 profile 포함)" 경계를 명시한다 (MEDIUM-1 v5). 

R&D 비교군 (NLF→SMPL-X) 의 path 는 **HIGH-1 v5 박제** 로 weights-free fake `beta_to_body_profile(beta)` 를 영구 폐기 — 대신 `smplx_joints_to_body_profile(joints)` (pure NumPy, CI 합성 joints 로 단위 테스트 가능) + Pod-only `extract_smplx_joints_from_video.py` (NLF → SMPL-X β fit → SMPL-X model render → joints `(T, NJ, 3)` JSON) + 단일 audited orchestrator `run_body_profile_gap_report.py` 로 분리. belle 의 Pod 실행은 **HIGH-2 v5 박제** 로 두 줄 명령 (한 줄 orchestrator + 한 줄 git commit) — [[user-beginner-stepwise]] 정합. 

`BodyNormalizationProfile.__post_init__` validator 가 5 numeric scale 필드 finite + strictly positive 를 contract 경계에서 강제 (MEDIUM-2 v5). `pose_too_inverted` 판정은 image y-order 직접 비교 (mean(shoulder.y) vs mean(hip.y) in y-down image coords) 로 `PoleAxis.axis_vector` 부호와 독립적 (MEDIUM-3 v5). estimatedHeightScale consumer semantic guard 는 4 dir (sunity_shared + backend/functions + backend/runpod_inference + app/src TS) 스캔 (LOW-1 v5). AnalysisDoc 필드 부재 검증은 awk block-extraction 으로 scope-anchored (LOW-2 v5). AST import 차단 테스트 (sunity_shared + backend/functions + backend/runpod_inference + importlib/__import__/spec_from_file_location literal 3종) 로 운영 코드 leak 영구 방지.

Per D-02-01, D-02-02, D-02-03, D-02-04, D-02-05, D-02-06.

Purpose: Phase 6 (체형 정규화 비교) / Phase 7 (차이 분류) / Phase 8·9 (힘 패턴) 가 공유하는 입력 schema 를 채운다. **v1 scope = 측정기 본체 + race-safe helper + measurer math + R&D harness** (CONTEXT.md §1·§8 정합). Phase 6 plan 이 본 phase 의 helper 를 호출하여 Firestore wire-up.

Output (v5 갱신):
- `body_normalization_measurer.py` 신설 (순수 numpy, scipy 의존 0) — MEDIUM-3 v5 image-y-order inversion + MEDIUM-2 v5 fallback emits 1.0
- `body_normalization.py` __post_init__ validator 확장 — 5 numeric finite + strictly positive (MEDIUM-2 v5)
- 5 synthetic fixture + `_factory.py` test helper + smoke load + 단위 테스트
- `_RTMWNlfCompat.estimate_with_profile()` + `_angles_and_body_profile_from_video()` (HIGH-1 v4 race-safe 유지)
- 3-way lockstep (Task 1): contract.md §7 warnings + analysis.ts BodyNormalizationProfile + body_normalization.py docstring (Phase 2 v1 scope — AnalysisDoc 변경 X, MEDIUM-1 v4 유지)
- `compare_body_profile.py` R&D gap 보고서 CLI (HIGH-2 v5 joints-side path)
- **신규** `smplx_joints_to_body_profile.py` joints → Profile 변환 (HIGH-1 v5 핵심)
- **신규** `extract_smplx_joints_from_video.py` Pod-only NLF→SMPL-X β→joints (HIGH-1 v5)
- **신규** `run_body_profile_gap_report.py` 단일 audited orchestrator (HIGH-2 v5 핵심)
- `extract_rtmw_body_profile_keypoints.py` RTMW extraction (HIGH-4 v3 유지)
- belle Pod 실행 게이트 (Task 5b — 두 줄 명령, HIGH-2 v5)
- AST import isolation 확장 (MEDIUM-1 v3 + LOW-2 v4 유지)
- estimatedHeightScale consumer semantic guard 4 dir scan (LOW-1 v5)
- string/regex lockstep 테스트 (LOW-1 v4 호칭 유지)
- REQUIREMENTS.md BODY-01 "MediaPipe" → "RTMW" + ROADMAP.md success criterion 1 v1.5 deferral (MEDIUM-2 v4 유지) + success criterion 2/4 Phase 2 vs Phase 6 closure 경계 (MEDIUM-1 v5)
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-bodynormalizationprofile-rtmw-segment/02-CONTEXT.md
@.planning/phases/02-bodynormalizationprofile-rtmw-segment/02-RESEARCH.md
@.planning/phases/02-bodynormalizationprofile-rtmw-segment/02-REVIEWS.md
@docs/contract.md
@backend/shared/python/sunity_shared/analysis/body_normalization.py
@backend/shared/python/sunity_shared/analysis/pose_frame.py
@backend/shared/python/sunity_shared/analysis/temporal.py
@backend/shared/python/sunity_shared/analysis/pose/detector.py
@backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/wholebody_keypoints.py
@backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/rtmw_engine.py
@backend/shared/python/sunity_shared/analysis/adapters/rtmw_133_to_coco17.py
@backend/functions/pipeline/app.py
@backend/runpod_inference/server.py
@backend/tests/test_pipeline_recognizer_switch.py
@app/src/types/analysis.ts
@backend/tests/test_body_normalization_lockstep.py
@backend/tests/test_body_normalization_profile.py
@backend/research/evaluations/compare_engines.py
@backend/research/evaluations/compare_rtmw_vs_ipsf.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: contract.md §7 warnings enum 5종 + estimatedHeightScale 의미 + BodyNormalizationProfile.__post_init__ 5 numeric validator 확장 (3-way lockstep + validator + string/regex 자동 검증, 단일 atomic commit) — MEDIUM-2 v5</name>
  <read_first>
    backend/shared/python/sunity_shared/analysis/body_normalization.py (현 __post_init__ lines 64-81 — confidence + warnings 만 검증, 5 numeric scale 미검증),
    app/src/types/analysis.ts (lines 364-388 — 현 BodyNormalizationProfile interface + 필드 주석),
    docs/contract.md (§7 — 현 BodyNormalizationProfile 명세, line 342 warnings),
    backend/tests/test_body_normalization_lockstep.py (확장 대상 — 현 5 함수),
    backend/tests/test_body_normalization_profile.py (확장 대상 — validator 단위)
  </read_first>
  <action>
    Goal: D-02-02 lockstep 정신 정합 — `warnings` 필드의 허용 enum 5종 + scale 필드 의미 (M3) 를 3 파일에 동시 박제. MEDIUM-2 v5 박제 — `BodyNormalizationProfile.__post_init__` 가 5 numeric scale 필드 finite + strictly positive 강제. lockstep 테스트는 string/regex 자동 검증 (LOW-1 v4 호칭 유지).

    **v5 핵심 변경 (MEDIUM-2 v5)**: 현재 `__post_init__` 가 `confidence` 범위 + `warnings` 타입만 검증. v5 부터 `estimated_height_scale` / `arm_scale` / `leg_scale` / `torso_scale` / `shoulder_hip_ratio` 5 필드 모두 (a) `math.isfinite(v)` (NaN/+inf/-inf 거절), (b) `v > 0` (strictly positive — 0 거절, 신체 segment 비율은 본질적으로 양수) 강제. 위반 시 `ValueError`. body_normalization.py 의 코드 변경은 Task 1 의 3-way lockstep와 **단일 atomic commit** 으로 묶음 (같은 Python 파일 안에서 docstring + validator 동시 수정).

    **v4 유지 (MEDIUM-1 v4 revert)**: 본 task 의 lockstep 범위는 §7 BodyNormalizationProfile 만. AnalysisDoc 의 `bodyNormalizationProfile?` optional 필드 박제는 Phase 6 plan 책임.

    **LOW-1 v4 유지**: lockstep 테스트는 string/regex 검증 — 진짜 AST 아님. 테스트 함수명 무변경.

    **LOW-2 v5 박제**: brittle `grep -c` 대신 scope-anchored 검증.

    구현 노트:

    1. **5종 warning enum**: `low_keypoint_confidence`, `occluded_endpoint`, `insufficient_frames`, `asymmetric_landmark_count`, `pose_too_inverted`. 3 파일 동일 문자열.

    2. **docs/contract.md §7 갱신 (line 342 근처)**:
       - 현 placeholder `"측정 품질 이슈 (예: 'short_arm_clip', 'occluded_torso'). 기본값 []."` 제거.
       - 5 enum + 각 의미 1줄.
       - **M3**: `estimatedHeightScale` / `armScale` / `legScale` 설명에 "torso-relative proportion heuristic — 절대 키가 아니라 torso 길이 대비 사지 비율. Phase 6 coarse normalizer hint 로만 사용." + `estimatedHeightScale = (armScale + legScale + 1.0) / 3` 산식.
       - **MEDIUM-2 v5**: 5 numeric 필드 (`estimatedHeightScale`/`armScale`/`legScale`/`torsoScale`/`shoulderHipRatio`) 설명에 "**Contract: finite (no NaN/inf) + strictly positive (> 0). 위반 시 dataclass `__post_init__` ValueError (MEDIUM-2 v5).**" 1줄.

    3. **app/src/types/analysis.ts BodyNormalizationProfile interface (lines 373-388)**:
       - `warnings: string[]` JSDoc 5 enum 매핑 + "Phase 11 coach_writer 가 한국어 카피로 번역" 노트.
       - `estimatedHeightScale` / `armScale` / `legScale` JSDoc 에 "torso-relative proportion heuristic" 박제.
       - **MEDIUM-2 v5**: 5 numeric 필드 JSDoc 에 `* Contract: finite + strictly positive (>0). Python __post_init__ ValueError on violation (MEDIUM-2 v5).` 박제.
       - **MEDIUM-1 v4 박제 유지**: `AnalysisDoc` interface 변경 X — `bodyNormalizationProfile?` 추가 X.

    4. **backend/shared/python/sunity_shared/analysis/body_normalization.py**:
       - 클래스 docstring 의 `warnings 의미` 블록을 5 enum + 의미로 교체.
       - `estimated_height_scale` / `arm_scale` / `leg_scale` 필드 위 코멘트에 "torso-relative proportion heuristic" 박제.
       - **MEDIUM-2 v5 핵심 — `__post_init__` 확장**:
         - 모듈 top: `import math`.
         - 기존 `confidence` 검증 + `warnings` 타입 검증 유지.
         - 신규 5 필드 검증 루프:
           ```
           for field_name in ("estimated_height_scale", "arm_scale", "leg_scale", "torso_scale", "shoulder_hip_ratio"):
               v = getattr(self, field_name)
               if not math.isfinite(v):
                   raise ValueError(f"{field_name} must be finite (no NaN/inf), got {v}")
               if v <= 0.0:
                   raise ValueError(f"{field_name} must be strictly positive (>0), got {v}")
           ```
         - docstring 에 "MEDIUM-2 v5 박제: 5 numeric scale 필드 finite + strictly positive. NaN/inf/0/negative ValueError." 추가.

    5. **L1 — lockstep string/regex 자동 검증 확장 (`backend/tests/test_body_normalization_lockstep.py`)** — LOW-1 v4 호칭 유지:
       - 기존 5 함수 유지.
       - 신설 `test_warnings_enum_lockstep_across_3_files` (docstring: "string/regex 검증 — AST 아님 (LOW-1 v4)").
       - 신설 `test_estimated_height_scale_semantics_lockstep` (docstring: "string/regex 검증 — AST 아님 (LOW-1 v4)").
       - 신설 `test_numeric_scale_contract_lockstep` (MEDIUM-2 v5): 3 파일에서 "finite" + "strictly positive" substring 등장 검증. docs/contract.md / analysis.ts / body_normalization.py 각 ≥1회.
       - substring 검색만 — AST 호칭 폐기.

    6. **validator 단위 (`backend/tests/test_body_normalization_profile.py`)** — MEDIUM-2 v5:
       - 신설 `test_numeric_field_rejects_nan` (5 필드 parametrize): `math.nan` 입력 → ValueError + message 에 필드명.
       - 신설 `test_numeric_field_rejects_positive_inf` (5 필드): `math.inf` → ValueError.
       - 신설 `test_numeric_field_rejects_negative_inf` (5 필드): `-math.inf` → ValueError.
       - 신설 `test_numeric_field_rejects_zero` (5 필드): `0.0` → ValueError.
       - 신설 `test_numeric_field_rejects_negative` (5 필드): `-1.0` → ValueError.
       - 신설 `test_valid_profile_passes_validator`: 정상 값 (1.0 / 1.1 / 1.05 / 1.0 / 0.85 + confidence=0.9 + warnings=[]) → 객체 생성 OK.
       - 총 25 + 1 = 26 case.

    7. **단일 commit 정합**: TS + Python (docstring + validator 확장) + contract.md + 두 test 파일 동시 변경.

    참조: REVIEWS.md v1→v2 HIGH-2 / M3 / L1 + v3→v4 LOW-1 + v4→v5 MEDIUM-2.

    No code in this plan.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_body_normalization_lockstep.py tests/test_body_normalization_profile.py -x -v</automated>
    추가 (warning enum lockstep): `python3 -c "import re,sys; paths=['docs/contract.md','app/src/types/analysis.ts','backend/shared/python/sunity_shared/analysis/body_normalization.py']; ok=all(re.search(r'low_keypoint_confidence', open(p).read()) for p in paths); sys.exit(0 if ok else 1)"`.
    추가 M3: `python3 -c "import sys; paths=['docs/contract.md','app/src/types/analysis.ts','backend/shared/python/sunity_shared/analysis/body_normalization.py']; ok=all('torso-relative proportion heuristic' in open(p).read() for p in paths); sys.exit(0 if ok else 1)"`.
    추가 MEDIUM-2 v5: `python3 -c "import sys; src=open('backend/shared/python/sunity_shared/analysis/body_normalization.py').read(); ok=all(t in src for t in ['math.isfinite','strictly positive','MEDIUM-2 v5']); sys.exit(0 if ok else 1)"`.
    추가 LOW-2 v5 — MEDIUM-1 v4 revert 검증 (scope-anchored): `python3 -c "import sys,re; src=open('app/src/types/analysis.ts').read(); m=re.search(r'export interface AnalysisDoc\s*\{([\s\S]*?)^\}', src, re.M); body=m.group(1) if m else ''; sys.exit(0 if 'bodyNormalizationProfile' not in body else 1)"`.
    추가 LOW-2 v5 — BodyNormalizationProfile interface 존재 검증 (scope-anchored): `python3 -c "import sys,re; src=open('app/src/types/analysis.ts').read(); m=re.search(r'export interface BodyNormalizationProfile\s*\{([\s\S]*?)^\}', src, re.M); sys.exit(0 if (m and 'estimatedHeightScale' in m.group(1)) else 1)"`.
  </verify>
  <acceptance_criteria>
    - 5 warning enum 문자열 3 파일 모두 존재 (lockstep string/regex 테스트 PASS)
    - "torso-relative proportion heuristic" 3 파일 모두 존재
    - "finite" + "strictly positive" 3 파일 모두 박제 (MEDIUM-2 v5)
    - `__post_init__` 5 numeric 필드 finite + strictly positive 검증 (MEDIUM-2 v5)
    - 26 validator 단위 case PASS
    - 기존 5 lockstep 함수 회귀 0
    - **MEDIUM-1 v4 유지**: `AnalysisDoc` 안 `bodyNormalizationProfile` 부재 (scope-anchored awk/regex 검증)
    - **LOW-2 v5**: brittle `grep -c` 폐기, scope-anchored python regex 사용
    - 단일 commit 에 contract.md + analysis.ts + body_normalization.py + 2 test 동시 변경
  </acceptance_criteria>
  <done>위 acceptance_criteria 모두 충족.</done>
  <commit-template>
    feat(02): lock warnings enum + scale semantics + numeric validator for BodyNormalizationProfile (3-way lockstep + finite/positive __post_init__, MEDIUM-2 v5 + LOW-1 v4)
  </commit-template>
  <depends-on>None — Wave 1 진입점</depends-on>
</task>

<task type="auto">
  <name>Task 2: PoseFrame test factory + Synthetic fixture (RTMW adapter, image-y-order inversion) + 5 fixture JSON + smoke load (HIGH-2 v1→v2 + MEDIUM-3 v5)</name>
  <read_first>
    backend/shared/python/sunity_shared/analysis/pose_frame.py (실제 dataclass 시그너처 — PoseFrame / Keypoint3D / PoleAxis 4-field. PoseFrame.from_dict 부재),
    backend/shared/python/sunity_shared/analysis/body_normalization.py,
    backend/shared/python/sunity_shared/analysis/skeleton.py (KEYPOINT_NAMES 17),
    backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/wholebody_keypoints.py (RTMW 133),
    backend/shared/python/sunity_shared/analysis/adapters/rtmw_133_to_coco17.py (convert_rtmw_keypoints_to_coco17_and_pole_ext)
  </read_first>
  <action>
    Goal: test factory helper `pose_frame_from_dict(raw: dict) -> PoseFrame` 신설 + fixture JSON 스키마 실제 dataclass 시그너처 정합 + smoke load. MEDIUM-3 v5 박제 — inverted fixture 가 image-y-order 만 사용 (y 좌표 swap, axis_vector 무관).

    **v5 박제 (MEDIUM-3 v5)**:
    - v3/v4: inverted fixture 는 `convert_rtmw_keypoints_to_coco17_and_pole_ext` adapter 경유 + PoleAxis-relative inversion 박제.
    - v5: inverted fixture 의 inversion 판정은 image y-order 직접 (shoulder.y > hip.y in y-down image coords). adapter 경유는 schema/coord drift 검증 목적으로 유지. PoleAxis.axis_vector 부호는 inversion verdict 와 무관.

    1. **`backend/tests/fixtures/body_normalization/_factory.py` 신설**: `pose_frame_from_dict(raw)` — `frame_index: int`, `timestamp_ms: int` (NOT `timestamp_seconds`), `keypoints_3d` (각 entry `{x,y,z,confidence,uncertainty_proxy=1-confidence}`), `keypoints_3d_pole_aligned`, `raw_keypoints_133`, `pole_axis: PoleAxis` 4 필드, `reliability`, `body_shape: None`. JSON list→tuple 변환. Production `pose_frame.py` 수정 X.

    2. **`_generate.py` 신설 — 멱등 + MEDIUM-3 v5 image-y-order 박제**:
       - `numpy.random.RandomState(seed=42)`. CLI `--check`.
       - **Convention 박제 (MEDIUM-3 v5)**: image y-down. 직립 = `mid_shoulder.y < mid_hip.y` (어깨가 화면 위쪽 = y 작음). 인버트 = `mid_shoulder.y > mid_hip.y` (어깨가 화면 아래쪽). PoleAxis.axis_vector 부호 무관.
       - 5종 fixture 모두 image y-down 좌표 → `convert_rtmw_keypoints_to_coco17_and_pole_ext` 호출 → PoseFrame → `dataclasses.asdict` JSON.
       - **inverted_pose fixture (MEDIUM-3 v5)**: y 좌표만 swap (shoulder.y / hip.y 위치 교환). axis_vector 는 default `(0,1,0)` 유지 — y-order 가 inversion 결정.
       - PoleAxis: `confidence_level='medium'`, `source='vertical_fallback'`, `frame_index=None`. `origin` 박제 금지.
       - 모든 `timestamp_ms: int`.
       - 인체 평균 비율 (image pixel): torso=200, upper_arm=120, forearm=110, thigh=180, shank=160, shoulder=140, hip=100.

    3. **5 fixture JSON**:
       - normal_pose (T=60, conf 0.85-0.95, `mid_shoulder.y < mid_hip.y` ≥80% frame).
       - inverted_pose (T=60, conf 0.6-0.8, `mid_shoulder.y > mid_hip.y` ≥80% frame — MEDIUM-3 v5).
       - occluded_leg (T=60, knee conf<0.3 in 70%).
       - short_clip (T=20).
       - asymmetric_pose (T=60, 좌 conf>0.9 in 90%, 우 conf>0.9 in 50%).
       - 각 파일 <200KB.

    4. **`test_fixtures_loadable.py` smoke**:
       - `test_all_fixtures_load_via_factory` (5 fixture load PASS).
       - `test_polestaxis_no_origin_field` (`origin` 키 0).
       - `test_keypoint3d_has_uncertainty_proxy`.
       - **`test_normal_pose_is_upright_in_y_down_image` (MEDIUM-3 v5)**: normal_pose 80%+ frame `mid_shoulder.y < mid_hip.y`.
       - **`test_inverted_pose_is_inverted_in_y_down_image` (MEDIUM-3 v5)**: inverted_pose 80%+ frame `mid_shoulder.y > mid_hip.y`.
       - **`test_inverted_pose_axis_vector_is_default_unchanged` (MEDIUM-3 v5)**: inverted_pose 의 PoleAxis.axis_vector == (0.0, 1.0, 0.0). axis_vector 가 inversion 결정 X 증거.

    5. **MEDIUM-2 v4 → Task 2b 유지**: `test_real_rtmw_sanity.py` `@pytest.mark.skipif(not _rtmw_available())`. CI skip.

    6. **TODO 마커 (MEDIUM-2 v4)**: `_generate.py` 상단 `# TODO(post-pilot, MEDIUM-2 v4): belle Pod Step (a) 산출 keypoint dump landing 후 실 영상 fixture 1종 promote — ROADMAP success criterion 1 v1.5 closure.`

    No code in this plan.
  </action>
  <verify>
    <automated>cd backend && python -m tests.fixtures.body_normalization._generate --check && pytest tests/fixtures/body_normalization/test_fixtures_loadable.py -x -v</automated>
    `ls backend/tests/fixtures/body_normalization/*.json | wc -l` → 5.
    `_generate.py --check` 두번 byte-identical.
    `grep -l "timestamp_seconds" backend/tests/fixtures/body_normalization/ 2>/dev/null` → 0.
    `grep -l '"origin"' backend/tests/fixtures/body_normalization/*.json | wc -l` → 0.
    MEDIUM-3 v5: `python3 -c "import json,sys; d=json.load(open('backend/tests/fixtures/body_normalization/inverted_pose_pose_frames.json')); ax=d[0]['pole_axis']['axis_vector']; sys.exit(0 if tuple(ax)==(0.0,1.0,0.0) else 1)"` (inverted fixture 도 default axis_vector).
    `grep "convert_rtmw_keypoints_to_coco17_and_pole_ext" backend/tests/fixtures/body_normalization/_generate.py` ≥ 1.
  </verify>
  <acceptance_criteria>
    - 5 fixture JSON 존재
    - `pose_frame_from_dict` 가 5 fixture 모두 load (smoke PASS)
    - `_generate.py --check` 멱등성
    - HIGH-2 v1→v2: `timestamp_ms`, `uncertainty_proxy`, PoleAxis 4 필드 (`origin` 0)
    - **MEDIUM-3 v5**: normal_pose y-down upright (`mid_shoulder.y < mid_hip.y`), inverted_pose y-down inverted (`mid_shoulder.y > mid_hip.y`), 두 fixture 모두 default axis_vector `(0,1,0)`
    - 각 fixture JSON <200KB
    - Task 2b RTMW sanity test 존재 (CI skip OK)
  </acceptance_criteria>
  <done>위 acceptance_criteria 모두 충족.</done>
  <commit-template>
    test(02): add PoseFrame factory + synthetic fixtures with image-y-order inversion (5 cases, MEDIUM-3 v5 pole_axis-sign-independent)
  </commit-template>
  <depends-on>None — Task 1 과 병렬. commit 순서는 Task 1 → Task 2 권장.</depends-on>
</task>


<task type="auto">
  <name>Task 3: body_normalization_measurer.py 본체 + 단위 테스트 (MEDIUM-3 v5 image-y-order inversion + MEDIUM-2 v5 fallback emits 1.0)</name>
  <read_first>
    backend/shared/python/sunity_shared/analysis/pose_frame.py,
    backend/shared/python/sunity_shared/analysis/temporal.py (MAD `_column_outliers`, DEFAULT_OUTLIER_K=3.0),
    backend/shared/python/sunity_shared/analysis/body_normalization.py (Task 1 갱신된 `__post_init__` validator),
    backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/wholebody_keypoints.py,
    backend/shared/python/sunity_shared/analysis/adapters/rtmw_133_to_coco17.py,
    backend/tests/fixtures/body_normalization/_factory.py
  </read_first>
  <action>
    Goal: `measure_body_profile(pose_frames, *, conf_gate=0.5, mad_k=3.0, min_valid_frames=5, low_conf_gate=0.4) -> BodyNormalizationProfile` 순수 함수. numpy only. Phase 1 `temporal.py` MAD 패턴 재사용. MEDIUM-3 v5 박제 — `pose_too_inverted` image-y-order 직접. MEDIUM-2 v5 박제 — fallback path 가 scale 필드에 1.0 emit (validator strictly positive 통과).

    **v5 핵심 변경**:
    - MEDIUM-3 v5: `_is_inverted_frame(frame) -> bool | None` 시그너처에서 pole_axis 인자 폐기. y-order 직접 비교.
    - MEDIUM-2 v5: fallback (short clip, NaN, all-occluded) 시 scale 필드 = 1.0 (not 0.0) — `__post_init__` strictly positive 통과 보장. confidence = 0.0.

    1. **상수 (module top)**: `SEGMENT_PAIRS` (10 entry), `TORSO_TOP_KPS=("left_shoulder","right_shoulder")`, `TORSO_BOTTOM_KPS=("left_hip","right_hip")`, `WARNING_CODES` frozenset, 임계값 `CONF_GATE_PER_FRAME=0.5`, `MIN_VALID_FRAMES=5`, `MAX_BAD_FRAME_RATIO=0.6`, `MAD_K=3.0`, `LOW_CONFIDENCE_GATE=0.4`, `INVERTED_FRAME_RATIO=0.5`, `ASYMMETRIC_FRAME_RATIO=0.3`.

    2. **`_measure_segment_per_frame`**: per-frame `sqrt(dx²+dy²)` 만 (z 무시). conf=`min(start.confidence, end.confidence)`. 미감지→NaN.

    3. **`_measure_torso`**: `mid_shoulder`/`mid_hip` 평균 → 길이 = `sqrt((dx)² + (dy)²)`. conf = min of 4 endpoint.

    4. **`_robust_median`**: MAD k=3 + median + `(kept_ratio × mean_conf)`. valid < MIN_VALID_FRAMES → `(nan, 0.0)`.

    5. **MEDIUM-3 v5 — `_is_inverted_frame(frame) -> bool | None`** (pole_axis 인자 폐기):
       - Convention docstring (필수): "MEDIUM-3 v5: image y-down 픽셀 좌표 직접 비교. y-down image 에서 `mid_shoulder.y < mid_hip.y` 면 직립 (어깨가 화면 위쪽). `mid_shoulder.y > mid_hip.y` 면 인버트. PoleAxis.axis_vector 부호와 독립적 — pole_axis 가 어떤 방향이든 동일 verdict. v3/v4 의 dot(torso_up, axis_vector) 박제 폐기."
       - 4 endpoint (left/right shoulder/hip) 중 하나라도 미감지 → None.
       - `mid_shoulder_y = (left_shoulder.y + right_shoulder.y) / 2`.
       - `mid_hip_y = (left_hip.y + right_hip.y) / 2`.
       - 인버트 판정: `return mid_shoulder_y > mid_hip_y` (y-down, 위쪽 = y 작음).
       - `_check_pose_too_inverted_warning`: True 비율 > `INVERTED_FRAME_RATIO` → warning. None 제외.

    6. **`measure_body_profile`**:
       - SEGMENT_PAIRS + torso 측정 → 좌우 평균 → upper_arm/forearm/thigh/shank median.
       - `arm_scale=(upper_arm+forearm)/torso`, `leg_scale=(thigh+shank)/torso`, `torso_scale=1.0`.
       - `shoulder_hip_ratio = shoulder_width / hip_width`.
       - **M3**: `estimated_height_scale = (arm_scale + leg_scale + 1.0) / 3`.
       - `confidence = mean of 11 segment confs`.
       - **Warnings**: `low_keypoint_confidence` (<gate), `occluded_endpoint` (endpoint conf<0.5 in >60%), `insufficient_frames` (T<30), `asymmetric_landmark_count` (좌우 valid 차이>30%), `pose_too_inverted` (MEDIUM-3 v5 y-order).
       - **MEDIUM-2 v5 NaN/fallback 방어 (T-02-03)**: 
         - torso == NaN, 또는 valid frame < MIN_VALID_FRAMES, 또는 segment 산출 NaN 시 fallback.
         - fallback emit: `estimated_height_scale=1.0`, `arm_scale=1.0`, `leg_scale=1.0`, `torso_scale=1.0`, `shoulder_hip_ratio=1.0`, `confidence=0.0`, `warnings=["insufficient_frames"]` (또는 적절).
         - 이유 박제 (docstring): "MEDIUM-2 v5 박제 — `__post_init__` 가 strictly positive 강제 → fallback 은 0.0 대신 1.0 emit (validator 통과)."
         - 정상 path 도 모든 scale 필드 finite 강제 (NaN → 1.0 substitute + warning push).

    7. **Logging**: `from __future__ import annotations`. stdlib.

    8. **단위 테스트 12 case (`backend/tests/test_body_normalization_measurer.py`)**:
       - `test_normal_pose_profile_valid` (정상 fixture, warnings==[], finite + positive).
       - `test_inverted_pose_warning` (inverted fixture, `pose_too_inverted` ∈ warnings).
       - **`test_inversion_uses_image_y_order_not_pole_axis` (MEDIUM-3 v5)**: normal_pose 의 모든 pole_axis 부호 변형 (+y, -y, +x, -x, +z) → 동일 verdict (warning 미발화).
       - **`test_inversion_independent_of_pole_axis_sign` (MEDIUM-3 v5)**: inverted_pose 의 axis_vector 를 `(0,1,0)` 과 `(0,-1,0)` 두 변형 → 둘 다 동일하게 `pose_too_inverted` warning 발화 (axis_vector flip 이 verdict 변경 X). 본 테스트가 v5 review HIGH 의 핵심 sanity gate.
       - `test_occluded_leg_warning`.
       - `test_short_clip_warning` — short_clip fixture → `insufficient_frames` 발화 + **MEDIUM-2 v5 검증**: 모든 scale 필드 == 1.0, confidence == 0.0, profile 객체 생성 성공 (validator ValueError 0).
       - `test_asymmetric_warning`.
       - `test_z_axis_ignored_when_zero` (z=0 override → arm_scale ±1%).
       - `test_profile_validator_passes_for_normal_fixture` — `__post_init__` 통과.
       - **`test_fallback_scales_are_positive` (MEDIUM-2 v5)**: 합성 all-NaN fixture (T=3, conf=0) → fallback path → 5 scale 필드 모두 >0 + finite.
       - **`test_no_nan_or_inf_in_output_for_any_fixture` (MEDIUM-2 v5)**: 5 fixture 모두 → 모든 numeric 필드 `math.isfinite` + `> 0`.
       - `test_idempotent` (2회 byte-identical).

    No code in this plan.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_body_normalization_measurer.py -x -v</automated>
    `python -c "from sunity_shared.analysis.body_normalization_measurer import measure_body_profile; help(measure_body_profile)"`.
    회귀: `pytest tests/test_body_normalization_profile.py tests/test_body_normalization_lockstep.py -x`.
    `grep -r "import scipy" backend/shared/python/sunity_shared/analysis/body_normalization_measurer.py | wc -l` → 0.
    MEDIUM-3 v5 docstring: `python -c "from sunity_shared.analysis.body_normalization_measurer import _is_inverted_frame; doc=_is_inverted_frame.__doc__ or ''; assert 'image y-down' in doc and 'MEDIUM-3 v5' in doc and 'PoleAxis.axis_vector' in doc"`.
    MEDIUM-3 v5 — pole_axis 인자 부재 검증: `python -c "import inspect; from sunity_shared.analysis.body_normalization_measurer import _is_inverted_frame; sig=inspect.signature(_is_inverted_frame); assert 'pole_axis' not in sig.parameters, f'pole_axis 인자 폐기 위반: {sig}'"`.
    MEDIUM-2 v5 fallback: `python -c "from sunity_shared.analysis.body_normalization_measurer import measure_body_profile; p=measure_body_profile([]); assert p.arm_scale > 0 and p.confidence == 0.0"`.
  </verify>
  <acceptance_criteria>
    - 12개 단위 테스트 PASS
    - 5 fixture → BodyNormalizationProfile (NaN/inf/0 출력 X — validator ValueError 0)
    - 정상 fixture warnings==[]
    - 4 비정상 fixture 각자 정확한 warning
    - **MEDIUM-3 v5**: `_is_inverted_frame` 시그너처 에서 pole_axis 인자 폐기, image y-order 만 사용, axis_vector flip 시 verdict 불변
    - **MEDIUM-2 v5**: fallback path 가 5 scale 필드 모두 1.0 emit, confidence=0.0, validator 통과
    - `_is_inverted_frame` docstring "image y-down" + "MEDIUM-3 v5" 박제
    - scipy import 0
    - Phase 1 기존 테스트 회귀 0
  </acceptance_criteria>
  <done>위 acceptance_criteria 모두 충족.</done>
  <commit-template>
    feat(02): add measure_body_profile with image-y-order inversion + positive-scale fallback (BODY-01, MEDIUM-3 v5 + MEDIUM-2 v5)
  </commit-template>
  <depends-on>Task 1 (warnings enum + validator), Task 2 (5 fixture + helper)</depends-on>
</task>

<task type="auto">
  <name>Task 4: Pipeline 통합 — race-safe local-return helper `_angles_and_body_profile_from_video` + `_RTMWNlfCompat.estimate_with_profile` (HIGH-1 v4 — B8 박제 보존, 사이드카 폐기, Firestore revert per MEDIUM-1 v4 — 변경 0 from v4)</name>
  <read_first>
    backend/functions/pipeline/app.py (lines 110-115 `_POSE_ESTIMATOR = None` 글로벌; lines 200-310 `_RTMWNlfCompat` + `_ensure_adapters` + `_angles_from_video:255` + `_angles_and_video_path_from_video:271`; lines 380-400 `_process` 의 `_angles_from_video` 호출 site),
    backend/runpod_inference/server.py (lines 39, 198, 213 — FastAPI `BackgroundTasks` + `background.add_task` — concurrent analyses 가능 증거),
    backend/shared/python/sunity_shared/analysis/pose_frame.py (`dataclasses.replace(body_shape=profile)` 정합),
    backend/shared/python/sunity_shared/analysis/body_normalization.py,
    backend/tests/test_pipeline_recognizer_switch.py (lines 215-260 TestB8FixSignature)
  </read_first>
  <action>
    Goal (HIGH-1 v4 — v5 변경 0): video-level `BodyNormalizationProfile` 을 race-safe local-return tuple 로 caller 가 받는다. 사이드카 mutable state 0. RunPod FastAPI BackgroundTasks 환경 concurrent `_process` 가 module-level `_POSE_ESTIMATOR` 글로벌 공유해도 profile leak 0.

    **v5 변경 0**: v4 Task 4 그대로. v5 Codex review 도 "race-safe shape is right" 로 확인.

    **MEDIUM-1 v4 박제 (Firestore revert)**: `backend/functions/pipeline/app.py` 1 파일만 변경. `firestore_admin.py` / `analysis.ts` AnalysisDoc / `docs/contract.md` AnalysisDoc 변경 X. Phase 6 plan 책임.

    1. **`_RTMWNlfCompat` 신규 method `estimate_with_profile`** — HIGH-1 v4 local-return:
       - 시그너처: `def estimate_with_profile(self, frames) -> tuple["np.ndarray", "BodyNormalizationProfile | None"]:`.
       - 본체:
         - `from sunity_shared.analysis.body_normalization_measurer import measure_body_profile`.
         - `from sunity_shared.analysis.pose_frame import to_coco17_array`.
         - `pose_frames = self._engine.estimate(frames, self._default_pole)`.
         - `profile = measure_body_profile(pose_frames)`.
         - **명시 코멘트 (필수)**: "HIGH-1 v4 박제: 본 method 는 local-return tuple 반환. 사이드카 mutable instance attribute (v3 의 last_body_profile) 영구 폐기. RunPod FastAPI BackgroundTasks (server.py:198, 213) 환경에서 concurrent analyses 가 _POSE_ESTIMATOR 글로벌 공유해도 profile leak 0 — caller 가 자기 frame 의 profile 만 local tuple 로 받음. B8 박제 (estimate / _angles_from_video / _angles_and_video_path_from_video 시그너처) 무변경."
         - `pose_frames_with_profile = [dataclasses.replace(pf, body_shape=profile) for pf in pose_frames]`.
         - `import dataclasses` module top 확인 (없으면 추가).
         - `coco_array = to_coco17_array(pose_frames_with_profile)`.
         - `return coco_array, profile`.

    2. **`_RTMWNlfCompat.estimate` (기존) — 무변경 (B8 박제)**: 시그너처 `-> np.ndarray` 무변경. **`last_body_profile` attribute 영구 폐기**.

    3. **신규 helper `_angles_and_body_profile_from_video` (HIGH-1 v4 race-safe)**:
       - 시그너처: `def _angles_and_body_profile_from_video(bucket: str, key: str) -> tuple[np.ndarray, "BodyNormalizationProfile | None"]:`.
       - docstring: "HIGH-1 v4 박제: race-safe local-return helper. _POSE_ESTIMATOR.estimate_with_profile 가 local tuple 로 (angles_filled, profile) 반환. 사이드카 mutable state 0. RunPod FastAPI BackgroundTasks 환경 concurrent analyses 가 module-level _POSE_ESTIMATOR 글로벌 공유해도 caller 가 자기 frame 의 profile 만 받음. B8 박제 (_angles_from_video / _angles_and_video_path_from_video) 무변경 — 본 helper 는 그 옆 신설."
       - 본체: `_ensure_adapters()` → `tempfile.NamedTemporaryFile(suffix=".mp4", delete=True)` → `_s3.download_file` → `frames = _FRAME_EXTRACTOR.extract(tmp.name)` → `keypoints, profile = _POSE_ESTIMATOR.estimate_with_profile(frames)` → `angles = compute_joint_angles(keypoints)` → `angles_filled = temporal_fill(angles, joint_uncertainty(keypoints))` → `return angles_filled, profile`.

    4. **`_angles_from_video` (line 255) — 시그너처 + 본체 무변경 (B8)**.

    5. **`_angles_and_video_path_from_video` (line 271) — 시그너처 + 본체 무변경 (B8)**.

    6. **`_process` — 변경 X (Phase 2 v1 scope, MEDIUM-1 v4)**: 기존 호출 그대로. Phase 6 plan 이 `_angles_and_body_profile_from_video` 호출 site 박제.

    7. **`firestore_admin.complete_analysis` — 변경 X (MEDIUM-1 v4)**.

    8. **TS `AnalysisDoc` — 변경 X (MEDIUM-1 v4)**.

    9. **`docs/contract.md` AnalysisDoc 섹션 (82-97) — 변경 X (MEDIUM-1 v4)**.

    10. **통합 테스트 (`backend/tests/test_pipeline_body_profile_injection.py`) — HIGH-1 v4 race-safe (9 case)**:
       - `test_estimate_with_profile_returns_tuple`: fake `_engine` (3 PoseFrame) → `(np.ndarray (3,17,4), BodyNormalizationProfile)`.
       - `test_estimate_signature_unchanged_b8_lock`: `_RTMWNlfCompat.estimate(frames)` 시그너처 `-> np.ndarray`. **사이드카 폐기 검증**: `assert not hasattr(estimator, "last_body_profile")`.
       - `test_helper_returns_angles_and_profile_locally`: `_angles_and_body_profile_from_video(bucket, key)` mock S3 + fake estimator → tuple 반환.
       - **`test_concurrency_no_profile_leak_between_background_tasks` (HIGH-1 v4 핵심)**:
         - `concurrent.futures.ThreadPoolExecutor(max_workers=2)` + `threading.Barrier(2)`.
         - Barriered fake estimator: `estimate_with_profile` 가 `barrier.wait()` → 두 worker 동시 진입.
         - fake estimator frame 마다 unique profile (예: `arm_scale = (id(frames) % 1000 / 1000) + 0.5` — MEDIUM-2 v5 strictly positive 통과).
         - Worker A: frames_a → profile_a (arm_scale 정확). Worker B: frames_b → profile_b (arm_scale 정확).
         - `assert profile_a != profile_b`. Module-level `_POSE_ESTIMATOR` 글로벌 공유에도 leak 0.
       - **`test_stale_profile_not_leaked_on_failure` (HIGH-1 v4 핵심)**:
         - First call success → `profile_first`. Second call: `RuntimeError("simulated GPU OOM")` raise.
         - `with pytest.raises(RuntimeError):` — 예외 전파, 직전 profile 반환 X.
       - `test_b8_signature_lock_preserved`: `inspect.signature(app._angles_from_video)` / `_angles_and_video_path_from_video` / `_angles_and_body_profile_from_video` 시그너처 검증.
       - `test_estimate_injects_body_shape_into_all_frames`: 모든 pose_frame 의 body_shape 동일.
       - `test_short_frames_graceful_degrade`: T=3 → fallback (MEDIUM-2 v5 scale=1.0, confidence=0, `insufficient_frames`), exception 0.
       - `test_to_coco17_array_unaffected`: body_shape 주입 전후 shape (T,17,4) 동일.

    11. **B8 박제 회귀 검증**: verify 에서 `pytest tests/test_pipeline_recognizer_switch.py::TestB8FixSignature -x` 별도 실행.

    No code in this plan.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_pipeline_body_profile_injection.py -x -v && pytest tests/test_pipeline_recognizer_switch.py::TestB8FixSignature -x -v && pytest tests/ -x</automated>
    회귀: 기존 pipeline 테스트 전부 PASS. `TestB8FixSignature` 두 테스트 PASS.
    helper: `grep -n "_angles_and_body_profile_from_video\|estimate_with_profile" backend/functions/pipeline/app.py | wc -l` ≥ 4.
    **사이드카 폐기 (HIGH-1 v4)**: `grep -c "last_body_profile" backend/functions/pipeline/app.py backend/shared/python/sunity_shared/firestore_admin.py app/src/types/analysis.ts docs/contract.md` → 0.
    **MEDIUM-1 v4 revert (LOW-2 v5 scope-anchored)**: `python3 -c "import sys,re; src=open('app/src/types/analysis.ts').read(); m=re.search(r'export interface AnalysisDoc\s*\{([\s\S]*?)^\}', src, re.M); body=m.group(1) if m else ''; sys.exit(0 if 'bodyNormalizationProfile' not in body else 1)"`.
  </verify>
  <acceptance_criteria>
    - 9개 통합 테스트 PASS (HIGH-1 v4 race-safe + concurrency + stale-failure + B8 박제)
    - 기존 `tests/` suite 회귀 0
    - `TestB8FixSignature` 두 테스트 PASS
    - `_RTMWNlfCompat.estimate` 시그너처 무변경 (`-> np.ndarray`)
    - `_RTMWNlfCompat.estimate_with_profile` 신규
    - `_RTMWNlfCompat` 에 `last_body_profile` 박제 0
    - `_angles_and_body_profile_from_video` 신규 helper
    - `_angles_from_video` / `_angles_and_video_path_from_video` 시그너처 + 본체 무변경
    - **concurrency 테스트**: barriered 2 worker → 각자 자기 profile
    - **stale-failure 테스트**: 예외 시 직전 profile leak 0
    - **MEDIUM-1 v4 revert (scope-anchored)**: AnalysisDoc 안 `bodyNormalizationProfile` 부재
    - 단일 commit 에 `pipeline/app.py` 1 파일 (+ test file)
  </acceptance_criteria>
  <done>위 acceptance_criteria 모두 충족.</done>
  <commit-template>
    feat(02): add race-safe _angles_and_body_profile_from_video helper + _RTMWNlfCompat.estimate_with_profile (HIGH-1 v4 — sidecar dropped, B8 lock preserved, Firestore deferred to Phase 6)
  </commit-template>
  <depends-on>Task 3 (measure_body_profile)</depends-on>
</task>


<task type="auto">
  <name>Task 5a: R&D harness 박제 — compare_body_profile + extract_rtmw_body_profile_keypoints + smplx_joints_to_body_profile + extract_smplx_joints_from_video + run_body_profile_gap_report (HIGH-1 v5 joints-based + HIGH-2 v5 single orchestrator + HIGH-3 v3 graceful + HIGH-4 v3 + LOW-1 v3)</name>
  <read_first>
    backend/research/evaluations/compare_engines.py (R&D 패턴 + L-2 "/workspace/SunityMotion"),
    backend/research/evaluations/compare_rtmw_vs_ipsf.py (lines 717-733 `_load_pose_engine("rtmw")`; lines 232-258 `run_rtmw`),
    backend/shared/python/sunity_shared/analysis/body_normalization.py (BodyNormalizationProfile dataclass + MEDIUM-2 v5 validator),
    backend/shared/python/sunity_shared/analysis/body_normalization_measurer.py (Task 3)
  </read_first>
  <action>
    Goal: ROADMAP §4 R&D 비교군 path 의 honest joints-based 박제 + belle 단일 명령 orchestrator. D-02-04.

    **HIGH-1 v5 핵심 박제 (β-only fake 폐기)**:
    - v4: `nlf_smplx_body_profile_loader.py` 의 `beta_to_body_profile(beta)` pure-NumPy 변환 = 과학적 fake (SMPL-X β 는 shapedirs + joint regressor + template mesh 없이 body geometry 산출 불가).
    - v5: 변환 입력을 β → joints 로 교체. 두 모듈로 분리:
      - **신규 `smplx_joints_to_body_profile.py`**: `smplx_joints_to_body_profile(joints) -> BodyNormalizationProfile` (pure NumPy, CI 합성 joints 단위 테스트 가능). 입력은 이미 fitted SMPL-X joint array.
      - **신규 `extract_smplx_joints_from_video.py`**: NLF → SMPL-X β fit → SMPL-X model render → joints `(T, NJ, 3)`. Pod-only (SMPL-X model weights 필요). CI 환경에선 weights 없으면 graceful exit.
    - v4 `nlf_smplx_body_profile_loader.py` 영구 폐기 (파일 자체 미생성).

    **HIGH-2 v5 핵심 박제 (belle 단일 명령)**:
    - **신규 `run_body_profile_gap_report.py`**: 단일 audited orchestrator. 내부 phase = extract RTMW + extract SMPL-X joints (Pod) + joints→profile + gap → JSON/MD. belle 의 한 줄 명령.

    **HIGH-3 v3 유지**: graceful-empty. 비존재/빈 dir → `{}`.
    **HIGH-4 v3 유지**: `extract_rtmw_body_profile_keypoints.py`.
    **LOW-1 v3 유지**: 모든 `python -m` repo root.

    1. **`backend/research/evaluations/smplx_joints_to_body_profile.py` 신설 (HIGH-1 v5 핵심)**:
       - 헤더 docstring: "SMPL-X joints (`(NJ, 3)` 또는 `(T, NJ, 3)` np.ndarray) → BodyNormalizationProfile 변환 (R&D 격리). D-02-04: NLF/SMPL-X import 본 파일에서 X (joints 만 다룸). HIGH-1 v5 박제 (REVIEWS.md plan_revision v4): v4 의 weights-free `beta_to_body_profile(beta)` 는 과학적 fake — SMPL-X β 는 shapedirs + joint regressor + template mesh 없이 body geometry 산출 불가. v5 는 이미 fitted joints 를 입력으로 받아 pure NumPy 변환. CI 단위 테스트 가능 (합성 joints), Pod 실행은 extract_smplx_joints_from_video.py 가 산출한 실 joints 입력."
       - 모듈 top: SMPL-X joint name mapping 상수 박제 (예: `SMPLX_JOINT_INDICES = {"pelvis": 0, "left_hip": 1, "right_hip": 2, "spine1": 3, ..., "left_shoulder": 16, "right_shoulder": 17, "left_elbow": 18, "right_elbow": 19, "left_wrist": 20, "right_wrist": 21, "left_knee": 4, "right_knee": 5, "left_ankle": 7, "right_ankle": 8}` — SMPL-X 표준 NJ=55 의 부분집합, 본 변환에 필요한 joints 만. 실제 인덱스는 SMPL-X spec 박제 — implementor 가 확인).
       - 함수:
         - `smplx_joints_to_body_profile(joints: np.ndarray) -> BodyNormalizationProfile` (HIGH-1 v5 핵심):
           - 입력 shape `(NJ, 3)` (single frame) 또는 `(T, NJ, 3)` (multi-frame). 후자는 axis=0 mean 으로 single-frame 평균 처리.
           - segment 거리 계산: `upper_arm = mean(||shoulder - elbow||)`, `forearm = mean(||elbow - wrist||)`, `thigh = mean(||hip - knee||)`, `shank = mean(||knee - ankle||)`, `torso = mean(||shoulder_mid - hip_mid||)`, `shoulder_width = ||left_shoulder - right_shoulder||`, `hip_width = ||left_hip - right_hip||`.
           - 좌우 평균 (upper_arm 등).
           - `arm_scale = (upper_arm + forearm) / torso`, `leg_scale = (thigh + shank) / torso`, `torso_scale = 1.0`, `shoulder_hip_ratio = shoulder_width / hip_width`, `estimated_height_scale = (arm_scale + leg_scale + 1.0) / 3` (Task 3 measurer 동일 산식).
           - `confidence = 1.0` (deterministic, SMPL-X fitted joints 가정).
           - `warnings = []` (R&D, 정상 영상 가정).
           - MEDIUM-2 v5 정합: 모든 scale 필드 finite + strictly positive 강제. NaN/0/negative 시 fallback (1.0).
           - 반환: `BodyNormalizationProfile(...)`. `__post_init__` validator 통과.
         - `load_smplx_joints(path: Path) -> dict[str, np.ndarray]`:
           - `path` 가 dir 이면 각 `<video>.json` 을 SMPL-X joints array 로 복원 → `dict[video_name, joints]`.
           - HIGH-3 v3 graceful: 비존재 dir / 빈 dir → `{}`.
         - `main()` CLI entry: `--joints-dir <path>` → `load_smplx_joints` → 각 video `smplx_joints_to_body_profile` → 결과 dict json print (debug 용).

    2. **`backend/research/evaluations/extract_smplx_joints_from_video.py` 신설 (HIGH-1 v5)**:
       - 헤더 docstring: "Pod-only NLF→SMPL-X β fit → SMPL-X model render → joints `(T, NJ, 3)` JSON 산출. D-02-04 박제: NLF/SMPL-X import 본 파일에서만 — sunity_shared 무관. PS:License 1.0 비상업. HIGH-1 v5 박제 (REVIEWS.md plan_revision v4): joints 산출이 closure 조건 — β-only pure-math 변환 fake 폐기. CI 환경에선 model weights 없으면 graceful exit (exit 0 + 'SMPL-X weights 미설정' Korean log)."
       - CLI argparse: `--videos`, `--output-dir` (default `reports/smplx_joints_{date}/`), `--video-source` (S3 default), `--nlf-model-path` (env NLF_MODEL_PATH), `--smplx-model-path` (env SMPLX_MODEL_PATH).
       - 함수:
         - `_load_nlf_engine(model_path)` — lazy import NLF.
         - `_load_smplx_model(model_path)` — lazy import smplx model.
         - `_extract_nlf_keypoints_per_frame(video_frames, nlf_engine) -> np.ndarray` (T,J,3).
         - `_fit_smplx_beta(nlf_keypoints_3d, smplx_model) -> np.ndarray` (10,).
         - `_render_smplx_joints(beta, smplx_model) -> np.ndarray` (T, NJ, 3) — beta + SMPL-X model 로 mean pose 의 joints rendering.
         - `extract_smplx_joints(motion_id, output_dir, nlf_engine, smplx_model) -> Path` — 영상→NLF→β→joints rendering→`{motion_id}.json` (joints array list).
         - `main()` CLI:
           - SMPLX_MODEL_PATH unset / 파일 없음 → stderr "SMPL-X weights 미설정 — CI 환경에선 정상. Pod 환경에선 SMPLX_MODEL_PATH 박제 필요." + `sys.exit(0)` (graceful).
           - 그 외 정상 flow.

    3. **`backend/research/evaluations/compare_body_profile.py` (HIGH-1 v5 + HIGH-2 v5 갱신)**:
       - 헤더 docstring 갱신: "HIGH-1 v5 박제: NLF→SMPL-X path 는 joints-based (smplx_joints_to_body_profile). v4 의 β-only fake 폐기. HIGH-2 v5 박제: run_body_profile_gap_report.py 가 orchestrator — 본 파일은 두 profile dict 의 gap 계산 + 보고서만. HIGH-3 v3: load_smplx_profiles graceful-empty (비존재 dir → {})."
       - CLI: `--rtmw-keypoints-dir`, `--smplx-joints-dir` (v4 의 `--nlf-profiles-dir` 폐기), `--videos`, `--output`.
       - 함수:
         - `load_rtmw_profiles(path, videos) -> dict[str, BodyNormalizationProfile]`: 비존재/빈 → `{}`. 각 keypoint JSON → `list[PoseFrame]` (factory 재사용) → `measure_body_profile` → `dict[video] = profile`.
         - `load_smplx_profiles(path, videos) -> dict[str, BodyNormalizationProfile]` (HIGH-1 v5 박제): `load_smplx_joints(path)` 호출 → 각 video joints → `smplx_joints_to_body_profile(joints)` → `dict[video] = profile`. 비존재/빈 → `{}` (HIGH-3 v3 graceful).
         - 명시 코멘트: "HIGH-1 v5 박제: extract_smplx_joints_from_video.py 산출 joints dir load → smplx_joints_to_body_profile 변환. v4 의 `load_nlf_smplx_profiles` (β 기반 fake) 폐기. HIGH-3 v3 graceful."
         - `compute_profile_gap(rtmw_profiles, smplx_profiles) -> dict`: per-video 5 numeric scale 필드 abs diff + aggregate mean diff + `verdict` ("within_5pct_tolerance" if all <0.05 else "gap_too_wide").
         - `main()` JSON + Markdown.

    4. **`extract_rtmw_body_profile_keypoints.py` (HIGH-4 v3 유지)**: 헤더 + LOW-1 v3. CLI `--videos`, `--output-dir`, `--video-source`. 함수: `_load_video_frames`, `_detect_pole_axis` (vertical fallback), `extract(motion_id, output_dir)` — `RTMWPoseEngine()` + `run_rtmw` → `list[PoseFrame]` → `dataclasses.asdict` JSON. Lazy import.

    5. **`backend/research/evaluations/run_body_profile_gap_report.py` 신설 (HIGH-2 v5 핵심)**:
       - 헤더 docstring: "단일 audited orchestrator. belle Pod 의 한 줄 명령 (HIGH-2 v5 박제, [[user-beginner-stepwise]] 정합). 내부 phase: (a) extract_rtmw_body_profile_keypoints subprocess, (b) extract_smplx_joints_from_video subprocess (Pod env 만), (c) load_smplx_joints + smplx_joints_to_body_profile 변환, (d) compare_body_profile.compute_profile_gap → JSON + MD 보고서. 각 phase stdout progress 로그 + 실패 시 actionable Korean 메시지."
       - CLI argparse: `--videos` (required, nargs='+'), `--date` (required, format YYYYMMDD), `--reports-root` (default `backend/research/evaluations/reports/`).
       - 함수:
         - `_check_pod_env()` — `RUNPOD_AUTH_TOKEN`, `NLF_MODEL_PATH`, `SMPLX_MODEL_PATH` 박제 검증. 미설정 시 actionable Korean 메시지 stderr + exit 2.
         - `_run_extract_rtmw(videos, date, reports_root)` — subprocess `python -m backend.research.evaluations.extract_rtmw_body_profile_keypoints --videos ... --output-dir <reports_root>/sweep_rtmw_<date>/keypoints/`. stdout 로그 prefix `[1/4 RTMW extract]`.
         - `_run_extract_smplx_joints(videos, date, reports_root)` — subprocess `python -m backend.research.evaluations.extract_smplx_joints_from_video --videos ... --output-dir <reports_root>/smplx_joints_<date>/`. stdout 로그 prefix `[2/4 SMPL-X joints extract]`.
         - `_run_joints_to_profile(date, reports_root)` — in-process `load_smplx_joints` + `smplx_joints_to_body_profile` → `<reports_root>/smplx_profiles_<date>/<video>.json` 산출. stdout 로그 prefix `[3/4 joints→profile]`.
         - `_run_compare(date, reports_root)` — subprocess `python -m backend.research.evaluations.compare_body_profile --rtmw-keypoints-dir ... --smplx-joints-dir ... --output <reports_root>/body_profile_gap_<date>.json`. stdout 로그 prefix `[4/4 gap report]`.
         - `main()` — argparse → `_check_pod_env` → 4 phase 순차 실행 → 성공 시 exit 0 + 보고서 경로 출력. 실패 시 phase index + error message.

    6. **운영 import 정합**:
       - `from sunity_shared.analysis.body_normalization_measurer import measure_body_profile` OK.
       - `from sunity_shared.analysis.body_normalization import BodyNormalizationProfile` OK.
       - NLF/SMPL-X import 는 `extract_smplx_joints_from_video.py` 함수 body 안 lazy.
       - RTMW import (extract.py) 함수 body 안.
       - `smplx_joints_to_body_profile.py` 는 numpy + body_normalization 만 import — NLF/SMPL-X import 0.
       - `run_body_profile_gap_report.py` 는 stdlib + numpy + body_normalization + smplx_joints_to_body_profile + 자체 subprocess — NLF/SMPL-X import 0.

    7. **Smoke (`backend/tests/test_compare_body_profile_smoke.py`) — 12 case**:
       - `test_argparse_help_compare` (LOW-1 v3): `python -m ...compare_body_profile --help` → exit 0, "rtmw-keypoints-dir" + "smplx-joints-dir".
       - `test_argparse_help_extract_rtmw` (HIGH-4 v3): "--videos" + "--output-dir".
       - **`test_argparse_help_extract_smplx_joints` (HIGH-1 v5)**: `python -m ...extract_smplx_joints_from_video --help` → "--videos" + "--output-dir" + "--smplx-model-path".
       - **`test_argparse_help_joints_to_profile` (HIGH-1 v5)**: `python -m ...smplx_joints_to_body_profile --help` → "--joints-dir".
       - **`test_argparse_help_orchestrator` (HIGH-2 v5)**: `python -m ...run_body_profile_gap_report --help` → "--videos" + "--date".
       - `test_compute_profile_gap_zero` / `test_compute_profile_gap_nonzero`.
       - `test_load_rtmw_profiles_empty_dir`: 비존재/빈 → `{}`.
       - **`test_load_smplx_joints_returns_empty_for_missing_dir` (HIGH-3 v3)**: `load_smplx_joints(Path("/tmp/nonexistent"))` → `{}` (raise X).
       - `test_extract_rtmw_helper_callable_with_mock` (HIGH-4 v3): mock RTMW + frames → `extract` → `tmp_path/test.json`.
       - **`test_smplx_joints_to_body_profile_synthetic_upright` (HIGH-1 v5 핵심)**: 합성 직립 SMPL-X joints (확정된 segment 거리: shoulder=140, hip=100, upper_arm=120, forearm=110, thigh=180, shank=160, torso=200) → `smplx_joints_to_body_profile(joints)` → profile. `assert math.isclose(profile.arm_scale, (120+110)/200, abs_tol=0.05)` + `math.isclose(profile.leg_scale, (180+160)/200, abs_tol=0.05)` + `math.isclose(profile.shoulder_hip_ratio, 140/100, abs_tol=0.05)`. SMPL-X model weights 호출 X.
       - **`test_smplx_joints_to_body_profile_two_inputs_yield_different_profiles` (HIGH-1 v5)**: joints_tall (upper_arm=140) vs joints_short (upper_arm=100) → `profile_tall.arm_scale != profile_short.arm_scale`. 결정적 변화 검증.
       - **`test_smplx_joints_to_body_profile_passes_validator` (MEDIUM-2 v5)**: 합성 joints → 모든 scale 필드 finite + strictly positive (validator ValueError 0).
       - **`test_extract_smplx_joints_graceful_exit_without_weights` (HIGH-1 v5)**: SMPLX_MODEL_PATH 미설정 환경 (CI) 에서 `python -m ...extract_smplx_joints_from_video --videos x --output-dir /tmp/x` → exit 0 + stderr 한국어 메시지.

    8. **운영 코드 leak 0 검증**: smoke test 가 `import smplx` 또는 NLF import 없이 `smplx_joints_to_body_profile` PASS (joints array 만 다룸).

    참조: REVIEWS.md v1→v2 HIGH-4 + v2→v3 HIGH-3 + v2→v3 HIGH-4 + v2→v3 LOW-1 + v3→v4 HIGH-2 (superseded) + v4→v5 HIGH-1 + v4→v5 HIGH-2.

    No code in this plan.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_compare_body_profile_smoke.py -x -v</automated>
    `python -m backend.research.evaluations.compare_body_profile --help 2>&1 | grep -q "rtmw-keypoints-dir"`.
    `python -m backend.research.evaluations.compare_body_profile --help 2>&1 | grep -q "smplx-joints-dir"`.
    `python -m backend.research.evaluations.extract_rtmw_body_profile_keypoints --help 2>&1 | grep -q -- "--videos"`.
    `python -m backend.research.evaluations.extract_smplx_joints_from_video --help 2>&1 | grep -q -- "--videos"`.
    `python -m backend.research.evaluations.smplx_joints_to_body_profile --help 2>&1 | grep -q -- "--joints-dir"`.
    `python -m backend.research.evaluations.run_body_profile_gap_report --help 2>&1 | grep -q -- "--videos"`.
    `python -m backend.research.evaluations.run_body_profile_gap_report --help 2>&1 | grep -q -- "--date"`.
    HIGH-3 v3: `grep -c "NotImplementedError" backend/research/evaluations/compare_body_profile.py backend/research/evaluations/smplx_joints_to_body_profile.py backend/research/evaluations/run_body_profile_gap_report.py 2>/dev/null` → 0.
    HIGH-1 v5: `grep -c "beta_to_body_profile" backend/research/evaluations/ -r` → 0 (v4 함수명 영구 폐기).
    HIGH-1 v5: `ls backend/research/evaluations/nlf_smplx_body_profile_loader.py 2>/dev/null | wc -l` → 0 (v4 파일 미생성).
    HIGH-1 v5: `grep -c "smplx_joints_to_body_profile" backend/research/evaluations/*.py | grep -v ':0$' | wc -l` ≥ 3 (smplx_joints_to_body_profile.py + compare + orchestrator).
    HIGH-2 v5: `grep -c "subprocess\|_run_extract" backend/research/evaluations/run_body_profile_gap_report.py` ≥ 2.
    운영 leak 0: `python -c "import importlib; m=importlib.import_module('backend.research.evaluations.smplx_joints_to_body_profile'); assert 'smplx' not in [name for name in dir(m) if not name.startswith('_')] or True"` (import 가능 + crash 0).
  </verify>
  <acceptance_criteria>
    - 12개 smoke test PASS (5 argparse + 2 gap + load_rtmw empty + load_smplx_joints graceful + extract mock + 3 joints→profile + extract_smplx graceful exit)
    - 5개 CLI repo root 실행 가능
    - HIGH-3 v3: graceful-empty (NotImplementedError 0)
    - HIGH-4 v3: extract_rtmw helper 박제
    - **HIGH-1 v5**: `smplx_joints_to_body_profile.py` 신설 + `smplx_joints_to_body_profile(joints)` pure NumPy math + CI 합성 joints 단위 테스트
    - **HIGH-1 v5**: `extract_smplx_joints_from_video.py` Pod-only 신설 + CI weights 부재 graceful exit
    - **HIGH-1 v5**: v4 `nlf_smplx_body_profile_loader.py` + `beta_to_body_profile` 폐기 (파일/함수명 등장 0)
    - **HIGH-1 v5**: `compare_body_profile.py` 가 `--smplx-joints-dir` 인자 + `load_smplx_profiles` (joints-based) 사용
    - **HIGH-2 v5**: `run_body_profile_gap_report.py` 신설 + 4 phase 내부 orchestrator + Korean error 메시지
    - 운영 import 격리: NLF/SMPL-X 는 `extract_smplx_joints_from_video.py` 함수 body 안. RTMW 는 `extract.py` 함수 body 안. `smplx_joints_to_body_profile.py` 와 `run_body_profile_gap_report.py` 는 NLF/SMPL-X import 0.
  </acceptance_criteria>
  <done>위 acceptance_criteria 모두 충족.</done>
  <commit-template>
    chore(02): R&D harness with joints-based SMPL-X path + single orchestrator (HIGH-1 v5 smplx_joints_to_body_profile + HIGH-2 v5 run_body_profile_gap_report)
  </commit-template>
  <depends-on>Task 3 (BodyNormalizationProfile 측정기)</depends-on>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5b: belle Pod 두 줄 실행 (run_body_profile_gap_report + git commit) — execution-only, HIGH-2 v5, ROADMAP §4 closure gate</name>
  <files>
    backend/research/evaluations/reports/sweep_rtmw_&lt;YYYYMMDD&gt;/keypoints/ref-foxtop.json (create — orchestrator phase 1),
    backend/research/evaluations/reports/sweep_rtmw_&lt;YYYYMMDD&gt;/keypoints/ref-foxtop-split.json (create),
    backend/research/evaluations/reports/sweep_rtmw_&lt;YYYYMMDD&gt;/keypoints/ref-invert.json (create),
    backend/research/evaluations/reports/sweep_rtmw_&lt;YYYYMMDD&gt;/keypoints/ref-sideway-spin.json (create),
    backend/research/evaluations/reports/sweep_rtmw_&lt;YYYYMMDD&gt;/keypoints/ref-climb.json (create),
    backend/research/evaluations/reports/smplx_joints_&lt;YYYYMMDD&gt;/ref-foxtop.json (create — orchestrator phase 2, HIGH-1 v5),
    backend/research/evaluations/reports/smplx_joints_&lt;YYYYMMDD&gt;/ref-foxtop-split.json (create),
    backend/research/evaluations/reports/smplx_joints_&lt;YYYYMMDD&gt;/ref-invert.json (create),
    backend/research/evaluations/reports/smplx_joints_&lt;YYYYMMDD&gt;/ref-sideway-spin.json (create),
    backend/research/evaluations/reports/smplx_joints_&lt;YYYYMMDD&gt;/ref-climb.json (create),
    backend/research/evaluations/reports/smplx_profiles_&lt;YYYYMMDD&gt;/*.json (create — orchestrator phase 3),
    backend/research/evaluations/reports/body_profile_gap_&lt;YYYYMMDD&gt;.json (create — orchestrator phase 4),
    backend/research/evaluations/reports/body_profile_gap_&lt;YYYYMMDD&gt;.md (create)
  </files>
  <read_first>
    backend/research/evaluations/run_body_profile_gap_report.py (Task 5a 박제 — HIGH-2 v5 단일 orchestrator),
    docs/licenses/rtmw-weights-audit.md
  </read_first>
  <action>
    Goal (HIGH-2 v5): ROADMAP success criterion 4 closure. **execution-only — 설계/구현 결정 0, 환경 manipulation 0 ([[runpod-gpu-env]] 외)**. belle 의 console 작업은 두 줄 — orchestrator 한 줄 + git commit 한 줄.

    **HIGH-2 v5 박제 (v4 와 차이)**:
    - v4: belle 4단계 console 작업 (extract → loader → compare → commit).
    - v5: belle 두 줄 명령 — `run_body_profile_gap_report` orchestrator + `git commit`. [[user-beginner-stepwise]] 정합.

    **HIGH-1 v5 박제**: orchestrator 내부 phase 2 가 SMPL-X joints 산출 (β-only fake 폐기).

    **LOW-1 v3 박제 유지**: 모든 `python -m` 은 repo root.
  </action>
  <what-built>
    Task 5a 가 다음 5개 박제 스크립트 + smoke commit 완료:
    - `compare_body_profile.py` (gap math + CLI, HIGH-1 v5 joints-based).
    - `smplx_joints_to_body_profile.py` (HIGH-1 v5 — joints → Profile pure NumPy).
    - `extract_smplx_joints_from_video.py` (HIGH-1 v5 — NLF→SMPL-X β→joints, Pod-only).
    - `extract_rtmw_body_profile_keypoints.py` (HIGH-4 v3 — RTMW dump CLI).
    - `run_body_profile_gap_report.py` (HIGH-2 v5 — 단일 audited orchestrator).
  </what-built>
  <how-to-verify>
    belle 가 두 줄 명령 실행 (모든 `python -m` 은 repo root, LOW-1 v3):

    **Pod ssh + env 확인 (이미 박제, [[runpod-gpu-env]])**:

    1. ssh + `cd /workspace/SunityMotion` + `git pull origin main`.
    2. Pod env 확인 (orchestrator 가 자동 검증 + 미설정 시 Korean stderr): `RUNPOD_AUTH_TOKEN`, `NLF_MODEL_PATH`, `SMPLX_MODEL_PATH`.

    **Step 1 — orchestrator 한 줄 (HIGH-2 v5)**:

    3. `cd /workspace/SunityMotion && python -m backend.research.evaluations.run_body_profile_gap_report --videos ref-foxtop ref-foxtop-split ref-invert ref-sideway-spin ref-climb --date $(date +%Y%m%d)`

       내부 자동 실행 (belle 개입 0):
       - `[1/4 RTMW extract]` extract_rtmw_body_profile_keypoints → `sweep_rtmw_<date>/keypoints/<video>.json` × 5.
       - `[2/4 SMPL-X joints extract]` extract_smplx_joints_from_video → `smplx_joints_<date>/<video>.json` × 5 (HIGH-1 v5 joints array).
       - `[3/4 joints→profile]` smplx_joints_to_body_profile → `smplx_profiles_<date>/<video>.json` × 5 (BodyNormalizationProfile field-level dict).
       - `[4/4 gap report]` compare_body_profile → `body_profile_gap_<date>.json` + `.md`.

       성공 시 stdout 마지막 줄 `[OK] body profile gap report: backend/research/evaluations/reports/body_profile_gap_<date>.json`.

    **Step 2 — git commit 한 줄**:

    4. `git add backend/research/evaluations/reports/sweep_rtmw_$(date +%Y%m%d)/keypoints/*.json backend/research/evaluations/reports/smplx_joints_$(date +%Y%m%d)/*.json backend/research/evaluations/reports/smplx_profiles_$(date +%Y%m%d)/*.json backend/research/evaluations/reports/body_profile_gap_$(date +%Y%m%d).* && git commit -m "data(02): SMPL-X vs RTMW body profile gap report (Phase 2 ROADMAP §4)"`

    **종료 신호**:
    - belle "approved" + `body_profile_gap_<date>.json` 경로 응답.
    - 또는 환경 문제 시 "not feasible — defer to v1.5" → ROADMAP §4 v1.5 defer 박제.
  </how-to-verify>
  <resume-signal>belle 가 "approved" + `body_profile_gap_<date>.json` 경로 응답, 또는 "not feasible — defer to v1.5" 응답 시 다음 task 진행</resume-signal>
  <acceptance_criteria>
    - **Step 1 HIGH-2 v5**: orchestrator 한 줄 실행 → 4 phase 내부 자동 → 모든 산출 파일 체크인 가능 상태
    - **Step 2**: 단일 git commit 에 keypoints (5) + joints (5) + profiles (5) + gap (2) 파일
    - 또는 belle "not feasible" + ROADMAP §4 v1.5 defer 박제
    - 모든 `python -m` repo root 실행
    - belle console 작업 = 두 줄 (orchestrator + commit), 설계/구현 결정 0
    - 본 task closed 시에만 phase verifier 가 ROADMAP success criterion 4 PASS
  </acceptance_criteria>
  <done>위 둘 중 하나 충족.</done>
  <commit-template>
    data(02): SMPL-X vs RTMW body profile gap report (Phase 2 ROADMAP §4)
  </commit-template>
  <depends-on>Task 5a (5개 박제 스크립트 + smoke)</depends-on>
</task>

<task type="auto">
  <name>Task 6: AST import isolation 확장 + REQUIREMENTS BODY-01 RTMW + ROADMAP success criterion 1 v1.5 deferral (MEDIUM-2 v4) + ROADMAP success criterion 2/4 Phase 2 vs Phase 6 closure 경계 (MEDIUM-1 v5)</name>
  <read_first>
    backend/shared/python/sunity_shared/analysis/pose_engines/__init__.py (lazy mediapipe),
    backend/shared/python/sunity_shared/analysis/pose_engines/mediapipe_engine.py (live mediapipe lines 74-76, 197),
    backend/functions/pipeline/app.py (MEDIUM-1 v3 대상),
    backend/runpod_inference/server.py (line 31 `import importlib.util` + line 73 `spec_from_file_location` legitimate),
    .planning/REQUIREMENTS.md (line 30 BODY-01),
    .planning/ROADMAP.md (line 113 Phase 2 success criterion 1 — MEDIUM-2 v4; success criterion 2 + 4 — MEDIUM-1 v5),
    backend/research/evaluations/compare_body_profile.py (Task 5a),
    backend/research/evaluations/extract_rtmw_body_profile_keypoints.py (Task 5a),
    backend/research/evaluations/smplx_joints_to_body_profile.py (Task 5a — HIGH-1 v5),
    backend/research/evaluations/extract_smplx_joints_from_video.py (Task 5a — HIGH-1 v5),
    backend/research/evaluations/run_body_profile_gap_report.py (Task 5a — HIGH-2 v5)
  </read_first>
  <action>
    Goal:
    (1) **AST 스캔 확장 (LOW-2 v4)**: deployable product 가 `backend.research` / `research` / `nlf` / `smplx` / `smpl_x` import 시 fail. `importlib.import_module(literal)` + `__import__(literal)` + `spec_from_file_location(name, literal_path)` 모두 차단. mediapipe 제외.
    (2) **REQUIREMENTS.md BODY-01** "MediaPipe" → "RTMW".
    (3) **ROADMAP.md Phase 2 success criterion 1 acceptance text 갱신 (MEDIUM-2 v4 유지)**.
    (4) **MEDIUM-1 v5 신규** — ROADMAP.md success criterion 2 + 4 acceptance text 갱신 (Phase 2 closure vs Phase 6 closure 경계).

    **MEDIUM-3 v3 유지**: Task 5a 만 의존.

    **(1) `backend/tests/test_research_import_isolation.py` 신설 (MEDIUM-1 v3 + LOW-2 v4)**:

    - 파일 상단 docstring:
      "Phase 2 R&D 격리 + 라이선스 차단 AST 자동 검증. MEDIUM-1 v3: 스캔 범위 sunity_shared + backend/functions + backend/runpod_inference. LOW-2 v4 박제: importlib.import_module + __import__ + spec_from_file_location literal 모두 차단. 잔여 위험: 비-literal 동적 import (variable 인자) 정적 분석 한계 — PR manual review 정합. v1→v2 HIGH-3 박제 유지 — mediapipe 차단 제외."

    - `ast.parse` 로 3 디렉터리 `.py` 스캔: `backend/shared/python/sunity_shared/` + `backend/functions/` + `backend/runpod_inference/`. 제외: `backend/tests/`, `backend/research/`.

    - 금지 정적 import: `import research`, `import backend.research`, `from research import ...`, `from backend.research import ...`, `import nlf`, `from nlf import ...`, `import smplx`, `from smplx import ...`, `import smpl_x`, `from smpl_x import ...`.

    - **LOW-2 v4 박제 — 동적 3종 차단**:
      - `importlib.import_module(literal)`: `ast.Call` 의 `node.func: ast.Attribute` + `node.func.attr == "import_module"` + 첫 positional arg `ast.Constant` + value 가 차단 prefix.
      - `__import__(literal)`: `ast.Call` 의 `node.func: ast.Name` + `node.func.id == "__import__"` + 첫 positional arg `ast.Constant` + value 가 차단 prefix.
      - `spec_from_file_location(name, literal)`: `ast.Call` 의 `node.func: ast.Attribute` + `node.func.attr == "spec_from_file_location"` + 두 번째 positional arg `ast.Constant` + value basename 차단 모듈명 매치.
        - **runpod_inference/server.py:73 legitimate**: `spec_from_file_location("sunity_pipeline_app", pipeline_path)` — `pipeline_path` 가 변수 + name "sunity_pipeline_app" 매치 0 → PASS.

    - 잔여 위험 TODO.

    - **`mediapipe` 차단 제외 (v1→v2 HIGH-3 박제 유지)**.

    - **테스트 함수 5개**:
      - `test_sunity_shared_does_not_import_research_or_nlf_or_smplx`.
      - `test_backend_functions_does_not_import_research_or_nlf_or_smplx`.
      - `test_backend_runpod_inference_does_not_import_research_or_nlf_or_smplx`.
      - `test_no_dynamic_importlib_import_of_research_or_nlf_or_smplx`.
      - `test_no_dynamic_dunder_import_or_spec_from_file_location_of_research_or_nlf_or_smplx`.

    **(2) `.planning/REQUIREMENTS.md` line 30**:
    - 현재: `"**BODY-01**: MediaPipe 키포인트로부터 ...`
    - 갱신: `"**BODY-01**: RTMW 133 wholebody 키포인트로부터 ...`. 뒷부분 유지.
    - footer note: `*Updated 2026-06-07 — Phase 2 plan 01 RTMW pivot 정합 (BODY-01 MediaPipe → RTMW 갱신, v4/v5 박제).*`.

    **(3) `.planning/ROADMAP.md` Phase 2 success criterion 1 갱신 (MEDIUM-2 v4 유지)**:
    - 현재: `1. RTMW 키포인트에서 segment 길이가 시간 평균으로 안정적으로 추출된다 (jitter 스무딩)`
    - 갱신: `1. RTMW 키포인트에서 segment 길이가 시간 평균으로 안정적으로 추출된다 (jitter 스무딩) — v1 acceptance: synthetic + RTMW adapter-path validation; real RTMW network output coverage 는 v1.5 sweep 실행 (belle Pod 5영상 keypoint dump 산출) 으로 deferred (MEDIUM-2 v4 박제)`.

    **(4) MEDIUM-1 v5 — ROADMAP success criterion 2 + 4 갱신**:
    - success criterion 2 현재 (가정): `2. BodyNormalizationProfile이 ... 산출된다`
    - 갱신: `2. **Phase 2 v1 closure**: 측정기 함수 measure_body_profile + helper _angles_and_body_profile_from_video 가 박제되고 단위 테스트가 BodyNormalizationProfile 7필드 산출을 검증한다. _process / Firestore 통합은 Phase 6 plan 책임 — **Phase 6 closure**: production analysis document (Firestore AnalysisDoc) 가 bodyNormalizationProfile 을 포함한다 (Phase 6 plan 이 본 phase 의 helper 호출 site 박제) (MEDIUM-1 v5 박제).`
    - success criterion 4 현재 (가정): `4. R&D 비교군: 동일 영상에서 NLF→SMPL-X β로 추출한 BodyNormalizationProfile 과의 갭을 보고서로 출력`
    - 갱신: `4. R&D 비교군: 동일 영상에서 NLF→SMPL-X β fitting + β→SMPL-X model→joints rendering + joints→BodyNormalizationProfile 변환 + RTMW segment 측정과의 갭 보고서. β-only pure-math 변환은 fake (SMPL-X β 는 shapedirs/joint regressor/template mesh 없이 body geometry 산출 불가) 이므로 ROADMAP §4 closure 조건 X — 실 joint 산출이 closure 조건 (HIGH-1 v5 박제). Pod 실 실행 (Task 5b — run_body_profile_gap_report orchestrator) 으로 closure.`

    **(5) 단일 atomic commit**: test + REQUIREMENTS.md + ROADMAP.md (success criterion 1 + 2 + 4) 동시 변경.

    No code in this plan.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_research_import_isolation.py -x -v</automated>
    REQUIREMENTS: `grep -n "RTMW" .planning/REQUIREMENTS.md | grep "BODY-01"` ≥ 1. `grep "MediaPipe" .planning/REQUIREMENTS.md | grep "BODY-01"` → 0.
    HIGH-3 박제: `grep -c "mediapipe" backend/tests/test_research_import_isolation.py` ≥ 1 (TODO).
    MEDIUM-1 v3: `grep -c "backend/functions\|backend/runpod_inference\|importlib" backend/tests/test_research_import_isolation.py` ≥ 3.
    **LOW-2 v4**: `grep -c "__import__\|spec_from_file_location" backend/tests/test_research_import_isolation.py` ≥ 2.
    **MEDIUM-2 v4**: `grep "v1.5 sweep" .planning/ROADMAP.md` ≥ 1.
    **MEDIUM-1 v5 — success criterion 2**: `grep "Phase 2 v1 closure" .planning/ROADMAP.md` ≥ 1. `grep "Phase 6 closure" .planning/ROADMAP.md` ≥ 1.
    **MEDIUM-1 v5 — success criterion 4**: `grep "β-only pure-math 변환은 fake" .planning/ROADMAP.md` ≥ 1. `grep "joints rendering" .planning/ROADMAP.md` ≥ 1.
  </verify>
  <acceptance_criteria>
    - 5개 AST 테스트 PASS
    - MEDIUM-1 v3: 스캔 3 디렉터리
    - **LOW-2 v4**: `__import__(literal)` + `spec_from_file_location(name, literal_path)` 차단
    - 잔여 위험 TODO
    - `runpod_inference/server.py:73` legitimate 사용 → 매치 0 → PASS
    - mediapipe 차단 코드 없음 (TODO 만)
    - REQUIREMENTS.md BODY-01 RTMW 갱신, MediaPipe 제거
    - REQUIREMENTS.md footer note 추가
    - **MEDIUM-2 v4**: ROADMAP success criterion 1 "v1.5 sweep deferred" 박제
    - **MEDIUM-1 v5**: ROADMAP success criterion 2 "Phase 2 v1 closure" + "Phase 6 closure" 경계 박제
    - **MEDIUM-1 v5**: ROADMAP success criterion 4 "β-only pure-math 변환은 fake" + "joints rendering" 박제
    - 단일 atomic commit
  </acceptance_criteria>
  <done>위 acceptance_criteria 모두 충족.</done>
  <commit-template>
    chore(02): expand AST R&D import isolation + align BODY-01 to RTMW + ROADMAP §1 v1.5 deferral + §2/§4 Phase 2 vs Phase 6 closure boundary (MEDIUM-1 v5 + MEDIUM-2 v4 + LOW-2 v4)
  </commit-template>
  <depends-on>Task 5a (R&D harness 존재 — MEDIUM-3 v3: Task 5b 무관, belle Pod 평행)</depends-on>
</task>


<task type="auto">
  <name>Task 7: estimatedHeightScale consumer semantic guard lint test — 4 dir scan (Python sunity_shared + backend/functions + backend/runpod_inference + TS app/src) — LOW-1 v5</name>
  <read_first>
    backend/shared/python/sunity_shared/analysis/body_normalization.py (estimated_height_scale 정의),
    backend/shared/python/sunity_shared/ (Python consumer scan 대상 1),
    backend/functions/ (Python consumer scan 대상 2),
    backend/runpod_inference/ (Python consumer scan 대상 3),
    app/src/ (TS consumer scan 대상 4)
  </read_first>
  <action>
    Goal (LOW-1 v5): `estimatedHeightScale` / `estimated_height_scale` identifier 의 모든 deployable consumer (sunity_shared + backend/functions + backend/runpod_inference + app/src TS) 가 인접 (±3 라인) 에 "torso-relative proportion heuristic" (또는 TS 측 "torso-relative") 주석/JSDoc 박제했는지 lint scan. **현재 4 dir 모두 consumer 0 → vacuous PASS**. Phase 6+ consumer 추가 시 load-bearing.

    **v5 변경 (LOW-1 v5)**:
    - v4: scan root 1 dir (`backend/shared/python/sunity_shared`).
    - v5: scan root 4 dir + TS 측 scan 추가. 제외: `backend/research/` (R&D throwaway) + `backend/tests/` + `app/scripts/`.

    **Rename Option B reject 사유 유지**: 필드 rename 은 3-way lockstep + CONTEXT.md + dataclass 모두 깨짐. Option A (lint) 만 채택.

    1. **`backend/tests/test_estimated_height_scale_consumer_semantics.py` 신설**:
       - 헤더 docstring: "estimatedHeightScale / estimated_height_scale identifier 의 모든 deployable consumer 가 인접 (±3 라인) 에 'torso-relative proportion heuristic' (또는 TS 'torso-relative') 주석 박제했는지 lint scan. MEDIUM-3 v4 + LOW-1 v5 박제: 필드명이 absolute height 처럼 보이는 risk 차단. scan 4 dir = Python (sunity_shared + backend/functions + backend/runpod_inference) + TS (app/src). 제외 = backend/research/ + backend/tests/ + app/scripts/. 현재 consumer 0 → vacuous PASS. Phase 6+ consumer 추가 시 load-bearing."

    2. **Python 스캔 함수 `_scan_python_consumer_lines(root: Path) -> list[ConsumerSite]`**:
       - `root.rglob("*.py")` 전체.
       - 제외 (root 가 sunity_shared 일 때만): `body_normalization.py` 자체, `body_normalization_measurer.py` 자체.
       - 정규식 `r"\b(estimatedHeightScale|estimated_height_scale)\b"` 매치.
       - `ConsumerSite(file=Path, line=int, content=str, lang='python')` 박제.

    3. **TS 스캔 함수 `_scan_ts_consumer_lines(root: Path) -> list[ConsumerSite]`** (LOW-1 v5 신규):
       - `root.rglob("*.ts")` + `root.rglob("*.tsx")` 전체.
       - 제외: `analysis.ts` 의 BodyNormalizationProfile interface 안 정의 line (interface 본체 안 첫 등장만 제외, consumer 측 등장은 포함).
       - 정규식 `r"\bestimatedHeightScale\b"` 매치.
       - `ConsumerSite(file=Path, line=int, content=str, lang='ts')` 박제.

    4. **검증 함수 `_assert_semantic_comment_nearby(site, *, required_substring)`**:
       - 매치 line ±3 라인 안에 `required_substring` substring 검사.
       - Python 측 `required_substring = "torso-relative proportion heuristic"`.
       - TS 측 `required_substring = "torso-relative"` (JSDoc 약간 짧아도 OK).
       - 없으면 `assert False, f"LOW-1 v5 박제 위반: {site.file}:{site.line} ({site.lang}) — estimatedHeightScale 사용 시 인접 (±3 라인) 에 '{required_substring}' 주석 박제 필수. 본 필드는 absolute height 아님 — torso 길이 대비 사지 비율 heuristic (per Phase 2 Task 1 lockstep)."`.

    5. **테스트 3개 (LOW-1 v5)**:
       - `test_python_consumers_have_semantic_comment_in_sunity_shared`:
         - `sites = _scan_python_consumer_lines(Path("backend/shared/python/sunity_shared"))`.
         - 정의 파일 제외 후 사이트 → 각 `_assert_semantic_comment_nearby(..., required="torso-relative proportion heuristic")`.
         - 현재 vacuous PASS.
       - `test_python_consumers_have_semantic_comment_in_functions_and_runpod` (LOW-1 v5 신규):
         - `sites = _scan_python_consumer_lines(Path("backend/functions")) + _scan_python_consumer_lines(Path("backend/runpod_inference"))`.
         - 각 site assert.
         - 현재 vacuous PASS.
       - `test_ts_consumers_have_semantic_comment_in_app_src` (LOW-1 v5 신규):
         - `sites = _scan_ts_consumer_lines(Path("app/src"))`.
         - `analysis.ts` 의 BodyNormalizationProfile interface 안 정의 line 제외 (consumer 측 등장 검색).
         - 각 site `_assert_semantic_comment_nearby(..., required="torso-relative")`.
         - 현재 vacuous PASS (consumer 측 등장 0).

    6. **테스트 `test_scan_excludes_definition_files`**:
       - 정의 파일 (body_normalization.py / body_normalization_measurer.py) 의 정의 line 결과에 포함 0.
       - analysis.ts 의 BodyNormalizationProfile interface 안 정의 line 결과에 포함 0.

    7. **테스트 `test_scan_excludes_research_tests_scripts`**:
       - `backend/research/` 의 estimatedHeightScale 등장 (e.g., smplx_joints_to_body_profile.py) 은 scan 결과 포함 0.
       - `backend/tests/` 등장 포함 0.
       - `app/scripts/` 등장 포함 0.

    No code in this plan.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_estimated_height_scale_consumer_semantics.py -x -v</automated>
    vacuous PASS (Python 3 dir): `grep -rn "estimatedHeightScale\|estimated_height_scale" backend/shared/python/sunity_shared/ backend/functions/ backend/runpod_inference/ 2>/dev/null | grep -v "body_normalization.py\|body_normalization_measurer.py" | wc -l` → 0.
    vacuous PASS (TS): `grep -rn "estimatedHeightScale" app/src/ 2>/dev/null | grep -v "interface BodyNormalizationProfile" | wc -l` 출력 확인 (현재 consumer 측 등장 0 가정).
    제외 확인: `grep -rn "estimatedHeightScale" backend/research/ backend/tests/ app/scripts/ 2>/dev/null | wc -l` 무관 (이들은 scan 제외).
  </verify>
  <acceptance_criteria>
    - 5개 테스트 PASS (3 dir × {vacuous} + definition exclusion + research/tests/scripts exclusion)
    - 현재 4 dir 모두 consumer 0 → vacuous PASS
    - Python 측 검증 substring "torso-relative proportion heuristic"
    - TS 측 검증 substring "torso-relative"
    - 스캔 함수가 정의 파일 (Python + TS) + R&D + tests + scripts 제외
    - assert 메시지가 박제 요구 명시
    - LOW-1 v5: scan 범위가 sunity_shared + backend/functions + backend/runpod_inference + app/src TS 4 dir
  </acceptance_criteria>
  <done>위 acceptance_criteria 모두 충족.</done>
  <commit-template>
    test(02): expand estimatedHeightScale consumer semantic guard to 4 dirs (Python + TS, LOW-1 v5 — vacuous PASS, Phase 6+ load-bearing)
  </commit-template>
  <depends-on>Task 1 (3-way lockstep 박제 — "torso-relative proportion heuristic" 정의 측 박제)</depends-on>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RunPod Pod ↔ Lambda | RTMW pose_frames 가 RunPod 에서 측정 후 Lambda 가 결과 받음 — Phase 2 는 내부 호출만 |
| sunity_shared + backend/functions + backend/runpod_inference (운영) ↔ backend.research (R&D) | 라이선스 격리. MEDIUM-1 v3 + LOW-2 v4 — 스캔 범위 확장 + 3종 동적 import 차단 |
| `_RTMWNlfCompat.estimate_with_profile` (Pod, race-safe local-return) ↔ caller helper (`_angles_and_body_profile_from_video`) | BodyNormalizationProfile propagation 경계 — RunPod FastAPI BackgroundTasks 환경에서 concurrent analyses 가 module-level `_POSE_ESTIMATOR` 글로벌 공유해도 local tuple 로 leak 0 (HIGH-1 v4). Phase 6 plan 이 본 helper 호출하여 Firestore wire-up (Phase 2 scope 외 — MEDIUM-1 v4 + MEDIUM-1 v5 wording 박제) |
| `smplx_joints_to_body_profile` (pure NumPy) ↔ `extract_smplx_joints_from_video` (Pod, NLF+SMPL-X model weights) | HIGH-1 v5 박제 — 변환 math 와 joints 산출 분리. CI 는 합성 joints, Pod 는 실 fitted joints. β-only fake 영구 폐기 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | BodyNormalizationProfile numeric 필드 범위 벗어남 (NaN/inf/0/negative) | mitigate | **MEDIUM-2 v5 박제**: dataclass `__post_init__` validator 가 5 numeric scale 필드 finite + strictly positive 강제. 위반 시 ValueError. measurer fallback path 가 scale=1.0 emit (validator 통과). |
| T-02-02 | Repudiation / 법적 | 운영 코드가 backend.research / SMPL-X / NLF import (라이선스 위반) | mitigate | Task 6 AST 자동 차단 (HIGH-3 narrowed, MEDIUM-1 v3 3 디렉터리, LOW-2 v4 importlib + __import__ + spec_from_file_location literal 3종). 잔여 위험: 비-literal 동적 import (PR manual review) |
| T-02-03 | DoS | 인버트/짧은 영상 NaN profile → 다운스트림 crash | mitigate | `insufficient_frames` warning + scale=1.0 (MEDIUM-2 v5 positive 통과) + conf=0 폴백. `__post_init__` finite + positive 강제 |
| T-02-04 | Info Disclosure | R&D 보고서 사용자 영상 메타데이터 leak | accept | belle Pod 내부 실행만. private repo |
| T-02-05 | Tampering | `_RTMWNlfCompat` 시그너처 변경이 B8 박제 위반 | mitigate | HIGH-1 v4 race-safe local-return helper: 기존 `estimate(frames) -> np.ndarray` 무변경, 신규 `estimate_with_profile(frames) -> tuple[...]` 박제. Task 4 `test_b8_signature_lock_preserved` + verify |
| T-02-06 | Tampering | Module-level `_POSE_ESTIMATOR` 글로벌 + RunPod `BackgroundTasks` 동시 호출 시 profile leak | mitigate | HIGH-1 v4 race-safe + 사이드카 폐기: `last_body_profile` 영구 폐기. local tuple → caller 자기 frame 만. Task 4 concurrency barrier + stale-failure 테스트 |
| T-02-07 | Info Disclosure | Phase 2 가 Firestore 저장 시 CONTEXT.md §1 v1 scope 위반 + Phase 6 미구현 상태 dangling field | mitigate | MEDIUM-1 v4 박제 (revert) + MEDIUM-1 v5 (wording 명시): Phase 2 v1 scope = 측정기 본체 + helper. Firestore 저장 + AnalysisDoc 박제 Phase 6 책임. ROADMAP success criterion 2/4 acceptance text 가 "Phase 2 closure vs Phase 6 closure" 경계 박제 (MEDIUM-1 v5) |
| T-02-08 | Integrity | R&D 비교군 path 가 weights-free fake 로 ROADMAP §4 fake closure | mitigate | **HIGH-1 v5 박제**: `beta_to_body_profile(beta)` (v4 weights-free fake) 영구 폐기. 대신 `smplx_joints_to_body_profile(joints)` + Pod-only `extract_smplx_joints_from_video.py`. 실 fitted SMPL-X joints 입력. ROADMAP §4 acceptance text 가 "β-only pure-math 변환은 fake" 명시 (MEDIUM-1 v5) |
| T-02-09 | Tampering | `pose_too_inverted` 가 PoleAxis.axis_vector 부호 의존 — production canonicalization 깨지면 임의 flip | mitigate | **MEDIUM-3 v5 박제**: image y-order 직접 비교 (mid_shoulder.y vs mid_hip.y in y-down image). pole_axis 인자 폐기. `_is_inverted_frame` 시그너처에서 pole_axis 제거. axis_vector flip 시 verdict 불변 회귀 테스트 |
| T-02-10 | Tampering | Task 5b 가 belle 비개발자에게 멀티스텝 console 요구 → 실행 실패 / 환경 manipulation 위험 | mitigate | **HIGH-2 v5 박제**: 단일 audited orchestrator `run_body_profile_gap_report.py`. belle console = 두 줄 (run + commit). 환경 manipulation 0. [[user-beginner-stepwise]] 정합 |

> 본 phase 외부 입력 X. V2/V3/V4/V6 적용 X.
</threat_model>

<verification>
## Phase-level 검증 (전 task 완료 후)

1. **단위 테스트 전체**: `cd backend && pytest tests/ -x` — 회귀 0.
2. **3-way lockstep + validator (L1 + MEDIUM-2 v5)**: `pytest tests/test_body_normalization_lockstep.py tests/test_body_normalization_profile.py -x` PASS.
3. **Fixture smoke (MEDIUM-3 v5 axis_vector independence)**: `pytest tests/fixtures/body_normalization/test_fixtures_loadable.py -x` PASS.
4. **Measurer (MEDIUM-3 v5 + MEDIUM-2 v5 fallback)**: `pytest tests/test_body_normalization_measurer.py -x` PASS (12 case).
5. **Pipeline 통합 race-safe (HIGH-1 v4)**: `pytest tests/test_pipeline_body_profile_injection.py -x` PASS (9).
6. **B8 박제 (HIGH-1 v4 핵심)**: `pytest tests/test_pipeline_recognizer_switch.py::TestB8FixSignature -x -v` PASS — 2 테스트.
7. **R&D 격리 (HIGH-3 v1→v2 + MEDIUM-1 v3 + LOW-2 v4)**: `pytest tests/test_research_import_isolation.py -x` PASS (5).
8. **R&D harness smoke (HIGH-1 v5 + HIGH-2 v5 + HIGH-3 v3 + HIGH-4 v3 + LOW-1 v3)**: `pytest tests/test_compare_body_profile_smoke.py -x` PASS (12).
9. **estimatedHeightScale consumer semantics 4 dir (LOW-1 v5)**: `pytest tests/test_estimated_height_scale_consumer_semantics.py -x` PASS (5, vacuous).
10. **HIGH-4 v3 + HIGH-1 v5 게이트**: `ls .../sweep_rtmw_*/keypoints/*.json` ≥ 5 + `ls .../smplx_joints_*/*.json` ≥ 5 + `ls .../smplx_profiles_*/*.json` ≥ 5 + `ls .../body_profile_gap_*.json` ≥ 1 (또는 v1.5 defer).
11. **REQUIREMENTS**: BODY-01 "RTMW" + "MediaPipe" 미등장.
12. **MEDIUM-2 v4**: ROADMAP Phase 2 success criterion 1 "v1.5 sweep" 박제.
13. **MEDIUM-1 v5**: ROADMAP Phase 2 success criterion 2 "Phase 2 v1 closure" + "Phase 6 closure" 박제. success criterion 4 "β-only pure-math 변환은 fake" + "joints rendering" 박제.
14. **의존성**: `grep -r "import scipy" backend/shared/python/sunity_shared/analysis/body_normalization_measurer.py` → 0.
15. **HIGH-1 v4 사이드카 폐기**: `grep -c "last_body_profile" backend/functions/pipeline/app.py backend/shared/python/sunity_shared/firestore_admin.py app/src/types/analysis.ts docs/contract.md` → 0.
16. **HIGH-1 v4 helper**: `grep -n "_angles_and_body_profile_from_video\|estimate_with_profile" backend/functions/pipeline/app.py | wc -l` ≥ 4.
17. **MEDIUM-1 v4 revert (LOW-2 v5 scope-anchored)**: AnalysisDoc 안 `bodyNormalizationProfile` 부재 — python regex block-extract.
18. **HIGH-3 박제**: `grep -c "mediapipe" backend/tests/test_research_import_isolation.py` ≥ 1 (TODO).
19. **LOW-1 v3**: Task 5a verify `python -m backend.research.evaluations.*` repo root.
20. **HIGH-1 v5**: `grep -rn "beta_to_body_profile" backend/research/evaluations/` → 0 (v4 함수명 폐기). `ls backend/research/evaluations/nlf_smplx_body_profile_loader.py 2>/dev/null` → 0 (v4 파일 미생성). `grep -c "smplx_joints_to_body_profile" backend/research/evaluations/*.py | grep -v ':0$' | wc -l` ≥ 3.
21. **HIGH-2 v5**: `python -m backend.research.evaluations.run_body_profile_gap_report --help` exit 0 + `--videos` + `--date`.
22. **MEDIUM-3 v5**: `_is_inverted_frame` 시그너처에 pole_axis 인자 부재 (inspect.signature 검증).

## closure_boundary 노트 (MEDIUM-1 v5)

| Closure | Trigger | 책임 |
|---|---|---|
| **Phase 2 v1 closure** | Task 1-7 PASS + Task 5b belle Pod 실행 (또는 v1.5 defer) | 본 plan — measurer + helper + R&D harness + import isolation + REQUIREMENTS/ROADMAP wording |
| **Phase 6 closure** | Phase 6 plan 이 `_angles_and_body_profile_from_video` 호출 site + Firestore 저장 + AnalysisDoc 박제 + 다운스트림 normalize 결합 | Phase 6 plan (out of Phase 2 scope) |
| **ROADMAP §4 closure** | Task 5b 완료 (orchestrator 산출 5+5+5+1 파일 commit) | belle Pod 두 줄 실행 (HIGH-2 v5) |
| **ROADMAP §1 v1.5 closure** | belle Pod sweep 후 실 RTMW network output fixture promote (별 plan) | post-pilot |
</verification>

<success_criteria>
## ROADMAP Success Criteria 매핑 (v5 박제)

| Criterion | Task | 검증 | 본 phase closure 가능? |
|-----------|------|------|--|
| 1. RTMW segment 시간 평균 안정 추출 — **v1: synthetic + adapter-path; real RTMW v1.5 deferred (MEDIUM-2 v4)** | Task 3 + Task 2b skip | `test_normal_pose_profile_valid` + RTMW sanity skip | 예 (v1 + v1.5 deferral) |
| 2. **Phase 2 v1 closure**: measurer + helper 박제 / **Phase 6 closure**: production AnalysisDoc 가 profile 포함 (MEDIUM-1 v5 wording) | Task 3 (measurer) + Task 4 (helper) + Task 6 (ROADMAP wording) | `test_profile_validator_passes_for_normal_fixture` + Phase 6 plan 책임 박제 | 예 (Phase 2 절반) |
| 3. confidence 낮을 때 warnings 표기 | Task 3 (5종 + MEDIUM-3 v5) | warning 테스트 | 예 |
| 4. R&D 비교군: NLF→SMPL-X β fit → joints → Profile + RTMW 갭 보고서 (HIGH-1 v5 — β-only fake 폐기, MEDIUM-1 v5 wording) | Task 5a (5 박제 스크립트) + Task 5b (orchestrator 한 줄 실행, HIGH-2 v5) + Task 6 (ROADMAP wording) | scaffold smoke + 실 보고서 (5 keypoint + 5 joints + 5 profile + 1 gap) | **Task 5b 완료 시에만 — HIGH-1 v5 + HIGH-2 v5 게이트** |
| 5. 데이터 계약 3-way lockstep (Phase 2 v1 scope — AnalysisDoc 은 Phase 6) | Task 1 | `test_body_normalization_lockstep` | 예 |

**v5 Done (executor scope)**: criterion 1 (v1 + v1.5 deferral), 2 (Phase 2 half), 3, 5 PASS + criterion 4 의 5a 5 박제 스크립트 PASS + Phase 1 회귀 0 + Pipeline race-safe verified + B8 박제 보존 + REQUIREMENTS BODY-01 RTMW + ROADMAP §1 v1.5 deferral + §2/§4 closure boundary wording (MEDIUM-1 v5).

**ROADMAP §4 closure**: Task 5b 완료 시. autonomous=false 박제.

**Phase 6 unblock**: `_angles_and_body_profile_from_video(bucket, key) -> tuple[np.ndarray, BodyNormalizationProfile | None]` helper + `_RTMWNlfCompat.estimate_with_profile` + `measure_body_profile` 박제 완료. Phase 6 plan 이 wire-up (Firestore 저장 + AnalysisDoc 박제 + 다운스트림 normalize) 입력 완성.
</success_criteria>

<source_audit>
## Multi-Source Coverage Audit (v5 박제 — Codex 4차 review 정합)

### GOAL (ROADMAP)
| Item | Plan | Task |
|------|------|------|
| RTMW 키포인트 → BodyNormalizationProfile 자동 출력 | 02-01 | Task 3 |
| 두 엔진 공유 입력 | 02-01 | Task 4 (race-safe helper — Phase 6 가 wire-up; HIGH-1 v4 + MEDIUM-1 v4 + MEDIUM-1 v5 wording) |
| SMPL-X 비교군 R&D 만 | 02-01 | Task 5a (HIGH-1 v5 joints-based + HIGH-2 v5 orchestrator), Task 5b (execution-only), Task 6 (import 차단 + ROADMAP §4 wording) |

### REQ (REQUIREMENTS.md)
| Req ID | Plan | Task |
|--------|------|------|
| BODY-01 | 02-01 | Task 3 (측정기 + MEDIUM-3 v5) + Task 4 (race-safe helper HIGH-1 v4) + Task 6 (RTMW 정합 + ROADMAP wording) + Task 1 (validator MEDIUM-2 v5) |

### RESEARCH (02-RESEARCH.md)
| Feature | Plan | Task |
|---------|------|------|
| body_normalization_measurer.py (순수 numpy) | 02-01 | Task 3 |
| MAD outlier + robust median | 02-01 | Task 3 |
| torso self-reference normalize | 02-01 | Task 3 |
| 5종 warnings enum | 02-01 | Task 1 (3-way + L1) + Task 3 (MEDIUM-3 v5 y-order) |
| 5 synthetic fixture (RTMW adapter) | 02-01 | Task 2 |
| body_shape 주입 + propagation | 02-01 | Task 4 (HIGH-1 v4 race-safe, 사이드카 폐기) |
| R&D 비교 harness | 02-01 | Task 5a (HIGH-1 v5 joints + HIGH-2 v5 orchestrator), Task 5b (실행) |
| AST import isolation (HIGH-3 v1→v2 + MEDIUM-1 v3 + LOW-2 v4) | 02-01 | Task 6 |
| Phase 1 sweep keypoint dump (M1) | DEFERRED v1.5 (MEDIUM-2 v4) | Task 2 TODO + Task 2b skip + Task 6 ROADMAP §1 갱신 |
| Firestore 저장 (CONTEXT.md §1 + §8) | **REVERTED — Phase 6 (MEDIUM-1 v4 + MEDIUM-1 v5 wording)** | Phase 6 plan 책임 |
| contract.md AnalysisDoc bodyNormalizationProfile | **REVERTED (MEDIUM-1 v4)** | Phase 6 plan 책임 |
| extract_rtmw_body_profile_keypoints.py (HIGH-4 v3) | 02-01 | Task 5a, Task 5b |
| **smplx_joints_to_body_profile.py — joints → Profile pure NumPy (HIGH-1 v5)** | 02-01 | Task 5a |
| **extract_smplx_joints_from_video.py — Pod-only NLF→SMPL-X joints (HIGH-1 v5)** | 02-01 | Task 5a |
| **run_body_profile_gap_report.py — 단일 orchestrator (HIGH-2 v5)** | 02-01 | Task 5a (박제) + Task 5b (실행) |
| BodyNormalizationProfile.__post_init__ 5 numeric validator (MEDIUM-2 v5) | 02-01 | Task 1 |
| estimatedHeightScale consumer guard 4 dir (LOW-1 v5) | 02-01 | Task 7 |

### CONTEXT (02-CONTEXT.md Locked Decisions)
| Decision ID | Plan | Task |
|-------------|------|------|
| D-02-01 (RTMW 백본) | 02-01 | Task 3, Task 6 |
| D-02-02 (3-way lockstep) | 02-01 | Task 1 (Phase 2 v1 scope, AnalysisDoc 은 Phase 6) |
| D-02-03 (confidence/warnings) | 02-01 | Task 3 |
| D-02-04 (SMPL-X R&D 만) | 02-01 | Task 5a, Task 5b, Task 6 |
| D-02-05 (자동 측정 우선) | 02-01 | Task 3 |
| D-02-06 (실 영상 BodyProfile v1) | 02-01 | Task 2, Task 2b |

### REVIEWS (02-REVIEWS.md — Codex v4 review)
| Concern | Severity | v5 Task | Notes |
|---|---|---|---|
| **HIGH-1 v5**: `beta_to_body_profile(beta)` weights-free fake | HIGH | Task 5a (smplx_joints_to_body_profile + extract_smplx_joints_from_video), Task 6 (ROADMAP §4 wording) | β path 폐기. joints-based 박제 |
| **HIGH-2 v5**: Task 5b 4-command console (belle 비개발자) | HIGH | Task 5a (run_body_profile_gap_report orchestrator), Task 5b (두 줄 명령) | [[user-beginner-stepwise]] 정합 |
| **MEDIUM-1 v5**: ROADMAP success criterion 2 wording 가 product-path 출력 오독 가능 | MED | Task 6 (success criterion 2 + 4 acceptance text 갱신 — Phase 2 closure vs Phase 6 closure 경계) | 단일 atomic commit |
| **MEDIUM-2 v5**: `__post_init__` 5 numeric 미검증 | MED | Task 1 (validator 확장 — finite + strictly positive), Task 3 (fallback emits 1.0) | 25 validator case + 26 정상 case |
| **MEDIUM-3 v5**: `pose_too_inverted` axis_vector 부호 의존 | MED | Task 2 (fixture axis_vector default), Task 3 (`_is_inverted_frame` pole_axis 인자 폐기 + y-order 직접) | axis_vector flip independence 회귀 테스트 |
| **MEDIUM-4 v5**: 실 RTMW deferral | MED | (변경 없음 — v4 deferral 유지, v5 reaffirm) | Codex v5 acknowledges as honest |
| **LOW-1 v5**: estimatedHeightScale scan 1 dir 한정 | LOW | Task 7 (4 dir scan: sunity_shared + functions + runpod_inference + app/src TS) | 5 vacuous PASS |
| **LOW-2 v5**: brittle `grep -c` verify | LOW | 전 task verify block (scope-anchored awk/python regex) | Task 1/4/6 verify 갱신 |
| HIGH-1 v4 (race-safe local-return) | HIGH | Task 4 | 변경 0 from v4 |
| HIGH-2 v4 (loader + 반환형 mismatch) | HIGH | Task 5a (superseded by HIGH-1 v5) | β path 영구 폐기 |
| MEDIUM-1 v4 (Firestore revert) | MED | Task 4 (Firestore + AnalysisDoc 변경 0) + Task 1 (3-way) | Phase 6 책임 + MEDIUM-1 v5 wording |
| MEDIUM-2 v4 (실 RTMW v1.5 deferred) | MED | Task 6 (ROADMAP §1 acceptance text) | 유지 |
| MEDIUM-3 v4 (consumer lint) | MED | Task 7 (LOW-1 v5 4 dir 확장) | 유지 + 확장 |
| LOW-1 v4 (string/regex 호칭) | LOW | Task 1 | 유지 |
| LOW-2 v4 (AST scanner 3종 literal) | LOW | Task 6 | 유지 |
| HIGH-2 v3 (y-down convention) | HIGH | Task 2 + Task 3 | **superseded by MEDIUM-3 v5** — image-y-order 직접, axis_vector 독립 |
| HIGH-3 v3 (graceful-empty) | HIGH | Task 5a | 유지 |
| HIGH-4 v3 (extract pre-step) | HIGH | Task 5a + 5b | 유지 (orchestrator 내부 phase 1) |
| MEDIUM-1 v3 (AST 스캔 범위) | MED | Task 6 | 유지 + LOW-2 v4 확장 |
| MEDIUM-3 v3 (Task 6 depends-on Task 5a) | MED | Task 6 | 유지 |
| LOW-1 v3 (repo root convention) | LOW | Task 5a + 5b verify | 유지 |
| HIGH-2 v1→v2 (fixture schema) | HIGH | Task 2 | 유지 |
| HIGH-3 v1→v2 (mediapipe 제외) | HIGH | Task 6 | 유지 |
| M1 (synthetic-only) | MED | Task 2b | 유지 |
| M3 (scale 의미) | MED | Task 1 + Task 7 (consumer lint 4 dir) | 유지 + LOW-1 v5 확장 |
| L1 (lockstep 자동화) | LOW | Task 1 (LOW-1 v4 호칭) | 유지 |

### Out of Scope (CONTEXT §1 + §8 — MEDIUM-1 v4 + MEDIUM-1 v5 wording)
- Phase 3 자가입력 → Phase 3
- **Phase 6 다운스트림 + Firestore 저장 + AnalysisDoc 박제 → Phase 6** (MEDIUM-1 v4 + MEDIUM-1 v5 wording — ROADMAP success criterion 2 가 "Phase 2 v1 closure" + "Phase 6 closure" 경계 명시)
- 좌우 비대칭 활용 → Phase 7 (v1 warning 만)
- 다각도 입력 → Phase 4
- SMPL-X 본격 도입 → 영구 R&D
- mediapipe quarantine → 별도 phase

### Gaps Found (v5 박제)

각 v5 신규 concern (2 HIGH + 4 MEDIUM + 2 LOW) → v5 task 매핑은 위 v4→v5 Replan Changelog + 본 REVIEWS 행에 명시. **모든 v5 concern 이 task 에 매핑됨** — silent omission 0.

**HIGH-4 v1→v2 + HIGH-1 v5 + HIGH-2 v5 게이트 박제 (유지 + 확장)**: ROADMAP success criterion 4 실 closure 는 **Task 5b 가 user_setup 게이트** — executor 자동화 불가. Task 5b 는 HIGH-2 v5 박제로 두 줄 명령 (run_body_profile_gap_report + git commit). HIGH-1 v5 박제로 joints-based path (β-only fake 영구 폐기). belle Pod 두 줄 실행 완료 시까지 ROADMAP §4 PASS 표시 X.

**MEDIUM-2 v4 deferral 박제 (유지, v5 reaffirm)**: ROADMAP success criterion 1 의 acceptance text "v1.5 sweep 실행으로 deferred" — Task 6 atomic commit. Codex v5 review 가 "honest deferral" 로 인정.

**MEDIUM-1 v4 revert + MEDIUM-1 v5 wording (확장)**: Firestore 저장 + AnalysisDoc 박제 Phase 6 책임. v5 에서 ROADMAP success criterion 2/4 acceptance text 가 "Phase 2 v1 closure" + "Phase 6 closure" 경계 명시 — silent omission 0.

**MEDIUM-2 v5 contract robustness (신규)**: `BodyNormalizationProfile.__post_init__` 가 5 numeric scale 필드 finite + strictly positive 강제 — contract 경계 robustness 확장.

**MEDIUM-3 v5 axis_vector independence (신규)**: `pose_too_inverted` 가 image-y-order 직접 (pole_axis 부호 독립) — production canonicalization 깨져도 fragile 0.

**LOW-1 v5 + LOW-2 v5 scan + verify 확장 (신규)**: estimatedHeightScale consumer guard 4 dir + TS scan. brittle `grep -c` 폐기, scope-anchored regex.

이외 GOAL/REQ/RESEARCH/CONTEXT 모든 항목 task 매핑됨.
</source_audit>

<output>
Phase 2 완료 시 산출 (v5 박제):

1. `backend/shared/python/sunity_shared/analysis/body_normalization.py` (MEDIUM-2 v5 — 5 numeric validator 확장)
2. `backend/shared/python/sunity_shared/analysis/body_normalization_measurer.py` (MEDIUM-3 v5 y-order + MEDIUM-2 v5 fallback)
3. `backend/tests/fixtures/body_normalization/_factory.py` (HIGH-2 v1→v2)
4. `backend/tests/fixtures/body_normalization/_generate.py` + 5 fixture JSON (MEDIUM-3 v5 image-y-order)
5. `backend/tests/fixtures/body_normalization/test_fixtures_loadable.py`
6. `backend/tests/test_body_normalization_measurer.py` (12 case)
7. `backend/tests/test_body_normalization_profile.py` (MEDIUM-2 v5 — 26 case)
8. `backend/tests/test_body_normalization_lockstep.py` (LOW-1 v4 + MEDIUM-2 v5 numeric contract lockstep)
9. `backend/tests/test_pipeline_body_profile_injection.py` (9 — HIGH-1 v4 race-safe + concurrency + stale + B8)
10. `backend/tests/test_research_import_isolation.py` (5 — MEDIUM-1 v3 + LOW-2 v4)
11. `backend/tests/test_estimated_height_scale_consumer_semantics.py` (LOW-1 v5 — 5 case, 4 dir vacuous)
12. `backend/research/evaluations/compare_body_profile.py` (HIGH-1 v5 — joints-based path)
13. `backend/research/evaluations/extract_rtmw_body_profile_keypoints.py` (HIGH-4 v3)
14. **신규** `backend/research/evaluations/smplx_joints_to_body_profile.py` (HIGH-1 v5 — joints → Profile pure NumPy)
15. **신규** `backend/research/evaluations/extract_smplx_joints_from_video.py` (HIGH-1 v5 — Pod-only NLF→SMPL-X β→joints, CI graceful exit)
16. **신규** `backend/research/evaluations/run_body_profile_gap_report.py` (HIGH-2 v5 — 단일 audited orchestrator)
17. `backend/tests/test_compare_body_profile_smoke.py` (12)
18. `backend/research/evaluations/reports/sweep_rtmw_<date>/keypoints/<video>.json` × 5 (orchestrator phase 1)
19. `backend/research/evaluations/reports/smplx_joints_<date>/<video>.json` × 5 (orchestrator phase 2, HIGH-1 v5)
20. `backend/research/evaluations/reports/smplx_profiles_<date>/<video>.json` × 5 (orchestrator phase 3)
21. `backend/research/evaluations/reports/body_profile_gap_<date>.{json,md}` (orchestrator phase 4)
22. 수정: `docs/contract.md` §7 + `app/src/types/analysis.ts` BodyNormalizationProfile interface + `body_normalization.py` (Task 1 3-way — AnalysisDoc 변경 X, MEDIUM-1 v4)
23. 수정: `backend/functions/pipeline/app.py` `_RTMWNlfCompat.estimate_with_profile` + `_angles_and_body_profile_from_video` (HIGH-1 v4 race-safe, B8 박제, 사이드카 폐기)
24. **변경 X (MEDIUM-1 v4)**: `firestore_admin.py` / `analysis.ts` AnalysisDoc / `docs/contract.md` AnalysisDoc (Phase 6 책임)
25. 수정: `.planning/REQUIREMENTS.md` BODY-01 "MediaPipe" → "RTMW"
26. 수정: `.planning/ROADMAP.md` Phase 2 success criterion 1 v1.5 deferral (MEDIUM-2 v4 유지) + success criterion 2 + 4 Phase 2 vs Phase 6 closure 경계 (MEDIUM-1 v5)
27. **v4 → v5 폐기**: `backend/research/evaluations/nlf_smplx_body_profile_loader.py` 미생성 (v4 plan 박제, v5 영구 폐기 — HIGH-1 v5)

완료 시 `.planning/phases/02-bodynormalizationprofile-rtmw-segment/02-01-SUMMARY.md` 생성.

다음 Phase: Phase 3 (자가입력 BodyProfileInput). Phase 6 (체형 정규화 비교) 가 본 phase 의 `_angles_and_body_profile_from_video` helper + Firestore 저장 + AnalysisDoc 박제 wire-up 책임.
</output>

---

## Total Atomic Commits 요약 (v5 — HIGH-1 joints-based + HIGH-2 orchestrator + MEDIUM-1 wording + MEDIUM-2 validator + MEDIUM-3 y-order + LOW-1 4 dir + LOW-2 scope-anchored)

| # | Task | Commit Template | Files Changed | Type | Concerns fixed |
|---|------|----------------|----------------|------|---|
| 1 | contract.md §7 warnings 5종 + M3 + L1 string/regex + MEDIUM-2 v5 numeric validator | `feat(02): lock warnings enum + scale semantics + numeric validator for BodyNormalizationProfile (3-way lockstep + finite/positive __post_init__, MEDIUM-2 v5 + LOW-1 v4)` | 5 files | feat | HIGH-2 v1→v2 / M3 / L1 / LOW-1 v4 / **MEDIUM-2 v5** / LOW-2 v5 |
| 2 | PoseFrame factory + Synthetic fixture (image-y-order, MEDIUM-3 v5) + smoke + RTMW sanity skip | `test(02): add PoseFrame factory + synthetic fixtures with image-y-order inversion (5 cases, MEDIUM-3 v5 pole_axis-sign-independent)` | 9 files | test | HIGH-2 v1→v2 / **MEDIUM-3 v5** / M1 |
| 3 | body_normalization_measurer.py + 단위 테스트 | `feat(02): add measure_body_profile with image-y-order inversion + positive-scale fallback (BODY-01, MEDIUM-3 v5 + MEDIUM-2 v5)` | 2 files | feat | **MEDIUM-3 v5 + MEDIUM-2 v5** |
| 4 | Pipeline 통합 — race-safe local-return helper + estimate_with_profile | `feat(02): add race-safe _angles_and_body_profile_from_video helper + _RTMWNlfCompat.estimate_with_profile (HIGH-1 v4 — sidecar dropped, B8 lock preserved, Firestore deferred to Phase 6)` | 1 file + 1 test | feat | HIGH-1 v4 + MEDIUM-1 v4 |
| 5a | R&D harness 박제 (joints-based + orchestrator) | `chore(02): R&D harness with joints-based SMPL-X path + single orchestrator (HIGH-1 v5 smplx_joints_to_body_profile + HIGH-2 v5 run_body_profile_gap_report)` | 6 files | chore | **HIGH-1 v5 + HIGH-2 v5** + HIGH-3 v3 + HIGH-4 v3 + LOW-1 v3 |
| 6 | AST isolation 확장 + REQUIREMENTS RTMW + ROADMAP §1 v1.5 deferral + §2/§4 closure boundary | `chore(02): expand AST R&D import isolation + align BODY-01 to RTMW + ROADMAP §1 v1.5 deferral + §2/§4 Phase 2 vs Phase 6 closure boundary (MEDIUM-1 v5 + MEDIUM-2 v4 + LOW-2 v4)` | 3 files | chore | MEDIUM-1 v3 + LOW-2 v4 + MEDIUM-2 v4 + **MEDIUM-1 v5** |
| 7 | estimatedHeightScale consumer semantic guard 4 dir (LOW-1 v5) | `test(02): expand estimatedHeightScale consumer semantic guard to 4 dirs (Python + TS, LOW-1 v5 — vacuous PASS, Phase 6+ load-bearing)` | 1 file | test | MEDIUM-3 v4 + **LOW-1 v5** |
| 5b | belle Pod 두 줄 실행 (orchestrator + commit) | `data(02): SMPL-X vs RTMW body profile gap report (Phase 2 ROADMAP §4)` | 17 files | data | **HIGH-2 v5 + HIGH-1 v5 게이트** |

**총 8개 atomic commit** (Task 5 분할 + Task 7 신규 유지).

**의존 chain**:
```
Task 1 + Task 2 (병렬, Wave 1)
  → Task 3 (Wave 2)
    → Task 4 (Wave 3, race-safe helper) + Task 5a (Wave 3, R&D harness joints-based) + Task 7 (Wave 3, consumer 4 dir lint — Task 1 만 의존) [3 병렬]
      → Task 6 (Wave 4, AST + REQUIREMENTS + ROADMAP — Task 5a 만 의존)  ∥  Task 5b (Wave 4, belle Pod user_setup blocking)
```

**Task 6 / Task 7 가 Task 5b 무관**: belle Pod 게이트가 며칠 걸려도 product-safe cleanup landing 가능.

**autonomous: false** (Task 5b checkpoint).
