---
phase: 02-bodynormalizationprofile-rtmw-segment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/contract.md
  - app/src/types/analysis.ts
  - backend/shared/python/sunity_shared/analysis/body_normalization.py
  - backend/shared/python/sunity_shared/analysis/body_normalization_measurer.py
  - backend/tests/fixtures/body_normalization/_generate.py
  - backend/tests/fixtures/body_normalization/normal_pose_pose_frames.json
  - backend/tests/fixtures/body_normalization/inverted_pose_pose_frames.json
  - backend/tests/fixtures/body_normalization/occluded_leg_pose_frames.json
  - backend/tests/fixtures/body_normalization/short_clip_pose_frames.json
  - backend/tests/fixtures/body_normalization/asymmetric_pose_frames.json
  - backend/tests/test_body_normalization_measurer.py
  - backend/tests/test_pipeline_body_profile_injection.py
  - backend/tests/test_research_import_isolation.py
  - backend/functions/pipeline/app.py
  - backend/research/evaluations/compare_body_profile.py
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - BODY-01
user_setup: []

must_haves:
  truths:
    - "RTMW PoseFrame[]을 측정기에 넘기면 시간 평균·MAD 스무딩된 segment 길이가 산출된다"
    - "BodyNormalizationProfile이 estimatedHeightScale·armScale·legScale·torsoScale·shoulderHipRatio·confidence·warnings 7필드 모두 채워서 반환된다"
    - "낮은 confidence/occlusion/insufficient frames/asymmetric/inverted 5종 warning이 정확한 조건에서 발화된다"
    - "_RTMWNlfCompat가 모든 PoseFrame에 동일한 video-level body_shape를 박제한다"
    - "R&D 비교 harness가 backend/research/에 격리되며 sunity_shared가 backend.research를 import하지 않는다 (AST 단위테스트 강제)"
    - "contract.md §7 warnings 행이 5종 enum으로 갱신되고 TS/Python 주석이 3-way lockstep으로 동시 변경된다"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/body_normalization_measurer.py"
      provides: "measure_body_profile(pose_frames) -> BodyNormalizationProfile 순수 함수"
      contains: "measure_body_profile"
    - path: "backend/tests/fixtures/body_normalization/_generate.py"
      provides: "seed=42 멱등 fixture 생성기 + 5종 JSON"
      contains: "RandomState(42)"
    - path: "backend/tests/test_body_normalization_measurer.py"
      provides: "5 fixture × profile assertion 단위 테스트"
    - path: "backend/tests/test_pipeline_body_profile_injection.py"
      provides: "_RTMWNlfCompat 통합 회귀 테스트"
    - path: "backend/tests/test_research_import_isolation.py"
      provides: "AST 스캔으로 sunity_shared → backend.research import 차단"
    - path: "backend/research/evaluations/compare_body_profile.py"
      provides: "R&D NLF→SMPL-X β vs RTMW segment gap 보고서 CLI"
  key_links:
    - from: "backend/functions/pipeline/app.py::_RTMWNlfCompat.estimate"
      to: "sunity_shared.analysis.body_normalization_measurer.measure_body_profile"
      via: "video-level 1회 호출 + dataclasses.replace로 모든 frame body_shape 박제"
      pattern: "measure_body_profile\\(pose_frames\\)"
    - from: "docs/contract.md §7 warnings"
      to: "app/src/types/analysis.ts BodyNormalizationProfile.warnings + body_normalization.py docstring"
      via: "3-way lockstep (single atomic commit)"
      pattern: "low_keypoint_confidence|occluded_endpoint|insufficient_frames|asymmetric_landmark_count|pose_too_inverted"
---

<objective>
RTMW 133 wholebody 키포인트로부터 신체 segment 길이·비율·confidence·warnings를 자동 산출하는 `measure_body_profile()` 측정기를 박제하고, `_RTMWNlfCompat` 통합으로 모든 PoseFrame에 video-level `BodyNormalizationProfile`이 채워지게 한다. R&D 비교군 (NLF→SMPL-X β) harness는 `backend/research/`에 격리하고 AST import 차단 테스트로 운영 코드 leak을 영구 방지한다.

Purpose: Phase 6 (체형 정규화 비교) / Phase 7 (차이 분류) / Phase 8·9 (힘 패턴) 가 공유하는 입력 schema를 채운다. v1 = 측정기 본체만, Firestore 저장은 Phase 6 통합 시점으로 보류.

Output:
- `body_normalization_measurer.py` 신설 (순수 numpy, scipy/외부 의존 0)
- 5 synthetic fixture + 5 단위 테스트 (normal/inverted/occluded/short/asymmetric)
- `_RTMWNlfCompat` 한 함수 수정 (한 줄 변경 정신)
- contract.md §7 warnings enum 5종 박제 (3-way lockstep 단일 commit)
- `backend/research/evaluations/compare_body_profile.py` R&D 격리 harness
- AST import isolation test (Plan 01-24 정합)
- REQUIREMENTS.md BODY-01 "MediaPipe" → "RTMW" 정합 갱신
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-bodynormalizationprofile-rtmw-segment/02-CONTEXT.md
@.planning/phases/02-bodynormalizationprofile-rtmw-segment/02-RESEARCH.md
@docs/contract.md
@backend/shared/python/sunity_shared/analysis/body_normalization.py
@backend/shared/python/sunity_shared/analysis/pose_frame.py
@backend/shared/python/sunity_shared/analysis/temporal.py
@backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/wholebody_keypoints.py
@backend/shared/python/sunity_shared/analysis/adapters/rtmw_133_to_coco17.py
@backend/functions/pipeline/app.py
@app/src/types/analysis.ts
@backend/tests/test_body_normalization_lockstep.py
@backend/tests/test_body_normalization_profile.py
@backend/research/evaluations/compare_engines.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: contract.md §7 warnings enum 5종 박제 (3-way lockstep, 단일 atomic commit)</name>
  <files>
    docs/contract.md (modify §7 line 342),
    app/src/types/analysis.ts (modify BodyNormalizationProfile.warnings 주석 lines ~380-385),
    backend/shared/python/sunity_shared/analysis/body_normalization.py (modify warnings 필드 docstring)
  </files>
  <action>
    Goal: D-02-02 lockstep 정신 정합 — `warnings` 필드의 허용 enum 5종을 3 파일에 동시 박제. RESEARCH.md "Warnings enum" 섹션의 5종 그대로 사용 — `low_keypoint_confidence`, `occluded_endpoint`, `insufficient_frames`, `asymmetric_landmark_count`, `pose_too_inverted`.

    구현 노트 (RESEARCH.md "BodyNormalizationProfile Schema 3-way" + "Warnings enum"):
    - **docs/contract.md:342** — 현 텍스트 `"측정 품질 이슈 (예: 'short_arm_clip', 'occluded_torso'). 기본값 []."` 를 5종 enum 명시 + 각 의미 한 줄로 교체. 예시 placeholder (`short_arm_clip` / `occluded_torso`) 제거.
    - **app/src/types/analysis.ts** — `warnings: string[]` 필드 위 주석을 한국어 5종 enum 나열 + "Phase 11 coach_writer가 한국어 카피로 번역" 노트 추가. TS 타입은 `string[]` 그대로 (literal union은 Phase 6+ 결정).
    - **body_normalization.py** — `warnings: list[str]` field default factory는 그대로. 클래스 docstring 또는 필드 주석에 5종 enum + 의미 명시. `__post_init__` validator는 v1에서 enum membership 검증 X (D-02-03 정합 — 측정기가 알려진 enum만 출력하면 충분, 다른 모듈이 새 warning 추가 가능성 열어둠).
    - 단일 commit: TS / Python / contract.md 동시 변경 (`test_body_normalization_lockstep.py`가 drift 자동 차단).

    참조: RESEARCH.md "Confidence + Warnings Spec" + "BodyNormalizationProfile Schema (3-way)".

    No code in this plan — implementation in execution. Per D-02-02 / D-02-03.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_body_normalization_lockstep.py -x -v</automated>
    추가 검증: `grep -l "low_keypoint_confidence" docs/contract.md app/src/types/analysis.ts backend/shared/python/sunity_shared/analysis/body_normalization.py | wc -l` → 3 (3 파일 모두 enum 박제 확인).
  </verify>
  <done>
    - 5종 warning enum이 정확히 3 파일에 등장 (grep 검증)
    - `test_body_normalization_lockstep.py` PASS (drift 0)
    - 기존 `short_arm_clip` / `occluded_torso` placeholder 제거됨
    - 단일 commit에 docs/contract.md + analysis.ts + body_normalization.py 동시 변경
  </done>
  <commit-template>
    feat(02): lock warnings enum 5-way for BodyNormalizationProfile (3-way lockstep)
  </commit-template>
  <depends-on>None — Wave 1 진입점</depends-on>
</task>

<task type="auto">
  <name>Task 2: Synthetic fixture 생성기 + 5종 fixture JSON 박제</name>
  <files>
    backend/tests/fixtures/body_normalization/_generate.py (create),
    backend/tests/fixtures/body_normalization/normal_pose_pose_frames.json (create),
    backend/tests/fixtures/body_normalization/inverted_pose_pose_frames.json (create),
    backend/tests/fixtures/body_normalization/occluded_leg_pose_frames.json (create),
    backend/tests/fixtures/body_normalization/short_clip_pose_frames.json (create),
    backend/tests/fixtures/body_normalization/asymmetric_pose_frames.json (create),
    backend/tests/fixtures/body_normalization/__init__.py (create empty)
  </files>
  <action>
    Goal: Task 3 측정기가 검증할 5종 PoseFrame[] fixture를 `numpy.random.RandomState(seed=42)` synthetic 으로 생성. 실 영상 sweep keypoint dump가 없음 (확인됨 — `backend/research/evaluations/reports/sweep_rtmw_20260603_1409/` 에는 `report.json`/`report.md`만 존재, .npz 없음, RESEARCH.md A6 risk 적중). Synthetic 만으로 충분 (RESEARCH.md "Test Fixture Plan" 권장).

    구현 노트 (RESEARCH.md "Test Fixture Plan" + "RTMW 133 Keypoint Segment Mapping"):
    - **`_generate.py`** — 멱등 생성기. `RandomState(seed=42)` 박제. 모든 fixture는 `PoseFrame.from_dict()` 통과 가능한 JSON. body 17 keypoint (COCO-17 인덱스 0~16)만 채움. PoleAxis는 default (axis_vector=(0,1,0), origin=(0.5, 0.5)).
    - **normal_pose** — T=60 frame (6 sec @ 9fps), 정상 자세 (mid_shoulder.y > mid_hip.y), 모든 keypoint conf 0.85~0.95 jitter. 인체 평균 비율 (torso=200px, upper_arm=120px, forearm=110px, thigh=180px, shank=160px, shoulder=140px, hip=100px → shoulder_hip_ratio≈1.4). 노이즈 ±2% gaussian.
    - **inverted_pose** — T=60, mid_shoulder.y < mid_hip.y in 80% frames (인버트 자세). conf 0.6~0.8 (인버트 occlusion). → `pose_too_inverted` warning 기대.
    - **occluded_leg** — T=60, `left_knee` / `right_knee` conf < 0.3 in 70% frames. 나머지 정상. → `occluded_endpoint` warning 기대.
    - **short_clip** — T=20 frame (`MIN_VALID_FRAMES`=5 통과하지만 T<30 임계값 미달). 정상 자세. → `insufficient_frames` warning 기대.
    - **asymmetric_pose** — T=60, 좌측 keypoint conf > 0.9 in 90% frames, 우측 keypoint conf > 0.9 in 50% frames만 (frame 수 차이 40%). → `asymmetric_landmark_count` warning 기대.
    - JSON 형식: `{"frames": [{"frame_index": 0, "timestamp_seconds": 0.0, "keypoints_3d": {"left_shoulder": {"x":..., "y":..., "z":0.0, "confidence":...}, ...}, "raw_keypoints_133": {...}, "pole_axis": {...}}, ...]}`. `raw_keypoints_133` 는 body 17 만 채우고 나머지 117 슬롯은 conf=0 placeholder.
    - 생성 후 fixture JSON을 commit. `_generate.py`는 재실행 가능하지만 fixture는 hand-edit 금지 (멱등성 정합).

    **TODO 주석:** `_generate.py` 상단에 "synthetic = RTMW 실측 분포 근사. v1.5에서 Phase 1 sweep keypoint dump 재추출 후 실 영상 fixture 1종 추가 권장." (RESEARCH.md A6).

    참조: `backend/shared/python/sunity_shared/analysis/pose_frame.py` `PoseFrame.from_dict` schema.

    No code in this plan.
  </action>
  <verify>
    <automated>cd backend && python -m tests.fixtures.body_normalization._generate --check && pytest tests/fixtures/body_normalization/ --collect-only 2>&1 | grep -v 'no tests collected' || true</automated>
    추가: `ls backend/tests/fixtures/body_normalization/*.json | wc -l` → 5.
    멱등성: `_generate.py` 두번 실행해도 byte-identical JSON.
  </verify>
  <done>
    - 5 fixture JSON 파일 존재
    - 각 fixture가 `PoseFrame.from_dict()` 로 load 가능 (smoke import 통과)
    - `_generate.py --check` 가 seed=42 멱등성 확인 (재실행해도 동일 output)
    - 각 fixture JSON 평균 size < 200KB
  </done>
  <commit-template>
    test(02): add synthetic PoseFrame fixtures for body normalization measurer (5 cases, seed=42)
  </commit-template>
  <depends-on>None — Task 1과 병렬 가능, 그러나 commit 순서는 Task 1 → Task 2 권장</depends-on>
</task>

<task type="auto">
  <name>Task 3: body_normalization_measurer.py 본체 + 단위 테스트 (5 fixture)</name>
  <files>
    backend/shared/python/sunity_shared/analysis/body_normalization_measurer.py (create),
    backend/tests/test_body_normalization_measurer.py (create)
  </files>
  <action>
    Goal: `measure_body_profile(pose_frames: list[PoseFrame], *, conf_gate=0.5, mad_k=3.0, min_valid_frames=5, low_conf_gate=0.4) -> BodyNormalizationProfile` 순수 함수 박제. numpy only (scipy 의존 0). Phase 1 `temporal.py` MAD 패턴 재사용.

    구현 노트 (RESEARCH.md "Smoothing 알고리즘" + "Length Normalization Unit" + "Confidence + Warnings Spec"):
    1. **상수**: `SEGMENT_PAIRS` dict (RESEARCH.md `RTMW 133 Keypoint Segment Mapping` 그대로), `TORSO_TOP_KPS`, `TORSO_BOTTOM_KPS`, `WARNING_CODES` frozenset, 임계값 5종 module 상수 (`CONF_GATE_PER_FRAME=0.5`, `MIN_VALID_FRAMES=5`, `MAX_BAD_FRAME_RATIO=0.6`, `MAD_K=3.0`, `LOW_CONFIDENCE_GATE=0.4`, `INVERTED_FRAME_RATIO=0.5`, `ASYMMETRIC_FRAME_RATIO=0.3`).
    2. **`_measure_segment_per_frame(pose_frames, kp_start, kp_end) -> (lengths: np.ndarray, confs: np.ndarray)`** — RESEARCH 의 코드 sketch 그대로. `sqrt(dx²+dy²)` 만 사용 (z 무시 — Pitfall 2 정합 RESEARCH "Common Pitfalls" + Risk 3 mitigation).
    3. **`_measure_torso(pose_frames) -> (lengths, confs)`** — mid_shoulder = (left_shoulder + right_shoulder)/2, mid_hip = (left_hip + right_hip)/2 의 4 keypoint 평균 후 길이. confidence = min of 4 endpoints.
    4. **`_robust_median(lengths, confs, conf_threshold, mad_k) -> (median, segment_confidence)`** — RESEARCH 코드 sketch 그대로 MAD k=3 rejection + median + 자체 confidence 계산.
    5. **`measure_body_profile(...)`**:
       - 모든 SEGMENT_PAIRS + torso 길이 측정
       - 좌우 평균 (left_upper_arm + right_upper_arm)/2 → upper_arm_median, 동일하게 forearm/thigh/shank
       - `torso_length` = `_measure_torso` 의 robust median
       - `arm_scale = (upper_arm_median + forearm_median) / torso_length` (절반 단위 — RESEARCH 공식)
       - `leg_scale = (thigh_median + shank_median) / torso_length`
       - `torso_scale = 1.0` (self-reference 정의)
       - `shoulder_hip_ratio = shoulder_width_median / hip_width_median`
       - `estimated_height_scale = (arm_scale + leg_scale + 1.0) / 3` (placeholder, A2 가정 — Phase 6 검토)
       - `profile.confidence` = 11 segment confidence 의 단순 mean
       - **Warnings 판정**:
         - `low_keypoint_confidence`: profile.confidence < 0.4
         - `occluded_endpoint`: 임의 segment 가 endpoint conf < 0.5 in > 60% frames
         - `insufficient_frames`: T < 30
         - `asymmetric_landmark_count`: 좌측 valid frame 수 vs 우측 valid frame 수 차이 > 30%
         - `pose_too_inverted`: mid_shoulder.y < mid_hip.y in > 50% frames
       - NaN 방어: torso_length == NaN 이거나 valid frames < `MIN_VALID_FRAMES` → 모든 scale 1.0 + confidence=0 + warnings=["insufficient_frames"] + 다른 warning 도 가능
       - finite 강제: `__post_init__` 통과 위해 NaN/inf → 1.0 폴백
    6. **Logging**: stdlib `logging.getLogger(__name__)` — `log.info("body_profile arm_scale=%.3f leg_scale=%.3f conf=%.3f warnings=%s", ...)`. Secret 없음.
    7. **타입 힌트**: `from __future__ import annotations`, dataclass return type.

    **단위 테스트 (`test_body_normalization_measurer.py`)** — 5 fixture × per-case 검증:
    - `test_normal_pose_profile_valid`: 정상 fixture → profile.confidence > 0.7, warnings == [], arm_scale ∈ [0.8, 1.4], leg_scale ∈ [1.0, 1.8], shoulder_hip_ratio ∈ [1.1, 1.6]
    - `test_inverted_pose_warning`: inverted fixture → "pose_too_inverted" in warnings
    - `test_occluded_leg_warning`: occluded fixture → "occluded_endpoint" in warnings, leg_scale segment confidence < 0.5
    - `test_short_clip_warning`: short fixture → "insufficient_frames" in warnings
    - `test_asymmetric_warning`: asymmetric fixture → "asymmetric_landmark_count" in warnings
    - `test_z_axis_ignored_when_zero`: 정상 fixture 의 모든 z=0 으로 override 후 측정해도 arm_scale 동일 ± 1% (Pitfall 2 검증)
    - `test_profile_validator_passes`: 5 fixture × `BodyNormalizationProfile.__post_init__` 통과 (NaN 출력 X)
    - `test_idempotent`: 동일 fixture 2번 측정 → byte-identical output (deterministic)

    참조: RESEARCH.md "Pure Algorithm + Lazy Adapter 경계" 패턴 + `analysis/temporal.py:_column_outliers`.

    No code in this plan.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_body_normalization_measurer.py -x -v</automated>
    추가: `python -c "from sunity_shared.analysis.body_normalization_measurer import measure_body_profile; help(measure_body_profile)"` (import 가능 + docstring 존재).
    회귀: `pytest tests/test_body_normalization_profile.py tests/test_body_normalization_lockstep.py -x` (Phase 1 박제 회귀 0).
  </verify>
  <done>
    - 8개 단위 테스트 모두 PASS
    - `measure_body_profile` 가 5 fixture 에서 BodyNormalizationProfile 반환 (NaN/inf 출력 X)
    - 정상 fixture 에서 warnings == []
    - 4 비정상 fixture 가 각자 1개 이상 정확한 warning 발화
    - scipy import 없음 (`grep -r "import scipy" backend/shared/python/sunity_shared/analysis/body_normalization_measurer.py` → 0)
    - Phase 1 기존 테스트 전부 PASS (회귀 0)
  </done>
  <commit-template>
    feat(02): add measure_body_profile RTMW segment measurer with MAD smoothing + 5-case warnings (BODY-01)
  </commit-template>
  <depends-on>Task 1 (warnings enum 박제), Task 2 (5 fixture)</depends-on>
</task>

<task type="auto">
  <name>Task 4: Pipeline 통합 + 회귀 테스트 (_RTMWNlfCompat body_shape 주입)</name>
  <files>
    backend/functions/pipeline/app.py (modify _RTMWNlfCompat.estimate around line 202),
    backend/tests/test_pipeline_body_profile_injection.py (create)
  </files>
  <action>
    Goal: `_RTMWNlfCompat.estimate(frames)` 가 RTMW pose_frames 생성 후 1회 `measure_body_profile()` 호출 → `dataclasses.replace(pf, body_shape=profile)` 로 모든 frame 에 video-level profile 박제. PoleAxis D-10 패턴 정합.

    구현 노트 (RESEARCH.md "Pipeline Integration Plan"):
    - **`backend/functions/pipeline/app.py:202`** `_RTMWNlfCompat.estimate(frames)`:
      ```
      pose_frames = self._engine.estimate(frames, self._default_pole)
      profile = measure_body_profile(pose_frames)
      pose_frames = [dataclasses.replace(pf, body_shape=profile) for pf in pose_frames]
      return to_coco17_array(pose_frames)
      ```
      - `import dataclasses` 가 모듈 top-level 에 이미 있는지 확인 (없으면 추가).
      - `from sunity_shared.analysis.body_normalization_measurer import measure_body_profile` 추가.
      - **lazy import** 패턴 정합 — 본 함수 안에서 import (Pipeline 콜드스타트 시점 최소화 + scipy/torch 의존성 늘리지 않음). 단, body_normalization_measurer 는 numpy only 라 module top import 도 OK.
    - **Firestore 저장 X**: RESEARCH.md "Pipeline Integration Plan" + CONTEXT.md "v1 scope = 측정기 본체" 정합. body_shape 는 RAM only. Phase 6 통합 시 별도 plan.
    - **회귀 위험**: `to_coco17_array(pose_frames)` 의 output shape `(T,17,4)` 는 body_shape 무관. 기존 angle/score 계산 path 영향 0.
    - Pitfall 3 (`dataclasses.replace` frozen 우회) 정합 — 직접 attribute 할당 금지.

    **통합 테스트 (`test_pipeline_body_profile_injection.py`)**:
    - `test_rtmwnlfcompat_injects_body_shape`: fake `_engine` (3 PoseFrame return) → `_RTMWNlfCompat.estimate(frames)` 호출 → 모든 frame 의 `body_shape` 가 동일 instance (`id()` 동일 또는 dataclass 동등성)
    - `test_body_shape_is_valid_profile`: 주입된 body_shape 가 `BodyNormalizationProfile` instance + confidence ∈ [0, 1]
    - `test_to_coco17_array_unaffected`: `to_coco17_array` output shape (T,17,4) 가 body_shape 주입 전후 동일
    - `test_short_frames_graceful_degrade`: T=3 frame (MIN_VALID_FRAMES 미달) → measure_body_profile 이 confidence=0 + insufficient_frames warning 폴백, exception 발생 X, pose_frames 정상 반환

    참조: RESEARCH.md "Pipeline Integration Plan" 코드 sketch.

    No code in this plan.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_pipeline_body_profile_injection.py -x -v && pytest tests/ -x</automated>
    회귀: 기존 pipeline 테스트 전부 PASS.
  </verify>
  <done>
    - 4개 통합 테스트 모두 PASS
    - 기존 `tests/` suite 전체 회귀 0
    - `_RTMWNlfCompat.estimate` 가 body_shape 채워진 PoseFrame 반환 (모든 frame 동일 profile)
    - Firestore schema 변경 0 (RAM only)
    - lazy import 또는 top-level import 정합
  </done>
  <commit-template>
    feat(02): inject video-level BodyNormalizationProfile in _RTMWNlfCompat (BODY-01 pipeline integration)
  </commit-template>
  <depends-on>Task 3 (measure_body_profile 함수)</depends-on>
</task>

<task type="auto">
  <name>Task 5: R&D 비교 harness compare_body_profile.py + smoke test</name>
  <files>
    backend/research/evaluations/compare_body_profile.py (create),
    backend/tests/test_compare_body_profile_smoke.py (create)
  </files>
  <action>
    Goal: NLF→SMPL-X β path 와 RTMW segment path 의 BodyNormalizationProfile 갭을 보고하는 CLI 박제. **`backend/research/`에 격리** — `sunity_shared` 무관. D-02-04 정합 (SMPL-X 상업 라이선스 X — R&D 만).

    구현 노트 (RESEARCH.md "R&D 비교 Harness"):
    - **파일 헤더 docstring**: "D-02-04 박제: NLF/SMPL-X 모듈 import 는 본 파일에서만. sunity_shared 무관. [CITED: license-blocklist-pose memory — SMPL-X 상업 불가 / R&D 만] 실행 환경: belle 사내 RunPod GPU pod."
    - **CLI argparse**:
      - `--rtmw-keypoints-dir` (Phase 1 sweep dump 경로, 향후 재추출 시 사용)
      - `--nlf-keypoints-dir` (R&D NLF→SMPL-X path 출력)
      - `--videos` (영상 리스트, default = `ref-foxtop ref-foxtop-split ref-invert ref-sideway-spin ref-climb`)
      - `--output` (default `backend/research/evaluations/reports/body_profile_gap_{date}.json`)
    - **함수 구조**:
      - `load_rtmw_keypoints(path)` → `list[PoseFrame]`
      - `load_nlf_smplx_keypoints(path)` → `list[PoseFrame]` (NLF→SMPL-X β path — 실 구현은 belle Pod 환경에서 NLF 모듈 직접 import. 본 PR 에서는 **stub + TODO** 로 박제 — placeholder 가 `NotImplementedError("belle pod 에서 NLF mock 실데이터로 교체")` 발생)
      - `compute_profile_gap(rtmw_profile, smplx_profile) -> dict` — abs diff per field
      - `main()` → JSON + markdown 보고서 산출
    - `compare_engines.py` 패턴 정합 — `compare_engines.py` 의 reporting 구조 (verdict + aggregate) 재사용.
    - **운영 코드 import 차단**: `from sunity_shared.analysis.body_normalization_measurer import measure_body_profile` 는 OK (RTMW path 가 sunity_shared 의 측정기 호출). NLF/SMPL-X import 는 본 파일 안에서만.
    - **SMPL-X β → BodyNormalizationProfile 변환**: SMPL-X β 파라미터 (10D shape) → segment 길이 → RTMW와 같은 schema 로 변환. 본 PR 은 stub. 실 belle Pod 실행은 별도 PR.

    **Smoke test (`test_compare_body_profile_smoke.py`)** — NLF/SMPL-X 실제 호출 X (모킹):
    - `test_argparse_no_args`: 인자 없으면 도움말 표시 (sys.exit 0 또는 2)
    - `test_compute_profile_gap_zero`: 동일 profile 2개 입력 → 모든 diff == 0
    - `test_compute_profile_gap_nonzero`: 다른 profile → abs diff 정확
    - `test_load_rtmw_smoke`: 빈 디렉터리 → graceful empty list (raise X)
    - `test_load_nlf_smplx_raises_stub`: NLF stub 함수가 `NotImplementedError` raise (실 구현 전까지 안전망)

    참조: RESEARCH.md "R&D 비교 Harness" + `backend/research/evaluations/compare_engines.py` 패턴.

    No code in this plan.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_compare_body_profile_smoke.py -x -v && python -m backend.research.evaluations.compare_body_profile --help 2>&1 | grep -q "rtmw-keypoints-dir"</automated>
  </verify>
  <done>
    - 5개 smoke test 모두 PASS
    - `--help` 실행 시 CLI 인자 노출
    - NLF/SMPL-X import 가 본 파일 안에서만 (Task 6 의 AST 테스트로 강제)
    - 파일 헤더에 D-02-04 + license-blocklist-pose memory 인용
    - belle Pod 실행 미완 — `NotImplementedError` stub 박제 (향후 별 PR)
  </done>
  <commit-template>
    chore(02): scaffold R&D compare_body_profile harness (R&D isolation, NLF/SMPL-X stub)
  </commit-template>
  <depends-on>Task 3 (BodyNormalizationProfile 측정기 — gap 계산에 사용)</depends-on>
</task>

<task type="auto">
  <name>Task 6: AST import isolation 테스트 + REQUIREMENTS.md BODY-01 정합 갱신</name>
  <files>
    backend/tests/test_research_import_isolation.py (create),
    .planning/REQUIREMENTS.md (modify line 30)
  </files>
  <action>
    Goal:
    (1) AST 스캔으로 `sunity_shared/**/*.py` 가 `backend.research` 또는 `from research` import 시 fail 하는 자동 차단 테스트 박제 (Plan 01-24 정합).
    (2) REQUIREMENTS.md BODY-01 의 "MediaPipe" → "RTMW" 정합 갱신.

    구현 노트:

    **(1) `test_research_import_isolation.py`**:
    - `ast.parse` 로 `backend/shared/python/sunity_shared/` 하위 모든 `.py` 파일 스캔.
    - 금지 패턴 검출:
      - `import research`
      - `import backend.research`
      - `from research import ...`
      - `from backend.research import ...`
      - `from research.X import ...`
      - `from backend.research.X import ...`
    - 검출 시 `assert False, f"sunity_shared module {path} illegally imports backend.research: {node}"`.
    - 테스트 1개 (`test_sunity_shared_does_not_import_research`).
    - 추가: NLF / SMPL-X / smplx / mediapipe import 도 차단 (RESEARCH.md "Architectural Responsibility Map" R&D 격리 정합):
      - `nlf`, `smplx`, `smpl_x`, `mediapipe` module name 직접 import 차단.
      - 테스트 2개: `test_sunity_shared_does_not_import_research`, `test_sunity_shared_does_not_import_nlf_or_smplx`.
    - **운영 path 만 검증** — `backend/tests/` / `backend/research/` / `backend/functions/` 는 검증 대상 X (테스트와 R&D 가 R&D 모듈 import 허용).
    - pytest 실행 시 < 1초 (순수 AST).

    **(2) REQUIREMENTS.md line 30 갱신**:
    - 현 텍스트: `"**BODY-01**: MediaPipe 키포인트로부터 신체 segment 길이·비율·좌우 비대칭이 자동 추출되어 ...`
    - 갱신: `"**BODY-01**: RTMW 133 wholebody 키포인트로부터 ...`
    - 뒷부분 `SMPL-X β 비교군은 R&D 평가 스크립트에서만 갭 보고 (제품 코드 비호출)` 는 유지.
    - 추가 footer note: `"(2026-06-07 Phase 2 plan 01 — RTMW pivot 정합, MediaPipe → RTMW 갱신)"`.
    - line 27 POSE-01 도 "MediaPipe" 언급 있음 — **본 task scope X** (POSE-01 은 Phase 1 책임, 이미 RTMW pivot 박제됨). BODY-01 1줄만 변경.

    참조: RESEARCH.md "R&D 비교 Harness" + CONTEXT.md "Requirements" + Plan 01-24 정합.

    No code in this plan.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_research_import_isolation.py -x -v</automated>
    추가: `grep -n "RTMW" .planning/REQUIREMENTS.md | grep "BODY-01"` → 1 (line 30 RTMW 박제 확인).
    추가: `grep "MediaPipe" .planning/REQUIREMENTS.md | grep "BODY-01"` → 0 (BODY-01 에서 MediaPipe 제거 확인).
  </verify>
  <done>
    - 2개 AST 테스트 PASS
    - sunity_shared 의 backend.research / NLF / SMPL-X / mediapipe import 0 확인
    - REQUIREMENTS.md BODY-01 (line 30) 가 RTMW 로 갱신, MediaPipe 제거
    - REQUIREMENTS.md footer note (2026-06-07 RTMW pivot 정합) 추가
  </done>
  <commit-template>
    chore(02): enforce sunity_shared R&D import isolation via AST + align BODY-01 to RTMW
  </commit-template>
  <depends-on>Task 5 (R&D harness 가 존재해야 import isolation 의 의미 있음)</depends-on>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RunPod Pod ↔ Lambda | RTMW pose_frames 가 RunPod 에서 측정 후 Lambda 가 결과 받음 — Phase 2 는 내부 호출만 (외부 입력 X) |
| sunity_shared (운영) ↔ backend.research (R&D) | 라이선스 격리 (SMPL-X 상업 불가) — import leak 시 법적 리스크 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | BodyNormalizationProfile confidence 값이 [0, 1] 범위 벗어남 | mitigate | dataclass `__post_init__` validator 이미 박제 (Phase 1 산출). 측정기 출력 `min(1.0, ...)` 강제. NaN → 0.0 폴백 |
| T-02-02 | Repudiation / 법적 | sunity_shared 가 backend.research 또는 SMPL-X / NLF 모듈 import (라이선스 위반) | mitigate | Task 6 AST 단위테스트 자동 차단. CI gate. Plan 01-24 `.samignore` 와 정합 |
| T-02-03 | Denial of Service | 인버트/짧은 영상에서 NaN profile 출력 → 다운스트림 (Phase 6) crash | mitigate | `insufficient_frames` warning + confidence=0 + scale=1.0 폴백. `__post_init__` 가 finite 강제. Task 3 `test_short_frames_graceful_degrade` 검증 |
| T-02-04 | Information Disclosure | R&D 보고서 (`body_profile_gap_*.json`) 에 사용자 영상 메타데이터 leak | accept | belle Pod 환경 (내부) 만 실행. 보고서는 `backend/research/evaluations/reports/` (private repo). 외부 노출 path 없음 |

> 본 phase 는 외부 입력 X (Lambda HTTP API 없음, 모두 Pod 내부 함수). V2/V3/V4/V6 적용 X.
</threat_model>

<verification>
## Phase-level 검증 (전 task 완료 후)

1. **단위 테스트 전체**: `cd backend && pytest tests/ -x` — 회귀 0
2. **3-way lockstep 자동 검증**: `pytest tests/test_body_normalization_lockstep.py -x` PASS
3. **Measurer 5 fixture**: `pytest tests/test_body_normalization_measurer.py -x` PASS (8 case)
4. **Pipeline 통합**: `pytest tests/test_pipeline_body_profile_injection.py -x` PASS (4 case)
5. **R&D 격리**: `pytest tests/test_research_import_isolation.py -x` PASS (2 case)
6. **R&D harness smoke**: `pytest tests/test_compare_body_profile_smoke.py -x` PASS (5 case)
7. **REQUIREMENTS.md 정합**: BODY-01 텍스트 grep — "RTMW" 등장 + "MediaPipe" 미등장
8. **의존성 검증**: `grep -r "import scipy" backend/shared/python/sunity_shared/analysis/body_normalization_measurer.py` → 0 (numpy only)
9. **콘솔 점검**: belle Pod 에서 RTMW estimate → body_shape 채워진 PoseFrame 출력 (수동 확인, 옵션 — Pod 환경 있을 때만)
</verification>

<success_criteria>
## ROADMAP Success Criteria 매핑

| Criterion | Task | 검증 |
|-----------|------|------|
| 1. RTMW 키포인트에서 segment 길이가 시간 평균으로 안정적으로 추출 (jitter 스무딩) | Task 3 (MAD smoothing) | `test_normal_pose_profile_valid` |
| 2. BodyNormalizationProfile 7필드 자동 산출 | Task 3 | `test_profile_validator_passes` |
| 3. 낮은 confidence 시 warnings 표기 (단정 X) | Task 3 (5종 warning) | `test_inverted_pose_warning`, `test_occluded_leg_warning`, `test_short_clip_warning`, `test_asymmetric_warning` |
| 4. R&D 비교군 NLF→SMPL-X β 갭 보고서 (제품 코드 비호출) | Task 5 (compare_body_profile) + Task 6 (AST 차단) | `test_compare_body_profile_smoke`, `test_research_import_isolation` |
| 5. 데이터 계약 3-way lockstep | Task 1 | `test_body_normalization_lockstep` |

**v1 Done**: 5 success criteria PASS + Phase 1 회귀 0 + Pipeline 통합 verified + REQUIREMENTS.md BODY-01 RTMW 정합.
</success_criteria>

<source_audit>
## Multi-Source Coverage Audit

### GOAL (ROADMAP)
| Item | Plan | Task |
|------|------|------|
| RTMW 키포인트 → BodyNormalizationProfile 자동 출력 | 02-01 | Task 3 |
| 두 엔진 (체형 보정 + 힘 패턴) 의 공유 입력 | 02-01 | Task 4 (pipeline 주입) |
| SMPL-X β R&D 비교군만 | 02-01 | Task 5, 6 |

### REQ (REQUIREMENTS.md)
| Req ID | Plan | Task |
|--------|------|------|
| BODY-01 | 02-01 | Task 3 (측정기) + Task 4 (통합) + Task 6 (텍스트 RTMW 정합) |

### RESEARCH (02-RESEARCH.md)
| Feature | Plan | Task |
|---------|------|------|
| body_normalization_measurer.py (순수 numpy) | 02-01 | Task 3 |
| MAD outlier + robust median smoothing | 02-01 | Task 3 |
| torso self-reference normalize | 02-01 | Task 3 |
| 5종 warnings enum | 02-01 | Task 1 (3-way) + Task 3 (판정) |
| 5 synthetic fixture (seed=42) | 02-01 | Task 2 |
| `_RTMWNlfCompat` body_shape 주입 | 02-01 | Task 4 |
| R&D 비교 harness compare_body_profile.py | 02-01 | Task 5 |
| AST import isolation (Plan 01-24 정합) | 02-01 | Task 6 |
| Phase 1 sweep keypoint dump 재추출 (A6) | DEFERRED to v1.5 | Task 2 노트 박제 — synthetic 으로 v1 충족 |
| Firestore 저장 (선택) | OUT OF SCOPE — Phase 6 | RESEARCH.md "Plan Task Suggestions" Task 7 정합 |

### CONTEXT (02-CONTEXT.md Locked Decisions)
| Decision ID | Plan | Task |
|-------------|------|------|
| D-02-01 (RTMW 운영 백본, MediaPipe X) | 02-01 | Task 3 (RTMW 만 측정), Task 6 (REQUIREMENTS RTMW 갱신) |
| D-02-02 (3-way lockstep) | 02-01 | Task 1 (단일 atomic commit) |
| D-02-03 (confidence/warnings 항상 출력) | 02-01 | Task 3 (warnings 판정 + 폴백) |
| D-02-04 (SMPL-X R&D 만) | 02-01 | Task 5 (R&D 격리), Task 6 (import 차단) |
| D-02-05 (자동 측정 우선) | 02-01 | Task 3 (자동 측정기 본체) |
| D-02-06 (실 영상 BodyProfile 만 v1) | 02-01 | Task 2 (synthetic fixture, 실영상 v1.5) |

### Out of Scope (CONTEXT §1)
- Phase 3 자가입력 → Phase 3
- Phase 6 다운스트림 활용 → Phase 6
- 좌우 비대칭 활용 → Phase 7 (v1 은 warning 만)
- 다각도 입력 → Phase 4
- SMPL-X 본격 도입 → 영구 R&D

### Gaps Found
**없음**. 모든 GOAL/REQ/RESEARCH/CONTEXT 항목이 task 에 매핑됨. RESEARCH Task 7 (Firestore 저장) 는 v1 scope 의도적 보류 — CONTEXT.md "v1 = 측정기 본체" 정합.
</source_audit>

<output>
Phase 2 가 완료되면 다음 산출:

1. `backend/shared/python/sunity_shared/analysis/body_normalization_measurer.py` — 순수 함수 측정기 (Phase 6/7/8/9 의 공유 입력)
2. `backend/tests/fixtures/body_normalization/` — 5 synthetic fixture + 멱등 생성기
3. `backend/tests/test_body_normalization_measurer.py` — 8개 단위 테스트
4. `backend/tests/test_pipeline_body_profile_injection.py` — 4개 통합 테스트
5. `backend/tests/test_research_import_isolation.py` — AST 자동 차단 2개 테스트
6. `backend/research/evaluations/compare_body_profile.py` — R&D 비교 harness (stub + belle Pod 실행 대기)
7. `backend/tests/test_compare_body_profile_smoke.py` — 5개 smoke
8. 수정: `docs/contract.md` §7 warnings 5 enum + `app/src/types/analysis.ts` 주석 + `body_normalization.py` docstring (3-way lockstep)
9. 수정: `backend/functions/pipeline/app.py` `_RTMWNlfCompat.estimate` (한 함수 본체 한 줄 변경 수준)
10. 수정: `.planning/REQUIREMENTS.md` BODY-01 "MediaPipe" → "RTMW"

완료 시 `.planning/phases/02-bodynormalizationprofile-rtmw-segment/02-01-SUMMARY.md` 생성.

다음 Phase: Phase 3 (자가입력 BodyProfileInput) — BODY-01 완료 후 unblock.
</output>

---

## Total Atomic Commits 요약

| # | Task | Commit Template | Files Changed | Type |
|---|------|----------------|----------------|------|
| 1 | contract.md §7 warnings enum 5종 박제 (3-way lockstep) | `feat(02): lock warnings enum 5-way for BodyNormalizationProfile (3-way lockstep)` | 3 files | feat |
| 2 | Synthetic fixture 생성기 + 5 fixture JSON | `test(02): add synthetic PoseFrame fixtures for body normalization measurer (5 cases, seed=42)` | 7 files (5 JSON + generator + __init__) | test |
| 3 | body_normalization_measurer.py 본체 + 단위 테스트 | `feat(02): add measure_body_profile RTMW segment measurer with MAD smoothing + 5-case warnings (BODY-01)` | 2 files | feat |
| 4 | Pipeline 통합 + 회귀 테스트 (`_RTMWNlfCompat`) | `feat(02): inject video-level BodyNormalizationProfile in _RTMWNlfCompat (BODY-01 pipeline integration)` | 2 files | feat |
| 5 | R&D 비교 harness `compare_body_profile.py` + smoke | `chore(02): scaffold R&D compare_body_profile harness (R&D isolation, NLF/SMPL-X stub)` | 2 files | chore |
| 6 | AST import isolation + REQUIREMENTS BODY-01 RTMW 갱신 | `chore(02): enforce sunity_shared R&D import isolation via AST + align BODY-01 to RTMW` | 2 files | chore |

**총 6개 atomic commit**. 평균 commit 당 2~3 파일. 의존 chain: `Task 1 + Task 2 (병렬) → Task 3 → Task 4 + Task 5 (병렬) → Task 6`.
