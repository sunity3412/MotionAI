---
phase: 02-bodynormalizationprofile-rtmw-segment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/contract.md
  - app/src/types/analysis.ts
  - backend/shared/python/sunity_shared/analysis/body_normalization.py
  - backend/shared/python/sunity_shared/analysis/body_normalization_measurer.py
  - backend/tests/fixtures/body_normalization/__init__.py
  - backend/tests/fixtures/body_normalization/_factory.py
  - backend/tests/fixtures/body_normalization/_generate.py
  - backend/tests/fixtures/body_normalization/normal_pose_pose_frames.json
  - backend/tests/fixtures/body_normalization/inverted_pose_pose_frames.json
  - backend/tests/fixtures/body_normalization/occluded_leg_pose_frames.json
  - backend/tests/fixtures/body_normalization/short_clip_pose_frames.json
  - backend/tests/fixtures/body_normalization/asymmetric_pose_frames.json
  - backend/tests/fixtures/body_normalization/test_fixtures_loadable.py
  - backend/tests/test_body_normalization_measurer.py
  - backend/tests/test_pipeline_body_profile_injection.py
  - backend/tests/test_research_import_isolation.py
  - backend/tests/test_estimated_height_scale_consumer_semantics.py
  - backend/tests/test_body_normalization_lockstep.py
  - backend/functions/pipeline/app.py
  - backend/research/evaluations/compare_body_profile.py
  - backend/research/evaluations/extract_rtmw_body_profile_keypoints.py
  - backend/research/evaluations/nlf_smplx_body_profile_loader.py
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - BODY-01
user_setup:
  - service: runpod-gpu-pod
    why: "ROADMAP success criterion 4 (R&D NLF→SMPL-X β BodyNormalizationProfile 갭 보고서) 충족은 belle 사내 RunPod GPU pod 에서 (a) RTMW 5영상 keypoint dump 추출 + (b) NLF/SMPL-X 실데이터 실행 둘 다 필요. v4 박제 (HIGH-2 v4): NLF→SMPL-X β path 본체 구현 + β→BodyNormalizationProfile 변환 math 가 Task 5a 에 박제됨. belle 의 5b 는 박제된 스크립트 실행 + git commit 뿐 (설계/구현 결정 0)."
    env_vars:
      - name: RUNPOD_AUTH_TOKEN
        source: "belle pod env (이미 박제)"
      - name: RTMW_ONNX_PATH
        source: "belle pod env (이미 박제 — Pod 의 rtmw end2end.onnx 절대 경로)"
      - name: YOLOX_ONNX_PATH
        source: "belle pod env (이미 박제 — rtmlib detector 의존)"
      - name: NLF_MODEL_PATH
        source: "belle pod env (이미 박제 — Pod 의 nlf_l_multi.torchscript)"
      - name: SMPLX_MODEL_PATH
        source: "belle 사내 NLF→SMPL-X R&D weights (PS:License 1.0 비상업)"
    dashboard_config:
      - task: "v4 — 모든 단계가 박제 스크립트 실행. 설계/구현 결정 0. Step (a) — `python -m backend.research.evaluations.extract_rtmw_body_profile_keypoints --videos ref-foxtop ref-foxtop-split ref-invert ref-sideway-spin ref-climb --output-dir backend/research/evaluations/reports/sweep_rtmw_$(date +%Y%m%d)/keypoints/`. Step (b) — `python -m backend.research.evaluations.nlf_smplx_body_profile_loader --videos <same 5> --output-dir backend/research/evaluations/reports/nlf_smplx_$(date +%Y%m%d)/`. Step (c) — `python -m backend.research.evaluations.compare_body_profile --rtmw-keypoints-dir <step a output> --nlf-profiles-dir <step b output> --output backend/research/evaluations/reports/body_profile_gap_$(date +%Y%m%d).json`. Step (d) — `git add backend/research/evaluations/reports/sweep_rtmw_*/keypoints/*.json backend/research/evaluations/reports/nlf_smplx_*/*.json backend/research/evaluations/reports/body_profile_gap_*.{json,md} && git commit -m 'data(02): belle Pod 5-video gap report'`. All commands run from repo root /workspace/SunityMotion."
        location: "belle RunPod pod 1ablelgbtrzcgb (현 활성)"

must_haves:
  truths:
    - "신규 helper `_angles_and_body_profile_from_video(bucket, key) -> tuple[np.ndarray, BodyNormalizationProfile | None]` 가 박제되며 사이드카 mutable state 0 (race-safe). B8 박제 (_angles_from_video, _angles_and_video_path_from_video 시그너처) 무변경. (per HIGH-1 v4)"
    - "동시 background task 2개가 barrier 로 인터리브되어도 각각 자기 frame 의 profile 만 받는다 (concurrency 회귀 테스트) (per HIGH-1 v4)"
    - "estimate 가 예외를 던지면 helper 가 예외를 전파하며 직전 성공 호출의 profile 이 leak 되지 않는다 (stale-failure 테스트) (per HIGH-1 v4)"
    - "Phase 2 v1 scope 박제 — Firestore 저장은 Phase 6 plan 으로 보류. AnalysisDoc / firestore_admin 변경 0. (per MEDIUM-1 v4 + CONTEXT.md §1·§8)"
    - "Task 5b 는 execution-only — NLF→SMPL-X loader 와 β-to-Profile 변환 math 는 Task 5a 에서 박제되며 5b 는 belle Pod 에서 박제된 스크립트 실행만 한다 (per HIGH-2 v4)"
    - "load_nlf_smplx_keypoints 반환형은 dict[str, BodyNormalizationProfile] (per HIGH-2 v4 — list[PoseFrame] 폐기)"
    - "estimatedHeightScale 의 모든 sunity_shared consumer 가 'torso-relative proportion heuristic' 주석을 인접에 박제하는지 자동 검증한다 (현재 vacuous PASS, Phase 6+ 에서 load-bearing) (per MEDIUM-3 v4)"
    - "Task 6 AST scanner 가 importlib.import_module + __import__ + spec_from_file_location 의 string literal 인자 모두를 차단하며 비-literal 동적 import 는 잔여 위험으로 명시 박제된다 (per LOW-2 v4)"
    - "lockstep 테스트가 5종 warning enum 문자열을 3 파일에서 string/regex parse 한 결과로 자동 검증한다 (per LOW-1 v4 — 'AST lockstep' 호칭 폐기, Task 6 만 진짜 AST)"
    - "RTMW 실 출력 coverage 는 v1.5 deferral — ROADMAP 성공 기준 1 의 acceptance text 가 'synthetic + adapter-path validation; real RTMW output validation deferred to v1.5 sweep run' 으로 갱신된다 (per MEDIUM-2 v4)"
    - "5종 fixture JSON이 PoseFrame dataclass 시그너처 (timestamp_ms / Keypoint3D.uncertainty_proxy / PoleAxis 4-field axis_vector+confidence_level+source+frame_index) 와 정확히 일치하며 test factory helper로 load 가능하다 (per HIGH-2 v1→v2)"
    - "pose_too_inverted 판정은 RTMW 이미지 y-down 픽셀 좌표 + 기본 pole_axis=(0,1,0) convention 에 맞게 부호 정합되며 실제 RTMW adapter (convert_rtmw_keypoints_to_coco17_and_pole_ext) 경유 fixture 로 검증된다 (per HIGH-2 v3)"
    - "AST import isolation 은 sunity_shared 뿐 아니라 backend/functions 와 backend/runpod_inference 까지 스캔하며 importlib.import_module / __import__ / spec_from_file_location 모두 차단한다 (per MEDIUM-1 v3 + LOW-2 v4)"
    - "estimatedHeightScale은 절대 키가 아닌 torso-relative proportion heuristic 임이 docs/contract.md / app/src/types/analysis.ts / backend/.../body_normalization.py 3 파일에 동시 명시된다 (per v1→v2 M3)"
    - "Task 5b 산출 (`reports/body_profile_gap_<date>.json`) 체크인 또는 belle 명시 v1.5 defer 박제까지 ROADMAP §4 closure 표시 X"
    - "Task 6 (AST isolation + REQUIREMENTS RTMW 정합) 는 Task 5a 만 의존하며 Task 5b human gate 와 평행 진행 가능하다 (per MEDIUM-3 v3)"
    - "`python -m backend.research.evaluations.{compare_body_profile|extract_rtmw_body_profile_keypoints|nlf_smplx_body_profile_loader}` 는 모두 repo root (`/workspace/SunityMotion`) 에서 실행되며 plan 의 모든 verify 블록과 commit 메시지가 이 convention 을 박제한다 (per LOW-1 v3)"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/body_normalization_measurer.py"
      provides: "measure_body_profile(pose_frames) -> BodyNormalizationProfile 순수 함수 (M2 PoleAxis-relative inversion 박제)"
      contains: "measure_body_profile"
    - path: "backend/tests/fixtures/body_normalization/_factory.py"
      provides: "pose_frame_from_dict(raw: dict) -> PoseFrame test helper — PoseFrame.from_dict 부재 우회 (HIGH-2)"
      contains: "pose_frame_from_dict"
    - path: "backend/tests/fixtures/body_normalization/_generate.py"
      provides: "seed=42 멱등 fixture 생성기 + 5종 JSON (실제 dataclass 시그너처 정합). HIGH-2 v3: inverted fixture 는 convert_rtmw_keypoints_to_coco17_and_pole_ext 어댑터 경유 산출"
      contains: "RandomState(42)"
    - path: "backend/tests/fixtures/body_normalization/test_fixtures_loadable.py"
      provides: "5 fixture × pose_frame_from_dict() smoke load + RTMW y-down convention guard (HIGH-2 v3)"
    - path: "backend/tests/test_body_normalization_measurer.py"
      provides: "5 fixture × profile assertion 단위 테스트 + y-down PoleAxis-relative inversion 검증 (HIGH-2 v3 + v1→v2 M2)"
    - path: "backend/tests/test_pipeline_body_profile_injection.py"
      provides: "신규 helper `_angles_and_body_profile_from_video` 단위 + concurrency barrier + stale-failure + B8 박제 회귀 (HIGH-1 v4 race-safe local-return)"
    - path: "backend/tests/test_research_import_isolation.py"
      provides: "AST 스캔으로 sunity_shared + backend/functions + backend/runpod_inference → backend.research/nlf/smplx 차단 (mediapipe 제외; importlib.import_module + __import__ + spec_from_file_location 모두 string-literal 차단 — MEDIUM-1 v3 + LOW-2 v4)"
    - path: "backend/tests/test_estimated_height_scale_consumer_semantics.py"
      provides: "sunity_shared consumer 가 estimatedHeightScale / estimated_height_scale 사용 시 인접에 'torso-relative proportion heuristic' 주석을 박제했는지 lint-style scan. 현재 consumer 0 → vacuous PASS, Phase 6+ 에서 load-bearing (MEDIUM-3 v4)"
    - path: "backend/research/evaluations/compare_body_profile.py"
      provides: "R&D NLF→SMPL-X β vs RTMW segment gap 보고서 CLI (scaffold + gap math, HIGH-2 v4 — loader 본체 분리 후 본 파일은 비교만 책임)"
    - path: "backend/research/evaluations/extract_rtmw_body_profile_keypoints.py"
      provides: "belle Pod 에서 5영상 → RTMW keypoint dump (`reports/sweep_rtmw_<date>/keypoints/<video>.json`) 산출 helper — Task 5b 실행 pre-step (HIGH-4 v3)"
    - path: "backend/research/evaluations/nlf_smplx_body_profile_loader.py"
      provides: "NLF→SMPL-X β path 본체 + β→BodyNormalizationProfile 변환 math + CLI (HIGH-2 v4 — Task 5a 박제, Task 5b 는 실행만)"
  key_links:
    - from: "backend/functions/pipeline/app.py::_angles_and_body_profile_from_video"
      to: "backend/functions/pipeline/app.py::_RTMWNlfCompat.estimate_with_profile"
      via: "local-return tuple (np.ndarray, BodyNormalizationProfile | None) — 사이드카 mutable state 0 (HIGH-1 v4)"
      pattern: "_angles_and_body_profile_from_video\\("
    - from: "backend/functions/pipeline/app.py::_RTMWNlfCompat.estimate_with_profile"
      to: "sunity_shared.analysis.body_normalization_measurer.measure_body_profile"
      via: "video-level 1회 호출 + tuple return — 시그너처 (frames) -> tuple[np.ndarray, BodyNormalizationProfile | None] (HIGH-1 v4)"
      pattern: "estimate_with_profile"
    - from: "docs/contract.md §7 warnings + estimatedHeightScale 의미"
      to: "app/src/types/analysis.ts BodyNormalizationProfile.warnings + body_normalization.py docstring"
      via: "3-way lockstep (single atomic commit) + lockstep test string/regex 검증 (MEDIUM-1 v4 revert — AnalysisDoc 변경 없음, Phase 6 scope)"
      pattern: "low_keypoint_confidence|occluded_endpoint|insufficient_frames|asymmetric_landmark_count|pose_too_inverted"
    - from: "backend/tests/fixtures/body_normalization/*.json"
      to: "backend/tests/fixtures/body_normalization/_factory.py::pose_frame_from_dict"
      via: "test factory helper (PoseFrame.from_dict 부재 우회 — HIGH-2 v1→v2)"
      pattern: "pose_frame_from_dict"
    - from: "backend/research/evaluations/extract_rtmw_body_profile_keypoints.py"
      to: "backend/research/evaluations/compare_body_profile.py --rtmw-keypoints-dir"
      via: "keypoint dump → compare 입력 (HIGH-4 v3 — pre-step)"
      pattern: "sweep_rtmw_.*?/keypoints"
    - from: "backend/research/evaluations/nlf_smplx_body_profile_loader.py"
      to: "backend/research/evaluations/compare_body_profile.py --nlf-profiles-dir"
      via: "NLF→SMPL-X β path 본체 + β→Profile 변환 (HIGH-2 v4 박제) → compare 입력"
      pattern: "nlf_smplx_.*?/.*\\.json"
---

## v1 → v2 Replan Changelog (preserved — audit history)

본 plan 은 `02-REVIEWS.md` (Codex 1차 리뷰) 의 4 HIGH + 3 MEDIUM + 1 LOW concern 을 fix 한 v2 를 기반으로 한다. v2 → v3 → v4 에서 일부 결정이 재조정되었지만 **v1 → v2 의 다음 fix 는 그대로 보존된다**:

| Concern | Severity | v1 task | v2 fix (이후 버전에서 유지) | Evidence (orchestrator verified) |
|---|---|---|---|---|
| HIGH-2: Fixture plan 이 존재하지 않는 PoseFrame API 와 잘못된 필드 (`timestamp_seconds` / `Keypoint3D.uncertainty_proxy` 누락 / `PoleAxis.origin` 환상) 참조 | HIGH | v1 Task 2 (fixture) | (a) `PoseFrame.from_dict` 가 코드베이스에 **없음** 확인 → test factory helper `pose_frame_from_dict()` 신설. (b) fixture JSON 스키마: `timestamp_ms: int`, `Keypoint3D.uncertainty_proxy: float = 1 - confidence`, `PoleAxis` 4 필드 (`axis_vector`/`confidence_level`/`source`/`frame_index`), `origin` 박멸. (c) smoke load 테스트 신설 | `backend/shared/python/sunity_shared/analysis/pose_frame.py:228-246` PoseFrame, `:103-116` Keypoint3D, `:167-200` PoleAxis 4 필드 |
| HIGH-3: Import-isolation rule 이 legacy mediapipe code 와 충돌 | HIGH | v1 Task 6 (AST 차단) | AST scan 차단 대상을 narrow: `backend.research` / `research` / `nlf` / `smplx` / `smpl_x` 만. `mediapipe` 차단 제외 + TODO 마커 박제 | `backend/shared/python/sunity_shared/analysis/pose_engines/__init__.py` lazy mediapipe + `pose_engines/mediapipe_engine.py:74-76, 197` live import |
| HIGH-4 (v2 정의): R&D `NotImplementedError` stub 으로 ROADMAP §4 충족 표시 — scaffold ≠ gap report | HIGH | v1 Task 5 (R&D harness) | Task 5 분할: Task 5a (scaffold, executor 가능) + Task 5b (belle Pod 실행, user_setup gate). autonomous=false. ROADMAP §4 closure 는 5b 완료 시에만 | ROADMAP.md success criterion #4 — scaffold 만으론 closure X |
| M1: Synthetic-only tests 가 RTMW 좌표/naming drift 탐지 불가 | MED | v1 Task 2 | 5 synthetic fixture 유지 + Task 2b RTMW sanity skip (Pod 환경에서만 실행) | `backend/research/evaluations/reports/sweep_rtmw_20260603_1409/` 에 keypoint dump 없음 |
| M3: armScale/legScale/estimatedHeightScale semantics 미명세 | MED | v1 Task 1 (contract) | 3-way 박제: "torso-relative proportion heuristic — 절대 키가 아니라 torso 길이 대비 사지 비율. Phase-6 coarse normalizer hint 로만 사용". `estimatedHeightScale = (armScale + legScale + 1.0) / 3` 산식 박제 | M3 |
| L1: Lockstep warning enum 만 grep 검증 → 자동화 부족 | LOW | v1 Task 1 verify | `test_body_normalization_lockstep.py` 확장 — 5 warning enum 문자열을 3 파일에서 자동 검증 | Phase 1 산출 lockstep 파일에 warnings enum 검증 없음 |

## v2 → v3 Replan Changelog (preserved — audit history)

Codex 2차 리뷰 가 v2 의 4 HIGH + 3 MEDIUM + 1 LOW 신규 concern 을 보고. v3 fix 중 **HIGH-1 (사이드카) 와 MEDIUM-2 (Firestore lockstep) 는 v4 에서 reverted** (아래 v3→v4 changelog 참조). 나머지는 보존:

| Concern (v2 → v3) | Severity | Affected v2 task | v3 fix (v4 status) | Evidence (orchestrator verified) |
|---|---|---|---|---|
| HIGH-1 (v3): tuple-return propagation 이 B8 fix 박제 위반 | HIGH | Task 4 (전체 재작성) | **v4 에서 폐기** — 사이드카 → race-safe local-return helper 로 교체 (v3→v4 HIGH-1 참조) | `backend/tests/test_pipeline_recognizer_switch.py:225-238` |
| HIGH-2 (v3): `_is_inverted_frame` 부호가 RTMW image y-down 에서 반전 | HIGH | Task 2 (fixture), Task 3 (measurer) | **유지** — convention 명시 박제 + RTMW adapter 경유 fixture | `backend/.../adapters/rtmw_133_to_coco17.py:184-189` |
| HIGH-3 (v3): NotImplementedError raise 가 5b landing 시 회귀 | HIGH | Task 5a (smoke), Task 5b (Pod 실행) | **유지** — graceful-empty 패턴 (단, HIGH-2 v4 로 반환형이 `dict[str, BodyNormalizationProfile]` 로 갱신됨) | v2 plan Task 5a action 5번째 bullet |
| HIGH-4 (v3): 5b 가 RTMW keypoint dump 부재로 실행 불가 | HIGH | Task 5a (helper 추가), Task 5b (pre-step 박제) | **유지** — extract_rtmw_body_profile_keypoints.py 신설 | `ls .../sweep_rtmw_20260603_1409/` keypoint 없음 |
| MEDIUM-1 (v3): AST 스캔이 sunity_shared 만 | MED | Task 6 | **유지 + LOW-2 v4 확장** — 3 디렉터리 + importlib + `__import__` + `spec_from_file_location` 모두 차단 | `pipeline/app.py:454-461`, `runpod_inference/server.py` |
| MEDIUM-2 (v3): contract.md AnalysisDoc 갱신 누락 | MED | Task 4 (4-way lockstep) | **v4 에서 폐기 (revert)** — Firestore 저장은 Phase 6 scope (CONTEXT.md §1 + §8). AnalysisDoc 변경 0 (v3→v4 MEDIUM-1 참조) | `docs/contract.md:82-97` — Phase 6 가 박제할 곳 |
| MEDIUM-3 (v3): Task 6 가 Task 5b 뒤로 묶임 | MED | Task 6 depends-on | **유지** — Task 6 의 depends-on 은 Task 5a 만. Task 5b 와 평행 | v2 plan 의존 chain |
| LOW-1 (v3): `cd backend && python -m backend.research...` 가 module resolution 실패 | LOW | Task 5a verify, Task 5b dashboard | **유지** — 모든 `python -m` 은 repo root 실행. pytest 는 `cd backend && pytest` 유지 | `backend/research/evaluations/compare_engines.py:28-34` |

## v3 → v4 Replan Changelog (THIS revision)

Codex 3차 리뷰 (02-REVIEWS.md, reviewed_at: 2026-06-07T12:14:36Z, plan_revision: v3) 가 v3 의 신규 concern 을 보고. orchestrator 가 codebase grep 으로 모두 검증 완료. v4 가 다음과 같이 수정한다:

| Concern (v3 → v4) | Severity | v3 mechanism | v4 fix | File/line evidence (orchestrator verified) |
|---|---|---|---|---|
| **HIGH-1 (v4)**: `_RTMWNlfCompat.last_body_profile` 사이드카 mutable state 가 RunPod FastAPI BackgroundTasks 환경에서 race / leak 가능. `--workers 1` 은 process 수 제약일 뿐 단일 프로세스 안 동시 background task 를 막지 않음. v3 sequential leakage 테스트는 interleaved 실행을 증명 X. | HIGH | `_RTMWNlfCompat.last_body_profile` instance attribute + `_process` 가 caller 시점에 read | **사이드카 폐기 → race-safe local-return helper**: (1) `_RTMWNlfCompat` 에 신규 method `estimate_with_profile(frames) -> tuple[np.ndarray, BodyNormalizationProfile \| None]` 박제. 기존 `estimate(frames) -> np.ndarray` 시그너처/반환 무변경 (B8 박제 + downstream 호환). (2) `backend/functions/pipeline/app.py` 에 신규 helper `_angles_and_body_profile_from_video(bucket: str, key: str) -> tuple[np.ndarray, BodyNormalizationProfile \| None]` 박제. 내부에서 `_ensure_adapters()` + `_POSE_ESTIMATOR.estimate_with_profile(frames)` 호출 → angles + profile local tuple 반환. mutable shared state 0. (3) `_angles_from_video(bucket, key) -> np.ndarray` 및 `_angles_and_video_path_from_video(bucket, key) -> tuple[np.ndarray, str]` 무변경 (B8 박제). (4) `last_body_profile` attribute 모든 박제 폐기. (5) 테스트: (a) helper 가 profile 반환 검증, (b) concurrency 회귀 — `concurrent.futures.ThreadPoolExecutor(max_workers=2)` + barriered fake estimator 로 interleaved 실행 → 각 호출이 자기 frame 의 profile 만 받음, (c) stale-failure — `estimate_with_profile` 가 예외 raise 시 helper 가 예외 전파 + 직전 성공 profile leak 0. | `backend/functions/pipeline/app.py:110-115` `_POSE_ESTIMATOR=None` 모듈-레벨 글로벌. `backend/runpod_inference/server.py:39, 198, 213` `BackgroundTasks` + `background.add_task`. `backend/tests/test_pipeline_recognizer_switch.py:225-238` `TestB8FixSignature` lock. |
| **HIGH-2 (v4)**: Task 5b 가 belle 에게 "NLF→SMPL-X β loader 실 구현 + RTMW extraction + 보고서 생성 + commit" 을 요구 → 설계/구현이 human checkpoint 뒤에 숨음. 또한 v3 scaffold 의 `load_nlf_smplx_keypoints() -> list[PoseFrame]` 반환형이 R&D gap 보고에 필요한 단위 (per-video `BodyNormalizationProfile`) 와 불일치. | HIGH | Task 5a scaffold (graceful-empty stub) + Task 5b belle 가 본체 구현 | **Task 5a 가 loader 본체 + math 박제 (executor 가 완성). Task 5b 는 실행만**: (1) 신규 모듈 `backend/research/evaluations/nlf_smplx_body_profile_loader.py` 박제 — NLF 3D keypoints → SMPL-X β fit → β→BodyNormalizationProfile 변환 math (pure NumPy, Pod weights 없이 CI 단위 테스트 가능) + CLI entry. (2) `backend/research/evaluations/compare_body_profile.py` 의 `load_nlf_smplx_keypoints` 폐기 → `load_nlf_smplx_profiles(path: Path) -> dict[str, BodyNormalizationProfile]` 으로 교체 (graceful-empty 패턴 유지: 비존재 dir → `{}`). (3) `compute_profile_gap` 시그너처 도 `dict[str, BodyNormalizationProfile]` 입력으로 갱신. (4) `load_rtmw_profiles(path) -> dict[str, BodyNormalizationProfile]` 도 동일 키 (video name) 정합. (5) Task 5b 의 `dashboard_config` 는 4단계 박제 명령 (extract → loader → compare → commit) 만 — 설계/구현 결정 0. (6) Pod weights 가 필요한 부분 (NLF TorchScript forward + SMPL-X β fit) 은 `nlf_smplx_body_profile_loader.py` 의 함수 body 안 lazy import — CI 환경에선 weights 없으면 graceful-empty, Pod 에선 실측. β-to-Profile 변환 math (pure NumPy: β → bone length ratios → BodyNormalizationProfile 필드) 는 weights 없이 단위 테스트. | v3 Task 5b how-to-verify Step (b) "load_nlf_smplx_keypoints 실 구현 (편집)" — belle 에게 구현 요구. v3 scaffold `list[PoseFrame]` 반환형은 gap 보고 단위와 mismatch. |
| **MEDIUM-1 (v4)**: Firestore 저장 + AnalysisDoc lockstep 이 CONTEXT.md §1 ("Phase 6 다운스트림 활용" out-of-scope) + §8 ("Firestore 저장은 Phase 6 통합 시점으로 보류") 의 v1 scope 박제 위반. v2 review 의 "body_shape never reaches Phase 6" 는 약간의 misdiagnosis — Phase 6 는 Phase 2 시점에 실시간 접근 필요 X, 함수/schema 가 존재하면 Phase 6 plan 이 wire up 함. | MED | Task 4 가 `firestore_admin.complete_analysis(body_normalization_profile=...)` kw + `AnalysisDoc.bodyNormalizationProfile?` + `docs/contract.md` AnalysisDoc 행 추가 (4-way lockstep) | **Firestore 확장 revert**: (1) `backend/shared/python/sunity_shared/firestore_admin.py` 변경 0 — `complete_analysis` 에 `body_normalization_profile=` kwarg 추가 X. (2) `app/src/types/analysis.ts` `AnalysisDoc` 변경 0 — `bodyNormalizationProfile?` 필드 추가 X (v3 의 추가를 revert). (3) `docs/contract.md` AnalysisDoc 섹션 (line 82-97) 변경 0 (v3 의 추가를 revert). (4) `files_modified` 에서 firestore_admin.py 제거. (5) Task 4 의 lockstep 은 1 파일 (`pipeline/app.py`) 변경으로 축소. (6) must_haves.truths 에서 "Firestore persistence" / "Phase-6 boundary 박제" 등 truth 삭제, 대신 "Phase 6 unblock = helper 함수 + measurer math 박제, persistence 는 Phase 6 scope 박제" 박제. (7) 본 변경으로 MEDIUM-2 v4 (real RTMW coverage) 도 less load-bearing — v1 deliverable 이 narrower 해짐. | `.planning/phases/02-bodynormalizationprofile-rtmw-segment/02-CONTEXT.md` §1 (Out of scope: "BodyProfile 의 다운스트림 활용 → Phase 6 에서") + §8 ("v1 = 측정기 본체만, Firestore 저장은 Phase 6 통합 시점으로 보류"). `02-RESEARCH.md` "Pipeline Integration Plan" → Firestore 저장 권장 X (v1 RAM only). |
| **MEDIUM-2 (v4)**: 실 RTMW 엔진 출력 coverage 가 Task 5b human gate 뒤로 deferred. v3 fixture 는 `convert_rtmw_keypoints_to_coco17_and_pole_ext` adapter 호출로 schema/coordinate drift 는 잡지만, 실 RTMW 네트워크의 confidence 분포 / keypoint naming / 실 영상 detector 행동을 검증 X. ROADMAP success criterion 1 ("RTMW 키포인트에서 segment 길이가 시간 평균으로 안정적으로 추출") 가 부분만 증명됨. | MED | Task 2b: `@pytest.mark.skipif(not _rtmw_available())` RTMW sanity test (synthetic 보강) | **v1.5 deferral + ROADMAP acceptance text 갱신**: (1) Task 2b 의 `test_real_rtmw_sanity.py` 는 그대로 유지 (Pod 환경에서만 실행 가능). (2) **ROADMAP.md Phase 2 Success Criterion 1 의 acceptance text 갱신**: 기존 "RTMW 키포인트에서 segment 길이가 시간 평균으로 안정적으로 추출된다 (jitter 스무딩)" 뒤에 다음 박제 추가 — "(v1 acceptance: synthetic + RTMW adapter-path validation. 실 RTMW network output coverage 는 v1.5 sweep 실행으로 deferred — belle Pod 5영상 keypoint dump 가 산출되면 본 criterion v1.5 closure)". (3) Task 6 의 REQUIREMENTS.md 갱신 옆에 ROADMAP.md 갱신도 동일 atomic commit 으로 묶음. (4) committed test data 로 promote 는 belle Pod Step (a) 산출 (Task 5b) 이후 가능 — 이 시점에 Task 2b 의 skip 해제는 별 plan. | ROADMAP.md:113 success criterion 1. v3 Task 2b 가 skip-if-no-rtmw 패턴이라 CI 에서 evidence 0. |
| **MEDIUM-3 (v4)**: `estimatedHeightScale` 필드명이 absolute height 를 의미하는 것처럼 보임. v3 가 docstring lockstep ("torso-relative proportion heuristic") 박제했지만 consumer 측 강제 메커니즘 0. Phase 6+ 가 본 필드를 absolute height 로 잘못 다룰 risk. | MED | Task 1 의 3-way docstring lockstep | **Consumer semantic guard lint test 신설**: (1) 신규 테스트 파일 `backend/tests/test_estimated_height_scale_consumer_semantics.py` 박제. (2) `backend/shared/python/sunity_shared/**/*.py` 에서 `estimatedHeightScale` 또는 `estimated_height_scale` identifier 등장 line 모두 스캔. (3) 각 등장 위치의 ±3 라인 안에 `torso-relative proportion heuristic` 주석이 박제됐는지 검증. (4) 현재 Phase 2 시점에 consumer 0 → vacuous PASS. Phase 6+ 에서 consumer 추가 시 load-bearing. (5) 필드 rename (Option B) 은 reject — v3 3-way lockstep + CONTEXT.md + 박제된 dataclass 모두 깨짐. Option A (lint test) 만 채택. | `backend/shared/python/sunity_shared/analysis/body_normalization.py:30-82` 필드 정의. 현재 sunity_shared consumer 0 (`grep -r "estimated_height_scale\|estimatedHeightScale" backend/shared/`). |
| **LOW-1 (v4)**: v3 plan 의 "AST lockstep" 호칭이 실제로는 substring/regex matching. Task 6 의 진짜 `ast.parse` scanner 와 혼동. | LOW | v3 plan Task 1 의 `test_warnings_enum_lockstep_across_3_files` 를 "AST lockstep" 으로 호칭 | **호칭 갱신**: (1) plan 본문에서 "AST lockstep" → "string/regex lockstep" 으로 변경 (Task 1 박제). (2) 테스트 함수명 그대로 유지 (회귀 risk 회피) 하되 docstring 에 "string/regex 검증 — AST 아님" 명시. (3) Task 6 의 import isolation scanner 만 "AST" 호칭 유지 (실제 `ast.parse` 사용). | v3 plan Task 1 action bullet 5 "lockstep AST 자동 검증 확장" + comment "regex parse 권장". |
| **LOW-2 (v4)**: v3 AST scanner 가 `importlib.import_module("smplx")` 만 catch. `__import__("smplx")` / `importlib.util.spec_from_file_location` + literal source 는 catch X. 비-literal `importlib.import_module(variable)` 는 정적 분석 불가. | LOW | Task 6 AST scanner | **scanner 확장 + residual risk 박제**: (1) `backend/tests/test_research_import_isolation.py` 의 AST scanner 가 추가로 catch: (a) `ast.Call` where `node.func.id == "__import__"` + 첫 positional arg 가 `ast.Constant` + value 가 차단 prefix; (b) `ast.Call` where `node.func.attr == "spec_from_file_location"` + 두 번째 positional arg (`location` 또는 source path) 가 `ast.Constant` + value 가 차단 모듈 경로 매치. (2) 비-literal `importlib.import_module(var)` / `__import__(var)` 는 residual risk — `test_research_import_isolation.py` 파일 상단 TODO 마커 박제: "정적 분석 한계 — variable 인자 동적 import 는 data-flow 분석 필요. PR 단계 manual review 정합." (3) test 함수 4 → 5개로 확장 (sunity_shared / functions / runpod_inference / importlib literal / `__import__` + spec_from_file_location literal). | v3 plan Task 6 의 importlib.import_module 만 catch. `backend/runpod_inference/server.py:31, 73` `importlib.util.spec_from_file_location` 사용 (legitimate — 본 검사 대상은 차단 prefix 매치만). |

v4 plan 의 v3 → v4 변경 summary:
- **Task 1**: LOW-1 v4 박제 — "AST lockstep" 호칭 폐기, "string/regex lockstep" 으로 갱신. 테스트 함수명 무변경 (회귀 0).
- **Task 2**: 변경 없음. v3 그대로.
- **Task 3**: 변경 없음. v3 그대로.
- **Task 4**: HIGH-1 v4 전면 재작성 (사이드카 → race-safe local-return helper). MEDIUM-1 v4 revert (Firestore + AnalysisDoc + contract.md AnalysisDoc 변경 0). files_modified 가 4 → 1 (`pipeline/app.py` 만).
- **Task 5a**: HIGH-2 v4 박제 — `nlf_smplx_body_profile_loader.py` 신설 (loader 본체 + β→Profile math + CLI). `compare_body_profile.py` 의 `load_nlf_smplx_keypoints` → `load_nlf_smplx_profiles` (반환형 `dict[str, BodyNormalizationProfile]`). compute_profile_gap 시그너처 갱신.
- **Task 5b**: HIGH-2 v4 박제 — execution-only. dashboard_config 가 4단계 박제 명령 (extract → loader → compare → commit) 만. 설계/구현 결정 0.
- **Task 6**: LOW-2 v4 박제 — AST scanner 확장 (`__import__` + `spec_from_file_location` literal). residual risk TODO 박제. MEDIUM-2 v4 박제 — ROADMAP.md success criterion 1 acceptance text 갱신 commit 추가.
- **신규 Task 7 (executor auto)**: MEDIUM-3 v4 박제 — `test_estimated_height_scale_consumer_semantics.py` lint test.

<objective>
RTMW 133 wholebody 키포인트로부터 신체 segment 길이·비율·confidence·warnings 를 자동 산출하는 `measure_body_profile()` 측정기를 박제하고, `_RTMWNlfCompat.estimate_with_profile()` + 신규 helper `_angles_and_body_profile_from_video()` 의 **race-safe local-return tuple** 을 통해 video-level `BodyNormalizationProfile` 이 caller 가 사이드카 mutable state 없이 받을 수 있도록 한다. **B8 fix 시그너처 박제** (`_angles_from_video(bucket, key) -> np.ndarray`, `_angles_and_video_path_from_video(bucket, key) -> tuple[np.ndarray, str]`, `_RTMWNlfCompat.estimate(frames) -> np.ndarray`) 는 모두 무변경. **Firestore 저장 + AnalysisDoc 박제는 본 phase scope 외 (Phase 6 plan 책임)** — CONTEXT.md §1 + §8 정합. R&D 비교군 (NLF→SMPL-X β) 의 loader 본체 + β→Profile 변환 math 는 `nlf_smplx_body_profile_loader.py` 에 박제하여 Task 5a 의 executor 가 완성하며, belle Pod (Task 5b) 는 박제 스크립트 실행 + commit 만 책임. AST import 차단 테스트 (sunity_shared + backend/functions + backend/runpod_inference + `importlib.import_module` + `__import__` + `spec_from_file_location` literal 차단) 로 운영 코드 leak 을 영구 방지.

Per D-02-01, D-02-02, D-02-03, D-02-04, D-02-05, D-02-06.

Purpose: Phase 6 (체형 정규화 비교) / Phase 7 (차이 분류) / Phase 8·9 (힘 패턴) 가 공유하는 입력 schema 를 채운다. **v1 scope = 측정기 본체 + race-safe helper 함수 + measurer math + R&D harness 박제** (CONTEXT.md §1·§8 정합). Phase 6 plan 이 Phase 2 의 helper 를 호출하여 Firestore 저장 wire-up.

Output (v4 갱신):
- `body_normalization_measurer.py` 신설 (순수 numpy, scipy/외부 의존 0) — HIGH-2 v3 y-down convention 박제
- 5 synthetic fixture + `_factory.py` test helper + smoke load + 6 단위 테스트 (HIGH-2 v3 RTMW 어댑터 경유 inverted fixture)
- `_RTMWNlfCompat.estimate_with_profile()` 신규 method + `_angles_and_body_profile_from_video()` 신규 helper — HIGH-1 v4 race-safe local-return tuple
- B8 박제 보존 회귀 + concurrency 회귀 + stale-failure 테스트
- 3-way lockstep (Task 1): `docs/contract.md` §7 warnings + `app/src/types/analysis.ts` BodyNormalizationProfile + `body_normalization.py` docstring (Phase 2 v1 scope — AnalysisDoc 변경 없음, MEDIUM-1 v4 revert)
- `backend/research/evaluations/compare_body_profile.py` R&D gap 보고서 CLI (scaffold + math, HIGH-2 v4 loader 분리)
- `backend/research/evaluations/nlf_smplx_body_profile_loader.py` NLF→SMPL-X β path 본체 + β→Profile math + CLI (HIGH-2 v4)
- `backend/research/evaluations/extract_rtmw_body_profile_keypoints.py` RTMW extraction helper (HIGH-4 v3)
- belle Pod 실행 게이트 (Task 5b — user_setup, execution-only)
- AST import isolation 확장 (sunity_shared + backend/functions + backend/runpod_inference + `importlib.import_module` + `__import__` + `spec_from_file_location` literal) — MEDIUM-1 v3 + LOW-2 v4
- estimatedHeightScale consumer semantic guard lint test (MEDIUM-3 v4) — 현재 vacuous PASS, Phase 6+ load-bearing
- string/regex lockstep 테스트 (warning enum 자동 검증) — L1 (LOW-1 v4 호칭 갱신)
- REQUIREMENTS.md BODY-01 "MediaPipe" → "RTMW" 정합 갱신 + ROADMAP.md Phase 2 success criterion 1 acceptance text v1.5 deferral 박제 (MEDIUM-2 v4)
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-bodynormalizationprofile-rtmw-segment/02-CONTEXT.md
@.planning/phases/02-bodynormalizationprofile-rtmw-segment/02-RESEARCH.md
@.planning/phases/02-bodynormalizationprofile-rtmw-segment/02-REVIEWS.md
@docs/contract.md
@backend/shared/python/sunity_shared/analysis/body_normalization.py
@backend/shared/python/sunity_shared/analysis/pose_frame.py
@backend/shared/python/sunity_shared/analysis/temporal.py
@backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/wholebody_keypoints.py
@backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/rtmw_engine.py
@backend/shared/python/sunity_shared/analysis/adapters/rtmw_133_to_coco17.py
@backend/functions/pipeline/app.py
@backend/runpod_inference/server.py
@backend/tests/test_pipeline_recognizer_switch.py
@app/src/types/analysis.ts
@backend/tests/test_body_normalization_lockstep.py
@backend/tests/test_body_normalization_profile.py
@backend/research/evaluations/compare_engines.py
@backend/research/evaluations/compare_rtmw_vs_ipsf.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: contract.md §7 warnings enum 5종 + estimatedHeightScale 의미 박제 + lockstep string/regex 자동 검증 (3-way lockstep, 단일 atomic commit) — LOW-1 v4 호칭 갱신</name>
  <read_first>
    backend/shared/python/sunity_shared/analysis/body_normalization.py (현 docstring + warnings 필드 코멘트),
    app/src/types/analysis.ts (lines 364-388 — 현 BodyNormalizationProfile interface + 필드 주석),
    docs/contract.md (§7 — 현 BodyNormalizationProfile 명세, line 342 warnings),
    backend/tests/test_body_normalization_lockstep.py (확장 대상 — 현 5 함수)
  </read_first>
  <action>
    Goal: D-02-02 lockstep 정신 정합 — `warnings` 필드의 허용 enum 5종 + scale 필드 의미 (M3) 를 3 파일에 동시 박제. lockstep 테스트를 string/regex 자동 검증으로 확장 (L1, LOW-1 v4 호칭 갱신).

    **v4 주의 (MEDIUM-1 v4 revert)**: 본 task 의 lockstep 범위는 §7 BodyNormalizationProfile 만. AnalysisDoc 의 `bodyNormalizationProfile?` optional 필드 박제는 **Phase 2 scope 외 (Phase 6 plan 책임)** — v3 의 4-way commit 박제는 폐기, v2 의 3-way commit 으로 복귀.

    **LOW-1 v4 호칭 박제**: 본 task 의 신규 lockstep 함수는 substring/regex 검사 — 진짜 AST 검사 아님. 테스트 함수명은 회귀 위험 회피를 위해 유지하되, docstring 에 "string/regex 검증 — AST 아님 (LOW-1 v4 박제)" 명시. plan 본문에서 "AST lockstep" → "string/regex lockstep" 으로 호칭 갱신. Task 6 의 진짜 `ast.parse` import scanner 만 "AST" 호칭 유지.

    구현 노트:

    1. **5종 warning enum**: `low_keypoint_confidence`, `occluded_endpoint`, `insufficient_frames`, `asymmetric_landmark_count`, `pose_too_inverted`. 3 파일에 정확히 같은 문자열로 박제.

    2. **docs/contract.md §7 갱신 (line 342 근처)**:
       - 현 placeholder `"측정 품질 이슈 (예: 'short_arm_clip', 'occluded_torso'). 기본값 []."` 제거.
       - 5 enum 명시 + 각 의미 1줄.
       - **M3 박제 추가** — `estimatedHeightScale` / `armScale` / `legScale` 필드 설명에 "torso-relative proportion heuristic — 절대 키가 아니라 torso 길이 대비 사지 비율. Phase 6 coarse normalizer hint 로만 사용." + `estimatedHeightScale = (armScale + legScale + 1.0) / 3` 산식.

    3. **app/src/types/analysis.ts BodyNormalizationProfile interface (lines 373-388)**:
       - `warnings: string[]` JSDoc 5 enum 매핑 + "Phase 11 coach_writer 가 한국어 카피로 번역" 노트.
       - `estimatedHeightScale` / `armScale` / `legScale` JSDoc 에 "torso-relative proportion heuristic" 박제.
       - **MEDIUM-1 v4 박제**: `AnalysisDoc` interface 변경 X — `bodyNormalizationProfile?` 필드 추가 X. (Phase 6 plan 책임)

    4. **backend/shared/python/sunity_shared/analysis/body_normalization.py**:
       - 클래스 docstring 의 `warnings 의미` 블록을 5 enum + 의미로 교체.
       - `estimated_height_scale` / `arm_scale` / `leg_scale` 필드 위 코멘트에 "torso-relative proportion heuristic" 박제.

    5. **L1 — lockstep string/regex 자동 검증 확장 (`backend/tests/test_body_normalization_lockstep.py`)** — LOW-1 v4 호칭 박제:
       - 기존 5 함수 유지.
       - 신설 함수 `test_warnings_enum_lockstep_across_3_files` (docstring: "string/regex 검증 — AST 아님 (LOW-1 v4)"): 5 enum frozenset 박제 + 3 파일 substring 검사.
       - 신설 함수 `test_estimated_height_scale_semantics_lockstep` (docstring: "string/regex 검증 — AST 아님 (LOW-1 v4)"): "torso-relative proportion heuristic" 3 파일 등장.
       - substring 검색만 사용 — AST 호칭 폐기. 기존 5 함수 회귀 0.

    6. **단일 commit 정합**: TS + Python + contract.md + test 동시 변경.

    참조: REVIEWS.md v1→v2 HIGH-2 / M3 / L1 + v3→v4 LOW-1.

    No code in this plan.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_body_normalization_lockstep.py -x -v</automated>
    추가: `grep -c "low_keypoint_confidence" docs/contract.md app/src/types/analysis.ts backend/shared/python/sunity_shared/analysis/body_normalization.py | grep -v ':0$' | wc -l` → 3.
    추가 M3: `grep -c "torso-relative proportion heuristic" docs/contract.md app/src/types/analysis.ts backend/shared/python/sunity_shared/analysis/body_normalization.py | grep -v ':0$' | wc -l` → 3.
    MEDIUM-1 v4 revert 검증: `grep -c "bodyNormalizationProfile" app/src/types/analysis.ts` → 1 (BodyNormalizationProfile interface 안 자기 자신 1회 — AnalysisDoc 행 추가 X).
  </verify>
  <acceptance_criteria>
    - 5 warning enum 문자열 3 파일 모두 존재 (lockstep string/regex 테스트 PASS)
    - "torso-relative proportion heuristic" 3 파일 모두 존재
    - 기존 placeholder 제거
    - `test_warnings_enum_lockstep_across_3_files` + `test_estimated_height_scale_semantics_lockstep` 신설 PASS — docstring 에 "string/regex 검증 — AST 아님 (LOW-1 v4)" 박제
    - 기존 5 lockstep 함수 회귀 0
    - **MEDIUM-1 v4 박제 (revert)**: `AnalysisDoc` 에 `bodyNormalizationProfile?` 필드 추가 X
    - 단일 commit 에 contract.md + analysis.ts + body_normalization.py + test 동시 변경
  </acceptance_criteria>
  <done>위 acceptance_criteria 모두 충족.</done>
  <commit-template>
    feat(02): lock warnings enum 5-way + scale semantics for BodyNormalizationProfile (3-way lockstep + string/regex gate, LOW-1 v4)
  </commit-template>
  <depends-on>None — Wave 1 진입점</depends-on>
</task>

<task type="auto">
  <name>Task 2: PoseFrame test factory + Synthetic fixture (RTMW adapter 경유) + 5 fixture JSON + smoke load (HIGH-2 v1→v2 + HIGH-2 v3) — v3 그대로</name>
  <read_first>
    backend/shared/python/sunity_shared/analysis/pose_frame.py (실제 dataclass 시그너처 — PoseFrame / Keypoint3D / PoleAxis 4-field. PoseFrame.from_dict 부재),
    backend/shared/python/sunity_shared/analysis/body_normalization.py,
    backend/shared/python/sunity_shared/analysis/skeleton.py (KEYPOINT_NAMES 17),
    backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/wholebody_keypoints.py (RTMW 133),
    backend/shared/python/sunity_shared/analysis/adapters/rtmw_133_to_coco17.py (HIGH-2 v3 — convert_rtmw_keypoints_to_coco17_and_pole_ext)
  </read_first>
  <action>
    Goal (HIGH-2 v1→v2 + HIGH-2 v3): test factory helper `pose_frame_from_dict(raw: dict) -> PoseFrame` 신설 + fixture JSON 스키마 실제 dataclass 시그너처 박제 + smoke load + inverted fixture RTMW adapter 경유 생성. v3 와 동일 — v4 변경 없음.

    1. **`backend/tests/fixtures/body_normalization/_factory.py` 신설**: `pose_frame_from_dict(raw)` — `frame_index: int`, `timestamp_ms: int` (NOT `timestamp_seconds`), `keypoints_3d` (각 entry `{x,y,z,confidence,uncertainty_proxy=1-confidence}`), `keypoints_3d_pole_aligned`, `raw_keypoints_133`, `pole_axis: PoleAxis` 4 필드 (`axis_vector` norm=1.0 / `confidence_level` / `source` / `frame_index`. `origin` 박멸), `reliability`, `body_shape: None`. JSON list→tuple 변환. Production `pose_frame.py` 수정 X.

    2. **`_generate.py` 신설 — 멱등 + HIGH-2 v3 RTMW adapter 경유**:
       - `numpy.random.RandomState(seed=42)`. CLI `--check`.
       - Convention: image y-down, default `pole_axis=(0.0, 1.0, 0.0)`. Upright `mid_shoulder.y < mid_hip.y`. Inverted swap.
       - 5종 fixture 모두 image y-down 좌표 → `convert_rtmw_keypoints_to_coco17_and_pole_ext` 호출 → PoseFrame → `dataclasses.asdict` JSON. inverted_pose 는 shoulder/hip y swap 후 동일 adapter.
       - PoleAxis: `confidence_level='medium'`, `source='vertical_fallback'`, `frame_index=None`. `origin` 박제 금지.
       - 모든 `timestamp_ms: int`.
       - 인체 평균 비율 (image pixel): torso=200, upper_arm=120, forearm=110, thigh=180, shank=160, shoulder=140, hip=100.

    3. **5 fixture JSON**:
       - normal_pose (T=60, conf 0.85-0.95), inverted_pose (T=60, mid_shoulder.y>mid_hip.y 80%, conf 0.6-0.8), occluded_leg (T=60, knee conf<0.3 in 70%), short_clip (T=20), asymmetric_pose (T=60, 좌 conf>0.9 in 90%, 우 conf>0.9 in 50%).
       - 각 파일 <200KB.

    4. **`test_fixtures_loadable.py` smoke**:
       - `test_all_fixtures_load_via_factory` (5 fixture load PASS).
       - `test_polestaxis_no_origin_field` (`origin` 키 0).
       - `test_keypoint3d_has_uncertainty_proxy`.
       - **`test_normal_pose_is_upright_in_y_down_convention` (HIGH-2 v3)**: normal_pose 80%+ frame `mid_hip.y > mid_shoulder.y`.
       - **`test_inverted_pose_is_inverted_in_y_down_convention` (HIGH-2 v3)**: inverted_pose 80%+ frame `mid_hip.y < mid_shoulder.y`.

    5. **MEDIUM-2 v4 → Task 2b 유지**: `test_real_rtmw_sanity.py` `@pytest.mark.skipif(not _rtmw_available())`. 짧은 embedded clip → RTMWPoseEngine → COCO-17 17개 + `uncertainty_proxy ∈ [0,1]`. CI skip.

    6. **TODO 마커 (MEDIUM-2 v4)**: `_generate.py` 상단 `# TODO(post-pilot, MEDIUM-2 v4): belle Pod Step (a) 산출 keypoint dump landing 후 실 영상 fixture 1종 promote — ROADMAP success criterion 1 v1.5 closure.`

    No code in this plan.
  </action>
  <verify>
    <automated>cd backend && python -m tests.fixtures.body_normalization._generate --check && pytest tests/fixtures/body_normalization/test_fixtures_loadable.py -x -v</automated>
    `ls backend/tests/fixtures/body_normalization/*.json | wc -l` → 5.
    `_generate.py --check` 두번 byte-identical.
    `grep -l "timestamp_seconds" backend/tests/fixtures/body_normalization/ 2>/dev/null` → 0.
    `grep -l '"origin"' backend/tests/fixtures/body_normalization/*.json | wc -l` → 0.
    `grep -c "uncertainty_proxy" backend/tests/fixtures/body_normalization/*.json | grep -v ':0$' | wc -l` → 5.
    `grep "convert_rtmw_keypoints_to_coco17_and_pole_ext" backend/tests/fixtures/body_normalization/_generate.py` ≥ 1.
  </verify>
  <acceptance_criteria>
    - 5 fixture JSON 존재
    - `pose_frame_from_dict` 가 5 fixture 모두 load (smoke PASS)
    - `_generate.py --check` 멱등성
    - HIGH-2 v1→v2: `timestamp_ms`, `uncertainty_proxy`, PoleAxis 4 필드 (`origin` 0)
    - HIGH-2 v3: `_generate.py` adapter 호출. normal y-down upright, inverted y-down inverted
    - 각 fixture JSON <200KB
    - Task 2b RTMW sanity test 존재 (CI skip OK)
  </acceptance_criteria>
  <done>위 acceptance_criteria 모두 충족.</done>
  <commit-template>
    test(02): add PoseFrame factory helper + RTMW-adapter-derived synthetic fixtures (5 cases) + y-down convention smoke (HIGH-2 v3 schema + convention, M1 fallback)
  </commit-template>
  <depends-on>None — Task 1 과 병렬. commit 순서는 Task 1 → Task 2 권장.</depends-on>
</task>

<task type="auto">
  <name>Task 3: body_normalization_measurer.py 본체 + 단위 테스트 (HIGH-2 v3 image y-down PoleAxis-relative inversion) — v3 그대로</name>
  <read_first>
    backend/shared/python/sunity_shared/analysis/pose_frame.py,
    backend/shared/python/sunity_shared/analysis/temporal.py (MAD `_column_outliers`, DEFAULT_OUTLIER_K=3.0),
    backend/shared/python/sunity_shared/analysis/body_normalization.py (`__post_init__` validator),
    backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/wholebody_keypoints.py,
    backend/shared/python/sunity_shared/analysis/adapters/rtmw_133_to_coco17.py (HIGH-2 v3),
    backend/tests/fixtures/body_normalization/_factory.py
  </read_first>
  <action>
    Goal: `measure_body_profile(pose_frames, *, conf_gate=0.5, mad_k=3.0, min_valid_frames=5, low_conf_gate=0.4) -> BodyNormalizationProfile` 순수 함수. numpy only. Phase 1 `temporal.py` MAD 패턴 재사용. HIGH-2 v3 — `pose_too_inverted` RTMW image y-down convention 박제. v4 변경 없음.

    1. **상수 (module top)**: `SEGMENT_PAIRS` (10 entry), `TORSO_TOP_KPS`, `TORSO_BOTTOM_KPS`, `WARNING_CODES` frozenset, 임계값 `CONF_GATE_PER_FRAME=0.5`, `MIN_VALID_FRAMES=5`, `MAX_BAD_FRAME_RATIO=0.6`, `MAD_K=3.0`, `LOW_CONFIDENCE_GATE=0.4`, `INVERTED_FRAME_RATIO=0.5`, `ASYMMETRIC_FRAME_RATIO=0.3`.

    2. **`_measure_segment_per_frame`**: per-frame `sqrt(dx²+dy²)` 만 (z 무시). conf=`min(start.confidence, end.confidence)`. 미감지→NaN.

    3. **`_measure_torso`**: `mid_shoulder`/`mid_hip` 평균 → 길이 = `sqrt((dx)² + (dy)²)`. conf = min of 4 endpoint.

    4. **`_robust_median`**: MAD k=3 + median + `(kept_ratio × mean_conf)`. valid < MIN_VALID_FRAMES → `(nan, 0.0)`.

    5. **HIGH-2 v3 — `_is_inverted_frame(frame) -> bool | None`**:
       - Convention docstring 필수 박제: "RTMW image y-down pixel coord 박제. mid_hip - mid_shoulder 가 default pole_axis (0,1,0) 와 같은 방향이면 upright. upright iff dot(torso_up_in_image, pole_axis) > 0. (HIGH-2 v3)"
       - 4 endpoint 중 하나 없으면 None. `pole_axis` None → None.
       - `torso_up_in_image = mid_hip - mid_shoulder`. `proj = dot(torso_up_in_image, pole_axis.axis_vector)`.
       - **인버트 박제**: `proj < 0` → inverted.
       - `_check_pose_too_inverted_warning`: True 비율 > `INVERTED_FRAME_RATIO` → warning. None 제외.

    6. **`measure_body_profile`**:
       - SEGMENT_PAIRS + torso 측정 → 좌우 평균 → upper_arm/forearm/thigh/shank median.
       - `arm_scale=(upper_arm+forearm)/torso`, `leg_scale=(thigh+shank)/torso`, `torso_scale=1.0`.
       - `shoulder_hip_ratio = shoulder_width / hip_width`.
       - **M3**: `estimated_height_scale = (arm_scale + leg_scale + 1.0) / 3`.
       - `confidence = mean of 11 segment confs`.
       - **Warnings**: `low_keypoint_confidence` (<gate), `occluded_endpoint` (endpoint conf<0.5 in >60%), `insufficient_frames` (T<30), `asymmetric_landmark_count` (좌우 valid 차이>30%), `pose_too_inverted` (HIGH-2 v3).
       - **NaN 방어 (T-02-03)**: torso==NaN 또는 valid<MIN → scale=1.0+conf=0+`insufficient_frames` 폴백. finite 강제.

    7. **Logging**: `from __future__ import annotations`. stdlib.

    8. **단위 테스트 11 case (`backend/tests/test_body_normalization_measurer.py`)**:
       - `test_normal_pose_profile_valid`, `test_inverted_pose_warning`, `test_inverted_warning_uses_y_down_pole_axis_convention` (HIGH-2 v3 — normal 미발화), `test_inverted_warning_fires_for_y_down_inverted_fixture` (HIGH-2 v3), `test_inverted_warning_via_rtmw_adapter_path` (HIGH-2 v3 — 합성 RTMW 133 upright/inverted → adapter → 60 frame → upright 미발화/inverted 발화), `test_occluded_leg_warning`, `test_short_clip_warning`, `test_asymmetric_warning`, `test_z_axis_ignored_when_zero` (z=0 override → arm_scale ±1%), `test_profile_validator_passes` (`__post_init__`), `test_idempotent` (2회 byte-identical).

    No code in this plan.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_body_normalization_measurer.py -x -v</automated>
    `python -c "from sunity_shared.analysis.body_normalization_measurer import measure_body_profile; help(measure_body_profile)"`.
    회귀: `pytest tests/test_body_normalization_profile.py tests/test_body_normalization_lockstep.py -x`.
    `grep -r "import scipy" backend/shared/python/sunity_shared/analysis/body_normalization_measurer.py | wc -l` → 0.
    HIGH-2 v3: y_down + adapter path 테스트 PASS.
    `python -c "from sunity_shared.analysis.body_normalization_measurer import _is_inverted_frame; assert 'RTMW image y-down' in _is_inverted_frame.__doc__"`.
  </verify>
  <acceptance_criteria>
    - 11개 단위 테스트 PASS
    - 5 fixture → BodyNormalizationProfile (NaN/inf 출력 X)
    - 정상 fixture warnings==[]
    - 4 비정상 fixture 각자 정확한 warning
    - HIGH-2 v3: y-down + adapter path 정합
    - `_is_inverted_frame` docstring "RTMW image y-down" 박제
    - scipy import 0
    - Phase 1 기존 테스트 회귀 0
  </acceptance_criteria>
  <done>위 acceptance_criteria 모두 충족.</done>
  <commit-template>
    feat(02): add measure_body_profile RTMW segment measurer with MAD smoothing + image-y-down PoleAxis inversion (BODY-01, HIGH-2 v3 fix)
  </commit-template>
  <depends-on>Task 1 (warnings enum), Task 2 (5 fixture + helper)</depends-on>
</task>

<task type="auto">
  <name>Task 4: Pipeline 통합 — race-safe local-return helper `_angles_and_body_profile_from_video` + `_RTMWNlfCompat.estimate_with_profile` (HIGH-1 v4 — B8 박제 보존, 사이드카 폐기, Firestore revert per MEDIUM-1 v4)</name>
  <read_first>
    backend/functions/pipeline/app.py (lines 110-115 `_POSE_ESTIMATOR = None` 글로벌; lines 200-310 `_RTMWNlfCompat` + `_ensure_adapters` + `_angles_from_video:255` + `_angles_and_video_path_from_video:271`; lines 380-400 `_process` 의 `_angles_from_video` 호출 site),
    backend/runpod_inference/server.py (lines 39, 198, 213 — FastAPI `BackgroundTasks` + `background.add_task` — concurrent analyses 가능 증거),
    backend/shared/python/sunity_shared/analysis/pose_frame.py (`dataclasses.replace(body_shape=profile)` 정합),
    backend/shared/python/sunity_shared/analysis/body_normalization.py,
    backend/tests/test_pipeline_recognizer_switch.py (lines 215-260 TestB8FixSignature)
  </read_first>
  <action>
    Goal (HIGH-1 v4): video-level `BodyNormalizationProfile` 을 race-safe local-return tuple 로 caller 가 받는다. **사이드카 mutable state 0** (`last_body_profile` 박제 영구 폐기). RunPod FastAPI BackgroundTasks 환경에서 concurrent `_process` 호출이 module-level `_POSE_ESTIMATOR` 글로벌 인스턴스를 공유해도 profile leak 0.

    **MEDIUM-1 v4 박제 (Firestore revert)**: `backend/functions/pipeline/app.py` 1 파일만 변경. `firestore_admin.py` / `analysis.ts` AnalysisDoc / `docs/contract.md` AnalysisDoc 행 변경 X. Phase 6 plan 책임.

    1. **`_RTMWNlfCompat` 신규 method `estimate_with_profile` (`backend/functions/pipeline/app.py:222` 근처)** — HIGH-1 v4 local-return:
       - 시그너처: `def estimate_with_profile(self, frames) -> tuple["np.ndarray", "BodyNormalizationProfile | None"]:`.
       - 본체:
         - `from sunity_shared.analysis.body_normalization_measurer import measure_body_profile` (top-level OK).
         - `from sunity_shared.analysis.pose_frame import to_coco17_array`.
         - `pose_frames = self._engine.estimate(frames, self._default_pole)`.
         - `profile = measure_body_profile(pose_frames)`.
         - **명시 코멘트 박제 (필수)**: "HIGH-1 v4 박제 (REVIEWS.md plan_revision v3): 본 method 는 local-return tuple 반환. 사이드카 mutable instance attribute (v3 의 last_body_profile) 박제 폐기. RunPod FastAPI BackgroundTasks (server.py:198, 213) 환경에서 concurrent analyses 가 _POSE_ESTIMATOR 글로벌 공유해도 profile leak 0 — caller 가 자기 frame 의 profile 만 local tuple 로 받음. B8 박제 (estimate / _angles_from_video / _angles_and_video_path_from_video 시그너처) 무변경."
         - `pose_frames_with_profile = [dataclasses.replace(pf, body_shape=profile) for pf in pose_frames]`.
         - `import dataclasses` module top 확인 (없으면 추가).
         - `coco_array = to_coco17_array(pose_frames_with_profile)`.
         - `return coco_array, profile`.

    2. **`_RTMWNlfCompat.estimate` (기존) — 무변경 (B8 박제)**: 시그너처 `-> np.ndarray` 무변경. 본체 무변경. **`last_body_profile` attribute 박제 영구 폐기** — `__init__` 에서 `self.last_body_profile = None` 같은 줄 박제 X.

    3. **신규 helper `_angles_and_body_profile_from_video` (HIGH-1 v4 race-safe)**:
       - 시그너처: `def _angles_and_body_profile_from_video(bucket: str, key: str) -> tuple[np.ndarray, "BodyNormalizationProfile | None"]:`.
       - docstring 박제: "HIGH-1 v4 박제 (REVIEWS.md plan_revision v3): race-safe local-return helper. _POSE_ESTIMATOR.estimate_with_profile 가 local tuple 로 (angles_filled, profile) 반환. 사이드카 mutable state 0. RunPod FastAPI BackgroundTasks 환경에서 concurrent analyses 가 module-level _POSE_ESTIMATOR 글로벌 공유해도 caller 가 자기 frame 의 profile 만 받음. B8 박제 (_angles_from_video / _angles_and_video_path_from_video 시그너처) 무변경 — 본 helper 는 그 옆 신설."
       - 본체: `_ensure_adapters()` → `tempfile.NamedTemporaryFile(suffix=".mp4", delete=True)` block → `_s3.download_file` → `frames = _FRAME_EXTRACTOR.extract(tmp.name)` → `keypoints, profile = _POSE_ESTIMATOR.estimate_with_profile(frames)` → `angles = compute_joint_angles(keypoints)` → `angles_filled = temporal_fill(angles, joint_uncertainty(keypoints))` → `return angles_filled, profile`.

    4. **`_angles_from_video` (line 255) — 시그너처 + 본체 무변경 (B8)**.

    5. **`_angles_and_video_path_from_video` (line 271) — 시그너처 + 본체 무변경 (B8)**.

    6. **`_process` (line 364~) — 변경 X (Phase 2 v1 scope, MEDIUM-1 v4)**: 기존 호출 그대로. `firestore_admin.complete_analysis(...)` 에 `body_normalization_profile=` kw 추가 X. 본 phase 의 helper `_angles_and_body_profile_from_video` 는 Phase 6 plan 이 호출 site 박제.

    7. **`firestore_admin.complete_analysis` — 변경 X (MEDIUM-1 v4)**: 시그너처 + payload 변경 X.

    8. **TS `AnalysisDoc` — 변경 X (MEDIUM-1 v4)**: `bodyNormalizationProfile?` 추가 X.

    9. **`docs/contract.md` AnalysisDoc 섹션 (82-97) — 변경 X (MEDIUM-1 v4)**.

    10. **통합 테스트 (`backend/tests/test_pipeline_body_profile_injection.py`) — HIGH-1 v4 race-safe 박제 (9 case)**:
       - `test_estimate_with_profile_returns_tuple`: fake `_engine` (3 PoseFrame) → `(np.ndarray (3,17,4), BodyNormalizationProfile)`.
       - `test_estimate_signature_unchanged_b8_lock`: `_RTMWNlfCompat.estimate(frames)` 시그너처 `-> np.ndarray`. **사이드카 폐기 검증**: `assert not hasattr(estimator, "last_body_profile")`.
       - `test_helper_returns_angles_and_profile_locally`: `_angles_and_body_profile_from_video(bucket, key)` mock S3 + fake estimator → tuple 반환.
       - **`test_concurrency_no_profile_leak_between_background_tasks` (HIGH-1 v4 핵심)**:
         - `concurrent.futures.ThreadPoolExecutor(max_workers=2)` + `threading.Barrier(2)`.
         - Barriered fake estimator: `estimate_with_profile` 가 `barrier.wait()` → 두 worker 동시 진입.
         - fake estimator frame 마다 unique profile (예: `arm_scale = id(frames) % 1000 / 1000`).
         - Worker A: frames_a → profile_a (arm_scale 정확). Worker B: frames_b → profile_b (arm_scale 정확).
         - `assert profile_a != profile_b`. Module-level `_POSE_ESTIMATOR` 글로벌 공유에도 leak 0.
       - **`test_stale_profile_not_leaked_on_failure` (HIGH-1 v4 핵심)**:
         - First call success → `profile_first`. Second call: `RuntimeError("simulated GPU OOM")` raise.
         - `with pytest.raises(RuntimeError):` — 예외 전파, 직전 profile 반환 X.
       - `test_b8_signature_lock_preserved`: `inspect.signature(app._angles_from_video)` / `_angles_and_video_path_from_video` / `_angles_and_body_profile_from_video` 시그너처 검증.
       - `test_estimate_injects_body_shape_into_all_frames`: 모든 pose_frame 의 body_shape 동일.
       - `test_short_frames_graceful_degrade`: T=3 → confidence=0+insufficient_frames 폴백, exception 0.
       - `test_to_coco17_array_unaffected`: body_shape 주입 전후 shape (T,17,4) 동일.

    11. **B8 박제 회귀 검증**: verify 에서 `pytest tests/test_pipeline_recognizer_switch.py::TestB8FixSignature -x` 별도 실행.

    No code in this plan.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_pipeline_body_profile_injection.py -x -v && pytest tests/test_pipeline_recognizer_switch.py::TestB8FixSignature -x -v && pytest tests/ -x</automated>
    회귀: 기존 pipeline 테스트 전부 PASS. `TestB8FixSignature` 두 테스트 PASS.
    helper: `grep -n "_angles_and_body_profile_from_video\|estimate_with_profile" backend/functions/pipeline/app.py | wc -l` ≥ 4.
    **사이드카 폐기 (HIGH-1 v4)**: `grep -c "last_body_profile" backend/functions/pipeline/app.py backend/shared/python/sunity_shared/firestore_admin.py app/src/types/analysis.ts docs/contract.md` → 0.
    **MEDIUM-1 v4 revert**: `git diff --stat backend/shared/python/sunity_shared/firestore_admin.py app/src/types/analysis.ts docs/contract.md` 출력 없음 (AnalysisDoc 행 변경 0).
  </verify>
  <acceptance_criteria>
    - 9개 통합 테스트 PASS (HIGH-1 v4 race-safe + concurrency barrier + stale-failure + B8 박제)
    - 기존 `tests/` suite 회귀 0
    - `TestB8FixSignature` 두 테스트 PASS
    - `_RTMWNlfCompat.estimate` 시그너처 무변경 (`-> np.ndarray`)
    - `_RTMWNlfCompat.estimate_with_profile` 신규 (`tuple[np.ndarray, BodyNormalizationProfile | None]`)
    - `_RTMWNlfCompat` 에 `last_body_profile` 박제 0
    - `_angles_and_body_profile_from_video` 신규 helper
    - `_angles_from_video` / `_angles_and_video_path_from_video` 시그너처 + 본체 무변경
    - **concurrency 테스트**: barriered 2 worker → 각자 자기 profile
    - **stale-failure 테스트**: 예외 시 직전 profile leak 0
    - **MEDIUM-1 v4 revert**: `firestore_admin.complete_analysis` / `AnalysisDoc` / `docs/contract.md` AnalysisDoc 무변경
    - 단일 commit 에 `pipeline/app.py` 1 파일 (+ test file)
  </acceptance_criteria>
  <done>위 acceptance_criteria 모두 충족.</done>
  <commit-template>
    feat(02): add race-safe _angles_and_body_profile_from_video helper + _RTMWNlfCompat.estimate_with_profile (HIGH-1 v4 — sidecar dropped, B8 lock preserved, Firestore deferred to Phase 6 per MEDIUM-1 v4)
  </commit-template>
  <depends-on>Task 3 (measure_body_profile)</depends-on>
</task>

<task type="auto">
  <name>Task 5a: R&D compare_body_profile.py + nlf_smplx_body_profile_loader.py (loader 본체 + β→Profile math + CLI) + extract_rtmw_body_profile_keypoints.py + smoke (HIGH-2 v4 + HIGH-3 v3 + HIGH-4 v3 + LOW-1 v3)</name>
  <read_first>
    backend/research/evaluations/compare_engines.py (R&D 패턴 + L-2 "/workspace/SunityMotion"),
    backend/research/evaluations/compare_rtmw_vs_ipsf.py (lines 717-733 `_load_pose_engine("rtmw")`; lines 232-258 `run_rtmw` — HIGH-4 v3 재사용),
    backend/shared/python/sunity_shared/analysis/body_normalization.py (BodyNormalizationProfile dataclass),
    backend/shared/python/sunity_shared/analysis/body_normalization_measurer.py (Task 3)
  </read_first>
  <action>
    Goal: NLF→SMPL-X β vs RTMW segment gap 보고 CLI scaffold + RTMW extraction pre-step helper + **HIGH-2 v4 — NLF→SMPL-X β path 본체 + β→Profile 변환 math 박제 (Task 5b 는 실행만)**. `backend/research/` 격리. D-02-04.

    **v4 박제**:
    - 신규 `nlf_smplx_body_profile_loader.py`: NLF 3D → SMPL-X β fit → β→BodyNormalizationProfile math (pure NumPy, CI 단위 테스트 가능) + CLI.
    - `compare_body_profile.py` 의 `load_nlf_smplx_keypoints` (반환 `list[PoseFrame]`) 폐기 → `load_nlf_smplx_profiles(path) -> dict[str, BodyNormalizationProfile]`.
    - `compute_profile_gap` 시그너처 `dict[str, BodyNormalizationProfile]` 입력.
    - Task 5b 는 박제 스크립트 4단계 실행만.

    **HIGH-3 v3 유지**: graceful-empty. 비존재/빈 dir → `{}` (raise X).
    **HIGH-4 v3 유지**: `extract_rtmw_body_profile_keypoints.py` scaffold.
    **LOW-1 v3 유지**: 모든 `python -m` repo root.

    1. **`backend/research/evaluations/nlf_smplx_body_profile_loader.py` 신설 (HIGH-2 v4 핵심)**:
       - 헤더 docstring: "NLF 3D keypoints → SMPL-X β fit → β→BodyNormalizationProfile 변환 (R&D 격리). D-02-04 박제: NLF/SMPL-X import 본 파일에서만. sunity_shared 무관. license-blocklist-pose memory — SMPL-X 상업 불가. 실행 환경: belle 사내 RunPod GPU pod. HIGH-2 v4 박제 (REVIEWS.md plan_revision v3): Task 5a 가 loader 본체 + β→Profile math 박제. Task 5b 는 실행만."
       - CLI argparse: `--videos`, `--output-dir` (default `reports/nlf_smplx_{date}/`), `--video-source` (S3 default), `--nlf-model-path` (env NLF_MODEL_PATH), `--smplx-model-path` (env SMPLX_MODEL_PATH).
       - 함수:
         - `_load_nlf_engine(model_path)` lazy import.
         - `_load_smplx_fitter(model_path)` lazy import.
         - `_extract_nlf_keypoints_per_frame(video_frames, nlf_engine) -> np.ndarray` (T,J,3).
         - `_fit_smplx_beta(nlf_keypoints_3d, smplx_fitter) -> np.ndarray` (shape 10,).
         - **`beta_to_body_profile(beta: np.ndarray) -> BodyNormalizationProfile` (HIGH-2 v4 핵심 — pure NumPy, weights 무관, CI 가능)**:
           - SMPL-X β 표준 mean shape vertex 위치에서 bone length 계산 (mean SMPL-X joint mapping 박제).
           - β → bone length scale factors → torso-relative ratios → 7 필드.
           - `estimated_height_scale = (arm_scale + leg_scale + 1.0) / 3` (Task 3 measurer 와 동일).
           - `confidence = 1.0` (deterministic, v1).
           - `warnings = []` (R&D, 정상 영상 가정).
           - body 가 SMPL-X weights 없이 호출 가능.
         - `extract(motion_id, output_dir, nlf_engine, smplx_fitter) -> Path` — 영상→NLF→β→profile→`{motion_id}.json` (field-level dict).
         - `main()` CLI entry.

    2. **`backend/research/evaluations/compare_body_profile.py` (HIGH-2 v4 refactor)**:
       - 헤더 docstring 갱신: "HIGH-2 v4 박제: loader 본체 + β→Profile math 는 nlf_smplx_body_profile_loader.py 책임. 본 파일은 두 profile dict 의 gap 계산 + 보고서만. HIGH-3 v3: load_nlf_smplx_profiles graceful-empty (비존재 dir → {})."
       - CLI: `--rtmw-keypoints-dir`, `--nlf-profiles-dir`, `--videos`, `--output`.
       - 함수:
         - `load_rtmw_profiles(path, videos) -> dict[str, BodyNormalizationProfile]`: 비존재/빈 → `{}`. 각 keypoint JSON → `list[PoseFrame]` (factory 재사용) → `measure_body_profile` → `dict[video] = profile`.
         - `load_nlf_smplx_profiles(path, videos) -> dict[str, BodyNormalizationProfile]` (HIGH-3 v3 graceful + HIGH-2 v4 dict): 비존재/빈 → `{}` (NotImplementedError X). 각 video JSON → `BodyNormalizationProfile` 복원 → `dict[video]`.
         - 명시 코멘트: "HIGH-2 v4 박제: nlf_smplx_body_profile_loader.py 산출 dir load. 반환형 dict[str, BodyNormalizationProfile] — v3 list[PoseFrame] 폐기 (per-video Profile 가 gap 단위). HIGH-3 v3: 비존재/빈 dir → {}."
         - `compute_profile_gap(rtmw_profiles, smplx_profiles) -> dict`: per-video 6 numeric 필드 abs diff + aggregate mean diff + `verdict` ("within_5pct_tolerance" if all <0.05 else "gap_too_wide").
         - `main()` JSON + Markdown.

    3. **`extract_rtmw_body_profile_keypoints.py` (HIGH-4 v3 유지)**: 헤더 (HIGH-4 v3 박제) + LOW-1 v3. CLI `--videos`, `--output-dir`, `--video-source`. 함수: `_load_video_frames`, `_detect_pole_axis` (vertical fallback), `extract(motion_id, output_dir)` — `RTMWPoseEngine()` + `run_rtmw` → `list[PoseFrame]` → `dataclasses.asdict` JSON. Lazy import.

    4. **운영 import 정합**:
       - `from sunity_shared.analysis.body_normalization_measurer import measure_body_profile` OK.
       - `from sunity_shared.analysis.body_normalization import BodyNormalizationProfile` OK.
       - NLF/SMPL-X import 는 `nlf_smplx_body_profile_loader.py` 함수 body 안 lazy.
       - RTMW import (extract.py) 함수 body 안.

    5. **Smoke (`backend/tests/test_compare_body_profile_smoke.py`) — 10 case**:
       - `test_argparse_help_compare` (LOW-1 v3): repo root `python -m ...compare_body_profile --help` → exit 0, "rtmw-keypoints-dir" + "nlf-profiles-dir".
       - `test_argparse_help_extract` (HIGH-4 v3): "--videos" + "--output-dir".
       - **`test_argparse_help_loader` (HIGH-2 v4)**: `python -m ...nlf_smplx_body_profile_loader --help` → "--videos" + "--output-dir" + "--nlf-model-path".
       - `test_compute_profile_gap_zero` / `test_compute_profile_gap_nonzero`.
       - `test_load_rtmw_profiles_empty_dir`: 비존재/빈 → `{}`.
       - **`test_load_nlf_smplx_profiles_returns_empty_for_missing_dir` (HIGH-3 v3)**: `load_nlf_smplx_profiles(Path("/tmp/nonexistent"), ["a"])` → `{}` (raise X).
       - `test_extract_rtmw_helper_callable_with_mock` (HIGH-4 v3): mock RTMW + frames → `extract` → `tmp_path/test.json`.
       - **`test_beta_to_body_profile_pure_numpy` (HIGH-2 v4 핵심)**: `beta = np.zeros(10)` → `beta_to_body_profile(beta)` → BodyNormalizationProfile (`__post_init__` 통과). mean shape ratios: arm_scale ∈ [0.9, 1.2], leg_scale ∈ [1.1, 1.5]. SMPL-X weights 호출 X.
       - **`test_beta_to_body_profile_non_zero_beta_changes_profile` (HIGH-2 v4)**: `beta_tall=[2,0,...]` vs `beta_short=[-2,0,...]` → `profile_tall.estimated_height_scale != profile_short.estimated_height_scale`.

    참조: REVIEWS.md v1→v2 HIGH-4 + v2→v3 HIGH-3 + v2→v3 HIGH-4 + v2→v3 LOW-1 + v3→v4 HIGH-2.

    No code in this plan.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_compare_body_profile_smoke.py -x -v</automated>
    `python -m backend.research.evaluations.compare_body_profile --help 2>&1 | grep -q "rtmw-keypoints-dir"`.
    `python -m backend.research.evaluations.compare_body_profile --help 2>&1 | grep -q "nlf-profiles-dir"`.
    `python -m backend.research.evaluations.extract_rtmw_body_profile_keypoints --help 2>&1 | grep -q -- "--videos"`.
    `python -m backend.research.evaluations.nlf_smplx_body_profile_loader --help 2>&1 | grep -q -- "--videos"`.
    HIGH-3 v3: `grep -c "NotImplementedError" backend/research/evaluations/compare_body_profile.py backend/research/evaluations/nlf_smplx_body_profile_loader.py` → 0.
    HIGH-2 v4: `grep -c "load_nlf_smplx_keypoints" backend/research/evaluations/compare_body_profile.py` → 0 (v3 함수명 폐기). `grep -c "load_nlf_smplx_profiles\|beta_to_body_profile" backend/research/evaluations/*.py` ≥ 3.
  </verify>
  <acceptance_criteria>
    - 10개 smoke test PASS (3 argparse + 2 gap math + load_rtmw empty + load_nlf_smplx graceful + extract mock + 2 beta_to_body_profile pure NumPy)
    - 3개 CLI repo root 실행 가능
    - HIGH-3 v3: graceful-empty (NotImplementedError 0)
    - HIGH-4 v3: extract helper 박제
    - **HIGH-2 v4**: `nlf_smplx_body_profile_loader.py` 신설 + `beta_to_body_profile` pure NumPy math + CI 단위 테스트
    - **HIGH-2 v4**: v3 `load_nlf_smplx_keypoints` 폐기 → `load_nlf_smplx_profiles` (`dict[str, BodyNormalizationProfile]`)
    - **HIGH-2 v4**: `compute_profile_gap` 시그너처 `dict[str, BodyNormalizationProfile]` 갱신
    - 운영 import 격리: NLF/SMPL-X 는 `nlf_smplx_body_profile_loader.py` 함수 body 안. RTMW 는 `extract.py` 함수 body 안.
  </acceptance_criteria>
  <done>위 acceptance_criteria 모두 충족.</done>
  <commit-template>
    chore(02): scaffold R&D compare_body_profile + extract_rtmw_body_profile_keypoints + nlf_smplx_body_profile_loader with beta_to_body_profile math (HIGH-2 v4 + HIGH-3 v3 + HIGH-4 v3 + LOW-1 v3)
  </commit-template>
  <depends-on>Task 3 (BodyNormalizationProfile 측정기)</depends-on>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5b: belle Pod 박제 스크립트 4단계 실행 (extract → loader → compare → commit) — execution-only, HIGH-2 v4, ROADMAP §4 closure gate</name>
  <files>
    backend/research/evaluations/reports/sweep_rtmw_&lt;YYYYMMDD&gt;/keypoints/ref-foxtop.json (create — Step (a)),
    backend/research/evaluations/reports/sweep_rtmw_&lt;YYYYMMDD&gt;/keypoints/ref-foxtop-split.json (create — Step (a)),
    backend/research/evaluations/reports/sweep_rtmw_&lt;YYYYMMDD&gt;/keypoints/ref-invert.json (create — Step (a)),
    backend/research/evaluations/reports/sweep_rtmw_&lt;YYYYMMDD&gt;/keypoints/ref-sideway-spin.json (create — Step (a)),
    backend/research/evaluations/reports/sweep_rtmw_&lt;YYYYMMDD&gt;/keypoints/ref-climb.json (create — Step (a)),
    backend/research/evaluations/reports/nlf_smplx_&lt;YYYYMMDD&gt;/ref-foxtop.json (create — Step (b), HIGH-2 v4 박제 loader 산출),
    backend/research/evaluations/reports/nlf_smplx_&lt;YYYYMMDD&gt;/ref-foxtop-split.json (create — Step (b)),
    backend/research/evaluations/reports/nlf_smplx_&lt;YYYYMMDD&gt;/ref-invert.json (create — Step (b)),
    backend/research/evaluations/reports/nlf_smplx_&lt;YYYYMMDD&gt;/ref-sideway-spin.json (create — Step (b)),
    backend/research/evaluations/reports/nlf_smplx_&lt;YYYYMMDD&gt;/ref-climb.json (create — Step (b)),
    backend/research/evaluations/reports/body_profile_gap_&lt;YYYYMMDD&gt;.json (create — Step (c)),
    backend/research/evaluations/reports/body_profile_gap_&lt;YYYYMMDD&gt;.md (create — Step (c))
  </files>
  <read_first>
    backend/research/evaluations/extract_rtmw_body_profile_keypoints.py (Task 5a 박제),
    backend/research/evaluations/nlf_smplx_body_profile_loader.py (Task 5a 박제 — HIGH-2 v4 loader 본체),
    backend/research/evaluations/compare_body_profile.py (Task 5a 박제),
    docs/licenses/rtmw-weights-audit.md
  </read_first>
  <action>
    Goal (HIGH-2 v4): ROADMAP success criterion 4 closure. **execution-only — 설계/구현 결정 0**. Task 5a 가 박제한 3개 스크립트를 belle Pod 에서 순서대로 실행 + git commit.

    **HIGH-2 v4 박제 (v3 와 차이)**:
    - v3: belle 이 `load_nlf_smplx_keypoints` stub 본체를 belle Pod 에서 실 구현.
    - v4: Task 5a 가 `nlf_smplx_body_profile_loader.py` 본체 (NLF→SMPL-X β fit + β→Profile math + CLI) 박제 완료. belle 은 `python -m ... nlf_smplx_body_profile_loader ...` 실행만.

    **LOW-1 v3 박제 유지**: 모든 `python -m` 은 repo root.
  </action>
  <what-built>
    Task 5a 가 다음 3개 박제 스크립트 + smoke test commit 완료:
    - `compare_body_profile.py` (gap math + CLI).
    - `nlf_smplx_body_profile_loader.py` (HIGH-2 v4 — NLF→SMPL-X β fit + β→Profile math + CLI 본체).
    - `extract_rtmw_body_profile_keypoints.py` (HIGH-4 v3 — RTMW dump CLI).
  </what-built>
  <how-to-verify>
    belle 가 박제 스크립트 4단계 실행 (모든 `python -m` 은 repo root, LOW-1 v3):

    **Pod ssh + env 확인 (이미 박제)**:

    1. ssh + `cd /workspace/SunityMotion` + `git pull origin main`.
    2. Pod env 확인: `echo $RTMW_ONNX_PATH $YOLOX_ONNX_PATH $NLF_MODEL_PATH $SMPLX_MODEL_PATH` → 4개 절대 경로.

    **Step (a) — RTMW keypoint dump 추출 (박제 스크립트)**:

    3. `cd /workspace/SunityMotion`
       `export DATE=$(date +%Y%m%d)`
       `python -m backend.research.evaluations.extract_rtmw_body_profile_keypoints --videos ref-foxtop ref-foxtop-split ref-invert ref-sideway-spin ref-climb --output-dir backend/research/evaluations/reports/sweep_rtmw_${DATE}/keypoints/`

       산출: `sweep_rtmw_${DATE}/keypoints/<video>.json` × 5.

    **Step (b) — NLF→SMPL-X β path 실행 (박제 스크립트, HIGH-2 v4)**:

    4. `python -m backend.research.evaluations.nlf_smplx_body_profile_loader --videos ref-foxtop ref-foxtop-split ref-invert ref-sideway-spin ref-climb --output-dir backend/research/evaluations/reports/nlf_smplx_${DATE}/`

       산출: `nlf_smplx_${DATE}/<video>.json` × 5 (각 BodyNormalizationProfile field-level dict).
       belle 은 본 스크립트 본체 편집 X — Task 5a 박제 그대로 실행.

    **Step (c) — gap 보고서 산출 (박제 스크립트)**:

    5. `python -m backend.research.evaluations.compare_body_profile --rtmw-keypoints-dir backend/research/evaluations/reports/sweep_rtmw_${DATE}/keypoints/ --nlf-profiles-dir backend/research/evaluations/reports/nlf_smplx_${DATE}/ --output backend/research/evaluations/reports/body_profile_gap_${DATE}.json`

       산출: `body_profile_gap_${DATE}.json` + `.md`.

    **Step (d) — git commit**:

    6. `git add backend/research/evaluations/reports/sweep_rtmw_${DATE}/keypoints/*.json backend/research/evaluations/reports/nlf_smplx_${DATE}/*.json backend/research/evaluations/reports/body_profile_gap_${DATE}.*`
       `git commit -m "data(02): belle Pod 5-video RTMW + NLF→SMPL-X β + gap report (HIGH-2 v4 + HIGH-4 v3 ROADMAP §4 closure)"`

    **종료 신호**:
    - belle "approved" + 파일 경로 응답.
    - 또는 환경 문제 시 "not feasible — defer to v1.5" → ROADMAP §4 v1.5 defer 박제.
  </how-to-verify>
  <resume-signal>belle 가 "approved" + `body_profile_gap_<date>.json` 경로 응답, 또는 "not feasible — defer to v1.5" 응답 시 다음 task 진행</resume-signal>
  <acceptance_criteria>
    - **Step (a)**: `sweep_rtmw_<date>/keypoints/<video>.json` 5개 체크인
    - **Step (b) HIGH-2 v4**: `nlf_smplx_<date>/<video>.json` 5개 체크인 (loader 본체 편집 0)
    - **Step (c)**: `body_profile_gap_<date>.{json,md}` 체크인
    - **Step (d)**: 단일 git commit 에 12 파일
    - 또는 belle "not feasible" + ROADMAP §4 v1.5 defer 박제
    - 모든 `python -m` repo root 실행
    - 본 task closed 시에만 phase verifier 가 ROADMAP success criterion 4 PASS
  </acceptance_criteria>
  <done>위 둘 중 하나 충족.</done>
  <commit-template>
    data(02): belle Pod 5-video RTMW + NLF→SMPL-X β + gap report (HIGH-2 v4 + HIGH-4 v3 ROADMAP §4 closure)
  </commit-template>
  <depends-on>Task 5a (3개 박제 스크립트 + smoke)</depends-on>
</task>

<task type="auto">
  <name>Task 6: AST import isolation 확장 (sunity_shared + functions + runpod_inference + importlib + __import__ + spec_from_file_location literal — MEDIUM-1 v3 + LOW-2 v4) + REQUIREMENTS.md BODY-01 RTMW 정합 + ROADMAP success criterion 1 v1.5 deferral (MEDIUM-2 v4)</name>
  <read_first>
    backend/shared/python/sunity_shared/analysis/pose_engines/__init__.py (lazy mediapipe — HIGH-3 root cause),
    backend/shared/python/sunity_shared/analysis/pose_engines/mediapipe_engine.py (live mediapipe lines 74-76, 197),
    backend/functions/pipeline/app.py (MEDIUM-1 v3 대상),
    backend/runpod_inference/server.py (MEDIUM-1 v3 대상, line 31 `import importlib.util` + line 73 `spec_from_file_location` legitimate 사용 확인),
    .planning/REQUIREMENTS.md (line 30 BODY-01),
    .planning/ROADMAP.md (line 113 Phase 2 success criterion 1 — MEDIUM-2 v4 갱신 대상),
    backend/research/evaluations/compare_body_profile.py (Task 5a),
    backend/research/evaluations/extract_rtmw_body_profile_keypoints.py (Task 5a),
    backend/research/evaluations/nlf_smplx_body_profile_loader.py (Task 5a — HIGH-2 v4)
  </read_first>
  <action>
    Goal:
    (1) **AST 스캔 확장 (LOW-2 v4)**: deployable product (sunity_shared + backend/functions + backend/runpod_inference) 가 `backend.research` / `research` / `nlf` / `smplx` / `smpl_x` import 시 fail. `importlib.import_module(literal)` + `__import__(literal)` + `importlib.util.spec_from_file_location(name, literal_path)` 모두 차단. HIGH-3 v1→v2 유지 — `mediapipe` 제외.
    (2) **REQUIREMENTS.md BODY-01** "MediaPipe" → "RTMW".
    (3) **ROADMAP.md Phase 2 success criterion 1 acceptance text 갱신 (MEDIUM-2 v4)**.

    **MEDIUM-3 v3 유지**: Task 5a 만 의존. Task 5b 와 평행.

    **(1) `backend/tests/test_research_import_isolation.py` 신설 (MEDIUM-1 v3 + LOW-2 v4)**:

    - 파일 상단 docstring:
      "Phase 2 R&D 격리 + 라이선스 차단 AST 자동 검증. MEDIUM-1 v3: 스캔 범위 sunity_shared + backend/functions + backend/runpod_inference. LOW-2 v4 박제 (REVIEWS.md plan_revision v3): importlib.import_module + __import__ + spec_from_file_location literal 모두 차단. 잔여 위험: 비-literal 동적 import (variable 인자) 는 정적 분석 한계 — data-flow 분석 필요. PR 단계 manual review 정합. v1→v2 HIGH-3 박제 유지 — mediapipe 차단 제외."

    - `ast.parse` 로 3 디렉터리 `.py` 스캔: `backend/shared/python/sunity_shared/` + `backend/functions/` + `backend/runpod_inference/`. 제외: `backend/tests/`, `backend/research/`.

    - 금지 정적 import: `import research`, `import backend.research`, `from research import ...`, `from backend.research import ...`, `import nlf`, `from nlf import ...`, `import smplx`, `from smplx import ...`, `import smpl_x`, `from smpl_x import ...`.

    - **LOW-2 v4 박제 — 동적 3종 차단**:
      - **`importlib.import_module(literal)` (v3 유지)**: `ast.Call` 의 `node.func: ast.Attribute` + `node.func.attr == "import_module"` + 첫 positional arg 가 `ast.Constant` + value 가 차단 prefix.
      - **`__import__(literal)` (LOW-2 v4)**: `ast.Call` 의 `node.func: ast.Name` + `node.func.id == "__import__"` + 첫 positional arg 가 `ast.Constant` + value 가 차단 prefix.
      - **`spec_from_file_location(name, literal)` (LOW-2 v4)**: `ast.Call` 의 `node.func: ast.Attribute` + `node.func.attr == "spec_from_file_location"` + 두 번째 positional arg (`location`) 가 `ast.Constant` + value 의 basename 또는 path component 가 차단 모듈명 매치.
        - **runpod_inference/server.py:73 legitimate**: `spec_from_file_location("sunity_pipeline_app", pipeline_path)` — `pipeline_path` 가 변수 + name "sunity_pipeline_app" — 매치 0 → PASS.

    - 잔여 위험 TODO (파일 상단):
      "TODO(post-v4): variable 인자 동적 import (`importlib.import_module(var)`, `__import__(var)`) 는 정적 분석 불가. PR review + grep gate manual 검증. Phase 6+ data-flow 분석 고려."

    - **`mediapipe` 차단 제외 박제 유지 (v1→v2 HIGH-3)**: 큰 TODO 코멘트.

    - **테스트 함수 5개 (LOW-2 v4)**:
      - `test_sunity_shared_does_not_import_research_or_nlf_or_smplx`.
      - `test_backend_functions_does_not_import_research_or_nlf_or_smplx`.
      - `test_backend_runpod_inference_does_not_import_research_or_nlf_or_smplx`.
      - `test_no_dynamic_importlib_import_of_research_or_nlf_or_smplx` (importlib literal).
      - **`test_no_dynamic_dunder_import_or_spec_from_file_location_of_research_or_nlf_or_smplx` (LOW-2 v4)**: 3 디렉터리 `__import__(literal)` + `spec_from_file_location(name, literal_path)` 매치 0.

    - pytest < 1초 (순수 AST).

    **(2) `.planning/REQUIREMENTS.md` line 30**:
    - 현재: `"**BODY-01**: MediaPipe 키포인트로부터 ...`
    - 갱신: `"**BODY-01**: RTMW 133 wholebody 키포인트로부터 ...`. 뒷부분 유지.
    - footer note: `*Updated 2026-06-07 — Phase 2 plan 01 RTMW pivot 정합 (BODY-01 MediaPipe → RTMW 갱신, v4 박제).*`.
    - POSE-01 (line 27) MediaPipe 언급 — **본 task scope X**.

    **(3) `.planning/ROADMAP.md` Phase 2 success criterion 1 갱신 (MEDIUM-2 v4)**:
    - 현재 (line 113): `1. RTMW 키포인트에서 segment 길이가 시간 평균으로 안정적으로 추출된다 (jitter 스무딩)`
    - 갱신: `1. RTMW 키포인트에서 segment 길이가 시간 평균으로 안정적으로 추출된다 (jitter 스무딩) — v1 acceptance: synthetic + RTMW adapter-path validation; real RTMW network output coverage 는 v1.5 sweep 실행 (belle Pod 5영상 keypoint dump 산출) 으로 deferred (MEDIUM-2 v4 박제)`.

    No code in this plan.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_research_import_isolation.py -x -v</automated>
    REQUIREMENTS: `grep -n "RTMW" .planning/REQUIREMENTS.md | grep "BODY-01"` → 1. `grep "MediaPipe" .planning/REQUIREMENTS.md | grep "BODY-01"` → 0.
    HIGH-3 박제: `grep -c "mediapipe" backend/tests/test_research_import_isolation.py` ≥ 1 (TODO).
    MEDIUM-1 v3: `grep -c "backend/functions\|backend/runpod_inference\|importlib" backend/tests/test_research_import_isolation.py` ≥ 3.
    **LOW-2 v4**: `grep -c "__import__\|spec_from_file_location" backend/tests/test_research_import_isolation.py` ≥ 2.
    **MEDIUM-2 v4**: `grep "v1.5 sweep" .planning/ROADMAP.md` ≥ 1.
  </verify>
  <acceptance_criteria>
    - 5개 AST 테스트 PASS
    - MEDIUM-1 v3: 스캔 3 디렉터리
    - **LOW-2 v4**: `__import__(literal)` + `spec_from_file_location(name, literal_path)` 차단
    - 잔여 위험 TODO 박제
    - `runpod_inference/server.py:73` legitimate 사용 → 매치 0 → PASS
    - mediapipe 차단 코드 없음 (TODO 만)
    - REQUIREMENTS.md BODY-01 RTMW 갱신, MediaPipe 제거
    - REQUIREMENTS.md footer note 추가
    - **MEDIUM-2 v4**: ROADMAP Phase 2 success criterion 1 "v1.5 sweep 실행으로 deferred" 박제
    - 단일 atomic commit 에 test + REQUIREMENTS.md + ROADMAP.md
  </acceptance_criteria>
  <done>위 acceptance_criteria 모두 충족.</done>
  <commit-template>
    chore(02): expand AST R&D import isolation (functions+runpod_inference+importlib+__import__+spec_from_file_location, MEDIUM-1 v3 + LOW-2 v4) + align BODY-01 to RTMW + ROADMAP §1 v1.5 deferral (MEDIUM-2 v4)
  </commit-template>
  <depends-on>Task 5a (R&D harness 존재 — MEDIUM-3 v3: Task 5b 무관, belle Pod 평행)</depends-on>
</task>

<task type="auto">
  <name>Task 7: estimatedHeightScale consumer semantic guard lint test (MEDIUM-3 v4 — vacuous PASS now, Phase 6+ load-bearing)</name>
  <read_first>
    backend/shared/python/sunity_shared/analysis/body_normalization.py (estimated_height_scale 정의),
    backend/shared/python/sunity_shared/ (consumer scan 대상)
  </read_first>
  <action>
    Goal (MEDIUM-3 v4): `estimatedHeightScale` / `estimated_height_scale` identifier 가 sunity_shared 에 등장 시 인접 (±3 라인) 에 "torso-relative proportion heuristic" 주석 박제했는지 lint-style scan. **현재 consumer 0 → vacuous PASS**. Phase 6+ consumer 추가 시 load-bearing.

    **Rename Option B reject 사유**: 필드 rename 은 3-way lockstep + CONTEXT.md + dataclass 모두 깨짐. Option A (lint) 만 채택.

    1. **`backend/tests/test_estimated_height_scale_consumer_semantics.py` 신설**:
       - 헤더 docstring: "estimatedHeightScale / estimated_height_scale 의 모든 sunity_shared consumer 가 인접 (±3 라인) 에 'torso-relative proportion heuristic' 주석 박제했는지 lint scan. MEDIUM-3 v4 박제 (REVIEWS.md plan_revision v3): 필드명이 absolute height 처럼 보이는 risk 차단. 현재 consumer 0 → vacuous PASS. Phase 6+ consumer 추가 시 load-bearing."

    2. **스캔 함수 `_scan_consumer_lines(root: Path) -> list[ConsumerSite]`**:
       - `root.rglob("*.py")` 전체.
       - 제외: `body_normalization.py` 자체, `body_normalization_measurer.py` 자체.
       - 정규식 `r"\b(estimatedHeightScale|estimated_height_scale)\b"` 매치.
       - `ConsumerSite(file=Path, line=int, content=str)` 박제.

    3. **검증 함수 `_assert_semantic_comment_nearby(site)`**:
       - 매치 line ±3 라인 안에 `"torso-relative proportion heuristic"` substring 검사.
       - 없으면 `assert False, f"MEDIUM-3 v4 박제 위반: {site.file}:{site.line} — estimatedHeightScale 사용 시 인접 (±3 라인) 에 'torso-relative proportion heuristic' 주석 박제 필수. 본 필드는 absolute height 아님 — torso 길이 대비 사지 비율 heuristic (per Phase 2 Task 1 lockstep)."`.

    4. **테스트 `test_estimated_height_scale_consumers_have_semantic_comment`**:
       - `sites = _scan_consumer_lines(Path("backend/shared/python/sunity_shared"))`.
       - **현재 sites == []** → vacuous PASS.
       - 각 site `_assert_semantic_comment_nearby`.
       - Phase 6+ consumer 추가 시 즉시 fail → Phase 6 plan 박제 게이트 강제.

    5. **테스트 `test_scan_excludes_definition_files`**:
       - 정의 파일 (body_normalization.py / body_normalization_measurer.py) 의 정의 line 결과에 포함 0.

    No code in this plan.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_estimated_height_scale_consumer_semantics.py -x -v</automated>
    vacuous PASS: `grep -rn "estimatedHeightScale\|estimated_height_scale" backend/shared/python/sunity_shared/ | grep -v "body_normalization.py\|body_normalization_measurer.py" | wc -l` → 0.
  </verify>
  <acceptance_criteria>
    - 2개 테스트 PASS (vacuous + definition exclusion)
    - 현재 consumer 0 → vacuous PASS
    - 스캔 함수 정의 파일 제외
    - `_assert_semantic_comment_nearby` 박제 — Phase 6+ 강제
    - assert 메시지 "torso-relative proportion heuristic" 박제 요구 명시
  </acceptance_criteria>
  <done>위 acceptance_criteria 모두 충족.</done>
  <commit-template>
    test(02): add estimatedHeightScale consumer semantic guard lint (MEDIUM-3 v4 — vacuous PASS now, load-bearing in Phase 6+)
  </commit-template>
  <depends-on>Task 1 (3-way lockstep 박제 — "torso-relative proportion heuristic" 정의 측 박제 정합)</depends-on>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RunPod Pod ↔ Lambda | RTMW pose_frames 가 RunPod 에서 측정 후 Lambda 가 결과 받음 — Phase 2 는 내부 호출만 |
| sunity_shared + backend/functions + backend/runpod_inference (운영) ↔ backend.research (R&D) | 라이선스 격리. **MEDIUM-1 v3 + LOW-2 v4 박제 — 스캔 범위 확장 + 3종 동적 import 차단** |
| `_RTMWNlfCompat.estimate_with_profile` (Pod, race-safe local-return) ↔ caller helper (`_angles_and_body_profile_from_video`) | BodyNormalizationProfile propagation 경계 — RunPod FastAPI BackgroundTasks 환경에서 concurrent analyses 가 module-level `_POSE_ESTIMATOR` 글로벌 공유해도 local tuple 로 leak 0 (HIGH-1 v4). Phase 6 plan 이 본 helper 호출하여 Firestore wire-up (Phase 2 scope 외 — MEDIUM-1 v4) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | BodyNormalizationProfile confidence [0, 1] 범위 벗어남 | mitigate | dataclass `__post_init__` validator 박제. NaN → 0.0 폴백 |
| T-02-02 | Repudiation / 법적 | 운영 코드가 backend.research / SMPL-X / NLF import (라이선스 위반) | mitigate | Task 6 AST 자동 차단 (HIGH-3 narrowed, MEDIUM-1 v3 3 디렉터리, **LOW-2 v4 — importlib + __import__ + spec_from_file_location literal 3종**). CI gate. 잔여 위험: 비-literal 동적 import (정적 분석 한계, PR manual review) |
| T-02-03 | DoS | 인버트/짧은 영상 NaN profile → 다운스트림 crash | mitigate | `insufficient_frames` warning + conf=0 + scale=1.0 폴백. `__post_init__` finite 강제 |
| T-02-04 | Info Disclosure | R&D 보고서 사용자 영상 메타데이터 leak | accept | belle Pod 내부 실행만. private repo |
| T-02-05 | Tampering | `_RTMWNlfCompat` 시그너처 변경이 B8 박제 위반 | mitigate | **HIGH-1 v4 race-safe local-return helper**: 기존 `estimate(frames) -> np.ndarray` 무변경, 신규 `estimate_with_profile(frames) -> tuple[np.ndarray, BodyNormalizationProfile \| None]` 박제. `_angles_from_video` / `_angles_and_video_path_from_video` 무변경. Task 4 `test_b8_signature_lock_preserved` + verify `pytest tests/test_pipeline_recognizer_switch.py::TestB8FixSignature -x` |
| T-02-06 | Tampering | Module-level `_POSE_ESTIMATOR` 글로벌 + RunPod `BackgroundTasks` 동시 호출 시 profile leak | mitigate | **HIGH-1 v4 race-safe local-return + 사이드카 폐기**: `last_body_profile` mutable instance attribute 영구 폐기. `estimate_with_profile` local tuple → caller 자기 frame 의 profile 만. Task 4 `test_concurrency_no_profile_leak_between_background_tasks` (barriered ThreadPoolExecutor) + `test_stale_profile_not_leaked_on_failure` 가 mutable shared state 0 검증 |
| T-02-07 | Info Disclosure | Phase 2 가 Firestore 저장 시 CONTEXT.md §1 v1 scope 위반 + Phase 6 미구현 상태 dangling field | mitigate | **MEDIUM-1 v4 박제 (revert)**: Phase 2 v1 scope = 측정기 본체 + helper 함수만. Firestore 저장 + AnalysisDoc 박제는 Phase 6 plan 책임. `firestore_admin.complete_analysis` / `AnalysisDoc` / `docs/contract.md` AnalysisDoc 변경 X. CONTEXT.md §1 + §8 정합 |

> 본 phase 외부 입력 X. V2/V3/V4/V6 적용 X.
</threat_model>

<verification>
## Phase-level 검증 (전 task 완료 후)

1. **단위 테스트 전체**: `cd backend && pytest tests/ -x` — 회귀 0.
2. **3-way lockstep (L1, LOW-1 v4 호칭)**: `pytest tests/test_body_normalization_lockstep.py -x` PASS (7 — docstring "string/regex 검증, AST 아님").
3. **Fixture smoke**: `pytest tests/fixtures/body_normalization/test_fixtures_loadable.py -x` PASS.
4. **Measurer (HIGH-2 v3)**: `pytest tests/test_body_normalization_measurer.py -x` PASS (11 case).
5. **Pipeline 통합 race-safe (HIGH-1 v4)**: `pytest tests/test_pipeline_body_profile_injection.py -x` PASS (9 — concurrency barrier + stale-failure + B8 박제).
6. **B8 박제 (HIGH-1 v4 핵심)**: `pytest tests/test_pipeline_recognizer_switch.py::TestB8FixSignature -x -v` PASS — 2 테스트.
7. **R&D 격리 (HIGH-3 v1→v2 + MEDIUM-1 v3 + LOW-2 v4)**: `pytest tests/test_research_import_isolation.py -x` PASS (5 — sunity_shared + functions + runpod_inference + importlib literal + __import__/spec_from_file_location literal).
8. **R&D harness smoke (HIGH-3 v3 + HIGH-4 v3 + HIGH-2 v4 + LOW-1 v3)**: `pytest tests/test_compare_body_profile_smoke.py -x` PASS (10).
9. **estimatedHeightScale consumer semantics (MEDIUM-3 v4)**: `pytest tests/test_estimated_height_scale_consumer_semantics.py -x` PASS (2, vacuous).
10. **HIGH-4 v3 + HIGH-2 v4 게이트**: `ls .../sweep_rtmw_*/keypoints/*.json` ≥ 5 + `ls .../nlf_smplx_*/*.json` ≥ 5 + `ls .../body_profile_gap_*.json` ≥ 1 (또는 v1.5 defer).
11. **REQUIREMENTS**: BODY-01 "RTMW" + "MediaPipe" 미등장.
12. **MEDIUM-2 v4**: ROADMAP Phase 2 success criterion 1 "v1.5 sweep" 박제 검증.
13. **의존성**: `grep -r "import scipy" backend/shared/python/sunity_shared/analysis/body_normalization_measurer.py` → 0.
14. **HIGH-1 v4 사이드카 폐기**: `grep -c "last_body_profile" backend/functions/pipeline/app.py backend/shared/python/sunity_shared/firestore_admin.py app/src/types/analysis.ts docs/contract.md` → 0.
15. **HIGH-1 v4 helper**: `grep -n "_angles_and_body_profile_from_video\|estimate_with_profile" backend/functions/pipeline/app.py | wc -l` ≥ 4.
16. **MEDIUM-1 v4 revert**: Task 4 `git diff --stat backend/shared/python/sunity_shared/firestore_admin.py app/src/types/analysis.ts docs/contract.md` → AnalysisDoc 변경 0.
17. **HIGH-3 박제**: `grep -c "mediapipe" backend/tests/test_research_import_isolation.py` ≥ 1 (TODO).
18. **LOW-1 v3**: Task 5a verify `python -m backend.research.evaluations.compare_body_profile --help` repo root.
</verification>

<success_criteria>
## ROADMAP Success Criteria 매핑 (v4 박제)

| Criterion | Task | 검증 | 본 phase closure 가능? |
|-----------|------|------|--|
| 1. RTMW segment 길이 시간 평균 안정 추출 — **v1 acceptance: synthetic + adapter-path; real RTMW network output v1.5 sweep deferred (MEDIUM-2 v4)** | Task 3 + Task 2b skip | `test_normal_pose_profile_valid` + RTMW sanity skip | 예 (v1 + v1.5 deferral) |
| 2. BodyNormalizationProfile 7필드 산출 | Task 3 | `test_profile_validator_passes` | 예 |
| 3. confidence 낮을 때 warnings 표기 | Task 3 (5종 + HIGH-2 v3) | 6개 warning 테스트 | 예 |
| 4. R&D 비교군 NLF→SMPL-X β 갭 보고서 | Task 5a (scaffold + extract + nlf_smplx_body_profile_loader 본체) + **Task 5b (belle Pod execution-only 4단계)** | scaffold smoke + 실 보고서 (5 keypoint + 5 nlf_smplx + 1 gap) | **Task 5b 완료 시에만 — HIGH-2 v4 + HIGH-4 v3 게이트** |
| 5. 데이터 계약 3-way lockstep (Phase 2 v1 scope — AnalysisDoc 은 Phase 6) | Task 1 | `test_body_normalization_lockstep` (7) | 예 |

**v4 Done (executor scope)**: criterion 1 (v1 + v1.5 deferral), 2, 3, 5 PASS + criterion 4 의 5a scaffold + 3개 박제 스크립트 PASS + Phase 1 회귀 0 + Pipeline race-safe verified + B8 박제 보존 + REQUIREMENTS BODY-01 RTMW + ROADMAP §1 v1.5 deferral.

**ROADMAP §4 closure**: Task 5b 완료 시. autonomous=false 박제.

**Phase 6 unblock**: `_angles_and_body_profile_from_video(bucket, key) -> tuple[np.ndarray, BodyNormalizationProfile | None]` helper + `_RTMWNlfCompat.estimate_with_profile` + `measure_body_profile` 박제 완료. Phase 6 plan 이 wire-up (Firestore 저장 + AnalysisDoc 박제 + 다운스트림 normalize) 입력 완성.
</success_criteria>

<source_audit>
## Multi-Source Coverage Audit (v4 박제 — Codex 3차 review 정합)

### GOAL (ROADMAP)
| Item | Plan | Task |
|------|------|------|
| RTMW 키포인트 → BodyNormalizationProfile 자동 출력 | 02-01 | Task 3 |
| 두 엔진 공유 입력 | 02-01 | Task 4 (race-safe helper — Phase 6 가 wire-up; HIGH-1 v4 + MEDIUM-1 v4) |
| SMPL-X β R&D 비교군만 | 02-01 | Task 5a (HIGH-2 v4 loader 본체), Task 5b (execution-only), Task 6 (import 차단 MEDIUM-1 v3 + LOW-2 v4) |

### REQ (REQUIREMENTS.md)
| Req ID | Plan | Task |
|--------|------|------|
| BODY-01 | 02-01 | Task 3 (측정기) + Task 4 (race-safe helper HIGH-1 v4) + Task 6 (RTMW 정합 + ROADMAP v1.5 deferral) |

### RESEARCH (02-RESEARCH.md)
| Feature | Plan | Task |
|---------|------|------|
| body_normalization_measurer.py (순수 numpy) | 02-01 | Task 3 |
| MAD outlier + robust median | 02-01 | Task 3 |
| torso self-reference normalize | 02-01 | Task 3 |
| 5종 warnings enum | 02-01 | Task 1 (3-way + L1) + Task 3 (HIGH-2 v3) |
| 5 synthetic fixture (RTMW adapter) | 02-01 | Task 2 |
| body_shape 주입 + propagation | 02-01 | Task 4 (HIGH-1 v4 race-safe estimate_with_profile + local-return helper, 사이드카 폐기) |
| R&D 비교 harness | 02-01 | Task 5a (scaffold + math), Task 5b (실행) |
| AST import isolation (HIGH-3 v1→v2 + MEDIUM-1 v3 + LOW-2 v4) | 02-01 | Task 6 |
| Phase 1 sweep keypoint dump (M1) | DEFERRED v1.5 (MEDIUM-2 v4) | Task 2 TODO + Task 2b skip + Task 6 ROADMAP §1 갱신 |
| Firestore 저장 (CONTEXT.md §1 + §8) | **REVERTED — Phase 6 (MEDIUM-1 v4)** | Phase 6 plan 책임 |
| contract.md AnalysisDoc bodyNormalizationProfile | **REVERTED (MEDIUM-1 v4)** | Phase 6 plan 책임 |
| extract_rtmw_body_profile_keypoints.py (HIGH-4 v3) | 02-01 | Task 5a, Task 5b |
| **nlf_smplx_body_profile_loader.py 본체 + β→Profile math (HIGH-2 v4)** | 02-01 | Task 5a (math + CLI), Task 5b (실행) |

### CONTEXT (02-CONTEXT.md Locked Decisions)
| Decision ID | Plan | Task |
|-------------|------|------|
| D-02-01 (RTMW 백본) | 02-01 | Task 3, Task 6 |
| D-02-02 (3-way lockstep) | 02-01 | Task 1 (Phase 2 v1 scope, AnalysisDoc 은 Phase 6) |
| D-02-03 (confidence/warnings) | 02-01 | Task 3 |
| D-02-04 (SMPL-X R&D 만) | 02-01 | Task 5a, Task 5b, Task 6 |
| D-02-05 (자동 측정 우선) | 02-01 | Task 3 |
| D-02-06 (실 영상 BodyProfile v1) | 02-01 | Task 2, Task 2b |

### REVIEWS (02-REVIEWS.md — Codex v3 review)
| Concern | Severity | v4 Task | Notes |
|---|---|---|---|
| **HIGH-1 v4**: `last_body_profile` 사이드카 race / leak | HIGH | Task 4 (race-safe local-return helper + 사이드카 폐기) | concurrency barrier + stale-failure |
| **HIGH-2 v4**: Task 5b 가 설계/구현 요구 + 반환형 mismatch | HIGH | Task 5a (nlf_smplx_body_profile_loader.py + β→Profile math), Task 5b (execution-only 4단계) | 반환형 `dict[str, BodyNormalizationProfile]` |
| **MEDIUM-1 v4**: Firestore 저장이 CONTEXT.md §1 위반 | MED | Task 4 (Firestore + AnalysisDoc 변경 0 — revert), Task 1 (3-way) | Phase 6 책임 |
| **MEDIUM-2 v4**: 실 RTMW coverage v1.5 deferred | MED | Task 6 (ROADMAP §1 acceptance text "v1.5 sweep deferred") | Task 2b skip-if-no-rtmw 유지 |
| **MEDIUM-3 v4**: `estimatedHeightScale` 필드명 misleading | MED | Task 7 (lint test — vacuous PASS, Phase 6+ load-bearing) | Option A lint, rename reject |
| **LOW-1 v4**: "AST lockstep" 호칭 mismatch | LOW | Task 1 (string/regex 호칭 갱신; Task 6 만 진짜 AST) | 함수명 무변경 |
| **LOW-2 v4**: 동적 import 차단 partial | LOW | Task 6 (AST scanner 3종 literal 차단) | 비-literal residual TODO |
| HIGH-2 v3 (y-down convention) | HIGH | Task 2 + Task 3 | 유지 |
| HIGH-3 v3 (graceful-empty) | HIGH | Task 5a | 유지 (반환형 HIGH-2 v4 dict) |
| HIGH-4 v3 (extract pre-step) | HIGH | Task 5a + 5b | 유지 |
| MEDIUM-1 v3 (AST 스캔 범위) | MED | Task 6 | 유지 + LOW-2 v4 확장 |
| MEDIUM-3 v3 (Task 6 depends-on Task 5a) | MED | Task 6 | 유지 |
| LOW-1 v3 (repo root convention) | LOW | Task 5a + 5b verify | 유지 |
| HIGH-2 v1→v2 (fixture schema) | HIGH | Task 2 | 유지 |
| HIGH-3 v1→v2 (mediapipe 제외) | HIGH | Task 6 | 유지 |
| M1 (synthetic-only) | MED | Task 2b | 유지 |
| M3 (scale 의미) | MED | Task 1 + Task 7 (consumer lint) | 유지 + MEDIUM-3 v4 추가 |
| L1 (lockstep 자동화) | LOW | Task 1 (LOW-1 v4 호칭) | 호칭 갱신 |

### Out of Scope (CONTEXT §1 + §8 — MEDIUM-1 v4 정합)
- Phase 3 자가입력 → Phase 3
- **Phase 6 다운스트림 + Firestore 저장 + AnalysisDoc 박제 → Phase 6** (MEDIUM-1 v4 — v3 4-way lockstep revert)
- 좌우 비대칭 활용 → Phase 7 (v1 warning 만)
- 다각도 입력 → Phase 4
- SMPL-X 본격 도입 → 영구 R&D
- mediapipe quarantine → 별도 phase

### Gaps Found (v4 박제)

각 v4 신규 concern → v4 task 매핑은 위 v3→v4 Replan Changelog + 본 REVIEWS 행에 명시. **모든 v4 concern (2 HIGH + 3 MEDIUM + 2 LOW) 이 task 에 매핑됨** — silent omission 0.

**HIGH-4 v1→v2 + HIGH-2 v4 게이트 박제 (유지)**: ROADMAP success criterion 4 실 closure 는 **Task 5b 가 user_setup 게이트** — executor 자동화 불가. Task 5b 는 HIGH-2 v4 박제로 execution-only (설계/구현 결정 0). belle Pod 박제 스크립트 4단계 (extract → loader → compare → commit) 완료 시까지 ROADMAP §4 PASS 표시 X.

**MEDIUM-2 v4 deferral 박제 (신규)**: ROADMAP success criterion 1 의 acceptance text 가 "v1 acceptance: synthetic + RTMW adapter-path validation; real RTMW network output coverage 는 v1.5 sweep 실행으로 deferred" 로 갱신 — Task 6 atomic commit.

**MEDIUM-1 v4 revert (신규)**: Firestore 저장 + AnalysisDoc 박제 + docs/contract.md AnalysisDoc 박제 모두 Phase 2 scope 외 — Phase 6 plan 책임. v3 의 4-way lockstep 폐기, v2 의 3-way lockstep (BodyNormalizationProfile §7 만) 복귀. CONTEXT.md §1 + §8 정합.

이외 GOAL/REQ/RESEARCH/CONTEXT 모든 항목 task 매핑됨.
</source_audit>

<output>
Phase 2 완료 시 산출:

1. `backend/shared/python/sunity_shared/analysis/body_normalization_measurer.py` (HIGH-2 v3 y-down)
2. `backend/tests/fixtures/body_normalization/_factory.py` (HIGH-2 v1→v2)
3. `backend/tests/fixtures/body_normalization/_generate.py` + 5 fixture JSON (HIGH-2 v3 adapter)
4. `backend/tests/fixtures/body_normalization/test_fixtures_loadable.py`
5. `backend/tests/test_body_normalization_measurer.py` (11 case)
6. `backend/tests/test_pipeline_body_profile_injection.py` (9 — HIGH-1 v4 race-safe + concurrency + stale + B8)
7. `backend/tests/test_research_import_isolation.py` (5 — MEDIUM-1 v3 + LOW-2 v4)
8. `backend/tests/test_estimated_height_scale_consumer_semantics.py` (MEDIUM-3 v4 — vacuous PASS)
9. `backend/research/evaluations/compare_body_profile.py` (HIGH-2 v4 — `load_nlf_smplx_profiles` `dict[str, BodyNormalizationProfile]`)
10. `backend/research/evaluations/extract_rtmw_body_profile_keypoints.py` (HIGH-4 v3)
11. **신규** `backend/research/evaluations/nlf_smplx_body_profile_loader.py` (HIGH-2 v4 — NLF→SMPL-X β fit + β→Profile math + CLI)
12. `backend/tests/test_compare_body_profile_smoke.py` (10)
13. `backend/research/evaluations/reports/sweep_rtmw_<date>/keypoints/<video>.json` × 5 (Step (a))
14. `backend/research/evaluations/reports/nlf_smplx_<date>/<video>.json` × 5 (Step (b), HIGH-2 v4)
15. `backend/research/evaluations/reports/body_profile_gap_<date>.{json,md}` (Step (c))
16. 수정: `docs/contract.md` §7 + `app/src/types/analysis.ts` BodyNormalizationProfile interface + `body_normalization.py` (Task 1 3-way — **AnalysisDoc 변경 X**, MEDIUM-1 v4)
17. 수정: `backend/functions/pipeline/app.py` `_RTMWNlfCompat.estimate_with_profile` + `_angles_and_body_profile_from_video` (HIGH-1 v4 race-safe, B8 박제, 사이드카 폐기)
18. **변경 X (MEDIUM-1 v4)**: `firestore_admin.py` / `analysis.ts` AnalysisDoc / `docs/contract.md` AnalysisDoc (Phase 6 책임)
19. 수정: `.planning/REQUIREMENTS.md` BODY-01 "MediaPipe" → "RTMW"
20. 수정: `.planning/ROADMAP.md` Phase 2 success criterion 1 v1.5 deferral (MEDIUM-2 v4)

완료 시 `.planning/phases/02-bodynormalizationprofile-rtmw-segment/02-01-SUMMARY.md` 생성.

다음 Phase: Phase 3 (자가입력 BodyProfileInput). Phase 6 (체형 정규화 비교) 가 본 phase 의 `_angles_and_body_profile_from_video` helper + Firestore 저장 + AnalysisDoc 박제 wire-up 책임.
</output>

---

## Total Atomic Commits 요약 (v4 — HIGH-1 race-safe + HIGH-2 loader + MEDIUM-1 revert + MEDIUM-3 lint + LOW-2 AST 확장)

| # | Task | Commit Template | Files Changed | Type | Concerns fixed |
|---|------|----------------|----------------|------|---|
| 1 | contract.md §7 warnings 5종 + M3 + L1 string/regex (LOW-1 v4) | `feat(02): lock warnings enum 5-way + scale semantics for BodyNormalizationProfile (3-way lockstep + string/regex gate, LOW-1 v4)` | 4 files | feat | HIGH-2 v1→v2 / M3 / L1 / LOW-1 v4 |
| 2 | PoseFrame factory + Synthetic fixture (RTMW adapter) + smoke + RTMW sanity skip | `test(02): add PoseFrame factory helper + RTMW-adapter-derived synthetic fixtures (5 cases) + y-down convention smoke (HIGH-2 v3 schema + convention, M1 fallback)` | 9 files | test | HIGH-2 v1→v2 + v3 / M1 |
| 3 | body_normalization_measurer.py + 단위 테스트 | `feat(02): add measure_body_profile RTMW segment measurer with MAD smoothing + image-y-down PoleAxis inversion (BODY-01, HIGH-2 v3 fix)` | 2 files | feat | HIGH-2 v3 |
| 4 | Pipeline 통합 — race-safe local-return helper + estimate_with_profile (사이드카 폐기, B8 박제, Firestore revert) | `feat(02): add race-safe _angles_and_body_profile_from_video helper + _RTMWNlfCompat.estimate_with_profile (HIGH-1 v4 — sidecar dropped, B8 lock preserved, Firestore deferred to Phase 6 per MEDIUM-1 v4)` | 1 file (pipeline/app.py) + 1 test file | feat | **HIGH-1 v4 + MEDIUM-1 v4** |
| 5a | R&D compare + extract + nlf_smplx_body_profile_loader + smoke | `chore(02): scaffold R&D compare_body_profile + extract_rtmw_body_profile_keypoints + nlf_smplx_body_profile_loader with beta_to_body_profile math (HIGH-2 v4 + HIGH-3 v3 + HIGH-4 v3 + LOW-1 v3)` | 4 files | chore | HIGH-2 v4 + HIGH-3 v3 + HIGH-4 v3 + LOW-1 v3 |
| 6 | AST isolation 확장 (3 디렉터리 + importlib + `__import__` + spec_from_file_location) + REQUIREMENTS BODY-01 RTMW + ROADMAP §1 v1.5 deferral | `chore(02): expand AST R&D import isolation (functions+runpod_inference+importlib+__import__+spec_from_file_location, MEDIUM-1 v3 + LOW-2 v4) + align BODY-01 to RTMW + ROADMAP §1 v1.5 deferral (MEDIUM-2 v4)` | 3 files | chore | MEDIUM-1 v3 + MEDIUM-3 v3 + LOW-2 v4 + MEDIUM-2 v4 + HIGH-3 v1→v2 |
| 7 | estimatedHeightScale consumer semantic guard lint (MEDIUM-3 v4) | `test(02): add estimatedHeightScale consumer semantic guard lint (MEDIUM-3 v4 — vacuous PASS now, load-bearing in Phase 6+)` | 1 file | test | MEDIUM-3 v4 |
| 5b | belle Pod 박제 스크립트 4단계 실행 | `data(02): belle Pod 5-video RTMW + NLF→SMPL-X β + gap report (HIGH-2 v4 + HIGH-4 v3 ROADMAP §4 closure)` | 12 files | data | **HIGH-2 v4 + HIGH-4 v3 게이트** |

**총 8개 atomic commit** (Task 5 분할 + Task 7 신규 = +1 from v3).

**의존 chain (MEDIUM-3 v3 + MEDIUM-3 v4)**:
```
Task 1 + Task 2 (병렬, Wave 1)
  → Task 3 (Wave 2)
    → Task 4 (Wave 3, race-safe helper) + Task 5a (Wave 3, R&D scaffold + loader 본체) + Task 7 (Wave 3, consumer semantic lint — Task 1 만 의존) [3 병렬]
      → Task 6 (Wave 4, AST + REQUIREMENTS + ROADMAP — Task 5a 만 의존)  ∥  Task 5b (Wave 4, belle Pod user_setup blocking)
```

**Task 6 / Task 7 가 Task 5b 무관**: belle Pod 게이트가 며칠 걸려도 product-safe cleanup landing 가능.

**autonomous: false** 박제 (Task 5b checkpoint).
