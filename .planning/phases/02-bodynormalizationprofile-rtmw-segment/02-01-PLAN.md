---
phase: 02-bodynormalizationprofile-rtmw-segment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/contract.md
  - app/src/types/analysis.ts
  - backend/shared/python/sunity_shared/analysis/body_normalization.py
  - backend/shared/python/sunity_shared/analysis/body_normalization_measurer.py
  - backend/tests/fixtures/body_normalization/__init__.py
  - backend/tests/fixtures/body_normalization/_factory.py
  - backend/tests/fixtures/body_normalization/_generate.py
  - backend/tests/fixtures/body_normalization/normal_pose_pose_frames.json
  - backend/tests/fixtures/body_normalization/inverted_pose_pose_frames.json
  - backend/tests/fixtures/body_normalization/occluded_leg_pose_frames.json
  - backend/tests/fixtures/body_normalization/short_clip_pose_frames.json
  - backend/tests/fixtures/body_normalization/asymmetric_pose_frames.json
  - backend/tests/fixtures/body_normalization/test_fixtures_loadable.py
  - backend/tests/test_body_normalization_measurer.py
  - backend/tests/test_pipeline_body_profile_injection.py
  - backend/tests/test_research_import_isolation.py
  - backend/tests/test_body_normalization_lockstep.py
  - backend/functions/pipeline/app.py
  - backend/shared/python/sunity_shared/firestore_admin.py
  - backend/research/evaluations/compare_body_profile.py
  - backend/research/evaluations/extract_rtmw_body_profile_keypoints.py
  - .planning/REQUIREMENTS.md
autonomous: false
requirements:
  - BODY-01
user_setup:
  - service: runpod-gpu-pod
    why: "ROADMAP success criterion 4 (R&D NLF→SMPL-X β BodyNormalizationProfile 갭 보고서) 충족은 belle 사내 RunPod GPU pod 에서 (a) RTMW 5영상 keypoint dump 추출 + (b) NLF/SMPL-X 실데이터 실행 둘 다 필요. Lambda/scaffold 단독으로 closure 불가. v3 박제 (HIGH-4): pre-step 으로 RTMW keypoint dump 생성 단계 추가."
    env_vars:
      - name: RUNPOD_AUTH_TOKEN
        source: "belle pod env (이미 박제)"
      - name: RTMW_ONNX_PATH
        source: "belle pod env (이미 박제 — Pod 의 rtmw end2end.onnx 절대 경로)"
      - name: YOLOX_ONNX_PATH
        source: "belle pod env (이미 박제 — rtmlib detector 의존)"
      - name: SMPLX_MODEL_PATH
        source: "belle 사내 NLF→SMPL-X R&D weights (PS:License 1.0 비상업)"
    dashboard_config:
      - task: "Step (a) — Pod 에서 5영상 (ref-foxtop / ref-foxtop-split / ref-invert / ref-sideway-spin / ref-climb) 으로 extract_rtmw_body_profile_keypoints.py 실행 → reports/sweep_rtmw_<date>/keypoints/<video>.json (Task 5a 산출 helper). Step (b) — NLF→SMPL-X β path 구현 + compare_body_profile.py 실행 → reports/body_profile_gap_<date>.json. Step (c) — git commit."
        location: "belle RunPod pod 1ablelgbtrzcgb (현 활성)"

must_haves:
  truths:
    - "RTMW PoseFrame[]을 측정기에 넘기면 시간 평균·MAD 스무딩된 segment 길이가 산출된다"
    - "BodyNormalizationProfile이 estimatedHeightScale·armScale·legScale·torsoScale·shoulderHipRatio·confidence·warnings 7필드 모두 채워서 반환된다"
    - "낮은 confidence/occlusion/insufficient frames/asymmetric/inverted 5종 warning이 정확한 조건에서 발화된다"
    - "B8 fix 박제 (_angles_from_video / _angles_and_video_path_from_video 시그너처 무변경) 가 보존되며 body_profile 은 _RTMWNlfCompat 인스턴스 attribute 사이드카로 전파된다 (per HIGH-1 v3 — Option D 사이드카, B8 박제 위반 0)"
    - "pose_too_inverted 판정은 RTMW 이미지 y-down 픽셀 좌표 + 기본 pole_axis=(0,1,0) convention 에 맞게 부호 정합되며 실제 RTMW adapter (convert_rtmw_keypoints_to_coco17_and_pole_ext) 경유 fixture 로 검증된다 (per HIGH-2 v3)"
    - "Task 5a smoke 테스트는 graceful-empty 패턴을 검증하므로 Task 5b 실 구현 landing 후에도 회귀 없이 통과한다 (per HIGH-3 v3 — NotImplementedError 박제 폐기)"
    - "Task 5b 의 belle Pod 실행 전 RTMW keypoint dump 추출 단계 (extract_rtmw_body_profile_keypoints.py) 가 명시되며 sweep_rtmw_<date>/keypoints/<video>.json 산출 경로가 user_setup dashboard_config 와 Task 5a 헬퍼 docstring 에 박제된다 (per HIGH-4 v3)"
    - "AST import isolation 은 sunity_shared 뿐 아니라 backend/functions 와 backend/runpod_inference 까지 스캔하며 importlib.import_module 동적 import 도 차단한다 (per MEDIUM-1 v3)"
    - "Firestore bodyNormalizationProfile 필드는 docs/contract.md AnalysisDoc 섹션 / app/src/types/analysis.ts / backend/shared/python/sunity_shared/firestore_admin.py 3-way lockstep 으로 동일 atomic commit 에 박제된다 (per MEDIUM-2 v3)"
    - "5종 fixture JSON이 PoseFrame dataclass 시그너처 (timestamp_ms / Keypoint3D.uncertainty_proxy / PoleAxis 4-field axis_vector+confidence_level+source+frame_index) 와 정확히 일치하며 test factory helper로 load 가능하다 (per HIGH-2 v1→v2)"
    - "AST import isolation은 backend.research / research / nlf / smplx / smpl_x 만 차단하며 mediapipe는 본 phase scope 외로 명시 제외된다 (per v1→v2 HIGH-3 — TODO 마커 박제)"
    - "ROADMAP success criterion 4 충족은 belle Pod 실행 산출 (`reports/body_profile_gap_<date>.json`) 체크인까지 보류되며 scaffold만으로 충족 표시하지 않는다 (per v1→v2 HIGH-4 — user_setup 게이트)"
    - "estimatedHeightScale은 절대 키가 아닌 torso-relative proportion heuristic 임이 docs/contract.md / app/src/types/analysis.ts / backend/.../body_normalization.py 3 파일에 동시 명시된다 (per v1→v2 M3)"
    - "lockstep 테스트가 5종 warning enum 문자열을 3 파일에서 AST/regex parse 한 결과로 자동 검증한다 (per v1→v2 L1)"
    - "Task 5b R&D 보고서 (`reports/body_profile_gap_<date>.json`) 체크인 또는 belle 명시 v1.5 defer 박제까지 ROADMAP §4 closure 표시 X"
    - "Task 6 (AST isolation + REQUIREMENTS RTMW 정합) 는 Task 5a 만 의존하며 Task 5b human gate 와 평행 진행 가능하다 (per MEDIUM-3 v3)"
    - "`python -m backend.research.evaluations.compare_body_profile` / `python -m backend.research.evaluations.extract_rtmw_body_profile_keypoints` 는 모두 repo root (`/workspace/SunityMotion`) 에서 실행되며 plan 의 모든 verify 블록과 commit 메시지가 이 convention 을 박제한다 (per LOW-1 v3)"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/body_normalization_measurer.py"
      provides: "measure_body_profile(pose_frames) -> BodyNormalizationProfile 순수 함수 (M2 PoleAxis-relative inversion 박제)"
      contains: "measure_body_profile"
    - path: "backend/tests/fixtures/body_normalization/_factory.py"
      provides: "pose_frame_from_dict(raw: dict) -> PoseFrame test helper — PoseFrame.from_dict 부재 우회 (HIGH-2)"
      contains: "pose_frame_from_dict"
    - path: "backend/tests/fixtures/body_normalization/_generate.py"
      provides: "seed=42 멱등 fixture 생성기 + 5종 JSON (실제 dataclass 시그너처 정합). HIGH-2 v3: inverted fixture 는 convert_rtmw_keypoints_to_coco17_and_pole_ext 어댑터 경유 산출"
      contains: "RandomState(42)"
    - path: "backend/tests/fixtures/body_normalization/test_fixtures_loadable.py"
      provides: "5 fixture × pose_frame_from_dict() smoke load + RTMW y-down convention guard (HIGH-2 v3)"
    - path: "backend/tests/test_body_normalization_measurer.py"
      provides: "5 fixture × profile assertion 단위 테스트 + y-down PoleAxis-relative inversion 검증 (HIGH-2 v3 + v1→v2 M2)"
    - path: "backend/tests/test_pipeline_body_profile_injection.py"
      provides: "_RTMWNlfCompat.last_body_profile 사이드카 + B8 박제 보존 회귀 + Phase 6 boundary Firestore payload 검증 (HIGH-1 v3 sidecar)"
    - path: "backend/tests/test_research_import_isolation.py"
      provides: "AST 스캔으로 sunity_shared + backend/functions + backend/runpod_inference → backend.research/nlf/smplx 차단 (mediapipe 제외, importlib.import_module 동적 import 도 차단 — MEDIUM-1 v3)"
    - path: "backend/research/evaluations/compare_body_profile.py"
      provides: "R&D NLF→SMPL-X β vs RTMW segment gap 보고서 CLI (scaffold + smoke 가능, ROADMAP §4 실 보고서는 belle Pod 실행 필요 — HIGH-4)"
    - path: "backend/research/evaluations/extract_rtmw_body_profile_keypoints.py"
      provides: "belle Pod 에서 5영상 → RTMW keypoint dump (`reports/sweep_rtmw_<date>/keypoints/<video>.json`) 산출 helper — Task 5b 실행 pre-step (HIGH-4 v3)"
  key_links:
    - from: "backend/functions/pipeline/app.py::_RTMWNlfCompat.estimate"
      to: "sunity_shared.analysis.body_normalization_measurer.measure_body_profile"
      via: "video-level 1회 호출 + self.last_body_profile 사이드카 저장 (per HIGH-1 v3 — B8 박제 보존)"
      pattern: "self\\.last_body_profile\\s*=\\s*measure_body_profile"
    - from: "backend/functions/pipeline/app.py::_process"
      to: "Firestore via firestore_admin.complete_analysis(body_normalization_profile=_POSE_ESTIMATOR.last_body_profile)"
      via: "사이드카 read + complete_analysis kw — Phase 6 consumer 경계 (HIGH-1 v3)"
      pattern: "complete_analysis\\(.*body_normalization_profile|complete_analysis\\(.*bodyNormalizationProfile"
    - from: "docs/contract.md §3 AnalysisDoc + §7 warnings"
      to: "app/src/types/analysis.ts BodyNormalizationProfile.warnings + AnalysisDoc.bodyNormalizationProfile + body_normalization.py docstring + firestore_admin.complete_analysis"
      via: "3-way → 4-way lockstep (single atomic commit) + lockstep test AST 검증 (MEDIUM-2 v3 + L1)"
      pattern: "low_keypoint_confidence|occluded_endpoint|insufficient_frames|asymmetric_landmark_count|pose_too_inverted|bodyNormalizationProfile"
    - from: "backend/tests/fixtures/body_normalization/*.json"
      to: "backend/tests/fixtures/body_normalization/_factory.py::pose_frame_from_dict"
      via: "test factory helper (PoseFrame.from_dict 부재 우회 — HIGH-2 v1→v2)"
      pattern: "pose_frame_from_dict"
    - from: "backend/research/evaluations/extract_rtmw_body_profile_keypoints.py"
      to: "backend/research/evaluations/compare_body_profile.py --rtmw-keypoints-dir"
      via: "keypoint dump → compare 입력 (HIGH-4 v3 — pre-step)"
      pattern: "sweep_rtmw_.*?/keypoints"
---

## v1 → v2 Replan Changelog (preserved)

본 plan 은 `02-REVIEWS.md` (Codex 1차 리뷰) 의 4 HIGH + 3 MEDIUM + 1 LOW concern 을 fix 한 v2 를 기반으로 한다. v3 에서 일부 v2 결정 (HIGH-1 mechanism / HIGH-2 fixture convention / HIGH-3 5a smoke / HIGH-4 RTMW input) 이 재조정되었지만, **v1 → v2 의 다음 fix 는 그대로 보존된다**:

| Concern | Severity | v1 task | v2 fix (v3 에서 유지) | Evidence (orchestrator verified) |
|---|---|---|---|---|
| HIGH-2: Fixture plan 이 존재하지 않는 PoseFrame API 와 잘못된 필드 (`timestamp_seconds` / `Keypoint3D.uncertainty_proxy` 누락 / `PoleAxis.origin` 환상) 참조 | HIGH | v1 Task 2 (fixture) | (a) `PoseFrame.from_dict` 가 코드베이스에 **없음** 확인 → test factory helper `pose_frame_from_dict()` 신설. (b) fixture JSON 스키마: `timestamp_ms: int`, `Keypoint3D.uncertainty_proxy: float = 1 - confidence`, `PoleAxis` 4 필드 (`axis_vector`/`confidence_level`/`source`/`frame_index`), `origin` 박멸. (c) smoke load 테스트 신설 | `backend/shared/python/sunity_shared/analysis/pose_frame.py:228-246` PoseFrame, `:103-116` Keypoint3D, `:167-200` PoleAxis 4 필드 |
| HIGH-3: Import-isolation rule 이 legacy mediapipe code 와 충돌 | HIGH | v1 Task 6 (AST 차단) | AST scan 차단 대상을 narrow: `backend.research` / `research` / `nlf` / `smplx` / `smpl_x` 만. `mediapipe` 차단 제외 + TODO 마커 박제 | `backend/shared/python/sunity_shared/analysis/pose_engines/__init__.py` lazy mediapipe + `pose_engines/mediapipe_engine.py:74-76, 197` live import |
| HIGH-4 (v2 정의): R&D `NotImplementedError` stub 으로 ROADMAP §4 충족 표시 — scaffold ≠ gap report | HIGH | v1 Task 5 (R&D harness) | Task 5 분할: Task 5a (scaffold, executor 가능) + Task 5b (belle Pod 실행, user_setup gate). autonomous=false. ROADMAP §4 closure 는 5b 완료 시에만 | ROADMAP.md success criterion #4 — scaffold 만으론 closure X |
| M1: Synthetic-only tests 가 RTMW 좌표/naming drift 탐지 불가 | MED | v1 Task 2 | 5 synthetic fixture 유지 + Task 2b RTMW sanity skip (Pod 환경에서만 실행) | `backend/research/evaluations/reports/sweep_rtmw_20260603_1409/` 에 keypoint dump 없음 |
| M3: armScale/legScale/estimatedHeightScale semantics 미명세 | MED | v1 Task 1 (contract) | 3-way 박제: "torso-relative proportion heuristic — 절대 키가 아니라 torso 길이 대비 사지 비율. Phase-6 coarse normalizer hint 로만 사용". `estimatedHeightScale = (arm_scale + leg_scale + 1.0) / 3` 산식 박제 | M3 |
| L1: Lockstep warning enum 만 grep 검증 → 자동화 부족 | LOW | v1 Task 1 verify | `test_body_normalization_lockstep.py` 확장 — 5 warning enum 문자열을 3 파일에서 자동 검증 | Phase 1 산출 lockstep 파일에 warnings enum 검증 없음 |

## v2 → v3 Replan Changelog (THIS revision)

Codex 2차 리뷰 (02-REVIEWS.md, reviewed_at: 2026-06-07T11:45:10Z) 가 v2 의 4 HIGH + 3 MEDIUM + 1 LOW 신규 concern 을 보고. orchestrator 가 codebase grep 으로 모두 검증 완료. v3 가 다음과 같이 수정한다:

| Concern (v2 → v3) | Severity | Affected v2 task | v3 fix | Evidence (orchestrator verified) |
|---|---|---|---|---|
| **HIGH-1 (v3)**: v2 의 tuple-return propagation 이 B8 fix 박제 (`TestB8FixSignature` at `backend/tests/test_pipeline_recognizer_switch.py:225`) 의 `_angles_from_video(bucket, key) -> np.ndarray` + `_angles_and_video_path_from_video(bucket, key) -> tuple[np.ndarray, str]` 시그너처 lock 을 위반. v2 `pytest tests/ -x` 즉시 실패. | HIGH | Task 4 (전체 재작성) | **Mechanism 전환: Option A (tuple return) → Option D (사이드카 instance attribute)**. `_RTMWNlfCompat.estimate()` 는 `-> np.ndarray` 반환 유지 + `self.last_body_profile: BodyNormalizationProfile \| None` instance attribute 박제. `_angles_from_video` / `_angles_and_video_path_from_video` 시그너처 무변경 → B8 박제 위반 0. `_process` 가 `_POSE_ESTIMATOR.last_body_profile` 직접 read → `firestore_admin.complete_analysis(body_normalization_profile=...)` 호출. 신규 회귀 테스트: (a) `estimate()` 호출 후 `last_body_profile` populated, (b) sequential `estimate()` 두 번 → leakage 0, (c) `TestB8FixSignature` 두 테스트 모두 PASS. | `backend/tests/test_pipeline_recognizer_switch.py:225-238` 의 `assert sig.return_annotation in (np.ndarray, "np.ndarray")` lock. `backend/functions/pipeline/app.py:255-268` `_angles_from_video` 시그너처. `:271-305` `_angles_and_video_path_from_video` 시그너처. |
| **HIGH-2 (v3)**: v2 의 `_is_inverted_frame` 부호 정의가 RTMW image y-down pixel coord convention 에서 정상 upright 자세를 inverted 로 오판. RTMW adapter `convert_rtmw_keypoints_to_coco17_and_pole_ext` 는 image pixel 좌표를 그대로 보존 (`backend/shared/python/sunity_shared/analysis/adapters/rtmw_133_to_coco17.py:184-189`). image y-down 에서는 upright frame 에 대해 `mid_shoulder.y < mid_hip.y` → `(mid_shoulder - mid_hip).y < 0` → 기존 정의 `dot(shoulder - hip, (0,1,0)) < 0 ⇒ inverted` 가 normal upright 를 inverted 로 잡음. | HIGH | Task 2 (fixture), Task 3 (measurer) | **Convention 명시 박제**: image y-down + 기본 `pole_axis = (0, 1, 0)` (image y-down 의 down direction). Upright 정의: `dot(mid_hip - mid_shoulder, pole_axis.axis_vector) > 0` (mid_hip - mid_shoulder 는 image 에서 y 양 → pole_axis 와 같은 방향). Inverted 정의: `dot(mid_hip - mid_shoulder, pole_axis.axis_vector) < 0`. 함수 docstring 에 "RTMW image y-down pixel coord 박제 — `mid_hip - mid_shoulder` 가 default pole_axis 와 같은 방향이면 upright" 명시. **Inverted fixture 는 `convert_rtmw_keypoints_to_coco17_and_pole_ext` 어댑터를 직접 호출하여 산출** — 손으로 쓴 y-up 좌표 X. Convention 검증 테스트 신설: (a) RTMW 어댑터 경유 upright 합성 keypoint → `pose_too_inverted` warning 미발화, (b) RTMW 어댑터 경유 inverted (mid_hip.y < mid_shoulder.y) 합성 keypoint → warning 발화. | `backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/rtmw_engine.py` (RTMW 가 image pixel 좌표 출력). `backend/.../adapters/rtmw_133_to_coco17.py:184-189` adapter pass-through. `backend/functions/pipeline/app.py:215-220` `_default_pole = (0,1,0)`. |
| **HIGH-3 (v3)**: v2 Task 5a 가 `load_nlf_smplx_keypoints()` → `NotImplementedError` raise 를 박제하고 `test_load_nlf_smplx_raises_stub` 가 그것을 검증. Task 5b 가 NotImplementedError 를 실 구현으로 교체하면 5a 의 smoke 테스트가 즉시 fail. | HIGH | Task 5a (smoke), Task 5b (Pod 실행) | **Smoke 패턴 전환: NotImplementedError-raises → graceful-empty**. Task 5a 의 `load_nlf_smplx_keypoints(path)` 는 `path.exists() and any(path.iterdir())` 미충족 시 `[]` 반환 (raise X). 5a smoke 테스트 `test_load_nlf_smplx_returns_empty_for_missing_dir` 가 빈/비존재 디렉터리 → 빈 list 반환 검증. Task 5b 가 실 구현 추가해도 (입력 디렉터리에 NLF→SMPL-X 산출 파일 박제 후 list 반환) 5a 의 graceful-empty 테스트는 비존재 디렉터리에 대해서는 그대로 통과. `NotImplementedError` 박제 폐기. | v2 plan Task 5a action 의 5번째 bullet (`test_load_nlf_smplx_raises_stub`). |
| **HIGH-4 (v3)**: v2 Task 5b 의 명령이 `sweep_rtmw_<date>/keypoints/` 디렉터리 존재를 전제로 함. orchestrator 검증: `backend/research/evaluations/reports/sweep_rtmw_20260603_1409/` 에 `report.json` + `report.md` 만 존재, keypoint dump 없음. belle 가 NLF→SMPL-X β path 구현 전에 RTMW extraction 도 따로 실행해야 5b 가 가능. | HIGH | Task 5a (helper 추가), Task 5b (pre-step 박제) | **Pre-step helper 신설**: `backend/research/evaluations/extract_rtmw_body_profile_keypoints.py` (Task 5a 가 scaffold). 5영상 → RTMWPoseEngine.estimate → `reports/sweep_rtmw_<date>/keypoints/<video>.json` (per-video PoseFrame list serialized). 기존 `compare_rtmw_vs_ipsf.py:717-733` 의 `_load_pose_engine` 패턴 재사용. Task 5b 의 dashboard_config 와 how-to-verify 가 명시: **Step (a)** extract_rtmw_body_profile_keypoints.py 실행 → keypoint dump 생성 (필수 pre-step), **Step (b)** NLF→SMPL-X β path 실 구현 + compare_body_profile.py 실행, **Step (c)** 양쪽 산출 git commit. | `ls backend/research/evaluations/reports/sweep_rtmw_20260603_1409/` → `report.json` + `report.md` 만, keypoint dump 없음. `backend/research/evaluations/compare_rtmw_vs_ipsf.py:717-733` `_load_pose_engine("rtmw")` 패턴 존재. |
| **MEDIUM-1 (v3)**: v2 AST 스캔이 `backend/shared/python/sunity_shared/` 만 검사. Deployable product code 에는 `backend/functions/` + `backend/runpod_inference/` 도 포함. `pipeline/app.py` 가 `backend.research` import 하면 legal/license leak 차단 X. 또한 `importlib.import_module("smplx")` 같은 동적 import 도 v2 가 catch 안 함. | MED | Task 6 (AST 스캔 확장) | **스캔 범위 확장**: `backend/shared/python/sunity_shared/` + `backend/functions/` + `backend/runpod_inference/` 3 디렉터리 scan. 제외: `backend/tests/` + `backend/research/` (이 둘은 R&D 모듈 import 허용). 추가 검출: `ast.Call` 노드에서 `func.attr == 'import_module'` (importlib) 의 string literal 인자가 차단 prefix 와 매치하면 위반. 테스트 함수 3 → 4개로 확장: (1) sunity_shared isolation, (2) backend/functions isolation, (3) backend/runpod_inference isolation, (4) importlib dynamic import 차단. | `pipeline/app.py:454-461` `import dataclasses` / `from sunity_shared...` (운영 path). `backend/runpod_inference/server.py` 도 deployable product code. |
| **MEDIUM-2 (v3)**: v2 Task 4 가 `app/src/types/analysis.ts` + `backend/shared/python/sunity_shared/firestore_admin.py` 만 수정. `docs/contract.md` 의 `AnalysisDoc` Firestore 섹션 (line 86~97) 이 unchanged → 3-way contract drift. | MED | Task 4 (lockstep 확장) | **3-way → 4-way lockstep**: Task 4 의 atomic commit 에 `docs/contract.md` 의 `AnalysisDoc` 섹션 (line 86~97) 갱신 추가. `bodyNormalizationProfile? BodyNormalizationProfile` optional 필드를 AnalysisDoc 정의에 박제 + "status='done' 일 때 채워짐 — Phase 6 가 읽는 곳" 코멘트. Task 1 의 5-way warning enum lockstep 패턴 정합. acceptance_criteria 와 commit-template 가 4-way commit (`contract.md` + `analysis.ts` + `firestore_admin.py` + `pipeline/app.py`) 박제. | `docs/contract.md:82-97` AnalysisDoc 정의 — `bodyNormalizationProfile` 미박제. |
| **MEDIUM-3 (v3)**: v2 dependency chain `Task 5a → Task 5b → Task 6` 가 Task 6 (AST isolation + REQUIREMENTS RTMW 정합 — 안전한 product cleanup) 을 belle Pod 의 manual gate (Task 5b) 뒤로 묶음. 며칠 걸리는 Pod 게이트가 product-safe cleanup landing 을 막음. | MED | Task 6 depends-on, Total Atomic Commits chain | **재정렬**: Task 6 의 depends-on 을 `Task 5a` 로 한정 (Task 5b 무관). Task 5b 와 Task 6 가 평행 (Task 5b 는 user gate, Task 6 는 executor autonomous). Total Atomic Commits 의 dependency chain note: `Task 1 + Task 2 (병렬) → Task 3 → Task 4 + Task 5a (병렬) → Task 6 (5a 만 의존, 5b 평행) ∥ Task 5b (user_setup blocking, ROADMAP §4 closure 게이트)`. | v2 plan 의 "의존 chain" 끝부분이 `Task 5b (belle Pod user_setup, blocking) → Task 6` 으로 박제. |
| **LOW-1 (v3)**: v2 verify 블록이 `cd backend && python -m backend.research.evaluations.compare_body_profile --help` 같은 형식 — `cd backend` 후 `backend.research...` 는 module resolution 실패. 기존 R&D convention (`backend/research/evaluations/compare_engines.py:30`) 은 repo root 에서 `python -m backend.research.evaluations.X`. | LOW | Task 5a verify, Task 5b dashboard | **Convention 표준화**: 모든 `python -m` invocation 을 repo root (`/workspace/SunityMotion`) 에서 실행. `cd backend` prefix 제거. Task 5a verify, Task 5b dashboard_config + how-to-verify 모두 갱신. pytest 는 `cd backend && pytest tests/...` 박제 유지 (pytest 는 backend dir 기준 conftest 정합). | `backend/research/evaluations/compare_engines.py:28-34` 박제 "실행 경로 (L-2 박제): /workspace/SunityMotion ... python -m backend.research.evaluations.compare_engines". |

v3 plan 의 v2 → v3 변경 summary:
- **Task 1**: 변경 없음 (v2 그대로). MEDIUM-2 의 contract.md 변경은 Task 4 atomic commit 으로 이동.
- **Task 2**: HIGH-2 v3 박제 — inverted fixture 생성 path 가 `convert_rtmw_keypoints_to_coco17_and_pole_ext` 어댑터 경유 명시.
- **Task 3**: HIGH-2 v3 박제 — `_is_inverted_frame` 부호 정의 RTMW image y-down convention 정합. 테스트 명/박제 갱신.
- **Task 4**: HIGH-1 v3 (sidecar) 전면 재작성 + MEDIUM-2 v3 contract.md 추가.
- **Task 5a**: HIGH-3 v3 (graceful-empty smoke) + HIGH-4 v3 (extract helper 신설).
- **Task 5b**: HIGH-4 v3 dashboard_config Step (a)~(c) + LOW-1 v3 repo-root convention.
- **Task 6**: MEDIUM-1 v3 (스캔 범위 확장 + importlib) + MEDIUM-3 v3 (depends-on Task 5a only) + LOW-1 v3.

<objective>
RTMW 133 wholebody 키포인트로부터 신체 segment 길이·비율·confidence·warnings 를 자동 산출하는 `measure_body_profile()` 측정기를 박제하고, `_RTMWNlfCompat` 의 instance attribute 사이드카 (`last_body_profile`) 를 통해 video-level `BodyNormalizationProfile` 이 caller (`_process`) 가 접근 가능하면서도 **B8 fix 시그너처 박제** (`_angles_from_video(bucket, key) -> np.ndarray`, `_angles_and_video_path_from_video(bucket, key) -> tuple[np.ndarray, str]`) 가 위반 0 으로 보존되도록 한다. profile 은 `firestore_admin.complete_analysis(body_normalization_profile=...)` 로 Firestore 에 저장되어 Phase 6 consumer 가 읽는다. R&D 비교군 (NLF→SMPL-X β) harness 는 `backend/research/` 에 격리하고 AST import 차단 테스트 (sunity_shared + backend/functions + backend/runpod_inference + importlib 동적 차단) 로 운영 코드 leak 을 영구 방지한다. ROADMAP success criterion 4 (실 영상 갭 보고서) 는 belle Pod 실행 게이트 (Task 5b, user_setup, **RTMW extraction pre-step 명시**) 로 closure.

Per D-02-01, D-02-02, D-02-03, D-02-04, D-02-05, D-02-06.

Purpose: Phase 6 (체형 정규화 비교) / Phase 7 (차이 분류) / Phase 8·9 (힘 패턴) 가 공유하는 입력 schema 를 채운다. v1 = 측정기 본체 + sidecar propagation 검증, **Firestore 저장은 본 phase 에 포함** (HIGH-1 v2 fix 의 일부 — Phase 6 가 읽을 곳).

Output (v3 갱신):
- `body_normalization_measurer.py` 신설 (순수 numpy, scipy/외부 의존 0) — HIGH-2 v3 y-down convention 박제
- 5 synthetic fixture + `_factory.py` test helper + smoke load + 6 단위 테스트 (HIGH-2 v3 RTMW 어댑터 경유 inverted fixture)
- `_RTMWNlfCompat.last_body_profile` 사이드카 instance attribute + B8 박제 보존 검증 — HIGH-1 v3
- `firestore_admin.complete_analysis(body_normalization_profile=...)` kw + Firestore payload (`bodyNormalizationProfile`)
- 4-way lockstep (MEDIUM-2 v3): `docs/contract.md` AnalysisDoc + `app/src/types/analysis.ts` + `body_normalization.py` docstring + `firestore_admin.py` payload 동일 atomic commit
- `backend/research/evaluations/compare_body_profile.py` R&D 격리 harness (scaffold + smoke — HIGH-3 v3 graceful-empty)
- `backend/research/evaluations/extract_rtmw_body_profile_keypoints.py` Task 5a 산출 helper — HIGH-4 v3 pre-step
- belle Pod 실행 게이트 (Task 5b — user_setup, Step (a) RTMW 추출 + Step (b) NLF→SMPL-X β + Step (c) commit)
- AST import isolation 확장 (sunity_shared + backend/functions + backend/runpod_inference + importlib 동적 차단) — MEDIUM-1 v3
- 확장 lockstep 테스트 (warning enum AST 자동 검증) — L1
- REQUIREMENTS.md BODY-01 "MediaPipe" → "RTMW" 정합 갱신
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-bodynormalizationprofile-rtmw-segment/02-CONTEXT.md
@.planning/phases/02-bodynormalizationprofile-rtmw-segment/02-RESEARCH.md
@.planning/phases/02-bodynormalizationprofile-rtmw-segment/02-REVIEWS.md
@docs/contract.md
@backend/shared/python/sunity_shared/analysis/body_normalization.py
@backend/shared/python/sunity_shared/analysis/pose_frame.py
@backend/shared/python/sunity_shared/analysis/temporal.py
@backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/wholebody_keypoints.py
@backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/rtmw_engine.py
@backend/shared/python/sunity_shared/analysis/adapters/rtmw_133_to_coco17.py
@backend/shared/python/sunity_shared/firestore_admin.py
@backend/functions/pipeline/app.py
@backend/runpod_inference/server.py
@backend/tests/test_pipeline_recognizer_switch.py
@app/src/types/analysis.ts
@backend/tests/test_body_normalization_lockstep.py
@backend/tests/test_body_normalization_profile.py
@backend/research/evaluations/compare_engines.py
@backend/research/evaluations/compare_rtmw_vs_ipsf.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: contract.md §7 warnings enum 5종 + estimatedHeightScale 의미 박제 + lockstep AST 자동 검증 (3-way lockstep, 단일 atomic commit) — v2 그대로</name>
  <read_first>
    backend/shared/python/sunity_shared/analysis/body_normalization.py (현 docstring + warnings 필드 코멘트),
    app/src/types/analysis.ts (lines 364-388 — 현 BodyNormalizationProfile interface + 필드 주석),
    docs/contract.md (§7 — 현 BodyNormalizationProfile 명세, line 342 warnings),
    backend/tests/test_body_normalization_lockstep.py (확장 대상 — 현 5 함수)
  </read_first>
  <action>
    Goal: D-02-02 lockstep 정신 정합 — `warnings` 필드의 허용 enum 5종 + scale 필드 의미 (M3) 를 3 파일에 동시 박제. lockstep 테스트를 AST 자동 검증으로 확장 (L1).

    **v3 주의**: 본 task 의 lockstep 범위는 §7 BodyNormalizationProfile 만. AnalysisDoc 의 `bodyNormalizationProfile?` optional 필드 박제는 Task 4 의 4-way commit (MEDIUM-2 v3) 으로 이동했다. 본 task 는 v2 그대로 유지.

    구현 노트:
    1. **5종 warning enum (per HIGH-2 v1→v2 + REVIEWS.md "Warnings enum")**: `low_keypoint_confidence`, `occluded_endpoint`, `insufficient_frames`, `asymmetric_landmark_count`, `pose_too_inverted`. 3 파일에 정확히 같은 문자열로 박제.

    2. **docs/contract.md §7 갱신 (line 342 근처)**:
       - 현 placeholder `"측정 품질 이슈 (예: 'short_arm_clip', 'occluded_torso'). 기본값 []."` 제거.
       - 5 enum 명시 + 각 의미 1줄. 예: `low_keypoint_confidence — profile.confidence < 0.4`, `occluded_endpoint — 60% 초과 frame 에서 endpoint conf<0.5`, `insufficient_frames — T<30`, `asymmetric_landmark_count — 좌우 valid frame 차이 >30%`, `pose_too_inverted — RTMW image y-down convention 기준 mid_hip - mid_shoulder 의 pole_axis projection<0 in 50%+ frames` (HIGH-2 v3 박제).
       - **M3 박제 추가** — `estimatedHeightScale` / `armScale` / `legScale` 필드 설명에 다음 문구 (정확히 동일하게 3 파일 박제): "torso-relative proportion heuristic — 절대 키가 아니라 torso 길이 대비 사지 비율. Phase 6 coarse normalizer hint 로만 사용." 그리고 `estimatedHeightScale = (armScale + legScale + 1.0) / 3` 산식 명시 + "average torso-relative proportion" 코멘트.

    3. **app/src/types/analysis.ts BodyNormalizationProfile interface (lines 373-388)**:
       - `warnings: string[]` JSDoc 위에 5 enum 한국어/영문 매핑 nested list 추가. "Phase 11 coach_writer 가 한국어 카피로 번역" 노트.
       - `estimatedHeightScale` JSDoc 갱신 — "torso-relative proportion heuristic" 문구 (M3 박제).
       - `armScale` / `legScale` JSDoc 도 동일 문구 박제.
       - TS 타입은 `string[]` 유지 (literal union 은 Phase 6+ 결정).

    4. **backend/shared/python/sunity_shared/analysis/body_normalization.py**:
       - 클래스 docstring 의 `warnings 의미` 블록을 5 enum + 각 의미 명시로 교체.
       - `estimated_height_scale` / `arm_scale` / `leg_scale` 필드 위 코멘트 박제 — "torso-relative proportion heuristic" 문구.
       - `__post_init__` 의 enum membership 검증은 v1 에 추가 X (CONTEXT D-02-03 정합 — 다른 모듈이 새 warning 추가 가능성 열어둠).

    5. **L1 — lockstep AST 자동 검증 확장 (`backend/tests/test_body_normalization_lockstep.py`)**:
       - 기존 5 함수 유지.
       - **신설 함수 `test_warnings_enum_lockstep_across_3_files`**:
         - 5 enum 상수를 module-level frozenset 박제 (`_EXPECTED_WARNINGS`).
         - `docs/contract.md` 텍스트에서 각 enum 문자열이 모두 등장하는지 substring 검사.
         - `app/src/types/analysis.ts` 도 동일.
         - `body_normalization.py` 도 동일.
         - assert 메시지에 어느 파일에서 누락됐는지 명시.
       - **신설 함수 `test_estimated_height_scale_semantics_lockstep`**:
         - "torso-relative proportion heuristic" 문구가 3 파일에 모두 등장 (M3 박제).
         - 누락 파일 명시.
       - regex parse 권장 (substring 검색 — AST 는 비실용적, 박제된 문자열만 검증).
       - 기존 5 함수 회귀 0.

    6. **단일 commit 정합**: TS + Python + contract.md + test 동시 변경 (D-02-02).

    참조: REVIEWS.md v1→v2 HIGH-2 / M3 / L1 + RESEARCH.md "Confidence + Warnings Spec" + "BodyNormalizationProfile Schema (3-way)".

    Implementation deferred to execution — per D-02-02 / D-02-03. No code in this plan.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_body_normalization_lockstep.py -x -v</automated>
    추가: `grep -c "low_keypoint_confidence" docs/contract.md app/src/types/analysis.ts backend/shared/python/sunity_shared/analysis/body_normalization.py | grep -v ':0$' | wc -l` → 3.
    추가 M3: `grep -c "torso-relative proportion heuristic" docs/contract.md app/src/types/analysis.ts backend/shared/python/sunity_shared/analysis/body_normalization.py | grep -v ':0$' | wc -l` → 3.
  </verify>
  <acceptance_criteria>
    - 5 warning enum 문자열이 3 파일 모두에 존재 (lockstep AST 테스트 PASS)
    - "torso-relative proportion heuristic" 문구가 3 파일 모두에 존재 (M3 박제)
    - 기존 `short_arm_clip` / `occluded_torso` placeholder 제거됨
    - `test_warnings_enum_lockstep_across_3_files` + `test_estimated_height_scale_semantics_lockstep` 신설 PASS
    - 기존 5 lockstep 함수 회귀 0
    - 단일 commit 에 docs/contract.md + analysis.ts + body_normalization.py + test 동시 변경
  </acceptance_criteria>
  <done>
    위 acceptance_criteria 모두 충족.
  </done>
  <commit-template>
    feat(02): lock warnings enum 5-way + scale semantics for BodyNormalizationProfile (3-way lockstep + AST gate)
  </commit-template>
  <depends-on>None — Wave 1 진입점</depends-on>
</task>

<task type="auto">
  <name>Task 2: PoseFrame test factory helper + Synthetic fixture 생성기 (RTMW adapter 경유 inverted fixture) + 5종 fixture JSON + smoke load 테스트 (HIGH-2 v1→v2 + HIGH-2 v3)</name>
  <read_first>
    backend/shared/python/sunity_shared/analysis/pose_frame.py (실제 dataclass 시그너처 — PoseFrame / Keypoint3D / PoleAxis 4-field / Landmark3D 모두 확인. PoseFrame.from_dict 부재 박제),
    backend/shared/python/sunity_shared/analysis/body_normalization.py (BodyNormalizationProfile dataclass),
    backend/shared/python/sunity_shared/analysis/skeleton.py (KEYPOINT_NAMES 17 — COCO-17 인덱스 0~16),
    backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/wholebody_keypoints.py (RTMW 133 인덱스),
    backend/shared/python/sunity_shared/analysis/adapters/rtmw_133_to_coco17.py (HIGH-2 v3 박제 — convert_rtmw_keypoints_to_coco17_and_pole_ext 어댑터 경로 + image pixel 좌표 보존)
  </read_first>
  <action>
    Goal (per HIGH-2 v1→v2): `PoseFrame.from_dict` 가 **코드베이스에 없음** 확인됨 (orchestrator verified). 따라서 (a) test factory helper `pose_frame_from_dict(raw: dict) -> PoseFrame` 신설, (b) fixture JSON 스키마를 **실제 dataclass 시그너처** 로 재작성, (c) smoke load 테스트 신설.

    **HIGH-2 v3 박제 추가**: inverted fixture 는 손으로 쓴 y-up 좌표 X — RTMW adapter (`convert_rtmw_keypoints_to_coco17_and_pole_ext`) 를 직접 호출하여 생성해야 RTMW image y-down pixel coord convention 이 fixture 에 반영된다. normal fixture 도 동일 어댑터 경유로 생성하여 두 fixture 의 좌표계가 정합.

    구현 노트:

    1. **`backend/tests/fixtures/body_normalization/_factory.py` 신설 — test helper**:
       - `pose_frame_from_dict(raw: dict) -> PoseFrame` — raw dict → `PoseFrame` 인스턴스화.
       - 처리해야 할 필드 (실제 dataclass 시그너처):
         - `frame_index: int`
         - `timestamp_ms: int` (NOT `timestamp_seconds` — HIGH-2 fix)
         - `keypoints_3d: dict[str, Keypoint3D]` — 각 entry 는 `{x, y, z, confidence, uncertainty_proxy}` 모두 포함 (HIGH-2 fix: `uncertainty_proxy = 1 - confidence` 명시).
         - `keypoints_3d_pole_aligned: dict[str, Keypoint3DAligned]` — `{x, y, z}` 만.
         - `raw_keypoints_133: dict[str, Keypoint3D] | None` — RTMW path 박제, body 17 채우고 117 슬롯은 conf=0.0 + uncertainty_proxy=1.0.
         - `pole_axis: PoleAxis` — **정확히 4 필드**: `axis_vector: tuple[float, float, float]` (norm=1.0 검증 필수), `confidence_level: 'high'|'medium'|'low'`, `source: 'detected'|'vertical_fallback'`, `frame_index: int | None`. **`origin` 필드 없음** (HIGH-2 fix).
         - `reliability: 'high'|'medium'|'low'`.
         - `body_shape: None` (Phase 2 측정기가 채울 예정 — fixture 는 None).
       - 누락 필드는 default factory 사용 (`raw_landmarks_33={}` 등).
       - JSON tuple 은 list 로 deserialize 되므로 `tuple(...)` 변환 박제.
       - **Important**: `pose_frame.py` Production 코드는 수정 X — helper 는 `backend/tests/fixtures/body_normalization/` 안에 격리.

    2. **`backend/tests/fixtures/body_normalization/_generate.py` 신설 — 멱등 생성기 + HIGH-2 v3 RTMW 어댑터 경유**:
       - 헤더: `numpy.random.RandomState(seed=42)` 박제. CLI `--check` 옵션 (재실행 멱등성 검증).
       - 모든 fixture JSON 은 `pose_frame_from_dict` 통과 가능.
       - **HIGH-2 v3 박제 — Coordinate convention**:
         - RTMW 는 **image pixel 좌표 (y 증가 = 아래쪽)** 출력. adapter `convert_rtmw_keypoints_to_coco17_and_pole_ext` 는 좌표를 pass-through.
         - Default `pole_axis = (0.0, 1.0, 0.0)` (= image y-down 의 down 방향).
         - **Upright 자세 정의 박제 (image y-down)**: `mid_shoulder.y < mid_hip.y` (어깨가 image 상단 = y 작음, 엉덩이 가 image 하단 = y 큼). 즉 `(mid_hip.y - mid_shoulder.y) > 0`. 결과: `dot(mid_hip - mid_shoulder, pole_axis) > 0` 이면 upright.
         - **Inverted 자세 정의**: `mid_shoulder.y > mid_hip.y` (어깨가 image 하단). 결과: `dot(mid_hip - mid_shoulder, pole_axis) < 0`.
       - **Fixture 생성 path (HIGH-2 v3 박제)**:
         - normal_pose / occluded_leg / short_clip / asymmetric_pose: image y-down 좌표로 직접 dict 생성 + RTMW adapter `convert_rtmw_keypoints_to_coco17_and_pole_ext` 를 호출하여 PoseFrame 생성 → `dataclasses.asdict` 로 JSON 직렬화. RTMW 133 input 은 body 17 만 의미있는 값 + 117 슬롯 zero confidence padding.
         - inverted_pose: 동일 path + `mid_shoulder.y` 와 `mid_hip.y` (및 관련 어깨/엉덩이 좌우 keypoint y) 를 swap 한 후 어댑터 호출. 즉 inverted fixture 도 adapter 경유 → image y-down convention 강제 반영.
         - 본 path 가 RTMW pixel coord 좌표계 + adapter 행동을 fixture 에 박제 → `_is_inverted_frame` 의 부호 검증이 실제 adapter path 와 정합.
       - PoleAxis default: `axis_vector=(0.0, 1.0, 0.0)`, `confidence_level='medium'`, `source='vertical_fallback'`, `frame_index=None`. **`origin` 필드 박제 금지** (HIGH-2 fix).
       - `Keypoint3D` 생성 시 `uncertainty_proxy = 1.0 - confidence` 박제 (HIGH-2 fix). adapter 가 자동 산출하므로 별도 박제 불필요.
       - 모든 timestamp 는 `timestamp_ms: int` (NOT `timestamp_seconds`).
       - 인체 평균 비율 (image pixel 좌표 기준, torso=200px, upper_arm=120px, forearm=110px, thigh=180px, shank=160px, shoulder=140px, hip=100px → shoulder_hip_ratio≈1.4).

    3. **5 fixture JSON 스키마 (per HIGH-2 v3 정합 — 모두 RTMW adapter 경유)**:
       - **normal_pose** — T=60 frame, 정상 자세 (image y-down 에서 mid_shoulder.y < mid_hip.y). 모든 keypoint conf 0.85~0.95 ±2% gaussian.
       - **inverted_pose** — T=60, mid_shoulder.y > mid_hip.y in 80% frames (HIGH-2 v3 박제 — RTMW image y-down convention 정합). conf 0.6~0.8.
       - **occluded_leg** — T=60, `left_knee`/`right_knee` conf<0.3 in 70% frames. 나머지 정상.
       - **short_clip** — T=20 (MIN_VALID_FRAMES=5 통과하지만 T<30 임계값 미달). 정상 자세.
       - **asymmetric_pose** — T=60, 좌측 keypoint conf>0.9 in 90% frames, 우측 keypoint conf>0.9 in 50% frames 만 (frame 수 차이 40%).
       - JSON 파일 평균 size 200KB 이내.

    4. **`backend/tests/fixtures/body_normalization/test_fixtures_loadable.py` 신설 (smoke)**:
       - `test_all_fixtures_load_via_factory` — 5 fixture JSON 을 `pose_frame_from_dict` 로 load → `PoseFrame` 인스턴스 (raise X). 각 fixture 의 첫 frame `frame_index=0`, `timestamp_ms` 가 `int`.
       - `test_polestaxis_no_origin_field` — 5 fixture 의 PoleAxis JSON 에 `origin` 키 등장 0 (HIGH-2 박제).
       - `test_keypoint3d_has_uncertainty_proxy` — 5 fixture 의 keypoints_3d entry 가 `uncertainty_proxy` 키 보유 (HIGH-2 박제).
       - **신설 `test_normal_pose_is_upright_in_y_down_convention` (HIGH-2 v3 박제)**:
         - normal_pose fixture load → 모든 frame 의 `mid_shoulder = (left_shoulder + right_shoulder) / 2`, `mid_hip = (left_hip + right_hip) / 2`.
         - 80% 이상 frame 에서 `mid_hip.y > mid_shoulder.y` 검증 (image y-down 박제).
       - **신설 `test_inverted_pose_is_inverted_in_y_down_convention` (HIGH-2 v3 박제)**:
         - inverted_pose fixture load → 80% 이상 frame 에서 `mid_hip.y < mid_shoulder.y` (RTMW y-down 에서 인버트).
       - 본 smoke 가 PASS 해야만 Task 3 measurer 테스트가 의미 있음.

    5. **M1 — Task 2b (Real-RTMW sanity check, optional skip)**:
       - `backend/tests/fixtures/body_normalization/test_real_rtmw_sanity.py` 신설.
       - `@pytest.mark.skipif(not _rtmw_available(), reason="RTMW ONNX 미설치 — Pod 환경 외 skip")`.
       - 짧은 embedded video clip (또는 Pod 환경에서만 실행) 로 RTMWPoseEngine 호출 → 첫 frame 의 `keypoints_3d` 가 COCO-17 keypoint 이름 17개 정확히 포함 + `uncertainty_proxy` 범위 [0,1].
       - 통과 X 면 skip — CI 환경에서 안전. Pod 환경에선 실행 (M1 mitigation).

    6. **TODO 마커**: `_generate.py` 상단에 `# TODO(post-pilot): Phase 1 sweep keypoint dump 재추출 후 실 영상 fixture 1종 추가 — REVIEWS.md M1 정합`.

    참조: REVIEWS.md v1→v2 HIGH-2 + M1 + v2→v3 HIGH-2 + RESEARCH.md "Test Fixture Plan" + `backend/.../adapters/rtmw_133_to_coco17.py` adapter path.

    No code in this plan.
  </action>
  <verify>
    <automated>cd backend && python -m tests.fixtures.body_normalization._generate --check && pytest tests/fixtures/body_normalization/test_fixtures_loadable.py -x -v</automated>
    추가: `ls backend/tests/fixtures/body_normalization/*.json | wc -l` → 5.
    멱등성: `_generate.py --check` 두번 실행해도 byte-identical JSON.
    HIGH-2 박제: `grep -l "timestamp_seconds" backend/tests/fixtures/body_normalization/ 2>/dev/null` → 0 (timestamp_seconds 없음 확인).
    `grep -l '"origin"' backend/tests/fixtures/body_normalization/*.json | wc -l` → 0 (origin 없음 확인).
    `grep -c "uncertainty_proxy" backend/tests/fixtures/body_normalization/*.json | grep -v ':0$' | wc -l` → 5 (5 fixture 모두 uncertainty_proxy 박제).
    HIGH-2 v3 박제: `grep "convert_rtmw_keypoints_to_coco17_and_pole_ext" backend/tests/fixtures/body_normalization/_generate.py` ≥ 1 (adapter 경유 박제 확인).
  </verify>
  <acceptance_criteria>
    - 5 fixture JSON 파일 존재
    - `pose_frame_from_dict` helper 가 5 fixture 모두 load 성공 (smoke PASS)
    - `_generate.py --check` 멱등성 (재실행 시 byte-identical output)
    - HIGH-2 v1→v2 fix 검증: `timestamp_ms` 사용, `Keypoint3D.uncertainty_proxy` 박제, PoleAxis 에 `origin` 없음 (4 필드만)
    - HIGH-2 v3 fix 검증: `_generate.py` 가 `convert_rtmw_keypoints_to_coco17_and_pole_ext` adapter 호출하여 fixture 생성. normal_pose 는 image y-down upright (mid_shoulder.y < mid_hip.y), inverted_pose 는 image y-down inverted (mid_shoulder.y > mid_hip.y)
    - 각 fixture JSON size < 200KB
    - Task 2b RTMW sanity test 존재 (skip OK in CI, Pod 에서 실행 가능)
  </acceptance_criteria>
  <done>
    위 acceptance_criteria 모두 충족.
  </done>
  <commit-template>
    test(02): add PoseFrame factory helper + RTMW-adapter-derived synthetic fixtures (5 cases) + y-down convention smoke (HIGH-2 v3 schema + convention, M1 fallback)
  </commit-template>
  <depends-on>None — Task 1 과 병렬 가능. 그러나 commit 순서는 Task 1 → Task 2 권장 (lockstep 박제 먼저).</depends-on>
</task>

<task type="auto">
  <name>Task 3: body_normalization_measurer.py 본체 + 단위 테스트 (5 fixture + image y-down PoleAxis-relative inversion — HIGH-2 v3)</name>
  <read_first>
    backend/shared/python/sunity_shared/analysis/pose_frame.py (PoseFrame / Keypoint3D / PoleAxis 실제 시그너처 — Task 2 의 helper 와 1:1 정합),
    backend/shared/python/sunity_shared/analysis/temporal.py (MAD outlier `_column_outliers` 패턴 — DEFAULT_OUTLIER_K=3.0),
    backend/shared/python/sunity_shared/analysis/body_normalization.py (BodyNormalizationProfile dataclass — 7 필드 + __post_init__ validator),
    backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/wholebody_keypoints.py (RTMW_KEYPOINT_INDICES — body 17 인덱스),
    backend/shared/python/sunity_shared/analysis/adapters/rtmw_133_to_coco17.py (HIGH-2 v3 — adapter 가 image pixel 좌표 pass-through),
    backend/tests/fixtures/body_normalization/_factory.py (Task 2 산출 — helper 사용)
  </read_first>
  <action>
    Goal: `measure_body_profile(pose_frames: list[PoseFrame], *, conf_gate=0.5, mad_k=3.0, min_valid_frames=5, low_conf_gate=0.4) -> BodyNormalizationProfile` 순수 함수 박제. numpy only (scipy 의존 0). Phase 1 `temporal.py` MAD 패턴 재사용. **HIGH-2 v3 박제** — `pose_too_inverted` 가 RTMW image y-down convention 기준 PoleAxis-relative 판정.

    구현 노트:

    1. **상수 박제 (module top)**:
       - `SEGMENT_PAIRS: dict[str, tuple[str, str]]` (RESEARCH.md `RTMW 133 Keypoint Segment Mapping` 표 그대로 — 좌우 8 segment + shoulder_width + hip_width = 10 entry).
       - `TORSO_TOP_KPS = ("left_shoulder", "right_shoulder")`, `TORSO_BOTTOM_KPS = ("left_hip", "right_hip")`.
       - `WARNING_CODES = frozenset({"low_keypoint_confidence", "occluded_endpoint", "insufficient_frames", "asymmetric_landmark_count", "pose_too_inverted"})` — Task 1 박제 5 enum 정합.
       - 임계값 module 상수: `CONF_GATE_PER_FRAME=0.5`, `MIN_VALID_FRAMES=5`, `MAX_BAD_FRAME_RATIO=0.6`, `MAD_K=3.0`, `LOW_CONFIDENCE_GATE=0.4`, `INVERTED_FRAME_RATIO=0.5`, `ASYMMETRIC_FRAME_RATIO=0.3`.

    2. **`_measure_segment_per_frame(pose_frames, kp_start, kp_end) -> (lengths: np.ndarray, confs: np.ndarray)`**:
       - per-frame `sqrt(dx² + dy²)` 만 사용 (z 무시 — RESEARCH Pitfall 2 + Risk 3 mitigation).
       - confidence = `min(start.confidence, end.confidence)`.
       - 미감지 frame → NaN + conf=0.

    3. **`_measure_torso(pose_frames) -> (lengths, confs)`**:
       - `mid_shoulder = (left_shoulder + right_shoulder) / 2` (좌표 평균).
       - `mid_hip = (left_hip + right_hip) / 2`.
       - 길이 = `sqrt((mid_shoulder.x - mid_hip.x)² + (mid_shoulder.y - mid_hip.y)²)`.
       - conf = min of 4 endpoint confidences.

    4. **`_robust_median(lengths, confs, conf_threshold, mad_k) -> (median: float, segment_conf: float)`**:
       - RESEARCH.md 코드 sketch 그대로. MAD k=3 rejection + median + `(kept_ratio × mean_conf)` 산출.
       - NaN 안전: valid frame < `MIN_VALID_FRAMES` 시 `(nan, 0.0)` 반환.

    5. **HIGH-2 v3 박제 — `_is_inverted_frame(frame: PoseFrame) -> bool | None`**:
       - **Convention docstring (필수 박제)**:
         ```
         """RTMW image y-down pixel coord convention 기준 인버트 여부.

         RTMW 가 image pixel 좌표를 출력 (y 증가 = image 하단). 어댑터
         convert_rtmw_keypoints_to_coco17_and_pole_ext 가 좌표 pass-through.
         Default pole_axis = (0, 1, 0) (image y-down 의 down direction).

         정의:
           torso_up_in_image = mid_hip - mid_shoulder
             (upright 자세에서 shoulder.y < hip.y → torso_up_in_image.y > 0,
              즉 image 좌표계의 down 방향과 같은 방향)

         upright iff dot(torso_up_in_image, pole_axis.axis_vector) > 0
         inverted iff dot(torso_up_in_image, pole_axis.axis_vector) < 0

         (HIGH-2 v3 박제 — REVIEWS.md 2026-06-07 정합)
         """
         ```
       - frame 의 `mid_shoulder = ((left_shoulder + right_shoulder) / 2)`, `mid_hip = ((left_hip + right_hip) / 2)` 좌표 (3D — x, y, z) 추출 (4 endpoint 중 하나라도 없으면 None 반환).
       - `torso_up_in_image = (mid_hip.x - mid_shoulder.x, mid_hip.y - mid_shoulder.y, mid_hip.z - mid_shoulder.z)`.
       - `pole_axis = frame.pole_axis` — None 이면 None 반환 (판정 불가).
       - `proj = dot(torso_up_in_image, pole_axis.axis_vector)` — scalar projection.
       - **인버트 판정 박제 (HIGH-2 v3)**: `proj < 0` → inverted (torso 가 pole_axis 와 반대 방향).
       - 본 함수가 `_check_pose_too_inverted_warning(pose_frames) -> bool` 에서 사용:
         - 모든 frame 의 `_is_inverted_frame` 결과 중 True 비율 > `INVERTED_FRAME_RATIO` (0.5) → warning 발화.
         - None 반환 frame 은 분모에서 제외.

    6. **`measure_body_profile(pose_frames, *, conf_gate=0.5, mad_k=3.0, min_valid_frames=5, low_conf_gate=0.4) -> BodyNormalizationProfile`**:
       - 모든 SEGMENT_PAIRS + torso 길이 측정.
       - 좌우 평균 → upper_arm_median, forearm_median, thigh_median, shank_median.
       - `torso_length = _measure_torso(pose_frames)` 의 robust median.
       - `arm_scale = (upper_arm_median + forearm_median) / torso_length`.
       - `leg_scale = (thigh_median + shank_median) / torso_length`.
       - `torso_scale = 1.0` (self-reference 정의 — M3 박제: "torso 대비 사지 비율").
       - `shoulder_hip_ratio = shoulder_width_median / hip_width_median`.
       - **M3 박제** — `estimated_height_scale = (arm_scale + leg_scale + 1.0) / 3` (평균 torso-relative proportion heuristic, placeholder).
       - `profile.confidence = mean of 11 segment confidences`.
       - **Warnings 판정**:
         - `low_keypoint_confidence`: profile.confidence < `LOW_CONFIDENCE_GATE`.
         - `occluded_endpoint`: 임의 segment 가 endpoint conf < `CONF_GATE_PER_FRAME` in > `MAX_BAD_FRAME_RATIO` (60%) frames.
         - `insufficient_frames`: T < 30.
         - `asymmetric_landmark_count`: 좌측 valid frame 수 vs 우측 valid frame 수 차이 > `ASYMMETRIC_FRAME_RATIO` (30%).
         - `pose_too_inverted`: `_check_pose_too_inverted_warning(pose_frames)` 결과 (HIGH-2 v3 박제 — image y-down PoleAxis-relative).
       - **NaN 방어 (T-02-03 mitigation)**: torso_length == NaN 이거나 valid frames < `MIN_VALID_FRAMES` → 모든 scale=1.0 + confidence=0 + warnings 에 `insufficient_frames` 포함.
       - finite 강제 (`__post_init__` 통과): NaN/inf → 1.0 폴백.

    7. **Logging**: `from __future__ import annotations` 박제. stdlib `logging.getLogger(__name__)`. `log.info("body_profile arm_scale=%.3f leg_scale=%.3f conf=%.3f warnings=%s", ...)`. Secret 없음.

    8. **단위 테스트 (`backend/tests/test_body_normalization_measurer.py`)** — 5 fixture × per-case + HIGH-2 v3 박제 + 회귀:
       - `test_normal_pose_profile_valid`: 정상 fixture → profile.confidence > 0.7, warnings == [], arm_scale ∈ [0.8, 1.4], leg_scale ∈ [1.0, 1.8], shoulder_hip_ratio ∈ [1.1, 1.6].
       - `test_inverted_pose_warning`: inverted fixture → "pose_too_inverted" in warnings.
       - **`test_inverted_warning_uses_y_down_pole_axis_convention` (HIGH-2 v3 박제)** — normal fixture 의 정상 자세는 warning 발화 X 검증:
         - normal_pose fixture (image y-down upright, mid_shoulder.y < mid_hip.y) load.
         - `measure_body_profile(frames)` 결과 `"pose_too_inverted" not in warnings` 검증.
         - 본 테스트가 v2 의 sign-reversed 정의로는 실패 (정상 자세를 inverted 로 오판) → v3 박제 정의로만 PASS.
       - **`test_inverted_warning_fires_for_y_down_inverted_fixture` (HIGH-2 v3 박제)**:
         - inverted_pose fixture (image y-down 에서 mid_shoulder.y > mid_hip.y, 80% frames) load.
         - `measure_body_profile(frames)` 결과 `"pose_too_inverted" in warnings` 검증.
       - **`test_inverted_warning_via_rtmw_adapter_path` (HIGH-2 v3 박제 — adapter 정합)**:
         - 합성 RTMW 133 keypoint dict 2개 작성: (1) image y-down upright (shoulder.y=100, hip.y=200), (2) image y-down inverted (shoulder.y=200, hip.y=100).
         - 각각 `convert_rtmw_keypoints_to_coco17_and_pole_ext` 직접 호출 → 60 frame PoseFrame list 생성.
         - upright 의 `measure_body_profile` 결과 `"pose_too_inverted" not in warnings`.
         - inverted 의 `measure_body_profile` 결과 `"pose_too_inverted" in warnings`.
         - 본 테스트가 v3 박제 convention 이 실제 RTMW adapter path 와 정합 검증.
       - `test_occluded_leg_warning`: occluded fixture → "occluded_endpoint" in warnings.
       - `test_short_clip_warning`: short fixture → "insufficient_frames" in warnings.
       - `test_asymmetric_warning`: asymmetric fixture → "asymmetric_landmark_count" in warnings.
       - `test_z_axis_ignored_when_zero`: 정상 fixture 의 모든 z=0 으로 override 후 측정해도 arm_scale 동일 ± 1% (Pitfall 2 검증).
       - `test_profile_validator_passes`: 5 fixture × `BodyNormalizationProfile.__post_init__` 통과 (NaN 출력 X).
       - `test_idempotent`: 동일 fixture 2번 측정 → byte-identical output (deterministic).
       - 모든 테스트는 Task 2 의 `pose_frame_from_dict` helper 또는 RTMW adapter 로 fixture load.

    참조: RESEARCH.md "Smoothing 알고리즘" + "Length Normalization Unit" + REVIEWS.md v1→v2 M2 / M3 + v2→v3 HIGH-2 + Phase 1 `temporal.py:_column_outliers`.

    No code in this plan.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_body_normalization_measurer.py -x -v</automated>
    추가: `python -c "from sunity_shared.analysis.body_normalization_measurer import measure_body_profile; help(measure_body_profile)"` (import + docstring).
    회귀: `pytest tests/test_body_normalization_profile.py tests/test_body_normalization_lockstep.py -x` (Phase 1 회귀 0).
    의존성: `grep -r "import scipy\|from scipy" backend/shared/python/sunity_shared/analysis/body_normalization_measurer.py | wc -l` → 0.
    HIGH-2 v3 박제: 위 `test_inverted_warning_uses_y_down_pole_axis_convention` + `test_inverted_warning_via_rtmw_adapter_path` 모두 PASS — image y-down convention 정합.
    docstring: `python -c "from sunity_shared.analysis.body_normalization_measurer import _is_inverted_frame; assert 'RTMW image y-down' in _is_inverted_frame.__doc__"`.
  </verify>
  <acceptance_criteria>
    - 11개 단위 테스트 모두 PASS (5 fixture × per-case + HIGH-2 v3 3개 + z=0 + validator + idempotent)
    - `measure_body_profile` 가 5 fixture 에서 BodyNormalizationProfile 반환 (NaN/inf 출력 X)
    - 정상 fixture 에서 warnings == []
    - 4 비정상 fixture 가 각자 1개 이상 정확한 warning 발화
    - HIGH-2 v3 박제: image y-down convention 기준 PoleAxis-relative inversion 판정. normal fixture (upright) warning 미발화, inverted fixture warning 발화, RTMW adapter path 정합 검증
    - `_is_inverted_frame` docstring 에 "RTMW image y-down" 문구 박제
    - scipy import 0 (numpy only)
    - Phase 1 기존 테스트 전부 PASS (회귀 0)
  </acceptance_criteria>
  <done>
    위 acceptance_criteria 모두 충족.
  </done>
  <commit-template>
    feat(02): add measure_body_profile RTMW segment measurer with MAD smoothing + image-y-down PoleAxis inversion (BODY-01, HIGH-2 v3 fix)
  </commit-template>
  <depends-on>Task 1 (warnings enum 박제), Task 2 (5 fixture + pose_frame_from_dict helper + RTMW adapter 경유 fixture)</depends-on>
</task>

<task type="auto">
  <name>Task 4: Pipeline 통합 — `_RTMWNlfCompat.last_body_profile` 사이드카 + Firestore 저장 + 4-way lockstep (HIGH-1 v3 sidecar, MEDIUM-2 v3, B8 박제 보존)</name>
  <read_first>
    backend/functions/pipeline/app.py (lines 200-310 — `_RTMWNlfCompat` 정의, `_ensure_adapters`, `_angles_from_video` line 255, `_angles_and_video_path_from_video` line 271, `_process` 호출 경로),
    backend/functions/pipeline/app.py (lines 480-510 — `firestore_admin.complete_analysis` 호출 site),
    backend/shared/python/sunity_shared/firestore_admin.py (lines 45-72 — `complete_analysis` 시그너처),
    backend/shared/python/sunity_shared/analysis/pose_frame.py (PoseFrame 시그너처 — dataclasses.replace 정합),
    backend/shared/python/sunity_shared/analysis/body_normalization.py (BodyNormalizationProfile dataclass — Firestore serialize 시 사용),
    backend/tests/test_pipeline_recognizer_switch.py (lines 215-260 — TestB8FixSignature 박제 — B8 시그너처 lock 위반 검증 대상),
    docs/contract.md (lines 82-97 — AnalysisDoc Firestore 섹션, MEDIUM-2 v3 4-way lockstep 대상)
  </read_first>
  <action>
    Goal (per HIGH-1 v3, Option D 사이드카): `_RTMWNlfCompat.estimate()` 의 **반환 시그너처 무변경** (`-> np.ndarray`). video-level `BodyNormalizationProfile` 은 `self.last_body_profile` instance attribute 사이드카에 저장. caller (`_process`) 가 `_POSE_ESTIMATOR.last_body_profile` read → `firestore_admin.complete_analysis(body_normalization_profile=...)` 로 Firestore 에 저장. 이로써 (a) B8 fix 박제 (`TestB8FixSignature`) 가 보존되고, (b) Phase 6 가 Firestore 에서 `bodyNormalizationProfile` 을 읽을 수 있다.

    **MEDIUM-2 v3 박제**: 본 task 의 atomic commit 에 `docs/contract.md` AnalysisDoc 섹션 (lines 82-97) 갱신 포함 → 4-way lockstep (`contract.md` + `analysis.ts` + `firestore_admin.py` + `pipeline/app.py`).

    구현 노트:

    1. **`_RTMWNlfCompat.estimate()` 변경 (`backend/functions/pipeline/app.py:222-226`)** — HIGH-1 v3 사이드카:
       - 시그너처: `def estimate(self, frames) -> np.ndarray:` (**무변경 — B8 박제 정합**).
       - `__init__` 에서 `self.last_body_profile: BodyNormalizationProfile | None = None` 초기화 박제 (`BodyNormalizationProfile` 은 lazy / forward-ref 박제 OK).
       - 본체:
         - `pose_frames = self._engine.estimate(frames, self._default_pole)` (기존).
         - `from sunity_shared.analysis.body_normalization_measurer import measure_body_profile` (top-level OK — numpy only).
         - `profile = measure_body_profile(pose_frames)`.
         - `self.last_body_profile = profile`  (사이드카 박제 — HIGH-1 v3).
         - **명시적 코멘트 박제 (필수)**:
           ```
           # HIGH-1 v3 박제 (REVIEWS.md 2026-06-07):
           #   _angles_from_video / _angles_and_video_path_from_video 의 B8 시그너처 lock
           #   (test_pipeline_recognizer_switch.py:225 TestB8FixSignature) 위반 0 을 위해
           #   tuple 반환 X. 대신 instance attribute 사이드카 `last_body_profile` 저장.
           #   caller (_process) 가 _POSE_ESTIMATOR.last_body_profile read → Firestore 저장.
           #   Lambda 는 single-invocation per container 이므로 thread-safe.
           ```
         - `pose_frames_with_profile = [dataclasses.replace(pf, body_shape=profile) for pf in pose_frames]` (Pitfall 3 정합 — frozen dataclass).
         - `import dataclasses` 가 module top-level 에 이미 있는지 확인 (없으면 추가).
         - `from sunity_shared.analysis.pose_frame import to_coco17_array`.
         - `coco_array = to_coco17_array(pose_frames_with_profile)`.
         - `return coco_array` (단일 ndarray 반환 — B8 박제 정합).

    2. **`_angles_from_video` (`backend/functions/pipeline/app.py:255`) — 시그너처 무변경 (HIGH-1 v3 박제)**:
       - 시그너처: `def _angles_from_video(bucket: str, key: str) -> np.ndarray:` (**무변경 — B8 박제 정합**).
       - 반환 무변경 — 기존 `return temporal_fill(angles, joint_uncertainty(keypoints))`.
       - 본 함수는 수정 없음. profile 은 `_POSE_ESTIMATOR.last_body_profile` 로 caller (`_process`) 가 read.

    3. **`_angles_and_video_path_from_video` (`backend/functions/pipeline/app.py:271`) — 시그너처 무변경**:
       - 시그너처: `def _angles_and_video_path_from_video(bucket: str, key: str) -> tuple[np.ndarray, str]:` (**무변경 — B8 박제 정합**).
       - 본 함수도 수정 없음. profile 은 `_POSE_ESTIMATOR.last_body_profile` 로 caller (`_process` Gemini 분기) 가 read.

    4. **`_process` 안 (`backend/functions/pipeline/app.py:300~500` 영역) 갱신** — HIGH-1 v3 사이드카 read:
       - 현 `angles = _angles_from_video(bucket, key)` (or Gemini 분기) 호출 직후 박제:
         - `body_profile = _POSE_ESTIMATOR.last_body_profile`  (사이드카 read — HIGH-1 v3).
         - **명시 코멘트**: `# HIGH-1 v3 박제 — _RTMWNlfCompat.estimate 가 사이드카에 박제 후 caller read.`
       - `firestore_admin.complete_analysis(...)` 호출 (line ~493) 에 `body_normalization_profile=body_profile` kw 추가.

    5. **`firestore_admin.complete_analysis` 시그너처 확장 (`backend/shared/python/sunity_shared/firestore_admin.py:45`)**:
       - 시그너처: `def complete_analysis(uid: str, analysis_id: str, result: dict, *, angles=None, angles_joint_keys=None, angles_frames=None, body_normalization_profile: "BodyNormalizationProfile | None" = None) -> None:`.
       - 본체:
         - 기존 payload 빌드 유지.
         - `if body_normalization_profile is not None:` `payload["bodyNormalizationProfile"] = {"estimatedHeightScale": p.estimated_height_scale, "armScale": p.arm_scale, "legScale": p.leg_scale, "torsoScale": p.torso_scale, "shoulderHipRatio": p.shoulder_hip_ratio, "confidence": p.confidence, "warnings": list(p.warnings)}` (TS camelCase 정합).
       - **Firestore nested-array 제약 (RESEARCH Pitfall 4 + [[firestore-nested-array-flat]] 정합)**: `warnings: list[str]` 은 flat string list → nested array 아님 → 안전. 단위 테스트로 검증.

    6. **TS AnalysisDoc lockstep (`app/src/types/analysis.ts`)**:
       - `AnalysisDoc` interface 에 `bodyNormalizationProfile?: BodyNormalizationProfile;` optional 필드 추가.
       - 주석 박제: "Phase 2 BODY-01 propagation 박제 — Phase 6 가 읽음".

    7. **MEDIUM-2 v3 — `docs/contract.md` AnalysisDoc 섹션 (lines 82-97) 갱신**:
       - 기존 AnalysisDoc 정의에 다음 한 줄 추가:
         ```
         bodyNormalizationProfile?  BodyNormalizationProfile   status='done' 일 때 채워짐 — Phase 6 (체형 정규화 비교) 가 읽는 곳. §7 참조.
         ```
       - 위치: 기존 `result?` 줄 직후 박제 (논리적 그루핑).
       - 본 변경은 Task 4 의 단일 atomic commit 에 포함 — 4-way lockstep (`contract.md` + `analysis.ts` + `firestore_admin.py` + `pipeline/app.py`).

    8. **통합 테스트 (`backend/tests/test_pipeline_body_profile_injection.py`) 신설** — HIGH-1 v3 사이드카 박제:
       - `test_rtmwnlfcompat_estimate_returns_ndarray_only` (HIGH-1 v3 박제):
         - fake `_engine` (3 PoseFrame return) → `_RTMWNlfCompat.estimate(frames)` 호출 → 반환값이 `np.ndarray` (tuple 아님). shape == (3, 17, 4).
       - `test_rtmwnlfcompat_populates_last_body_profile` (HIGH-1 v3 박제):
         - fake `_engine` setup → `estimator.last_body_profile is None` (init 후).
         - `estimator.estimate(frames)` 호출 → `estimator.last_body_profile is not None` + `isinstance(estimator.last_body_profile, BodyNormalizationProfile)` + `__post_init__` 통과.
       - `test_rtmwnlfcompat_no_profile_leak_between_estimates` (HIGH-1 v3 박제 — leakage 0):
         - 첫 `estimate(frames_a)` → profile_a 박제. 둘째 `estimate(frames_b)` (다른 fixture) → profile_b ≠ profile_a (`id()` 다름 또는 다른 측정값).
         - 두 번째 호출 후 `estimator.last_body_profile is profile_b` (이전 a 가 새 값으로 교체됨, 누적/leak X).
       - `test_b8_signature_lock_preserved` (HIGH-1 v3 박제 — B8 박제 보존):
         - `inspect.signature(app._angles_from_video)` → params == ["bucket", "key"] + return_annotation in (np.ndarray, "np.ndarray").
         - `inspect.signature(app._angles_and_video_path_from_video)` → params == ["bucket", "key"] + return_annotation in (`tuple[np.ndarray, str]`, "tuple[np.ndarray, str]") (또는 동등 표현).
         - 본 테스트는 `TestB8FixSignature` 와 평행 박제 — 추가 보호막.
       - `test_estimate_injects_body_shape_into_all_frames`:
         - fake `_engine` setup → `_RTMWNlfCompat.estimate` 호출 후 reflection 으로 (또는 monkeypatch 로 `to_coco17_array` 입력 가로채서) 입력된 `pose_frames` 가 모두 동일 profile 의 `body_shape` 보유 검증.
       - `test_short_frames_graceful_degrade`:
         - T=3 frame (MIN_VALID_FRAMES 미달) → measure_body_profile 이 confidence=0 + insufficient_frames warning 폴백, exception 발생 X.
         - `estimator.last_body_profile` 가 fallback profile 박제 + `__post_init__` 통과.
       - `test_complete_analysis_persists_body_profile_for_phase6` (Phase-6 boundary 박제 — HIGH-1 v3 핵심):
         - monkeypatch `firestore_admin._doc(...).set(...)` 로 payload capture.
         - `firestore_admin.complete_analysis(uid="u1", analysis_id="a1", result={}, body_normalization_profile=profile)` 호출.
         - captured payload 에 `bodyNormalizationProfile` 키 존재 + 7 camelCase 필드 (estimatedHeightScale 등) 모두 박제.
         - `warnings` 가 `list[str]` (flat — Firestore nested-array 안전).
       - `test_contract_md_documents_body_normalization_profile_field` (MEDIUM-2 v3 박제):
         - `docs/contract.md` substring 검사 — `bodyNormalizationProfile` + "BodyNormalizationProfile" 둘 다 AnalysisDoc 섹션 (line 82~97 부근) 등장 검증.
       - `test_to_coco17_array_unaffected`:
         - body_shape 주입 전후 `to_coco17_array` 결과 shape (T,17,4) 동일.

    9. **B8 박제 회귀 검증 (필수)**:
       - 본 task 의 verify 단계가 `pytest tests/test_pipeline_recognizer_switch.py::TestB8FixSignature -x` 를 별도 실행. v3 변경이 `TestB8FixSignature` 의 두 테스트 (`test_angles_from_video_signature_unchanged` + `test_angles_and_video_path_helper_exists`) PASS 유지 검증.

    참조: REVIEWS.md v2→v3 HIGH-1 (Option D 사이드카) + MEDIUM-2 v3 + RESEARCH.md "Pipeline Integration Plan" + firestore-nested-array-flat memory + `backend/tests/test_pipeline_recognizer_switch.py:215-260` B8 lock.

    No code in this plan.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_pipeline_body_profile_injection.py -x -v && pytest tests/test_pipeline_recognizer_switch.py::TestB8FixSignature -x -v && pytest tests/ -x</automated>
    회귀: 기존 pipeline 테스트 전부 PASS.
    B8 박제 검증: 위 `TestB8FixSignature` 두 테스트 PASS (HIGH-1 v3 박제 정합).
    propagation grep: `grep -n "bodyNormalizationProfile\|body_normalization_profile" backend/functions/pipeline/app.py backend/shared/python/sunity_shared/firestore_admin.py app/src/types/analysis.ts docs/contract.md | wc -l` ≥ 4 (4-way lockstep — MEDIUM-2 v3).
    사이드카 박제: `grep -n "last_body_profile" backend/functions/pipeline/app.py | wc -l` ≥ 3 (init + write + caller read).
  </verify>
  <acceptance_criteria>
    - 9개 통합 테스트 모두 PASS (HIGH-1 v3 사이드카 + Phase-6 boundary + MEDIUM-2 v3 contract.md)
    - 기존 `tests/` suite 전체 회귀 0
    - `TestB8FixSignature` 두 테스트 모두 PASS (B8 박제 보존)
    - `_RTMWNlfCompat.estimate` 가 `np.ndarray` 반환 (시그너처 무변경 — HIGH-1 v3 박제)
    - `_RTMWNlfCompat.last_body_profile` instance attribute 존재 + estimate 호출 후 BodyNormalizationProfile 박제
    - sequential estimate 호출 시 last_body_profile leak 0
    - `_angles_from_video` 시그너처 무변경 (`(bucket, key) -> np.ndarray`)
    - `_angles_and_video_path_from_video` 시그너처 무변경 (`(bucket, key) -> tuple[np.ndarray, str]`)
    - `firestore_admin.complete_analysis` 가 `body_normalization_profile` kw 받음 + `bodyNormalizationProfile` 키로 Firestore 저장
    - TS `AnalysisDoc` 에 `bodyNormalizationProfile?` optional 필드 박제
    - **MEDIUM-2 v3**: `docs/contract.md` AnalysisDoc 섹션 (lines 82-97 부근) 에 `bodyNormalizationProfile?` 박제 — atomic commit 에 포함
    - Phase-6 boundary 테스트 PASS — payload capture 검증
    - 단일 atomic commit 에 `contract.md` + `analysis.ts` + `firestore_admin.py` + `pipeline/app.py` 동시 변경 (4-way lockstep)
  </acceptance_criteria>
  <done>
    위 acceptance_criteria 모두 충족.
  </done>
  <commit-template>
    feat(02): propagate BodyNormalizationProfile via _RTMWNlfCompat.last_body_profile sidecar + complete_analysis kw (HIGH-1 v3 Option D — B8 lock preserved, MEDIUM-2 v3 4-way lockstep)
  </commit-template>
  <depends-on>Task 3 (measure_body_profile 함수)</depends-on>
</task>

<task type="auto">
  <name>Task 5a: R&D 비교 harness compare_body_profile.py scaffold + RTMW extraction helper + graceful-empty smoke (HIGH-3 v3 + HIGH-4 v3)</name>
  <read_first>
    backend/research/evaluations/compare_engines.py (R&D harness 패턴 — argparse + reporting 구조 + L-2 박제 "/workspace/SunityMotion" 실행 경로),
    backend/research/evaluations/compare_rtmw_vs_ipsf.py (lines 717-733 — `_load_pose_engine("rtmw")` 패턴, lines 232-258 — `run_rtmw` 영상 → PoseFrame 변환 — HIGH-4 v3 helper 가 동일 패턴 재사용),
    backend/shared/python/sunity_shared/analysis/body_normalization.py (BodyNormalizationProfile dataclass — 갭 계산 대상),
    backend/shared/python/sunity_shared/analysis/body_normalization_measurer.py (Task 3 산출 — RTMW path 측정기 호출)
  </read_first>
  <action>
    Goal: NLF→SMPL-X β path 와 RTMW segment path 의 BodyNormalizationProfile 갭을 보고하는 CLI scaffold + Task 5b 가 belle Pod 에서 사용할 RTMW extraction pre-step helper. **`backend/research/` 격리** — `sunity_shared` 무관. D-02-04 정합 (SMPL-X 상업 라이선스 X — R&D 만).

    **v3 박제 (HIGH-3 + HIGH-4)**: 
    - HIGH-3 v3: `load_nlf_smplx_keypoints` 가 `NotImplementedError` raise 박제 폐기 → graceful-empty 패턴. 빈/비존재 디렉터리 → `[]` 반환. Task 5b 가 실 구현 추가해도 5a smoke 가 통과.
    - HIGH-4 v3: 별도 helper `backend/research/evaluations/extract_rtmw_body_profile_keypoints.py` scaffold (Task 5a 산출). belle 가 Pod 에서 본 helper 실행 → `reports/sweep_rtmw_<date>/keypoints/<video>.json` 생성 → Task 5b 의 `--rtmw-keypoints-dir` 입력.

    **HIGH-4 분할 박제 (v2 정의 유지)**: 본 task (5a) 는 scaffold + smoke test + math + extraction helper scaffold — executor 가능. **belle Pod 실행 + 실 보고서 체크인은 Task 5b (user_setup gate)** 로 분리.

    구현 노트:

    1. **파일 헤더 docstring (`backend/research/evaluations/compare_body_profile.py`)** — LOW-1 v3 박제:
       ```
       """RTMW segment vs NLF→SMPL-X β BodyNormalizationProfile 갭 보고.

       D-02-04 박제: NLF/SMPL-X 모듈 import 는 본 파일에서만. sunity_shared 무관.
       [CITED: license-blocklist-pose memory — SMPL-X 상업 불가 / R&D 만]
       실행 환경: belle 사내 RunPod GPU pod (Lambda/RunPod 운영 path X).

       실행 경로 (L-2 박제, LOW-1 v3 정합):
         cd /workspace/SunityMotion
         python -m backend.research.evaluations.compare_body_profile --help

       HIGH-4 v3 분할 박제 (REVIEWS.md):
         - Task 5a (본 PR scope): scaffold + smoke + gap 산식 + extract_rtmw_body_profile_keypoints.py helper
         - Task 5b (belle Pod 실행, user_setup): RTMW keypoint dump 추출 → NLF→SMPL-X β 호출 → reports/body_profile_gap_<date>.json 체크인

       HIGH-3 v3 박제 (REVIEWS.md): load_nlf_smplx_keypoints 는 NotImplementedError raise X.
         graceful-empty 패턴 — 빈/비존재 디렉터리 → []. 실 NLF 산출 박제 시 list 반환.
       """
       ```

    2. **CLI argparse (`compare_body_profile.py`)**:
       - `--rtmw-keypoints-dir` (Task 5b 가 extract_rtmw_body_profile_keypoints.py 로 생성한 디렉터리).
       - `--nlf-keypoints-dir` (R&D NLF→SMPL-X β path 출력).
       - `--videos` (영상 리스트, default = `ref-foxtop ref-foxtop-split ref-invert ref-sideway-spin ref-climb`).
       - `--output` (default `backend/research/evaluations/reports/body_profile_gap_{date}.json`).

    3. **`compare_body_profile.py` 함수 구조**:
       - `load_rtmw_keypoints(path: Path) -> list[PoseFrame]` — Task 5a helper 산출 디렉터리 load. 디렉터리 비존재/빈 → `[]` (graceful-empty — HIGH-3 v3 정합).
       - `load_nlf_smplx_keypoints(path: Path) -> list[PoseFrame]` — **HIGH-3 v3 박제 — graceful-empty**:
         - `if not path.exists() or not any(path.iterdir()): return []` (NotImplementedError X).
         - 디렉터리 + 파일 존재 시 NLF→SMPL-X β path 출력 load.
         - **본 PR (5a) 에서는 실제 NLF 호출 로직 비어있음** — 디렉터리 존재 + 파일 존재 시에도 stub 구현은 빈 list 반환 또는 TODO 마커.
         - belle Pod (Task 5b) 가 본 함수의 실 구현 추가 — 함수 시그너처/return type 은 v3 박제 유지.
         - **명시 코멘트 (필수)**:
           ```
           # HIGH-3 v3 박제 (REVIEWS.md 2026-06-07):
           #   v2 의 NotImplementedError raise 패턴 폐기.
           #   graceful-empty — Task 5b 가 실 구현 추가해도 5a smoke 통과.
           #   비존재 디렉터리 → [] (raise X). belle Pod 가 NLF→SMPL-X β 산출
           #   파일을 본 디렉터리에 박제 후 list 반환.
           # TODO(belle-pod, Task 5b): 실 NLF→SMPL-X β path load + parse.
           ```
       - `compute_profile_gap(rtmw_profile: BodyNormalizationProfile, smplx_profile: BodyNormalizationProfile) -> dict` — abs diff per field. **본 PR 의 수학은 완전 구현** (smoke 가능):
         - `arm_scale_abs_diff = abs(rtmw.arm_scale - smplx.arm_scale)`.
         - 6 numeric 필드 모두 (warnings 제외).
         - `verdict` = `"within_5pct_tolerance"` if all diffs < 0.05 else `"gap_too_wide"`.
       - `main()` → JSON + markdown 보고서 산출 (compare_engines.py 패턴).

    4. **`backend/research/evaluations/extract_rtmw_body_profile_keypoints.py` 신설 (HIGH-4 v3 박제 — Task 5b pre-step helper)**:
       - 파일 헤더 docstring:
         ```
         """belle Pod RTMW keypoint dump 추출 helper — Task 5b 의 pre-step.

         HIGH-4 v3 박제 (REVIEWS.md 2026-06-07): Task 5b 의 compare_body_profile.py 실행
         전에 belle 가 본 helper 로 5영상 → RTMW keypoint dump 산출.

         실행 경로 (L-2 박제 + LOW-1 v3 정합):
           cd /workspace/SunityMotion
           python -m backend.research.evaluations.extract_rtmw_body_profile_keypoints \\
             --videos ref-foxtop ref-foxtop-split ref-invert ref-sideway-spin ref-climb \\
             --output-dir backend/research/evaluations/reports/sweep_rtmw_$(date +%Y%m%d)/keypoints/

         산출: <output-dir>/<video>.json — per-video serialized list[PoseFrame].
         compare_body_profile.py 가 --rtmw-keypoints-dir 인자로 본 디렉터리 사용.

         [CITED: backend/research/evaluations/compare_rtmw_vs_ipsf.py:717-733]
                 _load_pose_engine("rtmw") 패턴 재사용.
         [CITED: backend/research/evaluations/compare_rtmw_vs_ipsf.py:232-258]
                 run_rtmw 영상 → PoseFrame 패턴 재사용.
         """
         ```
       - CLI argparse: `--videos` (list of motion id), `--output-dir` (default `backend/research/evaluations/reports/sweep_rtmw_{date}/keypoints/`), `--video-source` (default S3 — `s3://sunity-motion-pilot-videos/reference/<video>.mp4`).
       - 함수 구조:
         - `_load_video_frames(motion_id: str) -> np.ndarray` — `compare_rtmw_vs_ipsf.py` 의 `_download_s3_video` 재사용 또는 동일 패턴.
         - `_detect_pole_axis(video_frames) -> PoleAxis` — `compare_rtmw_vs_ipsf.py:200-225` 의 detect_pole_axis 또는 vertical fallback 재사용.
         - `extract(motion_id, output_dir)`:
           - 영상 load → `RTMWPoseEngine()` instantiate (compare_rtmw_vs_ipsf 의 `_load_pose_engine("rtmw")` 패턴 재사용) → `run_rtmw(video_frames, pole_axis, pose_engine)` 호출 → `list[PoseFrame]` 산출.
           - `dataclasses.asdict` 또는 custom serializer → `output_dir/{motion_id}.json` 저장.
         - `main()` — CLI entry.
       - **Lazy import**: `RTMWPoseEngine` / `boto3` 는 함수 body 에서만 lazy import (Lambda fail-fast 안전).
       - **본 PR 에서는 scaffold + smoke (mock) 가능** — belle Pod 에서 실제 RTMW ONNX 호출.

    5. **운영 코드 import (양 파일 공통)**:
       - `from sunity_shared.analysis.body_normalization_measurer import measure_body_profile` 는 OK (RTMW path 가 운영 측정기 호출).
       - `from sunity_shared.analysis.body_normalization import BodyNormalizationProfile` 도 OK (dataclass type).
       - NLF / SMPL-X import 는 `compare_body_profile.py` 함수 body 안의 lazy import — module-top 금지.
       - RTMW lazy import (extract_rtmw_body_profile_keypoints.py) 는 함수 body 안 — module-top 금지.

    6. **Smoke test (`backend/tests/test_compare_body_profile_smoke.py`)** — NLF/SMPL-X 실호출 X, RTMW 실호출 X (mock):
       - `test_argparse_help_compare` (LOW-1 v3 박제): `python -m backend.research.evaluations.compare_body_profile --help` 실행 시 sys.exit 0, "rtmw-keypoints-dir" 문자열 등장. 본 명령은 **repo root 에서 실행** — `cd backend` 사용 X.
       - `test_argparse_help_extract` (HIGH-4 v3 + LOW-1 v3): `python -m backend.research.evaluations.extract_rtmw_body_profile_keypoints --help` 실행 시 sys.exit 0, "--videos" + "--output-dir" 등장.
       - `test_compute_profile_gap_zero`: 동일 profile 2개 입력 → 모든 diff == 0, verdict == "within_5pct_tolerance".
       - `test_compute_profile_gap_nonzero`: 다른 profile (arm_scale 차이 0.10) → abs diff 0.10, verdict == "gap_too_wide".
       - `test_load_rtmw_empty_dir`: 비존재 디렉터리 + 빈 디렉터리 → graceful empty list (raise X).
       - **`test_load_nlf_smplx_returns_empty_for_missing_dir` (HIGH-3 v3 박제)**:
         - `load_nlf_smplx_keypoints(Path("/tmp/nonexistent_dir_xyz"))` → `[]` (`NotImplementedError` raise X).
         - `load_nlf_smplx_keypoints(empty_tmp_dir)` → `[]`.
         - 본 테스트가 Task 5b 의 실 구현 landing 후에도 비존재 디렉터리에 대해서는 PASS 유지 — 회귀 0.
       - `test_extract_rtmw_helper_callable_with_mock` (HIGH-4 v3 박제):
         - mock RTMWPoseEngine + mock video frames → `extract(motion_id="test", output_dir=tmp_path)` 호출 → `tmp_path/test.json` 파일 생성 + load 가능 (raise X).

    7. **TODO 박제 (compare_body_profile.py 하단)**:
       - `# TODO(belle-pod, Task 5b): NLF→SMPL-X β path 실 구현 (load_nlf_smplx_keypoints 본체).`
       - `# TODO(belle-pod, Task 5b): Pod 에서 (a) extract_rtmw_body_profile_keypoints 실행 → (b) NLF→SMPL-X β path 실행 → (c) compare_body_profile 실행 → reports/body_profile_gap_<date>.json 산출 → git commit.`
       - `# 본 TODO 해소 = ROADMAP success criterion 4 closure (HIGH-4 게이트).`

    참조: REVIEWS.md v1→v2 HIGH-4 + v2→v3 HIGH-3 + v2→v3 HIGH-4 + v2→v3 LOW-1 + RESEARCH.md "R&D 비교 Harness" + `backend/research/evaluations/compare_engines.py:28-34` 실행 경로 박제.

    No code in this plan.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_compare_body_profile_smoke.py -x -v</automated>
    추가 (repo root 실행 — LOW-1 v3 박제): `python -m backend.research.evaluations.compare_body_profile --help 2>&1 | grep -q "rtmw-keypoints-dir"` (cd backend X).
    추가 (HIGH-4 v3 helper): `python -m backend.research.evaluations.extract_rtmw_body_profile_keypoints --help 2>&1 | grep -q -- "--videos"`.
    HIGH-3 v3 박제 검증: `grep -c "NotImplementedError" backend/research/evaluations/compare_body_profile.py` → 0 (NotImplementedError raise 박제 폐기).
  </verify>
  <acceptance_criteria>
    - 7개 smoke test 모두 PASS (compare argparse + extract argparse + 2 gap math + load_rtmw empty + load_nlf_smplx graceful empty + extract mock)
    - `python -m backend.research.evaluations.compare_body_profile --help` 가 repo root 에서 실행 가능 (LOW-1 v3 박제)
    - `python -m backend.research.evaluations.extract_rtmw_body_profile_keypoints --help` 가 repo root 에서 실행 가능
    - `compare_body_profile.py`: NLF/SMPL-X import 가 본 파일 함수 body 에서만 (Task 6 의 AST 테스트로 강제)
    - `extract_rtmw_body_profile_keypoints.py`: RTMW import 가 본 파일 함수 body 에서만
    - 두 파일 헤더에 D-02-04 + license-blocklist-pose memory + L-2 박제 실행 경로 인용
    - **HIGH-3 v3 박제**: `load_nlf_smplx_keypoints` 가 `NotImplementedError` raise X — graceful-empty 반환. Task 5b landing 후 회귀 0.
    - **HIGH-4 v3 박제**: `extract_rtmw_body_profile_keypoints.py` 가 신설되고 belle Pod 가 사용할 pre-step helper 박제 (compare_rtmw_vs_ipsf.py 패턴 재사용)
    - gap 산식 완전 구현 (5a scope) — Task 5b 가 실 입력만 채우면 됨
    - 파일 하단 TODO(belle-pod, Task 5b) Step (a)~(c) 박제
  </acceptance_criteria>
  <done>
    위 acceptance_criteria 모두 충족.
  </done>
  <commit-template>
    chore(02): scaffold R&D compare_body_profile + extract_rtmw_body_profile_keypoints with graceful-empty NLF loader (HIGH-3 v3 + HIGH-4 v3 + LOW-1 v3)
  </commit-template>
  <depends-on>Task 3 (BodyNormalizationProfile 측정기 — gap 계산 대상)</depends-on>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5b: belle Pod (a) RTMW keypoint dump 추출 → (b) NLF→SMPL-X β 실행 → (c) body_profile_gap_&lt;date&gt;.json 체크인 (HIGH-4 v3 + LOW-1 v3 — ROADMAP §4 closure gate)</name>
  <files>
    backend/research/evaluations/reports/sweep_rtmw_&lt;YYYYMMDD&gt;/keypoints/ref-foxtop.json (create — belle Pod 산출, Step (a)),
    backend/research/evaluations/reports/sweep_rtmw_&lt;YYYYMMDD&gt;/keypoints/ref-foxtop-split.json (create — belle Pod 산출, Step (a)),
    backend/research/evaluations/reports/sweep_rtmw_&lt;YYYYMMDD&gt;/keypoints/ref-invert.json (create — belle Pod 산출, Step (a)),
    backend/research/evaluations/reports/sweep_rtmw_&lt;YYYYMMDD&gt;/keypoints/ref-sideway-spin.json (create — belle Pod 산출, Step (a)),
    backend/research/evaluations/reports/sweep_rtmw_&lt;YYYYMMDD&gt;/keypoints/ref-climb.json (create — belle Pod 산출, Step (a)),
    backend/research/evaluations/reports/body_profile_gap_&lt;YYYYMMDD&gt;.json (create — belle Pod 산출, Step (c)),
    backend/research/evaluations/reports/body_profile_gap_&lt;YYYYMMDD&gt;.md (create — belle Pod 산출, Step (c))
  </files>
  <read_first>
    .planning/phases/02-bodynormalizationprofile-rtmw-segment/02-RESEARCH.md (R&D 격리 + NLF→SMPL-X β 변환 경로 + Architectural Responsibility Map),
    backend/research/evaluations/compare_body_profile.py (Task 5a 산출 scaffold — load_nlf_smplx_keypoints graceful-empty 위치 + gap math),
    backend/research/evaluations/extract_rtmw_body_profile_keypoints.py (Task 5a 산출 — HIGH-4 v3 pre-step helper),
    backend/research/evaluations/compare_engines.py (R&D harness 패턴 — 보고서 산출 형식 참조),
    backend/research/evaluations/compare_rtmw_vs_ipsf.py (RTMWPoseEngine + run_rtmw 패턴),
    docs/licenses/rtmw-weights-audit.md (라이선스 게이트 — SMPL-X 비상업 박제 확인),
    backend/shared/python/sunity_shared/analysis/body_normalization.py (BodyNormalizationProfile dataclass — RTMW 측정값과 비교할 target schema)
  </read_first>
  <action>
    Goal (per HIGH-4 v3 게이트): ROADMAP success criterion 4 ("R&D 비교군: 동일 영상에서 NLF→SMPL-X β로 추출한 BodyNormalizationProfile 과의 갭을 보고서로 출력") 의 실제 closure. executor (자동화 path) 가 수행 불가 — belle 의 RunPod GPU pod 에서 (a) RTMW ONNX 호출 + (b) NLF/SMPL-X 모델 weights + (c) 실 영상 access 가 필요.

    **HIGH-4 v3 박제**: scaffold (Task 5a) 만으로는 ROADMAP §4 closure 표시 X. v2 와 달리 v3 는 **Step (a) RTMW extraction pre-step 명시** — belle 가 Task 5a 산출 helper (`extract_rtmw_body_profile_keypoints.py`) 로 keypoint dump 를 먼저 생성해야 compare_body_profile.py 가 실행 가능. 본 task 가 (a) + (b) + (c) 3 단계 모두 완료되어야만 phase verifier 가 success criterion 4 PASS 처리.

    **LOW-1 v3 박제**: 모든 `python -m` invocation 은 repo root (`/workspace/SunityMotion`) 에서 실행. `cd backend` 후 `backend.research...` 박제 금지.
  </action>
  <what-built>
    Task 5a 의 `compare_body_profile.py` scaffold + `extract_rtmw_body_profile_keypoints.py` helper + smoke (executor 가 commit). `load_nlf_smplx_keypoints` 는 graceful-empty (비존재 디렉터리 → `[]`, HIGH-3 v3).
  </what-built>
  <how-to-verify>
    belle 가 수행 (모든 `python -m` 은 repo root 에서, LOW-1 v3 박제):

    **Step (a) — HIGH-4 v3 pre-step: RTMW keypoint dump 추출 (필수)**:

    1. **belle Pod (1ablelgbtrzcgb 또는 후속) ssh**:
       ```
       ssh -p <port> -i ~/.ssh/id_ed25519 root@<pod-ip>
       cd /workspace/SunityMotion
       git pull origin main  # Task 5a scaffold 받음
       ```

    2. **Pod env 확인 (이미 박제)**:
       - `echo $RTMW_ONNX_PATH` → 절대 경로 출력 (rtmw end2end.onnx).
       - `echo $YOLOX_ONNX_PATH` → 절대 경로 출력 (rtmlib detector).
       - 미박제 시 [[runpod-gpu-env]] 박제 참조 후 재설정.

    3. **5영상 keypoint dump 추출**:
       ```
       cd /workspace/SunityMotion
       export OUT=backend/research/evaluations/reports/sweep_rtmw_$(date +%Y%m%d)/keypoints
       mkdir -p "$OUT"
       python -m backend.research.evaluations.extract_rtmw_body_profile_keypoints \
         --videos ref-foxtop ref-foxtop-split ref-invert ref-sideway-spin ref-climb \
         --output-dir "$OUT"
       ```
       산출 — `$OUT/<video>.json` 5개 파일.

    **Step (b) — NLF→SMPL-X β path 실 구현 + 실행**:

    4. **`load_nlf_smplx_keypoints` 실 구현 (편집)**:
       - `backend/research/evaluations/compare_body_profile.py` 의 graceful-empty stub 본체를 belle Pod 에서 실 NLF 호출로 교체 (NLF weights 이미 박제).
       - 5영상 (`ref-foxtop ref-foxtop-split ref-invert ref-sideway-spin ref-climb`) 의 raw frames → NLF 3D keypoints → SMPL-X β fit → segment 비율 변환 → `BodyNormalizationProfile`.
       - 산출 디렉터리: `backend/research/evaluations/reports/nlf_smplx_$(date +%Y%m%d)/<video>.json`.
       - 함수 시그너처 `(path: Path) -> list[PoseFrame]` 무변경 — HIGH-3 v3 박제 정합. 비존재 디렉터리에 대해서는 여전히 `[]` 반환.

    5. **gap 보고서 산출 (repo root 에서, LOW-1 v3 박제)**:
       ```
       cd /workspace/SunityMotion
       python -m backend.research.evaluations.compare_body_profile \
         --rtmw-keypoints-dir backend/research/evaluations/reports/sweep_rtmw_$(date +%Y%m%d)/keypoints/ \
         --nlf-keypoints-dir backend/research/evaluations/reports/nlf_smplx_$(date +%Y%m%d)/ \
         --output backend/research/evaluations/reports/body_profile_gap_$(date +%Y%m%d).json
       ```

    **Step (c) — 결과 검증 + commit**:

    6. **결과 검증**:
       - JSON + Markdown 보고서가 5영상 모두에 대해 RTMW vs SMPL-X β BodyNormalizationProfile gap 표시.
       - `verdict` 가 belle 검토 — within tolerance 또는 gap_too_wide.

    7. **git commit**:
       ```
       git add backend/research/evaluations/reports/sweep_rtmw_*/keypoints/*.json
       git add backend/research/evaluations/reports/body_profile_gap_*.{json,md}
       git commit -m "data(02): add RTMW keypoint dumps + body_profile_gap_<date>.{json,md} from belle Pod 5-video run (HIGH-4 v3 ROADMAP §4 closure)"
       ```

    8. **본 plan 의 task 5b 종료 신호**:
       - belle 가 SUMMARY 에 산출 파일 경로 박제 후 "approved" 라고 응답.
       - 만약 NLF→SMPL-X β path 가 belle Pod 에서 구현 어려우면 ("not feasible") — Task 5b 박제 보류 + ROADMAP §4 를 v1.5 로 deferred 박제 (belle 결정).
  </how-to-verify>
  <resume-signal>belle 가 "approved" + `body_profile_gap_<date>.json` 파일 경로 응답, 또는 "not feasible — defer to v1.5" 응답 시 다음 task 진행</resume-signal>
  <acceptance_criteria>
    - **Step (a) HIGH-4 v3 박제**: `backend/research/evaluations/reports/sweep_rtmw_<date>/keypoints/<video>.json` 5개 파일 체크인 (5영상 모두)
    - **Step (b)**: `compare_body_profile.py::load_nlf_smplx_keypoints` 가 실 NLF→SMPL-X β path 호출 구현 (graceful-empty 시그너처 유지 — HIGH-3 v3 박제 정합)
    - **Step (c)**: `backend/research/evaluations/reports/body_profile_gap_<date>.json` + `.md` 파일이 5영상 데이터 포함하여 체크인됨 (approved path)
    - 또는 belle "not feasible" 박제 + ROADMAP §4 v1.5 defer 박제 (alternative path)
    - 모든 `python -m` invocation 이 repo root 에서 실행됨 (LOW-1 v3 박제)
    - 본 task 가 closed 되어야 phase verifier 가 ROADMAP success criterion 4 PASS 처리
  </acceptance_criteria>
  <done>
    위 둘 중 하나 충족.
  </done>
  <commit-template>
    data(02): add RTMW keypoint dumps + body_profile_gap_<date>.{json,md} from belle Pod 5-video run (HIGH-4 v3 ROADMAP §4 closure)
  </commit-template>
  <depends-on>Task 5a (compare_body_profile.py scaffold + extract_rtmw_body_profile_keypoints.py helper)</depends-on>
</task>

<task type="auto">
  <name>Task 6: AST import isolation 확장 (sunity_shared + backend/functions + backend/runpod_inference + importlib 차단 — MEDIUM-1 v3) + REQUIREMENTS.md BODY-01 RTMW 정합 갱신</name>
  <read_first>
    backend/shared/python/sunity_shared/analysis/pose_engines/__init__.py (lazy mediapipe import 박제 — v1→v2 HIGH-3 root cause),
    backend/shared/python/sunity_shared/analysis/pose_engines/mediapipe_engine.py (live mediapipe import lines 74-76, 197),
    backend/functions/pipeline/app.py (deployable product path — MEDIUM-1 v3 박제 대상),
    backend/runpod_inference/server.py (deployable product path — MEDIUM-1 v3 박제 대상),
    .planning/REQUIREMENTS.md (line 30 BODY-01 — MediaPipe 언급),
    backend/research/evaluations/compare_body_profile.py (Task 5a 산출 — import 차단 의미 부여),
    backend/research/evaluations/extract_rtmw_body_profile_keypoints.py (Task 5a 산출 — Task 6 가 본 path 도 import 차단 검증 대상에 포함)
  </read_first>
  <action>
    Goal:
    (1) AST 스캔으로 **deployable product code** (`sunity_shared/**/*.py` + `backend/functions/**/*.py` + `backend/runpod_inference/**/*.py`) 가 운영-격리 대상 모듈 (`backend.research` / `research` / `nlf` / `smplx` / `smpl_x`) 을 import 시 fail 하는 자동 차단 테스트 박제. **MEDIUM-1 v3 fix — 스캔 범위 확장**. 추가: `importlib.import_module(...)` 동적 string literal import 도 차단 (MEDIUM-1 v3 박제). **v1→v2 HIGH-3 fix 유지 — `mediapipe` 차단 제외** (legacy code 살아있음, RTMW pivot post-cleanup 별 phase).
    (2) REQUIREMENTS.md BODY-01 의 "MediaPipe" → "RTMW" 정합 갱신.

    **MEDIUM-3 v3 박제**: 본 task 는 Task 5a 만 의존 (Task 5b user_setup gate 무관). belle Pod 게이트 지연 시에도 본 task 가 평행 진행 가능.

    구현 노트:

    **(1) `backend/tests/test_research_import_isolation.py` 신설 — MEDIUM-1 v3 박제**:

    - `ast.parse` 로 다음 3 디렉터리 하위 모든 `.py` 파일 스캔:
      - `backend/shared/python/sunity_shared/` (운영 shared 패키지)
      - `backend/functions/` (Lambda 함수 — MEDIUM-1 v3 추가)
      - `backend/runpod_inference/` (RunPod GPU 서버 — MEDIUM-1 v3 추가)
    - 스캔 제외 (R&D 모듈 import 허용):
      - `backend/tests/`
      - `backend/research/`
    - 금지 import 패턴 (HIGH-3 narrowed list — v1→v2 정합):
      - `import research`
      - `import backend.research`
      - `from research import ...`
      - `from research.X import ...`
      - `from backend.research import ...`
      - `from backend.research.X import ...`
      - `import nlf`
      - `from nlf import ...`
      - `from nlf.X import ...`
      - `import smplx`
      - `from smplx import ...`
      - `from smplx.X import ...`
      - `import smpl_x`
      - `from smpl_x import ...`
    - **MEDIUM-1 v3 박제 — importlib 동적 import 차단**:
      - `ast.walk` 로 `ast.Call` 노드 모두 검사.
      - `node.func` 가 `ast.Attribute` 이고 `node.func.attr == "import_module"` (importlib) 인 경우.
      - 첫 번째 positional arg 가 `ast.Constant` (string literal) 일 때 그 값이 `'research'`, `'backend.research'`, `'nlf'`, `'smplx'`, `'smpl_x'` 또는 이들 prefix 로 시작하는 dotted path 이면 위반.
      - 비-literal 인자 (변수, expression) 는 v3 scope 밖 — 정적 분석 불가, TODO 마커 박제.
    - **`mediapipe` 차단 제외 (v1→v2 HIGH-3 박제 유지)**:
      - 본 phase 는 mediapipe 차단 책임 X.
      - 파일 상단에 큰 TODO 코멘트 박제:
        ```
        # TODO(post-mediapipe-removal): RTMW pivot (commit 2a8aa72) 후속 작업 —
        # mediapipe quarantine 은 별도 phase 책임 ([[rtmw-free-stack-pivot]] 정합).
        # 본 phase (Phase 2) 가 mediapipe import 차단까지 떠안으면 sunity_shared/pose_engines/
        # mediapipe_engine.py 와 mediapipe_lifter_engine.py 가 즉시 violations 으로 잡힘
        # (현재 legacy live code). REVIEWS.md v1→v2 HIGH-3 박제 — narrowed 범위.
        ```
    - 위반 검출 시 `assert False, f"Deployable product module {path} illegally imports {forbidden}: {node.lineno}"`.
    - **테스트 함수 4개 (MEDIUM-1 v3 확장)**:
      - `test_sunity_shared_does_not_import_research_or_nlf_or_smplx`: sunity_shared 의 backend.research / research / nlf / smplx / smpl_x 차단 검증.
      - `test_backend_functions_does_not_import_research_or_nlf_or_smplx` (MEDIUM-1 v3): backend/functions/ (pipeline, upload-url, reference-api) 의 동일 차단 검증.
      - `test_backend_runpod_inference_does_not_import_research_or_nlf_or_smplx` (MEDIUM-1 v3): backend/runpod_inference/ 의 동일 차단 검증.
      - `test_no_dynamic_importlib_import_of_research_or_nlf_or_smplx` (MEDIUM-1 v3 박제): 위 3 디렉터리 전체에서 `importlib.import_module(literal)` 의 literal 인자가 차단 prefix 매치 0 검증.
    - **운영 path 만 검증** — `backend/tests/` / `backend/research/` 는 검증 대상 X (Task 5a 의 `compare_body_profile.py` 는 R&D 디렉터리이므로 검증 대상 X 정합).
    - pytest 실행 시 < 1초 (순수 AST).

    **(2) `.planning/REQUIREMENTS.md` line 30 갱신**:
    - 현 텍스트: `"**BODY-01**: MediaPipe 키포인트로부터 신체 segment 길이·비율·좌우 비대칭이 자동 추출되어 ...`
    - 갱신: `"**BODY-01**: RTMW 133 wholebody 키포인트로부터 ...`.
    - 뒷부분 `SMPL-X β 비교군은 R&D 평가 스크립트에서만 갭 보고 (제품 코드 비호출)` 유지.
    - footer note 추가 (REQUIREMENTS.md 하단 update 로그):
      - `*Updated 2026-06-07 — Phase 2 plan 01 RTMW pivot 정합 (BODY-01 MediaPipe → RTMW 갱신).*`.
    - line 27 POSE-01 도 MediaPipe 언급 있음 — **본 task scope X** (Phase 1 책임, 이미 RTMW swap 완료).
    - 본 task 는 BODY-01 1줄만 변경 (atomic + scoped).

    참조: REVIEWS.md v1→v2 HIGH-3 + v2→v3 MEDIUM-1 + v2→v3 MEDIUM-3 + RESEARCH.md "R&D 비교 Harness" + Plan 01-24 정합.

    No code in this plan.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_research_import_isolation.py -x -v</automated>
    추가: `grep -n "RTMW" .planning/REQUIREMENTS.md | grep "BODY-01"` → 1.
    추가: `grep "MediaPipe" .planning/REQUIREMENTS.md | grep "BODY-01"` → 0 (BODY-01 에서 MediaPipe 제거).
    HIGH-3 박제 유지: `grep -c "mediapipe" backend/tests/test_research_import_isolation.py` ≥ 1 (TODO 코멘트 + 박제 사유 존재).
    MEDIUM-1 v3 박제: `grep -c "backend/functions\|backend/runpod_inference\|importlib" backend/tests/test_research_import_isolation.py` ≥ 3 (3 디렉터리 + importlib 검사 박제).
  </verify>
  <acceptance_criteria>
    - 4개 AST 테스트 PASS (sunity_shared + backend/functions + backend/runpod_inference + importlib 동적)
    - **MEDIUM-1 v3**: 스캔 범위가 `backend/shared/python/sunity_shared/` + `backend/functions/` + `backend/runpod_inference/` 3 디렉터리 박제
    - **MEDIUM-1 v3**: `importlib.import_module(literal)` 의 string literal 차단 prefix 매치 0 검증
    - sunity_shared + backend/functions + backend/runpod_inference 의 backend.research / NLF / SMPL-X import 0 확인
    - mediapipe 차단 코드 **없음** (TODO 박제만 존재 — v1→v2 HIGH-3 fix 유지)
    - REQUIREMENTS.md BODY-01 (line 30) 이 RTMW 로 갱신, MediaPipe 제거
    - REQUIREMENTS.md footer note (2026-06-07 RTMW pivot 정합) 추가
  </acceptance_criteria>
  <done>
    위 acceptance_criteria 모두 충족.
  </done>
  <commit-template>
    chore(02): expand AST R&D import isolation to functions+runpod_inference+importlib (MEDIUM-1 v3, mediapipe excluded — HIGH-3 v2) + align BODY-01 to RTMW
  </commit-template>
  <depends-on>Task 5a (R&D harness 존재해야 import isolation 의 의미 있음 — MEDIUM-3 v3 박제: Task 5b 무관, belle Pod 게이트와 평행 진행 가능)</depends-on>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RunPod Pod ↔ Lambda | RTMW pose_frames 가 RunPod 에서 측정 후 Lambda 가 결과 받음 — Phase 2 는 내부 호출만 (외부 입력 X) |
| sunity_shared + backend/functions + backend/runpod_inference (운영) ↔ backend.research (R&D) | 라이선스 격리 (SMPL-X 상업 불가) — import leak 시 법적 리스크. **MEDIUM-1 v3 박제 — 스캔 범위 확장 + importlib 동적 차단** |
| `_RTMWNlfCompat.estimate` (Pod) ↔ Firestore (Lambda complete_analysis) | BodyNormalizationProfile propagation 경계 — Phase 6 consumer 가 읽는 곳 (HIGH-1 v3 사이드카 박제) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | BodyNormalizationProfile confidence 값이 [0, 1] 범위 벗어남 | mitigate | dataclass `__post_init__` validator 박제 (Phase 1 산출). 측정기 출력 `min(1.0, ...)` 강제. NaN → 0.0 폴백 |
| T-02-02 | Repudiation / 법적 | 운영 코드 (sunity_shared + backend/functions + backend/runpod_inference) 가 backend.research 또는 SMPL-X / NLF 모듈 import (라이선스 위반) | mitigate | Task 6 AST 단위테스트 자동 차단 (HIGH-3 narrowed — mediapipe 제외, **MEDIUM-1 v3 — 3 디렉터리 + importlib 동적 차단 박제**). CI gate |
| T-02-03 | Denial of Service | 인버트/짧은 영상에서 NaN profile 출력 → 다운스트림 (Phase 6) crash | mitigate | `insufficient_frames` warning + confidence=0 + scale=1.0 폴백. `__post_init__` 가 finite 강제. Task 3 `test_short_frames_graceful_degrade` 검증 |
| T-02-04 | Information Disclosure | R&D 보고서 (`body_profile_gap_*.json`) 에 사용자 영상 메타데이터 leak | accept | belle Pod 환경 (내부) 만 실행. 보고서는 `backend/research/evaluations/reports/` (private repo). 외부 노출 path 없음 |
| T-02-05 | Tampering | `_RTMWNlfCompat.estimate` 시그너처 변경이 B8 박제 (`TestB8FixSignature`) 위반 | mitigate | **HIGH-1 v3 박제 — Option D 사이드카**: 시그너처 무변경 (-> np.ndarray). instance attribute `last_body_profile` 박제. Task 4 의 `test_b8_signature_lock_preserved` + 본 task verify 가 `pytest tests/test_pipeline_recognizer_switch.py::TestB8FixSignature -x` 실행 |
| T-02-06 | Information Disclosure | Firestore `bodyNormalizationProfile` 필드가 nested array 위반 | mitigate | warnings = flat string list (`firestore-nested-array-flat` memory). Task 4 `test_complete_analysis_persists_body_profile_for_phase6` 가 payload shape 검증 |
| T-02-07 | Tampering | `_RTMWNlfCompat.last_body_profile` 이 sequential `estimate()` 호출 간 leak (이전 video 의 profile 이 다음 video 의 Firestore 에 박제) | mitigate | **HIGH-1 v3 박제** — Task 4 `test_rtmwnlfcompat_no_profile_leak_between_estimates` 가 두 번째 호출 시 attribute overwrite 검증. Lambda 는 single-invocation per container — 동시성 leak 0 |

> 본 phase 는 외부 입력 X (Lambda HTTP API 없음, 모두 Pod/Lambda 내부 함수). V2/V3/V4/V6 적용 X.
</threat_model>

<verification>
## Phase-level 검증 (전 task 완료 후)

1. **단위 테스트 전체**: `cd backend && pytest tests/ -x` — 회귀 0.
2. **3-way lockstep 자동 검증** (L1 박제 포함): `pytest tests/test_body_normalization_lockstep.py -x` PASS (7 함수 — 기존 5 + L1 신설 2).
3. **Fixture smoke load (HIGH-2 v1→v2 + v3)**: `pytest tests/fixtures/body_normalization/test_fixtures_loadable.py -x` PASS.
4. **Measurer 5 fixture + HIGH-2 v3**: `pytest tests/test_body_normalization_measurer.py -x` PASS (11 case — 기존 8 + HIGH-2 v3 3).
5. **Pipeline 통합 + Phase-6 boundary (HIGH-1 v3 사이드카 + MEDIUM-2 v3 contract.md)**: `pytest tests/test_pipeline_body_profile_injection.py -x` PASS (9 case).
6. **B8 박제 보존 (HIGH-1 v3 핵심)**: `pytest tests/test_pipeline_recognizer_switch.py::TestB8FixSignature -x -v` PASS — 2 테스트 모두 통과.
7. **R&D 격리 (HIGH-3 v1→v2 narrowed + MEDIUM-1 v3 확장)**: `pytest tests/test_research_import_isolation.py -x` PASS (4 case — sunity_shared + functions + runpod_inference + importlib).
8. **R&D harness smoke (HIGH-3 v3 graceful-empty + HIGH-4 v3 pre-step + LOW-1 v3)**: `pytest tests/test_compare_body_profile_smoke.py -x` PASS (7 case).
9. **HIGH-4 v3 게이트**: `ls backend/research/evaluations/reports/sweep_rtmw_*/keypoints/*.json 2>/dev/null | wc -l` ≥ 5 (Step (a) 산출) + `ls backend/research/evaluations/reports/body_profile_gap_*.json 2>/dev/null | wc -l` ≥ 1 (Step (c) 산출, 또는 v1.5 defer 박제).
10. **REQUIREMENTS.md 정합**: BODY-01 텍스트 grep — "RTMW" 등장 + "MediaPipe" 미등장.
11. **의존성 검증**: `grep -r "import scipy" backend/shared/python/sunity_shared/analysis/body_normalization_measurer.py` → 0 (numpy only).
12. **Propagation 검증 (4-way lockstep)**: `grep -n "bodyNormalizationProfile\|body_normalization_profile" backend/functions/pipeline/app.py backend/shared/python/sunity_shared/firestore_admin.py app/src/types/analysis.ts docs/contract.md | wc -l` ≥ 4 (HIGH-1 v3 + MEDIUM-2 v3 박제).
13. **사이드카 박제 (HIGH-1 v3)**: `grep -n "last_body_profile" backend/functions/pipeline/app.py | wc -l` ≥ 3 (init + write + caller read).
14. **HIGH-3 박제 (v1→v2 유지)**: `grep -c "mediapipe" backend/tests/test_research_import_isolation.py` ≥ 1 (TODO 코멘트).
15. **LOW-1 v3 박제**: Task 5a verify 의 `python -m backend.research.evaluations.compare_body_profile --help` 가 repo root 에서 실행 (cd backend X).
</verification>

<success_criteria>
## ROADMAP Success Criteria 매핑 (v3 박제)

| Criterion | Task | 검증 | 본 phase closure 가능? |
|-----------|------|------|--|
| 1. RTMW 키포인트에서 segment 길이가 시간 평균으로 안정적으로 추출 (jitter 스무딩) | Task 3 (MAD smoothing) | `test_normal_pose_profile_valid` | 예 |
| 2. BodyNormalizationProfile 7필드 자동 산출 | Task 3 | `test_profile_validator_passes` | 예 |
| 3. 낮은 confidence 시 warnings 표기 (단정 X) | Task 3 (5종 warning + HIGH-2 v3 박제) | `test_inverted_pose_warning`, `test_occluded_leg_warning`, `test_short_clip_warning`, `test_asymmetric_warning`, `test_inverted_warning_uses_y_down_pole_axis_convention`, `test_inverted_warning_via_rtmw_adapter_path` | 예 |
| 4. R&D 비교군 NLF→SMPL-X β 갭 보고서 (제품 코드 비호출) | Task 5a (scaffold + extract helper) **+ Task 5b (belle Pod Step (a)+(b)+(c) 실 보고서 체크인)** | scaffold smoke + 실 보고서 ls 검증 (5 keypoint dump + 1 gap report) | **Task 5b 완료 시에만 PASS — HIGH-4 v3 게이트** |
| 5. 데이터 계약 4-way lockstep (MEDIUM-2 v3) | Task 1 + Task 4 | `test_body_normalization_lockstep` (확장 7 함수) + `test_contract_md_documents_body_normalization_profile_field` | 예 |

**v3 Done (executor scope)**: criterion 1, 2, 3, 5 PASS + criterion 4 의 5a scaffold + extract helper PASS + Phase 1 회귀 0 + Pipeline 통합 verified (B8 박제 보존) + REQUIREMENTS.md BODY-01 RTMW 정합.

**ROADMAP §4 closure**: Task 5b 완료 (belle Pod Step (a) RTMW keypoint dump + Step (b) NLF→SMPL-X β + Step (c) git commit) 시. autonomous=false 박제 — verifier 가 자동 close 금지.
</success_criteria>

<source_audit>
## Multi-Source Coverage Audit (v3 박제 — Codex 2차 review 정합)

### GOAL (ROADMAP)
| Item | Plan | Task |
|------|------|------|
| RTMW 키포인트 → BodyNormalizationProfile 자동 출력 | 02-01 | Task 3 |
| 두 엔진 (체형 보정 + 힘 패턴) 의 공유 입력 | 02-01 | Task 4 (pipeline 사이드카 propagation + Firestore 저장 — HIGH-1 v3) |
| SMPL-X β R&D 비교군만 | 02-01 | Task 5a (scaffold + extract helper), Task 5b (실 실행), Task 6 (import 차단 — MEDIUM-1 v3 확장) |

### REQ (REQUIREMENTS.md)
| Req ID | Plan | Task |
|--------|------|------|
| BODY-01 | 02-01 | Task 3 (측정기) + Task 4 (사이드카 propagation + Firestore + MEDIUM-2 v3 contract.md) + Task 6 (텍스트 RTMW 정합) |

### RESEARCH (02-RESEARCH.md)
| Feature | Plan | Task |
|---------|------|------|
| body_normalization_measurer.py (순수 numpy) | 02-01 | Task 3 |
| MAD outlier + robust median smoothing | 02-01 | Task 3 |
| torso self-reference normalize | 02-01 | Task 3 |
| 5종 warnings enum | 02-01 | Task 1 (3-way + L1 박제) + Task 3 (판정 + HIGH-2 v3 박제) |
| 5 synthetic fixture (seed=42, RTMW adapter 경유) | 02-01 | Task 2 (HIGH-2 v1→v2 schema + v3 convention 정합) |
| `_RTMWNlfCompat` body_shape 주입 + propagation | 02-01 | Task 4 (HIGH-1 v3 사이드카 + Firestore) |
| R&D 비교 harness compare_body_profile.py | 02-01 | Task 5a (scaffold + extract helper), Task 5b (실 실행) |
| AST import isolation (HIGH-3 v1→v2 narrowed + MEDIUM-1 v3 확장) | 02-01 | Task 6 |
| Phase 1 sweep keypoint dump 재추출 (M1) | DEFERRED to v1.5 | Task 2 TODO 박제 + Task 2b sanity skip |
| Firestore 저장 (HIGH-1 v3 사이드카 fix 의 일부) | 02-01 (v1 scope 갱신) | Task 4 — Phase 6 boundary 박제 |
| contract.md AnalysisDoc bodyNormalizationProfile 박제 (MEDIUM-2 v3) | 02-01 | Task 4 (4-way lockstep) |
| extract_rtmw_body_profile_keypoints.py helper (HIGH-4 v3) | 02-01 | Task 5a (scaffold), Task 5b (Pod 실행) |

### CONTEXT (02-CONTEXT.md Locked Decisions)
| Decision ID | Plan | Task |
|-------------|------|------|
| D-02-01 (RTMW 운영 백본, MediaPipe X) | 02-01 | Task 3 (RTMW 만 측정), Task 6 (REQUIREMENTS RTMW 갱신) |
| D-02-02 (3-way lockstep) | 02-01 | Task 1 (단일 atomic commit + L1 AST 박제) + Task 4 (4-way 확장 — MEDIUM-2 v3) |
| D-02-03 (confidence/warnings 항상 출력) | 02-01 | Task 3 (warnings 판정 + 폴백) |
| D-02-04 (SMPL-X R&D 만) | 02-01 | Task 5a (R&D 격리), Task 5b (실 실행 — Pod), Task 6 (import 차단 + MEDIUM-1 v3 확장) |
| D-02-05 (자동 측정 우선) | 02-01 | Task 3 (자동 측정기 본체) |
| D-02-06 (실 영상 BodyProfile 만 v1) | 02-01 | Task 2 (synthetic fixture), Task 2b (실 RTMW sanity skip) |

### REVIEWS (02-REVIEWS.md — Codex 2차)
| Concern | Severity | v3 Task | Notes |
|---|---|---|---|
| **HIGH-1 v3**: tuple return 이 B8 박제 위반 | HIGH | Task 4 (Option D 사이드카) | `_RTMWNlfCompat.last_body_profile` instance attribute. B8 박제 보존 + Firestore 저장 |
| **HIGH-2 v3**: pose_too_inverted sign 이 image y-down 에서 반전 | HIGH | Task 2 + Task 3 (RTMW adapter 경유 fixture + image y-down convention 박제) | mid_hip - mid_shoulder 의 pole_axis projection 기준 |
| **HIGH-3 v3**: Task 5a/5b NotImplementedError lifecycle 충돌 | HIGH | Task 5a (graceful-empty 패턴) | load_nlf_smplx_keypoints 가 비존재 dir → [] |
| **HIGH-4 v3**: 5b 가 RTMW keypoint dump 부재로 실행 불가 | HIGH | Task 5a (extract helper 신설), Task 5b (Step (a) pre-step 박제) | extract_rtmw_body_profile_keypoints.py |
| **MEDIUM-1 v3**: AST 스캔 sunity_shared 만 | MED | Task 6 (스캔 확장 + importlib) | 3 디렉터리 + 동적 import 박제 |
| **MEDIUM-2 v3**: contract.md AnalysisDoc 갱신 누락 | MED | Task 4 (4-way lockstep) | 단일 atomic commit |
| **MEDIUM-3 v3**: Task 6 dependency chain ordering | MED | Task 6 (depends-on Task 5a only) | Task 5b 평행 진행 |
| **LOW-1 v3**: verify 경로 fragile (cd backend) | LOW | Task 5a + 5b verify/dashboard | repo root 실행 표준화 |
| HIGH-2 v1→v2: fixture API/schema 환상 | HIGH | Task 1 + Task 2 (factory helper + 실 dataclass 정합) | PoseFrame.from_dict 부재 박제 |
| HIGH-3 v1→v2: import isolation legacy code 충돌 | HIGH | Task 6 (mediapipe 제외, TODO 박제) | mediapipe 별 phase |
| HIGH-4 v1→v2: scaffold ≠ gap report | HIGH | Task 5a + 5b 분할 (user_setup 게이트) | autonomous=false 박제 |
| M1: synthetic-only 한계 | MED | Task 2b (RTMW sanity skip) | Pod 환경에서만 실행 |
| M3: scale 의미 미명세 | MED | Task 1 (3-way 박제 "torso-relative proportion heuristic") | 3 파일 동일 문구 |
| L1: lockstep grep-only | LOW | Task 1 (lockstep AST 자동 검증 확장) | 5 enum 자동 검증 |

### Out of Scope (CONTEXT §1)
- Phase 3 자가입력 → Phase 3
- Phase 6 다운스트림 활용 → Phase 6
- 좌우 비대칭 활용 → Phase 7 (v1 은 warning 만)
- 다각도 입력 → Phase 4
- SMPL-X 본격 도입 → 영구 R&D
- mediapipe quarantine → 별도 phase ([[rtmw-free-stack-pivot]] 정합)

### Gaps Found (v3 박제)

각 v3 신규 concern → v3 task 매핑은 위 v2→v3 Replan Changelog 표 + 본 REVIEWS 행에 명시. **모든 v3 concern (4 HIGH + 3 MEDIUM + 1 LOW) 이 task 에 매핑됨** — silent omission 0.

**HIGH-4 v1→v2 명시 박제** (유지): ROADMAP success criterion 4 의 실 closure 는 **Task 5b 가 user_setup 게이트** — executor 자동화 불가. belle Pod 실 실행 + (a) RTMW dump + (b) NLF→SMPL-X β + (c) `reports/body_profile_gap_<date>.json` 체크인 완료 시까지 ROADMAP §4 PASS 표시 X. 본 plan 의 `autonomous: false` 박제 정합.

이외 GOAL/REQ/RESEARCH/CONTEXT 모든 항목이 task 에 매핑됨.

**HIGH-2 v1→v2 박제 위치** (유지, Codex 정정): 본 plan 은 `PoseFrame.from_dict` 가 **코드베이스에 없음** 을 인정 — 따라서 test factory helper (`pose_frame_from_dict` in `backend/tests/fixtures/body_normalization/_factory.py`) 박제로 우회. Production 코드 수정 X.

**HIGH-1 v3 박제 위치** (신규): `backend/tests/test_pipeline_recognizer_switch.py:225-238` 의 B8 시그너처 lock 을 위반하지 않는 mechanism (Option D 사이드카 — `_RTMWNlfCompat.last_body_profile` instance attribute) 으로 propagation. tuple return X.
</source_audit>

<output>
Phase 2 가 완료되면 다음 산출:

1. `backend/shared/python/sunity_shared/analysis/body_normalization_measurer.py` — 순수 함수 측정기 + HIGH-2 v3 image y-down PoleAxis-relative inversion (Phase 6/7/8/9 의 공유 입력)
2. `backend/tests/fixtures/body_normalization/_factory.py` — `pose_frame_from_dict` test helper (HIGH-2 v1→v2 — PoseFrame.from_dict 부재 우회)
3. `backend/tests/fixtures/body_normalization/_generate.py` + 5 fixture JSON — HIGH-2 v3 schema 정합 + RTMW adapter 경유 생성 (image y-down convention 박제)
4. `backend/tests/fixtures/body_normalization/test_fixtures_loadable.py` — smoke load + y-down convention guard (HIGH-2 v3)
5. `backend/tests/test_body_normalization_measurer.py` — 11개 단위 테스트 (HIGH-2 v3 박제 3개 포함)
6. `backend/tests/test_pipeline_body_profile_injection.py` — 9개 통합 테스트 (HIGH-1 v3 사이드카 + MEDIUM-2 v3 contract.md + Phase-6 boundary)
7. `backend/tests/test_research_import_isolation.py` — AST 자동 차단 4개 테스트 (MEDIUM-1 v3 — 3 디렉터리 + importlib 동적)
8. `backend/research/evaluations/compare_body_profile.py` — R&D 비교 harness scaffold (HIGH-3 v3 graceful-empty)
9. `backend/research/evaluations/extract_rtmw_body_profile_keypoints.py` — Task 5b pre-step helper (HIGH-4 v3)
10. `backend/tests/test_compare_body_profile_smoke.py` — 7개 smoke (LOW-1 v3 repo root + HIGH-3 v3 graceful + HIGH-4 v3 extract)
11. `backend/research/evaluations/reports/sweep_rtmw_<date>/keypoints/<video>.json` × 5 — belle Pod Step (a) 산출 (HIGH-4 v3 user_setup pre-step)
12. `backend/research/evaluations/reports/body_profile_gap_<date>.{json,md}` — belle Pod Step (c) 산출 (HIGH-4 v3 user_setup closure)
13. 수정: `docs/contract.md` §3 AnalysisDoc + §7 warnings 5 enum + M3 "torso-relative proportion heuristic" + `app/src/types/analysis.ts` 주석 + `body_normalization.py` docstring + `firestore_admin.py` payload + `pipeline/app.py` complete_analysis kw (Task 1 의 3-way lockstep + Task 4 의 4-way lockstep — MEDIUM-2 v3)
14. 수정: `backend/functions/pipeline/app.py` `_RTMWNlfCompat.last_body_profile` 사이드카 + `_process` complete_analysis kw (HIGH-1 v3, B8 박제 보존)
15. 수정: `backend/shared/python/sunity_shared/firestore_admin.py` `complete_analysis(body_normalization_profile=...)` kw 박제 + `bodyNormalizationProfile` Firestore 필드 (HIGH-1 v3)
16. 수정: `app/src/types/analysis.ts` `AnalysisDoc.bodyNormalizationProfile?` optional (HIGH-1 v3 propagation lockstep)
17. 수정: `.planning/REQUIREMENTS.md` BODY-01 "MediaPipe" → "RTMW"

완료 시 `.planning/phases/02-bodynormalizationprofile-rtmw-segment/02-01-SUMMARY.md` 생성.

다음 Phase: Phase 3 (자가입력 BodyProfileInput) — BODY-01 완료 후 unblock.
</output>

---

## Total Atomic Commits 요약 (v3 — Task 5 split + MEDIUM-3 v3 reorder 박제)

| # | Task | Commit Template | Files Changed | Type | Concerns fixed |
|---|------|----------------|----------------|------|---|
| 1 | contract.md §7 warnings enum 5종 + M3 의미 + L1 lockstep AST | `feat(02): lock warnings enum 5-way + scale semantics for BodyNormalizationProfile (3-way lockstep + AST gate)` | 4 files | feat | HIGH-2 v1→v2 (enum scope) / M3 / L1 |
| 2 | PoseFrame factory helper + Synthetic fixture (RTMW adapter 경유) + smoke + RTMW sanity skip | `test(02): add PoseFrame factory helper + RTMW-adapter-derived synthetic fixtures (5 cases) + y-down convention smoke (HIGH-2 v3 schema + convention, M1 fallback)` | 9 files (factory + 5 JSON + generator + 2 test) | test | HIGH-2 v1→v2 + v3 / M1 |
| 3 | body_normalization_measurer.py 본체 + 단위 테스트 + HIGH-2 v3 박제 | `feat(02): add measure_body_profile RTMW segment measurer with MAD smoothing + image-y-down PoleAxis inversion (BODY-01, HIGH-2 v3 fix)` | 2 files | feat | HIGH-2 v3 (convention) |
| 4 | Pipeline 통합 — 사이드카 last_body_profile + Firestore 저장 + AnalysisDoc + contract.md 4-way lockstep | `feat(02): propagate BodyNormalizationProfile via _RTMWNlfCompat.last_body_profile sidecar + complete_analysis kw (HIGH-1 v3 Option D — B8 lock preserved, MEDIUM-2 v3 4-way lockstep)` | 4 files (pipeline + firestore_admin + analysis.ts + contract.md) | feat | **HIGH-1 v3** + MEDIUM-2 v3 |
| 5a | R&D 비교 harness scaffold + extract helper + graceful-empty smoke | `chore(02): scaffold R&D compare_body_profile + extract_rtmw_body_profile_keypoints with graceful-empty NLF loader (HIGH-3 v3 + HIGH-4 v3 + LOW-1 v3)` | 3 files (compare + extract + smoke) | chore | HIGH-3 v3 + HIGH-4 v3 + LOW-1 v3 |
| 6 | AST import isolation 확장 (3 디렉터리 + importlib) + REQUIREMENTS BODY-01 RTMW | `chore(02): expand AST R&D import isolation to functions+runpod_inference+importlib (MEDIUM-1 v3, mediapipe excluded — HIGH-3 v2) + align BODY-01 to RTMW` | 2 files | chore | MEDIUM-1 v3 + MEDIUM-3 v3 + HIGH-3 v1→v2 |
| 5b | belle Pod (a) RTMW dump + (b) NLF→SMPL-X β + (c) body_profile_gap_<date>.json 체크인 | `data(02): add RTMW keypoint dumps + body_profile_gap_<date>.{json,md} from belle Pod 5-video run (HIGH-4 v3 ROADMAP §4 closure)` | 7 files (5 keypoint dump + json + md) | data | **HIGH-4 v3 게이트** |

**총 7개 atomic commit** (Task 5 분할 = +1).

**의존 chain (MEDIUM-3 v3 박제 — Task 6 가 Task 5a 만 의존, Task 5b 평행)**:
```
Task 1 + Task 2 (병렬, Wave 1)
  → Task 3 (Wave 2)
    → Task 4 (Wave 3, 사이드카 propagation) + Task 5a (Wave 3, R&D scaffold) [병렬]
      → Task 6 (Wave 4, AST + REQUIREMENTS — Task 5a 만 의존)  ∥  Task 5b (Wave 4, belle Pod user_setup blocking, ROADMAP §4 closure 게이트)
```

**Task 6 가 Task 5b 무관**: belle Pod 게이트가 며칠 걸려도 Task 6 (product-safe AST cleanup + REQUIREMENTS RTMW 정합) 은 평행 landing 가능 (MEDIUM-3 v3 박제).

**autonomous: false** 박제 (Task 5b 가 user_setup checkpoint).
