---
phase: 03-bodyprofileinput
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/types/analysis.ts
  - backend/shared/python/sunity_shared/models.py
  - docs/contract.md
  - backend/functions/pipeline/app.py
  - app/src/lib/bodyProfile.ts
  - app/src/lib/userAnalyses.ts
  - app/src/app/analysis/loading.tsx
  - backend/tests/test_body_profile.py
autonomous: true
requirements: [BODY-02]

must_haves:
  truths:
    - "BodyProfile 타입이 TS·Python·contract.md 세 곳에 동일 필드로 존재한다 (3-way lockstep)"
    - "미입력/부분/잘못된 bodyProfile 에서도 pipeline 이 crash 없이 graceful 진행한다 (SC#4)"
    - "bodyProfile 이 분석 doc 에 snapshot 되어 pipeline meta 로 읽히고 coach context 에 전달된다 (thin E2E)"
    - "snapshot 된 bodyProfile 이 AnalysisDoc 에 보존되어 결과 화면이 분석-당시 값을 읽을 수 있다 (R1 재현성)"
    - "loading.tsx 가 raw getDoc spread 가 아니라 getBodyProfileOnce() (client normalize) 로 snapshot 한다 (R5)"
    - "weightKg 가 scoring/analysis consumer 모듈에 유입되지 않는다 (D-05, R4 확장 gate)"
  artifacts:
    - path: "app/src/types/analysis.ts"
      provides: "BodyProfile interface + ExperienceLevel/DominantHand/PainArea unions + AnalysisDoc.bodyProfile 필드"
      contains: "interface BodyProfile"
    - path: "backend/shared/python/sunity_shared/models.py"
      provides: "EXPERIENCE_LEVELS/DOMINANT_HANDS/PAIN_AREAS 상수 + normalize_body_profile"
      contains: "def normalize_body_profile"
    - path: "app/src/lib/bodyProfile.ts"
      provides: "useBodyProfile() hook + saveBodyProfile() + normalizeBodyProfile() export + getBodyProfileOnce()"
      contains: "export function normalizeBodyProfile"
    - path: "app/src/lib/userAnalyses.ts"
      provides: "normalize() 가 raw.bodyProfile 을 normalizeBodyProfile 로 보존"
      contains: "normalizeBodyProfile"
    - path: "backend/tests/test_body_profile.py"
      provides: "normalize_body_profile + coach-context graceful 검증"
      contains: "def test_"
  key_links:
    - from: "app/src/app/analysis/loading.tsx"
      to: "users/{uid}/analyses/{id}.bodyProfile"
      via: "getBodyProfileOnce() → setDoc spread (referenceMotionId 옆)"
      pattern: "getBodyProfileOnce"
    - from: "app/src/lib/userAnalyses.ts normalize"
      to: "AnalysisDoc.bodyProfile"
      via: "normalizeBodyProfile(raw.bodyProfile)"
      pattern: "normalizeBodyProfile"
    - from: "backend/functions/pipeline/app.py _process"
      to: "_build_coach_context body_profile kwarg"
      via: "normalize_body_profile(meta.get('bodyProfile'))"
      pattern: "body_profile"
---

<objective>
Phase 3 의 thin end-to-end 수직 슬라이스 + 데이터 계약 토대. BodyProfile 타입을 TS·Python·contract.md 3-way lockstep 으로 신설하고 `AnalysisDoc` 에 `bodyProfile?: BodyProfile | null` snapshot 필드를 추가한다. 게스트 프로필을 `users/{uid}.bodyProfile` 에 읽고/쓰는 데이터소스 hook(`bodyProfile.ts`)을 만들고, 분석 시작 시점(`loading.tsx`)에 `getBodyProfileOnce()` (client normalize)로 프로필을 analysis doc 에 snapshot 한다. `userAnalyses.ts` normalize 가 `raw.bodyProfile` 을 같은 normalizer 로 보존하여 결과 화면이 snapshot 을 읽을 수 있게 하고, pipeline 이 그 값을 meta 로 읽어 `_build_coach_context` 의 `bodyProfile` 키(D-04 seam)로 전달한다. 이로써 "1개 필드라도 input→Firestore→snapshot→AnalysisDoc→pipeline→coach context 까지 관통"하는 동작하는 thin slice 가 완성된다.

Purpose: 이후 UI 슬라이스(03-02 폼, 03-03 권유 모달/결과 표기)가 올라설 계약·저장·snapshot-보존 경로를 먼저 박제한다. 새 HTTP endpoint·validation.py·upload-url Lambda·Firestore rules 변경 없음 (RESEARCH §Don't-Hand-Roll — referenceMotionId 와 동일 free-read 메커니즘 재사용). 결과 화면 재현성(R1): live profile 이 아니라 analysis snapshot 이 source-of-truth.
Output: 3-way 계약 + AnalysisDoc.bodyProfile + `bodyProfile.ts` hook/normalizer/getBodyProfileOnce + userAnalyses snapshot 보존 + loading.tsx snapshot + pipeline coach seam + Wave 0 backend 테스트.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-bodyprofileinput/03-CONTEXT.md
@.planning/phases/03-bodyprofileinput/03-RESEARCH.md
@.planning/phases/03-bodyprofileinput/03-PATTERNS.md
@CLAUDE.md
@app/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — failing backend test + 3-way BodyProfile 계약 lockstep + AnalysisDoc.bodyProfile</name>
  <files>backend/tests/test_body_profile.py, app/src/types/analysis.ts, backend/shared/python/sunity_shared/models.py, docs/contract.md</files>
  <read_first>
    - app/src/types/analysis.ts (lockstep #1 — 기존 union `AnalysisMode`/`ScoreDimension` + `BodyNormalizationProfile` interface + 헤더 co-edit 명령 줄 1-4 패턴 + `interface AnalysisDoc` 286-301 — bodyProfile 추가 위치)
    - backend/shared/python/sunity_shared/models.py (lockstep #2 — tuple 상수 `MODES`/`SCORE_DIMENSIONS` + frozenset `SYNTHESIS_WARNING_CODES` 패턴)
    - docs/contract.md §7 BodyNormalizationProfile (line 331-385 — lockstep #3 섹션 형식 analog)
    - 03-RESEARCH.md §"Data Contract — 3-Way Lockstep" (권장 BodyProfile shape 리터럴) + §Pitfall 1/7
    - 03-PATTERNS.md §analysis.ts / §models.py / §contract.md 항목
    - backend/shared/python/sunity_shared/validation.py:36-86 (per-field 방어 pure-function analog — normalize_body_profile 는 raise 대신 None 반환)
  </read_first>
  <action>
    TDD RED 먼저: `backend/tests/test_body_profile.py` 신설 — `normalize_body_profile` (models.py) 가 (a) None/빈dict → None, (b) 부분 dict (heightCm 만) → 나머지 None, (c) 잘못된 enum 값 (experience="pro") → None, (d) painAreas 안 비-string 요소 filter, (e) 알 수 없는 키 무시, (f) heightCm/weightKg 범위 밖(예: 5, 9999) → None 을 검증하는 테스트 작성. 이 시점엔 normalize_body_profile 미존재 → 테스트 RED (import error 허용).
    그 다음 3-way 계약을 단일 atomic 으로 신설:
    (1) analysis.ts — `ExperienceLevel = 'beginner'|'intermediate'|'advanced'`, `DominantHand = 'left'|'right'|'both'`, `PainArea = 'shoulder'|'wrist'|'lower_back'|'knee'|'ankle'|'neck'|'hip'|'elbow'` (RESEARCH A1 + 폴스포츠-지식.md 고하중 관절 정합) union + `interface BodyProfile { heightCm:number|null; weightKg:number|null; experience:ExperienceLevel|null; painAreas:PainArea[]; dominantHand:DominantHand|null; updatedAt?:number; promptDismissedAt?:number }`. heightCm/weightKg/experience/dominantHand 모두 nullable, painAreas 는 flat scalar array (Firestore nested-array 안전, Pitfall 7). weightKg 필드에 "보조 ONLY — 분석 단정·점수 가중 금지 (D-05)" 주석. lockstep 헤더 주석으로 models.py + contract.md mirror 명시.
    (1b) [R1] 같은 atomic 에서 `interface AnalysisDoc` (286-301) 에 `bodyProfile?: BodyProfile | null;` 필드 추가. 주석: "분석-당시 자가입력 SNAPSHOT (live `users/{uid}.bodyProfile` 아님 — 결과 화면 재현성, R1). 03-03 result.tsx 가 이 snapshot 을 우선 표시. loading.tsx 가 getBodyProfileOnce() 로 기록." painAreas 는 top-level scalar array 라 AnalysisDoc 안에서도 nested-array 위반 없음 (Pitfall 7).
    (2) models.py — `EXPERIENCE_LEVELS=("beginner","intermediate","advanced")`, `DOMINANT_HANDS=("left","right","both")`, `PAIN_AREAS=("shoulder","wrist","lower_back","knee","ankle","neck","hip","elbow")` 상수 + doc-shape 주석 + `normalize_body_profile(meta_value: dict | None) -> dict | None` 방어 helper (per-field: get→type/enum/range check→정상값 또는 None; painAreas 는 PAIN_AREAS 멤버인 string 만 남김; heightCm 90-250 / weightKg 25-200 범위 밖 → None; 전 필드 None/빈이면 전체 None 반환). raise 안 함 (D-06 graceful, owner-write client 값이라 HTTP validator 아님).
    (3) docs/contract.md — §7 근처에 "BodyProfile (자가입력)" 섹션: 필드표 + 저장 위치(`users/{uid}.bodyProfile` 라이브 + per-analysis SNAPSHOT `users/{uid}/analyses/{id}.bodyProfile` — 결과 화면은 snapshot 을 source-of-truth 로 읽음, R1) + D-05 weightKg 보조-only 제약 + SC#4 graceful-missing.
    GREEN: normalize_body_profile 구현 후 test PASS.
    validation.py·upload-url Lambda·firestore.rules 는 손대지 않는다 (Anti-pattern: UploadUrlRequest 경유 금지).
  </action>
  <behavior>
    - normalize_body_profile(None) → None
    - normalize_body_profile({}) → None (전 필드 빈 → 전체 None)
    - normalize_body_profile({"heightCm":165}) → {"heightCm":165, 나머지 None/[]}
    - normalize_body_profile({"experience":"pro"}) → experience None (알 수 없는 enum)
    - normalize_body_profile({"painAreas":["shoulder",3,"wrist","xxx"]}) → painAreas ["shoulder","wrist"] (비-string·비-멤버 제거)
    - normalize_body_profile({"weightKg":9999}) → weightKg None (범위 밖)
  </behavior>
  <verify>
    <automated>cd backend && python -m pytest tests/test_body_profile.py -x -q && cd ../app && npm run typecheck</automated>
    <automated>grep -l "BodyProfile" app/src/types/analysis.ts && grep -q "bodyProfile?: BodyProfile" app/src/types/analysis.ts && grep -l "PAIN_AREAS" backend/shared/python/sunity_shared/models.py && grep -l "BodyProfile" docs/contract.md</automated>
  </verify>
  <done>test_body_profile.py GREEN, typecheck clean, BodyProfile/PAIN_AREAS 가 analysis.ts·models.py·contract.md 세 곳에 모두 존재 (3-way parity), AnalysisDoc 에 `bodyProfile?: BodyProfile | null` 필드 존재 (R1).</done>
</task>

<task type="auto">
  <name>Task 2: bodyProfile.ts 데이터소스 hook (normalizeBodyProfile export + useBodyProfile + getBodyProfileOnce + saveBodyProfile) + userAnalyses snapshot 보존</name>
  <files>app/src/lib/bodyProfile.ts, app/src/lib/userAnalyses.ts</files>
  <read_first>
    - app/src/lib/userAnalyses.ts:1-19 (헤더/격리 주석 + import), :27-42 (normalize per-field 방어), :218-227 (normalize return 객체 — bodyProfile 추가 위치), :231-273 (useAnalysisDoc 단일 doc onSnapshot), :239-242/:283-286 (onAuthStateChanged cold-start race guard — 게스트 uid mount 시 미준비 가능)
    - app/src/lib/firebase.ts (auth, db export)
    - app/src/types/analysis.ts (Task 1 신설 BodyProfile/union + AnalysisDoc.bodyProfile)
    - 03-RESEARCH.md §"Pattern 1: Data-source hook with defensive normalize" + §"Pattern 2"
    - 03-PATTERNS.md §bodyProfile.ts 항목 (saveBodyProfile merge-write 패턴 명시)
  </read_first>
  <action>
    (1) `app/src/lib/bodyProfile.ts` 신설. import 에 `setDoc`/`getDoc`/`doc`/`onSnapshot` (firebase/firestore) + `onAuthStateChanged` (firebase/auth) + `{ auth, db }` 포함.
    (a) [R1/R5] `export function normalizeBodyProfile(raw: Record<string,unknown> | undefined | null): BodyProfile | null` — 각 필드 독립 검증, 잘못된 타입 → null, experience/dominantHand 는 union 멤버만, painAreas 는 PainArea 멤버 string 만 filter, heightCm 90-250 / weightKg 25-200 범위 밖 → null, 부분/없음 graceful (D-06). **전 필드 None/빈이면 전체 null 반환** ("all-empty → omit snapshot" 규칙을 helper 안에 둠, R5). 절대 throw 안 함. 이 함수는 userAnalyses.ts 와 getBodyProfileOnce 가 공유하는 단일 normalizer.
    (b) `export function useBodyProfile()` — `useAnalysisDoc` 단일 doc 구독 형태를 `doc(db,'users',uid)` 에 적용 (analysisId 파라미터 없음), `normalizeBodyProfile(snap.data()?.bodyProfile)` 로 정규화, `{ profile, loading }` 반환, onAuthStateChanged race guard 필수. (profile-tab 라이브 read / old-doc fallback 전용 — 결과 화면 기본 소스 아님, R1.)
    (c) [R5] `export async function getBodyProfileOnce(): Promise<BodyProfile | null>` — one-shot `getDoc(doc(db,'users',uid))` → `normalizeBodyProfile(snap.data()?.bodyProfile)` 반환. uid 없으면 null. loading.tsx snapshot 전용 (raw spread 금지 강제). all-empty → null (normalizeBodyProfile 규칙 그대로) 이라 snapshot 생략 신호.
    (d) `export async function saveBodyProfile(partial: Partial<BodyProfile>)` — `setDoc(doc(db,'users',uid), { bodyProfile: { ...partial, updatedAt: Date.now() } }, { merge: true })` (부분 입력 merge, D-06).
    (e) `export async function dismissBodyProfilePrompt()` — `setDoc(...,{ bodyProfile: { promptDismissedAt: Date.now() } },{ merge:true })` (03-03 once-flag 용). 화면은 Firestore 직접 접근 금지 — 모두 이 hook/helper 경유.
    (2) [R1] `app/src/lib/userAnalyses.ts` normalize(27-227) 의 return 객체(218-227)에 `bodyProfile: normalizeBodyProfile(raw.bodyProfile as Record<string,unknown> | undefined)` 추가. userAnalyses.ts 에서 `import { normalizeBodyProfile } from './bodyProfile'`. 순환 import 주의: bodyProfile.ts 는 userAnalyses.ts 를 import 하지 않아야 함 (useBodyProfile 은 useAnalysisDoc 을 재사용하지 말고 onSnapshot 을 직접 구현 — 03-RESEARCH Pattern 1 형태 복제). 이로써 useAnalysisDoc().doc.bodyProfile 이 snapshot 을 보존 → 결과 화면이 storedDoc?.bodyProfile 로 읽음.
  </action>
  <verify>
    <automated>cd app && npm run typecheck</automated>
    <automated>grep -q "export function normalizeBodyProfile" app/src/lib/bodyProfile.ts && grep -q "export function useBodyProfile" app/src/lib/bodyProfile.ts && grep -q "getBodyProfileOnce" app/src/lib/bodyProfile.ts && grep -q "saveBodyProfile" app/src/lib/bodyProfile.ts && grep -q "merge: true" app/src/lib/bodyProfile.ts</automated>
    <automated>grep -q "normalizeBodyProfile" app/src/lib/userAnalyses.ts</automated>
    <automated>! grep -nE "#[0-9A-Fa-f]{6}|fontSize:" app/src/lib/bodyProfile.ts</automated>
  </verify>
  <done>typecheck clean, normalizeBodyProfile/useBodyProfile/getBodyProfileOnce/saveBodyProfile/dismissBodyProfilePrompt export 존재, merge-write 사용, userAnalyses.ts normalize 가 normalizeBodyProfile 로 raw.bodyProfile 보존(R1), 순환 import 없음, 하드코딩 색/폰트 없음.</done>
</task>

<task type="auto">
  <name>Task 3: loading.tsx snapshot (getBodyProfileOnce) + pipeline coach seam (D-04) + D-05 확장 grep gate</name>
  <files>app/src/app/analysis/loading.tsx, backend/functions/pipeline/app.py, backend/tests/test_body_profile.py</files>
  <read_first>
    - app/src/app/analysis/loading.tsx:82-118 (startAnalysisUpload + setDoc:101-111 — referenceMotionId 조건부 spread 위치, import doc/setDoc:15)
    - app/src/lib/bodyProfile.ts (Task 2 — getBodyProfileOnce one-shot read 전용)
    - backend/functions/pipeline/app.py:738-779 (_build_coach_context — 기존 5 키 + writers-ignore-unknown-keys 주석 748-751), :1504-1573 (_process meta = get_analysis, meta.get("referenceMotionId"):1564), :1813-1818 (caller)
    - backend/shared/python/sunity_shared/models.py (Task 1 normalize_body_profile)
    - 03-RESEARCH.md §"Coach Hook Seam (D-04)" + §"Pattern 3: Snapshot-at-creation" + §Pitfall 5/6
    - 03-PATTERNS.md §loading.tsx / §pipeline/app.py 항목
  </read_first>
  <action>
    (앱, R5) loading.tsx `startAnalysisUpload` 의 setDoc(:101) 직전에 `const bodyProfile = await getBodyProfileOnce();` 호출 (raw `getDoc` spread 금지 — client normalize + all-empty→null 규칙이 helper 안에 박제됨, R5). setDoc payload 에 `...(bodyProfile ? { bodyProfile } : {})` 를 referenceMotionId spread(:108-110) 옆에 추가 (snapshot-at-creation, live cross-read 아님 — RESEARCH Pattern 3). painAreas 는 normalizeBodyProfile 이 이미 top-level scalar array 로 유지, 기존 array 필드 안에 nest 금지 (Pitfall 7). null 이면 spread 안 함 (graceful + snapshot 생략).
    (백엔드) `_build_coach_context` 에 `body_profile: dict | None = None` kwarg + 반환 dict 에 `"bodyProfile": body_profile` 키 추가. 주석: "Phase 3 (D-04) 자가입력 컨텍스트 훅 — Phase 13 LLM 이 소비(통증부위 회피·경력별 톤). 현 writer 미사용 키 graceful 무시(748-751). weightKg 보조 ONLY(D-05) — 점수 경로 진입 금지." caller(:1813)에 `body_profile=models.normalize_body_profile(meta.get("bodyProfile"))` 전달. _process meta 는 이미 get_analysis(:1508)로 scope 내 — get_analysis·get_reference_motion 변경 없음.
    test_body_profile.py 에 coach-context 케이스 추가: `_build_coach_context(..., body_profile=None)` 가 "bodyProfile" 키를 None 으로 포함하고 crash 없음 (SC#4 graceful), body_profile dict 전달 시 그대로 echo.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_body_profile.py -x -q && cd ../app && npm run typecheck</automated>
    <automated>! grep -rnE "weightKg|weight_kg" backend/shared/python/sunity_shared/analysis/dimensions.py backend/shared/python/sunity_shared/analysis/kismam.py backend/shared/python/sunity_shared/analysis/body_normalization.py backend/shared/python/sunity_shared/analysis/body_normalizer.py backend/shared/python/sunity_shared/analysis/force_signals.py backend/shared/python/sunity_shared/analysis/force_pattern.py</automated>
    <automated>grep -q '"bodyProfile"' backend/functions/pipeline/app.py && grep -q "getBodyProfileOnce" app/src/app/analysis/loading.tsx && grep -q "bodyProfile" app/src/app/analysis/loading.tsx</automated>
  </verify>
  <done>D-05 확장 grep gate 0 matches (dimensions/kismam/body_normalization/body_normalizer/force_signals/force_pattern 6개 scoring-consumer 모듈에 weightKg 없음 — R4), pipeline _build_coach_context 에 bodyProfile 키 존재, loading.tsx 가 getBodyProfileOnce()로 snapshot(raw spread 아님 — R5), coach-context graceful 테스트 GREEN, typecheck clean. (D-04 허용 경로: pipeline/app.py·coach_writer 는 grep 대상 제외 — 거기서는 bodyProfile 컨텍스트 전달이 의도된 동작.)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App(게스트) → Firestore users/{uid} | 사용자 자가입력값이 client-write 로 owner-only doc 에 들어감 |
| Firestore analysis doc → pipeline meta | snapshot 된 bodyProfile 이 백엔드로 free-read 됨 |
| Firestore raw bodyProfile → analysis snapshot | client normalize(getBodyProfileOnce) 후에만 snapshot — raw spread 금지 (R5) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | client-write bodyProfile (위조 height/통증) | mitigate | owner-only rules(`users/{uid}/{**}`)로 blast radius=본인 doc 한정 + getBodyProfileOnce 의 normalizeBodyProfile(client) + pipeline `normalize_body_profile`(server) 이중 정규화로 unknown enum/범위밖 → None + D-05 로 어떤 프로필값도 scoring 미유입(위조 height 로 점수 game 불가) |
| T-03-02 | Information Disclosure | 타 사용자 프로필 read | accept→mitigate | firestore.rules 비-owner deny (검증됨) — rules 변경 불필요 |
| T-03-03 | Information Disclosure | 통증부위(건강 인접 PII) 노출 | mitigate | owner-scoped Firestore 유지 + 평문 analytics 로깅 금지 + 외부 export 금지 (파일럿 게스트 한정) |
| T-03-08 | Tampering | 오래된/dev-console 수정 raw bodyProfile 이 analysis snapshot 오염 | mitigate | loading.tsx 가 raw getDoc spread 대신 getBodyProfileOnce() (client normalize + all-empty→null) 만 사용 (R5) |

High 위협 없음 (자가입력 보조 데이터, 점수 미유입, owner-only 격리). ASVS L1 통과.
</threat_model>

<verification>
- `cd backend && python -m pytest tests/test_body_profile.py -x -q` GREEN
- `cd app && npm run typecheck` clean
- 3-way parity: BodyProfile/PAIN_AREAS 가 analysis.ts·models.py·contract.md 모두 존재 + AnalysisDoc.bodyProfile (R1)
- userAnalyses normalize 가 normalizeBodyProfile 로 snapshot 보존 (R1)
- loading.tsx 가 getBodyProfileOnce() 로 snapshot (raw spread 아님 — R5)
- D-05 확장 grep gate: dimensions/kismam/body_normalization/body_normalizer/force_signals/force_pattern 6개에 weightKg 0 matches (R4)
- 회귀: `cd backend && python -m pytest tests/ -q` 기존 통과 유지
</verification>

<success_criteria>
- BodyProfile 타입 3-way lockstep + AnalysisDoc.bodyProfile snapshot 필드 완성 (SC: 계약 토대, R1)
- normalize_body_profile / normalizeBodyProfile 가 부분/없음/잘못된 입력 graceful (SC#4)
- bodyProfile snapshot→AnalysisDoc 보존→pipeline→coach context thin E2E 관통 (D-04 seam)
- loading.tsx client-normalize snapshot (R5)
- weightKg scoring 미유입 — 6 모듈 확장 gate (SC#3 / D-05 / R4)
</success_criteria>

<output>
Create `.planning/phases/03-bodyprofileinput/03-01-SUMMARY.md` when done
</output>
