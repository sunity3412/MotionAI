---
phase: 03-bodyprofileinput
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - app/src/components/BodyProfileForm.tsx
  - app/src/app/(tabs)/profile.tsx
autonomous: true
requirements: [BODY-02]

must_haves:
  truths:
    - "사용자가 마이페이지에서 키·몸무게·경력·통증부위·우세손을 입력하고 저장할 수 있다 (SC#1)"
    - "입력값이 Firestore users/{uid}.bodyProfile 에 저장된다 (SC#2, saveBodyProfile)"
    - "부분입력도 저장되고 마이페이지 카드가 요약/미입력 상태를 graceful 표시한다 (D-06)"
    - "작은 화면(iPhone SE급)에서 키보드가 떠도 저장 CTA 가 가려지지 않고 접근 가능하다 (R6)"
  artifacts:
    - path: "app/src/components/BodyProfileForm.tsx"
      provides: "공유 입력 폼 (5필드, RN primitive, a11y, 토큰, keyboard-safe)"
      contains: "BodyProfileForm"
    - path: "app/src/app/(tabs)/profile.tsx"
      provides: "BodyProfileCard 편집 진입점 (D-01/D-02)"
      contains: "useBodyProfile"
  key_links:
    - from: "app/src/components/BodyProfileForm.tsx"
      to: "app/src/lib/bodyProfile.ts saveBodyProfile"
      via: "저장 버튼 onPress merge-write"
      pattern: "saveBodyProfile"
    - from: "app/src/app/(tabs)/profile.tsx BodyProfileCard"
      to: "BodyProfileForm"
      via: "탭 → 폼 진입 (미입력/수정)"
      pattern: "BodyProfileForm"
---

<objective>
마이페이지 상시 편집 진입점(D-01/D-02) 수직 슬라이스. RN core primitive(TextInput/Pressable, 신규 패키지 0)로 5필드 공유 폼 `BodyProfileForm` 을 만들고, `profile.tsx` 에 `BodyProfileCard`(미입력=권유 카드 / 입력됨=요약+수정)를 추가한다. 폼은 03-01 의 `saveBodyProfile` 로 부분/전체 merge-write 하고, 카드는 `useBodyProfile` 로 현재값을 구독해 graceful 표시한다. 폼은 앱의 첫 실제 TextInput 화면이므로 KeyboardAvoidingView/ScrollView 로 작은 화면에서도 CTA 가 키보드에 가려지지 않게 한다 (R6).

Purpose: 사용자가 실제로 자가입력을 완료할 수 있는 1차 동작 진입점. 입력→저장→재진입 표시까지 한 슬라이스로 사용자 가치 발생.
Output: `BodyProfileForm.tsx` (keyboard-safe) + `profile.tsx` BodyProfileCard.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-bodyprofileinput/03-RESEARCH.md
@.planning/phases/03-bodyprofileinput/03-PATTERNS.md
@.planning/phases/03-bodyprofileinput/03-01-SUMMARY.md
@app/CLAUDE.md
@app/AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: BodyProfileForm.tsx — 5필드 공유 폼 (RN primitive + a11y + 토큰 + keyboard-safe)</name>
  <files>app/src/components/BodyProfileForm.tsx</files>
  <read_first>
    - Figma: get_metadata 후 get_design_context 로 fileKey jrdI7kp245HkPfLB0nclsz 에서 프로필/마이페이지 편집/BodyProfile 입력 frame 확인 ([[ui-figma-first]] 의무). frame 있으면 그 디자인이 layout source-of-truth. 없으면 design.md 토큰 + 아래 RESEARCH UI/UX flow map 으로 fallback (RESEARCH A2).
    - app/src/app/(tabs)/profile.tsx:13,96-205 (StatBox/InfoRow 인-파일 named helper + StyleSheet-at-bottom + 토큰 import `{ colors, layout, radius, spacing, typography }`)
    - app/src/app/(tabs)/analyze.tsx:135,188-291 (state + 한국어 inline error + pressable a11y `accessibilityRole/State/Label/hitSlop`)
    - app/src/lib/bodyProfile.ts (03-01 — saveBodyProfile, BodyProfile 타입)
    - app/src/theme/index.ts + colors.ts (colors.brand/brandTint/cardBg/divider/textWhite/inputError(#54B8CD teal), layout.inputHeight=54, radius.button=13/card=15)
    - 03-RESEARCH.md §B (input 컴포넌트 표 + iOS number-pad no-done caveat) + §E (a11y) + §F (토큰) + §Code Examples (Segmented/chip/numeric 리터럴 패턴) + §D (부분입력)
    - 03-PATTERNS.md §BodyProfileForm.tsx 항목
    - https://docs.expo.dev/versions/v54.0.0/ — KeyboardAvoidingView/ScrollView RN 0.81 API 확인 (AGENTS.md 의무)
    - docs/research/폴스포츠-지식.md (통증부위 칩 KO 라벨 도메인 정합 — 고하중 관절 어깨/손목/허리/무릎/발목/목/고관절/팔꿈치. 모호 시 NotebookLM IPSF lookup 또는 belle 확인)
  </read_first>
  <action>
    `app/src/components/BodyProfileForm.tsx` 신설. Props: `{ initial?: BodyProfile; onSaved?: () => void; onClose?: () => void }`. 내부 state: heightStr/weightStr(string, 빈=null) + experience/dominantHand(union|null) + painAreas(PainArea[]). initial prefill (수정 진입).
    [R6 keyboard-safe] 폼 루트를 `KeyboardAvoidingView`(iOS behavior="padding", Android "height") + `ScrollView`(contentContainerStyle 에 하단 패딩, `keyboardShouldPersistTaps="handled"`) 로 감싼다. 저장 CTA 는 ScrollView 안 마지막 요소로 두되 하단 safe-area inset(useSafeAreaInsets bottom 또는 spacing 토큰)을 확보해 키보드 open 상태에서도 스크롤로 도달 가능해야 함. 작은 화면(iPhone SE급)에서 5필드+CTA 가 키보드 뒤에 숨지 않도록 한다.
    인-파일 named helper:
    - 키 cm / 몸무게 kg: `TextInput` keyboardType="number-pad" maxLength={3}, suffix `<Text>cm/kg</Text>` sibling, onChangeText 에서 `replace(/[^0-9]/g,'')`, 범위 검증(키 90-250 / 몸무게 25-200) 밖이면 한국어 inline error (`colors.inputError` teal). 빈값 허용(D-06). accessibilityLabel("키, 센티미터" 등). iOS number-pad 는 done 키 없음 → 폼 전체를 `Pressable onPress={Keyboard.dismiss}` 로 감싸거나 명시 "완료" affordance.
    - 경력(초/중/고급) + 우세손(좌/우/양): 제네릭 `Segmented<T>` Pressable row — selected = `colors.brand` bg + `colors.textWhite` text, unselected = border. accessibilityRole="button" + accessibilityState={{ selected }} + accessibilityLabel("경력 중급, 선택됨").
    - 통증부위: `flexWrap:'wrap'` chip 그리드, toggle, painAreas string[] (D-03 다중선택). selected = brandTint/brand. 빈 배열=없음 graceful. PAIN_AREAS KO 라벨 매핑 (shoulder→어깨 등).
    저장 CTA: full-width height 54 radius.button `colors.brand` → `saveBodyProfile({ heightCm, weightKg, experience, painAreas, dominantHand })` (빈 필드는 null/[] 로 merge — 부분 OK) → onSaved(). weightKg state/필드 위에 "보조 ONLY (D-05)" 주석. StyleSheet.create 는 파일 하단, 모든 값 토큰 참조 (하드코딩 색/spacing/fontSize 금지 — app/CLAUDE.md). 신규 npm 패키지 설치 금지 (EAS Build 리스크 0).
  </action>
  <verify>
    <automated>cd app && npm run typecheck</automated>
    <automated>grep -q "BodyProfileForm" app/src/components/BodyProfileForm.tsx && grep -q "saveBodyProfile" app/src/components/BodyProfileForm.tsx && grep -q "accessibilityState" app/src/components/BodyProfileForm.tsx</automated>
    <automated>grep -qE "KeyboardAvoidingView|ScrollView" app/src/components/BodyProfileForm.tsx</automated>
    <automated>! grep -nE "#[0-9A-Fa-f]{6}|fontSize:[0-9]" app/src/components/BodyProfileForm.tsx</automated>
    <automated>! grep -rn "segmented-control\|multi-select" app/package.json</automated>
    <human-check>iPhone SE급 작은 화면(또는 시뮬레이터 SE)에서 폼 진입 → 모든 입력 칸 탭 → 키보드 open 상태에서 저장 CTA 가 가려지지 않고 스크롤로 도달·탭 가능, 통증부위 칩이 하단 safe area 와 겹치지 않음 (R6). 부분 입력 저장도 성공.</human-check>
  </verify>
  <done>typecheck clean, 5필드 폼 존재(신규 파일이라 full-file hardcode grep 적용 — 0), saveBodyProfile 호출, a11y props, KeyboardAvoidingView/ScrollView keyboard-safe, 토큰만(하드코딩 0), 신규 native 패키지 미설치. 작은 화면 CTA 도달 manual-verify 통과(R6).</done>
</task>

<task type="auto">
  <name>Task 2: profile.tsx — BodyProfileCard 진입점 (미입력 권유 / 입력됨 요약+수정)</name>
  <files>app/src/app/(tabs)/profile.tsx</files>
  <read_first>
    - Figma: get_design_context fileKey jrdI7kp245HkPfLB0nclsz 마이페이지 카드 frame 확인 ([[ui-figma-first]]). 없으면 design.md + RESEARCH §A flow map fallback.
    - app/src/app/(tabs)/profile.tsx:30-95 (Profile 화면 구조 — 게스트 카드:45-56 / 통계:58-66 / InfoRow:68-82 / styles.card 토큰), :177 (기존 `fontSize: 28` — Phase 3 무관, 건드리지 말 것)
    - app/src/components/BodyProfileForm.tsx (Task 1)
    - app/src/lib/bodyProfile.ts (useBodyProfile)
    - 03-RESEARCH.md §A ENTRY POINT 1 (미입력 카드 → 폼 → 저장 → 요약 카드 state 전이) + §D (empty/partial summary)
    - 03-PATTERNS.md §profile.tsx 항목
  </read_first>
  <action>
    profile.tsx 에 `useBodyProfile()` 구독 추가 + 게스트 카드와 통계 사이(또는 통계 뒤)에 `BodyProfileCard` 삽입. 미입력(profile null 또는 전 필드 빈): brandTint 권유 카드 "내 몸 정보 입력하기 ›" (권유 톤). 입력됨: 요약 row ("165cm · 중급 · 어깨·손목" 형태 — 채워진 필드만, 부분이면 partial 표시 "165cm · 경력 미입력") + "수정 ›". 둘 다 탭 → BodyProfileForm 진입 (전체화면 route 또는 인라인/모달 확장 — Figma/design.md 판단, 기존값 prefill). 저장 후 onSnapshot 으로 카드 자동 갱신. 기존 styles.card(토큰) 재사용, Ionicons 사용. 모든 pressable a11y. 하드코딩 색/spacing 금지.
    [R3] 기존 line(예: :177 `fontSize: 28`)은 Phase 3 와 무관 — 제거/리팩터 금지. 새로 추가하는 BodyProfileCard styles 섹션만 토큰 사용. (검증은 full-file grep 이 아니라 git diff 의 추가 라인만 검사 — 아래 verify 참조.)
  </action>
  <verify>
    <automated>cd app && npm run typecheck</automated>
    <automated>grep -q "useBodyProfile" app/src/app/\(tabs\)/profile.tsx && grep -q "BodyProfileForm" app/src/app/\(tabs\)/profile.tsx</automated>
    <automated>! git diff -- app/src/app/\(tabs\)/profile.tsx | grep -E "^\+" | grep -vE "^\+\+\+" | grep -nE "#[0-9A-Fa-f]{6}|fontSize:[0-9]"</automated>
  </verify>
  <done>typecheck clean, profile.tsx 가 useBodyProfile 구독 + BodyProfileCard(미입력/입력됨 분기) + BodyProfileForm 진입, **추가된 diff 라인에** 하드코딩 색/폰트 0 (기존 :177 fontSize:28 등 무관 라인은 보존 — R3).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App 폼 입력 → bodyProfile.ts | 사용자 키 입력값이 client-side 검증 후 merge-write 됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | 폼 numeric 입력 (비숫자/범위밖) | mitigate | onChangeText `[^0-9]` strip + 범위 검증 inline error + saveBodyProfile 빈→null. 저장값은 03-01 normalize_body_profile 가 백엔드에서 재검증 |
| T-03-05 | Input Validation | 통증부위 임의 string | mitigate | 칩은 PainArea 닫힌 union 만 — 자유텍스트 불가 (D-03). painAreas flat scalar array (Pitfall 7) |

UI 슬라이스 — 저장 경로는 03-01 owner-only rules + normalize 로 격리. High 위협 없음. ASVS L1 통과.
</threat_model>

<verification>
- `cd app && npm run typecheck` clean
- BodyProfileForm 5필드 + a11y + 토큰 + saveBodyProfile + KeyboardAvoidingView/ScrollView (R6)
- profile.tsx BodyProfileCard 미입력/입력됨 분기 + useBodyProfile
- 하드코딩 grep gate: 신규 파일 BodyProfileForm 은 full-file 0 / 기존 profile.tsx 는 diff-추가-라인만 0 (R3 — 기존 라인 false-fail 회피)
- 신규 native 패키지 미설치 (package.json 불변)
- manual: iPhone SE급 작은 화면 입력/저장 시나리오 (R6)
</verification>

<success_criteria>
- 마이페이지에서 5필드 입력·저장 가능 (SC#1)
- 입력값 Firestore 저장 (SC#2)
- 부분입력 graceful + 재진입 요약 표시 (D-06)
- 작은 화면 keyboard-safe CTA (R6)
</success_criteria>

<output>
Create `.planning/phases/03-bodyprofileinput/03-02-SUMMARY.md` when done
</output>
