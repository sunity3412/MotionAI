---
phase: 03-bodyprofileinput
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - app/src/components/BodyProfilePromptModal.tsx
  - app/src/app/(tabs)/analyze.tsx
  - app/src/app/analysis/result.tsx
autonomous: true
requirements: [BODY-02]

must_haves:
  truths:
    - "첫 분석 직전 프로필 미입력 게스트에게 dismissible 권유 모달이 1회 뜬다 (D-01)"
    - "건너뛰기/백드롭/뒤로 3가지로 dismiss 되고 once-flag 후 재권유 안 함 (D-06)"
    - "프로필 이미 있으면 게이트 통과 (모달 미표시), 미입력이어도 분석 graceful 진행 (SC#4)"
    - "pendingPicked state 로 라우팅을 보류했다가 모달 결과(입력완료/건너뛰기/dismiss) 후 동일 picked 로 재개한다 (R2)"
    - "결과 화면이 live useBodyProfile 이 아니라 storedDoc.bodyProfile SNAPSHOT 을 source-of-truth 로 표기한다 (R1 재현성)"
  artifacts:
    - path: "app/src/components/BodyProfilePromptModal.tsx"
      provides: "첫분석 dismissible bottom-sheet (DimensionDetailModal 재사용)"
      contains: "BodyProfilePromptModal"
    - path: "app/src/app/(tabs)/analyze.tsx"
      provides: "pendingPicked 상태머신 게이트 (pick 후 routeAfterPick 전 권유)"
      contains: "pendingPicked"
    - path: "app/src/app/analysis/result.tsx"
      provides: "결과화면 BodyProfile snapshot 표기 (D-04 / R1)"
      contains: "bodyProfile"
  key_links:
    - from: "app/src/app/(tabs)/analyze.tsx pick flow"
      to: "BodyProfilePromptModal"
      via: "gate: profile null AND !promptDismissedAt → setPendingPicked + setPromptVisible"
      pattern: "pendingPicked"
    - from: "BodyProfilePromptModal 건너뛰기"
      to: "bodyProfile.ts dismissBodyProfilePrompt → continuePendingRoute"
      via: "once-flag 세팅 후 보류된 picked 로 분석 계속"
      pattern: "continuePendingRoute"
    - from: "app/src/app/analysis/result.tsx"
      to: "storedDoc.bodyProfile snapshot"
      via: "useAnalysisDoc().doc?.bodyProfile (live read 아님)"
      pattern: "storedDoc"
---

<objective>
첫 분석 직전 1회 가벼운 권유 진입점(D-01) + 미입력 graceful(D-06/SC#4) + 결과화면 표기(D-04) 수직 슬라이스. `BodyProfilePromptModal`(DimensionDetailModal bottom-sheet 재사용, dismissible 3-way)을 만들고, `analyze.tsx` 의 pick→routeAfterPick 사이에 `pendingPicked` 상태머신(R2) 게이트를 둔다: "프로필 미입력 AND 권유 미열람"이면 picked 영상을 보류하고 모달을 띄우며, 모달 결과(입력완료/건너뛰기/백드롭/back)는 모두 단일 `continuePendingRoute()` 로 수렴해 동일 picked 로 라우팅을 재개한다(stale closure/영상 유실 방지). 건너뛰면 once-flag(`promptDismissedAt`) 세팅 후 재권유 안 함. 결과 화면(`result.tsx`)은 live profile 이 아니라 분석 doc 의 `bodyProfile` SNAPSHOT(R1)을 표기한다.

Purpose: 자가입력 가치를 분석 흐름에 자연스럽게 연결하되 절대 강제하지 않음(게스트 우선). 결과화면이 분석-당시 입력값을 재현 가능하게 표기해 입력→가치 루프를 닫음(R1 재현성).
Output: `BodyProfilePromptModal.tsx` + analyze.tsx pendingPicked 게이트 + result.tsx snapshot 표기.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-bodyprofileinput/03-RESEARCH.md
@.planning/phases/03-bodyprofileinput/03-PATTERNS.md
@.planning/phases/03-bodyprofileinput/03-01-SUMMARY.md
@.planning/phases/03-bodyprofileinput/03-02-SUMMARY.md
@app/CLAUDE.md
@app/AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: BodyProfilePromptModal.tsx — dismissible 첫분석 권유 시트</name>
  <files>app/src/components/BodyProfilePromptModal.tsx</files>
  <read_first>
    - Figma: get_design_context fileKey jrdI7kp245HkPfLB0nclsz 권유/온보딩 모달 frame 확인 ([[ui-figma-first]]). 없으면 design.md + RESEARCH §C fallback.
    - app/src/components/DimensionDetailModal.tsx:1-31 (Modal + Pressable 백드롭 + bottom-sheet skeleton, radius.modal=20, 백드롭 dim, useWindowDimensions)
    - app/src/components/BodyProfileForm.tsx (03-02 — [입력하기] → 폼 진입)
    - app/src/lib/bodyProfile.ts (03-01 — dismissBodyProfilePrompt)
    - 03-RESEARCH.md §C ("가벼운 권유" non-forcing 요건 — dismissible 3-way + skip equal-weight + no-nag once-flag) + §E (accessibilityViewIsModal)
    - 03-PATTERNS.md §BodyProfilePromptModal.tsx 항목
  </read_first>
  <action>
    `app/src/components/BodyProfilePromptModal.tsx` 신설. DimensionDetailModal 의 Modal + Pressable 백드롭 + bottom-sheet 골격 복제. Props: `{ visible: boolean; onInput: () => void; onSkip: () => void }`. 헤드라인 "더 정확한 코칭을 위해 (선택)" + 부텍스트 "키·경력·통증부위를 알려주면 분석이 더 구체적이에요". 두 동등 CTA: [입력하기](`colors.brand`) → onInput, [건너뛰기](secondary, 버려진 회색 링크 아님 — equal weight) → onSkip. dismiss 3-way 모두 onSkip 동등: 건너뛰기 버튼 + 백드롭 `Pressable onPress` + native back(Modal onRequestClose). 시트 컨테이너에 `accessibilityViewIsModal`, 버튼 a11y. 토큰만 (radius.modal/button, colors, typography — 하드코딩 금지).
  </action>
  <verify>
    <automated>cd app && npm run typecheck</automated>
    <automated>grep -q "BodyProfilePromptModal" app/src/components/BodyProfilePromptModal.tsx && grep -q "accessibilityViewIsModal" app/src/components/BodyProfilePromptModal.tsx && grep -q "onRequestClose" app/src/components/BodyProfilePromptModal.tsx</automated>
    <automated>! grep -nE "#[0-9A-Fa-f]{6}|fontSize:[0-9]" app/src/components/BodyProfilePromptModal.tsx</automated>
  </verify>
  <done>typecheck clean, dismissible 3-way bottom-sheet(버튼/백드롭/onRequestClose), 두 equal-weight CTA, accessibilityViewIsModal, 신규 파일 full-file 하드코딩 0.</done>
</task>

<task type="auto">
  <name>Task 2: analyze.tsx pendingPicked 게이트 상태머신 (R2) + result.tsx BodyProfile snapshot 표기 (R1)</name>
  <files>app/src/app/(tabs)/analyze.tsx, app/src/app/analysis/result.tsx</files>
  <read_first>
    - app/src/app/(tabs)/analyze.tsx:31-36 (type Picked), :40-64 (referenceMotionId/mode state), :83-122 (routeAfterPick — mode1 referenceMotionId 유무 분기 + mode3, router.push), :124-141 (handleResult validate → routeAfterPick 즉시 호출 — R2 가 끊을 지점), :143-175 (pick busy/try-catch-finally), :188-291 (a11y)
    - app/src/lib/bodyProfile.ts (03-01 — useBodyProfile, dismissBodyProfilePrompt)
    - app/src/components/BodyProfilePromptModal.tsx (Task 1)
    - app/src/components/BodyProfileForm.tsx (03-02 — [입력하기] 진입 폼)
    - app/src/app/analysis/result.tsx:34 (import useAnalysisDoc), :383 (const { doc: storedDoc } = useAnalysisDoc(analysisId)), :386-398 (storedDoc.result 소비 패턴 — bodyProfile 동일 소스), :95/:236 (기존 `#FF4B33` 주석 — Phase 3 무관, 건드리지 말 것), InfoRow/표기 row 패턴
    - 03-RESEARCH.md §A ENTRY POINT 2 (게이트 로직 + once flag) + §"Coach Hook — Result-screen 표기" + Open Question 3 (표기 위치/형식)
    - 03-PATTERNS.md §analyze.tsx 항목
  </read_first>
  <action>
    (analyze.tsx — R2 상태머신) `useBodyProfile()` 구독 + 두 state 추가: `const [pendingPicked, setPendingPicked] = useState<Picked | null>(null);` 와 `const [promptVisible, setPromptVisible] = useState(false);`. 게이트 위치는 **영상 pick 직후, mode1 의 reference 선택 전** (RESEARCH 위치 결정).
    - `handleResult()` 는 validate 통과 후 `routeAfterPick(picked)` 를 직접 호출하지 말고 `maybePromptBeforeRoute(picked)` 를 호출한다.
    - `maybePromptBeforeRoute(picked: Picked)`: 게이트 조건(once-flag 미설정 `!profile?.promptDismissedAt` AND profile null/전필드빈 = 미입력)이 true 면 `setPendingPicked(picked); setPromptVisible(true); return;`. 아니면 즉시 `routeAfterPick(picked);`.
    - `continuePendingRoute()`: `const p = pendingPicked; setPendingPicked(null); setPromptVisible(false); if (p) routeAfterPick(p);` (단일 수렴점 — stale closure/영상 유실 방지, R2).
    - 모달 wiring: `<BodyProfilePromptModal visible={promptVisible} onInput={...} onSkip={async () => { await dismissBodyProfilePrompt(); continuePendingRoute(); }} />`. onInput → BodyProfileForm(03-02) 진입(전체화면 route 또는 모달 확장) → 저장 onSaved 콜백에서 `continuePendingRoute()` (입력 완료 후 동일 picked 로 분석 계속). 백드롭/native back 은 PromptModal 내부에서 onSkip 으로 수렴(Task 1) → 동일하게 dismissBodyProfilePrompt + continuePendingRoute.
    - [입력하기]저장완료 / [건너뛰기] / backdrop / native back **4 경로 모두 continuePendingRoute() 로 수렴**. 게이트는 1회만 — 한 번 dismiss 후 promptDismissedAt 으로 재권유 X (D-06). 기존 busy/try-catch-finally + a11y 관례 유지.
    (result.tsx — R1 snapshot) **live `useBodyProfile()` 추가 금지.** 기존 `const { doc: storedDoc } = useAnalysisDoc(analysisId)`(:383) 의 `storedDoc?.bodyProfile` SNAPSHOT 을 source-of-truth 로 읽는다(분석-당시 값 재현성). 입력값 있으면 작은 표기 row ("키 165cm · 경력 중급 · 통증 어깨" — 채워진 필드만, RESEARCH Open Q3). snapshot 없으면(구 doc) `useBodyProfile()` live read 를 **fallback 으로만** 허용. 미입력이면 표기 row 생략(graceful). weightKg 표기는 보조 정보 표시일 뿐 점수 경로 무관(D-05). 위치/형식은 Figma/design.md 우선.
  </action>
  <verify>
    <automated>cd app && npm run typecheck</automated>
    <automated>grep -q "BodyProfilePromptModal" app/src/app/\(tabs\)/analyze.tsx && grep -q "pendingPicked" app/src/app/\(tabs\)/analyze.tsx && grep -q "continuePendingRoute" app/src/app/\(tabs\)/analyze.tsx && grep -qE "promptDismissedAt|dismissBodyProfilePrompt" app/src/app/\(tabs\)/analyze.tsx</automated>
    <automated>grep -q "storedDoc" app/src/app/analysis/result.tsx && grep -q "bodyProfile" app/src/app/analysis/result.tsx</automated>
    <automated>! git diff -- app/src/app/analysis/result.tsx | grep -E "^\+" | grep -vE "^\+\+\+" | grep -nE "#[0-9A-Fa-f]{6}|fontSize:[0-9]"</automated>
  </verify>
  <done>typecheck clean; analyze.tsx 가 pendingPicked+promptVisible state + maybePromptBeforeRoute 게이트(미입력 AND !dismissed → 모달 보류, 그 외 즉시 routeAfterPick) + continuePendingRoute 4-경로 단일 수렴 + once-flag (R2); result.tsx 가 storedDoc.bodyProfile SNAPSHOT 표기(live read 아님, graceful 생략 포함 — R1); result.tsx **추가 diff 라인에** 하드코딩 0 (기존 :95/:236 #FF4B33 주석 보존 — R3).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| analyze 권유 게이트 → 분석 진입 | 게이트가 분석 진행을 막지 않아야 함 (게스트 우선) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Denial of Service (UX) | 권유 모달이 분석을 강제/반복 차단 | mitigate | dismissible 3-way + once-flag(promptDismissedAt) 재권유 0 + profile 있으면 게이트 우회 + 미입력 graceful 진행(SC#4) + pendingPicked 4-경로 모두 continuePendingRoute 로 분석 재개 보장(R2) — 회원가입/입력 강제 금지 (Pitfall 3, CLAUDE.md §2 게스트 우선) |
| T-03-07 | Information Disclosure | result.tsx 통증부위 snapshot 표기 노출 | accept | 본인 결과화면 한정(owner-only doc 구독), 외부 공유/로깅 없음 |

UI 슬라이스 — 저장/읽기 경로는 03-01 owner-only 격리. High 위협 없음. ASVS L1 통과.
</threat_model>

<verification>
- `cd app && npm run typecheck` clean
- analyze.tsx pendingPicked 상태머신 게이트 + 4-경로 continuePendingRoute 수렴 + once-flag + 미입력 graceful 통과 (R2)
- result.tsx storedDoc.bodyProfile SNAPSHOT 표기 (live read 아님, graceful 생략 — R1)
- 하드코딩 grep gate: 신규 modal 은 full-file 0 / 기존 result.tsx 는 diff-추가-라인만 0 (R3)
- 신규 native 패키지 미설치
</verification>

<success_criteria>
- 첫 분석 직전 1회 dismissible 권유 + pendingPicked 로 라우팅 보류/재개 (D-01 / R2)
- 건너뛰기 3-way + 재권유 안 함 (D-06)
- 미입력 graceful 분석 진행 (SC#4)
- 결과화면 BodyProfile snapshot 표기 (D-04 / R1)
</success_criteria>

<output>
Create `.planning/phases/03-bodyprofileinput/03-03-SUMMARY.md` when done
</output>
