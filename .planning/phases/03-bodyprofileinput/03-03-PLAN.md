---
phase: 03-bodyprofileinput
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - app/src/components/BodyProfilePromptModal.tsx
  - app/src/app/(tabs)/analyze.tsx
  - app/src/app/analysis/result.tsx
autonomous: true
requirements: [BODY-02]

must_haves:
  truths:
    - "첫 분석 직전 프로필 미입력 게스트에게 dismissible 권유 모달이 1회 뜬다 (D-01)"
    - "건너뛰기/백드롭/뒤로 3가지로 dismiss 되고 once-flag 후 재권유 안 함 (D-06)"
    - "프로필 이미 있으면 게이트 통과 (모달 미표시), 미입력이어도 분석 graceful 진행 (SC#4)"
    - "결과 화면에 입력한 BodyProfile 이 표기된다 (D-04 display)"
  artifacts:
    - path: "app/src/components/BodyProfilePromptModal.tsx"
      provides: "첫분석 dismissible bottom-sheet (DimensionDetailModal 재사용)"
      contains: "BodyProfilePromptModal"
    - path: "app/src/app/(tabs)/analyze.tsx"
      provides: "pick 후 routeAfterPick 전 권유 게이트"
      contains: "BodyProfilePromptModal"
    - path: "app/src/app/analysis/result.tsx"
      provides: "결과화면 BodyProfile 표기 (D-04)"
      contains: "useBodyProfile"
  key_links:
    - from: "app/src/app/(tabs)/analyze.tsx pick flow"
      to: "BodyProfilePromptModal"
      via: "gate: profile null AND !promptDismissedAt"
      pattern: "promptDismissedAt"
    - from: "BodyProfilePromptModal 건너뛰기"
      to: "bodyProfile.ts dismissBodyProfilePrompt"
      via: "once-flag 세팅 후 분석 계속"
      pattern: "dismissBodyProfilePrompt"
---

<objective>
첫 분석 직전 1회 가벼운 권유 진입점(D-01) + 미입력 graceful(D-06/SC#4) + 결과화면 표기(D-04) 수직 슬라이스. `BodyProfilePromptModal`(DimensionDetailModal bottom-sheet 재사용, dismissible 3-way)을 만들고, `analyze.tsx` 의 pick→routeAfterPick 사이에 "프로필 미입력 AND 권유 미열람" 게이트를 두고, 건너뛰면 once-flag(`promptDismissedAt`) 세팅 후 분석 계속(재권유 X). 결과 화면(`result.tsx`)에 `useBodyProfile` 로 입력값을 표기한다.

Purpose: 자가입력 가치를 분석 흐름에 자연스럽게 연결하되 절대 강제하지 않음(게스트 우선). 결과화면 표기로 입력→가치 루프 닫음.
Output: `BodyProfilePromptModal.tsx` + analyze.tsx 게이트 + result.tsx 표기.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-bodyprofileinput/03-RESEARCH.md
@.planning/phases/03-bodyprofileinput/03-PATTERNS.md
@.planning/phases/03-bodyprofileinput/03-01-SUMMARY.md
@.planning/phases/03-bodyprofileinput/03-02-SUMMARY.md
@app/CLAUDE.md
@app/AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: BodyProfilePromptModal.tsx — dismissible 첫분석 권유 시트</name>
  <files>app/src/components/BodyProfilePromptModal.tsx</files>
  <read_first>
    - Figma: get_design_context fileKey jrdI7kp245HkPfLB0nclsz 권유/온보딩 모달 frame 확인 ([[ui-figma-first]]). 없으면 design.md + RESEARCH §C fallback.
    - app/src/components/DimensionDetailModal.tsx:1-31 (Modal + Pressable 백드롭 + bottom-sheet skeleton, radius.modal=20, 백드롭 dim, useWindowDimensions)
    - app/src/components/BodyProfileForm.tsx (03-02 — [입력하기] → 폼 진입)
    - app/src/lib/bodyProfile.ts (03-01 — dismissBodyProfilePrompt)
    - 03-RESEARCH.md §C ("가벼운 권유" non-forcing 요건 — dismissible 3-way + skip equal-weight + no-nag once-flag) + §E (accessibilityViewIsModal)
    - 03-PATTERNS.md §BodyProfilePromptModal.tsx 항목
  </read_first>
  <action>
    `app/src/components/BodyProfilePromptModal.tsx` 신설. DimensionDetailModal 의 Modal + Pressable 백드롭 + bottom-sheet 골격 복제. Props: `{ visible: boolean; onInput: () => void; onSkip: () => void }`. 헤드라인 "더 정확한 코칭을 위해 (선택)" + 부텍스트 "키·경력·통증부위를 알려주면 분석이 더 구체적이에요". 두 동등 CTA: [입력하기](`colors.brand`) → onInput, [건너뛰기](secondary, 버려진 회색 링크 아님 — equal weight) → onSkip. dismiss 3-way 모두 onSkip 동등: 건너뛰기 버튼 + 백드롭 `Pressable onPress` + native back. 시트 컨테이너에 `accessibilityViewIsModal`, 버튼 a11y. 토큰만 (radius.modal/button, colors, typography — 하드코딩 금지).
  </action>
  <verify>
    <automated>cd app && npm run typecheck</automated>
    <automated>grep -q "BodyProfilePromptModal" app/src/components/BodyProfilePromptModal.tsx && grep -q "accessibilityViewIsModal" app/src/components/BodyProfilePromptModal.tsx</automated>
    <automated>! grep -nE "#[0-9A-Fa-f]{6}|fontSize:[0-9]" app/src/components/BodyProfilePromptModal.tsx</automated>
  </verify>
  <done>typecheck clean, dismissible 3-way bottom-sheet, 두 equal-weight CTA, accessibilityViewIsModal, 하드코딩 0.</done>
</task>

<task type="auto">
  <name>Task 2: analyze.tsx 권유 게이트 + result.tsx BodyProfile 표기 (D-04)</name>
  <files>app/src/app/(tabs)/analyze.tsx, app/src/app/analysis/result.tsx</files>
  <read_first>
    - app/src/app/(tabs)/analyze.tsx:40-64 (referenceMotionId/mode state), :83-135 (routeAfterPick + pick → router.push('/analysis/loading')), :188-291 (busy/a11y)
    - app/src/lib/bodyProfile.ts (03-01 — useBodyProfile, dismissBodyProfilePrompt)
    - app/src/components/BodyProfilePromptModal.tsx (Task 1)
    - app/src/app/analysis/result.tsx:34,372-383 (useAnalysisDoc 패턴 — useBodyProfile 동일 추가) + InfoRow/표기 row 패턴
    - 03-RESEARCH.md §A ENTRY POINT 2 (게이트 로직 + once flag) + §Coach Hook "Result-screen 표기" (option b — hook live read, 백엔드 echo 불필요) + Open Question 3 (표기 위치/형식)
    - 03-PATTERNS.md §analyze.tsx 항목
  </read_first>
  <action>
    (analyze.tsx) `useBodyProfile()` 구독 + `BodyProfilePromptModal` state 추가. pick 완료 후 `routeAfterPick` 직전 게이트: profile 이 null/전필드빈 AND `profile?.promptDismissedAt` 미설정 이면 모달 표시(분석 보류). 그 외엔 기존 흐름 그대로 통과(SC#4 — 미입력이어도 graceful, 이미입력이면 게이트 우회). 모달 [입력하기] → BodyProfileForm(03-02) 진입 → 저장 후 분석 계속. [건너뛰기]/백드롭/back → `dismissBodyProfilePrompt()` (once-flag) 후 routeAfterPick 계속. 기존 busy/try-catch-finally + a11y 관례 유지. 게이트는 1회만 (한 번 dismiss 후 재권유 X — D-06).
    (result.tsx) `useBodyProfile()` 추가, 입력값 있으면 작은 표기 row ("키 165cm · 경력 중급 · 통증 어깨" — 채워진 필드만, RESEARCH Open Q3). 위치/형식은 Figma/design.md 우선. 미입력이면 표기 row 생략(graceful). weightKg 표기는 보조 정보 표시일 뿐 점수 경로 무관(D-05). 백엔드 echo 불필요 — hook live read.
  </action>
  <verify>
    <automated>cd app && npm run typecheck</automated>
    <automated>grep -q "BodyProfilePromptModal" app/src/app/\(tabs\)/analyze.tsx && grep -q "promptDismissedAt\|dismissBodyProfilePrompt" app/src/app/\(tabs\)/analyze.tsx && grep -q "useBodyProfile" app/src/app/analysis/result.tsx</automated>
    <automated>! grep -nE "#[0-9A-Fa-f]{6}|fontSize:[0-9]" app/src/app/analysis/result.tsx</automated>
  </verify>
  <done>typecheck clean, analyze.tsx 게이트(미입력 AND !dismissed → 모달, 그 외 통과) + once-flag, result.tsx BodyProfile 표기(graceful 생략 포함), 하드코딩 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| analyze 권유 게이트 → 분석 진입 | 게이트가 분석 진행을 막지 않아야 함 (게스트 우선) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Denial of Service (UX) | 권유 모달이 분석을 강제/반복 차단 | mitigate | dismissible 3-way + once-flag(promptDismissedAt) 재권유 0 + profile 있으면 게이트 우회 + 미입력 graceful 진행(SC#4) — 회원가입/입력 강제 금지 (Pitfall 3, CLAUDE.md §2 게스트 우선) |
| T-03-07 | Information Disclosure | result.tsx 통증부위 표기 노출 | accept | 본인 결과화면 한정(owner-only doc 구독), 외부 공유/로깅 없음 |

UI 슬라이스 — 저장/읽기 경로는 03-01 owner-only 격리. High 위협 없음. ASVS L1 통과.
</threat_model>

<verification>
- `cd app && npm run typecheck` clean
- analyze.tsx 게이트 + once-flag + 미입력 graceful 통과
- result.tsx BodyProfile 표기 (graceful 생략)
- 하드코딩 색/폰트 grep gate 0
- 신규 native 패키지 미설치
</verification>

<success_criteria>
- 첫 분석 직전 1회 dismissible 권유 (D-01)
- 건너뛰기 3-way + 재권유 안 함 (D-06)
- 미입력 graceful 분석 진행 (SC#4)
- 결과화면 BodyProfile 표기 (D-04)
</success_criteria>

<output>
Create `.planning/phases/03-bodyprofileinput/03-03-SUMMARY.md` when done
</output>
