---
phase: 04
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/tests/phase04/__init__.py
  - backend/tests/phase04/conftest.py
  - backend/tests/phase04/test_confidence_gate.py
  - backend/tests/phase04/test_synthesis_adapter.py
  - backend/tests/phase04/test_synthesis_merge.py
  - backend/tests/phase04/test_warning_lockstep.py
  - backend/tests/phase04/test_synthesis_firestore_flat.py
  - backend/tests/phase04/test_synthesis_g4_guard.py
autonomous: true
requirements:
  - POSE-03

must_haves:
  truths:
    - "pytest backend/tests/phase04/ -x -q 가 존재하고 수집 가능하다 (tests 디렉토리 + __init__ + conftest)"
    - "POSE-03-a~f 를 커버하는 6개 test 파일이 모두 존재하고 import error 0"
    - "conftest.py 가 (T=30, J=17) 및 (T=60, J=17) synthetic joint sequence + confidence array fixture 를 모두 제공한다 — joint_seq_30f, joint_seq_60f, conf_seq_30f, conf_seq_60f"
    - "test_warning_lockstep.py 는 models.py frozenset 을 직접 import 해 ai_synthesis_failed / ai_synthesis_partial 이 포함됐는지 단언한다 (D-08, POSE-03-d)"
  artifacts:
    - path: backend/tests/phase04/__init__.py
      provides: "pytest discovery"
    - path: backend/tests/phase04/conftest.py
      provides: "synthetic fixture — joint_seq_30f, joint_seq_60f, conf_seq_30f, conf_seq_60f, low_conf_seq, scene_findings_occlusion, scene_findings_clean"
    - path: backend/tests/phase04/test_confidence_gate.py
      provides: "POSE-03-a coverage"
    - path: backend/tests/phase04/test_synthesis_adapter.py
      provides: "POSE-03-b coverage (GeminiViewReasoner / CylindricalMeshAdapter mock)"
    - path: backend/tests/phase04/test_synthesis_merge.py
      provides: "POSE-03-c coverage (temporal.py 재사용)"
    - path: backend/tests/phase04/test_warning_lockstep.py
      provides: "POSE-03-d coverage (3-way lockstep 사전 assertion)"
    - path: backend/tests/phase04/test_synthesis_firestore_flat.py
      provides: "POSE-03-e coverage"
    - path: backend/tests/phase04/test_synthesis_g4_guard.py
      provides: "POSE-03-f coverage"
  key_links:
    - from: backend/tests/phase04/test_warning_lockstep.py
      to: backend/shared/python/sunity_shared/models.py
      via: "frozenset import"
      pattern: "SYNTHESIS_WARNING_CODES.*ai_synthesis_failed"
---

<objective>
Phase 4 Wave 0 — pytest 인프라 신설. POSE-03-a~f 를 커버하는 6개 단위 테스트 파일 + conftest fixture 를 생성한다. Wave 1 executor 가 실제 구현 코드를 작성하기 전에 실패하는 테스트가 먼저 존재해야 한다 (TDD 게이트).

Purpose: Wave 1 합성 어댑터 + confidence gate 구현의 Nyquist gate. Wave 0 이 존재하지 않으면 Wave 1 이 회귀 게이트 없이 진행돼 안전성이 없다.
Output: backend/tests/phase04/ 디렉토리 + 8파일 (import 가능, 일부 현재 RED)
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-ux-occlusion-confidence/04-CONTEXT.md
@.planning/phases/04-ux-occlusion-confidence/04-VALIDATION.md
@.planning/phases/04-ux-occlusion-confidence/04-RESEARCH.md
@.planning/ROADMAP.md
@.planning/STATE.md

@backend/tests/conftest.py
@backend/tests/phase08_1/__init__.py
@backend/tests/phase08_1/conftest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: phase04 test 인프라 신설 (conftest + 6 test 파일)</name>
  <files>
    backend/tests/phase04/__init__.py,
    backend/tests/phase04/conftest.py,
    backend/tests/phase04/test_confidence_gate.py,
    backend/tests/phase04/test_synthesis_adapter.py,
    backend/tests/phase04/test_synthesis_merge.py,
    backend/tests/phase04/test_warning_lockstep.py,
    backend/tests/phase04/test_synthesis_firestore_flat.py,
    backend/tests/phase04/test_synthesis_g4_guard.py
  </files>

  <read_first>
    Analogs (읽어야 할 순서):
    1. backend/tests/phase08_1/__init__.py — 빈 파일 패턴
    2. backend/tests/phase08_1/conftest.py — synthetic fixture + factory 패턴
    3. backend/tests/phase08/__init__.py (있으면) — pytest discovery 패턴
    4. backend/shared/python/sunity_shared/models.py (lines 220-260) — warning code frozenset 카탈로그 현재 상태 (ai_synthesis_failed 아직 없음 — RED 테스트 기준점)
    5. backend/shared/python/sunity_shared/analysis/temporal.py (모듈 헤더 + temporal_fill 시그니처) — POSE-03-c 재사용 목적
    6. 04-VALIDATION.md §Per-Task Verification Map — POSE-03-a~f 각 테스트 파일 역할 확인
    7. 04-RESEARCH.md §Validation Architecture §Wave 0 Gaps — 구체 파일 목록
    8. 04-PATTERNS.md §models.py — warning frozenset 추가 위치 패턴
  </read_first>

  <behavior>
    conftest.py:
    - fixture joint_seq_30f: np.ndarray shape (30, 17, 3), dtype float32, 값 0~1 normalized
    - fixture joint_seq_60f: np.ndarray shape (60, 17, 3), dtype float32, 값 0~1 normalized — evaluate_4way / cylindrical mesh 테스트(04-03, 04-05)가 T=60 기대
    - fixture conf_seq_30f: np.ndarray shape (30, 17), dtype float32, 값 0~1
    - fixture conf_seq_60f: np.ndarray shape (60, 17), dtype float32, 값 0~1
    - fixture low_conf_seq: conf_seq_30f 의 frame 10~20, joint 3~7 을 0.1 로 낮춘 버전 (occlusion 시뮬)
    - fixture scene_findings_occlusion: dict {"occlusion_severe": True, "camera_angle_problematic": False}
    - fixture scene_findings_clean: dict {"occlusion_severe": False, "camera_angle_problematic": False}

    test_confidence_gate.py (POSE-03-a):
    - test_synthesis_mask_low_confidence: conf < 0.3 frame → mask True
    - test_synthesis_mask_all_high: 모든 conf >= 0.5 → mask 전체 False
    - test_synthesis_mask_scene_occlusion: scene_findings occlusion_severe=True → 영향 frame mask True (인터페이스 미구현이면 ImportError → RED 허용)

    test_synthesis_adapter.py (POSE-03-b):
    - test_gemini_degrade: GeminiViewReasoner 가 Exception raise 하면 degrade wrapper 가 SynthesisResult(status="failed", joints=None, confidence=None, warnings=("ai_synthesis_failed",)) 를 반환한다. joints/confidence 는 None 이어야 하고 all-zero array 를 반환해서는 안 된다.
    - test_cylindrical_degrade: CylindricalMeshAdapter 가 Exception raise 하면 동일하게 SynthesisResult(status="failed", joints=None, confidence=None, warnings=("ai_synthesis_failed",)) 반환. zeros-sentinel 계약 아님.
    - 두 테스트 모두 unittest.mock.MagicMock 으로 adapter mock — 실 구현체 없어도 실행 가능. degrade wrapper 미구현이면 xfail.

    test_synthesis_merge.py (POSE-03-c):
    - test_merge_higher_confidence_wins: SynthesisResult(status="applied") 로 반환된 합성이 synth_conf > primary_conf 인 frame/joint 만 교체됨을 검증
    - test_merge_primary_unchanged_when_synth_lower: SynthesisResult(status="skipped") 또는 synth_conf <= primary_conf 이면 primary 유지 검증
    - test_merge_status_failed_skips_merge: SynthesisResult(status="failed") 는 merge 단계에서 primary 를 전혀 수정하지 않음 — joints/confidence None 이므로 merge 시도 자체 없어야 함
    - test_merge_status_partial_merges_subset: SynthesisResult(status="partial") 는 일부 frame 만 교체되고 나머지 primary 유지 검증 (4 케이스 모두 status 기반 분기 확인)
    - temporal.py 존재 확인 테스트 (import 만)

    test_warning_lockstep.py (POSE-03-d):
    - test_ai_synthesis_failed_in_frozenset: models.py 에 SYNTHESIS_WARNING_CODES frozenset 존재 + "ai_synthesis_failed" 포함 확인 (Wave 1 전 RED — models.py 미수정이므로 실패해야 함)
    - test_ai_synthesis_partial_in_frozenset: "ai_synthesis_partial" 포함 확인 (Wave 1 전 RED)
    - test_synthesis_warning_codes_type: frozenset 타입 확인

    test_synthesis_firestore_flat.py (POSE-03-e):
    - test_ai_synthesis_meta_no_nested_array: {"synthesizedJoints": [1.0, 2.0], "synthesizedJointsFrames": 1, "synthesizedJointsJointKeys": ["lShoulder"]} → _validate_flat_dict_no_nested_array 통과
    - test_ai_synthesis_meta_nested_array_rejected: {"synthesizedJoints": [[1.0], [2.0]]} → TypeError 발생
    - _validate_flat_dict_no_nested_array 는 firestore_admin.py 에서 import

    test_synthesis_g4_guard.py (POSE-03-f):
    - test_g4_guard_blocks_synthesis_for_reference: is_reference=True 인자가 있으면 SynthesisResult(status="skipped", warnings=("g4_reference_guard",)) 반환 (HIGH-2 — None 아님, R2 계약. 미구현이면 RED 허용)
    - test_g4_guard_allows_synthesis_for_user: is_reference=False 이면 합성 진행 (mock으로 검증)
  </behavior>

  <action>
    1. backend/tests/phase04/__init__.py 생성: 빈 파일 (backend/tests/phase08_1/__init__.py 패턴 정합)

    2. backend/tests/phase04/conftest.py 생성: from __future__ import annotations, numpy import, pytest fixture 7개 (joint_seq_30f, joint_seq_60f, conf_seq_30f, conf_seq_60f, low_conf_seq, scene_findings_occlusion, scene_findings_clean). dtype=float32, shape assertions 포함. 모듈 헤더에 "Phase 4 Wave 0 — synthetic fixture (Spike 002d 패턴 재사용). joint_seq_60f / conf_seq_60f 는 evaluate_4way (04-03 Task 2, 04-05 Task 2) 가 T=60 기대에 대응." 주석.

    3. test_confidence_gate.py: 함수 3개. 실제 identify_occlusion_targets 가 없으면 ImportError → pytest.importorskip 또는 @pytest.mark.xfail(reason="Wave 1 미구현") 처리해 RED 상태로 수집만 됨. Phase 04 conftest fixture 사용.

    4. test_synthesis_adapter.py: unittest.mock.MagicMock 사용. adapter.synthesize_occluded_joints side_effect=Exception("api fail") 설정 후 degrade wrapper 호출 — 반환값이 SynthesisResult(status="failed") 임을 assert. joints/confidence 가 None 인지 확인, all-zero array 가 아님을 확인(assert result.joints is None). degrade wrapper 미구현이면 xfail. POSE-03-b 2개 케이스(Gemini / Cylindrical).

    5. test_synthesis_merge.py: merge_with_temporal 인터페이스 미존재 시 xfail. conftest joint/conf fixture 재사용. 5개 테스트 — status별 분기 4케이스(applied/skipped/failed/partial) + temporal.py import 확인. SynthesisResult.status 값("applied", "skipped", "failed", "partial")으로 merge 경로가 분기됨을 검증. (None,None) 또는 zeros sentinel 로 분기하는 테스트 금지.

    6. test_warning_lockstep.py: models.py 에서 직접 import (from sunity_shared.models import ...). SYNTHESIS_WARNING_CODES frozenset 미존재 시 AttributeError → RED. Wave 1 전에는 반드시 RED 여야 함 (xfail 처리 금지 — 이 테스트는 Wave 1 완료를 증명하는 gate).

    7. test_synthesis_firestore_flat.py: firestore_admin._validate_flat_dict_no_nested_array 직접 import + 2개 테스트. 이 함수는 이미 존재하므로 test_ai_synthesis_meta_nested_array_rejected 는 Wave 0 시점부터 GREEN 가능.

    8. test_synthesis_g4_guard.py: _call_synthesis_adapter stub 미존재 시 xfail. is_reference=True 시 SynthesisResult(status="skipped") 반환 계약 검증 (HIGH-2 — None 아님).

    naming 규칙: 모든 함수 snake_case, 모듈 헤더 docstring (purpose + Phase 4 POSE-03 sub-req 박제).
    import 방식: 모든 sunity_shared import 는 try/except ImportError → pytest.skip("not implemented yet") 패턴 (xfail 과 구분 — collect error 방지).
  </action>

  <verify>
    <automated>
      cd /Users/kimtaesung/Dev/SunityMotion &&
      python -m pytest backend/tests/phase04/ --collect-only -q 2>&1 | tail -20
    </automated>
  </verify>

  <acceptance_criteria>
    - pytest --collect-only 가 phase04/ 에서 8개 파일 collect (0 errors)
    - test_warning_lockstep.py 의 test_ai_synthesis_failed_in_frozenset 은 현재 RED (SYNTHESIS_WARNING_CODES 미존재)
    - test_synthesis_firestore_flat.py 의 test_ai_synthesis_meta_nested_array_rejected 는 GREEN (firestore_admin._validate_flat_dict_no_nested_array 기존 구현)
    - 모든 파일 import error 0 (xfail / skip 사용으로 ImportError 흡수)
    - 전체 backend suite 회귀 0: pytest backend/tests/ -q --ignore=backend/tests/phase04 PASS 카운트 불변
    - conftest.py 에 joint_seq_60f fixture 존재: grep -c "joint_seq_60f" backend/tests/phase04/conftest.py | grep -v '^0$'
  </acceptance_criteria>

  <done>pytest --collect-only -q 가 phase04/ 에서 > 10개 test item 을 에러 없이 collect. test_warning_lockstep 은 RED (Wave 1 gate). joint_seq_60f fixture 존재. 전체 suite 회귀 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → sunity_shared | 테스트 코드가 models.py frozenset 을 직접 import — 미구현 시 AttributeError |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-W0-01 | Tampering | test fixtures | accept | 합성 test fixture 는 순수 numpy array — 외부 입력 없음, 조작 불가 |
| T-04-W0-SC | Tampering | npm/pip installs | accept | Wave 0 에서 신규 패키지 설치 없음 — 기존 pytest 환경만 사용 |
</threat_model>

<verification>
pytest backend/tests/phase04/ --collect-only -q → 8개 파일 collect, 0 errors
pytest backend/tests/ -q (기존 suite, phase04 포함) → 기존 PASS 카운트 불변 + phase04 파일 신규 추가분 추가
grep -c "joint_seq_60f" backend/tests/phase04/conftest.py | grep -v '^0$'
</verification>

<success_criteria>
Wave 0 완료 기준:
1. backend/tests/phase04/ 존재 + 8파일 모두 존재
2. pytest --collect-only 에러 0
3. POSE-03-a~f 각 파일이 1개 이상 test 를 수집
4. test_warning_lockstep 은 RED (Wave 1 전 게이트 역할 확인)
5. conftest.py joint_seq_60f fixture 존재 (04-03/04-05 T=60 fixture 의존성 충족)
6. 기존 backend 회귀 0
</success_criteria>

<output>
Create `.planning/phases/04-ux-occlusion-confidence/04-00-SUMMARY.md` when done
</output>
