---
phase: 04
plan: "01"
type: execute
wave: 1
depends_on:
  - 04-00
files_modified:
  - backend/shared/python/sunity_shared/analysis/synthesis/__init__.py
  - backend/shared/python/sunity_shared/analysis/synthesis/interfaces.py
  - backend/shared/python/sunity_shared/analysis/synthesis/gemini_view_reasoner.py
  - backend/shared/python/sunity_shared/models.py
  - backend/shared/python/sunity_shared/firestore_admin.py
  - backend/functions/pipeline/app.py
  - backend/tests/phase04/test_synthesis_result_status.py
  - app/src/types/analysis.ts
  - docs/contract.md
autonomous: true
requirements:
  - POSE-03

must_haves:
  truths:
    - "SynthesisResult dataclass 가 정의되고 GeminiViewReasoner 가 (zeros,zeros) 대신 SynthesisResult 를 반환한다 (D-18 PRIMARY — R2 fix)"
    - "pipeline 분기가 synth_result.status 기반이다 — failed=merge 안 함+ai_synthesis_failed, skipped=경고 없음, partial=표시된 관절만 merge+ai_synthesis_partial, applied=전체 merge (R2 fix)"
    - "models.py 에 SYNTHESIS_WARNING_CODES frozenset 이 추가되고 ai_synthesis_failed / ai_synthesis_partial 이 포함된다 (D-08)"
    - "pipeline/app.py 의 _process 가 confidence 미달 phase 시 GeminiViewReasoner 를 조건부 호출하고 실패 시 graceful degrade + ai_synthesis_failed warning 주입한다 (D-07)"
    - "G4 is_reference=True 시 합성 호출이 발생하지 않는다 (G4 occlusion FP 가드)"
    - "firestore_admin.complete_analysis 에 ai_synthesis_meta + joints3d 파라미터가 추가되고 flat 저장 validator 를 통과한다 (D-09 + R3 fix)"
    - "합성 output(SynthesisResult.joints/confidence)은 KeypointReport/aiSynthesisMeta/joints3d 에만 흐르고 DTW/kismam/IPSF coco_array 를 절대 mutate 하지 않는다 (R1 non-scoring 하드월)"
    - "contract.md §9.8 + models.py SYNTHESIS_WARNING_CODES + analysis.ts SynthesisWarningCode + AnalysisResult.joints3d 가 3-way lockstep 으로 동시 갱신된다 (R3 fix)"
  artifacts:
    - path: backend/shared/python/sunity_shared/analysis/synthesis/interfaces.py
      provides: "SynthesisResult dataclass + SynthesisAdapter Protocol + identify_occlusion_targets signature (R2/R6 fix)"
      contains: "class SynthesisResult"
    - path: backend/shared/python/sunity_shared/analysis/synthesis/gemini_view_reasoner.py
      provides: "GeminiViewReasoner — SynthesisResult 반환 (R2 fix)"
      contains: "OCCLUDED_JOINT_REASONING_PROMPT"
    - path: backend/shared/python/sunity_shared/models.py
      provides: "SYNTHESIS_WARNING_CODES frozenset"
      contains: "ai_synthesis_failed"
    - path: backend/shared/python/sunity_shared/firestore_admin.py
      provides: "complete_analysis ai_synthesis_meta + joints3d kwargs (R3 fix)"
      contains: "joints3d"
    - path: backend/functions/pipeline/app.py
      provides: "_call_synthesis_adapter wiring — status 기반 분기 (R2 fix)"
      contains: "_call_synthesis_adapter"
    - path: app/src/types/analysis.ts
      provides: "AiSynthesisMeta interface + SynthesisWarningCode union + AnalysisResult joints3d 필드 (R3 fix 3-way lockstep TS side)"
      contains: "AiSynthesisMeta"
  key_links:
    - from: backend/functions/pipeline/app.py
      to: backend/shared/python/sunity_shared/analysis/synthesis/gemini_view_reasoner.py
      via: "_call_synthesis_adapter lazy import → SynthesisResult.status 분기"
      pattern: "_call_synthesis_adapter"
    - from: backend/shared/python/sunity_shared/analysis/synthesis/gemini_view_reasoner.py
      to: backend/shared/python/sunity_shared/gemini/scene_finder.py
      via: "GeminiVisionCall + resolve_model 재사용"
      pattern: "GeminiVisionCall"
    - from: backend/functions/pipeline/app.py
      to: backend/shared/python/sunity_shared/models.py
      via: "extra_warnings + dataclasses.replace"
      pattern: "ai_synthesis_failed"
    - from: app/src/types/analysis.ts
      to: docs/contract.md
      via: "3-way lockstep (AiSynthesisMeta + joints3d mirror)"
      pattern: "AiSynthesisMeta"
    - from: backend/functions/pipeline/app.py
      to: backend/shared/python/sunity_shared/firestore_admin.py
      via: "complete_analysis joints3d flat kwarg (R3 fix — 04-02 가 reshapePose3dData 로 이 필드를 읽음)"
      pattern: "joints3d"

user_setup: []
---

<objective>
Phase 4 Wave 1 — Stage 1+2 backend 수직 슬라이스. SynthesisResult typed dataclass + SynthesisAdapter Protocol + GeminiViewReasoner (PRIMARY) 신설, models.py warning code 3-way lockstep, pipeline wiring, firestore meta 저장, 실 3D joint 좌표 Firestore 저장 신설.

이 plan 이 완료되면 분석 파이프라인이 confidence 미달 frame 을 감지하고 Gemini Vision 으로 조건부 보완하며 결과를 Firestore 에 flat 저장한다 (사용자는 모름 — 블랙박스 D-05).

합성 output 은 영구 non-scoring (UI/메타 전용). DTW/kismam/IPSF coco_array 에 흘러들어가지 않는다 (R1 하드월). promotion 은 별도 후속 phase.

Purpose: POSE-03 핵심 backend 구현. 점수 계산(DTW/kismam/IPSF)은 변경하지 않고 사용자 표시 신뢰도 + 가림 구간 설명 + 3D 시각적 검토 환경을 제공하는 수직 슬라이스 (R1 non-scoring 하드월 — score/metric improvement 아님, MEDIUM-3).
Output: synthesis/ 패키지 신설 + pipeline wiring + Firestore meta schema + TS AiSynthesisMeta/joints3d 3-way lockstep (D-08 R1 R2 R3 R6)
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-ux-occlusion-confidence/04-CONTEXT.md
@.planning/phases/04-ux-occlusion-confidence/04-RESEARCH.md
@.planning/phases/04-ux-occlusion-confidence/04-PATTERNS.md
@.planning/phases/04-ux-occlusion-confidence/04-VALIDATION.md
@.planning/ROADMAP.md

@backend/shared/python/sunity_shared/analysis/interfaces.py
@backend/shared/python/sunity_shared/gemini/scene_finder.py
@backend/shared/python/sunity_shared/models.py
@backend/shared/python/sunity_shared/firestore_admin.py
@backend/functions/pipeline/app.py
@docs/contract.md
@app/src/types/analysis.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SynthesisResult dataclass + SynthesisAdapter Protocol + GeminiViewReasoner + warning 3-way lockstep</name>
  <files>
    backend/shared/python/sunity_shared/analysis/synthesis/__init__.py,
    backend/shared/python/sunity_shared/analysis/synthesis/interfaces.py,
    backend/shared/python/sunity_shared/analysis/synthesis/gemini_view_reasoner.py,
    backend/shared/python/sunity_shared/models.py,
    app/src/types/analysis.ts,
    docs/contract.md
  </files>

  <read_first>
    필수:
    1. backend/shared/python/sunity_shared/analysis/interfaces.py (전체) — PoseEngine Protocol 패턴 복사 기준
    2. backend/shared/python/sunity_shared/gemini/scene_finder.py (전체) — GeminiVisionCall 재사용 + G4 가드 + graceful degrade 패턴
    3. backend/shared/python/sunity_shared/models.py (lines 220-260) — warning code frozenset 카탈로그 현재 상태 + 추가 위치
    4. backend/shared/python/sunity_shared/analysis/temporal.py (lines 52-79) — occluded_mask: a.ndim != 2 raise 확인 (R6 재사용 금지 근거)
    5. 04-PATTERNS.md §SynthesisAdapter 신규 선언 — interfaces.py 작성 기준
    6. 04-PATTERNS.md §gemini_view_reasoner.py — scene_finder 패턴 복사 기준 + Gemini 프롬프트 박제
    7. 04-RESEARCH.md §Pattern 1: Synthesis Adapter Protocol — SynthesisAdapter 시그니처
    8. 04-RESEARCH.md §Code Examples — OCCLUDED_JOINT_REASONING_PROMPT 전문
    9. app/src/types/analysis.ts (lines 1-60 + AnalysisResult 185-224) — AiSynthesisMeta 추가 위치 + AnalysisResult joints3d 추가 위치 (BLOCKER-1)
    10. docs/contract.md §9.8 — warning code enum 현재 항목 + Phase 4 신설 2개 추가 위치
    11. 04-CONTEXT.md D-18, D-20, D-07, D-08 — PRIMARY path + license + degrade 정책
  </read_first>

  <behavior>
    test_warning_lockstep.py (Wave 0):
    - test_ai_synthesis_failed_in_frozenset: models.py SYNTHESIS_WARNING_CODES frozenset 에 "ai_synthesis_failed" 포함 → GREEN
    - test_ai_synthesis_partial_in_frozenset: "ai_synthesis_partial" 포함 → GREEN

    test_synthesis_result_status.py (Wave 0 — R2 fix):
    - test_synthesis_result_failed_is_not_merged: SynthesisResult(status="failed") 시 pipeline 이 merge 를 시도하지 않고 ai_synthesis_failed warning 을 추가한다
    - test_synthesis_result_skipped_no_warning: SynthesisResult(status="skipped") 시 warning 추가 없음
    - test_synthesis_result_applied_has_joints: SynthesisResult(status="applied", joints=arr, confidence=arr) 시 joints/confidence 모두 non-None

    synthesis/interfaces.py (SynthesisResult + SynthesisAdapter + identify_occlusion_targets):
    - SynthesisResult: @dataclass(frozen=True), 필드 = status: Literal["applied","partial","skipped","failed"] / joints: np.ndarray|None = None / confidence: np.ndarray|None = None / warnings: tuple[str,...] = () / meta: dict = field(default_factory=dict). all-zero array 를 실패 sentinel 로 쓰지 않는다 (R2 fix).
    - SynthesisAdapter Protocol: synthesize_occluded_joints(joint_sequence, confidence_sequence, occlusion_mask, scene_findings) → SynthesisResult (R2 fix — 반환 타입 변경)
    - identify_occlusion_targets(keypoint_confidence, scene_flags, phase_boundaries, threshold=0.3) → np.ndarray bool mask shape (T,17). 입력: keypoint confidence matrix (T,17) + scene flags + phase boundaries. temporal.occluded_mask 와 무관 — 그쪽은 angles (T,J) 2D 전용이고 (T,17,3) 형상 전달 시 raise 한다 (R6 fix, temporal.py:65-66 확인). 새 함수는 keypoint confidence 를 직접 소비한다.
    - merge_with_temporal(primary_joints, primary_conf, synth_result, synthesis_mask) → tuple[ndarray, ndarray]. SynthesisResult.status == "applied"|"partial" 인 경우만 merge. better = synthesis_mask & (synth_result.confidence > primary_conf) 로직 (RESEARCH.md Pattern 2). status == "failed"|"skipped" 시 primary 그대로 반환.
    - 모든 함수 from __future__ import annotations + numpy type hints + 모듈 docstring

    synthesis/gemini_view_reasoner.py (Stage 1 PRIMARY — R2 fix SynthesisResult 반환):
    - GeminiViewReasoner 클래스 (SynthesisAdapter 구현)
    - synthesize_occluded_joints 반환 타입 = SynthesisResult (R2 fix — zeros 반환 폐기)
    - API 실패 / None 응답 → SynthesisResult(status="failed", warnings=("gemini_api_error",))
    - JSON 파싱 실패 → SynthesisResult(status="failed", warnings=("gemini_parse_error",))
    - indeterminate 응답 관절 → conf=0.0 처리, status="partial"
    - 모든 관절 정상 추론 → status="applied"
    - SYNTHESIS_ENABLED=0 에서 호출 시 → SynthesisResult(status="skipped")
    - is_reference=True guard → SynthesisResult(status="skipped", warnings=("g4_reference_guard",))
    - OCCLUDED_JOINT_REASONING_PROMPT: Spike 003 박제 프롬프트 그대로 (한국어 폴스포츠 도메인 컨텍스트 포함)
    - 모델 string: resolve_model("C", env_override="GEMINI_C_MODEL_OVERRIDE") 재사용 (Vision 영역 gemini-2.5-pro 일시 허용 — 3.x 강제 금지, Google Vision 3.x 미출시)
    - log = logging.getLogger(__name__)

    models.py 수정:
    - SYNTHESIS_WARNING_CODES: frozenset[str] = frozenset({"ai_synthesis_failed", "ai_synthesis_partial"}) 신규 선언
    - 기존 warning 카탈로그 주석 블록에 Phase 4 신설 2개 추가 (contract.md §9.8 mirror)

    analysis.ts 수정:
    - SynthesisWarningCode = 'ai_synthesis_failed' | 'ai_synthesis_partial' string-literal union
    - AiSynthesisMeta interface: synthesizedFrameCount, synthesizedJointKeys, synthesisPath ('gemini_view' | 'cylindrical_mesh' | 'none'), degraded boolean + 감사 필드(modelId: string, modelVersion: string, promptHash: string) + cost 카운터(framesConsidered: number, framesSynthesized: number, geminiCalls: number, framesSkipped: number, framesFailed: number, estCostUsd: number) (R5 fix)
    - AnalysisResult 에 aiSynthesisMeta?: AiSynthesisMeta optional 필드 추가
    - 파일 헤더 주석 갱신: "이 파일이 바뀌면 docs/contract.md 와 백엔드도 같이 맞춰야 함." (이미 있음 — 확인만)

    docs/contract.md §9.8 수정:
    - warning code 테이블에 "ai_synthesis_failed" / "ai_synthesis_partial" 행 추가 (Phase 4 신설 표기)
    - AiSynthesisMeta 스키마 섹션 신설: 감사 필드(modelId/modelVersion/promptHash) + cost 카운터 6개 명시 (R5 fix)
  </behavior>

  <action>
    1. synthesis/__init__.py 생성: 빈 파일 (패키지 선언).

    2. synthesis/interfaces.py 생성: PoseEngine Protocol 패턴 정확히 복사해 SynthesisResult dataclass + SynthesisAdapter Protocol 선언.
       - SynthesisResult: @dataclass(frozen=True), 5 필드 — status(Literal["applied","partial","skipped","failed"]) / joints(np.ndarray|None=None) / confidence(np.ndarray|None=None) / warnings(tuple[str,...]=()) / meta(dict=field(default_factory=dict)). 모듈 docstring에 "all-zero array 를 실패 sentinel 로 쓰지 않는다" 명시 (R2 fix).
       - SynthesisAdapter Protocol: synthesize_occluded_joints 반환 타입 = SynthesisResult.
       - identify_occlusion_targets(keypoint_confidence: np.ndarray, scene_flags: dict, phase_boundaries: list, threshold: float = 0.3) → np.ndarray bool mask (T,17). 함수 docstring: "Phase 4 전용 occlusion target detector. 입력 = keypoint confidence matrix (T,17) + scene flags + phase boundaries. temporal.occluded_mask 재사용 금지 — occluded_mask 는 angles (T,J) 2차원 전용이고 (T,17,3) 형상 전달 시 ValueError 를 raise 한다 (R6 fix, temporal.py:65-66)." D-04 (a)(b)(c)(d) 4가지 트리거를 mask_a | mask_b | mask_c 로 결합, 초기 임계값 threshold: float = 0.3 (A1 + Claude's Discretion).
       - merge_with_temporal: SynthesisResult 파라미터로 변경. status "failed"|"skipped" 시 primary 그대로 반환. 픽셀 합성 X, 좌표 추정만 (D-18 Anti-Pattern 주석).

    3. synthesis/gemini_view_reasoner.py 생성: scene_finder.py 의 import 구조 + GeminiVisionCall + resolve_model 재사용. G4 가드 (is_reference=True → SynthesisResult(status="skipped", warnings=("g4_reference_guard",)) + log.info, 근거: G4 occlusion FP 가드). synthesize_occluded_joints 에서 OCCLUDED_JOINT_REASONING_PROMPT 를 Spike 003 박제 그대로 사용. 반환은 항상 SynthesisResult — (zeros, zeros) 절대 반환 금지 (R2 fix). JSON 파싱 실패/API 예외 → SynthesisResult(status="failed", warnings=(...)). indeterminate 관절 → conf=0.0, status="partial". 모든 except Exception: log.exception + SynthesisResult(status="failed", warnings=("exception",)).

    4. models.py 수정: SYNTHESIS_WARNING_CODES frozenset 추가. warning code 주석 갱신 (D-08). 기존 코드 일체 변경 금지.

    5. analysis.ts 수정: SynthesisWarningCode + AiSynthesisMeta interface (감사 필드 + cost 카운터 6개 포함, R5 fix) + AnalysisResult.aiSynthesisMeta? optional 필드. 3-way lockstep 주석. 기존 interface 필드 변경 금지.

    6. docs/contract.md §9.8 수정: Phase 4 신설 2개 warning code 행 추가 + AiSynthesisMeta 감사/cost 필드 스키마 명시 (R5 fix). 원본 형식 그대로 유지.

    Commit: 단일 atomic commit "feat(04-01): SynthesisResult typed dataclass + SynthesisAdapter + GeminiViewReasoner + warning 3-way lockstep (POSE-03 D-08 R1 R2 R6)"
  </action>

  <verify>
    <automated>
      cd /Users/kimtaesung/Dev/SunityMotion &&
      python -m pytest backend/tests/phase04/test_warning_lockstep.py backend/tests/phase04/test_synthesis_result_status.py -x -q 2>&1 | tail -15
    </automated>
  </verify>

  <acceptance_criteria>
    - test_warning_lockstep.py GREEN (SYNTHESIS_WARNING_CODES 존재 + 2개 code 포함)
    - test_synthesis_result_status.py GREEN (R2 fix — status 분기 검증)
    - python -c "from sunity_shared.analysis.synthesis.interfaces import SynthesisResult, SynthesisAdapter, identify_occlusion_targets, merge_with_temporal" 성공
    - python -c "from sunity_shared.analysis.synthesis.gemini_view_reasoner import GeminiViewReasoner" 성공
    - npm run typecheck (app/) — 0 errors (AiSynthesisMeta interface 추가 후)
    - grep -c "ai_synthesis_failed" docs/contract.md | grep -v '^0$'
    - grep -c "AiSynthesisMeta" app/src/types/analysis.ts | grep -v '^0$'
    - grep -c "modelId" app/src/types/analysis.ts | grep -v '^0$' (R5 감사 필드 존재 확인)
    - NON-SCORING 하드월: grep -v '^#' backend/shared/python/sunity_shared/analysis/synthesis/interfaces.py | grep -c "occluded_mask" — 반드시 0 (temporal.occluded_mask 참조 없음, R6 fix)
  </acceptance_criteria>

  <done>SynthesisResult typed dataclass GREEN + SYNTHESIS_WARNING_CODES frozenset 3-way lockstep (models.py + analysis.ts + contract.md) 모두 갱신 + synthesis 패키지 import 성공 + AiSynthesisMeta + 감사/cost 필드 TS 추가 + occluded_mask 재사용 0건 확인.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: joints3d Firestore 저장 신설 + firestore_admin kwargs + pipeline _call_synthesis_adapter wiring</name>
  <files>
    backend/shared/python/sunity_shared/firestore_admin.py,
    backend/functions/pipeline/app.py,
    app/src/types/analysis.ts,
    docs/contract.md
  </files>

  <read_first>
    필수:
    1. backend/shared/python/sunity_shared/firestore_admin.py (lines 600-680) — complete_analysis 시그니처 + keypoint_report 파라미터 추가 패턴
    2. backend/shared/python/sunity_shared/firestore_admin.py (lines 45-101) — _validate_flat_dict_no_nested_array 함수 (재사용)
    3. backend/functions/pipeline/app.py (lines 1837-1852) — complete_analysis 호출 지점 (현재 kwarg 목록 + joints3d 추가 위치)
    4. backend/functions/pipeline/app.py (lines 320-370) — _call_wave1_scene_finder + _call_wave2_keypoint_augmenter wave-gate 패턴
    5. 04-PATTERNS.md §pipeline/app.py — _call_synthesis_adapter 구현 패턴 (G4 가드 + SYNTHESIS_ENABLED env + extra_warnings injection)
    6. 04-PATTERNS.md §firestore_admin.py — aiSynthesisMeta 추가 패턴
    7. 04-RESEARCH.md §Pattern 3: Graceful Degrade + Warning Injection — dataclasses.replace
    8. 04-RESEARCH.md §Pitfall 4 — Firestore nested-array 금지 (joints3d flat 저장)
    9. 04-CONTEXT.md D-07, D-08, D-03 — degrade / warning / 조건부 트리거 정책
    10. app/src/types/analysis.ts (lines 185-224) — AnalysisResult 정의 (joints3d 필드 추가 위치. AnalysisDoc top-level 아님 — BLOCKER-1)
    11. docs/contract.md — §9.x 섹션 구조 확인 (joints3d 계약 박제 위치)
  </read_first>

  <behavior>
    test_synthesis_g4_guard.py (Wave 0):
    - test_g4_guard_blocks_synthesis_for_reference: _call_synthesis_adapter(is_reference=True) → SynthesisResult(status="skipped") (R2 fix — None 대신 SynthesisResult)

    test_synthesis_firestore_flat.py (Wave 0):
    - test_ai_synthesis_meta_no_nested_array: flat dict → _validate_flat_dict_no_nested_array 통과
    - test_ai_synthesis_meta_nested_array_rejected: nested → TypeError
    - test_joints3d_flat_no_nested_array: joints3d flat list → validator 통과 (R3 fix)
    - test_joints3d_nested_rejected: nested array → validator 차단

    firestore_admin.complete_analysis (R3 fix — joints3d 신설):
    - ai_synthesis_meta: dict | None = None 파라미터 추가
    - joints3d: list[float] | None = None 파라미터 추가 (T×17×3 flat)
    - joints3d_keys: list[str] | None = None (RTMW 17 keypoint 이름, length 17)
    - joints3d_frames: int | None = None (T)
    - coord_dim: int | None = None (=3, 명시적 저장)
    - space: str | None = None ("rtmw3d" | "pole_aligned")
    - 저장 경로 (BLOCKER-1 — result 내부. angles 가 top-level 인 건 reference doc 호환 quirk 이고, joints3d 는 analysis 산출물이므로 result 에 둠. 04-02 가 doc.result.joints3d 로 읽으니 위치 일치): payload["result"]["joints3d"] / payload["result"]["joints3dKeys"] / payload["result"]["joints3dFrames"] / payload["result"]["coordDim"] / payload["result"]["space"].
    - 전용 validator _validate_joints3d_payload(joints3d, joints3d_keys, joints3d_frames, coord_dim, space) 통과 필수 (BLOCKER-4 — 범용 _validate_flat_dict_no_nested_array 우회 아님): flat length == frames*len(keys)*coord_dim, finite only, coord_dim==3, space in {"rtmw3d","pole_aligned"}.
    - ai_synthesis_meta is not None → _validate_flat_dict_no_nested_array(ai_synthesis_meta, path="aiSynthesisMeta") → payload["result"]["aiSynthesisMeta"] = ai_synthesis_meta (warnings 필드 포함 — BLOCKER-3 canonical surface)
    - 기존 파라미터 순서·기본값 변경 금지

    pipeline/app.py (_call_synthesis_adapter — R2 fix status 기반 분기):
    - is_reference=True → _call_synthesis_adapter 자체를 건너뜀 (G4 가드 최우선)
    - SYNTHESIS_ENABLED=0 → SynthesisResult(status="skipped") 반환
    - identify_occlusion_targets 호출 → mask.any() = False → SynthesisResult(status="skipped")
    - GeminiViewReasoner.synthesize_occluded_joints 호출 → SynthesisResult 수신
    - status == "failed" → ai_synthesis_meta["warnings"] 에 "ai_synthesis_failed" 추가 (canonical = result.aiSynthesisMeta.warnings, BLOCKER-3 — profile.extra_warnings 에 넣지 않음, warning surface 단일화) + merge 없음
    - status == "partial" → ai_synthesis_meta["warnings"] 에 "ai_synthesis_partial" 추가 + merge_with_temporal(partial 관절만)
    - status == "applied" → merge_with_temporal(전체) + warning 없음
    - status == "skipped" → warning 없음
    - 합성 output 은 KeypointReport/aiSynthesisMeta/joints3d 에만 흐름 — coco_array (DTW/kismam 입력) 는 절대 변경 금지 (R1 non-scoring 하드월)

    pipeline joints3d flat 저장 (R3 fix + BLOCKER-4 source 정정):
    - 현재 pipeline 에 keypoints_3d ndarray 없음. source = inputs.keypoints_4ch[:, :, :3] (shape (T,17,3) — 4번째 채널 uncertainty_proxy 제외). to_coco17_array(pose_frames) 산출 (_VideoAnalysisInputs.keypoints_4ch).
    - .reshape(-1).tolist() flat 전달 (length T*17*3).
    - joints3d_keys = COCO-17 17 keypoint 순서 (skeleton COCO-17 KEYPOINT_NAMES — angle 용 8개 JOINT_KEYS 아님, BLOCKER-4).
    - joints3d_frames = T
    - coord_dim = 3, space = "pole_aligned" (BLOCKER-1 정정: to_coco17_array 가 keypoints_3d_pole_aligned 에서 산출하므로 좌표계 = pole_aligned. "rtmw3d" 아님 — pose_frame.py to_coco17_array docstring 확인). 주의: RTMW 2D path 에서 z degenerate 가능 — viewer 는 평면 투영 허용(MVP).

    analysis.ts AnalysisResult (R3 fix 3-way lockstep — BLOCKER-1: AnalysisDoc top-level 아니라 AnalysisResult 내부):
    - joints3d?: number[] — 길이 = joints3dFrames * 17 * 3 (T×17×3 flat). 04-02 가 doc.result.joints3d → reshapePose3dData 로 읽음
    - joints3dKeys?: string[] — 길이 17 (COCO-17)
    - joints3dFrames?: number — T
    - coordDim?: number — =3
    - space?: 'rtmw3d' | 'pole_aligned'
    - AiSynthesisMeta.warnings?: SynthesisWarningCode[] — canonical warning surface (BLOCKER-3). UI 는 hasSynthesisWarning(result) 로 읽음 (result.warnings top-level 신설 안 함)
  </behavior>

  <action>
    1. firestore_admin.py 수정: complete_analysis 시그니처 끝에 ai_synthesis_meta: dict | None = None + joints3d: list | None = None + joints3d_keys: list | None = None + joints3d_frames: int | None = None + coord_dim: int | None = None + space: str | None = None 파라미터 추가 (default-after-non-default 회피). 전용 validator _validate_joints3d_payload 신설 (flat length == frames*len(keys)*coord_dim, finite only, coord_dim==3, space in {"rtmw3d","pole_aligned"} — BLOCKER-4). payload 조립 블록에 조건부 저장 — joints3d is not None: _validate_joints3d_payload 통과 후 **result 내부** 저장 (BLOCKER-1): payload["result"]["joints3d"]=joints3d / payload["result"]["joints3dKeys"]=joints3d_keys / payload["result"]["joints3dFrames"]=joints3d_frames / payload["result"]["coordDim"]=coord_dim / payload["result"]["space"]=space. ai_synthesis_meta is not None: _validate_flat_dict_no_nested_array + payload["result"]["aiSynthesisMeta"]=ai_synthesis_meta. 기존 코드 일체 변경 금지.

    2. pipeline/app.py 수정:
       a. _call_synthesis_adapter 함수 신설 (wave-gate 패턴 정합). 함수 docstring: "Phase 4 — SynthesisResult 기반 조건부 합성 (D-03/D-07 R2 fix). G4 가드: is_reference=True 시 skipped 반환. 합성 output 은 KeypointReport/aiSynthesisMeta/joints3d 에만 — coco_array mutate 금지 (R1 non-scoring 하드월)." is_reference 체크 최우선, 그 다음 SYNTHESIS_ENABLED env 체크.
       b. _synthesis_enabled() 헬퍼: os.environ.get("SYNTHESIS_ENABLED", "0") truthy 판정 (default OFF).
       c. _get_synthesis_adapter() lazy singleton 헬퍼: 모듈레벨 _SYNTHESIS_ADAPTER + lazy init GeminiViewReasoner.
       d. _process 함수 내: RTMW 분석 완료 + scene_findings 산출 후, _call_synthesis_adapter 1줄 호출. synth_result.status 기반 분기 (R2 fix). warning 은 profile.extra_warnings 가 아니라 ai_synthesis_meta["warnings"] 리스트에 누적 (BLOCKER-3 canonical surface). complete_analysis 호출 시 ai_synthesis_meta= + joints3d= + joints3d_keys= + joints3d_frames= + coord_dim= + space= kwarg 주입 (R3 fix).
       e. joints3d flat 변환 (BLOCKER-4): src = inputs.keypoints_4ch[:, :, :3] (T,17,3 — 4ch uncertainty 제외). np.asarray(src).reshape(-1).tolist(). keypoints_4ch 는 _VideoAnalysisInputs (to_coco17_array 산출, lines ~937-963) 에서 이미 보유 — keypoints_3d 변수는 존재하지 않음. complete_analysis 호출 직전(lines ~1841) 에서 추출.

    3. analysis.ts 수정 (BLOCKER-1: AnalysisResult 내부, AnalysisDoc top-level 아님): AnalysisResult 에 joints3d? / joints3dKeys? / joints3dFrames? / coordDim? / space? 필드 추가 + 주석 "R3 fix — 04-02 가 doc.result.joints3d 를 reshapePose3dData 로 읽음. angles(관절각, AnalysisDoc top-level quirk) 와 별개. flat 저장 (T×17×3 → length joints3dFrames*51)." + AiSynthesisMeta 인터페이스에 warnings?: SynthesisWarningCode[] 추가 (BLOCKER-3 canonical warning surface). 기존 필드 변경 금지.

    4. docs/contract.md: joints3d flat 저장 스키마 섹션 신설 (§ 번호는 기존 구조 뒤에 배치). joints3d/joints3dKeys/joints3dFrames/coordDim/space 필드 정의 + "04-02 가 reshapePose3dData 로 소비" 메모. 원본 형식 그대로.

    Commit 1: "feat(04-01): firestore_admin joints3d flat kwargs + ai_synthesis_meta (R3 fix D-09)"
    Commit 2: "feat(04-01): pipeline _call_synthesis_adapter SynthesisResult-based wiring (R2 R1 fix D-07)"
  </action>

  <verify>
    <automated>
      cd /Users/kimtaesung/Dev/SunityMotion &&
      python -m pytest backend/tests/phase04/ -x -q 2>&1 | tail -15 &&
      python -m pytest backend/tests/ -q --ignore=backend/tests/phase04 2>&1 | tail -5
    </automated>
  </verify>

  <acceptance_criteria>
    - backend/tests/phase04/ 전체 — POSE-03-a/b/c/d/e/f xfail 제외 모두 GREEN
    - test_synthesis_g4_guard.py::test_g4_guard_blocks_synthesis_for_reference GREEN (is_reference=True → SynthesisResult.status=="skipped")
    - test_synthesis_firestore_flat.py 4개 모두 GREEN (ai_synthesis_meta + joints3d flat validator)
    - 기존 backend suite (phase04 제외) 회귀 0
    - python -c "from sunity_shared.firestore_admin import complete_analysis; import inspect; sig=inspect.signature(complete_analysis); print(all(f in sig.parameters for f in ['ai_synthesis_meta','joints3d','joints3d_keys','joints3d_frames','coord_dim','space']))" → True
    - grep -c "ai_synthesis_failed" backend/functions/pipeline/app.py | grep -v '^0$'
    - grep -c "joints3d" backend/shared/python/sunity_shared/firestore_admin.py | grep -v '^0$'
    - grep -c "joints3d" app/src/types/analysis.ts | grep -v '^0$'
    - NON-SCORING 하드월: grep -c "coco_array" backend/shared/python/sunity_shared/analysis/synthesis/interfaces.py 이 0 이어야 함 (합성이 coco_array 참조 금지)
  </acceptance_criteria>

  <done>firestore_admin.complete_analysis joints3d + ai_synthesis_meta 파라미터 존재 + pipeline _call_synthesis_adapter SynthesisResult status 기반 분기 완료 + phase04 suite GREEN (xfail 제외) + 기존 회귀 0 + AnalysisResult joints3d 필드 TS/contract.md 3-way lockstep.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RunPod → Gemini API | Gemini API key 가 Pod env 에서 HTTPS 로 전송됨 |
| Gemini 응답 → joint 좌표 | Gemini 출력 JSON 이 SynthesisResult.joints 를 통해 KeypointReport/joints3d 에 흘러들어감 — DTW/kismam/IPSF coco_array 에 절대 흘러들어가지 않음 (R1 non-scoring 하드월) |
| user video → pipeline | 사용자 영상이 Gemini Vision 에 전달됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-W1-01 | Information Disclosure | Gemini API key (GEMINI_API_KEY) | mitigate | AWS SSM Parameter Store 주입, log.exception 에서 API key 마스킹 — log 에 직접 출력 금지. CLAUDE.md §3 시크릿 정책 정합. |
| T-04-W1-02 | Tampering | Gemini hallucinated coordinates → SynthesisResult.joints | mitigate | 출력 좌표 0~1 clamp + "indeterminate" → conf=0.0. merge_with_temporal 이 synth_conf > primary_conf 조건 기반이므로 낮은 confidence 합성은 자동 제거. DTW/kismam/IPSF coco_array 에 합성 좌표 주입 금지 (R1 non-scoring 하드월 — RESEARCH.md Pitfall 7). |
| T-04-W1-03 | Elevation of Privilege | G4 guard bypass (합성이 reference 오염) | mitigate | is_reference=True 체크가 _call_synthesis_adapter 최우선 분기 → SynthesisResult(status="skipped"). test_synthesis_g4_guard 가 회귀 게이트. |
| T-04-W1-04 | Denial of Service | Gemini API timeout/rate-limit | mitigate | SYNTHESIS_ENABLED env default="0" (운영 안전 스위치). graceful degrade → SynthesisResult(status="failed") + ai_synthesis_failed warning. |
| T-04-W1-05 | Tampering | SynthesisResult.status="failed" 가 pipeline 에서 성공으로 분기 | mitigate | R2 fix — status 기반 명시적 분기. "failed" 시 merge 없음, ai_synthesis_failed 추가 강제. test_synthesis_result_status.py 가 회귀 게이트. |
| T-04-W1-SC | Tampering | pip installs (synthesis/ 신규 모듈) | accept | synthesis 패키지는 신규 .py 파일 신설 — 외부 패키지 설치 없음. |
</threat_model>

<verification>
pytest backend/tests/phase04/ -x -q → POSE-03-d GREEN (warning lockstep) + POSE-03-e/f GREEN + test_synthesis_result_status.py GREEN (R2 fix)
pytest backend/tests/ -q → 기존 suite PASS 카운트 불변
npm run typecheck (app/) → 0 errors (AiSynthesisMeta + AnalysisResult joints3d 추가 후)
grep "ai_synthesis_failed" backend/shared/python/sunity_shared/models.py docs/contract.md app/src/types/analysis.ts — 3파일 모두 발견
grep "AiSynthesisMeta" app/src/types/analysis.ts — 발견 (3-way lockstep TS side)
grep "joints3d" app/src/types/analysis.ts backend/shared/python/sunity_shared/firestore_admin.py docs/contract.md — 3파일 모두 발견 (R3 fix 3-way lockstep)
grep -v '^#' backend/shared/python/sunity_shared/analysis/synthesis/interfaces.py | grep -c "occluded_mask" — 0 (R6 fix 준수)
</verification>

<success_criteria>
Wave 1 완료 기준 (ROADMAP SC #2 + SC #3 + SC #5 관련):
1. SynthesisResult typed dataclass + SynthesisAdapter Protocol + GeminiViewReasoner 구현 완료 (R2 fix — zeros sentinel 폐기)
2. SYNTHESIS_WARNING_CODES frozenset 3-way lockstep 완료 (models.py + analysis.ts + contract.md)
3. pipeline _call_synthesis_adapter: SynthesisResult.status 기반 4-way 분기 + G4 occlusion FP 가드 + SYNTHESIS_ENABLED env OFF default + graceful degrade (R2 fix)
4. identify_occlusion_targets Phase 4 전용 함수: temporal.occluded_mask 재사용 없음 (R6 fix)
5. firestore_admin.complete_analysis: ai_synthesis_meta flat 저장 + joints3d flat 저장 신설 (R3 fix — 04-02 reshapePose3dData 소비 계약)
6. AnalysisResult.joints3d 필드 TS + contract.md 3-way lockstep (R3 fix)
7. AiSynthesisMeta 감사 필드(modelId/modelVersion/promptHash) + cost 카운터 6개 (R5 fix)
8. 합성 output 은 KeypointReport/aiSynthesisMeta/joints3d 에만 흐름 — DTW/kismam/IPSF coco_array mutate 없음 (R1 non-scoring 하드월)
9. phase04 test suite POSE-03-a~f GREEN (xfail 제외) + test_synthesis_result_status.py GREEN
10. 기존 backend suite 회귀 0 + npm run typecheck 0 errors
</success_criteria>

<output>
Create `.planning/phases/04-ux-occlusion-confidence/04-01-SUMMARY.md` when done
</output>
