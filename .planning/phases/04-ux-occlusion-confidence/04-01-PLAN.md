---
phase: 04
plan: "01"
type: execute
wave: 1
depends_on:
  - 04-00
files_modified:
  - backend/shared/python/sunity_shared/analysis/synthesis/__init__.py
  - backend/shared/python/sunity_shared/analysis/synthesis/interfaces.py
  - backend/shared/python/sunity_shared/analysis/synthesis/gemini_view_reasoner.py
  - backend/shared/python/sunity_shared/models.py
  - backend/shared/python/sunity_shared/firestore_admin.py
  - backend/functions/pipeline/app.py
  - app/src/types/analysis.ts
  - docs/contract.md
autonomous: true
requirements:
  - POSE-03

must_haves:
  truths:
    - "SynthesisAdapter Protocol 이 정의되고 GeminiViewReasoner 가 구현한다 (D-18 PRIMARY — Spike 003, per D-18)"
    - "models.py 에 SYNTHESIS_WARNING_CODES frozenset 이 추가되고 ai_synthesis_failed / ai_synthesis_partial 이 포함된다 (D-08, per D-08)"
    - "pipeline/app.py 의 _process 가 confidence 미달 phase 시 GeminiViewReasoner 를 조건부 호출하고 실패 시 graceful degrade + ai_synthesis_failed warning 주입한다 (D-07, per D-07)"
    - "G4 is_reference=True 시 합성 호출이 발생하지 않는다 (G4 occlusion FP 가드 — 04-CONTEXT code_context)"
    - "firestore_admin.complete_analysis 에 ai_synthesis_meta 파라미터가 추가되고 flat 저장 validator 를 통과한다 (per D-09)"
    - "contract.md §9.8 + models.py SYNTHESIS_WARNING_CODES + analysis.ts SynthesisWarningCode 가 3-way lockstep 으로 동시 갱신된다"
  artifacts:
    - path: backend/shared/python/sunity_shared/analysis/synthesis/interfaces.py
      provides: "SynthesisAdapter Protocol + identify_synthesis_targets signature"
      contains: "class SynthesisAdapter(Protocol)"
    - path: backend/shared/python/sunity_shared/analysis/synthesis/gemini_view_reasoner.py
      provides: "GeminiViewReasoner (Stage 1 PRIMARY adapter)"
      contains: "OCCLUDED_JOINT_REASONING_PROMPT"
    - path: backend/shared/python/sunity_shared/models.py
      provides: "SYNTHESIS_WARNING_CODES frozenset"
      contains: "ai_synthesis_failed"
    - path: backend/shared/python/sunity_shared/firestore_admin.py
      provides: "complete_analysis ai_synthesis_meta kwarg"
      contains: "ai_synthesis_meta"
    - path: backend/functions/pipeline/app.py
      provides: "_call_synthesis_adapter wiring"
      contains: "_call_synthesis_adapter"
    - path: app/src/types/analysis.ts
      provides: "AiSynthesisMeta interface + SynthesisWarningCode union (3-way lockstep TS side)"
      contains: "AiSynthesisMeta"
  key_links:
    - from: backend/functions/pipeline/app.py
      to: backend/shared/python/sunity_shared/analysis/synthesis/gemini_view_reasoner.py
      via: "_call_synthesis_adapter lazy import"
      pattern: "_call_synthesis_adapter"
    - from: backend/shared/python/sunity_shared/analysis/synthesis/gemini_view_reasoner.py
      to: backend/shared/python/sunity_shared/gemini/scene_finder.py
      via: "GeminiVisionCall + resolve_model 재사용"
      pattern: "GeminiVisionCall"
    - from: backend/functions/pipeline/app.py
      to: backend/shared/python/sunity_shared/models.py
      via: "extra_warnings + dataclasses.replace"
      pattern: "ai_synthesis_failed"
    - from: app/src/types/analysis.ts
      to: docs/contract.md
      via: "3-way lockstep (AiSynthesisMeta mirror)"
      pattern: "AiSynthesisMeta"

user_setup: []
---

<objective>
Phase 4 Wave 1 — Stage 1+2 backend 수직 슬라이스. Synthesis adapter Protocol + GeminiViewReasoner (PRIMARY) 신설, models.py warning code 3-way lockstep, pipeline wiring, firestore meta 저장. 이 plan 이 완료되면 분석 파이프라인이 confidence 미달 frame 을 감지하고 Gemini Vision 으로 조건부 보완하며 결과를 Firestore 에 flat 저장한다 (사용자는 모름 — 블랙박스 D-05).

Purpose: POSE-03 핵심 backend 구현. 사용자 UX 변경 없이 분석 정확도가 향상되는 수직 슬라이스.
Output: synthesis/ 패키지 신설 + pipeline wiring + Firestore meta schema + TS AiSynthesisMeta 3-way lockstep
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-ux-occlusion-confidence/04-CONTEXT.md
@.planning/phases/04-ux-occlusion-confidence/04-RESEARCH.md
@.planning/phases/04-ux-occlusion-confidence/04-PATTERNS.md
@.planning/phases/04-ux-occlusion-confidence/04-VALIDATION.md
@.planning/ROADMAP.md

@backend/shared/python/sunity_shared/analysis/interfaces.py
@backend/shared/python/sunity_shared/gemini/scene_finder.py
@backend/shared/python/sunity_shared/models.py
@backend/shared/python/sunity_shared/firestore_admin.py
@backend/functions/pipeline/app.py
@docs/contract.md
@app/src/types/analysis.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SynthesisAdapter Protocol + GeminiViewReasoner + warning 3-way lockstep</name>
  <files>
    backend/shared/python/sunity_shared/analysis/synthesis/__init__.py,
    backend/shared/python/sunity_shared/analysis/synthesis/interfaces.py,
    backend/shared/python/sunity_shared/analysis/synthesis/gemini_view_reasoner.py,
    backend/shared/python/sunity_shared/models.py,
    app/src/types/analysis.ts,
    docs/contract.md
  </files>

  <read_first>
    필수:
    1. backend/shared/python/sunity_shared/analysis/interfaces.py (전체) — PoseEngine Protocol 패턴 복사 기준
    2. backend/shared/python/sunity_shared/gemini/scene_finder.py (전체) — GeminiVisionCall 재사용 + G4 가드 + graceful degrade 패턴
    3. backend/shared/python/sunity_shared/models.py (lines 220-260) — warning code frozenset 카탈로그 현재 상태 + 추가 위치
    4. 04-PATTERNS.md §SynthesisAdapter 신규 선언 — interfaces.py 작성 기준
    5. 04-PATTERNS.md §gemini_view_reasoner.py — scene_finder 패턴 복사 기준 + Gemini 프롬프트 박제
    6. 04-RESEARCH.md §Pattern 1: Synthesis Adapter Protocol — SynthesisAdapter 시그니처
    7. 04-RESEARCH.md §Code Examples — OCCLUDED_JOINT_REASONING_PROMPT 전문
    8. app/src/types/analysis.ts (lines 1-60) — AiSynthesisMeta interface 추가 위치
    9. docs/contract.md §9.8 — warning code enum 현재 항목 + Phase 4 신설 2개 추가 위치
    10. 04-CONTEXT.md D-18, D-20, D-07, D-08 — PRIMARY path + license + degrade 정책
  </read_first>

  <behavior>
    test_warning_lockstep.py (Wave 0):
    - test_ai_synthesis_failed_in_frozenset: models.py SYNTHESIS_WARNING_CODES frozenset 에 "ai_synthesis_failed" 포함 → GREEN (Wave 1 후)
    - test_ai_synthesis_partial_in_frozenset: "ai_synthesis_partial" 포함 → GREEN

    synthesis/interfaces.py:
    - SynthesisAdapter Protocol: synthesize_occluded_joints(joint_sequence, confidence_sequence, occluded_mask, scene_findings) → tuple[ndarray, ndarray]
    - identify_synthesis_targets(joint_seq, confidence_seq, occlusion_high_phases, scene_flags, threshold=0.3) → np.ndarray bool mask
    - merge_with_temporal(primary_joints, primary_conf, synth_joints, synth_conf, synthesis_mask) → tuple[ndarray, ndarray]
    - 모든 함수 from __future__ import annotations + numpy type hints + 모듈 docstring

    synthesis/gemini_view_reasoner.py (Stage 1 PRIMARY):
    - GeminiViewReasoner 클래스 (SynthesisAdapter 구현)
    - synthesize_occluded_joints: is_reference guard (G4 occlusion FP 가드 — 04-CONTEXT code_context) → Gemini API 호출 (GeminiVisionCall 재사용) → JSON 응답 파싱 → (synth_joints, synth_conf) 반환
    - API 실패 / None 응답 → (np.zeros_like(joint_sequence), np.zeros_like(confidence_sequence)) 반환 (degrade 신호)
    - OCCLUDED_JOINT_REASONING_PROMPT: Spike 003 박제 프롬프트 그대로 (한국어 폴스포츠 도메인 컨텍스트 포함)
    - 모델 string: resolve_model("C", env_override="GEMINI_C_MODEL_OVERRIDE") 재사용 (WRAP-UP-SUMMARY: Vision 영역 gemini-2.5-pro 일시 허용 → env override 로 제어)
    - log = logging.getLogger(__name__)

    models.py 수정:
    - SYNTHESIS_WARNING_CODES: frozenset[str] = frozenset({"ai_synthesis_failed", "ai_synthesis_partial"}) 신규 선언
    - 기존 warning 카탈로그 주석 블록 (lines ~233) 에 Phase 4 신설 2개 추가 (contract.md §9.8 mirror)

    analysis.ts 수정:
    - SynthesisWarningCode = 'ai_synthesis_failed' | 'ai_synthesis_partial' string-literal union
    - AiSynthesisMeta interface: synthesizedFrameCount, synthesizedJointKeys, synthesisPath ('gemini_view' | 'cylindrical_mesh' | 'none'), degraded boolean
    - AnalysisResult 에 aiSynthesisMeta?: AiSynthesisMeta optional 필드 추가
    - 파일 헤더 주석: "이 파일이 바뀌면 docs/contract.md 와 백엔드도 같이 맞춰야 함."

    docs/contract.md §9.8 수정:
    - warning code 테이블에 "ai_synthesis_failed" / "ai_synthesis_partial" 행 추가 (Phase 4 신설 표기)
  </behavior>

  <action>
    1. synthesis/__init__.py 생성: 빈 파일 (패키지 선언).

    2. synthesis/interfaces.py 생성: interfaces.py 의 PoseEngine Protocol 패턴 정확히 복사해 SynthesisAdapter 선언. identify_synthesis_targets 함수는 D-04 (a)(b)(c)(d) 4가지 트리거를 mask_a | mask_b | mask_c 로 결합 — 초기 임계값 threshold: float = 0.3 (RESEARCH.md Assumptions A1, Claude's Discretion). merge_with_temporal 함수는 "better = synthesis_mask & (synth_conf > primary_conf)" 로직 (RESEARCH.md Pattern 2 + Code Examples 코드 구조 참조). 픽셀 합성 X, 좌표 추정만 (D-18 Anti-Pattern 박제 주석).

    3. synthesis/gemini_view_reasoner.py 생성: scene_finder.py 의 import 구조 + GeminiVisionCall + resolve_model 재사용. G4 가드 (is_reference=True → 즉시 (zeros, zeros) 반환 + log.info, 근거: G4 occlusion FP 가드 — 04-CONTEXT code_context). synthesize_occluded_joints 에서 OCCLUDED_JOINT_REASONING_PROMPT 를 Spike 003 박제 그대로 사용 (한국어 분석 컨텍스트 포함, motion_category / occlusion_severe / camera_angle / pole_axis_pixel_x / visible_anchors / prev_frame_pose / occluded_joints 파라미터 포맷). JSON 파싱 실패 → (zeros, zeros). indeterminate 응답 관절 → conf=0.0 처리. 모든 예외 except Exception: log.exception + (zeros, zeros) 반환 (D-07 graceful degrade 계약).

    4. models.py 수정: SYNTHESIS_WARNING_CODES frozenset 추가. warning code 주석 갱신 (D-08 ai_synthesis_failed / ai_synthesis_partial). 기존 코드 일체 변경 금지.

    5. analysis.ts 수정: SynthesisWarningCode + AiSynthesisMeta interface + AnalysisResult.aiSynthesisMeta? optional 필드. 3-way lockstep 주석 ("docs/contract.md §Phase4 + models.py SYNTHESIS_WARNING_CODES mirror"). 기존 interface 필드 변경 금지.

    6. docs/contract.md §9.8 수정: Phase 4 신설 2개 warning code 행 추가. 원본 형식 그대로 유지.

    Commit: 단일 atomic commit "feat(04-01): SynthesisAdapter Protocol + GeminiViewReasoner + warning 3-way lockstep (POSE-03 D-08)"
  </action>

  <verify>
    <automated>
      cd /Users/kimtaesung/Dev/SunityMotion &&
      python -m pytest backend/tests/phase04/test_warning_lockstep.py -x -q 2>&1 | tail -10
    </automated>
  </verify>

  <acceptance_criteria>
    - test_warning_lockstep.py 3개 테스트 모두 GREEN (SYNTHESIS_WARNING_CODES 존재 + 2개 code 포함)
    - python -c "from sunity_shared.analysis.synthesis.interfaces import SynthesisAdapter, identify_synthesis_targets, merge_with_temporal" 성공
    - python -c "from sunity_shared.analysis.synthesis.gemini_view_reasoner import GeminiViewReasoner" 성공
    - npm run typecheck (app/) — 0 errors (AiSynthesisMeta interface 추가 후)
    - contract.md §9.8 에 "ai_synthesis_failed" 문자열 포함 확인: grep -c "ai_synthesis_failed" docs/contract.md | grep -v '^0$'
    - grep -c "AiSynthesisMeta" app/src/types/analysis.ts | grep -v '^0$'
  </acceptance_criteria>

  <done>SYNTHESIS_WARNING_CODES frozenset GREEN + 3-way lockstep (models.py + analysis.ts + contract.md) 모두 갱신 + synthesis 패키지 import 성공 + AiSynthesisMeta interface TS 추가.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: firestore_admin aiSynthesisMeta + pipeline _call_synthesis_adapter wiring</name>
  <files>
    backend/shared/python/sunity_shared/firestore_admin.py,
    backend/functions/pipeline/app.py
  </files>

  <read_first>
    필수:
    1. backend/shared/python/sunity_shared/firestore_admin.py (lines 600-680) — complete_analysis 시그니처 + keypoint_report 파라미터 추가 패턴 (04-PATTERNS.md §firestore_admin.py 패턴)
    2. backend/shared/python/sunity_shared/firestore_admin.py (lines 45-101) — _validate_flat_dict_no_nested_array 함수 (재사용)
    3. backend/functions/pipeline/app.py (lines 320-370) — _call_wave1_scene_finder + _call_wave2_keypoint_augmenter wave-gate 패턴 (04-PATTERNS.md §pipeline/app.py 참조)
    4. 04-PATTERNS.md §pipeline/app.py — _call_synthesis_adapter 구현 패턴 (G4 가드 + SYNTHESIS_ENABLED env + extra_warnings injection)
    5. 04-PATTERNS.md §firestore_admin.py — aiSynthesisMeta 추가 패턴
    6. 04-RESEARCH.md §Pattern 3: Graceful Degrade + Warning Injection — R8 fix 패턴 (dataclasses.replace)
    7. 04-RESEARCH.md §Pitfall 4 — Firestore nested-array 금지 (synthesizedJoints flat 저장)
    8. 04-RESEARCH.md §Pitfall 5 — G4 occlusion FP guard (is_reference=True 시 합성 0)
    9. 04-CONTEXT.md D-07, D-08, D-03 — degrade / warning / 조건부 트리거 정책
    10. 04-CONTEXT.md code_context §G4 occlusion FP 가드 — is_reference=True + occlusion_severe 동시 시 합성 0 근거
  </read_first>

  <behavior>
    test_synthesis_g4_guard.py (Wave 0):
    - test_g4_guard_blocks_synthesis_for_reference: _call_synthesis_adapter(is_reference=True) → None (Wave 1 후 GREEN)

    test_synthesis_firestore_flat.py (Wave 0):
    - test_ai_synthesis_meta_no_nested_array: flat dict → _validate_flat_dict_no_nested_array 통과 (이미 GREEN)
    - test_ai_synthesis_meta_nested_array_rejected: nested → TypeError (이미 GREEN)

    firestore_admin.complete_analysis:
    - ai_synthesis_meta: dict | None = None 파라미터 추가 (기존 keypoint_report 파라미터 추가 방식 정합)
    - ai_synthesis_meta is not None → _validate_flat_dict_no_nested_array(ai_synthesis_meta, path="aiSynthesisMeta") → payload["result"]["aiSynthesisMeta"] = ai_synthesis_meta
    - 기존 파라미터 순서·기본값 변경 금지

    pipeline/app.py:
    - _call_synthesis_adapter 함수 신설 (wave-gate 패턴 정합):
      - is_reference=True → G4 occlusion FP 가드 (04-CONTEXT code_context, log + return None)
      - _synthesis_enabled() → SYNTHESIS_ENABLED env truthy check (default OFF = "0")
      - identify_synthesis_targets 호출 → mask.any() = False → return None (비용 0)
      - _get_synthesis_adapter() lazy singleton (GeminiViewReasoner)
      - adapter.synthesize_occluded_joints 호출
      - except Exception → log.exception + return None
    - _process 에서 stage 1 RTMW 완료 후 _call_synthesis_adapter 1줄 호출
    - synth_result is None → extra_warnings.append("ai_synthesis_failed") + dataclasses.replace(profile, extra_warnings=...)
    - synth_result is not None → merge_with_temporal 호출 → merged_joints, merged_conf
    - complete_analysis 호출 시 ai_synthesis_meta kwarg 주입 (flat dict: synthesizedFrameCount, synthesizedJointKeys, synthesisPath, degraded)
  </behavior>

  <action>
    1. firestore_admin.py 수정: complete_analysis 시그니처에 ai_synthesis_meta: dict | None = None 파라미터 추가 (함수 맨 끝 — default-after-non-default 회피, R1 fix 패턴 정합). payload 조립 블록에 if ai_synthesis_meta is not None: _validate_flat_dict_no_nested_array + payload["result"]["aiSynthesisMeta"] = ai_synthesis_meta 추가. 기존 코드 일체 변경 금지.

    2. pipeline/app.py 수정:
       a. _call_synthesis_adapter 함수 신설 (lines 340 부근, _call_wave1_scene_finder 다음). 함수 docstring 에 "Phase 4 — 합성 어댑터 조건부 호출 (D-03/D-07 박제). G4 가드: is_reference=True 시 합성 0 (G4 occlusion FP 가드 — 04-CONTEXT code_context)." 명시. G4 가드 먼저 (is_reference 체크), 그 다음 SYNTHESIS_ENABLED env 체크.
       b. _synthesis_enabled() 헬퍼 함수: os.environ.get("SYNTHESIS_ENABLED", "0") truthy 판정 (GEMINI_FINDING_ENABLED 패턴 정합, default OFF → 운영 안전).
       c. _get_synthesis_adapter() lazy singleton 헬퍼: 모듈레벨 _SYNTHESIS_ADAPTER 변수 + lazy init GeminiViewReasoner.
       d. _process 함수 내: RTMW 분석 완료 + scene_findings 산출 후, _call_synthesis_adapter 호출. extra_warnings list 관리. dataclasses.replace(profile, extra_warnings=tuple(extra_warnings)) R8 fix 패턴 정합. complete_analysis 호출 시 ai_synthesis_meta= 주입.

    Commit 1: firestore_admin ai_synthesis_meta kwarg
    Commit 2: pipeline _call_synthesis_adapter wiring (D-07/D-08, G4 occlusion FP 가드)
  </action>

  <verify>
    <automated>
      cd /Users/kimtaesung/Dev/SunityMotion &&
      python -m pytest backend/tests/phase04/ -x -q 2>&1 | tail -15 &&
      python -m pytest backend/tests/ -q --ignore=backend/tests/phase04 2>&1 | tail -5
    </automated>
  </verify>

  <acceptance_criteria>
    - backend/tests/phase04/ 전체 실행 — POSE-03-a/b/c/d/e/f 케이스 중 xfail 제외 모두 GREEN
    - test_synthesis_g4_guard.py::test_g4_guard_blocks_synthesis_for_reference GREEN (is_reference=True → None)
    - test_synthesis_firestore_flat.py 2개 모두 GREEN
    - 기존 backend suite (phase04 제외) 회귀 0: 전체 PASS 카운트 이전과 동일
    - python -c "from sunity_shared.firestore_admin import complete_analysis; import inspect; sig=inspect.signature(complete_analysis); print('ai_synthesis_meta' in sig.parameters)" → True
    - grep -c "ai_synthesis_failed" backend/functions/pipeline/app.py | grep -v '^0$'
  </acceptance_criteria>

  <done>firestore_admin.complete_analysis ai_synthesis_meta 파라미터 존재 + pipeline _call_synthesis_adapter wiring 완료 + phase04 suite GREEN (xfail 제외) + 기존 회귀 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RunPod → Gemini API | Gemini API key 가 Pod env 에서 HTTPS로 전송됨 |
| Gemini 응답 → joint 좌표 | Gemini 출력 JSON 이 coco_array 를 오염하면 채점 결과가 합성 품질에 직접 의존함 |
| user video → pipeline | 사용자 영상이 Gemini Vision 에 전달됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-W1-01 | Information Disclosure | Gemini API key (GEMINI_API_KEY) | mitigate | AWS SSM Parameter Store 주입, log.exception 에서 API key 마스킹 — log 에 직접 출력 금지. CLAUDE.md §3 시크릿 정책 정합. |
| T-04-W1-02 | Tampering | Gemini 출력 hallucinated coordinates | mitigate | 출력 좌표 0~1 clamp + "indeterminate" → conf=0.0 처리. coco_array (DTW/kismam) 에 합성 좌표 주입 금지 (RESEARCH.md Pitfall 7 — 합성은 KeypointReport + aiSynthesisMeta 만). |
| T-04-W1-03 | Elevation of Privilege | G4 guard bypass (합성이 reference 오염) | mitigate | is_reference=True 체크가 _call_synthesis_adapter 최우선 분기 (G4 occlusion FP 가드 — 04-CONTEXT code_context). test_synthesis_g4_guard 가 회귀 게이트. |
| T-04-W1-04 | Denial of Service | Gemini API timeout/rate-limit | mitigate | SYNTHESIS_ENABLED env default="0" (운영 안전 스위치). graceful degrade (except Exception → None + ai_synthesis_failed). |
| T-04-W1-SC | Tampering | pip installs (synthesis/ 신규 모듈) | accept | synthesis 패키지는 신규 .py 파일 신설 — 외부 패키지 설치 없음. |
</threat_model>

<verification>
pytest backend/tests/phase04/ -x -q → POSE-03-d GREEN (warning lockstep) + POSE-03-e/f GREEN
pytest backend/tests/ -q → 기존 suite PASS 카운트 불변
npm run typecheck (app/) → 0 errors (AiSynthesisMeta interface)
grep "ai_synthesis_failed" backend/shared/python/sunity_shared/models.py docs/contract.md app/src/types/analysis.ts — 3파일 모두 발견
grep "AiSynthesisMeta" app/src/types/analysis.ts — 발견 (3-way lockstep TS side)
</verification>

<success_criteria>
Wave 1 완료 기준 (ROADMAP SC #2 + SC #3 + SC #5 관련):
1. SynthesisAdapter Protocol + GeminiViewReasoner 구현 완료
2. SYNTHESIS_WARNING_CODES frozenset 3-way lockstep 완료 (models.py + analysis.ts + contract.md)
3. pipeline _call_synthesis_adapter: G4 occlusion FP 가드 + SYNTHESIS_ENABLED env OFF default + graceful degrade
4. firestore_admin.complete_analysis ai_synthesis_meta flat 저장
5. phase04 test suite POSE-03-a~f GREEN (xfail 케이스 제외)
6. 기존 backend suite 회귀 0 + npm run typecheck 0 errors
</success_criteria>

<output>
Create `.planning/phases/04-ux-occlusion-confidence/04-01-SUMMARY.md` when done
</output>
