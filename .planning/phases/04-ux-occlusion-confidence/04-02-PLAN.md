---
phase: 04
plan: "02"
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - app/src/theme/colors.ts
  - app/src/lib/joints.ts
  - app/src/lib/userAnalyses.ts
  - app/src/components/AccuracyLimitBadge.tsx
  - app/src/components/PoseViewer3D.tsx
  - app/src/app/analysis/result.tsx
  - app/package.json
  - app/package-lock.json
autonomous: false
requirements:
  - POSE-03

must_haves:
  truths:
    - "결과 화면에 3D 자세 뷰어 (PoseViewer3D) 가 VideoCompare 아래에 삽입되고, joints3d null 이면 섹션이 graceful 생략된다 (ROADMAP SC #6, per D-29~D-32)"
    - "PoseViewer3D 가 /native import 경로로 R3F Canvas 를 마운트하고, 사용자가 손가락으로 회전할 수 있다 (per D-32, RESEARCH Pitfall 2)"
    - "AccuracyLimitBadge 가 ai_synthesis_failed warning 있을 때 결과 화면 헤더 하단에 표시된다 (D-08, per D-08)"
    - "가림/저신뢰 구간에서 '추정' 카피가 결과 화면에 표시된다 (Phase 12.5 자세히 모달 톤 정합 — '이 구간은 가림 또는 측정 불확실로 추정값입니다.' 카피 유지). D-05 블랙박스 준수 — 'AI 보완' 노출 금지, occlusion '추정' 일반 카피만."
    - "AiSynthesisMeta Firestore flat 데이터를 reshapeJoints3d 로 (T, 17, 3) 로 복원하고 userAnalyses.ts normalize() 를 거쳐 result.tsx 로 흐른다"
    - "npm run typecheck 0 errors — PoseViewer3D + joints.ts 타입 정합 (POSE-03-h)"
    - "기존 result.tsx 화면 기능 (VideoCompare, 점수 카드, 코칭 팁) 회귀 없음"
  artifacts:
    - path: app/src/lib/joints.ts
      provides: "reshapeJoints3d — Firestore flat → (T,17,3)"
      exports: ["reshapeJoints3d"]
    - path: app/src/lib/userAnalyses.ts
      provides: "normalize() aiSynthesisMeta / warnings 추출 compat layer"
      contains: "aiSynthesisMeta"
    - path: app/src/components/AccuracyLimitBadge.tsx
      provides: "정확도 제한 배지 컴포넌트 (D-08)"
      exports: ["AccuracyLimitBadge"]
    - path: app/src/components/PoseViewer3D.tsx
      provides: "Stage 3 사용자 3D 뷰어 (Spike 005)"
      exports: ["PoseViewer3D"]
    - path: app/src/theme/colors.ts
      provides: "Phase 4 신규 alias 토큰 6개"
      contains: "viewer3dBg"
  key_links:
    - from: app/src/app/analysis/result.tsx
      to: app/src/components/PoseViewer3D.tsx
      via: "joints3d conditional render"
      pattern: "PoseViewer3D"
    - from: app/src/app/analysis/result.tsx
      to: app/src/components/AccuracyLimitBadge.tsx
      via: "warnings.includes('ai_synthesis_failed')"
      pattern: "AccuracyLimitBadge"
    - from: app/src/components/PoseViewer3D.tsx
      to: "@react-three/fiber/native"
      via: "Canvas import /native 경로"
      pattern: "from '@react-three/fiber/native'"
    - from: app/src/lib/userAnalyses.ts
      to: app/src/app/analysis/result.tsx
      via: "normalize() → aiSynthesisMeta, warnings 필드 → result.tsx 소비"
      pattern: "aiSynthesisMeta"

user_setup: []
---

<objective>
Phase 4 Wave 2 — Stage 3 사용자 3D 뷰어 수직 슬라이스. npm install (three + R3F 생태계) → joints.ts reshaper → userAnalyses.ts aiSynthesisMeta wiring → AccuracyLimitBadge → PoseViewer3D (R3F Canvas + stick figure + preset + scrubber) → result.tsx 통합. 이 plan 이 완료되면 사용자가 결과 화면에서 손가락으로 3D 자세를 360° 회전해 볼 수 있다 (ROADMAP SC #6). 실기기 R3F/expo-gl 검증은 Wave 2 착수 전 checkpoint 로 보장.

Purpose: belle 결정적 깨달음 (Spike 005) — "AI 영상 생성 불필요, 수학적 가상 카메라 연산으로 360° 가능." MVP 가능 기능 승격.
Output: 3D 뷰어 작동 + ai_synthesis_failed 배지 표시 + typecheck 0 errors + userAnalyses.ts Firestore wiring
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-ux-occlusion-confidence/04-UI-SPEC.md
@.planning/phases/04-ux-occlusion-confidence/04-RESEARCH.md
@.planning/phases/04-ux-occlusion-confidence/04-PATTERNS.md
@.planning/phases/04-ux-occlusion-confidence/04-CONTEXT.md
@.planning/ROADMAP.md

@app/src/theme/colors.ts
@app/src/lib/userAnalyses.ts
@app/src/components/VideoCompare.tsx
@app/src/app/analysis/result.tsx
@app/src/types/analysis.ts
@app/package.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Wave 2 착수 전 검증 체크포인트. npm install (three + @react-three/fiber + expo-three + @react-three/drei) + expo install expo-gl 를 실행해 의존성을 설치한 후, npx expo-doctor 로 SDK 54 호환성 확인, EAS preview 빌드로 실기기에서 R3F Canvas(@react-three/fiber/native) + expo-gl + 제스처 동작이 실제로 작동하는지 확인한다. RESEARCH Q3(OrbitControls RN 호환 MEDIUM confidence) + R3F New Architecture(Expo 54 + newArchEnabled:true) 실기기 미검증 상태이므로 이 체크포인트가 autonomous:false 의 근거다.
  </what-built>
  <how-to-verify>
    1. 의존성 설치 (Claude 가 실행):
       cd app
       npm install three @react-three/fiber expo-three @react-three/drei
       npx expo install expo-gl

    2. expo-doctor 호환성 확인:
       npx expo-doctor 2>&1 | grep -E "warning|error|incompatible" | head -20
       SDK 54 관련 버전 경고가 없으면 PASS.

    3. 최소 R3F smoke test 파일 생성 후 typecheck:
       app/src/components/__r3f_smoke__.tsx 에 아래 내용만 작성:
         import { Canvas } from '@react-three/fiber/native';
         export default function R3FSmoke() { return <Canvas><></></Canvas>; }
       npm run typecheck 0 errors → /native import 경로 유효 확인.
       확인 후 smoke 파일 삭제.

    4. EAS preview 빌드 + 실기기 실행:
       eas build --profile preview --platform ios (또는 android) 로 빌드.
       TestFlight/Android 설치 후 기존 화면(홈, 분석, 결과)이 정상 동작하는지 확인.
       (R3F 실제 렌더는 Task 2에서 구현 후 별도 확인 — 이 체크포인트는 패키지 설치 후 기존 화면 회귀가 없는지 확인)

    5. @react-three/drei OrbitControls /native export 존재 여부 확인:
       node -e "const d = require('@react-three/drei/native'); console.log('OrbitControls' in d);"
       True 이면 OrbitControls 직접 사용.
       False 이면 react-native-gesture-handler PanResponder 대체 경로로 진행 — Task 2 action 에 이미 명시됨.
  </how-to-verify>
  <resume-signal>
    다음 중 하나를 입력하세요:
    - "approved" — expo-doctor PASS + typecheck 0 errors + drei OrbitControls 가용 여부 확인 결과 (예: "OrbitControls available: true" 또는 "PanResponder fallback needed")
    - "issue: [문제 내용]" — 설치 에러, 버전 충돌, typecheck 실패 등 구체적 내용
  </resume-signal>
</task>

<task type="auto">
  <name>Task 1: npm install + colors 토큰 + joints.ts reshaper + userAnalyses.ts wiring + AccuracyLimitBadge</name>
  <files>
    app/package.json,
    app/package-lock.json,
    app/src/theme/colors.ts,
    app/src/lib/joints.ts,
    app/src/lib/userAnalyses.ts,
    app/src/components/AccuracyLimitBadge.tsx
  </files>

  <read_first>
    필수:
    1. app/package.json (전체) — 현재 의존성 목록 + expo SDK 버전 확인 (expo-gl 이미 포함 여부)
    2. app/src/theme/colors.ts (전체) — Phase 12 신설 토큰 추가 패턴 (04-PATTERNS.md §colors.ts)
    3. app/src/lib/userAnalyses.ts (전체) — normalize() 방어 패턴 + Phase 8/9/12 compat layer 추가 패턴 (aiSynthesisMeta wiring 삽입 위치 파악)
    4. app/src/types/analysis.ts — AiSynthesisMeta interface (Wave 1 에서 추가됨 — 읽어 확인, 특히 aiSynthesisMeta 필드 타입)
    5. app/src/components/VideoCompare.tsx (lines 1-30) — 컴포넌트 파일 헤더 + imports 패턴
    6. app/src/app/analysis/result.tsx (lines 530-560, 723-730, 941-953) — showOcclusionBadge / occlusionBadge 패턴 (AccuracyLimitBadge analog, 04-PATTERNS.md §AccuracyLimitBadge.tsx) + 기존 "추정" 카피 구현 확인
    7. 04-UI-SPEC.md §Surface 2 — AccuracyLimitBadge 규격 (배경, 테두리, 타이포, 카피)
    8. 04-RESEARCH.md §Package Legitimacy Audit — three/R3F/expo-three/drei 버전 확인
    9. 04-RESEARCH.md §Standard Stack — npm install 명령 + expo install expo-gl 명령
    10. 04-RESEARCH.md §Pitfall 1 — expo-gl 버전 불일치 함정 (expo install expo-gl 로 SDK 54 자동 맞춤)
  </read_first>

  <action>
    1. app/ 디렉토리에서 npm install 실행 (체크포인트 이미 완료된 상태여야 함):
       three @react-three/fiber expo-three @react-three/drei
       그 후: expo install expo-gl (SDK 54 자동 버전 맞춤 — Pitfall 1 방지)
       설치 후 npm view three scripts.postinstall, npm view @react-three/fiber scripts.postinstall 확인 (없으면 pass)

    2. app/src/theme/colors.ts 수정: Phase 4 신규 alias 6개 추가 (04-UI-SPEC.md Color §신규 토큰):
       viewer3dBg, viewer3dBone, viewer3dJoint, viewer3dJointNormal, accuracyLimitBg, accuracyLimitText
       alias 방식 — 기존 토큰 hex 복사 금지, 주석 "── Phase 4 신설 토큰 ──────" 헤더 추가. 기존 토큰 변경 금지.

    3. app/src/lib/joints.ts 신규: reshapeJoints3d(angles, jointKeys, frames) → number[][][] | null 함수.
       04-PATTERNS.md §joints.ts normalize 방어 패턴 그대로. Array.isArray + typeof frames !== 'number' guard.
       angles.length !== T * J * 3 guard. 3중 for loop (T × J × 3). 파일 헤더 주석: "Firestore flat → (T,17,3) reshape / [[firestore-nested-array-flat]] / firestore_admin.complete_analysis 계약 정합."
       named export reshapeJoints3d.

    4. app/src/lib/userAnalyses.ts 수정: Phase 8/9/12 compat layer 패턴(lines 74-133) 이후에 Phase 4 aiSynthesisMeta compat block 추가.
       if (result?.aiSynthesisMeta) {
         const meta = result.aiSynthesisMeta;
         result = {
           ...result,
           aiSynthesisMeta: {
             synthesizedFrameCount: meta.synthesizedFrameCount ?? 0,
             synthesizedJointKeys: meta.synthesizedJointKeys ?? [],
             synthesisPath: meta.synthesisPath ?? 'none',
             degraded: meta.degraded ?? true,
           },
         };
       }
       주석: "Phase 4 (04-02) — AiSynthesisMeta null-guard. Firestore 에 field 없으면 undefined 유지 (AnalysisResult.aiSynthesisMeta? optional 정합). Phase 8 forceSignalsReport 패턴 mirror."
       이 wiring 이 result.tsx 로 aiSynthesisMeta / warnings 를 흘리는 단일 경로 (다른 파일에서 Firestore raw data 를 직접 읽지 않음). 기존 코드 변경 금지.

    5. app/src/components/AccuracyLimitBadge.tsx 신규: 04-PATTERNS.md §AccuracyLimitBadge.tsx 코드 그대로 구현 (occlusionBadge StyleSheet 확장).
       AccuracyLimitBadgeProps: visible boolean. visible false → return null.
       카피 상수: "가림 구간 정확도가 제한적이에요" / "측면 관절 추정 오차가 포함될 수 있어요." (D-05 블랙박스: "AI 보완 실패" 금지).
       StyleSheet: colors.softBg 배경 + warnAmber 테두리 + radius.card + 패딩 12/16.
       Ionicons "warning" size=14, colors.warnAmber. typography.boxLabel + typography.caption.
       named export AccuracyLimitBadge.
  </action>

  <verify>
    <automated>
      cd /Users/kimtaesung/Dev/SunityMotion/app &&
      npm run typecheck 2>&1 | tail -10
    </automated>
  </verify>

  <acceptance_criteria>
    - app/package.json 에 "three" + "@react-three/fiber" + "expo-three" + "@react-three/drei" 의존성 존재
    - app/src/theme/colors.ts 에 "viewer3dBg" 토큰 존재: grep -c "viewer3dBg" app/src/theme/colors.ts | grep -v '^0$'
    - app/src/lib/joints.ts 에 reshapeJoints3d export: grep -c "reshapeJoints3d" app/src/lib/joints.ts | grep -v '^0$'
    - app/src/lib/userAnalyses.ts 에 "aiSynthesisMeta" wiring 존재: grep -c "aiSynthesisMeta" app/src/lib/userAnalyses.ts | grep -v '^0$'
    - app/src/components/AccuracyLimitBadge.tsx 에 "가림 구간 정확도가 제한적이에요" 카피 존재: grep -c "가림 구간" app/src/components/AccuracyLimitBadge.tsx | grep -v '^0$'
    - npm run typecheck (app/) 0 errors
    - "AI 보완 실패" 금지 문구: grep -c "AI 보완 실패" app/src/components/AccuracyLimitBadge.tsx 결과 0
  </acceptance_criteria>

  <done>npm install 완료 + colors 토큰 6개 추가 + reshapeJoints3d 구현 + userAnalyses.ts aiSynthesisMeta wiring + AccuracyLimitBadge 구현 + typecheck 0 errors.</done>
</task>

<task type="auto">
  <name>Task 2: PoseViewer3D 컴포넌트 + result.tsx 통합</name>
  <files>
    app/src/components/PoseViewer3D.tsx,
    app/src/app/analysis/result.tsx
  </files>

  <read_first>
    필수:
    1. app/src/components/VideoCompare.tsx (전체, ~200줄) — PoseViewer3D 파일 헤더 + imports + StyleSheet 구조 analog
    2. app/src/app/analysis/result.tsx (전체) — 섹션 삽입 위치 + 기존 state 구조 + occlusionBadge 참조 패턴 (04-PATTERNS.md §result.tsx) + 기존 추정 표기 로직(lines 530-560)
    3. 04-UI-SPEC.md §Surface 1 — PoseViewer3D 레이아웃 + 인터랙션 규격 + 카메라 preset 버튼 4개 + scrubber + 상태별 UI
    4. 04-PATTERNS.md §PoseViewer3D.tsx — Canvas 코드 구조 + SkeletonMesh + OrbitControls
    5. 04-RESEARCH.md §Pattern 4 — R3F PoseViewer3D 코드 예시 (/native import 경로 필수)
    6. 04-RESEARCH.md §Pitfall 2 — /native import 경로 미사용 위험
    7. 04-RESEARCH.md §Pitfall 1 — expo-gl 버전 (expo install expo-gl 로 SDK 54 자동)
    8. 04-RESEARCH.md §Open Questions Q3 — @react-three/drei OrbitControls RN 호환 여부 (체크포인트에서 확인된 결과 적용)
    9. 04-UI-SPEC.md §Color — viewer3dBg #F5F5F5, viewer3dBone #1F2024, 다크 배경 금지
    10. 04-UI-SPEC.md §Copywriting Contract — 한국어 관절 이름 테이블
    11. 04-CONTEXT.md D-05, D-06 — 블랙박스 원칙 (AI 보완 사실 미노출)
    12. app/src/types/analysis.ts — AiSynthesisMeta + AnalysisResult 타입 (Wave 1 에서 추가됨)
    13. app/src/lib/userAnalyses.ts (Task 1 에서 수정됨) — aiSynthesisMeta normalize 패턴 확인
  </read_first>

  <action>
    1. app/src/components/PoseViewer3D.tsx 신규:
       - 파일 헤더: "Phase 4 Wave 2 — Stage 3 사용자 3D 뷰어 (react-three-fiber + expo-three). Spike 005 VALIDATED-ARCHITECTURE. /native import 경로 필수 — web path 는 RN 에서 window is not defined."
       - import: Canvas from '@react-three/fiber/native' (경로 /native 필수, Pitfall 2 방지)
       - OrbitControls: @react-three/drei/native 에서 import 시도. 체크포인트에서 drei OrbitControls /native export 미존재 확인 시 → react-native-gesture-handler PanResponder 로 대체. 대체 경로 주석: "// OrbitControls /native export 미존재 — PanResponder 대체 (RESEARCH Q3 MEDIUM confidence fallback)". 두 경로 모두 canvas 위 onPanResponderMove 로 카메라 yaw/pitch 업데이트 동일 UX 제공. drei OrbitControls 가용 시에는 직접 사용.
       - PoseViewer3DProps interface: joints (number[][][] | null), referenceJoints? (number[][][] | null), ipsfViolationFrames? (number[]), currentFrame (number), onFrameChange ((frame:number)=>void)
       - joints null → return null (graceful, Phase 4 이전 분석 doc 호환)
       - SkeletonMesh 내부 컴포넌트: Three.js sphere (joint) + line (bone) geometry. joint sphereGeometry radius=0.04. bone lineSegments. 정상 관절: colors.viewer3dJointNormal (#ACACAC). IPSF 위반 관절: colors.viewer3dJoint (#FF4B33). 뼈대 line: colors.viewer3dBone (#1F2024).
       - scene background: Three.js scene.background = new THREE.Color(0xF5F5F5) (colors.viewer3dBg — 다크 배경 금지, CLAUDE.md §4)
       - CameraPresetBar: 정면/측면/후면/위 버튼 4개. 각 버튼 accessibilityRole="button", accessibilityLabel={`${label} 시점으로 전환`}, hitSlop {top:8,bottom:8,left:8,right:8}. 활성: tedurite 1pt colors.brand. 비활성: colors.softBg 배경. typography.boxLabel.
       - TimelineScrubber: PanResponder onPanResponderMove → frame index 계산. accessibilityRole="adjustable", accessibilityValue={{min:0, max:totalFrames, now:currentFrame}}.
       - 3D 로딩 (joints null 이 아닌데 Three.js init 중): 중앙 dot-pulse animation (colors.brand 3점, sequential opacity).
       - 렌더 실패 EmptyState: 아이콘 "ios-cube-outline" + 텍스트 "3D 뷰어를 로드하지 못했어요." + hint "일반 결과는 아래에서 확인하세요." (design.md §0 빈 상태 패턴)
       - mode1 두 skeleton: 사용자 회색 (#ACACAC), reference 브랜드 틴트 (colors.brandSoft)
       - StyleSheet: VideoCompare 의 styles 구조 그대로. canvas height=280, container borderRadius=radius.card, divider border. 하드코딩 hex 금지 — 토큰만.
       - named export PoseViewer3D.

    2. app/src/app/analysis/result.tsx 수정:
       a. import AccuracyLimitBadge from '../../components/AccuracyLimitBadge'
       b. import { PoseViewer3D } from '../../components/PoseViewer3D'
       c. import { reshapeJoints3d } from '../../lib/joints'
       d. useMemo: joints3d = reshapeJoints3d(doc?.result?.angles, doc?.result?.anglesJointKeys, doc?.result?.anglesFrames) — Wave 1 Task 2 에서 확정된 Firestore 필드명 정합 (aiSynthesisMeta 는 userAnalyses.ts normalize() 를 통해 doc.result.aiSynthesisMeta 로 접근).
       e. AccuracyLimitBadge 삽입: 헤더 styles.header 하단, 기존 sub 텍스트 아래. visible={doc?.result?.warnings?.includes('ai_synthesis_failed') ?? false}
       f. PoseViewer3D 섹션 삽입: VideoCompare 바로 아래, 04-UI-SPEC.md §Surface 1 배치 순서 정합. {joints3d && ( <PoseViewer3D joints={joints3d} ... /> )}
       g. currentFrame state: const [currentFrame, setCurrentFrame] = useState(0)
       h. 기존 추정 표기 로직 (showOcclusionBadge, isAngleEstimated, showEstimateTooltip 기반의 "추정 N°" 카피) 변경 금지 — Phase 12.5 구현 그대로 유지. D-05 블랙박스: AccuracyLimitBadge 카피("가림 구간 정확도가 제한적이에요")와 기존 "추정" 카피는 공존하지만 "AI 보완" 문구는 어느 쪽에도 미노출.
       i. 기존 VideoCompare, 점수 카드, 코칭 팁, CTA 섹션 변경 없음.
       j. 접근성: PoseViewer3D 감싸는 View 에 accessibilityRole="image" + accessibilityLabel="3D 자세 뷰어, 손가락으로 회전 가능"
  </action>

  <verify>
    <automated>
      cd /Users/kimtaesung/Dev/SunityMotion/app &&
      npm run typecheck 2>&1 | tail -10
    </automated>
  </verify>

  <acceptance_criteria>
    - npm run typecheck 0 errors (POSE-03-h 충족)
    - grep -c "from '@react-three/fiber/native'" app/src/components/PoseViewer3D.tsx | grep -v '^0$' (Pitfall 2 방지 검증)
    - grep -c "AccuracyLimitBadge" app/src/app/analysis/result.tsx | grep -v '^0$' (D-08 통합)
    - grep -c "PoseViewer3D" app/src/app/analysis/result.tsx | grep -v '^0$' (Stage 3 통합)
    - grep -c "viewer3dBg\|viewer3dBone" app/src/components/PoseViewer3D.tsx | grep -v '^0$' (토큰 사용, 하드코딩 0)
    - "다크 배경" 금지: grep -c "0x2A2A2A\|#2A2A2A\|#1a1a1a\|#000000" app/src/components/PoseViewer3D.tsx 결과 0 (viewer3dBg #F5F5F5 만 허용)
    - "AI 보완" 금지 카피: grep -c "AI.*보완\|AI가 처리\|다각도 분석" app/src/components/PoseViewer3D.tsx app/src/app/analysis/result.tsx 결과 0 (D-05 블랙박스)
    - grep -c "aiSynthesisMeta" app/src/lib/userAnalyses.ts | grep -v '^0$' (Firestore wiring 단일 경로)
    - OrbitControls PanResponder fallback: grep -c "PanResponder\|OrbitControls" app/src/components/PoseViewer3D.tsx | grep -v '^0$' (Q3 대응 분기 존재)
  </acceptance_criteria>

  <done>PoseViewer3D /native import + result.tsx 통합 + AccuracyLimitBadge 통합 + userAnalyses.ts aiSynthesisMeta wiring + npm run typecheck 0 errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Three.js Canvas | WebGL 컨텍스트가 expo-gl GLView 위에서 동작 — 네이티브 GPU 직접 접근 |
| Firestore flat → reshape | flat array 를 (T,17,3) 로 reshape 시 길이 검증 실패 → null graceful |
| userAnalyses.ts normalize() | Firestore raw → TS typed 변환 유일 경로 — aiSynthesisMeta 누락 시 undefined 유지 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-W2-01 | Tampering | reshapeJoints3d 입력 검증 | mitigate | angles.length !== T*J*3 guard → null 반환 (방어적 null return, CLAUDE.md Firestore nested-array 금지 정합) |
| T-04-W2-02 | Spoofing | AccuracyLimitBadge 카피 | mitigate | "AI 보완 실패" 등 오해 소지 카피 금지 (D-05). 블랙박스 원칙 테스트 — grep 게이트 포함 |
| T-04-W2-03 | Denial of Service | Three.js init 실패 (구형 기기 GL 미지원) | mitigate | joints null guard + ErrorBoundary 수준 빈 상태 UI ("3D 뷰어를 로드하지 못했어요.") — 결과 화면 진입 차단 0 |
| T-04-W2-SC | Tampering | npm install (three/R3F 새 패키지) | mitigate | Package Legitimacy Audit 표 (RESEARCH.md) 확인 완료 (all Approved). 설치 후 postinstall 스크립트 없음 재확인 필수. |
</threat_model>

<verification>
npm run typecheck (app/) → 0 errors (POSE-03-h GREEN)
grep "from '@react-three/fiber/native'" app/src/components/PoseViewer3D.tsx → 발견 (/native 경로 확인)
grep "AccuracyLimitBadge\|PoseViewer3D" app/src/app/analysis/result.tsx → 양쪽 발견
grep "aiSynthesisMeta" app/src/lib/userAnalyses.ts → 발견 (Firestore wiring 단일 경로)
</verification>

<success_criteria>
Wave 2 완료 기준 (ROADMAP SC #4 + SC #6 관련):
1. 실기기 R3F/expo-gl 검증 체크포인트 PASS (autonomous:false 의 근거)
2. PoseViewer3D 컴포넌트: /native import + stick figure SkeletonMesh + CameraPresetBar 4개 + TimelineScrubber + drei OrbitControls 또는 PanResponder fallback (Q3 분기)
3. AccuracyLimitBadge: ai_synthesis_failed 트리거, 블랙박스 카피 준수, "추정" 기존 카피와 공존
4. result.tsx 통합: VideoCompare 아래 삽입 + 헤더 하단 배지
5. userAnalyses.ts normalize() → aiSynthesisMeta/warnings wiring 완료 (단일 Firestore 읽기 경로)
6. npm run typecheck 0 errors (POSE-03-h)
7. 다크 배경 금지, 토큰 전용 (하드코딩 0)
8. 기존 result.tsx 기능 회귀 없음
</success_criteria>

<output>
Create `.planning/phases/04-ux-occlusion-confidence/04-02-SUMMARY.md` when done
</output>
