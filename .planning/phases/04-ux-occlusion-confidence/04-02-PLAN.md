---
phase: 04
plan: "02"
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - app/src/theme/colors.ts
  - app/src/lib/joints.ts
  - app/src/lib/userAnalyses.ts
  - app/src/components/AccuracyLimitBadge.tsx
  - app/src/components/PoseViewer3D.tsx
  - app/src/components/PoseViewer3DSmokeScreen.tsx
  - app/src/app/analysis/result.tsx
  - app/package.json
  - app/package-lock.json
autonomous: false
requirements:
  - POSE-03

must_haves:
  truths:
    - "결과 화면에 3D 자세 뷰어 (PoseViewer3D) 가 VideoCompare 아래에 삽입되고, joints3d null 이면 섹션이 graceful 생략된다 (ROADMAP SC #6, per D-29~D-32)"
    - "PoseViewer3D 가 /native import 경로로 R3F Canvas 를 마운트하고, 사용자가 손가락으로 회전할 수 있다 (per D-32, RESEARCH Pitfall 2)"
    - "AccuracyLimitBadge 가 ai_synthesis_failed warning 있을 때 결과 화면 헤더 하단에 표시된다 (D-08). 카피는 블랙박스('가림 구간 정확도가 제한적이에요' / '측면 관절 추정 오차가 포함될 수 있어요.') — 'AI 보완' 류 전면 금지 (D-05 R7)"
    - "가림/저신뢰 구간에서 '추정' 카피가 결과 화면에 표시된다 (Phase 12.5 자세히 모달 톤 정합 — '이 구간은 가림 또는 측정 불확실로 추정값입니다.' 카피 유지). D-05 블랙박스 준수 — 'AI 보완' 노출 금지, occlusion '추정' 일반 카피만."
    - "reshapePose3dData 는 04-01 신설 joints3d(flat T×17×3) + joints3dKeys + joints3dFrames 를 읽는다. result.angles 를 인자로 받으면 런타임 타입 가드에서 null 반환 (R3 — angles 는 관절각이므로 3D 좌표 소스 사용 불가)"
    - "userAnalyses.ts normalize() 가 joints3d / joints3dKeys / joints3dFrames / coordDim / space + aiSynthesisMeta / warnings 를 추출해 result.tsx 로 흘린다"
    - "Canvas/GL 컨텍스트 초기화 실패 시 error boundary 가 3D 뷰어 섹션만 생략하고 점수 카드/코칭 팁 등 일반 결과 화면은 정상 표시된다 (R8 — result route 죽지 않음)"
    - "PoseViewer3DSmokeScreen 실기기 smoke 통과 전 result.tsx 에 PoseViewer3D 병합 금지 (R8 blocking checkpoint)"
    - "npm run typecheck 0 errors — PoseViewer3D + joints.ts 타입 정합 (POSE-03-h)"
    - "기존 result.tsx 화면 기능 (VideoCompare, 점수 카드, 코칭 팁) 회귀 없음"
  artifacts:
    - path: app/src/lib/joints.ts
      provides: "reshapePose3dData — Firestore flat joints3d → (T,17,3). angles 입력 시 null 반환 타입 가드 포함"
      exports: ["reshapePose3dData"]
    - path: app/src/lib/userAnalyses.ts
      provides: "normalize() joints3d/joints3dKeys/joints3dFrames/coordDim/space + aiSynthesisMeta/warnings compat layer"
      contains: "joints3dKeys"
    - path: app/src/components/AccuracyLimitBadge.tsx
      provides: "정확도 제한 배지 컴포넌트 (D-08, R7 블랙박스 카피)"
      exports: ["AccuracyLimitBadge"]
    - path: app/src/components/PoseViewer3DSmokeScreen.tsx
      provides: "feature-flag smoke screen — GL/R3F 실기기 검증 전용 컴포넌트 (R8)"
      exports: ["PoseViewer3DSmokeScreen"]
    - path: app/src/components/PoseViewer3D.tsx
      provides: "Stage 3 사용자 3D 뷰어 (Spike 005) + Canvas ErrorBoundary + GL fallback"
      exports: ["PoseViewer3D"]
    - path: app/src/theme/colors.ts
      provides: "Phase 4 신규 alias 토큰 6개"
      contains: "viewer3dBg"
  key_links:
    - from: app/src/app/analysis/result.tsx
      to: app/src/components/PoseViewer3D.tsx
      via: "joints3d conditional render + smoke 통과 후 병합"
      pattern: "PoseViewer3D"
    - from: app/src/app/analysis/result.tsx
      to: app/src/components/AccuracyLimitBadge.tsx
      via: "warnings.includes('ai_synthesis_failed')"
      pattern: "AccuracyLimitBadge"
    - from: app/src/components/PoseViewer3D.tsx
      to: "@react-three/fiber/native"
      via: "Canvas import /native 경로"
      pattern: "from '@react-three/fiber/native'"
    - from: app/src/lib/userAnalyses.ts
      to: app/src/app/analysis/result.tsx
      via: "normalize() → joints3d/joints3dKeys/joints3dFrames/aiSynthesisMeta/warnings → result.tsx 소비"
      pattern: "joints3dKeys"
    - from: app/src/lib/joints.ts
      to: app/src/components/PoseViewer3D.tsx
      via: "reshapePose3dData → (T,17,3) — angles reject guard"
      pattern: "reshapePose3dData"

user_setup: []
---

<objective>
Phase 4 Wave 2 — Stage 3 사용자 3D 뷰어 수직 슬라이스. npm install (three + R3F 생태계) → joints.ts reshaper(joints3d 전용, angles reject) → userAnalyses.ts joints3d + aiSynthesisMeta wiring → AccuracyLimitBadge(블랙박스 카피) → PoseViewer3DSmokeScreen(R8 feature-flag 검증) → PoseViewer3D (R3F Canvas + GL ErrorBoundary + stick figure + preset + scrubber) → result.tsx 통합. 이 plan 이 완료되면 사용자가 결과 화면에서 손가락으로 3D 자세를 360° 회전해 볼 수 있다 (ROADMAP SC #6).

Note: UI-SPEC status draft → approved. 이 plan 실행 전 04-UI-SPEC.md 는 executor 가 approved 로 기록할 것.

Purpose: belle 결정적 깨달음 (Spike 005) — "AI 영상 생성 불필요, 수학적 가상 카메라 연산으로 360° 가능." MVP 가능 기능 승격. 3D 소스는 04-01 신설 joints3d 필드 전용 — angles(관절각) 오용 영구 차단.
Output: 3D 뷰어 작동 + GL fallback + smoke screen 검증 + ai_synthesis_failed 배지 표시 + typecheck 0 errors + userAnalyses.ts joints3d Firestore wiring
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-ux-occlusion-confidence/04-UI-SPEC.md
@.planning/phases/04-ux-occlusion-confidence/04-RESEARCH.md
@.planning/phases/04-ux-occlusion-confidence/04-PATTERNS.md
@.planning/phases/04-ux-occlusion-confidence/04-CONTEXT.md
@.planning/ROADMAP.md

@app/src/theme/colors.ts
@app/src/lib/userAnalyses.ts
@app/src/components/VideoCompare.tsx
@app/src/app/analysis/result.tsx
@app/src/types/analysis.ts
@app/package.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Wave 2 착수 전 검증 체크포인트 (R8 강화판). npm install (three + @react-three/fiber + expo-three + @react-three/drei) + expo install expo-gl 를 실행해 의존성을 설치한 후, npx expo-doctor 로 SDK 54 호환성 확인. 이어서 PoseViewer3DSmokeScreen (feature-flag 전용 컴포넌트) 을 EAS preview 빌드로 실기기 배포해 R3F Canvas(@react-three/fiber/native) + expo-gl + 제스처 동작 + PanResponder fallback 이 실제 기기에서 정상 작동하는지 확인. RESEARCH Q3(OrbitControls RN 호환 MEDIUM confidence) + R3F New Architecture(Expo 54 + newArchEnabled:true) 실기기 미검증 → 이 체크포인트가 autonomous:false 의 근거. smoke 미통과 시 Task 2 의 result.tsx 병합(PoseViewer3D 삽입) 금지.
  </what-built>
  <how-to-verify>
    1. 의존성 설치 (Claude 가 실행):
       cd /Users/kimtaesung/Dev/SunityMotion/app
       npm install three @react-three/fiber expo-three @react-three/drei
       npx expo install expo-gl

    2. expo-doctor 호환성 확인:
       npx expo-doctor 2>&1 | grep -E "warning|error|incompatible" | head -20
       SDK 54 관련 버전 경고가 없으면 PASS.

    3. /native import 경로 typecheck:
       app/src/components/PoseViewer3DSmokeScreen.tsx (Task 0 에서 생성됨) 로
       import { Canvas } from '@react-three/fiber/native' 경로 유효 확인.
       npm run typecheck 0 errors → PASS.

    4. drei OrbitControls /native export 존재 여부 확인:
       node -e "const d = require('@react-three/drei/native'); console.log('OrbitControls' in d);"
       True → OrbitControls 직접 사용.
       False → PanResponder fallback 경로로 진행 (Task 1 action 에 분기 명시됨).

    5. EAS preview 빌드 + 실기기 smoke 확인:
       eas build --profile preview --platform ios 로 빌드.
       TestFlight 설치 후:
         a. 기존 화면(홈, 분석, 결과)이 정상 동작하는지 (회귀 없음)
         b. PoseViewer3DSmokeScreen 이 충돌/블랙스크린 없이 마운트되는지
         c. Canvas/GL 컨텍스트가 정상 초기화되는지 (흰 배경 #F5F5F5 표시)
         d. 손가락 제스처(회전)에 카메라가 반응하는지
         → 모두 PASS 시 smoke 통과. Task 2 의 result.tsx 병합 진행 가능.

    6. smoke 실패 시:
       "issue: [실패 내용]" 입력. Task 2 중 result.tsx 의 PoseViewer3D 삽입 부분을 보류하고
       PoseViewer3DSmokeScreen 단독 배포 상태로 작업 종료.
  </how-to-verify>
  <resume-signal>
    다음 중 하나를 입력하세요:
    - "approved OrbitControls:[true/false]" — expo-doctor PASS + typecheck 0 errors + drei OrbitControls 가용 여부 + 실기기 smoke PASS
    - "smoke-fail: [문제 내용]" — smoke 통과 못 함. Task 2 result.tsx 병합 금지로 진행
    - "issue: [문제 내용]" — 설치 에러, 버전 충돌, typecheck 실패 등
  </resume-signal>
</task>

<task type="auto">
  <name>Task 1: npm install + colors 토큰 + joints.ts reshaper(joints3d 전용) + userAnalyses.ts wiring + AccuracyLimitBadge + PoseViewer3DSmokeScreen</name>
  <files>
    app/package.json,
    app/package-lock.json,
    app/src/theme/colors.ts,
    app/src/lib/joints.ts,
    app/src/lib/userAnalyses.ts,
    app/src/components/AccuracyLimitBadge.tsx,
    app/src/components/PoseViewer3DSmokeScreen.tsx
  </files>

  <read_first>
    필수:
    1. app/package.json (전체) — 현재 의존성 목록 + expo SDK 버전 확인 (체크포인트 완료 후 실행)
    2. app/src/theme/colors.ts (전체) — Phase 12 신설 토큰 추가 패턴 (04-PATTERNS.md §colors.ts)
    3. app/src/lib/userAnalyses.ts (전체) — normalize() 방어 패턴 + Phase 8/9/12 compat layer 추가 패턴 (joints3d wiring 삽입 위치 파악)
    4. app/src/types/analysis.ts — AiSynthesisMeta interface + joints3d/joints3dKeys/joints3dFrames/coordDim/space 타입 (04-01 에서 추가됨 — 읽어 확인)
    5. app/src/components/VideoCompare.tsx (lines 1-30) — 컴포넌트 파일 헤더 + imports 패턴
    6. app/src/app/analysis/result.tsx (lines 530-560, 723-730, 941-953) — showOcclusionBadge / occlusionBadge 패턴 (AccuracyLimitBadge analog) + 기존 "추정" 카피 구현 확인
    7. 04-UI-SPEC.md §Surface 2 — AccuracyLimitBadge 규격 (배경, 테두리, 타이포, 카피)
    8. 04-RESEARCH.md §Package Legitimacy Audit — three/R3F/expo-three/drei 버전 확인
    9. 04-RESEARCH.md §Standard Stack — npm install 명령 + expo install expo-gl 명령
    10. 04-RESEARCH.md §Pitfall 1 — expo-gl 버전 불일치 함정 (expo install expo-gl 로 SDK 54 자동 맞춤)
  </read_first>

  <action>
    1. app/ 디렉토리에서 npm install 실행 (체크포인트 이미 완료된 상태여야 함):
       three @react-three/fiber expo-three @react-three/drei
       그 후: expo install expo-gl (SDK 54 자동 버전 맞춤 — Pitfall 1 방지)
       설치 후 npm view three scripts.postinstall, npm view @react-three/fiber scripts.postinstall 확인 (없으면 pass)

    2. app/src/theme/colors.ts 수정: Phase 4 신규 alias 6개 추가 (04-UI-SPEC.md Color §신규 토큰):
       viewer3dBg, viewer3dBone, viewer3dJoint, viewer3dJointNormal, accuracyLimitBg, accuracyLimitText
       alias 방식 — 기존 토큰 hex 복사 금지, 주석 "── Phase 4 신설 토큰 ──────" 헤더 추가. 기존 토큰 변경 금지.

    3. app/src/lib/joints.ts 신규: reshapePose3dData(flat, jointKeys, frames) → number[][][] | null 함수.
       파일 헤더: "Firestore flat joints3d → (T,17,3) reshape. angles(T,J) 관절각 인자는 reject — 04-01 신설 joints3d 필드 전용. firestore-nested-array-flat 박제 정합."
       함수 시그니처: reshapePose3dData(flat: number[], jointKeys: string[], frames: number): number[][][] | null
       guards:
         - !Array.isArray(flat) || !Array.isArray(jointKeys) || typeof frames !== 'number' → null
         - flat.length !== frames * jointKeys.length * 3 → null (coordDim=3 고정)
         - joints.length === 0 → null
       3중 for loop (T × J × 3) 로 (T,17,3) 반환.
       명시적 주석: "result.angles 는 (T,J) 관절각 스칼라 배열 — XYZ 좌표가 아님. 이 함수에 angles 를 전달하면 length guard 에서 null 반환."
       named export reshapePose3dData. (이전 plan 초안의 reshapeJoints3d 이름은 이 plan 에서 사용하지 않는다.)
       단위 테스트 (B3 완전성): `reshapePose3dData([], [], 0) === null` (빈 배열 — length===0 guard 가 length-product 검사보다 먼저 실행) + `reshapePose3dData(anglesFlat, anglesKeys, anglesFrames) === null` (angles 오입력 시 length 불일치로 null). joints.ts 인접 test 또는 result.tsx 검증에 단언.

    4. app/src/lib/userAnalyses.ts 수정: Phase 12 compat block (lines 116-133) 이후에 Phase 4 joints3d + aiSynthesisMeta compat block 추가.

       joints3d block (04-01 신설 필드 — result 에서 추출):
         if (result?.joints3d !== undefined || result?.joints3dKeys !== undefined) {
           result = {
             ...result,
             joints3d: Array.isArray(result.joints3d) ? result.joints3d : null,
             joints3dKeys: Array.isArray(result.joints3dKeys) ? result.joints3dKeys : null,
             joints3dFrames: typeof result.joints3dFrames === 'number' ? result.joints3dFrames : null,
             coordDim: result.coordDim === 3 ? 3 : null,
             space: typeof result.space === 'string' ? result.space : null,
           };
         }

       aiSynthesisMeta block (기존 초안 유지):
         if (result?.aiSynthesisMeta) {
           const meta = result.aiSynthesisMeta;
           result = {
             ...result,
             aiSynthesisMeta: {
               synthesizedFrameCount: meta.synthesizedFrameCount ?? 0,
               synthesizedJointKeys: meta.synthesizedJointKeys ?? [],
               synthesisPath: meta.synthesisPath ?? 'none',
               degraded: meta.degraded ?? true,
             },
           };
         }

       주석: "Phase 4 (04-02 R3) — joints3d Firestore flat null-guard. 04-01 신설 joints3d/joints3dKeys/joints3dFrames/coordDim/space 필드. angles 는 이 compat block 에서 절대 읽지 않는다 — 관절각 스칼라이므로 3D 좌표 소스 불가."
       이 wiring 이 result.tsx 로 joints3d 관련 필드를 흘리는 단일 경로 (다른 파일에서 Firestore raw 직접 읽지 않음). 기존 코드 변경 금지.

    5. app/src/components/AccuracyLimitBadge.tsx 신규: 04-PATTERNS.md §AccuracyLimitBadge.tsx 코드 그대로 구현.
       AccuracyLimitBadgeProps: visible boolean. visible false → return null.
       카피 상수 (R7 블랙박스 — 오해 소지 문구 금지):
         LINE_OCCLUSION: "가림 구간 정확도가 제한적이에요"
         LINE_SIDE: "측면 관절 추정 오차가 포함될 수 있어요."
       "AI 보완", "AI가 처리", "다각도 분석", "ai_synthesis_failed" 등 내부 코드·기술 용어는 사용자에게 미노출.
       StyleSheet: colors.softBg 배경 + warnAmber 테두리 + radius.card + 패딩 12/16.
       Ionicons "warning" size=14, colors.warnAmber. typography.boxLabel + typography.caption.
       named export AccuracyLimitBadge.

    6. app/src/components/PoseViewer3DSmokeScreen.tsx 신규 (R8 feature-flag — smoke 전용):
       파일 헤더: "Phase 4 Wave 2 R8 — PoseViewer3D 실기기 smoke screen. result.tsx 병합 전 GL/R3F 검증 전용. smoke PASS 후 이 파일은 삭제 가능."
       단순 Canvas 마운트 + 배경색 확인 + PanResponder 제스처 echo 용도만.
       import { Canvas } from '@react-three/fiber/native' (/native 경로 필수).
       Canvas 마운트 실패(Error) 시 fallback View 로 "3D 뷰어 초기화 실패 — smoke 미통과" 텍스트 표시 (결과 화면 죽지 않음 패턴 미리 검증).
       named export PoseViewer3DSmokeScreen.
  </action>

  <verify>
    <automated>
      cd /Users/kimtaesung/Dev/SunityMotion/app &&
      npm run typecheck 2>&1 | tail -10
    </automated>
  </verify>

  <done>
    - app/package.json 에 "three" + "@react-three/fiber" + "expo-three" + "@react-three/drei" 의존성 존재
    - app/src/theme/colors.ts 에 "viewer3dBg" 토큰 존재
    - app/src/lib/joints.ts 에 reshapePose3dData export 존재 (reshapeJoints3d 이름 사용 금지)
    - app/src/lib/userAnalyses.ts 에 "joints3dKeys" wiring 존재
    - app/src/components/AccuracyLimitBadge.tsx 에 "가림 구간 정확도가 제한적이에요" 카피 존재
    - app/src/components/PoseViewer3DSmokeScreen.tsx 존재 (feature-flag smoke)
    - grep -c "AI 보완\|AI가 처리\|ai_synthesis_failed" app/src/components/AccuracyLimitBadge.tsx = 0 (블랙박스 R7)
    - 회귀 가드 (W4): result.tsx 에서 visible = warnings.includes('ai_synthesis_failed') 로 파생 — warning 없는 정상 분석에서는 visible=false (배지 미표시). 정상 doc 픽스처로 배지 미렌더 단언 (오표시 방지)
    - npm run typecheck 0 errors
  </done>
</task>

<task type="auto">
  <name>Task 2: PoseViewer3D(GL ErrorBoundary) + result.tsx 통합 (smoke 통과 후만 병합)</name>
  <files>
    app/src/components/PoseViewer3D.tsx,
    app/src/app/analysis/result.tsx
  </files>

  <read_first>
    필수:
    1. app/src/components/VideoCompare.tsx (전체, ~200줄) — PoseViewer3D 파일 헤더 + imports + StyleSheet 구조 analog
    2. app/src/app/analysis/result.tsx (전체) — 섹션 삽입 위치 + 기존 state 구조 + occlusionBadge 참조 패턴 (04-PATTERNS.md §result.tsx) + 기존 추정 표기 로직(lines 530-560)
    3. 04-UI-SPEC.md §Surface 1 — PoseViewer3D 레이아웃 + 인터랙션 규격 + 카메라 preset 버튼 4개 + scrubber + 상태별 UI
    4. 04-PATTERNS.md §PoseViewer3D.tsx — Canvas 코드 구조 + SkeletonMesh + OrbitControls
    5. 04-RESEARCH.md §Pattern 4 — R3F PoseViewer3D 코드 예시 (/native import 경로 필수)
    6. 04-RESEARCH.md §Pitfall 2 — /native import 경로 미사용 위험
    7. 04-RESEARCH.md §Open Questions Q3 — @react-three/drei OrbitControls RN 호환 여부 (체크포인트에서 확인된 결과 적용)
    8. 04-UI-SPEC.md §Color — viewer3dBg #F5F5F5, viewer3dBone #1F2024, 다크 배경 금지
    9. 04-UI-SPEC.md §Copywriting Contract — 한국어 관절 이름 테이블
    10. 04-CONTEXT.md D-05, D-06 — 블랙박스 원칙 (AI 보완 사실 미노출)
    11. app/src/types/analysis.ts — AiSynthesisMeta + AnalysisResult 타입 + joints3d/joints3dKeys/joints3dFrames (04-01 추가됨)
    12. app/src/lib/joints.ts (Task 1 에서 생성됨) — reshapePose3dData 시그니처 확인
    13. app/src/lib/userAnalyses.ts (Task 1 에서 수정됨) — joints3d normalize 패턴 확인
  </read_first>

  <action>
    전제: 체크포인트 resume-signal 이 "approved" 또는 "smoke-fail" 중 하나. "smoke-fail" 시 아래 6번(result.tsx PoseViewer3D 삽입)은 skip — PoseViewer3DSmokeScreen 유지 상태로 task 완료.

    1. app/src/components/PoseViewer3D.tsx 신규:
       - 파일 헤더: "Phase 4 Wave 2 — Stage 3 사용자 3D 뷰어 (react-three-fiber + expo-three). Spike 005 VALIDATED-ARCHITECTURE. /native import 경로 필수 — web path 는 RN 에서 window is not defined. joints3d(T×17×3) 전용 — result.angles(관절각) 입력 불가."
       - import: Canvas from '@react-three/fiber/native' (경로 /native 필수, Pitfall 2 방지)
       - Canvas/GL ErrorBoundary (R8): class ErrorBoundary extends React.Component. componentDidCatch 로 GL 충돌 포착. fallback render: View + 텍스트 "3D 뷰어를 불러오지 못했어요." + hint "일반 결과는 아래에서 확인하세요." (design.md §0 빈 상태 패턴). ErrorBoundary 가 PoseViewer3D 전체 Canvas 를 감싸서 result.tsx route 가 죽지 않도록 보장.
       - OrbitControls: @react-three/drei/native 에서 import 시도. 체크포인트에서 drei OrbitControls /native export 미존재 확인 시 → react-native-gesture-handler PanResponder 로 대체. 대체 경로 주석: "// OrbitControls /native export 미존재 — PanResponder 대체 (RESEARCH Q3 MEDIUM confidence fallback)". 두 경로 모두 canvas 위 onPanResponderMove 로 카메라 yaw/pitch 업데이트 동일 UX 제공. drei OrbitControls 가용 시에는 직접 사용.
       - PoseViewer3DProps interface: joints (number[][][] | null), referenceJoints? (number[][][] | null), ipsfViolationFrames? (number[]), currentFrame (number), onFrameChange ((frame:number)=>void)
       - joints null → return null (graceful, Phase 4 이전 분석 doc 호환 + joints3d 없는 doc)
       - SkeletonMesh 내부 컴포넌트: Three.js sphere (joint) + line (bone) geometry. joint sphereGeometry radius=0.04. bone lineSegments. 정상 관절: colors.viewer3dJointNormal (#ACACAC). IPSF 위반 관절: colors.viewer3dJoint (#FF4B33). 뼈대 line: colors.viewer3dBone (#1F2024).
       - scene background: Three.js scene.background = new THREE.Color(0xF5F5F5) (colors.viewer3dBg — 다크 배경 금지, CLAUDE.md §4)
       - CameraPresetBar: 정면/측면/후면/위 버튼 4개. 각 버튼 accessibilityRole="button", accessibilityLabel={`${label} 시점으로 전환`}, hitSlop {top:8,bottom:8,left:8,right:8}. 활성: tedurite 1pt colors.brand. 비활성: colors.softBg 배경. typography.boxLabel.
       - TimelineScrubber: PanResponder onPanResponderMove → frame index 계산. accessibilityRole="adjustable", accessibilityValue={{min:0, max:totalFrames, now:currentFrame}}.
       - 3D 로딩 (joints null 이 아닌데 Three.js init 중): 중앙 dot-pulse animation (colors.brand 3점, sequential opacity).
       - mode1 두 skeleton: 사용자 회색 (#ACACAC), reference 브랜드 틴트 (colors.brandSoft)
       - StyleSheet: VideoCompare 의 styles 구조 그대로. canvas height=280, container borderRadius=radius.card, divider border. 하드코딩 hex 금지 — 토큰만.
       - named export PoseViewer3D.

    2. app/src/app/analysis/result.tsx 수정 (smoke PASS 시에만):
       a. import AccuracyLimitBadge from '../../components/AccuracyLimitBadge'
       b. import { PoseViewer3D } from '../../components/PoseViewer3D'
       c. import { reshapePose3dData } from '../../lib/joints'
          주의: reshapeJoints3d 이름으로 import 하지 말 것 (Task 1 에서 reshapePose3dData 로 정의됨).
       d. useMemo: joints3d = reshapePose3dData(
            doc?.result?.joints3d ?? null,
            doc?.result?.joints3dKeys ?? [],
            doc?.result?.joints3dFrames ?? 0
          )
          result.angles 를 절대 전달하지 말 것 — angles 는 관절각 (T,J) 스칼라이므로 reshape 불가. reshapePose3dData 의 length guard 에서 null 반환하지만, 의미상 오용이므로 source 레벨에서 차단.
       e. AccuracyLimitBadge 삽입: 헤더 styles.header 하단, 기존 sub 텍스트 아래.
          visible={doc?.result?.warnings?.includes('ai_synthesis_failed') ?? false}
       f. PoseViewer3D 섹션 삽입: VideoCompare 바로 아래, 04-UI-SPEC.md §Surface 1 배치 순서 정합.
          {joints3d != null && (
            <View accessibilityRole="image" accessibilityLabel="3D 자세 뷰어, 손가락으로 회전 가능">
              <PoseViewer3D joints={joints3d} currentFrame={currentFrame} onFrameChange={setCurrentFrame} />
            </View>
          )}
       g. currentFrame state: const [currentFrame, setCurrentFrame] = useState(0)
       h. 기존 추정 표기 로직 (showOcclusionBadge, isAngleEstimated, showEstimateTooltip 기반의 "추정 N°" 카피) 변경 금지 — Phase 12.5 구현 그대로 유지. D-05 블랙박스: AccuracyLimitBadge 카피("가림 구간 정확도가 제한적이에요")와 기존 "추정" 카피는 공존하지만 "AI 보완" 문구는 어느 쪽에도 미노출.
       i. 기존 VideoCompare, 점수 카드, 코칭 팁, CTA 섹션 변경 없음.
  </action>

  <verify>
    <automated>
      cd /Users/kimtaesung/Dev/SunityMotion/app &&
      npm run typecheck 2>&1 | tail -10
    </automated>
  </verify>

  <done>
    - npm run typecheck 0 errors (POSE-03-h 충족)
    - grep -c "from '@react-three/fiber/native'" app/src/components/PoseViewer3D.tsx 결과 != 0 (Pitfall 2 방지)
    - grep -c "reshapePose3dData" app/src/app/analysis/result.tsx 결과 != 0 (R3 정합)
    - grep -c "result\.angles" app/src/app/analysis/result.tsx 결과 0 (angles 오용 차단)
    - grep -c "joints3d" app/src/app/analysis/result.tsx 결과 != 0 (04-01 신설 필드 소비)
    - grep -c "AccuracyLimitBadge" app/src/app/analysis/result.tsx 결과 != 0 (D-08 통합)
    - grep -c "PoseViewer3D" app/src/app/analysis/result.tsx 결과 != 0 (smoke PASS 시, Stage 3 통합)
    - grep -c "viewer3dBg\|viewer3dBone" app/src/components/PoseViewer3D.tsx 결과 != 0 (토큰 사용, 하드코딩 0)
    - grep -c "0x2A2A2A\|#2A2A2A\|#1a1a1a\|#000000" app/src/components/PoseViewer3D.tsx 결과 0 (다크 배경 금지)
    - grep -c "AI.*보완\|AI가 처리\|다각도 분석" app/src/components/PoseViewer3D.tsx app/src/app/analysis/result.tsx 결과 0 (D-05 블랙박스 R7)
    - grep -c "ErrorBoundary" app/src/components/PoseViewer3D.tsx 결과 != 0 (R8 GL fallback)
    - grep -c "aiSynthesisMeta" app/src/lib/userAnalyses.ts 결과 != 0 (Firestore wiring)
    - grep -c "joints3dKeys" app/src/lib/userAnalyses.ts 결과 != 0 (R3 joints3d wiring)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Three.js Canvas | WebGL 컨텍스트가 expo-gl GLView 위에서 동작 — 네이티브 GPU 직접 접근. 초기화 실패 → ErrorBoundary 격리 (R8) |
| Firestore flat → reshape | joints3d flat array 를 (T,17,3) 로 reshape 시 길이 검증 실패 → null graceful. angles 오입력은 length guard 에서 null 반환 (R3) |
| userAnalyses.ts normalize() | Firestore raw → TS typed 변환 유일 경로 — joints3d/aiSynthesisMeta 누락 시 null 유지 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-W2-01 | Tampering | reshapePose3dData 입력 검증 | mitigate | flat.length !== T*J*3 guard → null 반환. angles 입력 시도는 길이 불일치로 null (CLAUDE.md Firestore nested-array 금지 정합). |
| T-04-W2-02 | Spoofing | AccuracyLimitBadge 카피 (R7) | mitigate | "AI 보완", "AI가 처리", "ai_synthesis_failed" 등 내부 코드 노출 금지. 블랙박스 카피만 ("가림 구간 정확도가 제한적이에요"). grep 게이트로 CI 수준 검증. |
| T-04-W2-03 | Denial of Service | Three.js init 실패 / GL 컨텍스트 충돌 (R8) | mitigate | class ErrorBoundary 가 Canvas 전체 감쌈 → 뷰어 섹션만 생략, result.tsx route 계속 동작. 실기기 smoke screen 으로 사전 검증. |
| T-04-W2-04 | Information Disclosure | joints3d 데이터 소스 오용 (R3) | mitigate | result.angles 전달 시 reshapePose3dData length guard → null. userAnalyses.ts compat block 에서 angles 필드 접근 코드 부재 확인 필수. |
| T-04-W2-SC | Tampering | npm install (three/R3F 새 패키지) | mitigate | Package Legitimacy Audit 표 (RESEARCH.md) 확인 완료 (all Approved). 설치 후 postinstall 스크립트 없음 재확인 필수. |
</threat_model>

<verification>
npm run typecheck (app/) → 0 errors (POSE-03-h GREEN)
grep "from '@react-three/fiber/native'" app/src/components/PoseViewer3D.tsx → 발견 (/native 경로 확인)
grep "reshapePose3dData" app/src/app/analysis/result.tsx → 발견 (R3: joints3d 소스, angles 오용 차단)
grep "joints3dKeys" app/src/lib/userAnalyses.ts → 발견 (joints3d Firestore wiring)
grep "AccuracyLimitBadge\|PoseViewer3D" app/src/app/analysis/result.tsx → 양쪽 발견 (smoke PASS 시)
grep "ErrorBoundary" app/src/components/PoseViewer3D.tsx → 발견 (R8 GL fallback)
grep "AI.*보완\|ai_synthesis_failed" app/src/components/AccuracyLimitBadge.tsx → 0 (R7 블랙박스)
grep "result\.angles" app/src/app/analysis/result.tsx → 0 (R3 angles 오용 완전 차단)
</verification>

<success_criteria>
Wave 2 완료 기준 (ROADMAP SC #4 + SC #6 관련):
1. 실기기 R3F/expo-gl PoseViewer3DSmokeScreen 검증 체크포인트 PASS (autonomous:false 의 근거)
2. PoseViewer3D 컴포넌트: /native import + ErrorBoundary(R8) + stick figure SkeletonMesh + CameraPresetBar 4개 + TimelineScrubber + drei OrbitControls 또는 PanResponder fallback (Q3 분기)
3. joints.ts reshapePose3dData: joints3d(T×17×3) 전용, result.angles 오용 차단 (R3)
4. userAnalyses.ts normalize(): joints3d/joints3dKeys/joints3dFrames/coordDim/space + aiSynthesisMeta/warnings wiring (단일 Firestore 읽기 경로, R3)
5. AccuracyLimitBadge: ai_synthesis_failed 트리거, 블랙박스 카피 준수 (R7), "추정" 기존 카피와 공존
6. result.tsx 통합: VideoCompare 아래 삽입 + 헤더 하단 배지 (smoke PASS 시)
7. npm run typecheck 0 errors (POSE-03-h)
8. 다크 배경 금지, 토큰 전용 (하드코딩 0)
9. 기존 result.tsx 기능 회귀 없음
10. UI-SPEC status draft → approved 기록 (executor 수행)
</success_criteria>

<output>
Create `.planning/phases/04-ux-occlusion-confidence/04-02-SUMMARY.md` when done
</output>
