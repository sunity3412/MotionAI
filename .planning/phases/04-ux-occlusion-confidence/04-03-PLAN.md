---
phase: "04"
plan: "03"
type: execute
wave: 3
depends_on:
  - 04-01
files_modified:
  - backend/shared/python/sunity_shared/analysis/synthesis/cylindrical_mesh.py
  - backend/shared/python/sunity_shared/analysis/synthesis/virtual_renderer.py
  - backend/runpod_inference/requirements.txt
  - backend/tests/phase04/test_evaluate_4way.py
autonomous: false
requirements:
  - POSE-03

must_haves:
  truths:
    - "Wave 3a smoke: build_humanoid_mesh + render_12_views_safe 가 RunPod EGL 환경에서 crash 없이 12 view artifact 를 산출하고, Mac local 에서는 dummy fallback 이 분석을 중단시키지 않는다 (D-07, RESEARCH Pitfall 3) — 정확도 주장 0"
    - "Wave 3a license gate: cylindrical_mesh.py 에 smplx import 가 없고, trimesh MIT + pyrender MIT + numpy BSD 만 사용됨이 단위 테스트로 확인된다 (D-20)"
    - "Wave 3b 실 RunPod accuracy gate: 실 RTMW 재추론 on rendered views + evaluate_4way baseline 대비 실 비교가 @pytest.mark.integration 테스트로 박제된다 — CI 제외 가능, RunPod 필요"
    - "합성 출력은 KeypointReport + aiSynthesisMeta 에만 흐르고 coco_array (DTW/kismam/IPSF 채점 입력) 를 mutate 하지 않는다 (R1 non-scoring 하드월, RESEARCH Pitfall 7)"
    - "G4 is_reference 가드: synthesize_occluded_joints 호출 시 scene_findings['is_reference']=True 이면 SynthesisResult(status=\"skipped\", warnings=(\"g4_reference_guard\",)) 반환 (zeros sentinel 금지 — R2, G4 occlusion FP 가드 — 04-CONTEXT code_context)"
    - "Wave 3b 미완이어도 Wave 2(UI) 와 Wave 5(reference 재처리) 는 Wave 1 Gemini path 기준으로 진행 가능하다 — 3b 는 04-05 depends_on 을 차단하지 않는다 (반박#2)"
  artifacts:
    - path: backend/shared/python/sunity_shared/analysis/synthesis/cylindrical_mesh.py
      provides: "CylindricalMeshAdapter (SynthesisAdapter Protocol 구현체) — Wave 3a smoke 경로"
      contains: "class CylindricalMeshAdapter"
    - path: backend/shared/python/sunity_shared/analysis/synthesis/virtual_renderer.py
      provides: "render_12_views_safe (EGL headless + Mac dummy fallback)"
      contains: "PYOPENGL_PLATFORM"
    - path: backend/runpod_inference/requirements.txt
      provides: "trimesh + pyrender + pyopengl RunPod 의존성"
      contains: "trimesh"
    - path: backend/tests/phase04/test_evaluate_4way.py
      provides: "Wave 3a smoke/license 게이트 + Wave 3b @pytest.mark.integration 실 RunPod 재추론 게이트 skeleton (04-05 append 예정)"
      contains: "evaluate_4way"
  key_links:
    - from: backend/shared/python/sunity_shared/analysis/synthesis/cylindrical_mesh.py
      to: backend/shared/python/sunity_shared/analysis/synthesis/interfaces.py
      via: "SynthesisAdapter Protocol 구현 (04-01 신설)"
      pattern: "class CylindricalMeshAdapter"
    - from: backend/shared/python/sunity_shared/analysis/synthesis/cylindrical_mesh.py
      to: backend/shared/python/sunity_shared/analysis/synthesis/virtual_renderer.py
      via: "render_12_views_safe 호출"
      pattern: "render_12_views_safe"
    - from: backend/tests/phase04/test_evaluate_4way.py
      to: ".planning/spikes/001-dataset-eval-harness/metrics.py"
      via: "evaluate_4way 함수 재사용 (PathOutput 형식 정합)"
      pattern: "evaluate_4way"

user_setup: []
---

<objective>
Phase 4 Wave 3 — Stage 3' backend mesh render. Wave 3 는 두 단계로 분리된다.

Wave 3a (smoke): Cylindrical humanoid mesh + 12 virtual camera pyrender headless render (Spike 002b VALIDATED-SKELETON) 를 SynthesisAdapter Protocol (04-01 신설) 의 2번째 구현체인 CylindricalMeshAdapter 로 이관. mesh build + 12-view render artifact 가 RunPod EGL 환경에서 동작함을 검증. 정확도 주장 없음 — `+0.15 boosted confidence` 는 pipeline 연결 검증용 임시 반환이며, 이것만으로 scoring promote 하거나 acceptance 를 통과시키지 않는다 (R1 non-scoring 하드월, calibration-source-hard-gate).

Wave 3b (실 RunPod accuracy gate): 실 RTMW 재추론 on rendered views + evaluate_4way baseline 대비 실 비교. @pytest.mark.integration 으로 박제 (RunPod 필요). Wave 3b 완료 전에도 Wave 2(UI), Wave 5(reference 재처리) 는 Wave 1 Gemini path 기준으로 진행 가능하다 (반박#2). 3b 는 정확도 게이트 전용이며 phase 전체를 blocking 하지 않는다.

Purpose: D-18 SECONDARY path 실구현. Gemini Vision (PRIMARY) 이 indeterminate 반환하거나 비용 손절 발동 시 mesh render fallback 으로 occlusion 보완 정확도를 추가로 확보한다. Spike 001 evaluate_4way 를 POSE-03-g 통합 게이트로 재사용.

Output: cylindrical_mesh.py (CylindricalMeshAdapter) + virtual_renderer.py (EGL headless + Mac dummy fallback) + RunPod requirements.txt 업데이트 + test_evaluate_4way.py (Wave 3a smoke/license + Wave 3b integration skeleton — 04-05 에서 RunPod 통합 테스트 append)
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-ux-occlusion-confidence/04-CONTEXT.md
@.planning/phases/04-ux-occlusion-confidence/04-RESEARCH.md
@.planning/phases/04-ux-occlusion-confidence/04-PATTERNS.md
@.planning/phases/04-ux-occlusion-confidence/04-VALIDATION.md
@.planning/phases/04-ux-occlusion-confidence/04-01-SUMMARY.md

@backend/shared/python/sunity_shared/analysis/synthesis/interfaces.py
@backend/runpod_inference/requirements.txt
@.planning/spikes/002b-cylindrical-mesh-virtual-render/mesh_builder.py
@.planning/spikes/002b-cylindrical-mesh-virtual-render/render.py
@.planning/spikes/001-dataset-eval-harness/metrics.py
@.planning/spikes/001-dataset-eval-harness/harness.py
@backend/tests/phase04/conftest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 3a smoke): CylindricalMeshAdapter + virtual_renderer.py — mesh build + 12-view render artifact + license gate (정확도 주장 0)</name>
  <files>
    backend/shared/python/sunity_shared/analysis/synthesis/cylindrical_mesh.py,
    backend/shared/python/sunity_shared/analysis/synthesis/virtual_renderer.py,
    backend/runpod_inference/requirements.txt
  </files>

  <read_first>
    필수:
    1. .planning/spikes/002b-cylindrical-mesh-virtual-render/mesh_builder.py (전체) — build_humanoid_mesh, SEGMENTS, _cylinder_between 이관 기준 원본
    2. .planning/spikes/002b-cylindrical-mesh-virtual-render/render.py (전체) — render_12_views, PYOPENGL_PLATFORM=egl 패턴, make_12_camera_set, _camera_pose, PYRENDER_AVAILABLE fallback 이관 기준 원본
    3. backend/shared/python/sunity_shared/analysis/synthesis/interfaces.py (전체) — SynthesisAdapter Protocol 시그니처, identify_occlusion_targets, merge_with_temporal (04-01 신설)
    4. 04-PATTERNS.md §cylindrical_mesh.py — 파일 헤더 패턴 + PYOPENGL_PLATFORM 선언 위치
    5. 04-RESEARCH.md §Pattern 5: pyrender EGL Headless — PYOPENGL_PLATFORM=egl os.environ.setdefault 위치 + renderer.delete() 필수 박제
    6. 04-RESEARCH.md §Pitfall 3 — EGL 초기화 순서 (import pyrender 보다 먼저)
    7. 04-RESEARCH.md §Pitfall 4 — Firestore nested-array (합성 메타데이터 flat)
    8. 04-RESEARCH.md §Pitfall 7 — RTMW 3D scoring 행렬에 합성 좌표 주입 금지
    9. 04-CONTEXT.md D-18, D-20, D-29~D-30 — SECONDARY path 위치 + license gate + Stage 3' 아키텍처
    10. backend/runpod_inference/requirements.txt (전체) — 현재 의존성 목록 확인 후 trimesh/pyrender/pyopengl 추가
  </read_first>

  <behavior>
    virtual_renderer.py:
    - PYOPENGL_PLATFORM=egl 설정은 모듈 최상단 (import 문 이전) os.environ.setdefault("PYOPENGL_PLATFORM", "egl") 로 위치 — import pyrender 보다 반드시 먼저 (RESEARCH Pitfall 3).
    - try/except (ImportError, OSError) 로 pyrender import 시도 → PYRENDER_AVAILABLE bool 박제 (Spike 002b render.py 패턴 그대로).
    - render_12_views_safe(mesh, width=256, height=256) → list[np.ndarray]: PYRENDER_AVAILABLE=False 시 np.full dummy 12장 반환 (Mac local 개발 graceful).
    - PYRENDER_AVAILABLE=True 시: pyrender.OffscreenRenderer → 12 카메라 루프 → renderer.delete() 명시적 해제 (GPU 메모리 관리).
    - make_12_camera_set(distance=3.0, pitch_deg=0.0) → list[VirtualCamera] (30° 간격 12 yaw).
    - _camera_pose(camera, target) → np.ndarray 4x4 (pyrender camera-to-world).

    cylindrical_mesh.py:
    - 모듈 헤더 docstring: "Stage 3' CylindricalMeshAdapter — Spike 002b VALIDATED-SKELETON 이관. SynthesisAdapter Protocol (D-18 SECONDARY). license: trimesh MIT + pyrender MIT + numpy BSD. SMPL-X 의존 0 (D-20 박제). Wave 3a = mesh build + render artifact smoke gate (정확도 주장 0). 실 RTMW 재추론 → Wave 3b (test_evaluate_4way.py @integration)."
    - from __future__ import annotations + os.environ.setdefault("PYOPENGL_PLATFORM", "egl") 상단 선언 (virtual_renderer 임포트 전).
    - build_humanoid_mesh(joints: np.ndarray) → trimesh.Trimesh: Spike 002b mesh_builder.py 코드 그대로 이관. COCO-17 joint index 상수, SEGMENTS dataclass, _cylinder_between 헬퍼 포함.
    - CylindricalMeshAdapter 클래스: SynthesisAdapter Protocol 구현 (04-01 신설 인터페이스).
      - synthesize_occluded_joints(joint_sequence, confidence_sequence, occluded_mask, scene_findings) → **SynthesisResult** (04-01 계약 — GeminiViewReasoner 와 동일 Protocol 반환 타입. tuple/(zeros,zeros) 절대 금지, R2 fix). synthesis/interfaces.py 에서 SynthesisResult import.
        - is_reference guard: scene_findings.get("is_reference", False) → **SynthesisResult(status="skipped", warnings=("g4_reference_guard",))** + log.info (G4 occlusion FP 가드 — 04-CONTEXT code_context).
        - occluded_mask.any() = False → **SynthesisResult(status="skipped")** 조기 반환 (비용 0).
        - occluded frame 별 build_humanoid_mesh → render_12_views_safe → 12 view 이미지 저장 (Wave 3b 가 이 이미지에서 실 RTMW 재추론을 수행). _rerun_rtmw_on_views 헬퍼는 현재 NotImplementedError 대신 기본 joints 그대로 반환 + log.warning "RTMW rerun not wired — Wave 3b RunPod task 에서 구현 예정 (test_evaluate_4way.py @integration 확인)" + boosted confidence 임시 반환.
        - 성공 경로(Wave 3a placeholder): **SynthesisResult(status="applied", joints=primary_joints, confidence=boosted_conf, meta={"synthesisPath": "cylindrical_smoke_placeholder"})**. boosted confidence(임시): 합성 대상 frame 에서 occluded_mask=True 관절에 conf = min(primary_conf + 0.15, 1.0) 적용. 이 +0.15 boosted confidence 는 pipeline 연결 검증용 임시값이며 scoring promote 근거로 사용하지 않는다 (meta.synthesisPath=placeholder 표식). 실 RunPod 재추론 결과만 scoring 게이트 진입 가능 (calibration-source-hard-gate, R1 non-scoring 하드월).
        - 모든 예외 except Exception: log.exception + **SynthesisResult(status="failed", warnings=("exception",))** 반환 (D-07 graceful degrade 계약. zeros array sentinel 절대 금지 — R2).
    - SMPL-X import 금지 주석: "# SMPL-X 절대 금지 — D-20 박제 (belle 명시 완전 최후의 보류, $7,300/yr)"
    - coco_array (DTW/kismam 채점 입력) mutate 금지 주석: "# RTMW 3D scoring matrix mutate 0 — 합성 결과는 KeypointReport + aiSynthesisMeta 만 영향 (RESEARCH Pitfall 7, R1 non-scoring 하드월)"

    requirements.txt (runpod_inference):
    - trimesh>=4.12.2 추가
    - pyrender>=0.1.45 추가
    - pyopengl 추가 (EGL 런타임; NVIDIA 드라이버 포함이나 pip 명시 박제)
    - 기존 항목 변경 금지.
  </behavior>

  <action>
    1. virtual_renderer.py 신설: Spike 002b render.py 를 그대로 이관. 파일명 render.py → virtual_renderer.py. export 함수명 render_12_views → render_12_views_safe (safe suffix: Mac dummy fallback 포함 명시). 모듈 docstring: "Stage 3' 12 virtual camera pyrender EGL headless render. PYOPENGL_PLATFORM=egl 는 이 모듈 import 즉시 os.environ.setdefault 로 설정됨 — 반드시 import pyrender 보다 먼저 실행되어야 한다 (RESEARCH Pitfall 3). Mac local 개발 환경(PYRENDER_AVAILABLE=False)에서는 dummy 128-gray 12장 반환."

    2. cylindrical_mesh.py 신설: Spike 002b mesh_builder.py (build_humanoid_mesh + 보조 함수 전체) + CylindricalMeshAdapter 클래스. 파일 헤더 주석에 Spike 002b VALIDATED-SKELETON 출처 + D-18 SECONDARY + D-20 license gate + Wave 3a smoke(정확도 주장 0) + Wave 3b @integration 안내 명시. CylindricalMeshAdapter.synthesize_occluded_joints 는 3단계 처리: (a) G4 가드 → (b) 합성 필요 frame 루프 → build_humanoid_mesh + render_12_views_safe 호출 → (c) _rerun_rtmw_on_views placeholder (boosted conf 임시 반환, Wave 3b 에서 실 구현). 주석: "TODO(Wave 3b): _rerun_rtmw_on_views 에 실제 RTMW 추론 파이프라인 연결. +0.15 boosted confidence 는 pipeline 연결 검증용 임시값이며 scoring promote 근거 아님 (calibration-source-hard-gate)."

    3. backend/runpod_inference/requirements.txt 수정: 기존 파일 읽기 후 trimesh>=4.12.2, pyrender>=0.1.45, pyopengl 세 줄 추가 (기존 항목 순서 유지, 가장 뒤에 추가).

    Commit: 단일 atomic commit "feat(04-03): Wave 3a CylindricalMeshAdapter smoke — mesh build + 12-view render (POSE-03 D-18 SECONDARY, 정확도 주장 0)"
  </action>

  <verify>
    <automated>
      cd /Users/kimtaesung/Dev/SunityMotion &&
      python3 -c "
import sys; sys.path.insert(0, 'backend/shared/python')
from sunity_shared.analysis.synthesis.cylindrical_mesh import CylindricalMeshAdapter, build_humanoid_mesh
from sunity_shared.analysis.synthesis.virtual_renderer import render_12_views_safe, PYRENDER_AVAILABLE
import numpy as np
joints = np.zeros((17, 3), dtype=float)
joints[5] = [-0.2, 1.5, 0]; joints[6] = [0.2, 1.5, 0]
joints[11] = [-0.15, 0.9, 0]; joints[12] = [0.15, 0.9, 0]
mesh = build_humanoid_mesh(joints)
assert mesh.vertices.shape[1] == 3
views = render_12_views_safe(mesh)
assert len(views) == 12
print('PYRENDER_AVAILABLE:', PYRENDER_AVAILABLE)
print('mesh vertices:', mesh.vertices.shape)
print('12 views: OK')
adapter = CylindricalMeshAdapter()
seq = np.tile(joints, (30, 1, 1))
conf = np.full((30, 17), 0.25)
mask = conf < 0.3
res = adapter.synthesize_occluded_joints(seq, conf, mask, {})
assert res.status == 'applied', f'status: {res.status}'
assert res.joints.shape == (30, 17, 3), f'shape: {res.joints.shape}'
ref = adapter.synthesize_occluded_joints(seq, conf, mask, {'is_reference': True})
assert ref.status == 'skipped' and 'g4_reference_guard' in ref.warnings, 'G4 guard failed'
print('CylindricalMeshAdapter.synthesize_occluded_joints: OK')
print('G4 is_reference guard: OK')
src = open('backend/shared/python/sunity_shared/analysis/synthesis/cylindrical_mesh.py').read().lower()
assert 'smplx' not in src, 'SMPL-X detected!'
print('SMPL-X gate: PASS')
" 2>&1
    </automated>
  </verify>

  <acceptance_criteria>
    Wave 3a smoke acceptance (정확도 주장 0):
    - build_humanoid_mesh(zeros_joints) → trimesh.Trimesh vertices.shape[1]==3 (crash 없음)
    - render_12_views_safe(mesh) → list 길이 12 (PYRENDER_AVAILABLE=False 시 dummy 회색 이미지)
    - CylindricalMeshAdapter().synthesize_occluded_joints(seq, conf, mask, {}) → SynthesisResult(status="applied", joints shape (T,17,3), confidence (T,17), meta placeholder) — RTMW 재추론 실구현 없어도 smoke PASS
    - G4 가드: is_reference=True → SynthesisResult(status="skipped", warnings=("g4_reference_guard",)) 반환 + 예외 없음 (zeros 아님)
    - cylindrical_mesh.py 소스에 "smplx" 미포함 (license gate, D-20)
    - grep "trimesh" backend/runpod_inference/requirements.txt — 발견
    - grep "pyrender" backend/runpod_inference/requirements.txt — 발견

    Wave 3a 에서 통과 불가 항목 (scoring 차단):
    - +0.15 boosted confidence 단독으로 evaluate_4way axis_b 를 근거로 scoring promote 하는 acceptance 없음
    - 실 RunPod RTMW 재추론 없이 정확도 수치를 acceptance 로 주장하는 항목 없음
    - 위 두 항목은 Wave 3b @integration 테스트에서만 검증됨 (calibration-source-hard-gate)
  </acceptance_criteria>

  <done>CylindricalMeshAdapter import 성공 + synthesize_occluded_joints shape contract 통과 + G4 가드 동작 + SMPL-X 의존 0 확인 + RunPod requirements.txt 업데이트 완료. Wave 3a smoke 기준 완료 — 실 RTMW 재추론은 Wave 3b @integration task 에서 별도 게이트.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1.5 (Wave 3a checkpoint): Wave 3a smoke artifacts 확인</name>
  <what-built>
    Task 1 완료 후: cylindrical_mesh.py + virtual_renderer.py 신설, RunPod requirements.txt 업데이트. Wave 3a smoke acceptance 전부 자동 검증됨. 이 checkpoint 는 Wave 3b @integration 진행 여부 결정을 위한 human gate.
  </what-built>
  <how-to-verify>
    1. 위 verify 명령 출력에서 "12 views: OK", "SMPL-X gate: PASS", "G4 is_reference guard: OK" 확인.
    2. cylindrical_mesh.py 헤더에 "Wave 3a = mesh build + render artifact smoke gate (정확도 주장 0)" 주석 포함 확인.
    3. Wave 3b 진행 결정: RunPod Pod 현재 가용 여부 확인. 가용하면 Task 2 (Wave 3b) 즉시 진행. 미가용이면 Wave 2(UI) + Wave 5(reference) 를 먼저 진행하고 Wave 3b 는 RunPod 재기동 후 별도 실행 (반박#2 — 3b 는 phase blocker 아님).
    4. **Wave 3b skip 시 하드 게이트 (B4):** CylindricalMeshAdapter 는 pipeline 에서 `SYNTHESIS_MESH_ENABLED=0` (기본 비활성) 유지 필수. Wave 3b @integration(실 RTMW 재추론) GREEN 전까지 mesh path 는 점수/UI 에 영향 0 — placeholder boosted confidence 가 운영에 새지 않게 차단 (R1 non-scoring 하드월 + calibration-source-hard-gate). Gemini path(Wave 1)만 활성.
  </how-to-verify>
  <resume-signal>"approved" 또는 "Wave 3b 건너뛰고 Wave 2/5 진행" (→ SYNTHESIS_MESH_ENABLED=0 유지, mesh 비활성 박제) 또는 Wave 3b 진행 지시</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (Wave 3a smoke test + Wave 3b skeleton): test_evaluate_4way.py — POSE-03-g 게이트 신규 작성</name>
  <files>
    backend/tests/phase04/test_evaluate_4way.py
  </files>

  <read_first>
    필수:
    1. .planning/spikes/001-dataset-eval-harness/metrics.py (전체) — evaluate_4way 함수 + PathOutput dataclass + axis_a/b/c 계산 로직
    2. .planning/spikes/001-dataset-eval-harness/harness.py (전체) — make_synthetic_path fixture 패턴 + smoke_test evaluate_4way 호출 패턴 (테스트 구조 기준)
    3. .planning/spikes/001-dataset-eval-harness/ipsf_criteria.py — FULLY_EXTENDED.tolerance ±20°, PHASE4_EVAL_AXIS_MAPPING (axis_b = occlusion 감점 감소 게이트)
    4. backend/tests/phase04/conftest.py — joint_seq_60f fixture (04-00 에서 추가됨, T=60 J=17 3) + pytest 구조
    5. 04-RESEARCH.md §Spike 001 evaluate_4way 를 Verification Gate 로 활용 — 통과 기준 박제
    6. 04-RESEARCH.md §Validation Architecture Test Framework — pytest 실행 명령
    7. 04-VALIDATION.md §POSE-03-g — "cylindrical mesh path vs baseline RTMW-mirror IPSF 감점 비교" 게이트
    8. 04-CONTEXT.md D-22 — IPSF occlusion 감점 -0.5pt/회, baseline -7.60pts/video (기준선)
    9. 04-CONTEXT.md D-21 — IPSF Page 19 split angle must remain same (axis_a 통과 기준)
  </read_first>

  <behavior>
    test_evaluate_4way.py 구조 — Wave 3a (smoke/license, unit) + Wave 3b (실 RunPod 재추론, @integration skeleton):

    파일 최상단 주석: "04-03 신규 작성 — Wave 3a smoke + license 게이트 (unit). Wave 3b @integration skeleton (RunPod 필요). 04-05 에서 RunPod 통합 테스트 (test_evaluate_4way_reprocess_vs_baseline) 를 이 파일 하단에 append."

    [Wave 3a — unit, 정확도 주장 0]

    test_axis_b_smoke_synthetic_boost:
    - synthetic boost 시뮬: cylindrical_mesh PathOutput confidence = clip(rtmw_mirror_conf + 0.15, 0, 1).
    - evaluate_4way 호출 후 report.axis_b["cylindrical_mesh"]["rate_reduction_pct"] >= 0.0 → PASS.
    - 중요 주석: "이 테스트는 +0.15 synthetic boost 가 evaluate_4way 계산에서 NaN 없이 동작함을 검증하는 smoke test 임. 실 RunPod RTMW 재추론 없이 산출된 rate_reduction_pct 는 scoring promote 근거가 아님 (calibration-source-hard-gate). 실 accuracy gate 는 test_axis_b_real_rtmw_integration (@integration) 에서 수행."
    - 3-way paths: rtmw_mirror (baseline), gemini_view (PRIMARY), cylindrical_mesh (SECONDARY per D-18). Higgsfield/MagicMan 제외 (D-19 INVALIDATED).

    test_mesh_adapter_excluded_without_env_flag (MEDIUM-1 — accidental activation 방지):
    - SYNTHESIS_MESH_ENABLED unset/0 일 때 _get_synthesis_adapter() 가 **CylindricalMeshAdapter instance 를 반환하지 않음**을 단언 (04-01 은 _get_synthesis_adapter 를 lazy singleton GeminiViewReasoner 로 정의 — MEDIUM-1: "active chain" 추상화 신설 아님. mesh 모듈 import 가능 ≠ adapter 반환). Wave 3b GREEN 전까지 placeholder(status="applied") 가 운영 경로(점수/UI)에 새지 않게 차단 — B4 게이트의 unit 증명.

    test_axis_a_split_angle_consistency:
    - 3-way path 모두 axis_a pass rate >= 0.0 (smoke — NaN 없이 계산 완료 확인).
    - split_frame_indices = [0, 5, 10, 15, 20].

    test_cylindrical_mesh_no_smplx_import:
    - cylindrical_mesh.py 소스 text 에 "smplx" 미포함 확인 (D-20 license gate).

    test_cylindrical_mesh_license_stack:
    - pytest.importorskip("trimesh") + pytest.importorskip("pyrender") (미설치 환경 → SKIP 처리).
    - 각 모듈 __file__ 경로 print (slopcheck 수동 참조용).

    [Wave 3b skeleton — @pytest.mark.integration, RunPod 필요]

    test_axis_b_real_rtmw_integration:
    - @pytest.mark.integration 마커 (CI 제외: pytest -m "not integration").
    - Wave 3b 실구현 완료 전: pytest.skip("Wave 3b 실 RunPod RTMW 재추론 미구현 — _rerun_rtmw_on_views 연결 후 skip 제거") 로 skeleton 박제.
    - 완료 기준 주석 박제: "RunPod 에서 12-view 렌더 이미지 → 실 RTMW 재추론 → PathOutput wrapping → evaluate_4way axis_b rate_reduction_pct 실 검증. 산출물(렌더 이미지/배열) persisted artifact 필수. baseline evaluate_4way 와 실 비교. acceptance: rate_reduction_pct > 0 (baseline 대비 실 개선 확인)."
    - 이 skeleton 의 위치: 파일 하단. 04-05 는 이 skeleton 아래에 RunPod 통합 테스트를 append.

    Wave 3b 미완이어도 Wave 2(UI), Wave 5(reference) 진행 가능:
    - test_axis_b_real_rtmw_integration 가 skip 상태이어도 Wave 2, Wave 5 의 depends_on 이 충족됨을 파일 주석에 명시.
    - "Wave 3b @integration skip = Wave 2/5 진행 차단 아님 (04-05 depends_on: 04-03 3a smoke까지)".

    conftest.py sys.path 처리:
    - conftest.py 에 spike harness path 없으면 test_evaluate_4way.py 상단에 sys.path.insert(0, str(pathlib.Path(__file__).parents[3] / ".planning/spikes/001-dataset-eval-harness")).
    - joint_seq_60f / conf_seq_60f fixture 사용 (T=60, 30f 사용 금지).
  </behavior>

  <action>
    1. test_evaluate_4way.py 신규 작성 (04-03 소유 — 04-05 는 이 파일에 RunPod 통합 테스트 append, 04-05 에서 재신설 금지). Spike 001 harness.py 의 smoke_test 구조를 pytest 형식으로 변환. make_synthetic_path 헬퍼를 파일 내 로컬 함수로 복사 (harness.py 의존성 제거). evaluate_4way 는 .planning/spikes/001-dataset-eval-harness/metrics.py 에서 직접 import.

    2. Wave 3a unit tests (4개): test_axis_b_smoke_synthetic_boost / test_axis_a_split_angle_consistency / test_cylindrical_mesh_no_smplx_import / test_cylindrical_mesh_license_stack. 각 함수 docstring 에 POSE-03-g 레퍼런스 + 평가 기준 박제.

    3. Wave 3b @integration skeleton (1개): test_axis_b_real_rtmw_integration — pytest.skip skeleton. 완료 기준 + 산출물 명세 주석 완비. 04-05 append 예정 표기.

    4. 파일 최상단에 Wave 3a/3b 분리 구조, phase blocker 비차단 원칙, 04-05 append 예정 명시 (블록 주석).

    Commit: "test(04-03): Wave 3a smoke/license gate + Wave 3b @integration skeleton (POSE-03-g, 04-05 append 예정)"
  </action>

  <verify>
    <automated>
      cd /Users/kimtaesung/Dev/SunityMotion &&
      python3 -m pytest backend/tests/phase04/test_evaluate_4way.py -x -q -m "not integration" 2>&1 | tail -15
    </automated>
  </verify>

  <acceptance_criteria>
    - pytest backend/tests/phase04/test_evaluate_4way.py -x -q -m "not integration" → Wave 3a 전체 GREEN (또는 trimesh 미설치 시 SKIP — PASS 처리)
    - test_cylindrical_mesh_no_smplx_import → PASS
    - test_axis_b_smoke_synthetic_boost → PASS + "scoring promote 근거 아님" 주석 포함
    - test_axis_a_split_angle_consistency → PASS (NaN 없이 3-way 계산)
    - test_axis_b_real_rtmw_integration → SKIP (Wave 3b skeleton — skip 이 PASS 취급됨)
    - pytest backend/tests/phase04/ -x -q → 기존 POSE-03-a~f suite 회귀 0
    - grep -v '^#' backend/tests/phase04/test_evaluate_4way.py | grep -c "integration" | grep -v '^0$' (Wave 3b @integration marker 존재 확인)
    - grep -c "joint_seq_60f" backend/tests/phase04/test_evaluate_4way.py | grep -v '^0$' (T=60 fixture 사용 확인)
  </acceptance_criteria>

  <done>test_evaluate_4way.py 신규 작성 (04-03 소유) + Wave 3a smoke gate GREEN + Wave 3b @integration skeleton 박제 + T=60 fixture 사용 + 기존 phase04 suite 회귀 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RTMW joints → mesh → render | joint 좌표가 RTMW 원본에서 오며 외부 파일 입력 없음 (mesh 는 코드 생성) |
| RunPod GPU Pod → render output | EGL headless render 가 GPU Pod 환경에서만 실행됨. Lambda CPU 경로 없음 |
| synthesis output → aiSynthesisMeta | 합성 결과가 KeypointReport/aiSynthesisMeta 에만 저장되고 coco_array (DTW/kismam 채점) 에 주입되지 않음 |
| Wave 3a boosted conf → scoring path | +0.15 boosted confidence 는 pipeline 연결 검증용 임시값이며 scoring promote 경로 없음 (R1 non-scoring 하드월) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-W3-01 | Tampering | cylindrical_mesh.py — coco_array mutate 위험 | mitigate | synthesize_occluded_joints 반환값은 KeypointReport + aiSynthesisMeta 경로만. DTW/kismam 입력인 coco_array 에 합성 좌표 주입 금지 주석 + Pitfall 7 참조 강제 (RESEARCH.md Pitfall 7). |
| T-04-W3-02 | Denial of Service | pyrender EGL 초기화 실패 (OSError) | mitigate | PYRENDER_AVAILABLE=False graceful fallback. 분석 흐름 차단 0. ai_synthesis_failed warning 주입 (D-07 계약). |
| T-04-W3-03 | Information Disclosure | RunPod GPU 영상 데이터 render 처리 | accept | 영상 데이터는 이미 S3/RunPod 내부에서 처리 중. mesh render 는 joint 좌표 기반 (픽셀 영상 미사용) — 추가 데이터 노출 없음. |
| T-04-W3-04 | Tampering | trimesh OBJ 외부 파일 입력 위험 | mitigate | mesh 는 항상 RTMW joint sequence 에서 코드로 생성 (build_humanoid_mesh). 외부 .obj 파일 입력 경로 0. humanoid.obj (spike 산출물) 는 production 코드에서 참조 금지 주석 박제. |
| T-04-W3-05 | Elevation of Privilege | +0.15 synthetic boost → scoring promote 경로 | mitigate | Wave 3a acceptance 에 boosted confidence 단독 scoring 통과 항목 없음. R1 non-scoring 하드월 주석 코드/테스트 양측 박제. calibration-source-hard-gate: 실 RunPod 재추론(Wave 3b @integration)만 scoring gate 진입 허용. |
| T-04-W3-SC | Tampering | pip install trimesh + pyrender + pyopengl | mitigate | Package Legitimacy Audit (RESEARCH.md §Package Legitimacy Audit): trimesh PyPI 2012+, pyrender PyPI 2019+, 모두 [ASSUMED-OK]. RunPod 설치 시 pip install --no-deps 금지 (정상 설치). |
</threat_model>

<verification>
[Wave 3a smoke — RunPod 없이 실행 가능]
python3 verify 스크립트: 12 views OK + SMPL-X gate PASS + G4 guard OK
pytest backend/tests/phase04/test_evaluate_4way.py -x -q -m "not integration" → Wave 3a GREEN
pytest backend/tests/phase04/ -x -q → 기존 POSE-03-a~f suite 회귀 0
python3 -c "from sunity_shared.analysis.synthesis.cylindrical_mesh import CylindricalMeshAdapter" → import 성공
grep "trimesh" backend/runpod_inference/requirements.txt && grep "pyrender" backend/runpod_inference/requirements.txt → 양쪽 발견

[Wave 3b @integration — RunPod 필요, phase blocker 아님]
pytest backend/tests/phase04/test_evaluate_4way.py -m "integration" → SKIP (skeleton) 또는 Wave 3b 구현 후 GREEN
Wave 3b skip 상태에서도 Wave 2(UI) + Wave 5(reference) 진행 가능 확인
</verification>

<success_criteria>
Wave 3 완료 기준:

Wave 3a smoke (RunPod 없이 완료 가능):
1. CylindricalMeshAdapter 신설 + SynthesisAdapter Protocol (04-01) 구현 완료
2. virtual_renderer.py EGL headless 설정 + Mac dummy fallback 동작
3. G4 is_reference=True 가드 → SynthesisResult(status="skipped") 반환 (G4 occlusion FP 가드 — 04-CONTEXT code_context)
4. SMPL-X 의존 0 — cylindrical_mesh.py 소스 "smplx" 미포함 확인 (D-20)
5. RunPod requirements.txt trimesh + pyrender + pyopengl 추가
6. test_evaluate_4way.py 신규 작성 (04-03 소유) + Wave 3a smoke gate GREEN (synthetic fixture, RunPod 없이)
7. Wave 3b @integration skeleton 박제 완료 (skip 상태 — 04-05 append 예정)
8. 기존 phase04 suite (POSE-03-a~f) 회귀 0
9. +0.15 boosted confidence 단독 scoring acceptance 항목 없음 (R1 non-scoring 하드월, calibration-source-hard-gate)

Wave 3b accuracy gate (RunPod 필요, 별도 실행 — phase blocker 아님):
10. test_axis_b_real_rtmw_integration: skip 제거 + 실 RTMW 재추론 + evaluate_4way rate_reduction_pct > 0 확인
11. Wave 2(UI) + Wave 5(reference) 는 Wave 3b 완료 전에도 Wave 1 Gemini path 기준으로 진행 완료 가능 (반박#2 정합)
</success_criteria>

<output>
Create `.planning/phases/04-ux-occlusion-confidence/04-03-SUMMARY.md` when done
</output>
