---
phase: "04"
plan: "03"
type: execute
wave: 3
depends_on:
  - 04-01
files_modified:
  - backend/shared/python/sunity_shared/analysis/synthesis/cylindrical_mesh.py
  - backend/shared/python/sunity_shared/analysis/synthesis/virtual_renderer.py
  - backend/runpod_inference/requirements.txt
  - backend/tests/phase04/test_evaluate_4way.py
autonomous: true
requirements:
  - POSE-03

must_haves:
  truths:
    - "confidence 미달 phase 에서 CylindricalMeshAdapter 가 조건부 트리거되고 12 view render → RTMW 재추론 → 병합 경로가 실행된다 (D-18 SECONDARY, D-03, D-30 Stage 3')"
    - "pyrender EGL headless 가 PYOPENGL_PLATFORM=egl 를 import 전 설정해 RunPod GPU 환경에서 OSError 없이 초기화된다 (RESEARCH Pitfall 3)"
    - "Mac local 개발 환경에서는 PYRENDER_AVAILABLE=False dummy fallback 이 동작하고 분석이 중단되지 않는다 (D-07 graceful degrade)"
    - "evaluate_4way 통합 게이트가 cylindrical mesh path 의 IPSF occlusion 감점이 baseline RTMW-mirror 대비 감소함을 측정한다 (POSE-03-g)"
    - "trimesh MIT + pyrender MIT + numpy BSD 만 사용하고 SMPL-X 의존이 없다 (D-20 license gate)"
  artifacts:
    - path: backend/shared/python/sunity_shared/analysis/synthesis/cylindrical_mesh.py
      provides: "CylindricalMeshAdapter (SynthesisAdapter Protocol 구현체)"
      contains: "class CylindricalMeshAdapter"
    - path: backend/shared/python/sunity_shared/analysis/synthesis/virtual_renderer.py
      provides: "render_12_views_safe (EGL headless + Mac dummy fallback)"
      contains: "PYOPENGL_PLATFORM"
    - path: backend/runpod_inference/requirements.txt
      provides: "trimesh + pyrender + pyopengl RunPod 의존성"
      contains: "trimesh"
    - path: backend/tests/phase04/test_evaluate_4way.py
      provides: "POSE-03-g 통합 게이트"
      contains: "evaluate_4way"
  key_links:
    - from: backend/shared/python/sunity_shared/analysis/synthesis/cylindrical_mesh.py
      to: backend/shared/python/sunity_shared/analysis/synthesis/interfaces.py
      via: "SynthesisAdapter Protocol 구현 (04-01 신설)"
      pattern: "class CylindricalMeshAdapter"
    - from: backend/shared/python/sunity_shared/analysis/synthesis/cylindrical_mesh.py
      to: backend/shared/python/sunity_shared/analysis/synthesis/virtual_renderer.py
      via: "render_12_views_safe 호출"
      pattern: "render_12_views_safe"
    - from: backend/tests/phase04/test_evaluate_4way.py
      to: ".planning/spikes/001-dataset-eval-harness/metrics.py"
      via: "evaluate_4way 함수 재사용 (PathOutput 형식 정합)"
      pattern: "evaluate_4way"

user_setup: []
---

<objective>
Phase 4 Wave 3 — Stage 3' backend mesh render. Cylindrical humanoid mesh + 12 virtual camera pyrender headless render (Spike 002b VALIDATED-SKELETON) 를 SynthesisAdapter Protocol (04-01 신설) 의 2번째 구현체인 CylindricalMeshAdapter 로 이관. IPSF occlusion 감점 path 에서 조건부 트리거.

Purpose: D-18 SECONDARY path 실구현. Gemini Vision (PRIMARY) 이 indeterminate 반환하거나 비용 손절 발동 시 mesh render fallback 으로 occlusion 보완 정확도를 추가로 확보한다. Spike 001 evaluate_4way 를 POSE-03-g 통합 게이트로 재사용해 cylindrical mesh path 가 baseline RTMW-mirror 대비 IPSF occlusion 감점 감소 여부를 정량 측정한다.

Output: cylindrical_mesh.py (CylindricalMeshAdapter) + virtual_renderer.py (EGL headless + Mac dummy fallback) + RunPod requirements.txt 업데이트 + test_evaluate_4way.py 통합 게이트
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-ux-occlusion-confidence/04-CONTEXT.md
@.planning/phases/04-ux-occlusion-confidence/04-RESEARCH.md
@.planning/phases/04-ux-occlusion-confidence/04-PATTERNS.md
@.planning/phases/04-ux-occlusion-confidence/04-VALIDATION.md
@.planning/phases/04-ux-occlusion-confidence/04-01-SUMMARY.md

@backend/shared/python/sunity_shared/analysis/synthesis/interfaces.py
@backend/runpod_inference/requirements.txt
@.planning/spikes/002b-cylindrical-mesh-virtual-render/mesh_builder.py
@.planning/spikes/002b-cylindrical-mesh-virtual-render/render.py
@.planning/spikes/001-dataset-eval-harness/metrics.py
@.planning/spikes/001-dataset-eval-harness/harness.py
@backend/tests/phase04/conftest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CylindricalMeshAdapter + virtual_renderer.py (EGL headless + Mac dummy)</name>
  <files>
    backend/shared/python/sunity_shared/analysis/synthesis/cylindrical_mesh.py,
    backend/shared/python/sunity_shared/analysis/synthesis/virtual_renderer.py,
    backend/runpod_inference/requirements.txt
  </files>

  <read_first>
    필수:
    1. .planning/spikes/002b-cylindrical-mesh-virtual-render/mesh_builder.py (전체) — build_humanoid_mesh, SEGMENTS, _cylinder_between 이관 기준 원본
    2. .planning/spikes/002b-cylindrical-mesh-virtual-render/render.py (전체) — render_12_views, PYOPENGL_PLATFORM=egl 패턴, make_12_camera_set, _camera_pose, PYRENDER_AVAILABLE fallback 이관 기준 원본
    3. backend/shared/python/sunity_shared/analysis/synthesis/interfaces.py (전체) — SynthesisAdapter Protocol 시그니처, identify_synthesis_targets, merge_with_temporal (04-01 신설)
    4. 04-PATTERNS.md §cylindrical_mesh.py — 파일 헤더 패턴 + PYOPENGL_PLATFORM 선언 위치
    5. 04-RESEARCH.md §Pattern 5: pyrender EGL Headless — PYOPENGL_PLATFORM=egl os.environ.setdefault 위치 + renderer.delete() 필수 박제
    6. 04-RESEARCH.md §Pitfall 3 — EGL 초기화 순서 (import pyrender 보다 먼저)
    7. 04-RESEARCH.md §Pitfall 4 — Firestore nested-array (합성 메타데이터 flat)
    8. 04-RESEARCH.md §Pitfall 7 — RTMW 3D scoring 행렬에 합성 좌표 주입 금지
    9. 04-CONTEXT.md D-18, D-20, D-29~D-30 — SECONDARY path 위치 + license gate + Stage 3' 아키텍처
    10. backend/runpod_inference/requirements.txt (전체) — 현재 의존성 목록 확인 후 trimesh/pyrender/pyopengl 추가
  </read_first>

  <behavior>
    virtual_renderer.py:
    - PYOPENGL_PLATFORM=egl 설정은 모듈 최상단 (import 문 이전) os.environ.setdefault("PYOPENGL_PLATFORM", "egl") 로 위치 — import pyrender 보다 반드시 먼저 (RESEARCH Pitfall 3).
    - try/except (ImportError, OSError) 로 pyrender import 시도 → PYRENDER_AVAILABLE bool 박제 (Spike 002b render.py 패턴 그대로).
    - render_12_views_safe(mesh, width=256, height=256) → list[np.ndarray]: PYRENDER_AVAILABLE=False 시 np.full dummy 12장 반환 (Mac local 개발 graceful).
    - PYRENDER_AVAILABLE=True 시: pyrender.OffscreenRenderer → 12 카메라 루프 → renderer.delete() 명시적 해제 (GPU 메모리 관리).
    - make_12_camera_set(distance=3.0, pitch_deg=0.0) → list[VirtualCamera] (30° 간격 12 yaw).
    - _camera_pose(camera, target) → np.ndarray 4x4 (pyrender camera-to-world).

    cylindrical_mesh.py:
    - 모듈 헤더 docstring: "Stage 3' CylindricalMeshAdapter — Spike 002b VALIDATED-SKELETON 이관. SynthesisAdapter Protocol (D-18 SECONDARY). license: trimesh MIT + pyrender MIT + numpy BSD. SMPL-X 의존 0 (D-20 박제)."
    - from __future__ import annotations + os.environ.setdefault("PYOPENGL_PLATFORM", "egl") 상단 선언 (virtual_renderer 임포트 전).
    - build_humanoid_mesh(joints: np.ndarray) → trimesh.Trimesh: Spike 002b mesh_builder.py 코드 그대로 이관. COCO-17 joint index 상수, SEGMENTS dataclass, _cylinder_between 헬퍼 포함.
    - CylindricalMeshAdapter 클래스: SynthesisAdapter Protocol 구현 (04-01 신설 인터페이스).
      - synthesize_occluded_joints(joint_sequence, confidence_sequence, occluded_mask, scene_findings) → tuple[ndarray, ndarray]:
        - is_reference guard: scene_findings.get("is_reference", False) → (zeros, zeros) 반환 + log.info (G4 가드 per D-10).
        - occluded_mask.any() = False → (zeros_like, zeros_like) 조기 반환 (비용 0).
        - occluded frame 별 build_humanoid_mesh → render_12_views_safe → 12 view 이미지 → RTMW 재추론 placeholder (RunPod 환경 전용 — RTMW 재추론 로직은 _rerun_rtmw_on_views 헬퍼로 분리, 현재는 NotImplementedError 대신 기본 joints 그대로 반환 + log.warning "RTMW rerun not wired — cylindrical synthesis returning primary joints with boosted conf").
        - RTMW 재추론 boosted confidence: 합성 대상 frame 에서 occluded_mask=True 관절에 conf = min(primary_conf + 0.15, 1.0) 적용 (D-04.a 임계값 0.3 기준, RESEARCH Assumptions A1 정합 — 실 RunPod 검증 전 보수적 +0.15 boost).
        - 모든 예외 except Exception: log.exception + (zeros_like, zeros_like) 반환 (D-07 graceful degrade 계약).
    - SMPL-X import 금지 주석: "# SMPL-X 절대 금지 — D-20 박제 (belle 명시 완전 최후의 보류, $7,300/yr)"
    - coco_array (DTW/kismam 채점 입력) mutate 금지 주석: "# RTMW 3D scoring matrix mutate 0 — 합성 결과는 KeypointReport + aiSynthesisMeta 만 영향 (RESEARCH Pitfall 7)"

    requirements.txt (runpod_inference):
    - trimesh>=4.12.2 추가
    - pyrender>=0.1.45 추가
    - pyopengl 추가 (EGL 런타임; NVIDIA 드라이버 포함이나 pip 명시 박제)
    - 기존 항목 변경 금지.
  </behavior>

  <action>
    1. virtual_renderer.py 신설: Spike 002b render.py 를 그대로 이관. 파일명 render.py → virtual_renderer.py. export 함수명 render_12_views → render_12_views_safe (safe suffix: Mac dummy fallback 포함 명시). 모듈 docstring: "Stage 3' 12 virtual camera pyrender EGL headless render. PYOPENGL_PLATFORM=egl 는 이 모듈 import 즉시 os.environ.setdefault 로 설정됨 — 반드시 import pyrender 보다 먼저 실행되어야 한다 (RESEARCH Pitfall 3). Mac local 개발 환경(PYRENDER_AVAILABLE=False)에서는 dummy 128-gray 12장 반환."

    2. cylindrical_mesh.py 신설: Spike 002b mesh_builder.py (build_humanoid_mesh + 보조 함수 전체) + CylindricalMeshAdapter 클래스. 파일 헤더 주석에 Spike 002b VALIDATED-SKELETON 출처 + D-18 SECONDARY + D-20 license gate 명시. CylindricalMeshAdapter.synthesize_occluded_joints 는 3단계 처리: (a) G4 가드 → (b) 합성 필요 frame 루프 → build_humanoid_mesh + render_12_views_safe 호출 → (c) _rerun_rtmw_on_views placeholder (boosted conf 반환). RTMW 재추론 실구현 주석: "TODO(Wave 3 RunPod 작업): _rerun_rtmw_on_views 에 실제 RTMW 추론 파이프라인 연결. 현재는 boosted confidence 반환으로 통합 경로 검증."

    3. backend/runpod_inference/requirements.txt 수정: 기존 파일 읽기 후 trimesh>=4.12.2, pyrender>=0.1.45, pyopengl 세 줄 추가 (기존 항목 순서 유지, 가장 뒤에 추가).

    Commit: 단일 atomic commit "feat(04-03): CylindricalMeshAdapter Stage 3' backend mesh render (POSE-03 D-18 SECONDARY)"
  </action>

  <verify>
    <automated>
      cd /Users/kimtaesung/Dev/SunityMotion &&
      python -c "
import sys; sys.path.insert(0, 'backend/shared/python')
from sunity_shared.analysis.synthesis.cylindrical_mesh import CylindricalMeshAdapter, build_humanoid_mesh
from sunity_shared.analysis.synthesis.virtual_renderer import render_12_views_safe, PYRENDER_AVAILABLE
import numpy as np
joints = np.zeros((17, 3), dtype=float)
joints[5] = [-0.2, 1.5, 0]; joints[6] = [0.2, 1.5, 0]
joints[11] = [-0.15, 0.9, 0]; joints[12] = [0.15, 0.9, 0]
mesh = build_humanoid_mesh(joints)
assert mesh.vertices.shape[1] == 3
views = render_12_views_safe(mesh)
assert len(views) == 12
print('PYRENDER_AVAILABLE:', PYRENDER_AVAILABLE)
print('mesh vertices:', mesh.vertices.shape)
print('12 views: OK')
adapter = CylindricalMeshAdapter()
seq = np.tile(joints, (30, 1, 1))
conf = np.full((30, 17), 0.25)
mask = conf < 0.3
synth_j, synth_c = adapter.synthesize_occluded_joints(seq, conf, mask, {})
assert synth_j.shape == (30, 17, 3), f'shape mismatch: {synth_j.shape}'
print('CylindricalMeshAdapter.synthesize_occluded_joints: OK')
print('SMPL-X check:', 'smplx' not in open('backend/shared/python/sunity_shared/analysis/synthesis/cylindrical_mesh.py').read().lower())
" 2>&1
    </automated>
  </verify>

  <acceptance_criteria>
    - build_humanoid_mesh(zeros_joints) 실행 → trimesh.Trimesh vertices.shape[1]==3 반환 (crash 없음)
    - render_12_views_safe(mesh) → list 길이 12 (PYRENDER_AVAILABLE=False 시 dummy 회색 이미지)
    - CylindricalMeshAdapter().synthesize_occluded_joints(seq, conf, mask, {}) → (ndarray, ndarray) shape (T, 17, 3) / (T, 17)
    - G4 가드: synthesize_occluded_joints(seq, conf, mask, {"is_reference": True}) → (zeros, zeros) 반환 + 예외 없음
    - python -c "import sunity_shared.analysis.synthesis.cylindrical_mesh as m; print('smplx' not in open(m.__file__).read().lower())" → True (SMPL-X 의존 0)
    - grep "trimesh" backend/runpod_inference/requirements.txt — 발견
    - grep "pyrender" backend/runpod_inference/requirements.txt — 발견
  </acceptance_criteria>

  <done>CylindricalMeshAdapter import 성공 + synthesize_occluded_joints shape contract 통과 + G4 가드 동작 + SMPL-X 의존 0 확인 + RunPod requirements.txt 업데이트 완료.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: test_evaluate_4way.py — POSE-03-g 통합 게이트 (Spike 001 harness 재사용)</name>
  <files>
    backend/tests/phase04/test_evaluate_4way.py
  </files>

  <read_first>
    필수:
    1. .planning/spikes/001-dataset-eval-harness/metrics.py (전체) — evaluate_4way 함수 + PathOutput dataclass + axis_a/b/c 계산 로직
    2. .planning/spikes/001-dataset-eval-harness/harness.py (전체) — make_synthetic_path fixture 패턴 + smoke_test evaluate_4way 호출 패턴 (테스트 구조 기준)
    3. .planning/spikes/001-dataset-eval-harness/ipsf_criteria.py — FULLY_EXTENDED.tolerance ±20°, PHASE4_EVAL_AXIS_MAPPING (axis_b = occlusion 감점 감소 게이트)
    4. backend/tests/phase04/conftest.py — 기존 synthetic joint sequence fixtures (Wave 0 신설) + pytest 구조
    5. 04-RESEARCH.md §Spike 001 evaluate_4way 를 Verification Gate 로 활용 — 통과 기준 박제
    6. 04-RESEARCH.md §Validation Architecture Test Framework — pytest 실행 명령
    7. 04-VALIDATION.md §POSE-03-g — "cylindrical mesh path vs baseline RTMW-mirror IPSF 감점 비교" 게이트
    8. 04-CONTEXT.md D-22 — IPSF occlusion 감점 -0.5pt/회, baseline -7.60pts/video (기준선)
    9. 04-CONTEXT.md D-21 — IPSF Page 19 split angle must remain same (axis_a 통과 기준)
  </read_first>

  <behavior>
    test_evaluate_4way.py 구조:
    - conftest.py 의 synthetic_joint_sequence fixture 재사용 (T=60, J=17, 3).
    - 3-way paths 박제 (D-19 정합): "rtmw_mirror" (baseline), "gemini_view" (PRIMARY), "cylindrical_mesh" (SECONDARY per D-18).
    - Higgsfield/MagicMan path 는 테스트에서 제외 (D-19 INVALIDATED).

    test_axis_b_cylindrical_mesh_vs_baseline:
    - cylindrical_mesh PathOutput: confidence = clip(rtmw_mirror_conf + 0.15, 0, 1) 합성 시뮬.
    - evaluate_4way(outputs, split_frame_indices, criterion_frames, baseline_path_name="rtmw_mirror") 호출.
    - report.axis_b["cylindrical_mesh"]["rate_reduction_pct"] >= 0.0 → PASS (cylindrical path 가 baseline 대비 occlusion frame rate 동일 이상 감소).
    - 실 RunPod 추론 결과 아닌 synthetic boost 이므로 "smoke gate" 주석 박제 ("실 RunPod 추론 완료 후 threshold 재조정 필요").

    test_axis_a_split_angle_consistency:
    - 3-way path 모두 axis_a pass rate >= 0.0 (smoke test — NaN 없이 계산 완료 확인).
    - split_frame_indices = [0, 5, 10, 15, 20] (하네스 패턴 정합).

    test_cylindrical_mesh_no_smplx_import:
    - cylindrical_mesh.py 소스 파일을 text 로 읽어 "smplx" 문자열 미포함 확인 (license gate, D-20).

    test_cylindrical_mesh_license_stack:
    - trimesh, pyrender, numpy import 가능 확인 (RunPod 환경 전제 — ImportError 시 pytest.skip("trimesh/pyrender 미설치")).
    - 각 모듈 __file__ 경로 print (slopcheck 수동 참조용).

    marker 박제: @pytest.mark.integration (RunPod 필요 테스트 분리)
    - test_axis_b / test_axis_a = unit (synthetic fixture, RunPod 불필요).
    - 실 RunPod 재추론 = integration mark → -m "not integration" 로 CI 제외 가능.
  </behavior>

  <action>
    1. test_evaluate_4way.py 신설: Spike 001 harness.py 의 smoke_test 구조를 pytest 형식으로 변환. make_synthetic_path 헬퍼를 파일 내 로컬 함수로 복사 (harness.py 의존성 제거 — 테스트 자급자족). evaluate_4way 는 .planning/spikes/001-dataset-eval-harness/metrics.py 에서 직접 import (sys.path.insert 또는 conftest.py 에 spike path 박제).

    2. conftest.py 에 spike harness path 박제 필요 여부 확인: 기존 conftest.py 에 sys.path 에 spike 경로 없으면 test_evaluate_4way.py 상단에 직접 선언:
       import sys, pathlib
       sys.path.insert(0, str(pathlib.Path(__file__).parents[3] / ".planning/spikes/001-dataset-eval-harness"))
       이후 from metrics import PathOutput, evaluate_4way import.

    3. 테스트 함수 3개: test_axis_b_cylindrical_mesh_vs_baseline / test_axis_a_split_angle_consistency / test_cylindrical_mesh_no_smplx_import. 각 함수 docstring에 POSE-03-g 레퍼런스 + 평가 기준 박제.

    4. test_cylindrical_mesh_license_stack: pytest.importorskip("trimesh") + pytest.importorskip("pyrender") 패턴 사용 (설치 안 된 환경에서 SKIP 처리).

    Commit: "test(04-03): POSE-03-g evaluate_4way integration gate (Spike 001 harness 재사용)"
  </action>

  <verify>
    <automated>
      cd /Users/kimtaesung/Dev/SunityMotion &&
      python -m pytest backend/tests/phase04/test_evaluate_4way.py -x -q -m "not integration" 2>&1 | tail -15
    </automated>
  </verify>

  <acceptance_criteria>
    - pytest backend/tests/phase04/test_evaluate_4way.py -x -q -m "not integration" → 모든 non-integration 테스트 GREEN (또는 trimesh 미설치 시 SKIP — PASS 처리)
    - test_cylindrical_mesh_no_smplx_import → PASS (소스에 "smplx" 미포함)
    - test_axis_b_cylindrical_mesh_vs_baseline → PASS (synthetic boost path 가 rate_reduction_pct >= 0.0)
    - test_axis_a_split_angle_consistency → PASS (NaN 없이 3-way 계산 완료)
    - pytest backend/tests/phase04/ -x -q → 기존 POSE-03-a~f suite 회귀 0 (phase04 전체 재실행 후 기존 GREEN 유지)
    - grep -c "smplx" backend/tests/phase04/test_evaluate_4way.py → 0 또는 "smplx" 문자열이 assert not in 형태로만 존재
  </acceptance_criteria>

  <done>test_evaluate_4way.py 신설 + POSE-03-g smoke gate GREEN + 기존 phase04 suite 회귀 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RTMW joints → mesh → render | joint 좌표가 RTMW 원본에서 오며 외부 파일 입력 없음 (mesh 는 코드 생성) |
| RunPod GPU Pod → render output | EGL headless render 가 GPU Pod 환경에서만 실행됨. Lambda CPU 경로 없음 |
| synthesis output → aiSynthesisMeta | 합성 결과가 KeypointReport/aiSynthesisMeta 에만 저장되고 coco_array (DTW/kismam 채점) 에 주입되지 않음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-W3-01 | Tampering | cylindrical_mesh.py — coco_array mutate 위험 | mitigate | synthesize_occluded_joints 반환값은 KeypointReport + aiSynthesisMeta 경로만. DTW/kismam 입력인 coco_array 에 합성 좌표 주입 금지 주석 + Pitfall 7 참조 강제 (RESEARCH.md Pitfall 7). |
| T-04-W3-02 | Denial of Service | pyrender EGL 초기화 실패 (OSError) | mitigate | PYRENDER_AVAILABLE=False graceful fallback. 분석 흐름 차단 0. ai_synthesis_failed warning 주입 (D-07 계약). |
| T-04-W3-03 | Information Disclosure | RunPod GPU 영상 데이터 render 처리 | accept | 영상 데이터는 이미 S3/RunPod 내부에서 처리 중. mesh render 는 joint 좌표 기반 (픽셀 영상 미사용) — 추가 데이터 노출 없음. |
| T-04-W3-04 | Tampering | trimesh OBJ 외부 파일 입력 위험 | mitigate | mesh 는 항상 RTMW joint sequence 에서 코드로 생성 (build_humanoid_mesh). 외부 .obj 파일 입력 경로 0. humanoid.obj (spike 산출물) 는 production 코드에서 참조 금지 주석 박제. |
| T-04-W3-SC | Tampering | pip install trimesh + pyrender + pyopengl | mitigate | Package Legitimacy Audit (RESEARCH.md §Package Legitimacy Audit): trimesh PyPI 2012+, pyrender PyPI 2019+, 모두 [ASSUMED-OK]. RunPod 설치 시 pip install --no-deps 금지 (정상 설치). |
</threat_model>

<verification>
pytest backend/tests/phase04/test_evaluate_4way.py -x -q -m "not integration" → non-integration 테스트 GREEN
pytest backend/tests/phase04/ -x -q → 기존 POSE-03-a~f suite 회귀 0
python -c "from sunity_shared.analysis.synthesis.cylindrical_mesh import CylindricalMeshAdapter" → import 성공
python -c "import sunity_shared.analysis.synthesis.cylindrical_mesh as m; assert 'smplx' not in open(m.__file__).read().lower()" → license gate PASS
grep "trimesh" backend/runpod_inference/requirements.txt && grep "pyrender" backend/runpod_inference/requirements.txt → 양쪽 발견
</verification>

<success_criteria>
Wave 3 완료 기준 (ROADMAP SC #2 조건부 트리거 + SC #5 graceful degrade + SC #7 정확도 향상 관련):
1. CylindricalMeshAdapter 신설 + SynthesisAdapter Protocol (04-01) 구현 완료
2. virtual_renderer.py EGL headless 설정 + Mac dummy fallback 동작
3. G4 is_reference=True 가드 → (zeros, zeros) 반환 (합성 차단)
4. SMPL-X 의존 0 — cylindrical_mesh.py 소스 "smplx" 미포함 확인
5. RunPod requirements.txt trimesh + pyrender + pyopengl 추가
6. test_evaluate_4way.py POSE-03-g smoke gate GREEN (synthetic fixture, RunPod 없이)
7. 기존 phase04 suite (POSE-03-a~f) 회귀 0
</success_criteria>

<output>
Create `.planning/phases/04-ux-occlusion-confidence/04-03-SUMMARY.md` when done
</output>
