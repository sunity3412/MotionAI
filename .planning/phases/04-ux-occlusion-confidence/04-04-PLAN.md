---
phase: "04"
plan: "04"
type: execute
wave: 4
depends_on:
  - 04-01
files_modified:
  - backend/shared/python/sunity_shared/analysis/synthesis/interfaces.py
  - backend/shared/python/sunity_shared/analysis/synthesis/video_gen_adapter.py
  - backend/tests/phase04/test_video_gen_adapter.py
autonomous: true
requirements:
  - POSE-03

must_haves:
  truths:
    - "VideoGenerationAdapter Protocol 이 정의되어 있고 import 가 성공한다 (D-30, D-31, per D-30)"
    - "OmniVertexAdapter stub 이 존재하고 호출 시 NotImplementedError 를 발생시킨다 (D-30, per D-30)"
    - "SYNTHESIS_VIDEO_GEN_ENABLED env flag 가 기본 False 이며 비활성 시 어댑터를 호출하지 않는다 (D-31, per D-31)"
    - "단위 테스트가 비활성 flag 시 어댑터 미호출을 검증한다"
  artifacts:
    - path: backend/shared/python/sunity_shared/analysis/synthesis/interfaces.py
      provides: "VideoGenerationAdapter Protocol (기존 SynthesisAdapter 옆에 추가)"
      contains: "class VideoGenerationAdapter(Protocol)"
    - path: backend/shared/python/sunity_shared/analysis/synthesis/video_gen_adapter.py
      provides: "OmniVertexAdapter stub + VeoAdapter stub + get_video_gen_adapter 팩토리"
      contains: "class OmniVertexAdapter"
    - path: backend/tests/phase04/test_video_gen_adapter.py
      provides: "단위 테스트 — stub 비활성 + NotImplementedError 검증"
      contains: "test_video_gen_disabled_by_default"
  key_links:
    - from: backend/shared/python/sunity_shared/analysis/synthesis/video_gen_adapter.py
      to: backend/shared/python/sunity_shared/analysis/synthesis/interfaces.py
      via: "VideoGenerationAdapter Protocol 구현 (structural subtyping)"
      pattern: "VideoGenerationAdapter"
    - from: backend/tests/phase04/test_video_gen_adapter.py
      to: backend/shared/python/sunity_shared/analysis/synthesis/video_gen_adapter.py
      via: "SYNTHESIS_VIDEO_GEN_ENABLED=0 조건 pytest"
      pattern: "test_video_gen_disabled_by_default"
---

<objective>
Phase 4 Wave 4 — Stage 4 영상 생성 plug-in 인터페이스 stub 박제 (활성화 X).

D-30/D-31 정합: Gemini Omni (Vertex GA 후, mid-late June 2026) / Veo 3.1 을 끼울 VideoGenerationAdapter Protocol 만 정의. 실제 API 호출은 없고, OmniVertexAdapter / VeoAdapter 는 NotImplementedError stub 이다. SYNTHESIS_VIDEO_GEN_ENABLED env flag 가 기본 False 이므로 파이프라인에 어떤 영향도 없다.

비용 주석 박제 (D-28): Omni $2-6/10초 영상 (30~60초 = $6-36/영상) vs Gemini Vision reasoning $0.12/video. Omni 는 occluded frame 조건부 트리거 전용으로 설계됨.

Purpose: 모델 교체 시 구현체 1개 + config flag 만으로 활성화 가능한 어댑터 경계 박제. Vertex GA 후 OmniVertexAdapter 에 실 구현 채워 넣으면 끝.
Output: VideoGenerationAdapter Protocol + OmniVertexAdapter/VeoAdapter stub + 비활성 검증 테스트
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-ux-occlusion-confidence/04-CONTEXT.md
@.planning/phases/04-ux-occlusion-confidence/04-RESEARCH.md
@.planning/phases/04-ux-occlusion-confidence/04-PATTERNS.md
@.planning/ROADMAP.md

@backend/shared/python/sunity_shared/analysis/synthesis/interfaces.py
@backend/shared/python/sunity_shared/analysis/interfaces.py
@backend/functions/pipeline/app.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: VideoGenerationAdapter Protocol + OmniVertexAdapter / VeoAdapter stub</name>
  <files>
    backend/shared/python/sunity_shared/analysis/synthesis/interfaces.py,
    backend/shared/python/sunity_shared/analysis/synthesis/video_gen_adapter.py
  </files>

  <read_first>
    필수:
    1. backend/shared/python/sunity_shared/analysis/synthesis/interfaces.py (전체) — Wave 1 이 생성한 SynthesisAdapter Protocol 패턴 복사 기준. VideoGenerationAdapter 를 같은 파일에 추가.
    2. backend/shared/python/sunity_shared/analysis/interfaces.py (lines 42-85) — PoseEngine Protocol 원본 패턴 (D-24 "어떤 백본 구현체도 직접 import 하지 않는다" 원칙).
    3. 04-CONTEXT.md D-24, D-25, D-27, D-28, D-30, D-31 — Omni 미등록 사유 + HYBRID PRIMARY 시간축 + 비용 주석 + plug-in 슬롯 정책 + Wave 4 = 인터페이스만 박제.
    4. 04-RESEARCH.md §Pattern 1: Synthesis Adapter Protocol — Protocol structural subtyping 패턴.
    5. 04-RESEARCH.md §Pitfall 6: Gemini Omni 조기 도입 — Vertex endpoint 미등록 배경 (Wave 4 stub 필요 근거).
    6. 04-RESEARCH.md §Security Domain §Known Threat Patterns — Gemini API key Information Disclosure 패턴 (stub 단계 위협 표면 명시 기준).
  </read_first>

  <behavior>
    test_video_gen_adapter.py:
    - test_video_gen_disabled_by_default: SYNTHESIS_VIDEO_GEN_ENABLED env 없거나 "0" 일 때 get_video_gen_adapter() 가 None 을 반환한다.
    - test_omni_adapter_raises_not_implemented: OmniVertexAdapter().generate_virtual_view(...) 가 NotImplementedError 를 발생시킨다.
    - test_veo_adapter_raises_not_implemented: VeoAdapter().generate_virtual_view(...) 가 NotImplementedError 를 발생시킨다.
    - test_protocol_structural_subtyping: OmniVertexAdapter / VeoAdapter 가 VideoGenerationAdapter Protocol 을 구조적 서브타입으로 만족한다 (isinstance check 는 Protocol @runtime_checkable 로 검증).
  </behavior>

  <action>
    1. interfaces.py 수정 (SynthesisAdapter 아래에 추가, 기존 코드 변경 금지):
       VideoGenerationAdapter Protocol 선언. 메서드: generate_virtual_view(frames: np.ndarray, target_camera_delta: tuple[float, float, float]) → np.ndarray. 시그니처: frames = (T, H, W, 3) RGB uint8 입력 프레임, target_camera_delta = (pan_deg, tilt_deg, roll_deg) 가상 카메라 이동량 (픽셀 합성 X — 향후 Omni 의 conversational edit prompt 파라미터로 변환될 자리). 반환 = (T', H, W, 3) 합성 프레임 or NotImplementedError.
       모듈 docstring 갱신: "Stage 4 VideoGenerationAdapter 추가 — Omni Vertex GA 후 활성화 예정 (D-30). 현재 stub 만 존재."
       비용 주석 (D-28 정합): "# Stage 4 활성화 시 예상 비용: Omni $2-6/10초 영상, Veo 3.1 Vertex Public Preview (가격 변동 가능). 조건부 트리거 (occluded frame 만) 전용."
       @runtime_checkable 데코레이터를 VideoGenerationAdapter 에 추가 (isinstance stub 테스트 지원).

    2. video_gen_adapter.py 신설:
       모듈 docstring: Phase 4 Wave 4 Stage 4 plug-in 슬롯 박제. D-25 (Vertex endpoint 미등록 — mid-to-late June 2026 윈도우) 정합. 활성화 조건 (R9 — env flag 단독 불충분) = SYNTHESIS_VIDEO_GEN_ENABLED="1" env + Omni Vertex endpoint 공식 등록(model id/terms/region/IO shape) 확인 + 10-video pose consistency gate 통과 + belle 승인. 4개 모두 충족 전 활성화 금지.
       모듈 docstring 추가 (W5 — R1 하드월 미래 우회 방지): "실 구현 시 generate_virtual_view 의 raw ndarray 반환을 pipeline 에 직결하지 말 것 — SynthesisResult 로 래핑하거나 _call_synthesis_adapter 가 SynthesisResult 로 변환해야 non-scoring 채널 분리(R1) 유지."
       OmniVertexAdapter 클래스: VideoGenerationAdapter Protocol 구현. generate_virtual_view: 즉시 raise NotImplementedError("OmniVertexAdapter 미구현 — Vertex GA 후 활성화 (D-25). Vertex endpoint 등록 확인: Vertex AI Model Garden console."). __init__: 로그 한 줄 "OmniVertexAdapter stub — 활성화 전 호출 금지". 라이선스 주석: "Google Gemini API: Apache-2.0 SDK + Google ToS (DPA + zero-data-retention), D-20 상업 OK".
       VeoAdapter 클래스: VideoGenerationAdapter Protocol 구현. generate_virtual_view: 즉시 raise NotImplementedError("VeoAdapter 미구현 — Veo 3.1 Vertex Public Preview PoC 대상 (D-27). 구현 전 spike 선행 필요."). 라이선스 주석: "Veo 3.1: Vertex Public Preview, Phase 17 DPA cover (D-27)".
       get_video_gen_adapter(adapter_type: str = "omni") → VideoGenerationAdapter | None 팩토리 함수: SYNTHESIS_VIDEO_GEN_ENABLED env = "0" (default) 이면 None 반환 + log.debug("video gen adapter disabled"). "1" 일 때 adapter_type == "omni" → OmniVertexAdapter(), "veo" → VeoAdapter(), 그 외 → log.warning + None. 두 adapter 모두 현재 __init__ 에서 NotImplementedError 를 던지지 않고, generate_virtual_view 호출 시에만 NotImplementedError. 따라서 팩토리는 인스턴스 생성까지는 성공하고 실제 API 호출 경로만 차단됨.
       라이선스/비용 주석 (D-28) 파일 헤더에 박제:
         "# 비용 박제 (D-28): Omni $2-6/10초 → 30~60초 영상 = $6-36/영상 (조건부 트리거 필수)"
         "# Veo 3.1: Vertex Public Preview 가격 (가격 변동 확인 필요)"
         "# Gemini Vision reasoning (현 PRIMARY): $0.12/video — 비교 기준선"
       log = logging.getLogger(__name__) 모듈 레벨 선언.
       from __future__ import annotations 최상단 포함.

    Commit: "feat(04-04): Stage 4 VideoGenerationAdapter Protocol stub + flag-gated factory (POSE-03 D-30 D-31)"
  </action>

  <verify>
    <automated>
      cd /Users/kimtaesung/Dev/SunityMotion &&
      python3 -m pytest backend/tests/phase04/test_video_gen_adapter.py -x -q 2>&1 | tail -15
    </automated>
  </verify>

  <acceptance_criteria>
    - test_video_gen_disabled_by_default GREEN (SYNTHESIS_VIDEO_GEN_ENABLED 미설정 → None)
    - test_omni_adapter_raises_not_implemented GREEN
    - test_veo_adapter_raises_not_implemented GREEN
    - test_protocol_structural_subtyping GREEN
    - python3 -c "from sunity_shared.analysis.synthesis.interfaces import VideoGenerationAdapter; print('OK')" 성공
    - python3 -c "from sunity_shared.analysis.synthesis.video_gen_adapter import OmniVertexAdapter, VeoAdapter, get_video_gen_adapter; print('OK')" 성공
    - grep -c "SYNTHESIS_VIDEO_GEN_ENABLED" backend/shared/python/sunity_shared/analysis/synthesis/video_gen_adapter.py | grep -v '^0$'
    - grep -c "ai_synthesis_failed\|VideoGenerationAdapter\|NotImplementedError" backend/shared/python/sunity_shared/analysis/synthesis/video_gen_adapter.py | grep -v '^0$'
  </acceptance_criteria>

  <done>VideoGenerationAdapter Protocol import 성공 + OmniVertexAdapter/VeoAdapter stub NotImplementedError 검증 GREEN + SYNTHESIS_VIDEO_GEN_ENABLED 비활성 → None 반환 확인.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 비활성 단위 테스트 + 기존 phase04 suite 회귀 확인</name>
  <files>
    backend/tests/phase04/test_video_gen_adapter.py
  </files>

  <read_first>
    필수:
    1. backend/tests/phase04/test_warning_lockstep.py (전체) — Wave 0/1 단위 테스트 작성 패턴 (pytest fixture 없이 직접 assert, monkeypatch / os.environ 활용 패턴).
    2. backend/tests/phase04/test_synthesis_g4_guard.py (전체) — 함수 호출 차단 테스트 패턴 (is_reference=True → SynthesisResult(status="skipped") 검증. 구 return None 계약 아님 — MEDIUM-2).
    3. 04-RESEARCH.md §Validation Architecture — POSE-03 테스트 맵 (test_video_gen_adapter.py 위치 확인).
    4. 04-CONTEXT.md D-31 — "인터페이스만 박제" 범위 확인 (활성화 경로 테스트 불필요 — stub 단계).
  </read_first>

  <behavior>
    test_video_gen_adapter.py (4개 테스트):
    - test_video_gen_disabled_by_default: os.environ 에 SYNTHESIS_VIDEO_GEN_ENABLED 없을 때 get_video_gen_adapter() is None.
    - test_video_gen_explicitly_disabled: os.environ["SYNTHESIS_VIDEO_GEN_ENABLED"] = "0" 일 때 get_video_gen_adapter() is None.
    - test_omni_adapter_raises_not_implemented: OmniVertexAdapter().generate_virtual_view(None, (0, 0, 0)) 가 NotImplementedError.
    - test_veo_adapter_raises_not_implemented: VeoAdapter().generate_virtual_view(None, (0, 0, 0)) 가 NotImplementedError.
    - test_protocol_structural_subtyping: isinstance(OmniVertexAdapter(), VideoGenerationAdapter) is True (runtime_checkable Protocol).

    회귀 검증:
    - backend/tests/phase04/ 전체 (Wave 1~4 테스트) — 기존 GREEN 유지.
    - Wave 4 신규 테스트가 Wave 1~3 테스트에 영향 없음.
  </behavior>

  <action>
    1. test_video_gen_adapter.py 작성: pytest import + os.environ monkeypatch 사용 (pytest monkeypatch fixture 또는 unittest.mock.patch.dict — Wave 1 테스트 파일 패턴 정합). 총 5개 테스트 함수. 각 테스트는 독립적 (shared fixture 없이 인라인 셋업). conftest.py 수정 불필요 — 신규 테스트 파일 추가만.

    2. test_video_gen_disabled_by_default: os.environ 에서 SYNTHESIS_VIDEO_GEN_ENABLED 키 제거 (monkeypatch.delenv("SYNTHESIS_VIDEO_GEN_ENABLED", raising=False)) 후 get_video_gen_adapter() is None assert.

    3. test_video_gen_explicitly_disabled: monkeypatch.setenv("SYNTHESIS_VIDEO_GEN_ENABLED", "0") 후 get_video_gen_adapter() is None assert.

    4. test_omni_adapter_raises_not_implemented: import pytest; with pytest.raises(NotImplementedError): OmniVertexAdapter().generate_virtual_view(None, (0.0, 0.0, 0.0)).

    5. test_veo_adapter_raises_not_implemented: with pytest.raises(NotImplementedError): VeoAdapter().generate_virtual_view(None, (0.0, 0.0, 0.0)).

    6. test_protocol_structural_subtyping: from sunity_shared.analysis.synthesis.interfaces import VideoGenerationAdapter; assert isinstance(OmniVertexAdapter(), VideoGenerationAdapter) is True.

    Commit: "test(04-04): video gen adapter 비활성 단위 테스트 5개 (stub gate)"
  </action>

  <verify>
    <automated>
      cd /Users/kimtaesung/Dev/SunityMotion &&
      python3 -m pytest backend/tests/phase04/ -x -q 2>&1 | tail -20 &&
      python3 -m pytest backend/tests/ -q 2>&1 | tail -5
    </automated>
  </verify>

  <acceptance_criteria>
    - test_video_gen_adapter.py 5개 테스트 모두 GREEN
    - backend/tests/phase04/ 전체 GREEN (Wave 1~4 통합)
    - backend/tests/ 전체 suite 회귀 0 (기존 PASS 카운트 불변)
    - SYNTHESIS_VIDEO_GEN_ENABLED 미설정 상태에서 pipeline 이 video gen adapter 를 일절 호출하지 않음 — get_video_gen_adapter() 가 None 이므로 pipeline 에서 분기 불필요 (Wave 4 는 pipeline/app.py 수정 없음)
  </acceptance_criteria>

  <done>test_video_gen_adapter.py 5개 GREEN + phase04 suite 전체 GREEN + 기존 backend suite 회귀 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SYNTHESIS_VIDEO_GEN_ENABLED env → get_video_gen_adapter | env flag 조작으로 stub 이 호출될 수 있는 경계 (현재 stub 이므로 NotImplementedError 로 차단됨) |
| 미래 Omni Vertex endpoint → Lambda/RunPod | Vertex GA 후 OmniVertexAdapter 구현 시 Gemini API key + 영상 데이터 경계 발생 (stub 단계에서는 표면 없음) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-W4-01 | Tampering | SYNTHESIS_VIDEO_GEN_ENABLED env flag | mitigate | flag 기본값 "0" (미설정 = 비활성). get_video_gen_adapter() 에서 truthy check — 빈 문자열/누락 모두 None 반환. 테스트로 회귀 게이트 (test_video_gen_disabled_by_default). |
| T-04-W4-02 | Information Disclosure | 미래 Omni API key (stub 단계 — 아직 없음) | accept | stub 단계 위협 표면 없음. OmniVertexAdapter 구현 시 GEMINI_API_KEY = SSM Parameter Store (CLAUDE.md §3 시크릿 정책 적용 필수 — 미래 plan 항목). |
| T-04-W4-03 | Denial of Service | 실수로 SYNTHESIS_VIDEO_GEN_ENABLED="1" 설정 후 OmniVertexAdapter 호출 | mitigate | generate_virtual_view 에서 NotImplementedError 즉시 raise → 파이프라인이 graceful degrade (ai_synthesis_failed warning, D-07 정합). NotImplementedError 는 pipeline _call_synthesis_adapter 의 except Exception 블록이 흡수. |
| T-04-W4-SC | Tampering | pip/npm 설치 없음 (stub 는 .py 파일 신설만) | accept | 신규 외부 패키지 설치 없음. 기존 interfaces.py + 표준 라이브러리만 의존. |
</threat_model>

<verification>
pytest backend/tests/phase04/test_video_gen_adapter.py -x -q → 5개 GREEN
pytest backend/tests/phase04/ -q → Wave 1~4 전체 GREEN
pytest backend/tests/ -q → 기존 suite PASS 카운트 불변
python3 -c "from sunity_shared.analysis.synthesis.interfaces import VideoGenerationAdapter; from sunity_shared.analysis.synthesis.video_gen_adapter import OmniVertexAdapter, VeoAdapter, get_video_gen_adapter; print('OK')"
grep -c "SYNTHESIS_VIDEO_GEN_ENABLED" backend/shared/python/sunity_shared/analysis/synthesis/video_gen_adapter.py | grep -v "^0$"
</verification>

<success_criteria>
Wave 4 완료 기준:
1. VideoGenerationAdapter Protocol 이 interfaces.py 에 존재하고 import 성공
2. OmniVertexAdapter / VeoAdapter stub 이 generate_virtual_view 호출 시 NotImplementedError 발생
3. SYNTHESIS_VIDEO_GEN_ENABLED 미설정 = get_video_gen_adapter() None → 파이프라인 영향 없음
4. 단위 테스트 5개 GREEN (비활성 + stub NotImplementedError + Protocol structural subtyping)
5. 기존 Phase 4 suite (Wave 1~3) 회귀 0
6. 비용/라이선스 주석 박제 (D-28 정합): 파일 헤더에 Omni $6-36/영상 vs $0.12/video 기준선 명시
</success_criteria>

<output>
Create `.planning/phases/04-ux-occlusion-confidence/04-04-SUMMARY.md` when done
</output>
