---
phase: 04
plan: "05"
type: execute
wave: 5
depends_on:
  - 04-03
files_modified:
  - backend/scripts/reprocess_reference_motions_phase4.py
  - backend/tests/phase04/test_evaluate_4way.py
  - backend/tests/phase04/test_reprocess_reference_g4.py

autonomous: false
requirements:
  - POSE-03

must_haves:
  truths:
    - "정은지 5영상(ref-sideway-spin/ref-climb/ref-invert/ref-foxtop/ref-foxtop-split)이 Phase 4 신규 파이프라인(Wave 1 Gemini + Wave 3 cylindrical mesh)으로 재처리되고 Firestore referenceMotions 문서가 원자적으로 갱신된다 (D-09, per D-09)"
    - "G4 가드가 재처리 중 유지된다 — is_reference=True 로 호출되므로 합성 트리거는 발동하지 않으며, RTMW 품질 향상(1차 분석) 자체는 그대로 적용된다 (D-10, per D-10)"
    - "evaluate_4way(POSE-03-g)로 재처리 전/후 IPSF 추정 감점(axis_b occlusion)이 비교되고, cylindrical mesh path 가 baseline(rtmw_mirror) 대비 axis_b 감점을 줄이거나 동등하다 (G4 악화 0 조건 — D-10)"
    - "belle 시각 검증(manual-only): 재처리된 5영상 ref 점수가 Phase 4 이전보다 낮아지지 않는다"
  artifacts:
    - path: backend/scripts/reprocess_reference_motions_phase4.py
      provides: "정은지 5영상 Phase 4 파이프라인 재처리 스크립트 (RunPod GPU 전용)"
      contains: "MOTION_IDS"
    - path: backend/tests/phase04/test_evaluate_4way.py
      provides: "POSE-03-g evaluate_4way 통합 테스트 (04-03 신규 작성 파일에 RunPod 통합 테스트 append)"
      contains: "evaluate_4way"
    - path: backend/tests/phase04/test_reprocess_reference_g4.py
      provides: "G4 가드 + 재처리 결과 비악화 단위 테스트"
      contains: "test_g4_guard_no_synthesis_for_reference_reprocess"
  key_links:
    - from: backend/scripts/reprocess_reference_motions_phase4.py
      to: backend/shared/python/sunity_shared/analysis/synthesis/interfaces.py
      via: "identify_synthesis_targets + is_reference=True → mask 전체 False 보장"
      pattern: "is_reference"
    - from: backend/scripts/reprocess_reference_motions_phase4.py
      to: backend/shared/python/sunity_shared/firestore_admin.py
      via: "complete_analysis(ai_synthesis_meta=None) — reference 재처리는 meta 미기록"
      pattern: "complete_analysis"
    - from: backend/tests/phase04/test_evaluate_4way.py
      to: .planning/spikes/001-dataset-eval-harness/metrics.py
      via: "evaluate_4way + PathOutput 재사용 (04-03 신규 파일에 04-05 가 append)"
      pattern: "evaluate_4way"

user_setup: []
---

<objective>
Phase 4 Wave 5 — 정은지 5영상 재처리 + 정확도 비교 + G4 가드 검증. 신규 파이프라인(Wave 1 Gemini view reasoning + Wave 3 cylindrical mesh)으로 reference 5영상을 재처리하고, mode1 비교의 양쪽이 동일 파이프라인으로 분석됨을 보장한다(D-09). G4 가드(is_reference=True 시 합성 트리거 0)가 재처리 중에도 유지되며, evaluate_4way(POSE-03-g)로 재처리 전/후 IPSF axis_b 감점 변화를 정량 검증한다(D-10). belle 시각 검증은 manual-only.

Purpose: mode1 비교의 신뢰도 보장 — reference 와 사용자 영상이 동일 파이프라인을 거칠 때만 점수 비교가 의미 있다. 정은지 5영상 재처리 = Phase 4 의 최종 수직 슬라이스.
Output: reprocess 스크립트 + test_evaluate_4way.py 에 RunPod 통합 테스트 append(기존 파일 수정) + test_reprocess_reference_g4.py + RunPod 실행 기록 + belle 시각 검증 체크포인트
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-ux-occlusion-confidence/04-CONTEXT.md
@.planning/phases/04-ux-occlusion-confidence/04-RESEARCH.md
@.planning/phases/04-ux-occlusion-confidence/04-VALIDATION.md
@.planning/ROADMAP.md

@backend/scripts/extract_reference_keypoint_reports.py
@backend/scripts/extract_reference_angles.py
@.planning/spikes/001-dataset-eval-harness/metrics.py
@.planning/spikes/001-dataset-eval-harness/ipsf_criteria.py
@.planning/spikes/001-dataset-eval-harness/dataset.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: reprocess_reference_motions_phase4.py + G4 guard 단위 테스트</name>
  <files>
    backend/scripts/reprocess_reference_motions_phase4.py,
    backend/tests/phase04/test_reprocess_reference_g4.py
  </files>

  <read_first>
    필수:
    1. backend/scripts/extract_reference_keypoint_reports.py (전체) — 재처리 스크립트 구조 원형 (S3 download → frame extract → RTMW pose → 결과 직렬화 패턴)
    2. backend/scripts/extract_reference_angles.py (전체) — _extract_one 로직 + MOTION_IDS 5개 + _VERTICAL_POLE_AXIS fallback + temporal_fill + occluded_mask 순서
    3. backend/shared/python/sunity_shared/analysis/synthesis/interfaces.py — identify_synthesis_targets 시그니처 + is_reference 체크 패턴 (Wave 1 산출물 — 읽어 확인)
    4. backend/functions/pipeline/app.py — _call_synthesis_adapter 함수 구현 (Wave 1 산출물 — is_reference=True 분기 확인)
    5. backend/shared/python/sunity_shared/firestore_admin.py (lines 600-680) — complete_analysis ai_synthesis_meta 파라미터 (Wave 1 산출물) + _validate_flat_dict_no_nested_array
    6. 04-CONTEXT.md D-09, D-10 — 재처리 자동화 + G4 가드 + 비악화 검증 정책
    7. 04-RESEARCH.md §Pitfall 5 — G4 occlusion FP 가드 (is_reference=True + occlusion_severe 동시 시 합성 0)
    8. 04-RESEARCH.md §Runtime State Inventory — Firestore referenceMotions 덮어쓰기 vs pipelineVersion 필드 전략
    9. .planning/spikes/001-dataset-eval-harness/metrics.py (전체) — PathOutput dataclass + evaluate_4way signature (test_evaluate_4way.py 에서 재사용)
    10. backend/tests/phase04/test_synthesis_g4_guard.py — 기존 G4 가드 테스트 패턴 (Wave 1 산출물, 재처리 전용 신규 테스트 작성 시 참조)
  </read_first>

  <behavior>
    test_reprocess_reference_g4.py:

    test_g4_guard_no_synthesis_for_reference_reprocess:
      - reprocess 스크립트의 _reprocess_one 함수를 직접 호출하지 않고, 스크립트 임포트 후
        스크립트가 _call_synthesis_adapter(is_reference=True) 를 사용하거나 동등한
        is_reference=True 게이팅 코드를 갖는지 AST 또는 grep 검증.
        구체적으로: "is_reference" 문자열이 reprocess 스크립트에 존재해야 GREEN.

    test_reprocess_pipelineVersion_field_added:
      - 재처리 스크립트가 Firestore payload 에 pipelineVersion 필드를 포함하는지 확인.
        "pipelineVersion" 문자열이 reprocess 스크립트에 존재 → GREEN.
        (RESEARCH.md Assumptions A3: 덮어쓰기 vs 버전 필드 — pipelineVersion 추가 방식 선택)

    test_reprocess_motion_ids_complete:
      - MOTION_IDS 리스트가 5개 전부 포함하는지: 스크립트 임포트 후 len(MOTION_IDS) == 5 → GREEN.

    reprocess_reference_motions_phase4.py:
      MOTION_IDS = ["ref-sideway-spin", "ref-climb", "ref-invert", "ref-foxtop", "ref-foxtop-split"]
      S3_BUCKET = "sunity-motion-pilot-videos", S3_PREFIX = "reference"

      _reprocess_one(motion_id, s3, extractor, engine, target_fps, s3_prefix):
        - extract_reference_angles.py 의 _extract_one + extract_reference_keypoint_reports.py 의
          _process_motion 을 합친 구조. 두 스크립트 분리 호출을 1회 실행으로 통합.
        - S3 download → FfmpegFrameExtractor → RTMWPoseEngine.estimate + measure_body_profile +
          to_coco17_array → compute_joint_angles + temporal_fill (angles 산출)
        - build_keypoint_report(pose_frames, fps=target_fps) (keypointReport 산출)
        - is_reference=True 를 명시적으로 기록해 identify_synthesis_targets 호출 시
          합성 mask 가 전부 False 임을 주석으로 박제.
          실제 합성 어댑터는 호출하지 않음 — reference 재처리는 1차 RTMW 품질 향상만 적용.
        - pipelineVersion = "phase4_v1" 필드 추가 (RESEARCH.md A3 권장 방식)
        - 반환 dict: angles (flat list), anglesJointKeys, anglesFrames, keypointReport(camelCase),
          pipelineVersion, reprocessedAt (ISO 8601 UTC)
        - NaN 잔여 시 0.0 치환 (extract_reference_angles.py:143 패턴 동일)

      main():
        - --out Path (default /workspace/reference-phase4-reprocess.json)
        - --motions 부분 재처리 지원 (단일 영상 디버그용)
        - --target-fps float (default 18.0, extract_reference_keypoint_reports.py 18fps 정합)
        - --dry-run flag: S3 download 및 Firestore 쓰기 생략, 스크립트 구조 검증용
        - 부분 실패 허용 (continue on except) — extract_reference_angles.py:172 패턴
        - 완료 후 belle 안내 카피 출력: "재처리 완료 → 로컬로 받아 seed-reference-motions.mjs 투입"
        - 전체 목표: 단독 스크립트 1회 실행으로 5영상 angles + keypointReport 재산출
  </behavior>

  <action>
    1. backend/tests/phase04/test_reprocess_reference_g4.py 신규 작성:
       파일 상단 주석: "Phase 4 Wave 5 — 정은지 5영상 재처리 G4 가드 + pipelineVersion 단위 테스트. RunPod 불필요 (파일 내용 grep + import 검증만)."
       3개 테스트 작성 (behavior 대로). importlib.util.spec_from_file_location 으로
       scripts/reprocess_reference_motions_phase4.py 임포트 — sys.path 에 backend/scripts 추가.
       from __future__ import annotations, pytest, pathlib.Path 사용.

    2. backend/scripts/reprocess_reference_motions_phase4.py 신규 작성:
       모듈 docstring: "Phase 4 정은지 5영상 재처리 스크립트 (RunPod GPU 전용).
       Phase 4 신규 파이프라인(Wave 1 + Wave 3) 완성 후 reference 5영상을 동일 파이프라인으로
       재처리해 mode1 비교의 양쪽 원터필 보장(D-09). G4 가드 정합: is_reference=True 이므로
       합성 트리거 발동 없음 — RTMW 1차 분석 품질 향상만 적용(D-10).

       Usage (RunPod GPU):
         cd /workspace/SunityMotion/backend
         source pod.env
         export PYTHONPATH=$PWD:$PWD/shared/python
         python scripts/reprocess_reference_motions_phase4.py --out /workspace/reference-phase4-reprocess.json

       Output: 재처리된 angles + keypointReport JSON → seed-reference-motions.mjs 투입."

       from __future__ import annotations 포함. import 순서: stdlib → boto3 → sunity_shared (sys.path 주입 패턴 = extract_reference_keypoint_reports.py:39-41 그대로).
       MOTION_IDS 5개 상수, S3_BUCKET, S3_PREFIX 상수.
       _VERTICAL_POLE_AXIS = PoleAxis(axis_vector=(0.0,1.0,0.0), confidence_level="low", source="vertical_fallback") — extract_reference_angles.py 82-87 정합.
       _reprocess_one 함수: 두 기존 스크립트의 핵심 로직 통합 (설명은 behavior 참조).
       is_reference=True 주석 박제 (합성 트리거 발동 0 보장 설명).
       main() argparse: --out, --motions (nargs="+"), --target-fps (default 18.0), --dry-run.
       부분 실패 continue 패턴 (extract_reference_angles.py:172 정합).

    Commit: 단일 atomic commit "feat(04-05): reprocess_reference_motions_phase4 + G4 guard 테스트 (POSE-03 D-09/D-10)"
  </action>

  <verify>
    <automated>
      cd /Users/kimtaesung/Dev/SunityMotion &&
      python -m pytest backend/tests/phase04/test_reprocess_reference_g4.py -x -q 2>&1 | tail -15
    </automated>
  </verify>

  <acceptance_criteria>
    - test_reprocess_reference_g4.py 3개 테스트 모두 GREEN:
      test_g4_guard_no_synthesis_for_reference_reprocess (is_reference 문자열 존재)
      test_reprocess_pipelineVersion_field_added (pipelineVersion 문자열 존재)
      test_reprocess_motion_ids_complete (MOTION_IDS 길이 5)
    - python -m pytest backend/tests/phase04/ -x -q — 기존 phase04 suite 회귀 0 (새 테스트 포함 모두 GREEN)
    - python -c "import sys; sys.path.insert(0,'backend/scripts'); sys.path.insert(0,'backend/shared/python'); import reprocess_reference_motions_phase4; print(len(reprocess_reference_motions_phase4.MOTION_IDS))" → 5
    - grep -c "is_reference" backend/scripts/reprocess_reference_motions_phase4.py | grep -v '^0$'
    - grep -c "pipelineVersion" backend/scripts/reprocess_reference_motions_phase4.py | grep -v '^0$'
  </acceptance_criteria>

  <done>reprocess 스크립트 작성 완료 + G4 가드 + pipelineVersion 단위 테스트 3개 GREEN + phase04 suite 회귀 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: test_evaluate_4way.py — RunPod 통합 테스트 append (04-03 신규 파일 수정)</name>
  <files>
    backend/tests/phase04/test_evaluate_4way.py
  </files>

  <read_first>
    필수:
    1. backend/tests/phase04/test_evaluate_4way.py (전체 — 04-03 에서 신규 작성된 파일) — 기존 내용 확인 후 append 할 위치 파악 (파일 말미에 RunPod 통합 테스트 추가)
    2. .planning/spikes/001-dataset-eval-harness/metrics.py (전체) — evaluate_4way 함수 + PathOutput dataclass + axis_b_occlusion_reduction_score 시그니처
    3. .planning/spikes/001-dataset-eval-harness/ipsf_criteria.py (전체) — ALL_CRITERIA + PHASE4_EVAL_AXIS_MAPPING + PRESENTATION GeometricCriterion
    4. .planning/spikes/001-dataset-eval-harness/harness.py (전체, 100줄) — make_synthetic_path fixture 패턴 + 4-way 비교 호출 예시
    5. .planning/spikes/001-dataset-eval-harness/dataset.py — JEONGEUNJI_5 fixture (5영상 메타 데이터)
    6. 04-RESEARCH.md §Validation Architecture §POSE-03-g — "evaluate_4way RunPod 필요 시 xfail 마킹, synthetic fixture 로 harness 동작 검증은 local pass"
    7. 04-VALIDATION.md §Per-Task Verification Map POSE-03-g — "integration, RunPod 필요"
    8. 04-CONTEXT.md D-10, D-22 — baseline -7.60pts/video, cylindrical mesh path > baseline 조건
    9. backend/tests/phase04/conftest.py — synthetic joint fixture 재사용 패턴 (Wave 0 산출물 — 읽어 확인)
  </read_first>

  <behavior>
    test_evaluate_4way.py 에 append 할 내용 (04-03 에서 신규 작성된 파일에 추가 — 재신설 금지):

    test_evaluate_4way_harness_smoke 와 test_cylindrical_mesh_no_smplx_import 등은
    04-03 에서 이미 작성됨. 이 태스크는 RunPod 전용 통합 테스트 2개를 파일 말미에 append 한다.

    _runpod_available() 헬퍼 (파일에 없으면 추가, 이미 있으면 재사용):
      os.environ.get("RUNPOD_ANALYZE_URL") 이 설정된 경우 True (Pod 실행 환경 감지).

    @pytest.mark.runpod
    test_evaluate_4way_harness_smoke_60f (non-skip, synthetic fixture):
      - conftest.py 의 joint_seq_60f fixture 재사용 (T=60 버전 명시적 검증 — 04-03 test_axis_b 와 별도).
      - PathOutput 3개 생성 (rtmw_mirror / gemini_view / cylindrical_mesh).
      - evaluate_4way(outputs, split_frame_indices=[10,20,30,40,50]) 호출 성공.
      - results dict 에 3개 path key 포함 → GREEN.
      - 이미 04-03 에 유사 smoke 있더라도, 60f fixture 명시 경로로 추가 검증.

    @pytest.mark.runpod
    test_evaluate_4way_reprocess_vs_baseline (RunPod 전용, xfail on local):
      - IPSF D-22 baseline: rtmw_mirror path = -7.60pts/video (occlusion 감점 시뮬).
      - reprocess_reference_motions_phase4.py --dry-run 으로 스크립트 실행 경로 검증 후,
        실 RunPod 실행 시에는 5영상 재처리 결과 PathOutput 로 wrap.
      - evaluate_4way 호출 → axis_b: cylindrical_mesh path axis_b score >= baseline axis_b score.
        (G4 악화 0 조건 — D-10: axis_b 감점이 baseline 보다 나빠지면 FAIL)
      - pytest.mark.xfail(strict=False) 로 마킹 (RunPod 없는 CI 에서 XFAIL 허용).
      - pytest.mark.skipif(not _runpod_available(), reason="RunPod 미연결") 로 local skip.
  </behavior>

  <action>
    1. backend/tests/phase04/test_evaluate_4way.py 읽기 (04-03 에서 신규 작성된 기존 파일 확인 필수).
       파일 말미에 RunPod 통합 테스트 2개를 append:
       - test_evaluate_4way_harness_smoke_60f
       - test_evaluate_4way_reprocess_vs_baseline

       파일 상단에 이미 "04-03 신규 작성" 주석이 있으면 아래에 다음 주석 1줄 삽입:
       "# 04-05 append: RunPod 통합 테스트 (test_evaluate_4way_harness_smoke_60f / test_evaluate_4way_reprocess_vs_baseline)"

       _runpod_available() 가 파일에 없으면 append 앞에 정의. 이미 있으면 재사용.

       sys.path 주입 (이미 파일 상단에 있으면 중복 추가 금지):
       spikes/001-dataset-eval-harness/ 경로. 기존 패턴 유지.

    2. test_evaluate_4way_harness_smoke_60f: joint_seq_60f fixture 파라미터 사용
       (conftest.py 의 04-00 신설 fixture). split_frame_indices=[10,20,30,40,50] (T=60 정합).

    3. test_evaluate_4way_reprocess_vs_baseline:
       @pytest.mark.xfail(strict=False, reason="RunPod GPU 필요 — local XFAIL 허용")
       @pytest.mark.skipif(not _runpod_available(), reason="RunPod 미연결")
       두 마킹 모두 적용 (local = skip, RunPod = 실행).
       RunPod 실행 흐름: 재처리 스크립트 --dry-run 후 실 PathOutput 으로 evaluate_4way 호출.
       axis_b assert: cylindrical_mesh_result["axis_b"] >= baseline_result["axis_b"] (nan 은 skip).
       D-10 + D-22 참조 주석 박제.

    Commit: "test(04-05): evaluate_4way RunPod 통합 테스트 append (D-10 G4 비악화 검증)"
  </action>

  <verify>
    <automated>
      cd /Users/kimtaesung/Dev/SunityMotion &&
      python -m pytest backend/tests/phase04/test_evaluate_4way.py -x -q -m "not runpod" 2>&1 | tail -15 &&
      python -m pytest backend/tests/phase04/ -q 2>&1 | tail -5
    </automated>
  </verify>

  <acceptance_criteria>
    - test_evaluate_4way_harness_smoke_60f GREEN (joint_seq_60f fixture, RunPod 불필요)
    - test_evaluate_4way_reprocess_vs_baseline SKIPPED 또는 XFAIL (RunPod 미연결 시 정상)
    - backend/tests/phase04/ 전체 suite: 기존 GREEN 테스트 (04-03 smoke gate 포함) 회귀 0
    - grep -c "evaluate_4way" backend/tests/phase04/test_evaluate_4way.py | grep -v '^0$'
    - grep -c "axis_b" backend/tests/phase04/test_evaluate_4way.py | grep -v '^0$'
    - grep -c "RUNPOD_ANALYZE_URL" backend/tests/phase04/test_evaluate_4way.py | grep -v '^0$'
    - grep -c "joint_seq_60f" backend/tests/phase04/test_evaluate_4way.py | grep -v '^0$'
  </acceptance_criteria>

  <done>test_evaluate_4way.py 에 RunPod 통합 테스트 2개 append 완료 (04-05 수정, 04-03 신규 내용 보존) + RunPod 없는 환경에서 SKIP/XFAIL + phase04 suite 전체 회귀 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Task 1+2 완료 후: reprocess_reference_motions_phase4.py 가 작성됐고,
    RunPod Pod 에서 실행해 5영상(ref-sideway-spin/ref-climb/ref-invert/ref-foxtop/ref-foxtop-split)을
    Phase 4 파이프라인으로 재처리해야 한다. 재처리 결과 JSON 을 belle 가 확인하고 시각적으로 점수
    비악화 여부를 검증한다.
  </what-built>
  <how-to-verify>
    RunPod Pod 기동 후 실행 (Claude 가 SSH 실행):

    1. GitHub push 확인:
       git log --oneline -3 — 최신 커밋 2개(Task 1+2) 확인.
       Pod 에서 git pull origin main.

    2. 스크립트 실행:
       cd /workspace/SunityMotion/backend
       source pod.env
       export PYTHONPATH=$PWD:$PWD/shared/python
       python scripts/reprocess_reference_motions_phase4.py \
         --out /workspace/reference-phase4-reprocess.json \
         --target-fps 18.0

    3. evaluate_4way 통합 테스트 실행 (RunPod 에서):
       export RUNPOD_ANALYZE_URL=http://localhost:8000
       cd /workspace/SunityMotion
       python -m pytest backend/tests/phase04/test_evaluate_4way.py -x -q 2>&1 | tail -20

    4. 재처리 JSON 확인 (Claude 가 로컬로 가져옴):
       scp pod:/workspace/reference-phase4-reprocess.json /tmp/reference-phase4-reprocess.json
       - 5개 motionId 모두 존재하는지 확인
       - pipelineVersion: "phase4_v1" 필드 존재 여부 확인
       - angles 각 배열 길이가 이전 reference-angles.json 과 비교해 합리적인지 확인

    5. belle 시각 검증:
       재처리 결과 JSON 을 seed-reference-motions.mjs staging 환경에 투입 후
       mode1 결과 화면에서 정은지 기준 점수가 Phase 4 이전보다 낮아지지 않는지 확인.
       특히 ref-sideway-spin (G4 occlusion FP 대상) 의 score 변화 확인.
  </how-to-verify>
  <resume-signal>
    아래 중 하나를 입력하세요:
    - "approved" + evaluate_4way axis_b 비교 결과 (예: "cylindrical_mesh axis_b >= baseline: pass")
    - "approved" + belle 시각 확인 결과 (점수 비악화 확인)
    - 문제 발생 시 구체적 에러 메시지 또는 재처리 실패 motionId
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RunPod Pod → S3 | boto3 AWS 자격증명이 Pod env 에 평문 주입됨 — pod.env 통해 주입, 로그 출력 금지 |
| RunPod Pod → Firestore | Firebase SA 키가 Pod env (FIREBASE_SA_JSON / FIREBASE_SA_PATH) 에 주입됨 |
| reprocess 결과 JSON → Firestore seed | 로컬로 받은 JSON 을 seed-reference-motions.mjs 로 Firestore 에 쓸 때 레이스 컨디션 가능 (mode1 사용자가 동시에 reference 조회 중) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-W5-01 | Information Disclosure | pod.env 에서 AWS/Firebase 키 로그 출력 | mitigate | 스크립트 내 print/log 에서 env var 값 직접 출력 금지. log.info 에 키 마스킹 — extract_reference_angles.py:180-185 패턴 정합. |
| T-04-W5-02 | Tampering | G4 guard bypass — is_reference=True 없이 합성 트리거 발동 | mitigate | 재처리 스크립트에 is_reference=True 주석 박제 + test_g4_guard_no_synthesis_for_reference_reprocess 테스트가 회귀 게이트. G4 악화 = FAIL 조건 (D-10). |
| T-04-W5-03 | Tampering | reprocess 결과로 Firestore referenceMotions 덮어쓰기 중 기존 사용자 mode1 조회 불일치 | mitigate | pipelineVersion 필드 추가 + seed 스크립트의 원자적 Firestore update (기존 seed-reference-motions.mjs 패턴 재사용 — 부분 갱신 중 읽기 발생 시 클라이언트는 이전 버전 읽음, 다음 onSnapshot 에서 정합). |
| T-04-W5-04 | Elevation of Privilege | S3 presigned URL 없이 reference 영상 직접 접근 | accept | Pod 내부에서 boto3 S3 client 직접 사용 (IAM 권한 sunity-motion 키 범위). 사용자 데이터 아님 — reference 공개 영상. |
| T-04-W5-SC | Tampering | pip installs on Pod (신규 패키지 없음) | accept | reprocess 스크립트 신규 외부 패키지 없음 (boto3 + sunity_shared 재사용). Phase 3 cylindrical mesh requirements.txt 이미 검증됨. |
</threat_model>

<verification>
pytest backend/tests/phase04/test_reprocess_reference_g4.py -x -q → 3개 GREEN (G4 가드 + pipelineVersion + MOTION_IDS 길이)
pytest backend/tests/phase04/test_evaluate_4way.py -x -q -m "not runpod" → GREEN (synthetic fixture + 60f smoke)
pytest backend/tests/phase04/ -q → 전체 suite 회귀 0
RunPod 실행 후: test_evaluate_4way_reprocess_vs_baseline — cylindrical_mesh axis_b >= baseline axis_b (D-10 G4 악화 0)
belle 시각 검증: ref-sideway-spin 재처리 점수 >= Phase 4 이전 점수
</verification>

<success_criteria>
Wave 5 완료 기준 (ROADMAP SC #7 정은지 5영상 재처리 + D-10 G4 악화 0):
1. reprocess_reference_motions_phase4.py 작성 완료 — MOTION_IDS 5개, is_reference=True 게이트, pipelineVersion 필드
2. test_reprocess_reference_g4.py 3개 GREEN — G4 가드 + pipelineVersion + MOTION_IDS 완전성
3. test_evaluate_4way_harness_smoke_60f GREEN (joint_seq_60f fixture, RunPod 불필요)
4. RunPod 실행 후: 5영상 재처리 JSON 생성 완료 + evaluate_4way axis_b: cylindrical_mesh >= baseline
5. belle 시각 검증 PASS: ref-sideway-spin 재처리 후 점수 비악화 확인
6. phase04 전체 test suite 회귀 0
</success_criteria>

<output>
Create `.planning/phases/04-ux-occlusion-confidence/04-05-SUMMARY.md` when done
</output>
