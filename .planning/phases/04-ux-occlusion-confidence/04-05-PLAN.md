---
phase: 04
plan: "05"
type: execute
wave: 5
depends_on:
  - 04-01
  - 04-03
  # depends_on 04-03 = Wave 3a smoke까지만 (cylindrical mesh build + 12-view render artifact + license).
  # 04-03 Wave 3b(실 RunPod RTMW 재추론 accuracy gate)가 미완이어도 Wave 5 진행 가능 — Gemini path 기준 재처리 가능.
  # 반박#2 채택: 3b 미완 시 reprocess 스크립트는 Wave 1 Gemini adapter 경로로 동작.
files_modified:
  - backend/scripts/reprocess_reference_motions_phase4.py
  - backend/scripts/rollback_reference_motions_phase4.py
  - backend/tests/phase04/test_evaluate_4way.py
  - backend/tests/phase04/test_reprocess_reference_g4.py

autonomous: false
requirements:
  - POSE-03

must_haves:
  truths:
    - "정은지 5영상(ref-sideway-spin/ref-climb/ref-invert/ref-foxtop/ref-foxtop-split)이 Phase 4-compatible 재처리(RTMW pose extraction + keypointReport + angles + joints3d payload, **is_reference=True 이므로 Gemini/mesh 합성 호출 0** — BLOCKER-1)되어 Firestore reference/{id}/versions/phase4_v1 에 버전별 기록 후 5개 전부 schema+metric gate 통과 시에만 active 포인터 flip + top-level mirror (D-09, per D-09)"
    - "G4 가드가 재처리 중 유지된다 — is_reference=True 로 호출되므로 합성 트리거는 발동하지 않으며, RTMW 품질 향상(1차 분석) 자체는 그대로 적용된다 (D-10, per D-10). 행위 기반 테스트(fake adapter)로 증명."
    - "evaluate_4way(POSE-03-g)로 재처리 전/후 IPSF 추정 감점(axis_b occlusion)이 비교되고, cylindrical mesh path 가 baseline(rtmw_mirror) 대비 axis_b 감점을 줄이거나 동등하다 (G4 악화 0 조건 — D-10)"
    - "belle 시각 검증(manual-only): 재처리된 5영상 ref 점수가 Phase 4 이전보다 낮아지지 않는다"
    - "rollback 경로 존재: rollback_reference_motions_phase4.py 실행으로 active 포인터를 이전 버전으로 되돌릴 수 있다"
  artifacts:
    - path: backend/scripts/reprocess_reference_motions_phase4.py
      provides: "정은지 5영상 Phase 4 파이프라인 재처리 스크립트 (RunPod GPU 전용) — versioned/atomic write + dry-run JSON schema 검증 포함"
      contains: "MOTION_IDS"
    - path: backend/scripts/rollback_reference_motions_phase4.py
      provides: "active 포인터 롤백 스크립트 — 이전 버전으로 되돌리기"
      contains: "rollback"
    - path: backend/tests/phase04/test_evaluate_4way.py
      provides: "POSE-03-g evaluate_4way 통합 테스트 (04-03 신규 작성 파일에 RunPod 통합 테스트 append)"
      contains: "evaluate_4way"
    - path: backend/tests/phase04/test_reprocess_reference_g4.py
      provides: "G4 가드 behavioral 테스트 (fake adapter 주입) + dry-run schema 검증 + versioned write 검증"
      contains: "test_g4_guard_no_synthesis_for_reference_reprocess"
  key_links:
    - from: backend/scripts/reprocess_reference_motions_phase4.py
      to: backend/shared/python/sunity_shared/analysis/synthesis/interfaces.py
      via: "identify_occlusion_targets + is_reference=True → mask 전체 False 보장 (fake adapter 행위 테스트로 증명)"
      pattern: "is_reference"
    - from: backend/scripts/reprocess_reference_motions_phase4.py
      to: "Firestore reference/{id}/versions/phase4_v1"
      via: "versioned write → 5개 schema gate 전부 통과 후 active 포인터 flip"
      pattern: "versions/phase4_v1"
    - from: backend/tests/phase04/test_evaluate_4way.py
      to: .planning/spikes/001-dataset-eval-harness/metrics.py
      via: "evaluate_4way + PathOutput 재사용 (04-03 신규 파일에 04-05 가 append)"
      pattern: "evaluate_4way"

user_setup: []
---

<objective>
Phase 4 Wave 5 — 정은지 5영상 **Phase 4-compatible 재처리** + versioned/atomic write + rollback. reference 5영상을 RTMW pose extraction → keypointReport + angles + joints3d payload 로 재처리한다 (pipelineVersion="phase4_v1"). **합성(Gemini/mesh)은 호출하지 않는다 — is_reference=True 이므로 G4 가드로 합성 트리거 0** (BLOCKER-1: reference active doc 에 synthetic 좌표를 쓰지 않음. mode1 양쪽이 동일한 "1차 RTMW + Phase 4 payload" 기준으로 분석됨을 보장, D-09). G4 가드는 fake adapter 주입 행위 테스트로 증명. evaluate_4way(POSE-03-g)는 **reference migration 과 분리된 평가 산출물** — cylindrical mesh path 의 IPSF axis_b 효과를 측정할 뿐 reference active doc 에 쓰지 않는다(D-10). Firestore write는 versioned/atomic — 5개 schema+metric gate 통과 전까지 쓰기 0, 통과 후 active 포인터 flip + top-level mirror. 롤백 스크립트 제공. belle 시각 검증은 manual-only.

Purpose: mode1 비교의 신뢰도 보장 — reference 와 사용자 영상이 동일 파이프라인을 거칠 때만 점수 비교가 의미 있다. versioned write = mode1 양쪽 오염 방지(분석 정확도 최우선).
Output: reprocess 스크립트 + rollback 스크립트 + test_evaluate_4way.py 에 RunPod 통합 테스트 append(기존 파일 수정) + test_reprocess_reference_g4.py(behavioral 테스트) + RunPod 실행 기록 + belle 시각 검증 체크포인트
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-ux-occlusion-confidence/04-CONTEXT.md
@.planning/phases/04-ux-occlusion-confidence/04-RESEARCH.md
@.planning/phases/04-ux-occlusion-confidence/04-VALIDATION.md
@.planning/ROADMAP.md

@backend/scripts/extract_reference_keypoint_reports.py
@backend/scripts/extract_reference_angles.py
@.planning/spikes/001-dataset-eval-harness/metrics.py
@.planning/spikes/001-dataset-eval-harness/ipsf_criteria.py
@.planning/spikes/001-dataset-eval-harness/dataset.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: reprocess_reference_motions_phase4.py + rollback 스크립트 + G4 가드 behavioral 테스트</name>
  <files>
    backend/scripts/reprocess_reference_motions_phase4.py,
    backend/scripts/rollback_reference_motions_phase4.py,
    backend/tests/phase04/test_reprocess_reference_g4.py
  </files>

  <read_first>
    필수:
    1. backend/scripts/extract_reference_keypoint_reports.py (전체) — 재처리 스크립트 구조 원형 (S3 download → frame extract → RTMW pose → 결과 직렬화 패턴)
    2. backend/scripts/extract_reference_angles.py (전체) — _extract_one 로직 + MOTION_IDS 5개 + _VERTICAL_POLE_AXIS fallback + temporal_fill + occluded_mask 순서
    3. backend/shared/python/sunity_shared/analysis/synthesis/interfaces.py — identify_occlusion_targets 시그니처 + is_reference 체크 패턴 (Wave 1 산출물 — 읽어 확인)
    4. backend/functions/pipeline/app.py — _call_synthesis_adapter 함수 구현 (Wave 1 산출물 — is_reference=True 분기 확인)
    5. backend/shared/python/sunity_shared/firestore_admin.py (lines 600-680) — complete_analysis ai_synthesis_meta 파라미터 (Wave 1 산출물) + _validate_flat_dict_no_nested_array
    6. 04-CONTEXT.md D-09, D-10 — 재처리 자동화 + G4 가드 + 비악화 검증 정책
    7. 04-RESEARCH.md §Pitfall 5 — G4 occlusion FP 가드 (is_reference=True + occlusion_severe 동시 시 합성 0)
    8. 04-RESEARCH.md §Runtime State Inventory — Firestore reference collection 덮어쓰기 vs versioned write 전략 (BLOCKER-2 정정: canonical path = reference/{id}, referenceMotions 아님)
    9. .planning/spikes/001-dataset-eval-harness/metrics.py (전체) — PathOutput dataclass + evaluate_4way signature
    10. backend/tests/phase04/test_synthesis_g4_guard.py — 기존 G4 가드 테스트 패턴 (Wave 1 산출물, behavioral fake adapter 패턴 참조)
  </read_first>

  <behavior>
    test_reprocess_reference_g4.py — 4개 테스트 (R10 behavioral 교체):

    FakeSynthesisAdapter (테스트 파일 내 정의):
      - 합성 어댑터 인터페이스를 구현하는 간단한 fake.
      - synthesize() 또는 동등한 메서드가 호출되면 즉시 AssertionError("synthesis adapter called for reference — G4 guard violated") raise.
      - 이 fake를 is_reference=True 경로에 주입해 호출 시 FAIL 을 행위로 증명.

    test_g4_guard_no_synthesis_for_reference_reprocess:
      - FakeSynthesisAdapter 를 reprocess 경로의 synthesis adapter 자리에 주입.
      - _reprocess_one(motion_id, ..., synthesis_adapter=FakeSynthesisAdapter()) 호출 시
        AssertionError 가 raise 되지 않으면 GREEN (합성 호출이 0임을 행위로 증명).
      - is_reference=True 로 인해 합성 경로를 전혀 타지 않아야 한다.
      - 이 테스트는 RunPod 불필요 — 네트워크/S3/GPU 의존 없이 pure Python 로직 검증.

    test_dry_run_schema_validation_all_5:
      - reprocess 스크립트를 --dry-run 모드로 호출: S3 download 및 Firestore 쓰기를 생략하고
        synthetic fixture 를 이용해 5개 motionId 각각에 대해 _build_payload() 또는
        동등한 함수를 호출해 반환 dict 의 JSON schema 를 검증.
      - 검증 항목: angles(list), anglesJointKeys(list), anglesFrames(int), keypointReport(dict),
        joints3d(list flat), joints3dKeys(list len 17 COCO-17), joints3dFrames(int), coordDim(==3),
        space("pole_aligned"), pipelineVersion("phase4_v1"), reprocessedAt(str ISO 8601) 11개 키 모두 존재
        (BLOCKER-2 — joints3d 필드 세트 추가, 04-01 사용자 경로 contract 와 동일).
      - 5개 motionId 전부 통과해야 GREEN. 1개라도 key 누락 → FAIL.
      - "5개 전부 통과 전 쓰기 0" 원칙의 dry-run 검증.

    test_versioned_write_path_uses_versions_subpath (HIGH-2 — fake Firestore client behavioral, AST 검색 아님):
      - FakeFirestoreClient 주입 — .document(path)/.collection(path) 호출 path 를 전부 기록.
      - _write_versioned(fake, "ref-foxtop", payload) 후 기록된 write path == "reference/ref-foxtop/versions/phase4_v1" 정확 일치 단언.
      - "referenceMotions" 문자열이 어떤 호출 path 에도 등장하지 않음 단언 (잘못된 collection write 금지).
      - _flip_active_pointer: completed 5개 미만일 때 top-level merge/update 호출 횟수 == 0 단언 (gate 전 쓰기 0).
      - 5개 완료 후에만 active pointer + top-level mirror(joints3d 포함) write 발생 확인.
      - (PIPELINE_VERSION="phase4_v1" 상수 존재만으로 통과하던 AST 검색 폐기 — 실 write path 증명.)

    test_active_pointer_flip_requires_all_5:
      - reprocess 스크립트의 main() 또는 _flip_active_pointer() 로직을 import 후 검사.
      - partial_results = {4개 motionId: payload} (5개 미만) 상태에서 flip 함수 호출 시
        flip이 실행되지 않거나 ValueError/AssertionError 가 raise 됨을 확인.
      - 5개 전부 성공 시에만 active pointer flip 진행 — 분析 정확도 최우선(D-09).

    reprocess_reference_motions_phase4.py:
      MOTION_IDS = ["ref-sideway-spin", "ref-climb", "ref-invert", "ref-foxtop", "ref-foxtop-split"]
      S3_BUCKET = "sunity-motion-pilot-videos", S3_PREFIX = "reference"
      PIPELINE_VERSION = "phase4_v1"

      _validate_payload_schema(payload: dict, motion_id: str) -> None:
        - angles, anglesJointKeys, anglesFrames, keypointReport, joints3d, joints3dKeys, joints3dFrames, coordDim(==3), space("pole_aligned"), pipelineVersion, reprocessedAt **11개 키** 존재 검증 (BLOCKER-2 — test_dry_run_schema_validation_all_5 의 11키와 동일. joints3d 5필드 포함).
        - pipelineVersion == PIPELINE_VERSION 검증.
        - angles가 빈 list 아님 검증.
        - 실패 시 ValueError(f"{motion_id}: schema 검증 실패 — {missing keys}") raise.
        - 이 함수가 5개 전부 통과하기 전에는 Firestore write가 일어나지 않음.

      _reprocess_one(motion_id, s3, extractor, engine, target_fps, s3_prefix, synthesis_adapter=None):
        - synthesis_adapter 파라미터 추가 (기본값 None = 합성 비활성).
        - is_reference=True 를 명시적으로 기록해 identify_occlusion_targets 호출 시
          합성 mask 가 전부 False 임을 주석으로 박제. synthesis_adapter 는 호출 안 함.
        - S3 download → FfmpegFrameExtractor → RTMWPoseEngine.estimate + measure_body_profile +
          to_coco17_array → compute_joint_angles + temporal_fill (angles 산출)
        - joints3d 산출 (BLOCKER-2): to_coco17_array(pose_frames)[:, :, :3] (T,17,3, pole_aligned) → .reshape(-1).tolist() flat. joints3d_keys = COCO-17 17 keypoint 순서. joints3d_frames = T. coord_dim = 3. space = "pole_aligned" (04-01 동일 — 합성 아닌 1차 RTMW pose 좌표).
        - build_keypoint_report(pose_frames, fps=target_fps) (keypointReport 산출)
        - pipelineVersion = PIPELINE_VERSION, reprocessedAt = ISO 8601 UTC 타임스탬프 포함.
        - NaN 잔여 시 0.0 치환 (extract_reference_angles.py:143 패턴 동일)
        - 반환 dict: angles (flat list), anglesJointKeys, anglesFrames, keypointReport(camelCase),
          joints3d (flat), joints3dKeys, joints3dFrames, coordDim, space, pipelineVersion, reprocessedAt

      _write_versioned(fs_client, motion_id: str, payload: dict) -> str:
        - Firestore 경로: reference/{motion_id}/versions/phase4_v1 에 payload 저장.
        - 반환값: 저장된 문서 경로 문자열.
        - 직접 active 포인터를 건드리지 않음 — flip은 별도 단계.

      _flip_active_pointer(fs_client, motion_ids: list[str], completed: dict) -> None:
        - completed dict에 motion_ids 전체가 포함될 때만 (len(completed) == len(motion_ids)) 진행.
        - 그렇지 않으면 raise ValueError(f"active flip 차단: {len(completed)}/{len(motion_ids)} 완료").
        - 각 motion_id 에 대해: (a) flip 전 현재 top-level 필드 스냅샷을 reference/{motion_id}/versions/pre_phase4 에 백업 (rollback 소스), (b) reference/{motion_id}.activeVersion = "phase4_v1" 업데이트, (c) **top-level mirror (BLOCKER-2 핵심)**: versions/phase4_v1 의 consumer 소비 필드(angles / anglesJointKeys / anglesFrames / joints3d / joints3dKeys / joints3dFrames / coordDim / space 등)를 reference/{motion_id} top-level 에 merge. 현재 앱/백엔드 consumer 는 activeVersion resolver 없이 top-level 을 직접 읽음(referenceMotions.ts `collection(db,'reference')` + firestore_admin `reference/{id}`) — mirror 없으면 activeVersion 만 바꿔도 무효과.
        - flip 전 이전 activeVersion + top-level 스냅샷을 로컬에 기록(출력) → rollback 스크립트 참조용.

      main():
        - --out Path (default /workspace/reference-phase4-reprocess.json)
        - --motions 부분 재처리 지원 (단일 영상 디버그용)
        - --target-fps float (default 18.0)
        - --dry-run flag: S3 download 및 Firestore 쓰기 생략, _validate_payload_schema 만 실행
          (synthetic fixture 기반이 아닌 실제 --dry-run 경로 — schema 구조 검증용).
        - --no-flip flag: 재처리+버전 쓰기는 하되 active 포인터 flip 생략 (단계적 배포용).
        - 순서: 1) 전체 _reprocess_one 실행 → results dict 수집,
                 2) 전체 _validate_payload_schema 통과 (5개 전부 — 1개 실패 시 중단),
                 3) _write_versioned (각 motion에 versions/phase4_v1 저장),
                 4) _flip_active_pointer (--no-flip 없으면 실행).
        - 부분 실패 허용 단계: step 1(reprocess) 만. step 2(schema gate) 이후는 전체 통과 필수.
        - 완료 후 belle 안내 카피 출력: "재처리 완료 → versions/phase4_v1 저장 → active flip 완료. rollback 필요 시 rollback_reference_motions_phase4.py 사용."

    rollback_reference_motions_phase4.py:
      - argparse: --to-version (기본 "pre_phase4") — 롤백 대상 버전.
      - --motions (nargs="+" 선택적, 기본 MOTION_IDS 전체).
      - MOTION_IDS 공유 (import 또는 상수 복사).
      - 각 motion_id 에 대해: (a) reference/{motion_id}.activeVersion = --to-version 업데이트, (b) **top-level 복원**: reference/{motion_id}/versions/{to_version} (기본 pre_phase4 스냅샷) 의 consumer 필드 = angles/anglesJointKeys/anglesFrames + **joints3d/joints3dKeys/joints3dFrames/coordDim/space** (flip 의 top-level mirror 와 **동일 필드 세트**, W3) 를 reference/{motion_id} top-level 에 merge. activeVersion 만 되돌리면 top-level mirror 가 phase4 값으로 남아 consumer/3D viewer 가 여전히 새 값을 봄 (BLOCKER-2 대칭).
      - 실행 전 현재 activeVersion + top-level 핵심 필드를 출력해 사용자가 확인 가능하도록 함.
      - 완료 후: "rollback 완료. activeVersion → {to_version} + top-level mirror 복원" 출력.
  </behavior>

  <action>
    1. backend/tests/phase04/test_reprocess_reference_g4.py 신규 작성:
       파일 상단 주석: "Phase 4 Wave 5 — 정은지 5영상 재처리 G4 가드 behavioral 테스트 + dry-run schema 검증 + versioned write 검증. RunPod 불필요 (pure Python 로직 검증)."
       4개 테스트 작성 (behavior 대로):
         - test_g4_guard_no_synthesis_for_reference_reprocess: FakeSynthesisAdapter 주입 → 호출 없음 행위 증명.
         - test_dry_run_schema_validation_all_5: synthetic fixture로 5개 motionId payload schema 검증.
         - test_versioned_write_path_uses_versions_subpath: FakeFirestoreClient 주입 → write path == "reference/{id}/versions/phase4_v1" 정확 일치 + "referenceMotions" 미등장 + flip 전 top-level write 0 단언 (HIGH-2 — AST 검색 폐기, behavioral path assertion).
         - test_active_pointer_flip_requires_all_5: 4개 미만 완료 시 flip 차단 확인.
       importlib.util.spec_from_file_location 으로 reprocess 스크립트 import.
       from __future__ import annotations, import ast, pytest, pathlib.Path 사용.

    2. backend/scripts/reprocess_reference_motions_phase4.py 신규 작성:
       모듈 docstring: "Phase 4 정은지 5영상 재처리 스크립트 (RunPod GPU 전용).
       Phase 4 신규 파이프라인(Wave 1 + Wave 3) 완성 후 reference 5영상을 동일 파이프라인으로
       재처리해 mode1 비교의 양쪽 원터필 보장(D-09). G4 가드 정합: is_reference=True 이므로
       합성 트리거 발동 없음 — RTMW 1차 분석 품질 향상만 적용(D-10).
       versioned/atomic write: reference/{id}/versions/phase4_v1 에 버전 저장 후
       5개 전부 schema gate 통과 시에만 active 포인터 flip. rollback: rollback_reference_motions_phase4.py.

       Usage (RunPod GPU):
         cd /workspace/SunityMotion/backend
         source pod.env
         export PYTHONPATH=$PWD:$PWD/shared/python
         python scripts/reprocess_reference_motions_phase4.py --out /workspace/reference-phase4-reprocess.json

       Output: versioned Firestore 저장 + 재처리 JSON (seed-reference-motions.mjs 투입용)."

       from __future__ import annotations. import 순서: stdlib → boto3 → sunity_shared.
       sys.path 주입 패턴: extract_reference_keypoint_reports.py:39-41 그대로.
       MOTION_IDS 5개, S3_BUCKET, S3_PREFIX, PIPELINE_VERSION = "phase4_v1" 상수.
       _VERTICAL_POLE_AXIS: extract_reference_angles.py:82-87 정합.
       함수: _validate_payload_schema, _reprocess_one (synthesis_adapter=None 파라미터 포함),
             _write_versioned, _flip_active_pointer, main().
       is_reference=True 주석 박제 (합성 트리거 발동 0 보장 설명) — _reprocess_one 내부.
       --dry-run 시 _validate_payload_schema 만 실행, Firestore/S3 write 0.
       --no-flip 시 _write_versioned 까지만, _flip_active_pointer 생략.

    3. backend/scripts/rollback_reference_motions_phase4.py 신규 작성:
       모듈 docstring: "Phase 4 정은지 5영상 재처리 rollback 스크립트.
       reprocess_reference_motions_phase4.py 가 active pointer 를 phase4_v1 로 flip 한 뒤
       문제 발생 시 이전 버전으로 되돌리기 위해 사용.
       Usage: python scripts/rollback_reference_motions_phase4.py --to-version pre_phase4"
       MOTION_IDS 상수 (reprocess 스크립트와 동기 유지).
       --to-version (default "pre_phase4"), --motions (nargs="+").
       flip 전 현재 activeVersion 출력 → 사용자 확인 후 실행.

    Commit: 단일 atomic commit "feat(04-05): reprocess + rollback 스크립트 + G4 behavioral 테스트 + versioned write (POSE-03 D-09/D-10)"
  </action>

  <verify>
    <automated>
      cd /Users/kimtaesung/Dev/SunityMotion &&
      python -m pytest backend/tests/phase04/test_reprocess_reference_g4.py -x -q 2>&1 | tail -15
    </automated>
  </verify>

  <acceptance_criteria>
    - test_reprocess_reference_g4.py 4개 테스트 모두 GREEN (RunPod 불필요):
      test_g4_guard_no_synthesis_for_reference_reprocess — FakeSynthesisAdapter 주입 시 호출 0 (행위 증명)
      test_dry_run_schema_validation_all_5 — 5개 motionId payload schema 전부 통과
      test_versioned_write_path_uses_versions_subpath — FakeFirestoreClient write path == reference/{id}/versions/phase4_v1 정확 일치 + referenceMotions 미등장 + flip 전 0 write (HIGH-2, AST 검색 폐기)
      test_active_pointer_flip_requires_all_5 — 4개 미만 완료 시 flip 차단 확인
    - python -m pytest backend/tests/phase04/ -x -q — 기존 phase04 suite 회귀 0 (새 테스트 포함 모두 GREEN)
    - python -c "import sys; sys.path.insert(0,'backend/scripts'); sys.path.insert(0,'backend/shared/python'); import reprocess_reference_motions_phase4; print(len(reprocess_reference_motions_phase4.MOTION_IDS))" → 5
    - grep -c "referenceMotions" backend/scripts/reprocess_reference_motions_phase4.py → 0 (canonical reference collection 만 사용, HIGH-2/HIGH-3)
    - rollback_reference_motions_phase4.py 존재 + MOTION_IDS 길이 5 확인
  </acceptance_criteria>

  <done>reprocess + rollback 스크립트 작성 완료 + G4 가드 behavioral 테스트 4개 GREEN (FakeSynthesisAdapter 행위 증명 포함) + versioned/atomic write 구조 + phase04 suite 회귀 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: test_evaluate_4way.py — RunPod 통합 테스트 append (04-03 신규 파일 수정)</name>
  <files>
    backend/tests/phase04/test_evaluate_4way.py
  </files>

  <read_first>
    필수:
    1. backend/tests/phase04/test_evaluate_4way.py (전체 — 04-03 에서 신규 작성된 파일) — 기존 내용 확인 후 append 할 위치 파악 (파일 말미에 RunPod 통합 테스트 추가)
    2. .planning/spikes/001-dataset-eval-harness/metrics.py (전체) — evaluate_4way 함수 + PathOutput dataclass + axis_b_occlusion_reduction_score 시그니처
    3. .planning/spikes/001-dataset-eval-harness/ipsf_criteria.py (전체) — ALL_CRITERIA + PHASE4_EVAL_AXIS_MAPPING + PRESENTATION GeometricCriterion
    4. .planning/spikes/001-dataset-eval-harness/harness.py (전체, 100줄) — make_synthetic_path fixture 패턴 + 4-way 비교 호출 예시
    5. .planning/spikes/001-dataset-eval-harness/dataset.py — JEONGEUNJI_5 fixture (5영상 메타 데이터)
    6. 04-RESEARCH.md §Validation Architecture §POSE-03-g — "evaluate_4way RunPod 필요 시 xfail 마킹, synthetic fixture 로 harness 동작 검증은 local pass"
    7. 04-VALIDATION.md §Per-Task Verification Map POSE-03-g — "integration, RunPod 필요"
    8. 04-CONTEXT.md D-10, D-22 — baseline -7.60pts/video, cylindrical mesh path > baseline 조건
    9. backend/tests/phase04/conftest.py — synthetic joint fixture 재사용 패턴 (Wave 0 산출물 — 읽어 확인)
  </read_first>

  <behavior>
    test_evaluate_4way.py 에 append 할 내용 (04-03 에서 신규 작성된 파일에 추가 — 재신설 금지):

    test_evaluate_4way_harness_smoke 와 test_cylindrical_mesh_no_smplx_import 등은
    04-03 에서 이미 작성됨. 이 태스크는 RunPod 전용 통합 테스트 2개를 파일 말미에 append 한다.

    _runpod_available() 헬퍼 (파일에 없으면 추가, 이미 있으면 재사용):
      os.environ.get("RUNPOD_ANALYZE_URL") 이 설정된 경우 True (Pod 실행 환경 감지).

    @pytest.mark.runpod
    test_evaluate_4way_harness_smoke_60f (non-skip, synthetic fixture):
      - conftest.py 의 joint_seq_60f fixture 재사용 (T=60 버전 명시적 검증 — 04-03 test_axis_b 와 별도).
      - PathOutput 3개 생성 (rtmw_mirror / gemini_view / cylindrical_mesh).
      - evaluate_4way(outputs, split_frame_indices=[10,20,30,40,50]) 호출 성공.
      - results dict 에 3개 path key 포함 → GREEN.
      - 이미 04-03 에 유사 smoke 있더라도, 60f fixture 명시 경로로 추가 검증.

    @pytest.mark.runpod
    test_evaluate_4way_reprocess_vs_baseline (RunPod 전용, xfail on local):
      - IPSF D-22 baseline: rtmw_mirror path = -7.60pts/video (occlusion 감점 시뮬).
      - reprocess_reference_motions_phase4.py --dry-run 으로 스크립트 실행 경로 검증 후,
        실 RunPod 실행 시에는 5영상 재처리 결과 PathOutput 로 wrap.
      - evaluate_4way 호출 → axis_b: cylindrical_mesh path axis_b score >= baseline axis_b score.
        (G4 악화 0 조건 — D-10: axis_b 감점이 baseline 보다 나빠지면 FAIL)
      - pytest.mark.xfail(strict=False) 로 마킹 (RunPod 없는 CI 에서 XFAIL 허용).
      - pytest.mark.skipif(not _runpod_available(), reason="RunPod 미연결") 로 local skip.
  </behavior>

  <action>
    1. backend/tests/phase04/test_evaluate_4way.py 읽기 (04-03 에서 신규 작성된 기존 파일 확인 필수).
       파일 말미에 RunPod 통합 테스트 2개를 append:
       - test_evaluate_4way_harness_smoke_60f
       - test_evaluate_4way_reprocess_vs_baseline

       파일 상단에 이미 "04-03 신규 작성" 주석이 있으면 아래에 다음 주석 1줄 삽입:
       "# 04-05 append: RunPod 통합 테스트 (test_evaluate_4way_harness_smoke_60f / test_evaluate_4way_reprocess_vs_baseline)"

       _runpod_available() 가 파일에 없으면 append 앞에 정의. 이미 있으면 재사용.

       sys.path 주입 (이미 파일 상단에 있으면 중복 추가 금지):
       spikes/001-dataset-eval-harness/ 경로. 기존 패턴 유지.

    2. test_evaluate_4way_harness_smoke_60f: joint_seq_60f fixture 파라미터 사용
       (conftest.py 의 04-00 신설 fixture). split_frame_indices=[10,20,30,40,50] (T=60 정합).

    3. test_evaluate_4way_reprocess_vs_baseline:
       @pytest.mark.xfail(strict=False, reason="RunPod GPU 필요 — local XFAIL 허용")
       @pytest.mark.skipif(not _runpod_available(), reason="RunPod 미연결")
       두 마킹 모두 적용 (local = skip, RunPod = 실행).
       RunPod 실행 흐름: 재처리 스크립트 --dry-run 후 실 PathOutput 으로 evaluate_4way 호출.
       axis_b assert: cylindrical_mesh_result["axis_b"] >= baseline_result["axis_b"] (nan 은 skip).
       D-10 + D-22 참조 주석 박제.

    Commit: "test(04-05): evaluate_4way RunPod 통합 테스트 append (D-10 G4 비악화 검증)"
  </action>

  <verify>
    <automated>
      cd /Users/kimtaesung/Dev/SunityMotion &&
      python -m pytest backend/tests/phase04/test_evaluate_4way.py -x -q -m "not runpod" 2>&1 | tail -15 &&
      python -m pytest backend/tests/phase04/ -q 2>&1 | tail -5
    </automated>
  </verify>

  <acceptance_criteria>
    - test_evaluate_4way_harness_smoke_60f GREEN (joint_seq_60f fixture, RunPod 불필요)
    - test_evaluate_4way_reprocess_vs_baseline SKIPPED 또는 XFAIL (RunPod 미연결 시 정상)
    - backend/tests/phase04/ 전체 suite: 기존 GREEN 테스트 (04-03 smoke gate 포함) 회귀 0
    - python -c "import ast; from pathlib import Path; src=Path('backend/tests/phase04/test_evaluate_4way.py').read_text(); assert 'evaluate_4way' in src and 'axis_b' in src and 'RUNPOD_ANALYZE_URL' in src and 'joint_seq_60f' in src"
  </acceptance_criteria>

  <done>test_evaluate_4way.py 에 RunPod 통합 테스트 2개 append 완료 (04-05 수정, 04-03 신규 내용 보존) + RunPod 없는 환경에서 SKIP/XFAIL + phase04 suite 전체 회귀 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Task 1+2 완료 후: reprocess_reference_motions_phase4.py 가 작성됐고,
    RunPod Pod 에서 실행해 5영상(ref-sideway-spin/ref-climb/ref-invert/ref-foxtop/ref-foxtop-split)을
    Phase 4 파이프라인으로 재처리해야 한다. versioned/atomic write 순서:
    (1) 5개 전부 reprocess → (2) schema gate 5개 통과 → (3) versions/phase4_v1 저장 → (4) active pointer flip.
    재처리 결과 JSON 을 belle 가 확인하고 시각적으로 점수 비악화 여부를 검증한다.
    문제 발생 시 rollback_reference_motions_phase4.py 로 즉시 되돌릴 수 있다.
  </what-built>
  <how-to-verify>
    RunPod Pod 기동 후 실행 (Claude 가 SSH 실행):

    1. GitHub push 확인:
       git log --oneline -3 — 최신 커밋 2개(Task 1+2) 확인.
       Pod 에서 git pull origin main.

    2. 스크립트 실행 (versioned/atomic write):
       cd /workspace/SunityMotion/backend
       source pod.env
       export PYTHONPATH=$PWD:$PWD/shared/python
       python scripts/reprocess_reference_motions_phase4.py \
         --out /workspace/reference-phase4-reprocess.json \
         --target-fps 18.0

       순서 확인: (1) 5개 reprocess → (2) schema gate → (3) versions/phase4_v1 write → (4) active flip.

    3. evaluate_4way 통합 테스트 실행 (RunPod 에서):
       export RUNPOD_ANALYZE_URL=http://localhost:8000
       cd /workspace/SunityMotion
       python -m pytest backend/tests/phase04/test_evaluate_4way.py -x -q 2>&1 | tail -20

    4. 재처리 JSON 확인 (Claude 가 로컬로 가져옴):
       scp pod:/workspace/reference-phase4-reprocess.json /tmp/reference-phase4-reprocess.json
       - 5개 motionId 모두 존재하는지 확인
       - pipelineVersion: "phase4_v1" 필드 존재 여부 확인
       - angles 각 배열 길이가 이전 reference-angles.json 과 비교해 합리적인지 확인

    5. 활성화 전 old/new self-test: mode1 smoke metric 비교 (active flip 전후 점수 확인).

    6. belle 시각 검증:
       재처리 결과 JSON 을 seed-reference-motions.mjs staging 환경에 투입 후
       mode1 결과 화면에서 정은지 기준 점수가 Phase 4 이전보다 낮아지지 않는지 확인.
       특히 ref-sideway-spin (G4 occlusion FP 대상) 의 score 변화 확인.
       검증 범위 제약 (W3): joints3d 3D 뷰어 시각 확인은 04-02 완료 후에만 가능. 04-02 미완 시 본 checkpoint 는 angles/점수 비악화 + pipelineVersion="phase4_v1" 확인으로 범위 제한 (joints3d 필드 자체는 04-01 산출이므로 저장 여부는 Firestore doc 으로 확인 가능, 시각 렌더만 04-02 의존).

    7. 롤백 경로 확인 (문제 발생 시):
       python scripts/rollback_reference_motions_phase4.py --to-version pre_phase4
  </how-to-verify>
  <resume-signal>
    아래 중 하나를 입력하세요:
    - "approved" + evaluate_4way axis_b 비교 결과 (예: "cylindrical_mesh axis_b >= baseline: pass")
    - "approved" + belle 시각 확인 결과 (점수 비악화 확인) + versioned write 완료 (versions/phase4_v1 저장 + active flip 완료)
    - 문제 발생 시 구체적 에러 메시지 또는 재처리 실패 motionId (rollback 경로 사용 여부 포함)
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RunPod Pod → S3 | boto3 AWS 자격증명이 Pod env 에 평문 주입됨 — pod.env 통해 주입, 로그 출력 금지 |
| RunPod Pod → Firestore | Firebase SA 키가 Pod env (FIREBASE_SA_JSON / FIREBASE_SA_PATH) 에 주입됨 |
| reprocess 결과 JSON → Firestore versioned write | versions/phase4_v1 쓰기 후 active pointer flip 전까지 mode1 사용자는 이전 버전 읽음. flip 순간 onSnapshot 트리거 — 레이스 컨디션은 Firestore 원자적 update 로 완화. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-W5-01 | Information Disclosure | pod.env 에서 AWS/Firebase 키 로그 출력 | mitigate | 스크립트 내 print/log 에서 env var 값 직접 출력 금지. log.info 에 키 마스킹 — extract_reference_angles.py:180-185 패턴 정합. |
| T-04-W5-02 | Tampering | G4 guard bypass — is_reference=True 없이 합성 트리거 발동 | mitigate | _reprocess_one 에 synthesis_adapter=None 기본값 + is_reference=True 주석 박제. test_g4_guard_no_synthesis_for_reference_reprocess (FakeSynthesisAdapter 행위 테스트) 가 회귀 게이트. G4 악화 = FAIL 조건 (D-10). |
| T-04-W5-03 | Tampering | 5개 미통과 상태에서 active pointer flip 발생 → mode1 오염 | mitigate | _flip_active_pointer: len(completed) == len(MOTION_IDS) 강제 검사. 미통과 시 ValueError raise. test_active_pointer_flip_requires_all_5 가 회귀 게이트. 분析 정확도 최우선(D-09). |
| T-04-W5-04 | Elevation of Privilege | S3 presigned URL 없이 reference 영상 직접 접근 | accept | Pod 내부에서 boto3 S3 client 직접 사용 (IAM 권한 sunity-motion 키 범위). 사용자 데이터 아님 — reference 공개 영상. |
| T-04-W5-05 | Tampering | rollback 스크립트가 잘못된 버전으로 activeVersion flip | mitigate | rollback 실행 전 현재 activeVersion 출력 + 사용자 확인. --to-version 명시 필수 (기본값 "pre_phase4" 안전 fallback). |
| T-04-W5-SC | Tampering | pip installs on Pod (신규 패키지 없음) | accept | reprocess 스크립트 신규 외부 패키지 없음 (boto3 + sunity_shared 재사용). Phase 3 cylindrical mesh requirements.txt 이미 검증됨. |
</threat_model>

<verification>
pytest backend/tests/phase04/test_reprocess_reference_g4.py -x -q → 4개 GREEN (G4 behavioral + dry-run schema + versioned write + flip gate)
pytest backend/tests/phase04/test_evaluate_4way.py -x -q -m "not runpod" → GREEN (synthetic fixture + 60f smoke)
pytest backend/tests/phase04/ -q → 전체 suite 회귀 0
RunPod 실행 후: test_evaluate_4way_reprocess_vs_baseline — cylindrical_mesh axis_b >= baseline axis_b (D-10 G4 악화 0)
versioned write 확인: Firestore reference/{id}/versions/phase4_v1 5개 저장 + active pointer flip
belle 시각 검증: ref-sideway-spin 재처리 점수 >= Phase 4 이전 점수
rollback 경로 확인: rollback_reference_motions_phase4.py 존재 + 실행 가능
</verification>

<success_criteria>
Wave 5 완료 기준 (ROADMAP SC #7 정은지 5영상 재처리 + D-10 G4 악화 0):
1. reprocess_reference_motions_phase4.py 작성 완료 — MOTION_IDS 5개, is_reference=True 게이트(행위 테스트로 증명), pipelineVersion 필드, versioned/atomic write (_validate_payload_schema → _write_versioned → _flip_active_pointer 순서 강제)
2. rollback_reference_motions_phase4.py 작성 완료 — activeVersion 이전 버전 복원 가능
3. test_reprocess_reference_g4.py 4개 GREEN — G4 behavioral(FakeSynthesisAdapter) + dry-run schema 5개 + versioned write ast 검증 + flip gate
4. test_evaluate_4way_harness_smoke_60f GREEN (joint_seq_60f fixture, RunPod 불필요)
5. RunPod 실행 후: 5영상 재처리 JSON 생성 완료 + versions/phase4_v1 저장 + active flip + evaluate_4way axis_b: cylindrical_mesh >= baseline
6. belle 시각 검증 PASS: ref-sideway-spin 재처리 후 점수 비악화 확인
7. phase04 전체 test suite 회귀 0
</success_criteria>

<output>
Create `.planning/phases/04-ux-occlusion-confidence/04-05-SUMMARY.md` when done
</output>
