---
phase: 05-gemini
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/research/spikes/measure_eunji_reference.py
  - backend/judging_data/criteria/ref-climb.yaml
  - backend/judging_data/criteria/ref-foxtop.yaml
  - backend/judging_data/criteria/ref-foxtop-split.yaml
  - backend/judging_data/criteria/ref-invert.yaml
  - backend/judging_data/criteria/ref-sideway-spin.yaml
  - backend/research/evaluations/reports/eunji_reference_measurements/measurements.json
autonomous: false
requirements:
  - SCORE-01
user_setup:
  - service: aws-s3
    why: 정은지 reference 영상 5개 (ref-climb / ref-foxtop / ref-foxtop-split / ref-invert / ref-sideway-spin) S3 박제 정합 확인
    env_vars:
      - name: AWS_ACCESS_KEY_ID
        source: 박제된 sunity-motion 키 (memory aws-keys-and-bucket)
      - name: AWS_SECRET_ACCESS_KEY
        source: 박제된 sunity-motion 키
      - name: AWS_DEFAULT_REGION
        source: ap-northeast-2
    dashboard_config: []

must_haves:
  truths:
    - "5영상 정은지 reference 영상의 hold moment frame 별 6관절 angle 측정값이 JSON 으로 박제되어 있다 (left/right shoulder/hip/knee)"
    - "yaml 5개 (ref-climb / ref-foxtop / ref-foxtop-split / ref-invert / ref-sideway-spin) 의 hold_moment angle_target / tolerance_full / minimum_requirement / source_ref 가 정은지 reference 측정값 박제로 갱신되어 있다"
    - "yaml 5개 의 source_ref 가 모두 '정은지 reference 측정값 (분기 2 path) — IPSF source 박제 X' 로 변경되어 있다 (NotebookLM lookup 2026-06-04 결과 박제 정합)"
    - "ref-climb yaml 의 hold_moment 는 빈 list 유지 + IPSF Climbs 카테고리 = angle 채점 X + D-20 (이동 횟수 차원 = 별 phase) 주석 박제 + source_ref 코멘트 'IPSF Climbs 카테고리 = angle 차원 X, D-20 별 phase 책임 — Plan 5-05 게이트 산정 시 out-of-scope PASS counted' 명시 (B1 fix 2026-06-04 revision)"
    - "ref-invert yaml 의 hold_moment 는 6관절 angle 박제 (정은지 측정값) + D-19 (Body Position 차원 = 별 phase) 주석 박제"
    - "belle 가 측정 결과 + tolerance/minimum 룰 + yaml 갱신 내용을 검토하고 승인했다 (blocking-human checkpoint)"
  artifacts:
    - path: backend/research/spikes/measure_eunji_reference.py
      provides: 정은지 reference 영상 5개 S3 다운로드 + 프레임 추출 + RTMW pose 산출 + hold_window 측정값 JSON 출력 spike
      contains: "def main"
      min_lines: 120
    - path: backend/research/evaluations/reports/eunji_reference_measurements/measurements.json
      provides: 5영상 × 6관절 × {target, tolerance, minimum, hold_frame_indices, raw_angles_at_hold} 박제
      contains: ref-foxtop
    - path: backend/judging_data/criteria/ref-foxtop.yaml
      provides: 정은지 reference 측정값 기반 hold_moment 6관절 criteria
      contains: "정은지 reference 측정값"
    - path: backend/judging_data/criteria/ref-invert.yaml
      provides: 정은지 reference 측정값 기반 hold_moment 6관절 criteria + D-19 주석
      contains: D-19
    - path: backend/judging_data/criteria/ref-climb.yaml
      provides: hold_moment 빈 list 유지 + D-20 주석 + source_ref 변경
      contains: D-20
  key_links:
    - from: backend/research/spikes/measure_eunji_reference.py
      to: backend/shared/python/sunity_shared/analysis/pose_engines
      via: RTMW pose engine import (PoseEngine Protocol 박제)
      pattern: "pose_engines"
    - from: backend/research/spikes/measure_eunji_reference.py
      to: backend/shared/python/sunity_shared/analysis/dimensions.py
      via: hold_window 함수 호출 (frame-mean 측정)
      pattern: "hold_window"
    - from: backend/judging_data/criteria/ref-foxtop.yaml
      to: backend/research/evaluations/reports/eunji_reference_measurements/measurements.json
      via: angle_target / tolerance_full / minimum_requirement 수치 박제 source
      pattern: "정은지 reference 측정값"
---

<objective>
**Plan 5-00 (선행 필수)** — yaml source 정은지 reference 측정값 정정 작업.

Purpose: NotebookLM IPSF Code of Points 2024-2025 lookup (2026-06-04, `05-IPSF-LOOKUP.md` 박제) 결과 = 5영상 yaml 의 hold_moment angle_target=180° / tolerance=±20° / minimum=160° 박제는 **IPSF source 박제 X** (5영상 중 ref-climb 은 Transitions & Climbs 카테고리로 angle 채점 X, ref-invert 은 Body Position ±20° 채점만, 나머지 3영상 미등재). belle 박제 결정 (D-17) = Phase 5 첫 plan 책임으로 yaml source 를 **정은지 reference 측정값 (분기 2 path)** 으로 정정. Plan 23 sweep 재실행 게이트 = "정은지 reference 기준 5/5 PASS" (D-01) 박제 정합.

Output:
- 정은지 reference 영상 5개 × hold moment × 6관절 angle 측정값 JSON 박제
- yaml 5개 source_ref / angle_target / tolerance_full / minimum_requirement 정정
- belle 승인 checkpoint (blocking-human)

박제 정신:
- [[gap-and-line-angle-mandatory-gates.md]] "강등/우회 금지" — IPSF 우회 X, 다른 객관 기준 (정은지 reference 측정값) 박제
- [[analysis-objectivity-no-human-scores.md]] — 정은지 reference 측정값 (객관 수치) 박제 OK, "정은지가 좋다고 판단" (사람 점수) 박제 X
- [[studio-term-3branch-system.md]] 분기 2 — "한국 학원 통용 + 정은지 reference 비등재 동작" path 정합
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-gemini/05-CONTEXT.md
@.planning/phases/05-gemini/05-IPSF-LOOKUP.md
@.planning/phases/05-gemini/05-RESEARCH.md
@.planning/phases/05-gemini/05-PATTERNS.md

# Reference source files
@backend/judging_data/criteria/ref-climb.yaml
@backend/judging_data/criteria/ref-foxtop.yaml
@backend/judging_data/criteria/ref-foxtop-split.yaml
@backend/judging_data/criteria/ref-invert.yaml
@backend/judging_data/criteria/ref-sideway-spin.yaml
@backend/research/spikes/spike_gemini_moment.py
@backend/shared/python/sunity_shared/analysis/dimensions.py
@backend/shared/python/sunity_shared/analysis/skeleton.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 정은지 reference 측정 spike 신설 (S3 다운로드 + RTMW pose + hold_window 측정값 JSON 출력)</name>
  <files>backend/research/spikes/measure_eunji_reference.py</files>
  <read_first>
    - backend/research/spikes/spike_gemini_moment.py (Plan 01-13 spike — CLI 인자 / S3 다운로드 / lazy import / argparse 박제 패턴 사용)
    - backend/shared/python/sunity_shared/analysis/pose_engines/__init__.py (RTMW engine 박제 path 확인)
    - backend/shared/python/sunity_shared/analysis/dimensions.py (hold_window 함수 시그너처 확인)
    - backend/shared/python/sunity_shared/analysis/skeleton.py (JOINT_KEYS 8관절 순서 확인 — 6관절 (shoulder/hip/knee L+R) 인덱싱)
    - backend/judging_data/criteria/ref-foxtop.yaml (출력 JSON schema 가 yaml hold_moment 갱신 시 1:1 매핑 가능해야 함)
    - .planning/phases/05-gemini/05-PATTERNS.md (Plan 5-00 섹션 — S3 다운로드 + 프레임 추출 + RTMW pose 박제 패턴)
  </read_first>
  <action>
    `backend/research/spikes/measure_eunji_reference.py` 신설.

    CLI 인자 (argparse):
    - `--motion {ref-climb,ref-foxtop,ref-foxtop-split,ref-invert,ref-sideway-spin,all}` (필수, default `all`)
    - `--bucket` (S3 bucket, default `sunity-motion-pilot-videos`)
    - `--hold-timestamps-json <path>` (belle 가 미리 박제한 hold moment timestamp 박제 JSON, schema `{motion: {hold_start_sec: float, hold_end_sec: float}}`)
    - `--output <path>` (default `backend/research/evaluations/reports/eunji_reference_measurements/measurements.json`)
    - `--video-path <path>` (선택, S3 우회용 로컬 테스트)
    - `--window-size <int>` (hold_window frame 수, default = hold 지속시간 × 9 fps)

    구조 (PATTERNS.md Plan 5-00 박제 패턴 따라):
    1. `_download_s3(s3_key, bucket, dest)` — `boto3.client('s3').download_file` (lazy import)
    2. `_resolve_video(motion, video_path, bucket, tmp_dir)` — S3 key = `reference/{motion}.mp4`
    3. `_extract_frames(video_path)` — `FfmpegFrameExtractor` (9 fps / 640px, lazy import)
    4. `_compute_rtmw_angles(frames)` — RTMW pose engine 호출 → (T, 8) angles 산출 (lazy import — `sunity_shared.analysis.pose_engines` 박제 모듈 사용)
    5. `_measure_hold_window(angles, hold_start_sec, hold_end_sec, fps=9.0)` — hold 구간 frame indices 산출 (start_frame = hold_start_sec * 9, end_frame = hold_end_sec * 9) → 해당 구간 6관절 (left_shoulder/right_shoulder/left_hip/right_hip/left_knee/right_knee) mean angle 산출
    6. `_apply_tolerance_rule(measured_deg)` — D-18 박제 룰: `angle_target = measured_deg`, `tolerance_full = 15.0` (측정값 ±15° 박제), `minimum_requirement = measured_deg - 25.0` (측정값 - 25° 박제). 이 룰은 belle 검토 + 승인 시 변경 가능 — 룰 자체를 JSON 메타에 박제 (`tolerance_rule_version: "D-18-2026-06-04"`)
    7. JSON 출력 schema (motion 별):
       ```json
       {
         "motion": "ref-foxtop",
         "video_path": "s3://.../ref-foxtop.mp4",
         "hold_window": {"start_sec": 4.2, "end_sec": 7.5, "start_frame": 38, "end_frame": 67, "fps": 9.0},
         "joints": {
           "left_shoulder": {"measured_deg": 142.3, "angle_target": 142.3, "tolerance_full": 15.0, "minimum_requirement": 117.3},
           "right_shoulder": {...},
           ...
         },
         "tolerance_rule_version": "D-18-2026-06-04",
         "source": "정은지 reference 측정값 (분기 2 path) — RTMW wholebody + hold_window mean",
         "ipsf_source": null,
         "ipsf_source_note": "NotebookLM IPSF Code of Points 2024-2025 lookup 2026-06-04 결과 IPSF source 박제 X (05-IPSF-LOOKUP.md)",
         "measured_at_utc": "2026-06-04T..."
       }
       ```
    8. ref-climb 특수 처리: hold_window 측정 skip, JSON 에 `{"motion": "ref-climb", "joints": {}, "ipsf_source": "IPSF Transitions & Climbs", "ipsf_source_note": "Climbs 카테고리 = 이동 횟수 채점 (D-20: 별 phase), angle 채점 X — yaml hold_moment 빈 list 유지"}` 박제

    박제 정신 (D-16): `boto3` / `imageio` / `mmpose` / `torch` 는 함수 내부 lazy import. 모듈 로드 시 0 import.

    main() = motion `all` 시 5영상 loop, 결과 dict 합쳐서 JSON 한 파일에 박제. 로그는 `log.info` 한국어 + spec 인용 (CLAUDE.md cross-cutting 박제).

    **belle 가 hold timestamp 박제 JSON 을 미리 작성** — 본 task 의 spike 는 hold timestamp 가 박제된 상태에서 시작. hold timestamp 박제 자체는 belle 수동 작업 (Task 2 checkpoint 안에서 확인).

    수행 per D-17, D-18, IPSF-LOOKUP.md (가+다) 옵션.

    **B6 fix (2026-06-04 revision)**: Task 1 verify 가 Task 2 산출물 (hold timestamps JSON) 을 요구하면 순환 의존 → Task 1 verify 는 **import + lazy 검증만**. 추가 fixture stub fallback 박제: `--hold-timestamps-json` 인자 미설정 시 영상 길이의 중간 30% 구간 (예: 10s 영상 → hold=3.5s~6.5s) 을 fallback 박제. CLI flag = `--hold-timestamps-default-fallback` 박제 (default False, Task 2 belle 작업 전 단위 검증 시점에서 True 박제).
  </action>
  <verify>
    <automated>cd backend && python -c "import sys; sys.path.insert(0, '.'); import research.spikes.measure_eunji_reference as m; assert hasattr(m, 'main'); assert 'boto3' not in sys.modules; assert 'torch' not in sys.modules; assert 'mmpose' not in sys.modules; print('import + lazy OK')"</automated>
  </verify>
  <done>
    `backend/research/spikes/measure_eunji_reference.py` 가 import 가능 (B6 fix: hold_timestamps JSON 없이도 import 단독 검증). lazy import 검증 (boto3/torch/mmpose 모듈 로드 시 미import) PASS. 실 측정 실행은 Task 2 belle Pod 단계 (hold timestamps JSON 박제 후).
  </done>
  <acceptance_criteria>
    - 파일 존재 + line count ≥ 120
    - `argparse` 5 인자 박제 (`--motion`, `--bucket`, `--hold-timestamps-json`, `--output`, `--video-path`)
    - 모듈 로드 시 `boto3` / `mmpose` / `torch` / `imageio` 미import (`grep -v '^#' backend/research/spikes/measure_eunji_reference.py | head -30 | grep -E '^(import|from) (boto3|torch|mmpose|imageio)' | wc -l` == 0)
    - 출력 JSON schema 안에 `tolerance_rule_version`, `ipsf_source`, `source`, `joints.{joint}.measured_deg/angle_target/tolerance_full/minimum_requirement` 모두 존재
    - ref-climb 특수 처리 (joints 빈 dict + ipsf_source 박제) `grep -c 'ref-climb' backend/research/spikes/measure_eunji_reference.py` ≥ 1
    - 한국어 docstring + 박제 spec 인용 (D-17 / D-18 / 05-IPSF-LOOKUP.md 인용 ≥ 3건)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: belle Pod 정은지 reference 측정 + hold timestamp 박제 + 결과 검토 checkpoint</name>
  <what-built>
    Task 1 spike 가 정은지 reference 영상 5개 × hold moment × 6관절 angle 측정값 JSON 산출. belle 가 (1) hold timestamp 박제 (5영상 × {hold_start_sec, hold_end_sec}), (2) Pod sweep 실행, (3) 측정 결과 검토 + (4) D-18 tolerance/minimum 룰 (±15° / -25°) 승인 또는 변경.
  </what-built>
  <how-to-verify>
    1. **belle 단계 (Pod 작업, 박제 [[gsd-pod-work-push-first.md]] — 로컬 commit + GitHub push 먼저)**:
       a. 로컬에서 Task 1 spike + `tmp/eunji_hold_timestamps.json` (belle 직접 박제) commit + `git push origin main`
       b. Pod 에서 `cd /workspace/SunityMotion && git pull`
       c. Pod 에서 RTMW 환경 사전 박제 확인 (Plan 11/23 박제 — torch / mmpose / mmcv 이미 설치됨, `runpod-gpu-env.md` 참조)
       d. Pod 에서 `cd backend && python -m research.spikes.measure_eunji_reference --motion all --hold-timestamps-json /workspace/eunji_hold_timestamps.json --output /workspace/SunityMotion/backend/research/evaluations/reports/eunji_reference_measurements/measurements.json`
       e. 실행 결과 (5영상 × 6관절) 검토:
          - 측정값 인체학적 정합 (예: 정은지 ref-foxtop right_shoulder = 142° 같은 굽힘 각도 측정 정합? right_shoulder=18° 같은 비정상 값 0건?)
          - left/right 좌우 대칭 차이 ≤ 30° (자세 자체가 비대칭이면 인정, 측정 오류로 인한 차이는 거부)
          - ref-climb 의 joints 가 빈 dict 박제 정합 (IPSF Climbs 카테고리 박제 정합)
       f. Pod 에서 `git add backend/research/evaluations/reports/eunji_reference_measurements/measurements.json && git commit -m "data(05-00): 정은지 reference 측정값 5영상 박제" && git push origin main`
       g. 로컬에서 `git pull` 받은 후 측정 결과 belle 가 검토

    2. **belle 결정 항목 (REPLY 형식)**:
       - **(A) 측정값 승인** — yes / no (no 일 시 root cause = "재측정 필요" / "hold timestamp 박제 변경 필요" / "RTMW pose 정확도 의심" 박제)
       - **(B) tolerance 룰** — D-18 박제 (±15°) 유지 / 변경 (변경 시 새 값 박제)
       - **(C) minimum 룰** — D-18 박제 (-25°) 유지 / 변경 (변경 시 새 값 박제)
       - **(D) ref-invert Body Position 차원** — D-19 박제 (별 phase) 유지 OK?
       - **(E) ref-climb 이동 횟수 차원** — D-20 박제 (별 phase) 유지 OK?

    3. **승인 후** = Task 3 진입 (yaml 5개 정정). 거부 시 = root cause 박제 후 plan 재실행 또는 별 plan 분리.
  </how-to-verify>
  <resume-signal>belle 가 "측정값 승인 (A=yes, B=±15° / C=-25° / D=OK / E=OK)" 또는 변경 사항 박제 후 입력</resume-signal>
  <acceptance_criteria>
    - belle 의 (A)~(E) 항목 박제 답변 모두 박제됨 (서면 박제)
    - measurements.json git 커밋 + push 완료
    - belle 가 (A) yes 응답 시 — Task 3 진입 권한 박제
    - **I1 박제 (2026-06-04 revision)**: D-18 tolerance/minimum 룰 변경 시 (B/C 항목 답변에서 박제 변경 박제 박제) → `python -m research.spikes.measure_eunji_reference --motion all --hold-timestamps-json /workspace/eunji_hold_timestamps.json --output /workspace/SunityMotion/backend/research/evaluations/reports/eunji_reference_measurements/measurements.json` 재실행 필수 (tolerance_rule_version 값 박제 갱신 + measurements.json 재생성). 재실행 후 git commit + push 반복 박제.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: yaml 5개 source 정정 (정은지 reference 측정값 박제 + source_ref 변경 + D-19/D-20 주석)</name>
  <files>
    backend/judging_data/criteria/ref-climb.yaml,
    backend/judging_data/criteria/ref-foxtop.yaml,
    backend/judging_data/criteria/ref-foxtop-split.yaml,
    backend/judging_data/criteria/ref-invert.yaml,
    backend/judging_data/criteria/ref-sideway-spin.yaml
  </files>
  <read_first>
    - backend/research/evaluations/reports/eunji_reference_measurements/measurements.json (Task 2 산출물, 5영상 측정값 source)
    - backend/judging_data/criteria/ref-climb.yaml (현재 박제 — `source:` 헤더 + `hold_moment: []` 유지 패턴)
    - backend/judging_data/criteria/ref-foxtop.yaml (현재 박제 — `hold_moment` 6관절 list + `source_ref` per-joint 패턴)
    - backend/judging_data/criteria/ref-invert.yaml (현재 박제 — 가장 완성된 yaml, 6관절 박제 + source_ref 풍부)
    - backend/judging_data/README.md (yaml 작성 가이드)
    - .planning/phases/05-gemini/05-IPSF-LOOKUP.md (5영상 × IPSF 등재/미등재 + 정정 path)
    - .planning/phases/05-gemini/05-PATTERNS.md (Plan 5-00 yaml 박제 변경 후 예시)
  </read_first>
  <action>
    measurements.json 의 5영상 × 6관절 측정값을 yaml 5개에 박제. 변경 규칙 (D-17 + IPSF-LOOKUP.md (가+다) 옵션):

    **공통 변경 (5개 yaml)**:
    - `source:` 헤더 = `"정은지 reference 측정값 (분기 2 path — studio-term-3branch-system) — NotebookLM IPSF Code of Points 2024-2025 lookup 2026-06-04 결과 IPSF source 박제 X (05-IPSF-LOOKUP.md)"`

    **ref-foxtop.yaml, ref-foxtop-split.yaml, ref-sideway-spin.yaml** (IPSF 미등재):
    - `hold_moment` 6관절 list 박제 = measurements.json `joints` 그대로 매핑
      - `angle_target`: measurements.json `measured_deg`
      - `tolerance_full`: measurements.json `tolerance_full` (D-18 = 15.0)
      - `minimum_requirement`: measurements.json `minimum_requirement` (D-18 = measured - 25)
      - `deduction_per_step`: 0.2 (기존 유지)
      - `source_ref`: `"정은지 reference 측정값 (분기 2 path) — RTMW wholebody hold_window mean = {measured:.1f}° ± {tol:.1f}° / minimum {min:.1f}° — IPSF 미등재 동작 (NotebookLM lookup 2026-06-04). 박제 룰 D-18 (tolerance=±15° / minimum=-25°)"`
    - `setup_moment`, `peak_moment`, `release_moment`: `[]` 유지 (D-05 v1 dead, D-06 v2 자동 활성)
    - 주석 헤더에 박제 추가: `# Plan 5-00 (2026-06-04): 정은지 reference 측정값 박제 — IPSF 미등재 동작 (NotebookLM lookup 05-IPSF-LOOKUP.md). yaml source_ref = 분기 2 (studio-term-3branch-system).`

    **ref-invert.yaml** (IPSF 등재 — Body Position Inverted):
    - `hold_moment` 6관절 list = measurements.json 그대로 (위와 동일 박제 패턴)
    - `source_ref` per-joint = `"정은지 reference 측정값 (분기 2 path) — RTMW wholebody hold_window mean = ... — IPSF Body Position Inverted 등재 (NotebookLM citation [4-7]) 이지만 yaml 채점 차원 (관절 angle) 은 IPSF source X — Body Position 차원 (골반-머리 상대 위치 ±20°) 은 D-19 박제 별 phase 책임"`
    - 주석 헤더에 박제: `# Plan 5-00 (2026-06-04): 정은지 reference 측정값 박제. IPSF 등재 = Body Position Inverted (관절 angle X). D-19 박제 = Body Position 차원 추가 = 별 phase (Phase 8 또는 신설).`

    **ref-climb.yaml** (IPSF 등재 — Transitions & Climbs):
    - `hold_moment: []` 유지 (의도된 빈 list, IPSF Climbs 카테고리 = 이동 횟수 채점, angle 차원 X)
    - `source:` 헤더 = `"정은지 reference 측정값 (분기 2 path) — IPSF Transitions & Climbs 등재 (NotebookLM citation [2, 3]) 이지만 hold = 이동 2회 이상 채점 = 관절 angle X. D-20 박제 = 이동 횟수 차원 추가 = 별 phase 책임"`
    - 주석 헤더에 박제: `# Plan 5-00 (2026-06-04): IPSF Transitions & Climbs 카테고리 박제. D-20 박제 = 이동 횟수 차원 = 별 phase. v1 = hold_moment 빈 list 유지 (angle 채점 X).`
    - setup/peak/release 도 모두 `[]` 유지

    **검증 path (자동)**:
    - 5개 yaml 모두 `pyyaml` 로드 가능 (syntax error 0)
    - `source:` 헤더에 `"정은지 reference 측정값"` 문자열 포함
    - ref-foxtop / ref-foxtop-split / ref-invert / ref-sideway-spin 의 hold_moment list length = 6
    - ref-climb 의 hold_moment list length = 0
    - 모든 6관절 entry 의 `source_ref` 에 `"D-18"` 또는 `"분기 2 path"` 박제 포함

    **박제 정신 (D-17 + D-18 + D-19 + D-20)**:
    - IPSF 우회 X (다른 객관 기준 = 정은지 reference 측정값 박제)
    - 사람 점수 라벨링 X (객관 수치 라벨링 OK — [[analysis-objectivity-no-human-scores]])
    - 분기 2 path 정합 — [[studio-term-3branch-system]]
  </action>
  <verify>
    <automated>cd backend && python -c "
import yaml
from pathlib import Path
files = ['ref-climb', 'ref-foxtop', 'ref-foxtop-split', 'ref-invert', 'ref-sideway-spin']
for f in files:
    p = Path(f'judging_data/criteria/{f}.yaml')
    data = yaml.safe_load(p.read_text())
    assert '정은지 reference 측정값' in data['source'], f'{f} source 헤더 누락'
    hm = data['criteria']['hold_moment']
    if f == 'ref-climb':
        assert hm == [], f'{f} hold_moment 빈 list 여야'
    else:
        assert len(hm) == 6, f'{f} hold_moment 6관절 필요'
        for j in hm:
            assert '분기 2' in j['source_ref'] or 'D-18' in j['source_ref'], f'{f} {j[\"joint\"]} source_ref 박제 누락'
print('5 yaml 박제 정합 OK')
"</automated>
  </verify>
  <done>
    5 yaml 파일 모두 정은지 reference 측정값 박제 + source_ref 변경 + D-19/D-20 주석 박제. 자동 검증 PASS.
  </done>
  <acceptance_criteria>
    - 5 yaml `source:` 헤더 모두 `정은지 reference 측정값` 문자열 포함 (5/5)
    - ref-foxtop / ref-foxtop-split / ref-invert / ref-sideway-spin 의 hold_moment = 6관절 박제 (4 × 6 = 24 entry)
    - ref-climb hold_moment = 빈 list 박제
    - 모든 24 hold_moment entry 의 source_ref 에 `D-18` 또는 `분기 2 path` 문자열 포함
    - ref-invert.yaml 주석에 `D-19` 박제, ref-climb.yaml 주석에 `D-20` 박제
    - `python -c "import yaml; ..."` 로드 시 syntax error 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local-dev → S3 bucket | 정은지 reference 영상 다운로드 — AWS 자격증명 박제 정합 필수 |
| Pod → RTMW pose engine | local model loading + GPU 실행 — Pod 내부 신뢰 boundary |
| measurements.json → yaml 5개 | 데이터 박제 → config 박제 변환 — belle 검토 단계 박제 필수 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-00-01 | Tampering | yaml 박제 source_ref 누락 | mitigate | Task 3 자동 검증 — 24 entry × source_ref `D-18` / `분기 2 path` 문자열 강제 |
| T-05-00-02 | Information Disclosure | AWS 키 노출 | mitigate | env 박제 (`.env` 하드코딩 금지, CLAUDE.md §3 박제 정합) |
| T-05-00-03 | Repudiation | belle 승인 박제 없이 yaml 변경 | mitigate | Task 2 blocking-human checkpoint — belle 서면 박제 (A/B/C/D/E) 필수 |
| T-05-00-04 | Denial of Service | Pod RTMW pose 실패 시 측정 결과 부재 | accept | Task 1 spike fail-fast — error log 박제 + retry path 별 plan |
| T-05-00-SC | Tampering | npm/pip/cargo installs | mitigate | 본 plan 신설 install 0 — Pod 환경은 Plan 11/23 박제 재사용 (boto3/torch/mmpose 기존 박제) |
</threat_model>

<verification>
- 5 yaml × source_ref 박제 정합 자동 검증 (Task 3 verify)
- measurements.json 박제 + git 커밋 + push 완료 (Task 2 belle checkpoint)
- belle 의 D-18 룰 승인 + D-19/D-20 박제 확인 (Task 2 응답)
- ref-climb 의 hold_moment 빈 list 유지 + D-20 주석 박제 정합
- ref-invert 의 D-19 주석 박제 정합
</verification>

<success_criteria>
- 5 yaml 박제 정은지 reference 측정값 source 정정 완료
- belle 박제 승인 (A=yes, B/C/D/E 박제)
- Plan 5-01 ~ 5-05 진입 권한 박제 (게이트 = "정은지 reference 기준 5/5 PASS")
</success_criteria>

<output>
Create `.planning/phases/05-gemini/05-00-SUMMARY.md` when done. Include:
- 정은지 reference 측정값 5영상 × 6관절 박제 결과 (mean / std / 좌우 차이)
- belle 박제 승인 응답 (A/B/C/D/E)
- yaml 5개 정정 박제 diff 요약
- 박제 룰 D-18 확정 또는 변경 박제
</output>
