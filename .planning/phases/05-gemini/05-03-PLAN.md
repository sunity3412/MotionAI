---
phase: 05-gemini
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified:
  - backend/functions/pipeline/app.py
  - backend/tests/test_pipeline_recognizer_switch.py
  - backend/tests/test_pipeline_gemini_integration.py
autonomous: true
requirements:
  - SCORE-01

must_haves:
  truths:
    - "pipeline/app.py 의 _RECOGNIZER 즉시 박제 → _ensure_recognizer() lazy creation 패턴 박제 (D-16 정합)"
    - "env GEMINI_RECOGNIZER_ENABLED 또는 RECOGNIZER_BACKEND=gemini 시 GeminiTechniqueRecognizer 합성 (extractor + cache + unregistered_hook 의존성 주입)"
    - "env 미설정 시 FallbackRecognizer 유지 (default = fallback, A/B 비교 가능 박제, 회귀 위험 최소)"
    - "_process 안에서 recognize(angles, frames=local_video_path) 호출 — frames 인자 추가 + tempfile 컨텍스트 확장으로 video path 살림"
    - "FallbackRecognizer 도 recognize(angles, frames=None) 시그너처 호환 (Protocol 정합) — 본 plan 에서 단위 테스트로 박제"
    - "B8 fix (2026-06-04 revision): `_angles_from_video` 시그너처 무변경 + 별 helper `_angles_and_video_path_from_video` 신설. 기존 호출처 (RunPod server.py D-12 무수정 박제 정합) 회귀 위험 0."
    - "RunPod server.py 무수정 — pipeline 모듈 import 만으로 env 기반 swap 자동 작동 (D-12 1pass 정신)"
    - "통합 테스트 — mock GeminiMomentExtractor 주입으로 _process Lambda fallback path 흐름 검증"
  artifacts:
    - path: backend/functions/pipeline/app.py
      provides: _ensure_recognizer + _gemini_enabled env switch + recognize(angles, frames) 호출 + tempfile 컨텍스트 확장
      contains: "_ensure_recognizer"
    - path: backend/tests/test_pipeline_recognizer_switch.py
      provides: env switch + lazy import + 어댑터 type + Protocol 호환 단위 테스트
      contains: "test_env_enables_gemini"
      min_lines: 100
    - path: backend/tests/test_pipeline_gemini_integration.py
      provides: _process 흐름 통합 테스트 (mock-based)
      contains: "test_process_with_gemini_recognizer"
      min_lines: 80
  key_links:
    - from: backend/functions/pipeline/app.py
      to: backend/shared/python/sunity_shared/analysis/gemini_technique_recognizer.py
      via: GeminiTechniqueRecognizer 생성 (lazy import + DI)
      pattern: "GeminiTechniqueRecognizer"
    - from: backend/functions/pipeline/app.py
      to: backend/shared/python/sunity_shared/analysis/technique_cache.py
      via: TechniqueCache 생성 + 의존성 주입
      pattern: "TechniqueCache"
    - from: backend/functions/pipeline/app.py
      to: backend/shared/python/sunity_shared/firestore_admin.py
      via: record_unregistered_keyword (D-09 case 3 hook 박제)
      pattern: "record_unregistered_keyword"
---

<objective>
**Plan 5-03** — pipeline `_RECOGNIZER` swap + env switch + `_process` 흐름 wiring.

Purpose: Plan 5-01 (GeminiTechniqueRecognizer) + Plan 5-02 (TechniqueCache + firestore_admin helper) 박제를 pipeline 본체에 wiring. env switch 박제로 A/B 비교 가능. RunPod server.py 무수정 — pipeline 모듈 import 만으로 자동 작동 (D-12 박제 정신).

Output:
- `_RECOGNIZER` 즉시 박제 → `_ensure_recognizer()` lazy creation 패턴
- env switch 박제 (default = fallback, A/B 비교 가능)
- `_process` 안에서 `profile = recognizer.recognize(angles, frames=local_video_path)` 호출 + tempfile 컨텍스트 확장
- 단위 테스트 + 통합 테스트

박제 정신:
- D-12: RunPod Pod 안 1pass — server.py 무수정, pipeline 모듈 1점 swap
- D-16: lazy import — 모듈 로드 시 google-genai / boto3 / firebase-admin 미import
- 박제 보존 — env 미설정 시 FallbackRecognizer 유지 (회귀 위험 최소, A/B 비교 가능)
- [[gsd-pod-work-push-first]] — pipeline 변경 후 commit + push + Pod git pull 단위
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-gemini/05-CONTEXT.md
@.planning/phases/05-gemini/05-RESEARCH.md
@.planning/phases/05-gemini/05-PATTERNS.md
@.planning/phases/05-gemini/05-01-SUMMARY.md
@.planning/phases/05-gemini/05-02-SUMMARY.md

# Source files
@backend/functions/pipeline/app.py
@backend/runpod_inference/server.py
@backend/shared/python/sunity_shared/analysis/technique.py
@backend/shared/python/sunity_shared/analysis/gemini_technique_recognizer.py
@backend/shared/python/sunity_shared/analysis/technique_cache.py
@backend/tests/test_pipeline_dispatch.py
</context>

<tasks>

<task type="tdd">
  <name>Task 1: pipeline/app.py _RECOGNIZER swap + env switch + recognize(angles, frames) wiring</name>
  <files>backend/functions/pipeline/app.py, backend/tests/test_pipeline_recognizer_switch.py</files>
  <read_first>
    - backend/functions/pipeline/app.py (현재 박제 — line 120 `_RECOGNIZER` 즉시 생성, line 230 `recognize(angles)` 호출, lines 123-135 `_ensure_adapters` lazy 패턴, lines 71-78 `_runpod_enabled` env switch 패턴, lines 146-153 `_angles_from_video` tempfile 컨텍스트)
    - backend/shared/python/sunity_shared/analysis/technique.py (Protocol + FallbackRecognizer 박제, recognize(angles, frames=None) 시그너처 확인)
    - backend/shared/python/sunity_shared/analysis/gemini_technique_recognizer.py (Plan 5-01 신설, recognize 시그너처 + DI 인자)
    - backend/shared/python/sunity_shared/analysis/technique_cache.py (Plan 5-02 신설, TechniqueCache 생성자 박제)
    - backend/tests/test_pipeline_dispatch.py (pipeline env switch 박제 테스트 패턴 — _runpod_enabled 검증)
    - .planning/phases/05-gemini/05-PATTERNS.md (§Plan 5-03 박제 패턴, lines 461-573)
    - .planning/phases/05-gemini/05-RESEARCH.md (§Common Operation 4 — env switch 박제 예시, §Production Wiring 2 Lambda 변경)
  </read_first>
  <behavior>
    Test 1: env 미설정 → `_ensure_recognizer()` 반환 instance type = FallbackRecognizer
    Test 2: env `RECOGNIZER_BACKEND=gemini` → `_ensure_recognizer()` 반환 instance type = GeminiTechniqueRecognizer
    Test 3: env `GEMINI_RECOGNIZER_ENABLED=1` → GeminiTechniqueRecognizer (호환 alias 박제)
    Test 4: env `GEMINI_RECOGNIZER_ENABLED=TRUE` (case-insensitive) → GeminiTechniqueRecognizer
    Test 5: `_ensure_recognizer()` 멱등 (2회 호출 = 같은 instance)
    Test 6: pipeline 모듈 import 시 google.genai / firebase_admin / boto3 미import (lazy 검증)
    Test 7: GeminiTechniqueRecognizer 합성 시 cache + unregistered_hook 자동 박제 주입
    Test 8: FallbackRecognizer.recognize(angles, frames=None) 호환 검증 (Protocol 정합)
    Test 9: FallbackRecognizer.recognize(angles, frames="/tmp/fake.mp4") frames 인자 ignore 정합
    Test 10 (B8 fix 검증): `_angles_from_video` 시그너처 무변경 (signature inspect = `(bucket, key) -> np.ndarray`)
    Test 11 (B8 fix 검증): `_angles_and_video_path_from_video` 신설 + 시그너처 = `(bucket, key) -> tuple[np.ndarray, str]`
  </behavior>
  <action>
    `backend/functions/pipeline/app.py` 박제 갱신 4건:

    **변경 1** — line 117-120 박제 갱신:
    - 현재: `_RECOGNIZER: technique.TechniqueRecognizer = technique.FallbackRecognizer()` (즉시 생성)
    - 갱신 후: `_RECOGNIZER: technique.TechniqueRecognizer | None = None` + `_RECOGNIZER_LOCK = threading.Lock()`
    - 주석에 박제 spec 인용 (D-12 / D-16 / Plan 5-03)

    **변경 2** — 신설 함수 2개 (`_RECOGNIZER` 박제 다음 블록):
    - `def _gemini_enabled() -> bool` — env switch 체크. 두 env var 박제 (`GEMINI_RECOGNIZER_ENABLED`, `RECOGNIZER_BACKEND`), case-insensitive, 박제 값 = {"1", "true", "on", "yes", "gemini"} 중 하나일 때 True. default False
    - `def _ensure_recognizer() -> technique.TechniqueRecognizer` — lazy creation. `_RECOGNIZER_LOCK` 안에서 더블체크 + env 기반 swap. Gemini 선택 시 lazy import 후 `GeminiTechniqueRecognizer(extractor=GeminiMomentExtractor(), cache=TechniqueCache(), unregistered_hook=lambda kw, vh: firestore_admin.record_unregistered_keyword(kw, uid="anonymous-pipeline", video_hash=vh))` 합성

    **변경 3** — `_process` 안 line 230 박제 갱신:
    - 현재: `profile = _RECOGNIZER.recognize(angles)`
    - 갱신 후: `recognizer = _ensure_recognizer()` + `profile = recognizer.recognize(angles, frames=local_video_path)`

    **변경 4 (B8 fix 2026-06-04 revision)** — tempfile 컨텍스트 확장 박제 (helper 신설 path):
    - 기존 `_angles_from_video(bucket, key) -> np.ndarray` 시그너처 **무변경 유지** (회귀 위험 0 + RunPod server.py D-12 무수정 박제 정합)
    - 별 helper 함수 `_angles_and_video_path_from_video(bucket, key) -> tuple[np.ndarray, str]` 신설:
      ```python
      def _angles_and_video_path_from_video(bucket: str, key: str) -> tuple[np.ndarray, str]:
          """B8 fix: 시그너처 변경 회귀 보호. Gemini 어댑터 path 만 사용 — frames 인자 path 살림.
          
          tempfile 박제 = delete=False (caller 가 finally 에서 unlink 책임).
          기존 `_angles_from_video` 와 분리 — 호출처 갱신 0, RunPod server.py 무수정 정합 (D-12).
          """
          _ensure_adapters()
          tmp = tempfile.NamedTemporaryFile(suffix=".mp4", delete=False)
          tmp_path = tmp.name
          tmp.close()
          try:
              _s3.download_file(bucket, key, tmp_path)
              frames = _FRAME_EXTRACTOR.extract(tmp_path)
              keypoints = _POSE_ESTIMATOR.estimate(frames)
              angles = compute_joint_angles(keypoints)
              angles_filled = temporal_fill(angles, joint_uncertainty(keypoints))
              return angles_filled, tmp_path
          except Exception:
              Path(tmp_path).unlink(missing_ok=True)
              raise
      ```
    - `_process` caller 안 분기 박제:
      ```python
      recognizer = _ensure_recognizer()
      if _gemini_enabled():
          # Gemini path — video_path 필요 (frames 인자)
          angles, local_video_path = _angles_and_video_path_from_video(bucket, key)
          try:
              profile = recognizer.recognize(angles, frames=local_video_path)
          finally:
              Path(local_video_path).unlink(missing_ok=True)
      else:
          # Fallback path — 기존 박제 무변경 (회귀 0)
          angles = _angles_from_video(bucket, key)
          profile = recognizer.recognize(angles)
      ```

    **테스트** `backend/tests/test_pipeline_recognizer_switch.py` 신설:
    - `_import_pipeline()` helper — `importlib.util` 로 `functions/pipeline/app.py` 동적 import (Lambda 모듈 path 박제 패턴)
    - `_reset_recognizer_module` autouse fixture — 각 테스트마다 module reload (global state 격리)
    - TestEnvSwitch 클래스 (5 테스트):
      - test_default_is_fallback: env 미설정 → FallbackRecognizer instance
      - test_env_enables_gemini: RECOGNIZER_BACKEND=gemini → GeminiTechniqueRecognizer instance
      - test_env_alias_gemini_recognizer_enabled: GEMINI_RECOGNIZER_ENABLED=1 → GeminiTechniqueRecognizer
      - test_env_case_insensitive: GEMINI_RECOGNIZER_ENABLED=TRUE → GeminiTechniqueRecognizer
      - test_ensure_recognizer_idempotent: 2회 호출 → 같은 instance
    - TestLazyImport 클래스 (2 테스트):
      - test_no_gemini_module_at_pipeline_load: sys.modules 에 google.genai / google.generativeai 미존재
      - test_no_firebase_admin_at_pipeline_load: sys.modules 에 firebase_admin 미존재
    - TestProtocolCompat 클래스 (2 테스트):
      - test_fallback_recognize_accepts_frames_none: FallbackRecognizer.recognize(angles, frames=None) 정상 동작
      - test_fallback_recognize_accepts_frames_path: FallbackRecognizer.recognize(angles, frames="/tmp/x.mp4") frames ignore 정합
    - TestGeminiComposition 클래스 (2 테스트):
      - test_gemini_recognizer_has_cache_injected: GeminiTechniqueRecognizer instance 의 cache 박제 None X
      - test_gemini_recognizer_has_unregistered_hook: callable(r.unregistered_hook)

    박제 정신 (D-12, D-16):
    - `_RECOGNIZER` 즉시 박제 X (모듈 로드 시 boto3 / google-genai / firebase-admin 0 import)
    - default = fallback (회귀 위험 최소, A/B 비교 가능)
    - lazy import 검증 sys.modules grep test 박제
    - threading.Lock 으로 멀티 thread 안전 (Pod 단일 worker 기본 — `--workers 1` 박제 정합)
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_pipeline_recognizer_switch.py -x --no-header -q</automated>
  </verify>
  <done>
    pipeline/app.py 변경 4건 박제 + 11 단위 테스트 PASS. lazy import 검증 PASS. 모든 env switch path 동작.
  </done>
  <acceptance_criteria>
    - `pipeline/app.py` 에 `_ensure_recognizer`, `_gemini_enabled` 2 함수 박제 (grep 2건)
    - `_RECOGNIZER: technique.TechniqueRecognizer | None = None` 박제 (즉시 생성 X)
    - `_RECOGNIZER_LOCK = threading.Lock()` 박제
    - **B8 fix 검증**:
      - 기존 `_angles_from_video(bucket, key) -> np.ndarray` 시그너처 **무변경** (회귀 0 — `grep '_angles_from_video' backend/` 결과 모든 호출처 시그너처 정합 PASS, `grep -rn '_angles_from_video' backend/` 모든 호출처 갱신 검증 추가)
      - 별 helper `_angles_and_video_path_from_video(bucket, key) -> tuple[np.ndarray, str]` 신설 박제 (`grep -c '_angles_and_video_path_from_video' backend/functions/pipeline/app.py` ≥ 2 — 정의 + 호출)
      - RunPod server.py 무수정 박제 검증 (`git diff --stat backend/runpod_inference/server.py` 변경 0, D-12 박제 정합)
    - `_process` 안에서 `recognizer.recognize(angles, frames=local_video_path)` 호출 (Gemini path 만) + tempfile finally cleanup 박제
    - `backend/tests/test_pipeline_recognizer_switch.py` 신설 + test 함수 ≥ 11개
    - `pytest tests/test_pipeline_recognizer_switch.py -x` PASS
    - pipeline 모듈 import 시 google.genai / firebase_admin 미import 검증 PASS
    - FallbackRecognizer recognize(angles, frames=None) 호환 검증 PASS
  </acceptance_criteria>
</task>

<task type="tdd">
  <name>Task 2: _process 흐름 통합 테스트 (mock-based, Lambda fallback path 박제)</name>
  <files>backend/tests/test_pipeline_gemini_integration.py</files>
  <read_first>
    - backend/functions/pipeline/app.py (Task 1 갱신 후 — `_process` + `_ensure_recognizer` + `_angles_from_video` 박제 흐름)
    - backend/shared/python/sunity_shared/analysis/gemini_technique_recognizer.py (Plan 5-01 — recognize 시그너처)
    - backend/shared/python/sunity_shared/analysis/technique_cache.py (Plan 5-02 — cache 의존성)
    - backend/tests/test_pipeline_recognizer_switch.py (Task 1 — _import_pipeline helper 박제 패턴)
    - backend/tests/conftest.py (기존 fixture 박제 확인)
  </read_first>
  <behavior>
    Test 1: env RECOGNIZER_BACKEND=gemini + mock GeminiMomentExtractor → _process 정상 흐름 → recognizer 가 GeminiTechniqueRecognizer 사용 검증
    Test 2: env 미설정 + mock S3 + mock pose engine → _process 흐름 → recognizer 가 FallbackRecognizer (기존 path) 사용 검증 (회귀 0)
    Test 3: GeminiTechniqueRecognizer 의 API 실패 시 FallbackRecognizer 자동 위임 + 분석 흐름 계속 (D-09 case 1 박제)
    Test 4: tempfile cleanup 검증 — _process 호출 후 local_video_path 가 unlink (FileNotFoundError 검증)
  </behavior>
  <action>
    `backend/tests/test_pipeline_gemini_integration.py` 신설. mock-based 통합 테스트.

    구조:
    - `_import_pipeline` helper (Task 1 박제 재사용 — copy 또는 conftest 박제)
    - autouse fixture — 각 테스트마다 module reload
    - Mock S3 client (`@mock.patch("boto3.client")`)
    - Mock pose engine (`_POSE_ESTIMATOR` injection → angles 반환)
    - Mock GeminiMomentExtractor (`sunity_shared.judging.GeminiMomentExtractor` 클래스 patch — `extract_key_moments` 가 stub KeyMoment list 반환)
    - Mock firestore_admin (write 호출만 검증, 실 Firestore 호출 X)

    테스트 4 종:
    
    **test_process_with_gemini_recognizer_uses_gemini**:
    - env RECOGNIZER_BACKEND=gemini
    - mock extract_key_moments 가 5 KeyMoment 반환 (mean confidence 0.85)
    - _process 호출
    - mock GeminiMomentExtractor.extract_key_moments 가 1회 호출됨 검증
    - 분석 결과 profile.name = "ref-foxtop" (mock 응답 기준) 검증

    **test_process_without_env_uses_fallback**:
    - env 미설정
    - _process 호출
    - GeminiMomentExtractor patch 호출 0건 검증
    - profile.name = "미상" (FallbackRecognizer default) 검증

    **test_gemini_api_failure_falls_back_to_fallback**:
    - env RECOGNIZER_BACKEND=gemini
    - mock extract_key_moments 가 RuntimeError raise
    - _process 호출 → 분석 흐름 crash 0
    - profile.category = "api_failure" 검증 (Plan 5-01 _override_category 박제 정합)
    - dimensions.absolute_dimension_scores 호출 정합 (분석 계속)

    **test_tempfile_cleanup**:
    - mock _angles_from_video 가 임시 파일 박제
    - _process 호출 후 임시 파일 unlink 검증 (Path(local_video_path).exists() == False)

    박제 정신 (D-12, D-09):
    - 통합 흐름 박제 — 실 Gemini 호출 X (mock injection)
    - 회귀 보존 — env 미설정 default fallback path 검증
    - graceful degrade — API 실패 시 분석 흐름 계속
    - tempfile cleanup 박제 (메모리/디스크 누수 방지)
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_pipeline_gemini_integration.py -x --no-header -q</automated>
  </verify>
  <done>
    _process 흐름 통합 테스트 4 케이스 PASS. mock GeminiMomentExtractor + mock S3 + mock pose engine 박제 정합.
  </done>
  <acceptance_criteria>
    - `backend/tests/test_pipeline_gemini_integration.py` 신설 + line count >= 80 + test 함수 >= 4개
    - `pytest tests/test_pipeline_gemini_integration.py -x` PASS
    - test_process_with_gemini_recognizer_uses_gemini PASS — mock extract_key_moments 호출 검증
    - test_process_without_env_uses_fallback PASS — GeminiMomentExtractor patch 호출 0건
    - test_gemini_api_failure_falls_back_to_fallback PASS — RuntimeError 시 graceful degrade + category="api_failure"
    - test_tempfile_cleanup PASS — local_video_path unlink 검증
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env var → `_ensure_recognizer` | env var 가 어댑터 swap 결정 — 사용자 입력 X (Pod env 박제만) |
| pipeline `_process` → 어댑터 | Protocol 의존성만 — 어댑터 구현체 신뢰 boundary |
| tempfile lifecycle | `_angles_from_video` -> `recognize` -> unlink 박제. 누락 시 디스크 누수 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Tampering | env var 오타 (`RECOGNIZER_BACKEN`) → 의도 안 한 fallback | accept | default = fallback 박제 정합 (safe default — 분석은 작동). 박제 로그 = `Recognizer = Fallback (env switch OFF — default)` 명시 |
| T-05-03-02 | Denial of Service | tempfile cleanup 누락 → 디스크 누수 | mitigate | try/finally Path.unlink(missing_ok=True) 박제 + 단위 테스트 검증 |
| T-05-03-03 | Spoofing | 어댑터 instance 위조 (test 환경) | accept | 단위 테스트 박제만, production env 영향 X |
| T-05-03-04 | Information Disclosure | env switch 로그 leak (예: API 키 노출) | accept | log.info 박제는 env 종류만 (`Recognizer = Gemini`), 키 값 출력 X |
| T-05-03-SC | Tampering | npm/pip/cargo installs | mitigate | 본 plan 신규 install 0 — Plan 5-01/5-02 의존성 재사용 |
</threat_model>

<verification>
- env switch 5 케이스 단위 테스트 PASS
- lazy import 검증 PASS (pipeline 모듈 import 시 google.genai / firebase_admin 미import)
- Protocol 호환 검증 PASS (FallbackRecognizer.recognize(frames=None) 정합)
- _process 흐름 통합 테스트 4 케이스 PASS (mock-based)
- tempfile cleanup 박제 정합
- 박제 spec 정합 (D-12 / D-16 / D-09 case 1)
</verification>

<success_criteria>
- pipeline _RECOGNIZER lazy swap 박제 + 단위 테스트 PASS
- 통합 테스트 mock-based PASS (Gemini path + fallback path 모두 검증)
- RunPod server.py 무수정 (D-12 정합)
- Plan 5-04 (Pod requirements + setup.sh) 진입 가능
</success_criteria>

<output>
Create `.planning/phases/05-gemini/05-03-SUMMARY.md` when done. Include:
- pipeline/app.py 변경 4건 박제 diff 요약
- env switch 박제 (GEMINI_RECOGNIZER_ENABLED / RECOGNIZER_BACKEND alias 2종)
- lazy import 검증 결과 (pipeline 모듈 import 시 0 import 박제)
- 단위 + 통합 테스트 15 케이스 결과 (PASS/FAIL)
- 박제 spec 정합 확인 (D-12 / D-16 / D-09 case 1)
</output>
