---
phase: 05-gemini
plan: 04
type: execute
wave: 3
depends_on:
  - 05-03
files_modified:
  - backend/runpod_inference/requirements.txt
  - backend/runpod_inference/setup.sh
  - backend/runpod_inference/server.py
  - backend/tests/test_runpod_startup_gemini.py
autonomous: true
requirements:
  - SCORE-01
user_setup:
  - service: gemini-api
    why: Gemini 3.1 Pro API 키 — Pod env 주입 wiring (D-13/D-15)
    env_vars:
      - name: GEMINI_API_KEY
        source: AWS Parameter Store `/sunity/motion/gemini-api-key` (SecureString, belle 박제 2026-06-01) 또는 env 직접 export
      - name: RECOGNIZER_BACKEND
        source: Pod env 직접 export (`gemini` = Gemini 어댑터, 미설정 = FallbackRecognizer)
    dashboard_config: []

must_haves:
  truths:
    - "Pod requirements.txt 에 google-genai>=1.0,<2.0 박제 (Apache 2.0, D-13 박제)"
    - "Pod setup.sh 에 GEMINI_API_KEY SSM fetch + RECOGNIZER_BACKEND env 박제 안내 추가 (D-15)"
    - "Pod server.py 의 _warmup() 가 _ensure_recognizer() 명시 호출 — Pod 시작 시 fail-loud (RESEARCH.md Common Pitfall 4 박제)"
    - "Pod 가 GEMINI_API_KEY 미설정 + RECOGNIZER_BACKEND=gemini 상태에서 startup 시 _warmup 안 log.error + log.exception (RuntimeError) 박제 — outer except 가 흡수해 server 는 살아있음, 첫 /analyze 호출 시점에 503/실패 (fail-loud 박제 정신 = log 명시 + 첫 분석 시점 명시 실패, 갑작스러운 미스터리 X). W1 fix 박제 (2026-06-04 iter 2)"
    - "Pod 가 RECOGNIZER_BACKEND 미설정 시 정상 startup (FallbackRecognizer default — 회귀 0)"
  artifacts:
    - path: backend/runpod_inference/requirements.txt
      provides: google-genai 의존성 박제 (Pod pip install 1회)
      contains: "google-genai"
    - path: backend/runpod_inference/setup.sh
      provides: GEMINI_API_KEY SSM fetch + RECOGNIZER_BACKEND env 안내 block
      contains: "GEMINI_API_KEY"
    - path: backend/runpod_inference/server.py
      provides: _warmup 가 _ensure_recognizer 명시 호출 (fail-loud)
      contains: "_ensure_recognizer"
    - path: backend/tests/test_runpod_startup_gemini.py
      provides: Pod startup fail-loud 단위 테스트 (mock-based)
      contains: "test_warmup_fails_loud_when_gemini_enabled_without_key"
      min_lines: 60
  key_links:
    - from: backend/runpod_inference/server.py
      to: backend/functions/pipeline/app.py
      via: _load_pipeline_module + _ensure_recognizer 명시 호출
      pattern: "_ensure_recognizer"
    - from: backend/runpod_inference/setup.sh
      to: AWS Parameter Store /sunity/motion/gemini-api-key
      via: aws ssm get-parameter 박제 안내
      pattern: "/sunity/motion/gemini-api-key"
---

<objective>
**Plan 5-04** — Pod environment wiring (requirements + setup.sh + startup fail-loud).

Purpose: Plan 5-03 의 pipeline `_ensure_recognizer` 가 production 에서 작동하도록 RunPod Pod 환경 박제. `google-genai` Pod 설치 + `GEMINI_API_KEY` env 주입 안내 + Pod startup 시 fail-loud (Common Pitfall 4 박제 — Pod env 누락 시 첫 분석에서 갑작스러운 RuntimeError 박제 회피).

Output:
- `requirements.txt` 에 `google-genai>=1.0,<2.0` 1줄 박제
- `setup.sh` 에 `GEMINI_API_KEY` SSM fetch + `RECOGNIZER_BACKEND` env 박제 안내 block
- `server.py` 의 `_warmup` 가 `_ensure_recognizer()` 명시 호출 (fail-loud)
- 단위 테스트 (mock-based startup)

박제 정신:
- D-13: Gemini 3.1 Pro 단일, `google-genai` (신 SDK Apache 2.0)
- D-15: AWS Parameter Store `/sunity/motion/gemini-api-key` (SecureString) + env fallback
- D-16: lazy import (server.py 자체는 google-genai 미import, pipeline 모듈 로드 시점도 0)
- D-12: Pod 1pass — server.py 무수정 정신은 유지, 단 _warmup 안에서 _ensure_recognizer 명시 호출 추가 (fail-loud 박제 — Common Pitfall 4 정합)
- [[runpod-gpu-env]] — Pod 환경 박제 누적 (Plan 11/23 박제 재사용)
- [[gsd-pod-work-push-first]] — 본 plan 변경 후 commit + push 필수
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-gemini/05-CONTEXT.md
@.planning/phases/05-gemini/05-RESEARCH.md
@.planning/phases/05-gemini/05-PATTERNS.md
@.planning/phases/05-gemini/05-03-SUMMARY.md

# Source files
@backend/runpod_inference/requirements.txt
@backend/runpod_inference/setup.sh
@backend/runpod_inference/server.py
@backend/functions/pipeline/app.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pod requirements.txt + setup.sh 박제 (google-genai install + GEMINI_API_KEY env 안내)</name>
  <files>backend/runpod_inference/requirements.txt, backend/runpod_inference/setup.sh</files>
  <read_first>
    - backend/runpod_inference/requirements.txt (현재 박제 — fastapi/boto3/firebase-admin/imageio 박제 패턴)
    - backend/runpod_inference/setup.sh (현재 박제 — lines 113-125 env 안내 block 박제 패턴, Step 1-6 박제 흐름)
    - .planning/phases/05-gemini/05-RESEARCH.md (§Standard Stack + §Production Wiring 1 — google-genai 박제 라인 + SSM fetch 박제 예시)
    - .planning/phases/05-gemini/05-PATTERNS.md (§Plan 5-04 박제 패턴, lines 589-655)
    - .claude/projects/-Users-kimtaesung-Dev-SunityMotion/memory/runpod-gpu-env.md (Pod 환경 박제 — 현재 Plan 11/23 박제, install 함정 5종)
  </read_first>
  <action>
    **Step A**: `backend/runpod_inference/requirements.txt` 박제 (append):
    ```
    
    # Phase 5 (Plan 5-04, 2026-06-04) — Gemini 기술 인식기 (D-13 박제).
    # 신 SDK (google-genai) 만 사용. legacy `google-generativeai` 0.8.x 는
    # 새 AI Studio `AQ.` 키 포맷 미지원 (2026-06-01 박제 gemini_moment_extractor.py 주석 참조).
    # Apache 2.0 라이선스 박제.
    google-genai>=1.0,<2.0
    ```

    **Step B**: `backend/runpod_inference/setup.sh` 박제 갱신 (lines 113-125 env 안내 block 확장):
    
    현재 마지막 블록 (line 117-122) 다음에 박제:
    ```bash
    echo ""
    echo "Phase 5 (Plan 5-04) — Gemini 기술 인식기 env 박제:"
    echo ""
    echo "  # 옵션 A — Parameter Store SSM fetch (D-15 박제 권장 path)"
    echo "  export GEMINI_API_KEY=\$(aws ssm get-parameter \\"
    echo "    --name /sunity/motion/gemini-api-key \\"
    echo "    --with-decryption \\"
    echo "    --query 'Parameter.Value' --output text \\"
    echo "    --region ap-northeast-2)"
    echo ""
    echo "  # 옵션 B — env 직접 export (Plan 01-13 fallback path)"
    echo "  # export GEMINI_API_KEY=<key>"
    echo ""
    echo "  # 어댑터 swap — gemini (Plan 5-03 박제), 미설정 = FallbackRecognizer default"
    echo "  export RECOGNIZER_BACKEND=gemini  # 또는 GEMINI_RECOGNIZER_ENABLED=1 (alias)"
    echo ""
    echo "  # 경고: GEMINI_API_KEY 누락 + RECOGNIZER_BACKEND=gemini 상태에서 server 기동 시"
    echo "  #       _warmup() 가 RuntimeError fail-loud (Plan 5-04 박제, Common Pitfall 4)"
    ```

    박제 정신:
    - setup.sh 안에서 SSM fetch 직접 실행 X (Pod 재기동 시 IAM 만료 위험) — belle 가 수동 export
    - gemini_moment_extractor.py 의 `_load_api_key` 박제 정합 (env 우선 → SSM fallback)
    - 옵션 B (env 직접 export) 박제 = Plan 01-13 박제 fallback path 정합

    **검증 (자동)**:
    - `requirements.txt` 에 `google-genai>=1.0,<2.0` 박제 (`grep -c 'google-genai' requirements.txt` >= 1)
    - `setup.sh` 에 `GEMINI_API_KEY` 박제 (`grep -c 'GEMINI_API_KEY' setup.sh` >= 2 — fetch + 경고)
    - `setup.sh` 에 `RECOGNIZER_BACKEND` 박제 (`grep -c 'RECOGNIZER_BACKEND' setup.sh` >= 1)
    - `setup.sh` 에 `/sunity/motion/gemini-api-key` 박제 (`grep -c '/sunity/motion/gemini-api-key' setup.sh` >= 1)
  </action>
  <verify>
    <automated>cd backend && bash -n runpod_inference/setup.sh && grep -c 'google-genai' runpod_inference/requirements.txt && grep -c 'GEMINI_API_KEY' runpod_inference/setup.sh && grep -c 'RECOGNIZER_BACKEND' runpod_inference/setup.sh && grep -c '/sunity/motion/gemini-api-key' runpod_inference/setup.sh</automated>
  </verify>
  <done>
    requirements.txt + setup.sh 박제 갱신 완료. 자동 검증 4건 PASS.
  </done>
  <acceptance_criteria>
    - `requirements.txt` 에 `google-genai>=1.0,<2.0` 박제
    - `setup.sh` syntax 검증 PASS (`bash -n`)
    - `GEMINI_API_KEY` 박제 >= 2 줄
    - `RECOGNIZER_BACKEND` 박제 >= 1 줄
    - `/sunity/motion/gemini-api-key` 박제 >= 1 줄 (D-15 박제 정합)
    - 박제 주석에 `Plan 5-04`, `D-13`, `D-15`, `Apache 2.0` 인용
  </acceptance_criteria>
</task>

<task type="tdd">
  <name>Task 2: server.py _warmup fail-loud + Pod startup 단위 테스트</name>
  <files>backend/runpod_inference/server.py, backend/tests/test_runpod_startup_gemini.py</files>
  <read_first>
    - backend/runpod_inference/server.py (현재 박제 — `_warmup` 박제 lines 139-150, `_load_pipeline_module` lines 59-80, `_auth` 박제 패턴)
    - backend/functions/pipeline/app.py (Plan 5-03 갱신 — `_ensure_recognizer` 박제, `_ensure_adapters` 호출 패턴)
    - backend/shared/python/sunity_shared/judging/gemini_moment_extractor.py (`_load_api_key` 박제 — RuntimeError 발생 path)
    - .planning/phases/05-gemini/05-RESEARCH.md (§Common Pitfall 4 — Pod env 미설정 fail-loud 박제 정신)
  </read_first>
  <behavior>
    Test 1: env 미설정 (RECOGNIZER_BACKEND 미설정) → _warmup 정상 완료 (FallbackRecognizer 박제, 회귀 0)
    Test 2: env RECOGNIZER_BACKEND=gemini + GEMINI_API_KEY 미설정 + boto3 SSM mock fail → _warmup 가 RuntimeError 발생 (fail-loud 박제)
    Test 3: env RECOGNIZER_BACKEND=gemini + GEMINI_API_KEY="test-key" → _warmup 정상 완료 (Gemini 어댑터 박제)
    Test 4: _warmup 실패 시 server 자체는 살아있음 (FastAPI 503 응답 path 박제) — log.exception 박제만 검증
  </behavior>
  <action>
    `backend/runpod_inference/server.py` 박제 갱신 — `_warmup` (lines 139-150) 안에서 `_ensure_recognizer()` 명시 호출 추가.

    현재 박제 (lines 139-150):
    ```python
    @app.on_event("startup")
    def _warmup() -> None:
        """모델/어댑터 미리 GPU 메모리 로드. 실패해도 첫 /analyze 에서 재시도.
        pipeline 모듈은 어댑터 import 를 lazy 로 미루므로 _ensure_adapters() 까지
        명시적으로 호출 — startup 비용을 첫 요청이 아닌 부팅 시점에 지불."""
        log.info("RunPod 분석 서버 startup — auth=%s", "ON" if _AUTH_TOKEN else "OFF")
        try:
            mod = _load_pipeline_module()
            mod._ensure_adapters()
            log.info("어댑터 워밍업 완료 (NLF/ffmpeg/coach)")
        except Exception:
            log.exception("워밍업 실패 — 첫 요청 처리 시 재시도")
    ```

    박제 변경 후:
    ```python
    @app.on_event("startup")
    def _warmup() -> None:
        """모델/어댑터 미리 GPU 메모리 로드. 실패해도 첫 /analyze 에서 재시도.
        pipeline 모듈은 어댑터 import 를 lazy 로 미루므로 _ensure_adapters() + _ensure_recognizer()
        까지 명시적으로 호출 — startup 비용을 첫 요청이 아닌 부팅 시점에 지불.
        
        Phase 5 (Plan 5-04, 2026-06-04) — _ensure_recognizer() fail-loud 박제:
        env RECOGNIZER_BACKEND=gemini + GEMINI_API_KEY 누락 시 startup 에서 RuntimeError 발생.
        Pod 가 503 응답 — 첫 분석에서 갑작스러운 RuntimeError 박제 회피 (Common Pitfall 4 박제 정신).
        """
        log.info("RunPod 분석 서버 startup — auth=%s", "ON" if _AUTH_TOKEN else "OFF")
        try:
            mod = _load_pipeline_module()
            mod._ensure_adapters()
            log.info("어댑터 워밍업 완료 (NLF/ffmpeg/coach)")
            
            # Plan 5-04 박제 — Gemini 어댑터 fail-loud (Common Pitfall 4)
            recognizer = mod._ensure_recognizer()
            log.info("Recognizer 워밍업 완료 — type=%s", type(recognizer).__name__)
            
            # Gemini 선택 시 API 키 검증 명시 호출 (recognize 첫 호출 전에 fail-loud)
            if mod._gemini_enabled():
                # _load_api_key() 호출 시도 — env / SSM 둘 다 없으면 RuntimeError
                try:
                    from sunity_shared.judging.gemini_moment_extractor import _load_api_key
                    _load_api_key()  # 키 값 자체는 버림 (검증만)
                    log.info("Gemini API 키 검증 완료")
                except RuntimeError as exc:
                    log.error(
                        "Gemini API 키 검증 실패 — Pod env GEMINI_API_KEY 또는 SSM "
                        "Parameter Store /sunity/motion/gemini-api-key 박제 누락: %s", exc
                    )
                    raise  # fail-loud — 503 응답으로 명시
        except Exception:
            log.exception("워밍업 실패 — 첫 요청 처리 시 재시도")
    ```

    **테스트** `backend/tests/test_runpod_startup_gemini.py` 신설:
    
    구조:
    - `_import_server()` helper — `importlib.util` 로 `runpod_inference/server.py` 동적 import
    - autouse fixture — 각 테스트마다 module reload + env 초기화
    - Mock `_load_pipeline_module` → return mock module with `_ensure_adapters`, `_ensure_recognizer`, `_gemini_enabled` 박제
    - Mock `_load_api_key` (sunity_shared.judging.gemini_moment_extractor) → RuntimeError 또는 정상 string 반환
    
    테스트 4 종:
    
    **test_warmup_fallback_default_succeeds**:
    - env 미설정 (RECOGNIZER_BACKEND 미설정)
    - mock _gemini_enabled() → False
    - _warmup 호출 → 예외 발생 0 검증
    - 회귀 0 (기존 behavior 유지)
    
    **test_warmup_fails_loud_when_gemini_enabled_without_key**:
    - env RECOGNIZER_BACKEND=gemini
    - mock _gemini_enabled() → True
    - mock _load_api_key → RuntimeError
    - _warmup 호출 → log.exception 호출 검증 (fail-loud 박제)
    - 분석 흐름 영향: server 자체는 살아있음 (FastAPI 503 응답 path, exception 흡수)
    
    **test_warmup_succeeds_with_gemini_and_key**:
    - env RECOGNIZER_BACKEND=gemini + GEMINI_API_KEY="test-key"
    - mock _gemini_enabled() → True
    - mock _load_api_key → "test-key" 반환
    - _warmup 호출 → 정상 완료 (예외 0)
    
    **test_warmup_calls_ensure_recognizer_when_gemini_enabled**:
    - env RECOGNIZER_BACKEND=gemini
    - mock _ensure_recognizer + mock _load_api_key (정상)
    - _warmup 호출 → mock _ensure_recognizer 1회 호출 검증
    
    박제 정신 (D-12, D-15, D-16, Common Pitfall 4):
    - server.py 변경 = _warmup 안만 (외부 시그너처 무변경)
    - fail-loud 박제 — Pod env 누락 시 startup 시점에 명시 (첫 분석 갑작스러운 에러 X)
    - log.exception 박제 — exception 흡수 시 trace 박제 (debug 용이성)
    - mock-based 단위 — 실 Gemini 호출 X
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_runpod_startup_gemini.py -x --no-header -q</automated>
  </verify>
  <done>
    server.py _warmup 박제 갱신 + 4 단위 테스트 PASS. fail-loud 박제 정합. 회귀 0 (기본 fallback path 정상).
  </done>
  <acceptance_criteria>
    - `server.py` _warmup 안에 `_ensure_recognizer()` 호출 박제 (grep `_ensure_recognizer` >= 1)
    - `server.py` _warmup 안에 `_load_api_key` import + 호출 박제
    - `backend/tests/test_runpod_startup_gemini.py` 신설 + line count >= 60 + test 함수 >= 4개
    - `pytest tests/test_runpod_startup_gemini.py -x` PASS
    - 회귀 검증: env 미설정 default fallback path 정상 (test_warmup_fallback_default_succeeds PASS)
    - fail-loud 박제: GEMINI_API_KEY 누락 시 log.error + RuntimeError 박제 정합
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pod env → server.py | env var 박제 신뢰 boundary — belle 수동 export |
| AWS SSM → Pod boto3 | IAM 권한 박제. Pod 재기동 시 권한 만료 위험 |
| google-genai SDK install | Pod base image + pip install — SDK 버전 변동 위험 (Common Pitfall 3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Information Disclosure | GEMINI_API_KEY 로그 leak | mitigate | _load_api_key 박제 = 키 값 자체 미출력 (Plan 01-13 박제). _warmup 박제 로그 = "검증 완료" 만 |
| T-05-04-02 | Denial of Service | google-genai install fail (네트워크 / SDK 시그너처 변경) | mitigate | requirements.txt 박제 = `>=1.0,<2.0` upper bound. Pod 재기동 후 `pip freeze` 박제 권장 |
| T-05-04-03 | Tampering | setup.sh 의 SSM fetch path 우회 (env 직접 export, 잘못된 키) | accept | _load_api_key 박제 = env 우선 path 의도된 박제 (Plan 01-13 fallback). 잘못된 키 시 Gemini API 401 → 어댑터 fallback path (Plan 5-01 D-09 case 1) |
| T-05-04-04 | Elevation of Privilege | Pod 가 IAM 권한 없는 상태에서 SSM 조회 시도 | accept | RuntimeError 박제 → _warmup fail-loud → 503. 박제 정합 |
| T-05-04-SC | Tampering | google-genai package SLOP 위험 | mitigate | [VERIFIED] google-genai = Google 공식 GitHub repo (RESEARCH.md §Package Legitimacy Audit 박제). 본 install 박제는 [ASSUMED] disposition 정합 |
</threat_model>

<verification>
- requirements.txt google-genai 박제 정합
- setup.sh 박제 syntax PASS + 박제 인용 4건 정합
- server.py _warmup fail-loud 박제 정합
- 단위 테스트 4 케이스 PASS (회귀 0 + fail-loud 박제)
- 박제 spec 정합 (D-13 / D-15 / D-16 / Common Pitfall 4)
</verification>

<success_criteria>
- Pod 환경 박제 완료 (requirements + setup.sh + server.py _warmup)
- Plan 5-05 (sweep flag + belle Pod checkpoint) 진입 가능
- belle 가 Pod 에서 `git pull && cd backend && pip install -r runpod_inference/requirements.txt && bash runpod_inference/setup.sh && export RECOGNIZER_BACKEND=gemini && uvicorn ...` 실행 path 박제
</success_criteria>

<output>
Create `.planning/phases/05-gemini/05-04-SUMMARY.md` when done. Include:
- requirements.txt + setup.sh diff 박제
- server.py _warmup 변경 박제 (fail-loud)
- 단위 테스트 4 케이스 결과
- Pod 환경 준비 체크리스트 (belle Pod 실행 전 박제 항목)
- 박제 spec 정합 확인 (D-13 / D-15 / Common Pitfall 4)
</output>
