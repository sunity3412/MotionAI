---
phase: 05-gemini
plan: 05
type: execute
wave: 4
depends_on:
  - 05-04
files_modified:
  - backend/research/evaluations/compare_rtmw_vs_ipsf.py
  - backend/tests/test_compare_rtmw_vs_ipsf_recognizer_flag.py
  - backend/research/evaluations/reports/sweep_rtmw_gemini/report.md
autonomous: false
requirements:
  - SCORE-01

must_haves:
  truths:
    - "compare_rtmw_vs_ipsf.py 에 --recognizer {fallback,gemini} CLI flag 박제. default = fallback (회귀 보존)"
    - "--recognizer gemini 선택 시 GeminiTechniqueRecognizer (extractor + cache + unregistered_hook) 합성 + compute_line_angle_gates 에 주입"
    - "회귀 보존 — 기존 Plan 23 sweep 11 PASS 테스트 통과 (default fallback path 무변경)"
    - "belle Pod sweep 5영상 (ref-climb / ref-foxtop / ref-foxtop-split / ref-invert / ref-sideway-spin) × --recognizer gemini 실행 + verdict 박제"
    - "**B1 fix (2026-06-04 revision belle 박제 결정)**: Phase 5 게이트 = `ref-climb 제외 4/4 PASS + ref-climb out-of-scope counted as PASS` (D-20 박제 정신 정합 — ref-climb 차원 = 별 phase 책임). `compute_line_angle_gates` 가 `line_score/angle_score = None` 인 모션 = `out-of-scope PASS counted` (False 아님) 박제 분기 신설. verdict 표 = `angle PASS X/N where N = 채점 영역 모션 수`."
    - "verdict report.md 박제 + git commit + push ([[gsd-pod-work-push-first]] 정합)"
    - "belle 가 sweep 결과 검토 + Phase 5 통과 / Plan 24/25 (D-16 보류 해제) 진입 / 재시도 결정 (blocking-human checkpoint)"
    - "**W6 fix (2026-06-04 revision)**: belle Pod sweep 전 `startup fail-loud 검증 = mandatory step` (Plan 5-04 _warmup 박제 정합 검증). 박제 결정 항목 (G) `startup fail-loud 검증 결과: PASS/FAIL` 추가."
  artifacts:
    - path: backend/research/evaluations/compare_rtmw_vs_ipsf.py
      provides: --recognizer {fallback,gemini} CLI flag + recognizer DI to compute_line_angle_gates
      contains: "--recognizer"
    - path: backend/tests/test_compare_rtmw_vs_ipsf_recognizer_flag.py
      provides: CLI flag 단위 테스트 + recognizer 주입 검증
      contains: "test_recognizer_flag_default_is_fallback"
      min_lines: 60
    - path: backend/research/evaluations/reports/sweep_rtmw_gemini/report.md
      provides: 5영상 sweep verdict (Phase 5 게이트 통과 여부 박제)
      contains: phase5_ready
  key_links:
    - from: backend/research/evaluations/compare_rtmw_vs_ipsf.py
      to: backend/shared/python/sunity_shared/analysis/gemini_technique_recognizer.py
      via: GeminiTechniqueRecognizer 생성 + compute_line_angle_gates 주입
      pattern: "GeminiTechniqueRecognizer"
    - from: backend/research/evaluations/compare_rtmw_vs_ipsf.py
      to: backend/shared/python/sunity_shared/analysis/technique_cache.py
      via: TechniqueCache 생성 (sweep 재실행 시 cache hit 보장)
      pattern: "TechniqueCache"
---

<objective>
**Plan 5-05** — sweep `--recognizer gemini` flag 추가 + belle Pod sweep checkpoint (Phase 5 게이트).

Purpose: Plan 23 sweep (`compare_rtmw_vs_ipsf.py`) 재실행을 Gemini 어댑터로 — Phase 5 의 진짜 게이트 = "정은지 reference 측정값 기준 5/5 PASS" (D-01 박제) 통과 검증. belle Pod 실행 → verdict 박제 → 통과 시 Plan 24/25 (D-16 보류 해제) 진입.

Output:
- `--recognizer {fallback,gemini}` CLI flag 박제 (default fallback, 회귀 0)
- recognizer DI to `compute_line_angle_gates` (Plan 23 sweep 박제 함수 시그너처 확장)
- CLI flag 단위 테스트
- belle Pod sweep + verdict report.md + checkpoint

박제 정신:
- D-01 게이트 = "정은지 reference 측정값 기준 5/5 PASS" (NotebookLM lookup 후 정의)
- D-12: Pod 안 1pass — sweep 도 Pod 안에서 실행 (Plan 11/23 박제 환경 재사용)
- D-14: cache 박제 효과 — 첫 sweep 만 Gemini 호출, 재실행 시 cache hit (belle 시연 박제 효과)
- [[gap-and-line-angle-mandatory-gates]] "강등/우회 금지" — 게이트 우회 X, 다른 객관 기준 (정은지 reference) 박제
- [[gsd-pod-work-push-first]] — Pod 작업 단위 = commit + push + Pod git pull
- [[runpod-gpu-env]] — 현재 Pod 환경 박제 재사용 (Plan 11/23 박제 Pod RTX 3090 + RTMW)
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-gemini/05-CONTEXT.md
@.planning/phases/05-gemini/05-RESEARCH.md
@.planning/phases/05-gemini/05-PATTERNS.md
@.planning/phases/05-gemini/05-03-SUMMARY.md
@.planning/phases/05-gemini/05-04-SUMMARY.md

# Source files
@backend/research/evaluations/compare_rtmw_vs_ipsf.py
@backend/shared/python/sunity_shared/analysis/gemini_technique_recognizer.py
@backend/shared/python/sunity_shared/analysis/technique_cache.py
@backend/shared/python/sunity_shared/judging/gemini_moment_extractor.py
@backend/tests/test_compare_rtmw_vs_ipsf_smoke.py
@backend/research/evaluations/reports/sweep_rtmw_20260603_1409/report.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: compare_rtmw_vs_ipsf.py --recognizer flag 박제 + compute_line_angle_gates DI</name>
  <files>backend/research/evaluations/compare_rtmw_vs_ipsf.py, backend/tests/test_compare_rtmw_vs_ipsf_recognizer_flag.py</files>
  <read_first>
    - backend/research/evaluations/compare_rtmw_vs_ipsf.py (현재 박제 — argparse 박제 패턴, `compute_line_angle_gates` 박제 lines 323-346, `FallbackRecognizer` 박제 import line 58, sweep main loop 박제)
    - backend/shared/python/sunity_shared/analysis/gemini_technique_recognizer.py (Plan 5-01 박제 — DI 인자)
    - backend/shared/python/sunity_shared/analysis/technique_cache.py (Plan 5-02 박제 — TechniqueCache 박제)
    - backend/tests/test_compare_rtmw_vs_ipsf_smoke.py (sweep smoke test 박제 패턴 — argparse + main 박제)
    - backend/tests/test_compare_rtmw_vs_ipsf_pole_axis.py (sweep 부분 테스트 박제 패턴)
    - .planning/phases/05-gemini/05-PATTERNS.md (§Plan 5-05 박제 패턴, lines 659-704)
    - .planning/phases/05-gemini/05-RESEARCH.md (§Production Wiring 5 — sweep 통합 박제)
  </read_first>
  <behavior>
    Test 1: argparse `--recognizer` flag 인식 + 기본값 `fallback` 박제
    Test 2: `--recognizer fallback` 명시 시 compute_line_angle_gates 가 FallbackRecognizer 사용 검증 (기존 회귀 0)
    Test 3: `--recognizer gemini` 명시 시 compute_line_angle_gates 가 GeminiTechniqueRecognizer 사용 검증 (mock)
    Test 4: 잘못된 값 (`--recognizer foo`) → argparse 오류 (choices 강제)
    Test 5: --recognizer gemini 시 GeminiTechniqueRecognizer 합성 = extractor + cache + unregistered_hook 박제
    Test 6: compute_line_angle_gates 시그너처 호환 검증 — recognizer 인자 default = FallbackRecognizer instance (회귀 보존)
    Test 7: sweep main loop 안에서 같은 recognizer instance 재사용 (5영상 sweep 시 cache hit 보존)
  </behavior>
  <action>
    `backend/research/evaluations/compare_rtmw_vs_ipsf.py` 박제 갱신.

    **변경 1** — argparse 박제 추가 (main() 또는 cli 정의 함수 안):
    ```python
    parser.add_argument(
        "--recognizer",
        choices=["fallback", "gemini"],
        default="fallback",
        help=(
            "기술 인식기 선택. fallback (default, Plan 11/23 박제) = FallbackRecognizer. "
            "gemini (Plan 5-05 박제) = GeminiTechniqueRecognizer + 영상 hash 캡싱. "
            "Phase 5 게이트 = --recognizer gemini 로 5영상 sweep 5/5 PASS (D-01)."
        ),
    )
    ```

    **변경 2** — recognizer factory 함수 박제 (sweep main 안):
    ```python
    def _build_recognizer(name: str):
        """recognizer name → instance. lazy import (D-16)."""
        if name == "gemini":
            from sunity_shared.analysis.gemini_technique_recognizer import (
                GeminiTechniqueRecognizer,
            )
            from sunity_shared.analysis.technique_cache import TechniqueCache
            from sunity_shared.judging import GeminiMomentExtractor
            from sunity_shared import firestore_admin
            return GeminiTechniqueRecognizer(
                extractor=GeminiMomentExtractor(),
                cache=TechniqueCache(),
                unregistered_hook=lambda kw, vh: firestore_admin.record_unregistered_keyword(
                    kw, uid="sweep-rtmw-gemini", video_hash=vh,
                ),
            )
        # default fallback (Plan 11/23 박제 보존)
        from sunity_shared.analysis.technique import FallbackRecognizer
        return FallbackRecognizer()
    ```

    **변경 3 (B1 fix 2026-06-04 revision)** — `compute_line_angle_gates` 시그너처 확장 + out-of-scope 분기 신설 (lines 323-346 박제 갱신):
    ```python
    # B1 fix 박제 — 게이트 verdict 박제 3-state literal
    # "PASS" = 채점 영역 통과, "FAIL" = 채점 영역 미통과, "out_of_scope_PASS" = 채점 영역 외 (ref-climb 등) - PASS counted
    # W4 fix 박제 (2026-06-04 iter 2) — Literal import 명시:
    from typing import Literal  # noqa: E402  (action snippet 최상단 import 박제)
    GateVerdict = Literal["PASS", "FAIL", "out_of_scope_PASS"]
    
    def compute_line_angle_gates(
        joint_angles: np.ndarray,
        pole_axis: PoleAxis,
        recognizer = None,  # Plan 5-05 박제 — DI. None = FallbackRecognizer default (회귀 보존)
        video_path: str | None = None,  # Plan 5-05 박제 — GeminiTechniqueRecognizer 의 frames 인자
    ) -> tuple[GateVerdict, GateVerdict]:
        """line_score + stability_score (angle proxy) 에서 게이트 PASS 여부 계산.
        
        B1 fix (2026-06-04 revision belle 박제 결정):
        - line_score 또는 angle_score = None (yaml hold_moment 빈 list, 예: ref-climb) → "out_of_scope_PASS" 박제
          (False 박제 X — D-20 ref-climb 차원 별 phase 책임 정합)
        - 채점 영역 모션 (line/angle score 산출 가능) → "PASS" / "FAIL" 박제
        - 박제 [[gap-and-line-angle-mandatory-gates.md]] "강등/우회 금지" 정신 정합 = 정의 정확화 (강등 X)
        
        T-23-03 박제 (기존): None = N/A — 단, B1 fix 박제로 "N/A 박제 = 별 phase 채점 영역" 의도된 박제 시
        "out_of_scope_PASS" counted (D-20 정합). 의도되지 않은 N/A 는 여전히 "FAIL".
        
        Plan 5-05 박제: recognizer + video_path 인자 추가 (Gemini 어댑터 주입 path).
        """
        if recognizer is None:
            from sunity_shared.analysis.technique import FallbackRecognizer
            recognizer = FallbackRecognizer()
        
        # Plan 5-05 박제 — GeminiTechniqueRecognizer 는 frames=video_path 사용
        profile = recognizer.recognize(joint_angles, frames=video_path)
        
        ls = line_score(joint_angles, profile)
        # B1 fix 박제: profile.joint_expectations 가 빈 dict (ref-climb yaml hold_moment 빈 list 정합)
        # 또는 ls = None (line_score 산출 불가) 시 = "out_of_scope_PASS"
        if not profile.joint_expectations:
            # ref-climb 같은 out-of-scope 모션 — yaml hold_moment 빈 list (Plan 5-00 박제)
            line_verdict: GateVerdict = "out_of_scope_PASS"
            angle_verdict: GateVerdict = "out_of_scope_PASS"
        else:
            line_verdict = "PASS" if (ls is not None and ls >= 50) else "FAIL"
            ss = stability_score(joint_angles)
            angle_verdict = "PASS" if ss >= 50 else "FAIL"
        
        return line_verdict, angle_verdict
    
    
    def gate_passed(verdict: GateVerdict) -> bool:
        """B1 fix 박제 — verdict counter (X/N) 산정 helper.
        
        PASS 와 out_of_scope_PASS 모두 PASS counted (D-20 정합).
        """
        return verdict in ("PASS", "out_of_scope_PASS")
    
    
    def is_in_scope(verdict: GateVerdict) -> bool:
        """B1 fix 박제 — 채점 영역 모션 카운트 (out_of_scope 박제 제외).
        
        verdict 표의 분모 N = 채점 영역 모션 수 산정용.
        """
        return verdict != "out_of_scope_PASS"
    ```
    
    **변경 3-bis (B1 fix 박제 정합)** — 기존 sweep main / write_report 박제 갱신:
    - `compute_line_angle_gates` 반환 = `tuple[GateVerdict, GateVerdict]` (bool 박제 X)
    - sweep main loop 안에서 `line_pass = gate_passed(line_verdict)`, `angle_pass = gate_passed(angle_verdict)` 박제
    - verdict 산정 박제 = `line_pass_count = sum(gate_passed(v) for v in line_verdicts)` + `line_inscope_count = sum(is_in_scope(v) for v in line_verdicts)` → verdict 표 = `line PASS {line_pass_count}/{line_inscope_count + out_of_scope_count}` (총 N=5 박제, 분자 = out_of_scope_PASS counted)
    - report.md 박제 갱신: `**angle PASS:** 4/4 (채점 영역 모션, ref-climb out-of-scope counted as PASS)` 형식 박제

    **변경 4** — sweep main loop 안에서 recognizer 1회 생성 + 5영상 loop 안에서 재사용 (cache hit 효과):
    ```python
    # main() 또는 sweep 함수 안에서
    recognizer = _build_recognizer(args.recognizer)
    log.info("sweep recognizer = %s (instance type=%s)", args.recognizer, type(recognizer).__name__)
    
    # 5영상 loop:
    for motion in motions:
        # ... pose 산출 ...
        line_pass, angle_pass = compute_line_angle_gates(
            joint_angles, pole_axis, recognizer=recognizer, video_path=local_video_path,
        )
        # ... verdict 박제 ...
    ```

    **변경 5** — report.md 박제 갱신 (sweep 결과 출력에 recognizer + verdict 박제):
    - report.md 의 header 에 `**Recognizer:** {fallback|gemini}` 박제
    - verdict table 의 박제 — Phase 5 게이트 ("정은지 reference 기준 5/5 PASS") 통과 여부 박제
    - `phase5_ready_to_release_d16_block: True | False` 박제 (Plan 24/25 D-16 보류 해제 게이트 정합)

    **테스트** `backend/tests/test_compare_rtmw_vs_ipsf_recognizer_flag.py` 신설:
    - `test_recognizer_flag_default_is_fallback`: argparse default 박제
    - `test_recognizer_flag_accepts_gemini`: `--recognizer gemini` 인식
    - `test_recognizer_flag_rejects_invalid`: `--recognizer foo` → SystemExit (argparse choices)
    - `test_build_recognizer_fallback`: `_build_recognizer("fallback")` → FallbackRecognizer instance
    - `test_build_recognizer_gemini`: `_build_recognizer("gemini")` → GeminiTechniqueRecognizer instance + cache 박제 + hook 박제
    - `test_compute_line_angle_gates_default_recognizer_none`: recognizer=None → FallbackRecognizer default (회귀 보존)
    - `test_compute_line_angle_gates_uses_injected_recognizer`: mock recognizer 주입 → recognize 호출 검증
    - `test_compute_line_angle_gates_passes_video_path_to_recognizer`: video_path 인자 전달 검증

    박제 정신 (D-01, D-12, D-14, [[gap-and-line-angle-mandatory-gates]], [[gsd-pod-work-push-first]]):
    - default 박제 = fallback (회귀 0)
    - cli choices 강제 (잘못된 값 0)
    - recognizer 1회 생성 + 재사용 (cache 효과 보존)
    - video_path 인자 박제 (Gemini 어댑터 frames 의존성 정합)
    - 게이트 정의 박제 = "정은지 reference 기준 5/5 PASS" report.md 명시
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_compare_rtmw_vs_ipsf_recognizer_flag.py -x --no-header -q && pytest tests/test_compare_rtmw_vs_ipsf_smoke.py -x --no-header -q && pytest tests/test_compare_rtmw_vs_ipsf_pole_axis.py -x --no-header -q</automated>
  </verify>
  <done>
    sweep CLI flag 박제 + DI 박제 + 단위 테스트 8 PASS + 기존 sweep smoke + pole_axis 테스트 회귀 0.
  </done>
  <acceptance_criteria>
    - `compare_rtmw_vs_ipsf.py` 에 `--recognizer` flag 박제 (`grep -c '\-\-recognizer' compare_rtmw_vs_ipsf.py` >= 1)
    - `_build_recognizer` 함수 박제 (grep >= 1)
    - `compute_line_angle_gates` 시그너처에 `recognizer=` + `video_path=` 인자 추가
    - default = fallback 박제 정합 (argparse default 검증)
    - **B7 fix 검증 (2026-06-04 revision)**: 기존 `--videos` / `--output-dir` flag 호환 보존 — `--recognizer` flag 만 추가 박제, `--motions` / `--output` 신설 박제 X. `python -m research.evaluations.compare_rtmw_vs_ipsf --help` 출력 안에 `--videos`, `--output-dir`, `--recognizer` 3 flag 박제 (`--motions` / `--output` 박제 X).
    - **B1 fix 검증 (2026-06-04 revision)**:
      - `compute_line_angle_gates` 반환 type = `tuple[GateVerdict, GateVerdict]` (bool 박제 X)
      - `gate_passed` / `is_in_scope` helper 함수 박제 (`grep -c 'def gate_passed\|def is_in_scope' compare_rtmw_vs_ipsf.py` ≥ 2)
      - 단위 시험: ref-climb (joint_expectations 빈) → `("out_of_scope_PASS", "out_of_scope_PASS")` 반환 박제
      - 단위 시험: ref-foxtop 정상 채점 → `("PASS"|"FAIL", "PASS"|"FAIL")` 반환 박제
      - report.md 박제 verdict 표 = `angle PASS {pass_count}/{inscope_count}` 형식 박제 (out_of_scope 분리 박제)
    - `backend/tests/test_compare_rtmw_vs_ipsf_recognizer_flag.py` 신설 + line count >= 60 + test 함수 >= 8개
    - `pytest tests/test_compare_rtmw_vs_ipsf_recognizer_flag.py -x` PASS
    - 기존 sweep smoke + pole_axis 테스트 회귀 0 (Plan 23 박제 11 PASS 보존)
    - report.md 박제 헤더에 `**Recognizer:**` + `phase5_ready_to_release_d16_block` 필드
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: belle Pod sweep --recognizer gemini 실행 + verdict 박제 + Phase 5 게이트 판정</name>
  <what-built>
    Task 1 박제로 `compare_rtmw_vs_ipsf.py --recognizer gemini` 가 실행 가능. belle 가 Pod 에서 sweep 실행 → 5영상 verdict 박제 → Phase 5 게이트 ("정은지 reference 측정값 기준 5/5 PASS", D-01) 통과 판정.
  </what-built>
  <how-to-verify>
    1. **Pod 준비 (belle 단계, [[gsd-pod-work-push-first]] 박제)**:
       a. 로컬에서 Plan 5-00 ~ 5-04 박제 변경 commit + `git push origin main`
       b. Pod 에서 `cd /workspace/SunityMotion && git pull`
       c. Pod 에서 Plan 5-04 박제 dependency 설치:
          ```bash
          cd backend
          pip install -r runpod_inference/requirements.txt  # google-genai install
          # 또는 직접: pip install 'google-genai>=1.0,<2.0'
          ```
       d. Pod env 박제:
          ```bash
          export GEMINI_API_KEY=$(aws ssm get-parameter \
            --name /sunity/motion/gemini-api-key \
            --with-decryption --query 'Parameter.Value' \
            --output text --region ap-northeast-2)
          export RECOGNIZER_BACKEND=gemini  # 또는 sweep CLI flag 사용
          ```
       e. **(W6 fix 2026-06-04 revision: mandatory step 박제)** startup fail-loud 검증:
          ```bash
          uvicorn runpod_inference.server:app --host 0.0.0.0 --port 8000 --workers 1 &
          UVICORN_PID=$!
          sleep 10
          # 박제 log 확인 (CloudWatch 또는 stdout)
          # 박제 = "Recognizer 워밍업 완료 — type=GeminiTechniqueRecognizer"
          # 박제 = "Gemini API 키 검증 완료"
          # 박제 X = "Gemini API 키 검증 실패" 또는 RuntimeError trace
          curl -sf http://localhost:8000/health || echo "health check FAIL"
          kill $UVICORN_PID
          ```
          박제 결정 항목 (G) = `startup fail-loud 검증 결과: PASS | FAIL` 박제. FAIL 시 = Pod env 박제 누락 (GEMINI_API_KEY 또는 SSM IAM) → belle 가 박제 후 재시도. PASS 시 = sweep 진입.

    2. **belle Pod sweep 실행 (B7 fix 2026-06-04 revision: 실 CLI flag `--videos` / `--output-dir` 사용, `--motions`/`--output` 미존재)**:
       ```bash
       cd /workspace/SunityMotion/backend
       python -m research.evaluations.compare_rtmw_vs_ipsf \
         --recognizer gemini \
         --videos s3://sunity-motion-pilot-videos/reference/ref-climb.mp4 \
                  s3://sunity-motion-pilot-videos/reference/ref-foxtop.mp4 \
                  s3://sunity-motion-pilot-videos/reference/ref-foxtop-split.mp4 \
                  s3://sunity-motion-pilot-videos/reference/ref-invert.mp4 \
                  s3://sunity-motion-pilot-videos/reference/ref-sideway-spin.mp4 \
         --output-dir research/evaluations/reports/sweep_rtmw_gemini/
       ```
       박제 정합: Task 1 acceptance 박제 — 기존 `--videos` / `--output-dir` flag 호환 보존 (시그너처 변경 0 + `--recognizer` 만 추가 박제).
       
       실행 시간 예상: 5영상 × Gemini 호출 ~10초 = 약 1~2분 (첫 sweep, cache miss). 두 번째 이후 = 5초 미만 (cache hit).

    3. **belle 결과 검토** — `research/evaluations/reports/sweep_rtmw_gemini/report.md` 박제 데이터:
       
       | 게이트 | 박제 기준 | Plan 23 박제 (2026-06-03) | Plan 5-05 박제 (2026-06-04+) |
       |---|---|---|---|
       | IPSF within_tolerance | 5/5 PASS | 1/5 | ? |
       | line PASS | 5/5 PASS | 3/5 | ? |
       | angle PASS | 5/5 PASS | 0/5 | ? |
       
       **B1 fix (2026-06-04 revision belle 박제 결정)** 게이트 정의 정정 = `"ref-climb 제외 4/4 PASS + ref-climb out-of-scope counted as PASS"` (D-20 박제 정신 정합 — ref-climb 차원 = 별 phase 책임). D-01 박제는 orchestrator 가 별 step 으로 CONTEXT.md 갱신. Plan 5-00 박제 yaml source 정정 후 + Plan 5-01 어댑터 박제 후 sweep 결과.

    4. **belle 결정 항목 (REPLY 형식)**:
       - **(A) 5영상 IPSF within_tolerance 결과**: X/N 박제 (B1 fix 박제: X = PASS counted, N = 채점 영역 모션 수 + out_of_scope_PASS counted)
       - **(B) 5영상 line PASS 결과**: X/N 박제 (B1 fix 박제 정합)
       - **(C) 5영상 angle PASS 결과**: X/N 박제 (B1 fix 박제: ref-climb = out_of_scope_PASS counted)
       - **(D) Gemini 응답 좌표/점수/판단 reject pattern 위반**: 0건 / X건 (reject patterns 가드 검증)
       - **(E) Gemini motion 분류 정확도**: 5/5 / X/5 (5영상 의도된 motion 으로 분류했는지)
       - **(F) Phase 5 게이트 verdict**: `pass` / `partial` / `fail` 박제 (B1 fix 박제 게이트 = "ref-climb 제외 4/4 PASS + ref-climb out-of-scope counted as PASS")
       - **(G) startup fail-loud 검증 결과**: `PASS` / `FAIL` 박제 (W6 fix 2026-06-04 revision: mandatory step e 결과)
         - `pass` = 5/5 (or D-01 게이트 통과) → Plan 24/25 D-16 보류 해제 진입
         - `partial` = 3~4/5 → root cause 박제 + 후속 plan (예: Low conf threshold D-10, motion 정규화 spike 추가)
         - `fail` = 0~2/5 → 별 plan 후퇴 (예: Gemini 모델 변경 plan 평가, scope 재검토)

    5. **승인 (F=pass) 후** = Plan 24/25 D-16 보류 해제 진입 (Phase 1 잔여 + 정상 출시 path). `partial`/`fail` 시 후속 plan 박제 분리.

    6. **Pod commit + push**: belle 가 Pod 에서 `git add research/evaluations/reports/sweep_rtmw_gemini/ && git commit -m "data(05-05): belle Pod sweep gemini 결과 박제 — verdict={F}" && git push origin main`. 로컬 git pull 후 verdict 박제 확인.
  </how-to-verify>
  <resume-signal>belle 가 (A)~(G) 박제 답변 후 "Phase 5 게이트 verdict = pass" (또는 partial/fail) 입력</resume-signal>
  <acceptance_criteria>
    - belle 의 (A)~(G) 항목 박제 답변 모두 박제됨 (서면 박제)
    - report.md git 커밋 + push 완료
    - (D) reject pattern 위반 = 0건 ([[analysis-objectivity-no-human-scores]] 박제 정신 정합 검증)
    - **(G) startup fail-loud 검증 = PASS 박제 (W6 fix 2026-06-04 revision mandatory step)** — FAIL 박제 시 sweep 진입 금지, belle Pod env 박제 후 재시도
    - F=pass 시 — Plan 24/25 D-16 보류 해제 진입 권한 박제 (STATE.md 갱신 권장)
    - F=partial 시 — root cause 박제 + 후속 plan 5-06 (Low conf threshold) 또는 별 plan 박제
    - F=fail 시 — Phase 5 후퇴 plan 박제 (Gemini 모델 변경 또는 scope 재검토)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sweep CLI → recognizer factory | argparse choices 강제 박제 |
| Gemini 응답 → sweep result | reject patterns 가드 + 정규화 박제 |
| Pod sweep → Firestore cache | TechniqueCache 박제 정합 (재실행 시 cache hit) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Tampering | sweep verdict 위조 (잘못된 5/5 PASS 박제) | mitigate | report.md 박제 정합 검증 (raw Gemini 응답 + measured 각도 함께 박제) + belle 검토 박제 (blocking-human) |
| T-05-05-02 | Information Disclosure | report.md 에 GEMINI_API_KEY 노출 | mitigate | report.md 박제 = sweep 결과만 (env 값 미박제) |
| T-05-05-03 | Tampering | Gemini 응답 reject pattern 우회 (좌표/점수/판단) | mitigate | Plan 5-01 박제 2차 가드 + sweep 결과에 reject pattern 위반 카운트 박제 (D 항목) |
| T-05-05-04 | Denial of Service | Gemini API quota 초과 시 sweep 중단 | accept | D-14 박제 cache hit 효과로 재실행 비용 0. 첫 sweep 실패 시 Pod 재시도 |
| T-05-05-05 | Repudiation | belle 미승인 시 Plan 24/25 진입 위험 | mitigate | blocking-human checkpoint — F=pass 명시 박제 없이는 진입 권한 X |
| T-05-05-SC | Tampering | npm/pip/cargo installs | mitigate | 본 plan 신규 install 0 — Plan 5-04 박제 의존성 재사용 |
</threat_model>

<verification>
- `--recognizer` CLI flag 박제 + 단위 테스트 8 PASS
- compute_line_angle_gates DI 박제 + 회귀 0 (sweep smoke + pole_axis 테스트 PASS)
- belle Pod sweep 5영상 실행 + report.md 박제
- 게이트 verdict 박제 (A~F 6 항목)
- reject pattern 위반 0건 박제 (D 항목)
- 박제 spec 정합 (D-01 / D-12 / D-14 / [[gap-and-line-angle-mandatory-gates]] / [[gsd-pod-work-push-first]])
</verification>

<success_criteria>
- sweep CLI flag 박제 + 단위 테스트 PASS
- belle Pod sweep verdict 박제 + belle 승인
- F=pass 시 Plan 24/25 D-16 보류 해제 진입 권한 박제
- F=partial 시 후속 plan 박제 (Low conf threshold 등)
- F=fail 시 후퇴 plan 박제 (scope 재검토 등)
</success_criteria>

<output>
Create `.planning/phases/05-gemini/05-05-SUMMARY.md` when done. Include:
- sweep `--recognizer gemini` flag + DI 박제 diff
- belle Pod sweep 실행 결과 (5영상 × IPSF within_tolerance / line / angle 게이트)
- Gemini 응답 raw excerpt (좌표/점수/판단 reject 0건 검증)
- belle 박제 verdict (A~F 6 항목)
- Phase 5 게이트 통과 / 부분 통과 / 미통과 결정
- Plan 24/25 진입 권한 박제 (F=pass 시) 또는 후속 plan 박제 (F=partial/fail 시)
- 박제 spec 정합 확인 (D-01 / D-12 / D-14 / 박제 정신 4종)
</output>
