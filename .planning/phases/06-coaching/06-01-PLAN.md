---
phase: 06-coaching
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/shared/python/sunity_shared/analysis/body_normalizer.py
  - backend/shared/python/sunity_shared/models.py
  - app/src/types/analysis.ts
  - docs/contract.md
  - backend/tests/phase06/__init__.py
  - backend/tests/phase06/conftest.py
  - backend/tests/phase06/fixtures/__init__.py
  - backend/tests/phase06/fixtures/_factory.py
  - backend/tests/phase06/fixtures/test_fixtures_loadable.py
  - backend/tests/phase06/fixtures/fixture_160cm_pro_vs_140cm_student.json
  - backend/tests/phase06/fixtures/fixture_lefty_vs_righty_twist.json
  - backend/tests/phase06/fixtures/fixture_foreshortening_lying_pose.json
  - backend/tests/phase06/fixtures/fixture_unstable_arm_swing.json
  - backend/tests/phase06/fixtures/fixture_split_angle_hipline.json
  - backend/tests/phase06/fixtures/fixture_high_dispersion_arms_sprawled.json
  - backend/tests/phase06/test_body_normalizer_kinematic_tree.py
  - backend/tests/phase06/test_body_normalizer_confidence.py
  - backend/tests/phase06/test_body_normalizer_ipsf_deficit.py
  - backend/tests/phase06/test_compare_body_profiles.py
  - backend/tests/phase06/test_body_comparison_report_lockstep.py
autonomous: true
requirements: [PERS-01]
tags: [body-normalization, kinematic-tree, ipsf-deficit, comparison-type, phase-06]

must_haves:
  truths:
    - "프로/수강생 BodyNormalizationProfile 차이로 ScaleProfile(5 필드) 이 산출되고 Kinematic Tree Bone-Length Reprojection (NotebookLM §1.1) 으로 세그먼트별 상대 좌표가 재계산된다 (D-06 success #1)"
    - "단순 확대/축소가 아닌 세그먼트별 정규화 (`normalize_pose_by_segments`) 가 구현된다 — root→leaf 순차 적용, 방향 벡터 보존, 뼈 길이만 target(student) 비율로 덮어쓴다. 시그너처 = `(source_keypoints, source_profile, target_profile, target_torso_px)` 이며 L_ref 산식은 `_ref_segment_ratio(parent, child, target_profile) × target_torso_px` 이다 (D-06 success #2, C1 fix 2026-06-08 revision — target=student 의 segment ratio 사용, source(pro) 의 비율은 방향만 제공)"
    - "L_ref 는 source(pro) profile 이 아닌 target(student) profile 의 segment ratio 로 산출된다 — pro 의 모션 방향은 보존되되 뼈 길이는 student 본인 신체 비율을 그대로 따른다. 동일 키 + 다른 팔/다리 비율 두 학생은 reproject 후 서로 다른 정규화 좌표를 받는다 (C1 fix — segment-aware ≠ uniform scale). **R10 fix (2026-06-08 round-2): source_profile 인자는 reserved/debug 용. 동일 source_keypoints + target_profile + target_torso_px 이면 source_profile 의 값과 무관하게 출력 동일 — `test_normalize_pose_by_segments_output_independent_of_source_profile` 가 회귀 방지.**"
    - "동일 동작에서 체형이 다른 두 사용자 (160cm pro + 140cm student fixture) 가 각자 체형 비율 기준의 점수를 받는다 — IPSF GeometricCriterion 절대 deficit 은 정규화된 keypoint 에서 측정, 체형 ratio 를 deficit 에 직접 곱하지 않는다 (D-06 success #3 + Notebook §3.3)"
    - "**R9 fix (2026-06-08 round-2)**: deficit 구성 = **5 IPSF geometric deficits (knee_toe_alignment / clean_lines / extension / posture / body_placement) + Sunity 자체 `pose_reliability_low` (카메라 occlusion proxy, C14 fix). `poor_transitions` 는 v1.5 deferred (Phase 8 jerk/jitter 와 통합)**. '7종' 이라는 옛 표현은 사용 금지."
    - "coaching 모드 출력에 `bodyNormalizationConfidence` 가 항상 포함된다 — temporal variance (5-10%) + spatial dispersion 통합 산식, 0~1 범위, comparisonType 무관 (D-06 success #4 + D-06-U1). 본 Plan 의 알고리즘 본체는 `pose_frames` 가 주어졌을 때 full 산식 사용 (Plan 06-02 wiring 에서 production path 연결)"
    - "**R5 fix (2026-06-08 round-2)**: spatial_dispersion_penalty 산식 = `clip((C_s / shoulder_width - DISPERSION_BASELINE) / DISPERSION_RANGE, 0, 1)` (DISPERSION_BASELINE=1.5, DISPERSION_RANGE=1.5). 자연스러운 방향: high dispersion → high penalty. warning name = `spatial_dispersion_high` (산식과 의미 일치). 신규 fixture `fixture_high_dispersion_arms_sprawled` 가 high-dispersion case 의 confidence 감소를 검증."
    - "**R6 fix (2026-06-08 round-2)**: PoseFrame → keypoints 변환은 `to_coco17_array(pose_frames)` (이미 pose_frame.py:325 존재) 를 사용해 `(T, 17, 4)` 4채널 (x, y, z, uncertainty_proxy) 을 보존. 별도 `(T, 17, 3)` stack 금지 — `joint_uncertainty` 가 4번째 채널을 활용해야 temporal_fill 이 의미를 가진다. `test_to_coco17_array_preserves_confidence_channel` 회귀 방지."
    - "Universal Principle (D-06-U1) 발현: confidence < 0.5 시 정규화 OFF + raw 비교 + warning, confidence >= 0.5 시 5 필드 정규화 ON + 모든 deficit 출력"
    - "comparisonType 3 분기 (mode1 / mode3_first / mode3_progress) 가 단일 `BodyComparisonReport` schema 로 통합 (D-06-B3 — 3 ComparisonType 만). mode3_first Gemini fallback 매칭 시 동일 comparisonType='mode3_first' 유지 + sibling 필드 `usedReferenceFallback: bool` (default false) (W1)"
    - "C14 fix: deficit code `bad_angle` 은 `pose_reliability_low` 로 rename. IPSF Page 21 의 judge-observation deduction (카메라 가림으로 심판이 각도를 못 봄) 과 본 시스템의 'pose confidence frame 비율 측정' 을 구분. docs/contract.md §8 에 IPSF semantic divergence 명시"
    - "**R2 fix (2026-06-08 round-2)**: BodyComparisonReport 의 reference 측 입력은 (1) `BodyNormalizationProfile` (target_profile 산출용 segment ratio) + (2) **신규 `BodyComparisonSourcePose` (대표 hold frame 의 17 keypoint 4채널 flat)** 두 종류. Plan 06-03 백필이 reference 컬렉션에 둘 다 저장. mode1 + mode3 fallback path 가 둘 다 fetch — source_pose 미존재 시 warnings ⊃ 'reference_source_pose_missing' + confidence 하향. Firestore nested-array 회피를 위해 source_pose 의 `values` 는 flat float array (length = 4 × len(jointKeys))."
    - "**R8 fix (2026-06-08 round-2)**: `compare_body_profiles(..., extra_warnings: list[str] | None = None)` 시그너처. caller (Plan 06-02 pipeline) 가 'fallback_reference_not_found' / 'reference_source_pose_missing' 등 외부 신호 warning 을 인자로 주입. 내부에서 기존 warnings 와 merge + 7 enum frozenset 으로 validate. `dataclasses.replace` 우회 금지 — validation 보장."
  artifacts:
    - path: backend/shared/python/sunity_shared/analysis/body_normalizer.py
      provides: "Kinematic Tree reproject (target-profile-based L_ref, C1 fix) + ScaleProfile/BodyComparisonFinding/BodyComparisonReport/BodyComparisonSourcePose dataclass + confidence 산식 (R5 spatial dispersion + R6 4채널 보존) + IPSF absolute deficit (pose_reliability_low rename, C14 fix) + usedReferenceFallback 필드 + compare_body_profiles extra_warnings 파라미터 (R8)"
      contains: ["def normalize_pose_by_segments(", "def compute_body_normalization_confidence(", "def measure_ipsf_absolute_deficits(", "def compare_body_profiles(", "class BodyComparisonReport", "class BodyComparisonFinding", "class BodyComparisonSourcePose", "class ScaleProfile", "KINEMATIC_TREE_EDGES", "CONFIDENCE_GATE = 0.5", "DISPERSION_BASELINE", "DISPERSION_RANGE", "used_reference_fallback", "pose_reliability_low", "extra_warnings"]
    - path: app/src/types/analysis.ts
      provides: "TS contract — BodyComparisonReport + ComparisonType (3 cases) + ScaleProfile + BodyComparisonFinding + BodyComparisonSourcePose (R2) + usedReferenceFallback boolean + AnalysisResult.bodyComparisonReport + ReferenceMotion.bodyNormalizationProfile + ReferenceMotion.bodyComparisonSourcePose (R2). deficit code 'pose_reliability_low' (C14 fix)"
      contains: ["export interface BodyComparisonReport", "export type ComparisonType", "export interface ScaleProfile", "export interface BodyComparisonFinding", "export interface BodyComparisonSourcePose", "usedReferenceFallback", "pose_reliability_low"]
    - path: backend/shared/python/sunity_shared/models.py
      provides: "Python re-export of BodyComparisonReport family + BodyComparisonSourcePose (R2) + 3-way lockstep header"
      contains: ["from .analysis.body_normalizer import", "BodyComparisonReport", "BodyComparisonSourcePose"]
    - path: docs/contract.md
      provides: "§8 BodyComparisonReport 명세 (3-way 동시 갱신 헤더) + 3 ComparisonType + usedReferenceFallback 보조 필드 + IPSF divergence note (C14) + §8.2 BodyComparisonSourcePose 명세 (R2 — flat values, Firestore nested-array 회피)"
      contains: ["§8", "BodyComparisonReport", "comparisonType", "usedReferenceFallback", "pose_reliability_low", "BodyComparisonSourcePose"]
    - path: backend/tests/phase06/test_body_comparison_report_lockstep.py
      provides: "3-way lockstep drift defense (TS interface ↔ Python dataclass ↔ docs/contract.md §8) + usedReferenceFallback + pose_reliability_low + BodyComparisonSourcePose 검증 (R2)"
  key_links:
    - from: app/src/types/analysis.ts
      to: backend/shared/python/sunity_shared/analysis/body_normalizer.py
      via: "3-way contract — camelCase TS ↔ snake_case Python ↔ docs §8 atomic commit"
      pattern: "BodyComparisonReport"
    - from: backend/shared/python/sunity_shared/models.py
      to: backend/shared/python/sunity_shared/analysis/body_normalizer.py
      via: "from .analysis.body_normalizer import BodyComparisonReport, BodyComparisonFinding, BodyComparisonSourcePose, ScaleProfile, ComparisonType"
      pattern: "from .analysis.body_normalizer import"
---

<objective>
Phase 6 vertical slice **알고리즘 + contract 본체** — 체형 정규화 비교 엔진의 본체 함수 + dataclass + 데이터 contract + 단위 테스트를 단일 plan 으로. 본 Plan 의 산출물 = pure-function 본체 + 5 fixture pure-function 테스트 + 3-way contract lockstep. **파이프라인 wiring (mode1/mode3/Gemini fallback 분기 + Firestore complete_analysis 확장 + frontend type sync + SAM build smoke) 은 Plan 06-02 책임.**

**2026-06-08 revision (cross-AI review C1/C14 incorporated):**
- C1: `normalize_pose_by_segments` 시그너처를 `(source_keypoints, source_profile, target_profile, target_torso_px)` 로 변경. L_ref 는 target(student) 의 segment ratio 사용. 기존 plan 의 "pro_profile 비율 × student_torso" 는 uniform scale 로 degenerate 되므로 폐기.
- C14: deficit code `bad_angle` → `pose_reliability_low` 로 rename. IPSF judge-observation deduction 과 본 시스템의 pose confidence frame ratio 측정의 의미 차이를 contract 에 명시.

**2026-06-08 round-2 revision (Codex Direct Review R5/R6/R8/R9/R10 + R2 contract incorporated):**
- **R2 (HIGH)**: 신규 dataclass `BodyComparisonSourcePose` 추가 — reference 측 대표 hold frame 의 17 keypoint × 4채널 flat 저장. `compare_body_profiles` 가 `source_keypoints` 인자로 받음. Plan 06-03 백필이 Firestore reference 컬렉션에 `bodyComparisonSourcePose` flat dict 저장. mode1 + mode3 fallback path 가 둘 다 read (Plan 06-02 wiring).
- **R5 (MEDIUM)**: spatial dispersion 산식 + 네이밍 수정. 신 산식 = `clip((C_s / shoulder_width - DISPERSION_BASELINE) / DISPERSION_RANGE, 0, 1)`. high dispersion → high penalty (자연 방향). 신규 fixture `fixture_high_dispersion_arms_sprawled`.
- **R6 (MEDIUM)**: `to_coco17_array(pose_frames)` (pose_frame.py:325) 사용해 `(T, 17, 4)` 4채널 보존. joint_uncertainty 가 4번째 채널 활용. `(T, 17, 3)` stack 패턴 금지.
- **R8 (MEDIUM)**: `compare_body_profiles` 시그너처에 `extra_warnings: list[str] | None = None` 추가. caller 가 외부 warning 신호를 주입 — 내부 validation 통과. `dataclasses.replace` 우회 패턴 금지.
- **R9 (LOW)**: must_haves.truths 의 "7 deficits" 문구를 "5 IPSF + Sunity pose_reliability_low; poor_transitions deferred" 로 정정.
- **R10 (LOW)**: `test_normalize_pose_by_segments_output_independent_of_source_profile` 회귀 방지 테스트 — source_profile 만 다른 두 호출의 출력이 동일.

Purpose:
- 분석 정확도 향상 — 정은지 41점 같은 체형 위양성 제거 (MPJPE 237.43mm → PA-MPJPE 91.04mm, NotebookLM §1.4)
- IPSF GeometricCriterion 절대 기준 정합 — 체형 ratio 를 deficit 에 직접 곱하지 않음 ([[scoring-dimensions-ipsf]] + Notebook §3.3)
- Universal Principle (D-06-U1) — confidence-tiered hybrid: 낮으면 안전 fallback, 높으면 모든 path 활성화
- 3-way contract lockstep — TS/Python/docs §8 동시 atomic commit (drift 방어)
- D-06-B3 honor — 3 ComparisonType enum + Gemini fallback 신호는 sibling boolean `usedReferenceFallback` (W1)
- **C1 fix (review)** — segment-aware reprojection 의 의미를 실제로 구현 (target profile 기반 L_ref)
- **C14 fix (review)** — IPSF semantic 과 구분되는 자체 신호 명명
- **R2/R5/R6/R8/R9/R10 (round-2 reviews)** — production reference keypoints 영속, dispersion 산식 자연화, 4채널 보존, warning injection validation, deficit 표현 일관, source_profile 회귀 방지

Output:
- 신규 `backend/shared/python/sunity_shared/analysis/body_normalizer.py` (pure numpy, ~450 LOC — BodyComparisonSourcePose 신설 포함)
- 갱신 `app/src/types/analysis.ts` + `backend/.../models.py` + `docs/contract.md §8 + §8.2` (3-way lockstep)
- 신규 `backend/tests/phase06/` 디렉토리 (6 fixture JSON — 신규 `fixture_high_dispersion_arms_sprawled` 포함 + sanity test + 4 algorithm test 파일 + conftest)
- 본 Plan 에서 X (Plan 06-02 책임): pipeline _process wiring, firestore_admin.complete_analysis 확장, frontend normalize, SAM build smoke
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-coaching/06-CONTEXT.md
@.planning/phases/06-coaching/06-RESEARCH.md
@.planning/phases/06-coaching/06-PATTERNS.md
@.planning/phases/06-coaching/06-NOTEBOOKLM-FINDINGS.md
@.planning/phases/06-coaching/06-VALIDATION.md
@.planning/phases/06-coaching/06-REVIEWS.md
</context>

## Phase Goal

**As a** 폴스포츠 수강생 (140cm), **I want to** 정은지 (160cm) 기준 모션과 본인 영상을 비교 분석 요청을 보내, **so that** 키 차이로 인한 위양성 감점 없이 자세 품질만 평가되어 BodyComparisonReport (comparisonType=mode1, scaleRatios=5 필드, findings=IPSF GeometricCriterion 절대 deficit, bodyNormalizationConfidence, usedReferenceFallback) 가 Firestore AnalysisDoc 에 저장되어 결과 화면이 받을 준비가 된다.

(MVP_MODE vertical slice: backend phase — UI 책임 없음. 본 Plan 은 algorithm + contract 본체; Plan 06-02 가 pipeline wiring 책임. UI 노출은 Phase 12 / 12.5 책임.)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| body_normalizer 모듈 입력 (raw keypoints + BodyNormalizationProfile dataclass + BodyComparisonSourcePose) | 본 Plan 의 진입 — caller (Plan 06-02 wiring) 가 검증 책임. 단 본 모듈은 numeric overflow 방어 (NaN/inf 입력 차단). BodyComparisonSourcePose 의 values 길이 / finite 검증은 `__post_init__` 수행. |
| 3-way contract (TS / Python / docs) | drift 방지 — atomic commit + lockstep test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Tampering | `BodyComparisonReport.__post_init__` numeric overflow (NaN/inf in scale ratio) | mitigate | `math.isfinite()` + 0~1 confidence range validator + `ValueError` raise (`body_normalization.py:121` 패턴 정합). 잘못된 input → caller (Plan 06-02) 가 `fail_analysis(server_error)` 호출 |
| T-06-01-02 | Tampering | `KINEMATIC_TREE_EDGES` 사이클 또는 누락 edge 로 인한 누적 좌표 drift | mitigate | 단위 테스트 `test_kinematic_tree_edges_are_dag` — 13 edge 가 mid_hip root, 사이클 없음, 모든 keypoint 도달 가능 검증 |
| T-06-01-03 | Tampering | 3-way contract drift (TS interface 만 갱신, Python dataclass 또는 docs 미갱신) | mitigate | `test_body_comparison_report_lockstep` 의 _FIELD_MAP 기반 양방향 검증 (camelCase ↔ snake_case). 모든 field 가 양쪽에 존재해야 통과 |
| T-06-01-04 | Information Disclosure | reference BodyProfile (정은지 키/체형 정보) 가 PII 일 수 있음 | accept | 본인 동의 + 공개 athletic 정보 (대회 record 기반). Firestore `reference/{motionId}` = 앱 read-only, admin SDK 만 write |
| T-06-01-05 | Tampering | BodyComparisonSourcePose.values 의 길이 / finite 위반 (Firestore 손상 데이터) | mitigate | `__post_init__` validator — `len(values) == 4 * len(joint_keys)`, 모든 값 `math.isfinite()`. 위반 시 `ValueError` raise. (R2 — flat float array 정합) |
| T-06-01-SC | Tampering | npm/pip install 없음 (numpy 만 사용, 기존 의존) | accept | 신규 외부 패키지 0. slopcheck 불요 |

(security_enforcement=true. ASVS V5 Input Validation = T-06-01-01/02/05. 본 Plan 에 신규 trust boundary 없음 — Plan 06-02 가 보안 확장 책임 (Firestore admin SDK uid 격리, nested-array 차단 validator).)
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — 6 fixture JSON + conftest + factory helper (Validation Architecture, R5 신규 fixture 추가)</name>
  <files>
    backend/tests/phase06/__init__.py,
    backend/tests/phase06/conftest.py,
    backend/tests/phase06/fixtures/__init__.py,
    backend/tests/phase06/fixtures/_factory.py,
    backend/tests/phase06/fixtures/test_fixtures_loadable.py,
    backend/tests/phase06/fixtures/fixture_160cm_pro_vs_140cm_student.json,
    backend/tests/phase06/fixtures/fixture_lefty_vs_righty_twist.json,
    backend/tests/phase06/fixtures/fixture_foreshortening_lying_pose.json,
    backend/tests/phase06/fixtures/fixture_unstable_arm_swing.json,
    backend/tests/phase06/fixtures/fixture_split_angle_hipline.json,
    backend/tests/phase06/fixtures/fixture_high_dispersion_arms_sprawled.json
  </files>
  <read_first>
    backend/tests/conftest.py,
    backend/tests/fixtures/body_normalization/_factory.py,
    backend/tests/fixtures/body_normalization/normal_pose_pose_frames.json,
    backend/tests/test_body_normalization_measurer.py,
    .planning/phases/06-coaching/06-NOTEBOOKLM-FINDINGS.md,
    .planning/phases/06-coaching/06-VALIDATION.md,
    backend/shared/python/sunity_shared/analysis/skeleton.py,
    backend/shared/python/sunity_shared/analysis/pose_frame.py
  </read_first>
  <behavior>
    - Test 1 (test_fixtures_loadable.py — sanity smoke): 6 fixture JSON 모두 `_factory.pose_frame_from_dict` 로 list[PoseFrame] 로 로딩 가능, 각 fixture >= 10 frame, 모든 frame 의 keypoints_3d 가 RTMW COCO-17 17 keypoint 정합
    - Test 2: fixture_160cm_pro_vs_140cm_student 는 "pro_frames" + "student_frames" 두 list 보유 (동일 동작, scale 만 다름)
    - Test 3: fixture_lefty_vs_righty_twist 는 Twist 자세 좌우 비대칭 (shoulder_hip_ratio 1.3 vs 0.77 표현) + intentional_asymmetry flag
    - Test 4: fixture_foreshortening_lying_pose 는 어깨-골반 픽셀 거리 < 150px + 카메라 Z축 < 60°
    - Test 5: fixture_unstable_arm_swing 는 armScale temporal variance > 10% (30 frame 에 걸쳐 팔 길이 픽셀 분산 보장)
    - Test 6: fixture_split_angle_hipline 는 동일 split 자세 두 변형 — short_leg vs long_leg PoseFrame. hip→knee 각도 동일, toe→toe Euclidean 다름
    - **Test 7 (R5 fix 신규)**: fixture_high_dispersion_arms_sprawled 는 팔이 어깨너비 대비 매우 넓게 펼쳐진 자세 — `C_s / shoulder_width` 평균 >= 3.0 (DISPERSION_BASELINE 1.5 의 2배 이상). 15 frame. metadata: `{"expected_dispersion_ratio": 3.2, "expected_spatial_dispersion_penalty": 1.0}` — Task 3 의 spatial_dispersion_penalty 가 high 출력하는지 검증 (Task 3 의 Test 와 짝).
  </behavior>
  <action>
Wave 0 — Plan 06-01 의 모든 후속 task 의 `<verify>` 가 이 fixture 에 의존. 신규 디렉토리 `backend/tests/phase06/` 생성 (기존 `backend/tests/` 의 conftest.py 가 path 주입 자동 — 별도 path 주입 X).

(A) `backend/tests/phase06/__init__.py` — 빈 파일 (Python package 표시).

(B) `backend/tests/phase06/conftest.py` — fixture 디렉토리 path 상수. 기존 `backend/tests/conftest.py` (parents[1]/shared/python path 주입) 가 그대로 상속 — 별도 path 주입 X. 모듈 docstring (한국어) "Phase 6 단위 테스트 공통 helper. Validation Architecture 6 fixture 정합 (R5 fix 신규 fixture_high_dispersion_arms_sprawled 포함)."

(C) `backend/tests/phase06/fixtures/__init__.py` — 빈 파일.

(D) `backend/tests/phase06/fixtures/_factory.py` — 신규 helper. 기존 `backend/tests/fixtures/body_normalization/_factory.py::pose_frame_from_dict` 재사용 경로 (re-export 권장). 추가 helper `load_paired_frames(name)` — fixture_160cm_pro_vs_140cm_student 의 두 list 분리 로딩.

(E) 6 fixture JSON 박제 — 합성 (synthetic) 데이터:
  - `fixture_160cm_pro_vs_140cm_student.json` — `{"pro_frames": [...], "student_frames": [...], "metadata": {"pro_height_cm": 160, "student_height_cm": 140, "scale_ratio": 0.875}}`. 동일 직립 자세 30 frame, scale 만 다름 (정은지 키포인트 픽셀 좌표 × 0.875 = 수강생). 각 frame = 17 keypoint dict (RTMW COCO-17, skeleton.KEYPOINT_NAMES 정합), confidence 0.9~1.0
  - `fixture_lefty_vs_righty_twist.json` — Twist 자세 (어깨 +30° / 골반 -30°). 10 frame. metadata: {"intentional_asymmetry": true, "expected_shoulder_hip_ratio": 1.3}
  - `fixture_foreshortening_lying_pose.json` — 카메라 시선 평행 누운 자세. 15 frame. 어깨-골반 픽셀 거리 = 100px (직립 평균 350px 의 28%). metadata: {"shoulder_hip_pixel_distance_avg": 100, "torso_camera_angle_deg": 30}
  - `fixture_unstable_arm_swing.json` — 빠른 팔 swing. 30 frame. 좌측 팔 길이 픽셀 표준편차 / 평균 = 0.15 (15% — Notebook §4.2 임계 10% 초과). metadata: {"arm_temporal_variance_ratio": 0.15}
  - `fixture_split_angle_hipline.json` — split 다리 자세 두 변형. `{"short_leg_frames": [...], "long_leg_frames": [...], "metadata": {"hip_knee_angle_deg": 175, "toe_to_toe_distance_short_px": 200, "toe_to_toe_distance_long_px": 350}}`. hip→knee 각도 동일 175°, ankle Euclidean 다름.
  - **`fixture_high_dispersion_arms_sprawled.json` (R5 fix 신규)** — 어깨너비 대비 팔이 매우 넓게 펼쳐진 자세 (예: T자 또는 X자). 15 frame. 양손 wrist 위치가 어깨에서 좌우로 shoulder_width × 2.5 이상 떨어지고, 양발 ankle 도 좌우로 넓게. `C_s / shoulder_width` 평균 >= 3.0. metadata: `{"expected_dispersion_ratio": 3.2, "expected_spatial_dispersion_penalty": 1.0, "rationale": "R5 fix — DISPERSION_BASELINE 1.5 의 2배 이상 → penalty=1.0. high dispersion → high penalty 의 자연 방향 검증용."}`

산식 (합성 데이터 생성 규칙) — Korean docstring 으로 `_factory.py` 모듈 헤더에: "본 fixture 는 합성. 실제 RTMW 출력 대용. 6 fixture 의 의도된 임계값은 NotebookLM Notebook 1 §1.4/1.5/3.4 + Notebook 4 §4.2 인용 + 2026-06-08 Codex Direct Review R5 (high dispersion)."

(F) 6 fixture sanity test `backend/tests/phase06/fixtures/test_fixtures_loadable.py` 신설 — Task 1 자체 검증 (Wave 0 selfcheck). 이 sanity test 가 GREEN 이면 Task 1 완료. Tasks 2~5 는 본 fixture 의존.

[per D-06-A1, D-06-A3, D-06-A4, D-06-B1, D-06-U1, Notebook 1 §1.4/§1.5/§3.4, Notebook 4 §4.2, R5 fix (2026-06-08 reviews round-2 — fixture_high_dispersion_arms_sprawled)]
  </action>
  <verify>
    <automated>cd backend && pytest tests/phase06/fixtures/test_fixtures_loadable.py -v</automated>
  </verify>
  <acceptance_criteria>
    - Source: 6 fixture JSON 파일 모두 `backend/tests/phase06/fixtures/` 존재, 각 파일 `json.load` 성공 + `pose_frame_from_dict` 변환 성공
    - Source: `backend/tests/phase06/conftest.py` 존재 + 한국어 docstring
    - Source: `backend/tests/phase06/fixtures/test_fixtures_loadable.py` 존재
    - Behavior: `pytest backend/tests/phase06/fixtures/test_fixtures_loadable.py -v` exits 0 + 6 fixture 전부 loadable
    - Behavior: fixture_160cm_pro_vs_140cm_student 의 metadata.scale_ratio == 0.875 + pro_frames/student_frames 둘 다 >= 10 frame
    - Behavior: fixture_split_angle_hipline 의 short_leg vs long_leg 의 hip→knee 각도 (cosine law) 차이 < 0.5° (동일 자세) + toe→toe Euclidean 거리 차이 > 100px
    - **Behavior (R5)**: fixture_high_dispersion_arms_sprawled 의 `C_s / shoulder_width` 평균 >= 3.0 + metadata.expected_dispersion_ratio == 3.2
  </acceptance_criteria>
  <done>Wave 0 — 6 fixture JSON + sanity test GREEN (R5 fix 신규 fixture 포함). Tasks 2~5 가 본 fixture 의존 진입 가능.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: body_normalizer.py 신규 모듈 — Kinematic Tree Reprojection (target-profile-based L_ref, C1 fix) + ScaleProfile + KINEMATIC_TREE_EDGES + 상수 + BodyComparisonSourcePose dataclass (R2 fix)</name>
  <files>
    backend/shared/python/sunity_shared/analysis/body_normalizer.py,
    backend/tests/phase06/test_body_normalizer_kinematic_tree.py
  </files>
  <read_first>
    backend/shared/python/sunity_shared/analysis/body_normalization.py,
    backend/shared/python/sunity_shared/analysis/body_normalization_measurer.py,
    backend/shared/python/sunity_shared/analysis/motiondtw.py,
    backend/shared/python/sunity_shared/analysis/dimensions.py,
    backend/shared/python/sunity_shared/analysis/skeleton.py,
    backend/shared/python/sunity_shared/analysis/pose_frame.py,
    .planning/phases/06-coaching/06-RESEARCH.md,
    .planning/phases/06-coaching/06-NOTEBOOKLM-FINDINGS.md,
    .planning/phases/06-coaching/06-REVIEWS.md,
    backend/tests/phase06/fixtures/fixture_160cm_pro_vs_140cm_student.json,
    backend/tests/phase06/fixtures/fixture_split_angle_hipline.json,
    backend/tests/phase06/fixtures/_factory.py
  </read_first>
  <behavior>
    - Test 1 (test_kinematic_tree_reproject_pro_to_student_140cm): source_keypoints = 160cm pro pose, source_profile = pro profile, target_profile = student profile (estimated_height_scale=0.875 등), target_torso_px = student 본인 torso 픽셀 → reproject 결과의 mid_hip→mid_shoulder 픽셀 거리 = target_torso_px × target_profile.torsoScale (1.0 self-reference) ± 1px
    - Test 2 (test_kinematic_tree_edges_are_dag): KINEMATIC_TREE_EDGES 13 edge 가 mid_hip root, 사이클 없음, RTMW COCO-17 모든 keypoint (mid_shoulder/mid_hip 가상 keypoint 포함) 도달 가능
    - Test 3 (test_split_angle_hipline_invariant_to_leg_length): fixture_split_angle_hipline 의 short_leg/long_leg → 정규화 후 hip→knee 라인 각도 동일 (Notebook §3.4 위양성 회피)
    - Test 4 (test_shoulder_hip_ratio_off_returns_neutral_width): `apply_shoulder_hip_ratio=False` 시 폭 보정 OFF, mid_shoulder ↔ {l,r}_shoulder ratio = 0.25 (neutral)
    - Test 5 (test_scale_profile_post_init_rejects_nan): ScaleProfile(estimated_height_scale=float('nan'), ...) → ValueError
    - Test 6 (test_normalize_direction_b_target_scale_is_student, C1 fix): source = 160cm pro keypoints + pro_profile, target_profile + target_torso_px = 140cm student → reproject 결과 bounding box 의 height 가 student 의 height scale 을 따르고, pro 의 height scale 을 따르지 않음 (방향 B / D-06-A2)
    - Test 7 (test_segment_aware_not_uniform_scale, C1 fix): 같은 height (target_torso_px 동일) 의 두 student profile 을 만든다. studentA = {armScale: 1.10, legScale: 0.95, torsoScale: 1.0, ...}, studentB = {armScale: 0.90, legScale: 1.10, torsoScale: 1.0, ...}. 동일 source (pro) keypoints + 동일 target_torso_px 으로 reproject → studentA 결과의 mid_shoulder→l_wrist 픽셀 거리 ≠ studentB 결과의 동일 거리 (segment-aware 정상). 차이 > 5% — uniform scale 이면 동일했어야 함. mid_hip→l_ankle 도 동일 검증 (leg 비율 차이 반영).
    - **Test 8 (R10 fix 신규 — test_normalize_pose_by_segments_output_independent_of_source_profile)**: 동일 source_keypoints + 동일 target_profile + 동일 target_torso_px 으로 두 번 호출하되 source_profile 만 다르게 (sourceA = pro_profile, sourceB = student_profile 자체 또는 무관한 임의 profile). 두 출력의 keypoint 좌표가 `np.allclose(out_a, out_b, atol=1e-6)` 로 동일. source_profile 인자가 현재 구현에 영향을 주지 않음을 확정 (debug/reserved 인자) — 향후 실수로 source_profile 의 segment ratio 를 사용하는 회귀 방지.
    - **Test 9 (R2 fix 신규 — test_body_comparison_source_pose_post_init_validates_flat_values)**: BodyComparisonSourcePose(joint_keys=("nose", "left_eye"), values=(1.0, 2.0, 3.0, 0.9), frame_index=0, torso_px=350.0, confidence=0.8, measured_at=1700000000000) → ValueError (values 길이 4 != 4 × 2 = 8 필요). 올바른 길이 (4 × 2 = 8) 는 PASS.
    - **Test 10 (R2 fix 신규 — test_body_comparison_source_pose_to_keypoints_array_reshape)**: BodyComparisonSourcePose(joint_keys=("nose", "left_eye"), values=(1.0, 2.0, 3.0, 0.9, 4.0, 5.0, 6.0, 0.8), ...).to_keypoints_array() → shape (2, 4) ndarray, [0]==[1.0, 2.0, 3.0, 0.9], [1]==[4.0, 5.0, 6.0, 0.8].
  </behavior>
  <action>
신규 모듈 `backend/shared/python/sunity_shared/analysis/body_normalizer.py` — 정규화 알고리즘 본체. pure numpy + stdlib (math/dataclasses/typing) 만, scipy/torch/외부 모델 의존 0. `from __future__ import annotations` 첫 줄.

(A) 모듈 docstring (한국어, decision history):
  - 첫 줄: `"""body_normalizer — Kinematic Tree Bone-Length Reprojection + IPSF GeometricCriterion 절대 deficit + bodyNormalizationConfidence 산출. pure numpy."""`
  - D-06-A1/A2/A3/A4/B1/B2/B3/U1 인용 (Universal Principle confidence-tiered hybrid)
  - 작성 일자 2026-06-08 + belle 직접 인용 ("분석이 제대로 되는 게 목표")
  - 메모리 인용: [[scoring-dimensions-ipsf]] (shoulderHipRatio 점수 차원 미적용), [[ipsf-5-track-scoring]] (Page 9 단독 트랙), [[license-blocklist-pose]] (SMPL/SMPL-X 금지), [[firestore-nested-array-flat]] (flat 저장)
  - 7 warning enum: `low_confidence_normalization_off`, `foreshortening_off`, `shoulder_hip_ratio_off`, `temporal_variance_high`, `spatial_dispersion_high`, `reference_profile_missing`, `fallback_reference_not_found`, **`reference_source_pose_missing` (R2 fix — 신규 8번째)** (총 8 enum)
  - **C1 fix (2026-06-08 reviews revision)**: `normalize_pose_by_segments` 시그너처 = `(source_keypoints, source_profile, target_profile, target_torso_px)`. L_ref = `_ref_segment_ratio(parent, child, target_profile) × target_torso_px`. target = student. source(pro) 의 segment ratio 는 무시되고 방향 벡터만 사용된다. source_profile 인자는 reserved (debugging 비교용) — 현 구현은 호출하지 않으나 시그너처 유지.
  - **C14 fix (2026-06-08 reviews revision)**: deficit code `bad_angle` → `pose_reliability_low`. IPSF Page 21 의 'bad angle' (judge 가 카메라 가림으로 angle 을 관찰 못함) 과 본 시스템의 'pose confidence frame ratio > 0.5' 측정은 의미가 다르다. divergence docs/contract.md §8.
  - **R2 fix (2026-06-08 round-2)**: 신규 dataclass `BodyComparisonSourcePose` — Firestore reference 컬렉션에 영속될 대표 hold frame 의 키포인트. flat `values` array (Firestore nested-array 회피) + reshape helper `to_keypoints_array()`. Plan 06-03 백필이 RTMW 결과의 대표 hold frame (hold_window 중앙 또는 confidence 최고 frame) 을 저장.
  - **R5 fix (2026-06-08 round-2)**: spatial_dispersion_penalty 산식 = `clip((C_s / shoulder_width - DISPERSION_BASELINE) / DISPERSION_RANGE, 0, 1)`. high dispersion → high penalty. DISPERSION_BASELINE=1.5, DISPERSION_RANGE=1.5.
  - **R6 fix (2026-06-08 round-2)**: `to_coco17_array(pose_frames)` (pose_frame.py:325 기존) 으로 (T,17,4) 4채널 보존.
  - **R8 fix (2026-06-08 round-2)**: `compare_body_profiles(..., extra_warnings: list[str] | None = None)`. caller injected warnings 가 내부 validation 거쳐 BodyComparisonReport.warnings 에 merge.

(B) 상수 (`body_normalization_measurer.py:37-73` 패턴 정합):
  - `KINEMATIC_TREE_EDGES: tuple[tuple[str, str], ...]` — 13 edge (RESEARCH.md §Pattern 1). mid_hip root → mid_shoulder, mid_shoulder → l/r_shoulder, l/r_shoulder → l/r_elbow → l/r_wrist, mid_hip → l/r_hip → l/r_knee → l/r_ankle
  - `CONFIDENCE_GATE: float = 0.5` (D-06-A4)
  - `FORESHORTENING_ANGLE_DEG: float = 60.0` (Notebook §1.5)
  - `SHOULDER_HIP_HARD_THRESHOLD_PX: float = 150.0` (Notebook §1.5)
  - `TEMPORAL_VARIANCE_RATIO_THRESHOLD: float = 0.10` (Notebook §4.2)
  - `TEMPORAL_VARIANCE_EXCELLENT_THRESHOLD: float = 0.05` (Notebook §4.2)
  - **`DISPERSION_BASELINE: float = 1.5` (R5 fix — 정상 자세의 C_s/shoulder_width 평균 추정값)**
  - **`DISPERSION_RANGE: float = 1.5` (R5 fix — high dispersion 까지의 range. baseline 1.5 + range 1.5 = 3.0 에서 penalty 1.0)**
  - `LINE_TOL_DEG: float = 20.0` (`dimensions.py:_LINE_TOL_DEG` 재사용)
  - `POSE_RELIABILITY_LOW_CONF_THRESHOLD: float = 0.4` (C14 fix — frame confidence 임계, 'pose_reliability_low' deficit 산출용)
  - `POSE_RELIABILITY_LOW_FRAME_RATIO: float = 0.5` (C14 fix — > 50% frame 이 임계 아래면 deficit)
  - `_EPS: float = 1e-8`
  - `BODY_COMPARISON_WARNING_CODES: frozenset[str]` — **8 warning enum frozen (reference_source_pose_missing 포함, R2 fix)**

(C) `ComparisonType: Literal["mode1", "mode3_first", "mode3_progress"]` (3 cases 만 — D-06-B3, W1). Gemini fallback 은 sibling boolean `used_reference_fallback` 으로 처리 (Task 4 의 BodyComparisonReport 필드).

(D) `@dataclass(frozen=True) class ScaleProfile` — RESEARCH.md Example 1 정합. 6 필드: estimated_height_scale, arm_scale, leg_scale, torso_scale, shoulder_hip_ratio, shoulder_hip_ratio_applied (bool). `__post_init__` validator — 5 numeric 필드 finite + strictly positive (math.isfinite + > 0) → ValueError raise.

(D') **R2 fix — `@dataclass(frozen=True) class BodyComparisonSourcePose`**:
  - Korean docstring: `"""R2 fix (2026-06-08 round-2). reference 측 대표 hold frame 의 17 keypoint × 4채널 (x, y, z, confidence). Firestore reference 컬렉션에 flat values 형태로 영속 (nested-array 회피 — [[firestore-nested-array-flat]]). Plan 06-02 mode1 + mode3 fallback path 가 본 dataclass 를 fetch 해서 compare_body_profiles 의 source_keypoints 인자로 to_keypoints_array() 변환 후 전달."""`
  - 필드:
    - `joint_keys: tuple[str, ...]` — RTMW COCO-17 joint 이름 17개 (e.g., ('nose', 'left_eye', ...))
    - `values: tuple[float, ...]` — flat float array, 길이 = 4 × len(joint_keys). 순서: [x_0, y_0, z_0, c_0, x_1, y_1, z_1, c_1, ...]
    - `frame_index: int` — 대표 frame 의 원본 frame index (디버깅용)
    - `torso_px: float` — 해당 frame 의 mid_shoulder ↔ mid_hip 픽셀 거리 (scale anchor)
    - `confidence: float` — 0.0~1.0 (대표 frame 의 평균 keypoint confidence)
    - `measured_at: int` — unix ms timestamp
  - `__post_init__` validator:
    - `len(self.values) == 4 * len(self.joint_keys)` 위반 시 ValueError
    - 모든 `v in self.values` 가 `math.isfinite(v)` — 위반 시 ValueError
    - `0 <= self.confidence <= 1.0` 위반 시 ValueError
    - `self.torso_px > 0` 위반 시 ValueError
  - method `def to_keypoints_array(self) -> np.ndarray`:
    - `return np.array(self.values, dtype=np.float32).reshape(len(self.joint_keys), 4)`
    - Korean docstring: `"""flat values → (J, 4) ndarray (x, y, z, confidence). compare_body_profiles 가 source_keypoints 로 변환할 때 사용."""`

(E) **C1 fix — `def normalize_pose_by_segments(source_keypoints, source_profile, target_profile, target_torso_px, *, apply_shoulder_hip_ratio=True) -> dict[str, tuple[float, float, float]]`**:
  - 파라미터 명명 (C1 fix 핵심):
    - `source_keypoints`: dict[str, tuple[float, float, float]] — 변환 대상 (방향 B 에서는 pro/정은지) raw 키포인트. 방향 벡터의 source.
    - `source_profile`: BodyNormalizationProfile — source 의 BodyProfile. **현 구현은 사용하지 않음 (debugging 용 reserved 파라미터). R10 fix — `test_normalize_pose_by_segments_output_independent_of_source_profile` 가 회귀 방지**.
    - `target_profile`: BodyNormalizationProfile — 결과 좌표가 따라야 할 비율의 출처 (방향 B 에서는 student). **L_ref 산출의 단독 소스**.
    - `target_torso_px`: float — target 영상에서 측정한 target 본인 torso 픽셀 길이 (절대 스케일 anchor).
  - Korean docstring:
    - "방향 B (D-06-A2) — source 의 모션을 target scale 로 reproject. source 의 키포인트가 제공하는 것은 '각 edge 의 방향 단위벡터' 뿐이며, 뼈 길이는 target_profile 의 segment ratio × target_torso_px 로 새로 산출된다."
    - "**C1 fix (2026-06-08 reviews)**: 이전 시그너처 `(pro_raw_keypoints, pro_profile, student_target_torso_px)` 는 L_ref = pro_profile_ratio × student_torso 였고, 이는 'pro 의 비율을 student 의 전체 크기로 일괄 축소' = uniform scale normalization 으로 degenerate. segment-aware 가 되려면 L_ref 는 student(target) 의 segment ratio 를 써야 한다. 그래야 동일 키 + 다른 팔/다리 비율 두 student 가 서로 다른 normalized pose 를 받는다."
    - "**R10 fix (2026-06-08 round-2)**: source_profile 인자는 의도적으로 사용 안 함. 향후 source ↔ target 비교 디버깅이 필요하면 본 인자 활용. 현 구현이 source_profile 을 사용하면 회귀 — `test_normalize_pose_by_segments_output_independent_of_source_profile` 가 detection."
  - 단계:
    - 1단계: mid_hip 가상 keypoint (left_hip + right_hip 평균), Root Centering — source_keypoints 기반
    - mid_shoulder 가상 keypoint (left_shoulder + right_shoulder 평균) — source_keypoints 기반
    - 2단계: KINEMATIC_TREE_EDGES 순회. 각 edge:
      - `v_unit = (C_source_raw - P_source_raw) / ||C_source_raw - P_source_raw||` (방향: source 에서)
      - `L_ref = _ref_segment_ratio(parent, child, target_profile, apply_shoulder_hip_ratio) × target_torso_px` (**스케일: target 에서**)
      - `C_norm = P_norm + v_unit × L_ref`
    - endpoint 미감지 (`parent not in source_keypoints` 또는 `child not in source_keypoints`) 시 해당 edge skip — `body_normalization_measurer._measure_segment_per_frame:78-94` endpoint conf 게이트 패턴 정합
    - 결과 = dict[str, tuple[float, float, float]]

(F) `def _ref_segment_ratio(parent, child, profile, *, apply_shoulder_hip_ratio) -> float`:
  - profile 인자는 **target_profile** 임이 명시 (Korean docstring 에 "target_profile (student) 의 segment ratio 를 반환")
  - 5 필드 → 13 edge 매핑. `apply_shoulder_hip_ratio=False` 시 폭 보정 OFF (mid_shoulder ↔ l/r_shoulder + mid_hip ↔ l/r_hip = neutral 0.25)
  - 상완/전완 50/50 분할 (Assumption A1)
  - Phase 2 v5 정합, v1.5 에서 upper_arm_ratio/forearm_ratio 세분화 검토.

(G) 단위 테스트 `backend/tests/phase06/test_body_normalizer_kinematic_tree.py` — `behavior` 10 test. fixture loader 는 `_factory.load_paired_frames`. **Test 7 (test_segment_aware_not_uniform_scale)** 가 C1 fix 의 핵심. **Test 8 (R10 — source_profile independence)** + **Test 9/10 (R2 — BodyComparisonSourcePose 검증)** 가 round-2 reviews fix 의 핵심.

`from .body_normalization import BodyNormalizationProfile` (Phase 2 dataclass 재사용). `from .pose_frame import PoseFrame, Keypoint3D`. `from .skeleton import KEYPOINT_NAMES`.

이모지/슬롭 코드 금지. Korean comments + English identifiers. fenced code block X.

[per D-06-A2 방향 B, D-06-A3 5 필드, D-06-A4 게이트, NotebookLM §1.1 Kinematic Tree Reprojection, §3.4 split, §4.4 ShoulderHipRatio anchor, C1 fix (2026-06-08 reviews — target-profile-based L_ref), R2 + R10 fix (2026-06-08 round-2)]
  </action>
  <verify>
    <automated>cd backend && pytest tests/phase06/test_body_normalizer_kinematic_tree.py -v</automated>
  </verify>
  <acceptance_criteria>
    - Source: `backend/shared/python/sunity_shared/analysis/body_normalizer.py` 존재 + `KINEMATIC_TREE_EDGES` 13 edge + `CONFIDENCE_GATE = 0.5` + `FORESHORTENING_ANGLE_DEG = 60.0` + `SHOULDER_HIP_HARD_THRESHOLD_PX = 150.0` + `TEMPORAL_VARIANCE_RATIO_THRESHOLD = 0.10` + `POSE_RELIABILITY_LOW_CONF_THRESHOLD = 0.4` (C14) + `DISPERSION_BASELINE = 1.5` + `DISPERSION_RANGE = 1.5` (R5)
    - Source: `class ScaleProfile` frozen dataclass + 6 필드 + `__post_init__` finite/positive validator
    - Source: `class BodyComparisonSourcePose` frozen dataclass + 6 필드 + `__post_init__` flat-values validator + `def to_keypoints_array(` method (R2 fix)
    - Source: `def normalize_pose_by_segments(source_keypoints, source_profile, target_profile, target_torso_px` (C1 fix — 4-arg 시그너처)
    - Source: `def _ref_segment_ratio(parent, child, profile` signature
    - Source: `ComparisonType = Literal["mode1", "mode3_first", "mode3_progress"]` (W1 — 3 cases)
    - Source grep: `grep -c "mode3_first_with_fallback" backend/shared/python/sunity_shared/analysis/body_normalizer.py` == 0 (W1)
    - Source grep (C1 fix): `grep -v '^#' backend/shared/python/sunity_shared/analysis/body_normalizer.py | grep -c "pro_raw_keypoints"` == 0 — 구 시그너처의 인자명이 살아있으면 안 됨
    - Source grep (C1 fix): `grep -c "target_profile" backend/shared/python/sunity_shared/analysis/body_normalizer.py` >= 2 — 새 시그너처의 인자명
    - **Source grep (R2 fix)**: `grep -c "class BodyComparisonSourcePose" backend/shared/python/sunity_shared/analysis/body_normalizer.py` == 1
    - **Source grep (R2 fix)**: `grep -c "to_keypoints_array" backend/shared/python/sunity_shared/analysis/body_normalizer.py` >= 1
    - **Source grep (R2 fix)**: `grep -c "reference_source_pose_missing" backend/shared/python/sunity_shared/analysis/body_normalizer.py` >= 1 (warning enum)
    - **Source grep (R5 fix)**: `grep -c "DISPERSION_BASELINE\\|DISPERSION_RANGE" backend/shared/python/sunity_shared/analysis/body_normalizer.py` >= 2 (상수 정의 + 사용)
    - Behavior: `pytest tests/phase06/test_body_normalizer_kinematic_tree.py -v` exits 0 + 10 test PASS (Test 8/9/10 신규 포함)
    - Behavior: test_kinematic_tree_edges_are_dag PASS — 13 edge DAG + mid_hip root + 모든 RTMW COCO-17 17 keypoint 도달
    - Behavior: test_split_angle_hipline_invariant_to_leg_length PASS
    - Behavior: test_normalize_direction_b_target_scale_is_student PASS (방향 B, student scale)
    - Behavior: test_segment_aware_not_uniform_scale PASS (C1 fix — studentA 결과 ≠ studentB 결과, 차이 > 5%)
    - **Behavior (R10 fix)**: test_normalize_pose_by_segments_output_independent_of_source_profile PASS (source_profile 변경에 출력 불변)
    - **Behavior (R2 fix)**: test_body_comparison_source_pose_post_init_validates_flat_values PASS
    - **Behavior (R2 fix)**: test_body_comparison_source_pose_to_keypoints_array_reshape PASS
    - Source grep: `grep -n "import smplx\\|from smpl\\|import betas\\|shape_params" backend/shared/python/sunity_shared/analysis/body_normalizer.py | wc -l` == 0 (SMPL 차단)
    - Source grep: `grep -v '^#' backend/shared/python/sunity_shared/analysis/body_normalizer.py | grep -c "toe_to_toe"` == 0 (Notebook §3.4)
  </acceptance_criteria>
  <done>body_normalizer 의 정규화 본체 + ScaleProfile + BodyComparisonSourcePose (R2 fix) + 상수 + 3 ComparisonType (W1) + C1 fix (target-profile-based L_ref) + C14 fix 상수 (POSE_RELIABILITY_LOW_*) + R5 상수 + R10 회귀 테스트. Task 3 (confidence) 진입 가능.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: bodyNormalizationConfidence 산식 + foreshortening 검출 + temporal variance + spatial dispersion (R5 fix 산식 + R6 fix 4채널 보존)</name>
  <files>
    backend/shared/python/sunity_shared/analysis/body_normalizer.py,
    backend/tests/phase06/test_body_normalizer_confidence.py
  </files>
  <read_first>
    backend/shared/python/sunity_shared/analysis/body_normalizer.py,
    backend/shared/python/sunity_shared/analysis/body_normalization_measurer.py,
    backend/shared/python/sunity_shared/analysis/temporal.py,
    backend/shared/python/sunity_shared/analysis/pose_frame.py,
    backend/tests/phase06/fixtures/fixture_foreshortening_lying_pose.json,
    backend/tests/phase06/fixtures/fixture_unstable_arm_swing.json,
    backend/tests/phase06/fixtures/fixture_high_dispersion_arms_sprawled.json,
    .planning/phases/06-coaching/06-NOTEBOOKLM-FINDINGS.md
  </read_first>
  <behavior>
    - Test 1 (test_foreshortening_lying_pose_disables_width_correction): fixture_foreshortening_lying_pose → is_foreshortening_detected=True → apply_shoulder_hip_ratio=False, warnings ⊃ "foreshortening_off"
    - Test 2 (test_unstable_arm_swing_lowers_confidence_below_threshold): fixture_unstable_arm_swing → temporal variance > 10% → confidence < 0.5, warnings ⊃ "temporal_variance_high"
    - Test 3 (test_stable_pose_confidence_above_threshold): fixture_160cm_pro_vs_140cm_student.student_frames (안정) → confidence >= 0.5, warnings == [] 또는 reference_profile_missing 만
    - Test 4 (test_confidence_clamped_to_unit_interval): 극단 입력 시 confidence ∈ [0.0, 1.0]
    - Test 5 (test_reference_profile_missing_emits_warning): reference_profile=None + comparison_type != 'mode3_first' → warnings ⊃ "reference_profile_missing" + confidence 하향
    - Test 6 (test_confidence_handles_none_pose_frames_gracefully): pose_frames=None or len(pose_frames)==0 → temporal_penalty = spatial_dispersion_penalty = 0.0 → base = student_profile.confidence + reference_match_bonus (Plan 06-02 wiring 에서 pose_frames 전달 필수 — B2)
    - **Test 7 (R5 fix 신규 — test_high_dispersion_arms_sprawled_increases_penalty)**: fixture_high_dispersion_arms_sprawled → spatial_dispersion_penalty 가 1.0 근사 (high dispersion → high penalty 의 자연 방향). warnings ⊃ "spatial_dispersion_high". confidence 가 안정 fixture 대비 명확히 낮음.
    - **Test 8 (R6 fix 신규 — test_to_coco17_array_preserves_confidence_channel)**: 합성 pose_frames (3 frame, 각 frame 의 keypoints_3d 의 uncertainty_proxy 가 0.2, 0.5, 0.8 변동) → `to_coco17_array(pose_frames)` 호출 → 결과 shape (3, 17, 4), `result[:, :, 3]` 가 입력의 uncertainty_proxy 값 보존 (0.2/0.5/0.8). `(T, 17, 3)` stack 만 했다면 conf 채널 0/default 로 평탄화되어 회귀.
    - **Test 9 (R6 fix 신규 — test_compute_body_normalization_confidence_uses_to_coco17_array)**: 동일 pose_frames 입력 시 `compute_body_normalization_confidence` 의 temporal_penalty 산출에서 `to_coco17_array` 결과의 conf 채널이 `joint_uncertainty` 의 입력이 됨 (간접 검증 — monkeypatch 으로 to_coco17_array 의 호출 카운트 == 1 검증, 또는 자체 stack 시 발생하는 결과 차이 비교).
  </behavior>
  <action>
Task 2 의 `body_normalizer.py` 에 3 함수 추가:

(A) `def is_foreshortening_detected(pose_frames: list[PoseFrame]) -> bool` — RESEARCH.md §Pattern 5. 조건:
  - 어깨-골반 벡터 vs 카메라 Z축 (0, 0, 1) 가정 각도 (cosine law). frames aggregate (평균)
  - 평균 각도 < `FORESHORTENING_ANGLE_DEG` (60°) → True
  - OR 평균 어깨-골반 픽셀 거리 < `SHOULDER_HIP_HARD_THRESHOLD_PX` (150px) → True
  - 둘 다 OR — 하나라도 만족 시 True
  - Korean docstring — Notebook §1.5 + Pitfall 1

(B) `def _compute_temporal_variance_per_segment(pose_frames, segment_pair) -> tuple[float, float]` helper — 단일 segment 의 (normalized_variance, mean_length). normalized_variance = var(length over frames) / mean(length)^2. NaN-safe (mean < EPS 시 (0.0, 0.0)).

(C) `def _compute_spatial_dispersion(pose_frames) -> float` helper — Notebook §4.2 B. `C_s(t) = (1/J) Σ_j ||P_j(t) - centroid(t)||₂`. shoulder_width 정규화. NaN-safe. **R6 fix — pose_frames → keypoints 변환은 `to_coco17_array(pose_frames)` 사용해서 (T, 17, 4) 4채널 보존**.

(D) `def compute_body_normalization_confidence(pose_frames, student_profile, reference_profile, comparison_type) -> tuple[float, list[str]]` — RESEARCH.md §Pattern 3:
  - 1) base = `student_profile.confidence`
  - 2) `if pose_frames is None or len(pose_frames) == 0`: temporal_penalty = 0.0, spatial_dispersion_penalty = 0.0 → base + reference_match_bonus 만 계산. (Plan 06-02 의 sibling helper `_extract_video_analysis_inputs` 가 production 에서 pose_frames 를 제공 — B2)
  - 3) pose_frames 있을 때:
    - **R6 fix**: `keypoints_4ch = to_coco17_array(pose_frames)` — (T, 17, 4) 4채널. 자체 `np.stack` (T, 17, 3) 금지.
    - temporal_penalty = KINEMATIC_TREE_EDGES 중 5 핵심 (l_upper_arm, l_lower_arm, l_thigh, l_lower_leg, torso) 의 normalized_variance 평균. clip ((variance - 0.05) / 0.05, 0, 1)
  - 4) **R5 fix — spatial_dispersion_penalty 산식**:
    - `dispersion_ratio = _compute_spatial_dispersion(pose_frames)`  # C_s / shoulder_width 평균
    - `spatial_dispersion_penalty = clip((dispersion_ratio - DISPERSION_BASELINE) / DISPERSION_RANGE, 0.0, 1.0)`
    - 의미: dispersion_ratio == DISPERSION_BASELINE (1.5) → penalty 0.0. dispersion_ratio == BASELINE + RANGE (3.0) → penalty 1.0. high dispersion = high penalty 의 자연 방향.
  - 5) reference_match_bonus = 0.0 if reference_profile is None else 0.1 × min(reference_profile.confidence, 1.0)
  - 6) confidence = clamp(0, 1, base - 0.5*temporal_penalty - 0.3*spatial_dispersion_penalty + reference_match_bonus)
  - warnings emit:
    - temporal_penalty > 0.5 → "temporal_variance_high"
    - **spatial_dispersion_penalty > 0.5 → "spatial_dispersion_high" (R5 fix — warning 이름과 의미 일치)**
    - reference_profile is None + comparison_type != "mode3_first" → "reference_profile_missing"

(E) 단위 테스트 `backend/tests/phase06/test_body_normalizer_confidence.py` — behavior 9 test. Test 7 (R5 high dispersion fixture) + Test 8 (R6 to_coco17_array 보존) + Test 9 (R6 사용처 검증) 신규.

이모지 / fenced code block X.

[per D-06-A4, D-06-U1, NotebookLM §1.5, §4.2, B2 (Plan 06-02 wiring), R5 fix (dispersion 산식 + 네이밍), R6 fix (to_coco17_array)]
  </action>
  <verify>
    <automated>cd backend && pytest tests/phase06/test_body_normalizer_confidence.py -v</automated>
  </verify>
  <acceptance_criteria>
    - Source: `def is_foreshortening_detected(`, `def compute_body_normalization_confidence(` signature
    - **Source grep (R5)**: `grep -c "DISPERSION_BASELINE\\|DISPERSION_RANGE" backend/shared/python/sunity_shared/analysis/body_normalizer.py` >= 2
    - **Source grep (R5)**: `grep -c "spatial_dispersion_penalty.*=.*clip" backend/shared/python/sunity_shared/analysis/body_normalizer.py` >= 1 — 신 산식 적용
    - **Source grep (R6)**: `grep -c "to_coco17_array" backend/shared/python/sunity_shared/analysis/body_normalizer.py` >= 1 — pose_frame.py:325 함수 사용
    - **Source grep (R6 회귀 차단)**: `grep -v '^#' backend/shared/python/sunity_shared/analysis/body_normalizer.py | grep -c "np.stack.*keypoints_3d\\|np.array.*keypoints_3d"` == 0 — 자체 (T, 17, 3) stack 금지
    - Behavior: `pytest tests/phase06/test_body_normalizer_confidence.py -v` exits 0 + 9 test PASS
    - Behavior: test_foreshortening_lying_pose_disables_width_correction PASS
    - Behavior: test_unstable_arm_swing_lowers_confidence_below_threshold PASS — confidence < 0.5
    - Behavior: test_confidence_clamped_to_unit_interval PASS
    - Behavior: test_confidence_handles_none_pose_frames_gracefully PASS (B2 wiring)
    - **Behavior (R5)**: test_high_dispersion_arms_sprawled_increases_penalty PASS — spatial_dispersion_penalty ≈ 1.0
    - **Behavior (R6)**: test_to_coco17_array_preserves_confidence_channel PASS — uncertainty_proxy 채널 보존
    - **Behavior (R6)**: test_compute_body_normalization_confidence_uses_to_coco17_array PASS
  </acceptance_criteria>
  <done>confidence 산식 + foreshortening 검출 + R5 spatial dispersion 산식 자연화 + R6 4채널 보존. Task 4 (IPSF deficit + compare_body_profiles 본체) 진입 가능.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: IPSF 절대 deficit (pose_reliability_low rename, C14 fix) + BodyComparisonFinding + compare_body_profiles 본체 (R8 fix extra_warnings 파라미터 + R2 fix source_keypoints 인자) + usedReferenceFallback (W1) + foreshortening proxy (W6)</name>
  <files>
    backend/shared/python/sunity_shared/analysis/body_normalizer.py,
    backend/tests/phase06/test_body_normalizer_ipsf_deficit.py,
    backend/tests/phase06/test_compare_body_profiles.py
  </files>
  <read_first>
    backend/shared/python/sunity_shared/analysis/body_normalizer.py,
    backend/shared/python/sunity_shared/analysis/dimensions.py,
    backend/shared/python/sunity_shared/analysis/skeleton.py,
    backend/shared/python/sunity_shared/analysis/technique.py,
    backend/tests/phase06/fixtures/fixture_160cm_pro_vs_140cm_student.json,
    backend/tests/phase06/fixtures/fixture_lefty_vs_righty_twist.json,
    backend/tests/phase06/fixtures/fixture_split_angle_hipline.json,
    .planning/phases/06-coaching/06-NOTEBOOKLM-FINDINGS.md,
    .planning/phases/06-coaching/06-REVIEWS.md
  </read_first>
  <behavior>
    - Test 1 (test_160cm_pro_vs_140cm_student_removes_false_positive): pro + student + 정규화 ON → deduction_score 합산이 raw 비교 대비 60% 감소 (NotebookLM §1.4)
    - Test 2 (test_twist_motion_shoulder_hip_ratio_not_in_findings): fixture_lefty_vs_righty_twist → findings 에 "rounded_shoulders" 가능하나 shoulderHipRatio 자체는 deficit 없음
    - Test 3 (test_split_angle_uses_hip_knee_line_not_toe_to_toe): fixture_split_angle_hipline short_leg vs long_leg → split deficit 동일
    - Test 4 (test_compare_body_profiles_mode1_returns_full_report): mode1 + reference_profile + 안정 fixture → BodyComparisonReport(comparison_type='mode1', scale_profile != None, findings non-empty, body_normalization_confidence >= 0.5, used_reference_fallback=False)
    - Test 5 (test_compare_body_profiles_low_confidence_fallback): mode3_first + reference_profile=None + 불안정 fixture → BodyComparisonReport(comparison_type='mode3_first', scale_profile=None, findings ⊂ Page 9 단독, warnings ⊃ ["low_confidence_normalization_off"], used_reference_fallback=False)
    - Test 6 (W1 — test_compare_body_profiles_mode3_first_with_gemini_fallback_sets_boolean): mode3_first + reference_profile (caller 전달) + confidence >= 0.5 + used_reference_fallback=True (caller arg) → BodyComparisonReport(comparison_type='mode3_first', scale_profile != None, used_reference_fallback=True). comparisonType 은 'mode3_first' 유지 (D-06-B3 honor).
    - Test 7 (test_compare_body_profiles_mode3_progress_requires_previous_id): mode3_progress + previous_analysis_id=None → ValueError
    - Test 8 (W6 — test_foreshortening_proxy_triggers_shoulder_hip_ratio_off): fixture_foreshortening_lying_pose → is_foreshortening_detected=True → apply_shoulder_hip_ratio=False + warnings ⊃ "shoulder_hip_ratio_off"
    - Test 9 (C14 fix — test_pose_reliability_low_deficit_code_replaces_bad_angle): pose_frames 의 평균 confidence < 0.4 인 frame 이 50% 초과 → findings 에 `deficit_code == "pose_reliability_low"` 존재. `bad_angle` 라는 string 은 findings 어디에도 없음.
    - **Test 10 (R8 fix 신규 — test_compare_body_profiles_merges_extra_warnings_with_validation)**: `compare_body_profiles(..., extra_warnings=["fallback_reference_not_found"])` 호출 → 반환된 BodyComparisonReport.warnings 가 내부 산출 warnings + ["fallback_reference_not_found"] merge. 8 warning enum frozen set 검증 통과 (BODY_COMPARISON_WARNING_CODES 정합).
    - **Test 11 (R8 fix 신규 — test_compare_body_profiles_rejects_invalid_extra_warning)**: `compare_body_profiles(..., extra_warnings=["not_a_valid_code"])` 호출 → ValueError raise (warning enum frozen set 외부 코드). validation 우회 차단 보장.
    - **Test 12 (R2 fix 신규 — test_compare_body_profiles_source_keypoints_from_body_comparison_source_pose)**: caller 가 `source_pose: BodyComparisonSourcePose` → `source_keypoints = source_pose.to_keypoints_array()` 형태로 변환해 인자 전달 (caller 책임). `compare_body_profiles` 자체는 source_keypoints 를 ndarray 또는 dict 로 받음 — source_pose 직접 받지 않음 (분리된 책임). 본 test 는 변환된 ndarray 가 normalize_pose_by_segments 까지 흘러가는지 검증.
  </behavior>
  <action>
Task 2/3 의 `body_normalizer.py` 에 dataclass + 함수 추가:

(A) `@dataclass(frozen=True) class BodyComparisonFinding` — RESEARCH.md Example 1.
  - 필드: `deficit_code: str`, `joint_key: str | None`, `measured_value: float`, `deduction_score: float`, `confidence: float`, `body_type_adjusted: bool`
  - `__post_init__` — `0 <= confidence <= 1`
  - **C14 fix**: deficit_code enum 6종 (knee_toe_alignment / clean_lines / extension / posture / body_placement + `pose_reliability_low`). **R9 fix (2026-06-08 round-2)**: '7종' 표현 X — 5 IPSF geometric + 1 Sunity 자체 신호. `poor_transitions` v1.5 deferred (RESEARCH.md Open Question 4 RESOLVED, Phase 8 jerk/jitter 와 통합).

(B) **W1** — `@dataclass(frozen=True) class BodyComparisonReport`:
  - `comparison_type: ComparisonType` (3 cases — mode1 / mode3_first / mode3_progress)
  - `body_normalization_confidence: float` (success #4 — 항상 emit)
  - `scale_profile: ScaleProfile | None` (정규화 OFF 시 None)
  - `findings: list[BodyComparisonFinding] = field(default_factory=list)`
  - `warnings: list[str] = field(default_factory=list)`
  - `reference_motion_id: str | None = None`
  - `reference_athlete_name: str | None = None`
  - `previous_analysis_id: str | None = None`
  - `used_reference_fallback: bool = False` (W1 — Gemini fallback 신호)
  - `__post_init__`:
    - 0 <= confidence <= 1
    - comparison_type == "mode1" → reference_motion_id 필수 X (scale_profile is None OK — 백필 미완 fallback)
    - comparison_type == "mode3_progress" → previous_analysis_id is None 시 ValueError
    - comparison_type != "mode3_first" + used_reference_fallback == True → ValueError
    - warnings list[str] type
    - **R8 fix**: 모든 warnings element 가 `BODY_COMPARISON_WARNING_CODES` frozenset 내. 위반 시 ValueError. (extra_warnings 주입 path 도 본 validation 통과 필수)

(C) `def measure_ipsf_absolute_deficits(angles: np.ndarray, technique_profile, normalized_keypoints: dict | None = None, *, pose_frames: list | None = None) -> list[BodyComparisonFinding]`:
  - `knee_toe_alignment` (-0.2): joint_angle_deg(big_toe, knee, ankle) 180° 박제 ±20° (`LINE_TOL_DEG`). RTMW COCO-17 에는 big_toe 없음 — ankle 사용
  - `clean_lines` (-0.2): technique_profile.expects_extension == True 관절들의 평균 각도 < 180° - LINE_TOL_DEG
  - `extension` (-0.2): mid_shoulder ↔ mid_hip ↔ neck 라인 평균 각도 < 160° (v1 단순)
  - `posture` (-0.2): 좌우 어깨 line z-axis 깊이 차이 > shoulder_width × 0.3 (rounded shoulders)
  - `body_placement` (-0.2): mid_hip 의 폴 축 (PoleAxis) 대비 수평 거리 > shoulder_width × 0.5 (v1 단순)
  - **`pose_reliability_low` (-0.5) — C14 fix (구 bad_angle rename)**: pose_frames 가 주어지면, 각 frame 의 평균 keypoint confidence < `POSE_RELIABILITY_LOW_CONF_THRESHOLD` (0.4) 인 frame 의 비율 > `POSE_RELIABILITY_LOW_FRAME_RATIO` (0.5) 일 때 finding 추가. Korean docstring 에 명시: "IPSF Page 21 의 'bad angle' (judge 가 카메라 가림으로 angle 을 관찰 못함) 과 의미가 다르다. 본 deficit 은 우리 시스템의 pose-estimation confidence 가 낮은 frame 의 비율을 측정한다. docs/contract.md §8 의 divergence note 참조."
  - **체형 ratio 곱하지 말 것** — finding 의 deduction_score = IPSF 절대값 그대로. `body_type_adjusted=True` 는 _coordinate_ 가 정규화된 keypoint 에서 측정되었는지 여부 (정규화 좌표 = True, raw = False)
  - **split 각도** — toe-to-toe Euclidean 절대 금지, hip→knee 라인 각도만 (NotebookLM §3.4)
  - `poor_transitions` v1.5 deferred (R9 fix — '7 deficits' 옛 표현 사용 금지)

(D) **R8 fix** — `def compare_body_profiles(pose_frames, student_profile, reference_profile, comparison_type, *, source_keypoints=None, angles, technique_profile, reference_motion_id=None, reference_athlete_name=None, previous_analysis_id=None, used_reference_fallback=False, target_torso_px=None, extra_warnings: list[str] | None = None) -> BodyComparisonReport`:
  - 1) `confidence, warnings = compute_body_normalization_confidence(pose_frames, student_profile, reference_profile, comparison_type)`
  - 2) `foreshortening = is_foreshortening_detected(pose_frames) if pose_frames else False`
  - 3) **W6** — `apply_shoulder_hip_ratio = not foreshortening`. foreshortening 시 warnings ⊃ "foreshortening_off" + "shoulder_hip_ratio_off". RESEARCH.md Open Question 2 RESOLVED — D-06-A3 의 ideal (per-segment shoulder_hip_ratio confidence) 은 v1.5. v1 은 foreshortening 60° proxy.
  - 4) gate — `if confidence < CONFIDENCE_GATE or reference_profile is None or source_keypoints is None`: scale_profile = None, findings = measure_ipsf_absolute_deficits(angles, technique_profile, normalized_keypoints=None, pose_frames=pose_frames), warnings ⊃ "low_confidence_normalization_off". comparison_type == "mode3_first" + reference_profile is None 시 "reference_profile_missing" 은 emit 안 함 (의도된 fallback). **source_keypoints is None 시 warnings ⊃ "reference_source_pose_missing" (R2 fix — production 에서 백필 미완 reference 의 silently OFF 차단)**.
  - 5) confidence >= 0.5 + reference_profile 있을 시 + source_keypoints 있을 시: ScaleProfile 5 필드 산출. **C1 fix — normalize_pose_by_segments 호출 시 인자 순서 = `(source_keypoints, source_profile, target_profile, target_torso_px, apply_shoulder_hip_ratio=...)`**. 본 함수 entry 에서 source = source_keypoints (caller 가 BodyComparisonSourcePose.to_keypoints_array() 로 변환해 주입 — R2), target = student. `target_torso_px` 는 caller 가 student 영상에서 측정해 인자로 전달 (None 시 student_profile 의 torso scale 으로 fallback). normalized_keypoints = 정규화 결과 → measure_ipsf_absolute_deficits 에 전달 (body_type_adjusted=True)
  - 6) **R8 fix — extra_warnings merge + validate**:
    - `if extra_warnings: for w in extra_warnings: if w not in BODY_COMPARISON_WARNING_CODES: raise ValueError(f"invalid extra_warning: {w}")`
    - merged_warnings = list(dict.fromkeys([*warnings, *(extra_warnings or [])]))  # dedup preserve order
  - 7) BodyComparisonReport 조립 (used_reference_fallback 포함 + merged_warnings) + 반환. `__post_init__` 의 frozenset 검증이 R8 의 validation gate.

(E) 단위 테스트 2 파일:
  - `test_body_normalizer_ipsf_deficit.py` — Test 1/2/3 + W6 Test 8 + **C14 Test 9 (pose_reliability_low rename)**
  - `test_compare_body_profiles.py` — Test 4/5/6 (W1 used_reference_fallback) / Test 7 + **R8 Test 10/11 (extra_warnings merge + validation)** + **R2 Test 12 (source_keypoints from BodyComparisonSourcePose.to_keypoints_array)**

이모지 / fenced code block X.

[per D-06-A1, D-06-A3, D-06-B1, D-06-B3 (3 cases — W1), D-06-U1, NotebookLM §3.2, §3.3, §3.4, W1 used_reference_fallback, W6 foreshortening proxy, C14 pose_reliability_low rename, R8 fix extra_warnings, R2 fix source_keypoints, R9 fix '5+1 deficits' wording]
  </action>
  <verify>
    <automated>cd backend && pytest tests/phase06/test_body_normalizer_ipsf_deficit.py tests/phase06/test_compare_body_profiles.py -v</automated>
  </verify>
  <acceptance_criteria>
    - Source: `class BodyComparisonFinding`, `class BodyComparisonReport`, `def measure_ipsf_absolute_deficits(`, `def compare_body_profiles(`
    - Source: `used_reference_fallback: bool` in BodyComparisonReport (W1)
    - Source: `compare_body_profiles` 시그너처에 `used_reference_fallback=False` (W1) + `extra_warnings: list[str] | None = None` (R8) + `source_keypoints=None` (R2)
    - Source grep: `grep -c "mode3_first_with_fallback" backend/shared/python/sunity_shared/analysis/body_normalizer.py` == 0 (W1)
    - Source grep: `grep -v '^#' backend/shared/python/sunity_shared/analysis/body_normalizer.py | grep -c "toe_to_toe"` == 0
    - **Source grep (C14 fix)**: `grep -v '^#' backend/shared/python/sunity_shared/analysis/body_normalizer.py | grep -c '"bad_angle"'` == 0 — 문자열 리터럴로 bad_angle 이 살아있으면 안 됨
    - **Source grep (C14 fix)**: `grep -c "pose_reliability_low" backend/shared/python/sunity_shared/analysis/body_normalizer.py` >= 2 — 상수 + 문자열 사용처
    - **Source grep (R8 fix)**: `grep -c "extra_warnings" backend/shared/python/sunity_shared/analysis/body_normalizer.py` >= 2 (signature + body)
    - **Source grep (R8 fix)**: `grep -v '^#' backend/shared/python/sunity_shared/analysis/body_normalizer.py | grep -c "dataclasses.replace"` == 0 — 우회 패턴 부재
    - **Source grep (R2 fix)**: `grep -c "source_keypoints" backend/shared/python/sunity_shared/analysis/body_normalizer.py` >= 2 (signature + body)
    - **Source grep (R2 fix)**: `grep -c "reference_source_pose_missing" backend/shared/python/sunity_shared/analysis/body_normalizer.py` >= 1
    - Behavior: 12 test PASS (Test 9~12 신규)
    - Behavior: test_160cm_pro_vs_140cm_student_removes_false_positive PASS
    - Behavior: test_compare_body_profiles_low_confidence_fallback PASS
    - Behavior: test_compare_body_profiles_mode3_first_with_gemini_fallback_sets_boolean PASS (W1)
    - Behavior: test_compare_body_profiles_mode3_progress_requires_previous_id PASS
    - Behavior: test_foreshortening_proxy_triggers_shoulder_hip_ratio_off PASS (W6)
    - **Behavior: test_pose_reliability_low_deficit_code_replaces_bad_angle PASS (C14)**
    - **Behavior (R8)**: test_compare_body_profiles_merges_extra_warnings_with_validation PASS
    - **Behavior (R8)**: test_compare_body_profiles_rejects_invalid_extra_warning PASS
    - **Behavior (R2)**: test_compare_body_profiles_source_keypoints_from_body_comparison_source_pose PASS
  </acceptance_criteria>
  <done>body_normalizer 본체 + W1 (used_reference_fallback) + W6 (foreshortening proxy) + C14 (pose_reliability_low rename) + R8 (extra_warnings 파라미터 + validation) + R2 (source_keypoints 인자 + reference_source_pose_missing warning). Task 5 (3-way contract lockstep) 진입 가능.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: 3-way contract lockstep — TS interface (3 ComparisonType + usedReferenceFallback + pose_reliability_low + BodyComparisonSourcePose R2) + models.py re-export + docs/contract.md §8 + §8.2 BodyComparisonSourcePose 명세 + IPSF divergence note + drift test (W1 + C14 + R2)</name>
  <files>
    app/src/types/analysis.ts,
    backend/shared/python/sunity_shared/models.py,
    docs/contract.md,
    backend/tests/phase06/test_body_comparison_report_lockstep.py
  </files>
  <read_first>
    app/src/types/analysis.ts,
    backend/shared/python/sunity_shared/models.py,
    docs/contract.md,
    backend/shared/python/sunity_shared/analysis/body_normalizer.py,
    backend/tests/test_body_normalization_lockstep.py
  </read_first>
  <behavior>
    - Test 1 (test_ts_body_comparison_report_interface): app/src/types/analysis.ts 에 `interface BodyComparisonReport` + 9 필드 (comparisonType, bodyNormalizationConfidence, scaleProfile, findings, warnings, referenceMotionId?, referenceAthleteName?, previousAnalysisId?, usedReferenceFallback) + `type ComparisonType` (3 cases — mode1, mode3_first, mode3_progress)
    - Test 2 (test_python_re_export_consistency): models.py 에 `from .analysis.body_normalizer import BodyComparisonReport, BodyComparisonFinding, BodyComparisonSourcePose, ScaleProfile, ComparisonType` (R2 — BodyComparisonSourcePose 포함)
    - Test 3 (test_docs_contract_md_section_8): docs/contract.md 에 `## §8` + "BodyComparisonReport" 명칭 + 3 ComparisonType + usedReferenceFallback + 8 warning enum (reference_source_pose_missing 포함 — R2) + **C14 IPSF divergence note ("pose_reliability_low differs from IPSF 'bad_angle' judge-observation")** + **§8.2 BodyComparisonSourcePose 명세 (R2 — flat values, Firestore nested-array 회피)**
    - Test 4 (test_camelcase_snake_case_field_map): _FIELD_MAP — comparisonType ↔ comparison_type, bodyNormalizationConfidence ↔ body_normalization_confidence, scaleProfile ↔ scale_profile, referenceMotionId ↔ reference_motion_id, referenceAthleteName ↔ reference_athlete_name, previousAnalysisId ↔ previous_analysis_id, **usedReferenceFallback ↔ used_reference_fallback (W1)**
    - Test 5 (test_no_legacy_mode3_first_with_fallback): grep — TS + Python + docs/contract.md §8 에 `mode3_first_with_fallback` 부재 (W1 — 4th case 금지)
    - **Test 6 (C14 — test_pose_reliability_low_present_bad_angle_absent)**: TS BodyComparisonFinding.deficitCode 의 docstring 또는 union literal 에 'pose_reliability_low' 존재 + 'bad_angle' 부재
    - **Test 7 (R2 fix 신규 — test_body_comparison_source_pose_lockstep)**: TS `interface BodyComparisonSourcePose` 6 필드 (jointKeys, values, frameIndex, torsoPx, confidence, measuredAt) ↔ Python `class BodyComparisonSourcePose` 6 필드 ↔ docs/contract.md §8.2 모두 정합. `ReferenceMotion.bodyComparisonSourcePose?: BodyComparisonSourcePose | null` (R2 — nullable, 백필 미완 reference 정합).
  </behavior>
  <action>
3-way atomic update (단일 commit).

(A) `app/src/types/analysis.ts` — 기존 `BodyNormalizationProfile` interface (line 373-416) 바로 아래:
  - JSDoc lockstep header — "Phase 6, D-06-B3. Python lockstep: backend/.../body_normalizer.py BodyComparisonReport. 변경 시 양쪽 + docs/contract.md §8 동시 갱신 (CLAUDE.md Cross-cutting). W1 (2026-06-08): 3 ComparisonType + usedReferenceFallback. C14 (2026-06-08 reviews): deficit code 'pose_reliability_low' (IPSF judge-observation 'bad_angle' 과 의미 다름). **R2 (2026-06-08 round-2)**: BodyComparisonSourcePose 신규 — Firestore reference 컬렉션 영속, flat values."
  - `export type ComparisonType = 'mode1' | 'mode3_first' | 'mode3_progress'` (W1)
  - `export interface ScaleProfile` — 6 필드 camelCase
  - `export interface BodyComparisonFinding` — 6 필드. deficitCode JSDoc 에 enum 명시 6종 (pose_reliability_low 포함, bad_angle 부재)
  - `export interface BodyComparisonReport` — 9 필드 (usedReferenceFallback 포함)
  - **`export interface BodyComparisonSourcePose` (R2 신규)**:
    - JSDoc: "Reference 측 대표 hold frame keypoints (17 joint × 4 channel). Firestore reference 컬렉션의 `bodyComparisonSourcePose` 필드. nested-array 회피 위해 values 는 flat float array (length = 4 × jointKeys.length). Plan 06-02 mode1 + mode3 fallback path 가 fetch 해서 backend body_normalizer.BodyComparisonSourcePose.to_keypoints_array() 로 reshape."
    - `jointKeys: string[]` (17개)
    - `values: number[]` (flat, length = 4 × 17 = 68)
    - `frameIndex: number`
    - `torsoPx: number`
    - `confidence: number` (0-1)
    - `measuredAt: number` (unix ms)
  - `AnalysisResult` interface 에 `bodyComparisonReport?: BodyComparisonReport` 추가
  - `ReferenceMotion` interface 에 `bodyNormalizationProfile?: BodyNormalizationProfile | null` + **`bodyComparisonSourcePose?: BodyComparisonSourcePose | null` (R2 fix)** 추가

(B) `backend/shared/python/sunity_shared/models.py` — 기존 BodyNormalizationProfile re-export 바로 아래:
  - 한국어 주석: `# Phase 6 (2026-06-08) — D-06-B3. BodyComparisonReport = ... (W1) + R2 (BodyComparisonSourcePose 신규). 확장: ScaleProfile, BodyComparisonFinding, BodyComparisonSourcePose, ComparisonType Literal (3 cases). C14 deficit_code 'pose_reliability_low' (IPSF divergence — docs/contract.md §8). R8 extra_warnings + R2 reference_source_pose_missing warning. TS 미러: app/src/types/analysis.ts BodyComparisonReport + BodyComparisonSourcePose interface. 변경 시 TS + contract.md §8/§8.2 동시 갱신.`
  - `from .analysis.body_normalizer import BodyComparisonFinding, BodyComparisonReport, BodyComparisonSourcePose, ComparisonType, ScaleProfile` (R2 — BodyComparisonSourcePose 추가)

(C) `docs/contract.md` — §7 (BodyNormalizationProfile) 바로 아래 신규 §8:
  - 헤더: `## §8. BodyComparisonReport (Plan 06-01 신설 — D-06-B3 confidence-tiered hybrid + W1 + C14 IPSF divergence + R8 extra_warnings + R2 source pose)`
  - 3-way 동시 갱신 헤더 (TS + Python + 본 §8 + §8.2)
  - ComparisonType union 3 케이스 (mode1 / mode3_first / mode3_progress)
  - `usedReferenceFallback: boolean (default false) — Gemini fallback 신호`
  - ScaleProfile 6 필드
  - BodyComparisonFinding 6 필드 + **deficit_code 6 enum 표** (5 IPSF + pose_reliability_low). **R9 fix — '7 deficits' 옛 표현 금지. poor_transitions v1.5 deferred (Phase 8).**
  - **C14 IPSF divergence note (subsection §8.1)** — bad_angle vs pose_reliability_low 의미 차이 설명
  - bodyNormalizationConfidence [0.0, 1.0] + Universal Principle D-06-U1
  - **8 warning enum (R2 fix — reference_source_pose_missing 추가)**: low_confidence_normalization_off / foreshortening_off / shoulder_hip_ratio_off / temporal_variance_high / spatial_dispersion_high / reference_profile_missing / fallback_reference_not_found / **reference_source_pose_missing**
  - **§8.2 BodyComparisonSourcePose (R2 fix 신설)**:
    - 헤더: `## §8.2 BodyComparisonSourcePose (Plan 06-01 신설, R2 fix 2026-06-08 round-2 reviews) — Reference 측 대표 hold frame keypoints flat 영속`
    - 목적: "Phase 6 의 `normalize_pose_by_segments` 가 reference 측 keypoints 를 source 로 받아 student 의 target_profile 비율로 reproject. reference 영상을 매 분석마다 재실행하지 않고 백필된 대표 frame 을 영속해 분석 latency 와 비용을 줄임."
    - 6 필드 명세 (jointKeys, values, frameIndex, torsoPx, confidence, measuredAt) + 각 필드의 의미
    - Firestore 저장 path: `reference/{motionId}.bodyComparisonSourcePose` (top-level)
    - **Firestore nested-array 회피 정합**: `values` 는 flat float array (length = 4 × len(jointKeys) = 68 for COCO-17). reshape 책임은 backend `BodyComparisonSourcePose.to_keypoints_array()`.
    - 백필 procedure 참조: Plan 06-03 Task 2 + Task 3 (extract_reference_body_profiles.py 가 RTMW 결과의 대표 hold frame — hold_window 중앙 또는 confidence 최고 frame — 을 추출).
    - mode1 + mode3 fallback path 의 read 경로 명시 (Plan 06-02)
  - footer: `*Plan 06-01 §8 + §8.2 추가: 2026-06-08 — BodyComparisonReport (D-06-B3 + W1 + C14) + BodyComparisonSourcePose (R2 round-2 reviews).*`

(D) `backend/tests/phase06/test_body_comparison_report_lockstep.py` — 기존 `backend/tests/test_body_normalization_lockstep.py` 패턴 정합. `_FIELD_MAP` 9 entry + BodyComparisonSourcePose 별도 _SOURCE_POSE_FIELD_MAP 6 entry:
  - comparisonType ↔ comparison_type
  - bodyNormalizationConfidence ↔ body_normalization_confidence
  - scaleProfile ↔ scale_profile
  - findings ↔ findings
  - warnings ↔ warnings
  - referenceMotionId ↔ reference_motion_id
  - referenceAthleteName ↔ reference_athlete_name
  - previousAnalysisId ↔ previous_analysis_id
  - usedReferenceFallback ↔ used_reference_fallback (W1)

  _SOURCE_POSE_FIELD_MAP (R2 신규):
  - jointKeys ↔ joint_keys
  - values ↔ values
  - frameIndex ↔ frame_index
  - torsoPx ↔ torso_px
  - confidence ↔ confidence
  - measuredAt ↔ measured_at

7 test — behavior block 1:1. **Test 7 (R2)** 가 round-2 fix 의 lockstep 검증.

단일 atomic commit (TS + Python + docs + test).

[per D-06-B3, CLAUDE.md Cross-cutting, W1, C14 (2026-06-08 reviews), R2 + R8 + R9 (2026-06-08 round-2 reviews)]
  </action>
  <verify>
    <automated>cd backend && pytest tests/phase06/test_body_comparison_report_lockstep.py -v && cd ../app && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "interface BodyComparisonReport" app/src/types/analysis.ts` == 1
    - Source: `grep -c "type ComparisonType" app/src/types/analysis.ts` == 1
    - Source: `grep -c "mode3_first_with_fallback" app/src/types/analysis.ts` == 0 (W1)
    - Source: `grep -c "usedReferenceFallback" app/src/types/analysis.ts` >= 1 (W1)
    - Source: `grep -c "pose_reliability_low" app/src/types/analysis.ts` >= 1 (C14)
    - **Source (R2)**: `grep -c "interface BodyComparisonSourcePose" app/src/types/analysis.ts` == 1
    - **Source (R2)**: `grep -c "bodyComparisonSourcePose" app/src/types/analysis.ts` >= 1 (ReferenceMotion 필드)
    - **Source (R2)**: `grep -c "reference_source_pose_missing" docs/contract.md` >= 1 (8 warning enum)
    - Source (C14): `grep -v '^//' app/src/types/analysis.ts | grep -c "'bad_angle'"` == 0
    - Source: `grep -c "BodyComparisonReport\\|BodyComparisonSourcePose" backend/shared/python/sunity_shared/models.py` >= 2
    - Source: `grep -c "^## §8\\." docs/contract.md` >= 2 (§8 + §8.1 + §8.2)
    - Source: `grep -c "mode3_first_with_fallback" docs/contract.md` == 0 (W1)
    - Source: `grep -c "usedReferenceFallback" docs/contract.md` >= 1 (W1)
    - Source: `grep -c "pose_reliability_low" docs/contract.md` >= 1 (C14)
    - Source: `grep -ci "IPSF.*bad_angle\\|judge-observation" docs/contract.md` >= 1 (C14 divergence note)
    - **Source (R2)**: `grep -c "BodyComparisonSourcePose" docs/contract.md` >= 1 (§8.2)
    - Behavior: `pytest backend/tests/phase06/test_body_comparison_report_lockstep.py -v` exits 0 + 7 test PASS (Test 7 R2 신규 포함)
    - Behavior: `cd app && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>3-way contract lockstep + W1 3 ComparisonType + usedReferenceFallback + C14 pose_reliability_low rename + IPSF divergence note + R2 BodyComparisonSourcePose lockstep (TS + Python + docs §8.2). Plan 06-01 완료 — Plan 06-02 (pipeline wiring) 진입 가능.</done>
</task>

</tasks>

<verification>
## Plan 06-01 verification (algorithm + contract)

- [ ] `pytest backend/tests/phase06/fixtures/test_fixtures_loadable.py backend/tests/phase06/test_body_normalizer_kinematic_tree.py backend/tests/phase06/test_body_normalizer_confidence.py backend/tests/phase06/test_body_normalizer_ipsf_deficit.py backend/tests/phase06/test_compare_body_profiles.py backend/tests/phase06/test_body_comparison_report_lockstep.py -v` 전부 GREEN
- [ ] `cd app && npx tsc --noEmit` exits 0
- [ ] `grep -c "interface BodyComparisonReport" app/src/types/analysis.ts` == 1
- [ ] `grep -c "^## §8\\." docs/contract.md` >= 2 (§8 + §8.2)
- [ ] `grep -c "BodyComparisonReport\\|BodyComparisonSourcePose" backend/shared/python/sunity_shared/models.py` >= 2
- [ ] `grep -c "mode3_first_with_fallback" app/src/types/analysis.ts backend/shared/python/sunity_shared/analysis/body_normalizer.py docs/contract.md` == 0 (W1)
- [ ] `grep -c "usedReferenceFallback" app/src/types/analysis.ts docs/contract.md` >= 2 (W1)
- [ ] **(C1 fix)** `grep -c "target_profile" backend/shared/python/sunity_shared/analysis/body_normalizer.py` >= 2 (새 시그너처)
- [ ] **(C1 fix)** `grep -v '^#' backend/shared/python/sunity_shared/analysis/body_normalizer.py | grep -c "pro_raw_keypoints"` == 0 (구 시그너처 부재)
- [ ] **(C14 fix)** `grep -c "pose_reliability_low" backend/shared/python/sunity_shared/analysis/body_normalizer.py app/src/types/analysis.ts docs/contract.md` >= 3
- [ ] **(C14 fix)** `grep -v '^#' backend/shared/python/sunity_shared/analysis/body_normalizer.py | grep -c '"bad_angle"'` == 0
- [ ] `grep -v '^#' backend/shared/python/sunity_shared/analysis/body_normalizer.py | grep -c "toe_to_toe"` == 0
- [ ] `grep -v '^#' backend/shared/python/sunity_shared/analysis/body_normalizer.py | grep -c "import smplx\\|from smplx\\|shape_params\\|betas"` == 0
- [ ] **(R2 fix)** `grep -c "class BodyComparisonSourcePose" backend/shared/python/sunity_shared/analysis/body_normalizer.py` == 1
- [ ] **(R2 fix)** `grep -c "to_keypoints_array" backend/shared/python/sunity_shared/analysis/body_normalizer.py` >= 1
- [ ] **(R2 fix)** `grep -c "interface BodyComparisonSourcePose" app/src/types/analysis.ts` == 1
- [ ] **(R2 fix)** `grep -c "reference_source_pose_missing" backend/shared/python/sunity_shared/analysis/body_normalizer.py docs/contract.md` >= 2
- [ ] **(R5 fix)** `grep -c "DISPERSION_BASELINE\\|DISPERSION_RANGE" backend/shared/python/sunity_shared/analysis/body_normalizer.py` >= 2
- [ ] **(R6 fix)** `grep -c "to_coco17_array" backend/shared/python/sunity_shared/analysis/body_normalizer.py` >= 1
- [ ] **(R6 fix)** `grep -v '^#' backend/shared/python/sunity_shared/analysis/body_normalizer.py | grep -c "np.stack.*keypoints_3d\\|np.array.*keypoints_3d"` == 0
- [ ] **(R8 fix)** `grep -c "extra_warnings" backend/shared/python/sunity_shared/analysis/body_normalizer.py` >= 2
- [ ] **(R8 fix)** `grep -v '^#' backend/shared/python/sunity_shared/analysis/body_normalizer.py | grep -c "dataclasses.replace"` == 0
- [ ] 신규 라이브러리 0
</verification>

<success_criteria>
Plan 06-01 Success Criteria — algorithm + contract 본체:

1. ✅ 프로/수강생 BodyNormalizationProfile 차이로 ScaleProfile 5 필드 산출 + Kinematic Tree Bone-Length Reprojection 적용 (Tasks 2 + fixture_160cm_pro_vs_140cm_student)
2. ✅ **C1 fix** — `normalize_pose_by_segments(source_keypoints, source_profile, target_profile, target_torso_px)` — L_ref = target(student) profile 의 segment ratio × target_torso_px. 단순 확대/축소 아닌 segment-aware reprojection. Test 7 (test_segment_aware_not_uniform_scale) 가 같은 키 / 다른 비율 두 student 가 서로 다른 normalized pose 를 받음을 검증.
3. ✅ **R10 fix (2026-06-08 round-2)** — source_profile 인자는 reserved/debug. Test 8 (test_normalize_pose_by_segments_output_independent_of_source_profile) 가 회귀 방지.
4. ✅ 동일 동작 + 체형 차이 = 정규화 후 deduction_score 합 60% 감소, IPSF 절대 deficit 5 + Sunity pose_reliability_low (C14 + R9) (Task 4 + test_160cm_pro_vs_140cm_student_removes_false_positive)
5. ✅ `bodyNormalizationConfidence` 항상 emit — comparisonType 무관, 0~1 범위. pose_frames=None 시 base + reference_match_bonus (B2 Plan 06-02 wiring)
6. ✅ Universal Principle (D-06-U1) — confidence < 0.5 → 정규화 OFF + warning, >= 0.5 → 5 필드 정규화 ON
7. ✅ **W1** 3-way contract lockstep — TS + Python + docs §8 + drift test + 3 ComparisonType + usedReferenceFallback (Task 5)
8. ✅ **W6** foreshortening proxy — shoulderHipRatio OFF 게이트
9. ✅ **C14 fix** — deficit code `pose_reliability_low` + docs/contract.md §8.1 IPSF divergence note
10. ✅ **R2 fix (round-2)** — BodyComparisonSourcePose dataclass + flat values + to_keypoints_array reshape + docs §8.2 + 3-way lockstep. ReferenceMotion 의 신규 `bodyComparisonSourcePose` 필드. Plan 06-03 백필 contract 의 source.
11. ✅ **R5 fix (round-2)** — spatial_dispersion_penalty 산식 자연화 (`clip((C_s/shoulder_width - 1.5) / 1.5, 0, 1)`) + 신규 fixture_high_dispersion_arms_sprawled + 검증 test
12. ✅ **R6 fix (round-2)** — to_coco17_array 사용으로 4채널 (uncertainty_proxy) 보존
13. ✅ **R8 fix (round-2)** — `compare_body_profiles(..., extra_warnings=[...])` 파라미터 + frozenset validation + dataclasses.replace 우회 차단
14. ✅ **R9 fix (round-2)** — '7 deficits' 표현 정정: 5 IPSF + Sunity pose_reliability_low; poor_transitions deferred
15. ✅ 6 fixture Validation Architecture (Task 1, R5 신규 fixture 포함)

**Plan 06-02 책임 (본 Plan 에서 X)**: pipeline _process 의 comparisonType 분기 wiring + Gemini fallback (usedReferenceFallback=True), Firestore complete_analysis 확장 + nested-array 차단 validator, frontend userAnalyses.normalize defensive validation, SAM Lambda Layer 빌드 smoke. **C2 (reviews fix)** — Phase 5 retroactive patch: TechniqueProfile.motion_id 필드 추가 (R1 fix: 맨 끝 위치) + Gemini recognizer populate + exact-match fallback. **B2** — pose_frames None handling production path via unified helper `_extract_video_analysis_inputs` (R3 fix). **R4** — student_profile non-null contract. **R2 wiring** — mode1 + mode3 fallback path 가 reference 의 bodyComparisonSourcePose 도 fetch 해서 source_keypoints 인자로 전달.

**PERS-01**: Phase 6 의 책임 = 정규화 + findings raw 산출. Phase 7 의 책임 = findings category 분류.
</success_criteria>

<output>
Create `.planning/phases/06-coaching/06-01-SUMMARY.md` when done — Tasks 1~5 완료 + C1 (target-profile L_ref) + C14 (pose_reliability_low rename + IPSF divergence) + W1/W6 + R2 (BodyComparisonSourcePose contract) + R5 (dispersion 산식) + R6 (4채널 보존) + R8 (extra_warnings) + R9 (deficits 표현) + R10 (source_profile 회귀 방지) 박제. Plan 06-02 의 진입 시그널 명시.
</output>
