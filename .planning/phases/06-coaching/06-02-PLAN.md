---
phase: 06-coaching
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - backend/shared/python/sunity_shared/analysis/technique.py
  - backend/shared/python/sunity_shared/analysis/gemini_technique_recognizer.py
  - backend/functions/pipeline/app.py
  - backend/shared/python/sunity_shared/firestore_admin.py
  - app/src/lib/userAnalyses.ts
  - backend/tests/phase06/test_technique_profile_motion_id.py
  - backend/tests/phase06/test_gemini_recognizer_populates_motion_id.py
  - backend/tests/phase06/test_pipeline_body_comparison.py
  - backend/tests/phase06/test_firestore_admin_body_comparison.py
  - backend/tests/phase06/test_pipeline_firestore_integration.py
  - backend/tests/phase06/test_dataclass_to_camel_case_dict.py
  - backend/tests/phase06/test_phase06_integration_smoke.py
autonomous: true
requirements: [PERS-01]
tags: [pipeline-wiring, firestore, sam-build, integration-smoke, phase-06, retro-phase5-patch]

must_haves:
  truths:
    - "pipeline `_process` 가 mode1/mode3 분기에서 Plan 06-01 의 body_normalizer.compare_body_profiles 를 호출하고 BodyComparisonReport 를 Firestore AnalysisDoc.result.bodyComparisonReport 에 저장한다 (D-06 success #1-4 의 production wiring)"
    - "C2 fix (2026-06-08 reviews) — Phase 5 retroactive patch: TechniqueProfile dataclass 에 `motion_id: str | None = None` 필드 추가. GeminiTechniqueRecognizer._build_profile (gemini_technique_recognizer.py:310) 이 canonical motion 이름을 motion_id 로 populate. mode3-first Gemini fallback path 는 `firestore_admin.get_reference_motion(profile.motion_id)` exact-match 로 변경 — `profile.name` 문자열 매칭 폐기."
    - "C3 fix (2026-06-08 reviews) — sibling helper `_angles_profile_and_frames_from_video(bucket, key, default_pole) -> tuple[ndarray, BodyNormalizationProfile|None, list[PoseFrame]]` 는 `RTMWPoseEngine.estimate(frames, pole_axis)` 의 반환값 (이미 list[PoseFrame]) 을 그대로 반환한다. coco17 round-trip 또는 _build_pose_frames 헬퍼 신설 없음. 별도 `compute_joint_angles` + `temporal_fill` 단계는 pose_frames 의 keypoints_3d 로부터 산출."
    - "B2 (pose_frames production path) — production bodyNormalizationConfidence 가 temporal variance + spatial dispersion 산식을 실제로 작동시킨다. 기존 `_angles_and_body_profile_from_video` (Phase 2 caller) 는 무수정."
    - "C9 fix (2026-06-08 reviews) — Pitfall 6 canary test 추가: `test_process_mode1_missing_ref_body_profile_emits_canary_warning` 는 가짜 reference (bodyNormalizationProfile=None) 로 mode1 실행 시 warnings ⊃ ['reference_profile_missing'] AND bodyNormalizationConfidence < CONFIDENCE_GATE 둘 다 검증."
    - "Firestore nested-array 차단 validator `_validate_flat_dict_no_nested_array` 는 list[str] (warnings) + list[dict-of-scalars-only] (findings) 만 허용. list[list] / list[dict-with-nested-list] 는 TypeError raise. (W5)"
    - "comparisonType 3 cases ('mode1' | 'mode3_first' | 'mode3_progress'). Gemini fallback 매칭 시 'mode3_first' + usedReferenceFallback=True. (W1)"
    - "C8 fix (2026-06-08 reviews) — `_dataclass_to_camel_case_dict` 헬퍼는 4 case 를 명시적으로 처리: (1) None → None, (2) dataclasses.is_dataclass → asdict + snake→camel key, (3) list → recurse map, (4) Enum/Literal → str(value). 4 unit test 로 검증."
    - "C14 propagation — `bad_angle` → `pose_reliability_low` rename 은 pipeline + Firestore validator + frontend normalize 검사 모두에 적용. Phase 6 어디에도 'bad_angle' 문자열이 코드 경로에 남아있지 않다."
    - "C15 fix (2026-06-08 reviews) — SAM Lambda Layer build smoke 는 `sam build --use-container` exit 0 뿐 아니라 `[ -f .aws-sam/build/SunityLayer/python/sunity_shared/analysis/body_normalizer.py ]` artifact 존재까지 검증. 신규 모듈 5종 (body_normalizer, body_normalization, _validate_flat_dict, _dataclass_to_camel_case, technique) 모두 동일."
  artifacts:
    - path: backend/shared/python/sunity_shared/analysis/technique.py
      provides: "C2 fix — TechniqueProfile dataclass 에 motion_id: str | None = None 필드 추가 (Phase 5 retroactive patch, default None 으로 backwards-compatible)"
      contains: ["motion_id: str | None = None"]
    - path: backend/shared/python/sunity_shared/analysis/gemini_technique_recognizer.py
      provides: "C2 fix — _build_profile (line 310) 이 motion_id=motion (canonical name) populate. low_confidence/unregistered/cache 경로의 TechniqueProfile 생성자도 동일 처리 (motion_id=None or cached value)"
      contains: ["motion_id=motion", "motion_id="]
    - path: backend/functions/pipeline/app.py
      provides: "C3 fix sibling helper `_angles_profile_and_frames_from_video` (RTMWPoseEngine.estimate 반환값 직접 사용) + C2 fix `_match_reference_by_motion_id` helper (firestore_admin.get_reference_motion exact-match) + `_process` comparisonType 분기 wiring + `_dataclass_to_camel_case_dict` (C8 4-case 명세)"
      contains: ["body_normalizer.compare_body_profiles", "_angles_profile_and_frames_from_video", "_match_reference_by_motion_id", "used_reference_fallback", "_dataclass_to_camel_case_dict", "fallback_reference_not_found"]
    - path: backend/shared/python/sunity_shared/firestore_admin.py
      provides: "complete_analysis(body_comparison_report=...) 시그너처 확장 + `_validate_flat_dict_no_nested_array` (W5 — list[str] + list[dict-scalar] 허용). `list_reference_motions_by_name` 헬퍼는 폐기 (C2 fix — exact-match motion_id 로 대체) — 기존 `get_reference_motion` 재사용"
      contains: ["def complete_analysis(", "def _validate_flat_dict_no_nested_array(", "body_comparison_report"]
    - path: app/src/lib/userAnalyses.ts
      provides: "normalize 에 bodyComparisonReport 통과 — TS 타입 추가만 (defensive validation), 별도 runtime 변환 없음. I2 positive assertion (grep >= 1)"
    - path: backend/tests/phase06/test_phase06_integration_smoke.py
      provides: "End-to-end smoke — _process → Firestore mock → bodyComparisonReport 검증 + must_haves.truths 박제 + C15 SAM artifact 존재 검증"
  key_links:
    - from: backend/functions/pipeline/app.py
      to: backend/shared/python/sunity_shared/analysis/body_normalizer.py
      via: "from sunity_shared.analysis import body_normalizer; body_normalizer.compare_body_profiles(...)"
      pattern: "body_normalizer\\.compare_body_profiles"
    - from: backend/functions/pipeline/app.py
      to: backend/shared/python/sunity_shared/firestore_admin.py
      via: "firestore_admin.complete_analysis(..., body_comparison_report=...)"
      pattern: "complete_analysis\\(.*body_comparison_report"
    - from: backend/functions/pipeline/app.py
      to: backend/shared/python/sunity_shared/firestore_admin.py
      via: "firestore_admin.get_reference_motion(profile.motion_id) — C2 fix exact-match"
      pattern: "get_reference_motion\\(.*motion_id"
    - from: backend/functions/pipeline/app.py
      to: backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/rtmw_engine.py
      via: "RTMWPoseEngine.estimate(frames, pole_axis) -> list[PoseFrame] — C3 fix 직접 반환"
      pattern: "estimate\\("
---

<objective>
Phase 6 vertical slice **production wiring** — Plan 06-01 의 algorithm + contract 본체를 production code path 에 연결.

**2026-06-08 revision (cross-AI review C2/C3/C7/C8/C9/C14/C15 incorporated):**
- **C2 (HIGH)**: Phase 5 retroactive patch. TechniqueProfile.motion_id 필드 추가 + Gemini recognizer populate + Plan 06-02 의 fallback path 가 exact-match `firestore_admin.get_reference_motion(profile.motion_id)` 사용. 구 plan 의 `profile.name` 문자열 매칭 + `list_reference_motions_by_name` 헬퍼는 폐기.
- **C3 (HIGH)**: `_build_pose_frames` 헬퍼 신설 폐기. `RTMWPoseEngine.estimate(frames, pole_axis)` 가 이미 `list[PoseFrame]` 반환 (rtmw_engine.py:165-236 확인) — 그대로 사용.
- **C7**: 박제 filler 제거. Task 1 의 (E) 가 구체적인 단계로 재작성.
- **C8**: `_dataclass_to_camel_case_dict` 4-case 명세 + 4 unit test 추가.
- **C9**: Pitfall 6 canary test 추가 — warnings + confidence 둘 다 검증.
- **C14**: Phase 6 의 어떤 production path 에도 `bad_angle` 문자열이 남아있지 않게 grep gate.
- **C15**: SAM build smoke 에 artifact 존재 acceptance criteria 추가.

본 Plan 의 4 task (Task 0 = C2 retroactive Phase 5 patch, Task 1 = pipeline wiring, Task 2 = firestore_admin + validator, Task 3 = camelCase helper + frontend, Task 4 = SAM smoke + 통합).

Output:
- 갱신 `backend/shared/python/sunity_shared/analysis/technique.py` (C2 — motion_id field)
- 갱신 `backend/shared/python/sunity_shared/analysis/gemini_technique_recognizer.py` (C2 — populate motion_id)
- 갱신 `backend/functions/pipeline/app.py` (sibling helper C3 + exact-match fallback C2 + 분기 wiring + camelCase 변환 C8)
- 갱신 `backend/shared/python/sunity_shared/firestore_admin.py` (complete_analysis 확장 + validator W5)
- 갱신 `app/src/lib/userAnalyses.ts` (defensive validation)
- 신규 6 test 파일 (technique motion_id, gemini populates motion_id, pipeline body comparison, firestore admin body comparison, pipeline firestore integration, dataclass_to_camel_case_dict 4-case + canary, integration smoke)
- SAM build artifact 검증 (C15)
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/phases/06-coaching/06-CONTEXT.md
@.planning/phases/06-coaching/06-RESEARCH.md
@.planning/phases/06-coaching/06-PATTERNS.md
@.planning/phases/06-coaching/06-REVIEWS.md
@.planning/phases/06-coaching/06-01-PLAN.md
@backend/shared/python/sunity_shared/analysis/technique.py
@backend/shared/python/sunity_shared/analysis/gemini_technique_recognizer.py
@backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/rtmw_engine.py
@backend/functions/pipeline/app.py
@backend/shared/python/sunity_shared/firestore_admin.py
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RunPod GPU 서버 → Lambda 위임 | `X-RunPod-Token` 헤더 (Phase 5 close-out). 본 plan 무수정 |
| Firestore Admin SDK → Firestore | service account key (Parameter Store) — 백엔드만 write |
| `_process` 내부 BodyNormalizationProfile / reference fetch | Firestore admin SDK + 기존 `users/{uid}/analyses/{id}` 격리. 본 plan 신규 field 추가만 |
| Gemini fallback path (C2 fix) | `profile.motion_id` (Gemini canonical) → exact-match `get_reference_motion`. 문자열 free-form 매칭 path 폐기 → 식별자 collision 위험 0 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Tampering | Firestore nested-array bypass (findings 안에 list 들어갈 시 TypeError) | mitigate | `_validate_flat_dict_no_nested_array` 헬퍼 신설 (W5 — list[str] / list[dict-of-scalars] 허용, list[list] reject). `complete_analysis(body_comparison_report=...)` 진입 시 호출 → 위반 시 TypeError raise, fail_analysis 진입 |
| T-06-02-02 | Spoofing | 다른 uid 의 BodyNormalizationProfile read/write 시도 | mitigate | Firestore admin SDK 가 `users/{uid}/analyses/{id}` 격리 (기존). `complete_analysis` 의 uid 인자는 `auth._ensure_firebase()` 검증 후 전달 |
| T-06-02-03 | Tampering | Gemini fallback path 가 잘못된 reference 매칭 (C2 fix 이전: free-form `profile.name` 매칭 → collision 가능) | mitigate | **C2 fix** — `profile.motion_id` (Gemini canonical) → `get_reference_motion(motion_id)` exact-match. motion_id 없거나 reference 미발견 시 fallback 비활성 + warnings ⊃ "fallback_reference_not_found" |
| T-06-02-04 | Denial of Service | mode1 분석 시 reference doc 의 `bodyNormalizationProfile` 미박제 (백필 미실행) → silently OFF | mitigate | Plan 06-01 의 `compare_body_profiles` 가 `reference_profile is None` 시 명시적 warning `reference_profile_missing` emit + confidence 하향. **C9 canary test 추가** — warnings + confidence 둘 다 검증. Plan 06-03 (백필) 가 후속. |
| T-06-02-05 | Tampering | `_dataclass_to_camel_case_dict` 변환 누락 (snake_case 가 Firestore 에 쓰여 contract drift) | mitigate | **C8 fix** — 4-case 명시 (None/dataclass/list/Enum) + 4 unit test |
| T-06-02-SC | Tampering | npm/pip install 없음 | accept | 신규 외부 패키지 0 |

(security_enforcement=true. ASVS V5 Input Validation = T-06-02-01/03/05. V4 Access Control = T-06-02-02.)
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0 (C2 retro Phase 5 patch): TechniqueProfile.motion_id 필드 + GeminiTechniqueRecognizer populate</name>
  <files>
    backend/shared/python/sunity_shared/analysis/technique.py,
    backend/shared/python/sunity_shared/analysis/gemini_technique_recognizer.py,
    backend/tests/phase06/test_technique_profile_motion_id.py,
    backend/tests/phase06/test_gemini_recognizer_populates_motion_id.py
  </files>
  <read_first>
    backend/shared/python/sunity_shared/analysis/technique.py,
    backend/shared/python/sunity_shared/analysis/gemini_technique_recognizer.py,
    backend/shared/python/sunity_shared/analysis/gemini_motion_classifier.py,
    .planning/phases/06-coaching/06-REVIEWS.md
  </read_first>
  <behavior>
    - Test 1 (test_technique_profile_carries_motion_id): TechniqueProfile(name="inversion", motion_id="inversion", category="recognized", joint_expectations={}) 생성 → profile.motion_id == "inversion"
    - Test 2 (test_technique_profile_motion_id_default_none): TechniqueProfile(name="x", category="unknown", joint_expectations={}) 생성 (motion_id 미지정) → profile.motion_id is None (backwards-compatible default)
    - Test 3 (test_fallback_recognizer_motion_id_none): FallbackRecognizer().recognize(angles=np.zeros((10, 17))) → profile.motion_id is None
    - Test 4 (test_gemini_build_profile_populates_motion_id): GeminiTechniqueRecognizer._build_profile("inversion", moments=[]) → returned profile.motion_id == "inversion" (canonical name 그대로)
    - Test 5 (test_gemini_low_confidence_path_motion_id_none): low-confidence path (mean_conf < threshold) 의 TechniqueProfile → motion_id is None (name="신뢰도 낮음")
    - Test 6 (test_gemini_unregistered_path_motion_id_none): unregistered path 의 TechniqueProfile → motion_id is None (name="미등록: ...")
    - Test 7 (test_gemini_cache_path_motion_id_from_cached_motion): `_profile_from_cache({"motion": "split"})` → motion_id == "split"
  </behavior>
  <action>
**C2 (HIGH severity reviews fix) — Phase 5 retroactive patch.**

(A) `backend/shared/python/sunity_shared/analysis/technique.py` 수정:
  - `TechniqueProfile` dataclass 의 필드에 `motion_id: str | None = None` 추가. 위치: `name: str` 바로 아래가 가독성 best (기존 `name` 가 표시용, `motion_id` 가 식별자). 기존 인스턴스 생성자 호출 (FallbackRecognizer 등) 은 default None 으로 backwards-compatible.
  - 한국어 docstring 갱신: `motion_id` 필드 위에 `# C2 fix (2026-06-08, Plan 06-02 reviews). Gemini canonical motion name 을 reference 컬렉션 lookup 의 stable key 로 박제. None 가능 (FallbackRecognizer / Gemini low_confidence / unregistered path 등에서). Plan 06-02 의 mode3-first Gemini fallback path 가 본 필드를 사용해 firestore_admin.get_reference_motion(motion_id) exact-match 수행.`
  - FallbackRecognizer.recognize() 의 TechniqueProfile 생성자 호출 무수정 (motion_id default None 이 자동 적용).

(B) `backend/shared/python/sunity_shared/analysis/gemini_technique_recognizer.py` 수정 — 4 곳의 TechniqueProfile 생성자 호출 (line 174, 197, 310, 322) 모두 motion_id 명시:
  - line 174 (low_confidence path): `TechniqueProfile(name="신뢰도 낮음", motion_id=None, category="low_confidence", ...)`
  - line 197 (unregistered path): `TechniqueProfile(name=f"미등록: {raw_motion_name}", motion_id=None, category="unregistered", ...)`
  - line 310 (_build_profile 정상 path): `TechniqueProfile(name=motion, motion_id=motion, category="recognized", joint_expectations=expectations, ..., hold_window=hold_window_tuple)`. canonical motion 이 곧 motion_id (primary key).
  - line 322 (_profile_from_cache): `TechniqueProfile(name=cached.get("motion", "cached"), motion_id=cached.get("motion"), category="recognized", ...)` — cache 의 motion 도 canonical name 이므로 motion_id 로 그대로 사용.
  - cache.store payload (line 215-219) 에 motion_id 별도 추가 불요 — payload["motion"] 이 곧 canonical, _profile_from_cache 가 motion_id 로 복원.

(C) 단위 테스트 2 파일:
  - `backend/tests/phase06/test_technique_profile_motion_id.py` — Test 1/2/3 (dataclass + FallbackRecognizer)
  - `backend/tests/phase06/test_gemini_recognizer_populates_motion_id.py` — Test 4/5/6/7 (4 path 모두). Gemini extractor 는 monkeypatch 로 stub (실 API 호출 X). cache 도 monkeypatch.

이모지 / 슬롭 코드 / fenced code block X. Korean comments.

[per C2 (2026-06-08 reviews HIGH), Phase 5 retroactive patch, backwards-compatible default None]
  </action>
  <verify>
    <automated>cd backend && pytest tests/phase06/test_technique_profile_motion_id.py tests/phase06/test_gemini_recognizer_populates_motion_id.py -v</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "motion_id:" backend/shared/python/sunity_shared/analysis/technique.py` >= 1 (field 정의)
    - Source: `grep -c "motion_id=motion" backend/shared/python/sunity_shared/analysis/gemini_technique_recognizer.py` >= 1 (line 310 path)
    - Source: `grep -c "motion_id=" backend/shared/python/sunity_shared/analysis/gemini_technique_recognizer.py` >= 4 (4 path 모두 populate)
    - Behavior: `pytest tests/phase06/test_technique_profile_motion_id.py tests/phase06/test_gemini_recognizer_populates_motion_id.py -v` exits 0 + 7 test PASS
    - Behavior: Phase 5 의 기존 GeminiTechniqueRecognizer 테스트가 회귀 없이 PASS (`pytest backend/tests/ -k gemini_technique -v` exits 0)
    - Behavior: FallbackRecognizer 기존 테스트 회귀 없음
  </acceptance_criteria>
  <done>C2 retroactive Phase 5 patch 완료 — TechniqueProfile.motion_id 필드 + Gemini 4 path populate. Task 1 (pipeline wiring) 이 본 필드를 exact-match fallback 에 사용.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: pipeline `_angles_profile_and_frames_from_video` sibling helper (C3 fix — RTMW estimate 직접 사용) + `_match_reference_by_motion_id` (C2 fix exact-match) + `_process` comparisonType 분기 wiring + usedReferenceFallback (W1)</name>
  <files>
    backend/functions/pipeline/app.py,
    backend/tests/phase06/test_pipeline_body_comparison.py
  </files>
  <read_first>
    backend/functions/pipeline/app.py,
    backend/shared/python/sunity_shared/analysis/body_normalizer.py,
    backend/shared/python/sunity_shared/analysis/body_normalization.py,
    backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/rtmw_engine.py,
    backend/shared/python/sunity_shared/analysis/pose_frame.py,
    backend/shared/python/sunity_shared/analysis/technique.py,
    backend/shared/python/sunity_shared/analysis/gemini_technique_recognizer.py,
    backend/shared/python/sunity_shared/firestore_admin.py,
    backend/tests/test_pipeline_mode1_smoke.py
  </read_first>
  <behavior>
    - Test 1 (test_process_mode1_with_ref_body_profile_returns_mode1_report): mode1 + ref 의 bodyNormalizationProfile 박제 → body_comparison_report.comparison_type == 'mode1' + scale_profile != None + used_reference_fallback == False
    - **Test 2 (C9 fix canary — test_process_mode1_missing_ref_body_profile_emits_canary_warning)**: mode1 + ref.bodyNormalizationProfile is None → comparison_type == 'mode1' + warnings ⊃ ["reference_profile_missing"] AND bodyNormalizationConfidence < CONFIDENCE_GATE (0.5). 두 assertion 모두 필수 (Pitfall 6 canary).
    - Test 3 (test_process_mode3_first_no_prev_no_gemini_match_returns_mode3_first): mode3 + prev=None + Gemini OFF (또는 profile.motion_id is None) → comparison_type == 'mode3_first' + scale_profile is None + used_reference_fallback == False
    - **Test 4 (C2 + W1 — test_process_mode3_first_with_gemini_fallback_exact_match)**: mode3 + prev=None + Gemini matched (`profile.motion_id == "inversion"`) + matched ref (`get_reference_motion("inversion")` 반환) + ref.bodyNormalizationProfile 박제 + confidence >= 0.5 → comparison_type == 'mode3_first' (W1, 4th case 부재) + scale_profile != None + **used_reference_fallback == True (W1)**. caller path 가 `get_reference_motion(profile.motion_id)` exact-match 사용 (free-form name 매칭 X).
    - **Test 5 (C2 — test_process_mode3_first_gemini_motion_id_missing_warns_fallback_not_found)**: mode3 + prev=None + Gemini matched (`profile.motion_id == "obscure_motion"`) + `get_reference_motion("obscure_motion")` 반환 None → comparison_type == 'mode3_first' + scale_profile is None + used_reference_fallback == False + **warnings ⊃ "fallback_reference_not_found"** (motion_id 는 있었으나 reference 미발견 신호).
    - Test 6 (test_process_mode3_progress_with_prev_body_profile): mode3 + prev.bodyNormalizationProfile 박제 → comparison_type == 'mode3_progress' + previous_analysis_id != None + used_reference_fallback == False
    - **Test 7 (C3 fix — test_process_uses_rtmw_estimate_directly_no_coco17_roundtrip)**: `_angles_profile_and_frames_from_video` 가 RTMWPoseEngine.estimate 결과 (list[PoseFrame]) 를 그대로 반환 — coco17 → PoseFrame round-trip 없음. mock RTMWPoseEngine.estimate 가 sentinel list 반환 → `_process` 가 호출한 compare_body_profiles 의 pose_frames arg == sentinel list (identity check 가능).
    - Test 8 (B2 — test_unstable_arm_swing_lowers_production_confidence): fixture_unstable_arm_swing → bodyNormalizationConfidence < 0.5 (production path 의 temporal variance trigger)
  </behavior>
  <action>
`backend/functions/pipeline/app.py::_process` 수정. 기존 시그너처 무변경. C2/C3 fix.

(A) **C3 fix — 신규 sibling helper `_angles_profile_and_frames_from_video(bucket, key, default_pole)`** (`_angles_and_body_profile_from_video` 패턴 정합, 별도 helper). 시그너처:
  - `def _angles_profile_and_frames_from_video(bucket: str, key: str, default_pole: PoleAxis) -> tuple[np.ndarray, BodyNormalizationProfile | None, list[PoseFrame]]:`
  - Korean docstring: `"""C3 fix (2026-06-08, Plan 06-02 reviews). Phase 6 production bodyNormalizationConfidence 의 temporal/spatial 산식이 작동하도록 pose_frames 를 함께 반환. RTMWPoseEngine.estimate(frames, pole_axis) 가 이미 list[PoseFrame] 반환 (rtmw_engine.py:165-236 확인) — 그대로 사용, coco17 round-trip 없음. 기존 _angles_and_body_profile_from_video (Phase 2 caller) 는 무수정 — 회귀 위험 0. (angles, profile, pose_frames) 반환."""`
  - 본체 단계:
    - `_ensure_adapters()` 호출 (싱글톤 NLF/RTMW lazy import)
    - `tempfile.NamedTemporaryFile(suffix=".mp4", delete=True)` (기존 패턴 정합)
    - `_s3.download_file(bucket, key, tmp.name)`
    - `frames = _FRAME_EXTRACTOR.extract(tmp.name)` — (T, H, W, 3) ndarray
    - `pose_frames = _RTMW_ENGINE.estimate(frames, default_pole)` — list[PoseFrame], 길이 = T
    - profile 산출: `from sunity_shared.analysis.body_normalization_measurer import measure_body_profile; profile = measure_body_profile(pose_frames)` — Phase 2 helper 재사용. 측정 실패 시 None.
    - angles 산출: pose_frames 의 keypoints_3d 를 (T, 17, 3) ndarray 로 stack 한 뒤 `compute_joint_angles(keypoints)` 호출 (기존 `_angles_and_body_profile_from_video` 의 동일 경로). 그 후 `temporal_fill(angles, joint_uncertainty(keypoints))` 적용.
    - return `(angles_filled, profile, pose_frames)`
  - default_pole 은 caller 가 `PoleAxis(axis_vector=(0.0, 1.0, 0.0), confidence_level="low", source="vertical_fallback", frame_index=None)` 으로 전달 (Phase 6 v1 — pole 검출 별도 phase 책임).

(B) **C2 fix — `_match_reference_by_motion_id(motion_id: str | None) -> dict | None`** 헬퍼 신설:
  - `if not motion_id: return None`
  - `return firestore_admin.get_reference_motion(motion_id)` (기존 함수 재사용 — `firestore_admin.py:106`). exact-match, None-safe.
  - Korean docstring: `"""C2 fix (2026-06-08, Plan 06-02 reviews). TechniqueProfile.motion_id (Gemini canonical) → reference 컬렉션 exact-match. 구 plan 의 list_reference_motions_by_name + free-form name 매칭 폐기 — collision 위험 0."""`

(C) `_process` 의 ML 산출 단계 수정 — Gemini ON/OFF path 분기:
  - 기존 `_angles_and_body_profile_from_video` 호출 위치를 식별 (Phase 2 추가). Phase 6 에서는 mode 분기 진입 직전에 새로 추가된 sibling helper 사용:
    - `default_pole = PoleAxis(...)` 구성 (위 (A) 참조)
    - `angles, student_profile, pose_frames = _angles_profile_and_frames_from_video(bucket, key, default_pole)`
  - Gemini ON path (`_gemini_enabled()` True): 기존 video_path 산출 + `_GEMINI_RECOGNIZER.recognize(angles, frames=video_path)` 으로 profile 산출 (Phase 5 정합). 별도 분기 유지.
  - Gemini OFF path: `profile = _FALLBACK_RECOGNIZER.recognize(angles)` (기존).
  - 양 path 모두 위 sibling helper 가 반환한 pose_frames 를 후속 compare_body_profiles 호출에 전달.

(D) comparisonType 분기 (W1 — 3 cases 만):
  - `if mode == models.MODE_EXPERT` (mode1):
    - `ref = firestore_admin.get_reference_motion(meta.get("referenceMotionId"))`
    - `ref_profile = BodyNormalizationProfile(**ref["bodyNormalizationProfile"]) if ref and ref.get("bodyNormalizationProfile") else None`
    - `body_comparison_report = body_normalizer.compare_body_profiles(pose_frames=pose_frames, student_profile=student_profile, reference_profile=ref_profile, comparison_type="mode1", angles=angles, technique_profile=profile, reference_motion_id=meta.get("referenceMotionId"), reference_athlete_name=ref.get("athleteName") if ref else None, used_reference_fallback=False, target_torso_px=_extract_target_torso_px(pose_frames))`
  - `else` (mode3 — MODE_SELF):
    - `prev = firestore_admin.get_previous_analysis(uid, analysis_id, mode=models.MODE_SELF)`
    - prev 없거나 prev.bodyNormalizationProfile 없으면 mode3_first:
      - `used_fallback = False`
      - `ref_profile = None`
      - `if profile.motion_id is not None and student_profile and student_profile.confidence >= 0.5`:
        - `matched = _match_reference_by_motion_id(profile.motion_id)` (C2 fix — exact-match)
        - `if matched and matched.get("bodyNormalizationProfile")`:
          - `ref_profile = BodyNormalizationProfile(**matched["bodyNormalizationProfile"])`
          - `used_fallback = True`
        - `elif matched is None`:
          - extra_warning = "fallback_reference_not_found" (C2 fix — motion_id 있었으나 reference 미발견 신호. body_comparison_report 의 warnings 에 caller 가 append. 본 task 의 wiring 은 ref_profile=None 으로 정상 진행. warning emit 은 compare_body_profiles 가 reference_profile is None 경로 처리 시 의도된 신호. 본 추가 warning 은 별도 분기 — pipeline 이 compare_body_profiles 호출 후 반환된 BodyComparisonReport.warnings 에 'fallback_reference_not_found' 를 추가하는 mutation 또는 wrapper로 처리.)
      - `body_comparison_report = body_normalizer.compare_body_profiles(pose_frames=pose_frames, student_profile=student_profile, reference_profile=ref_profile, comparison_type="mode3_first", angles=angles, technique_profile=profile, used_reference_fallback=used_fallback)`
      - motion_id 있었으나 매칭 실패 시 (extra_warning set): `body_comparison_report = replace(body_comparison_report, warnings=[*body_comparison_report.warnings, "fallback_reference_not_found"])` (frozen dataclass replace)
    - else (prev 박제):
      - `prev_profile = BodyNormalizationProfile(**prev["bodyNormalizationProfile"])`
      - `body_comparison_report = body_normalizer.compare_body_profiles(pose_frames=pose_frames, student_profile=student_profile, reference_profile=prev_profile, comparison_type="mode3_progress", angles=angles, technique_profile=profile, previous_analysis_id=prev.get("analysisId"), used_reference_fallback=False)`

(E) `_extract_target_torso_px(pose_frames)` 헬퍼 — pose_frames 의 평균 mid_shoulder↔mid_hip 픽셀 거리. NaN-safe (frames 없거나 keypoint 미감지 시 None 반환). compare_body_profiles 가 None 시 student_profile.torso_scale 로 fallback.

(F) `body_comparison_report` 저장은 Task 3 책임 (firestore_admin.complete_analysis 호출 wiring + camelCase 변환).

(G) 단위 테스트 `backend/tests/phase06/test_pipeline_body_comparison.py` — mock (firestore_admin.get_reference_motion / get_previous_analysis / complete_analysis monkeypatch). RTMWPoseEngine 도 monkeypatch — `estimate` 가 sentinel list[PoseFrame] 반환. compute_joint_angles / temporal_fill 도 monkeypatch. `_process` 직접 호출. `student_profile` = `BodyNormalizationProfile(estimated_height_scale=0.875, ..., confidence=0.8, warnings=[])`. assertion = behavior block Test 1~8 1:1. **Test 2 (C9)** + **Test 4 (C2 + W1)** + **Test 5 (C2 fallback_reference_not_found)** + **Test 7 (C3 직접 사용)** 가 reviews fix 핵심.

이모지 / 슬롭 코드 / fenced code block X. Korean comments.

[per D-06-B1, D-06-B2, D-06-B3 (3 cases — W1), C2 (motion_id exact-match), C3 (RTMW estimate 직접 사용), C9 (canary), W1 used_reference_fallback]
  </action>
  <verify>
    <automated>cd backend && pytest tests/phase06/test_pipeline_body_comparison.py -v</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "body_normalizer.compare_body_profiles" backend/functions/pipeline/app.py` >= 3 (mode1 + mode3 first + mode3 progress)
    - Source: `grep -c "_match_reference_by_motion_id" backend/functions/pipeline/app.py` >= 2 (선언 + 호출) (C2 fix)
    - Source: `grep -c "_angles_profile_and_frames_from_video" backend/functions/pipeline/app.py` >= 2 (선언 + 호출) (C3 fix)
    - Source: `grep -c "mode3_first_with_fallback" backend/functions/pipeline/app.py` == 0 (W1)
    - Source: `grep -c "used_reference_fallback" backend/functions/pipeline/app.py` >= 2 (W1)
    - **Source (C2)**: `grep -c "profile.motion_id" backend/functions/pipeline/app.py` >= 1 — motion_id 사용
    - **Source (C2)**: `grep -c "list_reference_motions_by_name" backend/functions/pipeline/app.py` == 0 — 구 free-form path 부재
    - **Source (C2)**: `grep -c "fallback_reference_not_found" backend/functions/pipeline/app.py` >= 1 — motion_id 매칭 실패 신호
    - **Source (C14)**: `grep -v '^#' backend/functions/pipeline/app.py | grep -c '"bad_angle"'` == 0
    - Behavior: `pytest tests/phase06/test_pipeline_body_comparison.py -v` exits 0 + 8 test PASS
    - Behavior: **Test 2 (C9 canary) PASS** — warnings ⊃ "reference_profile_missing" AND bodyNormalizationConfidence < 0.5
    - Behavior: **Test 4 (C2 exact-match) PASS** — used_reference_fallback==True via get_reference_motion(motion_id)
    - Behavior: **Test 5 (C2 fallback_reference_not_found) PASS**
    - Behavior: **Test 7 (C3 direct RTMW) PASS** — pose_frames identity check
    - Behavior: Test 8 (B2 production) PASS — bodyNormalizationConfidence < 0.5
  </acceptance_criteria>
  <done>pipeline _process comparisonType 분기 + C2 exact-match fallback + C3 RTMW 직접 사용 + W1 + C9 canary + C14 grep gate. Task 2 (firestore_admin + validator) 진입 가능.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: firestore_admin.complete_analysis 시그너처 확장 + nested-array 차단 validator (W5)</name>
  <files>
    backend/shared/python/sunity_shared/firestore_admin.py,
    backend/tests/phase06/test_firestore_admin_body_comparison.py
  </files>
  <read_first>
    backend/shared/python/sunity_shared/firestore_admin.py,
    backend/shared/python/sunity_shared/analysis/body_normalizer.py,
    backend/shared/python/sunity_shared/models.py
  </read_first>
  <behavior>
    - Test 1 (test_complete_analysis_accepts_body_comparison_report): complete_analysis(..., body_comparison_report={"comparisonType": "mode1", "findings": [{"deficitCode": "knee_toe_alignment", ...}], "scaleProfile": {...}, "warnings": [], "usedReferenceFallback": false}) → Firestore set merge 호출 + payload['result']['bodyComparisonReport'] 박제
    - Test 2 (W5 — test_validate_flat_dict_allows_list_of_strings): {"comparisonType": "mode1", "warnings": ["a", "b", "c"], "findings": []} → PASS
    - Test 3 (W5 — test_validate_flat_dict_allows_list_of_dict_scalars): {"comparisonType": "mode1", "findings": [{"deficitCode": "x", "confidence": 0.9}], ...} → PASS
    - Test 4 (test_validate_flat_dict_rejects_nested_array_in_list): {"warnings": [["nested"], ["bad"]]} → TypeError raise
    - Test 5 (test_validate_flat_dict_rejects_nested_dict_with_list): {"findings": [{"deficitCode": "x", "extraList": [1,2,3]}]} → TypeError raise (list-inside-dict-inside-list)
    - Test 6 (test_complete_analysis_body_comparison_none_skips_field): body_comparison_report=None → payload 에 bodyComparisonReport key 부재
    - Test 7 (test_complete_analysis_body_normalization_profile_flat_dict): body_normalization_profile=BodyNormalizationProfile dict → payload['bodyNormalizationProfile'] flat dict (mode3 progress fetch path)
    - **Test 8 (C14 — test_validate_flat_dict_accepts_pose_reliability_low_finding)**: {"findings": [{"deficitCode": "pose_reliability_low", "confidence": 0.3, ...}]} → PASS. deficitCode 가 string scalar 이므로 검증 통과.
  </behavior>
  <action>
`backend/shared/python/sunity_shared/firestore_admin.py::complete_analysis` 확장.

(A) 시그너처 변경 (Korean prose — fenced code 금지):
  - `def complete_analysis(uid, analysis_id, result, *, angles=None, angles_joint_keys=None, angles_frames=None, body_comparison_report=None, body_normalization_profile=None) -> None`

(B) **W5 — `_validate_flat_dict_no_nested_array(payload: dict, *, path: str = "") -> None`** 헬퍼 신설. `store_gemini_cache:186-199` 의 기존 검증 패턴을 일반화. 명세:
  - dict 입력 → 각 (key, value) 순회
  - value 가 scalar (str, int, float, bool, None) → PASS
  - value 가 dict → 재귀 `_validate_flat_dict_no_nested_array(value, path=f"{path}.{key}")`
  - value 가 list →
    - list 원소가 scalar (str, int, float, bool) → PASS (warnings list[str] 정합)
    - list 원소가 dict → 각 원소에 대해 재귀 `_validate_flat_dict_no_nested_array(item, path=f"{path}.{key}[{i}]")`. 단, 재귀 호출이 list-of-list 또는 dict-with-nested-list 만나면 TypeError raise. (findings list[dict] 정합)
    - list 원소가 list → `TypeError(f"{path}.{key} contains nested list (firestore-nested-array-flat)")` raise. (Firestore 의 nested-array 금지 정합 — [[firestore-nested-array-flat]])
    - list 원소가 None / 빈 → PASS
  - 예외 메시지 형식: `f"{path}.{key} must be flat (firestore-nested-array-flat): got nested {type}"`
  - **Korean docstring**: `"""W5 (2026-06-08, Plan 06-02). Firestore nested-array 금지 ([[firestore-nested-array-flat]]) 보장. BodyComparisonReport.warnings (list[str]) + BodyComparisonReport.findings (list[dict-of-scalars]) 허용. list[list] / dict-with-nested-list reject. store_gemini_cache:186-199 의 기존 검증을 일반화 — 동일 정신, 광범위 응용. 위반 시 TypeError + path 정보. caller (pipeline) 가 catch → fail_analysis(server_error) 진입."""`

(C) `complete_analysis` 본체:
  - `if body_comparison_report is not None`: `_validate_flat_dict_no_nested_array(body_comparison_report, path="bodyComparisonReport")`. `payload["result"] = {**(result or {}), "bodyComparisonReport": body_comparison_report}` (result 내부 — analysis.ts AnalysisResult.bodyComparisonReport 정합).
  - `if body_normalization_profile is not None`: `_validate_flat_dict_no_nested_array(body_normalization_profile, path="bodyNormalizationProfile")`. `payload["bodyNormalizationProfile"] = body_normalization_profile` (top-level — mode3 progress fetch path).
  - `_doc(models.analysis_doc_path(uid, analysis_id)).set(payload, merge=True)`

(D) Korean 주석: `# Phase 6 (2026-06-08, Plan 06-02) — D-06-B3 + [[firestore-nested-array-flat]] + W5 validator + C14 (pose_reliability_low). bodyComparisonReport = result 내부 (AnalysisResult.bodyComparisonReport 정합), bodyNormalizationProfile = top-level (mode3 progress prev fetch path).`

(E) 단위 테스트 `backend/tests/phase06/test_firestore_admin_body_comparison.py` — Firestore mock (`monkeypatch.setattr(firestore_admin, "_doc", mock_doc)`). behavior block Test 1~8 1:1.

**C2 fix 정합 — `list_reference_motions_by_name` 신설 폐기**. 구 plan 의 helper 는 명시적으로 만들지 않음 (C2 fix 가 `get_reference_motion(motion_id)` exact-match 로 대체).

이모지 / fenced code X. Korean comments.

[per D-06-B3, [[firestore-nested-array-flat]], W5, C2 (list_reference_motions_by_name 폐기)]
  </action>
  <verify>
    <automated>cd backend && pytest tests/phase06/test_firestore_admin_body_comparison.py -v</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "body_comparison_report" backend/shared/python/sunity_shared/firestore_admin.py` >= 3 (시그너처 + body + validator)
    - Source: `grep -c "_validate_flat_dict_no_nested_array" backend/shared/python/sunity_shared/firestore_admin.py` >= 2 (선언 + 호출)
    - **Source (C2)**: `grep -c "def list_reference_motions_by_name" backend/shared/python/sunity_shared/firestore_admin.py` == 0 — 구 helper 부재
    - Behavior: `pytest tests/phase06/test_firestore_admin_body_comparison.py -v` exits 0 + 8 test PASS
    - Behavior: Test 2 PASS (W5 — list[str] 허용)
    - Behavior: Test 3 PASS (W5 — list[dict-scalar] 허용)
    - Behavior: Test 4 PASS (list[list] reject)
    - Behavior: Test 5 PASS (dict-with-nested-list reject)
    - Behavior: Test 8 PASS (C14 — pose_reliability_low deficitCode 통과)
  </acceptance_criteria>
  <done>firestore_admin.complete_analysis 확장 + W5 validator + C2 정합 (구 helper 폐기). Task 3 (camelCase 변환 + complete_analysis 호출 wiring) 진입 가능.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: pipeline `_dataclass_to_camel_case_dict` helper (C8 4-case 명세) + complete_analysis 호출 wiring + frontend userAnalyses.normalize defensive validation</name>
  <files>
    backend/functions/pipeline/app.py,
    app/src/lib/userAnalyses.ts,
    backend/tests/phase06/test_pipeline_firestore_integration.py,
    backend/tests/phase06/test_dataclass_to_camel_case_dict.py
  </files>
  <read_first>
    backend/functions/pipeline/app.py,
    backend/shared/python/sunity_shared/firestore_admin.py,
    backend/shared/python/sunity_shared/analysis/body_normalizer.py,
    app/src/lib/userAnalyses.ts,
    app/src/types/analysis.ts
  </read_first>
  <behavior>
    - Test 1 (test_process_calls_complete_analysis_with_body_comparison_report): mode1 분석 end-to-end (mock) → firestore_admin.complete_analysis 호출 + call_args 의 body_comparison_report arg 가 camelCase 필드 (comparisonType, bodyNormalizationConfidence, usedReferenceFallback) 보유
    - Test 2 (test_process_calls_complete_analysis_with_body_normalization_profile): student_profile 있을 때 → complete_analysis 가 body_normalization_profile arg 받음 + camelCase (estimatedHeightScale, armScale, ...)
    - Test 3 (test_process_handles_none_body_comparison_report_gracefully): body_comparison_report 가 None 일 수 있는 path → complete_analysis 호출 시 None 으로 전달 — fail_analysis 호출 X
    - Test 4 (test_process_validates_flat_dict_before_firestore_write): findings 에 nested list 발생 시 validator 가 TypeError → fail_analysis(server_error) 진입
    - Test 5 (test_userAnalyses_normalize_passes_through_body_comparison_report): app/src/lib/userAnalyses.ts::normalize 가 raw.result.bodyComparisonReport 통과 — TS 타입만 정의, runtime 변환 없음. tsc --noEmit 통과.
    - **Test 6 (C8 — test_dataclass_to_camel_case_dict_handles_none)**: `_dataclass_to_camel_case_dict(None)` → returns None
    - **Test 7 (C8 — test_dataclass_to_camel_case_dict_recurses_nested_dataclass)**: `_dataclass_to_camel_case_dict(BodyComparisonReport(comparison_type='mode1', scale_profile=ScaleProfile(estimated_height_scale=1.0, ...), ...))` → returns {"comparisonType": "mode1", "scaleProfile": {"estimatedHeightScale": 1.0, ...}, ...} — 중첩 dataclass 도 변환
    - **Test 8 (C8 — test_dataclass_to_camel_case_dict_maps_list_of_dataclasses)**: `_dataclass_to_camel_case_dict(BodyComparisonReport(..., findings=[BodyComparisonFinding(deficit_code='x', ...)]))` → returns {..., "findings": [{"deficitCode": "x", ...}]} — list 원소 dataclass 도 recurse
    - **Test 9 (C8 — test_dataclass_to_camel_case_dict_serializes_enum_literal)**: `_dataclass_to_camel_case_dict(BodyComparisonReport(comparison_type=ComparisonType.MODE1, ...))` (Literal 타입은 str 이므로 사실상 Test 7 의 'mode1' 와 동일 검증) — 명시적으로 Enum 도 `str(value)` 로 직렬화 — `from enum import Enum; class E(Enum): A = 'a'; _dataclass_to_camel_case_dict(E.A) == 'a'` 또는 동등 검증
  </behavior>
  <action>
Task 1 의 `_process` 에 firestore_admin.complete_analysis 호출 wiring 추가. Task 2 의 helper + validator 활용.

(A) **C8 fix — `_dataclass_to_camel_case_dict(obj)`** helper 신설. pipeline/app.py 모듈 상단 또는 sunity_shared 의 새 utility 모듈 (implementer 선택). 4-case 명세 (reviews fix 핵심):
  - **Case 1 — None**: `if obj is None: return None`
  - **Case 2 — dataclass**: `if dataclasses.is_dataclass(obj)`: `raw = dataclasses.asdict(obj)` 호출 (dict 반환). 그 결과 dict 의 각 (k, v) 에 대해 key 를 `_snake_to_camel(k)` 변환 + value 를 재귀 `_dataclass_to_camel_case_dict(v)` 변환. 그러나 asdict 가 이미 list / dict / scalar 로 풀어버리므로, 재귀 시 v 는 dataclass 가 아닌 dict/list/scalar 형태일 수 있음. 따라서 재귀 로직이 dict/list/scalar 도 같은 함수에서 처리.
  - **Case 3 — list**: `if isinstance(obj, list)`: `return [_dataclass_to_camel_case_dict(x) for x in obj]`
  - **Case 4 — dict**: `if isinstance(obj, dict)`: `return {_snake_to_camel(k): _dataclass_to_camel_case_dict(v) for k, v in obj.items()}` (asdict 가 만든 dict, 또는 nested dict)
  - **Case 5 — Enum**: `if isinstance(obj, Enum): return str(obj.value)` (또는 obj.value)
  - **Default — scalar**: `return obj` (str, int, float, bool, etc.)
  - `_snake_to_camel(key: str) -> str`: split on '_' → 첫 단어 lowercase + 나머지 capitalize → join. 예: `comparison_type` → `comparisonType`, `body_normalization_confidence` → `bodyNormalizationConfidence`, `used_reference_fallback` → `usedReferenceFallback`.
  - Korean docstring: `"""C8 fix (2026-06-08, Plan 06-02 reviews). dataclass / list / dict / Enum / scalar 5 case 명시. BodyComparisonReport 의 중첩 ScaleProfile + list[BodyComparisonFinding] 까지 모두 camelCase 변환. None 입력 시 None 반환 (Firestore 가 None 필드 skip)."""`

(B) `_process` 의 `complete_analysis` 호출 wiring:
  - `body_comparison_report_dict = _dataclass_to_camel_case_dict(body_comparison_report) if body_comparison_report else None`
  - `student_profile_dict = _dataclass_to_camel_case_dict(student_profile) if student_profile else None`
  - `firestore_admin.complete_analysis(uid, analysis_id, result, angles=..., body_comparison_report=body_comparison_report_dict, body_normalization_profile=student_profile_dict)`

(C) try/except — `_validate_flat_dict_no_nested_array` TypeError catch → `firestore_admin.fail_analysis(uid, analysis_id, "server_error", str(e))`. RESEARCH.md §Pitfall 5.

(D) `app/src/lib/userAnalyses.ts::normalize`:
  - Plan 06-01 Task 5 의 `AnalysisResult.bodyComparisonReport?: BodyComparisonReport` 타입은 이미 정의됨. normalize 함수는 `result: raw.result as AnalysisDoc['result']` passthrough (TypeScript type 정합으로 충분). 별도 runtime defensive validation 불요 (Firestore admin SDK 가 validator 통해 쓴 데이터만 도달).
  - Korean 주석: `// Phase 6 (2026-06-08, Plan 06-02): AnalysisResult.bodyComparisonReport 는 TS 타입 정합으로 자동 정상화 — 별도 normalize 함수 불요. raw passthrough. backend validator 가 nested-array 차단 보장 (W5).`
  - I2 positive assertion — 본 파일에 `bodyComparisonReport` 문자열이 최소 1회 등장 (TS 타입 정의 의존하는 주석 또는 type annotation 또는 narrow 분기).

(E) 단위 테스트:
  - `backend/tests/phase06/test_pipeline_firestore_integration.py` — `complete_analysis` mock. behavior Test 1~5.
  - `backend/tests/phase06/test_dataclass_to_camel_case_dict.py` — **C8 4-case 명세 검증 (Test 6/7/8/9)**. fixture 로 BodyComparisonReport + ScaleProfile + BodyComparisonFinding 인스턴스 사용.

이모지 / 슬롭 코드 / fenced code X. Korean comments.

[per D-06-B3, [[firestore-nested-array-flat]], C8 (4-case 명세), Pitfall 5 nested array]
  </action>
  <verify>
    <automated>cd backend && pytest tests/phase06/test_pipeline_firestore_integration.py tests/phase06/test_dataclass_to_camel_case_dict.py -v && cd ../app && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "_dataclass_to_camel_case_dict\\|_to_camel_case" backend/functions/pipeline/app.py` >= 2
    - Source: `grep -c "body_comparison_report=" backend/functions/pipeline/app.py` >= 1
    - Source: `grep -c "body_normalization_profile=" backend/functions/pipeline/app.py` >= 1
    - Source: `grep -c "usedReferenceFallback\\|used_reference_fallback" backend/functions/pipeline/app.py` >= 1 (W1)
    - Source (I2): `grep -c "bodyComparisonReport" app/src/lib/userAnalyses.ts` >= 1
    - Behavior: `pytest tests/phase06/test_pipeline_firestore_integration.py tests/phase06/test_dataclass_to_camel_case_dict.py -v` exits 0 + 9 test PASS
    - Behavior: Test 1 PASS — body_comparison_report arg 의 dict 키 = camelCase (comparisonType / bodyNormalizationConfidence / usedReferenceFallback)
    - **Behavior: Test 6 PASS (C8 None)**
    - **Behavior: Test 7 PASS (C8 nested dataclass)**
    - **Behavior: Test 8 PASS (C8 list of dataclasses)**
    - **Behavior: Test 9 PASS (C8 Enum)**
    - Behavior: `cd app && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>pipeline → Firestore wiring + camelCase 변환 (C8 4-case 명세) + frontend defensive validation. Task 4 (SAM build artifact 검증 + 통합 smoke) 진입 가능.</done>
</task>

<task type="auto">
  <name>Task 4: SAM Lambda Layer 빌드 smoke + artifact 존재 검증 (C15 fix) + 전체 통합 smoke test + I2 positive assertion</name>
  <files>
    backend/tests/phase06/test_phase06_integration_smoke.py
  </files>
  <read_first>
    backend/template.yaml,
    backend/functions/pipeline/app.py,
    backend/shared/python/sunity_shared/analysis/body_normalizer.py,
    backend/shared/python/sunity_shared/firestore_admin.py,
    backend/shared/python/sunity_shared/analysis/technique.py,
    app/src/lib/userAnalyses.ts
  </read_first>
  <action>
Phase 6 전체 통합 smoke + SAM build artifact 검증.

(A) 통합 smoke test `backend/tests/phase06/test_phase06_integration_smoke.py` — Plan 06-01 Wave 0 fixture + Plan 06-01 Tasks 2~5 + Plan 06-02 Tasks 0~3 모두 통합. Tests:
  - `test_full_pipeline_mode1_smoke` — fixture_160cm_pro_vs_140cm_student + mock Firestore + mock S3 + RTMW estimator stub (sentinel pose_frames) → `_process` 전체 호출 → Firestore complete_analysis 호출 → bodyComparisonReport.comparisonType == 'mode1' + scaleProfile != None + findings non-empty + bodyNormalizationConfidence > 0 + usedReferenceFallback == False
  - `test_full_pipeline_mode3_first_no_fallback_smoke` — fixture + Gemini OFF (profile.motion_id is None) → comparisonType == 'mode3_first' + scaleProfile is None + usedReferenceFallback == False + warnings ⊃ "low_confidence_normalization_off"
  - **`test_full_pipeline_mode3_first_with_gemini_fallback_smoke` (C2 + W1 reviews fix)** — fixture + Gemini matched (`profile.motion_id = "inversion"`) + matched ref 박제 → comparisonType == 'mode3_first' + scaleProfile != None + **usedReferenceFallback == True**. 사용 경로 = `_match_reference_by_motion_id("inversion")` → `get_reference_motion("inversion")` exact-match.
  - **`test_full_pipeline_mode3_first_with_gemini_motion_id_no_ref_match_smoke` (C2)** — fixture + Gemini matched (`profile.motion_id = "obscure_motion"`) + `get_reference_motion` 반환 None → comparisonType == 'mode3_first' + usedReferenceFallback == False + warnings ⊃ "fallback_reference_not_found"
  - `test_full_pipeline_all_must_haves_emitted` — must_haves.truths 모두 검증 (scale_profile 산출, normalize_pose_by_segments 호출, IPSF deficit 절대, bodyNormalizationConfidence 항상 emit, C2 motion_id exact-match, C3 RTMW 직접 사용, C9 canary case 별도 verify)
  - **`test_pose_reliability_low_deficit_code_in_findings` (C14)** — 불안정 pose_frames 입력 시 findings 의 deficit_code 중 적어도 하나가 `pose_reliability_low`. `bad_angle` 문자열은 어디에도 없음.

(B) **C15 fix — SAM Lambda Layer artifact 검증**: `backend/template.yaml` 의 sunity_shared layer 가 신규 모듈을 포함하는지 정적 확인. 별도 shell test 형태 (implementer 가 수동 실행 or CI 통합):
  - 1단계: `cd backend && sam build --use-container` (메모리 [[sam-build-native-deps]] 정합 — Mac native binary 가 Lambda Linux 에서 ImportError 방지)
  - 2단계: 다음 artifact 존재 검증 (acceptance criteria):
    - `[ -f .aws-sam/build/SunityLayer/python/sunity_shared/analysis/body_normalizer.py ]`
    - `[ -f .aws-sam/build/SunityLayer/python/sunity_shared/analysis/body_normalization.py ]`
    - `[ -f .aws-sam/build/SunityLayer/python/sunity_shared/analysis/body_normalization_measurer.py ]`
    - `[ -f .aws-sam/build/SunityLayer/python/sunity_shared/analysis/technique.py ]` (C2 fix 의 motion_id field 가 production 에 도달하는지)
    - `[ -f .aws-sam/build/SunityLayer/python/sunity_shared/firestore_admin.py ]` (W5 validator + complete_analysis 시그너처 확장)
  - layer 이름이 `SunityLayer` 가 아닐 경우 (template.yaml 확인 필요) implementer 가 동등 path 로 교정.
  - deploy 는 본 task 책임 X (Pod 동기화 별도, [[gsd-pod-work-push-first]] 정합).

(C) 전체 phase06 unit suite + TS check — `cd backend && pytest tests/phase06 -v` + `cd app && npx tsc --noEmit`.

(D) **C14 grep gate (final)** — `grep -rn '"bad_angle"' backend/functions/pipeline/app.py backend/shared/python/sunity_shared/analysis/body_normalizer.py backend/shared/python/sunity_shared/firestore_admin.py app/src/types/analysis.ts app/src/lib/userAnalyses.ts | grep -v '^#'` exit 1 (matches == 0). Phase 6 production path 어디에도 `bad_angle` literal 부재.

이모지 / 슬롭 코드 X.

[per CLAUDE.md Cross-cutting 3-way lockstep, [[sam-build-native-deps]], [[gsd-pod-work-push-first]], I2 positive assertion, C2/C3/C14/C15 reviews fix]
  </action>
  <verify>
    <automated>cd backend && pytest tests/phase06 -v && cd ../app && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `pytest backend/tests/phase06 -v` exits 0 + Phase 6 전체 unit suite PASS
    - Behavior: `cd app && npx tsc --noEmit` exits 0
    - Behavior: test_full_pipeline_mode1_smoke PASS
    - Behavior: test_full_pipeline_mode3_first_with_gemini_fallback_smoke PASS (C2 + W1)
    - Behavior: test_full_pipeline_mode3_first_with_gemini_motion_id_no_ref_match_smoke PASS (C2 fallback_reference_not_found)
    - Behavior: test_pose_reliability_low_deficit_code_in_findings PASS (C14)
    - **(C15 SAM artifact)** — `cd backend && sam build --use-container && [ -f .aws-sam/build/SunityLayer/python/sunity_shared/analysis/body_normalizer.py ] && [ -f .aws-sam/build/SunityLayer/python/sunity_shared/analysis/body_normalization.py ] && [ -f .aws-sam/build/SunityLayer/python/sunity_shared/analysis/technique.py ] && [ -f .aws-sam/build/SunityLayer/python/sunity_shared/firestore_admin.py ]` exit 0 (layer 이름은 template.yaml 확인 후 교정)
    - I2 — `grep -c "bodyComparisonReport" app/src/lib/userAnalyses.ts` >= 1
    - **(C14 final grep gate)** — `grep -rn '"bad_angle"' backend/functions/pipeline/app.py backend/shared/python/sunity_shared/analysis/body_normalizer.py backend/shared/python/sunity_shared/firestore_admin.py app/src/types/analysis.ts app/src/lib/userAnalyses.ts | grep -v '^#' | wc -l` == 0
  </acceptance_criteria>
  <done>Phase 6 vertical slice production wiring 완료. C15 SAM artifact 검증 + C14 grep gate + I2 positive assertion 모두 GREEN. Plan 06-03 (정은지 reference 백필) 진입 가능.</done>
</task>

</tasks>

<verification>
## Plan 06-02 verification (production wiring)

- [ ] `pytest backend/tests/phase06/test_technique_profile_motion_id.py tests/phase06/test_gemini_recognizer_populates_motion_id.py tests/phase06/test_pipeline_body_comparison.py tests/phase06/test_firestore_admin_body_comparison.py tests/phase06/test_pipeline_firestore_integration.py tests/phase06/test_dataclass_to_camel_case_dict.py tests/phase06/test_phase06_integration_smoke.py -v` 전부 GREEN
- [ ] `cd app && npx tsc --noEmit` exits 0
- [ ] `grep -c "body_normalizer" backend/functions/pipeline/app.py` >= 3
- [ ] `grep -c "body_comparison_report" backend/shared/python/sunity_shared/firestore_admin.py` >= 3
- [ ] **(C2)** `grep -c "motion_id" backend/shared/python/sunity_shared/analysis/technique.py` >= 1
- [ ] **(C2)** `grep -c "motion_id=" backend/shared/python/sunity_shared/analysis/gemini_technique_recognizer.py` >= 4
- [ ] **(C2)** `grep -c "_match_reference_by_motion_id" backend/functions/pipeline/app.py` >= 2
- [ ] **(C2)** `grep -c "list_reference_motions_by_name" backend/shared/python/sunity_shared/firestore_admin.py backend/functions/pipeline/app.py` == 0
- [ ] **(C2)** `grep -c "fallback_reference_not_found" backend/functions/pipeline/app.py` >= 1
- [ ] **(C3)** `grep -c "_angles_profile_and_frames_from_video" backend/functions/pipeline/app.py` >= 2
- [ ] **(C3)** `grep -c "_build_pose_frames" backend/functions/pipeline/app.py` == 0 (C3 fix — 별도 헬퍼 신설 폐기, RTMW 직접 사용)
- [ ] **(C8)** `grep -c "_dataclass_to_camel_case_dict\\|_snake_to_camel" backend/functions/pipeline/app.py` >= 2
- [ ] **(C9)** `pytest backend/tests/phase06/test_pipeline_body_comparison.py::test_process_mode1_missing_ref_body_profile_emits_canary_warning -v` PASS
- [ ] **(C14)** `grep -rn '"bad_angle"' backend/functions/pipeline/app.py backend/shared/python/sunity_shared/analysis/body_normalizer.py backend/shared/python/sunity_shared/firestore_admin.py app/src/types/analysis.ts app/src/lib/userAnalyses.ts | grep -v '^#' | wc -l` == 0
- [ ] **(C15)** `sam build --use-container` exit 0 + 5 artifact 존재
- [ ] `grep -c "mode3_first_with_fallback" backend/functions/pipeline/app.py` == 0 (W1)
- [ ] `grep -c "used_reference_fallback\\|usedReferenceFallback" backend/functions/pipeline/app.py` >= 1 (W1)
- [ ] `grep -c "bodyComparisonReport" app/src/lib/userAnalyses.ts` >= 1 (I2)
</verification>

<success_criteria>
Plan 06-02 Success Criteria — production wiring (reviews-revised):

1. ✅ pipeline `_process` 의 comparisonType 분기 wiring + Gemini fallback (Task 1)
2. ✅ **C2 fix (HIGH reviews)** — Phase 5 retroactive patch: TechniqueProfile.motion_id 필드 + Gemini recognizer 4 path populate + pipeline 의 `_match_reference_by_motion_id` exact-match (Task 0 + Task 1 + Task 2 정합)
3. ✅ **C3 fix (HIGH reviews)** — `_build_pose_frames` 헬퍼 신설 폐기. `RTMWPoseEngine.estimate()` 반환 list[PoseFrame] 직접 사용 — coco17 round-trip 없음 (Task 1)
4. ✅ Firestore flat 저장 + W5 nested-array 차단 validator + result.bodyComparisonReport (Task 2 + Task 3)
5. ✅ **W5** — list[str] (warnings) + list[dict-of-scalars] (findings) 허용. list[list] / list[dict-with-nested-list] reject (Task 2)
6. ✅ **W1** wiring: comparisonType 3 cases 박제. Gemini matched 시 usedReferenceFallback=True (Task 1 + Task 3)
7. ✅ **C8 fix (reviews)** — `_dataclass_to_camel_case_dict` 4-case 명세 (None / dataclass / list / Enum) + 4 unit test (Task 3)
8. ✅ **C9 fix (reviews)** — Pitfall 6 canary test: mode1 missing ref → warnings + confidence 둘 다 검증 (Task 1)
9. ✅ **C14 fix (reviews)** — Phase 6 production path 의 `bad_angle` literal 부재. grep gate (Task 1 + Task 4)
10. ✅ frontend defensive validation — TS contract 자동 정상화 (Task 3) + I2 positive assertion (`bodyComparisonReport` 문자열 >= 1)
11. ✅ **C15 fix (reviews)** — SAM Lambda Layer artifact 5종 존재 검증 + build smoke (Task 4)
12. ✅ 통합 smoke test — must_haves.truths 박제 (Task 4)

**Plan 06-03 책임 (본 Plan 에서 X)**: 정은지 reference 5개 영상의 bodyNormalizationProfile 백필. C5 dry-run flag + C6 belle Pod sweep (deferred) + C7 filler 제거 + C12 revert script.
</success_criteria>

<output>
Create `.planning/phases/06-coaching/06-02-SUMMARY.md` when done — Tasks 0~4 완료 + C2/C3/C7/C8/C9/C14/C15 reviews fix 명시 + Pod 동기화 필요 표기 ([[gsd-pod-work-push-first]] 정합).
</output>
