---
phase: 06-coaching
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - backend/shared/python/sunity_shared/analysis/technique.py
  - backend/shared/python/sunity_shared/analysis/gemini_technique_recognizer.py
  - backend/functions/pipeline/app.py
  - backend/shared/python/sunity_shared/firestore_admin.py
  - app/src/lib/userAnalyses.ts
  - backend/tests/phase06/test_technique_profile_motion_id.py
  - backend/tests/phase06/test_gemini_recognizer_populates_motion_id.py
  - backend/tests/phase06/test_pipeline_body_comparison.py
  - backend/tests/phase06/test_firestore_admin_body_comparison.py
  - backend/tests/phase06/test_pipeline_firestore_integration.py
  - backend/tests/phase06/test_dataclass_to_camel_case_dict.py
  - backend/tests/phase06/test_phase06_integration_smoke.py
autonomous: true
requirements: [PERS-01]
tags: [pipeline-wiring, firestore, sam-build, integration-smoke, phase-06, retro-phase5-patch]

must_haves:
  truths:
    - "pipeline `_process` 가 mode1/mode3 분기에서 Plan 06-01 의 body_normalizer.compare_body_profiles 를 호출하고 BodyComparisonReport 를 Firestore AnalysisDoc.result.bodyComparisonReport 에 저장한다 (D-06 success #1-4 의 production wiring)"
    - "**R1 fix (2026-06-08 round-2)** — TechniqueProfile dataclass 에 `motion_id: str | None = None` 필드 추가, 위치는 **dataclass 의 맨 끝 (기존 `hold_window` 필드 뒤)**. default 값 있는 필드가 default 없는 필드 앞에 오면 Python dataclass 가 `non-default argument follows default argument` 로 import 거부 — 본 fix 가 그것을 회피. 기존 positional caller (FallbackRecognizer 등) 영향 0. GeminiTechniqueRecognizer._build_profile 4 path (line 174 / 197 / 310 / 322) 모두 keyword argument `motion_id=` 로 populate."
    - "**R3 fix (2026-06-08 round-2)** — 기존 `_angles_and_video_path_from_video` + 신설 예정이었던 `_angles_profile_and_frames_from_video` 두 helper 를 단일 `_extract_video_analysis_inputs(bucket, key, default_pole, *, keep_local_video=False) -> _VideoAnalysisInputs` 로 통합. 반환 = `(angles, student_profile, pose_frames, local_video_path: Path | None)`. S3 download + frame_extract + RTMW estimate 단 1회만 실행. Gemini ON 이면 `keep_local_video=True` 로 video_path 보존, OFF 면 cleanup. 양 helper 가 동시 호출되어 double download / double RTMW 시도 회귀 차단."
    - "**R4 fix (2026-06-08 round-2)** — `_extract_video_analysis_inputs` 의 student_profile 반환 타입은 `BodyNormalizationProfile` (**non-null 계약**). 기존 `measure_body_profile` 이 실패 시 `_fallback_profile(warnings=['profile_measurement_failed'])` 의 BodyNormalizationProfile 을 반환 — None 반환 path 없음. confidence 산식이 student_profile.confidence 를 안전하게 참조. caller 별도 None check 불요."
    - "C2 fix (2026-06-08 reviews) — Phase 5 retroactive patch: TechniqueProfile dataclass 에 `motion_id: str | None = None` 필드 추가 (R1 fix 위치 정합). GeminiTechniqueRecognizer._build_profile (gemini_technique_recognizer.py:310) 이 canonical motion 이름을 motion_id 로 populate. mode3-first Gemini fallback path 는 `firestore_admin.get_reference_motion(profile.motion_id)` exact-match 로 변경 — `profile.name` 문자열 매칭 폐기."
    - "**R2 wiring (2026-06-08 round-2)** — mode1 path 와 mode3 fallback path 가 각각 reference doc 에서 `bodyNormalizationProfile` + **신규 `bodyComparisonSourcePose`** 두 필드를 모두 fetch. `compare_body_profiles(source_keypoints=ref_source_pose.to_keypoints_array(), source_profile=ref_profile, target_pose_frames=pose_frames, target_profile=student_profile, ...)` 호출. source_pose 미존재 시 `extra_warnings=['reference_source_pose_missing']` 으로 주입 (R8 정합) + confidence 산출 하향. **`_extract_target_torso_px(pose_frames)` helper 신설** — target 영상의 평균 mid_shoulder↔mid_hip 픽셀 거리 산출."
    - "**B2 (pose_frames production path)** — production bodyNormalizationConfidence 가 temporal variance + spatial dispersion 산식을 실제로 작동. R3 통합 helper 가 pose_frames 를 보장."
    - "C9 fix (2026-06-08 reviews) — Pitfall 6 canary test 추가: `test_process_mode1_missing_ref_body_profile_emits_canary_warning` 는 가짜 reference (bodyNormalizationProfile=None) 로 mode1 실행 시 warnings ⊃ ['reference_profile_missing'] AND bodyNormalizationConfidence < CONFIDENCE_GATE 둘 다 검증. **R2 신규 canary** — `test_process_mode1_missing_ref_source_pose_emits_canary_warning` (bodyComparisonSourcePose=None) → warnings ⊃ ['reference_source_pose_missing'] AND scale_profile is None."
    - "Firestore nested-array 차단 validator `_validate_flat_dict_no_nested_array` 는 list[str] (warnings) + list[dict-of-scalars-only] (findings) 만 허용. list[list] / list[dict-with-nested-list] 는 TypeError raise. (W5)"
    - "comparisonType 3 cases ('mode1' | 'mode3_first' | 'mode3_progress'). Gemini fallback 매칭 시 'mode3_first' + usedReferenceFallback=True. (W1)"
    - "C8 fix (2026-06-08 reviews) — `_dataclass_to_camel_case_dict` 헬퍼는 4 case 를 명시적으로 처리: (1) None → None, (2) dataclasses.is_dataclass → asdict + snake→camel key, (3) list → recurse map, (4) Enum/Literal → str(value). 4 unit test 로 검증."
    - "C14 propagation — `bad_angle` → `pose_reliability_low` rename 은 pipeline + Firestore validator + frontend normalize 검사 모두에 적용. Phase 6 어디에도 'bad_angle' 문자열이 코드 경로에 남아있지 않다."
    - "C15 fix (2026-06-08 reviews) — SAM Lambda Layer build smoke 는 `sam build --use-container` exit 0 뿐 아니라 `[ -f .aws-sam/build/SunityLayer/python/sunity_shared/analysis/body_normalizer.py ]` artifact 존재까지 검증. 신규 모듈 5종 모두 동일."
    - "**R8 wiring (2026-06-08 round-2)** — pipeline 이 'fallback_reference_not_found' 또는 'reference_source_pose_missing' 을 emit 할 때 `compare_body_profiles(..., extra_warnings=[...])` 인자로 주입. `dataclasses.replace` 우회 패턴 금지 — Plan 06-01 의 frozen-dataclass validation 통과."
    - "**R9 fix (2026-06-08 round-2)** — '7 deficits' 옛 표현 금지. 본 plan 의 acceptance / smoke test 모두 5 IPSF + Sunity pose_reliability_low; poor_transitions deferred (Phase 8 jerk/jitter 와 통합) 으로 기술."
  artifacts:
    - path: backend/shared/python/sunity_shared/analysis/technique.py
      provides: "C2 + R1 fix — TechniqueProfile dataclass 에 motion_id: str | None = None 필드 추가 (**위치: 맨 끝 — hold_window 필드 뒤**, R1 fix). default 값 있는 필드가 non-default 필드 앞에 오면 import 거부 회피. Phase 5 retroactive patch, backwards-compatible."
      contains: ["motion_id: str | None = None"]
    - path: backend/shared/python/sunity_shared/analysis/gemini_technique_recognizer.py
      provides: "C2 fix — _build_profile (line 310) 이 motion_id=motion (canonical name) populate. low_confidence / unregistered / cache 경로의 TechniqueProfile 생성자도 keyword argument `motion_id=` 로 호출 (R1 정합 — positional 인자 순서 무관)."
      contains: ["motion_id=motion", "motion_id="]
    - path: backend/functions/pipeline/app.py
      provides: "**R3 fix** 단일 helper `_extract_video_analysis_inputs(bucket, key, default_pole, *, keep_local_video=False) -> _VideoAnalysisInputs` (NamedTuple 또는 dataclass — angles, student_profile [non-null], pose_frames, local_video_path | None). 기존 `_angles_and_video_path_from_video` + 신설 `_angles_profile_and_frames_from_video` 폐기. Gemini ON 시 keep_local_video=True. 기존 `_angles_and_body_profile_from_video` (Phase 2 caller) 는 무수정 보존. + C2 fix `_match_reference_by_motion_id` helper (firestore_admin.get_reference_motion exact-match) + `_process` comparisonType 분기 wiring + `_dataclass_to_camel_case_dict` (C8 4-case 명세) + R2 wiring (bodyComparisonSourcePose fetch + `_extract_target_torso_px` helper)"
      contains: ["body_normalizer.compare_body_profiles", "_extract_video_analysis_inputs", "_match_reference_by_motion_id", "used_reference_fallback", "_dataclass_to_camel_case_dict", "fallback_reference_not_found", "reference_source_pose_missing", "_extract_target_torso_px"]
    - path: backend/shared/python/sunity_shared/firestore_admin.py
      provides: "complete_analysis(body_comparison_report=...) 시그너처 확장 + `_validate_flat_dict_no_nested_array` (W5). `list_reference_motions_by_name` 폐기 (C2). `get_reference_motion` 의 반환 dict 이 `bodyComparisonSourcePose` 필드도 포함 (R2 wiring — schema 무수정, 데이터만 갱신은 Plan 06-03 책임)."
      contains: ["def complete_analysis(", "def _validate_flat_dict_no_nested_array(", "body_comparison_report"]
    - path: app/src/lib/userAnalyses.ts
      provides: "normalize 에 bodyComparisonReport 통과 — TS 타입 추가만 (defensive validation), 별도 runtime 변환 없음. I2 positive assertion (grep >= 1)"
    - path: backend/tests/phase06/test_phase06_integration_smoke.py
      provides: "End-to-end smoke — _process → Firestore mock → bodyComparisonReport 검증 + must_haves.truths 박제 + C15 SAM artifact 존재 검증 + R2 / R3 / R4 신규 canary"
  key_links:
    - from: backend/functions/pipeline/app.py
      to: backend/shared/python/sunity_shared/analysis/body_normalizer.py
      via: "from sunity_shared.analysis import body_normalizer; body_normalizer.compare_body_profiles(..., source_keypoints=ref_source_pose.to_keypoints_array(), extra_warnings=[...])"
      pattern: "body_normalizer\\.compare_body_profiles"
    - from: backend/functions/pipeline/app.py
      to: backend/shared/python/sunity_shared/firestore_admin.py
      via: "firestore_admin.complete_analysis(..., body_comparison_report=...)"
      pattern: "complete_analysis\\(.*body_comparison_report"
    - from: backend/functions/pipeline/app.py
      to: backend/shared/python/sunity_shared/firestore_admin.py
      via: "firestore_admin.get_reference_motion(profile.motion_id) — C2 fix exact-match. 반환 dict 의 bodyComparisonSourcePose / bodyNormalizationProfile 둘 다 read (R2 wiring)"
      pattern: "get_reference_motion\\(.*motion_id"
    - from: backend/functions/pipeline/app.py
      to: backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/rtmw_engine.py
      via: "RTMWPoseEngine.estimate(frames, pole_axis) -> list[PoseFrame] — C3 fix 직접 반환. R3 fix — single helper 안에서 1회만 호출."
      pattern: "estimate\\("
---

<objective>
Phase 6 vertical slice **production wiring** — Plan 06-01 의 algorithm + contract 본체를 production code path 에 연결.

**2026-06-08 revision (cross-AI review C2/C3/C7/C8/C9/C14/C15 incorporated):**
- C2 (HIGH): Phase 5 retroactive patch. TechniqueProfile.motion_id 필드 추가 + Gemini recognizer populate + Plan 06-02 의 fallback path 가 exact-match `firestore_admin.get_reference_motion(profile.motion_id)` 사용.
- C3 (HIGH): RTMW estimate 직접 사용 — `_build_pose_frames` 신설 폐기.
- C7: 박제 filler 제거.
- C8: `_dataclass_to_camel_case_dict` 4-case 명세 + 4 unit test.
- C9: Pitfall 6 canary test 추가.
- C14: Phase 6 production path 의 `bad_angle` literal 부재.
- C15: SAM build smoke 에 artifact 존재 acceptance criteria 추가.

**2026-06-08 round-2 revision (Codex Direct Review R1/R3/R4/R8 + R2 wiring incorporated):**
- **R1 (HIGH)**: TechniqueProfile.motion_id 필드의 위치를 dataclass 의 **맨 끝 (hold_window 뒤)** 으로. 기존 plan 의 'name 바로 아래' 는 default 값 있는 필드가 non-default 필드 앞에 오는 Python dataclass 위반 — import 실패. Test 1 의 keyword argument 순서를 R1 정합으로 갱신.
- **R3 (HIGH)**: 기존 `_angles_and_video_path_from_video` + 신설 예정 `_angles_profile_and_frames_from_video` 를 단일 `_extract_video_analysis_inputs(bucket, key, default_pole, *, keep_local_video=False)` 로 통합. S3 download + frame extract + RTMW estimate 단 1회 실행. Gemini ON 이면 local video path 유지, OFF 면 cleanup. 양 helper 호출 시 발생할 double-RTMW 회귀 차단.
- **R4 (HIGH)**: 통합 helper 의 student_profile 반환 타입을 `BodyNormalizationProfile | None` 이 아닌 **`BodyNormalizationProfile` (non-null)** 으로. measure_body_profile 의 fallback 동작 (`_fallback_profile`) 정합. confidence 산식이 .confidence 안전 참조.
- **R2 wiring (HIGH)**: Plan 06-01 의 `BodyComparisonSourcePose` 영속 contract 가 production 에 흐름. mode1 + mode3 fallback 양 path 가 reference 의 `bodyComparisonSourcePose` 도 fetch. `compare_body_profiles` 의 `source_keypoints` 인자에 `ref_source_pose.to_keypoints_array()` 전달. source_pose 미존재 시 `extra_warnings=['reference_source_pose_missing']` (R8 정합) + scale_profile None.
- **R8 (HIGH)**: warning 주입은 `compare_body_profiles(..., extra_warnings=[...])` 로. `dataclasses.replace` 우회 금지 — Plan 06-01 frozen-dataclass validation 통과.
- **R9 (LOW)**: '7 deficits' 옛 표현 정정 — 5 IPSF + Sunity pose_reliability_low; poor_transitions deferred.

본 Plan 의 5 task (Task 0 = C2 + R1 retroactive Phase 5 patch, Task 1 = pipeline wiring + R2/R3/R4/R8 wiring, Task 2 = firestore_admin + validator, Task 3 = camelCase helper + frontend, Task 4 = SAM smoke + 통합).

Output:
- 갱신 `backend/shared/python/sunity_shared/analysis/technique.py` (C2 + R1 — motion_id field at END)
- 갱신 `backend/shared/python/sunity_shared/analysis/gemini_technique_recognizer.py` (C2 — populate motion_id via keyword)
- 갱신 `backend/functions/pipeline/app.py` (R3 unified helper `_extract_video_analysis_inputs` + R4 non-null student_profile + R2 source_pose fetch + R8 extra_warnings injection + exact-match fallback C2 + 분기 wiring + camelCase 변환 C8)
- 갱신 `backend/shared/python/sunity_shared/firestore_admin.py` (complete_analysis 확장 + validator W5)
- 갱신 `app/src/lib/userAnalyses.ts` (defensive validation)
- 신규 7 test 파일 (technique motion_id [R1], gemini populates motion_id, pipeline body comparison [R2/R3/R4 canary], firestore admin body comparison, pipeline firestore integration, dataclass_to_camel_case_dict 4-case + canary, integration smoke)
- SAM build artifact 검증 (C15)
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/phases/06-coaching/06-CONTEXT.md
@.planning/phases/06-coaching/06-RESEARCH.md
@.planning/phases/06-coaching/06-PATTERNS.md
@.planning/phases/06-coaching/06-REVIEWS.md
@.planning/phases/06-coaching/06-01-PLAN.md
@backend/shared/python/sunity_shared/analysis/technique.py
@backend/shared/python/sunity_shared/analysis/gemini_technique_recognizer.py
@backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/rtmw_engine.py
@backend/functions/pipeline/app.py
@backend/shared/python/sunity_shared/firestore_admin.py
@backend/shared/python/sunity_shared/analysis/body_normalization_measurer.py
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RunPod GPU 서버 → Lambda 위임 | `X-RunPod-Token` 헤더 (Phase 5 close-out). 본 plan 무수정 |
| Firestore Admin SDK → Firestore | service account key (Parameter Store) — 백엔드만 write |
| `_process` 내부 BodyNormalizationProfile / reference fetch | Firestore admin SDK + 기존 `users/{uid}/analyses/{id}` 격리. 본 plan 신규 field 추가만 |
| Gemini fallback path (C2 fix) | `profile.motion_id` (Gemini canonical) → exact-match `get_reference_motion`. 식별자 collision 위험 0 |
| Reference 측 source_keypoints (R2 wiring) | Plan 06-03 백필이 보장한 BodyComparisonSourcePose flat values. 본 plan 은 read only — write 책임 0. validator 가 schema 보장. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Tampering | Firestore nested-array bypass (findings 안에 list 들어갈 시 TypeError) | mitigate | `_validate_flat_dict_no_nested_array` 헬퍼 신설 (W5). `complete_analysis(body_comparison_report=...)` 진입 시 호출 → 위반 시 TypeError raise, fail_analysis 진입 |
| T-06-02-02 | Spoofing | 다른 uid 의 BodyNormalizationProfile read/write 시도 | mitigate | Firestore admin SDK 가 `users/{uid}/analyses/{id}` 격리 (기존). `complete_analysis` 의 uid 인자는 `auth._ensure_firebase()` 검증 후 전달 |
| T-06-02-03 | Tampering | Gemini fallback path 가 잘못된 reference 매칭 (C2 fix 이전: free-form `profile.name` 매칭 → collision 가능) | mitigate | **C2 fix** — `profile.motion_id` (Gemini canonical) → `get_reference_motion(motion_id)` exact-match. motion_id 없거나 reference 미발견 시 fallback 비활성 + `extra_warnings=['fallback_reference_not_found']` (R8 정합) |
| T-06-02-04 | Denial of Service | mode1 분석 시 reference doc 의 `bodyNormalizationProfile` 또는 **`bodyComparisonSourcePose` (R2)** 미박제 (백필 미실행) → silently OFF | mitigate | Plan 06-01 의 `compare_body_profiles` 가 reference_profile / source_keypoints 둘 다 검사 → 미존재 시 명시적 warning emit + confidence 하향. **C9 + R2 canary test 추가**. Plan 06-03 (백필) 가 후속. |
| T-06-02-05 | Tampering | `_dataclass_to_camel_case_dict` 변환 누락 (snake_case 가 Firestore 에 쓰여 contract drift) | mitigate | **C8 fix** — 4-case 명시 + 4 unit test |
| T-06-02-06 | Tampering | **R3 fix** — 통합 helper 가 RTMW estimate 1회만 호출하지 않으면 GPU 자원 낭비 + 두 호출의 keypoint 가 frame-level 비일치 (RTMW 의 stochastic / batch 영향) | mitigate | 단일 helper `_extract_video_analysis_inputs` — frames 1회 추출 → RTMW 1회 호출. test_pipeline_body_comparison 의 monkeypatch RTMW estimate call count == 1 검증. |
| T-06-02-SC | Tampering | npm/pip install 없음 | accept | 신규 외부 패키지 0 |

(security_enforcement=true. ASVS V5 Input Validation = T-06-02-01/03/05. V4 Access Control = T-06-02-02. R3 (T-06-02-06) = 자원 효율 + 일관성.)
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0 (C2 + R1 retro Phase 5 patch): TechniqueProfile.motion_id 필드 (**위치: 맨 끝, R1 fix**) + GeminiTechniqueRecognizer populate (keyword arg)</name>
  <files>
    backend/shared/python/sunity_shared/analysis/technique.py,
    backend/shared/python/sunity_shared/analysis/gemini_technique_recognizer.py,
    backend/tests/phase06/test_technique_profile_motion_id.py,
    backend/tests/phase06/test_gemini_recognizer_populates_motion_id.py
  </files>
  <read_first>
    backend/shared/python/sunity_shared/analysis/technique.py,
    backend/shared/python/sunity_shared/analysis/gemini_technique_recognizer.py,
    backend/shared/python/sunity_shared/analysis/gemini_motion_classifier.py,
    .planning/phases/06-coaching/06-REVIEWS.md
  </read_first>
  <behavior>
    - Test 1 (test_technique_profile_carries_motion_id, R1 fix — keyword arg): `TechniqueProfile(name="inversion", category="recognized", joint_expectations={}, motion_id="inversion")` 생성 → profile.motion_id == "inversion". motion_id 는 **마지막 keyword argument**. positional 7개 (name / category / joint_expectations / required_split_deg / requires_hold / is_symmetric / hold_window) 뒤 위치.
    - Test 2 (test_technique_profile_motion_id_default_none): TechniqueProfile(name="x", category="unknown", joint_expectations={}) 생성 (motion_id 미지정) → profile.motion_id is None (backwards-compatible default)
    - **Test 3 (R1 fix 신규 — test_technique_profile_dataclass_imports_without_default_order_error)**: `from sunity_shared.analysis.technique import TechniqueProfile` 자체가 ImportError / TypeError 없이 성공. R1 회피 — default 있는 필드가 default 없는 필드 앞에 위치하면 Python 이 import time 거부.
    - Test 4 (test_fallback_recognizer_motion_id_none): FallbackRecognizer().recognize(angles=np.zeros((10, 17))) → profile.motion_id is None
    - Test 5 (test_gemini_build_profile_populates_motion_id): GeminiTechniqueRecognizer._build_profile("inversion", moments=[]) → returned profile.motion_id == "inversion" (canonical name 그대로)
    - Test 6 (test_gemini_low_confidence_path_motion_id_none): low-confidence path → profile.motion_id is None (name="신뢰도 낮음")
    - Test 7 (test_gemini_unregistered_path_motion_id_none): unregistered path → profile.motion_id is None
    - Test 8 (test_gemini_cache_path_motion_id_from_cached_motion): `_profile_from_cache({"motion": "split"})` → motion_id == "split"
  </behavior>
  <action>
**C2 (HIGH severity reviews fix) + R1 (HIGH round-2 fix) — Phase 5 retroactive patch.**

(A) `backend/shared/python/sunity_shared/analysis/technique.py` 수정:
  - `TechniqueProfile` dataclass 의 **맨 끝 (기존 `hold_window: tuple[int, int] | None = None` 필드 바로 뒤)** 에 `motion_id: str | None = None` 추가. **R1 fix 핵심 — 기존 plan 의 'name 바로 아래' 위치는 default 있는 필드가 non-default 필드 앞에 와서 Python dataclass import time 거부 (`non-default argument follows default argument`)**. dataclass 의 모든 default-있는 필드 (required_split_deg, requires_hold, is_symmetric, hold_window) 뒤에 새 default-있는 필드 motion_id 를 둠 — Python 규칙 정합.
  - 한국어 docstring 갱신: `motion_id` 필드 위에 `# C2 fix (2026-06-08, Plan 06-02 reviews) + R1 fix (2026-06-08 round-2). Gemini canonical motion name 을 reference 컬렉션 lookup 의 stable key 로 박제. None 가능 (FallbackRecognizer / Gemini low_confidence / unregistered path 등). 위치 = dataclass 맨 끝 (hold_window 뒤) — R1 fix: default 있는 필드는 non-default 필드 뒤에 와야 dataclass import 거부 회피. Plan 06-02 의 mode3-first Gemini fallback path 가 본 필드를 사용해 firestore_admin.get_reference_motion(motion_id) exact-match 수행.`
  - FallbackRecognizer.recognize() 의 TechniqueProfile 생성자 호출 무수정 (motion_id default None 이 자동 적용).

(B) `backend/shared/python/sunity_shared/analysis/gemini_technique_recognizer.py` 수정 — 4 곳의 TechniqueProfile 생성자 호출 (line 174 / 197 / 310 / 322) 모두 **keyword argument** `motion_id=` 명시 (R1 정합 — positional 순서 무관, dataclass 의 새 위치 정합):
  - line 174 (low_confidence path): `TechniqueProfile(name="신뢰도 낮음", category="low_confidence", joint_expectations={}, required_split_deg=None, requires_hold=True, is_symmetric=False, motion_id=None)`
  - line 197 (unregistered path): `TechniqueProfile(name=f"미등록: {raw_motion_name}", category="unregistered", joint_expectations={}, required_split_deg=None, requires_hold=True, is_symmetric=False, motion_id=None)`
  - line 310 (_build_profile 정상 path): `TechniqueProfile(name=motion, category="recognized", joint_expectations=expectations, required_split_deg=None, requires_hold=True, is_symmetric=False, hold_window=hold_window_tuple, motion_id=motion)`. canonical motion 이 곧 motion_id.
  - line 322 (_profile_from_cache): `TechniqueProfile(name=cached.get("motion", "cached"), category="recognized", joint_expectations=cached.get("joint_expectations", {}), required_split_deg=None, requires_hold=True, is_symmetric=False, motion_id=cached.get("motion"))`. cache 의 motion 도 canonical name.
  - cache.store payload (line 215-219) 무수정 — payload["motion"] 이 곧 canonical, _profile_from_cache 가 motion_id 로 복원.

(C) 단위 테스트 2 파일:
  - `backend/tests/phase06/test_technique_profile_motion_id.py` — Test 1/2/3/4 (dataclass + import success + FallbackRecognizer). **Test 3 (R1 fix) 가 import 자체의 success 를 보장**.
  - `backend/tests/phase06/test_gemini_recognizer_populates_motion_id.py` — Test 5/6/7/8. Gemini extractor monkeypatch.

이모지 / 슬롭 코드 / fenced code block X. Korean comments.

[per C2 (2026-06-08 reviews HIGH), R1 (2026-06-08 round-2 HIGH), Phase 5 retroactive patch, backwards-compatible default None]
  </action>
  <verify>
    <automated>cd backend && pytest tests/phase06/test_technique_profile_motion_id.py tests/phase06/test_gemini_recognizer_populates_motion_id.py -v</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "motion_id:" backend/shared/python/sunity_shared/analysis/technique.py` >= 1 (field 정의)
    - **Source (R1 fix)**: TechniqueProfile dataclass 의 `motion_id` 필드 위치 = `hold_window` 필드 바로 뒤. `python -c "import sys; sys.path.insert(0, 'shared/python'); from sunity_shared.analysis.technique import TechniqueProfile; print(list(TechniqueProfile.__dataclass_fields__))"` 의 마지막 필드명 == 'motion_id'.
    - Source: `grep -c "motion_id=motion" backend/shared/python/sunity_shared/analysis/gemini_technique_recognizer.py` >= 1 (line 310 path)
    - Source: `grep -c "motion_id=" backend/shared/python/sunity_shared/analysis/gemini_technique_recognizer.py` >= 4 (4 path 모두 populate, keyword arg)
    - Behavior: `pytest tests/phase06/test_technique_profile_motion_id.py tests/phase06/test_gemini_recognizer_populates_motion_id.py -v` exits 0 + 8 test PASS
    - **Behavior (R1)**: Test 3 PASS — `import TechniqueProfile` 자체 ImportError 없음
    - Behavior: Phase 5 의 기존 GeminiTechniqueRecognizer 테스트가 회귀 없이 PASS (`pytest backend/tests/ -k gemini_technique -v` exits 0)
    - Behavior: FallbackRecognizer 기존 테스트 회귀 없음
  </acceptance_criteria>
  <done>C2 + R1 retroactive Phase 5 patch 완료 — TechniqueProfile.motion_id 필드 (맨 끝 위치) + Gemini 4 path keyword populate. Task 1 (pipeline wiring) 이 본 필드를 exact-match fallback 에 사용.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: pipeline `_extract_video_analysis_inputs` 통합 helper (R3 fix — 단일 RTMW 실행) + non-null student_profile 계약 (R4 fix) + `_match_reference_by_motion_id` (C2 fix exact-match) + `_extract_target_torso_px` (R2 wiring) + `_process` comparisonType 분기 wiring + source_keypoints fetch + extra_warnings injection (R8) + usedReferenceFallback (W1)</name>
  <files>
    backend/functions/pipeline/app.py,
    backend/tests/phase06/test_pipeline_body_comparison.py
  </files>
  <read_first>
    backend/functions/pipeline/app.py,
    backend/shared/python/sunity_shared/analysis/body_normalizer.py,
    backend/shared/python/sunity_shared/analysis/body_normalization.py,
    backend/shared/python/sunity_shared/analysis/body_normalization_measurer.py,
    backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/rtmw_engine.py,
    backend/shared/python/sunity_shared/analysis/pose_frame.py,
    backend/shared/python/sunity_shared/analysis/technique.py,
    backend/shared/python/sunity_shared/analysis/gemini_technique_recognizer.py,
    backend/shared/python/sunity_shared/firestore_admin.py,
    backend/tests/test_pipeline_mode1_smoke.py
  </read_first>
  <behavior>
    - Test 1 (test_process_mode1_with_full_ref_returns_mode1_report): mode1 + ref 의 bodyNormalizationProfile + bodyComparisonSourcePose 둘 다 박제 → body_comparison_report.comparison_type == 'mode1' + scale_profile != None + used_reference_fallback == False + warnings 가 reference 관련 warning 미포함
    - **Test 2 (C9 fix canary — test_process_mode1_missing_ref_body_profile_emits_canary_warning)**: mode1 + ref.bodyNormalizationProfile is None + ref.bodyComparisonSourcePose 박제 → comparison_type == 'mode1' + warnings ⊃ ["reference_profile_missing"] AND bodyNormalizationConfidence < CONFIDENCE_GATE (0.5). 두 assertion 모두 필수.
    - **Test 3 (R2 wiring 신규 canary — test_process_mode1_missing_ref_source_pose_emits_canary_warning)**: mode1 + ref.bodyNormalizationProfile 박제 + **ref.bodyComparisonSourcePose is None** → comparison_type == 'mode1' + warnings ⊃ ["reference_source_pose_missing"] AND scale_profile is None (정규화 OFF). caller 가 `compare_body_profiles(..., source_keypoints=None, extra_warnings=['reference_source_pose_missing'])` 호출 path.
    - **Test 4 (R2 + R8 wiring 신규 — test_process_mode1_fetches_both_profile_and_source_pose)**: mode1 path 가 reference doc 에서 두 필드 모두 fetch. mock `firestore_admin.get_reference_motion` 의 호출 결과 검사 — 반환 dict 의 `bodyNormalizationProfile` 와 `bodyComparisonSourcePose` 모두 read 되어 BodyNormalizationProfile + BodyComparisonSourcePose 인스턴스로 변환됨. 후속 compare_body_profiles 호출의 source_keypoints arg 는 source_pose.to_keypoints_array() 결과.
    - Test 5 (test_process_mode3_first_no_prev_no_gemini_match_returns_mode3_first): mode3 + prev=None + Gemini OFF (또는 profile.motion_id is None) → comparison_type == 'mode3_first' + scale_profile is None + used_reference_fallback == False
    - **Test 6 (C2 + W1 + R2 — test_process_mode3_first_with_gemini_fallback_exact_match_and_source_pose)**: mode3 + prev=None + Gemini matched (profile.motion_id == "inversion") + matched ref (get_reference_motion("inversion") 반환) + ref.bodyNormalizationProfile + ref.bodyComparisonSourcePose 모두 박제 + confidence >= 0.5 → comparison_type == 'mode3_first' + scale_profile != None + **used_reference_fallback == True**. mode3 fallback path 도 mode1 과 동일하게 두 필드 모두 fetch.
    - **Test 7 (C2 + R8 — test_process_mode3_first_gemini_motion_id_missing_uses_extra_warnings)**: mode3 + prev=None + Gemini matched (profile.motion_id == "obscure") + get_reference_motion 반환 None → caller 가 `compare_body_profiles(..., extra_warnings=['fallback_reference_not_found'])` 호출. 반환 BodyComparisonReport.warnings ⊃ "fallback_reference_not_found". **`dataclasses.replace` 사용 부재** — grep `dataclasses.replace` in pipeline/app.py == 0.
    - Test 8 (test_process_mode3_progress_with_prev_body_profile): mode3 + prev.bodyNormalizationProfile 박제 → comparison_type == 'mode3_progress' + previous_analysis_id != None + used_reference_fallback == False
    - **Test 9 (R3 fix — test_extract_video_analysis_inputs_runs_rtmw_exactly_once)**: monkeypatch RTMWPoseEngine.estimate call counter. `_extract_video_analysis_inputs(bucket, key, default_pole, keep_local_video=False)` 1회 호출 → estimate count == 1. Gemini ON path 에서 helper 가 keep_local_video=True 로 1회 호출 → estimate count == 1, local video path 반환. helper 가 두 번 호출되거나 별도 helper 가 RTMW 재호출하면 count > 1 — FAIL.
    - **Test 10 (R3 fix — test_legacy_helpers_not_in_pipeline_anymore)**: grep gate. `grep -c "_angles_and_video_path_from_video" backend/functions/pipeline/app.py` == 0. `grep -c "_angles_profile_and_frames_from_video" backend/functions/pipeline/app.py` == 0. 기존 `_angles_and_body_profile_from_video` 는 Phase 2 caller 정합 — 무수정 보존.
    - **Test 11 (R4 fix — test_extract_video_analysis_inputs_student_profile_never_null)**: monkeypatch `measure_body_profile` 가 raise 또는 fallback path → `_extract_video_analysis_inputs` 반환의 student_profile 은 BodyNormalizationProfile 인스턴스 (None 아님). profile.confidence == 0.0 + profile.warnings ⊃ "profile_measurement_failed" 또는 measure_body_profile 의 기존 fallback warning.
    - Test 12 (B2 — test_unstable_arm_swing_lowers_production_confidence): fixture_unstable_arm_swing → bodyNormalizationConfidence < 0.5
    - **Test 13 (R2 wiring — test_extract_target_torso_px_helper)**: `_extract_target_torso_px(pose_frames)` 반환 = 평균 mid_shoulder↔mid_hip 픽셀 거리. pose_frames 빈 또는 keypoint 미감지 시 None. 결과는 compare_body_profiles 의 target_torso_px arg 로 전달.
  </behavior>
  <action>
`backend/functions/pipeline/app.py::_process` 수정. 기존 시그너처 무변경.

(A) **R3 fix — 신규 통합 helper `_extract_video_analysis_inputs`** + 기존 `_angles_and_video_path_from_video` **폐기**. `_angles_and_body_profile_from_video` (Phase 2 caller) 는 무수정 보존.
  - NamedTuple 선언: `class _VideoAnalysisInputs(NamedTuple): angles: np.ndarray; student_profile: BodyNormalizationProfile; pose_frames: list[PoseFrame]; local_video_path: Path | None`
  - 시그너처: `def _extract_video_analysis_inputs(bucket: str, key: str, default_pole: PoleAxis, *, keep_local_video: bool = False) -> _VideoAnalysisInputs:`
  - Korean docstring: `"""R3 fix (2026-06-08 round-2, Plan 06-02 reviews). Phase 6 + Gemini path 의 video extraction 을 단일 helper 로 통합. S3 download + frame_extract + RTMW estimate 단 1회 실행 — 기존 `_angles_and_video_path_from_video` 와 신설 예정이었던 `_angles_profile_and_frames_from_video` 가 동시 호출되면 double RTMW 실행 (T-06-02-06). keep_local_video=True 시 local_video_path 반환 (caller 가 unlink 책임). keep_local_video=False 시 cleanup. student_profile 은 measure_body_profile 의 fallback 보장으로 non-null (R4 fix)."""`
  - 본체 단계:
    - `_ensure_adapters()` 호출
    - keep_local_video=True: `tempfile.NamedTemporaryFile(suffix=".mp4", delete=False)` (caller cleanup). keep_local_video=False: `tempfile.NamedTemporaryFile(suffix=".mp4", delete=True)` (with block 자동 unlink). 두 경우 모두 `_s3.download_file(bucket, key, tmp.name)`.
    - `frames = _FRAME_EXTRACTOR.extract(tmp.name)` — (T, H, W, 3) ndarray, 1회만.
    - `pose_frames = _RTMW_ENGINE.estimate(frames, default_pole)` — list[PoseFrame], 1회만.
    - `student_profile = measure_body_profile(pose_frames)` — `_fallback_profile` 로 None 반환 path 없음 (R4 정합).
    - angles 산출: `keypoints_4ch = to_coco17_array(pose_frames)` (R6 정합 — pose_frame.py:325 활용) — (T, 17, 4). `angles = compute_joint_angles(keypoints_4ch)` 후 `angles_filled = temporal_fill(angles, joint_uncertainty(keypoints_4ch))`.
    - local_video_path = `Path(tmp.name)` if keep_local_video else None.
    - return `_VideoAnalysisInputs(angles=angles_filled, student_profile=student_profile, pose_frames=pose_frames, local_video_path=local_video_path)`
  - default_pole 은 caller 가 `PoleAxis(axis_vector=(0.0, 1.0, 0.0), confidence_level="low", source="vertical_fallback", frame_index=None)` 으로 전달.

(A') **기존 `_angles_and_video_path_from_video` 함수 박제 제거** — 전체 def 블록 삭제. 호출처 (`_process` 의 Gemini ON 분기) 도 `_extract_video_analysis_inputs(bucket, key, default_pole, keep_local_video=True)` 로 갱신.

(B) **C2 fix — `_match_reference_by_motion_id(motion_id: str | None) -> dict | None`** 헬퍼 신설:
  - `if not motion_id: return None`
  - `return firestore_admin.get_reference_motion(motion_id)` (기존 함수 재사용). exact-match, None-safe.
  - Korean docstring: `"""C2 fix (2026-06-08, Plan 06-02 reviews). TechniqueProfile.motion_id (Gemini canonical) → reference 컬렉션 exact-match. R2 wiring 정합 — 반환 dict 의 bodyNormalizationProfile + bodyComparisonSourcePose 모두 read."""`

(B') **R2 wiring — `_extract_target_torso_px(pose_frames: list[PoseFrame]) -> float | None`** 헬퍼 신설:
  - Korean docstring: `"""R2 wiring (2026-06-08 round-2). target 영상의 평균 mid_shoulder↔mid_hip 픽셀 거리 산출 (절대 scale anchor). 각 frame 의 keypoints_3d_pole_aligned 에서 mid_shoulder = (l_shoulder + r_shoulder) / 2, mid_hip = (l_hip + r_hip) / 2. 두 좌표의 Euclidean 거리. NaN-safe — endpoint 미감지 frame skip + 모든 frame skip 시 None 반환 (compare_body_profiles 가 student_profile.torso_scale 로 fallback)."""`
  - 본체: 모든 frame 의 거리 list → numpy mean. 결과 finite 검증, finite 아니면 None.

(C) `_process` 의 ML 산출 단계 수정:
  - default_pole 구성 (위 (A) 참조)
  - **R3 fix 적용 — Gemini ON/OFF 분기 통합**:
    - `inputs = _extract_video_analysis_inputs(bucket, key, default_pole, keep_local_video=_gemini_enabled())`
    - `angles = inputs.angles; student_profile = inputs.student_profile; pose_frames = inputs.pose_frames; local_video_path = inputs.local_video_path`
    - Gemini ON path: `profile = _GEMINI_RECOGNIZER.recognize(angles, frames=str(local_video_path))` (기존 정합). try/finally 로 `if local_video_path: local_video_path.unlink(missing_ok=True)` cleanup.
    - Gemini OFF path: `profile = _FALLBACK_RECOGNIZER.recognize(angles)`. local_video_path is None — cleanup 불요.

(D) comparisonType 분기 + **R2 wiring** (W1 — 3 cases 만):
  - target_torso = `_extract_target_torso_px(pose_frames)`
  - `if mode == models.MODE_EXPERT` (mode1):
    - `ref = firestore_admin.get_reference_motion(meta.get("referenceMotionId"))`
    - `ref_profile = BodyNormalizationProfile(**ref["bodyNormalizationProfile"]) if ref and ref.get("bodyNormalizationProfile") else None`
    - `ref_source_pose = BodyComparisonSourcePose(**ref["bodyComparisonSourcePose"]) if ref and ref.get("bodyComparisonSourcePose") else None`  # **R2 wiring**
    - `source_keypoints = ref_source_pose.to_keypoints_array() if ref_source_pose else None`
    - `extra_warnings: list[str] = []`
    - `if ref and ref.get("bodyNormalizationProfile") and not ref_source_pose: extra_warnings.append("reference_source_pose_missing")`  # R2 canary
    - `body_comparison_report = body_normalizer.compare_body_profiles(pose_frames=pose_frames, student_profile=student_profile, reference_profile=ref_profile, comparison_type="mode1", source_keypoints=source_keypoints, angles=angles, technique_profile=profile, reference_motion_id=meta.get("referenceMotionId"), reference_athlete_name=(ref or {}).get("athleteName"), used_reference_fallback=False, target_torso_px=target_torso, extra_warnings=extra_warnings or None)`
  - `else` (mode3 — MODE_SELF):
    - `prev = firestore_admin.get_previous_analysis(uid, analysis_id, mode=models.MODE_SELF)`
    - prev 없거나 prev.bodyNormalizationProfile 없으면 mode3_first:
      - `used_fallback = False; ref_profile = None; source_keypoints = None; extra_warnings: list[str] = []`
      - `if profile.motion_id is not None and student_profile.confidence >= 0.5`:
        - `matched = _match_reference_by_motion_id(profile.motion_id)`
        - `if matched and matched.get("bodyNormalizationProfile")`:
          - `ref_profile = BodyNormalizationProfile(**matched["bodyNormalizationProfile"])`
          - `ref_source_pose = BodyComparisonSourcePose(**matched["bodyComparisonSourcePose"]) if matched.get("bodyComparisonSourcePose") else None`  # R2 wiring
          - `source_keypoints = ref_source_pose.to_keypoints_array() if ref_source_pose else None`
          - `if not ref_source_pose: extra_warnings.append("reference_source_pose_missing")`  # R2 canary
          - `used_fallback = True`
        - `elif matched is None`:
          - `extra_warnings.append("fallback_reference_not_found")`  # **R8 fix — extra_warnings 로 전달, dataclasses.replace 미사용**
      - `body_comparison_report = body_normalizer.compare_body_profiles(pose_frames=pose_frames, student_profile=student_profile, reference_profile=ref_profile, comparison_type="mode3_first", source_keypoints=source_keypoints, angles=angles, technique_profile=profile, used_reference_fallback=used_fallback, target_torso_px=target_torso, extra_warnings=extra_warnings or None)`
    - else (prev 박제):
      - `prev_profile = BodyNormalizationProfile(**prev["bodyNormalizationProfile"])`
      - `prev_source_pose_raw = prev.get("bodyComparisonSourcePose")`
      - `prev_source_pose = BodyComparisonSourcePose(**prev_source_pose_raw) if prev_source_pose_raw else None`
      - `prev_source_keypoints = prev_source_pose.to_keypoints_array() if prev_source_pose else None`
      - `prev_extra_warnings = ["reference_source_pose_missing"] if (prev_profile and not prev_source_pose) else None`
      - `body_comparison_report = body_normalizer.compare_body_profiles(pose_frames=pose_frames, student_profile=student_profile, reference_profile=prev_profile, comparison_type="mode3_progress", source_keypoints=prev_source_keypoints, angles=angles, technique_profile=profile, previous_analysis_id=prev.get("analysisId"), used_reference_fallback=False, target_torso_px=target_torso, extra_warnings=prev_extra_warnings)`

(E) `body_comparison_report` 저장은 Task 3 책임 (firestore_admin.complete_analysis 호출 wiring + camelCase 변환).

(F) 단위 테스트 `backend/tests/phase06/test_pipeline_body_comparison.py` — mock (firestore_admin / RTMWPoseEngine / compute_joint_angles / temporal_fill monkeypatch). `_process` 직접 호출. `student_profile` = `BodyNormalizationProfile(estimated_height_scale=0.875, ..., confidence=0.8, warnings=[])`. 13 test (Test 3/4/6/7/9/10/11/13 신규 R2/R3/R4/R8 fix 핵심).

이모지 / 슬롭 코드 / fenced code block X. Korean comments.

[per D-06-B1, D-06-B2, D-06-B3 (3 cases — W1), C2 (motion_id exact-match), C3 (RTMW estimate 직접 사용), C9 (canary), W1 used_reference_fallback, R1 (motion_id 위치), R2 wiring (source_keypoints fetch + reference_source_pose_missing canary), R3 (단일 helper), R4 (non-null student_profile), R8 (extra_warnings injection)]
  </action>
  <verify>
    <automated>cd backend && pytest tests/phase06/test_pipeline_body_comparison.py -v</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "body_normalizer.compare_body_profiles" backend/functions/pipeline/app.py` >= 3 (mode1 + mode3 first + mode3 progress)
    - Source: `grep -c "_match_reference_by_motion_id" backend/functions/pipeline/app.py` >= 2 (선언 + 호출) (C2 fix)
    - **Source (R3 fix)**: `grep -c "def _extract_video_analysis_inputs" backend/functions/pipeline/app.py` == 1
    - **Source (R3 fix — 폐기 검증)**: `grep -c "def _angles_and_video_path_from_video" backend/functions/pipeline/app.py` == 0
    - **Source (R3 fix — 폐기 검증)**: `grep -c "def _angles_profile_and_frames_from_video" backend/functions/pipeline/app.py` == 0
    - **Source (R3 fix — 호출처 갱신 검증)**: `grep -c "_extract_video_analysis_inputs(" backend/functions/pipeline/app.py` >= 2 (선언 + 1+ 호출처)
    - **Source (Phase 2 무수정)**: `grep -c "def _angles_and_body_profile_from_video" backend/functions/pipeline/app.py` == 1 (보존)
    - **Source (R2 wiring)**: `grep -c "bodyComparisonSourcePose\\|BodyComparisonSourcePose" backend/functions/pipeline/app.py` >= 3 (mode1 + mode3 fallback + mode3 progress fetch)
    - **Source (R2 wiring)**: `grep -c "_extract_target_torso_px" backend/functions/pipeline/app.py` >= 2 (선언 + 호출)
    - **Source (R2 wiring canary)**: `grep -c "reference_source_pose_missing" backend/functions/pipeline/app.py` >= 1
    - **Source (R8 fix)**: `grep -c "extra_warnings=" backend/functions/pipeline/app.py` >= 3 (mode1 + mode3 first + mode3 progress)
    - **Source (R8 fix — 우회 차단)**: `grep -v '^#' backend/functions/pipeline/app.py | grep -c "dataclasses.replace.*body_comparison_report\\|dataclasses.replace.*BodyComparisonReport"` == 0
    - Source: `grep -c "mode3_first_with_fallback" backend/functions/pipeline/app.py` == 0 (W1)
    - Source: `grep -c "used_reference_fallback" backend/functions/pipeline/app.py` >= 2 (W1)
    - **Source (C2)**: `grep -c "profile.motion_id" backend/functions/pipeline/app.py` >= 1
    - **Source (C2)**: `grep -c "list_reference_motions_by_name" backend/functions/pipeline/app.py` == 0
    - **Source (C2)**: `grep -c "fallback_reference_not_found" backend/functions/pipeline/app.py` >= 1
    - **Source (C14)**: `grep -v '^#' backend/functions/pipeline/app.py | grep -c '"bad_angle"'` == 0
    - Behavior: `pytest tests/phase06/test_pipeline_body_comparison.py -v` exits 0 + 13 test PASS
    - **Behavior: Test 2 (C9 canary) PASS** — warnings ⊃ "reference_profile_missing" AND bodyNormalizationConfidence < 0.5
    - **Behavior: Test 3 (R2 canary source pose missing) PASS** — warnings ⊃ "reference_source_pose_missing" AND scale_profile is None
    - **Behavior: Test 4 (R2 wiring fetch both) PASS** — bodyNormalizationProfile + bodyComparisonSourcePose 둘 다 read
    - **Behavior: Test 6 (C2 exact-match + R2) PASS** — used_reference_fallback==True via get_reference_motion(motion_id) + source_keypoints from to_keypoints_array
    - **Behavior: Test 7 (C2 + R8 extra_warnings) PASS** — fallback_reference_not_found via extra_warnings, no dataclasses.replace
    - **Behavior: Test 9 (R3 single RTMW call) PASS** — estimate call count == 1
    - **Behavior: Test 10 (R3 legacy helper deletion) PASS** — grep gates green
    - **Behavior: Test 11 (R4 non-null student_profile) PASS** — fallback profile returned, never None
    - Behavior: Test 12 (B2 production) PASS — bodyNormalizationConfidence < 0.5
    - **Behavior: Test 13 (R2 target_torso_px helper) PASS**
  </acceptance_criteria>
  <done>pipeline _process comparisonType 분기 + C2 exact-match fallback + R3 단일 helper (RTMW 1회) + R4 non-null student_profile + R2 wiring (source_keypoints fetch + reference_source_pose_missing canary + _extract_target_torso_px) + R8 extra_warnings injection (dataclasses.replace 폐기) + W1 + C9 canary + C14 grep gate. Task 2 (firestore_admin + validator) 진입 가능.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: firestore_admin.complete_analysis 시그너처 확장 + nested-array 차단 validator (W5)</name>
  <files>
    backend/shared/python/sunity_shared/firestore_admin.py,
    backend/tests/phase06/test_firestore_admin_body_comparison.py
  </files>
  <read_first>
    backend/shared/python/sunity_shared/firestore_admin.py,
    backend/shared/python/sunity_shared/analysis/body_normalizer.py,
    backend/shared/python/sunity_shared/models.py
  </read_first>
  <behavior>
    - Test 1 (test_complete_analysis_accepts_body_comparison_report): complete_analysis(..., body_comparison_report={"comparisonType": "mode1", "findings": [{"deficitCode": "knee_toe_alignment", ...}], "scaleProfile": {...}, "warnings": [], "usedReferenceFallback": false}) → Firestore set merge 호출 + payload['result']['bodyComparisonReport'] 박제
    - Test 2 (W5 — test_validate_flat_dict_allows_list_of_strings): {"comparisonType": "mode1", "warnings": ["a", "b", "c"], "findings": []} → PASS
    - Test 3 (W5 — test_validate_flat_dict_allows_list_of_dict_scalars): {"comparisonType": "mode1", "findings": [{"deficitCode": "x", "confidence": 0.9}], ...} → PASS
    - Test 4 (test_validate_flat_dict_rejects_nested_array_in_list): {"warnings": [["nested"], ["bad"]]} → TypeError raise
    - Test 5 (test_validate_flat_dict_rejects_nested_dict_with_list): {"findings": [{"deficitCode": "x", "extraList": [1,2,3]}]} → TypeError raise
    - Test 6 (test_complete_analysis_body_comparison_none_skips_field): body_comparison_report=None → payload 에 bodyComparisonReport key 부재
    - Test 7 (test_complete_analysis_body_normalization_profile_flat_dict): body_normalization_profile=BodyNormalizationProfile dict → payload['bodyNormalizationProfile'] flat dict (mode3 progress fetch path)
    - **Test 8 (C14 — test_validate_flat_dict_accepts_pose_reliability_low_finding)**: {"findings": [{"deficitCode": "pose_reliability_low", "confidence": 0.3, ...}]} → PASS
  </behavior>
  <action>
`backend/shared/python/sunity_shared/firestore_admin.py::complete_analysis` 확장.

(A) 시그너처 변경 (Korean prose — fenced code 금지):
  - `def complete_analysis(uid, analysis_id, result, *, angles=None, angles_joint_keys=None, angles_frames=None, body_comparison_report=None, body_normalization_profile=None) -> None`

(B) **W5 — `_validate_flat_dict_no_nested_array(payload: dict, *, path: str = "") -> None`** 헬퍼 신설. `store_gemini_cache:186-199` 의 기존 검증 패턴을 일반화. 명세:
  - dict 입력 → 각 (key, value) 순회
  - value 가 scalar (str, int, float, bool, None) → PASS
  - value 가 dict → 재귀 `_validate_flat_dict_no_nested_array(value, path=f"{path}.{key}")`
  - value 가 list →
    - list 원소가 scalar (str, int, float, bool) → PASS (warnings list[str] 정합)
    - list 원소가 dict → 각 원소에 대해 재귀 `_validate_flat_dict_no_nested_array(item, path=f"{path}.{key}[{i}]")`. 재귀 시 list-of-list 또는 dict-with-nested-list 만나면 TypeError.
    - list 원소가 list → `TypeError(f"{path}.{key} contains nested list (firestore-nested-array-flat)")` raise.
    - list 원소가 None / 빈 → PASS
  - 예외 메시지 형식: `f"{path}.{key} must be flat (firestore-nested-array-flat): got nested {type}"`
  - **Korean docstring**: `"""W5 (2026-06-08, Plan 06-02). Firestore nested-array 금지 ([[firestore-nested-array-flat]]) 보장. BodyComparisonReport.warnings (list[str]) + BodyComparisonReport.findings (list[dict-of-scalars]) 허용. list[list] / dict-with-nested-list reject. store_gemini_cache:186-199 의 기존 검증을 일반화. 위반 시 TypeError + path 정보. caller (pipeline) 가 catch → fail_analysis(server_error) 진입."""`

(C) `complete_analysis` 본체:
  - `if body_comparison_report is not None`: `_validate_flat_dict_no_nested_array(body_comparison_report, path="bodyComparisonReport")`. `payload["result"] = {**(result or {}), "bodyComparisonReport": body_comparison_report}` (result 내부 — analysis.ts AnalysisResult.bodyComparisonReport 정합).
  - `if body_normalization_profile is not None`: `_validate_flat_dict_no_nested_array(body_normalization_profile, path="bodyNormalizationProfile")`. `payload["bodyNormalizationProfile"] = body_normalization_profile` (top-level — mode3 progress fetch path).
  - `_doc(models.analysis_doc_path(uid, analysis_id)).set(payload, merge=True)`

(D) Korean 주석: `# Phase 6 (2026-06-08, Plan 06-02) — D-06-B3 + [[firestore-nested-array-flat]] + W5 validator + C14 (pose_reliability_low). bodyComparisonReport = result 내부, bodyNormalizationProfile = top-level (mode3 progress prev fetch path).`

(E) 단위 테스트 `backend/tests/phase06/test_firestore_admin_body_comparison.py` — Firestore mock (`monkeypatch.setattr(firestore_admin, "_doc", mock_doc)`). behavior block Test 1~8 1:1.

**C2 fix 정합 — `list_reference_motions_by_name` 신설 폐기**.

이모지 / fenced code X. Korean comments.

[per D-06-B3, [[firestore-nested-array-flat]], W5, C2 (list_reference_motions_by_name 폐기)]
  </action>
  <verify>
    <automated>cd backend && pytest tests/phase06/test_firestore_admin_body_comparison.py -v</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "body_comparison_report" backend/shared/python/sunity_shared/firestore_admin.py` >= 3 (시그너처 + body + validator)
    - Source: `grep -c "_validate_flat_dict_no_nested_array" backend/shared/python/sunity_shared/firestore_admin.py` >= 2 (선언 + 호출)
    - **Source (C2)**: `grep -c "def list_reference_motions_by_name" backend/shared/python/sunity_shared/firestore_admin.py` == 0
    - Behavior: `pytest tests/phase06/test_firestore_admin_body_comparison.py -v` exits 0 + 8 test PASS
    - Behavior: Test 2/3/4/5/8 PASS (W5 + C14)
  </acceptance_criteria>
  <done>firestore_admin.complete_analysis 확장 + W5 validator + C2 정합. Task 3 (camelCase 변환 + wiring) 진입 가능.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: pipeline `_dataclass_to_camel_case_dict` helper (C8 4-case 명세) + complete_analysis 호출 wiring + frontend userAnalyses.normalize defensive validation</name>
  <files>
    backend/functions/pipeline/app.py,
    app/src/lib/userAnalyses.ts,
    backend/tests/phase06/test_pipeline_firestore_integration.py,
    backend/tests/phase06/test_dataclass_to_camel_case_dict.py
  </files>
  <read_first>
    backend/functions/pipeline/app.py,
    backend/shared/python/sunity_shared/firestore_admin.py,
    backend/shared/python/sunity_shared/analysis/body_normalizer.py,
    app/src/lib/userAnalyses.ts,
    app/src/types/analysis.ts
  </read_first>
  <behavior>
    - Test 1 (test_process_calls_complete_analysis_with_body_comparison_report): mode1 분석 end-to-end (mock) → firestore_admin.complete_analysis 호출 + call_args 의 body_comparison_report arg 가 camelCase 필드 (comparisonType, bodyNormalizationConfidence, usedReferenceFallback) 보유
    - Test 2 (test_process_calls_complete_analysis_with_body_normalization_profile): student_profile (R4 non-null) → complete_analysis 가 body_normalization_profile arg 받음 + camelCase (estimatedHeightScale, armScale, ...)
    - Test 3 (test_process_handles_none_body_comparison_report_gracefully): body_comparison_report 가 None 일 수 있는 path → complete_analysis 호출 시 None 으로 전달 — fail_analysis 호출 X
    - Test 4 (test_process_validates_flat_dict_before_firestore_write): findings 에 nested list 발생 시 validator 가 TypeError → fail_analysis(server_error) 진입
    - Test 5 (test_userAnalyses_normalize_passes_through_body_comparison_report): app/src/lib/userAnalyses.ts::normalize 가 raw.result.bodyComparisonReport 통과 — TS 타입만 정의. tsc --noEmit 통과.
    - **Test 6 (C8 — test_dataclass_to_camel_case_dict_handles_none)**: `_dataclass_to_camel_case_dict(None)` → returns None
    - **Test 7 (C8 — test_dataclass_to_camel_case_dict_recurses_nested_dataclass)**: `_dataclass_to_camel_case_dict(BodyComparisonReport(comparison_type='mode1', scale_profile=ScaleProfile(estimated_height_scale=1.0, ...), ...))` → returns {"comparisonType": "mode1", "scaleProfile": {"estimatedHeightScale": 1.0, ...}, ...}
    - **Test 8 (C8 — test_dataclass_to_camel_case_dict_maps_list_of_dataclasses)**: `_dataclass_to_camel_case_dict(BodyComparisonReport(..., findings=[BodyComparisonFinding(deficit_code='x', ...)]))` → returns {..., "findings": [{"deficitCode": "x", ...}]}
    - **Test 9 (C8 — test_dataclass_to_camel_case_dict_serializes_enum_literal)**: Enum 도 `str(value)` 로 직렬화
  </behavior>
  <action>
Task 1 의 `_process` 에 firestore_admin.complete_analysis 호출 wiring 추가. Task 2 의 helper + validator 활용.

(A) **C8 fix — `_dataclass_to_camel_case_dict(obj)`** helper 신설. pipeline/app.py 모듈 상단 또는 sunity_shared utility 모듈. 4-case 명세:
  - Case 1 — None: `if obj is None: return None`
  - Case 2 — dataclass: `if dataclasses.is_dataclass(obj)`: `raw = dataclasses.asdict(obj)`. dict 의 각 (k, v) 에 대해 key 를 `_snake_to_camel(k)` 변환 + value 를 재귀.
  - Case 3 — list: `if isinstance(obj, list)`: `return [_dataclass_to_camel_case_dict(x) for x in obj]`
  - Case 4 — dict: `if isinstance(obj, dict)`: `return {_snake_to_camel(k): _dataclass_to_camel_case_dict(v) for k, v in obj.items()}`
  - Case 5 — Enum: `if isinstance(obj, Enum): return str(obj.value)`
  - Default — scalar: `return obj`
  - `_snake_to_camel(key)`: split on '_' → 첫 단어 lowercase + 나머지 capitalize → join.
  - Korean docstring: `"""C8 fix (2026-06-08, Plan 06-02 reviews). dataclass / list / dict / Enum / scalar 5 case 명시. BodyComparisonReport 의 중첩 ScaleProfile + list[BodyComparisonFinding] 까지 모두 camelCase 변환. None 입력 시 None 반환."""`

(B) `_process` 의 `complete_analysis` 호출 wiring:
  - `body_comparison_report_dict = _dataclass_to_camel_case_dict(body_comparison_report) if body_comparison_report else None`
  - `student_profile_dict = _dataclass_to_camel_case_dict(student_profile) if student_profile else None`  # R4 정합 — student_profile 은 항상 non-null 이므로 사실상 항상 변환
  - `firestore_admin.complete_analysis(uid, analysis_id, result, angles=..., body_comparison_report=body_comparison_report_dict, body_normalization_profile=student_profile_dict)`

(C) try/except — `_validate_flat_dict_no_nested_array` TypeError catch → `firestore_admin.fail_analysis(uid, analysis_id, "server_error", str(e))`. RESEARCH.md §Pitfall 5.

(D) `app/src/lib/userAnalyses.ts::normalize`:
  - Plan 06-01 Task 5 의 `AnalysisResult.bodyComparisonReport?: BodyComparisonReport` 타입 정의됨. normalize 함수는 `result: raw.result as AnalysisDoc['result']` passthrough.
  - Korean 주석: `// Phase 6 (2026-06-08, Plan 06-02): AnalysisResult.bodyComparisonReport 는 TS 타입 정합으로 자동 정상화. backend validator 가 nested-array 차단 보장 (W5).`
  - I2 positive assertion — `bodyComparisonReport` 문자열이 최소 1회 등장.

(E) 단위 테스트:
  - `backend/tests/phase06/test_pipeline_firestore_integration.py` — behavior Test 1~5
  - `backend/tests/phase06/test_dataclass_to_camel_case_dict.py` — Test 6/7/8/9

이모지 / 슬롭 코드 / fenced code X. Korean comments.

[per D-06-B3, [[firestore-nested-array-flat]], C8 (4-case 명세), Pitfall 5 nested array, R4 (student_profile non-null wiring)]
  </action>
  <verify>
    <automated>cd backend && pytest tests/phase06/test_pipeline_firestore_integration.py tests/phase06/test_dataclass_to_camel_case_dict.py -v && cd ../app && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "_dataclass_to_camel_case_dict\\|_to_camel_case" backend/functions/pipeline/app.py` >= 2
    - Source: `grep -c "body_comparison_report=" backend/functions/pipeline/app.py` >= 1
    - Source: `grep -c "body_normalization_profile=" backend/functions/pipeline/app.py` >= 1
    - Source: `grep -c "usedReferenceFallback\\|used_reference_fallback" backend/functions/pipeline/app.py` >= 1 (W1)
    - Source (I2): `grep -c "bodyComparisonReport" app/src/lib/userAnalyses.ts` >= 1
    - Behavior: `pytest tests/phase06/test_pipeline_firestore_integration.py tests/phase06/test_dataclass_to_camel_case_dict.py -v` exits 0 + 9 test PASS
    - Behavior: `cd app && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>pipeline → Firestore wiring + camelCase 변환 (C8 4-case 명세) + frontend defensive validation. Task 4 (SAM build artifact + 통합 smoke) 진입 가능.</done>
</task>

<task type="auto">
  <name>Task 4: SAM Lambda Layer 빌드 smoke + artifact 존재 검증 (C15 fix) + 전체 통합 smoke test + I2 positive assertion + R2/R3/R4 통합 smoke</name>
  <files>
    backend/tests/phase06/test_phase06_integration_smoke.py
  </files>
  <read_first>
    backend/template.yaml,
    backend/functions/pipeline/app.py,
    backend/shared/python/sunity_shared/analysis/body_normalizer.py,
    backend/shared/python/sunity_shared/firestore_admin.py,
    backend/shared/python/sunity_shared/analysis/technique.py,
    app/src/lib/userAnalyses.ts
  </read_first>
  <action>
Phase 6 전체 통합 smoke + SAM build artifact 검증.

(A) 통합 smoke test `backend/tests/phase06/test_phase06_integration_smoke.py`:
  - `test_full_pipeline_mode1_smoke` — fixture_160cm_pro_vs_140cm_student + mock Firestore (ref 가 bodyNormalizationProfile + bodyComparisonSourcePose 둘 다 박제) + mock S3 + RTMW estimator stub → `_process` 전체 호출 → Firestore complete_analysis 호출 → bodyComparisonReport.comparisonType == 'mode1' + scaleProfile != None + findings non-empty + bodyNormalizationConfidence > 0 + usedReferenceFallback == False
  - `test_full_pipeline_mode3_first_no_fallback_smoke` — Gemini OFF → comparisonType == 'mode3_first' + scaleProfile is None + usedReferenceFallback == False + warnings ⊃ "low_confidence_normalization_off"
  - **`test_full_pipeline_mode3_first_with_gemini_fallback_smoke` (C2 + W1 + R2)** — Gemini matched + matched ref 박제 (둘 다) → comparisonType == 'mode3_first' + scaleProfile != None + **usedReferenceFallback == True**.
  - **`test_full_pipeline_mode3_first_with_gemini_motion_id_no_ref_match_smoke` (C2 + R8)** — Gemini matched + get_reference_motion 반환 None → comparisonType == 'mode3_first' + usedReferenceFallback == False + warnings ⊃ "fallback_reference_not_found" (extra_warnings 경로)
  - **`test_full_pipeline_mode1_ref_source_pose_missing_smoke` (R2 canary)** — mode1 + ref.bodyNormalizationProfile 박제 + ref.bodyComparisonSourcePose is None → warnings ⊃ "reference_source_pose_missing" + scale_profile is None
  - **`test_full_pipeline_rtmw_estimate_call_count_is_one_smoke` (R3)** — Gemini ON path 전체 실행에서 RTMW estimate 호출 카운트 == 1 (R3 핵심 회귀 차단)
  - **`test_full_pipeline_student_profile_non_null_smoke` (R4)** — measure_body_profile fallback path 트리거 → student_profile 이 BodyComparisonReport 작성에 BodyNormalizationProfile 인스턴스로 흘러감 (None 분기 발생 안 함)
  - `test_full_pipeline_all_must_haves_emitted` — must_haves.truths 검증
  - **`test_pose_reliability_low_deficit_code_in_findings` (C14)** — 불안정 pose_frames → findings 중 적어도 하나가 `pose_reliability_low`. `bad_angle` 부재.

(B) **C15 fix — SAM Lambda Layer artifact 검증**: `sam build --use-container` + 5 artifact 존재 검증:
  - `[ -f .aws-sam/build/SunityLayer/python/sunity_shared/analysis/body_normalizer.py ]`
  - `[ -f .aws-sam/build/SunityLayer/python/sunity_shared/analysis/body_normalization.py ]`
  - `[ -f .aws-sam/build/SunityLayer/python/sunity_shared/analysis/body_normalization_measurer.py ]`
  - `[ -f .aws-sam/build/SunityLayer/python/sunity_shared/analysis/technique.py ]` (C2 + R1)
  - `[ -f .aws-sam/build/SunityLayer/python/sunity_shared/firestore_admin.py ]` (W5)

(C) 전체 phase06 unit suite + TS check — `cd backend && pytest tests/phase06 -v` + `cd app && npx tsc --noEmit`.

(D) **C14 grep gate (final)** — `grep -rn '"bad_angle"' backend/functions/pipeline/app.py backend/shared/python/sunity_shared/analysis/body_normalizer.py backend/shared/python/sunity_shared/firestore_admin.py app/src/types/analysis.ts app/src/lib/userAnalyses.ts | grep -v '^#'` exit 1 (matches == 0).

이모지 / 슬롭 코드 X.

[per CLAUDE.md Cross-cutting 3-way lockstep, [[sam-build-native-deps]], [[gsd-pod-work-push-first]], I2 positive assertion, C2/C3/C14/C15 + R2/R3/R4/R8 reviews fix]
  </action>
  <verify>
    <automated>cd backend && pytest tests/phase06 -v && cd ../app && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `pytest backend/tests/phase06 -v` exits 0 + Phase 6 전체 unit suite PASS
    - Behavior: `cd app && npx tsc --noEmit` exits 0
    - Behavior: test_full_pipeline_mode1_smoke PASS
    - Behavior: test_full_pipeline_mode3_first_with_gemini_fallback_smoke PASS (C2 + W1 + R2)
    - Behavior: test_full_pipeline_mode3_first_with_gemini_motion_id_no_ref_match_smoke PASS (C2 + R8 extra_warnings)
    - **Behavior: test_full_pipeline_mode1_ref_source_pose_missing_smoke PASS (R2 canary)**
    - **Behavior: test_full_pipeline_rtmw_estimate_call_count_is_one_smoke PASS (R3)**
    - **Behavior: test_full_pipeline_student_profile_non_null_smoke PASS (R4)**
    - Behavior: test_pose_reliability_low_deficit_code_in_findings PASS (C14)
    - **(C15 SAM artifact)** — `cd backend && sam build --use-container && [ -f .aws-sam/build/SunityLayer/python/sunity_shared/analysis/body_normalizer.py ] && [ -f .aws-sam/build/SunityLayer/python/sunity_shared/analysis/body_normalization.py ] && [ -f .aws-sam/build/SunityLayer/python/sunity_shared/analysis/technique.py ] && [ -f .aws-sam/build/SunityLayer/python/sunity_shared/firestore_admin.py ]` exit 0
    - I2 — `grep -c "bodyComparisonReport" app/src/lib/userAnalyses.ts` >= 1
    - **(C14 final grep gate)** — `grep -rn '"bad_angle"' backend/functions/pipeline/app.py backend/shared/python/sunity_shared/analysis/body_normalizer.py backend/shared/python/sunity_shared/firestore_admin.py app/src/types/analysis.ts app/src/lib/userAnalyses.ts | grep -v '^#' | wc -l` == 0
  </acceptance_criteria>
  <done>Phase 6 vertical slice production wiring 완료. C15 SAM artifact 검증 + C14 grep gate + I2 positive assertion + R2/R3/R4/R8 통합 smoke 모두 GREEN. Plan 06-03 (정은지 reference 백필 — bodyNormalizationProfile + bodyComparisonSourcePose 둘 다) 진입 가능.</done>
</task>

</tasks>

<verification>
## Plan 06-02 verification (production wiring)

- [ ] `pytest backend/tests/phase06/test_technique_profile_motion_id.py tests/phase06/test_gemini_recognizer_populates_motion_id.py tests/phase06/test_pipeline_body_comparison.py tests/phase06/test_firestore_admin_body_comparison.py tests/phase06/test_pipeline_firestore_integration.py tests/phase06/test_dataclass_to_camel_case_dict.py tests/phase06/test_phase06_integration_smoke.py -v` 전부 GREEN
- [ ] `cd app && npx tsc --noEmit` exits 0
- [ ] `grep -c "body_normalizer" backend/functions/pipeline/app.py` >= 3
- [ ] `grep -c "body_comparison_report" backend/shared/python/sunity_shared/firestore_admin.py` >= 3
- [ ] **(C2 + R1)** `grep -c "motion_id" backend/shared/python/sunity_shared/analysis/technique.py` >= 1 + 위치가 dataclass 끝
- [ ] **(C2)** `grep -c "motion_id=" backend/shared/python/sunity_shared/analysis/gemini_technique_recognizer.py` >= 4
- [ ] **(C2)** `grep -c "_match_reference_by_motion_id" backend/functions/pipeline/app.py` >= 2
- [ ] **(C2)** `grep -c "list_reference_motions_by_name" backend/shared/python/sunity_shared/firestore_admin.py backend/functions/pipeline/app.py` == 0
- [ ] **(C2)** `grep -c "fallback_reference_not_found" backend/functions/pipeline/app.py` >= 1
- [ ] **(R3)** `grep -c "def _extract_video_analysis_inputs" backend/functions/pipeline/app.py` == 1
- [ ] **(R3)** `grep -c "def _angles_and_video_path_from_video" backend/functions/pipeline/app.py` == 0 (폐기)
- [ ] **(R3)** `grep -c "def _angles_profile_and_frames_from_video" backend/functions/pipeline/app.py` == 0 (신설 안 함)
- [ ] **(R3)** `grep -c "def _angles_and_body_profile_from_video" backend/functions/pipeline/app.py` == 1 (Phase 2 보존)
- [ ] **(R2)** `grep -c "bodyComparisonSourcePose\\|BodyComparisonSourcePose" backend/functions/pipeline/app.py` >= 3
- [ ] **(R2)** `grep -c "_extract_target_torso_px" backend/functions/pipeline/app.py` >= 2
- [ ] **(R2)** `grep -c "reference_source_pose_missing" backend/functions/pipeline/app.py` >= 1
- [ ] **(R8)** `grep -c "extra_warnings=" backend/functions/pipeline/app.py` >= 3
- [ ] **(R8 우회 차단)** `grep -v '^#' backend/functions/pipeline/app.py | grep -c "dataclasses.replace.*body_comparison_report\\|dataclasses.replace.*BodyComparisonReport"` == 0
- [ ] **(C8)** `grep -c "_dataclass_to_camel_case_dict\\|_snake_to_camel" backend/functions/pipeline/app.py` >= 2
- [ ] **(C9)** `pytest backend/tests/phase06/test_pipeline_body_comparison.py::test_process_mode1_missing_ref_body_profile_emits_canary_warning -v` PASS
- [ ] **(R2 canary)** `pytest backend/tests/phase06/test_pipeline_body_comparison.py::test_process_mode1_missing_ref_source_pose_emits_canary_warning -v` PASS
- [ ] **(C14)** `grep -rn '"bad_angle"' backend/functions/pipeline/app.py backend/shared/python/sunity_shared/analysis/body_normalizer.py backend/shared/python/sunity_shared/firestore_admin.py app/src/types/analysis.ts app/src/lib/userAnalyses.ts | grep -v '^#' | wc -l` == 0
- [ ] **(C15)** `sam build --use-container` exit 0 + 5 artifact 존재
- [ ] `grep -c "mode3_first_with_fallback" backend/functions/pipeline/app.py` == 0 (W1)
- [ ] `grep -c "used_reference_fallback\\|usedReferenceFallback" backend/functions/pipeline/app.py` >= 1 (W1)
- [ ] `grep -c "bodyComparisonReport" app/src/lib/userAnalyses.ts` >= 1 (I2)
</verification>

<success_criteria>
Plan 06-02 Success Criteria — production wiring (reviews-revised + round-2):

1. ✅ pipeline `_process` 의 comparisonType 분기 wiring + Gemini fallback (Task 1)
2. ✅ **C2 fix (HIGH reviews) + R1 (HIGH round-2)** — Phase 5 retroactive patch: TechniqueProfile.motion_id 필드 (dataclass 맨 끝, R1 fix) + Gemini recognizer 4 path populate (keyword arg) + pipeline 의 `_match_reference_by_motion_id` exact-match
3. ✅ **C3 fix (HIGH reviews)** — RTMW estimate 직접 사용
4. ✅ **R3 fix (HIGH round-2)** — 단일 `_extract_video_analysis_inputs` helper. 기존 `_angles_and_video_path_from_video` 폐기. RTMW 1회 실행 보장 (T-06-02-06)
5. ✅ **R4 fix (HIGH round-2)** — student_profile 반환 non-null (BodyNormalizationProfile). measure_body_profile 의 _fallback_profile 정합
6. ✅ Firestore flat 저장 + W5 nested-array 차단 validator (Task 2 + Task 3)
7. ✅ **W5** — list[str] (warnings) + list[dict-of-scalars] (findings) 허용
8. ✅ **W1** wiring: comparisonType 3 cases. usedReferenceFallback=True (Task 1 + Task 3)
9. ✅ **C8 fix (reviews)** — `_dataclass_to_camel_case_dict` 4-case 명세 + 4 unit test
10. ✅ **C9 fix (reviews)** — Pitfall 6 canary test (mode1 missing ref_profile)
11. ✅ **R2 wiring (HIGH round-2)** — mode1 + mode3 fallback 양 path 가 reference 의 bodyComparisonSourcePose 도 fetch. source_keypoints=ref_source_pose.to_keypoints_array() 전달. 미존재 시 extra_warnings=['reference_source_pose_missing'] canary
12. ✅ **R8 fix (HIGH round-2)** — `extra_warnings` injection. dataclasses.replace 우회 폐기
13. ✅ **R9 fix (LOW round-2)** — '7 deficits' 옛 표현 제거. 본 plan 의 acceptance / smoke 모두 5 IPSF + Sunity pose_reliability_low
14. ✅ **C14 fix (reviews)** — Phase 6 production path 의 `bad_angle` literal 부재. grep gate
15. ✅ frontend defensive validation (Task 3) + I2 positive assertion
16. ✅ **C15 fix (reviews)** — SAM Lambda Layer artifact 5종 존재 검증 + build smoke
17. ✅ 통합 smoke test — must_haves.truths + R2/R3/R4/R8 신규 canary 박제

**Plan 06-03 책임 (본 Plan 에서 X)**: 정은지 reference 5개 영상의 bodyNormalizationProfile **+ bodyComparisonSourcePose (R2 신규)** 백필. C5 dry-run flag + C6 belle Pod sweep (deferred) + C7 filler 제거 + C12 revert script + **R7 dry-run ordering**.
</success_criteria>

<output>
Create `.planning/phases/06-coaching/06-02-SUMMARY.md` when done — Tasks 0~4 완료 + C2/C3/C7/C8/C9/C14/C15 + R1/R2/R3/R4/R8/R9 reviews fix 명시 + Pod 동기화 필요 표기 ([[gsd-pod-work-push-first]] 정합).
</output>
