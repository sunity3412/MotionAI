---
phase: 06-coaching
plan: 03
type: execute
wave: 3
depends_on: [06-02]
files_modified:
  - backend/shared/python/sunity_shared/firestore_admin.py
  - app/scripts/seed-reference-body-profile.mjs
  - app/scripts/revert-reference-body-profile.mjs
  - app/package.json
  - backend/scripts/extract_reference_body_profiles.py
  - backend/tests/phase06/test_firestore_admin_update_reference_body_data.py
  - backend/tests/phase06/test_backfill_scripts_dry_run.py
autonomous: false
requirements: [PERS-01]
tags: [reference-backfill, body-profile, body-comparison-source-pose, operational, phase-06, dry-run, revert]

must_haves:
  truths:
    - "현재 등록된 정은지 reference 5개 (ref-climb / ref-foxtop / ref-foxtop-split / ref-invert / ref-sideway-spin) 의 Firestore reference 컬렉션에 `bodyNormalizationProfile` 필드 + **신규 `bodyComparisonSourcePose` 필드 (R2 fix 2026-06-08 round-2)** 가 박제되어 mode1 분석 시 silently OFF 가 발생하지 않는다. 두 필드가 모두 박제되어야 Plan 06-02 의 compare_body_profiles 의 source_keypoints + reference_profile 둘 다 활성. (RESEARCH.md Pitfall 6 + R2 canary)"
    - "**R2 fix (2026-06-08 round-2)** — Plan 06-01 신설 BodyComparisonSourcePose 의 백필 책임. 대표 hold frame (hold_window 중앙 또는 confidence 가장 높은 frame) 의 17 keypoint × 4채널 (x, y, z, confidence) 을 flat values 형태로 저장. Firestore nested-array 회피 ([[firestore-nested-array-flat]]) — values 는 단일 flat float array (길이 = 4 × 17 = 68). torsoPx 도 같이 저장 (해당 frame 의 mid_shoulder↔mid_hip 픽셀 거리)."
    - "백필 스크립트는 idempotent — 이미 박제된 reference 는 skip, --force 시에만 덮어씀. 두 필드 (bodyNormalizationProfile + bodyComparisonSourcePose) 가 함께 갱신."
    - "**R2 fix (2026-06-08 round-2)** — 단일 helper `firestore_admin.update_reference_body_data(motion_id, body_profile, source_pose)` 가 백필 + Phase 14 정은지 reference 등록 두 caller 의 단일 진입점. 두 필드를 묶어 atomic merge. 기존 plan 의 `update_reference_body_profile` (단일 필드) 폐기."
    - "C5 fix (2026-06-08 reviews) — Python extract + Node seed 양 스크립트가 `--dry-run` 플래그 지원. dry-run 은 Firestore write 없이 proposed JSON 만 stdout 출력. Task 3.5 (자동) 가 dry-run 통합 검증."
    - "**R7 fix (2026-06-08 round-2)** — `seed-reference-body-profile.mjs` 의 실행 순서가 명시: **(Step 1)** argparse + JSON fixture load + schema validate (Firebase init 박제 X), **(Step 2)** if --dry-run: stdout proposed payload + exit 0 (Firebase init / ADC 호출 호환 — ADC 미설정 환경에서 dry-run 가능), **(Step 3)** real-run: initializeApp() + getFirestore() + batch.commit(). Test 3.5 의 subprocess 검증이 ADC 미설정 환경에서 dry-run exit 0 + Firebase 호출 카운트 == 0 검증."
    - "C12 fix (2026-06-08 reviews) — `app/scripts/revert-reference-body-profile.mjs` 는 `--motion-ids <ids>` argv 로 reference 컬렉션의 bodyNormalizationProfile + bodyComparisonSourcePose **두 필드 모두** FieldValue.delete() 처리 (idempotent). dry-run 모드 지원."
    - "C6 fix (2026-06-08 reviews) — Task 6 (deferred) 가 belle Pod sweep 사양 박제."
    - "**R9 fix (2026-06-08 round-2)** — '7 deficits' 옛 표현 사용 금지. 본 plan 의 검증 카피 모두 5 IPSF + Sunity pose_reliability_low; poor_transitions deferred (Phase 8 jerk/jitter 와 통합)."
  artifacts:
    - path: backend/shared/python/sunity_shared/firestore_admin.py
      provides: "**R2 fix** 단일 helper `update_reference_body_data(motion_id, body_profile, source_pose)` 신설 — body_profile + source_pose 두 필드를 atomic merge. nested-array 차단 validator (Plan 06-02) 재사용. 기존 plan 의 update_reference_body_profile (단일 필드) 폐기 — 본 단일 진입점이 단순화."
      contains: ["def update_reference_body_data("]
    - path: backend/scripts/extract_reference_body_profiles.py
      provides: "정은지 reference 5개 영상의 BodyNormalizationProfile + **BodyComparisonSourcePose (R2 fix)** 측정 스크립트 (Pod GPU 직접 실행). RTMW + measure_body_profile + 대표 hold frame 추출. 대표 frame 선정 = (1) Gemini hold_window 중앙 frame (가능 시), (2) fallback = pose_frames 중 평균 confidence 가장 높은 frame. C5 fix — --dry-run 플래그. W3 — 직접 실행 (`python backend/scripts/...`, `-m` invocation X)"
    - path: app/scripts/seed-reference-body-profile.mjs
      provides: "JSON fixture → Firestore reference/{motionId} **두 필드 (bodyNormalizationProfile + bodyComparisonSourcePose)** atomic merge. Firebase Admin SDK + ADC. C5 + R7 fix — --dry-run 플래그 (Firebase init 박제 X for dry-run path)."
    - path: app/scripts/revert-reference-body-profile.mjs
      provides: "C12 fix + R2 — 두 필드 (bodyNormalizationProfile + bodyComparisonSourcePose) 모두 FieldValue.delete() 처리. --motion-ids 필수 + dry-run 지원."
    - path: backend/tests/phase06/test_backfill_scripts_dry_run.py
      provides: "C5 + R7 fix — Python + Node 양 스크립트 dry-run 통합 검증. R7 검증: subprocess + ADC 미설정 환경에서 Node 스크립트 dry-run 모드 → exit 0 + Firebase initializeApp / getFirestore 호출 카운트 == 0."
  key_links:
    - from: app/scripts/seed-reference-body-profile.mjs
      to: backend/shared/python/sunity_shared/firestore_admin.py
      via: "동일 firestore reference 컬렉션 + **두 필드 (bodyNormalizationProfile + bodyComparisonSourcePose) atomic merge** (Plan 06-01 contract + Plan 06-02 validator 정합)"
      pattern: "bodyNormalizationProfile.*bodyComparisonSourcePose|bodyComparisonSourcePose.*bodyNormalizationProfile"
    - from: backend/scripts/extract_reference_body_profiles.py
      to: backend/shared/python/sunity_shared/analysis/body_normalization_measurer.py
      via: "measure_body_profile(pose_frames) → BodyNormalizationProfile + 대표 hold frame → BodyComparisonSourcePose 산출 (R2 fix)"
      pattern: "measure_body_profile"
    - from: backend/scripts/extract_reference_body_profiles.py
      to: backend/shared/python/sunity_shared/analysis/body_normalizer.py
      via: "BodyComparisonSourcePose dataclass 인스턴스 생성 (R2 fix — flat values 산출)"
      pattern: "BodyComparisonSourcePose"
    - from: app/scripts/revert-reference-body-profile.mjs
      to: Firestore reference/{motionId}
      via: "FieldValue.delete() 로 두 필드 모두 제거 (R2 — bodyNormalizationProfile + bodyComparisonSourcePose)"
      pattern: "FieldValue.delete"
---

<objective>
정은지 reference 5개 영상의 `bodyNormalizationProfile` + **`bodyComparisonSourcePose` (R2 신규)** 백필. Plan 06-01 의 contract + Plan 06-02 의 pipeline wiring 완료 후 진입.

미실행 시:
- mode1 분석에서 reference_profile is None → 정규화 silently OFF + warnings ⊃ "reference_profile_missing" (RESEARCH.md Pitfall 6)
- **R2 신규**: source_pose is None → source_keypoints None → scale_profile None + warnings ⊃ "reference_source_pose_missing" (Plan 06-01 R2 canary + Plan 06-02 wiring canary)

두 필드 모두 박제되어야 Phase 6 의 정규화가 production 에서 실제 작동.

**2026-06-08 revision (cross-AI review C5/C6/C7/C12 incorporated):**
- C5 (MEDIUM): Task blast radius 축소 — 양 스크립트 `--dry-run` 추가. Task 4.5 (자동) 가 dry-run 통합 검증. Task 5 (수동 checkpoint) 는 real-execute + verify 만.
- C6 (MEDIUM): Task 6 (deferred but tracked) — belle Pod sweep 사양 박제.
- C7: Task 5 의 박제 filler 제거.
- C12 (LOW): `app/scripts/revert-reference-body-profile.mjs` 신설.

**2026-06-08 round-2 revision (Codex Direct Review R2/R7/R9 incorporated):**
- **R2 (HIGH)**: BodyComparisonSourcePose 백필 contract. Plan 06-01 의 신규 dataclass 가 Firestore reference 컬렉션의 새 필드 `bodyComparisonSourcePose` 로 영속. 대표 hold frame 의 17 keypoint × 4채널 (x, y, z, confidence) flat values + torsoPx + frameIndex + confidence + measuredAt. 단일 helper `update_reference_body_data` 가 body_profile + source_pose 두 필드를 atomic merge. 기존 plan 의 단일 필드 helper `update_reference_body_profile` 폐기.
- **R7 (MEDIUM)**: `seed-reference-body-profile.mjs` 의 실행 순서 명시. parse/load/validate 가 Firebase init 보다 먼저. dry-run mode 는 Firebase init 미수행 (ADC 미설정 환경 호환). Test 의 subprocess 검증이 ADC 환경 변수 비워두고 dry-run exit 0 + Firebase 호출 0 검증.
- **R9 (LOW)**: '7 deficits' 옛 표현 정정 — 5 IPSF + Sunity pose_reliability_low; poor_transitions deferred.

Task 구조 (총 7 task):
- Task 1 (자동, R2 fix): firestore_admin.update_reference_body_data helper (두 필드 atomic merge) + nested-array 차단
- Task 2 (자동, R2 fix): extract_reference_body_profiles.py — BodyNormalizationProfile + **BodyComparisonSourcePose 함께** 산출 + --dry-run (C5)
- Task 3 (자동, R7 fix): seed-reference-body-profile.mjs — **두 필드 atomic merge + 명시적 ordering** (parse → validate → if dry-run: stdout + exit, else: Firebase init + commit) + --dry-run (C5)
- Task 4 (자동, C5 + R7): dry-run 통합 검증 unit test (ADC 미설정 환경에서 dry-run exit 0 + Firebase 호출 0)
- Task 4.5 (자동, C12): revert-reference-body-profile.mjs — **두 필드 모두** FieldValue.delete
- Task 5 (수동 checkpoint, C7): Pod GPU 실행 + 로컬 seed + Firestore Console verify (두 필드 모두 verify, 구체 expected output)
- Task 6 (deferred, C6): belle Pod sweep 사양 박제

운영 작업 — Path A (PREFERRED):
Pod GPU 에서 `extract_reference_body_profiles.py` 실행 → 5 영상 다운로드 → RTMW estimate + measure_body_profile + 대표 hold frame 추출 → `reference-body-data.json` (두 필드 데이터) → 로컬 다운로드 → `npm run seed:body-profile` 으로 Firestore reference 컬렉션 atomic merge (두 필드 동시)

Purpose:
- Plan 06-01 의 mode1 정규화 silently OFF 차단 (두 canary: reference_profile_missing + reference_source_pose_missing)
- Phase 14 정은지 reference 본격 등록의 단일 helper 재사용 (R2)
- 운영 idempotent — 동일 reference 2회 실행 시 동일 결과
- **C5 — blast radius 축소 (dry-run gate)**
- **C12 — rollback 옵션 (운영 안전, 두 필드)**
- **C6 — 정량적 검증 사양 박제**
- **R2 — production 정규화 path 가 reference 측 source_keypoints 까지 영속**
- **R7 — dry-run path 가 ADC 의존성 없이 안전 검증**

Output:
- 신규 `firestore_admin.update_reference_body_data(motion_id, body_profile, source_pose)` helper (R2)
- 신규 `backend/scripts/extract_reference_body_profiles.py` (Pod GPU, --dry-run, 두 필드 산출)
- 신규 `app/scripts/seed-reference-body-profile.mjs` (Firebase Admin SDK ADC, --dry-run, 두 필드 merge, R7 ordering)
- 신규 `app/scripts/revert-reference-body-profile.mjs` (C12 + R2 — 두 필드 delete)
- 갱신 `app/package.json` — `seed:body-profile` + `revert:body-profile` npm script
- 신규 `backend/tests/phase06/test_backfill_scripts_dry_run.py` (C5 + R7)
- Firestore reference 컬렉션 5 doc 갱신 (Task 5 — 두 필드 모두, 실 실행은 checkpoint)
- Task 6 — belle Pod sweep 사양 (실행은 별도)
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/06-coaching/06-CONTEXT.md
@.planning/phases/06-coaching/06-RESEARCH.md
@.planning/phases/06-coaching/06-PATTERNS.md
@.planning/phases/06-coaching/06-REVIEWS.md
@.planning/phases/06-coaching/06-01-PLAN.md
@.planning/phases/06-coaching/06-02-PLAN.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local dev → Firebase Admin SDK → Firestore | Application Default Credentials (gcloud auth). sunity3412@gmail.com ([[firebase-project-account]]) |
| Pod GPU → S3 → 영상 다운로드 | 기존 IAM. 본 plan 신규 권한 0 |
| revert 스크립트 → Firestore | C12 rollback path. ADC 정합 + idempotent + dry-run 안전망. R2 정합 — 두 필드 모두 처리. |
| **R7 dry-run path → 없음** | dry-run 분기는 Firebase init 자체를 skip — ADC 부재 환경에서도 안전. validation 만 수행. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Tampering | 백필 스크립트 의 reference 컬렉션 우발적 덮어쓰기 | mitigate | `--force` 명시 시에만 덮어쓰기 (default idempotent skip). C5 fix — `--dry-run` 1차 검증. R2 정합 — 두 필드 atomic, 부분 갱신 회피. |
| T-06-03-02 | Tampering | nested-array (BodyComparisonSourcePose.values 가 list[list] 인 경우) | mitigate | `update_reference_body_data` 가 `_validate_flat_dict_no_nested_array` (Plan 06-02 Task 2 validator) 호출. BodyComparisonSourcePose 의 values 는 정의상 flat float list — list[list] 발생 시 TypeError. |
| T-06-03-03 | Information Disclosure | 정은지 BodyProfile (키/체형) + 대표 frame 키포인트 | accept | 공개 athletic 정보. Firestore reference 컬렉션 = 앱 read-only |
| T-06-03-04 | Tampering | revert 스크립트 의도치 않은 다른 motion 삭제 | mitigate | C12 fix — `--motion-ids <ids>` argv 필수. dry-run default-off → 명시적 --dry-run / --commit 분리. R2 정합 — 두 필드 모두 delete 시 명시 |
| T-06-03-05 | Tampering | **R7** — dry-run 가 실수로 Firebase init 호출 → ADC 부재 환경에서 crash 또는 의도치 않은 prod write | mitigate | seed-reference-body-profile.mjs 의 dry-run 분기가 initializeApp / getFirestore 호출 전에 early return. test_backfill_scripts_dry_run 의 R7 검증이 ADC 미설정 subprocess 환경에서 exit 0 + Firebase init 호출 0 검증. |
| T-06-03-SC | Tampering | firebase-admin npm package | accept | 기존 devDependency |

(ASVS V4 Access Control = 기존 패턴 reuse. V5 Input Validation = T-06-03-02 + R2 BodyComparisonSourcePose flat values validator.)
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: firestore_admin.update_reference_body_data helper (R2 fix — 두 필드 atomic merge) + nested-array 차단</name>
  <files>
    backend/shared/python/sunity_shared/firestore_admin.py,
    backend/tests/phase06/test_firestore_admin_update_reference_body_data.py
  </files>
  <read_first>
    backend/shared/python/sunity_shared/firestore_admin.py,
    backend/shared/python/sunity_shared/analysis/body_normalization.py,
    backend/shared/python/sunity_shared/analysis/body_normalizer.py,
    backend/shared/python/sunity_shared/models.py
  </read_first>
  <behavior>
    - Test 1 (test_update_reference_body_data_sets_both_fields): update_reference_body_data("ref-climb", body_profile={"estimatedHeightScale": 1.0, ...}, source_pose={"jointKeys": [...], "values": [...], "frameIndex": 12, "torsoPx": 350.0, "confidence": 0.85, "measuredAt": 1700000000000}) → Firestore set merge 호출 + payload 의 `bodyNormalizationProfile` + `bodyComparisonSourcePose` 두 필드 모두 박제 + 두 필드의 *UpdatedAt 타임스탬프
    - Test 2 (test_update_reference_body_data_rejects_nested_array_in_body_profile): body_profile 의 warnings 가 list[list] → TypeError raise
    - Test 3 (test_update_reference_body_data_rejects_nested_array_in_source_pose): source_pose 의 values 안에 list 가 들어있으면 TypeError raise (정의상 flat float list 만 허용)
    - Test 4 (test_update_reference_body_data_rejects_missing_motion_id): motion_id="" → ValueError
    - Test 5 (test_update_reference_body_data_validates_body_profile_required_fields): body_profile 에 estimatedHeightScale 누락 → ValueError
    - Test 6 (test_update_reference_body_data_validates_source_pose_required_fields): source_pose 에 jointKeys 또는 values 누락 → ValueError
    - **Test 7 (R2 — test_update_reference_body_data_source_pose_values_length_matches_joint_keys)**: source_pose.values 의 length != 4 × len(jointKeys) → ValueError. e.g., jointKeys 17개 + values 60개 → ValueError (필요 = 68).
    - Test 8 (test_update_reference_body_data_accepts_list_of_strings_in_warnings): body_profile.warnings 가 list[str] → PASS
    - **Test 9 (R2 — test_update_reference_body_data_source_pose_optional)**: source_pose=None → body_profile 만 merge, bodyComparisonSourcePose 필드 무수정 (partial backfill 허용 — 백필 중 일부 motion 의 source_pose 추출 실패 시 graceful)
  </behavior>
  <action>
`backend/shared/python/sunity_shared/firestore_admin.py` 에 helper 추가. **R2 fix — 단일 진입점이 두 필드를 atomic merge**.

(A) `def update_reference_body_data(motion_id: str, body_profile: dict, source_pose: dict | None = None) -> None`:
  - Korean docstring: `"""R2 fix (2026-06-08 round-2, Plan 06-03). 정은지 reference 의 두 필드 (bodyNormalizationProfile + bodyComparisonSourcePose) atomic 백필 + Phase 14 정은지 reference 등록 helper (단일 진입점, D-06-B2). idempotent — 동일 입력 2회 실행 시 동일 결과. source_pose=None 시 body_profile 만 merge (partial backfill 허용 — 백필 도중 일부 motion 의 대표 frame 추출 실패 graceful)."""`
  - 본체:
    - `if not motion_id: raise ValueError("motion_id required")`
    - body_profile validation:
      - `_BP_REQUIRED = ("estimatedHeightScale", "armScale", "legScale", "torsoScale", "shoulderHipRatio", "confidence", "warnings")`
      - `missing = set(_BP_REQUIRED) - body_profile.keys()` → ValueError
      - `_validate_flat_dict_no_nested_array(body_profile, path="bodyNormalizationProfile")` (Plan 06-02 Task 2 validator)
    - source_pose validation (None-aware):
      - `if source_pose is not None`:
        - `_SP_REQUIRED = ("jointKeys", "values", "frameIndex", "torsoPx", "confidence", "measuredAt")`
        - `missing_sp = set(_SP_REQUIRED) - source_pose.keys()` → ValueError
        - `if len(source_pose["values"]) != 4 * len(source_pose["jointKeys"]): raise ValueError(...)`  # R2 fix
        - `_validate_flat_dict_no_nested_array(source_pose, path="bodyComparisonSourcePose")` — flat float list 정합 확인 (values 안에 list 가 있으면 fail)
    - payload 조립:
      - `payload = {"bodyNormalizationProfile": body_profile, "bodyNormalizationProfileUpdatedAt": int(time.time() * 1000)}`
      - `if source_pose is not None: payload["bodyComparisonSourcePose"] = source_pose; payload["bodyComparisonSourcePoseUpdatedAt"] = int(time.time() * 1000)`
    - `_doc(models.reference_motion_path(motion_id)).set(payload, merge=True)`
    - `log.info("update_reference_body_data ok motion_id=%s body_conf=%s source_pose_present=%s", motion_id, body_profile.get("confidence"), source_pose is not None)`

(B) Korean 주석: `# Phase 6 (2026-06-08, Plan 06-03) — D-06-B2 + R2 fix (round-2). mode1 silently OFF 차단 (두 canary: reference_profile_missing + reference_source_pose_missing) + Phase 14 정은지 reference 등록 helper. 두 필드 atomic merge.`

(C) 단위 테스트 `backend/tests/phase06/test_firestore_admin_update_reference_body_data.py` — `monkeypatch.setattr(firestore_admin, "_doc", mock_doc)`. assertion = behavior Test 1~9 1:1.

이모지 X. fenced code block X.

[per D-06-B2, R2 fix (2026-06-08 round-2 — 두 필드 atomic), [[firestore-nested-array-flat]], Plan 06-02 Task 2 validator]
  </action>
  <verify>
    <automated>cd backend && pytest tests/phase06/test_firestore_admin_update_reference_body_data.py -v</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "def update_reference_body_data(" backend/shared/python/sunity_shared/firestore_admin.py` == 1
    - Source: `grep -c "bodyComparisonSourcePose" backend/shared/python/sunity_shared/firestore_admin.py` >= 2 (payload + validator path)
    - **Source (R2 폐기 검증)**: `grep -c "def update_reference_body_profile(" backend/shared/python/sunity_shared/firestore_admin.py` == 0 — 구 단일 필드 helper 부재 (R2 단일 진입점 정신)
    - Behavior: 9 test PASS
    - Behavior: Test 7 PASS — values length 검증 (4 × len(jointKeys))
    - Behavior: Test 9 PASS — source_pose=None partial backfill 허용
  </acceptance_criteria>
  <done>R2 fix 단일 helper update_reference_body_data (두 필드 atomic) + nested-array 차단. Task 2 진입 가능.</done>
</task>

<task type="auto">
  <name>Task 2: extract_reference_body_profiles.py — Pod GPU 측정 스크립트 (BodyNormalizationProfile + **BodyComparisonSourcePose**, R2 fix) + --dry-run (C5, W3 직접 실행)</name>
  <files>
    backend/scripts/extract_reference_body_profiles.py,
    backend/scripts/README_extract_reference_body_profiles.md
  </files>
  <read_first>
    backend/shared/python/sunity_shared/analysis/body_normalization_measurer.py,
    backend/shared/python/sunity_shared/analysis/body_normalizer.py,
    backend/shared/python/sunity_shared/analysis/frame_extractor.py,
    backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/rtmw_engine.py,
    backend/shared/python/sunity_shared/analysis/pose_frame.py,
    backend/shared/python/sunity_shared/firestore_admin.py,
    backend/functions/pipeline/app.py,
    .planning/phases/06-coaching/06-REVIEWS.md
  </read_first>
  <action>
신규 Python 스크립트 — Pod GPU 에서 정은지 reference 5개 영상의 **두 필드** 측정. Lambda 실행 X (CPU NaN).

**W3 (직접 실행)**: `python backend/scripts/extract_reference_body_profiles.py ...` 형태로만 실행. `python -m backend.scripts....` 미사용. sys.path 주입 in-script.

(A) `backend/scripts/extract_reference_body_profiles.py`:
  - shebang 미사용
  - sys.path 주입: `from pathlib import Path; import sys; sys.path.insert(0, str(Path(__file__).resolve().parents[1] / "shared" / "python"))`
  - import: `from sunity_shared.analysis.body_normalization_measurer import measure_body_profile`, `from sunity_shared.analysis.body_normalizer import BodyComparisonSourcePose`, `from sunity_shared.analysis.pose_engines.rtmw.rtmw_engine import RTMWPoseEngine`, `from sunity_shared.analysis.frame_extractor import FfmpegFrameExtractor`, `from sunity_shared.analysis.pose_frame import PoleAxis, to_coco17_array`, `from sunity_shared.analysis.skeleton import KEYPOINT_NAMES`, `from sunity_shared import firestore_admin`
  - Module docstring (한국어): `"""정은지 reference 5개 영상의 두 필드 백필 데이터 측정. Pod GPU 직접 실행 (CPU NaN). 산출 = JSON fixture (`reference-body-data.json`) — 로컬 다운로드 후 `node app/scripts/seed-reference-body-profile.mjs` 으로 Firestore atomic merge. **R2 fix (2026-06-08 round-2)**: bodyNormalizationProfile + bodyComparisonSourcePose 두 필드를 함께 산출. 대표 frame 선정 = pose_frames 중 평균 keypoint confidence 가장 높은 frame (단순/robust). 실행: `python backend/scripts/extract_reference_body_profiles.py --bucket ... --output ...` (`-m` 미사용 — W3). C5 fix: --dry-run 플래그."""`
  - argparse:
    - `--bucket` (required)
    - `--output` (required)
    - `--motion-ids` (optional, default `ref-climb,ref-foxtop,ref-foxtop-split,ref-invert,ref-sideway-spin`)
    - `--dry-run` (optional, default False)
  - 본체 단계 per motion:
    - S3 영상 다운로드
    - `FfmpegFrameExtractor().extract(tmp)` → frames
    - default_pole = `PoleAxis(axis_vector=(0.0, 1.0, 0.0), confidence_level="low", source="vertical_fallback", frame_index=None)`
    - `pose_frames = RTMWPoseEngine.estimate(frames, default_pole)` — list[PoseFrame]
    - `body_profile = measure_body_profile(pose_frames)` → BodyNormalizationProfile
    - **R2 fix — 대표 frame 선정**:
      - 각 pose_frame 의 평균 keypoint confidence 산출 (keypoints_3d 의 uncertainty_proxy 1.0 - mean = confidence proxy)
      - 가장 높은 confidence 의 frame 의 index = `representative_frame_idx`
      - 해당 frame 의 17 keypoint × 4채널 (x, y, z, confidence) flat 값 산출 — `to_coco17_array(pose_frames[representative_frame_idx:representative_frame_idx+1])` 의 `.reshape(-1).tolist()` (길이 68)
      - 해당 frame 의 mid_shoulder ↔ mid_hip 픽셀 거리 = `representative_torso_px`
      - `source_pose = BodyComparisonSourcePose(joint_keys=tuple(KEYPOINT_NAMES), values=tuple(flat_values), frame_index=int(representative_frame_idx), torso_px=float(representative_torso_px), confidence=float(mean_conf_of_rep_frame), measured_at=int(time.time() * 1000))`
      - validation (BodyComparisonSourcePose `__post_init__`) 통과 — 위반 시 source_pose=None + warning log (graceful)
    - dataclass → camelCase dict 변환 (inline `_dataclass_to_camel_case_dict` 또는 Plan 06-02 helper import)
  - 산출 JSON 형식: `{"motions": {"ref-climb": {"bodyNormalizationProfile": {...}, "bodyComparisonSourcePose": {...} | null}, ...}, "measuredAt": <ISO timestamp>, "rtmwOnnxPath": <env>}`
  - **--dry-run 분기**: `if args.dry_run: print(json.dumps(payload, indent=2)); return`
  - 기본: `with open(args.output, "w") as f: json.dump(payload, f, indent=2)`
  - error handling: motion 단위 try/except, fail 시 motion_id + traceback log
  - logging: per-motion 측정 진행 + body confidence + source_pose 존재 여부 + dry-run/real-run 명시
  - `if __name__ == "__main__": main()`

(B) `backend/scripts/README_extract_reference_body_profiles.md` (한국어):
  - 본 스크립트는 정은지 reference 5개 영상의 **두 필드 (bodyNormalizationProfile + bodyComparisonSourcePose)** 측정. Pod GPU 전용. 산출 JSON 은 로컬에서 seed 스크립트로 Firestore atomic merge.
  - 실행 단계:
    1. Pod SSH (`ssh xbdkj1g2ylnfwi-64411701@ssh.runpod.io ...`)
    2. `cd /workspace/SunityMotion && git pull` ([[gsd-pod-work-push-first]])
    3. 환경 변수: `RTMW_ONNX_PATH=...`, `YOLOX_ONNX_PATH=...`, `FIREBASE_SA_PATH=...`
    4. **(C5 권장)** dry-run: `python backend/scripts/extract_reference_body_profiles.py --bucket sunity-motion-pilot-videos --output /workspace/reference-body-data.json --dry-run` — 출력 검토 + 5 motion 모두 bodyNormalizationProfile.confidence >= 0.5 + bodyComparisonSourcePose 존재
    5. real-run: 동일 명령 — `--dry-run` 제거
    6. 산출 JSON 로컬 다운로드 (`scp ...:/workspace/reference-body-data.json .`)
    7. Task 3 (seed 스크립트) 의 dry-run + commit 진입
  - W3 박제: `python -m backend.scripts....` 금지

이모지 X.

[per [[runpod-gpu-env]], [[gsd-pod-work-push-first]], R2 fix (2026-06-08 round-2 — 두 필드 산출), W3, C5 (--dry-run)]
  </action>
  <verify>
    <automated>python backend/scripts/extract_reference_body_profiles.py --help</automated>
  </verify>
  <acceptance_criteria>
    - Source: `backend/scripts/extract_reference_body_profiles.py` 존재 + argparse + 한국어 docstring
    - Source: `backend/scripts/README_extract_reference_body_profiles.md` 존재 + Pod SSH 단계 + dry-run 우선 단계 (C5)
    - Source grep (W3): `grep -c "python -m backend.scripts" backend/scripts/README_extract_reference_body_profiles.md` == 0
    - **Source grep (R2)**: `grep -c "BodyComparisonSourcePose" backend/scripts/extract_reference_body_profiles.py` >= 2 (import + 생성 코드)
    - **Source grep (R2)**: `grep -c "bodyComparisonSourcePose" backend/scripts/extract_reference_body_profiles.py` >= 1 (payload key)
    - Source grep (C5): `grep -c "\\-\\-dry-run" backend/scripts/extract_reference_body_profiles.py` >= 1
    - Source grep (C5): `grep -c "\\-\\-dry-run" backend/scripts/README_extract_reference_body_profiles.md` >= 1
    - Behavior: `python backend/scripts/extract_reference_body_profiles.py --help` exits 0 + usage (--bucket / --output / --dry-run)
  </acceptance_criteria>
  <done>Python extract 스크립트 — 두 필드 산출 (R2) + --dry-run (C5). Task 3 진입 가능.</done>
</task>

<task type="auto">
  <name>Task 3: seed-reference-body-profile.mjs — JSON fixture → Firestore atomic merge of **두 필드** + --dry-run (C5) + **명시적 ordering (R7 fix)**</name>
  <files>
    app/scripts/seed-reference-body-profile.mjs,
    app/package.json
  </files>
  <read_first>
    app/scripts/seed-reference-motions.mjs,
    app/scripts/verify-firebase.mjs,
    app/package.json,
    backend/shared/python/sunity_shared/firestore_admin.py
  </read_first>
  <action>
신규 Node.js 스크립트 — `seed-reference-motions.mjs` 패턴 정합. **R7 fix — Step 1 (parse/validate) → Step 2 (if dry-run: stdout + exit) → Step 3 (real-run: Firebase init + commit) 의 명시적 ordering 보장**.

(A) `app/scripts/seed-reference-body-profile.mjs`:
  - 헤더 주석 (Korean): "정은지 reference 두 필드 (bodyNormalizationProfile + bodyComparisonSourcePose) atomic seed. JSON fixture (`reference-body-data.json`, Task 2 산출) → Firestore reference/{motionId} 두 필드 동시 merge. C5: --dry-run. **R7 fix (2026-06-08 round-2)**: Step 1 parse/load/validate → Step 2 if --dry-run: stdout proposed payload + exit 0 (Firebase init 호출 X — ADC 미설정 환경 호환) → Step 3 real-run: initializeApp + getFirestore + batch.commit."
  - 사용법:
    - 사전: `gcloud auth application-default login` (real-run 만)
    - dry-run 우선: `cd app && npm run seed:body-profile -- --profiles ../reference-body-data.json --dry-run` (ADC 불요)
    - commit: `cd app && npm run seed:body-profile -- --profiles ../reference-body-data.json`
    - `--force` — 이미 박제된 reference 덮어쓰기
  - **R7 fix — main() 함수의 명시적 ordering**:
    - **Step 1 (parse + load + validate, Firebase 미접촉)**:
      - `const args = parseArgs(process.argv.slice(2))` — `--profiles <path>` + `--force` + `--dry-run`
      - `if (!args.profiles) { console.error('--profiles required'); process.exit(1) }`
      - `const payload = loadProfilesPayload(args.profiles)` — JSON.parse + `data.motions` dict 검증 + 각 motion 의 두 필드 schema validate (bodyNormalizationProfile 필수 + bodyComparisonSourcePose nullable)
      - 검증 실패 시 throw — Firebase 호출 전 fail-fast
    - **Step 2 (dry-run 분기 — Firebase init 호출 전 early return)**:
      - `if (args.dryRun) {`
      -   `const proposed = Object.entries(payload.motions).map(([id, data]) => ({ motionId: id, bodyNormalizationProfile: data.bodyNormalizationProfile, bodyComparisonSourcePose: data.bodyComparisonSourcePose }))`
      -   `console.log(JSON.stringify({ dryRun: true, willMerge: proposed.length, motions: proposed }, null, 2))`
      -   `return  // R7 — Firebase init 호출 X, ADC 의존 X`
      - `}`
    - **Step 3 (real-run — Firebase init 진입)**:
      - `import { applicationDefault, initializeApp } from 'firebase-admin/app'`
      - `import { getFirestore } from 'firebase-admin/firestore'`
      - `const PROJECT_ID = 'sunity-ai-coach'`
      - `initializeApp({ credential: applicationDefault(), projectId: PROJECT_ID })`
      - `const db = getFirestore()`
      - `const batch = db.batch()`
      - for each (motionId, data) in payload.motions:
        - existing check (`--force` 미설정 시 skip 박제된 doc)
        - flat validate (warnings nested-array reject)
        - `const docPayload = { bodyNormalizationProfile: data.bodyNormalizationProfile, bodyNormalizationProfileUpdatedAt: Date.now() }`
        - `if (data.bodyComparisonSourcePose) { docPayload.bodyComparisonSourcePose = data.bodyComparisonSourcePose; docPayload.bodyComparisonSourcePoseUpdatedAt = Date.now() }`
        - `batch.set(db.collection('reference').doc(motionId), docPayload, { merge: true })`
      - `await batch.commit()`
      - 읽기 검증 (`seed-reference-motions.mjs:290-300` 패턴) — 각 motion 의 두 필드 존재 확인

(B) `app/package.json` `scripts`:
  - `"seed:body-profile": "node scripts/seed-reference-body-profile.mjs"`

(C) firebase-admin devDependency 기존 — 신규 의존 0.

이모지 X.

[per D-06-B2, [[firebase-project-account]], 06-PATTERNS.md §4, C5 (--dry-run), R7 fix (명시적 ordering, dry-run 의 Firebase 미접촉)]
  </action>
  <verify>
    <automated>cd app && node --check scripts/seed-reference-body-profile.mjs && node scripts/seed-reference-body-profile.mjs --help 2>&1 | head -10 || true</automated>
  </verify>
  <acceptance_criteria>
    - Source: `app/scripts/seed-reference-body-profile.mjs` 존재
    - Source: `grep -c "seed:body-profile" app/package.json` == 1
    - Source (R7 fix — ordering): `seed-reference-body-profile.mjs` 내부에서 `initializeApp` 호출이 `if (args.dryRun)` early return 분기보다 텍스트상 뒤에 위치
    - Source (R7 fix — ordering grep): `grep -n "if.*dryRun.*return\\|args.dryRun" app/scripts/seed-reference-body-profile.mjs` 의 line number < `grep -n "initializeApp" app/scripts/seed-reference-body-profile.mjs` 의 line number
    - Source (R2): `grep -c "bodyComparisonSourcePose" app/scripts/seed-reference-body-profile.mjs` >= 2 (proposed + docPayload)
    - Source (C5): `grep -c "dry-run\\|dryRun" app/scripts/seed-reference-body-profile.mjs` >= 2
    - Behavior: `node --check app/scripts/seed-reference-body-profile.mjs` exit 0 (syntax 정합)
  </acceptance_criteria>
  <done>Node seed 스크립트 — 두 필드 atomic merge (R2) + --dry-run (C5) + R7 명시적 ordering + npm script. Task 4 (dry-run 통합 검증) 진입 가능.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4 (C5 + R7 fix): dry-run 통합 검증 — Python + Node 양 스크립트 + R7 ADC-free dry-run 검증</name>
  <files>
    backend/tests/phase06/test_backfill_scripts_dry_run.py,
    backend/tests/phase06/fixtures/test_reference_body_data.json
  </files>
  <read_first>
    backend/scripts/extract_reference_body_profiles.py,
    app/scripts/seed-reference-body-profile.mjs,
    backend/shared/python/sunity_shared/firestore_admin.py
  </read_first>
  <behavior>
    - Test 1 (test_extract_dry_run_outputs_json_to_stdout_without_writing): subprocess 로 `python backend/scripts/extract_reference_body_profiles.py --bucket test-bucket --output /tmp/should-not-exist.json --dry-run --motion-ids test-motion` (S3/RTMW/firestore_admin monkeypatch 또는 dependency injection). exit 0 + stdout 에 JSON ("motions" 포함). 출력 파일 미생성.
    - **Test 2 (R7 fix — test_seed_dry_run_works_without_adc_no_firebase_init)**: subprocess 로 `node app/scripts/seed-reference-body-profile.mjs --profiles fixtures/test_reference_body_data.json --dry-run` 을 **GOOGLE_APPLICATION_CREDENTIALS / ADC 환경 변수 모두 비운 상태에서** 실행. exit 0 + stdout 에 `"dryRun":true` 포함. **Firebase init 호출이 발생하지 않았음 검증** — stdout / stderr 에 `Could not load the default credentials` 또는 `applicationDefault` 관련 에러 메시지 부재. (R7 핵심: dry-run path 가 Firebase init 호출 전에 early return)
    - Test 3 (test_seed_real_run_calls_batch_commit_when_dry_run_absent): mock-Firebase-Admin 환경 또는 emulator 에서 `--dry-run` 없이 실행 → batch.commit() 호출됨.
    - **Test 4 (R7 fix — test_seed_dry_run_validates_schema_before_early_return)**: malformed fixture (e.g., motions[].bodyNormalizationProfile.estimatedHeightScale 누락) + --dry-run → exit non-zero + stderr 에 schema validation 에러. Firebase init 호출 부재.
    - Test 5 (test_extract_real_run_writes_file_when_dry_run_absent): mock 환경에서 `--dry-run` 없이 실행 시 output 파일 생성.
  </behavior>
  <action>
**C5 fix Task 4 + R7 fix** — 양 스크립트의 dry-run 동작 검증. 본 task 가 GREEN 이면 Task 5 (수동 checkpoint) 의 blast radius 1차 검증 완료.

(A) test fixture `backend/tests/phase06/fixtures/test_reference_body_data.json` 신설:
  - 1 motion 만 포함:
    - `{"motions": {"test-motion": {"bodyNormalizationProfile": {"estimatedHeightScale": 1.0, "armScale": 1.0, "legScale": 1.0, "torsoScale": 1.0, "shoulderHipRatio": 1.0, "confidence": 0.8, "warnings": []}, "bodyComparisonSourcePose": {"jointKeys": ["nose", ...17개], "values": [<flat 68 float>], "frameIndex": 12, "torsoPx": 350.0, "confidence": 0.85, "measuredAt": 1700000000000}}}, "measuredAt": "2026-06-08T00:00:00Z"}`

(B) `backend/tests/phase06/test_backfill_scripts_dry_run.py`:
  - **Test 1 (Python extract dry-run)**: `subprocess.run([sys.executable, "backend/scripts/extract_reference_body_profiles.py", "--bucket", "x", "--output", "/tmp/sentinel-must-not-exist.json", "--dry-run", "--motion-ids", "test-motion"], env={...mock env, S3/RTMW stub injection 위해 dependency injection 사용 또는 monkeypatch}, capture_output=True, timeout=30)`. exit 0 + stdout 에 "motions" + 파일 부재.
  - **Test 2 (R7 — ADC 미설정 + dry-run)**: `env = {**os.environ, "GOOGLE_APPLICATION_CREDENTIALS": "", "GOOGLE_CLOUD_PROJECT": ""}` (또는 환경에서 명시적 제거). `subprocess.run(["node", "app/scripts/seed-reference-body-profile.mjs", "--profiles", str(fixture_path), "--dry-run"], env=env, capture_output=True, timeout=30)`. exit 0 + stdout 에 `"dryRun":true` + **stderr 에 ADC 에러 메시지 부재** (`assert "Could not load" not in result.stderr.decode()`). R7 fix 핵심 — Firebase init 호출 전 early return.
  - Test 3: 별도 emulator 또는 mock Firebase npm 패키지 사용. (구현이 복잡하면 본 test 는 skip 또는 mark.integration — Test 4 (R7 schema) 가 더 중요).
  - **Test 4 (R7 — validation order)**: malformed fixture (estimatedHeightScale 누락) + --dry-run + ADC 미설정 → exit non-zero + stderr 에 schema 에러. Firebase init 호출 부재. validation 이 Firebase init 보다 먼저 실행됨을 증명.
  - Test 5: mock 환경에서 --dry-run 없이 output 파일 생성 검증.

(C) 본 task 의 요구 = Task 2/3 의 스크립트가 dependency injection 또는 명시적 ordering 으로 본 검증 가능해야 함:
  - `extract_reference_body_profiles.py`: dry-run 시에도 S3/RTMW 호출 필요 (측정이 본질). dependency injection — `def main(args, *, s3_client=None, rtmw_engine=None, firestore_client=None): ...` 형태. test 는 `from backend.scripts.extract_reference_body_profiles import main; main(args, s3_client=mock_s3, rtmw_engine=mock_rtmw)` 호출 (subprocess 회피 path).
  - `seed-reference-body-profile.mjs`: R7 fix 의 핵심 — dry-run 시 Firebase init 자체를 skip (Step 2 early return). 별도 mock 불요 — ADC 환경 비우면 R7 fix 가 작동하는지 직접 검증.

[per C5 (dry-run 통합 검증), R7 fix (ADC-free dry-run, validation before init), Task 5 blast radius 축소]
  </action>
  <verify>
    <automated>cd backend && pytest tests/phase06/test_backfill_scripts_dry_run.py -v</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: 5 test PASS (Test 3 은 mock 복잡도 시 skip 또는 mark.integration 허용)
    - **Behavior (R7)**: Test 2 PASS — ADC 미설정 + dry-run 환경에서 seed 스크립트 exit 0 + Firebase 에러 부재
    - **Behavior (R7)**: Test 4 PASS — malformed fixture + dry-run + ADC 미설정 → schema 에러 (Firebase 호출 전)
    - Behavior: dry-run 시 파일 미생성 + Firestore write 0
    - Source: 테스트 fixture `test_reference_body_data.json` 존재 + bodyNormalizationProfile + bodyComparisonSourcePose 둘 다 포함
  </acceptance_criteria>
  <done>C5 + R7 fix dry-run 통합 검증 GREEN. Task 5 의 blast radius 1차 검증 가능. Task 4.5 (revert) 진입 가능.</done>
</task>

<task type="auto">
  <name>Task 4.5 (C12 fix + R2): revert-reference-body-profile.mjs — **두 필드 모두** 제거 (rollback)</name>
  <files>
    app/scripts/revert-reference-body-profile.mjs,
    app/package.json
  </files>
  <read_first>
    app/scripts/seed-reference-motions.mjs,
    app/scripts/seed-reference-body-profile.mjs,
    app/package.json
  </read_first>
  <action>
**C12 fix (2026-06-08 reviews LOW) + R2 정합 — 두 필드 모두 rollback**.

(A) `app/scripts/revert-reference-body-profile.mjs`:
  - 헤더 주석 (Korean): "정은지 reference 의 **두 필드 (bodyNormalizationProfile + bodyComparisonSourcePose, R2 정합)** 제거 (rollback). C12 fix (2026-06-08 reviews). 백필이 잘못된 데이터로 reference 컬렉션을 오염시킬 경우 실행. FieldValue.delete() 두 필드 모두 처리 — reference doc 자체는 유지. idempotent."
  - import:
    - `import { applicationDefault, initializeApp } from 'firebase-admin/app'`
    - `import { getFirestore, FieldValue } from 'firebase-admin/firestore'`
  - argparse:
    - `--motion-ids <id1,id2,...>` (required)
    - `--dry-run` (optional, default false)
    - `--commit` (optional, default false) — 명시적 실 실행. 둘 다 없으면 dry-run 강제 (안전 기본값)
  - 본체:
    - motion_ids 빈 list 거부 (`throw new Error('--motion-ids required (no bulk-revert allowed)')`)
    - `if (!args.dryRun && !args.commit) { console.log('No --commit flag — forcing --dry-run for safety'); args.dryRun = true }`
    - `if (args.dryRun) { console.log(JSON.stringify({ dryRun: true, willDelete: args.motionIds.map(id => ({ motionId: id, fields: ['bodyNormalizationProfile', 'bodyComparisonSourcePose'] })) }, null, 2)); return }`
    - real-run: `initializeApp(...)`, batch:
      - `for (const id of args.motionIds) { batch.update(db.collection('reference').doc(id), { bodyNormalizationProfile: FieldValue.delete(), bodyNormalizationProfileUpdatedAt: FieldValue.delete(), bodyComparisonSourcePose: FieldValue.delete(), bodyComparisonSourcePoseUpdatedAt: FieldValue.delete() }) }` — R2 정합 두 필드 모두
      - `await batch.commit()`
      - `console.log(\`reverted ${args.motionIds.length} reference docs (두 필드 모두)\`)`

(B) `app/package.json` scripts: `"revert:body-profile": "node scripts/revert-reference-body-profile.mjs"`

(C) Korean 주석 운영 절차: "사용 예: dry-run 1차 — `cd app && npm run revert:body-profile -- --motion-ids ref-climb,ref-foxtop --dry-run` 후 출력 검토 → 실 실행 `--commit` 추가. ADC 필수 (sunity3412@gmail.com)."

이모지 X. fenced code block X (action 내부).

[per C12 (2026-06-08 reviews LOW) + R2 (round-2 — 두 필드 모두)]
  </action>
  <verify>
    <automated>cd app && node --check scripts/revert-reference-body-profile.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Source: `app/scripts/revert-reference-body-profile.mjs` 존재 + `FieldValue.delete` import + `--motion-ids` argparse
    - Source: `grep -c "revert:body-profile" app/package.json` == 1
    - **Source (R2)**: `grep -c "bodyComparisonSourcePose" app/scripts/revert-reference-body-profile.mjs` >= 1 — 두 필드 정합
    - **Source (R2)**: `grep -c "FieldValue.delete" app/scripts/revert-reference-body-profile.mjs` >= 2 — 두 필드 각각 delete
    - Source (안전 기본값): `grep -c "forcing --dry-run for safety" app/scripts/revert-reference-body-profile.mjs` >= 1
    - Behavior: `node --check app/scripts/revert-reference-body-profile.mjs` exit 0
  </acceptance_criteria>
  <done>C12 + R2 rollback 스크립트 (두 필드 모두). Task 5 (실행 checkpoint) 진입 가능.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: 실행 checkpoint — Pod GPU 측정 + 로컬 dry-run → real-run seed + Firestore Console verify **두 필드 모두** (C7 + R2 정합)</name>
  <files>(none — 실 실행, Firestore reference 5 doc 갱신)</files>
  <what-built>
Task 1~4.5 산출:
- `firestore_admin.update_reference_body_data` helper (Task 1, R2)
- `backend/scripts/extract_reference_body_profiles.py` Pod GPU 측정 + 두 필드 산출 + --dry-run (Task 2)
- `app/scripts/seed-reference-body-profile.mjs` 로컬 seed + 두 필드 atomic merge + --dry-run + R7 ordering (Task 3)
- `backend/tests/phase06/test_backfill_scripts_dry_run.py` (Task 4 — 자동 dry-run + R7 검증 GREEN)
- `app/scripts/revert-reference-body-profile.mjs` rollback + 두 필드 (Task 4.5)

본 checkpoint 가 실 실행 (Pod GPU 측정 → 로컬 dry-run → real-run → Firestore Console verify 두 필드 모두) 을 책임.
  </what-built>
  <how-to-verify>
**Step 1: Pod GPU 측정 (dry-run 우선)**

```
ssh xbdkj1g2ylnfwi-64411701@ssh.runpod.io -i ~/.ssh/id_ed25519
cd /workspace/SunityMotion
git pull origin main
export RTMW_ONNX_PATH=/workspace/rtmw_weights/rtmw-x-384.onnx
export YOLOX_ONNX_PATH=/workspace/yolox_weights/yolox_m.onnx
export FIREBASE_SA_PATH=/workspace/firebase-sa.json
python backend/scripts/extract_reference_body_profiles.py \
    --bucket sunity-motion-pilot-videos \
    --output /workspace/reference-body-data.json \
    --dry-run
```

**기대 출력 (dry-run)**: stdout 에 JSON — `"motions": {"ref-climb": {"bodyNormalizationProfile": {...}, "bodyComparisonSourcePose": {"jointKeys": [...17], "values": [<68 float>], "frameIndex": ..., "torsoPx": ..., "confidence": ..., "measuredAt": ...}}, ...}`. 5 motion 모두 등장. 파일 `/workspace/reference-body-data.json` 미생성.

**검증 항목 (dry-run 출력)**:
- 5 motion ID 모두 측정됨
- 각 motion 의 bodyNormalizationProfile.confidence >= 0.5
- 각 motion 의 bodyComparisonSourcePose 존재 (null 아님)
- bodyComparisonSourcePose.jointKeys 길이 == 17 (COCO-17)
- bodyComparisonSourcePose.values 길이 == 68 (= 4 × 17)
- bodyComparisonSourcePose.torsoPx > 0 + finite
- bodyComparisonSourcePose.confidence >= 0.5

**Step 2: Pod GPU 실 측정 (dry-run 통과 후)**

```
python backend/scripts/extract_reference_body_profiles.py \
    --bucket sunity-motion-pilot-videos \
    --output /workspace/reference-body-data.json
```

**기대**: `/workspace/reference-body-data.json` 생성. dry-run stdout 과 동일 내용.

**Step 3: 로컬 다운로드 + ADC**

```
scp xbdkj1g2ylnfwi-64411701@ssh.runpod.io:/workspace/reference-body-data.json .
gcloud auth application-default login   # sunity3412@gmail.com
```

**Step 4: seed 스크립트 dry-run (R7 — ADC 통과/미통과 모두 작동)**

```
cd app
npm run seed:body-profile -- --profiles ../reference-body-data.json --dry-run
```

**기대 출력**: stdout 에 `{"dryRun": true, "willMerge": 5, "motions": [{"motionId": "ref-climb", "bodyNormalizationProfile": {...}, "bodyComparisonSourcePose": {...}}, ...]}` — 5 motion 모두. Firestore 호출 0회. (R7 — Firebase init 호출 0.)

**Step 5: seed 스크립트 real-run (dry-run 통과 후)**

```
npm run seed:body-profile -- --profiles ../reference-body-data.json
```

**기대 출력**: `queued ref-climb body_conf=0.x source_pose=true` 형태 5줄 + `batch.commit OK` + read-back 검증.

**Step 6: Firestore Console verify (R2 — 두 필드 모두 검증)**

URL: `https://console.firebase.google.com/project/sunity-ai-coach/firestore/data/~2Freference`

**검증 항목 (각 reference doc)**:
- `bodyNormalizationProfile` 필드 존재 (object) + 7 sub-필드
- `bodyNormalizationProfileUpdatedAt` 필드 (timestamp ms)
- **`bodyComparisonSourcePose` 필드 존재 (object) — R2 신규**
- **`bodyComparisonSourcePose.jointKeys` 길이 17 (COCO-17)**
- **`bodyComparisonSourcePose.values` 길이 68 (4 × 17)**
- **`bodyComparisonSourcePose.values` 내부 nested-array 없음 — 단일 flat number array**
- **`bodyComparisonSourcePose.torsoPx > 0 + finite`**
- **`bodyComparisonSourcePose.confidence ∈ [0, 1]`**
- **`bodyComparisonSourcePoseUpdatedAt` 필드 (timestamp ms)**
- 5 doc 모두 동일 schema

**Step 7: idempotent**

```
npm run seed:body-profile -- --profiles ../reference-body-data.json
```

**기대**: 5 motion 모두 `skip ref-climb — already has bodyNormalizationProfile` (또는 동등 로그). batch.commit 0회 또는 빈 batch.

**문제 발생 시 — C12 + R2 rollback (두 필드 모두)**:

```
npm run revert:body-profile -- --motion-ids ref-climb,ref-foxtop,ref-foxtop-split,ref-invert,ref-sideway-spin --dry-run
# 출력 검토 후
npm run revert:body-profile -- --motion-ids ref-climb,ref-foxtop,ref-foxtop-split,ref-invert,ref-sideway-spin --commit
```

→ 5 reference 모두 두 필드 (bodyNormalizationProfile + bodyComparisonSourcePose) 제거 (FieldValue.delete). doc 자체는 유지.

**Approval 기준**:
- Step 1 (dry-run) 검증 항목 모두 통과 (특히 R2 — bodyComparisonSourcePose 존재 + values 길이 68)
- Step 6 Firestore Console 5 doc 모두 두 필드 schema 정합
- Step 7 idempotent 통과
  </how-to-verify>
  <resume-signal>"approved" 또는 측정값에 대한 구체적 우려 (예: "ref-climb 의 bodyComparisonSourcePose.confidence 0.4 — 임계 미달, 영상 재촬영 필요")</resume-signal>
  <action>Task 1~4.5 자동 완료 후 본 checkpoint 진입. C7 + R2 정합 — `<how-to-verify>` 의 모든 단계가 두 필드 모두 검증 + 구체적 expected output 명시.</action>
  <verify>
    <human-check>Step 1~7 의 각 expected output 이 실제 출력과 일치. dry-run 우선 → real-run 분리로 blast radius 최소. C12 + R2 rollback (두 필드) 가용 확인.</human-check>
  </verify>
  <done>5 reference doc 의 bodyNormalizationProfile + bodyComparisonSourcePose 두 필드 박제 + idempotent + Firestore Console verify 통과. mode1 silently OFF 차단 (두 canary: reference_profile_missing + reference_source_pose_missing).</done>
</task>

<task type="auto">
  <name>Task 6 (C6 fix, deferred but tracked): belle Pod sweep 사양 박제 — normalization ON vs OFF deduction_score reduction % 검증</name>
  <files>
    .planning/phases/06-coaching/06-03-DEFERRED-POD-SWEEP.md
  </files>
  <read_first>
    .planning/phases/06-coaching/06-NOTEBOOKLM-FINDINGS.md,
    .planning/phases/06-coaching/06-REVIEWS.md
  </read_first>
  <action>
**C6 fix (2026-06-08 reviews MEDIUM) — deferred but tracked**. 본 Task 는 사양 문서만. 실 실행은 별도.

신규 파일 `.planning/phases/06-coaching/06-03-DEFERRED-POD-SWEEP.md` 생성 — (이전 plan 의 내용 유지, R9 정합 카피 보완):

# Phase 6 — Pod Sweep Validation (deferred from Plan 06-03)

## 목적

NotebookLM §1.4 의 "PA-MPJPE 가 raw MPJPE 대비 60% 감소" 주장을 belle Pod 실 영상으로 검증.

## 입력

- 5 reference videos (정은지) — Plan 06-03 Task 5 에서 백필한 5 motion 의 원본 영상 (두 필드 모두 백필 완료 상태)
- 5 student videos — belle 또는 학원 수강생 (다양한 체형 — 키 140cm~170cm, 팔/다리 비율 다양)

## 실행 단계

1. Pod SSH + git pull (latest Plan 06-02 wiring 반영)
2. 10 videos 모두 S3 업로드
3. `_process` 를 normalization ON (Phase 6 wiring) 으로 5×5 = 25 조합 분석
4. `_process` 를 normalization OFF 강제 (compare_body_profiles 의 reference_profile=None + source_keypoints=None 강제) 로 동일 25 조합 분석
5. 각 조합의 `BodyComparisonReport.findings[].deductionScore` 합산 추출

## 출력 — Reduction % Table

| Student | Reference | normalization OFF (총 deduction) | normalization ON (총 deduction) | Reduction % |
|---------|-----------|----------------------------------|----------------------------------|-------------|
| S1 (140cm) | ref-climb | x | y | (1 - y/x) × 100 |
| ... | ... | ... | ... | ... |

평균 reduction % 산출.

## 검증 기준

- 평균 reduction >= 50% → NotebookLM §1.4 60% 주장 검증 (10% 허용 오차 내)
- 평균 reduction < 30% → 원인 분석 (foreshortening 비율, RTMW confidence 분포)
- **R9 정합 카피**: deduction 합산은 5 IPSF deficits + Sunity pose_reliability_low (poor_transitions 제외 — Phase 8 jerk/jitter 와 통합 deferred)
- 결과는 `.planning/phases/06-coaching/06-03-POD-SWEEP-RESULTS.md` 에 박제 (별도 commit)

## 실행 시점

- belle 의 운영 sweep 일정에 따라
- 우선순위: Phase 6 → Phase 7 진입 전 권장. Phase 7 이 본 sweep 결과를 hard-block 하지는 않음 — observational.
- 관련 메모리: [[feedback-analysis-first]], [[mvp-simple-pilot-quality]]

## 본 Plan 06-03 에서 본 Task 의 의미

본 Task 는 **사양만 박제**. 실 실행 deferred. Plan 06-03 의 Task 5 (수동 checkpoint) 가 완료되면 Plan 06-03 은 closed; 본 Task 6 은 백로그로 tracked.

이모지 X.

[per C6 (2026-06-08 reviews MEDIUM), deferred but tracked, NotebookLM §1.4 검증, R9 정합 카피]
  </action>
  <verify>
    <automated>test -f .planning/phases/06-coaching/06-03-DEFERRED-POD-SWEEP.md</automated>
  </verify>
  <acceptance_criteria>
    - Source: `.planning/phases/06-coaching/06-03-DEFERRED-POD-SWEEP.md` 존재
    - Source: `grep -c "5 reference" .planning/phases/06-coaching/06-03-DEFERRED-POD-SWEEP.md` >= 1
    - Source: `grep -c "Reduction %" .planning/phases/06-coaching/06-03-DEFERRED-POD-SWEEP.md` >= 1
    - Source: `grep -c "deferred" .planning/phases/06-coaching/06-03-DEFERRED-POD-SWEEP.md` >= 1
    - **Source (R9)**: `grep -c "pose_reliability_low\\|5 IPSF" .planning/phases/06-coaching/06-03-DEFERRED-POD-SWEEP.md` >= 1
  </acceptance_criteria>
  <done>C6 + R9 fix 사양 박제. 실 실행은 별도.</done>
</task>

</tasks>

<verification>
- [ ] `pytest backend/tests/phase06/test_firestore_admin_update_reference_body_data.py tests/phase06/test_backfill_scripts_dry_run.py -v` PASS
- [ ] `python backend/scripts/extract_reference_body_profiles.py --help` exits 0
- [ ] `grep -c "python -m backend.scripts" backend/scripts/README_extract_reference_body_profiles.md` == 0 (W3)
- [ ] **(C5)** `grep -c "\\-\\-dry-run" backend/scripts/extract_reference_body_profiles.py app/scripts/seed-reference-body-profile.mjs` >= 2
- [ ] **(R2)** `grep -c "def update_reference_body_data(" backend/shared/python/sunity_shared/firestore_admin.py` == 1
- [ ] **(R2)** `grep -c "def update_reference_body_profile(" backend/shared/python/sunity_shared/firestore_admin.py` == 0 — 구 단일 필드 helper 폐기
- [ ] **(R2)** `grep -c "bodyComparisonSourcePose" backend/scripts/extract_reference_body_profiles.py app/scripts/seed-reference-body-profile.mjs app/scripts/revert-reference-body-profile.mjs backend/shared/python/sunity_shared/firestore_admin.py` >= 4
- [ ] **(R7)** Test 2 of test_backfill_scripts_dry_run.py PASS — ADC 미설정 + dry-run + Firebase init 호출 0
- [ ] **(C12)** `test -f app/scripts/revert-reference-body-profile.mjs` exit 0
- [ ] **(C12)** `grep -c "FieldValue.delete" app/scripts/revert-reference-body-profile.mjs` >= 2 (두 필드)
- [ ] **(C12)** `grep -c "revert:body-profile" app/package.json` == 1
- [ ] **(C6)** `test -f .planning/phases/06-coaching/06-03-DEFERRED-POD-SWEEP.md` exit 0
- [ ] `app/package.json` 에 `seed:body-profile` script
- [ ] Firestore console 의 5 reference doc 모두 bodyNormalizationProfile + bodyComparisonSourcePose 두 필드 박제 (Task 5 수동 checkpoint approved)
</verification>

<success_criteria>
- [ ] Plan 06-01 의 `reference/{motionId}.bodyNormalizationProfile` + `reference/{motionId}.bodyComparisonSourcePose` (R2) 두 필드가 5 reference 에 실 데이터로 박제
- [ ] idempotent — 동일 실행 2회 시 무변경 (Task 5 Step 7)
- [ ] mode1 분석 silently OFF 차단 (RESEARCH.md Pitfall 6 해소 + R2 canary 해소 — reference_profile_missing + reference_source_pose_missing 둘 다 미발생)
- [ ] Phase 14 정은지 reference 본격 등록이 동일 helper 재사용 (Task 1, R2 단일 진입점)
- [ ] **R2 fix (round-2)** — Plan 06-01 의 BodyComparisonSourcePose 영속 contract 실데이터 박제. flat values 길이 68 + Firestore nested-array 회피.
- [ ] **R7 fix (round-2)** — seed 스크립트 dry-run path 가 ADC 의존성 없이 작동. validation 이 Firebase init 보다 먼저.
- [ ] **R9 fix (round-2)** — '7 deficits' 옛 표현 부재. 본 plan 의 검증 카피 모두 5 IPSF + Sunity pose_reliability_low.
- [ ] **C5 fix** — dry-run 검증 통합 (Task 4 자동 GREEN) + 운영 검증 단계 분리 (Task 5 의 Step 1/4 dry-run 우선)
- [ ] **C6 fix** — Pod sweep 사양 박제 (Task 6 deferred 문서)
- [ ] **C7 fix** — Task 5 `<how-to-verify>` 의 박제 filler 제거, 구체적 expected output + 두 필드 모두 검증 항목 명시
- [ ] **C12 fix** — revert 스크립트 박제 + npm script + 안전 기본값 + R2 정합 (두 필드 모두 delete)
</success_criteria>

<output>
Create `.planning/phases/06-coaching/06-03-SUMMARY.md` when done — Tasks 1~6 완료 + C5/C6/C7/C12 + R2/R7/R9 reviews fix 명시 + Firestore Console verify 결과 (두 필드 모두) 박제 + (가능 시) C6 deferred sweep trigger 일정.
</output>
