---
phase: 06-coaching
plan: 03
type: execute
wave: 3
depends_on: [06-02]
files_modified:
  - backend/shared/python/sunity_shared/firestore_admin.py
  - app/scripts/seed-reference-body-profile.mjs
  - app/scripts/revert-reference-body-profile.mjs
  - app/package.json
  - backend/scripts/extract_reference_body_profiles.py
  - backend/tests/phase06/test_firestore_admin_update_reference_body_profile.py
  - backend/tests/phase06/test_backfill_scripts_dry_run.py
autonomous: false
requirements: [PERS-01]
tags: [reference-backfill, body-profile, operational, phase-06, dry-run, revert]

must_haves:
  truths:
    - "현재 등록된 정은지 reference 5개 (ref-climb / ref-foxtop / ref-foxtop-split / ref-invert / ref-sideway-spin) 의 Firestore reference 컬렉션에 `bodyNormalizationProfile` 필드가 박제되어 mode1 분석 시 silently OFF 가 발생하지 않는다 (RESEARCH.md Pitfall 6)"
    - "백필 스크립트는 idempotent — 이미 박제된 reference 는 skip, --force 시에만 덮어씀"
    - "`firestore_admin.update_reference_body_profile(motion_id, profile_dict)` helper 가 백필 + Phase 14 정은지 reference 등록 두 caller 의 단일 helper"
    - "C5 fix (2026-06-08 reviews) — Python extract + Node seed 양 스크립트가 `--dry-run` 플래그 지원. dry-run 은 Firestore write 없이 proposed JSON 만 stdout 출력. Task 3.5 (자동) 가 dry-run 통합 검증. Task 4 (수동 checkpoint) 는 real-execute + Console verify 만."
    - "C12 fix (2026-06-08 reviews) — `app/scripts/revert-reference-body-profile.mjs` 는 `--motion-ids <ids>` argv 로 reference 컬렉션의 `bodyNormalizationProfile` 필드를 FieldValue.delete() 처리 (idempotent). dry-run 모드 지원."
    - "C6 fix (2026-06-08 reviews) — Task 5 (deferred) 가 belle Pod sweep 사양 박제: 5 reference + 5 student videos × normalization ON/OFF 조합 → deduction_score reduction % table 산출. NotebookLM §1.4 60% 주장 검증."
  artifacts:
    - path: backend/shared/python/sunity_shared/firestore_admin.py
      provides: "update_reference_body_profile helper 신설 + nested-array 차단 validator 재사용 (Plan 06-02 validator)"
      contains: ["def update_reference_body_profile("]
    - path: backend/scripts/extract_reference_body_profiles.py
      provides: "정은지 reference 5개 영상의 BodyProfile 측정 스크립트 (Pod GPU 직접 실행, RTMW + measure_body_profile). C5 fix — --dry-run 플래그 (Firestore write 없이 JSON stdout). 직접 실행 (`python backend/scripts/...`, `-m` invocation X — W3)"
    - path: app/scripts/seed-reference-body-profile.mjs
      provides: "JSON fixture → Firestore reference/{motionId}.bodyNormalizationProfile merge (Firebase Admin SDK, ADC). C5 fix — --dry-run 플래그 (write 없이 proposed payload stdout)"
    - path: app/scripts/revert-reference-body-profile.mjs
      provides: "C12 fix (2026-06-08 reviews) — reference 의 bodyNormalizationProfile 필드 제거 (FieldValue.delete). --motion-ids <ids> argv. idempotent + --dry-run 지원"
    - path: backend/tests/phase06/test_backfill_scripts_dry_run.py
      provides: "C5 fix Task 3.5 — Python + Node 양 스크립트 dry-run 통합 검증 (단일 test reference 대상). 자동 (autonomous=true 영역 내 task)"
  key_links:
    - from: app/scripts/seed-reference-body-profile.mjs
      to: backend/shared/python/sunity_shared/firestore_admin.py
      via: "동일 firestore reference 컬렉션 + bodyNormalizationProfile 필드 (Plan 06-01 contract + Plan 06-02 validator 정합)"
      pattern: "bodyNormalizationProfile"
    - from: backend/scripts/extract_reference_body_profiles.py
      to: backend/shared/python/sunity_shared/analysis/body_normalization_measurer.py
      via: "measure_body_profile(pose_frames) → BodyNormalizationProfile"
      pattern: "measure_body_profile"
    - from: app/scripts/revert-reference-body-profile.mjs
      to: Firestore reference/{motionId}
      via: "FieldValue.delete() 로 bodyNormalizationProfile 필드 제거 (C12 rollback)"
      pattern: "FieldValue.delete"
---

<objective>
정은지 reference 5개 영상의 `bodyNormalizationProfile` 백필. Plan 06-01 의 contract 갱신 (`reference/{motionId}.bodyNormalizationProfile` nullable 필드) + Plan 06-02 의 pipeline wiring 완료 후 진입. 미실행 시 mode1 분석에서 reference_profile is None → 정규화 silently OFF + warnings ⊃ "reference_profile_missing" (RESEARCH.md Pitfall 6).

**2026-06-08 revision (cross-AI review C5/C6/C7/C12 incorporated):**
- **C5 (MEDIUM)**: Task 4 의 blast radius (Pod SSH + git pull + 5 RTMW + scp + seed + Console verify) 를 줄이기 위해 양 스크립트에 `--dry-run` 추가. 신규 Task 3.5 (자동) 가 dry-run 통합 검증. Task 4 (수동 checkpoint) 는 real-execute + verify 만.
- **C6 (MEDIUM)**: Task 5 (deferred but tracked) — belle Pod sweep 5 reference + 5 student videos × normalization ON/OFF → deduction_score reduction % table. NotebookLM §1.4 60% 주장 검증. 본 phase 에서 실행 X, 사양만 박제.
- **C7**: Task 4 의 박제 filler 제거. `<how-to-verify>` 의 모든 단계가 구체적인 expected behavior + assertion 으로 재작성.
- **C12 (LOW)**: `app/scripts/revert-reference-body-profile.mjs` 신설 (Task 3.6). `--motion-ids <ids>` 로 reference 컬렉션의 bodyNormalizationProfile 필드 FieldValue.delete. idempotent + dry-run.

Task 구조 (총 6 task):
- Task 1 (자동): firestore_admin.update_reference_body_profile helper + nested-array 차단
- Task 2 (자동): extract_reference_body_profiles.py + --dry-run (C5)
- Task 3 (자동): seed-reference-body-profile.mjs + --dry-run (C5)
- Task 3.5 (자동, C5): dry-run 통합 검증 unit test
- Task 3.6 (자동, C12): revert-reference-body-profile.mjs
- Task 4 (수동 checkpoint): Pod GPU 실행 + 로컬 seed + Firestore Console verify (C7 filler 제거, 구체적 expected output 명시)
- Task 5 (deferred, C6): belle Pod sweep 사양 박제 (실행 별도 phase)

운영 작업 — 두 path:
1. **Path A (PREFERRED)**: Pod GPU 에서 `extract_reference_body_profiles.py` 실행 → 5 영상 다운로드 → RTMW estimate + measure_body_profile → `reference-body-profiles.json` → 로컬 다운로드 → `npm run seed:body-profile` 으로 Firestore reference 컬렉션 merge
2. **Path B (fallback)**: 합성 fixture (Plan 06-01 fixture 재활용) — 정은지 본인 측정값이 아니므로 임시 검증용. v1.5 에 정은지 본인 영상으로 교체.

Purpose:
- Plan 06-01 의 mode1 정규화 silently OFF 차단
- Phase 14 정은지 reference 본격 등록의 helper (재사용)
- 운영 idempotent — 동일 reference 2회 실행 시 동일 결과
- **C5 — blast radius 축소 (dry-run gate)**
- **C12 — rollback 옵션 (운영 안전)**
- **C6 — 정량적 검증 사양 박제 (NotebookLM 60% 주장)**

Output:
- 신규 `firestore_admin.update_reference_body_profile(motion_id, profile_dict)` helper
- 신규 `backend/scripts/extract_reference_body_profiles.py` (Pod GPU 직접 실행, --dry-run)
- 신규 `app/scripts/seed-reference-body-profile.mjs` (Firebase Admin SDK, --dry-run)
- **신규 `app/scripts/revert-reference-body-profile.mjs` (C12 rollback)**
- 갱신 `app/package.json` — `seed:body-profile` + `revert:body-profile` npm script
- 신규 `backend/tests/phase06/test_backfill_scripts_dry_run.py` (C5 Task 3.5)
- Firestore reference 컬렉션 5개 doc 갱신 (Task 4 — 실 실행은 checkpoint)
- Task 5 — belle Pod sweep 사양 (실행은 별도)
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/06-coaching/06-CONTEXT.md
@.planning/phases/06-coaching/06-RESEARCH.md
@.planning/phases/06-coaching/06-PATTERNS.md
@.planning/phases/06-coaching/06-REVIEWS.md
@.planning/phases/06-coaching/06-01-PLAN.md
@.planning/phases/06-coaching/06-02-PLAN.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local dev → Firebase Admin SDK → Firestore | Application Default Credentials (gcloud auth). sunity3412@gmail.com ([[firebase-project-account]]) |
| Pod GPU → S3 → 영상 다운로드 | 기존 IAM. 본 plan 신규 권한 0 |
| revert 스크립트 → Firestore | C12 rollback path. ADC 정합 + idempotent + dry-run 안전망 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Tampering | 백필 스크립트 의 reference 컬렉션 우발적 덮어쓰기 | mitigate | `--force` 명시 시에만 덮어쓰기 (default idempotent skip). C5 fix — `--dry-run` 으로 1차 검증 후 real-execute |
| T-06-03-02 | Tampering | nested-array (warnings 가 list[list] 인 경우) | mitigate | `update_reference_body_profile` 가 `_validate_flat_dict_no_nested_array` (Plan 06-02 Task 2 validator) 호출 — list[str] / list[dict-scalar] 허용 |
| T-06-03-03 | Information Disclosure | 정은지 BodyProfile (키/체형) | accept | 공개 athletic 정보 (대회 record). Firestore reference 컬렉션 = 앱 read-only |
| T-06-03-04 | Tampering | revert 스크립트 의도치 않은 다른 motion 삭제 | mitigate | C12 fix — `--motion-ids <ids>` argv 필수 (전체 reference 덮어쓰기 불가). dry-run 모드 default-off → 명시적 --dry-run / --commit 분리 |
| T-06-03-SC | Tampering | firebase-admin npm package | accept | 기존 devDependency, Phase 14 에도 사용 |

(ASVS V4 Access Control = 기존 패턴 reuse. V5 Input Validation = T-06-03-02 = Plan 06-02 validator.)
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: firestore_admin.update_reference_body_profile helper + nested-array 차단</name>
  <files>
    backend/shared/python/sunity_shared/firestore_admin.py,
    backend/tests/phase06/test_firestore_admin_update_reference_body_profile.py
  </files>
  <read_first>
    backend/shared/python/sunity_shared/firestore_admin.py,
    backend/shared/python/sunity_shared/analysis/body_normalization.py,
    backend/shared/python/sunity_shared/models.py
  </read_first>
  <behavior>
    - Test 1 (test_update_reference_body_profile_sets_field): update_reference_body_profile("ref-climb", {"estimatedHeightScale": 1.0, ...}) → Firestore set merge 호출 + payload 의 bodyNormalizationProfile + camelCase 필드 박제
    - Test 2 (test_update_reference_body_profile_rejects_nested_array): warnings 가 list[list] (예: `{"warnings": [["nested"], ["bad"]]}`) → TypeError raise
    - Test 3 (test_update_reference_body_profile_rejects_missing_motion_id): motion_id="" → ValueError raise
    - Test 4 (test_update_reference_body_profile_validates_required_fields): profile_dict 에 필수 필드 (예: estimatedHeightScale) 누락 → ValueError raise
    - Test 5 (test_update_reference_body_profile_accepts_list_of_strings): `{"warnings": ["a", "b"], ...}` → PASS (Plan 06-02 validator 재사용)
  </behavior>
  <action>
`backend/shared/python/sunity_shared/firestore_admin.py` 에 helper 추가. 06-PATTERNS.md §10 정합.

(A) `def update_reference_body_profile(motion_id: str, profile_dict: dict) -> None`:
  - Korean docstring: `"""정은지 reference 의 bodyNormalizationProfile 백필 + Phase 14 정은지 reference 등록 helper (단일 진입점, D-06-B2). idempotent — 동일 입력 2회 실행 시 동일 결과."""`
  - 본체:
    - `if not motion_id: raise ValueError("motion_id required")`
    - `_REQUIRED_FIELDS = ("estimatedHeightScale", "armScale", "legScale", "torsoScale", "shoulderHipRatio", "confidence", "warnings")`
    - `missing = set(_REQUIRED_FIELDS) - profile_dict.keys()` → `if missing: raise ValueError(f"profile_dict missing required fields: {missing}")`
    - `_validate_flat_dict_no_nested_array(profile_dict, path="bodyNormalizationProfile")` (Plan 06-02 Task 2 validator)
    - `_doc(models.reference_motion_path(motion_id)).set({"bodyNormalizationProfile": profile_dict, "bodyNormalizationProfileUpdatedAt": int(time.time() * 1000)}, merge=True)`
    - `log.info("update_reference_body_profile ok motion_id=%s confidence=%s", motion_id, profile_dict.get("confidence"))`

(B) Korean 주석: `# Phase 6 (2026-06-08, Plan 06-03) — D-06-B2. mode1 silently OFF 차단 + Phase 14 정은지 reference 등록 helper.`

(C) 단위 테스트 `backend/tests/phase06/test_firestore_admin_update_reference_body_profile.py` — `monkeypatch.setattr(firestore_admin, "_doc", mock_doc)`. assertion = behavior block Test 1~5 1:1.

[per D-06-B2, [[firestore-nested-array-flat]], Plan 06-02 Task 2 validator]
  </action>
  <verify>
    <automated>cd backend && pytest tests/phase06/test_firestore_admin_update_reference_body_profile.py -v</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "def update_reference_body_profile(" backend/shared/python/sunity_shared/firestore_admin.py` == 1
    - Behavior: 5 test PASS
    - Behavior: Test 2 PASS (TypeError raise)
    - Behavior: Test 5 PASS (list[str] 허용)
  </acceptance_criteria>
  <done>helper + nested-array 차단. Task 2 진입 가능.</done>
</task>

<task type="auto">
  <name>Task 2: extract_reference_body_profiles.py — Pod GPU 측정 스크립트 + --dry-run (C5 fix, 직접 실행 — `-m` 금지)</name>
  <files>
    backend/scripts/extract_reference_body_profiles.py,
    backend/scripts/README_extract_reference_body_profiles.md
  </files>
  <read_first>
    backend/shared/python/sunity_shared/analysis/body_normalization_measurer.py,
    backend/shared/python/sunity_shared/analysis/frame_extractor.py,
    backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/rtmw_engine.py,
    backend/shared/python/sunity_shared/firestore_admin.py,
    backend/functions/pipeline/app.py,
    .planning/phases/06-coaching/06-REVIEWS.md
  </read_first>
  <action>
신규 Python 스크립트 — Pod GPU 에서 정은지 reference 5개 영상의 BodyProfile 측정 → JSON fixture 산출 (RESEARCH.md Example 4 정합). Lambda 실행 X (CPU NaN).

**W3 (직접 실행)**: `python backend/scripts/extract_reference_body_profiles.py ...` 형태로만 실행. `python -m backend.scripts....` invocation 미사용. 이유: `__init__.py` 가 backend/ + backend/scripts/ 에 없음. sys.path 주입은 스크립트 자체에서: `sys.path.insert(0, str(Path(__file__).resolve().parents[1] / "shared" / "python"))`.

(A) `backend/scripts/extract_reference_body_profiles.py`:
  - shebang 미사용 (직접 실행 패턴)
  - sys.path 주입: `from pathlib import Path; import sys; sys.path.insert(0, str(Path(__file__).resolve().parents[1] / "shared" / "python"))`
  - import: `from sunity_shared.analysis.body_normalization_measurer import measure_body_profile`, `from sunity_shared.analysis.pose_engines.rtmw.rtmw_engine import RTMWPoseEngine`, `from sunity_shared.analysis.frame_extractor import FfmpegFrameExtractor`, `from sunity_shared.analysis.pose_frame import PoleAxis`, `from sunity_shared import firestore_admin`
  - Module docstring (한국어): `"""정은지 reference 5개 영상 (ref-climb / ref-foxtop / ref-foxtop-split / ref-invert / ref-sideway-spin) 의 bodyNormalizationProfile 측정. Pod GPU 직접 실행 (CPU NaN 차단). 산출 = JSON fixture (`reference-body-profiles.json`) — 로컬 다운로드 후 `node app/scripts/seed-reference-body-profile.mjs` 으로 Firestore merge. 실행: `python backend/scripts/extract_reference_body_profiles.py --bucket ... --output ...` (`-m` invocation 미사용 — W3). C5 fix (2026-06-08 reviews): --dry-run 플래그 — Firestore write 없이 측정 결과만 stdout 출력."""`
  - argparse:
    - `--bucket` (required) — S3 video bucket (`sunity-motion-pilot-videos`)
    - `--output` (required) — JSON fixture path
    - `--motion-ids` (optional, default `ref-climb,ref-foxtop,ref-foxtop-split,ref-invert,ref-sideway-spin`)
    - **`--dry-run` (optional, default False, C5 fix)** — Firestore write/JSON write 없이 측정 결과를 stdout JSON 으로 출력. 실 실행 전 1차 검증 용도.
  - 본체 단계:
    - `firestore_admin.list_reference_motions()` → motion_id 필터
    - 각 motion 순회:
      - S3 영상 다운로드 (`s3.download_file(bucket, video_key, tmp)`)
      - `FfmpegFrameExtractor().extract(tmp)` → frames ndarray
      - default_pole = `PoleAxis(axis_vector=(0.0, 1.0, 0.0), confidence_level="low", source="vertical_fallback", frame_index=None)`
      - `RTMWPoseEngine.estimate(frames, default_pole)` → list[PoseFrame] (Plan 06-02 C3 fix 정합)
      - `measure_body_profile(pose_frames)` → BodyNormalizationProfile
      - dataclass → camelCase dict 변환 (Plan 06-02 Task 3 `_dataclass_to_camel_case_dict` 재사용 또는 inline 변환)
    - 산출 JSON 형식: `{"motions": {"ref-climb": {"estimatedHeightScale": ..., "armScale": ..., ..., "confidence": ..., "warnings": [...]}, ...}, "measuredAt": <ISO timestamp>, "rtmwOnnxPath": <env>}`
    - **--dry-run 분기**: `if args.dry_run: print(json.dumps(payload, indent=2)); return` — 파일 write 없음.
    - 기본 (non-dry-run): `with open(args.output, "w") as f: json.dump(payload, f, indent=2)`
    - error handling: motion 단위 try/except, fail 시 motion_id + traceback log
  - logging: 측정 진행 + per-motion confidence 값 + dry-run/real-run mode 명시
  - `if __name__ == "__main__": main()`

(B) `backend/scripts/README_extract_reference_body_profiles.md` (한국어):
  - "본 스크립트는 정은지 reference 5개 영상의 BodyNormalizationProfile 을 측정한다. Pod GPU 실행 전용 (CPU NaN). 산출 JSON 은 로컬로 가져와 seed 스크립트로 Firestore 에 merge."
  - 실행 단계:
    1. Pod SSH (`ssh xbdkj1g2ylnfwi-64411701@ssh.runpod.io ...`)
    2. `cd /workspace/SunityMotion && git pull` ([[gsd-pod-work-push-first]] 정합)
    3. 환경 변수: `RTMW_ONNX_PATH=/workspace/rtmw_weights/rtmw-x-384.onnx`, `YOLOX_ONNX_PATH=/workspace/yolox_weights/yolox_m.onnx`, `FIREBASE_SA_PATH=/workspace/firebase-sa.json`
    4. **(C5 — 권장 우선 실행)** dry-run: `python backend/scripts/extract_reference_body_profiles.py --bucket sunity-motion-pilot-videos --output /workspace/reference-body-profiles.json --dry-run` — 측정 결과 stdout 으로 확인 + 5 motion 모두 confidence >= 0.5 + warnings 빈 list 검증
    5. (검증 통과 시) real run: `python backend/scripts/extract_reference_body_profiles.py --bucket sunity-motion-pilot-videos --output /workspace/reference-body-profiles.json`
    6. 산출 JSON 로컬 다운로드 (`scp xbdkj1g2ylnfwi-...:/workspace/reference-body-profiles.json .`)
    7. Task 3 (seed 스크립트) 의 dry-run + commit 진입
  - W3 박제: `python -m backend.scripts....` invocation 금지

이모지 X.

[per [[runpod-gpu-env]], [[gsd-pod-work-push-first]], RESEARCH.md Example 4, W3, C5 (--dry-run)]
  </action>
  <verify>
    <automated>python backend/scripts/extract_reference_body_profiles.py --help</automated>
  </verify>
  <acceptance_criteria>
    - Source: `backend/scripts/extract_reference_body_profiles.py` 존재 + argparse + 한국어 docstring
    - Source: `backend/scripts/README_extract_reference_body_profiles.md` 존재 + Pod SSH 단계 + dry-run 우선 실행 단계 (C5)
    - Source grep (W3): `grep -c "python -m backend.scripts" backend/scripts/README_extract_reference_body_profiles.md` == 0
    - **Source grep (C5)**: `grep -c "\\-\\-dry-run" backend/scripts/extract_reference_body_profiles.py` >= 1
    - **Source grep (C5)**: `grep -c "\\-\\-dry-run" backend/scripts/README_extract_reference_body_profiles.md` >= 1
    - Behavior: `python backend/scripts/extract_reference_body_profiles.py --help` exits 0 + usage (--bucket / --output / --dry-run)
  </acceptance_criteria>
  <done>Python extract 스크립트 + --dry-run (C5). Task 3 진입 가능.</done>
</task>

<task type="auto">
  <name>Task 3: seed-reference-body-profile.mjs — JSON fixture → Firestore merge + --dry-run (C5 fix)</name>
  <files>
    app/scripts/seed-reference-body-profile.mjs,
    app/package.json
  </files>
  <read_first>
    app/scripts/seed-reference-motions.mjs,
    app/scripts/verify-firebase.mjs,
    app/package.json,
    backend/shared/python/sunity_shared/firestore_admin.py
  </read_first>
  <action>
신규 Node.js 스크립트 — `seed-reference-motions.mjs` 패턴 정합 (06-PATTERNS.md §4). Firebase Admin SDK + ADC.

(A) `app/scripts/seed-reference-body-profile.mjs`:
  - 헤더 주석 (Korean): "정은지 reference bodyNormalizationProfile seed. JSON fixture (`reference-body-profiles.json`, Task 2 산출) → Firestore reference/{motionId}.bodyNormalizationProfile merge. C5 fix (2026-06-08 reviews): --dry-run 플래그 — Firestore write 없이 proposed payload 만 stdout 출력."
  - 사용법:
    - `gcloud auth application-default login` 선행 (sunity3412@gmail.com — [[firebase-project-account]])
    - dry-run 우선: `cd app && npm run seed:body-profile -- --profiles ../reference-body-profiles.json --dry-run`
    - commit: `cd app && npm run seed:body-profile -- --profiles ../reference-body-profiles.json`
    - `--force` — 이미 박제된 reference 도 덮어쓰기
  - import + init (`seed-reference-motions.mjs:1-21,47-48,228-237` 정합):
    - `import { applicationDefault, initializeApp } from 'firebase-admin/app'`
    - `import { getFirestore } from 'firebase-admin/firestore'`
    - `const PROJECT_ID = 'sunity-ai-coach'`
    - `initializeApp({ credential: applicationDefault(), projectId: PROJECT_ID })`
  - `parseArgs(argv)` — `--profiles <path>` + `--force` + **`--dry-run` (C5 fix)**
  - `loadProfilesPayload(path)` — JSON.parse + `data.motions` dict 검증
  - main:
    - `const db = getFirestore()`
    - `const proposed = []` — dry-run 출력용 버퍼
    - for each (motionId, profile_dict) in payload.motions:
      - `const existing = (await db.collection('reference').doc(motionId).get()).data()?.bodyNormalizationProfile`
      - `if (existing && !args.force) { console.log(\`skip ${motionId} — already has bodyNormalizationProfile\`); continue }`
      - flat validate (warnings 가 list[list] 면 throw)
      - `const payload = { bodyNormalizationProfile: profile_dict, bodyNormalizationProfileUpdatedAt: Date.now() }`
      - **dry-run 분기**: `if (args.dryRun) { proposed.push({ motionId, payload }); console.log(\`proposed ${motionId} confidence=${profile_dict.confidence}\`); continue }`
      - real run: `batch.set(db.collection('reference').doc(motionId), payload, { merge: true })`
      - `console.log(\`queued ${motionId} confidence=${profile_dict.confidence}\`)`
    - **dry-run 분기**: `if (args.dryRun) { console.log(JSON.stringify({ dryRun: true, proposed }, null, 2)); return }`
    - real run: `await batch.commit()`
    - 읽기 검증 (`seed-reference-motions.mjs:290-300` 패턴) — 각 motion 의 bodyNormalizationProfile 존재 + confidence 일치 확인

(B) `app/package.json` `scripts`:
  - `"seed:body-profile": "node scripts/seed-reference-body-profile.mjs"`

(C) firebase-admin devDependency 는 기존 `seed-reference-motions.mjs` 가 이미 사용 — 신규 의존 없음.

이모지 X.

[per D-06-B2, [[firebase-project-account]], 06-PATTERNS.md §4, C5 (--dry-run)]
  </action>
  <verify>
    <automated>cd app && node scripts/seed-reference-body-profile.mjs --help 2>&1 | head -10 || true</automated>
  </verify>
  <acceptance_criteria>
    - Source: `app/scripts/seed-reference-body-profile.mjs` 존재 + `applicationDefault` + `getFirestore`
    - Source: `grep -c "seed:body-profile" app/package.json` == 1
    - Source: `grep -c "PROJECT_ID = 'sunity-ai-coach'" app/scripts/seed-reference-body-profile.mjs` >= 1
    - **Source (C5)**: `grep -c "dry-run\\|dryRun" app/scripts/seed-reference-body-profile.mjs` >= 2
    - Behavior: 스크립트 syntax 정합 — `node --check app/scripts/seed-reference-body-profile.mjs` exit 0
  </acceptance_criteria>
  <done>Node seed 스크립트 + --dry-run (C5) + npm script. Task 3.5 (dry-run 통합 검증) 진입 가능.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3.5 (C5 fix): dry-run 통합 검증 — Python + Node 양 스크립트 단일 test reference 대상</name>
  <files>
    backend/tests/phase06/test_backfill_scripts_dry_run.py
  </files>
  <read_first>
    backend/scripts/extract_reference_body_profiles.py,
    app/scripts/seed-reference-body-profile.mjs,
    backend/shared/python/sunity_shared/firestore_admin.py
  </read_first>
  <behavior>
    - Test 1 (test_extract_dry_run_outputs_json_to_stdout_without_writing): subprocess 로 `python backend/scripts/extract_reference_body_profiles.py --bucket test-bucket --output /tmp/should-not-exist.json --dry-run --motion-ids test-motion` 실행 (S3/RTMW/firestore_admin 은 monkeypatch 또는 mock-injectable hook 으로 stub). exit 0 + stdout 에 JSON 문자열 ("motions" key 포함). `/tmp/should-not-exist.json` 파일 미생성.
    - Test 2 (test_seed_dry_run_outputs_proposed_payload_without_writing): subprocess 로 `node app/scripts/seed-reference-body-profile.mjs --profiles backend/tests/phase06/fixtures/test_reference_body_profile.json --dry-run` 실행 (Firebase Admin SDK 는 ADC stub 또는 mock initializeApp). exit 0 + stdout 에 "dryRun":true 포함. Firestore write 호출 카운트 == 0.
    - Test 3 (test_extract_real_run_writes_file_when_dry_run_absent): --dry-run 없이 실행 시 output 파일 생성됨 (mock 환경).
    - Test 4 (test_seed_real_run_calls_batch_commit_when_dry_run_absent): --dry-run 없이 실행 시 batch.commit() 호출됨 (mock 환경).
  </behavior>
  <action>
**C5 fix Task 3.5** — 양 스크립트의 dry-run 동작이 실제로 write 를 우회하는지 단위 테스트로 검증. 본 task 가 GREEN 이면 Task 4 (수동 checkpoint) 의 blast radius 가 축소된다 (1차 dry-run 검증 → 2차 real-run).

(A) test fixture `backend/tests/phase06/fixtures/test_reference_body_profile.json` 신설:
  - 1 motion 만 포함: `{"motions": {"test-motion": {"estimatedHeightScale": 1.0, "armScale": 1.0, "legScale": 1.0, "torsoScale": 1.0, "shoulderHipRatio": 1.0, "confidence": 0.8, "warnings": []}}, "measuredAt": "2026-06-08T00:00:00Z"}`

(B) `backend/tests/phase06/test_backfill_scripts_dry_run.py`:
  - Test 1: `subprocess.run([sys.executable, "backend/scripts/extract_reference_body_profiles.py", "--bucket", "x", "--output", "/tmp/sentinel-must-not-exist.json", "--dry-run", "--motion-ids", "test-motion"], env={...mock env...}, capture_output=True)`. S3 / RTMW / firestore_admin 은 monkeypatch 가능하도록 스크립트가 dependency injection 또는 env-based stub 지원해야 함. **간단 path**: pytest 가 스크립트를 import 한 후 main(args, deps=mocks) 직접 호출. 이 경우 스크립트의 main 함수는 deps 를 kwarg 로 받을 수 있게 박제 (default = real deps).
  - Test 2: `subprocess.run(["node", "app/scripts/seed-reference-body-profile.mjs", "--profiles", str(fixture_path), "--dry-run"], capture_output=True)`. Firebase Admin SDK 는 ADC 미설정 환경에서 fail 할 수 있으므로 dry-run path 가 ADC 호출 전에 분기되거나, mock-Firebase-Admin npm fixture 사용. **간단 path**: 스크립트가 dry-run 분기에서 db init 도 skip 하도록 수정 (--dry-run 시에는 Firestore 연결 자체를 시도하지 않음).
  - Test 3/4: dry-run flag 부재 시 정상 write path 가 호출되는지 (mock 으로) 검증.
  - subprocess timeout 30s, env 격리.

(C) (B) 의 "간단 path" 가 실현되려면 Task 2/3 의 스크립트에서 dry-run 시 외부 dependency 초기화도 skip 해야 함. 본 task 에서 그 요구사항을 명시:
  - `extract_reference_body_profiles.py`: dry-run 시에도 RTMW + S3 호출은 필요 (측정이 본질). 따라서 dependency injection 으로 stub 가능하게: `def main(args, *, s3_client=None, rtmw_engine=None, firestore_client=None): ...` 형태.
  - `seed-reference-body-profile.mjs`: dry-run 시 Firebase Admin SDK init 자체를 skip — payload 빌드 → stdout 출력만. real-run 시에만 initializeApp + getFirestore.

[per C5 (dry-run 통합 검증), Task 4 blast radius 축소]
  </action>
  <verify>
    <automated>cd backend && pytest tests/phase06/test_backfill_scripts_dry_run.py -v</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: 4 test PASS
    - Behavior: dry-run 시 파일 미생성 + Firestore write 0
    - Behavior: dry-run 없을 때 정상 write path 호출
    - Source: 테스트 fixture `test_reference_body_profile.json` 존재
  </acceptance_criteria>
  <done>C5 fix dry-run 통합 검증 GREEN. Task 4 의 blast radius 1차 검증 가능. Task 3.6 (revert) 진입 가능.</done>
</task>

<task type="auto">
  <name>Task 3.6 (C12 fix): revert-reference-body-profile.mjs — bodyNormalizationProfile 필드 제거 (rollback)</name>
  <files>
    app/scripts/revert-reference-body-profile.mjs,
    app/package.json
  </files>
  <read_first>
    app/scripts/seed-reference-motions.mjs,
    app/scripts/seed-reference-body-profile.mjs,
    app/package.json
  </read_first>
  <action>
**C12 fix (2026-06-08 reviews LOW)** — 백필이 잘못된 confidence/warnings 로 reference 컬렉션을 오염시킬 경우의 운영 rollback path. Operationally cheap insurance.

(A) `app/scripts/revert-reference-body-profile.mjs`:
  - 헤더 주석 (Korean): "정은지 reference bodyNormalizationProfile 필드 제거 (rollback). C12 fix (2026-06-08 reviews). 백필이 잘못된 confidence 또는 warnings 로 reference 컬렉션을 오염시켰을 때 실행. FieldValue.delete() 으로 필드만 제거 — reference doc 자체는 유지 (name, athleteName 등 기타 필드 무수정). idempotent (이미 부재한 필드 삭제는 no-op)."
  - import:
    - `import { applicationDefault, initializeApp } from 'firebase-admin/app'`
    - `import { getFirestore, FieldValue } from 'firebase-admin/firestore'`
  - argparse (process.argv 파싱):
    - `--motion-ids <id1,id2,...>` (required) — 콤마 구분 motion id 리스트. 전체 reference 박탈 방지 — 명시적 ids 만 처리.
    - `--dry-run` (optional, default false) — Firestore write 없이 stdout 으로 proposed delete 만 출력.
    - `--commit` (optional, default false) — 실 실행 명시. dry-run + commit 둘 다 없으면 dry-run 으로 강제 (안전 기본값).
  - 본체:
    - motion_ids 빈 list 거부 (`throw new Error('--motion-ids required (no bulk-revert allowed)')`)
    - `if (!args.dryRun && !args.commit) { console.log('No --commit flag — forcing --dry-run for safety'); args.dryRun = true }`
    - `if (args.dryRun) { console.log(JSON.stringify({ dryRun: true, willDelete: args.motionIds.map(id => ({ motionId: id, field: 'bodyNormalizationProfile' })) }, null, 2)); return }`
    - real run: `initializeApp({ credential: applicationDefault(), projectId: 'sunity-ai-coach' })`, batch:
      - `for (const id of args.motionIds) { batch.update(db.collection('reference').doc(id), { bodyNormalizationProfile: FieldValue.delete(), bodyNormalizationProfileUpdatedAt: FieldValue.delete() }) }`
      - `await batch.commit()`
      - `console.log(\`reverted ${args.motionIds.length} reference docs\`)`

(B) `app/package.json` scripts: `"revert:body-profile": "node scripts/revert-reference-body-profile.mjs"`

(C) Korean 주석으로 운영 절차 박제: "사용 예: dry-run 1차 — `cd app && npm run revert:body-profile -- --motion-ids ref-climb,ref-foxtop --dry-run` 후 출력 검토 → 실 실행 `--commit` 추가. ADC 필수 (sunity3412@gmail.com)."

이모지 X. fenced code block X (action 내부).

[per C12 (2026-06-08 reviews LOW), operational rollback insurance]
  </action>
  <verify>
    <automated>cd app && node --check scripts/revert-reference-body-profile.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Source: `app/scripts/revert-reference-body-profile.mjs` 존재 + `FieldValue.delete` import + `--motion-ids` argparse
    - Source: `grep -c "revert:body-profile" app/package.json` == 1
    - Source: `grep -c "FieldValue.delete" app/scripts/revert-reference-body-profile.mjs` >= 1
    - Source (안전 기본값): `grep -c "forcing --dry-run for safety" app/scripts/revert-reference-body-profile.mjs` >= 1
    - Behavior: `node --check app/scripts/revert-reference-body-profile.mjs` exit 0 (syntax 정합)
  </acceptance_criteria>
  <done>C12 rollback 스크립트. Task 4 (실행 checkpoint) 진입 가능.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: 실행 checkpoint — Pod GPU 측정 + 로컬 dry-run → real-run seed + Firestore Console verify (C7 filler 제거)</name>
  <files>(none — 실 실행, Firestore reference 5 doc 갱신)</files>
  <what-built>
Task 1~3.6 산출:
- `firestore_admin.update_reference_body_profile` helper (Task 1)
- `backend/scripts/extract_reference_body_profiles.py` Pod GPU 측정 스크립트 + --dry-run (Task 2)
- `app/scripts/seed-reference-body-profile.mjs` 로컬 seed 스크립트 + --dry-run (Task 3)
- `backend/tests/phase06/test_backfill_scripts_dry_run.py` (Task 3.5 — 자동 dry-run 검증 GREEN)
- `app/scripts/revert-reference-body-profile.mjs` rollback 스크립트 (Task 3.6)

본 checkpoint 가 실 실행 (Pod GPU 측정 → 로컬 dry-run → 로컬 real-run → Firestore Console verify) 을 책임진다.
  </what-built>
  <how-to-verify>
**Step 1: Pod GPU 측정 (dry-run 우선)**

```
ssh xbdkj1g2ylnfwi-64411701@ssh.runpod.io -i ~/.ssh/id_ed25519
cd /workspace/SunityMotion
git pull origin main
export RTMW_ONNX_PATH=/workspace/rtmw_weights/rtmw-x-384.onnx
export YOLOX_ONNX_PATH=/workspace/yolox_weights/yolox_m.onnx
export FIREBASE_SA_PATH=/workspace/firebase-sa.json
python backend/scripts/extract_reference_body_profiles.py \
    --bucket sunity-motion-pilot-videos \
    --output /workspace/reference-body-profiles.json \
    --dry-run
```

(W3: `python -m backend.scripts....` 미사용. __init__.py 부재.)

**기대 출력 (dry-run)**: stdout 에 JSON 형식 — `"motions": {"ref-climb": {"estimatedHeightScale": ..., "armScale": ..., ..., "confidence": 0.x, "warnings": [...]}, ...}`. 5 motion 모두 등장. 파일 `/workspace/reference-body-profiles.json` 미생성.

**검증 항목 (dry-run 출력)**:
- 5 motion ID (`ref-climb`, `ref-foxtop`, `ref-foxtop-split`, `ref-invert`, `ref-sideway-spin`) 모두 측정됨
- 각 motion 의 confidence >= 0.5 (D-06-U1 게이트 통과)
- warnings 가 빈 list `[]` (foreshortening_off / shoulder_hip_ratio_off 등 emit 시 영상 재촬영 검토)
- torsoScale == 1.0 (self-reference)
- estimatedHeightScale + armScale + legScale + shoulderHipRatio 모두 양수 + finite

**Step 2: Pod GPU 실 측정 (dry-run 검증 통과 후)**

```
python backend/scripts/extract_reference_body_profiles.py \
    --bucket sunity-motion-pilot-videos \
    --output /workspace/reference-body-profiles.json
```

**기대**: `/workspace/reference-body-profiles.json` 생성. dry-run stdout 과 동일 내용.

**Step 3: 로컬 다운로드 + ADC 인증**

```
scp xbdkj1g2ylnfwi-64411701@ssh.runpod.io:/workspace/reference-body-profiles.json .
gcloud auth application-default login   # sunity3412@gmail.com
```

**Step 4: seed 스크립트 dry-run**

```
cd app
npm run seed:body-profile -- --profiles ../reference-body-profiles.json --dry-run
```

**기대 출력**: stdout 에 `{"dryRun": true, "proposed": [{"motionId": "ref-climb", "payload": {...}}, ...]}` — 5 motion 모두 등장. Firestore 호출 0회.

**Step 5: seed 스크립트 real-run (dry-run 검증 통과 후)**

```
npm run seed:body-profile -- --profiles ../reference-body-profiles.json
```

**기대 출력**: `queued ref-climb confidence=0.x` 형태 5줄 + `batch.commit OK` + read-back 검증 (`seed-reference-motions.mjs:290-300` 패턴).

**Step 6: Firestore Console verify**

URL: `https://console.firebase.google.com/project/sunity-ai-coach/firestore/data/~2Freference`

**검증 항목 (Console 에서 5 reference doc 각각)**:
- `bodyNormalizationProfile` 필드 존재 (object)
- 7 sub-필드 모두 존재 (estimatedHeightScale, armScale, legScale, torsoScale, shoulderHipRatio, confidence, warnings)
- `bodyNormalizationProfileUpdatedAt` 필드 (timestamp ms) 존재
- 5 doc 모두 동일 schema

**Step 7: idempotent 검증**

```
npm run seed:body-profile -- --profiles ../reference-body-profiles.json
```

**기대 출력**: 5 motion 모두 `skip ref-climb — already has bodyNormalizationProfile` 형태 로그. batch.commit 호출 0회 (또는 빈 batch). Firestore 무변경.

**문제 발생 시 — C12 rollback 사용법**:
- 잘못된 profile 이 박제되었다고 판단되면 (예: confidence 0.3 같은 너무 낮은 값으로 silently 통과):
  ```
  npm run revert:body-profile -- --motion-ids ref-climb,ref-foxtop,ref-foxtop-split,ref-invert,ref-sideway-spin --dry-run
  # 출력 검토 후
  npm run revert:body-profile -- --motion-ids ref-climb,ref-foxtop,ref-foxtop-split,ref-invert,ref-sideway-spin --commit
  ```
  → 5 reference 모두 bodyNormalizationProfile 필드 제거 (FieldValue.delete). doc 자체는 유지.

**Approval 기준**:
- Step 1 (dry-run) 검증 항목 5종 통과
- Step 6 Firestore Console 5 doc 모두 schema 정합
- Step 7 idempotent 통과
  </how-to-verify>
  <resume-signal>"approved" 또는 측정값에 대한 우려 사항 명시 (예: "ref-climb confidence 가 0.4 라 게이트 미달 — 영상 재촬영 필요" 같은 구체적 redirect)</resume-signal>
  <action>Task 1~3.6 자동 완료 후 본 checkpoint 진입. C7 fix 정합 — `<how-to-verify>` 의 모든 단계가 구체적 expected output + 검증 항목 명시.</action>
  <verify>
    <human-check>Step 1~7 의 각 expected output 이 실제 출력과 일치하는지 검토. dry-run 우선 → real-run 분리로 blast radius 최소. C12 rollback 가용성 확인.</human-check>
  </verify>
  <done>5 reference doc 의 bodyNormalizationProfile 필드 박제 + idempotent 통과 + Firestore Console verify 통과. mode1 silently OFF 차단 (RESEARCH.md Pitfall 6).</done>
</task>

<task type="auto">
  <name>Task 5 (C6 fix, deferred but tracked): belle Pod sweep 사양 박제 — normalization ON vs OFF deduction_score reduction % 검증</name>
  <files>
    .planning/phases/06-coaching/06-03-DEFERRED-POD-SWEEP.md
  </files>
  <read_first>
    .planning/phases/06-coaching/06-NOTEBOOKLM-FINDINGS.md,
    .planning/phases/06-coaching/06-REVIEWS.md
  </read_first>
  <action>
**C6 fix (2026-06-08 reviews MEDIUM) — deferred but tracked**. 본 Task 는 사양 문서만 박제. 실 실행은 별도 phase 또는 belle 의 별도 운영 sweep 으로 진행.

신규 파일 `.planning/phases/06-coaching/06-03-DEFERRED-POD-SWEEP.md` 생성:

# Phase 6 — Pod Sweep Validation (deferred from Plan 06-03)

## 목적

NotebookLM §1.4 의 "PA-MPJPE 가 raw MPJPE 대비 60% 감소" 주장을 belle Pod 실 영상으로 검증. 합성 fixture (Plan 06-01 Task 1) 는 hand-constructed 이므로 RTMW 의 correlated noise / foreshortening artifact / pole-occlusion 패턴을 mimic 하지 못한다. Phase 6 의 production win 은 실 sweep 으로만 증명 가능.

## 입력

- 5 reference videos (정은지) — Plan 06-03 Task 4 에서 백필한 5 motion 의 원본 영상
- 5 student videos — belle 또는 학원 수강생 영상 (다양한 체형 — 키 140cm~170cm 범위, 팔/다리 비율 다양)

## 실행 단계

1. Pod SSH + git pull (latest Plan 06-02 wiring 반영)
2. 10 videos 모두 S3 업로드
3. `_process` 를 normalization ON (Phase 6 wiring) 으로 5×5 = 25 조합 분석
4. `_process` 를 normalization OFF 강제 (compare_body_profiles 의 reference_profile=None 강제) 로 동일 25 조합 분석
5. 각 조합의 `BodyComparisonReport.findings[].deductionScore` 합산 추출

## 출력 — Reduction % Table

| Student | Reference | normalization OFF (총 deduction) | normalization ON (총 deduction) | Reduction % |
|---------|-----------|----------------------------------|----------------------------------|-------------|
| S1 (140cm) | ref-climb | x | y | (1 - y/x) × 100 |
| ... | ... | ... | ... | ... |

평균 reduction % 산출.

## 검증 기준

- 평균 reduction >= 50% → NotebookLM §1.4 60% 주장 직접 검증 (10% 허용 오차 내)
- 평균 reduction < 30% → Phase 6 의 algorithmic gain 이 합성 fixture 수치보다 실 영상에서 작음 → 원인 분석 (foreshortening 비율, RTMW confidence 분포)
- 결과는 `.planning/phases/06-coaching/06-03-POD-SWEEP-RESULTS.md` 에 박제 (별도 commit)

## 실행 시점

- belle 의 운영 sweep 일정에 따라 (본 Plan 06-03 의 자동 task 영역 아님)
- 우선순위: Phase 6 → Phase 7 (차이 분류) 진입 전 권장. 단 Phase 7 이 본 sweep 결과를 hard-block 하지는 않음 — observational 검증.
- 관련 메모리: [[feedback-analysis-first]] (분석 정확도 우선), [[mvp-simple-pilot-quality]] (단순 fallback + 점진적 정밀화)

## 본 Plan 06-03 에서 본 Task 의 의미

본 Task 는 **사양만 박제**. 실 실행은 deferred. 본 phase 의 Task 4 (수동 checkpoint) 가 완료되면 Plan 06-03 은 closed; 본 Task 5 는 백로그로 tracked.

이모지 X.

[per C6 (2026-06-08 reviews MEDIUM), deferred but tracked, NotebookLM §1.4 검증]
  </action>
  <verify>
    <automated>test -f .planning/phases/06-coaching/06-03-DEFERRED-POD-SWEEP.md</automated>
  </verify>
  <acceptance_criteria>
    - Source: `.planning/phases/06-coaching/06-03-DEFERRED-POD-SWEEP.md` 존재
    - Source: `grep -c "5 reference" .planning/phases/06-coaching/06-03-DEFERRED-POD-SWEEP.md` >= 1
    - Source: `grep -c "Reduction %" .planning/phases/06-coaching/06-03-DEFERRED-POD-SWEEP.md` >= 1
    - Source: `grep -c "deferred" .planning/phases/06-coaching/06-03-DEFERRED-POD-SWEEP.md` >= 1
  </acceptance_criteria>
  <done>C6 fix 사양 박제. 실 실행은 별도 운영 trigger.</done>
</task>

</tasks>

<verification>
- [ ] `pytest backend/tests/phase06/test_firestore_admin_update_reference_body_profile.py tests/phase06/test_backfill_scripts_dry_run.py -v` PASS
- [ ] `python backend/scripts/extract_reference_body_profiles.py --help` exits 0
- [ ] `grep -c "python -m backend.scripts" backend/scripts/README_extract_reference_body_profiles.md` == 0 (W3)
- [ ] **(C5)** `grep -c "\\-\\-dry-run" backend/scripts/extract_reference_body_profiles.py app/scripts/seed-reference-body-profile.mjs` >= 2
- [ ] **(C12)** `test -f app/scripts/revert-reference-body-profile.mjs` exit 0
- [ ] **(C12)** `grep -c "FieldValue.delete" app/scripts/revert-reference-body-profile.mjs` >= 1
- [ ] **(C12)** `grep -c "revert:body-profile" app/package.json` == 1
- [ ] **(C6)** `test -f .planning/phases/06-coaching/06-03-DEFERRED-POD-SWEEP.md` exit 0
- [ ] `app/package.json` 에 `seed:body-profile` script
- [ ] Firestore console 의 5 reference doc 모두 bodyNormalizationProfile 박제 (Task 4 수동 checkpoint approved)
</verification>

<success_criteria>
- [ ] Plan 06-01 의 `reference/{motionId}.bodyNormalizationProfile` nullable 필드가 5 reference 에 실 데이터로 박제
- [ ] idempotent — 동일 실행 2회 시 무변경 (Task 4 Step 7)
- [ ] mode1 분석 silently OFF 차단 (RESEARCH.md Pitfall 6 해소)
- [ ] Phase 14 정은지 reference 본격 등록이 동일 helper 재사용 (Task 1)
- [ ] **C5 fix** — dry-run 검증 통합 (Task 3.5 자동 GREEN) + 운영 검증 단계 분리 (Task 4 의 Step 1/4 dry-run 우선)
- [ ] **C6 fix** — Pod sweep 사양 박제 (Task 5 deferred 문서)
- [ ] **C7 fix** — Task 4 `<how-to-verify>` 의 박제 filler 제거, 구체적 expected output + 검증 항목 명시
- [ ] **C12 fix** — revert 스크립트 박제 + npm script + 안전 기본값 (--dry-run forced when neither flag set)
</success_criteria>

<output>
Create `.planning/phases/06-coaching/06-03-SUMMARY.md` when done — Tasks 1~5 완료 + C5/C6/C7/C12 reviews fix 명시 + Firestore Console verify 결과 박제 + (가능 시) C6 deferred sweep 의 trigger 일정.
</output>
