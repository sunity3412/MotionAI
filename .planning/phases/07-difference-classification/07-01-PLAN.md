---
phase: 07-difference-classification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/shared/python/sunity_shared/analysis/body_normalizer.py
  - backend/shared/python/sunity_shared/analysis/copy_templates.py
  - backend/shared/python/sunity_shared/models.py
  - app/src/types/analysis.ts
  - docs/contract.md
  - backend/tests/phase07/__init__.py
  - backend/tests/phase07/conftest.py
  - backend/tests/phase07/fixtures/__init__.py
  - backend/tests/phase07/fixtures/_factory.py
  - backend/tests/phase07/fixtures/fixture_classification_allowed.json
  - backend/tests/phase07/fixtures/fixture_classification_needs.json
  - backend/tests/phase07/fixtures/fixture_classification_uncertain_raw.json
  - backend/tests/phase07/fixtures/fixture_classification_uncertain_low_conf.json
  - backend/tests/phase07/fixtures/fixture_classification_uncertain_global_low.json
  - backend/tests/phase07/fixtures/fixture_classification_mode3_first_fallback.json
  - backend/tests/phase07/test_copy_templates_no_forbidden.py
  - backend/tests/phase07/test_copy_templates_render.py
  - backend/tests/phase07/test_body_comparison_report_phase7_lockstep.py
autonomous: true
requirements: [PERS-01]
tags: [classification, canned-copy, schema-lockstep, phase-07, test-scaffold]
scope_sanity_note: "17 파일 중 10 개는 fixture JSON (정적 데이터) / `__init__.py` (빈 디렉토리 marker) / 정적 파일 — 실제 코드 변경은 5-6 파일 수준 (body_normalizer.py + copy_templates.py + analysis.ts + contract.md + 3 test 파일 + factory.py). scope 분할 불필요 — 단일 plan ~50% context budget 정합."

must_haves:
  truths:
    - "BodyComparisonFinding 가 Phase 7 신설 4 필드 (category Literal enum + phase nullable str + body_type_interpretation + recommendation) 를 보유하고 __post_init__ 가 category enum 3종 (body_type_allowed / needs_adjustment / uncertain) 외 값을 ValueError 로 거부한다 — per D-07-A1 + D-07-C1"
    - "BodyComparisonReport 가 Phase 7 신설 2 필드 (do_not_over_correct: list[str], recommended_focus: list[str]) 를 보유하고 field(default_factory=list) 로 빈 list[] 가 기본값이다 — per D-07-B1 + D-07-B3"
    - "TS interface (app/src/types/analysis.ts) ↔ Python dataclass (body_normalizer.py) ↔ docs/contract.md §8 세 곳이 한 commit 으로 동시 갱신된다 (3-way atomic lockstep, Phase 6 116f400 / a444726 패턴) — 부분 commit 금지"
    - "Task 3 의 단일 atomic commit 박제: BodyComparisonFinding 4 필드 확장 + measure_ipsf_absolute_deficits 의 6 호출 위치 (line 945/977/1027/1056/1076/1102) placeholder (`category=\"body_type_allowed\"`) 동시 박제 → Phase 6 회귀 0 보장. 부분 commit 시 Phase 6 회귀 발생, 단일 atomic 강제"
    - "copy_templates.py 모듈이 신설되어 _COPY_TEMPLATES (21 항 dict literal) + _MODE_PREFIX (3 항 dict literal) + render_finding_copy() 함수 + FORBIDDEN_PHRASES (6 research §10.3) + FORBIDDEN_PHRASES_SUNITY (3 톤 룰) tuple 을 export 한다 — per D-07-B2 + D-07-D1"
    - "9 금지 표현 grep gate test (test_copy_templates_no_forbidden.py) 가 AST 기반으로 _COPY_TEMPLATES + _MODE_PREFIX 의 string constant 만 iterate (FORBIDDEN tuple 자체는 검사 scope 밖) → 0 violations PASS — per D-07-D2"
    - "render_finding_copy() 가 21 canned key 모두 lookup 성공 + 3 mode prefix 가 recommendation 에만 prepend + interpretation 은 mode 무관 + 키 미발견 시 graceful fallback tuple 반환 + mode3_first + used_reference_fallback path 의 단일 fallback 카피 박제 (RESEARCH.md Open Questions Q1 RESOLVED) — per D-07-D3"
    - "3-way lockstep test 가 4+2 신설 필드 (category / phase / bodyTypeInterpretation / recommendation / doNotOverCorrect / recommendedFocus) 를 TS regex + Python regex + contract.md regex 세 위치 모두에서 발견하지 못하면 fail-fast (drift defense)"
    - "Wave 0 test 인프라 (phase07/__init__.py + conftest.py + fixtures/_factory.py + 6 fixture JSON) 가 박제되어 Plan 02 의 classify_findings + integration test 가 즉시 실행 가능하다"
    - "copy_templates.py 모듈 상단에 `from __future__ import annotations` 박제 (Phase 6 모듈 일관성 정합)"
  artifacts:
    - path: backend/shared/python/sunity_shared/analysis/body_normalizer.py
      provides: "BodyComparisonFinding 4 신설 필드 (category Literal + phase nullable + body_type_interpretation + recommendation) + __post_init__ category enum validator + BodyComparisonReport 2 신설 필드 (do_not_over_correct + recommended_focus, field(default_factory=list)) + module-level constants CATEGORY_GATE = 0.2, CATEGORY_CONF_GATE = 0.5 + measure_ipsf_absolute_deficits 의 6 BodyComparisonFinding 호출 위치 category placeholder"
      contains:
        - "category: Literal[\"body_type_allowed\", \"needs_adjustment\", \"uncertain\"]"
        - "phase: str | None"
        - "body_type_interpretation: str | None"
        - "recommendation: str | None"
        - "do_not_over_correct: list[str] = field(default_factory=list)"
        - "recommended_focus: list[str] = field(default_factory=list)"
        - "CATEGORY_GATE = 0.2"
        - "CATEGORY_CONF_GATE = 0.5"
    - path: backend/shared/python/sunity_shared/analysis/copy_templates.py
      provides: "Phase 7 canned string mapping module — 21 카피 dict literal + 3 mode prefix + render_finding_copy() lookup + FORBIDDEN_PHRASES + FORBIDDEN_PHRASES_SUNITY tuple. 정적 dict literal, secret 무관, LLM/network 호출 0. `from __future__ import annotations` 모듈 헤더 박제"
      contains:
        - "from __future__ import annotations"
        - "_COPY_TEMPLATES: dict[tuple[str, Category, JointGroup], tuple[str, str]]"
        - "_MODE_PREFIX: dict[ComparisonType, str]"
        - "def render_finding_copy("
        - "FORBIDDEN_PHRASES"
        - "FORBIDDEN_PHRASES_SUNITY"
        - "from .skeleton import JOINT_LABEL_KO"
    - path: app/src/types/analysis.ts
      provides: "BodyComparisonFinding interface 4 신설 필드 (category union + phase optional + bodyTypeInterpretation optional + recommendation optional) + BodyComparisonReport interface 2 신설 필드 (doNotOverCorrect: string[], recommendedFocus: string[] — non-optional; frontend normalize() 의 default 처리는 Phase 12 책임, 본 phase 미수정)"
      contains:
        - "category: 'body_type_allowed' | 'needs_adjustment' | 'uncertain'"
        - "phase?: string | null"
        - "bodyTypeInterpretation?: string | null"
        - "recommendation?: string | null"
        - "doNotOverCorrect: string[]"
        - "recommendedFocus: string[]"
    - path: docs/contract.md
      provides: "§8 BodyComparisonFinding 표에 4 행 추가 + BodyComparisonReport 표에 2 행 추가 + §8.3 Phase 7 분류 룰 신규 서브섹션 (D-07-A1/A2/U1 + 21 canned coverage 표)"
      contains:
        - "§8.3"
        - "category"
        - "doNotOverCorrect"
        - "recommendedFocus"
        - "bodyTypeInterpretation"
    - path: backend/shared/python/sunity_shared/models.py
      provides: "Python re-export 정합 — body_normalizer 의 신설 필드는 dataclass 본체 변경만, models.py import 헤더는 무수정 (BodyComparisonFinding / BodyComparisonReport 이미 import 됨)"
      contains:
        - "from .analysis.body_normalizer import"
    - path: backend/tests/phase07/conftest.py
      provides: "Phase 7 fixture 디렉토리 상수 PHASE07_FIXTURES_DIR 노출 (Phase 6 conftest 패턴 정합)"
      contains:
        - "PHASE07_FIXTURES_DIR"
    - path: backend/tests/phase07/fixtures/_factory.py
      provides: "load_fixture_raw() + load_finding_from_fixture(name) 헬퍼 — JSON → BodyComparisonFinding 변환"
      contains:
        - "def load_fixture_raw("
        - "def load_finding_from_fixture("
    - path: backend/tests/phase07/test_copy_templates_no_forbidden.py
      provides: "9 금지 표현 (6 research + 3 톤 룰) parametrize grep gate (AST 기반) — _COPY_TEMPLATES + _MODE_PREFIX iterate, 위반 시 위치 정확히 지목"
      contains:
        - "@pytest.mark.parametrize"
        - "FORBIDDEN_PHRASES + FORBIDDEN_PHRASES_SUNITY"
    - path: backend/tests/phase07/test_copy_templates_render.py
      provides: "21 카피 매핑 coverage + 3 mode prefix prepend + fallback graceful + interpretation mode-invariant 검증"
      contains:
        - "def test_render_finding_copy_mode1_prefix"
        - "def test_render_finding_copy_mode3_first_prefix"
        - "def test_render_finding_copy_mode3_progress_prefix"
        - "def test_render_finding_copy_fallback_on_missing_key"
        - "def test_interpretation_is_mode_invariant"
    - path: backend/tests/phase07/test_body_comparison_report_phase7_lockstep.py
      provides: "TS / Python / contract.md 3-way drift defense — 4+2 신설 필드 모두 세 곳에서 grep 통과"
      contains:
        - "_FIELD_MAP"
        - "category"
        - "doNotOverCorrect"
        - "recommendedFocus"
  key_links:
    - from: app/src/types/analysis.ts
      to: backend/shared/python/sunity_shared/analysis/body_normalizer.py
      via: "3-way contract — camelCase TS ↔ snake_case Python ↔ docs §8 + §8.3 atomic commit"
      pattern: "BodyComparisonFinding|BodyComparisonReport"
    - from: backend/shared/python/sunity_shared/analysis/copy_templates.py
      to: backend/shared/python/sunity_shared/analysis/skeleton.py
      via: "from .skeleton import JOINT_LABEL_KO — 8 관절 한국어 라벨 재사용"
      pattern: "from .skeleton import JOINT_LABEL_KO"
    - from: backend/tests/phase07/test_copy_templates_no_forbidden.py
      to: backend/shared/python/sunity_shared/analysis/copy_templates.py
      via: "AST 기반 _COPY_TEMPLATES + _MODE_PREFIX string constant iterate — FORBIDDEN tuple 자체는 검사 scope 밖"
      pattern: "FORBIDDEN_PHRASES"
---

<objective>
Phase 7 vertical slice (1/2) — **schema + canned copy + test scaffold**. 3-way contract lockstep 으로 BodyComparisonFinding 에 4 필드 (category / phase / body_type_interpretation / recommendation) + BodyComparisonReport 에 2 필드 (do_not_over_correct / recommended_focus) 를 단일 atomic commit 으로 박제하고, 신규 모듈 `copy_templates.py` 에 21 canned 카피 + 3 mode prefix + 9 금지 표현 grep gate (AST 기반) 를 박제한다. Plan 02 의 classify_findings + integration test 가 즉시 실행 가능한 Wave 0 test 인프라 (phase07/ 디렉토리 + 6 fixture JSON) 도 본 plan 에서 박제.

본 plan 산출 = (a) Python dataclass + TS interface + contract.md §8/§8.3 3-way lockstep + (b) copy_templates.py 신설 모듈 (정적 dict literal, LLM/network 호출 0) + (c) AST 기반 grep gate + render lookup test 2 종 + 3-way lockstep drift defense test 1 종 + (d) Wave 0 fixture 6 JSON + factory loader.

Purpose: PERS-01 Phase 7 SC #1 (category 분류 schema) + SC #2 (doNotOverCorrect / recommendedFocus 출력 schema) + SC #4 (금지 표현 grep gate) 의 schema/카피 layer 박제. Plan 02 의 분류 함수 + pipeline wiring 의 입력 contract 박제. per D-07-A1 + D-07-B1 + D-07-B2 + D-07-C1 + D-07-D1 + D-07-D2 + D-07-D3.

Output: 신설 모듈 1 + 5 파일 수정 (body_normalizer.py / models.py / analysis.ts / contract.md / phase07 test 디렉토리) + 신설 13 파일 (phase07/__init__.py + conftest.py + fixtures/__init__.py + _factory.py + 6 JSON + 3 test 파일). Task 단위 3 commit 권장 — Task 1 fixture infra / Task 2 copy_templates 모듈 + 2 test / Task 3 lockstep atomic.

**박제 메모 정합:** [[scoring-dimensions-ipsf]] (IPSF 기반 분류) + [[mode3-progress-not-similarity]] (FORBIDDEN_PHRASES_SUNITY 에 "%일치"/"유사도" 포함) + [[analysis-objectivity-no-human-scores]] (canned string 객관성) + [[feedback-analysis-first]] (가능성 언어 톤) + [[no-baekje-filler]] (canned 안 "박제" 단어 grep gate) + [[mvp-simple-pilot-quality]] (캔드 우선 — LLM 풍부화는 Phase 11) + [[feedback-no-echo-confirm]] (AI 보조 도구 톤) + [[ipsf-5-track-scoring]] (Page 9 절대 트랙 단독 path fallback 카피).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-difference-classification/07-CONTEXT.md
@.planning/phases/07-difference-classification/07-RESEARCH.md
@.planning/phases/07-difference-classification/07-PATTERNS.md
@.planning/phases/07-difference-classification/07-VALIDATION.md
@.planning/phases/06-coaching/06-CONTEXT.md
@.planning/phases/06-coaching/06-01-SUMMARY.md
@CLAUDE.md
@docs/contract.md
@backend/shared/python/sunity_shared/analysis/body_normalizer.py
@backend/shared/python/sunity_shared/analysis/skeleton.py
@backend/shared/python/sunity_shared/analysis/assemble.py
@backend/shared/python/sunity_shared/models.py
@app/src/types/analysis.ts
@backend/tests/phase06/conftest.py
@backend/tests/phase06/fixtures/_factory.py
@backend/tests/phase06/test_body_comparison_report_lockstep.py
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Phase 7 receives Phase 6 outputs only — no new user input crosses the boundary. canned string dict literal is static, secret-free. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | T (Tampering) | copy_templates._COPY_TEMPLATES dict literal | mitigate | Static Python dict literal — injection surface 0. Module import 1회 로드. 외부 input 없음. `dataclass __post_init__` Literal enum validator (BodyComparisonFinding.category) backstop. |
| T-07-02 | I (Information disclosure / 사용자 부정 경험) | canned string Korean copy 9 forbidden phrases | mitigate | AST 기반 grep gate parametrize test (test_copy_templates_no_forbidden.py) — 9 표현 (6 research §10.3 + 3 톤 룰) 검증. AST 로 `_COPY_TEMPLATES` + `_MODE_PREFIX` dict 의 string constant 만 iterate — FORBIDDEN tuple 자체는 검사 scope 밖. CI failure 시 위치 정확 지목. belle plan-review 검수 추가. |
| T-07-03 | T (Tampering / schema drift) | TS interface ↔ Python dataclass ↔ docs/contract.md §8 lockstep | mitigate | 3-way lockstep drift defense test (test_body_comparison_report_phase7_lockstep.py) — Phase 6 박제 패턴 (116f400/a444726) 정합, 부분 commit 시 test fail-fast. |
| T-07-04 | D (Denial of Service / Firestore nested-array crash) | BodyComparisonReport.do_not_over_correct + recommended_focus list[str] | accept | Phase 6 W5 `_validate_flat_dict_no_nested_array` 박제 — list[str] 자동 통과. 신규 코드 변경 0. Plan 02 integration test 가 backstop. |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | 신규 의존성 0 — Phase 6 환경 그대로 사용 (Python 3.12 stdlib dataclasses/typing + 기존 pytest >=8,<9). RESEARCH.md §Package Legitimacy Audit "N/A (no new packages)" 박제. slopcheck 검증 불요. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 test 인프라 (phase07/ 디렉토리 + 6 fixture JSON + factory loader)</name>
  <files>
    backend/tests/phase07/__init__.py
    backend/tests/phase07/conftest.py
    backend/tests/phase07/fixtures/__init__.py
    backend/tests/phase07/fixtures/_factory.py
    backend/tests/phase07/fixtures/fixture_classification_allowed.json
    backend/tests/phase07/fixtures/fixture_classification_needs.json
    backend/tests/phase07/fixtures/fixture_classification_uncertain_raw.json
    backend/tests/phase07/fixtures/fixture_classification_uncertain_low_conf.json
    backend/tests/phase07/fixtures/fixture_classification_uncertain_global_low.json
    backend/tests/phase07/fixtures/fixture_classification_mode3_first_fallback.json
  </files>
  <read_first>
    backend/tests/phase06/__init__.py
    backend/tests/phase06/conftest.py
    backend/tests/phase06/fixtures/_factory.py
    backend/tests/phase06/fixtures/fixture_160cm_pro_vs_140cm_student.json
    .planning/phases/07-difference-classification/07-RESEARCH.md (§"Test Fixtures" + §"Test Specifications" + §"Wave 0 Gaps")
    .planning/phases/07-difference-classification/07-PATTERNS.md (§"backend/tests/phase07/__init__.py + conftest.py" + §"_factory.py")
    .planning/phases/07-difference-classification/07-VALIDATION.md (§"Wave 0 Requirements")
  </read_first>
  <behavior>
    - 빈 `__init__.py` 2 개 (phase07/ + phase07/fixtures/)
    - `conftest.py` 가 `PHASE07_FIXTURES_DIR: Path = Path(__file__).resolve().parent / "fixtures"` 단일 상수 노출. Phase 6 conftest.py 문구 그대로 미러 (docstring + sys.path 자동 주입 X)
    - `fixtures/_factory.py` = `load_fixture_raw(name: str) -> dict[str, Any]` (Phase 6 미러) + `load_finding_from_fixture(name: str) -> tuple[BodyComparisonFinding, dict, dict]` 신설 (return: finding, context dict {body_normalization_confidence, comparison_type, used_reference_fallback}, expected dict)
    - 6 fixture JSON 박제 (RESEARCH.md §"Test Fixtures" 박제 그대로):
      1. `fixture_classification_allowed.json` — deficit_code=knee_toe_alignment, joint_key=left_knee, deduction_score=-0.2, confidence=0.85, body_type_adjusted=true, context.body_normalization_confidence=0.85, context.comparison_type=mode1, expected.category=body_type_allowed, expected.phase=hold, expected.do_not_over_correct_count=1, expected.recommended_focus_count=0
      2. `fixture_classification_needs.json` — deficit_code=pose_reliability_low, joint_key=null, deduction_score=-0.5, confidence=0.9, body_type_adjusted=true, context.body_normalization_confidence=0.85, expected.category=needs_adjustment, expected.recommended_focus_count=1, expected.do_not_over_correct_count=0
      3. `fixture_classification_uncertain_raw.json` — deficit_code=extension, joint_key=null, deduction_score=-0.2, confidence=0.85, body_type_adjusted=false, expected.category=uncertain
      4. `fixture_classification_uncertain_low_conf.json` — deficit_code=knee_toe_alignment, joint_key=left_knee, deduction_score=-0.2, confidence=0.3 (< 0.5), body_type_adjusted=true, expected.category=uncertain
      5. `fixture_classification_uncertain_global_low.json` — finding 자체는 valid (deduction=-0.2, confidence=0.85, body_type_adjusted=true), context.body_normalization_confidence=0.3 (< 0.5), expected.category=uncertain
      6. `fixture_classification_mode3_first_fallback.json` — finding valid (deduction=-0.2, confidence=0.85, body_type_adjusted=true), context.body_normalization_confidence=0.85, context.comparison_type=mode3_first, context.used_reference_fallback=true, expected.category=uncertain (Decision 1), expected.recommendation 본문 = "이 동작은 기준 영상이 없어, AI 가 IPSF 절대 기준만으로 분석했어요. 강사와 함께 확인 권유드려요." (RESEARCH.md Open Questions Q1 RESOLVED)
  </behavior>
  <action>
    Phase 6 backend/tests/phase06/conftest.py + fixtures/_factory.py 그대로 mirror — `PHASE07_FIXTURES_DIR` 상수 단일 노출, `load_fixture_raw` 동일 시그너처. `load_finding_from_fixture` 신규 헬퍼는 JSON dict 의 "finding" 키를 `BodyComparisonFinding(**finding_dict)` 로 변환 (Task 2 의 신설 4 필드는 default 값 활용). 6 fixture JSON 의 deduction_score 값 정확히 `-0.2` / `-0.5`, confidence 정확히 `0.85` / `0.3` / `0.9`, body_normalization_confidence 정확히 `0.85` / `0.3`. fixture 위치 = `backend/tests/phase07/fixtures/fixture_classification_*.json`. `__init__.py` 는 빈 파일 2 개 (디렉토리 marker). fixture #6 의 expected.recommendation = RESOLVED Q1 단일 fallback 카피 (위 `<behavior>` 박제).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && python -c "from pathlib import Path; import json; d = Path('backend/tests/phase07/fixtures'); assert (d / 'fixture_classification_allowed.json').exists() and json.loads((d / 'fixture_classification_allowed.json').read_text())['finding']['deduction_score'] == -0.2 and json.loads((d / 'fixture_classification_needs.json').read_text())['finding']['deduction_score'] == -0.5 and json.loads((d / 'fixture_classification_uncertain_raw.json').read_text())['finding']['body_type_adjusted'] is False and json.loads((d / 'fixture_classification_uncertain_low_conf.json').read_text())['finding']['confidence'] == 0.3 and json.loads((d / 'fixture_classification_uncertain_global_low.json').read_text())['context']['body_normalization_confidence'] == 0.3 and json.loads((d / 'fixture_classification_mode3_first_fallback.json').read_text())['context']['used_reference_fallback'] is True; print('FIXTURES_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `backend/tests/phase07/__init__.py` (empty), `conftest.py` (PHASE07_FIXTURES_DIR exported), `fixtures/__init__.py` (empty), `fixtures/_factory.py` (load_fixture_raw + load_finding_from_fixture defined)
    - All 6 fixture JSON files exist with field values matching `<behavior>` block exactly
    - Source: `grep -c "PHASE07_FIXTURES_DIR" backend/tests/phase07/conftest.py` >= 1
    - Source: `grep -c "def load_finding_from_fixture" backend/tests/phase07/fixtures/_factory.py` >= 1
    - Source: `python -c "import json; d = json.loads(open('backend/tests/phase07/fixtures/fixture_classification_mode3_first_fallback.json').read()); assert '기준 영상이 없어' in d['expected']['recommendation']; print('Q1_FALLBACK_OK')"` prints `Q1_FALLBACK_OK`
    - Behavior: bash verify command above prints `FIXTURES_OK` and exits 0
  </acceptance_criteria>
  <done>
    phase07/ 디렉토리 완료. 6 fixture JSON (RESEARCH.md §"Test Fixtures" 100% 정합). fixture #6 의 expected.recommendation 에 RESOLVED Q1 단일 fallback 카피 박제. _factory.py 의 load_finding_from_fixture 가 Task 2 schema 확장 후 BodyComparisonFinding 자동 변환. pytest collection 자동.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: copy_templates.py 신설 모듈 + 21 카피 + 3 mode prefix + AST 기반 grep gate test + render lookup test</name>
  <files>
    backend/shared/python/sunity_shared/analysis/copy_templates.py
    backend/tests/phase07/test_copy_templates_no_forbidden.py
    backend/tests/phase07/test_copy_templates_render.py
  </files>
  <read_first>
    backend/shared/python/sunity_shared/analysis/skeleton.py
    backend/shared/python/sunity_shared/analysis/assemble.py
    .planning/phases/07-difference-classification/07-RESEARCH.md (§"Canned Copy Mapping Table" + §"Initial Canned String Draft" + §Code Examples Example 3 + §Open Questions Q1 RESOLVED)
    .planning/phases/07-difference-classification/07-PATTERNS.md (§"copy_templates.py (NEW)" + §"test_copy_templates_no_forbidden.py" + §"test_copy_templates_render.py")
    .planning/phases/07-difference-classification/07-CONTEXT.md (§Decisions B + D)
    backend/tests/phase06/test_body_normalizer_ipsf_deficit.py (단위 test 패턴)
  </read_first>
  <behavior>
    - Test 1 (`test_copy_templates_no_forbidden.py`): AST 기반 — `_COPY_TEMPLATES` + `_MODE_PREFIX` dict literal 의 string constant 만 iterate. pytest parametrize over `FORBIDDEN_PHRASES + FORBIDDEN_PHRASES_SUNITY` (9 phrases). 각 phrase 가 어디에도 발견되지 않음 — 0 violations PASS. 위반 시 assert message 는 위반 string + 위반 phrase 정확 지목. FORBIDDEN tuple 자체는 검사 scope 밖 (W4 self-violation 회피).
    - Test 2 (`test_copy_templates_render.py::test_renders_21_canned_keys`): `_COPY_TEMPLATES.keys()` 전체 (21 항) iterate 시 `render_finding_copy(deficit_code, category, joint_group, comparison_type='mode1')` 호출 모두 정상 반환 (KeyError 없음, fallback 트리거 0 회)
    - Test 3 (`test_copy_templates_render.py::test_mode_prefix_prepended_to_recommendation_only`): 동일 (deficit_code, category, joint_group) 입력 + comparison_type=mode1 vs mode3_first vs mode3_progress 호출 시 `recommendation` 출력만 prefix 다름 (interpretation 동일)
    - Test 4 (`test_copy_templates_render.py::test_fallback_on_missing_key`): 없는 key 호출 시 graceful tuple 반환 (`"이 부분은 AI 분석 결과예요."`, `"강사와 함께 영상을 한 번 더 확인해 보세요."`) + caplog 에 WARNING 캡처
    - Test 5 (`test_copy_templates_render.py::test_imports_joint_label_ko_from_skeleton`): `copy_templates` 모듈이 `JOINT_LABEL_KO` 를 `from .skeleton import` 로 가져옴 (재정의 X)
    - Test 6 (`test_copy_templates_render.py::test_mode3_first_fallback_recommendation`): `render_finding_copy(deficit_code, 'uncertain', joint_group, comparison_type='mode3_first')` 호출 시 mode3_first prefix `"세계 심사 기준 (IPSF) 으로 보면"` 시작. (mode3_first + used_reference_fallback 결합 시의 단일 fallback 카피 검증은 Plan 02 의 classify_findings test 가 담당. 본 test 는 mode3_first prefix 자체 박제 확인.)
  </behavior>
  <action>
    `backend/shared/python/sunity_shared/analysis/copy_templates.py` 신설. 모듈 상단에 `from __future__ import annotations` 박제 (W3 fix — Phase 6 모듈 일관성 정합). 모듈 docstring 에 `research §10.1 권장 카피 4종 톤 정합 + §10.3 금지 6종 grep gate. Sunity 추가 톤 룰 3종 (가능성 언어 / AI 보조 도구 톤 / 부위별 원인 언어).` 박제 (CLAUDE.md Cross-cutting `§` shorthand 정합).

    시그너처:
      - `Category = Literal["body_type_allowed", "needs_adjustment", "uncertain"]`
      - `JointGroup = Literal["arm", "leg", "torso", "pole_axis", "global"]`
      - `ComparisonType = Literal["mode1", "mode3_first", "mode3_progress"]`
      - `_MODE_PREFIX: dict[ComparisonType, str]` 3 항 — RESEARCH.md §"Initial Canned String Draft" line 437-441 그대로 ("정은지 선수 영상 기준으로 보면", "세계 심사 기준 (IPSF) 으로 보면", "이전 영상 대비")
      - `_COPY_TEMPLATES: dict[tuple[str, Category, JointGroup], tuple[str, str]]` 21 항 — RESEARCH.md §"Initial Canned String Draft" line 445-545 그대로 (7 deficit_code × 3 category = 21)
      - `FORBIDDEN_PHRASES: tuple[str, ...]` 6 항 — RESEARCH.md line 549-556 ("프로보다 못합니다", "정답 자세가 아닙니다", "근육량이 부족합니다", "체형이 안 맞습니다", "대회 총점", "감점입니다")
      - `FORBIDDEN_PHRASES_SUNITY: tuple[str, ...]` 3 항 ("박제" per [[no-baekje-filler]], "%일치" per [[mode3-progress-not-similarity]], "유사도" per [[mode3-progress-not-similarity]])
      - `from .skeleton import JOINT_LABEL_KO` (skeleton 8 관절 한국어 라벨 재사용)
      - `def render_finding_copy(deficit_code: str, category: Category, joint_group: JointGroup, comparison_type: ComparisonType) -> tuple[str, str]:` — `_COPY_TEMPLATES.get((deficit_code, category, joint_group))` lookup, 미발견 시 graceful fallback tuple `("이 부분은 AI 분석 결과예요.", "강사와 함께 영상을 한 번 더 확인해 보세요.")` + `logging.warning(...)`. 발견 시 `_MODE_PREFIX[comparison_type]` 가 빈 문자열이 아니고 recom 이 prefix 로 시작하지 않을 때만 `f"{prefix} {recom}"` 으로 prepend (interpretation 은 무수정, mode 무관 per D-07-D3).

    `test_copy_templates_no_forbidden.py` — W4 fix: AST 기반 검증. acceptance_criteria 의 AST 명령을 본 test 모듈의 helper 로 박제. 구현:

      ```
      import ast
      from pathlib import Path
      import pytest
      from sunity_shared.analysis.copy_templates import FORBIDDEN_PHRASES, FORBIDDEN_PHRASES_SUNITY

      MODULE_PATH = Path(__file__).resolve().parents[2] / "backend" / "shared" / "python" / "sunity_shared" / "analysis" / "copy_templates.py"

      def _extract_template_strings() -> list[tuple[str, int]]:
          src = MODULE_PATH.read_text()
          tree = ast.parse(src)
          strings: list[tuple[str, int]] = []
          for node in ast.walk(tree):
              if isinstance(node, ast.Assign) and len(node.targets) == 1 and isinstance(node.targets[0], ast.Name) and node.targets[0].id in ("_COPY_TEMPLATES", "_MODE_PREFIX"):
                  for sub in ast.walk(node):
                      if isinstance(sub, ast.Constant) and isinstance(sub.value, str):
                          strings.append((sub.value, sub.lineno))
          return strings

      @pytest.mark.parametrize("phrase", FORBIDDEN_PHRASES + FORBIDDEN_PHRASES_SUNITY)
      def test_no_forbidden_phrase_in_copy_templates(phrase):
          strings = _extract_template_strings()
          violations = [(s, ln) for s, ln in strings if phrase in s]
          assert not violations, f"금지 표현 {phrase!r} 발견: {violations}"
      ```

    `test_copy_templates_render.py` = 21 카피 keys() iterate parametrize + 3 mode prefix prepend test + fallback test + JOINT_LABEL_KO import 검증 + mode3_first prefix 박제 확인.

    카피 안 이모지 금지 (CLAUDE.md §7). per [[no-baekje-filler]] — 카피 + 모듈 docstring 안 "박제" 단어 사용 X (FORBIDDEN_PHRASES_SUNITY 의 첫 항이 grep target).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && pytest backend/tests/phase07/test_copy_templates_no_forbidden.py backend/tests/phase07/test_copy_templates_render.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `backend/shared/python/sunity_shared/analysis/copy_templates.py`
    - Source: `head -10 backend/shared/python/sunity_shared/analysis/copy_templates.py | grep -c "from __future__ import annotations"` >= 1 (W3 fix)
    - Source: `python -c "from sunity_shared.analysis.copy_templates import _COPY_TEMPLATES, _MODE_PREFIX, FORBIDDEN_PHRASES, FORBIDDEN_PHRASES_SUNITY, render_finding_copy; assert len(_COPY_TEMPLATES) == 21; assert len(_MODE_PREFIX) == 3; assert len(FORBIDDEN_PHRASES) == 6; assert len(FORBIDDEN_PHRASES_SUNITY) == 3; print('OK')"` prints `OK`
    - Source (AST 기반 W4 fix — FORBIDDEN_PHRASES tuple 자체는 검사 scope 밖):
      `python -c "import ast, pathlib; src = pathlib.Path('backend/shared/python/sunity_shared/analysis/copy_templates.py').read_text(); tree = ast.parse(src); dicts = [n for n in ast.walk(tree) if isinstance(n, ast.Assign) and len(n.targets) == 1 and isinstance(n.targets[0], ast.Name) and n.targets[0].id in ('_COPY_TEMPLATES', '_MODE_PREFIX')]; strings = []; [strings.extend([s.value for s in ast.walk(d) if isinstance(s, ast.Constant) and isinstance(s.value, str)]) for d in dicts]; forbidden = ['프로보다 못합', '정답 자세가 아닙', '근육량이 부족', '체형이 안 맞', '대회 총점', '감점입니다', '%일치', '유사도']; violations = [(p, s) for p in forbidden for s in strings if p in s]; assert not violations, violations; print(f'NO_FORBIDDEN_OK strings_checked={len(strings)}')"`
      prints `NO_FORBIDDEN_OK strings_checked=N` (N >= 42 — 21 항 × 2 (interp + recom) + 3 mode prefix)
    - Source: `grep -c "from .skeleton import JOINT_LABEL_KO" backend/shared/python/sunity_shared/analysis/copy_templates.py` >= 1
    - Behavior: `pytest backend/tests/phase07/test_copy_templates_no_forbidden.py -x` PASSES 9 parametrize cases (6 research + 3 톤 룰)
    - Behavior: `pytest backend/tests/phase07/test_copy_templates_render.py -x` PASSES — 21 카피 lookup + 3 mode prefix + fallback + JOINT_LABEL_KO import + mode3_first prefix 검증 모두 green
  </acceptance_criteria>
  <done>
    copy_templates.py 신설 완료 — `from __future__ import annotations` 헤더 + 21 canned + 3 prefix + 9 grep gate (AST 기반) + render_finding_copy fallback graceful. 단위 test 2 종 (no_forbidden + render) 모두 green. W3 (from __future__) + W4 (AST grep gate) + Q1 (mode3_first prefix) 박제. CLAUDE.md §7 (이모지 0) + [[no-baekje-filler]] (FORBIDDEN_PHRASES_SUNITY 첫 항).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 3-way contract lockstep — 4 + 2 필드 + docs §8/§8.3 + drift defense test (SINGLE ATOMIC COMMIT)</name>
  <files>
    backend/shared/python/sunity_shared/analysis/body_normalizer.py
    backend/shared/python/sunity_shared/models.py
    app/src/types/analysis.ts
    docs/contract.md
    backend/tests/phase07/test_body_comparison_report_phase7_lockstep.py
  </files>
  <read_first>
    backend/shared/python/sunity_shared/analysis/body_normalizer.py (line 787-895 BodyComparisonFinding + BodyComparisonReport 본체 + line 945/977/1027/1056/1076/1102 measure_ipsf_absolute_deficits 안 6 BodyComparisonFinding 호출 위치)
    backend/shared/python/sunity_shared/models.py (line 125-150 re-export 헤더)
    app/src/types/analysis.ts (line 430-560 BodyComparisonReport family)
    docs/contract.md (§8 + §8.1 + §8.2 Phase 6 박제 표)
    backend/tests/phase06/test_body_comparison_report_lockstep.py (3-way regex grep 패턴)
    .planning/phases/07-difference-classification/07-RESEARCH.md (§"Schema Extension" line 612-705)
    .planning/phases/07-difference-classification/07-PATTERNS.md (§"body_normalizer.py" + §"analysis.ts" + §"contract.md" + §"test_body_comparison_report_phase7_lockstep.py")
    .planning/phases/07-difference-classification/07-CONTEXT.md (§Decisions A + B + C)
  </read_first>
  <behavior>
    - Python `BodyComparisonFinding` (frozen dataclass) 4 신설 필드: `category: Literal["body_type_allowed", "needs_adjustment", "uncertain"]` (default 없음 — required) + `phase: str | None = "hold"` + `body_type_interpretation: str | None = None` + `recommendation: str | None = None`. `__post_init__` 가 category 값을 3 enum 외 거부 (ValueError, 메시지 `category must be one of (body_type_allowed, needs_adjustment, uncertain), got {self.category!r}`)
    - Python `BodyComparisonReport` 2 신설 필드: `do_not_over_correct: list[str] = field(default_factory=list)` + `recommended_focus: list[str] = field(default_factory=list)` (Phase 6 `warnings` 의 `field(default_factory=list)` 패턴 정합)
    - 모듈 상수: `CATEGORY_GATE = 0.2` (D-07-A1) + `CATEGORY_CONF_GATE = 0.5` (D-07-A2 — D-07-U1 단일 게이트 의도 명확화)
    - TS `BodyComparisonFinding` 4 신설 필드: `category: 'body_type_allowed' | 'needs_adjustment' | 'uncertain';` (non-optional) + `phase?: string | null;` + `bodyTypeInterpretation?: string | null;` + `recommendation?: string | null;`
    - TS `BodyComparisonReport` 2 신설 필드: `doNotOverCorrect: string[];` (non-optional) + `recommendedFocus: string[];` (non-optional)
    - `docs/contract.md §8` BodyComparisonFinding 표 4 행 + BodyComparisonReport 표 2 행 추가. `§8.3 Phase 7 분류 룰` 서브섹션 신규 — D-07-A1 + D-07-A2 + D-07-U1 + 21 canned coverage 표
    - **measure_ipsf_absolute_deficits 의 6 BodyComparisonFinding 호출 위치 (line 945 / 977 / 1027 / 1056 / 1076 / 1102) 모두 `category="body_type_allowed"` placeholder 추가** — Plan 02 classify_findings 가 재할당. 단일 atomic commit (W1 fix) 박제 강제.
    - 3-way drift defense test (`test_body_comparison_report_phase7_lockstep.py`) — Phase 6 `test_body_comparison_report_lockstep.py` 미러, `_FIELD_MAP` 에 6 신설 필드 (4 finding + 2 report) 추가, 3 위치 (TS/Python/contract.md) regex grep 모두 발견 시 PASS
  </behavior>
  <action>
    **W1 SINGLE ATOMIC COMMIT 강제** — Phase 6 commit `116f400` / `a444726` 패턴 정합. 부분 commit 금지. Task 3 의 모든 변경 (BodyComparisonFinding 4 필드 확장 + measure_ipsf_absolute_deficits 의 6 호출 위치 placeholder 동시 박제 + TS interface + contract.md + lockstep test) 은 **단일 atomic commit** 박제. 부분 commit 시 Phase 6 회귀 발생 (BodyComparisonFinding 의 category 가 required 인데 placeholder 박제 안 되면 measure_ipsf_absolute_deficits 호출 모두 TypeError).

    `body_normalizer.py` 수정:
      - BodyComparisonFinding 4 필드 추가 (위 `<behavior>` 정합)
      - `__post_init__` 에 category Literal enum 검증 분기 (`if self.category not in ("body_type_allowed", "needs_adjustment", "uncertain"): raise ValueError(...)`)
      - BodyComparisonReport 2 필드 추가 (`do_not_over_correct: list[str] = field(default_factory=list)` + `recommended_focus: list[str] = field(default_factory=list)`)
      - 모듈 상수 `CATEGORY_GATE = 0.2` + `CATEGORY_CONF_GATE = 0.5` 추가
      - **measure_ipsf_absolute_deficits 의 6 BodyComparisonFinding(...) 호출 위치 (line 945 / 977 / 1027 / 1056 / 1076 / 1102) 에 `category="body_type_allowed"` 추가** — Phase 6 회귀 0 보장. Plan 02 의 classify_findings 가 재할당. **본 변경은 Task 3 의 atomic commit 안에 포함 — 부분 commit 금지.**

    `models.py` 무수정 (re-export 자동) — header 주석에 Phase 7 행만 추가 (`# Phase 7 (2026-06-08) +4 필드 (category/phase/body_type_interpretation/recommendation) +2 필드 (do_not_over_correct/recommended_focus).`).

    `app/src/types/analysis.ts` (line 490-507 BodyComparisonFinding + line 536-550 BodyComparisonReport) 수정 — 4 + 2 필드 박제 (JSDoc 주석에 D-07-XX 인용).

    `docs/contract.md §8` BodyComparisonFinding 표 4 행 + BodyComparisonReport 표 2 행. `§8.3 Phase 7 분류 룰` 신규 서브섹션 — D-07-A1 + D-07-A2 + D-07-U1 + 21 canned coverage 표 (RESEARCH.md §"Canned String Coverage Audit" 박제 그대로).

    `test_body_comparison_report_phase7_lockstep.py` 신설 — Phase 6 `test_body_comparison_report_lockstep.py` line 1-50 미러, _FIELD_MAP 에 6 신설 필드 추가. 3 위치 (TS/Python/contract.md) regex grep 모두 발견 시 PASS, drift 시 누락 위치 정확히 assert 메시지 박제.

    per D-07-B3 / D-07-C1 / D-07-A1 / D-07-A2 / D-07-U1 — **W1 SINGLE ATOMIC COMMIT**.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && pytest backend/tests/phase07/test_body_comparison_report_phase7_lockstep.py -x -v && pytest backend/tests/phase06/ -x && cd app && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "category: Literal\[\"body_type_allowed\", \"needs_adjustment\", \"uncertain\"\]" backend/shared/python/sunity_shared/analysis/body_normalizer.py` >= 1
    - Source: `grep -c "phase: str | None" backend/shared/python/sunity_shared/analysis/body_normalizer.py` >= 1
    - Source: `grep -c "body_type_interpretation: str | None" backend/shared/python/sunity_shared/analysis/body_normalizer.py` >= 1
    - Source: `grep -c "recommendation: str | None" backend/shared/python/sunity_shared/analysis/body_normalizer.py` >= 1
    - Source: `grep -c "do_not_over_correct: list\[str\]" backend/shared/python/sunity_shared/analysis/body_normalizer.py` >= 1
    - Source: `grep -c "recommended_focus: list\[str\]" backend/shared/python/sunity_shared/analysis/body_normalizer.py` >= 1
    - Source: `grep -c "CATEGORY_GATE = 0.2" backend/shared/python/sunity_shared/analysis/body_normalizer.py` >= 1
    - Source: `grep -c "CATEGORY_CONF_GATE = 0.5" backend/shared/python/sunity_shared/analysis/body_normalizer.py` >= 1
    - Source (W1 placeholder 6 위치): `grep -c 'category="body_type_allowed"' backend/shared/python/sunity_shared/analysis/body_normalizer.py` >= 6
    - Source: `grep -c "category: 'body_type_allowed' | 'needs_adjustment' | 'uncertain'" app/src/types/analysis.ts` >= 1
    - Source: `grep -c "doNotOverCorrect: string\[\]" app/src/types/analysis.ts` >= 1
    - Source: `grep -c "recommendedFocus: string\[\]" app/src/types/analysis.ts` >= 1
    - Source: `grep -c "§8.3" docs/contract.md` >= 1
    - Source: `grep -c "doNotOverCorrect" docs/contract.md` >= 1
    - Behavior: `python -c "from sunity_shared.analysis.body_normalizer import BodyComparisonFinding; raised=False;\ntry:\n  BodyComparisonFinding(deficit_code='x', joint_key=None, measured_value=0.0, deduction_score=-0.2, confidence=0.85, body_type_adjusted=True, category='bogus')\nexcept ValueError: raised=True\nassert raised, 'category enum validator must raise'; print('VALIDATOR_OK')"` prints `VALIDATOR_OK`
    - Behavior: `pytest backend/tests/phase07/test_body_comparison_report_phase7_lockstep.py -x` PASS (6 신설 필드 + 기존 Phase 6 필드 drift 검증)
    - Behavior: `cd app && npx tsc --noEmit` exits 0 (TS interface 확장 후 type-check clean)
    - Behavior: `pytest backend/tests/phase06/ -x` PASS — W1 atomic commit 박제로 Phase 6 회귀 0
    - Source (W1 atomic commit 검증 — 본 task 의 commit log 확인 시): commit 메시지에 `Phase 7 lockstep` + `placeholder 6 위치` 동시 언급, `git show --stat HEAD` 가 위 5 파일 동시 변경 출력
  </acceptance_criteria>
  <done>
    W1 SINGLE ATOMIC COMMIT 완료 — BodyComparisonFinding 4 필드 + BodyComparisonReport 2 필드 + docs §8/§8.3 + measure_ipsf_absolute_deficits 의 6 호출 placeholder 동시 박제. Phase 6 commit 패턴 (116f400/a444726) 정합. Phase 6 회귀 0. TS tsc --noEmit clean. 3-way drift defense test PASS.
  </done>
</task>

</tasks>

<verification>
- `pytest backend/tests/phase07/ -x` PASS (test_copy_templates_no_forbidden.py + test_copy_templates_render.py + test_body_comparison_report_phase7_lockstep.py 3 test 파일 모두 green)
- `pytest backend/tests/phase06/ -x` PASS (W1 atomic commit 박제로 Phase 6 회귀 0)
- `pytest backend/tests/ -x` PASS (전체 backend suite green)
- `cd app && npx tsc --noEmit` exits 0
- AST 기반 grep gate (W4 fix): `python -c "import ast, pathlib; ..."` AST 명령 (Task 2 acceptance_criteria 박제) 가 NO_FORBIDDEN_OK 출력 — FORBIDDEN tuple 자체는 검사 scope 밖
- 3-way drift defense — `_FIELD_MAP` 의 6 신설 필드가 TS + Python + contract.md 모두에서 grep 통과
- W1 SINGLE ATOMIC COMMIT — Task 3 의 5 파일 + measure_ipsf_absolute_deficits 의 6 placeholder 가 단일 commit (Task 1/2 와는 별 commit OK — Task 1 fixture infra + Task 2 copy_templates 모듈 / test 2 종 = 별 commit 권장. 총 3 commit).
- W3 `from __future__ import annotations` — Task 2 의 copy_templates.py 모듈 헤더 박제
- per CLAUDE.md §7 — 이모지 0, 슬롭 코드 0
- per [[no-baekje-filler]] — canned string 안 "박제" 단어 0 (FORBIDDEN_PHRASES_SUNITY 첫 항 AST grep gate)
- per [[mode3-progress-not-similarity]] — canned string 안 "%일치" / "유사도" 0
</verification>

<success_criteria>
1. **schema lockstep PASS (W1 atomic)**: BodyComparisonFinding 4 신설 필드 + BodyComparisonReport 2 신설 필드 + TS / contract.md 3-way 동시 갱신 + measure_ipsf_absolute_deficits 의 6 호출 placeholder 동시 박제 (단일 atomic commit) → `test_body_comparison_report_phase7_lockstep.py` PASS + `tsc --noEmit` clean + `pytest backend/tests/phase06/ -x` PASS
2. **canned copy mapping PASS (W3 + Q1)**: copy_templates.py 신설 모듈 + `from __future__ import annotations` 헤더 + 21 카피 + 3 mode prefix + render_finding_copy fallback graceful + mode3_first prefix `"세계 심사 기준 (IPSF) 으로 보면"` 박제 → `test_copy_templates_render.py` 모든 test PASS (6 test — 21 lookup + 3 prefix + fallback + mode-invariant interp + JOINT_LABEL_KO import + mode3_first prefix 검증)
3. **AST grep gate PASS (W4)**: 9 금지 표현 (6 research §10.3 + 3 톤 룰) AST 기반 parametrize. `_COPY_TEMPLATES` + `_MODE_PREFIX` 의 string constant 만 iterate (FORBIDDEN tuple 자체는 검사 scope 밖) → `test_copy_templates_no_forbidden.py` 9 parametrize cases all PASS
4. **Wave 0 fixture + Q1 fallback 박제**: phase07/__init__.py + conftest.py + fixtures/__init__.py + _factory.py + 6 fixture JSON (allowed / needs / uncertain_raw / uncertain_low_conf / uncertain_global_low / mode3_first_fallback) 모두 박제 — fixture #6 의 expected.recommendation 에 RESOLVED Q1 fallback 카피 inline
5. **Phase 6 회귀 0 (W1)**: BodyComparisonFinding 의 6 호출 위치 (measure_ipsf_absolute_deficits 안) 에 category="body_type_allowed" placeholder 동시 박제 → `pytest backend/tests/phase06/ -x` PASS. W1 atomic commit 강제.
6. **백엔드 전체 PASS**: `pytest backend/tests/ -x` (phase06 136 + phase07 ~15) all green
7. **박제 메모 정합**: [[no-baekje-filler]] (canned 안 "박제" 0) + [[mode3-progress-not-similarity]] (canned 안 "%일치"/"유사도" 0) + [[analysis-objectivity-no-human-scores]] (canned 객관성) + [[feedback-analysis-first]] (가능성 언어 톤) + [[ipsf-5-track-scoring]] (mode3_first Page 9 단독 path fallback) — 모두 AST grep gate / 단위 test 로 backstop
</success_criteria>

<output>
Create `.planning/phases/07-difference-classification/07-01-SUMMARY.md` upon completion. Include:
- 4 + 2 신설 필드 박제 위치 (body_normalizer.py 줄 번호 + analysis.ts 줄 번호 + contract.md §8 + §8.3 위치)
- 21 canned + 3 mode prefix coverage 확인
- 9 AST grep gate test 통과 결과
- Phase 6 회귀 0 확인 (BodyComparisonFinding placeholder 6 위치 박제 — W1 atomic commit hash 명시)
- W1 SINGLE ATOMIC commit hash (Task 3) — Phase 6 116f400 / a444726 패턴 정합 확인
- Wave 0 fixture 6 JSON + factory loader 박제 + fixture #6 의 RESOLVED Q1 fallback 카피 박제 보고
</output>
