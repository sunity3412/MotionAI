---
phase: 07-difference-classification
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - backend/shared/python/sunity_shared/analysis/body_normalizer.py
  - app/src/lib/userAnalyses.ts
  - backend/tests/phase07/test_classify_findings.py
  - backend/tests/phase07/test_compare_body_profiles_phase7_integration.py
  - backend/tests/phase07/test_dataclass_to_camel_case_dict_phase7.py
  - backend/tests/phase07/test_classify_findings_preserves_measurement_fields.py
autonomous: true
requirements: [PERS-01]
tags: [classify-findings, pipeline-wiring, integration, camel-case, phase-07, frontend-normalize]
revision_notes:
  iteration: 2
  source: 07-REVIEW.md (cross-AI plan-risk review, 2026-06-08)
  fixes_in_this_plan:
    - CR-01 — classify_findings() 가 used_reference_fallback 케이스에서 render_finding_copy(..., used_reference_fallback=True) 호출 + recommendation 단일 카피 + body_type_interpretation=None 박제. fixture #6 expected.recommendation exact match assertion.
    - WR-02 — **iteration 1 의 B1 fix (Plan 02 Task 3 삭제) 결정 retract**. Plan 02 Task 3 재추가 — frontend `userAnalyses.normalize()` mini-fix (4 필드 null-guard, ~6 줄). Old Firestore doc 호환성 박제.
    - WR-03 — classify_findings() 가 recommended_focus[] 빈 list 일 때 BodyComparisonReport.recommended_focus_fallback 필드에 _EMPTY_FOCUS_FALLBACK 단일 카피 박제.
    - INF-01 — 신규 test `test_classify_findings_preserves_measurement_fields.py` 추가 (behavioral primary safety property — 6 원본 측정 필드 정확 보존). AST grep (`dataclasses.replace` 부재) 은 secondary convention test 유지.

must_haves:
  truths:
    - "`classify_findings(findings, body_normalization_confidence, comparison_type, *, used_reference_fallback=False) -> tuple[list[BodyComparisonFinding], list[str], list[str], str | None]` 박제 (return tuple 4-항 — WR-03 fix: 4th element = recommended_focus_fallback). 순수 함수 (numpy / boto3 / network / LLM 무관) — per D-07-A1"
    - "분류 룰: body_type_adjusted=True + |deduction_score| <= 0.2 → body_type_allowed / body_type_adjusted=True + |deduction_score| > 0.2 → needs_adjustment / body_type_adjusted=False → uncertain — per D-07-A1"
    - "confidence-tiered demotion (OR 합집합): body_normalization_confidence < 0.5 → 모든 finding uncertain demotion + finding.confidence < 0.5 → 개별 uncertain + comparison_type='mode3_first' AND used_reference_fallback=True → 모든 finding uncertain demotion (Decision 1) — per D-07-A2 + D-07-U1"
    - "**CR-01 fix — used_reference_fallback=True 케이스에서 classify_findings 가 `render_finding_copy(..., used_reference_fallback=True)` 호출** → recommendation = unprefixed 단일 카피 (\"이 동작은 기준 영상이 없어, AI 가 IPSF 절대 기준만으로 분석했어요. 강사와 함께 확인 권유드려요.\") + body_type_interpretation = None. mode prefix 결합 X."
    - "**WR-03 fix — classify_findings 가 recommended_focus[] 빈 list 일 때 `recommended_focus_fallback = _EMPTY_FOCUS_FALLBACK` 박제** (copy_templates.py 상수 import). 빈 list[] 가 아닐 때는 `recommended_focus_fallback = None`."
    - "classify_findings 가 새 BodyComparisonFinding 인스턴스 생성 (dataclasses.replace 우회 X) — frozen dataclass __post_init__ validator 통과. AST grep (secondary convention) + behavioral preserve fields test (primary INF-01 fix)"
    - "**INF-01 fix — `test_classify_findings_preserves_measurement_fields.py` 신설** — behavioral primary safety property. 6 원본 측정 필드 (deficit_code/joint_key/measured_value/deduction_score/confidence/body_type_adjusted) 가 input → output 정확 일치 + 신규 4 필드 (category/phase/body_type_interpretation/recommendation) 만 새로 박제 검증"
    - "분배 룰: category='body_type_allowed' → do_not_over_correct[].append, category='needs_adjustment' OR 'uncertain' → recommended_focus[].append — per D-07-B3 + Decision 1"
    - "compare_body_profiles() 가 measure_ipsf_absolute_deficits() 호출 직후 classify_findings() 1줄 wiring + BodyComparisonReport 조립 시 findings=classified_findings + do_not_over_correct + recommended_focus + recommended_focus_fallback 4 kwarg 주입"
    - "module-level mapping dicts 박제 순서: `_DEFICIT_TO_GROUP` → `_JOINT_TO_GROUP` → `_resolve_joint_group` → `classify_findings`"
    - "_dataclass_to_camel_case_dict (Phase 6 C8) 가 신설 4+3 필드 자동 camelCase 변환 — do_not_over_correct → doNotOverCorrect / recommended_focus → recommendedFocus / recommended_focus_fallback → recommendedFocusFallback / body_type_interpretation → bodyTypeInterpretation. 코드 변경 0"
    - "Phase 6 fixture (fixture_160cm_pro_vs_140cm_student.json) 재사용 integration test 가 compare_body_profiles() 호출 후 BodyComparisonReport.findings[*].category 모두 valid enum + 두 list populated 또는 빈 list[] 정상 검증"
    - "**WR-02 fix — frontend `userAnalyses.normalize()` mini-fix** — `bodyComparisonReport` 존재 시 4 필드 null-guard 박제 (~6 줄). Old Firestore doc 호환성. iteration 1 의 B1 결정 (Plan 02 Task 3 삭제) retract."
  artifacts:
    - path: backend/shared/python/sunity_shared/analysis/body_normalizer.py
      provides: "classify_findings() pure function (D-07-A1 + D-07-A2 + Decision 1 + CR-01 + WR-03 fix) + _DEFICIT_TO_GROUP + _JOINT_TO_GROUP + _resolve_joint_group() + compare_body_profiles() wiring + BodyComparisonReport 조립 시 4 kwarg (findings/do_not_over_correct/recommended_focus/recommended_focus_fallback)"
      contains:
        - "def classify_findings("
        - "def _resolve_joint_group("
        - "_DEFICIT_TO_GROUP"
        - "_JOINT_TO_GROUP"
        - "from .copy_templates import render_finding_copy, _EMPTY_FOCUS_FALLBACK"
        - "do_not_over_correct=do_not_over_correct"
        - "recommended_focus=recommended_focus"
        - "recommended_focus_fallback=recommended_focus_fallback"
    - path: app/src/lib/userAnalyses.ts
      provides: "WR-02 fix — normalize() 안 bodyComparisonReport 4 필드 null-guard. iteration 1 의 B1 retract."
      contains:
        - "bodyComparisonReport"
        - "doNotOverCorrect"
        - "recommendedFocus"
    - path: backend/tests/phase07/test_classify_findings.py
      provides: "9 단위 test — classify_findings 분류 룰 (allowed/needs/uncertain×3 + phase default + aggregate + AST replace + CR-01 fixture #6 exact + WR-03 fallback)"
      contains:
        - "def test_allowed_when_adjusted_and_small_deduction"
        - "def test_needs_adjustment_when_large_deduction"
        - "def test_uncertain_when_raw_coords"
        - "def test_uncertain_when_finding_low_confidence"
        - "def test_uncertain_when_report_low_confidence"
        - "def test_uncertain_when_mode3_first_fallback"
        - "def test_phase_default_hold"
        - "def test_aggregate_lists_populated"
        - "def test_classify_findings_uses_replace_pattern_zero"
        - "def test_recommended_focus_fallback_populated_when_empty"
    - path: backend/tests/phase07/test_classify_findings_preserves_measurement_fields.py
      provides: "INF-01 fix — behavioral primary safety property. 6 원본 측정 필드 input → output 정확 일치 검증"
      contains:
        - "def test_classify_findings_preserves_measurement_fields"
    - path: backend/tests/phase07/test_compare_body_profiles_phase7_integration.py
      provides: "Phase 6 fixture 재사용 integration — compare_body_profiles() 호출 후 BodyComparisonReport.findings[*].category enum valid + 두 list + recommended_focus_fallback (WR-03)"
      contains:
        - "def test_compare_body_profiles_emits_classified_findings"
        - "def test_compare_body_profiles_emits_do_not_over_correct"
        - "def test_compare_body_profiles_emits_recommended_focus"
        - "def test_compare_body_profiles_emits_recommended_focus_fallback_when_empty"
    - path: backend/tests/phase07/test_dataclass_to_camel_case_dict_phase7.py
      provides: "신설 4+3 필드 자동 camelCase 변환 회귀 차단 (Phase 6 C8 helper)"
      contains:
        - "def test_phase7_new_fields_camel_case_automatic"
        - "doNotOverCorrect"
        - "recommendedFocus"
        - "recommendedFocusFallback"
        - "bodyTypeInterpretation"
  key_links:
    - from: backend/shared/python/sunity_shared/analysis/body_normalizer.py
      to: backend/shared/python/sunity_shared/analysis/copy_templates.py
      via: "from .copy_templates import render_finding_copy, _EMPTY_FOCUS_FALLBACK — per-finding canned lookup + 빈 focus fallback 카피"
      pattern: "render_finding_copy|_EMPTY_FOCUS_FALLBACK"
    - from: backend/shared/python/sunity_shared/analysis/body_normalizer.py (compare_body_profiles)
      to: backend/shared/python/sunity_shared/analysis/body_normalizer.py (classify_findings)
      via: "measure_ipsf_absolute_deficits() 직후 1줄 wiring + BodyComparisonReport 조립 시 4 kwarg"
      pattern: "classified_findings, do_not_over_correct, recommended_focus, recommended_focus_fallback = classify_findings\\("
    - from: backend/functions/pipeline/app.py (_dataclass_to_camel_case_dict)
      to: backend/shared/python/sunity_shared/analysis/body_normalizer.py (BodyComparisonReport)
      via: "Phase 6 C8 5-case helper 자동 변환 — do_not_over_correct → doNotOverCorrect 등 신설 7 필드"
      pattern: "_dataclass_to_camel_case_dict"
    - from: app/src/lib/userAnalyses.ts (normalize)
      to: app/src/types/analysis.ts (BodyComparisonReport)
      via: "WR-02 fix — Firestore raw doc 의 bodyComparisonReport 4 필드 null-guard. Old doc 호환성."
      pattern: "doNotOverCorrect|recommendedFocus"
---

<objective>
Phase 7 vertical slice (2/2) — **classify_findings 본체 + pipeline wiring + integration + camelCase 자동 변환 + frontend normalize mini-fix**. Plan 01 의 schema + canned 카피 박제 위에서 분류 함수 본체 + Phase 6 `compare_body_profiles()` wiring + 단위 test 10종 + integration test 4종 + camelCase 자동 변환 test + frontend normalize null-guard.

본 plan 산출 = (a) `classify_findings()` pure function (CR-01 thread + WR-03 fallback) + `_resolve_joint_group()` + module-level mapping dicts + (b) `compare_body_profiles()` 안 classify_findings 호출 + BodyComparisonReport 4 kwarg 주입 + (c) test 4 파일 (classify_findings 단위 + preserves_fields INF-01 + compare_body_profiles integration + camelCase) + (d) **WR-02 fix — frontend userAnalyses.normalize() mini-fix (~6 줄)**.

**iteration 2 (2026-06-08) cross-AI review fix:** CR-01 (used_reference_fallback thread), WR-02 (frontend normalize 재추가 — B1 retract), WR-03 (recommended_focus_fallback 박제), INF-01 (behavioral preserve fields test).

Purpose: PERS-01 Phase 7 SC #1 (category 별 산출) + SC #2 (doNotOverCorrect / recommendedFocus 출력) + SC #3 (confidence < 0.5 → uncertain demotion) + SC #4 (canned string production). per D-07-A1 + D-07-A2 + D-07-B3 + D-07-D3 + D-07-U1 + Decision 1 + CR-01 + WR-02 + WR-03 + INF-01.

Output: 신설 함수 1 + module-level dict 2 + helper 1 (body_normalizer.py) + pipeline wiring (compare_body_profiles) + test 4 파일 + frontend mini-fix. Task 단위 3 commit — Task 1 classify_findings + 10 단위 test + INF-01 preserves test / Task 2 wiring + integration + camelCase / **Task 3 frontend normalize mini-fix (WR-02)**.

**박제 메모 정합:** [[mvp-simple-pilot-quality]] (캔드 우선) + [[feedback-analysis-first]] (가능성 언어 톤 + WR-03 빈 focus 활성화) + [[analysis-objectivity-no-human-scores]] (분류 룰 belle/강사 점수 라벨 무관) + [[mode3-progress-not-similarity]] (mode3_progress prefix carry-over) + [[no-baekje-filler]] (canned 안 "박제" 0).

**Phase 6 의존:**
- BodyComparisonFinding 6 위치 (measure_ipsf_absolute_deficits 안 line 945/977/1027/1056/1076/1102) 는 Plan 01 Task 3 (W1 atomic commit) 에서 **WR-01 fix — `category="uncertain"` placeholder** 박제 완료 (fail-safe). Plan 02 classify_findings 가 재할당.
- compare_body_profiles wiring 위치 = body_normalizer.py:1263-1269 부근 — classify_findings 1줄 + BodyComparisonReport 조립 (line 1280-1297) 시 findings 교체 + **4 kwarg 주입** (WR-03 fallback 포함).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-difference-classification/07-CONTEXT.md
@.planning/phases/07-difference-classification/07-RESEARCH.md
@.planning/phases/07-difference-classification/07-PATTERNS.md
@.planning/phases/07-difference-classification/07-VALIDATION.md
@.planning/phases/07-difference-classification/07-REVIEW.md
@.planning/phases/07-difference-classification/07-01-PLAN.md
@.planning/phases/06-coaching/06-02-SUMMARY.md
@backend/shared/python/sunity_shared/analysis/body_normalizer.py
@backend/shared/python/sunity_shared/analysis/copy_templates.py
@backend/shared/python/sunity_shared/analysis/skeleton.py
@backend/functions/pipeline/app.py
@backend/tests/phase06/test_dataclass_to_camel_case_dict.py
@backend/tests/phase06/test_compare_body_profiles.py
@backend/tests/phase06/fixtures/fixture_160cm_pro_vs_140cm_student.json
@app/src/lib/userAnalyses.ts
@app/src/types/analysis.ts
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Phase 7 receives Phase 6 outputs only — classify_findings is pure function. Plan 01 AST grep gate 가 production canned string 차단. Frontend normalize() 는 Firestore raw doc 의 정상화 layer — old doc 호환성. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | T (Tampering) | classify_findings - dataclasses.replace 우회 패턴 | mitigate | classify_findings 가 새 BodyComparisonFinding 인스턴스 생성 (replace X) — Phase 6 R8 정합. AST grep test (secondary convention) + **INF-01 fix — behavioral preserve test (primary safety property)** 회귀 차단. |
| T-07-06 | T (Tampering / Firestore drift) | _dataclass_to_camel_case_dict 자동 변환 | mitigate | Phase 6 C8 5-case helper 자동 변환 — 신설 4+3 필드 코드 변경 0. test_dataclass_to_camel_case_dict_phase7.py 회귀 차단. |
| T-07-08 | I (Information disclosure) | mode3_first + used_reference_fallback=True path | mitigate | Decision 1 룰 + **CR-01 fix — render_finding_copy(used_reference_fallback=True) thread**. classify_findings 가 본 path 에서 모든 finding uncertain demotion + recommendation = unprefixed 단일 카피 + body_type_interpretation = None. fixture #6 exact match 검증. |
| T-07-10 | T (Tampering / old Firestore doc 깨짐) | userAnalyses.normalize() Old doc 호환성 | mitigate | **WR-02 fix — normalize() 의 bodyComparisonReport 4 필드 null-guard 박제** (~6 줄). Phase 6 이전 doc 가 신설 필드 없어도 doNotOverCorrect=[] / recommendedFocus=[] / category='uncertain' / phase='hold' 자동 default. TS interface non-optional 유지 (production 데이터는 항상 보유, normalize() 가 compat layer). |
| T-07-11 | I (Information disclosure / 빈 UX) | recommended_focus[] 빈 list 케이스 UX gap | mitigate | **WR-03 fix — classify_findings 가 recommended_focus_fallback 박제** (Plan 01 의 `_EMPTY_FOCUS_FALLBACK` 활용). 빈 list 일 때 단일 fallback 카피 noticeable → 결과 화면 "다음 단계" 박스 비어 보이지 않음. |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | 신규 의존성 0. RESEARCH.md §Package Legitimacy Audit "N/A". |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: classify_findings + _resolve_joint_group + mapping dicts + 10 단위 test (8 분류 + AST grep + WR-03 fallback) + INF-01 preserves test</name>
  <files>
    backend/shared/python/sunity_shared/analysis/body_normalizer.py
    backend/tests/phase07/test_classify_findings.py
    backend/tests/phase07/test_classify_findings_preserves_measurement_fields.py
  </files>
  <read_first>
    backend/shared/python/sunity_shared/analysis/body_normalizer.py (line 787-1297 — BodyComparisonFinding dataclass + measure_ipsf_absolute_deficits + compare_body_profiles. Plan 01 W1 atomic 적용 후 4 필드 + `category="uncertain"` placeholder WR-01 박제)
    backend/shared/python/sunity_shared/analysis/copy_templates.py (Plan 01 신설 — render_finding_copy 시그너처 + _EMPTY_FOCUS_FALLBACK 상수)
    backend/shared/python/sunity_shared/analysis/skeleton.py (KEYPOINT_NAMES + JOINT_LABEL_KO)
    .planning/phases/07-difference-classification/07-RESEARCH.md (§"Code Examples Example 1" line 834-942 + §"Schema Extension Decision 1" line 706-744 + §"Validation Architecture Test Specifications" line 1184-1212)
    .planning/phases/07-difference-classification/07-PATTERNS.md (§"body_normalizer.py" Pattern B + §"test_classify_findings.py")
    .planning/phases/07-difference-classification/07-CONTEXT.md (§Decisions A + U)
    .planning/phases/07-difference-classification/07-REVIEW.md §CR-01 + §WR-03 + §INF-01
    backend/tests/phase07/conftest.py (Plan 01 신설)
    backend/tests/phase07/fixtures/_factory.py (Plan 01 신설)
    backend/tests/phase07/fixtures/fixture_classification_allowed.json (Plan 01 신설)
    backend/tests/phase07/fixtures/fixture_classification_mode3_first_fallback.json (Plan 01 신설 — CR-01 fix exact match)
  </read_first>
  <behavior>
    - **CR-01 + WR-03 fix — classify_findings 시그너처 갱신**: `classify_findings(findings, body_normalization_confidence, comparison_type, *, used_reference_fallback=False) -> tuple[list[BodyComparisonFinding], list[str], list[str], str | None]` 박제 (반환 tuple 4-항 — 4th element = recommended_focus_fallback)
    - `_DEFICIT_TO_GROUP: dict[str, str]` module-level — 5 항 (knee_toe_alignment→leg / extension→torso / posture→arm / body_placement→pole_axis / pose_reliability_low→global, clean_lines 제외)
    - `_JOINT_TO_GROUP: dict[str, str]` module-level — 12 항 (arm/leg)
    - `_resolve_joint_group(finding)` helper — finding.deficit_code=='clean_lines' AND finding.joint_key 가 _JOINT_TO_GROUP 에 있으면 그 값 반환, **clean_lines + joint_key=None 케이스 → "global" 반환 (CR-02 fix path 정합 — Plan 01 의 12 global keys 활용)**, 그 외 _DEFICIT_TO_GROUP.get(deficit_code, 'global')
    - **module-level 박제 순서 (B3):** `_DEFICIT_TO_GROUP` → `_JOINT_TO_GROUP` → `_resolve_joint_group` → `classify_findings`
    - 분류 룰:
      1. `is_low_global = (body_normalization_confidence < CATEGORY_CONF_GATE) OR (comparison_type == 'mode3_first' AND used_reference_fallback)` — CATEGORY_CONF_GATE = 0.5 (Plan 01)
      2. 각 finding 에 대해: `is_low_global` OR `f.confidence < CATEGORY_CONF_GATE` OR `not f.body_type_adjusted` → category='uncertain'
      3. 그 외: `abs(f.deduction_score) <= CATEGORY_GATE` → 'body_type_allowed', 아니면 'needs_adjustment'
    - **CR-01 fix — render_finding_copy 호출 시 used_reference_fallback thread**:
      * `is_mode3_first_fallback = (comparison_type == 'mode3_first' and used_reference_fallback)`
      * 본 path 의 모든 finding 에 대해 `(interp, recom) = render_finding_copy(f.deficit_code, 'uncertain', group, comparison_type, used_reference_fallback=True)` 호출
      * 결과: interp = None (해석 없음), recom = unprefixed 단일 카피 ("이 동작은 기준 영상이 없어, ...")
      * 정상 path: `render_finding_copy(f.deficit_code, category, group, comparison_type)` (used_reference_fallback default=False)
    - 새 BodyComparisonFinding 인스턴스 생성 (dataclasses.replace 우회 X) + 4 신설 필드 (category / phase='hold' / body_type_interpretation=interp / recommendation=recom)
    - 분배: category=='body_type_allowed' → do_not_over_correct.append(recom) / 그 외 (needs_adjustment + uncertain) → recommended_focus.append(recom)
    - **WR-03 fix — recommended_focus_fallback 박제**:
      * after iteration: `if not recommended_focus: recommended_focus_fallback = _EMPTY_FOCUS_FALLBACK` (copy_templates.py 상수 import)
      * 빈 list 가 아닐 때: `recommended_focus_fallback = None`
      * return tuple 4-항: `(classified, do_not_over_correct, recommended_focus, recommended_focus_fallback)`
    - 10 단위 test:
      1. `test_allowed_when_adjusted_and_small_deduction` — fixture_classification_allowed.json + mode1 → category='body_type_allowed', len(dnoc)==1, len(rec)==0, fallback=_EMPTY_FOCUS_FALLBACK (빈 list 케이스)
      2. `test_needs_adjustment_when_large_deduction` — fixture_classification_needs.json + mode1 → category='needs_adjustment', len(rec)==1, len(dnoc)==0, fallback=None
      3. `test_uncertain_when_raw_coords` — fixture_classification_uncertain_raw.json + mode1 → category='uncertain', recommendation contains "강사" or "확인"
      4. `test_uncertain_when_finding_low_confidence` — fixture_classification_uncertain_low_conf.json → category='uncertain'
      5. `test_uncertain_when_report_low_confidence` — fixture_classification_uncertain_global_low.json → 모든 finding category='uncertain'
      6. **(CR-01 fix) `test_uncertain_when_mode3_first_fallback`** — fixture_classification_mode3_first_fallback.json (mode3_first + used_reference_fallback=True) → 모든 finding category='uncertain' (Decision 1) + **recommendation == "이 동작은 기준 영상이 없어, AI 가 IPSF 절대 기준만으로 분석했어요. 강사와 함께 확인 권유드려요." exact match (mode prefix 결합 X)** + body_type_interpretation == None
      7. `test_phase_default_hold` — 모든 classified finding 의 `phase == 'hold'`
      8. `test_aggregate_lists_populated` — 3 finding 입력 (allowed + needs + uncertain) → do_not_over_correct 1 + recommended_focus 2 (uncertain 통합 per Decision 1)
      9. `test_classify_findings_uses_replace_pattern_zero` — AST 로 `classify_findings` 함수 본체 정확히 추출 → `ast.unparse(fn_body)` 안에 `dataclasses.replace` substring 0 회 검증 (secondary convention test — INF-01 정합)
      10. **(WR-03 fix) `test_recommended_focus_fallback_populated_when_empty`** — input findings 모두 body_type_allowed (deduction=-0.2, body_type_adjusted=True, confidence=0.85) → recommended_focus=[], recommended_focus_fallback == _EMPTY_FOCUS_FALLBACK; input 에 needs_adjustment finding 1 개 추가 → recommended_focus != [], recommended_focus_fallback is None
    - **INF-01 fix — 신규 test 파일 `test_classify_findings_preserves_measurement_fields.py`** (primary behavioral safety property):
      * input BodyComparisonFinding: deficit_code='knee_toe_alignment', joint_key='left_knee', measured_value=120.0, deduction_score=-0.2, confidence=0.85, body_type_adjusted=True, category='uncertain' (Plan 01 WR-01 placeholder)
      * `classified, _, _, _ = classify_findings([f], body_normalization_confidence=0.85, comparison_type='mode1')`
      * **6 원본 측정 필드 정확 일치 assertion** — `classified[0].deficit_code == f.deficit_code` / `classified[0].joint_key == f.joint_key` / `classified[0].measured_value == f.measured_value` / `classified[0].deduction_score == f.deduction_score` / `classified[0].confidence == f.confidence` / `classified[0].body_type_adjusted == f.body_type_adjusted`
      * 신규 4 필드 만 박제 — `classified[0].category in ('body_type_allowed', 'needs_adjustment', 'uncertain')` / `classified[0].phase == 'hold'` / `classified[0].body_type_interpretation is not None` (정상 path) / `classified[0].recommendation is not None`
      * output is new object — `id(classified[0]) != id(f)`
  </behavior>
  <action>
    `body_normalizer.py` 의 BodyComparisonFinding dataclass 정의 다음 + measure_ipsf_absolute_deficits 정의 전 위치에 module-level 박제 (순서 강제):

      1. `_DEFICIT_TO_GROUP: dict[str, str]` (5 항)
      2. `_JOINT_TO_GROUP: dict[str, str]` (12 항)
      3. `def _resolve_joint_group(finding: BodyComparisonFinding) -> str` — CR-02 fix 정합:
         * `if finding.deficit_code == "clean_lines" and finding.joint_key: return _JOINT_TO_GROUP.get(finding.joint_key, "arm")`
         * `if finding.deficit_code == "clean_lines": return "global"` (joint_key=None case — Plan 01 12 global keys 활용)
         * else `return _DEFICIT_TO_GROUP.get(finding.deficit_code, "global")`
      4. `def classify_findings(findings, body_normalization_confidence, comparison_type, *, used_reference_fallback=False) -> tuple[list[BodyComparisonFinding], list[str], list[str], str | None]`

    `from .copy_templates import render_finding_copy, _EMPTY_FOCUS_FALLBACK` import 박제 (body_normalizer.py import block 상단).

    classify_findings 본체 (CR-01 + WR-03 fix):
      - is_low_global 산식: `body_normalization_confidence < CATEGORY_CONF_GATE or (comparison_type == "mode3_first" and used_reference_fallback)`
      - is_mode3_first_fallback: `comparison_type == "mode3_first" and used_reference_fallback` (CR-01 분기 — render_finding_copy 에 thread)
      - 각 finding iterate — category 결정 + group = _resolve_joint_group(f) + **(interp, recom) = render_finding_copy(f.deficit_code, category, group, comparison_type, used_reference_fallback=is_mode3_first_fallback)**
      - 새 BodyComparisonFinding(...) 생성 (frozen, dataclasses.replace 호출 금지) — 신설 4 필드
      - 분배: category=='body_type_allowed' → dnoc.append(recom) / 그 외 → rec_focus.append(recom)
      - **after loop (WR-03 fix)**: `recommended_focus_fallback = _EMPTY_FOCUS_FALLBACK if not recommended_focus else None`
      - `return (classified, dnoc, rec_focus, recommended_focus_fallback)`

    `test_classify_findings.py` 신설 — Plan 01 박제 fixture loader (`load_finding_from_fixture`) 활용. 10 단위 test 박제 (위 `<behavior>` 정합). test 6 (`test_uncertain_when_mode3_first_fallback`) 의 recommendation exact match:

      ```
      def test_uncertain_when_mode3_first_fallback(load_finding_from_fixture):
          finding, ctx, expected = load_finding_from_fixture('fixture_classification_mode3_first_fallback')
          classified, dnoc, rec, fallback = classify_findings(
              [finding],
              body_normalization_confidence=ctx['body_normalization_confidence'],
              comparison_type=ctx['comparison_type'],
              used_reference_fallback=ctx['used_reference_fallback'],
          )
          assert classified[0].category == 'uncertain'
          assert classified[0].body_type_interpretation is None  # CR-01 fix
          assert classified[0].recommendation == "이 동작은 기준 영상이 없어, AI 가 IPSF 절대 기준만으로 분석했어요. 강사와 함께 확인 권유드려요."  # CR-01 fix exact match
          assert classified[0].recommendation == expected['recommendation']  # fixture #6 박제 정합
      ```

    test 9 (`test_classify_findings_uses_replace_pattern_zero`) AST 기반:

      ```
      import ast, pathlib

      def test_classify_findings_uses_replace_pattern_zero():
          src = pathlib.Path('backend/shared/python/sunity_shared/analysis/body_normalizer.py').read_text()
          tree = ast.parse(src)
          fn = next(n for n in ast.walk(tree) if isinstance(n, ast.FunctionDef) and n.name == 'classify_findings')
          body = ast.unparse(fn)
          assert 'dataclasses.replace' not in body, body
      ```

    test 10 (WR-03 fix):

      ```
      def test_recommended_focus_fallback_populated_when_empty():
          # All findings → body_type_allowed → recommended_focus=[]
          allowed = BodyComparisonFinding(
              deficit_code='knee_toe_alignment', joint_key='left_knee',
              measured_value=120.0, deduction_score=-0.2, confidence=0.85,
              body_type_adjusted=True, category='uncertain',  # Plan 01 placeholder
          )
          classified, dnoc, rec, fallback = classify_findings([allowed], body_normalization_confidence=0.85, comparison_type='mode1')
          assert rec == []
          from sunity_shared.analysis.copy_templates import _EMPTY_FOCUS_FALLBACK
          assert fallback == _EMPTY_FOCUS_FALLBACK

          # Add a needs_adjustment finding → recommended_focus 채워짐 → fallback None
          needs = BodyComparisonFinding(
              deficit_code='pose_reliability_low', joint_key=None,
              measured_value=0.3, deduction_score=-0.5, confidence=0.9,
              body_type_adjusted=True, category='uncertain',  # Plan 01 placeholder
          )
          classified, dnoc, rec, fallback = classify_findings([allowed, needs], body_normalization_confidence=0.85, comparison_type='mode1')
          assert rec != []
          assert fallback is None
      ```

    **INF-01 fix — `test_classify_findings_preserves_measurement_fields.py` 신설**:

      ```
      from sunity_shared.analysis.body_normalizer import BodyComparisonFinding, classify_findings

      def test_classify_findings_preserves_measurement_fields():
          """INF-01 fix — primary behavioral safety property.
          
          dataclasses.replace 부재 AST grep 은 convention 검증. 
          본 test 가 측정 필드 보존의 actual contract 검증.
          """
          input_f = BodyComparisonFinding(
              deficit_code='knee_toe_alignment',
              joint_key='left_knee',
              measured_value=120.5,
              deduction_score=-0.2,
              confidence=0.85,
              body_type_adjusted=True,
              category='uncertain',  # Plan 01 WR-01 placeholder
          )
          classified, _, _, _ = classify_findings(
              [input_f],
              body_normalization_confidence=0.85,
              comparison_type='mode1',
          )
          out = classified[0]
          # 6 원본 측정 필드 정확 일치
          assert out.deficit_code == input_f.deficit_code
          assert out.joint_key == input_f.joint_key
          assert out.measured_value == input_f.measured_value
          assert out.deduction_score == input_f.deduction_score
          assert out.confidence == input_f.confidence
          assert out.body_type_adjusted == input_f.body_type_adjusted
          # 신규 4 필드 박제 확인
          assert out.category in ('body_type_allowed', 'needs_adjustment', 'uncertain')
          assert out.phase == 'hold'
          assert out.body_type_interpretation is not None  # mode1 정상 path, interp non-None
          assert out.recommendation is not None
          # output is new object
          assert id(out) != id(input_f)
      ```

    per D-07-A1 + D-07-A2 + D-07-B3 + Decision 1 + D-07-U1 + D-07-C1 + **CR-01 + WR-03 + INF-01**.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && pytest backend/tests/phase07/test_classify_findings.py backend/tests/phase07/test_classify_findings_preserves_measurement_fields.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "def classify_findings(" backend/shared/python/sunity_shared/analysis/body_normalizer.py` >= 1
    - Source: `grep -c "def _resolve_joint_group(" backend/shared/python/sunity_shared/analysis/body_normalizer.py` >= 1
    - Source: `grep -c "_DEFICIT_TO_GROUP" backend/shared/python/sunity_shared/analysis/body_normalizer.py` >= 1
    - Source: `grep -c "_JOINT_TO_GROUP" backend/shared/python/sunity_shared/analysis/body_normalizer.py` >= 1
    - Source: `grep -c "from .copy_templates import render_finding_copy" backend/shared/python/sunity_shared/analysis/body_normalizer.py` >= 1
    - Source (WR-03): `grep -c "_EMPTY_FOCUS_FALLBACK" backend/shared/python/sunity_shared/analysis/body_normalizer.py` >= 1
    - Source (CR-01): `grep -c "used_reference_fallback=is_mode3_first_fallback" backend/shared/python/sunity_shared/analysis/body_normalizer.py` >= 1 (또는 동등 thread 패턴)
    - Source (AST 기반 — INF-01 secondary convention): `python -c "import ast, pathlib; src = pathlib.Path('backend/shared/python/sunity_shared/analysis/body_normalizer.py').read_text(); tree = ast.parse(src); fn = next(n for n in ast.walk(tree) if isinstance(n, ast.FunctionDef) and n.name == 'classify_findings'); body = ast.unparse(fn); assert 'dataclasses.replace' not in body, body; print('NO_REPLACE_OK')"` prints `NO_REPLACE_OK`
    - Source (module-level 박제 순서): `python -c "import re, pathlib; src = pathlib.Path('backend/shared/python/sunity_shared/analysis/body_normalizer.py').read_text(); positions = {name: src.find(name) for name in ('_DEFICIT_TO_GROUP', '_JOINT_TO_GROUP', 'def _resolve_joint_group', 'def classify_findings')}; assert positions['_DEFICIT_TO_GROUP'] < positions['_JOINT_TO_GROUP'] < positions['def _resolve_joint_group'] < positions['def classify_findings'], positions; print('ORDER_OK')"` prints `ORDER_OK`
    - Behavior: `pytest backend/tests/phase07/test_classify_findings.py -x` 10 test PASS (8 분류 + AST grep + WR-03 fallback)
    - Behavior (INF-01 fix): `pytest backend/tests/phase07/test_classify_findings_preserves_measurement_fields.py -x` PASS
    - Behavior (CR-01 fix exact match): `test_uncertain_when_mode3_first_fallback` 의 recommendation exact match assertion 통과 (mode prefix 결합 X 검증)
    - Behavior: `pytest backend/tests/phase06/ -x` 회귀 0 (Plan 01 W1 atomic + classify_findings 진입 전 Phase 6 path 정상)
  </acceptance_criteria>
  <done>
    classify_findings 본체 완료 — pure function + frozen dataclass __post_init__ validator + 새 인스턴스 생성 (replace X) + **CR-01 fix (used_reference_fallback thread)** + **WR-03 fix (recommended_focus_fallback)** + **INF-01 fix (preserves test)**. 10 단위 test (8 분류 + AST grep + WR-03) + 1 preserves test 모두 green. module-level 박제 순서 강제. Phase 6 회귀 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: compare_body_profiles wiring + integration test 4종 (WR-03 포함) + _dataclass_to_camel_case_dict 자동 변환 test</name>
  <files>
    backend/shared/python/sunity_shared/analysis/body_normalizer.py
    backend/tests/phase07/test_compare_body_profiles_phase7_integration.py
    backend/tests/phase07/test_dataclass_to_camel_case_dict_phase7.py
  </files>
  <read_first>
    backend/shared/python/sunity_shared/analysis/body_normalizer.py (line 1115-1297 compare_body_profiles + BodyComparisonReport 조립)
    .planning/phases/07-difference-classification/07-RESEARCH.md (§"Code Examples Example 2" line 944-983 + §"Code Examples Example 4" line 1019-1056)
    .planning/phases/07-difference-classification/07-PATTERNS.md (§"compare_body_profiles wiring 위치" + §"test_compare_body_profiles_phase7_integration.py" + §"test_dataclass_to_camel_case_dict_phase7.py")
    .planning/phases/07-difference-classification/07-CONTEXT.md (§Code Insights — Integration Points)
    .planning/phases/07-difference-classification/07-REVIEW.md §WR-03
    backend/functions/pipeline/app.py (line 584-617 _snake_to_camel + _dataclass_to_camel_case_dict)
    backend/tests/phase06/test_compare_body_profiles.py
    backend/tests/phase06/test_dataclass_to_camel_case_dict.py
    backend/tests/phase06/fixtures/fixture_160cm_pro_vs_140cm_student.json
  </read_first>
  <behavior>
    - `compare_body_profiles()` 안 measure_ipsf_absolute_deficits() 호출 직후 (line 1263-1269 부근) classify_findings() 1줄 wiring:
      `classified_findings, do_not_over_correct, recommended_focus, recommended_focus_fallback = classify_findings(findings, body_normalization_confidence=confidence, comparison_type=comparison_type, used_reference_fallback=used_reference_fallback)`
    - BodyComparisonReport 조립 (line 1280-1297) 시 4 kwarg 박제: `findings=classified_findings` + `do_not_over_correct=do_not_over_correct` + `recommended_focus=recommended_focus` + **`recommended_focus_fallback=recommended_focus_fallback` (WR-03 fix)**
    - integration test 4종 (Phase 6 fixture `fixture_160cm_pro_vs_140cm_student.json` 재사용):
      1. `test_compare_body_profiles_emits_classified_findings` — Phase 6 fixture → 결과 `BodyComparisonReport.findings[*].category` 모두 valid enum + `phase=='hold'` + interp/recom non-None
      2. `test_compare_body_profiles_emits_do_not_over_correct` — `report.do_not_over_correct: list[str]` 타입 + populated 시 mode prefix 시작
      3. `test_compare_body_profiles_emits_recommended_focus` — `report.recommended_focus: list[str]` 타입 + populated 시 mode prefix 시작
      4. **(WR-03 fix) `test_compare_body_profiles_emits_recommended_focus_fallback_when_empty`** — Phase 6 fixture (양질 영상, pose_reliability_low 없음) → `report.recommended_focus == []` AND `report.recommended_focus_fallback == _EMPTY_FOCUS_FALLBACK` (둘 다 검증)
    - `test_dataclass_to_camel_case_dict_phase7.py`:
      1. `test_phase7_new_fields_camel_case_automatic` — BodyComparisonFinding + BodyComparisonReport (recommended_focus_fallback 포함) 합성 → `_dataclass_to_camel_case_dict(report)` 호출 → 결과 dict 에 `doNotOverCorrect` + `recommendedFocus` + `recommendedFocusFallback` 키 존재 + `findings[0]['category']` + `findings[0]['phase']` + `findings[0]['bodyTypeInterpretation']` + `findings[0]['recommendation']` 모두 정상 변환
  </behavior>
  <action>
    `body_normalizer.py compare_body_profiles()` 함수 내부 — Plan 01 W1 atomic 위에서 measure_ipsf_absolute_deficits() 호출 직후 1 단락 추가 (`# 5.5) [Phase 7] 분류 + 카피 wiring` 주석):

      `classified_findings, do_not_over_correct, recommended_focus, recommended_focus_fallback = classify_findings(findings, body_normalization_confidence=confidence, comparison_type=comparison_type, used_reference_fallback=used_reference_fallback)`

    BodyComparisonReport 조립 시 `findings=findings` → `findings=classified_findings` + 3 kwarg 추가 (do_not_over_correct / recommended_focus / **recommended_focus_fallback WR-03 fix**). RESEARCH.md §"Code Examples Example 2" line 970-982 + WR-03 fix 정합.

    `test_compare_body_profiles_phase7_integration.py` 신설 — Phase 6 패턴 미러. Phase 6 fixture 재사용. 4 integration test 박제. test 4 (WR-03 fix):

      ```
      from sunity_shared.analysis.copy_templates import _EMPTY_FOCUS_FALLBACK

      def test_compare_body_profiles_emits_recommended_focus_fallback_when_empty():
          """WR-03 fix — 양질 영상 (pose_reliability_low 없음) → recommended_focus=[] AND fallback 카피 populated.
          sweep_rtmw_20260603_1409 5 영상 데이터상 일반 케이스."""
          report = compare_body_profiles(...)  # Phase 6 fixture 로 호출
          if not report.recommended_focus:
              assert report.recommended_focus_fallback == _EMPTY_FOCUS_FALLBACK
          else:
              assert report.recommended_focus_fallback is None
      ```

    `test_dataclass_to_camel_case_dict_phase7.py` 신설 — RESEARCH.md §"Code Examples Example 4" 박제 + Phase 6 importlib reload 패턴 미러. 신설 4+3 필드 자동 변환 검증 (recommendedFocusFallback 포함).

    per D-07-B1 + D-07-B3 + Decision 1 + WR-03.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && pytest backend/tests/phase07/test_compare_body_profiles_phase7_integration.py backend/tests/phase07/test_dataclass_to_camel_case_dict_phase7.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "classified_findings, do_not_over_correct, recommended_focus, recommended_focus_fallback = classify_findings(" backend/shared/python/sunity_shared/analysis/body_normalizer.py` >= 1
    - Source: `grep -c "findings=classified_findings" backend/shared/python/sunity_shared/analysis/body_normalizer.py` >= 1
    - Source: `grep -c "do_not_over_correct=do_not_over_correct" backend/shared/python/sunity_shared/analysis/body_normalizer.py` >= 1
    - Source: `grep -c "recommended_focus=recommended_focus" backend/shared/python/sunity_shared/analysis/body_normalizer.py` >= 1
    - Source (WR-03 fix): `grep -c "recommended_focus_fallback=recommended_focus_fallback" backend/shared/python/sunity_shared/analysis/body_normalizer.py` >= 1
    - Behavior: `pytest backend/tests/phase07/test_compare_body_profiles_phase7_integration.py -x` 4 test PASS — classified + dnoc + rec + WR-03 fallback
    - Behavior: `pytest backend/tests/phase07/test_dataclass_to_camel_case_dict_phase7.py -x` PASS — 7 신설 필드 (4+3 incl recommendedFocusFallback) 자동 camelCase 변환
    - Behavior: `pytest backend/tests/phase06/ -x` 회귀 0
    - Behavior: `pytest backend/tests/ -x` (전체 backend suite) all green
  </acceptance_criteria>
  <done>
    compare_body_profiles wiring 완료 — measure_ipsf_absolute_deficits 호출 직후 classify_findings 1줄 + **4 kwarg 주입 (recommended_focus_fallback WR-03 fix 포함)**. 4 integration test + 1 camelCase 자동 변환 test 모두 green. Phase 6 회귀 0. Phase 6 C8 5-case helper 자동 활용.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: WR-02 fix — frontend userAnalyses.normalize() mini-fix (4 필드 null-guard, ~6 줄)</name>
  <files>
    app/src/lib/userAnalyses.ts
  </files>
  <read_first>
    app/src/lib/userAnalyses.ts (line 27-56 — normalize() 본체)
    app/src/types/analysis.ts (Plan 01 Task 3 확장 후 — BodyComparisonReport 7 신설 필드)
    .planning/phases/07-difference-classification/07-REVIEW.md §WR-02
    .planning/phases/07-difference-classification/07-RESEARCH.md §"Open Questions" Q4 (iteration 1 의 B1 결정 retract 정합)
    app/CLAUDE.md (디자인 + 컬러 + 라이트 테마 박제 — 본 task 는 UI 변경 X)
  </read_first>
  <behavior>
    - **WR-02 fix — iteration 1 의 B1 fix (Plan 02 Task 3 삭제) 결정 retract**.
    - normalize() 함수 안 `result: raw.result as AnalysisDoc['result']` line 직전에 mini-fix 박제 (~6 줄):
      ```
      const rawResult = raw.result as AnalysisDoc['result'] | undefined;
      if (rawResult?.bodyComparisonReport) {
          const report = rawResult.bodyComparisonReport;
          // Phase 7 (2026-06-08, Plan 07-02 WR-02): old Firestore doc 호환성 — 신설 4+3 필드 default
          (report as any).doNotOverCorrect ??= [];
          (report as any).recommendedFocus ??= [];
          (report as any).recommendedFocusFallback ??= null;
          (report.findings ?? []).forEach((f: any) => {
              f.category ??= 'uncertain';
              f.phase ??= 'hold';
          });
      }
      ```
      또는 immutable pattern (recommended — TS strict mode 정합):
      ```
      let result = raw.result as AnalysisDoc['result'] | undefined;
      if (result?.bodyComparisonReport) {
          const report = result.bodyComparisonReport;
          result = {
              ...result,
              bodyComparisonReport: {
                  ...report,
                  doNotOverCorrect: report.doNotOverCorrect ?? [],
                  recommendedFocus: report.recommendedFocus ?? [],
                  recommendedFocusFallback: report.recommendedFocusFallback ?? null,
                  findings: (report.findings ?? []).map(f => ({
                      ...f,
                      category: f.category ?? 'uncertain',
                      phase: f.phase ?? 'hold',
                  })),
              },
          };
      }
      ```
    - TS interface non-optional 유지 (production 데이터는 항상 보유, normalize() 가 compat layer).
    - `cd app && npx tsc --noEmit` exits 0.
    - scope = `normalize()` 안 1 위치, ~10-12 줄 추가 (immutable pattern) 또는 ~6 줄 (mutation pattern). deep-mapping 미필요 (`bodyComparisonReport` 만 별도 처리).
    - 본 task = 단일 commit. iteration 1 의 B1 fix 결정 retract 명시 (commit 메시지에 `WR-02 retract B1` 포함).
  </behavior>
  <action>
    `app/src/lib/userAnalyses.ts::normalize()` 안 line 47-56 의 return 직전 mini-fix 박제. 

    구현 선택: **immutable pattern 권장** (TS strict mode + React 19 정합). 위 behavior 박제. Phase 6 패턴 (`result: raw.result as AnalysisDoc['result']` 단순 assertion) 의 boundary 안에서 minimal 확장.

    주석 박제 (Phase 6 패턴 정합):
      ```
      // Phase 7 (2026-06-08, Plan 07-02 Task 3 WR-02 fix): old Firestore doc 호환성 — 
      // bodyComparisonReport 의 신설 4+3 필드 (Plan 01 Task 3 lockstep) default 처리. 
      // iteration 1 의 B1 fix (Plan 02 Task 3 삭제) retract — old doc 가 신설 필드 없어도 crash X.
      // TS interface 는 non-optional 유지 (production 데이터는 항상 보유, normalize() 가 compat layer).
      ```

    per WR-02 fix + iteration 1 B1 retract.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && npx tsc --noEmit && grep -c "doNotOverCorrect" src/lib/userAnalyses.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "doNotOverCorrect" app/src/lib/userAnalyses.ts` >= 1
    - Source: `grep -c "recommendedFocus" app/src/lib/userAnalyses.ts` >= 1
    - Source: `grep -c "bodyComparisonReport" app/src/lib/userAnalyses.ts` >= 1
    - Source: `grep -c "WR-02" app/src/lib/userAnalyses.ts` >= 1 (commit traceability)
    - Behavior: `cd app && npx tsc --noEmit` exits 0
    - Behavior: 본 task 단일 commit. commit 메시지에 `WR-02 retract B1` 포함.
  </acceptance_criteria>
  <done>
    WR-02 fix 완료 — frontend `userAnalyses.normalize()` 안 bodyComparisonReport 4 필드 null-guard 박제 (~10 줄, immutable pattern). iteration 1 의 B1 fix (Plan 02 Task 3 삭제) retract. Old Firestore doc 호환성 박제. TS interface non-optional 유지. tsc --noEmit clean.
  </done>
</task>

</tasks>

<verification>
- `pytest backend/tests/phase07/ -x` PASS (전체 phase07 suite — Plan 01 박제 4 test + Plan 02 박제 4 test = 8 파일 ~20-25 test all green)
- `pytest backend/tests/phase06/ -x` 회귀 0 (Plan 01 W1 atomic placeholder `category="uncertain"` WR-01 fail-safe + classify_findings 가 재할당)
- `pytest backend/tests/ -x` (전체 backend suite — phase06 136 + phase07 ~25 = ~161 + 1 skip) all green
- `cd app && npx tsc --noEmit` exits 0 (Plan 01 TS 확장 + Plan 02 Task 3 normalize() 확장 후 type-check clean)
- **CR-01 fix** — classify_findings 가 used_reference_fallback=True 케이스에서 `render_finding_copy(..., used_reference_fallback=True)` thread + recommendation exact match (unprefixed 단일 문장) + body_type_interpretation=None
- **WR-02 fix** — frontend `userAnalyses.normalize()` 의 4 필드 null-guard (iteration 1 의 B1 retract). old doc 호환성 박제
- **WR-03 fix** — classify_findings 가 빈 recommended_focus[] 케이스에서 recommended_focus_fallback 박제 (_EMPTY_FOCUS_FALLBACK 단일 카피)
- **INF-01 fix** — `test_classify_findings_preserves_measurement_fields.py` (primary behavioral safety) + AST grep (secondary convention)
- classify_findings 안 `dataclasses.replace` 0회 (AST 기반)
- compare_body_profiles wiring — 4 kwarg 주입 (findings/dnoc/rec_focus/**recommended_focus_fallback WR-03**)
- module-level 박제 순서: _DEFICIT_TO_GROUP → _JOINT_TO_GROUP → _resolve_joint_group → classify_findings
- Phase 6 C8 helper 자동 변환 회귀 차단 — `test_dataclass_to_camel_case_dict_phase7.py` PASS (7 신설 필드)
- per CLAUDE.md §7 (이모지 0, 슬롭 코드 0) + [[no-baekje-filler]] (한국어 주석 안 의미 있는 사양 인용만)
- per [[analysis-objectivity-no-human-scores]] — classify_findings 분류 룰이 사람 점수 라벨 무관
- per [[mvp-simple-pilot-quality]] — 캔드 우선 + LLM 풍부화는 Phase 11
- per [[mode3-progress-not-similarity]] — mode3_progress canned prefix carry-over
</verification>

<success_criteria>
1. **classify_findings + 11 단위 test PASS**: pure function + frozen dataclass __post_init__ Literal validator + dataclasses.replace 우회 X (AST + INF-01 behavioral) + **CR-01 fix used_reference_fallback thread** + **WR-03 fix recommended_focus_fallback** → `test_classify_findings.py` 10 test + `test_classify_findings_preserves_measurement_fields.py` 1 test all PASS
2. **pipeline wiring + 4 integration test PASS**: compare_body_profiles() 안 classify_findings 1줄 + **4 kwarg 주입 (recommended_focus_fallback WR-03 fix 포함)** + Phase 6 fixture 재사용 → `test_compare_body_profiles_phase7_integration.py` 4 test PASS
3. **_dataclass_to_camel_case_dict 자동 변환 회귀 차단 (7 신설 필드)**: Phase 6 C8 helper 자동 활용 → `test_dataclass_to_camel_case_dict_phase7.py` PASS — `doNotOverCorrect` / `recommendedFocus` / `recommendedFocusFallback` / `bodyTypeInterpretation` 모두 정상 변환
4. **module-level 박제 순서**: `_DEFICIT_TO_GROUP` → `_JOINT_TO_GROUP` → `_resolve_joint_group` → `classify_findings` → ORDER_OK
5. **Phase 6 회귀 0**: `pytest backend/tests/phase06/ -x` PASS (Plan 01 W1 atomic + WR-01 fail-safe placeholder + classify_findings 재할당)
6. **백엔드 전체 PASS**: `pytest backend/tests/ -x` (phase06 136 + phase07 ~25) all green
7. **CR-01 fix — mode3_first fallback Decision 1 exact match**: `test_uncertain_when_mode3_first_fallback` 가 recommendation = unprefixed 단일 문장 exact match assertion 통과 + body_type_interpretation == None
8. **WR-02 fix — frontend normalize**: `userAnalyses.normalize()` 가 4 필드 null-guard 박제 (iteration 1 B1 retract). tsc --noEmit clean.
9. **WR-03 fix — recommended_focus_fallback**: classify_findings 가 빈 list 케이스에서 단일 fallback 카피 박제. integration test 가 검증.
10. **INF-01 fix — preserves test**: 6 원본 측정 필드 input → output 정확 일치 (primary behavioral). AST grep (secondary convention) 유지.
11. **박제 메모 정합**: [[analysis-objectivity-no-human-scores]] + [[mvp-simple-pilot-quality]] + [[mode3-progress-not-similarity]] + [[feedback-analysis-first]] (WR-03 빈 focus 활성화) — 모두 단위 test backstop
12. **Phase 7 ROADMAP 4 success criteria 박제 완료** (Plan 01 + Plan 02 합산):
    - SC #1 (category 별 산출): classify_findings 11 test + integration 4 test
    - SC #2 (doNotOverCorrect / recommendedFocus 출력): test_compare_body_profiles_emits_* (3+1 WR-03)
    - SC #3 (confidence < 0.5 → uncertain): test_uncertain_when_*_low_confidence
    - SC #4 (금지 표현 grep gate): Plan 01 test_copy_templates_no_forbidden.py 9 parametrize PASS
</success_criteria>

<output>
Create `.planning/phases/07-difference-classification/07-02-SUMMARY.md` upon completion. Include:
- classify_findings 본체 박제 위치 (body_normalizer.py 줄 번호) + _resolve_joint_group / _DEFICIT_TO_GROUP / _JOINT_TO_GROUP + 박제 순서 확인
- compare_body_profiles wiring 1줄 추가 위치 + BodyComparisonReport 조립 **4 kwarg 주입** (WR-03 fallback 포함)
- 11 단위 test (10 분류 + INF-01 preserves) + 4 integration test (WR-03 fallback 포함) + 1 camelCase 자동 변환 test PASS 결과
- AST 기반 replace 우회 검증 결과 (NO_REPLACE_OK) — INF-01 secondary convention
- Phase 6 회귀 0 확인
- 전체 backend suite 결과 ~161 PASS + 1 skip
- **WR-02 fix — frontend userAnalyses.normalize() mini-fix 확인** (iteration 1 의 B1 retract 명시)
- **CR-01 fix — mode3_first + used_reference_fallback Decision 1 + recommendation exact match (unprefixed)** (fixture #6 정합)
- **WR-03 fix — recommended_focus_fallback (_EMPTY_FOCUS_FALLBACK 활성화)** 결과
- Phase 7 ROADMAP 4 success criteria 달성 매핑 (SC#1-#4)
- iteration 2 cross-AI review fix 7 finding (CR-01 / CR-02 - Plan 01 / WR-01 - Plan 01 / WR-02 / WR-03 / WR-04 - Plan 01 / INF-01) 각 mitigation 위치 보고
- 단위 test 커버리지: classify_findings 11 + INF-01 1 + integration 4 + camelCase 1 + Plan 01 AST grep gate 9 + Plan 01 render 7 + Plan 01 resolver coverage 18+15 + Plan 01 lockstep 7 필드 = ~74 test
</output>
