---
phase: 07-difference-classification
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - backend/shared/python/sunity_shared/analysis/body_normalizer.py
  - app/src/lib/userAnalyses.ts
  - backend/tests/phase07/test_classify_findings.py
  - backend/tests/phase07/test_compare_body_profiles_phase7_integration.py
  - backend/tests/phase07/test_dataclass_to_camel_case_dict_phase7.py
autonomous: true
requirements: [PERS-01]
tags: [classify-findings, pipeline-wiring, integration, camel-case, frontend-normalize, phase-07]

must_haves:
  truths:
    - "`classify_findings(findings, body_normalization_confidence, comparison_type, *, used_reference_fallback=False) -> tuple[list[BodyComparisonFinding], list[str], list[str]]` 가 body_normalizer.py 에 박제되어 순수 함수 (numpy / boto3 / network / LLM 무관) 로 동작한다 — per D-07-A1"
    - "분류 룰: body_type_adjusted=True + |deduction_score| <= 0.2 → body_type_allowed / body_type_adjusted=True + |deduction_score| > 0.2 → needs_adjustment / body_type_adjusted=False → uncertain — per D-07-A1"
    - "confidence-tiered demotion (OR 합집합): body_normalization_confidence < 0.5 → 모든 finding uncertain demotion + finding.confidence < 0.5 → 개별 uncertain + comparison_type='mode3_first' AND used_reference_fallback=True → 모든 finding uncertain demotion (Decision 1, Page 9 절대 트랙 단독 path) — per D-07-A2 + D-07-U1"
    - "classify_findings 가 dataclasses.replace 우회 패턴을 사용하지 않고 새 BodyComparisonFinding 인스턴스를 생성한다 — frozen dataclass __post_init__ validator 통과 보장 (Phase 6 R8 박제 정합)"
    - "분배 룰: category='body_type_allowed' → do_not_over_correct[].append, category='needs_adjustment' OR 'uncertain' → recommended_focus[].append — per D-07-B3 + Decision 1 (uncertain 통합)"
    - "compare_body_profiles() 가 measure_ipsf_absolute_deficits() 호출 직후 classify_findings() 1줄 wiring 추가 + BodyComparisonReport 조립 시 findings=classified_findings + do_not_over_correct + recommended_focus 두 kwarg 주입 — Phase 6 박제 패턴 정합"
    - "frontend `userAnalyses.normalize()` 가 Firestore raw doc 의 `bodyComparisonReport.doNotOverCorrect` / `recommendedFocus` 부재 시 graceful default `[]` 처리 — backward compatibility 박제 (Phase 6 close-out 이전 Firestore doc 정상 렌더)"
    - "_dataclass_to_camel_case_dict (Phase 6 C8 박제 5-case helper) 가 신설 4+2 필드 자동 camelCase 변환 — do_not_over_correct → doNotOverCorrect / recommended_focus → recommendedFocus / body_type_interpretation → bodyTypeInterpretation. 코드 변경 0, 회귀 차단 단위 test 만 추가"
    - "Phase 6 fixture (fixture_160cm_pro_vs_140cm_student.json) 재사용 integration test 가 compare_body_profiles() 호출 후 결과 BodyComparisonReport.findings[*].category 모두 valid enum + do_not_over_correct + recommended_focus 두 list 가 populated 또는 빈 list[] (정상) 검증"
  artifacts:
    - path: backend/shared/python/sunity_shared/analysis/body_normalizer.py
      provides: "classify_findings() pure function (D-07-A1 + D-07-A2 + Decision 1 mode3_first fallback 룰) + _DEFICIT_TO_GROUP module-level dict (5 deficit_code → joint_group 매핑) + _JOINT_TO_GROUP module-level dict (12 joint → arm/leg 매핑, clean_lines.joint_key 분기용) + _resolve_joint_group() helper (deficit_code=clean_lines 시 joint_key 분기 + 그 외 _DEFICIT_TO_GROUP fallback) + compare_body_profiles() 안 classify_findings() 1줄 wiring + BodyComparisonReport 조립 시 2 kwarg 주입"
      contains:
        - "def classify_findings("
        - "def _resolve_joint_group("
        - "_DEFICIT_TO_GROUP"
        - "_JOINT_TO_GROUP"
        - "from .copy_templates import render_finding_copy"
        - "do_not_over_correct=do_not_over_correct"
        - "recommended_focus=recommended_focus"
    - path: app/src/lib/userAnalyses.ts
      provides: "normalize() 함수에 신설 필드 graceful default 박제 — raw.bodyComparisonReport.doNotOverCorrect / recommendedFocus 부재 시 빈 list[] 반환, raw.bodyComparisonReport.findings[*].category / phase / bodyTypeInterpretation / recommendation 부재 시 optional 통과 (Phase 6 close-out 이전 Firestore doc 호환)"
      contains:
        - "doNotOverCorrect"
        - "recommendedFocus"
    - path: backend/tests/phase07/test_classify_findings.py
      provides: "8 단위 test — classify_findings 분류 룰 전체 검증 (allowed/needs/uncertain×3 + phase default + aggregate + replace grep)"
      contains:
        - "def test_allowed_when_adjusted_and_small_deduction"
        - "def test_needs_adjustment_when_large_deduction"
        - "def test_uncertain_when_raw_coords"
        - "def test_uncertain_when_finding_low_confidence"
        - "def test_uncertain_when_report_low_confidence"
        - "def test_uncertain_when_mode3_first_fallback"
        - "def test_phase_default_hold"
        - "def test_aggregate_lists_populated"
        - "def test_no_dataclasses_replace_usage"
    - path: backend/tests/phase07/test_compare_body_profiles_phase7_integration.py
      provides: "Phase 6 fixture 재사용 integration test — compare_body_profiles() 호출 후 BodyComparisonReport.findings[*].category enum valid + do_not_over_correct / recommended_focus 두 list populated 또는 빈 list[]"
      contains:
        - "def test_compare_body_profiles_emits_classified_findings"
        - "def test_compare_body_profiles_emits_do_not_over_correct"
        - "def test_compare_body_profiles_emits_recommended_focus"
    - path: backend/tests/phase07/test_dataclass_to_camel_case_dict_phase7.py
      provides: "신설 4+2 필드 자동 camelCase 변환 회귀 차단 — Phase 6 C8 helper 검증"
      contains:
        - "def test_phase7_new_fields_camel_case_automatic"
        - "doNotOverCorrect"
        - "recommendedFocus"
        - "bodyTypeInterpretation"
  key_links:
    - from: backend/shared/python/sunity_shared/analysis/body_normalizer.py
      to: backend/shared/python/sunity_shared/analysis/copy_templates.py
      via: "from .copy_templates import render_finding_copy — per-finding canned string lookup"
      pattern: "render_finding_copy"
    - from: backend/shared/python/sunity_shared/analysis/body_normalizer.py (compare_body_profiles)
      to: backend/shared/python/sunity_shared/analysis/body_normalizer.py (classify_findings)
      via: "measure_ipsf_absolute_deficits() 호출 직후 classify_findings() 1줄 wiring, BodyComparisonReport 조립 시 두 kwarg 주입"
      pattern: "classified_findings, do_not_over_correct, recommended_focus = classify_findings\\("
    - from: app/src/lib/userAnalyses.ts (normalize)
      to: app/src/types/analysis.ts (BodyComparisonReport)
      via: "Firestore raw doc → AnalysisDoc 변환 시 doNotOverCorrect/recommendedFocus 부재 graceful default []"
      pattern: "doNotOverCorrect|recommendedFocus"
    - from: backend/functions/pipeline/app.py (_dataclass_to_camel_case_dict)
      to: backend/shared/python/sunity_shared/analysis/body_normalizer.py (BodyComparisonReport)
      via: "Phase 6 C8 5-case helper 자동 변환 — do_not_over_correct → doNotOverCorrect 등 신설 6 필드"
      pattern: "_dataclass_to_camel_case_dict"
---

<objective>
Phase 7 vertical slice (2/2) — **classify_findings 본체 + pipeline wiring + integration + frontend normalize + camelCase 자동 변환 회귀 차단**. Plan 01 의 schema + canned 카피 박제 위에서 분류 함수 본체 + Phase 6 `compare_body_profiles()` wiring 1줄 추가 + 단위 test 8종 + integration test 3종 + camelCase 자동 변환 test 1종 + frontend `userAnalyses.normalize()` 의 graceful default 처리.

본 plan 산출 = (a) `classify_findings()` pure function + `_resolve_joint_group()` + module-level mapping dicts + (b) `compare_body_profiles()` 안 classify_findings 호출 wiring 1줄 + BodyComparisonReport 조립 시 2 kwarg 주입 + (c) test 3 파일 (classify_findings 단위 / compare_body_profiles integration / camelCase 자동 변환) + (d) frontend `userAnalyses.normalize()` 의 신설 필드 graceful default 처리 + `tsc --noEmit` clean.

Purpose: PERS-01 Phase 7 SC #1 (category 별 산출 — classified_findings list) + SC #2 (doNotOverCorrect / recommendedFocus 출력 본체) + SC #3 (confidence < 0.5 → uncertain demotion) + SC #4 (canned string production 진입 — grep gate Plan 01 박제 위에서 실제 출력 검증). per D-07-A1 + D-07-A2 + D-07-B3 + D-07-D3 + D-07-U1 + Decision 1 (mode3_first fallback).

Output: 신설 함수 1 + module-level dict 2 + helper 1 (body_normalizer.py 안) + pipeline wiring 1줄 (compare_body_profiles 안 5.5 위치) + frontend normalize 확장 + test 3 파일 (8 + 3 + 1 = 12 test). Phase 6 commit 패턴 정합 — 분류 본체 + wiring + test 가 단일 commit 또는 task 단위 분리 (Task 1 classify_findings 본체 / Task 2 wiring + integration / Task 3 frontend normalize + camelCase test).

**박제 메모 정합:** [[mvp-simple-pilot-quality]] (캔드 우선 박제 — Phase 11 LLM 풍부화는 후속) + [[feedback-analysis-first]] (가능성 언어 톤 — canned 안 발현) + [[analysis-objectivity-no-human-scores]] (분류 룰이 belle/강사 점수 라벨 무관, `body_type_adjusted` + `deduction_score` 만 사용) + [[mode3-progress-not-similarity]] (mode3_progress 카피 prefix 박제) + [[no-baekje-filler]] (canned 안 "박제" 0 — Plan 01 grep gate 가 backstop).

**Phase 6 분석 (이미 close-out):**
- BodyComparisonFinding 6 위치 (measure_ipsf_absolute_deficits 안 line 945/977/1027/1056/1076/1102) 는 Plan 01 Task 3 에서 `category="body_type_allowed"` placeholder 박제 완료 — Plan 02 classify_findings 가 재할당. Phase 6 회귀 0.
- compare_body_profiles wiring 위치 = measure_ipsf_absolute_deficits 호출 직후 (body_normalizer.py:1263-1269 부근) — classify_findings 1줄 추가 후 BodyComparisonReport 조립 (line 1280-1297 부근) 시 findings 교체 + 2 kwarg 주입.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-difference-classification/07-CONTEXT.md
@.planning/phases/07-difference-classification/07-RESEARCH.md
@.planning/phases/07-difference-classification/07-PATTERNS.md
@.planning/phases/07-difference-classification/07-VALIDATION.md
@.planning/phases/07-difference-classification/07-01-PLAN.md
@.planning/phases/06-coaching/06-02-SUMMARY.md
@backend/shared/python/sunity_shared/analysis/body_normalizer.py
@backend/shared/python/sunity_shared/analysis/copy_templates.py
@backend/shared/python/sunity_shared/analysis/skeleton.py
@backend/functions/pipeline/app.py
@app/src/lib/userAnalyses.ts
@app/src/types/analysis.ts
@backend/tests/phase06/test_dataclass_to_camel_case_dict.py
@backend/tests/phase06/test_compare_body_profiles.py
@backend/tests/phase06/fixtures/fixture_160cm_pro_vs_140cm_student.json
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Phase 7 receives Phase 6 outputs only — classify_findings is pure function, BodyComparisonFinding dataclass __post_init__ Literal enum validator backstop. Plan 01 grep gate 가 production canned string 차단. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | T (Tampering) | classify_findings - dataclasses.replace 우회 패턴 | mitigate | classify_findings 가 새 BodyComparisonFinding 인스턴스 생성 (replace X) — Phase 6 R8 박제 정합. `test_no_dataclasses_replace_usage` grep test 회귀 차단. |
| T-07-06 | T (Tampering / Firestore drift) | _dataclass_to_camel_case_dict 자동 변환 | mitigate | Phase 6 C8 박제 5-case helper (dataclass / list / dict / Enum / scalar) 자동 변환 — 신설 4+2 필드 코드 변경 0. `test_dataclass_to_camel_case_dict_phase7.py` 회귀 차단. |
| T-07-07 | D (Denial of Service / frontend crash) | userAnalyses.normalize - 신설 필드 부재 시 crash | mitigate | normalize() 가 `raw.bodyComparisonReport.doNotOverCorrect ?? []` / `recommendedFocus ?? []` graceful default. Phase 6 close-out 이전 Firestore doc backward-compatible. |
| T-07-08 | I (Information disclosure) | mode3_first + used_reference_fallback=True 인 path 의 finding 분류 | mitigate | Decision 1 룰 — 모든 finding `uncertain` demotion + canned string Plan 01 박제 (`"이 부분은 AI 분석 결과예요. 강사와 함께 영상을 한 번 더 확인해 보세요."` fallback). 정직성 우위, 잘못된 분류 production 진입 차단. |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | 신규 의존성 0 — Plan 01 의 stdlib + 기존 pytest 재사용. RESEARCH.md §Package Legitimacy Audit "N/A (no new packages)" 박제. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: classify_findings 본체 + _resolve_joint_group + mapping dicts + 단위 test 8종</name>
  <files>
    backend/shared/python/sunity_shared/analysis/body_normalizer.py
    backend/tests/phase07/test_classify_findings.py
  </files>
  <read_first>
    backend/shared/python/sunity_shared/analysis/body_normalizer.py (line 787-1297 — BodyComparisonFinding dataclass + measure_ipsf_absolute_deficits + compare_body_profiles 본체)
    backend/shared/python/sunity_shared/analysis/copy_templates.py (Plan 01 신설 — render_finding_copy 시그너처)
    backend/shared/python/sunity_shared/analysis/skeleton.py (KEYPOINT_NAMES + JOINT_LABEL_KO)
    .planning/phases/07-difference-classification/07-RESEARCH.md (§"Code Examples Example 1" line 834-942 — classify_findings 구현 박제)
    .planning/phases/07-difference-classification/07-RESEARCH.md (§"Schema Extension Decision 1" line 706-744 — mode3_first fallback 룰)
    .planning/phases/07-difference-classification/07-RESEARCH.md (§"Validation Architecture Test Specifications" line 1184-1212 — 8 단위 test 박제)
    .planning/phases/07-difference-classification/07-PATTERNS.md (§"body_normalizer.py" Pattern B + §"test_classify_findings.py")
    .planning/phases/07-difference-classification/07-CONTEXT.md (§Decisions A + U)
    backend/tests/phase07/conftest.py (Plan 01 신설)
    backend/tests/phase07/fixtures/_factory.py (Plan 01 신설)
    backend/tests/phase07/fixtures/fixture_classification_allowed.json (Plan 01 신설)
  </read_first>
  <behavior>
    - `classify_findings(findings, body_normalization_confidence, comparison_type, *, used_reference_fallback=False)` 시그너처 박제 (RESEARCH.md §Example 1 line 874-941 정합) — 반환 `tuple[list[BodyComparisonFinding], list[str], list[str]]`
    - `_DEFICIT_TO_GROUP: dict[str, str]` module-level — knee_toe_alignment→leg / extension→torso / posture→arm / body_placement→pole_axis / pose_reliability_low→global (5 항, clean_lines 제외)
    - `_JOINT_TO_GROUP: dict[str, str]` module-level — 12 joint → arm/leg 매핑 (left/right elbow/shoulder/wrist→arm + left/right hip/knee/ankle→leg)
    - `_resolve_joint_group(finding)` helper — finding.deficit_code=='clean_lines' AND finding.joint_key 가 _JOINT_TO_GROUP 에 있으면 그 값 반환, 그 외 _DEFICIT_TO_GROUP.get(deficit_code, 'global')
    - 분류 룰 (RESEARCH.md §Example 1 line 894-918):
      1. `is_low_global = (body_normalization_confidence < 0.5) OR (comparison_type == 'mode3_first' AND used_reference_fallback)`
      2. 각 finding 에 대해: `is_low_global` OR `f.confidence < 0.5` OR `not f.body_type_adjusted` → category='uncertain'
      3. 그 외: `abs(f.deduction_score) <= 0.2` → category='body_type_allowed', 아니면 'needs_adjustment'
    - render_finding_copy 호출 → 새 BodyComparisonFinding 인스턴스 생성 (dataclasses.replace X) + 4 신설 필드 모두 채움 (category / phase='hold' / body_type_interpretation=interp / recommendation=recom)
    - 분배 (D-07-B3 + Decision 1): category='body_type_allowed' → do_not_over_correct.append(recom) / 그 외 (needs_adjustment + uncertain) → recommended_focus.append(recom)
    - 8 단위 test (RESEARCH.md §"Test Specifications" + PATTERNS.md §"test_classify_findings.py"):
      1. `test_allowed_when_adjusted_and_small_deduction` — fixture_classification_allowed.json + mode1 → category='body_type_allowed', len(dnoc)==1, len(rec)==0
      2. `test_needs_adjustment_when_large_deduction` — fixture_classification_needs.json + mode1 → category='needs_adjustment', len(rec)==1, len(dnoc)==0
      3. `test_uncertain_when_raw_coords` — fixture_classification_uncertain_raw.json + mode1 → category='uncertain', recommendation contains "강사" or "확인"
      4. `test_uncertain_when_finding_low_confidence` — fixture_classification_uncertain_low_conf.json → category='uncertain'
      5. `test_uncertain_when_report_low_confidence` — fixture_classification_uncertain_global_low.json (body_normalization_confidence=0.3) → 모든 finding category='uncertain'
      6. `test_uncertain_when_mode3_first_fallback` — fixture_classification_mode3_first_fallback.json (mode3_first + used_reference_fallback=True) → 모든 finding category='uncertain' (Decision 1)
      7. `test_phase_default_hold` — 모든 classified finding 의 `phase == 'hold'` (D-07-C1 v1 단일)
      8. `test_aggregate_lists_populated` — 3 finding 입력 (allowed + needs + uncertain) → do_not_over_correct 1 + recommended_focus 2 (uncertain 통합 per Decision 1)
    - 추가 test 9 (grep): `test_no_dataclasses_replace_usage` — body_normalizer.py 안에서 classify_findings 함수 body 가 `dataclasses.replace` 호출 0회 (회귀 차단 grep, Phase 6 R8 박제 정합)
  </behavior>
  <action>
    `body_normalizer.py` 의 BodyComparisonFinding dataclass 정의 다음 + measure_ipsf_absolute_deficits 정의 전 위치에 module-level 박제:

      ```
      _DEFICIT_TO_GROUP: dict[str, str] = {
          "knee_toe_alignment": "leg",
          "extension": "torso",
          "posture": "arm",
          "body_placement": "pole_axis",
          "pose_reliability_low": "global",
      }

      _JOINT_TO_GROUP: dict[str, str] = {
          "left_elbow": "arm", "right_elbow": "arm",
          "left_shoulder": "arm", "right_shoulder": "arm",
          "left_wrist": "arm", "right_wrist": "arm",
          "left_hip": "leg", "right_hip": "leg",
          "left_knee": "leg", "right_knee": "leg",
          "left_ankle": "leg", "right_ankle": "leg",
      }
      ```

    `from .copy_templates import render_finding_copy` import 박제 (body_normalizer.py 의 import block 상단).

    `_resolve_joint_group(finding: BodyComparisonFinding) -> str` 박제 — `if finding.deficit_code == "clean_lines" and finding.joint_key: return _JOINT_TO_GROUP.get(finding.joint_key, "arm")` else `return _DEFICIT_TO_GROUP.get(finding.deficit_code, "global")`.

    `classify_findings(findings, body_normalization_confidence, comparison_type, *, used_reference_fallback=False) -> tuple[list[BodyComparisonFinding], list[str], list[str]]` 본체 박제 (RESEARCH.md §"Code Examples Example 1" line 874-941 정합):

      - is_low_global 산식: `body_normalization_confidence < CATEGORY_CONF_GATE or (comparison_type == "mode3_first" and used_reference_fallback)` — `CATEGORY_CONF_GATE` 는 Plan 01 박제 (= 0.5)
      - 각 finding iterate — category 결정 (uncertain → allowed → needs_adjustment 순차 분기) + group = _resolve_joint_group(f) + (interp, recom) = render_finding_copy(f.deficit_code, category, group, comparison_type)
      - 새 BodyComparisonFinding(...) 생성 (frozen dataclass, dataclasses.replace X) — 신설 4 필드 (category / phase='hold' / body_type_interpretation=interp / recommendation=recom) + 기존 6 필드는 입력 finding 에서 복사
      - 분배: category=='body_type_allowed' → dnoc.append(recom) / 그 외 → rec_focus.append(recom)
      - return (classified, dnoc, rec_focus)

    `test_classify_findings.py` 신설 — Plan 01 박제 fixture loader (`load_finding_from_fixture`) 활용. 8 단위 test (위 `<behavior>` 박제 정합) + 9 번째 `test_no_dataclasses_replace_usage` grep test (`grep -E 'dataclasses\.replace' backend/shared/python/sunity_shared/analysis/body_normalizer.py` 가 classify_findings 함수 body 안에 0 match — body_normalizer.py source 를 Path 로 읽어 classify_findings 함수 위치 (def classify_findings ~ 다음 def) 범위 안에서 `dataclasses.replace` 0회 검증).

    per D-07-A1 + D-07-A2 + D-07-B3 + Decision 1 + D-07-U1 + D-07-C1.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && pytest backend/tests/phase07/test_classify_findings.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "def classify_findings(" backend/shared/python/sunity_shared/analysis/body_normalizer.py` >= 1
    - Source: `grep -c "def _resolve_joint_group(" backend/shared/python/sunity_shared/analysis/body_normalizer.py` >= 1
    - Source: `grep -c "_DEFICIT_TO_GROUP" backend/shared/python/sunity_shared/analysis/body_normalizer.py` >= 1
    - Source: `grep -c "_JOINT_TO_GROUP" backend/shared/python/sunity_shared/analysis/body_normalizer.py` >= 1
    - Source: `grep -c "from .copy_templates import render_finding_copy" backend/shared/python/sunity_shared/analysis/body_normalizer.py` >= 1
    - Source: `awk '/^def classify_findings\(/,/^def [a-z]/' backend/shared/python/sunity_shared/analysis/body_normalizer.py | grep -v '^def [a-z]' | grep -c 'dataclasses\.replace'` == 0 (classify_findings 함수 body 안 replace 호출 0)
    - Behavior: `pytest backend/tests/phase07/test_classify_findings.py -x` 9 test PASSES (8 분류 test + 1 grep test)
    - Behavior: `pytest backend/tests/phase06/ -x` 회귀 0 (BodyComparisonFinding Plan 01 placeholder 가 Plan 02 classify_findings 진입 전엔 영향 0)
  </acceptance_criteria>
  <done>
    classify_findings 본체 박제 완료 — pure function (numpy / boto3 / network / LLM 무관) + frozen dataclass __post_init__ Literal validator 통과 + 새 인스턴스 생성 (replace 우회 X). 9 단위 test (8 분류 + 1 grep) 모두 green. Phase 6 회귀 0. _resolve_joint_group 의 clean_lines.joint_key 분기 박제 (RESEARCH.md §"Implementation Risks 3" 박제 정합 — fallback arm).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: compare_body_profiles wiring + integration test 3종 + _dataclass_to_camel_case_dict 자동 변환 test</name>
  <files>
    backend/shared/python/sunity_shared/analysis/body_normalizer.py
    backend/tests/phase07/test_compare_body_profiles_phase7_integration.py
    backend/tests/phase07/test_dataclass_to_camel_case_dict_phase7.py
  </files>
  <read_first>
    backend/shared/python/sunity_shared/analysis/body_normalizer.py (line 1115-1297 compare_body_profiles 본체 + 6) BodyComparisonReport 조립 위치)
    .planning/phases/07-difference-classification/07-RESEARCH.md (§"Code Examples Example 2" line 944-983 compare_body_profiles wiring + §"Code Examples Example 4" line 1019-1056 camelCase 자동 변환 test)
    .planning/phases/07-difference-classification/07-PATTERNS.md (§"compare_body_profiles wiring 위치" + §"test_compare_body_profiles_phase7_integration.py" + §"test_dataclass_to_camel_case_dict_phase7.py")
    .planning/phases/07-difference-classification/07-CONTEXT.md (§Code Insights — Integration Points)
    backend/functions/pipeline/app.py (line 584-617 _snake_to_camel + _dataclass_to_camel_case_dict 5-case helper)
    backend/tests/phase06/test_compare_body_profiles.py (Phase 6 integration test 패턴)
    backend/tests/phase06/test_dataclass_to_camel_case_dict.py (Phase 6 C8 helper test 패턴)
    backend/tests/phase06/fixtures/fixture_160cm_pro_vs_140cm_student.json (Phase 6 fixture 재사용)
  </read_first>
  <behavior>
    - `compare_body_profiles()` 안에서 measure_ipsf_absolute_deficits() 호출 직후 (body_normalizer.py:1263-1269 부근, RESEARCH.md §Example 2 line 957-964 박제 위치) classify_findings() 1줄 wiring 추가:
      ```
      classified_findings, do_not_over_correct, recommended_focus = classify_findings(
          findings,
          body_normalization_confidence=confidence,
          comparison_type=comparison_type,
          used_reference_fallback=used_reference_fallback,
      )
      ```
    - BodyComparisonReport 조립 (line 1280-1297 부근) 시 `findings=classified_findings` (Plan 01 placeholder 들어간 raw findings 교체) + `do_not_over_correct=do_not_over_correct` + `recommended_focus=recommended_focus` 2 kwarg 추가
    - integration test 3종 (Phase 6 fixture `fixture_160cm_pro_vs_140cm_student.json` 재사용 — Plan 01 fixture 박제 불요):
      1. `test_compare_body_profiles_emits_classified_findings` — Phase 6 fixture 로 compare_body_profiles() 호출 → 결과 `BodyComparisonReport.findings[*].category` 모두 valid enum 3종 중 하나 + `phase=='hold'` 모두 + `body_type_interpretation` / `recommendation` 신설 필드 모두 non-None (canned string 채워짐)
      2. `test_compare_body_profiles_emits_do_not_over_correct` — `report.do_not_over_correct: list[str]` 타입 검증 + 빈 list[] 도 정상 (sweep_rtmw_20260603_1409 데이터상 needs_adjustment 빈 리스트 가능성 박제) + populated 시 모든 항목 mode prefix ("정은지 선수 영상 기준으로 보면") 시작 또는 그 외 mode prefix
      3. `test_compare_body_profiles_emits_recommended_focus` — `report.recommended_focus: list[str]` 타입 검증 + 빈 list[] 정상 + populated 시 mode prefix 시작
    - `test_dataclass_to_camel_case_dict_phase7.py` (RESEARCH.md §Example 4 line 1019-1056 박제 그대로):
      1. `test_phase7_new_fields_camel_case_automatic` — Plan 01 박제 schema (BodyComparisonFinding + BodyComparisonReport) 합성 → `_dataclass_to_camel_case_dict(report)` 호출 → 결과 dict 에 `doNotOverCorrect` + `recommendedFocus` 키 존재 + `findings[0]['category']` + `findings[0]['phase']` + `findings[0]['bodyTypeInterpretation']` + `findings[0]['recommendation']` 모두 정상 변환 (Phase 6 C8 5-case helper 자동 변환 검증)
  </behavior>
  <action>
    `body_normalizer.py compare_body_profiles()` 함수 내부 — Plan 01 Task 3 박제 위에서 measure_ipsf_absolute_deficits() 호출 직후 1 단락 추가 (`# 5.5) [Phase 7] 분류 + 카피 wiring` 주석 박제):

      ```
      classified_findings, do_not_over_correct, recommended_focus = classify_findings(
          findings,
          body_normalization_confidence=confidence,
          comparison_type=comparison_type,
          used_reference_fallback=used_reference_fallback,
      )
      ```

    BodyComparisonReport 조립 시 `findings=findings` → `findings=classified_findings` 로 교체 + `do_not_over_correct=do_not_over_correct` + `recommended_focus=recommended_focus` 2 kwarg 추가. RESEARCH.md §"Code Examples Example 2" line 970-982 박제 정합.

    `test_compare_body_profiles_phase7_integration.py` 신설 — Phase 6 `test_compare_body_profiles.py` 패턴 미러. Phase 6 fixture `fixture_160cm_pro_vs_140cm_student.json` 재사용 (Plan 01 fixture 박제 불요). 3 integration test 박제 — 위 `<behavior>` 박제 정합. 빈 list[] 도 정상 검증 (sweep_rtmw_20260603_1409 5 영상 데이터상 needs_adjustment 빈 리스트 가능성 박제 per RESEARCH.md §"Classification Rule Calibration" line 366-374).

    `test_dataclass_to_camel_case_dict_phase7.py` 신설 — RESEARCH.md §"Code Examples Example 4" line 1019-1056 박제 그대로 + Phase 6 `test_dataclass_to_camel_case_dict.py` 의 importlib reload 패턴 미러 (sys.modules.pop / importlib.reload). 신설 4+2 필드 자동 변환 검증.

    per D-07-B1 + D-07-B3 + Decision 1.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && pytest backend/tests/phase07/test_compare_body_profiles_phase7_integration.py backend/tests/phase07/test_dataclass_to_camel_case_dict_phase7.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "classified_findings, do_not_over_correct, recommended_focus = classify_findings(" backend/shared/python/sunity_shared/analysis/body_normalizer.py` >= 1
    - Source: `grep -c "findings=classified_findings" backend/shared/python/sunity_shared/analysis/body_normalizer.py` >= 1
    - Source: `grep -c "do_not_over_correct=do_not_over_correct" backend/shared/python/sunity_shared/analysis/body_normalizer.py` >= 1
    - Source: `grep -c "recommended_focus=recommended_focus" backend/shared/python/sunity_shared/analysis/body_normalizer.py` >= 1
    - Behavior: `pytest backend/tests/phase07/test_compare_body_profiles_phase7_integration.py -x` 3 test PASSES — classified_findings + do_not_over_correct + recommended_focus 모두 정상 산출
    - Behavior: `pytest backend/tests/phase07/test_dataclass_to_camel_case_dict_phase7.py -x` PASSES — 6 신설 필드 자동 camelCase 변환 검증
    - Behavior: `pytest backend/tests/phase06/ -x` 회귀 0 — Phase 6 fixture 재사용 path, classify_findings 가 placeholder findings 교체 후 정상 동작
    - Behavior: `pytest backend/tests/ -x` (전체 backend suite) all green
  </acceptance_criteria>
  <done>
    compare_body_profiles wiring 박제 완료 — measure_ipsf_absolute_deficits 호출 직후 classify_findings 1줄 + 2 kwarg 주입. 3 integration test + 1 camelCase 자동 변환 test 모두 green. Phase 6 회귀 0. Phase 6 C8 5-case helper (Phase 6 박제 패턴) 자동 활용 — 신설 4+2 필드 코드 변경 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: frontend userAnalyses.normalize() 신설 필드 graceful default + tsc clean</name>
  <files>
    app/src/lib/userAnalyses.ts
  </files>
  <read_first>
    app/src/lib/userAnalyses.ts (normalize 함수 line 27-90 본체)
    app/src/types/analysis.ts (Plan 01 박제 BodyComparisonReport + BodyComparisonFinding interface)
    .planning/phases/07-difference-classification/07-RESEARCH.md (§"Open Questions Q4" line 1102-1105 — normalize default 처리 박제)
    .planning/phases/07-difference-classification/07-PATTERNS.md (§"app/src/lib/userAnalyses.ts" — normalize 확장 박제)
    .planning/phases/07-difference-classification/07-CONTEXT.md (§Existing Code Insights — Reusable Assets)
    .planning/phases/06-coaching/06-02-SUMMARY.md (Phase 6 normalize() 박제 패턴 — bodyComparisonReport literal positive assertion)
  </read_first>
  <behavior>
    - `userAnalyses.normalize()` 가 `raw.bodyComparisonReport` 가 존재하는 path 에서 신설 2 필드 (`doNotOverCorrect` / `recommendedFocus`) 가 raw doc 에 없으면 빈 list[] default 처리. Phase 6 close-out 이전 (Plan 07 백엔드 변경 전) Firestore doc 호환 — frontend crash X
    - BodyComparisonFinding 의 신설 4 필드 (`category` / `phase` / `bodyTypeInterpretation` / `recommendation`) 는 TS interface 가 optional + non-optional 분기 (Plan 01 박제 정합 — category non-optional, 나머지 optional). normalize 가 raw 가 `category` 없을 때 임의 default 부여 X (TS interface 가 non-optional 이므로 데이터 누락 시 finding 자체 누락 또는 graceful skip 박제 — 단, Phase 7 백엔드가 항상 산출하므로 production path 에선 무관, 단지 backward-compatible safety net)
    - `tsc --noEmit` exits 0
  </behavior>
  <action>
    `app/src/lib/userAnalyses.ts` 의 `normalize()` 함수 안 bodyComparisonReport 정규화 부분 (Phase 6 박제 위치) 확장:

      - `raw.bodyComparisonReport` 가 존재할 때 추출하는 fields 에 `doNotOverCorrect: (rawReport.doNotOverCorrect as string[] | undefined) ?? []` 와 `recommendedFocus: (rawReport.recommendedFocus as string[] | undefined) ?? []` 두 줄 추가 박제 (TS optional chaining + nullish coalescing default `[]`)
      - `findings` 정규화 시 (이미 Phase 6 박제 매핑 존재) 각 finding 에 신설 4 필드 mapping 박제:
        - `category: (rawFinding.category as 'body_type_allowed' | 'needs_adjustment' | 'uncertain') ?? 'uncertain'` (raw 누락 시 graceful 'uncertain' default — 정직성 우위, 잘못된 분류 production 진입 차단)
        - `phase: (rawFinding.phase as string | null | undefined) ?? null`
        - `bodyTypeInterpretation: (rawFinding.bodyTypeInterpretation as string | null | undefined) ?? null`
        - `recommendation: (rawFinding.recommendation as string | null | undefined) ?? null`
      - 한국어 주석 박제 (Phase 6 패턴 정합 + CLAUDE.md Cross-cutting `§` shorthand): `// Phase 7 신설 필드 graceful default — Phase 7 백엔드 변경 이전 Firestore doc 호환 (per .planning/phases/07-difference-classification/07-CONTEXT.md D-07-B1)`
    
    per D-07-B1 + D-07-B3 + Decision 1 + RESEARCH.md §"Open Questions Q4" 박제.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "doNotOverCorrect" app/src/lib/userAnalyses.ts` >= 1
    - Source: `grep -c "recommendedFocus" app/src/lib/userAnalyses.ts` >= 1
    - Source: `grep -c "?? \[\]" app/src/lib/userAnalyses.ts` >= 2 (doNotOverCorrect default [] + recommendedFocus default [])
    - Source: `grep -c "category" app/src/lib/userAnalyses.ts` >= 1 (신설 finding mapping)
    - Behavior: `cd app && npx tsc --noEmit` exits 0 (TS type-check clean — Plan 01 의 TS interface 확장 정합)
    - Behavior: `grep "??" app/src/lib/userAnalyses.ts | grep -E "doNotOverCorrect|recommendedFocus" | wc -l` >= 2 (두 필드 모두 nullish coalescing default)
  </acceptance_criteria>
  <done>
    frontend userAnalyses.normalize() 의 graceful default 박제 완료 — Phase 6 close-out 이전 Firestore doc 호환 (frontend crash X) + Phase 7 백엔드 변경 이후 정상 렌더. tsc --noEmit clean. Phase 12 (실측 각도 + 키포인트 오버레이) 의 화면 분기 책임 진입 contract 박제 완료.
  </done>
</task>

</tasks>

<verification>
- `pytest backend/tests/phase07/ -x` PASSES (전체 phase07 test suite — Plan 01 박제 3 test + Plan 02 박제 4 test = 7 파일 ~15-20 test all green)
- `pytest backend/tests/phase06/ -x` 회귀 0 (Phase 6 close-out 패턴 유지, BodyComparisonFinding Plan 01 placeholder 가 Plan 02 classify_findings 통과 시 재할당)
- `pytest backend/tests/ -x` (전체 backend suite — phase06 136 + phase07 ~20 = ~156 + 1 skip) all green
- `cd app && npx tsc --noEmit` exits 0 (Plan 01 TS 확장 + Plan 02 normalize 확장 후 type-check clean)
- classify_findings 안 `dataclasses.replace` 0회 — `awk '/^def classify_findings\(/,/^def [a-z]/' backend/shared/python/sunity_shared/analysis/body_normalizer.py | grep -c 'dataclasses\.replace'` == 0
- compare_body_profiles wiring 박제 — `grep -c 'classified_findings, do_not_over_correct, recommended_focus = classify_findings(' backend/shared/python/sunity_shared/analysis/body_normalizer.py` >= 1
- Phase 6 C8 helper 자동 변환 회귀 차단 — `test_dataclass_to_camel_case_dict_phase7.py` PASSES
- frontend graceful default — userAnalyses.normalize() 에 nullish coalescing default `?? []` 두 줄 박제
- per CLAUDE.md §7 (이모지 0, 슬롭 코드 0) + 박제 메모 [[no-baekje-filler]] (한국어 주석 안 "박제" 단어 남용 X — 의미 있는 사양 인용만)
- per [[analysis-objectivity-no-human-scores]] — classify_findings 분류 룰이 belle/강사/심사자 점수 라벨 무관 (`body_type_adjusted` + `deduction_score` + `confidence` 만 사용)
- per [[mvp-simple-pilot-quality]] — 캔드 우선 + LLM 풍부화는 Phase 11 후속 (Phase 7 = LLM 호출 0)
- per [[mode3-progress-not-similarity]] — mode3_progress canned string prefix "이전 영상 대비" 박제 (Plan 01) + classify_findings 가 mode 분기 carry-over (used_reference_fallback 신호 활용)
</verification>

<success_criteria>
1. **classify_findings 본체 + 9 단위 test PASS**: pure function 박제 (numpy / boto3 / network / LLM 무관) + frozen dataclass __post_init__ Literal validator 통과 + dataclasses.replace 우회 X → `test_classify_findings.py` 9 test (8 분류 + 1 grep) all PASS
2. **pipeline wiring + 3 integration test PASS**: compare_body_profiles() 안 classify_findings 1줄 wiring + BodyComparisonReport 조립 시 2 kwarg 주입 + Phase 6 fixture 재사용 integration → `test_compare_body_profiles_phase7_integration.py` 3 test PASS
3. **_dataclass_to_camel_case_dict 자동 변환 회귀 차단**: Phase 6 C8 5-case helper 자동 활용 (코드 변경 0) → `test_dataclass_to_camel_case_dict_phase7.py` PASS — `doNotOverCorrect` / `recommendedFocus` / `bodyTypeInterpretation` 모두 정상 변환
4. **frontend graceful default**: userAnalyses.normalize() 에 신설 2 필드 (doNotOverCorrect / recommendedFocus) default `[]` + 신설 finding 4 필드 graceful 처리 → `tsc --noEmit` clean
5. **Phase 6 회귀 0**: `pytest backend/tests/phase06/ -x` PASSES (BodyComparisonFinding placeholder Plan 01 박제 + classify_findings 재할당 = Phase 6 path 정합)
6. **백엔드 전체 PASS**: `pytest backend/tests/ -x` (phase06 136 + phase07 ~20 = ~156 + 1 skip) all green
7. **mode3_first fallback Decision 1 박제**: classify_findings 가 `used_reference_fallback=True` 인 mode3_first path 에서 모든 finding `uncertain` demotion → `test_uncertain_when_mode3_first_fallback` PASS
8. **박제 메모 정합**: [[analysis-objectivity-no-human-scores]] (분류 룰 사람 점수 라벨 무관) + [[mvp-simple-pilot-quality]] (Phase 7 LLM 호출 0, Phase 11 후속) + [[mode3-progress-not-similarity]] (mode3_progress prefix carry-over) — 모두 단위 test 또는 grep gate 로 backstop
9. **Phase 7 ROADMAP 4 success criteria 박제 완료** (Plan 01 + Plan 02 합산):
   - SC #1 (category 별 산출): classify_findings 9 test + integration 3 test
   - SC #2 (doNotOverCorrect / recommendedFocus 출력): test_compare_body_profiles_emits_do_not_over_correct + test_compare_body_profiles_emits_recommended_focus
   - SC #3 (confidence < 0.5 → uncertain): test_uncertain_when_report_low_confidence + test_uncertain_when_finding_low_confidence
   - SC #4 (금지 표현 grep gate): Plan 01 test_copy_templates_no_forbidden.py 9 parametrize PASS
</success_criteria>

<output>
Create `.planning/phases/07-difference-classification/07-02-SUMMARY.md` upon completion. Include:
- classify_findings 본체 박제 위치 (body_normalizer.py 줄 번호) + _resolve_joint_group / _DEFICIT_TO_GROUP / _JOINT_TO_GROUP 위치
- compare_body_profiles wiring 1줄 추가 위치 (5.5 분기 박제) + BodyComparisonReport 조립 2 kwarg 주입
- 9 단위 test + 3 integration test + 1 camelCase 자동 변환 test PASS 결과
- Phase 6 회귀 0 확인 (pytest backend/tests/phase06/ all green)
- 전체 backend suite (pytest backend/tests/) 결과 ~156 PASS + 1 skip
- frontend userAnalyses.normalize() graceful default 박제 + tsc --noEmit clean
- mode3_first + used_reference_fallback Decision 1 박제 결과 (모든 finding uncertain demotion)
- Phase 7 ROADMAP 4 success criteria 달성 매핑 (SC#1-#4)
- 단위 test 커버리지: classify_findings 9 + integration 3 + camelCase 1 + Plan 01 grep gate 9 + Plan 01 render 5 + Plan 01 lockstep 6 = ~33 test
</output>
