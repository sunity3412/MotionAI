---
phase: 08-jerk-jitter
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/shared/python/sunity_shared/analysis/pole_geometry.py
  - backend/shared/python/sunity_shared/analysis/body_scale.py
  - backend/shared/python/sunity_shared/analysis/pose_frame.py
  - app/src/types/analysis.ts
  - docs/contract.md
  - backend/tests/phase08/__init__.py
  - backend/tests/phase08/conftest.py
  - backend/tests/phase08/fixtures/__init__.py
  - backend/tests/phase08/fixtures/_factory.py
  - backend/tests/phase08/test_pole_geometry_contract.py
  - backend/tests/phase08/test_body_scale_helper.py
  - backend/tests/phase08/test_preflight_label_schema.py
  - .planning/phases/08-jerk-jitter/preflight/PREFLIGHT-LABEL-SPEC.md
  - .planning/phases/08-jerk-jitter/preflight/preflight_label_template.csv
autonomous: true
requirements: [FORCE-01]
subsystem: backend-analysis-core
tags: [phase-08, coordinate-contract, scale-contract, preflight-gate, wave-0-infra, reviews-cycle-1]

scope_sanity_note: "신설 plan — Codex review cycle 1 의 R1 (PoleAxis position 부재), R2 (torsoScale 오용), R3 (contact primitive 불명확) 3 HIGH blocker 해소를 위해 reviewer 권고대로 thin 08-00 plan 박제. 14 파일 중 8 개는 test/fixture/preflight 데이터. 실제 신설 소스 = pole_geometry.py + body_scale.py 2 모듈 (pure-function) + pose_frame.py PoleAxisMeasurement 신설 + 3-way contract 박제 (TS + docs §9.0). Plan 08-01/02/03 가 본 plan 의 contract 위에 박제. dimensions.stability_wobble helper / contact_points.yaml / fixture JSON 박제는 08-01 로 이동 (본 plan = coordinate/scale + preflight 만)."

must_haves:
  truths:
    - "본 plan 의 단일 목적 = Phase 8 의 두 미해결 contract (pole position + observed length denominator) 박제 + contact primitive 분류 + Layer-1 pre-flight 25-timestamp label spec. Plan 08-01/02/03 가 본 plan 의 contract dependency. per REVIEWS R1 + R2 + R3 + R4 + R5."
    - "PoleLine2D / PoleAxisMeasurement 신설 — PoleAxis 는 direction 만 박제 (pose_frame.py 박제 본체 유지, 회귀 0). 본 plan 의 PoleAxisMeasurement = (line: PoleLine2D | None, axis_3d: PoleAxis, coordinate_space, confidence, source). axis distance 계산은 coordinate_space='image_2d' (line 가용 시) 또는 'unavailable' (line 미가용 시 → distance 필드 = null) 으로 명시. per REVIEWS R1 blocker 해소."
    - "PoleLine2D = (point_image: (x, y), direction_image: (dx, dy), confidence, source). image 좌표계 (normalized 0~1) 박제 — Phase 1 의 HoughPoleDetector 가 이미 image 평면에서 검출 (`pole/detector.py` 박제 정합). axis distance 산출 = 점-직선 거리 공식 (image 2D), 추후 aligned-3D 로 lift 필요 시 별도 plan."
    - "median_torso_length(pose_frames, *, space) helper 신설 — observed shoulder-hip midpoint distance 의 median (frame 단위). space='image_2d' (Keypoint2D 사용) / 'world_3d' (Keypoint3D 사용) / 'pole_aligned' (Keypoint3DAligned 사용). 모든 axis 거리 metric 의 denominator 박제. **`BodyNormalizationProfile.torso_scale` 사용 금지** (ratio 1.0 self-reference, observed length 아님) — REVIEWS R2 blocker 해소."
    - "ContactPrimitiveKind enum 신설 — Literal['keypoint', 'segment', 'region_proxy']. 12 contact point 박제 시 각 entry 가 kind 명시:\n  · keypoint: left_hand / right_hand / left_foot / right_foot / left_ankle / right_ankle / left_knee / right_knee (COCO-17 직접 가용)\n  · segment: left_inner_thigh / right_inner_thigh (left_hip↔left_knee / right_hip↔right_knee 의 mid-segment 거리)\n  · region_proxy: hip (left_hip+right_hip midpoint)\nyaml 박제 시 kind 필드 동행 — per REVIEWS R3 blocker 해소."
    - "coordinateSpace 필드 신설 (3종 metric 모두) — Literal['image_2d', 'pole_aligned', 'world_3d', 'unavailable']. axis distance 와 contact distance metric 이 어느 공간에서 측정됐는지 명시. 'unavailable' 시 numeric 필드 null + warning 'pole_line_missing' / 'scale_unavailable'. per REVIEWS R1 + R2 + Suggestions §1."
    - "Pre-flight 25-timestamp label spec 박제 — `.planning/phases/08-jerk-jitter/preflight/PREFLIGHT-LABEL-SPEC.md` (체크리스트 + 라벨 schema) + `preflight_label_template.csv` (5영상 × 5 phase = 25 timestamp 입력란). belle 운영 작업 — Layer 1 휴리스틱 검증 source. ≥80% (±200ms) 일치 시 Layer-1 confidence='medium' 승급. 미만 시 confidence='low' 박제 강제 — per REVIEWS R4."
    - "본 plan 의 contract 활성 진입 차단 = pre-flight label 미완료 시 Plan 08-03 의 manual checkpoint (Task 3) 가 `RECOGNIZER_BACKEND` env unset + `FORCE_SIGNALS_LAYER2_ENABLED` env unset 으로 force_signals 산출 차단 가능 — analysis pipeline 자체는 영향 0 (D-08-E3 정합, REVIEWS R4 권고 정합)."
    - "Wave 0 test 인프라 (phase08/__init__.py + conftest.py + fixtures/__init__.py + _factory.py) 박제 — Plan 08-01 의 fixture JSON 박제 진입 차단 해소. 본 plan 의 fixture factory 는 **programmatic** (dataclass 직접 인스턴스화) — JSON 박제는 Plan 08-01 가 본 factory 위에 박제. REVIEWS R9 권고 (programmatic factories 우선) 정합."
    - "_factory.py 의 PoseFrame 박제 시그너처 = `PoseFrame(frame_index=..., timestamp_ms=..., keypoints_3d=..., keypoints_3d_pole_aligned=..., keypoints_2d=..., pole_axis=..., reliability=...)`. Keypoint3DAligned 박제 = `Keypoint3DAligned(x=, y=, z=)` (visibility 필드 없음, REVIEWS H1 dataclass mismatch 해소). BodyNormalizationProfile 박제 시 `asymmetry` 필드 X — 7 필드 (estimated_height_scale / arm_scale / leg_scale / torso_scale / shoulder_hip_ratio / confidence / warnings) 만 박제 (REVIEWS H1 dataclass mismatch 해소)."
    - "pose_frame.py 의 PoleAxis 박제 본체 변경 0 — 기존 lockstep 회귀 0. PoleAxisMeasurement / PoleLine2D 는 pole_geometry.py 신설 모듈에 박제 (pose_frame.py import). Phase 1 의 axis_vector 박제 source 유지."
    - "3-way contract 박제 (Plan 08-01 의 lockstep 패턴 정합) — TS interface (analysis.ts) 의 PoleLine2D / PoleAxisMeasurement / ContactPrimitiveKind / CoordinateSpace 박제 + docs/contract.md §9.0 (Coordinate/Scale Contract) 신설 + Python 본체 (pole_geometry.py + body_scale.py) atomic commit. Plan 08-01 가 본 contract 위에 ForceSignalsReport schema 박제."
  artifacts:
    - path: backend/shared/python/sunity_shared/analysis/pole_geometry.py
      provides: "PoleLine2D + PoleAxisMeasurement 신설 박제 + 점-직선 거리 helper (image 2D 평면). pure-function (numpy only). pose_frame.PoleAxis import (axis_3d 박제). per REVIEWS R1 blocker 해소."
      contains:
        - "from .pose_frame import PoleAxis"
        - "@dataclass(frozen=True)"
        - "class PoleLine2D"
        - "class PoleAxisMeasurement"
        - "CoordinateSpace = Literal"
        - "image_2d"
        - "pole_aligned"
        - "unavailable"
        - "def point_to_pole_line_distance_2d"
        - "def build_pole_axis_measurement"
    - path: backend/shared/python/sunity_shared/analysis/body_scale.py
      provides: "median_torso_length helper 신설 — observed shoulder-hip midpoint distance median. space='image_2d' / 'world_3d' / 'pole_aligned' 분기. BodyNormalizationProfile.torso_scale 사용 금지 박제 (ratio self-reference, observed length 아님). per REVIEWS R2 blocker 해소."
      contains:
        - "def median_torso_length"
        - "image_2d"
        - "pole_aligned"
        - "world_3d"
        - "scale_unavailable"
        - "shoulder"
        - "hip"
    - path: backend/shared/python/sunity_shared/analysis/pose_frame.py
      provides: "변경 0 (기존 박제 본체 유지). 본 plan 의 PoleAxisMeasurement / PoleLine2D 는 pole_geometry.py 신설 모듈에 박제 — PoleAxis 본체는 unchanged. Plan 01 박제 lockstep 회귀 0 강제."
      contains:
        - "class PoleAxis"
        - "axis_vector"
    - path: app/src/types/analysis.ts
      provides: "TS contract 박제 — PoleLine2D / PoleAxisMeasurement / ContactPrimitiveKind / CoordinateSpace 신설 interface + type. Plan 08-01 의 ForceSignalsReport schema 박제 source. AnalysisDoc 영향 0 (sub-contract 박제만)."
      contains:
        - "export type CoordinateSpace ="
        - "export type ContactPrimitiveKind ="
        - "export interface PoleLine2D"
        - "export interface PoleAxisMeasurement"
        - "image_2d"
        - "pole_aligned"
        - "unavailable"
        - "keypoint"
        - "segment"
        - "region_proxy"
    - path: docs/contract.md
      provides: "§9.0 Coordinate/Scale Contract (Plan 08-00 신설 — Phase 8 pre-contract). PoleLine2D / PoleAxisMeasurement / CoordinateSpace / ContactPrimitiveKind / median_torso_length 정의 + Layer 1 pre-flight label gate 박제. §9 (Plan 08-01 ForceSignalsReport) 의 의존 contract."
      contains:
        - "## §9.0. Coordinate/Scale Contract"
        - "PoleLine2D"
        - "PoleAxisMeasurement"
        - "CoordinateSpace"
        - "ContactPrimitiveKind"
        - "median_torso_length"
        - "image_2d"
        - "preflight"
    - path: backend/tests/phase08/__init__.py
      provides: "phase08 test 패키지 marker (Phase 7 패턴 정합, 빈 파일)."
      contains: []
    - path: backend/tests/phase08/conftest.py
      provides: "phase08 공용 pytest fixture — PHASE08_FIXTURES_DIR 상수 + synthetic_pose_frames factory fixture (programmatic, REVIEWS R9 권고 정합) + synthetic_body_profile (BodyNormalizationProfile 7 필드 박제 — asymmetry 없음, REVIEWS H1 해소) + synthetic_pole_axis_measurement (PoleLine2D + PoleAxis 박제). Plan 08-01 의 JSON fixture 박제 진입 차단 해소."
      contains:
        - "PHASE08_FIXTURES_DIR"
        - "synthetic_pose_frames"
        - "synthetic_body_profile"
        - "synthetic_pole_axis_measurement"
        - "frame_index"
        - "import pytest"
    - path: backend/tests/phase08/fixtures/_factory.py
      provides: "programmatic factory helper — make_pose_frame(frame_index, timestamp_ms, keypoints_3d_pole_aligned, reliability) + make_keypoint3d_aligned(x, y, z) + make_body_profile(**kwargs) + make_pole_axis_measurement(image_line, axis_vector, confidence_level, source). 모든 박제 dataclass 의 정확한 시그너처 박제 — REVIEWS H1 (dataclass mismatch) 영구 차단. JSON 박제는 Plan 08-01 가 본 factory 위에 박제."
      contains:
        - "def make_pose_frame"
        - "def make_keypoint3d_aligned"
        - "def make_body_profile"
        - "def make_pole_axis_measurement"
        - "frame_index"
        - "estimated_height_scale"
    - path: backend/tests/phase08/test_pole_geometry_contract.py
      provides: "PoleLine2D + PoleAxisMeasurement contract test — frozen=True + 점-직선 거리 공식 검증 (analytical sanity) + coordinate_space='unavailable' 시 distance 산출 None 분기 박제."
      contains:
        - "def test_pole_line_2d_frozen"
        - "def test_pole_axis_measurement_unavailable_returns_none_distance"
        - "def test_point_to_pole_line_distance_2d_analytical"
        - "def test_build_pole_axis_measurement_image_line_present"
    - path: backend/tests/phase08/test_body_scale_helper.py
      provides: "median_torso_length helper test — observed shoulder-hip midpoint distance 산출 검증. space='image_2d' / 'world_3d' / 'pole_aligned' 3 분기 + missing keypoint 시 None 반환 + scale_unavailable warning 검증."
      contains:
        - "def test_median_torso_length_image_2d"
        - "def test_median_torso_length_pole_aligned"
        - "def test_median_torso_length_missing_keypoints_returns_none"
        - "def test_torso_scale_is_not_used_as_denominator"
    - path: backend/tests/phase08/test_preflight_label_schema.py
      provides: "Pre-flight CSV schema test — preflight_label_template.csv 의 column 박제 (video_id, motion_id, phase, timestamp_ms_belle, timestamp_ms_layer1, delta_ms, agreed) + 5영상 × 5 phase = 25 row 박제 (header 제외). belle 운영 작업 진입 차단 해소."
      contains:
        - "def test_template_csv_header"
        - "def test_template_csv_25_rows"
        - "def test_label_spec_md_exists"
    - path: .planning/phases/08-jerk-jitter/preflight/PREFLIGHT-LABEL-SPEC.md
      provides: "Layer-1 휴리스틱 검증 gate 운영 docs. belle 가 5영상 (ref-invert / ref-foxtop / ref-foxtop-split / ref-climb / ref-sideway-spin) × 5 phase boundary (entry start / lock start / transition start / final_shape start / hold start) 의 timestamp 라벨링 후 Layer 1 산출과 ±200ms 일치 비율 산출. ≥80% PASS → Layer 1 confidence='medium' 승급. 미만 → Layer 1 confidence='low' 강제. Plan 08-03 Task 3 manual checkpoint 의 진입 차단 해소 source. per REVIEWS R4."
      contains:
        - "25-timestamp"
        - "200ms"
        - "80%"
        - "ref-invert"
        - "preflight_label_template.csv"
    - path: .planning/phases/08-jerk-jitter/preflight/preflight_label_template.csv
      provides: "Pre-flight 25-timestamp 라벨링 입력 template — CSV (video_id, motion_id, phase, timestamp_ms_belle, timestamp_ms_layer1, delta_ms, agreed). header 1줄 + 25 row (5영상 × 5 phase) 박제. belle 가 본 파일 채워 PASS/FAIL 판정."
      contains:
        - "video_id,motion_id,phase,timestamp_ms_belle,timestamp_ms_layer1,delta_ms,agreed"
        - "ref-invert"
        - "ref-foxtop"
        - "ref-climb"
  key_links:
    - from: backend/shared/python/sunity_shared/analysis/pole_geometry.py
      to: backend/shared/python/sunity_shared/analysis/pose_frame.py
      via: "PoleAxisMeasurement 가 PoseFrame.pole_axis 의 PoleAxis 를 axis_3d 필드로 박제 — Phase 1 의 vertical_fallback / detected source 메타 보존. PoleAxis 본체 변경 0 (회귀 0)."
      pattern: "PoleAxis"
    - from: backend/shared/python/sunity_shared/analysis/body_scale.py
      to: backend/shared/python/sunity_shared/analysis/pose_frame.py
      via: "median_torso_length(pose_frames, *, space) — PoseFrame.keypoints_3d / keypoints_3d_pole_aligned / keypoints_2d 의 left_shoulder/right_shoulder/left_hip/right_hip lookup. shoulder midpoint - hip midpoint Euclidean median 박제."
      pattern: "keypoints_3d|keypoints_2d|left_shoulder|right_hip"
    - from: app/src/types/analysis.ts
      to: docs/contract.md
      via: "3-way contract — TS PoleLine2D / PoleAxisMeasurement / ContactPrimitiveKind / CoordinateSpace ↔ docs §9.0 표 atomic commit. Plan 08-01 의 ForceSignalsReport (§9) 가 본 §9.0 contract 참조."
      pattern: "PoleLine2D|PoleAxisMeasurement|CoordinateSpace|ContactPrimitiveKind"
    - from: .planning/phases/08-jerk-jitter/preflight/PREFLIGHT-LABEL-SPEC.md
      to: backend/shared/python/sunity_shared/analysis/force_signals.py
      via: "pre-flight 25-timestamp label PASS (≥80%) → Layer 1 confidence='medium' 승급. 미만 → confidence='low' 강제. Plan 08-02 의 compute_phase_boundaries 본체 박제 시 본 gate 결과 박제 (env flag / config). 본 plan 은 spec + template 만 박제."
      pattern: "preflight|confidence"
---

<objective>
Phase 8 vertical slice (0/3) — **REVIEWS cycle 1 BLOCKER 해소 thin contract plan**. Codex 가 호출한 3 HIGH blocker (R1 PoleAxis position 부재 / R2 torsoScale 오용 / R3 contact primitive 불명확) 와 Layer-1 validation gate 부재 (R4) 를 본 plan 단일로 박제. Plan 08-01/02/03 가 본 plan 의 contract 위에 박제.

(a) **PoleLine2D + PoleAxisMeasurement 신설** — image 2D 평면 직선 박제 (point + direction) + axis_3d (Phase 1 PoleAxis 박제 유지) + coordinate_space enum (image_2d / pole_aligned / world_3d / unavailable). axis distance 계산 = 점-직선 거리 공식 (image 2D), line 미가용 시 distance=null + warning. **REVIEWS R1 blocker 해소.**
(b) **median_torso_length helper 신설** — observed shoulder-hip midpoint Euclidean median. space 분기 (image_2d / world_3d / pole_aligned). BodyNormalizationProfile.torso_scale (ratio 1.0 self-reference) 영구 사용 금지. **REVIEWS R2 blocker 해소.**
(c) **ContactPrimitiveKind enum + 12 contact 분류** — keypoint / segment / region_proxy 3 kind 박제. yaml 의 expected_contact_points entry 가 kind 필드 동행 (Plan 08-01 yaml 박제 시 정합). **REVIEWS R3 blocker 해소.**
(d) **Pre-flight 25-timestamp label spec + CSV template** — belle 운영 작업 박제. Layer 1 confidence 박제 source. **REVIEWS R4 blocker 해소.**
(e) **Wave 0 test 인프라 신설** — programmatic factory (REVIEWS R9 권고 정합) + 정확한 PoseFrame / Keypoint3DAligned / BodyNormalizationProfile dataclass 시그너처 박제. **REVIEWS H1 (dataclass mismatch) 영구 차단.**
(f) **3-way contract atomic commit** — TS interface + docs §9.0 + Python 본체 단일 commit. Plan 08-01 박제 패턴 정합.

Purpose: REVIEWS cycle 1 의 9 HIGH concern 중 4 blocker (R1/R2/R3/R4) + H1 (dataclass fixture mismatch) 영구 해소. Plan 08-01 진입 차단 해소.
Output: pole_geometry.py + body_scale.py 신설 + analysis.ts 박제 + docs/contract.md §9.0 신설 + Wave 0 test 인프라 + preflight spec + CSV template.

**박제 메모 정합:**
- [[single-camera-first-multi-view-last]] — coordinate_space='image_2d' 우선 박제 (Phase 1 의 HoughPoleDetector image 평면 검출 정합). pole_aligned 3D 는 future plan.
- [[mvp-simple-pilot-quality]] — line 미가용 시 distance=null + warning 박제 (분석 죽지 않음 / graceful degrade).
- [[firestore-nested-array-flat]] — PoleAxisMeasurement dataclass 의 line: PoleLine2D | None 박제 시 flat dict 변환 정합 (Plan 08-01 lockstep 박제).
- [[scoring-dimensions-ipsf]] — pre-flight 25-timestamp label gate = belle 운영 검증 (human label 은 belle 운영 박제, ground-truth score 영구 금지 정합 — REVIEWS Suggestions §2 정합).
- [[analysis-objectivity-no-human-scores]] — 25-timestamp label = phase boundary 시각 박제 (수치 score 아님). [Codex Suggestions: domain/IPSF-inspired thresholds OK as starting hypotheses. 정은지 distributions = sanity checks only, NOT calibration ground truth.]
- [[no-baekje-filler]] — 본 plan 박제 표현 남용 X.

per REVIEWS Cycle 1 R1 + R2 + R3 + R4 (HIGH blockers) + H1 (dataclass mismatch) + R9 (programmatic factories) + Suggestions §1~§3.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-jerk-jitter/08-CONTEXT.md
@.planning/phases/08-jerk-jitter/08-RESEARCH.md
@.planning/phases/08-jerk-jitter/08-REVIEWS.md
@.planning/phases/08-jerk-jitter/08-DIRECT-REVIEW.md
@.planning/phases/08-jerk-jitter/08-VALIDATION.md
@CLAUDE.md
@docs/contract.md
@app/src/types/analysis.ts
@backend/shared/python/sunity_shared/analysis/pose_frame.py
@backend/shared/python/sunity_shared/analysis/body_normalization.py
@backend/shared/python/sunity_shared/analysis/pole/detector.py
@backend/shared/python/sunity_shared/analysis/pole/aligner.py
@backend/functions/pipeline/app.py
@backend/tests/phase07/conftest.py
@backend/tests/phase07/fixtures/_factory.py
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Phase 8 = backend 분석 코어 layer. 본 plan = contract + helper + test infra + preflight spec. 사용자 입력 영역 0. preflight CSV = belle 운영 박제. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-00-01 | T (Tampering / schema drift) | PoleLine2D / PoleAxisMeasurement / CoordinateSpace 3-way lockstep | mitigate | TS + docs §9.0 + Python 본체 단일 atomic commit (Plan 08-01 박제 lockstep 패턴 정합). test_pole_geometry_contract.py 가 검증. |
| T-08-00-02 | T (Tampering / observed length mis-derivation) | median_torso_length helper | mitigate | space enum (image_2d / world_3d / pole_aligned) 강제 박제 + BodyNormalizationProfile.torso_scale 사용 금지 정책 박제 (REVIEWS R2). test_torso_scale_is_not_used_as_denominator 가 검증 — body_scale.py 가 BodyNormalizationProfile import X 강제. |
| T-08-00-03 | T (Tampering / contact primitive 잘못 분류) | ContactPrimitiveKind enum + 12 contact entry | mitigate | docs §9.0 의 12 contact entry 표 박제 + Plan 08-01 yaml 박제 시 kind 필드 동행 강제. test 가 kind 분류 정합 검증 (Plan 08-01 yaml load test 가 본 박제 참조). |
| T-08-00-04 | I (Information disclosure / preflight CSV PII) | preflight_label_template.csv + PREFLIGHT-LABEL-SPEC.md | accept | CSV 박제 column = video_id (reference 식별자) + motion_id + phase + timestamp_ms. 사용자 영상 path / PII 박제 X. repo 박제 가능. |
| T-08-00-05 | D (DoS / Phase 1 pose_frame.py 회귀) | PoleAxis 본체 변경 시 Phase 1 lockstep test 회귀 | mitigate | 본 plan 의 pose_frame.py 변경 = 0 (PoleAxis 본체 unchanged). PoleAxisMeasurement / PoleLine2D 는 신설 모듈 pole_geometry.py 에 박제. test 가 phase01 lockstep 회귀 0 검증 (`pytest backend/tests/phase01/ -x`). |
| T-08-00-SC | Tampering | npm/pip/cargo installs | mitigate | 신규 의존성 0 — 기존 환경 (numpy + dataclass 박제). slopcheck 불요. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pole_geometry.py + body_scale.py 신설 + Wave 0 test 인프라 (phase08/ + conftest + _factory) — programmatic dataclass 박제</name>
  <files>
    backend/shared/python/sunity_shared/analysis/pole_geometry.py
    backend/shared/python/sunity_shared/analysis/body_scale.py
    backend/tests/phase08/__init__.py
    backend/tests/phase08/conftest.py
    backend/tests/phase08/fixtures/__init__.py
    backend/tests/phase08/fixtures/_factory.py
    backend/tests/phase08/test_pole_geometry_contract.py
    backend/tests/phase08/test_body_scale_helper.py
  </files>
  <read_first>
    backend/shared/python/sunity_shared/analysis/pose_frame.py (PoleAxis 박제 본체 직접 — axis_vector / confidence_level / source / frame_index 4 필드 정확히 mirror)
    backend/shared/python/sunity_shared/analysis/body_normalization.py (BodyNormalizationProfile 7 필드 정확히 mirror — asymmetry 필드 없음)
    backend/shared/python/sunity_shared/analysis/pole/detector.py (Phase 1 박제 image 평면 Hough 검출 — PoleLine2D 박제 정합)
    backend/shared/python/sunity_shared/analysis/pole/aligner.py (Phase 1 박제 회전행렬 — pole_aligned 좌표계 박제 정합)
    backend/tests/phase07/__init__.py
    backend/tests/phase07/conftest.py (phase07 conftest 패턴 mirror)
    backend/tests/phase07/fixtures/_factory.py (Phase 7 programmatic factory 패턴 mirror)
    .planning/phases/08-jerk-jitter/08-REVIEWS.md §"Concerns" + §"Newly Surfaced Concerns" (H1 dataclass mismatch)
  </read_first>
  <behavior>
    - **pole_geometry.py 신설** — 4 박제:
      * 모듈 docstring: "Plan 08-00 신설. PoleLine2D + PoleAxisMeasurement + CoordinateSpace enum + 점-직선 거리 helper. REVIEWS R1 blocker 해소 — PoleAxis 의 direction-only 한계를 image 2D line position 박제로 보강. pose_frame.py 의 PoleAxis 본체 변경 0 (Phase 1 lockstep 회귀 0)."
      * Imports: `from __future__ import annotations`, `from dataclasses import dataclass`, `from typing import Literal`, `import numpy as np`, `from .pose_frame import PoleAxis`
      * `CoordinateSpace = Literal["image_2d", "pole_aligned", "world_3d", "unavailable"]`
      * `@dataclass(frozen=True) class PoleLine2D` 필드:
        - `point_image: tuple[float, float]`  # (x, y) normalized 0~1, image 평면
        - `direction_image: tuple[float, float]`  # (dx, dy) unit vector
        - `confidence: float`  # 0.0 ~ 1.0
        - `source: Literal["detected", "vertical_fallback"]`
        - `__post_init__` — direction_image norm ≈ 1.0 검증 (atol 1e-3), confidence 범위 [0,1] 검증.
      * `@dataclass(frozen=True) class PoleAxisMeasurement` 필드:
        - `axis_3d: PoleAxis`  # Phase 1 박제 본체 reuse (direction-only 메타)
        - `line: PoleLine2D | None`  # image 2D line position — Hough 검출 시 박제, fallback 시 None
        - `coordinate_space: CoordinateSpace`  # axis distance 산출 공간. line None 시 'unavailable'
        - `frame_index: int | None = None`  # video-level (Phase 1 D-10 정합) 시 None
        - `__post_init__` — line None 일 때 coordinate_space 가 'unavailable' 강제. line 박제 시 coordinate_space != 'unavailable' 강제.
      * `def point_to_pole_line_distance_2d(point: tuple[float, float], line: PoleLine2D) -> float` — image 2D 점-직선 수직거리 공식 (`|((x-x0)*dy - (y-y0)*dx)|` — direction_image 가 unit vector 가정 박제). NaN 입력 시 NaN 반환 (graceful).
      * `def build_pole_axis_measurement(axis_3d: PoleAxis, line: PoleLine2D | None, *, frame_index: int | None = None) -> PoleAxisMeasurement` — coordinate_space 자동 결정 (line 가용 시 'image_2d', 미가용 시 'unavailable'). future plan 이 pole_aligned 3D 활성 시 본 helper 가 lift 박제.
    - **body_scale.py 신설** — 단일 helper:
      * 모듈 docstring: "Plan 08-00 신설. median_torso_length helper — observed shoulder-hip midpoint Euclidean median. REVIEWS R2 blocker 해소 — BodyNormalizationProfile.torso_scale (ratio 1.0 self-reference) 영구 사용 금지. axis distance / contact distance metric 의 denominator 박제."
      * Imports: `from __future__ import annotations`, `from typing import Literal`, `import numpy as np`, `from .pose_frame import PoseFrame` (TYPE_CHECKING 아래)
      * **Critical: BodyNormalizationProfile import 영구 금지** — 본 모듈은 observed length 박제 source 단일. body_normalization.py import 금지 박제 (drift defense).
      * `Space = Literal["image_2d", "pole_aligned", "world_3d"]`
      * `def median_torso_length(pose_frames: list[PoseFrame], *, space: Space) -> float | None`
        - space='image_2d': PoseFrame.keypoints_2d 의 left_shoulder/right_shoulder/left_hip/right_hip lookup
        - space='pole_aligned': PoseFrame.keypoints_3d_pole_aligned 의 동일 4 keypoint lookup
        - space='world_3d': PoseFrame.keypoints_3d 의 동일 4 keypoint lookup
        - 각 frame 별 shoulder midpoint = (left_shoulder + right_shoulder) / 2, hip midpoint = (left_hip + right_hip) / 2 산출 → Euclidean distance 산출 → 모든 frame median 반환.
        - 4 keypoint 중 하나라도 missing 인 frame → 해당 frame skip (NaN 처리)
        - valid frame 수 < 5 시 None 반환 (warning 'scale_unavailable' source 박제 — caller 가 warning 추가)
    - **phase08/__init__.py + fixtures/__init__.py** — 빈 패키지 marker (Phase 7 패턴 정합).
    - **phase08/conftest.py** 박제:
      * `PHASE08_FIXTURES_DIR: Path = Path(__file__).resolve().parent / "fixtures"` 상수
      * `synthetic_pose_frames` pytest fixture — _factory.make_pose_frame 호출해서 60 frame 리스트 박제. PoseFrame(frame_index=..., timestamp_ms=...) 정확히 박제 (REVIEWS H1 해소 — `frame_idx` 아님).
      * `synthetic_body_profile` pytest fixture — _factory.make_body_profile() 호출. BodyNormalizationProfile 7 필드 (estimated_height_scale / arm_scale / leg_scale / torso_scale / shoulder_hip_ratio / confidence / warnings) 박제. asymmetry 필드 박제 X (REVIEWS H1 해소).
      * `synthetic_pole_axis_measurement` pytest fixture — _factory.make_pole_axis_measurement() 호출. PoleAxisMeasurement(line=PoleLine2D(...), axis_3d=PoleAxis(...), coordinate_space='image_2d', frame_index=None) 박제.
    - **phase08/fixtures/_factory.py** 박제 (programmatic factory, REVIEWS R9 권고 정합):
      * `def make_keypoint3d_aligned(x: float, y: float, z: float) -> Keypoint3DAligned` — visibility 필드 박제 X (REVIEWS H1 해소). 3 필드만.
      * `def make_keypoint3d(x: float, y: float, z: float, *, confidence: float = 1.0) -> Keypoint3D` — confidence + uncertainty_proxy 자동 박제 (`uncertainty_proxy=1.0-confidence`).
      * `def make_pose_frame(frame_index: int, timestamp_ms: int, *, keypoints_3d_pole_aligned: dict | None = None, keypoints_3d: dict | None = None, keypoints_2d: dict | None = None, pole_axis: PoleAxis | None = None, reliability: str = "high") -> PoseFrame` — PoseFrame(frame_index=..., timestamp_ms=..., ...) 정확히 박제. `frame_idx` 박제 X (REVIEWS H1 영구 차단).
      * `def make_body_profile(*, estimated_height_scale: float = 1.0, arm_scale: float = 1.0, leg_scale: float = 1.0, torso_scale: float = 1.0, shoulder_hip_ratio: float = 1.0, confidence: float = 1.0, warnings: list[str] | None = None) -> BodyNormalizationProfile` — 7 필드만. asymmetry 박제 X.
      * `def make_pole_line_2d(*, point_image: tuple = (0.5, 0.5), direction_image: tuple = (0.0, 1.0), confidence: float = 0.9, source: str = "detected") -> PoleLine2D` — vertical pole 박제 default (image y down 가정 → direction (0, 1)).
      * `def make_pole_axis(*, axis_vector: tuple = (0.0, 1.0, 0.0), confidence_level: str = "high", source: str = "detected", frame_index: int | None = None) -> PoleAxis` — vertical 박제 default.
      * `def make_pole_axis_measurement(*, line: PoleLine2D | None = None, axis_3d: PoleAxis | None = None, coordinate_space: str = "image_2d", frame_index: int | None = None) -> PoleAxisMeasurement` — line None 시 coordinate_space='unavailable' 자동 박제.
    - **test_pole_geometry_contract.py** 박제 (4 test):
      * test_pole_line_2d_frozen — PoleLine2D(point_image=..., direction_image=..., confidence=0.9, source='detected') 박제 후 setattr 시도 → FrozenInstanceError raise 검증.
      * test_pole_axis_measurement_unavailable_returns_none_distance — line=None 박제 시 coordinate_space='unavailable' 강제 검증 + distance 산출 분기 (caller 책임 박제 docstring 검증).
      * test_point_to_pole_line_distance_2d_analytical — point=(0.5, 0.5), line(point=(0.0, 0.0), direction=(1.0, 0.0)) (horizontal line) → distance = 0.5 (analytical). 추가 case = vertical line point=(0.0, 0.0), direction=(0.0, 1.0), point=(0.3, 0.5) → distance = 0.3.
      * test_build_pole_axis_measurement_image_line_present — line 박제 시 coordinate_space='image_2d' 자동 박제 검증.
    - **test_body_scale_helper.py** 박제 (4 test):
      * test_median_torso_length_image_2d — synthetic_pose_frames 박제 (image_2d coordinate, shoulder y=0.3, hip y=0.6) → median_torso_length(frames, space='image_2d') = ~0.3 (검증 atol=1e-2).
      * test_median_torso_length_pole_aligned — 동일 박제 + pole_aligned 좌표 → 동일 산출.
      * test_median_torso_length_missing_keypoints_returns_none — 60 frame 중 55 frame 가 missing keypoint → valid frame < 5 → None 반환 검증.
      * test_torso_scale_is_not_used_as_denominator — `import body_scale; import inspect; src = inspect.getsource(body_scale); assert 'BodyNormalizationProfile' not in src and 'torso_scale' not in src.split('def median_torso_length')[1]` — body_scale.py 의 본체에서 BodyNormalizationProfile.torso_scale 참조 영구 차단 검증 (drift defense, REVIEWS R2).
  </behavior>
  <action>
    backend/shared/python/sunity_shared/analysis/pole_geometry.py 신설 — 위 behavior 의 모듈 docstring + 4 박제 (CoordinateSpace / PoleLine2D / PoleAxisMeasurement / 2 helper). PoleLine2D 의 __post_init__ direction_image norm 검증 atol=1e-3 + confidence 범위 [0,1]. PoleAxisMeasurement 의 __post_init__ line None ↔ coordinate_space='unavailable' invariant 강제.

    backend/shared/python/sunity_shared/analysis/body_scale.py 신설 — 단일 median_torso_length helper. BodyNormalizationProfile import 영구 금지 박제 (drift defense). missing keypoint 시 NaN propagation 후 nanmedian. valid frame < 5 시 None 반환.

    backend/tests/phase08/__init__.py + fixtures/__init__.py 빈 파일 박제.

    backend/tests/phase08/conftest.py 박제 — 3 fixture (synthetic_pose_frames / synthetic_body_profile / synthetic_pole_axis_measurement) + PHASE08_FIXTURES_DIR 상수. Phase 7 conftest 패턴 정확히 mirror.

    backend/tests/phase08/fixtures/_factory.py 박제 — 7 programmatic factory (make_keypoint3d_aligned / make_keypoint3d / make_pose_frame / make_body_profile / make_pole_line_2d / make_pole_axis / make_pole_axis_measurement). 모든 박제 dataclass 시그너처는 backend/shared/python/sunity_shared/analysis/{pose_frame,body_normalization}.py 의 실제 시그너처 정확히 mirror (REVIEWS H1 영구 차단).

    2 test 파일 박제 — 위 behavior 의 8 test. pytest fixture conftest 박제 활용.

    Critical drift defense — test_torso_scale_is_not_used_as_denominator 가 body_scale.py 본체 source 안 'BodyNormalizationProfile' 영구 차단 (REVIEWS R2). 본 test 가 PASS 유지 강제 — body_scale.py 의 median_torso_length helper 가 observed length 만 산출 강제.

    per REVIEWS R1 + R2 + R9 + H1 + Suggestions §1.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && PYTHONPATH=backend/shared/python:. backend/.venv/bin/python3 -m pytest backend/tests/phase08/test_pole_geometry_contract.py backend/tests/phase08/test_body_scale_helper.py -x && PYTHONPATH=backend/shared/python:. backend/.venv/bin/python3 -c "from sunity_shared.analysis.pole_geometry import PoleLine2D, PoleAxisMeasurement, CoordinateSpace, point_to_pole_line_distance_2d, build_pole_axis_measurement; from sunity_shared.analysis.body_scale import median_torso_length; print('CONTRACT_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File exists: backend/shared/python/sunity_shared/analysis/pole_geometry.py with PoleLine2D + PoleAxisMeasurement + CoordinateSpace + 2 helper
    - File exists: backend/shared/python/sunity_shared/analysis/body_scale.py with median_torso_length helper
    - File exists: backend/tests/phase08/__init__.py / conftest.py / fixtures/__init__.py / fixtures/_factory.py
    - Source: `grep -c "class PoleLine2D" backend/shared/python/sunity_shared/analysis/pole_geometry.py` >= 1
    - Source: `grep -c "class PoleAxisMeasurement" backend/shared/python/sunity_shared/analysis/pole_geometry.py` >= 1
    - Source: `grep -c "def median_torso_length" backend/shared/python/sunity_shared/analysis/body_scale.py` >= 1
    - Source: `grep -c "BodyNormalizationProfile" backend/shared/python/sunity_shared/analysis/body_scale.py` == 0 (drift defense, REVIEWS R2)
    - Source: `grep -c "frame_index" backend/tests/phase08/fixtures/_factory.py` >= 1
    - Source: `grep -c "frame_idx" backend/tests/phase08/fixtures/_factory.py` == 0 (REVIEWS H1 영구 차단)
    - Source: `grep -c "visibility" backend/tests/phase08/fixtures/_factory.py` == 0 (Keypoint3DAligned 박제 정합, REVIEWS H1 영구 차단)
    - Source: `grep -c "asymmetry" backend/tests/phase08/fixtures/_factory.py` == 0 (BodyNormalizationProfile 박제 정합, REVIEWS H1 영구 차단)
    - Behavior: 8 test PASS (4 pole_geometry + 4 body_scale)
    - Behavior: phase01 / phase06 / phase07 회귀 0 (`pytest backend/tests/phase01/ backend/tests/phase06/ backend/tests/phase07/ -x`)
    - Behavior: pytest exit 0
  </acceptance_criteria>
  <done>
    pole_geometry.py + body_scale.py 신설 완료. Wave 0 test 인프라 박제 — programmatic factory 가 정확한 dataclass 시그너처 박제 (REVIEWS H1 영구 차단). body_scale.py 본체 안 BodyNormalizationProfile 영구 사용 금지 (REVIEWS R2 drift defense). Plan 08-01 의 JSON fixture 박제 진입 차단 해소 — JSON 박제는 본 factory 위에 박제.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 3-way contract 박제 — TS interface (analysis.ts) + docs/contract.md §9.0 + lockstep test</name>
  <files>
    app/src/types/analysis.ts
    docs/contract.md
    backend/tests/phase08/test_pole_geometry_contract.py
  </files>
  <read_first>
    app/src/types/analysis.ts (line 280-450 PoleAxis / PoseFrame / BodyNormalizationProfile 박제 패턴 직접)
    docs/contract.md (§8 / §8.3 박제 패턴 mirror)
    backend/shared/python/sunity_shared/analysis/pole_geometry.py (Task 1 박제 완료 본체)
    backend/tests/phase08/test_pole_geometry_contract.py (Task 1 박제 — 본 task 가 lockstep 검증 case 추가)
    backend/tests/phase07/test_body_comparison_report_phase7_lockstep.py (Phase 7 lockstep 패턴 정합)
  </read_first>
  <behavior>
    - **app/src/types/analysis.ts** 박제:
      * Plan 08-00 박제 영역 (PoseFrame / PoleAxis 박제 영역 직후):
        ```
        // ── Phase 8 Coordinate/Scale Contract (Plan 08-00 신설) ────────────
        // REVIEWS Cycle 1 R1/R2/R3 blocker 해소.
        // 박제 위치: pose_frame 박제 영역 직후, ForceSignalsReport (Plan 08-01) 직전.

        export type CoordinateSpace = 'image_2d' | 'pole_aligned' | 'world_3d' | 'unavailable';

        export type ContactPrimitiveKind = 'keypoint' | 'segment' | 'region_proxy';

        export interface PoleLine2D {
          pointImage: [number, number];   // (x, y) normalized 0~1, image 평면
          directionImage: [number, number]; // (dx, dy) unit vector
          confidence: number;              // 0.0 ~ 1.0
          source: 'detected' | 'vertical_fallback';
        }

        export interface PoleAxisMeasurement {
          axis3d: PoleAxis;                // Phase 1 박제 본체 (direction-only)
          line: PoleLine2D | null;         // image 2D line position
          coordinateSpace: CoordinateSpace; // line null ↔ 'unavailable'
          frameIndex: number | null;       // null = video-level (Phase 1 D-10)
        }
        // ────────────────────────────────────────────────────────────────────
        ```
    - **docs/contract.md** §9.0 신설:
      * Heading: `## §9.0. Coordinate/Scale Contract (Plan 08-00 신설 — Phase 8 pre-contract)`
      * 박제 내용:
        - §9.0 개요 (1 paragraph): Phase 8 의 axis distance / contact distance metric 의 좌표공간 + observed length denominator 박제. PoleAxis (direction-only) 한계 보강 + BodyNormalizationProfile.torso_scale (ratio self-reference) 사용 영구 금지.
        - §9.0.1 CoordinateSpace enum 표 (image_2d / pole_aligned / world_3d / unavailable + 의미).
        - §9.0.2 ContactPrimitiveKind enum 표 (keypoint / segment / region_proxy + 12 contact 분류).
        - §9.0.3 PoleLine2D 표 (필드 / type / 의미 / Plan 08-00 박제 위치).
        - §9.0.4 PoleAxisMeasurement 표 (필드 / type / 의미 — line None ↔ coordinate_space='unavailable' invariant).
        - §9.0.5 median_torso_length helper 박제 (시그너처 + space 분기 + BodyNormalizationProfile.torso_scale 사용 영구 금지 정책).
        - §9.0.6 Layer-1 pre-flight 25-timestamp label gate 박제 (≥80% 일치 → confidence='medium', 미만 → confidence='low' 강제).
        - §9.0.7 12 contact point × ContactPrimitiveKind 분류 표 (left_hand=keypoint / right_hand=keypoint / left_inner_thigh=segment / right_inner_thigh=segment / left_knee=keypoint / right_knee=keypoint / left_foot=keypoint / right_foot=keypoint / left_ankle=keypoint / right_ankle=keypoint / hip=region_proxy / unknown=keypoint).
      * 박제 위치: §8.3 (Phase 7 분류) 직후, §9 (Plan 08-01 ForceSignalsReport) 직전. 헤딩 cross-ref 박제.
    - **test_pole_geometry_contract.py** 신설 lockstep test 4 (Task 1 박제 test 의 확장):
      * test_ts_interface_has_coordinate_space_contract — analysis.ts 안 `CoordinateSpace` / `ContactPrimitiveKind` / `PoleLine2D` / `PoleAxisMeasurement` 4 박제 grep 통과 검증.
      * test_contract_md_has_section_9_0 — docs/contract.md 안 `## §9.0` 박제 + 7 subsection (9.0.1~9.0.7) grep 통과 검증.
      * test_contract_md_has_contact_primitive_classification_table — docs/contract.md 안 12 contact point × kind 분류 박제 (left_inner_thigh.*segment / hip.*region_proxy / left_hand.*keypoint) 모두 grep 통과 검증.
      * test_ts_python_field_camel_snake_mirror — `pointImage` ↔ `point_image` / `directionImage` ↔ `direction_image` / `axis3d` ↔ `axis_3d` / `coordinateSpace` ↔ `coordinate_space` / `frameIndex` ↔ `frame_index` 5 박제 정합. analysis.ts + pole_geometry.py 양쪽 grep 통과 검증.
  </behavior>
  <action>
    Phase 7 박제 패턴 (d4d8af4) — 단일 atomic commit 박제. 본 task 의 3 파일 (analysis.ts + contract.md + test_pole_geometry_contract.py) 은 단일 git commit. partial commit 시 lockstep test 가 fail-fast.

    `app/src/types/analysis.ts` 박제 위치: PoseFrame / PoleAxis 박제 영역 직후 (line ~450 근처), AnalysisDoc 박제 영역 직전. 박제 영역 헤더/푸터 박제.

    `docs/contract.md` 박제 위치: §8.3 (Phase 7 분류) 직후. 7 subsection (9.0.1~9.0.7) 박제. §9.0.7 의 12 contact 분류 표는 Plan 08-01 yaml 박제 source.

    test_pole_geometry_contract.py 신설 4 lockstep test 박제. Path.read_text() + grep pattern (Phase 7 패턴 mirror).

    Critical 회귀 0 — TS strict mode: `cd app && npx tsc --noEmit` exit 0 강제. AnalysisDoc 영향 0 (sub-contract 박제만).

    per REVIEWS R1 + R2 + R3 + Suggestions §1 + Phase 7 박제 lockstep 패턴.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && PYTHONPATH=backend/shared/python:. backend/.venv/bin/python3 -m pytest backend/tests/phase08/test_pole_geometry_contract.py -x && cd app && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File modified: app/src/types/analysis.ts contains `export interface PoleLine2D` + `export interface PoleAxisMeasurement` + `export type CoordinateSpace` + `export type ContactPrimitiveKind`
    - File modified: docs/contract.md contains `## §9.0. Coordinate/Scale Contract` heading + 7 subsection (9.0.1~9.0.7)
    - Source: `grep -c "PoleLine2D" app/src/types/analysis.ts` >= 2 (interface + PoleAxisMeasurement field)
    - Source: `grep -c "## §9.0" docs/contract.md` >= 1
    - Source: `grep -c "ContactPrimitiveKind" docs/contract.md` >= 2
    - Source: `grep -c "region_proxy" docs/contract.md` >= 2 (subsection + 분류 표)
    - Behavior (TS strict): `cd app && npx tsc --noEmit` exits 0
    - Behavior (lockstep): 4 신설 lockstep test PASS
    - Behavior: pytest exit 0
    - Git: 본 3 파일 단일 commit (`git log -1 --name-only` 시 3 파일 모두 포함)
  </acceptance_criteria>
  <done>
    3-way contract 박제 완료 (TS interface + docs §9.0 + lockstep test 단일 atomic commit). Plan 08-01 의 ForceSignalsReport 박제 시 본 contract import / 참조 강제. REVIEWS R1/R2/R3 blocker 영구 차단 — Plan 08-02 의 compute_axis_deviation / compute_contact_stability 가 본 contract 위에 박제.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pre-flight 25-timestamp label spec + CSV template + schema test</name>
  <files>
    .planning/phases/08-jerk-jitter/preflight/PREFLIGHT-LABEL-SPEC.md
    .planning/phases/08-jerk-jitter/preflight/preflight_label_template.csv
    backend/tests/phase08/test_preflight_label_schema.py
  </files>
  <read_first>
    .planning/phases/08-jerk-jitter/08-REVIEWS.md §"Agreed Concerns" §4 (Layer-1 5-phase 미검증) + §"Recommended Next Steps" §6 (pre-flight validation gate 박제)
    .planning/phases/08-jerk-jitter/08-VALIDATION.md §"Manual-Only Verifications"
    .planning/phases/08-jerk-jitter/08-CONTEXT.md §Decisions A1~A4 (5단계 분할 박제)
  </read_first>
  <behavior>
    - **PREFLIGHT-LABEL-SPEC.md** 박제 내용:
      * Front matter: title / belle 운영 박제 / Phase 8 진입 차단 해소 source.
      * §1 목적 — Layer-1 motion-agnostic 휴리스틱 5 phase boundary 검증 gate. REVIEWS R4 blocker 해소.
      * §2 라벨링 범위 — 5 reference 영상 (ref-invert / ref-foxtop / ref-foxtop-split / ref-climb / ref-sideway-spin) × 5 phase boundary (entry start / lock start / transition start / final_shape start / hold start) = 25 timestamp.
      * §3 라벨링 방법 — belle 가 영상 시청 + 각 phase boundary 시점 timestamp_ms 박제. Layer 1 산출 timestamp_ms 와 delta_ms = abs(belle - layer1) 산출.
      * §4 PASS / FAIL 기준 — 25 timestamp 중 delta_ms <= 200ms 비율 >= 80% (20/25 이상) → PASS → Layer 1 confidence='medium' 승급. 미만 → FAIL → Layer 1 confidence='low' 강제 + warning 'preflight_label_gate_failed' 박제.
      * §5 입력 파일 — `preflight_label_template.csv` (본 디렉토리). belle 가 본 파일 채워 PASS/FAIL 판정.
      * §6 FAIL 시 unwind path — Plan 08-03 Task 3 manual checkpoint 가 RECOGNIZER_BACKEND env unset + FORCE_SIGNALS_LAYER2_ENABLED env unset 으로 force_signals 산출 차단 (D-08-E3 정합). 분석 pipeline 자체는 영향 0 (Layer 1 confidence='low' 박제로 downstream 가 보수적 추론).
      * §7 재실행 — Plan 08-02 의 LAYER1 threshold 박제 재조정 후 본 gate 재실행. researcher 검토 + belle 박제.
      * §8 박제 메모 — REVIEWS Cycle 1 R4 + Suggestions §3 + D-08-E3 정합.
    - **preflight_label_template.csv** 박제 내용:
      * Header: `video_id,motion_id,phase,timestamp_ms_belle,timestamp_ms_layer1,delta_ms,agreed`
      * 25 row 박제 (5영상 × 5 phase):
        - video_id=ref-invert × 5 phase (entry_start / lock_start / transition_start / final_shape_start / hold_start)
        - video_id=ref-foxtop × 5 phase
        - video_id=ref-foxtop-split × 5 phase
        - video_id=ref-climb × 5 phase
        - video_id=ref-sideway-spin × 5 phase
      * timestamp_ms_belle / timestamp_ms_layer1 / delta_ms / agreed 컬럼 = 빈칸 (belle 가 채움). agreed = 'yes' if delta_ms <= 200 else 'no' (belle 가 수동 박제 또는 sheet 박제 spreadsheet 자동 산출).
    - **test_preflight_label_schema.py** 박제 (3 test):
      * test_label_spec_md_exists — `.planning/phases/08-jerk-jitter/preflight/PREFLIGHT-LABEL-SPEC.md` exists + 7 section heading (§1~§8) 박제 검증.
      * test_template_csv_header — preflight_label_template.csv 의 첫 라인 = `video_id,motion_id,phase,timestamp_ms_belle,timestamp_ms_layer1,delta_ms,agreed` 정확히 박제 검증.
      * test_template_csv_25_rows — preflight_label_template.csv 의 total row 수 = 26 (header + 25). video_id distinct 수 = 5 + phase distinct 수 = 5 (entry_start / lock_start / transition_start / final_shape_start / hold_start) 검증.
  </behavior>
  <action>
    `.planning/phases/08-jerk-jitter/preflight/` 디렉토리 신설. 본 디렉토리는 운영 작업 (belle) 박제 source.

    `PREFLIGHT-LABEL-SPEC.md` 박제 — 위 behavior 의 8 section. 본 spec 은 belle 운영 작업 docs (코드 X) — gate 통과 시 Plan 08-03 의 manual checkpoint 가 force_signals 산출 활성. FAIL 시 confidence='low' 강제 박제.

    `preflight_label_template.csv` 박제 — header 1줄 + 25 row (5 영상 × 5 phase). belle 가 본 CSV 채워 delta_ms / agreed 산출. spreadsheet (Google Sheets / Excel) 자동 산출 박제 가능 (수동 박제 가능).

    `test_preflight_label_schema.py` 박제 — 3 test (spec 존재 + CSV header + CSV row count). CSV 의 belle 입력 값 (timestamp_ms_belle / agreed) 은 빈칸 박제 — test 는 schema 만 검증.

    per REVIEWS R4 + R5 + Recommended Next Steps §6.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && PYTHONPATH=backend/shared/python:. backend/.venv/bin/python3 -m pytest backend/tests/phase08/test_preflight_label_schema.py -x</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `.planning/phases/08-jerk-jitter/preflight/PREFLIGHT-LABEL-SPEC.md` with 8 section headings
    - File exists: `.planning/phases/08-jerk-jitter/preflight/preflight_label_template.csv` with header + 25 row
    - Source: `grep -c "80%" .planning/phases/08-jerk-jitter/preflight/PREFLIGHT-LABEL-SPEC.md` >= 1
    - Source: `grep -c "200ms" .planning/phases/08-jerk-jitter/preflight/PREFLIGHT-LABEL-SPEC.md` >= 1
    - Source: `head -1 .planning/phases/08-jerk-jitter/preflight/preflight_label_template.csv` == `video_id,motion_id,phase,timestamp_ms_belle,timestamp_ms_layer1,delta_ms,agreed`
    - Source: `wc -l .planning/phases/08-jerk-jitter/preflight/preflight_label_template.csv` outputs 26 (header + 25 row)
    - Source: `grep -c "ref-invert" .planning/phases/08-jerk-jitter/preflight/preflight_label_template.csv` == 5
    - Source: `grep -c "ref-foxtop" .planning/phases/08-jerk-jitter/preflight/preflight_label_template.csv` >= 5
    - Behavior: 3 schema test PASS
  </acceptance_criteria>
  <done>
    Pre-flight 25-timestamp label spec + CSV template + schema test 박제 완료. belle 운영 작업 박제 — Layer 1 confidence 박제 source. REVIEWS R4 blocker 해소. Plan 08-03 Task 3 manual checkpoint 가 본 gate 결과 박제 후 Layer 1 / Layer 2 활성 박제 (FAIL 시 unwind path = env unset, 분석 죽지 않음).
  </done>
</task>

</tasks>

<verification>
## Phase 8 Plan 00 Verification

**Goal-backward derived truths (REVIEWS cycle 1 BLOCKER 해소):**

- pole_geometry.py 신설 — PoleLine2D + PoleAxisMeasurement + CoordinateSpace + 점-직선 거리 helper (REVIEWS R1 blocker 해소)
- body_scale.py 신설 — median_torso_length helper + BodyNormalizationProfile.torso_scale 영구 사용 금지 (REVIEWS R2 blocker 해소)
- ContactPrimitiveKind enum + 12 contact 분류 박제 (REVIEWS R3 blocker 해소)
- Pre-flight 25-timestamp label spec + CSV template + schema test (REVIEWS R4 blocker 해소)
- Wave 0 test 인프라 — programmatic factory (REVIEWS R9 권고) + 정확한 dataclass 시그너처 (REVIEWS H1 영구 차단)
- 3-way contract 단일 atomic commit (TS + docs §9.0 + Python 본체)
- Plan 08-01/02/03 진입 차단 해소 (본 contract 위에 박제)

**Manual verification (belle 운영 작업 박제):**

- preflight_label_template.csv 25 timestamp 박제 (belle 운영 작업, Plan 08-03 Task 3 manual checkpoint 박제)

**Regression checks:**

- `pytest backend/tests/phase01/ backend/tests/phase06/ backend/tests/phase07/ -x` PASS (PoleAxis 본체 변경 0, pose_frame.py 회귀 0)
- `cd app && npx tsc --noEmit` exits 0 (analysis.ts 박제 후 TS strict 통과)
- 8 신설 test PASS (pole_geometry 4 + body_scale 4 + preflight 3 = 11 total)

</verification>

<success_criteria>
- [x] pole_geometry.py 신설 + PoleLine2D / PoleAxisMeasurement / CoordinateSpace 박제 (REVIEWS R1)
- [x] body_scale.py 신설 + median_torso_length helper + torso_scale 사용 영구 금지 (REVIEWS R2)
- [x] ContactPrimitiveKind enum + 12 contact 분류 표 (REVIEWS R3)
- [x] Pre-flight 25-timestamp label spec + CSV template (REVIEWS R4)
- [x] Wave 0 test 인프라 — programmatic factory (REVIEWS R9) + 정확한 dataclass 시그너처 (REVIEWS H1)
- [x] 3-way contract 단일 atomic commit (TS + docs §9.0 + Python 본체)
- [x] phase01 / phase06 / phase07 회귀 0 + tsc clean
- [x] Plan 08-01 진입 차단 해소
</success_criteria>

<output>
Create `.planning/phases/08-jerk-jitter/08-00-SUMMARY.md` when done. Include:
- 3 task commit SHA + 각 task scope
- pole_geometry.py + body_scale.py 신설 결과 (REVIEWS R1/R2 blocker 해소)
- ContactPrimitiveKind enum + 12 분류 박제 결과 (REVIEWS R3 blocker 해소)
- Pre-flight 25-timestamp label spec + CSV template 박제 결과 (REVIEWS R4 blocker 해소)
- Wave 0 test 인프라 — programmatic factory 박제 결과 (REVIEWS H1 영구 차단)
- phase01/06/07 회귀 0 확인 + tsc clean
- 11 신설 test PASS
- Plan 08-01 진입 시그널 (본 contract 위에 schema 박제)
- Deviation (Rule 1 fix / Rule 3 environment fix 등) 있으면 박제
</output>
