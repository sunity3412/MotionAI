---
phase: 08-jerk-jitter
plan: 01
type: execute
wave: 1
depends_on: [08-00]
files_modified:
  - app/src/types/analysis.ts
  - backend/shared/python/sunity_shared/models.py
  - docs/contract.md
  - backend/shared/python/sunity_shared/analysis/dimensions.py
  - backend/judging_data/__init__.py
  - backend/judging_data/contact_points.yaml
  - backend/tests/phase08/test_force_signals_lockstep.py
  - backend/tests/phase08/test_contact_points_yaml_load.py
  - backend/tests/phase08/test_stability_wobble_drift_defense.py
  - backend/tests/phase08/fixtures/_fixture_builders.py
autonomous: true
requirements: [FORCE-01]
subsystem: backend-analysis-core
tags: [phase-08, force-signals, schema-lockstep, drift-defense, yaml-data, reviews-cycle-1-revised]

scope_sanity_note: "REVIEWS Cycle 1 (cycle: 1, codex) 반영 revised. depends_on 신설: [08-00]. Wave 0 test 인프라 박제는 Plan 08-00 으로 이동 — 본 plan = schema lockstep + drift defense helper + contact_points.yaml 박제 (kind 필드 동행, ContactPrimitiveKind enum 정합) + fixture builder helper (08-00 의 _factory.py 위에 박제). JSON fixture 박제는 본 plan 의 _fixture_builders.py 가 programmatic 박제. axis distance / contact distance metric 의 numeric 필드는 nullable + coordinateSpace 동행 (REVIEWS R1/R2 정합). estimatedStable = nullable evidence (REVIEWS R3 정합)."

must_haves:
  truths:
    - "본 plan = Plan 08-00 박제 contract 위에 ForceSignalsReport schema 박제 — TS + Python placeholder + docs §9 3-way lockstep (Phase 6/7 패턴 정합) + dimensions.stability_wobble raw helper 분리 (drift defense source) + contact_points.yaml 박제 (kind 필드 동행, ContactPrimitiveKind enum 정합) + programmatic fixture builder. Plan 08-02 (force_signals.py 본체) 진입 차단 해소. per REVIEWS R5 + R7 + R8 + R9 정합."
    - "axis distance 필드 명세 변경 (REVIEWS R1 + Suggestions §1 정합) — `pelvisDistanceFromPoleAxis: number | null` (line null 시 null) + `chestDistanceFromPoleAxis: number | null` + `coordinateSpace: CoordinateSpace` (Plan 08-00 박제 enum) + `scaleDenominator: 'observed_torso_length' | 'unavailable'` (Plan 08-00 박제 median_torso_length helper 정합 — BodyNormalizationProfile.torso_scale 영구 사용 금지). line 미가용 시 모든 numeric 필드 null + warning 'pole_line_missing' / 'scale_unavailable'."
    - "ContactStabilityMetric 명세 변경 (REVIEWS R3 + Suggestions §1 정합) — `estimatedStable: boolean | null` (evidence 약하면 null), `distanceToPoleNorm: number | null` (normalized to observed_torso_length, line 미가용 시 null), `nearPoleRatio: number | null` (phase 내 frame 중 distance <= threshold 비율), `lostNearPoleAtMs: number | null` (기존 lostContactAtMs 명칭 변경 — boolean truth 가 아닌 distance evidence 박제 정합), `measurementKind: ContactPrimitiveKind` (Plan 08-00 박제 — keypoint / segment / region_proxy), `coordinateSpace: CoordinateSpace`."
    - "StabilityMetric 명세 변경 (REVIEWS R5 + Suggestions §1 정합) — `jerkScore` 단위는 `deg/sec^3` (NOT deg/frame^3) — FPS 정규화 박제. `jerkUnit: 'deg_per_sec_cubed'` 필드 추가 (downstream 가 단위 확인 가능). `jitterScore` 단위는 `deg` (frame inter median wobble, unchanged). Plan 08-02 의 _compute_jerk 가 dt=1/fps 정규화 박제 강제."
    - "PhaseBoundary 명세 보강 (REVIEWS R4 + Suggestions §2 정합) — `confidence: MetricConfidence` 기본 박제 + `preflightLabelGatePassed: boolean | null` 신설 필드 (Plan 08-00 박제 preflight gate 결과 박제 source). null = gate 미실행 (default). true = gate PASS (≥80%, Layer 1 confidence='medium' 승급 박제). false = gate FAIL (Layer 1 confidence='low' 강제 박제 + warning 'preflight_label_gate_failed'). Plan 08-02 의 compute_phase_boundaries 가 본 필드 박제."
    - "ForceSignalsReport 신설 13 warning code (Plan 08-01 v1 박제) + REVIEWS Cycle 1 반영 신설 6 + REVIEWS Cycle 2 반영 신설 1 = total 20 enum:\n  기존 13: occlusion_high_in_phase / layer2_unavailable / layer_disagreement_minor / layer_disagreement_major / layer2_call_failed / motion_unrecognized / motion_unrecognized_layer1_only / abnormal_release_during_hold / partial_motion_video / video_too_short / heavy_occlusion / entry_not_detected / all_frames_unreliable.\n  Cycle 1 신설 6: pole_line_missing / scale_unavailable / preflight_label_gate_failed / fps_normalization_applied / contact_evidence_only / coordinate_space_unavailable.\n  Cycle 2 신설 1 (REVIEWS Cycle 2 §3 MEDIUM 정합): preflight_gate_pending (gate 미실행 default — Plan 08-02 본체가 이미 emit 박제, §9.8 enum 누락 보강)."
    - "dimensions.stability_wobble() raw helper 분리 — Plan 08-01 v1 박제 본체 유지 (drift defense source, Plan 08-02 의 force_signals.compute_stability_metrics 가 본 helper import 강제). stability_score() refactor 회귀 0 — Phase 6 156 test PASS 유지."
    - "contact_points.yaml 박제 — Plan 08-00 박제 ContactPrimitiveKind 정합 (각 entry 가 kind 필드 동행). 5 motion + default + kind 분류 박제. per REVIEWS R3 + Plan 08-00 §9.0.7."
    - "programmatic fixture builder 신설 (_fixture_builders.py) — Plan 08-00 _factory.py 위에 박제. 6 fixture 박제 (clean_invert / pelvis_drop / occluded_lock / motion_id_unrecognized / jerk_high / layback_release) 가 build_*() 함수로 박제 (JSON 박제 X — REVIEWS R9 정합). pelvis_drop / layback_release 는 line 가용 박제 (coordinate_space='image_2d'). motion_id_unrecognized 는 measurement_kind=null + estimated_stable=null."
    - "Python re-export 본 plan 박제는 placeholder — Plan 08-02 가 dataclass 본체 신설. models.py 의 import 는 Plan 08-02 에서 활성화. 본 plan 은 TS interface + contract.md §9 schema 박제만 (Python 측은 forward-declare 주석)."
    - "3-way lockstep test (test_force_signals_lockstep.py) 가 Plan 08-00 박제 contract 도 박제 — coordinateSpace / measurementKind / preflightLabelGatePassed / jerkUnit / scaleDenominator / nearPoleRatio / distanceToPoleNorm / lostNearPoleAtMs 모두 grep 통과."
  artifacts:
    - path: app/src/types/analysis.ts
      provides: "Phase 8 Force Signals 신설 7 interface — MotionPhase / DeviationDirection / SeverityLevel / MetricConfidence / ContactPoint enum + PhaseBoundary (preflightLabelGatePassed 필드 추가) + AxisDeviationMetric (coordinateSpace + scaleDenominator + nullable distance 필드 추가) + StabilityMetric (jerkUnit 필드 추가) + ContactStabilityMetric (estimatedStable nullable + distanceToPoleNorm + nearPoleRatio + lostNearPoleAtMs + measurementKind + coordinateSpace 변경) + ForceSignalsReport interface + AnalysisResult.forceSignalsReport optional nullable field. Plan 08-00 박제 contract import (CoordinateSpace / ContactPrimitiveKind / PoleAxisMeasurement)."
      contains:
        - "export type MotionPhase = 'entry' | 'lock' | 'transition' | 'final_shape' | 'hold'"
        - "export type DeviationDirection ="
        - "export type SeverityLevel = 'low' | 'medium' | 'high'"
        - "export type MetricConfidence = 'low' | 'medium' | 'high'"
        - "export type ContactPoint ="
        - "export interface PhaseBoundary"
        - "preflightLabelGatePassed"
        - "export interface AxisDeviationMetric"
        - "coordinateSpace"
        - "scaleDenominator"
        - "export interface StabilityMetric"
        - "jerkUnit"
        - "export interface ContactStabilityMetric"
        - "estimatedStable: boolean | null"
        - "distanceToPoleNorm"
        - "nearPoleRatio"
        - "lostNearPoleAtMs"
        - "measurementKind"
        - "export interface ForceSignalsReport"
        - "forceSignalsReport?: ForceSignalsReport | null"
    - path: backend/shared/python/sunity_shared/models.py
      provides: "Python re-export placeholder — Plan 08-02 가 dataclass 신설 후 import 활성화. 본 plan 은 주석으로 forward-declare 만 (3-way lockstep 의 Python 측면 박제 위치 강제)."
      contains:
        - "# Phase 8 Force Signals — Plan 08-02 신설 후 import 활성화"
        - "# from .analysis.force_signals import"
        - "PhaseBoundary"
        - "AxisDeviationMetric"
        - "StabilityMetric"
        - "ContactStabilityMetric"
        - "ForceSignalsReport"
    - path: docs/contract.md
      provides: "§9 ForceSignalsReport (Plan 08-01 revised — REVIEWS Cycle 1 반영). 7 type alias + 5 dataclass schema 표 + 20 warning code enum + ContactPoint enum + MotionPhase enum 박제. Plan 08-00 박제 §9.0 contract 참조 (CoordinateSpace / ContactPrimitiveKind / PoleAxisMeasurement / median_torso_length)."
      contains:
        - "## §9. ForceSignalsReport"
        - "MotionPhase"
        - "PhaseBoundary"
        - "preflightLabelGatePassed"
        - "AxisDeviationMetric"
        - "coordinateSpace"
        - "scaleDenominator"
        - "StabilityMetric"
        - "jerkUnit"
        - "deg_per_sec_cubed"
        - "ContactStabilityMetric"
        - "distanceToPoleNorm"
        - "nearPoleRatio"
        - "lostNearPoleAtMs"
        - "measurementKind"
        - "ForceSignalsReport"
        - "forceSignalsReport"
        - "pole_line_missing"
        - "scale_unavailable"
        - "preflight_label_gate_failed"
        - "preflight_gate_pending"
        - "fps_normalization_applied"
        - "contact_evidence_only"
        - "coordinate_space_unavailable"
    - path: backend/shared/python/sunity_shared/analysis/dimensions.py
      provides: "stability_wobble() raw helper 분리 + stability_score() refactor (helper 호출 + kismam.score_from_deviation 분리). drift defense source — Plan 08-02 force_signals.compute_stability_metrics 의 jitter_score (NOT jerk_score, jerk 는 별도 dt 정규화 박제) 가 본 helper import 강제."
      contains:
        - "def stability_wobble(angles"
        - "Plan 08-01 신설"
        - "force_signals.py 가 본 helper import"
    - path: backend/judging_data/contact_points.yaml
      provides: "motion_id × expected_contact_points 매핑 — Plan 08-00 박제 ContactPrimitiveKind 정합 (각 entry 가 kind 필드 동행). 5 motion entry + default + kind 분류 박제 (left_inner_thigh=segment / hip=region_proxy / 나머지 keypoint). yaml.safe_load 사용 강제."
      contains:
        - "motions:"
        - "ref-invert:"
        - "ref-foxtop:"
        - "ref-foxtop-split:"
        - "ref-climb:"
        - "ref-sideway-spin:"
        - "default:"
        - "expected_contact_points:"
        - "kind:"
        - "segment"
        - "region_proxy"
    - path: backend/judging_data/__init__.py
      provides: "judging_data 패키지 marker (Phase 7 박제 패턴 정합, 빈 파일 또는 이미 박제 존재 확인)."
      contains: []
    - path: backend/tests/phase08/fixtures/_fixture_builders.py
      provides: "programmatic fixture builder — Plan 08-00 _factory.py 위에 6 build_*() 함수 박제 (build_clean_invert / build_pelvis_drop / build_occluded_lock / build_motion_id_unrecognized / build_jerk_high / build_layback_release). 각 builder 가 (pose_frames, pole_axis_measurement, body_profile, expected_dict) 반환. JSON 박제 영구 차단 (REVIEWS R9). pelvis_drop / layback_release 는 line 가용 (coordinate_space='image_2d'). motion_id_unrecognized 는 measurement_kind=null."
      contains:
        - "def build_clean_invert"
        - "def build_pelvis_drop"
        - "def build_occluded_lock"
        - "def build_motion_id_unrecognized"
        - "def build_jerk_high"
        - "def build_layback_release"
        - "PoleAxisMeasurement"
        - "coordinate_space"
        - "make_pose_frame"
        - "make_body_profile"
    - path: backend/tests/phase08/test_force_signals_lockstep.py
      provides: "3-way lockstep drift defense — 7 신설 TS interface / Python placeholder / contract.md §9 의 field name 모두 grep 통과 검증. Plan 08-00 박제 contract 도 grep 검증 (coordinateSpace / measurementKind / preflightLabelGatePassed / jerkUnit / scaleDenominator / nearPoleRatio / distanceToPoleNorm / lostNearPoleAtMs)."
      contains:
        - "_FIELD_MAP"
        - "MotionPhase"
        - "ForceSignalsReport"
        - "phaseBoundaries"
        - "axisMetrics"
        - "coordinateSpace"
        - "measurementKind"
        - "preflightLabelGatePassed"
        - "jerkUnit"
        - "scaleDenominator"
        - "nearPoleRatio"
        - "distanceToPoleNorm"
        - "lostNearPoleAtMs"
    - path: backend/tests/phase08/test_contact_points_yaml_load.py
      provides: "contact_points.yaml 구조 유효성 + 12 ContactPoint enum 정합 + ContactPrimitiveKind 동행 검증. motions / default 키 박제 + 5 motion entry 의 expected_contact_points 가 list[dict-with-name-and-kind] 박제 (Plan 08-00 ContactPrimitiveKind 정합)."
      contains:
        - "_VALID_CONTACT_POINTS"
        - "_VALID_KINDS"
        - "def test_yaml_structure"
        - "def test_motion_entries_all_valid_contact_points"
        - "def test_motion_entries_kind_field_present"
        - "def test_default_entry_present"
        - "def test_yaml_uses_safe_load_path"
    - path: backend/tests/phase08/test_stability_wobble_drift_defense.py
      provides: "drift defense — dimensions.stability_wobble() 직접 호출 vs 산식 일치 검증. Plan 08-02 force_signals.jitter_score (NOT jerk_score) 의 drift 차단 source."
      contains:
        - "def test_stability_wobble_matches_direct_formula"
        - "def test_stability_wobble_short_input_returns_zero"
        - "def test_stability_score_refactor_preserves_output"
  key_links:
    - from: app/src/types/analysis.ts
      to: docs/contract.md
      via: "3-way contract — camelCase TS ↔ Python placeholder ↔ docs §9 atomic commit (Phase 6/7 박제 패턴, 116f400 / a444726 / d4d8af4). Plan 08-00 박제 §9.0 contract import (CoordinateSpace / ContactPrimitiveKind / PoleAxisMeasurement)."
      pattern: "ForceSignalsReport|MotionPhase|PhaseBoundary|AxisDeviationMetric|StabilityMetric|ContactStabilityMetric|coordinateSpace|measurementKind"
    - from: backend/shared/python/sunity_shared/analysis/dimensions.py
      to: backend/tests/phase08/test_stability_wobble_drift_defense.py
      via: "drift defense — stability_wobble() helper 신설 + 직접 산식 일치 검증. Plan 08-02 force_signals.jitter_score 가 본 helper import 강제 (drift 차단)"
      pattern: "stability_wobble"
    - from: backend/judging_data/contact_points.yaml
      to: backend/tests/phase08/test_contact_points_yaml_load.py
      via: "정적 yaml 구조 + 12 ContactPoint enum + ContactPrimitiveKind 동행 검증. Plan 08-02 force_signals._load_expected_contact_points 가 본 파일 load + kind 박제 활용."
      pattern: "expected_contact_points|kind"
    - from: backend/tests/phase08/fixtures/_fixture_builders.py
      to: backend/tests/phase08/fixtures/_factory.py
      via: "programmatic fixture builder가 Plan 08-00 박제 _factory.py 의 make_pose_frame / make_body_profile / make_pole_axis_measurement 호출 — JSON 박제 영구 차단 (REVIEWS R9)."
      pattern: "make_pose_frame|make_body_profile|make_pole_axis_measurement"
---

<objective>
Phase 8 vertical slice (1/3) revised — **REVIEWS Cycle 1 반영 schema lockstep + drift defense + yaml**. Plan 08-00 박제 contract (PoleLine2D / PoleAxisMeasurement / CoordinateSpace / ContactPrimitiveKind / median_torso_length) 위에 ForceSignalsReport schema 박제:

(a) **3-way contract lockstep (revised)** — TS interface 7 신설 + Python placeholder + docs §9 신설. PhaseBoundary 에 `preflightLabelGatePassed` / AxisDeviationMetric 에 `coordinateSpace` + `scaleDenominator` + nullable distance 필드 / StabilityMetric 에 `jerkUnit='deg_per_sec_cubed'` / ContactStabilityMetric 에 `estimatedStable: boolean | null` + `distanceToPoleNorm` + `nearPoleRatio` + `lostNearPoleAtMs` + `measurementKind` 박제. **REVIEWS R1/R2/R3/R5 반영.**
(b) **dimensions.stability_wobble() raw helper 분리** — 본 plan v1 박제 본체 유지 (drift defense source).
(c) **contact_points.yaml 박제** — Plan 08-00 박제 ContactPrimitiveKind 정합 (각 entry kind 필드 동행). 5 motion + default + kind 분류 박제. **REVIEWS R3 반영.**
(d) **programmatic fixture builder 신설** — `_fixture_builders.py` 위에 6 build_*() 함수 박제 (JSON 박제 영구 차단). **REVIEWS R9 반영.**
(e) **20 warning code enum 박제** — 기존 13 + Cycle 1 신설 6 (pole_line_missing / scale_unavailable / preflight_label_gate_failed / fps_normalization_applied / contact_evidence_only / coordinate_space_unavailable) + Cycle 2 신설 1 (preflight_gate_pending — REVIEWS Cycle 2 §3 MEDIUM 정합). **REVIEWS R1/R2/R4/R5 반영.**

Purpose: REVIEWS Cycle 1 의 9 HIGH concern 중 schema 측면 (R1/R2/R3/R5/R9) 영구 차단 — Plan 08-02 (force_signals.py 본체) 진입 차단 해소.
Output: 4 파일 수정 (analysis.ts + models.py + contract.md + dimensions.py) + 신설 3 파일 (contact_points.yaml + judging_data/__init__.py + _fixture_builders.py) + 3 test 파일.

**박제 메모 정합:** [[firestore-nested-array-flat]] + [[scoring-dimensions-ipsf]] + [[analysis-objectivity-no-human-scores]] + [[mvp-simple-pilot-quality]] + [[feedback-analysis-first]] + [[no-baekje-filler]].

per REVIEWS Cycle 1 R1 + R2 + R3 + R5 + R9 + H1 + Plan 08-00 박제 dependency.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-jerk-jitter/08-CONTEXT.md
@.planning/phases/08-jerk-jitter/08-RESEARCH.md
@.planning/phases/08-jerk-jitter/08-REVIEWS.md
@.planning/phases/08-jerk-jitter/08-VALIDATION.md
@.planning/phases/08-jerk-jitter/08-00-PLAN.md
@CLAUDE.md
@docs/contract.md
@app/src/types/analysis.ts
@backend/shared/python/sunity_shared/models.py
@backend/shared/python/sunity_shared/analysis/dimensions.py
@backend/shared/python/sunity_shared/analysis/pole_geometry.py
@backend/shared/python/sunity_shared/analysis/body_scale.py
@backend/shared/python/sunity_shared/firestore_admin.py
@backend/shared/python/sunity_shared/judging/loader.py
@backend/tests/phase08/conftest.py
@backend/tests/phase08/fixtures/_factory.py
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Phase 8 = backend 분석 코어 layer. 본 plan = schema + yaml + fixture builder + test. 사용자 입력 영역 0. yaml = 정적 repo 박제. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | T (Tampering / schema drift) | TS interface ↔ Python placeholder ↔ docs §9 lockstep | mitigate | 3-way lockstep drift defense test. Plan 08-00 contract 도 grep 검증 (coordinateSpace / measurementKind / preflightLabelGatePassed / jerkUnit). |
| T-08-01-02 | T (Tampering / yaml RCE) | contact_points.yaml load | mitigate | yaml.safe_load 강제. test_yaml_uses_safe_load_path 검증. |
| T-08-01-03 | T (Tampering / drift between dimensions vs force_signals) | dimensions.stability_score / stability_wobble helper 분리 | mitigate | drift defense test — refactor 전후 동일 input → 동일 output 회귀 0. |
| T-08-01-04 | T (Tampering / schema incompleteness) | nullable distance / measurementKind 박제 누락 시 downstream silent failure | mitigate | TS strict mode + lockstep test 가 모든 신설 필드 grep 검증. distance numeric null 박제 시 coordinateSpace='unavailable' invariant 강제 (Plan 08-00 박제 PoleAxisMeasurement __post_init__ 정합). |
| T-08-01-05 | I (Information disclosure / yaml 안 PII) | contact_points.yaml 내용 | accept | 정적 도메인 데이터 (motion_id + ContactPoint enum + kind). PII 없음. |
| T-08-01-06 | D (DoS / Firestore nested-array crash) | ForceSignalsReport schema | mitigate | list[dict-of-scalars-only-or-list-of-scalar] 박제. Plan 08-03 가 validator 명세 확장. |
| T-08-01-SC | Tampering | npm/pip/cargo installs | mitigate | 신규 의존성 0. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: dimensions.stability_wobble() raw helper 분리 + stability_score() refactor + drift defense test</name>
  <files>
    backend/shared/python/sunity_shared/analysis/dimensions.py
    backend/tests/phase08/test_stability_wobble_drift_defense.py
  </files>
  <read_first>
    backend/shared/python/sunity_shared/analysis/dimensions.py (line 66-79 stability_score 본체 + line 126-170 _select_window + stability_wobble_by_joint)
    backend/shared/python/sunity_shared/analysis/kismam.py (score_from_deviation 시그너처)
    .planning/phases/08-jerk-jitter/08-RESEARCH.md §"StabilityMetric Algorithm" + §"Code Examples Example 2"
    .planning/phases/08-jerk-jitter/08-CONTEXT.md §Decisions C
  </read_first>
  <behavior>
    - **dimensions.stability_wobble() helper 신설** (raw 산식 분리):
      * 시그너처: `def stability_wobble(angles, profile: "TechniqueProfile | None" = None) -> float`
      * 본체: `sliced, _ = _select_window(angles, profile); if sliced.shape[0] < 2: return 0.0; inter_frame_diff = np.abs(np.diff(sliced, axis=0)); median_jerk = np.nanmedian(inter_frame_diff, axis=0); return float(np.nanmean(median_jerk))`
      * docstring: "Raw inter-frame median wobble (degrees) — stability_score 의 score 변환 전 값. force_signals.py (Plan 08-02) 의 compute_stability_metrics 가 본 helper 호출해 jitter_score 산출 — drift 방지 (Phase 12.5 v4 Codex HIGH-2 패턴 정합). 단위 = degrees (frame inter median). jerk_score (deg/sec^3) 는 별도 산출 — REVIEWS R5 FPS 정규화 박제."
    - **dimensions.stability_score() refactor** (산식 복제 차단):
      * 본체 변경: wobble 산식 직접 X → `wobble = stability_wobble(angles, profile); sliced, _ = _select_window(angles, profile); if sliced.shape[0] < 2: return 100; return kismam.score_from_deviation(wobble, _STABILITY_TOL_DEG)`
      * 회귀 0 — refactor 전후 동일 input → 동일 output 강제
    - **test_stability_wobble_drift_defense.py** 박제 3 test:
      * Test 1 `test_stability_wobble_matches_direct_formula`: rng=np.random.default_rng(0), angles=rng.normal(120, 5, size=(50, 8)). `direct = float(np.nanmean(np.nanmedian(np.abs(np.diff(angles, axis=0)), axis=0)))`. `helper = dimensions.stability_wobble(angles, profile=None)`. `assert abs(helper - direct) < 1e-9`
      * Test 2 `test_stability_wobble_short_input_returns_zero`: angles.shape[0] == 1 → stability_wobble returns 0.0
      * Test 3 `test_stability_score_refactor_preserves_output`: 동일 input (T=50, J=8) 에 stability_score 호출 시 refactor 전후 동일 output (회귀 0 검증).
  </behavior>
  <action>
    `dimensions.py` 의 stability_score 본체를 두 함수로 분리. 모듈 docstring 끝에 박제 메모 추가.

    `test_stability_wobble_drift_defense.py` 박제 — 위 behavior 의 3 test.

    per REVIEWS R5 (jerk FPS 정규화는 Plan 08-02 책임, 본 task = jitter wobble helper 분리만).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && PYTHONPATH=backend/shared/python:. backend/.venv/bin/python3 -m pytest backend/tests/phase08/test_stability_wobble_drift_defense.py backend/tests/phase06/ -x</automated>
  </verify>
  <acceptance_criteria>
    - File modified: dimensions.py 의 stability_wobble helper 신설 + stability_score refactor
    - Source: `grep -c "def stability_wobble" backend/shared/python/sunity_shared/analysis/dimensions.py` >= 1
    - Source: `grep -c "Plan 08-01" backend/shared/python/sunity_shared/analysis/dimensions.py` >= 1
    - Behavior: test_stability_wobble_matches_direct_formula PASS (abs < 1e-9)
    - Behavior: phase06 156 test PASS (refactor 회귀 0)
    - Behavior: pytest exit 0
  </acceptance_criteria>
  <done>
    dimensions.stability_wobble() raw helper 분리 완료. stability_score() refactor 회귀 0. Plan 08-02 의 force_signals.compute_stability_metrics 가 본 helper import 강제 (drift defense).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: contact_points.yaml 박제 (kind 필드 동행) + judging_data 패키지 marker + yaml load test + programmatic fixture builder</name>
  <files>
    backend/judging_data/__init__.py
    backend/judging_data/contact_points.yaml
    backend/tests/phase08/test_contact_points_yaml_load.py
    backend/tests/phase08/fixtures/_fixture_builders.py
  </files>
  <read_first>
    backend/shared/python/sunity_shared/judging/loader.py (PyYAML 사용 패턴 — yaml.safe_load)
    backend/shared/python/sunity_shared/analysis/pole_geometry.py (Plan 08-00 박제 ContactPrimitiveKind)
    backend/tests/phase08/fixtures/_factory.py (Plan 08-00 박제 — make_pose_frame / make_body_profile / make_pole_axis_measurement)
    backend/tests/phase08/conftest.py (Plan 08-00 박제 PHASE08_FIXTURES_DIR 상수)
    .planning/phases/08-jerk-jitter/08-CONTEXT.md §Decisions B (contact_points.yaml)
    docs/contract.md §9.0 (Plan 08-00 박제 — 12 contact 분류 표 source)
  </read_first>
  <behavior>
    - **backend/judging_data/__init__.py** 신설 (빈 파일 — 이미 존재하면 변경 X).
    - **backend/judging_data/contact_points.yaml** 박제:
      * yaml 상단 헤더 주석: "# Plan 08-01 신설 (REVIEWS Cycle 1 revised). motion_id × expected_contact_points 매핑 — Plan 08-00 박제 ContactPrimitiveKind 정합 (각 entry 가 kind 필드 동행). belle/강사 도메인 검수 필수 — researcher 초안."
      * 박제 schema — 각 entry 는 `{name: <ContactPoint>, kind: <ContactPrimitiveKind>}` dict:
        ```yaml
        motions:
          ref-invert:
            expected_contact_points:
              - {name: left_hand, kind: keypoint}
              - {name: right_hand, kind: keypoint}
              - {name: left_inner_thigh, kind: segment}
              - {name: right_inner_thigh, kind: segment}
          ref-foxtop:
            expected_contact_points:
              - {name: left_hand, kind: keypoint}
              - {name: right_hand, kind: keypoint}
              - {name: left_ankle, kind: keypoint}
              - {name: right_ankle, kind: keypoint}
              - {name: hip, kind: region_proxy}
          ref-foxtop-split:
            expected_contact_points:
              - {name: left_hand, kind: keypoint}
              - {name: right_hand, kind: keypoint}
              - {name: left_ankle, kind: keypoint}
              - {name: right_ankle, kind: keypoint}
              - {name: hip, kind: region_proxy}
          ref-climb:
            expected_contact_points:
              - {name: left_hand, kind: keypoint}
              - {name: right_hand, kind: keypoint}
              - {name: left_inner_thigh, kind: segment}
              - {name: right_inner_thigh, kind: segment}
          ref-sideway-spin:
            expected_contact_points:
              - {name: left_hand, kind: keypoint}
              - {name: right_hand, kind: keypoint}
              - {name: left_knee, kind: keypoint}
              - {name: right_knee, kind: keypoint}
        default:
          expected_contact_points: []
        ```
    - **test_contact_points_yaml_load.py** 박제 5 test:
      * 모듈 상단 `_VALID_CONTACT_POINTS = frozenset({"left_hand", "right_hand", "left_inner_thigh", "right_inner_thigh", "left_knee", "right_knee", "left_foot", "right_foot", "left_ankle", "right_ankle", "hip", "unknown"})` + `_VALID_KINDS = frozenset({"keypoint", "segment", "region_proxy"})`
      * Test 1 `test_yaml_structure`: yaml.safe_load 호출. raw["motions"] is dict + raw["default"] is dict. 5 motion entry 모두 존재.
      * Test 2 `test_motion_entries_all_valid_contact_points`: 각 motion entry 의 expected_contact_points 가 list of dict + 모든 name 이 _VALID_CONTACT_POINTS 안 박제.
      * Test 3 `test_motion_entries_kind_field_present`: 각 entry 가 kind 필드 박제 + _VALID_KINDS 안 박제. left_inner_thigh.kind == 'segment' + hip.kind == 'region_proxy' + left_hand.kind == 'keypoint' 검증.
      * Test 4 `test_default_entry_present`: raw["default"]["expected_contact_points"] == [].
      * Test 5 `test_yaml_uses_safe_load_path`: contact_points.yaml 의 파일 내용에 `!!python/` 가 없는지 검증 — safe_load 호환성.
    - **_fixture_builders.py** 박제 6 build_*() 함수 (programmatic, REVIEWS R9 정합):
      * `build_clean_invert() -> tuple[list[PoseFrame], PoleAxisMeasurement, BodyNormalizationProfile, dict]`:
        - motion_id="ref-invert", T=60, reliability='high' 90%+, 5단계 phase 분명. line 가용 (coordinate_space='image_2d'). expected: severity all 'low' / estimated_stable=true (각 expected_contact_point) / nearPoleRatio >= 0.9 / lostNearPoleAtMs=null.
      * `build_pelvis_drop() -> tuple`:
        - motion_id="ref-foxtop", T=60, hold phase frame 30~50 사이 pelvis x-coord +0.35 outward drift. line 가용. expected: axis_metrics[hold].severity='high' / deviation_direction='outward' / pelvisDistanceFromPoleAxis 가 frame 평균 0.35 (scaleDenominator='observed_torso_length').
      * `build_occluded_lock() -> tuple`:
        - motion_id="ref-foxtop", T=60, lock 구간 (frame 5~15) 6/10 frame reliability='low'. line 가용. expected: warning_emitted='occlusion_high_in_phase' / lock metric confidence='low'.
      * `build_motion_id_unrecognized() -> tuple`:
        - motion_id=None, T=60, line 가용. expected: expected_contact_points=[] / contact metric estimated_stable=null / measurementKind=null / warning='motion_unrecognized'.
      * `build_jerk_high() -> tuple`:
        - motion_id="ref-foxtop", T=60, transition (frame 15~25) 안 angles 3차 미분 > 15 deg/sec^3 (FPS-normalized, fps=9). expected: stability_metrics[transition].jerk_score > 15 / jerkUnit='deg_per_sec_cubed' / severity='high'.
      * `build_layback_release() -> tuple`:
        - motion_id="ref-foxtop", T=60, hold phase frame 40 시점 left_hand keypoint 폴축 거리 > 0.20 observed_torso_length. line 가용. expected: contact_metrics["left_hand"][hold].lostNearPoleAtMs=4444 (frame 40 × 111ms @ 9fps) / nearPoleRatio < 0.5 / warning='abnormal_release_during_hold'.
      * 모든 builder = Plan 08-00 박제 `make_pose_frame` / `make_body_profile` / `make_pole_axis_measurement` 호출 (JSON 박제 영구 차단, REVIEWS R9).
      * 각 builder 의 expected dict 는 Plan 08-02 의 단위 test 가 assertion 박제.
  </behavior>
  <action>
    backend/judging_data/__init__.py 신설 (빈 파일, 이미 박제 시 skip).

    backend/judging_data/contact_points.yaml 박제 — 위 behavior 의 5 motion + default + kind 필드. Plan 08-00 박제 ContactPrimitiveKind 정합 (12 contact 분류 표 source).

    test_contact_points_yaml_load.py 박제 — 위 behavior 의 5 test. yaml.safe_load 사용 강제 + kind 필드 정합 검증.

    _fixture_builders.py 박제 — 위 behavior 의 6 build_*() 함수. Plan 08-00 _factory.py 박제 함수 호출 (programmatic, REVIEWS R9 정합). 각 builder 가 (pose_frames, pole_axis_measurement, body_profile, expected_dict) 반환.

    per REVIEWS R3 + R9 + Plan 08-00 박제 ContactPrimitiveKind.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && PYTHONPATH=backend/shared/python:. backend/.venv/bin/python3 -m pytest backend/tests/phase08/test_contact_points_yaml_load.py -x && PYTHONPATH=backend/shared/python:. backend/.venv/bin/python3 -c "from backend.tests.phase08.fixtures._fixture_builders import build_clean_invert, build_pelvis_drop, build_occluded_lock, build_motion_id_unrecognized, build_jerk_high, build_layback_release; frames, pole, profile, expected = build_clean_invert(); assert len(frames) == 60; print('FIXTURE_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File exists: backend/judging_data/contact_points.yaml with motions / default + 5 motion entry + kind field
    - File exists: backend/judging_data/__init__.py
    - File exists: backend/tests/phase08/fixtures/_fixture_builders.py with 6 build_*() functions
    - Source: `python3 -c "import yaml; raw = yaml.safe_load(open('backend/judging_data/contact_points.yaml').read()); inv = raw['motions']['ref-invert']['expected_contact_points']; assert all('kind' in e for e in inv); assert {e['kind'] for e in inv} <= {'keypoint', 'segment'}; print('YAML_OK')"` prints YAML_OK
    - Source: `grep -c "def build_clean_invert\|def build_pelvis_drop\|def build_occluded_lock\|def build_motion_id_unrecognized\|def build_jerk_high\|def build_layback_release" backend/tests/phase08/fixtures/_fixture_builders.py` >= 6
    - Behavior: 5 yaml load test PASS
    - Behavior: 6 fixture builder 호출 결과 검증 (60 frame list 반환 + expected dict 비어있지 않음)
    - Behavior: pytest exit 0
  </acceptance_criteria>
  <done>
    contact_points.yaml 박제 완료 (kind 필드 동행, ContactPrimitiveKind 정합). 6 programmatic fixture builder 박제 (JSON 박제 영구 차단, REVIEWS R9). Plan 08-02 의 단위 test 가 본 builder 호출 + expected dict assertion.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 3-way contract lockstep 단일 atomic commit — TS 신설 + Python placeholder + contract.md §9 + lockstep test (REVIEWS Cycle 1 반영)</name>
  <files>
    app/src/types/analysis.ts
    backend/shared/python/sunity_shared/models.py
    docs/contract.md
    backend/tests/phase08/test_force_signals_lockstep.py
  </files>
  <read_first>
    app/src/types/analysis.ts (Plan 08-00 박제 §9.0 contract 영역 + BodyComparisonReport 박제 패턴)
    backend/shared/python/sunity_shared/models.py
    docs/contract.md (Plan 08-00 박제 §9.0 + §8 / §8.3 BodyComparisonReport 패턴 mirror)
    .planning/phases/08-jerk-jitter/08-REVIEWS.md §"Agreed Concerns" + §"Newly Surfaced Concerns"
    .planning/phases/08-jerk-jitter/08-RESEARCH.md §"3-Way Contract Additions"
    backend/tests/phase07/test_body_comparison_report_phase7_lockstep.py
  </read_first>
  <behavior>
    - **app/src/types/analysis.ts** 박제 위치: Plan 08-00 박제 §9.0 contract 영역 직후 (CoordinateSpace / PoleAxisMeasurement 박제 직후):
      * 박제 영역 헤더: `// ── Phase 8 Force Signals (Plan 08-01 revised — REVIEWS Cycle 1) ───`
      * 5 type alias (Plan 08-01 v1 박제 그대로):
        - MotionPhase = 'entry' | 'lock' | 'transition' | 'final_shape' | 'hold'
        - DeviationDirection = 'up' | 'down' | 'left' | 'right' | 'outward' | 'inward' | 'unknown'
        - SeverityLevel = 'low' | 'medium' | 'high'
        - MetricConfidence = 'low' | 'medium' | 'high'
        - ContactPoint = 'left_hand' | 'right_hand' | 'left_inner_thigh' | 'right_inner_thigh' | 'left_knee' | 'right_knee' | 'left_foot' | 'right_foot' | 'left_ankle' | 'right_ankle' | 'hip' | 'unknown'
      * 5 interface (REVIEWS Cycle 1 반영):
        - `PhaseBoundary { phase: MotionPhase; startFrameIdx: number; endFrameIdx: number; startMs: number; endMs: number; confidence: MetricConfidence; source: 'heuristic' | 'gemini_assisted' | 'heuristic_fallback'; preflightLabelGatePassed: boolean | null; }`  // REVIEWS R4 신설 필드
        - `AxisDeviationMetric { phase: MotionPhase; pelvisDistanceFromPoleAxis: number | null; chestDistanceFromPoleAxis: number | null; shoulderTilt: number | null; hipTilt: number | null; deviationDirection: DeviationDirection; severity: SeverityLevel; confidence: MetricConfidence; coordinateSpace: CoordinateSpace; scaleDenominator: 'observed_torso_length' | 'unavailable'; warnings: string[]; }`  // REVIEWS R1 + R2: nullable distance + coordinateSpace + scaleDenominator
        - `StabilityMetric { phase: MotionPhase; jitterScore: number; jerkScore: number; jerkUnit: 'deg_per_sec_cubed'; holdStabilityScore: number | null; unstableBodyParts: string[]; severity: SeverityLevel; confidence: MetricConfidence; warnings: string[]; }`  // REVIEWS R5: jerkUnit 명시 박제
        - `ContactStabilityMetric { phase: MotionPhase; contactPoint: ContactPoint; measurementKind: ContactPrimitiveKind | null; estimatedStable: boolean | null; distanceToPoleNorm: number | null; nearPoleRatio: number | null; lostNearPoleAtMs: number | null; coordinateSpace: CoordinateSpace; severity: SeverityLevel; confidence: MetricConfidence; warnings: string[]; }`  // REVIEWS R3: evidence-with-confidence + measurementKind + nullable
        - `ForceSignalsReport { version: string; overallConfidence: MetricConfidence; warnings: string[]; phaseBoundaries: PhaseBoundary[]; axisMetrics: AxisDeviationMetric[]; stabilityMetrics: StabilityMetric[]; contactMetrics: ContactStabilityMetric[]; }`
      * AnalysisResult 확장: `forceSignalsReport?: ForceSignalsReport | null;`
      * 박제 영역 끝: `// ────────────────────────────────────────────────────────────────────`
    - **backend/shared/python/sunity_shared/models.py** placeholder 박제:
      * 본 plan 의 변경 = forward-declare 주석만 (Plan 08-02 가 실제 import 활성화):
        ```
        # Phase 8 Force Signals — Plan 08-02 신설 후 import 활성화
        # from .analysis.force_signals import (
        #     MotionPhase,
        #     DeviationDirection,
        #     SeverityLevel,
        #     MetricConfidence,
        #     ContactPoint,
        #     PhaseBoundary,
        #     AxisDeviationMetric,
        #     StabilityMetric,
        #     ContactStabilityMetric,
        #     ForceSignalsReport,
        # )
        ```
    - **docs/contract.md §9 신설** (Plan 08-00 박제 §9.0 직후):
      * Top heading: `## §9. ForceSignalsReport (Plan 08-01 revised — REVIEWS Cycle 1 반영 — FORCE-01 신호 layer)`
      * 박제 내용:
        - §9 개요 (1 paragraph): Phase 8 = Phase 9 의 inferForceDirectionPattern 신호 layer + 5단계 motion-phase 분할 + 가림 스무딩 + confidence 가중 처리. **Plan 08-00 §9.0 contract 위에 박제** (PoleLine2D / PoleAxisMeasurement / CoordinateSpace / ContactPrimitiveKind / median_torso_length import).
        - §9.1 Enum / Type Alias: 5 type 표.
        - §9.2 PhaseBoundary 표 + `preflightLabelGatePassed: boolean | null` 신설 필드 박제 (REVIEWS R4).
        - §9.3 AxisDeviationMetric 표 + nullable distance / coordinateSpace / scaleDenominator 신설 필드 박제 (REVIEWS R1 + R2). 박제 메모: "line 미가용 시 pelvisDistanceFromPoleAxis / chestDistanceFromPoleAxis 모두 null + warning 'pole_line_missing' + coordinateSpace='unavailable' + scaleDenominator='unavailable'."
        - §9.4 StabilityMetric 표 + `jerkUnit='deg_per_sec_cubed'` 신설 필드 박제 (REVIEWS R5). 박제 메모: "jerkScore = deg/sec^3 (FPS-normalized, NOT deg/frame^3). dt=1/fps 정규화 박제 — 9 fps vs 18 fps 동일 값 강제 (Plan 08-02 _compute_jerk 책임)."
        - §9.5 ContactStabilityMetric 표 + estimatedStable nullable / distanceToPoleNorm / nearPoleRatio / lostNearPoleAtMs / measurementKind 박제 (REVIEWS R3). 박제 메모: "v1 = evidence-with-confidence (NOT boolean truth). estimatedStable=null 시 evidence 부족 박제. measurementKind=null 시 motion_id 미인식 fallback."
        - §9.6 ForceSignalsReport 표 (top-level umbrella).
        - §9.7 AnalysisResult.forceSignalsReport optional 필드 박제.
        - §9.8 Warning code enum — 20 enum:
          기존 13: occlusion_high_in_phase / layer2_unavailable / layer_disagreement_minor / layer_disagreement_major / layer2_call_failed / motion_unrecognized / motion_unrecognized_layer1_only / abnormal_release_during_hold / partial_motion_video / video_too_short / heavy_occlusion / entry_not_detected / all_frames_unreliable.
          Cycle 1 신설 6: pole_line_missing / scale_unavailable / preflight_label_gate_failed / fps_normalization_applied / contact_evidence_only / coordinate_space_unavailable.
          Cycle 2 신설 1 (REVIEWS Cycle 2 §3 MEDIUM 정합): preflight_gate_pending — Plan 08-02 본체가 이미 emit (preflight gate None default 시) 박제, §9.8 enum 누락 보강.
        - §9.9 D-결정 요약 (D-08-A1~U3 CONTEXT.md 인용).
        - §9.10 Plan 08-00 contract dependency 박제 — CoordinateSpace / ContactPrimitiveKind / PoleAxisMeasurement / median_torso_length helper 의존성 명시.
    - **test_force_signals_lockstep.py** 박제 — Phase 7 박제 패턴 정합 + REVIEWS Cycle 1 반영:
      * 모듈 상단에 `_FIELD_MAP: list[tuple[str, str, str]]` 박제 — (ts_field, py_or_doc_field, label) tuple.
      * 박제 entry (v1 박제 + 신설 추가):
        v1 박제 (모두 유지):
        - ("MotionPhase", "MotionPhase", "type alias")
        - ("DeviationDirection", "DeviationDirection", "type alias")
        - ("SeverityLevel", "SeverityLevel", "type alias")
        - ("MetricConfidence", "MetricConfidence", "type alias")
        - ("ContactPoint", "ContactPoint", "type alias")
        - ("PhaseBoundary", "PhaseBoundary", "dataclass")
        - ("AxisDeviationMetric", "AxisDeviationMetric", "dataclass")
        - ("StabilityMetric", "StabilityMetric", "dataclass")
        - ("ContactStabilityMetric", "ContactStabilityMetric", "dataclass")
        - ("ForceSignalsReport", "ForceSignalsReport", "dataclass")
        - ("forceSignalsReport", "forceSignalsReport", "AnalysisResult field")
        - ("phaseBoundaries", "phase_boundaries", "camelCase ↔ snake_case")
        - ("axisMetrics", "axis_metrics", "camelCase ↔ snake_case")
        - ("stabilityMetrics", "stability_metrics", "camelCase ↔ snake_case")
        - ("contactMetrics", "contact_metrics", "camelCase ↔ snake_case")
        - ("overallConfidence", "overall_confidence", "camelCase ↔ snake_case")
        - ("startFrameIdx", "start_frame_idx", "camelCase ↔ snake_case")
        - ("endFrameIdx", "end_frame_idx", "camelCase ↔ snake_case")
        - ("startMs", "start_ms", "camelCase ↔ snake_case")
        - ("endMs", "end_ms", "camelCase ↔ snake_case")
        - ("pelvisDistanceFromPoleAxis", "pelvis_distance_from_pole_axis", "camelCase ↔ snake_case")
        - ("chestDistanceFromPoleAxis", "chest_distance_from_pole_axis", "camelCase ↔ snake_case")
        - ("shoulderTilt", "shoulder_tilt", "camelCase ↔ snake_case")
        - ("hipTilt", "hip_tilt", "camelCase ↔ snake_case")
        - ("deviationDirection", "deviation_direction", "camelCase ↔ snake_case")
        - ("jitterScore", "jitter_score", "camelCase ↔ snake_case")
        - ("jerkScore", "jerk_score", "camelCase ↔ snake_case")
        - ("holdStabilityScore", "hold_stability_score", "camelCase ↔ snake_case")
        - ("unstableBodyParts", "unstable_body_parts", "camelCase ↔ snake_case")
        - ("contactPoint", "contact_point", "camelCase ↔ snake_case")
        신설 (REVIEWS Cycle 1):
        - ("coordinateSpace", "coordinate_space", "REVIEWS R1/R2")
        - ("scaleDenominator", "scale_denominator", "REVIEWS R2")
        - ("measurementKind", "measurement_kind", "REVIEWS R3")
        - ("preflightLabelGatePassed", "preflight_label_gate_passed", "REVIEWS R4")
        - ("jerkUnit", "jerk_unit", "REVIEWS R5")
        - ("estimatedStable", "estimated_stable", "camelCase, v1)")
        - ("distanceToPoleNorm", "distance_to_pole_norm", "REVIEWS R3 신설")
        - ("nearPoleRatio", "near_pole_ratio", "REVIEWS R3 신설")
        - ("lostNearPoleAtMs", "lost_near_pole_at_ms", "REVIEWS R3 — lostContactAtMs 명칭 변경")
        - ("pole_line_missing", "pole_line_missing", "warning code REVIEWS R1")
        - ("scale_unavailable", "scale_unavailable", "warning code REVIEWS R2")
        - ("preflight_label_gate_failed", "preflight_label_gate_failed", "warning code REVIEWS R4")
        - ("preflight_gate_pending", "preflight_gate_pending", "warning code REVIEWS Cycle 2 §3 MEDIUM")
        - ("fps_normalization_applied", "fps_normalization_applied", "warning code REVIEWS R5")
        - ("contact_evidence_only", "contact_evidence_only", "warning code REVIEWS R3")
        - ("coordinate_space_unavailable", "coordinate_space_unavailable", "warning code REVIEWS R1/R2")
      * 3 test (v1 박제 패턴 정합):
        - `test_ts_interface_has_all_fields` — analysis.ts 안 각 ts_field grep
        - `test_python_placeholder_has_all_fields` — models.py 안 각 py_field grep (주석 박제 OK)
        - `test_contract_md_has_all_fields` — docs/contract.md 안 각 ts_field 또는 py_field grep
  </behavior>
  <action>
    Phase 6/7 박제 패턴 — 단일 atomic commit 강제. 본 task 의 4 파일 (analysis.ts + models.py + contract.md + lockstep test) 은 단일 git commit. partial commit 시 lockstep test 가 fail-fast.

    `app/src/types/analysis.ts` 박제 위치: Plan 08-00 §9.0 contract 영역 (CoordinateSpace / PoleAxisMeasurement 박제) 직후. 박제 영역 헤더/푸터 박제.

    `backend/shared/python/sunity_shared/models.py` 박제 위치: 기존 body_normalizer import 영역 직후. forward-declare 주석 박제 — Plan 08-02 가 활성화.

    `docs/contract.md` 박제 위치: Plan 08-00 박제 §9.0 직후. 헤딩 + 10 subsection (§9.1~§9.10) 박제.

    test_force_signals_lockstep.py 박제 — Phase 7 패턴 정합. pytest parametrize over _FIELD_MAP + Path.read_text() + assert `count >= 1`.

    Critical 회귀 0 — TS strict mode + phase06 156 test 모두 PASS 유지 강제.

    per REVIEWS Cycle 1 R1/R2/R3/R4/R5 모두 박제 + Plan 08-00 contract dependency 명시 + Phase 6/7 박제 lockstep 패턴.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && PYTHONPATH=backend/shared/python:. backend/.venv/bin/python3 -m pytest backend/tests/phase08/test_force_signals_lockstep.py backend/tests/phase06/ backend/tests/phase07/ -x && cd app && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File modified: app/src/types/analysis.ts contains all 5 type aliases + 5 interfaces + REVIEWS Cycle 1 신설 필드 (coordinateSpace / scaleDenominator / measurementKind / preflightLabelGatePassed / jerkUnit / distanceToPoleNorm / nearPoleRatio / lostNearPoleAtMs)
    - File modified: backend/shared/python/sunity_shared/models.py contains forward-declare comment block with all 10 names
    - File modified: docs/contract.md contains `## §9. ForceSignalsReport` heading + §9.1~§9.10 + 20 warning code enum
    - Source: `grep -c "export interface ForceSignalsReport" app/src/types/analysis.ts` >= 1
    - Source: `grep -c "coordinateSpace" app/src/types/analysis.ts` >= 2 (AxisDeviationMetric + ContactStabilityMetric)
    - Source: `grep -c "measurementKind" app/src/types/analysis.ts` >= 1
    - Source: `grep -c "preflightLabelGatePassed" app/src/types/analysis.ts` >= 1
    - Source: `grep -c "jerkUnit" app/src/types/analysis.ts` >= 1
    - Source: `grep -c "lostNearPoleAtMs" app/src/types/analysis.ts` >= 1
    - Source: `grep -c "nearPoleRatio" app/src/types/analysis.ts` >= 1
    - Source: `grep -c "distanceToPoleNorm" app/src/types/analysis.ts` >= 1
    - Source: `grep -c "## §9\\.[0-9]" docs/contract.md` >= 10 (§9.1~§9.10)
    - Source: `grep -c "pole_line_missing\|scale_unavailable\|preflight_label_gate_failed\|preflight_gate_pending\|fps_normalization_applied\|contact_evidence_only\|coordinate_space_unavailable" docs/contract.md` >= 7
    - Behavior: `cd app && npx tsc --noEmit` exits 0 (TS strict mode pass)
    - Behavior: lockstep test PASS (모든 _FIELD_MAP entries grep-detected)
    - Behavior: phase06 / phase07 회귀 0
    - Behavior: pytest exit 0
    - Git: 본 4 파일 단일 commit 박제
  </acceptance_criteria>
  <done>
    3-way contract lockstep 단일 atomic commit 완료 (REVIEWS Cycle 1 반영). TS 7 interface + Python placeholder + contract.md §9 + 20 warning code enum 박제. REVIEWS R1/R2/R3/R4/R5 모두 schema 측면 영구 차단. Plan 08-02 의 force_signals.py dataclass 본체 신설 시 본 schema 정확히 정합.
  </done>
</task>

</tasks>

<verification>
## Phase 8 Plan 01 revised Verification

**Goal-backward derived truths (REVIEWS Cycle 1 schema 측면 반영):**

- TS interface 의 신설 필드 모두 `tsc --noEmit clean` 통과 — coordinateSpace / scaleDenominator / measurementKind / preflightLabelGatePassed / jerkUnit / distanceToPoleNorm / nearPoleRatio / lostNearPoleAtMs (REVIEWS R1/R2/R3/R4/R5)
- Python re-export placeholder 의 forward-declare 주석에 10 name 모두 grep 발견
- docs/contract.md §9 신설 (heading + §9.1~§9.10 subsection + 20 warning code enum 박제)
- dimensions.stability_wobble() helper 박제 + phase06 156 test 회귀 0
- contact_points.yaml 5 motion + default + kind 필드 박제 (Plan 08-00 박제 ContactPrimitiveKind 정합)
- 6 programmatic fixture builder 박제 (JSON 박제 영구 차단, REVIEWS R9)
- drift defense test: dimensions.stability_wobble(angles) == 직접 산식 (abs < 1e-9)
- yaml load test: 5 motion entry 의 kind 필드 정합 (left_inner_thigh=segment / hip=region_proxy / 나머지 keypoint)

**Manual verification (belle 검수 박제 대기):**

- contact_points.yaml 도메인 검수 — 인버트 / 후굴 / 클라임의 expected_contact_points + kind 분류가 폴스포츠 도메인 정합한지 belle/강사 검수 (08-VALIDATION.md §"Manual-Only Verifications" 박제).

**Regression checks:**

- `pytest backend/tests/phase06/ -x` PASS (156 test, stability_score refactor 회귀 0)
- `pytest backend/tests/phase07/ -x` PASS (108 test)
- `pytest backend/tests/phase08/ -x` PASS (Plan 08-00 11 test + Plan 08-01 신설 test)
- `cd app && npx tsc --noEmit` exits 0
</verification>

<success_criteria>
- [x] 3-way contract lockstep 단일 atomic commit (TS + Python placeholder + docs §9, REVIEWS Cycle 1 반영)
- [x] AxisDeviationMetric nullable distance + coordinateSpace + scaleDenominator 박제 (REVIEWS R1 + R2)
- [x] ContactStabilityMetric estimatedStable nullable + distanceToPoleNorm + nearPoleRatio + lostNearPoleAtMs + measurementKind 박제 (REVIEWS R3)
- [x] PhaseBoundary preflightLabelGatePassed 박제 (REVIEWS R4)
- [x] StabilityMetric jerkUnit='deg_per_sec_cubed' 박제 (REVIEWS R5)
- [x] 20 warning code enum (기존 13 + REVIEWS 신설 6) 박제
- [x] dimensions.stability_wobble() raw helper 분리 + drift defense test PASS
- [x] contact_points.yaml 5 motion + default + kind 필드 박제 (REVIEWS R3)
- [x] 6 programmatic fixture builder 박제 (REVIEWS R9)
- [x] phase06 / phase07 회귀 0 + tsc clean
- [x] Plan 08-02 진입 차단 해소
</success_criteria>

<output>
Create `.planning/phases/08-jerk-jitter/08-01-SUMMARY.md` when done. Include:
- 3 task commit SHA + 각 task scope
- TS / Python / contract.md 3-way lockstep 검증 결과 (REVIEWS Cycle 1 신설 필드 박제 확인)
- 20 warning code enum 박제 결과
- drift defense test 결과
- contact_points.yaml 5 motion + kind 필드 박제 (Plan 08-00 ContactPrimitiveKind 정합)
- 6 programmatic fixture builder 박제 결과 (JSON 박제 영구 차단)
- phase06 / phase07 회귀 0 확인
- Plan 08-02 진입 시그널 (force_signals.py 본체)
- Deviation (Rule 1 fix / Rule 3 environment fix 등) 있으면 박제
</output>
