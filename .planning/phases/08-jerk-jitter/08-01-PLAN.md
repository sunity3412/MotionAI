---
phase: 08-jerk-jitter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/types/analysis.ts
  - backend/shared/python/sunity_shared/models.py
  - docs/contract.md
  - backend/shared/python/sunity_shared/analysis/dimensions.py
  - backend/judging_data/__init__.py
  - backend/judging_data/contact_points.yaml
  - backend/tests/phase08/__init__.py
  - backend/tests/phase08/conftest.py
  - backend/tests/phase08/fixtures/__init__.py
  - backend/tests/phase08/fixtures/_factory.py
  - backend/tests/phase08/fixtures/fixture_clean_invert.json
  - backend/tests/phase08/fixtures/fixture_pelvis_drop.json
  - backend/tests/phase08/fixtures/fixture_occluded_lock.json
  - backend/tests/phase08/fixtures/fixture_motion_id_unrecognized.json
  - backend/tests/phase08/fixtures/fixture_jerk_high.json
  - backend/tests/phase08/fixtures/fixture_layback_release.json
  - backend/tests/phase08/test_force_signals_lockstep.py
  - backend/tests/phase08/test_contact_points_yaml_load.py
  - backend/tests/phase08/test_stability_wobble_drift_defense.py
autonomous: true
requirements: [FORCE-01]
subsystem: backend-analysis-core
tags: [phase-08, force-signals, schema-lockstep, drift-defense, yaml-data, wave-0-infra]

scope_sanity_note: "19 파일 중 11 개는 fixture JSON / __init__.py / yaml 정적 데이터 / docs.md — 실제 코드 변경은 6 파일 수준 (analysis.ts + models.py + contract.md + dimensions.py + 3 test 파일). drift defense + Wave 0 infra + 3-way lockstep 모두 단일 plan 50% context budget 정합 — Phase 7 Plan 01 동일 패턴."

must_haves:
  truths:
    - "force_signals.py 모듈은 본 plan 에서 생성하지 않음 — Plan 08-02 가 구현. 본 plan 은 schema (3-way lockstep) + drift defense helper + yaml 데이터 + Wave 0 test infra 만 제공"
    - "TS interface (app/src/types/analysis.ts) ↔ Python re-export (backend/shared/python/sunity_shared/models.py) ↔ docs/contract.md §9 세 곳이 단일 atomic commit 으로 동시 갱신 (Phase 6/7 박제 패턴 정합) — per D-08-U3"
    - "7 신설 TS 타입 / Python re-export 정합: MotionPhase / DeviationDirection / SeverityLevel / MetricConfidence / ContactPoint enum + PhaseBoundary / AxisDeviationMetric / StabilityMetric / ContactStabilityMetric / ForceSignalsReport dataclass + AnalysisResult.forceSignalsReport optional field"
    - "Python re-export 는 본 plan 에서 placeholder — 실제 dataclass 모듈은 Plan 08-02 가 신설. models.py 의 import 는 Plan 08-02 에서 활성화. 본 plan 은 TS interface + contract.md §9 schema 박제만 (Python 측은 forward-declare 주석)"
    - "dimensions.stability_wobble(angles, profile=None) -> float helper 가 dimensions.py 에 신설되어 stability_score 의 raw inter-frame median wobble 산식을 분리 제공 (drift defense source — Plan 08-02 의 force_signals.jitter_score 가 본 helper 호출) — per D-08-C1 + RESEARCH §StabilityMetric Algorithm Option A"
    - "기존 dimensions.stability_score() 는 stability_wobble() 호출 + kismam.score_from_deviation 분리 형태로 refactor — 동일 input 시 refactor 전후 동일 output 회귀 0 (Phase 12.5 v4 Codex HIGH-2 drift defense 패턴 정합)"
    - "backend/judging_data/contact_points.yaml 신설: motion_id × expected_contact_points (12 ContactPoint enum) 매핑. ref-invert / ref-foxtop / ref-foxtop-split / ref-climb / ref-sideway-spin 5 motion + default 1 = 6 entries. yaml.safe_load 사용 강제 (yaml.load 금지 — V12 security)"
    - "contact_points.yaml 매핑은 researcher 초안 + belle 검수 박제 대기 — 본 plan 작성 시 5 motion entry 박제 + 박제 후속 belle 검수 manual checkpoint 박제 (manual-only verification, 본 plan 의 자동 검증은 yaml 구조 유효성 + 12 ContactPoint enum 정합만)"
    - "Wave 0 test 인프라 13 신설: phase08/__init__.py + conftest.py + fixtures/__init__.py + _factory.py + 6 fixture JSON + 3 test 파일 (lockstep + yaml_load + stability_wobble drift defense). Plan 08-02 / 08-03 의 단위 test 가 본 인프라 위에 즉시 실행 가능"
    - "drift defense test (test_stability_wobble_drift_defense.py) 가 동일 (T=50, J=8) angles input 에서 dimensions.stability_wobble(angles) 호출 결과와 직접 산식 (np.nanmean(np.nanmedian(np.abs(np.diff(angles, axis=0)), axis=0))) 의 차이 < 1e-9 검증 — Plan 08-02 의 force_signals 가 본 helper import 강제. 산식 복제 차단"
    - "3-way lockstep test 가 7 신설 TS interface / Python placeholder / contract.md §9 의 field name 모두 grep 통과 — 부분 commit 검출. (camelCase TS ↔ snake_case Python ↔ docs §9 atomic)"
    - "yaml load test 는 contact_points.yaml 의 motions 키 + default 키 + 5 motion entry 의 expected_contact_points 가 list[str] 이고 모든 원소가 12 ContactPoint enum 값임을 검증"
  artifacts:
    - path: app/src/types/analysis.ts
      provides: "Phase 8 Force Signals 신설 7 interface — MotionPhase / DeviationDirection / SeverityLevel / MetricConfidence / ContactPoint enum + PhaseBoundary / AxisDeviationMetric / StabilityMetric / ContactStabilityMetric / ForceSignalsReport interface + AnalysisResult.forceSignalsReport optional nullable field"
      contains:
        - "export type MotionPhase = 'entry' | 'lock' | 'transition' | 'final_shape' | 'hold'"
        - "export type DeviationDirection ="
        - "export type SeverityLevel = 'low' | 'medium' | 'high'"
        - "export type MetricConfidence = 'low' | 'medium' | 'high'"
        - "export type ContactPoint ="
        - "export interface PhaseBoundary"
        - "export interface AxisDeviationMetric"
        - "export interface StabilityMetric"
        - "export interface ContactStabilityMetric"
        - "export interface ForceSignalsReport"
        - "forceSignalsReport?: ForceSignalsReport | null"
    - path: backend/shared/python/sunity_shared/models.py
      provides: "Python re-export placeholder — Plan 08-02 가 dataclass 신설 후 import 활성화. 본 plan 은 주석으로 forward-declare 만 (3-way lockstep 의 Python 측면 박제 위치 강제)"
      contains:
        - "# Phase 8 Force Signals — Plan 08-02 신설 후 import 활성화"
        - "# from .analysis.force_signals import"
    - path: docs/contract.md
      provides: "§9 ForceSignalsReport (Phase 8 신설 — FORCE-01 신호 layer) 신규 서브섹션. 7 type alias + 5 dataclass schema 표 + warning code enum + ContactPoint enum + MotionPhase enum 박제. §8 / §8.3 (Phase 6/7) 패턴 정합"
      contains:
        - "## §9. ForceSignalsReport (Phase 8 신설 — FORCE-01 신호 layer)"
        - "MotionPhase"
        - "PhaseBoundary"
        - "AxisDeviationMetric"
        - "StabilityMetric"
        - "ContactStabilityMetric"
        - "ForceSignalsReport"
        - "forceSignalsReport"
    - path: backend/shared/python/sunity_shared/analysis/dimensions.py
      provides: "stability_wobble() raw helper 분리 + stability_score() refactor (helper 호출 + kismam.score_from_deviation 분리). drift defense source — Plan 08-02 force_signals.compute_stability_metrics 가 본 helper import"
      contains:
        - "def stability_wobble(angles"
        - "Plan 08-01 신설"
        - "force_signals.py 가 본 helper import"
    - path: backend/judging_data/contact_points.yaml
      provides: "motion_id × expected_contact_points (12 ContactPoint enum) 매핑 정적 yaml. 5 motion entry (ref-invert / ref-foxtop / ref-foxtop-split / ref-climb / ref-sideway-spin) + default. yaml.safe_load 사용 (yaml.load 금지)"
      contains:
        - "motions:"
        - "ref-invert:"
        - "ref-foxtop:"
        - "ref-foxtop-split:"
        - "ref-climb:"
        - "ref-sideway-spin:"
        - "default:"
        - "expected_contact_points:"
    - path: backend/tests/phase08/conftest.py
      provides: "Phase 8 fixture 디렉토리 상수 PHASE08_FIXTURES_DIR + synthetic PoseFrame factory fixture + GeminiMomentExtractor mock fixture (Plan 08-03 사용) + BodyNormalizationProfile synthetic factory (Phase 6/7 conftest 패턴 정합)"
      contains:
        - "PHASE08_FIXTURES_DIR"
        - "synthetic_pose_frames"
        - "mock_gemini_extractor"
        - "synthetic_body_profile"
    - path: backend/tests/phase08/fixtures/_factory.py
      provides: "load_fixture_raw + 6 fixture JSON loader 헬퍼. Phase 7 _factory.py 패턴 정합 — JSON → 합성 PoseFrame list 변환"
      contains:
        - "def load_fixture_raw"
        - "def load_pose_frames_from_fixture"
    - path: backend/tests/phase08/test_force_signals_lockstep.py
      provides: "3-way lockstep drift defense — 7 신설 TS interface / Python placeholder / contract.md §9 의 field name 모두 grep 통과 검증 (Phase 6/7 lockstep test 패턴 정합)"
      contains:
        - "_FIELD_MAP"
        - "MotionPhase"
        - "ForceSignalsReport"
        - "phaseBoundaries"
        - "axisMetrics"
    - path: backend/tests/phase08/test_contact_points_yaml_load.py
      provides: "contact_points.yaml 구조 유효성 + 12 ContactPoint enum 정합 검증. motions / default 키 박제 + 5 motion entry 의 expected_contact_points list[str] 검증"
      contains:
        - "_VALID_CONTACT_POINTS"
        - "def test_yaml_structure"
        - "def test_motion_entries_all_valid_contact_points"
        - "def test_default_entry_present"
    - path: backend/tests/phase08/test_stability_wobble_drift_defense.py
      provides: "drift defense — dimensions.stability_wobble() 직접 호출 vs 산식 np.nanmean(np.nanmedian(np.abs(np.diff(angles, axis=0)), axis=0)) 일치 검증. Plan 08-02 force_signals.jitter_score 의 drift 차단 source"
      contains:
        - "def test_stability_wobble_matches_direct_formula"
        - "def test_stability_wobble_short_input_returns_zero"
        - "def test_stability_score_refactor_preserves_output"
  key_links:
    - from: app/src/types/analysis.ts
      to: docs/contract.md
      via: "3-way contract — camelCase TS ↔ Python placeholder ↔ docs §9 atomic commit (Phase 6/7 박제 패턴, 116f400 / a444726 / d4d8af4)"
      pattern: "ForceSignalsReport|MotionPhase|PhaseBoundary|AxisDeviationMetric|StabilityMetric|ContactStabilityMetric"
    - from: backend/shared/python/sunity_shared/analysis/dimensions.py
      to: backend/tests/phase08/test_stability_wobble_drift_defense.py
      via: "drift defense — stability_wobble() helper 신설 + 직접 산식 일치 검증. Plan 08-02 force_signals.jitter_score 가 본 helper import 강제 (drift 차단)"
      pattern: "stability_wobble"
    - from: backend/judging_data/contact_points.yaml
      to: backend/tests/phase08/test_contact_points_yaml_load.py
      via: "정적 yaml 구조 + 12 ContactPoint enum 정합 검증. Plan 08-02 force_signals._load_expected_contact_points 가 본 파일 load"
      pattern: "expected_contact_points"
---

<objective>
Phase 8 vertical slice (1/3) — **schema lockstep + drift defense helper + yaml + Wave 0 test 인프라**. 본 plan 은 Phase 8 의 모든 후속 작업이 의존하는 schema / data / test 기반을 제공:

(a) **3-way contract lockstep** — TS interface (app/src/types/analysis.ts) + Python re-export placeholder (backend/shared/python/sunity_shared/models.py) + docs/contract.md §9 신설을 단일 atomic commit (D-08-U3 + Phase 6/7 박제 패턴).
(b) **dimensions.stability_wobble() raw helper 분리** — force_signals.py 가 jitter_score 산출 시 import 강제 (drift defense, Phase 12.5 v4 Codex HIGH-2 패턴, D-08-C1).
(c) **backend/judging_data/contact_points.yaml** — motion_id × expected_contact_points 매핑 정적 데이터 (5 motion + default, researcher 초안 — belle 검수 manual gate 박제).
(d) **Wave 0 test 인프라** — phase08/ 디렉토리 + 6 fixture JSON + conftest.py + 3 test 파일 (lockstep + yaml_load + drift defense). Plan 08-02 / 08-03 의 단위 test 가 즉시 실행 가능.

Purpose: FORCE-01 success criteria #1~4 의 schema 박제 + Plan 08-02 (본체 구현) 진입 차단 해소.
Output: 신설 3 파일 (dimensions.py wobble helper / contact_points.yaml / 3 test 파일) + 수정 3 파일 (analysis.ts / models.py / contract.md) + Wave 0 신설 11 파일 (phase08/__init__.py + conftest.py + fixtures/__init__.py + _factory.py + 6 JSON fixture + judging_data/__init__.py).

**박제 메모 정합:** [[firestore-nested-array-flat]] (warnings list[str] schema 박제) + [[scoring-dimensions-ipsf]] (severity 임계 fixed) + [[analysis-objectivity-no-human-scores]] (canned warning code 영어 snake_case) + [[mvp-simple-pilot-quality]] (motion-agnostic Layer 1 schema + yaml 1줄 박제) + [[feedback-analysis-first]] (5단계 분할 정확도) + [[no-baekje-filler]] (test 안 "박제" 단어 X).

per D-08-A1 (5단계 enum) + D-08-A2 (Layer 1+2 confidence enum) + D-08-B1 (yaml 매핑) + D-08-C1 (stability_wobble helper 분리) + D-08-D1 (body-scale 정규화 schema) + D-08-U1 (가림 스무딩 — Plan 08-02 가 활용할 schema) + D-08-U2 (confidence 필드) + D-08-U3 (3-way lockstep).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-jerk-jitter/08-CONTEXT.md
@.planning/phases/08-jerk-jitter/08-RESEARCH.md
@.planning/phases/08-jerk-jitter/08-VALIDATION.md
@.planning/phases/06-coaching/06-CONTEXT.md
@.planning/phases/07-difference-classification/07-CONTEXT.md
@.planning/phases/07-difference-classification/07-01-PLAN.md
@CLAUDE.md
@docs/contract.md
@app/src/types/analysis.ts
@backend/shared/python/sunity_shared/models.py
@backend/shared/python/sunity_shared/analysis/dimensions.py
@backend/shared/python/sunity_shared/firestore_admin.py
@backend/shared/python/sunity_shared/judging/loader.py
@backend/tests/phase07/conftest.py
@backend/tests/phase07/fixtures/_factory.py
@backend/tests/phase07/test_body_comparison_report_phase7_lockstep.py
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Phase 8 = backend 분석 코어 layer. 본 plan = schema + yaml + test infra. 사용자 입력 영역 0. yaml 은 정적 repo 박제 (gitignore X). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | T (Tampering / schema drift) | TS interface ↔ Python placeholder ↔ docs/contract.md §9 lockstep | mitigate | 3-way lockstep drift defense test (test_force_signals_lockstep.py) — Phase 6/7 박제 패턴 정합 (Phase 6 116f400 / Phase 7 d4d8af4). 부분 commit 차단. |
| T-08-01-02 | T (Tampering / yaml deserialization RCE) | backend/judging_data/contact_points.yaml load | mitigate | yaml.safe_load 강제 (yaml.load 금지). loader 는 Plan 08-02 가 박제 — 본 plan 의 yaml load test 는 safe_load 사용. CLAUDE.md §3 + RESEARCH §Security V12 정합. |
| T-08-01-03 | T (Tampering / drift between dimensions vs force_signals) | dimensions.stability_score / stability_wobble helper 분리 | mitigate | drift defense test (test_stability_wobble_drift_defense.py) — refactor 전후 동일 input → 동일 output 회귀 0. Plan 08-02 의 force_signals.jitter_score 가 본 helper import 강제 (산식 복제 차단). Phase 12.5 v4 Codex HIGH-2 패턴 정합. |
| T-08-01-04 | I (Information disclosure / yaml 안 PII) | contact_points.yaml 내용 | accept | 정적 도메인 데이터 (motion_id + ContactPoint enum). PII 없음. repo public 가능. |
| T-08-01-05 | D (Denial of Service / Firestore nested-array crash) | ForceSignalsReport schema (warnings: list[str] at multiple levels) | mitigate | Phase 6 W5 `_validate_flat_dict_no_nested_array` 가 list[str] 자동 통과 (firestore_admin.py:78-95). 단 list[dict-of-scalars-only] 안 warnings list[str] 는 `_validate_dict_only_scalars` 가 reject — **Plan 08-03 가 명세 확장** (본 plan 은 schema 박제만, validator 확장은 Plan 08-03 에서). |
| T-08-01-SC | Tampering | npm/pip/cargo installs | mitigate | 신규 의존성 0 — 기존 환경 그대로 (numpy + PyYAML 기존 박제). RESEARCH §Package Legitimacy Audit "N/A (no new packages)". slopcheck 불요. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 test 인프라 (phase08/ 디렉토리 + 6 fixture JSON + conftest.py + factory loader)</name>
  <files>
    backend/tests/phase08/__init__.py
    backend/tests/phase08/conftest.py
    backend/tests/phase08/fixtures/__init__.py
    backend/tests/phase08/fixtures/_factory.py
    backend/tests/phase08/fixtures/fixture_clean_invert.json
    backend/tests/phase08/fixtures/fixture_pelvis_drop.json
    backend/tests/phase08/fixtures/fixture_occluded_lock.json
    backend/tests/phase08/fixtures/fixture_motion_id_unrecognized.json
    backend/tests/phase08/fixtures/fixture_jerk_high.json
    backend/tests/phase08/fixtures/fixture_layback_release.json
  </files>
  <read_first>
    backend/tests/phase07/__init__.py
    backend/tests/phase07/conftest.py
    backend/tests/phase07/fixtures/_factory.py
    backend/tests/phase07/fixtures/fixture_classification_allowed.json
    backend/tests/__init__.py
    backend/shared/python/sunity_shared/analysis/pose_frame.py
    backend/shared/python/sunity_shared/analysis/skeleton.py
    backend/shared/python/sunity_shared/analysis/body_normalization.py
    .planning/phases/08-jerk-jitter/08-RESEARCH.md §"Test + Sweep Strategy" + §"Wave 0 Gaps" + §"Code Examples"
    .planning/phases/08-jerk-jitter/08-VALIDATION.md §"Wave 0 Requirements"
  </read_first>
  <behavior>
    - 빈 `__init__.py` 2 개 (phase08/ + phase08/fixtures/)
    - `conftest.py` 는 3 fixture 박제:
      * `PHASE08_FIXTURES_DIR: Path = Path(__file__).resolve().parent / "fixtures"` 상수 (Phase 7 conftest 패턴 미러)
      * `synthetic_body_profile()` pytest fixture — BodyNormalizationProfile (torso_scale=1.0, estimated_height_scale=1.0, etc.) 합성 (Plan 08-02 의 distance 정규화 입력)
      * `mock_gemini_extractor()` pytest fixture — GeminiMomentExtractor.extract_key_moments() 가 미리 박제된 KeyMoment list 반환하는 unittest.mock.MagicMock (Plan 08-03 Layer 2 test 입력)
    - `fixtures/_factory.py` = `load_fixture_raw(name: str) -> dict[str, Any]` (Phase 7 미러) + `load_pose_frames_from_fixture(name: str) -> tuple[list[PoseFrame], PoleAxis, BodyNormalizationProfile, dict]` 신설 (return: pose_frames, pole_axis, body_profile, expected dict)
    - 6 fixture JSON 박제 (RESEARCH.md §"Wave 0 Gaps" + 08-VALIDATION.md 정합):
      1. `fixture_clean_invert.json` — 정은지급 깔끔한 invert. T=60 frame, motion_id="ref-invert", 모든 phase 분명 (entry 5f / lock 10f / transition 5f / final_shape 5f / hold 35f), reliability='high' 90%+, body_profile.torso_scale=1.0. expected.severity_all="low" / expected.estimated_stable=true.
      2. `fixture_pelvis_drop.json` — pelvis 가 hold 중 outward 이동. T=60, motion_id="ref-foxtop", hold phase 안 pelvis x-coord 가 frame 30~50 사이 +0.35 (body-scale 30% 이상) outward drift. expected.axis_metrics[hold].severity="high" / deviation_direction="outward".
      3. `fixture_occluded_lock.json` — lock phase frame 의 reliability='low' 60%. T=60, lock 구간 (frame 5~15) 안 6/10 frame reliability='low'. expected.warning_emitted="occlusion_high_in_phase" / lock metric confidence='low'.
      4. `fixture_motion_id_unrecognized.json` — motion_id=None fallback. T=60, motion_id=null. expected.expected_contact_points=[] / contact metric estimated_stable=null / warning="motion_unrecognized".
      5. `fixture_jerk_high.json` — transition phase jerk > 15 deg/frame^3. T=60, transition (frame 15~25) 안 angles 3차 미분 > 15. expected.stability_metrics[transition].jerk_score>15 / severity="high".
      6. `fixture_layback_release.json` — hold 안 lostContactAtMs (시간 패턴 abnormal release). motion_id="ref-foxtop", hold phase (frame 25~55) 안 frame 40 시점 left_hand keypoint 가 폴축에서 멀어짐 (distance > 0.20 body-scale, debounce 2 frames). expected.contact_metrics["left_hand"][hold].lost_contact_at_ms=4444 (frame 40 × 111ms @ 9fps) / warning="abnormal_release_during_hold".
    - 각 fixture JSON 구조:
      ```
      {
        "metadata": { "name": ..., "motion_id": ..., "fps": 9.0, "frames": 60 },
        "pose_frames": [ { "frame_idx": ..., "timestamp_ms": ..., "keypoints_3d_pole_aligned": {...}, "reliability": "high"|"medium"|"low" }, ... ],
        "pole_axis": { "axis_vector": [0, 1, 0], "base_point": [0, 0, 0], "confidence_level": "high" | "low", "source": "detected" | "vertical_fallback" },
        "body_profile": { "torso_scale": 1.0, "estimated_height_scale": 1.0, ... (BodyNormalizationProfile 필수 필드) },
        "expected": { ... — 각 fixture 별 assertion 박제 ... }
      }
      ```
  </behavior>
  <action>
    Phase 7 의 backend/tests/phase07/conftest.py + fixtures/_factory.py 패턴을 그대로 미러. `PHASE08_FIXTURES_DIR` 상수만 변경. `load_pose_frames_from_fixture` 헬퍼는 JSON 의 "pose_frames" 키를 `PoseFrame(frame_idx=..., timestamp_ms=..., keypoints_3d_pole_aligned=..., reliability=...)` list 로 변환. PoseFrame.keypoints_3d_pole_aligned 의 dict[str, Keypoint3DAligned] 박제 — JSON 의 nested dict 를 `Keypoint3DAligned(x=, y=, z=, visibility=)` instance 로 변환 (pose_frame.py 의 dataclass 시그너처 정확히 mirror).

    `conftest.py` 의 `synthetic_body_profile` fixture 는 body_normalization.BodyNormalizationProfile 의 모든 필수 필드 (torso_scale, estimated_height_scale, leg_scale, arm_scale, asymmetry, warnings, confidence) 를 합성 (Plan 06 박제 v5 정합 — measurer fallback 1.0).

    `mock_gemini_extractor` fixture 는 unittest.mock.MagicMock 인스턴스를 반환 — `extract_key_moments` method 가 미리 박제된 KeyMoment list 반환 (timestamps: setup=0.55s / hold=1.65s / peak=3.0s / release=6.0s). Plan 08-03 의 Layer 2 test 가 본 fixture 입력 사용.

    6 fixture JSON 의 keypoints_3d_pole_aligned 박제는 합성 (실 RTMW 출력 mimic) — synthetic 좌표 박제 (예: frame 0 의 left_hip = {x: -0.1, y: 0.5, z: 0.0}). 정확한 좌표값은 RESEARCH §Code Examples + body-scale (torso_scale=1.0) 정합 박제.

    per RESEARCH §"Wave 0 Gaps" + VALIDATION §"Wave 0 Requirements".
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && python3 -c "from pathlib import Path; import json; d = Path('backend/tests/phase08/fixtures'); fixtures = ['fixture_clean_invert.json','fixture_pelvis_drop.json','fixture_occluded_lock.json','fixture_motion_id_unrecognized.json','fixture_jerk_high.json','fixture_layback_release.json']; assert all((d/f).exists() for f in fixtures), 'missing fixture'; clean = json.loads((d/'fixture_clean_invert.json').read_text()); assert clean['metadata']['motion_id'] == 'ref-invert' and clean['metadata']['frames'] == 60; unrecog = json.loads((d/'fixture_motion_id_unrecognized.json').read_text()); assert unrecog['metadata']['motion_id'] is None; layback = json.loads((d/'fixture_layback_release.json').read_text()); assert layback['expected']['contact_metrics']['left_hand']['hold']['lost_contact_at_ms'] == 4444; conf = Path('backend/tests/phase08/conftest.py'); assert 'PHASE08_FIXTURES_DIR' in conf.read_text() and 'synthetic_body_profile' in conf.read_text() and 'mock_gemini_extractor' in conf.read_text(); print('WAVE0_INFRA_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `backend/tests/phase08/__init__.py` (empty), `phase08/conftest.py`, `phase08/fixtures/__init__.py` (empty), `phase08/fixtures/_factory.py`
    - All 6 fixture JSON exist with `metadata.motion_id` / `metadata.frames` / `pose_frames` / `pole_axis` / `body_profile` / `expected` keys
    - Source: `grep -c "PHASE08_FIXTURES_DIR" backend/tests/phase08/conftest.py` >= 1
    - Source: `grep -c "synthetic_body_profile" backend/tests/phase08/conftest.py` >= 1
    - Source: `grep -c "mock_gemini_extractor" backend/tests/phase08/conftest.py` >= 1
    - Source: `grep -c "def load_pose_frames_from_fixture" backend/tests/phase08/fixtures/_factory.py` >= 1
    - Source: `python3 -c "import json; d=json.loads(open('backend/tests/phase08/fixtures/fixture_motion_id_unrecognized.json').read()); assert d['metadata']['motion_id'] is None; print('OK')"` prints OK
    - Source: `python3 -c "import json; d=json.loads(open('backend/tests/phase08/fixtures/fixture_layback_release.json').read()); assert d['expected']['contact_metrics']['left_hand']['hold']['lost_contact_at_ms'] == 4444; print('OK')"` prints OK
    - Behavior: bash verify command above prints `WAVE0_INFRA_OK` and exits 0
    - Behavior: `PYTHONPATH=backend/shared/python:. backend/.venv/bin/python3 -m pytest backend/tests/phase08/ --collect-only` exits 0 (collection success, no errors)
  </acceptance_criteria>
  <done>
    phase08/ Wave 0 test 인프라 완료. 6 fixture JSON 박제 (synthetic PoseFrame list + body_profile + pole_axis + expected dict). _factory.py 의 load_pose_frames_from_fixture 가 Plan 08-02 의 모든 단위 test 입력 source. conftest.py 의 3 fixture (synthetic_body_profile / mock_gemini_extractor / PHASE08_FIXTURES_DIR) 가 Plan 08-02 / 08-03 단위 test 의존성 해소.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: dimensions.stability_wobble() raw helper 분리 + stability_score() refactor + drift defense test + contact_points.yaml 신설 + yaml load test</name>
  <files>
    backend/shared/python/sunity_shared/analysis/dimensions.py
    backend/judging_data/__init__.py
    backend/judging_data/contact_points.yaml
    backend/tests/phase08/test_stability_wobble_drift_defense.py
    backend/tests/phase08/test_contact_points_yaml_load.py
  </files>
  <read_first>
    backend/shared/python/sunity_shared/analysis/dimensions.py (line 66-79 stability_score 본체 + line 126-170 _select_window + stability_wobble_by_joint 직접 읽기)
    backend/shared/python/sunity_shared/analysis/kismam.py (score_from_deviation 시그너처)
    backend/shared/python/sunity_shared/judging/loader.py (PyYAML 사용 패턴)
    backend/judging_data/criteria/ (있다면 yaml 구조 참고)
    .planning/phases/08-jerk-jitter/08-RESEARCH.md §"StabilityMetric Algorithm" + §"ContactStabilityMetric Algorithm — expected_contact_points yaml" + §"Code Examples Example 2"
    .planning/phases/08-jerk-jitter/08-CONTEXT.md §Decisions B + C
  </read_first>
  <behavior>
    - **dimensions.stability_wobble() helper 신설** (raw 산식 분리):
      * 시그너처: `def stability_wobble(angles, profile: "TechniqueProfile | None" = None) -> float`
      * 본체: `sliced, _ = _select_window(angles, profile); if sliced.shape[0] < 2: return 0.0; inter_frame_diff = np.abs(np.diff(sliced, axis=0)); median_jerk = np.nanmedian(inter_frame_diff, axis=0); return float(np.nanmean(median_jerk))`
      * docstring: "Raw inter-frame median wobble (degrees) — stability_score 의 score 변환 전 값. force_signals.py (Plan 08-02) 가 본 helper import 해 jitter_score 산출 — drift 방지 (Phase 12.5 v4 Codex HIGH-2 패턴 정합)."
    - **dimensions.stability_score() refactor** (산식 복제 차단):
      * 본체 변경: wobble 산식 직접 X → `wobble = stability_wobble(angles, profile); sliced, _ = _select_window(angles, profile); if sliced.shape[0] < 2: return 100; return kismam.score_from_deviation(wobble, _STABILITY_TOL_DEG)`
      * 회귀 0 — refactor 전후 동일 input → 동일 output 강제 (drift defense test 가 검증)
    - **backend/judging_data/contact_points.yaml 신설** (D-08-B1 + D-08-B5):
      * `motions:` top-level + 5 motion entry + `default:` entry
      * 각 entry 는 `expected_contact_points: [...]` list of ContactPoint enum string
      * researcher 초안 박제 (belle 검수 manual gate):
        - ref-invert: [left_hand, right_hand, left_inner_thigh, right_inner_thigh]
        - ref-foxtop: [left_hand, right_hand, left_ankle, right_ankle, hip]
        - ref-foxtop-split: [left_hand, right_hand, left_ankle, right_ankle, hip]
        - ref-climb: [left_hand, right_hand, left_inner_thigh, right_inner_thigh]
        - ref-sideway-spin: [left_hand, right_hand, left_knee, right_knee]
        - default: [] (motion_id 미인식 fallback)
      * yaml 상단에 헤더 주석: "# Plan 08-01 신설. motion_id × expected_contact_points 매핑 (12 ContactPoint enum). belle/강사 도메인 검수 필수 — researcher 초안."
    - **backend/judging_data/__init__.py 신설** (빈 파일 — 패키지 marker. judging_data/criteria/ 가 이미 존재하면 __init__.py 도 존재할 가능성. 없으면 신설).
    - **test_stability_wobble_drift_defense.py 신설**:
      * Test 1 `test_stability_wobble_matches_direct_formula`: rng=np.random.default_rng(0), angles=rng.normal(120, 5, size=(50, 8)). `direct = float(np.nanmean(np.nanmedian(np.abs(np.diff(angles, axis=0)), axis=0)))`. `helper = dimensions.stability_wobble(angles, profile=None)`. `assert abs(helper - direct) < 1e-9`
      * Test 2 `test_stability_wobble_short_input_returns_zero`: angles.shape[0] == 1 → stability_wobble returns 0.0
      * Test 3 `test_stability_score_refactor_preserves_output`: refactor 전후 동일 output 검증 — 같은 input (T=50, J=8) 에 stability_score 호출 시 100 또는 0~99 사이 정수 반환. NaN-only input → 100 또는 graceful fallback. (Phase 6 회귀 0 확인 — Phase 12.5 / Phase 6 기존 test 가 Plan 08-01 후 PASS 유지 강제)
    - **test_contact_points_yaml_load.py 신설**:
      * 모듈 상단 `_VALID_CONTACT_POINTS = frozenset({"left_hand", "right_hand", "left_inner_thigh", "right_inner_thigh", "left_knee", "right_knee", "left_foot", "right_foot", "left_ankle", "right_ankle", "hip", "unknown"})` (12 enum)
      * Test 1 `test_yaml_structure`: yaml.safe_load 호출. raw["motions"] is dict + raw["default"] is dict. 5 motion entry 모두 존재 (ref-invert / ref-foxtop / ref-foxtop-split / ref-climb / ref-sideway-spin).
      * Test 2 `test_motion_entries_all_valid_contact_points`: 각 motion entry 의 `expected_contact_points` is list of str + 모든 원소가 `_VALID_CONTACT_POINTS` 안 박제.
      * Test 3 `test_default_entry_present_and_empty`: raw["default"]["expected_contact_points"] == [].
      * Test 4 `test_yaml_uses_safe_load_path`: contact_points.yaml 의 파일 내용에 `!!python/` (yaml 임의 객체 marker) 가 없는지 검증 — safe_load 호환성 강제.
  </behavior>
  <action>
    `dimensions.py` 의 line 66-79 stability_score 본체를 두 함수로 분리:
      (1) 신설 `stability_wobble(angles, profile=None) -> float` — 산식 본체 (np.diff + median + mean)
      (2) refactor `stability_score(angles, profile=None) -> int` — stability_wobble 호출 + kismam.score_from_deviation 분리. 시그너처 / 반환값 동일 (회귀 0).

    line 79 직전 (`return kismam.score_from_deviation(wobble, _STABILITY_TOL_DEG)` 위) 에 신설 helper 박제. stability_wobble 위치는 stability_score 직전 (helper-first 패턴). 모듈 docstring 끝에 신설 helper 박제 메모 추가: "Plan 08-01 (2026-06-08): stability_wobble() raw helper 분리 — force_signals.py (Plan 08-02) 가 jitter_score 산출 시 본 helper import 강제 (drift defense, Phase 12.5 v4 Codex HIGH-2 패턴 정합)."

    `backend/judging_data/contact_points.yaml` 박제 — researcher 초안 5 motion + default + 헤더 주석 (belle 검수 manual gate 표기). yaml.safe_load 호환 (no `!!python/` marker).

    `backend/judging_data/__init__.py` 가 이미 존재하면 박제 변경 X, 없으면 빈 파일 신설.

    `test_stability_wobble_drift_defense.py` 와 `test_contact_points_yaml_load.py` 박제 — 위 behavior 의 5 test (3 drift + 4 yaml) 박제. test 파일 상단에 `from __future__ import annotations` + `import numpy as np` + `import pytest` + `import yaml` + 모듈 import.

    yaml 파일 path 박제는 `Path(__file__).resolve().parent.parent.parent.parent / "judging_data" / "contact_points.yaml"` (backend/tests/phase08/test_contact_points_yaml_load.py → backend/) — RESEARCH.md §"Loader" 패턴 정합. 또는 `BACKEND_ROOT` 헬퍼 박제.

    per RESEARCH §StabilityMetric Algorithm Option A + §expected_contact_points yaml + §Security V12.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && PYTHONPATH=backend/shared/python:. backend/.venv/bin/python3 -m pytest backend/tests/phase08/test_stability_wobble_drift_defense.py backend/tests/phase08/test_contact_points_yaml_load.py backend/tests/phase06/ -x</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `backend/judging_data/contact_points.yaml` with `motions:` and `default:` keys
    - File exists: `backend/tests/phase08/test_stability_wobble_drift_defense.py` and `test_contact_points_yaml_load.py`
    - Source: `grep -c "def stability_wobble" backend/shared/python/sunity_shared/analysis/dimensions.py` >= 1
    - Source: `grep -c "Plan 08-01" backend/shared/python/sunity_shared/analysis/dimensions.py` >= 1
    - Source: `python3 -c "import yaml; raw = yaml.safe_load(open('backend/judging_data/contact_points.yaml').read()); assert set(raw['motions'].keys()) >= {'ref-invert', 'ref-foxtop', 'ref-foxtop-split', 'ref-climb', 'ref-sideway-spin'}; assert raw['default']['expected_contact_points'] == []; print('OK')"` prints OK
    - Source: `grep -c "ref-invert" backend/judging_data/contact_points.yaml` >= 1
    - Behavior (drift defense): test_stability_wobble_matches_direct_formula PASS (helper 산식 vs 직접 산식 일치 abs < 1e-9)
    - Behavior (refactor 회귀 0): phase06 기존 156 test (stability_score 사용) 모두 PASS (refactor 전후 동일 output)
    - Behavior (yaml load): test_motion_entries_all_valid_contact_points PASS — 5 motion 의 모든 expected_contact_points 가 12 ContactPoint enum 정합
    - Behavior: pytest exit 0 (위 verify command)
  </acceptance_criteria>
  <done>
    dimensions.stability_wobble() raw helper 분리 완료 (drift defense source). stability_score() refactor 회귀 0. contact_points.yaml 5 motion + default + yaml.safe_load 호환 박제 (belle 검수 manual gate 박제). drift defense test + yaml load test PASS. Plan 08-02 의 force_signals.compute_stability_metrics 가 dimensions.stability_wobble import 강제 + force_signals._load_expected_contact_points 가 yaml.safe_load 호출 가능.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 3-way contract lockstep 단일 atomic commit — TS 7 interface + Python placeholder + contract.md §9 + lockstep drift defense test</name>
  <files>
    app/src/types/analysis.ts
    backend/shared/python/sunity_shared/models.py
    docs/contract.md
    backend/tests/phase08/test_force_signals_lockstep.py
  </files>
  <read_first>
    app/src/types/analysis.ts (line 1-200 BodyComparisonReport 박제 패턴 직접 읽기)
    backend/shared/python/sunity_shared/models.py
    docs/contract.md (§8 BodyComparisonReport + §8.3 Phase 7 분류 — 패턴 mirror)
    .planning/phases/08-jerk-jitter/08-RESEARCH.md §"3-Way Contract Additions"
    .planning/phases/08-jerk-jitter/08-CONTEXT.md §Decisions U
    backend/tests/phase07/test_body_comparison_report_phase7_lockstep.py
    backend/tests/phase06/test_body_comparison_report_lockstep.py
  </read_first>
  <behavior>
    - **app/src/types/analysis.ts** — 신설 7 type / interface + 1 AnalysisResult field:
      * 5 type alias: `MotionPhase = 'entry' | 'lock' | 'transition' | 'final_shape' | 'hold'` / `DeviationDirection = 'up' | 'down' | 'left' | 'right' | 'outward' | 'inward' | 'unknown'` / `SeverityLevel = 'low' | 'medium' | 'high'` / `MetricConfidence = 'low' | 'medium' | 'high'` / `ContactPoint = 'left_hand' | 'right_hand' | 'left_inner_thigh' | 'right_inner_thigh' | 'left_knee' | 'right_knee' | 'left_foot' | 'right_foot' | 'left_ankle' | 'right_ankle' | 'hip' | 'unknown'`
      * 5 interface (RESEARCH §"TS additions" 정확히 mirror):
        - `PhaseBoundary { phase: MotionPhase; startFrameIdx: number; endFrameIdx: number; startMs: number; endMs: number; confidence: MetricConfidence; source: 'heuristic' | 'gemini_assisted' | 'heuristic_fallback'; }`
        - `AxisDeviationMetric { phase: MotionPhase; pelvisDistanceFromPoleAxis: number; chestDistanceFromPoleAxis: number; shoulderTilt: number; hipTilt: number; deviationDirection: DeviationDirection; severity: SeverityLevel; confidence: MetricConfidence; warnings: string[]; }`
        - `StabilityMetric { phase: MotionPhase; jitterScore: number; jerkScore: number; holdStabilityScore: number | null; unstableBodyParts: string[]; severity: SeverityLevel; confidence: MetricConfidence; warnings: string[]; }`
        - `ContactStabilityMetric { phase: MotionPhase; contactPoint: ContactPoint; estimatedStable: boolean | null; lostContactAtMs: number | null; distanceAtLostMs: number | null; severity: SeverityLevel; confidence: MetricConfidence; warnings: string[]; }`
        - `ForceSignalsReport { version: string; overallConfidence: MetricConfidence; warnings: string[]; phaseBoundaries: PhaseBoundary[]; axisMetrics: AxisDeviationMetric[]; stabilityMetrics: StabilityMetric[]; contactMetrics: ContactStabilityMetric[]; }`
      * AnalysisResult interface 확장: `forceSignalsReport?: ForceSignalsReport | null;` (Phase 6 bodyComparisonReport 박제 패턴 정합, non-optional 가능하나 backward-compat 위해 optional).
      * 박제 위치: BodyComparisonReport 신설 영역 직후 (Phase 6/7 박제 영역 아래) + 헤더 주석 "// ── Phase 8 Force Signals (Plan 08-01 신설 — FORCE-01 신호 layer) ──────"
    - **backend/shared/python/sunity_shared/models.py** — placeholder 박제:
      * 본 plan 에서는 dataclass 본체 미신설 (Plan 08-02 가 force_signals.py 신설). 본 plan 의 models.py 변경은 주석으로 forward-declare 만:
        ```
        # Phase 8 Force Signals — Plan 08-02 신설 후 import 활성화
        # from .analysis.force_signals import (
        #     MotionPhase,
        #     DeviationDirection,
        #     SeverityLevel,
        #     MetricConfidence,
        #     ContactPoint,
        #     PhaseBoundary,
        #     AxisDeviationMetric,
        #     StabilityMetric,
        #     ContactStabilityMetric,
        #     ForceSignalsReport,
        # )
        ```
      * 위치: 기존 Phase 6 body_normalizer import 영역 직후. 주석 안 type / interface 이름은 lockstep test 가 grep 으로 잡음 (Python 측면 3-way lockstep entry).
    - **docs/contract.md §9 신설** — Phase 6/7 §8 / §8.3 패턴 mirror:
      * Top heading: `## §9. ForceSignalsReport (Plan 08-01 신설 — FORCE-01 신호 layer)`
      * 박제 내용:
        - §9 개요 (1 paragraph): Phase 8 = Phase 9 의 inferForceDirectionPattern 신호 layer + 5단계 motion-phase 분할 + 가림 스무딩 + confidence 가중 처리.
        - §9.1 Enum / Type Alias: 5 type (MotionPhase / DeviationDirection / SeverityLevel / MetricConfidence / ContactPoint) 표.
        - §9.2 PhaseBoundary 표 (field name / type / 의미).
        - §9.3 AxisDeviationMetric 표.
        - §9.4 StabilityMetric 표.
        - §9.5 ContactStabilityMetric 표.
        - §9.6 ForceSignalsReport 표 (top-level umbrella).
        - §9.7 AnalysisResult.forceSignalsReport optional 필드 박제 (Phase 6 bodyComparisonReport 패턴 정합).
        - §9.8 Warning code enum (snake_case 영어): `occlusion_high_in_phase` / `layer2_unavailable` / `layer_disagreement_minor` / `layer_disagreement_major` / `layer2_call_failed` / `motion_unrecognized` / `motion_unrecognized_layer1_only` / `abnormal_release_during_hold` / `partial_motion_video` / `video_too_short` / `heavy_occlusion` / `entry_not_detected` / `all_frames_unreliable`.
        - §9.9 D-결정 요약 (D-08-A1~U3 CONTEXT.md 인용).
      * 박제 위치: §8.3 (Phase 7 분류) 직후, §99 (있다면) 직전. 기존 §8 / §8.3 박제 패턴 mirror — table + 박제 메모 + D-결정 인용.
    - **test_force_signals_lockstep.py 신설** — 3-way drift defense (Phase 7 test_body_comparison_report_phase7_lockstep.py 패턴 정합):
      * 모듈 상단에 `_FIELD_MAP: list[tuple[str, str, str]]` 박제 — (ts_field, py_or_doc_field, label) tuple. 박제 entry:
        - ("MotionPhase", "MotionPhase", "type alias")
        - ("DeviationDirection", "DeviationDirection", "type alias")
        - ("SeverityLevel", "SeverityLevel", "type alias")
        - ("MetricConfidence", "MetricConfidence", "type alias")
        - ("ContactPoint", "ContactPoint", "type alias")
        - ("PhaseBoundary", "PhaseBoundary", "dataclass")
        - ("AxisDeviationMetric", "AxisDeviationMetric", "dataclass")
        - ("StabilityMetric", "StabilityMetric", "dataclass")
        - ("ContactStabilityMetric", "ContactStabilityMetric", "dataclass")
        - ("ForceSignalsReport", "ForceSignalsReport", "dataclass")
        - ("forceSignalsReport", "forceSignalsReport", "AnalysisResult field")
        - ("phaseBoundaries", "phase_boundaries", "camelCase ↔ snake_case")
        - ("axisMetrics", "axis_metrics", "camelCase ↔ snake_case")
        - ("stabilityMetrics", "stability_metrics", "camelCase ↔ snake_case")
        - ("contactMetrics", "contact_metrics", "camelCase ↔ snake_case")
        - ("overallConfidence", "overall_confidence", "camelCase ↔ snake_case")
        - ("startFrameIdx", "start_frame_idx", "camelCase ↔ snake_case")
        - ("endFrameIdx", "end_frame_idx", "camelCase ↔ snake_case")
        - ("startMs", "start_ms", "camelCase ↔ snake_case")
        - ("endMs", "end_ms", "camelCase ↔ snake_case")
        - ("pelvisDistanceFromPoleAxis", "pelvis_distance_from_pole_axis", "camelCase ↔ snake_case")
        - ("chestDistanceFromPoleAxis", "chest_distance_from_pole_axis", "camelCase ↔ snake_case")
        - ("shoulderTilt", "shoulder_tilt", "camelCase ↔ snake_case")
        - ("hipTilt", "hip_tilt", "camelCase ↔ snake_case")
        - ("deviationDirection", "deviation_direction", "camelCase ↔ snake_case")
        - ("jitterScore", "jitter_score", "camelCase ↔ snake_case")
        - ("jerkScore", "jerk_score", "camelCase ↔ snake_case")
        - ("holdStabilityScore", "hold_stability_score", "camelCase ↔ snake_case")
        - ("unstableBodyParts", "unstable_body_parts", "camelCase ↔ snake_case")
        - ("contactPoint", "contact_point", "camelCase ↔ snake_case")
        - ("estimatedStable", "estimated_stable", "camelCase ↔ snake_case")
        - ("lostContactAtMs", "lost_contact_at_ms", "camelCase ↔ snake_case")
        - ("distanceAtLostMs", "distance_at_lost_ms", "camelCase ↔ snake_case")
      * 3 test 박제:
        - `test_ts_interface_has_all_fields` — Path("app/src/types/analysis.ts").read_text() 안 각 ts_field grep (`>= 1`)
        - `test_python_placeholder_has_all_fields` — models.py 안 각 py_field grep (주석 안 박제 OK — Plan 08-02 가 import 활성화)
        - `test_contract_md_has_all_fields` — docs/contract.md 안 각 ts_field 또는 py_field grep (3-way 정합 검증)
  </behavior>
  <action>
    **Phase 6/7 박제 패턴 — 단일 atomic commit 강제**. Task 3 의 4 파일 (analysis.ts + models.py + contract.md + lockstep test) 은 단일 git commit 으로 박제. 부분 commit 시 lockstep test 가 fail-fast — Phase 6 의 116f400 / Phase 7 의 d4d8af4 패턴 정합.

    `app/src/types/analysis.ts` 박제 위치: BodyComparisonReport interface 직후 + AnalysisResult 박제 위치 (§9.7 정합) 에 `forceSignalsReport?: ForceSignalsReport | null;` 추가. 박제 영역 헤더: `// ── Phase 8 Force Signals (Plan 08-01 신설 — FORCE-01 신호 layer) ──────`. 박제 영역 끝: `// ────────────────────────────────────────────────────────────────────`.

    `backend/shared/python/sunity_shared/models.py` 박제 위치: 기존 body_normalizer import 영역 직후. forward-declare 주석 박제 — Plan 08-02 가 활성화. 박제 주석 정확히 본 behavior 박제 (lockstep test grep target).

    `docs/contract.md` 박제 위치: §8.3 (Phase 7 분류) 직후. 헤딩 = `## §9. ForceSignalsReport (Plan 08-01 신설 — FORCE-01 신호 layer)`. table 형식 = §8 / §8.3 그대로 mirror (field / type / 의미 / source D-XX). 박제 메모 끝: "본 schema 의 정확한 dataclass 본체는 Plan 08-02 에서 신설. 본 §9 박제는 3-way contract lockstep 단일 atomic commit (Phase 6/7 패턴 정합)."

    `test_force_signals_lockstep.py` 박제 위치: backend/tests/phase08/. Phase 7 test_body_comparison_report_phase7_lockstep.py 패턴 정확히 mirror — pytest parametrize over _FIELD_MAP + Path.read_text() + assert `count >= 1`. test 모듈 상단에 `from pathlib import Path` + `import pytest` + 박제 메모 "Phase 8 3-way drift defense (Plan 08-01)".

    grep 함정 회피: `grep -c` 사용 시 헤더 주석 자체가 self-counting 문제 — Python `count(substring)` 또는 `grep -v '^#' | grep -c` 사용. Phase 7 test_body_comparison_report_phase7_lockstep.py 박제 패턴 (assert ts_count >= 1) 정합.

    **Critical** — 본 task 의 3-way 4 파일은 단일 `git commit` 박제. partial commit (e.g. analysis.ts 만 박제하고 contract.md 미박제) 시 lockstep test 가 fail-fast.

    per D-08-U3 + RESEARCH §"3-Way Contract Additions" + Phase 6 D-06-C8 (lockstep atomic) + Phase 7 d4d8af4 패턴.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && PYTHONPATH=backend/shared/python:. backend/.venv/bin/python3 -m pytest backend/tests/phase08/test_force_signals_lockstep.py -x && cd app && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File modified: `app/src/types/analysis.ts` contains all 5 type aliases + 5 interfaces + AnalysisResult.forceSignalsReport field
    - File modified: `backend/shared/python/sunity_shared/models.py` contains forward-declare comment block with all 10 names (MotionPhase / DeviationDirection / SeverityLevel / MetricConfidence / ContactPoint / PhaseBoundary / AxisDeviationMetric / StabilityMetric / ContactStabilityMetric / ForceSignalsReport)
    - File modified: `docs/contract.md` contains `## §9. ForceSignalsReport` heading + all 5 dataclass tables + warning code enum + D-결정 인용
    - Source: `grep -c "export interface ForceSignalsReport" app/src/types/analysis.ts` >= 1
    - Source: `grep -c "forceSignalsReport" app/src/types/analysis.ts` >= 2 (interface + AnalysisResult)
    - Source: `grep -c "MotionPhase" backend/shared/python/sunity_shared/models.py` >= 1
    - Source: `grep -c "## §9" docs/contract.md` >= 1
    - Source: `grep -c "ForceSignalsReport" docs/contract.md` >= 5 (heading + tables)
    - Behavior: `cd app && npx tsc --noEmit` exits 0 (TS strict mode pass)
    - Behavior: `pytest backend/tests/phase08/test_force_signals_lockstep.py -x` PASS (all _FIELD_MAP entries grep-detected in all 3 sources)
    - Behavior: pytest exit 0 (위 verify command)
    - Git: 본 4 파일 (analysis.ts + models.py + contract.md + lockstep test) 단일 commit 박제 — `git log -1 --name-only` 시 4 파일 모두 포함 (partial commit detection)
  </acceptance_criteria>
  <done>
    3-way contract lockstep 단일 atomic commit 완료. TS 7 interface + Python placeholder + contract.md §9 박제. 부분 commit 차단. `tsc --noEmit clean` + lockstep test PASS. Plan 08-02 의 force_signals.py dataclass 본체 신설 시 본 schema 정확히 정합 (lockstep test 가 forward-compat 강제).
  </done>
</task>

</tasks>

<verification>
## Phase 8 Plan 01 Verification

**Goal-backward derived truths (FORCE-01 신호 layer schema + drift defense + yaml + Wave 0):**

- TS interface (app/src/types/analysis.ts) 의 7 신설 entry 모두 `tsc --noEmit clean` 통과
- Python re-export placeholder (models.py) 의 forward-declare 주석에 10 name 모두 grep 발견
- docs/contract.md §9 신설 (heading + 5 dataclass 표 + 13 warning code enum + D-08 인용)
- dimensions.stability_wobble() helper 가 dimensions.py 에 박제 + stability_score() refactor 후 phase06 156 test 회귀 0
- contact_points.yaml 5 motion + default + yaml.safe_load 호환 + 모든 expected_contact_points 가 12 ContactPoint enum 정합
- Wave 0 13 파일 신설 (phase08/__init__.py + conftest.py + fixtures/__init__.py + _factory.py + 6 JSON fixture + judging_data/__init__.py + 3 test 파일) — Plan 08-02 / 08-03 단위 test 즉시 실행 가능
- drift defense test: dimensions.stability_wobble(angles) == 직접 산식 (abs < 1e-9) — Plan 08-02 의 force_signals.jitter_score 가 본 helper import 강제 (산식 복제 차단)
- yaml load test: 5 motion 의 모든 expected_contact_points 가 12 ContactPoint enum 안 박제 — yaml.load (unsafe) 금지 + RCE 방지

**Manual verification (belle 검수 박제 대기):**
- contact_points.yaml 도메인 검수 — 인버트 / 후굴 / 클라임의 expected_contact_points 매핑이 폴스포츠 도메인 정합한지 belle/강사 검수 (08-VALIDATION.md §"Manual-Only Verifications" 박제). 본 plan 의 자동 검증은 yaml 구조 + enum 정합만.

**Regression checks:**
- `PYTHONPATH=backend/shared/python:. backend/.venv/bin/python3 -m pytest backend/tests/phase06/ -x` PASS (156 phase06 test, stability_score refactor 회귀 0)
- `PYTHONPATH=backend/shared/python:. backend/.venv/bin/python3 -m pytest backend/tests/phase07/ -x` PASS (108 phase07 test, 회귀 0)
- `cd app && npx tsc --noEmit` exits 0 (TS strict mode pass, AnalysisResult.forceSignalsReport optional 박제)
</verification>

<success_criteria>
- [x] 3-way contract lockstep 단일 atomic commit (TS + Python placeholder + contract.md §9)
- [x] dimensions.stability_wobble() raw helper 분리 + drift defense test PASS
- [x] backend/judging_data/contact_points.yaml 신설 + yaml.safe_load 호환 + 5 motion + default
- [x] Wave 0 test 인프라 13 신설 (phase08/ + 6 fixture + 3 test + __init__)
- [x] phase06 / phase07 회귀 0 (stability_score refactor 검증)
- [x] `tsc --noEmit` clean
- [x] FORCE-01 schema 박제 — Plan 08-02 (force_signals.py 본체) 진입 차단 해소
</success_criteria>

<output>
Create `.planning/phases/08-jerk-jitter/08-01-SUMMARY.md` when done. Include:
- 3 task commit SHA + 각 task scope
- TS / Python / contract.md 3-way lockstep 검증 결과
- drift defense test 결과 (force_signals.jitter_score 와 dimensions.stability_wobble 동일성)
- contact_points.yaml 5 motion + default 박제 (belle 검수 manual gate 박제 표기)
- Wave 0 인프라 완료 — Plan 08-02 진입 시그널
- phase06 / phase07 회귀 0 확인
- Deviation (Rule 1 fix / Rule 3 environment fix 등) 있으면 박제
</output>
