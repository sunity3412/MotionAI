---
phase: 08-jerk-jitter
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - backend/shared/python/sunity_shared/analysis/force_signals.py
  - backend/shared/python/sunity_shared/models.py
  - backend/tests/phase08/test_compute_phase_boundaries.py
  - backend/tests/phase08/test_compute_axis_deviation.py
  - backend/tests/phase08/test_compute_stability_metrics.py
  - backend/tests/phase08/test_compute_contact_stability.py
  - backend/tests/phase08/test_compute_force_signals.py
autonomous: true
requirements: [FORCE-01]
subsystem: backend-analysis-core
tags: [phase-08, force-signals, layer1-heuristic, axis-deviation, stability-metric, contact-stability, drift-defense]

scope_sanity_note: "7 파일 중 5 개는 test 파일. 실제 소스 변경은 force_signals.py 신설 (단일 모듈, ~500-700 lines) + models.py import 활성화 (Plan 08-01 placeholder 주석 해제). Layer 2 Gemini 통합은 본 plan scope X — Plan 08-03 가 wiring. 본 plan = Layer 1 단독 작동 + 4 metric 산출 + drift defense. 단일 plan ~45% context budget — Phase 6/7 plan 본체 task 패턴 동일."

must_haves:
  truths:
    - "backend/shared/python/sunity_shared/analysis/force_signals.py 모듈 신설 — 5 dataclass (PhaseBoundary / AxisDeviationMetric / StabilityMetric / ContactStabilityMetric / ForceSignalsReport) + 5 public 함수 (compute_phase_boundaries / compute_axis_deviation / compute_stability_metrics / compute_contact_stability / compute_force_signals umbrella) + 5 type alias (MotionPhase / DeviationDirection / SeverityLevel / MetricConfidence / ContactPoint) — Plan 08-01 박제 TS schema 정확히 mirror. per D-08-A1 + D-08-A2 + D-08-B1 + D-08-C1~C4 + D-08-D1 + D-08-U1~U3"
    - "5 dataclass 모두 frozen=True + __post_init__ Literal enum validator (severity / confidence / phase / deviation_direction / contact_point / source). field(default_factory=list) 로 warnings 빈 list[] 기본값 (Phase 6/7 박제 패턴 정합)"
    - "compute_phase_boundaries() Layer 1 motion-agnostic 휴리스틱 — foot_y (양 발 y 평균) + hand_pole_dist (양 손 - pole_axis 최소 거리) + keypoint_velocity (17 keypoint 평균 inter-frame 변화율) 3 신호 cutoff 룰로 5단계 boundary (entry/lock/transition/final_shape/hold) 산출. Layer 2 hook 시그너처 박제 (gemini_extractor: GeminiMomentExtractor | None = None) — 단 본 plan 의 본체 = no-op (None 분기 only, Plan 08-03 가 활성화)"
    - "Layer 1 threshold 박제 (module-level 상수): LAYER1_GROUND_Y_RATIO=0.85 / LAYER1_HAND_POLE_DIST_LOCK=0.15 / LAYER1_VELOCITY_MID=0.03 / LAYER1_VELOCITY_HIGH=0.06 / LAYER1_MIN_PHASE_FRAMES=2 — RESEARCH §5-Phase Motion Split Layer 1 정합. [ASSUMED] belle Pod sweep sanity check 박제 manual gate (08-VALIDATION.md)"
    - "compute_axis_deviation() — _pelvis_distance + _chest_distance (mid-point pole-axis 정사영 거리 / torso_scale) + _shoulder_tilt + _hip_tilt (signed degrees) + _deviation_direction enum + severity. body-scale 정규화 (D-08-D1)"
    - "compute_stability_metrics() — jitter_score = dimensions.stability_wobble (Plan 08-01 박제 helper import 강제, drift defense source) + jerk_score = _compute_jerk (np.abs(np.diff(angles, n=3, axis=0)) + MAD outlier rejection + nanmedian) + hold_stability_score = dimensions.stability_wobble on hold phase window (또는 None if phase != hold) + unstable_body_parts = dimensions.stability_wobble_by_joint reuse + threshold 12.0 deg filter. per D-08-C1 + D-08-C2"
    - "compute_contact_stability() — Plan 08-01 박제 contact_points.yaml load (_load_expected_contact_points + cache) + proximity threshold 0.08 × torso_scale + hold debounce 2 frames + lost_contact_debounce 2 frames + _detect_abnormal_release (lock.start_ms <= lost_contact_at_ms <= hold.end_ms → warning 'abnormal_release_during_hold') + motion_id 미인식 fallback (expected_contact_points=[] + estimated_stable=null + confidence='low'). per D-08-B1~B5"
    - "compute_force_signals() umbrella 헬퍼 — Step 1 temporal.temporal_fill (D-08-U1 가림 보간) → Step 2 compute_phase_boundaries → Step 3 3 metric 함수 호출 → Step 4 _overall_confidence → Step 5 ForceSignalsReport 조립. pipeline/app.py 가 본 함수 1줄로 호출 (Plan 08-03 wiring)"
    - "drift defense test_compute_stability_metrics::test_drift_defense — force_signals.compute_stability_metrics 의 jitter_score 가 dimensions.stability_wobble (Plan 08-01 박제) 직접 호출과 abs < 1e-9 일치 검증. Phase 12.5 v4 Codex HIGH-2 패턴 정합"
    - "Layer 2 hook 시그너처 박제 — compute_phase_boundaries(gemini_extractor, video_uri 키워드 인자) — 본 plan 의 본체는 gemini_extractor=None 분기만 활성 (Layer 1 단독 + confidence='medium'). Plan 08-03 가 Layer 2 활성화 + agreement 룰. Test = test_layer2_hook_not_invoked_when_gemini_extractor_none"
    - "models.py 의 Plan 08-01 placeholder 주석을 활성 import 로 전환 — from .analysis.force_signals import (10 names). 단일 atomic commit (Plan 08-01 의 3-way lockstep 활성화 완성)"
    - "6 fixture (Plan 08-01 신설) 모두 unit test 에서 활용 — fixture_clean_invert (severity all low) / fixture_pelvis_drop (axis severity high outward) / fixture_occluded_lock (confidence low warning) / fixture_motion_id_unrecognized (fallback) / fixture_jerk_high (jerk severity high) / fixture_layback_release (abnormal release warning)"
    - "5 단위 test 파일 모두 PASS — test_compute_phase_boundaries (Layer 1 only + edge cases) + test_compute_axis_deviation (4 metric + direction + severity) + test_compute_stability_metrics (jitter + jerk + hold + unstable + drift defense) + test_compute_contact_stability (yaml load + proximity + debounce + abnormal release + fallback) + test_compute_force_signals (umbrella E2E + temporal_fill + confidence)"
  artifacts:
    - path: backend/shared/python/sunity_shared/analysis/force_signals.py
      provides: "Phase 8 본체 모듈. 5 dataclass + 5 public function + 5 type alias + 15+ private helper (_layer1_heuristic_boundaries / _phase_signals / _point_to_axis_distance / _pelvis_distance / _chest_distance / _shoulder_tilt / _hip_tilt / _deviation_direction / _compute_jerk / _compute_unstable_body_parts / _load_expected_contact_points / _detect_contact_stability / _detect_abnormal_release / _weighted_aggregate / _overall_confidence / _should_invoke_layer2 / _longest_consecutive_true / _find_first_loss). pure-function (numpy + 기존 helper import + yaml.safe_load). 외부 의존 0 (Layer 2 = 어댑터 경계, Plan 08-03 wiring)."
      contains:
        - "from __future__ import annotations"
        - "from . import temporal, dimensions"
        - "from .body_normalization import BodyNormalizationProfile"
        - "from .pose_frame import PoseFrame, PoleAxis"
        - "MotionPhase = Literal"
        - "DeviationDirection = Literal"
        - "SeverityLevel = Literal"
        - "MetricConfidence = Literal"
        - "ContactPoint = Literal"
        - "@dataclass(frozen=True)"
        - "class PhaseBoundary"
        - "class AxisDeviationMetric"
        - "class StabilityMetric"
        - "class ContactStabilityMetric"
        - "class ForceSignalsReport"
        - "def compute_phase_boundaries("
        - "def compute_axis_deviation("
        - "def compute_stability_metrics("
        - "def compute_contact_stability("
        - "def compute_force_signals("
        - "stability_wobble"
        - "CONTACT_PROXIMITY_THRESHOLD"
        - "JERK_MAD_K"
        - "UNSTABLE_BODY_PART_THRESHOLD_DEG"
    - path: backend/shared/python/sunity_shared/models.py
      provides: "Plan 08-01 placeholder 주석을 활성 import 로 전환 — 10 name import 활성화. 단일 atomic commit (Plan 08-01 의 3-way lockstep 완성)"
      contains:
        - "from .analysis.force_signals import"
        - "MotionPhase"
        - "DeviationDirection"
        - "SeverityLevel"
        - "MetricConfidence"
        - "ContactPoint"
        - "PhaseBoundary"
        - "AxisDeviationMetric"
        - "StabilityMetric"
        - "ContactStabilityMetric"
        - "ForceSignalsReport"
    - path: backend/tests/phase08/test_compute_phase_boundaries.py
      provides: "Layer 1 휴리스틱 단위 test — 3 신호 cutoff 룰 + 5단계 boundary 산출 + edge case (영상 hold 만 / 짧음 / heavy occlusion). Layer 2 hook 비호출 검증 (gemini_extractor=None 분기 only)"
      contains:
        - "def test_layer1_only_with_clean_invert_fixture"
        - "def test_layer1_only_emits_5_phases"
        - "def test_video_too_short_falls_back_to_single_hold"
        - "def test_heavy_occlusion_returns_low_confidence"
        - "def test_layer2_hook_not_invoked_when_gemini_extractor_none"
    - path: backend/tests/phase08/test_compute_axis_deviation.py
      provides: "AxisDeviationMetric 단위 test — pelvis/chest distance + shoulder/hip tilt + deviation_direction enum + severity 임계 검증. body-scale 정규화 (torso_scale=1.0 fixture) 활용"
      contains:
        - "def test_pelvis_distance_body_scale_normalized"
        - "def test_chest_distance_body_scale_normalized"
        - "def test_shoulder_tilt_signed_degrees"
        - "def test_hip_tilt_signed_degrees"
        - "def test_deviation_direction_outward"
        - "def test_severity_thresholds"
        - "def test_pelvis_drop_fixture_emits_high_severity_outward"
    - path: backend/tests/phase08/test_compute_stability_metrics.py
      provides: "StabilityMetric 단위 test — jitter (dimensions reuse) + jerk (MAD outlier) + hold (dimensions reuse) + unstable_body_parts. drift defense test — force_signals.jitter_score == dimensions.stability_wobble (abs < 1e-9). per D-08-C1 + D-08-C2 + Phase 12.5 v4 Codex HIGH-2"
      contains:
        - "def test_drift_defense"
        - "def test_jitter_score_uses_dimensions_helper"
        - "def test_jerk_score_mad_filtered"
        - "def test_hold_stability_score_on_hold_phase_only"
        - "def test_hold_stability_score_returns_none_when_phase_not_hold"
        - "def test_unstable_body_parts_threshold_filter"
        - "def test_jerk_high_fixture_emits_high_severity"
    - path: backend/tests/phase08/test_compute_contact_stability.py
      provides: "ContactStabilityMetric 단위 test — yaml load + proximity threshold + debounce + lost_contact_at_ms detection + abnormal release warning + motion_id 미인식 fallback. per D-08-B1~B5"
      contains:
        - "def test_yaml_load_motion_id_lookup"
        - "def test_proximity_threshold_body_scale"
        - "def test_estimated_stable_debounce"
        - "def test_lost_contact_at_ms_detection"
        - "def test_abnormal_release_during_hold_warning"
        - "def test_motion_unrecognized_fallback_estimated_stable_null"
        - "def test_layback_release_fixture_emits_abnormal_warning"
    - path: backend/tests/phase08/test_compute_force_signals.py
      provides: "umbrella 통합 test — compute_force_signals() E2E. Step 1 temporal_fill 적용 + Step 2~4 metric 산출 + Step 5 overall_confidence + warnings emission. fixture_clean_invert E2E + fixture_occluded_lock confidence weighting + temporal_fill 적용 검증"
      contains:
        - "def test_compute_force_signals_e2e_clean_invert"
        - "def test_temporal_fill_applied_before_metrics"
        - "def test_confidence_weighting_emits_warning_on_high_low_reliability"
        - "def test_overall_confidence_min_propagation"
        - "def test_motion_unrecognized_emits_warning"
        - "def test_layer2_unavailable_emits_warning"
  key_links:
    - from: backend/shared/python/sunity_shared/analysis/force_signals.py
      to: backend/shared/python/sunity_shared/analysis/dimensions.py
      via: "from .dimensions import stability_wobble — Plan 08-01 박제 helper import. force_signals.compute_stability_metrics 의 jitter_score 산출 source. drift defense (산식 복제 차단)"
      pattern: "stability_wobble"
    - from: backend/shared/python/sunity_shared/analysis/force_signals.py
      to: backend/shared/python/sunity_shared/analysis/temporal.py
      via: "from . import temporal — temporal_fill 호출 (D-08-U1 가림 보간). compute_force_signals umbrella Step 1"
      pattern: "temporal_fill"
    - from: backend/shared/python/sunity_shared/analysis/force_signals.py
      to: backend/judging_data/contact_points.yaml
      via: "_load_expected_contact_points: yaml.safe_load + motion_id lookup (Plan 08-01 박제 yaml). default fallback (expected_contact_points=[])"
      pattern: "expected_contact_points"
    - from: backend/shared/python/sunity_shared/models.py
      to: backend/shared/python/sunity_shared/analysis/force_signals.py
      via: "Python re-export — 10 name (5 type + 5 dataclass) import 활성화. Plan 08-01 placeholder 의 주석 해제"
      pattern: "from .analysis.force_signals import"
    - from: backend/tests/phase08/test_compute_stability_metrics.py
      to: backend/shared/python/sunity_shared/analysis/dimensions.py
      via: "drift defense — force_signals.jitter_score 와 dimensions.stability_wobble 직접 호출이 abs < 1e-9 일치. 산식 복제 차단 (Plan 08-01 박제 drift defense 패턴 확장)"
      pattern: "stability_wobble"
---

<objective>
Phase 8 vertical slice (2/3) — **force_signals.py 본체 + 5 public 함수 + 단위 test**. Plan 08-01 박제 schema 위에 실제 산출 logic 박제:

(a) **5 dataclass + 5 type alias** — Plan 08-01 의 TS interface / contract.md §9 정확히 mirror. frozen=True + __post_init__ Literal enum validator (Phase 6/7 박제 패턴 정합).
(b) **compute_phase_boundaries() Layer 1 휴리스틱** — foot_y / hand_pole_dist / keypoint_velocity 3 신호 cutoff 룰 + 5단계 boundary (entry/lock/transition/final_shape/hold). Layer 2 hook 시그너처 박제 (Plan 08-03 가 활성화).
(c) **compute_axis_deviation()** — pelvis/chest pole-axis 거리 (body-scale) + shoulder/hip tilt (signed degrees) + deviation_direction enum + severity 임계.
(d) **compute_stability_metrics()** — dimensions.stability_wobble import (Plan 08-01 박제 helper, drift defense source) + jerk_score (np.diff n=3 + MAD outlier rejection) + hold_stability_score + unstable_body_parts (dimensions.stability_wobble_by_joint reuse + threshold filter).
(e) **compute_contact_stability()** — Plan 08-01 박제 contact_points.yaml load + proximity threshold + debounce + lost_contact_at_ms + abnormal release detection + motion_id 미인식 fallback.
(f) **compute_force_signals() umbrella** — temporal.temporal_fill (D-08-U1 가림 보간) → 4 metric 함수 호출 → overall_confidence → ForceSignalsReport. pipeline/app.py 가 1줄로 호출 (Plan 08-03 wiring).
(g) **models.py 활성화** — Plan 08-01 placeholder 주석 해제 (3-way lockstep 완성).
(h) **5 단위 test 파일** — drift defense + Layer 1 단독 + 4 metric 산출 + 6 fixture 전부 활용.

Purpose: FORCE-01 success criteria #1 + #2 + #3 + #4 의 backend 산출 logic + 단위 test.
Output: force_signals.py 신설 (~500-700 lines) + models.py 1 import 활성화 + 5 단위 test 파일.

**박제 메모 정합:** [[scoring-dimensions-ipsf]] (severity 임계 fixed) + [[analysis-objectivity-no-human-scores]] (canned warning code 영어 snake_case) + [[mvp-simple-pilot-quality]] (motion-agnostic Layer 1) + [[feedback-analysis-first]] (5단계 분할 정확도 + 가림 confidence 가중) + [[firestore-nested-array-flat]] (dataclass list 안 dict-of-scalars-only + list[str] schema) + [[no-baekje-filler]] (코드/test 안 "박제" 단어 X — canned warning code 영어).

per D-08-A1 (5단계 enum) + D-08-A2 (Layer 1 단독, Layer 2 hook 박제만) + D-08-B1~B5 (yaml 매핑 + proximity + abnormal release + fallback) + D-08-C1~C4 (dimensions reuse + jerk 신설 + 새 모듈) + D-08-D1 (body-scale 정규화) + D-08-U1 (가림 스무딩) + D-08-U2 (confidence 필드).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-jerk-jitter/08-CONTEXT.md
@.planning/phases/08-jerk-jitter/08-RESEARCH.md
@.planning/phases/08-jerk-jitter/08-VALIDATION.md
@.planning/phases/08-jerk-jitter/08-01-PLAN.md
@CLAUDE.md
@backend/shared/python/sunity_shared/models.py
@backend/shared/python/sunity_shared/analysis/dimensions.py
@backend/shared/python/sunity_shared/analysis/temporal.py
@backend/shared/python/sunity_shared/analysis/features.py
@backend/shared/python/sunity_shared/analysis/pose_frame.py
@backend/shared/python/sunity_shared/analysis/body_normalization.py
@backend/shared/python/sunity_shared/analysis/skeleton.py
@backend/shared/python/sunity_shared/analysis/body_normalizer.py
@backend/shared/python/sunity_shared/judging/gemini_moment_extractor.py
@backend/judging_data/contact_points.yaml
@backend/tests/phase08/conftest.py
@backend/tests/phase08/fixtures/_factory.py
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Phase 8 = backend 분석 코어 layer. 본 plan = pure-function 모듈 + 단위 test. 사용자 입력 영역 0. Layer 2 Gemini 어댑터 경계는 Plan 08-03 wiring. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | T (Tampering / drift between dimensions vs force_signals) | force_signals.compute_stability_metrics 의 jitter_score | mitigate | dimensions.stability_wobble import 강제 (산식 복제 차단). drift defense test (test_drift_defense) — abs < 1e-9 일치 검증. Plan 08-01 helper 분리 + 본 plan import 강제 = Phase 12.5 v4 Codex HIGH-2 패턴 완성. |
| T-08-02-02 | T (Tampering / Layer 1 heuristic threshold drift) | _layer1_heuristic_boundaries threshold | mitigate | module-level 상수 박제 (단일 source). RESEARCH §5-Phase Motion Split Layer 1 정합. belle Pod sweep sanity check 박제 manual gate (08-VALIDATION.md). |
| T-08-02-03 | I (Information disclosure / warning string 안 PII) | ForceSignalsReport.warnings + metric.warnings | mitigate | canned snake_case 영어 warning code 만 (Plan 08-01 §9.8 박제 13 enum). 한국어 카피 X (Phase 11 책임). 사용자 입력 string concat X. |
| T-08-02-04 | T (Tampering / yaml RCE) | _load_expected_contact_points | mitigate | yaml.safe_load 강제 (yaml.load 금지). Plan 08-01 박제 yaml load test 가 검증. CLAUDE.md §3 + RESEARCH §Security V12 정합. |
| T-08-02-05 | D (DoS / 무한 loop in Layer 1 heuristic edge case) | _layer1_heuristic_boundaries | mitigate | Edge case 박제 — T < 10 frames → 단일 hold phase + warning 'video_too_short' / all-NaN → 단일 hold + warning 'heavy_occlusion'. test 검증. 무한 loop 가능성 0 — for 루프 + numpy vectorized. |
| T-08-02-06 | D (DoS / Firestore nested-array crash) | ForceSignalsReport list 안 dict 의 list[str] warnings | accept | Plan 08-03 가 _validate_dict_only_scalars 명세 확장 (list[str] 허용) — 본 plan 의 dataclass 는 schema 박제만. Firestore 저장 검증은 Plan 08-03 scope. |
| T-08-02-SC | Tampering | npm/pip/cargo installs | mitigate | 신규 의존성 0 — 기존 환경 그대로 (numpy + PyYAML 기존 박제). RESEARCH §Package Legitimacy Audit "N/A". slopcheck 불요. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: force_signals.py 신설 — 5 dataclass + 5 type alias + 5 public 함수 시그너처 (시그너처 박제 + 본체 stub) + models.py import 활성화</name>
  <files>
    backend/shared/python/sunity_shared/analysis/force_signals.py
    backend/shared/python/sunity_shared/models.py
  </files>
  <read_first>
    backend/shared/python/sunity_shared/analysis/body_normalizer.py
    backend/shared/python/sunity_shared/analysis/dimensions.py
    backend/shared/python/sunity_shared/analysis/pose_frame.py
    backend/shared/python/sunity_shared/analysis/body_normalization.py
    backend/shared/python/sunity_shared/analysis/technique.py
    backend/shared/python/sunity_shared/models.py
    app/src/types/analysis.ts
    .planning/phases/08-jerk-jitter/08-RESEARCH.md
    .planning/phases/08-jerk-jitter/08-CONTEXT.md
  </read_first>
  <behavior>
    - force_signals.py 모듈 신설 (RESEARCH §"Pattern 1" + §"Public API signatures" 정확히 mirror)
    - 모듈 docstring 박제: Phase 8 본체 + D-08-C3 분리 근거 + dimensions.stability_wobble 재사용 정합 + Phase 12.5 v4 Codex HIGH-2 drift defense 박제
    - Imports: `from __future__ import annotations`, `from dataclasses import dataclass, field`, `from typing import Literal, TYPE_CHECKING`, `import numpy as np`, `from . import temporal, dimensions, features, skeleton`, `from .body_normalization import BodyNormalizationProfile`, `from .pose_frame import PoseFrame, PoleAxis, Keypoint3DAligned`, TYPE_CHECKING 아래 `from ..judging.gemini_moment_extractor import GeminiMomentExtractor`
    - 5 Type alias 박제 — Plan 08-01 TS schema 정확히 mirror (string literal 동일):
      * MotionPhase = Literal["entry", "lock", "transition", "final_shape", "hold"]
      * DeviationDirection = Literal["up", "down", "left", "right", "outward", "inward", "unknown"]
      * SeverityLevel = Literal["low", "medium", "high"]
      * MetricConfidence = Literal["low", "medium", "high"]
      * ContactPoint = Literal["left_hand", "right_hand", "left_inner_thigh", "right_inner_thigh", "left_knee", "right_knee", "left_foot", "right_foot", "left_ankle", "right_ankle", "hip", "unknown"]
    - 5 frozen dataclass 박제 (Phase 6/7 BodyComparisonReport 박제 패턴 mirror):
      * PhaseBoundary 필드: phase / start_frame_idx / end_frame_idx / start_ms / end_ms / confidence / source (Literal["heuristic", "gemini_assisted", "heuristic_fallback"]). __post_init__: phase + confidence + source enum 검증 (ValueError raise on invalid).
      * AxisDeviationMetric 필드: phase / pelvis_distance_from_pole_axis / chest_distance_from_pole_axis / shoulder_tilt / hip_tilt / deviation_direction / severity / confidence / warnings (list[str], field(default_factory=list)). __post_init__: phase + deviation_direction + severity + confidence enum 검증.
      * StabilityMetric 필드: phase / jitter_score / jerk_score / hold_stability_score (float | None) / unstable_body_parts (list[str], field(default_factory=list)) / severity / confidence / warnings (list[str], field(default_factory=list)). __post_init__: phase + severity + confidence enum 검증.
      * ContactStabilityMetric 필드: phase / contact_point / estimated_stable (bool | None) / lost_contact_at_ms (int | None) / distance_at_lost_ms (float | None) / severity / confidence / warnings (list[str], field(default_factory=list)). __post_init__: phase + contact_point + severity + confidence enum 검증.
      * ForceSignalsReport 필드: version (str, default="1.0") / overall_confidence / warnings (list[str], field(default_factory=list)) / phase_boundaries (list[PhaseBoundary]) / axis_metrics (list[AxisDeviationMetric]) / stability_metrics (list[StabilityMetric]) / contact_metrics (list[ContactStabilityMetric]). __post_init__: overall_confidence enum 검증.
    - 5 public 함수 시그너처 박제 (본체 stub `raise NotImplementedError("Task 2/3 가 구현")` — Task 2 가 본체 박제, Task 1 은 시그너처만):
      * compute_phase_boundaries(pose_frames, pole_axis, body_profile, angles, *, motion_id=None, gemini_extractor=None, video_uri=None, fps=9.0) -> list[PhaseBoundary]
      * compute_axis_deviation(pose_frames, phase_boundaries, pole_axis, body_profile) -> list[AxisDeviationMetric]
      * compute_stability_metrics(angles, phase_boundaries, pose_frames) -> list[StabilityMetric]
      * compute_contact_stability(pose_frames, phase_boundaries, pole_axis, body_profile, motion_id) -> list[ContactStabilityMetric]
      * compute_force_signals(pose_frames, pole_axis, body_profile, *, angles=None, motion_id=None, gemini_extractor=None, video_uri=None, fps=9.0) -> ForceSignalsReport
    - Module-level 상수 박제 (RESEARCH §"Layer 1 Heuristic" + §"AxisDeviation severity" + §"StabilityMetric severity" + §"ContactStability proximity"):
      * LAYER1_HAND_POLE_DIST_LOCK = 0.15
      * LAYER1_VELOCITY_MID = 0.03
      * LAYER1_VELOCITY_HIGH = 0.06
      * LAYER1_GROUND_Y_RATIO = 0.85
      * LAYER1_MIN_PHASE_FRAMES = 2
      * AXIS_PELVIS_DISTANCE_THRESHOLDS = (0.15, 0.30)
      * AXIS_CHEST_DISTANCE_THRESHOLDS = (0.20, 0.40)
      * AXIS_TILT_THRESHOLDS_DEG = (10.0, 25.0)
      * JITTER_SEVERITY_THRESHOLDS = (8.0, 20.0)
      * JERK_SEVERITY_THRESHOLDS = (5.0, 15.0)
      * JERK_MAD_K = 3.0
      * JERK_SMOOTH_WINDOW = 3
      * UNSTABLE_BODY_PART_THRESHOLD_DEG = 12.0
      * CONTACT_PROXIMITY_THRESHOLD = 0.08
      * CONTACT_HOLD_MIN_FRAMES = 2
      * LOST_CONTACT_DEBOUNCE_FRAMES = 2
      * CONTACT_DISTANCE_SEVERITY_THRESHOLDS = (0.10, 0.20)
      * RELIABILITY_WEIGHT = {"high": 1.0, "medium": 0.7, "low": 0.3}
      * LOW_RELIABILITY_PHASE_THRESHOLD = 0.4
      * LAYER_AGREEMENT_TOLERANCE_FRAMES = 2
      * LAYER_DISAGREEMENT_MAJOR_FRAMES = 5
      * _CONTACT_POINT_TO_KEYPOINTS dict (RESEARCH §"Proximity threshold + debounce" 표 정확히 mirror)
      * _CONTACT_POINTS_PATH = Path(__file__).parent.parent.parent.parent.parent / "judging_data" / "contact_points.yaml"
      * _CONTACT_POINTS_CACHE: dict | None = None
    - models.py 활성화 — Plan 08-01 placeholder 주석 (10 name forward-declare) 을 활성 `from .analysis.force_signals import (10 names)` 으로 전환. 주석 prefix `# ` 제거.
  </behavior>
  <action>
    backend/shared/python/sunity_shared/analysis/force_signals.py 신설. 모듈 상단 docstring 박제. `from __future__ import annotations` 첫 줄. 5 Type alias 박제 (Plan 08-01 TS schema 정확히 mirror — string literal 동일). 5 frozen dataclass 박제 (Phase 6/7 BodyComparisonReport 패턴 정확히 mirror — `@dataclass(frozen=True)` decorator + `field(default_factory=list)` + `__post_init__` validator). __post_init__ validator 는 Phase 6 의 BodyComparisonFinding.__post_init__ 패턴 정합 — invalid enum value 시 `raise ValueError(f"...invalid... value={value!r}. Valid = {VALID_VALUES}.")`.

    5 public 함수 시그너처 박제 — RESEARCH §"Public API signatures" 의 시그너처 정확히 mirror. 본체 = `raise NotImplementedError("compute_<X>: Task 2 가 구현")`. Task 2 가 본체 박제 (본 task 가 stub 만 박제 이유 = test 가 collection 시 import 만 통과하면 됨, 본체 implementation 단계 분리).

    Module-level 상수 박제 — 위 behavior 의 20+ 상수 모두 박제. RESEARCH 의 각 §"thresholds" 표 정합.

    _CONTACT_POINT_TO_KEYPOINTS dict 정확히 박제 (RESEARCH §"Proximity threshold + debounce" 표):
      _CONTACT_POINT_TO_KEYPOINTS = {
          "left_hand": ("left_wrist",),
          "right_hand": ("right_wrist",),
          "left_inner_thigh": ("left_hip", "left_knee"),
          "right_inner_thigh": ("right_hip", "right_knee"),
          "left_knee": ("left_knee",),
          "right_knee": ("right_knee",),
          "left_foot": ("left_ankle",),
          "right_foot": ("right_ankle",),
          "left_ankle": ("left_ankle",),
          "right_ankle": ("right_ankle",),
          "hip": ("left_hip", "right_hip"),
          "unknown": (),
      }

    _CONTACT_POINTS_PATH 박제 — force_signals.py 위치에서 backend/judging_data/contact_points.yaml 까지의 상대 경로 (parent.parent.parent.parent.parent / "judging_data" / "contact_points.yaml"). RESEARCH §"Loader" 패턴 정확히 정합.

    models.py 의 Plan 08-01 placeholder 주석 라인 (`# from .analysis.force_signals import (` 부터 닫는 `# )` 까지) 의 주석 prefix `# ` 제거 — 활성 import 로 전환. 단일 atomic commit (Plan 08-01 의 3-way lockstep 활성화 완성).

    Critical — 본 commit 후 Plan 08-01 의 lockstep test 가 import 활성화 후도 PASS 유지 (test 가 placeholder 주석/활성 import 양쪽 모두 grep 통과 설계).

    per RESEARCH §"Module Structure" + §"Pattern 1" + D-08-C3 + D-08-U3.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && PYTHONPATH=backend/shared/python:. backend/.venv/bin/python3 -c "from sunity_shared.analysis import force_signals; assert hasattr(force_signals, 'compute_force_signals') and hasattr(force_signals, 'compute_phase_boundaries') and hasattr(force_signals, 'compute_axis_deviation') and hasattr(force_signals, 'compute_stability_metrics') and hasattr(force_signals, 'compute_contact_stability'); from sunity_shared.models import MotionPhase, ForceSignalsReport, PhaseBoundary, AxisDeviationMetric, StabilityMetric, ContactStabilityMetric; print('IMPORT_OK')" && PYTHONPATH=backend/shared/python:. backend/.venv/bin/python3 -m pytest backend/tests/phase08/test_force_signals_lockstep.py -x</automated>
  </verify>
  <acceptance_criteria>
    - File exists: backend/shared/python/sunity_shared/analysis/force_signals.py
    - File modified: backend/shared/python/sunity_shared/models.py contains active `from .analysis.force_signals import` (not comment)
    - Source: `grep -c "@dataclass(frozen=True)" backend/shared/python/sunity_shared/analysis/force_signals.py` >= 5
    - Source: `grep -c "MotionPhase = Literal" backend/shared/python/sunity_shared/analysis/force_signals.py` >= 1
    - Source: `grep -c "ContactPoint = Literal" backend/shared/python/sunity_shared/analysis/force_signals.py` >= 1
    - Source: `grep -c "def compute_" backend/shared/python/sunity_shared/analysis/force_signals.py` >= 5
    - Source: `grep -v '^#' backend/shared/python/sunity_shared/models.py | grep -c "from .analysis.force_signals import"` >= 1 (active import, not comment)
    - Behavior: `python3 -c "from sunity_shared.analysis import force_signals; ..."` exits 0 with IMPORT_OK printed
    - Behavior: Plan 08-01 lockstep test still PASS after models.py activation
    - Behavior: pytest exit 0 (verify command)
  </acceptance_criteria>
  <done>
    force_signals.py 모듈 신설 (시그너처 + dataclass + 상수 only, 본체 stub). models.py import 활성화. Plan 08-01 의 3-way lockstep 활성화 완성. Task 2 가 5 public 함수 본체 박제 후 단위 test 가 즉시 PASS.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 4 metric 함수 + Layer 1 휴리스틱 본체 + 4 단위 test</name>
  <files>
    backend/shared/python/sunity_shared/analysis/force_signals.py
    backend/tests/phase08/test_compute_phase_boundaries.py
    backend/tests/phase08/test_compute_axis_deviation.py
    backend/tests/phase08/test_compute_stability_metrics.py
    backend/tests/phase08/test_compute_contact_stability.py
  </files>
  <read_first>
    backend/shared/python/sunity_shared/analysis/force_signals.py (Task 1 박제 시그너처 + dataclass + 상수 확인)
    backend/shared/python/sunity_shared/analysis/dimensions.py (stability_wobble Plan 08-01 박제 helper + stability_wobble_by_joint + _select_window)
    backend/shared/python/sunity_shared/analysis/temporal.py (temporal_fill)
    backend/shared/python/sunity_shared/analysis/features.py (compute_joint_angles + joint_uncertainty)
    backend/shared/python/sunity_shared/analysis/pose_frame.py (PoseFrame + PoleAxis + Keypoint3DAligned 시그너처)
    backend/shared/python/sunity_shared/analysis/skeleton.py (KEYPOINT_NAMES + JOINT_KEYS)
    backend/judging_data/contact_points.yaml (Plan 08-01 박제 5 motion + default)
    backend/tests/phase08/conftest.py (synthetic_body_profile + synthetic fixtures)
    backend/tests/phase08/fixtures/_factory.py (load_pose_frames_from_fixture)
    .planning/phases/08-jerk-jitter/08-RESEARCH.md §"5-Phase Motion Split — Layer 1 Heuristic" + §"AxisDeviationMetric Algorithm" + §"StabilityMetric Algorithm" + §"ContactStabilityMetric Algorithm"
  </read_first>
  <behavior>
    - **compute_phase_boundaries() Layer 1 본체** (Layer 2 hook = no-op when gemini_extractor=None):
      * 내부 helper `_phase_signals(pose_frames, pole_axis, body_profile) -> dict[str, np.ndarray]` — 3 신호 (foot_y / hand_pole_dist / keypoint_velocity) 산출. RESEARCH §"Signal sources" code 정확히 정합.
      * 내부 helper `_layer1_heuristic_boundaries(pose_frames, pole_axis, body_profile, angles) -> list[PhaseBoundary]` — RESEARCH §"Phase detection rules" 표 정확히 정합. ground_y = nanmax(foot_y). entry/lock/transition/final_shape/hold cutoff 룰 박제. min_phase_frames=2 미만 phase 는 인접 phase 흡수. hold phase = dimensions._select_window(angles) 호출 (Phase 12.5 박제 helper 재사용).
      * 내부 helper `_should_invoke_layer2(motion_id, gemini_extractor, video_uri) -> bool` — RESEARCH §"Layer 2 호출 조건" 정확히 정합. 본 plan 에선 항상 False (gemini_extractor=None 분기 only).
      * 본체: Layer 1 호출 → start_ms/end_ms 박제 (pose_frames[idx].timestamp_ms 로 resolve) → confidence='medium' if motion_id else 'low' + source='heuristic'/heuristic_fallback'. Layer 2 hook 비활성 (raise X, 시그너처만 박제).
      * Edge cases: T < 10 → 단일 'hold' phase + warning 'video_too_short' / all-NaN angles → 단일 'hold' + warning 'heavy_occlusion'.
    - **compute_axis_deviation() 본체**:
      * 내부 helper `_point_to_axis_distance(point, pole_axis) -> float` — 3D 정사영 거리. RESEARCH §"Distance computation" code 정확히 정합.
      * 내부 helper `_pelvis_distance(frame, pole_axis, body_profile) -> float | None` — left_hip + right_hip midpoint 거리 / torso_scale.
      * 내부 helper `_chest_distance(...)` — left_shoulder + right_shoulder midpoint 거리 / torso_scale.
      * 내부 helper `_shoulder_tilt(frame, pole_axis) -> float | None` — RESEARCH §"Tilt computation" code 정확히 정합. signed degrees [-90, +90].
      * 내부 helper `_hip_tilt(frame, pole_axis) -> float | None` — same formula with left_hip / right_hip.
      * 내부 helper `_deviation_direction(...) -> DeviationDirection` — RESEARCH §"deviationDirection enum mapping rules" 정확히 정합. priority order: distance_delta (outward/inward) > y_delta (up/down) > x_delta (left/right).
      * 내부 helper `_severity_from_distance(distance, thresholds) -> SeverityLevel` — module-level thresholds tuple (low, medium boundary) lookup.
      * 본체: 각 PhaseBoundary 별로 phase window 안 _weighted_aggregate (RESEARCH §"Frame-level reliability weighting" 정확히 정합) median 으로 pelvis/chest/tilt 산출 + max(severity_pelvis, severity_chest, severity_tilt) 가 metric.severity + confidence = _weighted_aggregate.confidence (RESEARCH 정합). warnings = aggregate.warnings.
    - **compute_stability_metrics() 본체**:
      * 내부 helper `_compute_jerk(angles_window) -> float` — RESEARCH §"jerkScore" code 정확히 정합. np.abs(np.diff(angles, n=3, axis=0)) + MAD outlier rejection (k=3) + nanmedian. T < 5 → 0.0.
      * 내부 helper `_compute_unstable_body_parts(angles_window, profile=None) -> list[str]` — dimensions.stability_wobble_by_joint(angles_window, profile=None) 호출 + UNSTABLE_BODY_PART_THRESHOLD_DEG (12.0) 초과 joint key 만 sorted list 반환.
      * 본체: 각 PhaseBoundary 별로 phase window 안 angles slice (angles[start_frame_idx:end_frame_idx]).
        - jitter_score = `dimensions.stability_wobble(sliced, profile=None)` (Plan 08-01 박제 helper import 강제 — drift defense source)
        - jerk_score = `_compute_jerk(sliced)`
        - hold_stability_score = `dimensions.stability_wobble(sliced, profile=None)` if phase.phase == 'hold' else None
        - unstable_body_parts = `_compute_unstable_body_parts(sliced)`
        - severity = max(jitter_severity, jerk_severity, unstable_severity) (module-level thresholds)
        - confidence = reliability weighted (phase 안 frame reliability 평균)
        - warnings = canned (occlusion_high_in_phase if low ratio > 0.4 등)
    - **compute_contact_stability() 본체**:
      * 내부 helper `_load_expected_contact_points(motion_id) -> list[ContactPoint]` — RESEARCH §"Loader" code 정확히 정합. yaml.safe_load + cache + motion_id None → [] + motion_id not in mapping → [].
      * 내부 helper `_detect_contact_stability(contact_point, pose_frames, phase_window, pole_axis, body_profile) -> tuple[bool|None, int|None, float|None, MetricConfidence]` — RESEARCH §"estimatedStable + lostContactAtMs detection" code 정확히 정합. proximity_threshold 0.08 × torso_scale + hold debounce 2 + lost debounce 2 + reliability 가중 confidence.
      * 내부 helper `_detect_abnormal_release(metric, phase_boundaries) -> str | None` — RESEARCH §"Abnormal release detection" 정확히 정합. lock.start_ms <= lost_contact_at_ms <= hold.end_ms → 'abnormal_release_during_hold' 반환.
      * 내부 helper `_longest_consecutive_true(mask: np.ndarray) -> int` + `_find_first_loss(mask: np.ndarray, debounce: int) -> int | None` — numpy boolean run-length helper.
      * 본체: expected_contact_points 가 비어있으면 fallback path (RESEARCH §"motion_id 미인식 fallback" 정확히 정합 — fallback_points=[left_hand, right_hand, left_foot, right_foot] × 각 phase × estimated_stable=None + confidence='low' + warning 'motion_unrecognized'). expected 가 비어있지 않으면 normal path (각 phase × expected_contact_points iterate + abnormal release detect + severity 산출).
    - **compute_force_signals() umbrella 본체** (RESEARCH §"Code Examples Example 1" 정확히 정합):
      * Step 1: angles None 시 `features.compute_joint_angles + joint_uncertainty` 호출 (pose_frame.to_coco17_array 박제 정합)
      * Step 2: `angles_filled = temporal.temporal_fill(angles, uncertainty)` 호출 (D-08-U1)
      * Step 3: compute_phase_boundaries 호출
      * Step 4: 3 metric 함수 호출 (compute_axis_deviation / compute_stability_metrics / compute_contact_stability)
      * Step 5: _overall_confidence(phase_boundaries, axis, stability, contact) -> MetricConfidence (RESEARCH §"overall_confidence" 정확히 정합 — min propagation: low > medium > high)
      * Step 6: warnings 조립 (motion_id None → 'motion_unrecognized_layer1_only', gemini_extractor None → 'layer2_unavailable')
      * Step 7: ForceSignalsReport 조립 + return
    - 4 단위 test 본 task scope (test_compute_force_signals 는 Task 3 또는 Task 2 에서 박제 — 본 task 가 박제):
      * test_compute_phase_boundaries.py: 5 test (Layer 1 only fixture_clean_invert 활용 / 5 phase emit / video_too_short / heavy_occlusion / Layer 2 hook 비호출)
      * test_compute_axis_deviation.py: 7 test (pelvis distance body-scale / chest distance / shoulder tilt / hip tilt / deviation_direction outward / severity threshold / pelvis_drop fixture 활용)
      * test_compute_stability_metrics.py: 7 test 포함 **drift defense** (test_drift_defense: 동일 (T=50, J=8) angles → force_signals jitter == dimensions.stability_wobble abs < 1e-9)
      * test_compute_contact_stability.py: 7 test (yaml load + proximity + debounce + abnormal release + fallback + layback_release fixture)
  </behavior>
  <action>
    Task 1 의 5 stub 함수 본체를 RESEARCH §"Pattern 2/3/4" + §"AxisDeviationMetric Algorithm" + §"StabilityMetric Algorithm" + §"ContactStabilityMetric Algorithm" + §"Confidence + Smoothing" 의 code 정확히 박제. 15+ private helper 함수 (_phase_signals / _layer1_heuristic_boundaries / _point_to_axis_distance / _pelvis_distance / _chest_distance / _shoulder_tilt / _hip_tilt / _deviation_direction / _severity_from_distance / _severity_from_tilt / _severity_from_jitter / _severity_from_jerk / _compute_jerk / _compute_unstable_body_parts / _load_expected_contact_points / _detect_contact_stability / _detect_abnormal_release / _weighted_aggregate / _overall_confidence / _should_invoke_layer2 / _longest_consecutive_true / _find_first_loss) 모두 박제.

    Critical drift defense — compute_stability_metrics 의 jitter_score 산출 단계는 `from .dimensions import stability_wobble` import 후 `jitter_score = stability_wobble(sliced, profile=None)` 직접 호출. **산식 복제 금지** (drift defense test 가 검증).

    Critical Layer 2 hook — compute_phase_boundaries 안 `if _should_invoke_layer2(motion_id, gemini_extractor, video_uri):` 분기는 박제하되 본 plan 의 본체 = `# Plan 08-03 가 Layer 2 활성화` 주석 + Layer 1 단독 반환. 즉 Layer 2 분기 진입 X (gemini_extractor None 박제 강제). Plan 08-03 가 본 분기 안 실제 Gemini 호출 + agreement 룰 박제.

    Edge case 박제:
      - T < 10 frames: _layer1_heuristic_boundaries 시작점에서 detect → 단일 hold phase (start=0, end=T) + warning 'video_too_short' + confidence='low'.
      - all-NaN angles (after temporal_fill 도 NaN 잔존): 단일 hold + warning 'heavy_occlusion' + confidence='low'.

    4 단위 test 파일 박제 — 위 behavior 의 각 test 함수 박제. pytest fixture 활용 (conftest.py 의 synthetic_body_profile + load_pose_frames_from_fixture). drift defense test 박제 정확히 — `rng = np.random.default_rng(0); angles = rng.normal(120, 5, size=(50, 8)); direct = dimensions.stability_wobble(angles, profile=None); pb = [PhaseBoundary("hold", 0, 50, 0, 5444, "medium", "heuristic")]; metrics = force_signals.compute_stability_metrics(angles, pb, pose_frames=[]); assert abs(metrics[0].jitter_score - direct) < 1e-9`.

    per RESEARCH §"AxisDeviationMetric Algorithm" + §"StabilityMetric Algorithm" + §"ContactStabilityMetric Algorithm" + §"Confidence + Smoothing" + D-08-A1~U3 모두.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && PYTHONPATH=backend/shared/python:. backend/.venv/bin/python3 -m pytest backend/tests/phase08/test_compute_phase_boundaries.py backend/tests/phase08/test_compute_axis_deviation.py backend/tests/phase08/test_compute_stability_metrics.py backend/tests/phase08/test_compute_contact_stability.py -x</automated>
  </verify>
  <acceptance_criteria>
    - File modified: force_signals.py 의 5 public 함수 본체가 NotImplementedError 가 아닌 실제 구현
    - Source: `grep -c "from .dimensions import stability_wobble\|from . import dimensions" backend/shared/python/sunity_shared/analysis/force_signals.py` >= 1
    - Source: `grep -c "yaml.safe_load" backend/shared/python/sunity_shared/analysis/force_signals.py` >= 1
    - Source: `grep -c "def _" backend/shared/python/sunity_shared/analysis/force_signals.py` >= 15 (15+ private helpers)
    - Behavior (drift defense): test_drift_defense PASS — force_signals.compute_stability_metrics 의 jitter_score 가 dimensions.stability_wobble 호출 결과와 abs < 1e-9
    - Behavior (Layer 1 only): test_layer2_hook_not_invoked_when_gemini_extractor_none PASS — gemini_extractor=None 호출 시 Layer 2 분기 진입 X
    - Behavior (edge cases): test_video_too_short_falls_back_to_single_hold + test_heavy_occlusion_returns_low_confidence PASS
    - Behavior (fixture): test_pelvis_drop_fixture_emits_high_severity_outward + test_jerk_high_fixture_emits_high_severity + test_layback_release_fixture_emits_abnormal_warning PASS
    - Behavior (yaml): test_yaml_load_motion_id_lookup PASS — ref-invert / ref-foxtop / motion=None / motion=unknown 모두 정합
    - Behavior: 4 test 파일 26 test 모두 PASS (pytest exit 0)
  </acceptance_criteria>
  <done>
    force_signals.py 의 4 metric 함수 본체 박제 완료. Layer 1 휴리스틱 + 5단계 boundary + AxisDeviation + Stability + ContactStability 단위 test 모두 PASS. drift defense 검증 (jitter_score == dimensions.stability_wobble). Task 3 가 compute_force_signals umbrella 본체 + 통합 test 박제.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: compute_force_signals umbrella 본체 + 통합 test (temporal_fill + confidence weighting + warnings)</name>
  <files>
    backend/shared/python/sunity_shared/analysis/force_signals.py
    backend/tests/phase08/test_compute_force_signals.py
  </files>
  <read_first>
    backend/shared/python/sunity_shared/analysis/force_signals.py (Task 1 + Task 2 박제 본체 확인)
    backend/shared/python/sunity_shared/analysis/temporal.py (temporal_fill 시그너처)
    backend/shared/python/sunity_shared/analysis/features.py (compute_joint_angles + joint_uncertainty + pose_frame.to_coco17_array)
    backend/tests/phase08/conftest.py
    backend/tests/phase08/fixtures/_factory.py
    .planning/phases/08-jerk-jitter/08-RESEARCH.md §"Code Examples Example 1" + §"Confidence + Smoothing" + §"overall_confidence"
  </read_first>
  <behavior>
    - compute_force_signals() umbrella 본체 박제 (RESEARCH §"Code Examples Example 1" 정확히 정합) — 7 step orchestrator:
      * Step 1 angles 산출 (None 시 features 호출, else 입력 사용)
      * Step 2 temporal.temporal_fill 적용 (D-08-U1)
      * Step 3 compute_phase_boundaries 호출
      * Step 4 compute_axis_deviation + compute_stability_metrics + compute_contact_stability 호출
      * Step 5 _overall_confidence 산출 (low > medium > high min propagation)
      * Step 6 warnings 조립 (motion_id None → 'motion_unrecognized_layer1_only' / gemini_extractor None → 'layer2_unavailable')
      * Step 7 ForceSignalsReport(version="1.0", overall_confidence=..., warnings=..., phase_boundaries=..., axis_metrics=..., stability_metrics=..., contact_metrics=...) 조립 + return
    - _overall_confidence(phase_boundaries, axis, stability, contact) -> MetricConfidence 박제 — RESEARCH §"overall_confidence" code 정확히 정합. 모든 confidence 수집 후 low > medium > high 우선순위로 min 반환.
    - 6 통합 test 박제 (test_compute_force_signals.py):
      * test_compute_force_signals_e2e_clean_invert: fixture_clean_invert 입력 → ForceSignalsReport(version="1.0", overall_confidence in {'high', 'medium', 'low'}, phase_boundaries len==5, axis_metrics len>=5, stability_metrics len>=5, contact_metrics len>=4) — 정은지급 깔끔한 영상에서 모든 metric severity='low' 또는 'medium'
      * test_temporal_fill_applied_before_metrics: mock temporal.temporal_fill (unittest.mock.patch) — 호출됨 + 호출 후 angles 가 metric 함수에 전달 검증
      * test_confidence_weighting_emits_warning_on_high_low_reliability: fixture_occluded_lock 활용 → lock phase 의 axis_metric / stability_metric 의 warnings 안 'occlusion_high_in_phase' 박제
      * test_overall_confidence_min_propagation: 합성 입력 — 1 metric confidence='low' → ForceSignalsReport.overall_confidence == 'low'. 모든 'medium' → 'medium'. 모든 'high' → 'high'.
      * test_motion_unrecognized_emits_warning: motion_id=None 호출 → ForceSignalsReport.warnings contains 'motion_unrecognized_layer1_only'
      * test_layer2_unavailable_emits_warning: gemini_extractor=None 호출 → warnings contains 'layer2_unavailable'
  </behavior>
  <action>
    Task 2 의 force_signals.py 본체에 compute_force_signals umbrella + _overall_confidence helper 박제. RESEARCH §"Code Examples Example 1" 의 code 정확히 mirror — Step 1~7 orchestrator.

    Step 1 angles 산출: `if angles is None: from .pose_frame import to_coco17_array; kp_array = to_coco17_array(pose_frames); angles = features.compute_joint_angles(kp_array); uncertainty = features.joint_uncertainty(kp_array); else: uncertainty = None`.

    Step 2 temporal_fill: `angles_filled = temporal.temporal_fill(angles, uncertainty)`.

    Step 3 phase boundaries: compute_phase_boundaries 호출 (Layer 1 only — gemini_extractor=None 분기, Plan 08-03 가 Layer 2 활성).

    Step 4 3 metric: compute_axis_deviation + compute_stability_metrics + compute_contact_stability 호출 (각 phase_boundaries 입력).

    Step 5 overall_confidence: 모든 metric 의 confidence 수집 + min 반환.

    Step 6 warnings 조립: list 박제 — motion_id None / gemini_extractor None / temporal_fill warning 등.

    Step 7 ForceSignalsReport 조립 + return.

    6 통합 test 박제 — 위 behavior 의 6 test 모두 박제. fixture 활용 (fixture_clean_invert + fixture_occluded_lock). mock 활용 (unittest.mock.patch 로 temporal.temporal_fill mock — call_args 검증).

    per RESEARCH §"Code Examples Example 1" + §"Confidence + Smoothing" + §"overall_confidence" + D-08-U1 + D-08-U2.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && PYTHONPATH=backend/shared/python:. backend/.venv/bin/python3 -m pytest backend/tests/phase08/ -x</automated>
  </verify>
  <acceptance_criteria>
    - File modified: force_signals.py 의 compute_force_signals umbrella + _overall_confidence helper 본체 박제 (NotImplementedError 제거)
    - Source: `grep -c "temporal.temporal_fill\|temporal_fill" backend/shared/python/sunity_shared/analysis/force_signals.py` >= 1
    - Source: `grep -c "def _overall_confidence" backend/shared/python/sunity_shared/analysis/force_signals.py` >= 1
    - Behavior (E2E clean invert): test_compute_force_signals_e2e_clean_invert PASS — ForceSignalsReport with 5 phase boundaries + all metric lists non-empty
    - Behavior (temporal_fill): test_temporal_fill_applied_before_metrics PASS — temporal_fill 호출 검증
    - Behavior (confidence weighting): test_confidence_weighting_emits_warning_on_high_low_reliability PASS — occlusion warning emission
    - Behavior (overall confidence): test_overall_confidence_min_propagation PASS — low > medium > high min
    - Behavior (warnings): test_motion_unrecognized_emits_warning + test_layer2_unavailable_emits_warning PASS
    - Behavior (full suite): `pytest backend/tests/phase08/ -x` exit 0 (Task 1 + Task 2 + Task 3 모든 test PASS — drift defense 포함)
    - Behavior (regression): `pytest backend/tests/phase06/ backend/tests/phase07/ backend/tests/pipeline/ -x` exit 0 (회귀 0)
  </acceptance_criteria>
  <done>
    compute_force_signals umbrella 본체 박제 완료. temporal_fill 적용 + 5 step orchestrator + overall_confidence min propagation + warnings emission. 통합 test 6 PASS. phase08 전체 단위 test PASS + phase06 / phase07 / pipeline 회귀 0. Plan 08-03 가 Layer 2 Gemini wiring + pipeline 통합 + Firestore + frontend normalize 박제.
  </done>
</task>

</tasks>

<verification>
## Phase 8 Plan 02 Verification

**Goal-backward derived truths (FORCE-01 backend 산출 logic):**

- force_signals.py 신설 — 5 dataclass + 5 type alias + 5 public 함수 + 15+ private helper, pure-function numpy + dimensions/temporal helper reuse
- compute_phase_boundaries Layer 1 휴리스틱: 5단계 boundary (entry/lock/transition/final_shape/hold) 산출 (motion-agnostic, D-08-A1 + D-08-A3)
- compute_axis_deviation: pelvis/chest distance + shoulder/hip tilt + deviation_direction + severity (body-scale 정규화, D-08-D1)
- compute_stability_metrics: jitter (dimensions.stability_wobble reuse) + jerk (np.diff n=3 + MAD outlier) + hold (dimensions reuse) + unstable_body_parts (D-08-C1 + D-08-C2)
- compute_contact_stability: contact_points.yaml load + proximity + debounce + abnormal release + motion_id 미인식 fallback (D-08-B1~B5)
- compute_force_signals umbrella: temporal_fill (D-08-U1) → 4 metric → overall_confidence → warnings → ForceSignalsReport
- drift defense test PASS: force_signals.jitter_score == dimensions.stability_wobble (abs < 1e-9)
- models.py active import — Plan 08-01 의 3-way lockstep 활성화 완성
- Layer 2 hook 시그너처만 박제 (본체 = Layer 1 단독 분기 only) — Plan 08-03 가 Layer 2 wiring

**Manual verification (belle 검수 박제 대기):**
- Layer 1 휴리스틱 25-timestamp belle labeling sanity (5영상 × 5 phase) — 80% 일치 가설 검증 (08-VALIDATION.md §"Manual-Only Verifications")
- severity 임계 분포 sanity check (정은지 5영상 90%+ 'low' 박제)

**Regression checks:**
- `pytest backend/tests/phase06/` PASS (156 test)
- `pytest backend/tests/phase07/` PASS (108 test)
- `pytest backend/tests/pipeline/` PASS
- `pytest backend/tests/phase08/` PASS (29+ test — lockstep 3 + yaml 4 + drift 3 + phase_boundaries 5 + axis 7 + stability 7 + contact 7 + force_signals 6)
- `tsc --noEmit clean` (Plan 08-01 박제 schema 활성 유지)
</verification>

<success_criteria>
- [x] force_signals.py 5 dataclass + 5 type alias + 5 public 함수 + 15+ private helper 박제
- [x] models.py active import (Plan 08-01 lockstep 활성화 완성)
- [x] Layer 1 휴리스틱 5단계 boundary 산출 (motion-agnostic)
- [x] compute_axis_deviation body-scale 정규화 + severity
- [x] compute_stability_metrics dimensions.stability_wobble reuse + jerk MAD + drift defense PASS
- [x] compute_contact_stability yaml load + proximity + debounce + abnormal release + fallback
- [x] compute_force_signals umbrella temporal_fill + overall_confidence + warnings
- [x] 29+ 단위 test PASS (drift defense 포함)
- [x] phase06 / phase07 / pipeline 회귀 0
</success_criteria>

<output>
Create `.planning/phases/08-jerk-jitter/08-02-SUMMARY.md` when done. Include:
- 3 task commit SHA + 각 task scope
- 5 public 함수 + 15+ private helper 박제 결과
- drift defense test 결과 (force_signals.jitter_score vs dimensions.stability_wobble)
- Layer 2 hook 시그너처 박제 (Plan 08-03 가 활성화 위치)
- 29+ phase08 test PASS + phase06/07/pipeline 회귀 0 확인
- belle 검수 manual gate 박제 (Layer 1 25-timestamp + severity 분포 sanity check)
- Plan 08-03 진입 시그널 (pipeline wiring + Firestore + frontend normalize + Layer 2 Gemini)
- Deviation (Rule 1 fix / Rule 3 environment fix 등) 있으면 박제
</output>
