---
phase: 08-jerk-jitter
plan: 02
type: execute
wave: 2
depends_on: [08-00, 08-01]
files_modified:
  - backend/shared/python/sunity_shared/analysis/force_signals.py
  - backend/shared/python/sunity_shared/models.py
  - backend/tests/phase08/test_compute_phase_boundaries.py
  - backend/tests/phase08/test_compute_axis_deviation.py
  - backend/tests/phase08/test_compute_stability_metrics.py
  - backend/tests/phase08/test_compute_contact_stability.py
  - backend/tests/phase08/test_compute_force_signals.py
  - backend/tests/phase08/test_fps_invariance.py
autonomous: true
requirements: [FORCE-01]
subsystem: backend-analysis-core
tags: [phase-08, force-signals, layer1-heuristic, axis-deviation, stability-metric, contact-stability, drift-defense, reviews-cycle-1-revised, fps-normalized]

scope_sanity_note: "REVIEWS Cycle 1 (cycle: 1, codex) 반영 revised. depends_on 신설: [08-00, 08-01]. axis_deviation 본체가 Plan 08-00 박제 point_to_pole_line_distance_2d + median_torso_length 사용 (REVIEWS R1/R2 정합). compute_stability_metrics 의 jerk_score 가 dt=1/fps 정규화 (REVIEWS R5 정합). compute_force_signals umbrella 의 temporal_fill 중복 호출 차단 (REVIEWS R5 정합 — pipeline 의 temporal_fill 결과 angles 인자로 받음). Layer 1 confidence default = 'low' (preflight gate PASS 전, REVIEWS R4 정합). contact 본체가 evidence-with-confidence (estimated_stable nullable, REVIEWS R3 정합). 신설 test_fps_invariance.py 박제 — 9 fps vs 18 fps 동일 jerk_score 검증 (REVIEWS R5)."

must_haves:
  truths:
    - "force_signals.py 모듈 신설 — 5 dataclass + 5 public 함수 + 5 type alias + 20+ private helper. Plan 08-00 박제 PoleLine2D / PoleAxisMeasurement / median_torso_length 사용. Plan 08-01 박제 schema 정확히 mirror. per REVIEWS R1 + R2 + R3 + R4 + R5."
    - "compute_axis_deviation() 본체 (REVIEWS R1 + R2 정합) — pelvis/chest distance 산출 시 Plan 08-00 박제 `point_to_pole_line_distance_2d(point, line)` 호출 (line 가용 시) 또는 None 반환 (line 미가용 시). denominator = Plan 08-00 박제 `median_torso_length(pose_frames, space='image_2d')` 호출 (line space 정합). BodyNormalizationProfile.torso_scale 사용 영구 금지 강제 — `assert 'torso_scale' not in compute_axis_deviation_source` import 차단 test. line 미가용 시 모든 distance 필드 null + warning 'pole_line_missing' + coordinateSpace='unavailable' + scaleDenominator='unavailable'. observed length 미가용 (valid frame < 5) 시 distance null + warning 'scale_unavailable' + scaleDenominator='unavailable'."
    - "compute_stability_metrics() FPS 정규화 (REVIEWS R5 정합) — `_compute_jerk(angles_window, fps)` 시그너처. 본체: `dt = 1.0 / fps; raw_jerk_per_frame = np.abs(np.diff(angles, n=3, axis=0)); jerk_per_sec_cubed = raw_jerk_per_frame / (dt ** 3); 최종 = MAD outlier rejection + nanmedian`. 단위 = deg/sec^3. jerk_unit = 'deg_per_sec_cubed' 박제. 9 fps vs 18 fps 동일 motion → 동일 jerk_score (FPS invariance test 검증). jitter_score = dimensions.stability_wobble 직접 호출 (단위 = deg/frame, drift defense)."
    - "compute_force_signals() umbrella temporal_fill 중복 차단 (REVIEWS R5 정합) — 시그너처 변경: `compute_force_signals(pose_frames, pole_axis_measurement, body_profile, *, angles, fps, motion_id=None, gemini_extractor=None, video_uri=None) -> ForceSignalsReport`. angles 인자 = 호출자가 이미 temporal_fill 적용된 (T, J) 배열 박제 강제 (pipeline._process 가 _extract_video_analysis_inputs 단계에서 1회 박제). compute_force_signals 본체 = temporal_fill 호출 X — 단일 smoothing pass 강제. angles 인자 None 박제 영구 금지 (caller responsibility)."
    - "Layer 1 confidence default = 'low' (REVIEWS R4 + Plan 08-00 박제 preflight gate 정합) — compute_phase_boundaries 시그너처 변경: `preflight_label_gate_passed: bool | None = None` 키워드 인자 박제. None (default) → confidence='low' + warning 'preflight_gate_pending'. False → confidence='low' + warning 'preflight_label_gate_failed'. True → confidence='medium'. Layer 2 활성 시 (Plan 08-03 가 박제) confidence='high'/'medium' (agreement 룰, ceiling 박제 박제). D-08-A2 의 'Layer 1 만 → medium' 박제 강제 완화 — preflight gate 박제 source. **신설 helper `_layer1_confidence_from_preflight(preflight_label_gate_passed) -> (confidence, warnings)` 박제** — Plan 08-03 의 Layer 2 except 분기 + success 분기 가 본 helper reuse 박제 (REVIEWS Cycle 2 NEW HIGH #2 차단 정합 — preflight gate ceiling 위로 promote 영구 X)."
    - "compute_contact_stability() evidence-with-confidence (REVIEWS R3 정합) — Plan 08-00 박제 contact_points.yaml load (kind 필드 동행 박제) + Plan 08-00 박제 median_torso_length (denominator) + Plan 08-00 박제 point_to_pole_line_distance_2d (line 가용 시). 본체: 각 contact_point × phase 별 산출:\n  · distance_to_pole_norm: distance / observed_torso_length (line + scale 모두 가용 시) 또는 None\n  · near_pole_ratio: phase 내 frame 중 distance <= CONTACT_PROXIMITY_THRESHOLD (0.08) 비율 (line 가용 시) 또는 None\n  · estimated_stable: near_pole_ratio >= 0.8 + frame count >= CONTACT_HOLD_MIN_FRAMES (2) → True / near_pole_ratio < 0.3 → False / 그 외 → None (evidence 불충분, warning 'contact_evidence_only')\n  · lost_near_pole_at_ms: distance > 0.20 observed_torso_length 시점의 첫 timestamp_ms (debounce 2 frames) 또는 None\n  · measurement_kind: yaml entry 의 kind 박제 (또는 motion_id None 시 null + warning 'motion_unrecognized')\n  · coordinate_space: 'image_2d' (line 가용 시) 또는 'unavailable'\n  · _detect_abnormal_release: lock.start_ms <= lost_near_pole_at_ms <= hold.end_ms → warning 'abnormal_release_during_hold'."
    - "5 dataclass 모두 frozen=True + __post_init__ Literal enum validator. field(default_factory=list) 로 warnings 빈 list[] 기본값."
    - "compute_phase_boundaries() Layer 1 motion-agnostic 휴리스틱 — foot_y + hand_pole_dist (Plan 08-00 박제 point_to_pole_line_distance_2d 사용, line 가용 시) + keypoint_velocity 3 신호 cutoff 룰로 5단계 boundary 산출. Layer 2 hook 시그너처 박제 (gemini_extractor: GeminiMomentExtractor | None = None) — 본 plan 의 본체 = no-op (None 분기 only, Plan 08-03 가 활성화)."
    - "Layer 1 threshold 박제 (module-level 상수): LAYER1_GROUND_Y_RATIO=0.85 / LAYER1_HAND_POLE_DIST_LOCK=0.15 / LAYER1_VELOCITY_MID=0.03 / LAYER1_VELOCITY_HIGH=0.06 / LAYER1_MIN_PHASE_FRAMES=2. [ASSUMED] belle Pod sweep sanity check 박제 manual gate (08-VALIDATION.md). REVIEWS R4 정합 — threshold 자체는 도메인/IPSF-inspired starting hypothesis 박제 (Suggestions §2 정합)."
    - "compute_force_signals() umbrella 변경 — Step 1 angles 인자 검증 (None 영구 금지) → Step 2 compute_phase_boundaries (preflight gate 박제 source 전달) → Step 3 3 metric 함수 호출 → Step 4 _overall_confidence → Step 5 warnings 조립 (pole_line_missing / scale_unavailable / preflight_label_gate_failed / fps_normalization_applied 등 박제) → Step 6 ForceSignalsReport 조립. pipeline/app.py 가 본 함수 1줄로 호출 (Plan 08-03 wiring)."
    - "drift defense test_compute_stability_metrics::test_drift_defense — force_signals.compute_stability_metrics 의 jitter_score 가 dimensions.stability_wobble 직접 호출과 abs < 1e-9 일치 검증."
    - "FPS invariance test (test_fps_invariance.py 신설, REVIEWS R5 정합) — 9 fps vs 18 fps 동일 motion (programmatic factory 박제 후 frame 2x upsample) → jerk_score 동일 (rel tolerance 5%). jitter_score 는 frame-rate dependent (검증 안 함). REVIEWS R5 권고 정합."
    - "models.py 의 Plan 08-01 placeholder 주석을 활성 import 로 전환 — from .analysis.force_signals import (10 names). 단일 atomic commit (Plan 08-01 의 3-way lockstep 활성화 완성)."
    - "Plan 08-01 박제 6 programmatic fixture builder (_fixture_builders.py) 모두 unit test 에서 활용 — JSON 박제 영구 차단 (REVIEWS R9)."
  artifacts:
    - path: backend/shared/python/sunity_shared/analysis/force_signals.py
      provides: "Phase 8 본체 모듈. 5 dataclass + 5 public function + 5 type alias + 20+ private helper. pure-function (numpy + Plan 08-00 박제 helper import + Plan 08-01 박제 dimensions.stability_wobble + yaml.safe_load). Plan 08-00 박제 PoleLine2D / PoleAxisMeasurement / median_torso_length 사용 (REVIEWS R1/R2 정합)."
      contains:
        - "from __future__ import annotations"
        - "from . import dimensions"
        - "from .pole_geometry import"
        - "PoleLine2D"
        - "PoleAxisMeasurement"
        - "point_to_pole_line_distance_2d"
        - "from .body_scale import median_torso_length"
        - "from .pose_frame import PoseFrame, PoleAxis"
        - "MotionPhase = Literal"
        - "DeviationDirection = Literal"
        - "SeverityLevel = Literal"
        - "MetricConfidence = Literal"
        - "ContactPoint = Literal"
        - "@dataclass(frozen=True)"
        - "class PhaseBoundary"
        - "preflight_label_gate_passed"
        - "class AxisDeviationMetric"
        - "coordinate_space"
        - "scale_denominator"
        - "class StabilityMetric"
        - "jerk_unit"
        - "class ContactStabilityMetric"
        - "estimated_stable"
        - "distance_to_pole_norm"
        - "near_pole_ratio"
        - "lost_near_pole_at_ms"
        - "measurement_kind"
        - "class ForceSignalsReport"
        - "def compute_phase_boundaries("
        - "def compute_axis_deviation("
        - "def compute_stability_metrics("
        - "def compute_contact_stability("
        - "def compute_force_signals("
        - "stability_wobble"
        - "deg_per_sec_cubed"
        - "fps_normalization_applied"
        - "_layer1_confidence_from_preflight"
        - "preflight_gate_pending"
    - path: backend/shared/python/sunity_shared/models.py
      provides: "Plan 08-01 placeholder 주석을 활성 import 로 전환 — 10 name import 활성화. 단일 atomic commit (Plan 08-01 의 3-way lockstep 완성)."
      contains:
        - "from .analysis.force_signals import"
        - "MotionPhase"
        - "DeviationDirection"
        - "SeverityLevel"
        - "MetricConfidence"
        - "ContactPoint"
        - "PhaseBoundary"
        - "AxisDeviationMetric"
        - "StabilityMetric"
        - "ContactStabilityMetric"
        - "ForceSignalsReport"
    - path: backend/tests/phase08/test_compute_phase_boundaries.py
      provides: "Layer 1 휴리스틱 단위 test — preflight gate 박제 source 분기 (None / False / True) 검증 (REVIEWS R4 정합). 영상 hold 만 / 짧음 / heavy occlusion edge case + Layer 2 hook 비호출 검증."
      contains:
        - "def test_layer1_only_with_clean_invert"
        - "def test_layer1_default_confidence_low_without_preflight_gate"
        - "def test_layer1_warning_preflight_gate_pending_when_none"
        - "def test_layer1_confidence_medium_when_preflight_gate_passed_true"
        - "def test_layer1_warning_preflight_label_gate_failed_when_false"
        - "def test_layer1_confidence_from_preflight_helper_three_state"
        - "def test_video_too_short_falls_back_to_single_hold"
        - "def test_heavy_occlusion_returns_low_confidence"
        - "def test_layer2_hook_not_invoked_when_gemini_extractor_none"
    - path: backend/tests/phase08/test_compute_axis_deviation.py
      provides: "AxisDeviationMetric 단위 test — Plan 08-00 박제 point_to_pole_line_distance_2d + median_torso_length 사용 검증 (REVIEWS R1/R2). line 가용 / 미가용 분기 검증 + scale 가용 / 미가용 분기 검증."
      contains:
        - "def test_pelvis_distance_uses_observed_torso_length_denominator"
        - "def test_chest_distance_uses_observed_torso_length_denominator"
        - "def test_line_missing_returns_null_distance_with_warning"
        - "def test_scale_unavailable_returns_null_distance_with_warning"
        - "def test_coordinate_space_image_2d_when_line_present"
        - "def test_coordinate_space_unavailable_when_line_none"
        - "def test_torso_scale_not_used_as_denominator"
        - "def test_pelvis_drop_fixture_emits_high_severity_outward"
        - "def test_shoulder_tilt_signed_degrees"
        - "def test_hip_tilt_signed_degrees"
        - "def test_deviation_direction_outward"
    - path: backend/tests/phase08/test_compute_stability_metrics.py
      provides: "StabilityMetric 단위 test — jitter (dimensions reuse) + jerk (FPS-normalized, deg/sec^3) + hold + unstable. drift defense test + jerk_unit='deg_per_sec_cubed' 박제 검증."
      contains:
        - "def test_drift_defense"
        - "def test_jitter_score_uses_dimensions_helper"
        - "def test_jerk_score_fps_normalized_deg_per_sec_cubed"
        - "def test_jerk_unit_field_is_deg_per_sec_cubed"
        - "def test_jerk_score_mad_filtered"
        - "def test_hold_stability_score_on_hold_phase_only"
        - "def test_hold_stability_score_returns_none_when_phase_not_hold"
        - "def test_unstable_body_parts_threshold_filter"
        - "def test_jerk_high_fixture_emits_high_severity"
    - path: backend/tests/phase08/test_compute_contact_stability.py
      provides: "ContactStabilityMetric 단위 test — Plan 08-00 박제 yaml (kind 필드 동행) load + estimated_stable nullable evidence + nearPoleRatio + lostNearPoleAtMs + abnormal release + measurementKind + motion_id 미인식 fallback (REVIEWS R3 정합)."
      contains:
        - "def test_yaml_load_motion_id_lookup_returns_kind"
        - "def test_distance_to_pole_norm_uses_observed_torso_length"
        - "def test_near_pole_ratio_phase_internal"
        - "def test_estimated_stable_true_when_evidence_strong"
        - "def test_estimated_stable_false_when_evidence_low"
        - "def test_estimated_stable_null_when_evidence_unclear"
        - "def test_measurement_kind_from_yaml"
        - "def test_lost_near_pole_at_ms_detection"
        - "def test_abnormal_release_during_hold_warning"
        - "def test_motion_unrecognized_fallback_estimated_stable_null"
        - "def test_layback_release_fixture_emits_abnormal_warning"
    - path: backend/tests/phase08/test_compute_force_signals.py
      provides: "umbrella 통합 test — compute_force_signals() E2E. temporal_fill 호출 X 강제 (REVIEWS R5 정합 — angles 인자 = caller 가 1회 박제) + 4 metric 산출 + overall_confidence + warnings emission."
      contains:
        - "def test_compute_force_signals_e2e_clean_invert"
        - "def test_compute_force_signals_does_not_call_temporal_fill"
        - "def test_compute_force_signals_requires_angles_not_none"
        - "def test_confidence_weighting_emits_warning_on_high_low_reliability"
        - "def test_overall_confidence_min_propagation"
        - "def test_motion_unrecognized_emits_warning"
        - "def test_layer2_unavailable_emits_warning"
        - "def test_warning_fps_normalization_applied_emitted"
    - path: backend/tests/phase08/test_fps_invariance.py
      provides: "FPS invariance test 신설 (REVIEWS R5 정합) — 9 fps vs 18 fps 동일 motion → jerk_score 동일 (rel tolerance 5%) 검증. jitter_score 는 frame-rate dependent (검증 안 함, 박제 메모만)."
      contains:
        - "def test_jerk_score_invariant_under_fps_change"
        - "def test_jerk_unit_remains_deg_per_sec_cubed_at_both_fps"
        - "def test_jitter_score_is_frame_rate_dependent_documented"
  key_links:
    - from: backend/shared/python/sunity_shared/analysis/force_signals.py
      to: backend/shared/python/sunity_shared/analysis/dimensions.py
      via: "from .dimensions import stability_wobble — Plan 08-01 박제 helper import. force_signals.compute_stability_metrics 의 jitter_score 산출 source. drift defense."
      pattern: "stability_wobble"
    - from: backend/shared/python/sunity_shared/analysis/force_signals.py
      to: backend/shared/python/sunity_shared/analysis/pole_geometry.py
      via: "from .pole_geometry import PoleLine2D, PoleAxisMeasurement, point_to_pole_line_distance_2d — Plan 08-00 박제 contract 사용. axis distance / contact distance 산출 source. REVIEWS R1 blocker 해소."
      pattern: "point_to_pole_line_distance_2d|PoleAxisMeasurement"
    - from: backend/shared/python/sunity_shared/analysis/force_signals.py
      to: backend/shared/python/sunity_shared/analysis/body_scale.py
      via: "from .body_scale import median_torso_length — Plan 08-00 박제 helper 사용. observed length denominator source. REVIEWS R2 blocker 해소. BodyNormalizationProfile.torso_scale 영구 사용 금지."
      pattern: "median_torso_length"
    - from: backend/shared/python/sunity_shared/analysis/force_signals.py
      to: backend/judging_data/contact_points.yaml
      via: "_load_expected_contact_points: yaml.safe_load + motion_id lookup (Plan 08-01 박제 yaml + kind 필드 동행)."
      pattern: "expected_contact_points|kind"
    - from: backend/shared/python/sunity_shared/models.py
      to: backend/shared/python/sunity_shared/analysis/force_signals.py
      via: "Python re-export — 10 name import 활성화. Plan 08-01 placeholder 의 주석 해제."
      pattern: "from .analysis.force_signals import"
    - from: backend/tests/phase08/test_compute_stability_metrics.py
      to: backend/shared/python/sunity_shared/analysis/dimensions.py
      via: "drift defense — force_signals.jitter_score 와 dimensions.stability_wobble 직접 호출이 abs < 1e-9 일치."
      pattern: "stability_wobble"
    - from: backend/tests/phase08/test_fps_invariance.py
      to: backend/shared/python/sunity_shared/analysis/force_signals.py
      via: "FPS invariance — _compute_jerk(angles, fps) 가 dt=1/fps 정규화 검증. REVIEWS R5 정합."
      pattern: "_compute_jerk|fps_normalization_applied"
---

<objective>
Phase 8 vertical slice (2/3) revised — **REVIEWS Cycle 1 반영 force_signals.py 본체**. Plan 08-00 박제 contract (PoleLine2D / PoleAxisMeasurement / median_torso_length) + Plan 08-01 박제 schema 위에 실제 산출 logic 박제:

(a) **5 dataclass + 5 type alias** — Plan 08-01 박제 TS interface / contract.md §9 정확히 mirror (REVIEWS Cycle 1 신설 필드 박제).
(b) **compute_phase_boundaries() Layer 1 휴리스틱** — preflight_label_gate_passed 키워드 인자 박제 (REVIEWS R4 정합 — Layer 1 default confidence='low').
(c) **compute_axis_deviation()** — Plan 08-00 박제 point_to_pole_line_distance_2d + median_torso_length 사용 (REVIEWS R1/R2 정합). BodyNormalizationProfile.torso_scale 영구 사용 금지. line 미가용 시 distance null + warning + coordinateSpace='unavailable'.
(d) **compute_stability_metrics()** — jitter (dimensions.stability_wobble 직접 호출, drift defense) + jerk FPS 정규화 (deg/sec^3, REVIEWS R5 정합). jerk_unit='deg_per_sec_cubed' 박제.
(e) **compute_contact_stability()** — evidence-with-confidence (REVIEWS R3 정합). estimated_stable nullable + distanceToPoleNorm + nearPoleRatio + lostNearPoleAtMs + measurementKind. abnormal release detection.
(f) **compute_force_signals() umbrella** — angles 인자 caller responsibility (temporal_fill 중복 차단, REVIEWS R5 정합). 4 metric 호출 → overall_confidence → warnings → ForceSignalsReport.
(g) **models.py 활성화** — Plan 08-01 placeholder 주석 해제 (3-way lockstep 완성).
(h) **6 단위 test 파일** — drift defense + FPS invariance + Layer 1 + 4 metric + 6 programmatic fixture 활용.

Purpose: REVIEWS Cycle 1 의 9 HIGH concern 중 compute 측면 (R1/R2/R3/R4/R5) 영구 해소 + FORCE-01 backend 산출 logic.
Output: force_signals.py 신설 + models.py 1 import 활성화 + 6 단위 test 파일.

**박제 메모 정합:** [[scoring-dimensions-ipsf]] + [[analysis-objectivity-no-human-scores]] (정은지 distribution = sanity check only, NOT calibration ground truth) + [[mvp-simple-pilot-quality]] + [[feedback-analysis-first]] + [[firestore-nested-array-flat]] + [[no-baekje-filler]].

per REVIEWS Cycle 1 R1 + R2 + R3 + R4 + R5 + Plan 08-00 + Plan 08-01 dependency.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-jerk-jitter/08-CONTEXT.md
@.planning/phases/08-jerk-jitter/08-RESEARCH.md
@.planning/phases/08-jerk-jitter/08-REVIEWS.md
@.planning/phases/08-jerk-jitter/08-VALIDATION.md
@.planning/phases/08-jerk-jitter/08-00-PLAN.md
@.planning/phases/08-jerk-jitter/08-01-PLAN.md
@CLAUDE.md
@backend/shared/python/sunity_shared/models.py
@backend/shared/python/sunity_shared/analysis/dimensions.py
@backend/shared/python/sunity_shared/analysis/temporal.py
@backend/shared/python/sunity_shared/analysis/features.py
@backend/shared/python/sunity_shared/analysis/pose_frame.py
@backend/shared/python/sunity_shared/analysis/body_normalization.py
@backend/shared/python/sunity_shared/analysis/pole_geometry.py
@backend/shared/python/sunity_shared/analysis/body_scale.py
@backend/shared/python/sunity_shared/analysis/skeleton.py
@backend/judging_data/contact_points.yaml
@backend/tests/phase08/conftest.py
@backend/tests/phase08/fixtures/_factory.py
@backend/tests/phase08/fixtures/_fixture_builders.py
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Phase 8 = backend 분석 코어 layer. 본 plan = pure-function 모듈 + 단위 test. Layer 2 Gemini 어댑터 경계는 Plan 08-03 wiring. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | T (drift between dimensions vs force_signals) | force_signals.jitter_score | mitigate | dimensions.stability_wobble import 강제 + drift defense test. |
| T-08-02-02 | T (drift between BodyNormalizationProfile vs body_scale) | force_signals.axis distance denominator | mitigate | body_scale.median_torso_length import 강제 + test_torso_scale_not_used_as_denominator (BodyNormalizationProfile import 영구 차단). REVIEWS R2 정합. |
| T-08-02-03 | T (drift PoleAxis direction vs PoleLine2D position) | force_signals.axis distance 산출 | mitigate | point_to_pole_line_distance_2d 사용 강제 (line 가용 시) + line 미가용 시 None 반환 + warning 'pole_line_missing'. REVIEWS R1 정합. |
| T-08-02-04 | T (FPS-dependent threshold drift) | _compute_jerk 의 deg/frame^3 vs deg/sec^3 | mitigate | dt=1/fps 정규화 강제 + test_fps_invariance.py 신설 + jerk_unit='deg_per_sec_cubed' 박제. REVIEWS R5 정합. |
| T-08-02-05 | T (double smoothing) | compute_force_signals umbrella + pipeline temporal_fill | mitigate | umbrella 의 angles 인자 = caller responsibility (temporal_fill 1회 박제). umbrella 본체 temporal_fill 호출 영구 금지. test_compute_force_signals_does_not_call_temporal_fill 검증. REVIEWS R5 정합. |
| T-08-02-06 | I (PII in warning string) | ForceSignalsReport.warnings | mitigate | canned snake_case 영어 warning code 만. |
| T-08-02-07 | T (yaml RCE) | _load_expected_contact_points | mitigate | yaml.safe_load 강제. |
| T-08-02-08 | D (DoS / 무한 loop in Layer 1 heuristic edge case) | _layer1_heuristic_boundaries | mitigate | Edge case 박제 — T < 10 frames → single hold + warning 'video_too_short'. numpy vectorized. |
| T-08-02-SC | Tampering | npm/pip/cargo installs | mitigate | 신규 의존성 0. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: force_signals.py 신설 — 5 dataclass + 5 type alias + 5 public 함수 시그너처 + module-level 상수 + models.py import 활성화</name>
  <files>
    backend/shared/python/sunity_shared/analysis/force_signals.py
    backend/shared/python/sunity_shared/models.py
  </files>
  <read_first>
    backend/shared/python/sunity_shared/analysis/pole_geometry.py (Plan 08-00 박제)
    backend/shared/python/sunity_shared/analysis/body_scale.py (Plan 08-00 박제)
    backend/shared/python/sunity_shared/analysis/dimensions.py (Plan 08-01 박제 stability_wobble)
    backend/shared/python/sunity_shared/analysis/pose_frame.py
    backend/shared/python/sunity_shared/analysis/body_normalization.py
    backend/shared/python/sunity_shared/analysis/technique.py
    backend/shared/python/sunity_shared/models.py
    app/src/types/analysis.ts (Plan 08-01 박제 schema)
    .planning/phases/08-jerk-jitter/08-RESEARCH.md
    .planning/phases/08-jerk-jitter/08-CONTEXT.md
  </read_first>
  <behavior>
    - force_signals.py 모듈 신설 (Plan 08-01 박제 schema 정확히 mirror)
    - 모듈 docstring 박제: Phase 8 본체 + REVIEWS Cycle 1 반영 메모 (R1/R2/R3/R4/R5) + Plan 08-00 박제 contract 의존성 + drift defense.
    - Imports:
      ```python
      from __future__ import annotations
      from dataclasses import dataclass, field
      from pathlib import Path
      from typing import Literal, TYPE_CHECKING
      import logging
      import numpy as np
      import yaml
      from . import dimensions
      from .pole_geometry import PoleLine2D, PoleAxisMeasurement, point_to_pole_line_distance_2d, CoordinateSpace
      from .body_scale import median_torso_length
      from .pose_frame import PoseFrame, PoleAxis, Keypoint3DAligned
      if TYPE_CHECKING:
          from ..judging.gemini_moment_extractor import GeminiMomentExtractor
      ```
    - **Critical: BodyNormalizationProfile import 영구 금지** — REVIEWS R2 drift defense. body_scale.median_torso_length 만 사용.
    - 5 Type alias 박제 — Plan 08-01 TS schema 정확히 mirror:
      * MotionPhase = Literal["entry", "lock", "transition", "final_shape", "hold"]
      * DeviationDirection = Literal["up", "down", "left", "right", "outward", "inward", "unknown"]
      * SeverityLevel = Literal["low", "medium", "high"]
      * MetricConfidence = Literal["low", "medium", "high"]
      * ContactPoint = Literal["left_hand", "right_hand", "left_inner_thigh", "right_inner_thigh", "left_knee", "right_knee", "left_foot", "right_foot", "left_ankle", "right_ankle", "hip", "unknown"]
    - 5 frozen dataclass 박제 (REVIEWS Cycle 1 신설 필드 반영):
      * PhaseBoundary 필드: phase / start_frame_idx / end_frame_idx / start_ms / end_ms / confidence / source (Literal["heuristic", "gemini_assisted", "heuristic_fallback"]) / **preflight_label_gate_passed: bool | None = None** (REVIEWS R4 신설). __post_init__: 모든 enum 검증.
      * AxisDeviationMetric 필드: phase / pelvis_distance_from_pole_axis (float | None) / chest_distance_from_pole_axis (float | None) / shoulder_tilt (float | None) / hip_tilt (float | None) / deviation_direction / severity / confidence / **coordinate_space: CoordinateSpace** (REVIEWS R1) / **scale_denominator: Literal["observed_torso_length", "unavailable"]** (REVIEWS R2) / warnings. __post_init__: enum + coordinate_space-distance invariant (coordinate_space='unavailable' ↔ 모든 distance None) + scale_denominator-distance invariant.
      * StabilityMetric 필드: phase / jitter_score (float) / jerk_score (float) / **jerk_unit: Literal["deg_per_sec_cubed"] = "deg_per_sec_cubed"** (REVIEWS R5) / hold_stability_score (float | None) / unstable_body_parts (list[str]) / severity / confidence / warnings. __post_init__: enum + jerk_unit 검증.
      * ContactStabilityMetric 필드 (REVIEWS R3 정합): phase / contact_point / **measurement_kind: Literal["keypoint", "segment", "region_proxy"] | None** (Plan 08-00 ContactPrimitiveKind, None = motion_id 미인식 fallback) / **estimated_stable: bool | None** (REVIEWS R3 — nullable evidence) / **distance_to_pole_norm: float | None** / **near_pole_ratio: float | None** / **lost_near_pole_at_ms: int | None** / coordinate_space: CoordinateSpace / severity / confidence / warnings. __post_init__: enum.
      * ForceSignalsReport 필드: version (str, default="1.0") / overall_confidence / warnings / phase_boundaries / axis_metrics / stability_metrics / contact_metrics.
    - 5 public 함수 시그너처 박제 (본체 stub `raise NotImplementedError("Task 2/3 가 구현")`):
      * `compute_phase_boundaries(pose_frames, pole_axis_measurement, body_profile, angles, *, motion_id=None, fps=9.0, preflight_label_gate_passed=None, gemini_extractor=None, video_uri=None) -> list[PhaseBoundary]`
      * `compute_axis_deviation(pose_frames, phase_boundaries, pole_axis_measurement, *, fps=9.0) -> list[AxisDeviationMetric]`
      * `compute_stability_metrics(angles, phase_boundaries, pose_frames, *, fps=9.0) -> list[StabilityMetric]`
      * `compute_contact_stability(pose_frames, phase_boundaries, pole_axis_measurement, motion_id, *, fps=9.0) -> list[ContactStabilityMetric]`
      * `compute_force_signals(pose_frames, pole_axis_measurement, body_profile, *, angles, fps, motion_id=None, preflight_label_gate_passed=None, gemini_extractor=None, video_uri=None) -> ForceSignalsReport` — angles 필수 키워드 (None 영구 금지)
    - Module-level 상수 박제:
      * LAYER1_HAND_POLE_DIST_LOCK = 0.15
      * LAYER1_VELOCITY_MID = 0.03
      * LAYER1_VELOCITY_HIGH = 0.06
      * LAYER1_GROUND_Y_RATIO = 0.85
      * LAYER1_MIN_PHASE_FRAMES = 2
      * AXIS_PELVIS_DISTANCE_THRESHOLDS = (0.15, 0.30)  # normalized to observed_torso_length
      * AXIS_CHEST_DISTANCE_THRESHOLDS = (0.20, 0.40)
      * AXIS_TILT_THRESHOLDS_DEG = (10.0, 25.0)
      * JITTER_SEVERITY_THRESHOLDS = (8.0, 20.0)  # deg/frame (frame-rate dependent — 박제 메모)
      * JERK_SEVERITY_THRESHOLDS_DEG_PER_SEC_CUBED = (5000.0, 15000.0)  # deg/sec^3 (FPS-normalized — REVIEWS R5)
      * JERK_MAD_K = 3.0
      * JERK_SMOOTH_WINDOW = 3
      * UNSTABLE_BODY_PART_THRESHOLD_DEG = 12.0
      * CONTACT_PROXIMITY_THRESHOLD_NORM = 0.08  # observed_torso_length 단위
      * CONTACT_LOST_THRESHOLD_NORM = 0.20
      * CONTACT_HOLD_MIN_FRAMES = 2
      * LOST_CONTACT_DEBOUNCE_FRAMES = 2
      * NEAR_POLE_RATIO_STABLE = 0.8
      * NEAR_POLE_RATIO_UNSTABLE = 0.3
      * RELIABILITY_WEIGHT = {"high": 1.0, "medium": 0.7, "low": 0.3}
      * LOW_RELIABILITY_PHASE_THRESHOLD = 0.4
      * LAYER_AGREEMENT_TOLERANCE_FRAMES = 2
      * LAYER_DISAGREEMENT_MAJOR_FRAMES = 5
      * MIN_VALID_FRAMES_FOR_SCALE = 5
      * _CONTACT_POINT_TO_KEYPOINTS dict (12 entries 박제)
      * _CONTACT_POINT_TO_SEGMENT dict (left_inner_thigh / right_inner_thigh entries 박제 — segment 산출 source)
      * _CONTACT_POINT_TO_REGION dict (hip entry 박제 — region_proxy 산출 source)
      * _CONTACT_POINTS_PATH = Path(__file__).parent.parent.parent.parent.parent / "judging_data" / "contact_points.yaml"
      * _CONTACT_POINTS_CACHE: dict | None = None
    - models.py 활성화 — Plan 08-01 placeholder 주석 (10 name forward-declare) 을 활성 `from .analysis.force_signals import (10 names)` 으로 전환.
  </behavior>
  <action>
    backend/shared/python/sunity_shared/analysis/force_signals.py 신설. 위 behavior 의 5 dataclass + 5 type alias + 5 public 함수 시그너처 + 25+ module-level 상수 박제.

    Critical drift defense — **BodyNormalizationProfile import 영구 금지**. 본 모듈은 body_scale.median_torso_length 만 사용 (REVIEWS R2). 본 plan 의 verify command 가 inspect.getsource 로 검증.

    Critical FPS 정규화 — JERK_SEVERITY_THRESHOLDS_DEG_PER_SEC_CUBED 박제 (NOT deg/frame^3). jerk_unit dataclass 필드 default = 'deg_per_sec_cubed' (REVIEWS R5).

    Critical contact primitive — _CONTACT_POINT_TO_KEYPOINTS / _CONTACT_POINT_TO_SEGMENT / _CONTACT_POINT_TO_REGION 3 dict 박제 — 12 contact 별 kind 분류 박제 (Plan 08-00 ContactPrimitiveKind 정합, REVIEWS R3). yaml 의 kind 필드 활용해 적절한 lookup 호출.

    models.py 의 Plan 08-01 placeholder 주석 prefix `# ` 제거 — 활성 import 로 전환.

    Critical 본 commit 후 Plan 08-01 의 lockstep test 가 import 활성화 후도 PASS 유지.

    per REVIEWS R1 + R2 + R3 + R4 + R5 + Plan 08-00 + Plan 08-01.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && PYTHONPATH=backend/shared/python:. backend/.venv/bin/python3 -c "from sunity_shared.analysis import force_signals; assert hasattr(force_signals, 'compute_force_signals') and hasattr(force_signals, 'compute_phase_boundaries') and hasattr(force_signals, 'compute_axis_deviation') and hasattr(force_signals, 'compute_stability_metrics') and hasattr(force_signals, 'compute_contact_stability'); from sunity_shared.models import MotionPhase, ForceSignalsReport, PhaseBoundary, AxisDeviationMetric, StabilityMetric, ContactStabilityMetric; import inspect; src = inspect.getsource(force_signals); assert 'BodyNormalizationProfile' not in src, 'REVIEWS R2 violation — force_signals.py imports BodyNormalizationProfile (drift defense)'; assert 'JERK_SEVERITY_THRESHOLDS_DEG_PER_SEC_CUBED' in src, 'REVIEWS R5 violation — jerk threshold not FPS-normalized'; print('FORCE_SIGNALS_OK')" && PYTHONPATH=backend/shared/python:. backend/.venv/bin/python3 -m pytest backend/tests/phase08/test_force_signals_lockstep.py -x</automated>
  </verify>
  <acceptance_criteria>
    - File exists: backend/shared/python/sunity_shared/analysis/force_signals.py
    - File modified: backend/shared/python/sunity_shared/models.py contains active `from .analysis.force_signals import`
    - Source: `grep -c "@dataclass(frozen=True)" backend/shared/python/sunity_shared/analysis/force_signals.py` >= 5
    - Source: `grep -c "BodyNormalizationProfile" backend/shared/python/sunity_shared/analysis/force_signals.py` == 0 (REVIEWS R2 drift defense)
    - Source: `grep -c "JERK_SEVERITY_THRESHOLDS_DEG_PER_SEC_CUBED" backend/shared/python/sunity_shared/analysis/force_signals.py` >= 1 (REVIEWS R5)
    - Source: `grep -c "median_torso_length" backend/shared/python/sunity_shared/analysis/force_signals.py` >= 1 (REVIEWS R2)
    - Source: `grep -c "point_to_pole_line_distance_2d\|PoleAxisMeasurement" backend/shared/python/sunity_shared/analysis/force_signals.py` >= 2 (REVIEWS R1)
    - Source: `grep -c "preflight_label_gate_passed" backend/shared/python/sunity_shared/analysis/force_signals.py` >= 2 (REVIEWS R4)
    - Source: `grep -c "measurement_kind" backend/shared/python/sunity_shared/analysis/force_signals.py` >= 1 (REVIEWS R3)
    - Source: `grep -c "distance_to_pole_norm\|near_pole_ratio\|lost_near_pole_at_ms" backend/shared/python/sunity_shared/analysis/force_signals.py` >= 3 (REVIEWS R3)
    - Behavior: import + inspect drift defense PASS (verify command above)
    - Behavior: Plan 08-01 lockstep test PASS after models.py activation
    - Behavior: pytest exit 0
  </acceptance_criteria>
  <done>
    force_signals.py 모듈 신설 (시그너처 + dataclass + 상수 only, 본체 stub). models.py import 활성화. REVIEWS R1/R2/R3/R4/R5 schema 측면 영구 차단 (BodyNormalizationProfile 영구 금지 + jerk FPS 정규화 박제 + preflight gate + contact primitive + nullable evidence). Task 2 가 4 metric 본체 + Task 3 가 umbrella 본체 박제.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 4 metric 함수 본체 (Layer 1 + axis + stability FPS-normalized + contact evidence) + 5 단위 test</name>
  <files>
    backend/shared/python/sunity_shared/analysis/force_signals.py
    backend/tests/phase08/test_compute_phase_boundaries.py
    backend/tests/phase08/test_compute_axis_deviation.py
    backend/tests/phase08/test_compute_stability_metrics.py
    backend/tests/phase08/test_compute_contact_stability.py
    backend/tests/phase08/test_fps_invariance.py
  </files>
  <read_first>
    backend/shared/python/sunity_shared/analysis/force_signals.py (Task 1 박제 stub + 상수 + dataclass)
    backend/shared/python/sunity_shared/analysis/dimensions.py
    backend/shared/python/sunity_shared/analysis/pole_geometry.py
    backend/shared/python/sunity_shared/analysis/body_scale.py
    backend/shared/python/sunity_shared/analysis/temporal.py
    backend/shared/python/sunity_shared/analysis/features.py
    backend/shared/python/sunity_shared/analysis/pose_frame.py
    backend/shared/python/sunity_shared/analysis/skeleton.py
    backend/judging_data/contact_points.yaml
    backend/tests/phase08/conftest.py
    backend/tests/phase08/fixtures/_factory.py
    backend/tests/phase08/fixtures/_fixture_builders.py
    .planning/phases/08-jerk-jitter/08-RESEARCH.md §"5-Phase Motion Split" + §"AxisDeviationMetric Algorithm" + §"StabilityMetric Algorithm" + §"ContactStabilityMetric Algorithm"
  </read_first>
  <behavior>
    - **compute_phase_boundaries() Layer 1 본체** (REVIEWS R4 정합 + REVIEWS Cycle 2 NEW HIGH #2 ceiling helper 박제):
      * 내부 helper `_phase_signals(pose_frames, pole_axis_measurement, body_profile)` — 3 신호 산출. hand_pole_dist 는 line 가용 시 point_to_pole_line_distance_2d 호출, 미가용 시 NaN.
      * 내부 helper `_layer1_heuristic_boundaries(pose_frames, pole_axis_measurement, body_profile, angles)` — phase detection rules.
      * 내부 helper `_should_invoke_layer2(motion_id, gemini_extractor, video_uri) -> bool` — 본 plan 항상 False.
      * 신설 helper `_layer1_confidence_from_preflight(preflight_label_gate_passed: bool | None) -> tuple[MetricConfidence, list[str]]` — preflight gate 3-state 박제 ceiling 박제 source (Plan 08-03 의 Layer 2 분기도 본 helper reuse 박제, REVIEWS Cycle 2 NEW HIGH #2 차단 정합):
        ```python
        def _layer1_confidence_from_preflight(preflight_label_gate_passed: bool | None) -> tuple[str, list[str]]:
            if preflight_label_gate_passed is True:
                return ("medium", [])
            if preflight_label_gate_passed is False:
                return ("low", ["preflight_label_gate_failed"])
            # None — gate 미실행 (default)
            # REVIEWS Cycle 2 §3 MEDIUM 정합: 'preflight_gate_pending' warning 박제 — Plan 08-01 §9.8 enum 박제 정합.
            return ("low", ["preflight_gate_pending"])
        ```
      * 본체 — preflight_label_gate_passed 분기 (REVIEWS R4):
        ```python
        layer1 = _layer1_heuristic_boundaries(...)
        base_confidence, base_warnings = _layer1_confidence_from_preflight(preflight_label_gate_passed)
        source = "heuristic" if motion_id else "heuristic_fallback"
        return [PhaseBoundary(phase=b.phase, ..., confidence=base_confidence, source=source, preflight_label_gate_passed=preflight_label_gate_passed) for b in layer1]
        ```
      * Edge cases: T < 10 → single 'hold' phase + warning 'video_too_short' / all-NaN angles → single 'hold' + warning 'heavy_occlusion'.
    - **compute_axis_deviation() 본체** (REVIEWS R1 + R2 정합):
      * 내부 helper `_observed_torso_length(pose_frames) -> float | None` — body_scale.median_torso_length(pose_frames, space='image_2d') 호출. None 반환 시 caller 가 distance None + warning 'scale_unavailable'.
      * 내부 helper `_pelvis_position_image_2d(frame) -> tuple | None` — left_hip + right_hip midpoint (image 2D). missing keypoint 시 None.
      * 내부 helper `_chest_position_image_2d(frame) -> tuple | None` — left_shoulder + right_shoulder midpoint.
      * 내부 helper `_shoulder_tilt_2d(frame) -> float | None` — image 2D 평면에서 shoulder line vs pole_line direction angle (signed degrees).
      * 내부 helper `_hip_tilt_2d(frame) -> float | None` — same for hip.
      * 내부 helper `_deviation_direction(...) -> DeviationDirection` — priority: distance_delta > y_delta > x_delta.
      * 내부 helper `_severity_from_distance(distance, thresholds)` — module-level thresholds lookup.
      * 본체:
        ```python
        torso_length = _observed_torso_length(pose_frames)
        line = pole_axis_measurement.line
        if line is None:
            coordinate_space = "unavailable"
            scale_denominator = "unavailable"
            warnings_base = ["pole_line_missing"]
            if torso_length is None:
                warnings_base.append("scale_unavailable")
            # 모든 phase 의 metric: distance 4 필드 None + severity='low' + confidence='low' + warnings 박제
            return [AxisDeviationMetric(phase=b.phase, pelvis_distance_from_pole_axis=None, ..., coordinate_space="unavailable", scale_denominator="unavailable", warnings=warnings_base) for b in phase_boundaries]
        if torso_length is None:
            coordinate_space = "image_2d"
            scale_denominator = "unavailable"
            # distance numeric 산출 시도하되 normalization 불가 → None + warning
            warnings_base = ["scale_unavailable"]
            # ...
        else:
            coordinate_space = "image_2d"
            scale_denominator = "observed_torso_length"
            warnings_base = []
        # phase 별 산출:
        for b in phase_boundaries:
            phase_frames = pose_frames[b.start_frame_idx:b.end_frame_idx]
            pelvis_distances = [point_to_pole_line_distance_2d(_pelvis_position_image_2d(f), line) / torso_length for f in phase_frames if _pelvis_position_image_2d(f) is not None]
            # ... median, severity, etc.
        ```
    - **compute_stability_metrics() FPS-normalized 본체** (REVIEWS R5 정합):
      * 내부 helper `_compute_jerk(angles_window, fps) -> float` — REVIEWS R5 정합:
        ```python
        if angles_window.shape[0] < 5:
            return 0.0
        dt = 1.0 / fps
        # 3차 미분: deg/frame^3 → deg/sec^3 (FPS-normalized)
        raw_jerk_per_frame = np.abs(np.diff(angles_window, n=3, axis=0))
        jerk_per_sec_cubed = raw_jerk_per_frame / (dt ** 3)
        # MAD outlier rejection
        median = np.nanmedian(jerk_per_sec_cubed, axis=0)
        mad = np.nanmedian(np.abs(jerk_per_sec_cubed - median), axis=0)
        threshold = median + JERK_MAD_K * mad
        filtered = np.where(jerk_per_sec_cubed > threshold, np.nan, jerk_per_sec_cubed)
        return float(np.nanmedian(filtered))
        ```
      * 내부 helper `_compute_unstable_body_parts(angles_window, profile=None) -> list[str]` — dimensions.stability_wobble_by_joint reuse + threshold 12.0 deg filter.
      * 본체: 각 PhaseBoundary 별로 angles slice:
        - jitter_score = `dimensions.stability_wobble(sliced, profile=None)` (deg/frame, drift defense)
        - jerk_score = `_compute_jerk(sliced, fps)` (deg/sec^3, FPS-normalized)
        - jerk_unit = "deg_per_sec_cubed" (default)
        - hold_stability_score = `dimensions.stability_wobble(sliced, profile=None)` if phase.phase == 'hold' else None
        - unstable_body_parts = `_compute_unstable_body_parts(sliced)`
        - severity, confidence, warnings 박제
    - **compute_contact_stability() evidence-with-confidence 본체** (REVIEWS R3 정합):
      * 내부 helper `_load_expected_contact_points(motion_id) -> list[dict]` — yaml.safe_load + cache + motion_id None / 미인식 시 [].
      * 내부 helper `_contact_point_position(contact_point, kind, frame) -> tuple | None` — kind 별 lookup:
        - kind='keypoint': _CONTACT_POINT_TO_KEYPOINTS dict 호출
        - kind='segment': _CONTACT_POINT_TO_SEGMENT dict (left_inner_thigh = midpoint of left_hip+left_knee)
        - kind='region_proxy': _CONTACT_POINT_TO_REGION dict (hip = midpoint of left_hip+right_hip)
      * 내부 helper `_detect_contact_evidence(contact_point, kind, pose_frames, phase_window, line, torso_length) -> tuple[bool | None, float | None, float | None, int | None]`:
        - line None 또는 torso_length None → 모두 None (evidence 불가)
        - 각 frame 의 distance = point_to_pole_line_distance_2d(_contact_point_position(...), line) / torso_length
        - distances 의 median = distance_to_pole_norm
        - near_pole_ratio = (distances <= 0.08).mean()
        - estimated_stable: near_pole_ratio >= 0.8 + valid_frames >= 2 → True / near_pole_ratio < 0.3 → False / 그 외 → None
        - lost_near_pole_at_ms: distance > 0.20 첫 timestamp (debounce 2 frames) 또는 None
      * 내부 helper `_detect_abnormal_release(metric, phase_boundaries) -> str | None` — lock.start_ms <= lost_near_pole_at_ms <= hold.end_ms → 'abnormal_release_during_hold'.
      * 본체:
        ```python
        torso_length = body_scale.median_torso_length(pose_frames, space='image_2d')
        line = pole_axis_measurement.line
        expected = _load_expected_contact_points(motion_id)
        if not expected:
            # motion_id 미인식 fallback (REVIEWS R3)
            fallback_points = [{'name': p, 'kind': 'keypoint'} for p in ['left_hand', 'right_hand', 'left_foot', 'right_foot']]
            return [ContactStabilityMetric(phase=b.phase, contact_point=e['name'], measurement_kind=None, estimated_stable=None, distance_to_pole_norm=None, near_pole_ratio=None, lost_near_pole_at_ms=None, coordinate_space="unavailable", severity='low', confidence='low', warnings=['motion_unrecognized', 'contact_evidence_only']) for b in phase_boundaries for e in fallback_points]
        # normal path
        results = []
        for b in phase_boundaries:
            for entry in expected:
                stable, dist_norm, ratio, lost_at = _detect_contact_evidence(entry['name'], entry['kind'], pose_frames, (b.start_frame_idx, b.end_frame_idx), line, torso_length)
                coordinate_space = "image_2d" if line is not None else "unavailable"
                warnings_list = []
                if line is None: warnings_list.append("pole_line_missing")
                if torso_length is None: warnings_list.append("scale_unavailable")
                if stable is None: warnings_list.append("contact_evidence_only")
                metric = ContactStabilityMetric(phase=b.phase, contact_point=entry['name'], measurement_kind=entry['kind'], estimated_stable=stable, distance_to_pole_norm=dist_norm, near_pole_ratio=ratio, lost_near_pole_at_ms=lost_at, coordinate_space=coordinate_space, severity=..., confidence=..., warnings=warnings_list)
                abnormal = _detect_abnormal_release(metric, phase_boundaries)
                if abnormal: metric.warnings.append(abnormal)  # frozen dataclass 박제 시 dataclasses.replace 사용
                results.append(metric)
        return results
        ```
    - 4 단위 test 파일 본 task scope:
      * test_compute_phase_boundaries.py 박제 — Layer 1 default confidence='low' (gate None / False), gate=True 시 'medium' 승급, fixture 활용 (build_clean_invert)
      * test_compute_axis_deviation.py 박제 — point_to_pole_line_distance_2d 사용 검증, median_torso_length 사용 검증, torso_scale 영구 사용 금지 검증, line/scale 미가용 시 distance None + warning 박제
      * test_compute_stability_metrics.py 박제 — drift defense (jitter == dimensions.stability_wobble abs<1e-9) + jerk FPS-normalized + jerk_unit='deg_per_sec_cubed'
      * test_compute_contact_stability.py 박제 — yaml load kind 필드 동행 검증, distance_to_pole_norm, near_pole_ratio, lost_near_pole_at_ms, estimated_stable nullable evidence, measurement_kind, abnormal release, motion_unrecognized fallback
    - **test_fps_invariance.py 신설** (REVIEWS R5 정합) 3 test:
      * test_jerk_score_invariant_under_fps_change — programmatic factory 박제: 9 fps motion (T=60) → 18 fps upsample (T=120, linear interpolation) → _compute_jerk(9fps_angles, 9.0) ≈ _compute_jerk(18fps_angles, 18.0) (rel tolerance 5%)
      * test_jerk_unit_remains_deg_per_sec_cubed_at_both_fps — 9 fps + 18 fps 모두 StabilityMetric.jerk_unit == 'deg_per_sec_cubed' 박제 검증
      * test_jitter_score_is_frame_rate_dependent_documented — docstring 안 "jitter_score is frame-rate dependent" 박제 검증 (test 가 docstring grep 검증, 박제 메모 강제)
  </behavior>
  <action>
    Task 1 의 4 stub 함수 본체를 위 behavior 박제. 25+ private helper 박제.

    Critical drift defense — compute_stability_metrics 의 jitter_score 산출 = `dimensions.stability_wobble(sliced)` 직접 호출 (산식 복제 영구 금지).

    Critical FPS 정규화 — _compute_jerk(angles, fps) 가 dt=1/fps 정규화 강제. test_fps_invariance 가 9 fps vs 18 fps 동일 jerk_score 검증.

    Critical Layer 2 hook — compute_phase_boundaries 안 `if _should_invoke_layer2(motion_id, gemini_extractor, video_uri):` 분기는 박제하되 본 plan 의 본체 = `# Plan 08-03 가 Layer 2 활성화` 주석 + Layer 1 단독 반환.

    Critical contact evidence — estimated_stable 가 nullable. measurement_kind 가 nullable (motion_id 미인식 시 None).

    Edge case 박제:
      - T < 10 frames: single hold + warning 'video_too_short'.
      - all-NaN angles: single hold + warning 'heavy_occlusion'.
      - line None: 모든 axis distance None + warning 'pole_line_missing'.
      - torso_length None: distance numeric 산출 시도해도 normalization 불가 → None + warning 'scale_unavailable'.

    4 단위 test + 1 FPS invariance test 박제 — programmatic fixture builder (Plan 08-01 박제) 활용.

    per REVIEWS R1 + R2 + R3 + R4 + R5.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && PYTHONPATH=backend/shared/python:. backend/.venv/bin/python3 -m pytest backend/tests/phase08/test_compute_phase_boundaries.py backend/tests/phase08/test_compute_axis_deviation.py backend/tests/phase08/test_compute_stability_metrics.py backend/tests/phase08/test_compute_contact_stability.py backend/tests/phase08/test_fps_invariance.py -x</automated>
  </verify>
  <acceptance_criteria>
    - File modified: force_signals.py 의 4 metric 함수 본체가 NotImplementedError 가 아닌 실제 구현
    - Source: `grep -c "from .dimensions import stability_wobble\|from . import dimensions" backend/shared/python/sunity_shared/analysis/force_signals.py` >= 1
    - Source: `grep -c "yaml.safe_load" backend/shared/python/sunity_shared/analysis/force_signals.py` >= 1
    - Source: `grep -c "median_torso_length" backend/shared/python/sunity_shared/analysis/force_signals.py` >= 2
    - Source: `grep -c "point_to_pole_line_distance_2d" backend/shared/python/sunity_shared/analysis/force_signals.py` >= 2
    - Source: `grep -c "def _" backend/shared/python/sunity_shared/analysis/force_signals.py` >= 20
    - Behavior (drift defense): test_drift_defense PASS (jitter_score == dimensions.stability_wobble abs<1e-9)
    - Behavior (FPS invariance): test_jerk_score_invariant_under_fps_change PASS (9 fps vs 18 fps rel tolerance 5%)
    - Behavior (Layer 1 default low): test_layer1_default_confidence_low_without_preflight_gate PASS
    - Behavior (preflight gate True → medium): test_layer1_confidence_medium_when_preflight_gate_passed_true PASS
    - Behavior (axis line null): test_line_missing_returns_null_distance_with_warning PASS
    - Behavior (axis torso_scale ban): test_torso_scale_not_used_as_denominator PASS
    - Behavior (contact evidence nullable): test_estimated_stable_null_when_evidence_unclear PASS
    - Behavior (motion_id fallback): test_motion_unrecognized_fallback_estimated_stable_null PASS
    - Behavior (abnormal release): test_layback_release_fixture_emits_abnormal_warning PASS
    - Behavior: 5 test 파일 모두 PASS (pytest exit 0)
  </acceptance_criteria>
  <done>
    force_signals.py 의 4 metric 함수 본체 박제 완료 (REVIEWS R1/R2/R3/R4/R5 모두 반영). drift defense + FPS invariance + Layer 1 confidence default low + axis distance Plan 08-00 contract 사용 + contact evidence nullable. Task 3 가 compute_force_signals umbrella + temporal_fill 중복 차단 본체.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: compute_force_signals umbrella 본체 (temporal_fill 중복 차단) + 통합 test</name>
  <files>
    backend/shared/python/sunity_shared/analysis/force_signals.py
    backend/tests/phase08/test_compute_force_signals.py
  </files>
  <read_first>
    backend/shared/python/sunity_shared/analysis/force_signals.py (Task 1 + Task 2 박제 본체)
    backend/shared/python/sunity_shared/analysis/temporal.py (temporal_fill 시그너처)
    backend/tests/phase08/conftest.py
    backend/tests/phase08/fixtures/_fixture_builders.py
    .planning/phases/08-jerk-jitter/08-REVIEWS.md §"Agreed Concerns" §5 (double smoothing)
  </read_first>
  <behavior>
    - compute_force_signals() umbrella 본체 박제 — REVIEWS R5 정합 (temporal_fill 중복 차단):
      * Step 1: angles 인자 검증 — None 시 `raise ValueError("compute_force_signals: angles 인자는 caller 책임 (pipeline._extract_video_analysis_inputs 가 1회 temporal_fill 적용 후 전달). REVIEWS R5 double smoothing 차단.")`. None 영구 금지.
      * Step 2: compute_phase_boundaries 호출 (preflight_label_gate_passed 인자 전달)
      * Step 3: compute_axis_deviation + compute_stability_metrics + compute_contact_stability 호출
      * Step 4: _overall_confidence(phase_boundaries, axis, stability, contact) -> MetricConfidence (min propagation: low > medium > high)
      * Step 5: warnings 조립 — motion_id None → 'motion_unrecognized_layer1_only' / gemini_extractor None → 'layer2_unavailable' / preflight gate None → 'preflight_gate_pending' / preflight gate False → 'preflight_label_gate_failed' / 모든 axis_metric coordinate_space='unavailable' → 'coordinate_space_unavailable' / 'fps_normalization_applied' (jerk FPS 정규화 박제 메모 — REVIEWS R5)
      * Step 6: ForceSignalsReport 조립 + return
    - _overall_confidence 본체 박제 — 모든 metric 의 confidence + phase_boundaries 의 confidence 수집 → low > medium > high min 반환.
    - 8 통합 test (test_compute_force_signals.py):
      * test_compute_force_signals_e2e_clean_invert — build_clean_invert() 입력 + angles + fps=9.0 → ForceSignalsReport(phase_boundaries len==5, 모든 metric list non-empty, severity 대부분 'low').
      * test_compute_force_signals_does_not_call_temporal_fill — mock temporal.temporal_fill (unittest.mock.patch) → compute_force_signals 호출 시 temporal_fill 호출 0회 검증 (REVIEWS R5 정합).
      * test_compute_force_signals_requires_angles_not_none — angles=None 호출 시 ValueError raise 검증 (REVIEWS R5).
      * test_confidence_weighting_emits_warning_on_high_low_reliability — build_occluded_lock() → lock phase metric 의 warnings contains 'occlusion_high_in_phase'.
      * test_overall_confidence_min_propagation — 1 metric confidence='low' → overall='low'. 모든 'medium' → 'medium'. 모든 'high' → 'high'.
      * test_motion_unrecognized_emits_warning — motion_id=None 호출 → warnings contains 'motion_unrecognized_layer1_only'.
      * test_layer2_unavailable_emits_warning — gemini_extractor=None 호출 → warnings contains 'layer2_unavailable'.
      * test_warning_fps_normalization_applied_emitted — fps=9.0 정상 호출 시 warnings contains 'fps_normalization_applied' (박제 메모, REVIEWS R5).
  </behavior>
  <action>
    Task 2 의 force_signals.py 본체에 compute_force_signals umbrella + _overall_confidence helper 박제.

    Critical REVIEWS R5 — temporal_fill 호출 영구 차단. angles 인자 필수 (None ValueError). test_compute_force_signals_does_not_call_temporal_fill 가 unittest.mock.patch 로 temporal_fill mock + call_count == 0 검증.

    Step 5 warnings 조립 — pole_line_missing / scale_unavailable / preflight_label_gate_failed / fps_normalization_applied / contact_evidence_only / coordinate_space_unavailable 6 신설 warning 박제 strategy. metric 별 warning 은 각 metric.warnings 에 박제 (이미 Task 2 박제) + top-level ForceSignalsReport.warnings 박제 = (a) motion_id None / Layer 2 unavailable / preflight gate 미실행 / (b) 모든 metric 이 'unavailable' coordinate_space 일 때 top-level summary warning.

    8 통합 test 박제 — Plan 08-01 박제 6 fixture builder 활용.

    per REVIEWS R5 + R4 + Plan 08-01 박제 schema.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && PYTHONPATH=backend/shared/python:. backend/.venv/bin/python3 -m pytest backend/tests/phase08/ -x</automated>
  </verify>
  <acceptance_criteria>
    - File modified: force_signals.py 의 compute_force_signals umbrella + _overall_confidence helper 본체 박제
    - Source: `grep -c "temporal.temporal_fill\|temporal_fill" backend/shared/python/sunity_shared/analysis/force_signals.py` == 0 (REVIEWS R5 — temporal_fill 호출 영구 차단)
    - Source: `grep -c "def _overall_confidence" backend/shared/python/sunity_shared/analysis/force_signals.py` >= 1
    - Source: `grep -c "fps_normalization_applied" backend/shared/python/sunity_shared/analysis/force_signals.py` >= 1
    - Behavior: test_compute_force_signals_does_not_call_temporal_fill PASS (temporal_fill call_count == 0)
    - Behavior: test_compute_force_signals_requires_angles_not_none PASS (ValueError raise)
    - Behavior (E2E): test_compute_force_signals_e2e_clean_invert PASS
    - Behavior (warnings): test_warning_fps_normalization_applied_emitted + test_motion_unrecognized_emits_warning + test_layer2_unavailable_emits_warning PASS
    - Behavior (full suite): `pytest backend/tests/phase08/ -x` exit 0
    - Behavior (regression): `pytest backend/tests/phase06/ backend/tests/phase07/ backend/tests/pipeline/ -x` exit 0
  </acceptance_criteria>
  <done>
    compute_force_signals umbrella 본체 박제 완료. temporal_fill 중복 호출 영구 차단 (REVIEWS R5). angles caller responsibility. overall_confidence min propagation. warnings emission (6 신설 enum). 8 통합 test PASS + phase06/07/pipeline 회귀 0. Plan 08-03 가 Layer 2 Gemini wiring + pipeline 통합 + Firestore + frontend 박제.
  </done>
</task>

</tasks>

<verification>
## Phase 8 Plan 02 revised Verification

**Goal-backward derived truths (REVIEWS Cycle 1 compute 측면 반영):**

- force_signals.py 신설 — 5 dataclass + 5 type alias + 5 public 함수 + 20+ private helper
- BodyNormalizationProfile import 영구 차단 (REVIEWS R2)
- jerk_score = deg/sec^3 (FPS-normalized, REVIEWS R5)
- temporal_fill 호출 영구 차단 (REVIEWS R5 — angles caller responsibility)
- Layer 1 confidence default = 'low' (preflight gate PASS 전, REVIEWS R4)
- compute_axis_deviation: Plan 08-00 박제 point_to_pole_line_distance_2d + median_torso_length 사용 (REVIEWS R1/R2)
- compute_contact_stability: estimated_stable nullable + measurement_kind + distance_to_pole_norm + near_pole_ratio + lost_near_pole_at_ms (REVIEWS R3)
- compute_phase_boundaries: preflight_label_gate_passed 인자 (REVIEWS R4)
- drift defense test: jitter_score == dimensions.stability_wobble (abs<1e-9)
- FPS invariance test: 9 fps vs 18 fps jerk_score 동일 (rel tolerance 5%, REVIEWS R5)
- models.py active import — Plan 08-01 의 3-way lockstep 활성화 완성

**Manual verification (belle 검수 박제 대기 — Plan 08-00 박제 preflight gate):**

- Layer 1 휴리스틱 25-timestamp belle labeling (5영상 × 5 phase) — `.planning/phases/08-jerk-jitter/preflight/preflight_label_template.csv` 박제. ≥80% 일치 시 preflight_label_gate_passed=True 박제 강제 → Layer 1 confidence='medium' 승급.
- severity 임계 분포 sanity check (정은지 영상 90%+ 'low' 박제 — sanity check only, NOT calibration ground truth, REVIEWS Suggestions §2 정합)

**Regression checks:**

- `pytest backend/tests/phase06/` PASS (156 test)
- `pytest backend/tests/phase07/` PASS (108 test)
- `pytest backend/tests/pipeline/` PASS
- `pytest backend/tests/phase08/` PASS (Plan 08-00 11 test + Plan 08-01 신설 test + Plan 08-02 30+ test)
- `tsc --noEmit clean`
</verification>

<success_criteria>
- [x] force_signals.py 5 dataclass + 5 type alias + 5 public 함수 + 20+ private helper 박제
- [x] BodyNormalizationProfile import 영구 차단 (REVIEWS R2 drift defense)
- [x] jerk FPS-normalized (deg/sec^3) + jerk_unit 박제 (REVIEWS R5)
- [x] temporal_fill 중복 호출 차단 + angles caller responsibility (REVIEWS R5)
- [x] Layer 1 confidence default 'low' + preflight_label_gate_passed 인자 (REVIEWS R4)
- [x] axis distance: point_to_pole_line_distance_2d + median_torso_length 사용 (REVIEWS R1/R2)
- [x] contact: estimated_stable nullable + measurement_kind + nearPoleRatio + lostNearPoleAtMs (REVIEWS R3)
- [x] drift defense + FPS invariance test PASS
- [x] models.py active import (Plan 08-01 lockstep 활성화 완성)
- [x] 30+ phase08 test PASS + phase06/07/pipeline 회귀 0
</success_criteria>

<output>
Create `.planning/phases/08-jerk-jitter/08-02-SUMMARY.md` when done. Include:
- 3 task commit SHA + 각 task scope
- 5 public 함수 + 20+ private helper 박제 결과
- drift defense + FPS invariance test 결과
- BodyNormalizationProfile 영구 차단 + temporal_fill 중복 차단 검증
- Layer 1 preflight_label_gate_passed 분기 박제 결과
- 30+ phase08 test PASS + phase06/07/pipeline 회귀 0 확인
- belle 검수 manual gate 박제 (preflight 25-timestamp + severity 분포 sanity check)
- Plan 08-03 진입 시그널 (pipeline wiring + Layer 2 Gemini moment reuse)
- Deviation (Rule 1 fix / Rule 3 environment fix 등) 있으면 박제
</output>
