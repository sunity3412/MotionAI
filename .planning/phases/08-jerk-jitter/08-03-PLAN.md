---
phase: 08-jerk-jitter
plan: 03
type: execute
wave: 3
depends_on: [08-00, 08-01, 08-02]
files_modified:
  - backend/shared/python/sunity_shared/analysis/force_signals.py
  - backend/shared/python/sunity_shared/analysis/gemini_technique_recognizer.py
  - backend/shared/python/sunity_shared/judging/gemini_moment_extractor.py
  - backend/shared/python/sunity_shared/analysis/technique.py
  - backend/shared/python/sunity_shared/firestore_admin.py
  - backend/functions/pipeline/app.py
  - app/src/lib/userAnalyses.ts
  - backend/runpod_inference/requirements.txt
  - backend/tests/phase08/test_compute_phase_boundaries.py
  - backend/tests/phase08/test_firestore_lockstep.py
  - backend/tests/phase08/test_compute_force_signals.py
  - backend/tests/phase08/test_gemini_model_env_driven.py
  - backend/tests/pipeline/test_pipeline_phase8.py
autonomous: false
requirements: [FORCE-01]
subsystem: backend-pipeline-frontend
tags: [phase-08, layer2-recognizer-reuse, pipeline-wiring, firestore, frontend-normalize, sam-smoke, reviews-cycle-2-revised]

scope_sanity_note: "REVIEWS Cycle 2 (cycle: 2, codex) 반영 revised. 4 unresolved HIGH 영구 차단:\n  · R4 carryover (preflight gate plumbing): pipeline/app.py 의 _preflight_label_gate_passed() env helper 신설 (`PREFLIGHT_LABEL_GATE_PASSED` env). compute_force_signals 호출 시 본 helper 결과 주입 — belle 운영 작업 시 env 박제만으로 gate flip 박제 (코드 변경 0).\n  · R8 carryover (Gemini model default + scope): 실 default 위치 = `judging/gemini_moment_extractor.py` (recognizer.py 가 아님 — Codex Cycle 2 R8 박제). 본 plan 에서 `DEFAULT_GEMINI_MODEL = os.environ.get('GEMINI_MODEL', 'gemini-2.5-flash')` 박제 (gemini-2.0-flash-exp 2026-06-01 EOL 박제, Google docs 정합). recognizer.py 도 동일 GEMINI_MODEL env 박제.\n  · NEW Layer-2 except 분기 confidence promotion 차단: compute_phase_boundaries 의 Layer 2 except 분기는 `_layer1_confidence_from_preflight(preflight_label_gate_passed)` 박제 — 'medium' hardcoded 영구 제거. preflight gate ceiling 위로 promote X.\n  · NEW Firestore invariant: `_validate_dict_only_scalars` 본체 변경 영구 차단 (project-wide 영향 영구 0). 대신 force_signals 전용 scoped validator `_validate_force_signals_report()` 신설 — `forceSignalsReport` 필드에만 list[scalar] inside list[dict] 허용. 다른 필드는 strict 유지 ([[firestore-nested-array-flat]] 영구 보존).\n\n  depends_on 신설: [08-00, 08-01, 08-02]. Layer 2 = 기존 GeminiTechniqueRecognizer / TechniqueCache 박제 moments reuse (REVIEWS R6 정합 — 신규 GeminiMomentExtractor singleton 제거). FORCE_SIGNALS_LAYER2_ENABLED 별도 env flag 박제 (REVIEWS R7 — Phase 5 RECOGNIZER_BACKEND 와 분리). _VideoAnalysisInputs 에 pole_axis_measurement 신설 (REVIEWS R10 — pipeline 의 vertical fallback 한계 노출). _gemini_enabled() helper 재사용 (REVIEWS R6 helper drift 차단). 10 파일 + manual checkpoint 1."

must_haves:
  truths:
    - "Layer 2 = GeminiTechniqueRecognizer 박제 moments reuse (REVIEWS R6 정합) — 신규 GeminiMomentExtractor singleton 제거 (중복 Gemini call 영구 차단). TechniqueProfile.key_moments 필드 신설 (Task 1 박제 결정). **REVIEWS Cycle 2 §3 MEDIUM 정합 — TechniqueCache round-trip 박제 보장**: TechniqueCache 의 cache-hit reconstruction 시 `key_moments` 도 round-trip 박제 — 캐시 hit 시 None 으로 박제되어 Layer 2 가 silently 비활성되는 박제 영구 차단. Plan 5 박제 TechniqueCache 의 serialize/deserialize 박제 path 박제 시 key_moments tuple ↔ list[dict] 박제 정합."
    - "force_signals.compute_phase_boundaries() 의 Layer 2 본체 활성 — `_should_invoke_layer2(motion_id, technique_profile, video_uri) -> bool` 시그너처 변경 (gemini_extractor 인자 제거). technique_profile.key_moments != None 시 True (reuse). _layer2_boundaries_from_technique_profile(layer1, technique_profile.key_moments, fps, frames_total) -> list[PhaseBoundary] 신설 — moments 직접 활용. graceful fallback 박제 (technique_profile.key_moments None / 이상 시 Layer 1 단독 + warning 'layer2_unavailable')."
    - "FORCE_SIGNALS_LAYER2_ENABLED 별도 env flag 박제 (REVIEWS R7 정합) — Phase 5 의 RECOGNIZER_BACKEND='gemini' 와 분리. Phase 8 Layer 2 활성 조건 = (FORCE_SIGNALS_LAYER2_ENABLED truthy) AND (technique_profile.key_moments != None). RECOGNIZER_BACKEND='gemini' 시도 FORCE_SIGNALS_LAYER2_ENABLED unset 이면 Phase 8 Layer 2 = OFF (default off 박제, REVIEWS R7)."
    - "Gemini model name = env/config 박제 (REVIEWS Cycle 2 R8 정합 — Cycle 1 미해소 carryover 박제 차단) — 실 default 위치 = `backend/shared/python/sunity_shared/judging/gemini_moment_extractor.py:48` (Codex Cycle 2 R8 박제 — Cycle 1 plan 이 recognizer.py 에만 박제하여 path 누락). 본 plan 변경 박제: (a) `gemini_moment_extractor.py` 의 `DEFAULT_GEMINI_MODEL = 'gemini-3.1-pro-preview'` hardcoded 박제 제거 → `DEFAULT_GEMINI_MODEL = os.environ.get('GEMINI_MODEL', 'gemini-2.5-flash')` 박제. (b) `gemini_technique_recognizer.py` 도 동일 `GEMINI_MODEL` env 박제 (cycle 1 박제 유지). (c) Default 'gemini-2.5-flash' 박제 근거 — Codex Cycle 2 R8 박제: 'gemini-2.0-flash-exp' 가 2026-06-01 EOL 박제. 'gemini-2.5-flash' 가 현재 stable Flash family. Gemini 3.x Pro 가용 시 belle 가 env 박제 변경 (코드 변경 0). (d) startup 시 models.list 호출 X (성능 + cold-start 박제 차단) — invalid 시 첫 Gemini call 의 RuntimeError 가 graceful fallback (force_signals.compute_phase_boundaries 의 except 분기 박제)."
    - "_VideoAnalysisInputs 에 pole_axis_measurement 필드 신설 (REVIEWS R10 정합) — 기존 NamedTuple 5 필드 (angles / student_profile / pose_frames / local_video_path 4 + 신설 pole_axis_measurement). _extract_video_analysis_inputs() 본체 변경: vertical fallback PoleAxis 박제 + Plan 08-00 박제 build_pole_axis_measurement(axis_3d=vertical_pole, line=None, frame_index=None) 호출 → PoleAxisMeasurement(coordinate_space='unavailable') 박제. 추후 (별도 plan) HoughPoleDetector 활성화 시 line 박제 — 본 plan = vertical fallback default 박제 박제 (분석 죽지 않음, Plan 08-02 의 axis distance None 분기 활성)."
    - "Firestore flat-array invariant 박제 영구 보존 (REVIEWS Cycle 2 §3 NEW HIGH 정합 — Cycle 1 의 `_validate_dict_only_scalars` 본체 변경 영구 차단). `_validate_dict_only_scalars` 본체 변경 X — project-wide 영향 0. [[firestore-nested-array-flat]] 메모 영구 보존. 대신 force_signals 전용 **scoped validator** 신설: `_validate_force_signals_report(payload: dict) -> None` — `forceSignalsReport` 필드 안 metric 의 `warnings: list[str]` / `unstableBodyParts: list[str]` 만 list[scalar] inside list[dict] 박제 허용. 다른 path (bodyComparisonReport 등) 는 기존 `_validate_dict_only_scalars` strict 유지. complete_analysis 호출 시 `force_signals_report` kwarg 가 전달되면 본 scoped validator 만 호출 (다른 validator 와 분리). per Cycle 2 reviewer 의 'scoped relaxation' (option b) 박제."
    - "firestore_admin.complete_analysis(force_signals_report: dict | None = None) kwarg 신설 — Phase 6 박제 body_comparison_report 패턴 정합. `_validate_force_signals_report(force_signals_report)` scoped validator 통과 강제 (REVIEWS Cycle 2 §3 NEW HIGH 정합). result.forceSignalsReport 위치에 저장. body_comparison_report 박제 경로는 기존 `_validate_flat_dict_no_nested_array` 유지 (다른 validator 박제 영향 0)."
    - "pipeline/app.py::_process Phase 8 wiring — compare_body_profiles 호출 직후 force_signals.compute_force_signals 호출 + force_signals_dict 변환 + complete_analysis 의 force_signals_report kwarg 주입. mode 분기 무관 (mode1 / mode3_first / mode3_progress 모두). angles 인자 = _extract_video_analysis_inputs 의 angles (이미 temporal_fill 적용, REVIEWS R5 정합 — double smoothing 차단). pole_axis_measurement 인자 = _extract_video_analysis_inputs 의 신설 필드 (REVIEWS R10). technique_profile 인자 = compare_body_profiles 호출 시점에 이미 박제됨 — reuse."
    - "_get_force_signals_layer2_recognizer() singleton helper 박제 — FORCE_SIGNALS_LAYER2_ENABLED env truthy 시만 lazy 활성. Phase 5 박제 GeminiTechniqueRecognizer 직접 reuse (신규 instance 영구 차단, REVIEWS R6). 본 helper 는 recognizer.last_key_moments cache 박제 활용."
    - "Preflight gate env plumbing 박제 (REVIEWS Cycle 2 NEW HIGH #1 / R4 carryover 정합) — pipeline/app.py 모듈 top-level 에 `_preflight_label_gate_passed() -> bool | None` helper 신설: `os.environ.get('PREFLIGHT_LABEL_GATE_PASSED', '').strip().lower()` 값 박제 — '1'/'true'/'on'/'yes' → True, '0'/'false'/'off'/'no' → False, '' (unset) → None. `_process` 안 compute_force_signals 호출 시 `preflight_label_gate_passed=_preflight_label_gate_passed()` 박제 (Cycle 1 의 `preflight_label_gate_passed=None` hardcoded 박제 영구 제거). belle 운영 작업: 25-timestamp PASS 후 `aws lambda update-function-configuration` 또는 RunPod env 박제 `PREFLIGHT_LABEL_GATE_PASSED=1` 박제만으로 Layer 1 confidence='medium' 승급 박제 — 코드 변경 0. Plan 08-00 박제 preflight_label_template.csv + Plan 08-03 Task 3 manual checkpoint 박제 정합."
    - "_extract_video_analysis_inputs(keep_local_video=_gemini_enabled())` 호출 — 기존 helper 재사용 (REVIEWS R6 — helper drift 차단). _should_keep_local_video() 신규 박제 X (`_gemini_enabled()` 가 이미 RECOGNIZER_BACKEND + GEMINI_RECOGNIZER_ENABLED 양쪽 박제, helper drift 차단)."
    - "app/src/lib/userAnalyses.ts::normalize() 의 result spread 안 forceSignalsReport 필드 null-guard — Phase 7 WR-02 B1 패턴 정합 (immutable spread + ?? null fallback). TS interface 는 optional 유지 (Plan 08-01 박제), normalize() 가 compat layer."
    - "backend/runpod_inference/requirements.txt 검토 — google-genai SDK 이미 박제됨 (Phase 5 박제 정합). 변경 0 가능성."
    - "pipeline 통합 test (test_pipeline_phase8.py) — _process mock E2E (Layer 2 ON path + Layer 2 OFF path 둘 다). FORCE_SIGNALS_LAYER2_ENABLED env 활성/비활성 + RECOGNIZER_BACKEND 활성/비활성 4 combinations 검증."
    - "SAM build smoke (manual checkpoint) — cd backend && sam validate exit 0 + sam build --use-container exit 0. RunPod Pod 갱신 + preflight 25-timestamp label gate 운영 작업 (manual gate, Plan 08-00 박제 preflight + 08-VALIDATION.md 정합)."
  artifacts:
    - path: backend/shared/python/sunity_shared/analysis/force_signals.py
      provides: "Layer 2 본체 활성 (recognizer moments reuse, REVIEWS R6) — _layer2_boundaries_from_technique_profile + _map_moments_to_5phase + _confidence_from_agreement + _promote_layer1 helper. compute_phase_boundaries 의 Layer 2 분기 활성화 + technique_profile 인자 사용 (gemini_extractor 인자 제거). graceful fallback (technique_profile.key_moments None / 이상 시 Layer 1 + warning 'layer2_unavailable'). FORCE_SIGNALS_LAYER2_ENABLED env check."
      contains:
        - "_layer2_boundaries_from_technique_profile"
        - "_map_moments_to_5phase"
        - "_confidence_from_agreement"
        - "_promote_layer1"
        - "FORCE_SIGNALS_LAYER2_ENABLED"
        - "technique_profile"
        - "key_moments"
        - "layer2_call_failed"
        - "layer_disagreement_minor"
        - "layer_disagreement_major"
        - "gemini_assisted"
    - path: backend/shared/python/sunity_shared/analysis/gemini_technique_recognizer.py
      provides: "Gemini model env wiring 박제 (REVIEWS Cycle 2 R8 carryover 정합 — recognizer 측 박제). `GEMINI_MODEL` env 박제 reuse (실 default 위치 = judging/gemini_moment_extractor.py:48 — Codex Cycle 2 R8 박제). TechniqueProfile.key_moments 박제 (Phase 8 Layer 2 reuse source). recognizer 가 GeminiMomentExtractor 박제 시 model_name 인자 박제 시 env 박제 우선."
      contains:
        - "GEMINI_MODEL"
        - "os.environ.get"
        - "key_moments"
    - path: backend/shared/python/sunity_shared/judging/gemini_moment_extractor.py
      provides: "**REVIEWS Cycle 2 R8 NEW HIGH 차단** — 실 default `DEFAULT_GEMINI_MODEL = 'gemini-3.1-pro-preview'` 박제 (line 48) 가 본 파일 박제. 본 plan 박제 변경: `DEFAULT_GEMINI_MODEL = os.environ.get('GEMINI_MODEL', 'gemini-2.5-flash')` 박제 — 'gemini-2.0-flash-exp' 2026-06-01 EOL 박제 (Codex Cycle 2 R8 박제). 'gemini-2.5-flash' 박제 = 현재 stable Flash family. belle env 박제 변경만으로 Gemini 3.x Pro 박제 가능 (코드 변경 0). file header 의 D-13 박제 메모 갱신 박제 (model env-driven 박제 noted)."
      contains:
        - "GEMINI_MODEL"
        - "os.environ.get"
        - "DEFAULT_GEMINI_MODEL"
        - "gemini-2.5-flash"
    - path: backend/shared/python/sunity_shared/analysis/technique.py
      provides: "TechniqueProfile 확장 — key_moments: list[KeyMoment] | None = None 신설 필드. Phase 8 Layer 2 가 본 필드 reuse (recognizer 중복 호출 영구 차단, REVIEWS R6)."
      contains:
        - "key_moments"
        - "KeyMoment"
        - "list[KeyMoment] | None"
    - path: backend/shared/python/sunity_shared/firestore_admin.py
      provides: "**REVIEWS Cycle 2 §3 NEW HIGH 차단** — `_validate_dict_only_scalars` 본체 변경 영구 0 ([[firestore-nested-array-flat]] 박제 보존). 대신 force_signals 전용 **scoped validator** `_validate_force_signals_report(payload: dict) -> None` 신설 — `forceSignalsReport` 필드 안 metric 의 `warnings: list[str]` / `unstableBodyParts: list[str]` 만 list[scalar] inside list[dict] 박제 허용 (scoped exception, 다른 path strict 유지). complete_analysis(force_signals_report: dict | None = None) kwarg 신설 — 본 kwarg 전달 시 `_validate_force_signals_report` 만 호출, `_validate_dict_only_scalars` 박제 X. result.forceSignalsReport 위치 저장. 기존 phase06 156 test 회귀 0 (validator 본체 박제 영향 0)."
      contains:
        - "force_signals_report: dict | None = None"
        - "_validate_force_signals_report"
        - "forceSignalsReport"
        - "_validate_dict_only_scalars"
    - path: backend/functions/pipeline/app.py
      provides: "Phase 8 wiring — compare_body_profiles 직후 force_signals.compute_force_signals 호출 + complete_analysis kwarg 주입. _get_force_signals_layer2_recognizer singleton (FORCE_SIGNALS_LAYER2_ENABLED 별도 env flag, REVIEWS R7). _VideoAnalysisInputs 에 pole_axis_measurement 신설 (REVIEWS R10). _gemini_enabled() 재사용 (REVIEWS R6 helper drift 차단). **REVIEWS Cycle 2 NEW HIGH #1 차단** — `_preflight_label_gate_passed() -> bool | None` env helper 신설 (`PREFLIGHT_LABEL_GATE_PASSED` env). compute_force_signals 호출 시 본 helper 결과 박제 — belle 운영 작업 시 코드 변경 0 박제 strict."
      contains:
        - "force_signals"
        - "compute_force_signals"
        - "_get_force_signals_layer2_recognizer"
        - "FORCE_SIGNALS_LAYER2_ENABLED"
        - "_preflight_label_gate_passed"
        - "PREFLIGHT_LABEL_GATE_PASSED"
        - "force_signals_report"
        - "pole_axis_measurement"
        - "build_pole_axis_measurement"
        - "_gemini_enabled()"
    - path: app/src/lib/userAnalyses.ts
      provides: "normalize() 의 result spread 안 forceSignalsReport null-guard — Phase 7 WR-02 B1 패턴."
      contains:
        - "forceSignalsReport"
        - "?? null"
    - path: backend/runpod_inference/requirements.txt
      provides: "google-genai SDK 박제 확인 (Phase 5 박제 이미 존재 가능성 높음). 없으면 신설."
      contains:
        - "google-genai"
    - path: backend/tests/phase08/test_compute_phase_boundaries.py
      provides: "Layer 2 wiring 신설 test 5 — technique_profile.key_moments 활용 검증, FORCE_SIGNALS_LAYER2_ENABLED env 분기, agreement 룰, Layer 2 호출 실패 fallback, motion_id=None 시 비호출."
      contains:
        - "def test_layer2_uses_technique_profile_key_moments"
        - "def test_force_signals_layer2_enabled_env_required"
        - "def test_layer1_layer2_agreement_high_confidence"
        - "def test_layer1_layer2_disagreement_major_falls_back_to_layer1"
        - "def test_layer2_runtime_error_falls_back_to_layer1"
        - "def test_layer2_no_duplicate_gemini_call"
    - path: backend/tests/phase08/test_firestore_lockstep.py
      provides: "Firestore scoped validator test (REVIEWS Cycle 2 §3 NEW HIGH 정합) — `_validate_force_signals_report` 가 list[str] inside list[dict] 박제 PASS, 기존 `_validate_dict_only_scalars` 본체 변경 X — bodyComparisonReport schema strict 유지 박제 회귀 0. ForceSignalsReport dict camelCase 변환 PASS. nested list (list[list], list[dict-of-dict]) reject 유지."
      contains:
        - "def test_validate_force_signals_report_passes_warnings_list_of_str"
        - "def test_validate_force_signals_report_rejects_nested_list_inside_metric_dict"
        - "def test_validate_dict_only_scalars_remains_strict_for_body_comparison_report"
        - "def test_complete_analysis_uses_scoped_validator_only_for_force_signals"
        - "def test_camel_case_conversion_force_signals_report"
    - path: backend/tests/phase08/test_compute_force_signals.py
      provides: "Layer 2 wiring 통합 test 신설 — technique_profile.key_moments 활용 시 Layer 2 활성 + 'gemini_assisted' source. FORCE_SIGNALS_LAYER2_ENABLED 환경 박제. **REVIEWS Cycle 2 NEW HIGH #2 차단** — Layer 2 except 분기가 preflight gate ceiling 위로 promote X (test_layer2_failure_does_not_exceed_preflight_ceiling)."
      contains:
        - "def test_layer2_activates_with_technique_profile_moments"
        - "def test_layer2_agreement_high_confidence_propagates_to_overall"
        - "def test_layer2_failure_does_not_exceed_preflight_ceiling"
        - "def test_layer2_failure_with_preflight_none_stays_low"
        - "def test_layer2_failure_with_preflight_passed_stays_medium"
    - path: backend/tests/phase08/test_gemini_model_env_driven.py
      provides: "**REVIEWS Cycle 2 R8 NEW HIGH 차단** — Gemini model env-driven 박제 검증. GEMINI_MODEL env 설정 시 GeminiMomentExtractor + GeminiTechniqueRecognizer 둘 다 본 env 박제 + default 'gemini-2.5-flash' 박제 (non-EOL) + 'gemini-2.0-flash-exp' 박제 영구 제거."
      contains:
        - "def test_gemini_moment_extractor_default_is_non_eol"
        - "def test_gemini_moment_extractor_env_override"
        - "def test_gemini_technique_recognizer_env_override"
        - "def test_gemini_default_is_not_gemini_2_0_flash_exp"
        - "GEMINI_MODEL"
        - "gemini-2.5-flash"
    - path: backend/tests/pipeline/test_pipeline_phase8.py
      provides: "pipeline 통합 test — _process mock E2E. 4 env combination: (RECOGNIZER_BACKEND=gemini, FORCE_SIGNALS_LAYER2_ENABLED=truthy) / (RECOGNIZER_BACKEND=gemini, FSL2=unset) / (RECOGNIZER_BACKEND=unset, FSL2=truthy) / (둘 다 unset). REVIEWS R7 정합 — Phase 5 와 Phase 8 Layer 2 분리 검증. **REVIEWS Cycle 2 NEW HIGH #1 차단** — `PREFLIGHT_LABEL_GATE_PASSED` env 3-state (truthy/falsy/unset) 박제 → compute_force_signals 의 `preflight_label_gate_passed` 인자 박제 검증."
      contains:
        - "def test_pipeline_phase8_layer2_off_when_force_signals_flag_unset"
        - "def test_pipeline_phase8_layer2_on_when_both_flags_set"
        - "def test_pipeline_phase8_layer2_off_when_recognizer_backend_unset"
        - "def test_pipeline_phase8_default_off_safe"
        - "def test_pipeline_phase8_mode1_force_signals_emitted"
        - "def test_pipeline_phase8_mode3_force_signals_emitted"
        - "def test_pipeline_phase8_complete_analysis_force_signals_kwarg"
        - "def test_pipeline_phase8_pole_axis_measurement_present"
        - "def test_pipeline_phase8_preflight_gate_env_truthy_passes_true"
        - "def test_pipeline_phase8_preflight_gate_env_falsy_passes_false"
        - "def test_pipeline_phase8_preflight_gate_env_unset_passes_none"
  key_links:
    - from: backend/shared/python/sunity_shared/analysis/force_signals.py
      to: backend/shared/python/sunity_shared/analysis/technique.py
      via: "_layer2_boundaries_from_technique_profile: technique_profile.key_moments 직접 활용 (REVIEWS R6 — recognizer 중복 호출 영구 차단). TechniqueProfile.key_moments 확장 필드 박제 source."
      pattern: "key_moments|technique_profile"
    - from: backend/functions/pipeline/app.py
      to: backend/shared/python/sunity_shared/analysis/force_signals.py
      via: "compute_force_signals 호출 + _get_force_signals_layer2_recognizer singleton (FORCE_SIGNALS_LAYER2_ENABLED env, REVIEWS R7)."
      pattern: "compute_force_signals|_get_force_signals_layer2_recognizer"
    - from: backend/functions/pipeline/app.py
      to: backend/shared/python/sunity_shared/analysis/pole_geometry.py
      via: "_VideoAnalysisInputs 에 pole_axis_measurement 신설 — build_pole_axis_measurement(axis_3d=vertical_pole, line=None) 박제 (REVIEWS R10)."
      pattern: "build_pole_axis_measurement|PoleAxisMeasurement"
    - from: backend/functions/pipeline/app.py
      to: backend/shared/python/sunity_shared/firestore_admin.py
      via: "complete_analysis(force_signals_report=force_signals_dict) kwarg 주입."
      pattern: "force_signals_report"
    - from: app/src/lib/userAnalyses.ts
      to: app/src/types/analysis.ts
      via: "normalize() 가 forceSignalsReport null-guard — Plan 08-01 박제 optional interface compat layer."
      pattern: "forceSignalsReport"
---

<objective>
Phase 8 vertical slice (3/3) revised v2 — **REVIEWS Cycle 2 NEW HIGH 4개 차단 박제 + Cycle 1 박제 보존**. Plan 08-02 박제 force_signals.py 본체 위에 production 통합:

**REVIEWS Cycle 2 NEW HIGH 4개 영구 차단 (이번 replan 의 핵심):**
(a) **NEW HIGH #1 차단 — Preflight gate env plumbing (R4 carryover)**: pipeline/app.py 에 `_preflight_label_gate_passed()` env helper 박제 (PREFLIGHT_LABEL_GATE_PASSED env 3-state). Cycle 1 의 hardcoded `preflight_label_gate_passed=None` 박제 영구 제거. belle 운영 작업 시 env 박제만으로 gate flip (코드 변경 0).
(b) **NEW HIGH #2 차단 — Layer-2 except 분기 confidence promotion**: compute_phase_boundaries 의 Layer 2 except 분기는 `_layer1_confidence_from_preflight()` 박제 ceiling 박제 사용 (hardcoded 'medium' 영구 제거). Layer 2 success 분기도 `_min_confidence(agreement, ceiling)` 박제 — preflight gate ceiling 위로 promote 영구 X.
(c) **NEW HIGH #3 차단 — Firestore flat-array invariant project-wide 보존**: `_validate_dict_only_scalars` 본체 변경 영구 0 ([[firestore-nested-array-flat]] project-wide 보존). 신설 scoped validator `_validate_force_signals_report` — forceSignalsReport 만 list[str] 박제 허용, 다른 path 박제 strict 유지.
(d) **R8 carryover 차단 — Gemini model env-driven 실 default 위치**: judging/gemini_moment_extractor.py (실 default 위치 박제, Cycle 1 plan 누락 박제) + recognizer.py 둘 다 GEMINI_MODEL env 박제. default 'gemini-2.5-flash' (non-EOL, 'gemini-2.0-flash-exp' 2026-06-01 EOL 박제).

**REVIEWS Cycle 1 박제 그대로 보존:**
(e) **Layer 2 = recognizer moments reuse (REVIEWS R6 정합)** — 신규 GeminiMomentExtractor singleton 영구 제거. TechniqueProfile.key_moments 확장 + GeminiTechniqueRecognizer 박제 후 cache 에 박제됨. force_signals 가 cache reuse — 중복 Gemini call 영구 차단.
(f) **FORCE_SIGNALS_LAYER2_ENABLED 별도 env flag (REVIEWS R7 정합)** — Phase 5 RECOGNIZER_BACKEND='gemini' 와 분리. Phase 8 Layer 2 default off 강제.
(g) **_VideoAnalysisInputs 에 pole_axis_measurement 신설 (REVIEWS R10 정합)** — 기존 vertical fallback 한계 explicit 노출.
(h) **complete_analysis force_signals_report kwarg + pipeline wiring** — Phase 6 박제 패턴 정합.
(i) **frontend normalize** — Phase 7 WR-02 B1 패턴.
(j) **_gemini_enabled() 재사용 (REVIEWS R6 helper drift 차단)** — 신규 _should_keep_local_video helper 제거.
(k) **22 신설 단위 test + 11 pipeline 통합 test + SAM build smoke**.

Purpose: REVIEWS Cycle 2 NEW HIGH 4개 영구 차단 + Cycle 1 박제 보존 + FORCE-01 close-out.
Output: force_signals.py Layer 2 본체 + technique.py + gemini_technique_recognizer.py + **judging/gemini_moment_extractor.py** + firestore_admin.py + pipeline/app.py + userAnalyses.ts + 5 test 파일 + SAM smoke.

**박제 메모 정합:** [[firestore-nested-array-flat]] (Cycle 2 NEW HIGH #3 차단으로 강하게 보존) + [[mvp-simple-pilot-quality]] + [[feedback-analysis-first]] + [[no-baekje-filler]] + [[runpod-gpu-env]] (Pod env FORCE_SIGNALS_LAYER2_ENABLED + PREFLIGHT_LABEL_GATE_PASSED + GEMINI_MODEL 박제).

per REVIEWS Cycle 2 NEW HIGH #1 + #2 + #3 + R8 carryover + REVIEWS Cycle 1 R6/R7/R10 + Plan 08-00 + Plan 08-01 + Plan 08-02 dependency.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-jerk-jitter/08-CONTEXT.md
@.planning/phases/08-jerk-jitter/08-RESEARCH.md
@.planning/phases/08-jerk-jitter/08-REVIEWS.md
@.planning/phases/08-jerk-jitter/08-VALIDATION.md
@.planning/phases/08-jerk-jitter/08-00-PLAN.md
@.planning/phases/08-jerk-jitter/08-01-PLAN.md
@.planning/phases/08-jerk-jitter/08-02-PLAN.md
@.planning/phases/06-coaching/06-02-PLAN.md
@CLAUDE.md
@backend/shared/python/sunity_shared/analysis/force_signals.py
@backend/shared/python/sunity_shared/analysis/gemini_technique_recognizer.py
@backend/shared/python/sunity_shared/judging/gemini_moment_extractor.py
@backend/shared/python/sunity_shared/analysis/technique.py
@backend/shared/python/sunity_shared/analysis/pole_geometry.py
@backend/shared/python/sunity_shared/firestore_admin.py
@backend/functions/pipeline/app.py
@backend/runpod_inference/server.py
@backend/runpod_inference/requirements.txt
@app/src/lib/userAnalyses.ts
@app/src/types/analysis.ts
@backend/tests/phase06/test_compare_body_profiles_pipeline_wiring.py
@backend/tests/pipeline/
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Layer 2 Gemini API | 외부 LLM. Phase 5 박제 GeminiTechniqueRecognizer reuse (신규 Gemini call 0, REVIEWS R6). Plan 01-13 박제 정규식 가드 3 카테고리 — 본 plan 변경 0. |
| Firestore write | Lambda → Firestore Admin SDK. 신규 force_signals_report dict 추가. PII 0. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | S (Spoofing) | Gemini 응답 좌표/점수/판단 시도 | mitigate | 정규식 가드 3 카테고리 (Plan 01-13 박제). Phase 8 코드 변경 0 — reuse only. |
| T-08-03-02 | I (Information disclosure / Gemini API 키 leak) | env / log / Firestore | mitigate | Parameter Store SecureString (Plan 01-13 박제). Phase 8 변경 0. |
| T-08-03-03 | D (DoS / Gemini 무응답) | _layer2_boundaries_from_technique_profile | mitigate | technique_profile.key_moments None 박제 시 Layer 1 단독 + warning 'layer2_unavailable'. 분석 죽지 않음. |
| T-08-03-04 | D (Firestore nested-array crash) | force_signals_report list[str] warnings | mitigate | **REVIEWS Cycle 2 §3 NEW HIGH #3 차단** — `_validate_dict_only_scalars` 본체 변경 영구 X ([[firestore-nested-array-flat]] project-wide 보존). 신설 `_validate_force_signals_report` scoped validator — `forceSignalsReport` 안 list[str] 박제만 허용, 다른 path 박제 strict 유지. nested list (list[list], list[dict-of-dict]) 박제 영구 reject. |
| T-08-03-05 | T (pipeline wiring drift) | _process Phase 8 wiring + complete_analysis kwarg | mitigate | pipeline 통합 test (test_pipeline_phase8.py) — 4 env combination 모두 검증. mock E2E 패턴. |
| T-08-03-06 | I (영상 임시 파일 누수) | _extract_video_analysis_inputs(keep_local_video) | mitigate | finally 블록의 unlink (기존 박제). _gemini_enabled() 재사용 (REVIEWS R6 helper drift 차단). |
| T-08-03-07 | T (Firestore 저장 schema drift) | force_signals_report camelCase + flat validator | mitigate | _dataclass_to_camel_case_dict + _validate_flat_dict_no_nested_array 자동 적용. test_firestore_lockstep 검증. |
| T-08-03-08 | T (Layer 2 활성 drift — Phase 5 와 분리 X) | RECOGNIZER_BACKEND env share | mitigate | FORCE_SIGNALS_LAYER2_ENABLED 별도 env flag 박제 (REVIEWS R7). 4 env combination 검증. |
| T-08-03-09 | T (Gemini model name 박제 drift) | hardcoded model name 박제 | mitigate | **REVIEWS Cycle 2 R8 carryover 차단** — 실 default 위치 `judging/gemini_moment_extractor.py:48` env-driven 박제 + recognizer.py 도 동일 GEMINI_MODEL env 박제. default `gemini-2.5-flash` (non-EOL, 'gemini-2.0-flash-exp' 2026-06-01 EOL 박제 차단). `grep "gemini-2.0-flash-exp" == 0` 박제 안전망 박제. |
| T-08-03-11 | T (preflight gate plumbing gap) | pipeline 박제 hardcoded `preflight_label_gate_passed=None` | mitigate | **REVIEWS Cycle 2 NEW HIGH #1 차단** — `_preflight_label_gate_passed()` env helper 박제 (`PREFLIGHT_LABEL_GATE_PASSED` env, 3-state). belle 가 env 박제만으로 gate flip 박제 (코드 변경 0). `grep "preflight_label_gate_passed=None" == 0` 박제 안전망 박제. |
| T-08-03-12 | T (Layer 2 except 분기 confidence promotion) | hardcoded "medium" 박제 promote | mitigate | **REVIEWS Cycle 2 NEW HIGH #2 차단** — Layer 2 except 분기는 `_layer1_confidence_from_preflight(preflight_label_gate_passed)` 박제 ceiling 박제 사용. Layer 2 success 분기도 `_min_confidence(agreement, ceiling)` 박제 적용 — preflight gate ceiling 위로 promote 영구 X. test_layer2_failure_does_not_exceed_preflight_ceiling + test_layer2_success_capped_by_preflight_ceiling 박제 영구 안전망. |
| T-08-03-10 | T (pole_axis vertical fallback 한계 hidden) | pipeline 박제 default_pole | mitigate | _VideoAnalysisInputs 의 pole_axis_measurement 박제 (coordinate_space='unavailable') — Plan 08-02 의 axis distance None 분기 자동 활성 (REVIEWS R10). 사용자에게 명시 노출. |
| T-08-03-SC | Tampering | npm/pip/cargo installs | mitigate | google-genai 는 Phase 5 박제. 신규 외부 의존 0. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Layer 2 recognizer moments reuse + Cycle 2 HIGH 차단 — TechniqueProfile.key_moments 확장 + GEMINI_MODEL env-driven (recognizer.py + judging/gemini_moment_extractor.py 둘 다, REVIEWS Cycle 2 R8) + force_signals.py Layer 2 본체 (except 분기 preflight gate ceiling 준수, REVIEWS Cycle 2 NEW HIGH #2) + Firestore scoped validator _validate_force_signals_report (REVIEWS Cycle 2 NEW HIGH #3) + 신설 test</name>
  <files>
    backend/shared/python/sunity_shared/analysis/technique.py
    backend/shared/python/sunity_shared/analysis/gemini_technique_recognizer.py
    backend/shared/python/sunity_shared/judging/gemini_moment_extractor.py
    backend/shared/python/sunity_shared/analysis/force_signals.py
    backend/shared/python/sunity_shared/firestore_admin.py
    backend/tests/phase08/test_compute_phase_boundaries.py
    backend/tests/phase08/test_firestore_lockstep.py
    backend/tests/phase08/test_compute_force_signals.py
    backend/tests/phase08/test_gemini_model_env_driven.py
  </files>
  <read_first>
    backend/shared/python/sunity_shared/analysis/technique.py (TechniqueProfile dataclass 시그너처)
    backend/shared/python/sunity_shared/analysis/gemini_technique_recognizer.py (GeminiTechniqueRecognizer + extract_key_moments 호출 위치)
    backend/shared/python/sunity_shared/judging/gemini_moment_extractor.py (line 40-50 — **DEFAULT_GEMINI_MODEL 실 박제 위치**, KeyMoment 시그너처)
    backend/shared/python/sunity_shared/analysis/force_signals.py (Plan 08-02 박제 stub Layer 2 hook + _layer1_confidence_from_preflight helper)
    backend/shared/python/sunity_shared/firestore_admin.py (line 45-170 — _validate_dict_only_scalars 박제 위치)
    backend/tests/phase08/conftest.py
    .planning/phases/08-jerk-jitter/08-REVIEWS.md §Cycle 2 "New HIGH Concerns" §2 / §3 + §"Cycle 1 HIGH Resolution Audit" R4/R8 entries
  </read_first>
  <behavior>
    - **technique.py — TechniqueProfile 확장**:
      * 기존 frozen dataclass 에 `key_moments: tuple[KeyMoment, ...] | None = None` 신설 필드 (frozen dataclass 정합 — tuple 사용). KeyMoment import (judging.gemini_moment_extractor).
      * docstring 박제: "Plan 08-03 신설 (REVIEWS R6 정합) — Phase 8 Layer 2 가 본 필드 reuse (recognizer 중복 호출 영구 차단). GeminiTechniqueRecognizer 가 박제 후 cache 활용."
    - **gemini_technique_recognizer.py — model env reuse + key_moments cache** (REVIEWS Cycle 2 R8 carryover 정합):
      * 본 파일의 GEMINI_MODEL env 박제 (Cycle 1 박제 유지). recognizer 가 GeminiMomentExtractor 인스턴스화 시 model_name 인자 박제하지 않음 — extractor 의 default (env 박제) 박제 자동 reuse 박제.
      * recognize() 본체 변경 — moment_extractor.extract_key_moments() 호출 시 결과 (KeyMoment list) 를 TechniqueProfile 신설 필드 `key_moments` 에 박제. tuple 변환 (frozen dataclass 정합).
    - **judging/gemini_moment_extractor.py — model env 박제 (REVIEWS Cycle 2 R8 NEW HIGH 차단 — 실 default 위치)**:
      * 본 파일 line 48 의 `DEFAULT_GEMINI_MODEL = "gemini-3.1-pro-preview"` 박제 = 실 default 박제 위치 (Codex Cycle 2 R8 박제). Cycle 1 plan 이 recognizer.py 에만 박제하여 본 path 누락 → 본 plan 이 차단.
      * 박제 변경: `DEFAULT_GEMINI_MODEL = os.environ.get("GEMINI_MODEL", "gemini-2.5-flash")`.
      * 'gemini-2.5-flash' 박제 근거 (Codex Cycle 2 R8 박제): `gemini-2.0-flash-exp` 가 2026-06-01 EOL 박제 (Google deprecation page). `gemini-2.5-flash` 박제 = 현재 stable Flash family. Gemini 3.x Pro 가 belle 가 가용 박제 시 env 박제 `GEMINI_MODEL=gemini-3.1-pro-preview` 박제만으로 즉시 박제 (코드 변경 0).
      * 파일 head 의 D-13 박제 메모 갱신 — "model env-driven 박제 (REVIEWS Cycle 2 R8), 실 모델명은 GEMINI_MODEL env 박제로 belle 가 박제." 주석 박제.
      * 본 파일의 다른 박제 (KeyMoment dataclass / _COORDINATE_REJECT_PATTERNS / extract_key_moments 본체 등) 박제 변경 0 — 회귀 영구 차단.
    - **force_signals.py — Layer 2 본체 활성 (REVIEWS R6)**:
      * 신설 helper `_layer2_boundaries_from_technique_profile(layer1, key_moments, fps, frames_total) -> list[PhaseBoundary]`:
        - **import path 박제 (REVIEWS Cycle 2 §3 MEDIUM — import path drift 정합)**: `from sunity_shared.judging.gemini_moment_extractor import assign_frame_indices` 박제 (`.features` 박제 X — `assign_frame_indices` 실 위치는 `judging.gemini_moment_extractor:489` 박제).
        - key_moments 의 frame_idx 박제 후 `_map_moments_to_5phase(layer1, key_moments)` 호출.
      * 신설 helper `_map_moments_to_5phase(layer1, key_moments) -> list[PhaseBoundary]`:
        - setup.frame → entry.end / lock.start
        - hold.frame → final_shape.end / hold.start
        - peak.frame → final_shape representative (boundary 변경 X)
        - release.frame → hold.end
        - transition boundary 는 Gemini 미제공 → Layer 1 유지
        - **REVIEWS R6 MEDIUM 정합**: setup→lock start / hold→hold start 가정은 recognizer prompt 의 박제 contract 가 아니므로 docstring 박제 warning + monotonic boundary 검증 (start_ms < end_ms 강제 + 5 phase 의 start_ms 순서 강제). 위반 시 raise ValueError → caller (compute_phase_boundaries) 가 except 박제 → Layer 1 단독 + warning 'layer2_call_failed'.
      * 신설 helper `_confidence_from_agreement(layer1, layer2) -> tuple[MetricConfidence, list[str]]` — Plan 08-02 박제 본체와 동일 (timestamp 차이 abs 의 max → high / medium + warning / low + warning).
      * 신설 helper `_promote_layer1(boundary, confidence, source, warnings, preflight_label_gate_passed) -> PhaseBoundary` — boundary 재구성.
      * **compute_phase_boundaries() Layer 2 분기 활성화 (시그너처 변경) — REVIEWS Cycle 2 NEW HIGH #2 차단 (Layer-2 except 분기는 preflight gate ceiling 위로 promote 영구 X)**:
        - 기존 `gemini_extractor: GeminiMomentExtractor | None = None` 인자 제거 (REVIEWS R6 — 신규 singleton 영구 차단).
        - 신설 `technique_profile: TechniqueProfile | None = None` 인자.
        - 본체:
          ```python
          import os
          layer1 = _layer1_heuristic_boundaries(...)
          # preflight gate ceiling 박제 — Layer 2 성공 / 실패 / 미활성 박제 모두 본 ceiling 박제 강제.
          # (REVIEWS Cycle 2 NEW HIGH #2 차단 — Layer-2 except 분기가 preflight gate 위로 promote 영구 X)
          ceiling_confidence, ceiling_warnings = _layer1_confidence_from_preflight(preflight_label_gate_passed)
          layer2_enabled = os.environ.get("FORCE_SIGNALS_LAYER2_ENABLED", "").lower() in ("1", "true", "on", "yes")
          if layer2_enabled and technique_profile is not None and technique_profile.key_moments:
              try:
                  layer2 = _layer2_boundaries_from_technique_profile(layer1, technique_profile.key_moments, fps, len(pose_frames))
                  agreement_confidence, agreement_warnings = _confidence_from_agreement(layer1, layer2)
                  # Layer 2 가 agreement='high' 박제 시도 preflight gate 박제 ceiling 박제 위로 박제 X.
                  # _min_confidence 박제 helper — 'low' < 'medium' < 'high' 순서 박제.
                  effective_confidence = _min_confidence(agreement_confidence, ceiling_confidence)
                  effective_warnings = list(ceiling_warnings) + list(agreement_warnings)
                  if effective_confidence == "low":
                      return [_promote_layer1(b, effective_confidence, "heuristic", effective_warnings, preflight_label_gate_passed) for b in layer1]
                  else:
                      return [_promote_layer1(b, effective_confidence, "gemini_assisted", effective_warnings, preflight_label_gate_passed) for b in layer2]
              except (RuntimeError, ValueError) as exc:
                  log.warning("Layer 2 fallback to Layer 1 — %s", exc)
                  # **REVIEWS Cycle 2 NEW HIGH #2 차단** — Cycle 1 의 hardcoded "medium" 박제 영구 제거.
                  # except 분기는 preflight gate ceiling 박제 그대로 박제 — 'medium' 박제 강제 promote 영구 X.
                  fail_warnings = list(ceiling_warnings) + ["layer2_call_failed"]
                  return [_promote_layer1(b, ceiling_confidence, "heuristic", fail_warnings, preflight_label_gate_passed) for b in layer1]
          else:
              # Layer 1 only (Plan 08-02 박제 본체 유지 — preflight gate 분기)
              source = "heuristic" if motion_id else "heuristic_fallback"
              return [_promote_layer1(b, ceiling_confidence, source, ceiling_warnings, preflight_label_gate_passed) for b in layer1]
          ```
        - 신설 helper `_min_confidence(a, b) -> MetricConfidence` — 'low' < 'medium' < 'high' 순서 박제 (간단한 ordered enum 박제). preflight ceiling 박제 가 Layer 2 agreement 위 박제 강제 source.
    - **Firestore scoped validator 박제 (REVIEWS Cycle 2 §3 NEW HIGH #3 차단 — `_validate_dict_only_scalars` 본체 변경 영구 0)** (firestore_admin.py):
      * **`_validate_dict_only_scalars` 본체 변경 X** — [[firestore-nested-array-flat]] project-wide 박제 영구 보존. body_comparison_report 등 다른 path 박제 strict 유지 박제 강제. phase06 156 test PASS 박제 영구 회귀 0.
      * 신설 `_validate_force_signals_report(payload: dict, *, path: str = 'forceSignalsReport') -> None` scoped validator 박제 — 본 validator 만 list[scalar] inside list[dict] 박제 허용 (scoped exception):
        - 본체: payload 안 metric (axisMetrics / stabilityMetrics / contactMetrics / phaseBoundaries) list 박제 traverse 시 dict 안의 `warnings: list[str]` / `unstableBodyParts: list[str]` 박제 만 박제 PASS. 다른 list[scalar] 박제 시 ValueError raise.
        - list[list] / list[dict-of-dict] / list[None] 박제 reject 박제 (Firestore nested-array 박제 영구 차단).
        - top-level `forceSignalsReport.warnings: list[str]` 박제 도 PASS.
      * `complete_analysis(force_signals_report: dict | None = None)` kwarg 신설 — Phase 6 박제 body_comparison_report 패턴 정합. 본 kwarg 전달 시 `_validate_force_signals_report(force_signals_report)` 박제 호출 (scoped validator), `_validate_dict_only_scalars` 박제 호출 X (다른 path 박제 strict 유지). `payload["result"]["forceSignalsReport"] = force_signals_report` 박제.
    - **신설 단위 test**:
      * test_compute_phase_boundaries.py 신설 8 test:
        - test_layer2_uses_technique_profile_key_moments — TechniqueProfile(key_moments=...) 박제 + FORCE_SIGNALS_LAYER2_ENABLED=1 → Layer 2 활성 + source='gemini_assisted'
        - test_force_signals_layer2_enabled_env_required — FORCE_SIGNALS_LAYER2_ENABLED unset + technique_profile.key_moments != None → Layer 2 비활성 (REVIEWS R7)
        - test_layer1_layer2_agreement_high_confidence — <=2 frames 차이 + preflight_label_gate_passed=True → 'high' + source='gemini_assisted'
        - test_layer1_layer2_disagreement_major_falls_back_to_layer1 — >5 frames 차이 → 'low' + warning + source='heuristic'
        - test_layer2_runtime_error_falls_back_to_layer1 — _map_moments_to_5phase 가 ValueError raise → Layer 1 + warning 'layer2_call_failed'
        - test_layer2_no_duplicate_gemini_call — mock GeminiTechniqueRecognizer.recognize → 신규 호출 0회 (REVIEWS R6, recognizer 중복 차단)
        - **test_layer2_failure_does_not_exceed_preflight_ceiling** — preflight_label_gate_passed=None (default) + Layer 2 except 분기 박제 시 confidence='low' 박제 (NOT 'medium'). REVIEWS Cycle 2 NEW HIGH #2 차단 박제.
        - **test_layer2_success_capped_by_preflight_ceiling** — preflight_label_gate_passed=None + Layer 2 agreement='high' 박제 시 effective confidence='low' (ceiling 박제). preflight_label_gate_passed=True + Layer 2 agreement='high' → 'high' 박제 (ceiling 박제 해제).
      * test_firestore_lockstep.py 신설 5 test (REVIEWS Cycle 2 §3 NEW HIGH #3 차단 — scoped validator):
        - test_validate_force_signals_report_passes_warnings_list_of_str — `forceSignalsReport.axisMetrics[0].warnings = ['pole_line_missing']` 박제 PASS
        - test_validate_force_signals_report_rejects_nested_list_inside_metric_dict — `forceSignalsReport.axisMetrics[0].weird = [[1,2],[3,4]]` 박제 ValueError
        - **test_validate_dict_only_scalars_remains_strict_for_body_comparison_report** — bodyComparisonReport 안 list[str] 박제 시도 시 `_validate_dict_only_scalars` 박제 strict reject 박제 (project-wide 박제 영향 0 박제 검증)
        - test_complete_analysis_uses_scoped_validator_only_for_force_signals — complete_analysis(force_signals_report=...) 호출 시 `_validate_force_signals_report` 박제 호출 + `_validate_dict_only_scalars` 박제 호출 X (mock 박제 검증)
        - test_camel_case_conversion_force_signals_report — phase_boundaries → phaseBoundaries / axis_metrics → axisMetrics / measurement_kind → measurementKind / preflight_label_gate_passed → preflightLabelGatePassed / jerk_unit → jerkUnit
      * test_compute_force_signals.py 신설 5 test:
        - test_layer2_activates_with_technique_profile_moments — TechniqueProfile(key_moments=...) 박제 + FORCE_SIGNALS_LAYER2_ENABLED=1 → Layer 2 활성 + phase_boundaries[*].source='gemini_assisted'
        - test_layer2_agreement_high_confidence_propagates_to_overall — Layer 2 agreement 'high' + preflight_label_gate_passed=True + 모든 metric 'high' → overall_confidence='high'
        - **test_layer2_failure_does_not_exceed_preflight_ceiling** — preflight=None + Layer 2 except 박제 시 phase_boundaries[*].confidence='low' 박제 (REVIEWS Cycle 2 NEW HIGH #2 차단)
        - test_layer2_failure_with_preflight_none_stays_low — preflight_label_gate_passed=None + Layer 2 raise → confidence='low'
        - test_layer2_failure_with_preflight_passed_stays_medium — preflight_label_gate_passed=True + Layer 2 raise → confidence='medium' (preflight ceiling 박제 그대로, NOT 'high' promote)
      * **test_gemini_model_env_driven.py 신설 4 test (REVIEWS Cycle 2 R8 NEW HIGH 차단)**:
        - test_gemini_moment_extractor_default_is_non_eol — DEFAULT_GEMINI_MODEL == 'gemini-2.5-flash' (NOT 'gemini-2.0-flash-exp' 박제 EOL)
        - test_gemini_moment_extractor_env_override — monkeypatch.setenv('GEMINI_MODEL', 'gemini-3.1-pro-preview') + reload module → DEFAULT_GEMINI_MODEL == 'gemini-3.1-pro-preview' (belle 가 env 박제 변경만으로 model 박제 변경)
        - test_gemini_technique_recognizer_env_override — recognizer 도 동일 env 박제 reuse 검증 (recognizer 가 GeminiMomentExtractor 인스턴스화 시 model_name 인자 박제 X — extractor default 박제 자동 reuse)
        - test_gemini_default_is_not_gemini_2_0_flash_exp — 박제 안전망 — 'gemini-2.0-flash-exp' 박제 grep 박제 ZERO (REVIEWS Cycle 2 R8 박제 EOL 영구 차단)
  </behavior>
  <action>
    `technique.py` 의 TechniqueProfile 에 `key_moments: tuple[KeyMoment, ...] | None = None` 신설 필드 박제. KeyMoment import (lazy 가능). frozen=True dataclass 정합.

    `judging/gemini_moment_extractor.py` — **REVIEWS Cycle 2 R8 NEW HIGH 차단** (실 default 위치). line 48 의 `DEFAULT_GEMINI_MODEL = "gemini-3.1-pro-preview"` 박제 → `DEFAULT_GEMINI_MODEL = os.environ.get("GEMINI_MODEL", "gemini-2.5-flash")` 박제. file header 의 D-13 박제 메모 갱신 박제 (env-driven 박제 noted). 다른 박제 (KeyMoment / _COORDINATE_REJECT_PATTERNS / extract_key_moments 본체) 변경 0.

    `gemini_technique_recognizer.py` — GEMINI_MODEL env 박제 reuse 박제 (extractor 가 default 박제 env 박제 자동 reuse). recognize() 본체 변경: extract_key_moments() 결과를 TechniqueProfile(key_moments=tuple(moments)) 박제.

    `force_signals.py` — Plan 08-02 박제 stub Layer 2 hook 활성화. _layer2_boundaries_from_technique_profile + _map_moments_to_5phase + _confidence_from_agreement + _promote_layer1 + _layer1_confidence_from_preflight + **_min_confidence** 6 helper 박제. compute_phase_boundaries 시그너처 변경 (gemini_extractor → technique_profile). FORCE_SIGNALS_LAYER2_ENABLED env check 박제 (REVIEWS R7).

    **REVIEWS Cycle 2 NEW HIGH #2 차단** — Layer 2 except 분기는 `_layer1_confidence_from_preflight(preflight_label_gate_passed)` 박제 ceiling 박제 박제 사용 (Cycle 1 의 hardcoded "medium" 박제 영구 제거). Layer 2 success 분기도 ceiling 박제 위로 promote 박제 X — `_min_confidence(agreement, ceiling)` 박제 적용 박제.

    monotonic boundary 검증 박제 (REVIEWS R6 MEDIUM 정합) — _map_moments_to_5phase 안 5 phase 의 start_ms 순서 강제 + start_ms < end_ms 강제. 위반 시 ValueError → caller 가 except → Layer 1 fallback (preflight ceiling 박제 유지).

    `firestore_admin.py` — **REVIEWS Cycle 2 §3 NEW HIGH #3 차단**. `_validate_dict_only_scalars` 본체 변경 영구 X ([[firestore-nested-array-flat]] project-wide 보존). 신설 `_validate_force_signals_report` scoped validator + complete_analysis force_signals_report kwarg 박제 (본 kwarg 만 scoped validator 박제 호출, 다른 path strict 유지). 단일 atomic commit (phase06 156 test 회귀 0 강제).

    22 신설 test 박제 (8 + 5 + 5 + 4). pytest fixture 활용. mock TechniqueProfile + mock GeminiTechniqueRecognizer + monkeypatch.setenv.

    Critical 회귀 검증 — phase06 156 test PASS 유지 강제. `_validate_dict_only_scalars` 본체 변경 영구 0 박제 → 기존 BodyComparisonReport schema 회귀 0 박제 보장.

    per REVIEWS Cycle 1 R6 + R7 + R8 + REVIEWS Cycle 2 NEW HIGH #1/#2/#3 + R4 carryover + R8 carryover + Plan 08-02 dependency.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && PYTHONPATH=backend/shared/python:. backend/.venv/bin/python3 -m pytest backend/tests/phase08/ backend/tests/phase06/ backend/tests/phase07/ -x</automated>
  </verify>
  <acceptance_criteria>
    - File modified: technique.py 의 TechniqueProfile 에 `key_moments` 필드 박제
    - File modified: **judging/gemini_moment_extractor.py 의 DEFAULT_GEMINI_MODEL 가 env-driven** (REVIEWS Cycle 2 R8 — 실 default 위치)
    - File modified: gemini_technique_recognizer.py 의 GEMINI_MODEL env reuse 박제
    - File modified: force_signals.py 의 compute_phase_boundaries 시그너처 변경 (technique_profile 인자 + gemini_extractor 인자 제거)
    - File modified: firestore_admin.py — **`_validate_dict_only_scalars` 본체 변경 X** + `_validate_force_signals_report` scoped validator 신설 + complete_analysis kwarg
    - Source: `grep -c "key_moments" backend/shared/python/sunity_shared/analysis/technique.py` >= 1
    - Source: `grep -c "GEMINI_MODEL\|os.environ.get" backend/shared/python/sunity_shared/judging/gemini_moment_extractor.py` >= 2 (REVIEWS Cycle 2 R8 — 실 default 위치)
    - Source: `grep -c "gemini-2.5-flash" backend/shared/python/sunity_shared/judging/gemini_moment_extractor.py` >= 1 (non-EOL default)
    - Source: `grep -c "gemini-2.0-flash-exp" backend/shared/python/sunity_shared/judging/gemini_moment_extractor.py` == 0 (REVIEWS Cycle 2 R8 — EOL 영구 차단)
    - Source: `grep -c "GEMINI_MODEL\|os.environ.get" backend/shared/python/sunity_shared/analysis/gemini_technique_recognizer.py` >= 1
    - Source: `grep -c "FORCE_SIGNALS_LAYER2_ENABLED" backend/shared/python/sunity_shared/analysis/force_signals.py` >= 1
    - Source: `grep -c "technique_profile" backend/shared/python/sunity_shared/analysis/force_signals.py` >= 2
    - Source: `grep -c "gemini_extractor" backend/shared/python/sunity_shared/analysis/force_signals.py` == 0 (REVIEWS R6 — 신규 singleton 영구 차단)
    - Source: `grep -c "_min_confidence\|_layer1_confidence_from_preflight" backend/shared/python/sunity_shared/analysis/force_signals.py` >= 2 (REVIEWS Cycle 2 NEW HIGH #2 — preflight ceiling 박제)
    - Source: `grep -c "ceiling_confidence" backend/shared/python/sunity_shared/analysis/force_signals.py` >= 1 (NEW HIGH #2)
    - Source: `grep -c "force_signals_report\|forceSignalsReport" backend/shared/python/sunity_shared/firestore_admin.py` >= 2
    - Source: `grep -c "_validate_force_signals_report" backend/shared/python/sunity_shared/firestore_admin.py` >= 2 (REVIEWS Cycle 2 NEW HIGH #3 — scoped validator)
    - Source: **`git diff backend/shared/python/sunity_shared/firestore_admin.py | grep "^[-+].*def _validate_dict_only_scalars"` returns 박제 변경 0** — `_validate_dict_only_scalars` 본체 변경 영구 0 박제 ([[firestore-nested-array-flat]] 보존)
    - Behavior (Layer 2 reuse): test_layer2_uses_technique_profile_key_moments + test_layer2_no_duplicate_gemini_call PASS
    - Behavior (env separation): test_force_signals_layer2_enabled_env_required PASS (REVIEWS R7)
    - Behavior (model env — Cycle 2 R8): test_gemini_moment_extractor_default_is_non_eol + test_gemini_moment_extractor_env_override + test_gemini_technique_recognizer_env_override + test_gemini_default_is_not_gemini_2_0_flash_exp PASS
    - Behavior (preflight ceiling — Cycle 2 NEW HIGH #2): test_layer2_failure_does_not_exceed_preflight_ceiling + test_layer2_success_capped_by_preflight_ceiling + test_layer2_failure_with_preflight_none_stays_low + test_layer2_failure_with_preflight_passed_stays_medium PASS
    - Behavior (scoped validator — Cycle 2 NEW HIGH #3): test_validate_force_signals_report_passes_warnings_list_of_str + test_validate_force_signals_report_rejects_nested_list_inside_metric_dict + test_validate_dict_only_scalars_remains_strict_for_body_comparison_report + test_complete_analysis_uses_scoped_validator_only_for_force_signals PASS
    - Behavior (agreement / disagreement): test_layer1_layer2_agreement_high_confidence + test_layer1_layer2_disagreement_major_falls_back_to_layer1 + test_layer2_runtime_error_falls_back_to_layer1 PASS
    - Behavior (camelCase): test_camel_case_conversion_force_signals_report PASS (preflightLabelGatePassed / measurementKind / jerkUnit 포함)
    - Behavior (회귀 0): phase06 156 + phase07 108 test PASS — `_validate_dict_only_scalars` 본체 변경 영구 0 박제로 body_comparison_report schema 회귀 0 박제 보장
    - Behavior: pytest exit 0
  </acceptance_criteria>
  <done>
    REVIEWS Cycle 2 NEW HIGH 3개 영구 차단 박제 완료: (a) Gemini model env-driven 박제 실 default 위치 (judging/gemini_moment_extractor.py + recognizer.py 둘 다, default='gemini-2.5-flash' non-EOL 박제, REVIEWS Cycle 2 R8). (b) Layer 2 except 분기는 preflight ceiling 박제 유지 (hardcoded 'medium' 영구 제거, REVIEWS Cycle 2 NEW HIGH #2). (c) Firestore scoped validator `_validate_force_signals_report` 신설 박제 — `_validate_dict_only_scalars` 본체 변경 영구 0 박제 ([[firestore-nested-array-flat]] project-wide 보존, REVIEWS Cycle 2 NEW HIGH #3). Cycle 1 박제 (R6 / R7 / R8 / R10 / H1) 박제 그대로 보존. 22 신설 test PASS + phase06/07 회귀 0. Task 2 가 pipeline wiring + frontend + pole_axis_measurement + **_preflight_label_gate_passed env helper** (Cycle 2 NEW HIGH #1).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: pipeline/app.py wiring + _VideoAnalysisInputs.pole_axis_measurement 신설 + frontend userAnalyses.ts normalize + pipeline 통합 test (4 env combination) + RunPod requirements 검토</name>
  <files>
    backend/functions/pipeline/app.py
    app/src/lib/userAnalyses.ts
    backend/runpod_inference/requirements.txt
    backend/tests/pipeline/test_pipeline_phase8.py
  </files>
  <read_first>
    backend/functions/pipeline/app.py (line 375-440 _VideoAnalysisInputs + _extract_video_analysis_inputs + line 750-1010 _process)
    backend/shared/python/sunity_shared/analysis/force_signals.py (Task 1 박제 완료 상태)
    backend/shared/python/sunity_shared/analysis/pole_geometry.py (Plan 08-00 박제 build_pole_axis_measurement)
    backend/shared/python/sunity_shared/firestore_admin.py (Task 1 박제 force_signals_report kwarg)
    backend/shared/python/sunity_shared/analysis/gemini_technique_recognizer.py (Phase 5 박제 GeminiTechniqueRecognizer)
    backend/runpod_inference/requirements.txt
    app/src/lib/userAnalyses.ts
    app/src/types/analysis.ts (Plan 08-01 박제 forceSignalsReport optional)
    backend/tests/phase06/test_compare_body_profiles_pipeline_wiring.py
    .planning/phases/06-coaching/06-02-PLAN.md
    .planning/phases/08-jerk-jitter/08-REVIEWS.md §"Newly Surfaced Concerns" R10 + R6 helper drift
  </read_first>
  <behavior>
    - **_VideoAnalysisInputs 확장** (REVIEWS R10 정합):
      * 기존 4 필드 → 5 필드: `angles / student_profile / pose_frames / local_video_path / pole_axis_measurement`.
      * pole_axis_measurement 필드 type = `PoleAxisMeasurement` (Plan 08-00 박제 import).
      * docstring 박제 — "REVIEWS R10 정합: 기존 vertical_fallback PoleAxis 한계 explicit 노출. Plan 08-00 박제 build_pole_axis_measurement(axis_3d=vertical_pole, line=None) 박제 → coordinate_space='unavailable'. Phase 8 의 axis distance None 분기 자동 활성. 추후 HoughPoleDetector 활성 plan 박제 시 line 박제."
    - **_extract_video_analysis_inputs() 본체 변경**:
      * `default_pole` 박제 직후 (기존 박제 위치) Plan 08-00 박제 `from sunity_shared.analysis.pole_geometry import build_pole_axis_measurement` import + `pole_axis_measurement = build_pole_axis_measurement(axis_3d=default_pole, line=None, frame_index=None)` 박제.
      * return 시 `pole_axis_measurement=pole_axis_measurement` 박제.
    - **pipeline/app.py wiring** (compare_body_profiles 호출 직후):
      * 모듈 top-level 신설 (REVIEWS Cycle 2 NEW HIGH #1 차단 — preflight gate env plumbing 박제):
        ```python
        from sunity_shared.analysis import force_signals as fs
        from sunity_shared.analysis.gemini_technique_recognizer import GeminiTechniqueRecognizer
        from sunity_shared.analysis.pole_geometry import build_pole_axis_measurement

        _FORCE_SIGNALS_LAYER2_RECOGNIZER: GeminiTechniqueRecognizer | None = None

        def _force_signals_layer2_enabled() -> bool:
            """REVIEWS R7 — FORCE_SIGNALS_LAYER2_ENABLED 별도 env flag 박제.
            Phase 5 RECOGNIZER_BACKEND 와 분리. Phase 8 Layer 2 default off."""
            raw = os.environ.get("FORCE_SIGNALS_LAYER2_ENABLED", "")
            return raw.strip().lower() in ("1", "true", "on", "yes")

        def _preflight_label_gate_passed() -> bool | None:
            """**REVIEWS Cycle 2 NEW HIGH #1 차단** (R4 carryover 박제 plumbing).

            belle 운영 작업: Plan 08-00 박제 25-timestamp PASS 검증 후 Lambda env 또는
            RunPod Pod env 박제 `PREFLIGHT_LABEL_GATE_PASSED=1` 박제만으로 Layer 1
            confidence='medium' 승급 박제 — 코드 변경 0 박제.

            3-state 박제:
              · '1'/'true'/'on'/'yes' → True  (gate PASS, confidence='medium' 승급)
              · '0'/'false'/'off'/'no' → False  (gate FAIL, confidence='low' 강제 + warning 'preflight_label_gate_failed')
              · '' (unset) 또는 그 외 → None  (gate 미실행 default, confidence='low' + warning 'preflight_gate_pending')

            Plan 08-00 박제 preflight_label_template.csv + Plan 08-02 박제 compute_phase_boundaries 의
            preflight_label_gate_passed 인자 박제 정합.
            """
            raw = os.environ.get("PREFLIGHT_LABEL_GATE_PASSED", "").strip().lower()
            if raw in ("1", "true", "on", "yes"):
                return True
            if raw in ("0", "false", "off", "no"):
                return False
            return None

        def _get_force_signals_layer2_recognizer() -> GeminiTechniqueRecognizer | None:
            """REVIEWS R6 정합 — Phase 5 박제 GeminiTechniqueRecognizer 재사용 (신규 instance 영구 차단)."""
            global _FORCE_SIGNALS_LAYER2_RECOGNIZER
            if not _force_signals_layer2_enabled():
                return None
            if not _gemini_enabled():
                return None  # Phase 5 recognizer 활성 시만 reuse
            if _FORCE_SIGNALS_LAYER2_RECOGNIZER is None:
                _FORCE_SIGNALS_LAYER2_RECOGNIZER = GeminiTechniqueRecognizer()
            return _FORCE_SIGNALS_LAYER2_RECOGNIZER
        ```
      * _process 안 compare_body_profiles 호출 직후 Phase 8 wiring 박제:
        ```python
        # Phase 8 (Plan 08-03 wiring) — force signals 산출.
        # REVIEWS R5 정합: angles 는 _extract_video_analysis_inputs 가 1회 temporal_fill 적용 (double smoothing 차단)
        # REVIEWS R10 정합: pole_axis_measurement 의 coordinate_space='unavailable' 시 axis distance None (graceful)
        # REVIEWS R6 정합: technique_profile 는 compare_body_profiles 호출 시점에 이미 박제됨 (recognizer reuse)
        # REVIEWS R7 정합: FORCE_SIGNALS_LAYER2_ENABLED env flag check (Phase 5 와 분리)
        # REVIEWS Cycle 2 NEW HIGH #1 차단 (R4 carryover): preflight_label_gate_passed 는 env 박제 helper 활용
        #   — belle 가 Lambda env / RunPod Pod env 박제 PREFLIGHT_LABEL_GATE_PASSED=1 박제만으로 gate flip 박제 (코드 변경 0)
        layer2_recognizer = _get_force_signals_layer2_recognizer()
        force_signals_report = fs.compute_force_signals(
            pose_frames=inputs.pose_frames,
            pole_axis_measurement=inputs.pole_axis_measurement,  # REVIEWS R10
            body_profile=student_profile,
            angles=angles,  # _extract_video_analysis_inputs 가 temporal_fill 적용된 상태 (REVIEWS R5)
            fps=9.0,
            motion_id=getattr(profile, "motion_id", None),
            preflight_label_gate_passed=_preflight_label_gate_passed(),  # REVIEWS Cycle 2 NEW HIGH #1 — env 박제 helper
            technique_profile=profile if layer2_recognizer is not None else None,  # REVIEWS R6 reuse
        )
        force_signals_dict = _dataclass_to_camel_case_dict(force_signals_report)
        ```
      * complete_analysis 호출에 `force_signals_report=force_signals_dict` kwarg 추가
      * `_extract_video_analysis_inputs(... keep_local_video=_gemini_enabled())` 호출 박제 — `_gemini_enabled()` 재사용 (REVIEWS R6 helper drift 차단 — 신규 _should_keep_local_video 영구 차단).
    - **frontend userAnalyses.ts normalize** (Phase 7 WR-02 B1 패턴):
      * 기존 bodyComparisonReport null-guard 블록 직후에 forceSignalsReport null-guard 박제:
        ```typescript
        if (result?.forceSignalsReport) {
            const report = result.forceSignalsReport;
            result = {
                ...result,
                forceSignalsReport: {
                    version: report.version ?? "1.0",
                    overallConfidence: report.overallConfidence ?? "medium",
                    warnings: report.warnings ?? [],
                    phaseBoundaries: report.phaseBoundaries ?? [],
                    axisMetrics: report.axisMetrics ?? [],
                    stabilityMetrics: report.stabilityMetrics ?? [],
                    contactMetrics: report.contactMetrics ?? [],
                },
            };
        }
        ```
    - **backend/runpod_inference/requirements.txt 검토**:
      * Phase 5 박제 시점에 google-genai SDK 이미 박제됨. 변경 가능성 0. `grep -c "google-genai\|google_genai" backend/runpod_inference/requirements.txt` 확인. 없으면 `google-genai>=0.1,<1.0` 추가.
    - **pipeline 통합 test (test_pipeline_phase8.py)** — 4 env combination 검증 (REVIEWS R7):
      * test_pipeline_phase8_layer2_off_when_force_signals_flag_unset — RECOGNIZER_BACKEND='gemini' + FORCE_SIGNALS_LAYER2_ENABLED unset → Layer 2 비활성 (force_signals.warnings contains 'layer2_unavailable')
      * test_pipeline_phase8_layer2_on_when_both_flags_set — RECOGNIZER_BACKEND='gemini' + FORCE_SIGNALS_LAYER2_ENABLED='1' + mock GeminiTechniqueRecognizer (key_moments 박제) → Layer 2 활성 (force_signals.phaseBoundaries[*].source='gemini_assisted')
      * test_pipeline_phase8_layer2_off_when_recognizer_backend_unset — RECOGNIZER_BACKEND unset + FORCE_SIGNALS_LAYER2_ENABLED='1' → Layer 2 비활성 (Phase 5 recognizer 비활성)
      * test_pipeline_phase8_default_off_safe — 모든 env unset → Layer 1 단독 + force_signals_report 정상 산출 (분석 죽지 않음)
      * test_pipeline_phase8_mode1_force_signals_emitted — mode='mode1' + mock → force_signals_report kwarg 전달
      * test_pipeline_phase8_mode3_force_signals_emitted — mode='mode3' + mock → force_signals_report kwarg 전달
      * test_pipeline_phase8_complete_analysis_force_signals_kwarg — complete_analysis mock 의 call_args 검사 — force_signals_report kwarg dict + 'phaseBoundaries' / 'axisMetrics' / 'stabilityMetrics' / 'contactMetrics' / 'overallConfidence' 키 모두 박제
      * test_pipeline_phase8_pole_axis_measurement_present — _extract_video_analysis_inputs 반환 NamedTuple 의 pole_axis_measurement 필드 존재 + coordinate_space='unavailable' (vertical fallback, REVIEWS R10)
      * **test_pipeline_phase8_preflight_gate_env_truthy_passes_true** — monkeypatch.setenv('PREFLIGHT_LABEL_GATE_PASSED', '1') → compute_force_signals 의 preflight_label_gate_passed 인자 = True 박제 검증 (mock 의 call_args 검사). REVIEWS Cycle 2 NEW HIGH #1 차단 박제.
      * **test_pipeline_phase8_preflight_gate_env_falsy_passes_false** — monkeypatch.setenv('PREFLIGHT_LABEL_GATE_PASSED', '0') → preflight_label_gate_passed=False 박제 + warning 'preflight_label_gate_failed' 박제 검증.
      * **test_pipeline_phase8_preflight_gate_env_unset_passes_none** — monkeypatch.delenv('PREFLIGHT_LABEL_GATE_PASSED', raising=False) → preflight_label_gate_passed=None 박제 + warning 'preflight_gate_pending' 박제 검증.
  </behavior>
  <action>
    `backend/functions/pipeline/app.py` 변경:
      (1) top-level import + **3 singleton helper** (_force_signals_layer2_enabled / **_preflight_label_gate_passed** / _get_force_signals_layer2_recognizer) 박제.
      (2) _VideoAnalysisInputs NamedTuple 에 pole_axis_measurement 필드 신설 (REVIEWS R10).
      (3) _extract_video_analysis_inputs 본체 변경 — build_pole_axis_measurement 호출 + pole_axis_measurement return.
      (4) _process 안 Phase 8 wiring 박제 (compare_body_profiles 직후 / body_comparison_report_dict 변환 사이).
        **REVIEWS Cycle 2 NEW HIGH #1 차단** — `preflight_label_gate_passed=_preflight_label_gate_passed()` 박제 (Cycle 1 의 hardcoded `None` 박제 영구 제거). belle 가 PREFLIGHT_LABEL_GATE_PASSED env 박제만으로 gate flip 박제 (코드 변경 0).
      (5) complete_analysis 호출에 force_signals_report kwarg 추가.
      (6) `_extract_video_analysis_inputs(... keep_local_video=_gemini_enabled())` 박제 — `_gemini_enabled()` 재사용 (REVIEWS R6 — 신규 _should_keep_local_video 영구 차단). 신규 helper 박제 X.

    `app/src/lib/userAnalyses.ts` 변경: 기존 bodyComparisonReport null-guard 직후 forceSignalsReport null-guard 박제 (Phase 7 WR-02 B1 immutable spread + ?? null fallback 패턴 정확히 mirror).

    `backend/runpod_inference/requirements.txt` 검토: grep 확인. 없으면 google-genai>=0.1,<1.0 추가.

    `backend/tests/pipeline/test_pipeline_phase8.py` 신설 — **11 통합 test** (Cycle 1 8 + Cycle 2 NEW HIGH #1 plumbing 3). monkeypatch.setenv/delenv (FORCE_SIGNALS_LAYER2_ENABLED + RECOGNIZER_BACKEND 4 조합 + PREFLIGHT_LABEL_GATE_PASSED 3-state) + unittest.mock.patch (firestore_admin / GeminiTechniqueRecognizer / _extract_video_analysis_inputs / compare_body_profiles 모두 mock).

    Critical Phase 6 회귀 0 — 본 wiring 으로 pipeline 통합 test 모두 PASS 유지.

    per REVIEWS Cycle 1 R6 + R7 + R10 + REVIEWS Cycle 2 NEW HIGH #1 (R4 plumbing) + Plan 08-00 build_pole_axis_measurement + Phase 6 박제 박제 패턴.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && PYTHONPATH=backend/shared/python:. backend/.venv/bin/python3 -m pytest backend/tests/pipeline/ backend/tests/phase08/ backend/tests/phase06/ backend/tests/phase07/ -x && cd app && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File modified: pipeline/app.py contains `from sunity_shared.analysis import force_signals as fs` + `from sunity_shared.analysis.pole_geometry import build_pole_axis_measurement`
    - File modified: pipeline/app.py contains `_force_signals_layer2_enabled` + `_preflight_label_gate_passed` + `_get_force_signals_layer2_recognizer` singleton (3 helper)
    - Source: `grep -c "_preflight_label_gate_passed" backend/functions/pipeline/app.py` >= 2 (definition + call — REVIEWS Cycle 2 NEW HIGH #1 차단)
    - Source: `grep -c "PREFLIGHT_LABEL_GATE_PASSED" backend/functions/pipeline/app.py` >= 1 (REVIEWS Cycle 2 NEW HIGH #1 env name 박제)
    - Source: **`grep -c "preflight_label_gate_passed=None" backend/functions/pipeline/app.py` == 0** — Cycle 1 박제 hardcoded None 박제 영구 제거 검증 (Cycle 2 NEW HIGH #1)
    - File modified: pipeline/app.py 의 _VideoAnalysisInputs NamedTuple 에 `pole_axis_measurement` 필드 신설
    - File modified: pipeline/app.py contains `force_signals_report=force_signals_dict` in complete_analysis call
    - File modified: pipeline/app.py contains `keep_local_video=_gemini_enabled()` (REVIEWS R6 helper drift 차단 — 신규 _should_keep_local_video 박제 X)
    - File modified: userAnalyses.ts contains forceSignalsReport null-guard with immutable spread
    - Source: `grep -c "compute_force_signals" backend/functions/pipeline/app.py` >= 1
    - Source: `grep -c "_get_force_signals_layer2_recognizer" backend/functions/pipeline/app.py` >= 2 (definition + call)
    - Source: `grep -c "FORCE_SIGNALS_LAYER2_ENABLED" backend/functions/pipeline/app.py` >= 1 (REVIEWS R7)
    - Source: `grep -c "_should_keep_local_video" backend/functions/pipeline/app.py` == 0 (REVIEWS R6 helper drift 차단)
    - Source: `grep -c "build_pole_axis_measurement\|pole_axis_measurement" backend/functions/pipeline/app.py` >= 2 (REVIEWS R10)
    - Source: `grep -c "forceSignalsReport" app/src/lib/userAnalyses.ts` >= 1
    - Source: `grep -c "google-genai\|google_genai" backend/runpod_inference/requirements.txt` >= 1
    - Behavior (Layer 2 off when force_signals flag unset): test_pipeline_phase8_layer2_off_when_force_signals_flag_unset PASS (REVIEWS R7)
    - Behavior (Layer 2 on with both flags): test_pipeline_phase8_layer2_on_when_both_flags_set PASS
    - Behavior (Layer 2 off when recognizer backend unset): test_pipeline_phase8_layer2_off_when_recognizer_backend_unset PASS
    - Behavior (default off safe): test_pipeline_phase8_default_off_safe PASS — 분석 죽지 않음
    - Behavior (pole_axis_measurement): test_pipeline_phase8_pole_axis_measurement_present PASS — coordinate_space='unavailable' 검증 (REVIEWS R10)
    - Behavior (mode): test_pipeline_phase8_mode1_force_signals_emitted + test_pipeline_phase8_mode3_force_signals_emitted PASS
    - Behavior (kwarg): test_pipeline_phase8_complete_analysis_force_signals_kwarg PASS
    - **Behavior (preflight env plumbing — Cycle 2 NEW HIGH #1)**: test_pipeline_phase8_preflight_gate_env_truthy_passes_true + test_pipeline_phase8_preflight_gate_env_falsy_passes_false + test_pipeline_phase8_preflight_gate_env_unset_passes_none 3 test PASS — belle 가 env 박제만으로 gate flip 박제 검증
    - Behavior (tsc): `cd app && npx tsc --noEmit` exits 0
    - Behavior (regression): phase06 / phase07 / phase08 / pipeline 모두 PASS
    - Behavior: pytest exit 0
  </acceptance_criteria>
  <done>
    pipeline wiring + frontend normalize + RunPod requirements 검토 + **preflight gate env plumbing (REVIEWS Cycle 2 NEW HIGH #1 차단)** 박제 완료. 11 pipeline 통합 test PASS (4 Layer2 env combination + 3 preflight env state) + tsc clean + 회귀 0. REVIEWS R6 (helper drift 차단) + R7 (별도 env flag) + R10 (pole_axis_measurement explicit 노출) + Cycle 2 NEW HIGH #1 (preflight env helper) 모두 영구 차단. Task 3 (SAM build smoke + manual checkpoint) 박제.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: SAM build smoke + RunPod Pod env 갱신 + Plan 08-00 박제 preflight 25-timestamp label gate (belle 운영 작업 manual checkpoint)</name>
  <what-built>
    Plan 08-03 의 모든 코드 변경 완료 (REVIEWS Cycle 2 NEW HIGH 3개 차단 박제):
    (a) force_signals.py Layer 2 recognizer moments reuse (REVIEWS R6 — 신규 GeminiMomentExtractor singleton 영구 제거)
    (b) FORCE_SIGNALS_LAYER2_ENABLED 별도 env flag (REVIEWS R7 — Phase 5 와 분리)
    (c) **GEMINI_MODEL env-driven 박제 — judging/gemini_moment_extractor.py 실 default 위치 + recognizer.py 둘 다, default 'gemini-2.5-flash' non-EOL (REVIEWS Cycle 2 R8 carryover 차단)**
    (d) TechniqueProfile.key_moments 확장 (Phase 8 Layer 2 reuse source)
    (e) **Firestore scoped validator `_validate_force_signals_report` 신설 박제 (REVIEWS Cycle 2 NEW HIGH #3 차단). `_validate_dict_only_scalars` 본체 변경 영구 0 — project-wide [[firestore-nested-array-flat]] 보존.** complete_analysis force_signals_report kwarg
    (f) pipeline/app.py _process Phase 8 wiring + _VideoAnalysisInputs.pole_axis_measurement 신설 (REVIEWS R10) + **`_preflight_label_gate_passed()` env helper 박제 (REVIEWS Cycle 2 NEW HIGH #1 차단)**
    (g) userAnalyses.ts normalize() forceSignalsReport null-guard
    (h) _gemini_enabled() 재사용 (REVIEWS R6 — 신규 _should_keep_local_video helper 영구 차단)
    (i) RunPod requirements google-genai 박제 확인
    (j) **Layer 2 except 분기 preflight ceiling 박제 유지 박제 (REVIEWS Cycle 2 NEW HIGH #2 차단 — Cycle 1 hardcoded 'medium' 박제 영구 제거)**
    (k) 22 신설 단위 test (Layer 2 reuse + env separation + scoped validator + preflight ceiling + Gemini model env) + 11 pipeline 통합 test (4 Layer2 env combination + 3 preflight env state) + 회귀 0

    본 task = SAM build smoke + RunPod Pod env 갱신 + **PREFLIGHT_LABEL_GATE_PASSED env 박제 (코드 변경 0)** + Plan 08-00 박제 preflight 25-timestamp label gate + 5영상 sweep sanity check (belle 운영 작업).
  </what-built>
  <how-to-verify>
    **belle 운영 작업 (manual gates from 08-VALIDATION.md + Plan 08-00 박제 preflight gate):**

    1. **SAM build smoke** (개발자 또는 belle):
       ```bash
       cd /Users/kimtaesung/Dev/SunityMotion/backend
       sam validate
       sam build --use-container
       ```

    2. **Lambda env 동기화** (belle 운영 작업):
       ```bash
       # Cycle 2 신설 env 박제 — FORCE_SIGNALS_LAYER2_ENABLED (REVIEWS R7) + GEMINI_MODEL (Cycle 2 R8) +
       #   PREFLIGHT_LABEL_GATE_PASSED (Cycle 2 NEW HIGH #1 — gate flip plumbing)
       aws lambda update-function-configuration \
         --function-name sunity-motion-pipeline \
         --region ap-northeast-2 \
         --environment 'Variables={...기존 env..., FORCE_SIGNALS_LAYER2_ENABLED=1, GEMINI_MODEL=gemini-2.5-flash, PREFLIGHT_LABEL_GATE_PASSED=0}'
       # 박제 메모: PREFLIGHT_LABEL_GATE_PASSED 박제 초기값 박제 = '0' (false, gate 미실행 박제 명시).
       #   step 4 의 25-timestamp PASS 박제 후 belle 가 본 env 박제 '1' 로 박제 갱신 박제 (코드 변경 0).

       # 검증
       aws lambda get-function-configuration \
         --function-name sunity-motion-pipeline \
         --region ap-northeast-2 \
         --query 'Environment.Variables'
       ```

    3. **RunPod Pod env 갱신** (belle 운영 작업):
       ```bash
       ssh xbdkj1g2ylnfwi-64411701@ssh.runpod.io -i ~/.ssh/id_ed25519

       # Cycle 2 신설 env 박제 (.bashrc 또는 supervisord env)
       grep "FORCE_SIGNALS_LAYER2_ENABLED\|GEMINI_MODEL\|PREFLIGHT_LABEL_GATE_PASSED" ~/.bashrc

       # git pull + restart
       cd /workspace/SunityMotion && git pull origin main
       find /workspace/SunityMotion -name "__pycache__" -type d -exec rm -rf {} + 2>/dev/null
       pkill -9 uvicorn
       cd /workspace/SunityMotion/backend
       PYTHONPATH=shared/python:. uvicorn runpod_inference.server:app --host 0.0.0.0 --port 8000 --workers 1 &

       curl -s -H "X-RunPod-Token: $RUNPOD_AUTH_TOKEN" https://xbdkj1g2ylnfwi-8000.proxy.runpod.net/health | jq
       ```

    4. **Plan 08-00 박제 preflight 25-timestamp label gate 실행 + env flip** (belle 운영 작업, REVIEWS Cycle 2 NEW HIGH #1 차단 plumbing 정합):
       - `.planning/phases/08-jerk-jitter/preflight/preflight_label_template.csv` 박제 25 timestamp 채우기 (5영상 × 5 phase)
       - 각 timestamp 의 delta_ms = abs(belle - layer1) 산출 (수동 또는 spreadsheet)
       - delta_ms <= 200ms 비율 산출 — ≥80% PASS / 미만 FAIL
       - **PASS 시 — env 박제만으로 gate flip 박제 (코드 변경 0, REVIEWS Cycle 2 NEW HIGH #1 차단)**:
         ```bash
         # Lambda
         aws lambda update-function-configuration \
           --function-name sunity-motion-pipeline \
           --region ap-northeast-2 \
           --environment 'Variables={...기존 env..., PREFLIGHT_LABEL_GATE_PASSED=1}'

         # RunPod Pod (.bashrc 박제 갱신 + uvicorn restart)
         # PREFLIGHT_LABEL_GATE_PASSED=1 박제 후 다음 분석부터 Layer 1 confidence='medium' 승급 박제.
         ```
       - **FAIL 시**: PREFLIGHT_LABEL_GATE_PASSED 박제 '0' 박제 유지 (confidence='low' 강제 + warning 'preflight_label_gate_failed'). Plan 08-02 의 LAYER1 threshold 박제 재조정 후 재실행 (researcher 검토 필요).
       - **미실행 시 (default)**: PREFLIGHT_LABEL_GATE_PASSED 박제 unset 시 None 박제 (warning 'preflight_gate_pending') — 분석 죽지 않음 + Layer 1 confidence='low' 박제 보수적.

    5. **mock E2E 1회 실행** (belle 운영 작업):
       - Pod 안에서 _process 직접 호출
       - ref-invert mp4 1개 + mock uid/analysis_id → _process 실행
       - Firestore Console 에서 결과 doc 확인:
         * status='done'
         * result.bodyComparisonReport (Phase 6/7 박제)
         * **result.forceSignalsReport (Phase 8 신설)** — version="1.0" + overallConfidence + phaseBoundaries (5 entries with preflightLabelGatePassed) + axisMetrics (with coordinateSpace='unavailable' 박제 — vertical fallback default) + stabilityMetrics (with jerkUnit='deg_per_sec_cubed') + contactMetrics (with measurementKind / estimatedStable)

    6. **5영상 sweep sanity check** (belle 검수 — sanity check only, NOT calibration ground truth, REVIEWS Suggestions §2 정합):
       - 정은지 reference 5영상 각 mock E2E 실행
       - 각 영상의 forceSignalsReport 출력 hist:
         * axis_metrics severity 분포: 정은지 90%+ 'low' 가설 (vertical fallback 박제 시 distance None — 분포는 line 활성 후 의미)
         * stability_metrics severity 분포: 정은지 90%+ 'low' (jitter / jerk 박제, jerk 는 deg/sec^3 FPS-normalized 박제)
         * contact_metrics estimated_stable: 모든 expected_contact_points 가 True 또는 None (line 없으면 None)
         * phase_boundaries length=5
       - 분포가 합리적이면 sanity check PASS. 아니면 Plan 08-02 의 threshold 재조정 (researcher 검토).

    7. **contact_points.yaml 도메인 검수** (belle / 강사 — Plan 08-01 박제):
       - 5 motion 의 expected_contact_points + kind 분류 (keypoint / segment / region_proxy) 가 폴스포츠 도메인 정합한지 검증
       - 정합 안 되면 yaml 직접 수정 + Plan 08-02 단위 test 재실행

    **본 checkpoint 통과 시 Phase 8 close-out 진입 — `/gsd-verify-work 8` 실행 가능.**
  </how-to-verify>
  <resume-signal>belle 가 "approved" 또는 preflight gate 결과 + sweep 결과 박제 후 진입 신호. preflight FAIL 시 Plan 08-02 threshold 재조정 plan 신설. sweep 분포 비정합 시 (정은지 영상 high severity 다수) 별도 issue 박제.</resume-signal>
</task>

</tasks>

<verification>
## Phase 8 Plan 03 revised Verification

**Goal-backward derived truths (REVIEWS Cycle 2 NEW HIGH 4개 차단 + Cycle 1 production 통합 측면 반영):**

REVIEWS Cycle 2 NEW HIGH 4개 영구 차단 박제:
- **NEW HIGH #1 차단 (R4 carryover plumbing)**: pipeline/app.py 의 `_preflight_label_gate_passed()` env helper 박제 (PREFLIGHT_LABEL_GATE_PASSED env 3-state). belle 가 env 박제만으로 gate flip (코드 변경 0).
- **NEW HIGH #2 차단 (Layer-2 except 분기)**: compute_phase_boundaries 의 Layer 2 except 분기는 `_layer1_confidence_from_preflight()` 박제 ceiling 박제 사용 (hardcoded 'medium' 영구 제거). Layer 2 success 분기도 `_min_confidence(agreement, ceiling)` 박제.
- **NEW HIGH #3 차단 (Firestore invariant)**: `_validate_dict_only_scalars` 본체 변경 영구 0 ([[firestore-nested-array-flat]] project-wide 보존). 신설 `_validate_force_signals_report` scoped validator — forceSignalsReport 만 list[str] 박제 허용.
- **R8 carryover 차단 (Gemini model)**: 실 default 위치 judging/gemini_moment_extractor.py + recognizer.py 둘 다 env-driven (GEMINI_MODEL env). default 'gemini-2.5-flash' (non-EOL). 'gemini-2.0-flash-exp' 박제 영구 grep 0 안전망.

REVIEWS Cycle 1 박제 그대로 보존:
- Layer 2 = recognizer moments reuse (REVIEWS R6 — 신규 GeminiMomentExtractor singleton 영구 제거)
- FORCE_SIGNALS_LAYER2_ENABLED 별도 env flag (REVIEWS R7 — Phase 5 와 분리)
- TechniqueProfile.key_moments 확장 (Layer 2 reuse source)
- complete_analysis(force_signals_report) kwarg 박제
- pipeline/app.py _process wiring + _VideoAnalysisInputs.pole_axis_measurement 신설 (REVIEWS R10)
- _gemini_enabled() 재사용 (REVIEWS R6 — 신규 _should_keep_local_video helper 영구 차단)
- userAnalyses.ts normalize() forceSignalsReport null-guard
- RunPod requirements google-genai 박제 확인
- **22 신설 단위 test + 11 pipeline 통합 test** PASS (4 Layer2 env combination + 3 preflight env state + Cycle 2 NEW HIGH safety nets)
- phase06 / phase07 / phase08 / pipeline 회귀 0
- tsc --noEmit clean
- SAM build smoke (manual checkpoint) + RunPod Pod env 갱신 + Plan 08-00 박제 preflight gate + **PREFLIGHT_LABEL_GATE_PASSED env flip** (belle 운영 작업, 코드 변경 0)

**Manual verification (belle 운영 작업):**

- Plan 08-00 박제 preflight 25-timestamp label gate 실행 (≥80% 일치 → PREFLIGHT_LABEL_GATE_PASSED=1 env 박제 flip → Layer 1 confidence='medium' 자동 승급 박제, 코드 변경 0) — REVIEWS Cycle 2 NEW HIGH #1 차단 정합
- 5영상 sweep sanity check (severity 분포 90%+ low for 정은지 영상) — sanity check only, NOT calibration ground truth (REVIEWS Suggestions §2)
- contact_points.yaml 도메인 검수 (belle/강사 12 enum + kind 분류 정합)
- TS tsc + Firestore output 양립 검증
- Layer 2 Gemini key_moments timestamp sensible check (ref-invert ±300ms belle 육안)

**Regression checks (Plan 08-03 종료 시점):**

- `pytest backend/tests/phase06/ -x` PASS (156 test, `_validate_dict_only_scalars` 본체 변경 0 박제 보장으로 회귀 0)
- `pytest backend/tests/phase07/ -x` PASS (108 test)
- `pytest backend/tests/phase08/ -x` PASS (Plan 08-00 11 + Plan 08-01 신설 + Plan 08-02 30+ + Plan 08-03 22)
- `pytest backend/tests/pipeline/ -x` PASS (Phase 8 wiring + 4 Layer2 env combination + 3 preflight env state)
- `tsc --noEmit clean`
- `sam validate exit 0`
- `sam build --use-container exit 0`
- `grep -c "gemini-2.0-flash-exp" backend/shared/python/sunity_shared/judging/gemini_moment_extractor.py` == 0 (Cycle 2 R8 안전망)
- `grep -c "preflight_label_gate_passed=None" backend/functions/pipeline/app.py` == 0 (Cycle 2 NEW HIGH #1 안전망)
</verification>

<success_criteria>
REVIEWS Cycle 2 NEW HIGH 4개 영구 차단:
- [x] **NEW HIGH #1 (R4 plumbing)**: `_preflight_label_gate_passed()` env helper 박제 — PREFLIGHT_LABEL_GATE_PASSED env 3-state, hardcoded None 박제 영구 제거 (grep == 0 안전망)
- [x] **NEW HIGH #2 (Layer-2 except)**: `_layer1_confidence_from_preflight()` ceiling + `_min_confidence()` 박제 — Layer 2 success/failure 모두 preflight ceiling 박제 강제, hardcoded 'medium' promote 영구 제거
- [x] **NEW HIGH #3 (Firestore invariant)**: `_validate_dict_only_scalars` 본체 변경 영구 0 ([[firestore-nested-array-flat]] 보존) + 신설 scoped validator `_validate_force_signals_report`
- [x] **R8 carryover (Gemini model)**: judging/gemini_moment_extractor.py 실 default 위치 env-driven + recognizer.py 도 env-driven, default 'gemini-2.5-flash' non-EOL, 'gemini-2.0-flash-exp' grep 0 안전망

REVIEWS Cycle 1 박제 그대로 보존:
- [x] Layer 2 = GeminiTechniqueRecognizer moments reuse (REVIEWS R6 — 신규 singleton 영구 차단)
- [x] FORCE_SIGNALS_LAYER2_ENABLED 별도 env flag (REVIEWS R7 — Phase 5 와 분리)
- [x] TechniqueProfile.key_moments 확장
- [x] complete_analysis force_signals_report kwarg 박제
- [x] pipeline/app.py _process wiring + _VideoAnalysisInputs.pole_axis_measurement 신설 (REVIEWS R10)
- [x] _gemini_enabled() 재사용 (REVIEWS R6 helper drift 차단)
- [x] userAnalyses.ts normalize() forceSignalsReport null-guard
- [x] **22 신설 단위 test + 11 pipeline 통합 test** PASS (4 Layer2 env combination + 3 preflight env state + Cycle 2 safety nets)
- [x] phase06 / phase07 / phase08 / pipeline 회귀 0 + tsc clean
- [x] SAM build smoke + RunPod Pod env 갱신 + Plan 08-00 preflight gate + PREFLIGHT_LABEL_GATE_PASSED env flip (belle 운영 manual checkpoint, 코드 변경 0)
- [x] FORCE-01 success criteria #1~4 모두 production 통합 완료 — Phase 8 close-out 진입 가능
</success_criteria>

<output>
Create `.planning/phases/08-jerk-jitter/08-03-SUMMARY.md` when done. Include:
- 3 task commit SHA + 각 task scope
- **REVIEWS Cycle 2 NEW HIGH 4개 차단 박제 결과**:
  · NEW HIGH #1 (R4 plumbing): `_preflight_label_gate_passed()` env helper + PREFLIGHT_LABEL_GATE_PASSED env 3-state
  · NEW HIGH #2 (Layer-2 except): `_layer1_confidence_from_preflight()` ceiling + `_min_confidence()` 박제 — Cycle 1 hardcoded 'medium' 영구 제거
  · NEW HIGH #3 (Firestore invariant): scoped validator `_validate_force_signals_report` 신설, `_validate_dict_only_scalars` 본체 변경 영구 0
  · R8 carryover: judging/gemini_moment_extractor.py + recognizer.py 둘 다 env-driven, default 'gemini-2.5-flash' non-EOL
- Layer 2 recognizer moments reuse 결과 (REVIEWS R6 — 신규 singleton 영구 차단)
- FORCE_SIGNALS_LAYER2_ENABLED 별도 env flag 결과 (REVIEWS R7 — Phase 5 와 분리)
- _VideoAnalysisInputs.pole_axis_measurement 신설 결과 (REVIEWS R10)
- _gemini_enabled() 재사용 결과 (REVIEWS R6 helper drift 차단)
- pipeline wiring 결과 (4 Layer2 env combination + 3 preflight env state 모두 PASS)
- frontend normalize 결과 (tsc clean + forceSignalsReport 7 필드 null-guard)
- SAM build smoke 결과 (sam validate + build --use-container exit 0)
- belle 운영 작업 박제 (RunPod Pod env 갱신 + preflight 25-timestamp label gate + PREFLIGHT_LABEL_GATE_PASSED env flip + 5영상 sweep sanity check + yaml 검수)
- Phase 8 close-out 진입 시그널 (FORCE-01 신호 layer 완성)
- belle chain 다음 진입: Phase 9 (ForceDirectionPattern + 실패 원인 후보 3개) 시그널 박제
- Deviation (Rule 1 fix / Rule 3 environment fix 등) 있으면 박제
</output>
