---
phase: 08-jerk-jitter
plan: 03
type: execute
wave: 3
depends_on: [08-01, 08-02]
files_modified:
  - backend/shared/python/sunity_shared/analysis/force_signals.py
  - backend/shared/python/sunity_shared/firestore_admin.py
  - backend/functions/pipeline/app.py
  - app/src/lib/userAnalyses.ts
  - backend/runpod_inference/requirements.txt
  - backend/tests/phase08/test_compute_phase_boundaries.py
  - backend/tests/phase08/test_firestore_lockstep.py
  - backend/tests/phase08/test_compute_force_signals.py
  - backend/tests/pipeline/test_pipeline_phase8.py
autonomous: false
requirements: [FORCE-01]
subsystem: backend-pipeline-frontend
tags: [phase-08, layer2-gemini, pipeline-wiring, firestore, frontend-normalize, sam-smoke]

scope_sanity_note: "9 파일 중 1 개는 SAM build smoke (manual touch — runpod requirements 라인 추가). 실제 코드 변경은 force_signals.py Layer 2 본체 + firestore_admin.py validator 명세 확장 + pipeline/app.py wiring 1줄 (Phase 8 helper 호출) + userAnalyses.ts normalize + 4 test 파일. 단일 plan ~50% context budget — Phase 6 Plan 02 (pipeline wiring + firestore + frontend) 동일 패턴."

must_haves:
  truths:
    - "compute_phase_boundaries() 의 Layer 2 Gemini 본체 활성 — _should_invoke_layer2 가 True (motion_id != None + gemini_extractor != None + video_uri 가 로컬 path) 시 GeminiMomentExtractor.extract_key_moments() 호출 + assign_frame_indices() 호출 + _layer2_gemini_boundaries (Gemini 4 moment → 5-phase mapping) + _confidence_from_agreement (timestamp 차이 <=2 frames → 'high' / <=5 frames → 'medium' + 'layer_disagreement_minor' / >5 frames → 'low' + 'layer_disagreement_major' + Layer 1 사용) — RESEARCH §5-Phase Motion Split Layer 2 정확히 정합. per D-08-A2 + A11"
    - "Layer 2 호출 실패 (RuntimeError / ValueError / 120s timeout 등) 시 graceful fallback — Layer 1 단독 + confidence='medium' + warning='layer2_call_failed' (RESEARCH §Code Examples Example 3 정합)"
    - "gemini_moment_extractor.py 코드 변경 0 — Plan 01-13 박제 모듈 reuse only (extract_key_moments + assign_frame_indices + DEFAULT_GEMINI_MODEL + _cache + Parameter Store API 키 모두 박제됨)"
    - "_validate_dict_only_scalars 명세 확장 — list[str] 허용 (warnings list[str] 가 list[dict-of-scalars-only] 안에 있을 때도 PASS). Phase 6 박제 validator (firestore_admin.py:104-123) 수정 + 기존 phase06 156 test 회귀 0 + 신설 phase08 schema 정합. per RESEARCH Open Question 1 Option A"
    - "firestore_admin.complete_analysis(force_signals_report: dict | None = None) kwarg 신설 — Phase 6 박제 body_comparison_report / body_normalization_profile 패턴 정합. _validate_flat_dict_no_nested_array 통과 강제. result.forceSignalsReport 위치에 저장 (top-level X — Phase 6 body_comparison_report 의 result 내부 박제 패턴 정합)"
    - "pipeline/app.py::_process Phase 8 wiring 1줄 — compare_body_profiles 호출 직후 (line 1000 근처) force_signals.compute_force_signals 호출 + force_signals_dict 변환 + complete_analysis 의 force_signals_report kwarg 주입. mode 분기 무관 (mode1 / mode3_first / mode3_progress 모두 적용)"
    - "_get_gemini_moment_extractor() singleton helper 박제 — RECOGNIZER_BACKEND='gemini' env 시만 lazy 활성. Plan 01-13 박제 GeminiMomentExtractor 직접 reuse"
    - "_extract_video_analysis_inputs(keep_local_video=True) 호출 (Phase 8 wiring path) — Gemini File API 로컬 path 제약 정합. caller (pipeline._process) 가 finally 블록에서 unlink 책임 (기존 박제 patterns 정합)"
    - "app/src/lib/userAnalyses.ts::normalize() 의 result spread 안 forceSignalsReport 7 신설 필드 null-guard — Phase 7 WR-02 B1 패턴 정합 (immutable spread + ?? null fallback). TS interface 는 optional 유지 (Plan 08-01 박제, normalize() 가 compat layer)"
    - "backend/runpod_inference/requirements.txt 검토 — Plan 01-13 박제로 google-genai SDK 이미 설치됨 (변경 0 가능성). 만약 없으면 google-genai >= 0.1 신설 + RunPod Pod 환경 정합 박제 (runpod-gpu-env memory 정합)"
    - "pipeline 통합 test (test_pipeline_phase8.py) — _process mock E2E (Gemini ON path + Gemini OFF path 둘 다). force_signals_report 가 complete_analysis 의 kwarg 로 전달 검증 + camelCase 변환 + _validate_dict_only_scalars 명세 확장 후 PASS"
    - "SAM build smoke (manual checkpoint) — cd backend && sam validate exit 0 + sam build --use-container exit 0. RunPod Pod 갱신은 belle 운영 작업 (manual gate, 08-VALIDATION.md §"Manual-Only Verifications" 정합)"
  artifacts:
    - path: backend/shared/python/sunity_shared/analysis/force_signals.py
      provides: "Layer 2 Gemini 본체 활성 — _layer2_gemini_boundaries (Gemini 4 moment → 5-phase mapping) + _map_moments_to_5phase + _confidence_from_agreement + _promote_layer1 helper 박제. compute_phase_boundaries 의 Layer 2 분기 활성화. graceful fallback 박제 (RuntimeError / ValueError catch → Layer 1 단독 + warning)"
      contains:
        - "_layer2_gemini_boundaries"
        - "_map_moments_to_5phase"
        - "_confidence_from_agreement"
        - "_promote_layer1"
        - "extract_key_moments"
        - "assign_frame_indices"
        - "layer2_call_failed"
        - "layer_disagreement_minor"
        - "layer_disagreement_major"
        - "gemini_assisted"
    - path: backend/shared/python/sunity_shared/firestore_admin.py
      provides: "_validate_dict_only_scalars 명세 확장 — list[str] 허용 (warnings list[str] 가 list[dict-of-scalars-only] 안에서도 PASS). + complete_analysis(force_signals_report: dict | None = None) kwarg 신설. 기존 Phase 6 패턴 (body_comparison_report / body_normalization_profile) 정합. _validate_flat_dict_no_nested_array(force_signals_report, path='forceSignalsReport') 통과 강제. result.forceSignalsReport 위치 저장"
      contains:
        - "force_signals_report: dict | None = None"
        - "_validate_flat_dict_no_nested_array"
        - "forceSignalsReport"
        - "list[str]"
        - "_validate_dict_only_scalars"
    - path: backend/functions/pipeline/app.py
      provides: "Phase 8 wiring 1줄 — compare_body_profiles 호출 직후 force_signals.compute_force_signals 호출 + force_signals_dict 변환 + complete_analysis kwarg 주입. _get_gemini_moment_extractor singleton 박제 (RECOGNIZER_BACKEND='gemini' env 활성 조건). _extract_video_analysis_inputs(keep_local_video=True) 호출 path 박제 (Layer 2 활성 시)"
      contains:
        - "force_signals"
        - "compute_force_signals"
        - "_get_gemini_moment_extractor"
        - "force_signals_report"
        - "GeminiMomentExtractor"
        - "keep_local_video=True"
    - path: app/src/lib/userAnalyses.ts
      provides: "normalize() 의 result spread 안 forceSignalsReport null-guard — Phase 7 WR-02 B1 패턴 (immutable spread + ?? null fallback). TS interface 는 optional 유지 (Plan 08-01 박제), normalize() 가 compat layer"
      contains:
        - "forceSignalsReport"
        - "?? null"
    - path: backend/runpod_inference/requirements.txt
      provides: "google-genai SDK 박제 확인 (Plan 01-13 박제 이미 존재 가능성 높음). 없으면 신설 google-genai >= 0.1"
      contains:
        - "google-genai"
    - path: backend/tests/phase08/test_compute_phase_boundaries.py
      provides: "Layer 2 wiring 신설 test 5 + 기존 5 (Plan 08-02) = 10 test. Layer 1+Layer 2 일치 → high confidence + 'gemini_assisted' source / Layer 1+Layer 2 불일치 minor → medium + warning / Layer 1+Layer 2 불일치 major → low + Layer 1 사용 / Gemini RuntimeError → fallback Layer 1 + warning / motion_id=None → Layer 2 hook 비호출 검증"
      contains:
        - "def test_layer1_layer2_agreement_high_confidence"
        - "def test_layer1_layer2_disagreement_minor_warning"
        - "def test_layer1_layer2_disagreement_major_falls_back_to_layer1"
        - "def test_layer2_runtime_error_falls_back_to_layer1"
        - "def test_layer2_hook_invoked_only_when_local_path"
    - path: backend/tests/phase08/test_firestore_lockstep.py
      provides: "Firestore validator 명세 확장 test — _validate_dict_only_scalars 가 list[str] 안 허용. ForceSignalsReport dict (camelCase 변환 후) 가 _validate_flat_dict_no_nested_array 통과. 기존 phase06 BodyComparisonReport 패턴 회귀 0"
      contains:
        - "def test_validator_passes_force_signals_dict"
        - "def test_validator_passes_warnings_list_of_str_inside_metric_dict"
        - "def test_validator_rejects_nested_list_inside_metric_dict"
        - "def test_camel_case_conversion_force_signals_report"
    - path: backend/tests/phase08/test_compute_force_signals.py
      provides: "Plan 08-02 의 6 통합 test + Layer 2 wiring 통합 test 신설 — gemini_extractor=mock 시 Layer 2 활성 + agreement 'high' confidence + 'gemini_assisted' source"
      contains:
        - "def test_layer2_gemini_activates_with_mock_extractor"
        - "def test_layer2_agreement_high_confidence_propagates_to_overall"
    - path: backend/tests/pipeline/test_pipeline_phase8.py
      provides: "pipeline 통합 test — _process mock E2E. Gemini OFF (RECOGNIZER_BACKEND != 'gemini') path: force_signals.compute_force_signals 호출 + Layer 1 단독 + complete_analysis 의 force_signals_report kwarg 전달. Gemini ON path: GeminiMomentExtractor singleton + Layer 2 활성 + force_signals_report 전달. mode 분기 무관 (mode1 / mode3_first / mode3_progress 모두 force_signals 산출)"
      contains:
        - "def test_pipeline_phase8_gemini_off_path"
        - "def test_pipeline_phase8_gemini_on_path"
        - "def test_pipeline_phase8_mode1_force_signals_emitted"
        - "def test_pipeline_phase8_mode3_first_force_signals_emitted"
        - "def test_pipeline_phase8_complete_analysis_force_signals_kwarg"
  key_links:
    - from: backend/shared/python/sunity_shared/analysis/force_signals.py
      to: backend/shared/python/sunity_shared/judging/gemini_moment_extractor.py
      via: "_layer2_gemini_boundaries: GeminiMomentExtractor.extract_key_moments() + assign_frame_indices() 호출 — Plan 01-13 박제 모듈 reuse only (코드 변경 0)"
      pattern: "extract_key_moments|assign_frame_indices"
    - from: backend/functions/pipeline/app.py
      to: backend/shared/python/sunity_shared/analysis/force_signals.py
      via: "compute_force_signals 호출 1줄 + _get_gemini_moment_extractor singleton — compare_body_profiles 직후 wiring"
      pattern: "compute_force_signals|_get_gemini_moment_extractor"
    - from: backend/functions/pipeline/app.py
      to: backend/shared/python/sunity_shared/firestore_admin.py
      via: "complete_analysis(force_signals_report=force_signals_dict) kwarg 주입 — _dataclass_to_camel_case_dict 자동 변환 후"
      pattern: "force_signals_report"
    - from: app/src/lib/userAnalyses.ts
      to: app/src/types/analysis.ts
      via: "normalize() 가 forceSignalsReport null-guard — Plan 08-01 박제 optional interface compat layer (Phase 7 WR-02 B1 패턴)"
      pattern: "forceSignalsReport"
---

<objective>
Phase 8 vertical slice (3/3) — **Layer 2 Gemini 활성 + pipeline wiring + Firestore + frontend normalize + SAM smoke**. Plan 08-02 박제 force_signals.py 본체 위에 production 통합:

(a) **Layer 2 Gemini 활성** — _layer2_gemini_boundaries / _map_moments_to_5phase / _confidence_from_agreement / _promote_layer1 helper 박제 (Plan 08-02 의 Layer 2 hook 활성화). Plan 01-13 박제 gemini_moment_extractor.py reuse only (코드 변경 0). graceful fallback 박제.
(b) **_validate_dict_only_scalars 명세 확장** — list[str] 허용 (warnings list[str] 가 list[dict-of-scalars-only] 안 박제 PASS). RESEARCH Open Question 1 Option A.
(c) **complete_analysis kwarg 신설** — force_signals_report (Phase 6 박제 body_comparison_report 패턴 정합).
(d) **pipeline/app.py::_process wiring** — compare_body_profiles 직후 1줄 + _get_gemini_moment_extractor singleton + _extract_video_analysis_inputs(keep_local_video=True) path. mode 분기 무관.
(e) **frontend normalize** — userAnalyses.ts 의 forceSignalsReport null-guard (Phase 7 WR-02 B1 패턴).
(f) **SAM build smoke** — sam validate + sam build --use-container exit 0. RunPod Pod 갱신은 belle 운영 작업 (manual gate).
(g) **5 test 신설** — Layer 2 wiring + validator 명세 확장 + camelCase 변환 + pipeline 통합 + Layer 2 agreement.

Purpose: FORCE-01 success criteria #1~4 모두의 production 통합 완료 + Phase 8 close-out.
Output: force_signals.py Layer 2 본체 활성 + firestore_admin.py validator + complete_analysis kwarg + pipeline wiring 1줄 + userAnalyses.ts normalize + 4 test 파일 + SAM smoke.

**박제 메모 정합:** [[firestore-nested-array-flat]] (validator list[str] 허용 + nested list reject 유지) + [[mvp-simple-pilot-quality]] (Gemini OFF path graceful fallback — 분석 죽지 않음) + [[feedback-analysis-first]] (Layer 2 disagreement → low confidence + Layer 1 사용) + [[no-baekje-filler]] (warning code 영어 snake_case) + [[runpod-gpu-env]] (Pod env RECOGNIZER_BACKEND=gemini + google-genai SDK 박제 정합).

per D-08-A2 (Layer 2 활성 + agreement 룰 + graceful fallback) + D-08-U1 (가림 스무딩 production 통합) + D-08-U2 (confidence 필드 production wiring) + D-08-U3 (Firestore 저장 schema 정합) + A11 (Plan 01-13 verdict 와 Phase 8 use case 분리 — timestamp 만 사용, IPSF 비교 X).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-jerk-jitter/08-CONTEXT.md
@.planning/phases/08-jerk-jitter/08-RESEARCH.md
@.planning/phases/08-jerk-jitter/08-VALIDATION.md
@.planning/phases/08-jerk-jitter/08-01-PLAN.md
@.planning/phases/08-jerk-jitter/08-02-PLAN.md
@.planning/phases/06-coaching/06-02-PLAN.md
@CLAUDE.md
@backend/shared/python/sunity_shared/analysis/force_signals.py
@backend/shared/python/sunity_shared/judging/gemini_moment_extractor.py
@backend/shared/python/sunity_shared/firestore_admin.py
@backend/functions/pipeline/app.py
@backend/runpod_inference/server.py
@backend/runpod_inference/requirements.txt
@app/src/lib/userAnalyses.ts
@app/src/types/analysis.ts
@backend/tests/phase06/test_compare_body_profiles_pipeline_wiring.py
@backend/tests/pipeline/
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Layer 2 Gemini API | 외부 LLM (multimodal video → key_moment timestamp). Plan 01-13 박제 정규식 가드 3 카테고리 (좌표/점수/판단 차단) — 본 plan 변경 0. |
| Firestore write | Lambda → Firestore Admin SDK. 신규 force_signals_report dict 추가. PII 0 (관절 좌표 + warning code 만). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | S (Spoofing) | Gemini 응답이 좌표/점수/심사 판단 포함 시도 | mitigate | 정규식 가드 3 카테고리 (Plan 01-13 박제, gemini_moment_extractor.py:_enforce_no_coordinate_or_score 자동 발동). Phase 8 코드 변경 0 — reuse only. |
| T-08-03-02 | I (Information disclosure) | Gemini API 키 leak (env / log / Firestore) | mitigate | Parameter Store SecureString (`/sunity/motion/gemini-api-key`, Plan 01-13 박제). `log.debug` 만 (key 본문 0). `.env` 하드코딩 금지 (CLAUDE.md §3). Phase 8 변경 0. |
| T-08-03-03 | D (Denial of Service / Gemini 무응답) | _layer2_gemini_boundaries Gemini 호출 | mitigate | Plan 01-13 박제: 120s max wait (FILE_API processing) + `RuntimeError` raise. force_signals.py 가 try/except 박제 → Layer 1 단독 fallback + warning 'layer2_call_failed'. 분석 죽지 않음 (D-08-A4 정합). |
| T-08-03-04 | D (DoS / Firestore nested-array crash) | force_signals_report dict 의 list[str] warnings | mitigate | _validate_dict_only_scalars 명세 확장 — list[str] 허용 (recursive validator). 기존 nested list 금지 패턴 유지 (test_validator_rejects_nested_list_inside_metric_dict 검증). |
| T-08-03-05 | T (Tampering / pipeline wiring drift) | _process Phase 8 wiring 1줄 + complete_analysis kwarg | mitigate | pipeline 통합 test (test_pipeline_phase8.py) — Gemini ON / OFF / mode 분기 모두 검증. mock E2E 패턴 (Phase 6 박제). |
| T-08-03-06 | I (Information disclosure / 영상 임시 파일 누수) | _extract_video_analysis_inputs(keep_local_video=True) 의 local_video_path | mitigate | pipeline._process 의 finally 블록에서 `Path(local_video_path).unlink(missing_ok=True)` 박제 (Phase 6 박제 patterns 정합). Plan 01-13 박제 + RESEARCH Open Question 2 정합. |
| T-08-03-07 | T (Tampering / Firestore 저장 schema drift) | force_signals_report 의 camelCase 변환 + flat validator | mitigate | _dataclass_to_camel_case_dict (Phase 6 박제 C8) + _validate_flat_dict_no_nested_array (Phase 6 박제 W5) 자동 적용. test_firestore_lockstep.py 가 검증. |
| T-08-03-SC | Tampering | npm/pip/cargo installs | mitigate | google-genai 는 Plan 01-13 박제 (RunPod Pod 환경 정합). 신규 외부 의존 0 가능성 — requirements.txt 확인 후 박제. RESEARCH §Package Legitimacy Audit "N/A (no new packages)". slopcheck 불요. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Layer 2 Gemini 본체 활성 + _validate_dict_only_scalars 명세 확장 + 신설 단위 test</name>
  <files>
    backend/shared/python/sunity_shared/analysis/force_signals.py
    backend/shared/python/sunity_shared/firestore_admin.py
    backend/tests/phase08/test_compute_phase_boundaries.py
    backend/tests/phase08/test_firestore_lockstep.py
    backend/tests/phase08/test_compute_force_signals.py
  </files>
  <read_first>
    backend/shared/python/sunity_shared/analysis/force_signals.py (Plan 08-02 박제 5 public 함수 + Layer 2 hook 시그너처)
    backend/shared/python/sunity_shared/judging/gemini_moment_extractor.py (line 80-300 — GeminiMomentExtractor + KeyMoment + extract_key_moments + assign_frame_indices 시그너처)
    backend/shared/python/sunity_shared/firestore_admin.py (line 45-170 — _validate_flat_dict_no_nested_array + _validate_dict_only_scalars + complete_analysis 박제 패턴)
    backend/tests/phase08/conftest.py (mock_gemini_extractor fixture)
    .planning/phases/08-jerk-jitter/08-RESEARCH.md §"5-Phase Motion Split — Layer 2 Gemini Integration" + §"Code Examples Example 3" + §"Module Structure + Pipeline Wiring + Firestore Schema"
    .planning/phases/06-coaching/06-02-SUMMARY.md (W5 validator + camelCase 변환 패턴)
  </read_first>
  <behavior>
    - **Layer 2 본체 활성** (force_signals.py):
      * 신설 helper `_layer2_gemini_boundaries(layer1, video_uri, motion_id, fps, frames_total, gemini_extractor) -> list[PhaseBoundary]` — RESEARCH §"Code Examples Example 3" + §"Mapping rule" 정확히 정합:
        - `moments = gemini_extractor.extract_key_moments(video_uri=video_uri, motion=motion_id)` 호출
        - `moments = assign_frame_indices(moments, fps=fps, frames_total=frames_total)` 호출
        - `_map_moments_to_5phase(layer1, moments) -> list[PhaseBoundary]` 호출 (RESEARCH §"Mapping rule" 정확히 정합)
      * 신설 helper `_map_moments_to_5phase(layer1, moments) -> list[PhaseBoundary]` — Gemini 4 moment → 5-phase boundary mapping:
        - setup.frame → entry.end / lock.start
        - hold.frame → final_shape.end / hold.start
        - peak.frame → final_shape 의 representative (boundary 변경 X)
        - release.frame → hold.end
        - transition phase boundary 는 Gemini 가 제공 X → Layer 1 의 transition 유지
        - 5 PhaseBoundary 박제 (entry / lock / transition / final_shape / hold) 반환
      * 신설 helper `_confidence_from_agreement(layer1, layer2) -> tuple[MetricConfidence, list[str]]` — RESEARCH §"Agreement check" 정확히 정합:
        - 각 phase 의 (start_frame_idx, end_frame_idx) 차이 abs 의 max 산출
        - <= LAYER_AGREEMENT_TOLERANCE_FRAMES (2) → ('high', [])
        - <= LAYER_DISAGREEMENT_MAJOR_FRAMES (5) → ('medium', ['layer_disagreement_minor'])
        - > 5 frames or ordering 뒤집힘 → ('low', ['layer_disagreement_major']) — caller 가 Layer 1 사용 분기
        - layer2 가 None → ('medium', ['layer2_unavailable'])
      * 신설 helper `_promote_layer1(boundary, confidence, source, warnings) -> PhaseBoundary` — PhaseBoundary 재구성 (start_ms / end_ms resolve + confidence / source / warnings 박제)
      * **compute_phase_boundaries() Layer 2 분기 활성화** (Plan 08-02 의 hook):
        ```
        if _should_invoke_layer2(motion_id, gemini_extractor, video_uri):
            try:
                layer2 = _layer2_gemini_boundaries(layer1, video_uri, motion_id, fps, len(pose_frames), gemini_extractor)
                confidence, warnings = _confidence_from_agreement(layer1, layer2)
                if confidence == "low":
                    # major disagreement — Layer 1 사용
                    return [_promote_layer1(b, confidence, "heuristic", warnings) for b in layer1]
                else:
                    return [_promote_layer1(b, confidence, "gemini_assisted", warnings) for b in layer2]
            except (RuntimeError, ValueError) as exc:
                log.warning("Layer 2 Gemini failed — Layer 1 단독: %s", exc)
                return [_promote_layer1(b, "medium", "heuristic", ["layer2_call_failed"]) for b in layer1]
        else:
            # Layer 1 only (Plan 08-02 박제 본체 유지)
            return [_promote_layer1(b, "medium" if motion_id else "low", "heuristic" if motion_id else "heuristic_fallback", []) for b in layer1]
        ```
    - **_validate_dict_only_scalars 명세 확장** (firestore_admin.py):
      * 기존 본체 (line 104-123): list[dict] 의 dict 안에서 nested list / dict 금지 → 모든 nested 금지.
      * 신설 명세: **list[str] / list[int] / list[float] / list[bool] 허용** (recursive — sub-scalar list 만, list[list] / list[dict] reject 유지). 즉 dict 안 (key, value) 순회 시 value 가:
        - scalar (str, int, float, bool, None) → PASS (기존)
        - list of scalar (list[str/int/float/bool]) → **NEW PASS** (sub-element 모두 scalar 검증 후)
        - list[list] / list[dict] → reject (기존 정신 유지)
        - dict → reject (기존 — list-of-dict entry 안 nested dict 금지)
      * 신설 본체:
        ```python
        def _validate_dict_only_scalars(d, *, path):
            if not isinstance(d, dict):
                raise TypeError(...)
            for k, v in d.items():
                sub_path = f"{path}.{k}"
                if v is None or isinstance(v, (str, int, float, bool)):
                    continue
                if isinstance(v, list):
                    for i, item in enumerate(v):
                        if item is None or isinstance(item, (str, int, float, bool)):
                            continue
                        raise TypeError(
                            f"{sub_path}[{i}] must be scalar (firestore-nested-array-flat — "
                            f"list-of-dict entry 안에서는 list[scalar] 만 허용): got {type(item).__name__}"
                        )
                    continue
                raise TypeError(
                    f"{sub_path} must be scalar or list[scalar] (firestore-nested-array-flat — "
                    f"list-of-dict entry 안에서는 nested dict / list[non-scalar] 금지): got {type(v).__name__}"
                )
        ```
      * 회귀 0 — 기존 phase06 BodyComparisonReport.findings (list[dict-of-scalars-only]) 그대로 PASS. 신설 phase08 ForceSignalsReport.axis_metrics 안 warnings (list[str]) PASS.
    - **complete_analysis(force_signals_report: dict | None = None) kwarg 신설** (firestore_admin.py):
      * Phase 6 박제 body_comparison_report / body_normalization_profile 패턴 정확히 mirror:
        ```python
        if force_signals_report is not None:
            _validate_flat_dict_no_nested_array(
                force_signals_report, path="forceSignalsReport"
            )
            payload["result"]["forceSignalsReport"] = force_signals_report
        ```
      * 저장 위치 = `result.forceSignalsReport` (Phase 6 의 body_comparison_report 와 동일 위치 — AnalysisResult 안)
    - **신설 단위 test**:
      * test_compute_phase_boundaries.py 신설 5 test:
        - test_layer1_layer2_agreement_high_confidence — mock_gemini_extractor 가 Layer 1 boundary 와 <=2 frames 차이 KeyMoment 반환 → confidence='high' + source='gemini_assisted'
        - test_layer1_layer2_disagreement_minor_warning — <=5 frames 차이 → 'medium' + warning='layer_disagreement_minor'
        - test_layer1_layer2_disagreement_major_falls_back_to_layer1 — >5 frames 차이 → 'low' + warning='layer_disagreement_major' + source='heuristic' (Layer 1 사용)
        - test_layer2_runtime_error_falls_back_to_layer1 — mock_gemini_extractor.extract_key_moments raises RuntimeError → Layer 1 단독 + warning='layer2_call_failed'
        - test_layer2_hook_invoked_only_when_local_path — video_uri='s3://...' / 'http://...' → Layer 2 분기 진입 X (gemini_extractor=mock 도 호출 안 됨)
      * test_firestore_lockstep.py 신설 4 test:
        - test_validator_passes_force_signals_dict — ForceSignalsReport(...) → _dataclass_to_camel_case_dict → _validate_flat_dict_no_nested_array PASS
        - test_validator_passes_warnings_list_of_str_inside_metric_dict — {phase:"hold", warnings:["occlusion_high_in_phase"]} dict 가 _validate_dict_only_scalars PASS (신설 명세)
        - test_validator_rejects_nested_list_inside_metric_dict — {phase:"hold", nested:[[1,2],[3,4]]} → TypeError raise (기존 정신 유지)
        - test_camel_case_conversion_force_signals_report — ForceSignalsReport.phase_boundaries → "phaseBoundaries" + axis_metrics → axisMetrics 변환
      * test_compute_force_signals.py 신설 2 test:
        - test_layer2_gemini_activates_with_mock_extractor — mock_gemini_extractor + motion_id='ref-invert' + video_uri='/tmp/x.mp4' → Layer 2 활성 + 5 phase boundary 의 source='gemini_assisted'
        - test_layer2_agreement_high_confidence_propagates_to_overall — Layer 2 agreement 'high' + 모든 metric 'high' → overall_confidence='high'
  </behavior>
  <action>
    Plan 08-02 의 force_signals.py 의 Layer 2 hook 분기를 활성화. RESEARCH §"Code Examples Example 3" 의 try/except + agreement check code 정확히 박제. 4 신설 private helper (_layer2_gemini_boundaries / _map_moments_to_5phase / _confidence_from_agreement / _promote_layer1) 박제.

    Critical reuse: `from ..judging.gemini_moment_extractor import GeminiMomentExtractor, assign_frame_indices` (Plan 01-13 박제 모듈 코드 변경 0 — import 만). TYPE_CHECKING 아래 import (lazy) — actual 호출 시점에 module level import.

    `firestore_admin.py` 의 `_validate_dict_only_scalars` 본체 (line 104-123) 를 위 behavior 의 신설 본체로 교체. **명세 확장 단일 atomic commit** — 본 commit 으로 (a) phase06 156 test 회귀 0 + (b) phase08 force_signals_report dict validator 통과 + (c) 신설 test 4 PASS 동시 검증.

    `complete_analysis` 의 신설 kwarg `force_signals_report: dict | None = None` 박제. Phase 6 의 `body_comparison_report` 박제 위치 (line 160-164) 와 동일 패턴 박제. 저장 위치 = `result.forceSignalsReport` (camelCase, Phase 6 body_comparison_report 와 동일 nesting level).

    신설 11 test 박제 (5 phase_boundaries + 4 firestore_lockstep + 2 force_signals). pytest fixture 활용 (mock_gemini_extractor + synthetic_body_profile + load_pose_frames_from_fixture).

    Critical 회귀 검증: 본 commit 후 `pytest backend/tests/phase06/ backend/tests/phase07/ backend/tests/pipeline/` 모두 PASS — _validate_dict_only_scalars 명세 확장으로 기존 BodyComparisonReport / pipeline 패턴 영향 0 강제.

    per RESEARCH §"Layer 2 Gemini Integration" + §"Code Examples Example 3" + Open Question 1 Option A + D-08-A2 + D-08-U3.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && PYTHONPATH=backend/shared/python:. backend/.venv/bin/python3 -m pytest backend/tests/phase08/ backend/tests/phase06/ backend/tests/phase07/ -x</automated>
  </verify>
  <acceptance_criteria>
    - File modified: force_signals.py 의 compute_phase_boundaries Layer 2 분기 활성화 (raise NotImplementedError 또는 무조건 Layer 1 단독 분기 제거)
    - File modified: firestore_admin.py 의 _validate_dict_only_scalars 본체 신설 명세 박제 + complete_analysis 의 force_signals_report kwarg 박제
    - Source: `grep -c "_layer2_gemini_boundaries\|_map_moments_to_5phase\|_confidence_from_agreement" backend/shared/python/sunity_shared/analysis/force_signals.py` >= 3
    - Source: `grep -c "extract_key_moments\|assign_frame_indices" backend/shared/python/sunity_shared/analysis/force_signals.py` >= 2
    - Source: `grep -c "force_signals_report" backend/shared/python/sunity_shared/firestore_admin.py` >= 2
    - Source: `grep -c "forceSignalsReport" backend/shared/python/sunity_shared/firestore_admin.py` >= 1
    - Behavior (Layer 2 wiring): test_layer1_layer2_agreement_high_confidence + test_layer1_layer2_disagreement_minor_warning + test_layer1_layer2_disagreement_major_falls_back_to_layer1 + test_layer2_runtime_error_falls_back_to_layer1 + test_layer2_hook_invoked_only_when_local_path 5 test PASS
    - Behavior (validator 확장): test_validator_passes_warnings_list_of_str_inside_metric_dict PASS + test_validator_rejects_nested_list_inside_metric_dict PASS (기존 정신 유지)
    - Behavior (camelCase): test_camel_case_conversion_force_signals_report PASS
    - Behavior (회귀 0): phase06 156 test + phase07 108 test 모두 PASS (validator 명세 확장 후도 기존 BodyComparisonReport / BodyComparisonFinding 회귀 X)
    - Behavior: pytest exit 0 (위 verify command)
  </acceptance_criteria>
  <done>
    Layer 2 Gemini 본체 활성 완료 (Plan 01-13 reuse, 코드 변경 0). _validate_dict_only_scalars 명세 확장 (list[str] 허용) + complete_analysis force_signals_report kwarg 박제. 11 신설 test PASS + phase06/07 회귀 0. Task 2 가 pipeline wiring + frontend normalize + SAM smoke 박제.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: pipeline/app.py wiring + frontend userAnalyses.ts normalize + pipeline 통합 test + RunPod requirements 검토</name>
  <files>
    backend/functions/pipeline/app.py
    app/src/lib/userAnalyses.ts
    backend/runpod_inference/requirements.txt
    backend/tests/pipeline/test_pipeline_phase8.py
  </files>
  <read_first>
    backend/functions/pipeline/app.py (line 750-1010 _process Phase 6 wiring 패턴 + _extract_video_analysis_inputs + _dataclass_to_camel_case_dict + complete_analysis 호출)
    backend/shared/python/sunity_shared/analysis/force_signals.py (Task 1 박제 완료 상태)
    backend/shared/python/sunity_shared/firestore_admin.py (Task 1 박제 force_signals_report kwarg)
    backend/shared/python/sunity_shared/judging/gemini_moment_extractor.py (GeminiMomentExtractor 시그너처)
    backend/runpod_inference/requirements.txt (Plan 01-13 박제 google-genai 확인)
    backend/runpod_inference/server.py (Phase 6 박제 RunPod wiring 패턴 — pipeline 모듈 reuse 정합 확인)
    app/src/lib/userAnalyses.ts (line 1-100 — Phase 7 WR-02 B1 패턴: normalize() 의 result spread + bodyComparisonReport null-guard)
    app/src/types/analysis.ts (Plan 08-01 박제 forceSignalsReport optional interface)
    backend/tests/phase06/test_compare_body_profiles_pipeline_wiring.py (Phase 6 pipeline 통합 test 패턴 mirror)
    .planning/phases/06-coaching/06-02-PLAN.md (R3 + R4 + R8 wiring 패턴 reference)
    .planning/phases/08-jerk-jitter/08-RESEARCH.md §"Module Structure + Pipeline Wiring + Firestore Schema" + §"Frontend normalize" + Open Question 2
  </read_first>
  <behavior>
    - **pipeline/app.py wiring** (compare_body_profiles 호출 직후 — line ~1000 근처):
      * 모듈 top-level 신설:
        ```python
        from sunity_shared.analysis import force_signals as fs
        from sunity_shared.judging.gemini_moment_extractor import GeminiMomentExtractor

        _GEMINI_MOMENT_EXTRACTOR: GeminiMomentExtractor | None = None

        def _get_gemini_moment_extractor() -> GeminiMomentExtractor | None:
            """Singleton — RECOGNIZER_BACKEND='gemini' env 시만 활성 (Plan 01-13 박제 정합)."""
            global _GEMINI_MOMENT_EXTRACTOR
            if os.environ.get("RECOGNIZER_BACKEND", "fallback") != "gemini":
                return None
            if _GEMINI_MOMENT_EXTRACTOR is None:
                _GEMINI_MOMENT_EXTRACTOR = GeminiMomentExtractor()
            return _GEMINI_MOMENT_EXTRACTOR
        ```
      * _process 안 compare_body_profiles 호출 직후 (line ~990, body_comparison_report_dict + body_normalization_profile_dict 변환 사이 또는 직전) Phase 8 wiring 1줄 박제:
        ```python
        # Phase 8 (Plan 08-03 wiring) — force signals 산출. mode 분기 무관 (mode1/mode3 모두).
        force_signals_report = fs.compute_force_signals(
            pose_frames=inputs.pose_frames,
            pole_axis=inputs.pole_axis,
            body_profile=student_profile,
            angles=angles,
            motion_id=getattr(profile, "motion_id", None),
            gemini_extractor=_get_gemini_moment_extractor(),
            video_uri=str(inputs.local_video_path) if inputs.local_video_path else None,
            fps=9.0,
        )
        force_signals_dict = _dataclass_to_camel_case_dict(force_signals_report)
        ```
      * complete_analysis 호출에 `force_signals_report=force_signals_dict` kwarg 추가
      * Critical: `_extract_video_analysis_inputs` 호출 위치 (line ~752 근처 mode1 + mode3 branches) 의 `keep_local_video=` 인자를 conditional 변경 — RECOGNIZER_BACKEND='gemini' 일 때만 True. Phase 6 박제 default=False 유지 (기존 caller 영향 0). 본 변경은 mode1 + mode3 (mode3_first / mode3_progress) path 모두 적용. finally 블록의 `Path(local_video_path).unlink(missing_ok=True)` 기존 패턴 유지 (RESEARCH Open Question 2 정합).
    - **frontend userAnalyses.ts normalize** — Phase 7 WR-02 B1 패턴 (immutable spread + null-guard):
      * 기존 line 53-100 근처 `if (result?.bodyComparisonReport)` 블록과 동일 패턴 박제:
        ```typescript
        // Phase 8 (Plan 08-03) — forceSignalsReport null-guard. AnalysisResult.forceSignalsReport 는 optional
        // (Plan 08-01 박제), normalize() 가 compat layer.
        if (result?.forceSignalsReport) {
            const report = result.forceSignalsReport;
            result = {
                ...result,
                forceSignalsReport: {
                    version: report.version ?? "1.0",
                    overallConfidence: report.overallConfidence ?? "medium",
                    warnings: report.warnings ?? [],
                    phaseBoundaries: report.phaseBoundaries ?? [],
                    axisMetrics: report.axisMetrics ?? [],
                    stabilityMetrics: report.stabilityMetrics ?? [],
                    contactMetrics: report.contactMetrics ?? [],
                },
            };
        }
        ```
      * TS interface 는 Plan 08-01 박제 optional (`forceSignalsReport?: ForceSignalsReport | null`) 유지 — normalize() 가 compat layer
    - **backend/runpod_inference/requirements.txt 검토**:
      * Plan 01-13 박제 시점에 google-genai SDK 이미 박제됨 (Phase 5 Gemini 통합). 본 plan 의 변경 가능성 0. 단 grep 으로 확인 — 없으면 `google-genai >= 0.1` 신설.
      * RESEARCH §Environment Availability 정합 — RunPod Pod 에 google-genai 이미 박제됨 가정.
    - **pipeline 통합 test (test_pipeline_phase8.py)** — Phase 6 의 test_compare_body_profiles_pipeline_wiring.py 패턴 mirror:
      * test_pipeline_phase8_gemini_off_path: monkeypatch.delenv("RECOGNIZER_BACKEND") 또는 setenv("RECOGNIZER_BACKEND", "fallback") + mock _extract_video_analysis_inputs + mock compare_body_profiles → _process 호출 → _get_gemini_moment_extractor() 가 None 반환 검증 + force_signals_report 가 complete_analysis 의 kwarg 로 전달 (mock firestore_admin) + Layer 1 단독 (overall_confidence='medium', warnings contains 'layer2_unavailable')
      * test_pipeline_phase8_gemini_on_path: monkeypatch.setenv("RECOGNIZER_BACKEND", "gemini") + mock GeminiMomentExtractor (extract_key_moments 가 4 KeyMoment 반환) → _process 호출 → Layer 2 활성 + force_signals_report.phaseBoundaries[*].source == 'gemini_assisted' 검증
      * test_pipeline_phase8_mode1_force_signals_emitted: mode='mode1' + mock → force_signals_report kwarg 전달
      * test_pipeline_phase8_mode3_first_force_signals_emitted: mode='mode3' + previous_analysis=None (mode3_first) + mock → force_signals_report kwarg 전달
      * test_pipeline_phase8_complete_analysis_force_signals_kwarg: complete_analysis mock 의 call_args 검사 — `force_signals_report` kwarg 가 dict 타입 + 'phaseBoundaries' / 'axisMetrics' / 'stabilityMetrics' / 'contactMetrics' 키 모두 박제
  </behavior>
  <action>
    `backend/functions/pipeline/app.py` 변경:
      (1) top-level import + singleton helper 박제 (위 behavior).
      (2) `_process` 안 compare_body_profiles 호출 직후 (line ~990 근처, body_comparison_report_dict 변환 사이 또는 직전) Phase 8 wiring 1줄 박제.
      (3) complete_analysis 호출에 `force_signals_report=force_signals_dict` kwarg 추가.
      (4) `_extract_video_analysis_inputs` 호출 위치의 `keep_local_video=` 인자를 conditional 변경 — `keep_local_video=(os.environ.get("RECOGNIZER_BACKEND") == "gemini")` 또는 helper 함수로 분리. Phase 6 기존 caller 영향 0 강제.

    `app/src/lib/userAnalyses.ts` 변경: 기존 bodyComparisonReport null-guard 직후에 forceSignalsReport null-guard 박제 (위 behavior 의 immutable spread + ?? null fallback). 변경 위치 = line ~53 근처 (Phase 7 WR-02 B1 패턴 정확히 mirror).

    `backend/runpod_inference/requirements.txt` 검토: `grep -c "google-genai\|google_genai" backend/runpod_inference/requirements.txt` 확인. 없으면 `google-genai>=0.1,<1.0` 추가 (Plan 01-13 박제 SDK).

    `backend/tests/pipeline/test_pipeline_phase8.py` 신설 — Phase 6 test_compare_body_profiles_pipeline_wiring.py 패턴 mirror. 5 통합 test 박제 (위 behavior). monkeypatch.setenv 활용 (RECOGNIZER_BACKEND on/off) + unittest.mock.patch (firestore_admin / GeminiMomentExtractor / _extract_video_analysis_inputs / compare_body_profiles 모두 mock).

    Critical Phase 6 회귀 0: 본 wiring 으로 pipeline 통합 test (test_compare_body_profiles_pipeline_wiring.py 등) 모두 PASS 유지 강제. force_signals.compute_force_signals 가 input 추가만 (output 분리, kwarg 추가만) — 기존 path 영향 0.

    per RESEARCH §"Pipeline wiring" + §"firestore_admin.complete_analysis 확장" + §"Frontend normalize" + Open Question 2 (keep_local_video default 유지 + 명시적 True path).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && PYTHONPATH=backend/shared/python:. backend/.venv/bin/python3 -m pytest backend/tests/pipeline/ backend/tests/phase08/ backend/tests/phase06/ backend/tests/phase07/ -x && cd app && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File modified: backend/functions/pipeline/app.py contains `from sunity_shared.analysis import force_signals as fs` and `compute_force_signals` call
    - File modified: pipeline/app.py contains `_get_gemini_moment_extractor` singleton + `RECOGNIZER_BACKEND` env check
    - File modified: pipeline/app.py contains `force_signals_report=force_signals_dict` in complete_analysis call
    - File modified: app/src/lib/userAnalyses.ts contains `forceSignalsReport` null-guard with immutable spread (Phase 7 WR-02 B1 pattern)
    - Source: `grep -c "compute_force_signals" backend/functions/pipeline/app.py` >= 1
    - Source: `grep -c "_get_gemini_moment_extractor" backend/functions/pipeline/app.py` >= 2 (definition + call)
    - Source: `grep -c "force_signals_report" backend/functions/pipeline/app.py` >= 2
    - Source: `grep -c "forceSignalsReport" app/src/lib/userAnalyses.ts` >= 1
    - Source: `grep -c "google-genai\|google_genai" backend/runpod_inference/requirements.txt` >= 1
    - Behavior (Gemini OFF): test_pipeline_phase8_gemini_off_path PASS
    - Behavior (Gemini ON): test_pipeline_phase8_gemini_on_path PASS
    - Behavior (mode1): test_pipeline_phase8_mode1_force_signals_emitted PASS
    - Behavior (mode3_first): test_pipeline_phase8_mode3_first_force_signals_emitted PASS
    - Behavior (kwarg): test_pipeline_phase8_complete_analysis_force_signals_kwarg PASS — call_args 검사 PASS
    - Behavior (tsc): `cd app && npx tsc --noEmit` exits 0 (forceSignalsReport optional spread PASS)
    - Behavior (regression): phase06 / phase07 / phase08 / pipeline 모두 PASS (회귀 0)
    - Behavior: pytest exit 0 (위 verify command)
  </acceptance_criteria>
  <done>
    pipeline wiring + frontend normalize + RunPod requirements 검토 완료. 5 pipeline 통합 test PASS + tsc clean + phase06/07/08 회귀 0. Task 3 (SAM build smoke + manual checkpoint) 박제.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: SAM build smoke + RunPod Pod env 갱신 (belle 운영 작업 manual checkpoint)</name>
  <what-built>
    Plan 08-03 의 모든 코드 변경 완료:
    (a) force_signals.py Layer 2 Gemini 본체 활성 (Plan 01-13 reuse, 코드 변경 0)
    (b) firestore_admin.py _validate_dict_only_scalars 명세 확장 (list[str] 허용) + complete_analysis force_signals_report kwarg
    (c) pipeline/app.py _process Phase 8 wiring 1줄 + _get_gemini_moment_extractor singleton + keep_local_video conditional
    (d) userAnalyses.ts normalize() forceSignalsReport null-guard (Phase 7 WR-02 B1 패턴)
    (e) RunPod requirements.txt google-genai 박제 확인
    (f) 11 신설 test + 5 pipeline 통합 test + 회귀 0

    본 task = SAM build smoke + RunPod Pod env 갱신 + 5영상 sweep sanity check (belle 운영 작업).
  </what-built>
  <how-to-verify>
    **belle 운영 작업 (manual gates from 08-VALIDATION.md):**

    1. **SAM build smoke** (개발자 또는 belle 가 로컬 + Pod 양쪽 검증):
       ```bash
       cd /Users/kimtaesung/Dev/SunityMotion/backend
       sam validate
       # exit 0 + "Successfully validated" 출력 확인

       sam build --use-container
       # exit 0 + .aws-sam/build/ 생성 확인 (Mac native binary 함정 회피 — runpod-gpu-env memory §"SAM build native deps")
       ```

    2. **Lambda env 동기화** (belle 운영 작업, Plan 06-02 박제 패턴 정합):
       ```bash
       # RECOGNIZER_BACKEND=gemini 활성 (Phase 5 박제) — 이미 박제됨, 변경 0 가능성
       aws lambda get-function-configuration \
         --function-name sunity-motion-pipeline \
         --region ap-northeast-2 \
         --query 'Environment.Variables.RECOGNIZER_BACKEND'
       # "gemini" 출력 확인. 없으면 update-function-configuration 박제.
       ```

    3. **RunPod Pod env 갱신** (belle 운영 작업, runpod-gpu-env memory §Pod 환경 정합):
       ```bash
       # Pod xbdkj1g2ylnfwi SSH 접속
       ssh xbdkj1g2ylnfwi-64411701@ssh.runpod.io -i ~/.ssh/id_ed25519

       # .bashrc 확인 (Plan 01-13 박제 — google-genai SDK + RECOGNIZER_BACKEND=gemini 이미 박제됨 가정)
       grep "RECOGNIZER_BACKEND\|google" ~/.bashrc

       # google-genai SDK 확인
       pip show google-genai

       # SunityMotion git pull (Plan 08-01/02/03 commit 동기화)
       cd /workspace/SunityMotion && git pull origin main

       # uvicorn restart (stale __pycache__ 청소 + Plan 08 force_signals.py 로드)
       find /workspace/SunityMotion -name "__pycache__" -type d -exec rm -rf {} + 2>/dev/null
       pkill -9 uvicorn
       cd /workspace/SunityMotion/backend
       PYTHONPATH=shared/python:. uvicorn runpod_inference.server:app --host 0.0.0.0 --port 8000 --workers 1 &

       # /health 확인
       curl -s -H "X-RunPod-Token: $RUNPOD_AUTH_TOKEN" https://xbdkj1g2ylnfwi-8000.proxy.runpod.net/health | jq
       # pipeline_loaded: true, auth_configured: true 출력 확인
       ```

    4. **mock E2E 1회 실행** (belle 운영 작업):
       - Pod 안에서 _process 직접 호출 (Phase 6 박제 mock pattern):
       - ref-invert mp4 1개 + mock uid/analysis_id → _process 실행
       - Firestore Console 에서 결과 doc 확인:
         * status='done'
         * result.bodyComparisonReport (Phase 6/7 박제)
         * **result.forceSignalsReport (Phase 8 신설)** — version="1.0" + overallConfidence + phaseBoundaries (5 entries) + axisMetrics + stabilityMetrics + contactMetrics

    5. **5영상 sweep sanity check** (belle 검수 — 08-VALIDATION.md §"Manual-Only Verifications" 정합):
       - 정은지 reference 5영상 (ref-invert / ref-foxtop / ref-foxtop-split / ref-climb / ref-sideway-spin) 각 mock E2E 실행
       - 각 영상의 forceSignalsReport 출력 hist:
         * axis_metrics severity 분포: 정은지 90%+ 'low' 예상
         * stability_metrics severity 분포: 정은지 90%+ 'low'
         * contact_metrics estimated_stable: 모든 expected_contact_points 가 True (hold phase 안)
         * phase_boundaries length=5 (모든 5 phase detect)
       - 분포가 합리적이면 PASS. 아니면 Plan 08-02 의 threshold 박제 재조정 (researcher 검토).

    6. **Layer 1 휴리스틱 25-timestamp belle labeling sanity** (08-VALIDATION.md):
       - belle 가 5영상 × 5 phase boundary 수동 라벨링 (entry start / lock start / transition start / final_shape start / hold start, end 의 timestamp)
       - Layer 1 산출과 비교 — ±200ms 안 일치 비율 >= 80% PASS, < 80% 룰 cutoff 박제 재조정

    7. **contact_points.yaml 도메인 검수** (08-VALIDATION.md):
       - belle / 강사 가 contact_points.yaml 직접 검토 — 5 motion 의 expected_contact_points (12 enum 중) 가 폴스포츠 도메인 정합한지 검증
       - 정합 안 되면 yaml 직접 수정 (1줄 박제 — D-08-B5 정합)

    **본 checkpoint 통과 시 Phase 8 close-out 진입 — `/gsd-verify-work 8` 실행 가능.**
  </how-to-verify>
  <resume-signal>belle 가 "approved" 또는 sweep 결과 박제 후 진입 신호. sweep 결과가 분포 비정합 (정은지 영상 high severity 다수) 시 issue 박제 후 Plan 08-02 threshold 재조정 plan 신설 (별도 plan).</resume-signal>
</task>

</tasks>

<verification>
## Phase 8 Plan 03 Verification

**Goal-backward derived truths (FORCE-01 production 통합):**

- Layer 2 Gemini 본체 활성 — _layer2_gemini_boundaries / _map_moments_to_5phase / _confidence_from_agreement / _promote_layer1 + graceful fallback (D-08-A2)
- _validate_dict_only_scalars 명세 확장 — list[str] 허용 (RESEARCH Open Question 1 Option A)
- complete_analysis(force_signals_report) kwarg 박제 (Phase 6 박제 body_comparison_report 패턴)
- pipeline/app.py _process wiring 1줄 + _get_gemini_moment_extractor singleton + keep_local_video conditional
- userAnalyses.ts normalize() forceSignalsReport null-guard (Phase 7 WR-02 B1 패턴)
- RunPod requirements google-genai 박제 확인 (Plan 01-13 박제 reuse)
- 11 신설 단위 test + 5 pipeline 통합 test PASS
- phase06 / phase07 / phase08 / pipeline 모두 회귀 0
- tsc --noEmit clean
- SAM build smoke (manual checkpoint) — belle 운영 작업 박제

**Manual verification (belle 운영 작업 — 08-VALIDATION.md §"Manual-Only Verifications" 정합):**

- 5영상 sweep sanity check (severity 분포 90%+ low for 정은지 영상)
- Layer 2 Gemini key_moments timestamp sensible check (ref-invert ±300ms belle 육안)
- contact_points.yaml 도메인 검수 (belle/강사 12 enum 정합)
- TS tsc + Firestore output 양립 검증 (development build 1회 + Firestore Console)
- Layer 1 휴리스틱 25-timestamp belle labeling sanity (≥ 80% 일치)

**Regression checks (Plan 08-03 종료 시점):**

- `pytest backend/tests/phase06/ -x` PASS (156 test, _validate_dict_only_scalars 명세 확장 회귀 0)
- `pytest backend/tests/phase07/ -x` PASS (108 test)
- `pytest backend/tests/phase08/ -x` PASS (40+ test 누적)
- `pytest backend/tests/pipeline/ -x` PASS (Phase 8 wiring 추가, 기존 회귀 0)
- `tsc --noEmit clean`
- `sam validate exit 0`
- `sam build --use-container exit 0`
</verification>

<success_criteria>
- [x] Layer 2 Gemini 본체 활성 (Plan 01-13 reuse, 코드 변경 0)
- [x] _validate_dict_only_scalars 명세 확장 (list[str] 허용, 기존 nested list reject 유지)
- [x] complete_analysis force_signals_report kwarg 박제
- [x] pipeline/app.py _process Phase 8 wiring (1줄 helper 호출 + singleton + conditional keep_local_video)
- [x] userAnalyses.ts normalize() forceSignalsReport null-guard (Phase 7 WR-02 B1 패턴)
- [x] RunPod requirements google-genai 박제 확인
- [x] 11 신설 단위 test + 5 pipeline 통합 test PASS
- [x] phase06 / phase07 / phase08 / pipeline 회귀 0 + tsc clean
- [x] SAM build smoke (belle 운영 manual checkpoint)
- [x] FORCE-01 success criteria #1~4 모두 production 통합 완료 — Phase 8 close-out 진입 가능
</success_criteria>

<output>
Create `.planning/phases/08-jerk-jitter/08-03-SUMMARY.md` when done. Include:
- 3 task commit SHA + 각 task scope
- Layer 2 Gemini 본체 활성 결과 (5 신설 test PASS)
- _validate_dict_only_scalars 명세 확장 결과 (phase06 156 test 회귀 0)
- pipeline wiring 결과 (Gemini ON/OFF 양 path PASS)
- frontend normalize 결과 (tsc clean + forceSignalsReport 7 필드 null-guard)
- SAM build smoke 결과 (sam validate + build --use-container exit 0)
- belle 운영 작업 박제 (RunPod Pod env 갱신 + 5영상 sweep sanity check + Layer 1 25-timestamp labeling + yaml 검수 — 후속 belle 작업 박제 manual gate)
- Phase 8 close-out 진입 시그널 (FORCE-01 신호 layer 완성)
- belle chain 다음 진입: Phase 9 (ForceDirectionPattern + 실패 원인 후보 3개) 시그널 박제
- Deviation (Rule 1 fix / Rule 3 environment fix 등) 있으면 박제
</output>
