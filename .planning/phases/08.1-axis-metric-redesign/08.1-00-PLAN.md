---
phase: 08.1-axis-metric-redesign
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/shared/python/sunity_shared/analysis/force_signals.py
  - app/src/types/analysis.ts
  - docs/contract.md
  - app/src/lib/userAnalyses.ts
  - backend/shared/python/sunity_shared/firestore_admin.py
  - backend/tests/phase08_1/__init__.py
  - backend/tests/phase08_1/conftest.py
  - backend/tests/phase08_1/test_axis_schema_lockstep.py
  - backend/tests/phase08_1/test_axis_dataclass_invariants.py
  - backend/tests/phase08_1/test_firestore_axis_validator.py
autonomous: true
requirements: [FORCE-01]
subsystem: backend-contract-frontend
tags: [phase-08-1, axis-metric-redesign, wave-0-schema, 3-way-lockstep, distance-hard-break]

scope_sanity_note: "Wave 0 of 3 (D-06 정합). 단일 목적 = D-01 (Distance Hard Break) 의 3-way lockstep atomic migration. AxisDeviationMetric 에서 5 필드 제거 — `pelvis_distance_from_pole_axis` / `chest_distance_from_pole_axis` / `scale_denominator` / `coordinate_space` / `deviation_direction`. 남기는 필드 6개: `phase` / `shoulder_tilt` / `hip_tilt` / `severity` / `confidence` / `warnings`. Wave 1 (tilt metric 재설계 + 정은지 calibration) + Wave 2 (pipeline rewire + sweep verifier) 가 본 plan 의 contract 위에 박제. RESEARCH §4 α-4 결정 정합 — IPSF Code of Points 에 글로벌 distance 항목 부재 (NotebookLM citation 9). 본 plan 은 정적 (algorithm 변경 0, 위험 낮음, 회귀 0 강제). compute_axis_deviation 본체 변경 = Wave 1 (tilt-only 재작성 + threshold yaml load). pipeline wiring 변경 = Wave 2."

must_haves:
  truths:
    - "본 plan 의 단일 목적 = D-01 (Distance Hard Break) 의 3-way lockstep atomic — 5 필드 제거를 TS interface + Python dataclass + docs §9.3 한 commit 안에서 동시 변경. wave 분리 = D-06 정합 (정적 / 알고리즘 / production 통합 3 wave atomic boundary). per D-01."
    - "AxisDeviationMetric 6 필드 박제 (5 필드 제거): `phase` (MotionPhase) / `shoulder_tilt` (float | None, degrees) / `hip_tilt` (float | None, degrees) / `severity` (SeverityLevel) / `confidence` (MetricConfidence) / `warnings` (list[str]). 제거된 5 필드: `pelvis_distance_from_pole_axis` / `chest_distance_from_pole_axis` / `scale_denominator` / `coordinate_space` / `deviation_direction`. per D-01 + RESEARCH §4.3."
    - "Python dataclass __post_init__ 검증 단순화 — coordinate_space / scale_denominator / deviation_direction 검증 분기 제거. severity + confidence 검증 분기만 유지. line=None / coordinate_space='unavailable' invariant 제거 (필드 자체 부재). per D-01."
    - "compute_axis_deviation 본체 변경 0 (Wave 0 scope 외 — Wave 1 책임). 단, AxisDeviationMetric 생성자 호출 시 제거된 5 kwarg 가 코드에 박제돼 있어 Wave 0 commit 후 즉시 import error 발생. 본 plan Task 1 = dataclass 정의만 변경 (compute_axis_deviation 함수 본체는 Wave 1 가 완전 재작성). 임시 박제: compute_axis_deviation 의 AxisDeviationMetric() 생성자 호출부 (line 1079, 1212) 도 본 plan 에서 6 필드 호출로 정합 — 단, distance 계산 로직 (line 1107-1209) 은 Wave 1 가 재작성 (tilt-only 본체). Wave 0 종료 시점: dataclass 6 필드 + 기존 compute_axis_deviation 호출부 정합 (distance 산출 path 가 None 박제 fallback 유지 — 정은지 5/5 severity 계산은 Wave 1 종료까지 'low' default 박제)."
    - "TS interface AxisDeviationMetric 6 필드 박제 — TS 측 5 필드 제거: `pelvisDistanceFromPoleAxis` / `chestDistanceFromPoleAxis` / `scaleDenominator` / `coordinateSpace` / `deviationDirection`. 남기는 필드 6: `phase` / `shoulderTilt` / `hipTilt` / `severity` / `confidence` / `warnings`. JSDoc 박제 갱신 — REVIEWS R1/R2 박제 메모 제거 (Phase 8.1 distance hard break 메모로 대체)."
    - "docs/contract.md §9.3 박제 갱신 — 표에서 5 필드 row 제거 (pelvisDistanceFromPoleAxis / chestDistanceFromPoleAxis / scaleDenominator / coordinateSpace / deviationDirection). 박제 메모 (R1 + R2) 제거 + Phase 8.1 distance hard break 메모 신설 — '본 phase 의 axis 차원 = tilt-only 박제 (IPSF Code of Points 에 글로벌 distance 항목 부재 — NotebookLM citation 9 Page 87 Glossary)'. version bump 박제 (footer 의 *Plan 08-01 §9 추가* 박제 옆에 *Plan 08.1-00 §9.3 변경: distance 필드 제거* 신설)."
    - "Frontend null-guard 박제 (app/src/lib/userAnalyses.ts line 74-88) — Phase 8 박제 forceSignalsReport null-guard 유지. axisMetrics 박제 mapping 안 5 필드 reference 제거 (기존엔 직접 spread 박제 — `axisMetrics: report.axisMetrics ?? []` 만 박제, 필드 reference 없음 — 본 박제는 무변경 가능. 검증 Task 1 의 grep 박제로 확인). 변경 시 immutable spread 패턴 유지 (Phase 7 WR-02 B1 정합)."
    - "Firestore scoped validator `_validate_force_signals_report` (firestore_admin.py L148) 박제 정합 — 본 validator 의 화이트리스트 `_FORCE_SIGNALS_SCALAR_LIST_KEYS_IN_METRIC` (warnings / unstableBodyParts) 박제 변경 0 (제거되는 5 필드 박제 안 들어가 있음 — distance 필드 = scalar, coordinate_space / scale_denominator / deviation_direction = scalar). 회귀 0 강제 (existing phase08 test 358 PASS + 1 skipped 유지). 단, Phase 8 의 test_firestore_lockstep.py 박제 axisMetric dict 안 distance 필드 박제 시나리오는 본 plan 의 신설 test 박제로 obsolete (제거된 필드 박제 fixtures 가 더 이상 valid 가 아님)."
    - "Migration 정책 박제 — 기존 Firestore docs 의 distance 필드 = 무시 (read-side 가 안 읽음). 별도 backfill 불필요 (D-01 박제 정합). Firestore docs 박제 변경 없음 — `_validate_force_signals_report` 의 화이트리스트가 변경 후에도 backwards-compatible (read 시 unknown 필드 무시). frontend normalize 가 새 6 필드만 박제 — old doc 의 5 필드 자동 drop."
    - "신설 Test 3개 박제 (backend/tests/phase08_1/):\n  · test_axis_schema_lockstep.py — TS interface + Python dataclass + docs §9.3 의 6 필드 set 정합 grep 검증 (제거된 5 필드 부재 + 남기는 6 필드 존재). per Plan 08-01 lockstep 패턴 정합.\n  · test_axis_dataclass_invariants.py — AxisDeviationMetric 6 필드 dataclass 인스턴스화 PASS / invalid severity ValueError / invalid confidence ValueError / invalid phase ValueError 검증. 제거된 5 필드 박제 kwarg 시 TypeError raise (`AxisDeviationMetric() got an unexpected keyword argument 'pelvis_distance_from_pole_axis'`).\n  · test_firestore_axis_validator.py — `_validate_force_signals_report` 박제 호출 시 새 6 필드 axisMetrics dict PASS + 제거된 5 필드 박제 dict 도 PASS (scalar 박제 — backwards-compat 검증). list[str] warnings field PASS."
    - "Phase 08.1 test infra 박제 (Phase 7/8 패턴 정합) — backend/tests/phase08_1/__init__.py 빈 marker + backend/tests/phase08_1/conftest.py 박제 (PHASE08_1_FIXTURES_DIR 상수 + 본 plan 의 unit test 박제 source). conftest 는 phase08 fixtures 박제 import 가능 (cross-phase factory reuse, REVIEWS R9 정합)."
    - "회귀 게이트 (Wave 0 종료 시) — phase06 156 + phase07 88 + phase08 103 + phase08_1 3 (신설) + pipeline 11 = 361 PASS + 1 skipped (Phase 8 의 기존 axis distance 박제 test 박제 시점에서 Phase 8 tests 일부 변경 필요할 수 있음 — Task 2 박제). tsc --noEmit clean. 본 plan = production behavior 변경 0 (compute_axis_deviation 본체 변경은 Wave 1)."
  artifacts:
    - path: backend/shared/python/sunity_shared/analysis/force_signals.py
      provides: "AxisDeviationMetric dataclass 박제 6 필드 (5 필드 제거). __post_init__ 검증 단순화 (coordinate_space / scale_denominator / deviation_direction 검증 제거, severity + confidence 검증만 유지). compute_axis_deviation 함수 본체의 AxisDeviationMetric() 생성자 호출 (line 1079, 1212) 도 6 필드 정합 (Wave 1 가 본체 재작성 — 본 plan 은 dataclass + 호출부 정합만). distance threshold 박제 모듈 상수 (AXIS_PELVIS_DISTANCE_THRESHOLDS / AXIS_CHEST_DISTANCE_THRESHOLDS) 박제 보존 (Wave 1 가 제거). per D-01."
      contains:
        - "class AxisDeviationMetric"
        - "phase: MotionPhase"
        - "shoulder_tilt: float | None"
        - "hip_tilt: float | None"
        - "severity: SeverityLevel"
        - "confidence: MetricConfidence"
        - "warnings: list[str]"
    - path: app/src/types/analysis.ts
      provides: "TS interface AxisDeviationMetric 박제 6 필드 (5 필드 제거). JSDoc 박제 갱신 — Phase 8.1 distance hard break 메모 (IPSF Code of Points 글로벌 distance 항목 부재 + NotebookLM citation 9). Phase 8 REVIEWS R1/R2 메모 제거. 다른 interface (PoleLine2D / PoleAxisMeasurement / StabilityMetric / ContactStabilityMetric / ForceSignalsReport 등) 박제 변경 0."
      contains:
        - "export interface AxisDeviationMetric"
        - "phase: MotionPhase"
        - "shoulderTilt: number | null"
        - "hipTilt: number | null"
        - "severity: SeverityLevel"
        - "confidence: MetricConfidence"
        - "warnings: string[]"
        - "Phase 8.1"
    - path: docs/contract.md
      provides: "§9.3 AxisDeviationMetric 박제 표 갱신 — 5 필드 row 제거. 박제 메모 (R1 + R2) 제거 + Phase 8.1 distance hard break 메모 신설 (IPSF citation 박제). footer 의 version log 갱신 (Plan 08.1-00 §9.3 변경 박제 신설). 다른 섹션 (§9.0 / §9.1 / §9.2 / §9.4 / §9.5 / §9.6 / §9.7 / §9.8) 박제 변경 0."
      contains:
        - "### §9.3 AxisDeviationMetric"
        - "shoulderTilt"
        - "hipTilt"
        - "Phase 8.1"
        - "IPSF Code of Points"
        - "Plan 08.1-00"
    - path: app/src/lib/userAnalyses.ts
      provides: "normalize() 의 forceSignalsReport null-guard 박제 (line 74-88) — axisMetrics 박제 mapping 박제 변경 검증 (현재 박제 = `axisMetrics: report.axisMetrics ?? []` 직접 spread, 필드 reference 없음 — 변경 0). Phase 7 WR-02 B1 immutable spread 패턴 유지. 변경 필요 시 (e.g. 명시적 필드 default 박제) immutable .map() 박제."
      contains:
        - "forceSignalsReport"
        - "axisMetrics"
    - path: backend/shared/python/sunity_shared/firestore_admin.py
      provides: "`_validate_force_signals_report` 박제 변경 0 (화이트리스트 `_FORCE_SIGNALS_SCALAR_LIST_KEYS_IN_METRIC` 박제 보존). 제거되는 5 필드 박제 = scalar (str/float/None) → backwards-compat (read 시 unknown 필드 무시). 본 파일 Task 1 박제 실 수정 = grep 검증 박제만 (실 수정 0 가능)."
      contains:
        - "_validate_force_signals_report"
        - "_FORCE_SIGNALS_SCALAR_LIST_KEYS_IN_METRIC"
    - path: backend/tests/phase08_1/__init__.py
      provides: "phase08_1 test 패키지 marker (Phase 7/8 패턴 정합, 빈 파일)."
      contains: []
    - path: backend/tests/phase08_1/conftest.py
      provides: "phase08_1 공용 pytest fixture — PHASE08_1_FIXTURES_DIR 상수 + Phase 8 fixture factory import (cross-phase reuse, REVIEWS R9 정합). 본 plan 의 unit test 박제 source."
      contains:
        - "PHASE08_1_FIXTURES_DIR"
        - "import pytest"
    - path: backend/tests/phase08_1/test_axis_schema_lockstep.py
      provides: "3-way lockstep grep 검증 — TS interface + Python dataclass + docs §9.3 의 6 필드 set 정합. 제거된 5 필드 부재 (negative grep) + 남기는 6 필드 존재 (positive grep). per D-01 + Plan 08-01 lockstep 패턴 정합."
      contains:
        - "def test_axis_six_fields_present_in_ts"
        - "def test_axis_six_fields_present_in_python"
        - "def test_axis_six_fields_present_in_docs"
        - "def test_distance_fields_removed_from_ts"
        - "def test_distance_fields_removed_from_python"
        - "def test_distance_fields_removed_from_docs"
        - "pelvis_distance_from_pole_axis"
        - "shoulder_tilt"
    - path: backend/tests/phase08_1/test_axis_dataclass_invariants.py
      provides: "AxisDeviationMetric dataclass 박제 6 필드 인스턴스화 PASS + invalid severity / confidence / phase ValueError + 제거된 5 필드 kwarg 시 TypeError 검증. __post_init__ 검증 단순화 정합."
      contains:
        - "def test_axis_dataclass_six_fields_instantiation"
        - "def test_axis_dataclass_invalid_severity_raises"
        - "def test_axis_dataclass_invalid_confidence_raises"
        - "def test_axis_dataclass_invalid_phase_raises"
        - "def test_axis_dataclass_distance_kwarg_raises_typeerror"
        - "AxisDeviationMetric"
        - "pytest.raises"
    - path: backend/tests/phase08_1/test_firestore_axis_validator.py
      provides: "_validate_force_signals_report 박제 axis dict 박제 검증 — 새 6 필드 dict PASS + warnings list[str] PASS + 제거된 5 필드 박제 dict (legacy doc) 도 PASS (backwards-compat scalar). list[list] / list[dict] in metric 시 ValueError (기존 invariant 유지)."
      contains:
        - "def test_axis_metric_six_fields_pass"
        - "def test_axis_metric_warnings_list_str_pass"
        - "def test_axis_metric_legacy_distance_field_backwards_compat"
        - "def test_axis_metric_nested_list_rejects"
        - "_validate_force_signals_report"
  key_links:
    - from: app/src/types/analysis.ts
      to: backend/shared/python/sunity_shared/analysis/force_signals.py
      via: "AxisDeviationMetric 의 6 필드 set 박제 — TS interface 의 필드 set + Python dataclass 의 필드 set 정합 grep. test_axis_schema_lockstep.py 가 양쪽 grep 검증."
      pattern: "shoulder_tilt|shoulderTilt|hip_tilt|hipTilt|warnings"
    - from: app/src/types/analysis.ts
      to: docs/contract.md
      via: "AxisDeviationMetric §9.3 의 6 필드 표 박제 — TS interface 의 필드 set + docs 표의 row set 정합 grep."
      pattern: "shoulderTilt|hipTilt|warnings"
    - from: backend/shared/python/sunity_shared/firestore_admin.py
      to: backend/shared/python/sunity_shared/analysis/force_signals.py
      via: "`_validate_force_signals_report` 박제 axisMetrics list[dict] 박제 — 6 필드 metric dict + warnings list[str] 박제 허용. AxisDeviationMetric 박제 직접 import 안 함 (scoped validator 가 generic dict 검증)."
      pattern: "_validate_force_signals_report|axisMetrics"
    - from: app/src/lib/userAnalyses.ts
      to: app/src/types/analysis.ts
      via: "normalize() 의 forceSignalsReport mapping 박제 — TS interface AxisDeviationMetric 박제 6 필드 직접 사용 X (직접 spread 박제). 변경 0 가능 — Task 1 grep 박제로 확인."
      pattern: "AxisDeviationMetric|axisMetrics"
---

<objective>
Phase 8 의 `AxisDeviationMetric` schema 에서 distance 차원 5 필드를 3-way lockstep atomic 으로 제거한다. RESEARCH §4 의 α-4 결정 (IPSF Code of Points 에 글로벌 distance 항목 부재 — NotebookLM citation 9) + CONTEXT D-01 (Distance Hard Break) + D-06 (Wave 0 / 1 / 2 atomic boundary) 정합. Wave 1 의 tilt metric 재설계 + Wave 2 의 pipeline rewire 가 본 plan 의 contract 위에 박제.

Purpose: D-01 의 정적 schema 변경을 단일 commit 에서 atomic 으로 처리 — TS interface + Python dataclass + docs §9.3 동시 박제. 본 plan 종료 시 Phase 8.1 의 contract foundation 박제. Wave 1 (compute_axis_deviation 본체 재작성 + 정은지 calibration) 가 본 plan 의 6 필드 위에 박제. 정은지 sweep evidence 박제는 Wave 2 (production 통합).

Output:
- backend/shared/python/sunity_shared/analysis/force_signals.py (AxisDeviationMetric dataclass 6 필드)
- app/src/types/analysis.ts (TS interface 6 필드)
- docs/contract.md §9.3 (표 갱신 + Phase 8.1 박제 메모)
- app/src/lib/userAnalyses.ts (null-guard 검증)
- backend/shared/python/sunity_shared/firestore_admin.py (scoped validator 박제 보존)
- backend/tests/phase08_1/ (신설 test infra + 3 unit test)
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-axis-metric-redesign/08.1-CONTEXT.md
@.planning/phases/08.1-axis-metric-redesign/08.1-RESEARCH.md
@.planning/phases/08.1-axis-metric-redesign/README.md
@.planning/phases/08-jerk-jitter/PHASE8-INHERITED-ISSUES.md
@.planning/phases/08-jerk-jitter/08-03-SUMMARY.md

@backend/shared/python/sunity_shared/analysis/force_signals.py
@app/src/types/analysis.ts
@docs/contract.md
@app/src/lib/userAnalyses.ts
@backend/shared/python/sunity_shared/firestore_admin.py
@backend/tests/phase08/test_force_signals_lockstep.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AxisDeviationMetric 3-way lockstep atomic — TS interface + Python dataclass + docs §9.3</name>
  <files>backend/shared/python/sunity_shared/analysis/force_signals.py, app/src/types/analysis.ts, docs/contract.md, backend/tests/phase08_1/__init__.py, backend/tests/phase08_1/conftest.py, backend/tests/phase08_1/test_axis_schema_lockstep.py, backend/tests/phase08_1/test_axis_dataclass_invariants.py</files>
  <behavior>
    RED → GREEN cycle for 3-way lockstep schema.
    Failing tests first (test_axis_schema_lockstep.py + test_axis_dataclass_invariants.py):
      - test_axis_six_fields_present_in_ts: grep app/src/types/analysis.ts for `phase`, `shoulderTilt`, `hipTilt`, `severity`, `confidence`, `warnings` inside `export interface AxisDeviationMetric` block (6 fields exactly).
      - test_axis_six_fields_present_in_python: import force_signals.AxisDeviationMetric, assert dataclasses.fields() has exactly 6 names {phase, shoulder_tilt, hip_tilt, severity, confidence, warnings}.
      - test_axis_six_fields_present_in_docs: grep docs/contract.md §9.3 table rows for the 6 field names (camelCase + snake_case rows).
      - test_distance_fields_removed_from_ts: assert `pelvisDistanceFromPoleAxis` / `chestDistanceFromPoleAxis` / `scaleDenominator` / `coordinateSpace` / `deviationDirection` ABSENT inside the AxisDeviationMetric interface block (read interface block, grep negative). NOTE: identifiers may still exist elsewhere in analysis.ts (e.g. CoordinateSpace type alias for PoleAxisMeasurement) — assertion scope = AxisDeviationMetric interface block only.
      - test_distance_fields_removed_from_python: assert AxisDeviationMetric dataclass fields() does NOT contain pelvis_distance_from_pole_axis / chest_distance_from_pole_axis / scale_denominator / coordinate_space / deviation_direction.
      - test_distance_fields_removed_from_docs: read §9.3 table block, assert 5 removed-field rows absent.
      - test_axis_dataclass_six_fields_instantiation: AxisDeviationMetric(phase='hold', shoulder_tilt=12.5, hip_tilt=8.3, severity='low', confidence='medium', warnings=[]) PASS.
      - test_axis_dataclass_invalid_severity_raises: severity='invalid' → ValueError.
      - test_axis_dataclass_invalid_confidence_raises: confidence='invalid' → ValueError.
      - test_axis_dataclass_invalid_phase_raises: phase='invalid' → ValueError.
      - test_axis_dataclass_distance_kwarg_raises_typeerror: AxisDeviationMetric(... , pelvis_distance_from_pole_axis=1.0) → TypeError (unexpected keyword argument).
    GREEN: edit 3 source artifacts atomically.
  </behavior>
  <action>
Step 1 — Create test infra (test files first, RED state):
  - backend/tests/phase08_1/__init__.py: empty marker.
  - backend/tests/phase08_1/conftest.py: `PHASE08_1_FIXTURES_DIR = Path(__file__).parent / 'fixtures'` (per Phase 8 pattern, dir creation deferred to Wave 1 if needed). `import pytest`.
  - backend/tests/phase08_1/test_axis_schema_lockstep.py: 6 tests above. Pattern from backend/tests/phase08/test_force_signals_lockstep.py — same `_REPO_ROOT` / `_TS_PATH` / `_CONTRACT_PATH` constants. Use ast.parse on Python module for field detection (dataclasses.fields()), regex for TS interface block extraction and docs §9.3 table block extraction. For TS block extraction: match `export interface AxisDeviationMetric \{[\s\S]*?\n\}`. For docs §9.3 block: match from `### §9.3 AxisDeviationMetric` until next `### §9.` heading.
  - backend/tests/phase08_1/test_axis_dataclass_invariants.py: 5 tests above. Import `from sunity_shared.analysis.force_signals import AxisDeviationMetric`. Use `pytest.raises(ValueError)` and `pytest.raises(TypeError)`. For instantiation test, pass `phase='hold'` (a MotionPhase literal) and `warnings=[]`.

Step 2 — Python dataclass (RED → GREEN):
  - backend/shared/python/sunity_shared/analysis/force_signals.py L272-332:
    Replace AxisDeviationMetric dataclass body with the 6-field version. Final fields:
      phase: MotionPhase
      shoulder_tilt: float | None
      hip_tilt: float | None
      severity: SeverityLevel
      confidence: MetricConfidence
      warnings: list[str] = field(default_factory=list)
    Replace __post_init__ with 3 validators: phase ∈ _MOTION_PHASES / severity ∈ _SEVERITY_LEVELS / confidence ∈ _METRIC_CONFIDENCES. Remove coordinate_space / scale_denominator / deviation_direction / distance-None invariant checks.
    Update class docstring: replace 'REVIEWS R1/R2' block with 'Phase 8.1 박제 (2026-06-09): distance 차원 hard break — IPSF Code of Points 에 글로벌 distance 항목 부재 (NotebookLM citation 9, Aerial Pole CoP 2024-2025 Page 87 Glossary). tilt-only metric 박제. per D-01.'

Step 3 — Update compute_axis_deviation generator calls to 6 fields (transitional fix):
  - force_signals.py L1079 (unavailable branch) and L1212 (main branch): change AxisDeviationMetric(...) call sites to pass only the 6 surviving kwargs. Wave 1 will fully rewrite the body; this step keeps the module importable post-Wave-0. For the unavailable branch (L1079-1091): keep `phase=b.phase`, `shoulder_tilt=None`, `hip_tilt=None`, `severity='low'`, `confidence='low'`, `warnings=list(warnings_base)`. For the main branch (L1211-1224): keep `phase=b.phase`, `shoulder_tilt=shoulder_tilt_val`, `hip_tilt=hip_tilt_val`, `severity=severity`, `confidence=metric_confidence`, `warnings=list(scale_warnings)`. Module-level constants AXIS_PELVIS_DISTANCE_THRESHOLDS / AXIS_CHEST_DISTANCE_THRESHOLDS = transient (Wave 1 removes them; do NOT remove in Wave 0 to keep the module importable while Wave 1 rewrites the body). Existing distance/severity calc logic between L1107-1210 stays as-is for Wave 0 (Wave 1 replaces the whole function body) — local variables `pelvis_dist` / `chest_dist` / `direction` etc. become unused; comment them with `# Wave 0 transitional — Wave 1 가 본체 재작성 (distance 산출 제거).` Run `python -c "from sunity_shared.analysis.force_signals import AxisDeviationMetric, compute_axis_deviation"` to confirm import.

Step 4 — TS interface:
  - app/src/types/analysis.ts L535-551 (AxisDeviationMetric interface block):
    Replace with 6 fields: phase / shoulderTilt: number|null / hipTilt: number|null / severity / confidence / warnings: string[]. Remove pelvisDistanceFromPoleAxis / chestDistanceFromPoleAxis / scaleDenominator / coordinateSpace / deviationDirection.
    Update JSDoc block (L524-534): replace REVIEWS R1/R2 메모 with 'Phase 8.1 박제 (2026-06-09): distance 차원 hard break — IPSF Code of Points 에 글로벌 distance 항목 부재 (NotebookLM citation 9). tilt-only metric. shoulderTilt / hipTilt 는 degrees, rotation-only / origin-invariant. severity = max(shoulderTilt severity, hipTilt severity). per D-01.'

Step 5 — docs/contract.md §9.3:
  - L841-860 (§9.3 AxisDeviationMetric section):
    Update intro sentence: '중심축 이탈 metric — phase 별 측정. **Phase 8.1 박제 (2026-06-09)**: distance 차원 hard break (IPSF Code of Points 글로벌 distance 항목 부재, NotebookLM citation 9). tilt-only metric.'
    Replace table with 6 rows: phase / shoulderTilt + shoulder_tilt / hipTilt + hip_tilt / severity / confidence / warnings.
    Replace '박제 메모 (R1 + R2)' block with: '**박제 메모 (Phase 8.1)**: tilt 값은 rotation-only / origin-invariant. shoulderTilt / hipTilt = arcsin(|Δz|/||Δ||) 박제 (pole_aligned 좌표계). 미가용 시 두 tilt 모두 null + severity=low + warning `tilt_unavailable`. per RESEARCH §4.3 α-4 + IPSF NotebookLM citation 9.'
    Append to file footer (after existing version log): '*Plan 08.1-00 §9.3 변경: 2026-06-09 — AxisDeviationMetric distance 차원 hard break. 5 필드 (pelvisDistanceFromPoleAxis / chestDistanceFromPoleAxis / scaleDenominator / coordinateSpace / deviationDirection) 제거 + tilt-only 박제. IPSF Code of Points NotebookLM citation 9 정합 (Phase 87 Glossary — 'Tilt' / 'Lean' / 'Off-axis' 용어 부재). RESEARCH §4 α-4 + CONTEXT D-01 박제.*'

Step 6 — Frontend null-guard verification:
  - app/src/lib/userAnalyses.ts L74-88: 검증만 — 현재 박제 `axisMetrics: report.axisMetrics ?? []` 직접 spread (필드 reference 없음) → 변경 0. grep 박제로 확인: `grep -n 'pelvisDistanceFromPoleAxis\|chestDistanceFromPoleAxis\|scaleDenominator\|coordinateSpace\|deviationDirection' app/src/lib/userAnalyses.ts` → 0 결과 expected. 결과 있을 시 immutable .map 박제 변환 (Phase 7 WR-02 B1 정합).

Step 7 — Firestore validator verification:
  - backend/shared/python/sunity_shared/firestore_admin.py: 변경 0 (scoped validator 박제 보존). grep 박제로 확인: `_FORCE_SIGNALS_SCALAR_LIST_KEYS_IN_METRIC` 박제 그대로 (warnings / unstableBodyParts 박제 화이트리스트).

Step 8 — Phase 8 의 test_force_signals_lockstep.py 박제 obsolete handling:
  - backend/tests/phase08/test_force_signals_lockstep.py: _FIELD_MAP (L36-89) 박제 5 필드 reference 제거 (pelvisDistanceFromPoleAxis / chestDistanceFromPoleAxis / scaleDenominator / coordinateSpace / deviationDirection — 4 row 박제 lines 61, 62, 72, 73, 80 박제). 박제 행은 obsolete 박제 라 Wave 0 commit 안에서 같이 cleanup. test 가 강제 PASS 박제 유지.
  - backend/tests/phase08/test_firestore_lockstep.py: line 56-90, 99-110 의 axisMetrics dict 박제 시나리오 박제 distance 필드 (pelvisDistanceFromPoleAxis / chestDistanceFromPoleAxis / scaleDenominator / coordinateSpace / deviationDirection) 제거 + 6 필드 박제 시나리오로 정합. test_force_signals_layer2_round_trip 박제 시나리오 (if present) 도 동일 정합.
  - backend/tests/phase08/test_compute_axis_deviation.py: AxisDeviationMetric 인스턴스화 박제 시나리오 박제 5 필드 kwarg 제거. compute_axis_deviation 본체 변경은 Wave 1 — 본 plan 박제 test 는 distance 산출 path 박제 시나리오 (line=None branch, line present branch) 박제 두 branch 모두 6 필드 dataclass 박제 정합만 검증 (distance 값 자체 검증 X — Wave 1 박제).

Step 9 — atomic commit:
  - `git add` 8 files (force_signals.py, analysis.ts, contract.md, userAnalyses.ts if changed, firestore_admin.py if changed, test_force_signals_lockstep.py, test_firestore_lockstep.py, test_compute_axis_deviation.py, backend/tests/phase08_1/{__init__.py, conftest.py, test_axis_schema_lockstep.py, test_axis_dataclass_invariants.py}).
  - commit message: `feat(08.1-00): AxisDeviationMetric distance hard break — 3-way lockstep atomic (D-01) + Phase 8 test obsolete cleanup`
  </action>
  <verify>
    <automated>cd backend &amp;&amp; PYTHONPATH=shared/python pytest tests/phase08_1/test_axis_schema_lockstep.py tests/phase08_1/test_axis_dataclass_invariants.py -v</automated>
    <automated>cd backend &amp;&amp; PYTHONPATH=shared/python pytest tests/phase08/ -x</automated>
    <automated>cd app &amp;&amp; npm run typecheck</automated>
    <automated>grep -v '^[ \t]*//' app/src/types/analysis.ts | awk '/^export interface AxisDeviationMetric/,/^\}/' | grep -cE 'pelvisDistanceFromPoleAxis|chestDistanceFromPoleAxis|scaleDenominator|coordinateSpace|deviationDirection'</automated>
    <automated>awk '/^### §9.3 AxisDeviationMetric/,/^### §9.[4-9]/' docs/contract.md | grep -cE 'pelvisDistanceFromPoleAxis|chestDistanceFromPoleAxis|scaleDenominator|coordinateSpace|deviationDirection'</automated>
  </verify>
  <done>
    - AxisDeviationMetric dataclass (Python) has exactly 6 fields: phase, shoulder_tilt, hip_tilt, severity, confidence, warnings.
    - TS interface AxisDeviationMetric has exactly 6 fields (camelCase mirror).
    - docs/contract.md §9.3 table has exactly 6 field rows + Phase 8.1 박제 메모 + footer version log entry.
    - 5 grep checks above return 0 (distance fields removed from interface block and §9.3 block).
    - phase08_1 tests (5 tests in test_axis_schema_lockstep.py + 5 tests in test_axis_dataclass_invariants.py) all PASS.
    - Existing phase08 tests still PASS (after distance-field fixture cleanup in step 8).
    - `tsc --noEmit` clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Firestore scoped validator backwards-compat + regression sweep</name>
  <files>backend/tests/phase08_1/test_firestore_axis_validator.py</files>
  <behavior>
    `_validate_force_signals_report` 박제 axis dict 검증 — 본 plan 의 6 필드 dict + legacy 5 필드 dict (Phase 8 박제 doc) 박제 둘 다 PASS (backwards-compat scalar). list[list] / list[dict] in metric 박제 시 ValueError 박제 (firestore-nested-array-flat 보존).
    Tests:
      - test_axis_metric_six_fields_pass: axisMetrics = [{phase, shoulderTilt, hipTilt, severity, confidence, warnings}] → no exception.
      - test_axis_metric_warnings_list_str_pass: axisMetrics = [{... , warnings: ['tilt_unavailable']}] → no exception.
      - test_axis_metric_legacy_distance_field_backwards_compat: axisMetrics = [{phase, pelvisDistanceFromPoleAxis: 3.05, chestDistanceFromPoleAxis: 3.10, coordinateSpace: 'pole_aligned', scaleDenominator: 'observed_torso_length', deviationDirection: 'outward', shoulderTilt, hipTilt, severity, confidence, warnings}] (legacy Phase 8 doc) → no exception (scalar fields are allowed by validator; backwards-compat).
      - test_axis_metric_nested_list_rejects: axisMetrics = [{phase, weird: [[1,2],[3,4]], shoulderTilt, ...}] → ValueError raise (firestore-nested-array-flat invariant).
      - test_axis_metric_nested_dict_rejects: axisMetrics = [{phase, nested: {a: 1}, ...}] → ValueError raise.
  </behavior>
  <action>
backend/tests/phase08_1/test_firestore_axis_validator.py 박제 — Phase 8 의 test_firestore_lockstep.py 박제 pattern 정합. import `from sunity_shared.firestore_admin import _validate_force_signals_report`. 박제 5 tests above. payload 박제 dict 의 forceSignalsReport.axisMetrics list[dict] 박제 — 본 validator 가 `path='forceSignalsReport'` 박제 호출. 각 test 은 dict literal 박제 + `_validate_force_signals_report(payload)` 호출 + pytest.raises 박제. test_axis_metric_legacy_distance_field_backwards_compat 박제 = Wave 2 의 read-side 박제 정합 검증 (frontend normalize 가 6 필드만 picks up — legacy 5 필드 박제 silently 무시).

backwards-compat 검증 박제 근거: D-01 박제 정합 — '기존 Firestore docs 의 distance 필드 = 무시 (read-side 가 안 읽음, backfill X)'. validator 박제 변경 0 박제 정합 — scalar 필드 박제 backwards-compat 자동 박제.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; PYTHONPATH=shared/python pytest tests/phase08_1/test_firestore_axis_validator.py -v</automated>
    <automated>cd backend &amp;&amp; PYTHONPATH=shared/python pytest tests/phase06 tests/phase07 tests/phase08 tests/phase08_1 tests/pipeline -x</automated>
  </verify>
  <done>
    - 5 tests in test_firestore_axis_validator.py PASS.
    - phase06 156 + phase07 88 + phase08 (post-cleanup count) + phase08_1 (13 new) + pipeline 11 = total PASS + 1 skipped maintained.
    - tsc --noEmit clean (no app changes from Task 2).
    - commit: `test(08.1-00): firestore scoped validator backwards-compat tests`.
  </done>
</task>

</tasks>

<verification>
- Schema lockstep: TS + Python + docs §9.3 의 AxisDeviationMetric 박제 6 필드 set 정합 (test_axis_schema_lockstep.py 6 tests PASS).
- Dataclass invariants: 6 필드 인스턴스화 PASS + invalid severity/confidence/phase ValueError + 제거된 5 필드 kwarg TypeError (test_axis_dataclass_invariants.py 5 tests PASS).
- Firestore validator: 6 필드 + legacy 5 필드 dict 박제 둘 다 PASS + nested list/dict 박제 ValueError 박제 (test_firestore_axis_validator.py 5 tests PASS).
- Regression: phase06 + phase07 + phase08 + phase08_1 + pipeline 모든 test 박제 PASS (회귀 0). Phase 8 의 distance-field fixture cleanup 박제 정합.
- TS strict mode clean (`npm run typecheck`).
- Wave 1 진입 준비 박제 — AxisDeviationMetric 6 필드 박제 contract 위에 compute_axis_deviation 본체 재작성 가능.
</verification>

<success_criteria>
- AxisDeviationMetric 박제 6 필드 박제 (TS + Python + docs §9.3 atomic).
- 제거된 5 필드 (pelvisDistanceFromPoleAxis / chestDistanceFromPoleAxis / scaleDenominator / coordinateSpace / deviationDirection) 박제 3-way grep 박제 0.
- phase08_1 신설 test 13개 PASS (8 schema + 5 dataclass + 5 validator).
- 회귀 0: phase06 + phase07 + phase08 + pipeline + phase08_1 모두 PASS.
- 정은지 sweep 결과 박제 변경 0 (Wave 0 = 정적 schema 변경만, production behavior 변경은 Wave 1 + Wave 2). 정은지 5/5 영상 axis severity 박제 측정은 Wave 2.
- Wave 1 진입 차단 해소 — AxisDeviationMetric 6 필드 박제 contract 위에 compute_axis_deviation 본체 재작성 가능.
</success_criteria>

<output>
Create `.planning/phases/08.1-axis-metric-redesign/08.1-00-SUMMARY.md` when done.
</output>
