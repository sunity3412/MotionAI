---
mandatory_iteration_2_patches: CODEX-PATCHES.md (C-B1 + C-H1 + C-MH1 적용 — execute-phase 진입 전 본 patches 정합 확인 필수)
phase: 08.1-axis-metric-redesign
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/shared/python/sunity_shared/analysis/force_signals.py
  - app/src/types/analysis.ts
  - docs/contract.md
  - app/src/lib/userAnalyses.ts
  - backend/shared/python/sunity_shared/firestore_admin.py
  - backend/tests/phase08/test_compute_axis_deviation.py
  - backend/tests/phase08/test_force_signals_lockstep.py
  - backend/tests/phase08/test_firestore_lockstep.py
  - backend/tests/phase08_1/__init__.py
  - backend/tests/phase08_1/conftest.py
  - backend/tests/phase08_1/test_axis_schema_lockstep.py
  - backend/tests/phase08_1/test_axis_dataclass_invariants.py
  - backend/tests/phase08_1/test_firestore_axis_validator.py
autonomous: true
requirements: [FORCE-01]
subsystem: backend-contract-frontend
tags: [phase-08-1, axis-metric-redesign, wave-0-schema, 3-way-lockstep, distance-hard-break]

scope_sanity_note: "Wave 0 of 3 (D-06 정합). 단일 목적 = D-01 (Distance Hard Break) 의 3-way lockstep atomic migration. AxisDeviationMetric 에서 5 필드 제거 — `pelvis_distance_from_pole_axis` / `chest_distance_from_pole_axis` / `scale_denominator` / `coordinate_space` / `deviation_direction`. 남기는 6 필드: `phase` / `shoulder_tilt` / `hip_tilt` / `severity` / `confidence` / `warnings`. compute_axis_deviation 함수 본체 는 본 plan 박제 transitional stub 으로 교체 (모든 phase 에 대해 shoulder_tilt=None / hip_tilt=None / severity='low' / confidence='low' / warnings=['phase_8_1_wave_0_transitional'] 반환). 본체 의 실 tilt 알고리즘 + helper cleanup + threshold yaml 은 Wave 1. pipeline rewire + production verification 은 Wave 2. 본 plan 의 정적 성격 (algorithm 변경 0, 위험 낮음, 회귀 0 강제) 유지."

must_haves:
  truths:
    - "본 plan 의 단일 목적 = D-01 (Distance Hard Break) 의 3-way lockstep atomic — 5 필드 제거를 TS interface + Python dataclass + docs §9.3 한 commit 안에서 동시 변경. wave 분리 = D-06 정합 (정적 / 알고리즘 / production 통합 3 wave atomic boundary). per D-01."
    - "AxisDeviationMetric 6 필드: `phase` (MotionPhase) / `shoulder_tilt` (float | None, degrees) / `hip_tilt` (float | None, degrees) / `severity` (SeverityLevel) / `confidence` (MetricConfidence) / `warnings` (list[str]). 제거된 5 필드: `pelvis_distance_from_pole_axis` / `chest_distance_from_pole_axis` / `scale_denominator` / `coordinate_space` / `deviation_direction`. per D-01 + RESEARCH §4.3."
    - "Python dataclass __post_init__ 검증 단순화 — coordinate_space / scale_denominator / deviation_direction 검증 분기 제거. severity + confidence + phase 검증 분기만 유지. line=None / coordinate_space='unavailable' invariant 제거 (필드 자체 부재). per D-01."
    - "compute_axis_deviation 본체 = transitional stub 으로 교체. stub 의 signature 는 Wave 1 + Wave 2 와 호환 — `compute_axis_deviation(pose_frames, phase_boundaries, pole_axis_measurement, *, fps=9.0) -> list[AxisDeviationMetric]`. stub 본문은 phase_boundaries 의 각 entry 에 대해 `AxisDeviationMetric(phase=b.phase, shoulder_tilt=None, hip_tilt=None, severity='low', confidence='low', warnings=['phase_8_1_wave_0_transitional'])` 반환. distance 산출 로직 (L1037~1227 의 helper 호출 전체) 는 본 plan 에서 호출 끊김 — helper 함수 자체 는 보존 (Wave 1 가 cleanup). 모듈 상수 `AXIS_PELVIS_DISTANCE_THRESHOLDS` / `AXIS_CHEST_DISTANCE_THRESHOLDS` / `AXIS_TILT_THRESHOLDS_DEG` 도 보존 (Wave 1 가 cleanup)."
    - "stub 의 효과: Wave 0 commit 직후 정은지 5/5 영상 모든 phase 의 axis severity = 'low' (default). 단, shoulder_tilt / hip_tilt = None + warnings = ['phase_8_1_wave_0_transitional'] 이라 실 측정값은 부재 — 실 tilt 측정 + 정은지 분포 기반 threshold 는 Wave 1 가 배포. Wave 2 의 production sweep 검증 시 stub 잔여 흔적 (warning 'phase_8_1_wave_0_transitional') 이 발견되면 Pod 재배포 미완으로 판단 — Wave 2 의 게이트가 잡아냄."
    - "TS interface AxisDeviationMetric 6 필드 — TS 측 5 필드 제거: `pelvisDistanceFromPoleAxis` / `chestDistanceFromPoleAxis` / `scaleDenominator` / `coordinateSpace` / `deviationDirection`. 남기는 필드 6: `phase` / `shoulderTilt` / `hipTilt` / `severity` / `confidence` / `warnings`. JSDoc 갱신 — Phase 8 REVIEWS R1/R2 메모 제거 + Phase 8.1 distance hard break 메모 신설."
    - "docs/contract.md §9.3 갱신 — 표에서 5 필드 row 제거 (pelvisDistanceFromPoleAxis / chestDistanceFromPoleAxis / scaleDenominator / coordinateSpace / deviationDirection). 박제 메모 (R1 + R2) 제거 + Phase 8.1 distance hard break 메모 신설 — '본 phase 의 axis 차원 = tilt-only (IPSF Code of Points 에 글로벌 distance 항목 부재 — NotebookLM citation 9 Page 87 Glossary)'. footer 의 version log 에 *Plan 08.1-00 §9.3 변경: distance 필드 제거* 항목 신설."
    - "Frontend null-guard 보존 (app/src/lib/userAnalyses.ts L70-88) — 현재 코드 `axisMetrics: report.axisMetrics ?? []` 는 직접 spread 패턴 이라 제거된 필드를 reference 하지 않음 → 코드 변경 0. Task 1 Step 6 에서 grep 으로 0건 확인. immutable spread 패턴 (Phase 7 WR-02 B1) 유지."
    - "Firestore scoped validator `_validate_force_signals_report` (firestore_admin.py L131-209) — 화이트리스트 `_FORCE_SIGNALS_SCALAR_LIST_KEYS_IN_METRIC` (warnings / unstableBodyParts) 변경 0. 제거된 5 필드 는 scalar (str/float/None) 라 validator 가 unknown 필드 silently 통과. legacy Firestore docs (Phase 8 sweep_phase8_1780986673) 의 distance 필드는 read-side 에서 무시 — backwards-compat 자동 (D-01 정합, no backfill)."
    - "Phase 8 의 test_compute_axis_deviation.py / test_force_signals_lockstep.py / test_firestore_lockstep.py 박제 정합 수술 — Task 1 Step 8 의 audit list 참조. test 삭제 가 아니라 stub 동작 검증 으로 변환 (severity='low' default + warnings=['phase_8_1_wave_0_transitional']). 5 필드 reference 가 들어간 assertion 만 제거 + stub 동작 assertion 으로 대체. test 갯수 감소 분 수치는 Wave 0 종료 시 명시적으로 audit (예: phase08 103 → 99 PASS — 본 plan 의 Step 8 사후 audit 으로 확정)."
    - "신설 Test 3개 (backend/tests/phase08_1/):\n  · test_axis_schema_lockstep.py — TS interface + Python dataclass + docs §9.3 의 6 필드 set 정합 grep 검증 (제거된 5 필드 부재 + 남기는 6 필드 존재).\n  · test_axis_dataclass_invariants.py — AxisDeviationMetric 6 필드 dataclass 인스턴스화 PASS / invalid severity ValueError / invalid confidence ValueError / invalid phase ValueError 검증. 제거된 5 필드 kwarg 시 TypeError raise.\n  · test_firestore_axis_validator.py — `_validate_force_signals_report` 호출 시 새 6 필드 axisMetrics dict PASS + 제거된 5 필드 dict 도 PASS (scalar backwards-compat) + nested list/dict ValueError."
    - "Phase 08.1 test infra (Phase 7/8 패턴 정합) — backend/tests/phase08_1/__init__.py 빈 marker + backend/tests/phase08_1/conftest.py (PHASE08_1_FIXTURES_DIR 상수 + 본 plan 의 unit test source). conftest 는 phase08 fixtures import 가능 (cross-phase factory reuse, REVIEWS R9 정합)."
    - "Wave 0 종료 시 회귀 게이트 (exact target) — phase06 156 + phase07 88 + phase08 99 (post stub-cleanup; 103 baseline − 4 distance-only assertions converted to stub-only) + phase08_1 13 (3 schema lockstep + 5 dataclass invariants + 5 firestore validator) + pipeline 11 = 367 PASS + 1 skipped. tsc --noEmit clean. 본 plan 의 production behavior 변경 = stub 의 severity='low' default (Wave 1 + Wave 2 가 실 측정 + 검증)."
  artifacts:
    - path: backend/shared/python/sunity_shared/analysis/force_signals.py
      provides: "AxisDeviationMetric dataclass 6 필드 (5 필드 제거). __post_init__ 검증 단순화 (severity + confidence + phase 만). compute_axis_deviation 함수 본체 = transitional stub (`AxisDeviationMetric(phase=b.phase, shoulder_tilt=None, hip_tilt=None, severity='low', confidence='low', warnings=['phase_8_1_wave_0_transitional'])` per phase boundary). distance helper 함수들 (L825-1034) 보존 — Wave 1 가 cleanup. 모듈 상수 AXIS_PELVIS_DISTANCE_THRESHOLDS / AXIS_CHEST_DISTANCE_THRESHOLDS / AXIS_TILT_THRESHOLDS_DEG 보존 — Wave 1 가 정리."
      contains:
        - "class AxisDeviationMetric"
        - "phase: MotionPhase"
        - "shoulder_tilt: float | None"
        - "hip_tilt: float | None"
        - "severity: SeverityLevel"
        - "confidence: MetricConfidence"
        - "warnings: list[str]"
        - "phase_8_1_wave_0_transitional"
    - path: app/src/types/analysis.ts
      provides: "TS interface AxisDeviationMetric 6 필드 (5 필드 제거). JSDoc 갱신 — Phase 8.1 distance hard break 메모 (IPSF Code of Points 글로벌 distance 항목 부재 + NotebookLM citation 9). Phase 8 REVIEWS R1/R2 메모 제거. 다른 interface 변경 0."
      contains:
        - "export interface AxisDeviationMetric"
        - "phase: MotionPhase"
        - "shoulderTilt: number | null"
        - "hipTilt: number | null"
        - "severity: SeverityLevel"
        - "confidence: MetricConfidence"
        - "warnings: string[]"
        - "Phase 8.1"
    - path: docs/contract.md
      provides: "§9.3 AxisDeviationMetric 표 갱신 — 5 필드 row 제거. R1 + R2 메모 제거 + Phase 8.1 distance hard break 메모 신설 (IPSF citation). footer 의 version log 에 Plan 08.1-00 §9.3 변경 항목 신설. 다른 섹션 변경 0."
      contains:
        - "### §9.3 AxisDeviationMetric"
        - "shoulderTilt"
        - "hipTilt"
        - "Phase 8.1"
        - "IPSF Code of Points"
        - "Plan 08.1-00"
    - path: app/src/lib/userAnalyses.ts
      provides: "normalize() 의 forceSignalsReport null-guard 보존 (L70-88) — `axisMetrics: report.axisMetrics ?? []` 직접 spread, 5 필드 reference 없음 → 코드 변경 0. Task 1 Step 6 의 grep 으로 검증."
      contains:
        - "forceSignalsReport"
        - "axisMetrics"
    - path: backend/shared/python/sunity_shared/firestore_admin.py
      provides: "`_validate_force_signals_report` 변경 0 (화이트리스트 `_FORCE_SIGNALS_SCALAR_LIST_KEYS_IN_METRIC` 보존). 제거된 5 필드 = scalar → backwards-compat 자동. 본 파일 Task 1 박제 실 수정 = grep 검증만 (실 수정 0 가능)."
      contains:
        - "_validate_force_signals_report"
        - "_FORCE_SIGNALS_SCALAR_LIST_KEYS_IN_METRIC"
    - path: backend/tests/phase08/test_compute_axis_deviation.py
      provides: "Phase 8 axis test 의 stub 동작 정합 수술 (Task 1 Step 8). 5 필드 reference assertion 4건 제거 (pelvis_distance / chest_distance / scale_denominator / coordinate_space / deviation_direction 검증) + 본체 stub 동작 검증 추가 (severity='low' + warnings contains 'phase_8_1_wave_0_transitional'). 총 test 수: 11 → 7 PASS (4 assertion 제거)."
      contains:
        - "compute_axis_deviation"
        - "phase_8_1_wave_0_transitional"
        - "severity"
    - path: backend/tests/phase08/test_force_signals_lockstep.py
      provides: "_FIELD_MAP 박제 5 필드 entry 제거 — pelvisDistanceFromPoleAxis / chestDistanceFromPoleAxis / scaleDenominator / coordinateSpace / deviationDirection (L61, 62, 65, 72, 73). 3 lockstep test 함수 PASS 유지."
      contains:
        - "_FIELD_MAP"
        - "shoulderTilt"
        - "hipTilt"
    - path: backend/tests/phase08/test_firestore_lockstep.py
      provides: "axisMetrics dict fixture 박제 5 필드 (pelvisDistanceFromPoleAxis = None 등) 제거 + 6 필드 dict 로 정합. 기존 test 함수 갯수 보존."
      contains:
        - "axisMetrics"
        - "shoulderTilt"
    - path: backend/tests/phase08_1/__init__.py
      provides: "phase08_1 test 패키지 marker (빈 파일)."
      contains: []
    - path: backend/tests/phase08_1/conftest.py
      provides: "phase08_1 공용 pytest fixture — PHASE08_1_FIXTURES_DIR 상수 + Phase 8 fixture factory import 가능."
      contains:
        - "PHASE08_1_FIXTURES_DIR"
        - "import pytest"
    - path: backend/tests/phase08_1/test_axis_schema_lockstep.py
      provides: "3-way lockstep grep 검증 — TS interface + Python dataclass + docs §9.3 의 6 필드 set 정합. 제거된 5 필드 부재 + 남기는 6 필드 존재. per D-01."
      contains:
        - "def test_axis_six_fields_present_in_ts"
        - "def test_axis_six_fields_present_in_python"
        - "def test_axis_six_fields_present_in_docs"
        - "def test_distance_fields_removed_from_ts"
        - "def test_distance_fields_removed_from_python"
        - "def test_distance_fields_removed_from_docs"
        - "shoulder_tilt"
    - path: backend/tests/phase08_1/test_axis_dataclass_invariants.py
      provides: "AxisDeviationMetric 6 필드 인스턴스화 PASS + invalid severity/confidence/phase ValueError + 제거된 5 필드 kwarg 시 TypeError."
      contains:
        - "def test_axis_dataclass_six_fields_instantiation"
        - "def test_axis_dataclass_invalid_severity_raises"
        - "def test_axis_dataclass_invalid_confidence_raises"
        - "def test_axis_dataclass_invalid_phase_raises"
        - "def test_axis_dataclass_distance_kwarg_raises_typeerror"
        - "AxisDeviationMetric"
        - "pytest.raises"
    - path: backend/tests/phase08_1/test_firestore_axis_validator.py
      provides: "_validate_force_signals_report 박제 axis dict 검증 — 새 6 필드 dict PASS + legacy 5 필드 dict PASS (backwards-compat scalar) + nested list/dict ValueError."
      contains:
        - "def test_axis_metric_six_fields_pass"
        - "def test_axis_metric_warnings_list_str_pass"
        - "def test_axis_metric_legacy_distance_field_backwards_compat"
        - "def test_axis_metric_nested_list_rejects"
        - "def test_axis_metric_nested_dict_rejects"
        - "_validate_force_signals_report"
  key_links:
    - from: app/src/types/analysis.ts
      to: backend/shared/python/sunity_shared/analysis/force_signals.py
      via: "AxisDeviationMetric 의 6 필드 set — TS interface 의 필드 set + Python dataclass 의 필드 set 정합 grep. test_axis_schema_lockstep.py 가 양쪽 grep 검증."
      pattern: "shoulder_tilt|shoulderTilt|hip_tilt|hipTilt|warnings"
    - from: app/src/types/analysis.ts
      to: docs/contract.md
      via: "AxisDeviationMetric §9.3 의 6 필드 표 — TS interface 의 필드 set + docs 표의 row set 정합 grep."
      pattern: "shoulderTilt|hipTilt|warnings"
    - from: backend/shared/python/sunity_shared/firestore_admin.py
      to: backend/shared/python/sunity_shared/analysis/force_signals.py
      via: "_validate_force_signals_report 의 axisMetrics list[dict] — 6 필드 metric dict + warnings list[str] 허용. AxisDeviationMetric 박제 직접 import 안 함 (scoped validator 가 generic dict 검증)."
      pattern: "_validate_force_signals_report|axisMetrics"
    - from: app/src/lib/userAnalyses.ts
      to: app/src/types/analysis.ts
      via: "normalize() 의 forceSignalsReport mapping 박제 — TS interface AxisDeviationMetric 박제 6 필드 직접 사용 X (직접 spread). 변경 0 — Task 1 grep 박제로 확인."
      pattern: "AxisDeviationMetric|axisMetrics"
---

<objective>
Phase 8 의 `AxisDeviationMetric` schema 에서 distance 차원 5 필드를 3-way lockstep atomic 으로 제거하고, 동일 commit 안에서 compute_axis_deviation 함수 본체를 transitional stub 으로 교체한다. RESEARCH §4 의 α-4 결정 (IPSF Code of Points 에 글로벌 distance 항목 부재 — NotebookLM citation 9) + CONTEXT D-01 (Distance Hard Break) + D-06 (Wave 0/1/2 atomic boundary) 정합. Wave 1 의 tilt metric 실 구현 + 정은지 분포 calibration 과 Wave 2 의 production verification 이 본 plan 의 contract + stub 위에 박제.

Purpose: D-01 의 정적 schema 변경을 단일 commit 에서 atomic 으로 처리 — TS interface + Python dataclass + docs §9.3 동시 박제 + compute_axis_deviation 함수 stub. 본 plan 종료 시 Phase 8.1 의 contract foundation 박제. stub 은 모든 phase 에 대해 severity='low' default 를 반환하여 정은지 baseline 의 일관성 ("low" expected on champion footage) 을 단조 보존하고, warning 'phase_8_1_wave_0_transitional' 로 Wave 1 미완 상태를 명시한다. Wave 2 의 production sweep 이 이 warning 의 부재를 게이트로 사용한다.

Output:
- backend/shared/python/sunity_shared/analysis/force_signals.py (AxisDeviationMetric dataclass 6 필드 + compute_axis_deviation 본체 transitional stub)
- app/src/types/analysis.ts (TS interface 6 필드)
- docs/contract.md §9.3 (표 갱신 + Phase 8.1 박제 메모)
- app/src/lib/userAnalyses.ts (null-guard 검증, 코드 변경 0 기대)
- backend/shared/python/sunity_shared/firestore_admin.py (scoped validator 박제 보존)
- backend/tests/phase08/ 3개 파일 수술 (distance assertion 4건 제거 + stub 동작 assertion 추가)
- backend/tests/phase08_1/ (신설 test infra + 3 unit test 파일)
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-axis-metric-redesign/08.1-CONTEXT.md
@.planning/phases/08.1-axis-metric-redesign/08.1-RESEARCH.md
@.planning/phases/08.1-axis-metric-redesign/README.md
@.planning/phases/08-jerk-jitter/PHASE8-INHERITED-ISSUES.md
@.planning/phases/08-jerk-jitter/08-03-SUMMARY.md

@backend/shared/python/sunity_shared/analysis/force_signals.py
@app/src/types/analysis.ts
@docs/contract.md
@app/src/lib/userAnalyses.ts
@backend/shared/python/sunity_shared/firestore_admin.py
@backend/tests/phase08/test_force_signals_lockstep.py
@backend/tests/phase08/test_compute_axis_deviation.py
@backend/tests/phase08/test_firestore_lockstep.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AxisDeviationMetric 3-way lockstep atomic + compute_axis_deviation transitional stub + Phase 8 test 수술</name>
  <files>backend/shared/python/sunity_shared/analysis/force_signals.py, app/src/types/analysis.ts, docs/contract.md, backend/tests/phase08/test_compute_axis_deviation.py, backend/tests/phase08/test_force_signals_lockstep.py, backend/tests/phase08/test_firestore_lockstep.py, backend/tests/phase08_1/__init__.py, backend/tests/phase08_1/conftest.py, backend/tests/phase08_1/test_axis_schema_lockstep.py, backend/tests/phase08_1/test_axis_dataclass_invariants.py</files>
  <behavior>
    RED → GREEN cycle for 3-way lockstep schema + transitional stub.

    Failing tests first (phase08_1/test_axis_schema_lockstep.py + phase08_1/test_axis_dataclass_invariants.py):
      - test_axis_six_fields_present_in_ts: grep app/src/types/analysis.ts for `phase`, `shoulderTilt`, `hipTilt`, `severity`, `confidence`, `warnings` inside `export interface AxisDeviationMetric` block (exactly 6 fields).
      - test_axis_six_fields_present_in_python: import force_signals.AxisDeviationMetric, assert dataclasses.fields() names == {phase, shoulder_tilt, hip_tilt, severity, confidence, warnings} (exactly 6).
      - test_axis_six_fields_present_in_docs: read §9.3 table block, assert all 6 field names present (camelCase + snake_case rows).
      - test_distance_fields_removed_from_ts: assert pelvisDistanceFromPoleAxis / chestDistanceFromPoleAxis / scaleDenominator / coordinateSpace / deviationDirection ABSENT inside the AxisDeviationMetric interface block (scope = interface block only — CoordinateSpace type alias elsewhere in file is allowed).
      - test_distance_fields_removed_from_python: assert dataclass fields() does NOT contain pelvis_distance_from_pole_axis / chest_distance_from_pole_axis / scale_denominator / coordinate_space / deviation_direction.
      - test_distance_fields_removed_from_docs: read §9.3 table block, assert 5 removed-field rows absent.
      - test_axis_dataclass_six_fields_instantiation: AxisDeviationMetric(phase='hold', shoulder_tilt=12.5, hip_tilt=8.3, severity='low', confidence='medium', warnings=[]) PASS.
      - test_axis_dataclass_invalid_severity_raises: severity='invalid' → ValueError.
      - test_axis_dataclass_invalid_confidence_raises: confidence='invalid' → ValueError.
      - test_axis_dataclass_invalid_phase_raises: phase='invalid' → ValueError.
      - test_axis_dataclass_distance_kwarg_raises_typeerror: passing pelvis_distance_from_pole_axis=1.0 → TypeError (unexpected keyword argument).

    GREEN: edit 3 source artifacts atomically + stub function body + surgically update phase08 tests.
  </behavior>
  <action>
Step 1 — Create test infra (test files first, RED state):
  - backend/tests/phase08_1/__init__.py: empty marker.
  - backend/tests/phase08_1/conftest.py: `from pathlib import Path` + `import pytest` + `PHASE08_1_FIXTURES_DIR = Path(__file__).parent / 'fixtures'` (dir creation deferred to Wave 1).
  - backend/tests/phase08_1/test_axis_schema_lockstep.py: write the 6 grep tests above. Use the same `_REPO_ROOT` / `_TS_PATH` / `_CONTRACT_PATH` pattern as backend/tests/phase08/test_force_signals_lockstep.py. For TS interface block extraction: regex `export interface AxisDeviationMetric \{[\s\S]*?\n\}`. For docs §9.3 block: regex from `### §9.3 AxisDeviationMetric` until next `### §9.[4-9]` heading. For Python: import via `from sunity_shared.analysis.force_signals import AxisDeviationMetric` and call `{f.name for f in dataclasses.fields(AxisDeviationMetric)}`.
  - backend/tests/phase08_1/test_axis_dataclass_invariants.py: write the 5 invariant tests above. Use `pytest.raises(ValueError)` for invalid severity/confidence/phase and `pytest.raises(TypeError)` for distance kwarg. For valid instantiation use `phase='hold'`, `warnings=[]`.

Step 2 — Python dataclass (RED → GREEN):
  - backend/shared/python/sunity_shared/analysis/force_signals.py L271-332:
    Replace the AxisDeviationMetric dataclass with the 6-field version. Fields:
      `phase: MotionPhase`
      `shoulder_tilt: float | None`
      `hip_tilt: float | None`
      `severity: SeverityLevel`
      `confidence: MetricConfidence`
      `warnings: list[str] = field(default_factory=list)`
    Replace `__post_init__` with three validators only:
      - `if self.phase not in _MOTION_PHASES: raise ValueError(...)`
      - `if self.severity not in _SEVERITY_LEVELS: raise ValueError(...)`
      - `if self.confidence not in _METRIC_CONFIDENCES: raise ValueError(...)`
    Remove the coordinate_space / scale_denominator / deviation_direction / distance-None invariant checks.
    Update class docstring: replace the REVIEWS R1/R2 block with: `Phase 8.1 박제 (2026-06-09): distance 차원 hard break — IPSF Code of Points 에 글로벌 distance 항목 부재 (NotebookLM citation 9, Aerial Pole CoP 2024-2025 Page 87 Glossary). tilt-only metric. Wave 0 = transitional stub (severity='low' default + warning 'phase_8_1_wave_0_transitional'). Wave 1 = 실 tilt 측정 알고리즘 배포. per D-01.`

Step 3 — compute_axis_deviation 함수 본체 stub 교체 (B1 stub option):
  - force_signals.py L1037-1227 — 전체 함수 본체 (시그너처 직후부터 return 까지) 를 아래 transitional stub 으로 교체. 기존 distance 산출 path 와 tilt 산출 path 전체 제거 (Wave 1 가 새 본체 박제). signature 자체는 보존:
    ```
    def compute_axis_deviation(
        pose_frames: list[PoseFrame],
        phase_boundaries: list[PhaseBoundary],
        pole_axis_measurement: PoleAxisMeasurement,
        *,
        fps: float = 9.0,
    ) -> list[AxisDeviationMetric]:
        """Phase 8.1 Wave 0 transitional stub — 실 tilt 측정 알고리즘 + threshold 는 Wave 1 가 배포.
        모든 phase 에 대해 severity='low' default + warning 'phase_8_1_wave_0_transitional' 반환.
        Wave 2 production sweep 이 이 warning 의 부재를 게이트로 사용 (Pod 재배포 완료 검증).
        per D-01 + D-06.
        """
        return [
            AxisDeviationMetric(
                phase=b.phase,
                shoulder_tilt=None,
                hip_tilt=None,
                severity="low",
                confidence="low",
                warnings=["phase_8_1_wave_0_transitional"],
            )
            for b in phase_boundaries
        ]
    ```
  - distance helper 함수들 (L825-1034: `_observed_torso_length` / `_observed_torso_length_pole_aligned` / `_pelvis_position_pole_aligned` / `_chest_position_pole_aligned` / `_pole_aligned_axis_distance` / `_pelvis_position_image_2d` / `_chest_position_image_2d` / `_line_direction_angle_deg` / `_severity_from_distance` / `_deviation_direction_from_pelvis` / `_kp_pole_aligned_xy` / `_kp2d_xy` / `_midpoint` / `_shoulder_tilt_2d` / `_hip_tilt_2d` / `_shoulder_tilt_pole_aligned` / `_hip_tilt_pole_aligned`) — **본 plan 에서 보존**. Wave 1 가 cleanup 책임. 단, `_observed_torso_length` 와 `_kp2d_xy` / `_midpoint` 는 `compute_contact_stability` (L1527 외) 에서도 호출됨 → Wave 1 도 이 두 함수는 보존.
  - 모듈 상수 `AXIS_PELVIS_DISTANCE_THRESHOLDS` / `AXIS_CHEST_DISTANCE_THRESHOLDS` / `AXIS_TILT_THRESHOLDS_DEG` (L163-165) — **본 plan 에서 보존**. Wave 1 가 cleanup + yaml lazy load 로 교체.
  - import smoke: `python -c "from sunity_shared.analysis.force_signals import AxisDeviationMetric, compute_axis_deviation; print('OK')"` PASS.

Step 4 — TS interface (app/src/types/analysis.ts L524-551):
  - Replace the AxisDeviationMetric interface block with the 6 fields: `phase: MotionPhase` / `shoulderTilt: number | null` / `hipTilt: number | null` / `severity: SeverityLevel` / `confidence: MetricConfidence` / `warnings: string[]`. Remove the 5 distance fields.
  - Update JSDoc (L524-534): replace the REVIEWS R1/R2 메모 block with:
    `/**
      * Axis deviation metric per phase (Phase 8.1, 2026-06-09).
      * Phase 8.1 distance hard break — IPSF Code of Points 에 글로벌 distance 항목 부재
      * (NotebookLM citation 9, Aerial Pole CoP 2024-2025 Page 87 Glossary).
      * tilt-only metric. shoulderTilt / hipTilt = degrees, rotation-only / origin-invariant.
      * severity = max(shoulderTilt severity, hipTilt severity) per Wave 1 thresholds.
      * Wave 0 transitional: shoulderTilt/hipTilt=null + severity='low' + warning 'phase_8_1_wave_0_transitional'.
      * per D-01.
      */`

Step 5 — docs/contract.md §9.3 (L841-860):
  - Update intro sentence: `중심축 이탈 metric — phase 별 측정. **Phase 8.1 박제 (2026-06-09)**: distance 차원 hard break (IPSF Code of Points 글로벌 distance 항목 부재, NotebookLM citation 9). tilt-only metric.`
  - Replace the table with exactly 6 rows. Header `| Field (camelCase) | Field (snake_case) | Type | Description |`. Rows: `phase / phase / MotionPhase / ...`; `shoulderTilt / shoulder_tilt / number \| null / degrees, rotation-only, pole_aligned 좌표계`; `hipTilt / hip_tilt / number \| null / degrees, rotation-only, pole_aligned 좌표계`; `severity / severity / SeverityLevel / max(shoulderTilt severity, hipTilt severity)`; `confidence / confidence / MetricConfidence / phase frame reliability ratio`; `warnings / warnings / string[] / e.g. 'tilt_unavailable', 'tilt_thresholds_fallback', 'phase_8_1_wave_0_transitional'`.
  - Replace the `박제 메모 (R1 + R2)` block with: `**박제 메모 (Phase 8.1)**: tilt 값은 rotation-only / origin-invariant (RESEARCH §4.3). shoulderTilt / hipTilt = arcsin(|Δz|/||Δ||) (pole_aligned 좌표계). 미가용 시 두 tilt 모두 null + severity='low' + warning 'tilt_unavailable'. per RESEARCH §4 α-4 + IPSF NotebookLM citation 9.`
  - Append to the file footer (after the existing version log entries): `*Plan 08.1-00 §9.3 변경: 2026-06-09 — AxisDeviationMetric distance 차원 hard break. 5 필드 (pelvisDistanceFromPoleAxis / chestDistanceFromPoleAxis / scaleDenominator / coordinateSpace / deviationDirection) 제거 + tilt-only. Wave 0 = transitional stub. IPSF Code of Points NotebookLM citation 9 (Page 87 Glossary — 'Tilt' / 'Lean' / 'Off-axis' 용어 부재) 정합. RESEARCH §4 α-4 + CONTEXT D-01.*`

Step 6 — Frontend null-guard verification:
  - app/src/lib/userAnalyses.ts L70-88 — 코드 변경 0 기대. 검증 명령: `grep -nE 'pelvisDistanceFromPoleAxis|chestDistanceFromPoleAxis|scaleDenominator|coordinateSpace|deviationDirection' app/src/lib/userAnalyses.ts`. 0 결과 expected. 만약 결과가 1 이상이면 immutable .map 패턴으로 변환 (Phase 7 WR-02 B1 정합) — 단 본 plan 의 현재 grep 결과 = 0 검증 완료 후 작업 진행.

Step 7 — Firestore validator verification:
  - backend/shared/python/sunity_shared/firestore_admin.py — 변경 0. 검증 명령: `grep -nE '_FORCE_SIGNALS_SCALAR_LIST_KEYS_IN_METRIC' backend/shared/python/sunity_shared/firestore_admin.py`. 화이트리스트 entry `warnings` + `unstableBodyParts` 의 보존 확인.

Step 8 — Phase 8 test 수술 (audit list 기반):
  본 step 은 distance 필드 reference 가 들어간 assertion 만 제거 + stub 동작 assertion 으로 대체. test 파일 자체를 삭제하지 않고, 각 함수 단위로 다음과 같이 수정한다.

  (a) backend/tests/phase08/test_compute_axis_deviation.py — 다음 assertion 4건 제거 + stub 동작 assertion 추가:
    - L51-55 `test_pelvis_distance_uses_observed_torso_length_denominator` 함수 전체 제거 (distance + scale_denominator + coordinate_space assertion 박제).
    - L58-65 `test_chest_distance_uses_observed_torso_length_denominator` 함수 전체 제거.
    - L68-79 `test_line_missing_returns_null_distance_with_warning` 함수 전체 제거.
    - L82-91 `test_scale_unavailable_returns_null_distance_with_warning` 함수 전체 제거.
    - L94-98 `test_coordinate_space_image_2d_when_line_present` 함수 전체 제거.
    - L101-106 `test_coordinate_space_unavailable_when_line_none` 함수 전체 제거.
    - L109-122 `test_torso_scale_not_used_as_denominator` 함수 보존 — distance 와 무관, force_signals 모듈 inspect 만 함. Phase 8 BodyNormalizationProfile 차단 invariant 는 Wave 1 까지 유효.
    - L125-134 `test_pelvis_drop_fixture_emits_high_severity_outward` 함수 제거 (Wave 0 stub 은 severity='low' 만 반환).
    - L137-144 `test_shoulder_tilt_signed_degrees` 함수 stub 동작 으로 수정 — 새 함수 본문: `metrics = compute_axis_deviation(frames, boundaries, pole); assert metrics[0].shoulder_tilt is None; assert 'phase_8_1_wave_0_transitional' in metrics[0].warnings`.
    - L147-153 `test_hip_tilt_signed_degrees` 함수 동일 패턴 stub 동작 으로 수정 — `assert metrics[0].hip_tilt is None`.
    - L156-162 `test_deviation_direction_outward` 함수 제거.
    수술 결과: 기존 11 test 함수 → 4 test 함수 (test_torso_scale_not_used_as_denominator + test_shoulder_tilt_signed_degrees stub + test_hip_tilt_signed_degrees stub + 추가 신설 `test_compute_axis_returns_stub_for_all_phases` — 3개 phase boundary 입력 시 3개 metric 반환 + 모두 severity='low' + 모두 warning 'phase_8_1_wave_0_transitional' 검증). 총 phase08 test 갯수: 103 → 99 (4 distance-driven 함수 제거 + 2 stub 함수 신설 = -7+2-2 = net 95? — 실제 audit 은 Step 8 실행 후 `pytest --collect-only -q | tail -3` 결과로 확정. 본 plan 의 frontmatter `must_haves.truths` 박제 99 target 박제 기준 — 실 audit 결과 가 다르면 frontmatter 갱신 후 commit). 보수적 target: phase08 99 PASS.

  (b) backend/tests/phase08/test_force_signals_lockstep.py — `_FIELD_MAP` (L36-89) 박제 다음 entry 제거:
    - L61 `("pelvisDistanceFromPoleAxis", "pelvis_distance_from_pole_axis", ...)`.
    - L62 `("chestDistanceFromPoleAxis", "chest_distance_from_pole_axis", ...)`.
    - L65 `("deviationDirection", "deviation_direction", ...)`.
    - L72 `("coordinateSpace", "coordinate_space", "REVIEWS R1/R2")`.
    - L73 `("scaleDenominator", "scale_denominator", "REVIEWS R2")`.
    - L88 `("coordinate_space_unavailable", "coordinate_space_unavailable", "warning code REVIEWS R1/R2")` — Wave 0 stub 은 'phase_8_1_wave_0_transitional' warning 만 emit → 'coordinate_space_unavailable' warning code 도 deprecated. 보존 시 docs/contract.md grep PASS 가 깨짐 (방금 §9.3 표에서 제거). entry 제거.
    3 test 함수 (test_ts_interface_has_all_fields / test_python_placeholder_has_all_fields / test_contract_md_has_all_fields) 박제 PASS 유지.

  (c) backend/tests/phase08/test_firestore_lockstep.py — L56-110 사이의 axisMetrics dict fixture 박제 distance 5 필드 entry 제거 + 6 필드 (phase / shoulderTilt / hipTilt / severity / confidence / warnings) 만 보존. 기존 test 함수 갯수 보존 — fixture 데이터 수술만.

Step 9 — atomic commit:
  - `git add backend/shared/python/sunity_shared/analysis/force_signals.py app/src/types/analysis.ts docs/contract.md backend/tests/phase08/test_compute_axis_deviation.py backend/tests/phase08/test_force_signals_lockstep.py backend/tests/phase08/test_firestore_lockstep.py backend/tests/phase08_1/__init__.py backend/tests/phase08_1/conftest.py backend/tests/phase08_1/test_axis_schema_lockstep.py backend/tests/phase08_1/test_axis_dataclass_invariants.py`.
  - commit message: `feat(08.1-00): AxisDeviationMetric distance hard break (3-way lockstep) + compute_axis_deviation transitional stub + Phase 8 test 수술 (D-01)`.
  - push immediately per [[gsd-pod-work-push-first]].
  </action>
  <verify>
    <automated>cd backend &amp;&amp; PYTHONPATH=shared/python pytest tests/phase08_1/test_axis_schema_lockstep.py tests/phase08_1/test_axis_dataclass_invariants.py -v</automated>
    <automated>cd backend &amp;&amp; PYTHONPATH=shared/python pytest tests/phase08/ -v</automated>
    <automated>cd app &amp;&amp; npm run typecheck</automated>
    <automated>grep -v '^[ \t]*//' app/src/types/analysis.ts | awk '/^export interface AxisDeviationMetric/,/^\}/' | grep -cE 'pelvisDistanceFromPoleAxis|chestDistanceFromPoleAxis|scaleDenominator|coordinateSpace|deviationDirection'</automated>
    <automated>awk '/^### §9.3 AxisDeviationMetric/,/^### §9.[4-9]/' docs/contract.md | grep -cE 'pelvisDistanceFromPoleAxis|chestDistanceFromPoleAxis|scaleDenominator|coordinateSpace|deviationDirection'</automated>
    <automated>python3 -c "from sunity_shared.analysis.force_signals import AxisDeviationMetric, compute_axis_deviation; import dataclasses; assert {f.name for f in dataclasses.fields(AxisDeviationMetric)} == {'phase','shoulder_tilt','hip_tilt','severity','confidence','warnings'}; print('OK')"</automated>
  </verify>
  <done>
    - AxisDeviationMetric Python dataclass has exactly 6 fields: phase, shoulder_tilt, hip_tilt, severity, confidence, warnings.
    - TS interface AxisDeviationMetric has exactly 6 fields (camelCase mirror).
    - docs/contract.md §9.3 table has exactly 6 field rows + Phase 8.1 박제 메모 + footer version log entry.
    - 2 awk-scoped grep checks return 0 (distance fields removed from interface block and §9.3 block).
    - compute_axis_deviation body is the stub — for any non-empty phase_boundaries input, all returned metrics have severity='low' + confidence='low' + warnings contains 'phase_8_1_wave_0_transitional' + shoulder_tilt=None + hip_tilt=None.
    - phase08_1 tests in test_axis_schema_lockstep.py (6 tests) + test_axis_dataclass_invariants.py (5 tests) all PASS — total 11.
    - Phase 8 tests: post-수술 phase08 test count documented (target: 99 PASS — actual count audited and recorded in the commit log if it differs).
    - `tsc --noEmit` clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Firestore scoped validator backwards-compat unit test + Wave 0 종료 회귀 게이트</name>
  <files>backend/tests/phase08_1/test_firestore_axis_validator.py</files>
  <behavior>
    `_validate_force_signals_report` 동작 검증 — 본 plan 의 6 필드 axisMetrics dict + legacy 5 필드 dict (Phase 8 박제 doc) 둘 다 PASS (scalar fields are silently passed by the scoped validator). nested list/dict in metric → ValueError (firestore-nested-array-flat invariant).

    Tests:
      - test_axis_metric_six_fields_pass: `forceSignalsReport.axisMetrics = [{phase, shoulderTilt, hipTilt, severity, confidence, warnings}]` → validator returns without exception.
      - test_axis_metric_warnings_list_str_pass: `warnings = ['tilt_unavailable']` → no exception (list[str] 화이트리스트).
      - test_axis_metric_legacy_distance_field_backwards_compat: `axisMetrics = [{phase, pelvisDistanceFromPoleAxis: 3.05, chestDistanceFromPoleAxis: 3.10, coordinateSpace: 'pole_aligned', scaleDenominator: 'observed_torso_length', deviationDirection: 'outward', shoulderTilt, hipTilt, severity, confidence, warnings}]` (legacy Phase 8 doc) → no exception (scalar fields 자동 backwards-compat).
      - test_axis_metric_nested_list_rejects: `axisMetrics = [{phase, weird: [[1,2],[3,4]], shoulderTilt, ...}]` → ValueError raise (firestore-nested-array-flat invariant).
      - test_axis_metric_nested_dict_rejects: `axisMetrics = [{phase, nested: {a: 1}, ...}]` → ValueError raise.
  </behavior>
  <action>
backend/tests/phase08_1/test_firestore_axis_validator.py — Phase 8 의 test_firestore_lockstep.py pattern 정합. `from sunity_shared.firestore_admin import _validate_force_signals_report` import. 5 tests above. 각 test 는 payload dict literal (forceSignalsReport.axisMetrics list[dict]) + `_validate_force_signals_report(payload, path='forceSignalsReport')` 호출 + `pytest.raises(ValueError)` (nested 2개).

backwards-compat 근거: D-01 정합 — '기존 Firestore docs 의 distance 필드 = 무시 (read-side 가 안 읽음, backfill X)'. validator 변경 0 — scalar 필드는 자동 backwards-compat. legacy 5 필드 dict 는 read 시 frontend normalize 가 6 필드만 picks up (TS interface 정합 — 미존재 필드는 silently drop).

Step 1 — write the 5 tests.
Step 2 — run `pytest tests/phase08_1/test_firestore_axis_validator.py -v` → all PASS.
Step 3 — Wave 0 종료 회귀 게이트 실행 — `pytest tests/phase06 tests/phase07 tests/phase08 tests/phase08_1 tests/pipeline -x` → expected total PASS count = 156 + 88 + 99 (post-수술) + 13 (3 lockstep + 5 dataclass + 5 validator) + 11 = 367 PASS + 1 skipped. 실 count 가 target 과 다르면 SUMMARY.md 에 정확한 audit 결과 기록 후 commit (frontmatter must_haves.truths 의 367 target 은 보수적 견적 — 실 audit 우선).
Step 4 — `cd app && npm run typecheck` → clean.
Step 5 — commit: `test(08.1-00): firestore scoped validator backwards-compat + Wave 0 회귀 gate`.
Step 6 — push.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; PYTHONPATH=shared/python pytest tests/phase08_1/test_firestore_axis_validator.py -v</automated>
    <automated>cd backend &amp;&amp; PYTHONPATH=shared/python pytest tests/phase06 tests/phase07 tests/phase08 tests/phase08_1 tests/pipeline -x 2>&amp;1 | tail -5</automated>
    <automated>cd app &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>
    - 5 tests in test_firestore_axis_validator.py PASS.
    - phase06 156 + phase07 88 + phase08 99 (post-수술) + phase08_1 13 (Wave 0 total) + pipeline 11 = 367 PASS + 1 skipped maintained. 실 audit 결과 가 다르면 commit message + SUMMARY.md 에 정확한 count 기록.
    - tsc --noEmit clean (no app changes from Task 2).
    - commit pushed.
  </done>
</task>

</tasks>

<verification>
- Schema lockstep: TS + Python + docs §9.3 의 AxisDeviationMetric 6 필드 set 정합 (test_axis_schema_lockstep.py 6 tests PASS).
- Dataclass invariants: 6 필드 인스턴스화 PASS + invalid severity/confidence/phase ValueError + 제거된 5 필드 kwarg TypeError (test_axis_dataclass_invariants.py 5 tests PASS).
- compute_axis_deviation 본체 = transitional stub — 모든 phase boundary 입력에 대해 severity='low' default + warning 'phase_8_1_wave_0_transitional' 반환. Wave 2 production sweep 이 이 warning 의 부재 를 Pod 재배포 완료 게이트로 사용.
- Firestore validator: 6 필드 + legacy 5 필드 dict 둘 다 PASS + nested list/dict ValueError (test_firestore_axis_validator.py 5 tests PASS).
- Regression: phase06 + phase07 + phase08 (post-수술) + phase08_1 + pipeline 모든 test PASS (회귀 0).
- TS strict mode clean (`npm run typecheck`).
- Wave 1 진입 준비 — AxisDeviationMetric 6 필드 contract + stub 위에 compute_axis_deviation 본체 실 tilt 알고리즘 + threshold yaml 박제 가능.
</verification>

<success_criteria>
- AxisDeviationMetric 6 필드 (TS + Python + docs §9.3 atomic).
- 제거된 5 필드 (pelvisDistanceFromPoleAxis / chestDistanceFromPoleAxis / scaleDenominator / coordinateSpace / deviationDirection) 의 3-way scoped grep 결과 = 0 (interface block + §9.3 block + Python dataclass fields 박제).
- compute_axis_deviation 함수 본체 = transitional stub. 동작 단위 test PASS (`test_compute_axis_returns_stub_for_all_phases` 신설 + tilt 동작 stub 변환 test 2개).
- phase08_1 신설 test 13개 PASS (6 schema + 5 dataclass + 5 validator). 또는 audit 후 실 count 으로 갱신.
- Phase 8 test 수술 후 정확한 count audit — target 99 PASS (103 baseline − 4 distance-only assertion 함수 제거 = 99; 실 audit 결과 우선).
- 회귀 0: phase06 + phase07 + phase08 (post-수술) + pipeline + phase08_1 모두 PASS + 1 skipped 유지.
- 정은지 sweep evidence 박제 변경 0 — 본 plan 은 정적 schema + stub. 실 측정 + 검증 은 Wave 1 + Wave 2.
- Wave 1 진입 차단 해소 — AxisDeviationMetric 6 필드 contract + stub 위에 본체 재작성 가능.
</success_criteria>

<output>
Create `.planning/phases/08.1-axis-metric-redesign/08.1-00-SUMMARY.md` when done. SUMMARY 에 다음 audit 결과 명시:
- phase08 post-수술 정확한 test count (collect-only 결과).
- phase08_1 신설 test count.
- 회귀 게이트 합산 PASS + skipped count.
- compute_axis_deviation stub 의 정상 동작 evidence (각 phase 별 severity='low' + warning 'phase_8_1_wave_0_transitional').
</output>
</content>
</invoke>