---
phase: 08.1-axis-metric-redesign
plan: 00
subsystem: backend-contract-frontend
tags: [phase-08-1, axis-metric-redesign, wave-0-schema, 3-way-lockstep, distance-hard-break]
dependency_graph:
  requires:
    - Phase 8 (close-out) — AxisDeviationMetric 11 필드 baseline schema
    - 08.1-CONTEXT.md D-01 (Distance Hard Break) + D-06 (Wave 0/1/2 boundary)
    - 08.1-RESEARCH.md §4 α-4 (IPSF Code of Points 글로벌 distance 항목 부재)
  provides:
    - AxisDeviationMetric 6 필드 contract (TS + Python + docs §9.3 atomic)
    - compute_axis_deviation transitional stub (Wave 1 가 실 본체 로 교체 가능)
    - Wave 0 stub detection 신호: warnings ['phase_8_1_wave_0_transitional'] (per-metric)
      + top-level 'axis_metric_transitional' (compute_force_signals umbrella)
    - phase08_1 test infra (__init__.py + conftest.py + PHASE08_1_FIXTURES_DIR)
  affects:
    - Wave 1: tilt metric 본체 알고리즘 + 정은지 분포 기반 threshold yaml
    - Wave 2: pipeline rewire + production sweep 검증 (stub warning 부재 게이트)
    - Phase 9: forceSignalsReport.axisMetrics raw signal only guard (severity 무시)
tech_stack:
  added: []
  patterns:
    - 3-way lockstep atomic — TS interface + Python dataclass + docs §9.3 single commit
    - Transitional stub — Wave 0 단독 production 진입 금지 신호 박제
    - Backwards-compat scalar fields — legacy Firestore docs 자동 통과 (no backfill)
key_files:
  created:
    - backend/tests/phase08_1/__init__.py
    - backend/tests/phase08_1/conftest.py
    - backend/tests/phase08_1/test_axis_schema_lockstep.py
    - backend/tests/phase08_1/test_axis_dataclass_invariants.py
    - backend/tests/phase08_1/test_firestore_axis_validator.py
    - backend/tests/phase08_1/test_compute_force_signals_does_not_reference_coordinate_space.py
  modified:
    - backend/shared/python/sunity_shared/analysis/force_signals.py
    - backend/shared/python/sunity_shared/models.py
    - app/src/types/analysis.ts
    - docs/contract.md
    - backend/tests/phase08/test_compute_axis_deviation.py
    - backend/tests/phase08/test_force_signals_lockstep.py
    - backend/tests/phase08/test_firestore_lockstep.py
decisions:
  - "AxisDeviationMetric 6 필드 atomic: phase / shoulder_tilt / hip_tilt / severity / confidence / warnings (5 distance 필드 hard break per D-01)"
  - "compute_axis_deviation Wave 0 stub: 모든 phase 에 대해 severity='low' default + warning 'phase_8_1_wave_0_transitional'"
  - "compute_force_signals C-B1 fix: coordinate_space == 'unavailable' 참조 → axis_metric_transitional 검출 로직 (모든 axis_metric warnings 박제 stub 신호 포함 시 top-level emit)"
  - "Wave 0 단독 production 진입 금지 (C-H1): Wave 0 + Wave 1 한 release boundary 로 Pod 재배포 (Wave 2 책임). belle Wave 0 commit 만으로 production 진입 시 SWEEP-EVIDENCE §0 stub gate 가 warning 25/25 검출"
  - "AxisDeviationMetric naming caveat 박제 (C-MH1): 실 의미 tilt-only 박제 docstring/JSDoc, rename (예: AxisTiltMetric) 은 별도 plan"
  - "Firestore validator 변경 0: scalar 필드 자동 backwards-compat. legacy 5 distance 필드 dict 박제 read 시 frontend normalize 가 6 필드만 picks up"
metrics:
  duration_min: 12
  completed_date: "2026-06-09"
---

# Phase 08.1 Plan 00: Axis Metric Schema Migration (Wave 0) Summary

**One-liner**: AxisDeviationMetric 5 distance 필드 영구 제거 + transitional stub 으로 교체 (TS + Python + docs §9.3 3-way lockstep atomic, IPSF Code of Points 박제 글로벌 distance 항목 부재 정합).

## Objective Recap

Phase 8 의 `AxisDeviationMetric` schema 에서 distance 차원 5 필드 (pelvisDistanceFromPoleAxis / chestDistanceFromPoleAxis / scaleDenominator / coordinateSpace / deviationDirection) 를 3-way lockstep atomic 으로 제거하고, 동일 commit 안에서 compute_axis_deviation 함수 본체를 transitional stub 으로 교체. RESEARCH §4 α-4 결정 (IPSF Code of Points 글로벌 distance 항목 부재, NotebookLM citation 9) + CONTEXT D-01 (Distance Hard Break) + D-06 (Wave 0/1/2 atomic boundary) 정합.

## What Got Built

### Task 1: 3-way lockstep atomic + transitional stub + Phase 8 test 수술

Single atomic commit (`6a294d5`) — TS interface + Python dataclass + docs §9.3 + models.py 주석 + compute_axis_deviation 함수 + Phase 8 기존 3 test 파일 수술 + phase08_1 신설 4 test 파일 모두 한 commit.

**AxisDeviationMetric Python dataclass** (`backend/shared/python/sunity_shared/analysis/force_signals.py`):

- 5 필드 영구 제거 — `pelvis_distance_from_pole_axis`, `chest_distance_from_pole_axis`, `coordinate_space`, `scale_denominator`, `deviation_direction`
- 6 필드 유지 — `phase`, `shoulder_tilt`, `hip_tilt`, `severity`, `confidence`, `warnings`
- `__post_init__` 검증 단순화 — phase / severity / confidence 3 분기만
- docstring: Phase 8.1 distance hard break + IPSF NotebookLM citation 9 + naming caveat (C-MH1)

**compute_axis_deviation 본체 stub 교체** — distance 산출 path 영구 제거. Wave 0 stub 본체:

```python
return [
    AxisDeviationMetric(
        phase=b.phase,
        shoulder_tilt=None,
        hip_tilt=None,
        severity="low",
        confidence="low",
        warnings=["phase_8_1_wave_0_transitional"],
    )
    for b in phase_boundaries
]
```

distance helper 함수들 (`_observed_torso_length` / `_pelvis_position_image_2d` / `_pole_aligned_axis_distance` / `_shoulder_tilt_2d` 등) + 모듈 상수 `AXIS_PELVIS_DISTANCE_THRESHOLDS` / `AXIS_CHEST_DISTANCE_THRESHOLDS` / `AXIS_TILT_THRESHOLDS_DEG` 는 본 plan 에서 보존 — Wave 1 가 cleanup.

**C-B1 fix** — `compute_force_signals` 본체 L1741-1744 의 `coordinate_space == 'unavailable'` 참조 제거 + `axis_metric_transitional` 검출 로직 으로 교체. 모든 `axis_metric.warnings` 박제 `'phase_8_1_wave_0_transitional'` 포함 시 top-level `warnings` 에 `'axis_metric_transitional'` 추가. C-H1 downstream guard 의 동일 신호.

**TS interface** (`app/src/types/analysis.ts`) — 5 필드 camelCase mirror 영구 제거. JSDoc: Phase 8.1 distance hard break + rotation-only / origin-invariant + Wave 0 transitional default + C-MH1 naming caveat. `CoordinateSpace` / `DeviationDirection` type alias 는 ContactStabilityMetric / PoleAxisMeasurement 박제 사용 유지 → 별도 선언 보존.

**docs/contract.md §9.3** — 표 6 행 (5 distance 행 제거). 인트로 갱신: "**Phase 8.1 박제 (2026-06-09)**: distance 차원 hard break (IPSF Code of Points 글로벌 distance 항목 부재, NotebookLM citation 9). tilt-only metric." 박제 메모 (R1 + R2) 교체 — Phase 8.1 tilt rotation-only / origin-invariant 박제. footer version log entry 신설.

**models.py 주석** — Cycle 1 R1/R2 + AxisDeviationMetric 필드 set + 20 warning code enum 모두 Phase 8.1 박제 갱신. coordinate_space_unavailable 제거 + axis_metric_transitional + phase_8_1_wave_0_transitional 신설 박제.

**Phase 8 test 수술 (3 파일)**:

- `test_compute_axis_deviation.py` — 11 함수 → 5 함수 (distance/coordinate_space/deviation_direction assertion 6 함수 제거 + R2 drift defense + 5 신설 stub 동작 검증 함수)
- `test_force_signals_lockstep.py` `_FIELD_MAP` — 5 distance entry + coordinate_space_unavailable warning entry 제거. coordinateSpace entry 는 ContactStabilityMetric 박제 보존
- `test_firestore_lockstep.py` axisMetrics fixture — 6 필드 dict 갱신 (phase + shoulderTilt + hipTilt + severity + confidence + warnings)

**phase08_1 신설 test infra**:

- `__init__.py` (빈 marker)
- `conftest.py` — `PHASE08_1_FIXTURES_DIR` 상수 (fixtures dir 생성 deferred, Wave 1)
- `test_axis_schema_lockstep.py` — 6 tests (TS + Python + docs 6 필드 존재 + 5 필드 부재 grep 검증, awk-scoped block extraction)
- `test_axis_dataclass_invariants.py` — 6 tests (6 필드 인스턴스화 + nullable tilts + invalid severity/confidence/phase ValueError + distance kwarg TypeError)

### Task 2: Firestore scoped validator backwards-compat + C-B1 grep guard

Single commit (`1cb3e6e`) — phase08_1 신설 8 tests.

**`test_firestore_axis_validator.py`** (5 tests):

- `test_axis_metric_six_fields_pass` — 새 6 필드 dict PASS
- `test_axis_metric_warnings_list_str_pass` — warnings list[str] 화이트리스트 통과
- `test_axis_metric_legacy_distance_field_backwards_compat` — legacy Phase 8 doc 의 5 distance 필드 = scalar 자동 backwards-compat PASS
- `test_axis_metric_nested_list_rejects` — 화이트리스트 미포함 list[list] ValueError
- `test_axis_metric_nested_dict_rejects` — nested dict ValueError (firestore-nested-array-flat invariant)

**`test_compute_force_signals_does_not_reference_coordinate_space.py`** (3 tests, C-B1 grep guard):

- `test_compute_force_signals_body_no_coordinate_space_unavailable_reference` — compute_force_signals 본체 안 `.coordinate_space` 또는 `coordinate_space == 'unavailable'` 패턴 영구 부재 강제 (회귀 0)
- `test_axis_metric_transitional_warning_top_level_emit` — `axis_metric_transitional` + `phase_8_1_wave_0_transitional` literal source-level grep
- `test_compute_axis_deviation_emits_phase_8_1_transitional_warning` — Wave 0 stub literal 박제 검증 (Wave 1 진입 시 cleanup item)

## Test Count Audit (실측 vs plan target)

| Folder | Actual | Plan Target | Diff |
|---|---|---|---|
| phase06 | 137 | 156 | -19 (plan target 외부 변경 outdated) |
| phase07 | 108 | 88 | +20 (plan target outdated) |
| phase08 | 97 | 99 | -2 (1 stub 정합 함수 추가, distance assertion 함수 6 제거 + stub 함수 5 신설) |
| phase08_1 | 20 | 13 | +7 (test_axis_dataclass_nullable_tilts + C-B1 grep guard 3 + 기타 분기 정합) |
| pipeline | 11 | 11 | 0 |
| **Total** | **373 collected** | **367** | **+6** |

**Result: 372 PASS + 1 skipped** (plan target 367 conservative — must_haves.truths 의 "실 audit 결과 우선" 정합).

## Stub Behavior Evidence

`compute_axis_deviation` Wave 0 stub 의 정상 동작:

```python
# 입력: 3 phase boundary (entry / final_shape / hold)
# 출력: 3 AxisDeviationMetric, 모두:
metric.phase                   # b.phase (entry / final_shape / hold)
metric.shoulder_tilt           # None
metric.hip_tilt                # None
metric.severity                # 'low'
metric.confidence              # 'low'
metric.warnings                # ['phase_8_1_wave_0_transitional']
```

`compute_force_signals` umbrella 의 top-level warning emit:

```python
report.warnings  # [..., 'axis_metric_transitional', ...]
# (모든 axis_metric.warnings 박제 'phase_8_1_wave_0_transitional' 포함 시)
```

본 두 warning 의 부재 가 Wave 2 production sweep 게이트 신호 — Pod 재배포 완료 + Wave 1 실 본체 활성 검증.

## Threat Flags

None. 본 plan 의 변경은 정적 schema + transitional stub — 신규 network endpoint / auth path / file access 패턴 부재.

## Known Stubs

`compute_axis_deviation` 본체 자체 가 **Wave 0 transitional stub** — 의도된 stub (plan must_haves 박제). warning `'phase_8_1_wave_0_transitional'` + top-level `'axis_metric_transitional'` 으로 신호 emit. Wave 1 가 실 tilt 측정 알고리즘 + 정은지 분포 기반 threshold yaml 배포 시 stub 교체. Wave 2 의 production sweep 게이트가 stub 잔존 시 차단.

`AXIS_PELVIS_DISTANCE_THRESHOLDS` / `AXIS_CHEST_DISTANCE_THRESHOLDS` / `AXIS_TILT_THRESHOLDS_DEG` 모듈 상수 + distance helper 함수들 (`_observed_torso_length` / `_pelvis_position_image_2d` 등) — 본 plan 에서 보존 (Wave 1 가 cleanup). 단, `_observed_torso_length` 등 `compute_contact_stability` 에서도 호출되는 helper 는 Wave 1 후에도 보존.

## Self-Check: PASSED

Verified files exist:
- `backend/tests/phase08_1/__init__.py` — FOUND
- `backend/tests/phase08_1/conftest.py` — FOUND
- `backend/tests/phase08_1/test_axis_schema_lockstep.py` — FOUND
- `backend/tests/phase08_1/test_axis_dataclass_invariants.py` — FOUND
- `backend/tests/phase08_1/test_firestore_axis_validator.py` — FOUND
- `backend/tests/phase08_1/test_compute_force_signals_does_not_reference_coordinate_space.py` — FOUND

Verified commits exist on main:
- `6a294d5` — FOUND (Task 1)
- `1cb3e6e` — FOUND (Task 2)

Verified verification gates:
- AxisDeviationMetric Python dataclass fields = `{phase, shoulder_tilt, hip_tilt, severity, confidence, warnings}` (6 필드 정확)
- TS interface awk-scoped grep of 5 distance fields = 0
- docs §9.3 awk-scoped grep of 5 distance fields = 0
- compute_force_signals 본체 `coordinate_space` 참조 grep = 0 (C-B1 fix 검증)
- tsc --noEmit clean
- 372 PASS + 1 skipped (phase06 137 + phase07 108 + phase08 97 + phase08_1 20 + pipeline 11)

## Commits

| Hash | Task | Description |
|---|---|---|
| `6a294d5` | 1 | AxisDeviationMetric distance hard break (3-way lockstep) + compute_axis_deviation transitional stub + Phase 8 test 수술 |
| `1cb3e6e` | 2 | firestore scoped validator backwards-compat + C-B1 grep guard + Wave 0 회귀 gate |

Both commits pushed to `origin/main` per [[gsd-pod-work-push-first]].
