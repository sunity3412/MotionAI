---
phase: 08.1-axis-metric-redesign
plan: 01
type: execute
wave: 1
depends_on: [08.1-00]
files_modified:
  - backend/shared/python/sunity_shared/analysis/force_signals.py
  - backend/judging_data/tilt_thresholds.yaml
  - backend/scripts/calibrate_tilt_thresholds.py
  - backend/tests/phase08_1/test_compute_axis_tilt_only.py
  - backend/tests/phase08_1/test_tilt_thresholds_loader.py
  - backend/tests/phase08_1/test_tilt_thresholds_yaml_drift.py
  - backend/tests/phase08_1/fixtures/__init__.py
  - backend/tests/phase08_1/fixtures/_factory.py
  - backend/tests/phase08_1/conftest.py
autonomous: false
requirements: [FORCE-01]
subsystem: backend-analysis-core
tags: [phase-08-1, tilt-metric, calibration, ipsf-tolerance, jeongeunji-distribution, wave-1-algorithm]

scope_sanity_note: "Wave 1 of 3 (D-06 정합). 단일 목적 = D-02 + D-03 의 알고리즘 layer — compute_axis_deviation 본체 의 transitional stub 을 실 tilt-only 측정 알고리즘 으로 교체 + 정은지 5영상 sweep 분포 기반 calibration script + tilt_thresholds.yaml lazy load. Wave 0 의 6 필드 schema + stub 위에 박제. Wave 2 (production 통합 + 정은지 재sweep + verifier evidence) 가 본 plan 의 yaml + 새 본체 위에 박제. algorithm 위험 중간 — FPS-invariance + tilt 산출 무결성 + threshold load 분기. 본 plan 진입 전 precondition: Firestore Admin SA + sweep_phase8_1780986673 reachability 확인 (Task 2 의 hard blocker)."

must_haves:
  truths:
    - "본 plan 의 단일 목적 = D-02 (정은지 5영상 분포 기반 calibration) + D-03 (IPSF tolerance 매핑) 의 알고리즘 layer. Wave 0 의 6 필드 schema + transitional stub 위에 compute_axis_deviation 본체 실 tilt-only 알고리즘 + 정은지 분포 sweep script + tilt_thresholds.yaml lazy load. per D-02 + D-03 + D-04 (future calibration re-run)."
    - "Task 2 hard precondition (B4 정합) = Firestore Admin SA + sweep_phase8_1780986673 reachability 확인. belle 가 Task 2 진입 전 다음 두 가지 중 하나를 명시: (a) Firestore Admin SA 경로 (e.g. `FIREBASE_SA_JSON` env or `~/.gcp/sunity-ai-coach-sa.json`) 가 가용 + sweep_phase8_1780986673 reachable, 또는 (b) Wave 1 calibration 을 Wave 2 의 재sweep 후로 이관 (yaml fallback `(25.0, 37.5)` 만 Wave 1 가 사용, real calibration 은 Wave 2 Task 2 이후 별도 commit). 본 plan 의 default 가정 = (a). (b) 로 fall back 시 Task 2 의 yaml write 단계 skip + Wave 2 계획 보강."
    - "compute_axis_deviation 본체 stub → 실 알고리즘 교체. tilt-only path. 시그너처 유지 — `compute_axis_deviation(pose_frames, phase_boundaries, pole_axis_measurement, *, fps=9.0) -> list[AxisDeviationMetric]`. caller 영향 0."
    - "Helper dependency audit (W9 정합) — Wave 0 가 보존한 17 helper 중 본 plan 에서 cleanup 대상 13개 + 보존 4개. 각 helper 의 사용처 와 처분 표:\n  · `_observed_torso_length` (L825) — 보존 (`compute_contact_stability` L1527 에서 호출됨, 본 plan scope 외).\n  · `_observed_torso_length_pole_aligned` (L830) — 삭제 (사용처 = 기존 compute_axis_deviation 본체만 — stub 으로 교체된 후 dead code).\n  · `_kp_pole_aligned_xy` (L841) — 보존 (`_shoulder_tilt_pole_aligned` L881 + `_hip_tilt_pole_aligned` L903 에서 호출).\n  · `_pelvis_position_pole_aligned` (L855) — 삭제 (사용처 = 기존 본체만).\n  · `_chest_position_pole_aligned` (L865) — 삭제.\n  · `_pole_aligned_axis_distance` (L875) — 삭제.\n  · `_shoulder_tilt_pole_aligned` (L881) — 보존 (Wave 1 새 본체 가 호출).\n  · `_hip_tilt_pole_aligned` (L903) — 보존.\n  · `_kp2d_xy` (L919) — 보존 (`_shoulder_tilt_2d` L959 + `_hip_tilt_2d` L971 + `compute_contact_stability` 의 L1404 L1409 L1419 에서 호출).\n  · `_midpoint` (L931) — 보존 (`compute_contact_stability` 의 L1413 에서 호출).\n  · `_pelvis_position_image_2d` (L935) — 삭제.\n  · `_chest_position_image_2d` (L944) — 삭제.\n  · `_line_direction_angle_deg` (L953) — 보존 (`_shoulder_tilt_2d` L968 + `_hip_tilt_2d` L980 에서 호출).\n  · `_shoulder_tilt_2d` (L959) — 보존 (Wave 1 새 본체 가 호출 — fallback path).\n  · `_hip_tilt_2d` (L971) — 보존.\n  · `_severity_from_distance` (L983) — 삭제 (distance 차원 제거 정합).\n  · `_deviation_direction_from_pelvis` (L1007) — 삭제 (deviation_direction 필드 제거 정합).\n  보존 4 helper 중 `compute_contact_stability` 의존 helper (`_observed_torso_length`, `_kp2d_xy`, `_midpoint`) 는 Wave 1 종료 후에도 force_signals 모듈 안에 유지. 본 plan 종료 시점 기준 `_observed_torso_length_pole_aligned` 외 다른 distance helper 들 (`_pelvis_position_*`, `_chest_position_*`, `_pole_aligned_axis_distance`, `_severity_from_distance`, `_deviation_direction_from_pelvis`) 박제 = grep 0."
    - "신설 helper `_severity_from_tilt(tilt_abs_deg: float | None, thresholds: tuple[float, float]) -> SeverityLevel` — None → 'low'; >= thresholds[1] → 'high'; >= thresholds[0] → 'medium'; else 'low'. `_severity_from_distance` 의 tilt-only mirror."
    - "tilt 계산 path — pole_aligned 3D 우선 (`pose_frames[0].keypoints_3d_pole_aligned` 가용 시 `_shoulder_tilt_pole_aligned` + `_hip_tilt_pole_aligned` 호출). fallback = image_2d (`pose_frames[0].keypoints_2d` 가용 + `pole_axis_measurement.line` 가용 시 `_shoulder_tilt_2d` + `_hip_tilt_2d` 호출). 둘 다 미가용 시 tilt = None + severity='low' + confidence='low' + warning 'tilt_unavailable'. 정은지 5영상 = pole_aligned path (RTMW keypoints_2d 미박제, PHASE8-INHERITED-ISSUES §2.1 정합)."
    - "phase 별 tilt 산출 algorithm — `phase_frames = pose_frames[b.start_frame_idx : b.end_frame_idx]` → frame 별 shoulder_tilt / hip_tilt list → `np.median` → shoulder_tilt_val / hip_tilt_val (degrees, signed 보존). severity = `_max_severity(_severity_from_tilt(abs(shoulder_tilt_val), shoulder_thresholds), _severity_from_tilt(abs(hip_tilt_val), hip_thresholds))` (abs() input — 방향 무관 severity)."
    - "tilt_thresholds.yaml schema — `backend/judging_data/tilt_thresholds.yaml` 신설. structure:\n```\nversion: '1.0'\nschema_version: 1\nsource: 'jeongeunji_5_videos + ipsf_tolerance_20deg'\ncalibrated_at: '2026-06-09'\nsample_size: 25  # 5 videos × 5 phases\nshoulder_tilt:\n  p25: <float>\n  p50: <float>\n  p75: <float>\n  p90: <float>\n  medium_cutoff_deg: <float>  # max(p90, 20.0)\n  high_cutoff_deg: <float>  # medium × 1.5\nhip_tilt:\n  p25: <float>\n  p50: <float>\n  p75: <float>\n  p90: <float>\n  medium_cutoff_deg: <float>\n  high_cutoff_deg: <float>\nipsf_tolerance:\n  source_ref: 'NotebookLM citation 9 Page 87 Glossary + Aerial Pole CoP Page 63 S55 Iron X (±20°)'\n  default_global_tolerance_deg: 20.0\n```\n`schema_version` field (W11 정합) = 정수, 본 plan 기준 `1`. drift defense test 는 schema_version 검사 + value invariant (medium >= 20.0, high = medium × 1.5 ±0.01) 검사. 미래 schema 변경 시 schema_version bump + drift test 의 version-tolerant 분기 추가 (예: `if schema_version == 1: ...`). per D-04."
    - "tilt_thresholds.yaml lazy load + cache (W10 정합 — module-global mutable flag 제거):\n  · `_TILT_THRESHOLDS_PATH = Path(__file__).resolve().parents[4] / 'judging_data' / 'tilt_thresholds.yaml'` (force_signals.py 가 `backend/shared/python/sunity_shared/analysis/` 안 → parents[4] = `backend/`).\n  · `_TILT_THRESHOLDS_CACHE: tuple[tuple[float,float], tuple[float,float], list[str]] | None = None` — 마지막 element = base_warnings list (fallback 시 ['tilt_thresholds_fallback'], 정상 시 []).\n  · `_get_tilt_thresholds() -> tuple[tuple[float,float], tuple[float,float], list[str]]` — yaml 로드, fallback 시 base_warnings = ['tilt_thresholds_fallback'] (list 안에 담아 cache return 값에 포함, **module-global flag 제거**). PyYAML import (Phase 8 `_CONTACT_POINTS_CACHE` 패턴 정합).\n  · yaml 미가용 시 fallback = `((25.0, 37.5), (25.0, 37.5), ['tilt_thresholds_fallback'])`. 정은지 5영상 baseline 의 P90 (~58°) + IPSF ±20° default 의 보수적 default — 정은지 baseline 의 5/5 'low' 가 deterministic 으로 유지되지는 않으므로 (P90 가 fallback 25° 보다 큼) Wave 1 의 yaml write 가 완료된 후 의 효과는 Wave 2 의 재sweep 으로 검증. fallback 자체 는 Wave 1 yaml 미준비 환경 (개발 머신, CI) 에서 import 보장 + warning 명시.\n  · `_reset_tilt_thresholds_cache()` (test 전용) — `global _TILT_THRESHOLDS_CACHE; _TILT_THRESHOLDS_CACHE = None`."
    - "AXIS_*_THRESHOLDS 모듈 상수 cleanup — Wave 0 가 보존한 `AXIS_PELVIS_DISTANCE_THRESHOLDS` / `AXIS_CHEST_DISTANCE_THRESHOLDS` 삭제 (distance 차원 제거 정합). `AXIS_TILT_THRESHOLDS_DEG = (25.0, 37.5)` 보존 (legacy default — `_get_tilt_thresholds()` 의 fallback). Wave 1 종료 시점 모듈 상수 = 1개."
    - "calibrate_tilt_thresholds.py script — `backend/scripts/calibrate_tilt_thresholds.py` 신설. 인풋 = Firestore sweep uid + (선택) 영상 doc ID list. 본체:\n  · Firestore Admin SA init → sweep_uid 의 analyses subcollection stream → `result.forceSignalsReport.axisMetrics` 추출\n  · 각 axisMetric 의 shoulderTilt / hipTilt absolute value 수집 (25 data points 기대, 부족 시 경고)\n  · numpy.percentile(P25 / P50 / P75 / P90) 산출 (shoulder, hip 각각)\n  · medium_cutoff = max(P90, 20.0); high_cutoff = medium × 1.5\n  · tilt_thresholds.yaml write (PyYAML dump) + console 분포 표 출력\n  · CLI flag — `--sweep-uid` / `--output-path` / `--dry-run` / `--analysis-ids` (콤마 구분). dry-run 시 yaml write skip + 표만 출력."
    - "D-04 정합 (future re-run, W11 정합) — belle 가 정은지 추가 영상 확보 시 sweep uid + (선택) analysis-ids 로 script 재실행 → yaml update + commit. **zero-code-change** 의 정의 = value 변경 (P25/P50/P75/P90, medium/high cutoff, sample_size). schema 변경 (예: `confidence_band` 신설 field) 시는 `schema_version` bump 필요 — script + drift defense test 의 version-tolerant 분기 추가가 plan-level 작업으로 발생. 본 plan 의 D-04 영역 = value 변경에 한해 zero-code-change."
    - "신설 unit test 5개 + 1 신설 conftest fixture (backend/tests/phase08_1/):\n  · test_compute_axis_tilt_only.py — compute_axis_deviation 실 본체 검증. pole_aligned path / image_2d fallback path / 둘 다 미가용 path 3 branch. severity = `_max_severity(shoulder_tilt severity, hip_tilt severity)` 검증. FPS-invariance — 9 fps vs 18 fps (resample) 동일 tilt 산출 검증.\n  · test_tilt_thresholds_loader.py — `_get_tilt_thresholds()` lazy load + cache + yaml missing fallback. monkeypatch `_TILT_THRESHOLDS_PATH` → cache reset → yaml 가용/미가용 검증. fallback 시 base_warnings list contains 'tilt_thresholds_fallback' (module-global flag 부재 검증 — global 변수 set 가 없어야 함).\n  · test_tilt_thresholds_yaml_drift.py — tilt_thresholds.yaml actual file schema 검증 (version / schema_version=1 / source / calibrated_at / sample_size / shoulder_tilt + hip_tilt 의 P25/P50/P75/P90/medium_cutoff_deg/high_cutoff_deg / ipsf_tolerance.default_global_tolerance_deg=20.0 + source_ref). schema_version 1 case 한정 분기 (W11 version-tolerant 패턴).\n  · conftest.py augment — `@pytest.fixture(autouse=True) def _reset_tilt_cache(): from sunity_shared.analysis import force_signals as _fs; _fs._TILT_THRESHOLDS_CACHE = None; yield; _fs._TILT_THRESHOLDS_CACHE = None`. Wave 0 conftest 에 추가."
    - "Wave 1 종료 시 회귀 게이트 — phase06 156 + phase07 88 + phase08 99 (post Wave-0 수술) + phase08_1 (Wave 0 13 + Wave 1 5+) + pipeline 11 = 372+ PASS + 1 skipped 유지. tsc --noEmit clean (TS interface 변경 0). 본 plan 의 production behavior = compute_axis_deviation 실 tilt-only — Wave 2 의 Pod 재배포 + 재sweep 가 정은지 baseline 검증."
  artifacts:
    - path: backend/shared/python/sunity_shared/analysis/force_signals.py
      provides: "compute_axis_deviation 본체 실 tilt-only 알고리즘 (Wave 0 stub 교체). distance helper 13개 삭제 (위 W9 audit 참조). 신설 helper `_severity_from_tilt`. 신설 yaml loader `_get_tilt_thresholds` (cache return tuple 의 base_warnings list 통해 fallback 신호 — module-global flag 부재). 모듈 상수 cleanup — AXIS_PELVIS_DISTANCE_THRESHOLDS / AXIS_CHEST_DISTANCE_THRESHOLDS 삭제, AXIS_TILT_THRESHOLDS_DEG=(25.0, 37.5) 보존 (fallback)."
      contains:
        - "def compute_axis_deviation"
        - "def _severity_from_tilt"
        - "def _get_tilt_thresholds"
        - "def _reset_tilt_thresholds_cache"
        - "_TILT_THRESHOLDS_PATH"
        - "_TILT_THRESHOLDS_CACHE"
        - "tilt_thresholds.yaml"
        - "tilt_unavailable"
        - "tilt_thresholds_fallback"
        - "_shoulder_tilt_pole_aligned"
        - "_hip_tilt_pole_aligned"
        - "_shoulder_tilt_2d"
        - "_hip_tilt_2d"
        - "AXIS_TILT_THRESHOLDS_DEG"
    - path: backend/judging_data/tilt_thresholds.yaml
      provides: "정은지 5영상 sweep P25/P50/P75/P90 분포 + IPSF tolerance default (20°) + medium/high cutoff. Task 2 의 calibrate_tilt_thresholds.py 실행 산출. schema_version=1. D-02 + D-03 + D-04 정합."
      contains:
        - "version:"
        - "schema_version:"
        - "source:"
        - "calibrated_at:"
        - "sample_size:"
        - "shoulder_tilt:"
        - "hip_tilt:"
        - "p25:"
        - "p50:"
        - "p75:"
        - "p90:"
        - "medium_cutoff_deg:"
        - "high_cutoff_deg:"
        - "ipsf_tolerance:"
        - "default_global_tolerance_deg: 20.0"
        - "source_ref:"
    - path: backend/scripts/calibrate_tilt_thresholds.py
      provides: "정은지 sweep axisMetrics shoulder/hip tilt 25 data points → P25/P50/P75/P90 산출 → yaml write. CLI (argparse: --sweep-uid / --analysis-ids / --output-path / --dry-run). Firestore Admin SA init + sweep_phase8_1780986673 default. D-04 future re-run entry point."
      contains:
        - "def main"
        - "argparse"
        - "--sweep-uid"
        - "--analysis-ids"
        - "--output-path"
        - "--dry-run"
        - "numpy.percentile"
        - "yaml.safe_dump"
        - "sweep_phase8_1780986673"
        - "shoulderTilt"
        - "hipTilt"
        - "medium_cutoff_deg"
        - "high_cutoff_deg"
        - "schema_version"
    - path: backend/tests/phase08_1/test_compute_axis_tilt_only.py
      provides: "compute_axis_deviation 실 본체 tilt-only 검증 — 6 필드 + pole_aligned path + image_2d fallback path + 미가용 path 3 branch. severity 정합. FPS-invariance 9 fps vs 18 fps 동일 tilt."
      contains:
        - "def test_compute_axis_pole_aligned_path"
        - "def test_compute_axis_image_2d_path_fallback"
        - "def test_compute_axis_neither_path_unavailable"
        - "def test_severity_from_tilt_low_medium_high"
        - "def test_fps_invariance_tilt_metric"
        - "def test_axis_metric_has_only_six_fields"
        - "compute_axis_deviation"
        - "tilt_unavailable"
    - path: backend/tests/phase08_1/test_tilt_thresholds_loader.py
      provides: "`_get_tilt_thresholds()` lazy load + cache + yaml missing fallback. monkeypatch + cache reset. fallback 시 cache tuple 의 base_warnings list contains 'tilt_thresholds_fallback' (W10 정합: module-global flag 부재 검증)."
      contains:
        - "def test_loader_reads_yaml_when_present"
        - "def test_loader_falls_back_when_yaml_missing"
        - "def test_loader_caches_result"
        - "def test_loader_returns_shoulder_and_hip_thresholds"
        - "def test_loader_fallback_warnings_in_cache_tuple_not_global"
        - "_get_tilt_thresholds"
        - "_TILT_THRESHOLDS_CACHE"
    - path: backend/tests/phase08_1/test_tilt_thresholds_yaml_drift.py
      provides: "tilt_thresholds.yaml actual file schema 검증 + IPSF tolerance default=20° + medium_cutoff >= max(P90, 20.0) + high_cutoff = medium × 1.5. schema_version=1 분기 (W11 version-tolerant 패턴)."
      contains:
        - "def test_yaml_has_required_top_level_keys"
        - "def test_yaml_schema_version_is_1"
        - "def test_yaml_has_shoulder_and_hip_distributions"
        - "def test_yaml_ipsf_tolerance_default_20deg"
        - "def test_yaml_medium_cutoff_at_least_ipsf_default"
        - "def test_yaml_high_cutoff_is_medium_times_1_5"
        - "tilt_thresholds.yaml"
    - path: backend/tests/phase08_1/fixtures/__init__.py
      provides: "phase08_1 fixtures 패키지 marker (빈 파일)."
      contains: []
    - path: backend/tests/phase08_1/fixtures/_factory.py
      provides: "Wave 1 programmatic factory — `make_pose_frame_with_pole_aligned(frame_index, shoulder_tilt_deg, hip_tilt_deg)` + `make_pose_frame_with_image_2d_only(frame_index, shoulder_tilt_deg, hip_tilt_deg, pole_line)` + `make_phase_boundary(phase, start, end)` + `make_pole_axis_measurement_vertical_fallback()`. tilt 값 직접 set (degrees → keypoints_3d_pole_aligned 의 Δz/||Δ|| 시뮬). REVIEWS R9 정합 (programmatic factories 우선)."
      contains:
        - "def make_pose_frame_with_pole_aligned"
        - "def make_pose_frame_with_image_2d_only"
        - "def make_phase_boundary"
        - "def make_pole_axis_measurement_vertical_fallback"
        - "shoulder_tilt_deg"
        - "hip_tilt_deg"
    - path: backend/tests/phase08_1/conftest.py
      provides: "Wave 0 의 conftest augment — `_reset_tilt_cache` autouse fixture (test 간 cache 누수 방지). Wave 0 의 PHASE08_1_FIXTURES_DIR 상수 보존."
      contains:
        - "_reset_tilt_cache"
        - "PHASE08_1_FIXTURES_DIR"
        - "import pytest"
  key_links:
    - from: backend/shared/python/sunity_shared/analysis/force_signals.py
      to: backend/judging_data/tilt_thresholds.yaml
      via: "`_get_tilt_thresholds()` lazy load + cache. yaml 미가용 시 (25.0, 37.5) fallback + cache tuple 의 base_warnings list 에 'tilt_thresholds_fallback' (module-global flag 부재). 정은지 5영상 sweep P90 + IPSF ±20° default 정합."
      pattern: "_get_tilt_thresholds|tilt_thresholds\\.yaml"
    - from: backend/scripts/calibrate_tilt_thresholds.py
      to: backend/judging_data/tilt_thresholds.yaml
      via: "calibrate_tilt_thresholds.py 가 sweep axisMetrics shoulder/hip tilt 25 data points P25/P50/P75/P90 산출 → yaml write. schema_version=1. D-04 정합 (future re-run)."
      pattern: "yaml\\.safe_dump|tilt_thresholds\\.yaml"
    - from: backend/shared/python/sunity_shared/analysis/force_signals.py
      to: backend/shared/python/sunity_shared/analysis/pole/aligner.py
      via: "compute_axis_deviation tilt-only path 가 pose_frames keypoints_3d_pole_aligned 활용 — Phase 1 의 pole_aligner 회전 정합 보존. translation 부재는 tilt = rotation-only / origin-invariant 라 algorithm 영향 0."
      pattern: "keypoints_3d_pole_aligned|pole_aligned"
---

<objective>
compute_axis_deviation 본체 의 Wave 0 transitional stub 을 실 tilt-only 알고리즘 으로 교체 + 정은지 5영상 sweep 분포 기반 calibration script + tilt_thresholds.yaml lazy load 박제. Wave 0 의 6 필드 schema + stub 위에 박제. RESEARCH §4 α-4 + §4.3 β-1 + CONTEXT D-02 + D-03 + D-04 정합. Task 2 진입 전 Firestore Admin SA + sweep_phase8_1780986673 reachability hard precondition (B4 정합) — belle 가 명시적으로 확인 후 진입.

Purpose: distance 차원 hard break 후 axis severity 측정 을 tilt-only metric 으로 교체. 정은지 baseline (5/5 'low') 의 실 검증 은 Wave 2 의 production sweep 가 담당. 본 plan 종료 시점 = tilt 산출 알고리즘 + yaml threshold 준비 완료 + 회귀 0. FPS-invariance + Phase 8 정신 유지.

Output:
- backend/shared/python/sunity_shared/analysis/force_signals.py (compute_axis_deviation 실 tilt-only + `_get_tilt_thresholds` + `_severity_from_tilt` + distance helper 13개 cleanup, helper 보존 4개)
- backend/judging_data/tilt_thresholds.yaml (calibrated 정은지 5영상 분포 + IPSF tolerance, schema_version=1)
- backend/scripts/calibrate_tilt_thresholds.py (sweep 분포 산출 script — D-04 future re-run entry)
- backend/tests/phase08_1/ (5 신설 test + 1 fixture factory + conftest augment)
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-axis-metric-redesign/08.1-CONTEXT.md
@.planning/phases/08.1-axis-metric-redesign/08.1-RESEARCH.md
@.planning/phases/08.1-axis-metric-redesign/08.1-00-PLAN.md
@.planning/phases/08-jerk-jitter/PHASE8-INHERITED-ISSUES.md

@backend/shared/python/sunity_shared/analysis/force_signals.py
@backend/shared/python/sunity_shared/analysis/pole/aligner.py
@backend/shared/python/sunity_shared/firestore_admin.py
@backend/judging_data/contact_points.yaml
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 0 (precondition): Firestore Admin SA + sweep_phase8_1780986673 reachability 확인 — belle 결정 게이트 (B4 정합)</name>
  <files>(none — belle 의 환경 + Firestore 상태 확인)</files>
  <what-built>Wave 0 의 6 필드 schema + transitional stub 이 main 에 박제됨. Wave 1 진입 전 Task 2 calibration 의 hard precondition (Firestore Admin SA + sweep doc reachability) 을 belle 가 확인.</what-built>
  <action>belle 가 다음 두 옵션 중 하나를 선택하여 명시. Claude 는 본 task 에서 실 코드 변경 0 — belle 의 resume-signal 후 Task 1 + Task 2 진행.</action>
  <how-to-verify>
    belle 가 다음 두 옵션 중 하나를 선택하여 명시한다.

    **옵션 A — Firestore reachable (default 가정)**: belle 가 다음 셋 모두 확인.
      1. Firebase Admin Service Account JSON 경로 확보 (env `FIREBASE_SA_JSON` 또는 로컬 `firebase-sa.json` 또는 동일 효력 경로).
      2. SA 의 권한이 `sunity-ai-coach` 프로젝트 Firestore read 포함.
      3. sweep doc 경로 `users/sweep_phase8_1780986673/analyses/*` 존재 — Firebase console 또는 다음 명령 으로 확인:
         ```
         PYTHONPATH=backend/shared/python python -c "from sunity_shared import firestore_admin; fs = firestore_admin._get_client(); docs = list(fs.collection('users/sweep_phase8_1780986673/analyses').limit(1).stream()); print('count:', len(docs), 'sample id:', docs[0].id if docs else 'EMPTY')"
         ```
         expected: count 1 + 비어있지 않은 doc id.

    **옵션 B — Firestore unreachable (fallback path)**: belle 가 옵션 A 의 조건 중 하나라도 미충족이라고 명시. Claude 는 본 plan 의 Task 2 단계 의 yaml write 를 skip + Wave 1 종료 시 yaml = 부재 → force_signals.py 의 fallback `(25.0, 37.5)` 가 활성. Wave 2 의 재sweep 이후 별도 calibration commit 으로 yaml 박제. 본 옵션 선택 시 Wave 2 plan 에 calibration 재실행 task 추가가 필요 (Wave 2 Task 3 의 SUMMARY 산출 전).

    **Pass criteria**:
    - 옵션 A: belle 가 위 셋 모두 확인 + 위 python 명령의 결과 (count 1+, doc id 비어있지 않음) 를 resume-signal 에 포함.
    - 옵션 B: belle 가 명시적 "Firestore 미가용 — fallback path" 선언.

    **Fail handling**:
    - belle 가 옵션 A 라고 응답했으나 위 python 명령이 빈 결과 / auth error / 모듈 import 실패 → 옵션 B 로 자동 fall back + Wave 2 plan augment 필요.
  </how-to-verify>
  <verify>belle 의 resume-signal 이 옵션 A 또는 옵션 B 둘 중 하나 를 명시.</verify>
  <done>belle 가 옵션 A ("approved — Firestore reachable + sweep doc count {N}") 또는 옵션 B ("Firestore unreachable — Wave 2 calibration deferred") 둘 중 하나 반환.</done>
  <resume-signal>옵션 A 선택 시: "Firestore reachable, sweep doc count <N>, proceed Task 1 + Task 2". 옵션 B 선택 시: "Firestore unreachable, Task 2 yaml write skip, Wave 2 plan augment 필요".</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: compute_axis_deviation 실 tilt-only 본체 + yaml lazy loader + helper cleanup</name>
  <files>backend/shared/python/sunity_shared/analysis/force_signals.py, backend/tests/phase08_1/fixtures/__init__.py, backend/tests/phase08_1/fixtures/_factory.py, backend/tests/phase08_1/conftest.py, backend/tests/phase08_1/test_compute_axis_tilt_only.py, backend/tests/phase08_1/test_tilt_thresholds_loader.py</files>
  <behavior>
    RED → GREEN.

    Failing tests first:
      - test_compute_axis_pole_aligned_path: pose_frames with keypoints_3d_pole_aligned (factory `make_pose_frame_with_pole_aligned(shoulder_tilt_deg=15, hip_tilt_deg=10)`) over single 'hold' phase × 10 frames → compute_axis_deviation → axis_metrics[0].shoulder_tilt ≈ 15 (±2 tolerance, np.median) / hip_tilt ≈ 10 / severity='low' (fallback (25.0, 37.5) 기준 — 15° < 25°).
      - test_compute_axis_image_2d_path_fallback: pose_frames with keypoints_2d only (no pole_aligned) + pole_line (`make_pose_frame_with_image_2d_only(shoulder_tilt_deg=12, hip_tilt_deg=8, pole_line)`) → image_2d path → axis_metrics[0].shoulder_tilt ≈ 12 / hip_tilt ≈ 8 / severity='low'.
      - test_compute_axis_neither_path_unavailable: pose_frames with no keypoints_3d_pole_aligned AND no keypoints_2d → axis_metrics[0].shoulder_tilt is None / hip_tilt is None / severity='low' / warnings contains 'tilt_unavailable'.
      - test_severity_from_tilt_low_medium_high: `_severity_from_tilt(15.0, (25.0, 37.5))` == 'low'; `_severity_from_tilt(30.0, (25.0, 37.5))` == 'medium'; `_severity_from_tilt(40.0, (25.0, 37.5))` == 'high'; `_severity_from_tilt(None, (25.0, 37.5))` == 'low'.
      - test_fps_invariance_tilt_metric: 9 fps pose_frames (N frames) vs 18 fps pose_frames (2N frames, identical tilt per frame) — compute_axis_deviation 둘 다 동일 shoulder_tilt / hip_tilt (np.median 동일).
      - test_axis_metric_has_only_six_fields: `dataclasses.fields(AxisDeviationMetric)` set names == `{phase, shoulder_tilt, hip_tilt, severity, confidence, warnings}`.
      - test_loader_reads_yaml_when_present: monkeypatch `_TILT_THRESHOLDS_PATH` → tmp_path yaml with known values → `_get_tilt_thresholds()` → returns `((shoulder_medium, shoulder_high), (hip_medium, hip_high), [])` (base_warnings empty).
      - test_loader_falls_back_when_yaml_missing: monkeypatch path → nonexistent → `_get_tilt_thresholds()` → returns `((25.0, 37.5), (25.0, 37.5), ['tilt_thresholds_fallback'])`.
      - test_loader_caches_result: monkeypatch valid yaml → call `_get_tilt_thresholds()` twice → 2nd call does NOT re-read file (use unittest.mock to patch `yaml.safe_load` and assert call_count == 1).
      - test_loader_returns_shoulder_and_hip_thresholds: yaml separately specifies shoulder + hip distinct values → loader returns 2 distinct tuples.
      - test_loader_fallback_warnings_in_cache_tuple_not_global: 검증 — `_get_tilt_thresholds()` fallback 발동 후 force_signals 모듈 의 어떤 module-level 변수도 fallback 신호로 mutate 되지 않음. 검증 방법: `import sunity_shared.analysis.force_signals as fs; before = {k: v for k, v in vars(fs).items() if not k.startswith('_TILT_THRESHOLDS_CACHE')}; fs._TILT_THRESHOLDS_CACHE = None; result = fs._get_tilt_thresholds(); after = {k: v for k, v in vars(fs).items() if not k.startswith('_TILT_THRESHOLDS_CACHE')}; assert before == after` (단순화 — 실 구현 시 module-level 신규 boolean flag 도입 금지 검증).
  </behavior>
  <action>
Step 1 — Create fixture factory:
  - backend/tests/phase08_1/fixtures/__init__.py: empty marker.
  - backend/tests/phase08_1/fixtures/_factory.py:
    `make_pose_frame_with_pole_aligned(frame_index: int, shoulder_tilt_deg: float, hip_tilt_deg: float) -> PoseFrame` — synthesize keypoints_3d_pole_aligned such that arcsin(|Δz|/||Δ||) == shoulder_tilt_deg / hip_tilt_deg. Use: left_shoulder=(0, 0, 0), right_shoulder=(cos(θ_s), 0, sin(θ_s)) where θ_s = radians(shoulder_tilt_deg). Same pattern for hips. Reliability='high'. Fill non-tilt joints with safe defaults.
    `make_pose_frame_with_image_2d_only(frame_index, shoulder_tilt_deg, hip_tilt_deg, pole_line: PoleLine2D)` — keypoints_2d 합성 — shoulder line vector vs pole_line direction 의 각도 = shoulder_tilt_deg. `_line_direction_angle_deg(pole_line)` 의 출력 + shoulder_tilt_deg 를 이용하여 left_shoulder + right_shoulder 2D 좌표 synth.
    `make_phase_boundary(phase: MotionPhase, start: int, end: int) -> PhaseBoundary` — wrap PhaseBoundary with confidence='medium', source='heuristic', preflight_label_gate_passed=None.
    `make_pole_axis_measurement_vertical_fallback() -> PoleAxisMeasurement` — pole_axis_direction=(0,0,1), line=None.

Step 2 — Augment conftest.py:
  - backend/tests/phase08_1/conftest.py 박제 다음 fixture 추가 (Wave 0 의 PHASE08_1_FIXTURES_DIR 상수 보존):
    ```
    @pytest.fixture(autouse=True)
    def _reset_tilt_cache():
        from sunity_shared.analysis import force_signals as _fs
        _fs._TILT_THRESHOLDS_CACHE = None
        yield
        _fs._TILT_THRESHOLDS_CACHE = None
    ```

Step 3 — Write the failing tests (test_compute_axis_tilt_only.py + test_tilt_thresholds_loader.py) using the factory. Verify pytest RED.

Step 4 — Rewrite compute_axis_deviation 본체 + yaml loader (GREEN):
  - force_signals.py L163-165: `AXIS_PELVIS_DISTANCE_THRESHOLDS` + `AXIS_CHEST_DISTANCE_THRESHOLDS` 상수 삭제. `AXIS_TILT_THRESHOLDS_DEG = (25.0, 37.5)` 보존 (fallback default).
  - `_CONTACT_POINTS_CACHE` 블록 (~L230) 뒤에 yaml loader 추가:
    ```
    _TILT_THRESHOLDS_PATH = Path(__file__).resolve().parents[4] / 'judging_data' / 'tilt_thresholds.yaml'
    _TILT_THRESHOLDS_CACHE: tuple[tuple[float, float], tuple[float, float], list[str]] | None = None


    def _get_tilt_thresholds() -> tuple[tuple[float, float], tuple[float, float], list[str]]:
        global _TILT_THRESHOLDS_CACHE
        if _TILT_THRESHOLDS_CACHE is not None:
            return _TILT_THRESHOLDS_CACHE
        try:
            import yaml
            with open(_TILT_THRESHOLDS_PATH) as f:
                data = yaml.safe_load(f)
            shoulder = (
                float(data['shoulder_tilt']['medium_cutoff_deg']),
                float(data['shoulder_tilt']['high_cutoff_deg']),
            )
            hip = (
                float(data['hip_tilt']['medium_cutoff_deg']),
                float(data['hip_tilt']['high_cutoff_deg']),
            )
            _TILT_THRESHOLDS_CACHE = (shoulder, hip, [])
        except (FileNotFoundError, KeyError, ImportError, ValueError, OSError):
            _TILT_THRESHOLDS_CACHE = (
                AXIS_TILT_THRESHOLDS_DEG,
                AXIS_TILT_THRESHOLDS_DEG,
                ['tilt_thresholds_fallback'],
            )
        return _TILT_THRESHOLDS_CACHE


    def _reset_tilt_thresholds_cache() -> None:
        """Test-only helper — phase08_1/conftest.py 의 autouse fixture 가 호출."""
        global _TILT_THRESHOLDS_CACHE
        _TILT_THRESHOLDS_CACHE = None
    ```
    (단순화: module-global mutable boolean flag 부재 — fallback 신호는 cache tuple 의 세 번째 element 로 단일 source.)

  - 신설 `_severity_from_tilt(tilt_abs_deg: float | None, thresholds: tuple[float, float]) -> SeverityLevel` — None → 'low'; >= thresholds[1] → 'high'; >= thresholds[0] → 'medium'; else 'low'. `_severity_from_distance` 위에 배치 (Step 5 의 helper cleanup 에서 `_severity_from_distance` 삭제).

  - Helper cleanup (W9 audit 정합) — 다음 13 helper 삭제:
    `_observed_torso_length_pole_aligned` (L830), `_pelvis_position_pole_aligned` (L855), `_chest_position_pole_aligned` (L865), `_pole_aligned_axis_distance` (L875), `_pelvis_position_image_2d` (L935), `_chest_position_image_2d` (L944), `_severity_from_distance` (L983), `_deviation_direction_from_pelvis` (L1007). 보존: `_observed_torso_length` (L825), `_kp_pole_aligned_xy` (L841), `_kp2d_xy` (L919), `_midpoint` (L931), `_line_direction_angle_deg` (L953), `_shoulder_tilt_pole_aligned` (L881), `_hip_tilt_pole_aligned` (L903), `_shoulder_tilt_2d` (L959), `_hip_tilt_2d` (L971). 각 helper 의 use site 는 W9 audit 의 dependency graph (must_haves.truths 참조).

  - `compute_axis_deviation` 본체 (Wave 0 stub) 교체:
    ```
    def compute_axis_deviation(
        pose_frames: list[PoseFrame],
        phase_boundaries: list[PhaseBoundary],
        pole_axis_measurement: PoleAxisMeasurement,
        *,
        fps: float = 9.0,
    ) -> list[AxisDeviationMetric]:
        """Phase 8.1 Wave 1 — tilt-only metric. distance 차원 hard break (D-01).
        pole_aligned 3D path 우선 (정은지 영상). image_2d fallback. 둘 다 미가용 시 None + 'tilt_unavailable'.
        """
        line = pole_axis_measurement.line if pole_axis_measurement else None
        has_pole_aligned = bool(pose_frames and pose_frames[0].keypoints_3d_pole_aligned)
        has_kp2d = bool(pose_frames and pose_frames[0].keypoints_2d) and (line is not None)
        shoulder_thresh, hip_thresh, base_warnings = _get_tilt_thresholds()

        if not has_pole_aligned and not has_kp2d:
            return [
                AxisDeviationMetric(
                    phase=b.phase,
                    shoulder_tilt=None,
                    hip_tilt=None,
                    severity='low',
                    confidence='low',
                    warnings=list(base_warnings) + ['tilt_unavailable'],
                )
                for b in phase_boundaries
            ]

        metrics: list[AxisDeviationMetric] = []
        for b in phase_boundaries:
            phase_frames = pose_frames[b.start_frame_idx : b.end_frame_idx]
            if has_pole_aligned:
                shoulder_tilts = [
                    t for t in (_shoulder_tilt_pole_aligned(f) for f in phase_frames) if t is not None
                ]
                hip_tilts = [
                    t for t in (_hip_tilt_pole_aligned(f) for f in phase_frames) if t is not None
                ]
            else:
                shoulder_tilts = [
                    t for t in (_shoulder_tilt_2d(f, line) for f in phase_frames) if t is not None
                ]
                hip_tilts = [
                    t for t in (_hip_tilt_2d(f, line) for f in phase_frames) if t is not None
                ]
            shoulder_tilt_val = float(np.median(shoulder_tilts)) if shoulder_tilts else None
            hip_tilt_val = float(np.median(hip_tilts)) if hip_tilts else None
            sev_s = _severity_from_tilt(
                abs(shoulder_tilt_val) if shoulder_tilt_val is not None else None,
                shoulder_thresh,
            )
            sev_h = _severity_from_tilt(
                abs(hip_tilt_val) if hip_tilt_val is not None else None,
                hip_thresh,
            )
            severity = _max_severity(sev_s, sev_h)
            if phase_frames:
                high_count = sum(
                    1 for f in phase_frames if getattr(f, 'reliability', 'low') == 'high'
                )
                ratio = high_count / max(1, len(phase_frames))
                metric_confidence: MetricConfidence = 'medium' if ratio >= 0.7 else 'low'
            else:
                metric_confidence = 'low'
            metrics.append(
                AxisDeviationMetric(
                    phase=b.phase,
                    shoulder_tilt=shoulder_tilt_val,
                    hip_tilt=hip_tilt_val,
                    severity=severity,
                    confidence=metric_confidence,
                    warnings=list(base_warnings),
                )
            )
        return metrics
    ```

Step 5 — Run tests, verify all GREEN. Run full phase08_1 suite + phase06/07/08/pipeline regression. tsc --noEmit.

Step 6 — commit: `feat(08.1-01): compute_axis_deviation tilt-only + yaml lazy loader + helper cleanup (W9 audit) + module-global flag 부재 (W10)`. push.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; PYTHONPATH=shared/python pytest tests/phase08_1/test_compute_axis_tilt_only.py tests/phase08_1/test_tilt_thresholds_loader.py -v</automated>
    <automated>cd backend &amp;&amp; PYTHONPATH=shared/python pytest tests/phase06 tests/phase07 tests/phase08 tests/phase08_1 tests/pipeline -x</automated>
    <automated>cd app &amp;&amp; npm run typecheck</automated>
    <automated>python3 -c "from sunity_shared.analysis.force_signals import compute_axis_deviation, _get_tilt_thresholds, _severity_from_tilt; print('imports OK')"</automated>
    <automated>grep -nE '_observed_torso_length_pole_aligned|_pelvis_position_pole_aligned|_chest_position_pole_aligned|_pole_aligned_axis_distance|_pelvis_position_image_2d|_chest_position_image_2d|_severity_from_distance|_deviation_direction_from_pelvis|AXIS_PELVIS_DISTANCE_THRESHOLDS|AXIS_CHEST_DISTANCE_THRESHOLDS' backend/shared/python/sunity_shared/analysis/force_signals.py | grep -v '^[^:]*:[0-9]*:[[:space:]]*#' | wc -l</automated>
  </verify>
  <done>
    - compute_axis_deviation body returns AxisDeviationMetric with only 6 fields (no distance fields), tilt-only path active.
    - `_get_tilt_thresholds()` lazy-loads yaml, falls back to (25.0, 37.5) on missing, fallback signal carried via cache tuple's 3rd element (no module-global mutable flag).
    - `_severity_from_tilt(tilt_abs_deg, thresholds)` 정합 low/medium/high boundary semantics.
    - All Wave 1 tests in test_compute_axis_tilt_only.py (6 tests) + test_tilt_thresholds_loader.py (5 tests) PASS.
    - Regression: phase06/07/08/pipeline + phase08_1 (Wave 0+1) all PASS.
    - Last grep returns 0 (8 distance helpers + 2 distance constants 삭제 확인).
    - tsc --noEmit clean.
    - commit pushed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: calibrate_tilt_thresholds.py + tilt_thresholds.yaml 산출 + drift defense test</name>
  <files>backend/scripts/calibrate_tilt_thresholds.py, backend/judging_data/tilt_thresholds.yaml, backend/tests/phase08_1/test_tilt_thresholds_yaml_drift.py</files>
  <behavior>
    Script reads Firestore sweep docs → 25 data points (5 videos × 5 phases) of shoulder/hip tilt absolute values → P25/P50/P75/P90 → yaml write (schema_version=1).

    Drift defense tests:
      - test_yaml_has_required_top_level_keys: version, schema_version, source, calibrated_at, sample_size, shoulder_tilt, hip_tilt, ipsf_tolerance present.
      - test_yaml_schema_version_is_1: schema_version == 1 (W11 정합 — 미래 schema 변경 시 본 분기 보강).
      - test_yaml_has_shoulder_and_hip_distributions: p25, p50, p75, p90, medium_cutoff_deg, high_cutoff_deg present for both shoulder + hip.
      - test_yaml_ipsf_tolerance_default_20deg: ipsf_tolerance.default_global_tolerance_deg == 20.0.
      - test_yaml_medium_cutoff_at_least_ipsf_default: shoulder + hip medium_cutoff_deg >= 20.0 (max(P90, 20.0)).
      - test_yaml_high_cutoff_is_medium_times_1_5: high_cutoff_deg == medium_cutoff_deg * 1.5 (tolerance ±0.01).
  </behavior>
  <action>
**Precondition**: Task 0 의 옵션 A 확인 완료 (Firestore reachable). 옵션 B 의 경우 본 Task 의 yaml write step skip + Wave 2 plan 보강.

Step 1 — Create backend/scripts/calibrate_tilt_thresholds.py:
  - Argparse: `--sweep-uid` (default `sweep_phase8_1780986673`), `--analysis-ids` (콤마 구분, default = empty 면 sweep 전체 stream), `--output-path` (default `backend/judging_data/tilt_thresholds.yaml`), `--dry-run` (flag).
  - Header comment: `D-04 future re-run entry. 새 정은지 영상 확보 시 같은 sweep_uid 또는 신규 sweep_uid 로 재실행 → yaml update + commit. zero-code-change 조건 = value 변경에 한함. schema 변경 (예: confidence_band field 신설) 시는 schema_version bump + drift defense test 의 version-tolerant 분기 추가.`
  - Body:
    ```
    import argparse
    from datetime import datetime, timezone
    from pathlib import Path
    import numpy as np
    import yaml
    from sunity_shared import firestore_admin


    SCHEMA_VERSION = 1


    def main() -> int:
        parser = argparse.ArgumentParser()
        parser.add_argument('--sweep-uid', default='sweep_phase8_1780986673')
        parser.add_argument('--analysis-ids', default='')
        parser.add_argument('--output-path', default='backend/judging_data/tilt_thresholds.yaml')
        parser.add_argument('--dry-run', action='store_true')
        args = parser.parse_args()

        fs = firestore_admin._get_client()
        docs = list(fs.collection(f'users/{args.sweep_uid}/analyses').stream())
        if args.analysis_ids:
            wanted = {x.strip() for x in args.analysis_ids.split(',') if x.strip()}
            docs = [d for d in docs if d.id in wanted]

        shoulder_vals: list[float] = []
        hip_vals: list[float] = []
        for d in docs:
            data = d.to_dict() or {}
            report = (data.get('result') or {}).get('forceSignalsReport') or {}
            for m in report.get('axisMetrics') or []:
                s = m.get('shoulderTilt')
                h = m.get('hipTilt')
                if s is not None and np.isfinite(s):
                    shoulder_vals.append(abs(float(s)))
                if h is not None and np.isfinite(h):
                    hip_vals.append(abs(float(h)))

        if not shoulder_vals or not hip_vals:
            print(f'WARN: sweep_uid={args.sweep_uid} 의 axisMetrics 에 유효 tilt 값 부족 (shoulder={len(shoulder_vals)}, hip={len(hip_vals)}). Wave 0 stub 또는 Wave 1 미배포 sweep 일 가능성. fallback 사용 권장.')
            return 1

        def pct(vals: list[float]) -> dict[int, float]:
            return {p: float(np.percentile(vals, p)) for p in (25, 50, 75, 90)}

        s_pct = pct(shoulder_vals)
        h_pct = pct(hip_vals)
        s_medium = max(s_pct[90], 20.0)
        h_medium = max(h_pct[90], 20.0)
        s_high = round(s_medium * 1.5, 4)
        h_high = round(h_medium * 1.5, 4)

        yaml_data = {
            'version': '1.0',
            'schema_version': SCHEMA_VERSION,
            'source': f'jeongeunji_sweep + ipsf_tolerance_20deg ({args.sweep_uid})',
            'calibrated_at': datetime.now(timezone.utc).isoformat(),
            'sample_size': min(len(shoulder_vals), len(hip_vals)),
            'shoulder_tilt': {
                'p25': s_pct[25], 'p50': s_pct[50], 'p75': s_pct[75], 'p90': s_pct[90],
                'medium_cutoff_deg': float(s_medium), 'high_cutoff_deg': float(s_high),
            },
            'hip_tilt': {
                'p25': h_pct[25], 'p50': h_pct[50], 'p75': h_pct[75], 'p90': h_pct[90],
                'medium_cutoff_deg': float(h_medium), 'high_cutoff_deg': float(h_high),
            },
            'ipsf_tolerance': {
                'source_ref': 'NotebookLM citation 9 Page 87 Glossary + Aerial Pole CoP Page 63 S55 Iron X (±20°)',
                'default_global_tolerance_deg': 20.0,
            },
        }

        # console summary
        print(f'sweep_uid={args.sweep_uid}  docs={len(docs)}  shoulder_n={len(shoulder_vals)}  hip_n={len(hip_vals)}')
        print(f'shoulder P25/P50/P75/P90 = {s_pct}')
        print(f'hip      P25/P50/P75/P90 = {h_pct}')
        print(f'shoulder cutoffs medium={s_medium:.2f}°  high={s_high:.2f}°')
        print(f'hip      cutoffs medium={h_medium:.2f}°  high={h_high:.2f}°')

        if not args.dry_run:
            out = Path(args.output_path)
            out.parent.mkdir(parents=True, exist_ok=True)
            with open(out, 'w') as f:
                yaml.safe_dump(yaml_data, f, sort_keys=False, default_flow_style=False)
            print(f'WROTE {out}')
        else:
            print('DRY RUN — yaml 미저장.')
        return 0


    if __name__ == '__main__':
        raise SystemExit(main())
    ```

Step 2 — Run calibration (옵션 A precondition 정합):
  - `cd /Users/kimtaesung/Dev/SunityMotion && PYTHONPATH=backend/shared/python python backend/scripts/calibrate_tilt_thresholds.py --sweep-uid sweep_phase8_1780986673` → yaml 산출.
  - 결과 표 console capture (Wave 1 SUMMARY 산출 시 인용).
  - 옵션 B (Task 0 fallback) 의 경우: 본 step skip + Wave 1 종료 yaml 부재 + force_signals fallback `(25.0, 37.5)` 활성. Wave 2 의 재sweep + 별도 commit 으로 yaml 박제.

Step 3 — Write test_tilt_thresholds_yaml_drift.py (6 tests 위 'behavior' 참조):
  - 본 test 의 `test_yaml_schema_version_is_1` 가 W11 정합 — yaml 의 schema_version 이 1 일 때만 나머지 5 test 가 활성. schema_version 이 다른 값이면 pytest.skip(reason="schema_version bump — drift test version-tolerant 분기 보강 필요") 로 graceful exit (W11 정합 — value 변경 만 zero-code-change).
  - 옵션 B 인 경우: 본 test 파일은 `if not Path(...).exists(): pytest.skip(reason='Wave 1 yaml 미준비 — Task 0 옵션 B path')` 로 graceful skip. Wave 2 calibration 후 unskip.

Step 4 — Run tests:
  - `cd backend && PYTHONPATH=shared/python pytest tests/phase08_1/test_tilt_thresholds_yaml_drift.py -v` (옵션 A: 6 PASS; 옵션 B: 6 skip).
  - 회귀 sweep: `pytest tests/phase06 tests/phase07 tests/phase08 tests/phase08_1 tests/pipeline -x`.
  - 옵션 A 의 경우 force_signals 의 fallback 분기 가 비활성 — yaml 실 값 사용 검증 (test_compute_axis_pole_aligned_path 의 severity 가 yaml 의 medium_cutoff_deg 기준으로 변동 — yaml medium_cutoff 가 25 와 다르면 기존 test 의 tolerance 조정 필요. Task 1 의 test 는 fallback (25, 37.5) 기준으로 작성됐으므로, test 안에서 `_get_tilt_thresholds()` 결과 를 monkeypatch fallback 으로 forcing — fixture `_reset_tilt_cache` + monkeypatch `_TILT_THRESHOLDS_PATH` 를 `/nonexistent` 로 추가 — 이미 Task 1 test 에 이 패턴이 들어있음).

Step 5 — commits (2):
  - `feat(08.1-01): calibrate_tilt_thresholds.py + tilt_thresholds.yaml (schema_version=1) from sweep_phase8_1780986673`. push.
  - `test(08.1-01): yaml drift defense (6 tests, version-tolerant)`. push.
  - 옵션 B 의 경우 commit 1 은 yaml 없이 script + 6 skipped test 만.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion &amp;&amp; PYTHONPATH=backend/shared/python python backend/scripts/calibrate_tilt_thresholds.py --dry-run 2>&amp;1 | head -40</automated>
    <automated>cd backend &amp;&amp; PYTHONPATH=shared/python pytest tests/phase08_1/test_tilt_thresholds_yaml_drift.py -v</automated>
    <automated>cd backend &amp;&amp; PYTHONPATH=shared/python pytest tests/phase06 tests/phase07 tests/phase08 tests/phase08_1 tests/pipeline -x 2>&amp;1 | tail -5</automated>
    <automated>test -f backend/judging_data/tilt_thresholds.yaml &amp;&amp; head -25 backend/judging_data/tilt_thresholds.yaml || echo 'OPTION B PATH: yaml absent — Wave 2 calibration deferred'</automated>
  </verify>
  <done>
    - backend/scripts/calibrate_tilt_thresholds.py exists + dry-run executes + supports D-04 future re-run + version-tolerant.
    - 옵션 A: backend/judging_data/tilt_thresholds.yaml exists with schema_version=1 + required top-level keys + shoulder + hip P25/P50/P75/P90 + medium_cutoff_deg >= 20.0 + high_cutoff_deg = medium * 1.5.
    - 옵션 B: tilt_thresholds.yaml 부재 + drift defense 6 test skipped + Wave 2 plan augment 필요 신호 명시.
    - 회귀 0: phase06 + phase07 + phase08 + phase08_1 (Wave 0+1) + pipeline 모두 PASS + 1 skipped (옵션 A) 또는 + 7 skipped (옵션 B).
    - 2 commits + push.
  </done>
</task>

</tasks>

<verification>
- Task 0 의 belle 확인 (옵션 A / B) 완료.
- compute_axis_deviation 본체 실 tilt-only — distance 산출 path 부재 (grep `_observed_torso_length_pole_aligned` / `_pole_aligned_axis_distance` / `_severity_from_distance` / `_deviation_direction_from_pelvis` 박제 0).
- AxisDeviationMetric 6 필드 (Wave 0 schema 정합) — production behavior tilt-only path.
- W10 정합: module-global mutable flag 부재 — fallback 신호 는 `_TILT_THRESHOLDS_CACHE` tuple 의 세 번째 element 단일 source.
- W9 정합: 17 helper audit + 13 삭제 + 4 보존 (`compute_contact_stability` 의존 + tilt 산출 의존).
- 옵션 A: tilt_thresholds.yaml = 정은지 5영상 sweep_phase8_1780986673 의 25 data points 기반 P25/P50/P75/P90 + IPSF ±20° default + medium = max(P90, 20°) + high = medium × 1.5 + schema_version=1.
- 옵션 B: yaml 부재 + drift test skip + Wave 2 plan 보강 신호.
- calibrate_tilt_thresholds.py CLI + dry-run + D-04 future re-run entry (W11 version-tolerant 박제).
- FPS-invariance 9 fps vs 18 fps 동일 tilt (Phase 8 정신 유지).
- 회귀 0: phase06 + phase07 + phase08 + phase08_1 (Wave 0+1 total) + pipeline 모두 PASS + tsc --noEmit clean.
- Wave 2 진입 준비 — pipeline rewire (caller 정합 검증) + Pod 재배포 + 정은지 5/5 영상 재sweep evidence.

본 plan = analysis core 만. pipeline rewire = Wave 2. 정은지 5/5 영상 axis severity='low' production verification = Wave 2 (Pod 재배포 + 재sweep).
</verification>

<success_criteria>
- compute_axis_deviation 본체 = 실 tilt-only 알고리즘. 6 필드 AxisDeviationMetric 산출.
- 옵션 A: tilt_thresholds.yaml = 정은지 5영상 + IPSF 기반 calibrated (schema_version=1 + 모든 필수 schema key + medium/high cutoff invariants).
- 옵션 B: Wave 2 plan augment 필요 신호 + force_signals fallback (25, 37.5) 활성.
- calibrate_tilt_thresholds.py CLI + dry-run + D-04 future re-run (W11 version-tolerant).
- Wave 0 + Wave 1 종료 시 phase08_1 unit test 24+ PASS (Wave 0 13 + Wave 1 11) — 옵션 A. 옵션 B 의 경우 6 skipped 추가.
- FPS-invariance 검증 (Phase 8 정신 유지).
- W9/W10/W11 모두 정합 — helper dependency graph 사전 산출 + module-global flag 부재 + version-tolerant drift test.
- 회귀 0 + tsc --noEmit clean.
- Wave 2 진입 차단 해소 — pipeline rewire + Pod 재배포 + 재sweep 가능.
</success_criteria>

<output>
Create `.planning/phases/08.1-axis-metric-redesign/08.1-01-SUMMARY.md` when done. SUMMARY 에 다음 명시:
- Task 0 의 belle 선택 (옵션 A / B).
- 옵션 A 의 경우 yaml 의 실제 medium/high cutoff 값 + sample_size.
- 옵션 B 의 경우 Wave 2 plan augment 작업 list.
- helper cleanup audit 결과 (실 삭제 13개 + 보존 4개).
- 회귀 게이트 PASS + skipped count.
</output>
</content>
</invoke>