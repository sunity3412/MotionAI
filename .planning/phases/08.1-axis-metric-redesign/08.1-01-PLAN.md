---
phase: 08.1-axis-metric-redesign
plan: 01
type: execute
wave: 1
depends_on: [08.1-00]
files_modified:
  - backend/shared/python/sunity_shared/analysis/force_signals.py
  - backend/judging_data/tilt_thresholds.yaml
  - backend/scripts/calibrate_tilt_thresholds.py
  - backend/tests/phase08_1/test_compute_axis_tilt_only.py
  - backend/tests/phase08_1/test_tilt_thresholds_loader.py
  - backend/tests/phase08_1/test_tilt_thresholds_yaml_drift.py
  - backend/tests/phase08_1/fixtures/__init__.py
  - backend/tests/phase08_1/fixtures/_factory.py
autonomous: true
requirements: [FORCE-01]
subsystem: backend-analysis-core
tags: [phase-08-1, tilt-metric, calibration, ipsf-tolerance, jeongeunji-distribution, wave-1-algorithm]

scope_sanity_note: "Wave 1 of 3 (D-06 정합). 단일 목적 = D-02 + D-03 의 알고리즘 layer — compute_axis_deviation 본체 tilt-only 재작성 + 정은지 5영상 sweep 분포 박제 calibration script + tilt_thresholds.yaml lazy load. Wave 0 의 6 필드 schema 위에 박제. Wave 2 (production 통합 + 정은지 재sweep + verifier evidence) 가 본 plan 의 yaml + 재작성 본체 위에 박제. **algorithm 위험 중간 — FPS-invariance + tilt 산출 무결성 + threshold load 분기**. compute_axis_deviation 본체 변경 = (a) distance 산출 path 박제 제거 (line 1107-1158 박제 distance/torso_length 분기 제거) + (b) tilt-only path (3D pole_aligned + 2D image fallback) + (c) AXIS_*_THRESHOLDS module-level constant 박제 → yaml lazy load (tilt_thresholds.yaml). pipeline rewire 는 Wave 2 — 본 plan = analysis core 만."

must_haves:
  truths:
    - "본 plan 의 단일 목적 = D-02 (정은지 5영상 분포 기반 calibration) + D-03 (IPSF tolerance 매핑) 의 알고리즘 layer 박제. Wave 0 의 6 필드 schema 위에 compute_axis_deviation 본체 tilt-only 재작성 + 정은지 분포 sweep script + tilt_thresholds.yaml lazy load. per D-02 + D-03 + D-04 (future calibration re-run)."
    - "compute_axis_deviation 본체 재작성 — tilt-only path 박제. distance 산출 helper (`_observed_torso_length` / `_observed_torso_length_pole_aligned` / `_pelvis_position_pole_aligned` / `_chest_position_pole_aligned` / `_pelvis_position_image_2d` / `_chest_position_image_2d` / `_pole_aligned_axis_distance` / `_deviation_direction_from_pelvis` / `_severity_from_distance`) 박제 deprecated → 본 plan 박제 제거 (Wave 0 transitional 박제 정리). 남기는 helper: `_shoulder_tilt_2d` / `_hip_tilt_2d` / `_shoulder_tilt_pole_aligned` / `_hip_tilt_pole_aligned` / `_kp_pole_aligned_xy` / `_kp2d_xy` / `_midpoint` / `_max_severity`. severity 결정 helper `_severity_from_tilt(tilt_abs_deg, thresholds) -> SeverityLevel` 신설 (distance 가 아닌 tilt 입력)."
    - "compute_axis_deviation 시그너처 박제 변경 — `compute_axis_deviation(pose_frames, phase_boundaries, pole_axis_measurement, *, fps=9.0) -> list[AxisDeviationMetric]` 박제 유지 (caller 박제 영향 0). pole_axis_measurement 박제 = tilt 계산에서는 사용 안 함 (rotation-only) 이나 시그너처 유지 (interface 박제 정합). 본 plan 박제 본체 = pose_frames + phase_boundaries 만 사용 — pole_aligned 좌표계 박제 직접 활용 (이미 pole 축 = Z+ 정렬됨)."
    - "tilt 계산 path 박제 — pole_aligned 3D 박제 우선 (keypoints_3d_pole_aligned 박제 가용 시 `_shoulder_tilt_pole_aligned` / `_hip_tilt_pole_aligned` 박제). fallback = image_2d (keypoints_2d 박제 가용 시 `_shoulder_tilt_2d` / `_hip_tilt_2d` 박제 — pole line direction 박제 필요). 둘 다 미가용 시 tilt = None + severity='low' + confidence='low' + warning 'tilt_unavailable' 박제. 정은지 5영상은 pole_aligned path 박제 (RTMW keypoints_2d 미박제 — PHASE8-INHERITED-ISSUES §2.1 정합)."
    - "phase 별 tilt 산출 박제 — 본 plan 박제 algorithm: phase_frames 박제 [start_frame_idx : end_frame_idx] 추출 → frame 별 shoulder_tilt / hip_tilt 박제 list → np.median 박제 → shoulder_tilt_val / hip_tilt_val 박제 (degrees, |abs| 박제 아님 — sign 보존 박제). severity = `_max_severity(_severity_from_tilt(abs(shoulder_tilt_val)), _severity_from_tilt(abs(hip_tilt_val)))` 박제 (abs() 박제 input — 방향 무관 severity)."
    - "tilt_thresholds.yaml schema 박제 — `backend/judging_data/tilt_thresholds.yaml` 신설. structure:\n```\nversion: '1.0'\nsource: 'jeongeunji_5_videos + ipsf_tolerance_20deg'\ncalibrated_at: '2026-06-09'\nsample_size: 25  # 5 videos × 5 phases\nshoulder_tilt:\n  p25: <float>\n  p50: <float>\n  p75: <float>\n  p90: <float>\n  medium_cutoff_deg: <float>  # max(p90, 20.0)\n  high_cutoff_deg: <float>  # medium × 1.5\nhip_tilt:\n  p25: <float>\n  p50: <float>\n  p75: <float>\n  p90: <float>\n  medium_cutoff_deg: <float>\n  high_cutoff_deg: <float>\nipsf_tolerance:\n  source_ref: 'NotebookLM citation 9 Page 87 Glossary + Aerial Pole CoP Page 63 S55 Iron X (±20°)'\n  default_global_tolerance_deg: 20.0\n```\nper D-02 + D-03."
    - "tilt_thresholds.yaml lazy load + cache 박제 — force_signals.py 모듈 박제 `_TILT_THRESHOLDS_PATH = Path(__file__).parent.parent.parent.parent.parent / 'judging_data' / 'tilt_thresholds.yaml'` 박제 + `_TILT_THRESHOLDS_CACHE: dict | None = None` 박제 + `_get_tilt_thresholds() -> tuple[tuple[float, float], tuple[float, float]]` 박제 (shoulder, hip 박제 (medium, high) 튜플 박제 return). PyYAML 박제 import (Phase 8 박제 _CONTACT_POINTS_CACHE 패턴 정합). yaml 박제 missing 시 fallback = `((25.0, 37.5), (25.0, 37.5))` 박제 (RESEARCH §1.3 박제 정은지 tilt 분포 10°~58° 박제 + IPSF ±20° default 박제 + 1.5× medium 박제 — 보수적 default 박제, 정은지 baseline 박제 5/5 low 박제 fallback 안전망). warning 'tilt_thresholds_fallback' 박제 emit."
    - "AXIS_*_THRESHOLDS 모듈 상수 박제 cleanup — `AXIS_PELVIS_DISTANCE_THRESHOLDS` / `AXIS_CHEST_DISTANCE_THRESHOLDS` 박제 제거 (distance 차원 제거 정합). `AXIS_TILT_THRESHOLDS_DEG` 박제 보존 (legacy default — `_get_tilt_thresholds()` 박제 fallback 박제 활용). Wave 1 종료 시점 박제 모듈 상수 = 1개 (AXIS_TILT_THRESHOLDS_DEG = (25.0, 37.5) — 정은지 baseline 박제 안전망)."
    - "calibrate_tilt_thresholds.py script 박제 — `backend/scripts/calibrate_tilt_thresholds.py` 신설. 인풋 = Firestore sweep uid + 영상 list (default = sweep_phase8_1780986673 + 5 reference videos). 본체:\n  · Firestore admin 박제 init → 5 영상 docs read → `result.forceSignalsReport.axisMetrics` 추출\n  · axisMetrics 박제 각 metric 의 shoulderTilt / hipTilt 박제 (절대값 박제 — np.abs) 박제 25 data points 박제 list 생성\n  · numpy.percentile 박제 P25 / P50 / P75 / P90 산출 (shoulder, hip 박제 각각)\n  · medium_cutoff = max(P90, 20.0) 박제 (D-02 + D-03 정합)\n  · high_cutoff = medium × 1.5 박제\n  · tilt_thresholds.yaml 박제 write (PyYAML dump) + console 출력 (분포 표 + 적용 cutoff)\n  · CLI 박제 (argparse) — `--sweep-uid` / `--output-path` / `--dry-run` 박제. dry-run 시 yaml 박제 write 박제 안 함 + 결과 표만 출력."
    - "calibrate_tilt_thresholds.py 박제 D-04 정합 — belle 가 정은지 추가 영상 확보 시 sweep uid 박제 재실행 시 yaml 박제 update + commit (코드 변경 0, plan 변경 0, pipeline 영향 0). 본 plan 박제 script 의 실행 박제 = Wave 1 Task 2 박제 (정은지 5영상 기반 초기 calibration) + future re-run (D-04)."
    - "Wave 1 종료 시점 박제 yaml 박제 = sweep_phase8_1780986673 박제 정은지 5영상 박제 25 data points 박제 calibrated 박제. 단, 본 plan 박제 정은지 sweep 박제 = pole_aligned 3D path 박제 (RTMW keypoints_2d 미박제 — Phase 8 B' fix path 박제 정합). Wave 2 박제 재sweep 박제 시 axisMetrics 박제 distance 필드 박제 부재 (Wave 0 정합) + tilt 박제 박제 — 본 plan 박제 calibration 박제 = Phase 8 박제 axisMetrics (legacy 5 필드 박제 박제 있음) 박제 shoulder/hip tilt 박제 활용 (Wave 0 frontend normalize 박제 silently drop, 박제 sweep 결과 박제 forceSignalsReport.axisMetrics 박제 shoulderTilt / hipTilt 박제 보존 박제 — frontend 박제 안 보고 backend 박제 직접 raw firestore read 박제 calibration script 박제 활용 박제 안전 박제)."
    - "신설 unit test 5개 박제 (backend/tests/phase08_1/):\n  · test_compute_axis_tilt_only.py — compute_axis_deviation 박제 본체 박제 6 필드 (Wave 0) + tilt-only 박제 path 박제 검증. pole_aligned path / image_2d path / 둘 다 미가용 path 박제 3 branch 박제. severity 박제 _max_severity(shoulder_tilt severity, hip_tilt severity) 박제 검증.\n  · test_tilt_thresholds_loader.py — `_get_tilt_thresholds()` 박제 lazy load + cache + yaml missing fallback 박제 검증. monkeypatch 박제 _TILT_THRESHOLDS_PATH 박제 변경 → cache reset → yaml 박제 가용/미가용 박제 검증. fallback 시 warning 'tilt_thresholds_fallback' emit 박제.\n  · test_tilt_thresholds_yaml_drift.py — tilt_thresholds.yaml 박제 actual file 박제 schema 검증 (version / source / calibrated_at / sample_size / shoulder_tilt + hip_tilt 박제 P25/P50/P75/P90/medium_cutoff_deg/high_cutoff_deg 박제 / ipsf_tolerance.default_global_tolerance_deg=20.0 박제 + source_ref 박제). 본 plan 박제 calibration 박제 산출 박제 검증.\n  · test_fps_invariance_tilt_metric (test_compute_axis_tilt_only.py 박제 일부) — 9 fps vs 18 fps 동일 frames 박제 (resample 박제) 박제 tilt 산출 박제 동일 박제 (FPS-invariance 박제 Phase 8 박제 정신 유지).\n  · test_calibrate_tilt_thresholds_dry_run (별도 test 박제 가능 — test_calibrate_tilt_thresholds.py 박제 신설 박제 가능 — 단 cap 박제 5 박제 cap 안 박제 박제 — calibration script 박제 박제 박제 dry-run 박제 박제 sweep_uid 박제 박제 박제 mock Firestore 박제 박제)."
    - "Wave 1 종료 시점 박제 회귀 게이트 박제 — phase06 156 + phase07 88 + phase08 (post Wave-0 cleanup count) + phase08_1 (13 Wave-0 + 5+ Wave-1 신설) + pipeline 11 박제 PASS + 1 skipped 박제 유지. tsc --noEmit clean 박제 (TS interface 박제 변경 0). 본 plan 박제 production behavior 박제 = compute_axis_deviation 박제 본체 박제 tilt-only 박제 박제 — Wave 2 박제 pipeline 박제 재배포 박제 박제 정은지 박제 5/5 박제 severity 박제 박제 박제 박제 박제 박제 박제 박제 (production behavior 박제 박제 박제 박제 Pod 박제 박제 박제)."
  artifacts:
    - path: backend/shared/python/sunity_shared/analysis/force_signals.py
      provides: "compute_axis_deviation 박제 본체 tilt-only 재작성 (distance 산출 path 박제 제거). distance helper (`_observed_torso_length` / `_observed_torso_length_pole_aligned` / `_pelvis_position_pole_aligned` / `_chest_position_pole_aligned` / `_pelvis_position_image_2d` / `_chest_position_image_2d` / `_pole_aligned_axis_distance` / `_deviation_direction_from_pelvis` / `_severity_from_distance`) 박제 제거. 신설 helper `_severity_from_tilt(tilt_abs_deg, thresholds)` 박제. 신설 yaml loader `_get_tilt_thresholds() -> tuple[tuple[float, float], tuple[float, float]]` + lazy load cache `_TILT_THRESHOLDS_CACHE`. 모듈 상수 `AXIS_PELVIS_DISTANCE_THRESHOLDS` / `AXIS_CHEST_DISTANCE_THRESHOLDS` 박제 제거. `AXIS_TILT_THRESHOLDS_DEG = (25.0, 37.5)` 박제 보수적 default 박제 보존 (yaml fallback)."
      contains:
        - "def compute_axis_deviation"
        - "def _severity_from_tilt"
        - "def _get_tilt_thresholds"
        - "_TILT_THRESHOLDS_PATH"
        - "_TILT_THRESHOLDS_CACHE"
        - "tilt_thresholds.yaml"
        - "tilt_unavailable"
        - "tilt_thresholds_fallback"
        - "_shoulder_tilt_pole_aligned"
        - "_hip_tilt_pole_aligned"
        - "AXIS_TILT_THRESHOLDS_DEG"
    - path: backend/judging_data/tilt_thresholds.yaml
      provides: "정은지 5영상 sweep 박제 P25/P50/P75/P90 분포 + IPSF tolerance default (20°) 박제 + medium/high cutoff 박제. Wave 1 Task 2 calibrate_tilt_thresholds.py 박제 실행 박제 산출. D-02 + D-03 정합. D-04 정합 — 정은지 추가 영상 확보 시 yaml 박제 update + commit (코드 변경 0)."
      contains:
        - "version:"
        - "source:"
        - "calibrated_at:"
        - "sample_size:"
        - "shoulder_tilt:"
        - "hip_tilt:"
        - "p25:"
        - "p50:"
        - "p75:"
        - "p90:"
        - "medium_cutoff_deg:"
        - "high_cutoff_deg:"
        - "ipsf_tolerance:"
        - "default_global_tolerance_deg: 20.0"
        - "source_ref:"
    - path: backend/scripts/calibrate_tilt_thresholds.py
      provides: "정은지 sweep 박제 axisMetrics 박제 shoulder/hip tilt 박제 25 data points 박제 P25/P50/P75/P90 산출 박제 yaml 박제 write 박제. CLI (argparse: --sweep-uid / --output-path / --dry-run). Firestore admin init 박제 + 5 영상 ID 박제 default (PHASE8-INHERITED-ISSUES §1.1 박제 정합). D-04 정합 — future re-run 박제 entry point."
      contains:
        - "def main"
        - "argparse"
        - "--sweep-uid"
        - "--output-path"
        - "--dry-run"
        - "numpy.percentile"
        - "yaml.safe_dump"
        - "sweep_phase8_1780986673"
        - "shoulderTilt"
        - "hipTilt"
        - "medium_cutoff_deg"
        - "high_cutoff_deg"
    - path: backend/tests/phase08_1/test_compute_axis_tilt_only.py
      provides: "compute_axis_deviation 본체 tilt-only 박제 검증 — 6 필드 박제 (Wave 0) + pole_aligned path + image_2d path + 미가용 path 박제 3 branch 박제. severity 박제 _max_severity(shoulder, hip) 박제 검증. FPS-invariance 박제 9 fps vs 18 fps 박제 동일 tilt 산출 박제 검증."
      contains:
        - "def test_compute_axis_pole_aligned_path"
        - "def test_compute_axis_image_2d_path_fallback"
        - "def test_compute_axis_neither_path_unavailable"
        - "def test_severity_from_tilt_low_medium_high"
        - "def test_fps_invariance_tilt_metric"
        - "def test_axis_metric_has_only_six_fields"
        - "compute_axis_deviation"
        - "tilt_unavailable"
    - path: backend/tests/phase08_1/test_tilt_thresholds_loader.py
      provides: "_get_tilt_thresholds() 박제 lazy load + cache + yaml missing fallback 박제 검증. monkeypatch _TILT_THRESHOLDS_PATH 박제 → cache reset → yaml 가용 시 yaml 값 박제 / 미가용 시 fallback (25.0, 37.5) 박제."
      contains:
        - "def test_loader_reads_yaml_when_present"
        - "def test_loader_falls_back_when_yaml_missing"
        - "def test_loader_caches_result"
        - "def test_loader_returns_shoulder_and_hip_thresholds"
        - "_get_tilt_thresholds"
        - "_TILT_THRESHOLDS_CACHE"
    - path: backend/tests/phase08_1/test_tilt_thresholds_yaml_drift.py
      provides: "tilt_thresholds.yaml 박제 actual file 박제 schema 검증 + IPSF tolerance default=20° 박제 검증 + medium_cutoff >= max(P90, 20.0) 박제 검증 + high_cutoff = medium × 1.5 박제 검증 (D-02 + D-03 정합). 본 plan 박제 calibration 박제 산출 박제 schema 박제 drift defense."
      contains:
        - "def test_yaml_has_required_top_level_keys"
        - "def test_yaml_has_shoulder_and_hip_distributions"
        - "def test_yaml_ipsf_tolerance_default_20deg"
        - "def test_yaml_medium_cutoff_at_least_ipsf_default"
        - "def test_yaml_high_cutoff_is_medium_times_1_5"
        - "tilt_thresholds.yaml"
    - path: backend/tests/phase08_1/fixtures/__init__.py
      provides: "phase08_1 fixtures 패키지 marker (Phase 8 패턴 정합, 빈 파일)."
      contains: []
    - path: backend/tests/phase08_1/fixtures/_factory.py
      provides: "Wave 1 programmatic factory — make_pose_frame_with_pole_aligned(frame_index, shoulder_tilt_deg, hip_tilt_deg) 박제 + make_phase_boundary(phase, start, end) 박제 + make_pole_axis_measurement_vertical_fallback() 박제. tilt 값 박제 직접 set 박제 (degrees → arcsin(sin(deg)) 박제 inverse: Δz / ||Δ|| 박제 — keypoints_3d_pole_aligned 박제 시뮬). REVIEWS R9 정합 (programmatic factories 우선)."
      contains:
        - "def make_pose_frame_with_pole_aligned"
        - "def make_phase_boundary"
        - "def make_pole_axis_measurement_vertical_fallback"
        - "shoulder_tilt_deg"
        - "hip_tilt_deg"
        - "Keypoint3DAligned"
  key_links:
    - from: backend/shared/python/sunity_shared/analysis/force_signals.py
      to: backend/judging_data/tilt_thresholds.yaml
      via: "`_get_tilt_thresholds()` 박제 lazy load + cache 박제 — yaml 박제 미가용 시 (25.0, 37.5) fallback 박제 + warning 'tilt_thresholds_fallback' emit. 정은지 5영상 sweep 박제 P90 박제 + IPSF ±20° default 박제 정합."
      pattern: "_get_tilt_thresholds|tilt_thresholds\\.yaml"
    - from: backend/scripts/calibrate_tilt_thresholds.py
      to: backend/judging_data/tilt_thresholds.yaml
      via: "calibrate_tilt_thresholds.py 박제 sweep 박제 axisMetrics 박제 shoulder/hip tilt 박제 25 data points 박제 P25/P50/P75/P90 산출 → yaml 박제 write. D-04 정합 (future re-run)."
      pattern: "yaml\\.safe_dump|tilt_thresholds\\.yaml"
    - from: backend/shared/python/sunity_shared/analysis/force_signals.py
      to: backend/shared/python/sunity_shared/analysis/pole/aligner.py
      via: "compute_axis_deviation 박제 tilt-only 박제 path 박제 pose_frames 박제 keypoints_3d_pole_aligned 박제 활용 — Phase 1 박제 pole_aligner 박제 회전 박제 정합 보존. translation 박제 부재 박제 본 plan 박제 algorithm 박제 영향 0 (tilt = rotation-only, origin-invariant)."
      pattern: "keypoints_3d_pole_aligned|pole_aligned"
---

<objective>
compute_axis_deviation 본체를 tilt-only 박제 재작성 + 정은지 5영상 sweep 분포 박제 calibration script + tilt_thresholds.yaml lazy load 박제. Wave 0 의 6 필드 schema 위에 박제. RESEARCH §4 α-4 + §4.3 β-1 + CONTEXT D-02 + D-03 + D-04 정합.

Purpose: distance 차원 hard break 후 axis severity 박제 measurement 박제 tilt-only metric 박제 박제 — 정은지 baseline 박제 5/5 영상 박제 severity='low' 박제 박제 (Wave 2 박제 production 통합 박제 박제 박제 박제 정량 박제 박제). yaml 박제 lazy load 박제 D-04 정합 (정은지 추가 영상 확보 시 yaml 박제 update + commit 코드 변경 0). FPS-invariance 박제 Phase 8 박제 정신 유지.

Output:
- backend/shared/python/sunity_shared/analysis/force_signals.py (compute_axis_deviation tilt-only + _get_tilt_thresholds + _severity_from_tilt + distance helper 박제 cleanup)
- backend/judging_data/tilt_thresholds.yaml (calibrated 정은지 5영상 분포 + IPSF tolerance)
- backend/scripts/calibrate_tilt_thresholds.py (sweep 박제 분포 산출 script — D-04 정합 future re-run entry)
- backend/tests/phase08_1/ (5+ 신설 test)
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-axis-metric-redesign/08.1-CONTEXT.md
@.planning/phases/08.1-axis-metric-redesign/08.1-RESEARCH.md
@.planning/phases/08.1-axis-metric-redesign/08.1-00-PLAN.md
@.planning/phases/08-jerk-jitter/PHASE8-INHERITED-ISSUES.md

@backend/shared/python/sunity_shared/analysis/force_signals.py
@backend/shared/python/sunity_shared/analysis/pole/aligner.py
@backend/shared/python/sunity_shared/firestore_admin.py
@backend/judging_data/contact_points.yaml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: compute_axis_deviation 본체 tilt-only 재작성 + yaml lazy load + threshold loader</name>
  <files>backend/shared/python/sunity_shared/analysis/force_signals.py, backend/tests/phase08_1/fixtures/__init__.py, backend/tests/phase08_1/fixtures/_factory.py, backend/tests/phase08_1/test_compute_axis_tilt_only.py, backend/tests/phase08_1/test_tilt_thresholds_loader.py</files>
  <behavior>
    RED → GREEN.
    Failing tests first (test_compute_axis_tilt_only.py + test_tilt_thresholds_loader.py):
      - test_compute_axis_pole_aligned_path: pose_frames with keypoints_3d_pole_aligned 박제 (factory 박제 shoulder_tilt_deg=15, hip_tilt_deg=10) → compute_axis_deviation → axis_metrics[0].shoulder_tilt ≈ 15 (±2 tolerance, np.median 박제 단일 phase × N frames) / hip_tilt ≈ 10 / severity='low' (yaml fallback (25.0, 37.5) 박제 — 15° < 25° = low).
      - test_compute_axis_image_2d_path_fallback: pose_frames with keypoints_2d only (no pole_aligned) + pole_line 박제 → image_2d path → tilt 산출 박제. (factory 박제 박제 — Phase 1 박제 image_2d 박제 keypoints 박제 박제 박제 시뮬).
      - test_compute_axis_neither_path_unavailable: pose_frames 박제 no keypoints_3d_pole_aligned AND no keypoints_2d → axis_metrics[0].shoulder_tilt is None / hip_tilt is None / severity='low' / warnings contains 'tilt_unavailable'.
      - test_severity_from_tilt_low_medium_high: `_severity_from_tilt(15, (25, 37.5))` == 'low' / `_severity_from_tilt(30, (25, 37.5))` == 'medium' / `_severity_from_tilt(40, (25, 37.5))` == 'high' / `_severity_from_tilt(None, (25, 37.5))` == 'low'.
      - test_fps_invariance_tilt_metric: 9 fps pose_frames (N frames) vs 18 fps pose_frames (2N frames, identical tilt per frame) — compute_axis_deviation 박제 둘 다 동일 shoulder_tilt / hip_tilt 박제 (np.median 박제 동일).
      - test_axis_metric_has_only_six_fields: dataclasses.fields(AxisDeviationMetric) → set names == {phase, shoulder_tilt, hip_tilt, severity, confidence, warnings}.
      - test_loader_reads_yaml_when_present: monkeypatch `_TILT_THRESHOLDS_PATH` → tmp_path yaml with known values → `_get_tilt_thresholds()` → returns shoulder + hip cutoffs.
      - test_loader_falls_back_when_yaml_missing: monkeypatch path → nonexistent → `_get_tilt_thresholds()` → returns ((25.0, 37.5), (25.0, 37.5)) — fallback default 박제.
      - test_loader_caches_result: monkeypatch valid yaml → call `_get_tilt_thresholds()` twice → 2nd call does NOT re-read file (mock yaml.safe_load call_count == 1).
      - test_loader_returns_shoulder_and_hip_thresholds: yaml separately specifies shoulder + hip → loader returns 2 distinct tuples.
  </behavior>
  <action>
Step 1 — Create fixture factory:
  - backend/tests/phase08_1/fixtures/__init__.py: empty marker.
  - backend/tests/phase08_1/fixtures/_factory.py:
    `make_pose_frame_with_pole_aligned(frame_index: int, shoulder_tilt_deg: float, hip_tilt_deg: float) -> PoseFrame` — synthesize keypoints_3d_pole_aligned such that arcsin(|Δz|/||Δ||) == shoulder_tilt_deg / hip_tilt_deg. Use: shoulder left=(0, 0, 0), shoulder right=(cos(θ_s), 0, sin(θ_s)) where θ_s = radians(shoulder_tilt_deg). Same for hip. Reliability='high'.
    `make_phase_boundary(phase: MotionPhase, start: int, end: int) -> PhaseBoundary` — wrap PhaseBoundary with confidence='medium', source='heuristic'.
    `make_pole_axis_measurement_vertical_fallback() -> PoleAxisMeasurement` — PoleAxis(axis_vector=(0,0,1), confidence='low', source='vertical_fallback'), line=None, coordinate_space='unavailable'. Use existing pole_geometry.build_pole_axis_measurement helper.
    `make_pose_frame_with_image_2d_only(frame_index, shoulder_tilt_deg, hip_tilt_deg, pole_line: PoleLine2D)` — keypoints_2d 박제 synth such that shoulder line vs pole line direction angle == shoulder_tilt_deg.

Step 2 — Write the failing tests (test_compute_axis_tilt_only.py + test_tilt_thresholds_loader.py) using the factory. Verify pytest output RED.

Step 3 — Rewrite compute_axis_deviation body (GREEN):
  - force_signals.py L162-165: Delete `AXIS_PELVIS_DISTANCE_THRESHOLDS` and `AXIS_CHEST_DISTANCE_THRESHOLDS` constants. Keep `AXIS_TILT_THRESHOLDS_DEG = (25.0, 37.5)` as the fallback default (yaml-missing safety net per RESEARCH §1.3 + IPSF ±20° default × 1.5).
  - Add new yaml loader after the `_CONTACT_POINTS_CACHE` block (~L230). Implementation:
    ```
    _TILT_THRESHOLDS_PATH = Path(__file__).parent.parent.parent.parent.parent / 'judging_data' / 'tilt_thresholds.yaml'
    _TILT_THRESHOLDS_CACHE: tuple[tuple[float, float], tuple[float, float]] | None = None
    _TILT_THRESHOLDS_FALLBACK_EMITTED = False

    def _get_tilt_thresholds() -> tuple[tuple[float, float], tuple[float, float]]:
        global _TILT_THRESHOLDS_CACHE, _TILT_THRESHOLDS_FALLBACK_EMITTED
        if _TILT_THRESHOLDS_CACHE is not None:
            return _TILT_THRESHOLDS_CACHE
        try:
            import yaml
            with open(_TILT_THRESHOLDS_PATH) as f:
                data = yaml.safe_load(f)
            shoulder = (float(data['shoulder_tilt']['medium_cutoff_deg']), float(data['shoulder_tilt']['high_cutoff_deg']))
            hip = (float(data['hip_tilt']['medium_cutoff_deg']), float(data['hip_tilt']['high_cutoff_deg']))
            _TILT_THRESHOLDS_CACHE = (shoulder, hip)
        except (FileNotFoundError, KeyError, ImportError, ValueError):
            _TILT_THRESHOLDS_CACHE = (AXIS_TILT_THRESHOLDS_DEG, AXIS_TILT_THRESHOLDS_DEG)
            _TILT_THRESHOLDS_FALLBACK_EMITTED = True
        return _TILT_THRESHOLDS_CACHE
    ```
    Also expose `_reset_tilt_thresholds_cache()` for test monkeypatching.

  - Add `_severity_from_tilt(tilt_abs_deg: float | None, thresholds: tuple[float, float]) -> SeverityLevel` (mirror of `_severity_from_distance` but tilt-only): None → 'low'; >= thresholds[1] → 'high'; >= thresholds[0] → 'medium'; else 'low'. Place above `_severity_from_distance` (which will be deleted).

  - Delete distance helpers L825-1034 (Wave 0 transitional 박제 cleanup): `_observed_torso_length`, `_observed_torso_length_pole_aligned`, `_kp_pole_aligned_xy` (kept if still used by tilt helpers), `_pelvis_position_pole_aligned`, `_chest_position_pole_aligned`, `_pole_aligned_axis_distance`, `_kp2d_xy` (kept if still used by tilt helpers), `_midpoint` (kept — utility), `_pelvis_position_image_2d`, `_chest_position_image_2d`, `_line_direction_angle_deg` (kept if still used), `_severity_from_distance`, `_deviation_direction_from_pelvis`.
    Audit which helpers `_shoulder_tilt_2d` / `_hip_tilt_2d` / `_shoulder_tilt_pole_aligned` / `_hip_tilt_pole_aligned` depend on (`_kp2d_xy`, `_kp_pole_aligned_xy`, `_line_direction_angle_deg`) and KEEP them. Delete only the truly unused ones.

  - Replace `compute_axis_deviation` body (L1037-1227) with tilt-only:
    ```
    def compute_axis_deviation(pose_frames, phase_boundaries, pole_axis_measurement, *, fps=9.0):
        # Phase 8.1 (Wave 1) — tilt-only 박제. distance 차원 hard break (D-01).
        # pole_aligned 3D path 우선 (RTMW keypoints_2d 미박제 정합).
        # image_2d fallback (keypoints_2d 가용 시) — pole line direction 박제 필요.
        # 둘 다 미가용 → tilt None + severity='low' + warning 'tilt_unavailable'.
        line = pole_axis_measurement.line if pole_axis_measurement else None
        has_pole_aligned = bool(pose_frames and pose_frames[0].keypoints_3d_pole_aligned)
        has_kp2d = bool(pose_frames and pose_frames[0].keypoints_2d) and (line is not None)
        shoulder_thresh, hip_thresh = _get_tilt_thresholds()
        base_warnings: list[str] = []
        if _TILT_THRESHOLDS_FALLBACK_EMITTED:
            base_warnings.append('tilt_thresholds_fallback')

        if not has_pole_aligned and not has_kp2d:
            return [
                AxisDeviationMetric(
                    phase=b.phase,
                    shoulder_tilt=None,
                    hip_tilt=None,
                    severity='low',
                    confidence='low',
                    warnings=list(base_warnings) + ['tilt_unavailable'],
                )
                for b in phase_boundaries
            ]

        metrics = []
        for b in phase_boundaries:
            phase_frames = pose_frames[b.start_frame_idx : b.end_frame_idx]
            if has_pole_aligned:
                shoulder_tilts = [t for t in (_shoulder_tilt_pole_aligned(f) for f in phase_frames) if t is not None]
                hip_tilts = [t for t in (_hip_tilt_pole_aligned(f) for f in phase_frames) if t is not None]
            else:
                shoulder_tilts = [t for t in (_shoulder_tilt_2d(f, line) for f in phase_frames) if t is not None]
                hip_tilts = [t for t in (_hip_tilt_2d(f, line) for f in phase_frames) if t is not None]
            shoulder_tilt_val = float(np.median(shoulder_tilts)) if shoulder_tilts else None
            hip_tilt_val = float(np.median(hip_tilts)) if hip_tilts else None
            sev_s = _severity_from_tilt(abs(shoulder_tilt_val) if shoulder_tilt_val is not None else None, shoulder_thresh)
            sev_h = _severity_from_tilt(abs(hip_tilt_val) if hip_tilt_val is not None else None, hip_thresh)
            severity = _max_severity(sev_s, sev_h)
            # confidence from frame reliability ratio (Phase 8 박제 pattern).
            if phase_frames:
                high_count = sum(1 for f in phase_frames if getattr(f, 'reliability', 'low') == 'high')
                ratio = high_count / max(1, len(phase_frames))
                metric_confidence: MetricConfidence = 'medium' if ratio >= 0.7 else 'low'
            else:
                metric_confidence = 'low'
            metrics.append(
                AxisDeviationMetric(
                    phase=b.phase,
                    shoulder_tilt=shoulder_tilt_val,
                    hip_tilt=hip_tilt_val,
                    severity=severity,
                    confidence=metric_confidence,
                    warnings=list(base_warnings),
                )
            )
        return metrics
    ```

Step 4 — Run tests, verify all GREEN. Run full phase08_1 suite + phase06/07/08/pipeline regression. tsc --noEmit.

Step 5 — commit: `feat(08.1-01): compute_axis_deviation tilt-only + yaml lazy load + threshold loader`.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; PYTHONPATH=shared/python pytest tests/phase08_1/test_compute_axis_tilt_only.py tests/phase08_1/test_tilt_thresholds_loader.py -v</automated>
    <automated>cd backend &amp;&amp; PYTHONPATH=shared/python pytest tests/phase06 tests/phase07 tests/phase08 tests/phase08_1 tests/pipeline -x</automated>
    <automated>cd app &amp;&amp; npm run typecheck</automated>
    <automated>python -c "from sunity_shared.analysis.force_signals import compute_axis_deviation, _get_tilt_thresholds, _severity_from_tilt; print('imports OK')"</automated>
    <automated>grep -nE 'AXIS_PELVIS_DISTANCE_THRESHOLDS|AXIS_CHEST_DISTANCE_THRESHOLDS|_observed_torso_length|_pole_aligned_axis_distance|_deviation_direction_from_pelvis|_severity_from_distance' backend/shared/python/sunity_shared/analysis/force_signals.py | grep -v '^[^:]*:[0-9]*:[[:space:]]*#' | wc -l</automated>
  </verify>
  <done>
    - compute_axis_deviation body returns AxisDeviationMetric with only 6 fields (no distance fields).
    - `_get_tilt_thresholds()` lazy-loads yaml, falls back to (25.0, 37.5) on missing.
    - `_severity_from_tilt(tilt_abs_deg, thresholds)` exists and follows low/medium/high boundary semantics.
    - All Wave 1 tests in test_compute_axis_tilt_only.py + test_tilt_thresholds_loader.py PASS.
    - Regression: phase06/07/08/pipeline all PASS.
    - Last grep returns 0 (deleted helpers + constants).
    - tsc --noEmit clean (TS unchanged).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: calibrate_tilt_thresholds.py 신설 + 정은지 5영상 sweep 분포 산출 + tilt_thresholds.yaml write + drift defense test</name>
  <files>backend/scripts/calibrate_tilt_thresholds.py, backend/judging_data/tilt_thresholds.yaml, backend/tests/phase08_1/test_tilt_thresholds_yaml_drift.py</files>
  <behavior>
    Script reads Firestore sweep docs → 25 data points (5 videos × 5 phases) of shoulder/hip tilt absolute values → P25/P50/P75/P90 → yaml write.
    yaml drift defense tests:
      - test_yaml_has_required_top_level_keys: version, source, calibrated_at, sample_size, shoulder_tilt, hip_tilt, ipsf_tolerance present.
      - test_yaml_has_shoulder_and_hip_distributions: p25, p50, p75, p90, medium_cutoff_deg, high_cutoff_deg present for both shoulder + hip.
      - test_yaml_ipsf_tolerance_default_20deg: ipsf_tolerance.default_global_tolerance_deg == 20.0.
      - test_yaml_medium_cutoff_at_least_ipsf_default: shoulder/hip medium_cutoff_deg >= 20.0 (max(P90, 20.0)).
      - test_yaml_high_cutoff_is_medium_times_1_5: high_cutoff_deg == medium_cutoff_deg * 1.5 (tolerance ±0.01).
  </behavior>
  <action>
Step 1 — Create calibrate_tilt_thresholds.py:
  - Argparse: `--sweep-uid` (default `sweep_phase8_1780986673`), `--analysis-ids` (default = 5 reference video doc IDs from PHASE8-INHERITED-ISSUES §9 — `ref-invert/climb/foxtop/foxtop-split/sideway-spin`-derived doc IDs; if unknown, list will be passed; helper that lists all completed analyses under the sweep uid is acceptable), `--output-path` (default `backend/judging_data/tilt_thresholds.yaml`), `--dry-run` (flag).
  - Body:
    ```python
    from sunity_shared import firestore_admin
    fs = firestore_admin._get_client()
    docs = list(fs.collection(f'users/{sweep_uid}/analyses').stream())
    if analysis_ids:
        docs = [d for d in docs if d.id in analysis_ids]
    shoulder_vals, hip_vals = [], []
    for d in docs:
        data = d.to_dict() or {}
        report = (data.get('result') or {}).get('forceSignalsReport') or {}
        for m in report.get('axisMetrics') or []:
            s = m.get('shoulderTilt')
            h = m.get('hipTilt')
            if s is not None and np.isfinite(s):
                shoulder_vals.append(abs(float(s)))
            if h is not None and np.isfinite(h):
                hip_vals.append(abs(float(h)))
    # percentiles
    def pct(vals):
        return {p: float(np.percentile(vals, p)) for p in (25, 50, 75, 90)}
    s_pct = pct(shoulder_vals)
    h_pct = pct(hip_vals)
    s_medium = max(s_pct[90], 20.0)
    h_medium = max(h_pct[90], 20.0)
    s_high = s_medium * 1.5
    h_high = h_medium * 1.5
    yaml_data = {
        'version': '1.0',
        'source': f'jeongeunji_5_videos + ipsf_tolerance_20deg ({sweep_uid})',
        'calibrated_at': datetime.utcnow().isoformat() + 'Z',
        'sample_size': len(shoulder_vals),  # likely 25
        'shoulder_tilt': {'p25': s_pct[25], 'p50': s_pct[50], 'p75': s_pct[75], 'p90': s_pct[90], 'medium_cutoff_deg': s_medium, 'high_cutoff_deg': s_high},
        'hip_tilt': {'p25': h_pct[25], 'p50': h_pct[50], 'p75': h_pct[75], 'p90': h_pct[90], 'medium_cutoff_deg': h_medium, 'high_cutoff_deg': h_high},
        'ipsf_tolerance': {
            'source_ref': 'NotebookLM citation 9 Page 87 Glossary + Aerial Pole CoP Page 63 S55 Iron X (±20°)',
            'default_global_tolerance_deg': 20.0,
        },
    }
    if not dry_run:
        with open(output_path, 'w') as f:
            yaml.safe_dump(yaml_data, f, sort_keys=False, default_flow_style=False)
    print(table)  # render distribution + cutoffs to stdout
    ```
  - Logging + console summary table.
  - License header: D-04 정합 — re-run when belle confirms new 정은지 videos: `python backend/scripts/calibrate_tilt_thresholds.py --sweep-uid <new_sweep>`.

Step 2 — Run calibration locally (sweep already exists, Firestore reachable per Phase 8 close-out):
  - Activate AWS / Firebase admin SA env (per local dev setup).
  - Run: `python backend/scripts/calibrate_tilt_thresholds.py` (defaults to sweep_phase8_1780986673).
  - If Firestore unreachable from current shell (e.g. SA missing): construct yaml manually from PHASE8-INHERITED-ISSUES §1.1 raw tilt ranges (ref-invert shoulder 12–58 / hip 11–32; ref-climb 16–31 / 10–47; ref-foxtop 11–36 / 22–37; ref-foxtop-split 11–53 / 17–50; ref-sideway-spin 20–27 / 14–36) by computing approximate per-phase medians (use endpoint average as conservative proxy) then percentiles. Document the fallback path explicitly inside yaml `source` field if Firestore unavailable: `source: 'jeongeunji_5_videos_phase8_inherited_evidence + ipsf_tolerance_20deg'`. The yaml's accuracy is verified in Wave 2 via re-sweep; Wave 1 yaml is the working baseline.

Step 3 — Write test_tilt_thresholds_yaml_drift.py (5 tests above) — reads the file, asserts schema invariants. Use `yaml.safe_load`.

Step 4 — Cache reset hook in compute_axis_deviation tests:
  - Wave 1 Task 1 introduced `_TILT_THRESHOLDS_CACHE` module-level cache. Add `pytest` fixture in conftest.py: `@pytest.fixture(autouse=True) def _reset_tilt_cache(): force_signals._TILT_THRESHOLDS_CACHE = None; force_signals._TILT_THRESHOLDS_FALLBACK_EMITTED = False; yield; force_signals._TILT_THRESHOLDS_CACHE = None`. Ensures tests don't bleed cache across runs.

Step 5 — Run tests, verify all GREEN. Verify phase08_1/test_compute_axis_tilt_only.py 박제 fallback path 박제 NOT triggered when real yaml exists (severity 박제 yaml 박제 값 박제 사용 박제).

Step 6 — commits (2):
  - `feat(08.1-01): calibrate_tilt_thresholds.py + tilt_thresholds.yaml from sweep_phase8_1780986673`
  - `test(08.1-01): yaml drift defense + cache reset fixture`
  </action>
  <verify>
    <automated>cd backend &amp;&amp; PYTHONPATH=shared/python python scripts/calibrate_tilt_thresholds.py --dry-run 2>&amp;1 | head -40</automated>
    <automated>cd backend &amp;&amp; PYTHONPATH=shared/python pytest tests/phase08_1/test_tilt_thresholds_yaml_drift.py -v</automated>
    <automated>cd backend &amp;&amp; PYTHONPATH=shared/python pytest tests/phase06 tests/phase07 tests/phase08 tests/phase08_1 tests/pipeline -x</automated>
    <automated>test -f backend/judging_data/tilt_thresholds.yaml &amp;&amp; cat backend/judging_data/tilt_thresholds.yaml | head -25</automated>
  </verify>
  <done>
    - backend/scripts/calibrate_tilt_thresholds.py exists, runs with --dry-run, supports D-04 future re-run.
    - backend/judging_data/tilt_thresholds.yaml exists with required schema keys.
    - shoulder + hip medium_cutoff_deg >= 20.0 (IPSF tolerance floor).
    - high_cutoff_deg ≈ medium_cutoff_deg × 1.5.
    - 5 yaml drift defense tests PASS.
    - Regression 0: full pytest suite + tsc --noEmit clean.
    - 2 commits.
  </done>
</task>

</tasks>

<verification>
- compute_axis_deviation 본체 tilt-only 박제 — distance 산출 path 박제 부재 (grep `_observed_torso_length` / `_pole_aligned_axis_distance` / `_severity_from_distance` 박제 0).
- AxisDeviationMetric 6 필드 박제 (Wave 0 schema 정합) — production behavior 박제 tilt-only path 박제.
- tilt_thresholds.yaml 박제 정은지 5영상 sweep_phase8_1780986673 박제 25 data points 박제 P25/P50/P75/P90 + IPSF ±20° default + medium = max(P90, 20°) + high = medium × 1.5 박제 calibrated.
- calibrate_tilt_thresholds.py 박제 CLI + dry-run + D-04 future re-run entry 박제.
- FPS-invariance 박제 9 fps vs 18 fps 동일 tilt 박제 (Phase 8 정신 유지).
- 회귀 0: phase06 + phase07 + phase08 + phase08_1 + pipeline 모두 PASS + tsc --noEmit clean.
- Wave 2 진입 준비 박제 — pipeline rewire (caller 박제 정합) + Pod 재배포 + 정은지 5/5 영상 재sweep 박제 evidence 박제 진입 가능.

**중요 박제**: 본 plan 박제 = analysis core 만 박제. pipeline rewire 박제 = Wave 2. 정은지 5/5 영상 axis severity='low' 박제 production verification 박제 = Wave 2 (Pod 재배포 + 재sweep).
</verification>

<success_criteria>
- compute_axis_deviation 본체 tilt-only 박제 — 6 필드 AxisDeviationMetric 박제 산출.
- tilt_thresholds.yaml 박제 정은지 5영상 + IPSF 박제 calibrated 박제 (필수 schema keys + medium/high cutoff invariants).
- calibrate_tilt_thresholds.py 박제 CLI + dry-run + future re-run (D-04) entry.
- Wave 0 + Wave 1 종료 시점 박제 phase08_1 unit test 박제 18+ PASS (Wave 0 13 + Wave 1 5+).
- FPS-invariance 박제 검증 (Phase 8 정신 유지).
- 회귀 0 + tsc --noEmit clean.
- Wave 2 진입 차단 해소 — pipeline rewire + Pod 재배포 + 재sweep 박제 가능.
</success_criteria>

<output>
Create `.planning/phases/08.1-axis-metric-redesign/08.1-01-SUMMARY.md` when done.
</output>
