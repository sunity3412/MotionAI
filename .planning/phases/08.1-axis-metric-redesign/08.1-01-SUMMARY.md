---
phase: 08.1-axis-metric-redesign
plan: 01
subsystem: backend-analysis-core
tags: [phase-08-1, tilt-metric, calibration, ipsf-tolerance, jeongeunji-distribution, wave-1-algorithm, p100-plus-margin, schema-v2]
dependency_graph:
  requires:
    - Phase 08.1 Wave 0 (Plan 00) — AxisDeviationMetric 6 필드 schema + transitional stub
    - Phase 8 inherited sweep_phase8_1780986673 (25 axisMetrics 정은지 5영상)
    - 08.1-CONTEXT.md D-02 (P100+margin) + D-03 (IPSF tolerance) + D-04 (future re-run)
  provides:
    - compute_axis_deviation 실 tilt-only 본체 (pole_aligned 3D path 우선 + image_2d fallback + 둘 다 미가용 → 'tilt_unavailable')
    - tilt_thresholds.yaml schema_v2 ('elite_p100_plus_margin', null_tilt_verified=true)
    - calibrate_tilt_thresholds.py CLI (firestore / repo-artifact / wave2-explicit 분기 + C-H3 preflight hard gate)
    - 신설 helper `_normalize_angle_undirected` (C-M1, modulo 180° + min(a, 180-a) → [0, 90])
    - 신설 helper `_severity_from_tilt` (C-H2, boundary-low + epsilon 1e-9)
    - 신설 lazy loader `_get_tilt_thresholds` (W10 정합 — cache tuple 3rd element 단일 source, module-global flag 부재)
  affects:
    - Wave 2: pipeline rewire + Pod 재배포 + 정은지 5영상 재sweep evidence
    - Phase 9: raw `shoulder_tilt` / `hip_tilt` / `confidence` / `warnings` consumer (severity 직접 trust 금지, D-05 Codex C-M4 정합)
tech_stack:
  added: []
  patterns:
    - schema_v2 yaml lazy load + cache (W10 — fallback 신호 cache tuple 3rd element 단일 source, module-global mutable flag 부재)
    - P100 + margin operational cutoff (Codex C-H2 정합 — P90 폐기, 정은지 baseline 25/25 'low' 유지 보장)
    - boundary-low + epsilon 1e-9 severity (boundary value = 'low')
    - undirected line angle normalize (C-M1 — modulo 180° + min(a, 180-a) → unsigned [0, 90])
    - calibration source preflight hard gate (C-H3 — null tilt + transitional warning 부재 검증)
key_files:
  created:
    - backend/judging_data/tilt_thresholds.yaml
    - backend/scripts/calibrate_tilt_thresholds.py
    - backend/tests/phase08_1/fixtures/__init__.py
    - backend/tests/phase08_1/fixtures/_factory.py
    - backend/tests/phase08_1/test_compute_axis_tilt_only.py
    - backend/tests/phase08_1/test_axis_2d_angle_normalization.py
    - backend/tests/phase08_1/test_severity_boundary_value_is_low.py
    - backend/tests/phase08_1/test_severity_above_medium.py
    - backend/tests/phase08_1/test_severity_above_high.py
    - backend/tests/phase08_1/test_tilt_thresholds_loader.py
    - backend/tests/phase08_1/test_tilt_thresholds_yaml_drift.py
    - backend/tests/phase08_1/test_calibration_preflight.py
  modified:
    - backend/shared/python/sunity_shared/analysis/force_signals.py
    - backend/tests/phase08_1/conftest.py
    - backend/tests/phase08/test_compute_axis_deviation.py
    - backend/tests/phase08_1/test_compute_force_signals_does_not_reference_coordinate_space.py
decisions:
  - "옵션 A 진입 (Firestore reachable, sweep_phase8_1780986673 25 docs × 5 axisMetrics 검증 통과). Task 0 precondition 본 plan 진입 전 belle 박제 완료"
  - "정은지 5영상 sweep P100 + margin 5° = shoulder medium 63.28° / high 94.92°, hip medium 54.62° / high 81.93° (보수적 cutoff — 정은지 max 자체 가 큼)"
  - "Codex iteration 1-5 fix 모두 단일 release 박제 (Wave 0 + Wave 1 한 boundary): C-H2 P100+margin + 1e-9 epsilon, C-H3 calibration preflight hard gate, C-M1 _normalize_angle_undirected, C-MH1 docstring naming caveat"
  - "schema_v2 yaml loader 가 schema_version + calibration_method + source.null_tilt_verified 강제 검증 → 위반 시 fallback (production sweep 진입 차단 신호)"
  - "Wave 0 의 stub 본체 검증 4 test (test_compute_axis_returns_stub_for_all_phases 등) 제거 — 새 본체 검증은 phase08_1/test_compute_axis_tilt_only.py 가 보유. R2 drift defense (torso_scale denominator 영구 금지) 만 phase08 에서 보존"
metrics:
  duration_min: 40
  completed_date: "2026-06-09"
requirements_completed: [FORCE-01]
---

# Phase 08.1 Plan 01: Tilt Metric 본체 + 정은지 P100+Margin Calibration (Wave 1) Summary

**One-liner**: compute_axis_deviation 본체 가 Wave 0 transitional stub → 실 tilt-only 알고리즘 (pole_aligned 3D path 우선 + image_2d fallback + 'tilt_unavailable' 분기) + 정은지 25 data points P100+margin 5° calibration (schema_v2 'elite_p100_plus_margin', shoulder cutoff 63.28°/94.92°, hip 54.62°/81.93°) + C-M1 `_normalize_angle_undirected` (modulo 180° → unsigned [0, 90]) + C-H2 boundary-low severity + 1e-9 epsilon + C-H3 calibration preflight hard gate.

## Task 0 (Precondition): 옵션 A (Firestore reachable) — PASS

belle 의 환경 확인 + Claude 의 직접 검증:

| 검증 | 결과 |
|---|---|
| Firebase SA path | `/Users/kimtaesung/Dev/SunityMotion/firebase-sa.json` (`FIREBASE_SA_PATH` env 적용) |
| Firestore reachability | PASS — `users/sweep_phase8_1780986673/analyses` 5 doc stream OK |
| sweep doc count | 5 (정은지 5영상 정합) |
| axisMetrics 총 instance | 25 (5 docs × 5 phases) |
| null shoulderTilt / hipTilt | 0 |
| 'phase_8_1_wave_0_transitional' 부재 | OK (Phase 8 inherited sweep, Wave 0 stub 출력 아님) |
| 'tilt_unavailable' 부재 | OK |

**옵션 B 미진입** — repo artifact `08.1-CALIBRATION-SOURCE.json` 부재 (불필요).

본 plan 진입 = `--source-type firestore` (default) path.

## Task 1: compute_axis_deviation 실 tilt-only + yaml lazy loader + helper cleanup

Single atomic commit (`30fd7d2`).

### force_signals.py 본체 변경

**모듈 상수 cleanup** (W9 audit 정합):
- 삭제: `AXIS_PELVIS_DISTANCE_THRESHOLDS`, `AXIS_CHEST_DISTANCE_THRESHOLDS` (distance 차원 영구 제거).
- 갱신: `AXIS_TILT_THRESHOLDS_DEG = (25.0, 37.5)` — `_get_tilt_thresholds()` fallback default.

**신설 lazy loader** (`_CONTACT_POINTS_CACHE` 블록 뒤):
- `_TILT_THRESHOLDS_PATH = Path(__file__).parent.parent.parent.parent.parent / 'judging_data' / 'tilt_thresholds.yaml'` (5 .parent → backend/ root).
- `_TILT_THRESHOLDS_CACHE: tuple[tuple[float, float], tuple[float, float], list[str]] | None = None`.
- `_get_tilt_thresholds()` schema_v2 강제 검증 (schema_version==2 + calibration_method=='elite_p100_plus_margin' + source.null_tilt_verified==True). fallback 신호 = cache tuple 3rd element (`['tilt_thresholds_fallback']`). **W10 정합 — module-global mutable boolean flag 부재**.
- `_reset_tilt_thresholds_cache()` — test-only helper.

**신설 helpers**:
- `_normalize_angle_undirected(angle_deg)` — modulo 180° + min(a, 180-a) → [0, 90]. keypoint ordering swap (left↔right) artifact 차단 (C-M1).
- `_severity_from_tilt(tilt_deg, thresholds)` — boundary-low + epsilon 1e-9 (`tilt_deg > cutoff + 1e-9`). boundary value 정확히 cutoff → 'low'. None → 'low'. 정은지 baseline 25/25 'low' invariant (C-H2).

**Helper cleanup** (13 helpers 삭제):
- `_observed_torso_length_pole_aligned`, `_pelvis_position_pole_aligned`, `_chest_position_pole_aligned`, `_pole_aligned_axis_distance` — pole_aligned distance path 삭제.
- `_pelvis_position_image_2d`, `_chest_position_image_2d` — image_2d distance path 삭제.
- `_severity_from_distance` — distance severity 삭제.
- `_deviation_direction_from_pelvis` — deviation_direction 필드 제거 정합.

**Helper 보존** (4 helpers + tilt helpers):
- `_observed_torso_length` — `compute_contact_stability` 의존.
- `_kp_pole_aligned_xy`, `_kp2d_xy`, `_midpoint`, `_line_direction_angle_deg` — Wave 1 새 본체 + `compute_contact_stability` 의존.
- `_shoulder_tilt_pole_aligned`, `_hip_tilt_pole_aligned` — pole_aligned path 진입점 (Wave 0 보존, Wave 1 호출).
- `_shoulder_tilt_2d`, `_hip_tilt_2d` — Wave 1 갱신: `_normalize_angle_undirected` 적용 → unsigned [0, 90] 강제.

**`compute_axis_deviation` 본체 교체**:
```python
def compute_axis_deviation(
    pose_frames: list[PoseFrame],
    phase_boundaries: list[PhaseBoundary],
    pole_axis_measurement: PoleAxisMeasurement,
    *,
    fps: float = 9.0,
) -> list[AxisDeviationMetric]:
    # pole_aligned 3D path 우선 → image_2d fallback → 둘 다 미가용 → 'tilt_unavailable'.
    # severity = _max_severity(_severity_from_tilt(shoulder), _severity_from_tilt(hip))
    # confidence = 'medium' if >= 70% high-reliability frames else 'low'.
    # base_warnings (yaml fallback 시 'tilt_thresholds_fallback') 박제.
```

### 신설 phase08_1 test (28 PASS)

| 파일 | tests | 검증 |
|---|---|---|
| `test_compute_axis_tilt_only.py` | 6 | pole_aligned / image_2d / neither path + severity 정합 + FPS-invariance (9 vs 18 fps) + 6 필드 정합 |
| `test_axis_2d_angle_normalization.py` | 6 | C-M1 normalize 6 분기 (vertical / horizontal / near-180 / swap / negative / 120°→60°) |
| `test_severity_boundary_value_is_low.py` | 5 | C-H2 boundary semantics (medium/high 정확히 cutoff → low/medium 유지 + epsilon 1e-12 floor) |
| `test_severity_above_medium.py` | 3 | medium_cutoff + 0.001 → 'medium' (epsilon-aware) |
| `test_severity_above_high.py` | 3 | high_cutoff + 0.001 → 'high' (epsilon-aware) |
| `test_tilt_thresholds_loader.py` | 5 | lazy load + cache + fallback + W10 module-global flag 부재 검증 |

### Wave 0 cleanup item

- `phase08/test_compute_axis_deviation.py` — 4 stub assertion 제거, R2 drift defense (torso_scale denominator 영구 금지) 만 보존.
- `phase08_1/test_compute_force_signals_does_not_reference_coordinate_space.py` — `test_compute_axis_deviation_emits_phase_8_1_transitional_warning` (1 test) 제거 — Wave 1 새 본체 검증 은 `test_compute_axis_tilt_only.py` 가 보유. C-B1 grep guard (2 tests) 보존.

### conftest.py augment

`_reset_tilt_cache` autouse fixture 추가 — test 간 `_TILT_THRESHOLDS_CACHE` 누수 방지.

### fixtures/_factory.py (REVIEWS R9 정합)

Programmatic factory (fixture JSON 파일 대신):
- `make_pose_frame_with_pole_aligned(frame_index, shoulder_tilt_deg, hip_tilt_deg)` — `arcsin(|Δz|/||Δ||) == tilt_deg` 의 역산 좌표 합성.
- `make_pose_frame_with_image_2d_only(...)` — `_shoulder_tilt_2d(frame, line)` 가 계산하는 `_normalize_angle_undirected(sh_angle - pole_angle)` 가 의도 tilt_deg 산출.
- `make_pose_frame_neither_path(...)` — keypoints_3d_pole_aligned 빈 dict + keypoints_2d=None.
- `make_phase_boundary(phase, start, end)`, `make_pole_axis_measurement_vertical_fallback()`, `make_pole_line_vertical()`, `make_pole_axis_measurement_with_line(line)`.

## Task 2: calibrate_tilt_thresholds.py (schema_v2) + tilt_thresholds.yaml 산출 + drift + preflight tests

Two atomic commits (`e08be9c` + `80c9a6b`).

### calibrate_tilt_thresholds.py

```bash
FIREBASE_SA_PATH=/Users/kimtaesung/Dev/SunityMotion/firebase-sa.json \
PYTHONPATH=backend/shared/python \
python backend/scripts/calibrate_tilt_thresholds.py \
  --sweep-uid sweep_phase8_1780986673 \
  --source-type firestore
```

**Source 분기** (`--source-type`):
- `firestore` (default) — Phase 8 inherited sweep stream.
- `repo-artifact` — `.planning/phases/08.1-axis-metric-redesign/08.1-CALIBRATION-SOURCE.json` (Firestore 미가용 시).
- `wave2-explicit` — Wave 2 자기 sweep 재calibrate. `--allow-recalibrate` 명시 강제 (circular threshold chasing 차단).

**Preflight hard gate** (`_preflight_calibration_source`, C-H3 정합):
1. `len(records) == 5` (5 영상).
2. `len(axisMetrics) == 5` per doc (5 phases).
3. `shoulderTilt is not None` AND `hipTilt is not None`.
4. `'phase_8_1_wave_0_transitional' not in warnings` (Pod 재배포 완료 검증).
5. `'tilt_unavailable' not in warnings`.

위반 시 RuntimeError + 명시 doc_id.

**P100 + margin 산식** (C-H2 정합):
- `medium_cutoff_deg = max(P100 + margin_deg, ipsf_tolerance.tolerance_deg)`
- `high_cutoff_deg = max(medium_cutoff_deg * 1.5, ipsf_tolerance.major_fault_deg)`
- `margin_deg` default 5.0.
- `ipsf_tolerance.tolerance_deg` = 20.0 (NotebookLM citation 9 Aerial Pole CoP Page 63 S55 Iron X ±20°).
- `ipsf_tolerance.major_fault_deg` = 40.0.

### tilt_thresholds.yaml 실값 (옵션 A 산출)

| 항목 | shoulder | hip |
|---|---|---|
| sample_size | 25 | 25 |
| P25 | 17.12° | 22.01° |
| P50 | 24.84° | 27.73° |
| P75 | 31.24° | 36.25° |
| P90 | 42.36° | 44.36° |
| P100 (max) | **58.28°** | **49.62°** |
| medium_cutoff_deg | **63.28°** | **54.62°** |
| high_cutoff_deg | **94.92°** | **81.93°** |

**관찰**: 정은지 P100 자체 가 이미 IPSF tolerance (20°) 와 major_fault (40°) 를 초과 — 정은지 영상 일부 phase 가 큰 tilt (예: aerial 자세) 를 포함. medium = P100 + 5° = 63.28° / 54.62° 가 정은지 baseline 25/25 'low' 유지 보장.

**Note** — 본 cutoff 값 은 Wave 2 production sweep 의 sensitivity (정은지 25/25 'low' 유지 + 비숙련자 영상 'medium'/'high' 검출 비율) 으로 검증 필요. 정은지 P100 의 큰 값 자체 가 IPSF major_fault 와 충돌 의 가능성 — Wave 2 의 SWEEP-EVIDENCE §11 sensitivity 후 belle 가 margin_deg 또는 ipsf_tolerance 재평가 가능 (D-04 future re-run path).

### 신설 phase08_1 test (18 PASS)

| 파일 | tests | 검증 |
|---|---|---|
| `test_tilt_thresholds_yaml_drift.py` | 12 | schema_v2 정합 (top-level keys + schema_version + calibration_method + calibration_version + source metadata + shoulder/hip distribution + operational_cutoff + ipsf_tolerance + medium ≥ tolerance_deg + high ≥ medium). W11 version-tolerant. |
| `test_calibration_preflight.py` | 6 | C-H3 preflight 6 분기 (null shoulder/hip → RuntimeError + transitional warning → RuntimeError + tilt_unavailable → RuntimeError + clean 25 → PASS + 잘못된 doc count → RuntimeError) |

## Test Count Audit (실측)

| Folder | Actual | Wave 0 baseline | Diff |
|---|---|---|---|
| phase06 | 137 | 137 | 0 |
| phase07 | 108 | 108 | 0 |
| phase08 | 94 | 97 | -3 (Wave 0 stub assertion 3 함수 제거 — `phase08/test_compute_axis_deviation.py` cleanup) |
| phase08_1 | 63 | 20 | +43 (Wave 1 신설 46 + Wave 0 cleanup -3) |
| pipeline | 11 | 11 | 0 |
| **Total** | **413** | **373** | **+40** |

**Result: 413 PASS + 1 skipped** (success criteria 367 + N 정합 — Wave 0 + Wave 1 한 release boundary).

## Verification Gates

- [x] Task 0 precondition (옵션 A): Firestore reachable + 5 docs × 5 axisMetrics × 0 null × 0 transitional.
- [x] compute_axis_deviation 본체 = 실 tilt-only (Wave 0 stub 교체 완료).
- [x] AxisDeviationMetric 6 필드 (Wave 0 schema 정합).
- [x] tilt 값 unsigned [0, 90] (3D path arcsin + 2D path `_normalize_angle_undirected`).
- [x] `_severity_from_tilt` boundary-low + 1e-9 epsilon.
- [x] `_get_tilt_thresholds` schema_v2 강제 검증 (schema_version=2 + calibration_method=='elite_p100_plus_margin' + source.null_tilt_verified=true).
- [x] W10: module-global mutable flag 부재 — fallback 신호 cache tuple 3rd element 단일 source.
- [x] W9: helper cleanup 13 삭제 + 보존 4 + 신설 `_normalize_angle_undirected` + `_severity_from_tilt`.
- [x] tilt_thresholds.yaml 옵션 A 산출 정합 (schema_v2 + 25 sample + 5 doc_ids + null_tilt_verified=true).
- [x] grep: 8 distance helper + 2 distance constants 부재.
- [x] FPS-invariance 9 fps vs 18 fps 동일 tilt (test_fps_invariance_tilt_metric PASS).
- [x] Codex iteration 1-5 fix 모두 단일 release 박제 (C-H2 + C-H3 + C-M1 + C-MH1 + C-B1 carry-forward).
- [x] 회귀: phase06 + phase07 + phase08 + phase08_1 (Wave 0+1) + pipeline = 413 PASS + 1 skipped.
- [x] tsc --noEmit clean (TS 변경 0).
- [x] Wave 0 + Wave 1 commits 모두 origin/main push 완료 (6a294d5, 1cb3e6e, c3ec011, 30fd7d2, e08be9c, 80c9a6b).

## Stub Behavior Evidence

`compute_axis_deviation` Wave 1 본체 정상 동작 (factory-driven 합성 입력):

```
입력: 10 frames × pose_aligned (shoulder=15°, hip=10°), single 'hold' phase
출력: 1 AxisDeviationMetric
  metric.phase                   # 'hold'
  metric.shoulder_tilt           # ≈ 15.0 (±2 tolerance, np.median 정합)
  metric.hip_tilt                # ≈ 10.0
  metric.severity                # 'low' (15° < shoulder_cutoff=63.28°)
  metric.confidence              # 'medium' (high-reliability frame 100%)
  metric.warnings                # [] (yaml load 정상 시 base_warnings 빈 list)
```

`compute_axis_deviation` neither path 동작:

```
입력: 10 frames × keypoints_3d_pole_aligned={} AND keypoints_2d=None
출력: 1 AxisDeviationMetric
  metric.shoulder_tilt           # None
  metric.hip_tilt                # None
  metric.severity                # 'low'
  metric.confidence              # 'low'
  metric.warnings                # ['tilt_unavailable']
```

본 'tilt_unavailable' 신호 는 Phase 9 의 raw signal only guard 의 무시 조건 (D-05 정합).

## Threat Flags

None. 본 plan 의 변경 = analysis core 알고리즘 + judging_data yaml. 신규 network endpoint / auth path / file access pattern 부재.

## Known Stubs

None. Wave 0 의 `compute_axis_deviation` transitional stub 이 본 plan 에서 실 본체 로 교체 — 'phase_8_1_wave_0_transitional' warning 더 이상 emit X (정상 axisMetrics 시).

단, `compute_force_signals` umbrella 안 stub-detect 로직 (`'phase_8_1_wave_0_transitional' in m.warnings → 'axis_metric_transitional' top-level emit`) 은 legacy stub doc 박제 호환 보존 — Wave 2 (production sweep + Pod 재배포) 종료 후 stub-detect 로직 제거 plan 박제.

## Commits

| Hash | Task | Description |
|---|---|---|
| `30fd7d2` | 1 | compute_axis_deviation tilt-only + yaml lazy loader + helper cleanup (W9 audit) + module-global flag 부재 (W10) |
| `e08be9c` | 2a | calibrate_tilt_thresholds.py + tilt_thresholds.yaml (schema_v2, elite_p100_plus_margin) |
| `80c9a6b` | 2b | yaml drift v2 (12) + calibration preflight (6) |

3 commits all pushed to `origin/main` per [[gsd-pod-work-push-first]] (Wave 0 + Wave 1 한 release boundary).

## Deviations from Plan

None — plan executed exactly as written (Codex iteration 1-5 fix 모두 plan 본문 에 병합 후 진입).

## Self-Check: PASSED

Verified files exist:
- `backend/judging_data/tilt_thresholds.yaml` — FOUND
- `backend/scripts/calibrate_tilt_thresholds.py` — FOUND
- `backend/tests/phase08_1/fixtures/__init__.py` — FOUND
- `backend/tests/phase08_1/fixtures/_factory.py` — FOUND
- `backend/tests/phase08_1/test_compute_axis_tilt_only.py` — FOUND
- `backend/tests/phase08_1/test_axis_2d_angle_normalization.py` — FOUND
- `backend/tests/phase08_1/test_severity_boundary_value_is_low.py` — FOUND
- `backend/tests/phase08_1/test_severity_above_medium.py` — FOUND
- `backend/tests/phase08_1/test_severity_above_high.py` — FOUND
- `backend/tests/phase08_1/test_tilt_thresholds_loader.py` — FOUND
- `backend/tests/phase08_1/test_tilt_thresholds_yaml_drift.py` — FOUND
- `backend/tests/phase08_1/test_calibration_preflight.py` — FOUND

Verified commits exist on main:
- `30fd7d2` — FOUND (Task 1)
- `e08be9c` — FOUND (Task 2a)
- `80c9a6b` — FOUND (Task 2b)

Verified verification gates:
- AxisDeviationMetric fields = `{phase, shoulder_tilt, hip_tilt, severity, confidence, warnings}` (6 필드 정확)
- grep distance helpers + constants 부재 (force_signals.py)
- yaml schema_v2 + calibration_method='elite_p100_plus_margin' + source.null_tilt_verified=true (python -c assert PASS)
- tsc --noEmit clean
- 413 PASS + 1 skipped (phase06 137 + phase07 108 + phase08 94 + phase08_1 63 + pipeline 11)

## Next Phase Readiness

**Wave 2 진입 차단 해소** — schema_v2 `tilt_thresholds.yaml` 존재 + calibration_method='elite_p100_plus_margin' + source.null_tilt_verified=true 확인 완료. Wave 2 plan 작업:
- pipeline rewire (compute_axis_deviation caller 정합 audit — 본 plan 의 시그너처 변경 0이므로 caller 영향 0 expected).
- Pod 재배포 (RunPod uvicorn restart — Wave 1 본체 활성).
- 정은지 5영상 재sweep + SWEEP-EVIDENCE §0~§11 박제 (severity='low' 25/25 + sensitivity 검증).
- ROADMAP Phase 8.1 SC #1-5 evidence 박제.

**Phase 9 평행 진입 OK** — raw `shoulder_tilt` / `hip_tilt` / `confidence` / `warnings` consumer guard (D-05 Codex C-M4 정합). severity 직접 trust 금지 (Phase 8.1 SWEEP-EVIDENCE §11 sensitivity 통과 후만).

**Future calibration re-run path (D-04)**:
```bash
# belle 정은지 추가 영상 확보 시:
FIREBASE_SA_PATH=... PYTHONPATH=... python backend/scripts/calibrate_tilt_thresholds.py \
  --sweep-uid <new_sweep_uid> --source-type firestore
git add backend/judging_data/tilt_thresholds.yaml
git commit -m "chore(08.1): re-calibrate tilt thresholds — 정은지 +N영상"
git push
```
zero-code-change (value-only) per W11.

---

*Phase: 08.1-axis-metric-redesign*
*Plan: 01 (Wave 1)*
*Completed: 2026-06-09*
