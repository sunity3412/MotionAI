---
phase: 08.1-axis-metric-redesign
verified: 2026-06-09T13:30:00Z
status: passed
score: 5/5 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: null
  previous_score: null
  gaps_closed: []
  gaps_remaining: []
  regressions: []
---

# Phase 8.1: axis-metric-redesign Verification Report

**Phase Goal**: Phase 8 의 axis distance metric 이 도메인 정합성을 위반 (정은지 5/5 영상 모든 phase severity='high' 출력) — 좌표계 origin 미정의 + threshold 단위 mismatch. IPSF Code of Points 의 axis 채점 항목 + 폴스포츠 도메인 "축 이탈" 정의 research 위에서 axis metric 을 재설계. 정은지 baseline 이 `severity='low'` 로 출력되도록 도메인 정합 metric + threshold calibration.

**Verified**: 2026-06-09T13:30:00Z
**Status**: passed
**Re-verification**: No — initial verification

---

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | 정은지 5/5 reference 영상 axis severity 가 `low` 으로 출력 (현재 5/5 `high` → fix 후 5/5 `low`) | VERIFIED | `sweep_evidence_dump.json` 직접 검증 — `grep -c '"severity": "low"'` = 25, `"high"` = 0, `"medium"` = 0. 8조건 measured-low gate 8/8 PASS (SWEEP-EVIDENCE.md §3 + Summary). sweep_uid `sweep_phase8_1_1781009003567`. |
| 2 | 좌표계 origin 정의 또는 metric 자체 변경으로 axis distance 가 카메라 거리/줌/dancer 화면 위치에 invariant | VERIFIED | (a) Schema-level: `AxisDeviationMetric` (force_signals.py:356-397) = 6 필드 only (distance 5 필드 hard break), TS interface (analysis.ts:541-550) mirror. (b) Code-level: `_shoulder_tilt_pole_aligned` = `arcsin(\|Δz\|/\|\|Δ\|\|)` (rotation-only), `_normalize_angle_undirected` (force_signals.py:987-999) for 2D fallback (translation/scale invariant). (c) Empirical: 5 영상 framing 다양함에도 mean shoulder 25.87° / hip 28.21° 안정. SWEEP-EVIDENCE §6. |
| 3 | IPSF Code of Points 의 axis 채점 항목 (deduction category) 과 metric severity 가 mapping 박제 | VERIFIED | `tilt_thresholds.yaml` `ipsf_tolerance.source_ref = "NotebookLM citation 9 Page 87 Glossary + Aerial Pole CoP Page 63 S55 Iron X (±20°)"` + `tolerance_deg: 20.0` + `major_fault_deg: 40.0` (line 36-40). `docs/contract.md §9.3` 박제 메모 "Phase 8.1 박제 (2026-06-09): distance 차원 hard break (IPSF Code of Points 글로벌 distance 항목 부재, NotebookLM citation 9)" (line 843-844). SWEEP-EVIDENCE §7. |
| 4 | threshold 값 + 도출 근거 가 plan 산출물에 기록 | VERIFIED | `backend/judging_data/tilt_thresholds.yaml` schema_v2 + calibration_method='elite_p100_plus_margin' + calibration_version='1.1' + calibrated_at + source.{sweep_uid='sweep_phase8_1780986673', sample_size=25, source_doc_ids[5], null_tilt_verified=true, margin_deg=5.0} + shoulder/hip distribution (P25/P50/P75/P90/P100) + operational_cutoff (shoulder medium=63.28°/high=94.92°, hip medium=54.62°/high=81.93°). `backend/scripts/calibrate_tilt_thresholds.py` (P100+margin + preflight hard gate). SWEEP-EVIDENCE §8. |
| 5 | Phase 8.1 종료 시 sweep 재실행 + 정은지 분포 확인 + 분포 변경 evidence 박제 | VERIFIED | sweep `sweep_phase8_1_1781009003567` 재실행 완료, 5 Firestore docs + 25 axisMetric instance 산출. SWEEP-EVIDENCE §9 분포 변경 표: Phase 8 25/25 'high' → Phase 8.1 25/25 'low' (delta -25 high / +25 low). 출처: Phase 8 = `08-03-SUMMARY.md` (sweep_phase8_1780986673 25/25 high 박제 — grep 확인), Phase 8.1 = `sweep_evidence_dump.json`. |

**Score**: **5/5 truths verified**

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `backend/shared/python/sunity_shared/analysis/force_signals.py` | AxisDeviationMetric 6 필드 + compute_axis_deviation tilt-only + _severity_from_tilt boundary-low + _normalize_angle_undirected + _get_tilt_thresholds | VERIFIED | dataclass at L356-397 (6 필드: phase, shoulder_tilt, hip_tilt, severity, confidence, warnings). `compute_axis_deviation` at L1067-1156 (pole_aligned 3D path 우선 + image_2d fallback + neither → tilt_unavailable). `_severity_from_tilt` at L1035-1054 (boundary-low + 1e-9 epsilon). `_normalize_angle_undirected` at L987-999. `_get_tilt_thresholds` at L255-308 (schema_v2 lazy load + cache tuple 3rd element fallback signal). |
| `app/src/types/analysis.ts` | AxisDeviationMetric TS interface 6 필드 (3-way lockstep) | VERIFIED | L541-550 mirror — phase, shoulderTilt: number\|null, hipTilt: number\|null, severity, confidence, warnings. JSDoc + naming caveat 박제. tsc --noEmit clean. |
| `docs/contract.md §9.3` | tilt-only metric 메모 + IPSF citation 9 | VERIFIED | L841-859 표 6 행 + 박제 메모. Footer log entry L995-996 (Plan 08.1-00 §9.3 변경 박제). |
| `backend/judging_data/tilt_thresholds.yaml` | schema_v2 + calibration metadata + distribution + operational_cutoff + ipsf_tolerance | VERIFIED | 41 lines, schema_version=2, calibration_method='elite_p100_plus_margin', source.sample_size=25, source.null_tilt_verified=true, source.source_doc_ids[5], ipsf_tolerance.source_ref + tolerance_deg=20 + major_fault_deg=40. |
| `backend/scripts/calibrate_tilt_thresholds.py` | --source-type 3분기 + preflight hard gate + P100+margin 산식 | VERIFIED | File exists. Source 분기 (firestore/repo-artifact/wave2-explicit). C-H3 preflight 5 checks. |
| `backend/scripts/sweep_phase8_1.py` | C-M5 hygiene + production fail-fast (no --allow-fallback) | VERIFIED | File exists. `_yaml_metadata` 강제 검증 (schema_version=2 + method='elite_p100_plus_margin'). production sweep at commit 9aa0bc7 used elite_p100_plus_margin (sweep_evidence_dump.json line 8 confirms). |
| `backend/scripts/synthetic_sensitivity_check.py` | 5 case sensitivity gate (positive + negative + boundary) | VERIFIED | File exists. 실행 결과: 5/5 PASS (boundary low / just above medium / just below medium / above high / hip high). C-M3 metric validity 확보. |
| `.planning/phases/08.1-axis-metric-redesign/08.1-SWEEP-EVIDENCE.md` | 12 sections (§0~§11) ROADMAP SC #1-5 backward coverage | VERIFIED | 345 lines, §0 stub gate + §1~§11 모두 박제. §10 verifier 진입 준비 5 SC → PASS. §11 sensitivity 5/5 PASS. |
| `.planning/phases/08.1-axis-metric-redesign/sweep_evidence_dump.json` | Raw Firestore 5 docs × 5 axisMetric | VERIFIED | grep counts 직접 검증: severity=low 25, severity=high 0, severity=medium 0, phase_8_1_wave_0_transitional 0, tilt_unavailable 0, tilt_thresholds_fallback 0, axis_metric_transitional 0. |
| `backend/tests/phase08_1/` | Phase 8.1 신설 18 test files (Wave 0+1+2) | VERIFIED | 18 .py files (incl. fixtures + conftest). 회귀 419 PASS + 1 skipped (phase06 137 + phase07 108 + phase08 94 + phase08_1 69 + pipeline 11). |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `compute_axis_deviation` (force_signals.py) | `_get_tilt_thresholds` (cache) | direct call L1100 | WIRED | Wave 1 본체 호출 정합. shoulder/hip cutoff 동적 load. |
| `compute_axis_deviation` | `_severity_from_tilt` | L1135-1136 | WIRED | boundary-low rule 적용 — 정은지 baseline 25/25 'low' invariant 보장. |
| `compute_axis_deviation` | `_normalize_angle_undirected` | via `_shoulder_tilt_2d`/`_hip_tilt_2d` (L1016, 1032) | WIRED | C-M1 정합 — 2D fallback path 의 unsigned [0, 90] 강제. |
| `compute_force_signals` (umbrella) | `axis_metric_transitional` top-level warning | L1741-1744 (C-B1 fix) | WIRED | Wave 0 stub 잔존 검출 시 emit. 본 sweep 0/25 검출 (C-H1 downstream guard PASS). |
| `sweep_phase8_1.py` `_yaml_metadata` | tilt_thresholds.yaml schema_v2 강제 검증 | line 78-88 | WIRED | production sweep `--allow-fallback` 미사용 강제. sweep dump의 `thresholdCalibrationMethod='elite_p100_plus_margin'` 으로 검증. |
| `calibrate_tilt_thresholds.py` `_preflight_calibration_source` | C-H3 5-check preflight | source code | WIRED | null tilt + transitional warning + tilt_unavailable + 5 docs × 5 axisMetric 강제. |
| `Phase 9` → `axisMetrics raw signal only guard` | D-05 ROADMAP entry | ROADMAP L283 | WIRED | "Phase 9 가 `forceSignalsReport.axisMetrics[*].severity` 직접 trust 금지" — ROADMAP 박제 완료. SWEEP-EVIDENCE §11 sensitivity 통과 후 severity 사용 OK. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|-------------------|--------|
| `compute_axis_deviation` | `shoulder_tilt_val`, `hip_tilt_val` | `pole_aligned` 3D path 우선 (`_shoulder_tilt_pole_aligned`) → `image_2d` fallback (`_shoulder_tilt_2d`) | Yes — production sweep `sweep_phase8_1_1781009003567` 25/25 non-null tilt (shoulder min=10.74°, max=52.70°, mean=25.87° / hip min=10.37°, max=49.62°, mean=28.21°) | FLOWING |
| `_get_tilt_thresholds` | shoulder/hip cutoff tuple | `backend/judging_data/tilt_thresholds.yaml` (lazy load + cache) | Yes — yaml present + schema_v2 + calibration_method valid. sweep dump의 `thresholdChecksum=c94bb8c7...` 일치. | FLOWING |
| `sweep_phase8_1.py` | sweep results | Pod xbdkj1g2ylnfwi `_process` (real Lambda pipeline) | Yes — exit 0 + 5 Firestore docs + 25 instances + sweep_temp/ cleanup 5/5 | FLOWING |
| `synthetic_sensitivity_check.py` | severity outputs | `_severity_from_tilt` direct call | Yes — 5/5 cases match expected (boundary-low + above medium + above high + hip high). | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full regression suite passes | `PYTHONPATH=backend/shared/python python3 -m pytest backend/tests/phase06 backend/tests/phase07 backend/tests/phase08 backend/tests/phase08_1 backend/tests/pipeline -q` | 419 passed, 1 skipped in 2.11s | PASS |
| TypeScript clean | `npm run typecheck` (app/) | tsc --noEmit exit 0 (no output) | PASS |
| SAM template valid | `sam validate` (backend/) | "backend/template.yaml is a valid SAM Template" | PASS |
| Synthetic sensitivity 5/5 | `PYTHONPATH=backend/shared/python python3 backend/scripts/synthetic_sensitivity_check.py` | PASS — 5/5 cases matched expected severity (exit 0) | PASS |
| Sweep dump severity 분포 | `grep -c '"severity": "low"\|"high"\|"medium"' sweep_evidence_dump.json` | low=25, high=0, medium=0 | PASS |
| Sweep dump warnings 청결 | `grep -c phase_8_1_wave_0_transitional\|tilt_unavailable\|tilt_thresholds_fallback\|axis_metric_transitional sweep_evidence_dump.json` | 0, 0, 0, 0 | PASS |
| `--allow-fallback` 미사용 verification | sweep dump `thresholdCalibrationMethod` 값 | "elite_p100_plus_margin" (not "fallback") | PASS |

### Probe Execution

| Probe | Command | Result | Status |
|-------|---------|--------|--------|
| No probe-*.sh declared | — | N/A — Phase 8.1 sweep flow is the probe equivalent (already executed) | SKIPPED |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| FORCE-01 | 08.1-01-PLAN, 08.1-02-PLAN | axis 차원 의미 부여 — Phase 8 와 공유 | SATISFIED | tilt-only metric 도메인 정합 박제 (RESEARCH §4 α-4 결정, IPSF NotebookLM citation 9). 25/25 정은지 'low' invariant + 5/5 sensitivity 양방향 검출. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `backend/shared/python/sunity_shared/analysis/force_signals.py` | 1745 ("phase_8_1_wave_0_transitional" detection logic) | Legacy stub-detect 로직 보존 | Info | 의도된 backwards-compat (legacy Wave 0 stub doc 호환). 본 sweep 0/25 검출 — 향후 cleanup plan 박제로 제거 가능. SUMMARY-02 "Known Stubs" 섹션 명시. |
| `tilt_thresholds.yaml` source.sweep_uid | `sweep_phase8_1780986673` (Phase 8 inherited) | calibration source = Phase 8 sweep (자체 sweep 아님) | Info | D-02 의도 정합 — circular threshold chasing 차단 (Wave 1 이 자기 sweep 재calibrate 금지). SUMMARY-01 박제. |

No blockers. No warnings. Both findings are explicitly intended per Plan + ROADMAP design.

### Human Verification Required

None. All ROADMAP SC #1-5 are programmatically verifiable via:
- Codebase file existence + grep (artifact lockstep)
- Sweep evidence dump (Firestore 25/25 severity + null tilt + warnings 청결 grep)
- Behavioral spot-checks (full regression + sensitivity check + sam/tsc)
- Empirical sweep flow (sweep_phase8_1_1781009003567 actually executed against Pod, exit 0)

### Gaps Summary

None. Phase 8.1 의 5 ROADMAP Success Criteria 가 모두 codebase + 산출 evidence 로 backward coverage 박제 완료.

---

## Phase 8 vs Phase 8.1 비교 표 (Backward Evidence Chain)

| 영상 | Phase 8 severity (5 phases) | Phase 8.1 severity (5 phases) | Delta |
|------|------------------------------|--------------------------------|-------|
| ref-invert | high × 5 (sweep_phase8_1780986673) | low × 5 (sweep_phase8_1_1781009003567) | high → low |
| ref-climb | high × 5 | low × 5 | high → low |
| ref-foxtop | high × 5 | low × 5 | high → low |
| ref-foxtop-split | high × 5 | low × 5 | high → low |
| ref-sideway-spin | high × 5 | low × 5 | high → low |

**출처**:
- Phase 8: `08-jerk-jitter/08-03-SUMMARY.md` line 239 "5/5 영상 모든 phase axis severity='high' 출력 — 도메인적으로 잘못된 결과"
- Phase 8.1: `sweep_evidence_dump.json` (grep 검증: severity=low 25, high=0)

**Aggregate delta**: -25 high (-100%) / +25 low (+100%).

**분포 변경 origin**:
1. Wave 0 D-01 — distance 차원 5 필드 hard break (`AxisDeviationMetric` 6 필드 atomic) — false-positive signal source 영구 차단.
2. Wave 1 tilt-only path — `_shoulder_tilt_pole_aligned`/`_hip_tilt_pole_aligned` = `arcsin(|Δz|/||Δ||)` rotation-only + `_normalize_angle_undirected` unsigned [0, 90] 강제 — origin-invariant.
3. Wave 1 calibration — `tilt_thresholds.yaml` schema_v2 P100+margin 5° + IPSF tolerance (20°) / major_fault (40°) floor — 정은지 elite baseline 25/25 'low' invariant 보장.

---

## Codex 5 Iteration Review 핵심 Fix 박제 검증

| Fix | Source code 박제 | Status |
|-----|------------------|--------|
| C-B1 (Wave 0) — `coordinate_space == 'unavailable'` 참조 제거 + `axis_metric_transitional` 검출 logic | force_signals.py compute_force_signals 본체 (Wave 0 plan SUMMARY 박제, sweep dump 0/25 axis_metric_transitional emit) | VERIFIED |
| C-H1 (Wave 0) — Wave 0 단독 production 진입 금지 + downstream guard | Wave 0+1 한 release boundary (SWEEP-EVIDENCE §0 stub gate PASS = 0/25 transitional) | VERIFIED |
| C-MH1 (Wave 0) — Naming caveat docstring | force_signals.py:356-397 dataclass docstring + analysis.ts:541-550 JSDoc | VERIFIED |
| C-H2 (Wave 1) — P100 + margin operational cutoff (P90 폐기) + boundary-low + 1e-9 epsilon | tilt_thresholds.yaml `calibration_method: 'elite_p100_plus_margin'` + `_severity_from_tilt` L1050-1054 `tilt_deg > cutoff + 1e-9` | VERIFIED |
| C-H3 (Wave 1) — Calibration preflight hard gate (null tilt + transitional warning + tilt_unavailable + 5 docs × 5 axisMetric) | `_preflight_calibration_source` in `calibrate_tilt_thresholds.py` (5 checks) + `test_calibration_preflight.py` 6 분기 PASS | VERIFIED |
| C-M1 (Wave 1) — `_normalize_angle_undirected` (modulo 180° + min(a, 180-a) → unsigned [0, 90]) | force_signals.py:987-999 + applied at L1016, L1032. `test_axis_2d_angle_normalization.py` 6 분기 PASS | VERIFIED |
| C-M3 (Wave 2) — Synthetic sensitivity check (negative evidence) | `backend/scripts/synthetic_sensitivity_check.py` 5/5 PASS (재실행 검증) + `test_sensitivity_synthetic.py` 3 PASS | VERIFIED |
| C-M4 (Wave 2 / Phase 9 guard) — Raw signal only guard | ROADMAP line 283 Phase 9 entry 박제 — "raw `shoulder_tilt` + `hip_tilt` + `confidence` + `warnings` 만 사용" | VERIFIED |
| C-M5 (Wave 2) — sweep script hygiene (epoch ms / commit hash / yaml sha256 / S3 cleanup / exit 1 on failures) | `sweep_phase8_1.py` + sweep_evidence_dump.json `sweepCommitHash=9aa0bc78... thresholdChecksum=c94bb8c7...` | VERIFIED |
| Production sweep `--allow-fallback` 미사용 (Codex final) | sweep dump `thresholdCalibrationMethod='elite_p100_plus_margin'` (not 'fallback') | VERIFIED |
| schema_v2 yaml + null_tilt_verified=true | tilt_thresholds.yaml line 1-2 + 14 | VERIFIED |
| 회귀 0 — phase06 137 + phase07 108 + phase08 94 + phase08_1 69 + pipeline 11 = 419 PASS + 1 skipped | Pytest 재실행 — 직접 검증 (419 passed, 1 skipped in 2.11s) | VERIFIED |
| tsc --noEmit clean | npm run typecheck — exit 0 직접 검증 | VERIFIED |
| sam validate exit 0 | sam validate — "valid SAM Template" 직접 검증 | VERIFIED |

**Result**: 14/14 Codex review fixes 박제 codebase 박제 확인.

---

## 11 Commits (Phase 8.1 Wave 0+1+2) 박제 검증

git log direct read:

| # | Hash | Wave | Description |
|---|------|------|-------------|
| 1 | `6a294d5` | 0 | feat: AxisDeviationMetric distance hard break (3-way lockstep) + transitional stub + Phase 8 test 수술 (D-01) |
| 2 | `1cb3e6e` | 0 | test: firestore scoped validator backwards-compat + C-B1 grep guard + Wave 0 회귀 gate |
| 3 | `c3ec011` | 0 | docs: close-out Wave 0 (SUMMARY + STATE + ROADMAP) |
| 4 | `30fd7d2` | 1 | feat: compute_axis_deviation tilt-only + yaml lazy loader + helper cleanup (W9 audit) + module-global flag 부재 (W10) |
| 5 | `e08be9c` | 1 | feat: calibrate_tilt_thresholds.py + tilt_thresholds.yaml (schema_v2, elite_p100_plus_margin) |
| 6 | `80c9a6b` | 1 | test: yaml drift v2 (12) + calibration preflight (6) |
| 7 | `c88432f` | 1 | docs: close-out Wave 1 (SUMMARY + STATE + ROADMAP) |
| 8 | `5233018` | 2 | feat: backend/scripts/sweep_phase8_1.py (Phase 8 임시 스크립트 → repo 박제 정제 버전) |
| 9 | `9aa0bc7` | 2 | test: pipeline mock E2E (3) + synthetic sensitivity check (5 case + 3 test) |
| 10 | `1fe731c` | 2 | docs: SWEEP-EVIDENCE 박제 — 정은지 5/5 'low' (25/25 axisMetric) + 8조건 measured-low gate PASS |
| 11 | `a50a932` | 2 | docs: close-out Wave 2 (SUMMARY + STATE + ROADMAP + REQUIREMENTS) |

11/11 commits present on `origin/main` per [[gsd-pod-work-push-first]]. sweep_uid `sweep_phase8_1_1781009003567` matches sweepCommitHash `9aa0bc78cc4a2a8dadf962224155db2675feef74` (HEAD at sweep).

---

## Phase 9 진입 Ready 신호

Phase 9 (ForceDirectionPattern + 실패 원인 후보 3개) 평행 진입 OK 조건 박제 확인:

1. **ROADMAP entry line 283** — D-05 raw signal only guard 명시:
   > Phase 9 가 `forceSignalsReport.axisMetrics[*].severity` 직접 trust 금지. raw `shoulder_tilt` + `hip_tilt` + `confidence` + `warnings` 만 사용. severity 는 warnings 가 `axis_metric_transitional` 또는 `tilt_unavailable` 또는 `tilt_thresholds_fallback` 포함 시 무시. Phase 8.1 SWEEP-EVIDENCE §11 sensitivity 통과 후 severity 사용 OK.

2. **SWEEP-EVIDENCE §11 sensitivity 5/5 PASS** — severity 사용 차단 해소.
3. **본 sweep 25/25 instances `warnings: []`** — Phase 9 의 severity 무시 조건 (3 warning) 모두 부재.
4. **Phase 8.1 verifier PASS** — 8/8 measured-low gate + 5/5 ROADMAP SC.

Phase 9 plan 작성 시 acceptance 에 raw signal only guard 명시 강제 (Phase 8.1 D-05 carryover).

---

## Verification Conclusion

**Status**: passed
**Score**: 5/5 ROADMAP Success Criteria verified

Phase 8.1 (axis-metric-redesign) 완수. 정은지 5/5 영상 25/25 'high' → 25/25 'low' 분포 전환 + 좌표계 origin invariant + IPSF Code of Points mapping + threshold calibration plan 박제 + sweep 재실행 evidence 모두 codebase 박제 backward coverage.

도메인 정합성 (분석 정확도 최우선 — [[feedback-analysis-first]]) 회복 + analysis-objectivity 정합 ([[analysis-objectivity-no-human-scores]] — 정은지 점수 라벨링 없이 threshold 수치 calibration 만). Phase 9 진입 ready.

---

*Verified: 2026-06-09T13:30:00Z*
*Verifier: Claude (gsd-verifier)*
*Phase: 08.1-axis-metric-redesign (3 plans, 11 commits, Wave 0+1+2 close-out)*
