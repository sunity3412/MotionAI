---
plan_id: 09-01
phase: 09-forcedirectionpattern-3
plan: 01
wave: 0
type: execute
title: "Wave 0 — ForcePatternFinding/ForcePatternInference 3-way schema lockstep"
requirements: [FORCE-01, FEED-02]
mvp_slice: "Backend schema + Firestore validator + TS interface + docs §9.11 — Wave 1 inference 가 Firestore 까지 흘러갈 수 있는 vertical 골격 확보"
depends_on: []
files_modified:
  - app/src/types/analysis.ts
  - app/src/lib/userAnalyses.ts
  - backend/shared/python/sunity_shared/analysis/force_pattern.py
  - backend/shared/python/sunity_shared/firestore_admin.py
  - docs/contract.md
  - backend/tests/phase09/__init__.py
  - backend/tests/phase09/conftest.py
  - backend/tests/phase09/test_force_pattern_lockstep.py
  - backend/tests/phase09/test_dataclass_to_camel_case_dict_phase9.py
  - backend/tests/phase09/test_firestore_lockstep_phase9.py
  - backend/tests/phase09/test_force_pattern_dataclass.py
autonomous: true
mvp_mode: true
must_haves:
  truths:
    - "TS `ForcePatternFinding` + `ForcePatternInference` interface + `AnalysisResult.forcePatternInference?: ForcePatternInference | null` 신설 (per D-09-D1 / D-09-U1)."
    - "Python `force_pattern.py` 가 frozen dataclass + `__post_init__` validator (enum + confidence [0,1] + interpretation non-empty) 정합 (per D-09-D1 / D-09-U3)."
    - "`docs/contract.md §9.11 ForcePatternInference` 섹션 신설 — TS / Python 과 필드 표 1:1 (per D-09-U1)."
    - "`_validate_force_pattern_inference` scoped validator 박제 (nested-array 차단 + findings list[dict-of-scalars] + warnings list[str] 화이트리스트) — 본 wave 시점은 stub 호출 X, Wave 1 wiring 에 대비한 시그니처만 제공 (per D-09-U5)."
    - "`userAnalyses.ts::normalize` 가 `forcePatternInference ?? null` + findings 내부 `warnings ?? []` / `jointHint ?? null` 박제 — Phase 8 패턴 mirror (per RESEARCH Pitfall 8)."
    - "phase09 pytest harness (`__init__.py` + `conftest.py` + 4 unit test) 박제 + `pytest tests/phase09/ -x -q` 4 test green."
    - "Wave 0 commit = SINGLE atomic commit covering all 11 files (per D-09-U1)."
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/force_pattern.py"
      provides: "ForceDirectionPattern / ForceSourceSignal / ModeContext Literal aliases + frozen dataclass `ForcePatternFinding` + `ForcePatternInference` + `__post_init__` validator + `_PHASE_PRIORITY` / `_SIGNAL_PRIORITY` / `_SIGNAL_WEIGHT` / `_CONFIDENCE_TO_FACTOR` 상수 (Wave 1 가 본체 함수 추가)."
      contains: "class ForcePatternFinding"
    - path: "backend/shared/python/sunity_shared/firestore_admin.py"
      provides: "`_validate_force_pattern_inference` + `_validate_force_pattern_finding_dict` + `complete_analysis(..., force_pattern_inference=None)` kwarg 확장 + `result.forcePatternInference` 저장 path."
      contains: "_validate_force_pattern_inference"
    - path: "app/src/types/analysis.ts"
      provides: "`ForcePatternFinding` + `ForcePatternInference` interface + `AnalysisResult.forcePatternInference` optional field."
      contains: "ForcePatternInference"
    - path: "app/src/lib/userAnalyses.ts"
      provides: "normalize() 가 forcePatternInference null-guard + findings 내부 warnings/jointHint default."
      contains: "forcePatternInference"
    - path: "docs/contract.md"
      provides: "§9.11 ForcePatternInference contract — TS / Python 필드 표 + Firestore 저장 위치 + Phase 9 vs Phase 11/12 책임 경계 1 줄 박제."
      contains: "§9.11 ForcePatternInference"
    - path: "backend/tests/phase09/__init__.py"
      provides: "empty package marker."
      min_lines: 0
    - path: "backend/tests/phase09/conftest.py"
      provides: "factory helpers `_make_axis_metric` / `_make_stability_metric` / `_make_contact_metric` / `_make_phase_boundary` / `_make_force_signals_report` (frozen dataclass 직접 instantiate — per RESEARCH Open Q 4)."
      contains: "_make_axis_metric"
    - path: "backend/tests/phase09/test_force_pattern_lockstep.py"
      provides: "3-way schema lockstep — TS Literal union ↔ Python frozenset ↔ docs §9.11 필드 표 (mirrors `backend/tests/phase08_1/test_axis_schema_lockstep.py`)."
      min_lines: 60
    - path: "backend/tests/phase09/test_dataclass_to_camel_case_dict_phase9.py"
      provides: "camelCase 변환 검증 — `source_signal`→`sourceSignal` / `joint_hint`→`jointHint` / `mode_context`→`modeContext`."
      min_lines: 40
    - path: "backend/tests/phase09/test_firestore_lockstep_phase9.py"
      provides: "`_validate_force_pattern_inference` PASS + reject cases: nested dict / list[list] / list[non-whitelist] / non-scalar warnings entry + R2 strict: non-str warnings entry ([1] / [True] / [None]) + empty-string warnings entry ([\"\"]), applied to both finding-level and top-level warnings."
      min_lines: 50
    - path: "backend/tests/phase09/test_force_pattern_dataclass.py"
      provides: "`ForcePatternFinding.__post_init__` validator (invalid pattern enum / invalid sourceSignal / confidence > 1 / confidence < 0 / empty interpretation) + `ForcePatternInference.__post_init__` validator (findings length > 3 / invalid modeContext)."
      min_lines: 50
  key_links:
    - from: "backend/shared/python/sunity_shared/analysis/force_pattern.py"
      to: "app/src/types/analysis.ts"
      via: "Literal alias <-> TS Literal union 동일 enum 박제"
      pattern: "ForceDirectionPattern|ForceSourceSignal|ModeContext"
    - from: "backend/shared/python/sunity_shared/firestore_admin.py"
      to: "backend/shared/python/sunity_shared/analysis/force_pattern.py"
      via: "complete_analysis(force_pattern_inference=...) kwarg + scoped validator"
      pattern: "_validate_force_pattern_inference"
    - from: "app/src/lib/userAnalyses.ts"
      to: "app/src/types/analysis.ts"
      via: "normalize() 가 ForcePatternInference 타입 import + null-guard"
      pattern: "forcePatternInference"
---

<objective>
Wave 0 = **3-way schema lockstep**. `ForcePatternFinding` + `ForcePatternInference` Python frozen dataclass (new module `force_pattern.py`) + TS interface (`analysis.ts`) + `docs/contract.md §9.11` 동시 신설 + Firestore scoped validator + frontend null-guard. `complete_analysis(..., force_pattern_inference=None)` 시그니처를 미리 확장해 Wave 1 의 wiring 이 schema drift 없이 흘러 들어갈 수 있게 한다. **모든 변경은 단일 atomic commit** (per D-09-U1).

만족 SC / 결정:

- FORCE-01 SC#1 contract surface, FEED-02 schema surface (canned 본문은 Wave 1).
- D-09-D1 (8+5 필드), D-09-D5 (UI hint 없음 — Phase 9 frontend 변경은 normalize null-guard 만), D-09-U1 (3-way lockstep), D-09-U3 (frozen dataclass validator), D-09-U4 (camelCase 변환), D-09-U5 (Firestore nested-array 금지).

본 plan 이 산출 X:

- `infer_force_direction_pattern` 본체 / 6 signal detector / 18 canned KO 본문 / pipeline `_process` 호출 — **Wave 1 (Plan 09-02)** 책임.
- RunPod 재배포 / 정은지 sweep / production 검증 — D-09-E2 영구 OUT.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<files_to_read>
@.planning/phases/09-forcedirectionpattern-3/09-CONTEXT.md
@.planning/phases/09-forcedirectionpattern-3/09-RESEARCH.md
@.planning/phases/09-forcedirectionpattern-3/09-VALIDATION.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@CLAUDE.md
@backend/shared/python/sunity_shared/analysis/force_signals.py
@backend/shared/python/sunity_shared/firestore_admin.py
@backend/shared/python/sunity_shared/models.py
@backend/shared/python/sunity_shared/analysis/copy_templates.py
@backend/tests/phase08/test_force_signals_lockstep.py
@backend/tests/phase08_1/test_axis_schema_lockstep.py
@backend/tests/phase08/test_firestore_lockstep.py
@backend/tests/phase08_1/test_firestore_axis_validator.py
@backend/tests/phase07/test_copy_templates_no_forbidden.py
@app/src/types/analysis.ts
@app/src/lib/userAnalyses.ts
@docs/contract.md
</files_to_read>

<files_modified>

| Path | Role | New? |
|------|------|------|
| `backend/shared/python/sunity_shared/analysis/force_pattern.py` | 신설 module — Literal aliases + frozen dataclass + 상수 (Wave 1 본체 함수의 placeholder import target) | NEW |
| `backend/shared/python/sunity_shared/firestore_admin.py` | `_validate_force_pattern_inference` + `_validate_force_pattern_finding_dict` 추가 + `complete_analysis` 시그니처에 `force_pattern_inference: dict \| None = None` kwarg + `result.forcePatternInference` 저장 분기 | MODIFY |
| `app/src/types/analysis.ts` | `ForcePatternFinding` + `ForcePatternInference` interface + `AnalysisResult.forcePatternInference?: ForcePatternInference \| null` | MODIFY |
| `app/src/lib/userAnalyses.ts` | normalize() 안 forcePatternInference null-guard + findings 내부 warnings/jointHint default | MODIFY |
| `docs/contract.md` | §9.11 ForcePatternInference 신설 (필드 표 + Firestore 저장 위치 + Phase 11/12 책임 경계) | MODIFY |
| `backend/tests/phase09/__init__.py` | empty package marker | NEW |
| `backend/tests/phase09/conftest.py` | factory helpers (`_make_axis_metric` / `_make_stability_metric` / `_make_contact_metric` / `_make_phase_boundary` / `_make_force_signals_report`) — Phase 8 force_signals frozen dataclass 직접 instantiate | NEW |
| `backend/tests/phase09/test_force_pattern_lockstep.py` | 3-way schema lockstep (TS Literal union ↔ Python frozenset ↔ docs §9.11) | NEW |
| `backend/tests/phase09/test_dataclass_to_camel_case_dict_phase9.py` | `_dataclass_to_camel_case_dict` 가 신설 3 필드 camelCase 변환 | NEW |
| `backend/tests/phase09/test_firestore_lockstep_phase9.py` | `_validate_force_pattern_inference` PASS + 4 reject case | NEW |
| `backend/tests/phase09/test_force_pattern_dataclass.py` | `__post_init__` validator 케이스 | NEW |

</files_modified>

<context>
@.planning/phases/09-forcedirectionpattern-3/09-CONTEXT.md
@.planning/phases/09-forcedirectionpattern-3/09-RESEARCH.md
@.planning/phases/09-forcedirectionpattern-3/09-VALIDATION.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@backend/shared/python/sunity_shared/analysis/force_signals.py
@backend/shared/python/sunity_shared/firestore_admin.py
@app/src/types/analysis.ts
@app/src/lib/userAnalyses.ts
@docs/contract.md
</context>

<tasks>

<task type="auto" id="09-01-T1" tdd="true">
  <name>Task 09-01-T1: phase09 pytest harness 박제 + 4 RED test 작성 (commit X)</name>
  <files>
    backend/tests/phase09/__init__.py,
    backend/tests/phase09/conftest.py,
    backend/tests/phase09/test_force_pattern_lockstep.py,
    backend/tests/phase09/test_dataclass_to_camel_case_dict_phase9.py,
    backend/tests/phase09/test_firestore_lockstep_phase9.py,
    backend/tests/phase09/test_force_pattern_dataclass.py
  </files>
  <depends_on>[]</depends_on>
  <threat_ref>T-09-V5 (input validation), T-09-T2 (nested-array tampering)</threat_ref>
  <behavior>
    - `_make_axis_metric(phase='lock', shoulder_tilt=10.0, hip_tilt=10.0, confidence='high', warnings=())` returns a frozen `force_signals.AxisDeviationMetric` instance (Phase 8 dataclass) via fixture factory — used by all Wave 0/Wave 1 tests.
    - test_force_pattern_lockstep — asserts (a) Python `_FORCE_PATTERNS` frozenset == `{"pull","push","brace","rotate","release","unknown"}` (b) `_FORCE_SOURCE_SIGNALS` frozenset == `{"axis_tilt","pelvis_drop","late_contact","high_jitter","high_jerk","abnormal_release"}` (c) `_MODE_CONTEXTS` frozenset == `{"mode1","mode3_first","mode3_progress"}` (d) `app/src/types/analysis.ts` file text contains each Literal token verbatim (regex per token) (e) `docs/contract.md` text contains `§9.11 ForcePatternInference` heading + each of 8 ForcePatternFinding 필드 (pattern / phase / sourceSignal / reason / interpretation / confidence / jointHint / warnings) + each of 5 ForcePatternInference 필드 (version / findings / overallConfidence / warnings / modeContext).
    - test_dataclass_to_camel_case_dict_phase9 — uses `pipeline.app._dataclass_to_camel_case_dict` on a stub `ForcePatternFinding(pattern='unknown', phase='lock', source_signal='high_jerk', reason='x', interpretation='y', confidence=0.5, joint_hint=None, warnings=[])` and `ForcePatternInference(version='1.0', findings=[…], overall_confidence='low', mode_context='mode1', warnings=[])` (R7 — kwargs in dataclass field order) and asserts output keys include `sourceSignal`, `jointHint`, `modeContext`, `overallConfidence` (no snake_case keys remain).
    - test_firestore_lockstep_phase9 — calls `firestore_admin._validate_force_pattern_inference` with (a) valid payload (PASS) (b) nested dict in finding entry — raises ValueError (c) list[list] in finding warnings — raises (d) list field outside `{"warnings"}` whitelist inside finding — raises (e) non-scalar warnings entry (e.g. `{"foo":1}` element) — raises (f) **non-str warnings entry (e.g. `[1]` / `[True]` / `[None]`) — raises** (R2 / R10 정합) (g) **empty-string warnings entry (e.g. `[""]`) — raises** (R2 / R10). Same 4 rejects (f-g) applied to **both** finding-level warnings and top-level `ForcePatternInference.warnings` (8 reject cases total across the two tiers). Also asserts top-level `findings` accepts list[dict].
    - test_force_pattern_dataclass — asserts:
      - `ForcePatternFinding(pattern='nonsense', …)` raises ValueError;
      - `source_signal='zzz'` raises;
      - `confidence=1.01` raises;
      - `confidence=-0.1` raises;
      - `interpretation=''` raises;
      - **(R6) `ForcePatternFinding(warnings=[1])` raises** — warnings element 가 str 아님;
      - **(R6) `ForcePatternFinding(warnings=[''])` raises** — warnings element 가 empty str;
      - **(R6) `ForcePatternFinding(joint_hint=123)` raises** — joint_hint 가 str | None 외 타입;
      - valid construction succeeds and dataclass is frozen (`dataclasses.FrozenInstanceError` on attribute set).
      - `ForcePatternInference(findings=[…] * 4)` raises (length cap 3);
      - `mode_context='other'` raises;
      - **(R6) `ForcePatternInference(version='')` raises** — version 가 empty str;
      - **(R6) `ForcePatternInference(findings=[object()])` raises** — finding element 가 `ForcePatternFinding` 인스턴스 아님;
      - **(R6) `ForcePatternInference(warnings=[1])` raises** — top-level warnings element 가 str 아님.
  </behavior>
  <action>
Write 6 files under `backend/tests/phase09/`. Pattern source (R12 정합 — actual paths verified):
- Umbrella schema lockstep: `backend/tests/phase08/test_force_signals_lockstep.py`
- Axis literal lockstep: `backend/tests/phase08_1/test_axis_schema_lockstep.py`
- Firestore scoped validator pattern: `backend/tests/phase08/test_firestore_lockstep.py` + `backend/tests/phase08_1/test_firestore_axis_validator.py`
- AST helper / forbidden grep: `backend/tests/phase07/test_copy_templates_no_forbidden.py`

`__init__.py` = empty file.

`conftest.py` exposes 5 factory functions returning frozen `force_signals.*` instances:

- `_make_phase_boundary(phase='lock', start_ms=0, end_ms=1000, confidence='high', source='pose')`
- `_make_axis_metric(phase='lock', shoulder_tilt=10.0, hip_tilt=10.0, confidence='high', warnings=())` — wraps `force_signals.AxisDeviationMetric` and supplies any other required fields (inspect Phase 8 dataclass signature; missing scalars default to safe values like `severity='low'` since Phase 8 dataclass still defines that attribute even though Phase 9 must not read it).
- `_make_stability_metric(phase='lock', jitter_score=0.0, jerk_score=0.0, confidence='high', warnings=(), unstable_body_parts=())`
- `_make_contact_metric(phase='lock', estimated_stable=True, near_pole_ratio=1.0, warnings=(), measurement_kind='keypoint', contact_point='left_hand')`
- `_make_force_signals_report(*, phase_boundaries=(...), axis_metrics=(...), stability_metrics=(...), contact_metrics=(...), warnings=())` — Phase 8 umbrella.

`test_force_pattern_lockstep.py` performs the 5 lockstep assertions in `<behavior>`. For (d) TS check: `Path('app/src/types/analysis.ts').read_text()` + per-token `re.search(rf"['\"]{token}['\"]", text)`. For (e) docs check: same Path read + substring match per field name and `§9.11`.

`test_dataclass_to_camel_case_dict_phase9.py` imports `pipeline.app._dataclass_to_camel_case_dict` (existing Phase 6/8 helper) and applies it to stub instances built directly (NOT through Wave 1 inference — those are Wave 1's job). Asserts no snake_case key remains and `findings` list elements have `sourceSignal` / `jointHint` keys.

`test_firestore_lockstep_phase9.py` imports `firestore_admin._validate_force_pattern_inference` and runs 5 cases (1 PASS + 4 reject). Build payloads as raw dicts (NOT dataclass).

`test_force_pattern_dataclass.py` imports `force_pattern.ForcePatternFinding` + `ForcePatternInference` and runs ValueError + frozen instance checks. Use `pytest.raises(ValueError, match=…)`.

**DO NOT COMMIT YET** — these tests will be RED until Tasks T2-T6 land. Final commit happens in T7.

Comments: cite `D-09-D1`, `D-09-U3`, `D-09-U4`, `D-09-U5` in test docstrings (Phase 8/7 박제 패턴 정합).
  </action>
  <verify>
    <automated>cd backend &amp;&amp; python -c "import ast; [ast.parse(open(p).read()) for p in ['tests/phase09/__init__.py','tests/phase09/conftest.py','tests/phase09/test_force_pattern_lockstep.py','tests/phase09/test_dataclass_to_camel_case_dict_phase9.py','tests/phase09/test_firestore_lockstep_phase9.py','tests/phase09/test_force_pattern_dataclass.py']]; print('AST OK')"</automated>
    <automated>cd backend &amp;&amp; pytest tests/phase09/ --collect-only -q | grep -E "test_(force_pattern_lockstep|dataclass_to_camel_case_dict_phase9|firestore_lockstep_phase9|force_pattern_dataclass)" | wc -l | grep -E "^\s*[1-9][0-9]*$"</automated>
  </verify>
  <done>4 test 파일 + conftest 박제. RED 상태 (force_pattern.py / docs §9.11 / validator 미박제). pytest collect 가 모든 test 를 인식.</done>
</task>

<task type="auto" id="09-01-T2" tdd="true">
  <name>Task 09-01-T2: 신설 force_pattern.py — Literal alias + frozen dataclass + 상수 박제</name>
  <files>backend/shared/python/sunity_shared/analysis/force_pattern.py</files>
  <depends_on>[09-01-T1]</depends_on>
  <threat_ref>T-09-V5 (input validation)</threat_ref>
  <behavior>
    - `from __future__ import annotations` 박제.
    - Literal aliases: `ForceDirectionPattern` (6 값), `ForceSourceSignal` (6 값), `ModeContext` (3 값).
    - Module-level frozensets `_FORCE_PATTERNS` / `_FORCE_SOURCE_SIGNALS` / `_MODE_CONTEXTS` 박제.
    - Module-level priority/weight 상수: `_PHASE_PRIORITY` (lock=0/hold=1/transition=2/final_shape=3/entry=4), `_SIGNAL_PRIORITY` (axis_tilt=0/pelvis_drop=0/abnormal_release=1/late_contact=1/high_jerk=2/high_jitter=2), `_SIGNAL_WEIGHT` (axis_tilt=1.0/pelvis_drop=1.0/late_contact=0.95/abnormal_release=1.1/high_jerk=0.85/high_jitter=0.80), `_BASE_CONFIDENCE` (axis_tilt=0.72/pelvis_drop=0.72/late_contact=0.70/high_jitter=0.63/high_jerk=0.63/abnormal_release=0.75), `_CONFIDENCE_TO_FACTOR` ({"low":0.3,"medium":0.7,"high":1.0}), `_MOTION_ID_BOOST = 1.05`, `_IPSF_TOLERANCE_DEG = 20.0` (per RESEARCH Open Q 2 fixed const).
    - `@dataclass(frozen=True) class ForcePatternFinding` with 8 fields per D-09-D1 + `__post_init__` validator (pattern enum / source_signal enum / confidence [0,1] / interpretation non-empty / phase = `force_signals.MotionPhase` Literal value).
    - `@dataclass(frozen=True) class ForcePatternInference` with 5 fields per D-09-D1 + `__post_init__` validator (overall_confidence ∈ MetricConfidence / mode_context ∈ ModeContext / 0 ≤ len(findings) ≤ 3 / warnings = list[str]).
    - `__all__` 명시 export.
    - **No inference functions in this commit** — `infer_force_direction_pattern` / `_detect_*` / `_rank_top3` 본체는 Plan 09-02.
  </behavior>
  <action>
Create `backend/shared/python/sunity_shared/analysis/force_pattern.py` with module docstring:

`"""ForceDirectionPattern 추론 layer (Phase 9 D-09-A1~A5 / B1~B5 / C1~C3 / D1~D6). Schema only — 본체 함수는 Wave 1 (Plan 09-02) 추가. force_signals.py (Phase 8/8.1 진단 신호) 와 분리된 신설 모듈 (D-08-C3 패턴 정합). Pure function + numpy only (Layer 2 영구 차단 — D-09-C1).

Phase 9 책임 경계:
  - 산출: 6 signal × 5 phase 후보 → Top-3 ForcePatternFinding 카드
  - 영역 X: 자연어 풍부화 (Phase 11) / 영상 위 오버레이 (Phase 12) / 부상 위험 (Phase 10)
"""`

Imports: `from dataclasses import dataclass, field`; `from typing import Literal`; `from .force_signals import MetricConfidence, MotionPhase`.

Literal aliases + frozensets + priority constants per `<behavior>`.

`ForcePatternFinding` 8 필드 = `pattern: ForceDirectionPattern`, `phase: MotionPhase`, `source_signal: ForceSourceSignal`, `reason: str`, `interpretation: str`, `confidence: float`, `joint_hint: str | None`, `warnings: list[str] = field(default_factory=list)`. `__post_init__` raises `ValueError` for each invalid case (cite `_FORCE_PATTERNS` / `_FORCE_SOURCE_SIGNALS` frozensets in error messages — Phase 8 박제 패턴 정합).

`ForcePatternInference` 5 필드 (**non-default → default 순서 — Python dataclass rule: a non-default field cannot follow a default field. Codex R1 회귀 가드**) = `version: str`, `findings: list[ForcePatternFinding]`, `overall_confidence: MetricConfidence`, `mode_context: ModeContext`, `warnings: list[str] = field(default_factory=list)`. `__post_init__` validates:
- `len(findings) <= 3`,
- `mode_context in _MODE_CONTEXTS`,
- `overall_confidence in {"low","medium","high"}`,
- `isinstance(version, str) and version != ""` (R7),
- `all(isinstance(f, ForcePatternFinding) for f in findings)` (R7 — internal misuse 차단),
- `all(isinstance(w, str) for w in warnings)` (R7 — warning code 만 허용).

Same `__post_init__` strictness on `ForcePatternFinding` (R7): `all(isinstance(w, str) for w in warnings)`, `pattern in _FORCE_PATTERNS`, `source_signal in _FORCE_SOURCE_SIGNALS`, `0 <= confidence <= 1`, `interpretation != ""`, `joint_hint is None or isinstance(joint_hint, str)`.

Cite D-09 IDs in inline comments (D-09-A5 base / D-09-B2 weight / D-09-B3 priority / D-09-D1 schema / RESEARCH Pitfall 4 confidence map / Open Q 2 IPSF const).

Hardcoding 금지: 모든 magic number 는 module-level 상수.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; python -c "from sunity_shared.analysis.force_pattern import ForcePatternFinding, ForcePatternInference, _FORCE_PATTERNS, _FORCE_SOURCE_SIGNALS, _MODE_CONTEXTS, _PHASE_PRIORITY, _SIGNAL_PRIORITY, _SIGNAL_WEIGHT, _BASE_CONFIDENCE, _CONFIDENCE_TO_FACTOR, _MOTION_ID_BOOST, _IPSF_TOLERANCE_DEG; assert _FORCE_PATTERNS == {'pull','push','brace','rotate','release','unknown'}; assert _IPSF_TOLERANCE_DEG == 20.0; assert _MOTION_ID_BOOST == 1.05; print('OK')"</automated>
    <automated>cd backend &amp;&amp; pytest tests/phase09/test_force_pattern_dataclass.py -x -q</automated>
  </verify>
  <done>force_pattern.py import 성공 + dataclass validator test green + Wave 1 본체 함수는 부재 (확인: `grep -c "def infer_force_direction_pattern" backend/shared/python/sunity_shared/analysis/force_pattern.py` returns 0).</done>
</task>

<task type="auto" id="09-01-T3">
  <name>Task 09-01-T3: TS interface 신설 + AnalysisResult 확장 (app/src/types/analysis.ts)</name>
  <files>app/src/types/analysis.ts</files>
  <depends_on>[09-01-T2]</depends_on>
  <threat_ref>T-09-V5 (input validation surface)</threat_ref>
  <action>
Edit `app/src/types/analysis.ts`. After the existing `ForceSignalsReport` interface block (around the §9 source area, RESEARCH cites lines 541-635), append:

```ts
// ── Phase 9 §9.11 ForcePatternInference (D-09-D1) ─────────────────────────
// Source contract: docs/contract.md §9.11 + backend force_pattern.py (frozen dataclass).
// Phase 9 = canned KO only; Phase 11 가 LLM 자연어 풍부화. Phase 12 가 raw 수치 노출.

export type ForceDirectionPattern =
  | 'pull' | 'push' | 'brace' | 'rotate' | 'release' | 'unknown';

export type ForceSourceSignal =
  | 'axis_tilt' | 'pelvis_drop' | 'late_contact'
  | 'high_jitter' | 'high_jerk' | 'abnormal_release';

export type ForcePatternModeContext = 'mode1' | 'mode3_first' | 'mode3_progress';

export interface ForcePatternFinding {
  pattern: ForceDirectionPattern;
  phase: MotionPhase;
  sourceSignal: ForceSourceSignal;
  reason: string;
  interpretation: string;
  confidence: number;
  jointHint: string | null;
  warnings: string[];
}

export interface ForcePatternInference {
  version: string;
  findings: ForcePatternFinding[];
  overallConfidence: MetricConfidence;
  modeContext: ForcePatternModeContext;
  warnings: string[];
}
```

Then extend `AnalysisResult` (the existing interface that already holds `forceSignalsReport`): add the optional field:

```ts
  forcePatternInference?: ForcePatternInference | null;
```

Place the field directly after `forceSignalsReport` 정렬 박제. Do NOT reorder existing fields.

If `MotionPhase` / `MetricConfidence` types are not yet exported, expose them via `export type` so backend lockstep test can confirm presence by name.

Cite `D-09-D1` and `docs/contract.md §9.11` in a top-of-block comment.
  </action>
  <verify>
    <automated>cd app &amp;&amp; npm run typecheck</automated>
    <automated>cd backend &amp;&amp; pytest tests/phase09/test_force_pattern_lockstep.py -x -q -k "ts or analysis_ts"</automated>
  </verify>
  <done>tsc --noEmit 0 error. Lockstep test 의 TS Literal token presence assert PASS.</done>
</task>

<task type="auto" id="09-01-T4">
  <name>Task 09-01-T4: docs/contract.md §9.11 ForcePatternInference 신설</name>
  <files>docs/contract.md</files>
  <depends_on>[09-01-T2]</depends_on>
  <threat_ref>—</threat_ref>
  <action>
Append a new section after `§9.10 Plan 08-00 contract dependency` (and before any Phase 9.5+ existing markers; current tail is `§9.10` + change log entries). Use existing §9.* heading style (verified Phase 8 pattern in lines 664-994).

Insert:

```
### §9.11 ForcePatternInference (Phase 9 신설)

본 섹션은 Phase 9 (`ForceDirectionPattern + 실패 원인 후보 3개`) 가 산출하는 추론 layer 의 contract.

#### §9.11.1 Enum

| Alias | Values |
|-------|--------|
| `ForceDirectionPattern` | `pull` / `push` / `brace` / `rotate` / `release` / `unknown` (`rotate` = v2 deferred, Phase 9 backend emit 0 — Open Q 1) |
| `ForceSourceSignal` | `axis_tilt` / `pelvis_drop` / `late_contact` / `high_jitter` / `high_jerk` / `abnormal_release` |
| `ModeContext` | `mode1` / `mode3_first` / `mode3_progress` |

#### §9.11.2 ForcePatternFinding (8 필드)

| Field (camelCase / snake_case) | Type | Notes |
|--------------------------------|------|-------|
| `pattern` | `ForceDirectionPattern` | D-09-A1 매핑 |
| `phase` | `MotionPhase` | §9.2 PhaseBoundary 정합 |
| `sourceSignal` / `source_signal` | `ForceSourceSignal` | D-09-A1 6 종 |
| `reason` | `string` (EN) | LLM input 용 1 sentence — D-09-D1 |
| `interpretation` | `string` (KO canned) | D-09-D2 18 canned mapping, "가능성" 언어 (D-09-D3 grep gate) |
| `confidence` | `number` ∈ [0, 1] | D-09-A5 산식 |
| `jointHint` / `joint_hint` | `string \| null` | 부위 키워드 (코어 / 고관절 / 광배 / 내전근 / null) — D-09-D1 / Assumption A3 |
| `warnings` | `string[]` | signal-specific (예: `axis_signal_unavailable`) — list[str] (Firestore scalar list OK) |

#### §9.11.3 ForcePatternInference (5 필드)

| Field | Type | Notes |
|-------|------|-------|
| `version` | `string` | "1.0" 초기 (non-empty) |
| `findings` | `ForcePatternFinding[]` | 길이 [0, 3] — D-09-B4 fabrication 금지 |
| `overallConfidence` / `overall_confidence` | `MetricConfidence` (`low`/`medium`/`high`) | 0 finding 시 `'low'` + warning |
| `modeContext` / `mode_context` | `ModeContext` | pipeline `_process` 가 산출 (D-09-D6) |
| `warnings` | `string[]` | umbrella (예: `no_significant_force_pattern_signal`, `phase_unavailable_for_inference`, `axis_signal_unavailable`) — Python dataclass field 순서상 마지막 (default_factory=list, R1 정합) |

#### §9.11.4 Firestore 저장 위치

- `users/{uid}/analyses/{analysisId}.result.forcePatternInference` — `_validate_force_pattern_inference` scoped validator 박제 ([[firestore-nested-array-flat]] 정합).
- `complete_analysis(..., force_pattern_inference=...)` kwarg 로 write (Wave 1 Plan 09-02 wiring).

#### §9.11.5 Phase 책임 경계

- Phase 9 = canned KO interpretation 단독 (Layer 2 / Gemini 영구 차단 — D-09-C1).
- Phase 11 (CoachCommentHook) = `findings[].interpretation` 위 LLM 자연어 풍부화 (Gemini 자연어 번역만).
- Phase 12 = `confidence` / raw 수치 (예: `shoulder_tilt 87°`) UI 노출 책임.
- Phase 9 자체는 UI hint 없음 (D-09-D5).

#### §9.11.6 Warning Code (Phase 9 신설)

| Code | Trigger |
|------|---------|
| `phase_unavailable_for_inference` | phaseBoundaries 에 해당 phase 없음 (D-09-A4) |
| `axis_signal_unavailable` | top-level `forceSignalsReport.warnings` ∋ `axis_metric_transitional` (R4 report tier) OR axisMetric.warnings ∈ {`phase_8_1_wave_0_transitional`, `tilt_unavailable`, `tilt_thresholds_fallback`} (R4 per-metric tier) (D-09-A2 / RESEARCH Pitfall 2) |
| `no_significant_force_pattern_signal` | 6 signal 모두 detection 미통과 (D-09-B4) — fabrication 금지 |

*Phase 9 추가: 2026-06-10 — ForcePatternInference + ForcePatternFinding 신설. D-09-A1~U6 박제. 본 contract = TS `analysis.ts` + Python `force_pattern.py` 와 atomic commit lockstep (D-09-U1).*
```

Update the change log at the bottom of `§9.*` 섹션 (existing 박제 패턴 정합).
  </action>
  <verify>
    <automated>grep -c "§9.11" /Users/kimtaesung/Dev/SunityMotion/docs/contract.md | grep -E "^[1-9][0-9]*$"</automated>
    <automated>grep -E "no_significant_force_pattern_signal|axis_signal_unavailable|phase_unavailable_for_inference" /Users/kimtaesung/Dev/SunityMotion/docs/contract.md | wc -l | awk '$1>=3{print "OK"; exit 0} {exit 1}'</automated>
    <automated>cd backend &amp;&amp; pytest tests/phase09/test_force_pattern_lockstep.py -x -q</automated>
  </verify>
  <done>§9.11 신설 + 3 warning code 박제 + 3-way lockstep test green.</done>
</task>

<task type="auto" id="09-01-T5">
  <name>Task 09-01-T5: Firestore scoped validator + complete_analysis 시그니처 확장</name>
  <files>backend/shared/python/sunity_shared/firestore_admin.py</files>
  <depends_on>[09-01-T2]</depends_on>
  <threat_ref>T-09-T2 (nested-array tampering), T-09-V5</threat_ref>
  <behavior>
    - `_FORCE_PATTERN_FINDING_SCALAR_LIST_KEYS: frozenset[str] = frozenset({"warnings"})` 박제 (finding entry 안 list 허용 키 화이트리스트).
    - `_validate_force_pattern_finding_dict(d, *, path)` — finding entry dict 검증. dict / list[list] / non-scalar list element / 화이트리스트 외 list 모두 reject. `warnings` 키 인접 시 **모든 element 가 `isinstance(item, str)` 여야 함** (R10 정합 — 기존 Phase 8 helper 의 일반 scalar 허용보다 더 엄격: warning code 만 받음, int/bool/None 불허). 빈 string 도 reject (`assert item`).
    - `_validate_force_pattern_inference(payload, *, path='forcePatternInference')` — top-level dict 검증. `findings` (list[dict]) / `warnings` (R10 정합 — list[str] 만, 동일하게 non-empty str element 검사) / scalar 외 reject. None 입력 시 early return.
    - `complete_analysis(...)` 시그니처에 `force_pattern_inference: dict | None = None` kwarg 추가. None 이 아니면 `_validate_force_pattern_inference(...)` 호출 후 `payload["result"]["forcePatternInference"] = force_pattern_inference` 저장.
    - 기존 `force_signals_report` 처리 흐름 변경 X — 신설 분기만 박제 (Phase 8 패턴 정합).
  </behavior>
  <action>
Edit `backend/shared/python/sunity_shared/firestore_admin.py`. Reference: existing `_validate_force_signals_report` block at lines 148-260 (verified — `force_signals_report 전용 scoped validator`).

Append immediately AFTER the existing `_validate_force_signals_report` block (and before `complete_analysis`):

```python
# ── Phase 9 — force_pattern_inference 전용 scoped validator ────────────
# D-09-U5 + RESEARCH Pattern 5. `_validate_force_signals_report` 패턴 직접 mirror —
# nested-array 금지 + 화이트리스트 list field 만 허용. forcePatternInference 안에서만
# list[dict-of-scalars] (findings) + list[str] (warnings) 박제 가능.

_FORCE_PATTERN_FINDING_SCALAR_LIST_KEYS: frozenset[str] = frozenset({"warnings"})


def _validate_force_pattern_finding_dict(d: dict, *, path: str) -> None:
    """Phase 9 finding entry dict 검증 — 8 필드 화이트리스트."""
    if not isinstance(d, dict):
        raise ValueError(f"{path} must be dict, got {type(d).__name__}")
    for k, v in d.items():
        sub = f"{path}.{k}"
        if v is None or isinstance(v, (str, int, float, bool)):
            continue
        if isinstance(v, dict):
            raise ValueError(
                f"{sub} nested dict in finding entry not allowed "
                f"(forcePatternInference schema: scalar fields + 'warnings' list[str] only)"
            )
        if isinstance(v, list):
            if k not in _FORCE_PATTERN_FINDING_SCALAR_LIST_KEYS:
                raise ValueError(
                    f"{sub} list field not in force_pattern_finding whitelist "
                    f"({sorted(_FORCE_PATTERN_FINDING_SCALAR_LIST_KEYS)}); reject"
                )
            # R2 (Codex iteration-2) — `warnings` 는 contract 상 list[str] only.
            # Phase 8 의 permissive scalar 허용을 mirror 하지 X — Phase 9 는 더 strict.
            for i, item in enumerate(v):
                if isinstance(item, (list, dict)):
                    raise ValueError(f"{sub}[{i}] nested list/dict reject")
                if not isinstance(item, str) or not item:
                    raise ValueError(
                        f"{sub}[{i}] must be non-empty str warning code "
                        f"(contract list[str]), got {type(item).__name__}={item!r}"
                    )
            continue
        raise ValueError(
            f"{sub} unexpected type at force_pattern_finding: {type(v).__name__}"
        )


def _validate_force_pattern_inference(
    payload: dict, *, path: str = "forcePatternInference"
) -> None:
    """force_pattern_inference top-level scoped validator (D-09-U5)."""
    if payload is None:
        return
    if not isinstance(payload, dict):
        raise ValueError(
            f"_validate_force_pattern_inference: dict 입력만 허용. "
            f"path={path}, type={type(payload).__name__}"
        )
    for key, value in payload.items():
        sub = f"{path}.{key}"
        if value is None or isinstance(value, (str, int, float, bool)):
            continue
        if isinstance(value, dict):
            raise ValueError(
                f"{sub} unexpected nested dict at forcePatternInference top-level"
            )
        if isinstance(value, list):
            if key == "warnings":
                # R2 (Codex iteration-2) — top-level warnings 도 non-empty str only.
                for i, item in enumerate(value):
                    if not isinstance(item, str) or not item:
                        raise ValueError(
                            f"{sub}[{i}] must be non-empty str warning code "
                            f"(contract list[str]), got {type(item).__name__}={item!r}"
                        )
                continue
            if key == "findings":
                if len(value) > 3:
                    raise ValueError(
                        f"{sub} length > 3 — D-09-B4 fabrication 금지 (len={len(value)})"
                    )
                for i, item in enumerate(value):
                    _validate_force_pattern_finding_dict(item, path=f"{sub}[{i}]")
                continue
            raise ValueError(
                f"{sub} unexpected list at forcePatternInference top-level "
                f"(only 'warnings' and 'findings' allowed)"
            )
        raise ValueError(
            f"{sub} unexpected type at forcePatternInference: {type(value).__name__}"
        )
```

Modify `complete_analysis` signature (현재 `force_signals_report: dict | None = None` 직후 박제):

```python
def complete_analysis(
    uid: str,
    analysis_id: str,
    result: dict,
    *,
    angles=None,
    angles_joint_keys=None,
    angles_frames=None,
    body_comparison_report: dict | None = None,
    body_normalization_profile: dict | None = None,
    force_signals_report: dict | None = None,
    force_pattern_inference: dict | None = None,   # ◄── Phase 9 신설
) -> None:
```

And add the storage block immediately after the existing `force_signals_report` write block (Phase 8 패턴 1:1 mirror):

```python
    if force_pattern_inference is not None:
        # Phase 9 D-09-U5 — scoped validator (top-level forcePatternInference 만 nested
        # list[dict] 허용, [[firestore-nested-array-flat]] 보존).
        _validate_force_pattern_inference(force_pattern_inference)
        payload["result"]["forcePatternInference"] = force_pattern_inference
```

Comments must cite `D-09-U5`, `D-09-B4`, `Plan 09-01`, RESEARCH Pattern 5.

**No changes to other write paths** — existing strict default still applies elsewhere (RESEARCH Pitfall: scoped validator scope 외 데이터는 strict 유지).
  </action>
  <verify>
    <automated>cd backend &amp;&amp; pytest tests/phase09/test_firestore_lockstep_phase9.py -x -q</automated>
    <automated>cd backend &amp;&amp; python -c "import inspect; from sunity_shared.firestore_admin import complete_analysis; sig = inspect.signature(complete_analysis); assert 'force_pattern_inference' in sig.parameters; print('OK')"</automated>
    <automated>cd backend &amp;&amp; pytest tests/phase08/ -x -q</automated>
  </verify>
  <done>Validator test 5/5 PASS + complete_analysis 시그니처 확장 + Phase 8 회귀 0.</done>
</task>

<task type="auto" id="09-01-T6">
  <name>Task 09-01-T6: Frontend null-guard (app/src/lib/userAnalyses.ts)</name>
  <files>app/src/lib/userAnalyses.ts</files>
  <depends_on>[09-01-T3]</depends_on>
  <threat_ref>T-09-V5</threat_ref>
  <action>
Edit `app/src/lib/userAnalyses.ts::normalize()`. **R1 정합 (Codex iteration-2 review)** — `forcePatternInference` 는 `AnalysisDoc.result` 내부 필드이지 top-level 필드 X. `raw.result` 는 `unknown` 이므로 `raw?.result?.forcePatternInference` 접근은 `tsc --noEmit` 실패. 이미 narrowing 된 `result` 변수 (`raw.result as AnalysisDoc['result'] | undefined`) 를 사용해야 한다. 기존 `forceSignalsReport` null-guard (`userAnalyses.ts:74-88`) 패턴 1:1 mirror.

After the existing `forceSignalsReport` null-guard block (lines 74-88 of `userAnalyses.ts`), add a sibling block:

```ts
// Phase 9 §9.11 forcePatternInference null-guard (D-09-D1 / RESEARCH Pitfall 8 / R1).
// Mirrors forceSignalsReport pattern at lines 74-88. forcePatternInference 는
// result 내부 필드 — 이미 narrowing 된 result 변수를 사용해야 typecheck PASS.
if (result?.forcePatternInference) {
  const inference = result.forcePatternInference;
  result = {
    ...result,
    forcePatternInference: {
      ...inference,
      findings: (inference.findings ?? []).map((f) => ({
        ...f,
        warnings: f.warnings ?? [],
        jointHint: f.jointHint ?? null,
      })),
      warnings: inference.warnings ?? [],
    },
  };
}
```

**Do NOT** add `forcePatternInference` as a top-level field in the AnalysisDoc return object. It lives inside `result` (see TS interface in Wave 0 T3 — `AnalysisResult.forcePatternInference?: ForcePatternInference | null`). The existing return block (`{ analysisId, mode, status, fileName, createdAt, updatedAt, error, result }`) already carries the inference through `result`.

No new top-level import needed — `result` is already typed `AnalysisDoc['result'] | undefined`, and the inner spread inherits the optional `ForcePatternInference` type from `analysis.ts`.

Cite `D-09-D1`, `RESEARCH Pitfall 8`, and `R1 (Codex iteration-2)` in inline comment.
  </action>
  <verify>
    <automated>cd app &amp;&amp; npm run typecheck</automated>
    <automated>grep -c "forcePatternInference" /Users/kimtaesung/Dev/SunityMotion/app/src/lib/userAnalyses.ts | awk '$1>=2{print "OK"; exit 0} {exit 1}'</automated>
  </verify>
  <done>tsc --noEmit 0 error + normalize() 가 forcePatternInference null-guard + findings 내부 warnings/jointHint default.</done>
</task>

<task type="auto" id="09-01-T7">
  <name>Task 09-01-T7: Wave 0 atomic commit + 회귀 게이트</name>
  <files>(commit only — no file edits)</files>
  <depends_on>[09-01-T1, 09-01-T2, 09-01-T3, 09-01-T4, 09-01-T5, 09-01-T6]</depends_on>
  <threat_ref>—</threat_ref>
  <action>
**SINGLE atomic commit** (per D-09-U1). 11 files 모두 동시 박제:

1. Run full local gates first:
   - `cd backend && pytest tests/phase09/ -x -q` → 4 test PASS.
   - `cd backend && pytest tests/phase06/ tests/phase07/ tests/phase08/ tests/phase08_1/ -x -q` → 회귀 0.
   - `cd app && npm run typecheck` → 0 error.

2. Stage explicit file list (no `git add -A`):

   ```
   git add \
     backend/shared/python/sunity_shared/analysis/force_pattern.py \
     backend/shared/python/sunity_shared/firestore_admin.py \
     app/src/types/analysis.ts \
     app/src/lib/userAnalyses.ts \
     docs/contract.md \
     backend/tests/phase09/__init__.py \
     backend/tests/phase09/conftest.py \
     backend/tests/phase09/test_force_pattern_lockstep.py \
     backend/tests/phase09/test_dataclass_to_camel_case_dict_phase9.py \
     backend/tests/phase09/test_firestore_lockstep_phase9.py \
     backend/tests/phase09/test_force_pattern_dataclass.py
   ```

3. Commit message:

   ```
   feat(09-01): Wave 0 — ForcePatternFinding/Inference 3-way schema lockstep

   - force_pattern.py 신설 (frozen dataclass + Literal alias + 상수, 본체 함수는 Plan 09-02)
   - firestore_admin: _validate_force_pattern_inference + complete_analysis(force_pattern_inference=) 시그니처
   - analysis.ts: ForcePatternFinding + ForcePatternInference + AnalysisResult.forcePatternInference
   - userAnalyses.ts: normalize null-guard (findings 내부 warnings/jointHint default)
   - docs/contract.md §9.11 신설 (Phase 9 책임 경계 + warning code 3종)
   - phase09 pytest harness + 4 unit test (lockstep + camelCase + firestore + dataclass)

   D-09-D1 / D-09-U1 (3-way atomic lockstep) / D-09-U3 / D-09-U4 / D-09-U5.
   Wave 1 (Plan 09-02) 가 본체 함수 + canned + pipeline wiring 박제.
   ```

4. Post-commit verify:
   - `git log -1 --stat` → 11 files changed.
   - `cd backend && pytest tests/phase09/ -x -q` → 4 PASS (재확인).
  </action>
  <verify>
    <automated>cd backend &amp;&amp; pytest tests/phase09/ -x -q</automated>
    <automated>cd backend &amp;&amp; pytest tests/phase06/ tests/phase07/ tests/phase08/ tests/phase08_1/ -x -q</automated>
    <automated>cd app &amp;&amp; npm run typecheck</automated>
    <automated>git log -1 --name-only --pretty=format: | grep -E "(force_pattern\.py|firestore_admin\.py|analysis\.ts|userAnalyses\.ts|contract\.md|phase09/)" | sort -u | wc -l | awk '$1>=10{print "OK"; exit 0} {exit 1}'</automated>
  </verify>
  <done>Wave 0 SINGLE atomic commit 박제 (11 files). phase09 4/4 PASS + Phase 6/7/8/8.1 회귀 0 + tsc 0 error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pipeline `_process` → `_validate_force_pattern_inference` | Untrusted dataclass-derived dict (Phase 9 backend internal — 외부 input 0, but defense-in-depth) |
| Firestore client write path | `_validate_force_pattern_inference` 가 nested-array / list[non-whitelist] 차단 |
| TS interface ↔ Python frozen dataclass ↔ docs §9.11 | 3-way drift = downstream Phase 11/12 consume 시 schema crash |
| React Native `userAnalyses.ts::normalize` | Firestore raw doc 의 null/undefined → AnalysisDoc.result.forcePatternInference 안전 normalize |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-V5 | Tampering / Input Validation (ASVS V5) | ForcePatternFinding / ForcePatternInference dataclass | mitigate | Frozen dataclass + `__post_init__` validator (enum / confidence [0,1] / interpretation non-empty / findings length ≤ 3) — test_force_pattern_dataclass.py 회귀 차단 |
| T-09-T2 | Tampering | Firestore nested-array | mitigate | `_validate_force_pattern_inference` scoped validator — list[list] / nested dict / list[non-whitelist] 모두 reject. test_firestore_lockstep_phase9.py 5 case (1 PASS + 4 reject) 회귀 차단 |
| T-09-V5b | Tampering | Frontend null/undefined → React render crash | mitigate | normalize() 가 findings 내부 warnings/jointHint default + Phase 8 forceSignalsReport null-guard 패턴 mirror. tsc --noEmit 회귀 차단 |
| T-09-T-drift | Tampering | 3-way schema drift (TS ↔ Python ↔ docs §9.11) | mitigate | test_force_pattern_lockstep.py 가 frozenset / TS Literal token / docs §9.11 필드 표 1:1 검증. atomic commit (T7) 가 drift 발생 자체 차단 |
| T-09-SC | Supply chain | npm/pip install | accept | 신규 install 0 (RESEARCH §Package Legitimacy Audit) — slopcheck N/A |

</threat_model>

<verification>

**Per-task gates** (위 `<verify>` automated 블록 참조).

**Wave 0 종료 gate (commit 직후):**

- `cd backend && pytest tests/phase09/ -x -q` → 4/4 PASS (lockstep + camelCase + firestore + dataclass).
- `cd backend && pytest tests/phase06/ tests/phase07/ tests/phase08/ tests/phase08_1/ -x -q` → 회귀 0.
- `cd app && npm run typecheck` → 0 error.
- `git log -1 --name-only` → 11 files (1 atomic commit) 확인.

**Wave 1 진입 전 manual check** (per CONTEXT follow-up `Wave 0 종료 후`):

- 3-way schema field alignment 회귀 0 — `git diff HEAD~1 -- app/src/types/analysis.ts backend/shared/python/sunity_shared/analysis/force_pattern.py docs/contract.md` 시 1 atomic commit 안에 모두 박제 확인.

</verification>

<success_criteria>

- [ ] `backend/shared/python/sunity_shared/analysis/force_pattern.py` 신설 + import success + `ForcePatternFinding` / `ForcePatternInference` 박제 (본체 함수는 Wave 1).
- [ ] `firestore_admin._validate_force_pattern_inference` + `complete_analysis(force_pattern_inference=)` 시그니처 확장.
- [ ] `app/src/types/analysis.ts`: `ForcePatternFinding` + `ForcePatternInference` + `AnalysisResult.forcePatternInference?`.
- [ ] `app/src/lib/userAnalyses.ts::normalize`: forcePatternInference null-guard + findings 내부 default.
- [ ] `docs/contract.md §9.11` 신설 (8 + 5 필드 + 3 warning code + 책임 경계).
- [ ] `backend/tests/phase09/{__init__.py, conftest.py, 4 test files}` 신설 + `pytest tests/phase09/ -x -q` 4/4 PASS.
- [ ] Wave 0 = SINGLE atomic commit (11 files) — `git log -1 --stat` 확인.
- [ ] Phase 6/7/8/8.1 회귀 0 + `npm run typecheck` 0 error.

</success_criteria>

<follow_ups>

- **Wave 0 종료 후 (즉시 — CONTEXT follow-up):** 3-way schema lockstep 회귀 0 확인 → Wave 1 (Plan 09-02) 진입 OK.
- **belle 박제 검수 = N/A** (Wave 0 = schema only — 18 canned 본문은 Wave 1 책임).
- **Codex cross-AI plan-review = N/A** (Wave 1 종료 시점에 진행).

</follow_ups>

<rollback>

Wave 0 가 단일 commit 이므로 rollback = `git revert <wave0-commit-sha>` 한 번. 11 파일 동시 원복.

부분 rollback 시 schema drift (TS ↔ Python ↔ docs §9.11 1:1 깨짐) 발생 → 절대 금지. 부분 수정이 필요하면 새 commit 으로 fix-forward.

</rollback>

<output>
Create `.planning/phases/09-forcedirectionpattern-3/09-01-SUMMARY.md` when done (Phase 8 / 8.1 SUMMARY 박제 패턴 정합).
</output>
