---
plan_id: 09-02
phase: 09-forcedirectionpattern-3
plan: 02
wave: 1
type: execute
title: "Wave 1 — infer_force_direction_pattern 본체 + 18 canned + pipeline wiring"
requirements: [FORCE-01, FEED-02]
mvp_slice: "force_pattern.py 본체 (6 signal detection + Top-3 ranking + tie-break + motion_id boost + 0-finding fallback) + force_pattern_copy.py (18 canned KO interpretation + mode prefix + jointHint 부위 어휘) + pipeline _process wiring (mode_context inline + infer_force_direction_pattern 호출 + complete_analysis kwarg) — Firestore 의 forcePatternInference 가 실 데이터로 흘러 Phase 11/12 가 consume 가능한 E2E vertical 슬라이스 완성"
depends_on: [09-01]
files_modified:
  - backend/shared/python/sunity_shared/analysis/force_pattern.py
  - backend/shared/python/sunity_shared/analysis/force_pattern_copy.py
  - backend/functions/pipeline/app.py
  - backend/tests/phase09/test_infer_force_direction_pattern.py
  - backend/tests/phase09/test_force_pattern_ranking.py
  - backend/tests/phase09/test_force_pattern_copy_render.py
  - backend/tests/phase09/test_force_pattern_copy_no_forbidden.py
  - backend/tests/phase09/test_force_pattern_no_severity_use.py
  - backend/tests/pipeline/test_pipeline_phase9.py
  - .planning/phases/09-forcedirectionpattern-3/09-VALIDATION.md
autonomous: true
mvp_mode: true
must_haves:
  truths:
    - "`infer_force_direction_pattern(force_signals_report, motion_id, mode_context) → ForcePatternInference` 가 Phase 8 산출 위에 6 signal detection (axis_tilt / pelvis_drop / late_contact / high_jitter / high_jerk / abnormal_release) 을 적용 (per D-09-A1)."
    - "raw `shoulder_tilt` + `hip_tilt` + `confidence` + `warnings` 만 사용. `.severity` 접근 0 회 (AST gate 회귀 차단 per D-09-A2 / T-09-T1). stabilityMetric / contactMetric 의 severity 는 사용 가능 — guard scope 외 (per RESEARCH §Anti-Patterns)."
    - "axis warning surface = two-tier (R4): per-axisMetric warnings ∈ {phase_8_1_wave_0_transitional, tilt_unavailable, tilt_thresholds_fallback} OR top-level report warnings ∋ axis_metric_transitional 포함 시 axis 계열 detector skip + finding 미생성 + umbrella warning `axis_signal_unavailable` (per D-09-A2 / RESEARCH Pitfall 2). force_signals.py:1670-1677 verified — top-level `axis_metric_transitional` 은 모든 axisMetric 의 `phase_8_1_wave_0_transitional` 동행 시에만 emit."
    - "phaseBoundaries 미인식 시 해당 phase skip + umbrella `phase_unavailable_for_inference` warning emit (per D-09-A4)."
    - "`confidence = base_confidence × phase_metric_confidence_factor` where base ∈ {axis_tilt:0.72, pelvis_drop:0.72, late_contact:0.70, high_jitter:0.63, high_jerk:0.63, abnormal_release:0.75} and cf = min(_CONFIDENCE_TO_FACTOR[axisMetric.confidence], _CONFIDENCE_TO_FACTOR[stabilityMetric.confidence]) (per D-09-A5 / RESEARCH Pitfall 4)."
    - "Top-3 ranking by `score = confidence × signal_weight` with signal_weight ∈ {axis_tilt:1.0, pelvis_drop:1.0, late_contact:0.95, abnormal_release:1.1, high_jerk:0.85, high_jitter:0.80} (per D-09-B2)."
    - "Tie-break = (phase priority lock<hold<transition<final_shape<entry) → (signal priority axis<contact<stability) → (confidence DESC) (per D-09-B3 / RESEARCH Pitfall 6)."
    - "motion_id is not None → confidence × 1.05 (cap 1.0). Boost is applied BEFORE ranking so score sort reflects boosted values (per D-09-C2 / RESEARCH Pitfall 5)."
    - "(pattern, phase) 동일 조합 중복 차단 — top confidence 1개만 emit. 동일 pattern 다른 phase 는 OK (per D-09-B5)."
    - "findings length [0, 3]. 0개 시 overallConfidence='low' + umbrella warning `no_significant_force_pattern_signal` + canned fallback 단일 본문 '이 영상에서는 분명한 힘 흐름 이슈 신호가 보이지 않습니다. 강사와 함께 확인하는 것을 권장해요.' (per D-09-B4 / RESEARCH Open Q 3)."
    - "Phase 9 v1 emit 0 회 `pattern='rotate'` 회귀 가드 (Literal union 박제, 자동 검출 X — RESEARCH Open Q 1)."
    - "`force_pattern_copy.py` 신설 — 18 canned KO 본문 (sourceSignal × modeContext) + 3 mode prefix const + jointHint 부위 어휘 매핑 + 0-finding fallback body 단일 (per D-09-D2 / Assumption A5)."
    - "10 금지 표현 AST grep gate (8 substring + 2 regex incl. `근육 힘 방향.*확정` and `\\d+%\\s*감점`) → 0 violations on _FORCE_PATTERN_COPY + _MODE_PREFIX + _FALLBACK_BODY (per D-09-D3 / RESEARCH Pitfall 7 / T-09-R1). regex `근육 힘 방향.*확정` 은 `근육 힘 방향이 확정` / `근육 힘 방향은 확정` / `근육 힘 방향만 확정` 같은 조사 변형 모두 catch."
    - "pipeline `_process` 가 `compute_force_signals(...)` 호출 직후 mode_context 산출 (mode == MODE_EXPERT → 'mode1'; mode == MODE_SELF AND comparison['isFirst'] → 'mode3_first'; mode == MODE_SELF AND not comparison['isFirst'] → 'mode3_progress') 후 `infer_force_direction_pattern(...)` 호출 + `_dataclass_to_camel_case_dict(...)` 변환 + `complete_analysis(..., force_pattern_inference=force_pattern_dict)` 저장 (per D-09-D6 / RESEARCH Pitfall 3 / Pattern 6)."
    - "force_signals_report is None 시 (Phase 8 가 신호 산출 실패 — 매우 드문 분기) Phase 9 wiring 도 no-op — complete_analysis 에 `force_pattern_inference=None` 전달 (Wave 0 시그니처 default 유지)."
    - "전 회귀 gate green: `cd backend && pytest tests/phase09/ tests/pipeline/test_pipeline_phase9.py -x -q` 모든 신설 + Wave 0 test PASS / `cd backend && pytest tests/phase06/ tests/phase07/ tests/phase08/ tests/phase08_1/ -x -q` 회귀 0 / `cd app && npm run typecheck` 0 error / 금지 표현 grep gate 10/10 PASS."
    - "VALIDATION.md frontmatter `nyquist_compliant: true` flip + `wave_0_complete: true` 표기 (Wave 1 종료 표시)."
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/force_pattern.py"
      provides: "Wave 0 dataclasses + `_IPSF_TOLERANCE_DEG=20.0` (Wave 0 owned, R6 reuse) 위에 신설 본체: `infer_force_direction_pattern` public + 6 private `_detect_*` helper + `_phase_metric_confidence_factor` + `_apply_motion_id_boost` + `_rank_top3` + `_overall_confidence_from_findings` + `_AXIS_IGNORE_WARNINGS_PER_METRIC` + `_AXIS_IGNORE_WARNINGS_REPORT` (R4 two-tier)."
      contains: "def infer_force_direction_pattern"
      min_lines: 280
    - path: "backend/shared/python/sunity_shared/analysis/force_pattern_copy.py"
      provides: "Stateless singleton dict literals: `_MODE_PREFIX` (3 entry) + `_FORCE_PATTERN_COPY` ((sourceSignal, modeContext) → KO 본문, 18 entry) + `_JOINT_HINT_BY_SIGNAL` (6 entry incl. None) + `_FALLBACK_BODY` (단일 KO 문자열) + `FORBIDDEN_PHRASES_RESEARCH` (8 substring) + `FORBIDDEN_PHRASES_PHASE9_REGEX` (2 regex patterns: `근육 힘 방향.*확정` + `\\d+%\\s*감점`) + `force_pattern_canned_text(source_signal, mode_context)` + `joint_hint_for(source_signal)` + `fallback_body()` lookup helpers."
      contains: "_FORCE_PATTERN_COPY"
      min_lines: 130
    - path: "backend/functions/pipeline/app.py"
      provides: "Phase 8 wiring 직후 Phase 9 wiring 블록 — `force_signals_report is None` no-op + mode_context inline + `infer_force_direction_pattern(...)` 호출 + `_dataclass_to_camel_case_dict(...)` 변환 + `complete_analysis(force_pattern_inference=...)` kwarg."
      contains: "infer_force_direction_pattern"
    - path: "backend/tests/phase09/test_infer_force_direction_pattern.py"
      provides: "6 signal × ON/OFF/경계 + 0/1/2/3 finding scenario + axis warning 3종 ignore + phase 미인식 fallback (`phase_unavailable_for_inference`) + 0-finding fallback (`no_significant_force_pattern_signal`) + confidence 산식 검증 (low base × low cf = 0.216 / high base × high cf = base) + 'rotate' 미산출 회귀 가드 (Open Q 1)."
      min_lines: 220
    - path: "backend/tests/phase09/test_force_pattern_ranking.py"
      provides: "Top-3 ranking by score = confidence × signal_weight + tie-break 3 stage (phase priority / signal priority / confidence DESC) + (pattern, phase) dedup (동일 차단, 다른 phase OK) + motion_id boost (× 1.05 cap 1.0) BEFORE ranking 검증 (boost 켰을 때 Top-3 순서 변경 fixture)."
      min_lines: 140
    - path: "backend/tests/phase09/test_force_pattern_copy_render.py"
      provides: "18 canned lookup PASS (6 signal × 3 modeContext) + 3 mode prefix const 일치 + jointHint 부위 어휘 매핑 (axis_tilt='코어', pelvis_drop='고관절', late_contact='내전근', abnormal_release='광배', high_jerk=None, high_jitter=None) + fallback body 단일 본문 매칭."
      min_lines: 80
    - path: "backend/tests/phase09/test_force_pattern_copy_no_forbidden.py"
      provides: "AST 추출 (`_FORCE_PATTERN_COPY` + `_MODE_PREFIX` + `_FALLBACK_BODY`) → 8 substring forbidden (광배 N% 부족 / 근력이 부족 / 전완 힘이 약 / 이 자세는 틀렸 / AI가 정확히 측정 / 부상 위험 확정 / 힘이 정확히 / 프로보다 못합) parametrize + 2 regex (`근육 힘 방향.*확정`, `\\d+%\\s*감점`) parametrize test + AST 추출 sanity (≥ 22 string)."
      min_lines: 90
    - path: "backend/tests/phase09/test_force_pattern_no_severity_use.py"
      provides: "force_pattern.py AST walk → `attr == 'severity'` 노드 중 receiver name token ∈ {'axis', 'axis_metric', 'axisMetric', 'a'} 인 access 0 회 (whitelist receivers stability/contact 만 허용). 동시에 .py 안 'axis_metric.severity' / 'axisMetric.severity' / 'axis.severity' substring grep 0 회 (defense in depth)."
      min_lines: 70
    - path: "backend/tests/pipeline/test_pipeline_phase9.py"
      provides: "pipeline `_process` integration: mode1 path (mode_context='mode1' + comparison 없음) / mode3_first path (mode_context='mode3_first' + comparison={'isFirst':True}) / mode3_progress path (mode_context='mode3_progress' + comparison={'isFirst':False}) / force_signals_report=None no-op (complete_analysis 가 force_pattern_inference=None 받음). monkeypatch `firestore_admin.complete_analysis` 로 호출 인자 capture."
      min_lines: 160
    - path: ".planning/phases/09-forcedirectionpattern-3/09-VALIDATION.md"
      provides: "frontmatter `nyquist_compliant: true` + `wave_0_complete: true` flip (Wave 1 종료 박제)."
  key_links:
    - from: "backend/shared/python/sunity_shared/analysis/force_pattern.py"
      to: "backend/shared/python/sunity_shared/analysis/force_signals.py"
      via: "import AxisDeviationMetric / StabilityMetric / ContactStabilityMetric / PhaseBoundary / MotionPhase / MetricConfidence / ForceSignalsReport + JITTER_SEVERITY_THRESHOLDS + JERK_SEVERITY_THRESHOLDS_DEG_PER_SEC_CUBED"
      pattern: "from .force_signals import"
    - from: "backend/shared/python/sunity_shared/analysis/force_pattern.py"
      to: "backend/shared/python/sunity_shared/analysis/force_pattern_copy.py"
      via: "force_pattern_canned_text + joint_hint_for + fallback_body lookup"
      pattern: "from .force_pattern_copy import"
    - from: "backend/functions/pipeline/app.py"
      to: "backend/shared/python/sunity_shared/analysis/force_pattern.py"
      via: "infer_force_direction_pattern 호출 직후 _dataclass_to_camel_case_dict → complete_analysis(force_pattern_inference=) kwarg"
      pattern: "infer_force_direction_pattern\\("
    - from: "backend/functions/pipeline/app.py"
      to: "comparison['isFirst']"
      via: "mode_context 산출 single source — Phase 12.5 build_mode3 박제 재사용 (RESEARCH Pitfall 3)"
      pattern: "comparison\\.get\\(\"isFirst\""
---

<objective>
Wave 1 = **inference 본체 + canned 카피 + pipeline wiring 완성**. Wave 0 (Plan 09-01) 의 schema lockstep 위에 6 signal detection rules + Top-3 ranking + tie-break + motion_id boost + 0-finding fallback + 18 canned KO interpretation 을 박제하고, `pipeline._process` 가 `compute_force_signals(...)` 호출 직후 `infer_force_direction_pattern(...)` 을 호출해 Firestore `result.forcePatternInference` 까지 데이터가 흘러가는 E2E vertical 슬라이스를 완성한다.

만족 SC / 결정:

- **FORCE-01 SC#1~SC#4** (5 pattern enum / Top-3 카드 / confidence + interpretation 가능성 언어 / 단정 표현 미출력).
- **FEED-02** (18 canned + 부위 어휘 + 가능성 언어).
- **D-09-A1 / A2 / A4 / A5 / B1~B5 / C1 / C2 / D2 / D3 / D6 / U2 / U4 / U6** — 본 wave 의 직접 박제 대상.

본 plan 이 산출 X (영구 OUT — D-09-E2):

- RunPod 재배포 / 정은지 5 영상 재sweep / production 신뢰도 분포 검증 — **Phase 11 통합 시점 + Phase 15** 책임.
- Layer 2 Gemini 호출 — D-09-C1 영구 차단.
- LLM 자연어 풍부화 — Phase 11 (CoachCommentHook) 책임.
- 영상 위 오버레이 / 실측 raw 수치 (예: "shoulder_tilt 87°") 노출 — Phase 12 책임.
- "어깨 elevation" / "elbow lock" 절대 joint angle 신호 — v2 deferred (RESEARCH §State of the Art).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<files_to_read>
@.planning/phases/09-forcedirectionpattern-3/09-CONTEXT.md
@.planning/phases/09-forcedirectionpattern-3/09-RESEARCH.md
@.planning/phases/09-forcedirectionpattern-3/09-VALIDATION.md
@.planning/phases/09-forcedirectionpattern-3/09-01-PLAN.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@backend/shared/python/sunity_shared/analysis/force_signals.py
@backend/shared/python/sunity_shared/analysis/copy_templates.py
@backend/shared/python/sunity_shared/firestore_admin.py
@backend/shared/python/sunity_shared/models.py
@backend/functions/pipeline/app.py
@backend/tests/phase07/test_copy_templates_no_forbidden.py
@backend/tests/phase09/conftest.py
@app/src/types/analysis.ts
@app/src/lib/userAnalyses.ts
@docs/contract.md
@docs/research/02_힘방향_힘조절_엔진_FINAL.md
@docs/research/폴스포츠-지식.md
</files_to_read>

<files_modified>

| Path | Role | New? |
|------|------|------|
| `backend/shared/python/sunity_shared/analysis/force_pattern.py` | Wave 0 schema + Literal aliases + 상수 (`_IPSF_TOLERANCE_DEG`=20.0 reused, R6) 위에 본체 추가: `infer_force_direction_pattern` public + 6 `_detect_*` helper + `_phase_metric_confidence_factor` + `_apply_motion_id_boost` + `_rank_top3` + `_overall_confidence_from_findings` + `_AXIS_IGNORE_WARNINGS_PER_METRIC` + `_AXIS_IGNORE_WARNINGS_REPORT` (R4 two-tier) | MODIFY |
| `backend/shared/python/sunity_shared/analysis/force_pattern_copy.py` | Stateless singleton: `_MODE_PREFIX` + `_FORCE_PATTERN_COPY` (18 entry) + `_JOINT_HINT_BY_SIGNAL` + `_FALLBACK_BODY` + `FORBIDDEN_PHRASES_RESEARCH` + `FORBIDDEN_PHRASES_PHASE9_REGEX` + `force_pattern_canned_text` / `joint_hint_for` / `fallback_body` helpers | NEW |
| `backend/functions/pipeline/app.py` | `compute_force_signals(...)` 직후 mode_context inline + `infer_force_direction_pattern(...)` + camelCase 변환 + `complete_analysis(force_pattern_inference=...)` kwarg | MODIFY |
| `backend/tests/phase09/test_infer_force_direction_pattern.py` | 6 signal detection + 경계 + 0/1/2/3 finding + axis warning ignore (3종) + phase 미인식 + confidence 산식 + rotate 미산출 회귀 가드 | NEW |
| `backend/tests/phase09/test_force_pattern_ranking.py` | Top-3 ranking + tie-break (phase / signal / confidence) + (pattern, phase) dedup + motion_id boost-before-ranking | NEW |
| `backend/tests/phase09/test_force_pattern_copy_render.py` | 18 lookup + 3 mode prefix + jointHint 매핑 + fallback body 본문 | NEW |
| `backend/tests/phase09/test_force_pattern_copy_no_forbidden.py` | AST grep gate (8 substring + 2 regex incl. `근육 힘 방향.*확정` + `\d+%\s*감점`) | NEW |
| `backend/tests/phase09/test_force_pattern_no_severity_use.py` | force_pattern.py AST 안 axis severity 접근 0 회 + substring grep | NEW |
| `backend/tests/pipeline/test_pipeline_phase9.py` | pipeline `_process` integration — mirrors `backend/tests/pipeline/test_pipeline_phase8.py` fixture style (mode1 / mode3_first / mode3_progress / no-op when force_signals_report is None) + complete_analysis kwarg capture. Real `_process` invocation (R8 정합 — would have caught R3 wrong-constant) | NEW |
| `.planning/phases/09-forcedirectionpattern-3/09-VALIDATION.md` | frontmatter `nyquist_compliant: true` + `wave_0_complete: true` flip | MODIFY |

</files_modified>

<context>
@.planning/phases/09-forcedirectionpattern-3/09-CONTEXT.md
@.planning/phases/09-forcedirectionpattern-3/09-RESEARCH.md
@.planning/phases/09-forcedirectionpattern-3/09-VALIDATION.md
@.planning/phases/09-forcedirectionpattern-3/09-01-PLAN.md
@CLAUDE.md
@backend/shared/python/sunity_shared/analysis/force_signals.py
@backend/shared/python/sunity_shared/analysis/copy_templates.py
@backend/shared/python/sunity_shared/firestore_admin.py
@backend/shared/python/sunity_shared/models.py
@backend/functions/pipeline/app.py
@backend/tests/phase07/test_copy_templates_no_forbidden.py
@backend/tests/phase09/conftest.py
@docs/contract.md
</context>

<tasks>

<task type="auto" id="09-02-T1" tdd="true">
  <name>Task 09-02-T1: force_pattern_copy.py 신설 (18 canned + mode prefix + jointHint + fallback + forbidden 리스트) + 2 test 박제 + 첫 atomic commit</name>
  <files>
    backend/shared/python/sunity_shared/analysis/force_pattern_copy.py,
    backend/tests/phase09/test_force_pattern_copy_render.py,
    backend/tests/phase09/test_force_pattern_copy_no_forbidden.py
  </files>
  <depends_on>[]</depends_on>
  <threat_ref>T-09-T3 (canned escape), T-09-R1 (단정 표현 회귀), T-09-V5 (input validation)</threat_ref>
  <behavior>
    - `force_pattern_canned_text(source_signal='axis_tilt', mode_context='mode1')` returns the canned KO sentence starting with the mode1 prefix "정은지 선수 기준 패턴과 비교했을 때, " 본문 — full 18 entries cover all (sourceSignal × modeContext) Cartesian pairs.
    - `joint_hint_for('axis_tilt') == '코어'` / `'pelvis_drop' == '고관절'` / `'late_contact' == '내전근'` / `'abnormal_release' == '광배'` / `'high_jerk' is None` / `'high_jitter' is None` (per RESEARCH Assumption A3).
    - `fallback_body()` returns exactly: "이 영상에서는 분명한 힘 흐름 이슈 신호가 보이지 않습니다. 강사와 함께 확인하는 것을 권장해요." (Open Q 3 — 단일 본문, mode 분기 X).
    - `_MODE_PREFIX['mode1']` == "정은지 선수 기준 패턴과 비교했을 때, " / `_MODE_PREFIX['mode3_first']` == "이번 첫 분석에서, " / `_MODE_PREFIX['mode3_progress']` == "지난 영상 대비, ".
    - Every entry in `_FORCE_PATTERN_COPY` STARTS with the matching `_MODE_PREFIX[modeContext]` substring (mode prefix consistency invariant — RESEARCH §10.1 톤).
    - AST forbidden gate: 8 substring asserts (`광배 N% 부족`, `근력이 부족`, `전완 힘이 약`, `이 자세는 틀렸`, `AI가 정확히 측정`, `부상 위험 확정`, `힘이 정확히`, `프로보다 못합`) on extracted strings from `_FORCE_PATTERN_COPY` + `_MODE_PREFIX` + `_FALLBACK_BODY` dict/assignment nodes = 0 violations.
    - Regex forbidden gate (parametrized over `FORBIDDEN_PHRASES_PHASE9_REGEX` tuple): for each `pattern in (r"근육 힘 방향.*확정", r"\d+%\s*감점")`, `re.search(pattern, s)` over all extracted strings = 0 matches (RESEARCH Pitfall 7 + D-09-D3 신규 4종). regex `근육 힘 방향.*확정` 은 조사 변형 (`근육 힘 방향이 확정` / `근육 힘 방향은 확정` / `근육 힘 방향만 확정`) 까지 catch — SC#4 "근육 힘 방향 단정 표현 부재" 직접 정합.
    - AST extraction sanity: ≥ 22 string constants captured (18 canned + 3 mode prefix + 1 fallback minimum; tuple keys may add).
  </behavior>
  <action>
Create `backend/shared/python/sunity_shared/analysis/force_pattern_copy.py` with module docstring:

`"""Phase 9 ForcePatternFinding canned KO interpretation mapping (D-09-D2 / D-09-D3). Stateless singleton — caller-controlled input 0. force_pattern.py 가 `force_pattern_canned_text` / `joint_hint_for` / `fallback_body` lookup helper 호출. Phase 7 copy_templates.py 패턴 1:1 mirror — AST grep gate (test_force_pattern_copy_no_forbidden.py) 가 단정 표현 회귀 차단."""`

Imports (top-of-module):

```python
from __future__ import annotations

from types import MappingProxyType
from typing import TYPE_CHECKING, Literal

# R2 회귀 차단 — force_pattern_copy.py 는 force_pattern.py 를 runtime import 하지 X.
# force_pattern.py 가 force_pattern_copy.lookup helper 를 import 하므로 역방향이면 cycle.
# 따라서 ForceSourceSignal / ModeContext 는 local Literal alias 로 duplicate 박제하고,
# lockstep test (test_force_pattern_lockstep.py) 가 force_pattern.py 의 frozenset 과
# 1:1 일치 회귀를 강제한다. TYPE_CHECKING 블록은 type hint 호환만 보장.
if TYPE_CHECKING:  # pragma: no cover - type-only
    from .force_pattern import ForceSourceSignal as _FSS_Type
    from .force_pattern import ModeContext as _MC_Type

ForceSourceSignal = Literal[
    "axis_tilt",
    "pelvis_drop",
    "late_contact",
    "high_jitter",
    "high_jerk",
    "abnormal_release",
]
ModeContext = Literal["mode1", "mode3_first", "mode3_progress"]
```

Module-level constants:

- `_MODE_PREFIX: dict[ModeContext, str]` — 3 entries, trailing space + comma included so direct concat with body is grammatical:
  - `"mode1"`: `"정은지 선수 기준 패턴과 비교했을 때, "`
  - `"mode3_first"`: `"이번 첫 분석에서, "`
  - `"mode3_progress"`: `"지난 영상 대비, "`

- `_FORCE_PATTERN_COPY: Mapping[tuple[ForceSourceSignal, ModeContext], str]` — 18 entries, wrapped in `MappingProxyType(_FORCE_PATTERN_COPY_DATA)` for runtime immutability (R9 회귀 차단 — module-level dict 은 mutable, MappingProxyType 으로 caller-controlled input 0 invariant 박제). **Each value MUST begin with `_MODE_PREFIX[modeContext]` 문자열 (test gate 가 검증)**. Draft 본문 톤 = research §10.1 "가능성 언어" + 부위별 원인 + 강사 보조 (RESEARCH §Anti-Patterns 의 단정 표현 회피). Belle 검수는 follow-up 으로 분리. Reference pattern:
  ```python
  _FORCE_PATTERN_COPY_DATA: dict[tuple[ForceSourceSignal, ModeContext], str] = {
      ("axis_tilt", "mode1"): "정은지 선수 기준 패턴과 비교했을 때, 중심축이 살짝 흔들리는 가능성이 보여요.",
      # ... 17 more entries
  }
  _FORCE_PATTERN_COPY = MappingProxyType(_FORCE_PATTERN_COPY_DATA)
  ```
  **R8 (Codex iteration-2 narrowing)**: MappingProxyType wrap 은 `_FORCE_PATTERN_COPY` 에만 적용한다. `_MODE_PREFIX` 와 `_JOINT_HINT_BY_SIGNAL` 은 plain dict 유지 — 작은 lookup 이고 caller-controlled input 위협 표면 X. 18 canned 본문이 high-value invariant.

  Body 키워드 가이드 (research §10.1 + 폴스포츠-지식.md 정합 — 카피 작성 시 활용):
  - axis_tilt → "코어 / 중심축 / 척추 라인 / 고정 감각" 어휘
  - pelvis_drop → "골반 / 좌우 비대칭 / 상체 보상 / 고관절 안정" 어휘
  - late_contact → "내전근 / 다리 접촉 / 레그락 타이밍 / 코어-허벅지 연결" 어휘
  - high_jitter → "유지 근력 / 호흡 / 떨림 / 안정성" 어휘 — 부위 불명확 (jointHint=None per A3)
  - high_jerk → "힘 전달 부드러움 / 가속 변화 / 균일성" 어휘 — 부위 불명확 (jointHint=None per A3)
  - abnormal_release → "광배 / 견갑 / hold 중 접촉 / 그립 안정" 어휘

  Body 가이드 (mode 분기 — D-09-D2 + RESEARCH §10.1):
  - mode1 톤: "정은지 선수 기준 패턴과 비교했을 때, …가능성이 보여요" 류
  - mode3_first 톤: "이번 첫 분석에서, …흐름이 보이네요" 류
  - mode3_progress 톤: "지난 영상 대비, …흐름으로 …다시 점검해 볼 수 있어요" 류

  Body 작성 금기 (D-09-D3 정합 — AST gate 가 차단):
  - "박제" 단어 (일반 동사로 사용 금지 — [[no-baekje-filler]])
  - "%일치" / "유사도" (mode3 = 절대 지표 — [[mode3-progress-not-similarity]])
  - 8 forbidden substring + 2 regex (`근육 힘 방향.*확정`, `\d+%\s*감점`)

  실제 작성: planner 가 18 본문을 직접 박제 — research §10.1 의 5 예문 (가능성 어휘) 톤 + 위 키워드 가이드로 합성. 본 plan task 의 acceptance = AST gate PASS + 18 lookup PASS + 본문 길이 ≥ 25 chars + mode prefix consistency invariant. Belle 박제 검수 = follow-up (별도 작은 plan, 변경 시 본 파일 dict literal 만 갱신 + AST gate 재실행).

- `_JOINT_HINT_BY_SIGNAL: dict[ForceSourceSignal, str | None]`:
  ```
  {"axis_tilt": "코어", "pelvis_drop": "고관절", "late_contact": "내전근",
   "abnormal_release": "광배", "high_jerk": None, "high_jitter": None}
  ```

- `_FALLBACK_BODY: str` = `"이 영상에서는 분명한 힘 흐름 이슈 신호가 보이지 않습니다. 강사와 함께 확인하는 것을 권장해요."` (D-09-B4 / Open Q 3 단일 본문, mode 분기 X).

- `FORBIDDEN_PHRASES_RESEARCH: tuple[str, ...]` = research §10.2 6종 + Phase 9 신규 2 substring:
  `("광배 N% 부족", "근력이 부족", "전완 힘이 약", "이 자세는 틀렸", "AI가 정확히 측정", "부상 위험 확정", "힘이 정확히", "프로보다 못합")` — 8 entries.

- `FORBIDDEN_PHRASES_PHASE9_REGEX: tuple[str, ...]` = `(r"근육 힘 방향.*확정", r"\d+%\s*감점")` — D-09-D3 신규 4종 중 2 regex (RESEARCH Pitfall 7 박제). `근육 힘 방향.*확정` 의 `.*` 는 조사 변형 (`근육 힘 방향이 확정` / `근육 힘 방향은 확정` / `근육 힘 방향만 확정`) catch 보장 — SC#4 단정 표현 가드 직접 정합.

- `__all__` 명시 export (helpers + tuples). 내부 dict 은 `_` prefix 라 자동 제외.

Lookup helpers (3 public):

```python
def force_pattern_canned_text(
    source_signal: ForceSourceSignal, mode_context: ModeContext
) -> str:
    """Return canned KO 1-sentence interpretation. D-09-D2."""
    return _FORCE_PATTERN_COPY[(source_signal, mode_context)]


def joint_hint_for(source_signal: ForceSourceSignal) -> str | None:
    """Return 부위 어휘 hint (Assumption A3). None when 부위 불명확."""
    return _JOINT_HINT_BY_SIGNAL[source_signal]


def fallback_body() -> str:
    """D-09-B4 / Open Q 3 — 0 finding 시 단일 fallback body."""
    return _FALLBACK_BODY
```

Cite D-09 IDs in docstrings/comments (D-09-D2 / D-09-D3 / Open Q 3 / Assumption A3) and reference [[no-baekje-filler]] + [[mode3-progress-not-similarity]] as the rationale for the forbidden lists. Do not include any "박제" word inside `_FORCE_PATTERN_COPY` values.

Now create `backend/tests/phase09/test_force_pattern_copy_render.py`:

Imports: `pytest`, `force_pattern_copy` module-level constants + helpers, `force_pattern` Literal aliases.

Tests (≥ 6):

1. `test_eighteen_canned_entries_present` — assert `len(_FORCE_PATTERN_COPY) == 18` and Cartesian completeness: `set(_FORCE_PATTERN_COPY.keys()) == set(itertools.product(_FORCE_SOURCE_SIGNALS, _MODE_CONTEXTS))` (import frozensets from `force_pattern`).
2. `test_mode_prefix_const_exact_strings` — assert 3 expected mode prefix values match verbatim (incl trailing comma+space).
3. `test_mode_prefix_consistency_invariant` — for each `(sig, mc), body` in `_FORCE_PATTERN_COPY.items()`, assert `body.startswith(_MODE_PREFIX[mc])`.
4. `test_force_pattern_canned_text_lookup` — parametrize over 18 tuples, assert helper returns the same string as dict lookup.
5. `test_joint_hint_mapping` — assert 6 hint entries match Assumption A3 (4 strings + 2 None).
6. `test_fallback_body_single_sentence` — assert `fallback_body()` returns the exact CONTEXT.md 박제 문장.
7. `test_body_length_floor` — for each value in `_FORCE_PATTERN_COPY.values()`, assert `len(body) >= 25` (slop floor — single mode-prefix is ~12 chars so this enforces ≥ ~13 chars of real interpretation body).

Create `backend/tests/phase09/test_force_pattern_copy_no_forbidden.py` mirroring `backend/tests/phase07/test_copy_templates_no_forbidden.py`:

- Module docstring cites D-09-D3 / RESEARCH Pitfall 7.
- AST helper `_extract_force_pattern_copy_strings()` — open `force_pattern_copy.py` path via `Path(__file__).resolve().parents[3] / "backend" / "shared" / "python" / "sunity_shared" / "analysis" / "force_pattern_copy.py"`, `ast.parse`, walk Assign + AnnAssign nodes whose target id ∈ `{"_FORCE_PATTERN_COPY_DATA", "_MODE_PREFIX", "_FALLBACK_BODY"}` (R3 정합 — MappingProxyType wrap 으로 18 canned 본문이 `_FORCE_PATTERN_COPY_DATA` 안에 살아있음. `_FORCE_PATTERN_COPY` 자체는 `MappingProxyType(_FORCE_PATTERN_COPY_DATA)` call 결과라 AST literal 0개), collect every `ast.Constant(str)`. Return helper also separately returns the per-target string counts (so test 가 `_FORCE_PATTERN_COPY_DATA >= 18` 명시 assert 가능).
- `@pytest.mark.parametrize("phrase", FORBIDDEN_PHRASES_RESEARCH) def test_no_forbidden_substring(phrase)` — 8 substring assert phrase not in any extracted string.
- `@pytest.mark.parametrize("pattern", FORBIDDEN_PHRASES_PHASE9_REGEX) def test_no_forbidden_regex(pattern)` — iterate the 2-entry regex tuple (`근육 힘 방향.*확정`, `\d+%\s*감점`); `re.search(pattern, s)` against every extracted string, assert no match. Include positive-control inline comments: `"근육 힘 방향이 확정됩니다"` would match the first regex (조사 변형 가드 sanity); `"30% 감점입니다"` would match the second (수치 감점 가드 sanity). Neither is committed in source.
- `test_ast_gate_extracts_strings()` — assert `len(strings) >= 22` (sanity — 18 + 3 + 1 minimum) AND **`per_target['_FORCE_PATTERN_COPY_DATA'] >= 18`** (R3 정합 — guards against the extractor target drifting away from `_DATA` when MappingProxyType wrap was the original cause of R3). Also assert `per_target['_MODE_PREFIX'] >= 3` and `per_target['_FALLBACK_BODY'] >= 1`.

Final commit message:

```
feat(09-02): Wave 1 T1 — force_pattern_copy.py 18 canned + 10 forbidden grep gate

- 18 canned (sourceSignal × modeContext) + 3 mode prefix const + 6 jointHint + 1 fallback body
- AST grep gate: 8 substring + 2 regex (근육 힘 방향.*확정 / \d+%\s*감점) 회귀 차단
- 2 unit test PASS (render + no_forbidden, regex parametrized over 2-entry tuple)

D-09-D2 / D-09-D3 / Open Q 3 / Assumption A3 / A5.
Follow-up: belle 박제 검수 (18 canned 본문 + jointHint 부위 어휘 — 별도 작은 plan).
```

Pre-commit gate: `cd backend && pytest tests/phase09/test_force_pattern_copy_render.py tests/phase09/test_force_pattern_copy_no_forbidden.py -x -q`.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; pytest tests/phase09/test_force_pattern_copy_render.py tests/phase09/test_force_pattern_copy_no_forbidden.py -x -q</automated>
    <automated>cd backend &amp;&amp; python -c "from sunity_shared.analysis.force_pattern_copy import _FORCE_PATTERN_COPY, _MODE_PREFIX, _JOINT_HINT_BY_SIGNAL, _FALLBACK_BODY, FORBIDDEN_PHRASES_RESEARCH, FORBIDDEN_PHRASES_PHASE9_REGEX, force_pattern_canned_text, joint_hint_for, fallback_body; from types import MappingProxyType; assert len(_FORCE_PATTERN_COPY) == 18; assert isinstance(_FORCE_PATTERN_COPY, MappingProxyType); assert len(_MODE_PREFIX) == 3; assert len(FORBIDDEN_PHRASES_RESEARCH) == 8; assert len(FORBIDDEN_PHRASES_PHASE9_REGEX) == 2; print('OK')"</automated>
  </verify>
  <done>force_pattern_copy.py 18 canned + 3 mode prefix + 6 jointHint + 1 fallback + 8 forbidden substring + 2 regex (`근육 힘 방향.*확정`, `\d+%\s*감점`) 박제. `_FORCE_PATTERN_COPY` MappingProxyType wrap (R9 정합). 2 test green. AST grep gate 10/10 PASS on extracted copy values (R5 정합 — 파일 본문 raw grep 차단, docstring/comment 영향 X). T1 atomic commit 완료.</done>
</task>

<task type="auto" id="09-02-T2" tdd="true">
  <name>Task 09-02-T2: force_pattern.py — 6 signal detection + confidence formula + phase fallback + AST severity guard test + atomic commit</name>
  <files>
    backend/shared/python/sunity_shared/analysis/force_pattern.py,
    backend/tests/phase09/test_infer_force_direction_pattern.py,
    backend/tests/phase09/test_force_pattern_no_severity_use.py
  </files>
  <depends_on>[09-02-T1]</depends_on>
  <threat_ref>T-09-T1 (axis severity misuse), T-09-V5 (input validation), T-09-S1 (confidence fabrication)</threat_ref>
  <behavior>
    - `_phase_metric_confidence_factor(axis, stab) -> float`: cf = min(_CONFIDENCE_TO_FACTOR[axis.confidence], _CONFIDENCE_TO_FACTOR[stab.confidence]). axis or stab is None → fallback `_CONFIDENCE_TO_FACTOR['low']` (=0.3) — never raise (RESEARCH Pitfall 4). **R11 — conservative v1 strategy**: 이 global-min 룰은 contact-only / stability-only finding 도 axis 가 없으면 cf=0.3 으로 capped 된다. 이는 의도적 false-positive 감쇠 — Phase 9 의 모든 finding 은 "Phase 8 신호 신뢰도가 부분적이면 전체 신뢰도도 보수적"이라는 도메인 가정. 향후 source-specific cf (axis-only / contact-only / stability-only) 분리는 v2 작은 plan 으로 분리. T2 test 가 contact-only finding 이 axis 없는 phase 에서 cf=0.3 (= 'low') 로 capped 됨을 명시 검증.
    - `_detect_axis_tilt(axis, phase, cf, mode_context) -> list[ForcePatternFinding]`: axis is None → []. axis.warnings ∩ `_AXIS_IGNORE_WARNINGS_PER_METRIC` ({phase_8_1_wave_0_transitional, tilt_unavailable, tilt_thresholds_fallback}) 비어있지 않으면 [] (R4 정합 — phase-level 검사). Top-level `_AXIS_IGNORE_WARNINGS_REPORT` ({axis_metric_transitional}) hit 은 caller (`infer_force_direction_pattern`) 가 더 일찍 차단하므로 detector 본체는 axis 가 들어왔다고 가정 가능. raw `max(shoulder_tilt or 0.0, hip_tilt or 0.0) > _IPSF_TOLERANCE_DEG` 통과 시 1 finding emit (pattern='release', source_signal='axis_tilt', confidence = min(1.0, 0.72 * cf), joint_hint=joint_hint_for('axis_tilt')='코어', interpretation=force_pattern_canned_text('axis_tilt', mode_context), warnings=[], reason="Axis tilt exceeds IPSF tolerance (20°)").
    - `_detect_pelvis_drop(axis, phase, cf, mode_context)`: axis is None → []. axis warning ignore set 검사 (axis_tilt 와 동일). **R4 (Codex iteration-3) None guard — `axis.shoulder_tilt is None or axis.hip_tilt is None → return []`** (Phase 8.1 `compute_axis_deviation` 가 sample 부재 phase 에서 `shoulder_tilt=None / hip_tilt=None` 박제 가능, `force_signals.py:1132-1153` 확인). guard 없을 시 `hip_tilt > 20.0` 가 `TypeError: '>' not supported between 'NoneType' and 'float'` 발생. `_detect_axis_tilt` 는 `max(.. or 0.0, .. or 0.0)` 박제로 동등 안전. raw 검사 = `hip_tilt > 20.0 AND (hip_tilt - shoulder_tilt) > 10.0` (Assumption A1 / RESEARCH Pitfall 1) → 1 finding (pattern='release', source_signal='pelvis_drop', confidence = min(1.0, 0.72 * cf), joint_hint='고관절', reason="Hip tilt drops below shoulder line by >10° while >20°").
    - `_detect_late_contact(contacts, phase, cf, mode_context)`: phase ∈ {'lock'} 이외 → []. contacts iterate — `(cm.estimated_stable is False) OR (cm.near_pole_ratio is not None AND cm.near_pole_ratio < 0.5)` → 1 finding (pattern='brace', source_signal='late_contact', confidence = min(1.0, 0.70 * cf), joint_hint='내전근', reason="Lock-phase contact unstable or near_pole_ratio < 0.5"). 동일 phase 안 multiple contacts 매치 시 가장 first 1개만 emit (D-09-A3 multiple finding은 다른 signal 박제이므로 contact 안 multiple emit 시 dedup 단계가 처리).
    - `_detect_high_jitter(stab, phase, cf, mode_context)`: stab is None → []. stab.jitter_score is None → []. `stab.jitter_score > force_signals.JITTER_SEVERITY_THRESHOLDS[0]` (=8.0) → 1 finding (pattern='unknown', source_signal='high_jitter', confidence = min(1.0, 0.63 * cf), joint_hint=None, reason="Jitter score exceeds medium threshold 8.0").
    - `_detect_high_jerk(stab, phase, cf, mode_context)`: stab is None → []. stab.jerk_score is None → []. `stab.jerk_score > force_signals.JERK_SEVERITY_THRESHOLDS_DEG_PER_SEC_CUBED[0]` (=5000.0) → 1 finding (pattern='unknown', source_signal='high_jerk', confidence = min(1.0, 0.63 * cf), joint_hint=None, reason="Jerk score exceeds medium threshold 5000 deg/s³").
    - `_detect_abnormal_release(contacts, phase, cf, mode_context)`: 어떤 cm.warnings 라도 `'abnormal_release_during_hold' in (cm.warnings or [])` 일 때 1 finding (pattern='release', source_signal='abnormal_release', confidence = min(1.0, 0.75 * cf), joint_hint='광배', reason="Contact lost during lock/hold phase (abnormal_release_during_hold)").
    - `infer_force_direction_pattern(report, motion_id, mode_context) -> ForcePatternInference`: build per-phase grouped views (`boundaries_by_phase` / `axis_by_phase` / `stability_by_phase` / `contact_by_phase: dict[MotionPhase, list[ContactStabilityMetric]]`). Iterate phase ∈ ('entry', 'lock', 'transition', 'final_shape', 'hold') — phase not in boundaries_by_phase → umbrella warning `'phase_unavailable_for_inference'` 추가 (중복 없이) + continue. axis warning 무시 룰 hit 시 umbrella warning `'axis_signal_unavailable'` 추가 (중복 없이, phase 별 max 1 회). Else run 6 detectors with `cf = _phase_metric_confidence_factor(axis, stab)`. 결과 candidates concat.
    - Apply motion_id boost (× 1.05, cap 1.0) BEFORE ranking via `_apply_motion_id_boost(candidates)` (Pitfall 5).
    - `_rank_top3(candidates)` (Task T3 추가) — 본 task 에서는 stub `_rank_top3 = lambda xs: xs[:3]` placeholder OK; T3 가 정렬 본체 박제. **단**, T2 시점 단위 test 는 ranking 무관 케이스 (단일 phase / 단일 signal / 0-finding fallback / phase 미인식 / confidence 산식) 만 검증. Top-3 정렬 + tie-break + dedup + motion_id boost-순서 검증은 T3 의 책임.
    - 0 finding 시 (`not findings_after_rank`) → umbrella warning `'no_significant_force_pattern_signal'` 추가 + `overall_confidence='low'`. findings 0개 + warning 만 있는 `ForcePatternInference` 반환. interpretation 본문은 본 inference 모듈에서 emit X — frontend / Phase 11 은 fallback body 를 `force_pattern_copy.fallback_body()` 호출로 가져옴.
    - findings ≥ 1 시 `_overall_confidence_from_findings(findings)` = top finding.confidence 기준 → ≥ 0.7 'high' / ≥ 0.4 'medium' / else 'low' (Phase 8 박제 패턴 정합, 단순화).
    - `pattern='rotate'` 회귀 가드: 어떤 detector 도 `'rotate'` 미emit (Open Q 1 — enum 박제만, 자동 검출 X).
  </behavior>
  <action>
**Step A — extend `backend/shared/python/sunity_shared/analysis/force_pattern.py`** (Wave 0 가 박제한 dataclass + 상수 위에 본체 함수 추가).

Add imports (top, after existing):

```python
from .force_signals import (
    AxisDeviationMetric,
    ContactStabilityMetric,
    ForceSignalsReport,
    JITTER_SEVERITY_THRESHOLDS,
    JERK_SEVERITY_THRESHOLDS_DEG_PER_SEC_CUBED,
    PhaseBoundary,
    StabilityMetric,
)
from .force_pattern_copy import (
    fallback_body,
    force_pattern_canned_text,
    joint_hint_for,
)
```

(`fallback_body` import: used by callers — Phase 9 inference 자체는 fallback body 를 finding 으로 박제하지 않음. 단 module-level export 로 helper 노출.)

**Reuse** `_IPSF_TOLERANCE_DEG` (declared in Wave 0 per Plan 09-01 — R6 정합, 중복 정의 X). Detector helpers use the existing constant; do **not** re-add it. If Wave 0 was reverted (rollback scenario), Wave 1 also fails to import — that is the correct behavior since `force_pattern.py` is owned end-to-end by Wave 0+1.

Implement 7 private helpers (`_phase_metric_confidence_factor`, `_detect_axis_tilt`, `_detect_pelvis_drop`, `_detect_late_contact`, `_detect_high_jitter`, `_detect_high_jerk`, `_detect_abnormal_release`) following `<behavior>` semantics. **Critical**: NEVER access `axis.severity` — only `axis.shoulder_tilt`, `axis.hip_tilt`, `axis.confidence`, `axis.warnings` (D-09-A2). `stab.severity` / `cm.severity` 도 본 wave 의 detection 룰이 활용하지 않으므로 접근하지 말 것 — 미래 D-09-A2 guard 확장 시 회귀 차단 (RESEARCH §Anti-Patterns 2번째 항목 정합).

Axis warning surface is **two-tier** (R4 정합 — verified against `backend/shared/python/sunity_shared/analysis/force_signals.py:1670-1677`):

- **Per-axisMetric warnings** (검사 대상 = `axis.warnings`): `phase_8_1_wave_0_transitional` (Wave 0 transitional dataset signal), `tilt_unavailable` (raw tilt 산출 실패), `tilt_thresholds_fallback` (yaml fallback config).
- **Top-level report warning** (검사 대상 = `force_signals_report.warnings`): `axis_metric_transitional` — emitted by Phase 8.1 only when **all** axis metrics carry `phase_8_1_wave_0_transitional`. Plan 9 must short-circuit axis detectors when this top-level signal is present, since at that point no axisMetric is trustworthy.

```python
_AXIS_IGNORE_WARNINGS_PER_METRIC: frozenset[str] = frozenset({
    "phase_8_1_wave_0_transitional",
    "tilt_unavailable",
    "tilt_thresholds_fallback",
})

_AXIS_IGNORE_WARNINGS_REPORT: frozenset[str] = frozenset({
    "axis_metric_transitional",
})
```

`_apply_motion_id_boost(candidates) -> list[ForcePatternFinding]`: returns new list with each finding's `confidence` replaced by `min(1.0, c.confidence * 1.05)`. Since dataclass is frozen, use `dataclasses.replace(c, confidence=...)`. NOTE: caller in this task may pass through unchanged if `motion_id is None`.

`_overall_confidence_from_findings(findings)`:
```python
def _overall_confidence_from_findings(
    findings: list[ForcePatternFinding],
) -> MetricConfidence:
    if not findings:
        return "low"
    top = max(f.confidence for f in findings)
    if top >= 0.7:
        return "high"
    if top >= 0.4:
        return "medium"
    return "low"
```

`infer_force_direction_pattern(report, motion_id=None, mode_context='mode1') -> ForcePatternInference`:

```python
def infer_force_direction_pattern(
    force_signals_report: ForceSignalsReport,
    motion_id: str | None,
    mode_context: ModeContext,
) -> ForcePatternInference:
    """Layer 1 deterministic inference. D-09-A1~A5 / B1~B5 / C2 / D6 정합.

    Layer 2 (Gemini) 영구 차단 — D-09-C1.
    fabrication 금지 — 0 finding 시 빈 list + warning 만 (D-09-B4).
    """
    boundaries_by_phase: dict[str, PhaseBoundary] = {
        b.phase: b for b in force_signals_report.phase_boundaries
    }
    axis_by_phase: dict[str, AxisDeviationMetric] = {
        m.phase: m for m in force_signals_report.axis_metrics
    }
    stability_by_phase: dict[str, StabilityMetric] = {
        m.phase: m for m in force_signals_report.stability_metrics
    }
    contact_by_phase: dict[str, list[ContactStabilityMetric]] = {}
    for cm in force_signals_report.contact_metrics:
        contact_by_phase.setdefault(cm.phase, []).append(cm)

    umbrella_warnings: list[str] = []
    candidates: list[ForcePatternFinding] = []

    # R4 정합 — top-level report axis warning 가드. force_signals.py:1670-1677 가
    # 모든 axisMetric 의 'phase_8_1_wave_0_transitional' 동행 시 top-level
    # 'axis_metric_transitional' emit. 그 경우 axis tier 전체 신뢰 X.
    report_axis_unavailable = any(
        w in _AXIS_IGNORE_WARNINGS_REPORT
        for w in (force_signals_report.warnings or [])
    )
    if report_axis_unavailable:
        umbrella_warnings.append("axis_signal_unavailable")

    for phase in ("entry", "lock", "transition", "final_shape", "hold"):
        if phase not in boundaries_by_phase:
            if "phase_unavailable_for_inference" not in umbrella_warnings:
                umbrella_warnings.append("phase_unavailable_for_inference")
            continue

        axis = axis_by_phase.get(phase)
        stab = stability_by_phase.get(phase)
        contacts = contact_by_phase.get(phase, [])
        cf = _phase_metric_confidence_factor(axis, stab)

        # axis warning ignore 룰 hit 검사 — D-09-A2 / Pitfall 2 / R4 per-metric tier.
        # axis 가 None 이거나 per-metric warnings 무시 set 와 교차 시 axis 계열
        # detector skip + umbrella warning emit. 또한 top-level report 가 unavail
        # 이면 모든 phase 의 axis 도 차단.
        per_metric_blocked = axis is None or any(
            w in _AXIS_IGNORE_WARNINGS_PER_METRIC for w in axis.warnings
        )
        axis_blocked = report_axis_unavailable or per_metric_blocked
        if axis is not None and per_metric_blocked:
            if "axis_signal_unavailable" not in umbrella_warnings:
                umbrella_warnings.append("axis_signal_unavailable")

        if not axis_blocked:
            candidates.extend(_detect_axis_tilt(axis, phase, cf, mode_context))
            candidates.extend(_detect_pelvis_drop(axis, phase, cf, mode_context))

        candidates.extend(_detect_late_contact(contacts, phase, cf, mode_context))
        candidates.extend(_detect_high_jitter(stab, phase, cf, mode_context))
        candidates.extend(_detect_high_jerk(stab, phase, cf, mode_context))
        candidates.extend(_detect_abnormal_release(contacts, phase, cf, mode_context))

    # D-09-C2 motion_id boost — BEFORE ranking (Pitfall 5).
    if motion_id is not None:
        candidates = _apply_motion_id_boost(candidates)

    # Ranking + tie-break + dedup — T3 가 본체 박제. T2 stub: slice top-3 unsorted.
    findings = _rank_top3(candidates)

    if not findings:
        umbrella_warnings.append("no_significant_force_pattern_signal")
        overall: MetricConfidence = "low"
    else:
        overall = _overall_confidence_from_findings(findings)

    return ForcePatternInference(
        version="1.0",
        findings=findings,
        overall_confidence=overall,
        mode_context=mode_context,
        warnings=umbrella_warnings,
    )
```

`_rank_top3` placeholder for T2 only — T3 이 본체 박제:

```python
def _rank_top3(candidates: list[ForcePatternFinding]) -> list[ForcePatternFinding]:
    """Stub for T2 — T3 가 D-09-B2/B3/B5 본체 박제."""
    return candidates[:3]
```

Re-export `infer_force_direction_pattern` in `__all__`.

Cite D-09 IDs in helper docstrings (D-09-A1 / A2 / A4 / A5 / Pitfall 1 / Pitfall 2 / Pitfall 4 / Pitfall 5 / Open Q 1 / Open Q 2).

**Step B — write `backend/tests/phase09/test_infer_force_direction_pattern.py`**:

Imports: `pytest`, `force_pattern.*` (incl frozensets), `force_signals.*` (via conftest helpers). conftest factory `_make_axis_metric` / `_make_stability_metric` / `_make_contact_metric` / `_make_phase_boundary` / `_make_force_signals_report` are Wave 0 박제.

Test cases (≥ 14):

1. `test_six_signals_each_emit_one_finding` — 6 fixture (각 signal 별 ON 조건만 만족 + 다른 5 OFF). 각 케이스 결과 findings 길이 ≥ 1 + (pattern, source_signal) 일치. 5 phase 각각 1 case → axis_tilt @ lock, pelvis_drop @ entry, late_contact @ lock, high_jitter @ hold, high_jerk @ transition, abnormal_release @ hold (다양화).

2. `test_axis_warning_ignore_rule[phase_8_1_wave_0_transitional]` / `[tilt_unavailable]` / `[tilt_thresholds_fallback]` — per-axisMetric warnings 1종 추가 + axis tilt 25° 초과 (raw 검사 통과 조건) → 결과 findings 안 axis_tilt + pelvis_drop finding 부재 + umbrella warnings 안 `axis_signal_unavailable` 포함. Parametrize 3종. (R4: 신규 per-metric warning surface.)
2b. `test_axis_report_warning_blocks_all_phases` — top-level `force_signals_report.warnings = ['axis_metric_transitional']` + 모든 axisMetric tilt 25° 초과 (per-metric warnings 비어있어도) → 결과 findings 안 axis 계열 finding 0회 + umbrella warnings 안 `axis_signal_unavailable` 포함. (R4: 신규 report-tier surface — Phase 8.1 `force_signals.py:1670-1677` 에서 발현되는 시나리오 mirror.)

3. `test_axis_blocked_blocks_pelvis_drop_too` — axis warning 활성 시 pelvis_drop finding 도 미emit (axis 계열 전체 skip).

4. `test_axis_blocked_does_not_block_stability_or_contact` — axis warning + jerk_score > 5000 → axis_tilt 없음 + high_jerk finding emit.

5. `test_phase_unavailable_fallback` — `phase_boundaries=(lock_only,)` (1 phase 만) → 다른 4 phase 에 대해 umbrella warning `phase_unavailable_for_inference` 단일 emit (중복 없이) + lock 의 signal 은 정상 처리. 본 시나리오에서 lock phase 의 signal 모두 OFF → 결과 findings 0 + warnings 안 `phase_unavailable_for_inference` 와 `no_significant_force_pattern_signal` 둘 다 포함.

6. `test_no_signal_emits_fallback` — 모든 phase boundary 박제 + 모든 metric raw 값이 임계 미만 → findings 0 + overall_confidence='low' + warnings 안 `no_significant_force_pattern_signal` 포함. fallback body 는 helper `fallback_body()` 호출로 확인.

7. `test_confidence_formula_low_low` — base 0.72 (axis_tilt) × cf low=0.3 → finding.confidence = pytest.approx(0.216). axisMetric.confidence='low' + stabilityMetric.confidence='low'.

8. `test_confidence_formula_high_high` — base 0.72 × cf high=1.0 → finding.confidence = pytest.approx(0.72). axisMetric.confidence='high' + stabilityMetric.confidence='high'.

9. `test_confidence_formula_takes_min` — axisMetric.confidence='high', stabilityMetric.confidence='low' → cf = 0.3 (min). high_jitter detection (base 0.63) → 0.189.

10. `test_no_rotate_pattern_emitted` (Open Q 1 regression guard) — fixture 가 6 signal ON 모두 → findings 안 `pattern='rotate'` 0 회. Loop over `for f in result.findings: assert f.pattern != 'rotate'`.

11. `test_axis_tilt_boundary_at_ipsf_tolerance` — shoulder_tilt = 20.0 exactly → finding 미emit (strict `>` 비교). shoulder_tilt = 20.1 → finding emit.

12. `test_pelvis_drop_requires_both_conditions` — (hip=25, shoulder=20) → diff = 5, not > 10 → pelvis_drop 미emit. (hip=21, shoulder=10) → diff = 11 but hip not > 20 (21 > 20 PASS but 11 > 10 PASS) → emit. (hip=18, shoulder=5) → hip not > 20 → 미emit. Verify Assumption A1 박제.

12b. `test_pelvis_drop_skips_when_tilt_none` (R4 iter-3 regression guard) — `_make_axis_metric(shoulder_tilt=None, hip_tilt=None, warnings=())` (no `phase_8_1_wave_0_transitional` warning, so D-09-A2 guard does NOT skip — only None tilt does). Verify `_detect_pelvis_drop(axis, ...)` returns `[]` and **does not raise TypeError**. Also verify same fixture run through full `infer_force_direction_pattern(...)` does not crash and emits no pelvis_drop finding.

13. `test_late_contact_only_in_lock_phase` — late_contact fixture (estimated_stable=False) @ phase='entry' → 미emit. @ phase='lock' → emit.

14. `test_high_jitter_threshold_exact_boundary` — jitter_score = 8.0 → 미emit (strict `>`). jitter_score = 8.1 → emit.

15. `test_high_jerk_threshold_exact_boundary` — jerk_score = 5000.0 → 미emit. jerk_score = 5001.0 → emit.

16. `test_abnormal_release_detected_from_contact_warnings` — contactMetric.warnings = ['abnormal_release_during_hold'] → 1 finding (pattern='release', source_signal='abnormal_release', joint_hint='광배').

17. `test_overall_confidence_from_findings` — 1 finding @ confidence=0.8 → overall='high'. 1 finding @ confidence=0.5 → overall='medium'. 1 finding @ confidence=0.2 → overall='low'.

18. `test_contact_only_confidence_capped_when_axis_missing` (R11 — conservative v1 design intent regression guard) — phase='lock', axis_metric is None (axis_blocked from None branch), stabilityMetric.confidence='high', contactMetric.warnings=['abnormal_release_during_hold']. abnormal_release finding base=0.75, cf = min(_CONFIDENCE_TO_FACTOR[None→'low']=0.3, stability=1.0) = 0.3 → finding.confidence = pytest.approx(0.225). 명시 assertion: "axis 가 없으면 contact-only finding 도 cf=0.3 로 capped — Phase 9 v1 conservative strategy (R11)."

**Step C — write `backend/tests/phase09/test_force_pattern_no_severity_use.py`**:

Module docstring cites D-09-A2 / T-09-T1 / [[plan-vs-pivot-cross-check]].

```python
import ast
from pathlib import Path

_REPO_ROOT = Path(__file__).resolve().parents[3]
_MODULE_PATH = _REPO_ROOT / "backend" / "shared" / "python" / "sunity_shared" / "analysis" / "force_pattern.py"


def test_force_pattern_py_does_not_access_axis_severity_via_ast() -> None:
    """D-09-A2 — raw signal only guard. force_pattern.py 안 axisMetric.severity
    접근 0 회. AST walk 로 `ast.Attribute(attr='severity')` 노드 중 receiver
    이름 token 이 axis 계열인 케이스를 reject."""
    src = _MODULE_PATH.read_text("utf-8")
    tree = ast.parse(src)
    axis_receiver_tokens = {"axis", "axis_metric", "axisMetric", "a"}
    violations: list[tuple[int, str]] = []
    for node in ast.walk(tree):
        if isinstance(node, ast.Attribute) and node.attr == "severity":
            # receiver: ast.Name → id, ast.Attribute → 마지막 attr, ast.Call → skip.
            recv = node.value
            recv_name: str | None = None
            if isinstance(recv, ast.Name):
                recv_name = recv.id
            elif isinstance(recv, ast.Attribute):
                recv_name = recv.attr
            if recv_name in axis_receiver_tokens:
                violations.append((node.lineno, ast.unparse(node)))
    assert not violations, (
        f"D-09-A2 위반 — force_pattern.py 안 axis 계열 .severity 접근 발견: "
        f"{violations}"
    )


def test_force_pattern_py_no_axis_severity_substring() -> None:
    """Defense-in-depth — substring grep. axis_metric.severity / axisMetric.severity
    / axis.severity 모두 검출."""
    src = _MODULE_PATH.read_text("utf-8")
    for bad in ("axis.severity", "axis_metric.severity", "axisMetric.severity"):
        assert bad not in src, f"D-09-A2 위반 substring 발견: {bad!r}"


```

**R8 (Codex iteration-3) — third "always-pass" test removed.** 이전 draft 의 `test_stability_severity_access_is_allowed_by_guard` 는 placeholder substring (`"stability.severity_NOT_ALLOWED_PLACEHOLDER"`) 를 체크해서 영구 PASS — 동작 검증이 아니라 noise. 동등한 의도는 `force_pattern.py` 모듈 docstring 1줄로 박제 ("D-09-A2 guard scope = axis only; stability/contact severity 사용 가능") + AST guard test (`test_force_pattern_py_does_not_access_axis_severity`) 의 명시적 receiver whitelist 로 cover.

**Commit:**

```
feat(09-02): Wave 1 T2 — 6 signal detection + confidence formula + phase fallback + AST severity guard

- force_pattern.py: 6 _detect_* helper + _phase_metric_confidence_factor +
  _apply_motion_id_boost + _overall_confidence_from_findings + IPSF_TOL=20.0 +
  infer_force_direction_pattern public entry (Top-3 ranking stub for T3).
- test_infer_force_direction_pattern.py: 17 cases (6 signal + 3 axis warning ignore +
  phase fallback + 0-finding fallback + confidence formula 3 cases + boundary 3 +
  rotate 미산출 회귀 가드 + overall_confidence + late_contact lock 한정 + abnormal_release).
- test_force_pattern_no_severity_use.py: AST + substring guard (axis severity 0 회 +
  stability/contact 허용 sanity).

D-09-A1 / A2 / A4 / A5 / Open Q 1 / Open Q 2 / Pitfall 1 / Pitfall 2 / Pitfall 4 /
[[plan-vs-pivot-cross-check]].

T3 가 Top-3 ranking + tie-break + dedup + motion_id boost-순서 박제 — T2 stub 교체.
```

Pre-commit gate: `cd backend && pytest tests/phase09/test_infer_force_direction_pattern.py tests/phase09/test_force_pattern_no_severity_use.py -x -q`.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; pytest tests/phase09/test_infer_force_direction_pattern.py tests/phase09/test_force_pattern_no_severity_use.py -x -q</automated>
    <automated>cd backend &amp;&amp; python -c "from sunity_shared.analysis.force_pattern import infer_force_direction_pattern, _IPSF_TOLERANCE_DEG, _AXIS_IGNORE_WARNINGS_PER_METRIC, _AXIS_IGNORE_WARNINGS_REPORT; assert _IPSF_TOLERANCE_DEG == 20.0; assert _AXIS_IGNORE_WARNINGS_PER_METRIC == frozenset({'phase_8_1_wave_0_transitional','tilt_unavailable','tilt_thresholds_fallback'}); assert _AXIS_IGNORE_WARNINGS_REPORT == frozenset({'axis_metric_transitional'}); print('OK')"</automated>
    <automated>grep -cE "axis\.severity|axis_metric\.severity|axisMetric\.severity" /Users/kimtaesung/Dev/SunityMotion/backend/shared/python/sunity_shared/analysis/force_pattern.py | grep -E "^0$"</automated>
  </verify>
  <done>force_pattern.py 6 detector + confidence formula + phase fallback + IPSF_TOL=20.0 const + AXIS_IGNORE_WARNINGS frozenset (3 entries) 박제. 18 detection test green (R4 iter-3 추가 test_pelvis_drop_skips_when_tilt_none 포함) + AST severity guard 2 test green (R8 iter-3 — 항상 PASS placeholder 제거). axis severity substring 0 회. T2 atomic commit 완료.</done>
</task>

<task type="auto" id="09-02-T3" tdd="true">
  <name>Task 09-02-T3: Top-3 ranking + tie-break + (pattern, phase) dedup + motion_id boost-before-ranking + atomic commit</name>
  <files>
    backend/shared/python/sunity_shared/analysis/force_pattern.py,
    backend/tests/phase09/test_force_pattern_ranking.py
  </files>
  <depends_on>[09-02-T2]</depends_on>
  <threat_ref>T-09-S1 (confidence fabrication / score 조작)</threat_ref>
  <behavior>
    - `_rank_top3(candidates)` 본체 박제: sort key = `(-score, _PHASE_PRIORITY[phase], _SIGNAL_PRIORITY[source_signal], -confidence)` where `score = confidence * _SIGNAL_WEIGHT[source_signal]`. dedup (pattern, phase) — 동일 조합은 top confidence 1개만 유지. 결과 길이 ≤ 3.
    - Wave 0 박제된 priority/weight 상수 (`_PHASE_PRIORITY` / `_SIGNAL_PRIORITY` / `_SIGNAL_WEIGHT`) 그대로 사용. 본 task 에서 신설하지 말 것.
    - motion_id boost 는 T2 가 이미 `infer_force_direction_pattern` 내부에서 `_rank_top3` 호출 BEFORE 시점에 적용 — 본 task 는 ranking 본체만 박제 + ranking 입력 시점에 boost 가 이미 반영되어 있음을 단위 test 로 검증.
    - Ranking 테스트는 inference 본체 (`infer_force_direction_pattern`) 와 분리해 `_rank_top3` 직접 호출 + 합성 candidate fixture 로 sort key 의 4 stage 모두 검증.
    - Boost-before-ranking 검증: motion_id 켰을 때 vs 껐을 때 Top-3 의 순서가 바뀌는 fixture (RESEARCH Pitfall 5) — `infer_force_direction_pattern` 호출 결과로 검증.
  </behavior>
  <action>
**Step A — replace `_rank_top3` stub in `backend/shared/python/sunity_shared/analysis/force_pattern.py`** with full implementation (per RESEARCH "Top-3 ranking with tie-break" code example):

```python
def _rank_top3(candidates: list[ForcePatternFinding]) -> list[ForcePatternFinding]:
    """Top-3 by score + tie-break (D-09-B2 / B3) + (pattern, phase) dedup (D-09-B5).

    Sort key tuple — 모두 ASC 정렬 (낮을수록 우선):
      0. `-score` (score = confidence × _SIGNAL_WEIGHT[source_signal]) — DESC.
      1. `_PHASE_PRIORITY[phase]` — lock < hold < transition < final_shape < entry.
      2. `_SIGNAL_PRIORITY[source_signal]` — axis < contact < stability.
      3. `-confidence` — DESC (signal_weight 가 동일한 경우 confidence 단독 비교).

    Dedup: (pattern, phase) 조합 동일 시 top confidence 1개만 유지. 동일 pattern
    다른 phase 는 통과 (D-09-B5 정합).
    """
    def key(f: ForcePatternFinding) -> tuple[float, int, int, float]:
        score = f.confidence * _SIGNAL_WEIGHT[f.source_signal]
        return (
            -score,
            _PHASE_PRIORITY[f.phase],
            _SIGNAL_PRIORITY[f.source_signal],
            -f.confidence,
        )

    ranked = sorted(candidates, key=key)
    seen: set[tuple[str, str]] = set()
    result: list[ForcePatternFinding] = []
    for f in ranked:
        sig = (f.pattern, f.phase)
        if sig in seen:
            continue
        seen.add(sig)
        result.append(f)
        if len(result) == 3:
            break
    return result
```

**Step B — write `backend/tests/phase09/test_force_pattern_ranking.py`** (≥ 9 tests):

1. `test_top3_ranking_by_score` — 5 candidate (서로 다른 score) → `_rank_top3` 호출, 결과 순서가 score DESC 정렬되어야 함. 길이 3.

2. `test_signal_weight_overrides_confidence` — 2 candidate: axis_tilt (confidence=0.5, weight=1.0 → score 0.5) vs high_jerk (confidence=0.6, weight=0.85 → score 0.51) → high_jerk first (score 0.51 > 0.5).

3. `test_abnormal_release_weight_priority` — 2 candidate: axis_tilt (cf=0.7, weight=1.0 → 0.7) vs abnormal_release (cf=0.65, weight=1.1 → 0.715) → abnormal_release first.

4. `test_tie_break_phase_priority` — 2 candidate 동일 score (예: axis_tilt @ lock confidence=0.5 vs axis_tilt @ entry confidence=0.5 — score 동일 = 0.5) → lock first (phase priority 0 < entry priority 4).

5. `test_tie_break_signal_priority` — 2 candidate 동일 score + 동일 phase + 동일 signal_weight × confidence 곱 (예: axis_tilt @ lock confidence=0.5 vs late_contact @ lock confidence=0.5/0.95 = 0.526 — score 동일 0.5 by reduce confidence) — fixture 신중하게 박제 후 axis_tilt first (signal_priority axis=0 < contact=1).

6. `test_tie_break_confidence_desc` — 2 candidate score 동일 + phase 동일 + signal_priority 동일. Fixture: high_jerk @ lock confidence=0.6 (score = 0.6 × 0.85 = 0.51) vs high_jitter @ lock confidence=0.6375 (score = 0.6375 × 0.80 = 0.51). 둘 다 stability tier (signal_priority=2, 동일). Tie-break 3단계 → confidence DESC → **`high_jitter` first** (0.6375 > 0.6). **R5 (Codex iteration-3) 수정 — 이전 plan 은 "high_jerk first" 라 했으나 high_jitter 의 confidence 가 더 높음. 실 구현 결과와 일치하도록 expected winner 를 high_jitter 로 박제**. test 의 `assert result.findings[0].source_signal == 'high_jitter'`.

7. `test_pattern_dedup_same_phase` — 2 candidate (pattern='release', phase='lock', confidence=0.7) + (pattern='release', phase='lock', confidence=0.5) → 결과 1개 (top confidence 0.7).

8. `test_pattern_dedup_different_phase_ok` — 2 candidate (pattern='release', phase='lock') + (pattern='release', phase='hold') → 결과 2개 (다른 phase OK).

9. `test_max_three_findings` — 5 candidate 박제 → 결과 길이 정확히 3.

10. `test_motion_id_boost_changes_ranking` — `_make_force_signals_report` fixture 박제 (예: 4 candidate 중 score 미세하게 다른 2개 — boost 켤 때만 순서 바뀜). `infer_force_direction_pattern(report, motion_id=None, ...)` vs `infer_force_direction_pattern(report, motion_id='emerald_lift', ...)` 결과 Top-3 의 첫 finding 이 다른 경우 fixture. 동시에 boost 적용된 candidate 의 confidence 가 `min(1.0, original * 1.05)` 인지 검증.

11. `test_motion_id_boost_caps_at_one` — fixture 에 base × cf = 0.99 finding 박제 → motion_id 켜면 0.99 * 1.05 = 1.0395 → cap 1.0 정합 검증.

12. `test_motion_id_none_no_boost` — motion_id=None → 모든 finding confidence 가 원본 (base × cf) 그대로.

**Commit:**

```
feat(09-02): Wave 1 T3 — Top-3 ranking + tie-break + (pattern, phase) dedup + motion_id boost cap

- force_pattern.py: _rank_top3 본체 (4-stage sort key + (pattern, phase) dedup,
  T2 stub 교체).
- test_force_pattern_ranking.py: 12 cases (score / signal_weight 우선 / 3 stage
  tie-break / dedup 2 case / max 3 / motion_id boost ranking change + cap 1.0 +
  none-noop).

D-09-B2 / B3 / B5 / C2 / Pitfall 5 / Pitfall 6.
```

Pre-commit gate: `cd backend && pytest tests/phase09/test_force_pattern_ranking.py tests/phase09/test_infer_force_direction_pattern.py -x -q` (T2 회귀 0 + T3 ranking 12 PASS).
  </action>
  <verify>
    <automated>cd backend &amp;&amp; pytest tests/phase09/test_force_pattern_ranking.py tests/phase09/test_infer_force_direction_pattern.py tests/phase09/test_force_pattern_no_severity_use.py -x -q</automated>
    <automated>cd backend &amp;&amp; python -c "from sunity_shared.analysis.force_pattern import _rank_top3, ForcePatternFinding; xs = [ForcePatternFinding(pattern='release', phase='lock', source_signal='axis_tilt', reason='r', interpretation='정은지 선수 기준 패턴과 비교했을 때, x', confidence=0.5, joint_hint='코어', warnings=[])]; out = _rank_top3(xs); assert len(out) == 1; print('OK')"</automated>
  </verify>
  <done>_rank_top3 본체 박제 (sort + dedup + cap 3). 12 ranking test + 17 detection test + 3 severity guard test 모두 PASS. T2 회귀 0. T3 atomic commit 완료.</done>
</task>

<task type="auto" id="09-02-T4">
  <name>Task 09-02-T4: pipeline _process wiring + integration test + atomic commit</name>
  <files>
    backend/functions/pipeline/app.py,
    backend/tests/pipeline/test_pipeline_phase9.py
  </files>
  <depends_on>[09-02-T3]</depends_on>
  <threat_ref>T-09-T2 (Firestore nested-array — call path 검증), T-09-V5</threat_ref>
  <action>
**Step A — edit `backend/functions/pipeline/app.py`** at the existing `compute_force_signals` wiring block (verified line 1117-1140).

Add import (top imports — `force_pattern` 추가 위치는 `force_signals as fs` 인근):

```python
from sunity_shared.analysis import force_pattern as fp
```

Insert Phase 9 wiring block immediately AFTER `force_signals_dict = _dataclass_to_camel_case_dict(force_signals_report)` (line 1128) and BEFORE `firestore_admin.complete_analysis(...)` (line 1130):

```python
        # ── Phase 9 (Plan 09-02) — ForcePatternInference 추론 layer ──────
        # D-09-D6: mode_context 산출 inline (별도 helper 신설 X — RESEARCH Open Q,
        # Pattern 6, Pitfall 3 정합). pipeline `_mode3_comparison` 가 산출한
        # comparison["isFirst"] 를 single source 로 재사용 (Phase 12.5
        # build_mode3 패턴 정합 — assemble.py:244).
        #
        # D-09-A2 raw signal only guard 정합 — force_pattern.py 가 axis severity
        # 직접 trust X (AST gate test_force_pattern_no_severity_use.py 회귀 차단).
        # D-09-C1 Layer 2 (Gemini) 영구 차단 — pure-function inference.
        force_pattern_inference_dict: dict | None = None
        if force_signals_report is not None:
            if mode == models.MODE_EXPERT:
                mode_context: fp.ModeContext = "mode1"
            else:  # MODE_SELF
                is_first = (
                    comparison.get("isFirst", True)
                    if isinstance(comparison, dict)
                    else True
                )
                mode_context = "mode3_first" if is_first else "mode3_progress"

            force_pattern_inference = fp.infer_force_direction_pattern(
                force_signals_report,
                motion_id=getattr(profile, "motion_id", None),
                mode_context=mode_context,
            )
            force_pattern_inference_dict = _dataclass_to_camel_case_dict(
                force_pattern_inference
            )
        # ────────────────────────────────────────────────────────────────
```

Then extend the existing `firestore_admin.complete_analysis(...)` call (line 1130-1140) — add the new kwarg at the bottom of the kwargs list:

```python
        firestore_admin.complete_analysis(
            uid,
            analysis_id,
            result,
            angles=np.asarray(angles, dtype=float).reshape(-1).tolist(),
            angles_joint_keys=list(skeleton.JOINT_KEYS),
            angles_frames=int(np.asarray(angles).shape[0]),
            body_comparison_report=body_comparison_report_dict,
            body_normalization_profile=body_normalization_profile_dict,
            force_signals_report=force_signals_dict,
            force_pattern_inference=force_pattern_inference_dict,  # Phase 9 (Plan 09-02)
        )
```

No other changes to pipeline. `force_signals_report is None` 분기는 사실상 발생 안 함 (Phase 8 가 항상 dataclass 반환) but 방어 코드 박제 — RESEARCH Pitfall 외 보강.

**Step B — write `backend/tests/pipeline/test_pipeline_phase9.py`** (R8 정합 — actual file `backend/tests/pipeline/test_pipeline_phase8.py` already exists; mirror its fixture style and `_process` mock infrastructure directly so the wrong-constant bug class (R3) is caught by a real `_process` invocation):

Strategy: real `_process` invocation under monkeypatched I/O — copy the fixture pattern from `backend/tests/pipeline/test_pipeline_phase8.py`. Required monkeypatches (mirror the Phase 8 test verbatim where possible):

- `firestore_admin.complete_analysis` → capture all kwargs (return None).
- `fs.compute_force_signals` → return synthetic `ForceSignalsReport` fixture from `backend/tests/phase09/conftest.py`.
- `fp.infer_force_direction_pattern` 는 **실 호출** (검증 핵심 — wrong-MODE_*, axis-warning, mode-context bugs 모두 통과 가능).
- 그 외 pipeline 의 dependency (S3 download, NLF pose, Cerebras LLM) 는 Phase 8 test 가 박제한 패턴 그대로 재사용.

**Do not** extract a Phase 9 wiring helper from `_process`; reuse the existing `_process` entrypoint. **Do not** build a parallel mini-helper that mirrors the wiring logic — that would not have caught R3 (`MODE_REFERENCE`) because the mirror could embed the same typo.

**Required test cases (5)** — all run against the **real** `_process` entrypoint, no helper extraction:

1. `test_mode1_path_emits_mode1_context` — `mode == MODE_EXPERT` → captured `complete_analysis` kwargs include `force_pattern_inference.modeContext == "mode1"`.
2. `test_mode3_first_path_emits_mode3_first_context` — `mode == MODE_SELF` + `comparison["isFirst"] == True` → `modeContext == "mode3_first"`.
3. `test_mode3_progress_path_emits_mode3_progress_context` — `mode == MODE_SELF` + `comparison["isFirst"] == False` → `modeContext == "mode3_progress"`.
4. `test_complete_analysis_invocation_receives_kwarg` — `complete_analysis` called with `force_pattern_inference=` kwarg present + camelCase keys (sourceSignal / jointHint / overallConfidence / modeContext).
5. `test_force_signals_none_path_passes_none` — `compute_force_signals` monkeypatched to return `None` → `force_pattern_inference=None` passed through (no-op branch).

**Single implementation guidance (R5 — Codex iteration-2): copy `backend/tests/pipeline/test_pipeline_phase8.py` pattern 1:1.** It already imports the pipeline module, monkeypatches `_extract_video_analysis_inputs`, captures `complete_analysis`, and exercises the real `_process` flow. Reuse the same fixture style and patch points; add Phase 9-specific assertions.

**Do NOT:**
- Extract a `_compute_force_pattern_inference_dict` helper to make the test easier — that defeats R3/R8 (the wiring bug class only surfaces in the real `_process` invocation).
- Search for `backend/tests/phase08/test_pipeline_force_signals_wiring.py` — it does not exist (R5 stale reference removed). The real reference is `backend/tests/pipeline/test_pipeline_phase8.py`.
- Mock `fp.infer_force_direction_pattern` itself — let it run for real on the synthetic `ForceSignalsReport` fixture so the modeContext / camelCase / kwarg contract is exercised end-to-end.

5 case 모두 PASS 시 commit.

**Commit:**

```
feat(09-02): Wave 1 T4 — pipeline _process wiring + integration test

- pipeline/app.py: compute_force_signals 직후 mode_context inline (mode1 /
  mode3_first / mode3_progress) + fp.infer_force_direction_pattern + camelCase
  변환 + complete_analysis(force_pattern_inference=) kwarg. force_signals_report
  is None 일 때 wiring no-op (kwarg=None).
- tests/pipeline/test_pipeline_phase9.py: 5 integration case (mode1 / mode3_first /
  mode3_progress / kwarg capture + camelCase 변환 / force_signals None no-op).
  Mirrors backend/tests/pipeline/test_pipeline_phase8.py fixture style (R8 — real
  _process invocation, no mini-helper).

D-09-D6 / Pitfall 3 / Pattern 6.

Wave 0 의 _validate_force_pattern_inference scoped validator 가 Firestore 저장 시점
nested-array 차단 (T-09-T2 회귀 0).
```

Pre-commit gate: `cd backend && pytest tests/pipeline/test_pipeline_phase9.py -x -q`.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; pytest tests/pipeline/test_pipeline_phase9.py -x -q</automated>
    <automated>grep -cE "infer_force_direction_pattern" /Users/kimtaesung/Dev/SunityMotion/backend/functions/pipeline/app.py | awk '$1>=1{print "OK"; exit 0} {exit 1}'</automated>
    <automated>grep -cE "force_pattern_inference=" /Users/kimtaesung/Dev/SunityMotion/backend/functions/pipeline/app.py | awk '$1>=1{print "OK"; exit 0} {exit 1}'</automated>
  </verify>
  <done>pipeline `_process` 가 mode_context inline + infer_force_direction_pattern 호출 + complete_analysis(force_pattern_inference=) kwarg 박제. 5 integration test PASS. T4 atomic commit 완료.</done>
</task>

<task type="auto" id="09-02-T5">
  <name>Task 09-02-T5: Wave 1 종료 회귀 gate + VALIDATION.md flip + atomic commit</name>
  <files>.planning/phases/09-forcedirectionpattern-3/09-VALIDATION.md</files>
  <depends_on>[09-02-T4]</depends_on>
  <threat_ref>—</threat_ref>
  <action>
**Step A — full regression gate (commit X 단계, 실행만):**

```
cd backend && pytest tests/phase09/ tests/pipeline/test_pipeline_phase9.py -x -q
cd backend && pytest tests/phase06/ tests/phase07/ tests/phase08/ tests/phase08_1/ -x -q
cd app && npm run typecheck
cd backend && pytest tests/ -x -q   # 전체 회귀 final
```

각 단계 0 fail 검증. Fail 발생 시 본 task 중단 + executor 가 root cause 박제 후 fix-forward commit (별도 task 신설 가능).

**Step B — edit `.planning/phases/09-forcedirectionpattern-3/09-VALIDATION.md`** frontmatter:

```yaml
---
phase: 09
slug: forcedirectionpattern-3
status: draft
nyquist_compliant: true     # ← flip (Plan 09-02 종료 박제)
wave_0_complete: true        # ← flip (Wave 1 종료 시점에 Wave 0 도 commit 완료)
created: 2026-06-10
---
```

추가로 본 wave 의 task ID 가 seeded IDs (`09-W1-I1..I4`, `R1..R5`, `C1..C3`, `P1..P2`, `F1`) 와 일대일 매핑 — 본 plan 은 task ID 를 `09-02-T1..T5` 로 박제. VALIDATION.md 표의 task ID column 은 seeded 유지 (변경 X — orchestrator 의 reference 정합). 본 plan 의 task ID 와 VALIDATION.md 의 task ID 는 별도 namespace (orchestrator 가 validator gate 시 양쪽 모두 인식).

VALIDATION.md 본문 표는 변경 X. 단 sign-off 섹션의 체크박스를 갱신:

```markdown
## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 90s (full suite + tsc)
- [x] `nyquist_compliant: true` set in frontmatter (planner 박제 완료 후)

**Approval:** Wave 1 complete — verifier 진입 OK.
```

**Step C — commit:**

```
docs(09-02): Wave 1 close-out — VALIDATION.md nyquist_compliant + wave_0_complete flip

전 회귀 gate PASS:
- pytest tests/phase09/ tests/pipeline/test_pipeline_phase9.py -x -q  → all Wave 0 + Wave 1 test green (R7 iter-3 — pipeline test included in fast gate)
- pytest tests/phase06/ tests/phase07/ tests/phase08/ tests/phase08_1/ -x -q  → 회귀 0
- pytest tests/ -x -q  → 전체 회귀 0
- cd app && npm run typecheck  → 0 error
- 금지 표현 grep gate 10/10 PASS (8 substring + 2 regex: `근육 힘 방향.*확정`, `\d+%\s*감점`)

Phase 9 ForcePatternInference E2E vertical slice 완성 — Firestore
result.forcePatternInference 가 실 데이터로 흘러 Phase 11 / 12 가 consume 가능.

Wave 2 production sweep 영구 OUT (D-09-E2) — Phase 11 통합 시점 자연 검증 + Phase 15
sweep. 본 commit 후 follow-up:
  - belle 박제 검수 (18 canned + jointHint + pelvis_drop 임계)
  - Codex cross-AI plan-review (D-09-A2 raw guard + D-09-C1 차단 + D-09-D3 gate)
```
  </action>
  <verify>
    <automated>cd backend &amp;&amp; pytest tests/phase09/ tests/pipeline/test_pipeline_phase9.py -x -q</automated>
    <automated>cd backend &amp;&amp; pytest tests/phase06/ tests/phase07/ tests/phase08/ tests/phase08_1/ -x -q</automated>
    <automated>cd backend &amp;&amp; pytest tests/ -x -q</automated>
    <automated>cd app &amp;&amp; npm run typecheck</automated>
    <automated>grep -E "^nyquist_compliant: true$" /Users/kimtaesung/Dev/SunityMotion/.planning/phases/09-forcedirectionpattern-3/09-VALIDATION.md | head -1 | grep -q "true" &amp;&amp; echo "OK"</automated>
    <automated>grep -E "^wave_0_complete: true$" /Users/kimtaesung/Dev/SunityMotion/.planning/phases/09-forcedirectionpattern-3/09-VALIDATION.md | head -1 | grep -q "true" &amp;&amp; echo "OK"</automated>
  </verify>
  <done>전 회귀 gate green (phase09 + phase06/07/08/08.1 + full backend + tsc). VALIDATION.md frontmatter 2 flag flip. T5 atomic commit 완료. Phase 9 verifier 진입 OK.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `compute_force_signals(...)` → `infer_force_direction_pattern(...)` | Phase 8 산출 ForceSignalsReport 가 Phase 9 inference 의 단일 입력. frozen dataclass 라 caller-controlled mutation 0. 단 ForceSignalsReport 내부 metric (`warnings` 등) 의 의미적 신뢰는 Phase 8 본체에 위임. |
| `infer_force_direction_pattern(...)` → `_validate_force_pattern_inference(...)` (Wave 0) → Firestore | dataclass → dict (camelCase 변환) → Firestore scoped validator (Wave 0 박제) 가 nested-array / list[non-whitelist] 차단. T-09-T2 회귀 0 보장. |
| `_FORCE_PATTERN_COPY` dict literal singleton → finding.interpretation | 18 canned 본문은 모듈-level `MappingProxyType(_FORCE_PATTERN_COPY_DATA)` wrap (R8/R9 정합 — runtime mutation 차단, narrow scope: 본 1 dict 만). `_MODE_PREFIX` / `_JOINT_HINT_BY_SIGNAL` 은 plain dict (lookup만 / caller-input X). AST grep gate (test_force_pattern_copy_no_forbidden.py) 가 단정 표현 회귀 차단. |
| `pipeline._process` mode_context 산출 → `infer_force_direction_pattern(..., mode_context=)` | `comparison["isFirst"]` 가 single source (Phase 12.5 build_mode3 박제). isFirst misalign 시 카피 분기 오류 — 단위 test (T4) 가 3 case 검증. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-T1 | Tampering | `force_pattern.py` axis severity misuse (D-09-A2 위반) | mitigate | T2: AST gate `test_force_pattern_no_severity_use.py` 가 `axis*.severity` 접근 0 회 검증. substring gate 가 raw text grep. 회귀 시 즉시 fail. (Codex C-M4 / Phase 8.1 D-05 정합) |
| T-09-T2 | Tampering | Firestore nested-array tampering | mitigate | Wave 0 의 `_validate_force_pattern_inference` scoped validator 가 list[list] / nested dict / list[non-whitelist] 차단. T4 integration test 가 call path 검증 (force_pattern_inference kwarg → complete_analysis → validator 가 실 호출). |
| T-09-T3 | Tampering | canned 본문 escape / inject | mitigate | `_FORCE_PATTERN_COPY` 는 `MappingProxyType(_FORCE_PATTERN_COPY_DATA)` 로 runtime mutation 차단 (R8/R9 narrow scope). lookup helper 가 caller-controlled input 받지 않음 (Literal enum 검증 후 lookup). AST grep gate 가 dev-time 회귀 차단. |
| T-09-V5 | Input Validation (ASVS V5) | ForceSignalsReport → ForcePatternFinding 변환 | mitigate | Wave 0 의 frozen dataclass `__post_init__` validator (enum / confidence [0,1] / interpretation non-empty / findings length ≤ 3). T2/T3 test 가 경계 케이스 (boundary tilt / threshold) 검증. |
| T-09-S1 | Spoofing | confidence fabrication / 외부 강제 0.99 | mitigate | confidence 산출 = base × cf × motion_id_boost — 모두 internal source. caller-controlled 항목 0. T3 test 가 motion_id boost cap 1.0 검증, fabrication 0 회 가드. |
| T-09-R1 | Repudiation | refactor 시 단정 표현 회귀 ("X% 감점" / "근육 힘 방향이 확정" 등) | mitigate | T1: AST grep gate 10/10 (8 substring + 2 regex: `근육 힘 방향.*확정` + `\d+%\s*감점`). regex `.*` 가 조사 변형 catch — SC#4 직접 정합. 회귀 시 CI fail. [[no-baekje-filler]] + [[mode3-progress-not-similarity]] 정합. |
| T-09-SC | Supply chain | npm/pip install | accept | 신규 install 0 (RESEARCH §Package Legitimacy Audit — Phase 9 의존성 모두 기존). slopcheck N/A. |

</threat_model>

<verification>

**Per-task automated gates:** 위 각 task 의 `<verify><automated>` 블록 — 명시적 pytest 명령 + grep sanity.

**Wave 1 종료 gate (T5):**

- `cd backend && pytest tests/phase09/ tests/pipeline/test_pipeline_phase9.py -x -q` — Wave 0 (4 test) + Wave 1 신설 6 test = **10 test green**.
- `cd backend && pytest tests/phase06/ tests/phase07/ tests/phase08/ tests/phase08_1/ -x -q` — Phase 6/7/8/8.1 회귀 0.
- `cd backend && pytest tests/ -x -q` — 전체 backend 회귀 0.
- `cd app && npm run typecheck` — `tsc --noEmit` 0 error.
- 금지 표현 grep gate 10/10 PASS (8 substring + 2 regex: `근육 힘 방향.*확정`, `\d+%\s*감점`).
- AST severity guard PASS — `axis*.severity` 접근 0 회.
- VALIDATION.md frontmatter `nyquist_compliant: true` + `wave_0_complete: true`.

**Wave 1 종료 후 belle 박제 검수 (`<follow_ups>` 참조):**

- 18 canned KO interpretation 본문 톤 (가능성 언어 + 부위 어휘 + 강사 보조).
- jointHint 부위 어휘 매핑 (Assumption A3).
- pelvis_drop 임계 정량값 (Assumption A1 — 10° / 20°).

검수 결과 변경 시 `force_pattern_copy.py` dict literal 또는 `force_pattern.py` `_detect_pelvis_drop` 임계 1줄 갱신 + AST gate / detection test 재실행. 별도 작은 plan 으로 박제 (본 plan 의 scope 외).

</verification>

<success_criteria>

- [ ] `backend/shared/python/sunity_shared/analysis/force_pattern.py` 본체 완성 — `infer_force_direction_pattern` public + 6 `_detect_*` + `_phase_metric_confidence_factor` + `_apply_motion_id_boost` + `_rank_top3` (4-stage sort + dedup) + `_overall_confidence_from_findings` + reuse Wave 0 `_IPSF_TOLERANCE_DEG = 20.0` (R6 — 중복 정의 금지) + `_AXIS_IGNORE_WARNINGS_PER_METRIC` + `_AXIS_IGNORE_WARNINGS_REPORT` (R4 정합 two-tier).
- [ ] `backend/shared/python/sunity_shared/analysis/force_pattern_copy.py` 신설 — 18 canned + 3 mode prefix + 6 jointHint + 1 fallback body + 8 forbidden substring + 2 forbidden regex (`근육 힘 방향.*확정`, `\d+%\s*감점`) + 3 lookup helper.
- [ ] `backend/functions/pipeline/app.py::_process` — `compute_force_signals(...)` 직후 mode_context inline + `infer_force_direction_pattern(...)` + camelCase 변환 + `complete_analysis(force_pattern_inference=...)` kwarg 박제.
- [ ] `backend/tests/phase09/test_infer_force_direction_pattern.py` — 17 test green (6 signal + 3 axis warning ignore + 1 axis blocks pelvis + 1 axis allows stability+contact + phase 미인식 + 0 finding fallback + 3 confidence formula + 1 rotate regression + 3 boundary + late_contact lock-only + abnormal_release + overall_confidence).
- [ ] `backend/tests/phase09/test_force_pattern_ranking.py` — 12 test green (score / signal_weight / 3 stage tie-break / dedup 2 / max 3 / motion_id boost change + cap + none-noop).
- [ ] `backend/tests/phase09/test_force_pattern_copy_render.py` — 7 test green (18 entries + Cartesian / mode prefix / mode prefix consistency invariant / lookup helper / jointHint / fallback body / body length floor).
- [ ] `backend/tests/phase09/test_force_pattern_copy_no_forbidden.py` — 8 substring parametrize (PASS) + 2 regex parametrize (`근육 힘 방향.*확정`, `\d+%\s*감점`) + AST sanity (≥ 22 strings).
- [ ] `backend/tests/phase09/test_force_pattern_no_severity_use.py` — 2 test green (AST guard + substring guard). R8 iter-3 — third "always-pass" placeholder test 제거; guard scope (axis only) 는 모듈 docstring 박제.
- [ ] `backend/tests/pipeline/test_pipeline_phase9.py` — 5 test green (mode1 / mode3_first / mode3_progress / complete_analysis kwarg capture + camelCase / force_signals None no-op).
- [ ] `cd backend && pytest tests/phase06/ tests/phase07/ tests/phase08/ tests/phase08_1/ -x -q` — Phase 6/7/8/8.1 회귀 0.
- [ ] `cd backend && pytest tests/ -x -q` — backend 전 회귀 0.
- [ ] `cd app && npm run typecheck` — `tsc --noEmit` 0 error.
- [ ] `.planning/phases/09-forcedirectionpattern-3/09-VALIDATION.md` frontmatter `nyquist_compliant: true` + `wave_0_complete: true`.
- [ ] Wave 1 = 5 atomic commits (T1 / T2 / T3 / T4 / T5). 부분 rollback OK — Wave 1 not atomic per plan.

</success_criteria>

<follow_ups>

### Wave 1 commit 직후 — belle 박제 검수 (필수)

- **Trigger:** T5 commit + 전 회귀 gate PASS.
- **Action:** belle 가 (1) 18 canned KO interpretation 본문 톤 + 부위 어휘 정합성, (2) jointHint 부위 매핑 (axis_tilt='코어', pelvis_drop='고관절', late_contact='내전근', abnormal_release='광배', high_jerk/high_jitter=None) 정합성, (3) pelvis_drop 임계값 `(hip_tilt - shoulder_tilt) > 10° AND hip_tilt > 20°` 의 정은지 25 sample 분포 정합성 검수.
- **Boundary:** 변경 시 `force_pattern_copy.py` dict literal 또는 `force_pattern.py` `_detect_pelvis_drop` 임계 1줄 갱신 + AST gate / detection test 재실행. 별도 작은 plan (예: 09-03-PLAN.md) 으로 박제 가능 — 본 plan scope 외.
- 박제 메모리 정합: [[no-baekje-filler]] (canned 본문 안 "박제" 단어 0 회 — AST gate 가 차단하지 않지만 belle 검수 단계에서 확인 필요).

### Wave 1 commit 직후 — Codex cross-AI plan-review (강력 권장)

- **Trigger:** belle 검수 PASS 후.
- **Action:** Codex (`/plan-review-convergence` 또는 유사 cross-AI 검토) 가 (1) D-09-A2 raw signal guard 정합 (force_pattern.py AST 안 axis severity 접근 0 회), (2) D-09-C1 Layer 2 (Gemini) 차단 정합 (코드 안 Gemini / Cerebras / Layer 2 호출 0 회), (3) D-09-D3 grep gate 정합 (8 substring + 2 regex `근육 힘 방향.*확정` + `\d+%\s*감점` 가 실제 단정 표현 회귀 차단)을 검증.
- **Boundary:** Codex 가 narrow gate 잡아내는 사례 ([[cross-ai-plan-review-good]] 박제 / [[codex-reviewer-smplx-bias]] 박제 — 종료 시점 = belle 의 "그냥 반영하고 가자" 신호). 큰 plan 후 간간히 사용 권장.

### Phase 11 통합 시점 — 자연 검증 (out-of-scope reminder)

- **Trigger:** Phase 11 (CoachCommentHook + Gemini 자연어 번역) 진입.
- **Action:** mode1/mode3 실 영상에서 Phase 9 finding → Phase 11 LLM 풍부화 → 결과 화면 노출 정합성 동시 확인. Phase 9 의 fabrication 0 / overall_confidence 분포 / mode 분기 카피 정합 자연 검증.
- **Boundary:** 본 plan 의 책임 아님 (D-09-E2 영구 OUT). Phase 9 verifier 통과 = 본 wave 종료.

### Phase 15 통합 시점 — production sweep (out-of-scope reminder)

- **Trigger:** Phase 15 (Mode 1·Mode 3 실영상 + 신뢰도 게이트 + TestFlight).
- **Action:** 정은지 + 학생 영상 sweep → Phase 9 finding 분포 검증 (정은지 = 대부분 0~1 finding low confidence + 학생 = 1~3 finding medium confidence). 위양성 검증.
- **Boundary:** 본 plan 의 책임 아님 (D-09-E2 영구 OUT).

</follow_ups>

<rollback>

Wave 1 = 5 atomic commits. Per-task individual revert OK (Wave 1 not atomic).

Rollback 순서 (역순):

1. **T5 revert** — VALIDATION.md frontmatter flag 복원만. 코드 회귀 0.
2. **T4 revert** — pipeline `_process` 의 Phase 9 wiring 블록 + `complete_analysis(force_pattern_inference=...)` kwarg 제거. Wave 0 의 `force_pattern_inference: dict | None = None` default 가 시그니처 유지 — Phase 8 회귀 0.
3. **T3 revert** — `_rank_top3` 본체를 T2 stub (`return candidates[:3]`) 로 복원. ranking 테스트 fail 예상.
4. **T2 revert** — 6 `_detect_*` helper + `infer_force_direction_pattern` 본체 + `_AXIS_IGNORE_WARNINGS_PER_METRIC` + `_AXIS_IGNORE_WARNINGS_REPORT` 제거. Wave 0 의 `_IPSF_TOLERANCE_DEG` 는 그대로 둠 (R6 — Wave 0 자산).
5. **T1 revert** — `force_pattern_copy.py` 전체 삭제 + 2 test 삭제. Wave 0 schema 만 남음 — `force_pattern.py` import 가 `force_pattern_copy` 에 의존 시 fail (T2 이미 revert 시 OK).

부분 rollback 시 단위 test fail 정합 — fix-forward 권장. Wave 0 (Plan 09-01) 의 schema + Firestore validator + TS interface 는 본 wave revert 와 무관 (Wave 0 atomic commit 단독 revert 는 본 plan 외).

</rollback>

<output>
Create `.planning/phases/09-forcedirectionpattern-3/09-02-SUMMARY.md` when done (Phase 8 / 8.1 SUMMARY 박제 패턴 정합 — Wave 1 vertical slice 완성 + 5 atomic commit + 전 회귀 gate green + follow-up belle 검수 정합).
</output>
