---
phase: 10-injury-risk-flags
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/shared/python/sunity_shared/analysis/safety_flags.py
  - app/src/types/analysis.ts
  - backend/shared/python/sunity_shared/models.py
  - docs/contract.md
  - app/src/theme/colors.ts
  - backend/tests/phase10/__init__.py
  - backend/tests/phase10/conftest.py
  - backend/tests/phase10/test_safety_flags_dataclass.py
  - backend/tests/phase10/test_safety_flags_firing_rule.py
  - backend/tests/phase10/test_safety_flags_hyperextension.py
  - backend/tests/phase10/test_safety_flags_asymmetry.py
  - backend/tests/phase10/test_safety_flags_level.py
  - backend/tests/phase10/test_safety_flags_contract.py
autonomous: true
requirements: [SAFE-01]

must_haves:
  truths:
    - "SafetyFlag type exists identically in all three contract mirrors (analysis.ts, models.py, contract.md)"
    - "safety_flags.compute_safety_flags(...) is importable and returns a list (empty stub at this wave)"
    - "phase10 test suite collects cleanly; dataclass enum tests are GREEN; ALL no-flag invariants are GREEN against the empty stub (every case whose correct answer is [] — elite-no-FP, determinism, posture-without-control→none, AND-gate-temporal-disjoint→none, flexed-knee→none, ambiguous-geometry→none, equal-asymmetry→none, reference=None→none, Mode-3-level→none); ONLY positive future-behavior tests (a flag SHOULD fire) carry xfail(strict=True) until their wave lands"
    - "warnAmberBg token exists in colors.ts; brand #FF4B33 unchanged"
    - "Existing LLM CoachingTipDetail.injuryRisk is NOT modified (D-01 boundary)"
    - "from sunity_shared.models import SafetyFlag imports without a circular-import error"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/safety_flags.py"
      provides: "SafetyFlag frozen dataclass + enum guards + compute_safety_flags stub"
      contains: "class SafetyFlag"
    - path: "app/src/types/analysis.ts"
      provides: "SafetyFlag interface + safetyFlags?: SafetyFlag[] | null on AnalysisResult"
      contains: "SafetyFlag"
    - path: "backend/shared/python/sunity_shared/models.py"
      provides: "SafetyFlag re-export + enum tuples"
      contains: "SafetyFlag"
    - path: "docs/contract.md"
      provides: "SafetyFlag contract section"
      contains: "SafetyFlag"
    - path: "app/src/theme/colors.ts"
      provides: "warnAmberBg amber tint token"
      contains: "warnAmberBg"
    - path: "backend/tests/phase10/conftest.py"
      provides: "elite-no-FP fixture (정은지 angles + low-control-loss report) + window-disjoint posture/instability fixture + timing-shifted same-extreme reference fixture + synthetic (T,17,4) flexed/reverse-bent + ambiguous-geometry hyperextension arrays"
      contains: "def "
  key_links:
    - from: "backend/shared/python/sunity_shared/models.py"
      to: "backend/shared/python/sunity_shared/analysis/safety_flags.py"
      via: "bottom-of-file re-export (# noqa: E402, F401)"
      pattern: "from .analysis.safety_flags import"
    - from: "backend/tests/phase10/test_safety_flags_dataclass.py"
      to: "sunity_shared.analysis.safety_flags.SafetyFlag"
      via: "import + pytest.raises enum validation"
      pattern: "from sunity_shared.analysis.safety_flags import"
---

<objective>
Establish the Phase 10 foundation: the `SafetyFlag` shared contract (3-mirror), a pure `safety_flags.py` module scaffold (frozen dataclass + enum guards + `compute_safety_flags(...)` stub returning `[]`), the additive `warnAmberBg` UI token, and the full `backend/tests/phase10/` Nyquist test scaffold with the elite-no-FP, window-disjoint (temporal-alignment), timing-shifted-same-extreme (DTW-alignment), and synthetic/ambiguous hyperextension fixtures. This is the MVP "failing end-to-end test" foundation: ALL no-flag invariants land GREEN here (the empty stub already returns `[]`, which IS the correct answer for every no-flag case), while only positive firing-rule tests (a flag SHOULD fire) are `xfail(strict=True)` and flip to GREEN as each subsequent slice implements its rule.

This plan implements the data-structure half of D-01 (separate deterministic SafetyFlag layer, independent of the LLM `injuryRisk` prose) and respects the Claude's-Discretion contract-3-mirror rule from 10-CONTEXT.md. No firing logic yet — the AND-gate (D-02) and per-flag rules (D-03/04/05/06) arrive in later waves.

Purpose: every later slice needs the shared type, the module to extend, and the test scaffold (GREEN no-flag gates + xfail positive-behavior contracts) that defines correct behavior. Building them once, atomically, de-risks all four slices.
Output: `safety_flags.py` (stub), contract mirrors, `warnAmberBg`, and 6 test files + fixtures with explicit GREEN/xfail state.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-injury-risk-flags/10-CONTEXT.md
@.planning/phases/10-injury-risk-flags/10-RESEARCH.md
@.planning/phases/10-injury-risk-flags/10-PATTERNS.md
@.planning/phases/10-injury-risk-flags/10-VALIDATION.md
@.planning/phases/10-injury-risk-flags/10-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: safety_flags.py scaffold — SafetyFlag frozen dataclass + enum guards + compute_safety_flags stub</name>
  <files>backend/shared/python/sunity_shared/analysis/safety_flags.py, backend/tests/phase10/__init__.py, backend/tests/phase10/test_safety_flags_dataclass.py</files>
  <read_first>
    - backend/shared/python/sunity_shared/analysis/kismam.py:1-124 (module-header [CITED]/[ASSUMED] tagging convention + score_from_deviation pure-function style — 10-PATTERNS.md "safety_flags.py" section)
    - backend/shared/python/sunity_shared/analysis/force_signals.py:401-437 (StabilityMetric frozen dataclass + __post_init__ enum guard + _SEVERITY_LEVELS tuple — the dataclass pattern to mirror)
    - backend/shared/python/sunity_shared/analysis/skeleton.py:39-50 (JOINT_ANGLES / JOINT_KEYS the module will consume in later waves — note: unstable_body_parts keys are exactly these names: left_knee/right_knee/left_elbow/right_elbow/left_hip/right_hip)
    - backend/tests/phase09/test_force_pattern_dataclass.py:1-44 (_kwargs factory + pytest.raises(ValueError, match=...) unit-test pattern)
    - backend/tests/conftest.py (confirms shared/python is already on sys.path — no path hacks)
    - .planning/phases/10-injury-risk-flags/10-RESEARCH.md (Pattern 1 — exact SafetyFlag field list)
  </read_first>
  <behavior>
    - SafetyFlag(flag_type='trunk_hyperextension', severity='medium', confidence='low', mode_scope='both', body_region='허리', posture_condition='...', control_loss_signal='...') constructs successfully.
    - SafetyFlag(flag_type='nonsense', ...) raises ValueError matching "flag_type".
    - SafetyFlag(severity='extreme', ...) raises ValueError matching "severity".
    - SafetyFlag(mode_scope='mode3_only', ...) raises ValueError matching "mode_scope".
    - compute_safety_flags(...) is importable and returns [] for any input (stub).
  </behavior>
  <action>
    Create `backend/shared/python/sunity_shared/analysis/safety_flags.py` following the kismam/force_signals analog (10-PATTERNS.md). Open with a module docstring stating purpose (deterministic injury-risk SafetyFlag layer, D-01 separate from LLM injuryRisk) and a threshold-provenance note tagging future cutoffs [CITED]/[ASSUMED] per D-07 / [[scoring-redesign-must-generalize-no-overfit]] — no real thresholds yet, but document the rule. Use `from __future__ import annotations`, relative imports, numpy-only.
    Define module-level enum tuples: `_FLAG_TYPES = ('asymmetry','trunk_hyperextension','joint_hyperextension','level_mismatch')`, `_SEVERITY_LEVELS = ('low','medium','high')`, `_CONFIDENCE_LEVELS = ('low','medium','high')`, `_MODE_SCOPES = ('both','mode1_only')`.
    Define `@dataclass(frozen=True) class SafetyFlag` with scalar-only fields exactly per RESEARCH Pattern 1: `flag_type: str`, `body_region: str`, `severity: str`, `posture_condition: str`, `control_loss_signal: str`, `confidence: str`, `mode_scope: str`. NO nested lists (Firestore ban, Pitfall 1). Add `__post_init__` validating flag_type/severity/confidence/mode_scope against the tuples, raising `ValueError(f"{name} must be one of {ENUM}, got {value!r}")` (mirror StabilityMetric.__post_init__).
    Define the public stub `def compute_safety_flags(*, angles, keypoints_4ch, force_signals_report, dimension_scores, reference_angles=None, experience=None, reference_level=None, mode, profile) -> list[SafetyFlag]:` returning `[]` with a `# Wave 0 stub — firing rules land in 10-02/03/04` comment. Lock this signature now (consumed by _process in 10-02). NOTE: (a) the sagittal reference frame for D-05 is derived INTERNALLY from `keypoints_4ch` (pole-aligned 3D) in 10-03 — it is NOT an added kwarg; (b) the DTW time-alignment for D-03/D-04 reference comparison is ALSO recomputed INTERNALLY from `angles` + `reference_angles` (no alignment artifact kwarg — see 10-02/10-04); this signature is final.
    Create `backend/tests/phase10/__init__.py` (empty package marker) and `backend/tests/phase10/test_safety_flags_dataclass.py` mirroring phase09's `_kwargs` factory + `pytest.raises` enum-violation tests (these go GREEN immediately).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/phase10/test_safety_flags_dataclass.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && python -m pytest tests/phase10/test_safety_flags_dataclass.py -x -q` passes (dataclass constructs + 3 enum-violation ValueErrors raised).
    - `cd backend && python -c "from sunity_shared.analysis.safety_flags import SafetyFlag, compute_safety_flags"` exits 0.
    - `grep -n "field(default_factory=list)" backend/shared/python/sunity_shared/analysis/safety_flags.py` returns nothing (all fields scalar — Firestore ban).
  </acceptance_criteria>
  <done>SafetyFlag dataclass + enum guards + compute_safety_flags stub exist; dataclass tests GREEN; all fields scalar.</done>
</task>

<task type="auto">
  <name>Task 2: Contract 3-mirror — SafetyFlag in analysis.ts + models.py + contract.md, and warnAmberBg token</name>
  <files>app/src/types/analysis.ts, backend/shared/python/sunity_shared/models.py, docs/contract.md, app/src/theme/colors.ts</files>
  <read_first>
    - app/src/types/analysis.ts:518 (forceSignalsReport optional field on AnalysisResult — sibling placement), :870 (SeverityLevel union to REUSE), :1018-1026 (ForceSignalsReport interface structural analog), :19 + :578 (ExperienceLevel / SkillLevel to REUSE), :203 (CoachingTipDetail.injuryRisk — MUST stay untouched, D-01)
    - backend/shared/python/sunity_shared/models.py:206 (EXPERIENCE_LEVELS tuple to REUSE), :465-470 (bottom-of-file analysis re-export pattern with # noqa: E402, F401 — confirm safety_flags.py imports NOTHING from models.py, so the bottom re-export cannot create an import cycle)
    - docs/contract.md:994-1126 (§9 ForceSignalsReport table layout to mirror), :1451 (dated changelog footer style)
    - app/src/theme/colors.ts:46 (warnAmber declaration), :54-62 (Phase 4 additive-token block convention, inside the single `as const` object)
    - .planning/phases/10-injury-risk-flags/10-RESEARCH.md (Code Examples — exact camelCase TS SafetyFlag fields)
    - .planning/phases/10-injury-risk-flags/10-UI-SPEC.md (§Color — warnAmberBg = '#FFF6E5')
  </read_first>
  <action>
    Edit all three contract mirrors in lockstep (CLAUDE.md cross-cutting; 10-CONTEXT Claude's-Discretion explicitly mandates 3-mirror) plus the additive theme token. Keep field names camelCase in TS, snake_case in Python, both documented in contract.md.
    analysis.ts: add `export type SafetyFlagType = 'asymmetry' | 'trunk_hyperextension' | 'joint_hyperextension' | 'level_mismatch';` and `export interface SafetyFlag { flagType: SafetyFlagType; bodyRegion: string; severity: SeverityLevel; confidence: MetricConfidence; modeScope: 'both' | 'mode1_only'; postureCondition: string; controlLossSignal: string; }` near ForceSignalsReport. REUSE existing `SeverityLevel` (line 870) and `MetricConfidence`; do NOT redefine. Add `safetyFlags?: SafetyFlag[] | null;` to `AnalysisResult` beside `forceSignalsReport`. Do NOT touch `CoachingTipDetail.injuryRisk` (D-01 — independent LLM layer).
    models.py: add `SAFETY_FLAG_TYPES` / `SAFETY_FLAG_MODE_SCOPES` tuples (reuse force_signals `_SEVERITY_LEVELS` for severity), and re-export `SafetyFlag` from `.analysis.safety_flags` at the file bottom following the `# noqa: E402, F401` convention. Import-cycle guard: `safety_flags.py` must NOT `import models` (it imports only numpy + skeleton/kismam/dimensions/motiondtw/features which are already leaf modules) — keep the dependency one-directional (models → safety_flags), exactly like the existing force_signals re-export.
    contract.md: add a new `§N. SafetyFlag` section with the same `| field (camel) / field (snake) | type | cardinality |` table layout as §9, noting safetyFlags is optional/nullable on AnalysisResult and every field is scalar (no nested arrays). Add a dated changelog footer entry.
    colors.ts: inside the existing `as const` object, add `warnAmberBg: '#FFF6E5',` with a dated comment citing `10-UI-SPEC §Color`. Do NOT modify `brand: '#FF4B33'`.
  </action>
  <verify>
    <automated>cd app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && npm run typecheck` exits 0 (tsc --noEmit clean with the new SafetyFlag type + safetyFlags field).
    - `grep -c "SafetyFlag" app/src/types/analysis.ts docs/contract.md` shows the type present in both.
    - `grep -n "warnAmberBg: '#FFF6E5'" app/src/theme/colors.ts` returns a match; `grep -c "brand: '#FF4B33'" app/src/theme/colors.ts` returns 1 (unchanged).
    - `grep -c "injuryRisk" app/src/types/analysis.ts` is unchanged from before the edit (D-01 boundary preserved — CoachingTipDetail.injuryRisk untouched).
    - `cd backend && python -c "from sunity_shared.models import SafetyFlag"` exits 0 (no circular-import error — the import-cycle check).
    - `cd backend && python -c "import sunity_shared.analysis.safety_flags as s; assert not hasattr(s, 'models'), 'safety_flags must not import models'"` exits 0 (one-directional dependency).
  </acceptance_criteria>
  <done>SafetyFlag exists in all three mirrors with matching shape; warnAmberBg added; brand color and LLM injuryRisk untouched; typecheck green; no import cycle.</done>
</task>

<task type="auto">
  <name>Task 3: phase10 test scaffold + fixtures (all no-flag invariants GREEN, positive tests xfail-strict, temporal + DTW-shift + ambiguous-geometry fixtures)</name>
  <files>backend/tests/phase10/conftest.py, backend/tests/phase10/test_safety_flags_firing_rule.py, backend/tests/phase10/test_safety_flags_hyperextension.py, backend/tests/phase10/test_safety_flags_asymmetry.py, backend/tests/phase10/test_safety_flags_level.py, backend/tests/phase10/test_safety_flags_contract.py</files>
  <read_first>
    - backend/tests/phase08/conftest.py (programmatic fixture-factory pattern — build synthetic arrays + a ForceSignalsReport)
    - backend/shared/python/sunity_shared/analysis/force_signals.py:401-437,495 (StabilityMetric{phase, unstable_body_parts, severity} + ForceSignalsReport.stability_metrics/overall_confidence shape — the control-loss substrate; note unstable_body_parts holds JOINT_KEYS strings like 'left_knee')
    - backend/shared/python/sunity_shared/analysis/pose_frame.py:326 (to_coco17_array → (T,17,4) shape; ch3 = keypoint confidence/uncertainty for synthetic hyperextension arrays)
    - backend/functions/pipeline/app.py:1563-1619 (_angles_to_dtw_median_dicts — the DTW-path-aligned per-joint median precedent that 10-02/10-04 mirror inside safety_flags; this is why the timing-shifted fixture must cancel)
    - .planning/phases/10-injury-risk-flags/10-VALIDATION.md (Per-Task Verification Map + Wave 0 Requirements — the exact 6 test files + fixtures)
    - .planning/phases/10-injury-risk-flags/10-RESEARCH.md (Validation Architecture — elite-no-FP is the headline gate; synthetic flexed vs reverse-bent fixtures; success/fail pair = known-answer regression NOT fit target)
    - .planning/phases/10-injury-risk-flags/10-CONTEXT.md (D-02 AND-gate locality + temporal alignment, D-03/05/06 expected behaviors)
  </read_first>
  <action>
    Create `backend/tests/phase10/conftest.py` with programmatic fixtures (mirror phase08/conftest.py). PROVENANCE: every fixture sourced from the 정은지 success/fail pair dataset is a KNOWN-ANSWER REGRESSION FIXTURE, NOT a calibration/fit input — add a module-top comment saying so ([[calibration-source-hard-gate]] — no 13-video curve-fit). Fixtures:
    (1) `elite_angles` — a 정은지-style (T, J) angle matrix representing intentional extreme posture (e.g. near-180° hold).
    (2) `low_control_loss_report` — a `ForceSignalsReport` whose StabilityMetrics are all `severity='low'` with empty `unstable_body_parts` (the elite-no-FP partner).
    (3) `high_control_loss_report` — at least one metric `severity='medium'/'high'` with the relevant joint (e.g. 'left_hip' for trunk, 'left_knee' for D-05) in `unstable_body_parts`, scoped to the SAME phase/hold window as the posture condition.
    (4) `window_disjoint_report` — instability present (medium/high) but in a phase/window that does NOT overlap the posture hold window, AND with `unstable_body_parts` NOT containing the posture's joint. This is the HIGH-1 temporal-colocation negative fixture: posture-in-window-A + instability-only-in-window-B must yield NO flag.
    (5) `timing_shifted_reference_angles` — a (T, J) reference matrix carrying the SAME intentional extreme (asymmetry/extension) as a paired student matrix BUT at a DIFFERENT time offset (e.g. the extreme hold appears at frames 10-20 in the student and 30-40 in the reference). This is the HIGH-A DTW-alignment negative fixture: after DTW path-alignment the two extremes pair up and the reference-anchored excess cancels → NO flag (proves alignment, not raw same-index comparison). Provide alongside a matching `timing_shifted_student_angles`.
    (6) `flexed_knee_4ch` and `reverse_bent_knee_4ch` — hand-built (T,17,4) arrays (forward flexion vs reverse hyperextension) with a realistic ch3 confidence channel (high confidence); same for elbow.
    (7) `ambiguous_geometry_4ch` — a (T,17,4) array with degenerate/inverted/spinning geometry OR low ch3 confidence on the hinge keypoints, for the D-05 fail-conservative (no-flag) test.
    Create the 5 test files calling `compute_safety_flags(...)`. EXPLICIT GREEN/xfail discipline — the rule is simple: a test whose CORRECT answer is `[]` (no flag) is GREEN immediately (the empty stub already returns `[]`); a test whose correct answer is that a flag SHOULD fire is `@pytest.mark.xfail(strict=True, reason="firing rule lands in 10-NN")` and flips GREEN in its wave. NEVER put `xfail(strict=True)` on a no-flag case (it would XPASS against the stub and fail the suite).
    - `test_safety_flags_firing_rule.py`:
        GREEN now: `test_elite_posture_alone_no_flag` (elite_angles + low_control_loss_report → []), `test_deterministic` (same input → identical flags, [] == []), `test_posture_without_control_loss_no_flag` (no-flag), `test_and_gate_requires_temporal_colocation` (posture met but only window_disjoint_report → [], no-flag).
        xfail-strict (positive, flips in 10-02): `test_posture_and_control_loss_emits_flag`.
    - `test_safety_flags_hyperextension.py`:
        GREEN now (no-flag): `test_flexed_knee_no_flag`, `test_ambiguous_geometry_no_flag`, `test_elite_extension_no_joint_flag`.
        xfail-strict (positive, flips in 10-03): `test_reverse_bent_knee_with_control_loss_flags`.
    - `test_safety_flags_asymmetry.py`:
        GREEN now (no-flag): `test_equal_asymmetry_no_flag`, `test_no_reference_no_flag` (reference_angles=None → []), `test_timing_shifted_same_asymmetry_no_flag` (the HIGH-A DTW-shift cancellation, no-flag).
        xfail-strict (positive, flips in 10-04): `test_student_more_asymmetric_with_control_loss_flags`.
    - `test_safety_flags_level.py`:
        GREEN now (no-flag): `test_mode3_level_no_flag` (same advanced-ref × beginner inputs in Mode 3 → [], D-06 mode1-only), `test_level_without_control_loss_no_flag`.
        xfail-strict (positive, flips in 10-04): `test_mode1_level_mismatch_with_control_loss_flags`.
    - `test_safety_flags_contract.py`: guard the import of `_validate_safety_flags` (added in 10-02) so the file COLLECTS now; the "validator rejects a nested-list flag dict / accepts scalar-only" assertions are `xfail(strict=True, reason="validator lands in 10-02")` (this tests a not-yet-existing function's positive behavior, not a no-flag invariant); un-xfailed in 10-02.
    Rule restated: GREEN = every case whose answer is []. xfail(strict) = only cases where a flag must fire (or a validator must reject). This keeps later waves from inheriting ambiguous RED/GREEN state and prevents XPASS failures in Wave 0.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/phase10 -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && python -m pytest tests/phase10 -q --co` collects all 6 test files with zero collection/import errors.
    - `cd backend && python -m pytest tests/phase10 -q` reports ZERO failures and ZERO xpasses: every no-flag invariant (elite-no-FP, determinism, posture-without-control, temporal-colocation, flexed-knee, ambiguous-geometry, equal-asymmetry, no-reference, timing-shifted, Mode-3-level, level-without-control) PASSES; only the positive-flag tests + the contract validator test report XFAIL(strict).
    - `cd backend && python -m pytest tests/phase10/test_safety_flags_firing_rule.py::test_elite_posture_alone_no_flag -q` is GREEN against the stub.
    - `cd backend && python -m pytest tests/phase10/test_safety_flags_asymmetry.py::test_timing_shifted_same_asymmetry_no_flag -q` is GREEN against the stub (the DTW-shift negative case — it stays GREEN through 10-04 because the answer is always []).
    - conftest.py exposes elite_angles, low_control_loss_report, high_control_loss_report, window_disjoint_report, timing_shifted_student_angles/timing_shifted_reference_angles, and synthetic (T,17,4) flexed/reverse-bent + ambiguous-geometry fixtures (grep the fixture names); module-top comment marks them regression-only, not fit input.
  </acceptance_criteria>
  <done>All 6 phase10 test files collect; every no-flag invariant GREEN against the stub (incl. temporal-colocation + DTW timing-shift cancellation); only positive-flag + contract-validator tests xfail(strict=True); temporal-colocation, DTW-shift, and ambiguous-geometry fixtures present; zero XPASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→pipeline | `meta.bodyProfile.experience` originates client-side; sanitized before any flag uses it (D-06). |
| pipeline→Firestore | `result["safetyFlags"]` written server-side via Admin SDK; must be scalar-only. |
| Firestore→app | `safetyFlags` read by `result.tsx`; optional/nullable for legacy + graceful-omit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | `SafetyFlag` shape (nested-array injection) | mitigate | Dataclass fields scalar-only (Task 1); contract test asserts no nested list (Task 3, validator lands 10-02). |
| T-10-02 | Tampering | `bodyProfile.experience` spoof | mitigate (deferred to 10-04) | D-06 consumes `normalize_body_profile` enum-drop; this wave only declares the contract (`EXPERIENCE_LEVELS` reused). |
| T-10-03 | Info disclosure | medical over-claim ("부상 확정") | mitigate | Contract/UI copy contract forbids confirmed-injury language; this wave adds no copy, only types. |
| T-10-04 | Repudiation | LLM-layer contamination (D-01) | mitigate | `CoachingTipDetail.injuryRisk` left untouched; acceptance grep asserts unchanged. |
| T-10-09 | Tampering | import cycle via models re-export breaking pipeline load | mitigate | safety_flags.py imports no models; one-directional dependency asserted (Task 2). |
| T-10-SC | Tampering | npm/pip installs | accept | No package installs this phase (RESEARCH: numpy-only reuse, existing RN tokens). No registry surface. |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/phase10 -q --co` — all 6 files collect, zero errors.
- `cd backend && python -m pytest tests/phase10 -q` — every no-flag invariant GREEN; only positive-flag + contract-validator tests XFAIL(strict); zero XPASS, zero FAIL.
- `cd app && npm run typecheck` — GREEN (contract 3-mirror addition compiles).
- `cd backend && python -m pytest tests -q` — full backend suite GREEN (no regression in force_signals/dimensions/assemble from the new module + re-export).
</verification>

<success_criteria>
- SafetyFlag type present and shape-consistent across analysis.ts, models.py, contract.md.
- safety_flags.py importable: SafetyFlag dataclass (enum-guarded, scalar-only) + compute_safety_flags stub; no import cycle.
- phase10 test suite collects; every no-flag invariant GREEN against the stub; only positive-flag + contract-validator tests xfail(strict); temporal-colocation, DTW timing-shift, and ambiguous-geometry fixtures present; zero XPASS.
- warnAmberBg token added; brand #FF4B33 and CoachingTipDetail.injuryRisk untouched (D-01).
</success_criteria>

<output>
Create `.planning/phases/10-injury-risk-flags/10-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
