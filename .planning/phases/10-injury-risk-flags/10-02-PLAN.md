---
phase: 10-injury-risk-flags
plan: 02
type: execute
wave: 1
depends_on: [10-01]
files_modified:
  - backend/shared/python/sunity_shared/analysis/safety_flags.py
  - backend/functions/pipeline/app.py
  - backend/shared/python/sunity_shared/firestore_admin.py
  - app/src/app/analysis/result.tsx
  - app/src/lib/userAnalyses.ts
  - backend/tests/phase10/test_safety_flags_firing_rule.py
  - backend/tests/phase10/test_safety_flags_contract.py
autonomous: true
requirements: [SAFE-01]

must_haves:
  truths:
    - "정은지 elite posture alone produces ZERO flags (no false positive) — the headline gate"
    - "A flag fires ONLY when a posture condition AND a JOINT/REGION-LOCAL, TEMPORALLY CO-LOCATED control-loss indicator are both true (D-02 AND-gate). Instability in a different window or a different body part does NOT fire the flag."
    - "Trunk hyperextension (D-04) is computed against the DTW-PATH-ALIGNED reference (student hold window vs the DTW-corresponding reference frames, NOT raw same-index frames) so the elite's OWN intentional extension cancels even when student/reference timing differs; fires only with HIP-LOCAL control-loss (left_hip/right_hip in unstable_body_parts, co-located with the trunk hold phase) → renders as '허리 부담 가능성' amber card (Success Criterion 2)"
    - "Mode 1 anchors trunk reference angles on the 정은지 reference (a_ref); Mode 3 anchors on the previous-analysis angles (reshaped mode3_prev); the DTW alignment is recomputed INSIDE safety_flags from angles + reference_angles (same method as pipeline _deviation_against), so both modes are time-aligned identically; when no previous video exists (reference_angles=None) the reference-anchored trunk flag gracefully no-ops (known v1 limitation, surfaced — D-05 absolute carries the Mode-3 promise)"
    - "result['safetyFlags'] persists to Firestore as scalar-only dicts via a SINGLE persistence path (set in _process, validated by _validate_safety_flags inside complete_analysis — no duplicate kwarg)"
    - "Result screen shows a visually-distinct amber warning section with expert-referral copy when flags exist; section is OMITTED (no reassurance) when none (D-08, Success Criterion 4)"
    - "Flags are deterministic (LLM-free); same input → identical flags (the recomputed DTW path is deterministic — identical inputs → identical path)"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/safety_flags.py"
      provides: "compute_safety_flags D-02 LOCAL+TEMPORAL AND-gate + DTW-path-aligned reference helper + D-04 trunk firing rule (reference-anchored, hip-local control-loss)"
      contains: "def compute_safety_flags"
    - path: "backend/functions/pipeline/app.py"
      provides: "_process injection: derive reference_angles (mode1 a_ref / mode3 reshaped prev), compute_safety_flags after force_signals, result['safetyFlags'] set"
      contains: "compute_safety_flags"
    - path: "backend/shared/python/sunity_shared/firestore_admin.py"
      provides: "_validate_safety_flags scoped validator wired on result['safetyFlags']"
      contains: "_validate_safety_flags"
    - path: "app/src/app/analysis/result.tsx"
      provides: "InjuryRiskSection + InjuryRiskFlagCard (amber, all-4 copy map, omit-when-empty)"
      contains: "InjuryRiskSection"
  key_links:
    - from: "backend/functions/pipeline/app.py"
      to: "safety_flags.compute_safety_flags"
      via: "call after force_signals_report, before complete_analysis"
      pattern: "compute_safety_flags\\("
    - from: "backend/shared/python/sunity_shared/analysis/safety_flags.py"
      to: "motiondtw.motion_dtw + features.feature_vector"
      via: "DTW-path-aligned reference comparison (mirror pipeline _deviation_against / _angles_to_dtw_median_dicts)"
      pattern: "motion_dtw|feature_vector"
    - from: "backend/shared/python/sunity_shared/analysis/safety_flags.py"
      to: "force_signals_report.stability_metrics[].unstable_body_parts"
      via: "joint/region-local + temporal control-loss derivation (D-02 partner)"
      pattern: "unstable_body_parts"
    - from: "app/src/app/analysis/result.tsx"
      to: "analysis.result.safetyFlags"
      via: "conditional render after OctagonScore, before 동작 비교"
      pattern: "safetyFlags"
---

<objective>
Slice 1 (thinnest end-to-end vertical slice): make an injury-risk flag appear on the result screen. Implement the D-02 AND-gate core — and critically, make it LOCAL and TIME-ALIGNED (the single most important false-positive defense for 정은지): a posture condition fires only when paired with a control-loss signal that is (a) on the SAME body region/joint and (b) co-located in the SAME hold/phase window. Implement the D-04 trunk-femur hyperextension firing rule with **DTW-path-aligned reference comparison** (reuse the already-computed `left_hip`/`right_hip` angle, but compare the student against the DTW-corresponding reference frames, not raw same-index — see HIGH-A below), hip-local control-loss, wire `compute_safety_flags` into `_process`, persist `result["safetyFlags"]` through a new `_validate_safety_flags` scoped validator (single persistence path), and render the generic amber `InjuryRiskSection`/`InjuryRiskFlagCard` (with copy mappings for ALL four flag types so later slices auto-render).

This delivers Success Criteria 2 (요추 과신전 → "허리 부담 가능성") and 4 (visually-distinct warning + 전문가 확인 권유) end-to-end, and locks the project's headline gate: 정은지 elite posture ALONE — or elite posture plus unrelated wobble elsewhere — produces zero flags (D-02 LOCAL+TEMPORAL, Pitfall 2).

**D-04 scope refinement (MEDIUM-3, explicit — NOT a silent reduction):** D-04's original CONTEXT wording is "트렁크-대퇴 각도 프록시 … 절대 신호 → 양 모드 동작." This plan implements the trunk flag as **reference-anchored only** (no absolute lumbar cutoff). This is the deliberate RESEARCH A3 resolution: there is **no defensible absolute lumbar/trunk anatomical injury cutoff for v1** (IPSF has no medical numbers; the trunk-femur proxy conflates lumbar + hip — D-04's own locked rigid-body limitation), so an absolute number would be curve-fit (D-07 forbidden). "Both modes" is still satisfied — the flag fires in BOTH Mode 1 and Mode 3, anchored on whichever reference the mode provides (정은지 reference / previous video). The ABSOLUTE trunk rule is explicitly **DEFERRED** (would require a sourced biomechanical cutoff or a mid-spine keypoint — see CONTEXT Deferred Ideas "요추 전용 척추 키포인트"). The copy carries the rigid-body limitation parenthetical. (CONTEXT.md is NOT edited; this paragraph is where the resolution is surfaced for the checker and executor.)

Purpose: prove the full data-structure → LOCAL+TEMPORAL AND-gate → DTW-aligned reference comparison → assembly → persist → render pipe with the lowest-risk flag, so D-05/D-03/D-06 become pure backend additions that render automatically.
Output: working trunk-hyperextension flag visible on `result.tsx`; firing-rule (incl. temporal-colocation + DTW timing-shift cancellation) + contract tests GREEN.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-injury-risk-flags/10-CONTEXT.md
@.planning/phases/10-injury-risk-flags/10-RESEARCH.md
@.planning/phases/10-injury-risk-flags/10-PATTERNS.md
@.planning/phases/10-injury-risk-flags/10-UI-SPEC.md
@.planning/phases/10-injury-risk-flags/10-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-02 LOCAL+TEMPORAL AND-gate + DTW-aligned reference helper + D-04 trunk firing rule in safety_flags.py</name>
  <files>backend/shared/python/sunity_shared/analysis/safety_flags.py, backend/tests/phase10/test_safety_flags_firing_rule.py</files>
  <read_first>
    - backend/shared/python/sunity_shared/analysis/safety_flags.py (the 10-01 stub being extended)
    - backend/functions/pipeline/app.py:2606-2618 (_deviation_against — the established alignment core: match = motion_dtw(feature_vector(user_angles), feature_vector(a_ref)); user_seg = user_angles[match.start:match.end]; per_joint_deviation(match.path, user_seg, a_ref). THIS is the pattern to mirror inside safety_flags.)
    - backend/functions/pipeline/app.py:1563-1619 (_angles_to_dtw_median_dicts — the EXACT precedent for DTW-path-aligned per-joint medians: recomputes DTW internally, iterates match.path (u,r), collects seg[u,j]/a_ref[r,j], returns per-joint median dicts for student AND reference. Mirror this signature/logic in a safety_flags private helper.)
    - backend/shared/python/sunity_shared/analysis/motiondtw.py:26-69,94-130 (motion_dtw → MotionMatch{start,end,path}; per_joint_deviation path iteration — pure, numpy-only, importable by safety_flags)
    - backend/shared/python/sunity_shared/analysis/features.py:283 (feature_vector — pure; safety_flags imports it for the DTW feature space, same as pipeline)
    - backend/shared/python/sunity_shared/analysis/dimensions.py:292-317 (_select_window(angles, profile) → (sliced, (s,e)) — the SHARED hold-window selector; reuse it for the trunk hold window, do NOT compute a new window — drift ban)
    - backend/shared/python/sunity_shared/analysis/force_signals.py:401-437,495,1189-1200 (StabilityMetric.phase / .severity / .unstable_body_parts populated from dimensions.stability_wobble_by_joint keys = JOINT_KEYS; ForceSignalsReport.stability_metrics is PER-PHASE — this is the substrate for both locality AND phase-level temporal alignment)
    - backend/shared/python/sunity_shared/analysis/skeleton.py:39-50 (JOINT_ANGLES['left_hip']=('left_shoulder','left_hip','left_knee') = trunk-femur proxy; unstable_body_parts keys include 'left_hip'/'right_hip')
    - backend/shared/python/sunity_shared/analysis/kismam.py:111-124 (score_from_deviation — REUSE for reference-anchored excess; gaussian 100·exp(-½(dev/tol)²), tol default 20°)
    - .planning/phases/10-injury-risk-flags/10-RESEARCH.md (Pattern 2 _control_loss_for_joint + _maybe_flag; Pattern 4 reference-anchored note "ref already fetched for DTW"; Pitfall 4 trunk over-claim; State-of-the-Art A3 no absolute lumbar cutoff)
    - .planning/phases/10-injury-risk-flags/10-CONTEXT.md (D-02, D-04, D-07)
    - backend/tests/phase10/test_safety_flags_firing_rule.py (the xfail/GREEN tests to satisfy, incl. test_and_gate_requires_temporal_colocation)
    - backend/tests/phase10/conftest.py (timing_shifted_student_angles/timing_shifted_reference_angles fixtures from 10-01)
  </read_first>
  <behavior>
    - elite_angles + low_control_loss_report → compute_safety_flags returns [] (elite no-FP — headline gate).
    - trunk posture condition met in hold window W AND hip-local control-loss co-located in W (a StabilityMetric for phase∈W with severity medium/high AND 'left_hip' or 'right_hip' in its unstable_body_parts) → one SafetyFlag(flag_type='trunk_hyperextension', body_region='허리', mode_scope='both').
    - DTW-ALIGNMENT CANCELLATION (HIGH-A): timing_shifted_student_angles + timing_shifted_reference_angles (SAME intentional extension, shifted timing) → NO flag, because after DTW path-alignment the student's extreme frames pair with the reference's extreme frames and the reference-anchored excess ≈ 0. (A raw same-index comparison would FALSELY flag — this test proves the fix.)
    - trunk posture met in W, but control-loss only in a DIFFERENT window, or control-loss present in W but on unrelated joints (e.g. only 'left_wrist') → NO flag (test_and_gate_requires_temporal_colocation, the HIGH-1 locality+temporal gate).
    - trunk posture met WITHOUT any control-loss → no flag (D-02 AND-gate).
    - same input twice → identical flag list (deterministic — recomputed DTW path is deterministic).
    - SafetyFlag.confidence reflects force_signals_report.overall_confidence (graceful on low-confidence, Pitfall 5 — never hard-fail).
  </behavior>
  <action>
    Implement the DTW-aligned reference helper, the D-02 LOCAL+TEMPORAL AND-gate helpers, and the D-04 trunk rule in `safety_flags.py`.

    DTW-PATH-ALIGNED REFERENCE COMPARISON (HIGH-A — the new false-positive defense): the reference comparison MUST be time-aligned, never raw same-index. Add a pure private helper `_dtw_aligned_joint_medians(student_angles, reference_angles, joint_keys) -> tuple[dict[str,float], dict[str,float]]` that MIRRORS pipeline `_angles_to_dtw_median_dicts` (app.py:1563-1619): import `from .motiondtw import motion_dtw` and `from .features import feature_vector`; compute `match = motion_dtw(feature_vector(student_angles), feature_vector(reference_angles))`; `seg = student_angles[match.start:match.end]`; iterate `match.path` pairs `(u, r)`, collecting finite `seg[u, j]` into student-side and `reference_angles[r, j]` into reference-side per joint; return per-joint medians for student and reference. This is RECOMPUTED inside safety_flags (NOT threaded as a kwarg) because (a) the locked compute_safety_flags signature already passes `angles` + `reference_angles` for both modes, (b) the Mode-3 DTW match is computed inside `_mode3_comparison` and discarded — never in scope at the injection point — so recompute is the only uniform path, and (c) `_angles_to_dtw_median_dicts` itself recomputes DTW internally, so this matches the established pattern exactly. Document in a comment: "DTW alignment uses the pipeline's existing motion_dtw method (same as _deviation_against) — deterministic, NOT tuned offsets (D-07)." Empty path / None reference → return ({}, {}) (graceful).

    LOCALITY (HIGH-1): Make `_control_loss_for_joint(fsr, joint_key, *, phase=None) -> bool` the REQUIRED path for every joint/region-scoped flag. It returns True only when SOME StabilityMetric satisfies BOTH: (i) `joint_key in metric.unstable_body_parts` (region-local — the keys are JOINT_KEYS like 'left_hip'), and (ii) `metric.severity in {'medium','high'}`, and (iii) the metric's `phase` matches the posture's phase when `phase` is provided (temporal co-location). Restrict the phase-level fallback `_control_loss_phase_level(fsr)` (any-metric severity, no joint, no phase) to ONLY flags that have no localized joint signal defined; document in a comment that trunk/joint/asymmetry flags MUST use `_control_loss_for_joint` and that phase-level is reserved for level-mismatch (D-06, whole-body overreach). Trunk (D-04) uses `_control_loss_for_joint(fsr, 'left_hip', phase=P) or _control_loss_for_joint(fsr, 'right_hip', phase=P)` — hip-local, NOT bare phase-level.

    TEMPORAL ALIGNMENT — granularity statement (MEDIUM-2, explicit v1 limitation): `force_signals.ForceSignalsReport.stability_metrics` is PER-PHASE (StabilityMetric.phase), NOT per-frame. Therefore v1 temporal co-location is **phase-level**: the qualifying StabilityMetric's `.phase` must be the trunk hold's phase. Document this in a module comment as a KNOWN v1 LIMITATION: "Temporal co-location granularity is phase-level (the control-loss substrate is phase-scoped). A same-phase-but-different-frame instability can still co-locate with a sub-windowed posture; frame-level control-loss is deferred (CONTEXT Deferred: explicit slip/regrip event detection)." Determine the trunk posture's hold phase via the SHARED `dimensions._select_window(angles, profile)` (do NOT compute a new window — drift ban) and map it to the StabilityMetric phase identity. When phase windowing is unavailable, fall back to requiring joint membership at minimum (never widen to any-window). Add `_maybe_flag(posture_met, control_lost, **flag_kwargs) -> SafetyFlag | None` returning a flag ONLY when both true — posture alone NEVER fires (Pitfall 2).

    Implement `_trunk_hyperextension_flag(angles, reference_angles, fsr, profile) -> SafetyFlag | None` (D-04): use `_dtw_aligned_joint_medians(angles, reference_angles, skeleton.JOINT_KEYS)` to obtain DTW-aligned `student_med` and `ref_med`; the trunk-femur proxy angle is the `left_hip`/`right_hip` joint angle (JOINT_ANGLES). For each side compute `excess_side = max(0.0, student_med[hip] - ref_med[hip])` and take the max over sides; posture_met when `excess > 1.5 * tol` (tol=20° → 30°), i.e. `score_from_deviation(excess, tol=20.0) < 33` — substantially beyond the IPSF tolerance band. REFERENCE-ANCHORED ONLY, NO absolute lumbar cutoff (A3 / Pitfall 4 / MEDIUM-3). When `reference_angles is None` OR the aligned dicts are empty → return None (graceful; cannot anchor — the Mode-3-first no-op). Set posture_condition audit string to name the responsible side/joint and the excess; control_loss_signal names the hip-local instability. confidence ← `fsr.overall_confidence`. Tag threshold provenance in a comment: KISMAM tol=20° [CITED: IPSF CoP]; the 1.5× margin = "substantially beyond tolerance band" [ASSUMED reference-anchored margin], NOT 13-video curve-fit (D-07).

    Rewrite `compute_safety_flags` to collect `[f for f in (_trunk_hyperextension_flag(...),) if f is not None]` (other rules append in 10-03/10-04). Keep numpy-only, NaN-degrades-to-no-flag (never raise).

    Tests: un-xfail `test_posture_and_control_loss_emits_flag` (now implemented). Add `test_trunk_timing_shifted_same_extension_no_flag` using timing_shifted fixtures (HIGH-A regression: same extension in both at shifted timing → no flag, proving DTW cancellation). Keep elite-no-FP, determinism, posture-without-control, and temporal-colocation GREEN.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/phase10/test_safety_flags_firing_rule.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && python -m pytest tests/phase10/test_safety_flags_firing_rule.py::test_elite_posture_alone_no_flag -x -q` GREEN (정은지 posture alone → zero flags — MUST-HAVE headline gate).
    - `cd backend && python -m pytest tests/phase10/test_safety_flags_firing_rule.py::test_trunk_timing_shifted_same_extension_no_flag -x -q` GREEN (HIGH-A: same extension in student+reference at shifted timing → zero flags — proves DTW path-alignment cancels intentional reference extension, not raw same-index).
    - `cd backend && python -m pytest tests/phase10/test_safety_flags_firing_rule.py::test_and_gate_requires_temporal_colocation -x -q` GREEN (posture-in-phase-A + instability-only-in-phase-B/unrelated-joint → zero flags — the HIGH-1 locality+temporal gate).
    - `cd backend && python -m pytest tests/phase10/test_safety_flags_firing_rule.py -x -q` GREEN (AND-gate both directions + deterministic + DTW-shift).
    - `grep -nE "motion_dtw|feature_vector" backend/shared/python/sunity_shared/analysis/safety_flags.py` shows the DTW-aligned helper recomputes alignment via the pipeline's method (HIGH-A).
    - `grep -n "_control_loss_for_joint" backend/shared/python/sunity_shared/analysis/safety_flags.py` shows trunk uses joint-local (left_hip/right_hip) + phase, not bare phase-level.
    - `grep -nE "(>|>=)\\s*[0-9]+(\\.[0-9]+)?\\s*#.*(absolute|lumbar)" backend/shared/python/sunity_shared/analysis/safety_flags.py` returns nothing for trunk (no absolute lumbar cutoff, A3); reference-anchored only.
    - A module comment states the phase-level temporal granularity v1 limitation (grep for "phase-level" near the temporal/co-location code).
    - No threshold comment justifies a cutoff by "our videos" (grep for "13" / "our video" — none in D-07-sensitive code).
  </acceptance_criteria>
  <done>DTW-path-aligned reference helper (mirrors _angles_to_dtw_median_dicts) + D-02 LOCAL+TEMPORAL AND-gate + D-04 reference-anchored trunk rule (hip-local control-loss) implemented; elite-no-FP + temporal-colocation + DTW-timing-shift GREEN; deterministic; no absolute lumbar cutoff; phase-level temporal granularity documented.</done>
</task>

<task type="auto">
  <name>Task 2: _process injection (mode1/mode3 reference plumbing) + single-path _validate_safety_flags</name>
  <files>backend/functions/pipeline/app.py, backend/shared/python/sunity_shared/firestore_admin.py, backend/tests/phase10/test_safety_flags_contract.py</files>
  <read_first>
    - backend/functions/pipeline/app.py:3056-3071 (MODE_EXPERT: deviation, match, user_seg, a_ref = _deviation_against(angles, ref["angles"], num_joints); a_ref = reshaped 정은지 reference angle matrix; reference_angles_for_veto = a_ref at :3060 — the Mode-1 trunk/asymmetry anchor), :2970-2971 (ref = get_reference_motion → ref.get("level") for D-06), :3127-3136 (MODE_SELF: prev = get_previous_analysis(...); mode3_prev = prev; prev carries prev.get("angles") flat + prev.get("anglesJointKeys")), :2606-2618 (_deviation_against reshape idiom: a_ref reshaped from flat via num_joints), :2786-2788 (num_joints = len(prev.get("anglesJointKeys") or []) or skeleton.NUM_JOINTS; _deviation_against reshapes prev), :3458-3468 (force_signals_report computed + force_signals_dict = _dataclass_to_camel_case_dict — INJECT right after, line 3468), :3573 (normalize_body_profile already called → experience source), :3785-3794 (complete_analysis call — result already carries fields)
    - backend/shared/python/sunity_shared/firestore_admin.py:104 (_validate_dict_only_scalars), :223-285 (_validate_force_signals_report scoped validator to mirror), :917-921 (where force_signals_report inside result is validated/written — wire the safety_flags validation at the SAME single point, reading result["safetyFlags"], NOT a new kwarg)
    - backend/shared/python/sunity_shared/analysis/safety_flags.py (compute_safety_flags signature locked in 10-01; DTW alignment recomputed internally — Task 2 passes ONLY reference_angles, not a match artifact)
    - .planning/phases/10-injury-risk-flags/10-PATTERNS.md (_process injection + firestore_admin sections — A5 persistence decision = inside result)
    - .planning/phases/10-injury-risk-flags/10-RESEARCH.md (Pitfall 1 nested-array; Open Q3 RESOLVED data-flow; injection-point code example)
    - backend/tests/phase10/test_safety_flags_contract.py (the xfail contract test to un-xfail)
  </read_first>
  <action>
    HIGH-3 reference plumbing (PROVEN in source — no new fetch needed): inject Phase 10 into `_process` immediately after `force_signals_dict = _dataclass_to_camel_case_dict(force_signals_report)` (line 3468) and before `complete_analysis` (~line 3785). Derive `reference_angles` per mode from variables ALREADY in scope. NOTE: safety_flags recomputes the DTW alignment internally (Task 1), so the injection passes ONLY the reshaped reference angle matrix — do NOT thread `match`/`user_seg` (the Mode-3 match is not in scope anyway).
      - Mode 1 (mode == models.MODE_EXPERT): `reference_angles = a_ref` (the reshaped 정은지 reference angle matrix already computed by `_deviation_against` at line 3056 / aliased `reference_angles_for_veto` at line 3060).
      - Mode 3 (mode == models.MODE_SELF): reshape the previous-analysis angles from `mode3_prev` (= `prev`, line 3133): `reference_angles = _reshape_prev_angles(mode3_prev)` where the helper applies the existing idiom `num_joints = len(prev.get("anglesJointKeys") or []) or skeleton.NUM_JOINTS` then reshapes `prev.get("angles")` (same as _mode3_comparison at lines 2786-2788). When `mode3_prev` is None or lacks angles (the user's FIRST Mode-3 video) → `reference_angles = None`. (safety_flags then recomputes the student↔previous DTW alignment itself; on None it no-ops.)
      - `experience = (models.normalize_body_profile(meta.get("bodyProfile")) or {}).get("experience")`; `reference_level = ref.get("level") if mode == models.MODE_EXPERT and ref else None` (D-06 inputs).
    KNOWN-LIMITATION SURFACING (do NOT silently no-op): add a code comment AND a SUMMARY note stating that on a first Mode-3 video (no previous baseline) the reference-anchored flags (D-04 trunk, D-03 asymmetry) cannot fire — but belle's Mode-3 "내 자세가 이러면 위험" promise is still met because the ABSOLUTE D-05 joint-hyperextension flag (10-03) needs no reference and fires in Mode 3. This is a documented v1 limitation, not a silent dead end.
    Call `compute_safety_flags(...)` with the locked kwargs and set `result["safetyFlags"] = [_dataclass_to_camel_case_dict(f) for f in safety_flags]` (scalar-only camelCase). Implement D-04 fully here even though only trunk fires; do NOT stub-reduce or label "v1 placeholder".
    SINGLE PERSISTENCE PATH (resolves MEDIUM duplication): do NOT add a `safety_flags=` kwarg to `complete_analysis`. Flags travel inside `result`. In firestore_admin, at the existing single point where `result["forceSignalsReport"]` is validated before write (lines 917-921), add `if isinstance(result.get("safetyFlags"), list): _validate_safety_flags(result["safetyFlags"])`. Define `_validate_safety_flags(flags)` mirroring `_validate_force_signals_report` — it is a `list[dict]`; validate each element via `_validate_dict_only_scalars` (every SafetyFlag field is scalar — stricter than force_signals, no whitelisted list fields). One write path, no kwarg, no overwrite.
    Un-xfail `test_safety_flags_contract.py`: assert `_validate_safety_flags` rejects a flag dict containing a nested list and accepts scalar-only.
    Do NOT feed SafetyFlag into the LLM `injuryRisk` prose or vice versa (D-01 — strictly independent).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/phase10/test_safety_flags_contract.py -x -q && cd backend && python -m pytest tests -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && python -m pytest tests/phase10/test_safety_flags_contract.py -x -q` GREEN (nested-list rejected, scalar-only accepted).
    - `grep -n "compute_safety_flags" backend/functions/pipeline/app.py` shows the call placed after force_signals (line 3468) and before complete_analysis.
    - `grep -nE "a_ref|mode3_prev|reshape_prev" backend/functions/pipeline/app.py` confirms Mode-1 uses a_ref and Mode-3 reshapes mode3_prev angles (HIGH-3 plumbing in source).
    - `grep -c "safety_flags=" backend/shared/python/sunity_shared/firestore_admin.py` returns 0 (no duplicate kwarg path — single in-result persistence); `grep -n "_validate_safety_flags" backend/shared/python/sunity_shared/firestore_admin.py` shows definition + the single wired call reading result["safetyFlags"].
    - `cd backend && python -m pytest tests -q` full backend suite GREEN (no regression in force_signals/dimensions/assemble/pipeline).
    - No reference to `injuryRisk` added in safety_flags or pipeline injection (D-01 boundary).
  </acceptance_criteria>
  <done>compute_safety_flags wired into _process with proven mode1(a_ref)/mode3(mode3_prev) reference plumbing (alignment recomputed inside safety_flags, no artifact threading) + surfaced first-video limitation; result['safetyFlags'] persisted scalar-only via a single _validate_safety_flags path (no kwarg); contract + full backend suite GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: InjuryRiskSection + InjuryRiskFlagCard amber UI on result.tsx (all-4 copy map, omit-when-empty)</name>
  <files>app/src/app/analysis/result.tsx, app/src/lib/userAnalyses.ts</files>
  <read_first>
    - app/src/components/AccuracyLimitBadge.tsx:1-72 (container pattern: file-top spec-citing comment, if(!visible) return null, icon row + title + description, StyleSheet referencing tokens — closest analog for InjuryRiskFlagCard)
    - app/src/app/analysis/result.tsx:830-871 (OctagonScore card block — render injury section right after), :881-884 (동작 비교 sectionTitle — render before this), :1151-1170 (강사에게 확인할 점 conditional section block), :1569-1579 (coachQuestionRow/coachQuestionText icon-row styles to reuse), :115 (highlightNumbers)
    - app/src/lib/userAnalyses.ts (normalize() — add safetyFlags null-guard mirroring forceSignalsReport, immutable spread + ?? null)
    - app/src/theme/colors.ts:46 (warnAmber), warnAmberBg (added 10-01)
    - .planning/phases/10-injury-risk-flags/10-UI-SPEC.md (Placement, Per-flag card layout, Copywriting Contract — exact Korean copy for all 4 flag types + section disclaimer + expert-referral line; no brand red; omit-when-empty)
  </read_first>
  <action>
    Add an `InjuryRiskSection` block (inline or new component file following AccuracyLimitBadge) to result.tsx, rendered conditionally `{safetyFlags && safetyFlags.length > 0 && (...)}` placed immediately after the OctagonScore card block (after the isScoreSuppressed ternary closes) and before the "동작 비교" sectionTitle. Section structure per UI-SPEC: sectionTitle `부상 위험 신호` (typography.sectionTitle); one disclaimer line (typography.caption, textSecondary): `부상을 확정하는 신호가 아니라, 측정값에서 보이는 위험 가능성이에요. 전문가 확인을 권해드려요.`; then one `InjuryRiskFlagCard` per flag sorted severity-high-first.
    `InjuryRiskFlagCard` mirrors AccuracyLimitBadge container: amber surface `colors.warnAmberBg`, border `colors.warnAmber`, radius.card, spacing.cardPadding (tokens only — NO hardcoded colors/spacing; brand #FF4B33 FORBIDDEN here per UI-SPEC §Color). Row 1: `Ionicons name="warning"` size 16 warnAmber + flag title (typography.boxLabel, warnAmber). Row 2: "why" line (typography.caption, textPrimary, lineHeight 18). Per-card or section-footer expert-referral line: `정확한 판단은 강사 또는 전문가와 함께 확인해 주세요.`
    Implement a `flagType → {title, why}` copy map for ALL FOUR flag types from the UI-SPEC Copywriting Contract (asymmetry/trunk_hyperextension/joint_hyperextension/level_mismatch) — even though only trunk fires now, so 10-03/10-04 flags render automatically with zero UI change. The D-04 trunk "why" MUST include the rigid-body parenthetical (몸통을 한 덩어리로 추정한 값이라 정확하지 않을 수 있어요). When no flags: render NOTHING (no "안전합니다" reassurance — forbidden). Use `highlightNumbers()` for any inline number; never lead with a risk-score number.
    Add `safetyFlags` null-guard to userAnalyses.ts normalize() (immutable spread + `?? null`, mirroring forceSignalsReport).
    File-top block comment citing `10-UI-SPEC.md` (AccuracyLimitBadge convention).
  </action>
  <verify>
    <automated>cd app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && npm run typecheck` exits 0.
    - `grep -n "warnAmberBg" app/src/app/analysis/result.tsx` (or the new component) shows the amber surface used; `grep -c "#FF4B33\|colors.brand" <injury section file>` shows brand red NOT used in the injury section.
    - `grep -n "안전합니다\|부상 없음\|위험 없음" app/src/app/analysis/result.tsx` returns nothing (no reassurance text — omit-when-empty).
    - All four copy-map keys present: `grep -oE "asymmetry|trunk_hyperextension|joint_hyperextension|level_mismatch" <injury section file>` lists all 4.
    - safetyFlags null-guard present in userAnalyses.ts normalize().
  </acceptance_criteria>
  <done>Amber InjuryRiskSection renders trunk flag end-to-end, omits when empty, copy-maps all 4 flag types, no brand red, no reassurance; typecheck green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→pipeline | `meta.bodyProfile.experience` (D-06 input) crosses here untrusted. |
| pipeline→Firestore | `result["safetyFlags"]` list[dict] written via Admin SDK (single path). |
| Firestore→app | `safetyFlags` read and rendered read-only on result.tsx. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | Firestore nested-array injection via flag dict | mitigate | `_validate_safety_flags` scoped validator rejects nested lists before write at the single in-result write point (Task 2); contract test asserts it. |
| T-10-03 | Info disclosure | medical over-claim ("부상 확정") | mitigate | Copy contract forbids confirmed-injury wording; possibility-only + expert-referral enforced (Task 3); grep gate for forbidden reassurance/claim text. |
| T-10-04 | Repudiation/trust | LLM-layer contamination (D-01) | mitigate | safety_flags + injection never touch `injuryRisk`; acceptance grep asserts no injuryRisk reference added. |
| T-10-05 | Availability/trust | silent no-op → false "safe" assumption | mitigate | UI omits section when no flags (no reassurance text); backend graceful-omit on malformed/None data (never hard-fail, Pitfall 5); first-Mode-3-video reference-anchored no-op is SURFACED as a known limit (Task 2), absolute D-05 carries Mode-3. |
| T-10-FP | Tampering (domain) | posture-alone OR posture+unrelated-wobble OR timing-misaligned-reference FP on 정은지 | mitigate | D-02 LOCAL+TEMPORAL AND-gate + DTW-path-aligned reference comparison enforced in code + `test_elite_posture_alone_no_flag` AND `test_and_gate_requires_temporal_colocation` AND `test_trunk_timing_shifted_same_extension_no_flag` headline gates (Task 1). |
| T-10-SC | Tampering | npm/pip installs | accept | No package installs this phase. |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/phase10 -x -q` — firing-rule (incl. temporal-colocation + DTW-timing-shift + elite-no-FP) + contract GREEN.
- `cd backend && python -m pytest tests -q` — full backend suite GREEN (no regression).
- `cd app && npm run typecheck` — GREEN.
- Manual (per 10-VALIDATION.md): on device, open a Mode-1 result with a trunk flag → amber section appears after score gauge, before 동작 비교, possibility-only + expert-referral copy; a result with no flags → section absent (no reassurance); a first Mode-3 video → no trunk/asymmetry flag (expected), D-05 still able to fire.
</verification>

<success_criteria>
- 정은지 elite posture alone, elite posture + unrelated/other-window wobble, AND elite extension matched against a timing-shifted reference → zero flags (LOCAL+TEMPORAL + DTW-alignment no-FP gates GREEN).
- D-02 AND-gate enforced LOCAL + TIME-ALIGNED; reference comparison is DTW-path-aligned (not raw same-index).
- D-04 trunk flag fires reference-anchored (DTW-aligned) + hip-local control-loss → "허리 부담 가능성" amber card (SC 2); Mode-1 anchors on 정은지, Mode-3 on previous video, first-video gracefully no-ops (surfaced); reference-anchored-only is the explicit RESEARCH A3 refinement of D-04 (absolute trunk deferred), not a silent reduction.
- result['safetyFlags'] persists scalar-only via a single path; _validate_safety_flags rejects nested lists; no duplicate kwarg.
- Amber warning section visually distinct + 전문가 확인 권유, omitted when empty (SC 4).
- Deterministic; phase-level temporal granularity documented as a v1 limitation; D-01 LLM boundary preserved.
</success_criteria>

<output>
Create `.planning/phases/10-injury-risk-flags/10-02-SUMMARY.md` when done.
</output>
</content>
