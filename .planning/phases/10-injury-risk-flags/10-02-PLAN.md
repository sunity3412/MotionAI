---
phase: 10-injury-risk-flags
plan: 02
type: execute
wave: 1
depends_on: [10-01]
files_modified:
  - backend/shared/python/sunity_shared/analysis/safety_flags.py
  - backend/functions/pipeline/app.py
  - backend/shared/python/sunity_shared/firestore_admin.py
  - app/src/app/analysis/result.tsx
  - app/src/lib/userAnalyses.ts
  - backend/tests/phase10/test_safety_flags_firing_rule.py
  - backend/tests/phase10/test_safety_flags_contract.py
autonomous: true
requirements: [SAFE-01]

must_haves:
  truths:
    - "정은지 elite posture alone produces ZERO flags (no false positive) — the headline gate"
    - "A flag fires ONLY when a posture condition AND a control-loss indicator are both true (D-02 AND-gate)"
    - "Trunk hyperextension (D-04) fires reference-anchored + control-loss → renders as '허리 부담 가능성' amber card (Success Criterion 2)"
    - "result['safetyFlags'] persists to Firestore as scalar-only dicts via _validate_safety_flags"
    - "Result screen shows a visually-distinct amber warning section with expert-referral copy when flags exist; section is OMITTED (no reassurance) when none (D-08, Success Criterion 4)"
    - "Flags are deterministic (LLM-free); same input → identical flags"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/safety_flags.py"
      provides: "compute_safety_flags D-02 AND-gate + D-04 trunk firing rule (reference-anchored)"
      contains: "def compute_safety_flags"
    - path: "backend/functions/pipeline/app.py"
      provides: "_process injection: compute_safety_flags after force_signals, result['safetyFlags'] set, complete_analysis safety_flags kwarg"
      contains: "compute_safety_flags"
    - path: "backend/shared/python/sunity_shared/firestore_admin.py"
      provides: "_validate_safety_flags scoped validator + wired call"
      contains: "_validate_safety_flags"
    - path: "app/src/app/analysis/result.tsx"
      provides: "InjuryRiskSection + InjuryRiskFlagCard (amber, all-4 copy map, omit-when-empty)"
      contains: "InjuryRiskSection"
  key_links:
    - from: "backend/functions/pipeline/app.py"
      to: "safety_flags.compute_safety_flags"
      via: "call after force_signals_report, before complete_analysis"
      pattern: "compute_safety_flags\\("
    - from: "backend/shared/python/sunity_shared/analysis/safety_flags.py"
      to: "force_signals_report.stability_metrics"
      via: "control-loss derivation (D-02 partner)"
      pattern: "stability_metrics"
    - from: "app/src/app/analysis/result.tsx"
      to: "analysis.result.safetyFlags"
      via: "conditional render after OctagonScore, before 동작 비교"
      pattern: "safetyFlags"
---

<objective>
Slice 1 (thinnest end-to-end vertical slice): make an injury-risk flag appear on the result screen. Implement the D-02 AND-gate core (posture-condition AND control-loss) and the D-04 trunk-femur hyperextension firing rule (reference-anchored, reusing the already-computed `left_hip`/`right_hip` angle — least new math), wire `compute_safety_flags` into `_process`, persist `result["safetyFlags"]` through a new `_validate_safety_flags` scoped validator, and render the generic amber `InjuryRiskSection`/`InjuryRiskFlagCard` (with copy mappings for ALL four flag types so later slices auto-render).

This delivers Success Criteria 2 (요추 과신전 → "허리 부담 가능성") and 4 (visually-distinct warning + 전문가 확인 권유) end-to-end, and locks the project's headline gate: 정은지 elite posture ALONE produces zero flags (D-02, Pitfall 2). D-04 stays reference-anchored only (no absolute lumbar cutoff in v1 — RESEARCH A3 / Pitfall 4) and the copy carries the rigid-body limitation parenthetical.

Purpose: prove the full data-structure → AND-gate → assembly → persist → render pipe with the lowest-risk flag, so D-05/D-03/D-06 become pure backend additions that render automatically.
Output: working trunk-hyperextension flag visible on `result.tsx`; firing-rule + contract tests GREEN.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-injury-risk-flags/10-CONTEXT.md
@.planning/phases/10-injury-risk-flags/10-RESEARCH.md
@.planning/phases/10-injury-risk-flags/10-PATTERNS.md
@.planning/phases/10-injury-risk-flags/10-UI-SPEC.md
@.planning/phases/10-injury-risk-flags/10-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-02 AND-gate + D-04 trunk firing rule in safety_flags.py</name>
  <files>backend/shared/python/sunity_shared/analysis/safety_flags.py, backend/tests/phase10/test_safety_flags_firing_rule.py</files>
  <read_first>
    - backend/shared/python/sunity_shared/analysis/safety_flags.py (the 10-01 stub being extended)
    - backend/shared/python/sunity_shared/analysis/force_signals.py:401-437,495 (StabilityMetric.severity / unstable_body_parts / ForceSignalsReport.overall_confidence — the control-loss substrate, D-02)
    - backend/shared/python/sunity_shared/analysis/skeleton.py:39-50 (JOINT_ANGLES['left_hip']=('left_shoulder','left_hip','left_knee') = trunk-femur proxy; JOINT_KEYS contract order)
    - backend/shared/python/sunity_shared/analysis/kismam.py:111-124 (score_from_deviation — REUSE for reference-anchored excess, do not reimplement)
    - .planning/phases/10-injury-risk-flags/10-RESEARCH.md (Pattern 2 _control_loss_for_joint + _maybe_flag; Pattern 4; Open Q2 control-loss granularity; Pitfall 4 trunk over-claim; State-of-the-Art A3 no absolute lumbar cutoff)
    - .planning/phases/10-injury-risk-flags/10-CONTEXT.md (D-02, D-04, D-07)
    - backend/tests/phase10/test_safety_flags_firing_rule.py (the RED tests to satisfy)
  </read_first>
  <behavior>
    - elite_angles + low_control_loss_report → compute_safety_flags returns [] (elite no-FP — headline gate).
    - trunk posture condition met (student trunk-femur angle exceeds reference trunk-femur by reference-anchored margin) AND phase-level control-loss (severity medium/high) → one SafetyFlag(flag_type='trunk_hyperextension', body_region='허리', mode_scope='both').
    - trunk posture met WITHOUT control-loss → no flag (D-02 AND-gate).
    - same input twice → identical flag list (deterministic).
    - SafetyFlag.confidence reflects force_signals_report.overall_confidence (graceful on low-confidence, Pitfall 5 — never hard-fail).
  </behavior>
  <action>
    Implement the D-02 AND-gate helpers and the D-04 trunk rule in `safety_flags.py`. Add `_control_loss_phase_level(fsr) -> bool` (true if any StabilityMetric.severity in {'medium','high'}) and `_control_loss_for_joint(fsr, joint_key) -> bool` (phase severity OR joint_key in any metric's unstable_body_parts) per RESEARCH Pattern 2; trunk (D-04) uses phase-level severity (Open Q2). Add `_maybe_flag(posture_met, control_lost, **flag_kwargs) -> SafetyFlag | None` that returns a flag ONLY when both true, else None — posture alone NEVER fires (Pitfall 2).
    Implement `_trunk_hyperextension_flag(angles, reference_angles, fsr) -> SafetyFlag | None` (D-04): compute the student's trunk-femur angle from JOINT_ANGLES['left_hip']/['right_hip'] over the hold window (mirror how dimensions selects the window); compute the reference's same angle; `excess = max(0.0, student_trunk - reference_trunk)`; posture_met when `score_from_deviation(excess, tol=20.0)` falls below a reference-anchored threshold (i.e. excess >> tol) — REFERENCE-ANCHORED ONLY, NO absolute lumbar cutoff (A3 / Pitfall 4). Set posture_condition/control_loss_signal audit strings. confidence ← `fsr.overall_confidence`. Tag the threshold provenance in a comment: reference-anchored + KISMAM tol=20° [CITED: IPSF CoP], NOT 13-video curve-fit (D-07).
    Rewrite `compute_safety_flags` to collect `[f for f in (_trunk_hyperextension_flag(...),) if f is not None]` (other rules append in 10-03/10-04). Keep numpy-only, NaN-degrades-to-no-flag (never raise).
    Un-RED the firing-rule tests: implement test bodies (or un-xfail) for elite-no-FP, AND-gate both directions, deterministic.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/phase10/test_safety_flags_firing_rule.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && python -m pytest tests/phase10/test_safety_flags_firing_rule.py::test_elite_posture_alone_no_flag -x -q` GREEN (정은지 posture alone → zero flags — MUST-HAVE headline gate).
    - `cd backend && python -m pytest tests/phase10/test_safety_flags_firing_rule.py -x -q` GREEN (AND-gate both directions + deterministic).
    - `grep -nE "(>|>=)\\s*[0-9]+(\\.[0-9]+)?\\s*#.*(absolute|lumbar)" backend/shared/python/sunity_shared/analysis/safety_flags.py` returns nothing for trunk (no absolute lumbar cutoff, A3); reference-anchored only.
    - No threshold comment in safety_flags.py justifies a cutoff by "our videos" (grep for "13" / "our video" — none in D-07-sensitive code).
  </acceptance_criteria>
  <done>D-02 AND-gate + D-04 reference-anchored trunk rule implemented; elite-no-FP GREEN; deterministic; no absolute lumbar cutoff.</done>
</task>

<task type="auto">
  <name>Task 2: _process injection + _validate_safety_flags scoped validator</name>
  <files>backend/functions/pipeline/app.py, backend/shared/python/sunity_shared/firestore_admin.py, backend/tests/phase10/test_safety_flags_contract.py</files>
  <read_first>
    - backend/functions/pipeline/app.py:3458-3468 (force_signals_report computed + _dataclass_to_camel_case_dict — inject right after), :3785-3794 (complete_analysis kwarg pattern), :3270 + :3573 (normalize_body_profile already called → experience source, D-06 input), :2976-3104 (ref.get pattern → reference.level), :3481-3489 (mode == models.MODE_EXPERT branch)
    - backend/shared/python/sunity_shared/firestore_admin.py:104 (_validate_dict_only_scalars), :223-285 (_validate_force_signals_report scoped validator to mirror), :917-921 (where force_signals_report validator is wired), :922-927 (force_pattern_inference 1:1 mirror precedent)
    - backend/shared/python/sunity_shared/analysis/safety_flags.py (compute_safety_flags signature locked in 10-01)
    - .planning/phases/10-injury-risk-flags/10-PATTERNS.md (_process injection + firestore_admin sections — A5 persistence decision)
    - .planning/phases/10-injury-risk-flags/10-RESEARCH.md (Pitfall 1 nested-array; injection-point code example)
    - backend/tests/phase10/test_safety_flags_contract.py (the RED contract test to un-xfail)
  </read_first>
  <action>
    Inject Phase 10 into `_process` immediately after `force_signals_dict = _dataclass_to_camel_case_dict(force_signals_report)` (line ~3468) and before `complete_analysis` (line ~3785). Derive inputs already in scope: `experience = (models.normalize_body_profile(meta.get("bodyProfile")) or {}).get("experience")`; `reference_level = ref.get("level") if mode == models.MODE_EXPERT and ref else None` (D-06 inputs — reachable per RESEARCH Open Q3 RESOLVED); `reference_angles_for_sym` from ref angles (mode1) or previous-analysis angles (mode3, already in scope) — pass None when unavailable (graceful). Call `compute_safety_flags(...)` with the locked kwargs and set `result["safetyFlags"] = [_dataclass_to_camel_case_dict(f) for f in safety_flags]` (scalar-only camelCase). Implement D-04 fully here even though only trunk fires; D-05/D-03/D-06 land in later waves — do NOT stub-reduce or label "v1 placeholder".
    Add `_validate_safety_flags(flags)` to firestore_admin.py mirroring `_validate_force_signals_report` (it is a top-level `list[dict]`; validate each element via `_validate_dict_only_scalars` since every SafetyFlag field is scalar — stricter than force_signals, no whitelisted list fields). Add `safety_flags=` kwarg to `complete_analysis` and wire `if safety_flags is not None: _validate_safety_flags(safety_flags); payload["result"]["safetyFlags"] = safety_flags` beside the force_signals_report wiring (lines 917-921). Pass `safety_flags=result.get("safetyFlags")` from `_process`.
    Un-xfail `test_safety_flags_contract.py`: assert `_validate_safety_flags` rejects a flag dict containing a nested list and accepts scalar-only.
    Do NOT feed SafetyFlag into the LLM `injuryRisk` prose or vice versa (D-01 — strictly independent).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/phase10/test_safety_flags_contract.py -x -q && sam validate --template backend/template.yaml 2>/dev/null; cd backend && python -m pytest tests -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && python -m pytest tests/phase10/test_safety_flags_contract.py -x -q` GREEN (nested-list rejected, scalar-only accepted).
    - `grep -n "compute_safety_flags" backend/functions/pipeline/app.py` shows the call placed after force_signals and before complete_analysis.
    - `grep -n "_validate_safety_flags" backend/shared/python/sunity_shared/firestore_admin.py` shows definition + wired call.
    - `cd backend && python -m pytest tests -q` full backend suite GREEN (no regression in force_signals/dimensions/assemble/pipeline).
    - No reference to `injuryRisk` added in safety_flags or pipeline injection (D-01 boundary).
  </acceptance_criteria>
  <done>compute_safety_flags wired into _process; result['safetyFlags'] persisted scalar-only via _validate_safety_flags; contract test GREEN; full backend suite GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: InjuryRiskSection + InjuryRiskFlagCard amber UI on result.tsx (all-4 copy map, omit-when-empty)</name>
  <files>app/src/app/analysis/result.tsx, app/src/lib/userAnalyses.ts</files>
  <read_first>
    - app/src/components/AccuracyLimitBadge.tsx:1-72 (container pattern: file-top spec-citing comment, if(!visible) return null, icon row + title + description, StyleSheet referencing tokens — closest analog for InjuryRiskFlagCard)
    - app/src/app/analysis/result.tsx:830-871 (OctagonScore card block — render injury section right after), :881-884 (동작 비교 sectionTitle — render before this), :1151-1170 (강사에게 확인할 점 conditional section block), :1569-1579 (coachQuestionRow/coachQuestionText icon-row styles to reuse), :115 (highlightNumbers)
    - app/src/lib/userAnalyses.ts (normalize() — add safetyFlags null-guard mirroring forceSignalsReport, immutable spread + ?? null)
    - app/src/theme/colors.ts:46 (warnAmber), warnAmberBg (added 10-01)
    - .planning/phases/10-injury-risk-flags/10-UI-SPEC.md (Placement, Per-flag card layout, Copywriting Contract — exact Korean copy for all 4 flag types + section disclaimer + expert-referral line; no brand red; omit-when-empty)
  </read_first>
  <action>
    Add an `InjuryRiskSection` block (inline or new component file following AccuracyLimitBadge) to result.tsx, rendered conditionally `{safetyFlags && safetyFlags.length > 0 && (...)}` placed immediately after the OctagonScore card block (after the isScoreSuppressed ternary closes) and before the "동작 비교" sectionTitle. Section structure per UI-SPEC: sectionTitle `부상 위험 신호` (typography.sectionTitle); one disclaimer line (typography.caption, textSecondary): `부상을 확정하는 신호가 아니라, 측정값에서 보이는 위험 가능성이에요. 전문가 확인을 권해드려요.`; then one `InjuryRiskFlagCard` per flag sorted severity-high-first.
    `InjuryRiskFlagCard` mirrors AccuracyLimitBadge container: amber surface `colors.warnAmberBg`, border `colors.warnAmber`, radius.card, spacing.cardPadding (tokens only — NO hardcoded colors/spacing; brand #FF4B33 FORBIDDEN here per UI-SPEC §Color). Row 1: `Ionicons name="warning"` size 16 warnAmber + flag title (typography.boxLabel, warnAmber). Row 2: "why" line (typography.caption, textPrimary, lineHeight 18). Per-card or section-footer expert-referral line: `정확한 판단은 강사 또는 전문가와 함께 확인해 주세요.`
    Implement a `flagType → {title, why}` copy map for ALL FOUR flag types from the UI-SPEC Copywriting Contract (asymmetry/trunk_hyperextension/joint_hyperextension/level_mismatch) — even though only trunk fires now, so 10-03/10-04 flags render automatically with zero UI change. The D-04 trunk "why" MUST include the rigid-body parenthetical (몸통을 한 덩어리로 추정한 값이라 정확하지 않을 수 있어요). When no flags: render NOTHING (no "안전합니다" reassurance — forbidden). Use `highlightNumbers()` for any inline number; never lead with a risk-score number.
    Add `safetyFlags` null-guard to userAnalyses.ts normalize() (immutable spread + `?? null`, mirroring forceSignalsReport).
    File-top block comment citing `10-UI-SPEC.md` (AccuracyLimitBadge convention).
  </action>
  <verify>
    <automated>cd app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && npm run typecheck` exits 0.
    - `grep -n "warnAmberBg" app/src/app/analysis/result.tsx` (or the new component) shows the amber surface used; `grep -c "#FF4B33\|colors.brand" <injury section file>` shows brand red NOT used in the injury section.
    - `grep -n "안전합니다\|부상 없음\|위험 없음" app/src/app/analysis/result.tsx` returns nothing (no reassurance text — omit-when-empty).
    - All four copy-map keys present: `grep -oE "asymmetry|trunk_hyperextension|joint_hyperextension|level_mismatch" <injury section file>` lists all 4.
    - safetyFlags null-guard present in userAnalyses.ts normalize().
  </acceptance_criteria>
  <done>Amber InjuryRiskSection renders trunk flag end-to-end, omits when empty, copy-maps all 4 flag types, no brand red, no reassurance; typecheck green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→pipeline | `meta.bodyProfile.experience` (D-06 input) crosses here untrusted. |
| pipeline→Firestore | `result["safetyFlags"]` list[dict] written via Admin SDK. |
| Firestore→app | `safetyFlags` read and rendered read-only on result.tsx. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | Firestore nested-array injection via flag dict | mitigate | `_validate_safety_flags` scoped validator rejects nested lists before write (Task 2); contract test asserts it. |
| T-10-03 | Info disclosure | medical over-claim ("부상 확정") | mitigate | Copy contract forbids confirmed-injury wording; possibility-only + expert-referral enforced (Task 3); grep gate for forbidden reassurance/claim text. |
| T-10-04 | Repudiation/trust | LLM-layer contamination (D-01) | mitigate | safety_flags + injection never touch `injuryRisk`; acceptance grep asserts no injuryRisk reference added. |
| T-10-05 | Availability/trust | silent no-op → false "safe" assumption | mitigate | UI omits section when no flags (no reassurance text); backend graceful-omit on malformed/None data (never hard-fail, Pitfall 5). |
| T-10-FP | Tampering (domain) | posture-alone FP on 정은지 | mitigate | D-02 AND-gate enforced in code + `test_elite_posture_alone_no_flag` headline gate (Task 1). |
| T-10-SC | Tampering | npm/pip installs | accept | No package installs this phase. |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/phase10 -x -q` — firing-rule + contract GREEN (incl. elite-no-FP headline gate).
- `cd backend && python -m pytest tests -q` — full backend suite GREEN (no regression).
- `cd app && npm run typecheck` — GREEN.
- Manual (per 10-VALIDATION.md): on device, open a result with a trunk flag → amber section appears after score gauge, before 동작 비교, possibility-only + expert-referral copy; a result with no flags → section absent (no reassurance).
</verification>

<success_criteria>
- 정은지 elite posture alone → zero flags (headline no-FP gate GREEN).
- D-02 AND-gate enforced (posture alone never fires).
- D-04 trunk flag fires reference-anchored + control-loss → "허리 부담 가능성" amber card (SC 2).
- result['safetyFlags'] persists scalar-only; _validate_safety_flags rejects nested lists.
- Amber warning section visually distinct + 전문가 확인 권유, omitted when empty (SC 4).
- Deterministic; D-01 LLM boundary preserved.
</success_criteria>

<output>
Create `.planning/phases/10-injury-risk-flags/10-02-SUMMARY.md` when done.
</output>
