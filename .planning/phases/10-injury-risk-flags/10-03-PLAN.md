---
phase: 10-injury-risk-flags
plan: 03
type: execute
wave: 2
depends_on: [10-02]
files_modified:
  - backend/shared/python/sunity_shared/analysis/safety_flags.py
  - backend/tests/phase10/test_safety_flags_hyperextension.py
  - backend/tests/phase10/conftest.py
autonomous: true
requirements: [SAFE-01]

must_haves:
  truths:
    - "Knee/elbow reverse-bend (hyperextension) is distinguished from normal flexion by the sign of the hinge cross-product projected onto a DETERMINISTIC body medial-lateral (frontal) axis (D-05) — direction, not just magnitude — where hyperextension_amount = max(0, 180 - included_angle)"
    - "The body frame and the per-side flexion sign are constructed by ONE fixed deterministic rule (no 'e.g.', no choice): frontal axis = unit(right_hip - left_hip) for knee / unit(right_shoulder - left_shoulder) for elbow; flexion sign s_flex = sign(dot(cross(u,w), frontal)) at the clip's MINIMUM-included-angle (clearly-flexed) calibration frame; the firing function receives only keypoints_4ch + fsr"
    - "A synthetic flexed knee/elbow produces NO joint_hyperextension flag; a synthetic reverse-bent knee/elbow WITH joint-localized control-loss produces a joint_hyperextension flag"
    - "D-05's reliability gate reads `keypoints_4ch[:, :, 3]` as **uncertainty_proxy** (pose_frame.py:326,335 — 1.0 = undetected/worst, low = good; app.py:452 confirms >0.5 = low-confidence), NOT as confidence: the gate derives `confidence = clip(1 - uncertainty, 0, 1)` or gates `uncertainty <= MAX_KP_UNCERTAINTY`, and the fail-conservative branch fires (NO flag) when uncertainty is too HIGH — never the inverse"
    - "D-05 returns NO flag (fail-conservative) on EVERY ambiguity branch: HIGH keypoint uncertainty (ch3 > MAX_KP_UNCERTAINTY) on A/V/C or frontal-axis keypoints, inconsistent segment lengths across the hold window, single-frame (non-window-consistent) reverse-bend, degenerate frontal axis, frontal axis collinear with the longitudinal axis (inversion/spin — orientation unresolvable), collinear hinge segments (no defined bend axis), no clearly-flexed calibration frame, or NaN/inf"
    - "정은지 elite intentional extension — run through MULTIPLE real reference keypoint clips (not one synthetic fixture), including mirrored orientation, spin-around-pole, and BOTH left and right limbs — still produces ZERO joint_hyperextension flags (D-02 + fail-conservative gate preserved)"
    - "unstable_body_parts joint keys consumed by D-05 match force_signals output exactly (left_knee/right_knee/left_elbow/right_elbow)"
    - "Absolute joint floors (knee >5°, elbow >10°) are tagged [CITED] from external literature, NOT 13-video curve-fit (D-07)"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/safety_flags.py"
      provides: "D-05 cross-product hyperextension-direction algorithm + deterministic frontal-axis body frame + min-angle flexion calibration + per-branch fail-conservative gate (uncertainty_proxy-correct) + _joint_hyperextension_flag"
      contains: "cross"
    - path: "backend/tests/phase10/test_safety_flags_hyperextension.py"
      provides: "deterministic-sign + flexed-vs-reverse-bent + per-branch ambiguity-conservative + uncertainty-channel-not-inverted + mirrored/spin/left-right + MULTIPLE real-elite-clip regression coverage"
      contains: "reverse"
  key_links:
    - from: "backend/shared/python/sunity_shared/analysis/safety_flags.py"
      to: "inputs.keypoints_4ch (to_coco17_array 3D coords + ch3 uncertainty_proxy)"
      via: "cross-product on knee/elbow segments + deterministic frontal-axis sign + per-branch uncertainty/geometry gate (uncertainty <= MAX_KP_UNCERTAINTY)"
      pattern: "keypoints_4ch|cross"
    - from: "safety_flags.compute_safety_flags"
      to: "_control_loss_for_joint + _phase_for_window"
      via: "joint-localized + phase-co-located control-loss (D-02 partner for D-05; phase via 10-02 _phase_for_window)"
      pattern: "_control_loss_for_joint"
---

<objective>
Slice 2: add the D-05 joint-hyperextension flag — belle's stated Mode-3 priority ("내 자세가 이러면 부상 위험 높다"). This is the phase's single genuinely novel algorithm: distinguish normal flexion from reverse-bend (genu recurvatum / elbow hyperextension) by the SIGN of the hinge cross-product projected onto a deterministic body medial-lateral axis, because dot-product gives only direction-blind 0–180° magnitude. Knee and elbow are 1-DOF hinges whose flexion happens in the sagittal plane, so the cross-product (the hinge rotation axis) is parallel to the medial-lateral axis and its sign discriminates flexion vs reverse-bend. This is valid.

Codex (iter-2) flagged D-05's sign convention as still under-specified ("e.g. shoulder→hip" + a vague "known-flexion frame" left discretion in the most fragile part) and demanded real-keypoint coverage. The direct review (round 3) flagged a HIGH defect: `keypoints_4ch[:, :, 3]` is `uncertainty_proxy`, NOT confidence, so a "reject below MIN_KP_CONF" gate would be INVERTED — admitting undetected keypoints (uncertainty≈1.0) into a high-stakes warning while rejecting good ones. This plan removes ALL discretion: ONE fixed body-frame construction, ONE deterministic flexion-calibration rule, an explicit uncertainty-correct fail-conservative branch for every ambiguity, and a MULTI-clip real-elite-keypoint regression (mirrored orientation, spin-around-pole, both limbs) sourced from PINNED motion IDs.

The flag is absolute-signal (both modes), fires via the D-02 LOCAL+TEMPORAL AND-gate with joint-localized control-loss (`unstable_body_parts` containing the specific knee/elbow key) phase-co-located via the `_phase_for_window` helper from 10-02, and auto-renders through the `InjuryRiskSection` already built in 10-02. Absolute detection floors come from external clinical literature (knee >5° genu recurvatum; elbow >10° Beighton) tagged [CITED], severity bands at 10°/20° — NOT curve-fit to the 13 videos (D-07 / Pitfall 3).

Purpose: deliver the Mode-3 core risk signal with a deterministic, literature-sourced, conservatively-gated direction detector (with correct uncertainty_proxy channel semantics) validated against MULTIPLE real elite clips.
Output: D-05 firing rule in safety_flags.py; hyperextension tests (synthetic + per-branch ambiguity + uncertainty-channel-not-inverted + multi-clip real-elite incl. mirrored/spin/left-right) GREEN; auto-rendered flag.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-injury-risk-flags/10-CONTEXT.md
@.planning/phases/10-injury-risk-flags/10-RESEARCH.md
@.planning/phases/10-injury-risk-flags/10-PATTERNS.md
@.planning/phases/10-injury-risk-flags/10-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-05 deterministic cross-product hyperextension detector (frontal-axis body frame + min-angle flexion calibration + uncertainty_proxy-correct per-branch fail-conservative gate) + _joint_hyperextension_flag</name>
  <files>backend/shared/python/sunity_shared/analysis/safety_flags.py, backend/tests/phase10/conftest.py</files>
  <read_first>
    - backend/shared/python/sunity_shared/analysis/safety_flags.py (existing _control_loss_for_joint + _phase_for_window + _maybe_flag + compute_safety_flags from 10-02)
    - backend/shared/python/sunity_shared/analysis/pose_frame.py:326,335 (to_coco17_array → (T,17,4) pole-aligned 3D; [:, :, :3] is coords; **[:, :, 3] is uncertainty_proxy, undetected default 1.0** — NOT confidence; pose_frame.py:115 uncertainty_proxy = 1 - confidence) — this is the D-05 reliability-gate input
    - backend/functions/pipeline/app.py:452-466 (_low_uncertainty_frame_indices — authoritative reading of ch3: `> 0.5` = low-confidence/refinement target; docstring warns `min < 0.5` is the OPPOSITE meaning and a prior plan misread it — do NOT repeat that inversion), :3700-3715 (downstream converts back via `confidence = clip(1.0 - kp_arr[:,:,3], 0, 1)`)
    - backend/shared/python/sunity_shared/analysis/skeleton.py:39-50 + KEYPOINT_NAMES/_IDX (left_knee=('left_hip','left_knee','left_ankle'), left_elbow=('left_shoulder','left_elbow','left_wrist'); COCO-17 index mapping via kp_index for left_hip/right_hip/left_shoulder/right_shoulder)
    - backend/shared/python/sunity_shared/analysis/dimensions.py:292-317 (_select_window(angles, profile) → (sliced,(s,e)) — the SHARED hold-window selector reused for the D-05 hold window + _phase_for_window input)
    - backend/shared/python/sunity_shared/analysis/force_signals.py:321,505,1189-1200 (PhaseBoundary + phase_boundaries for _phase_for_window; _compute_unstable_body_parts returns JOINT_KEYS → unstable_body_parts holds exactly 'left_knee'/'right_knee'/'left_elbow'/'right_elbow')
    - .planning/phases/10-injury-risk-flags/10-RESEARCH.md (Pattern 3 — cross-product math; State-of-the-Art knee >5° / elbow >10° [CITED]; Assumptions A1/A2)
    - .planning/phases/10-injury-risk-flags/10-CONTEXT.md (D-05, D-02, D-07)
    - backend/tests/phase10/conftest.py (flexed/reverse-bent + ambiguous-geometry + uncertain_reverse_bent (T,17,4) fixtures from 10-01; ch3 = uncertainty_proxy convention)
  </read_first>
  <behavior>
    - flexed_knee_4ch → _joint_hyperextension_flag returns None (normal flexion, no flag).
    - reverse_bent_knee_4ch (ch3≈0.05, low uncertainty) + joint-localized control-loss ('left_knee'/'right_knee' in unstable_body_parts) → SafetyFlag(flag_type='joint_hyperextension', body_region='무릎·팔꿈치', mode_scope='both').
    - uncertain_reverse_bent_4ch (SAME reverse-bend coords but ch3≈0.9, HIGH uncertainty) + same control-loss → None (fail-conservative on high uncertainty — this is the channel-not-inverted proof: identical geometry, only uncertainty differs).
    - reverse_bent_knee WITHOUT control-loss → None (D-02 AND-gate).
    - elite intentional extension (straight, near-180° but correct flexion side) → None (elite no-FP).
    - elbow path symmetric to knee path (frontal axis from shoulders).
    - hyperextension magnitude below the absolute floor (knee ≤5° / elbow ≤10° past neutral) → None even if sign reversed (detection floor).
    - EVERY ambiguity branch → None (fail-conservative, regardless of control-loss): HIGH ch3 uncertainty (> MAX_KP_UNCERTAINTY); inconsistent segment lengths; single-frame-only reverse-bend; degenerate frontal axis; frontal collinear with longitudinal (inversion/spin); collinear hinge segments; no clearly-flexed calibration frame; NaN/inf.
  </behavior>
  <action>
    Implement the D-05 detector in safety_flags.py per RESEARCH Pattern 3 with EXACT, DETERMINISTIC math — NO "e.g.", NO alternatives.

    EXACT HINGE MATH: for a hinge with joints (A, V, C) — knee: A=hip, V=knee, C=ankle (JOINT_ANGLES['left_knee']/['right_knee']); elbow: A=shoulder, V=elbow, C=wrist (JOINT_ANGLES['left_elbow']/['right_elbow']) — compute per frame from `keypoints_4ch[t, idx, :3]`: `u = A - V`, `w = C - V`; `included_angle = degrees(arccos(clip(dot(u,w)/(|u||w|), -1, 1)))` (0..180, direction-blind); `hyperextension_amount = max(0.0, 180.0 - included_angle)`; `n = cross(u, w)` (hinge axis).

    DETERMINISTIC BODY FRAME (HIGH-B — ONE fixed rule, no choice): per frame, build the medial-lateral (frontal) axis and the longitudinal axis from COCO-17 keypoints:
      - For KNEE limbs: `frontal = unit(kp[right_hip] - kp[left_hip])`.
      - For ELBOW limbs: `frontal = unit(kp[right_shoulder] - kp[left_shoulder])`.
      - `hip_center = 0.5*(kp[left_hip]+kp[right_hip])`, `shoulder_center = 0.5*(kp[left_shoulder]+kp[right_shoulder])`, `longitudinal = unit(shoulder_center - hip_center)`.
    Rationale (document in comment): for a 1-DOF sagittal-plane hinge, the cross product `n = u×w` is parallel to the medial-lateral axis; the SIGN of `dot(n, frontal)` therefore discriminates flexion vs reverse-bend. The longitudinal axis is used only by the orientation-degeneracy gate below. Add `_hyperextension_direction(coords_frame, a_idx, v_idx, c_idx, frontal) -> tuple[float, float]` returning `(hyperextension_amount, signed_proj)` where `signed_proj = dot(n, frontal)`.

    DETERMINISTIC FLEXION CALIBRATION (HIGH-B — pin the sign so it cannot silently invert; NO external "known-flexion frame"): the per-side flexion sign is established from the data deterministically. Over the WHOLE clip for that side, compute `included_angle[t]`; let `cal_frame = argmin(included_angle)` and `min_angle = included_angle[cal_frame]`. A frame with a small included angle can ONLY be achieved by folding (you cannot hyperextend a knee/elbow to a small included angle), so the minimum-angle frame is unambiguously flexion. Require `min_angle < CLEAR_FLEXION_MAX` (= 90.0° [ASSUMED clear-flexion bound — a deeply-bent calibration frame]); if no frame is clearly flexed (`min_angle >= CLEAR_FLEXION_MAX`) the flexion direction CANNOT be calibrated → return None (fail-conservative). Else `s_flex = sign(signed_proj at cal_frame)`; if `s_flex == 0` (degenerate at cal frame) → None. HYPEREXTENSION at a frame := `sign(signed_proj) == -s_flex` AND `hyperextension_amount > floor`, floor = [CITED] clinical cutoff: knee >5° past neutral (genu recurvatum), elbow >10° past neutral (Beighton). Severity bands at 10°/20° (knee) — tag each constant `[CITED: source]`; NEVER justify by "our 13 videos" (D-07 / Pitfall 3).

    PER-BRANCH FAIL-CONSERVATIVE GATE (HIGH-B + review HIGH — each branch returns None, each separately tested in Task 2): emit only when geometry is trustworthy. CHANNEL SEMANTICS (review HIGH): `keypoints_4ch[t, idx, 3]` is `uncertainty_proxy` (pose_frame.py:326,335; app.py:452 — 1.0 = undetected/worst, low = good). Derive `uncertainty = keypoints_4ch[..., 3]` and EITHER `confidence = np.clip(1.0 - uncertainty, 0.0, 1.0)` OR gate directly with `uncertainty <= MAX_KP_UNCERTAINTY`. Do NOT use a `MIN_KP_CONF` constant and do NOT reject "low ch3" — that is the inverted gate the review forbids. Return None if ANY holds:
      (a) keypoint uncertainty — `keypoints_4ch[t, idx, 3]` (uncertainty_proxy) for A/V/C OR for the frontal-axis keypoints (left/right hip or shoulder) ABOVE `MAX_KP_UNCERTAINTY` on the qualifying frames (i.e. uncertainty too HIGH = unreliable detection → no flag);
      (b) segment-length inconsistency — |u| or |w| ratio varies beyond a sane bound across the hold window (limb-swap/teleport guard);
      (c) single-frame noise — reverse-bend not consistent across the hold window (require a majority of hold-window frames to agree, not one spike);
      (d) degenerate frontal axis — `|kp[right_hip]-kp[left_hip]|` (or shoulders) near zero;
      (e) orientation unresolvable (inversion/spin) — `|dot(unit(frontal), unit(longitudinal))|` exceeds `COLLINEAR_COS` (the body frame collapses → cannot resolve which side is which);
      (f) collinear hinge segments — `|cross(u,w)| < EPS * |u| * |w|` (straight limb, no defined bend axis) on the qualifying frames;
      (g) no clearly-flexed calibration frame (from calibration above);
      (h) NaN/inf anywhere.
    Each threshold constant (`MAX_KP_UNCERTAINTY`, the segment-ratio bound, `COLLINEAR_COS`, `EPS`, `CLEAR_FLEXION_MAX`) is tagged `[ASSUMED conservative gate]` in a comment — these widen the no-flag region (fail-safe), they are NOT detection thresholds and are NOT curve-fit. `MAX_KP_UNCERTAINTY` is an upper bound on uncertainty_proxy (e.g. 0.5, matching app.py:452's >0.5 = low-confidence convention) — a frame with uncertainty above it is rejected. NaN/degenerate → None, never raise.

    Aggregate across the hold window (use the shared `dimensions._select_window`) and across left/right. Add `_joint_hyperextension_flag(keypoints_4ch, fsr) -> SafetyFlag | None` emitting via `_maybe_flag(posture_met, _control_loss_for_joint(fsr, knee_or_elbow_key, phase=P), ...)` where `P = _phase_for_window(fsr.phase_boundaries, s, e)` (the 10-02 helper; the D-05 hold window (s,e) from `dimensions._select_window`) — joint-localized + phase-co-located control-loss (the key MUST be the exact unstable_body_parts string, e.g. 'left_knee'); when `P is None` the flag no-ops (consistent with the D-02 temporal gate). posture_condition audit names the side + hyperextension_amount; control_loss_signal names the joint instability. Append the flag to `compute_safety_flags` alongside the trunk flag.

    Extend conftest.py: ensure flexed vs reverse-bent (T,17,4) fixtures are unambiguous (clearly forward-folded vs backward-passed-straight) with LOW ch3 uncertainty_proxy (≈0.05) AND a valid hip/shoulder frontal axis; confirm `uncertain_reverse_bent_4ch` (same reverse-bend coords, ch3≈0.9) exists from 10-01 for the channel-not-inverted test. Add per-branch ambiguity fixtures so Task 2 can test EACH gate independently: `high_uncertainty_4ch` (ch3 > MAX_KP_UNCERTAINTY on hinge/frontal keypoints), `inconsistent_segment_4ch`, `single_frame_reverse_4ch`, `degenerate_frontal_4ch`, `spin_collinear_4ch` (frontal≈longitudinal), `collinear_segment_4ch`, `no_flexion_calibration_4ch` (limb always near-straight).
    Add a MULTI-CLIP real-elite regression set with PINNED sources (MEDIUM-2): the real-elite D-05 cross-product test MUST use actual 3D `(T,17,4)` keypoints. PROVENANCE CONSTRAINT — the checked-in `backend/scripts/reference-angles.json` is **joint-ANGLE-only** (numJoints=8, jointKeys = joint-angle names), so it CANNOT reconstruct `(T,17,4)` 3D coordinates and MUST NOT be used to claim D-05 3D cross-product coverage. Real 3D elite keypoints exist only via the RTMW extraction path (`backend/scripts/extract_reference_keypoint_reports.py`, Pod-GPU `referenceKeypointReport`). Therefore the executor MUST do ONE of: (i) run a named source reference motion video through the existing RTMW extraction path to produce real `(T,17,4)` keypoints, OR (ii) generate a checked-in COMPACT `(T,17,4)` 3D fixture derived from a named source WITH provenance recorded in a conftest comment (source motion ID + extraction method + date). PIN these canonical source motion IDs (from the 정은지 reference library — KNOWN-ANSWER regression, NOT a fit target, [[calibration-source-hard-gate]]): `ref-sideway-spin` (spin-around-pole), `ref-invert` (inverted / mirrored orientation), and `ref-foxtop-split` (extreme split exercising BOTH left and right limbs). Synthetic-only data MUST NOT satisfy this test — a conftest comment must state the provenance so an executor cannot believe real-video risk is covered when it is not.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/phase10/test_safety_flags_hyperextension.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && python -m pytest tests/phase10/test_safety_flags_hyperextension.py -x -q` GREEN (flexed→no flag, reverse-bent+control-loss→flag, high-uncertainty same-geometry→no flag, AND-gate, elbow symmetric, floor respected).
    - `cd backend && python -m pytest tests/phase10/test_safety_flags_firing_rule.py::test_elite_posture_alone_no_flag -x -q` STILL GREEN (D-05 did not break elite no-FP).
    - The deterministic frontal-axis + min-angle calibration is present: `grep -nE "right_hip|right_shoulder|argmin|CLEAR_FLEXION_MAX|s_flex|signed_proj" backend/shared/python/sunity_shared/analysis/safety_flags.py` shows the fixed body-frame + calibration rule (no 'e.g.' alternative).
    - The uncertainty gate is NOT inverted: `grep -nE "MAX_KP_UNCERTAINTY|uncertainty_proxy|1\\.0 - .*\\[.*3\\]|clip\\(1" backend/shared/python/sunity_shared/analysis/safety_flags.py` shows uncertainty-correct gating; `grep -n "MIN_KP_CONF" backend/shared/python/sunity_shared/analysis/safety_flags.py` returns NOTHING (the inverted-name constant must not exist).
    - CHANNEL-LABEL GREP GATE (review HIGH item 5): no new D-05 code/comment calls ch3 "confidence" — `grep -nE "keypoints_4ch\\[.*3\\].*confiden|ch3.*confiden|\\[:, *:, *3\\].*confiden" backend/shared/python/sunity_shared/analysis/safety_flags.py` returns nothing (the channel must be described as uncertainty_proxy, not confidence).
    - Fail-conservative gate has the orientation/degeneracy branch: `grep -nE "COLLINEAR_COS|collinear|MAX_KP_UNCERTAINTY|cross\\(" backend/shared/python/sunity_shared/analysis/safety_flags.py` shows the per-branch gate constants.
    - D-05 control-loss is phase-co-located via the 10-02 helper: `grep -nE "_phase_for_window|_control_loss_for_joint" backend/shared/python/sunity_shared/analysis/safety_flags.py` shows D-05 uses phase=P (no-op when None).
    - A MULTI-clip real-elite regression test exists and is GREEN (more than one clip, incl. mirrored + spin + both limbs → zero joint_hyperextension flags): grep test names (e.g. `test_real_elite_clips_no_hyperextension`, `test_real_elite_mirrored_no_flag`, `test_real_elite_spin_no_flag`); conftest names the pinned source motion IDs (ref-sideway-spin / ref-invert / ref-foxtop-split) + extraction provenance, and does NOT reconstruct 3D from reference-angles.json.
    - D-05 passes the exact unstable_body_parts joint keys: `grep -nE "left_knee|right_knee|left_elbow|right_elbow" backend/shared/python/sunity_shared/analysis/safety_flags.py`.
    - No threshold in safety_flags.py justified by "13"/"our video" (grep — none); gate constants tagged [ASSUMED conservative gate], floors [CITED].
  </acceptance_criteria>
  <done>D-05 deterministic cross-product detector (fixed frontal-axis body frame, min-angle flexion calibration, exact 180−angle math, [CITED] floors) with an uncertainty_proxy-correct per-branch fail-conservative gate (HIGH uncertainty → no flag; incl. inversion/spin orientation gate); flexed-vs-reverse-bent discriminated; same-geometry/high-uncertainty proves gate not inverted; AND-gate + phase-co-located control-loss + elite-no-FP preserved across MULTIPLE pinned-source real elite clips; flag auto-renders.</done>
</task>

<task type="auto">
  <name>Task 2: Deterministic-sign + uncertainty-channel-not-inverted + per-branch conservative-gate + multi-clip real-elite hardening + full-suite regression guard</name>
  <files>backend/tests/phase10/test_safety_flags_hyperextension.py</files>
  <read_first>
    - backend/tests/phase10/test_safety_flags_hyperextension.py (Task 1 tests, incl. the xfail `test_d05_uncertainty_channel_not_inverted` scaffolded in 10-01)
    - backend/shared/python/sunity_shared/analysis/safety_flags.py (the D-05 impl + deterministic frontal-axis sign + min-angle calibration + uncertainty-correct per-branch gate)
    - backend/shared/python/sunity_shared/analysis/pose_frame.py:326,335 + backend/functions/pipeline/app.py:452-466 (uncertainty_proxy channel semantics — the assertion ground truth)
    - backend/tests/phase10/conftest.py (per-branch ambiguity fixtures + uncertain_reverse_bent + multi-clip pinned-source real-elite fixtures from Task 1)
    - .planning/phases/10-injury-risk-flags/10-RESEARCH.md (sign-convention "validate on elite videos before trusting"; Metadata confidence note)
    - .planning/phases/10-injury-risk-flags/10-VALIDATION.md (Sampling Rate — per-wave full backend suite)
  </read_first>
  <action>
    Add robustness tests to test_safety_flags_hyperextension.py:
    (1) LEFT and RIGHT limbs both classify a forward-fold as flexion (no mirror-flip bug — frontal axis is per-side-consistent) and both correctly flag a reverse-bend.
    (2) DETERMINISM/SIGN-STABILITY: a borderline magnitude exactly at the [CITED] floor does not flip the verdict spuriously (boundary stability); the same clip run twice yields identical verdicts (the min-angle calibration is deterministic).
    (3) WINDOW AGGREGATION: a single reversed-sign frame inside an otherwise-flexed hold does NOT emit a flag.
    (4) UNCERTAINTY-CHANNEL-NOT-INVERTED (review HIGH item 4): un-xfail and complete `test_d05_uncertainty_channel_not_inverted` — feed the SAME reverse-bend coordinates + the SAME joint-localized control-loss twice: with LOW uncertainty (`reverse_bent_knee_4ch`, ch3≈0.05) the function CAN flag (returns a joint_hyperextension flag), and with HIGH uncertainty (`uncertain_reverse_bent_4ch`, ch3≈0.9) the function MUST NOT flag (returns None). This is the lockstep proof the gate is not inverted. Assert the channel semantics directly so a future inversion fails the test.
    (5) PER-BRANCH FAIL-CONSERVATIVE — one separate negative test PER ambiguity branch proving NO flag (even with control-loss present): `high_uncertainty_4ch` (ch3 > MAX_KP_UNCERTAINTY), `inconsistent_segment_4ch`, `single_frame_reverse_4ch`, `degenerate_frontal_4ch`, `spin_collinear_4ch` (inversion/spin → frontal≈longitudinal), `collinear_segment_4ch`, `no_flexion_calibration_4ch`. Each maps to a distinct branch in Task 1's gate.
    (6) MULTI-CLIP REAL-ELITE: re-run the multi-clip real-elite regression (pinned-source 정은지 clips: ref-sideway-spin / ref-invert / ref-foxtop-split, incl. mirrored orientation, spin-around-pole, both limbs) as an explicit guard — all yield zero hyperextension flags.
    These lock the deterministic sign convention + uncertainty-correct + per-branch conservative gate that Codex iter-2 and the round-3 review flagged.
    Then run the full backend suite as the per-wave regression gate to prove safety_flags additions did not perturb force_signals/dimensions/assemble/pipeline.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/phase10 -x -q && python -m pytest tests -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && python -m pytest tests/phase10 -x -q` GREEN including left/right symmetry, boundary/determinism, window-aggregation, uncertainty-channel-not-inverted, per-branch conservative-gate (one test per branch incl. high-uncertainty), and multi-clip real-elite (mirrored + spin + both limbs) tests; ZERO xfail remaining for `test_d05_uncertainty_channel_not_inverted` (it flips GREEN here).
    - `cd backend && python -m pytest tests -q` full backend suite GREEN (zero regression).
    - Left/right symmetry test present (grep for "left"/"right"/"mirror"); a SEPARATE per-branch conservative-gate test present for each branch (grep for "uncertain"/"high_uncertainty"/"segment"/"single_frame"/"degenerate"/"spin"/"collinear"/"calibration"); the channel-not-inverted test present (grep for "uncertainty_channel_not_inverted"); multi-clip real-elite test present (grep for "clips"/"mirrored"/"spin").
  </acceptance_criteria>
  <done>Deterministic sign convention + uncertainty_proxy-correct (channel-not-inverted) + per-branch fail-conservative gate hardened (per-side, boundary-stable, window-aggregated, one test per ambiguity branch incl. high-uncertainty + inversion/spin, multi-clip pinned-source real-elite incl. mirrored/spin/both-limbs); full backend suite GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream pose→safety_flags | `inputs.keypoints_4ch` 3D coords + ch3 uncertainty_proxy (may carry high-uncertainty/NaN/inverted/spinning frames) feed the cross-product. |
| pipeline→Firestore | joint_hyperextension flag persists scalar-only (validator from 10-02). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-FP | Tampering (domain) | posture-alone / misclassified FP on 정은지 intentional extension | mitigate | D-02 LOCAL+TEMPORAL AND-gate (phase via _phase_for_window) + window aggregation + per-branch fail-conservative gate + MULTI-clip pinned-source real-elite regression incl. mirrored/spin/both-limbs (Task 1/2). |
| T-10-06 | Tampering | curve-fit threshold provenance leak | mitigate | Floors [CITED] external literature; gate constants [ASSUMED conservative gate] (widen no-flag, not detection); grep gate forbids "13 videos" justification (D-07 / Pitfall 3). |
| T-10-07 | Availability | NaN/degenerate/inverted 3D vectors crash analysis or misfire | mitigate | Every ambiguity branch → no flag, never raise (per-branch conservative gate, Pitfall 5). |
| T-10-10 | Tampering (domain) | sign-convention inversion under spin/inversion → reverse-bend read as flexion or vice versa | mitigate | Deterministic frontal-axis sign + min-angle flexion calibration + orientation gate (frontal collinear with longitudinal → no flag); left/right + spin + inverted-orientation tests (Task 2). |
| T-10-12 | Tampering (domain) | INVERTED reliability gate — ch3 read as confidence admits undetected keypoints (uncertainty≈1.0) into a warning | mitigate | Gate reads ch3 as uncertainty_proxy, rejects uncertainty > MAX_KP_UNCERTAINTY (no MIN_KP_CONF); `test_d05_uncertainty_channel_not_inverted` + channel-label grep gate (Task 1/2). |
| T-10-SC | Tampering | npm/pip installs | accept | No package installs this phase. |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/phase10 -x -q` — all phase10 GREEN incl. D-05 deterministic-sign + uncertainty-channel-not-inverted + per-branch gate + multi-clip real-elite + elite-no-FP.
- `cd backend && python -m pytest tests -q` — full backend suite GREEN.
- Manual: on device, a result with a reverse-bend + instability shows the "무릎·팔꿈치 과신전 가능성" amber card (auto-rendered via 10-02 section); 정은지 elite clips (incl. mirrored/spin) show no joint flag.
</verification>

<success_criteria>
- D-05 distinguishes flexion from reverse hyperextension via exact 180−angle math + ONE deterministic frontal-axis body frame + min-angle flexion calibration (no discretion, sign cannot silently invert).
- The reliability gate reads ch3 as uncertainty_proxy (HIGH uncertainty → no flag), not confidence; channel-not-inverted regression GREEN; no MIN_KP_CONF constant.
- Flexed → no flag; reverse-bent + co-located control-loss → joint_hyperextension flag (D-02); EVERY ambiguity branch → no flag (fail-conservative), each separately tested.
- 정은지 elite no-FP preserved AND verified against MULTIPLE pinned-source real elite clips incl. mirrored orientation, spin-around-pole, and both limbs (not synthetic-only; reference-angles.json explicitly excluded as 3D source).
- Absolute floors [CITED] (knee >5°, elbow >10°); gate constants [ASSUMED conservative]; no 13-video curve-fit; unstable_body_parts keys match exactly.
- Flag auto-renders via 10-02 InjuryRiskSection; full backend suite GREEN.
</success_criteria>

<output>
Create `.planning/phases/10-injury-risk-flags/10-03-SUMMARY.md` when done.
</output>
</content>
