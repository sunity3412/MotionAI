---
phase: 10-injury-risk-flags
plan: 03
type: execute
wave: 2
depends_on: [10-02]
files_modified:
  - backend/shared/python/sunity_shared/analysis/safety_flags.py
  - backend/tests/phase10/test_safety_flags_hyperextension.py
  - backend/tests/phase10/conftest.py
autonomous: true
requirements: [SAFE-01]

must_haves:
  truths:
    - "Knee/elbow reverse-bend (hyperextension) is distinguished from normal flexion by the sign of the hinge cross-product projected onto a DETERMINISTIC body medial-lateral (frontal) axis (D-05) — direction, not just magnitude — where hyperextension_amount = max(0, 180 - included_angle)"
    - "The body frame and the per-side flexion sign are constructed by ONE fixed deterministic rule (no 'e.g.', no choice): frontal axis = unit(right_hip - left_hip) for knee / unit(right_shoulder - left_shoulder) for elbow; flexion sign s_flex = sign(dot(cross(u,w), frontal)) at the clip's MINIMUM-included-angle (clearly-flexed) calibration frame; the firing function `_joint_hyperextension_flags(*, angles, keypoints_4ch, fsr, profile)` receives angles + keypoints_4ch + fsr + profile (angles AND profile are REQUIRED to drive the shared `dimensions._select_window(angles, profile)` hold-window selector — D-05 cannot compute its (s,e) window or phase P from keypoints alone; the public `compute_safety_flags(*, angles, ..., keypoints_4ch, ..., profile)` already has both in scope — 10-01 L114)"
    - "D-05 covers up to FOUR simultaneous candidates (left_knee, right_knee, left_elbow, right_elbow); the helper returns `list[SafetyFlag]` and emits a SINGLE consolidated `joint_hyperextension` flag (matching the single 10-02 UI copy-map card key — NOT one card per joint) chosen by HIGHEST severity with a deterministic tie-break (severity desc, then the FIXED joint order left_knee, right_knee, left_elbow, right_elbow); the posture_condition audit string names the worst joint; zero qualifying candidates → empty list"
    - "A synthetic flexed knee/elbow produces NO joint_hyperextension flag; a synthetic reverse-bent knee/elbow WITH same-phase joint-localized control-loss produces a joint_hyperextension flag — the positive reverse-bend fixtures are TWO-SEGMENT clips (a calibration fold whose min included_angle < CLEAR_FLEXION_MAX FOLLOWED BY a reverse-bend hold; profile.hold_window targets the hold) so the calibration premise is satisfied and the positive path is reachable"
    - "D-05's reliability gate reads `keypoints_4ch[:, :, 3]` as **uncertainty_proxy** (pose_frame.py:326,335 — 1.0 = undetected/worst, low = good; app.py:452 confirms >0.5 = low-confidence), NOT as confidence: the gate derives `confidence = clip(1 - uncertainty, 0, 1)` or gates `uncertainty <= MAX_KP_UNCERTAINTY`, and the fail-conservative branch fires (NO flag) when uncertainty is too HIGH — never the inverse"
    - "The NaN/inf AND uncertainty gate is SCOPED to ONLY the keypoints actually used for a side's computation — the hinge triplet (knee: hip-knee-ankle / elbow: shoulder-elbow-wrist), the frontal-axis pair (left/right hip for knee, left/right shoulder for elbow), and the longitudinal-axis centers (left_hip, right_hip, left_shoulder, right_shoulder) — NaN/high-uncertainty in any OTHER (unused) keypoint, e.g. a face keypoint (nose/eyes/ears) which to_coco17_array defaults to NaN+uncertainty 1.0 (pose_frame.py:335), must NOT cause a no-op"
    - "D-05 returns NO flag (fail-conservative) on EVERY ambiguity branch: keypoints_4ch/angles frame-count mismatch, no derivable phase (P is None — empty phase_boundaries or <50% overlap), HIGH keypoint uncertainty (ch3 > MAX_KP_UNCERTAINTY) on the USED keypoints (A/V/C, frontal-axis pair, or longitudinal centers), inconsistent segment lengths across the hold window, single-frame (non-window-consistent) reverse-bend, degenerate frontal axis, frontal axis collinear with the longitudinal axis (inversion/spin — orientation unresolvable), collinear hinge segments (no defined bend axis), no clearly-flexed calibration frame, or NaN/inf in a USED keypoint"
    - "정은지 elite intentional extension — run through MULTIPLE real reference keypoint clips (not one synthetic fixture), including mirrored orientation, spin-around-pole, and BOTH left and right limbs — still produces ZERO joint_hyperextension flags (D-02 + fail-conservative gate preserved)"
    - "unstable_body_parts joint keys consumed by D-05 match force_signals output exactly (left_knee/right_knee/left_elbow/right_elbow)"
    - "Absolute joint floors (knee >5°, elbow >10°) are tagged [CITED] from external literature, NOT 13-video curve-fit (D-07)"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/safety_flags.py"
      provides: "D-05 cross-product hyperextension-direction algorithm + deterministic frontal-axis body frame + min-angle flexion calibration + per-branch fail-conservative gate (uncertainty_proxy-correct, SCOPED to used keypoints only) + `_joint_hyperextension_flags(*, angles, keypoints_4ch, fsr, profile) -> list[SafetyFlag]` (window-once + multi-joint consolidation)"
      contains: "cross"
    - path: "backend/tests/phase10/test_safety_flags_hyperextension.py"
      provides: "deterministic-sign + flexed-vs-reverse-bent (two-segment) + per-branch ambiguity-conservative + scoped-NaN-on-unused-keypoint-still-flags + uncertainty-channel-not-inverted + helper-contract (wrong-phase/wrong-joint/missing-phase-boundaries/frame-mismatch/same-phase-positive/multi-joint) + mirrored/spin/left-right + MULTIPLE real-elite-clip regression coverage + real-elite (T,17,4) fixture schema gate (to_coco17_array provenance)"
      contains: "reverse"
  key_links:
    - from: "backend/shared/python/sunity_shared/analysis/safety_flags.py"
      to: "inputs.keypoints_4ch (to_coco17_array 3D coords + ch3 uncertainty_proxy)"
      via: "cross-product on knee/elbow segments + deterministic frontal-axis sign + per-branch uncertainty/geometry gate (uncertainty <= MAX_KP_UNCERTAINTY) scoped to used keypoints"
      pattern: "keypoints_4ch|cross"
    - from: "safety_flags.compute_safety_flags"
      to: "_control_loss_for_joint + _phase_for_window + dimensions._select_window"
      via: "angles+profile-driven hold window (s,e) → phase P (once) → joint-localized + phase-co-located control-loss (D-02 partner for D-05; phase via 10-02 _phase_for_window)"
      pattern: "_control_loss_for_joint"
---

<objective>
Slice 2: add the D-05 joint-hyperextension flag — belle's stated Mode-3 priority ("내 자세가 이러면 부상 위험 높다"). This is the phase's single genuinely novel algorithm: distinguish normal flexion from reverse-bend (genu recurvatum / elbow hyperextension) by the SIGN of the hinge cross-product projected onto a deterministic body medial-lateral axis, because dot-product gives only direction-blind 0–180° magnitude. Knee and elbow are 1-DOF hinges whose flexion happens in the sagittal plane, so the cross-product (the hinge rotation axis) is parallel to the medial-lateral axis and its sign discriminates flexion vs reverse-bend. This is valid.

Codex (iter-2) flagged D-05's sign convention as still under-specified ("e.g. shoulder→hip" + a vague "known-flexion frame" left discretion in the most fragile part) and demanded real-keypoint coverage. The direct review (round 3) flagged a HIGH defect: `keypoints_4ch[:, :, 3]` is `uncertainty_proxy`, NOT confidence, so a "reject below MIN_KP_CONF" gate would be INVERTED — admitting undetected keypoints (uncertainty≈1.0) into a high-stakes warning while rejecting good ones. The direct review (round 5) flagged a HIGH defect: the D-05 helper's old `(keypoints_4ch, fsr)` signature was UNCALLABLE — it must call `dimensions._select_window(angles, profile)` (which REQUIRES `angles`) to get the hold window (s,e) and then `_phase_for_window`, yet `angles`/`profile` were never passed; and a MEDIUM: with up to 4 simultaneous candidates the singular `SafetyFlag | None` return left the multi-joint rule undefined. The direct review (round 6) flagged: (HIGH-1) the dual-variable `window_disjoint_report` could not isolate the D-05 phase gate (it had the wrong joint AND the wrong phase, so a pass could come from the joint gate while the phase gate silently failed) — D-05 now uses two SINGLE-VARIABLE fixtures (`wrong_phase_knee_control_loss_report` / `wrong_joint_same_phase_report`); (HIGH-2) the positive fixtures conflicted with the calibration premise (no guaranteed clear-flexion frame) — they are now TWO-SEGMENT clips (calibration fold + reverse-bend hold); (MEDIUM) the NaN/inf guard was over-broad (a blanket NaN-anywhere reject no-ops on irrelevant face keypoints which to_coco17_array defaults to NaN) — the guard is now SCOPED to the used keypoints only. This plan removes ALL discretion: ONE fixed body-frame construction, ONE deterministic flexion-calibration rule, an explicit uncertainty-correct fail-conservative branch for every ambiguity scoped to the used keypoints, a callable keyword-only helper that threads `angles`/`profile` and computes the phase ONCE, a pinned multi-joint consolidation rule (single worst-severity card with a fixed tie-break), and a MULTI-clip real-elite-keypoint regression (mirrored orientation, spin-around-pole, both limbs) sourced from PINNED motion IDs.

The flag is absolute-signal (both modes), fires via the D-02 LOCAL+TEMPORAL AND-gate with joint-localized control-loss (`unstable_body_parts` containing the specific knee/elbow key) phase-co-located via the `_phase_for_window` helper from 10-02, and auto-renders through the `InjuryRiskSection` already built in 10-02. Absolute detection floors come from external clinical literature (knee >5° genu recurvatum; elbow >10° Beighton) tagged [CITED], severity bands at 10°/20° — NOT curve-fit to the 13 videos (D-07 / Pitfall 3).

Purpose: deliver the Mode-3 core risk signal with a deterministic, literature-sourced, conservatively-gated direction detector (with correct uncertainty_proxy channel semantics scoped to used keypoints, a callable angles+profile-threaded helper, and a pinned multi-joint rule) validated against MULTIPLE real elite clips.
Output: D-05 firing rule in safety_flags.py; hyperextension tests (synthetic two-segment + per-branch ambiguity + scoped-NaN + helper-contract + uncertainty-channel-not-inverted + multi-clip real-elite incl. mirrored/spin/left-right) GREEN; auto-rendered flag.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-injury-risk-flags/10-CONTEXT.md
@.planning/phases/10-injury-risk-flags/10-RESEARCH.md
@.planning/phases/10-injury-risk-flags/10-PATTERNS.md
@.planning/phases/10-injury-risk-flags/10-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-05 deterministic cross-product hyperextension detector (frontal-axis body frame + min-angle flexion calibration + uncertainty_proxy-correct, scoped-to-used-keypoints per-branch fail-conservative gate) + callable `_joint_hyperextension_flags(*, angles, keypoints_4ch, fsr, profile)` with window-once + multi-joint consolidation</name>
  <files>backend/shared/python/sunity_shared/analysis/safety_flags.py, backend/tests/phase10/conftest.py</files>
  <read_first>
    - backend/shared/python/sunity_shared/analysis/safety_flags.py (existing _control_loss_for_joint + _phase_for_window + _maybe_flag + _trunk_hyperextension_flag(angles, reference_angles, fsr, profile) + compute_safety_flags(*, angles, keypoints_4ch, force_signals_report, ..., profile) from 10-01/10-02 — NOTE the trunk helper ALREADY threads angles+profile into _select_window; D-05 mirrors that exactly)
    - backend/shared/python/sunity_shared/analysis/pose_frame.py:326,335 (to_coco17_array → (T,17,4) pole-aligned 3D; [:, :, :3] is coords; **[:, :, 3] is uncertainty_proxy, undetected default 1.0** — NOT confidence; missing/UNUSED keypoints INCLUDING face keypoints default to NaN + uncertainty 1.0 — this is WHY a blanket NaN-anywhere guard over-rejects; pose_frame.py:115 uncertainty_proxy = 1 - confidence) — this is the D-05 reliability-gate input
    - backend/shared/python/sunity_shared/analysis/pose_frame.py:325 `to_coco17_array(pose_frames)` is THE ONLY (T,17,4) 3D extractor (x,y,z,uncertainty_proxy; 17 kp in skeleton.KEYPOINT_NAMES order); backend/scripts/extract_reference_keypoint_reports.py:113 + assemble.py:781,924 `build_keypoint_report` produce a 2D 8-keypoint overlay (data=T*J*2, J=8) and are NOT a (T,17,4) source — negative provenance for the real-elite fixture
    - backend/functions/pipeline/app.py:452-466 (_low_uncertainty_frame_indices — authoritative reading of ch3: `> 0.5` = low-confidence/refinement target; docstring warns `min < 0.5` is the OPPOSITE meaning and a prior plan misread it — do NOT repeat that inversion), :3700-3715 (downstream converts back via `confidence = clip(1.0 - kp_arr[:,:,3], 0, 1)`)
    - backend/shared/python/sunity_shared/analysis/skeleton.py:10-50 + KEYPOINT_NAMES/_IDX (left_knee=('left_hip','left_knee','left_ankle'), left_elbow=('left_shoulder','left_elbow','left_wrist'); COCO-17 index mapping via kp_index for left_hip/right_hip/left_shoulder/right_shoulder; face keypoints nose/left_eye/right_eye/left_ear/right_ear are indices 0-4 and are UNUSED by D-05 — the scoped-NaN guard must ignore them)
    - backend/shared/python/sunity_shared/analysis/dimensions.py:292-317 (_select_window(angles, profile) → (sliced,(s,e)) — the SHARED hold-window selector; angles is REQUIRED — reused for the D-05 hold window + _phase_for_window input; identical call to the 10-02 trunk helper)
    - backend/shared/python/sunity_shared/analysis/force_signals.py:321,505,1189-1200 (PhaseBoundary + phase_boundaries for _phase_for_window; _compute_unstable_body_parts returns JOINT_KEYS → unstable_body_parts holds exactly 'left_knee'/'right_knee'/'left_elbow'/'right_elbow')
    - .planning/phases/10-injury-risk-flags/10-RESEARCH.md (Pattern 3 — cross-product math; State-of-the-Art knee >5° / elbow >10° [CITED]; Assumptions A1/A2)
    - .planning/phases/10-injury-risk-flags/10-CONTEXT.md (D-05, D-02, D-07)
    - backend/tests/phase10/conftest.py (TWO-SEGMENT flexed/reverse-bent + ambiguous-geometry + uncertain_reverse_bent + multi_joint_reverse_bent + no_phase_boundaries_report + wrong_phase_knee_control_loss_report + wrong_joint_same_phase_report (T,17,4)/report fixtures from 10-01; ch3 = uncertainty_proxy convention)
  </read_first>
  <behavior>
    - flexed_knee_4ch → `_joint_hyperextension_flags(*, angles, keypoints_4ch, fsr, profile)` returns [] (normal flexion, no flag).
    - reverse_bent_knee_4ch (TWO-SEGMENT: calibration fold + reverse-bend hold, ch3≈0.05, low uncertainty) + SAME-phase joint-localized control-loss ('left_knee'/'right_knee' in unstable_body_parts) → [SafetyFlag(flag_type='joint_hyperextension', body_region='무릎·팔꿈치', mode_scope='both')] (exactly ONE consolidated flag).
    - uncertain_reverse_bent_4ch (SAME two-segment reverse-bend coords but ch3≈0.9, HIGH uncertainty) + same control-loss → [] (fail-conservative on high uncertainty — channel-not-inverted proof: identical geometry, only uncertainty differs).
    - SCOPED NaN (MEDIUM): reverse_bent_knee_4ch with NaN injected into an UNUSED keypoint (a face keypoint, e.g. 'nose' / index 0) + same-phase control-loss → STILL [one flag] (the NaN/inf + uncertainty guard checks ONLY the used keypoints: hinge triplet + frontal-axis pair + longitudinal centers).
    - reverse_bent_knee WITHOUT control-loss → [] (D-02 AND-gate).
    - WRONG-PHASE (single-variable): reverse_bent_knee + `wrong_phase_knee_control_loss_report` (the relevant knee IS in unstable_body_parts but only in a phase that does NOT overlap the D-05 hold window P) → [] (temporal gate — only the phase is wrong; the joint is correct).
    - WRONG-JOINT (single-variable): reverse_bent_knee + `wrong_joint_same_phase_report` (instability in the SAME/overlapping phase as P but on a DIFFERENT joint-angle key, e.g. 'left_elbow', NOT the knee) → [] (joint-locality gate — only the joint is wrong; the phase is correct).
    - MISSING phase_boundaries: fsr.phase_boundaries empty/None → _phase_for_window returns None → [] (no-op; never widen to "any phase").
    - FRAME MISMATCH: keypoints_4ch.shape[0] != angles.shape[0] → [] (fail-conservative; window indices derived from angles must index keypoints validly).
    - MULTI-JOINT: reverse-bend on BOTH a knee AND an elbow (each with same-phase control-loss) → exactly ONE consolidated flag = the worst by (severity desc, then the FIXED order left_knee, right_knee, left_elbow, right_elbow); posture_condition names that worst joint.
    - elite intentional extension (straight, near-180° but correct flexion side) → [] (elite no-FP).
    - elbow path symmetric to knee path (frontal axis from shoulders).
    - hyperextension magnitude below the absolute floor (knee ≤5° / elbow ≤10° past neutral) → [] even if sign reversed (detection floor).
    - EVERY ambiguity branch → [] (fail-conservative, regardless of control-loss): HIGH ch3 uncertainty (> MAX_KP_UNCERTAINTY) on a USED keypoint; inconsistent segment lengths; single-frame-only reverse-bend; degenerate frontal axis; frontal collinear with longitudinal (inversion/spin); collinear hinge segments; no clearly-flexed calibration frame; NaN/inf in a USED keypoint.
  </behavior>
  <action>
    Implement the D-05 detector in safety_flags.py per RESEARCH Pattern 3 with EXACT, DETERMINISTIC math — NO "e.g.", NO alternatives.

    EXACT HINGE MATH: for a hinge with joints (A, V, C) — knee: A=hip, V=knee, C=ankle (JOINT_ANGLES['left_knee']/['right_knee']); elbow: A=shoulder, V=elbow, C=wrist (JOINT_ANGLES['left_elbow']/['right_elbow']) — compute per frame from `keypoints_4ch[t, idx, :3]`: `u = A - V`, `w = C - V`; `included_angle = degrees(arccos(clip(dot(u,w)/(|u||w|), -1, 1)))` (0..180, direction-blind); `hyperextension_amount = max(0.0, 180.0 - included_angle)`; `n = cross(u, w)` (hinge axis).

    DETERMINISTIC BODY FRAME (HIGH-B — ONE fixed rule, no choice): per frame, build the medial-lateral (frontal) axis and the longitudinal axis from COCO-17 keypoints:
      - For KNEE limbs: `frontal = unit(kp[right_hip] - kp[left_hip])`.
      - For ELBOW limbs: `frontal = unit(kp[right_shoulder] - kp[left_shoulder])`.
      - `hip_center = 0.5*(kp[left_hip]+kp[right_hip])`, `shoulder_center = 0.5*(kp[left_shoulder]+kp[right_shoulder])`, `longitudinal = unit(shoulder_center - hip_center)`.
    Rationale (document in comment): for a 1-DOF sagittal-plane hinge, the cross product `n = u×w` is parallel to the medial-lateral axis; the SIGN of `dot(n, frontal)` therefore discriminates flexion vs reverse-bend. The longitudinal axis is used only by the orientation-degeneracy gate below. Add `_hyperextension_direction(coords_frame, a_idx, v_idx, c_idx, frontal) -> tuple[float, float]` returning `(hyperextension_amount, signed_proj)` where `signed_proj = dot(n, frontal)`.

    USED-KEYPOINT SET (MEDIUM — name the exact keypoints the gate is scoped to; this is the ONLY set the NaN/inf + uncertainty guard inspects): for a given candidate side the USED keypoints are exactly the union of — the hinge triplet (knee: `{left|right_hip, left|right_knee, left|right_ankle}`; elbow: `{left|right_shoulder, left|right_elbow, left|right_wrist}`), the frontal-axis pair (`{left_hip, right_hip}` for knee / `{left_shoulder, right_shoulder}` for elbow), and the longitudinal-axis centers (`{left_hip, right_hip, left_shoulder, right_shoulder}`). Face keypoints (nose/left_eye/right_eye/left_ear/right_ear, COCO indices 0-4) and the contralateral/unused-limb keypoints are NEVER in this set — their NaN/uncertainty MUST be ignored. Build the index list per side via `skeleton.kp_index(...)` and gate ONLY those columns.

    DETERMINISTIC FLEXION CALIBRATION (HIGH-B — pin the sign so it cannot silently invert; NO external "known-flexion frame"): the per-side flexion sign is established from the data deterministically. Over the WHOLE clip for that side, compute `included_angle[t]`; let `cal_frame = argmin(included_angle)` and `min_angle = included_angle[cal_frame]`. A frame with a small included angle can ONLY be achieved by folding (you cannot hyperextend a knee/elbow to a small included angle), so the minimum-angle frame is unambiguously flexion. Require `min_angle < CLEAR_FLEXION_MAX` (= 90.0° [ASSUMED clear-flexion bound — a deeply-bent calibration frame]); if no frame is clearly flexed (`min_angle >= CLEAR_FLEXION_MAX`) the flexion direction CANNOT be calibrated → no candidate for that side (fail-conservative). NOTE the positive (T,17,4) fixtures are TWO-SEGMENT (a calibration fold segment FOLLOWED by a reverse-bend hold segment, with `profile.hold_window` pinned to the hold) precisely so this whole-clip `argmin` lands in the calibration fold while the flaggable reverse-bend lives in the hold window — calibration scans the WHOLE clip, the per-frame hyperextension verdict is taken over the hold-window slice. Else `s_flex = sign(signed_proj at cal_frame)`; if `s_flex == 0` (degenerate at cal frame) → no candidate. HYPEREXTENSION at a frame := `sign(signed_proj) == -s_flex` AND `hyperextension_amount > floor`, floor = [CITED] clinical cutoff: knee >5° past neutral (genu recurvatum), elbow >10° past neutral (Beighton). Severity bands at 10°/20° (knee) — tag each constant `[CITED: source]`; NEVER justify by "our 13 videos" (D-07 / Pitfall 3).

    PER-BRANCH FAIL-CONSERVATIVE GATE (HIGH-B + review HIGH/MEDIUM — each branch yields NO candidate, each separately tested in Task 2; the gate inspects ONLY the USED-KEYPOINT SET above): emit only when geometry is trustworthy. CHANNEL SEMANTICS (review HIGH): `keypoints_4ch[t, idx, 3]` is `uncertainty_proxy` (pose_frame.py:326,335; app.py:452 — 1.0 = undetected/worst, low = good). Derive `uncertainty = keypoints_4ch[..., 3]` and EITHER `confidence = np.clip(1.0 - uncertainty, 0.0, 1.0)` OR gate directly with `uncertainty <= MAX_KP_UNCERTAINTY`. Do NOT use a `MIN_KP_CONF` constant and do NOT reject "low ch3" — that is the inverted gate the review forbids. A side yields no candidate if ANY holds (evaluated ONLY over the USED keypoints for that side):
      (a) keypoint uncertainty — `keypoints_4ch[t, idx, 3]` (uncertainty_proxy) for the hinge triplet A/V/C OR the frontal-axis pair (left/right hip or shoulder) OR the longitudinal centers ABOVE `MAX_KP_UNCERTAINTY` on the qualifying frames (i.e. uncertainty too HIGH = unreliable detection → no flag). UNUSED keypoints (face/contralateral) are NOT inspected;
      (b) segment-length inconsistency — |u| or |w| ratio varies beyond a sane bound across the hold window (limb-swap/teleport guard);
      (c) single-frame noise — reverse-bend not consistent across the hold window (require a majority of hold-window frames to agree, not one spike);
      (d) degenerate frontal axis — `|kp[right_hip]-kp[left_hip]|` (or shoulders) near zero;
      (e) orientation unresolvable (inversion/spin) — `|dot(unit(frontal), unit(longitudinal))|` exceeds `COLLINEAR_COS` (the body frame collapses → cannot resolve which side is which);
      (f) collinear hinge segments — `|cross(u,w)| < EPS * |u| * |w|` (straight limb, no defined bend axis) on the qualifying frames;
      (g) no clearly-flexed calibration frame (from calibration above);
      (h) NaN/inf in a USED keypoint — check `np.isnan`/`np.isinf` ONLY over the USED-KEYPOINT SET columns (coords + ch3) for that side. NaN/inf in an UNUSED keypoint (face/contralateral, which to_coco17_array defaults to NaN — pose_frame.py:335) MUST NOT trigger this branch.
    Each threshold constant (`MAX_KP_UNCERTAINTY`, the segment-ratio bound, `COLLINEAR_COS`, `EPS`, `CLEAR_FLEXION_MAX`) is tagged `[ASSUMED conservative gate]` in a comment — these widen the no-flag region (fail-safe), they are NOT detection thresholds and are NOT curve-fit. `MAX_KP_UNCERTAINTY` is an upper bound on uncertainty_proxy (e.g. 0.5, matching app.py:452's >0.5 = low-confidence convention) — a frame with uncertainty above it is rejected. NaN/degenerate → no candidate, never raise.

    CALLABLE FIRING FUNCTION (review HIGH — the helper MUST receive the inputs it needs; the old `(keypoints_4ch, fsr)` form was UNCALLABLE because it could not build the hold window): define
      `def _joint_hyperextension_flags(*, angles, keypoints_4ch, fsr, profile) -> list[SafetyFlag]:`
    keyword-only, plural return. Steps, in order:
      1. FRAME-ALIGN GUARD: if `keypoints_4ch.shape[0] != angles.shape[0]` → return `[]` (the two arrays MUST be frame-aligned for window indices derived from `angles` to index `keypoints_4ch` validly — fail-conservative). Also `[]` on empty arrays. (Per-keypoint NaN/inf is handled by the SCOPED gate (h) above, NOT a blanket array-wide check — a face-keypoint NaN must not abort here.)
      2. WINDOW ONCE: compute the hold window a SINGLE time via the SHARED selector `_, (s, e) = dimensions._select_window(angles, profile)` (drift ban — identical call to the 10-02 trunk helper; this is WHY angles+profile are required), then `P = _phase_for_window(fsr.phase_boundaries, s, e)` ONCE; if `P is None` (no boundary clears 50% overlap, or none exist) → return `[]` (no-op, never widen to any phase — consistent with the D-02 temporal gate). Reuse this single `P` for EVERY joint (do NOT recompute the window/phase per joint).
      3. PER-JOINT CANDIDATES: for each `joint_key` in the FIXED deterministic order `('left_knee','right_knee','left_elbow','right_elbow')`, run the cross-product / frontal-axis / WHOLE-clip calibration / per-branch-gate detector above; the per-frame hyperextension verdict is taken over the hold-window slice `keypoints_4ch[s:e]`. A candidate QUALIFIES when posture_met (sign reversed beyond the [CITED] floor, consistent across the window, all scoped gates pass) AND `_control_loss_for_joint(fsr, joint_key, phase=P)` is True (joint-localized + phase-co-located; the key MUST be the exact unstable_body_parts string). Each candidate records its severity (from the hyperextension_amount bands 10°/20°) and its audit strings.
      4. MULTI-JOINT CONSOLIDATION (review MEDIUM — pin ONE rule, no 'e.g.'): emit a SINGLE consolidated `joint_hyperextension` SafetyFlag (matching the single 10-02 UI copy-map card key — NOT one card per joint). Pick the WORST candidate by the TOTAL deterministic ordering `(severity desc, then the index of joint_key in ('left_knee','right_knee','left_elbow','right_elbow') asc)`. The consolidated flag: `flag_type='joint_hyperextension'`, `body_region='무릎·팔꿈치'`, `mode_scope='both'`, `severity` = worst candidate's severity, `confidence` ← `fsr.overall_confidence`, `posture_condition` audit names the worst joint + its hyperextension_amount, `control_loss_signal` names that joint's instability. Return `[flag]` when ≥1 candidate qualifies, else `[]`.

    CALL SITE: in `compute_safety_flags`, call `_joint_hyperextension_flags(angles=angles, keypoints_4ch=keypoints_4ch, fsr=force_signals_report, profile=profile)` (the public signature already has `angles`, `keypoints_4ch`, `force_signals_report`, `profile` in scope — 10-01 L114) and `.extend()` the returned list alongside the trunk flag. The trunk/asymmetry helpers stay singular-per-helper appended; ONLY D-05's list is `.extend()`-ed — keep the assembly consistent.

    Extend conftest.py: ensure the TWO-SEGMENT flexed vs reverse-bent (T,17,4) fixtures from 10-01 are unambiguous (clearly forward-folded calibration segment vs backward-passed-straight reverse-bend hold) with LOW ch3 uncertainty_proxy (≈0.05) AND a valid hip/shoulder frontal axis, `profile.hold_window` on the hold segment; confirm `uncertain_reverse_bent_4ch` (same two-segment reverse-bend coords, ch3≈0.9), `multi_joint_reverse_bent_4ch` (two-segment, reverse-bend on BOTH a knee and an elbow), `no_phase_boundaries_report` (knee control-loss present but EMPTY phase_boundaries), `wrong_phase_knee_control_loss_report` (knee in unstable_body_parts but non-overlapping phase), and `wrong_joint_same_phase_report` (different joint in the overlapping phase) exist from 10-01 for the channel-not-inverted, multi-joint, missing-phase, wrong-phase, and wrong-joint tests. Add per-branch ambiguity fixtures so Task 2 can test EACH gate independently: `high_uncertainty_4ch` (ch3 > MAX_KP_UNCERTAINTY on hinge/frontal/longitudinal keypoints), `inconsistent_segment_4ch`, `single_frame_reverse_4ch`, `degenerate_frontal_4ch`, `spin_collinear_4ch` (frontal≈longitudinal), `collinear_segment_4ch`, `no_flexion_calibration_4ch` (limb always near-straight — no clear-flexion frame). The wrong-phase / wrong-joint negatives reuse the single-variable reports (10-01); the frame-mismatch negative is built in-test by truncating `keypoints_4ch` rows relative to `angles`; the scoped-NaN positive is built in-test by injecting NaN into an unused face keypoint of `reverse_bent_knee_4ch`. All (T,17,4) fixtures MUST have the SAME row count as the angles matrix passed alongside them (frame-aligned), except the deliberately-mismatched frame-mismatch case.
    Add a MULTI-CLIP real-elite regression set with PINNED sources (MEDIUM-2): the real-elite D-05 cross-product test MUST use actual 3D `(T,17,4)` keypoints. NEGATIVE PROVENANCE (review HIGH — VERIFIED): TWO checked-in artifacts are in the SAME exclusion class and are FORBIDDEN as `(T,17,4)` 3D sources — (1) `backend/scripts/reference-angles.json` is joint-ANGLE-only (numJoints=8, jointKeys = joint-angle names) so it cannot reconstruct 3D coordinates; (2) `referenceKeypointReport` (produced by `backend/scripts/extract_reference_keypoint_reports.py:113` via `build_keypoint_report`, assemble.py:781) is a **2D 8-keypoint overlay** — its `data` length is `T*J*2` (x/y only, J=8 body keypoints + axisData polyline; assemble.py:924) so it ALSO cannot yield `(T,17,4)` and MUST NOT be used to claim D-05 3D cross-product coverage. The ONLY valid `(T,17,4)` 3D path is `to_coco17_array(pose_frames)` (pose_frame.py:325 → `(x, y, z, uncertainty_proxy)`, 17 keypoints in `skeleton.KEYPOINT_NAMES` order). Therefore the executor MUST do ONE of: (i) add a dedicated 3D extractor — a NEW script `backend/scripts/extract_reference_coco17_4ch.py` OR a `--out-coco17-4ch` mode on the existing script — that runs the RTMW path `frames -> engine.estimate(frames) -> pose_frames -> to_coco17_array(pose_frames)` (pose_estimator.py:120 → pose_frame.py:325) to emit real `(T,17,4)` with `ch3 = uncertainty_proxy`, OR (ii) generate a checked-in COMPACT `(T,17,4)` 3D fixture produced via `to_coco17_array` from a named source WITH provenance recorded in a conftest comment (source motion ID + `to_coco17_array` extraction method + date). PIN these canonical source motion IDs (from the 정은지 reference library — KNOWN-ANSWER regression, NOT a fit target, [[calibration-source-hard-gate]]): `ref-sideway-spin` (spin-around-pole), `ref-invert` (inverted / mirrored orientation), and `ref-foxtop-split` (extreme split exercising BOTH left and right limbs) — only the extraction PATH is corrected; the pinned IDs stand. Synthetic-only data MUST NOT satisfy this test — a conftest comment must state the provenance so an executor cannot believe real-video risk is covered when it is not.
    FIXTURE SCHEMA GATE (review HIGH item 3): add the real-elite `(T,17,4)` fixture(s) (e.g. `real_elite_coco17_4ch`, keyed by the pinned IDs) and ensure Task 2 adds `test_d05_real_elite_fixture_schema`, which asserts every real-elite D-05 fixture has shape `(T, 17, 4)` (`arr.shape[1] == 17`), joint order == `skeleton.KEYPOINT_NAMES`, and channel 3 carries uncertainty_proxy semantics (all `arr[..., 3]` values in `[0, 1]`; any fully-NaN-coord frame carries `ch3 == 1.0`). This makes it structurally impossible to satisfy the real-elite regression with 2D (J=8 `referenceKeypointReport`) or wrong-order data.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/phase10/test_safety_flags_hyperextension.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && python -m pytest tests/phase10/test_safety_flags_hyperextension.py -x -q` GREEN (flexed→no flag, reverse-bent+same-phase-control-loss→flag, high-uncertainty same-geometry→no flag, NaN-in-unused-keypoint→STILL flag, AND-gate, elbow symmetric, floor respected, wrong-phase/wrong-joint/missing-phase/frame-mismatch→no flag, multi-joint→one worst flag).
    - `cd backend && python -m pytest tests/phase10/test_safety_flags_firing_rule.py::test_elite_posture_alone_no_flag -x -q` STILL GREEN (D-05 did not break elite no-FP).
    - HELPER SIGNATURE callable + threads angles/profile (review HIGH): `grep -nE "def _joint_hyperextension_flags\\(\\*, *angles, *keypoints_4ch, *fsr, *profile\\)" backend/shared/python/sunity_shared/analysis/safety_flags.py` matches; `grep -n "_joint_hyperextension_flag(keypoints_4ch" backend/shared/python/sunity_shared/analysis/safety_flags.py` returns NOTHING (the uncallable singular form must not exist).
    - WINDOW-ONCE + FRAME GUARD: `grep -nE "_select_window\\(angles, *profile\\)|shape\\[0\\] *!=|_phase_for_window" backend/shared/python/sunity_shared/analysis/safety_flags.py` shows the angles+profile-driven single window, the frame-count guard, and the phase derived once; `compute_safety_flags` `.extend()`s the D-05 list (`grep -n "extend" backend/shared/python/sunity_shared/analysis/safety_flags.py`).
    - SCOPED NaN/UNCERTAINTY GATE (review MEDIUM): the NaN/inf + uncertainty checks are restricted to the used keypoints, not the whole array — `grep -nE "kp_index|used_idx|USED|hinge|frontal|longitudinal" backend/shared/python/sunity_shared/analysis/safety_flags.py` shows the used-keypoint index set; there is NO blanket `np.isnan(keypoints_4ch).any()` over the full array gating a return (`grep -nE "isnan\\(keypoints_4ch\\)\\.any\\(\\)|isfinite\\(keypoints_4ch\\)\\.all\\(\\)" backend/shared/python/sunity_shared/analysis/safety_flags.py` returns NOTHING — only scoped-column checks); `test_d05_nan_unused_keypoint_still_flags` GREEN.
    - MULTI-JOINT CONSOLIDATION pinned (review MEDIUM): exactly ONE consolidated `joint_hyperextension` flag per analysis, worst by (severity desc, then fixed order left_knee→right_knee→left_elbow→right_elbow); `grep -nE "left_knee.*right_knee.*left_elbow.*right_elbow|joint_hyperextension" backend/shared/python/sunity_shared/analysis/safety_flags.py` shows the fixed tie-break order; `test_d05_multi_joint_picks_worst_with_tiebreak` GREEN.
    - The deterministic frontal-axis + min-angle calibration is present: `grep -nE "right_hip|right_shoulder|argmin|CLEAR_FLEXION_MAX|s_flex|signed_proj" backend/shared/python/sunity_shared/analysis/safety_flags.py` shows the fixed body-frame + calibration rule (no 'e.g.' alternative).
    - The uncertainty gate is NOT inverted: `grep -nE "MAX_KP_UNCERTAINTY|uncertainty_proxy|1\\.0 - .*\\[.*3\\]|clip\\(1" backend/shared/python/sunity_shared/analysis/safety_flags.py` shows uncertainty-correct gating; `grep -n "MIN_KP_CONF" backend/shared/python/sunity_shared/analysis/safety_flags.py` returns NOTHING (the inverted-name constant must not exist).
    - CHANNEL-LABEL GREP GATE (review HIGH item 5): no new D-05 code/comment calls ch3 "confidence" — `grep -nE "keypoints_4ch\\[.*3\\].*confiden|ch3.*confiden|\\[:, *:, *3\\].*confiden" backend/shared/python/sunity_shared/analysis/safety_flags.py` returns nothing (the channel must be described as uncertainty_proxy, not confidence).
    - Fail-conservative gate has the orientation/degeneracy branch: `grep -nE "COLLINEAR_COS|collinear|MAX_KP_UNCERTAINTY|cross\\(" backend/shared/python/sunity_shared/analysis/safety_flags.py` shows the per-branch gate constants.
    - D-05 control-loss is phase-co-located via the 10-02 helper: `grep -nE "_phase_for_window|_control_loss_for_joint" backend/shared/python/sunity_shared/analysis/safety_flags.py` shows D-05 uses phase=P (no-op when None).
    - A MULTI-clip real-elite regression test exists and is GREEN (more than one clip, incl. mirrored + spin + both limbs → zero joint_hyperextension flags): grep test names (e.g. `test_real_elite_clips_no_hyperextension`, `test_real_elite_mirrored_no_flag`, `test_real_elite_spin_no_flag`); conftest names the pinned source motion IDs (ref-sideway-spin / ref-invert / ref-foxtop-split) + `to_coco17_array` extraction provenance, and does NOT reconstruct 3D from EITHER `reference-angles.json` (angle-only) OR `referenceKeypointReport`/`extract_reference_keypoint_reports.py` (2D 8-keypoint).
    - FIXTURE SCHEMA GATE GREEN: `test_d05_real_elite_fixture_schema` asserts every real-elite fixture has shape `(T,17,4)`, joint count == 17, joint order == `skeleton.KEYPOINT_NAMES`, and ch3 ∈ [0,1] with fully-NaN-coord frames → ch3 == 1.0 (blocks 2D / 8-keypoint / wrong-order data).
    - D-05 passes the exact unstable_body_parts joint keys: `grep -nE "left_knee|right_knee|left_elbow|right_elbow" backend/shared/python/sunity_shared/analysis/safety_flags.py`.
    - No threshold in safety_flags.py justified by "13"/"our video" (grep — none); gate constants tagged [ASSUMED conservative gate], floors [CITED].
  </acceptance_criteria>
  <done>Callable `_joint_hyperextension_flags(*, angles, keypoints_4ch, fsr, profile) -> list[SafetyFlag]` (frame-align guard + single angles+profile-driven window/phase + per-joint candidates + pinned multi-joint worst-severity consolidation) with a deterministic cross-product detector (fixed frontal-axis body frame, min-angle flexion calibration, exact 180−angle math, [CITED] floors) and an uncertainty_proxy-correct per-branch fail-conservative gate SCOPED to the used keypoints (HIGH uncertainty → no flag; NaN in unused face keypoint → still flag; incl. inversion/spin orientation gate); flexed-vs-reverse-bent discriminated via two-segment fixtures; same-geometry/high-uncertainty proves gate not inverted; wrong-phase/wrong-joint/missing-phase/frame-mismatch no-op; AND-gate + phase-co-located control-loss + elite-no-FP preserved across MULTIPLE pinned-source real elite clips; flag auto-renders.</done>
</task>

<task type="auto">
  <name>Task 2: Deterministic-sign + uncertainty-channel-not-inverted + scoped-NaN + per-branch conservative-gate + helper-contract (signature/window/multi-joint/wrong-phase/wrong-joint) + multi-clip real-elite hardening + full-suite regression guard</name>
  <files>backend/tests/phase10/test_safety_flags_hyperextension.py</files>
  <read_first>
    - backend/tests/phase10/test_safety_flags_hyperextension.py (Task 1 tests + the xfail `test_d05_uncertainty_channel_not_inverted`, `test_d05_nan_unused_keypoint_still_flags`, `test_d05_same_phase_positive`, `test_d05_multi_joint_picks_worst_with_tiebreak` scaffolded in 10-01, and the GREEN single-variable `test_d05_wrong_phase_no_flag` / `test_d05_wrong_joint_no_flag`)
    - backend/shared/python/sunity_shared/analysis/safety_flags.py (the D-05 impl: callable `_joint_hyperextension_flags(*, angles, keypoints_4ch, fsr, profile)` + deterministic frontal-axis sign + min-angle calibration + uncertainty-correct scoped-to-used-keypoints per-branch gate + window-once + multi-joint consolidation)
    - backend/shared/python/sunity_shared/analysis/pose_frame.py:326,335 + backend/functions/pipeline/app.py:452-466 (uncertainty_proxy channel semantics — the assertion ground truth; face keypoints default NaN+uncertainty 1.0)
    - backend/shared/python/sunity_shared/analysis/skeleton.py:10-50 (KEYPOINT_NAMES — to pick an UNUSED face keypoint index for the scoped-NaN test)
    - backend/tests/phase10/conftest.py (per-branch ambiguity fixtures + uncertain_reverse_bent + multi_joint_reverse_bent + no_phase_boundaries_report + wrong_phase_knee_control_loss_report + wrong_joint_same_phase_report + multi-clip pinned-source real-elite fixtures from Task 1)
    - .planning/phases/10-injury-risk-flags/10-RESEARCH.md (sign-convention "validate on elite videos before trusting"; Metadata confidence note)
    - .planning/phases/10-injury-risk-flags/10-VALIDATION.md (Sampling Rate — per-wave full backend suite)
  </read_first>
  <action>
    Add robustness tests to test_safety_flags_hyperextension.py:
    (1) LEFT and RIGHT limbs both classify a forward-fold as flexion (no mirror-flip bug — frontal axis is per-side-consistent) and both correctly flag a reverse-bend.
    (2) DETERMINISM/SIGN-STABILITY: a borderline magnitude exactly at the [CITED] floor does not flip the verdict spuriously (boundary stability); the same clip run twice yields identical verdicts (the min-angle calibration is deterministic).
    (3) WINDOW AGGREGATION: a single reversed-sign frame inside an otherwise-flexed hold does NOT emit a flag.
    (4) UNCERTAINTY-CHANNEL-NOT-INVERTED (review HIGH item 4): un-xfail and complete `test_d05_uncertainty_channel_not_inverted` — feed the SAME two-segment reverse-bend coordinates + the SAME joint-localized control-loss twice: with LOW uncertainty (`reverse_bent_knee_4ch`, ch3≈0.05) the function CAN flag (returns a one-element list with a joint_hyperextension flag), and with HIGH uncertainty (`uncertain_reverse_bent_4ch`, ch3≈0.9) the function MUST NOT flag (returns []). This is the lockstep proof the gate is not inverted. Assert the channel semantics directly so a future inversion fails the test.
    (4b) SCOPED-NaN (review MEDIUM): un-xfail and complete `test_d05_nan_unused_keypoint_still_flags` — take `reverse_bent_knee_4ch` (two-segment, otherwise-valid) + SAME-phase knee control-loss, inject NaN into an UNUSED keypoint (a face keypoint, `skeleton.kp_index('nose')`), and assert a joint_hyperextension flag STILL fires (the guard is scoped to used keypoints, not blanket NaN-anywhere). Add a companion assertion that injecting NaN into a USED keypoint (e.g. the candidate's `left_knee` index) DOES suppress the flag (proves the scope is correct on both sides).
    (5) PER-BRANCH FAIL-CONSERVATIVE — one separate negative test PER ambiguity branch proving NO flag (even with control-loss present): `high_uncertainty_4ch` (ch3 > MAX_KP_UNCERTAINTY on used keypoints), `inconsistent_segment_4ch`, `single_frame_reverse_4ch`, `degenerate_frontal_4ch`, `spin_collinear_4ch` (inversion/spin → frontal≈longitudinal), `collinear_segment_4ch`, `no_flexion_calibration_4ch`. Each maps to a distinct branch in Task 1's gate.
    (6) HELPER-CONTRACT (review HIGH/MEDIUM): keep GREEN against the impl, each asserting its precondition FIRST so a pass can only come from the gate under test — `test_d05_wrong_phase_no_flag` (`wrong_phase_knee_control_loss_report`: FIRST assert 'left_knee' IS in the report's unstable_body_parts so the joint gate is satisfied → only the phase is wrong; THEN assert []), `test_d05_wrong_joint_no_flag` (`wrong_joint_same_phase_report`: FIRST assert the instability phase DOES overlap the D-05 hold window via `_phase_for_window` so the phase gate is satisfied → only the joint is wrong; THEN assert []), `test_d05_missing_phase_boundaries_no_flag` (`no_phase_boundaries_report` → `_phase_for_window` None → []), `test_d05_frame_mismatch_no_flag` (truncate `keypoints_4ch` so `shape[0] != angles.shape[0]` → []). Un-xfail the positives: `test_d05_same_phase_positive` (two-segment reverse-bend + SAME-phase knee control-loss → exactly one joint_hyperextension flag; FIRST assert the fixture has a clear-flexion calibration frame `min(included_angle) < CLEAR_FLEXION_MAX` so the positive path is reachable per the calibration premise) and `test_d05_multi_joint_picks_worst_with_tiebreak` (`multi_joint_reverse_bent_4ch` + same-phase knee AND elbow control-loss → exactly ONE consolidated flag whose `posture_condition` names the worst joint per (severity desc, then fixed order left_knee,right_knee,left_elbow,right_elbow); assert len == 1 and the chosen joint; same calibration-frame precondition assertion).
    (7) MULTI-CLIP REAL-ELITE: re-run the multi-clip real-elite regression (pinned-source 정은지 clips: ref-sideway-spin / ref-invert / ref-foxtop-split, incl. mirrored orientation, spin-around-pole, both limbs) as an explicit guard — all yield zero hyperextension flags.
    (8) FIXTURE SCHEMA GATE (review HIGH item 3): add `test_d05_real_elite_fixture_schema` — for every real-elite D-05 fixture assert `arr.shape == (T, 17, 4)` (joint count == 17), joint order == `skeleton.KEYPOINT_NAMES`, and `arr[..., 3]` (uncertainty_proxy) ∈ [0,1] with any fully-NaN-coord frame carrying `ch3 == 1.0`. This makes it impossible to satisfy the real-elite regression with a 2D `referenceKeypointReport` (J=8) or wrong joint-order data.
    These lock the deterministic sign convention + uncertainty-correct + scoped-NaN + per-branch conservative gate + callable helper contract + single-variable wrong-phase/wrong-joint isolation + multi-joint rule that Codex iter-2 and the round-3/round-5/round-6 reviews flagged.
    Then run the full backend suite as the per-wave regression gate to prove safety_flags additions did not perturb force_signals/dimensions/assemble/pipeline.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/phase10 -x -q && python -m pytest tests -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && python -m pytest tests/phase10 -x -q` GREEN including left/right symmetry, boundary/determinism, window-aggregation, uncertainty-channel-not-inverted, scoped-NaN (unused→flag / used→no-flag), per-branch conservative-gate (one test per branch incl. high-uncertainty), helper-contract (wrong-phase/wrong-joint/missing-phase/frame-mismatch/same-phase-positive/multi-joint), and multi-clip real-elite (mirrored + spin + both limbs) tests; ZERO xfail remaining for `test_d05_uncertainty_channel_not_inverted`, `test_d05_nan_unused_keypoint_still_flags`, `test_d05_same_phase_positive`, `test_d05_multi_joint_picks_worst_with_tiebreak` (they flip GREEN here).
    - `cd backend && python -m pytest tests -q` full backend suite GREEN (zero regression).
    - Left/right symmetry test present (grep for "left"/"right"/"mirror"); a SEPARATE per-branch conservative-gate test present for each branch (grep for "uncertain"/"high_uncertainty"/"segment"/"single_frame"/"degenerate"/"spin"/"collinear"/"calibration"); the channel-not-inverted test present (grep for "uncertainty_channel_not_inverted"); the scoped-NaN test present (grep for "nan_unused_keypoint"); multi-clip real-elite test present (grep for "clips"/"mirrored"/"spin").
    - Helper-contract tests present and GREEN, each asserting its precondition first: `test_d05_wrong_phase_no_flag`, `test_d05_wrong_joint_no_flag`, `test_d05_missing_phase_boundaries_no_flag`, `test_d05_frame_mismatch_no_flag`, `test_d05_same_phase_positive`, `test_d05_multi_joint_picks_worst_with_tiebreak` (grep the names); the two positives carry ZERO remaining xfail after this wave.
    - `test_d05_real_elite_fixture_schema` present and GREEN (grep for "fixture_schema"): asserts each real-elite fixture is shape `(T,17,4)`, joint count == 17, order == `skeleton.KEYPOINT_NAMES`, ch3 ∈ [0,1] (NaN frames → 1.0) — so a 2D / 8-keypoint / wrong-order source cannot satisfy the regression.
  </acceptance_criteria>
  <done>Deterministic sign convention + uncertainty_proxy-correct (channel-not-inverted) + scoped-NaN (used-only) + per-branch fail-conservative gate + callable helper contract (signature threads angles/profile, window-once, frame-align guard, multi-joint worst-severity consolidation) hardened (per-side, boundary-stable, window-aggregated, one test per ambiguity branch incl. high-uncertainty + inversion/spin, single-variable wrong-phase + wrong-joint no-op, missing-phase/frame-mismatch no-op, same-phase + multi-joint positives, multi-clip pinned-source real-elite incl. mirrored/spin/both-limbs); full backend suite GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream pose→safety_flags | `inputs.keypoints_4ch` 3D coords + ch3 uncertainty_proxy (may carry high-uncertainty/NaN/inverted/spinning/frame-misaligned frames, AND unused face keypoints defaulted to NaN) feed the cross-product. |
| pipeline→Firestore | joint_hyperextension flag persists scalar-only (validator from 10-02). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-FP | Tampering (domain) | posture-alone / misclassified FP on 정은지 intentional extension | mitigate | D-02 LOCAL+TEMPORAL AND-gate (phase via _phase_for_window) + window aggregation + per-branch fail-conservative gate + MULTI-clip pinned-source real-elite regression incl. mirrored/spin/both-limbs (Task 1/2). |
| T-10-06 | Tampering | curve-fit threshold provenance leak | mitigate | Floors [CITED] external literature; gate constants [ASSUMED conservative gate] (widen no-flag, not detection); grep gate forbids "13 videos" justification (D-07 / Pitfall 3). |
| T-10-07 | Availability | NaN/degenerate/inverted/frame-misaligned 3D vectors crash analysis or misfire | mitigate | Frame-align guard + every ambiguity branch → no flag, never raise (per-branch conservative gate, Pitfall 5). |
| T-10-10 | Tampering (domain) | sign-convention inversion under spin/inversion → reverse-bend read as flexion or vice versa | mitigate | Deterministic frontal-axis sign + min-angle flexion calibration + orientation gate (frontal collinear with longitudinal → no flag); left/right + spin + inverted-orientation tests (Task 2). |
| T-10-11 | Tampering (domain) | uncallable/ambiguous helper silently never fires (or double-fires) → Mode-3 risk signal lost or noisy | mitigate | Callable `_joint_hyperextension_flags(*, angles, keypoints_4ch, fsr, profile)` threads required inputs; window/phase computed once; single consolidated flag by pinned worst-severity tie-break; single-variable wrong-phase/wrong-joint isolators + helper-contract tests (Task 2). |
| T-10-12 | Tampering (domain) | INVERTED reliability gate — ch3 read as confidence admits undetected keypoints (uncertainty≈1.0) into a warning | mitigate | Gate reads ch3 as uncertainty_proxy, rejects uncertainty > MAX_KP_UNCERTAINTY (no MIN_KP_CONF); `test_d05_uncertainty_channel_not_inverted` + channel-label grep gate (Task 1/2). |
| T-10-13 | Availability (domain) | over-broad NaN guard no-ops on an unused (face/contralateral) keypoint → real knee/elbow risk silently dropped | mitigate | NaN/inf + uncertainty gate SCOPED to the used-keypoint set only (hinge triplet + frontal pair + longitudinal centers); no blanket `isnan(keypoints_4ch).any()`; `test_d05_nan_unused_keypoint_still_flags` (used→no-flag / unused→flag) (Task 1/2). |
| T-10-SC | Tampering | npm/pip installs | accept | No package installs this phase. |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/phase10 -x -q` — all phase10 GREEN incl. D-05 deterministic-sign + uncertainty-channel-not-inverted + scoped-NaN + per-branch gate + helper-contract (wrong-phase/wrong-joint) + multi-clip real-elite + elite-no-FP.
- `cd backend && python -m pytest tests -q` — full backend suite GREEN.
- Manual: on device, a result with a reverse-bend + instability shows the "무릎·팔꿈치 과신전 가능성" amber card (auto-rendered via 10-02 section); 정은지 elite clips (incl. mirrored/spin) show no joint flag.
</verification>

<success_criteria>
- D-05 distinguishes flexion from reverse hyperextension via exact 180−angle math + ONE deterministic frontal-axis body frame + min-angle flexion calibration (no discretion, sign cannot silently invert); positive path validated via TWO-SEGMENT fixtures that satisfy the calibration premise.
- The D-05 firing function is CALLABLE: `_joint_hyperextension_flags(*, angles, keypoints_4ch, fsr, profile) -> list[SafetyFlag]` threads angles+profile into the shared `dimensions._select_window`, computes the hold window + phase ONCE, frame-align-guards keypoints vs angles, and emits a SINGLE consolidated flag by pinned worst-severity tie-break.
- The reliability gate reads ch3 as uncertainty_proxy (HIGH uncertainty → no flag), not confidence; channel-not-inverted regression GREEN; no MIN_KP_CONF constant. The NaN/inf + uncertainty gate is SCOPED to the used keypoints only — NaN in an unused face keypoint still flags; NaN in a used keypoint suppresses.
- Flexed → no flag; reverse-bent + co-located control-loss → joint_hyperextension flag (D-02); EVERY ambiguity branch (incl. frame-mismatch + missing/wrong phase + wrong joint) → no flag (fail-conservative), each separately tested via single-variable fixtures.
- 정은지 elite no-FP preserved AND verified against MULTIPLE pinned-source real elite clips incl. mirrored orientation, spin-around-pole, and both limbs (not synthetic-only). Real-elite (T,17,4) fixtures come ONLY from `to_coco17_array` (pose_frame.py:325); BOTH `referenceKeypointReport` (2D 8-keypoint) and `reference-angles.json` (angle-only) are excluded as 3D sources, enforced by `test_d05_real_elite_fixture_schema`.
- Absolute floors [CITED] (knee >5°, elbow >10°); gate constants [ASSUMED conservative]; no 13-video curve-fit; unstable_body_parts keys match exactly.
- Flag auto-renders via 10-02 InjuryRiskSection; full backend suite GREEN.
</success_criteria>

<output>
Create `.planning/phases/10-injury-risk-flags/10-03-SUMMARY.md` when done.
</output>
</content>
</output>
</output>
