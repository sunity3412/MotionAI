---
phase: 10-injury-risk-flags
plan: 04
type: execute
wave: 3
depends_on: [10-03]
files_modified:
  - backend/shared/python/sunity_shared/analysis/safety_flags.py
  - backend/tests/phase10/test_safety_flags_asymmetry.py
  - backend/tests/phase10/test_safety_flags_level.py
autonomous: true
requirements: [SAFE-01]

must_haves:
  truths:
    - "Left/right asymmetry fires only when the student is significantly MORE asymmetric than the reference (Mode 1) or the previous video (Mode 3) AND control-loss — absolute L/R asymmetry is NOT flagged (D-03)"
    - "Intentional asymmetry present equally in the reference auto-cancels (reference-anchored excess) — no false positive (Success Criterion 1)"
    - "Level mismatch fires in Mode 1 when reference.level outranks user experience AND control-loss; the same inputs in Mode 3 produce NO level flag (D-06 mode1-only, Success Criterion 3)"
    - "A spoofed/non-enum experience value simply omits the level flag (fail-safe, no crash)"
    - "Both flags auto-render via the InjuryRiskSection built in 10-02 (no UI change)"
    - "정은지 elite posture alone still produces ZERO flags (D-02 preserved)"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/safety_flags.py"
      provides: "_asymmetry_flag (reference-anchored, kismam reuse) + _level_mismatch_flag (mode1-only)"
      contains: "_asymmetry_flag"
  key_links:
    - from: "safety_flags._asymmetry_flag"
      to: "kismam.score_from_deviation"
      via: "reference-anchored excess quantification (D-03/D-07)"
      pattern: "score_from_deviation"
    - from: "safety_flags._level_mismatch_flag"
      to: "experience + reference_level"
      via: "Mode 1 only level-rank comparison (D-06)"
      pattern: "reference_level"
---

<objective>
Slices 3 + 4: complete the SafetyFlag layer with the two data-driven flags. D-03 asymmetry is reference-anchored — `excess = max(0, student_LR − ref_LR)` quantified with `kismam.score_from_deviation` so intentional asymmetry baked into the reference (or the previous video in Mode 3) auto-cancels (that is exactly why scoring deliberately dropped the symmetry dimension, `dimensions.py §12`). Absolute L/R asymmetry is NEVER flagged in v1 (too many false positives, D-03). D-06 level-mismatch is Mode 1 only — `reference.level` (basic/intermediate/advanced) vs user `experience` (beginner/intermediate/advanced); in Mode 3 the rule does not apply (move difficulty unknown — D-02 control-loss catches overreach instead).

Both fire via the D-02 AND-gate and auto-render through the `InjuryRiskSection` from 10-02. This delivers Success Criteria 1 (asymmetry flag) and 3 (level-mismatch), completing SAFE-01. The elite-no-FP headline gate is re-asserted across the now-complete rule set.

Purpose: finish the four-flag set with reference-anchored / data-driven rules that generalize (no curve-fit).
Output: D-03 + D-06 firing rules; asymmetry + level tests GREEN; full backend suite GREEN.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-injury-risk-flags/10-CONTEXT.md
@.planning/phases/10-injury-risk-flags/10-RESEARCH.md
@.planning/phases/10-injury-risk-flags/10-PATTERNS.md
@.planning/phases/10-injury-risk-flags/10-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-03 reference-anchored asymmetry flag</name>
  <files>backend/shared/python/sunity_shared/analysis/safety_flags.py, backend/tests/phase10/test_safety_flags_asymmetry.py</files>
  <read_first>
    - backend/shared/python/sunity_shared/analysis/safety_flags.py (existing _control_loss_for_joint + _maybe_flag + compute_safety_flags from 10-02/10-03)
    - backend/shared/python/sunity_shared/analysis/kismam.py:111-124 (score_from_deviation — REUSE for excess, do not reimplement)
    - backend/shared/python/sunity_shared/analysis/skeleton.py:39-50 (left/right joint pairs for L/R deviation)
    - backend/shared/python/sunity_shared/analysis/dimensions.py §12 (symmetry dimension deliberately excluded — the rationale anchoring D-03)
    - .planning/phases/10-injury-risk-flags/10-RESEARCH.md (Pattern 4 — student_LR vs ref_LR excess; Mode3 ref_LR = previous video; D-07 reference-anchored/KISMAM tol only)
    - .planning/phases/10-injury-risk-flags/10-CONTEXT.md (D-03, D-02, D-07)
    - backend/tests/phase10/test_safety_flags_asymmetry.py (RED tests from 10-01)
  </read_first>
  <behavior>
    - student_LR >> ref_LR AND control-loss → SafetyFlag(flag_type='asymmetry', body_region per copy, mode_scope='both').
    - student_LR ≈ ref_LR (intentional asymmetry present in both) → no flag (reference-anchored cancellation).
    - student_LR >> ref_LR WITHOUT control-loss → no flag (D-02).
    - reference_angles=None (graceful) → no asymmetry flag (cannot anchor).
    - Mode 3 uses previous-video angles as the anchor (passed as reference_angles).
  </behavior>
  <action>
    Implement `_asymmetry_flag(angles, reference_angles, fsr) -> SafetyFlag | None` in safety_flags.py per RESEARCH Pattern 4. Over the hold window compute `student_LR = |mean(student_left_joint) − mean(student_right_joint)|` for the relevant joint(s); compute `ref_LR` the same way from reference_angles; `excess = max(0.0, student_LR − ref_LR)`. posture_met when `score_from_deviation(excess, tol=20.0)` falls below a reference-anchored threshold (excess >> tol). Absolute L/R asymmetry is NEVER flagged — if reference_angles is None, return None (cannot anchor). Emit via `_maybe_flag(posture_met, _control_loss_*, ...)`. Tag provenance: reference-anchored + KISMAM tol=20° [CITED: IPSF CoP], no 13-video curve-fit (D-07). Append to compute_safety_flags. Un-RED the asymmetry tests.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/phase10/test_safety_flags_asymmetry.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && python -m pytest tests/phase10/test_safety_flags_asymmetry.py -x -q` GREEN (excess+control-loss→flag; equal asymmetry→none; no-anchor→none; AND-gate).
    - `grep -n "score_from_deviation" backend/shared/python/sunity_shared/analysis/safety_flags.py` shows kismam reuse (not reimplemented).
    - `cd backend && python -m pytest tests/phase10/test_safety_flags_firing_rule.py::test_elite_posture_alone_no_flag -x -q` STILL GREEN.
  </acceptance_criteria>
  <done>D-03 reference-anchored asymmetry flag implemented (kismam reuse); intentional asymmetry cancels; absolute L/R never flagged; elite-no-FP preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-06 Mode-1-only level-mismatch flag + final regression gate</name>
  <files>backend/shared/python/sunity_shared/analysis/safety_flags.py, backend/tests/phase10/test_safety_flags_level.py</files>
  <read_first>
    - backend/shared/python/sunity_shared/analysis/safety_flags.py (compute_safety_flags with mode + experience + reference_level kwargs already plumbed in 10-02)
    - backend/shared/python/sunity_shared/models.py:206 (EXPERIENCE_LEVELS = beginner/intermediate/advanced), normalize_body_profile enum-drop behavior (D-06 spoof fail-safe); MODE_EXPERT / MODE_SELF constants
    - app/src/types/analysis.ts:578 (SkillLevel basic/intermediate/advanced = reference.level)
    - .planning/phases/10-injury-risk-flags/10-RESEARCH.md (D-06 Open Q3 RESOLVED — experience + reference.level reachable; Security: spoofed experience → enum-drop omit)
    - .planning/phases/10-injury-risk-flags/10-CONTEXT.md (D-06 mode1-only, D-02)
    - backend/tests/phase10/test_safety_flags_level.py (RED tests from 10-01)
  </read_first>
  <behavior>
    - Mode 1, reference.level rank > experience rank (e.g. advanced ref × beginner user) AND control-loss → SafetyFlag(flag_type='level_mismatch', mode_scope='mode1_only').
    - Same inputs in Mode 3 → no level flag (D-06 mode1-only; Mode 3 overreach handled by D-02).
    - experience is None / non-enum (spoofed, dropped by normalize_body_profile) → no level flag (fail-safe).
    - reference_level None → no level flag.
    - level mismatch WITHOUT control-loss → no flag (D-02).
  </behavior>
  <action>
    Implement `_level_mismatch_flag(mode, experience, reference_level, fsr) -> SafetyFlag | None` in safety_flags.py. Return None immediately unless `mode == MODE_EXPERT` (D-06 mode1-only). Map both ladders to integer ranks (basic/beginner=0, intermediate=1, advanced=2); posture_met when `reference_rank − experience_rank >= 1` (reference outranks user). experience/reference_level None or non-enum → None (fail-safe — relies on the already-applied normalize_body_profile enum-drop; do not re-validate user text). Emit via `_maybe_flag(posture_met, _control_loss_phase_level(fsr), flag_type='level_mismatch', mode_scope='mode1_only', ...)`. Append to compute_safety_flags. Un-RED the level tests (incl. Mode-3-no-flag and spoof-omit). Then run the full backend suite as the final phase regression gate.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/phase10/test_safety_flags_level.py -x -q && python -m pytest tests -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && python -m pytest tests/phase10/test_safety_flags_level.py -x -q` GREEN (mode1 mismatch+control-loss→flag; mode3 same inputs→none; spoof/None experience→none; AND-gate).
    - `grep -n "MODE_EXPERT\|mode1_only" backend/shared/python/sunity_shared/analysis/safety_flags.py` confirms mode1-only gating.
    - `cd backend && python -m pytest tests/phase10 -x -q` ALL GREEN (4 flag types + elite-no-FP + contract + deterministic).
    - `cd backend && python -m pytest tests -q` full backend suite GREEN (zero regression).
    - `cd app && npm run typecheck` GREEN.
  </acceptance_criteria>
  <done>D-06 mode1-only level-mismatch flag implemented (spoof fail-safe); all 4 flag types complete; full phase10 + backend suite + typecheck GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→pipeline | `bodyProfile.experience` (D-06 input) untrusted; sanitized by normalize_body_profile. |
| reference doc→pipeline | `reference.level` from seeded admin docs (trusted, server-side). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02 | Tampering | spoofed `experience` to suppress/trigger level flag | mitigate | normalize_body_profile enum-drop → None → level flag omitted fail-safe (Task 2); test asserts spoof→no flag. |
| T-10-FP | Tampering (domain) | posture-alone FP on 정은지 | mitigate | D-02 AND-gate across all 4 rules; elite-no-FP test re-asserted with full rule set (Task 1/2). |
| T-10-08 | Tampering | curve-fit asymmetry threshold | mitigate | Reference-anchored + KISMAM tol only; grep gate forbids "13 videos" justification (D-07). |
| T-10-SC | Tampering | npm/pip installs | accept | No package installs this phase. |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/phase10 -x -q` — all 4 flag types + elite-no-FP + contract + deterministic GREEN.
- `cd backend && python -m pytest tests -q` — full backend suite GREEN.
- `cd app && npm run typecheck` — GREEN.
- Manual: Mode 1 result with advanced-ref × beginner-experience + instability → "레벨 대비 무리한 동작 가능성" card; Mode 3 same → no level card; high-asymmetry + instability → "좌우 비대칭 신호" card.
</verification>

<success_criteria>
- D-03 asymmetry fires reference-anchored + control-loss; intentional asymmetry cancels; absolute L/R never flagged (SC 1).
- D-06 level-mismatch fires Mode 1 only + control-loss; Mode 3 no level flag; spoof fail-safe (SC 3).
- All four flag types complete and auto-render via 10-02 section.
- 정은지 elite no-FP preserved across the full rule set.
- Full phase10 + backend suite + typecheck GREEN — SAFE-01 satisfied.
</success_criteria>

<output>
Create `.planning/phases/10-injury-risk-flags/10-04-SUMMARY.md` when done.
</output>
