---
phase: 10-injury-risk-flags
plan: 04
type: execute
wave: 3
depends_on: [10-03]
files_modified:
  - backend/shared/python/sunity_shared/analysis/safety_flags.py
  - backend/tests/phase10/test_safety_flags_asymmetry.py
  - backend/tests/phase10/test_safety_flags_level.py
autonomous: true
requirements: [SAFE-01]

must_haves:
  truths:
    - "Left/right asymmetry fires only when the student is significantly MORE asymmetric than the reference (Mode 1, anchor a_ref) or the previous video (Mode 3, anchor mode3_prev) AND joint/pair-localized control-loss — absolute L/R asymmetry is NOT flagged (D-03)"
    - "Asymmetry uses an explicit set of L/R joint pairs with MAX aggregation (the single most-asymmetric pair drives the flag), and the responsible pair is named in the posture_condition audit string"
    - "Intentional asymmetry present equally in the reference auto-cancels (reference-anchored excess) — no false positive (Success Criterion 1)"
    - "Mode 3 asymmetry uses the previous-analysis angles (reshaped mode3_prev, plumbed in 10-02); on a first Mode-3 video (reference_angles=None) it gracefully no-ops (surfaced limitation)"
    - "Level mismatch fires in Mode 1 when reference.level outranks user experience AND control-loss; severity scales with rank-gap AND instability severity so a rank gap of exactly 1 does not over-warn; the same inputs in Mode 3 produce NO level flag (D-06 mode1-only, Success Criterion 3)"
    - "A spoofed/non-enum experience value simply omits the level flag (fail-safe, no crash), with enum membership guarded inside safety_flags.py"
    - "정은지 elite posture alone still produces ZERO flags (D-02 LOCAL+TEMPORAL preserved)"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/safety_flags.py"
      provides: "_asymmetry_flag (reference-anchored, explicit pairs, MAX agg, pair-local control-loss, kismam reuse) + _level_mismatch_flag (mode1-only, rank-gap×instability severity, enum-guarded)"
      contains: "_asymmetry_flag"
  key_links:
    - from: "safety_flags._asymmetry_flag"
      to: "kismam.score_from_deviation"
      via: "reference-anchored excess quantification (D-03/D-07)"
      pattern: "score_from_deviation"
    - from: "safety_flags._level_mismatch_flag"
      to: "experience + reference_level"
      via: "Mode 1 only level-rank comparison (D-06)"
      pattern: "reference_level"
---

<objective>
Slices 3 + 4: complete the SafetyFlag layer with the two data-driven flags. D-03 asymmetry is reference-anchored — `excess = max(0, student_LR − ref_LR)` quantified with `kismam.score_from_deviation` so intentional asymmetry baked into the reference (or the previous video in Mode 3) auto-cancels (that is exactly why scoring deliberately dropped the symmetry dimension, `dimensions.py §12`). Absolute L/R asymmetry is NEVER flagged in v1 (too many false positives, D-03). D-06 level-mismatch is Mode 1 only — `reference.level` (basic/intermediate/advanced) vs user `experience` (beginner/intermediate/advanced); in Mode 3 the rule does not apply (move difficulty unknown — D-02 control-loss catches overreach instead).

This plan resolves the review's MEDIUM items: asymmetry now uses an EXPLICIT set of L/R joint pairs with MAX aggregation (named in the audit string) and pair-localized control-loss; Mode-3 asymmetry uses the previous-analysis angles plumbed in 10-02 (with the first-video no-op surfaced); and level-mismatch severity scales with BOTH the rank gap AND instability severity so a rank gap of exactly 1 does not over-warn intermediate users on advanced references. Both fire via the D-02 LOCAL+TEMPORAL AND-gate and auto-render through the `InjuryRiskSection` from 10-02. This delivers Success Criteria 1 (asymmetry) and 3 (level-mismatch), completing SAFE-01. The elite-no-FP headline gate is re-asserted across the now-complete rule set.

Purpose: finish the four-flag set with reference-anchored / data-driven rules that generalize (no curve-fit).
Output: D-03 + D-06 firing rules; asymmetry + level tests GREEN; full backend suite GREEN.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-injury-risk-flags/10-CONTEXT.md
@.planning/phases/10-injury-risk-flags/10-RESEARCH.md
@.planning/phases/10-injury-risk-flags/10-PATTERNS.md
@.planning/phases/10-injury-risk-flags/10-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-03 reference-anchored asymmetry flag (explicit pairs, MAX aggregation, pair-local control-loss)</name>
  <files>backend/shared/python/sunity_shared/analysis/safety_flags.py, backend/tests/phase10/test_safety_flags_asymmetry.py</files>
  <read_first>
    - backend/shared/python/sunity_shared/analysis/safety_flags.py (existing _control_loss_for_joint + _maybe_flag + compute_safety_flags from 10-02/10-03)
    - backend/shared/python/sunity_shared/analysis/kismam.py:111-124 (score_from_deviation — REUSE for excess; tol=20° [CITED IPSF CoP])
    - backend/shared/python/sunity_shared/analysis/skeleton.py:39-50 (JOINT_KEYS — the L/R joint pairs: (left_elbow,right_elbow), (left_shoulder,right_shoulder), (left_hip,right_hip), (left_knee,right_knee))
    - backend/shared/python/sunity_shared/analysis/dimensions.py §12 (symmetry dimension deliberately excluded — the rationale anchoring D-03)
    - backend/functions/pipeline/app.py:3056-3071,3127-3136 (the reference_angles plumbing 10-02 added: Mode-1 a_ref, Mode-3 reshaped mode3_prev — confirm _asymmetry_flag consumes the same reference_angles kwarg)
    - .planning/phases/10-injury-risk-flags/10-RESEARCH.md (Pattern 4 — student_LR vs ref_LR excess; Mode3 ref_LR = previous video; D-07 reference-anchored/KISMAM tol only)
    - .planning/phases/10-injury-risk-flags/10-CONTEXT.md (D-03, D-02, D-07)
    - backend/tests/phase10/test_safety_flags_asymmetry.py (xfail tests from 10-01)
  </read_first>
  <behavior>
    - student max-pair_LR >> ref same-pair_LR AND pair-localized control-loss → SafetyFlag(flag_type='asymmetry', body_region per copy, mode_scope='both'), posture_condition naming the responsible pair.
    - student_LR ≈ ref_LR (intentional asymmetry present in both) → no flag (reference-anchored cancellation).
    - student_LR >> ref_LR WITHOUT control-loss → no flag (D-02).
    - reference_angles=None (graceful — first Mode-3 video, or absent) → no asymmetry flag (cannot anchor).
    - Mode 3 uses previous-video angles as the anchor (passed as reference_angles by 10-02 plumbing).
  </behavior>
  <action>
    Implement `_asymmetry_flag(angles, reference_angles, fsr) -> SafetyFlag | None` in safety_flags.py per RESEARCH Pattern 4.
    EXPLICIT PAIRS + AGGREGATION (MEDIUM): define a module-level constant `_ASYMMETRY_PAIRS = (('left_elbow','right_elbow'), ('left_shoulder','right_shoulder'), ('left_hip','right_hip'), ('left_knee','right_knee'))`. For each pair compute over the hold window `student_LR = |mean(student_left) − mean(student_right)|` and `ref_LR = |mean(ref_left) − mean(ref_right)|`, then `excess_pair = max(0.0, student_LR − ref_LR)`. AGGREGATE with MAX (not average): `excess = max(excess_pair)` and remember `worst_pair` (argmax). Justification (document in comment): a single severely-asymmetric joint is the actionable risk signal; averaging across pairs would dilute one bad joint under several symmetric ones and hide the risk. posture_met when `excess > 1.5 * tol` (tol=20° → 30°), i.e. `score_from_deviation(excess, tol=20.0) < 33` — reference-anchored, same provenance as the trunk threshold; NOT 13-video curve-fit (D-07). Put `worst_pair` in the posture_condition audit string (e.g. "left/right knee asymmetry +Xdeg vs reference").
    PAIR-LOCAL CONTROL-LOSS (MEDIUM): the D-02 partner uses `_control_loss_for_joint(fsr, left_key, window=W) or _control_loss_for_joint(fsr, right_key, window=W)` for the WORST pair's two joints (joint/pair-localized + temporally co-located) — not a vague phase-level check. control_loss_signal names which side's instability fired.
    Absolute L/R asymmetry is NEVER flagged — if `reference_angles is None`, return None (cannot anchor; this is the first-Mode-3-video graceful no-op surfaced in 10-02). Emit via `_maybe_flag(posture_met, pair_local_control_loss, ...)`. Append to compute_safety_flags. NaN-degrades-to-no-flag. Un-xfail the asymmetry tests.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/phase10/test_safety_flags_asymmetry.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && python -m pytest tests/phase10/test_safety_flags_asymmetry.py -x -q` GREEN (worst-pair excess+pair-local control-loss→flag; equal asymmetry→none; no-anchor→none; AND-gate).
    - `grep -n "_ASYMMETRY_PAIRS\|score_from_deviation" backend/shared/python/sunity_shared/analysis/safety_flags.py` shows explicit pairs + kismam reuse (not reimplemented).
    - A test asserts MAX aggregation (one severely-asymmetric pair fires even when other pairs are symmetric) — grep test name (e.g. `test_asymmetry_max_pair_drives_flag`).
    - `grep -n "_control_loss_for_joint" backend/shared/python/sunity_shared/analysis/safety_flags.py` shows asymmetry uses pair-local (not bare phase-level) control-loss.
    - `cd backend && python -m pytest tests/phase10/test_safety_flags_firing_rule.py::test_elite_posture_alone_no_flag -x -q` STILL GREEN.
  </acceptance_criteria>
  <done>D-03 reference-anchored asymmetry flag (explicit pairs, MAX aggregation, pair-local+temporal control-loss, kismam reuse); intentional asymmetry cancels; absolute L/R never flagged; responsible pair in audit; elite-no-FP preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-06 Mode-1-only level-mismatch flag (rank-gap×instability severity, enum-guarded) + final regression gate</name>
  <files>backend/shared/python/sunity_shared/analysis/safety_flags.py, backend/tests/phase10/test_safety_flags_level.py</files>
  <read_first>
    - backend/shared/python/sunity_shared/analysis/safety_flags.py (compute_safety_flags with mode + experience + reference_level kwargs already plumbed in 10-02)
    - backend/shared/python/sunity_shared/models.py:206 (EXPERIENCE_LEVELS = beginner/intermediate/advanced), normalize_body_profile enum-drop behavior (D-06 spoof fail-safe); MODE_EXPERT / MODE_SELF constants
    - app/src/types/analysis.ts:578 (SkillLevel basic/intermediate/advanced = reference.level)
    - .planning/phases/10-injury-risk-flags/10-RESEARCH.md (D-06 Open Q3 RESOLVED — experience + reference.level reachable; Security: spoofed experience → enum-drop omit)
    - .planning/phases/10-injury-risk-flags/10-CONTEXT.md (D-06 mode1-only, D-02)
    - backend/tests/phase10/test_safety_flags_level.py (xfail tests from 10-01)
  </read_first>
  <behavior>
    - Mode 1, reference.level rank > experience rank (e.g. advanced ref × beginner user, gap=2) AND control-loss → SafetyFlag(flag_type='level_mismatch', mode_scope='mode1_only'); severity scales: gap=1 + medium instability → 'low'; gap≥2 or high instability → 'medium'/'high'.
    - Mode 1, gap=1 with only mild instability → conservative (low severity), does not over-warn.
    - Same inputs in Mode 3 → no level flag (D-06 mode1-only; Mode 3 overreach handled by D-02).
    - experience is None / non-enum (spoofed) → no level flag (fail-safe, enum-guarded inside safety_flags.py).
    - reference_level None / non-enum → no level flag.
    - level mismatch WITHOUT control-loss → no flag (D-02).
  </behavior>
  <action>
    Implement `_level_mismatch_flag(mode, experience, reference_level, fsr) -> SafetyFlag | None` in safety_flags.py. Return None immediately unless `mode == MODE_EXPERT` (D-06 mode1-only).
    ENUM GUARD (MEDIUM): even though upstream `normalize_body_profile` drops non-enum experience, guard membership INSIDE safety_flags.py — map via explicit ladders `{'basic':0,'beginner':0,'intermediate':1,'advanced':2}`; if `experience` or `reference_level` is not a known key (None/spoofed/unexpected) → return None (fail-safe, no crash, do not trust upstream blindly).
    SEVERITY = RANK-GAP × INSTABILITY (MEDIUM): `gap = reference_rank − experience_rank`; posture_met when `gap >= 1`. Compute severity from BOTH gap and the control-loss intensity (max StabilityMetric severity): gap==1 AND instability=='medium' → severity 'low'; (gap>=2) OR instability=='high' → 'medium' or 'high' (gap>=2 AND high → 'high'). This stops a gap of exactly 1 from over-warning. control-loss partner = `_control_loss_phase_level(fsr)` (whole-body overreach is the legitimate phase-level case — documented in 10-02 as the ONLY allowed phase-level flag).
    Emit via `_maybe_flag(posture_met, control_lost, flag_type='level_mismatch', mode_scope='mode1_only', severity=<computed>, ...)`. Append to compute_safety_flags. Un-xfail the level tests (incl. Mode-3-no-flag, gap=1-low-severity, and spoof-omit). Then run the full backend suite as the final phase regression gate.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/phase10/test_safety_flags_level.py -x -q && python -m pytest tests -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && python -m pytest tests/phase10/test_safety_flags_level.py -x -q` GREEN (mode1 mismatch+control-loss→flag; gap=1+mild→low severity; mode3 same inputs→none; spoof/None experience→none; AND-gate).
    - `grep -n "MODE_EXPERT\|mode1_only" backend/shared/python/sunity_shared/analysis/safety_flags.py` confirms mode1-only gating; a test asserts gap=1 yields lower severity than gap=2 (grep test name, e.g. `test_level_severity_scales_with_gap`).
    - Enum guard present: `grep -nE "experience.*in|\\.get\\(experience|ladder" backend/shared/python/sunity_shared/analysis/safety_flags.py` shows membership guarded inside the module.
    - `cd backend && python -m pytest tests/phase10 -x -q` ALL GREEN (4 flag types + elite-no-FP + temporal-colocation + contract + deterministic).
    - `cd backend && python -m pytest tests -q` full backend suite GREEN (zero regression).
    - `cd app && npm run typecheck` GREEN.
  </acceptance_criteria>
  <done>D-06 mode1-only level-mismatch flag (enum-guarded inside module, severity scales with gap×instability so gap=1 does not over-warn); all 4 flag types complete; full phase10 + backend suite + typecheck GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→pipeline | `bodyProfile.experience` (D-06 input) untrusted; sanitized by normalize_body_profile AND re-guarded inside safety_flags.py. |
| reference doc→pipeline | `reference.level` from seeded admin docs (trusted, server-side) — still enum-guarded before ranking. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02 | Tampering | spoofed `experience` to suppress/trigger level flag | mitigate | normalize_body_profile enum-drop + in-module ladder membership guard → None → level flag omitted fail-safe (Task 2); test asserts spoof→no flag. |
| T-10-FP | Tampering (domain) | posture-alone / over-warn FP on 정은지 or intermediate users | mitigate | D-02 LOCAL+TEMPORAL AND-gate across all 4 rules; gap=1 conservative severity; elite-no-FP re-asserted with full rule set (Task 1/2). |
| T-10-08 | Tampering | curve-fit asymmetry threshold | mitigate | Reference-anchored + KISMAM tol only (1.5× margin); grep gate forbids "13 videos" justification (D-07). |
| T-10-11 | Availability/trust | first-Mode-3-video asymmetry silently does nothing | mitigate | reference_angles=None → no-op is the SURFACED limitation from 10-02 (D-05 absolute carries Mode-3); not a hidden dead end. |
| T-10-SC | Tampering | npm/pip installs | accept | No package installs this phase. |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/phase10 -x -q` — all 4 flag types + elite-no-FP + temporal-colocation + contract + deterministic GREEN.
- `cd backend && python -m pytest tests -q` — full backend suite GREEN.
- `cd app && npm run typecheck` — GREEN.
- Manual: Mode 1 result with advanced-ref × beginner-experience + instability → "레벨 대비 무리한 동작 가능성" card; Mode 3 same → no level card; high single-pair asymmetry + that-pair instability → "좌우 비대칭 신호" card naming the pair; intermediate × advanced (gap=1) + mild instability → low-severity (or no over-warn).
</verification>

<success_criteria>
- D-03 asymmetry fires reference-anchored + pair-local control-loss with explicit pairs + MAX aggregation; intentional asymmetry cancels; absolute L/R never flagged; responsible pair audited (SC 1).
- D-06 level-mismatch fires Mode 1 only + control-loss; severity scales with gap×instability (gap=1 no over-warn); Mode 3 no level flag; spoof fail-safe (enum-guarded in-module) (SC 3).
- All four flag types complete and auto-render via 10-02 section; Mode-3 first-video no-op surfaced.
- 정은지 elite no-FP preserved across the full rule set.
- Full phase10 + backend suite + typecheck GREEN — SAFE-01 satisfied.
</success_criteria>

<output>
Create `.planning/phases/10-injury-risk-flags/10-04-SUMMARY.md` when done.
</output>
