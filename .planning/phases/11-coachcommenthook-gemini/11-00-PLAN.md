---
phase: 11-coachcommenthook-gemini
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/shared/python/sunity_shared/analysis/coach_hook.py
  - backend/shared/python/sunity_shared/analysis/coach_hook_builder.py
  - app/src/types/analysis.ts
  - backend/shared/python/sunity_shared/models.py
  - docs/contract.md
  - backend/tests/phase11/__init__.py
  - backend/tests/phase11/conftest.py
  - backend/tests/phase11/test_coach_hook_translation_only.py
  - backend/tests/phase11/test_coach_hook_fallback.py
  - backend/tests/phase11/test_coach_hook_nested_array.py
autonomous: true
requirements: [COACH-01, FEED-02]
must_haves:
  truths:
    - "CoachCommentHook 타입이 analysis.ts ↔ models.py ↔ contract.md 3-way lockstep 으로 추가된다 (COACH-01 SC#1)"
    - "CoachCommentHook 의 list 필드는 list[str] 전용 — nested array 금지 (Firestore flat 안전)"
    - "coach_hook.py 는 dataclass + pure validator 만 보유 (builder 는 별도 모듈) — circular-import 안전 (HIGH-3)"
    - "Wave 0 test scaffold 가 collection GREEN + intentional RED (NotImplementedError) 로 실행된다 (WARNING-1)"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/coach_hook.py"
      provides: "CoachCommentHook frozen dataclass + pure validators ONLY (no finding-class import)"
      contains: "class CoachCommentHook"
    - path: "backend/shared/python/sunity_shared/analysis/coach_hook_builder.py"
      provides: "build_canned_hook stub raising NotImplementedError (Wave 1 fills)"
      contains: "def build_canned_hook"
    - path: "backend/tests/phase11/conftest.py"
      provides: "golden ForcePatternFinding / BodyComparisonFinding fixtures"
    - path: "backend/tests/phase11/test_coach_hook_translation_only.py"
      provides: "D-05 forbidden-pattern + degree-guard gate scaffold"
    - path: "backend/tests/phase11/test_coach_hook_fallback.py"
      provides: "D-08 api_key_loader/None-return fallback gate scaffold"
    - path: "backend/tests/phase11/test_coach_hook_nested_array.py"
      provides: "Pitfall 1+2 validator gate scaffold (both report paths)"
  key_links:
    - from: "app/src/types/analysis.ts"
      to: "backend/shared/python/sunity_shared/models.py"
      via: "3-way lockstep mirror (re-export)"
      pattern: "CoachCommentHook"
---

<objective>
CoachCommentHook 데이터 구조를 정의하고 (3-way lockstep), Phase 11 의 Nyquist test scaffold (conftest + 3 test 파일) 를 **collection-green + intentional-RED** 상태로 박제한다. circular-import 회피를 위해 dataclass(`coach_hook.py`)와 builder/writer 진입점(`coach_hook_builder.py`)을 분리한다. 이 plan 은 측정 가능한 계약과 실패 테스트를 먼저 깐다 — Wave 1 (text-only hook writer + 부착 + validator) 이 이 테스트를 GREEN 으로 만든다.

Purpose: COACH-01 SC#1 (타입이 계약 양쪽에 추가) 충족 + D-05/D-08/nested-array 강제의 실패-우선 안전망 구축. 단일 atomic 계약 변경 (Pitfall 3 회피) + 모듈 분리로 force_pattern.py → coach_hook.py → force_pattern.py 순환 import 차단(HIGH-3).
Output: `CoachCommentHook` frozen dataclass + pure validators (coach_hook.py), `build_canned_hook` NotImplementedError stub (coach_hook_builder.py), TS interface + 두 report interface 의 `coachCommentHook?` 필드, models.py re-export, contract.md 섹션, phase11 test 디렉토리 5 파일.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-coachcommenthook-gemini/11-CONTEXT.md
@.planning/phases/11-coachcommenthook-gemini/11-REVIEWS.md
@.planning/phases/11-coachcommenthook-gemini/11-RESEARCH.md
@.planning/phases/11-coachcommenthook-gemini/11-PATTERNS.md
@.planning/phases/11-coachcommenthook-gemini/11-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: CoachCommentHook dataclass + pure validators (module split) + 3-way lockstep (atomic)</name>
  <files>backend/shared/python/sunity_shared/analysis/coach_hook.py, app/src/types/analysis.ts, backend/shared/python/sunity_shared/models.py, docs/contract.md</files>
  <read_first>
    - backend/shared/python/sunity_shared/analysis/coach_hook.py (신규 — 없으면 생성)
    - backend/shared/python/sunity_shared/analysis/force_pattern.py:228-262 (ForcePatternInference frozen dataclass + __post_init__ — 본 dataclass 가 coach_hook.CoachCommentHook 을 import 하므로 coach_hook 은 finding 클래스를 import 하면 안 됨 — HIGH-3 순환 방지 근거)
    - backend/shared/python/sunity_shared/analysis/body_normalizer.py:861-883 (BodyComparisonReport — trailing default 필드 precedent + 이 모듈도 coach_hook 을 import)
    - app/src/types/analysis.ts:186, :198, :317-354, :345, :908-920, :1174-1199 (ForcePatternInference / BodyComparisonReport interfaces + AnalysisResult optional-nullable 필드 precedent + Phase 11 경계 주석 — 읽되 detail2/CoachingTipDetail 은 절대 수정 금지, D-01)
    - backend/shared/python/sunity_shared/models.py:229-330 (파일 하단 re-export 패턴 + co-edit 주석)
    - docs/contract.md §8 (BodyComparisonReport) + §9.11 (ForcePatternInference)
    - .planning/phases/11-coachcommenthook-gemini/11-CONTEXT.md (D-01 / D-02 / D-06)
    - .planning/phases/11-coachcommenthook-gemini/11-PATTERNS.md (Pattern 1 + Pattern 2)
  </read_first>
  <action>
    신규 모듈 `coach_hook.py` 에 `@dataclass(frozen=True) class CoachCommentHook` 정의 (D-02 per-report 부착 단위). 필드: `auto_findings_summary: str`, `open_questions_for_coach: list[str]`, `suggested_cues: list[str]`, 그 뒤 default 필드 `coach_comment: str | None = None`, `reviewed_by: str | None = None`, `source_report: str | None = None` (v2 콘솔 집계용 provenance scalar, D-02 근거). non-default → default 순서 엄수 (force_pattern.py trailing default precedent). `__post_init__` 에서 `open_questions_for_coach` / `suggested_cues` 가 `list[str]` of non-empty str 임을 strict 검증 (ForcePatternInference.__post_init__ 패턴 mirror) — list[dict]/nested list 는 ValueError. coach_comment/reviewed_by = v1 항상 None 의도 (D-06).

    **HIGH-3 모듈 분리 강제:** coach_hook.py 는 **frozen dataclass + pure validator helper(예: `_validate_str_list(value, *, field)`) 만** 포함한다. `ForcePatternFinding`/`BodyComparisonFinding` 등 finding 클래스를 **import 하지 않는다** (모듈-로드 시점 import 금지). 이유: `force_pattern.py` 와 `body_normalizer.py` 가 `coach_hook.CoachCommentHook` 을 import 하므로, coach_hook.py 가 그 모듈들을 역참조하면 `force_pattern → coach_hook → force_pattern` 순환이 발생한다(HIGH-3). canned/builder 로직은 Task 의 다른 파트(coach_hook_builder.py, Task 2)에 둔다.

    같은 atomic 변경으로: (1) analysis.ts 에 `CoachCommentHook` interface 추가 (list 필드 = `string[]` 전용, coachComment/reviewedBy = `string | null`, sourceReport = `string | null`) + `ForcePatternInference` (L908) 와 `BodyComparisonReport` (L1174) 두 interface 에 `coachCommentHook?: CoachCommentHook | null` 추가 (AnalysisResult L334-354 의 옵셔널-nullable 후방호환 주석 패턴 mirror). (2) models.py 파일 하단 re-export 블록에 `from .analysis.coach_hook import CoachCommentHook` 추가 (재정의 금지, re-export only — models.py:271 패턴). (3) contract.md §8/§9.11 에 CoachCommentHook 하위 섹션 추가 (필드 표 + list[str] 전용 + v1 null 명시). detail2/CoachingTipDetail (analysis.ts:198) 는 절대 손대지 않음 (D-01). 4개 파일 단일 commit (Pitfall 3 — lockstep drift 방지).
  </action>
  <acceptance_criteria>
    - `grep -n "class CoachCommentHook" backend/shared/python/sunity_shared/analysis/coach_hook.py` → 1 hit
    - coach_hook.py 가 finding 클래스를 import 안 함: `grep -cE "import (ForcePatternFinding|BodyComparisonFinding|force_pattern|body_normalizer)" backend/shared/python/sunity_shared/analysis/coach_hook.py` → 0 (HIGH-3 순환 방지)
    - `python -c "import sunity_shared.analysis.force_pattern; import sunity_shared.analysis.coach_hook"` 가 순환 import 에러 없이 통과 (PYTHONPATH=backend/shared/python)
    - `grep -n "interface CoachCommentHook\|coachCommentHook" app/src/types/analysis.ts` → CoachCommentHook interface + 두 report interface 의 필드 = 최소 3 hit
    - `grep -n "CoachCommentHook" backend/shared/python/sunity_shared/models.py` → re-export 1 hit (재정의 아님 — `class CoachCommentHook` 가 models.py 에 없어야 함)
    - `grep -c "CoachingTipDetail\|detail2" app/src/types/analysis.ts` 가 변경 전과 동일 (detail2 미수정, D-01)
    - coach_hook.py 의 CoachCommentHook 필드 순서: 3 non-default (str + 2 list) → 3 default (None). `python -c "from sunity_shared.analysis.coach_hook import CoachCommentHook; CoachCommentHook(auto_findings_summary='a', open_questions_for_coach=['q'], suggested_cues=['c'])"` 가 예외 없이 import+생성 (default 필드 생략 가능 증명)
    - `cd app && npm run typecheck` clean (3-way mirror 컴파일)
  </acceptance_criteria>
  <verify>
    <automated>cd app && npm run typecheck</automated>
  </verify>
  <done>CoachCommentHook 가 3-way lockstep 으로 추가됨. coach_hook.py = dataclass + pure validator only (finding import 0, 순환 안전). list 필드 = list[str]/string[] 전용. coachComment/reviewedBy = v1 null. detail2 무수정. tsc clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: builder stub 분리 + Phase 11 Nyquist test scaffold (collection-green, intentional RED)</name>
  <files>backend/shared/python/sunity_shared/analysis/coach_hook_builder.py, backend/tests/phase11/__init__.py, backend/tests/phase11/conftest.py, backend/tests/phase11/test_coach_hook_translation_only.py, backend/tests/phase11/test_coach_hook_fallback.py, backend/tests/phase11/test_coach_hook_nested_array.py</files>
  <read_first>
    - backend/tests/phase09/test_force_pattern_copy_no_forbidden.py (AST forbidden-phrase grep gate precedent — string 상수 walk + parametrize)
    - backend/tests/phase13/test_branch2_forbidden_phrase_gate.py (forbidden-phrase gate 2nd precedent)
    - backend/shared/python/sunity_shared/gemini/guardrails.py:26-105 (_SCORE_PATTERNS / _JUDGMENT_PATTERNS / _enforce_no_reject_patterns — 금지 패턴 정렬 source. 도/°/deg 는 아직 없음 — Wave 1 이 추가, 테스트는 도 케이스 미리 박제)
    - backend/shared/python/sunity_shared/firestore_admin.py:343-404 (_validate_force_pattern_inference — nested_array test 가 호출할 scoped validator. dict 브랜치 :371-374 가 현재 ANY nested dict reject)
    - backend/shared/python/sunity_shared/analysis/force_pattern.py (ForcePatternFinding 생성 시그니처 — golden fixture 용)
    - backend/shared/python/sunity_shared/analysis/body_normalizer.py (BodyComparisonFinding 생성 시그니처 — golden fixture 용)
    - backend/shared/python/sunity_shared/analysis/coach_hook.py (Task 1 의 CoachCommentHook — builder stub 가 반환 타입으로 참조)
    - .planning/phases/11-coachcommenthook-gemini/11-VALIDATION.md (Per-Task Verification Map + Wave 0 Requirements — 각 파일이 묶이는 req)
  </read_first>
  <action>
    **builder 모듈 stub (WARNING-1 collection-green 보장):** 신규 `coach_hook_builder.py` 에 `def build_canned_hook(findings, *, source_report: str) -> CoachCommentHook: raise NotImplementedError("Wave 1: 11-01 Task 1")` stub 추가 (coach_hook.CoachCommentHook 만 import — finding 클래스는 `TYPE_CHECKING` 가드 또는 local import 로). 이렇게 하면 Wave 0 테스트가 `build_canned_hook` 를 **top-level import 해도 collection 에러가 아니라 호출 시 NotImplementedError(RED)** 가 된다.

    `backend/tests/phase11/` 디렉토리 신설 + `__init__.py` (phase08_1 처럼 환경 blocker 회피). `conftest.py` 에 shared golden fixture: 실제 측정값을 가진 `ForcePatternFinding` 1개 + `BodyComparisonFinding` 1개 (force_pattern.py / body_normalizer.py 의 실 생성 시그니처 사용 — 임의 dict 아님).

    `test_coach_hook_translation_only.py`: (a) `@pytest.mark.parametrize` over 금지 tuple (`("점", "/10", "좌표", "x=", "y=", "잘했다", "훌륭", "완벽", "양호")`) + **도(degree) 케이스 `("23도", "23°", "23 deg", "15%")`** (HIGH-1) — golden finding → `build_canned_hook(...)` 출력의 모든 텍스트 필드 blob 에 금지 표현 0 assert. 호출이 NotImplementedError 이므로 RED. (b) `test_guard_rejects_degree`: `_enforce_no_reject_patterns("무릎이 23도 차이", context="coach_hook")` 가 ValueError 를 raise 해야 한다 assert — 현재 guardrails 가 도(°/도/deg) 를 안 잡으므로 **RED** (Wave 1 이 regex 추가 시 GREEN). (c) `test_guard_rejects_score`: "87점"/"훌륭" → ValueError → canned fallback 경로 assert. import 은 collection-green, 동작은 RED.

    `test_coach_hook_fallback.py`: hook writer 의 **api_key_loader 실패 / hook 호출 None 반환 seam** (WARNING-2) → `build_canned_hook` 반환 + 분석 status done (절대 fail 안 함) assert (D-08, ROADMAP SC#5). Cerebras 의 private `_client` shape 를 직접 검사하지 않음. build_canned_hook NotImplementedError 로 RED.

    `test_coach_hook_nested_array.py`: (1) valid flat hook dict (`coachCommentHook` 키, list[str] 필드) 를 `_validate_force_pattern_inference` 에 넣어 통과 assert (Pitfall 1 — scoped validator 가 새 키 허용해야 함). (2) `coachCommentHook` 안 `openQuestionsForCoach` 를 list[dict]/nested list 로 만든 dict → ValueError assert (Pitfall 2 — nested-array reject). (3) BodyComparisonReport generic validator path 도 같은 flat hook 통과 assert (A2 — 양쪽 path 검증). validator 가 아직 coachCommentHook 키를 모르므로 (1) 이 현재 ValueError → RED.

    모든 테스트는 Wave 1 구현 전 **collection GREEN + assertion/NotImplementedError RED** 여야 한다 (WARNING-1: top-level import 에러 0). 슬롭/수치 채우기 금지 — 의미있는 assert 만.
  </action>
  <acceptance_criteria>
    - `cd backend && PYTHONPATH=shared/python python -m pytest tests/phase11 -q --co` 가 collection **성공** (import 에러 0 — WARNING-1). 0 collection error.
    - `grep -n "def build_canned_hook" backend/shared/python/sunity_shared/analysis/coach_hook_builder.py` → 1 hit + `raise NotImplementedError`
    - coach_hook_builder.py 가 finding 클래스를 top-level import 안 함: `grep -cE "^from .force_pattern import|^from .body_normalizer import" backend/shared/python/sunity_shared/analysis/coach_hook_builder.py` → 0 (TYPE_CHECKING/local import 만)
    - `cd backend && PYTHONPATH=shared/python python -m pytest tests/phase11 -q` 가 실제 test 실행 (collection 아닌) → RED(fail/NotImplementedError) 허용
    - `test_coach_hook_translation_only.py` 가 도(degree) 케이스(`23도`/`23°`/`deg`/`%`) 를 금지 tuple 에 포함 (HIGH-1) — `grep -n "23도\|23°\|deg\|%" backend/tests/phase11/test_coach_hook_translation_only.py` hit
    - `test_coach_hook_fallback.py` 가 `api_key_loader`/None-return seam 을 검사하고 `_client` private 필드를 검사하지 않음 (WARNING-2): `grep -c "_client" backend/tests/phase11/test_coach_hook_fallback.py` → 0
    - `test_coach_hook_nested_array.py` 가 `_validate_force_pattern_inference` 와 generic `_validate_flat_dict_no_nested_array` 둘 다 import (양쪽 report path)
    - conftest.py 의 golden fixture 가 ForcePatternFinding / BodyComparisonFinding 를 실 생성자로 빌드 (임의 dict mock 아님)
  </acceptance_criteria>
  <verify>
    <automated>cd backend && PYTHONPATH=shared/python python -m pytest tests/phase11 -q --co</automated>
  </verify>
  <done>coach_hook_builder.py stub + phase11 test 5 파일 존재, collection GREEN (import 에러 0), 4 게이트 (translation-only / degree-guard / fallback / nested-array) intentional RED. Wave 1 이 GREEN 으로 만들 대상 명확.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM output → Firestore/app | Gemini 가 낸 텍스트는 untrusted — 수치/판정/도(degree) mint 가능 |
| Firestore write → scoped validator | hook dict 가 nested-array gate 통과해야 함 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | CoachCommentHook list 필드 (list[dict] 유입) | mitigate | 타입을 list[str] 전용으로 박제 + __post_init__ strict + test_coach_hook_nested_array.py 가 list[dict] reject 검증 (D-02/Pattern 2) |
| T-11-02 | Tampering/Repudiation | LLM 이 점수/좌표/판정/도(degree) 생성 (D-04 위반) | mitigate | translation-only 정적 forbidden-phrase 게이트 + 도(degree) regex 케이스 미리 박제 (Wave 0) + Wave 1 런타임 _enforce_no_reject_patterns reject-and-fallback. test scaffold 가 게이트 우선 박제 |
| T-11-SC | Tampering | npm/pip 설치 | accept | Phase 11 신규 패키지 0 (RESEARCH §Package Legitimacy "Not applicable") — 설치 task 없음 |
</threat_model>

<verification>
- `cd app && npm run typecheck` clean (3-way lockstep 컴파일)
- `cd backend && PYTHONPATH=shared/python python -m pytest tests/phase11 -q --co` collection 성공 (import 에러 0)
- coach_hook.py / coach_hook_builder.py 가 finding 클래스 top-level import 0 (순환 안전, HIGH-3)
- detail2/CoachingTipDetail 미수정 (grep 카운트 불변, D-01)
</verification>

<success_criteria>
- CoachCommentHook 3-way lockstep 추가 (COACH-01 SC#1)
- coach_hook.py = dataclass + pure validator only; builder 별도 모듈 (circular-import 안전, HIGH-3)
- list 필드 list[str]/string[] 전용 (nested-array 안전)
- coachComment/reviewedBy v1 null (COACH-01 "UI/입력은 v2")
- detail2 공존/무수정 (D-01)
- phase11 test 5 파일: collection GREEN + intentional RED (D-05/도-guard/D-08/Pitfall 1+2, WARNING-1)
- FEED-02 = 기존 dual-coach 로 이미 Complete — 본 plan 재작업 0 (coach hook 은 그 위 layer)
</success_criteria>

<output>
Create `.planning/phases/11-coachcommenthook-gemini/11-00-SUMMARY.md` when done
</output>
</content>
