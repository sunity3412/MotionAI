---
phase: 11-coachcommenthook-gemini
plan: 01
type: execute
wave: 1
depends_on: ["11-00"]
files_modified:
  - backend/shared/python/sunity_shared/gemini/schemas.py
  - backend/shared/python/sunity_shared/gemini/guardrails.py
  - backend/shared/python/sunity_shared/gemini/coach_hook_writer.py
  - backend/shared/python/sunity_shared/analysis/coach_hook_builder.py
  - backend/functions/pipeline/app.py
  - backend/shared/python/sunity_shared/analysis/force_pattern.py
  - backend/shared/python/sunity_shared/analysis/body_normalizer.py
  - backend/shared/python/sunity_shared/firestore_admin.py
  - backend/tests/phase11/test_coach_hook_translation_only.py
  - backend/tests/phase11/test_coach_hook_fallback.py
  - backend/tests/phase11/test_coach_hook_nested_array.py
autonomous: true
requirements: [COACH-01, FEED-02]
must_haves:
  truths:
    - "신규 text-only GeminiCoachHookWriter 가 1회 분석당 hook 1회 호출로 두 리포트 hook 을 함께 산출한다 — 기존 Vision GeminiCoachWriter.write() 무수정, videoPath/GeminiVisionCall 미접촉 (D-03 correction, BLOCKER-1)"
    - "hook 생성이 force_pattern_inference 생성(:2099-2115) 이후 / complete_analysis(:2286) 이전에 일어난다 — ForcePatternInference.findings 를 hook 이 본다 (BLOCKER-2)"
    - "coach_details(joint writer) 와 coach_hooks(신규 writer) 가 별도 pipeline 변수 — joint-card success/fallback 집계 오염 0 (BLOCKER-3)"
    - "Gemini 가 점수/좌표/판정/ANY Arabic digit 생성 시 reject-and-fallback → canned hook. hook-scoped 가드가 모든 \\d (3초/2회/15cm/180 + 도/%) 를 reject (글로벌 _SCORE_PATTERNS 미오염 — scene_finder/reference_extractor 회귀 0, iter-2 BLOCKER-2). 런타임 sanitize 없음 — number-free 잠금 (D-04/D-05, iter-3 HIGH-2)"
    - "hook writer 가 _strip_unsupported_schema_keys(CoachHookBundle.model_json_schema()) config 를 재사용한다 — raw Pydantic schema 직접 주입 금지 ($defs/additionalProperties 잔존 시 live Gemini 400, iter-2 HIGH-1)"
    - "build_coach_hooks 는 CoachHookBundle | None 만 반환 (api/4xx/guard reject/retry-exhausted → None) — canned 미생성. pure helper resolve_coach_hook_bundle 이 per-report 폴백을 소유 (partial/None bundle 무관 두 리포트 모두 hook, iter-3 HIGH-1)"
    - "CoachCommentHookPayload 와 CoachHookBundle 둘 다 model_config=ConfigDict(extra=forbid) — LLM 경계에서 stray top-level key (예: Gemini typo bodyComparison) 가 ValidationError (조용한 drop 금지, iter-3 MEDIUM-1)"
    - "api_key_loader 실패 또는 hook 호출 None 반환 시 resolve_coach_hook_bundle 의 canned hook 으로 분석이 완료된다 (status done, D-08/SC#5, WARNING-2)"
    - "ForcePatternInference scoped validator 가 _validate_coach_comment_hook 로 coachCommentHook 키를 strict 검증한다 (list[str] only — list[dict] reject, iter-2 BLOCKER-1). body 는 complete_analysis precheck 로 동일 strict 검증 (generic validator 가 list[dict] 허용하므로 우회 불가)"
  artifacts:
    - path: "backend/shared/python/sunity_shared/gemini/coach_hook_writer.py"
      provides: "GeminiCoachHookWriter — text-only Gemini hook 생성 (resolve_model('B'), CoachHookBundle schema, _strip_unsupported_schema_keys config 재사용, hook-scoped \\d(number-free) 가드, retry path). build_coach_hooks() -> CoachHookBundle | None 만 반환 — canned 미생성"
      contains: "class GeminiCoachHookWriter"
    - path: "backend/shared/python/sunity_shared/gemini/schemas.py"
      provides: "CoachHookBundle + CoachCommentHookPayload Pydantic schema (두 optional report hook, 둘 다 ConfigDict(extra=forbid))"
    - path: "backend/shared/python/sunity_shared/analysis/coach_hook_builder.py"
      provides: "build_canned_hook (단일 report findings → 객관 number-free 한국어 hook) + resolve_coach_hook_bundle (pure helper: bundle|None + 양쪽 findings → (force_hook, body_hook) tuple, 둘 다 non-None 보장)"
    - path: "backend/shared/python/sunity_shared/firestore_admin.py"
      provides: "_validate_coach_comment_hook (전용 strict 화이트리스트) + force scoped validator 브랜치 + complete_analysis body precheck"
  key_links:
    - from: "backend/functions/pipeline/app.py"
      to: "backend/shared/python/sunity_shared/gemini/coach_hook_writer.py"
      via: "force_pattern_inference 생성 후 hook 1회 호출 (:2115~:2286 window)"
      pattern: "GeminiCoachHookWriter|build_coach_hooks"
    - from: "backend/functions/pipeline/app.py"
      to: "backend/shared/python/sunity_shared/analysis/coach_hook_builder.py"
      via: "resolve_coach_hook_bundle(bundle, force_findings=, body_findings=) per-report 폴백"
      pattern: "resolve_coach_hook_bundle"
    - from: "backend/functions/pipeline/app.py"
      to: "backend/shared/python/sunity_shared/firestore_admin.py"
      via: "complete_analysis(force_pattern_inference=, body_comparison_report=) hook 포함 dict"
      pattern: "coachCommentHook"
---

<objective>
백엔드 분석 파이프라인이 1회 분석에서 **신규 text-only `GeminiCoachHookWriter`** 를 1회 호출해 두 리포트(ForcePatternInference, BodyComparisonReport)의 `CoachCommentHook` 텍스트를 함께 산출하고, **pure helper `resolve_coach_hook_bundle`** 로 per-report 폴백을 해소하고, 두 리포트에 hook 을 부착하고, Firestore validator 를 hook 전용 strict 화이트리스트로 확장하여 list[str]-only 게이트를 통과시킨다. 이것이 Phase 11 의 thin end-to-end 백엔드 슬라이스 — Wave 0 의 RED 테스트를 GREEN 으로 만든다.

**LOCKED DESIGN (D-03 [2026-06-16 review correction], BLOCKER-1):** 기존 Vision `GeminiCoachWriter.write()` (coach_writer_v2.py, joint-card / videoPath 의존)는 **무수정**. 대신 **별도 text-only** `coach_hook_writer.py::GeminiCoachHookWriter` 를 신설한다 — `resolve_model('B')` + hook 전용 Pydantic schema(`CoachHookBundle`) + **기존 `_strip_unsupported_schema_keys` config 재사용**(iter-2 HIGH-1) + hook-scoped number-free 가드, `GeminiVisionCall`/`videoPath` 미접촉. "single call" = 1회 분석당 hook 호출 1회로 두 리포트 hook 을 함께 묶는다(report 당 1회 아님, joint writer dict 재사용 아님). 근거: 기존 writer 는 `coach_writer_v2.py:1` Vision 전용 + `write()` 가 videoPath 없으면 `video_path_missing` 폴백(:414-417) → 재사용 시 Vision 독립 잠금 위반 또는 Phase 11/17 경계 침범 + strict `CoachPayload`(joints-only) joint 집계 오염(BLOCKER-3).

**[2026-06-16 Codex iteration-2] 추가 LOCK:**
- **BLOCKER-2 (iter-2):** 도/% reject 는 **hook-scoped only**. 글로벌 `_SCORE_PATTERNS` 에 도/%(또는 임의 \\d) 추가 금지 — scene_finder.py(:75 "30도 이상 후굴", :77 "50% 이상 가려진") / reference_extractor.py 가 `enforce_object_guard=True` 로 같은 글로벌 가드를 쓰므로 글로벌 확장 시 Phase 17 Vision 호출이 정당한 notes_ko("30도"/"50%") 로 trip 됨.
- **HIGH-1 (iter-2):** hook writer 는 `_strip_unsupported_schema_keys(CoachHookBundle.model_json_schema())` + `response_mime_type="application/json"` + `HttpOptions(timeout=_HTTP_TIMEOUT_MS)` config 를 재사용 — `CoachHookBundle` 은 nested 라 `$defs` 가 생겨 raw 주입 시 live 400.
- **HIGH-2 (iter-2):** bundle 의 두 report hook 은 optional — partial(한쪽만) 반환 시 per-report 폴백으로 두 리포트 모두 hook 부착.
- **BLOCKER-1 (iter-2):** generic `_validate_flat_dict_no_nested_array` 는 list[dict](scalar-only) 를 허용 → hook 의 list[str] 계약에 미달. 전용 `_validate_coach_comment_hook` 신설 + force scoped 브랜치 + body precheck.

**[2026-06-16 Codex iteration-3] 추가 LOCK:**
- **HIGH-1 (iter-3) — fallback/resolution 소유권 분리:** `GeminiCoachHookWriter.build_coach_hooks(...) -> CoachHookBundle | None` 은 **parsed bundle 또는 None 만** 반환한다 (None on: api_key_loader 실패, 4xx, guard reject, retry 소진). canned hook 을 만들지 않는다. per-report 폴백은 `coach_hook_builder.py` 의 **pure helper** `resolve_coach_hook_bundle(bundle, *, force_findings, body_findings) -> tuple[CoachCommentHook, CoachCommentHook]` 가 소유한다 (AWS/Firestore/pipeline I/O 없음). Task 1 = writer + build_canned_hook + resolve_coach_hook_bundle + partial/None 폴백 테스트(helper 대상). Task 2 = wiring 만(call → resolve → replace → validator).
- **HIGH-2 (iter-3) — number-free 가드:** hook-scoped 가드가 hook text value 안의 **모든 Arabic digit(\\d)** 를 reject 한다 (3초/2회/15cm/180 + 도/% 전부). 글로벌 가드는 무수정. number-free 잠금(D-04).
- **MEDIUM-1 (iter-3):** `CoachCommentHookPayload` AND `CoachHookBundle` 둘 다 `model_config = ConfigDict(extra="forbid")` 명시 + drift unit test.

Purpose: COACH-01 SC#2 (모든 리포트에 부착, partial 폴백 포함) + ROADMAP SC#3 (번역만 강제, number-free 가드) + SC#5 (fallback 완료). Vision/영상 호출 없음 (Phase 17 경계).
Output: 신규 hook writer(schema-strip config 재사용 + retry, bundle|None 만 반환) + hook schema(둘 다 extra=forbid) + hook-scoped number-free 가드 + canned fallback 빌더 + pure resolve_coach_hook_bundle 헬퍼 + pipeline wiring(post-force-pattern window) + 두 리포트 dataclass 필드 + `_validate_coach_comment_hook`(force scoped + body precheck). phase11 게이트 GREEN.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-coachcommenthook-gemini/11-CONTEXT.md
@.planning/phases/11-coachcommenthook-gemini/11-REVIEWS.md
@.planning/phases/11-coachcommenthook-gemini/11-RESEARCH.md
@.planning/phases/11-coachcommenthook-gemini/11-PATTERNS.md
@.planning/phases/11-coachcommenthook-gemini/11-00-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: text-only GeminiCoachHookWriter (bundle|None only, schema-strip config 재사용 + retry) + CoachHookBundle schema (둘 다 extra=forbid) + hook-scoped number-free 가드 + build_canned_hook + pure resolve_coach_hook_bundle (per-report 폴백)</name>
  <files>backend/shared/python/sunity_shared/gemini/coach_hook_writer.py, backend/shared/python/sunity_shared/gemini/schemas.py, backend/shared/python/sunity_shared/gemini/guardrails.py, backend/shared/python/sunity_shared/analysis/coach_hook_builder.py, backend/tests/phase11/test_coach_hook_translation_only.py, backend/tests/phase11/test_coach_hook_fallback.py</files>
  <behavior>
    - golden ForcePatternFinding/BodyComparisonFinding → 생성된 hook 의 텍스트 필드(autoFindingsSummary/openQuestionsForCoach/suggestedCues) 에 점수/좌표/판정/도(°/도/deg)/% 패턴 0 (D-04/D-05)
    - hook-scoped number-free 가드 (iter-3 HIGH-2): hook text value 안의 **모든 Arabic digit(\\d)** reject. `_enforce_no_hook_number_patterns("3초 더 버티기")` (또는 `_enforce_no_reject_patterns(..., context="coach_hook", forbid_measurement_units=True)`) → ValueError. reject fixtures: "3초", "2회", "15cm", "180" (bare numeric, iter-3 HIGH-2) + "23도", "23°", "23 deg", "15%" (degree/% 유지). 도/%/숫자 reject 는 hook 전용 helper/flag 로만.
    - 글로벌 가드 비회귀 (iter-2 BLOCKER-2 핵심): 기본 `_enforce_no_reject_patterns("등이 30도 이상 후굴", context="scene_finder")` (forbid_measurement_units 기본 False) → 예외 **없음** (PASS). "신체의 50% 이상 가려짐" 도 기본 가드 PASS. scene_finder/reference_extractor path 회귀 0. (글로벌 가드는 \\d 를 reject 하지 않음 — hook 전용만.)
    - mock LLM 이 "87점"/"훌륭"/"3초" 출력 → hook writer 가 ValueError → build_coach_hooks 가 **None** 반환 (reject-and-fallback, sanitize 아님, D-05). canned 생성은 writer 가 아니라 resolve_coach_hook_bundle 책임.
    - schema-strip config 재사용 (iter-2 HIGH-1): hook writer 가 generate_content 에 넘기는 config 의 response_schema 에 `$defs`/`additionalProperties`/`discriminator` 키 0 — `_strip_unsupported_schema_keys(CoachHookBundle.model_json_schema())` 경유. raw Pydantic schema 직접 주입 금지.
    - extra=forbid drift (iter-3 MEDIUM-1): `CoachHookBundle.model_validate({"force_pattern_inference": {...valid...}, "unexpected": 1})` → ValidationError. `CoachCommentHookPayload` stray key 도 ValidationError. (Firestore validator 와 별개로 LLM 경계 schema-drift 조기 차단.)
    - schema-validation/5xx retry (iter-2 HIGH-1): mock client 가 1차 ValidationError 또는 APIError(5xx) → 1회 retry; 4xx → 즉시 None (GeminiVisionCall._generate_with_retry mirror).
    - api_key_loader 실패 또는 hook 호출 None 반환 → build_coach_hooks 가 None 반환 (D-08, WARNING-2). canned 텍스트 채움은 resolve_coach_hook_bundle 가 함. _client private 필드 검사 안 함
    - **resolve_coach_hook_bundle pure helper (iter-3 HIGH-1):** `resolve_coach_hook_bundle(bundle, *, force_findings, body_findings) -> (CoachCommentHook, CoachCommentHook)`. bundle=None → 양쪽 canned. bundle 이 force payload 만(body=None) → force 는 변환, body 는 build_canned_hook(body_findings, source_report="bodyComparisonReport"). 양쪽 절대 None 으로 남지 않음 (COACH-01 SC#2). 순수 함수 — AWS/Firestore/네트워크 0.
    - canned hook 도 forbidden-phrase + 모든 \\d 0 (canned 카피 자체가 객관·number-free — 구성상 digit-free 임을 assert)
  </behavior>
  <read_first>
    - backend/shared/python/sunity_shared/gemini/coach_writer_v2.py:1-34 (Vision 전용 문서 — 이 writer 는 **읽기만**, 무수정 확인용), :363-386 (__init__ = _api_key_loader/_model_override, _client 필드 없음 — WARNING-2 근거)
    - backend/shared/python/sunity_shared/gemini/client.py:64-72 (_default_api_key_loader — 신규 writer 의 api_key_loader seam), :64 _HTTP_TIMEOUT_MS module-level + _strip_unsupported_schema_keys module-level (둘 다 import 가능 — HIGH-1 config 재사용 근거), :167-226 (GeminiVisionCall.call 이 files.upload(video_path) 강제 — 재사용 불가 증명, 신규 writer 는 client.models.generate_content text-only path), :267-290 (_build_config = _strip_unsupported_schema_keys(self.schema.model_json_schema()) + response_mime_type + HttpOptions(timeout=_HTTP_TIMEOUT_MS) — 이 config 구성을 hook writer 가 1:1 재사용, iter-2 HIGH-1), :292-360 (_generate_with_retry — ValidationError/5xx 1회 retry, 4xx 즉시 None — hook writer 의 retry/parse path 가 mirror, iter-2 HIGH-1)
    - backend/shared/python/sunity_shared/gemini/config.py:56-87 (resolve_model('B') — region B = 코칭, raw model string 하드코딩 금지)
    - backend/shared/python/sunity_shared/gemini/schemas.py:121-153 (CoachPayload model_config=ConfigDict(extra="forbid") 패턴 :124 — CoachHookBundle/CoachCommentHookPayload 추가 위치 + 패턴, **둘 다** extra=forbid 명시)
    - backend/shared/python/sunity_shared/gemini/guardrails.py:23-105 (_SCORE_PATTERNS/_COORDINATE_PATTERNS/_JUDGMENT_PATTERNS/_enforce_no_reject_patterns — **임의 \\d/도/% 는 글로벌 tuple 에 추가 금지**(iter-2 BLOCKER-2). hook 전용 number-free regex 는 forbid_measurement_units flag 또는 _enforce_no_hook_number_patterns 로만)
    - backend/shared/python/sunity_shared/gemini/scene_finder.py:67-78 (프롬프트가 "30도"/"50%" 정의 — 글로벌 가드 확장 시 회귀할 정당 사용처, iter-2 BLOCKER-2 비회귀 테스트 근거), :161-169 (enforce_object_guard=True 호출)
    - backend/shared/python/sunity_shared/gemini/reference_extractor.py:333-341 (enforce_object_guard=True 2nd 호출자 — 동일 비회귀 대상)
    - backend/shared/python/sunity_shared/analysis/coach_writer.py:145-185 (_client is None graceful no-op 패턴 — 개념 참조, seam 은 api_key_loader)
    - backend/shared/python/sunity_shared/analysis/coach_hook.py (Wave 0 의 CoachCommentHook), coach_hook_builder.py (Wave 0 의 build_canned_hook NotImplementedError stub — 본 task 가 구현 + resolve_coach_hook_bundle 신설)
    - backend/tests/phase11/test_coach_hook_translation_only.py + test_coach_hook_fallback.py (Wave 0 RED 게이트 — 이 task 가 GREEN 으로)
    - .planning/phases/11-coachcommenthook-gemini/11-RESEARCH.md (Pattern 3 + Pitfall 4 + Code Examples) — 단, 본 파일의 [SUPERSEDED 2026-06-16 Codex review] 노트 우선. "GeminiCoachWriter.write() 재사용"/"_client" 전제 무효 (별도 writer + schema-strip config 재사용으로 대체)
  </read_first>
  <action>
    **hook-scoped number-free 가드 (guardrails.py, iter-2 BLOCKER-2 + iter-3 HIGH-2 — 글로벌 미오염):** 글로벌 `_SCORE_PATTERNS`/`_COORDINATE_PATTERNS`/`_JUDGMENT_PATTERNS` 는 **무수정**(scene_finder/reference_extractor 가 같은 글로벌 가드 사용 — "30도"/"50%" 정당 echo). 대신 둘 중 하나(preferred=옵션 B):
    (A) 신규 `_enforce_no_hook_number_patterns(text, *, context="coach_hook")` — **모든 Arabic digit** 전용 regex(`re.compile(r"\d")`) 매치 시 ValueError. (도/%/cm/초/회/bare 정수 전부 포괄 — number-free.) coach_hook_writer.py 가 기존 `_enforce_no_reject_patterns(raw, context="coach_hook")` **이후** 추가 호출.
    (B) `_enforce_no_reject_patterns(..., forbid_measurement_units: bool = False)` 시그니처 확장 — `forbid_measurement_units=True` 일 때만 `re.compile(r"\d")` (모든 숫자) 추가 검사. `GeminiCoachHookWriter` 만 `True` 로 호출, scene_finder/reference_extractor 는 기본 `False` 라 회귀 0.
    어느 쪽이든 **기본/글로벌 경로(forbid_measurement_units 미지정)는 "30도"/"50%"/임의 \\d 를 절대 reject 하지 않아야** 한다 (iter-2 BLOCKER-2). number-free regex 는 점/판정 superset 이 아니라 **hook 전용 별도 게이트** (iter-3 HIGH-2 — degree/% 만이 아니라 모든 \\d).

    **CoachHookBundle schema (schemas.py, iter-3 MEDIUM-1):** `CoachPayload` 의 `model_config = ConfigDict(extra="forbid")` 패턴으로 신규 `class CoachCommentHookPayload(BaseModel)` (`model_config = ConfigDict(extra="forbid")` **명시**, 필드: `auto_findings_summary: str`, `open_questions_for_coach: list[str]`, `suggested_cues: list[str]` — list[str] strict, list[dict] 금지) + `class CoachHookBundle(BaseModel)` (`model_config = ConfigDict(extra="forbid")` **명시**, `force_pattern_inference: CoachCommentHookPayload | None = None`, `body_comparison_report: CoachCommentHookPayload | None = None`). 두 모델 모두 extra=forbid — stray top-level key (Gemini typo) 가 ValidationError (조용한 drop 금지). 두 optional report hook 을 1 payload 에 묶음 (single call). nested model 이므로 `model_json_schema()` 에 `$defs` 발생 — config 에서 strip 필수(아래).

    **GeminiCoachHookWriter (신규 coach_hook_writer.py, iter-3 HIGH-1 — bundle|None only):** `class GeminiCoachHookWriter` — `__init__(self, *, api_key_loader=None, model_override=None)`. `build_coach_hooks(self, *, force_findings, body_findings) -> CoachHookBundle | None` 메서드 (**오직 parsed bundle 또는 None 반환 — canned 생성 금지**):
    (1) `resolve_model("B", model_override)` 로 model 결정 (raw string 금지).
    (2) api_key_loader (기본 `_default_api_key_loader`) 로 키 로드 — 실패/빈 키 시 **None 반환** (graceful, D-08 seam).
    (3) **config (iter-2 HIGH-1):** `genai_types.GenerateContentConfig(response_mime_type="application/json", response_schema=_strip_unsupported_schema_keys(CoachHookBundle.model_json_schema()), http_options=genai_types.HttpOptions(timeout=_HTTP_TIMEOUT_MS))` 로 구성. client.py 의 `_strip_unsupported_schema_keys`/`_HTTP_TIMEOUT_MS` 를 **import 해서 재사용**(주석으로 출처 명시) — raw `CoachHookBundle` 을 response_schema 에 직접 주입 **금지**($defs/additionalProperties → live 400). (factoring: 가능하면 client.py 에서 text-only config 헬퍼를 추출하되, 최소 변경 원칙상 import-and-reuse 도 허용.)
    (4) `genai.Client(api_key).models.generate_content(model=..., contents=[prompt], config=config)` **text-only** 호출 (prompt = force/body findings 의 구조화 텍스트/enum 필드만 — `kismam.top_issues`/generic top-joint deviation 금지, tip.detail2 중복 회피). **`GeminiVisionCall`/`files.upload`/`videoPath`/`contents=[active_file, ...]` 절대 미사용.**
    (5) **retry/parse (iter-2 HIGH-1):** `_generate_with_retry` mirror — ValidationError/JSONDecodeError 또는 APIError 5xx → attempt 1 만 retry, 4xx → 즉시 None. parsed 우선 → text JSON → `CoachHookBundle.model_validate` 폴백. retry 소진 → None.
    (6) raw text → `_enforce_no_reject_patterns(raw, context="coach_hook")` (allow_coords=False) **+ hook-scoped number-free 가드**(옵션 A 호출 또는 옵션 B 의 forbid_measurement_units=True) → ValueError 시 **None 반환** (canned 는 caller 의 resolve_coach_hook_bundle 가). **regex-strip-and-emit 금지(D-05).** prompt 시스템 절: "finding 자연어 풀이만, 수치·좌표·점수·판정·숫자·도(degree)·% 출력 금지" — 도(degree) deviation 을 프롬프트에 **주입하지 않음** (number-free).

    **build_canned_hook (coach_hook_builder.py):** Wave 0 의 `build_canned_hook(findings, *, source_report)` stub 의 NotImplementedError 를 구현으로 교체 — 단일 report findings 입력만 받음. finding 의 기존 텍스트/enum 필드(interpretation 등)만 조합해 객관 한국어 hook(CoachCommentHook) 생성 (LLM 미사용, number-free — \\d 미포함). autoFindingsSummary = 1 짧은 문단, openQuestionsForCoach = 1-3 항목 ("왼쪽 무릎 굽힘이 의도된 동작인지 강사와 확인" 형태), suggestedCues = 2-4 항목. coach_comment/reviewed_by = None (v1). finding 클래스는 TYPE_CHECKING/local import (순환 안전, HIGH-3 보존).

    **resolve_coach_hook_bundle pure helper (coach_hook_builder.py, iter-3 HIGH-1):** 신규 `def resolve_coach_hook_bundle(bundle, *, force_findings, body_findings) -> tuple[CoachCommentHook, CoachCommentHook]`. 순수 함수 — AWS/Firestore/네트워크 I/O 0. 각 report 독립 해소:
    - force_hook = (bundle 가 not None AND bundle.force_pattern_inference present → CoachCommentHookPayload → CoachCommentHook 변환) else `build_canned_hook(force_findings, source_report="forcePatternInference")`.
    - body_hook = (bundle 가 not None AND bundle.body_comparison_report present → 변환) else `build_canned_hook(body_findings, source_report="bodyComparisonReport")`.
    → 둘 다 절대 None 으로 남지 않음 (COACH-01 SC#2). bundle=None / partial / full 세 케이스 모두 양쪽 hook 보장. **이 helper 가 per-report 폴백을 단독 소유** — writer 도 pipeline 도 canned 로직을 갖지 않음. 이 task 로 test_coach_hook_translation_only.py + test_coach_hook_fallback.py(partial/None 폴백 포함) GREEN.
  </action>
  <acceptance_criteria>
    - `cd backend && PYTHONPATH=shared/python python -m pytest tests/phase11/test_coach_hook_translation_only.py tests/phase11/test_coach_hook_fallback.py -x -q` → PASS (Wave 0 RED → GREEN)
    - 기존 Vision writer 무수정: `git diff --stat backend/shared/python/sunity_shared/gemini/coach_writer_v2.py` → 변경 0 줄 (BLOCKER-1)
    - 신규 writer 가 video 미접촉: `grep -cE "GeminiVisionCall|files\.upload|videoPath|video_path|active_file" backend/shared/python/sunity_shared/gemini/coach_hook_writer.py` → 0
    - `grep -n "class GeminiCoachHookWriter" backend/shared/python/sunity_shared/gemini/coach_hook_writer.py` → 1 hit
    - **build_coach_hooks 가 bundle|None only (iter-3 HIGH-1):** `grep -nE "build_canned_hook|resolve_coach_hook_bundle" backend/shared/python/sunity_shared/gemini/coach_hook_writer.py` → 0 (writer 는 canned 미생성, helper 미호출)
    - `grep -n "class CoachHookBundle\|class CoachCommentHookPayload" backend/shared/python/sunity_shared/gemini/schemas.py` → 2 hit
    - **둘 다 extra=forbid (iter-3 MEDIUM-1):** `grep -cE "extra=\"forbid\"" backend/shared/python/sunity_shared/gemini/schemas.py` 가 기존 hit 대비 2 증가 (CoachCommentHookPayload + CoachHookBundle 각 1). drift 테스트: `CoachHookBundle.model_validate({"force_pattern_inference": <valid>, "unexpected": 1})` → ValidationError assert + `CoachCommentHookPayload` stray key → ValidationError assert
    - **schema-strip config 재사용 (iter-2 HIGH-1):** `grep -nE "_strip_unsupported_schema_keys" backend/shared/python/sunity_shared/gemini/coach_hook_writer.py` → 1+ hit. config-inspection 테스트가 generate_content 에 전달된 config 의 response_schema(dict) 를 walk 해 `additionalProperties`/`$defs`/`discriminator` 키 0 assert (mock client 캡처).
    - **글로벌 가드 비회귀 (iter-2 BLOCKER-2):** `python -c "from sunity_shared.gemini.guardrails import _enforce_no_reject_patterns; _enforce_no_reject_patterns('등이 30도 이상 후굴', context='scene_finder')"` → 예외 0 (PASS). 글로벌 tuple 미오염: hook 전용 \\d/도/% regex 가 `_SCORE_PATTERNS`/`_COORDINATE_PATTERNS`/`_JUDGMENT_PATTERNS` tuple **밖**(hook 전용 helper/flag 블록)에만 존재. test_coach_hook_translation_only.py 에 글로벌-가드 "30도"/"50%" PASS assert + hook-가드 "23도"/"23°"/"15%"/"3초"/"2회"/"15cm"/"180" reject assert 둘 다 존재.
    - **hook-scoped number-free 가드 (iter-3 HIGH-2):** `python -c "from sunity_shared.gemini.guardrails import _enforce_no_reject_patterns; _enforce_no_reject_patterns('3초 더 버티기', context='coach_hook', forbid_measurement_units=True)"` → ValueError (옵션 B) 또는 `_enforce_no_hook_number_patterns('3초 더 버티기')` → ValueError (옵션 A). "180"/"15cm"/"2회"/"23도"/"15%" 동일 reject.
    - **retry path (iter-2 HIGH-1):** test_coach_hook_fallback.py 에 mock client 가 1차 5xx APIError → retry → 2차 성공 시 bundle 반환, 1차 4xx → 즉시 None assert
    - **pure resolve_coach_hook_bundle (iter-3 HIGH-1):** `grep -n "def resolve_coach_hook_bundle" backend/shared/python/sunity_shared/analysis/coach_hook_builder.py` → 1 hit. test_coach_hook_fallback.py 가 (a) bundle=None → 양쪽 canned, (b) partial bundle(force payload only) → force 변환/body canned, (c) full bundle → 양쪽 변환 세 케이스 모두 두 hook is not None assert. helper 본문에 boto3/firestore/네트워크 import 0 (pure)
    - `grep -n "resolve_model" backend/shared/python/sunity_shared/gemini/coach_hook_writer.py` → 1+ hit; raw model 하드코딩 0: `grep -c "gemini-3" backend/shared/python/sunity_shared/gemini/coach_hook_writer.py` → 0
    - sanitize-and-emit 부재: hook writer/builder 에 LLM 출력 편집(`re.sub`/`.replace(` on raw) 0 (reject-and-fallback only, D-05)
    - fallback seam = api_key_loader/None-return (WARNING-2): `grep -c "_client" backend/shared/python/sunity_shared/gemini/coach_hook_writer.py` → 0
    - `grep -n "def build_canned_hook" backend/shared/python/sunity_shared/analysis/coach_hook_builder.py` → 1 hit + NotImplementedError 부재
  </acceptance_criteria>
  <verify>
    <automated>cd backend && PYTHONPATH=shared/python python -m pytest tests/phase11/test_coach_hook_translation_only.py tests/phase11/test_coach_hook_fallback.py -x -q</automated>
  </verify>
  <done>신규 text-only GeminiCoachHookWriter 가 single call 로 bundle|None 만 반환 (Vision writer 무수정, canned 미생성). pure resolve_coach_hook_bundle 헬퍼가 per-report 폴백 소유 (partial/None 무관 두 hook). schema-strip config 재사용 + retry. 두 모델 extra=forbid + drift 테스트. hook 가드가 모든 \\d reject (글로벌 비회귀). 객관성 reject-and-fallback. translation-only + fallback 게이트 GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 두 리포트 hook 부착 wiring (post-force-pattern window, resolve_coach_hook_bundle 소비) + _validate_coach_comment_hook (force scoped + body precheck) + pipeline wiring</name>
  <files>backend/functions/pipeline/app.py, backend/shared/python/sunity_shared/analysis/force_pattern.py, backend/shared/python/sunity_shared/analysis/body_normalizer.py, backend/shared/python/sunity_shared/firestore_admin.py, backend/tests/phase11/test_coach_hook_nested_array.py</files>
  <behavior>
    - valid flat coachCommentHook dict (list[str] 필드) → _validate_coach_comment_hook 통과 (force scoped + body precheck 둘 다)
    - coachCommentHook 안 openQuestionsForCoach 가 list[dict] → ValueError reject (iter-2 BLOCKER-1 — generic validator 가 허용하던 case 를 전용 validator 가 차단)
    - coachCommentHook 안 nested list (list[list]) → ValueError reject
    - coachCommentHook 안 unknown key (화이트리스트 외) → ValueError reject
    - force path: 위 4 case 가 _validate_force_pattern_inference 경유 검증
    - body path: 위 4 case 가 complete_analysis 의 body precheck 경유 검증 (generic validator 단독으로는 list[dict] 통과하므로 precheck 필수)
    - ForcePatternInference / BodyComparisonReport dataclass 에 coach_comment_hook = None default 필드 (field-order: 맨 끝)
    - hook 생성이 force_pattern_inference 생성 이후 / complete_analysis 이전에 일어남 (BLOCKER-2)
    - wiring = build_coach_hooks → resolve_coach_hook_bundle → dataclasses.replace (Task 2 는 폴백 정책 미보유 — helper 가 반환한 (force_hook, body_hook) tuple 을 그대로 소비, iter-3 HIGH-1)
  </behavior>
  <read_first>
    - backend/shared/python/sunity_shared/firestore_admin.py:45-101 (_validate_flat_dict_no_nested_array — list[dict] 를 _validate_dict_only_scalars 로 라우팅 = scalar-only dict 허용. 이게 iter-2 BLOCKER-1 의 root cause: hook list 필드가 list[dict] 여도 통과. 전용 validator 가 필요한 이유), :343-404 (_validate_force_pattern_inference — coachCommentHook 값은 dict 라 :371-374 dict 브랜치에서 먼저 raise 됨. 이 dict 브랜치 위에 화이트리스트 필요), :724-799 (complete_analysis — body 는 :780 generic validator 경유. body precheck 추가 site)
    - backend/shared/python/sunity_shared/analysis/force_pattern.py:228-262 (ForcePatternInference — warnings trailing default 뒤에 coach_comment_hook 추가)
    - backend/shared/python/sunity_shared/analysis/body_normalizer.py:861-883 (BodyComparisonReport — trailing default 뒤에 추가)
    - backend/functions/pipeline/app.py:2047 (body_comparison_report_dict 변환 site — 늦추거나 hook 부착 후 재변환), :2099-2117 (force_pattern_inference 생성 + high-score gate + force_pattern_inference_dict 변환 site — hook 부착은 이 직후), :1482-1502 (_dataclass_to_camel_case_dict — hook 직렬화), :2286-2296 (complete_analysis 호출 — force_pattern_inference_dict / body_comparison_report_dict 주입)
    - backend/shared/python/sunity_shared/gemini/coach_hook_writer.py (Task 1 의 GeminiCoachHookWriter.build_coach_hooks — bundle|None), backend/shared/python/sunity_shared/analysis/coach_hook_builder.py (Task 1 의 resolve_coach_hook_bundle — per-report 폴백 소유, pipeline 은 호출만)
    - backend/tests/phase11/test_coach_hook_nested_array.py (Wave 0 RED 게이트)
    - .planning/phases/11-coachcommenthook-gemini/11-PATTERNS.md (LANDMINE 섹션 + Pattern 4 — [SUPERSEDED] 노트 반영: generic 위임 아니라 전용 _validate_coach_comment_hook)
  </read_first>
  <action>
    `force_pattern.py` 의 ForcePatternInference 와 `body_normalizer.py` 의 BodyComparisonReport 각각에 `coach_comment_hook: CoachCommentHook | None = None` 을 모든 non-default 필드 뒤 맨 끝에 추가 (D-02, field-order rule). coach_hook.py 에서 import (이 모듈들은 coach_hook 만 import — 순환 안전, HIGH-3).

    **전용 hook validator (firestore_admin.py, iter-2 BLOCKER-1):** 신규 `def _validate_coach_comment_hook(payload, *, path: str) -> None` — generic `_validate_flat_dict_no_nested_array` 위임 **금지**(generic 은 list[dict] scalar-only 를 허용 → hook list[str] 계약 미달). 명시 화이트리스트:
    - payload 가 None → return (옵셔널 필드).
    - payload 가 dict 아님 → ValueError.
    - scalar-or-null 키: `autoFindingsSummary`, `coachComment`, `reviewedBy`, `sourceReport` — 값이 str/None 외면 ValueError.
    - list 키: `openQuestionsForCoach`, `suggestedCues` — 값이 list 여야 하고, 각 원소가 non-empty str 여야 함. list[dict]/list[list]/tuple/빈문자열 원소 → ValueError.
    - 화이트리스트 외 key → ValueError (unknown hook key reject).
    (camelCase 키 기준 — Firestore 직렬화 후 검증되므로.)

    **force scoped 브랜치 (firestore_admin.py):** `_validate_force_pattern_inference` 의 `isinstance(value, dict)` 브랜치(:371-374, 현재 ANY nested dict 에 raise) **위에** `if key == "coachCommentHook": _validate_coach_comment_hook(value, path=sub); continue` 추가. (generic validator 위임이 아니라 전용 validator 호출 — iter-2 BLOCKER-1.)

    **body precheck (firestore_admin.py complete_analysis):** body 는 `_validate_flat_dict_no_nested_array`(:780) 경유라 list[dict] hook 이 통과해버림. `if body_comparison_report is not None:` 블록에서 generic validator **이전 또는 직후** 에 precheck 추가: `hook = body_comparison_report.get("coachCommentHook"); if hook is not None: _validate_coach_comment_hook(hook, path="bodyComparisonReport.coachCommentHook")`. 그 다음 기존 generic validator 가 나머지(findings/warnings)를 검증. (force 와 body 둘 다 동일 strict 게이트 통과.)

    **pipeline wiring (single call, post-force-pattern window, BLOCKER-2 — iter-3 HIGH-1 wiring only):** hook 생성을 **force_pattern_inference 생성 + high-score gate(:2108-2114) 직후, complete_analysis(:2286) 이전**으로 배치한다 (기존 :1945 dual-track coach call site 아님). 거기서:
    - `bundle = GeminiCoachHookWriter().build_coach_hooks(force_findings=force_pattern_inference.findings, body_findings=body_comparison_report.findings)` 를 **1회** 호출(single call) — bundle 은 CoachHookBundle 또는 None.
    - `force_hook, body_hook = resolve_coach_hook_bundle(bundle, force_findings=force_pattern_inference.findings, body_findings=body_comparison_report.findings)` 로 per-report 폴백 해소 — **폴백 정책은 helper 소유, Task 2 는 tuple 소비만** (iter-3 HIGH-1). 두 hook 모두 non-None 보장 (COACH-01 SC#2).
    - `dataclasses.replace(force_pattern_inference, coach_comment_hook=force_hook)` + `dataclasses.replace(body_comparison_report, coach_comment_hook=body_hook)`.
    **dict 변환 순서:** `force_pattern_inference_dict`(:2115) / `body_comparison_report_dict`(:2047) 를 hook 부착 **이후**에 `_dataclass_to_camel_case_dict` 로 (재)변환. complete_analysis(:2286) 의 dict 에 coachCommentHook 포함. **coach_details(joint writer) 변수는 무수정 — coach_hooks 는 별도 변수**(BLOCKER-3). Gemini/키 미설정 또는 가드 reject 시에도 (bundle=None → helper canned) hook 부착(분석 절대 실패 안 함, D-08). 이 task 로 test_coach_hook_nested_array.py GREEN.
  </action>
  <acceptance_criteria>
    - `cd backend && PYTHONPATH=shared/python python -m pytest tests/phase11/test_coach_hook_nested_array.py -x -q` → PASS (force scoped + body precheck 양쪽, 4 reject/pass case 씩)
    - `grep -n "def _validate_coach_comment_hook" backend/shared/python/sunity_shared/firestore_admin.py` → 1 hit (전용 validator 신설, iter-2 BLOCKER-1)
    - 전용 validator 가 generic 위임 아님: `_validate_coach_comment_hook` 본문에 `_validate_flat_dict_no_nested_array` 호출 0 — 자체 화이트리스트 (list[dict] reject 보장)
    - force 브랜치 wiring: `grep -n "coachCommentHook" backend/shared/python/sunity_shared/firestore_admin.py` → _validate_force_pattern_inference 안 브랜치 + complete_analysis body precheck 둘 다 hit
    - body precheck: complete_analysis 의 `if body_comparison_report is not None:` 블록에 `_validate_coach_comment_hook(...)` 호출 1 hit (generic 단독 우회 차단)
    - test_coach_hook_nested_array.py 가 list[dict] hook → ValueError 를 **force path AND body path 둘 다** assert (iter-2 BLOCKER-1)
    - `grep -n "coach_comment_hook" backend/shared/python/sunity_shared/analysis/force_pattern.py backend/shared/python/sunity_shared/analysis/body_normalizer.py` → 각 1 hit (필드 추가)
    - **wiring = call → resolve → replace (iter-3 HIGH-1):** `grep -n "build_coach_hooks\|resolve_coach_hook_bundle\|coach_comment_hook" backend/functions/pipeline/app.py` → 세 패턴 모두 hit. pipeline 에 자체 per-report 폴백 분기(build_canned_hook 직접 호출 또는 bundle.force_pattern_inference 분기 if/else) 0 — `resolve_coach_hook_bundle` 한 줄로 위임. `grep -c "build_canned_hook" backend/functions/pipeline/app.py` → 0 (helper 만 호출)
    - hook 생성 site 가 force_pattern_inference 이후: pipeline 에서 `build_coach_hooks`/`resolve_coach_hook_bundle` 호출 줄번호 > force_pattern_inference 생성 줄번호(:2099대) AND < complete_analysis 호출 줄번호(:2286대) (SUMMARY 에 줄번호 증명 기록, BLOCKER-2)
    - coach_details 무오염: `grep -n "coach_details" backend/functions/pipeline/app.py` 에 hook 키 주입 0 — coach_hooks 별도 변수 (BLOCKER-3)
    - 단일 호출: app.py 에 `build_coach_hooks` 호출이 분석 path 당 정확히 1회 (SUMMARY 기록), report 당 별도 Gemini invoke 0
    - `cd backend && PYTHONPATH=shared/python python -m pytest tests/phase11 -x -q` 전체 GREEN
  </acceptance_criteria>
  <verify>
    <automated>cd backend && PYTHONPATH=shared/python python -m pytest tests/phase11 -x -q && cd ../app && npm run typecheck</automated>
  </verify>
  <done>두 리포트에 hook 부착 (resolve_coach_hook_bundle tuple 소비 — Task 2 폴백 정책 미보유) + Firestore flat 저장. _validate_coach_comment_hook 전용 strict validator (force scoped + body precheck, list[dict] reject). hook 생성 site = post-force-pattern/pre-complete window. single call. coach_details 무오염. phase11 게이트 GREEN + tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gemini LLM output → hook 텍스트 | untrusted — 수치/판정/도(degree)/%/임의 숫자 mint 가능 (number-free 잠금) |
| Gemini LLM output → CoachHookBundle parse | extra=forbid — stray top-level key (typo) ValidationError (schema drift 조기 차단) |
| hook dict → Firestore validator | list[str]-only gate (force=scoped 브랜치, body=complete_analysis precheck) — 둘 다 전용 _validate_coach_comment_hook |
| 키 미설정/가드 reject → 분석 완료 | resolve_coach_hook_bundle (pure helper) 가 분석을 절대 실패시키지 않아야 함 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02 | Tampering/Repudiation | GeminiCoachHookWriter (LLM 수치/판정/임의 숫자) | mitigate | 프롬프트 객관성 절(number-free) + _enforce_no_reject_patterns(글로벌) + hook-scoped \\d 가드(모든 숫자 reject, iter-3 HIGH-2) reject-and-fallback (런타임 sanitize 금지, D-05) + translation-only test GREEN. 글로벌 가드 미오염 (scene_finder/reference_extractor 회귀 0, iter-2 BLOCKER-2) |
| T-11-03 | DoS | malformed/oversized hook (list[dict] 포함) → Firestore write/app render | mitigate | _validate_coach_comment_hook (force scoped + body precheck) 가 list[dict]/nested/unknown-key reject → fail_analysis. generic validator 단독 우회 차단 (iter-2 BLOCKER-1). 단 valid hook 은 절대 분석 실패 안 시킴 (D-08) |
| T-11-04 | Elevation of Privilege | v2 coach-write 필드(coachComment/reviewedBy) 조기 노출 | mitigate | v1 항상 None, write endpoint 미추가 (COACH-01 "UI/입력은 v2") |
| T-11-05 | Information Disclosure | Gemini 키 로그 유출 | mitigate | Parameter Store only, lazy import (api_key_loader), 로그에 키 미출력 (기존 패턴 보존) |
| T-11-06 | Tampering | hook 키가 joint-card success 집계 오염 (BLOCKER-3) | mitigate | coach_hooks 별도 변수 + 별도 writer (joint CoachPayload dict 미혼합) |
| T-11-07 | Tampering | partial bundle → 한쪽 report hook 누락 (COACH-01 SC#2 위반) | mitigate | resolve_coach_hook_bundle pure helper — bundle None/partial 무관 두 report 모두 canned 또는 LLM hook 부착 (iter-3 HIGH-1) |
| T-11-08 | Tampering | Gemini typo top-level key → schema 조용한 drop | mitigate | CoachHookBundle/CoachCommentHookPayload 둘 다 extra=forbid → ValidationError (LLM 경계 drift 차단, iter-3 MEDIUM-1) |
| T-11-SC | Tampering | npm/pip 설치 | accept | Phase 11 신규 패키지 0 — 설치 task 없음 |
</threat_model>

<verification>
- `cd backend && PYTHONPATH=shared/python python -m pytest tests/phase11 -x -q` 게이트 GREEN
- `cd backend && PYTHONPATH=shared/python python -m pytest -q` 전체 회귀 0 (phase06~17 기존 suite — 특히 Vision writer test_coach_writer_v2.py + scene_finder/reference_extractor 가드 test 무변경 GREEN, iter-2 BLOCKER-2)
- `cd app && npm run typecheck` clean
- Vision GeminiCoachWriter.write() 무수정 (git diff 0줄)
- 글로벌 _SCORE_PATTERNS 미오염 — "30도"/"50%" 기본 가드 PASS (iter-2 BLOCKER-2)
- hook-scoped 가드가 모든 \\d reject — "3초"/"180"/"23도"/"15%" (iter-3 HIGH-2)
- build_coach_hooks 가 bundle|None only + resolve_coach_hook_bundle pure helper 가 폴백 소유 (iter-3 HIGH-1)
- CoachHookBundle + CoachCommentHookPayload 둘 다 extra=forbid + drift test (iter-3 MEDIUM-1)
- _validate_coach_comment_hook 전용 strict (force scoped + body precheck) — list[dict] reject (iter-2 BLOCKER-1)
- hook writer 가 _strip_unsupported_schema_keys config 재사용 (iter-2 HIGH-1)
- single hook call + post-force-pattern window 보존
- 백엔드 shared-layer 변경 — 배포 시 `sam build --use-container` 필요 (본 phase 는 pre-deploy, test 로컬 실행. 배포는 Phase 15 실증)
</verification>

<success_criteria>
- single call 로 두 리포트 hook 산출 (D-03 correction, 별도 text-only writer)
- 기존 Vision writer 무수정 (BLOCKER-1)
- hook 생성 site = post-force-pattern / pre-complete (BLOCKER-2)
- coach_details / coach_hooks 별도 변수 (BLOCKER-3)
- 번역만 강제 — 모든 \\d 는 hook-scoped 가드, 글로벌 비회귀 (ROADMAP SC#3, D-04/D-05, iter-2 BLOCKER-2 + iter-3 HIGH-2)
- hook writer schema-strip config 재사용 (iter-2 HIGH-1)
- build_coach_hooks bundle|None only + pure resolve_coach_hook_bundle 폴백 소유 (iter-3 HIGH-1)
- CoachHookBundle + CoachCommentHookPayload 둘 다 extra=forbid (iter-3 MEDIUM-1)
- api_key_loader/None seam fallback → canned hook 으로 분석 완료 (ROADMAP SC#5, D-08/WARNING-2)
- _validate_coach_comment_hook 전용 strict validator (force scoped + body precheck, iter-2 BLOCKER-1)
- 회귀 0 + tsc clean
- FEED-02 = 기존 dual-coach 로 Complete, 재작업 0
</success_criteria>

<output>
Create `.planning/phases/11-coachcommenthook-gemini/11-01-SUMMARY.md` when done
</output>
