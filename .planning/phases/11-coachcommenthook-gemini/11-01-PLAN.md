---
phase: 11-coachcommenthook-gemini
plan: 01
type: execute
wave: 1
depends_on: ["11-00"]
files_modified:
  - backend/shared/python/sunity_shared/gemini/coach_writer_v2.py
  - backend/shared/python/sunity_shared/analysis/coach_hook.py
  - backend/functions/pipeline/app.py
  - backend/shared/python/sunity_shared/analysis/force_pattern.py
  - backend/shared/python/sunity_shared/analysis/body_normalizer.py
  - backend/shared/python/sunity_shared/firestore_admin.py
  - backend/tests/phase11/test_coach_hook_translation_only.py
  - backend/tests/phase11/test_coach_hook_fallback.py
  - backend/tests/phase11/test_coach_hook_nested_array.py
autonomous: true
requirements: [COACH-01, FEED-02]
must_haves:
  truths:
    - "단일 LLM 호출(기존 dual-track coach call)이 hook 텍스트 필드를 함께 산출한다 — 두번째 round-trip 없음 (D-03, Pitfall 5)"
    - "두 리포트(ForcePatternInference, BodyComparisonReport) 각각에 coachCommentHook 이 부착되어 Firestore 에 flat 저장된다 (COACH-01 SC#2)"
    - "Gemini 가 점수/좌표/판정 생성 시 reject-and-fallback → canned hook (D-04/D-05). 런타임 sanitize 없음"
    - "Cerebras/Gemini 키 미설정 시 canned hook 으로 분석이 완료된다 (status done, D-08/ROADMAP SC#5)"
    - "ForcePatternInference scoped validator 가 coachCommentHook 키를 허용하도록 확장된다 (landmine — 미확장 시 server_error)"
  artifacts:
    - path: "backend/shared/python/sunity_shared/gemini/coach_writer_v2.py"
      provides: "hook 텍스트 산출 (text-only, 기존 _build_coach_context 공유)"
    - path: "backend/shared/python/sunity_shared/analysis/coach_hook.py"
      provides: "build_canned_hook 폴백 빌더 + guardrails reject 후 canned 진입점"
    - path: "backend/shared/python/sunity_shared/firestore_admin.py"
      provides: "_validate_force_pattern_inference 에 coachCommentHook 화이트리스트 브랜치"
  key_links:
    - from: "backend/functions/pipeline/app.py"
      to: "backend/shared/python/sunity_shared/gemini/coach_writer_v2.py"
      via: "기존 dual-track coach call (app.py:1945-2026) 출력 스키마 확장"
      pattern: "_build_coach_context|coachCommentHook"
    - from: "backend/functions/pipeline/app.py"
      to: "backend/shared/python/sunity_shared/firestore_admin.py"
      via: "complete_analysis(force_pattern_inference=, body_comparison_report=) hook 포함 dict"
      pattern: "coachCommentHook"
---

<objective>
백엔드 분석 파이프라인이 1회 분석에서 (기존 dual-track coach 호출을 확장하여) 각 리포트의 `CoachCommentHook` 텍스트 필드를 산출하고, 두 리포트에 hook 을 부착하고, Firestore scoped validator 를 확장하여 nested-array 게이트를 통과시킨다. 이것이 Phase 11 의 thin end-to-end 백엔드 슬라이스 — Wave 0 의 RED 테스트를 GREEN 으로 만든다.

**LOCKED DESIGN FORK (RESEARCH Open Question 1):** 단일 LLM 호출 — 기존 dual-track coach call (app.py:1945-2026) 의 출력 스키마를 확장해 hook 필드를 함께 받는다. 두번째 text-only Gemini round-trip 은 채택 안 함. 근거: D-03 "1회 분석의 기존 LLM 호출 경로에 통합" 명시 + Pitfall 5 (두번째 호출 = latency/cost 회귀) + 호출 site 이미 존재 (`_build_coach_context` 공유). 두 옵션 모두 D-03 honor 하나 단일 호출이 비용/지연 우위.

Purpose: COACH-01 SC#2 (모든 리포트에 부착) + ROADMAP SC#3 (번역만 강제) + SC#5 (fallback 완료). Vision/영상 호출 없음 (Phase 17 경계).
Output: hook 생성 메서드, canned fallback 빌더, pipeline wiring (단일 호출), 두 리포트 dataclass 필드, scoped validator 확장. phase11 3 게이트 GREEN.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-coachcommenthook-gemini/11-CONTEXT.md
@.planning/phases/11-coachcommenthook-gemini/11-RESEARCH.md
@.planning/phases/11-coachcommenthook-gemini/11-PATTERNS.md
@.planning/phases/11-coachcommenthook-gemini/11-00-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: hook 텍스트 생성 (text-only, 단일 호출) + canned fallback + 런타임 객관성 가드</name>
  <files>backend/shared/python/sunity_shared/gemini/coach_writer_v2.py, backend/shared/python/sunity_shared/analysis/coach_hook.py, backend/tests/phase11/test_coach_hook_translation_only.py, backend/tests/phase11/test_coach_hook_fallback.py</files>
  <behavior>
    - golden finding → 생성된 hook 의 텍스트 필드(autoFindingsSummary/openQuestionsForCoach/suggestedCues) 에 점수/좌표/판정 패턴 0 (D-04/D-05)
    - mock LLM 이 "87점"/"훌륭" 출력 → _enforce_no_reject_patterns ValueError → build_canned_hook 반환 (reject-and-fallback, sanitize 아님, D-05)
    - _client is None (키 미설정) → build_canned_hook 반환 (D-08) — 빈 hook 아님, canned 텍스트 채움
    - canned hook 도 forbidden-phrase 0 (canned 카피 자체가 객관적)
  </behavior>
  <read_first>
    - backend/shared/python/sunity_shared/gemini/coach_writer_v2.py:139-200 (_COACH_SYSTEM_INSTRUCTION 객관성 절 + resolve_model 경유 + write 시그니처), :350-420 (GeminiCoachWriter class, write(context) B3)
    - backend/shared/python/sunity_shared/gemini/guardrails.py:47-110 (_enforce_no_reject_patterns — allow_coords 기본 False 그대로)
    - backend/shared/python/sunity_shared/analysis/coach_writer.py:145-185 (_client is None → {} graceful no-op + gpt-oss-120b Cerebras fallback model)
    - backend/shared/python/sunity_shared/analysis/coach_hook.py (Wave 0 의 CoachCommentHook 정의 — build_canned_hook 추가 대상)
    - backend/tests/phase11/test_coach_hook_translation_only.py + test_coach_hook_fallback.py (Wave 0 RED 게이트 — 이 task 가 GREEN 으로)
    - .planning/phases/11-coachcommenthook-gemini/11-RESEARCH.md (Pattern 3 + Pitfall 4 + Code Examples "Reuse the objectivity runtime guard")
  </read_first>
  <action>
    `coach_hook.py` 에 `build_canned_hook(findings, *, source_report: str) -> CoachCommentHook` 추가 — finding 의 기존 텍스트 필드(예: interpretation, canned KO)만 조합해 객관적 한국어 hook 생성 (LLM 미사용 경로, D-08). autoFindingsSummary = 1 짧은 문단, openQuestionsForCoach = 1-3 항목 ("왼쪽 무릎 굽힘이 의도된 동작인지 강사와 확인" 형태), suggestedCues = 2-4 항목. 수치/판정 금지 — guardrails 정렬. coach_comment/reviewed_by = None (v1).

    `coach_writer_v2.py` 의 GeminiCoachWriter 에 hook 텍스트 산출 경로 추가 — **별도 두번째 호출 금지**. 기존 `write(context)` 의 출력 스키마(system prompt + response schema)를 확장해 hook 텍스트 필드를 함께 반환하거나, 같은 client/context 를 공유하는 메서드를 기존 호출 path 안에서 호출한다 (D-03, 단일 round-trip). `_COACH_SYSTEM_INSTRUCTION` 의 객관성 절("절대 금지: 점수/등급/x=/y=/좌표/잘했다/훌륭/완벽/양호") 재사용 — hook 프롬프트도 "finding 자연어 풀이만, 수치·좌표·점수·판정 출력 금지". model string 은 resolve_model 경유 (raw "gemini-3.1-pro-preview" 하드코딩 금지). `_client is None` → canned 경로 진입 신호 반환.

    런타임 가드: LLM raw 텍스트에 `_enforce_no_reject_patterns(raw, context="coach_hook")` (allow_coords=False 기본) 적용 → ValueError 시 `log.warning` + build_canned_hook 반환 (D-08). **regex-strip-and-emit 금지 (D-05)** — reject 후 canned 로 대체만. 이 task 로 test_coach_hook_translation_only.py + test_coach_hook_fallback.py 가 GREEN.
  </action>
  <acceptance_criteria>
    - `cd backend && python -m pytest tests/phase11/test_coach_hook_translation_only.py tests/phase11/test_coach_hook_fallback.py -x -q` → PASS (Wave 0 RED → GREEN)
    - `grep -n "_enforce_no_reject_patterns" backend/shared/python/sunity_shared/gemini/coach_writer_v2.py` → 1+ hit (런타임 가드 wiring)
    - `grep -n "def build_canned_hook" backend/shared/python/sunity_shared/analysis/coach_hook.py` → 1 hit
    - coach_writer_v2.py 에 raw model 문자열 하드코딩 0: `grep -c "gemini-3" backend/shared/python/sunity_shared/gemini/coach_writer_v2.py | grep -v resolve_model` — resolve_model 경유만
    - sanitize-and-emit 패턴 부재: hook 생성 경로에 `re.sub`/`.replace(` 로 LLM 출력 편집하는 코드 0 (reject-and-fallback only, D-05)
    - 두번째 Gemini round-trip 부재: hook 생성이 기존 `_build_coach_context` 결과를 공유하는 단일 경로 (SUMMARY 에 단일 호출 증명 기록)
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -m pytest tests/phase11/test_coach_hook_translation_only.py tests/phase11/test_coach_hook_fallback.py -x -q</automated>
  </verify>
  <done>hook 텍스트가 단일 호출로 산출됨. 객관성 가드 reject-and-fallback. 키 미설정 시 canned hook 으로 완료. translation-only + fallback 게이트 GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 두 리포트 hook 부착 + scoped validator 확장 (LANDMINE) + pipeline wiring</name>
  <files>backend/functions/pipeline/app.py, backend/shared/python/sunity_shared/analysis/force_pattern.py, backend/shared/python/sunity_shared/analysis/body_normalizer.py, backend/shared/python/sunity_shared/firestore_admin.py, backend/tests/phase11/test_coach_hook_nested_array.py</files>
  <behavior>
    - valid flat coachCommentHook dict (list[str] 필드) → _validate_force_pattern_inference 통과 (Pitfall 1)
    - coachCommentHook 안 list[dict]/nested list → ValueError reject (Pitfall 2)
    - BodyComparisonReport generic validator path 도 같은 flat hook 통과 (A2 — 양쪽 검증)
    - ForcePatternInference / BodyComparisonReport dataclass 에 coach_comment_hook = None default 필드 (field-order: 맨 끝)
  </behavior>
  <read_first>
    - backend/shared/python/sunity_shared/firestore_admin.py:343-404 (_validate_force_pattern_inference — dict 브랜치 :371-374 + 터미널 raise :398-404 둘 다 확장 대상. coachCommentHook 은 dict 라 :371 dict 브랜치에서 먼저 raise 됨 — 양쪽에 화이트리스트 필요), :724-799 (complete_analysis + body 가 generic :780 통과)
    - backend/shared/python/sunity_shared/analysis/force_pattern.py:228-260 (ForcePatternInference — warnings trailing default 뒤에 coach_comment_hook 추가)
    - backend/shared/python/sunity_shared/analysis/body_normalizer.py:861-883 (BodyComparisonReport — trailing default 뒤에 추가)
    - backend/functions/pipeline/app.py:1945-2026 (dual-track coach call site — hook 생성을 여기서 1회 수행), :2099 (force_pattern_inference 빌드 site), :1776+ (body_comparison 빌드 site), :1482-1502 (_dataclass_to_camel_case_dict — hook 직렬화), :2286-2296 (complete_analysis 호출 — force_pattern_inference_dict / body_comparison_report_dict 주입)
    - backend/tests/phase11/test_coach_hook_nested_array.py (Wave 0 RED 게이트 — 이 task 가 GREEN 으로)
    - .planning/phases/11-coachcommenthook-gemini/11-PATTERNS.md (LANDMINE 섹션 + Pattern 4)
  </read_first>
  <action>
    `force_pattern.py` 의 ForcePatternInference 와 `body_normalizer.py` 의 BodyComparisonReport 각각에 `coach_comment_hook: CoachCommentHook | None = None` 을 모든 non-default 필드 뒤 맨 끝에 추가 (D-02, field-order rule). coach_hook.py 에서 import.

    **LANDMINE 확장 (firestore_admin.py):** `_validate_force_pattern_inference` 에 `coachCommentHook` 화이트리스트 브랜치 추가. coachCommentHook 값은 `dict` 이므로 `isinstance(value, dict)` 브랜치(:371-374, 현재 ANY nested dict 에 raise)에서 먼저 걸린다 — 이 dict 브랜치 위에 `if key == "coachCommentHook": _validate_flat_dict_no_nested_array(value, path=sub); continue` 를 추가하고, 터미널 raise(:398-404) 도 우회되도록 브랜치 순서 정합. body_comparison 은 generic `_validate_flat_dict_no_nested_array` (:780) 경유라 변경 불필요 가능성 높으나 **첫 write test 로 검증** (A2). 미확장 시 coachCommentHook 부착이 `server_error` 로 분석 실패 — 이 확장이 본 plan 의 독립 acceptance criterion.

    **pipeline wiring (단일 호출, D-03):** app.py:1945-2026 의 기존 dual-track coach call 경로에서 hook 을 1회 생성 (공유 `_build_coach_context` 사용) → 두 리포트에 per-report hook 인스턴스 부착 (source_report scalar 로 provenance 구분, D-02). force_pattern_inference 빌드(:2099) / body_comparison 빌드(:1776+) 후 `dataclasses.replace` 또는 빌드 시점 주입으로 coach_comment_hook 세팅. `_dataclass_to_camel_case_dict` 가 hook 을 camelCase dict 로 직렬화 (수정 불필요 — 기존 dataclass 케이스 처리). complete_analysis(:2286) 의 force_pattern_inference_dict / body_comparison_report_dict 에 coachCommentHook 포함. Gemini/Cerebras 키 미설정 또는 가드 reject 시 build_canned_hook 부착 (분석 절대 실패 안 함, D-08). 이 task 로 test_coach_hook_nested_array.py GREEN.
  </action>
  <acceptance_criteria>
    - `cd backend && python -m pytest tests/phase11/test_coach_hook_nested_array.py -x -q` → PASS (Pitfall 1+2, 양쪽 report path)
    - `grep -n "coachCommentHook" backend/shared/python/sunity_shared/firestore_admin.py` → _validate_force_pattern_inference 안 화이트리스트 브랜치 1+ hit
    - `grep -n "coach_comment_hook" backend/shared/python/sunity_shared/analysis/force_pattern.py backend/shared/python/sunity_shared/analysis/body_normalizer.py` → 각 1 hit (필드 추가)
    - `grep -n "coach_comment_hook\|coachCommentHook\|build_canned_hook" backend/functions/pipeline/app.py` → wiring + 부착 hit
    - scoped validator 확장 검증: list[dict] hook → ValueError (test 안 명시), valid flat hook → 통과
    - 단일 호출 보존: app.py 에 hook 전용 두번째 `_call_coach_writer_with_retry` 또는 독립 Gemini invoke 0 (기존 call site 재사용)
    - `cd backend && python -m pytest tests/phase11 -x -q` 전체 GREEN (3 게이트)
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -m pytest tests/phase11 -x -q && cd ../app && npm run typecheck</automated>
  </verify>
  <done>두 리포트에 hook 부착 + Firestore flat 저장. scoped validator 확장(landmine 해소). 단일 호출 유지. phase11 3 게이트 GREEN + tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gemini LLM output → hook 텍스트 | untrusted — 수치/판정 mint 가능 |
| hook dict → Firestore scoped validator | nested-array gate (force=scoped, body=generic) |
| 키 미설정/가드 reject → 분석 완료 | fallback 경로가 분석을 절대 실패시키지 않아야 함 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02 | Tampering/Repudiation | coach_writer_v2 hook 생성 (LLM 수치/판정) | mitigate | 프롬프트 객관성 절 + _enforce_no_reject_patterns reject-and-fallback (런타임 sanitize 금지, D-05) + translation-only test GREEN |
| T-11-03 | DoS | malformed/oversized hook → Firestore write/app render | mitigate | scoped validator(force) + generic validator(body) 가 bad shape reject → fail_analysis. 단 valid hook 은 절대 분석 실패 안 시킴 (D-08) |
| T-11-04 | Elevation of Privilege | v2 coach-write 필드(coachComment/reviewedBy) 조기 노출 | mitigate | v1 항상 None, write endpoint 미추가 (COACH-01 "UI/입력은 v2") |
| T-11-05 | Information Disclosure | Gemini/Cerebras 키 로그 유출 | mitigate | Parameter Store only, lazy import, 로그에 키 미출력 (기존 패턴 보존) |
| T-11-SC | Tampering | npm/pip 설치 | accept | Phase 11 신규 패키지 0 — 설치 task 없음 |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/phase11 -x -q` 3 게이트 GREEN
- `cd backend && python -m pytest -q` 전체 회귀 0 (phase06~13 기존 suite)
- `cd app && npm run typecheck` clean
- scoped validator 확장 확인 (coachCommentHook 화이트리스트 브랜치)
- 단일 LLM 호출 보존 (두번째 round-trip 부재)
- 백엔드 shared-layer 변경 — 배포 시 `sam build --use-container` 필요 (본 phase 는 pre-deploy, test 로컬 실행. 배포는 Phase 15 실증)
</verification>

<success_criteria>
- 단일 호출로 hook 텍스트 산출 (D-03, Pitfall 5 회피)
- 두 리포트에 coachCommentHook 부착 + Firestore flat 저장 (COACH-01 SC#2)
- 번역만 강제 — reject-and-fallback (ROADMAP SC#3, D-04/D-05)
- 키 미설정 시 canned hook 으로 분석 완료 (ROADMAP SC#5, D-08)
- scoped validator 확장 (landmine 해소 — 독립 acceptance criterion)
- 회귀 0 + tsc clean
- FEED-02 = 기존 dual-coach 로 Complete, 재작업 0
</success_criteria>

<output>
Create `.planning/phases/11-coachcommenthook-gemini/11-01-SUMMARY.md` when done
</output>
