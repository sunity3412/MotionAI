---
phase: 11-coachcommenthook-gemini
plan: 01
type: execute
wave: 1
depends_on: ["11-00"]
files_modified:
  - backend/shared/python/sunity_shared/gemini/schemas.py
  - backend/shared/python/sunity_shared/gemini/guardrails.py
  - backend/shared/python/sunity_shared/gemini/coach_hook_writer.py
  - backend/shared/python/sunity_shared/analysis/coach_hook_builder.py
  - backend/functions/pipeline/app.py
  - backend/shared/python/sunity_shared/analysis/force_pattern.py
  - backend/shared/python/sunity_shared/analysis/body_normalizer.py
  - backend/shared/python/sunity_shared/firestore_admin.py
  - backend/tests/phase11/test_coach_hook_translation_only.py
  - backend/tests/phase11/test_coach_hook_fallback.py
  - backend/tests/phase11/test_coach_hook_nested_array.py
autonomous: true
requirements: [COACH-01, FEED-02]
must_haves:
  truths:
    - "신규 text-only GeminiCoachHookWriter 가 1회 분석당 hook 1회 호출로 두 리포트 hook 을 함께 산출한다 — 기존 Vision GeminiCoachWriter.write() 무수정, videoPath/GeminiVisionCall 미접촉 (D-03 correction, BLOCKER-1)"
    - "hook 생성이 force_pattern_inference 생성(:2099-2115) 이후 / complete_analysis(:2286) 이전에 일어난다 — ForcePatternInference.findings 를 hook 이 본다 (BLOCKER-2)"
    - "coach_details(joint writer) 와 coach_hooks(신규 writer) 가 별도 pipeline 변수 — joint-card success/fallback 집계 오염 0 (BLOCKER-3)"
    - "Gemini 가 점수/좌표/판정/도(degree)/% 생성 시 reject-and-fallback → canned hook. 런타임 sanitize 없음 (D-04/D-05, HIGH-1)"
    - "api_key_loader 실패 또는 hook 호출 None 반환 시 canned hook 으로 분석이 완료된다 (status done, D-08/SC#5, WARNING-2)"
    - "ForcePatternInference scoped validator 가 coachCommentHook 키를 허용하도록 확장된다 (landmine)"
  artifacts:
    - path: "backend/shared/python/sunity_shared/gemini/coach_hook_writer.py"
      provides: "GeminiCoachHookWriter — text-only Gemini hook 생성 (resolve_model('B'), CoachHookBundle schema, _enforce_no_reject_patterns)"
      contains: "class GeminiCoachHookWriter"
    - path: "backend/shared/python/sunity_shared/gemini/schemas.py"
      provides: "CoachHookBundle + CoachCommentHookPayload Pydantic schema (두 optional report hook)"
    - path: "backend/shared/python/sunity_shared/analysis/coach_hook_builder.py"
      provides: "build_canned_hook 폴백 빌더 (report findings → 객관 한국어 hook)"
    - path: "backend/shared/python/sunity_shared/firestore_admin.py"
      provides: "_validate_force_pattern_inference 에 coachCommentHook 화이트리스트 브랜치"
  key_links:
    - from: "backend/functions/pipeline/app.py"
      to: "backend/shared/python/sunity_shared/gemini/coach_hook_writer.py"
      via: "force_pattern_inference 생성 후 hook 1회 호출 (:2115~:2286 window)"
      pattern: "GeminiCoachHookWriter|build_coach_hooks"
    - from: "backend/functions/pipeline/app.py"
      to: "backend/shared/python/sunity_shared/firestore_admin.py"
      via: "complete_analysis(force_pattern_inference=, body_comparison_report=) hook 포함 dict"
      pattern: "coachCommentHook"
---

<objective>
백엔드 분석 파이프라인이 1회 분석에서 **신규 text-only `GeminiCoachHookWriter`** 를 1회 호출해 두 리포트(ForcePatternInference, BodyComparisonReport)의 `CoachCommentHook` 텍스트를 함께 산출하고, 두 리포트에 hook 을 부착하고, Firestore scoped validator 를 확장하여 nested-array 게이트를 통과시킨다. 이것이 Phase 11 의 thin end-to-end 백엔드 슬라이스 — Wave 0 의 RED 테스트를 GREEN 으로 만든다.

**LOCKED DESIGN (D-03 [2026-06-16 review correction], BLOCKER-1):** 기존 Vision `GeminiCoachWriter.write()` (coach_writer_v2.py, joint-card / videoPath 의존)는 **무수정**. 대신 **별도 text-only** `coach_hook_writer.py::GeminiCoachHookWriter` 를 신설한다 — `resolve_model('B')` + hook 전용 Pydantic schema(`CoachHookBundle`) + `_enforce_no_reject_patterns` 재사용, `GeminiVisionCall`/`videoPath` 미접촉. "single call" = 1회 분석당 hook 호출 1회로 두 리포트 hook 을 함께 묶는다(report 당 1회 아님, joint writer dict 재사용 아님). 근거: 기존 writer 는 `coach_writer_v2.py:1` Vision 전용 + `write()` 가 videoPath 없으면 `video_path_missing` 폴백(:414-417) → 재사용 시 Vision 독립 잠금 위반 또는 Phase 11/17 경계 침범 + strict `CoachPayload`(joints-only) joint 집계 오염(BLOCKER-3).

Purpose: COACH-01 SC#2 (모든 리포트에 부착) + ROADMAP SC#3 (번역만 강제, 도/% 포함) + SC#5 (fallback 완료). Vision/영상 호출 없음 (Phase 17 경계).
Output: 신규 hook writer + hook schema + 도(degree) reject regex + canned fallback 빌더 + pipeline wiring(post-force-pattern window) + 두 리포트 dataclass 필드 + scoped validator 확장. phase11 4 게이트 GREEN.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-coachcommenthook-gemini/11-CONTEXT.md
@.planning/phases/11-coachcommenthook-gemini/11-REVIEWS.md
@.planning/phases/11-coachcommenthook-gemini/11-RESEARCH.md
@.planning/phases/11-coachcommenthook-gemini/11-PATTERNS.md
@.planning/phases/11-coachcommenthook-gemini/11-00-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: text-only GeminiCoachHookWriter + CoachHookBundle schema + 도(degree) 가드 + canned fallback</name>
  <files>backend/shared/python/sunity_shared/gemini/coach_hook_writer.py, backend/shared/python/sunity_shared/gemini/schemas.py, backend/shared/python/sunity_shared/gemini/guardrails.py, backend/shared/python/sunity_shared/analysis/coach_hook_builder.py, backend/tests/phase11/test_coach_hook_translation_only.py, backend/tests/phase11/test_coach_hook_fallback.py</files>
  <behavior>
    - golden ForcePatternFinding/BodyComparisonFinding → 생성된 hook 의 텍스트 필드(autoFindingsSummary/openQuestionsForCoach/suggestedCues) 에 점수/좌표/판정/도(°/도/deg)/% 패턴 0 (D-04/D-05/HIGH-1)
    - _enforce_no_reject_patterns("무릎이 23도 차이", context="coach_hook") → ValueError (도 regex 추가 후, HIGH-1)
    - mock LLM 이 "87점"/"훌륭"/"23도" 출력 → ValueError → build_canned_hook 반환 (reject-and-fallback, sanitize 아님, D-05)
    - api_key_loader 실패 또는 hook 호출 None 반환 → build_canned_hook 반환 (D-08, WARNING-2) — 빈 hook 아님, canned 텍스트 채움. _client private 필드 검사 안 함
    - canned hook 도 forbidden-phrase + 도/% 0 (canned 카피 자체가 객관·number-free)
  </behavior>
  <read_first>
    - backend/shared/python/sunity_shared/gemini/coach_writer_v2.py:1-34 (Vision 전용 문서 — 이 writer 는 **읽기만**, 무수정 확인용), :363-386 (__init__ = _api_key_loader/_model_override, _client 필드 없음 — WARNING-2 근거)
    - backend/shared/python/sunity_shared/gemini/client.py:64-72 (_default_api_key_loader — 신규 writer 의 api_key_loader seam), :167-226 (GeminiVisionCall.call 이 files.upload(video_path) 강제 — 재사용 불가 증명, 신규 writer 는 client.models.generate_content text-only path), :268-292 (_generate_config response_schema 박제 helper precedent)
    - backend/shared/python/sunity_shared/gemini/config.py:56-87 (resolve_model('B') — region B = 코칭, raw model string 하드코딩 금지)
    - backend/shared/python/sunity_shared/gemini/schemas.py:121-153 (CoachPayload extra=forbid 패턴 — CoachHookBundle/CoachCommentHookPayload 추가 위치 + 패턴)
    - backend/shared/python/sunity_shared/gemini/guardrails.py:23-105 (_SCORE_PATTERNS/_JUDGMENT_PATTERNS/_enforce_no_reject_patterns — 도(°/도/deg)+% regex 추가 대상)
    - backend/shared/python/sunity_shared/analysis/coach_writer.py:145-185 (_client is None graceful no-op 패턴 — 개념 참조, seam 은 api_key_loader)
    - backend/shared/python/sunity_shared/analysis/coach_hook.py (Wave 0 의 CoachCommentHook), coach_hook_builder.py (Wave 0 의 build_canned_hook NotImplementedError stub — 본 task 가 구현)
    - backend/tests/phase11/test_coach_hook_translation_only.py + test_coach_hook_fallback.py (Wave 0 RED 게이트 — 이 task 가 GREEN 으로)
    - .planning/phases/11-coachcommenthook-gemini/11-RESEARCH.md (Pattern 3 + Pitfall 4 + Code Examples) — 단, "GeminiCoachWriter.write() 재사용" 전제는 D-03 correction 으로 무효 (별도 writer 로 대체)
  </read_first>
  <action>
    **도(degree)/% 가드 (guardrails.py, HIGH-1):** `_SCORE_PATTERNS` 에 hook 전용 reject 패턴 추가 — `re.compile(r"\d+(\.\d+)?\s*(°|도|deg)", re.IGNORECASE)` + `re.compile(r"\d+(\.\d+)?\s*%")`. 기존 점/분수/좌표/판정 패턴 보존(기존 Phase 17 호출자 회귀 0 — 도/% 추가는 superset). `_enforce_no_reject_patterns` 본체는 패턴 tuple 만 확장.

    **CoachHookBundle schema (schemas.py):** `CoachPayload` 의 `extra="forbid"` 패턴으로 신규 `class CoachCommentHookPayload(BaseModel)` (필드: `auto_findings_summary: str`, `open_questions_for_coach: list[str]`, `suggested_cues: list[str]` — list[str] strict, list[dict] 금지) + `class CoachHookBundle(BaseModel)` (`force_pattern_inference: CoachCommentHookPayload | None = None`, `body_comparison_report: CoachCommentHookPayload | None = None`). 두 optional report hook 을 1 payload 에 묶음 (single call).

    **GeminiCoachHookWriter (신규 coach_hook_writer.py):** `class GeminiCoachHookWriter` — `__init__(self, *, api_key_loader=None, model_override=None)`. `build_coach_hooks(self, *, force_findings, body_findings) -> CoachHookBundle | None` 메서드: (1) `resolve_model("B", model_override)` 로 model 결정 (raw string 금지). (2) api_key_loader (기본 `_default_api_key_loader`) 로 키 로드 — 실패/빈 키 시 **None 반환** (graceful, D-08 seam). (3) `genai.Client(api_key).models.generate_content` **text-only** 호출 (response_schema=CoachHookBundle, prompt = force/body findings 의 구조화 텍스트/enum 필드만 — `kismam.top_issues`/generic top-joint deviation 금지, tip.detail2 중복 회피). **`GeminiVisionCall`/`files.upload`/`videoPath` 절대 미사용.** (4) raw text → `_enforce_no_reject_patterns(raw, context="coach_hook")` (allow_coords=False) → ValueError 시 None 반환 (caller 가 canned 로). **regex-strip-and-emit 금지(D-05).** prompt 시스템 절: "finding 자연어 풀이만, 수치·좌표·점수·판정·도(degree)·% 출력 금지" — 도(degree) deviation 을 프롬프트에 **주입하지 않음** (number-free, HIGH-1).

    **canned fallback (coach_hook_builder.py):** Wave 0 의 `build_canned_hook(findings, *, source_report)` stub 의 NotImplementedError 를 구현으로 교체 — finding 의 기존 텍스트/enum 필드(interpretation 등)만 조합해 객관 한국어 hook(CoachCommentHook) 생성 (LLM 미사용, number-free). autoFindingsSummary = 1 짧은 문단, openQuestionsForCoach = 1-3 항목 ("왼쪽 무릎 굽힘이 의도된 동작인지 강사와 확인" 형태), suggestedCues = 2-4 항목. coach_comment/reviewed_by = None (v1). finding 클래스는 TYPE_CHECKING/local import (순환 안전, HIGH-3 보존). 이 task 로 test_coach_hook_translation_only.py + test_coach_hook_fallback.py GREEN.
  </action>
  <acceptance_criteria>
    - `cd backend && PYTHONPATH=shared/python python -m pytest tests/phase11/test_coach_hook_translation_only.py tests/phase11/test_coach_hook_fallback.py -x -q` → PASS (Wave 0 RED → GREEN)
    - 기존 Vision writer 무수정: `git diff --stat backend/shared/python/sunity_shared/gemini/coach_writer_v2.py` → 변경 0 줄 (BLOCKER-1)
    - 신규 writer 가 video 미접촉: `grep -cE "GeminiVisionCall|files\.upload|videoPath|video_path" backend/shared/python/sunity_shared/gemini/coach_hook_writer.py` → 0
    - `grep -n "class GeminiCoachHookWriter" backend/shared/python/sunity_shared/gemini/coach_hook_writer.py` → 1 hit
    - `grep -n "class CoachHookBundle\|class CoachCommentHookPayload" backend/shared/python/sunity_shared/gemini/schemas.py` → 2 hit
    - 도/% regex 추가: `grep -nE "°|도|deg|%" backend/shared/python/sunity_shared/gemini/guardrails.py` → reject 패턴 hit + `python -c "from sunity_shared.gemini.guardrails import _enforce_no_reject_patterns; _enforce_no_reject_patterns('23도', context='t')"` → ValueError
    - `grep -n "resolve_model" backend/shared/python/sunity_shared/gemini/coach_hook_writer.py` → 1+ hit; raw model 하드코딩 0: `grep -c "gemini-3" backend/shared/python/sunity_shared/gemini/coach_hook_writer.py` → 0
    - sanitize-and-emit 부재: hook writer/builder 에 LLM 출력 편집(`re.sub`/`.replace(` on raw) 0 (reject-and-fallback only, D-05)
    - fallback seam = api_key_loader/None-return (WARNING-2): `grep -c "_client" backend/shared/python/sunity_shared/gemini/coach_hook_writer.py` → 0
    - `grep -n "def build_canned_hook" backend/shared/python/sunity_shared/analysis/coach_hook_builder.py` → 1 hit + NotImplementedError 부재
  </acceptance_criteria>
  <verify>
    <automated>cd backend && PYTHONPATH=shared/python python -m pytest tests/phase11/test_coach_hook_translation_only.py tests/phase11/test_coach_hook_fallback.py -x -q</automated>
  </verify>
  <done>신규 text-only GeminiCoachHookWriter 가 single call 로 두 리포트 hook 산출 (Vision writer 무수정). 도/% 가드 추가. 객관성 reject-and-fallback. 키 미설정/None 시 canned hook. translation-only + fallback 게이트 GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 두 리포트 hook 부착 (post-force-pattern window) + scoped validator 확장 (LANDMINE) + pipeline wiring</name>
  <files>backend/functions/pipeline/app.py, backend/shared/python/sunity_shared/analysis/force_pattern.py, backend/shared/python/sunity_shared/analysis/body_normalizer.py, backend/shared/python/sunity_shared/firestore_admin.py, backend/tests/phase11/test_coach_hook_nested_array.py</files>
  <behavior>
    - valid flat coachCommentHook dict (list[str] 필드) → _validate_force_pattern_inference 통과 (Pitfall 1)
    - coachCommentHook 안 list[dict]/nested list → ValueError reject (Pitfall 2)
    - BodyComparisonReport generic validator path 도 같은 flat hook 통과 (A2 — 양쪽 검증)
    - ForcePatternInference / BodyComparisonReport dataclass 에 coach_comment_hook = None default 필드 (field-order: 맨 끝)
    - hook 생성이 force_pattern_inference 생성 이후 / complete_analysis 이전에 일어남 (BLOCKER-2)
  </behavior>
  <read_first>
    - backend/shared/python/sunity_shared/firestore_admin.py:343-404 (_validate_force_pattern_inference — coachCommentHook 값은 dict 라 :371-374 dict 브랜치에서 먼저 raise 됨. 이 dict 브랜치 위에 화이트리스트 필요), :724-799 (complete_analysis + body 가 generic validator 경유)
    - backend/shared/python/sunity_shared/analysis/force_pattern.py:228-262 (ForcePatternInference — warnings trailing default 뒤에 coach_comment_hook 추가)
    - backend/shared/python/sunity_shared/analysis/body_normalizer.py:861-883 (BodyComparisonReport — trailing default 뒤에 추가)
    - backend/functions/pipeline/app.py:2047 (body_comparison_report_dict 변환 site — 늦추거나 hook 부착 후 재변환), :2099-2117 (force_pattern_inference 생성 + high-score gate + force_pattern_inference_dict 변환 site — hook 부착은 이 직후), :1482-1502 (_dataclass_to_camel_case_dict — hook 직렬화), :2286-2296 (complete_analysis 호출 — force_pattern_inference_dict / body_comparison_report_dict 주입)
    - backend/shared/python/sunity_shared/gemini/coach_hook_writer.py (Task 1 의 GeminiCoachHookWriter — pipeline 이 1회 호출), backend/shared/python/sunity_shared/analysis/coach_hook_builder.py (build_canned_hook — fallback)
    - backend/tests/phase11/test_coach_hook_nested_array.py (Wave 0 RED 게이트)
    - .planning/phases/11-coachcommenthook-gemini/11-PATTERNS.md (LANDMINE 섹션 + Pattern 4)
  </read_first>
  <action>
    `force_pattern.py` 의 ForcePatternInference 와 `body_normalizer.py` 의 BodyComparisonReport 각각에 `coach_comment_hook: CoachCommentHook | None = None` 을 모든 non-default 필드 뒤 맨 끝에 추가 (D-02, field-order rule). coach_hook.py 에서 import (이 모듈들은 coach_hook 만 import — 순환 안전, HIGH-3).

    **LANDMINE 확장 (firestore_admin.py):** `_validate_force_pattern_inference` 에 `coachCommentHook` 화이트리스트 브랜치 추가. coachCommentHook 값은 `dict` 이므로 `isinstance(value, dict)` 브랜치(:371-374, 현재 ANY nested dict 에 raise)에서 먼저 걸린다 — 이 dict 브랜치 **위에** `if key == "coachCommentHook": _validate_flat_dict_no_nested_array(value, path=sub); continue` 를 추가 (hook 의 list[str] 필드만 통과, list[dict]/nested 는 ValueError). body_comparison 은 generic `_validate_flat_dict_no_nested_array` (:780) 경유라 변경 불필요 가능성 높으나 **첫 write test 로 검증** (A2). 미확장 시 coachCommentHook 부착이 `server_error` 로 분석 실패 — 이 확장이 본 plan 의 독립 acceptance criterion.

    **pipeline wiring (single call, post-force-pattern window, BLOCKER-2):** hook 생성을 **force_pattern_inference 생성 + high-score gate(:2108-2114) 직후, complete_analysis(:2286) 이전**으로 배치한다 (기존 :1945 dual-track coach call site 아님 — 그 site 는 ForcePatternInference 가 아직 없음). 거기서 `GeminiCoachHookWriter().build_coach_hooks(force_findings=force_pattern_inference.findings, body_findings=body_comparison_report.findings)` 를 **1회** 호출(single call) → 반환 None(키 미설정/reject) 시 `build_canned_hook(...)` 로 per-report fallback. `dataclasses.replace(force_pattern_inference, coach_comment_hook=...)` + `dataclasses.replace(body_comparison_report, coach_comment_hook=...)` 로 두 리포트에 부착 (source_report scalar 로 provenance, D-02). **dict 변환 순서:** `force_pattern_inference_dict`(:2115) / `body_comparison_report_dict`(:2047) 를 hook 부착 **이후**에 `_dataclass_to_camel_case_dict` 로 (재)변환 — 부착 전 변환된 dict 는 hook 누락이므로, 변환을 hook 부착 뒤로 옮기거나 replace 후 재변환. complete_analysis(:2286) 의 force_pattern_inference_dict / body_comparison_report_dict 에 coachCommentHook 포함. **coach_details(joint writer) 변수는 무수정 — coach_hooks 는 별도 변수**(BLOCKER-3). Gemini/키 미설정 또는 가드 reject 시 canned hook 부착(분석 절대 실패 안 함, D-08). 이 task 로 test_coach_hook_nested_array.py GREEN.
  </action>
  <acceptance_criteria>
    - `cd backend && PYTHONPATH=shared/python python -m pytest tests/phase11/test_coach_hook_nested_array.py -x -q` → PASS (Pitfall 1+2, 양쪽 report path)
    - `grep -n "coachCommentHook" backend/shared/python/sunity_shared/firestore_admin.py` → _validate_force_pattern_inference 안 화이트리스트 브랜치 1+ hit
    - `grep -n "coach_comment_hook" backend/shared/python/sunity_shared/analysis/force_pattern.py backend/shared/python/sunity_shared/analysis/body_normalizer.py` → 각 1 hit (필드 추가)
    - `grep -n "build_coach_hooks\|build_canned_hook\|coach_comment_hook" backend/functions/pipeline/app.py` → wiring + 부착 hit
    - hook 생성 site 가 force_pattern_inference 이후: pipeline 에서 `build_coach_hooks`/`build_canned_hook` 호출 줄번호 > force_pattern_inference 생성 줄번호(:2099대) AND < complete_analysis 호출 줄번호(:2286대) (SUMMARY 에 줄번호 증명 기록, BLOCKER-2)
    - coach_details 무오염: `grep -n "coach_details" backend/functions/pipeline/app.py` 에 hook 키 주입 0 — coach_hooks 별도 변수 (BLOCKER-3)
    - 단일 호출: app.py 에 `build_coach_hooks` 호출이 분석 path 당 정확히 1회 (SUMMARY 기록), report 당 별도 Gemini invoke 0
    - `cd backend && PYTHONPATH=shared/python python -m pytest tests/phase11 -x -q` 전체 GREEN (4 게이트)
  </acceptance_criteria>
  <verify>
    <automated>cd backend && PYTHONPATH=shared/python python -m pytest tests/phase11 -x -q && cd ../app && npm run typecheck</automated>
  </verify>
  <done>두 리포트에 hook 부착 + Firestore flat 저장. scoped validator 확장(landmine 해소). hook 생성 site = post-force-pattern/pre-complete window. single call. coach_details 무오염. phase11 4 게이트 GREEN + tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gemini LLM output → hook 텍스트 | untrusted — 수치/판정/도(degree)/% mint 가능 |
| hook dict → Firestore scoped validator | nested-array gate (force=scoped, body=generic) |
| 키 미설정/가드 reject → 분석 완료 | fallback 경로가 분석을 절대 실패시키지 않아야 함 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02 | Tampering/Repudiation | GeminiCoachHookWriter (LLM 수치/판정/도) | mitigate | 프롬프트 객관성 절(number-free) + _enforce_no_reject_patterns(도/% 포함) reject-and-fallback (런타임 sanitize 금지, D-05) + translation-only test GREEN |
| T-11-03 | DoS | malformed/oversized hook → Firestore write/app render | mitigate | scoped validator(force) + generic validator(body) 가 bad shape reject → fail_analysis. 단 valid hook 은 절대 분석 실패 안 시킴 (D-08) |
| T-11-04 | Elevation of Privilege | v2 coach-write 필드(coachComment/reviewedBy) 조기 노출 | mitigate | v1 항상 None, write endpoint 미추가 (COACH-01 "UI/입력은 v2") |
| T-11-05 | Information Disclosure | Gemini 키 로그 유출 | mitigate | Parameter Store only, lazy import (api_key_loader), 로그에 키 미출력 (기존 패턴 보존) |
| T-11-06 | Tampering | hook 키가 joint-card success 집계 오염 (BLOCKER-3) | mitigate | coach_hooks 별도 변수 + 별도 writer (joint CoachPayload dict 미혼합) |
| T-11-SC | Tampering | npm/pip 설치 | accept | Phase 11 신규 패키지 0 — 설치 task 없음 |
</threat_model>

<verification>
- `cd backend && PYTHONPATH=shared/python python -m pytest tests/phase11 -x -q` 4 게이트 GREEN
- `cd backend && PYTHONPATH=shared/python python -m pytest -q` 전체 회귀 0 (phase06~17 기존 suite — 특히 Vision writer test_coach_writer_v2.py 무변경 GREEN)
- `cd app && npm run typecheck` clean
- Vision GeminiCoachWriter.write() 무수정 (git diff 0줄)
- scoped validator 확장 확인 (coachCommentHook 화이트리스트 브랜치)
- single hook call + post-force-pattern window 보존
- 백엔드 shared-layer 변경 — 배포 시 `sam build --use-container` 필요 (본 phase 는 pre-deploy, test 로컬 실행. 배포는 Phase 15 실증)
</verification>

<success_criteria>
- single call 로 두 리포트 hook 산출 (D-03 correction, 별도 text-only writer)
- 기존 Vision writer 무수정 (BLOCKER-1)
- hook 생성 site = post-force-pattern / pre-complete (BLOCKER-2)
- coach_details / coach_hooks 별도 변수 (BLOCKER-3)
- 번역만 강제 — 도/% 포함 reject-and-fallback (ROADMAP SC#3, D-04/D-05/HIGH-1)
- api_key_loader/None seam fallback → canned hook 으로 분석 완료 (ROADMAP SC#5, D-08/WARNING-2)
- scoped validator 확장 (landmine 해소 — 독립 acceptance criterion)
- 회귀 0 + tsc clean
- FEED-02 = 기존 dual-coach 로 Complete, 재작업 0
</success_criteria>

<output>
Create `.planning/phases/11-coachcommenthook-gemini/11-01-SUMMARY.md` when done
</output>
</content>
