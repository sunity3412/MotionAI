---
phase: 11-coachcommenthook-gemini
plan: 02
type: execute
wave: 2
depends_on: ["11-00", "11-01"]
files_modified:
  - app/src/app/analysis/result.tsx
  - app/src/lib/userAnalyses.ts
autonomous: false
requirements: [FEED-03, FEED-02]
must_haves:
  truths:
    - "v1 결과 화면에 openQuestionsForCoach 만 '강사에게 확인할 점' 섹션으로 노출된다 (D-06)"
    - "autoFindingsSummary / suggestedCues 는 저장만 되고 v1 화면 비노출 (D-06)"
    - "결과 화면 상단 1줄 + 코치 섹션 헤더가 AI 를 '강사 보조 도구'로 포지셔닝한다 (FEED-03, D-07)"
    - "Mode 1 기준모션 라벨 근처에 '하나의 참고일 뿐' 문구가 표시된다 (ROADMAP SC#4)"
    - "이전 빌드 doc (coachCommentHook 없음) 도 null-guard 로 graceful 렌더된다"
  artifacts:
    - path: "app/src/app/analysis/result.tsx"
      provides: "강사에게 확인할 점 섹션 + 포지셔닝 카피 + 기준모션 참고 문구"
      contains: "강사에게 확인할 점"
    - path: "app/src/lib/userAnalyses.ts"
      provides: "coachCommentHook null-guard normalize (후방호환)"
  key_links:
    - from: "app/src/app/analysis/result.tsx"
      to: "result.forcePatternInference?.coachCommentHook?.openQuestionsForCoach"
      via: "?? [] null-guard 렌더"
      pattern: "coachCommentHook"
---

<objective>
v1 수강생 결과 화면에 `openQuestionsForCoach` 를 "강사에게 확인할 점" 섹션으로 노출하고, AI 를 "강사 보조 도구"로 포지셔닝하는 카피(상단 1줄 + 코치 섹션 헤더 + Mode 1 기준모션 "하나의 참고일 뿐")를 추가한다. 이것이 Phase 11 의 최종 슬라이스 — 수강생이 강사에게 가져갈 거리를 화면에서 본다 (FEED-03 강사 보조 도구 포지셔닝 직접 충족).

Purpose: FEED-03 (AI 대체 인상 제거) + ROADMAP SC#4 (기준모션 = 하나의 참고). D-06 노출 범위 엄수 — openQuestionsForCoach 만, autoFindingsSummary/suggestedCues 비노출, coachComment/reviewedBy v1 null.
Output: result.tsx 섹션 + 카피, userAnalyses.ts null-guard. tsc clean + 시각 checkpoint.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-coachcommenthook-gemini/11-CONTEXT.md
@.planning/phases/11-coachcommenthook-gemini/11-PATTERNS.md
@.planning/phases/11-coachcommenthook-gemini/11-00-SUMMARY.md
@.planning/phases/11-coachcommenthook-gemini/11-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 강사에게 확인할 점 섹션 + 포지셔닝 카피 + null-guard normalize</name>
  <files>app/src/app/analysis/result.tsx, app/src/lib/userAnalyses.ts</files>
  <read_first>
    - app/src/app/analysis/result.tsx:524-538 (force-pattern null-guard `?? []` precedent), :677-684 (refCard + styles.refName — Mode 1 기준모션 라벨 site, "하나의 참고일 뿐" 배치), :779-794 (sectionTitle + card 섹션 렌더 패턴), :1034+ (StyleSheet 토큰 블록)
    - app/src/theme/index.ts (colors.brand #FF4B33 / colors.cardBg / spacing / colors.divider — 토큰만 사용, 하드코딩 금지)
    - app/src/lib/userAnalyses.ts (normalize() — forcePatternInference / recommendedExercises null-guard precedent. coachCommentHook 도 nullable normalize)
    - app/src/types/analysis.ts (CoachCommentHook interface — Wave 0 추가됨)
    - .planning/phases/11-coachcommenthook-gemini/11-CONTEXT.md (D-06 노출 범위 + D-07 카피 위치)
    - .planning/phases/11-coachcommenthook-gemini/11-PATTERNS.md (Pattern 5)
  </read_first>
  <action>
    `userAnalyses.ts` 의 normalize() 에 coachCommentHook null-guard 추가 (forcePatternInference/recommendedExercises 패턴 1:1 mirror — malformed → null graceful, immutable spread). 두 리포트(forcePatternInference, bodyComparisonReport) 각각의 coachCommentHook 정규화.

    `result.tsx`: (1) openQuestions = `result.forcePatternInference?.coachCommentHook?.openQuestionsForCoach ?? result.bodyComparisonReport?.coachCommentHook?.openQuestionsForCoach ?? []` 로 수집 → length > 0 시에만 "강사에게 확인할 점" 섹션 렌더 (sectionTitle + card, 기존 "실패 원인 후보" 섹션 shape mirror). (2) **D-06 엄수**: openQuestionsForCoach 만 렌더. autoFindingsSummary/suggestedCues 는 화면에 절대 노출 안 함. coachComment/reviewedBy 는 v1 null 이라 미렌더. (3) D-07 포지셔닝: 결과 화면 상단 1줄 카피(예: "이 분석은 강사 지도를 돕는 참고예요") + 코치 섹션 헤더 카피. 전용 강조 배너 금지 (매 분석 반복 노출 거슬림). (4) ROADMAP SC#4: Mode 1(정은지 비교) 기준모션 라벨(styles.refName, :684) 근처에 "기준 모션은 하나의 참고일 뿐" 문구. **테마 토큰만** (brand #FF4B33, 라이트 전용, Pretendard) — 색/간격 하드코딩 금지 (app/CLAUDE.md). 이모지 금지. Korean 카피.

    Figma 우선 ([[ui-figma-first]], fileKey jrdI7kp245HkPfLB0nclsz) — 결과 화면 카피/섹션을 Figma 와 대조 후 확정. design.md 자체해석 전에 Figma 확인.
  </action>
  <acceptance_criteria>
    - `grep -n "강사에게 확인할 점" app/src/app/analysis/result.tsx` → 1 hit (D-06 섹션)
    - `grep -n "강사 지도를 돕는\|강사 보조\|하나의 참고" app/src/app/analysis/result.tsx` → 포지셔닝 1줄 + 기준모션 참고 문구 hit (FEED-03 + SC#4)
    - autoFindingsSummary/suggestedCues 미노출: `grep -c "autoFindingsSummary\|suggestedCues" app/src/app/analysis/result.tsx` → 0 (D-06 — 데이터만 저장, 화면 비노출)
    - 하드코딩 색상 부재: result.tsx 신규 코드에 `#` 로 시작하는 색상 리터럴 0 (테마 토큰만)
    - `grep -n "coachCommentHook" app/src/lib/userAnalyses.ts` → null-guard normalize hit (후방호환)
    - 이모지 0
    - `cd app && npm run typecheck` clean
  </acceptance_criteria>
  <verify>
    <automated>cd app && npm run typecheck</automated>
  </verify>
  <done>openQuestionsForCoach 섹션 + 포지셔닝 카피 + 기준모션 참고 문구 렌더. autoFindingsSummary/suggestedCues 비노출. 토큰만. null-guard 후방호환. tsc clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 결과 화면 카피/섹션 시각 검증 (belle)</name>
  <files>app/src/app/analysis/result.tsx</files>
  <action>
    belle 가 결과 화면 UI/카피를 시각 검증한다 (Task 1 자동 구현 후 인간 확인). 본 checkpoint 는 Task 1 의 자동 구현을 대체하지 않고 그 위에서 시각/톤/Figma 정합만 검증한다 (실 LLM E2E 는 Phase 15 deferred).
  </action>
  <what-built>
    result.tsx 에 "강사에게 확인할 점" 섹션(openQuestionsForCoach), 상단 포지셔닝 1줄("강사 지도를 돕는 참고"), 코치 섹션 헤더, Mode 1 기준모션 라벨 근처 "하나의 참고일 뿐" 문구. 테마 토큰만(브랜드 #FF4B33, 라이트), 이모지 0. userAnalyses.ts null-guard 로 이전 빌드 doc 도 graceful.
  </what-built>
  <how-to-verify>
    1. 완료된 분석 결과 화면을 연다 (coachCommentHook 포함 doc — Wave 1 백엔드가 채운 분석, 또는 시뮬 doc).
    2. "강사에게 확인할 점" 섹션이 openQuestionsForCoach 항목으로 렌더되는지 확인 (수강생이 강사에게 가져갈 질문 형태).
    3. autoFindingsSummary / suggestedCues 가 화면에 노출되지 않는지 확인 (D-06 — 저장만).
    4. 상단 포지셔닝 1줄 + 코치 섹션 헤더가 "AI = 강사 보조 도구" 톤인지, 강조 배너 없이 가벼운지 확인 (D-07).
    5. Mode 1 (정은지 비교) 화면에서 기준모션 라벨 근처 "하나의 참고일 뿐" 문구 확인 (SC#4).
    6. 라이트 테마 + 브랜드 컬러 + Pretendard + 이모지 0, Figma (fileKey jrdI7kp245HkPfLB0nclsz) 와 톤 정합 확인.
    7. coachCommentHook 없는 이전 doc 에서도 크래시 없이 graceful (섹션 미렌더).
    실 LLM E2E (실 Gemini hook 생성) 는 Phase 15 실증으로 deferred — 본 checkpoint 는 UI/카피 시각 검증만.
  </how-to-verify>
  <resume-signal>"approved" 또는 카피/레이아웃 수정 사항 기술</resume-signal>
  <done>belle 가 섹션/카피/Figma 정합 시각 검증 PASS (또는 수정 후 재확인). D-06 노출 범위 + D-07 포지셔닝 + SC#4 기준모션 문구 확인.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firestore doc → app render | 이전 빌드 doc 에 coachCommentHook 없음 — null-guard 필요 |
| v2 필드 → v1 화면 | coachComment/reviewedBy 가 v1 에 노출되면 안 됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | DoS | malformed hook → app render 크래시 | mitigate | userAnalyses.ts normalize null-guard (?? null) — malformed → null graceful (userAnalyses precedent) |
| T-11-04 | Elevation of Privilege | v2 coach-write 필드 v1 노출 | mitigate | result.tsx 가 openQuestionsForCoach 만 렌더. coachComment/reviewedBy/autoFindingsSummary/suggestedCues 미렌더 (D-06) |
| T-11-SC | Tampering | npm 설치 | accept | Phase 11 신규 패키지 0 — 설치 task 없음 |
</threat_model>

<verification>
- `cd app && npm run typecheck` clean
- "강사에게 확인할 점" 섹션 + 포지셔닝 카피 + 기준모션 문구 grep hit
- autoFindingsSummary/suggestedCues 화면 미노출 (grep 0)
- 하드코딩 색상 0 (테마 토큰만), 이모지 0
- belle 시각 checkpoint PASS (실 LLM E2E 는 Phase 15)
</verification>

<success_criteria>
- v1 화면에 openQuestionsForCoach 만 노출 (D-06)
- 포지셔닝 카피 = AI 강사 보조 도구 (FEED-03, D-07)
- Mode 1 기준모션 = 하나의 참고 (ROADMAP SC#4)
- 후방호환 null-guard
- 토큰만 / 라이트 / 이모지 0 / Figma 정합
- FEED-02 = 기존 피드백 순서 Complete, 본 plan 은 그 위 코치 레이어 노출
</success_criteria>

<output>
Create `.planning/phases/11-coachcommenthook-gemini/11-02-SUMMARY.md` when done
</output>
