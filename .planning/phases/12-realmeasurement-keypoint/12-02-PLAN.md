---
plan_id: 12-02
phase: 12-realmeasurement-keypoint
plan: 02
wave: 1
type: execute
title: "Wave 1 — UI 신영역 3 component 신설 + VideoCompare slot 확장 + result.tsx 6 영역 layout 재정비"
requirements: [FEED-01, VIS-01]
mvp_slice: "사용자가 결과 화면에서 6 영역 (점수 → 영상+오버레이 slot → Phase 9 카드 → 차원 → 각도 → 성장 차트) 순으로 본다. KeypointOverlay 는 정적 fallback (toggle/sync 는 Wave 2). ForcePatternCard + ForcePatternDetailModal 은 완성."
depends_on: [12-01]
files_modified:
  - app/src/components/KeypointOverlay.tsx                                      # 신설
  - app/src/components/ForcePatternCard.tsx                                     # 신설
  - app/src/components/ForcePatternDetailModal.tsx                              # 신설
  - app/src/components/VideoCompare.tsx
  - app/src/app/analysis/result.tsx
  - app/src/lib/userAnalyses.ts
  - app/src/theme/colors.ts
  - app/src/theme/index.ts
autonomous: true
mvp_mode: true

must_haves:
  truths:
    - "사용자가 결과 화면을 위에서 아래로 스크롤 시 6 영역이 D-12-A1 순서로 노출된다: 점수 게이지 → 영상+오버레이 slot → Phase 9 원인 카드 Top-3 → 차원 카드 → 각도 가이드 → (mode3 only) 성장 차트"
    - "ForcePatternCard 가 findings.length ∈ {0, 1, 2, 3} 모든 케이스에서 정확한 카드 수 렌더 (0=fallback big, 1=big only, 2=big+small, 3=big+small×2) — D-12-B1 박제"
    - "ForcePatternDetailModal 이 BottomSheet pattern (DimensionDetailModal mirror) 으로 finding tap 시 open + Backdrop tap / 닫기 버튼으로 close"
    - "VideoCompare.tsx 가 **render prop** overlay slot 확장 (`leftOverlay?: (player: VideoPlayer | null) => React.ReactNode, rightOverlay?: (player: VideoPlayer | null) => React.ReactNode`) — R7 (Codex 리뷰 2026-06-10) 정합. result.tsx 가 player state 들지 않고 VideoCompare 가 자기 player lifecycle 안에서 callback 호출. KeypointOverlay 가 same scope 에서 player 받아 Wave 2 useEvent 구독 (slot pattern D-12-A4)"
    - "KeypointOverlay 가 정적 fallback 으로 8 body keypoint + axisData polyline 렌더 (frame sync 와 delta 강조 + toggle 은 Wave 2 책임) — R11 정합: KeypointName Literal 8 + axisData 별도. visibility prop / props 로 들어온 frame=0 좌표 그대로 박제"
    - "result.tsx 의 enrichJoints 시뮬 픽스처 주석 line 78-110 제거 + Wave 0 wiring fix 위에 reference fallback 만 보존 (Pitfall 8 해소)"
    - "userAnalyses.ts::normalize 의 keypointReport null-guard 가 Wave 0 박제 위에 동작 (구 doc / 신 doc 둘 다 crash 0)"
    - "Per-task atomic commit — 5 task 예상, 각 task 단위 commit"
    - "tsc --noEmit clean (TS strict mode)"
  artifacts:
    - path: app/src/components/KeypointOverlay.tsx
      provides: "8 body keypoint + axisData polyline 렌더 + Bones 라인 + KEYPOINT_DELTA_HIGHLIGHT_DEG const 박제. Wave 1 = 정적 (frame index = 0 또는 prop). visibility prop + fallback (keypointReport null 시 placeholder)"
      contains: "export const KEYPOINT_DELTA_HIGHLIGHT_DEG = 10.0"
    - path: app/src/components/ForcePatternCard.tsx
      provides: "variant 'big'/'small' + pattern chip 색 매핑 (release/pull/push/brace/rotate/unknown) + jointHint chip + confidence label + tap callback. 0/1/2/3 edge case 분기"
      contains: "type ForcePatternCardProps"
    - path: app/src/components/ForcePatternDetailModal.tsx
      provides: "BottomSheet (Modal + transparent backdrop) — DimensionDetailModal pattern mirror. 닫기 버튼 + handle + finding props 표시"
      contains: "export function ForcePatternDetailModal"
    - path: app/src/components/VideoCompare.tsx
      provides: "leftOverlay / rightOverlay **render prop** ((player: VideoPlayer | null) => React.ReactNode, R7 정합) + absolute overlay container (pointerEvents: 'none')"
      contains: "leftOverlay?: (player"
    - path: app/src/app/analysis/result.tsx
      provides: "6 영역 layout 재정비 + Phase 9 카드 영역 + 영상 카드 신영역 + enrichJoints 시뮬 주석 제거"
      contains: "실패 원인 후보"
    - path: app/src/theme/colors.ts
      provides: "Phase 12 신설 토큰 (brandSoft / brandBg / softBg / estimateGray / progressGreen / progressRed / warnAmber / videoBg / textHi / textMid / textLo / border / trackBg) — UI-SPEC §1 박제"
      contains: "brandSoft"
  key_links:
    - from: app/src/app/analysis/result.tsx
      to: app/src/components/ForcePatternCard.tsx
      via: "result.forcePatternInference.findings → ForcePatternCard rank/variant prop"
      pattern: "ForcePatternCard"
    - from: app/src/app/analysis/result.tsx
      to: app/src/components/ForcePatternDetailModal.tsx
      via: "tap → setDetailFinding(finding) → modal open"
      pattern: "ForcePatternDetailModal"
    - from: app/src/app/analysis/result.tsx
      to: app/src/components/VideoCompare.tsx
      via: "leftOverlay={(player) => <KeypointOverlay player={player} ... />} (R7 render prop)"
      pattern: "leftOverlay"
    - from: app/src/components/VideoCompare.tsx
      to: app/src/components/KeypointOverlay.tsx
      via: "children/slot — KeypointOverlay 가 absolute layer 로 video 위 박제"
      pattern: "overlayContainer"
    - from: app/src/lib/userAnalyses.ts
      to: app/src/types/analysis.ts::KeypointReport
      via: "normalize() null-guard"
      pattern: "result\\?\\.keypointReport"
---

<objective>
**Wave 1 — UI 신영역 component 3 신설 + VideoCompare slot 확장 + result.tsx 6 영역 layout 재정비.**

Wave 0 가 박제한 backend 실데이터 + KeypointReport schema 위에, Figma 4-frame reference 박제대로 사용자가 보는 결과 화면을 6 영역 순서 (D-12-A1) 로 재구성한다. 신설 component 3개 (KeypointOverlay / ForcePatternCard / ForcePatternDetailModal) + 기존 component 2개 확장 (VideoCompare slot prop / result.tsx 6 영역 끼워넣기).

Wave 1 = **시각 layout + 정적 렌더 완성**. Frame 동기화 / delta 강조 / confidence 표기 / iOS UAT 은 Wave 2 책임.

Purpose:
  - FEED-01 의 영상+키포인트 오버레이 시각 layer 가 갖춰진다 (frame=0 정적 fallback, Wave 2 가 sync).
  - VIS-01 의 6 영역 layout + Phase 9 카드 노출이 완성된다.
  - Per-task atomic commit (5 task 예상) 으로 회귀 차단 + Phase 9 패턴 정합.

Output:
  - 3 신설 component + 3 수정 파일.
  - 5 atomic commit (per task).
  - typecheck clean + manual visual UAT (Figma frame01 mode1 reference 와 1:1 visual 비교).
  - Wave 2 진입 가능 — KeypointOverlay 가 props 만 박제, sync 추가 site 박제.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@app/CLAUDE.md
@app/AGENTS.md
@.planning/phases/12-realmeasurement-keypoint/12-CONTEXT.md
@.planning/phases/12-realmeasurement-keypoint/12-RESEARCH.md
@.planning/phases/12-realmeasurement-keypoint/12-UI-SPEC.md
@.planning/phases/12-realmeasurement-keypoint/12-01-SUMMARY.md
@.planning/phases/12-realmeasurement-keypoint/12-WAVE0-AUDIT.md

# Wave 1 수정 + 패턴 source
@app/src/app/analysis/result.tsx
@app/src/components/VideoCompare.tsx
@app/src/components/DimensionDetailModal.tsx
@app/src/components/OctagonScore.tsx
@app/src/components/GrowthChart.tsx
@app/src/components/CoachingTipDetailModal.tsx
@app/src/types/analysis.ts
@app/src/lib/userAnalyses.ts
@app/src/theme/colors.ts
@app/src/theme/index.ts
@app/src/theme/typography.ts
@app/package.json
</context>

<tasks>

<task id="T1" type="auto">
  <name>Task 1: theme 토큰 신설 + KeypointOverlay.tsx 신설 (정적 렌더, frame=0 fallback)</name>
  <files>
    app/src/theme/colors.ts,
    app/src/theme/index.ts,
    app/src/components/KeypointOverlay.tsx
  </files>
  <action>
**(1) theme/colors.ts 토큰 신설 (UI-SPEC §1 박제, 하드코딩 금지 CLAUDE.md §4):**
   - 기존 export `colors` object 에 신설 토큰 추가:
     - `brandSoft: '#FFD9D2'`, `brandBg: '#FFE5E0'` (Phase 9 카드 작은 chip 배경 + 옥타곤 outer ring 톤).
     - `softBg: '#F5F5F5'` (jointHint chip 배경).
     - `estimateGray: '#B0B0B0'` (저신뢰 "추정 N°" 컬러).
     - `progressGreen: '#22B47A'`, `progressRed: '#E64545'` (mode3_progress delta).
     - `warnAmber: '#E6A300'` (⚠ occlusion badge).
     - `videoBg: '#2A2A2A'` (영상 카드 배경 — design.md §5-1 다크 예외).
     - `textHi: '#1A1A1A'`, `textMid: '#5A5A5A'`, `textLo: '#888888'` (UI-SPEC §1 정합. 기존 textPrimary/textSecondary 와 별개 — Phase 12 신영역 전용 박제).
     - `trackBg: '#EBEBEB'` (점수 트랙 배경).
   - `theme/index.ts` re-export 확인 — colors export 그대로.
   - 기존 토큰 변경 0 (브랜드 #FF4B33 변경 절대 금지 CLAUDE.md §4 / D-12-U5).

**(2) KeypointOverlay.tsx 신설 — 정적 렌더 (Wave 1) + Wave 2 sync 진입 site 박제:**
   - RESEARCH §Code Examples §1 (line 580-743) 패턴 mirror. 단 Wave 1 은 `useEvent` 미사용 — `frameIndex` prop 으로 직접 받음 (Wave 2 가 useEvent 박제 site 교체).
   - Import: `import Svg, { Circle, Line, G, Rect, Text as SvgText } from 'react-native-svg'; import { colors } from '../theme'; import type { KeypointReport, KeypointName } from '../types/analysis';`
   - Module const: `export const KEYPOINT_DELTA_HIGHLIGHT_DEG = 10.0;  // D-12-C3 — Phase 9 IPSF tolerance 20° 와 분리. Wave 2 가 delta 강조 룰 박제.`
   - `BONES: [KeypointName, KeypointName][]` — UI-SPEC §5 + RESEARCH §Code Examples §1 박제 (`['left_shoulder', 'right_shoulder']`, `['left_shoulder', 'left_hip']`, `['right_shoulder', 'right_hip']`, `['left_hip', 'right_hip']`, `['left_hip', 'left_knee']`, `['right_hip', 'right_knee']`, `['left_shoulder', 'left_hand']`, `['right_shoulder', 'right_hand']`).
   - Props (Wave 1 박제 — Wave 2 가 player + useEvent 박제 site 추가):
     ```
     type KeypointOverlayProps = {
       keypointReport: KeypointReport | null;
       videoSize: { width: number; height: number };
       frameIndex?: number;  // Wave 1 = 0 default, Wave 2 = useEvent 산출
       visible: boolean;
       /** Wave 2 부터 사용 — delta 강조 / floating angle label. Wave 1 = 모두 default WHITE. */
       jointAngles?: Record<string, { current: number | null; target: number | null }>;
     };
     ```
   - 함수 본문:
     - keypointReport 없거나 visible=false → return null (D-12-U6 fallback caller 가 placeholder).
     - useMemo `positions`: frame index 의 8 body keypoint + axisData polyline 좌표 reshape (RESEARCH §Pattern 4 박제, single-frame reshape — flat array 전체 reshape 회피).
     - useMemo `highlightedJoints`: jointAngles prop 이 박제됐을 때만 delta 산출. Wave 1 = props 미사용 → 빈 Set. Wave 2 가 박제 site.
     - Render: `<View style={StyleSheet.absoluteFillObject} pointerEvents="none"><Svg viewBox={...} width="100%" height="100%">{Bones map} {Circles map}</Svg></View>`.
     - Bones: stroke=`isHi ? colors.brand : '#FFFFFF'`, strokeWidth=`isHi ? 3 : 1.8`, opacity=0.95.
     - Circles: r=10, fill=`isHi ? colors.brand : '#FFFFFF'`, stroke=`isHi ? colors.brand : 'rgba(0,0,0,0.6)'`, strokeWidth=1.5.
     - Floating angle label (highlighted joint 만): brand bg radius 9 rect + WHITE text "N°" 10pt semi bold — Wave 1 은 highlighted Set 가 비어있어 미렌더.
   - viewBox = `0 0 ${W} ${H}` — Wave 0 audit Task 2 결과 (좌표계 normalized 0~1 vs pixel) 에 따라 변경. 만약 normalized → `viewBox="0 0 1 1"` + `<G transform={\`scale(${W} ${H})\`}>` 박제. 12-WAVE0-AUDIT.md 의 §3 좌표계 결정 박제 source.
   - A11y: `accessibilityElementsHidden={!visible}` (시각 보조 layer — 별도 a11y label 없음).
   - **반박제 가드**: 좌표 자체 산출 금지 (D-12 §12 안티 패턴 + T-12-S1). props 박제 좌표 only. UI 단에서 angle 계산 금지 (jointAngles prop 의 current/target 만 사용).

**제약사항:**
   - 신설 package install 0 (`react-native-svg` 15.12.1 + `expo-video` ~3.0.16 기존).
   - 하드코딩 색/spacing/radius 금지 (CLAUDE.md §4 / D-12-U5 / app/CLAUDE.md "토큰만 사용").
   - 이모지 금지 / 박제 단어 카피 안 들어감 ([[no-baekje-filler]]).
   - Wave 2 진입 site 박제 (frameIndex prop / jointAngles prop 시그니처) — 다음 wave 가 useEvent 박제 site 교체.

**Per-task commit:** `feat(12-02): theme 토큰 신설 + KeypointOverlay.tsx 정적 렌더 박제 (D-12-A4 / D-12-C2 / D-12-U5)`
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck</automated>
    <decision_coverage>per D-12-A4 (신영역 component 분리), D-12-C2 (8 body keypoint + axisData polyline 박제), D-12-C5 (react-native-svg + svg viewBox), D-12-U5 (브랜드 컬러 토큰)</decision_coverage>
  </verify>
  <done>
    - colors.ts 에 13 신설 토큰 박제. 기존 brand #FF4B33 변경 0.
    - KeypointOverlay.tsx 신설 + `KEYPOINT_DELTA_HIGHLIGHT_DEG = 10.0` const export.
    - tsc clean.
    - Per-task atomic commit.
  </done>
</task>

<task id="T2" type="auto">
  <name>Task 2: ForcePatternCard.tsx 신설 (variant big/small + chip 색 + 0/1/2/3 edge case)</name>
  <files>
    app/src/components/ForcePatternCard.tsx
  </files>
  <action>
RESEARCH §Code Examples §4 (line 818-902) 패턴 mirror. UI-SPEC §4 박제. 신설 파일.

**Import:**
   - `import React from 'react';`
   - `import { Pressable, View, Text, StyleSheet } from 'react-native';`
   - `import { colors, radius, spacing, typography } from '../theme';`
   - `import type { ForcePatternFinding } from '../types/analysis';`

**Module const:**
   - `PATTERN_LABEL_KO: Record<string, string> = { release: 'RELEASE', pull: 'PULL', push: 'PUSH', brace: 'BRACE', rotate: 'ROTATE', unknown: '정보' }` — UI-SPEC §6 pattern chip color map.
   - `_FALLBACK_BODY = "이 영상에서는 분명한 힘 흐름 이슈 신호가 보이지 않습니다. 강사와 함께 확인하는 것을 권장해요."` — UI-SPEC §4 edge case 0 finding fallback.
   - `confidenceLabel(c: number): { text: string; color: string }`: c >= 0.7 → "신뢰도 높음" + brand / c >= 0.5 → "신뢰도 보통" + textMid / 그 외 → "신뢰도 낮음" + textLo.

**Props:**
   ```
   type ForcePatternCardProps = {
     finding: ForcePatternFinding;
     rank: 0 | 1 | 2;
     variant: 'big' | 'small';
     onTap: () => void;
   };
   ```

**Render — variant 'big' (UI-SPEC §4 layout 1:1 mirror):**
   - 358 × 168 카드, `colors.cardBg`, border 1pt `colors.divider`, radius 15pt.
   - Top row (flex row, align center, gap 8, flexWrap): `#N` (rank+1, 11pt Semi Bold textLo) + patternChip (brand bg, "RELEASE" 9pt Semi Bold WHITE, radius 11pt) + jointHint chip (`finding.jointHint` 없으면 미렌더 / softBg + 11pt Medium textHi) + confidence label (우측 정렬 marginLeft auto).
   - Body: `finding.interpretation` 14pt Semi Bold textHi lineHeight 22.
   - Bottom hint: "탭하여 자세히 보기 ›" 11pt Regular textLo 우측 정렬.
   - pattern === 'unknown' → patternChip bg = `colors.softBg`, text color = `colors.textMid` (UI-SPEC §6).

**Render — variant 'small' (174 × 110):**
   - 카드: WHITE bg, border 1pt divider, radius 12pt, flex:1, padding 12, gap 6.
   - Top row (flex row, gap 6): `#N` + patternChip (brandSoft bg, text color brand — 시각 hierarchy big 보다 약함).
   - smallJointHint (`finding.jointHint` 있을 때만): 10pt Medium textMid.
   - Body: `finding.interpretation` 11pt Regular textHi lineHeight 17, `numberOfLines={2}` `ellipsizeMode="tail"` (2-line clamp).

**Edge cases (caller 책임 — result.tsx 가 분기, 본 component 는 props 그대로 렌더):**
   - 0 finding → caller 가 fallback finding object 생성 (interpretation = `_FALLBACK_BODY`, pattern='unknown', confidence=0) → variant='big' 1개 렌더. **본 component 가 `_FALLBACK_BODY` const export** — caller 가 import 해서 사용.

**A11y (UI-SPEC §4 + §11):**
   - `accessibilityRole='button'`, `accessibilityLabel={실패 원인 ${rank+1}, ${patternLabel}, ${finding.jointHint ?? '정보'}, 자세히 보려면 탭}`, `hitSlop={8}`.

**제약사항:**
   - **NEVER**: pattern chip 색 / spacing / radius 하드코딩. 모두 theme 토큰.
   - **NEVER**: 박제 단어 카피 안 들어감 ([[no-baekje-filler]]).
   - **NEVER**: interpretation 본문 수정 (canned KO 출처 = `force_pattern_copy.py`, UI 가 그대로 렌더).
   - **NEVER**: mode 분기 코드 (백엔드 modeContext + interpretation prefix 가 박제 박제 — D-12-B4 + D-12-U3 + A6).
   - finding.confidence 가 undefined / null 경우 0 으로 fallback (typecheck strict path).

**Per-task commit:** `feat(12-02): ForcePatternCard.tsx 신설 — variant big/small + chip 색 + 0/1/2/3 edge case (D-12-B1/B2/B3)`
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck</automated>
    <decision_coverage>per D-12-B1 (finding[0] big + finding[1..2] small), D-12-B2 (canned KO 직접 표시), D-12-B3 (tap → modal), D-12-B4 (mode 자동 분기), D-12-U3 (UI 분기 코드 최소)</decision_coverage>
  </verify>
  <done>
    - ForcePatternCard.tsx 신설 + variant big/small + edge case 4가지 (0/1/2/3) 분기 박제.
    - `_FALLBACK_BODY` const export.
    - tsc clean.
    - Per-task commit.
  </done>
</task>

<task id="T3" type="auto">
  <name>Task 3: ForcePatternDetailModal.tsx 신설 (BottomSheet — DimensionDetailModal pattern mirror)</name>
  <files>
    app/src/components/ForcePatternDetailModal.tsx
  </files>
  <action>
UI-SPEC §7 + Phase 12.5 `DimensionDetailModal.tsx` (348 line) pattern mirror. 신설 파일.

**Import:**
   - `React`, `Modal`, `Pressable`, `View`, `Text`, `StyleSheet`, `ScrollView` from react-native.
   - `colors, radius, spacing, typography` from '../theme'.
   - `ForcePatternFinding` from '../types/analysis'.
   - `Ionicons` from '@expo/vector-icons' (DimensionDetailModal mirror).

**Props:**
   ```
   type ForcePatternDetailModalProps = {
     visible: boolean;
     finding: ForcePatternFinding | null;
     rank?: number;       // optional — caller 가 0/1/2 박제 가능
     onClose: () => void;
   };
   ```

**Render (UI-SPEC §7 layout 박제):**
   - `<Modal visible={visible} transparent={true} animationType="slide" onRequestClose={onClose}>` (RN 기본 Modal, react-native-bottom-sheet 신설 install X — RESEARCH "Don't Hand-Roll" 박제).
   - Backdrop: `<Pressable style={styles.backdrop} onPress={onClose}>` — 반투명 검정 `rgba(0,0,0,0.4)` flex:1.
   - Sheet container: bottom 박제 — `position: absolute; bottom: 0; left: 0; right: 0; backgroundColor: colors.bg; borderTopLeftRadius 24; borderTopRightRadius 24; padding 20; maxHeight: 88%`.
   - Handle: 40 × 4 BORDER, radius 2, marginBottom 12 alignSelf center.
   - Header row: title "실패 원인 #{rank+1}" 18pt Semi Bold + 닫기 Pressable (Ionicons "close" 22pt textMid) marginLeft auto.
   - Chips row (UI-SPEC §7 — finding.pattern + jointHint + confidence label):
     - patternChip (brand bg + WHITE text) + jointHint chip (softBg + textHi) + confidence label "신뢰도 ${높음|보통|낮음}" (color 동일 분류 ForcePatternCard 와).
   - Body: `finding.interpretation` 16pt Semi Bold textHi lineHeight 26.
   - 강사 안내 row: 12pt Regular textLo "강사가 함께 보면 더 구체적 피드백을 받을 수 있어요" — 이모지 사용 절대 금지 (CLAUDE.md §7), 단순 텍스트.
   - "이 원인은 어떻게 측정됐나" 카드 (softBg radius 12 padding 16): title 13pt Semi Bold textHi + body 12pt Regular textMid (`finding.reason` 또는 fallback "AI 가 동작 신호를 기반으로 추정한 가능성입니다.").
   - "관련 부위" + "확인하기" 점 리스트 (13pt Medium textHi):
     - "관련 부위 — ${finding.jointHint ?? '확인 필요'}"
     - "확인하기 — 거울 보며 동작 직접 재현"
   - Footer 안내 카드 (brandSoft 0.5 bg radius 12 padding 14): 11pt Regular textHi "AI 가 추정한 가능성이에요. 강사 수업과 함께 확인하면 가장 정확한 피드백을 받을 수 있어요." (D-12-B3 footer + [[feedback-no-echo-confirm]]).
   - 닫기 버튼: brand bg, radius 13, height 54, 16pt Semi Bold WHITE, marginTop 24 (design.md §5-3 CTA 박제).

**Behavior:**
   - Backdrop tap → onClose.
   - 닫기 버튼 tap → onClose.
   - swipe-down handle 은 v1 단순 — RN Modal 의 onRequestClose 만 (PanResponder 박제 X). RESEARCH "Don't Hand-Roll" + Phase 12.5 DimensionDetailModal mirror 정합.

**Phase 11 통합 site:**
   - `finding.interpretation` 본문은 canned KO (현재). Phase 11 통합 시 동일 필드를 LLM 풍부화 산출로 자동 교체 — 본 component 변경 X (D-12-B2 / [[mode3-progress-not-similarity]]).
   - `finding.reason` (EN debug) 도 동일 — Phase 11 시 KO 자연어 풍부화.

**제약사항:**
   - `finding === null` 시 `<Modal visible={false}>` 그대로 (조건부 렌더 X — 깜빡임 방지).
   - 토큰만 사용 / 하드코딩 금지 / 이모지 금지.
   - 박제 단어 일반 동사 대용 금지 ([[no-baekje-filler]]).

**Per-task commit:** `feat(12-02): ForcePatternDetailModal.tsx 신설 — BottomSheet pattern (DimensionDetailModal mirror) (D-12-B3 / D-12-U1)`
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck</automated>
    <decision_coverage>per D-12-B3 (tap → 자세히 모달), D-12-B4 (mode 자동 분기), D-12-U1 (Phase 12.5 시각 언어 1:1 mirror)</decision_coverage>
  </verify>
  <done>
    - ForcePatternDetailModal.tsx 신설 + BottomSheet + handle + header + chips + body + 측정 설명 카드 + footer 안내 카드 + 닫기 버튼 박제.
    - tsc clean.
    - Per-task commit.
  </done>
</task>

<task id="T4" type="auto">
  <name>Task 4: VideoCompare.tsx slot 확장 + result.tsx 6 영역 layout 재정비 + userAnalyses.ts keypointReport null-guard 확장 검증 + enrichJoints 시뮬 주석 제거</name>
  <files>
    app/src/components/VideoCompare.tsx,
    app/src/app/analysis/result.tsx,
    app/src/lib/userAnalyses.ts
  </files>
  <action>
**(1) VideoCompare.tsx slot 확장 — children/overlay slot prop 추가:**
   - 기존 290 line `VideoCompareProps` 에 신설 prop 박제:
     ```
     export type VideoCompareProps = {
       leftLabel: string;
       rightLabel: string;
       leftUrl?: string;
       rightUrl?: string;
       /** Phase 12 신설 — 영상 위 absolute layer (KeypointOverlay 등). pointerEvents 'none'.
        * R7 정합 — **render prop**: (player: VideoPlayer | null) => React.ReactNode.
        * KeypointOverlay 가 same scope 안에서 player 받아 Wave 2 useEvent(player, 'timeUpdate') 구독.
        * result.tsx 가 player state 별도 관리 X (R7 dual-state pattern 방지).
        */
       leftOverlay?: (player: VideoPlayer | null) => React.ReactNode;
       rightOverlay?: (player: VideoPlayer | null) => React.ReactNode;
     };
     ```
   - `VideoSlot` 함수 시그니처에 `overlay?: (player: VideoPlayer | null) => React.ReactNode` 추가 (R7 render prop) + slotFrame 안에서 player 와 함께 호출:
     ```
     <View style={styles.slotFrame}>
       {url && player ? (
         <>
           <VideoView player={player} style={styles.video} ... />
           {overlay && (
             <View style={styles.overlayContainer} pointerEvents="none">
               {overlay(player)}
             </View>
           )}
         </>
       ) : (
         <View style={styles.slotEmpty}>...</View>
       )}
     </View>
     ```
   - styles 신설:
     ```
     overlayContainer: {
       position: 'absolute',
       top: 0, left: 0, right: 0, bottom: 0,
     },
     ```
   - 본체 함수 `VideoCompare` 내부 `<VideoSlot ... overlay={leftOverlay} />` + `<VideoSlot ... overlay={rightOverlay} />` 박제. VideoSlot 내부에서 `overlay(player)` callback 호출.
   - **기존 250ms `setInterval` 폴링 제거 — Wave 2 가 `useEvent(player, 'timeUpdate', ...)` 로 통합 (R7 정합)**. `current` / `playing` state 는 timeline UI 용도로 유지하되 setInterval → useEvent 마이그레이션.
   - result.tsx 의 사용 예 (참고):
     ```tsx
     <VideoCompare
       leftUrl={refUrl}
       rightUrl={userUrl}
       leftOverlay={(player) => (
         <KeypointOverlay player={player} keypointReport={result.keypointReport} mode={analysisDoc.mode} side="reference" />
       )}
       rightOverlay={(player) => (
         <KeypointOverlay player={player} keypointReport={result.keypointReport} mode={analysisDoc.mode} side="user" />
       )}
     />
     ```

**(2) userAnalyses.ts — Wave 0 박제 위 동작 확인 (no code change):**
   - Wave 0 T1 이 이미 `result?.keypointReport` null-guard 박제. Wave 1 = 변경 0. Audit only — typecheck PASS 확인.

**(3) result.tsx 6 영역 layout 재정비 (D-12-A1 박제):**
   - 기존 779 line 구조 유지. 6 영역 순서 재정비 + 신영역 끼워넣기.
   - **enrichJoints 시뮬 픽스처 주석 제거 (Pitfall 8 해소):**
     - line 78-110 의 "시뮬 픽스처" 주석 + currentAngle null 분기 → 단순화. Wave 0 wiring fix 후 `currentAngle != null` 가 박제됐으므로 `enrichJoints` 는 reference fallback 만 담당:
       ```
       function enrichJoints(joints, meanAngles) {
         if (!meanAngles) return joints;
         return joints.map((j) => {
           const target = meanAngles[j.key];
           if (typeof target !== 'number' || !Number.isFinite(target)) return j;
           if (typeof j.currentAngle === 'number' && Number.isFinite(j.currentAngle)) {
             const signed = j.currentAngle - target;
             return {
               ...j,
               targetAngle: target,
               deltaDeg: signed,
               direction: directionFor(j.key, signed) ?? j.direction,
             };
           }
           // Wave 0 wiring fix 후 정상 path 는 currentAngle 박제 — 본 분기는 fallback 만 (구 doc 호환).
           return { ...j, targetAngle: target };
         });
       }
       ```
     - "시뮬 픽스처" / "백엔드 NLF 가 아직 채우지 못해" / "그대로 둔다" 등 주석 line 모두 삭제 + 신 주석으로 교체: "// Wave 0 (Plan 12-01) wiring fix 후 j.currentAngle 박제. enrichJoints 는 reference meanAngles 가 박제됐을 때 targetAngle/delta/direction 만 보강 (구 doc 호환 fallback)."
   - **6 영역 layout 재정비 (RESEARCH §Code Examples §5 + UI-SPEC §3 박제):**
     - 영역 1: 점수 게이지 (기존 OctagonScore — 그대로).
     - 영역 2 신설: 영상 + 오버레이 carousel. Wave 1 = static fallback — KeypointOverlay 의 frameIndex=0 정적 박제. mode1 = split (leftUrl + rightUrl 둘 다 + 각각 leftOverlay/rightOverlay 박제) / mode3 = single (leftUrl + leftOverlay 박제). `leftOverlay` 박제 시 keypointReport 가 null 이면 KeypointOverlay 가 null return → caller 가 별도 placeholder ("키포인트 데이터 미가용" 12pt textWhite over videoBg) 노출. **토글 component 는 Wave 2 책임** — Wave 1 = 항상 visible=true 박제.
     - 영역 3 신설: Phase 9 원인 카드 Top-3. `result.forcePatternInference` 가 박제됐을 때만 렌더 — null 시 영역 자체 미렌더. findings.length 분기:
       - 0 → ForcePatternCard variant='big' 1개 + fallback finding (interpretation = `_FALLBACK_BODY` import, pattern='unknown', confidence=0, sourceSignal='high_jitter', phase='hold', reason='', jointHint=null, warnings=[]).
       - 1 → big card 1개만.
       - 2 → big card 1 + small card 1 (왼쪽).
       - 3 → big card 1 + small card 2.
     - 영역 4: 차원 카드 (기존 Phase 12.5 detail-scores 영역 그대로 — Wave 1 변경 0. ⚠ occlusion badge 는 Wave 2 책임).
     - 영역 5: 각도 가이드 (기존 angleGuide 출력 — Wave 0 wiring fix 후 8 body joint (어깨/골반/무릎/손 좌우) + axisData polyline 모두 "현재 N° → 기준 M°" 박제. "추정 N°" / ⓘ 는 Wave 2 책임).
     - 영역 6 (mode3 only): 성장 차트 (기존 GrowthChart — footer 직전 위치).
     - Footer: "분석 결과는 강사와 함께 확인해 보세요" (UI-SPEC §3 박제).
   - **ForcePatternDetailModal 박제 site:**
     - `const [detailFinding, setDetailFinding] = useState<ForcePatternFinding | null>(null);` 박제.
     - 영역 3 의 ForcePatternCard 의 onTap → setDetailFinding(finding).
     - result.tsx 맨 아래 `<ForcePatternDetailModal visible={detailFinding != null} finding={detailFinding} onClose={() => setDetailFinding(null)} />` 박제.

**제약사항:**
   - 기존 779 line 구조 유지 (D-12-A2) — component 분리 추가 X (영역 3/2 만 끼워넣기, 영역 4/5/6 기존 그대로).
   - 토큰만 사용 / 하드코딩 금지.
   - 이모지 금지 / 박제 단어 카피 안 들어감 ([[no-baekje-filler]]).
   - **mode 분기 자동화** (D-12-U3): result.tsx 신영역 안 mode 별 분기는 영상 split vs single 만 (cmp.mode === 'mode1' vs 'mode3'). ForcePatternCard interpretation 본문 등은 modeContext 박제 자동.
   - **NEVER**: KeypointOverlay 에 player prop 박제 (Wave 2 책임). Wave 1 = 정적 frameIndex=0.

**Per-task commit:** `feat(12-02): VideoCompare slot 확장 + result.tsx 6 영역 layout 재정비 + enrichJoints 시뮬 주석 제거 (D-12-A1/A2/A4)`
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck</automated>
    <ast_check>grep -c "시뮬 픽스처" app/src/app/analysis/result.tsx</ast_check>
    <decision_coverage>per D-12-A1 (6 영역 순서), D-12-A2 (779줄 구조 유지), D-12-A4 (신영역 component 분리), D-12-C1 (mode1 split / mode3 single), D-12-U3 (UI 분기 최소), D-12-U6 (keypoint 미가용 fallback)</decision_coverage>
  </verify>
  <done>
    - VideoCompare.tsx slot prop 박제 + tsc clean.
    - result.tsx 6 영역 layout 박제 + enrichJoints 시뮬 주석 0 (`grep -c "시뮬 픽스처" result.tsx` == 0).
    - ForcePatternCard + ForcePatternDetailModal 박제 site 박제 + findings 0/1/2/3 edge case 분기 정상.
    - tsc clean.
    - Per-task commit.
  </done>
</task>

<task id="T5" type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Wave 1 시각 UAT — Figma frame01 (mode1) reference 와 1:1 비교 + 6 영역 순서 + findings edge case 박제 확인</name>
  <what-built>
    - 3 신설 component (KeypointOverlay 정적 / ForcePatternCard variant big+small / ForcePatternDetailModal BottomSheet).
    - VideoCompare slot prop 확장.
    - result.tsx 6 영역 layout 재정비.
    - enrichJoints 시뮬 주석 제거.
  </what-built>
  <how-to-verify>
    1. `cd /Users/kimtaesung/Dev/SunityMotion/app && npx expo start --ios` 또는 `--android`.
    2. mode1 결과 화면 진입 — Wave 0 가 박제한 production 분석 1건 (정은지 reference 박제) 또는 mock fixture.
       - 6 영역 순서 확인: 점수 게이지 → 영상+오버레이 (정은지+사용자 split, 정적 8 body keypoint + axisData polyline 박제) → Phase 9 원인 카드 (findings 3개 시 big + small × 2) → 차원 카드 → 각도 가이드 (5 joint × 좌우 모두 "현재 N° → 기준 M°" 박제) → footer.
       - Figma frame01 (`94:508`) 와 1:1 visual 박제 — 카드 spacing 28pt / 카드 radius 15pt / 브랜드 컬러 #FF4B33 / 다크 배경 0 / 이모지 0.
       - 각 ForcePatternCard tap → ForcePatternDetailModal BottomSheet open + 닫기 / Backdrop tap close.
    3. mode3_first 결과 화면 진입 (인사이트 카드 "첫 분석이에요" + 사용자 single 영상 + findings 0/1/2/3 edge case 분기 확인).
    4. mode3_progress 결과 화면 진입 (인사이트 카드 "+N점 발전" + 단일 영상 + 성장 차트 footer 직전 노출).
    5. 안티 패턴 검수:
       - 다크 배경 0 (videoBg 영상 카드만 예외) — CLAUDE.md §4 / D-12-U4 정합.
       - 박제 단어 일반 동사 대용 0 — [[no-baekje-filler]] 정합.
       - 이모지 0 — CLAUDE.md §7 정합.
       - 시뮬 픽스처 잔존 0 — Pitfall 8 정합 (`grep -c "시뮬 픽스처" result.tsx` == 0).
       - 영상 위 angle 라벨 자체 계산 0 (Wave 2 가 박제 — Wave 1 은 KeypointOverlay 정적).
    6. typecheck: `cd app && npm run typecheck` — 0 error.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (예: "find[0] 큰 카드 spacing 박제 다름", "patternChip 색 빠짐", "ForcePatternDetailModal 핸들 미박제").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firestore → App (normalize) | Wave 0 박제 keypointReport null-guard 가 구 doc / 신 doc 둘 다 crash 0 |
| App → KeypointOverlay (props) | KeypointReport schema 박제 — UI 단 좌표 산출 X (안티 패턴 차단) |
| ForcePatternCard ↔ result.tsx (tap → modal) | tap state 박제 + finding props 그대로 전달 |
| result.tsx ↔ VideoCompare (slot pattern) | overlay slot prop (ReactNode) — pointerEvents 'none' 박제 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02-T3 | Tampering (mode 분기 misuse) | result.tsx 신영역 안 mode 분기 코드 | mitigate | D-12-U3 박제 정합 — UI 단 mode 분기는 영상 split vs single (cmp.mode === 'mode1') 만. interpretation 본문 / dimensionScore / angleGuide 모두 modeContext + backend 자동 분기. AST gate `grep -c "mode === 'mode3_first'" app/src/app/analysis/result.tsx` 결과 0 권고 (mode_first/progress 박제 분기는 모두 backend 의 modeContext 자동). |
| T-12-02-S1 | Spoofing (fake keypoint) | KeypointOverlay | mitigate | `props.keypointReport` 만 소비 — UI 단 좌표 산출 X (D-12 §12 안티 패턴). AST gate `grep -E "Math\.(sin|cos|atan2)" app/src/components/KeypointOverlay.tsx` 결과 0 권고. |
| T-12-02-V5 | Input Validation (V5) | userAnalyses.ts::normalize keypointReport null-guard | mitigate | Wave 0 박제 — keypointReport 부재 시 undefined 유지. typecheck strict mode + Phase 9 패턴 mirror. |
| T-12-02-T2 | Tampering (브랜드 색 변경) | colors.ts brand #FF4B33 | mitigate | CLAUDE.md §4 / D-12-U5 박제 — brand 토큰 변경 절대 금지. 신설 토큰은 모두 신규 키 (brandSoft / brandBg / softBg / estimateGray / progressGreen / progressRed / warnAmber / videoBg / textHi / textMid / textLo / trackBg). 기존 brand / brandGradientStart / brandGradientEnd 변경 0. |
| T-12-02-R1 | Repudiation (시뮬 픽스처 잔존) | result.tsx::enrichJoints | mitigate | Pitfall 8 박제 — Wave 0 wiring fix 후 시뮬 fallback 잔존 시 회귀 누락. AST gate `grep -c "시뮬 픽스처" app/src/app/analysis/result.tsx == 0`. T4 verify 박제. |

**Package Legitimacy Audit:** N/A — Wave 1 신설 package install 0 (react-native-svg 15.12.1 + expo-video ~3.0.16 + Ionicons 기존). RESEARCH §Standard Stack verified.
</threat_model>

<verification>
**Per-task automated:**
- T1: typecheck.
- T2: typecheck.
- T3: typecheck.
- T4: typecheck + ast_check (`grep -c "시뮬 픽스처" result.tsx == 0`).
- T5: checkpoint manual UAT (Figma frame01 mode1 / mode3_first / mode3_progress 박제 확인 + 안티 패턴 검수).

**Wave 1 종료 gate:**
1. `cd app && npm run typecheck` clean.
2. 5 atomic commit 박제 (per-task).
3. T5 belle UAT approved.
4. Wave 2 진입 가능 — KeypointOverlay 의 frameIndex prop / jointAngles prop site 박제 박제.

**Decision coverage:** D-12-A1 (T4), D-12-A2 (T4), D-12-A3 (per-task atomic commit), D-12-A4 (T1/T2/T3), D-12-B1 (T2), D-12-B2 (T2/T3), D-12-B3 (T3 + T4 modal site), D-12-B4 (T2 자동 mode 분기), D-12-C1 (T4 split vs single), D-12-C2 (T1 8 body keypoint + axisData polyline), D-12-C5 (T1 react-native-svg), D-12-U1 (T3 Phase 12.5 mirror), D-12-U3 (T4 UI 분기 최소), D-12-U4 (T5 light theme), D-12-U5 (T1 브랜드 토큰), D-12-U6 (T4 fallback placeholder).
</verification>

<success_criteria>
- 결과 화면이 D-12-A1 6 영역 순서로 렌더 + Figma frame01 1:1 시각 박제.
- ForcePatternCard 의 0/1/2/3 finding edge case 분기 정확.
- ForcePatternDetailModal tap → open / close 정상.
- VideoCompare slot prop 박제 + KeypointOverlay 정적 8 body keypoint + axisData polyline 박제 (Wave 2 sync 진입 site 박제).
- enrichJoints 의 시뮬 픽스처 주석 0 + Wave 0 실측 wiring 위 동작.
- belle UAT approved.
</success_criteria>

<follow_ups>
- **Codex cross-AI plan-review checkpoint** (선택 — [[cross-ai-plan-review-good]]): Wave 1 commit 후 Codex 가 6 영역 layout + slot pattern + edge case 분기 검증.
- **belle UAT (T5)**: mode1/mode3_first/mode3_progress 각 1 건 + 안티 패턴 검수 + Figma 4-frame 박제.
- **Wave 2 진입 차단 해소**: KeypointOverlay 의 frameIndex prop / jointAngles prop site 박제 박제 → Wave 2 가 useEvent + delta 강조 박제 site 교체.
</follow_ups>

<rollback>
- 5 atomic commit — rollback path = 마지막 N 개 `git revert` (T1-T5 역순).
- Rollback 시 회복 상태 = Wave 0 close-out 기준 (Phase 9 + Phase 12.5 + Phase 12 Wave 0 schema lockstep).
- enrichJoints 의 시뮬 픽스처 주석은 rollback 시 다시 등장 — 회귀 박제 확인.
</rollback>

<output>
Create `.planning/phases/12-realmeasurement-keypoint/12-02-SUMMARY.md` when done.
</output>
</content>
</invoke>