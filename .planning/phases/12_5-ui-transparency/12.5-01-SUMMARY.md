---
phase: 12.5
plan: 12.5-01
status: closed-superseded
closed: 2026-08-31
closed_by: quick/260831-c3l-6-summary (미실행 꼬리 삼진 분류)
basis: "belle 2026-08-30 두 목적 — Mode1 잘 분석되는가 / Mode3 발전 확인되는가"
---

# 12.5-01 SUMMARY (closure stub — 실행 없이 닫음)

## 원 플랜 목표

result.tsx 차원별 카드에 baseline 카피 + 정수 weightPercent(합 100) + 산식-정합 deficit 요약 표시, backend assemble.py 가 `dimensionExplanation` 출력, contract 3면(TS/Python/contract.md) lockstep + TestFlight 빌드 12 (2026-06-07 작성).

## 처분

**사문 (closed-superseded).** backend 조립(assemble.py)·타입(analysis.ts) 면은 후속 구현으로 잔존 출력되나, 화면(result.tsx) 면은 구현됐다가 D-03/D-12 계열 결정으로 **제거**됐다 (제거 주석 result.tsx:1469 실측, app/src 내 weightPercent 소비처 0). 원 플랜의 UI 투명성(수치 표시) 목표는 후속 결정으로 사문.

## 실측 (2026-08-31, 이 스텁 작성 시 직접 확인)

```
$ grep -l 'dimensionExplanation' backend/shared/python/sunity_shared/analysis/assemble.py \
    app/src/app/analysis/result.tsx app/src/types/analysis.ts
backend/shared/python/sunity_shared/analysis/assemble.py
app/src/app/analysis/result.tsx
app/src/types/analysis.ts

$ grep -n 'dimensionExplanation' app/src/app/analysis/result.tsx
1469:  // (구 dims/dimensionExplanation/detailDim/DimensionDetailModal 제거 — D-03/D-12.
      (result.tsx 의 유일 매치 = 제거 주석 1줄)

$ grep -l 'weightPercent' backend/shared/python/sunity_shared/analysis/assemble.py \
    app/src/app/analysis/result.tsx app/src/types/analysis.ts
backend/shared/python/sunity_shared/analysis/assemble.py
app/src/types/analysis.ts
      (result.tsx 미매치 — grep -c = 0)

$ grep -rn 'weightPercent' app/src
app/src/types/analysis.ts:288, 290, 295 (타입 정의·주석 3줄뿐 — UI 소비처 0)
```

## 관측과 해석

- 관측: 조립·타입 면에는 dimensionExplanation/weightPercent 가 존재한다. 화면 면에는 표시 코드가 없고, 제거 사실을 적은 주석(D-03/D-12 지목)만 있다.
- 관측: 오케스트레이터/플래너의 초기 실측("3면 전부 존재")은 오진이었다 — `grep -l 'dimensionExplanation'` 이 result.tsx 의 **제거 주석**에 매치된 것을 "화면 면 존재"로 오독한 것. 다음 세션 주의: 파일 매치(grep -l) != 살아있는 소비처. 소비처 판정은 매치 라인을 열어 확인할 것.
- 내 해석: 처분 라벨(사문)은 어느 근거로든 성립한다 — "이미 구현돼 있어서"가 아니라 "구현 후 belle 결정(D-03/D-12, 수치 표기 제거 계열)으로 화면 표시가 철회돼서"다. 이 플랜을 되살리는 것은 belle 의 수치 표시 방침 재론을 뜻하므로 belle 이 먼저 꺼낼 때만.
