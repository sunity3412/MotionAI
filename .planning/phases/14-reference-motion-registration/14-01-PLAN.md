---
phase: 14-reference-motion-registration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_reference_backfill.py
  - backend/tests/phase14/__init__.py
  - app/scripts/audit-reference-fields.mjs
  - docs/contract.md
  - app/src/types/analysis.ts
  - app/src/lib/referenceMotions.ts
autonomous: true
requirements: [REF-01]
must_haves:
  truths:
    - "A Firestore-read audit (no GPU) lists, for all 11 references, which already carry bodyNormalizationProfile vs which are missing it (resolves A2)"
    - "Failing/scaffold tests exist asserting D-01 compute parity under the reference-v1 pinned config (REFERENCE_V1_FORCE_CONFIG: recognizer=FallbackRecognizer, technique_profile=None, preflight_label_gate_passed=None, layer-2 off — provably exact for that config, NOT 'student path exact'), plus an env-flip test proving the result CHANGES under PREFLIGHT_LABEL_GATE_PASSED=1, SC#4 single-view graceful, and the D-02 stored-sufficient vs hybrid verdict"
    - "The parity + env-flip RED-target guard is conditional on PHASE14_REQUIRE_BACKFILL_HELPER (importorskip when unset, hard-fail-on-missing when =1) so 14-02 can make a skipped D-01 parity fatal (R4-1)"
    - "The D-01 parity reference passes motion_id=None into compute_force_signals AND infer_force_direction_pattern (FallbackRecognizer profile motion_id is None; the force motion_id boost does NOT fire), asserting the reference-v1 fallback/null force-config choice explicitly (R4-2)"
    - "Validator-shape fixtures prove the forceDirectionPattern mirror uses the SCOPED force-pattern validator: a VALID findings[].warnings:string[] payload is accepted by _validate_force_pattern_inference and an INVALID nested/unknown-key payload is rejected (R3-1)"
    - "The helper parity test asserts the production call boundary: compute_reference_downstream is fed an orchestrator-built vertical-fallback pole_axis_measurement and the STORED active angles (matching _process passing inputs.pole_axis_measurement) (R6)"
    - "docs/contract.md §3 and app/src/types/analysis.ts ReferenceMotion both declare the new optional fields (techniqueProfile, forceDirectionPattern) and a single-view flag, in 3-way (or 4-way if a Python ReferenceMotion mirror is found) lockstep"
    - "app/src/lib/referenceMotions.ts normalize() surfaces the new optional fields (bodyNormalizationProfile, techniqueProfile, forceDirectionPattern, captureViews) null-safely so the type/contract/normalize stay in lockstep and the hook does not silently strip them (R2-4)"
  artifacts:
    - path: "backend/tests/test_reference_backfill.py"
      provides: "Wave-0 test harness: reference-v1 pinned-config parity + env-flip divergence (env-gated strict import R4-1, motion_id=None parity R4-2), single-view graceful, D-02 verdict, R3-1 forceDirectionPattern scoped-validator fixtures"
      min_lines: 60
    - path: "app/scripts/audit-reference-fields.mjs"
      provides: "Read-only audit of all 11 reference docs (which fields present)"
    - path: "docs/contract.md"
      provides: "§3 ReferenceMotion field table updated with new optional fields"
      contains: "techniqueProfile"
    - path: "app/src/types/analysis.ts"
      provides: "ReferenceMotion interface with techniqueProfile, forceDirectionPattern, captureViews"
      contains: "forceDirectionPattern"
    - path: "app/src/lib/referenceMotions.ts"
      provides: "normalize() returns the new optional fields (bodyNormalizationProfile, techniqueProfile, forceDirectionPattern, captureViews)"
      contains: "forceDirectionPattern"
  key_links:
    - from: "backend/tests/test_reference_backfill.py"
      to: "backend/functions/pipeline/app.py::_process downstream functions"
      via: "import-by-path of pipeline module + sunity_shared.analysis functions"
      pattern: "measure_body_profile|compute_force_signals|FallbackRecognizer"
    - from: "app/src/lib/referenceMotions.ts"
      to: "app/src/types/analysis.ts ReferenceMotion"
      via: "normalize() return object surfaces the new optional fields"
      pattern: "forceDirectionPattern|techniqueProfile|captureViews"
---

<objective>
Lay the verification + contract foundation for the reference downstream backfill. This is the Wave-0
slice: a no-GPU Firestore audit that resolves which of the 11 references already have a body profile
(A2), a pytest harness that pins D-01 compute parity under the reference-v1 pinned config
(REFERENCE_V1_FORCE_CONFIG), proves an env flip diverges, proves SC#4 (single-view graceful), the
D-02 verdict (stored-sufficient vs hybrid), AND the R3-1 forceDirectionPattern scoped-validator fixtures
(valid findings[].warnings accepted / nested-or-unknown rejected), and a 3-way contract lockstep adding
the new optional reference fields. No Pod, no Firestore writes — pure read + scaffold so Plan 14-02's
compute and Plan 14-03's seed have a green target and a stable contract.

Purpose: The backfill must compute outputs through the SAME `sunity_shared` functions the student
`_process` calls (D-01) but under an explicitly PINNED config (REFERENCE_V1_FORCE_CONFIG), because the
production `_process` passes an env-driven `_preflight_label_gate_passed()` and a Layer-2-conditional
`technique_profile` — so the correct contract is "same functions under reference-v1 pinned config",
NOT "student path exact" (R2). This plan makes that pinned config testable (provably exact for the
config), proves an env flip would diverge (so the test does not hide the config divergence), proves the
forceDirectionPattern reference mirror is validated by the scoped force-pattern validator (R3-1 — a valid
findings[].warnings:string[] must be ACCEPTED, a nested/unknown shape REJECTED), gates the parity RED
target behind PHASE14_REQUIRE_BACKFILL_HELPER so 14-02 can make a skipped D-01 parity fatal (R4-1),
asserts the reference-v1 fallback/null force-config choice (motion_id=None) explicitly (R4-2), and makes
the new fields legal in the contract AND surfaced through `referenceMotions.ts normalize()` (R2-4)
BEFORE any data is written.
Output: `backend/tests/test_reference_backfill.py` (+ `phase14/__init__.py`), `app/scripts/audit-reference-fields.mjs`,
contract + TS + normalize() lockstep edits.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-reference-motion-registration/14-CONTEXT.md
@.planning/phases/14-reference-motion-registration/14-RESEARCH.md
@.planning/phases/14-reference-motion-registration/14-PATTERNS.md
</context>

<phase_goal>
**As a** Sunity 운영자(belle), **I want to** 기존 11개 정은지 reference 가 meanAngles·EXTEND·BodyNormalizationProfile·ForceDirectionPattern 을 모두 갖추게 만들고, **so that** Mode 1 비교가 학생과 동일한 sunity_shared 함수를 reference-v1 pinned config 로 거쳐(위양성 방지) 바로 동작한다.
</phase_goal>

<tasks>

<task type="auto">
  <name>Task 1: Read-only Firestore audit of all 11 references (resolves A2)</name>
  <files>app/scripts/audit-reference-fields.mjs</files>
  <read_first>
    - app/scripts/seed-reference-body-profile.mjs (Step 1/2/3 structure, validate-before-init, ADC import lazy — lines 135-181)
    - app/scripts/verify-firebase.mjs (Firebase Admin read pattern, projectId 'sunity-ai-coach')
    - .planning/phases/14-reference-motion-registration/14-RESEARCH.md (Runtime State Inventory — the 6 later-added motions likely lack bodyNormalizationProfile)
  </read_first>
  <action>
    Create a Node admin script that READS all 11 reference docs (the union of the original 5 —
    ref-climb, ref-foxtop, ref-foxtop-split, ref-invert, ref-sideway-spin — and the 6 later-added:
    ref-combo, ref-elbow-twist-sister, ref-kip-up, ref-pdshape, ref-peter-pan, ref-power-spin) from
    Firestore collection `reference` and prints a per-motion presence table for these fields:
    meanAngles, angles, anglesJointKeys, anglesFrames, bodyNormalizationProfile, bodyComparisonSourcePose,
    techniqueProfile, forceDirectionPattern, captureViews, activeVersion. Mirror
    seed-reference-body-profile.mjs structure: define the full 11-id constant at top (no default-5 subset
    — cite [[reference-library-phase4-all11]] and Pitfall 1 in a Korean comment), lazy-import
    firebase-admin/app + firebase-admin/firestore inside an async main, init with applicationDefault() +
    projectId 'sunity-ai-coach'. READ ONLY — no set/merge. Print a final summary: per-field present-count
    AND, per the R3 seeder skip rule, a `completeRequiredSet` count = references where ALL of {meanAngles,
    techniqueProfile, bodyNormalizationProfile, forceDirectionPattern, captureViews} are present (so 14-02
    Task 2 can distinguish skipComplete vs repairMissing). Log keys-not-values (never print SA/ADC
    contents, security threat: Information Disclosure). Cite D-04 (admin CLI) and A2 (open question) in the
    header comment. This script is READ-only so it can run today against current Firestore state.
  </action>
  <verify>
    <automated>node --check app/scripts/audit-reference-fields.mjs && grep -c "ref-combo\|ref-kip-up\|ref-pdshape\|ref-peter-pan\|ref-power-spin\|ref-elbow-twist-sister" app/scripts/audit-reference-fields.mjs | grep -qv '^0$' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `node --check app/scripts/audit-reference-fields.mjs` exits 0 (valid JS)
    - All 11 motion IDs (union of 5 + 6) appear literally in the script
    - Script uses `.get()` reads only; no `.set(`/`.merge`/`batch.set(` occurrences (grep `set(` count = 0)
    - Summary prints both per-field present-counts and a `completeRequiredSet` count (all 5 Phase-14 required fields present) — feeding the 14-02 skip/repair logic
    - Header comment cites D-04 + [[reference-library-phase4-all11]] + A2
  </acceptance_criteria>
  <done>Audit script is syntactically valid, references all 11 motion IDs, performs reads only, and prints a field-presence table + missing-field summary + completeRequiredSet count.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wave-0 backfill test harness (reference-v1 pinned-config parity + env-flip divergence, env-gated strict import R4-1, motion_id=None parity R4-2, SC#4 graceful, D-02 verdict, R3-1 forceDirectionPattern scoped-validator fixtures)</name>
  <files>backend/tests/phase14/__init__.py, backend/tests/test_reference_backfill.py</files>
  <behavior>
    - D-01 reference-v1 pinned-config parity: feeding one synthetic `pose_frames` + STORED `angles` fixture + an orchestrator-built vertical-fallback `pole_axis_measurement` to the backfill compute helper produces dataclasses identical to calling the SAME `_process` downstream functions (measure_body_profile, FallbackRecognizer().recognize, compute_force_signals, infer_force_direction_pattern) directly in the same order with ONE temporal_fill — where compute_force_signals is invoked under REFERENCE_V1_FORCE_CONFIG (recognizer=FallbackRecognizer, technique_profile=None, preflight_label_gate_passed=None, layer-2 off). This is "same functions under the reference-v1 pinned config", provably exact FOR THAT CONFIG — NOT "student path exact" (R2).
    - R4-1 env-gated strict import (the parity + env-flip tests' RED-target guard is conditional): the test reads `os.environ.get("PHASE14_REQUIRE_BACKFILL_HELPER")`. When NOT set → `pytest.importorskip("backfill_reference_downstream")` (Wave-0 scaffold/RED behavior preserved — the helper does not yet exist). When set to `"1"` → import `compute_reference_downstream` from `backfill_reference_downstream` NORMALLY and FAIL (not skip) if the helper is missing. Document this in a test comment: 14-02's verification command runs with `PHASE14_REQUIRE_BACKFILL_HELPER=1` so once the helper exists the parity/env-flip tests can no longer silently SKIP (R4-1). This keeps 14-01 a green-or-skip scaffold while making 14-02's implementation gate strict (reviewer's preferred option 2).
    - R4-2 motion_id=None propagation parity (the v1 fallback/null force-config choice is machine-checked, NOT implicit): the D-01 parity direct-call reference MUST pass `motion_id=None` into BOTH `compute_force_signals` and `infer_force_direction_pattern` (mirroring the helper, which resolves `motion_id=getattr(profile,"motion_id",None)` from the FallbackRecognizer profile → None — verified: FallbackRecognizer.recognize does not set motion_id, so TechniqueProfile.motion_id stays None). Add an explicit assertion that `FallbackRecognizer().recognize(angles).motion_id is None` AND that the force-pattern motion_id boost does NOT fire (force_pattern._apply_motion_id_boost only runs when motion_id is not None — so the known reference id is intentionally ignored for the reference v1 force fields). Comment: reference v1 force fields are generated WITHOUT known-reference contact/boost semantics (forceMotionIdSource="fallback_profile_motion_id", forceMotionId=null) — Phase 15 must NOT assume the reference used selected-referenceMotionId force semantics (R4-2).
    - Env-flip divergence (R2 — proves the test does not hide the divergence): a second assertion calls compute_force_signals with `preflight_label_gate_passed=True` (the value an env `PREFLIGHT_LABEL_GATE_PASSED=1` would yield) and asserts the force-signals result CHANGES vs the pinned `None` reference (e.g. phase-boundary/confidence/warnings differ). The test therefore deliberately demonstrates that the reference uses a PINNED config that diverges from a non-default student env. Add a comment: Phase 15 must treat reference force fields as produced under REFERENCE_V1_FORCE_CONFIG when comparing to students.
    - Production call-boundary parity (R6): the helper is fed `pole_axis_measurement` and `angles` as injected arguments (orchestrator builds the vertical fallback), matching `_process` passing `inputs.pole_axis_measurement` + `angles` — the test asserts this call boundary, not an internally-fabricated measurement.
    - R3-1 forceDirectionPattern scoped-validator fixtures: import the existing `_validate_force_pattern_inference` from `sunity_shared.firestore_admin` and assert (a) a VALID forceDirectionPattern dict whose `findings[0].warnings = ["axis_signal_unavailable"]` (top-level + finding fields per the contract) is ACCEPTED (no raise); (b) an INVALID payload — `findings[0].warnings = [["nested"]]` (nested list) OR a finding with an unknown key — is REJECTED (raises ValueError). This proves the reference mirror MUST use the scoped validator, not the generic `_validate_flat_dict_no_nested_array` (which would reject the valid findings[].warnings). Comment: 14-02 helper applies `_validate_force_pattern_inference` to forceDirectionPattern and the generic flat validator to meanAngles/techniqueProfile/bodyNormalizationProfile (R3-1).
    - SC#4 single-view graceful: with vertical-fallback PoleAxis (axis_vector=(0,1,0), source="vertical_fallback") and pole `line=None`, compute_force_signals contact-stability metrics return None and emit a `pole_line_missing` warning — no exception raised.
    - D-02 STORED-SUFFICIENT: EXTEND (FallbackRecognizer().recognize) and meanAngles (np.nanmean over angles) computed from the STORED `angles` alone match the values produced via the full pose_frames path. (Note: in 14-02 these are computed from `reference.phase4_v1.angles`, never from re-run angles.)
    - D-02 HYBRID-NEEDED: measure_body_profile / compute_force_signals require live pose_frames (raw keypoints_3d + per-keypoint confidence); a from-flat reconstruction (pole-aligned, no confidence) is provably insufficient (assert it raises or yields NaN/degraded output vs the live-frames result).
  </behavior>
  <read_first>
    - backend/tests/test_pipeline_body_profile_injection.py (pipeline import-by-path + synthetic PoseFrame fixtures, lines 30-60)
    - backend/functions/pipeline/app.py (_extract_video_analysis_inputs :1135, _process downstream order: EXTEND :1610, force_signals :1899-1908, force_pattern :1932; _preflight_label_gate_passed() :851-871 returns True/False/None from env; _process passes preflight_label_gate_passed=_preflight_label_gate_passed() and technique_profile=profile if layer2_recognizer is not None else None — confirm the reference pin diverges from this)
    - backend/shared/python/sunity_shared/analysis/technique.py (FallbackRecognizer.recognize, angles-only, lines 92-113; TechniqueProfile.motion_id defaults None and FallbackRecognizer does NOT set it :57-113 — R4-2)
    - backend/shared/python/sunity_shared/analysis/body_normalization_measurer.py (measure_body_profile reads frame.keypoints_3d, lines 78-110)
    - backend/shared/python/sunity_shared/analysis/force_signals.py (compute_force_signals signature incl. preflight_label_gate_passed + technique_profile + motion_id kwargs; preflight gate effect 1579-1636; graceful line=None contact path 1443-1542; motion_id=None unrecognized fallback contact path 1447-1458; double-smooth ban 1596-1598)
    - backend/shared/python/sunity_shared/analysis/force_pattern.py (infer_force_direction_pattern consumes only the report, lines 642-666; _apply_motion_id_boost only fires when motion_id is not None :720-722 — R4-2)
    - backend/shared/python/sunity_shared/firestore_admin.py (_validate_force_pattern_inference :343 + _validate_force_pattern_finding_dict :293 — the scoped validator + finding-key whitelist {pattern, phase, sourceSignal, reason, interpretation, confidence, jointHint, warnings} the R3-1 fixtures exercise)
    - backend/shared/python/sunity_shared/analysis/pose_frame.py (PoseFrame fields, to_coco17_array, lines 204-354)
    - backend/shared/python/sunity_shared/analysis/pole_geometry.py (build_pole_axis_measurement, vertical fallback line=None — the orchestrator builds this and injects it)
  </read_first>
  <action>
    Create `backend/tests/phase14/__init__.py` (empty package marker) and
    `backend/tests/test_reference_backfill.py`. Reuse the import-by-path + synthetic-fixture pattern
    from test_pipeline_body_profile_injection.py to build a deterministic synthetic `pose_frames`
    list[PoseFrame] (with raw keypoints_3d + per-keypoint confidence + keypoints_3d_pole_aligned +
    reliability) without GPU. Define a module-level `REFERENCE_V1_FORCE_CONFIG = {"recognizer":
    "FallbackRecognizer", "techniqueProfileForForceSignals": None, "preflightLabelGatePassed": None,
    "forceSignalsLayer2Enabled": False, "forceMotionIdSource": "fallback_profile_motion_id",
    "forceMotionId": None}` (R4-2 — the fallback/null force-config choice is recorded in the config object,
    not just behavior) and reference it in the parity assertions. Tests must reference a single backfill
    compute helper by an agreed import name —
    `backfill_reference_downstream.compute_reference_downstream(pose_frames, *,
    pole_axis_measurement, angles, fps=9.0, motion_id=None, mode_context="mode1",
    force_config=REFERENCE_V1_FORCE_CONFIG)` (Plan 14-02 creates the implementation; this test is the RED
    target).

    R4-1 ENV-GATED STRICT IMPORT: do NOT unconditionally `importorskip`. At the top of the parity/env-flip
    test module read `_REQUIRE = os.environ.get("PHASE14_REQUIRE_BACKFILL_HELPER")`. If `_REQUIRE != "1"`:
    `compute_reference_downstream = pytest.importorskip("backfill_reference_downstream").compute_reference_downstream`
    (scaffold/RED behavior — skips while the helper does not exist). If `_REQUIRE == "1"`: import normally
    (`from backfill_reference_downstream import compute_reference_downstream`) so a MISSING helper raises an
    ImportError/collection error (FAIL, not skip). Add a Korean comment: 14-02 의 검증 커맨드는
    `PHASE14_REQUIRE_BACKFILL_HELPER=1` 로 실행되어 helper 가 생긴 뒤에는 parity/env-flip 가 skip 될 수 없다 (R4-1).
    Implement the SC#4 + D-02 + R3-1 tests directly against the production sunity_shared functions /
    validators (no skip — they assert the library behavior the helper must reproduce, independent of the
    env gate).

    For the D-01 parity test, build the direct-call reference by invoking compute_force_signals with
    `technique_profile=None`, `preflight_label_gate_passed=None` AND `motion_id=None`
    (REFERENCE_V1_FORCE_CONFIG — recognizer=Fallback, layer-2 off, fallback/null motion_id per R4-2), feed
    the helper an orchestrator-built vertical-fallback `pole_axis_measurement`
    (build_pole_axis_measurement(line=None)) and the STORED `angles`, then assert the helper's
    force-signals report equals that reference (proving the helper passes the SAME pinned config AND
    matches the production call boundary, R6). R4-2: also call infer_force_direction_pattern with
    `motion_id=None` in the reference, and assert `FallbackRecognizer().recognize(angles).motion_id is None`
    so the fallback/null force-config choice is machine-checked, not implicit; add a comment that the
    force-pattern motion_id boost does not fire for reference v1 and Phase 15 must not assume
    selected-referenceMotionId force semantics. Add an EXPLICIT env-flip test: call compute_force_signals
    with `preflight_label_gate_passed=True` and assert the result DIFFERS from the pinned-None reference
    (R2 — the divergence is encoded in the test, with a comment that Phase 15 must treat reference force
    fields as REFERENCE_V1_FORCE_CONFIG-produced). Add the R3-1 forceDirectionPattern scoped-validator
    fixtures: import `_validate_force_pattern_inference` from `sunity_shared.firestore_admin`; assert a
    VALID dict {version, findings:[{pattern, phase, sourceSignal, reason, interpretation, confidence,
    jointHint, warnings:["axis_signal_unavailable"]}], overallConfidence, modeContext, warnings:[...]} is
    accepted (no raise), and assert an INVALID dict — `findings[0].warnings=[["nested"]]` OR a finding with
    an unknown key like `unexpectedScalar` — raises ValueError. Comment that this is the contract 14-02's
    `update_reference_downstream_data` + the JS seeder must honor (R3-1: scoped validator for
    forceDirectionPattern, generic flat validator for the other three downstream dicts). For SC#4 build the
    vertical-fallback PoleAxis + build_pole_axis_measurement(line=None) and assert compute_force_signals
    returns graceful None contact metrics + `pole_line_missing` in warnings without raising. For D-02
    STORED-SUFFICIENT assert FallbackRecognizer().recognize(angles) and np.nanmean(angles, axis=0) match
    across two angle paths. For D-02 HYBRID assert a from-flat (pole-aligned, confidence-stripped)
    reconstruction fed to measure_body_profile diverges from the live-frames result (proving re-inference
    is required for body/force inputs only). Pass already-temporal_fill'ed angles (ONE smoothing — cite
    Pitfall 3 / REVIEWS R5 in a comment). No fabricated severity/finding numbers (force_pattern
    fabrication gate). Korean comments cite D-01/D-02/R2/R3-1/R4-1/R4-2 and the pinned-config rationale.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_reference_backfill.py -q 2>&1 | grep -E "passed|skipped|error" </automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && python -m pytest tests/test_reference_backfill.py -q` collects without import/collection errors (with PHASE14_REQUIRE_BACKFILL_HELPER UNSET: parity/env-flip skip via importorskip, RED-target documented in a comment)
    - SC#4 test and both D-02 tests PASS against production sunity_shared functions (no skip)
    - The R3-1 forceDirectionPattern scoped-validator fixtures PASS against `_validate_force_pattern_inference` (no skip): valid `findings[0].warnings=["axis_signal_unavailable"]` accepted; invalid `findings[0].warnings=[["nested"]]` (or unknown finding key) rejected — with a comment that 14-02 applies the scoped validator to forceDirectionPattern and the generic flat validator to the other three dicts
    - R4-1: the parity/env-flip RED-target import is gated on `PHASE14_REQUIRE_BACKFILL_HELPER` — unset → `pytest.importorskip("backfill_reference_downstream")`; `=1` → normal import that FAILS (not skips) when the helper is missing; a comment documents that 14-02 runs the gate with `PHASE14_REQUIRE_BACKFILL_HELPER=1`
    - The D-01 reference-v1 pinned-config parity test references REFERENCE_V1_FORCE_CONFIG and asserts the production call boundary (injected `pole_axis_measurement` + `angles`, R6)
    - R4-2: the parity reference passes `motion_id=None` into BOTH `compute_force_signals` AND `infer_force_direction_pattern`; an assertion confirms `FallbackRecognizer().recognize(angles).motion_id is None` (force motion_id boost does not fire); REFERENCE_V1_FORCE_CONFIG records `forceMotionIdSource="fallback_profile_motion_id"` + `forceMotionId=None`; a comment notes Phase 15 must not assume selected-referenceMotionId force semantics
    - An explicit env-flip test asserts compute_force_signals with `preflight_label_gate_passed=True` DIFFERS from the pinned `None` reference (R2 divergence encoded in the test, not just prose; comment notes Phase 15 must treat reference force fields as REFERENCE_V1_FORCE_CONFIG-produced)
    - `grep -c temporal_fill backend/tests/test_reference_backfill.py` ≥ 1 with a comment citing single-smoothing ban
  </acceptance_criteria>
  <done>The Wave-0 harness collects cleanly; SC#4 + D-02 + R3-1 scoped-validator fixtures pass against the real library; the reference-v1 pinned-config parity test is in place pointing at the Plan 14-02 helper (env-gated strict import R4-1, motion_id=None parity R4-2, provably exact for REFERENCE_V1_FORCE_CONFIG, asserting the production call boundary), and an env-flip test proves the config diverges from a non-default student env.</done>
</task>

<task type="auto">
  <name>Task 3: 3-way (or 4-way) contract lockstep + normalize() field surfacing — add new optional reference fields</name>
  <files>docs/contract.md, app/src/types/analysis.ts, app/src/lib/referenceMotions.ts</files>
  <read_first>
    - app/src/types/analysis.ts (ReferenceMotion interface lines 398-447 — meanAngles/bodyNormalizationProfile/bodyComparisonSourcePose already present; techniqueProfile/forceDirectionPattern absent)
    - docs/contract.md (§3 ReferenceMotion field table lines 105-128; §9.11 forceDirectionPattern shape if present)
    - backend/shared/python/sunity_shared/analysis/force_pattern.py (ForcePatternInference dataclass line 229 — field set for the contract entry)
    - backend/shared/python/sunity_shared/analysis/technique.py (TechniqueProfile / joint_expectations shape for the EXTEND field)
    - app/src/lib/referenceMotions.ts (normalize() returns a FIXED subset and currently DROPS bodyNormalizationProfile even though analysis.ts declares it — lines 70-155; this task must add the new fields to the return object)
  </read_first>
  <action>
    First run the lockstep-scope recheck (R8): execute
    `rg "ReferenceMotion|forceDirectionPattern|techniqueProfile|captureViews"` across the repo to confirm
    no Python `models`/Firestore validator enforces a typed ReferenceMotion mirror. If NONE exists, do a
    3-way lockstep (contract.md + analysis.ts + reuse the Python force_pattern.py/technique.py source
    shapes). If a Python ReferenceMotion mirror/validator IS found, EXPAND to a 4-way lockstep and edit it
    too (record the finding in the SUMMARY). Then add to BOTH docs/contract.md §3 ReferenceMotion field
    table AND the app/src/types/analysis.ts ReferenceMotion interface (per CLAUDE.md Cross-cutting):
    (1) `techniqueProfile?` — the EXTEND joint_expectations profile (FallbackRecognizer output;
    nullable, backfill-populated), (2) `forceDirectionPattern?` — the ForcePatternInference shape
    (reuse the existing ForcePatternInference TS type if declared, else reference it; nullable),
    (3) a single-view flag `captureViews?: number` (set to 1 for all 11) per D-03/SC#4 — confirm name
    `captureViews` (RESEARCH A5 / Open-Q4 default). Follow the existing comment style that cites the
    phase/plan + memory (mirror the `bodyNormalizationProfile?` Phase 6 comment at line 427). All new
    fields are OPTIONAL/nullable so pre-backfill references and `normalize()` stay valid. Do NOT add any
    T-scaled per-frame array (Pitfall 5 — 40k index). Cite D-03 (single-view), SC#2 (field set), and
    the lockstep rule in comments.

    THEN (R2-4 — the missing third lockstep edge): update `app/src/lib/referenceMotions.ts normalize()`
    so the returned object SURFACES the new fields instead of silently stripping them. The current
    `normalize()` returns a fixed subset that already DROPS `bodyNormalizationProfile` even though the
    type declares it; add null-safe extraction + return of: `bodyNormalizationProfile`, `techniqueProfile`,
    `forceDirectionPattern`, `captureViews`. Reuse the existing defensive-normalize idiom (typeof/object
    guards, `Array.isArray`, return undefined/null on malformed — mirror the `referenceKeypointReport`
    null-guard block at lines 106-130). Keep each new field optional/null-safe so pre-backfill docs stay
    valid. Korean comment cites R2-4 + the type/contract/normalize 3-way lockstep rule (CLAUDE.md
    Cross-cutting). Do NOT add a T-scaled per-frame array to the normalized object (Pitfall 5).
  </action>
  <verify>
    <automated>cd app && npm run typecheck && grep -q "forceDirectionPattern" src/types/analysis.ts && grep -q "captureViews" src/types/analysis.ts && grep -q "techniqueProfile" ../docs/contract.md && grep -Eq "bodyNormalizationProfile|techniqueProfile|forceDirectionPattern|captureViews" src/lib/referenceMotions.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The R8 recheck (`rg "ReferenceMotion|forceDirectionPattern|techniqueProfile|captureViews"`) was run; if a Python ReferenceMotion mirror/validator exists it was edited too (4-way) and the finding is recorded
    - `cd app && npm run typecheck` passes (tsc --noEmit clean)
    - `app/src/types/analysis.ts` ReferenceMotion declares `techniqueProfile?`, `forceDirectionPattern?`, `captureViews?` (all optional)
    - `docs/contract.md` §3 lists meanAngles + bodyNormalizationProfile + techniqueProfile + forceDirectionPattern + captureViews (closing the Open-Q3 contract gap)
    - `app/src/lib/referenceMotions.ts normalize()` return object includes the four field names `bodyNormalizationProfile`, `techniqueProfile`, `forceDirectionPattern`, `captureViews` (null-safe) — grep confirms all four appear in the file's normalize return (R2-4); the hook no longer strips them
    - New field comments cite the phase/plan + D-03/SC#2 + lockstep rule
  </acceptance_criteria>
  <done>Contract, TS interface, and `referenceMotions.ts normalize()` (and any Python ReferenceMotion mirror found via the R8 recheck) declare AND surface the new optional reference fields in lockstep; typecheck is clean; pre-backfill references remain valid (all fields optional); the hook no longer silently drops the new fields (R2-4).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI → Firestore (read) | Audit script reads Firestore via Firebase Admin (ADC); bypasses security rules by design (server-side only) |
| repo → contract consumers | Contract/TS/normalize edits change the shape app + GET /reference passthrough rely on |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Information Disclosure | audit-reference-fields.mjs (ADC/SA creds) | mitigate | Log keys-not-values; never print ADC/SA contents (reprocess_* T-04-W5-01 precedent) |
| T-14-02 | Tampering | contract.md / analysis.ts / referenceMotions.ts lockstep | mitigate | All new fields OPTIONAL/nullable; normalize() surfaces them null-safely; typecheck gate; R8 recheck for any Python mirror. No write path in this plan |
| T-14-03 | Tampering | audit script accidental write | mitigate | Read-only by construction; grep gate asserts zero `set(` occurrences |
| T-14-18 | Tampering | generic validator would reject a VALID forceDirectionPattern (findings[].warnings:string[]) | mitigate | R3-1 fixtures pin the scoped `_validate_force_pattern_inference` as the correct validator (valid accepted / nested-or-unknown rejected) so 14-02 cannot regress to the generic validator |
| T-14-19 | Spoofing/Integrity | a skipped D-01 parity test passes the automated gate, leaving "same functions" unproven | mitigate | R4-1 env-gated strict import (PHASE14_REQUIRE_BACKFILL_HELPER=1) lets 14-02 make a skipped parity FATAL; R4-2 records the fallback/null motion_id force-config choice so Phase 15 cannot silently assume known-reference force semantics |
| T-14-SC | Tampering | npm/pip installs | accept | No new packages installed (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/test_reference_backfill.py -q` — SC#4 + D-02 + R3-1 scoped-validator fixtures green, reference-v1 pinned-config parity present (skip/RED when PHASE14_REQUIRE_BACKFILL_HELPER unset) + env-flip divergence test present + motion_id=None parity assertion (R4-2)
- `cd app && npm run typecheck` — clean
- `node --check app/scripts/audit-reference-fields.mjs` — valid
- `grep` confirms `referenceMotions.ts normalize()` surfaces bodyNormalizationProfile/techniqueProfile/forceDirectionPattern/captureViews (R2-4)
- Audit script run output lists all 11 with field presence + completeRequiredSet count (manual run, no GPU)
</verification>

<success_criteria>
- Audit resolves A2: which of the 11 already have bodyNormalizationProfile + a completeRequiredSet count (read-only, runnable today)
- Wave-0 harness pins the reference-v1 pinned-config parity target (REFERENCE_V1_FORCE_CONFIG, production call boundary, env-gated strict import R4-1, motion_id=None parity R4-2) AND an env-flip divergence test (R2) AND the R3-1 forceDirectionPattern scoped-validator fixtures + proves SC#4 graceful + D-02 verdict against the real library
- Contract + TS + normalize() (and any Python mirror) declare AND surface techniqueProfile/forceDirectionPattern/captureViews/bodyNormalizationProfile in lockstep (R2-4); typecheck clean
</success_criteria>

<output>
Create `.planning/phases/14-reference-motion-registration/14-01-SUMMARY.md` when done
</output>
