---
phase: 14-reference-motion-registration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_reference_backfill.py
  - backend/tests/phase14/__init__.py
  - app/scripts/audit-reference-fields.mjs
  - docs/contract.md
  - app/src/types/analysis.ts
autonomous: true
requirements: [REF-01]
must_haves:
  truths:
    - "A Firestore-read audit (no GPU) lists, for all 11 references, which already carry bodyNormalizationProfile vs which are missing it (resolves A2)"
    - "Failing/scaffold tests exist asserting D-01 same-function compute parity, SC#4 single-view graceful, and the D-02 stored-sufficient vs hybrid verdict"
    - "docs/contract.md §3 and app/src/types/analysis.ts ReferenceMotion both declare the new optional fields (techniqueProfile, forceDirectionPattern) and a single-view flag, in 3-way lockstep"
  artifacts:
    - path: "backend/tests/test_reference_backfill.py"
      provides: "Wave-0 test harness: compute-parity, single-view graceful, D-02 verdict"
      min_lines: 60
    - path: "app/scripts/audit-reference-fields.mjs"
      provides: "Read-only audit of all 11 reference docs (which fields present)"
    - path: "docs/contract.md"
      provides: "§3 ReferenceMotion field table updated with new optional fields"
      contains: "techniqueProfile"
    - path: "app/src/types/analysis.ts"
      provides: "ReferenceMotion interface with techniqueProfile, forceDirectionPattern, captureViews"
      contains: "forceDirectionPattern"
  key_links:
    - from: "backend/tests/test_reference_backfill.py"
      to: "backend/functions/pipeline/app.py::_process downstream functions"
      via: "import-by-path of pipeline module + sunity_shared.analysis functions"
      pattern: "measure_body_profile|compute_force_signals|FallbackRecognizer"
---

<objective>
Lay the verification + contract foundation for the reference downstream backfill. This is the Wave-0
slice: a no-GPU Firestore audit that resolves which of the 11 references already have a body profile
(A2), a pytest harness that pins D-01 (동일 코드 1벌 compute parity), SC#4 (single-view graceful), and
the D-02 verdict (stored-sufficient vs hybrid), and a 3-way contract lockstep adding the new optional
reference fields. No Pod, no Firestore writes — pure read + scaffold so Plan 14-02's compute and Plan
14-03's seed have a green target and a stable contract.

Purpose: The backfill must compute outputs through the SAME functions the student `_process` calls
(D-01) and add fields without breaking the contract. This plan makes "same path" testable and makes the
new fields legal in the contract BEFORE any data is written.
Output: `backend/tests/test_reference_backfill.py` (+ `phase14/__init__.py`), `app/scripts/audit-reference-fields.mjs`,
contract + TS lockstep edits.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-reference-motion-registration/14-CONTEXT.md
@.planning/phases/14-reference-motion-registration/14-RESEARCH.md
@.planning/phases/14-reference-motion-registration/14-PATTERNS.md
</context>

<phase_goal>
**As a** Sunity 운영자(belle), **I want to** 기존 11개 정은지 reference 가 meanAngles·EXTEND·BodyNormalizationProfile·ForceDirectionPattern 을 모두 갖추게 만들고, **so that** Mode 1 비교가 학생과 동일한 계산 경로(위양성 방지)로 바로 동작한다.
</phase_goal>

<tasks>

<task type="auto">
  <name>Task 1: Read-only Firestore audit of all 11 references (resolves A2)</name>
  <files>app/scripts/audit-reference-fields.mjs</files>
  <read_first>
    - app/scripts/seed-reference-body-profile.mjs (Step 1/2/3 structure, validate-before-init, ADC import lazy — lines 135-181)
    - app/scripts/verify-firebase.mjs (Firebase Admin read pattern, projectId 'sunity-ai-coach')
    - .planning/phases/14-reference-motion-registration/14-RESEARCH.md (Runtime State Inventory — the 6 later-added motions likely lack bodyNormalizationProfile)
  </read_first>
  <action>
    Create a Node admin script that READS all 11 reference docs (the union of the original 5 —
    ref-climb, ref-foxtop, ref-foxtop-split, ref-invert, ref-sideway-spin — and the 6 later-added:
    ref-combo, ref-elbow-twist-sister, ref-kip-up, ref-pdshape, ref-peter-pan, ref-power-spin) from
    Firestore collection `reference` and prints a per-motion presence table for these fields:
    meanAngles, anglesJointKeys, bodyNormalizationProfile, bodyComparisonSourcePose, techniqueProfile,
    forceDirectionPattern, activeVersion. Mirror seed-reference-body-profile.mjs structure: define the
    full 11-id constant at top (no default-5 subset — cite [[reference-library-phase4-all11]] and
    Pitfall 1 in a Korean comment), lazy-import firebase-admin/app + firebase-admin/firestore inside an
    async main, init with applicationDefault() + projectId 'sunity-ai-coach'. READ ONLY — no set/merge.
    Print a final summary: count of references missing bodyNormalizationProfile, missing
    forceDirectionPattern, missing techniqueProfile. Log keys-not-values (never print SA/ADC contents,
    security threat: Information Disclosure). Cite D-04 (admin CLI) and A2 (open question) in the header
    comment. This script is READ-only so it can run today against current Firestore state.
  </action>
  <verify>
    <automated>node --check app/scripts/audit-reference-fields.mjs && grep -c "ref-combo\|ref-kip-up\|ref-pdshape\|ref-peter-pan\|ref-power-spin\|ref-elbow-twist-sister" app/scripts/audit-reference-fields.mjs | grep -qv '^0$' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `node --check app/scripts/audit-reference-fields.mjs` exits 0 (valid JS)
    - All 11 motion IDs (union of 5 + 6) appear literally in the script
    - Script uses `.get()` reads only; no `.set(`/`.merge`/`batch.set(` occurrences (grep `set(` count = 0)
    - Header comment cites D-04 + [[reference-library-phase4-all11]] + A2
  </acceptance_criteria>
  <done>Audit script is syntactically valid, references all 11 motion IDs, performs reads only, and prints a field-presence table + missing-field summary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wave-0 backfill test harness (D-01 parity, SC#4 graceful, D-02 verdict)</name>
  <files>backend/tests/phase14/__init__.py, backend/tests/test_reference_backfill.py</files>
  <behavior>
    - D-01 compute parity: feeding one synthetic `pose_frames` + `angles` fixture to the backfill compute helper produces dataclasses identical to calling the SAME `_process` downstream functions (measure_body_profile, FallbackRecognizer().recognize, compute_force_signals, infer_force_direction_pattern) directly in the same order with ONE temporal_fill.
    - SC#4 single-view graceful: with vertical-fallback PoleAxis (axis_vector=(0,1,0), source="vertical_fallback") and pole `line=None`, compute_force_signals contact-stability metrics return None and emit a `pole_line_missing` warning — no exception raised.
    - D-02 STORED-SUFFICIENT: EXTEND (FallbackRecognizer().recognize) and meanAngles (np.nanmean over angles) computed from `angles` alone match the values produced via the full pose_frames path.
    - D-02 HYBRID-NEEDED: measure_body_profile / compute_force_signals require live pose_frames (raw keypoints_3d + per-keypoint confidence); a from-flat reconstruction (pole-aligned, no confidence) is provably insufficient (assert it raises or yields NaN/degraded output vs the live-frames result).
  </behavior>
  <read_first>
    - backend/tests/test_pipeline_body_profile_injection.py (pipeline import-by-path + synthetic PoseFrame fixtures, lines 30-60)
    - backend/functions/pipeline/app.py (_extract_video_analysis_inputs :1135, _process downstream order: EXTEND :1610, force_signals :1899, force_pattern :1932)
    - backend/shared/python/sunity_shared/analysis/technique.py (FallbackRecognizer.recognize, angles-only, lines 92-113)
    - backend/shared/python/sunity_shared/analysis/body_normalization_measurer.py (measure_body_profile reads frame.keypoints_3d, lines 78-110)
    - backend/shared/python/sunity_shared/analysis/force_signals.py (compute_force_signals graceful line=None contact path, lines 1443-1542; double-smooth ban 1596-1598)
    - backend/shared/python/sunity_shared/analysis/force_pattern.py (infer_force_direction_pattern consumes only the report, lines 642-666)
    - backend/shared/python/sunity_shared/analysis/pose_frame.py (PoseFrame fields, to_coco17_array, lines 204-354)
  </read_first>
  <action>
    Create `backend/tests/phase14/__init__.py` (empty package marker) and
    `backend/tests/test_reference_backfill.py`. Reuse the import-by-path + synthetic-fixture pattern
    from test_pipeline_body_profile_injection.py to build a deterministic synthetic `pose_frames`
    list[PoseFrame] (with raw keypoints_3d + per-keypoint confidence + keypoints_3d_pole_aligned +
    reliability) without GPU. Tests must reference a single backfill compute helper by an agreed import
    name — `backend/scripts/backfill_reference_downstream.py::compute_reference_downstream(pose_frames,
    fps=9.0, motion_id=None, mode_context="mode1")` (Plan 14-02 creates the implementation; this test
    is the RED target). Since that module does not yet exist, guard the parity import with a
    pytest.importorskip on `backfill_reference_downstream` ONLY for the parity test, and implement the
    SC#4 + D-02 tests directly against the production sunity_shared functions (no skip — they assert the
    library behavior the helper must reproduce). For SC#4 build the vertical-fallback PoleAxis +
    build_pole_axis_measurement(line=None) and assert compute_force_signals returns graceful None
    contact metrics + `pole_line_missing` in warnings without raising. For D-02 STORED-SUFFICIENT assert
    FallbackRecognizer().recognize(angles) and np.nanmean(angles, axis=0) match across two angle paths.
    For D-02 HYBRID assert a from-flat (pole-aligned, confidence-stripped) reconstruction fed to
    measure_body_profile diverges from the live-frames result (proving re-inference is required).
    Pass already-temporal_fill'ed angles (ONE smoothing — cite Pitfall 3 / REVIEWS R5 in a comment). No
    fabricated severity/finding numbers (force_pattern fabrication gate). Korean comments cite D-01/D-02.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_reference_backfill.py -q 2>&1 | grep -E "passed|skipped|error" </automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && python -m pytest tests/test_reference_backfill.py -q` collects without import/collection errors
    - SC#4 test and both D-02 tests PASS against production sunity_shared functions (no skip)
    - The D-01 parity test is present and either PASS or skipped via importorskip on the not-yet-created `backfill_reference_downstream` module (RED target documented in a comment)
    - `grep -c temporal_fill backend/tests/test_reference_backfill.py` ≥ 1 with a comment citing single-smoothing ban
  </acceptance_criteria>
  <done>The Wave-0 harness collects cleanly; SC#4 + D-02 verdict tests pass against the real library; the D-01 parity test is in place pointing at the Plan 14-02 helper.</done>
</task>

<task type="auto">
  <name>Task 3: 3-way contract lockstep — add new optional reference fields</name>
  <files>docs/contract.md, app/src/types/analysis.ts</files>
  <read_first>
    - app/src/types/analysis.ts (ReferenceMotion interface lines 398-447 — meanAngles/bodyNormalizationProfile/bodyComparisonSourcePose already present; techniqueProfile/forceDirectionPattern absent)
    - docs/contract.md (§3 ReferenceMotion field table lines 105-128; §9.11 forceDirectionPattern shape if present)
    - backend/shared/python/sunity_shared/analysis/force_pattern.py (ForcePatternInference dataclass line 229 — field set for the contract entry)
    - backend/shared/python/sunity_shared/analysis/technique.py (TechniqueProfile / joint_expectations shape for the EXTEND field)
    - app/src/lib/referenceMotions.ts (normalize() ignores unknown fields gracefully — line 70+)
  </read_first>
  <action>
    Add to BOTH docs/contract.md §3 ReferenceMotion field table AND the app/src/types/analysis.ts
    ReferenceMotion interface (3-way lockstep per CLAUDE.md Cross-cutting; Python force_pattern.py /
    technique.py already define the source shapes so no Python contract edit needed beyond reuse):
    (1) `techniqueProfile?` — the EXTEND joint_expectations profile (FallbackRecognizer output;
    nullable, backfill-populated), (2) `forceDirectionPattern?` — the ForcePatternInference shape
    (reuse the existing ForcePatternInference TS type if declared, else reference it; nullable),
    (3) a single-view flag `captureViews?: number` (set to 1 for all 11) per D-03/SC#4 — confirm name
    `captureViews` (RESEARCH A5 / Open-Q4 default). Follow the existing comment style that cites the
    phase/plan + memory (mirror the `bodyNormalizationProfile?` Phase 6 comment at line 427). All new
    fields are OPTIONAL/nullable so pre-backfill references and `normalize()` stay valid. Do NOT add any
    T-scaled per-frame array (Pitfall 5 — 40k index). Cite D-03 (single-view), SC#2 (field set), and
    the lockstep rule in comments.
  </action>
  <verify>
    <automated>cd app && npm run typecheck && grep -q "forceDirectionPattern" src/types/analysis.ts && grep -q "captureViews" src/types/analysis.ts && grep -q "techniqueProfile" ../docs/contract.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && npm run typecheck` passes (tsc --noEmit clean)
    - `app/src/types/analysis.ts` ReferenceMotion declares `techniqueProfile?`, `forceDirectionPattern?`, `captureViews?` (all optional)
    - `docs/contract.md` §3 lists meanAngles + bodyNormalizationProfile + techniqueProfile + forceDirectionPattern + captureViews (closing the Open-Q3 contract gap)
    - New field comments cite the phase/plan + D-03/SC#2 + lockstep rule
  </acceptance_criteria>
  <done>Contract and TS interface declare the new optional reference fields in lockstep; typecheck is clean; pre-backfill references remain valid (all fields optional).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI → Firestore (read) | Audit script reads Firestore via Firebase Admin (ADC); bypasses security rules by design (server-side only) |
| repo → contract consumers | Contract/TS edits change the shape app + GET /reference passthrough rely on |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Information Disclosure | audit-reference-fields.mjs (ADC/SA creds) | mitigate | Log keys-not-values; never print ADC/SA contents (reprocess_* T-04-W5-01 precedent) |
| T-14-02 | Tampering | contract.md / analysis.ts lockstep | mitigate | All new fields OPTIONAL/nullable; normalize() ignores unknowns; typecheck gate. No write path in this plan |
| T-14-03 | Tampering | audit script accidental write | mitigate | Read-only by construction; grep gate asserts zero `set(` occurrences |
| T-14-SC | Tampering | npm/pip installs | accept | No new packages installed (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/test_reference_backfill.py -q` — SC#4 + D-02 tests green, D-01 parity present (skip/RED)
- `cd app && npm run typecheck` — clean
- `node --check app/scripts/audit-reference-fields.mjs` — valid
- Audit script run output lists all 11 with field presence (manual run, no GPU)
</verification>

<success_criteria>
- Audit resolves A2: which of the 11 already have bodyNormalizationProfile (read-only, runnable today)
- Wave-0 harness pins D-01 parity target + proves SC#4 graceful + D-02 verdict against the real library
- Contract + TS declare techniqueProfile/forceDirectionPattern/captureViews in lockstep; typecheck clean
</success_criteria>

<output>
Create `.planning/phases/14-reference-motion-registration/14-01-SUMMARY.md` when done
</output>
