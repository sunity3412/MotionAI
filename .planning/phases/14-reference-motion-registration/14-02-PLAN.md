---
phase: 14-reference-motion-registration
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - backend/scripts/backfill_reference_downstream.py
  - app/scripts/seed-reference-downstream.mjs
  - backend/shared/python/sunity_shared/firestore_admin.py
  - docs/reference-capture-guide.md
autonomous: true
requirements: [REF-01]
must_haves:
  truths:
    - "The backfill computes meanAngles + EXTEND(techniqueProfile) from the STORED active phase4_v1.angles (never from re-run angles), and BodyNormalizationProfile + ForceDirectionPattern from live pose_frames obtained by ONE RTMW re-inference, calling the SAME sunity_shared functions _process calls under the reference-v1 pinned config (REFERENCE_V1_FORCE_CONFIG: recognizer=FallbackRecognizer, technique_profile=None, preflight_label_gate_passed=None, layer-2 off) — provably exact for that config, not 'student path exact' (D-01, R1, R2)"
    - "A `--check-firestore` mode reads ONE reference doc via sunity_shared.firestore_admin / auth._ensure_firebase() (FIREBASE_SA_JSON/PATH/PARAM — never a hand-rolled credentials.Certificate path), verifies activeVersion + angles + anglesJointKeys + anglesFrames present, and EXITS before any S3/RTMW work — so a missing Pod-side Firebase SA fails fast, not mid-pipeline (R2-3)"
    - "A stored-vs-rerun angle gate guards version integrity: storedAnglesHash + rerunAnglesHash + anglesFrames + maxAngleDelta + meanAngleDelta are recorded; anglesFrames must equal len(pose_frames); if maxAngleDelta > epsilon (1.0 deg) the ENTIRE real seed aborts (this is a pose-version re-validation problem, not a derived-field backfill) (R1)"
    - "The fixture is split into `seedPayload` (per-motion {meanAngles, techniqueProfile, bodyNormalizationProfile, forceDirectionPattern, captureViews}) and `diagnostics` (per-motion {storedAnglesHash, rerunAnglesHash, maxAngleDelta, meanAngleDelta, forceSignalsReportSummary}); the seeder reads ONLY seedPayload and validates/rejects-nested-arrays there; diagnostics are logged, never validated or seeded (R2-5)"
    - "The Node seeder merges only the new fields ADD-only (set merge:true); skips a motion only when ALL Phase-14 required fields (meanAngles + techniqueProfile + bodyNormalizationProfile + forceDirectionPattern + captureViews) are present and valid AND --force is false; default behavior repairs MISSING fields only; --force overwrites existing valid fields; dry-run summary separates skippedComplete / repairMissing / forceOverwrite; rejects nested arrays — operator-run admin CLI, no app registration UI (D-04, R3)"
    - "Seeding is all-or-nothing on real data: the seeder rejects a real-run unless seedPayload has exactly 11 ids; the backfill script exits non-zero and emits no seedable fixture if failures>0 OR len(results)!=11 OR any NaN/inf (unless --allow-partial-diagnostic) (D-05, R5)"
    - "A multi-angle capture-guide markdown documents 촬영 조건·앵글·시점 수 (SC#3); single-view is the computed baseline and the multi-angle protocol is doc-only guidance with low-confidence flagging when absent (D-03)"
  artifacts:
    - path: "backend/scripts/backfill_reference_downstream.py"
      provides: "Pod-run orchestrator: --check-firestore credential/read gate → read stored phase4_v1.angles → S3 download → RTMW estimate (live pose_frames) → compute 4 outputs → angle gate → emit split JSON (seedPayload + diagnostics)"
      contains: "compute_reference_downstream"
      min_lines: 140
    - path: "app/scripts/seed-reference-downstream.mjs"
      provides: "Atomic merge seeder for the 4 new fields, reads only seedPayload, dry-run + complete/repair/overwrite split + 11-id all-or-nothing guard + verify"
      min_lines: 110
    - path: "backend/shared/python/sunity_shared/firestore_admin.py"
      provides: "ADD-only merge helper for the new downstream fields"
      contains: "update_reference_downstream"
    - path: "docs/reference-capture-guide.md"
      provides: "SC#3 multi-angle capture guide (촬영 조건·앵글·시점 수)"
  key_links:
    - from: "backend/scripts/backfill_reference_downstream.py"
      to: "sunity_shared.analysis (measure_body_profile / FallbackRecognizer / compute_force_signals / infer_force_direction_pattern)"
      via: "same functions, same order, same reference-v1 pinned config as _process downstream"
      pattern: "measure_body_profile|compute_force_signals|infer_force_direction_pattern|FallbackRecognizer"
    - from: "backend/scripts/backfill_reference_downstream.py"
      to: "reference/{id}.phase4_v1.angles (stored active)"
      via: "Firestore read + reshape (T,J); meanAngles/techniqueProfile source"
      pattern: "anglesJointKeys|anglesFrames|reference.phase4_v1.angles"
    - from: "app/scripts/seed-reference-downstream.mjs"
      to: "Firestore reference/{motionId}"
      via: "batch.set(ref, payload, {merge:true})"
      pattern: "merge:\\s*true|merge: true"
---

<objective>
Build the real backfill compute + seed slice. A single Pod-run orchestrator first runs a
`--check-firestore` credential/read gate (one reference doc via sunity_shared, exit before any S3/RTMW
work — R2-3), then reads the STORED active `phase4_v1.angles` for each motion (the source of `meanAngles`
+ EXTEND, never re-run angles) and runs RTMW once per motion from the already-stored S3 video ONLY to
obtain the live `pose_frames` that BodyNormalizationProfile + ForceDirectionPattern require. It threads
those through the SAME `sunity_shared.analysis` functions the student `_process` calls, under the
reference-v1 pinned config (REFERENCE_V1_FORCE_CONFIG — D-01 동일 코드 1벌, provably exact for that
config), to produce all four outputs, gated by a stored-vs-rerun angle integrity check, then emits a
camelCase JSON fixture SPLIT into `seedPayload` (seedable fields) and `diagnostics` (hashes/deltas/force
summary, never seeded — R2-5). A Node seeder ADD-only merges the `seedPayload` fields into
`reference/{motionId}` (dry-run first, complete/repair/overwrite split, 11-id all-or-nothing,
nested-array-rejecting), backed by a firestore_admin helper. The capture-guide markdown (SC#3) ships
alongside. This makes Plan 14-01's RED parity test GREEN.

Purpose: CONTEXT D-01/D-02 say belle's visually-verified active phase4_v1 pose is AUTHORITATIVE and only
MISSING inputs are hybridized. So `meanAngles` and EXTEND MUST be derived from the stored active
`angles` (R1) — never from a fresh re-inference — and the RTMW re-run exists ONLY to supply the live
`pose_frames` that BodyNormalizationProfile + ForceDirectionPattern consume (those raw keypoints +
confidence are NOT in phase4_v1). The re-run angles are computed for VALIDATION ONLY; a frame-count +
hash + max-angle-delta gate aborts the whole seed if the re-run diverges from the stored pose (that would
be a pose-version re-validation problem, not a backfill). D-01 parity is provably exact under
REFERENCE_V1_FORCE_CONFIG (pinned preflight=None, technique_profile=None, layer-2 off) — NOT "student
path exact", because production `_process` passes an env-driven preflight gate + Layer-2-conditional
technique_profile (R2). The Pod-side Firestore read is now load-bearing, so a `--check-firestore` gate
fails fast on a missing/unmounted Firebase SA before any expensive S3/RTMW work (R2-3).
Output: backfill_reference_downstream.py, seed-reference-downstream.mjs, firestore_admin helper, capture guide.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-reference-motion-registration/14-CONTEXT.md
@.planning/phases/14-reference-motion-registration/14-RESEARCH.md
@.planning/phases/14-reference-motion-registration/14-PATTERNS.md
@.planning/phases/14-reference-motion-registration/14-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backfill orchestrator — Firestore check gate, stored angles for derived fields, ONE RTMW re-inference for live-frame fields, angle integrity gate, split fixture (D-01/D-02/R1/R2/R6/R2-3/R2-5)</name>
  <files>backend/scripts/backfill_reference_downstream.py</files>
  <behavior>
    - `compute_reference_downstream(pose_frames, *, pole_axis_measurement, angles, fps=9.0, motion_id=None, mode_context="mode1", force_config=REFERENCE_V1_FORCE_CONFIG)` returns a dict (or dataclass) with meanAngles, techniqueProfile (EXTEND), bodyNormalizationProfile, forceDirectionPattern. meanAngles and techniqueProfile are derived from the `angles` ARGUMENT (which the orchestrator sets to the STORED active phase4_v1 angles — R1); bodyNormalizationProfile + forceDirectionPattern are computed from `pose_frames` (live RTMW). compute_force_signals is fed the STORED `angles` and the injected `pole_axis_measurement` (R6), with ONE temporal_fill already applied.
    - `--check-firestore` mode (R2-3): read ONE reference doc (default `--motions` first id, e.g. ref-climb) via `sunity_shared.firestore_admin` / `auth._ensure_firebase()` (which already supports FIREBASE_SA_JSON / FIREBASE_SA_PATH / FIREBASE_SA_PARAM — do NOT hand-roll a `credentials.Certificate(...)` path), assert `activeVersion` + `angles` + `anglesJointKeys` + `anglesFrames` are present, print a keys-not-values OK line, and EXIT 0 BEFORE any S3 download / RTMW work. On missing credential or missing fields → exit non-zero with an actionable message (Pod Firebase SA not mounted / doc incomplete). This is the fail-fast gate the Pod runs before the expensive path.
    - D-01 reference-v1 pinned-config parity (R2): the helper calls `compute_force_signals` under REFERENCE_V1_FORCE_CONFIG — `preflight_label_gate_passed=None`, `technique_profile=None`, recognizer=FallbackRecognizer, layer-2 off — so "동일 코드 1벌" is provably EXACT for that config, matching the 14-01 parity test's reference call exactly. This is NOT "student path exact" (production `_process` passes an env-driven preflight gate).
    - Provenance tags (in fixture diagnostics/run-log, NOT necessarily Firestore): `meanAnglesSource="reference.phase4_v1.angles"`, `techniqueProfileSource="reference.phase4_v1.angles"`, `forceConfig=REFERENCE_V1_FORCE_CONFIG`.
    - Angle integrity gate (R1): the orchestrator also computes re-run angles from the live pose_frames FOR VALIDATION ONLY and records `storedAnglesHash` (sha256), `rerunAnglesHash`, `anglesFrames`, `maxAngleDelta`, `meanAngleDelta` IN diagnostics. REQUIRE `anglesFrames == len(pose_frames)` (frame-alignment failure → abort). If `maxAngleDelta > EPSILON_DEG` (1.0 deg) → abort the ENTIRE real seed (pose-version re-validation problem).
    - Split fixture (R2-5): the emitted JSON is `{ generatedAt, seedPayload: { <id>: {meanAngles, techniqueProfile, bodyNormalizationProfile, forceDirectionPattern, captureViews} }, diagnostics: { <id>: {storedAnglesHash, rerunAnglesHash, maxAngleDelta, meanAngleDelta, forceSignalsReportSummary, meanAnglesSource, techniqueProfileSource, forceConfig} } }`. Only `seedPayload` is consumed by the seeder; `diagnostics` (incl. any nested force-report metric arrays) live outside the seedable set so they can never fail the seeder's flat-dict / nested-array validation.
    - Output `seedPayload[id]` equals the Plan 14-01 parity test's reference (calling the same functions directly under REFERENCE_V1_FORCE_CONFIG with the injected pole_axis_measurement + stored angles) — the RED parity + env-flip tests go GREEN.
    - Per-motion compute failure is isolated (logged), BUT the process exit status is all-or-nothing (R5): exit non-zero and emit NO seedable fixture if failures>0 OR len(seedPayload)!=11 OR any NaN/inf, unless --allow-partial-diagnostic is set.
    - --dry-run prints the split JSON to stdout, no file; real-run writes the JSON fixture for the seeder only when the all-or-nothing gate passes.
    - Defaults to / requires all 11 motion IDs; running without --motions never silently processes a 5-subset.
  </behavior>
  <read_first>
    - backend/scripts/extract_reference_body_profiles.py (sys.path injection + lazy heavy-import :38-48; S3 download + RTMW estimate :209-237; per-motion try/except + dry-run JSON emit :344-393; _bp_to_camel_dict :68-79; DEFAULT_MOTION_IDS trap :59-65)
    - backend/scripts/backfill_body_data_new6.py (the other 6 IDs :54-61; camelCase field map :161-177)
    - backend/functions/pipeline/app.py (_extract_video_analysis_inputs :1135 angles ONE temporal_fill :1174-1176; _process EXTEND :1610, force_signals :1899-1908, force_pattern :1932; _preflight_label_gate_passed() :851-871 env-driven True/False/None; _process passes preflight_label_gate_passed=_preflight_label_gate_passed() + technique_profile=profile if layer2_recognizer is not None else None + inputs.pole_axis_measurement — the reference pin diverges from this, see R2)
    - backend/shared/python/sunity_shared/auth.py (_load_service_account_dict :40-67 supports FIREBASE_SA_JSON/PATH/PARAM; _ensure_firebase :70+ — use this for the --check-firestore gate, do NOT hand-roll credentials.Certificate)
    - backend/shared/python/sunity_shared/firestore_admin.py (get_reference_motion :1047-1054 reads reference/{id}; how reference docs store flat angles/anglesJointKeys/anglesFrames :700-735; reshape-on-read convention)
    - backend/shared/python/sunity_shared/analysis/force_signals.py (compute_force_signals signature — confirm the preflight_label_gate_passed + technique_profile kwarg names)
    - backend/shared/python/sunity_shared/analysis/pose_frame.py (to_coco17_array, PoleAxis :204-354)
    - backend/shared/python/sunity_shared/analysis/pole_geometry.py (build_pole_axis_measurement, vertical fallback line=None — orchestrator builds + injects)
    - backend/shared/python/sunity_shared/analysis/skeleton.py (JOINT_KEYS for meanAngles dict)
    - backend/tests/test_reference_backfill.py (the parity/env-flip/SC#4/D-02 target written in Plan 14-01 — match its REFERENCE_V1_FORCE_CONFIG reference call + injected pole_axis_measurement/angles boundary)
  </read_first>
  <action>
    Create the Pod-run orchestrator following the extract_reference_body_profiles.py shape. Define a
    full 11-id constant ALL_MOTION_IDS = union of original 5 (ref-climb, ref-foxtop, ref-foxtop-split,
    ref-invert, ref-sideway-spin) + the 6 (ref-combo, ref-elbow-twist-sister, ref-kip-up, ref-pdshape,
    ref-peter-pan, ref-power-spin); --motions arg defaults to all 11 (never a subset — cite Pitfall 1 /
    [[reference-library-phase4-all11]]). Define `REFERENCE_V1_FORCE_CONFIG = {"recognizer":
    "FallbackRecognizer", "techniqueProfileForForceSignals": None, "preflightLabelGatePassed": None,
    "forceSignalsLayer2Enabled": False}` and `EPSILON_DEG = 1.0`. sys.path-inject shared/python;
    lazy-import the heavy deps (imageio, rtmlib/RTMWPoseEngine, boto3, firebase-admin) inside main so
    `--help` exits 0 on Mac. Add a fail-fast import check (rtmlib/imageio/boto3/firebase-admin) at backfill
    start (Wave-0 requirement).

    --check-firestore (R2-3): a dedicated early-exit mode that reads ONE reference doc (the first
    --motions id) through `sunity_shared.firestore_admin.get_reference_motion` after
    `auth._ensure_firebase()` (the SA source is FIREBASE_SA_JSON / FIREBASE_SA_PATH / FIREBASE_SA_PARAM via
    auth._load_service_account_dict — never a hand-rolled credentials.Certificate). Assert activeVersion +
    angles + anglesJointKeys + anglesFrames present; print a keys-not-values OK line; EXIT before any S3
    download or RTMW estimate. Missing creds or fields → exit non-zero with an actionable message. The Pod
    run sequence calls this gate first (14-03 Task 1) so a missing Pod Firebase SA fails fast.

    Per motion (full backfill path):
    (A) STORED ANGLES READ (R1): read `reference/{id}` active phase4_v1 — `angles` (flat), `anglesJointKeys`
        (len J), `anglesFrames` (T), `activeVersion` — and reshape angles to (T,J). Record
        `storedAnglesHash = sha256` of the canonical bytes (into diagnostics). These STORED angles are the
        ONLY source for meanAngles + techniqueProfile.
    (B) LIVE FRAMES (D-02 hybrid): download `reference/{motion_id}.mp4` from VIDEO_BUCKET via boto3,
        FfmpegFrameExtractor.extract, build vertical-fallback PoleAxis (axis_vector=(0,1,0),
        confidence_level="low", source="vertical_fallback"), RTMWPoseEngine.estimate → live pose_frames
        (raise if empty). Build pole_meas = build_pole_axis_measurement(axis_3d=pole, line=None,
        frame_index=None).
    (C) ANGLE INTEGRITY GATE (R1): compute re-run angles =
        temporal_fill(compute_joint_angles(to_coco17_array(pose_frames)), joint_uncertainty(...)) FOR
        VALIDATION ONLY (ONE smoothing — Pitfall 3). Record (into diagnostics) `rerunAnglesHash`,
        `anglesFrames`, `maxAngleDelta = max(abs(stored - rerun))`, `meanAngleDelta`. ASSERT `anglesFrames
        == len(pose_frames)` (frame-alignment failure → mark motion failed). If `maxAngleDelta > EPSILON_DEG`
        → ABORT the entire real seed (raise / exit non-zero before emitting fixture); record this as a
        pose-version re-validation problem in the run log. Korean comment: re-run angles 는 검증 전용,
        meanAngles/EXTEND 의 source 가 아니다.
    (D) COMPUTE: call
        `compute_reference_downstream(pose_frames, pole_axis_measurement=pole_meas, angles=STORED_angles,
        fps=9.0, motion_id=motion_id, mode_context="mode1", force_config=REFERENCE_V1_FORCE_CONFIG)`. Inside
        the helper: meanAngles = {k: float(nanmean over T)} over STORED angles for skeleton.JOINT_KEYS;
        profile = FallbackRecognizer().recognize(STORED angles) (D-01 same fn _process:1610 — Fallback per
        A1 default, NOT Gemini); body_profile = measure_body_profile(pose_frames) (same fn :1171); fsr =
        compute_force_signals(pose_frames, pole_axis_measurement, body_profile, angles=STORED angles,
        fps=9.0, motion_id=getattr(profile,"motion_id",None), technique_profile=None,
        preflight_label_gate_passed=None) (same fn :1899 — PIN preflight_label_gate_passed=None +
        technique_profile=None for D-01 EXACT parity under REFERENCE_V1_FORCE_CONFIG per R2; this is the
        SAME value the 14-01 parity test's direct-call reference uses. Add a Korean comment: 학생 _process
        는 env 기반 _preflight_label_gate_passed() 를 전달하므로 이것은 "student path exact" 가 아니라
        "same functions under reference-v1 pinned config" 이다 — env flip(PREFLIGHT_LABEL_GATE_PASSED=1) 시
        force-signal confidence 가 갈라진다); fpi = infer_force_direction_pattern(fsr, motion_id=...,
        mode_context="mode1") (same fn :1932, A4). Set meanAnglesSource / techniqueProfileSource =
        "reference.phase4_v1.angles" and forceConfig = REFERENCE_V1_FORCE_CONFIG in the diagnostics.
    (E) SPLIT FIXTURE ASSEMBLY (R2-5 + R7): build `seedPayload[id] = {meanAngles, techniqueProfile,
        bodyNormalizationProfile, forceDirectionPattern, captureViews}` (the ONLY seedable set) and
        `diagnostics[id] = {storedAnglesHash, rerunAnglesHash, maxAngleDelta, meanAngleDelta,
        forceSignalsReportSummary (warnings + key metrics), meanAnglesSource, techniqueProfileSource,
        forceConfig}`. The per-motion `forceSignalsReport` summary lives ONLY in diagnostics (artifact-only;
        note "persist raw report to Firestore?" as a DEFERRED decision in a comment) — it MUST NOT appear
        in seedPayload, so any nested metric arrays cannot collide with the seeder's flat-dict validation.

    Convert dataclasses to camelCase flat dicts for seedPayload (reuse the _bp_to_camel_dict pattern + the
    pipeline _dataclass_to_camel_case_dict shape — do NOT hand-roll a new converter shape; flat arrays
    only in seedPayload, nested-array banned [[firestore-nested-array-flat]]). Set captureViews=1 per motion
    (D-03 single-view). Per-motion try/except logs FAIL + traceback and continues compute, BUT enforce the
    all-or-nothing exit (R5): if failures>0 OR len(seedPayload)!=11 OR any NaN/inf in any seedPayload
    output → write a diagnostic JSON, exit non-zero, and DO NOT emit a seedable fixture unless
    `--allow-partial-diagnostic` is explicitly set. --dry-run → print the split JSON to stdout; real-run
    (gate passed) → write the split JSON fixture (e.g. reference-downstream-backfill.json) for the seeder.
    NEVER write Firestore from this script and NEVER touch joints3d/angles/activeVersion (Pitfall 4 /
    D-02). Log keys-not-values for AWS_*/FIREBASE_SA_* (security). Korean comments cite
    D-01/D-02/R1/R2/R2-3/R2-5 + source :lines.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_reference_backfill.py -x -q 2>&1 | grep -E "passed|error" && python scripts/backfill_reference_downstream.py --help >/dev/null 2>&1 && grep -q "check-firestore" scripts/backfill_reference_downstream.py && grep -q "seedPayload" scripts/backfill_reference_downstream.py && echo HELP_OK</automated>
  </verify>
  <acceptance_criteria>
    - `python backend/scripts/backfill_reference_downstream.py --help` exits 0 with no heavy-dep ImportError (lazy import works on Mac)
    - `--check-firestore` mode exists (R2-3): reads ONE reference doc via sunity_shared/auth._ensure_firebase (FIREBASE_SA_JSON/PATH/PARAM — grep confirms NO hand-rolled `credentials.Certificate`), verifies activeVersion + angles + anglesJointKeys + anglesFrames, and exits before any S3 download / RTMW estimate; missing creds/fields → non-zero
    - The Plan 14-01 reference-v1 pinned-config parity test now PASSES (importorskip no longer skips; compute helper equals direct-call reference under REFERENCE_V1_FORCE_CONFIG with the injected pole_axis_measurement + stored angles, R6); the env-flip test still proves divergence
    - meanAngles + techniqueProfile are derived from the STORED phase4_v1 angles (R1): `meanAnglesSource == "reference.phase4_v1.angles"` and `techniqueProfileSource == "reference.phase4_v1.angles"` are recorded in diagnostics; the helper accepts `angles` as an argument and never recomputes angles for these two fields
    - Angle integrity gate present (R1): storedAnglesHash + rerunAnglesHash + anglesFrames + maxAngleDelta + meanAngleDelta recorded in diagnostics; `anglesFrames == len(pose_frames)` enforced; `maxAngleDelta > 1.0` (EPSILON_DEG) aborts the entire real seed before any fixture is emitted
    - Fixture is SPLIT (R2-5): `seedPayload` holds only {meanAngles, techniqueProfile, bodyNormalizationProfile, forceDirectionPattern, captureViews}; `diagnostics` holds hashes/deltas/forceSignalsReportSummary; `forceSignalsReport` summary appears ONLY in diagnostics (grep: forceSignalsReportSummary not inside seedPayload assembly)
    - The helper passes `preflight_label_gate_passed=None` AND `technique_profile=None` into `compute_force_signals` (REFERENCE_V1_FORCE_CONFIG); a Korean comment documents this is "reference-v1 pinned config", NOT "student path exact", and the env-flip divergence (R2)
    - `compute_reference_downstream` signature includes `*, pole_axis_measurement, angles, ..., force_config=REFERENCE_V1_FORCE_CONFIG` (R6); the orchestrator builds the vertical-fallback pole_axis_measurement and injects it
    - All 11 motion IDs appear in the script; `--motions` defaults to the 11-union (no 5-subset default)
    - All-or-nothing exit (R5): grep/inspection confirms exit non-zero + no seedable fixture when failures>0 / len(seedPayload)!=11 / NaN-inf, unless `--allow-partial-diagnostic`
    - `grep -c "joints3d\s*=\|angles.*set(\|activeVersion.*set" backend/scripts/backfill_reference_downstream.py` shows zero active-pose overwrite; script performs no Firestore writes
    - `grep` confirms ONE temporal_fill call on the validation re-run angles (no double-smooth)
  </acceptance_criteria>
  <done>Orchestrator exposes a fail-fast --check-firestore credential/read gate, derives meanAngles/EXTEND from STORED phase4_v1 angles, uses ONE RTMW re-inference only for the live-frame fields, enforces the stored-vs-rerun angle integrity gate, computes force under REFERENCE_V1_FORCE_CONFIG via the exact student-path functions (provably exact for that config), makes the parity + env-flip tests green, emits a split seedPayload/diagnostics fixture, requires all 11 motions all-or-nothing, and never writes Firestore or overwrites active pose.</done>
</task>

<task type="auto">
  <name>Task 2: firestore_admin ADD-only merge helper + Node seeder (reads only seedPayload, complete/repair/overwrite split, 11-id all-or-nothing)</name>
  <files>backend/shared/python/sunity_shared/firestore_admin.py, app/scripts/seed-reference-downstream.mjs</files>
  <read_first>
    - backend/shared/python/sunity_shared/firestore_admin.py (update_reference_body_data :850-926 — required-field validation, _validate_flat_dict_no_nested_array, set(merge=True), *UpdatedAt, structured log)
    - app/scripts/seed-reference-body-profile.mjs (Step1/2/3 validate-before-init :135-181; nested-array rejection :61-119; idempotent skip + ADD-only merge + verify loop :189-241; per-field UpdatedAt :209,213)
    - app/scripts/audit-reference-fields.mjs (the 11-id constant + completeRequiredSet read pattern from Plan 14-01)
    - app/src/types/analysis.ts (ReferenceMotion new fields from Plan 14-01 — payload must match)
  </read_first>
  <action>
    (a) firestore_admin.py: add a sibling helper `update_reference_downstream_data(motion_id, *,
    mean_angles, technique_profile, body_normalization_profile, force_direction_pattern,
    capture_views=1)` mirroring update_reference_body_data: validate motion_id non-empty, validate
    required keys per field via new `_REF_*_REQUIRED` tuples, run _validate_flat_dict_no_nested_array on
    each dict ([[firestore-nested-array-flat]]), then `set(payload, merge=True)` with per-field
    `*UpdatedAt = int(time()*1000)` and `captureViews`. ADD-only — never include joints3d/angles/
    activeVersion in the payload (Pitfall 4 / D-02). Optionally accept lean `force_direction_pattern_source`
    / `force_signals_warnings` (R7) — but keep the payload lean (default off; comment the deferred
    "persist raw report?" decision). Structured log keys-not-values. Cite D-02 + §3.
    (b) seed-reference-downstream.mjs: extend the seed-reference-body-profile.mjs pattern. Define the
    full 11-id constant. Define `PHASE14_REQUIRED = [meanAngles, techniqueProfile, bodyNormalizationProfile,
    forceDirectionPattern, captureViews]`. Step 1: parseArgs (--input <json> [--dry-run] [--force]
    [--verify] [--motions]), loadPayload — read ONLY the fixture's `seedPayload` object (R2-5: NEVER read
    or validate `diagnostics`; if the fixture lacks `seedPayload`, reject with an actionable error) — then
    per-motion required-field validation + nested-array rejection (Array.isArray inside seedPayload values).
    ALL-OR-NOTHING (R5): if NOT --motions and `seedPayload` does not contain exactly 11 ids → REJECT a
    real-run (only --dry-run or an explicit `--motions <ids> --repair-missing` flow may proceed with a
    subset). Step 2: if --dry-run, compute and print a summary that SEPARATES counts (R3): `skippedComplete`
    (all PHASE14_REQUIRED present+valid AND --force false), `repairMissing` (some required field missing →
    default merge of MISSING fields only), `forceOverwrite` (--force set → overwrite existing valid fields)
    — and return BEFORE any Firebase init (ADC-safe). Step 3: lazy-import firebase-admin/app + firestore,
    init applicationDefault + projectId 'sunity-ai-coach'. Skip rule (R3): skip a motion ONLY when ALL
    PHASE14_REQUIRED are present and valid AND --force is false; otherwise the DEFAULT is repair-missing
    (merge only the missing fields); --force overwrites existing valid fields. Build docPayload with the
    (missing-or-all) fields from seedPayload + captureViews + per-field UpdatedAt, batch.set(ref, payload,
    {merge:true}). --verify: read all 11 back and print which of the 4 new fields + captureViews are present
    per motion + a completeRequiredSet count (the SC#1/SC#2 verify loop). NEVER flip activeVersion. Korean
    comments cite D-02/D-04/R3/R5/R2-5 + lockstep.
  </action>
  <verify>
    <automated>node --check app/scripts/seed-reference-downstream.mjs && grep -q "seedPayload" app/scripts/seed-reference-downstream.mjs && cd backend && python -c "import sys; sys.path.insert(0,'shared/python'); import sunity_shared.firestore_admin as f; assert hasattr(f,'update_reference_downstream_data')" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `node --check app/scripts/seed-reference-downstream.mjs` exits 0
    - Seeder reads ONLY the fixture's `seedPayload` object (R2-5); `diagnostics` is never read, validated, or seeded (grep: seeder references `seedPayload`, and the nested-array/required-field validation runs against seedPayload only)
    - `update_reference_downstream_data` exists in firestore_admin.py, uses `set(..., merge=True)`, runs `_validate_flat_dict_no_nested_array`, writes per-field `*UpdatedAt`, and never includes joints3d/angles/activeVersion (grep zero)
    - Seeder skip rule (R3): skips a motion ONLY when ALL of {meanAngles, techniqueProfile, bodyNormalizationProfile, forceDirectionPattern, captureViews} are present+valid AND --force is false; default is repair-missing (merge only missing fields); --force overwrites existing valid fields
    - dry-run summary separates `skippedComplete` / `repairMissing` / `forceOverwrite` counts (R3)
    - Seeder rejects a real-run unless `seedPayload` has exactly 11 ids (all-or-nothing, R5); partial repair only via explicit `--motions <ids> --repair-missing`
    - Seeder validates required fields BEFORE Firebase init and supports --dry-run early return, --force, --verify
    - Seeder + helper payloads include `captureViews` and the new fields only; nested-array guard present
  </acceptance_criteria>
  <done>An ADD-only merge helper and an idempotent dry-run-first seeder exist; the seeder reads only the fixture's seedPayload (never diagnostics), skips only when the full Phase-14 required set is valid, repairs missing fields by default, enforces 11-id all-or-nothing on real-runs, separates complete/repair/overwrite counts, rejects nested arrays, never touches active pose, and can verify all 11 references' new fields.</done>
</task>

<task type="auto">
  <name>Task 3: Multi-angle capture-guide doc (SC#3)</name>
  <files>docs/reference-capture-guide.md</files>
  <read_first>
    - .planning/phases/14-reference-motion-registration/14-CONTEXT.md (D-03 — 다각도 protocol reduced to doc-only; single-view computed baseline; confidence low when multi-angle absent)
    - .planning/ROADMAP.md (Phase 14 SC#3 line 413 — 촬영 조건·앵글·시점 수 재현 가능)
    - docs/contract.md (prose style + section shorthand convention for docs/*.md)
    - .claude/projects memory [[camera-angle-ai-single-view-synth]] / [[single-camera-first-multi-view-last]] (single-view first, multi-view last resort)
  </read_first>
  <action>
    Write a Korean markdown capture guide documenting the 정은지 세션 권장 촬영 조건으로 등록 정확도가
    재현 가능하도록: (1) 시점 수 — 단일시점이 v1 baseline (학생 입력과 동일 조건, D-03), 다각도는 권장이나
    부재 시 graceful + confidence 낮게 표기; (2) 앵글 — 폴 수직 정렬, 카메라 높이·거리·정면성 권장값
    (occlusion 최소화, 측정 오류 회피); (3) 촬영 조건 — 프레임율/해상도 하한, 조명, 전신 프레이밍, 폴 전체
    가시; (4) 단일시점 한계와 confidence 표기 정책 (captureViews=1 → low confidence flag, SC#4 정합);
    (5) 등록 절차 요약 = backfill_reference_downstream.py (Pod, --check-firestore 먼저) →
    seed-reference-downstream.mjs (seedPayload ADD-only merge, 11-id all-or-nothing). Cite D-03/D-05 +
    [[single-camera-first-multi-view-last]] + Phase 4 single-view AI 합성 path. No emoji, no slop. This is
    the SC#3 deliverable (manual-review SC).
  </action>
  <verify>
    <automated>test -f docs/reference-capture-guide.md && grep -q "촬영 조건" docs/reference-capture-guide.md && grep -q "앵글" docs/reference-capture-guide.md && grep -q "시점" docs/reference-capture-guide.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `docs/reference-capture-guide.md` exists and contains 촬영 조건, 앵글, 시점 수 sections
    - Documents single-view baseline + low-confidence flag policy (D-03/SC#4) and the registration procedure
    - No emoji; cites D-03/D-05 + single-view memory
  </acceptance_criteria>
  <done>The capture guide documents 촬영 조건·앵글·시점 수 such that registration accuracy is reproducible, satisfying SC#3.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pod → S3 | Backfill downloads reference videos with AWS creds (Pod env) |
| Pod → Firestore (read) | Backfill reads stored phase4_v1 angles for meanAngles/EXTEND source (R1); --check-firestore gate verifies credential+read before S3/RTMW (R2-3) |
| Pod compute → JSON fixture | Untrusted-fixture risk: corrupted/wrong-ecosystem JSON could seed bad data; split seedPayload/diagnostics isolates seedable fields (R2-5) |
| Node seeder → Firestore | Firebase Admin merge bypasses rules; ADD-only by design; reads only seedPayload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | Information Disclosure | backfill script AWS_*/FIREBASE_SA_* | mitigate | Log keys-not-values; never print secret contents (reprocess_* T-04-W5-01) |
| T-14-05 | Tampering | backfill overwriting active phase4_v1 pose | mitigate | Script writes no Firestore; helper payload excludes joints3d/angles/activeVersion (grep gate); ADD-only merge |
| T-14-06 | Tampering | corrupted/wrong fixture seeded | mitigate | Seeder reads only seedPayload (R2-5); required-field validation + nested-array rejection + 11-id all-or-nothing BEFORE commit; --dry-run; --verify read-back |
| T-14-07 | DoS/Tampering | partial backfill flips active state | mitigate | No activeVersion flip in any code path here; idempotent repair-missing merge of ADD-only fields |
| T-14-13 | Integrity | derived field diverges from active pose (re-run angles used for meanAngles/EXTEND) | mitigate | meanAngles/EXTEND sourced from STORED phase4_v1 angles only (R1); stored-vs-rerun angle gate aborts the seed if maxAngleDelta > 1.0 deg |
| T-14-15 | DoS/Operational | Pod runs expensive S3/RTMW then fails on missing Firebase SA | mitigate | --check-firestore gate (R2-3) reads one doc via auth._ensure_firebase and exits before any S3/RTMW work; missing creds → fail fast |
| T-14-SC | Tampering | npm/pip installs | accept | No new packages (RESEARCH Package Legitimacy Audit: N/A; Pod deps already present) |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/test_reference_backfill.py -x -q` — reference-v1 pinned-config parity now GREEN + env-flip divergence GREEN
- `cd backend && python -m pytest tests/ -q` — full suite green (regression 0)
- `python backend/scripts/backfill_reference_downstream.py --help` — exits 0 (lazy import)
- `grep -q "check-firestore" backend/scripts/backfill_reference_downstream.py` — --check-firestore gate present (R2-3)
- `grep -q "seedPayload" backend/scripts/backfill_reference_downstream.py app/scripts/seed-reference-downstream.mjs` — split fixture honored both sides (R2-5)
- `node --check app/scripts/seed-reference-downstream.mjs` — valid
- `docs/reference-capture-guide.md` exists with 촬영 조건·앵글·시점 수
- `cd app && npm run typecheck` — clean
</verification>

<success_criteria>
- Backfill exposes a --check-firestore credential/read gate (R2-3), derives meanAngles/EXTEND from STORED phase4_v1 angles (R1), computes BodyNormalizationProfile/ForceDirectionPattern from ONE RTMW re-inference (D-02 hybrid) under REFERENCE_V1_FORCE_CONFIG (D-01 provably exact for that config, R2); stored-vs-rerun angle gate enforced; parity + env-flip tests green; emits split seedPayload/diagnostics fixture (R2-5)
- Seeder reads only seedPayload, ADD-only merge, dry-run-first, complete/repair/overwrite split, 11-id all-or-nothing, nested-array-rejecting; never flip active pose
- All 11 motions required (no default-5); SC#3 capture guide shipped
</success_criteria>

<output>
Create `.planning/phases/14-reference-motion-registration/14-02-SUMMARY.md` when done
</output>
</content>
