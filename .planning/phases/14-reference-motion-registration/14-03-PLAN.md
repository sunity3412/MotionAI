---
phase: 14-reference-motion-registration
plan: 03
type: execute
wave: 3
depends_on: ["14-02"]
files_modified:
  - .planning/phases/14-reference-motion-registration/14-BACKFILL-RUN.md
  - backend/scripts/snapshot_reference_active_pose.mjs
  - backend/scripts/rollback_reference_downstream.mjs
autonomous: false
user_setup:
  - service: firebase-admin (ADC)
    why: "Node seeder real-run needs Application Default Credentials for project sunity-ai-coach"
    env_vars: []
    dashboard_config:
      - task: "gcloud auth application-default login (account sunity3412@gmail.com) — only if ADC not already present on the run machine"
        location: "local shell"
must_haves:
  truths:
    - "All 11 references carry meanAngles + EXTEND(techniqueProfile) + BodyNormalizationProfile + ForceDirectionPattern + captureViews after the backfill (SC#2)"
    - "A Firestore verify-read confirms the 4 new fields present + non-empty for all 11 (SC#1/SC#2)"
    - "The active phase4_v1 joints3d/angles/activeVersion are PROVABLY unchanged via a pre/post byte-level sha256 snapshot (not a run-log assertion): a JSON summary asserts unchangedActivePoseCount==11, changedActivePoseCount==0 (D-02, R4)"
    - "Seeding is all-or-nothing: the JSON summary asserts completeDownstreamFieldCount==11 and seededMotionCount==11 (R5)"
    - "A rollback/delete runbook exists that removes ONLY Phase-14-added fields (never joints3d/angles/activeVersion); any active-field change is treated as an incident (R4)"
    - "belle visually spot-checks the backfilled fields before any production state change is considered (no active flip in this phase)"
  artifacts:
    - path: ".planning/phases/14-reference-motion-registration/14-BACKFILL-RUN.md"
      provides: "Run log: Pod health gate, pre/post active-pose hash JSON summary, dry-run output, seed result, verify-read table for 11 motions, risk-response runbook"
    - path: "backend/scripts/snapshot_reference_active_pose.mjs"
      provides: "Pre/post active-pose snapshot + byte-level hash compare (activeVersion + angles/joints3d len+sha256)"
    - path: "backend/scripts/rollback_reference_downstream.mjs"
      provides: "ADD-only rollback: deletes ONLY Phase-14-added fields, never active pose"
  key_links:
    - from: "snapshot_reference_active_pose.mjs (pre)"
      to: "snapshot_reference_active_pose.mjs (post)"
      via: "byte-level sha256 compare of activeVersion/angles/joints3d → JSON summary (unchangedActivePoseCount)"
      pattern: "sha256|unchangedActivePoseCount"
    - from: "Pod backfill_reference_downstream.py run"
      to: "seed-reference-downstream.mjs --verify"
      via: "JSON fixture scp + ADD-only merge + read-back of all 11"
      pattern: "merge|verify"
---

<objective>
Execute the end-to-end backfill on real data and prove the slice with a byte-level integrity gate: take
a pre-seed active-pose snapshot (hashes of activeVersion + angles + joints3d for all 11), run the Pod
orchestrator across all 11 references (one RTMW re-inference each, stored-angles-sourced derived fields),
seed the four new fields ADD-only via the Node seeder (dry-run → real-run → verify), then take a
post-seed snapshot and byte-level hash-compare to PROVE the active phase4_v1 pose is untouched. A JSON
summary (not a prose assertion) gates success: unchangedActivePoseCount==11, changedActivePoseCount==0,
completeDownstreamFieldCount==11, seededMotionCount==11. A blocking human checkpoint puts belle's eyes on
the backfilled fields before any production state change, and a rollback runbook + risk-response table
stand by.

Purpose: Phase 14's user-facing capability — "all 11 정은지 references are Mode-1-ready with the full
engine output set" — only becomes TRUE when the backfill actually runs and is verified. Because this is
a production Firestore write over visually-verified pose, the "active pose unchanged" claim (D-02) must
be PROVABLE by hash, not asserted by a string in the log (R4). Pod ops = Claude runs; flip = belle
approves ([[pod-ops-claude-runs]]). Commit→push before Pod work ([[gsd-pod-work-push-first]]).
Output: 14-BACKFILL-RUN.md run log + pre/post hash snapshots + verified Firestore state + rollback runbook.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-reference-motion-registration/14-CONTEXT.md
@.planning/phases/14-reference-motion-registration/14-RESEARCH.md
@.planning/phases/14-reference-motion-registration/14-PATTERNS.md
@.planning/phases/14-reference-motion-registration/14-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit/push + Pod health gate + pre-seed snapshot + backfill run (all 11) + seed dry-run + real-run + verify-read + post-seed hash compare</name>
  <files>.planning/phases/14-reference-motion-registration/14-BACKFILL-RUN.md, backend/scripts/snapshot_reference_active_pose.mjs, backend/scripts/rollback_reference_downstream.mjs</files>
  <read_first>
    - backend/scripts/backfill_reference_downstream.py (the orchestrator from Plan 14-02 — entrypoint + --motions + --dry-run + all-or-nothing exit)
    - app/scripts/seed-reference-downstream.mjs (the seeder from Plan 14-02 — --input/--dry-run/--force/--verify + 11-id all-or-nothing)
    - app/scripts/audit-reference-fields.mjs (Plan 14-01 read pattern + completeRequiredSet, to confirm pre/post state)
    - app/scripts/seed-reference-body-profile.mjs (Firebase Admin read pattern, applicationDefault + projectId, lazy import)
    - backend/shared/python/sunity_shared/firestore_admin.py (how reference stores flat angles/anglesJointKeys/anglesFrames :700-735 — what to hash)
    - .planning/STATE.md (Pod qcf38vvsmub1y4 RTX PRO 4500 UP, /health ok; Lambda RUNPOD_ANALYZE_URL synced; [[runpod-gpu-env]] 2026-06-15 recipe)
    - .claude/projects memory [[gsd-pod-work-push-first]] / [[pod-ops-claude-runs]] / [[reference-library-phase4-all11]]
  </read_first>
  <action>
    First build two small Node admin scripts (mirror seed-reference-body-profile.mjs init: applicationDefault
    + projectId 'sunity-ai-coach', lazy import, keys-not-values logging):
    - `snapshot_reference_active_pose.mjs` (R4): reads all 11 `reference/{id}` and records, per motion,
      `activeVersion`, `angles` length + sha256, `joints3d` length + sha256, `anglesJointKeys`, and whether
      `versions/phase4_v1` exists. Modes: `--mode pre` writes
      `.planning/phases/14-reference-motion-registration/14-PRESEED-SNAPSHOT.json` (gitignored — add the
      path to .gitignore; the run log keeps only the hashes, not raw arrays); `--mode post` re-reads the
      same fields, byte-level compares each hash to the pre snapshot, and prints a JSON summary
      `{unchangedActivePoseCount, changedActivePoseCount, completeDownstreamFieldCount, seededMotionCount,
      perMotion:[...]}`. Any hash mismatch → exit non-zero.
    - `rollback_reference_downstream.mjs` (R4 runbook): deletes ONLY the Phase-14-added fields
      (meanAngles*/techniqueProfile*/forceDirectionPattern*/captureViews* + their *UpdatedAt — never
      joints3d/angles/activeVersion) via FieldValue.delete() merge; refuses to run if it would touch any
      active-pose field. --dry-run default; --confirm to execute.

    Then the run, with an explicit Pod-down ABORT GATE and the pre/post hash gate:
    (1) git commit + push HEAD before any Pod work ([[gsd-pod-work-push-first]]) — Pod git pull must
    receive the new scripts. (2) POD HEALTH GATE (abort condition): query the Pod qcf38vvsmub1y4
    `GET /health`. If /health is NOT ok (Pod down/unreachable), STOP immediately — record the failed
    health check in 14-BACKFILL-RUN.md and do NOT proceed to any backfill/seed/real-run step. The Pod is
    the ONLY valid HYBRID source; CPU yields NaN, so a degraded run is forbidden. Surface the blockage to
    belle (Pod restart needed). (3) PRE-SEED SNAPSHOT: run `node backend/scripts/snapshot_reference_active_pose.mjs
    --mode pre` and record the per-motion hashes in 14-BACKFILL-RUN.md. (4) On the Pod: git pull; fail-fast
    import check (rtmlib/imageio/boto3/firebase-admin — abort if any import fails); run
    `python backend/scripts/backfill_reference_downstream.py --motions <all 11 explicitly> --dry-run` first
    to validate JSON shape for all 11, then real-run to emit the JSON fixture
    (reference-downstream-backfill.json). The script's own all-or-nothing gate (R5) must pass (11/11
    computed, NaN/inf 0, failures 0, stored-vs-rerun maxAngleDelta <= 1.0 deg); if it exits non-zero,
    record the diagnostic JSON and STOP (no seed). (5) scp the JSON fixture to local. (6) Run
    `node app/scripts/seed-reference-downstream.mjs --input <fixture> --dry-run` to preview the
    skippedComplete/repairMissing/forceOverwrite split, then real-run (--force only if re-running over
    existing valid fields) — ADD-only merge, 11-id all-or-nothing, no active flip. (7) VERIFY-READ
    (distinct checkable step): run `node app/scripts/seed-reference-downstream.mjs --verify` AND
    `node app/scripts/audit-reference-fields.mjs` to read back all 11 and confirm the 4 new fields +
    captureViews present + non-empty (completeDownstreamFieldCount). (8) POST-SEED SNAPSHOT + HASH COMPARE
    (R4): run `node backend/scripts/snapshot_reference_active_pose.mjs --mode post`. If
    unchangedActivePoseCount != 11 OR changedActivePoseCount != 0 → write 14-BACKFILL-RUN.md as **FAIL**,
    run the rollback runbook for affected motions, and DO NOT proceed to the belle approval step (Task 2);
    treat any active-pose change as an incident.

    Record everything in 14-BACKFILL-RUN.md: the Pod /health gate result (pass/abort), proxy URL, the
    pre/post hash JSON summary (unchangedActivePoseCount / changedActivePoseCount /
    completeDownstreamFieldCount / seededMotionCount), dry-run summary (with the complete/repair/overwrite
    split), real-run result, the stored-vs-rerun angle gate result (maxAngleDelta per motion), and the
    11-motion verify-read table (motion_id × {meanAngles, techniqueProfile, bodyNormalizationProfile,
    forceDirectionPattern, captureViews} present/non-empty). Log keys-not-values (no AWS/SA/ADC secrets).
    Do NOT flip active version — that is belle's call in Task 2 (and not required for this phase). Append
    the risk-response runbook table (below) to 14-BACKFILL-RUN.md as a standing operator policy section.
  </action>
  <verify>
    <automated>test -f .planning/phases/14-reference-motion-registration/14-BACKFILL-RUN.md && node --check backend/scripts/snapshot_reference_active_pose.mjs && node --check backend/scripts/rollback_reference_downstream.mjs && grep -q "unchangedActivePoseCount" .planning/phases/14-reference-motion-registration/14-BACKFILL-RUN.md && grep -q "completeDownstreamFieldCount" .planning/phases/14-reference-motion-registration/14-BACKFILL-RUN.md && grep -q "health" .planning/phases/14-reference-motion-registration/14-BACKFILL-RUN.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - HEAD committed + pushed before Pod pull (run log records the commit SHA)
    - POD HEALTH GATE enforced: Pod `GET /health` not ok → STOP before any snapshot/seed/backfill/real-run (CPU yields NaN); the run log records the /health result and, if aborted, no seed/real-run occurred
    - `snapshot_reference_active_pose.mjs` + `rollback_reference_downstream.mjs` are valid JS (`node --check` exits 0) and never write joints3d/angles/activeVersion (grep zero)
    - PRE-SEED snapshot taken before backfill; POST-SEED snapshot + byte-level hash compare taken after seed; the run log records the JSON summary with `unchangedActivePoseCount == 11`, `changedActivePoseCount == 0`, `completeDownstreamFieldCount == 11`, `seededMotionCount == 11` (R4 — proven by hash, not a prose assertion)
    - Backfill ran for all 11 explicitly (--motions); the script's all-or-nothing gate passed (11/11, NaN/inf 0, failures 0, stored-vs-rerun maxAngleDelta <= 1.0 deg) before any seed
    - Seeder dry-run (with skippedComplete/repairMissing/forceOverwrite split) preceded real-run; ADD-only merge; 11-id all-or-nothing
    - VERIFY-READ (distinct step) recorded: `--verify` + audit read-back table shows the 4 new fields + captureViews present + non-empty for all 11
    - Any post-seed hash mismatch → run log written as FAIL, rollback runbook invoked, Task 2 NOT reached (incident path)
    - The risk-response runbook table is appended to 14-BACKFILL-RUN.md
    - No secret values appear in the run log
  </acceptance_criteria>
  <done>The Pod-health gate is enforced (no degraded CPU run), a pre/post byte-level hash gate PROVES the active pose unchanged (unchangedActivePoseCount==11 / changedActivePoseCount==0), all 11 references are backfilled all-or-nothing and verified (completeDownstreamFieldCount==11 / seededMotionCount==11), a rollback runbook + risk-response table stand by, and the full run is logged in 14-BACKFILL-RUN.md.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: belle visual spot-check of backfilled fields (before any production state change)</name>
  <files>.planning/phases/14-reference-motion-registration/14-BACKFILL-RUN.md</files>
  <action>
    Pause for belle to review the backfilled reference fields after the automated Task 1 run (which must
    have passed the post-seed hash gate — unchangedActivePoseCount==11). Present the 14-BACKFILL-RUN.md
    verify-read table, the pre/post hash JSON summary, and the spot-check guidance below. Do NOT flip
    activeVersion or make any production state change without belle's explicit approval
    ([[pod-ops-claude-runs]]). This is a verification checkpoint, not new implementation work.
  </action>
  <what-built>
    All 11 정은지 references now carry meanAngles + EXTEND(techniqueProfile) + BodyNormalizationProfile
    + ForceDirectionPattern + captureViews=1, ADD-only merged. The active phase4_v1 joints3d/angles are
    PROVABLY unchanged by a pre/post byte-level hash compare (unchangedActivePoseCount==11). The
    14-BACKFILL-RUN.md verify-read table lists each motion's field presence/non-emptiness, and the hash
    JSON summary shows the active pose untouched.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/14-reference-motion-registration/14-BACKFILL-RUN.md` and review the
       11-motion verify-read table — confirm all 4 new fields + captureViews are present + non-empty for
       every motion, and that the pre/post hash JSON summary shows unchangedActivePoseCount==11 /
       changedActivePoseCount==0 (activeVersion=phase4_v1 unchanged, angles/joints3d hashes match).
    2. Spot-check 2-3 motions' backfilled values for sanity: EXTEND profile is conservative ("모르면 깎지
       않음"), BodyNormalizationProfile proportions are plausible, ForceDirectionPattern findings are not
       fabricated (empty list + umbrella warning is acceptable when no finding). Use the per-motion
       forceSignalsReport summary (R7) in the run log to trace any odd finding.
    3. Confirm no active-version flip occurred (Phase 14 does not flip; any future flip is a separate
       belle-approved action — [[pod-ops-claude-runs]]).
  </how-to-verify>
  <verify>
    <human-check>belle confirms the 11-motion verify-read table is complete, the pre/post hash summary shows the active pose unchanged (unchangedActivePoseCount==11), and the spot-checked values are sane.</human-check>
  </verify>
  <acceptance_criteria>
    - belle confirms the verify-read table shows all 11 complete (4 fields + captureViews)
    - belle confirms the pre/post hash summary proves active pose unchanged (no flip, hashes match)
    - belle confirms the spot-checked values are sane (no fabricated findings, conservative EXTEND)
  </acceptance_criteria>
  <done>belle has approved the backfilled fields for all 11 references; active pose confirmed unchanged by hash.</done>
  <resume-signal>Type "approved" if the 11-reference backfill looks correct, or describe issues to fix.</resume-signal>
</task>

</tasks>

<risk_response_runbook>
## 리스크 대응표 (stop / recover / propose)

> Standing operator policy for the real backfill+seed. Each row = how the executor STOPS, RECOVERS
> (ADD-only rollback, never touch active pose), and what it PROPOSES next. Append this table to
> 14-BACKFILL-RUN.md. (Folded from 14-DIRECT-REVIEW.md § Risk Response.)

| Situation | Stop / Recover / Propose |
|-----------|--------------------------|
| Pod `/health` fails | STOP immediately; record only the health failure in 14-BACKFILL-RUN.md; no backfill/seed; CPU fallback forbidden (NaN). Propose: belle restarts/recreates Pod (Network Storage), then re-run from the health gate. |
| S3 video missing for one motion | Do NOT emit a seedable fixture. Record missing key/bucket/motion id; treat as an S3 inventory/upload problem. The other 10 are NOT seeded (all-or-nothing). Propose: fix the S3 object, re-run all 11. |
| re-run angles differ from active `phase4_v1.angles` (maxAngleDelta > 1.0 deg) | STOP the entire seed (R1). This is a pose-version re-validation problem, not a derived-field backfill. Derived fields (meanAngles/EXTEND) already source STORED angles; the gate guards the force/body live-frame alignment. Propose: promote to a pose-version re-validation phase; do not seed. |
| ForcePattern findings look fabricated / too aggressive | If BEFORE real-run: discard the fixture, inspect the per-motion forceSignalsReport (R7). If AFTER real-run: run `rollback_reference_downstream.mjs` to delete ONLY the ADD-only fields; never touch active pose. Empty findings + umbrella warning is acceptable. Propose: re-run the affected motion diagnostically. |
| Seeder dry-run says some fields already exist | Do NOT blanket-skip. Use the complete/repair/overwrite split (R3): repair-missing is the default merge; --force only overwrites existing valid fields. Propose: repair-missing run for the incomplete motions. |
| Real seed accidentally writes wrong fields | Use the pre-seed snapshot hashes to scope the impact. Run `rollback_reference_downstream.mjs` to delete/restore ONLY Phase-14-added fields. If `joints3d/angles/activeVersion` changed → STOP, treat as a separate incident (do not auto-recover). |
| ADC/auth fails locally | Keep only the dry-run output; do NOT real-run. Do not bypass via console manual edit. Propose: `gcloud auth application-default login` (sunity3412@gmail.com), then retry with the same fixture. |
| belle spot-check rejects values | Leave active pose untouched; run `rollback_reference_downstream.mjs` for the rejected motion's ADD-only fields. Record the rejected motion id + reason in the run log. Propose: diagnostic re-run for that motion only. |
</risk_response_runbook>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local → GitHub → Pod | Code must be pushed before Pod pull; un-pushed code = stale Pod run |
| Pod → S3 / Firestore-fixture | AWS creds + Firebase SA on Pod; fixture is the seed input |
| Node seeder → Firestore (write) | Real-run merges into production reference docs |
| snapshot/rollback → Firestore | Read (snapshot) + ADD-only delete (rollback); never touches active pose |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-08 | Tampering/DoS | partial backfill leaves active state inconsistent | mitigate | ADD-only merge; no activeVersion flip; 11-id all-or-nothing seed; per-motion verify-read; idempotent repair-missing |
| T-14-09 | Tampering | overwriting visually-verified active pose | mitigate | Pre/post byte-level sha256 hash compare (R4): unchangedActivePoseCount==11 / changedActivePoseCount==0 required; helper payload excludes joints3d/angles/activeVersion (Plan 14-02 gate); rollback runbook is ADD-only |
| T-14-10 | Information Disclosure | secrets in run log | mitigate | Log keys-not-values; no AWS/SA/ADC contents; snapshot stores hashes not raw arrays; PRESEED-SNAPSHOT.json gitignored |
| T-14-11 | Tampering | stale Pod code (un-pushed) | mitigate | Commit+push before pull ([[gsd-pod-work-push-first]]); run log records SHA |
| T-14-12 | DoS/Integrity | degraded CPU run when Pod down (NaN) | mitigate | Pod /health ABORT GATE — STOP before any snapshot/backfill/seed if /health not ok; no CPU fallback run |
| T-14-14 | Integrity | active-pose change masked by a prose "unchanged" assertion | mitigate | R4 replaces the prose assertion with a byte-level hash JSON summary; any mismatch → FAIL + incident + rollback, no belle step |
| T-14-SC | Tampering | npm/pip installs | accept | No new packages; Pod deps already present ([[rtmw-blackwell-lean-bootstrap]]) |
</threat_model>

<verification>
- `node backend/scripts/snapshot_reference_active_pose.mjs --mode post` — JSON summary: unchangedActivePoseCount==11, changedActivePoseCount==0, completeDownstreamFieldCount==11, seededMotionCount==11
- `node app/scripts/seed-reference-downstream.mjs --verify` — all 11 show 4 new fields + captureViews present
- `node app/scripts/audit-reference-fields.mjs` — post-state confirms no reference missing the new fields; activeVersion=phase4_v1 unchanged
- 14-BACKFILL-RUN.md records the Pod /health gate + pre/post hash JSON summary (run aborted if /health not ok or hash mismatch)
- `cd backend && python -m pytest tests/ -q` + `cd app && npm run typecheck` — green before /gsd-verify-work
- belle checkpoint approved (Task 2)
</verification>

<success_criteria>
- SC#1: all 11 references present in Mode 1 list (verify-read returns 11; normalize() unaffected)
- SC#2: each reference has meanAngles + EXTEND + BodyNormalizationProfile + ForceDirectionPattern (completeDownstreamFieldCount==11)
- D-02 (R4): active phase4_v1 joints3d/angles/activeVersion PROVABLY unchanged by pre/post byte-level hash (unchangedActivePoseCount==11 / changedActivePoseCount==0)
- R5: seeding all-or-nothing (seededMotionCount==11)
- Pod /health gate enforced (no degraded CPU run when Pod down); rollback runbook + risk-response table in place
- belle visual spot-check approved before any production state change
</success_criteria>

<output>
Create `.planning/phases/14-reference-motion-registration/14-03-SUMMARY.md` when done
</output>
</content>
</invoke>
