---
phase: 14-reference-motion-registration
plan: 03
type: execute
wave: 3
depends_on: ["14-02"]
requirements: [REF-01]
files_modified:
  - .planning/phases/14-reference-motion-registration/14-BACKFILL-RUN.md
  - .planning/phases/14-reference-motion-registration/14-BACKFILL-RUN-SUMMARY.json
  - app/scripts/snapshot-reference-phase14-state.mjs
  - app/scripts/rollback-reference-downstream.mjs
autonomous: false
user_setup:
  - service: firebase-admin (ADC)
    why: "Node seeder/snapshot/rollback real-run needs Application Default Credentials for project sunity-ai-coach"
    env_vars: []
    dashboard_config:
      - task: "gcloud auth application-default login (account sunity3412@gmail.com) — only if ADC not already present on the run machine"
        location: "local shell"
  - service: runpod-firestore-sa
    why: "Pod-side backfill reads stored phase4_v1 angles from Firestore (R1/R2-3); a Firebase SA must be mounted on the Pod or the --check-firestore gate fails fast"
    env_vars:
      - name: FIREBASE_SA_PATH
        source: "Pod-mounted Firebase service-account JSON path (or FIREBASE_SA_JSON inline). auth._load_service_account_dict reads FIREBASE_SA_JSON / FIREBASE_SA_PATH / FIREBASE_SA_PARAM; STATE.md says FIREBASE_SA_PATH is present — the --check-firestore gate verifies it explicitly"
must_haves:
  truths:
    - "All 11 references carry meanAngles + EXTEND(techniqueProfile) + BodyNormalizationProfile + ForceDirectionPattern + captureViews after the backfill (SC#2)"
    - "A Firestore verify-read confirms the 4 new fields present + non-empty for all 11 (SC#1/SC#2)"
    - "The Pod runs a --check-firestore credential/read gate with ALL 11 motion IDs (after /health, before any S3/RTMW) so a missing Pod Firebase SA OR an incomplete non-ref-climb doc fails fast on a cheap metadata check, not mid-pipeline (R2-3, R3-2)"
    - "The active phase4_v1 joints3d/angles/activeVersion are PROVABLY unchanged via a pre/post byte-level sha256 snapshot (not a run-log assertion): a JSON summary asserts unchangedActivePoseCount==11, changedActivePoseCount==0 (D-02, R4)"
    - "Seeding is all-or-nothing: the JSON summary asserts completeDownstreamFieldCount==11 and seededMotionCount==11 (R5)"
    - "The pre-seed snapshot captures BOTH active-pose hashes AND the full Phase-14 field state (meanAngles/techniqueProfile/bodyNormalizationProfile/forceDirectionPattern/captureViews + *UpdatedAt + optional forceDirectionPatternSource/forceSignalsWarnings) as {present, valueHash, value?} so rollback is restore-aware (R2-1)"
    - "A RESTORE-aware rollback runbook exists: a field ABSENT before seed is deleted, a field that EXISTED before seed is restored to its exact previous value; active pose (joints3d/angles/activeVersion) is never touched, and any active-field change is treated as an incident (R2-1, R4)"
    - "belle visually spot-checks the backfilled fields before any production state change is considered (no active flip in this phase)"
  artifacts:
    - path: ".planning/phases/14-reference-motion-registration/14-BACKFILL-RUN.md"
      provides: "Run log: Pod health gate, --check-firestore all-11 metadata gate result, pre/post active-pose hash JSON summary, dry-run output, seed result, verify-read table for 11 motions, restore-aware risk runbook"
    - path: ".planning/phases/14-reference-motion-registration/14-BACKFILL-RUN-SUMMARY.json"
      provides: "Machine-readable gate: {unchangedActivePoseCount, changedActivePoseCount, completeDownstreamFieldCount, seededMotionCount, perMotion:[...]}"
      contains: "unchangedActivePoseCount"
    - path: "app/scripts/snapshot-reference-phase14-state.mjs"
      provides: "Pre/post snapshot of active-pose hashes AND full Phase-14 field state ({present, valueHash, value?}) + byte-level compare → 14-BACKFILL-RUN-SUMMARY.json"
    - path: "app/scripts/rollback-reference-downstream.mjs"
      provides: "RESTORE-aware rollback: delete fields absent-before-seed, restore fields present-before-seed; never touches active pose"
  key_links:
    - from: "app/scripts/snapshot-reference-phase14-state.mjs (pre)"
      to: "app/scripts/snapshot-reference-phase14-state.mjs (post)"
      via: "byte-level sha256 compare of activeVersion/angles/joints3d + Phase-14 field state → 14-BACKFILL-RUN-SUMMARY.json"
      pattern: "sha256|unchangedActivePoseCount"
    - from: "Pod backfill_reference_downstream.py run"
      to: "app/scripts/seed-reference-downstream.mjs --verify"
      via: "JSON fixture scp + ADD-only merge (seedPayload) + read-back of all 11"
      pattern: "merge|verify"
---

<objective>
Execute the end-to-end backfill on real data and prove the slice with a byte-level integrity gate: take
a pre-seed Phase-14-state snapshot (active-pose hashes AND the full downstream field state for all 11),
run the Pod orchestrator across all 11 references (a --check-firestore all-11 cheap-metadata gate first, then one
RTMW re-inference each, stored-angles-sourced derived fields), seed the four new fields ADD-only via the
Node seeder (dry-run → real-run → verify), then take a post-seed snapshot and byte-level hash-compare to
PROVE the active phase4_v1 pose is untouched. A JSON summary file (not a prose assertion) gates success:
unchangedActivePoseCount==11, changedActivePoseCount==0, completeDownstreamFieldCount==11,
seededMotionCount==11. A blocking human checkpoint puts belle's eyes on the backfilled fields before any
production state change, and a RESTORE-aware rollback runbook + risk-response table stand by.

Purpose: Phase 14's user-facing capability — "all 11 정은지 references are Mode-1-ready with the full
engine output set" — only becomes TRUE when the backfill actually runs and is verified. Because this is
a production Firestore write over visually-verified pose, the "active pose unchanged" claim (D-02) must
be PROVABLE by hash, not asserted by a string in the log (R4). And because Phase 14 may repair existing
fields (e.g. a missing bodyNormalizationProfile) or --force overwrite valid ones, rollback must be
restore-aware — a delete-only rollback would lose pre-existing field values (R2-1). New Node scripts live
under app/scripts because firebase-admin is only installed under app/ (R2-2). Pod ops = Claude runs;
flip = belle approves ([[pod-ops-claude-runs]]). Commit→push before Pod work ([[gsd-pod-work-push-first]]).
Output: 14-BACKFILL-RUN.md run log + 14-BACKFILL-RUN-SUMMARY.json gate + pre/post Phase-14-state snapshots
+ verified Firestore state + restore-aware rollback runbook.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-reference-motion-registration/14-CONTEXT.md
@.planning/phases/14-reference-motion-registration/14-RESEARCH.md
@.planning/phases/14-reference-motion-registration/14-PATTERNS.md
@.planning/phases/14-reference-motion-registration/14-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit/push + Pod health gate + Pod --check-firestore gate + pre-seed Phase-14-state snapshot + backfill run (all 11) + seed dry-run + real-run + verify-read + post-seed hash compare</name>
  <files>.planning/phases/14-reference-motion-registration/14-BACKFILL-RUN.md, .planning/phases/14-reference-motion-registration/14-BACKFILL-RUN-SUMMARY.json, app/scripts/snapshot-reference-phase14-state.mjs, app/scripts/rollback-reference-downstream.mjs</files>
  <read_first>
    - backend/scripts/backfill_reference_downstream.py (the orchestrator from Plan 14-02 — --check-firestore gate + --motions + --dry-run + split seedPayload/diagnostics + all-or-nothing exit)
    - app/scripts/seed-reference-downstream.mjs (the seeder from Plan 14-02 — --input/--dry-run/--force/--verify + reads only seedPayload + 11-id all-or-nothing)
    - app/scripts/audit-reference-fields.mjs (Plan 14-01 read pattern + completeRequiredSet, to confirm pre/post state)
    - app/scripts/seed-reference-body-profile.mjs (Firebase Admin read pattern, applicationDefault + projectId, lazy import, FieldValue usage)
    - backend/shared/python/sunity_shared/firestore_admin.py (how reference stores flat angles/anglesJointKeys/anglesFrames :700-735 — what to hash; bodyNormalizationProfile/bodyComparisonSourcePose fields)
    - .planning/STATE.md (Pod qcf38vvsmub1y4 RTX PRO 4500 UP, /health ok; Lambda RUNPOD_ANALYZE_URL synced; FIREBASE_SA_PATH present on Pod; [[runpod-gpu-env]] 2026-06-15 recipe)
    - .claude/projects memory [[gsd-pod-work-push-first]] / [[pod-ops-claude-runs]] / [[reference-library-phase4-all11]]
  </read_first>
  <action>
    First build two Node admin scripts UNDER app/scripts (R2-2 — firebase-admin is only installed under
    app/, so a backend/scripts .mjs cannot resolve firebase-admin; mirror seed-reference-body-profile.mjs
    init: applicationDefault + projectId 'sunity-ai-coach', lazy import, keys-not-values logging). Run them
    as `cd app && node scripts/<name>.mjs ...`:

    - `snapshot-reference-phase14-state.mjs` (R2-1 broadened snapshot + R4): reads all 11
      `reference/{id}` and records, per motion, BOTH:
        (a) ACTIVE-POSE hashes: `activeVersion`, `angles` length + sha256, `joints3d` length + sha256,
            `anglesJointKeys`, and whether `versions/phase4_v1` exists.
        (b) FULL PHASE-14 FIELD STATE: for each of `meanAngles`, `techniqueProfile`,
            `bodyNormalizationProfile`, `forceDirectionPattern`, `captureViews`, their `*UpdatedAt`, and
            optional `forceDirectionPatternSource` / `forceSignalsWarnings` — record `{present: boolean,
            valueHash: string, value?: object}`. Include `bodyNormalizationProfile*` AND
            `bodyComparisonSourcePose*` explicitly: Phase 14 may repair bodyNormalizationProfile for
            references missing it, so it MUST be in the snapshot/rollback set (it is NOT only an active-pose
            field — R2-1).
      Modes: `--mode pre` writes `.planning/phases/14-reference-motion-registration/14-PRESEED-SNAPSHOT.json`
      (already gitignored — the local pre-seed JSON MAY hold the raw `value` for restore but MUST stay
      gitignored and secret-free; the run log keeps only the hashes, not raw arrays). `--mode post` re-reads
      the same fields, byte-level compares each active-pose hash to the pre snapshot, computes the
      downstream completeness, and WRITES the machine-readable gate file
      `.planning/phases/14-reference-motion-registration/14-BACKFILL-RUN-SUMMARY.json` =
      `{unchangedActivePoseCount, changedActivePoseCount, completeDownstreamFieldCount, seededMotionCount,
      perMotion:[...]}`. Any active-pose hash mismatch → exit non-zero.

    - `rollback-reference-downstream.mjs` (R2-1 RESTORE-aware runbook): reads the pre-seed snapshot and,
      per motion, for each Phase-14 field (meanAngles*/techniqueProfile*/bodyNormalizationProfile*/
      forceDirectionPattern*/captureViews* + their *UpdatedAt + optional forceDirectionPatternSource/
      forceSignalsWarnings):
        - if the field was ABSENT before seed (snapshot present:false) → FieldValue.delete() it;
        - if the field EXISTED before seed (snapshot present:true) → set(merge:true) it back to the exact
          previous `value` from the snapshot.
      It refuses to run if it would touch any active-pose field (joints3d/angles/activeVersion). --dry-run
      default; --confirm to execute. Requires the pre-seed snapshot to exist (cannot restore without it).

    Then the run, with an explicit Pod-down ABORT GATE, a Pod Firestore gate, and the pre/post hash gate:
    (1) git commit + push HEAD before any Pod work ([[gsd-pod-work-push-first]]) — Pod git pull must
    receive the new backfill script. (2) POD HEALTH GATE (abort condition): query the Pod qcf38vvsmub1y4
    `GET /health`. If /health is NOT ok (Pod down/unreachable), STOP immediately — record the failed
    health check in 14-BACKFILL-RUN.md and do NOT proceed to any backfill/seed/real-run step. The Pod is
    the ONLY valid HYBRID source; CPU yields NaN, so a degraded run is forbidden. Surface the blockage to
    belle (Pod restart needed). (3) POD FIRESTORE GATE (R2-3 + R3-2): on the Pod, after /health and BEFORE any S3
    download, run `python backend/scripts/backfill_reference_downstream.py --check-firestore --motions
    ref-climb ref-foxtop ref-foxtop-split ref-invert ref-sideway-spin ref-combo ref-elbow-twist-sister
    ref-kip-up ref-pdshape ref-peter-pan ref-power-spin` (ALL 11 explicitly — the cheap metadata gate
    verifies activeVersion + angles + anglesJointKeys + anglesFrames presence + frame-count sanity for
    every reference, NEVER calling S3 or RTMW, so a missing field on ANY non-ref-climb doc surfaces here
    not mid-pipeline); if it fails (Pod Firebase SA not mounted / ANY doc incomplete), STOP — record which
    motion failed in 14-BACKFILL-RUN.md and do NOT proceed to backfill (no S3/RTMW work). (4) PRE-SEED SNAPSHOT: run
    `cd app && node scripts/snapshot-reference-phase14-state.mjs --mode pre` and record the per-motion
    active-pose hashes + field-presence in 14-BACKFILL-RUN.md. (5) On the Pod: git pull; fail-fast import
    check (rtmlib/imageio/boto3/firebase-admin — abort if any import fails); run `python
    backend/scripts/backfill_reference_downstream.py --motions <all 11 explicitly> --dry-run` first to
    validate the split JSON shape (seedPayload + diagnostics) for all 11, then real-run to emit the JSON
    fixture (reference-downstream-backfill.json). The script's own all-or-nothing gate (R5) must pass (11/11
    computed, NaN/inf 0, failures 0, stored-vs-rerun maxAngleDelta <= 1.0 deg); if it exits non-zero, record
    the diagnostic JSON and STOP (no seed). (6) scp the JSON fixture to local. (7) Run `cd app && node
    scripts/seed-reference-downstream.mjs --input <fixture> --dry-run` to preview the
    skippedComplete/repairMissing/forceOverwrite split (seeder reads only seedPayload), then real-run
    (--force only if re-running over existing valid fields) — ADD-only merge, 11-id all-or-nothing, no
    active flip. (8) VERIFY-READ (distinct checkable step): run `cd app && node
    scripts/seed-reference-downstream.mjs --verify` AND `cd app && node scripts/audit-reference-fields.mjs`
    to read back all 11 and confirm the 4 new fields + captureViews present + non-empty
    (completeDownstreamFieldCount). (9) POST-SEED SNAPSHOT + HASH COMPARE (R4): run `cd app && node
    scripts/snapshot-reference-phase14-state.mjs --mode post`. If unchangedActivePoseCount != 11 OR
    changedActivePoseCount != 0 → write 14-BACKFILL-RUN.md as **FAIL**, run the RESTORE-aware rollback
    runbook for affected motions, and DO NOT proceed to the belle approval step (Task 2); treat any
    active-pose change as an incident.

    Record everything in 14-BACKFILL-RUN.md: the Pod /health gate result (pass/abort), the --check-firestore
    all-11 metadata gate result (R2-3 + R3-2 — per-motion activeVersion/angles/anglesJointKeys/anglesFrames
    presence + frame-count OK/FAIL for all 11, no S3/RTMW), proxy URL, the pre/post hash + field-state summary (the human-readable mirror of
    14-BACKFILL-RUN-SUMMARY.json: unchangedActivePoseCount / changedActivePoseCount /
    completeDownstreamFieldCount / seededMotionCount), dry-run summary (with the complete/repair/overwrite
    split), real-run result, the stored-vs-rerun angle gate result (maxAngleDelta per motion), and the
    11-motion verify-read table (motion_id × {meanAngles, techniqueProfile, bodyNormalizationProfile,
    forceDirectionPattern, captureViews} present/non-empty). Log keys-not-values (no AWS/SA/ADC secrets).
    Do NOT flip active version — that is belle's call in Task 2 (and not required for this phase). Append
    the restore-aware risk-response runbook table (below) to 14-BACKFILL-RUN.md as a standing operator
    policy section.
  </action>
  <verify>
    <automated>test -f .planning/phases/14-reference-motion-registration/14-BACKFILL-RUN.md && cd app && node --check scripts/snapshot-reference-phase14-state.mjs && node --check scripts/rollback-reference-downstream.mjs && cd .. && test -f .planning/phases/14-reference-motion-registration/14-BACKFILL-RUN-SUMMARY.json && node -e 'const s=require("./.planning/phases/14-reference-motion-registration/14-BACKFILL-RUN-SUMMARY.json"); if (s.unchangedActivePoseCount!==11||s.changedActivePoseCount!==0||s.completeDownstreamFieldCount!==11||s.seededMotionCount!==11) process.exit(1)' && grep -q "health" .planning/phases/14-reference-motion-registration/14-BACKFILL-RUN.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - HEAD committed + pushed before Pod pull (run log records the commit SHA)
    - POD HEALTH GATE enforced: Pod `GET /health` not ok → STOP before any snapshot/seed/backfill/real-run (CPU yields NaN); the run log records the /health result and, if aborted, no seed/real-run occurred
    - POD FIRESTORE GATE enforced (R2-3 + R3-2): `--check-firestore --motions <all 11 explicitly>` ran after /health and before any S3 download (cheap metadata check — no S3/RTMW — of activeVersion + angles + anglesJointKeys + anglesFrames + frame-count for ALL 11); failure (missing Pod Firebase SA / ANY doc incomplete) → STOP, the failed motion id recorded, no backfill
    - `app/scripts/snapshot-reference-phase14-state.mjs` + `app/scripts/rollback-reference-downstream.mjs` are valid JS (`node --check` exits 0, run via `cd app && node scripts/...`) and never write joints3d/angles/activeVersion (grep zero)
    - The pre-seed snapshot captures BOTH active-pose hashes AND the full Phase-14 field state ({present, valueHash, value?}) including bodyNormalizationProfile + bodyComparisonSourcePose (R2-1)
    - PRE-SEED snapshot taken before backfill; POST-SEED snapshot + byte-level hash compare taken after seed; `14-BACKFILL-RUN-SUMMARY.json` exists and the Node assertion passes (`unchangedActivePoseCount == 11`, `changedActivePoseCount == 0`, `completeDownstreamFieldCount == 11`, `seededMotionCount == 11`) — R4/R2-6 proven by JSON, not a prose grep
    - Backfill ran for all 11 explicitly (--motions); the script's all-or-nothing gate passed (11/11, NaN/inf 0, failures 0, stored-vs-rerun maxAngleDelta <= 1.0 deg) before any seed
    - Seeder dry-run (with skippedComplete/repairMissing/forceOverwrite split, reading only seedPayload) preceded real-run; ADD-only merge; 11-id all-or-nothing
    - VERIFY-READ (distinct step) recorded: `--verify` + audit read-back table shows the 4 new fields + captureViews present + non-empty for all 11
    - Any post-seed hash mismatch → run log written as FAIL, RESTORE-aware rollback runbook invoked, Task 2 NOT reached (incident path)
    - The restore-aware risk-response runbook table is appended to 14-BACKFILL-RUN.md
    - No secret values appear in the run log
  </acceptance_criteria>
  <done>The Pod-health gate AND the --check-firestore gate are enforced (no degraded CPU run, no mid-pipeline credential failure), a pre/post byte-level hash gate via 14-BACKFILL-RUN-SUMMARY.json PROVES the active pose unchanged (unchangedActivePoseCount==11 / changedActivePoseCount==0), all 11 references are backfilled all-or-nothing and verified (completeDownstreamFieldCount==11 / seededMotionCount==11), a RESTORE-aware rollback runbook + risk-response table stand by, and the full run is logged in 14-BACKFILL-RUN.md.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: belle visual spot-check of backfilled fields (before any production state change)</name>
  <files>.planning/phases/14-reference-motion-registration/14-BACKFILL-RUN.md</files>
  <action>
    Pause for belle to review the backfilled reference fields after the automated Task 1 run (which must
    have passed the post-seed hash gate — unchangedActivePoseCount==11 in 14-BACKFILL-RUN-SUMMARY.json).
    Present the 14-BACKFILL-RUN.md verify-read table, the pre/post hash JSON summary, and the spot-check
    guidance below. Do NOT flip activeVersion or make any production state change without belle's explicit
    approval ([[pod-ops-claude-runs]]). This is a verification checkpoint, not new implementation work.
  </action>
  <what-built>
    All 11 정은지 references now carry meanAngles + EXTEND(techniqueProfile) + BodyNormalizationProfile
    + ForceDirectionPattern + captureViews=1, ADD-only merged. The active phase4_v1 joints3d/angles are
    PROVABLY unchanged by a pre/post byte-level hash compare (unchangedActivePoseCount==11 in
    14-BACKFILL-RUN-SUMMARY.json). The 14-BACKFILL-RUN.md verify-read table lists each motion's field
    presence/non-emptiness, and the JSON summary shows the active pose untouched.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/14-reference-motion-registration/14-BACKFILL-RUN.md` and review the
       11-motion verify-read table — confirm all 4 new fields + captureViews are present + non-empty for
       every motion, and that `14-BACKFILL-RUN-SUMMARY.json` shows unchangedActivePoseCount==11 /
       changedActivePoseCount==0 (activeVersion=phase4_v1 unchanged, angles/joints3d hashes match).
    2. Spot-check 2-3 motions' backfilled values for sanity: EXTEND profile is conservative ("모르면 깎지
       않음"), BodyNormalizationProfile proportions are plausible, ForceDirectionPattern findings are not
       fabricated (empty list + umbrella warning is acceptable when no finding). Use the per-motion
       forceSignalsReportSummary (in diagnostics / run log, R7) to trace any odd finding.
    3. Confirm no active-version flip occurred (Phase 14 does not flip; any future flip is a separate
       belle-approved action — [[pod-ops-claude-runs]]).
  </how-to-verify>
  <verify>
    <human-check>belle confirms the 11-motion verify-read table is complete, the 14-BACKFILL-RUN-SUMMARY.json summary shows the active pose unchanged (unchangedActivePoseCount==11), and the spot-checked values are sane.</human-check>
  </verify>
  <acceptance_criteria>
    - belle confirms the verify-read table shows all 11 complete (4 fields + captureViews)
    - belle confirms the JSON summary proves active pose unchanged (no flip, hashes match)
    - belle confirms the spot-checked values are sane (no fabricated findings, conservative EXTEND)
  </acceptance_criteria>
  <done>belle has approved the backfilled fields for all 11 references; active pose confirmed unchanged by hash.</done>
  <resume-signal>Type "approved" if the 11-reference backfill looks correct, or describe issues to fix.</resume-signal>
</task>

</tasks>

<risk_response_runbook>
## 리스크 대응표 (stop / recover / propose)

> Standing operator policy for the real backfill+seed. Each row = how the executor STOPS, RECOVERS
> (RESTORE-aware rollback, never touch active pose), and what it PROPOSES next. Append this table to
> 14-BACKFILL-RUN.md. (Folded from 14-DIRECT-REVIEW.md § Risk Response.)

| Situation | Stop / Recover / Propose |
|-----------|--------------------------|
| Pod `/health` fails | STOP immediately; record only the health failure in 14-BACKFILL-RUN.md; no backfill/seed; CPU fallback forbidden (NaN). Propose: belle restarts/recreates Pod (Network Storage), then re-run from the health gate. |
| Pod `--check-firestore --motions <all 11>` fails (R2-3 + R3-2) | STOP before any S3/RTMW work; record the credential/read failure AND which motion(s) were incomplete. The Pod Firebase SA is not mounted OR a reference doc is missing activeVersion/angles/anglesJointKeys/anglesFrames. Propose: mount FIREBASE_SA_PATH/FIREBASE_SA_JSON on the Pod and/or repair the incomplete reference doc, re-run the all-11 check, then backfill. |
| S3 video missing for one motion | Do NOT emit a seedable fixture. Record missing key/bucket/motion id; treat as an S3 inventory/upload problem. The other 10 are NOT seeded (all-or-nothing). Propose: fix the S3 object, re-run all 11. |
| re-run angles differ from active `phase4_v1.angles` (maxAngleDelta > 1.0 deg) | STOP the entire seed (R1). This is a pose-version re-validation problem, not a derived-field backfill. Derived fields (meanAngles/EXTEND) already source STORED angles; the gate guards the force/body live-frame alignment. Propose: promote to a pose-version re-validation phase; do not seed. |
| ForcePattern findings look fabricated / too aggressive | If BEFORE real-run: discard the fixture, inspect the per-motion forceSignalsReportSummary in diagnostics (R7). If AFTER real-run: run `cd app && node scripts/rollback-reference-downstream.mjs --confirm` to RESTORE-aware revert ONLY the Phase-14 fields (delete absent-before, restore present-before); never touch active pose. Empty findings + umbrella warning is acceptable. Propose: re-run the affected motion diagnostically. |
| Seeder dry-run says some fields already exist | Do NOT blanket-skip. Use the complete/repair/overwrite split (R3): repair-missing is the default merge; --force only overwrites existing valid fields. Because rollback is restore-aware (R2-1), a later --force is safely reversible. Propose: repair-missing run for the incomplete motions. |
| Real seed accidentally writes wrong fields | Use the pre-seed snapshot ({present, valueHash, value?}) to scope the impact. Run `cd app && node scripts/rollback-reference-downstream.mjs --confirm` to RESTORE-aware revert ONLY Phase-14-added fields (delete fields absent before seed, restore fields present before seed). If `joints3d/angles/activeVersion` changed → STOP, treat as a separate incident (do not auto-recover). |
| ADC/auth fails locally | Keep only the dry-run output; do NOT real-run. Do not bypass via console manual edit. Propose: `gcloud auth application-default login` (sunity3412@gmail.com), then retry with the same fixture. |
| belle spot-check rejects values | Leave active pose untouched; run `cd app && node scripts/rollback-reference-downstream.mjs --confirm` for the rejected motion's Phase-14 fields (restore-aware). Record the rejected motion id + reason in the run log. Propose: diagnostic re-run for that motion only. |
</risk_response_runbook>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local → GitHub → Pod | Code must be pushed before Pod pull; un-pushed code = stale Pod run |
| Pod → Firestore (read gate) | --check-firestore reads ALL 11 docs (cheap metadata, no S3/RTMW) via Firebase SA before S3/RTMW (R2-3, R3-2) |
| Pod → S3 / Firestore-fixture | AWS creds + Firebase SA on Pod; fixture is the seed input |
| Node seeder → Firestore (write) | Real-run merges into production reference docs (seedPayload only) |
| snapshot/rollback → Firestore | Read (snapshot) + restore-aware delete/set (rollback); never touches active pose |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-08 | Tampering/DoS | partial backfill leaves active state inconsistent | mitigate | ADD-only merge; no activeVersion flip; 11-id all-or-nothing seed; per-motion verify-read; idempotent repair-missing |
| T-14-09 | Tampering | overwriting visually-verified active pose | mitigate | Pre/post byte-level sha256 hash compare (R4): unchangedActivePoseCount==11 / changedActivePoseCount==0 required (asserted on 14-BACKFILL-RUN-SUMMARY.json); helper payload excludes joints3d/angles/activeVersion (Plan 14-02 gate); rollback never touches active pose |
| T-14-10 | Information Disclosure | secrets in run log | mitigate | Log keys-not-values; no AWS/SA/ADC contents; snapshot stores hashes (run log) and gitignored value-bearing pre-seed JSON; PRESEED-SNAPSHOT.json gitignored |
| T-14-11 | Tampering | stale Pod code (un-pushed) | mitigate | Commit+push before pull ([[gsd-pod-work-push-first]]); run log records SHA |
| T-14-12 | DoS/Integrity | degraded CPU run when Pod down (NaN) | mitigate | Pod /health ABORT GATE — STOP before any snapshot/backfill/seed if /health not ok; no CPU fallback run |
| T-14-14 | Integrity | active-pose change masked by a prose "unchanged" assertion | mitigate | R4/R2-6 replace the prose assertion with a byte-level hash JSON summary file + Node assertion; any mismatch → FAIL + incident + rollback, no belle step |
| T-14-16 | Tampering/DoS | delete-only rollback loses pre-existing field values when --force overwrites or a field was repaired | mitigate | RESTORE-aware rollback (R2-1): pre-seed snapshot records {present, valueHash, value?}; rollback deletes absent-before fields and restores present-before values exactly |
| T-14-17 | Operational | Node script under backend/scripts cannot resolve firebase-admin | mitigate | Scripts live under app/scripts (R2-2) where firebase-admin is installed; run via `cd app && node scripts/...` |
| T-14-SC | Tampering | npm/pip installs | accept | No new packages; Pod deps already present ([[rtmw-blackwell-lean-bootstrap]]) |
</threat_model>

<verification>
- `cd app && node scripts/snapshot-reference-phase14-state.mjs --mode post` — writes 14-BACKFILL-RUN-SUMMARY.json: unchangedActivePoseCount==11, changedActivePoseCount==0, completeDownstreamFieldCount==11, seededMotionCount==11
- `node -e '...14-BACKFILL-RUN-SUMMARY.json...'` — Node assertion on the JSON gate passes (R2-6)
- `cd app && node scripts/seed-reference-downstream.mjs --verify` — all 11 show 4 new fields + captureViews present
- `cd app && node scripts/audit-reference-fields.mjs` — post-state confirms no reference missing the new fields; activeVersion=phase4_v1 unchanged
- `python backend/scripts/backfill_reference_downstream.py --check-firestore --motions <all 11 explicitly>` (on Pod) — credential + all-11 completeness gate passes (no S3/RTMW) before backfill (R2-3, R3-2); the all-11 metadata check result is recorded in 14-BACKFILL-RUN.md
- 14-BACKFILL-RUN.md records the Pod /health gate + --check-firestore gate + pre/post hash summary (run aborted if /health not ok, --check-firestore fails, or hash mismatch)
- `cd backend && python -m pytest tests/ -q` + `cd app && npm run typecheck` — green before /gsd-verify-work
- belle checkpoint approved (Task 2)
</verification>

<success_criteria>
- SC#1: all 11 references present in Mode 1 list (verify-read returns 11; normalize() surfaces the new fields, Plan 14-01 R2-4)
- SC#2: each reference has meanAngles + EXTEND + BodyNormalizationProfile + ForceDirectionPattern (completeDownstreamFieldCount==11)
- D-02 (R4/R2-6): active phase4_v1 joints3d/angles/activeVersion PROVABLY unchanged by pre/post byte-level hash, asserted on 14-BACKFILL-RUN-SUMMARY.json (unchangedActivePoseCount==11 / changedActivePoseCount==0)
- R5: seeding all-or-nothing (seededMotionCount==11)
- R2-1: pre-seed snapshot captures full Phase-14 field state; rollback is restore-aware (delete absent-before, restore present-before)
- R2-3/R3-2: Pod --check-firestore gate (all 11 IDs, cheap metadata, no S3/RTMW) enforced before S3/RTMW so a non-ref-climb completeness failure surfaces first; Pod /health gate enforced (no degraded CPU run when Pod down); restore-aware rollback runbook + risk-response table in place
- belle visual spot-check approved before any production state change
</success_criteria>

<output>
Create `.planning/phases/14-reference-motion-registration/14-03-SUMMARY.md` when done
</output>
