---
phase: 14-reference-motion-registration
plan: 03
type: execute
wave: 3
depends_on: ["14-02"]
files_modified:
  - .planning/phases/14-reference-motion-registration/14-BACKFILL-RUN.md
autonomous: false
requirements: [REF-01]
user_setup:
  - service: firebase-admin (ADC)
    why: "Node seeder real-run needs Application Default Credentials for project sunity-ai-coach"
    env_vars: []
    dashboard_config:
      - task: "gcloud auth application-default login (account sunity3412@gmail.com) — only if ADC not already present on the run machine"
        location: "local shell"
must_haves:
  truths:
    - "All 11 references carry meanAngles + EXTEND(techniqueProfile) + BodyNormalizationProfile + ForceDirectionPattern + captureViews after the backfill (SC#2)"
    - "A Firestore verify-read confirms the 4 new fields present + non-empty for all 11 (SC#1/SC#2)"
    - "The active phase4_v1 joints3d/angles/activeVersion are unchanged (D-02 — read-only on verified pose)"
    - "belle visually spot-checks the backfilled fields before any production state change is considered (no active flip in this phase)"
  artifacts:
    - path: ".planning/phases/14-reference-motion-registration/14-BACKFILL-RUN.md"
      provides: "Run log: Pod health gate, execution, dry-run output, seed result, verify-read table for 11 motions"
  key_links:
    - from: "Pod backfill_reference_downstream.py run"
      to: "seed-reference-downstream.mjs --verify"
      via: "JSON fixture scp + ADD-only merge + read-back of all 11"
      pattern: "merge|verify"
---

<objective>
Execute the end-to-end backfill on real data and prove the slice: run the Pod orchestrator across all
11 references (one RTMW re-inference each), seed the four new fields ADD-only via the Node seeder
(dry-run → real-run → verify), and confirm via a Firestore read-back that every one of the 11 carries
meanAngles + EXTEND + BodyNormalizationProfile + ForceDirectionPattern + captureViews — while the
active phase4_v1 pose stays untouched. A blocking human checkpoint puts belle's eyes on the backfilled
fields before any production state change.

Purpose: Phase 14's user-facing capability — "all 11 정은지 references are Mode-1-ready with the full
engine output set" — only becomes TRUE when the backfill actually runs and is verified. Pod ops = Claude
runs; flip = belle approves ([[pod-ops-claude-runs]]). Commit→push before Pod work ([[gsd-pod-work-push-first]]).
Output: 14-BACKFILL-RUN.md run log + verified Firestore state.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-reference-motion-registration/14-CONTEXT.md
@.planning/phases/14-reference-motion-registration/14-RESEARCH.md
@.planning/phases/14-reference-motion-registration/14-PATTERNS.md
@.planning/phases/14-reference-motion-registration/14-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit/push + Pod health gate + backfill run (all 11) + seed dry-run + real-run + verify-read</name>
  <files>.planning/phases/14-reference-motion-registration/14-BACKFILL-RUN.md</files>
  <read_first>
    - backend/scripts/backfill_reference_downstream.py (the orchestrator from Plan 14-02 — entrypoint + --motions + --dry-run)
    - app/scripts/seed-reference-downstream.mjs (the seeder from Plan 14-02 — --input/--dry-run/--force/--verify)
    - app/scripts/audit-reference-fields.mjs (Plan 14-01 read pattern, to confirm pre/post state)
    - .planning/STATE.md (Pod qcf38vvsmub1y4 RTX PRO 4500 UP, /health ok; Lambda RUNPOD_ANALYZE_URL synced; [[runpod-gpu-env]] 2026-06-15 recipe)
    - .claude/projects memory [[gsd-pod-work-push-first]] / [[pod-ops-claude-runs]] / [[reference-library-phase4-all11]]
  </read_first>
  <action>
    Order of operations (Claude runs the Pod work), with an explicit Pod-down ABORT GATE:
    (1) git commit + push HEAD before any Pod work ([[gsd-pod-work-push-first]]) — Pod git pull must
    receive the new scripts. (2) POD HEALTH GATE (abort condition): query the Pod qcf38vvsmub1y4
    `GET /health`. If /health is NOT ok (Pod down/unreachable), STOP immediately — record the failed
    health check in 14-BACKFILL-RUN.md and do NOT proceed to any backfill/seed/real-run step. The Pod is
    the ONLY valid HYBRID source; CPU yields NaN (RESEARCH Environment Availability), so a degraded run
    is forbidden. Surface the blockage to belle (Pod restart needed). Only if /health is ok: (3) On the
    Pod: git pull; fail-fast import check (rtmlib/imageio/boto3 — abort that motion/run if any import
    fails); run `python backend/scripts/backfill_reference_downstream.py --motions <all 11 explicitly>
    --dry-run` first to validate JSON shape for all 11, then real-run to emit the JSON fixture
    (reference-downstream-backfill.json). Confirm 11/11 computed, NaN 0, per-motion failures = 0 (or
    record any failures). (4) scp the JSON fixture to local. (5) Run
    `node app/scripts/seed-reference-downstream.mjs --input <fixture> --dry-run` to preview, then real-run
    (--force only if re-running over existing fields) — ADD-only merge, no active flip. (6) VERIFY-READ
    (distinct checkable step): run `node app/scripts/seed-reference-downstream.mjs --verify` AND
    `node app/scripts/audit-reference-fields.mjs` to read back all 11 and confirm the 4 new fields +
    captureViews present + non-empty, and that activeVersion is still phase4_v1 with joints3d/angles
    unchanged (D-02). Record everything in 14-BACKFILL-RUN.md: the Pod /health gate result (pass/abort),
    proxy URL, dry-run summary, real-run result, the 11-motion verify-read table (motion_id ×
    {meanAngles, techniqueProfile, bodyNormalizationProfile, forceDirectionPattern, captureViews}
    present/non-empty), and an explicit "activeVersion=phase4_v1 unchanged" assertion. Log
    keys-not-values (no AWS/SA/ADC secrets in the run log). Do NOT flip active version — that is belle's
    call in Task 2 (and not required for this phase).
  </action>
  <verify>
    <automated>test -f .planning/phases/14-reference-motion-registration/14-BACKFILL-RUN.md && grep -c "ref-climb\|ref-foxtop\|ref-foxtop-split\|ref-invert\|ref-sideway-spin\|ref-combo\|ref-elbow-twist-sister\|ref-kip-up\|ref-pdshape\|ref-peter-pan\|ref-power-spin" .planning/phases/14-reference-motion-registration/14-BACKFILL-RUN.md | grep -qv '^0$' && grep -q "activeVersion" .planning/phases/14-reference-motion-registration/14-BACKFILL-RUN.md && grep -q "health" .planning/phases/14-reference-motion-registration/14-BACKFILL-RUN.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - HEAD committed + pushed before Pod pull (run log records the commit SHA)
    - POD HEALTH GATE enforced: Pod `GET /health` not ok → STOP before any seed/backfill/real-run (CPU yields NaN per RESEARCH); the run log records the /health result and, if aborted, no seed/real-run occurred (no degraded CPU run)
    - Backfill ran for all 11 explicitly (--motions); run log shows 11/11 computed, NaN count, any per-motion failures
    - Seeder dry-run preceded real-run; ADD-only merge
    - VERIFY-READ (distinct step) recorded: `--verify` + audit read-back table shows the 4 new fields + captureViews present + non-empty for all 11
    - Run log asserts activeVersion=phase4_v1 + joints3d/angles unchanged (D-02 read-only)
    - No secret values appear in the run log
  </acceptance_criteria>
  <done>The Pod-health gate is enforced (no degraded CPU run), all 11 references are backfilled and verified (4 new fields + captureViews present), the active pose is provably unchanged, and the full run is logged in 14-BACKFILL-RUN.md.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: belle visual spot-check of backfilled fields (before any production state change)</name>
  <files>.planning/phases/14-reference-motion-registration/14-BACKFILL-RUN.md</files>
  <action>
    Pause for belle to review the backfilled reference fields after the automated Task 1 run. Present
    the 14-BACKFILL-RUN.md verify-read table and the spot-check guidance below. Do NOT flip activeVersion
    or make any production state change without belle's explicit approval ([[pod-ops-claude-runs]]). This
    is a verification checkpoint, not new implementation work.
  </action>
  <what-built>
    All 11 정은지 references now carry meanAngles + EXTEND(techniqueProfile) + BodyNormalizationProfile
    + ForceDirectionPattern + captureViews=1, ADD-only merged. The active phase4_v1 joints3d/angles are
    unchanged. The 14-BACKFILL-RUN.md verify-read table lists each motion's field presence/non-emptiness.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/14-reference-motion-registration/14-BACKFILL-RUN.md` and review the
       11-motion verify-read table — confirm all 4 new fields + captureViews are present + non-empty for
       every motion, and that activeVersion=phase4_v1 is unchanged.
    2. Spot-check 2-3 motions' backfilled values for sanity: EXTEND profile is conservative ("모르면 깎지
       않음"), BodyNormalizationProfile proportions are plausible, ForceDirectionPattern findings are not
       fabricated (empty list + umbrella warning is acceptable when no finding).
    3. Confirm no active-version flip occurred (Phase 14 does not flip; any future flip is a separate
       belle-approved action — [[pod-ops-claude-runs]]).
  </how-to-verify>
  <verify>
    <human-check>belle confirms the 11-motion verify-read table is complete and the spot-checked values are sane; active pose unchanged.</human-check>
  </verify>
  <acceptance_criteria>
    - belle confirms the verify-read table shows all 11 complete (4 fields + captureViews)
    - belle confirms active pose unchanged (no flip)
    - belle confirms the spot-checked values are sane (no fabricated findings, conservative EXTEND)
  </acceptance_criteria>
  <done>belle has approved the backfilled fields for all 11 references; active pose confirmed unchanged.</done>
  <resume-signal>Type "approved" if the 11-reference backfill looks correct, or describe issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local → GitHub → Pod | Code must be pushed before Pod pull; un-pushed code = stale Pod run |
| Pod → S3 / Firestore-fixture | AWS creds + Firebase SA on Pod; fixture is the seed input |
| Node seeder → Firestore (write) | Real-run merges into production reference docs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-08 | Tampering/DoS | partial backfill leaves active state inconsistent | mitigate | ADD-only merge; no activeVersion flip in this phase; per-motion verify-read; idempotent --force re-run |
| T-14-09 | Tampering | overwriting visually-verified active pose | mitigate | Run log asserts joints3d/angles/activeVersion unchanged; helper payload excludes them (Plan 14-02 gate) |
| T-14-10 | Information Disclosure | secrets in run log | mitigate | Log keys-not-values; no AWS/SA/ADC contents in 14-BACKFILL-RUN.md |
| T-14-11 | Tampering | stale Pod code (un-pushed) | mitigate | Commit+push before pull ([[gsd-pod-work-push-first]]); run log records SHA |
| T-14-12 | DoS/Integrity | degraded CPU run when Pod down (NaN) | mitigate | Pod /health ABORT GATE — STOP before any backfill/seed if /health not ok; no CPU fallback run |
| T-14-SC | Tampering | npm/pip installs | accept | No new packages; Pod deps already present ([[rtmw-blackwell-lean-bootstrap]]) |
</threat_model>

<verification>
- `node app/scripts/seed-reference-downstream.mjs --verify` — all 11 show 4 new fields + captureViews present
- `node app/scripts/audit-reference-fields.mjs` — post-state confirms no reference missing the new fields; activeVersion=phase4_v1 unchanged
- 14-BACKFILL-RUN.md records the Pod /health gate result (run aborted if /health not ok)
- `cd backend && python -m pytest tests/ -q` + `cd app && npm run typecheck` — green before /gsd-verify-work
- belle checkpoint approved (Task 2)
</verification>

<success_criteria>
- SC#1: all 11 references present in Mode 1 list (verify-read returns 11; normalize() unaffected)
- SC#2: each reference has meanAngles + EXTEND + BodyNormalizationProfile + ForceDirectionPattern (verify-read non-empty for all 11)
- D-02: active phase4_v1 joints3d/angles/activeVersion unchanged (read-only)
- Pod /health gate enforced (no degraded CPU run when Pod down)
- belle visual spot-check approved before any production state change
</success_criteria>

<output>
Create `.planning/phases/14-reference-motion-registration/14-03-SUMMARY.md` when done
</output>
</content>
</invoke>
