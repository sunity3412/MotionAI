---
phase: 15-mode-1-mode-3-testflight
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/scripts/sweep_phase15.py
  - backend/scripts/assert_falsepositive_gate.py
  - backend/scripts/upload_phase15_dataset.py
autonomous: true
requirements: [MODE-01, SCORE-04]
user_setup: []

must_haves:
  truths:
    - "정은지 6 페어 + combo (13 영상) 가 parse_upload_key 정규식(s3keys.py:18 ^uploads/[^/]+/[A-Za-z0-9]+\\.(mp4|mov)$)을 만족하는 키로 S3 sunity-motion-pilot-videos 에 업로드되어 있다 — 페어별 uid=phase15-{motion}(하이픈 허용), analysis_id=correct|fault(영숫자만)"
    - "비-reference S3 키를 입력으로 받는 sweep 변종이 _process 단일 path 를 그대로 호출한다 (신규 분석 경로 없음)"
    - "sweep 가 쓰는 모든 analysis doc 이 createdAt + updatedAt 을 설정해 get_previous_analysis(createdAt DESC order) 에서 누락되지 않는다 (sweep_phase8_1 은 sweepCreatedAtMs 만 씀 — 그대로 복제하면 Mode 3 deltaFromPrevious 붕괴)"
    - "페어별 uid 분리(phase15-{motion})로 success doc 의 prior-analysis lookup 이 같은 motion 의 fault doc 하고만 페어링된다 (cross-motion 누출 0, HIGH-1 per-pair uid isolation)"
    - "Mode 3 페어가 --pair-sequential 로 제출되어 fail doc 이 완전히 done + createdAt earlier 로 기록된 뒤에야 success doc 이 제출된다 (페어 내 병렬 금지)"
    - "위양성 assert 스크립트가 신규 run 의 severity/score 를 FROZEN 08.1 backend/judging_data/tilt_thresholds.yaml(checksum c94bb8) 에 대조하고 재calibrate 하지 않는다"
    - "11 reference 문서의 Mode-1 필수 4필드 존재 여부를 Firestore read 로 확인할 수 있다 (seed-reference-downstream.mjs --verify, Plan 03 에서 실행)"
  artifacts:
    - path: "backend/scripts/sweep_phase15.py"
      provides: "비-reference 임의 S3 키 → analysis doc(createdAt/updatedAt 포함) → pipeline._process → cleanup (sweep_phase8_1 패턴 재사용, --pair-sequential 추가)"
      contains: "_process"
    - path: "backend/scripts/assert_falsepositive_gate.py"
      provides: "신규 sweep evidence 를 08.1 frozen baseline 에 대조하는 evidence-doc 스타일 assert"
      contains: "c94bb8"
    - path: "backend/scripts/upload_phase15_dataset.py"
      provides: "13 dataset 영상 파일명 정규화 + parse_upload_key 호환 키(uploads/phase15-{motion}/correct|fault.mp4)로 S3 PUT (Content-Type 명시)"
  key_links:
    - from: "backend/scripts/sweep_phase15.py"
      to: "backend/functions/pipeline/app.py::_process"
      via: "직접 import + 호출 (분기 0)"
      pattern: "_process"
    - from: "backend/scripts/sweep_phase15.py"
      to: "firestore_admin.get_previous_analysis (createdAt DESC)"
      via: "createdAt/updatedAt 설정 + --pair-sequential 순차 제출"
      pattern: "createdAt"
    - from: "backend/scripts/upload_phase15_dataset.py"
      to: "backend/shared/python/sunity_shared/s3keys.py::parse_upload_key"
      via: "uploads/{uid}/{analysis_id}.{ext} 정규식 호환 키 생성 (uid=phase15-{motion}, analysis_id=correct|fault)"
      pattern: "uploads/phase15-"
    - from: "backend/scripts/assert_falsepositive_gate.py"
      to: "backend/judging_data/tilt_thresholds.yaml"
      via: "frozen checksum 대조 (read-only, no recalibrate)"
      pattern: "tilt_thresholds"
---

<objective>
Phase 15 의 Wave 0 도구 격차를 메우고 검증 데이터셋을 S3 에 올린다. RESEARCH §Wave 0 Gaps 의 3개 항목 + dataset 업로드를 닫는다: (1) 비-reference S3 키용 sweep 변종, (2) FROZEN 08.1 baseline 대조 위양성 assert 스크립트, (3) 13 dataset 영상 파일명 정규화 + parse_upload_key 호환 키로 S3 업로드. reference 필드 존재 확인은 Plan 03 에서 seed-reference-downstream.mjs --verify 로 수행한다.

Purpose: 실 E2E(Wave 2~3)가 타는 모든 도구를 Pod-독립적으로 먼저 준비한다. Pod 가 살아나기 전에 로컬에서 작성·단위 검증 가능한 순수 도구만 다룬다. MODE-01(Mode 1 sweep 입력 도구) + SCORE-04(위양성 assert 도구)의 토대.
Output: 2개 신규 스크립트 + 1개 업로드 스크립트 + S3 에 정규화된 13 영상.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-mode-1-mode-3-testflight/15-CONTEXT.md
@.planning/phases/15-mode-1-mode-3-testflight/15-RESEARCH.md
@.planning/phases/15-mode-1-mode-3-testflight/15-VALIDATION.md
@backend/scripts/sweep_phase8_1.py
@backend/scripts/backfill_reference_downstream.py
@backend/shared/python/sunity_shared/firestore_admin.py
@backend/shared/python/sunity_shared/s3keys.py
@.planning/phases/08.1-axis-metric-redesign/08.1-SWEEP-EVIDENCE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 비-reference S3 키용 sweep 변종 작성 (sweep_phase15.py)</name>
  <read_first>
    backend/scripts/sweep_phase8_1.py (canonical driver — S3 copy reference/{name}.mp4 at line 144, doc write, _process 직접 호출, cleanup, non-zero exit, --sweep-uid line 98; 주의: line 162 에서 sweepCreatedAtMs 만 쓰고 createdAt 은 쓰지 않음); backend/shared/python/sunity_shared/firestore_admin.py::get_previous_analysis (line 1322 — status==done 필터 + createdAt DESC order; createdAt 없는 doc 은 이 lookup 에서 누락됨); backend/functions/pipeline/app.py::_process (단일 분석 path); backend/shared/python/sunity_shared/models.py (MODE_SELF / MODE_EXPERT 상수)
  </read_first>
  <action>
    sweep_phase8_1.py 를 복제·적응해 backend/scripts/sweep_phase15.py 를 만든다. 차이점: (1) 입력 S3 키가 reference/{name}.mp4 고정이 아니라 임의 키 목록(--keys-file JSON, 각 항목 s3Key/motionId/mode/label)을 받는다. (2) 신규 분석 path 를 절대 만들지 말고 sweep_phase8_1 과 동일하게 pipeline._process 를 직접 import·호출한다 (분기 0; RESEARCH Anti-Pattern "Writing a new analysis path" 금지). (3) 항목별 mode 로 MODE_EXPERT(Mode 1) 또는 MODE_SELF(Mode 3) 를 지정한다. Mode 1 항목은 referenceMotionId 필드를 analysis doc 에 박는다 (CONTEXT Integration Points: Mode 1 = referenceMotionId lockstep). (4) HIGH 1 — 쓰는 모든 analysis doc 에 createdAt 과 updatedAt 을 반드시 설정한다(server timestamp 또는 monotonic). sweep_phase8_1 은 sweepCreatedAtMs 만 쓰는데 그대로 복제하면 firestore_admin.get_previous_analysis(line 1322, createdAt DESC order) 가 sweep doc 을 prior 후보에서 누락시켜 Mode 3 deltaFromPrevious 가 붕괴한다 — sweepCreatedAtMs 외에 createdAt/updatedAt 을 추가로 박아야 한다. (5) HIGH 1 per-pair uid isolation — sweep_uid 를 페어별로 phase15_keys.json 항목의 키 prefix(uploads/phase15-{motion}/...) 에서 파생된 motion 단위로 분리하거나, 각 doc 이 속한 페어 외 다른 motion 의 doc 을 prior 후보로 잡지 않도록 uid scope 를 motion 단위로 격리한다 — success doc 의 get_previous_analysis 가 같은 motion 의 fault doc 하고만 페어링되어야 한다(cross-motion 누출 0). Task 3 가 키를 uploads/phase15-{motion}/correct|fault.mp4 형태(페어별 uid=phase15-{motion}, analysis_id=correct|fault)로 이미 올려두므로, parse_upload_key 가 복원하는 uid 자체가 motion 단위 격리를 제공한다. (6) HIGH 1 — --pair-sequential 모드를 추가한다: Mode 3 페어를 처리할 때 한 motion 의 fail doc 이 완전히 status done 으로 기록되고 그 createdAt 이 success doc 보다 earlier 임을 보장한 뒤에만 success doc 을 제출한다. 페어 내부는 절대 병렬 제출하지 않는다(직전 fail 이 prior 로 잡히도록). sweep_phase8_1 의 cleanup·commit-hash·checksum hygiene·non-zero-exit-on-failure 동작을 보존한다. dataset 키는 Task 3 가 이미 S3 에 올렸으므로 S3-to-S3 copy 단계는 생략하고 기존 키를 직접 분석 입력으로 쓴다. --dry-run 지원 (Pod 없이 로컬 import·인자 파싱 검증). 한국어 주석으로 sweep_phase8_1 대비 차이를 명시하고 "Phase 15 / CONTEXT D-05 / HIGH 1 createdAt+pair-sequential+per-pair-uid" 인용.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && PYTHONPATH=backend/shared/python python3 backend/scripts/sweep_phase15.py --help</automated>
  </verify>
  <acceptance_criteria>
    --help exit 0 (로컬, GPU 의존 없이); --keys-file / mode 선택 / --sweep-uid / --pair-sequential / --dry-run 인자 존재; 파일 내 _process 직접 호출 grep -v '^#' backend/scripts/sweep_phase15.py 후 _process 카운트 1 이상; 파일 내 createdAt 설정 로직 존재 (grep -v '^#' 후 createdAt 카운트 1 이상 — sweepCreatedAtMs 만 쓰지 않음); 페어별 uid 격리(phase15-{motion} 단위)로 success doc 의 prior lookup 이 같은 motion fault 하고만 페어링; --pair-sequential 가 페어 내 fail→success 순차 제출 보장; --dry-run 이 GPU/Pod 접근 없이 통과
  </acceptance_criteria>
  <done>sweep_phase15.py 가 임의 S3 키 목록을 mode 별로 _process 에 흘려보내고, 모든 doc 에 createdAt/updatedAt 을 박으며, 페어별 uid 격리 + --pair-sequential 로 Mode 3 페어를 순차 제출하고, dry-run 이 로컬에서 통과한다.</done>
</task>

<task type="auto">
  <name>Task 2: 위양성 evidence-assert 스크립트 (assert_falsepositive_gate.py)</name>
  <read_first>
    .planning/phases/08.1-axis-metric-redesign/08.1-SWEEP-EVIDENCE.md (frozen baseline — thresholdChecksum sha256 c94bb8c7cc87120255c244548bd59464840130cbf9144fd109490588a3e1e87c, 정은지 25/25 'low' invariant, null_tilt_verified=true); backend/judging_data/tilt_thresholds.yaml (schema_v2, read-only — frozen 임계값의 single source-of-truth 경로); backend/shared/python/sunity_shared/analysis/force_signals.py:250 (이 경로를 로드하는 loader — assert 는 동일 경로를 import/mirror; _severity_from_tilt boundary='low', tilt_thresholds_fallback 카운터)
  </read_first>
  <action>
    backend/scripts/assert_falsepositive_gate.py 신설. 입력 = Firestore sweep evidence 문서(Task 1 sweep_uid scoped) + 분류 라벨(success/fail — 영상 입력 라벨만, CONTEXT D-06 ground-truth 점수 라벨 영구 금지). 동작: (1) MEDIUM 5 — 실행 시작에 backend/judging_data/tilt_thresholds.yaml 의 sha256 을 계산해 08.1 evidence 의 c94bb8c7cc87120255c244548bd59464840130cbf9144fd109490588a3e1e87c 와 일치하는지 hard gate — 경로는 force_signals.py:250 와 동일 경로를 import/mirror 하고(backend/shared/.../analysis/tilt_thresholds.yaml 같은 존재하지 않는 경로 금지), 불일치 시 non-zero exit + frozen baseline drift 메시지(재calibrate 차단, CONTEXT D-02 / calibration-source-hard-gate). (2) MEDIUM 6 — success 영상: axis severity 'low' (08.1 25/25 'low' invariant mirror) + 명시된 최소 점수 기대치(있으면)를 assert; fail 영상: 객관적 machine 기준 — fail 영상이 all-'low' severity 가 아님 / deficit 또는 finding 키를 노출함(가용 필드 기준)을 assert. 정확한 per-fault 기준을 지금 객관적으로 핀할 수 없으면 fail-video FAULT-TYPE assertion 을 "manual evidence review" 로 다운그레이드하고 자동 per-fault gating 은 Phase 18 로 명시 deferred(plan note). 임계 기준은 08.1 evidence + IPSF baseline 의 기존 확정값만 사용하고 신규 fit 금지. (3) MEDIUM 5 — evidence 가 분석된 force-signal warning 전반에서 tilt_thresholds_fallback 카운트 == 0 임을 확인(fail-open 탐지). threshold 를 이 스크립트에서 재산출/재저장하지 않는다 — 오직 대조만. evidence 문서·stdout 에 API 키를 절대 출력하지 않는다(RESEARCH Security: secret leakage 차단). per-video PASS/FAIL 표를 출력하고 하나라도 FAIL 이면 non-zero exit. --dry-run 에서는 checksum hard gate + 인자 파싱만 검증.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && PYTHONPATH=backend/shared/python python3 backend/scripts/assert_falsepositive_gate.py --help && grep -c c94bb8c7cc87120255c244548bd59464840130cbf9144fd109490588a3e1e87c backend/scripts/assert_falsepositive_gate.py</automated>
  </verify>
  <acceptance_criteria>
    --help exit 0; 파일에 frozen checksum c94bb8...e87c 상수 1회 이상 박힘; threshold 경로가 backend/judging_data/tilt_thresholds.yaml (force_signals.py:250 와 동일) — 존재하지 않는 analysis/ 경로 미사용; checksum 불일치 시 non-zero exit 로직 존재; tilt_thresholds_fallback == 0 검증 존재; 어디서도 calibrate/재fit 호출 없음(grep 으로 calibrate_tilt_thresholds import 0); success=low-severity, fail=객관 machine 기준(또는 manual-review 다운그레이드 + Phase 18 defer note) assert 분기 존재
  </acceptance_criteria>
  <done>assert_falsepositive_gate.py 가 backend/judging_data/tilt_thresholds.yaml 의 frozen checksum 을 hard-gate 하고, fallback==0 을 확인하며, success/fail evidence 를 객관 machine 기준으로 baseline 에 대조한다(재calibrate 0).</done>
</task>

<task type="auto">
  <name>Task 3: dataset 파일명 정규화 + parse_upload_key 호환 키로 S3 업로드 (upload_phase15_dataset.py)</name>
  <read_first>
    backend/shared/python/sunity_shared/s3keys.py (line 18 _UPLOAD_KEY_RE = ^uploads/(?P<uid>[^/]+)/(?P<analysis_id>[A-Za-z0-9]+)\.(?P<ext>mp4|mov)$ — uid 는 하이픈 허용, analysis_id 는 [A-Za-z0-9]+ 영숫자만; build_upload_key/parse_upload_key); app/scripts/seed-reference-motions.mjs (S3 presign + Content-Type 패턴, 재사용 참고); RESEARCH §Environment Availability (dataset = 7 success in 잘된 예시/fixtures:*-correct.mp4 + 6 fail 한글명 top-level, 더블 확장자 오타 fixtures:pdshape-correct.mp4  .mp4, combo=Mode-1-only); memory s3-presigned-video-playback (PUT 에 Content-Type 누락 시 octet-stream 저장)
  </read_first>
  <action>
    backend/scripts/upload_phase15_dataset.py 신설. 소스 = ~/Downloads/정은지 선수 추가 영상/ (잘된 예시/ 의 7 success + top-level 6 fail 한글명). 13 영상을 parse_upload_key 정규식(s3keys.py:18)을 만족하는 정규 S3 키로 업로드한다. R1 — 키 스킴은 페어별 uid + 영숫자 analysis_id 로 구성한다: success → uploads/phase15-{motion}/correct.mp4, fail → uploads/phase15-{motion}/fault.mp4 (예: uploads/phase15-elbow-twist-sister/correct.mp4). uid=phase15-{motion} 은 [^/]+ 라 하이픈 허용이고, analysis_id 는 correct|fault 영숫자라 [A-Za-z0-9]+ 를 통과한다 — 이전 스킴 uploads/phase15/{motion}-correct.mp4 는 analysis_id 가 {motion}-correct 라 하이픈 때문에 정규식 FAIL → Pod /analyze(server.py:201 parse_upload_key) 가 HTTP 400 "invalid key format" 반환. combo 는 자체 uid phase15-combo, analysis_id correct (uploads/phase15-combo/correct.mp4), Mode-1-only 로 표기(페어 없음, Deferred). 이 페어별 uid 격리는 R1+HIGH-1 을 동시에 만족한다(각 motion 페어가 독립 uid 라 Plan 01 Task 1 의 prior-analysis lookup 이 같은 motion 의 correct/fault 하고만 페어링). 동작 (CONTEXT D-05 / discretion 파일명 정규화): (1) 한글 fail 파일명(엘보트위스트시스터/클라임/킵업/파워스핀/피터팬/pdshape)을 motion 슬러그로 매핑(elbow-twist-sister/climb/kip-up/power-spin/peter-pan/pdshape). (2) fixtures: prefix 와 더블 확장자 오타 "pdshape-correct.mp4  .mp4" 를 정규화. (3) 각 PUT 에 Content-Type video/mp4 를 반드시 지정(memory s3-presigned-video-playback — 누락 시 octet-stream). (4) 업로드 전 각 키가 s3keys.py 의 _UPLOAD_KEY_RE 정규식을 통과하는지 self-check (불일치 키는 업로드 중단). AWS 자격증명은 sunity-motion 사용(RESEARCH Pitfall 2; sunity-api 는 Motion AI 권한 없음). 업로드 후 motion↔S3키↔mode 매핑을 backend/scripts/phase15_keys.json 으로 출력(Task 1 sweep_phase15.py 의 --keys-file 입력으로 직접 소비; 각 항목 s3Key/motionId/mode/label). --dry-run 으로 매핑·파일명 정규화·정규식 self-check 만 검증.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && python3 backend/scripts/upload_phase15_dataset.py --dry-run</automated>
  </verify>
  <acceptance_criteria>
    --dry-run exit 0 + 13 항목 매핑 출력(7 success + 6 fail, combo=mode1-only 표기); 모든 키가 ^uploads/[^/]+/[A-Za-z0-9]+\.(mp4|mov)$ 정규식 통과(s3keys.py:18) — uid=phase15-{motion}, analysis_id=correct|fault, combo=phase15-combo/correct, 이전 하이픈-analysis_id 스킴 미사용; 각 항목에 Content-Type video/mp4 명시; 더블 확장자/한글명 정규화 처리 확인; 업로드 전 정규식 self-check 존재; phase15_keys.json 스키마가 sweep_phase15.py --keys-file 과 호환(s3Key/motionId/mode/label)
  </acceptance_criteria>
  <done>13 dataset 영상이 parse_upload_key 호환 키(uploads/phase15-{motion}/correct|fault.mp4)로 S3 에 업로드되고 sweep 입력용 phase15_keys.json 이 생성된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local disk → S3 | belle 로컬 영상이 S3 sunity-motion-pilot-videos 로 PUT (Content-Type 강제, parse_upload_key 호환 키) |
| script → Firestore | sweep/assert 스크립트가 Admin SA 로 Firestore 읽고 씀 (보안 규칙 우회 경로) |
| script → tilt_thresholds.yaml | assert 스크립트가 frozen 임계값을 read-only 대조 (backend/judging_data/ 경로) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.01-01 | Tampering | assert_falsepositive_gate.py 가 임계값을 재calibrate | mitigate | backend/judging_data/tilt_thresholds.yaml sha256 hard gate 가 c94bb8...e87c 와 불일치 시 non-zero exit; calibrate_* import 금지 (D-02) |
| T-15.01-02 | Information Disclosure | sweep/assert evidence 또는 stdout 에 API 키/secret 노출 | mitigate | evidence 문서·로그에 키 출력 금지; SSM/Pod env 만 사용, 코드 하드코딩 0 |
| T-15.01-03 | Information Disclosure | S3 presigned PUT 에 Content-Type 누락 → octet-stream 저장 → 재생 실패 | mitigate | upload 스크립트가 모든 PUT 에 video/mp4 강제 (memory s3-presigned-video-playback) |
| T-15.01-04 | Spoofing | objectivity 위반 — 사람 작성 분석 .md 의 점수를 ground-truth 라벨로 사용 | mitigate | 입력 라벨은 success/fail 분류만, 점수 라벨 영구 금지 (D-06, analysis-objectivity-no-human-scores) |
| T-15.01-05 | Tampering | sweep doc 이 createdAt 누락 → get_previous_analysis 에서 prior 누락 → Mode 3 false-first-analysis | mitigate | 모든 analysis doc 에 createdAt/updatedAt 설정 + --pair-sequential 순차 제출 (HIGH 1) |
| T-15.01-06 | Tampering | S3 키가 parse_upload_key 정규식 불충족(하이픈 analysis_id) → Pod /analyze HTTP 400 → 전체 E2E 차단 | mitigate | 키 스킴 uploads/phase15-{motion}/correct|fault.mp4 (uid 하이픈 허용, analysis_id 영숫자); 업로드 전 정규식 self-check (R1, s3keys.py:18) |
| T-15.01-07 | Information Disclosure | cross-motion prior 누출 — success doc 이 다른 motion 의 doc 을 prior 로 잡음 | mitigate | 페어별 uid 격리(phase15-{motion}) — parse_upload_key 가 복원한 uid 가 motion 단위 scope 제공 (HIGH-1) |
| T-15.01-SC | Tampering | npm/pip/cargo installs | accept | Phase 15 는 신규 패키지 0 (RESEARCH Package Legitimacy: skipped) — install 작업 없음 |
</threat_model>

<verification>
- 3 스크립트 모두 --help / --dry-run 가 GPU·Pod 없이 로컬 exit 0
- sweep_phase15.py: 모든 doc createdAt/updatedAt 설정 + 페어별 uid 격리 + --pair-sequential 존재 (HIGH 1)
- assert 스크립트에 frozen checksum 박힘 + backend/judging_data/ 경로 + fallback==0 + 재calibrate import 0
- dataset 업로드 dry-run 이 13 항목 정규화 매핑 출력 + 모든 키 parse_upload_key 정규식(s3keys.py:18) 통과 (R1)
- 회귀: 기존 backend pytest 영향 없음(신규 스크립트는 import-only) — PYTHONPATH=backend/shared/python python3 -m pytest backend/tests -q
</verification>

<success_criteria>
- sweep_phase15.py: 임의 S3 키 목록 → _process 직접 호출, 모든 doc createdAt/updatedAt 설정, 페어별 uid 격리 + --pair-sequential, dry-run 통과 (MODE-01 도구, HIGH 1)
- assert_falsepositive_gate.py: backend/judging_data/tilt_thresholds.yaml frozen checksum hard-gate + fallback==0 + success/fail assert, 재calibrate 0 (SCORE-04 도구, MEDIUM 5/6)
- upload_phase15_dataset.py: 13 영상 parse_upload_key 호환 키(uploads/phase15-{motion}/correct|fault.mp4)로 정규화 업로드 + phase15_keys.json 산출 (R1)
- reference 필드 확인은 Plan 03 에서 seed-reference-downstream.mjs --verify 로 수행
</success_criteria>

<output>
Create `.planning/phases/15-mode-1-mode-3-testflight/15-01-SUMMARY.md` when done
</output>
</content>
</invoke>
