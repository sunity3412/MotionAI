---
phase: 15-mode-1-mode-3-testflight
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/scripts/sweep_phase15.py
  - backend/scripts/assert_falsepositive_gate.py
  - backend/scripts/upload_phase15_dataset.py
autonomous: true
requirements: [MODE-01, SCORE-04]
user_setup: []

must_haves:
  truths:
    - "정은지 6 페어 + combo (13 영상) 가 정규화된 파일명으로 S3 sunity-motion-pilot-videos 에 업로드되어 있다"
    - "비-reference S3 키를 입력으로 받는 sweep 변종이 _process 단일 path 를 그대로 호출한다 (신규 분석 경로 없음)"
    - "위양성 assert 스크립트가 신규 run 의 severity/score 를 FROZEN 08.1 tilt_thresholds.yaml(checksum c94bb8) 에 대조하고 재calibrate 하지 않는다"
    - "11 reference 문서의 Mode-1 필수 4필드 존재 여부를 Firestore read 로 확인할 수 있다 (--check-firestore)"
  artifacts:
    - path: "backend/scripts/sweep_phase15.py"
      provides: "비-reference 임의 S3 키 → analysis doc → pipeline._process → cleanup (sweep_phase8_1 패턴 재사용)"
      contains: "_process"
    - path: "backend/scripts/assert_falsepositive_gate.py"
      provides: "신규 sweep evidence 를 08.1 frozen baseline 에 대조하는 evidence-doc 스타일 assert"
      contains: "c94bb8"
    - path: "backend/scripts/upload_phase15_dataset.py"
      provides: "13 dataset 영상 파일명 정규화 + S3 PUT (Content-Type 명시)"
  key_links:
    - from: "backend/scripts/sweep_phase15.py"
      to: "backend/functions/pipeline/app.py::_process"
      via: "직접 import + 호출 (분기 0)"
      pattern: "_process"
    - from: "backend/scripts/assert_falsepositive_gate.py"
      to: "backend/shared/python/sunity_shared/analysis/tilt_thresholds.yaml"
      via: "frozen checksum 대조 (read-only, no recalibrate)"
      pattern: "tilt_thresholds"
---

<objective>
Phase 15 의 Wave 0 도구 격차를 메우고 검증 데이터셋을 S3 에 올린다. RESEARCH §Wave 0 Gaps 의 3개 항목 + dataset 업로드를 닫는다: (1) 비-reference S3 키용 sweep 변종, (2) FROZEN 08.1 baseline 대조 위양성 assert 스크립트, (3) 13 dataset 영상 파일명 정규화 + S3 업로드. reference 필드 존재 확인은 기존 backfill_reference_downstream.py 의 --check-firestore 모드를 재사용한다.

Purpose: 실 E2E(Wave 2~3)가 타는 모든 도구를 Pod-독립적으로 먼저 준비한다. Pod 가 살아나기 전에 로컬에서 작성·단위 검증 가능한 순수 도구만 다룬다. MODE-01(Mode 1 sweep 입력 도구) + SCORE-04(위양성 assert 도구)의 토대.
Output: 2개 신규 스크립트 + 1개 업로드 스크립트 + S3 에 정규화된 13 영상.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-mode-1-mode-3-testflight/15-CONTEXT.md
@.planning/phases/15-mode-1-mode-3-testflight/15-RESEARCH.md
@.planning/phases/15-mode-1-mode-3-testflight/15-VALIDATION.md
@backend/scripts/sweep_phase8_1.py
@backend/scripts/backfill_reference_downstream.py
@.planning/phases/08.1-axis-metric-redesign/08.1-SWEEP-EVIDENCE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 비-reference S3 키용 sweep 변종 작성 (sweep_phase15.py)</name>
  <read_first>
    backend/scripts/sweep_phase8_1.py (canonical driver — S3 copy reference/{name}.mp4 at line 144, doc write, _process 직접 호출, cleanup, non-zero exit, --sweep-uid line 98); backend/functions/pipeline/app.py::_process (단일 분석 path); backend/shared/python/sunity_shared/models.py (MODE_SELF / MODE_EXPERT 상수)
  </read_first>
  <action>
    sweep_phase8_1.py 를 복제·적응해 backend/scripts/sweep_phase15.py 를 만든다. 차이점만: 입력 S3 키가 reference/{name}.mp4 고정이 아니라 임의 키 목록(--keys-file JSON, 각 항목 s3Key/motionId/mode/label)을 받는다. 신규 분석 path 를 절대 만들지 말고 sweep_phase8_1 과 동일하게 pipeline._process 를 직접 import·호출한다 (분기 0; RESEARCH Anti-Pattern "Writing a new analysis path" 금지). 항목별 mode 로 MODE_EXPERT(Mode 1) 또는 MODE_SELF(Mode 3) 를 지정한다. Mode 1 항목은 referenceMotionId 필드를 analysis doc 에 박는다 (CONTEXT Integration Points: Mode 1 = referenceMotionId lockstep). sweep_phase8_1 의 cleanup·commit-hash·checksum hygiene·non-zero-exit-on-failure 동작을 보존한다. dataset 키는 Task 3 가 이미 S3 에 올렸으므로 S3-to-S3 copy 단계는 생략하고 기존 키를 직접 분석 입력으로 쓴다. --dry-run 지원 (Pod 없이 로컬 import·인자 파싱 검증). 한국어 주석으로 sweep_phase8_1 대비 차이를 명시하고 "Phase 15 / CONTEXT D-05" 인용.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && PYTHONPATH=backend/shared/python python3 backend/scripts/sweep_phase15.py --help</automated>
  </verify>
  <acceptance_criteria>
    --help exit 0 (로컬, GPU 의존 없이); --keys-file / mode 선택 / --sweep-uid / --dry-run 인자 존재; 파일 내 _process 직접 호출 grep -v '^#' backend/scripts/sweep_phase15.py 후 _process 카운트 1 이상; --dry-run 이 GPU/Pod 접근 없이 통과
  </acceptance_criteria>
  <done>sweep_phase15.py 가 임의 S3 키 목록을 mode 별로 _process 에 흘려보내고 dry-run 이 로컬에서 통과한다.</done>
</task>

<task type="auto">
  <name>Task 2: 위양성 evidence-assert 스크립트 (assert_falsepositive_gate.py)</name>
  <read_first>
    .planning/phases/08.1-axis-metric-redesign/08.1-SWEEP-EVIDENCE.md (frozen baseline — thresholdChecksum sha256 c94bb8c7cc87120255c244548bd59464840130cbf9144fd109490588a3e1e87c, 정은지 25/25 'low' invariant, null_tilt_verified=true); backend/shared/python/sunity_shared/analysis/tilt_thresholds.yaml (schema_v2, read-only); backend/shared/python/sunity_shared/analysis/force_signals.py (_severity_from_tilt boundary='low')
  </read_first>
  <action>
    backend/scripts/assert_falsepositive_gate.py 신설. 입력 = Firestore sweep evidence 문서(Task 1 sweep_uid scoped) + 분류 라벨(success/fail — 영상 입력 라벨만, CONTEXT D-06 ground-truth 점수 라벨 영구 금지). 동작: (1) 실행 시작에 tilt_thresholds.yaml 의 sha256 을 계산해 08.1 evidence 의 c94bb8c7cc87120255c244548bd59464840130cbf9144fd109490588a3e1e87c 와 일치하는지 hard gate — 불일치 시 non-zero exit + frozen baseline drift 메시지(재calibrate 차단, CONTEXT D-02 / calibration-source-hard-gate). (2) success 영상: overallScore 가 높음 + axis severity 가 low 임을 assert (임계 기준은 08.1 evidence + IPSF baseline 의 기존 확정값만 사용, 신규 fit 금지). (3) fail 영상: 분석기가 fault 를 독립적으로 잡아냈는지(해당 dimension deficit/finding 존재) + fail 에 높은 점수를 주지 않았는지 assert. threshold 를 이 스크립트에서 재산출/재저장하지 않는다 — 오직 대조만. evidence 문서·stdout 에 API 키를 절대 출력하지 않는다(RESEARCH Security: secret leakage 차단). per-video PASS/FAIL 표를 출력하고 하나라도 FAIL 이면 non-zero exit. --dry-run 에서는 checksum hard gate + 인자 파싱만 검증.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && PYTHONPATH=backend/shared/python python3 backend/scripts/assert_falsepositive_gate.py --help && grep -c c94bb8c7cc87120255c244548bd59464840130cbf9144fd109490588a3e1e87c backend/scripts/assert_falsepositive_gate.py</automated>
  </verify>
  <acceptance_criteria>
    --help exit 0; 파일에 frozen checksum c94bb8...e87c 상수 1회 이상 박힘; checksum 불일치 시 non-zero exit 로직 존재; 어디서도 calibrate/재fit 호출 없음(grep 으로 calibrate_tilt_thresholds import 0); success=low-severity, fail=fault-caught assert 분기 존재
  </acceptance_criteria>
  <done>assert_falsepositive_gate.py 가 frozen checksum 을 hard-gate 하고 success/fail evidence 를 baseline 에 대조한다(재calibrate 0).</done>
</task>

<task type="auto">
  <name>Task 3: dataset 파일명 정규화 + S3 업로드 (upload_phase15_dataset.py)</name>
  <read_first>
    app/scripts/seed-reference-motions.mjs (S3 presign + Content-Type 패턴, 재사용 참고); RESEARCH §Environment Availability (dataset = 7 success in 잘된 예시/fixtures:*-correct.mp4 + 6 fail 한글명 top-level, 더블 확장자 오타 fixtures:pdshape-correct.mp4  .mp4, combo=Mode-1-only); memory s3-presigned-video-playback (PUT 에 Content-Type 누락 시 octet-stream 저장)
  </read_first>
  <action>
    backend/scripts/upload_phase15_dataset.py 신설. 소스 = ~/Downloads/정은지 선수 추가 영상/ (잘된 예시/ 의 7 success + top-level 6 fail 한글명). 13 영상을 정규 S3 키로 업로드한다: success → uploads/phase15/{motion}-correct.mp4, fail → uploads/phase15/{motion}-fault.mp4. 동작 (CONTEXT D-05 / discretion 파일명 정규화): (1) 한글 fail 파일명(엘보트위스트시스터/클라임/킵업/파워스핀/피터팬/pdshape)을 motion 슬러그로 매핑(elbow-twist-sister/climb/kip-up/power-spin/peter-pan/pdshape). (2) fixtures: prefix 와 더블 확장자 오타 "pdshape-correct.mp4  .mp4" 를 정규화. (3) 각 PUT 에 Content-Type video/mp4 를 반드시 지정(memory s3-presigned-video-playback — 누락 시 octet-stream). (4) combo 는 Mode-1-only 로 표기(페어 없음, Deferred). AWS 자격증명은 sunity-motion 사용(RESEARCH Pitfall 2; sunity-api 는 Motion AI 권한 없음). 업로드 후 motion↔S3키↔mode 매핑을 backend/scripts/phase15_keys.json 으로 출력(Task 1 sweep_phase15.py 의 --keys-file 입력으로 직접 소비). --dry-run 으로 매핑·파일명 정규화만 검증.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && python3 backend/scripts/upload_phase15_dataset.py --dry-run</automated>
  </verify>
  <acceptance_criteria>
    --dry-run exit 0 + 13 항목 매핑 출력(7 success + 6 fail, combo=mode1-only 표기); 각 항목에 Content-Type video/mp4 명시; 더블 확장자/한글명 정규화 처리 확인; phase15_keys.json 스키마가 sweep_phase15.py --keys-file 과 호환(s3Key/motionId/mode/label)
  </acceptance_criteria>
  <done>13 dataset 영상이 정규 키로 S3 에 업로드되고 sweep 입력용 phase15_keys.json 이 생성된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local disk → S3 | belle 로컬 영상이 S3 sunity-motion-pilot-videos 로 PUT (Content-Type 강제) |
| script → Firestore | sweep/assert 스크립트가 Admin SA 로 Firestore 읽고 씀 (보안 규칙 우회 경로) |
| script → tilt_thresholds.yaml | assert 스크립트가 frozen 임계값을 read-only 대조 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.01-01 | Tampering | assert_falsepositive_gate.py 가 임계값을 재calibrate | mitigate | sha256 hard gate 가 c94bb8...e87c 와 불일치 시 non-zero exit; calibrate_* import 금지 (D-02) |
| T-15.01-02 | Information Disclosure | sweep/assert evidence 또는 stdout 에 API 키/secret 노출 | mitigate | evidence 문서·로그에 키 출력 금지; SSM/Pod env 만 사용, 코드 하드코딩 0 |
| T-15.01-03 | Information Disclosure | S3 presigned PUT 에 Content-Type 누락 → octet-stream 저장 → 재생 실패 | mitigate | upload 스크립트가 모든 PUT 에 video/mp4 강제 (memory s3-presigned-video-playback) |
| T-15.01-04 | Spoofing | objectivity 위반 — 사람 작성 분석 .md 의 점수를 ground-truth 라벨로 사용 | mitigate | 입력 라벨은 success/fail 분류만, 점수 라벨 영구 금지 (D-06, analysis-objectivity-no-human-scores) |
| T-15.01-SC | Tampering | npm/pip/cargo installs | accept | Phase 15 는 신규 패키지 0 (RESEARCH Package Legitimacy: skipped) — install 작업 없음 |
</threat_model>

<verification>
- 3 스크립트 모두 --help / --dry-run 가 GPU·Pod 없이 로컬 exit 0
- assert 스크립트에 frozen checksum 박힘 + 재calibrate import 0
- dataset 업로드 dry-run 이 13 항목 정규화 매핑 출력
- 회귀: 기존 backend pytest 영향 없음(신규 스크립트는 import-only) — PYTHONPATH=backend/shared/python python3 -m pytest backend/tests -q
</verification>

<success_criteria>
- sweep_phase15.py: 임의 S3 키 목록 → _process 직접 호출, dry-run 통과 (MODE-01 도구)
- assert_falsepositive_gate.py: frozen 08.1 checksum hard-gate + success/fail assert, 재calibrate 0 (SCORE-04 도구)
- upload_phase15_dataset.py: 13 영상 정규화 업로드 + phase15_keys.json 산출
- reference 필드 확인은 backfill_reference_downstream.py --check-firestore 재사용 확인(Plan 03 Task 1 에서 실행)
</success_criteria>

<output>
Create `.planning/phases/15-mode-1-mode-3-testflight/15-01-SUMMARY.md` when done
</output>
