---
phase: 15-mode-1-mode-3-testflight
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/scripts/sweep_phase15.py
  - backend/scripts/assert_falsepositive_gate.py
  - backend/scripts/upload_phase15_dataset.py
  - backend/scripts/phase15_keys.json
autonomous: true
requirements: [MODE-01, SCORE-04]
user_setup: []

must_haves:
  truths:
    - "정은지 6 페어 + combo (13 영상) 가 비-notified fixtures 프리픽스 fixtures/phase15/{motion}/correct|fault.mp4 (combo=fixtures/phase15/combo/correct.mp4) 로 S3 sunity-motion-pilot-videos 에 immutable SOURCE 로 업로드되어 있다 — uploads/ ObjectCreated notification 이 절대 발화하지 않는다 (HIGH 1)"
    - "비-reference S3 키를 입력으로 받는 sweep 변종이 _process 단일 path 를 그대로 호출한다 (신규 분석 경로 없음)"
    - "analysis identity 가 SOURCE fixture 키와 분리되어 실행마다·mode 마다 새로 생성된다 — fixed uid/analysisId 재사용 0 (HIGH 2): Mode 1 uid=phase15_mode1_<runId>, Mode 3 uid=phase15_mode3_<motion>_<runId>, analysisId 는 영숫자 [A-Za-z0-9]+ 만 (하이픈 슬러그 금지 — sanitize: elbow-twist-sister→elbowtwistsister 또는 short-hash) + runId stamp 로 rerun 충돌 0 (HIGH 2)"
    - "controlled trigger 가 mutually-exclusive 단일 경로다 — --trigger 플래그로 direct-process(default) / notification / analyze 중 정확히 하나만 쓰고 나머지 둘은 호출되지 않는다. direct-process 는 fixtures/ SOURCE 키를 _process 에 그대로 넘겨 직접 분석하므로 uploads/ COPY 가 ZERO 다(uploads/ ObjectCreated 비발화 → S3 notification double-trigger 0); notification 만 uploads/ 로 COPY 한다 (S3 notification + manual _process 동시 사용으로 같은 객체 double-analysis 금지, HIGH 1/MEDIUM 4)"
    - "sweep 가 쓰는 모든 analysis doc 이 createdAt + updatedAt 을 설정해 get_previous_analysis(createdAt DESC order) 에서 누락되지 않는다 (sweep_phase8_1 은 sweepCreatedAtMs 만 씀 — 그대로 복제하면 Mode 3 deltaFromPrevious 붕괴)"
    - "notification/analyze 모드에서 생성하는 모든 analysisId 가 dry-run 시 parse_upload_key(또는 ^uploads/[^/]+/[A-Za-z0-9]+\\.(mp4|mov)$ 정규식)를 통과함을 build-every-key assertion 으로 증명한다 — 비영숫자 analysisId 는 거부; direct-process 는 uploads/ 키를 만들지 않으므로 생성된 uid/analysisId 쌍의 영숫자 형식만 자체 검증한다 (HIGH 2)"
    - "evidence 표가 runId/uid/analysisId/mode/s3Key/createdAt 을 기록해 stale·overwritten doc 이 가시적으로 거부된다 (HIGH 2)"
    - "위양성 assert 스크립트가 신규 run 의 severity/score 를 FROZEN 08.1 backend/judging_data/tilt_thresholds.yaml(checksum c94bb8) 에 대조하고 재calibrate 하지 않는다"
    - "11 reference 문서의 Mode-1 필수 4필드 존재 여부를 Firestore read 로 확인할 수 있다 (seed-reference-downstream.mjs --verify, Plan 03 에서 실행)"
  artifacts:
    - path: "backend/scripts/sweep_phase15.py"
      provides: "비-reference SOURCE 키 → per-run/per-mode analysis identity(영숫자 analysisId, uid, createdAt/updatedAt 포함) → --trigger 단일 경로(direct-process default = fixtures/ 키로 _process 직접 호출, uploads/ COPY 0) → pipeline._process → cleanup (sweep_phase8_1 패턴 재사용, --pair-sequential 추가, notification/analyze 모드만 build-every-key parse assertion)"
      contains: "_process"
    - path: "backend/scripts/assert_falsepositive_gate.py"
      provides: "신규 sweep evidence 를 08.1 frozen baseline 에 대조하는 evidence-doc 스타일 assert"
      contains: "c94bb8"
    - path: "backend/scripts/upload_phase15_dataset.py"
      provides: "13 dataset 영상 파일명 정규화 + 비-notified fixtures/phase15/{motion}/correct|fault.mp4 SOURCE 키로 S3 PUT (Content-Type 명시, uploads/ 트리거 비발화)"
    - path: "backend/scripts/phase15_keys.json"
      provides: "upload_phase15_dataset.py 산출 매핑 — 각 항목 sourceS3Key(fixtures/ 키)/motionId/mode/label/referenceMotionId; sweep_phase15.py --keys-file 입력 (analysis identity 아님)"
  key_links:
    - from: "backend/scripts/sweep_phase15.py"
      to: "backend/functions/pipeline/app.py::_process"
      via: "직접 import + 호출 (분기 0); direct-process 는 fixtures/ SOURCE 키를 그대로 key 인자로 전달 (app.py:1633 _process 가 전달 uid/analysisId 사용 + bucket/key 직접 download, key 재파싱 없음)"
      pattern: "_process"
    - from: "backend/scripts/sweep_phase15.py"
      to: "firestore_admin.get_previous_analysis (createdAt DESC)"
      via: "per-run uid/analysisId + createdAt/updatedAt 설정 + --pair-sequential 순차 제출"
      pattern: "createdAt"
    - from: "backend/scripts/sweep_phase15.py"
      to: "backend/shared/python/sunity_shared/s3keys.py::parse_upload_key"
      via: "notification/analyze 모드에서만 doc 생성 후 sanitized 영숫자 analysisId 로 uploads/{uid}/{analysisId}.mp4 build + dry-run parse self-check; direct-process 는 uploads/ 키 미생성"
      pattern: "parse_upload_key"
    - from: "backend/scripts/upload_phase15_dataset.py"
      to: "S3 sunity-motion-pilot-videos (fixtures/ 비-notified)"
      via: "fixtures/phase15/{motion}/correct|fault.mp4 SOURCE PUT (uploads/ notification 비발화)"
      pattern: "fixtures/phase15/"
    - from: "backend/scripts/assert_falsepositive_gate.py"
      to: "backend/judging_data/tilt_thresholds.yaml"
      via: "frozen checksum 대조 (read-only, no recalibrate)"
      pattern: "tilt_thresholds"
---

<objective>
Phase 15 의 Wave 0 도구 격차를 메우고 검증 데이터셋을 S3 에 올린다. RESEARCH §Wave 0 Gaps 의 3개 항목 + dataset 업로드를 닫는다: (1) 비-reference S3 키용 sweep 변종, (2) FROZEN 08.1 baseline 대조 위양성 assert 스크립트, (3) 13 dataset 영상 파일명 정규화 + 비-notified fixtures/ SOURCE 키로 S3 업로드. reference 필드 존재 확인은 Plan 03 에서 seed-reference-downstream.mjs --verify 로 수행한다.

Purpose: 실 E2E(Wave 2~3)가 타는 모든 도구를 Pod-독립적으로 먼저 준비한다. Pod 가 살아나기 전에 로컬에서 작성·단위 검증 가능한 순수 도구만 다룬다. SOURCE fixture(immutable, 비-notified)와 analysis identity(per-run/per-mode 생성)를 분리해 production S3→SQS→pipeline 트리거가 fixture 업로드로 의도치 않게 발화하거나(HIGH 1) 같은 doc path 가 mode/rerun 간에 충돌하는(HIGH 2) 일을 차단한다. 특히 direct-process(default trigger)는 fixtures/ SOURCE 키를 _process 에 그대로 넘겨 직접 분석하므로 uploads/ 로의 COPY 가 전혀 없어 uploads/ ObjectCreated notification 이 발화하지 않는다 — 이로써 같은 객체가 manual _process 와 S3→SQS→Lambda delegate 양쪽으로 double-analysis 되는 일을 원천 차단한다(HIGH 1). 모든 analysisId 는 영숫자만(s3keys.py:18 analysis_id=[A-Za-z0-9]+ 통과 — 하이픈 슬러그 금지, HIGH 2). MODE-01(Mode 1 sweep 입력 도구) + SCORE-04(위양성 assert 도구)의 토대.
Output: 2개 신규 스크립트 + 1개 업로드 스크립트 + phase15_keys.json + S3 fixtures/ 에 정규화된 13 영상.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-mode-1-mode-3-testflight/15-CONTEXT.md
@.planning/phases/15-mode-1-mode-3-testflight/15-RESEARCH.md
@.planning/phases/15-mode-1-mode-3-testflight/15-VALIDATION.md
@backend/scripts/sweep_phase8_1.py
@backend/scripts/backfill_reference_downstream.py
@backend/shared/python/sunity_shared/firestore_admin.py
@backend/shared/python/sunity_shared/s3keys.py
@backend/functions/pipeline/app.py
@backend/README.md
@.planning/phases/08.1-axis-metric-redesign/08.1-SWEEP-EVIDENCE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 비-reference S3 키용 sweep 변종 작성 (sweep_phase15.py)</name>
  <read_first>
    backend/scripts/sweep_phase8_1.py (canonical driver — S3 copy reference/{name}.mp4 at line 144, doc write, _process 직접 호출, cleanup, non-zero exit, --sweep-uid line 98; 주의: line 162 에서 sweepCreatedAtMs 만 쓰고 createdAt 은 쓰지 않음); backend/functions/pipeline/app.py::_process (line 1633 — _process(bucket, key, uid, analysis_id): 전달된 uid/analysis_id 로 update_analysis_status/get_analysis 호출 + line 1663 _extract_video_analysis_inputs(bucket, key, ...) 로 bucket/key 직접 download — key 를 parse_upload_key 로 재파싱하지 않음 → fixtures/ 키로 직접 호출 가능; line 1678 _resolve_is_reference(key, meta) 는 key prefix 1차 + Firestore mode 2차라 MODE_SELF/MODE_EXPERT doc 의 mode 가 우선); backend/shared/python/sunity_shared/firestore_admin.py::get_previous_analysis (line 1322 — status==done 필터 + createdAt DESC order; createdAt 없는 doc 은 이 lookup 에서 누락됨); backend/shared/python/sunity_shared/s3keys.py (line 17 _UPLOAD_KEY_RE = ^uploads/(?P<uid>[^/]+)/(?P<analysis_id>[A-Za-z0-9]+)\.(mp4|mov)$ — uid 는 [^/]+ 라 하이픈/언더스코어 허용, analysis_id 는 [A-Za-z0-9]+ 영숫자만 — 하이픈 절대 불가; build_upload_key/parse_upload_key); backend/README.md (line 106-107 — S3 notification = s3:ObjectCreated:* prefix uploads/; fixtures/ 는 비-notified 라 트리거 비발화); backend/shared/python/sunity_shared/models.py (MODE_SELF / MODE_EXPERT 상수)
  </read_first>
  <action>
    sweep_phase8_1.py 를 복제·적응해 backend/scripts/sweep_phase15.py 를 만든다. 차이점:
    (1) 입력은 immutable SOURCE fixture 키 목록(--keys-file phase15_keys.json, 각 항목 sourceS3Key=fixtures/phase15/{motion}/correct|fault.mp4 / motionId / mode / label) 이다. SOURCE 키는 절대 분석 doc identity 로 쓰지 않는다 — SOURCE 와 identity 는 분리한다(HIGH 2).
    (2) 신규 분석 path 를 절대 만들지 말고 sweep_phase8_1 과 동일하게 pipeline._process 를 직접 import·호출한다 (분기 0; RESEARCH Anti-Pattern "Writing a new analysis path" 금지). 항목별 mode 로 MODE_EXPERT(Mode 1) 또는 MODE_SELF(Mode 3) 를 지정한다. Mode 1 항목은 referenceMotionId(ref-{motion}) 를 analysis doc 에 박는다 (CONTEXT Integration Points: Mode 1 = referenceMotionId lockstep).
    (3) HIGH 2 — analysis identity 를 실행마다·mode 마다 새로 생성한다(fixed uid/analysisId 재사용 절대 금지). 실행 시작에 runId(영숫자 stamp, 예: epoch_ms 또는 hex)를 한 번 만든다. analysisId 는 반드시 영숫자([A-Za-z0-9]+)만 사용한다 — motion 슬러그는 하이픈을 포함하므로(elbow-twist-sister, kip-up, power-spin, peter-pan) 절대 raw 슬러그를 analysisId 에 넣지 않고, 하이픈을 제거한 sanitized 슬러그(elbow-twist-sister→elbowtwistsister) 또는 short hash(예: m<shorthash>)로 변환한 뒤 role+runId 를 붙인다. Mode 1 항목은 uid=phase15_mode1_<runId> + analysisId=<sanitizedSlug>Success<runId>(또는 m<shorthash>Success<runId>), referenceMotionId=ref-{motion} 로, Mode 3 항목은 uid=phase15_mode3_{motion}_<runId> + analysisId=<sanitizedSlug>Fault<runId> 그다음 <sanitizedSlug>Success<runId>(영숫자, fault 가 earlier createdAt) 로 생성한다. uid 의 언더스코어는 s3keys.py:18 의 uid=[^/]+ 를 통과하지만, analysisId 는 영숫자만 통과하므로 하이픈을 절대 넣지 않는다.
    (4) HIGH 1/MEDIUM 4 — controlled trigger 는 mutually-exclusive 단일 경로다. --trigger 플래그로 정확히 하나의 모드만 선택한다:
    • direct-process(default — truly direct, uploads/ COPY 절대 없음): analysis doc(createdAt/updatedAt + mode + Mode 1 이면 referenceMotionId 포함) 을 생성한 뒤 곧바로 pipeline._process(bucket, sourceS3Key, uid, analysisId) 를 호출한다. key 인자로 항목의 fixtures/phase15/... SOURCE 키를 그대로 넘긴다 — _process(backend/functions/pipeline/app.py:1633)는 전달된 uid/analysisId 로 update_analysis_status/get_analysis 를 호출하고 _extract_video_analysis_inputs(bucket, key, ...)로 bucket/key 를 직접 download 할 뿐 key 를 parse_upload_key 로 재파싱하지 않으므로 fixtures/ 키로 직접 분석된다(line 1678 _resolve_is_reference 는 key prefix 1차 + Firestore mode 2차라 MODE_SELF/MODE_EXPERT doc 의 mode 가 우선). 따라서 direct-process 는 fixtures→uploads COPY 가 전혀 없고(uploads/ 로의 copy_object/put_object 호출 ZERO), _process 를 정확히 1회 호출하며, /analyze POST 도 하지 않는다 — 결정론적, 비동기 S3/SQS 타이밍 회피, uploads/ ObjectCreated notification 발화 0 → S3→SQS→Lambda delegate 와의 double-analysis race 원천 차단(HIGH 1). sweep evidence 기본값.
    • notification: analysis doc 생성 후 fixture 를 그 run 의 unique uploads/{uid}/{analysisId}.mp4 로 COPY 하고, 그 ObjectCreated 가 production S3→SQS→pipeline notification 을 발화해 분석을 구동한다. _process 직접 호출도 /analyze POST 도 하지 않는다.
    • analyze: analysis doc 생성 후 fixture 를 uploads/{uid}/{analysisId}.mp4 로 COPY 하고 직접 /analyze POST 한다(X-RunPod-Token). 주의 — /analyze 는 uploads/ 키를 요구하므로 live notified 버킷에서는 그 COPY 가 notification 도 발화해 double-trigger 가 된다. 따라서 analyze 모드는 live notified 버킷 비대상으로 명시하고(isolated/non-notified 환경 전용) 스크립트 헤더 주석·--help 에 그 경고를 남긴다.
    세 모드는 상호 배타적이며 한 run 에 정확히 하나만 invoke 된다 — 같은 객체에 둘 이상의 trigger(예: notification + manual _process)를 병행하면 double-analysis 가 되므로 절대 금지한다. 스크립트가 각 모드의 invariant 를 코드로 강제한다(카운터로 자체 검증): direct-process ⇒ uploads/ 로의 COPY/PUT 카운트 == 0 + _process 호출 == 1(per item) + /analyze POST == 0; notification ⇒ uploads/ COPY == 1 + _process 직접 호출 == 0 + /analyze == 0; analyze ⇒ /analyze POST == 1 + _process 직접 호출 == 0(+ live-notified 버킷 경고). 어느 trigger 모드가 선택됐는지와 나머지 둘이 호출되지 않았음을 evidence 와 스크립트 헤더 주석에 명시한다. SOURCE fixtures 키 자체는 절대 uploads/ 로 올리지 않는다(Task 3 가 fixtures/ 로 올림).
    (5) HIGH 1 — 쓰는 모든 analysis doc 에 createdAt 과 updatedAt 을 반드시 설정한다(server timestamp 또는 monotonic). sweep_phase8_1 은 sweepCreatedAtMs 만 쓰는데 그대로 복제하면 firestore_admin.get_previous_analysis(line 1322, createdAt DESC order) 가 sweep doc 을 prior 후보에서 누락시켜 Mode 3 deltaFromPrevious 가 붕괴한다 — sweepCreatedAtMs 외에 createdAt/updatedAt 을 추가로 박아야 한다.
    (6) per-run/per-mode uid 격리로 success doc 의 get_previous_analysis 가 같은 motion·같은 run 의 fault doc 하고만 페어링된다(cross-motion·cross-run 누출 0). uid=phase15_mode3_{motion}_<runId> 가 motion+run 단위 scope 를 제공한다.
    (7) HIGH 1 — --pair-sequential 모드를 추가한다: Mode 3 페어를 처리할 때 한 motion 의 fail doc 이 완전히 status done 으로 기록되고 그 createdAt 이 success doc 보다 earlier 임을 보장한 뒤에만 success doc 을 제출한다. 페어 내부는 절대 병렬 제출하지 않는다(직전 fail 이 prior 로 잡히도록).
    (8) HIGH 2 — evidence 출력(stdout + sweep evidence doc)이 페어·항목별로 runId/uid/analysisId/mode/s3Key(direct-process 는 fixtures/ SOURCE 키, notification/analyze 는 uploads/ 키)/createdAt 을 기록하게 한다(stale·overwritten doc 가시적 거부).
    (9) HIGH 2 — dry-run identity self-check + (notification/analyze 전용) build-every-key assertion: 실행 시작(또는 --dry-run)에 계획된 모든 항목의 생성 uid/analysisId 쌍을 만들어 analysisId 가 영숫자([A-Za-z0-9]+)인지 every item 검사하고, 비영숫자(특히 하이픈)가 하나라도 있으면 즉시 non-zero exit 으로 거부한다 — "영숫자" prose 에 의존하지 말고 실제로 검사한다. trigger 가 notification 또는 analyze 인 경우엔 추가로 각 항목의 uploads/{uid}/{analysisId}.mp4 키를 build 해 parse_upload_key(또는 ^uploads/[^/]+/[A-Za-z0-9]+\.(mp4|mov)$ 정규식)에 통과시켜 검증한다. direct-process 는 uploads/ 키를 만들지 않으므로(SOURCE fixtures/ 키로 직접 분석) build-every-uploads-key 단계가 없고, 그 대신 sourceS3Key 가 fixtures/phase15/ 프리픽스인지 + analysisId 가 영숫자인지만 검증한다.
    sweep_phase8_1 의 cleanup·commit-hash·checksum hygiene·non-zero-exit-on-failure 동작을 보존한다. --dry-run 지원 (Pod 없이 로컬 import·인자 파싱·identity 생성·trigger invariant 자체검증·every-key parse self-check 검증). 한국어 주석으로 sweep_phase8_1 대비 차이를 명시하고 "Phase 15 / CONTEXT D-05 / HIGH 1 fixture-vs-identity 분리 + direct-process=fixtures-key-no-uploads-copy + --trigger mutually-exclusive + createdAt + pair-sequential / HIGH 2 영숫자 analysisId + per-run identity + every-key parse" 인용.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && PYTHONPATH=backend/shared/python python3 backend/scripts/sweep_phase15.py --help</automated>
  </verify>
  <acceptance_criteria>
    --help exit 0 (로컬, GPU 의존 없이); --keys-file / mode 선택 / --trigger(direct-process|notification|analyze) / --pair-sequential / --dry-run 인자 존재; --trigger default 는 direct-process; 파일 내 _process 직접 호출 grep -v '^#' backend/scripts/sweep_phase15.py 후 _process 카운트 1 이상; direct-process 가 fixtures/ SOURCE 키를 _process key 인자로 그대로 넘기고 uploads/ 로의 copy_object/put_object 호출이 ZERO 임이 코드로 강제됨(direct-process 분기에 uploads COPY 없음 + uploads-copy 카운트 0 assert); identity 가 per-run/per-mode 로 생성됨 (runId/uid=phase15_mode1_ 또는 phase15_mode3_ 패턴 grep 존재, fixed correct|fault analysisId 재사용 0); 모든 analysisId 가 영숫자만([A-Za-z0-9]+) — raw 하이픈 슬러그를 analysisId 에 넣지 않음(sanitize/hash 로직 존재); dry-run 이 모든 항목 analysisId 영숫자 self-check 를 수행하고 비영숫자 analysisId 를 non-zero 로 거부하며, notification/analyze 모드에선 추가로 uploads 키를 build 해 parse_upload_key(또는 ^uploads/[^/]+/[A-Za-z0-9]+\.(mp4|mov)$ 정규식)에 통과시키는 assertion 존재; SOURCE fixtures 키와 analysis identity 가 분리됨 (sourceS3Key 와 uploads/{uid}/{analysisId} 가 서로 다른 변수; direct-process 는 sourceS3Key 만 사용); --trigger 가 mutually-exclusive(direct-process|notification|analyze 중 정확히 하나만 invoke, 나머지 둘 미호출 — acceptance 가 단순 _process grep 이 아니라 trigger 모드 배타성·모드별 invariant 카운터를 검사: direct-process uploads-copy==0 & _process==1 & analyze==0, notification uploads-copy==1 & _process==0, analyze /analyze==1 & _process==0); 모든 doc 에 createdAt 설정 로직 존재 (grep -v '^#' 후 createdAt 카운트 1 이상 — sweepCreatedAtMs 만 쓰지 않음); --pair-sequential 가 페어 내 fail→success 순차 제출 보장; evidence 출력에 runId/uid/analysisId/mode/s3Key/createdAt 기록; --dry-run 이 GPU/Pod 접근 없이 통과
  </acceptance_criteria>
  <done>sweep_phase15.py 가 immutable SOURCE fixture 키 목록을 받아 per-run/per-mode 영숫자 analysis identity 를 생성하고, --trigger 로 direct-process(default — fixtures/ 키를 _process 에 직접 넘겨 uploads/ COPY 0)/notification/analyze 중 mutually-exclusive 단일 경로로 _process 를 구동하며, 모든 doc 에 createdAt/updatedAt 을 박고, --pair-sequential 로 Mode 3 페어를 순차 제출하며, dry-run 이 모든 analysisId 를 영숫자 self-check(+notification/analyze 는 every-uploads-key parse) 하고 모드별 invariant(direct-process uploads-copy==0)를 강제하며, runId/uid/analysisId/mode/s3Key/createdAt evidence 를 기록한다.</done>
</task>

<task type="auto">
  <name>Task 2: 위양성 evidence-assert 스크립트 (assert_falsepositive_gate.py)</name>
  <read_first>
    .planning/phases/08.1-axis-metric-redesign/08.1-SWEEP-EVIDENCE.md (frozen baseline — thresholdChecksum sha256 c94bb8c7cc87120255c244548bd59464840130cbf9144fd109490588a3e1e87c, 정은지 25/25 'low' invariant, null_tilt_verified=true); backend/judging_data/tilt_thresholds.yaml (schema_v2, read-only — frozen 임계값의 single source-of-truth 경로); backend/shared/python/sunity_shared/analysis/force_signals.py:250 (이 경로를 로드하는 loader — assert 는 동일 경로를 import/mirror; _severity_from_tilt boundary='low', tilt_thresholds_fallback 카운터)
  </read_first>
  <action>
    backend/scripts/assert_falsepositive_gate.py 신설. 입력 = Firestore sweep evidence 문서(Task 1 per-run uid scoped) + 분류 라벨(success/fail — 영상 입력 라벨만, CONTEXT D-06 ground-truth 점수 라벨 영구 금지). 동작: (1) MEDIUM 5 — 실행 시작에 backend/judging_data/tilt_thresholds.yaml 의 sha256 을 계산해 08.1 evidence 의 c94bb8c7cc87120255c244548bd59464840130cbf9144fd109490588a3e1e87c 와 일치하는지 hard gate — 경로는 force_signals.py:250 와 동일 경로를 import/mirror 하고(backend/shared/.../analysis/tilt_thresholds.yaml 같은 존재하지 않는 경로 금지), 불일치 시 non-zero exit + frozen baseline drift 메시지(재calibrate 차단, CONTEXT D-02 / calibration-source-hard-gate). (2) MEDIUM 6 — success 영상: axis severity 'low' (08.1 25/25 'low' invariant mirror) + 명시된 최소 점수 기대치(있으면)를 assert; fail 영상: 객관적 machine 기준 — fail 영상이 all-'low' severity 가 아님 / deficit 또는 finding 키를 노출함(가용 필드 기준)을 assert. 정확한 per-fault 기준을 지금 객관적으로 핀할 수 없으면 fail-video FAULT-TYPE assertion 을 "manual evidence review" 로 다운그레이드하고 자동 per-fault gating 은 Phase 18 로 명시 deferred(plan note). 임계 기준은 08.1 evidence + IPSF baseline 의 기존 확정값만 사용하고 신규 fit 금지. (3) MEDIUM 5 — evidence 가 분석된 force-signal warning 전반에서 tilt_thresholds_fallback 카운트 == 0 임을 확인(fail-open 탐지). threshold 를 이 스크립트에서 재산출/재저장하지 않는다 — 오직 대조만. evidence 문서·stdout 에 API 키를 절대 출력하지 않는다(RESEARCH Security: secret leakage 차단). per-video PASS/FAIL 표를 출력하고 하나라도 FAIL 이면 non-zero exit. --dry-run 에서는 checksum hard gate + 인자 파싱만 검증.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && PYTHONPATH=backend/shared/python python3 backend/scripts/assert_falsepositive_gate.py --help && grep -c c94bb8c7cc87120255c244548bd59464840130cbf9144fd109490588a3e1e87c backend/scripts/assert_falsepositive_gate.py</automated>
  </verify>
  <acceptance_criteria>
    --help exit 0; 파일에 frozen checksum c94bb8...e87c 상수 1회 이상 박힘; threshold 경로가 backend/judging_data/tilt_thresholds.yaml (force_signals.py:250 와 동일) — 존재하지 않는 analysis/ 경로 미사용; checksum 불일치 시 non-zero exit 로직 존재; tilt_thresholds_fallback == 0 검증 존재; 어디서도 calibrate/재fit 호출 없음(grep 으로 calibrate_tilt_thresholds import 0); success=low-severity, fail=객관 machine 기준(또는 manual-review 다운그레이드 + Phase 18 defer note) assert 분기 존재
  </acceptance_criteria>
  <done>assert_falsepositive_gate.py 가 backend/judging_data/tilt_thresholds.yaml 의 frozen checksum 을 hard-gate 하고, fallback==0 을 확인하며, success/fail evidence 를 객관 machine 기준으로 baseline 에 대조한다(재calibrate 0).</done>
</task>

<task type="auto">
  <name>Task 3: dataset 파일명 정규화 + 비-notified fixtures/ SOURCE 키로 S3 업로드 (upload_phase15_dataset.py)</name>
  <read_first>
    backend/shared/python/sunity_shared/s3keys.py (line 17 _UPLOAD_KEY_RE = ^uploads/(?P<uid>[^/]+)/(?P<analysis_id>[A-Za-z0-9]+)\.(?P<ext>mp4|mov)$ — SOURCE fixtures 키는 이 정규식을 만족할 필요 없음, 분석 입력 아님; build_upload_key/parse_upload_key 는 uploads/ 전용); backend/README.md (line 79-107 — uploads/ prefix 에 lifecycle 30d + S3→SQS ObjectCreated:* notification; fixtures/ 프리픽스는 notification·lifecycle 비대상이라 트리거 비발화 + 영구 보존); app/scripts/seed-reference-motions.mjs (S3 presign + Content-Type 패턴, 재사용 참고); RESEARCH §Environment Availability (dataset = 7 success in 잘된 예시/fixtures:*-correct.mp4 + 6 fail 한글명 top-level, 더블 확장자 오타 fixtures:pdshape-correct.mp4  .mp4, combo=Mode-1-only); memory s3-presigned-video-playback (PUT 에 Content-Type 누락 시 octet-stream 저장)
  </read_first>
  <action>
    backend/scripts/upload_phase15_dataset.py 신설. 소스 = ~/Downloads/정은지 선수 추가 영상/ (잘된 예시/ 의 7 success + top-level 6 fail 한글명). 13 영상을 비-notified fixtures/ 프리픽스로 immutable SOURCE 로 업로드한다(HIGH 1).
    R1+HIGH 1 — 키 스킴은 fixtures/phase15/{motion}/correct.mp4(success) + fixtures/phase15/{motion}/fault.mp4(fail), combo 는 fixtures/phase15/combo/correct.mp4 (Mode-1-only, 페어 없음, Deferred) 다. fixtures/ 프리픽스는 backend/README.md 의 uploads/ ObjectCreated notification·lifecycle 대상이 아니므로(line 106-107 prefix=uploads/) 업로드 즉시 production S3→SQS→pipeline 트리거가 발화하지 않고 영구 SOURCE 로 보존된다. 이전 스킴 uploads/phase15-{motion}/correct|fault.mp4 는 parse-compatible 라서 업로드 즉시 notification 을 발화시켜(HIGH 1) Lambda/RunPod 가 doc 생성 전 partial 분석을 enqueue 할 수 있었다 — fixtures/ 로 옮겨 이 side-effect 를 제거한다. SOURCE fixtures 키는 parse_upload_key 정규식을 만족할 필요가 없다(분석 입력이 아니라 SOURCE 다 — sweep_phase15.py direct-process 가 이 fixtures/ 키를 _process 에 그대로 넘기고, notification/analyze 모드만 doc 생성 후 영숫자 analysisId 로 unique uploads/{uid}/{analysisId} 로 COPY).
    동작 (CONTEXT D-05 / discretion 파일명 정규화): (1) 한글 fail 파일명(엘보트위스트시스터/클라임/킵업/파워스핀/피터팬/pdshape)을 motion 슬러그로 매핑(elbow-twist-sister/climb/kip-up/power-spin/peter-pan/pdshape). (2) fixtures: prefix 와 더블 확장자 오타 "pdshape-correct.mp4  .mp4" 를 정규화. (3) 각 PUT 에 Content-Type video/mp4 를 반드시 지정(memory s3-presigned-video-playback — 누락 시 octet-stream). (4) 업로드 전 각 fixtures 키가 기대 스킴(fixtures/phase15/{motion}/correct|fault.mp4)을 따르는지 self-check. AWS 자격증명은 sunity-motion 사용(RESEARCH Pitfall 2; sunity-api 는 Motion AI 권한 없음). 업로드 후 motion↔SOURCE fixtures 키↔mode↔referenceMotionId(Mode 1) 매핑을 backend/scripts/phase15_keys.json 으로 출력(Task 1 sweep_phase15.py 의 --keys-file 입력으로 직접 소비; 각 항목 sourceS3Key/motionId/mode/label/referenceMotionId). sourceS3Key 는 fixtures/ 키이며 analysis identity 가 아니다(sweep direct-process 는 이 키로 직접 분석, notification/analyze 는 per-run 영숫자 analysisId 로 uploads/{uid}/{analysisId} 를 별도 생성). --dry-run 으로 매핑·파일명 정규화·키 스킴 self-check 만 검증.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && python3 backend/scripts/upload_phase15_dataset.py --dry-run</automated>
  </verify>
  <acceptance_criteria>
    --dry-run exit 0 + 13 항목 매핑 출력(7 success + 6 fail, combo=mode1-only 표기); 모든 SOURCE 키가 fixtures/phase15/{motion}/correct|fault.mp4 스킴(combo=fixtures/phase15/combo/correct.mp4) — uploads/ 프리픽스 미사용(notification 비발화, HIGH 1); 이전 uploads/phase15-{motion}/correct|fault.mp4 스킴 미사용; 각 항목에 Content-Type video/mp4 명시; 더블 확장자/한글명 정규화 처리 확인; 업로드 전 키 스킴 self-check 존재; phase15_keys.json 스키마가 sweep_phase15.py --keys-file 과 호환(sourceS3Key/motionId/mode/label/referenceMotionId)이고 sourceS3Key 가 fixtures/ 키(분석 identity 아님)
  </acceptance_criteria>
  <done>13 dataset 영상이 비-notified fixtures/phase15/{motion}/correct|fault.mp4 SOURCE 키로 S3 에 업로드되고(uploads/ 트리거 비발화), sweep 입력용 phase15_keys.json(sourceS3Key=fixtures/ 키, schema sourceS3Key/motionId/mode/label/referenceMotionId)이 생성된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local disk → S3 | belle 로컬 영상이 S3 sunity-motion-pilot-videos 의 비-notified fixtures/ 프리픽스로 PUT (Content-Type 강제, 트리거 비발화) |
| sweep → _process (direct-process) | sweep 가 doc 생성 후 fixtures/ SOURCE 키를 _process 에 그대로 넘겨 직접 분석 (uploads/ COPY 0, notification 비발화) |
| sweep → S3 uploads/ (notification/analyze) | notification/analyze 모드에서만 sweep 가 fixture 를 영숫자 analysisId 의 unique uploads/{uid}/{analysisId} 로 COPY |
| script → Firestore | sweep/assert 스크립트가 Admin SA 로 Firestore 읽고 씀 (보안 규칙 우회 경로) |
| script → tilt_thresholds.yaml | assert 스크립트가 frozen 임계값을 read-only 대조 (backend/judging_data/ 경로) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.01-01 | Tampering | assert_falsepositive_gate.py 가 임계값을 재calibrate | mitigate | backend/judging_data/tilt_thresholds.yaml sha256 hard gate 가 c94bb8...e87c 와 불일치 시 non-zero exit; calibrate_* import 금지 (D-02) |
| T-15.01-02 | Information Disclosure | sweep/assert evidence 또는 stdout 에 API 키/secret 노출 | mitigate | evidence 문서·로그에 키 출력 금지; SSM/Pod env 만 사용, 코드 하드코딩 0 |
| T-15.01-03 | Information Disclosure | S3 PUT 에 Content-Type 누락 → octet-stream 저장 → 재생 실패 | mitigate | upload 스크립트가 모든 PUT 에 video/mp4 강제 (memory s3-presigned-video-playback) |
| T-15.01-04 | Spoofing | objectivity 위반 — 사람 작성 분석 .md 의 점수를 ground-truth 라벨로 사용 | mitigate | 입력 라벨은 success/fail 분류만, 점수 라벨 영구 금지 (D-06, analysis-objectivity-no-human-scores) |
| T-15.01-05 | Tampering | sweep doc 이 createdAt 누락 → get_previous_analysis 에서 prior 누락 → Mode 3 false-first-analysis | mitigate | 모든 analysis doc 에 createdAt/updatedAt 설정 + --pair-sequential 순차 제출 (HIGH 1) |
| T-15.01-06 | Tampering | fixture 업로드가 uploads/ 프리픽스로 올라가 production S3→SQS→pipeline 트리거를 의도치 않게 발화 → doc 생성 전 partial 분석 enqueue | mitigate | SOURCE 는 비-notified fixtures/ 프리픽스로만 업로드(README line 106-107 prefix=uploads/); direct-process 는 fixtures/ 키로 직접 _process, notification 만 doc 생성 후 unique uploads/{uid}/{analysisId} 로 controlled COPY (HIGH 1) |
| T-15.01-07 | Tampering | direct-process 가 fixture 를 uploads/ 로 COPY → 그 ObjectCreated 가 S3→SQS→Lambda delegate 를 발화 → 같은 객체가 manual _process + Lambda 양쪽 double-analysis | mitigate | direct-process 는 fixtures/ SOURCE 키를 _process(app.py:1633, key 재파싱 없음)에 그대로 넘겨 직접 분석 → uploads/ COPY ZERO(코드 카운터 assert) → notification 비발화; --trigger 가 mutually-exclusive 단일 경로(나머지 둘 미호출); analyze 모드는 live-notified 버킷 비대상 경고 (HIGH 1/HIGH 2/MEDIUM 4) |
| T-15.01-08 | Tampering | fixed uid/analysisId 재사용 또는 하이픈 슬러그 analysisId 로 mode·rerun 간 doc path 충돌·overwrite 또는 parse_upload_key 거부 | mitigate | per-run/per-mode identity(phase15_mode1_<runId> / phase15_mode3_{motion}_<runId>) + 영숫자 sanitized analysisId; dry-run every-key parse_upload_key 통과 assertion(notification/analyze) + analysisId 영숫자 self-check(전 모드); evidence 에 runId/uid/analysisId/mode/s3Key/createdAt 기록 (HIGH 2) |
| T-15.01-SC | Tampering | npm/pip/cargo installs | accept | Phase 15 는 신규 패키지 0 (RESEARCH Package Legitimacy: skipped) — install 작업 없음 |
</threat_model>

<verification>
- 3 스크립트 모두 --help / --dry-run 가 GPU·Pod 없이 로컬 exit 0
- sweep_phase15.py: SOURCE/identity 분리 + per-run/per-mode 영숫자 analysisId + --trigger mutually-exclusive 단일 경로(direct-process default = fixtures/ 키 직접 _process, uploads/ COPY 0) + 모드별 invariant 카운터(direct-process uploads-copy==0 & _process==1) + 모든 doc createdAt/updatedAt + --pair-sequential + dry-run analysisId 영숫자 self-check(+notification/analyze every-key parse_upload_key 통과) + runId/uid/analysisId/mode/s3Key/createdAt evidence (HIGH 1/HIGH 2/MEDIUM 4)
- assert 스크립트에 frozen checksum 박힘 + backend/judging_data/ 경로 + fallback==0 + 재calibrate import 0
- dataset 업로드 dry-run 이 13 항목 정규화 매핑 출력 + 모든 SOURCE 키 fixtures/phase15/ 스킴(uploads/ 미사용, 트리거 비발화) + phase15_keys.json schema(sourceS3Key/motionId/mode/label/referenceMotionId)
- 회귀: 기존 backend pytest 영향 없음(신규 스크립트는 import-only) — PYTHONPATH=backend/shared/python python3 -m pytest backend/tests -q
</verification>

<success_criteria>
- sweep_phase15.py: SOURCE fixtures 입력 → per-run/per-mode 영숫자 identity → --trigger mutually-exclusive 단일 경로(direct-process default = fixtures/ 키 직접 _process, uploads/ COPY 0) → _process, 모든 doc createdAt/updatedAt, --pair-sequential, dry-run analysisId 영숫자 self-check(+notification/analyze every-key parse_upload_key 통과), runId/uid/analysisId/mode/s3Key/createdAt evidence, dry-run 통과 (MODE-01 도구, HIGH 1/HIGH 2/MEDIUM 4)
- assert_falsepositive_gate.py: backend/judging_data/tilt_thresholds.yaml frozen checksum hard-gate + fallback==0 + success/fail assert, 재calibrate 0 (SCORE-04 도구, MEDIUM 5/6)
- upload_phase15_dataset.py: 13 영상 비-notified fixtures/phase15/{motion}/correct|fault.mp4 SOURCE 키로 정규화 업로드(uploads/ 트리거 비발화) + phase15_keys.json(sourceS3Key=fixtures/ 키) 산출 (R1, HIGH 1, LOW 7)
- reference 필드 확인은 Plan 03 에서 seed-reference-downstream.mjs --verify 로 수행
</success_criteria>

<output>
Create `.planning/phases/15-mode-1-mode-3-testflight/15-01-SUMMARY.md` when done
</output>
