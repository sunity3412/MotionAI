---
phase: 15-mode-1-mode-3-testflight
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/15-mode-1-mode-3-testflight/15-POD-BRINGUP-EVIDENCE.md
autonomous: false
requirements: [MODE-01, MODE-02, SCORE-04]
user_setup:
  - service: runpod
    why: "GPU Pod 생성 (CUDA 필수, NLF/RTMW CPU NaN). belle 계정 sunity3412."
    dashboard_config:
      - task: "신규 GPU Pod 생성 (EU-RO-1 GPU priority 4090→PRO4500→5090→PRO6000→A4500) + Network Storage 사용"
        location: "RunPod Console (sunity3412 account)"

must_haves:
  truths:
    - "신규 RunPod Pod 가 /health 200 + pipeline_loaded:true + auth_configured:true 를 반환한다"
    - "Pod env 에 RTMW/YOLOX weights + RECOGNIZER_BACKEND=gemini + GEMINI_API_KEY + RUNPOD_AUTH_TOKEN + GEMINI_COACH_ENABLED=1 + Cerebras key 가 복원되어 있다"
    - "SSM 파라미터 /sunity/motion/runpod-analyze-url 이 신규 Pod /analyze URL 로 갱신되고(source of truth), live Lambda RUNPOD_ANALYZE_URL 이 동일 값으로 일치한다 — 다음 sam deploy 가 SSM resolve 로 되돌려도 동일 값이라 무회귀 (template.yaml:269)"
    - "실 LLM 1회 호출 path 가 작동한다 — recognizer Gemini + 듀얼 coach Gemini/Cerebras 가 silent fallback 이 아님"
    - "Gemini·Cerebras 키 둘 다 무효일 경우 silent 진행 없이 belle 에게 blocking escalation 으로 차단된다 (D-03/D-12 검증 불가)"
  artifacts:
    - path: ".planning/phases/15-mode-1-mode-3-testflight/15-POD-BRINGUP-EVIDENCE.md"
      provides: "신규 Pod id/proxy URL, /health 출력, SSM param == live Lambda env 동기화 확인, env 복원 체크리스트, LLM 키 liveness 결과(양쪽 실패 시 escalation)"
  key_links:
    - from: "backend/functions/pipeline/app.py"
      to: "신규 RunPod Pod /analyze"
      via: "SSM /sunity/motion/runpod-analyze-url → Lambda env RUNPOD_ANALYZE_URL (sunity-motion creds)"
      pattern: "RUNPOD_ANALYZE_URL"
---

<objective>
실 E2E(Wave 2~3)를 막는 단일 최상위 운영 블로커를 해소한다. RESEARCH 검증: 기존 Pod xbdkj1g2ylnfwi 는 죽었다(/health 404). 신규 GPU Pod 생성 → setup_pod_full.sh 부트스트랩 → env 복원 → uvicorn 기동 → /health 200 확인 → SSM source-of-truth(/sunity/motion/runpod-analyze-url) 와 live Lambda RUNPOD_ANALYZE_URL 을 둘 다 sunity-motion creds 로 신규 Pod /analyze 로 동기화한다. 동시에 실 LLM(recognizer Gemini + 듀얼 coach)이 silent fallback 이 아니라 실제로 발화함을 확인한다(CONTEXT D-03).

Purpose: 이 plan 이 GREEN 이 되기 전에는 어떤 실 E2E sweep 도 무의미하다(404 delegate). MODE-01/MODE-02/SCORE-04 의 공통 전제.
Output: 신규 Pod + SSM·live Lambda env 동기화 + bring-up evidence 문서.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-mode-1-mode-3-testflight/15-CONTEXT.md
@.planning/phases/15-mode-1-mode-3-testflight/15-RESEARCH.md
@.claude/scripts/setup_pod_full.sh
@backend/runpod_inference/server.py
@backend/template.yaml
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Checkpoint: belle 신규 RunPod GPU Pod 생성 (콘솔 액션)</name>
  <action>belle 가 RunPod Console(sunity3412)에서 신규 GPU Pod 를 생성하고 SSH·proxy 정보를 Claude 에게 전달한다. Claude 는 이후 SSH·env·uvicorn·SSM·Lambda 동기화를 전부 자동 실행하지만 Pod 인스턴스 생성 자체는 belle 콘솔 액션이라 여기서 일시 정지한다.</action>
  <what-built>신규 RunPod GPU Pod 생성은 belle 계정(sunity3412) 콘솔 액션이 필요하다 — Claude 가 SSH·env·uvicorn·SSM·Lambda 동기화는 전부 자동 실행하지만, Pod 인스턴스 생성 자체는 belle 콘솔에서 이뤄진다.</what-built>
  <read_first>
    .planning STATE.md (Pod 인프라 결정 2026-06-11 — Network Volume EU-RO-1); memory runpod-eu-ro1-gpu-priority (GPU 우선순위 4090→PRO4500→5090→PRO6000→A4500); memory next-pod-use-network-storage (신규 Pod 는 Network Storage 사용)
  </read_first>
  <how-to-verify>
    1. RunPod Console (sunity3412 계정) 에서 신규 GPU Pod 생성 — GPU 우선순위 4090 → PRO 4500 → 5090 → PRO 6000 → A4500 (EU-RO-1).
    2. Network Storage(sunity-motion-vol) 사용 — community cloud 호스트가 mount 미지원이면 ephemeral 진행하되 Claude 에게 알릴 것.
    3. SSH 접속 정보(host/port/key)와 proxy 도메인(`https://<podid>-8000.proxy.runpod.net`)을 Claude 에게 전달.
  </how-to-verify>
  <resume-signal>Pod id + SSH 정보 + proxy 도메인을 전달하면 Claude 가 부트스트랩부터 자동 진행한다.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Pod 부트스트랩 + env 복원 + uvicorn 기동 + /health 200 확인</name>
  <read_first>
    .claude/scripts/setup_pod_full.sh (RTMW/YOLOX HF mirror, Firebase SA, RUNPOD_AUTH_TOKEN auto-fetch, uvicorn __pycache__ 청소 — STATE 함정 20-27 자동 회피); backend/runpod_inference/server.py (/health, /analyze, X-RunPod-Token auth, server.py:99 token 미설정 시 503, pipeline_loaded/auth_configured 플래그); RESEARCH §Runtime State Inventory (env 목록 RTMW_ONNX_PATH/YOLOX_ONNX_PATH/RECOGNIZER_BACKEND=gemini/GEMINI_API_KEY/RUNPOD_AUTH_TOKEN/GEMINI_COACH_ENABLED); memory runpod-gpu-env (Pod 함정 박제)
  </read_first>
  <action>
    Claude 가 belle 전달 SSH 로 접속해 자동 실행한다 (pod-ops-claude-runs). (1) 로컬 commit 후 git push origin main → Pod git pull (memory gsd-pod-work-push-first). (2) setup_pod_full.sh 실행 — RTMW/YOLOX HF mirror, Firebase SA SSM, RUNPOD_AUTH_TOKEN auto-fetch. (3) env 복원: RTMW_ONNX_PATH / YOLOX_ONNX_PATH / RECOGNIZER_BACKEND=gemini / GEMINI_API_KEY(SSM /sunity/motion/gemini-api-key) / RUNPOD_AUTH_TOKEN / GEMINI_COACH_ENABLED=1 / Cerebras key. 키는 Pod env 에만 주입하고 코드·로그·commit 에 절대 하드코딩하지 않는다(RESEARCH Security V6). (4) uvicorn runpod_inference.server:app --host 0.0.0.0 --port 8000 --workers 1 기동. (5) curl proxy /health → 200 + pipeline_loaded:true + auth_configured:true 확인. RECOGNIZER_BACKEND / GEMINI_* default 를 source 코드에 추가하지 않는다(RESEARCH Anti-Pattern; app.py:198 Pod-env-driven).
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" "$RUNPOD_PROXY_URL/health"</automated>
  </verify>
  <acceptance_criteria>
    /health HTTP 200; 응답 body 에 pipeline_loaded:true + auth_configured:true; RTMW/YOLOX weights Pod 에 존재; RECOGNIZER_BACKEND=gemini + GEMINI_COACH_ENABLED=1 Pod env 에 설정; 어떤 키도 commit/로그에 하드코딩 0
  </acceptance_criteria>
  <done>신규 Pod 가 /health 200 + pipeline_loaded:true + auth_configured:true 를 반환하고 env 가 전부 복원되었다.</done>
</task>

<task type="auto">
  <name>Task 2: SSM source-of-truth + Lambda RUNPOD_ANALYZE_URL 동기화 (sunity-motion creds) + 실 LLM 발화 1회 확인 + 양쪽 키 실패 escalation</name>
  <read_first>
    backend/template.yaml:269 (RUNPOD_ANALYZE_URL: "{{resolve:ssm:/sunity/motion/runpod-analyze-url}}" — Lambda env 의 source of truth 는 SSM param 이고, live update-function-configuration 만 하면 다음 sam deploy 가 SSM resolve 로 되돌림 → SSM 을 반드시 같이 갱신); RESEARCH §Common Pitfalls Pitfall 2 (active sunity-api cred = lambda:ListFunctions AccessDenied; sunity-motion creds 필요); RESEARCH Code Examples Lambda env sync; backend/functions/pipeline/app.py:1989 (coach dual-track 섹션 조립 로그 — gemini_ok / cerebras_ok); CONTEXT D-03 (실 LLM 최소 1회 발화 필수) / D-12 (한쪽 drop → cross-fill = PASS, 양쪽 실패는 미정의 — 본 plan 에서 escalation 으로 닫음); memory aws-keys-and-bucket
  </read_first>
  <action>
    (1) AWS 자격증명을 sunity-motion 으로 전환(active sunity-api 는 Lambda AccessDenied — RESEARCH 검증). (2) R2 — SSM source-of-truth 갱신: aws ssm put-parameter --name /sunity/motion/runpod-analyze-url --value (신규 Pod proxy `https://<newpod>-8000.proxy.runpod.net/analyze`) --type String --overwrite (sunity-motion creds). template.yaml:269 가 이 SSM param 을 resolve 하므로 이것이 source of truth 다 — 이걸 빠뜨리고 live env 만 바꾸면 다음 sam deploy 가 옛 SSM 값으로 되돌려 회귀한다. (3) live 적용: SSM 갱신 후 즉시 효력을 위해 aws lambda update-function-configuration --region ap-northeast-2 로 pipeline 함수의 RUNPOD_ANALYZE_URL 을 동일 신규 Pod /analyze 로 갱신(기존 env 키들 보존). SSM 이 이미 같은 값이라 다음 sam deploy 는 회귀하지 않는다(원하면 sam deploy 로 갱신해도 동일 결과). URL 은 신규 Pod 생성 시점에 belle 가 전달한 proxy 도메인에서 runtime 으로 채우고 코드/문서에 하드코딩하지 않는다. (4) 실 LLM 발화 확인 — Pod /analyze 에 dataset 영상 1개(예: phase15-climb/correct)로 실 E2E 1회 호출 후 Pod 로그에서 "coach dual-track 섹션 조립" 라인을 grep 해 gemini_ok=True 또는 cross_filled 로 빈 섹션 0 임을 확인(RESEARCH Pitfall 6: heuristic-only 가 아닌 실 Gemini 발화). recognizer 가 line dimension 을 non-None 으로 채우는지도 확인(RESEARCH Open Question 3). 결과를 15-POD-BRINGUP-EVIDENCE.md 에 기록(키 노출 0). belle 가 Lambda 직접 권한이 없으면 이 단계는 Claude 가 sunity-motion 키로 수행. (5) 양쪽 키 실패 escalation 분기(W-3): Gemini 와 Cerebras 키 liveness 를 이 1회 호출로 판정해, 둘 중 하나라도 ok 면 정상 진행한다(D-12 한쪽 drop 은 cross-fill 로 PASS). 그러나 두 키 모두 검증 실패(gemini_ok=False AND cerebras_ok=False, 예: SSM 키 미펀딩/만료/401)면 silent 진행도 중단된 stall 도 하지 말고 즉시 STOP-and-ask 로 belle 에게 blocking escalation 한다 — 어느 키가 어떤 사유로 실패했는지 + dual-coach 실 LLM E2E(D-03/D-12)는 작동 키가 공급되기 전까지 검증 불가임을 명시하고, 작동 키 refresh 를 요청한다. 이 escalation 은 자동 우회 불가(belle 응답 전까지 Wave 2 로 진행 금지). 실패 사유·요청 키를 15-POD-BRINGUP-EVIDENCE.md §LLM-key-liveness 에 키 값 노출 없이 기록한다.
  </action>
  <verify>
    <automated>AWS_PROFILE=sunity-motion aws ssm get-parameter --region ap-northeast-2 --name /sunity/motion/runpod-analyze-url --query "Parameter.Value" --output text && AWS_PROFILE=sunity-motion aws lambda get-function-configuration --region ap-northeast-2 --function-name "$PIPELINE_FN" --query "Environment.Variables.RUNPOD_ANALYZE_URL" --output text</automated>
  </verify>
  <acceptance_criteria>
    SSM /sunity/motion/runpod-analyze-url 값 == live Lambda RUNPOD_ANALYZE_URL 값 == 신규 Pod proxy /analyze (둘 다 일치 — template.yaml:269 source-of-truth 이므로 SSM 갱신 누락 시 다음 sam deploy 회귀, R2); sunity-motion creds 로 갱신(sunity-api 아님); 실 E2E 1회에서 Pod 로그 "coach dual-track 섹션 조립" 에 gemini_ok=True 또는 cross-fill 로 빈 섹션 0; line dimension non-None on ≥1 video; gemini_ok 와 cerebras_ok 가 모두 False 인 경우 task 를 PASS 처리하지 않고 belle blocking escalation(실패 키 + 사유 + D-03/D-12 검증 불가 명시 + 키 refresh 요청)을 발동했음이 15-POD-BRINGUP-EVIDENCE.md §LLM-key-liveness 에 기록됨; 15-POD-BRINGUP-EVIDENCE.md 에 키 노출 없이 기록
  </acceptance_criteria>
  <done>SSM source-of-truth 와 live Lambda 가 둘 다 신규 Pod 를 가리키고(다음 sam deploy 무회귀), 실 LLM(recognizer + 듀얼 coach)이 silent fallback 이 아니라 실제 발화함을 1회 E2E 로 확인했다 — 단 양쪽 키가 모두 실패하면 PASS 대신 belle 에게 blocking escalation 으로 차단한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lambda → Pod proxy | RUNPOD_ANALYZE_URL delegate; X-RunPod-Token 인증 |
| SSM → Pod env | Gemini/Cerebras/AWS/Firebase 키 주입 (run-time only) |
| SSM → Lambda env | RUNPOD_ANALYZE_URL source of truth (template.yaml:269 resolve); AWS CLI 갱신 (sunity-motion creds 한정) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.02-01 | Information Disclosure | Pod proxy 미인증 노출 | mitigate | RUNPOD_AUTH_TOKEN 설정; server.py:99 토큰 미설정 시 503; auth_configured:true /health 확인 |
| T-15.02-02 | Information Disclosure | 키가 commit/로그/evidence 로 누출 | mitigate | 키는 Pod env·SSM 만; bring-up evidence 에 키 출력 금지(RESEARCH V6) |
| T-15.02-03 | Elevation of Privilege | 잘못된 AWS cred(sunity-api)로 Lambda/SSM 변경 시도 | mitigate | sunity-motion 전환 강제; sunity-api 는 Lambda AccessDenied (검증됨) |
| T-15.02-04 | Tampering | 신규 Pod env 누락으로 분석이 CPU NaN/heuristic 로 silent degrade | mitigate | /health pipeline_loaded:true + 실 LLM 발화 1회 assert; 양쪽 키 실패 시 belle blocking escalation(silent degrade 차단, W-3) |
| T-15.02-05 | Tampering | live Lambda env 만 갱신하고 SSM 누락 → 다음 sam deploy 가 옛 URL 로 회귀 → delegate 404 | mitigate | SSM /sunity/motion/runpod-analyze-url 을 source-of-truth 로 먼저 갱신 후 live 동기화; verify 가 SSM==live 일치 확인 (R2, template.yaml:269) |
| T-15.02-SC | Tampering | npm/pip/cargo installs | accept | setup_pod_full.sh 의 pip 는 locked stack 재설치만; 신규 패키지 0 (RESEARCH Package Legitimacy: skipped) |
</threat_model>

<verification>
- curl proxy /health == 200 + pipeline_loaded:true + auth_configured:true
- SSM /sunity/motion/runpod-analyze-url == live Lambda RUNPOD_ANALYZE_URL == 신규 Pod /analyze (sunity-motion get-parameter + get-function-configuration 둘 다 확인, R2)
- 실 E2E 1회 Pod 로그에 gemini_ok=True 또는 cross-fill 빈 섹션 0
- 양쪽 키 실패 시 belle blocking escalation 발동 + §LLM-key-liveness 기록 (silent 진행/stall 금지, W-3)
- 15-POD-BRINGUP-EVIDENCE.md 존재 + 키 노출 0
</verification>

<success_criteria>
- 신규 Pod /health 200 (pipeline_loaded + auth_configured)
- env 전부 복원 (RTMW/YOLOX/Gemini/Cerebras/RUNPOD_AUTH_TOKEN/GEMINI_COACH_ENABLED)
- SSM source-of-truth + live Lambda RUNPOD_ANALYZE_URL 동기화 일치 (sunity-motion, sam deploy 무회귀 — R2)
- 실 LLM 발화 확인 (D-03) — recognizer line non-None + 듀얼 coach 빈 섹션 0
- 양쪽 키 실패 시 belle blocking escalation (W-3 — silent 진행 금지, D-03/D-12 검증 불가 명시)
</success_criteria>

<output>
Create `.planning/phases/15-mode-1-mode-3-testflight/15-02-SUMMARY.md` when done
</output>
</content>
