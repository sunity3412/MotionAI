---
phase: 15-mode-1-mode-3-testflight
plan: 03
type: execute
wave: 2
depends_on: ["15-01", "15-02"]
files_modified:
  - .planning/phases/15-mode-1-mode-3-testflight/15-MODE1-FALSEPOSITIVE-EVIDENCE.md
autonomous: true
requirements: [MODE-01, SCORE-04]
user_setup: []

must_haves:
  truths:
    - "11 reference 문서가 Mode-1 downstream 필수 4필드(meanAngles, techniqueProfile, bodyNormalizationProfile, forceDirectionPattern) + captureViews 를 전부 보유한다 (seed-reference-downstream.mjs --verify completeRequiredSet == 11/11) — 11개 전부 FIELD-VERIFIED"
    - "정은지 student 영상이 있는 7 motion(climb, elbow-twist-sister, kip-up, pdshape, peter-pan, power-spin, combo)에 대해 Mode 1 실 E2E 가 각자의 matching reference(referenceMotionId)로 전문가 기준 점수를 산출한다 — student 영상 없는 4 reference(ref-foxtop, ref-foxtop-split, ref-invert, ref-sideway-spin)는 live 비교 대상 아님(coverage 한계로 명시 기록)"
    - "Mode 1(MODE_EXPERT) 7 motion sweep 이 크래시 없이 완주한다 — Mode 1 7/7 server_error == 0 (15-03 단독 gate). 13-영상 통합 SC4 집계는 15-05 가 소유한다(15-03/15-04 evidence 합산)"
    - "정은지 success 영상이 Mode 1(MODE_EXPERT) 전문가 비교에서 어떤 점수를 받는지 OBSERVATIONAL 로 기록된다 — overallScore + severity + referenceMotionId match + server_error==0 을 가시성 목적으로 표에 박되, 객관적 numeric floor 가 아직 없으므로 blocking PASS/FAIL 게이트가 아니다 (MEDIUM 6). 블로킹 SCORE-04 게이트는 15-04 의 frozen MODE_SELF baseline 이 단독 소유한다"
  artifacts:
    - path: ".planning/phases/15-mode-1-mode-3-testflight/15-MODE1-FALSEPOSITIVE-EVIDENCE.md"
      provides: "11-ref 필드 검증 결과(--verify 11/11) + 7-motion Mode 1 점수 표 + (4 ref no-student-video coverage 한계 명시) + Mode 1 success 영상 OBSERVATIONAL 행(overallScore/severity/referenceMotionId match/server_error) + Mode 1 7/7 server_error==0 gate"
  key_links:
    - from: "backend/scripts/sweep_phase15.py"
      to: "Firestore users/{uid}/analyses (MODE_EXPERT)"
      via: "MODE_EXPERT(Mode 1, 7 motion) 실 E2E evidence write — per-run uid (Plan 01 HIGH 2)"
      pattern: "referenceMotionId"
    - from: "app/scripts/seed-reference-downstream.mjs --verify"
      to: "Firestore reference/{motionId} downstream 4필드 + captureViews"
      via: "read-back completeRequiredSet 11/11 게이트 (11개 전부 FIELD-VERIFIED)"
      pattern: "completeRequiredSet"
---

<objective>
MODE-01(Mode 1 실영상 E2E)를 검증하고, 동시에 Mode 1(MODE_EXPERT) 전문가 비교에서 정은지 success 영상이 어떤 점수를 받는지 OBSERVATIONAL 로 기록한다(MEDIUM 6 — blocking 게이트 아님). 먼저 11 reference 의 Mode-1 downstream 필수 필드를 seed-reference-downstream.mjs --verify 로 확인하고(A1 open question, 11개 전부 FIELD-VERIFIED), 누락 시 두 단계 repair 후, 정은지 student 영상이 있는 7 motion(climb, elbow-twist-sister, kip-up, pdshape, peter-pan, power-spin, combo)을 각자 matching reference 대상 student 로 Mode 1 실 E2E 돌린다. Mode 1 7 motion sweep 이 크래시 없이 완주(7/7 server_error==0)함을 15-03 단독 gate 로 보고한다.

HIGH 3 ownership 분리: (a) MODE_SELF(reference-independent) axis severity 위양성 블로킹 게이트(SCORE-04)는 15-04 가 frozen MODE_SELF baseline 으로 단독 소유한다 — 15-03 은 15-04 의 MODE_SELF doc 을 절대 재사용·참조하지 않으며(같은 wave 라 race), 오직 Mode 1(MODE_EXPERT) 비교 evidence + 11-ref 필드검증 게이트 + Mode 1 7/7 server_error==0 만 소유한다. (b) 13-영상 통합 SC4 집계(Mode1 7 + Mode3 6, total/completed/no_human/not_pole_motion/server_error)는 15-05 가 소유한다 — 15-05 는 depends_on:["15-03","15-04"] 이라 두 evidence set 이 모두 존재하므로 거기서 합산한다. 15-03 은 13/13 통합 행을 더 이상 보유하지 않는다(이전 round 의 same-wave cross-reference race 제거).

R3 coverage 정직성: Mode 1 live E2E 는 정은지 student 영상이 존재하는 motion 에 대해서만 수행한다. 11 reference 는 전부 FIELD-VERIFIED(downstream 4필드 11/11)지만, student 영상이 없는 4 reference(ref-foxtop, ref-foxtop-split, ref-invert, ref-sideway-spin)는 이번 phase 에서 live 비교 대상이 아니다 — 이를 숨기지 않고 coverage 한계로 evidence 에 명시 기록한다.

Purpose: 전문가 기준 점수(MODE-01)가 실 Pod path(RTMW + Gemini 인식 + 듀얼 coach)에서 성립함을 evidence-doc 으로 증명하고(CONTEXT D-01/D-04), Mode 1 success 영상 점수를 관측 가능하게 기록한다(MEDIUM 6 observational — blocking 게이트는 15-04). Mode 1 7 motion sweep 의 크래시-프리 완주(7/7 server_error==0)를 단독 gate 로 박는다.
Output: 11-ref 필드 검증(--verify 11/11) + 7-motion Mode 1 점수(+ 4-ref no-student coverage 한계) + Mode 1 success OBSERVATIONAL 행 + Mode 1 7/7 server_error==0 gate evidence 문서.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-mode-1-mode-3-testflight/15-CONTEXT.md
@.planning/phases/15-mode-1-mode-3-testflight/15-RESEARCH.md
@.planning/phases/15-mode-1-mode-3-testflight/15-VALIDATION.md
@.planning/phases/15-mode-1-mode-3-testflight/15-01-SUMMARY.md
@.planning/phases/15-mode-1-mode-3-testflight/15-02-SUMMARY.md
@.planning/phases/08.1-axis-metric-redesign/08.1-SWEEP-EVIDENCE.md
@backend/scripts/backfill_reference_downstream.py
@app/scripts/seed-reference-downstream.mjs
@backend/shared/python/sunity_shared/models.py
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 11 reference Mode-1 downstream 필드 검증 (A1, seed-reference-downstream.mjs --verify) + 누락 시 2-step repair</name>
  <read_first>
    app/scripts/seed-reference-downstream.mjs (REAL Firestore downstream merge+verify 도구 — --verify 가 11 reference 의 downstream 4필드 meanAngles/techniqueProfile/bodyNormalizationProfile/forceDirectionPattern + captureViews 존재/완전성 read-back; --repair-missing --motions <ids> ADD-only merge; completeRequiredSet = N/11 출력; R5 all-or-nothing/부분 repair 규칙); backend/scripts/backfill_reference_downstream.py (fixture GENERATOR — docstring :44 NEVER writes Firestore; --check-firestore 는 activeVersion/angles/anglesJointKeys/anglesFrames stored-angle sanity 만 확인; --output 로 reference-downstream-backfill.json fixture 생성; ALL_MOTION_IDS 11-id, REFERENCE_V1_FORCE_CONFIG pinned — memory reference-v1-pinned-force-config); reference-downstream-backfill.json (untracked seedPayload, 적용 여부 미확인); memory firebase-project-account (owner sunity3412)
  </read_first>
  <action>
    두 도구의 역할을 분리해 검증한다 (HIGH 2 — 리뷰 정정: backfill_reference_downstream.py 는 fixture GENERATOR 라 Firestore 를 쓰지 않으며 --check-firestore 는 stored-angle sanity 만 본다. downstream 4필드 검증은 app/scripts/seed-reference-downstream.mjs --verify 가 REAL 도구다).
    (1) stored-angle sanity gate: backend/scripts/backfill_reference_downstream.py --check-firestore 를 11-id 전체로 실행해 activeVersion/angles/anglesJointKeys/anglesFrames 가 정상임만 확인한다(downstream 필드 검증으로 오인 금지 — 이 모드는 4필드를 보지 않는다).
    (2) downstream 4필드 검증(진짜 게이트): cd app && node scripts/seed-reference-downstream.mjs --input ../reference-downstream-backfill.json --verify 를 실행해 11 reference 가 downstream 4필드(meanAngles, techniqueProfile, bodyNormalizationProfile, forceDirectionPattern) + captureViews 를 전부 보유하는지 확인한다. completeRequiredSet 가 11/11 이어야 PASS — 11개 전부 FIELD-VERIFIED(round-1 HIGH 2 게이트). Firebase Admin SA(firebase-sa.json, owner sunity3412 — memory firebase-project-account)로 접근.
    (3) 누락 분기(2-step repair): --verify 가 11/11 미만이면 — (a) 필요 시 fixture 재생성 PYTHONPATH=backend/shared/python python3 backend/scripts/backfill_reference_downstream.py --bucket <video-bucket> --output reference-downstream-backfill.json (REFERENCE_V1_FORCE_CONFIG pinned 하에 생성 — "student path exact" 아님, memory reference-v1-pinned-force-config), (b) 적용 cd app && node scripts/seed-reference-downstream.mjs --input ../reference-downstream-backfill.json --repair-missing --motions <missing ids> (ADD-only merge, 부분 repair 는 --motions 명시 필수 — R5), (c) --verify 재실행해 completeRequiredSet 11/11 도달 확인.
    결과를 15-MODE1-FALSEPOSITIVE-EVIDENCE.md §필드검증 표에 per-motion 기록(meanAngles/techniqueProfile/bodyNormalizationProfile/forceDirectionPattern/captureViews complete=true/false + completeRequiredSet). 이 11/11 게이트는 Mode-1 live E2E(Task 2) 이전에 반드시 PASS. (11개 전부 필드 검증되지만 그 중 student 영상이 있는 7개만 Task 2 에서 live 비교됨 — R3 coverage 명시.)
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && node scripts/seed-reference-downstream.mjs --input ../reference-downstream-backfill.json --verify</automated>
  </verify>
  <acceptance_criteria>
    seed-reference-downstream.mjs --verify 가 11 motion 전부 보고하고 completeRequiredSet == 11/11(11개 전부 FIELD-VERIFIED); 11 문서 모두 4필드(meanAngles/techniqueProfile/bodyNormalizationProfile/forceDirectionPattern) + captureViews complete=true (또는 누락분 --repair-missing --motions 후 재verify 11/11); backfill_reference_downstream.py --check-firestore 는 stored-angle sanity 게이트로만 사용(4필드 검증으로 쓰지 않음); evidence 문서에 per-motion 필드 표 + completeRequiredSet 기록; 11/11 PASS 가 Task 2 live E2E 선행
  </acceptance_criteria>
  <done>seed-reference-downstream.mjs --verify 로 11 reference 가 Mode-1 downstream 4필드 + captureViews 를 전부 보유함(completeRequiredSet 11/11, 11개 전부 FIELD-VERIFIED)을 확인(누락 시 2-step repair 완료)했다.</done>
</task>

<task type="auto">
  <name>Task 2: 정은지 student 영상 7 motion Mode 1 실 E2E (MODE_EXPERT) + 전문가 점수 evidence + success OBSERVATIONAL + Mode 1 7/7 server_error==0 + 4-ref coverage 한계 기록</name>
  <read_first>
    backend/scripts/sweep_phase15.py (Plan 01 산출 — --keys-file/MODE_EXPERT/referenceMotionId, per-run/per-mode 영숫자 identity uid=phase15_mode1_<runId>, 모든 doc createdAt/updatedAt 설정, SOURCE fixtures→uploads --trigger 단일 경로 direct-process default); backend/scripts/phase15_keys.json (Plan 01 Task 3 산출 — sourceS3Key=fixtures/phase15/{motion}/correct.mp4 success 매핑 + referenceMotionId); backend/shared/python/sunity_shared/models.py (MODE_EXPERT="mode1" 정은지 비교 / MODE_SELF="mode3" 절대 자기성장); .planning/phases/08.1-axis-metric-redesign/08.1-SWEEP-EVIDENCE.md (정은지 25/25 'low' invariant — 고수 success 영상 baseline 참고); RESEARCH Open Question 2 (Mode 1 = success video as student vs matching registered reference); CONTEXT D-04 (Mode 1 = 11 reference 기준 — 단 student 영상 가용 7 motion 만 live, R3); 15-02-SUMMARY.md (Pod proxy URL alive)
  </read_first>
  <action>
    sweep_phase15.py 를 MODE_EXPERT 로 Pod 위에서 실행(pod-ops-claude-runs, SSH, --trigger direct-process default). R3 — 정은지 student 영상이 존재하는 7 motion(climb, elbow-twist-sister, kip-up, pdshape, peter-pan, power-spin, combo)에 대해서만 success 영상을 student 로, 대응 registered reference 를 referenceMotionId 로 페어링해 Mode 1 실 E2E 를 돌린다(RESEARCH Open Question 2 권고: success=student vs matching reference). student 영상이 없는 4 reference(ref-foxtop, ref-foxtop-split, ref-invert, ref-sideway-spin)는 live 비교 대상이 아니며, "FIELD-VERIFIED 11/11 이지만 student 영상 부재로 이번 phase live 미비교"라고 evidence §Mode1점수 표 하단 coverage 한계 행에 명시 기록한다(숨기지 말고 로그). identity 는 Plan 01 의 per-run uid=phase15_mode1_<runId>(throwaway, 보안 V4 cross-uid 누출 차단) + 영숫자 analysisId 를 사용하고 fixed uid/analysisId 재사용을 하지 않는다(HIGH 2). 산출 analysis doc 의 overallScore + dimensionScores + line dimension non-None + 듀얼 coach 섹션을 Firestore 에서 실제로 읽어 §Mode1점수 표에 per-motion 으로 기록한다(runId/uid/analysisId/referenceMotionId/createdAt 동반 — HIGH 2 stale 거부) — dry-run 출력만으로는 이 task 가 완료되지 않으며, 실 Firestore analysis doc 에서 파생된 PASS 행(motion 별 실제 overallScore + referenceMotionId)이 존재해야 done 이다.
    Mode 1 7/7 server_error==0 (15-03 단독 gate) — Mode 1 MODE_EXPERT 7 motion sweep 이 크래시 없이 완주했고 server_error == 0 임을 §Mode1점수 표에서 assert 한다. 하나라도 unexpected pipeline 실패(안전게이트 외 server_error/크래시)가 있으면 게이트 미충족으로 plan 차단. (13-영상 통합 SC4 집계는 15-05 소관 — 15-03 은 자기 Mode 1 7/7 만 gate 한다, HIGH 3.)
    MEDIUM 6 — success 영상 OBSERVATIONAL: 정은지 success 영상이 Mode 1(MODE_EXPERT) 전문가 비교에서 어떤 점수를 받는지 OBSERVATIONAL 로 기록한다 — overallScore + severity + referenceMotionId match + server_error==0 을 §Mode1점수 표에 가시성 목적으로 박는다. 단 이는 blocking PASS/FAIL 게이트가 아니다: 객관적 numeric floor(최소 점수/severity 임계)가 아직 정의되지 않았고(calibration-source-hard-gate — 임의 floor 신규 fit 금지) "high"/"부당" 같은 주관 판정으로 PASS/FAIL 하지 않는다. 블로킹 SCORE-04 게이트는 15-04 의 frozen MODE_SELF baseline 이 단독 소유한다(15-03 은 15-04 의 MODE_SELF doc 을 참조·재사용하지 않는다, HIGH 3). 점수가 "전문가 기준"인지의 baseline 대조 본체는 MODE_SELF 라 15-04 소관이고, 여기서는 Mode 1 raw evidence + success 영상 점수 observational 기록만 수집한다. 실 LLM 발화 확인(gemini_ok / cross-fill 빈 섹션 0)도 함께 기록. 사람 작성 분석 .md 의 점수를 라벨로 쓰지 않는다(D-06).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && PYTHONPATH=backend/shared/python python3 backend/scripts/sweep_phase15.py --keys-file backend/scripts/phase15_keys.json --dry-run</automated>
  </verify>
  <acceptance_criteria>
    Mode 1 실 E2E 가 정은지 student 영상 가용 7 motion(climb/elbow-twist-sister/kip-up/pdshape/peter-pan/power-spin/combo) × matching reference 페어로 완주; Mode 1 7/7 server_error == 0 (15-03 단독 gate — unexpected pipeline 실패 0); 각 doc 에 referenceMotionId + overallScore + dimensionScores 기록; line dimension non-None on ≥1 video(RESEARCH Open Q3); 듀얼 coach 빈 섹션 0; per-run uid=phase15_mode1_<runId> + 영숫자 analysisId 사용(fixed id 재사용 0, HIGH 2); §Mode1점수 표가 실 Firestore analysis doc 에서 파생된 7-motion 행(motion 별 실제 overallScore + referenceMotionId + runId/uid/analysisId/createdAt); success 영상 OBSERVATIONAL 행(overallScore + severity + referenceMotionId match + server_error) 기록 — blocking PASS/FAIL 게이트 아님, 임의 numeric floor 도입 0(MEDIUM 6; 블로킹 SCORE-04 는 15-04 소유); student 영상 없는 4 reference(ref-foxtop/ref-foxtop-split/ref-invert/ref-sideway-spin)는 coverage 한계 행으로 명시("FIELD-VERIFIED 11/11, live 미비교") — 11 live E2E 로 과대 주장하지 않음(R3); dry-run 출력만으로는 이 task 가 완료되지 않음(dry-run 은 로컬 도구 무결성 pre-check 일 뿐, 실행 evidence 는 실 Firestore doc 에서 채움)
  </acceptance_criteria>
  <done>정은지 student 영상이 있는 7 motion 의 Mode 1 실 E2E 가 matching reference 로 완주(7/7 server_error==0)하고 실 Firestore doc 파생 점수/섹션 evidence 가 §Mode1점수 표에 문서화되었으며(success 점수는 OBSERVATIONAL 기록 — blocking 게이트 아님, MEDIUM 6), student 영상 없는 4 reference 는 coverage 한계로 명시 기록되었다(11 live 주장 안 함, dry-run 단독 불충분).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sweep script → Firestore | Admin SA 로 per-run uid(phase15_mode1_<runId>) 아래 MODE_EXPERT evidence write/read |
| seed-reference-downstream.mjs → Firestore | Admin SA 로 reference/{motionId} downstream 4필드 read(--verify) / ADD-only merge(--repair-missing) |
| 사람 분석 .md → 검증 입력 | success/fail 분류 라벨만 (점수 라벨 금지) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.03-01 | Tampering | Mode 1 success 영상 점수를 주관 기준("high"/"부당")으로 PASS/FAIL 판정 | mitigate | success 영상은 OBSERVATIONAL 기록만(overallScore/severity/referenceMotionId/server_error), blocking 게이트 아님, 임의 numeric floor 도입 0(MEDIUM 6); 블로킹 SCORE-04 severity 게이트는 15-04 가 08.1 frozen baseline 으로 단독 소유 |
| T-15.03-02 | Spoofing | objectivity 위반 — 사람 점수 라벨을 ground truth 로 사용 | mitigate | 입력은 분류 라벨만, 점수 라벨 금지 (D-06) |
| T-15.03-03 | Information Disclosure | sweep evidence 가 다른 uid 데이터 누출 | mitigate | per-run throwaway uid=phase15_mode1_<runId> scoped (ASVS V4); cross-uid read 금지 |
| T-15.03-04 | Tampering | reference downstream 필드 누락으로 Mode 1 이 silent fallback | mitigate | Task 1 seed-reference-downstream.mjs --verify completeRequiredSet 11/11 선행 게이트(누락 시 2-step repair) |
| T-15.03-05 | Spoofing | Mode 1 coverage 과대 주장 (11 live E2E 라고 보고하나 실제 7 motion 만 student 영상 보유) | mitigate | R3 — 7 motion 만 live 비교, 4 ref 는 coverage 한계로 명시 기록(숨기지 않음); evidence §Mode1점수 표에 limitation 행 |
| T-15.03-06 | Tampering | 15-03 이 15-04 MODE_SELF doc 을 재sweep/재사용하거나 13/13 SC4 통합을 same-wave 로 시도 (HIGH 3) | mitigate | 15-03 은 MODE_SELF doc 을 소유·재sweep 하지 않고 Mode 1 7/7 만 gate; 13-영상 통합 SC4 집계는 15-05(depends_on 15-03+15-04) 소유; MODE_SELF severity 게이트는 15-04 단독 소유 |
| T-15.03-SC | Tampering | npm/pip/cargo installs | accept | 신규 패키지 0 (RESEARCH Package Legitimacy: skipped) |
</threat_model>

<verification>
- seed-reference-downstream.mjs --verify: completeRequiredSet 11/11 (4필드 + captureViews, 11개 전부 FIELD-VERIFIED)
- backfill_reference_downstream.py --check-firestore: stored-angle sanity 게이트로만 (downstream 검증 아님)
- Mode 1 실 E2E (MODE_EXPERT): student 영상 가용 7 motion 7/7 server_error 0(15-03 단독 gate) + line non-None + referenceMotionId lockstep + per-run uid (실 Firestore doc 파생, dry-run 단독 불충분); success 영상 OBSERVATIONAL 점수 기록(blocking 게이트 아님, MEDIUM 6); 4 ref no-student coverage 한계 명시 (R3)
- 13-영상 통합 SC4 집계는 15-05 소관 (15-03 은 Mode 1 7/7 만 gate, HIGH 3)
- 15-MODE1-FALSEPOSITIVE-EVIDENCE.md: 필드검증(11/11) + Mode1점수(7 motion + success OBSERVATIONAL + 4 ref limitation) + Mode 1 7/7 server_error==0 gate 전부 기록
</verification>

<success_criteria>
- MODE-01: student 영상 가용 7 motion 의 Mode 1 실 E2E 가 전문가 기준 점수 산출 (referenceMotionId lockstep, ROADMAP SC1); 11 ref 전부 FIELD-VERIFIED, 4 ref 는 coverage 한계 명시 (R3); Mode 1 7/7 server_error==0
- SCORE-04 (Mode 1 측면): 정은지 success 영상 점수 OBSERVATIONAL 기록(blocking 게이트 아님, 임의 floor 도입 0, MEDIUM 6); 블로킹 MODE_SELF reference-independent 게이트 본체는 15-04 가 단독 소유 (HIGH 3 single-owner)
- 13-영상 통합 SC4 집계는 15-05 가 소유 (15-03/15-04 evidence 합산, HIGH 3)
- 실 LLM path (recognizer line non-None + 듀얼 coach 빈 섹션 0, D-03)
</success_criteria>

<output>
Create `.planning/phases/15-mode-1-mode-3-testflight/15-03-SUMMARY.md` when done
</output>
