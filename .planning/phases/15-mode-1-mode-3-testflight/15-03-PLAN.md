---
phase: 15-mode-1-mode-3-testflight
plan: 03
type: execute
wave: 2
depends_on: ["15-01", "15-02"]
files_modified:
  - .planning/phases/15-mode-1-mode-3-testflight/15-MODE1-FALSEPOSITIVE-EVIDENCE.md
autonomous: true
requirements: [MODE-01, SCORE-04]
user_setup: []

must_haves:
  truths:
    - "11 reference 문서가 Mode-1 downstream 필수 4필드(meanAngles, techniqueProfile, bodyNormalizationProfile, forceDirectionPattern) + captureViews 를 전부 보유한다 (seed-reference-downstream.mjs --verify completeRequiredSet == 11/11)"
    - "정은지 success 영상이 11 reference 대상 Mode 1 실 E2E 에서 전문가 기준 점수(높음)로 산출된다 (referenceMotionId lockstep)"
    - "정은지 success 영상의 axis severity 가 frozen baseline 대비 'low' 이고 41점 같은 위양성이 없다"
    - "정은지 fail 영상에서 분석기가 fault 를 독립적으로 잡고 높은 점수를 주지 않는다"
    - "13 영상 전체 sweep 이 크래시 없이 일관되게 완주한다 — total/완료/no_human/not_pole_motion/server_error 집계로 unexpected pipeline 실패 0 (ROADMAP SC4)"
  artifacts:
    - path: ".planning/phases/15-mode-1-mode-3-testflight/15-MODE1-FALSEPOSITIVE-EVIDENCE.md"
      provides: "11-ref 필드 검증 결과(--verify 11/11) + Mode 1 점수 표 + 위양성 assert PASS/FAIL 표 (08.1 baseline 대조) + 13-영상 크래시/실패 집계 표 (SC4)"
  key_links:
    - from: "backend/scripts/sweep_phase15.py"
      to: "Firestore users/{sweep_uid}/analyses"
      via: "MODE_EXPERT(Mode 1) 실 E2E evidence write"
      pattern: "referenceMotionId"
    - from: "app/scripts/seed-reference-downstream.mjs --verify"
      to: "Firestore reference/{motionId} downstream 4필드 + captureViews"
      via: "read-back completeRequiredSet 11/11 게이트"
      pattern: "completeRequiredSet"
    - from: "backend/scripts/assert_falsepositive_gate.py"
      to: "08.1-SWEEP-EVIDENCE.md frozen baseline"
      via: "severity/score 대조 (재calibrate 0)"
      pattern: "c94bb8"
---

<objective>
MODE-01(Mode 1 실영상 E2E) + SCORE-04(위양성 게이트)를 한 셋의 실 sweep 으로 동시 검증한다. 두 요구사항은 같은 정은지 success/fail 영상 + 같은 sweep run 을 공유한다(RESEARCH downstream_consumer Wave 2). 먼저 11 reference 의 Mode-1 downstream 필수 필드를 seed-reference-downstream.mjs --verify 로 확인하고(A1 open question), 누락 시 두 단계 repair 후, success 영상을 11 reference 대상 student 로 Mode 1 실 E2E 돌리고, 산출 점수/severity 를 FROZEN 08.1 baseline 에 대조해 위양성 없음을 assert 한다. 마지막으로 13 영상 전체 sweep 의 크래시/실패를 집계해 다양한 동작·앵글 세트에서 분석이 크래시 없이 일관되게 완주함을 evidence 로 박는다(ROADMAP SC4).

Purpose: 전문가 기준 점수(MODE-01)와 고수 위양성 부재(SCORE-04)가 실 Pod path(RTMW + Gemini 인식 + 듀얼 coach)에서 성립함을 evidence-doc 으로 증명한다(CONTEXT D-01/D-04). 동시에 13 영상 일관성/크래시-프리(SC4)를 집계로 관측 가능하게 만든다.
Output: 11-ref 필드 검증(--verify 11/11) + Mode 1 점수 + 위양성 assert + 13-영상 크래시/실패 집계 evidence 문서.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-mode-1-mode-3-testflight/15-CONTEXT.md
@.planning/phases/15-mode-1-mode-3-testflight/15-RESEARCH.md
@.planning/phases/15-mode-1-mode-3-testflight/15-VALIDATION.md
@.planning/phases/15-mode-1-mode-3-testflight/15-01-SUMMARY.md
@.planning/phases/15-mode-1-mode-3-testflight/15-02-SUMMARY.md
@.planning/phases/08.1-axis-metric-redesign/08.1-SWEEP-EVIDENCE.md
@backend/scripts/backfill_reference_downstream.py
@app/scripts/seed-reference-downstream.mjs
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 11 reference Mode-1 downstream 필드 검증 (A1, seed-reference-downstream.mjs --verify) + 누락 시 2-step repair</name>
  <read_first>
    app/scripts/seed-reference-downstream.mjs (REAL Firestore downstream merge+verify 도구 — --verify 가 11 reference 의 downstream 4필드 meanAngles/techniqueProfile/bodyNormalizationProfile/forceDirectionPattern + captureViews 존재/완전성 read-back; --repair-missing --motions <ids> ADD-only merge; completeRequiredSet = N/11 출력; R5 all-or-nothing/부분 repair 규칙); backend/scripts/backfill_reference_downstream.py (fixture GENERATOR — docstring :44 NEVER writes Firestore; --check-firestore 는 activeVersion/angles/anglesJointKeys/anglesFrames stored-angle sanity 만 확인; --output 로 reference-downstream-backfill.json fixture 생성; ALL_MOTION_IDS 11-id, REFERENCE_V1_FORCE_CONFIG pinned — memory reference-v1-pinned-force-config); reference-downstream-backfill.json (untracked seedPayload, 적용 여부 미확인); memory firebase-project-account (owner sunity3412)
  </read_first>
  <action>
    두 도구의 역할을 분리해 검증한다 (HIGH 2 — 리뷰 정정: backfill_reference_downstream.py 는 fixture GENERATOR 라 Firestore 를 쓰지 않으며 --check-firestore 는 stored-angle sanity 만 본다. downstream 4필드 검증은 app/scripts/seed-reference-downstream.mjs --verify 가 REAL 도구다).
    (1) stored-angle sanity gate: backend/scripts/backfill_reference_downstream.py --check-firestore 를 11-id 전체로 실행해 activeVersion/angles/anglesJointKeys/anglesFrames 가 정상임만 확인한다(downstream 필드 검증으로 오인 금지 — 이 모드는 4필드를 보지 않는다).
    (2) downstream 4필드 검증(진짜 게이트): cd app && node scripts/seed-reference-downstream.mjs --input ../reference-downstream-backfill.json --verify 를 실행해 11 reference 가 downstream 4필드(meanAngles, techniqueProfile, bodyNormalizationProfile, forceDirectionPattern) + captureViews 를 전부 보유하는지 확인한다. completeRequiredSet 가 11/11 이어야 PASS. Firebase Admin SA(firebase-sa.json, owner sunity3412 — memory firebase-project-account)로 접근.
    (3) 누락 분기(2-step repair): --verify 가 11/11 미만이면 — (a) 필요 시 fixture 재생성 cd /Users/kimtaesung/Dev/SunityMotion && PYTHONPATH=backend/shared/python python3 backend/scripts/backfill_reference_downstream.py --bucket <video-bucket> --output reference-downstream-backfill.json (REFERENCE_V1_FORCE_CONFIG pinned 하에 생성 — "student path exact" 아님, memory reference-v1-pinned-force-config), (b) 적용 cd app && node scripts/seed-reference-downstream.mjs --input ../reference-downstream-backfill.json --repair-missing --motions <missing ids> (ADD-only merge, 부분 repair 는 --motions 명시 필수 — R5), (c) --verify 재실행해 completeRequiredSet 11/11 도달 확인.
    결과를 15-MODE1-FALSEPOSITIVE-EVIDENCE.md §필드검증 표에 per-motion 기록(meanAngles/techniqueProfile/bodyNormalizationProfile/forceDirectionPattern/captureViews complete=true/false + completeRequiredSet). 이 11/11 게이트는 Mode-1 live E2E(Task 2) 이전에 반드시 PASS.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && node scripts/seed-reference-downstream.mjs --input ../reference-downstream-backfill.json --verify</automated>
  </verify>
  <acceptance_criteria>
    seed-reference-downstream.mjs --verify 가 11 motion 전부 보고하고 completeRequiredSet == 11/11; 11 문서 모두 4필드(meanAngles/techniqueProfile/bodyNormalizationProfile/forceDirectionPattern) + captureViews complete=true (또는 누락분 --repair-missing --motions 후 재verify 11/11); backfill_reference_downstream.py --check-firestore 는 stored-angle sanity 게이트로만 사용(4필드 검증으로 쓰지 않음); evidence 문서에 per-motion 필드 표 + completeRequiredSet 기록; 11/11 PASS 가 Task 2 live E2E 선행
  </acceptance_criteria>
  <done>seed-reference-downstream.mjs --verify 로 11 reference 가 Mode-1 downstream 4필드 + captureViews 를 전부 보유함(completeRequiredSet 11/11)을 확인(누락 시 2-step repair 완료)했다.</done>
</task>

<task type="auto">
  <name>Task 2: 정은지 success 영상 Mode 1 실 E2E (11-ref, MODE_EXPERT) + 전문가 점수 evidence</name>
  <read_first>
    backend/scripts/sweep_phase15.py (Plan 01 산출 — --keys-file/MODE_EXPERT/referenceMotionId/--sweep-uid, 모든 doc createdAt/updatedAt 설정); backend/scripts/phase15_keys.json (Plan 01 Task 3 산출 — success 키 매핑); RESEARCH Open Question 2 (Mode 1 = success video as student vs matching registered reference); CONTEXT D-04 (Mode 1 = 11 reference 전부); 15-02-SUMMARY.md (Pod proxy URL alive)
  </read_first>
  <action>
    sweep_phase15.py 를 MODE_EXPERT 로 Pod 위에서 실행(pod-ops-claude-runs, SSH). success 영상을 student 로, 대응 registered reference 를 referenceMotionId 로 페어링해 Mode 1 실 E2E 를 돌린다(RESEARCH Open Question 2 권고: success=student vs matching reference). sweep-uid 는 sweep_phase15_<epoch_ms> 형태의 throwaway uid(보안 V4 cross-uid 누출 차단). 산출 analysis doc 의 overallScore + dimensionScores + line dimension non-None + 듀얼 coach 섹션을 Firestore 에서 실제로 읽어 15-MODE1-FALSEPOSITIVE-EVIDENCE.md §Mode1점수 표에 per-motion 으로 기록한다 — dry-run 출력만으로는 이 task 가 완료되지 않으며, 실 Firestore analysis doc 에서 파생된 PASS 행(motion 별 실제 overallScore + referenceMotionId)이 존재해야 done 이다. 점수가 "전문가 기준"(고수가 낮게 안 나옴)인지는 다음 Task 의 baseline assert 로 판정 — 여기서는 raw evidence 수집. 실 LLM 발화 확인(gemini_ok / cross-fill 빈 섹션 0) 도 함께 기록. 사람 작성 분석 .md 의 점수를 라벨로 쓰지 않는다(D-06).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && PYTHONPATH=backend/shared/python python3 backend/scripts/sweep_phase15.py --keys-file backend/scripts/phase15_keys.json --dry-run</automated>
  </verify>
  <acceptance_criteria>
    Mode 1 실 E2E 가 success 영상 × registered reference 페어로 완주(server_error 0); 각 doc 에 referenceMotionId + overallScore + dimensionScores 기록; line dimension non-None on ≥1 video(RESEARCH Open Q3); 듀얼 coach 빈 섹션 0; throwaway sweep_uid 사용; evidence 문서 §Mode1점수 표가 실 Firestore analysis doc 에서 파생된 PASS 행(motion 별 실제 overallScore + referenceMotionId)을 보유 — dry-run 출력만으로는 이 task 가 완료되지 않음(dry-run 은 로컬 도구 무결성 pre-check 일 뿐, 실행 evidence 는 실 Firestore doc 에서 채움)
  </acceptance_criteria>
  <done>11 reference 대상 Mode 1 실 E2E 가 완주하고 실 Firestore doc 에서 파생된 점수/섹션 evidence 가 §Mode1점수 표에 문서화되었다(dry-run 단독 불충분).</done>
</task>

<task type="auto">
  <name>Task 3: 위양성 게이트 assert (frozen 08.1, 객관 기준) + 13-영상 크래시/실패 집계 (SC4)</name>
  <read_first>
    backend/scripts/assert_falsepositive_gate.py (Plan 01 산출 — backend/judging_data/tilt_thresholds.yaml frozen checksum c94bb8 hard gate, fallback==0, success/fail 객관 기준 분기); .planning/phases/08.1-axis-metric-redesign/08.1-SWEEP-EVIDENCE.md (정은지 25/25 'low' invariant, frozen thresholds — fail-video score cutoff/exact fault-detection 필드는 정의 안 됨); CONTEXT D-01/D-02/D-03 (실 E2E 재측정 + 재calibrate 금지 + mock 금지); ROADMAP SC4 (다양한 동작/앵글 세트에서 크래시 없이 일관된 점수); backend/scripts/phase15_keys.json (13 영상 전체 매핑 — 7 success + 6 fail, combo 포함); memory analysis-objectivity-no-human-scores
  </read_first>
  <action>
    MEDIUM 6 — assert 실행 전 15-03 에 객관 machine 기준을 명시한다(주관·임의 금지). success gate = 정은지 success 영상의 axis severity 'low'(08.1 25/25 'low' invariant mirror) + 최소 점수 기대치(있으면). fail gate = 객관 machine 기준 — fail 영상이 all-'low' severity 가 아님 / deficit 또는 finding 키를 노출함(가용 필드 기준). 정확한 per-fault 기준을 지금 객관적으로 핀할 수 없으면 fail-video FAULT-TYPE assertion 을 "manual evidence review"(사람이 evidence 표를 눈으로 확인)로 다운그레이드하고 자동 per-fault gating 은 Phase 18(Expert deliberate-fault eval set)로 명시 deferred 한다 — 사람 점수 라벨 ground truth 금지(D-06). 
    실행: fail 영상도 sweep_phase15.py(MODE_EXPERT 또는 self, label=fault)로 실 E2E 돌려 evidence doc 을 수집한 뒤, assert_falsepositive_gate.py 를 Task 2 success + 이 fail evidence 에 대해 실행한다. assert 가 (1) backend/judging_data/tilt_thresholds.yaml sha256 == c94bb8...e87c hard gate 통과 + tilt_thresholds_fallback == 0(MEDIUM 5 — fail-open 탐지), (2) success: overallScore 높음 + axis severity 'low'(41점 같은 위양성 부재 — ROADMAP SC3), (3) fail: 위 객관 fail gate(또는 manual-review 다운그레이드) 를 검증하게 한다. 임계값을 Phase 15 sweep 으로 재calibrate 하지 않는다(D-02, circular tuning 금지 — calibration-source-hard-gate). 위양성 assert 결과를 15-MODE1-FALSEPOSITIVE-EVIDENCE.md §위양성assert 표에 per-video PASS/FAIL 로 기록(실 Firestore doc 의 overallScore + axis severity 에서 파생; dry-run 단독 불충분). 추가로 SC4 — 13 영상 전체 sweep(7 success + 6 fail, combo 포함)의 종료 상태를 집계해 15-MODE1-FALSEPOSITIVE-EVIDENCE.md §SC4-집계 표에 박는다: total 영상 수, 완료(completed) 수, no_human 수, not_pole_motion 수, server_error 수. 다양한 동작/앵글 세트에서 크래시·tracking 실패 없이 일관되게 완주했음을 assert — unexpected pipeline 실패(예기치 않은 server_error 또는 안전게이트 외 크래시) 0 이어야 한다(combo Mode-1-only 는 페어 없음 정상). 하나라도 위양성 FAIL 또는 unexpected pipeline 실패가 있으면 게이트 미충족으로 plan 차단.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && PYTHONPATH=backend/shared/python python3 backend/scripts/assert_falsepositive_gate.py --dry-run</automated>
  </verify>
  <acceptance_criteria>
    15-03 에 success/fail 객관 machine 기준 상수 명시(success=axis severity 'low'; fail=객관 기준 또는 manual-review 다운그레이드 + Phase 18 defer note); assert 스크립트 backend/judging_data/tilt_thresholds.yaml frozen checksum hard gate 통과 + fallback==0(불일치/fallback 시 차단); success 영상 axis severity == 'low' + 위양성 없음; fail 영상 객관 fail gate 충족; 재calibrate 호출 0; evidence 문서 §위양성assert 표가 실 Firestore doc 에서 파생된 per-video PASS/FAIL + 전체 PASS 를 보유(dry-run 단독 불충분 — dry-run 은 checksum gate pre-check 일 뿐); evidence 문서 §SC4-집계 표에 total/completed/no_human/not_pole_motion/server_error 카운트 기록 + unexpected pipeline 실패 0 assert(ROADMAP SC4)
  </acceptance_criteria>
  <done>정은지 success/fail 영상이 frozen 08.1 baseline 대비 객관 기준으로 위양성 없음(success=low, fail=객관 fault gate)으로 assert PASS 했고, 13-영상 sweep 집계가 크래시-프리·일관 완주(unexpected pipeline 실패 0, SC4)를 evidence 로 박았다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sweep script → Firestore | Admin SA 로 throwaway sweep_uid 아래 evidence write/read |
| seed-reference-downstream.mjs → Firestore | Admin SA 로 reference/{motionId} downstream 4필드 read(--verify) / ADD-only merge(--repair-missing) |
| assert script → frozen yaml | read-only checksum 대조 (backend/judging_data/, 재calibrate 금지) |
| 사람 분석 .md → 검증 입력 | success/fail 분류 라벨만 (점수 라벨 금지) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.03-01 | Tampering | Phase 15 sweep 으로 threshold 재calibrate (circular) | mitigate | assert backend/judging_data/tilt_thresholds.yaml checksum c94bb8 hard gate + fallback==0; calibrate import 0 (D-02) |
| T-15.03-02 | Spoofing | objectivity 위반 — 사람 점수 라벨을 ground truth 로 사용 | mitigate | 입력은 fault 종류/분류 라벨만, 점수 라벨 금지 (D-06); 객관 machine 기준 외 per-fault 는 Phase 18 defer |
| T-15.03-03 | Information Disclosure | sweep evidence 가 다른 uid 데이터 누출 | mitigate | throwaway sweep_uid scoped (ASVS V4); cross-uid read 금지 |
| T-15.03-04 | Tampering | reference downstream 필드 누락으로 Mode 1 이 silent fallback | mitigate | Task 1 seed-reference-downstream.mjs --verify completeRequiredSet 11/11 선행 게이트(누락 시 2-step repair) |
| T-15.03-SC | Tampering | npm/pip/cargo installs | accept | 신규 패키지 0 (RESEARCH Package Legitimacy: skipped) |
</threat_model>

<verification>
- seed-reference-downstream.mjs --verify: completeRequiredSet 11/11 (4필드 + captureViews)
- backfill_reference_downstream.py --check-firestore: stored-angle sanity 게이트로만 (downstream 검증 아님)
- Mode 1 실 E2E: server_error 0 + line non-None + referenceMotionId lockstep (실 Firestore doc 파생, dry-run 단독 불충분)
- assert_falsepositive_gate: backend/judging_data/ frozen checksum gate PASS + fallback==0 + success=low + fail=객관 fault gate
- SC4 집계: 13 영상 total/completed/no_human/not_pole_motion/server_error 카운트 + unexpected pipeline 실패 0
- 15-MODE1-FALSEPOSITIVE-EVIDENCE.md: 필드검증(11/11) + Mode1점수 + 위양성assert + SC4-집계 4개 표 전부 PASS
</verification>

<success_criteria>
- MODE-01: 11 reference 대상 Mode 1 실 E2E 가 전문가 기준 점수 산출 (referenceMotionId lockstep, ROADMAP SC1)
- SCORE-04: 정은지 success=high+low-severity, fail=객관 fault gate, 41점 위양성 부재 (ROADMAP SC3)
- SC4: 13 영상 sweep 크래시-프리·일관 완주 집계 (unexpected pipeline 실패 0, ROADMAP SC4)
- frozen 08.1 baseline 대조 (backend/judging_data/tilt_thresholds.yaml, 재calibrate 0, D-02)
- 실 LLM path (recognizer line non-None + 듀얼 coach 빈 섹션 0, D-03)
</success_criteria>

<output>
Create `.planning/phases/15-mode-1-mode-3-testflight/15-03-SUMMARY.md` when done
</output>
</content>
