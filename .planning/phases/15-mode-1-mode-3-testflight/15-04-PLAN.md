---
phase: 15-mode-1-mode-3-testflight
plan: 04
type: execute
wave: 2
depends_on: ["15-01", "15-02"]
files_modified:
  - .planning/phases/15-mode-1-mode-3-testflight/15-MODE3-DUALCOACH-EVIDENCE.md
autonomous: true
requirements: [MODE-02]
user_setup: []

must_haves:
  truths:
    - "정은지 동일 동작 fail→success 페어가 MODE_SELF 2 analysis doc 으로 제출되어 deltaFromPrevious 가 계산된다 (페어별 throwaway uid 또는 페어 내 monotonic createdAt 으로 success 가 자기 fail 과만 페어링)"
    - "각 success doc 의 comparison.previousAnalysisId 가 같은 motion 의 paired fail analysis_id 와 일치하고 deltaFromPrevious 가 non-empty 다"
    - "deltaFromPrevious 가 차원 점수(line/stability/angle) 세션 간 차이로 산출되고 부호가 발전 방향과 일치한다 (지난 분석보다 N점 발전 — build_mode3 / result.tsx:192 계약)"
    - "듀얼 coach 가 실 LLM 으로 발화하고 한쪽 drop 시 cross-fill 로 빈 섹션이 0 이다"
    - "Mode 3 표시가 %일치 헤드라인이 아니라 차원 점수 델타 형태다"
  artifacts:
    - path: ".planning/phases/15-mode-1-mode-3-testflight/15-MODE3-DUALCOACH-EVIDENCE.md"
      provides: "페어별 deltaFromPrevious 차원 점수 델타 표 + previousAnalysisId 페어링 검증 + 부호 검증 + 듀얼 coach sectionAudit(빈 섹션 0) 기록"
  key_links:
    - from: "backend/scripts/sweep_phase15.py"
      to: "firestore_admin.get_previous_analysis(mode=MODE_SELF)"
      via: "페어별 uid + --pair-sequential 2 analysis 페어링 → assemble.build_mode3 deltaFromPrevious"
      pattern: "deltaFromPrevious"
    - from: "backend/functions/pipeline/app.py"
      to: "assemble.assemble_dual_coach_sections"
      via: "Gemini+Cerebras 호출 + cross-fill"
      pattern: "assemble_dual_coach_sections"
---

<objective>
MODE-02(Mode 3 세션 간 발전 델타)를 정은지 동일 동작 fail→success 페어로 실 E2E 검증하고, 같은 run 에서 듀얼 coach 가 실 LLM 으로 발화하며 빈 섹션 0(cross-fill)임을 확인한다. belle 통찰(CONTEXT specifics): 동일 인물·동일 동작 페어가 Mode 3 델타를 충족한다. fail 을 이전 분석으로, success 를 현재 분석으로 MODE_SELF 로 제출해 deltaFromPrevious 가 "지난 분석보다 N점 발전" 형태의 차원 점수 델타로 산출되는지 본다. [D-13] 현 계약(build_mode3 + result.tsx:192)은 차원 점수 델타이며 관절 각도(°) 델타가 아니다 — Phase 15 는 검증-only 라 점수 델타로 통일하고, 각도 델타는 후속 phase Deferred.

Purpose: Mode 3 = 발전 not 일치(memory mode3-progress-not-similarity) — 차원 점수 세션 간 델타가 실영상에서 성립함을 증명(ROADMAP SC2). 동시에 듀얼 coach graceful degrade(CONTEXT D-12)를 실 LLM 으로 확인(D-03).
Output: 페어별 차원 점수 델타 + previousAnalysisId 페어링 + 부호 검증 + 듀얼 coach audit evidence 문서.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-mode-1-mode-3-testflight/15-CONTEXT.md
@.planning/phases/15-mode-1-mode-3-testflight/15-RESEARCH.md
@.planning/phases/15-mode-1-mode-3-testflight/15-VALIDATION.md
@.planning/phases/15-mode-1-mode-3-testflight/15-01-SUMMARY.md
@.planning/phases/15-mode-1-mode-3-testflight/15-02-SUMMARY.md
@backend/shared/python/sunity_shared/analysis/assemble.py
@backend/shared/python/sunity_shared/firestore_admin.py
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 정은지 fail→success 페어 Mode 3 실 E2E (MODE_SELF, 페어별 격리) + deltaFromPrevious 차원 점수 델타 검증</name>
  <read_first>
    backend/shared/python/sunity_shared/analysis/assemble.py:539-562 (build_mode3 — deltaFromPrevious[dim]=cur-prev, dim ∈ line/stability/angle 차원 점수); backend/functions/pipeline/app.py:1842 (get_previous_analysis(uid,id,mode=MODE_SELF) mode-scoped, commit 0bd6a48); backend/shared/python/sunity_shared/firestore_admin.py:1322 (get_previous_analysis — status==done + createdAt DESC order; createdAt 누락 doc 은 prior 후보에서 빠짐); app/src/app/analysis/result.tsx:189,661 (mode3Summary "지난 분석보다 N점 발전" + deltaFor — 차원 점수 델타); backend/scripts/sweep_phase15.py + phase15_keys.json (Plan 01 — MODE_SELF 페어, createdAt/updatedAt 설정, --pair-sequential); memory mode3-progress-not-similarity
  </read_first>
  <action>
    sweep_phase15.py 를 MODE_SELF + --pair-sequential 로 Pod 위에서 실행해, 6 동작 각각 fail 영상을 먼저(이전 분석), success 영상을 나중(현재 분석)으로 2 analysis doc 으로 제출한다. HIGH 1 페어링 보장: success doc 이 무관한 이전 mode3 doc 이 아니라 자기 동작의 fail doc 과만 페어링되도록, 페어별로 throwaway uid 를 따로 쓰거나(권장) 같은 uid 면 페어 내 monotonic createdAt(fail.createdAt < success.createdAt)을 보장하고 페어 내 순차(병렬 금지) 제출한다. sweep_phase15.py 가 모든 doc 에 createdAt/updatedAt 을 박으므로(Plan 01 HIGH 1) get_previous_analysis(firestore_admin.py:1322, createdAt DESC)가 직전 fail 분석을 prior 로 잡아 build_mode3 가 deltaFromPrevious 를 계산한다(RESEARCH Pattern 2; INFO note — 제출 순서 보존). 
    검증: 산출 success doc 의 (a) comparison.previousAnalysisId 가 같은 motion 의 paired fail analysis_id 와 일치하고, (b) deltaFromPrevious 가 non-empty 이며, (c) deltaFromPrevious[dim] 가 차원 점수 차이(cur-prev, dim ∈ line/stability/angle 점수)이고, (d) 부호가 발전 방향(fail→success 이므로 개선 dimension 은 양의 점수 개선)과 일치하는지를 실 Firestore analysis doc 에서 읽어 15-MODE3-DUALCOACH-EVIDENCE.md §델타 표에 per-페어 PASS 행으로 기록한다 — 아래 verify 의 pytest 는 회귀 pre-check 일 뿐이며, dry-run/pytest 통과만으로는 이 task 가 완료되지 않는다. §델타 표가 실 Firestore doc 에서 파생된 per-페어 실측 deltaFromPrevious(dim별 점수 cur-prev 값 + 부호 + previousAnalysisId 매칭)를 보유해야 done 이다. mode3Summary 헤드라인이 차원 점수 델타 형태("지난 분석보다 N점 발전")이고 %일치 헤드라인이 아님을 확인(memory mode3-progress-not-similarity, result.tsx:192). [D-13] 관절 각도(°) 델타는 현 계약에 없으므로 검증 대상이 아니다(후속 phase Deferred). combo 는 페어 없음 → 제외(Deferred). 사람 점수 라벨 금지(D-06).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && PYTHONPATH=backend/shared/python python3 -m pytest backend/tests -k "mode3 or assemble" -q</automated>
  </verify>
  <acceptance_criteria>
    6 페어(combo 제외) 각각 MODE_SELF 2 doc 을 페어별 격리(페어별 uid 또는 페어 내 monotonic createdAt)로 순차 제출; 각 success doc 의 comparison.previousAnalysisId == 같은 motion 의 paired fail analysis_id; deltaFromPrevious non-empty; 델타가 차원 점수(line/stability/angle 점수) 차이 + 부호가 발전 방향 일치; mode3Summary 가 차원 점수 델타 형태("N점 발전", %일치 헤드라인 아님); 관절 각도(°) 델타 검증 없음(D-13 Deferred); evidence 문서 §델타 표가 실 Firestore analysis doc 에서 파생된 per-페어 PASS 행(dim별 실측 점수 cur-prev + 부호 + previousAnalysisId 매칭)을 보유 — pytest 통과만으로는 task 완료 아님(pytest 는 회귀 pre-check); 기존 mode3/assemble pytest 회귀 0
  </acceptance_criteria>
  <done>fail→success 페어가 자기 fail 과만 페어링(previousAnalysisId 매칭)되어 deltaFromPrevious 차원 점수 델타를 산출하고, 실 Firestore doc 파생 per-페어 PASS 행이 §델타 표에 기록되어 부호/형태가 "N점 발전" 표시로 올바름이 evidence 로 확인됐다(pytest 단독 불충분).</done>
</task>

<task type="auto">
  <name>Task 2: 듀얼 coach 실 LLM 발화 + cross-fill 빈 섹션 0 검증 (CONTEXT D-12)</name>
  <read_first>
    backend/functions/pipeline/app.py:1966-2024 (_call_coach_writer_with_retry Gemini+Cerebras + assemble_dual_coach_sections, GEMINI_COACH_ENABLED default 1 app.py:211); backend/shared/python/sunity_shared/analysis/assemble.py:404 (assemble_dual_coach_sections — 섹션 출처 태깅 + cross-fill); app.py:1989 (로그 "coach dual-track 섹션 조립" gemini_ok/cerebras_ok/cross_filled); CONTEXT D-11/D-12 (원인=Gemini/처방=Cerebras/부상=Cerebras/강사확인=Gemini; 둘 다 best, drop 시 cross-fill 빈 섹션 0 = PASS); memory section-dual-coach-report
  </read_first>
  <action>
    Task 1 의 실 E2E doc 들에서 듀얼 coach 출력을 검사한다. (1) Pod 로그 "coach dual-track 섹션 조립" 라인을 grep 해 gemini_ok / cerebras_ok / cross_filled 값을 수집(RESEARCH Code Examples). (2) coachDetails 4 섹션(원인/처방/부상위험/강사확인)이 전부 채워졌는지 — best case 는 둘 다 발화(gemini_ok=True cerebras_ok=True cross_filled=[]). (3) 한쪽 drop 케이스에서 cross-fill 폴백으로 빈 섹션이 0 인지 확인(CONTEXT D-12 graceful degrade = PASS, 둘 다 필수 아님). (4) 최소 1개 영상에서 sectionAudit 이 non-cross-filled Gemini 섹션을 보유 = 실 Gemini 발화 증거(RESEARCH Pitfall 6 heuristic-only 차단). 결과를 15-MODE3-DUALCOACH-EVIDENCE.md §듀얼coach 표에 per-video(gemini_ok/cerebras_ok/빈 섹션 수) 기록. 빈 섹션이 0 보다 크면 FAIL. 벤더명 비노출·detail2 형상 불변 유지(13-C 박제 — UI 라벨은 기능 라벨만).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && PYTHONPATH=backend/shared/python python3 -m pytest backend/tests/phase13 -k "dual or section or cross" -q</automated>
  </verify>
  <acceptance_criteria>
    실 E2E 영상에서 듀얼 coach 발화 — best case 둘 다 ok 또는 drop 시 cross-fill 로 빈 섹션 0; ≥1 영상에 non-cross-filled Gemini 섹션(실 발화 증거, heuristic-only 아님); coachDetails 4 섹션 전부 채워짐; 벤더명 비노출 유지; evidence 문서 §듀얼coach 표 per-video 기록; phase13 dual/cross 회귀 0
  </acceptance_criteria>
  <done>듀얼 coach 가 실 LLM 으로 발화하고 한쪽 drop 시에도 cross-fill 로 빈 섹션 0(D-12 PASS)임을 확인했다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sweep script → Firestore | 페어별 throwaway uid(또는 monotonic createdAt) 아래 MODE_SELF 2 doc 페어링 |
| pipeline → Gemini/Cerebras | 실 LLM 호출 (키 Pod env) |
| 분석 결과 → UI | coachDetails 4 섹션 (벤더명 비노출) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.04-01 | Tampering | get_previous_analysis 가 mode1-prev 또는 무관한 mode3 doc 오염으로 잘못된 델타 | mitigate | mode-scoped MODE_SELF 페어링(app.py:1842 commit 0bd6a48); 페어별 throwaway uid 격리 또는 monotonic createdAt; success.previousAnalysisId == paired fail id assert (HIGH 1) |
| T-15.04-02 | Information Disclosure | 듀얼 coach 로그에 API 키 노출 | mitigate | 로그는 gemini_ok/cerebras_ok 플래그만; 키 출력 금지(RESEARCH V6) |
| T-15.04-03 | Spoofing | coach 섹션 벤더명 노출로 UI 신뢰 왜곡 | mitigate | 기능 라벨만, 벤더명 비노출(13-C 박제) |
| T-15.04-04 | Tampering | 듀얼 coach 가 heuristic-only 인데 발화로 오판 | mitigate | ≥1 non-cross-filled Gemini 섹션 + gemini_ok 로그 assert(Pitfall 6) |
| T-15.04-SC | Tampering | npm/pip/cargo installs | accept | 신규 패키지 0 (RESEARCH Package Legitimacy: skipped) |
</threat_model>

<verification>
- deltaFromPrevious non-empty + 차원 점수 차이 + 부호 발전 방향 일치 (6 페어, 실 Firestore doc 파생 — pytest 단독 불충분)
- 각 success doc previousAnalysisId == paired fail id (페어별 격리, HIGH 1)
- mode3Summary 차원 점수 델타 형태 ("N점 발전", %일치 헤드라인 아님, 관절 각도 델타 없음 — D-13)
- 듀얼 coach: 빈 섹션 0 + ≥1 non-cross-filled Gemini 섹션
- 15-MODE3-DUALCOACH-EVIDENCE.md: §델타 + §듀얼coach 표 전부 PASS
- 회귀: phase13 dual/cross + mode3/assemble pytest 0
</verification>

<success_criteria>
- MODE-02: fail→success 페어가 deltaFromPrevious 차원 점수 델타 산출, "지난 분석보다 N점 발전" 형태 (ROADMAP SC2; 관절 각도 델타는 D-13 Deferred)
- Mode 3 = 발전 not 일치 (mode3-progress-not-similarity)
- success doc previousAnalysisId == paired fail id + deltaFromPrevious non-empty (HIGH 1)
- 듀얼 coach 실 LLM 발화 + cross-fill 빈 섹션 0 (CONTEXT D-12, D-03)
</success_criteria>

<output>
Create `.planning/phases/15-mode-1-mode-3-testflight/15-04-SUMMARY.md` when done
</output>
</content>
