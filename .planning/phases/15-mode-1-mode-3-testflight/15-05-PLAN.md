---
phase: 15-mode-1-mode-3-testflight
plan: 05
type: execute
wave: 3
depends_on: ["15-03", "15-04"]
files_modified:
  - app/eas.json
  - .planning/phases/15-mode-1-mode-3-testflight/15-DELIV-EVIDENCE.md
autonomous: false
requirements: [DELIV-01]
user_setup:
  - service: testflight
    why: "실기기 게스트 완주 + 결과 영상 재생 + release-only SIGABRT 부재 확인은 사람만 가능 (belle, Claude-side PASS 이후)"
    dashboard_config:
      - task: "TestFlight 로 testflight-preview 빌드 설치 후 익명 게스트로 Mode 1+3 완주 + 결과 영상 재생 + SIGABRT 튕김 부재 확인"
        location: "iOS 실기기 (belle)"

must_haves:
  truths:
    - "eas.json 에 store-signed testflight-preview 빌드 프로필이 추가된다 (channel preview, production 과 동일 env, autoIncrement true, distribution:internal 없음 — App Store Connect/TestFlight submit 가능)"
    - "기존 preview 프로필(distribution:internal)은 내부 설치용으로 보존된다 (TestFlight submit 에 쓰지 않음)"
    - "EAS testflight-preview 빌드가 letterSpacing SIGABRT 회귀 없이(static/build 체크) 성공하고 --auto-submit-with-profile production 으로 submit 까지 Claude-side PASS 다"
    - "presigned URL 만료/Content-Type 회귀가 없다 (결과 영상 재생 가능)"
    - "belle 가 실기기에서 익명 게스트로 Mode 1+3 완주 + 결과 영상 재생 + 런타임 SIGABRT 부재를 확인한다 (Claude PASS 이후 핸드오프)"
  artifacts:
    - path: "app/eas.json"
      provides: "store-signed testflight-preview 빌드 프로필 (channel preview, autoIncrement true, production env mirror, distribution:internal 없음); submit 은 기존 production 프로필 재사용"
      contains: "testflight-preview"
    - path: ".planning/phases/15-mode-1-mode-3-testflight/15-DELIV-EVIDENCE.md"
      provides: "testflight-preview 프로필 diff + static/build 체크(tsc/track=()=>0/negative letterSpacing 0) + 빌드/submit 로그 요약 + 회귀 체크리스트 + belle 실기기 SIGABRT/완주 결과"
  key_links:
    - from: "app/eas.json testflight-preview.env"
      to: "Firebase + API base URL"
      via: "빌드 시점 EXPO_PUBLIC_* inline (Expo SDK 54)"
      pattern: "EXPO_PUBLIC_API_BASE_URL"
---

<objective>
DELIV-01(TestFlight 게스트 모드 Mode 1+3 실기기 완주)을 전달한다. SIGABRT 는 이미 source 에서 fix 됨(typography.ts:15 track=()=>0). 남은 작업: (1) HIGH 3 — 기존 preview 프로필은 distribution:internal 라 TestFlight submit 불가하므로, store-signed testflight-preview 빌드 프로필을 신설(channel preview, production env mirror, autoIncrement true, distribution:internal 없음), (2) R5 — EAS testflight-preview 빌드를 --auto-submit-with-profile production 로 돌려(기존 production submit 프로필/ASC creds 재사용) submit PASS 확인, (3) static/build 회귀 체크(tsc clean / track=()=>0 / negative letterSpacing 0) + presigned/Content-Type 회귀 체크리스트 통과, (4) Claude-side PASS 이후 belle 에게만 사람-전용 실기기 게스트 완주 + 영상 재생 + 런타임 SIGABRT 부재 단계를 핸드오프(CONTEXT D-09, 미검증 핸드오프 금지).

Purpose: 실 release 환경(store-signed EAS 빌드)에서 빌드가 동작 + config 정상 + static 회귀 부재를 Claude 가 먼저 증명하고, 런타임 SIGABRT 부재처럼 사람만 가능한 실기기 검증만 belle 에게 넘긴다. Plan 03/04 의 실 E2E PASS 가 선행(빌드가 가리키는 백엔드가 동작함).
Output: testflight-preview 프로필 추가 eas.json + 빌드/submit evidence + belle 실기기 핸드오프 결과.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-mode-1-mode-3-testflight/15-CONTEXT.md
@.planning/phases/15-mode-1-mode-3-testflight/15-RESEARCH.md
@.planning/phases/15-mode-1-mode-3-testflight/15-VALIDATION.md
@.planning/phases/15-mode-1-mode-3-testflight/15-03-SUMMARY.md
@.planning/phases/15-mode-1-mode-3-testflight/15-04-SUMMARY.md
@app/eas.json
@app/src/theme/typography.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: eas.json store-signed testflight-preview 빌드 프로필 신설 (production env mirror) + SIGABRT static 가드</name>
  <read_first>
    app/eas.json (build.preview.distribution = "internal" — App Store Connect/TestFlight submit 불가; build.production 만 env(EXPO_PUBLIC_FIREBASE_* 6개 + EXPO_PUBLIC_API_BASE_URL) + autoIncrement true 보유; submit 은 production 프로필만 존재 ascAppId 6772934567; preview 에 env 블록 없음); app/src/theme/typography.ts:15 (track=()=>0 SIGABRT fix); RESEARCH §Runtime State Inventory Build artifacts + Pitfall 3 (preview 빌드 env 누락 → app non-functional); memory eas-build-gotchas
  </read_first>
  <action>
    app/eas.json 의 build 에 store-signed 신규 빌드 프로필 testflight-preview 를 추가한다 — distribution:internal 을 절대 넣지 않고(이게 TestFlight submit 을 막는 원인, HIGH 3), channel:"preview", autoIncrement:true, ios.resourceClass 필요시 m-medium, 그리고 production 프로필의 env 블록(EXPO_PUBLIC_FIREBASE_API_KEY/_AUTH_DOMAIN/_PROJECT_ID/_STORAGE_BUCKET/_MESSAGING_SENDER_ID/_APP_ID + EXPO_PUBLIC_API_BASE_URL)을 그대로 복사한다(RESEARCH Pitfall 3 — EXPO_PUBLIC_* 는 빌드 시점 inline 되므로 env 없으면 Firebase/API 미설정으로 앱 비동작). R5 — submit 프로필은 새로 만들지 않는다: Expo automate-submissions 는 --auto-submit 가 빌드 프로필과 동일 이름의 submit 프로필을 찾는데 submit 에는 production 만 있어 testflight-preview submit 프로필이 없으면 실패한다 → Task 2 에서 --auto-submit-with-profile production 로 기존 production submit(ascAppId 6772934567)을 명시 재사용한다. 따라서 eas.json submit 섹션은 그대로 둔다. 기존 preview 빌드 프로필(distribution:internal)은 내부 설치 전용으로 그대로 보존한다 — TestFlight submit 에 재사용하지 않는다. 동시에 typography.ts:15 의 letterSpacing SIGABRT fix(track=()=>0)가 회귀하지 않았는지 grep 으로 확인 — letterSpacing 에 음수 size 곱셈 패턴이 재도입되지 않았는지(memory). tsc --noEmit clean 유지. 브랜드 컬러/spacing 하드코딩 금지(theme 토큰만 — CLAUDE.md).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && node -e "const e=require('./eas.json'); const p=e.build['testflight-preview']; if(!p)throw new Error('testflight-preview missing'); if(p.distribution==='internal')throw new Error('must not be internal'); if(p.channel!=='preview')throw new Error('channel must be preview'); if(p.autoIncrement!==true)throw new Error('autoIncrement must be true'); ['EXPO_PUBLIC_FIREBASE_API_KEY','EXPO_PUBLIC_FIREBASE_AUTH_DOMAIN','EXPO_PUBLIC_FIREBASE_PROJECT_ID','EXPO_PUBLIC_FIREBASE_STORAGE_BUCKET','EXPO_PUBLIC_FIREBASE_MESSAGING_SENDER_ID','EXPO_PUBLIC_FIREBASE_APP_ID','EXPO_PUBLIC_API_BASE_URL'].forEach(k=>{if(!p.env||!p.env[k])throw new Error('missing env '+k); if(p.env[k]!==e.build.production.env[k])throw new Error('env must mirror production '+k)}); if(e.build.preview.distribution!=='internal')throw new Error('preview must stay internal'); console.log('testflight-preview OK')" && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    eas.json 에 testflight-preview 빌드 프로필 존재 + distribution:internal 없음 + channel:"preview" + autoIncrement === true(R6 — node assert 가 명시 검사) + env 에 6 Firebase 키 + EXPO_PUBLIC_API_BASE_URL 이 production env 값과 동일(mirror) 검사; 기존 preview 프로필 distribution:"internal" 그대로 보존(내부 설치 전용); submit 섹션 미변경(production 만, Task 2 가 --auto-submit-with-profile production 재사용 — R5); typography.ts:15 track=()=>0 유지(SIGABRT static 가드 — letterSpacing 음수 곱셈 패턴 grep 0); tsc --noEmit clean
  </acceptance_criteria>
  <done>store-signed testflight-preview 빌드 프로필이 production env 를 mirror 하고 autoIncrement true·distribution:internal 없이 추가됐으며(submit 은 production 재사용), 기존 preview 는 보존되고 SIGABRT static 가드(track=()=>0) 회귀 없이 tsc clean 이다.</done>
</task>

<task type="auto">
  <name>Task 2: EAS testflight-preview 빌드 + --auto-submit-with-profile production submit (Claude-side PASS) + static/build 회귀 체크리스트</name>
  <read_first>
    app/eas.json (testflight-preview 빌드 프로필 + submit production ascAppId 6772934567); app/src/theme/typography.ts:15 (track=()=>0); Expo automate-submissions doc (https://docs.expo.dev/build/automate-submissions/ — --auto-submit 는 빌드 프로필과 동일 이름 submit 프로필을 찾음; 다른 이름은 --auto-submit-with-profile <name> 로 명시); RESEARCH §Don't Hand-Roll (EAS Submit ASC API key 무인 submit, A4); CONTEXT D-08/D-10 (preview 빌드 경로 + 회귀 체크리스트 presigned/Content-Type/SIGABRT); memory asc-app-id-and-api-key (submit 백그라운드 OK), eas-submit-needs-stdin (해제됨), s3-presigned-video-playback
  </read_first>
  <action>
    Claude 가 EAS testflight-preview 빌드를 실행하고 빌드 성공 + 아티팩트 존재를 확인한다. R5 — submit 은 --auto-submit-with-profile production 으로 기존 production submit 프로필(ascAppId 6772934567)을 명시 재사용한다: eas build --profile testflight-preview --platform ios --auto-submit-with-profile production. (이유: Expo automate-submissions 는 plain --auto-submit 시 빌드 프로필과 동일 이름의 submit 프로필을 찾는데 submit 에는 production 만 있어 testflight-preview submit 프로필 부재로 실패한다 — 동일-이름 default 회피 위해 명시 프로필 지정.) 빌드 후 분리 submit 이 필요하면 eas submit --platform ios --profile production 으로도 동일 ASC creds 를 쓴다(memory asc-app-id-and-api-key — 백그라운드 OK, A4 재확인). LOW 7 — SIGABRT 는 build 로그로 증명하지 않는다(release 런타임 크래시 경로라 build 로그는 못 잡음). 대신 static/build 체크만 Claude-side 로 확정: (1) tsc --noEmit clean, (2) typography.ts:15 track=()=>0 present, (3) letterSpacing 음수 곱셈 패턴 grep 0, (4) EAS 빌드 성공, (5) submit 성공. 추가 회귀 체크리스트(CONTEXT D-10): (a) presigned URL 만료 — POST /playback-url refresh path 존재 확인(7일 TTL, memory s3-presigned-video-playback), (b) S3 PUT Content-Type 명시(octet-stream 방지 — Plan 01 upload 스크립트 + 앱 api.ts 경로). 결과(빌드 id/submit 상태/static 체크 표/회귀 체크 표)를 15-DELIV-EVIDENCE.md 에 기록 — 런타임 SIGABRT 부재는 여기서 단언하지 않고 belle 실기기 checkpoint 결과로 채운다. 빌드·submit 이 PASS 가 아니면 belle 핸드오프 금지(D-09 미검증 핸드오프 금지). belle Apple Developer 계정 권한이 필요한 자격 단계가 막히면 그 지점만 belle 에게 알린다.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && test -f .planning/phases/15-mode-1-mode-3-testflight/15-DELIV-EVIDENCE.md && grep -q "testflight-preview" .planning/phases/15-mode-1-mode-3-testflight/15-DELIV-EVIDENCE.md && echo "evidence recorded"</automated>
  </verify>
  <acceptance_criteria>
    EAS testflight-preview 빌드 성공 + 아티팩트 존재; TestFlight submit PASS via --auto-submit-with-profile production (기존 production ASC submit 프로필 재사용, 동일-이름 default 회피 — R5); static/build 체크 PASS(tsc --noEmit clean / track=()=>0 present / negative letterSpacing 패턴 0 / 빌드 성공 / submit 성공) — 런타임 SIGABRT 단언은 build 로그로 하지 않음(LOW 7); 회귀 체크리스트 2항목(presigned refresh path 존재 / Content-Type 명시) PASS; 15-DELIV-EVIDENCE.md 에 빌드 id/submit 상태/static 체크 표/회귀 표 기록; 런타임 SIGABRT 부재 행은 belle 실기기 결과로 채워질 자리로 남김
  </acceptance_criteria>
  <done>EAS testflight-preview 빌드 + --auto-submit-with-profile production submit 이 Claude-side PASS 이고 static/build 회귀 체크가 통과해 belle 핸드오프 준비가 끝났다(런타임 SIGABRT 단언은 belle checkpoint 로 이연).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: belle 실기기 게스트 Mode 1+3 완주 + 결과 영상 재생 + 런타임 SIGABRT 부재 확인</name>
  <action>belle 가 TestFlight 로 testflight-preview 빌드를 실기기에 설치해 익명 게스트로 Mode 1+3 를 완주하고 결과 영상 재생 + 런타임 SIGABRT 부재를 확인한다. Claude-side 빌드+submit+static 회귀 PASS(Task 1·2) 이후에만 진행되는 사람-전용 검증이다(CONTEXT D-09 verify-before-handoff). 결과(특히 런타임 SIGABRT 부재 행)를 15-DELIV-EVIDENCE.md 에 기록한다 — build 로그가 아니라 device 결과가 런타임 SIGABRT 부재의 유일한 증거다(LOW 7).</action>
  <what-built>Claude 가 store-signed testflight-preview 빌드 프로필을 추가하고(production env mirror, autoIncrement true, distribution:internal 없음), EAS testflight-preview 빌드를 돌려 static/build 체크(tsc/track=()=>0/negative letterSpacing 0/빌드/submit) PASS 로 --auto-submit-with-profile production 으로 TestFlight submit 까지 확인했다(Task 1·2). 런타임 SIGABRT 부재는 build 로그로 증명 불가하므로 이제 사람만 가능한 실기기 게스트 완주 + 결과 영상 재생 + 런타임 크래시 부재 확인만 남았다.</what-built>
  <read_first>
    .planning/phases/15-mode-1-mode-3-testflight/15-DELIV-EVIDENCE.md (Claude-side 빌드/submit/static 회귀 PASS 기록 + 런타임 SIGABRT 행 미기입); CONTEXT D-09 (verify-before-handoff — Claude PASS 이후에만 핸드오프); ROADMAP SC5 (게스트 진입 → Mode 1+3 완주 → 결과 영상 재생, presigned/Content-Type/SIGABRT 회귀 없음)
  </read_first>
  <how-to-verify>
    1. TestFlight 에서 방금 submit 된 testflight-preview 빌드를 실기기에 설치한다.
    2. 회원가입 없이 익명 게스트로 진입한다(인트로 → 탭).
    3. Mode 1 완주 — 정은지 기준 모션 선택 → 본인 영상 업로드 → 실분석 결과 + 전문가 기준 점수 표시 확인.
    4. Mode 3 완주 — 본인 영상 2개 업로드 → "지난 분석보다 N점 발전" 차원 점수 델타 표시 확인.
    5. 결과 영상 재생 확인 — 결과 화면에서 영상이 octet-stream 깨짐/만료 없이 재생되는지.
    6. 런타임 SIGABRT 부재 확인 — 앱이 letterSpacing SIGABRT 로 튕기지 않는지(release 빌드에서만 재현되던 크래시, build 로그로는 못 잡음). 이 device 결과를 15-DELIV-EVIDENCE.md 런타임 SIGABRT 행에 기록.
  </how-to-verify>
  <resume-signal>완주 + 재생 + SIGABRT 부재 정상이면 "approved", 문제가 있으면 어떤 단계/증상인지 기술 (gap closure 후속).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eas.json env → release 빌드 | EXPO_PUBLIC_* 빌드 시점 inline (Firebase config 노출 — 보안은 Firestore rules) |
| app → S3 presigned | 결과 영상 GET (uid-scoped, 7일 TTL) |
| EAS submit → TestFlight | ASC API key 무인 submit (--auto-submit-with-profile production 으로 기존 production submit 프로필 재사용) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.05-01 | Information Disclosure | testflight-preview env 의 Firebase config 노출 | accept | EXPO_PUBLIC_ 는 비밀 아님(클라이언트 inline 의도); 보안은 Firestore rules 가 강제(CLAUDE.md) |
| T-15.05-02 | Tampering | 빌드가 env 누락으로 백엔드 미연결 ship | mitigate | Task 1 testflight-preview.env 게이트 + node assert(production mirror); 빌드 전 검증 |
| T-15.05-03 | Tampering | internal preview 프로필로 TestFlight submit 시도 → 막힘/ad-hoc | mitigate | store-signed testflight-preview(distribution:internal 없음) 신설, preview 는 내부 설치 전용 보존 (HIGH 3) |
| T-15.05-04 | Denial of Service | plain --auto-submit 가 동일-이름 submit 프로필(testflight-preview) 부재로 submit 실패 | mitigate | --auto-submit-with-profile production 으로 기존 production submit 명시 재사용 (R5, Expo automate-submissions 동작) |
| T-15.05-05 | Information Disclosure | presigned GET 만료/Content-Type 으로 재생 실패 또는 cross-uid 노출 | mitigate | POST /playback-url refresh + Content-Type 강제(D-10); uid-scoped 키(ASVS V4) |
| T-15.05-06 | Denial of Service | release-only SIGABRT 크래시로 게스트 완주 불가 | mitigate | typography.ts:15 fix static 가드 + belle 실기기 런타임 확인(build 로그 불가 — LOW 7) |
| T-15.05-07 | Spoofing | 미검증 빌드를 belle 에게 핸드오프 | mitigate | D-09 — Claude-side 빌드+submit+static 회귀 PASS 이후에만 핸드오프 |
| T-15.05-SC | Tampering | npm/pip/cargo installs | accept | 신규 패키지 0 (RESEARCH Package Legitimacy: skipped) |
</threat_model>

<verification>
- eas.json testflight-preview: distribution:internal 없음 + channel preview + autoIncrement === true(R6) + 6 Firebase 키 + API base URL 이 production env mirror (node assert); 기존 preview 는 internal 보존
- tsc --noEmit clean + SIGABRT static 가드(track=()=>0, negative letterSpacing 0) — 런타임 SIGABRT 는 build 로그로 단언 안 함(LOW 7)
- EAS testflight-preview 빌드 성공 + --auto-submit-with-profile production submit PASS (R5)
- 회귀 체크리스트 2항목 PASS (presigned refresh / Content-Type)
- belle 핸드오프: 실기기 게스트 Mode 1+3 완주 + 영상 재생 + 런타임 SIGABRT 부재 approved (15-DELIV-EVIDENCE.md 에 device 결과 기록)
</verification>

<success_criteria>
- DELIV-01: 게스트 모드 Mode 1+3 실기기 완주 + 결과 영상 재생 (ROADMAP SC5)
- store-signed testflight-preview 빌드 프로필 신설(HIGH 3 — internal preview 로 submit 불가 해소) + autoIncrement true 검증 (R6)
- submit 은 --auto-submit-with-profile production 으로 기존 production submit 재사용 (R5)
- SIGABRT static 가드 + presigned/Content-Type 회귀 부재 (D-10); 런타임 SIGABRT 부재는 belle device 결과로 증명 (LOW 7)
- Claude-side PASS 이후 belle 핸드오프 (D-09, verify-before-handoff)
</success_criteria>

<output>
Create `.planning/phases/15-mode-1-mode-3-testflight/15-05-SUMMARY.md` when done
</output>
</content>
