---
phase: 15-mode-1-mode-3-testflight
plan: 05
type: execute
wave: 3
depends_on: ["15-03", "15-04"]
files_modified:
  - app/eas.json
  - .planning/phases/15-mode-1-mode-3-testflight/15-DELIV-EVIDENCE.md
autonomous: false
requirements: [DELIV-01]
user_setup:
  - service: testflight
    why: "실기기 게스트 완주 + 결과 영상 재생 확인은 사람만 가능 (belle, Claude-side PASS 이후)"
    dashboard_config:
      - task: "TestFlight 로 preview 빌드 설치 후 익명 게스트로 Mode 1+3 완주 + 결과 영상 재생 확인"
        location: "iOS 실기기 (belle)"

must_haves:
  truths:
    - "eas.json preview 프로필이 production 과 동일한 env 블록(Firebase config + EXPO_PUBLIC_API_BASE_URL)을 보유한다"
    - "EAS preview 빌드가 letterSpacing SIGABRT 회귀 없이 성공하고 submit 까지 Claude-side PASS 다"
    - "presigned URL 만료/Content-Type 회귀가 없다 (결과 영상 재생 가능)"
    - "belle 가 실기기에서 익명 게스트로 Mode 1+3 완주 + 결과 영상 재생을 확인한다 (Claude PASS 이후 핸드오프)"
  artifacts:
    - path: "app/eas.json"
      provides: "preview 프로필 env 블록 (production mirror)"
      contains: "EXPO_PUBLIC_API_BASE_URL"
    - path: ".planning/phases/15-mode-1-mode-3-testflight/15-DELIV-EVIDENCE.md"
      provides: "env fix diff + 빌드/submit 로그 요약 + 회귀 체크리스트 + belle 핸드오프 결과"
  key_links:
    - from: "app/eas.json preview.env"
      to: "Firebase + API base URL"
      via: "빌드 시점 EXPO_PUBLIC_* inline (Expo SDK 54)"
      pattern: "EXPO_PUBLIC_API_BASE_URL"
---

<objective>
DELIV-01(TestFlight 게스트 모드 Mode 1+3 실기기 완주)을 전달한다. SIGABRT 는 이미 source 에서 fix 됨(typography.ts:15 track=()=>0). 남은 작업: (1) eas.json preview 프로필의 env 블록 누락(숨은 블로커 — preview 에 Firebase/API config 없음)을 production env 복사로 메움, (2) EAS preview 빌드 + submit 을 Claude 가 돌려 PASS 확인, (3) presigned/Content-Type/SIGABRT 회귀 체크리스트 통과, (4) Claude-side PASS 이후 belle 에게만 사람-전용 실기기 게스트 완주 + 영상 재생 단계를 핸드오프(CONTEXT D-09, 미검증 핸드오프 금지).

Purpose: 실 release 환경(EAS preview)에서 빌드가 동작 + config 정상 + 회귀 부재를 Claude 가 먼저 증명하고, 사람만 가능한 실기기 완주만 belle 에게 넘긴다. Plan 03/04 의 실 E2E PASS 가 선행(빌드가 가리키는 백엔드가 동작함).
Output: env-fixed eas.json + 빌드/submit evidence + belle 핸드오프 결과.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-mode-1-mode-3-testflight/15-CONTEXT.md
@.planning/phases/15-mode-1-mode-3-testflight/15-RESEARCH.md
@.planning/phases/15-mode-1-mode-3-testflight/15-VALIDATION.md
@.planning/phases/15-mode-1-mode-3-testflight/15-03-SUMMARY.md
@.planning/phases/15-mode-1-mode-3-testflight/15-04-SUMMARY.md
@app/eas.json
@app/src/theme/typography.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: eas.json preview env 블록 추가 (production mirror) + SIGABRT 회귀 가드</name>
  <read_first>
    app/eas.json (preview 프로필에 env 블록 없음 — production 만 EXPO_PUBLIC_FIREBASE_* 6개 + EXPO_PUBLIC_API_BASE_URL 보유; appVersionSource remote); app/src/theme/typography.ts:15 (track=()=>0 SIGABRT fix); RESEARCH §Runtime State Inventory Build artifacts + Pitfall 3 (preview 빌드 env 누락 → app non-functional); memory eas-build-gotchas
  </read_first>
  <action>
    app/eas.json 의 preview 프로필에 env 블록을 추가한다 — production 프로필의 env 블록(EXPO_PUBLIC_FIREBASE_API_KEY/_AUTH_DOMAIN/_PROJECT_ID/_STORAGE_BUCKET/_MESSAGING_SENDER_ID/_APP_ID + EXPO_PUBLIC_API_BASE_URL)을 그대로 복사(RESEARCH Pitfall 3). EXPO_PUBLIC_* 는 빌드 시점 inline 되므로(Expo SDK 54) env 없으면 Firebase/API 미설정으로 앱 비동작. preview channel/distribution 등 기존 키는 보존. 동시에 typography.ts:15 의 letterSpacing SIGABRT fix(track=()=>0)가 회귀하지 않았는지 grep 으로 확인 — letterSpacing 에 음수 size 곱셈 패턴이 재도입되지 않았는지(memory). tsc --noEmit clean 유지. 브랜드 컬러/spacing 하드코딩 금지(theme 토큰만 — CLAUDE.md).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && node -e "const e=require('./eas.json'); const p=e.build.preview.env; ['EXPO_PUBLIC_API_BASE_URL','EXPO_PUBLIC_FIREBASE_PROJECT_ID'].forEach(k=>{if(!p||!p[k])throw new Error('missing '+k)}); console.log('preview env OK')" && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    eas.json preview.env 에 6 Firebase 키 + EXPO_PUBLIC_API_BASE_URL 존재(production 과 동일 값); preview channel/distribution 보존; typography.ts:15 track=()=>0 유지(SIGABRT 회귀 가드 — letterSpacing 음수 곱셈 패턴 0); tsc --noEmit clean
  </acceptance_criteria>
  <done>preview 프로필이 production 과 동일한 env 를 갖고 SIGABRT fix 가 회귀하지 않았으며 tsc clean 이다.</done>
</task>

<task type="auto">
  <name>Task 2: EAS preview 빌드 + submit (Claude-side PASS) + 회귀 체크리스트</name>
  <read_first>
    app/eas.json (preview 프로필 + submit production ascAppId 6772934567); RESEARCH §Don't Hand-Roll (EAS Submit ASC API key 무인 submit, A4); CONTEXT D-08/D-10 (preview 빌드 경로 + 회귀 체크리스트 presigned/Content-Type/SIGABRT); memory asc-app-id-and-api-key (submit 백그라운드 OK), eas-submit-needs-stdin (해제됨), s3-presigned-video-playback
  </read_first>
  <action>
    Claude 가 EAS preview 빌드를 실행(eas build --profile preview --platform ios)하고 빌드 성공 + 아티팩트 존재를 확인한다. ASC API key 무인 submit(eas submit) 으로 TestFlight 에 올린다(memory asc-app-id-and-api-key — 백그라운드 OK, A4 재확인). 빌드 로그에서 SIGABRT/native crash 부재 확인. 회귀 체크리스트(CONTEXT D-10): (1) presigned URL 만료 — POST /playback-url refresh path 존재 확인(7일 TTL, memory s3-presigned-video-playback), (2) S3 PUT Content-Type 명시(octet-stream 방지 — Plan 01 upload 스크립트 + 앱 api.ts 경로), (3) letterSpacing SIGABRT 회귀 0. 결과(빌드 id/submit 상태/회귀 체크 표)를 15-DELIV-EVIDENCE.md 에 기록. 빌드·submit 이 PASS 가 아니면 belle 핸드오프 금지(D-09 미검증 핸드오프 금지). belle Apple Developer 계정 권한이 필요한 자격 단계가 막히면 그 지점만 belle 에게 알린다.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && test -f .planning/phases/15-mode-1-mode-3-testflight/15-DELIV-EVIDENCE.md && grep -q "preview" .planning/phases/15-mode-1-mode-3-testflight/15-DELIV-EVIDENCE.md && echo "evidence recorded"</automated>
  </verify>
  <acceptance_criteria>
    EAS preview 빌드 성공 + 아티팩트 존재; TestFlight submit PASS(무인 ASC API key); 빌드 로그 SIGABRT/native crash 0; 회귀 체크리스트 3항목(presigned refresh path 존재 / Content-Type 명시 / SIGABRT 회귀 0) PASS; 15-DELIV-EVIDENCE.md 에 빌드 id/submit 상태/회귀 표 기록
  </acceptance_criteria>
  <done>EAS preview 빌드+submit 이 Claude-side PASS 이고 회귀 체크리스트가 통과해 belle 핸드오프 준비가 끝났다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: belle 실기기 게스트 Mode 1+3 완주 + 결과 영상 재생 확인</name>
  <action>belle 가 TestFlight 로 preview 빌드를 실기기에 설치해 익명 게스트로 Mode 1+3 를 완주하고 결과 영상 재생을 확인한다. Claude-side 빌드+submit+회귀 PASS(Task 1·2) 이후에만 진행되는 사람-전용 검증이다(CONTEXT D-09 verify-before-handoff).</action>
  <what-built>Claude 가 eas.json preview env 를 고치고, EAS preview 빌드를 돌려 SIGABRT/회귀 없이 성공시키고, TestFlight submit 까지 PASS 확인했다(Task 1·2). 이제 사람만 가능한 실기기 게스트 완주 + 결과 영상 재생 확인만 남았다.</what-built>
  <read_first>
    .planning/phases/15-mode-1-mode-3-testflight/15-DELIV-EVIDENCE.md (Claude-side 빌드/submit/회귀 PASS 기록); CONTEXT D-09 (verify-before-handoff — Claude PASS 이후에만 핸드오프); ROADMAP SC5 (게스트 진입 → Mode 1+3 완주 → 결과 영상 재생, presigned/Content-Type/SIGABRT 회귀 없음)
  </read_first>
  <how-to-verify>
    1. TestFlight 에서 방금 submit 된 preview 빌드를 실기기에 설치한다.
    2. 회원가입 없이 익명 게스트로 진입한다(인트로 → 탭).
    3. Mode 1 완주 — 정은지 기준 모션 선택 → 본인 영상 업로드 → 실분석 결과 + 전문가 기준 점수 표시 확인.
    4. Mode 3 완주 — 본인 영상 2개 업로드 → "지난 분석보다 ... 개선" 절대지표 델타 표시 확인.
    5. 결과 영상 재생 확인 — 결과 화면에서 영상이 octet-stream 깨짐/만료 없이 재생되는지.
    6. 앱이 letterSpacing SIGABRT 로 튕기지 않는지(release 빌드에서만 재현되던 크래시).
  </how-to-verify>
  <resume-signal>완주 + 재생 정상이면 "approved", 문제가 있으면 어떤 단계/증상인지 기술 (gap closure 후속).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eas.json env → release 빌드 | EXPO_PUBLIC_* 빌드 시점 inline (Firebase config 노출 — 보안은 Firestore rules) |
| app → S3 presigned | 결과 영상 GET (uid-scoped, 7일 TTL) |
| EAS submit → TestFlight | ASC API key 무인 submit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.05-01 | Information Disclosure | preview env 의 Firebase config 노출 | accept | EXPO_PUBLIC_ 는 비밀 아님(클라이언트 inline 의도); 보안은 Firestore rules 가 강제(CLAUDE.md) |
| T-15.05-02 | Tampering | preview 빌드가 env 누락으로 백엔드 미연결 ship | mitigate | Task 1 preview.env 게이트 + node assert; 빌드 전 검증 |
| T-15.05-03 | Information Disclosure | presigned GET 만료/Content-Type 으로 재생 실패 또는 cross-uid 노출 | mitigate | POST /playback-url refresh + Content-Type 강제(D-10); uid-scoped 키(ASVS V4) |
| T-15.05-04 | Denial of Service | release-only SIGABRT 크래시로 게스트 완주 불가 | mitigate | typography.ts:15 fix 회귀 가드 + 빌드 로그 crash 0; 실기기 belle 확인 |
| T-15.05-05 | Spoofing | 미검증 빌드를 belle 에게 핸드오프 | mitigate | D-09 — Claude-side 빌드+submit+회귀 PASS 이후에만 핸드오프 |
| T-15.05-SC | Tampering | npm/pip/cargo installs | accept | 신규 패키지 0 (RESEARCH Package Legitimacy: skipped) |
</threat_model>

<verification>
- eas.json preview.env: 6 Firebase 키 + API base URL 존재 (node assert)
- tsc --noEmit clean + SIGABRT fix 회귀 0
- EAS preview 빌드 성공 + submit PASS + 빌드 로그 crash 0
- 회귀 체크리스트 3항목 PASS (presigned refresh / Content-Type / SIGABRT)
- belle 핸드오프: 실기기 게스트 Mode 1+3 완주 + 영상 재생 approved
</verification>

<success_criteria>
- DELIV-01: 게스트 모드 Mode 1+3 실기기 완주 + 결과 영상 재생 (ROADMAP SC5)
- eas.json preview env fix (숨은 블로커 해소)
- SIGABRT/presigned/Content-Type 회귀 부재 (D-10)
- Claude-side PASS 이후 belle 핸드오프 (D-09, verify-before-handoff)
</success_criteria>

<output>
Create `.planning/phases/15-mode-1-mode-3-testflight/15-05-SUMMARY.md` when done
</output>
