---
phase: 17-gemini-vision-integration-4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/shared/python/sunity_shared/gemini/__init__.py
  - backend/shared/python/sunity_shared/gemini/client.py
  - backend/shared/python/sunity_shared/gemini/schemas.py
  - backend/shared/python/sunity_shared/gemini/guardrails.py
  - backend/shared/python/sunity_shared/gemini/config.py
  - backend/functions/pipeline/app.py
  - backend/tests/gemini/__init__.py
  - backend/tests/gemini/test_client.py
  - backend/tests/gemini/test_schemas.py
  - backend/tests/gemini/test_guardrails.py
  - backend/tests/gemini/test_config.py
  - backend/tests/test_pipeline_vision_gate.py
autonomous: true
requirements:
  - VISION-01
  - VISION-02
  - VISION-03
  - VISION-04

must_haves:
  truths:
    - "공통 Gemini client 가 Files API 업로드 + ACTIVE 폴링 + Pydantic schema 검증 + 1회 retry 를 단일 호출 베이스로 제공한다"
    - "4 영역 Pydantic 모델 (ReferenceRegistration / CoachPayload / FindingFlags / KeypointRefinement) 이 model_validate 로 검증 가능하다"
    - "객관성 reject regex (G1) 가 좌표/점수/사람 판단 패턴을 hard fail 로 차단하며, 영역 D 만 좌표 허용 우회를 지원한다"
    - "API 키 미설정/4xx/2회 schema 실패 시 graceful return None 으로 caller 의 fallback path 가 발동된다"
    - "gemini/config.py 가 영역별 default model string (DEFAULT_A_MODEL/B/C/D + C_OVERRIDE) 을 단일 source 로 노출한다 — Plan 02/03/04/05 는 이 상수를 import 만"
    - "pipeline._gemini_vision_enabled() helper 가 Phase 17 4 영역 토글 중 하나라도 ON 이면 True 반환, keep_local_video gate = _gemini_enabled() or _gemini_vision_enabled()"
  artifacts:
    - path: "backend/shared/python/sunity_shared/gemini/client.py"
      provides: "GeminiVisionCall 베이스 (Files API + retry + guardrail)"
      contains: "class GeminiVisionCall"
    - path: "backend/shared/python/sunity_shared/gemini/schemas.py"
      provides: "4 영역 Pydantic 모델"
      contains: "class ReferenceRegistration"
    - path: "backend/shared/python/sunity_shared/gemini/guardrails.py"
      provides: "객관성 reject regex (G1) + IPSF 화이트리스트 가드 (G3) helper"
      contains: "_enforce_no_reject_patterns"
    - path: "backend/shared/python/sunity_shared/gemini/config.py"
      provides: "영역별 default model string 단일 source — DEFAULT_A_MODEL/B/C/D + C_OVERRIDE + ALLOWED_MODELS 화이트리스트"
      contains: "DEFAULT_A_MODEL"
    - path: "backend/functions/pipeline/app.py"
      provides: "_gemini_vision_enabled() helper + keep_local_video gate 확장"
      contains: "_gemini_vision_enabled"
    - path: "backend/tests/gemini/test_client.py"
      provides: "client 단위 테스트 (Files API mock)"
      contains: "def test_"
    - path: "backend/tests/gemini/test_schemas.py"
      provides: "4 영역 Pydantic 라운드트립 테스트"
      contains: "def test_"
    - path: "backend/tests/gemini/test_guardrails.py"
      provides: "객관성 reject regex hard fail 검증"
      contains: "def test_"
    - path: "backend/tests/gemini/test_config.py"
      provides: "config.py 상수 + ALLOWED_MODELS 화이트리스트 검증 (B1 정합)"
      contains: "def test_"
    - path: "backend/tests/test_pipeline_vision_gate.py"
      provides: "_gemini_vision_enabled + keep_local_video gate 4 토글 조합 테스트 (B4)"
      contains: "def test_"
  key_links:
    - from: "backend/shared/python/sunity_shared/gemini/client.py"
      to: "backend/shared/python/sunity_shared/gemini/schemas.py"
      via: "Generic[T] response_schema 주입"
      pattern: "from .schemas import"
    - from: "backend/shared/python/sunity_shared/gemini/client.py"
      to: "backend/shared/python/sunity_shared/gemini/guardrails.py"
      via: "_enforce_no_reject_patterns 호출"
      pattern: "_enforce_no_reject_patterns"
    - from: "backend/shared/python/sunity_shared/gemini/client.py"
      to: "backend/shared/python/sunity_shared/analysis/gemini_moment_extractor.py"
      via: "_load_api_key + AQ. 키 fallback 패턴 재사용"
      pattern: "_load_api_key"
---

<objective>
Phase 17 Wave 0 — 4 영역 (A reference 등록 / B 코칭 멘트 / C finding 인식 / D keypoint 보강) 이 공통으로 의존하는 Gemini Vision 호출 베이스 + Pydantic 스키마 + 객관성 guardrail 을 신설한다. AI-SPEC §3 Framework Quick Reference + §4 Implementation Guidance + §4b Async-First Design + §6 G1 guardrail 박제 패턴 그대로 코드화. 후속 plan (02~05) 이 region별 schema + prompt 만 교체해서 호출하는 단일 진입점을 제공한다.

Purpose: 후속 4 영역 plan 이 동일한 client / retry / 객관성 가드 / Pydantic 검증을 재구현하지 않도록 베이스를 박는다. 객관성 헌장 (메모리 analysis-objectivity-no-human-scores) 위반은 graceful 폴백 X, 반드시 ValueError raise — 시스템 의미 자체를 지킨다 (AI-SPEC §6 G1 "hard fail").
Output: `sunity_shared/gemini/` 신규 namespace (client.py, schemas.py, guardrails.py, __init__.py) + pytest 단위 테스트 3개.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-gemini-vision-integration-4/17-AI-SPEC.md
@.planning/phases/17-gemini-vision-integration-4/17-RESEARCH.md

# Phase 5 검증된 패턴 source — 그대로 재사용 (변경 0)
@backend/shared/python/sunity_shared/analysis/gemini_moment_extractor.py
# Phase 5 cache + recognizer 패턴 — schemas.py 의 client cache 키 설계 참조
@backend/shared/python/sunity_shared/analysis/gemini_technique_recognizer.py
# coach_writer interface — 영역 B (Wave 4) 가 따라야 할 graceful 패턴
@backend/shared/python/sunity_shared/analysis/coach_writer.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 4 영역 Pydantic schemas + 객관성 guardrail helper 정의</name>
  <files>
    backend/shared/python/sunity_shared/gemini/__init__.py,
    backend/shared/python/sunity_shared/gemini/schemas.py,
    backend/shared/python/sunity_shared/gemini/guardrails.py,
    backend/tests/gemini/__init__.py,
    backend/tests/gemini/test_schemas.py,
    backend/tests/gemini/test_guardrails.py
  </files>
  <behavior>
    - schemas.py: ReferenceRegistration(motion_name_ipsf, motion_name_ko, clip_start_seconds≥0, clip_end_seconds≥0, checkpoint_joints len 1~8, confidence 0~1) — model_validate 가 음수 clip_start 거부.
    - schemas.py: CheckpointJoint(joint_key Literal 8 entry, expectation Literal["EXTEND","BENT_OK","CONTACT"]).
    - schemas.py: CoachPayload(joints len 1~8 of JointCoaching(joint_key, detail, detail2:CoachDetail2)) — CoachDetail2.causes len 3~5 + 각 cause 가 title/explanation/fix 모두 필수.
    - schemas.py: FindingFlags(grip_visible bool, backbend_present bool, occlusion_severe bool, camera_angle_problematic bool, notes_ko max_length=200) — notes_ko 201자 거부.
    - schemas.py: KeypointRefinement(refined list[RefinedKeypoint] max_length=200) — RefinedKeypoint(frame_index≥0, x_normalized 0~1, y_normalized 0~1, confidence 0~1).
    - guardrails.py: _enforce_no_reject_patterns(text, context, allow_coords=False) — 정규식 1개라도 매치되면 ValueError raise (graceful X). 패턴: r"\d+(\.\d+)?\s*점" 단일 점수 / r"\d+\s*/\s*\d+" 분수 점수 / r"\b좌표\b|x\s*=|y\s*=" (allow_coords=True 면 좌표 패턴 skip) / r"잘했다|훌륭하다|훌륭|완벽" 사람 판단.
    - test_schemas.py: 각 schema 의 정상/경계/거부 케이스 model_validate 테스트.
    - test_guardrails.py: "이 자세는 87점" / "9/10" / "x=0.3" / "정은지는 잘했다" 가 ValueError. allow_coords=True 시 "x=0.3" 만 통과.
  </behavior>
  <action>
    AI-SPEC §4b "Structured Outputs with Pydantic" 의 4 영역 schema 정의를 backend/shared/python/sunity_shared/gemini/schemas.py 로 그대로 박는다 — Field description 한국어 박제 + Literal/min_length/max_length 제약 그대로. CheckpointJoint.joint_key Literal 은 RTMW COCO-17 의 8 관절 (left/right Elbow/Shoulder/Hip/Knee) — analysis/skeleton.py JOINT_KEYS 와 일치 확인 후 박제. guardrails.py 는 backend/shared/python/sunity_shared/analysis/gemini_moment_extractor.py L143~177 의 reject regex 박제 패턴을 옮겨오되, `allow_coords: bool=False` kwarg 추가 (영역 D 우회용 — AI-SPEC §6 G1 "영역 D 만 좌표 허용" 정합). 객관성 reject 는 graceful return X — ValueError raise 박제 ([[analysis-objectivity-no-human-scores]] 헌장 위반은 fallback 으로 우회 금지). __init__.py 는 `from .schemas import ReferenceRegistration, CoachPayload, FindingFlags, KeypointRefinement` + `from .guardrails import _enforce_no_reject_patterns` 박제. 테스트 파일 3개: pytest 기준, mock 불필요 (pure pydantic + regex).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/gemini/test_schemas.py tests/gemini/test_guardrails.py -x -q</automated>
  </verify>
  <done>4 영역 Pydantic schema 의 정상/거부 케이스 + 객관성 reject regex 의 hard fail 케이스 (점수/좌표/판단 어휘 4종) + allow_coords 우회 케이스가 모두 통과. test_schemas.py / test_guardrails.py 합쳐 ≥ 10개 테스트.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GeminiVisionCall 베이스 클라이언트 + Files API 폴링 + retry</name>
  <files>
    backend/shared/python/sunity_shared/gemini/client.py,
    backend/tests/gemini/test_client.py
  </files>
  <behavior>
    - GeminiVisionCall(schema, model, prompt, temperature=0.1, max_output_tokens=2048, thinking_budget=None, enforce_object_guard=True, api_key_loader=None).call(video_path) → schema 인스턴스 | None.
    - api_key 없으면 graceful return None (log.warning, raise X) — coach_writer.CerebrasCoachWriter 정합.
    - Files API: upload → state=PROCESSING 동안 2.0s 폴링 → ACTIVE 면 진행, FAILED 면 return None, 120s 초과 시 return None.
    - generate_content config: response_mime_type="application/json", response_schema=schema, temperature, max_output_tokens, http_options timeout=60_000ms. thinking_budget is not None 이면 types.ThinkingConfig(thinking_budget=...) 박제.
    - retry: ValidationError/JSONDecodeError 1회 재시도. APIError 5xx 1회 재시도 + 1초 backoff. 4xx 즉시 graceful return None.
    - enforce_object_guard=True 면 raw_text 에 _enforce_no_reject_patterns(text, context=model, allow_coords=False) 호출 — match 시 ValueError 그대로 raise (caller 가 hard fail 인지).
    - response.parsed 가 None 이면 schema.model_validate(json.loads(response.text)) 폴백.
    - test_client.py: monkeypatch 로 genai.Client / files.upload / files.get / models.generate_content stub. 케이스: (1) ACTIVE + parsed → 인스턴스, (2) PROCESSING 5회 → ACTIVE 성공, (3) FAILED → None, (4) timeout > 120s → None, (5) parsed=None + text JSON → model_validate 폴백, (6) ValidationError 2회 → None, (7) APIError 500 → 1회 retry 후 성공, (8) APIError 401 → 즉시 None, (9) reject regex 매치 → ValueError (graceful X), (10) api_key_loader RuntimeError → None.
  </behavior>
  <action>
    AI-SPEC §4 Core Pattern 의 GeminiVisionCall 데이터클래스 박제를 backend/shared/python/sunity_shared/gemini/client.py 에 박는다. Generic[T] (T=BaseModel) 로 typed return. _load_api_key 함수는 backend/shared/python/sunity_shared/analysis/gemini_moment_extractor.py 의 패턴 (env GEMINI_API_KEY → AWS Parameter Store CEREBRAS_KEY_PARAM 자리의 Gemini 키 fallback) 을 import 또는 재정의 — 중복 피하려면 from sunity_shared.analysis.gemini_moment_extractor import _load_api_key 박제. Files API mime_type 은 호출 시점에 video_path 확장자로 결정 ("video/mp4" 기본, .mov 면 "video/quicktime"). retry 루프는 attempt in (1, 2) for loop, 2회 모두 실패 시 return None. APIError 4xx 판별: `exc.code` 가 400~499 → 즉시 return None, 500~599 → attempt==1 일 때만 retry. test_client.py 는 monkeypatch.setattr 로 genai.Client 인스턴스의 files/models 어트리뷰트를 Mock 으로 치환 — 외부 API 콜 0. boto3 / 네트워크 의존 0.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/gemini/test_client.py -x -q</automated>
  </verify>
  <done>10개 client 테스트 케이스 (Files API state machine, retry, guardrail hard fail, graceful skip) 전체 통과. 외부 네트워크 호출 0 — 전부 mock. `python -c "from sunity_shared.gemini import GeminiVisionCall, ReferenceRegistration, FindingFlags"` import 성공.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: gemini/config.py 단일 source + ALLOWED_MODELS 화이트리스트 (B1 정합)</name>
  <files>
    backend/shared/python/sunity_shared/gemini/config.py,
    backend/tests/gemini/test_config.py
  </files>
  <behavior>
    - config.py 가 모듈 상수 5개 export: `DEFAULT_A_MODEL = "gemini-3.1-pro-preview"`, `DEFAULT_B_MODEL = "gemini-3.1-pro-preview"`, `DEFAULT_C_MODEL = "gemini-3.5-flash"`, `DEFAULT_C_MODEL_OVERRIDE = "gemini-3.1-pro-preview"`, `DEFAULT_D_MODEL = "gemini-3.1-pro-preview"` — AI-SPEC §4 Model Configuration 표 1:1 정합.
    - `ALLOWED_MODELS = frozenset({"gemini-3.1-pro-preview", "gemini-3.5-flash"})` — 배포 전 smoke 가드 (Codex review B1 권장: 404 silent fallback 차단).
    - `def resolve_model(region: Literal["A","B","C","D"], env_override: str | None = None) -> str` — env 우선, 없으면 default 반환. 반환 string 이 ALLOWED_MODELS 에 없으면 ValueError raise (deprecated 2.5 / typo 차단).
    - `__init__.py` 가 `from .config import resolve_model, ALLOWED_MODELS, DEFAULT_A_MODEL, DEFAULT_B_MODEL, DEFAULT_C_MODEL, DEFAULT_C_MODEL_OVERRIDE, DEFAULT_D_MODEL` 박제.
    - test_config.py: (1) DEFAULT_* 5개 string 박제 회귀 — Phase 17 외 raw string 박는 거 금지의 게이트, (2) ALLOWED_MODELS 멤버 정확히 2개 (preview suffix Pro + flash), (3) resolve_model("A") → DEFAULT_A_MODEL, (4) resolve_model("C", env_override="gemini-3.5-flash") → "gemini-3.5-flash", (5) resolve_model("A", env_override="gemini-2.5-pro") → ValueError (deprecated 2.5 차단), (6) resolve_model("A", env_override="gemini-3.1-pro") → ValueError (plain Pro 차단 — Codex B1 정확히 잡은 case), (7) resolve_model("X") → ValueError (잘못된 region).
  </behavior>
  <action>
    backend/shared/python/sunity_shared/gemini/config.py 신설. `from __future__ import annotations`, `from typing import Literal`. 상수 5개 + ALLOWED_MODELS frozenset + resolve_model 함수. 함수 body 는 ~15 줄 (region/env_override 검증 + ALLOWED_MODELS 화이트리스트 검증). 라이브러리 의존 0 — pure Python. test_config.py 는 monkeypatch 불필요 — pure 함수. Plan 02/03/04/05 의 model env default 박제는 후속 plan 패치 시 `gemini_config.DEFAULT_X_MODEL` 또는 `resolve_model("X", env_override=os.environ.get("GEMINI_X_MODEL"))` 호출로 단일 source 정합 (raw string 박제 0).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/gemini/test_config.py -x -q</automated>
  </verify>
  <done>7 케이스 테스트 통과 — 정상 default + env override + 2.5/plain Pro/잘못된 region 모두 ValueError. `grep -rn "gemini-3\.1-pro[^-]" backend/shared/python/sunity_shared/gemini/` (plain Pro 박제) 결과 0건. `python -c "from sunity_shared.gemini import resolve_model; assert resolve_model('A') == 'gemini-3.1-pro-preview'"` 통과.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: pipeline._gemini_vision_enabled() + keep_local_video gate 확장 (B4 정합)</name>
  <files>
    backend/functions/pipeline/app.py,
    backend/tests/test_pipeline_vision_gate.py
  </files>
  <behavior>
    - pipeline/app.py 에 `_gemini_vision_enabled() -> bool` helper 추가 — 다음 env 중 하나라도 truthy 면 True: `GEMINI_REFERENCE_ENABLED` (영역 A), `GEMINI_COACH_ENABLED` (영역 B, default "1"), `GEMINI_FINDING_ENABLED` (영역 C, default "1"), `GEMINI_D_ENABLED` (영역 D). truthy 판정 = `not in ("0", "false", "False", "")`.
    - `_extract_video_analysis_inputs` 호출 라인 (`pipeline/app.py:889-890`) 의 `keep_local_video=_gemini_enabled()` → `keep_local_video=_gemini_enabled() or _gemini_vision_enabled()` 로 확장.
    - 기존 `_gemini_enabled()` (Phase 5 recognizer 전용) 시그니처/동작 변경 0 — backward compat.
    - test_pipeline_vision_gate.py: (1) 4 영역 토글 모두 off + recognizer off → keep_local_video=False, (2) recognizer only on → True (기존 동작 회귀), (3) GEMINI_COACH_ENABLED=1 only → True, (4) GEMINI_D_ENABLED=1 only → True, (5) 모두 off (B/C default "1" override) GEMINI_COACH_ENABLED=0 + GEMINI_FINDING_ENABLED=0 + 나머지 미설정 → False, (6) `_gemini_enabled` mock 검증 — 호출 0 시점에 `_gemini_vision_enabled` 도 호출 0 (단락 평가는 X, OR 양쪽 다 evaluate OK).
  </behavior>
  <action>
    backend/functions/pipeline/app.py 의 `_gemini_enabled()` 정의 (라인 175) 바로 아래에 `_gemini_vision_enabled()` 추가. 4 env 변수 (`GEMINI_REFERENCE_ENABLED`, `GEMINI_COACH_ENABLED`, `GEMINI_FINDING_ENABLED`, `GEMINI_D_ENABLED`) 박제 — B/C 는 default "1" (Plan 02/04 정합). os.environ.get 4회 + truthy 판정 헬퍼 1줄. `_extract_video_analysis_inputs` 호출 라인 (현재 라인 889~890) 의 `keep_local_video` 인자 한 줄 수정. test_pipeline_vision_gate.py 는 monkeypatch.setenv / delenv 로 6 조합 검증 — Pod/Lambda runtime 0. Plan 02/03/04 는 이 task 가 박은 _gemini_vision_enabled 를 가정 (후속 plan 의 wave 안에서 "S3 download/RTMW 재실행 금지" 게이트는 `local_video_path is None` 검사로 graceful skip — 별도 helper 없이 표준 None 가드).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_pipeline_vision_gate.py -x -q</automated>
  </verify>
  <done>6 케이스 토글 조합 테스트 통과. `grep -n "_gemini_vision_enabled" backend/functions/pipeline/app.py | grep -v '^#' | grep -c .` ≥ 2 (정의 + 호출). `grep -n "keep_local_video=_gemini_enabled() or _gemini_vision_enabled()" backend/functions/pipeline/app.py | grep -c .` == 1. 기존 `_gemini_enabled` 테스트 회귀 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| video_path → Gemini Files API | 사용자/reference 영상 파일이 Google Gemini API 에 업로드. 신체 노출 콘텐츠 포함 가능 (AI-SPEC §1b "신체 노출 영상"). |
| Gemini response → Pydantic parse | LLM 출력이 신뢰 경계 — 환각/객관성 위반/schema 위반 가능. |
| api_key → genai.Client | API 키 (AQ.xxx) 가 env / Parameter Store 에서 client 로 전달. 로그/예외 메시지에 누출 금지. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | Gemini response → Pydantic | mitigate | response_schema=Pydantic 강제 + model_validate 2차 검증 + ValidationError → graceful None. (AI-SPEC §4b Pydantic) |
| T-17-02 | Information Disclosure | API key 로깅 | mitigate | log.warning/exception 메시지에 api_key 변수 절대 박제 금지. _load_api_key fallback 시 키 값 echo X (기존 gemini_moment_extractor.py 패턴 정합). |
| T-17-03 | Repudiation | 객관성 가드 우회 (점수/판단 출력) | mitigate | G1 reject regex hard fail (ValueError raise) — graceful fallback X. allow_coords=True 는 영역 D 전용 + 명시 opt-in. |
| T-17-04 | Denial of Service | Files API timeout / 5xx flood | mitigate | 120s upper bound (Files API 폴링) + APIError 5xx 1회 retry only + 4xx 즉시 graceful None. 무한 루프 0. |
| T-17-05 | Spoofing | api_key_loader 우회 | accept | 동일 process 내 함수 주입 — Lambda/Pod 내부 신뢰. 외부 공격 surface 없음. |
| T-17-SC | Tampering | pip installs (google-genai, pydantic) | mitigate | google-genai>=1.0,<2.0 + pydantic>=2.5,<3.0 — 이미 backend/runpod_inference/requirements.txt 에 박혀 있음. 신규 install X. RESEARCH §2 + AI-SPEC §3 정합. |
</threat_model>

<verification>
- pytest backend/tests/gemini/ backend/tests/test_pipeline_vision_gate.py -x -q 전체 통과 (≥ 10개 schema + ≥ 4개 guardrail + ≥ 10개 client + ≥ 7개 config + ≥ 6개 vision gate 테스트).
- `python -c "from sunity_shared.gemini import GeminiVisionCall, ReferenceRegistration, CoachPayload, FindingFlags, KeypointRefinement, _enforce_no_reject_patterns, resolve_model, ALLOWED_MODELS"` import 성공.
- 어떤 테스트도 외부 네트워크 (Gemini API / S3) 호출 0 — `grep -rn "genai.Client(api_key=os" backend/tests/gemini/ | grep -v '^#' | grep -c .` == 0 (monkeypatch 만 사용).
- 객관성 reject regex hard fail 회귀: test_guardrails.py 의 "점수/좌표/판단" 4종 케이스 전부 ValueError raise 확인 (`pytest.raises(ValueError)`).
- B1 plain model string 박제 0 회귀: `grep -rn "gemini-3\.1-pro[^-]" backend/shared/python/sunity_shared/gemini/ backend/functions/ | grep -v "# " | grep -c .` == 0 (`gemini-3.1-pro-preview` 만 허용).
- B4 vision gate 회귀: 기존 `_gemini_enabled()` 단독 테스트 6개 모두 통과 + `_gemini_vision_enabled()` 추가 6 조합 모두 통과.
</verification>

<success_criteria>
1. backend/shared/python/sunity_shared/gemini/ 디렉토리 신설 — __init__.py + client.py + schemas.py + guardrails.py + config.py 5개 파일.
2. backend/tests/gemini/ pytest 디렉토리 신설 — test_client.py + test_schemas.py + test_guardrails.py + test_config.py + (root) test_pipeline_vision_gate.py.
3. GeminiVisionCall.call(video_path) 의 모든 graceful 폴백 분기 (api_key 미설정 / Files API FAILED / Files API timeout / APIError 4xx / ValidationError 2회 / APIError 5xx 2회) 가 None 반환. enforce_object_guard=True 의 reject regex 매치만 ValueError raise.
4. 4 영역 Pydantic schema 가 AI-SPEC §4b 박제와 1:1 매치 (필드명/제약/Literal/Field description) — 후속 plan 이 schema 재정의 0.
5. 좌표 우회 (allow_coords=True) 가 영역 D 호출만 활성 — 영역 A/B/C 호출 시 좌표 출력은 ValueError hard fail.
6. (B1) gemini/config.py 가 영역별 default model 상수 + ALLOWED_MODELS 화이트리스트 + resolve_model 함수 export. raw model string 박제는 config.py 안에서만. Plan 02/03/04/05 는 이 상수만 import.
7. (B4) pipeline._gemini_vision_enabled() helper 존재 + `keep_local_video = _gemini_enabled() or _gemini_vision_enabled()` 갱신. Phase 17 4 영역 토글 중 하나만 ON 이어도 local_video_path 보존.
</success_criteria>

<output>
Create `.planning/phases/17-gemini-vision-integration-4/17-01-SUMMARY.md` when done.
</output>
