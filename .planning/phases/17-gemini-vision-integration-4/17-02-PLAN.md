---
phase: 17-gemini-vision-integration-4
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - backend/shared/python/sunity_shared/gemini/scene_finder.py
  - backend/shared/python/sunity_shared/firestore_admin.py
  - backend/functions/pipeline/app.py
  - backend/tests/gemini/test_scene_finder.py
  - backend/tests/test_pipeline_geminic_wiring.py
autonomous: true
requirements:
  - VISION-03

must_haves:
  truths:
    - "사용자 분석 1회당 영역 C (Finding flag) Gemini Flash 호출이 발동되어 grip_visible/backbend_present/occlusion_severe/camera_angle_problematic 4 flag 가 산출된다"
    - "정은지 reference 영상 (isReference=True) 에서 occlusion_severe=True 가 나오면 G4 guardrail 이 flag 를 폐기하고 belle 알림 + Firestore review_required=True 박는다"
    - "Pod _process 가 RTMW estimate 직후 wave 1 (영역 C) 호출을 asyncio 로 추가 호출하며, Gemini 실패 시 빈 flag (all False) 폴백으로 분석 흐름이 끊기지 않는다"
    - "Firestore users/{uid}/analyses/{id}.geminiC 필드에 4 flag + model + tokensUsed + latencyMs 가 flat object 로 박힌다 (nested array 금지 정합)"
  artifacts:
    - path: "backend/shared/python/sunity_shared/gemini/scene_finder.py"
      provides: "영역 C find_scene_flags(video_path) → FindingFlags | None"
      contains: "def find_scene_flags"
    - path: "backend/functions/pipeline/app.py"
      provides: "_process 내 wave 1 영역 C 호출 + Firestore geminiC 전달"
      contains: "find_scene_flags"
    - path: "backend/shared/python/sunity_shared/firestore_admin.py"
      provides: "complete_analysis 가 geminiC kwarg 수용"
      contains: "gemini_c"
    - path: "backend/tests/gemini/test_scene_finder.py"
      provides: "영역 C 호출 + G4 guardrail 단위 테스트"
      contains: "def test_"
    - path: "backend/tests/test_pipeline_geminic_wiring.py"
      provides: "_process wave 1 wiring + 폴백 통합 테스트"
      contains: "def test_"
  key_links:
    - from: "backend/shared/python/sunity_shared/gemini/scene_finder.py"
      to: "backend/shared/python/sunity_shared/gemini/client.py"
      via: "GeminiVisionCall(schema=FindingFlags, model=GEMINI_C_MODEL) 인스턴스화"
      pattern: "GeminiVisionCall"
    - from: "backend/functions/pipeline/app.py"
      to: "backend/shared/python/sunity_shared/gemini/scene_finder.py"
      via: "find_scene_flags 호출 (asyncio.gather wave 1)"
      pattern: "find_scene_flags"
    - from: "backend/functions/pipeline/app.py"
      to: "backend/shared/python/sunity_shared/firestore_admin.py"
      via: "complete_analysis(..., gemini_c=...)"
      pattern: "gemini_c"
---

<objective>
Phase 17 Wave 1 (RESEARCH §9 표기) — 영역 C Finding 장면 인식 (그립/백벤드/occlusion/카메라 angle) 을 Gemini 3.5 Flash 로 호출하고, Pod `_process` 의 RTMW estimate 직후에 추가 wave 로 박는다. Firestore `geminiC` 필드 박제 + G4 정은지 영상 occlusion_severe FP 가드 + graceful 폴백. AI-SPEC §5 E5 (정확도 ≥ 0.95) + E6 (정은지 영상 FP=0) + §6 G4 (정은지 영상 occlusion_severe block) 정합.

Purpose: 영역 C 는 4 영역 중 가장 단순한 boolean flag 추출 (latency 우선, Flash 충분, 빈도 100%). 후속 plan (D 영역) 이 occlusion_severe=True 시 skip 하는 게이트로도 쓰임. PROJECT.md "정은지 영상 41점 같은 위양성 없이" 직격 방지 — G4 가드 우선 박제.
Output: scene_finder.py 모듈 + pipeline _process 의 wave 1 wiring + Firestore geminiC schema + 단위/통합 테스트.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-gemini-vision-integration-4/17-AI-SPEC.md
@.planning/phases/17-gemini-vision-integration-4/17-RESEARCH.md
@.planning/phases/17-gemini-vision-integration-4/17-01-SUMMARY.md

# Pod 진입점 + _process 위치
@backend/functions/pipeline/app.py
# Firestore complete_analysis 시그너처 (kwarg 확장 대상)
@backend/shared/python/sunity_shared/firestore_admin.py
# RunPod FastAPI 진입점 — _process 가 BackgroundTask 로 호출되는 위치
@backend/runpod_inference/server.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: find_scene_flags 모듈 + G4 정은지 영상 가드 + Firestore geminiC schema</name>
  <files>
    backend/shared/python/sunity_shared/gemini/scene_finder.py,
    backend/shared/python/sunity_shared/firestore_admin.py,
    backend/tests/gemini/test_scene_finder.py
  </files>
  <behavior>
    - find_scene_flags(video_path, is_reference=False) → dict | None — 성공 시 {grip_visible, backbend_present, occlusion_severe, camera_angle_problematic, notes_ko, model, tokens_used, latency_ms}, 실패 시 None.
    - Model: env GEMINI_C_MODEL (default "gemini-3.5-flash"), GEMINI_C_MODEL_OVERRIDE 있으면 우선 (E5 auto-escalation gate).
    - GeminiVisionCall(schema=FindingFlags, model=..., prompt=한국어 prompt, temperature=0.0, max_output_tokens=512, thinking_budget=0, enforce_object_guard=True) 인스턴스화 → call(video_path).
    - G4 가드: is_reference=True + occlusion_severe=True 동시 시 → flag 전체 폐기 (4 flag 모두 False 박제) + return dict 에 guardrail_triggered="G4_reference_occlusion_fp" 박는다. PROJECT.md "정은지 영상 위양성" 직격.
    - Graceful: call() 이 None 이면 빈 flag (all False, model=..., guardrail_triggered=None, error="api_or_schema_fail") 반환 — caller 가 분석 흐름 끊지 않도록.
    - firestore_admin.complete_analysis 에 gemini_c: dict | None = None kwarg 추가. None 이면 Firestore 박지 않음, dict 면 analysis doc 의 `geminiC` 필드로 박제 (flat object).
    - test_scene_finder.py: (1) Flash 호출 성공 + 4 flag 정상 → dict 반환, (2) is_reference=True + occlusion_severe=True → G4 가드 발동 + guardrail_triggered 박힘, (3) call None → 빈 flag dict, (4) env GEMINI_C_MODEL_OVERRIDE 박힘 → override model 사용 확인.
  </behavior>
  <action>
    backend/shared/python/sunity_shared/gemini/scene_finder.py 신설. Prompt 박제 (한국어, 시스템 역할 + 4 flag 정의 + 객관성 헌장 명시: "좌표/점수/사람 판단 금지"). AI-SPEC §1b "What Domain Experts Evaluate Against" 4번째 항목의 정의를 prompt 에 직접 박는다 — 백벤드=등 30도 이상 후굴 1초+, occlusion=신체 50%+ 가림 1초+, camera_angle=측면/완전 뒤집힘 RTMW 좌우 혼선 각도. notes_ko 는 짧은 근거 1~2문장. GeminiVisionCall 의 call() 시 latency 측정 (time.monotonic) + parsed.model_dump() 결과에 model/tokens_used/latency_ms 첨가. tokens_used 는 response.usage_metadata.total_token_count (SDK 1.x) 박제 — AI-SPEC §7 M2 source. firestore_admin.complete_analysis 확장은 함수 정의 + Firestore update_data 에 `if gemini_c is not None: update_data["geminiC"] = gemini_c` 박제. nested array 금지 정합 — geminiC 는 flat object (4 flag + meta scalar). 테스트는 monkeypatch 로 GeminiVisionCall.call 을 stub.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/gemini/test_scene_finder.py -x -q</automated>
  </verify>
  <done>find_scene_flags 의 정상/None/G4 guardrail/env override 4 케이스 테스트 통과. complete_analysis(gemini_c={...}) 호출 시 Firestore mock 의 update_data 가 `geminiC` 키 포함 (mock 단위로 검증 가능).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: pipeline _process 에 wave 1 (영역 C) async 호출 + Firestore wiring 통합 테스트</name>
  <files>
    backend/functions/pipeline/app.py,
    backend/tests/test_pipeline_geminic_wiring.py
  </files>
  <behavior>
    - _process(uid, analysis_id, ..., local_video_path, is_reference) 의 RTMW estimate (`_RTMWNlfCompat.estimate` 또는 `_POSE_ESTIMATOR.estimate_with_profile`) 직후 + KISMAM/DTW 직전에 wave 1 호출 추가.
    - **B4 정합 — env gate**: `GEMINI_FINDING_ENABLED` env (default "1") — "0"/"false" 면 wave 1 skip. Plan 01 Task 4 의 `_gemini_vision_enabled()` 가 이 env 를 OR 항목으로 포함 → keep_local_video=True 보장 (local_video_path is None 위험 차단).
    - **B4 정합 — S3 재다운로드 / RTMW 재실행 금지 (hard gate)**: wave 1 은 caller 가 이미 박은 `local_video_path` (Phase 6 의 `_extract_video_analysis_inputs` 가 1회만 다운로드 + RTMW 1회만 실행) 만 사용. find_scene_flags 안에서 boto3 S3 download 호출 0건, `_POSE_ESTIMATOR.estimate` 재호출 0건. local_video_path is None 이면 즉시 wave 1 skip + geminiC=None (분석 흐름 차단 X).
    - asyncio.run(_call_wave1(local_video_path, is_reference)) 가 새 helper. _process 가 sync 컨텍스트면 asyncio.run, async 컨텍스트면 await — Pod RunPod FastAPI BackgroundTask 가 sync run → asyncio.run 박제.
    - _call_wave1 내부에서 sunity_shared.gemini.scene_finder.find_scene_flags 호출 (sync). 향후 wave 2 (B/D) 와 병렬화 위해 asyncio 베이스 박제.
    - Gemini 호출 실패 시 (find_scene_flags 가 빈 flag dict 반환) → 분석 흐름 계속, geminiC 는 그대로 dict 박제 (error 필드로 추적 가능).
    - firestore_admin.complete_analysis 호출 시 gemini_c=scene_result 전달.
    - is_reference 판별: S3 key 가 `reference/` prefix 또는 analysis doc 의 mode=="mode1_register" 시 True. 기존 _process 시그너처가 is_reference 를 받지 않으면 추가 — analysis_id 로 Firestore 조회 또는 S3 key 패턴 검사.
    - test_pipeline_geminic_wiring.py: (1) _process 가 find_scene_flags 를 1회 호출하는지 (monkeypatch 로 stub), (2) scene_result 가 None 일 때도 complete_analysis 가 정상 호출되는지, (3) is_reference=True + occlusion_severe=True 인 stub 결과가 G4 가드 거쳐 flag 폐기된 채 박히는지, (4) complete_analysis 가 gemini_c kwarg 받는지, (5) **B4 회귀**: GEMINI_FINDING_ENABLED=0 → find_scene_flags 호출 0 + geminiC=None, (6) **B4 회귀**: local_video_path=None → find_scene_flags 호출 0, (7) **B4 회귀**: find_scene_flags mock 내부에서 boto3.client/S3 download / `_POSE_ESTIMATOR.estimate` mock 호출 0건 (S3 재다운로드 / RTMW 재실행 금지).
  </behavior>
  <action>
    backend/functions/pipeline/app.py 의 `_process` 함수를 grep 으로 찾아 (L156 부근, RESEARCH §12 박제) RTMW estimate 다음 라인에 영역 C 호출 박제. import 추가: `from sunity_shared.gemini.scene_finder import find_scene_flags`. is_reference 판별 로직: 기존 _process 호출자가 S3 key 또는 analysis mode 를 알고 있으면 kwarg 로 박제 — 호출 path 가 SQS event 또는 RunPod /analyze 두 곳이라 둘 다 갱신. RunPod path 는 backend/runpod_inference/server.py 의 _process 호출 부분에 is_reference 박제 (request body 의 mode 또는 S3 key 패턴). Lambda SQS path 는 firestore_admin 으로 analysis doc 조회 후 mode 확인 또는 S3 key 의 `reference/` prefix 검사. 통합 테스트는 monkeypatch 로 find_scene_flags + _RTMWNlfCompat + complete_analysis 전부 stub — 실 GPU/AWS 호출 0. assertEqual 로 호출 횟수/kwarg 검증. 박제 — wave 1 의 latency 추가는 Flash 2~5s (AI-SPEC §4b), pipeline Lambda 900s timeout 정합.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_pipeline_geminic_wiring.py -x -q</automated>
  </verify>
  <done>통합 테스트 4 케이스 통과: (1) find_scene_flags 1회 호출, (2) None 결과 시 분석 흐름 차단 X, (3) G4 가드 동작, (4) complete_analysis 가 gemini_c 박제. backend/functions/pipeline/app.py 의 _process 안에 `find_scene_flags` 호출 라인 존재.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사용자 영상 → 영역 C Gemini | 사용자 영상이 Gemini Flash 로 업로드. Files API 48h 자동 만료 (AI-SPEC §1b). |
| 영역 C 출력 → Firestore geminiC 필드 | LLM 출력 (4 flag + notes_ko) 이 Firestore 에 박힘. 사용자에게 직접 노출되진 않지만 후속 분석 게이팅 (D 영역 skip, 채점 path 분기) 에 사용. |
| is_reference flag → G4 guardrail | reference 여부 판별 자체가 신뢰 경계. S3 key/analysis mode 어디서 박혀오는지 명확해야 G4 우회 방지. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-06 | Tampering | Gemini occlusion_severe FP on reference | mitigate | G4 가드 — is_reference=True + occlusion_severe=True 동시 시 flag 폐기 + belle 알림 + Firestore review_required=True. PROJECT.md "정은지 영상 위양성" 직격. |
| T-17-07 | Information Disclosure | notes_ko 에 점수/판단 어휘 | mitigate | enforce_object_guard=True (Plan 01 G1 reject regex) — match 시 ValueError. graceful 폴백 X. |
| T-17-08 | Denial of Service | 영역 C 호출 실패가 분석 흐름 차단 | mitigate | find_scene_flags 가 None 대신 빈 flag dict 반환 — _process 가 wave 1 결과 무관 흐름 진행. |
| T-17-09 | Spoofing | is_reference 판별 우회 (일반 영상이 reference 로 박힘) | mitigate | is_reference 는 analysis doc.mode 또는 S3 key prefix 둘 다 검사 — 단일 source 신뢰 X. analysis doc 은 Firebase Admin SDK 인증 통과한 path 로만 박힘. |
| T-17-10 | Repudiation | 영역 C 결과 audit | accept | Firestore geminiC.model + latencyMs + tokensUsed 박제 — Phoenix trace 와 join 가능 (Plan 06 에서 본격 박제). |
</threat_model>

<verification>
- pytest backend/tests/gemini/test_scene_finder.py backend/tests/test_pipeline_geminic_wiring.py -x -q 통과.
- `grep -n "find_scene_flags" backend/functions/pipeline/app.py | grep -v '^#' | grep -c .` ≥ 2 (import 1 + 호출 1).
- `grep -n "gemini_c" backend/shared/python/sunity_shared/firestore_admin.py | grep -v '^#' | grep -c .` ≥ 1 (kwarg 시그너처).
- G4 가드 회귀: test_scene_finder.py 의 "is_reference=True + occlusion_severe=True" 케이스가 guardrail_triggered 필드 박힌 dict 반환 — pytest assert 통과.
</verification>

<success_criteria>
1. sunity_shared/gemini/scene_finder.py 신설 — find_scene_flags 함수 export.
2. backend/functions/pipeline/app.py 의 _process 안에 영역 C 호출 wave 가 RTMW estimate 직후 + KISMAM/DTW 직전 위치에 박힘. import 추가 확인.
3. firestore_admin.complete_analysis 가 gemini_c kwarg 받아 Firestore `geminiC` 필드 박는다 (flat object, nested array 0).
4. G4 guardrail 발동 — is_reference=True + occlusion_severe=True 시 flag 전체 폐기 + guardrail_triggered 박힘.
5. Gemini API 실패/None 시 분석 흐름 차단 0 — _process 가 wave 1 결과 무관 진행.
</success_criteria>

<output>
Create `.planning/phases/17-gemini-vision-integration-4/17-02-SUMMARY.md` when done.
</output>
