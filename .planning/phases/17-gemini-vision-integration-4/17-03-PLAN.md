---
phase: 17-gemini-vision-integration-4
plan: 03
type: execute
wave: 3
depends_on:
  - 17-01
  - 17-02
files_modified:
  - backend/shared/python/sunity_shared/gemini/keypoint_augmenter.py
  - backend/functions/pipeline/app.py
  - backend/shared/python/sunity_shared/firestore_admin.py
  - backend/tests/gemini/test_keypoint_augmenter.py
  - backend/tests/test_pipeline_geminid_wiring.py
autonomous: true
requirements:
  - VISION-04

must_haves:
  truths:
    - "RTMW 출력 행렬에서 confidence < 0.5 frame 이 1개 이상이면 영역 D Gemini Pro 호출이 발동되어 해당 frame 의 keypoint 좌표가 정규화된 [0,1] 범위로 보강된다"
    - "보강 좌표는 RTMW 인접 frame (±2) 의 동일 joint 와 L2 거리 ≥ 0.15 이면 G5 guardrail 이 폐기하고 RTMW 원본 + interpolation 유지"
    - "영역 C 의 occlusion_severe=True 면 영역 D 는 skip — 가려진 frame 에 좌표 보강 시도 의미 없음 (AI-SPEC §4b async pattern)"
    - "영역 D 출력은 keypointReport 행렬에 직접 주입되어 후속 DTW + 8 관절각 차원 점수 계산이 보강된 좌표로 진행된다"
    - "Firestore users/{uid}/analyses/{id}.geminiD 에 augmentedFrames + originalConfidence + model + guardrail_blocked_count 가 flat 으로 박힌다"
  artifacts:
    - path: "backend/shared/python/sunity_shared/gemini/keypoint_augmenter.py"
      provides: "영역 D augment_low_confidence(video_path, low_conf_frame_indices, rtmw_array) → dict | None"
      contains: "def augment_low_confidence"
    - path: "backend/functions/pipeline/app.py"
      provides: "_process 내 wave 2 영역 D 호출 + RTMW 행렬 보강 + Firestore geminiD 전달"
      contains: "augment_low_confidence"
    - path: "backend/shared/python/sunity_shared/firestore_admin.py"
      provides: "complete_analysis 가 gemini_d kwarg 수용"
      contains: "gemini_d"
    - path: "backend/tests/gemini/test_keypoint_augmenter.py"
      provides: "영역 D 단위 테스트 + G5 좌표 환각 가드 검증"
      contains: "def test_"
    - path: "backend/tests/test_pipeline_geminid_wiring.py"
      provides: "_process wave 2 wiring + occlusion_severe skip + RTMW 행렬 주입 통합 테스트"
      contains: "def test_"
  key_links:
    - from: "backend/shared/python/sunity_shared/gemini/keypoint_augmenter.py"
      to: "backend/shared/python/sunity_shared/gemini/client.py"
      via: "GeminiVisionCall(schema=KeypointRefinement, enforce_object_guard=False) — 영역 D 만 좌표 허용"
      pattern: "enforce_object_guard=False"
    - from: "backend/functions/pipeline/app.py"
      to: "backend/shared/python/sunity_shared/gemini/keypoint_augmenter.py"
      via: "augment_low_confidence 호출 (RTMW 결과 + occlusion_severe 게이트 후)"
      pattern: "augment_low_confidence"
    - from: "backend/functions/pipeline/app.py"
      to: "rtmw_array (numpy (T, J, 4) — x, y, conf, ...)"
      via: "보강된 좌표가 rtmw_array[frame_index, joint_idx, 0:2] 에 in-place 주입 후 DTW/각도 계산 진행"
      pattern: "rtmw_array\\["
---

<objective>
Phase 17 Wave 2 (RESEARCH §9 표기) — 영역 D Keypoint 보강을 Gemini 3.1 Pro 로 호출한다. RTMW 의 약점 (inverted/twist/폐색 자세에서 confidence < 0.5 frame) 을 Gemini Vision 의 영상 이해로 보강. ROADMAP Success #4 "KeypointReport 의 data 의 0.5 미만 비율 < 5% (현 신규 6 = 13-35%)" 직격. 객관성 가드 OFF (좌표 출력 허용) — AI-SPEC §6 G1 박제 "영역 D 만 좌표 허용 (`enforce_object_guard=False`)". 단 G5 가드 (인접 frame 거리 ≥ 0.15 환각 의심) 가 hallucination 차단.

Purpose: RTMW 가 못 찾는 좌표를 Gemini 가 영상 단서 (의상/머리 방향/폴 접촉점) 로 추정 — 메모리 [[ml-pose-3d-pivot]] + [[rtmw-free-stack-pivot]] 의 RTMW 단일 백본 약점 직접 대응. PROJECT.md "고수가 낮게 나오는 위양성" 의 좌우 swap / occlusion 케이스 직격.
Output: keypoint_augmenter.py 모듈 + pipeline _process 의 wave 2 wiring + RTMW 행렬 주입 + Firestore geminiD schema + 단위/통합 테스트.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-gemini-vision-integration-4/17-AI-SPEC.md
@.planning/phases/17-gemini-vision-integration-4/17-RESEARCH.md
@.planning/phases/17-gemini-vision-integration-4/17-01-SUMMARY.md
@.planning/phases/17-gemini-vision-integration-4/17-02-SUMMARY.md

# RTMW 행렬 shape (T, J, 4) — x, y, conf
@backend/shared/python/sunity_shared/analysis/pose_estimator.py
# _process 의 RTMW estimate 위치 + scene_finder 호출 위치 (Plan 02 박은 라인)
@backend/functions/pipeline/app.py
@backend/shared/python/sunity_shared/analysis/skeleton.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: augment_low_confidence 모듈 + G5 좌표 환각 가드 + Firestore geminiD schema</name>
  <files>
    backend/shared/python/sunity_shared/gemini/keypoint_augmenter.py,
    backend/shared/python/sunity_shared/firestore_admin.py,
    backend/tests/gemini/test_keypoint_augmenter.py
  </files>
  <behavior>
    - augment_low_confidence(video_path, low_conf_frame_indices: list[int], rtmw_array: np.ndarray, frame_width: int, frame_height: int) → dict — {"refined": [...], "augmented_frames": [...], "original_confidence": [...], "guardrail_blocked_count": int, "model": str}.
    - low_conf_frame_indices 가 비어 있으면 즉시 빈 dict 반환 (Gemini 호출 X — 메모리 [[feedback-analysis-first]] "필요할 때만" 정합).
    - GeminiVisionCall(schema=KeypointRefinement, model=env GEMINI_D_MODEL default "gemini-3.1-pro", temperature=0.0, max_output_tokens=2048, thinking_budget=0, enforce_object_guard=False) — 영역 D 만 좌표 허용.
    - Prompt: 영상 + low_conf frame_indices 박제 + 8 관절 (RTMW COCO-17 의 leftElbow/rightElbow/... 8개) 좌표를 정규화 [0,1] 로 추출 요청. RTMW 의 기존 좌표 (있는 경우) 를 prompt 에 hint 로 박지 않음 — Gemini 의 독립 추정 (편향 방지).
    - G5 가드 — 보강된 (frame_index, joint_key, x, y) 각각에 대해 RTMW 인접 frame (±2) 의 동일 joint 좌표 (정규화) 와 L2 거리 < 0.15 검증. ≥ 0.15 면 폐기 + guardrail_blocked_count++. 인접 frame 도 confidence < 0.3 면 비교 skip (둘 다 못 믿는 케이스 — RTMW 원본 유지).
    - frame_width/frame_height 는 rtmw_array 의 정규화 단위 계산용. RTMW 가 pixel 좌표면 / width, / height 로 정규화 후 비교.
    - 좌표 환각 가드 통과한 refined 좌표만 결과 dict 의 "refined" 에 박는다. caller (Task 2) 가 rtmw_array 에 주입.
    - firestore_admin.complete_analysis 에 gemini_d: dict | None = None kwarg 추가. None 이면 박지 않음, dict 면 `geminiD` 필드로 박제 (augmented_frames + original_confidence + guardrail_blocked_count + model).
    - test_keypoint_augmenter.py: (1) low_conf_frame_indices=[] → 빈 dict + Gemini 호출 0, (2) Gemini 호출 성공 + G5 가드 통과 → refined 좌표 박힘, (3) G5 가드 발동 (인접 frame 거리 0.2) → 폐기 + guardrail_blocked_count=1, (4) call None → 빈 dict (graceful), (5) enforce_object_guard=False 확인 (좌표 출력이 ValueError raise X).
  </behavior>
  <action>
    backend/shared/python/sunity_shared/gemini/keypoint_augmenter.py 신설. RTMW COCO-17 의 8 관절 key (leftElbow/rightElbow/leftShoulder/rightShoulder/leftHip/rightHip/leftKnee/rightKnee) — backend/shared/python/sunity_shared/analysis/skeleton.py JOINT_KEYS 정합. Prompt 박제 (한국어, "이 영상의 N 번째 frame 에서 다음 8 관절의 정규화 좌표 (frame width/height 로 0~1 정규화) 를 추출하세요" + "RTMW 가 confidence 낮게 추정한 frame 입니다"). G5 가드 함수 _check_neighbor_distance(rtmw_array, frame_idx, joint_idx, refined_x, refined_y, frame_w, frame_h, threshold=0.15) 내부 helper 박제 — numpy L2. KeypointRefinement.refined 가 max_length=200 이므로 low_conf_frame_indices × 8 joints 가 200 초과면 frame 단위로 잘라 호출 (5~60s 영상의 9fps × 0.5 frame fail rate = 최대 ~27 frame, 8×27=216 → 호출 2회 split). 단순화: 200 / 8 = 25 frame chunk. test 는 monkeypatch 로 GeminiVisionCall.call stub.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/gemini/test_keypoint_augmenter.py -x -q</automated>
  </verify>
  <done>5 케이스 테스트 통과: 빈 input/정상/G5 가드/None graceful/객관성 가드 OFF. complete_analysis(gemini_d={...}) 호출 시 Firestore mock 의 update_data 에 `geminiD` 키 박힘.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: pipeline _process 에 wave 2 영역 D 호출 + RTMW 행렬 주입 + occlusion_severe skip 게이트</name>
  <files>
    backend/functions/pipeline/app.py,
    backend/tests/test_pipeline_geminid_wiring.py
  </files>
  <behavior>
    - _process 의 wave 1 (Plan 02 의 find_scene_flags) 직후 + KISMAM/DTW 직전에 wave 2 호출 추가.
    - 게이트: wave 1 의 scene_flags["occlusion_severe"]==True 면 영역 D skip (RESEARCH §4 박제 + AI-SPEC §4b async pattern "if not c_result.occlusion_severe").
    - 게이트 통과 시: RTMW 행렬 (`coco_array` shape (T, J, 4), [:, :, 3] = confidence) 에서 frame 단위 최소 confidence < 0.5 인 frame index 추출 — `low_conf_frame_indices = [i for i, c in enumerate(coco_array[:, :, 3].min(axis=1)) if c < 0.5]`.
    - augment_low_confidence(local_video_path, low_conf_frame_indices, coco_array, frame_w, frame_h) 호출. frame_w/h 는 frame_extractor 결과 또는 RTMW input resolution 에서 박제.
    - 결과 dict 의 "refined" 좌표를 coco_array 에 in-place 주입: 각 refined entry 의 (frame_index, joint_key, x_normalized, y_normalized) 를 RTMW joint 인덱스 매핑 (skeleton.JOINT_KEYS index_of) → coco_array[frame_index, joint_idx, 0] = x_normalized * frame_w, [1] = y_normalized * frame_h, [2] = confidence (Gemini 출력값). guardrail_blocked 좌표는 RTMW 원본 유지.
    - 보강 후 기존 DTW/KISMAM/각도 계산이 보강된 coco_array 로 진행 — 변경 0 (assemble 시 행렬만 다름).
    - firestore_admin.complete_analysis 호출 시 gemini_d=augment_result 전달.
    - 빈 low_conf_frame_indices 또는 augment 실패 시 분석 흐름 차단 X (graceful).
    - test_pipeline_geminid_wiring.py: (1) occlusion_severe=True → augment 호출 0 + geminiD=None, (2) occlusion_severe=False + low_conf frame 3개 → augment 1회 호출 + RTMW 행렬 주입 확인, (3) augment None → 분석 흐름 계속, (4) RTMW 행렬 in-place 주입이 후속 DTW/각도 계산 입력 행렬에 반영 (mock 으로 검증).
  </behavior>
  <action>
    backend/functions/pipeline/app.py 의 _process 에서 Plan 02 의 find_scene_flags 호출 직후 라인에 영역 D wave 2 박제. import: `from sunity_shared.gemini.keypoint_augmenter import augment_low_confidence`. occlusion_severe 게이트는 scene_result.get("occlusion_severe", False) 검사 — Plan 02 가 dict 반환 박제. low_conf_frame_indices 계산은 numpy 한 줄. frame_w/h 는 _process 가 frame_extractor 호출 시 받는 frames 의 첫 frame.shape 또는 RTMW estimate 호출 직전 박제값에서 박제. RTMW 행렬 in-place 주입 시 skeleton.JOINT_KEYS 의 index 매핑 (좌표 출력의 joint_key 문자열 → COCO-17 인덱스). 통합 테스트는 monkeypatch 로 모든 외부 함수 stub. wave 1 + wave 2 의 순차 호출 (asyncio.gather 병렬 X — wave 2 가 wave 1 결과에 의존) 박제. 박제 — wave 2 의 latency 추가는 Pro 5~15s (AI-SPEC §4b), pipeline Lambda 900s timeout 정합.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_pipeline_geminid_wiring.py -x -q</automated>
  </verify>
  <done>통합 테스트 4 케이스 통과. _process 의 `augment_low_confidence` 호출 라인 존재. RTMW 행렬 in-place 주입 후 DTW/각도 계산 입력이 보강된 좌표 반영 확인 (mock assertion).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gemini D 출력 좌표 → RTMW 행렬 in-place 주입 | LLM 좌표가 numerical 행렬에 직접 박힘. 환각 시 DTW/각도 점수 전체 왜곡. |
| occlusion_severe (Plan 02) → 영역 D skip 게이트 | wave 1 의 flag 가 wave 2 의 의사결정. wave 1 실패 시 게이트 fail-open vs fail-closed 정책 박제. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-11 | Tampering | Gemini D 좌표 환각 | mitigate | G5 가드 — RTMW 인접 frame ±2 동일 joint 와 L2 거리 ≥ 0.15 면 폐기. guardrail_blocked_count Firestore 박제. |
| T-17-12 | Tampering | Pydantic [0, 1] 범위 우회 | mitigate | RefinedKeypoint.x_normalized/y_normalized 가 Pydantic Field(ge=0, le=1) — schema 검증에서 1차 차단. |
| T-17-13 | Information Disclosure | 영역 D 좌표 출력의 객관성 헌장 위반 | accept | AI-SPEC §6 G1 박제 "영역 D 만 좌표 허용". enforce_object_guard=False — 좌표 출력 자체가 의도. 점수/판단 어휘는 schema 구조상 출력 불가 (RefinedKeypoint 필드에 없음). |
| T-17-14 | Denial of Service | occlusion_severe=False 인데 low_conf frame 폭증 (전 frame 다 낮음) | mitigate | augment_low_confidence 가 KeypointRefinement.refined max_length=200 + 25-frame chunk split. Pod GPU 시간 폭증 방지. |
| T-17-15 | Repudiation | RTMW 행렬 주입 audit | mitigate | augmented_frames + original_confidence + guardrail_blocked_count 가 Firestore geminiD 박제 — 어떤 frame 이 어떤 conf 로 보강됐는지 추적 가능. |
</threat_model>

<verification>
- pytest backend/tests/gemini/test_keypoint_augmenter.py backend/tests/test_pipeline_geminid_wiring.py -x -q 통과.
- `grep -n "augment_low_confidence" backend/functions/pipeline/app.py | grep -v '^#' | grep -c .` ≥ 2 (import + 호출).
- `grep -n "enforce_object_guard=False" backend/shared/python/sunity_shared/gemini/keypoint_augmenter.py | grep -v '^#' | grep -c .` ≥ 1 (좌표 허용 명시).
- occlusion_severe skip 회귀: test 의 occlusion_severe=True 케이스에서 augment_low_confidence mock 호출 횟수 == 0.
- G5 가드 회귀: test_keypoint_augmenter.py 의 "인접 frame 거리 0.2" 케이스가 guardrail_blocked_count=1 + refined len < 8 (해당 좌표 폐기) 반환.
</verification>

<success_criteria>
1. sunity_shared/gemini/keypoint_augmenter.py 신설 — augment_low_confidence 함수 export.
2. backend/functions/pipeline/app.py 의 _process 에 영역 D 호출 wave 가 wave 1 직후 박제. occlusion_severe=True 게이트 동작.
3. RTMW 행렬 in-place 주입 — coco_array[frame_idx, joint_idx, 0:3] 가 보강 좌표로 갱신. 후속 DTW/각도 계산이 보강된 행렬 사용.
4. G5 가드 — 인접 frame L2 거리 ≥ 0.15 좌표 폐기 + guardrail_blocked_count Firestore 박제.
5. firestore_admin.complete_analysis 가 gemini_d kwarg 받아 Firestore `geminiD` 필드 박는다 (flat object).
6. low_conf_frame_indices 비어 있을 때 Gemini 호출 0 — 빈도 낮음 박제 (AI-SPEC §4 영역 D "Pod 에서 RTMW failure path 만 호출").
</success_criteria>

<output>
Create `.planning/phases/17-gemini-vision-integration-4/17-03-SUMMARY.md` when done.
</output>
