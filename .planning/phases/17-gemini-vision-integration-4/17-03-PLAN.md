---
phase: 17-gemini-vision-integration-4
plan: 03
type: execute
wave: 3
depends_on:
  - 17-01
  - 17-02
files_modified:
  - backend/shared/python/sunity_shared/gemini/keypoint_augmenter.py
  - backend/functions/pipeline/app.py  # _VideoAnalysisInputs 확장 (keypoints_4ch 필드 추가) + D wave wiring (3차 R-B3)
  - backend/shared/python/sunity_shared/firestore_admin.py
  - backend/tests/gemini/test_keypoint_augmenter.py
  - backend/tests/test_pipeline_geminid_wiring.py
  - backend/tests/test_extract_video_analysis_inputs.py  # _VideoAnalysisInputs.keypoints_4ch 회귀 — RTMW 1회 보장 유지 검증 (3차 R-B3)
autonomous: true
requirements:
  - VISION-04

must_haves:
  truths:
    - "RTMW 출력 행렬의 `uncertainty_proxy > 0.5` (4번째 채널, pose_frame.py to_coco17_array 정합 — 1.0 = 미감지) 인 frame 이 1개 이상이면 영역 D Gemini Pro 호출이 발동되어 해당 frame 의 keypoint 좌표가 정규화된 [0,1] 범위로 보강된다"
    - "**B2 정합 (2026-06-12, Codex review)**: 영역 D 1차 PR 의 출력은 `KeypointReport.data` + `KeypointReport.confidence` 보강 (assemble 가 만드는 user-visible 2D 보고서) 까지만. RTMW 3D pole-aligned 행렬 (`coco_array`) 는 **mutate 금지** — DTW/8 관절각 차원 점수 계산 입력은 RTMW 원본 그대로. 3D 주입은 별도 후속 plan (좌표계 계약 + 2D→3D 투영 lifter 박은 다음) 으로 분리."
    - "보강 좌표는 `KeypointReport.data` 의 인접 frame (±2) 동일 joint 와 정규화 L2 거리 ≥ 0.15 이면 G5 guardrail 이 폐기 + guardrail_blocked_count++. 인접 frame 도 confidence < 0.3 면 비교 skip."
    - "추가 산출: **좌우 mirror hint** — refined 좌표의 left/right joint pair 가 RTMW 원본 대비 swap 의심 (예: RTMW left_wrist 가 frame 내 우측 픽셀, Gemini left_wrist 가 좌측) 일 때 `geminiD.mirrorHint = {frame_idx: 'swap_lr'}` 박제. 1차 PR 은 hint 박제만, mirror correction 자체는 후속 plan."
    - "영역 C 의 occlusion_severe=True 면 영역 D 는 skip — 가려진 frame 에 좌표 보강 시도 의미 없음 (AI-SPEC §4b async pattern)"
    - "Firestore users/{uid}/analyses/{id}.geminiD 에 augmentedFrames + originalUncertaintyProxy + model + guardrail_blocked_count + mirrorHint 가 flat 으로 박힌다"
  artifacts:
    - path: "backend/shared/python/sunity_shared/gemini/keypoint_augmenter.py"
      provides: "영역 D augment_low_confidence(video_path, low_uncertainty_frame_indices, keypoint_report_2d) → dict | None — 2D KeypointReport 만 입력/출력 (B2 정합). frame_w/h 인자 박제 X — Gemini 가 normalized [0,1] 강제 (3차 R-B3 정합)"
      contains: "def augment_low_confidence"
    - path: "backend/functions/pipeline/app.py"
      provides: "_process 내 wave 2 영역 D 호출 + KeypointReport.data/confidence 보강 + Firestore geminiD 전달 — coco_array 3D 행렬 mutate 0 (B2)"
      contains: "augment_low_confidence"
    - path: "backend/shared/python/sunity_shared/firestore_admin.py"
      provides: "complete_analysis 가 gemini_d kwarg 수용"
      contains: "gemini_d"
    - path: "backend/tests/gemini/test_keypoint_augmenter.py"
      provides: "영역 D 단위 테스트 + G5 좌표 환각 가드 검증"
      contains: "def test_"
    - path: "backend/tests/test_pipeline_geminid_wiring.py"
      provides: "_process wave 2 wiring + occlusion_severe skip + KeypointReport 보강 통합 테스트 + 3D coco_array 불변 회귀"
      contains: "def test_"
  key_links:
    - from: "backend/shared/python/sunity_shared/gemini/keypoint_augmenter.py"
      to: "backend/shared/python/sunity_shared/gemini/client.py"
      via: "GeminiVisionCall(schema=KeypointRefinement, enforce_object_guard=False) — 영역 D 만 좌표 허용"
      pattern: "enforce_object_guard=False"
    - from: "backend/functions/pipeline/app.py"
      to: "backend/shared/python/sunity_shared/gemini/keypoint_augmenter.py"
      via: "augment_low_confidence 호출 (KeypointReport 생성 직후 + occlusion_severe 게이트 후)"
      pattern: "augment_low_confidence"
    - from: "backend/functions/pipeline/app.py"
      to: "KeypointReport (assemble.build_keypoint_report 출력)"
      via: "보강된 좌표가 KeypointReport.data 의 (frame_idx*J + joint_idx)*2 위치에 박힘 + confidence 도 동일 위치 갱신. **`coco_array` 3D 행렬은 mutate 0** (B2 hard gate)."
      pattern: "KeypointReport(data=...)"
---

<objective>
Phase 17 Wave 2 (RESEARCH §9 표기) — 영역 D Keypoint 보강을 Gemini 3.1 Pro Preview 로 호출한다. RTMW 의 약점 (inverted/twist/폐색 자세에서 `uncertainty_proxy > 0.5` frame) 을 Gemini Vision 의 영상 이해로 보강. ROADMAP Success #4 "KeypointReport 의 confidence 의 0.5 미만 비율 < 5% (현 신규 6 = 13-35%)" 직격. 객관성 가드 OFF (좌표 출력 허용) — AI-SPEC §6 G1 박제 "영역 D 만 좌표 허용 (`enforce_object_guard=False`)". 단 G5 가드 (인접 frame 거리 ≥ 0.15 환각 의심) 가 hallucination 차단.

**B2 scope reduction (2026-06-12, Codex review 정합)**: 1차 PR 의 D 영역 출력은 user-visible `KeypointReport` (assemble.build_keypoint_report 가 만드는 2D normalized 보고서) 의 `data` + `confidence` + 좌우 mirror hint 까지만. RTMW 3D pole-aligned scoring 행렬 (`coco_array`) 은 **in-place mutate 금지** — DTW / 8 관절각 차원 점수 / KISMAM 계산 입력은 RTMW 원본 그대로 유지. 이유: Gemini 출력의 normalized 2D pixel 좌표 ↔ RTMW pole-aligned 3D 좌표는 좌표계가 다름. 직접 섞으면 점수 전체 왜곡. 3D 주입은 별도 후속 plan (좌표계 계약 + 2D→3D lifter 박은 다음 — Phase 17B 또는 18 후보) 으로 분리.

Purpose: RTMW 가 못 찾는 좌표를 Gemini 가 영상 단서 (의상/머리 방향/폴 접촉점) 로 추정 — 메모리 [[ml-pose-3d-pivot]] + [[rtmw-free-stack-pivot]] 의 RTMW 단일 백본 약점 직접 대응. PROJECT.md "고수가 낮게 나오는 위양성" 의 좌우 swap / occlusion 케이스를 (i) user-visible KeypointReport 시각화 정합 + (ii) Firestore mirrorHint audit 으로 노출 (mirror correction 자체는 후속).
Output: keypoint_augmenter.py 모듈 + pipeline _process 의 wave 2 wiring + KeypointReport.data/confidence 보강 + mirrorHint 박제 + Firestore geminiD schema + 단위/통합 테스트 (3D coco_array 불변 회귀 포함).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-gemini-vision-integration-4/17-AI-SPEC.md
@.planning/phases/17-gemini-vision-integration-4/17-RESEARCH.md
@.planning/phases/17-gemini-vision-integration-4/17-01-SUMMARY.md
@.planning/phases/17-gemini-vision-integration-4/17-02-SUMMARY.md

# RTMW 행렬 shape (T, J, 4) — x, y, conf
@backend/shared/python/sunity_shared/analysis/pose_estimator.py
# _process 의 RTMW estimate 위치 + scene_finder 호출 위치 (Plan 02 박은 라인)
@backend/functions/pipeline/app.py
@backend/shared/python/sunity_shared/analysis/skeleton.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: augment_low_confidence 모듈 + G5 좌표 환각 가드 + Firestore geminiD schema</name>
  <files>
    backend/shared/python/sunity_shared/gemini/keypoint_augmenter.py,
    backend/shared/python/sunity_shared/firestore_admin.py,
    backend/tests/gemini/test_keypoint_augmenter.py
  </files>
  <behavior>
    - **시그니처 (B2 + 3차 R-B3 정합)** — `augment_low_confidence(video_path: str, low_uncertainty_frame_indices: list[int], keypoint_report_2d: KeypointReport) -> dict | None`. **`frame_w`/`frame_h` 인자 박제 X (3차 R-B3 정합)** — Gemini 가 normalized [0,1] 좌표 직접 반환, KeypointReport.data 도 이미 normalized → coordinate-space 혼선 0. 반환 dict keys: `{"refined": [{frame_index, joint_key, x_normalized, y_normalized, confidence}, ...], "augmented_frames": [...], "original_rtmw_uncertainty_proxy": [...], "guardrail_blocked_count": int, "mirror_hint": {frame_idx: "swap_lr", ...}, "model": str}`. (3차 R-W2 정합: `original_uncertainty_proxy` → `original_rtmw_uncertainty_proxy` — KeypointReport.confidence 와 audit 분리.) **3D `coco_array` 는 인자로 받지 않음** (B2 hard gate — 입력/출력 양쪽에서 3D scoring 행렬과 격리).
    - low_uncertainty_frame_indices 가 비어 있으면 즉시 빈 dict 반환 (Gemini 호출 X — 메모리 [[feedback-analysis-first]] "필요할 때만" 정합).
    - GeminiVisionCall(schema=KeypointRefinement, model=env GEMINI_D_MODEL default `gemini_config.DEFAULT_D_MODEL` ("gemini-3.1-pro-preview"), temperature=0.0, max_output_tokens=2048, thinking_budget=0, enforce_object_guard=False) — 영역 D 만 좌표 허용.
    - Prompt (3차 R-B3 정합 — **normalized [0,1] 강제**): 영상 + low_uncertainty frame_indices 박제 + 8 관절 (RTMW COCO-17 의 leftElbow/rightElbow/... 8개) 좌표를 **frame 의 left/top 을 (0,0), right/bottom 을 (1,1) 로 normalized [0,1]** 좌표로 추출 요청. frame 절대 픽셀 크기 박제 X — Gemini 가 frame width/height 알 필요 없이 self-normalize. KeypointRefinement Pydantic schema 의 `x_normalized`/`y_normalized` Field(ge=0, le=1) 가 1차 차단. RTMW 의 기존 좌표 (있는 경우) 를 prompt 에 hint 로 박지 않음 — Gemini 의 독립 추정 (편향 방지).
    - **G5 좌표 환각 가드** — 보강된 (frame_index, joint_key, x, y) 각각에 대해 `keypoint_report_2d.data` 의 인접 frame (±2) 동일 joint 좌표 (이미 정규화 [0,1]) 와 L2 거리 검증. ≥ 0.15 면 폐기 + guardrail_blocked_count++. 인접 frame 도 `keypoint_report_2d.confidence` < 0.3 면 비교 skip (둘 다 못 믿는 케이스).
    - **mirror hint 산출** — refined 좌표의 left/right joint pair (예: left_wrist + right_wrist) 가 RTMW 원본 (keypoint_report_2d.data) 대비 frame 내 좌우 픽셀 위치 swap 의심 시 (refined left.x > refined right.x AND RTMW 원본은 반대) `mirror_hint[frame_idx] = "swap_lr"` 박제. 1차 PR 은 audit 만 — KeypointReport.data 에는 refined 좌표 그대로 반영하고 mirror correction 은 적용 X (후속 plan).
    - 좌표 환각 가드 통과한 refined 좌표만 결과 dict 의 "refined" 에 박는다. caller (Task 2) 가 KeypointReport.data + confidence 의 해당 위치 에 박는다.
    - `original_uncertainty_proxy` 기록은 RTMW `coco_array[:, :, 3]` 가 아니라 keypoint_report_2d.confidence 의 invert (`1.0 - confidence`) — KeypointReport 가 이미 user-visible confidence 박제. 양쪽 일관.
    - firestore_admin.complete_analysis 에 gemini_d: dict | None = None kwarg 추가. None 이면 박지 않음, dict 면 `geminiD` 필드로 박제 (augmented_frames + original_uncertainty_proxy + guardrail_blocked_count + mirror_hint + model).
    - test_keypoint_augmenter.py: (1) low_uncertainty_frame_indices=[] → 빈 dict + Gemini 호출 0, (2) Gemini 호출 성공 + G5 가드 통과 → refined 좌표 박힘, (3) G5 가드 발동 (인접 frame 거리 0.2) → 폐기 + guardrail_blocked_count=1, (4) call None → 빈 dict (graceful), (5) enforce_object_guard=False 확인 (좌표 출력이 ValueError raise X), (6) **mirror hint**: refined left_wrist.x=0.8, right_wrist.x=0.2 + RTMW 원본 반대 → mirror_hint[frame_idx]="swap_lr", (7) **B2 시그니처 회귀**: `inspect.signature(augment_low_confidence).parameters` 에 `coco_array` / `rtmw_array` 키워드 0건 — 3D scoring 행렬 격리.
  </behavior>
  <action>
    backend/shared/python/sunity_shared/gemini/keypoint_augmenter.py 신설. RTMW COCO-17 의 8 관절 key (leftElbow/rightElbow/leftShoulder/rightShoulder/leftHip/rightHip/leftKnee/rightKnee) — backend/shared/python/sunity_shared/analysis/skeleton.py JOINT_KEYS 정합. Prompt 박제 (한국어, "이 영상의 N 번째 frame 에서 다음 8 관절의 정규화 좌표 (frame width/height 로 0~1 정규화) 를 추출하세요" + "RTMW 가 confidence 낮게 추정한 frame 입니다"). G5 가드 함수 `_check_neighbor_distance(keypoint_report_2d, frame_idx, joint_idx, refined_x, refined_y, threshold=0.15)` 내부 helper 박제 — KeypointReport.data 의 `(frame_idx*J + joint_idx)*2` 위치 lookup + numpy L2. mirror hint helper `_detect_mirror_swap(keypoint_report_2d, refined_frame_dict, frame_idx)` 박제 — left/right pair 4 set (wrist/shoulder/hip/knee) 비교. KeypointRefinement.refined 가 max_length=200 이므로 low_uncertainty_frame_indices × 8 joints 가 200 초과면 frame 단위로 잘라 호출 (5~60s 영상의 9fps × 0.5 frame fail rate = 최대 ~27 frame, 8×27=216 → 호출 2회 split). 단순화: 200 / 8 = 25 frame chunk. test 는 monkeypatch 로 GeminiVisionCall.call stub + `KeypointReport` fixture 박제.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/gemini/test_keypoint_augmenter.py -x -q</automated>
  </verify>
  <done>5 케이스 테스트 통과: 빈 input/정상/G5 가드/None graceful/객관성 가드 OFF. complete_analysis(gemini_d={...}) 호출 시 Firestore mock 의 update_data 에 `geminiD` 키 박힘.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: pipeline _process wave 2 영역 D 호출 + KeypointReport.data/confidence 보강 + 3D coco_array 불변 회귀 (B2 정합)</name>
  <files>
    backend/functions/pipeline/app.py,
    backend/tests/test_pipeline_geminid_wiring.py
  </files>
  <behavior>
    - **삽입 위치 (2차 R-B1 정정)** — _process 의 실제 호출 순서 (`backend/functions/pipeline/app.py:1163` build_result → `:1263` build_keypoint_report → `:1275` complete_analysis) 검증 후, D wave 는 **`build_keypoint_report` 호출 직후 + `complete_analysis` 호출 직전** 에 박는다. `build_result` 직전 박제 X (build_result 가 먼저 일어남 — 1차 패치의 위치 오류 정정). KISMAM/DTW/각도/dim_scores/build_result 는 RTMW 원본으로 이미 끝나 있는 상태 — D 가 영향 줄 surface 0.
    - 게이트 1: wave 1 의 scene_flags["occlusion_severe"]==True 면 영역 D skip (RESEARCH §4 박제 + AI-SPEC §4b async pattern "if not c_result.occlusion_severe").
    - 게이트 2: `_gemini_vision_enabled()` (Plan 01 Task 4) 또는 `GEMINI_D_ENABLED=1` 만 켜진 경우 진입. local_video_path is None 이면 skip + log warning.
    - **B4 정합 — S3 재다운로드 / RTMW 재실행 금지 (hard gate)**: augment_low_confidence 는 caller 의 `local_video_path` (Plan 01 Task 4 가 `GEMINI_D_ENABLED` 포함 → keep_local_video=True) 만 사용. boto3 S3 download / pose estimate 재실행 0건. local_video_path is None 이면 skip + geminiD=None.
    - **`_VideoAnalysisInputs` 확장 (3차 R-B3 정합)** — pipeline/app.py:464-485 의 NamedTuple 에 `keypoints_4ch: np.ndarray` (shape (T, 17, 4)) 1 필드 추가. `_extract_video_analysis_inputs()` 의 line 527 `keypoints_4ch = to_coco17_array(pose_frames)` 결과를 그대로 반환 (재계산 0, 추가 RTMW 호출 0 — Phase 6 의 "RTMW estimate 1회" 보장 유지).
    - 게이트 통과 시: `_process` 가 `inputs.keypoints_4ch[:, :, 3].max(axis=1)` 로 uncertainty_proxy max 계산 (pose_frame.py:325-353 정합) — frame 단위 최대 uncertainty_proxy > 0.5 인 frame index 추출. `low_uncertainty_frame_indices = [i for i, u in enumerate(inputs.keypoints_4ch[:, :, 3].max(axis=1)) if u > 0.5]`. **`min(axis=1) < 0.5` 박제 금지** (이전 plan 오해 — 의미 정반대).
    - augment_low_confidence(local_video_path, low_uncertainty_frame_indices, keypoint_report_2d) 호출 — **frame_w/h 인자 박제 X** (3차 R-B3 정합: Gemini 가 normalized [0,1] 자체 반환). **`coco_array` 전달 금지** (B2 hard gate). keypoint_report_2d 는 assemble.build_keypoint_report 가 방금 만든 KeypointReport 인스턴스.
    - 결과 dict 의 "refined" 좌표를 **KeypointReport 의 data/confidence 만 보강**: dataclass 가 frozen 이므로 `dataclasses.replace(keypoint_report_2d, data=new_data, confidence=new_confidence, reliability=new_reliability, warnings=warnings + ["gemini_d_augmented"])` 패턴. 보강 위치: `(frame_idx * J + joint_idx) * 2` (data), `frame_idx * J + joint_idx` (confidence). guardrail_blocked 좌표는 RTMW 원본 유지. reliability 는 보강된 frame 의 신뢰도 등급 (compute_frame_reliability) 재산출.
    - **3D scoring 행렬 불변**: `coco_array` 는 D wave 진입 전/후 동일 (id() 동일 or array_equal). DTW/KISMAM/각도/dim_scores 는 이미 wave 2 전에 계산 완료 — D 가 점수에 영향 0. UI 의 KeypointReport 시각화만 풍부화.
    - **complete_analysis 인자 (2차 R-B1 정합)** — `firestore_admin.complete_analysis(..., keypoint_report=augmented_keypoint_report_dict, gemini_d=augment_result, ...)` 박제. `assemble.build_result` 시그니처에 `keypoint_report` 인자 없음 (`assemble.py:271` 검증) — build_result 는 D wave 전에 끝난 RTMW 원본 dim_scores/joints/comparison 으로 호출. result.keypointReport 는 complete_analysis 가 박는다 (`pipeline/app.py:1263-1275` 흐름). user-visible result.keypointReport vs audit top-level geminiD 분리 (WARNING-3 정합).
    - 빈 low_uncertainty_frame_indices 또는 augment 실패 시 분석 흐름 차단 X (graceful) — keypoint_report_2d 원본 유지.
    - test_pipeline_geminid_wiring.py: (1) occlusion_severe=True → augment 호출 0 + geminiD=None + KeypointReport 원본 유지, (2) GEMINI_D_ENABLED=0 → augment 호출 0, (3) occlusion_severe=False + low_uncertainty frame 3개 → augment 1회 호출 + KeypointReport.data/confidence 보강 확인 + warnings 에 "gemini_d_augmented" 추가, (4) augment None → 분석 흐름 계속 + KeypointReport 원본, (5) **B2 핵심 회귀**: D wave 진입 전 `coco_array.copy()` vs D wave 종료 후 `coco_array` — `np.array_equal` True (3D 행렬 불변), (6) **B2 핵심 회귀**: build_result mock 호출이 augment_low_confidence 호출보다 먼저 일어남 (call_order 검증), (7) **B2 핵심 회귀**: build_result 의 `dimension_scores` 인자 = D wave 전 계산값 (D 영향 0), (8) **2차 R-B1 회귀**: `complete_analysis` mock 호출의 `keypoint_report` 인자가 augment 적용된 dict + `gemini_d` 인자가 augment 결과 dict. build_result 인자에 `keypoint_report` kwarg 없음 검증.
  </behavior>
  <action>
    backend/functions/pipeline/app.py 의 변경 2개:
    (a) **`_VideoAnalysisInputs` NamedTuple 확장 (3차 R-B3 정합)** — `:464-485` 의 5종 필드 (angles/student_profile/pose_frames/local_video_path/pole_axis_measurement) 에 **`keypoints_4ch: np.ndarray` (shape (T, 17, 4))** 추가. `_extract_video_analysis_inputs()` 의 `:527` `keypoints_4ch = to_coco17_array(pose_frames)` 결과를 return tuple 에 그대로 박제. 재계산 0건 — Phase 6 의 "RTMW estimate 1회" 보장 유지.
    (b) `_process` 의 D wave wiring — **build_keypoint_report 호출 (`:1263`) 직후 + complete_analysis 호출 (`:1275`) 직전** 라인에 영역 D wave 박제. (1차 패치의 "build_result 직전" 박제는 정반대 위치 — `:1163` 의 build_result 가 먼저 일어남. 2차 R-B1 정정.) build_result + dim_scores/joints 는 RTMW 원본으로 끝난 상태 → D wave 가 점수 surface 0. import: `from sunity_shared.gemini.keypoint_augmenter import augment_low_confidence`. occlusion_severe 게이트는 scene_result.get("occlusion_severe", False) 검사. `GEMINI_D_ENABLED` env (default "0" — 영역 D 는 opt-in 박제; AI-SPEC §4 "Pod 에서 RTMW failure path 만 호출, 빈도 낮음" 정합) + local_video_path 가드. low_uncertainty_frame_indices 계산은 `inputs.keypoints_4ch[:, :, 3].max(axis=1) > 0.5` 한 줄 (3차 R-B3 정합 — `coco_array` 직접 박제 X). `augment_low_confidence(local_video_path, low_uncertainty_frame_indices, keypoint_report)` 호출 — **frame_w/h 인자 박제 X** (Gemini self-normalize), **`coco_array` 인자 박제 X** (B2 hard gate). KeypointReport 보강은 `dataclasses.replace` — frozen 정합. 통합 테스트는 monkeypatch 로 모든 외부 함수 stub + np.array_equal 회귀.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_pipeline_geminid_wiring.py -x -q</automated>
  </verify>
  <done>통합 테스트 7 케이스 통과. _process 의 `augment_low_confidence` 호출 위치가 assemble.build_keypoint_report 직후 + build_result 직전. coco_array 불변 회귀 + DTW/dim_scores call_order 회귀 (5/6/7 케이스) 통과. `grep -n "coco_array\[.*\] =" backend/functions/pipeline/app.py | grep -A2 -B2 augment_low_confidence | grep -c .` == 0 (3D 행렬 mutate 라인 0).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gemini D 출력 좌표 → KeypointReport.data/confidence 보강 (user-visible 2D) | LLM 좌표가 user-visible 시각화 보고서에 박힘. 환각 시 KeypointOverlay 가 잘못된 위치 표시. **3D scoring 행렬 (coco_array) 격리** — DTW/각도 점수에는 영향 0 (B2 정합). |
| occlusion_severe (Plan 02) → 영역 D skip 게이트 | wave 1 의 flag 가 wave 2 의 의사결정. wave 1 실패 시 게이트 fail-open vs fail-closed 정책 박제. |
| mirror_hint audit → 후속 plan 의 mirror correction 입력 | 1차 PR 은 hint 박제만. 후속 plan 이 실제 swap 적용. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-11 | Tampering | Gemini D 좌표 환각 → KeypointReport 시각화 왜곡 | mitigate | G5 가드 — KeypointReport.data 인접 frame ±2 동일 joint 와 정규화 L2 거리 ≥ 0.15 면 폐기. guardrail_blocked_count Firestore 박제. **3D scoring 격리로 점수 왜곡 surface 0** (B2). |
| T-17-12 | Tampering | Pydantic [0, 1] 범위 우회 | mitigate | RefinedKeypoint.x_normalized/y_normalized 가 Pydantic Field(ge=0, le=1) — schema 검증에서 1차 차단. |
| T-17-13 | Information Disclosure | 영역 D 좌표 출력의 객관성 헌장 위반 | accept | AI-SPEC §6 G1 박제 "영역 D 만 좌표 허용". enforce_object_guard=False — 좌표 출력 자체가 의도. 점수/판단 어휘는 schema 구조상 출력 불가 (RefinedKeypoint 필드에 없음). |
| T-17-14 | Denial of Service | occlusion_severe=False 인데 low_uncertainty frame 폭증 (전 frame 다 낮음) | mitigate | augment_low_confidence 가 KeypointRefinement.refined max_length=200 + 25-frame chunk split. Pod GPU 시간 폭증 방지. |
| T-17-15 | Repudiation | KeypointReport 보강 + mirror hint audit | mitigate | augmented_frames + original_uncertainty_proxy + guardrail_blocked_count + mirror_hint 가 Firestore geminiD 박제 — 어떤 frame 이 어떤 uncertainty 로 보강됐는지 추적 가능. |
| T-17-16-B2 | Tampering | 좌표계 혼합 (2D normalized vs 3D pole-aligned) | mitigate | **B2 scope reduction** — augment_low_confidence 시그니처에 `coco_array` 자체 없음. KeypointReport 만 입력/출력. 3D 행렬 mutate 라인이 코드에 존재 안 함. 좌표계 계약 박은 후 후속 plan 에서 3D 주입 재진입. |
</threat_model>

<verification>
- pytest backend/tests/gemini/test_keypoint_augmenter.py backend/tests/test_pipeline_geminid_wiring.py -x -q 통과.
- `grep -n "augment_low_confidence" backend/functions/pipeline/app.py | grep -v '^#' | grep -c .` ≥ 2 (import + 호출).
- `grep -n "enforce_object_guard=False" backend/shared/python/sunity_shared/gemini/keypoint_augmenter.py | grep -v '^#' | grep -c .` ≥ 1 (좌표 허용 명시).
- occlusion_severe skip 회귀: test 의 occlusion_severe=True 케이스에서 augment_low_confidence mock 호출 횟수 == 0.
- G5 가드 회귀: test_keypoint_augmenter.py 의 "인접 frame 거리 0.2" 케이스가 guardrail_blocked_count=1 + refined len < 8 (해당 좌표 폐기) 반환.
- **B2 시그니처 회귀**: `python -c "from inspect import signature; from sunity_shared.gemini.keypoint_augmenter import augment_low_confidence; assert 'coco_array' not in signature(augment_low_confidence).parameters and 'rtmw_array' not in signature(augment_low_confidence).parameters"` 통과.
- **B2 3D 불변 회귀**: test 의 `np.array_equal(coco_array_before, coco_array_after)` True.
- **B2 call_order 회귀**: DTW/KISMAM/dim_scores mock 호출이 augment_low_confidence 호출보다 모두 먼저.
- **uncertainty_proxy 정정 회귀**: low_uncertainty_frame_indices 가 `max(axis=1) > 0.5` 로 계산됨을 mock 으로 검증 (이전 plan 의 `min < 0.5` 박제 0).
</verification>

<success_criteria>
1. sunity_shared/gemini/keypoint_augmenter.py 신설 — augment_low_confidence 함수 export. 시그니처에 3D scoring 행렬 인자 0건 (B2).
2. backend/functions/pipeline/app.py 의 _process 에 영역 D 호출 wave 가 **build_keypoint_report (`:1263`) 직후 + complete_analysis (`:1275`) 직전** 박제 (build_result `:1163` 보다 늦음 — 2차 R-B1 정정). occlusion_severe=True / GEMINI_D_ENABLED=0 / local_video_path None 게이트 동작.
3. **KeypointReport 보강 (B2 핵심)** — `dataclasses.replace(keypoint_report_2d, data=new_data, confidence=new_confidence, reliability=new_reliability, warnings=warnings+["gemini_d_augmented"])`. user-visible 시각화만 풍부화.
4. **3D scoring 행렬 (coco_array) 불변** — D wave 진입 전후 `np.array_equal` True. DTW/KISMAM/8 관절각 차원 점수는 D 영향 0. assemble.build_result 의 dim_scores 인자가 D wave 전 계산값.
5. G5 가드 — KeypointReport.data 의 인접 frame 정규화 L2 거리 ≥ 0.15 좌표 폐기 + guardrail_blocked_count Firestore 박제.
6. mirror_hint 박제 — left/right joint pair 의 swap 의심 frame index 가 Firestore geminiD.mirrorHint 에 audit-only 로 박힘. mirror correction 자체는 후속 plan.
7. firestore_admin.complete_analysis 가 gemini_d kwarg 받아 Firestore top-level `geminiD` 필드 박는다 (flat object — augmentedFrames + originalUncertaintyProxy + guardrailBlockedCount + mirrorHint + model). user-visible result.keypointReport 와 audit 분리 (WARNING-3 정합).
8. low_uncertainty_frame_indices 비어 있을 때 Gemini 호출 0 — 빈도 낮음 박제 (AI-SPEC §4 영역 D "Pod 에서 RTMW failure path 만 호출").
9. **uncertainty_proxy 정정** — frame 식별이 `coco_array[:, :, 3].max(axis=1) > 0.5` (pose_frame.py to_coco17_array 정합). `min < 0.5` 박제 0건.

**B2 deferred (별도 후속 plan)**: 좌표계 계약 (RTMW pole-aligned 3D ↔ Gemini normalized 2D) + 2D→3D lifter + `coco_array` in-place 주입 + 보강된 3D 좌표로 DTW/KISMAM/각도 재계산. 본 plan 진입 시점 박제 미정 — 좌표계 계약 박힌 다음 ROADMAP 추가.
</success_criteria>

<output>
Create `.planning/phases/17-gemini-vision-integration-4/17-03-SUMMARY.md` when done.
</output>
