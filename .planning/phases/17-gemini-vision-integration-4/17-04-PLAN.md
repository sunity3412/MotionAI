---
phase: 17-gemini-vision-integration-4
plan: 04
type: execute
wave: 4
depends_on:
  - 17-01
  - 17-02
  - 17-03
files_modified:
  - backend/shared/python/sunity_shared/gemini/coach_writer_v2.py
  - backend/functions/pipeline/app.py
  - backend/shared/python/sunity_shared/firestore_admin.py
  - backend/tests/gemini/test_coach_writer_v2.py
  - backend/tests/test_pipeline_geminib_wiring.py
autonomous: true
requirements:
  - VISION-02

must_haves:
  truths:
    - "영역 B GeminiCoachWriter 가 CerebrasCoachWriter 와 동일 .write() 인터페이스를 제공해서 _process 의 coach 호출이 env GEMINI_COACH_ENABLED 토글로 swap 가능하다"
    - "Gemini Pro vision 호출이 영상 + RTMW 8 관절 deviation 수치 prompt 결합으로 한국어 코칭 멘트 (causes 3~5 + injury_risk + coach_note) 를 산출한다"
    - "코칭 멘트의 coach_note 는 '강사' + '함께' + '확인' 3 어휘를 모두 포함해서 '강사 보조 톤' 충족이 schema 단계에서 강제된다"
    - "Gemini 호출 실패 시 Cerebras 폴백 — coach_writer_v2 가 None 반환 시 _process 가 기존 CerebrasCoachWriter 로 자동 우회한다"
    - "Firestore users/{uid}/analyses/{id}.geminiB 에 causes + coach_note + model + judgeScore=null 이 박힌다 (F1 flywheel 이 후속 채움)"
  artifacts:
    - path: "backend/shared/python/sunity_shared/gemini/coach_writer_v2.py"
      provides: "GeminiCoachWriter.write(joints, dim_scores, video_path) — CerebrasCoachWriter interface 정합"
      contains: "class GeminiCoachWriter"
    - path: "backend/functions/pipeline/app.py"
      provides: "_process 내 wave 3 영역 B 호출 + Cerebras 폴백 + Firestore geminiB 전달"
      contains: "GeminiCoachWriter"
    - path: "backend/shared/python/sunity_shared/firestore_admin.py"
      provides: "complete_analysis 가 gemini_b kwarg 수용"
      contains: "gemini_b"
    - path: "backend/tests/gemini/test_coach_writer_v2.py"
      provides: "영역 B GeminiCoachWriter 단위 테스트 + 톤 검증 + Cerebras 폴백"
      contains: "def test_"
    - path: "backend/tests/test_pipeline_geminib_wiring.py"
      provides: "_process wave 3 wiring + env toggle + dual-track A/B 통합 테스트"
      contains: "def test_"
  key_links:
    - from: "backend/shared/python/sunity_shared/gemini/coach_writer_v2.py"
      to: "backend/shared/python/sunity_shared/gemini/client.py"
      via: "GeminiVisionCall(schema=CoachPayload, model=GEMINI_B_MODEL, temperature=0.4, max_output_tokens=2500, thinking_budget=4096) 인스턴스화"
      pattern: "GeminiVisionCall"
    - from: "backend/shared/python/sunity_shared/gemini/coach_writer_v2.py"
      to: "backend/shared/python/sunity_shared/analysis/coach_writer.py"
      via: "CerebrasCoachWriter 와 동일 .write(joints, dim_scores) 시그너처 + graceful None 반환 패턴"
      pattern: "class GeminiCoachWriter"
    - from: "backend/functions/pipeline/app.py"
      to: "backend/shared/python/sunity_shared/gemini/coach_writer_v2.py"
      via: "env GEMINI_COACH_ENABLED 토글 → GeminiCoachWriter 우선, None 시 CerebrasCoachWriter 폴백"
      pattern: "GEMINI_COACH_ENABLED"
---

<objective>
Phase 17 Wave 3 (RESEARCH §9 표기) — 영역 B 코칭 멘트를 Gemini 3.1 Pro Vision 으로 산출한다. 기존 CerebrasCoachWriter 와 dual-track — env `GEMINI_COACH_ENABLED=1` (default) 시 Gemini 우선, None 반환 시 Cerebras 폴백. RTMW 의 8 관절 deviation 수치를 prompt 에 박고 영상도 같이 입력 → Gemini Vision 이 "왼쪽 팔꿈치 hook 시 폴 접촉면 박힘 — 어깨 견갑 안정성 부족" 같은 구체 코칭 산출. AI-SPEC §5 E4 (causes 3~5 + 부위별 용어 + 강사 보조 톤 binary pass + LLM judge ≥ 0.7) 정합. ROADMAP Success #2 "구체성 강화 + belle/강사 1차 검수 통과" 직격.

Purpose: PROJECT.md "원인 → 해결 순서 피드백 (수치는 보조, 원인이 핵심)" + "AI = 강사 보조 도구" 포지셔닝을 schema 강제로 박는다. CoachDetail2.coach_note 가 '강사' / '함께' / '확인' 3 어휘 동시 포함 강제 (E4 dimension 3번 항목). 메모리 [[coaching-tone-customization]] + [[field-research-stakeholders]] 직격.
Output: coach_writer_v2.py 모듈 + pipeline _process 의 wave 3 wiring + env toggle + Cerebras 폴백 + Firestore geminiB schema + 단위/통합 테스트.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-gemini-vision-integration-4/17-AI-SPEC.md
@.planning/phases/17-gemini-vision-integration-4/17-RESEARCH.md
@.planning/phases/17-gemini-vision-integration-4/17-01-SUMMARY.md
@.planning/phases/17-gemini-vision-integration-4/17-02-SUMMARY.md
@.planning/phases/17-gemini-vision-integration-4/17-03-SUMMARY.md

# 기존 CerebrasCoachWriter — interface 정합 대상 (변경 0, 그대로 폴백)
@backend/shared/python/sunity_shared/analysis/coach_writer.py
# _process 의 coach 호출 위치
@backend/functions/pipeline/app.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GeminiCoachWriter + 강사 보조 톤 schema 강제 + Firestore geminiB schema</name>
  <files>
    backend/shared/python/sunity_shared/gemini/coach_writer_v2.py,
    backend/shared/python/sunity_shared/firestore_admin.py,
    backend/tests/gemini/test_coach_writer_v2.py
  </files>
  <behavior>
    - GeminiCoachWriter(api_key_loader=None, model=None) — model env GEMINI_B_MODEL default "gemini-3.1-pro".
    - .write(joints: list[dict], dim_scores: dict, video_path: str | None = None) → dict | None — CerebrasCoachWriter 와 동일 시그너처 + 추가 video_path kwarg (vision 분기). video_path=None 이면 None 반환 (Cerebras 폴백 활성화 — text-only 코칭은 Cerebras 책임).
    - GeminiVisionCall(schema=CoachPayload, model=..., temperature=0.4, max_output_tokens=2500, thinking_budget=4096, enforce_object_guard=True) — coach_writer.py L132 의 temp 정합.
    - Prompt 구성: system instruction (강사 보조 톤 + 원인-해결 순서 + 부위별 용어 화이트리스트: "고관절/후굴/코어/내전근/외회전/햄스트링/견갑/엉덩이 굴곡/회전근개/요추/흉추/슬괵/장요근/대퇴직근" 14개) + user prompt (영상 + 8 관절 deviation 수치 박제 "왼쪽 팔꿈치: 현재 87° → 기준 110° (deviation +23°)" 포맷, coach_writer.py L69~74 정합).
    - CoachPayload (Plan 01 schema): joints 1~8 of JointCoaching(joint_key, detail, detail2: CoachDetail2(causes 3~5 of {title, explanation, fix}, injury_risk: str|None, coach_note: str)).
    - **추가 schema 검증 (영역 B 전용 후처리)**:
      - 각 cause.explanation 에 부위별 용어 14개 중 1개 이상 포함 — 없으면 ValueError (E4 dimension 2번 항목 강제).
      - coach_note 에 "강사" + "함께" + "확인" 3 단어 동시 포함 — 없으면 ValueError (E4 dimension 3번 항목 강제, AI-SPEC §5 박제).
      - 단정/지시형 어휘 blocklist ("이렇게 하세요", "잘못됐습니다", "틀렸습니다", "당신은") 매치 시 ValueError (강사 대체 톤 금지, AI-SPEC §1 critical failure mode 3).
    - 후처리 검증 실패 시 1회 retry — Plan 01 의 client retry 와 별개의 영역 B 전용 retry 1회. 2회 실패 시 return None (Cerebras 폴백 활성).
    - 결과 dict 에 model/tokens_used/latency_ms/judge_score=None 박는다 (F1 flywheel 이 judge_score 후속 채움).
    - firestore_admin.complete_analysis 에 gemini_b: dict | None = None kwarg 추가.
    - test_coach_writer_v2.py: (1) 정상 호출 → CoachPayload 반환 + 부위별 용어 + coach_note 3 어휘 통과, (2) 부위별 용어 누락 stub → 1회 retry 후 None, (3) coach_note "강사" 누락 → 1회 retry 후 None, (4) blocklist "이렇게 하세요" 매치 → 1회 retry 후 None, (5) call() 가 None (api_key 없음) → write None (Cerebras 폴백 신호), (6) video_path=None → write None.
  </behavior>
  <action>
    backend/shared/python/sunity_shared/gemini/coach_writer_v2.py 신설. GeminiCoachWriter 클래스 박제 — backend/shared/python/sunity_shared/analysis/coach_writer.py 의 CerebrasCoachWriter L18~30 의 _SYSTEM 텍스트 패턴을 참고하되, vision 분기 박제: "당신은 폴스포츠 강사를 보조하는 코칭 AI 입니다. 강사를 대체하지 않습니다. 사용자에게 단정형/지시형 (이렇게 하세요/틀렸습니다) 으로 말하지 마세요. 항상 원인 → 해결 순서로 작성하고, 마지막에 '강사와 함께 영상 확인 권고' 로 마무리하세요." 부위별 용어 화이트리스트 14개 박제 (PROJECT.md "고관절/후굴/코어/내전근" 4개 + 강사 설문조사 docs 의 부위 용어 확장). 후처리 검증은 dataclass method (_validate_tone) 박제 — 각 cause 순회 + 정규식. retry 는 GeminiVisionCall 의 retry 와 별개로 GeminiCoachWriter.write 안에서 for attempt in (1, 2) 박제 (총 GeminiVisionCall retry 1 + GeminiCoachWriter retry 1 = 4 호출 worst case). 테스트는 monkeypatch 로 GeminiVisionCall.call stub.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/gemini/test_coach_writer_v2.py -x -q</automated>
  </verify>
  <done>6 케이스 테스트 통과. CoachPayload 검증 + 부위별 용어 + 강사 보조 톤 3 어휘 + blocklist + retry + Cerebras 폴백 신호 (None 반환). complete_analysis(gemini_b={...}) Firestore mock 의 update_data 에 `geminiB` 키 박힘.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: pipeline _process 에 wave 3 영역 B 호출 + Cerebras dual-track 폴백 + env toggle</name>
  <files>
    backend/functions/pipeline/app.py,
    backend/tests/test_pipeline_geminib_wiring.py
  </files>
  <behavior>
    - _process 의 wave 2 (Plan 03 의 augment_low_confidence) 직후 + assemble result 직전에 wave 3 호출 추가.
    - env GEMINI_COACH_ENABLED (default "1") — "0" 또는 "false" 면 Gemini swap skip + CerebrasCoachWriter 만 호출 (기존 path 그대로).
    - GEMINI_COACH_ENABLED=1 시: GeminiCoachWriter.write(joints, dim_scores, video_path=local_video_path) 시도 → 결과 dict 면 그걸로 tips[]/coach 박제 + geminiB Firestore 박제. None 이면 CerebrasCoachWriter 폴백 (기존 호출 그대로).
    - Cerebras 폴백 시 geminiB={..., "fallback": "cerebras"} 박제 (F6 A/B 분석용 — Plan 06 의 flywheel).
    - tips[] / coach 추출: CoachPayload.joints 의 detail/detail2 를 기존 coach_writer.py 의 출력 형식 (tips list of str + coach detail object) 으로 변환. detail = tips 의 한 줄, detail2 = result.coach 의 detail2 object.
    - 기존 result schema (app/src/types/analysis.ts 의 AnalysisDoc) 와 호환 — 추가 필드 없이 tips/coach 만 풍부화.
    - firestore_admin.complete_analysis 호출 시 gemini_b=gemini_result 전달 (성공 시) 또는 gemini_b={"fallback": "cerebras", "model": "gpt-oss-120b"} (폴백 시).
    - test_pipeline_geminib_wiring.py: (1) GEMINI_COACH_ENABLED=1 + GeminiCoachWriter.write 성공 → tips/coach 가 Gemini 결과로 박힘 + geminiB 박제, (2) GEMINI_COACH_ENABLED=1 + GeminiCoachWriter.write None → CerebrasCoachWriter.write 호출 + geminiB.fallback="cerebras", (3) GEMINI_COACH_ENABLED=0 → GeminiCoachWriter 호출 0 + CerebrasCoachWriter 만 호출, (4) GeminiCoachWriter + Cerebras 둘 다 None → 빈 tips/coach (기존 graceful 그대로).
  </behavior>
  <action>
    backend/functions/pipeline/app.py 의 _process 안에서 Plan 03 의 augment_low_confidence 호출 직후 라인에 wave 3 박제. import: `from sunity_shared.gemini.coach_writer_v2 import GeminiCoachWriter`. env 검사: `_gemini_coach_enabled = os.environ.get("GEMINI_COACH_ENABLED", "1") not in ("0", "false", "False", "")`. dual-track 로직은 simple if/else — Gemini 시도 → None 이면 Cerebras. tips/coach 변환은 helper `_convert_coach_payload_to_result(payload: CoachPayload) -> dict` 박제. 기존 result schema 호환 — `result["tips"] = [...]; result["coach"] = {...}` 형태 유지. firestore_admin.complete_analysis 호출에 gemini_b kwarg 추가. 통합 테스트는 monkeypatch + os.environ 패치 — 외부 호출 0. wave 3 의 latency 는 Pro thinking 4k = 10~25s (AI-SPEC §4b), pipeline Lambda 900s 정합.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_pipeline_geminib_wiring.py -x -q</automated>
  </verify>
  <done>통합 테스트 4 케이스 통과: dual-track Gemini→Cerebras fallback, env toggle, 둘 다 None graceful. `grep -n "GeminiCoachWriter" backend/functions/pipeline/app.py | grep -v '^#' | grep -c .` ≥ 2 (import + 호출).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gemini B 출력 코칭 멘트 → 사용자 화면 표시 | LLM 텍스트가 결과 화면 (result.tsx) 의 tips/coach 카드에 직접 박힘. 강사 대체 톤/단정형/부정확한 advice 출력 시 학원 운영자/강사 신뢰 즉시 파괴 (PROJECT.md). |
| RTMW deviation 수치 → Gemini prompt | RTMW 의 8 관절각 deviation 이 prompt 에 평문 박힘. Gemini 가 수치 오인 시 코칭이 잘못된 방향. |
| env GEMINI_COACH_ENABLED → coach path 분기 | env 가 분기 게이트. production 에서 잘못 박히면 모든 분석이 Cerebras 로만 가거나 Gemini 만 가는 단일 path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-16 | Information Disclosure | 강사 대체 톤 코칭 출력 | mitigate | _validate_tone schema 후처리 — coach_note 3 어휘 (강사/함께/확인) 동시 포함 강제 + blocklist (이렇게 하세요/틀렸습니다/당신은) 매치 시 ValueError + 1회 retry. AI-SPEC §1 critical failure mode 3 직격. |
| T-17-17 | Tampering | causes 가 generic 어휘로만 채워짐 | mitigate | 부위별 용어 14개 화이트리스트 — explanation 에 1개 이상 포함 강제. 없으면 retry/폴백. Moveally/MotionMentor 외부 리뷰의 "generic answer = 이탈" 직격. |
| T-17-18 | Tampering | Gemini 객관성 가드 우회 (점수/판단 출력) | mitigate | enforce_object_guard=True (Plan 01 G1) — coach_note 에 "9점", "잘했다" 같은 어휘 출력 시 ValueError. |
| T-17-19 | Repudiation | Gemini vs Cerebras 출처 audit | mitigate | geminiB.model + geminiB.fallback 박제 — Firestore 에서 어떤 LLM 이 어떤 분석 코칭했는지 식별 가능. F6 A/B 분석 입력. |
| T-17-20 | Denial of Service | Gemini retry × 2 + Cerebras retry × N | mitigate | GeminiCoachWriter.write 의 retry max 2 + GeminiVisionCall.retry max 1 = worst case 4 호출. 그 후 즉시 Cerebras 폴백 — 무한 retry 0. |
| T-17-21 | Spoofing | env GEMINI_COACH_ENABLED 외부 조작 | accept | env 는 Lambda/Pod 배포 시 박제 — 외부 사용자 접근 불가. AWS Parameter Store 또는 EAS env 박제 (CLAUDE.md §3 정합). |
</threat_model>

<verification>
- pytest backend/tests/gemini/test_coach_writer_v2.py backend/tests/test_pipeline_geminib_wiring.py -x -q 통과.
- `grep -n "GeminiCoachWriter" backend/functions/pipeline/app.py | grep -v '^#' | grep -c .` ≥ 2 (import + 호출).
- `grep -n "GEMINI_COACH_ENABLED" backend/functions/pipeline/app.py | grep -v '^#' | grep -c .` ≥ 1 (env 토글).
- 강사 보조 톤 회귀: test_coach_writer_v2.py 의 "coach_note 강사 누락" + "이렇게 하세요" blocklist 케이스가 None 반환 (1회 retry 후 Cerebras 폴백 신호).
- 부위별 용어 회귀: explanation 이 "허리를 펴세요" 같은 generic 어휘만 stub 일 때 None 반환.
- dual-track 회귀: GEMINI_COACH_ENABLED=1 + Gemini None 케이스에서 CerebrasCoachWriter.write mock 호출 횟수 == 1.
</verification>

<success_criteria>
1. sunity_shared/gemini/coach_writer_v2.py 신설 — GeminiCoachWriter 클래스 export, CerebrasCoachWriter 와 동일 .write() 시그너처.
2. coach_note 의 "강사" + "함께" + "확인" 3 어휘 schema 강제 — 누락 시 ValueError → retry → None (Cerebras 폴백).
3. 부위별 용어 14개 화이트리스트 — 각 cause.explanation 에 1개 이상 강제.
4. 단정/지시형 blocklist ("이렇게 하세요"/"틀렸습니다"/"당신은") 매치 시 hard reject.
5. dual-track wiring — GEMINI_COACH_ENABLED env 토글 + Gemini None 시 CerebrasCoachWriter 자동 폴백.
6. firestore_admin.complete_analysis(gemini_b=...) 가 Firestore `geminiB` 필드 박제 (causes + coach_note + model + fallback + judgeScore=null).
</success_criteria>

<output>
Create `.planning/phases/17-gemini-vision-integration-4/17-04-SUMMARY.md` when done.
</output>
