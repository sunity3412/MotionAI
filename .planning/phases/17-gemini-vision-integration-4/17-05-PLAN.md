---
phase: 17-gemini-vision-integration-4
plan: 05
type: execute
wave: 5
depends_on:
  - 17-01
  - 17-02
  - 17-03
  - 17-04
files_modified:
  - backend/shared/python/sunity_shared/gemini/reference_extractor.py
  - backend/functions/reference-auto-register/app.py
  - backend/functions/reference-auto-register/requirements.txt
  - backend/template.yaml
  - backend/shared/python/sunity_shared/firestore_admin.py
  - backend/tests/gemini/test_reference_extractor.py
  - backend/tests/test_reference_auto_register_handler.py
autonomous: true
requirements:
  - VISION-01

must_haves:
  truths:
    - "신규 Lambda endpoint POST /reference/auto-register 가 S3 key (정은지 영상) + 학원 통용명 입력으로 호출되면 Gemini Pro 가 IPSF 명칭 + clipRange + checkpointJoints 자동 산출 후 Firestore reference/{motionId} 박제"
    - "분기 라우팅 — (1) IPSF CoP 2025-2027 화이트리스트 매치 시 branch_1_ipsf, (2) belle 라벨링한 분기 2 entry (championPersonalAlias) 시 branch_2_studio, (3) 그 외 branch_3_auto (G3 guardrail 발동 + reviewRequired=True)"
    - "G3 guardrail — IPSF 화이트리스트 미매치 + 분기 2 entry 없음 동시 시 자동 분기 3 fallback + reviewRequired=True + isActive=false 박제 + belle 알림"
    - "endpoint 인증 — Firebase ID token 검증 (기존 sunity_shared.auth 패턴 정합) — belle 만 호출 가능"
    - "동일 영상 재호출 시 Firestore reference doc 의 motionId 가 idempotent — 기존 doc 의 isActive/inactiveReason 은 보존 (belle 검수 결과 유지)"
  artifacts:
    - path: "backend/shared/python/sunity_shared/gemini/reference_extractor.py"
      provides: "영역 A extract_reference_metadata(video_path, studio_alias) → ReferenceRegistration | None + 분기 라우팅"
      contains: "def extract_reference_metadata"
    - path: "backend/functions/reference-auto-register/app.py"
      provides: "신규 Lambda lambda_handler — Firebase Auth + S3 다운로드 + Gemini A + Firestore upsert"
      contains: "def lambda_handler"
    - path: "backend/functions/reference-auto-register/requirements.txt"
      provides: "Lambda 함수 deps — firebase-admin + google-genai (Layer 의존 minimize)"
      contains: "google-genai"
    - path: "backend/template.yaml"
      provides: "SAM resource — ReferenceAutoRegisterFunction + HTTP API route POST /reference/auto-register"
      contains: "ReferenceAutoRegisterFunction"
    - path: "backend/shared/python/sunity_shared/firestore_admin.py"
      provides: "set_reference_motion_with_gemini(motion_id, gemini_a, idempotent=True) 신설 또는 set_reference_motion kwargs 확장"
      contains: "set_reference_motion_with_gemini"
    - path: "backend/tests/gemini/test_reference_extractor.py"
      provides: "영역 A extract + 분기 라우팅 + G3 guardrail 단위 테스트"
      contains: "def test_"
    - path: "backend/tests/test_reference_auto_register_handler.py"
      provides: "Lambda handler 통합 테스트 — 인증/S3/Gemini/Firestore mock"
      contains: "def test_"
  key_links:
    - from: "backend/functions/reference-auto-register/app.py"
      to: "backend/shared/python/sunity_shared/gemini/reference_extractor.py"
      via: "extract_reference_metadata 호출 + 분기 라우팅"
      pattern: "extract_reference_metadata"
    - from: "backend/functions/reference-auto-register/app.py"
      to: "backend/shared/python/sunity_shared/firestore_admin.py"
      via: "set_reference_motion_with_gemini 호출 (idempotent upsert)"
      pattern: "set_reference_motion_with_gemini"
    - from: "backend/template.yaml"
      to: "backend/functions/reference-auto-register/"
      via: "AWS::Serverless::Function CodeUri + Events 박제"
      pattern: "CodeUri: functions/reference-auto-register/"
---

<objective>
Phase 17 Wave 4 (RESEARCH §9 표기) — 영역 A Reference 자동 등록. 신규 Lambda + 신규 SAM HTTP API route `POST /reference/auto-register`. 정은지 영상 S3 key 입력 → Gemini Pro Vision → IPSF 명칭 매칭 + clipRange + checkpointJoints 자동 산출 → Firestore `reference/{motionId}` 박제. 분기 1/2/3 라우팅 (메모리 [[studio-term-3branch-system]] 정합) + G3 guardrail (IPSF 화이트리스트 미매치 시 분기 3 fallback + belle 검수 큐) + Firebase Auth (belle 만 호출).

Purpose: ROADMAP Success #1 "정은지 영상 업로드 → Gemini Vision 이 IPSF 명칭 매칭 + clipRange + checkpoint joint 자동 산출" 직격. 메모리 [[phase-12-tomorrow-bcd-then-13a]] 의 referenceKeypointReport 산출 본 + UAT 2026-06-12 신규 6 motion 의 NLF↔RTMW 호환 깨짐 finding 직격 해소 path (Plan 07 이 신규 6 재활성화에 사용). 영구 박제 — 잘못된 IPSF 명칭/clipRange/checkpointJoints 가 들어가면 모든 mode1 비교 분석이 오염 (AI-SPEC §1b "Output Consequence" 영역 A).
Output: reference_extractor.py 모듈 + 신규 Lambda 함수 + SAM 리소스 + Firestore upsert 시그너처 확장 + 단위/통합 테스트.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-gemini-vision-integration-4/17-AI-SPEC.md
@.planning/phases/17-gemini-vision-integration-4/17-RESEARCH.md
@.planning/phases/17-gemini-vision-integration-4/17-01-SUMMARY.md

# 기존 Lambda 패턴 — auth, validation, S3, Firestore, responses 박제 정합
@backend/functions/upload-url/app.py
@backend/functions/reference-api/app.py
# 기존 SAM template — 신규 함수 resource 박제 정합
@backend/template.yaml
# 기존 firestore_admin — set_reference_motion 위치 확인
@backend/shared/python/sunity_shared/firestore_admin.py
# 기존 seed 패턴 (수동 — A 영역이 자동화) — 출력 필드 호환 박제
@app/scripts/seed-reference-motions.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: extract_reference_metadata + 분기 라우팅 + G3 guardrail + IPSF 화이트리스트 fixture</name>
  <files>
    backend/shared/python/sunity_shared/gemini/reference_extractor.py,
    backend/shared/python/sunity_shared/gemini/ipsf_whitelist.json,
    backend/shared/python/sunity_shared/gemini/studio_branch2_aliases.json,
    backend/tests/gemini/test_reference_extractor.py
  </files>
  <behavior>
    - extract_reference_metadata(video_path, studio_alias: str | None = None, override_motion_id: str | None = None) → dict — {motion_id, motion_name_ipsf, motion_name_ko, clip_range, checkpoint_joints, routing_branch, confidence, model, review_required, raw_response, registered_at}.
    - GeminiVisionCall(schema=ReferenceRegistration, model=env GEMINI_A_MODEL default "gemini-3.1-pro", temperature=0.1, max_output_tokens=2048, thinking_budget=-1, enforce_object_guard=True) 인스턴스화 + call(video_path).
    - Prompt: 영상 + studio_alias (있으면 hint, 없으면 free-form) + IPSF Code of Points 2025-2027 명칭 후보 화이트리스트 (system instruction 박제) + clipRange 정의 (prepStartS/execStartS/execPeakS/landEndS/recommendedRecordS) + 8 관절 checkpointJoints 정의.
    - 분기 라우팅 (호출 후):
      - branch_1_ipsf: result.motion_name_ipsf 가 ipsf_whitelist.json 의 entry 와 정확 매치 (case-insensitive, normalize 공백/hyphen).
      - branch_2_studio: studio_alias 가 studio_branch2_aliases.json 의 key 와 매치 (belle 라벨링한 폭스탑/사이드웨이 스핀 등 한국 학원 통용 비등재 동작).
      - branch_3_auto: 위 둘 다 아니면 — G3 guardrail 발동 → review_required=True, isActive=false, inactiveReason="ipsf_whitelist_miss".
    - ipsf_whitelist.json 초기 박제: AI-SPEC §1b Source 의 IPSF Pole Sports CoP 2025-2027 등재 코드 (예: F101 Inverted Crucifix, F401 Inverted Torso Hook, B201 Butterfly, B202 Cupid, B301 Deadlift, F501 Scorpio 등) — belle 의 NotebookLM IPSF CoP lookup 결과를 박제하되, 초기 fixture 는 ai-spec §5 reference dataset 의 정은지 5건 + AI-SPEC §1b 4번째 dimension 화이트리스트 13개 AKA 매핑 entry 박제. 추가는 belle 가 후속 PR 로 박는다.
    - studio_branch2_aliases.json 초기 박제: belle 라벨링 — "폭스탑", "폭스탑 스플릿", "사이드웨이 스핀" (메모리 [[studio-term-3branch-system]] 직접 박제). 정은지 통용 호칭 도 같이 박는다 (championPersonalAlias).
    - motion_id 결정: override_motion_id 있으면 그대로, 없으면 motion_name_ipsf 또는 studio_alias 의 slug (소문자 + hyphen + ascii) — Firestore key 정합.
    - test_reference_extractor.py: (1) IPSF 매치 stub → branch_1_ipsf + review_required=False, (2) studio_alias="폭스탑" stub → branch_2_studio + review_required=False, (3) IPSF 미매치 + studio_alias 미매치 → branch_3_auto + review_required=True, (4) GeminiVisionCall.call None → 전체 None 반환 (graceful), (5) 같은 영상 2회 호출 → 같은 motion_id (idempotent).
  </behavior>
  <action>
    backend/shared/python/sunity_shared/gemini/reference_extractor.py 신설. 화이트리스트는 JSON 파일 (ipsf_whitelist.json + studio_branch2_aliases.json) 로 박제 — Python 코드 변경 없이 belle 가 PR 로 추가 가능. normalize 함수 — `_normalize_name(s)` (lowercase + 공백/hyphen 제거 + unicode NFKC). 분기 판정 함수 `_route_branch(result, studio_alias)` 박제 — 분기 1/2/3 결정 + review_required + inactiveReason 결정. AI-SPEC §1b "Domain Expert Roles for Evaluation" belle 가 영역 A IPSF 명칭 매핑 + 분기 라우팅 라벨링 주체 — JSON fixture 가 belle 의 수동 라벨링 진입점. 테스트는 monkeypatch 로 GeminiVisionCall.call stub + JSON fixture 는 tmp_path 로 박제 (단위 테스트 격리).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/gemini/test_reference_extractor.py -x -q</automated>
  </verify>
  <done>5 케이스 테스트 통과: 분기 1/2/3 라우팅 + graceful None + idempotent motion_id. ipsf_whitelist.json 초기 entry ≥ 13개 박제 확인. studio_branch2_aliases.json 초기 entry ≥ 3개 박제 확인.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 신규 Lambda reference-auto-register + Firebase Auth + SAM template + Firestore upsert</name>
  <files>
    backend/functions/reference-auto-register/app.py,
    backend/functions/reference-auto-register/requirements.txt,
    backend/template.yaml,
    backend/shared/python/sunity_shared/firestore_admin.py,
    backend/tests/test_reference_auto_register_handler.py
  </files>
  <behavior>
    - POST /reference/auto-register body: {"s3Key": "reference/<motion>/<video>.mp4", "studioAlias": "폭스탑" | null, "overrideMotionId": "ref-..." | null}.
    - Firebase ID token 검증 (`Authorization: Bearer <token>`) → sunity_shared.auth.verify_token. 401 if missing/invalid. 추가 — belle uid 화이트리스트 (env BELLE_UID 박제) 만 통과, 아니면 403.
    - S3 download (boto3 get_object) — VIDEO_BUCKET env, key 는 body.s3Key. 로컬 /tmp/<analysis_id>.mp4 박제. AI-SPEC §3 Pitfall #2 정합 — presigned URL 직접 X.
    - extract_reference_metadata(local_path, studio_alias, override_motion_id) 호출.
    - 결과 None 이면 500 server_error 반환 (Gemini 실패) — graceful 단 caller (belle) 가 재시도 가능.
    - 결과 dict → firestore_admin.set_reference_motion_with_gemini(motion_id, gemini_a=result, idempotent=True) 호출. Firestore reference/{motionId} 박제:
      - 기존 doc 있으면 — `geminiA` 필드만 갱신, isActive/inactiveReason 보존 (belle 검수 결과 유지). G3 trigger 인 경우만 inactiveReason 갱신.
      - 새 doc 면 — geminiA + isActive=False (G3=True 시 false, G3=False 시 true) + inactiveReason 박제.
    - 응답 200: {"motionId", "routingBranch", "reviewRequired", "geminiA": {model, motion_name_ipsf, clip_range, checkpoint_joints, confidence}}.
    - requirements.txt: firebase-admin (Lambda Layer 박혀있음 — 비박제), google-genai>=1.0,<2.0 (Layer 박혀있음), pydantic>=2.5,<3.0 (Layer 박혀있음). Layer 의존 verify — sunity_shared 가 Layer 박혀있는지 확인 후 minimal requirements (boto3 빼고).
    - template.yaml: ReferenceAutoRegisterFunction 신설. Runtime python3.12, Architectures arm64, Timeout 120s (Files API 폴링 ~120s + Pro 호출 ~15s 정합), MemorySize 512MB, Layers (sunity_shared Layer ref), Environment Variables (VIDEO_BUCKET, FIREBASE_SA_PARAM, GEMINI_A_MODEL, BELLE_UID), Events: ApiPost POST /reference/auto-register on HttpApi. HttpApi 는 기존 reference-api 가 박은 자원 reuse — Ref ApiResource 확인.
    - test_reference_auto_register_handler.py: (1) 정상 호출 — auth/S3/Gemini/Firestore 전부 mock → 200 + body 검증, (2) 인증 실패 (token missing) → 401, (3) BELLE_UID 미일치 → 403, (4) S3 NoSuchKey → 404, (5) Gemini None → 500 server_error, (6) 같은 motion_id 2회 호출 → 첫 호출 isActive=False, 두 번째 호출에서도 isActive 유지 (idempotent — belle 검수 흐름 보호).
  </behavior>
  <action>
    backend/functions/reference-auto-register/app.py 신설. 기존 backend/functions/upload-url/app.py 의 lambda_handler 패턴 박제 — auth → validate → S3/Firestore → respond. 인증은 sunity_shared.auth.verify_token (기존 함수). BELLE_UID env 검사 추가 — belle 만 호출 가능 (학원 운영자/수강생은 X). S3 download 는 boto3.client('s3').get_object + body.read() → /tmp 박제 (AI-SPEC §3 Pitfall #2 박제: presigned URL 직접 X). firestore_admin.py 에 set_reference_motion_with_gemini(motion_id, gemini_a, idempotent=True) 신설 — 기존 doc 있으면 update_data 가 `geminiA`/`isActive`/`inactiveReason` 만 갱신 (다른 필드 보존), 없으면 create. backend/template.yaml 에 ReferenceAutoRegisterFunction resource 추가 — 기존 ReferenceApiFunction 의 정의 패턴 참조. SAM HttpApi route 박제 — 기존 ApiResource (`HttpApi`) reuse. requirements.txt 는 google-genai 만 박는다 (Layer 박혀있는 firebase-admin/pydantic/boto3 제외). 통합 테스트는 monkeypatch — boto3.client / firebase_admin.auth.verify_id_token / GeminiVisionCall.call / firestore_admin.set_reference_motion_with_gemini 전부 stub.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_reference_auto_register_handler.py -x -q</automated>
  </verify>
  <done>6 케이스 테스트 통과: 정상/인증/권한/S3/Gemini/idempotent. backend/template.yaml 에 ReferenceAutoRegisterFunction resource + POST /reference/auto-register HttpApi route 박힘. `grep -c "ReferenceAutoRegisterFunction" backend/template.yaml` ≥ 2 (resource 정의 + Ref 가 있다면). `sam validate -t backend/template.yaml` (Docker 없으면 best-effort skip — pytest 가 핵심 게이트).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP API → Lambda handler | 외부 인터넷 요청이 endpoint 진입. Firebase ID token + BELLE_UID 화이트리스트 = 이중 인증. |
| S3 reference 영상 → Lambda /tmp | S3 객체가 Lambda /tmp 에 다운로드. Lambda 격리 환경 신뢰. |
| Gemini A 출력 → Firestore reference doc | LLM 출력이 영구 reference DB 에 박힘. 모든 후속 mode1 비교의 baseline. blast radius = 전체 분석 (AI-SPEC §1b "Output Consequence"). |
| Gemini IPSF 명칭 환각 → 분기 1 강제 매핑 | LLM 이 존재하지 않는 IPSF 코드 생성 (예: F999) 시 reference 영구 박제 위험. G3 guardrail 의 화이트리스트 가드가 1차 차단. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-22 | Spoofing | endpoint 무권한 호출 | mitigate | Firebase ID token 검증 (sunity_shared.auth) + BELLE_UID env 화이트리스트 — 이중 인증. token verify 실패 401, uid 미일치 403. |
| T-17-23 | Tampering | Gemini IPSF 코드 환각 | mitigate | G3 guardrail — ipsf_whitelist.json 매치 강제. 미매치 시 자동 분기 3 fallback + reviewRequired=True + isActive=false + belle 알림 (Plan 06 의 P0 alert 박제). 영구 박제 차단. |
| T-17-24 | Information Disclosure | reference doc 의 S3 영상 URL 누출 | accept | reference bucket 은 belle 가 박은 정은지 영상 — 익명 사용자 접근 불가 (presigned URL 7일, AWS 권한). 기존 reference-api Lambda 의 path 와 동일 박제. |
| T-17-25 | Tampering | 동일 motion_id 재호출 시 belle 검수 결과 덮어쓰기 | mitigate | set_reference_motion_with_gemini(idempotent=True) — 기존 doc 의 isActive/inactiveReason 보존, geminiA 만 갱신. belle 가 한 번 isActive=true 박은 후 재호출해도 검수 결과 유지. |
| T-17-26 | Repudiation | reference 등록 audit | mitigate | geminiA.registered_at + geminiA.raw_response (원본 Gemini JSON) Firestore 박제. 후속 belle 검수 시 어떤 prompt 출력이 박혔는지 추적 가능. |
| T-17-27 | Denial of Service | belle 가 N 영상 동시 호출 (HTTP API rate limit) | accept | belle 1인 호출 — production 부하 0. HTTP API 기본 throttling (10000 RPS) 정합. |
</threat_model>

<verification>
- pytest backend/tests/gemini/test_reference_extractor.py backend/tests/test_reference_auto_register_handler.py -x -q 통과.
- `grep -c "ReferenceAutoRegisterFunction" backend/template.yaml` ≥ 1 (resource 정의).
- `grep -n "POST.*reference/auto-register\|Path:.*reference/auto-register" backend/template.yaml | grep -v '^#' | grep -c .` ≥ 1 (HttpApi route).
- `grep -n "set_reference_motion_with_gemini" backend/shared/python/sunity_shared/firestore_admin.py | grep -v '^#' | grep -c .` ≥ 1.
- G3 guardrail 회귀: test_reference_extractor.py 의 "IPSF 미매치 + studio_alias 미매치" 케이스가 routing_branch="branch_3_auto" + review_required=True 반환.
- Idempotent 회귀: test_reference_auto_register_handler.py 의 "같은 motion_id 2회" 케이스에서 Firestore mock 의 set_reference_motion_with_gemini 호출이 2번이지만 isActive 가 초기값 유지 (mock assertion).
- Fixture 박제: ipsf_whitelist.json entry ≥ 13개, studio_branch2_aliases.json entry ≥ 3개 (`jq 'length' backend/shared/python/sunity_shared/gemini/ipsf_whitelist.json`).
</verification>

<success_criteria>
1. backend/functions/reference-auto-register/app.py 신설 — lambda_handler export.
2. backend/template.yaml 에 ReferenceAutoRegisterFunction resource + HttpApi POST /reference/auto-register route 박힘.
3. extract_reference_metadata 가 분기 1/2/3 라우팅 정확 — IPSF 화이트리스트 매치 / studio_alias 매치 / G3 fallback.
4. G3 guardrail — IPSF 미매치 + 분기 2 미매치 시 자동 분기 3 + reviewRequired=True + isActive=false.
5. Firebase ID token + BELLE_UID 화이트리스트 이중 인증 — belle 만 endpoint 호출 가능.
6. set_reference_motion_with_gemini(idempotent=True) — belle 검수 결과 (isActive/inactiveReason) 재호출 시 보존.
7. ipsf_whitelist.json + studio_branch2_aliases.json 초기 fixture 박제 — belle 후속 PR 로 entry 추가 path 박힘.
</success_criteria>

<output>
Create `.planning/phases/17-gemini-vision-integration-4/17-05-SUMMARY.md` when done.
</output>
