---
phase: 17-gemini-vision-integration-4
plan: 06
type: execute
wave: 6
depends_on:
  - 17-01
  - 17-02
  - 17-03
  - 17-04
  - 17-05
files_modified:
  # ── Sub-phase 06A — telemetry deps + optional import phoenix_setup (W1 정합) ──
  - backend/runpod_inference/requirements.txt
  - backend/functions/pipeline/requirements.txt
  - backend/shared/python/requirements.txt
  - backend/shared/python/sunity_shared/eval/__init__.py
  - backend/shared/python/sunity_shared/eval/phoenix_setup.py
  - backend/tests/eval/__init__.py
  - backend/tests/eval/test_phoenix_setup.py
  # ── Sub-phase 06B — span event wiring on 4 영역 client path ──
  - backend/shared/python/sunity_shared/gemini/client.py
  - backend/shared/python/sunity_shared/gemini/scene_finder.py
  - backend/shared/python/sunity_shared/gemini/coach_writer_v2.py
  - backend/shared/python/sunity_shared/gemini/reference_extractor.py
  - backend/shared/python/sunity_shared/eval/llm_judge.py
  - backend/shared/python/sunity_shared/eval/sampling.py
  - backend/tests/eval/test_llm_judge.py
  - backend/tests/eval/test_sampling.py
  # ── Sub-phase 06C — Promptfoo local eval dataset + assertions (W6: CI 게이트는 별도 plan) ──
  - backend/evals/phase17/promptfooconfig.yaml
  - backend/evals/phase17/dataset/reference_dataset.yaml
  - backend/evals/phase17/dataset/labels.json
  - backend/evals/phase17/assertions/objectivity_reject.py
  - backend/evals/phase17/assertions/ipsf_routing.py
  - backend/evals/phase17/assertions/coach_tone.py
  - backend/evals/phase17/README.md
autonomous: true
requirements:
  - VISION-01
  - VISION-02
  - VISION-03
  - VISION-04

must_haves:
  truths:
    - "Arize Phoenix 셀프호스트 (Pod in-process 또는 OTLP HTTP exporter) 가 google-genai SDK 자동 계측으로 4 영역의 latency/cost/parse_success/guardrail trigger span 을 수집한다"
    - "**Sub-phase 06A (W1 정합)** — arize-phoenix / openinference-instrumentation-google-genai / opentelemetry-* 패키지가 3개 runtime requirements.txt (Pod / Lambda pipeline / shared layer) 에 박힌다. `bootstrap_tracing` 은 optional import — telemetry extras 미설치 시 warning + noop, 분석 흐름 차단 X."
    - "**Sub-phase 06C (W6 정합)** — Promptfoo config 는 **local eval config** (belle/Claude 가 PR 작성 전 수동 실행). 실제 CI gate (.github/workflows/phase17-evals.yml) 는 본 Phase 17 scope 밖 — 별도 plan 으로 후속 진입. PR block 표현은 본 plan 박제 X."
    - "Promptfoo dataset 30 examples (영역 × 시나리오 매트릭스) — 임계값: E1 (객관성) 100% / E6 (정은지 게이트) 100% / E2~E5/E7/E8 AI-SPEC §5 박제. local eval 실패 시 belle 가 PR 작성 보류 (수동 게이트)."
    - "LLM judge (Gemini Flash 0.0) 가 영역 B 코칭 톤 binary pass 산출 — belle binary 라벨 30건 calibration correlation ≥ 0.7"
    - "Smart sampling 가중치 (signal 기반) — isReference=True 100% / guardrail trigger 100% / 양극단 score 20% / 영역 C flag True 15% / 그 외 2% baseline 가 Firestore eval/phase17 박제"
    - "online guardrail 6개 (G1~G6) 가 client.py + scene_finder.py + coach_writer_v2.py + reference_extractor.py 의 호출 path 에 박혀 Phoenix span event 로 추적된다 (telemetry extras 설치 시)"
  artifacts:
    - path: "backend/shared/python/sunity_shared/eval/phoenix_setup.py"
      provides: "Phoenix OpenTelemetry tracer 부트스트랩 + google-genai auto-instrument"
      contains: "GoogleGenAIInstrumentor"
    - path: "backend/shared/python/sunity_shared/eval/llm_judge.py"
      provides: "Gemini Flash judge — 영역 B 코칭 톤 binary pass 산출 + belle 라벨 correlation 측정"
      contains: "def judge_coach_tone"
    - path: "backend/shared/python/sunity_shared/eval/sampling.py"
      provides: "Smart sampling 가중치 함수 + Firestore eval/phase17 박제"
      contains: "def should_sample"
    - path: "backend/evals/phase17/promptfooconfig.yaml"
      provides: "Promptfoo **local eval** config + 30 examples reference (2차 R-W1 정합 — CI 자동 게이트 별도 후속 plan)"
      contains: "assertions:"
    - path: "backend/evals/phase17/dataset/reference_dataset.yaml"
      provides: "영역 × 시나리오 매트릭스 30건 fixture (정은지 5/분기2 3/분기3 2/calibration 10/Finding 20/D 10/E6 5)"
      contains: "scenarios:"
    - path: "backend/evals/phase17/assertions/objectivity_reject.py"
      provides: "G1 객관성 reject regex Promptfoo assertion"
      contains: "def assert_no_reject_patterns"
  key_links:
    - from: "backend/shared/python/sunity_shared/eval/phoenix_setup.py"
      to: "backend/shared/python/sunity_shared/gemini/client.py"
      via: "GoogleGenAIInstrumentor().instrument() 가 GeminiVisionCall.call 의 generate_content 호출을 자동 span 화"
      pattern: "GoogleGenAIInstrumentor"
    - from: "backend/shared/python/sunity_shared/gemini/client.py"
      to: "Phoenix span event"
      via: "guardrail trigger 시 span.add_event('guardrail.G1.triggered') / 'gemini.parse_success'"
      pattern: "add_event"
    - from: "backend/evals/phase17/promptfooconfig.yaml"
      to: "backend/evals/phase17/assertions/*.py"
      via: "external assertion script 호출 — E1/E2/E3/E4 게이트 검증"
      pattern: "type: javascript|type: python"
---

<objective>
Phase 17 Wave 5 (RESEARCH §9 표기) — Eval + Guardrail wiring. AI-SPEC §5 (E1~E8 8 dimension) + §6 (G1~G6 6 guardrail) + §7 (Phoenix 5 metric + signal sampling) 박제 전체 코드화. Reference dataset 30 examples + Promptfoo **local eval** config (2차 R-W1 정합 — CI 자동 게이트 별도 후속 plan) + LLM judge (영역 B 톤) + Smart sampling. online guardrail 은 client.py / scene_finder.py / coach_writer_v2.py / reference_extractor.py 에 박힌 G1/G3/G4/G5 를 Phoenix span event 로 추적 + G2/G6 graceful 폴백 박제.

Purpose: AI-SPEC 의 evaluation strategy 가 코드로 박혀야 (1) **belle/Claude 가 PR 작성 전 수동 로컬 실행** 으로 객관성 위반/정은지 위양성 0건 검증 (자동 CI 게이트 X — Phase 17 scope 밖, 별도 후속 plan), (2) Production 신호 가시화 — belle 가 어떤 영역이 얼마나 정확한지 4-day rolling 으로 확인, (3) E5 auto-escalation gate 입력 — production 4-day rolling sample 의 E5 PASS rate 측정 (실제 승급 자체는 runtime config = Firestore `visionConfig.regionC.model` 또는 SSM, AI-SPEC §5 E5 정합. env 자동 set X). PROJECT.md "고수가 낮게 나오는 위양성 없이" + "정은지 영상 위양성" 직격.
Output: backend/shared/python/sunity_shared/eval/ 신설 + backend/evals/phase17/ dataset/config/assertions + 4 영역 호출 path 에 Phoenix span event 박제 + 단위 테스트.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<!--
  2차 R-Plan 06 split 결정 (2026-06-12, Codex caveat 정합):

  Codex 2차 review 는 17-06A/06B/06C 별도 파일 split 을 권장했으나,
  본 repo `.planning/phases/` 에 alphanumeric plan ID (예: 06A) 박힌 파일
  0건 — 전부 정수 (01-25, 05-00 등). GSD execute-phase tooling 의 frontmatter
  `plan: <int>` 파싱이 alphanumeric 거부 가능성 hard 검증 없이 박제 위험.

  대안 (renumber 17-06/17-08/17-09): 기존 17-07 (reactivate) 가 중간에
  끼어 wave 순서/depends_on 그래프 재작성 → blast radius 큼.

  결정: 현재 in-file Sub-phase 06A/06B/06C 구조 유지 + Task 별 summary
  checkpoint 박제로 결정 박제 ("Task 1 완료 후 06A summary 박제 + 06B 진입
  전 belle 검수 박제"). execute-phase 시점에 06A 만 먼저 검증 가능하도록
  Task 1/1b/2/3 가 각자 독립 verify command + done gate 박제.

  Codex 권장의 핵심 가치 (06A 조기 검증, 06C 작업이 wiring 과 섞이지 않음)
  는 in-file Task 단위 checkpoint 로 동등 박제. 외부 review 가 다시 split
  hard requirement 박제하면 그때 GSD tooling 검증 후 재진입.
-->


<context>
@.planning/PROJECT.md
@.planning/phases/17-gemini-vision-integration-4/17-AI-SPEC.md
@.planning/phases/17-gemini-vision-integration-4/17-RESEARCH.md
@.planning/phases/17-gemini-vision-integration-4/17-01-SUMMARY.md
@.planning/phases/17-gemini-vision-integration-4/17-02-SUMMARY.md
@.planning/phases/17-gemini-vision-integration-4/17-03-SUMMARY.md
@.planning/phases/17-gemini-vision-integration-4/17-04-SUMMARY.md
@.planning/phases/17-gemini-vision-integration-4/17-05-SUMMARY.md

# 영역별 호출 path — Phoenix span event 박을 위치 확인
@backend/shared/python/sunity_shared/gemini/client.py
@backend/shared/python/sunity_shared/gemini/scene_finder.py
@backend/shared/python/sunity_shared/gemini/coach_writer_v2.py
@backend/shared/python/sunity_shared/gemini/reference_extractor.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Sub-phase 06A): telemetry deps + Phoenix optional-import 부트스트랩 (W1 정합)</name>
  <files>
    backend/runpod_inference/requirements.txt,
    backend/functions/pipeline/requirements.txt,
    backend/shared/python/requirements.txt,
    backend/shared/python/sunity_shared/eval/__init__.py,
    backend/shared/python/sunity_shared/eval/phoenix_setup.py,
    backend/tests/eval/__init__.py,
    backend/tests/eval/test_phoenix_setup.py
  </files>
  <behavior>
    - **3개 runtime requirements.txt 박제 (W1 정합)** — 각 runtime 에 신규 패키지 추가:
      - `backend/runpod_inference/requirements.txt`: `arize-phoenix>=4.0,<5.0`, `openinference-instrumentation-google-genai>=0.1,<1.0`, `opentelemetry-sdk>=1.27,<2.0`, `opentelemetry-exporter-otlp-proto-http>=1.27,<2.0`. Pod 가 in-process Phoenix 호스트 (px.launch_app) 가능.
      - `backend/functions/pipeline/requirements.txt`: OTLP exporter 만 (`opentelemetry-sdk` + `opentelemetry-exporter-otlp-proto-http` + `openinference-instrumentation-google-genai`). Lambda 는 in-process Phoenix 안 띄움 — 외부 endpoint 로 전송만.
      - `backend/shared/python/requirements.txt`: 양쪽 공통 의존 (`opentelemetry-api>=1.27,<2.0`) — Lambda Layer 에 박제.
    - phoenix_setup.py: **optional import 가드 박제** — top 에서 `try: import phoenix as px; from openinference.instrumentation.google_genai import GoogleGenAIInstrumentor; from opentelemetry import trace; ...; _TELEMETRY_OK = True except ImportError as e: log.warning("Phase 17 telemetry extras 미설치: %s — bootstrap_tracing noop", e); _TELEMETRY_OK = False`. import 실패 시 module-level 상수 + bootstrap_tracing 이 noop 반환 (분석 흐름 차단 0).
    - bootstrap_tracing(in_process: bool | None = None) — `if not _TELEMETRY_OK: return` 첫 줄. 이후 env PHOENIX_INPROCESS=="1" 면 px.launch_app() (Pod in-process), 아니면 OTLPSpanExporter(endpoint=env PHOENIX_OTLP_ENDPOINT). env PHOENIX_OTLP_ENDPOINT 미설정 + in_process False 면 graceful noop (no-op TracerProvider).
    - bootstrap_tracing 은 idempotent — 두 번 호출되어도 TracerProvider/Instrumentor 한 번만 박는다 (provider 가 이미 SET 이면 skip).
    - `eval/__init__.py` 에 `from .phoenix_setup import bootstrap_tracing, TELEMETRY_OK` 박제 — caller 가 `if TELEMETRY_OK:` 검사 가능.
    - test_phoenix_setup.py: (1) env PHOENIX_INPROCESS=1 stub → px.launch_app 호출 1회, (2) env PHOENIX_OTLP_ENDPOINT 박힘 → OTLPSpanExporter 박힘, (3) 둘 다 미설정 → graceful noop (예외 X), (4) 2회 호출해도 instrument 1회만 (idempotent), (5) **W1 회귀**: `_TELEMETRY_OK=False` stub (deps 미설치 시뮬) → bootstrap_tracing(...) 노예외 + 모든 분기 noop, (6) **W1 회귀**: `pytest.importorskip("phoenix")` skip 으로 "telemetry extras installed" 검증 sub-test 별도 박제 — CI 에서 extras 설치 회귀 단독 게이트.
  </behavior>
  <action>
    각 runtime requirements.txt 3개에 telemetry extras 박제. AI-SPEC §5 Phoenix 부트스트랩 코드 박제를 backend/shared/python/sunity_shared/eval/phoenix_setup.py 신설로 가져오되, **module-level optional import 가드 박제** — telemetry extras 가 Pod/Lambda 어느 한쪽에 누락돼도 import 자체가 깨지지 않게. eval/__init__.py 가 TELEMETRY_OK 노출. test 는 monkeypatch 로 px.launch_app / OTLPSpanExporter / GoogleGenAIInstrumentor stub + sys.modules 조작으로 ImportError 시뮬.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/eval/test_phoenix_setup.py -x -q</automated>
  </verify>
  <done>Phoenix 부트스트랩 6 케이스 (정상 4 + W1 optional import 2) 통과. `grep -n "arize-phoenix" backend/runpod_inference/requirements.txt backend/functions/pipeline/requirements.txt backend/shared/python/requirements.txt | grep -c .` ≥ 1. `python -c "from sunity_shared.eval import bootstrap_tracing, TELEMETRY_OK; bootstrap_tracing()"` 통과 (extras 미설치 환경에서도).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1b (Sub-phase 06B): 4 영역 client 의 guardrail span event 박제</name>
  <files>
    backend/shared/python/sunity_shared/gemini/client.py,
    backend/shared/python/sunity_shared/gemini/scene_finder.py,
    backend/shared/python/sunity_shared/gemini/coach_writer_v2.py,
    backend/shared/python/sunity_shared/gemini/reference_extractor.py
  </files>
  <behavior>
    - client.py 갱신: GeminiVisionCall.call 안에 OpenTelemetry tracer 가져와 (`trace.get_tracer(__name__)`) span 박제 — generate_content 호출 자체는 GoogleGenAIInstrumentor 가 자동 span 화하므로, 우리는 추가 attribute (gemini.region, gemini.parse_success, gemini.retry_count) + event (guardrail.G1.triggered, guardrail.G2.fallback, guardrail.G5.coord_blocked) 박는다.
    - scene_finder.py 갱신: G4 guardrail 발동 시 span.add_event("guardrail.G4.reference_occlusion_fp", attributes={"motion_id": ...}).
    - coach_writer_v2.py 갱신: 강사 톤 검증 실패 (1회 retry 후 `{"_fallbackReason": "tone_validation_failed"}` 반환) 시 span.add_event("guardrail.G2.tone_validation_fallback").
    - reference_extractor.py 갱신: G3 guardrail (IPSF 화이트리스트 miss) 발동 시 span.add_event("guardrail.G3.routing_branch3_auto").
    - **W1 정합 — TELEMETRY_OK gate**: 모든 span 박제 라인 앞에 `if TELEMETRY_OK:` 박제 (또는 noop tracer 사용). telemetry extras 미설치 시 분석 흐름 차단 0.
  </behavior>
  <action>
    client.py / scene_finder.py / coach_writer_v2.py / reference_extractor.py 에 OpenTelemetry tracer import + span attribute/event 박제 추가 — 기존 graceful 폴백 path 그대로 두고 trace 만 박는다. 본 task 의 test 는 Task 1 의 test_phoenix_setup.py 가 stub 박은 span recorder 로 검증 (span event name 4개 박혀있는지 mock assertion 박제).
  </action>
  <verify>
    <automated>cd backend && grep -n "add_event.*guardrail" backend/shared/python/sunity_shared/gemini/*.py | grep -v "^#" | grep -c .</automated>
  </verify>
  <done>4 영역 file 에 guardrail span event 박힘 — `grep -n "add_event.*guardrail" backend/shared/python/sunity_shared/gemini/ -r | grep -v '^#' | grep -c .` ≥ 4 (G1/G2/G3/G4 최소 4 event). TELEMETRY_OK gate 박힘 — `grep -n "if TELEMETRY_OK" backend/shared/python/sunity_shared/gemini/ -r | grep -c .` ≥ 4.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LLM judge (영역 B 톤) + Smart sampling + Firestore eval 박제</name>
  <files>
    backend/shared/python/sunity_shared/eval/llm_judge.py,
    backend/shared/python/sunity_shared/eval/sampling.py,
    backend/tests/eval/test_llm_judge.py,
    backend/tests/eval/test_sampling.py
  </files>
  <behavior>
    - llm_judge.judge_coach_tone(coach_payload: dict, belle_label: bool | None = None) → dict — {pass: bool, confidence: float, reasoning_ko: str, judge_model: "gemini-3.5-flash"}.
    - GeminiVisionCall (영상 없이 텍스트 only — judge prompt + coach_payload JSON 텍스트) 사용. temperature=0.0, max_output_tokens=256, thinking_budget=0, enforce_object_guard=False (judge 의 "잘했다" 같은 어휘는 reasoning 안에 박힐 수 있음 — 객관성 가드 우회).
    - judge prompt: "다음 코칭 멘트가 (1) 강사 보조 톤 (단정/지시형 아님), (2) 원인 → 해결 순서, (3) 부위별 용어 사용 3 조건 모두 충족하면 pass=true, 1개라도 미달이면 pass=false 로 binary 판정 후 한국어 reasoning 1~2 문장."
    - belle_label 박혀있으면 calibration mode — judge.pass vs belle_label 일치 여부를 confidence 박제 후 (correlation 산출 시 사용).
    - sampling.should_sample(analysis_doc) → tuple[bool, str] — (샘플링 여부, 이유 라벨).
    - sampling rule (AI-SPEC §7 Smart Sampling 표 정합):
      - isReference=True → (True, "reference_100"), weight 1.0.
      - guardrail_triggered 박혀있음 → (True, "guardrail_100"), weight 1.0.
      - score < 30 or score > 95 → (random < 0.2, "score_extreme_20"), weight 0.2.
      - geminiB.causes len == 3 (최소값 — generic 의심) → (random < 0.3, "causes_min_30").
      - geminiC.occlusion_severe or camera_angle_problematic → (random < 0.15, "flag_true_15").
      - geminiA.routing_branch == "branch_3_auto" → (True, "branch3_100"), weight 1.0.
      - else → (random < 0.02, "baseline_2").
    - 샘플 박힘 시 Firestore eval/phase17/{analysis_id} 박제 — analysis_id + reason + sampled_at + 영역별 결과 dump.
    - test_llm_judge.py: (1) coach_payload 정상 — pass=True, (2) blocklist 어휘 stub — pass=False, (3) belle_label 일치 — confidence 1.0, (4) belle_label 불일치 — confidence 0.0, (5) judge GeminiVisionCall None — graceful (pass=None).
    - test_sampling.py: 각 rule 별 케이스 6개 + random seed 박제.
  </behavior>
  <action>
    backend/shared/python/sunity_shared/eval/llm_judge.py 신설. GeminiVisionCall 베이스 재사용하되 video_path 없이 prompt 만 (텍스트 모드 — GeminiVisionCall.call 의 시그너처 확장 필요? — 박제: 별도 generate_content_text(prompt) helper 박제 (client.py 갱신). 단순화: judge_coach_tone 안에서 genai.Client 직접 사용 — text-only 호출은 Files API 우회 (영상 없음). sampling.py 의 random 은 random.random() — test 에서 monkeypatch 로 seed. Firestore 박제는 firestore_admin.write_eval_sample(analysis_id, reason, payload) helper 박제 (firestore_admin 갱신 필요 — task 안에서 함수 추가). 단위 테스트는 monkeypatch — 외부 호출 0.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/eval/test_llm_judge.py tests/eval/test_sampling.py -x -q</automated>
  </verify>
  <done>llm_judge 5 케이스 + sampling 6 케이스 통과. judge_coach_tone 의 belle_label calibration mode 동작. Firestore eval/phase17 박제 함수 mock assertion 통과.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (Sub-phase 06C): Promptfoo **local eval** config + 30-example reference dataset + assertion scripts (W6 정합 — CI gate 는 별도 plan)</name>
  <files>
    backend/evals/phase17/promptfooconfig.yaml,
    backend/evals/phase17/dataset/reference_dataset.yaml,
    backend/evals/phase17/dataset/labels.json,
    backend/evals/phase17/assertions/__init__.py,
    backend/evals/phase17/assertions/objectivity_reject.py,
    backend/evals/phase17/assertions/ipsf_routing.py,
    backend/evals/phase17/assertions/coach_tone.py,
    backend/evals/phase17/README.md
  </files>
  <behavior>
    - **W6 정합 — local eval 박제**: 본 task 의 Promptfoo config 는 belle/Claude 가 PR 작성 전 **수동 로컬 실행** 하는 게이트. 실제 GitHub Actions / CI/CD merge block 은 본 Phase 17 scope 밖 — 별도 plan ("Phase 17C — eval CI integration" 또는 후속 Phase) 으로 진입. 본 task 는 dataset + assertion + config + README 코드만 박제. PR pre-merge "block" 표현 박제 0건.
    - reference_dataset.yaml: AI-SPEC §5 Reference Dataset 박제 매트릭스 30건 (영역 A: 정은지 IPSF 5건 + 분기2 3건 + 분기3 2건, 영역 B: calibration 10건 + 정은지 5건, 영역 C: 그립 5/occlusion 5/측면 5/스피닝 5 = 20건, 영역 D: 저신뢰 10건, E6: 정은지 hard gate 5건 — 중첩 박힘). 각 entry: {scenario_id, region, video_s3_key (또는 placeholder), expected_label}.
    - labels.json: AI-SPEC §5 Labeling Approach 박제 — belle/정은지/강사/운영자 별 라벨링 sheet. Phase 17 진입 시점에 정은지 5건 라벨링은 박혀있음 (메모리 [[phase-12-tomorrow-bcd-then-13a]] referenceKeypointReport 산출 본 재사용), 나머지 25건은 학원 파일럿 진입 시 belle 가 PR 로 추가 (placeholder + TODO 박제).
    - objectivity_reject.py: AI-SPEC §6 G1 reject regex 검증 — output 텍스트에 점수/좌표/판단 어휘 1건이라도 매치 시 fail. 영역 D 만 좌표 skip.
    - ipsf_routing.py: AI-SPEC §5 E2/E3 — motion_name_ipsf 가 화이트리스트 매치 + routing_branch 가 label 과 일치 confusion matrix.
    - coach_tone.py: AI-SPEC §5 E4 — Plan 04 의 _validate_tone 함수 재사용 (부위별 용어 + coach_note 3 어휘 + blocklist).
    - promptfooconfig.yaml: providers (sunity-shared 가 wrap — Promptfoo custom provider 박제, GeminiVisionCall 호출 위임), tests (reference_dataset.yaml 참조), assertions (objectivity_reject + ipsf_routing + coach_tone 호출). **local eval 임계값** (사용자 수동 검사용): E1=100% / E2≥90% / E3≥90% / E4≥85% binary pass + judge correlation≥0.7 / E5 precision≥0.95 recall≥0.95 / E6=100% / E7≥90% / E8 p95 latency≤40s + per-call cost≤$0.08 (AI-SPEC §5 박제). 미달 시 belle 가 PR 작성 보류 (자동 block 0 — W6 정합).
    - README.md: belle/Claude 가 Promptfoo 실행하는 절차 박제 — `cd backend/evals/phase17 && promptfoo eval --config promptfooconfig.yaml`. **CI 자동 게이트 (.github/workflows/phase17-evals.yml) 는 본 plan 박제 X** — 별도 후속 plan 으로 박제. README 에 명시.
  </behavior>
  <action>
    backend/evals/phase17/ 디렉토리 신설. promptfooconfig.yaml 의 providers 는 custom Python provider 박제 (Promptfoo 가 child process 로 호출). dataset YAML 은 30 entry — 각 entry 의 video_s3_key 는 s3://sunity-motion-pilot-videos/reference/<motion>/<video>.mp4 또는 calibration set 용 placeholder. labels.json 은 정은지 5건 박제 (메모리 [[phase-12-tomorrow-bcd-then-13a]] referenceKeypointReport — `find . -name "extract_reference_keypoint_reports*"` 로 박혀있는 산출본 재사용) + 나머지 25건 TODO 박제. assertion script 3개는 Python — Promptfoo 의 external assertion 지원 박제. README.md 박제 — belle 실행 절차 + **"본 plan = local eval. CI 자동 게이트 = 별도 후속 plan"** 명시.
  </action>
  <verify>
    <automated>cd backend/evals/phase17 && python -c "import yaml; d=yaml.safe_load(open('dataset/reference_dataset.yaml')); assert len(d['scenarios'])>=30, f'dataset {len(d[\"scenarios\"])}<30'" && python -m pytest backend/evals/phase17/assertions/ -x -q 2>/dev/null || python -c "from backend.evals.phase17.assertions.objectivity_reject import assert_no_reject_patterns; assert assert_no_reject_patterns('이 자세는 좋습니다')['pass']==True"</automated>
  </verify>
  <done>reference_dataset.yaml entry ≥ 30개. assertion 3개 모듈 import 성공 + 객관성/라우팅/톤 helper 함수 호출 가능. promptfooconfig.yaml 의 assertions 섹션에 3개 external script 박힘. README.md 박제 — belle 실행 절차 명시 + CI 게이트 별도 plan 박제 (W6 정합). `grep -i "pr.*block\|merge.*block\|pre-merge" backend/evals/phase17/ -r | grep -v "_block_\|README\|"block 0"" | wc -l` == 0 (PR block 표현 0건).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phoenix collector → 외부 OTLP endpoint | OpenTelemetry span 이 self-host Phoenix 로 전송. PHOENIX_OTLP_ENDPOINT 가 사내 endpoint 박혀있어야 외부 누출 0. |
| Promptfoo eval → reference 영상 S3 | 회귀 검증이 실제 reference 영상 호출. CI 환경에서 AWS 자격증명 박혀야 함. |
| LLM judge → Gemini Flash | judge 가 Gemini Flash 호출 — coach 출력을 평가용 input 으로. 별도 API quota 소비. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-28 | Information Disclosure | Phoenix span 에 영상 raw bytes 박힘 | mitigate | GoogleGenAIInstrumentor 의 default 가 file content 박지 않음 (token usage + model + duration 만). 명시적으로 file content capture OFF 박제. |
| T-17-29 | Tampering | judge prompt 우회 (coach 출력에 prompt injection) | mitigate | judge prompt 가 "다음 코칭 멘트를 평가하세요" + 평문 박제 — coach 출력은 JSON 으로 박제 (구조화). prompt injection 시도 시 JSON 파싱 실패 → graceful None. |
| T-17-30 | Repudiation | eval 결과 audit | mitigate | Firestore eval/phase17/{analysis_id} 박제 — 어떤 분석이 어떤 reason 으로 샘플링됐는지 + judge 결과 영구 기록. |
| T-17-31 | Denial of Service | Promptfoo 30 examples × 4 영역 = 120 호출 | accept | belle/Claude 의 PR pre-merge 시 1회 실행 — 비용 = 30 × $0.06 ≈ $1.80/PR. AI-SPEC §4b 연간 비용 박제 ($2,200/년) 정합. |
| T-17-32 | Spoofing | CI 환경의 GEMINI_API_KEY 누출 | mitigate | GitHub Actions secrets 또는 belle 로컬 env 박제 — CI config 파일에 plaintext 박지 않음. |
</threat_model>

<verification>
- pytest backend/tests/eval/ -x -q 통과 (phoenix_setup + llm_judge + sampling 합 ≥ 15개 테스트).
- `grep -n "add_event.*guardrail" backend/shared/python/sunity_shared/gemini/ -r | grep -v '^#' | wc -l` ≥ 3 (G1/G2/G4/G5 span event).
- reference dataset entry ≥ 30개 (`python -c "import yaml; print(len(yaml.safe_load(open('backend/evals/phase17/dataset/reference_dataset.yaml'))['scenarios']))"`).
- assertion 모듈 3개 import 성공.
- E1 reject regex 회귀 — assertion test: "이 자세는 8.5점입니다" 입력 → assert_no_reject_patterns 의 pass=False.
- Phoenix bootstrap idempotent — test 가 두 번 호출 시 instrument 1회만.
</verification>

<success_criteria>
1. backend/shared/python/sunity_shared/eval/ 신설 — phoenix_setup.py + llm_judge.py + sampling.py.
2. backend/evals/phase17/ 신설 — promptfooconfig.yaml + 30-entry dataset + labels.json + assertion 3개.
3. **(W1)** 3개 runtime requirements.txt 에 telemetry extras 박힘 (arize-phoenix + opentelemetry-* + openinference-instrumentation-google-genai). Pod (in-process 또는 OTLP) / Lambda (OTLP only) / shared layer 분리.
4. **(W1)** Phoenix 부트스트랩 — optional import 가드 박힘. telemetry extras 미설치 시 `bootstrap_tracing` noop + 분석 흐름 차단 0. CI 별도 "telemetry extras installed" sub-test 박제.
5. Phoenix 부트스트랩 — in-process + OTLP HTTP 두 path 모두 지원, env 미설정 시 graceful noop.
6. google-genai SDK 자동 계측 — GoogleGenAIInstrumentor 박힌 후 GeminiVisionCall.call 호출이 자동 span 화 (telemetry extras 설치 시).
7. G1/G2/G3/G4/G5 guardrail trigger 가 Phoenix span event 로 박힘 — client.py/scene_finder.py/coach_writer_v2.py/reference_extractor.py 갱신 + TELEMETRY_OK gate.
8. LLM judge (영역 B 톤) — belle binary 라벨 calibration mode 동작, correlation 측정 가능.
9. Smart sampling — AI-SPEC §7 표의 7 rule 박제 + Firestore eval/phase17 박제.
10. **(W6)** Promptfoo config = **local eval** (belle/Claude 수동 실행). PR/merge 자동 block 표현 박제 0건. CI 자동 게이트 (.github/workflows) 는 별도 후속 plan. README 에 명시.
11. Promptfoo config + 30 examples reference dataset (정은지 5건 라벨링 박혀있음, 나머지 25건 placeholder TODO) + 3 external assertion script.
</success_criteria>

<output>
**2차 R-Plan 06 split 정합 (in-file Sub-phase 박제):**

- Task 1 (06A) 완료 후 → `.planning/phases/17-gemini-vision-integration-4/17-06A-SUMMARY.md` 박제. belle 검수 (telemetry deps + Phoenix optional import 만 단독 검증). 6B 진입 전 게이트.
- Task 1b + Task 2 (06B) 완료 후 → `.planning/phases/17-gemini-vision-integration-4/17-06B-SUMMARY.md` 박제. 4 영역 span event wiring + LLM judge + sampling 검증. 6C 진입 전 게이트.
- Task 3 (06C) 완료 후 → `.planning/phases/17-gemini-vision-integration-4/17-06C-SUMMARY.md` 박제. Promptfoo local eval config 검증.
- 마지막에 통합 `.planning/phases/17-gemini-vision-integration-4/17-06-SUMMARY.md` 박제 — 3 sub-phase summary cross-reference.
</output>
