---
phase: 17-gemini-vision-integration-4
plan: 07
type: execute
wave: 7
depends_on:
  - 17-05
  - 17-06
files_modified:
  - backend/scripts/extract_reference_angles.py
  - backend/scripts/reactivate_new6_motions.py
  - app/scripts/seed-reference-motions.mjs
  - backend/tests/scripts/test_reactivate_new6_motions.py
autonomous: false
requirements:
  - VISION-01
  - VISION-04

must_haves:
  truths:
    - "신규 6 motion 의 angles 가 RTMW engine (NlfPoseEstimator 가 아닌 **RTMWPoseEngine direct path** — `_RTMWNlfCompat` private adapter 박제 X, 3차 R-B4 정합) 으로 재추출되어 운영 분석 pipeline 의 keypoint engine 과 일치한다"
    - "각 신규 6 motion 에 대해 영역 A endpoint (POST /reference/auto-register) 호출이 발동되어 motion_name_ipsf + clipRange + checkpointJoints + branch routing 이 Firestore 박제"
    - "belle 검수 후 신규 6 motion 의 isActive=true 박제 — UAT 2026-06-12 박은 isActive=false hard gate 해소"
    - "신규 6 영상에 대한 mock e2e 분석 (영상 1건) 의 KeypointReport.confidence < 0.5 비율 (`KeypointReport.confidence` field — `data` 는 flat x/y 좌표, `assemble.py:393-448` 검증. 2차 R-W5 정합)이 5% 미만 — Plan 03 의 영역 D 보강 효과 검증"
    - "분석 결과의 not_pole_motion 폴백 발동 0건 — RESEARCH §6 박제 'F4 finding 해소'"
  artifacts:
    - path: "backend/scripts/extract_reference_angles.py"
      provides: "NlfPoseEstimator → RTMWPoseEngine direct path (운영 path 정합 — `_RTMWNlfCompat` private adapter 박제 X, 3차 R-B4)"
      contains: "RTMWPoseEngine"
    - path: "backend/scripts/reactivate_new6_motions.py"
      provides: "신규 6 motion ID 박제 list + 영역 A endpoint 호출 + RTMW angles 재추출 + Firestore upsert 자동화 스크립트"
      contains: "NEW6_MOTION_IDS"
    - path: "backend/tests/scripts/test_reactivate_new6_motions.py"
      provides: "스크립트 단위 테스트 — mock endpoint + mock RTMW + mock Firestore"
      contains: "def test_"
  key_links:
    - from: "backend/scripts/reactivate_new6_motions.py"
      to: "backend/functions/reference-auto-register/app.py"
      via: "HTTP POST /reference/auto-register 호출 (boto3 invoke 또는 requests)"
      pattern: "reference/auto-register"
    - from: "backend/scripts/reactivate_new6_motions.py"
      to: "backend/scripts/extract_reference_angles.py"
      via: "RTMW angles 재추출 + Firestore reference doc upsert"
      pattern: "extract_reference_angles"
---

<objective>
Phase 17 Wave 6 (RESEARCH §9 표기) — 신규 6 motion 재활성화. UAT 2026-06-12 박은 신규 6 motion 의 NLF↔RTMW 호환 깨짐 finding (현재 isActive=false 박혀있음) 을 영역 A (Plan 05) + 영역 D (Plan 03) 가 해소. extract_reference_angles.py 의 NlfPoseEstimator → **RTMWPoseEngine direct path** swap (운영 pipeline 의 `_RTMWNlfCompat` private adapter 와 1:1 흐름 — 3차 R-B4 정합, script context 가 pipeline module side-effect 끌고 오는 거 차단) + 영역 A endpoint 호출로 motion_name_ipsf/clipRange/checkpointJoints 자동 산출 + belle 검수 박제 + isActive=true.

Purpose: Phase 17 의 진짜 가치 (RESEARCH §6 박제: "신규 reference 영상 추가 시 NLF↔RTMW 호환 박는 과정이 사라짐") 의 첫 실증. ROADMAP Success #4 "KeypointReport 의 data 의 0.5 미만 비율 < 5% (현 신규 6 = 13-35%)" 직격. belle 의 검수 (분기 라우팅 정합 + isActive 승인) 가 hard gate — checkpoint:human-verify 박힘.
Output: extract_reference_angles.py 의 engine swap + reactivate_new6_motions.py 자동화 스크립트 + 단위 테스트 + belle 검수 checkpoint.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-gemini-vision-integration-4/17-AI-SPEC.md
@.planning/phases/17-gemini-vision-integration-4/17-RESEARCH.md
@.planning/phases/17-gemini-vision-integration-4/17-05-SUMMARY.md
@.planning/phases/17-gemini-vision-integration-4/17-06-SUMMARY.md

# UAT 2026-06-12 finding 박제 — 신규 6 motion isActive=false 원인
@.planning/phases/12-realmeasurement-keypoint/12-DIRECT-REVIEW-ITERATION5.md
# 기존 reference 추출 스크립트 — engine swap 대상
@backend/scripts/extract_reference_angles.py
# Pod 운영 pipeline 의 RTMW engine — extract 스크립트가 swap 해야 할 engine 정의
@backend/functions/pipeline/app.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: extract_reference_angles.py engine swap (NlfPoseEstimator → RTMWPoseEngine direct path, 3차 R-B4 정합) + reactivate_new6_motions.py 자동화 스크립트</name>
  <files>
    backend/scripts/extract_reference_angles.py,
    backend/scripts/reactivate_new6_motions.py,
    backend/tests/scripts/__init__.py,
    backend/tests/scripts/test_reactivate_new6_motions.py
  </files>
  <behavior>
    - **(2차 R-B3 정합)** extract_reference_angles.py 의 NlfPoseEstimator import 를 **`RTMWPoseEngine` + `to_coco17_array` 직접 사용** 으로 swap. `_RTMWNlfCompat` 박제 X — 이 class 는 `backend/functions/pipeline/app.py:305` 의 private adapter 로 Lambda module side-effect (`_FRAME_EXTRACTOR`, boto3 client, RunPod env 등) 까지 끌고 옴. script context 는 pipeline interface compat 불필요. 기존 NLF path 는 주석 박제로 보존 (legacy, R&D 격리). swap 시 angles 행렬 shape 동일 (T, 8, ?) 확인 — `compute_joint_angles` 호환.
    - reactivate_new6_motions.py 신설:
      - `NEW6_MOTION_IDS = ["...", "...", ...]` (UAT 2026-06-12 박힌 6개 motion ID — `.planning/phases/12-realmeasurement-keypoint/` 박은 산출 본 또는 Firestore 조회로 확정). belle 가 수정 가능하도록 list 상단 박제.
      - **W4 정합 — `STUDIO_ALIAS_OVERRIDES` 명시 mapping**: `{motion_id: studio_alias}` 형식 dict 박제. 이유: 현재 `seed-reference-motions.mjs` 의 reference doc 에 `studioAlias` field 박혀있지 않음 (`videoS3Key`/`name`/`entryDescription`/`isActive` 만 존재 — `app/scripts/seed-reference-motions.mjs:481-483` 검증). belle 가 신규 6 motion 의 분기 2 라우팅 (예: 폭스탑/사이드웨이 스핀 매칭) 을 의도하면 이 dict 에 직접 박제. 미박제 motion 은 `studio_alias=None` 으로 호출 → Gemini A 가 분기 1 IPSF whitelist 매치 시도 → 미매치 시 분기 3 auto fallback (G3).
    - 각 motion 에 대해 순차 실행:
      1. Firestore reference/{motionId} 조회 — 기존 `videoS3Key` 확인 (videoUrl 은 presigned 7일 — 박제 안정성 위해 s3Key 직접 사용).
      2. studio_alias 결정: `STUDIO_ALIAS_OVERRIDES.get(motion_id)` 우선, 없으면 Firestore doc 에 `studioAlias` field 박혀있으면 그 값 (후속 seed script PR 으로 추가 가능), 없으면 None.
      3. extract_reference_angles.py 호출 (subprocess 또는 import) — RTMW engine 으로 angles 재추출 + bodyComparisonSourcePose 갱신.
      4. POST /reference/auto-register 호출 (env REFERENCE_AUTO_REGISTER_URL + belle Firebase ID token from env BELLE_ID_TOKEN) — body: {s3Key=videoS3Key, studioAlias=step2 결과, overrideMotionId=motion_id}.
      5. 결과의 routing_branch + review_required 박제 + Firestore reference doc 박혀있는지 확인. branch_3_auto 가 belle 의도 외이면 `STUDIO_ALIAS_OVERRIDES` 에 alias 추가 후 재실행 (또는 ipsf_whitelist.json/studio_branch2_aliases.json PR).
    - dry-run 모드: `--dry-run` flag 박혀 있으면 endpoint 호출 X + Firestore 갱신 X + log 만.
    - test_reactivate_new6_motions.py: (1) dry-run mode — endpoint mock 호출 0, (2) 정상 mode — endpoint mock 호출 6회, (3) endpoint 1건 실패 — 나머지 계속 진행 (graceful), (4) **RTMW engine swap 검증** (3차 R-B4 정합 — mock 으로 `RTMWPoseEngine` 인스턴스화 호출됐는지 + `_RTMWNlfCompat` import 박제 0건 — `extract_reference_angles.py` 의 import 라인 grep).
  </behavior>
  <action>
    backend/scripts/extract_reference_angles.py 의 import 라인 grep — `from .* import NlfPoseEstimator` 찾아 `from sunity_shared.analysis.pose_engines.rtmw.rtmw_engine import RTMWPoseEngine` + `from sunity_shared.analysis.pose_frame import PoleAxis, to_coco17_array` + `from sunity_shared.analysis.body_normalization_measurer import measure_body_profile` 로 swap (2차 R-B3 정합 — pipeline private `_RTMWNlfCompat` 박제 X). 호출부는 `_RTMWNlfCompat.estimate_with_profile` (pipeline/app.py:336~363) 와 동일 흐름 박제 — `engine = RTMWPoseEngine()`, `default_pole = PoleAxis(axis_vector=(0,1,0), confidence_level="low", source="vertical_fallback", frame_index=None)`, `pose_frames = engine.estimate(frames, default_pole)`, `profile = measure_body_profile(pose_frames)`, `pose_frames_with_profile = [dataclasses.replace(pf, body_shape=profile) for pf in pose_frames]`, `coco_array = to_coco17_array(pose_frames_with_profile)`. 기존 NLF 호출 라인 주석 처리 + comment "Phase 17 Plan 07: RTMW engine swap (운영 pipeline 정합 — pipeline/_RTMWNlfCompat estimate_with_profile 와 1:1 흐름). NLF path 는 R&D 격리 (memory ml-pose-3d-pivot). 2차 R-B3 정합 — pipeline private adapter import X.". reactivate_new6_motions.py 는 argparse CLI 박제. NEW6_MOTION_IDS 는 UAT 2026-06-12 박은 산출 본에서 직접 박는다 — `.planning/phases/12-realmeasurement-keypoint/12-DIRECT-REVIEW-ITERATION5.md` 의 신규 6 motion id 박혀있는지 확인 후 박는다. belle 가 환경 차이로 수정 가능하도록 list 상단 박제 + `--motion-ids` CLI flag 박제 (overrides). HTTP 호출은 stdlib urllib (Lambda 의 RUNPOD_ANALYZE_URL 호출 패턴 정합 — backend/functions/pipeline/app.py 박혀있음). 단위 테스트는 monkeypatch + tmp Firestore mock + endpoint mock.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/scripts/test_reactivate_new6_motions.py -x -q && grep -c "RTMWPoseEngine" backend/scripts/extract_reference_angles.py && ! grep -q "_RTMWNlfCompat" backend/scripts/extract_reference_angles.py</automated>
  </verify>
  <done>4 케이스 테스트 통과 + extract_reference_angles.py 의 engine swap 박힘 (`RTMWPoseEngine` 직접 import 라인 존재 + `_RTMWNlfCompat` 박제 0건 — 2차 R-B3 정합). reactivate_new6_motions.py 의 dry-run mode 동작.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: belle 가 신규 6 motion 의 분기 라우팅 + isActive 검수</name>
  <what-built>
    Plan 05 의 영역 A endpoint + Plan 07 Task 1 의 reactivate_new6_motions.py 가 신규 6 motion 의 (1) RTMW angles 재추출, (2) Gemini Pro 의 motion_name_ipsf + clipRange + checkpointJoints + routing_branch 박제, (3) Firestore reference/{motionId} 의 geminiA 필드 박제 + isActive=false (G3 trigger 시) 또는 isActive=true (분기 1/2 통과 시) 박혔다. UAT 2026-06-12 박은 신규 6 motion 의 NLF↔RTMW 호환 깨짐 finding 의 첫 자동 해소 path 가 실행됨.
  </what-built>
  <how-to-verify>
    1. CLI 실행 (Claude 가 belle 대신 실행 가능 — belle 의 Firebase ID token 박혀있어야 함, env BELLE_ID_TOKEN):
       `cd backend && python -m scripts.reactivate_new6_motions --dry-run` 으로 NEW6_MOTION_IDS 확인.
    2. dry-run 결과 belle 가 검토 — motion_id list 가 UAT 2026-06-12 박은 6개와 일치하는지.
    3. belle 승인 시: `python -m scripts.reactivate_new6_motions` (no --dry-run) 실행 — endpoint 6 회 호출 + Firestore 6 doc 갱신.
    4. Firestore Console 또는 `gsd-sdk` 로 reference/{motionId} 조회 — geminiA.motion_name_ipsf + clipRange + checkpointJoints + routing_branch + reviewRequired 박힌지 확인. 6개 중 routing_branch 분포 (분기 1/2/3 몇 개씩) 확인.
    5. belle 가 각 motion 의 분기 라우팅 판단:
       - branch_1_ipsf: IPSF 명칭 정합 확인 후 isActive=true 박는다 (belle 가 Firestore 직접 갱신 또는 별도 admin script).
       - branch_2_studio: championPersonalAlias 가 belle 의 폭스탑 라벨링과 일치하는지 확인.
       - branch_3_auto: G3 fallback — belle 가 IPSF whitelist 에 추가 (ipsf_whitelist.json PR) 또는 분기 2 entry 박는다 (studio_branch2_aliases.json PR), 그 후 재호출.
    6. 최종 — 신규 6 motion 모두 isActive=true 박는다.
    7. mock e2e 분석 (영상 1건, Pod 에서 직접) — KeypointReport.confidence < 0.5 비율 (`KeypointReport.confidence` field — `data` 는 flat x/y 좌표, `assemble.py:393-448` 검증. 2차 R-W5 정합) < 5% + not_pole_motion 폴백 0건 확인. RESEARCH §6 박제 "Phase 17 의 진짜 가치" 실증.
    8. **WARN-3 정합 — E6 정은지 영상 B 검수 (AI-SPEC §5 E6 (b))**: 정은지 영상 5건 (Phase 14 박은 정은지 reference set) 각각의 영역 B 출력 (geminiB.causes + coachNote) belle 가 binary 검수 — "강사 보조 톤 + 원인-해결 순서 + 부위별 용어" 3 조건 PASS / FAIL. 라벨 결과는 `backend/evals/phase17/dataset/labels.json` 의 `e6_coach_tone` section 에 박는다 (정은지 5건 entry). E6 hard gate 박제 — 5건 중 1건이라도 FAIL 시 Phase 17 production rollout 보류 + Plan 04 retry/Cerebras 폴백 활성화 패턴 belle 재설계.
    9. **WARN-4 정합 — SC3 high_score_finding_gated warning 비율 측정**: mock e2e 분석 (step 7) 의 result 에서 `forcePatternInference.findings[]` 의 `high_score_finding_gated` warning 출현 빈도 측정. 비교 baseline = Phase 5 GeminiTechniqueRecognizer wave 이전 분석 결과 (Phase 12 박은 산출 본). 신규 6 + 정은지 5 = 11건 mock e2e 후 high_score_finding_gated warning 비율 ≤ baseline × 50% (ROADMAP SC3 "감소" 정합 — 50% 박제 임계값은 mock 단계 박제, production 4-day rolling 으로 후속 조정).
  </how-to-verify>
  <resume-signal>Type "approved" if 신규 6 motion 의 isActive=true + confidence < 0.5 비율 < 5% + not_pole_motion 폴백 0건 + 정은지 5건 E6 B 검수 PASS + high_score_finding_gated warning 비율 ≤ baseline × 50% 모두 충족. 또는 "blocked: <issue>" 로 차단 사유 박제 (예: IPSF whitelist 누락, RTMW angles 추출 실패, endpoint 인증 오류, E6 B 검수 FAIL). blocked 시 Claude 가 issue 해소 후 재실행.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reactivate_new6_motions.py → 영역 A endpoint | 자동화 스크립트가 endpoint 6회 호출. belle 의 Firebase ID token + BELLE_UID 화이트리스트 통과해야 함. |
| extract_reference_angles.py → RTMW engine | reference 영상에서 angles 재추출 — RTMW model weight 가 Pod 에 박혀 있어야 함 (운영 path 와 동일). |
| belle 검수 → Firestore isActive=true | belle 의 수동 검수가 hard gate — 자동화 X. checkpoint:human-verify 박제 정합. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-33 | Tampering | reactivate 스크립트가 잘못된 motion 박는다 | mitigate | dry-run mode 박제 + NEW6_MOTION_IDS list 가 belle 검토 진입점. UAT 2026-06-12 박은 6개와 일치 검증 후 실행. |
| T-17-34 | Information Disclosure | belle ID token env 누출 | mitigate | BELLE_ID_TOKEN 은 belle 로컬 env 박제 (CI/CD 박지 않음). 스크립트 실행 시점에 belle 가 직접 박는다. |
| T-17-35 | Repudiation | 신규 6 isActive=true 박은 audit | mitigate | Firestore reference/{motionId}.geminiA.registered_at + belle 검수 시 updated_at 박제. 어떤 motion 이 언제 활성화됐는지 추적 가능. |
| T-17-36 | Denial of Service | RTMW angles 재추출이 GPU 시간 소비 | accept | 신규 6 영상 × ~30s 각 = ~3분 GPU 시간. Pod 1회 작업 박제. |
| T-17-37 | Spoofing | reactivate 스크립트의 endpoint 호출 우회 | mitigate | Plan 05 의 BELLE_UID 화이트리스트가 1차 차단. 다른 uid 의 token 으로 호출해도 403. |
</threat_model>

<verification>
- pytest backend/tests/scripts/test_reactivate_new6_motions.py -x -q 통과.
- **3차 R-B4 정합 — `_RTMWNlfCompat` 박제 0건 + `RTMWPoseEngine` direct path 박힘 검증**:
  - `grep -c "RTMWPoseEngine" backend/scripts/extract_reference_angles.py` ≥ 1 (direct import 박힘).
  - `! grep -q "_RTMWNlfCompat" backend/scripts/extract_reference_angles.py` (pipeline private adapter import 박제 0건).
- `grep -c "NEW6_MOTION_IDS" backend/scripts/reactivate_new6_motions.py` ≥ 1.
- belle 검수 후 Firestore reference 6 doc 의 isActive=true 박힘 — Firestore Console 또는 `gsd-sdk query` 로 확인.
- mock e2e 분석의 KeypointReport.confidence < 0.5 비율 (field = `confidence`, `data` 아님 — 2차 R-W5) < 5% (Plan 03 의 영역 D 보강 효과 + RTMW swap 효과 합산) — 정량 측정 후 박제.
- not_pole_motion 폴백 발동 0건 — analyses.{id}.status 가 "done" + result.score 가 NaN 0.
</verification>

<success_criteria>
1. backend/scripts/extract_reference_angles.py 의 NlfPoseEstimator → **RTMWPoseEngine direct path** swap 박힘 (3차 R-B4 정합 — pipeline private `_RTMWNlfCompat` adapter import 박제 0건).
2. backend/scripts/reactivate_new6_motions.py 신설 — NEW6_MOTION_IDS list + STUDIO_ALIAS_OVERRIDES mapping + dry-run mode + endpoint 호출 + Firestore upsert.
3. 신규 6 motion 의 영역 A endpoint 호출 6회 성공 (Gemini Pro → motion_name_ipsf + clipRange + checkpointJoints + routing_branch 박힘).
4. belle 검수 — 분기 라우팅 정합 + isActive=true 박는다 (checkpoint:human-verify 통과).
5. mock e2e 분석 — KeypointReport.confidence < 0.5 비율 (field = `confidence`, `data` 아님 — 2차 R-W5) < 5% (현 13-35% 에서 회복).
6. not_pole_motion 폴백 발동 0건 — UAT 2026-06-12 F4 finding 해소.
7. RESEARCH §6 박제 "Phase 17 의 진짜 가치" 실증 — "신규 reference 영상 추가 시 NLF↔RTMW 호환 박는 과정이 사라짐".
8. **WARN-3 정합 — E6 정은지 영상 5건 B 검수 PASS** — `labels.json::e6_coach_tone` 에 정은지 5건 belle binary 라벨 박힘 + 5/5 PASS (FAIL 시 Phase 17 production rollout 보류).
9. **WARN-4 정합 — SC3 high_score_finding_gated warning 비율 ≤ baseline × 50%** — 신규 6 + 정은지 5 = 11건 mock e2e 결과의 forcePatternInference.findings[] 안 high_score_finding_gated warning 비율 측정. baseline = Phase 5 GeminiTechniqueRecognizer wave 이전 분석 (Phase 12 산출 본).
</success_criteria>

<output>
Create `.planning/phases/17-gemini-vision-integration-4/17-07-SUMMARY.md` when done.
</output>
