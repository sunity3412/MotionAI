---
phase: 19-vision-hybrid
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/tests/test_kismam.py
  - backend/tests/test_dimensions.py
  - backend/tests/test_pipeline_mode3.py
  - backend/tests/test_anchor_known_answer.py
autonomous: true
requirements: [SCORE-06, SCORE-07, TRUST-01, TRUST-02, TRUST-03, TRUST-05]
must_haves:
  truths:
    - "재설계 채점기를 검증할 RED 테스트가 실행 가능한 형태로 존재한다 (구현 전 실제 실행 시 fail — collection 아닌 behavior 실패)"
    - "새 semantics 와 모순되는 기존 케이스(test_overall_from_dimensions_is_mean, test_score_monotonic_decreasing_with_deviation)가 새 계약으로 갱신된다"
    - "D-05 실영상 앵커는 per-test @requires_anchor_env 마커로 gate 되고, synthetic above-cutoff 케이스는 항상 실행된다 (module-level skip 금지 — ITER-2 HIGH-1)"
  artifacts:
    - path: "backend/tests/test_kismam.py"
      provides: "SCORE-06 + TRUST-02 신규 케이스 + monotonic 케이스 tolerance-aware 갱신 (D-01)"
      contains: "test_single_major_fault_dominates"
    - path: "backend/tests/test_dimensions.py"
      provides: "SCORE-07 + TRUST-02 신규 케이스 + is_mean→uses_core_dimensions 갱신 (D-01)"
      contains: "test_micro_bent_zero_track"
    - path: "backend/tests/test_pipeline_mode3.py"
      provides: "TRUST-01 + TRUST-03 + TRUST-05 신규 케이스 + build_mode3 backward-compat + vision identity (D-03)"
      contains: "test_display_matches_score_source"
    - path: "backend/tests/test_anchor_known_answer.py"
      provides: "D-05 6 앵커 방향검증 (per-test @requires_anchor_env) + always-on synthetic above-cutoff"
      contains: "requires_anchor_env"
  key_links:
    - from: "test_dimensions.py::test_micro_bent_zero_track"
      to: "dimensions.line_score"
      via: "expects_extension 필터 안 160° 임계 분기"
      pattern: "line_score"
    - from: "test_dimensions.py::test_overall_from_dimensions_uses_core_dimensions"
      to: "dimensions.overall_from_dimensions"
      via: "core(angle/line) min, stability 비기여, no-core fallback"
      pattern: "overall_from_dimensions"
---

<objective>
Phase 19 v1 의 모든 요구사항에 대한 RED 테스트를 먼저 만든다 (Nyquist — 구현 전 검증 인프라 확보). 재설계 채점기가 만족해야 할 행동(단일 major fault 지배 / 위양성 없음 / micro-bent 0점 / stability 비인플레 / 표시-점수 정합 / Mode3 미보유 게이트 / vision hook pass-through)을 결정론적 합성 angles 로 단언한다. **핵심 변경(이전 BLOCKER-3 / HIGH-5):** RED 는 collection 이 아니라 *실제 실행 시 behavior 실패*로 증명한다 — 새 케이스를 현재 평균식/stability 동작에 대해 단독 실행하면 fail 해야 한다. 또한 새 semantics 와 모순되는 기존 케이스(`test_overall_from_dimensions_is_mean`, `test_score_monotonic_decreasing_with_deviation`)를 새 계약으로 갱신한다 (Wave 1 회귀 정의의 일부).

**ITER-2 반영:**
- **HIGH-1:** D-05 6 앵커는 `RUN_PHASE19_ANCHORS` env-gate 가 필요하지만 **module-level `pytestmark` 는 금지** — 같은 파일의 synthetic above-cutoff 케이스(GPU 불필요)까지 skip 되는 모순. 대신 `requires_anchor_env = pytest.mark.skipif(...)` 데코레이터를 **실영상/GPU 앵커 테스트에만 per-test** 적용하고, `test_above_cutoff_synthetic_stays_high` 는 마커 없이 항상 실행.
- **MEDIUM-2:** build_mode3 backward-compat 케이스 추가 — `build_mode3(is_first=True)` (basis 미전달) 가 정확히 `{"mode":"mode3","isFirst":True}` 임을 단언, scoringBasis/scoringBasisLabel 은 전달 시에만 등장.
- **MEDIUM-3:** `_apply_vision_veto` identity 를 **SAME-object** 로 명시 — `assert out is score_result` + mutation 0.

Purpose: 후속 Wave 1/2 구현이 명확한 통과 기준을 갖게 하고, 회귀 안전망을 선확보한다. 테스트는 IPSF 근거 행동(방향)만 단언 — 보유 sweep 수치 타깃 금지 (D-05 경계).
Output: 3개 테스트 파일에 신규 케이스 + 2개 모순 케이스 갱신 + 신규 anchor 파일 1개. 신규 케이스는 단독 실행 시 RED(behavior fail). 실영상 anchor 는 per-test env-gate skip, synthetic above-cutoff 는 항상 실행. 19-01-SUMMARY.md 에 기대 실패 케이스명 기록.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-vision-hybrid/19-REVIEWS.md
@.planning/phases/19-vision-hybrid/19-VALIDATION.md
@.planning/phases/19-vision-hybrid/19-PATTERNS.md
@.planning/phases/19-vision-hybrid/19-D05-VISION-GROUNDING-SPIKE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 백엔드 채점 코어 RED 케이스 + 모순 케이스 갱신 (SCORE-06 / SCORE-07 / TRUST-02)</name>
  <files>backend/tests/test_kismam.py, backend/tests/test_dimensions.py</files>
  <read_first>
    - backend/tests/test_kismam.py (수정 대상 — 기존 `test_score_monotonic_decreasing_with_deviation` 19-27 = dev0/dev1/dev2 = 0/15/45° + `assert s0 > s1 > s2`; `test_tolerance_gaussian_value` 30-36 + `pytest.approx` 컨벤션 모방. score_from_deviation 은 불변이라 tolerance_gaussian 케이스는 보존)
    - backend/tests/test_dimensions.py (수정 대상 — `test_overall_from_dimensions_is_mean` 86-88 = `{"a":80,"b":60,"c":70}==70` + `{}==0`; 모듈 docstring "AWS 불필요" + `_pose`/`_profile` 결정적 픽스처 헬퍼, `np.full`/`np.tile`/`rng = np.random.default_rng(seed)` 패턴)
    - 19-REVIEWS.md (iter-1) BLOCKER-3 — 정확한 갱신 단언값 + HIGH-5 RED 증명 방식
    - 19-PATTERNS.md "신규 테스트" 섹션 — 케이스 이름 목록 + test 컨벤션
    - 19-VALIDATION.md Per-Task Verification Map (SCORE-06/07/TRUST-02 행) — 정확한 테스트 함수명
    - 19-IPSF-DEDUCTION-NOTES.md §A (트랙1 160° / 트랙2 누적 감점) — 단언이 IPSF 방향 근거
    - backend/shared/python/sunity_shared/analysis/kismam.py (overall_score 182-189, COACHING_FOCUS 53-62, JointAssessment 96-111, DEFAULT_TOLERANCE_DEG)
    - backend/shared/python/sunity_shared/analysis/dimensions.py (line_score 226-245, overall_from_dimensions 348-351, expects_extension 필터 238-242, DIM_ANGLE/DIM_LINE/DIM_STABILITY 151-153)
  </read_first>
  <action>
    먼저 **모순 케이스 2건 갱신** (새 semantics 가 깨야 할 옛 단언을 새 계약으로 교체):
    (A) test_dimensions.py `test_overall_from_dimensions_is_mean`(86-88) → `test_overall_from_dimensions_uses_core_dimensions` 로 rename + 단언 교체: `overall_from_dimensions({DIM_ANGLE:40, DIM_LINE:80, DIM_STABILITY:99}) == 40` (core min, stability 비기여) + `overall_from_dimensions({DIM_STABILITY:99}) == 99` (no-core fallback — 절대트랙 단독) + `overall_from_dimensions({}) == 0` (빈 입력 보존). DIM_* 상수 키 사용 (literal "angle" 하드코딩 금지).
    (B) test_kismam.py `test_score_monotonic_decreasing_with_deviation`(19-27) → tolerance dead-zone 인지 단조성으로 교체: dev = 0°/30°/60° (전부 tol=20° 초과 영역) → `s0 > s1 > s2` 유지 + `0 <= s2 <= 100`. 별도 신규 케이스 `test_within_tolerance_remains_high` 추가: 모든 관절 dev=10°(<tol) → 종합 high(예 `>= 90`). (감점식에서 dead-zone 안은 감점 0 이므로 15° 단조성이 깨질 수 있어 beyond-tolerance 로 교체.) score_from_deviation/assess/tolerance_gaussian 케이스는 불변 보존.

    그 다음 **신규 RED 케이스** (단독 실행 시 현재 평균식/stability 동작에 대해 fail 해야 함):
    test_kismam.py:
    (1) `test_single_major_fault_dominates` — assessments 대부분 deviation_deg≈0 + 한 관절 deviation_deg 큰값(50°). 단언 = 재설계 overall_score 가 "평균식이라면 나올 값"보다 현저히 낮다 — 구체적으로 평균식 예상값(예 ~88)을 명시 상수로 두고 `assert score < 평균식_예상 - margin`(예 `< 70`). 평균 희석 금지 방향만, 점수 타깃 아님. (현재 평균식에선 88 근처라 `< 70` 실패 → RED.)
    (2) `test_clean_pose_high_score` — 모든 assessment deviation_deg < tol → 종합 high(`>= 90`). 위양성 0, sensitivity gate. (현재도 통과할 수 있으나 above-cutoff 가드로 유지 — 회귀 안전망.)
    (3) `test_shoulder_focus_label` — TRUST-02. COACHING_FOCUS['left_shoulder']/['right_shoulder'] 가 '안정성'(stability=떨림 오인) 이 아님을 단언(`!= '안정성'` + '떨림' 부재). FOCUS 값에 부위 키워드 중복 금지("어깨 어깨" 부분문자열 0). (현재 라벨이 '안정성'이면 RED.)
    test_dimensions.py:
    (4) `test_micro_bent_zero_track` — SCORE-07. profile.expects_extension(knee)=True 관절 rep angle 160° 미만(140°) → line_score == 0 (요소 무효, 비례감점 아님). 160°=180°−20°tol [CITED: 19-IPSF-DEDUCTION-NOTES §A]. (현재 line_score 는 비례감점이라 140°→0 아님 → RED.)
    (5) `test_intentional_bend_not_penalized` — SCORE-07 위양성 가드. expects_extension(joint)=False 관절은 140°여도 0점 트랙 미적용 (Pitfall 4 chair-pose 위양성 차단).
    (6) `test_stability_does_not_inflate` — TRUST-02. dimension_scores 에서 DIM_STABILITY=99(매끄러운 fault)여도 DIM_ANGLE/DIM_LINE 낮으면(40) overall_from_dimensions 종합 낮음(`<= 50`). dimension_scores 키/순서 보존 단언 포함. (현재 평균식은 (40+40+99)/3≈60 → `<=50` 실패 → RED.)
    모든 신규 케이스: 단독 실행 시 현재 코드에 대해 fail(RED). 결정적 픽스처(np.random.default_rng seed 고정), 명시 점수 상수는 IPSF 근거 주석 동반. 이모지 금지, snake_case, from __future__ import annotations.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_kismam.py::test_single_major_fault_dominates tests/test_kismam.py::test_clean_pose_high_score tests/test_kismam.py::test_shoulder_focus_label tests/test_kismam.py::test_within_tolerance_remains_high tests/test_dimensions.py::test_micro_bent_zero_track tests/test_dimensions.py::test_intentional_bend_not_penalized tests/test_dimensions.py::test_stability_does_not_inflate tests/test_dimensions.py::test_overall_from_dimensions_uses_core_dimensions -q; echo "EXIT=$? (신규 RED 케이스 fail 기대 — 비-0 정상)"</automated>
  </verify>
  <acceptance_criteria>
    - `test_overall_from_dimensions_is_mean` 가 제거되고 `test_overall_from_dimensions_uses_core_dimensions` 가 단언값(40 / 99 / 0)으로 존재 (grep: `is_mean` 0건)
    - `test_score_monotonic_decreasing_with_deviation` 가 0/30/60° beyond-tolerance 값 사용 + `test_within_tolerance_remains_high` 신규 존재
    - 신규 RED 케이스(single_major_fault_dominates / micro_bent_zero_track / stability_does_not_inflate / shoulder_focus_label) 가 각각 단독 실행 시 **fail**(behavior RED) — pytest exit 비-0, 각 케이스가 collected & failed (errored 아님; import/syntax 0)
    - 단언이 IPSF 방향(fault<정상, 160° 임계, stability 비인플레)만 검사하고 보유 sweep 수치 타깃을 하드코딩하지 않는다 (grep: 명시 점수 상수가 IPSF 근거 주석 동반)
    - 19-01-SUMMARY.md 에 "단독 실행 시 RED 인 케이스명 목록 + 각 실패 사유 1줄" 기록
  </acceptance_criteria>
  <done>모순 케이스 2건 갱신 완료. 6 신규 케이스가 단독 실행 시 behavior RED. IPSF 방향만 단언. curve-fit 0.</done>
</task>

<task type="auto">
  <name>Task 2: pipeline RED 케이스 (TRUST-01 / TRUST-03 / TRUST-05) + build_mode3 backward-compat + vision identity + anchor per-test env-gate</name>
  <files>backend/tests/test_pipeline_mode3.py, backend/tests/test_anchor_known_answer.py</files>
  <read_first>
    - backend/tests/test_pipeline_mode3.py (수정 대상 — `_video`/`_as_prev` 픽스처, sys.path 부트스트랩 1-21 패턴)
    - 19-REVIEWS.md (iter-1) BLOCKER-1 is_reference_free_motion 판정 / BLOCKER-2 scoringBasis / HIGH-2 DTW median helper
    - 19-REVIEWS.md (iter-2) HIGH-1 (36-54) anchor per-test marker (module-level skip 금지, synthetic always-on) / HIGH-2 (56-66) scoringBasis source 라벨(Mode3 reference_motion 금지) / HIGH-3 (68-72) composite basis / MEDIUM-2 (86-94) build_mode3 backward-compat / MEDIUM-3 (95-106) _apply_vision_veto `out is score_result`
    - 19-PATTERNS.md "신규 파일 test_anchor_known_answer.py" — analog = test_pipeline_mode3.py 부트스트랩
    - 19-VALIDATION.md Known-Answer Anchor Validation 표 + Per-Task Verification Map (TRUST-01/03/05 행)
    - 19-D05-VISION-GROUNDING-SPIKE.md (6 앵커 표, "방향 판정" 경계)
    - backend/functions/pipeline/app.py (_angles_to_mean_dict 1515-1538, _deviation_against 1557-1569 = match.path/user_seg/a_ref 반환, _mode3_comparison 1572-1630 = first abs_dims+extension_target / progress prev_seg, MODE_SELF 1839-1847, NOT_POLE 1812-1820)
    - backend/shared/python/sunity_shared/analysis/assemble.py (_SAFE_DEFAULT_BRANCH 68-77 copyBranch="branch2_eunji_reference"=실 branch2 와 동일값, lookup_motion_branch 91-113, MotionBranchInfo 39-53 = angleSource/angleFixtureKey/ipsfCode/officialName 필드, build_mode3 539-562 = is_first early-return `{"mode":"mode3","isFirst":True}`)
    - 19-RESEARCH.md Wave 0 Gaps — pre-existing 실패 격리 목록
  </read_first>
  <action>
    test_pipeline_mode3.py 에 케이스 추가:
    (1) `test_display_matches_score_source` — TRUST-01. 표시 각도 dict(현재/기준)가 점수 산출 DTW-정렬 median 과 동일 source 임을 단언. ref full-clip 에 jitter 프레임을 넣어 "정렬 median ≠ whole-clip nanmean" 이 구분되는 합성 픽스처 구성 — Wave 2 가 추가할 path-aligned median helper(`_angles_to_dtw_median_dicts`)를 통해 표시값이 산출되어야 통과. (현재 `_angles_to_mean_dict` whole-clip nanmean 이라 RED.)
    (2) `test_unknown_move_gate` — TRUST-03 + BLOCKER-1 + ITER-2 HIGH-2/HIGH-3. parametrize 로 scoringBasis = **실제 채점 source** 검증:
        (a) mode1(MODE_EXPERT, reference 각도 비교) → scoringBasis="reference_motion".
        (b) Mode3 first + 미등록 motion_id → is_reference_free_motion(info) True → scoringBasis="reference_free_absolute" + label="기준 동작 없음 — 절대 자세 기준 평가" + 절대트랙.
        (c) Mode3 first + recognized profile(등재 motion_id) → scoringBasis="recognized_motion_absolute" (reference_motion 아님 — first 는 reference 각도 미사용).
        (d) Mode3 progress + known → scoringBasis="previous_analysis_plus_absolute".
        (e) Mode3 progress + reference-free → scoringBasis="previous_analysis_plus_reference_free_absolute" (composite, HIGH-3).
        **명시 단언:** (b)와 실 branch2(정은지 reference) 둘 다 copyBranch=="branch2_eunji_reference" 인데 scoringBasis 가 달라야 함(angleSource/angleFixtureKey/ipsfCode 로 구분 — copyBranch 단독 분기 금지). **Mode3 어디에도 scoringBasis=="reference_motion" 미등장 단언**(ITER-2 HIGH-2). fail-closed/raise 금지(점수는 주되 근거 명시). (현재 게이트 부재 + scoringBasis 필드 부재라 RED.)
    (3) `test_build_mode3_backward_compat` — ITER-2 MEDIUM-2. `assemble.build_mode3(is_first=True)` (basis 미전달) == `{"mode":"mode3","isFirst":True}` EXACT (dict 동등). scoring_basis 전달 시에만 "scoringBasis"/"scoringBasisLabel" 키 등장 단언. (현재 build_mode3 는 scoring_basis 인자 부재라 RED.)
    (4) `test_vision_hook_passthrough` — TRUST-05 + ITER-2 MEDIUM-3. v1 vision hook(_apply_vision_veto)이 OFF 시 **`out is score_result` (SAME object identity)** 단언 + score_result 의 점수/필드가 mutate 되지 않음 단언. (현재 _apply_vision_veto 부재라 RED.)
    신규 파일 test_anchor_known_answer.py 생성 (ITER-2 HIGH-1 — per-test marker):
    - test_pipeline_mode3.py 의 sys.path 부트스트랩 모방.
    - 모듈 상단: `import os, pytest` + `requires_anchor_env = pytest.mark.skipif(os.environ.get("RUN_PHASE19_ANCHORS") != "1", reason="RUN_PHASE19_ANCHORS=1 + S3/GPU 필요")`. **module-level `pytestmark` 금지** (ITER-2 HIGH-1 — synthetic 케이스까지 skip 되는 모순). 활성 명령을 모듈 docstring 에 명시: `RUN_PHASE19_ANCHORS=1 pytest tests/test_anchor_known_answer.py -q`.
    - D-05 6 페어(climb/kip-up/pdshape/elbow-twist-sister/power-spin/peter-pan) parametrize 한 실영상/GPU 앵커 테스트(예 `test_anchor_fault_lower_than_correct`)에만 `@requires_anchor_env` 데코레이터 적용. **skip 되더라도 본문에 real fixture S3 key 목록 + 페어별 expected dominant-dimension 메타(climb=line, kip-up=angle 등)를 상수로 보존** — 활성화가 mechanical 하도록. 단언 = fault 종합 < correct 종합(방향) + major 차원 지배. 19-D05 표의 ~편차는 주석 근거로만, assert 비교 임계로 하드코딩 금지 ([[calibration-source-hard-gate]]).
    - `test_above_cutoff_synthetic_stays_high` — **마커 없이 항상 실행** (ITER-2 HIGH-1). synthetic angles(정상 동작) → 종합 high 유지(sensitivity gate). GPU/S3 불필요. (현재 감점식 미구현이면 RED 가능 — Wave 1 통과 대상.)
    이모지 금지. pipeline 신규 케이스는 단독 실행 시 RED, 실영상 anchor 6 페어는 env 부재 시 skip, synthetic above-cutoff 는 항상 실행.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_pipeline_mode3.py::test_display_matches_score_source tests/test_pipeline_mode3.py::test_unknown_move_gate tests/test_pipeline_mode3.py::test_build_mode3_backward_compat tests/test_pipeline_mode3.py::test_vision_hook_passthrough -q; echo "PIPE_EXIT=$? (RED 기대)"; python -m pytest tests/test_anchor_known_answer.py -q; echo "ANCHOR_EXIT=$? (실영상 6 페어 skip + synthetic above-cutoff 실행)"</automated>
  </verify>
  <acceptance_criteria>
    - 4 pipeline 케이스가 collected (import 0) 되고 단독 실행 시 **fail**(behavior RED — 게이트/필드/helper/hook 부재). test_build_mode3_backward_compat 는 build_mode3 에 scoring_basis 인자 부재라 RED
    - test_unknown_move_gate 에 "실 branch2 와 reference-free 가 copyBranch 동일값인데 scoringBasis 가 다르다" 단언 + "Mode3 에 reference_motion 미등장" 단언 존재 (ITER-2 HIGH-2)
    - test_unknown_move_gate parametrize 가 5 scoringBasis source 라벨(reference_motion[mode1]/reference_free_absolute/recognized_motion_absolute/previous_analysis_plus_absolute/previous_analysis_plus_reference_free_absolute) 전부 커버 (composite 포함 — HIGH-3)
    - test_vision_hook_passthrough 가 `is` (SAME object identity) + mutation 0 단언 (ITER-2 MEDIUM-3, grep: ` is score_result`)
    - test_build_mode3_backward_compat 가 `{"mode":"mode3","isFirst":True}` EXACT 동등 + basis 전달 시에만 키 등장 단언 (ITER-2 MEDIUM-2)
    - test_anchor_known_answer.py 에 module-level `pytestmark` 0건 (grep), `requires_anchor_env` per-test 데코레이터 사용 (grep: @requires_anchor_env). `test_above_cutoff_synthetic_stays_high` 는 마커 없음
    - `pytest tests/test_anchor_known_answer.py -q`(env 미설정) 시 실영상 6 페어 skip + synthetic above-cutoff 는 collected/실행 (전부 skip 아님 — ITER-2 HIGH-1)
    - skip 본문에 real fixture key 목록 + 페어별 dominant-dimension 메타 상수 보존 (grep: climb/kip-up + dominant 메타)
    - 6 페어 단언이 `fault < correct` 방향 + 차원 지배만 검사 — 19-D05 ~편차 수치가 assert 비교값으로 등장 0건
    - pre-existing 실패(test_pole_detector / test_pipeline_geminid_wiring / test_spike_gemini_moment_smoke)는 본 plan 미수정
  </acceptance_criteria>
  <done>4 pipeline RED 케이스(display/gate/backward-compat/vision identity) + anchor 파일(실영상 per-test env-gate skip + synthetic always-on). 방향만 단언, curve-fit 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

본 plan 은 테스트 코드만 추가/수정한다 — 신규 endpoint/auth/PII/네트워크 표면 없음. 기존 Firebase 토큰 auth + Firestore 규칙 불변.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering | 테스트가 보유 sweep 수치를 임계로 박제 (curve-fit) | mitigate | acceptance_criteria grep — IPSF 방향만 단언, ~편차 assert 0건 ([[calibration-source-hard-gate]]) |
| T-19-10 | Tampering | RED 가 collection 만 증명 → 잘못된 단언 통과 (false coverage) | mitigate | 단독 실행 behavior fail 강제 + SUMMARY 에 실패 케이스명 기록 |
| T-19-17 | Tampering | module-level skip 이 synthetic above-cutoff 까지 skip → sensitivity gate 무력화 | mitigate | per-test @requires_anchor_env, synthetic 마커 없음 (ITER-2 HIGH-1), acceptance grep pytestmark 0 |
| T-19-SC | Tampering | npm/pip installs | accept | 본 plan 신규 패키지 설치 0 (기존 pytest/numpy 재사용) |

내부 테스트 추가만 — 위협 표면 낮음.
</threat_model>

<verification>
- 신규 pipeline/core 케이스 단독 실행 시 RED(behavior fail) — exit 비-0, errored 아님
- 모순 케이스 2건이 새 계약으로 갱신 (is_mean 제거, monotonic beyond-tolerance)
- test_anchor_known_answer.py 가 env 부재 시 실영상 6 페어만 skip + synthetic above-cutoff 실행 (module-level skip 0)
- 19-01-SUMMARY.md 에 단독 RED 케이스명 + 실패 사유 기록
</verification>

<success_criteria>
- 신규 케이스(4 core + 1 within-tol + 4 pipeline) + anchor 6 페어 parametrize(per-test gate) + synthetic always-on 가 수집/RED/skip 정합
- 모순 케이스 2건이 새 semantics 계약으로 갱신
- 모든 단언이 IPSF 방향만 검사, curve-fit 0건
- RED 가 behavior 실패로 증명됨 (collection 아님)
- build_mode3 backward-compat + vision SAME-object identity 단언 존재
</success_criteria>

<output>
Create `.planning/phases/19-vision-hybrid/19-01-SUMMARY.md` when done — 단독 실행 RED 케이스명 + 각 실패 사유 1줄 포함
</output>
