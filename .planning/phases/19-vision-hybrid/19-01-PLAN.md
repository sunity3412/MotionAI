---
phase: 19-vision-hybrid
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/tests/test_kismam.py
  - backend/tests/test_dimensions.py
  - backend/tests/test_pipeline_mode3.py
  - backend/tests/test_anchor_known_answer.py
autonomous: true
requirements: [SCORE-06, SCORE-07, TRUST-01, TRUST-02, TRUST-03, TRUST-05]
must_haves:
  truths:
    - "재설계 채점기를 검증할 RED 테스트가 실행 가능한 형태로 존재한다 (구현 전 실패)"
    - "D-05 6 앵커 방향검증 테스트가 GPU-skip marker 로 존재한다 (Pod 재개 시 활성)"
  artifacts:
    - path: "backend/tests/test_kismam.py"
      provides: "SCORE-06 + TRUST-02 신규 케이스 (감점식 + 어깨 라벨)"
      contains: "test_single_major_fault_dominates"
    - path: "backend/tests/test_dimensions.py"
      provides: "SCORE-07 + TRUST-02 신규 케이스 (micro-bent 0점 + stability 분리)"
      contains: "test_micro_bent_zero_track"
    - path: "backend/tests/test_pipeline_mode3.py"
      provides: "TRUST-01 + TRUST-03 + TRUST-05 신규 케이스"
      contains: "test_display_matches_score_source"
    - path: "backend/tests/test_anchor_known_answer.py"
      provides: "D-05 6 앵커 방향검증 (GPU skip)"
      contains: "skipif"
  key_links:
    - from: "test_dimensions.py::test_micro_bent_zero_track"
      to: "dimensions.line_score"
      via: "expects_extension 필터 안 160° 임계 분기"
      pattern: "line_score"
---

<objective>
Phase 19 v1 의 모든 요구사항에 대한 RED 테스트 스텁을 먼저 만든다 (Nyquist — 구현 전 검증 인프라 확보). 재설계 채점기가 만족해야 할 행동(단일 major fault 지배 / 위양성 없음 / micro-bent 0점 / stability 비인플레 / 표시-점수 정합 / Mode3 미보유 게이트 / vision hook pass-through)을 결정론적 합성 angles 로 단언한다. D-05 6 앵커는 GPU-skip marker 로 Pod 재개 시 활성화되도록 자리만 만든다.

Purpose: 후속 Wave 1/2 구현이 명확한 통과 기준을 갖게 하고, 회귀 안전망을 선확보한다. 테스트는 IPSF 근거 행동(방향)만 단언 — 보유 sweep 수치 타깃 금지 (D-05 경계).
Output: 4개 테스트 파일에 신규 케이스 추가 + 신규 anchor 파일 1개. 모두 RED (구현 전 실패) 또는 skip.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-vision-hybrid/19-VALIDATION.md
@.planning/phases/19-vision-hybrid/19-PATTERNS.md
@.planning/phases/19-vision-hybrid/19-D05-VISION-GROUNDING-SPIKE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 백엔드 채점 코어 RED 케이스 (SCORE-06 / SCORE-07 / TRUST-02)</name>
  <files>backend/tests/test_kismam.py, backend/tests/test_dimensions.py</files>
  <read_first>
    - backend/tests/test_kismam.py (수정 대상 — 기존 케이스 19-27/39-46 의 `assert s0 > s1 > s2` 단조성 + `pytest.approx` 컨벤션 모방)
    - backend/tests/test_dimensions.py (수정 대상 — 16-30 모듈 docstring "AWS 불필요" + `_pose`/`_profile` 결정적 픽스처 헬퍼, `np.full`/`np.tile`/`rng = np.random.default_rng(seed)` 패턴)
    - 19-PATTERNS.md "신규 테스트" 섹션 (220-238) — 케이스 이름 목록 + test 컨벤션
    - 19-VALIDATION.md Per-Task Verification Map (SCORE-06/07/TRUST-02 행) — 정확한 테스트 함수명
    - 19-IPSF-DEDUCTION-NOTES.md §A (트랙1 160° / 트랙2 누적 감점) — 단언이 IPSF 방향 근거
    - backend/shared/python/sunity_shared/analysis/kismam.py (overall_score 182-189, COACHING_FOCUS 53-62, JointAssessment 96-111)
    - backend/shared/python/sunity_shared/analysis/dimensions.py (line_score 226-245, overall_from_dimensions 348-351, expects_extension 필터 238-242, DIM_* 151-153)
  </read_first>
  <action>
    test_kismam.py 에 4 케이스 추가 (기존 픽스처 헬퍼 재사용, np.random.default_rng seed 고정):
    (1) `test_single_major_fault_dominates` — assessments 대부분 deviation_deg≈0 + 한 관절 deviation_deg 큰값(예 50°). 단언 = 재설계 집계 함수(overall_score 교체본)의 종합이 "평균식이라면 나올 값"보다 현저히 낮다 (단일 major 가 종합을 끌어내린다). 평균 희석 금지 방향만 단언, 특정 점수 타깃 금지.
    (2) `test_clean_pose_high_score` — 모든 assessment deviation_deg < tol(_IPSF_TOLERANCE_DEG) → 종합 high (위양성 0, sensitivity gate). 정상 동작이 높게 유지되는지.
    (3) `test_shoulder_focus_label` — TRUST-02. COACHING_FOCUS['left_shoulder'] / ['right_shoulder'] 가 '안정성'(stability=떨림 오인) 이 아님을 단언. 어깨는 STATIC POSE ANGLE 의미. JOINT_LABEL_KO 에 이미 부위 한글이 있으므로 FOCUS 값에 부위 키워드 중복 금지(예 "오른쪽 어깨 어깨 안정성" 금지)도 단언.
    test_dimensions.py 에 3 케이스 추가:
    (4) `test_micro_bent_zero_track` — SCORE-07. profile.expects_extension(knee)=True 인 관절의 rep angle 이 160° 미만(예 140°)이면 line_score 가 0 (요소 무효, 비례감점 아님). 160°=180°−20°tol [CITED: 19-IPSF-DEDUCTION-NOTES §A].
    (5) `test_intentional_bend_not_penalized` — SCORE-07 위양성 가드. expects_extension(joint)=False 관절은 160° 미만이어도 0점 트랙 미적용 (Pitfall 4 chair-pose 위양성 차단).
    (6) `test_stability_does_not_inflate` — TRUST-02. dimension_scores 에서 DIM_STABILITY 가 99(매끄러운 fault)여도 DIM_ANGLE/DIM_LINE 가 낮으면 overall_from_dimensions 교체본 종합이 낮다 (stability 인플레 차단). dimension_scores 키/순서 보존 단언도 포함.
    모든 케이스는 RED (구현 전 실패) — 단언 대상 함수가 아직 평균식이므로 fail 해야 정상. 이모지 금지, snake_case, from __future__ import annotations.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_kismam.py::test_single_major_fault_dominates tests/test_kismam.py::test_clean_pose_high_score tests/test_kismam.py::test_shoulder_focus_label tests/test_dimensions.py::test_micro_bent_zero_track tests/test_dimensions.py::test_intentional_bend_not_penalized tests/test_dimensions.py::test_stability_does_not_inflate --co -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest --co -q` 가 6 케이스 모두 수집(collect)한다 (import/syntax 에러 0)
    - 6 케이스가 구현 전 실행 시 RED (fail) 또는 — 평균식 함수가 우연히 통과하지 않도록 단언이 방향성을 갖는다 (`--co` 로 수집만 확인, 구현은 Wave 1)
    - 단언이 IPSF 방향(fault<정상, 160° 임계, stability 비인플레)만 검사하고 보유 sweep 수치 타깃을 하드코딩하지 않는다 (grep: 케이스 내 명시 점수 상수가 IPSF 근거 주석을 동반)
  </acceptance_criteria>
  <done>6 신규 케이스가 수집 가능하고 IPSF 방향만 단언한다. 구현 전 RED.</done>
</task>

<task type="auto">
  <name>Task 2: pipeline + anchor RED 케이스 (TRUST-01 / TRUST-03 / TRUST-05 + D-05 GPU-skip)</name>
  <files>backend/tests/test_pipeline_mode3.py, backend/tests/test_anchor_known_answer.py</files>
  <read_first>
    - backend/tests/test_pipeline_mode3.py (수정 대상 — `_video`/`_as_prev` 픽스처, sys.path 부트스트랩 1-21 패턴)
    - 19-PATTERNS.md "신규 테스트" + "신규 파일 test_anchor_known_answer.py" (235-238) — GPU skip marker 구조, analog = test_pipeline_mode3.py 부트스트랩
    - 19-VALIDATION.md Known-Answer Anchor Validation 표 (58-68) + Per-Task Verification Map (TRUST-01/03/05 행)
    - 19-D05-VISION-GROUNDING-SPIKE.md (6 앵커 표 18-27, "방향 판정" 경계 84-90)
    - backend/functions/pipeline/app.py (_angles_to_mean_dict 1515-1538, _mode3_comparison 1572-1629, MODE_SELF 1839-1847, NOT_POLE 1812)
    - 19-RESEARCH.md Wave 0 Gaps (407-413) — pre-existing 실패 격리 목록
  </read_first>
  <action>
    test_pipeline_mode3.py 에 3 케이스 추가:
    (1) `test_display_matches_score_source` — TRUST-01. 표시 각도 dict(현재/기준)가 점수 산출에 쓰는 DTW-정렬 median 과 동일 source 임을 단언. user 측·ref 측이 동일 시간 구간(DTW path 정렬)에서 도출되어 whole-clip nanmean 비대칭이 없음을 검사. 합성 angles 로 jitter 가 있는 프레임을 ref full-clip 에 넣어 "정렬 median ≠ whole-clip mean" 이 구분되는 픽스처 구성.
    (2) `test_unknown_move_gate` — TRUST-03. MODE_SELF 경로에서 미보유 동작(lookup_motion_branch 가 _SAFE_DEFAULT_BRANCH 반환)일 때 confident 97점 대신 근거 플래그("기준 동작 없음 — 절대 자세 기준 평가") + 절대트랙 채점이 적용됨을 단언. fail-closed/raise 금지 — 점수는 주되 근거가 명시되는지. (구현 전엔 게이트 부재라 RED.)
    (3) `test_vision_hook_passthrough` — TRUST-05. v1 vision hook(_apply_vision_veto)이 OFF 시 입력 score_result 를 그대로 반환(identity)함을 단언. 점수 불변.
    신규 파일 test_anchor_known_answer.py 생성:
    - test_pipeline_mode3.py 의 sys.path 부트스트랩 모방.
    - `@pytest.mark.skipif`(RTMW GPU/Pod 부재 조건 — env flag 또는 항상 skip) marker.
    - D-05 6 페어(climb/kip-up/pdshape/elbow-twist-sister/power-spin/peter-pan) parametrize. 단언 = fault 영상 종합 < correct 영상 종합 (방향 판정), 그리고 major 차원(climb=line, kip-up=angle 등)이 지배. 점수 수치 타깃 금지 — 19-D05 표의 ~편차는 주석 근거로만, 단언 임계로 하드코딩 금지 ([[calibration-source-hard-gate]]).
    - above-cutoff(미보유 고득점) synthetic 케이스 1개 — 정상 동작이 high 유지(sensitivity gate).
    이모지 금지. 모든 신규 케이스는 RED 또는 skip.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_pipeline_mode3.py::test_display_matches_score_source tests/test_pipeline_mode3.py::test_unknown_move_gate tests/test_pipeline_mode3.py::test_vision_hook_passthrough tests/test_anchor_known_answer.py --co -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest --co -q` 가 3 pipeline 케이스 + anchor 파일 케이스를 수집한다 (import 에러 0)
    - test_anchor_known_answer.py 가 `pytest -q` 실행 시 skip 으로 표시(GPU 부재 — fail 아님)
    - 6 페어 parametrize 단언이 `fault < correct` 방향과 차원 지배만 검사하고 ~편차/점수 수치를 단언 임계로 사용하지 않는다 (grep: anchor 파일에 19-D05 ~편차 수치가 assert 비교값으로 등장 0건)
    - pre-existing 실패(test_pole_detector / test_pipeline_geminid_wiring / test_spike_gemini_moment_smoke)는 본 plan 이 건드리지 않는다
  </acceptance_criteria>
  <done>3 pipeline RED 케이스 + anchor GPU-skip 파일이 수집/skip 가능하다. 방향만 단언, 수치 curve-fit 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

본 plan 은 테스트 코드만 추가한다 — 신규 endpoint/auth/PII/네트워크 표면 없음. 기존 Firebase 토큰 auth + Firestore 규칙 불변. Trust boundary 변화 없음.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering | 테스트가 보유 sweep 수치를 임계로 박제 (curve-fit) | mitigate | acceptance_criteria 에 "수치 타깃 0건" grep 게이트 — IPSF 방향만 단언 ([[calibration-source-hard-gate]]) |
| T-19-SC | Tampering | npm/pip installs | accept | 본 plan 신규 패키지 설치 0 (기존 pytest/numpy 재사용) — 19-RESEARCH "신규 외부 패키지 없음" |

내부 테스트 추가만 — 위협 표면 낮음 (19-RESEARCH Security Domain 정합).
</threat_model>

<verification>
- `cd backend && python -m pytest tests/test_kismam.py tests/test_dimensions.py tests/test_pipeline_mode3.py --co -q` 신규 케이스 전부 수집
- `cd backend && python -m pytest tests/test_anchor_known_answer.py -q` skip 표시
- 본 plan 직후 신규 케이스는 RED 정상 (구현 Wave 1/2). 기존 통과 케이스 회귀 0 (pre-existing 격리 제외).
</verification>

<success_criteria>
- 4개 테스트 파일에 9 신규 케이스(6 코어 + 3 pipeline) + anchor 파일 6 페어 parametrize 가 수집/skip 가능
- 모든 단언이 IPSF 방향(fault<정상, 160° 임계, stability 비인플레, 표시-점수 동일 source)만 검사
- 보유 sweep 수치 curve-fit 0건
</success_criteria>

<output>
Create `.planning/phases/19-vision-hybrid/19-01-SUMMARY.md` when done
</output>
