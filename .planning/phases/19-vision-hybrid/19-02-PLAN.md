---
phase: 19-vision-hybrid
plan: 02
type: execute
wave: 1
depends_on: [19-01]
files_modified:
  - backend/shared/python/sunity_shared/analysis/kismam.py
  - backend/shared/python/sunity_shared/analysis/dimensions.py
  - backend/shared/python/sunity_shared/analysis/assemble.py
  - app/src/types/analysis.ts
  - backend/shared/python/sunity_shared/models.py
  - docs/contract.md
  - app/src/lib/simulatedResult.ts
autonomous: true
requirements: [SCORE-06, SCORE-07, TRUST-02]
must_haves:
  truths:
    - "단일 major fault 가 종합점수를 지배한다 (평균 희석 제거)"
    - "정상 동작(모든 편차<tol)은 높은 점수를 유지한다 (위양성 없음)"
    - "신전 요구 관절이 160° 미달이면 line 차원이 0점(요소 무효)이다"
    - "의도적 굽힘(expects_extension False) 관절은 0점 트랙 미적용"
    - "DIM_STABILITY 가 높아도 angle/line 이 낮으면 종합이 낮다 (인플레 차단)"
    - "어깨 COACHING_FOCUS 라벨이 '안정성'(떨림) 오인이 아니다"
    - "stability 가 종합에 비기여임이 contributesToOverall=false + weightPercent=0 으로 표시된다 (필드는 OPTIONAL — legacy doc 미존재 시 true)"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/kismam.py"
      provides: "overall_score 감점식 교체 + COACHING_FOCUS 어깨 라벨 정정"
      contains: "def overall_score"
    - path: "backend/shared/python/sunity_shared/analysis/dimensions.py"
      provides: "line_score micro-bent 0점 트랙 + overall_from_dimensions stability 분리"
      contains: "def overall_from_dimensions"
    - path: "backend/shared/python/sunity_shared/analysis/assemble.py"
      provides: "build_dimension_explanation 에 contributesToOverall + 비기여 차원 weightPercent=0"
      contains: "contributesToOverall"
    - path: "app/src/lib/simulatedResult.ts"
      provides: "buildExplanationForSim 가 새 semantics 반영 (stability contributesToOverall=false, weightPercent=0)"
      contains: "contributesToOverall"
  key_links:
    - from: "dimensions.line_score"
      to: "profile.expects_extension"
      via: "신전 요구 관절만 160° 0점 트랙 (의도적 굽힘 위양성 차단)"
      pattern: "expects_extension"
    - from: "dimensions.overall_from_dimensions"
      to: "DIM_ANGLE, DIM_LINE"
      via: "core 차원만 종합 입력, DIM_STABILITY 제외"
      pattern: "DIM_ANGLE"
    - from: "assemble.build_dimension_explanation"
      to: "DimensionExplanation.contributesToOverall"
      via: "stability 비기여 표시 (weightPercent 오도 방지, OPTIONAL 필드)"
      pattern: "contributesToOverall"
---

<objective>
채점 집계 철학을 평균식에서 IPSF 감점식으로 교체한다 (D-01 핵심). 정은지 fault 영상이 94점/89% 로 나온 근본원인 = 이중 단순평균(관절→각도 평균, 차원→종합 평균)이 단일 major fault 를 다수 정상 관절/차원에 희석시키는 것. 이를 (1) kismam.overall_score 를 편차 임계 초과분 누적 감점으로, (2) dimensions.line_score 에 micro-bent 0점 트랙 추가, (3) dimensions.overall_from_dimensions 를 stability 분리 + min-of-core 로 교체한다. 어깨 라벨 오인('안정성')도 정정한다 (TRUST-02). **추가(HIGH-1):** stability 가 종합에서 제외되면 build_dimension_explanation 의 균등 weightPercent([34,33,33])는 거짓 — 비기여 차원에 `contributesToOverall` + `weightPercent=0` 을 주고 3중 계약에 필드 추가. 감점 임계는 IPSF 근거(19-IPSF-DEDUCTION-NOTES)에서만 — 보유 sweep 재calibrate 금지, penalty_per_deg 는 [ASSUMED] v1 휴리스틱.

**ITER-2 BLOCKER-1 반영:** `contributesToOverall` 는 **OPTIONAL TS 필드**(`contributesToOverall?: boolean`)다 — historical Firestore doc + 시뮬 doc 이 미보유하므로 required 면 typecheck/runtime 깨짐. 신 backend 는 항상 emit, UI 는 미존재(legacy)→ `true` default(옛 overall 은 stability 포함). 또한 `app/src/lib/simulatedResult.ts::buildExplanationForSim` 을 새 semantics 로 갱신(stability contributesToOverall=false, weightPercent=0)하고 files_modified 에 포함 — Task 2 가 typecheck 게이트를 통과하도록.

Purpose: 점수가 실제 자세 품질을 반영하게 한다 (PROJECT.md Core Value '점수 신뢰도'). 어떤 영상이든 일반화 — 보유 정은지 셋 overfit 금지.
Output: kismam.py + dimensions.py 집계 교체, COACHING_FOCUS 정정, assemble.build_dimension_explanation contributesToOverall, simulatedResult.ts 갱신, 3중 계약(analysis.ts/models.py/contract.md) DimensionExplanation 필드(OPTIONAL) 동기화.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-vision-hybrid/19-REVIEWS.md
@.planning/phases/19-vision-hybrid/19-IPSF-DEDUCTION-NOTES.md
@.planning/phases/19-vision-hybrid/19-PATTERNS.md
@.planning/phases/19-vision-hybrid/19-D05-VISION-GROUNDING-SPIKE.md
@.planning/phases/19-vision-hybrid/19-01-SUMMARY.md
@backend/shared/python/sunity_shared/analysis/kismam.py
@backend/shared/python/sunity_shared/analysis/dimensions.py
@backend/shared/python/sunity_shared/analysis/assemble.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: kismam 감점식 집계 + 어깨 라벨 정정 (SCORE-06 / TRUST-02)</name>
  <files>backend/shared/python/sunity_shared/analysis/kismam.py</files>
  <behavior>
    - test_single_major_fault_dominates: 한 관절 편차 50° + 나머지≈0 → 종합이 평균식보다 현저히 낮음 (< 70)
    - test_clean_pose_high_score: 모든 편차<tol → 종합 high (>= 90)
    - test_within_tolerance_remains_high: 모든 편차 10°(<tol) → 종합 high
    - test_score_monotonic_decreasing_with_deviation: 0/30/60° → s0>s1>s2 (beyond-tolerance 단조성)
    - test_shoulder_focus_label: COACHING_FOCUS['left_shoulder']/['right_shoulder'] != '안정성', 부위 키워드 중복 없음
  </behavior>
  <read_first>
    - backend/shared/python/sunity_shared/analysis/kismam.py (overall_score 182-189 교체 대상, score_from_deviation 87-93 + assess 114 + top_issues 192 재사용, COACHING_FOCUS 53-62, DEFAULT_WEIGHT 46, DEFAULT_TOLERANCE_DEG, JointAssessment 96-111, JOINT_LABEL_KO 주석 49-52)
    - 19-REVIEWS.md MEDIUM-1 (119-130) — penalty_per_deg = v1 [ASSUMED] 휴리스틱, IPSF fact 아님 + 경계 테스트 4종 (이 섹션은 iter-1 REVIEWS 기준 — iter-2 REVIEWS 에서는 penalty_per_deg 가 잔여 리스크로만 언급)
    - 19-IPSF-DEDUCTION-NOTES.md §A 트랙2 (누적 실행 감점, 회당 표) + "단일 major fault 지배"
    - 19-RESEARCH.md Code Examples "감점식 집계" — overall_score_deductive 형식 + penalty_per_deg [ASSUMED]
    - 19-PATTERNS.md kismam 섹션 — in-place 교체 + clamp 박제 + 라벨 정정 주의
    - backend/tests/test_kismam.py (Wave 0 케이스 — 통과 대상)
  </read_first>
  <action>
    overall_score(182-189) 를 평균식에서 감점식으로 교체: 100 에서 시작 → 관절별 `over = max(0, a.deviation_deg - tol)` 의 누적 감점 `total_penalty += over * penalty_per_deg`. 종합 = `max(0, min(100, int(round(100 - total_penalty))))` (clamp 는 score_from_deviation:93 패턴 박제). tol 은 모듈 상수 `_IPSF_TOLERANCE_DEG`(없으면 추가, 19-IPSF §A ±20° 근거 [CITED])로. **penalty_per_deg 는 모듈 상수 + `# [ASSUMED] v1 휴리스틱 — IPSF 트랙2 비율의 0..100 매핑은 가정. IPSF fact 아님. 보유 sweep 재calibrate 금지 (D-05 경계).` 주석 동반**. 단일 큰 편차가 종합을 끌어내리는 구조 보장 (평균 희석 제거). 경계 행동: within-tolerance high / one-major-dominates / multi-moderate-accumulates / above-cutoff high (Wave 0 케이스가 검증).
    score_from_deviation/assess/top_issues 는 교체 금지 — 관절 편차→점수 인프라 재사용 (19-RESEARCH "Don't Hand-Roll"). DEFAULT_WEIGHT 는 v1 동일 가중 유지.
    COACHING_FOCUS(53-62) 어깨 라벨 정정: 'left_shoulder'/'right_shoulder' = '안정성'(stability=떨림 오인) → STATIC POSE ANGLE 의미 라벨로 변경 (예: '자세각'). JOINT_LABEL_KO 에 이미 "오른쪽 어깨" 부위가 있으므로 FOCUS 값에 '어깨' 키워드 중복 금지 (50-52 주석 박제).
    모듈 docstring 상단에 왜 감점식인지 + 19-IPSF §A 트랙2 인용 + penalty_per_deg 가 [ASSUMED] 임을 명시. from __future__ import annotations, frozen dataclass, snake_case, 이모지 금지.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_kismam.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_single_major_fault_dominates / test_clean_pose_high_score / test_within_tolerance_remains_high / test_score_monotonic_decreasing_with_deviation / test_shoulder_focus_label GREEN
    - 무관한 기존 test_kismam.py 케이스(test_tolerance_gaussian_value / test_top_issues_picks_worst_three / test_assess_rejects_wrong_length / test_part_scores_average_within_part / test_zero_deviation_full_score) 회귀 0 — score_from_deviation/assess/top_issues 불변 (BLOCKER-3: "all old pass" 아닌 "무관한 케이스 회귀 0")
    - grep: overall_score 본문에 `np.nanmean`/단순평균(`/ den`) 패턴 0건 (감점식 교체 확인)
    - penalty_per_deg 상수에 `[ASSUMED]` 주석 + tol 상수에 `[CITED] 19-IPSF` 주석 동반, sweep 결과 인용 0건
  </acceptance_criteria>
  <done>overall_score 가 감점식이고 단일 major 가 종합을 지배. penalty_per_deg = [ASSUMED]. 어깨 라벨 정정. 무관한 케이스 회귀 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: dimensions micro-bent 0점 + stability 분리 + contributesToOverall(OPTIONAL) + simulatedResult + 3중 계약 (SCORE-07 / TRUST-02)</name>
  <files>backend/shared/python/sunity_shared/analysis/dimensions.py, backend/shared/python/sunity_shared/analysis/assemble.py, app/src/types/analysis.ts, backend/shared/python/sunity_shared/models.py, docs/contract.md, app/src/lib/simulatedResult.ts</files>
  <behavior>
    - test_micro_bent_zero_track: expects_extension(knee)=True + rep angle 140° → line_score == 0
    - test_intentional_bend_not_penalized: expects_extension=False 관절 140° → 0점 트랙 미적용
    - test_stability_does_not_inflate: DIM_STABILITY=99 + DIM_ANGLE/LINE 낮음 → overall_from_dimensions <= 50, dimension_scores 키/순서 보존
    - test_overall_from_dimensions_uses_core_dimensions: {angle:40,line:80,stability:99}->40, {stability:99}->99, {}->0
    - (앱 게이트) cd app && npm run typecheck green — simulatedResult.ts 가 새 필드(optional) 와 정합, 기존 DimensionExplanation 생성부 미파손
  </behavior>
  <read_first>
    - backend/shared/python/sunity_shared/analysis/dimensions.py (line_score 226-245 분기 추가, overall_from_dimensions 348-351 교체, expects_extension 필터 238-242, _select_window 270-289 재사용, DIM_*/ABSOLUTE_DIMENSIONS 151-155, _LINE_TOL_DEG/_FULL_EXTENSION_DEG 158-162, stability_score 223)
    - backend/shared/python/sunity_shared/analysis/assemble.py (build_dimension_explanation 196-266 = `_largest_remainder_pct(len(dims))` 균등 weightPercent 256-265, DimensionExplanation out dict 261-265)
    - 19-REVIEWS.md (iter-1) HIGH-1 — weightPercent over contributing-only OR contributesToOverall boolean + stability diagnostic 라벨
    - 19-REVIEWS.md (iter-2) BLOCKER-1 (15-27) — contributesToOverall = OPTIONAL `?: boolean`, legacy 미존재→true default, simulatedResult.ts 갱신 + files_modified 포함, typecheck WITH simulatedResult green
    - 19-IPSF-DEDUCTION-NOTES.md §A 트랙1 (요소 무효 0점, 160°=180°−20°tol)
    - 19-RESEARCH.md Code Examples "micro-bent 0점 트랙" + "종합 stability 분리" + 권장 하이브리드
    - 19-PATTERNS.md dimensions 섹션 + 3중 계약 섹션
    - app/src/types/analysis.ts (DimensionExplanation 263-267 = weightPercent/baseline/deficitSummary, dimensionExplanation 325 Partial Record)
    - backend/shared/python/sunity_shared/models.py (DIMENSION_EXPLANATION_KEYS 35), docs/contract.md (DimensionExplanation 181-194 정의부)
    - app/src/lib/simulatedResult.ts (buildExplanationForSim 211-235 = DimensionExplanation 생성부, out[key] 228-232 weightPercent/baseline/deficitSummary 만; SIM_BASELINE_MODE1/MODE3, GOOD_SCORE_THRESHOLD)
    - backend/tests/test_dimensions.py (Wave 0 케이스 — 통과 대상)
  </read_first>
  <action>
    line_score(226-245) micro-bent 0점 트랙 분기 추가: 신전 요구 관절(`profile.expects_extension(k)` True, 기존 필터 238-242 안에서만 — Pitfall 4 위양성 차단) 의 rep angle 이 `_SPLIT_FAIL_THRESHOLD_DEG`(신규 모듈 상수 = 160.0, 180°−20°tol [CITED: 19-IPSF §A], _LINE_TOL_DEG 158 처럼 IPSF 근거 주석 박제) 미만이면 `return 0` (요소 무효, 비례감점 아님 — 트랙1). 미달 관절 없으면 기존 부족분 가우시안(트랙2) 경로 유지. _select_window(270-289) 공유 helper 재사용 — 별도 window 계산 금지.
    overall_from_dimensions(348-351) 를 단순평균에서 stability 분리 + min-of-core 로 교체: `core = [v for k,v in dimension_scores.items() if k in (DIM_ANGLE, DIM_LINE)]` → `min(core)` (단일 major 차원 지배). core 비면 DIM_STABILITY fallback (절대트랙 단독, Wave 0 `{stability:99}->99` 케이스). DIM_STABILITY 는 dimension_scores 에 표시 유지하되 종합 입력에서만 제외 (Pitfall 2 — 키/순서 보존). 권장 하이브리드: angle 차원 자체는 kismam 감점합(Task 1), 종합은 min-of-core.
    **assemble.build_dimension_explanation(196-266) HIGH-1 수정:** 균등 `_largest_remainder_pct(len(dims))` 가 stability 제외 후 거짓이 되므로 — 기여 차원(DIM_ANGLE/DIM_LINE) 집합을 정의하고: 각 dim out dict 에 `"contributesToOverall": dim in (DIM_ANGLE, DIM_LINE)` 추가. weightPercent 는 **기여 차원에만 largest-remainder 분배**(기여 차원 합=100), 비기여(stability) 차원은 `weightPercent=0`. _largest_remainder_pct 를 기여 차원 개수로 호출. (stability 는 표시 유지하되 diagnostic/auxiliary 로 라벨.)
    **3중 계약 동기화 — contributesToOverall 은 OPTIONAL (ITER-2 BLOCKER-1):** app/src/types/analysis.ts `DimensionExplanation`(263-267)에 `contributesToOverall?: boolean;` **옵셔널** 추가 (required 금지 — 시뮬/legacy doc 미보유) + 주석 "미존재(legacy)→true (옛 overall 은 stability 포함), 신 backend 항상 emit, 비기여=false. weightPercent 도 contributesToOverall=false 면 0." models.py `DIMENSION_EXPLANATION_KEYS`(35) 에 "contributesToOverall" 추가 + 주석에 "옵셔널 — 옛 doc 미보유, UI default true". docs/contract.md DimensionExplanation 정의(181-194)에 `contributesToOverall bool optional` + "stability = false/weightPercent 0, 옛 doc 미보유→true default" 명시.
    **simulatedResult.ts 갱신 (ITER-2 BLOCKER-1):** buildExplanationForSim(211-235) 의 out[key](228-232) 에 새 semantics 반영 — `contributesToOverall: key === 'stability' ? false : true` 추가, stability 의 `weightPercent` 는 0 으로(나머지 기여 차원에만 largest-remainder 분배: base/remainder 계산을 기여 차원 keys 로 한정). backend buildExplanation 와 동일 산식 정합 주석(228 위 "backend 와 동일 산식" 박제 유지). 이로써 typecheck 가 simulatedResult 포함하여 green.
    모듈 docstring 에 stability 분리 이유 + 트랙1 근거 인용. from __future__ import annotations, snake_case, 이모지 금지. simulatedResult.ts 는 2-space indent, single quote.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_dimensions.py -x -q && cd ../app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - test_micro_bent_zero_track / test_intentional_bend_not_penalized / test_stability_does_not_inflate / test_overall_from_dimensions_uses_core_dimensions GREEN
    - 무관한 기존 test_dimensions.py 케이스 회귀 0 (BLOCKER-3 — line_deficits / stability_wobble / select_window 등 불변)
    - `cd app && npm run typecheck` (tsc --noEmit) clean — **simulatedResult.ts 포함** (ITER-2 BLOCKER-1: simulatedResult 가 files_modified 에 있고 새 필드와 정합)
    - grep: analysis.ts DimensionExplanation 의 contributesToOverall 이 `?:`(옵셔널) 로 선언 — required(`contributesToOverall:`) 0건
    - grep: overall_from_dimensions 본문에 `sum(vals) / len` 단순평균 패턴 0건
    - grep: _SPLIT_FAIL_THRESHOLD_DEG = 160.0 에 IPSF 근거 주석([CITED] 19-IPSF) 동반, sweep 인용 0건
    - build_dimension_explanation 출력 각 dim 에 `contributesToOverall` 키 존재, DIM_STABILITY weightPercent==0 + contributesToOverall==False
    - simulatedResult.ts buildExplanationForSim 가 stability 에 contributesToOverall=false + weightPercent=0 emit (grep: contributesToOverall)
    - 3중 계약 — `contributesToOverall` 필드가 analysis.ts(DimensionExplanation, 옵셔널) + models.py(DIMENSION_EXPLANATION_KEYS) + contract.md 에 동시 반영
  </acceptance_criteria>
  <done>line micro-bent 0점 + 종합 stability 분리 + contributesToOverall(OPTIONAL) + simulatedResult 갱신. 3중 계약 lockstep. 무관한 케이스 회귀 0, tsc clean (simulatedResult 포함).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

본 plan 은 ML core 순수함수(numpy) + assemble 조립 + 시뮬 데이터 + 3중 계약 필드만 변경 — 신규 endpoint/auth/PII/네트워크 표면 없음. RTMW angles → 점수 산출은 기존 `_as_tj` shape 검증 안쪽. Firestore flat 저장 불변(점수 schema 차원 키 불변, DimensionExplanation 옵셔널 필드 1개 추가).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-02 | Tampering | 감점 계수가 IPSF 근거 아닌 보유 sweep 에서 도출 (curve-fit) | mitigate | acceptance grep — penalty_per_deg [ASSUMED] + tol [CITED], sweep 인용 0 ([[calibration-source-hard-gate]]) |
| T-19-03 | Information disclosure | weightPercent 가 stability 제외 후 거짓 기여 표시 (사용자 오도) | mitigate | contributesToOverall + 비기여 weightPercent=0, 3중 계약 동기화 (HIGH-1) |
| T-19-13 | Tampering | contributesToOverall required → legacy/시뮬 doc typecheck/runtime 파손 | mitigate | OPTIONAL `?:` + UI default true + simulatedResult 갱신, typecheck WITH simulatedResult green (ITER-2 BLOCKER-1) |
| T-19-04 | DoS | NaN/Inf angle 입력 → 산식 crash | accept | 기존 `_as_tj` ValueError + np.isnan 필터(238-242) 재사용, 신규 표면 없음 |
| T-19-SC | Tampering | npm/pip installs | accept | 신규 패키지 설치 0 (numpy/기존만) |

내부 알고리즘 변경 — 위협 표면 낮음.
</threat_model>

<verification>
- `cd backend && python -m pytest tests/test_kismam.py tests/test_dimensions.py -x -q` GREEN (Wave 0 신규 + 갱신 케이스 통과)
- `cd backend && python -m pytest tests/ -q` 전체 suite green (pre-existing 실패 isolation 제외)
- `cd app && npm run typecheck` clean (simulatedResult.ts 포함)
- 감점 임계가 IPSF 근거에서만 — D-05 6 앵커는 방향검증용(Pod 재개 후), curve-fit 타깃 아님
</verification>

<success_criteria>
- overall_score 감점식 + line micro-bent 0점 + overall_from_dimensions stability 분리 + contributesToOverall(OPTIONAL) 동작
- 단일 major fault 가 종합 지배, 정상 동작 high 유지(위양성 0), 어깨 라벨 정정
- 3중 계약 lockstep (DimensionExplanation contributesToOverall 옵셔널 추가) + simulatedResult 정합
- 전체 backend suite + tsc green (simulatedResult 포함)
</success_criteria>

<output>
Create `.planning/phases/19-vision-hybrid/19-02-SUMMARY.md` when done — schema 키 추가로 EAS 재빌드 + sam build --use-container 필요 명시
</output>
