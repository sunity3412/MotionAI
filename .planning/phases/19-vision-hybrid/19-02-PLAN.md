---
phase: 19-vision-hybrid
plan: 02
type: execute
wave: 1
depends_on: [19-01]
files_modified:
  - backend/shared/python/sunity_shared/analysis/kismam.py
  - backend/shared/python/sunity_shared/analysis/dimensions.py
  - app/src/types/analysis.ts
  - backend/shared/python/sunity_shared/models.py
  - docs/contract.md
autonomous: true
requirements: [SCORE-06, SCORE-07, TRUST-02]
must_haves:
  truths:
    - "단일 major fault 가 종합점수를 지배한다 (평균 희석 제거)"
    - "정상 동작(모든 편차<tol)은 높은 점수를 유지한다 (위양성 없음)"
    - "신전 요구 관절이 160° 미달이면 line 차원이 0점(요소 무효)이다"
    - "의도적 굽힘(expects_extension False) 관절은 0점 트랙 미적용"
    - "DIM_STABILITY 가 높아도 angle/line 이 낮으면 종합이 낮다 (인플레 차단)"
    - "어깨 COACHING_FOCUS 라벨이 '안정성'(떨림) 오인이 아니다"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/kismam.py"
      provides: "overall_score 감점식 교체 + COACHING_FOCUS 어깨 라벨 정정"
      contains: "def overall_score"
    - path: "backend/shared/python/sunity_shared/analysis/dimensions.py"
      provides: "line_score micro-bent 0점 트랙 + overall_from_dimensions stability 분리"
      contains: "def overall_from_dimensions"
  key_links:
    - from: "dimensions.line_score"
      to: "profile.expects_extension"
      via: "신전 요구 관절만 160° 0점 트랙 (의도적 굽힘 위양성 차단)"
      pattern: "expects_extension"
    - from: "dimensions.overall_from_dimensions"
      to: "DIM_ANGLE, DIM_LINE"
      via: "core 차원만 종합 입력, DIM_STABILITY 제외"
      pattern: "DIM_ANGLE"
---

<objective>
채점 집계 철학을 평균식에서 IPSF 감점식으로 교체한다 (D-01 핵심). 정은지 fault 영상이 94점/89% 로 나온 근본원인 = 이중 단순평균(관절→각도 평균, 차원→종합 평균)이 단일 major fault 를 다수 정상 관절/차원에 희석시키는 것. 이를 (1) kismam.overall_score 를 편차 임계 초과분 누적 감점으로, (2) dimensions.line_score 에 micro-bent 0점 트랙 추가, (3) dimensions.overall_from_dimensions 를 stability 분리 + min-of-core 로 교체한다. 어깨 라벨 오인('안정성')도 정정한다 (TRUST-02). 감점 임계는 IPSF 근거(19-IPSF-DEDUCTION-NOTES)에서만 — 보유 sweep 재calibrate 금지.

Purpose: 점수가 실제 자세 품질을 반영하게 한다 (PROJECT.md Core Value '점수 신뢰도'). 어떤 영상이든 일반화 — 보유 정은지 셋 overfit 금지.
Output: kismam.py + dimensions.py 집계 함수 교체, COACHING_FOCUS 정정, 3중 계약(analysis.ts/models.py/contract.md) 동기화(차원 schema 또는 의미 변경 시).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-vision-hybrid/19-IPSF-DEDUCTION-NOTES.md
@.planning/phases/19-vision-hybrid/19-PATTERNS.md
@.planning/phases/19-vision-hybrid/19-D05-VISION-GROUNDING-SPIKE.md
@.planning/phases/19-vision-hybrid/19-01-SUMMARY.md
@backend/shared/python/sunity_shared/analysis/kismam.py
@backend/shared/python/sunity_shared/analysis/dimensions.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: kismam 감점식 집계 + 어깨 라벨 정정 (SCORE-06 / TRUST-02)</name>
  <files>backend/shared/python/sunity_shared/analysis/kismam.py</files>
  <behavior>
    - test_single_major_fault_dominates: 한 관절 편차 50° + 나머지≈0 → 종합이 평균식보다 현저히 낮음
    - test_clean_pose_high_score: 모든 편차<tol → 종합 high
    - test_shoulder_focus_label: COACHING_FOCUS['left_shoulder']/['right_shoulder'] != '안정성', 부위 키워드 중복 없음
  </behavior>
  <read_first>
    - backend/shared/python/sunity_shared/analysis/kismam.py (overall_score 182-189 교체 대상, score_from_deviation 87-93 + assess 114 + top_issues 192 재사용 금지교체, COACHING_FOCUS 53-62, DEFAULT_WEIGHT 46, JointAssessment 96-111, JOINT_LABEL_KO 주석 49-52)
    - 19-IPSF-DEDUCTION-NOTES.md §A 트랙2 (누적 실행 감점, 회당 표) + "단일 major fault 지배" (38-42)
    - 19-RESEARCH.md Code Examples "감점식 집계" (256-271) — overall_score_deductive 형식 + penalty_per_deg [ASSUMED]
    - 19-PATTERNS.md kismam 섹션 (36-62) — in-place 교체 + clamp 박제 + 라벨 정정 주의
    - backend/tests/test_kismam.py (Wave 0 케이스 — 통과 대상)
  </read_first>
  <action>
    overall_score(182-189) 를 평균식에서 감점식으로 교체: 100 에서 시작 → 관절별 `over = max(0, a.deviation_deg - tol)` 의 누적 감점 `total_penalty += over * penalty_per_deg`. 종합 = `max(0, min(100, int(round(100 - total_penalty))))` (clamp 는 score_from_deviation:93 패턴 박제). tol 은 모듈 상수 `_IPSF_TOLERANCE_DEG`(없으면 추가, 19-IPSF §A ±20° 근거 [CITED])로. penalty_per_deg 는 [ASSUMED] 초기값 — IPSF 트랙2 비율 매핑 근거 주석 동반, **보유 sweep 재calibrate 금지** (D-05 경계). 단일 큰 편차가 종합을 끌어내리는 구조 보장 (평균 희석 제거).
    score_from_deviation/assess/top_issues 는 교체 금지 — 관절 편차→점수 인프라 재사용 (19-RESEARCH "Don't Hand-Roll"). DEFAULT_WEIGHT 는 감점식에서 checkpoint 중요도로 재해석 가능하나 v1 은 동일 가중 유지.
    COACHING_FOCUS(53-62) 어깨 라벨 정정: 'left_shoulder'/'right_shoulder' = '안정성'(stability=떨림 오인) → STATIC POSE ANGLE 의미의 라벨로 변경 (예: '자세각' 또는 도메인 정합 한글). JOINT_LABEL_KO 에 이미 "오른쪽 어깨" 부위가 있으므로 FOCUS 값에 '어깨' 키워드 중복 금지 (50-52 주석 박제 — "오른쪽 어깨 어깨 안정성" 같은 중복 회피).
    모듈 docstring 상단에 왜 감점식인지 + 19-IPSF-DEDUCTION-NOTES §A 트랙2 인용 추가 (PATTERNS "모듈 docstring = 왜 + 도메인 인용"). from __future__ import annotations, frozen dataclass, snake_case, 이모지 금지.
    DEFAULT_WEIGHT/score_from_deviation 의 시그니처가 변하면 호출처(dimensions.stability_score 223 등) 회귀 확인.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_kismam.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_single_major_fault_dominates / test_clean_pose_high_score / test_shoulder_focus_label GREEN
    - 기존 test_kismam.py 케이스 회귀 0 (단조성/worst-3 등 — score_from_deviation/assess/top_issues 불변)
    - grep: overall_score 본문에 `np.nanmean`/단순평균(`/ den`) 패턴 0건 (감점식 교체 확인)
    - penalty_per_deg/tol 상수에 IPSF 근거 주석([ASSUMED] 또는 [CITED]) 동반, sweep 결과 인용 0건
  </acceptance_criteria>
  <done>overall_score 가 감점식이고 단일 major 가 종합을 지배한다. 어깨 라벨 오인 제거. 기존 케이스 회귀 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: dimensions micro-bent 0점 + stability 분리 + 3중 계약 (SCORE-07 / TRUST-02)</name>
  <files>backend/shared/python/sunity_shared/analysis/dimensions.py, app/src/types/analysis.ts, backend/shared/python/sunity_shared/models.py, docs/contract.md</files>
  <behavior>
    - test_micro_bent_zero_track: expects_extension(knee)=True + rep angle 140° → line_score == 0
    - test_intentional_bend_not_penalized: expects_extension=False 관절 140° → 0점 트랙 미적용
    - test_stability_does_not_inflate: DIM_STABILITY=99 + DIM_ANGLE/LINE 낮음 → overall_from_dimensions 낮음, dimension_scores 키/순서 보존
  </behavior>
  <read_first>
    - backend/shared/python/sunity_shared/analysis/dimensions.py (line_score 226-245 분기 추가, overall_from_dimensions 348-351 교체, expects_extension 필터 238-242, _select_window 270-289 재사용, DIM_*/ABSOLUTE_DIMENSIONS 151-155, _LINE_TOL_DEG/_FULL_EXTENSION_DEG 158-162, stability_score 223)
    - 19-IPSF-DEDUCTION-NOTES.md §A 트랙1 (요소 무효 0점, 160°=180°−20°tol)
    - 19-RESEARCH.md Code Examples "micro-bent 0점 트랙" (273-282) + "종합 stability 분리" (284-295) + 권장 하이브리드(295)
    - 19-RESEARCH.md Pitfall 2 (stability 분리 contract) + Pitfall 4 (micro-bent 위양성)
    - 19-PATTERNS.md dimensions 섹션 (66-97) + 3중 계약 섹션 (206-216)
    - app/src/types/analysis.ts (AnalysisResult/dimensionScores 317-374, dimensionScores 322 Partial Record), backend/shared/python/sunity_shared/models.py (DIM_*/SCORE_DIMENSIONS 14-23)
    - backend/tests/test_dimensions.py (Wave 0 케이스 — 통과 대상)
  </read_first>
  <action>
    line_score(226-245) 에 micro-bent 0점 트랙 분기 추가: 신전 요구 관절(`profile.expects_extension(k)` True, 기존 필터 238-242 안에서만 — Pitfall 4 위양성 차단) 의 rep angle 이 `_SPLIT_FAIL_THRESHOLD_DEG`(신규 모듈 상수 = 160.0, 180°−20°tol [CITED: 19-IPSF-DEDUCTION-NOTES §A], 기존 _LINE_TOL_DEG 158 처럼 IPSF 근거 주석 박제) 미만이면 `return 0` (요소 무효, 비례감점 아님 — 트랙1). 미달 관절 없으면 기존 부족분 가우시안(트랙2) 경로 유지. _select_window(270-289) 공유 helper 재사용 — 별도 window 계산 금지 (drift 방지).
    overall_from_dimensions(348-351) 를 단순평균에서 stability 분리 + min-of-core 로 교체: `core = [v for k,v in dimension_scores.items() if k in (DIM_ANGLE, DIM_LINE)]` → `min(core)` (단일 major 차원 지배). core 비면 DIM_STABILITY fallback (절대트랙 단독). DIM_STABILITY 는 dimension_scores 에 표시는 유지하되 종합 입력에서만 제외 (Pitfall 2 — 키/순서 보존, Mode3 delta 는 절대차원 유지). 권장 하이브리드: angle 차원 자체는 kismam 감점합(Task 1), 종합은 min-of-core.
    3중 계약 동기화 — dimensionScores 키/차원 의미가 변경되지 않으면(stability 표시 유지, 키 보존) schema 불변 → analysis.ts/models.py/contract.md 변경 불필요. 단 "stability 가 종합에서 분리됨"을 docs/contract.md §0(점수 산출) 에 명시(의미 변경) + models.py 주석 + analysis.ts 주석 동시 갱신 (Anti-Pattern: 한쪽만 수정 금지). 차원 키 추가/제거는 없음 — 의미 주석만 lockstep. schema 키 자체 변경 시에만 EAS 재빌드 + sam build --use-container 필요(본 task 는 키 불변이므로 재배포 불요, SUMMARY 에 명시).
    모듈 docstring 에 stability 분리 이유 + 트랙1 근거 인용. from __future__ import annotations, snake_case, 이모지 금지.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_dimensions.py -x -q && cd ../app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - test_micro_bent_zero_track / test_intentional_bend_not_penalized / test_stability_does_not_inflate GREEN
    - 기존 test_dimensions.py 케이스 회귀 0
    - app `npm run typecheck` (tsc --noEmit) clean
    - grep: overall_from_dimensions 본문에 `sum(vals) / len` 단순평균 패턴 0건
    - grep: _SPLIT_FAIL_THRESHOLD_DEG = 160.0 에 IPSF 근거 주석([CITED] 19-IPSF) 동반, sweep 인용 0건
    - 3중 계약 — dimensionScores 차원 키가 analysis.ts/models.py 에서 동일 유지 (키 추가/삭제 0), 의미 변경 주석이 contract.md + models.py + analysis.ts 에 동시 반영
  </acceptance_criteria>
  <done>line micro-bent 0점 트랙 + 종합 stability 분리 동작. 3중 계약 lockstep. 기존 케이스 회귀 0, tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

본 plan 은 ML core 순수함수(numpy) + 3중 계약 주석만 변경 — 신규 endpoint/auth/PII/네트워크 표면 없음. RTMW angles → 점수 산출은 기존 `_as_tj` shape 검증(이미 있음) 안쪽. Firestore flat 저장 불변(점수 schema 키 불변).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-02 | Tampering | 감점 계수가 IPSF 근거 아닌 보유 sweep 에서 도출 (curve-fit) | mitigate | acceptance_criteria grep — 상수에 IPSF [CITED]/[ASSUMED] 주석 강제, sweep 인용 0 ([[calibration-source-hard-gate]]) |
| T-19-03 | Information disclosure | 점수 schema 의미 변경이 3중 계약 한쪽만 반영 | mitigate | contract.md + models.py + analysis.ts 동시 갱신 acceptance gate |
| T-19-04 | DoS | NaN/Inf angle 입력 → 산식 crash | accept | 기존 `_as_tj` ValueError + np.isnan 필터(238-242) 재사용, 신규 표면 없음 |
| T-19-SC | Tampering | npm/pip installs | accept | 신규 패키지 설치 0 (numpy/기존만) — 19-RESEARCH "신규 외부 패키지 없음" |

내부 알고리즘 변경 — 위협 표면 낮음 (19-RESEARCH Security Domain: V2/V3/V4/V6 미해당, V5 기존 가드 재사용).
</threat_model>

<verification>
- `cd backend && python -m pytest tests/test_kismam.py tests/test_dimensions.py -x -q` GREEN (Wave 0 신규 케이스 통과)
- `cd backend && python -m pytest tests/ -q` 전체 suite green (pre-existing 실패 isolation 제외)
- `cd app && npm run typecheck` clean
- 감점 임계가 IPSF 근거에서만 — D-05 6 앵커는 방향검증용(Pod 재개 후), curve-fit 타깃 아님
</verification>

<success_criteria>
- overall_score 감점식 + line micro-bent 0점 + overall_from_dimensions stability 분리 동작
- 단일 major fault 가 종합 지배, 정상 동작 high 유지(위양성 0), 어깨 라벨 정정
- 3중 계약 lockstep (키 불변 + 의미 주석 동기화)
- 전체 backend suite + tsc green
</success_criteria>

<output>
Create `.planning/phases/19-vision-hybrid/19-02-SUMMARY.md` when done
</output>
