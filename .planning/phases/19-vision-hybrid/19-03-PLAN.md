---
phase: 19-vision-hybrid
plan: 03
type: execute
wave: 1
depends_on: [19-01]
files_modified:
  - app/src/lib/joints.ts
  - app/scripts/_smoke_joints_normalize.mjs
autonomous: false
requirements: [TRUST-04]
must_haves:
  truths:
    - "3D 골격 joints3d 가 골반중심 recenter + 몸통길이 정규화되어 viewer frustum 안에 들어온다"
    - "hip/shoulder 인덱스를 jointKeys 인자(COCO-17 KEYPOINT_NAMES)에서 indexOf 로 도출한다 (8 angle JOINT_KEYS 아님)"
    - "hip/shoulder 누락·torso=0 시 centroid+bbox fallback, 최후엔 그 frame null — raw 픽셀 passthrough 없음"
    - "synthetic raw COCO-17 frame(중심~(320,240)) 정규화 후 maxAbsCoord <= 3"
  artifacts:
    - path: "app/src/lib/joints.ts"
      provides: "reshapePose3dData 에 COCO-17 hip/shoulder indexOf recenter + torso/bbox normalize + null 최후수단"
      contains: "indexOf"
    - path: "app/scripts/_smoke_joints_normalize.mjs"
      provides: "synthetic raw COCO-17 정규화 → maxAbsCoord<=3 자동 수치검증 (JS 러너 부재 우회)"
      contains: "maxAbsCoord"
  key_links:
    - from: "app/src/lib/joints.ts::reshapePose3dData"
      to: "app/src/components/PoseViewer3D.tsx"
      via: "origin-centered normalized 좌표 → camera distance 3 / fov 50 frustum 내"
      pattern: "reshapePose3dData"
---

<objective>
3D 골격이 실기기에서 렌더되지 않는 버그를 고친다 (TRUST-04). 근본원인 = joints3d 가 RAW RTMW 픽셀좌표(x,y ~0-640 uncentered, z MotionBert-scale, pole_aligned 회전만)인데 viewer 는 origin-centered normalized 좌표(distance 3, fov 50, sphere r=0.04)를 기대 → 골격 중심 ~(320,240) 수백 단위 spread 가 frustum 밖 → GL 이 빈 회색으로 clear. 정규화를 읽는 쪽(joints.ts reshapePose3dData)에 두어 과거 doc 도 즉시 고치고 backend·3중 계약 불변.

**리뷰 반영:**
- HIGH-3: hip/shoulder 인덱스를 `jointKeys` 인자(joints3dKeys = COCO-17 `skeleton.KEYPOINT_NAMES`)에서 `indexOf('left_hip')` 등으로 도출 — 8 angle JOINT_KEYS 아님. 모든 좌표 finite 검증 후 렌더.
- HIGH-4: hip/shoulder 누락·torso=0 시 raw passthrough 금지 — centroid+bbox fallback, 최후엔 그 frame `null`. 자동 수치검증(maxAbsCoord<=3) 추가 (JS 러너 부재라 standalone .mjs smoke).

Purpose: 파일럿 데모에서 사용자가 본인/정은지 3D 골격을 실제로 본다. 과거 분석 기록도 즉시 렌더.
Output: joints.ts reshapePose3dData 정규화 + _smoke_joints_normalize.mjs 수치검증. backend/계약 불변.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-vision-hybrid/19-REVIEWS.md
@.planning/phases/19-vision-hybrid/19-PATTERNS.md
@app/src/lib/joints.ts
@app/src/components/PoseViewer3D.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: reshapePose3dData COCO-17 indexOf recenter + torso/bbox normalize + null 최후수단 (TRUST-04)</name>
  <files>app/src/lib/joints.ts, app/scripts/_smoke_joints_normalize.mjs</files>
  <read_first>
    - app/src/lib/joints.ts (reshapePose3dData 24-57 교체 대상, graceful null 정책 12-13/29-41, Firestore flat 주석 4-11, jointKeys 인자 = COCO-17 키 이름 list)
    - app/src/app/analysis/result.tsx (634-639 — reshapePose3dData(result.joints3d, result.joints3dKeys ?? [], result.joints3dFrames) 호출 = jointKeys 가 joints3dKeys=COCO-17 확인)
    - app/src/components/PoseViewer3D.tsx (camera position [0,0,3] / fov 50 / sphereGeometry [0.04] 345-356/157-168 — 정규화 기대값 근거, ErrorBoundary 24-29/76-98)
    - 19-REVIEWS.md HIGH-3 (86-99) — jointKeys.indexOf('left_hip'/'right_hip'/'left_shoulder'/'right_shoulder') + finite 검증 / HIGH-4 (101-111) — primary torso / fallback centroid+bbox / last-resort null + maxAbsCoord<=3
    - backend/shared/python/sunity_shared/analysis/skeleton.py (KEYPOINT_NAMES — COCO-17 순서, left_hip/right_hip/left_shoulder/right_shoulder 정확한 문자열 확인)
    - 19-RESEARCH.md Pattern 4 (선택지 B 권장) + Pitfall 3 (저장 vs 읽기) + Security V5/DoS (nan_to_num + 0-div 가드)
    - 19-PATTERNS.md joints.ts 섹션 — graceful null + length guard + flat 제약 주석 컨벤션
    - app/scripts/seed-reference-motions.mjs (.mjs 스크립트 컨벤션 — node 실행 헤더/exit code 패턴)
  </read_first>
  <action>
    reshapePose3dData(24-57) 에 frame별 정규화를 추가 (선택지 B — 읽는 쪽). 인덱스 도출은 **jointKeys 인자에서** (HIGH-3):
    함수 진입 시 1회 `const li = jointKeys.indexOf('left_hip')`, `const ri = jointKeys.indexOf('right_hip')`, `const lsi = jointKeys.indexOf('left_shoulder')`, `const rsi = jointKeys.indexOf('right_shoulder')` (정확한 문자열은 skeleton.KEYPOINT_NAMES 에서 확인). 8 angle JOINT_KEYS 참조 금지.
    각 frame `xyz[]` 마다:
    (1) primary (hip/shoulder 모두 존재 + 좌표 finite): hip midpoint `(left_hip + right_hip)/2` 를 center 로 모든 관절에서 차감 → torso length = shoulder_mid↔hip_mid 유클리드 거리 → 모든 좌표를 torso length 로 나눈다 (origin-centered normalized, viewer distance3/fov50 frustum 내).
    (2) fallback (hip 또는 shoulder 인덱스 -1 이거나 좌표 non-finite, 또는 torso length < epsilon): **raw passthrough 금지** — finite 관절들의 centroid 를 center 로, scale = max-axis range(또는 bbox 대각) 로 나눈다. centroid+bbox 정규화도 origin-centered normalized 를 보장.
    (3) last-resort (finite 관절 0개 또는 scale 산출 불가): 그 frame 을 `null` 로 표기 → out 에서 제외하거나 (기존 null 정책 정신) frame skip. **unnormalized raw 좌표를 out 에 넣지 않는다.**
    모든 좌표 finite 검증 (HIGH-3): NaN/Inf → fallback 또는 last-resort 분기로. PoseViewer3D ErrorBoundary 는 안전망일 뿐, 좌표 단계에서 차단.
    Firestore flat 제약 주석(4-11) 정신 유지 — 정규화는 flat→3D reshape 에 흡수 (저장부/3중 계약 불변, 과거 doc 호환). 정규화 이유 + 19-REVIEWS HIGH-3/HIGH-4 + Pattern 4 인용 주석 추가. 2-space indent, single quote, `import type` for 타입, 이모지 금지.
    result.tsx / PoseViewer3D.tsx 변경 불필요 (joints.ts 가 정규화 흡수).

    **수치검증 스크립트** app/scripts/_smoke_joints_normalize.mjs 생성 (JS 테스트 러너 부재 우회 — HIGH-4 acceptance):
    - reshapePose3dData 의 정규화 로직을 .mjs 에서 재현(또는 좌표 수학을 동일 함수로 추출해 import — TS 직접 import 불가하므로 정규화 수학을 순수 JS 로 복제하되 joints.ts 와 동일 알고리즘임을 주석 명시).
    - synthetic raw COCO-17 frame 구성: 17 keypoint 를 중심 ~(320, 240) 주변에 픽셀 스케일로 배치 (실제 RTMW 미정규화 모사). left_hip/right_hip/left_shoulder/right_shoulder 위치 명시.
    - 정규화 실행 → 모든 좌표 절대값 max(maxAbsCoord) 계산 → `if (maxAbsCoord > 3) { process.exit(1) }` (acceptance: <= 3). hip/shoulder 누락 fallback 케이스 + last-resort null 케이스도 각 1개 단언 (fallback 도 <=3, null 케이스는 frame 제외 확인).
    - PASS 시 `console` 없이 exit 0 (CLAUDE.md console.log 자제 — 단 스크립트 진단 출력은 허용, 슬롭 금지). 이모지 금지.
  </action>
  <verify>
    <automated>cd app && npm run typecheck && node scripts/_smoke_joints_normalize.mjs && echo "SMOKE_PASS maxAbsCoord<=3"</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && npm run typecheck` (tsc --noEmit) clean
    - hip/shoulder 인덱스가 `jointKeys.indexOf('left_hip'/'right_hip'/'left_shoulder'/'right_shoulder')` 로 도출 (grep: indexOf 4건, 8 angle JOINT_KEYS 참조 0)
    - 모든 좌표 finite 검증 분기 존재 (Number.isFinite 또는 동등)
    - fallback (centroid + bbox/max-axis) 분기 + last-resort null 분기 존재 — raw passthrough 경로 없음 (grep: 미정규화 frame.push 직행 0)
    - `node scripts/_smoke_joints_normalize.mjs` exit 0 — synthetic raw COCO-17(중심~(320,240)) 정규화 후 maxAbsCoord <= 3, fallback 케이스도 <=3, null 케이스 frame 제외
    - backend/app.py joints3d 저장부 + 3중 계약(analysis.ts space enum) 불변 (이 task files_modified 에 미포함)
  </acceptance_criteria>
  <done>reshapePose3dData 가 COCO-17 indexOf recenter + torso/bbox normalize, fallback/null 최후수단. raw passthrough 0. smoke maxAbsCoord<=3. tsc clean. backend 불변.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 실기기 3D 골격 렌더 육안 검증 (TRUST-04)</name>
  <action>Task 1 의 reshapePose3dData 정규화 결과를 실기기/시뮬레이터 result 화면에서 육안 확인한다. smoke 스크립트가 수치 정규화(maxAbsCoord<=3)는 자동 검증하지만, 실제 GL frustum 내 렌더는 디바이스 육안이 유일한 동작 게이트.</action>
  <what-built>reshapePose3dData 가 joints3d 를 COCO-17 hip 중심 recenter + torso/bbox 정규화하여 viewer frustum 안으로 보낸다. backend·계약 불변이라 과거 분석 doc(raw 픽셀좌표)도 즉시 정규화된다. smoke 스크립트가 maxAbsCoord<=3 을 자동 검증했고, 디바이스 육안이 GL 렌더 최종 게이트.</what-built>
  <how-to-verify>
    1. 실기기 또는 시뮬레이터에서 앱 실행 (EAS preview/development build 또는 expo run).
    2. 기존 분석 결과 1개(과거 raw 픽셀좌표 doc)와 신규 분석 1개의 result 화면 진입.
    3. 3D 골격 뷰어에서 골격이 화면 회색 영역이 아니라 카메라 안에 실제로 표시되는지 확인.
    4. OrbitControls 제스처로 회전/줌 시 골격이 frustum 안에 유지되는지 확인.
    5. occlusion 있던 과거 분석(hip/shoulder 일부 누락)도 fallback 정규화로 렌더되는지 확인.
    참고: belle TestFlight 실기기 검증은 다음 native build 시점(STATE.md Wave 2 override 박제)에 함께 가능.
  </how-to-verify>
  <resume-signal>"approved" 또는 발견한 문제(blank/잘림/회전 시 frustum 이탈 등) 기술</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

본 plan 은 앱 클라이언트 좌표 변환 순수함수 + standalone smoke 스크립트만 변경 — 신규 endpoint/auth/PII/네트워크 표면 없음. Firestore 읽기 데이터(joints3d flat)는 기존 경로, 저장부 불변.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05 | DoS (렌더) | torso=0 또는 NaN/Inf 좌표 → 나눗셈 crash / GL 컨텍스트 죽음 | mitigate | finite 검증 + torso<epsilon → centroid+bbox fallback, last-resort null, PoseViewer3D ErrorBoundary 안전망 |
| T-19-06 | DoS | hip/shoulder occlusion 누락 → 정규화 실패 / raw passthrough → frustum 밖 blank | mitigate | indexOf -1 → centroid+bbox fallback (raw passthrough 금지), smoke maxAbsCoord<=3 검증 (HIGH-4) |
| T-19-11 | Tampering | 잘못된 키셋(8 angle JOINT_KEYS) 인덱스 → 엉뚱한 점 중심 정규화 | mitigate | jointKeys 인자(COCO-17)에서 indexOf, acceptance grep (HIGH-3) |
| T-19-SC | Tampering | npm installs | accept | 신규 패키지 설치 0 (기존 react-three-fiber/drei + node stdlib 재사용) |

앱 좌표 변환만 — 위협 표면 낮음.
</threat_model>

<verification>
- `cd app && npm run typecheck` clean
- `node scripts/_smoke_joints_normalize.mjs` exit 0 (maxAbsCoord<=3, fallback/null 케이스 포함)
- 실기기/시뮬레이터 육안 — 신규+과거 doc 둘 다 3D 골격 frustum 내 렌더 (checkpoint)
- backend joints3d 저장부 + 3중 계약 불변 확인
</verification>

<success_criteria>
- reshapePose3dData 가 COCO-17 indexOf hip-center recenter + torso/bbox normalize, fallback/null 최후수단 (raw passthrough 0)
- smoke 수치검증 maxAbsCoord<=3 + tsc clean
- 실기기 골격 렌더 육안 PASS (과거 doc 포함)
- backend·계약 불변
</success_criteria>

<output>
Create `.planning/phases/19-vision-hybrid/19-03-SUMMARY.md` when done
</output>
