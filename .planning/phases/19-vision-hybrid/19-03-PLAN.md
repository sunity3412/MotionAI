---
phase: 19-vision-hybrid
plan: 03
type: execute
wave: 1
depends_on: [19-01]
files_modified:
  - app/src/lib/joints.ts
autonomous: false
requirements: [TRUST-04]
must_haves:
  truths:
    - "3D 골격 joints3d 가 골반중심 recenter + 몸통길이 정규화되어 viewer frustum 안에 들어온다"
    - "과거 분석 doc(raw 픽셀좌표)도 즉시 정규화되어 렌더된다"
    - "torso=0 또는 hip 누락 시 graceful fallback (0-div/NaN crash 없음)"
  artifacts:
    - path: "app/src/lib/joints.ts"
      provides: "reshapePose3dData 에 hip-center recenter + torso normalize"
      contains: "reshapePose3dData"
  key_links:
    - from: "app/src/lib/joints.ts::reshapePose3dData"
      to: "app/src/components/PoseViewer3D.tsx"
      via: "origin-centered normalized 좌표 → camera distance 3 / fov 50 frustum 내"
      pattern: "reshapePose3dData"
---

<objective>
3D 골격이 실기기에서 렌더되지 않는 버그를 고친다 (TRUST-04). 근본원인 = joints3d 가 RAW RTMW 픽셀좌표(x,y ~0-640 uncentered, z MotionBert-scale, pole_aligned 회전만·recenter/rescale 안 함)인데 viewer 는 origin-centered normalized 좌표(distance 3, fov 50, sphere r=0.04)를 기대 → 골격 중심 ~(320,240) 수백 단위 spread 가 frustum 밖 → GL 이 빈 회색으로 clear. 정규화를 읽는 쪽(joints.ts reshapePose3dData)에 두어 과거 doc 도 즉시 고치고 backend·3중 계약 불변(가장 안전, RESEARCH 권장 선택지 B).

Purpose: 파일럿 데모에서 사용자가 본인/정은지 3D 골격을 실제로 본다. 과거 분석 기록도 즉시 렌더.
Output: app/src/lib/joints.ts 의 reshapePose3dData 에 frame별 recenter+normalize 추가. backend/계약 불변.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-vision-hybrid/19-PATTERNS.md
@app/src/lib/joints.ts
@app/src/components/PoseViewer3D.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: reshapePose3dData 에 hip-center recenter + torso normalize (TRUST-04)</name>
  <files>app/src/lib/joints.ts</files>
  <read_first>
    - app/src/lib/joints.ts (reshapePose3dData 24-57 교체 대상, graceful null 정책 12-13/29-41, Firestore flat 주석 4-11, JointKeys lookup)
    - app/src/components/PoseViewer3D.tsx (camera position [0,0,3] / fov 50 / sphereGeometry [0.04] 345-356/157-168 — 정규화 기대값 근거, ErrorBoundary 24-29/76-98)
    - 19-RESEARCH.md Pattern 4 (177-190) — 선택지 B 권장, recenter=hip midpoint 빼기, normalize=torso length(shoulder_mid↔hip_mid) 나누기 + A4 occlusion fallback
    - 19-RESEARCH.md Pitfall 3 (저장 vs 읽기) + Security V5/DoS (nan_to_num + 0-div 가드)
    - 19-PATTERNS.md joints.ts 섹션 (163-177) — graceful null + length guard + flat 제약 주석 컨벤션
  </read_first>
  <action>
    reshapePose3dData(24-57) 에 정규화 단계를 추가 (선택지 B — 읽는 쪽). 각 frame 마다:
    (1) recenter: left_hip / right_hip 키포인트 인덱스를 JointKeys 로 lookup → hip midpoint `(left_hip + right_hip)/2` 를 모든 관절 좌표에서 뺀다 (origin-centered).
    (2) normalize: torso length = shoulder midpoint↔hip midpoint 유클리드 거리. 모든 좌표를 torso length 로 나눈다 (스케일 정규화 → viewer distance 3 / fov 50 frustum 내).
    graceful fallback (기존 12-13/29-41 null 정책 박제, Security V5 + A4):
    - torso length 이 0 또는 매우 작음(epsilon 미만) → 나누기 생략(recenter 만) 또는 안전 기본 스케일.
    - hip/shoulder 키포인트 누락(occlusion) → 해당 frame recenter/normalize skip, 기존 graceful null/skip 정책 유지.
    - NaN/Inf → finite 처리 (PoseViewer3D ErrorBoundary 가 GL crash 방지하지만 좌표 단계에서 차단).
    Firestore flat 제약 주석(4-11) 정신 유지 — 정규화는 flat→3D reshape 단계에 자연스럽게 흡수 (저장부/3중 계약 불변, 과거 doc 호환). 정규화 이유 + 19-RESEARCH Pattern 4 / Pitfall 3 인용 주석 추가.
    브랜드 컬러/스페이싱 하드코딩 무관(좌표 변환만). 이모지 금지, 2-space indent, single quote, `import type` for 타입.
    result.tsx / PoseViewer3D.tsx 변경 불필요 (joints.ts 가 정규화 흡수 — PATTERNS 195-202 확인).
  </action>
  <verify>
    <automated>cd app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && npm run typecheck` (tsc --noEmit) clean
    - reshapePose3dData 가 frame별 hip-center recenter + torso normalize 를 수행 (소스: hip/shoulder midpoint 계산 + 좌표 차감/나눗셈 존재)
    - 0-div 가드: torso length 0/epsilon 미만 분기 존재 (소스 확인)
    - occlusion(hip/shoulder 누락) graceful skip — throw 없이 null/기존 frame 정책 유지
    - backend/app.py joints3d 저장부 + 3중 계약(analysis.ts space enum) 불변 (이 task files_modified 에 미포함)
  </acceptance_criteria>
  <done>reshapePose3dData 가 origin-centered normalized 좌표를 반환하고 0-div/occlusion graceful. tsc clean. backend 불변.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 실기기 3D 골격 렌더 육안 검증 (TRUST-04)</name>
  <action>Task 1 의 reshapePose3dData 정규화 결과를 실기기/시뮬레이터 result 화면에서 육안 확인한다. 앱 JS 테스트 러너 부재로 자동화 불가 — 디바이스 렌더가 유일한 동작 게이트.</action>
  <what-built>reshapePose3dData 가 joints3d 를 골반중심 recenter + 몸통길이 정규화하여 viewer frustum 안으로 보낸다. backend·계약 불변이라 과거 분석 doc 도 즉시 정규화된다. (앱 JS 테스트 러너 부재 — CLAUDE.md "no JS test runner" — 이라 디바이스 육안 검증이 유일한 동작 게이트.)</what-built>
  <how-to-verify>
    1. 실기기 또는 시뮬레이터에서 앱 실행 (EAS preview/development build 또는 expo run).
    2. 기존 분석 결과 1개(과거 doc)와 신규 분석 1개의 result 화면 진입.
    3. 3D 골격 뷰어에서 골격이 화면 회색 영역이 아니라 카메라 안에 실제로 표시되는지 확인.
    4. OrbitControls 제스처로 회전/줌 시 골격이 frustum 안에 유지되는지 확인.
    5. 과거 분석 기록(이전 빌드에서 만든 raw 픽셀좌표 doc)도 정상 렌더되는지 확인.
    참고: belle TestFlight 실기기 검증은 다음 native build 시점(STATE.md Wave 2 override 박제)에 함께 가능.
  </how-to-verify>
  <resume-signal>"approved" 또는 발견한 문제(blank/잘림/회전 시 frustum 이탈 등) 기술</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

본 plan 은 앱 클라이언트 좌표 변환 순수함수만 변경 — 신규 endpoint/auth/PII/네트워크 표면 없음. Firestore 읽기 데이터(joints3d flat)는 기존 경로, 저장부 불변.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05 | DoS (렌더) | torso=0 또는 NaN/Inf 좌표 → 나눗셈 crash / GL 컨텍스트 죽음 | mitigate | reshape 단계 0-div 가드(epsilon) + finite 처리, PoseViewer3D ErrorBoundary 안전망 |
| T-19-06 | DoS | hip/shoulder occlusion 누락 → 정규화 실패 | mitigate | graceful skip — 기존 null 정책 박제, throw 금지 |
| T-19-SC | Tampering | npm installs | accept | 신규 패키지 설치 0 (기존 react-three-fiber/drei 재사용) — 19-RESEARCH "신규 외부 패키지 없음" |

앱 좌표 변환만 — 위협 표면 낮음 (19-RESEARCH Security Domain V5 DoS 가드).
</threat_model>

<verification>
- `cd app && npm run typecheck` clean
- 실기기/시뮬레이터 육안 — 신규+과거 doc 둘 다 3D 골격 frustum 내 렌더 (checkpoint)
- backend joints3d 저장부 + 3중 계약 불변 확인
</verification>

<success_criteria>
- reshapePose3dData 가 hip-center recenter + torso normalize 수행, 0-div/occlusion graceful
- tsc clean
- 실기기 골격 렌더 육안 PASS (과거 doc 포함)
- backend·계약 불변
</success_criteria>

<output>
Create `.planning/phases/19-vision-hybrid/19-03-SUMMARY.md` when done
</output>
