---
phase: 19-vision-hybrid
plan: 03
type: execute
wave: 1
depends_on: [19-01]
files_modified:
  - app/src/lib/joints.ts
  - app/src/lib/normalizePose3d.ts
  - app/scripts/_smoke_joints_normalize.mjs
autonomous: false
requirements: [TRUST-04]
must_haves:
  truths:
    - "3D 골격 joints3d 가 골반중심 recenter + 몸통길이 정규화되어 viewer frustum 안에 들어온다"
    - "hip/shoulder 인덱스를 jointKeys 인자(COCO-17 KEYPOINT_NAMES)에서 indexOf 로 도출한다 (8 angle JOINT_KEYS 아님)"
    - "정규화 출력 frame 수 == 입력 frame 수 (per-frame null/drop 금지) — last-resort 는 prev valid 또는 finite zero-skeleton, 안전 정규화 불가 시 whole-sequence null"
    - "정규화 순수 수학이 normalizePose3d 모듈 단일 source — joints.ts 와 smoke 가 동일 함수 import"
    - "synthetic raw COCO-17 frame(중심~(320,240)) 정규화 후 maxAbsCoord <= 3"
  artifacts:
    - path: "app/src/lib/normalizePose3d.ts"
      provides: "정규화 순수 수학 단일 source (frame-level recenter/scale + last-resort 정책). joints.ts + smoke 공용"
      contains: "normalizeFrames"
    - path: "app/src/lib/joints.ts"
      provides: "reshapePose3dData 가 normalizePose3d 의 normalizeFrames 호출 (frame 수 보존, whole null 정책)"
      contains: "normalizeFrames"
    - path: "app/scripts/_smoke_joints_normalize.mjs"
      provides: "PRODUCTION normalizeFrames 를 import 해 synthetic raw COCO-17 정규화 → maxAbsCoord<=3 + frame 수 보존 자동검증"
      contains: "normalizeFrames"
  key_links:
    - from: "app/src/lib/joints.ts::reshapePose3dData"
      to: "app/src/lib/normalizePose3d.ts::normalizeFrames"
      via: "단일 정규화 수학 source 호출 — origin-centered normalized 좌표"
      pattern: "normalizeFrames"
    - from: "app/scripts/_smoke_joints_normalize.mjs"
      to: "app/src/lib/normalizePose3d.ts::normalizeFrames"
      via: "PRODUCTION 함수 import (별도 copy 금지) → smoke 가 실제 production 검증"
      pattern: "normalizeFrames"
---

<objective>
3D 골격이 실기기에서 렌더되지 않는 버그를 고친다 (TRUST-04). 근본원인 = joints3d 가 RAW RTMW 픽셀좌표(x,y ~0-640 uncentered, z MotionBert-scale, pole_aligned 회전만)인데 viewer 는 origin-centered normalized 좌표(distance 3, fov 50, sphere r=0.04)를 기대 → 골격 중심 ~(320,240) 수백 단위 spread 가 frustum 밖 → GL 이 빈 회색으로 clear. 정규화를 읽는 쪽(joints.ts reshapePose3dData)에 두어 과거 doc 도 즉시 고치고 backend·3중 계약 불변.

**ITER-1 리뷰 반영:**
- HIGH-3: hip/shoulder 인덱스를 `jointKeys` 인자(joints3dKeys = COCO-17 `skeleton.KEYPOINT_NAMES`)에서 `indexOf('left_hip')` 등으로 도출 — 8 angle JOINT_KEYS 아님. 모든 좌표 finite 검증 후 렌더.

**ITER-2 리뷰 반영 (단일 source + frame 수 보존):**
- BLOCKER-2: smoke 가 알고리즘을 복제하면 production(joints.ts)이 틀려도 통과 → false coverage. **정규화 순수 수학을 JS-import 가능한 `app/src/lib/normalizePose3d.ts` 단일 모듈로 추출**하고 joints.ts 와 smoke 가 **둘 다 그 함수를 import** (single source of truth). smoke 가 production 함수를 실제 호출하므로 "smoke 가 reshapePose3dData 정규화를 검증한다" 주장이 정직해진다.
- HIGH-4: `reshapePose3dData` 반환은 `number[][][] | null` 이고 PoseViewer3D 의 모든 frame 은 `number[][]`(timeline currentFrame/ipsfViolationFrames 인덱싱). **per-frame null/drop 금지** — frame 수를 STABLE 하게 유지. last-resort frame 은 (1) 직전 valid 정규화 frame 재사용 또는 (2) finite all-zero skeleton frame(J×[0,0,0]). 시퀀스 전체가 안전 정규화 불가면 **whole-sequence null** 반환. 출력 frame 수 == 입력 frame 수(또는 whole null) 를 acceptance 로 단언.

Purpose: 파일럿 데모에서 사용자가 본인/정은지 3D 골격을 실제로 본다. 과거 분석 기록도 즉시 렌더. timeline/violation 인덱싱 desync 없음.
Output: normalizePose3d.ts(순수 수학 single source) + joints.ts(호출) + _smoke_joints_normalize.mjs(production import 검증). backend/계약 불변.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-vision-hybrid/19-REVIEWS.md
@.planning/phases/19-vision-hybrid/19-PATTERNS.md
@app/src/lib/joints.ts
@app/src/components/PoseViewer3D.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: normalizePose3d single-source + reshapePose3dData 호출 + frame-수-보존 + production-import smoke (TRUST-04)</name>
  <files>app/src/lib/normalizePose3d.ts, app/src/lib/joints.ts, app/scripts/_smoke_joints_normalize.mjs</files>
  <read_first>
    - app/src/lib/joints.ts (reshapePose3dData 24-57 = flat→(T,J,3) reshape; graceful null 정책 12-13/29-41, Firestore flat 주석 4-11, jointKeys 인자 = COCO-17 키 이름 list. 반환 `number[][][] | null` line 28)
    - app/src/app/analysis/result.tsx (634-639 — reshapePose3dData(result.joints3d, result.joints3dKeys ?? [], result.joints3dFrames) 호출 = jointKeys 가 joints3dKeys=COCO-17 확인)
    - app/src/components/PoseViewer3D.tsx (joints prop = number[][][]|null line 300; SkeletonMesh frame: number[][] line 125; camera position [0,0,3] / fov 50 / sphereGeometry [0.04] 345-356/157-168 — 정규화 기대값 근거; currentFrame/ipsfViolationFrames timeline 인덱싱 — frame 수 보존 필수; ErrorBoundary 24-29/76-98)
    - 19-REVIEWS.md (iter-2) BLOCKER-2 (29-33) — 단일 source 추출 또는 smoke 주장 downgrade / HIGH-4 (74-78) — frame 수 STABLE, per-frame null 금지, last-resort = prev valid / zero-skeleton / whole null
    - 19-REVIEWS.md (iter-1) HIGH-3 — jointKeys.indexOf('left_hip'/'right_hip'/'left_shoulder'/'right_shoulder') + finite 검증
    - backend/shared/python/sunity_shared/analysis/skeleton.py (KEYPOINT_NAMES — COCO-17 순서, left_hip/right_hip/left_shoulder/right_shoulder 정확한 문자열 확인)
    - 19-RESEARCH.md Pattern 4 (선택지 B 권장) + Pitfall 3 (저장 vs 읽기) + Security V5/DoS (nan_to_num + 0-div 가드)
    - 19-PATTERNS.md joints.ts 섹션 — graceful null + length guard + flat 제약 주석 컨벤션
    - app/scripts/seed-reference-motions.mjs (.mjs 스크립트 컨벤션 — node 실행 헤더/exit code 패턴, import 경로)
  </read_first>
  <action>
    **단일 source 모듈 신설 app/src/lib/normalizePose3d.ts (BLOCKER-2):** 정규화 순수 수학을 여기에 둔다 (TS, 의존성 없는 순수 함수 — JS import 가능하도록 react/expo import 0). export:
      `normalizeFrames(frames: number[][][], jointKeys: string[]): number[][][] | null`
    동작:
    - 진입 시 1회 `const li = jointKeys.indexOf('left_hip')`, `ri = indexOf('right_hip')`, `lsi = indexOf('left_shoulder')`, `rsi = indexOf('right_shoulder')` (정확한 문자열은 skeleton.KEYPOINT_NAMES 에서 확인). 8 angle JOINT_KEYS 참조 금지 (HIGH-3).
    - 각 frame `xyz[]` 마다 정규화 결과를 산출하되 **frame 수를 보존** (HIGH-4):
      (1) primary (hip/shoulder 모두 존재 + 좌표 finite): hip midpoint `(left_hip+right_hip)/2` 를 center 로 차감 → torso length = shoulder_mid↔hip_mid 유클리드 → 모든 좌표를 torso length 로 나눔 (origin-centered normalized).
      (2) fallback (hip/shoulder index -1 또는 좌표 non-finite, 또는 torso < epsilon): finite 관절 centroid center + max-axis range(또는 bbox 대각) scale 정규화. raw passthrough 금지.
      (3) last-resort (이 frame 자체가 정규화 불가 = finite 관절 0 또는 scale 산출 불가): **null/drop 금지** — 직전 valid 정규화 frame 이 있으면 그것을 복제, 없으면 finite all-zero skeleton frame(J × [0,0,0]) 을 emit. 모든 출력 frame 은 number[][] (NaN/Inf 0건).
    - 시퀀스 전체가 정규화 불가(예: 모든 frame 이 비유효 + epsilon scale)면 **whole-sequence null** 반환 (HIGH-4 — number[][][]|null 계약 준수).
    - 출력 길이 == 입력 길이 보장 (또는 null). 모든 좌표 Number.isFinite 단언 (산출 후 sanity). 2-space indent, single quote, `import type`, 이모지 금지. 정규화 이유 + 19-REVIEWS HIGH-3/HIGH-4 + Pattern 4 + "frame 수 보존 — timeline 인덱싱 desync 방지" 주석.
    **reshapePose3dData(joints.ts 24-57) 수정:** flat→(T,J,3) reshape 까지는 기존 로직 유지. reshape 직후 `const normalized = normalizeFrames(out, jointKeys as string[])` 호출 → 그 결과를 반환 (normalizeFrames 가 null 이면 null). 기존 graceful null 정책(형식/길이 불일치 시 null)은 유지. normalizePose3d 를 import (single source — joints.ts 안에 좌표 수학 복제 금지). 정규화는 flat→3D reshape 에 흡수 (저장부/3중 계약 불변, 과거 doc 호환). result.tsx / PoseViewer3D.tsx 변경 불필요.
    **smoke 스크립트 app/scripts/_smoke_joints_normalize.mjs (BLOCKER-2 — production import):**
    - **production `normalizeFrames` 를 실제 import** 해서 호출한다. TS 직접 import 가 node 에서 안 되면: (a) normalizePose3d.ts 를 순수 TS(no-deps)로 작성했으므로 smoke 실행 전 `npx tsc` 로 단일 파일 컴파일 후 산출 .js 를 import, 또는 (b) esbuild/ts-node 등 repo 에 이미 있는 도구로 on-the-fly transpile. **알고리즘 복제 금지** — smoke 가 production 함수를 호출해야 "정규화 검증" 주장이 정직 (BLOCKER-2). 컴파일 단계는 스크립트 내부 또는 verify 명령에서 수행.
    - synthetic raw COCO-17 frame 구성: 17 keypoint 를 중심 ~(320,240) 픽셀 스케일 배치 (실제 RTMW 미정규화 모사). left_hip/right_hip/left_shoulder/right_shoulder 위치 명시. 다중 frame(예 3)으로 구성.
    - `normalizeFrames(frames, keys)` 호출 → (1) 출력이 null 아님, (2) **출력 frame 수 == 입력 frame 수** 단언, (3) maxAbsCoord <= 3 단언 → 위반 시 `process.exit(1)`.
    - fallback 케이스(hip/shoulder 누락) 1개: 정규화 후에도 frame 수 보존 + maxAbsCoord<=3.
    - last-resort 케이스(특정 frame 만 비유효) 1개: 출력 frame 수 == 입력 frame 수(drop 0), 비유효 frame 이 prev-valid 복제 또는 zero-skeleton 으로 채워짐 + 모든 좌표 finite.
    - whole-null 케이스(모든 frame 비유효) 1개: normalizeFrames 가 null 반환.
    - PASS 시 exit 0 (진단 출력 허용, 슬롭 금지). 이모지 금지. 2-space indent.
  </action>
  <verify>
    <automated>cd app && npm run typecheck && node scripts/_smoke_joints_normalize.mjs && echo "SMOKE_PASS maxAbsCoord<=3 + frame_count_stable"</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && npm run typecheck` (tsc --noEmit) clean
    - normalizePose3d.ts 가 react/expo import 0 의 순수 모듈 (grep: from 'react'/'expo' 0건), normalizeFrames export
    - joints.ts reshapePose3dData 가 normalizeFrames 를 import & 호출 (grep: normalizeFrames 참조), joints.ts 안에 좌표 정규화 수학 복제 0 (single source)
    - hip/shoulder 인덱스가 normalizePose3d 에서 `jointKeys.indexOf('left_hip'/'right_hip'/'left_shoulder'/'right_shoulder')` 로 도출 (grep: indexOf 4건, 8 angle JOINT_KEYS 참조 0)
    - 모든 좌표 finite 검증 분기 존재 (Number.isFinite 또는 동등) — per-frame null/drop 0건 (grep: frames.push(null) / frame 제외 패턴 0)
    - **smoke 가 production normalizeFrames 를 import/호출** — 알고리즘 복제 아님 (grep: smoke 가 normalizePose3d 에서 import). smoke 가 (a) frame 수 보존, (b) maxAbsCoord<=3, (c) whole-null 케이스 null 을 단언
    - `node scripts/_smoke_joints_normalize.mjs` exit 0 — 출력 frame 수 == 입력 frame 수(또는 whole-null 케이스 null), maxAbsCoord<=3
    - backend/app.py joints3d 저장부 + 3중 계약(analysis.ts space enum) 불변 (이 task files_modified 에 미포함)
  </acceptance_criteria>
  <done>normalizePose3d single-source + reshapePose3dData 호출 + frame-수-보존(per-frame null 0, last-resort=prev/zero, 불가 시 whole null) + smoke 가 production import 검증. tsc clean. backend 불변.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 실기기 3D 골격 렌더 육안 검증 (TRUST-04)</name>
  <action>Task 1 의 reshapePose3dData → normalizeFrames 정규화 결과를 실기기/시뮬레이터 result 화면에서 육안 확인한다. smoke 스크립트가 production 함수로 수치 정규화(maxAbsCoord<=3) + frame 수 보존을 자동 검증하지만, 실제 GL frustum 내 렌더는 디바이스 육안이 유일한 동작 게이트.</action>
  <what-built>reshapePose3dData 가 joints3d 를 normalizePose3d.normalizeFrames(COCO-17 hip 중심 recenter + torso/bbox 정규화, frame 수 보존)로 viewer frustum 안으로 보낸다. backend·계약 불변이라 과거 분석 doc(raw 픽셀좌표)도 즉시 정규화된다. smoke 가 production 함수로 maxAbsCoord<=3 + frame 수 보존을 자동 검증했고, 디바이스 육안이 GL 렌더 최종 게이트.</what-built>
  <how-to-verify>
    1. 실기기 또는 시뮬레이터에서 앱 실행 (EAS preview/development build 또는 expo run).
    2. 기존 분석 결과 1개(과거 raw 픽셀좌표 doc)와 신규 분석 1개의 result 화면 진입.
    3. 3D 골격 뷰어에서 골격이 화면 회색 영역이 아니라 카메라 안에 실제로 표시되는지 확인.
    4. OrbitControls 제스처로 회전/줌 시 골격이 frustum 안에 유지되는지 확인.
    5. occlusion 있던 과거 분석(hip/shoulder 일부 누락)도 fallback 정규화로 렌더되는지 + timeline scrub 시 frame 누락/점프 없는지 확인.
    참고: belle TestFlight 실기기 검증은 다음 native build 시점(STATE.md Wave 2 override 박제)에 함께 가능.
  </how-to-verify>
  <resume-signal>"approved" 또는 발견한 문제(blank/잘림/회전 시 frustum 이탈/timeline 점프 등) 기술</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

본 plan 은 앱 클라이언트 좌표 변환 순수함수(normalizePose3d + joints) + standalone smoke 스크립트만 변경 — 신규 endpoint/auth/PII/네트워크 표면 없음. Firestore 읽기 데이터(joints3d flat)는 기존 경로, 저장부 불변.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05 | DoS (렌더) | torso=0 또는 NaN/Inf 좌표 → 나눗셈 crash / GL 컨텍스트 죽음 | mitigate | finite 검증 + torso<epsilon → centroid+bbox fallback, last-resort prev/zero-skeleton, PoseViewer3D ErrorBoundary 안전망 |
| T-19-06 | DoS | hip/shoulder occlusion 누락 → 정규화 실패 / raw passthrough → frustum 밖 blank | mitigate | indexOf -1 → centroid+bbox fallback (raw passthrough 금지), smoke maxAbsCoord<=3 검증 |
| T-19-11 | Tampering | 잘못된 키셋(8 angle JOINT_KEYS) 인덱스 → 엉뚱한 점 중심 정규화 | mitigate | jointKeys 인자(COCO-17)에서 indexOf, acceptance grep (HIGH-3) |
| T-19-14 | Tampering | per-frame null/drop → timeline currentFrame/ipsfViolationFrames 인덱싱 desync | mitigate | frame 수 STABLE (prev/zero last-resort, whole null), smoke frame-count 단언 (ITER-2 HIGH-4) |
| T-19-15 | Tampering | smoke 가 알고리즘 복제 → production 틀려도 통과 (false coverage) | mitigate | normalizePose3d single source + smoke 가 production import/호출 (ITER-2 BLOCKER-2) |
| T-19-SC | Tampering | npm installs | accept | 신규 패키지 설치 0 (기존 react-three-fiber/drei + node stdlib + 기존 tsc/esbuild 재사용) |

앱 좌표 변환만 — 위협 표면 낮음.
</threat_model>

<verification>
- `cd app && npm run typecheck` clean
- `node scripts/_smoke_joints_normalize.mjs` exit 0 (production normalizeFrames import, maxAbsCoord<=3, frame 수 보존, whole-null 케이스 포함)
- 실기기/시뮬레이터 육안 — 신규+과거 doc 둘 다 3D 골격 frustum 내 렌더 + timeline scrub desync 0 (checkpoint)
- backend joints3d 저장부 + 3중 계약 불변 확인
</verification>

<success_criteria>
- normalizePose3d single-source + reshapePose3dData 호출, COCO-17 indexOf hip-center recenter + torso/bbox normalize, frame 수 보존(per-frame null 0)
- smoke 가 production 함수 import 로 maxAbsCoord<=3 + frame 수 보존 검증 + tsc clean
- 실기기 골격 렌더 육안 PASS (과거 doc 포함, timeline desync 0)
- backend·계약 불변
</success_criteria>

<output>
Create `.planning/phases/19-vision-hybrid/19-03-SUMMARY.md` when done
</output>
