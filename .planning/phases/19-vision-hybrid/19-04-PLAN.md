---
phase: 19-vision-hybrid
plan: 04
type: execute
wave: 2
depends_on: [19-01, 19-02]
files_modified:
  - backend/functions/pipeline/app.py
  - backend/shared/python/sunity_shared/analysis/assemble.py
  - app/src/types/analysis.ts
  - backend/shared/python/sunity_shared/models.py
  - docs/contract.md
  - app/src/app/analysis/result.tsx
  - app/src/components/DimensionDetailModal.tsx
autonomous: true
requirements: [TRUST-01, TRUST-03, TRUST-05]
must_haves:
  truths:
    - "표시 각도값(현재/기준)이 점수 산출에 쓰는 DTW path-정렬 median 과 동일 source 다 (비대칭 제거)"
    - "MODE_SELF 미보유 동작은 is_reference_free_motion 으로 판정되어 '기준 동작 없음 — 절대 자세 기준 평가' scoringBasis + 절대트랙 채점을 받는다"
    - "scoringBasis/scoringBasisLabel 이 result 화면 Mode3 헤더 + DimensionDetailModal 에 표시된다 (백엔드 only 아님)"
    - "v1 vision hook 은 pass-through (점수 불변)이고 v2 거부권 슬롯을 막지 않는다"
  artifacts:
    - path: "backend/functions/pipeline/app.py"
      provides: "_angles_to_dtw_median_dicts 정합 + branch_info early lookup + MODE_SELF 게이트 + vision hook"
      contains: "_angles_to_dtw_median_dicts"
    - path: "backend/shared/python/sunity_shared/analysis/assemble.py"
      provides: "is_reference_free_motion + build_mode3 scoringBasis 카피"
      contains: "is_reference_free_motion"
    - path: "app/src/app/analysis/result.tsx"
      provides: "Mode3 scoringBasisLabel 헤더 렌더"
      contains: "scoringBasis"
    - path: "app/src/components/DimensionDetailModal.tsx"
      provides: "reference-free 시 하드코딩 '세계 심사 기준' 대신 basis 사용"
      contains: "scoringBasis"
  key_links:
    - from: "pipeline._process MODE_SELF"
      to: "assemble.is_reference_free_motion"
      via: "angleSource=='unavailable' + fixtureKey/ipsfCode None + officialName 없음 → reference_free (copyBranch 단독 분기 금지)"
      pattern: "is_reference_free_motion"
    - from: "assemble.build_mode3 scoringBasis"
      to: "app/src/app/analysis/result.tsx"
      via: "scoringBasisLabel 헤더 표시 (TRUST-03 가시화)"
      pattern: "scoringBasis"
    - from: "pipeline._process 점수 산출 직후"
      to: "_apply_vision_veto"
      via: "v1 pass-through hook (adapter Protocol 자리)"
      pattern: "_apply_vision_veto"
---

<objective>
파이프라인 표시·게이트·hook + 가시화를 마무리한다 — Wave 1 감점식 집계 위에서:
- **TRUST-01 (HIGH-2):** `_angles_to_mean_dict`(whole-clip nanmean, user matched-window vs ref full-clip 비대칭)를 신규 `_angles_to_dtw_median_dicts(path, user_seg, ref_angles, joint_keys)` 로 교체 — path-aligned (user_frame, ref_frame) 쌍 per-joint finite median (현재/기준 양측). per_joint_deviation 은 abs-diff median 만 반환하므로 별도 helper 필요. Mode3-first 는 extension_deviation 과 동일 hold-window source 사용 (whole-clip mean 아님).
- **TRUST-03 (BLOCKER-1 + BLOCKER-2 + MEDIUM-2):** branch_info lookup 을 recognize 직후로 이동(MEDIUM-2), `is_reference_free_motion(info)` helper 로 미보유 판정(BLOCKER-1 — copyBranch 단독 불가: _SAFE_DEFAULT_BRANCH.copyBranch == 실 branch2 동일값). build_mode3 가 scoringBasis/scoringBasisLabel emit → result.tsx + DimensionDetailModal 가 화면 표시(BLOCKER-2 — 백엔드 only 금지).
- **TRUST-05:** 점수 산출 직후 `_apply_vision_veto` pass-through (v1 identity, OFF 시 입력 그대로). v2 거부권 본체 DEFERRED.

Purpose: 시각적으로 동일한데 19° 차이나는 표시 모순 제거, 미보유 동작 거짓 confident 점수 차단 + 근거를 **사용자가 화면에서 보게**, v2 비전 거부권 슬롯 확보.
Output: pipeline/app.py(median helper + early branch_info + 게이트 + hook), assemble.py(is_reference_free_motion + scoringBasis 카피), 3중 계약 + result.tsx + DimensionDetailModal.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-vision-hybrid/19-REVIEWS.md
@.planning/phases/19-vision-hybrid/19-PATTERNS.md
@.planning/phases/19-vision-hybrid/19-IPSF-DEDUCTION-NOTES.md
@.planning/phases/19-vision-hybrid/19-02-SUMMARY.md
@backend/functions/pipeline/app.py
@backend/shared/python/sunity_shared/analysis/assemble.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 표시-점수 정합 — _angles_to_dtw_median_dicts (TRUST-01 / HIGH-2)</name>
  <files>backend/functions/pipeline/app.py</files>
  <behavior>
    - test_display_matches_score_source: user/ref 표시 각도가 동일 DTW path 정렬 구간의 per-joint finite median (whole-clip nanmean 비대칭 제거)
  </behavior>
  <read_first>
    - backend/functions/pipeline/app.py (_angles_to_mean_dict 1515-1538 교체, MODE_EXPERT 비대칭 1800-1801 = user_seg mean vs a_ref full-clip mean, _deviation_against 1557-1569 = match.path/user_seg/a_ref 반환, _mode3_comparison ref_mean 1609 + mode3-first user_mean 1588 + extension_deviation 1598)
    - 19-REVIEWS.md HIGH-2 (73-84) — per_joint_deviation 은 abs-diff median 만 반환, current/target angle median 미반환 → 전용 helper. Mode3-first 는 extension_deviation 과 동일 hold-window source
    - backend/shared/python/sunity_shared/analysis/motiondtw.py (per_joint_deviation 103-130 = path 따라 abs-diff median — 알고리즘 모방, 단 abs-diff 가 아닌 양측 angle median)
    - backend/shared/python/sunity_shared/analysis/dimensions.py (extension_deviation + _select_window — mode3-first hold-window source)
    - 19-RESEARCH.md Pattern 2 + 19-PATTERNS.md pipeline 섹션 TRUST-01
    - backend/tests/test_pipeline_mode3.py (test_display_matches_score_source — 통과 대상)
  </read_first>
  <action>
    신규 helper 추가: `_angles_to_dtw_median_dicts(path, user_seg, ref_angles, joint_keys) -> tuple[dict[str,float], dict[str,float]]`. per_joint_deviation(motiondtw 103-130) 의 path 순회 패턴을 모방하되 **abs-diff 가 아니라 양측 각도값을 수집**: path 의 각 (u, r) 쌍마다 `user_seg[u]` / `ref_angles[r]` 를 관절별로 모아 finite median 2개 dict 반환 (user_median_by_joint, ref_median_by_joint). NaN 무시(np.nanmedian 또는 finite 필터). 점수 경로(per_joint_deviation)와 **동일 path/구간** 사용 — 표시·점수 source 통일.
    MODE_EXPERT(1800-1801): `user_mean_mode1 = _angles_to_mean_dict(user_seg)` / `ref_mean_mode1 = _angles_to_mean_dict(a_ref)` 를 `_deviation_against` 가 이미 반환하는 `match`(=path) / `user_seg` / `a_ref` 로 `_angles_to_dtw_median_dicts(match.path, user_seg, a_ref, skeleton.JOINT_KEYS)` 호출로 교체. whole-clip nanmean 의 jitter/occlusion 민감성 + user matched-window vs ref full-clip 시간 비대칭 둘 다 제거.
    _mode3_comparison(1572-1630): progress 경로 ref_mean(1609) 를 동일 helper(_match.path/_user_seg/prev_seg 사용 — _deviation_against 반환 unpack 1605 에서 _match/_user_seg 를 실제 사용하도록 변경)로 교체. **mode3-first(1588-1603) user_mean** 은 whole-clip `_angles_to_mean_dict(angles)` 대신 extension_deviation(1598)이 쓰는 동일 hold-window(_select_window) source 의 median 으로 산출 (HIGH-2 — extension_deviation 과 정합). 별도 window 계산 금지 (drift 방지).
    `_angles_to_mean_dict`(1515-1538)는 호출처가 없어지면 제거 또는 helper 로 위임 — 표시·점수 동일 source 보장. 순수성 유지(numpy 만, AWS 무관). from __future__ import annotations, snake_case, 이모지 금지. graceful boundary(2312-2316 패턴) 유지.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_pipeline_mode3.py::test_display_matches_score_source -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_display_matches_score_source GREEN
    - `_angles_to_dtw_median_dicts` 가 path-aligned 양측 angle median 2 dict 반환 (소스: path 순회 + user/ref 각각 median)
    - 표시 각도가 점수 source(DTW path-정렬 median)와 동일 — grep: MODE_EXPERT 표시값 산출에 whole-clip `_angles_to_mean_dict`/`np.nanmean` 직접 호출 0건
    - mode3-first user 표시값이 extension_deviation 과 동일 hold-window source (소스: _select_window 공유)
    - 무관한 기존 pipeline 테스트 회귀 0 (pre-existing 격리 제외)
  </acceptance_criteria>
  <done>표시 각도 = 점수 산출 DTW path-정렬 median (양측). mode3-first hold-window 정합. 비대칭 제거. 회귀 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: branch_info early lookup + is_reference_free_motion + Mode3 게이트 + scoringBasis 카피 + vision hook (TRUST-03 / TRUST-05)</name>
  <files>backend/functions/pipeline/app.py, backend/shared/python/sunity_shared/analysis/assemble.py</files>
  <behavior>
    - test_unknown_move_gate (parametrize): branch1 → reference_motion / 실 branch2 → reference 경로 / reference-free → scoringBasis="reference_free_absolute" + label + 절대트랙. 실 branch2 와 reference-free 가 copyBranch 동일값인데 scoringBasis 다름. raise 없음
    - test_vision_hook_passthrough: _apply_vision_veto OFF 시 score_result 그대로 반환
  </behavior>
  <read_first>
    - backend/functions/pipeline/app.py (recognize 1733, abs_dims 1734, MODE_EXPERT 1740-1847, MODE_SELF 1839-1847, _mode3_comparison 1572-1630, NOT_POLE 1812-1820 = MODE_EXPERT 전용, branch_info 현재 lookup 1945-1947 = recognize/scoring 보다 늦음 → MEDIUM-2 로 이동, coach_context 1948-1959, build_result 호출부 ~2045 = branch_info forward, get_previous_analysis mode=MODE_SELF 1842 Pitfall 5)
    - backend/shared/python/sunity_shared/analysis/assemble.py (MotionBranchInfo 39-53 = copyBranch/ipsfCode/officialName/angleSource/angleFixtureKey 필드, _SAFE_DEFAULT_BRANCH 68-77 copyBranch="branch2_eunji_reference"=실 branch2 동일값/angleSource="unavailable"/angleFixtureKey=None/ipsfCode=None/officialName="", lookup_motion_branch 91-113, build_mode3 539-562, _DIMENSION_BASELINES_* 119-187, _baselines_for_branch 175-187)
    - 19-REVIEWS.md BLOCKER-1 (15-34) is_reference_free_motion 정확 정의 / BLOCKER-2 (36-48) scoringBasis 3-way enum + label / MEDIUM-2 (132-136) lookup 이동 / 비전 hook 5번 (156)
    - 19-RESEARCH.md Pattern 3 (3분기 + reference-free 절대트랙 + fail-closed 금지) + "v2 비전 hook 자리" + Pitfall 5
    - 19-IPSF-DEDUCTION-NOTES.md §B (reference-free 절대 트랙 — line/stability/posture)
    - 19-PATTERNS.md pipeline TRUST-03/TRUST-05 + assemble 섹션
    - backend/tests/test_pipeline_mode3.py (test_unknown_move_gate, test_vision_hook_passthrough — 통과 대상)
  </read_first>
  <action>
    **MEDIUM-2 — branch_info early lookup:** 현재 1945-1947 의 `branch_info = assemble.lookup_motion_branch(getattr(profile,"motion_id",None))` 를 `profile = recognizer.recognize(...)`(1733) **직후로 이동** → MODE_EXPERT/MODE_SELF 분기 전에 사용 가능. coach_context(1948-1959) / build_result(~2045) 는 이미 동일 branch_info 참조하므로 중복 lookup 제거 (single source). _mode3_comparison 시그니처에 branch_info 전달.
    **BLOCKER-1 — is_reference_free_motion:** assemble.py 에 helper 추가:
      `is_reference_free_motion(info: MotionBranchInfo | None) -> bool` = `info is None or (info.angleSource == "unavailable" and info.angleFixtureKey is None and info.ipsfCode is None and not info.officialName)`. copyBranch 로 판정 금지 (_SAFE_DEFAULT_BRANCH.copyBranch == 실 branch2 동일값 — 구분 불가). docstring 에 왜 copyBranch 단독 불가인지 명시.
    **TRUST-03 — Mode3 게이트:** MODE_SELF 분기(1839-1847) + _mode3_comparison 에서 branch_info 로 분기:
      - `is_reference_free_motion(branch_info)` True → confident 금지: scoringBasis="reference_free_absolute" + reference-free 절대트랙(line/stability/posture, 19-IPSF §B)으로 채점. **fail-closed/raise 금지** ([[motion-routing-generalize-principle]]).
      - 등재(branch1) → scoringBasis="reference_motion"(또는 mode3 progress = "previous_analysis"). 정은지 reference 보유(실 branch2, reference-free 아님) → 동일.
      not_pole 게이트(1812)는 MODE_EXPERT 전용 유지(이동 금지). get_previous_analysis(mode=MODE_SELF)(1842) Pitfall 5 가드 불변.
    **BLOCKER-2 backend — build_mode3 scoringBasis:** assemble.build_mode3(539-562) 시그니처에 `scoring_basis: str | None = None` 추가 → out dict 에 `scoringBasis`(enum: "reference_free_absolute"|"previous_analysis"|"reference_motion") + `scoringBasisLabel`(예 "기준 동작 없음 — 절대 자세 기준 평가") emit. 라벨 카피는 _DIMENSION_BASELINES_* 형식으로 상수화. BRANCH2_FORBIDDEN_PHRASES 패턴으로 "89% 일치"/"정은지와 거의 같음" 거짓 프레이밍 가드 ([[mode3-progress-not-similarity]]). 옵셔널 키(이전 빌드 doc 호환). _process 의 build_mode3 호출부(_mode3_comparison 1603/1624)에 scoring_basis 전달.
    **TRUST-05 vision hook:** 감점식 점수 산출 직후 `_apply_vision_veto(score_result, local_video_path, angles)` pass-through 자리 — v1 = identity. `_ensure_adapters()` lazy-import 패턴 모방, `_gemini_vision_enabled()` OFF 시 입력 반환. adapter Protocol 경계 — v2 본체 DEFERRED. graceful boundary(except Exception #noqa BLE001 + log.exception + skip) 박제. v1 schema 변경 없음.
    from __future__ import annotations, snake_case, 한국어 docstring(why), 이모지 금지.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_pipeline_mode3.py::test_unknown_move_gate tests/test_pipeline_mode3.py::test_vision_hook_passthrough -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_unknown_move_gate / test_vision_hook_passthrough GREEN
    - is_reference_free_motion 이 angleSource/angleFixtureKey/ipsfCode/officialName 으로 판정 (grep: copyBranch 로 reference-free 판정 0건 — BLOCKER-1)
    - branch_info lookup 이 recognize(1733) 직후 1회, MODE_SELF/coach_context/build_result 가 동일 객체 참조 (중복 lookup 0 — MEDIUM-2)
    - build_mode3 가 scoringBasis + scoringBasisLabel emit, 미보유 시 "기준 동작 없음 — 절대 자세 기준 평가"
    - 미보유 동작이 raise 하지 않고 점수+scoringBasis 반환 (fail-closed 금지 — _SAFE_DEFAULT_BRANCH 경로에 raise 없음)
    - build_mode3 근거 카피에 "% 일치"/"89%"/"정은지와 거의" forbidden 가드 존재
    - _apply_vision_veto OFF 시 입력 score_result identity 반환
    - not_pole(1812) MODE_EXPERT 전용 유지, get_previous_analysis(mode=MODE_SELF) 가드 불변
    - 무관한 backend suite 회귀 0 (pre-existing 격리 제외)
  </acceptance_criteria>
  <done>branch_info early single-lookup + is_reference_free_motion + scoringBasis emit + 게이트 + vision identity. fail-closed 금지. 거짓 프레이밍 가드. 회귀 0.</done>
</task>

<task type="auto">
  <name>Task 3: scoringBasis 3중 계약 + result.tsx 헤더 + DimensionDetailModal 가시화 (TRUST-03 / BLOCKER-2)</name>
  <files>app/src/types/analysis.ts, backend/shared/python/sunity_shared/models.py, docs/contract.md, app/src/app/analysis/result.tsx, app/src/components/DimensionDetailModal.tsx</files>
  <read_first>
    - 19-REVIEWS.md BLOCKER-2 (36-48) — scoringBasis 3-way enum + scoringBasisLabel + result.tsx 헤더 렌더 + DimensionDetailModal 하드코딩 "세계 심사 기준" 교체
    - app/src/types/analysis.ts (Mode3Comparison 249-256 = mode/isFirst/previousAnalysisId/deltaFromPrevious — scoringBasis 추가 위치, AnalysisMode 8)
    - backend/shared/python/sunity_shared/models.py (mode3 comparison schema / 키 상수)
    - docs/contract.md (mode3 comparison 정의부 — build_mode3 출력 계약)
    - app/src/app/analysis/result.tsx (헤더 sub 672-678 = mode3 sub 텍스트 677 아래가 basis 렌더 위치, cmp.mode mode3 분기 491-496, summary)
    - app/src/components/DimensionDetailModal.tsx (baselineFor 86-102 + 80-83 하드코딩 "세계 심사 기준" — reference-free 시 basis 사용, Props 111-121)
    - 19-PATTERNS.md 3중 계약 섹션 + result.tsx 표시 컨벤션
  </read_first>
  <action>
    **3중 계약 — scoringBasis 추가 (Task 2 build_mode3 emit 과 lockstep):**
    - app/src/types/analysis.ts `Mode3Comparison`(249-256)에 `scoringBasis?: 'reference_free_absolute' | 'previous_analysis' | 'reference_motion';` + `scoringBasisLabel?: string;` 추가 (옵셔널 — 이전 빌드 doc 호환). 주석에 enum 의미 명시.
    - backend/shared/python/sunity_shared/models.py 의 mode3 comparison 키 상수/주석에 scoringBasis/scoringBasisLabel 반영.
    - docs/contract.md mode3 comparison 정의에 scoringBasis enum + label + "미보유 동작 = reference_free_absolute, 거짓 % 일치 프레이밍 금지" 명시.
    **result.tsx 헤더 렌더 (BLOCKER-2 가시화):** mode3 헤더 sub(677) 아래에 `cmp.scoringBasisLabel` 이 있으면 1줄 표시 (스타일은 기존 styles.sub/coachPositioning 토큰 재사용 — 색/스페이싱 하드코딩 금지, #FF4B33 직접 사용 금지, theme 토큰만). reference-free 일 때 "기준 동작 없음 — 절대 자세 기준 평가" 가 사용자에게 보인다. mode1 / scoringBasisLabel 없을 때는 미렌더 (graceful).
    **DimensionDetailModal 하드코딩 교체 (BLOCKER-2):** baselineFor(86-102) + formulaFor(80-83)의 하드코딩 "세계 심사 기준" 카피가 reference-free Mode3 에서는 거짓 — scoringBasis 가 "reference_free_absolute" 일 때 basis(scoringBasisLabel 또는 절대트랙 카피)로 대체. Props 에 scoringBasis/scoringBasisLabel optional 전달 (result.tsx 가 cmp 에서 넘김). reference_motion/previous_analysis 일 때는 기존 카피 유지.
    이모지 금지, 2-space indent, single quote, `import type`, theme 토큰만 (브랜드 컬러/스페이싱 하드코딩 금지 — app/CLAUDE.md).
  </action>
  <verify>
    <automated>cd app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && npm run typecheck` (tsc --noEmit) clean
    - scoringBasis/scoringBasisLabel 이 analysis.ts(Mode3Comparison) + models.py + contract.md + build_mode3(Task 2) 에 동시 반영 (3중 계약 lockstep)
    - result.tsx 가 mode3 + scoringBasisLabel 존재 시 헤더에 라벨 렌더 (grep: scoringBasisLabel 참조) — 미보유 동작 근거가 화면 표시 (백엔드 only 아님)
    - DimensionDetailModal 이 reference-free 시 하드코딩 "세계 심사 기준" 대신 basis 사용 (grep: scoringBasis 분기 존재)
    - theme 토큰만 사용 (grep: 신규 추가 라인에 `#` hex 색/매직 px 0건)
  </acceptance_criteria>
  <done>scoringBasis 3중 계약 + result.tsx 헤더 + DimensionDetailModal 가시화. TRUST-03 근거가 화면에 보임. tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

본 plan 은 pipeline 오케스트레이션(기존 SQS consumer 경로) + ML core 조립 순수함수 + 앱 표시 카피 + 3중 계약만 변경 — 신규 endpoint/auth/PII 표면 없음. v2 vision hook 은 pass-through 자리만(본체 DEFERRED, Gemini 호출 없음). Firestore flat 저장 불변(scoringBasis = 옵셔널 키 추가).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-07 | Information disclosure / 신뢰성 | 미보유 동작 거짓 confident 점수("89% 일치") | mitigate | is_reference_free_motion 판정(BLOCKER-1) + scoringBasis="reference_free_absolute" + BRANCH2_FORBIDDEN_PHRASES + 화면 가시화(BLOCKER-2) |
| T-19-08 | DoS | 미보유 동작 fail-closed raise → 분석 흐름 차단 | mitigate | _SAFE_DEFAULT_BRANCH 점수 주되 근거 명시(raise 금지) ([[motion-routing-generalize-principle]]) |
| T-19-09 | Tampering | vision hook 이 v1 에서 점수를 변경 (객관성 위반) | mitigate | _apply_vision_veto v1 = identity, test_vision_hook_passthrough 점수 불변 단언 |
| T-19-12 | Information disclosure | scoringBasis 가 reference-free 인데 모달이 하드코딩 "세계 심사 기준" 표시 (모순 카피) | mitigate | DimensionDetailModal basis 분기 (BLOCKER-2) |
| T-19-SC | Tampering | npm/pip installs | accept | 신규 패키지 설치 0 (numpy/기존 adapter lazy-import 재사용) |

내부 오케스트레이션/카피/표시 변경 — 위협 표면 낮음.
</threat_model>

<verification>
- `cd backend && python -m pytest tests/test_pipeline_mode3.py -x -q` GREEN (TRUST-01/03/05 신규 케이스)
- `cd backend && python -m pytest tests/ -q` 전체 suite green (pre-existing 격리 제외)
- `cd app && npm run typecheck` clean
- D-05 anchor(test_anchor_known_answer.py) 는 RUN_PHASE19_ANCHORS env-gate — Pod 재개 후 방향검증
- scoringBasis schema 키 추가 → EAS 재빌드 (옵셔널 키라 백엔드 sam 재배포는 키 emit 시 필요)
</verification>

<success_criteria>
- 표시 각도 = 점수 DTW median source (TRUST-01), Mode3 미보유 게이트 is_reference_free_motion + scoringBasis 화면 가시화 (TRUST-03), v2 hook pass-through (TRUST-05)
- branch_info early single-lookup, fail-closed 금지, 거짓 프레이밍 가드, 점수 객관성(vision identity) 유지
- 3중 계약 lockstep (scoringBasis) + result.tsx + DimensionDetailModal
- 전체 backend suite + tsc green
</success_criteria>

<output>
Create `.planning/phases/19-vision-hybrid/19-04-SUMMARY.md` when done — scoringBasis schema 키 추가로 EAS 재빌드 필요 명시
</output>
