---
phase: 19-vision-hybrid
plan: 04
type: execute
wave: 2
depends_on: [19-01, 19-02]
files_modified:
  - backend/functions/pipeline/app.py
  - backend/shared/python/sunity_shared/analysis/assemble.py
autonomous: true
requirements: [TRUST-01, TRUST-03, TRUST-05]
must_haves:
  truths:
    - "표시 각도값(현재/기준)이 점수 산출에 쓰는 DTW-정렬 median 과 동일 source 다 (비대칭 제거)"
    - "MODE_SELF 미보유 동작은 confident 점수 대신 '기준 동작 없음 — 절대 자세 기준 평가' 근거 + 절대트랙 채점을 받는다"
    - "v1 vision hook 은 pass-through (점수 불변, OFF 시 입력 그대로)이고 v2 거부권 슬롯을 막지 않는다"
  artifacts:
    - path: "backend/functions/pipeline/app.py"
      provides: "_angles_to_mean_dict DTW-정렬 정합 + MODE_SELF 게이트 + vision hook 자리"
      contains: "_angles_to_mean_dict"
    - path: "backend/shared/python/sunity_shared/analysis/assemble.py"
      provides: "build_mode3 미보유 근거 카피"
      contains: "lookup_motion_branch"
  key_links:
    - from: "pipeline._process MODE_SELF"
      to: "assemble.lookup_motion_branch"
      via: "_SAFE_DEFAULT_BRANCH 미보유 → 근거 플래그 + 절대트랙 (fail-closed 금지)"
      pattern: "lookup_motion_branch"
    - from: "pipeline._process 점수 산출 직후"
      to: "_apply_vision_veto"
      via: "v1 pass-through hook (adapter Protocol 자리)"
      pattern: "_apply_vision_veto"
---

<objective>
파이프라인 표시·게이트·hook 3건을 마무리한다 — Wave 1 감점식 집계 위에서: (1) TRUST-01 표시-점수 정합 — `_angles_to_mean_dict`(whole-clip nanmean, user matched-window vs ref full-clip 비대칭)를 DTW path-정렬 동일 구간 median 으로 교체 (점수 source 와 통일). (2) TRUST-03 Mode3 미보유 게이트 — MODE_SELF 에 lookup_motion_branch 연결, 미보유(_SAFE_DEFAULT_BRANCH)면 "기준 동작 없음 — 절대 자세 기준 평가" 근거 플래그 + 절대트랙 채점(fail-closed 금지, raise 금지). (3) TRUST-05 v2 hook 자리 (D-02 비전 거부권의 비-차단 부분만 — D-04 v1/v2 분할에 따라 본체는 v2 deferred) — 점수 산출 직후 `_apply_vision_veto` pass-through (v1 identity, OFF 시 입력 그대로).

Purpose: 시각적으로 동일한데 19° 차이나는 표시 모순 제거(belle 우려), 미보유 동작 거짓 confident 점수 차단, v2 비전 거부권을 막지 않는 schema/hook 확보.
Output: pipeline/app.py 표시값·MODE_SELF 게이트·hook + assemble.py 근거 카피.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-vision-hybrid/19-PATTERNS.md
@.planning/phases/19-vision-hybrid/19-IPSF-DEDUCTION-NOTES.md
@.planning/phases/19-vision-hybrid/19-02-SUMMARY.md
@backend/functions/pipeline/app.py
@backend/shared/python/sunity_shared/analysis/assemble.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 표시-점수 정합 (TRUST-01) — DTW-정렬 median 으로 통일</name>
  <files>backend/functions/pipeline/app.py</files>
  <behavior>
    - test_display_matches_score_source: user/ref 표시 각도가 동일 DTW path 정렬 구간의 median (whole-clip nanmean 비대칭 제거)
  </behavior>
  <read_first>
    - backend/functions/pipeline/app.py (_angles_to_mean_dict 1515-1538 교체, MODE_EXPERT 비대칭 1800-1801, _deviation_against 1557-1569 — match.path/user_seg/a_ref 반환, _mode3_comparison ref_mean 1609)
    - backend/shared/python/sunity_shared/analysis/motiondtw.py (per_joint_deviation median — 점수 경로가 쓰는 정렬 방식, 재사용)
    - 19-RESEARCH.md Pattern 2 (152-160) — 근본원인 + DTW path 정렬 동일 구간 median 교체 위치
    - 19-PATTERNS.md pipeline 섹션 TRUST-01 (106-114) — 비대칭 코드 + _deviation_against 재사용
    - backend/tests/test_pipeline_mode3.py (test_display_matches_score_source — 통과 대상)
  </read_first>
  <action>
    표시 각도 dict 산출을 점수 산출 source 와 통일. MODE_EXPERT(1800-1801): `user_mean_mode1 = _angles_to_mean_dict(user_seg)` / `ref_mean_mode1 = _angles_to_mean_dict(a_ref)` 를 DTW path-정렬 동일 구간 median 으로 교체. `_deviation_against`(1557-1569)가 이미 반환하는 `match.path` / `user_seg` / `a_ref` 를 사용 — user_seg 와 a_ref 를 path 로 매핑한 후 per_joint median (점수 경로 per_joint_deviation 이 하는 방식 재사용, motiondtw). whole-clip nanmean 의 jitter/occlusion 민감성 + user matched-window vs ref full-clip 시간 비대칭 둘 다 제거.
    _mode3_comparison 의 `ref_mean = _angles_to_mean_dict(prev_seg)`(1609)도 동일하게 DTW-정렬 median 으로 교체 (Mode3 progress 경로).
    _angles_to_mean_dict(1515-1538) 는 호출처가 줄어들면 deprecate 또는 정렬 median helper 로 교체 — 새 helper 가 표시·점수 동일 source 를 보장. 별도 window 계산 금지 (drift 방지, _select_window 정신).
    순수성 유지 (numpy 만, AWS 무관). from __future__ import annotations, snake_case, 이모지 금지. graceful 에러 boundary(2312-2316 패턴) 유지.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_pipeline_mode3.py::test_display_matches_score_source -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_display_matches_score_source GREEN
    - 표시 각도가 점수 source(DTW-정렬 median)와 동일 — grep: MODE_EXPERT 표시값 산출에 whole-clip `np.nanmean` 직접 호출 0건 (정렬 median 경유)
    - user/ref 가 동일 시간 구간(DTW path 정렬)에서 도출 — 비대칭(user_seg mean vs a_ref full-clip mean) 제거
    - 기존 pipeline 테스트 회귀 0 (pre-existing 격리 제외)
  </acceptance_criteria>
  <done>표시 각도 = 점수 산출 source. user/ref 동일 구간. 비대칭 제거. 회귀 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mode3 미보유 게이트 + 근거 카피 + v2 hook 자리 (TRUST-03 / TRUST-05)</name>
  <files>backend/functions/pipeline/app.py, backend/shared/python/sunity_shared/analysis/assemble.py</files>
  <behavior>
    - test_unknown_move_gate: MODE_SELF 미보유 동작 → confident 점수 대신 근거 플래그("기준 동작 없음 — 절대 자세 기준 평가") + 절대트랙, fail-closed/raise 없음
    - test_vision_hook_passthrough: _apply_vision_veto OFF 시 score_result 그대로 반환 (점수 불변)
  </behavior>
  <read_first>
    - backend/functions/pipeline/app.py (MODE_SELF 분기 1839-1847, NOT_POLE 1812 = MODE_EXPERT 전용, get_previous_analysis mode=MODE_SELF 1842 — Pitfall 5 가드 유지, _ensure_adapters lazy-import 패턴, graceful boundary 2312-2316)
    - backend/shared/python/sunity_shared/analysis/assemble.py (lookup_motion_branch 91-113, MotionBranchInfo 39-53, _SAFE_DEFAULT_BRANCH 68-77 copyBranch=branch2, build_mode3 539-562, _DIMENSION_BASELINES_BRANCH1/2 + _baselines_for_branch 140-187, BRANCH2_FORBIDDEN_PHRASES 151-158, _largest_remainder_pct)
    - 19-RESEARCH.md Pattern 3 (162-175) — 3분기 + reference-free 절대트랙 + _SAFE_DEFAULT_BRANCH fail-closed 금지
    - 19-RESEARCH.md Code Examples "v2 비전 hook 자리" (297-305) + Pitfall 5 (Mode3 prev 오인)
    - 19-IPSF-DEDUCTION-NOTES.md §B (reference-free 절대 트랙 — line/stability/posture 절대 평가)
    - 19-PATTERNS.md pipeline TRUST-03/TRUST-05 (116-140) + assemble 섹션 (143-159)
    - backend/tests/test_pipeline_mode3.py (test_unknown_move_gate, test_vision_hook_passthrough — 통과 대상)
  </read_first>
  <action>
    TRUST-03 Mode3 게이트 (D-03 IPSF Page 9 절대 공통 트랙 + 비전 품질 판정 + 근거 명시): MODE_SELF 분기(1839-1847)에 `assemble.lookup_motion_branch(motion_id)` 연결. 반환 MotionBranchInfo 의 copyBranch 로 3분기:
    - branch1 (IPSF 등재) → IPSF 기준 판정 + 근거 + 발전 비교 (기존 경로).
    - branch2 (정은지 reference 보유 비등재) → 정은지 비교 + 근거 + 발전.
    - _SAFE_DEFAULT_BRANCH (미보유) → confident 점수 금지: "기준 동작 없음 — 절대 자세 기준 평가" 근거 플래그 설정 + reference-free 절대트랙(line/stability/posture, 19-IPSF §B)으로 채점. **fail-closed/raise 금지** — 점수는 주되 근거 명시 ([[motion-routing-generalize-principle]]). not_pole 게이트(1812)는 MODE_EXPERT 전용 유지(이동 금지). get_previous_analysis(mode=MODE_SELF)(1842) prev 함정 가드 변경 금지 (Pitfall 5).
    assemble.build_mode3(539-562) 에 미보유 근거 카피 추가 — _DIMENSION_BASELINES_BRANCH1/2(140-187) 형식으로 근거 카피 상수화. BRANCH2_FORBIDDEN_PHRASES(151-158) 패턴으로 "89% 일치"/"정은지와 거의 같음" 같은 거짓 프레이밍 금지 가드 추가. "% 일치" 헤드라인 금지, deltaFromPrevious 절대차원 유지 ([[mode3-progress-not-similarity]]). 근거 필드는 build_mode3 dict 에 옵셔널 키 추가 (이전 빌드 doc 호환).
    TRUST-05 v2 hook (D-02 기하학 주도 + 비전 거부권/교차검증의 자리만, D-04 분할상 v1 은 비-차단 hook 만): 감점식 점수 산출 직후 `_apply_vision_veto(score_result, local_video_path, angles)` pass-through 자리 추가 — v1 = identity (입력 그대로 반환). 기존 `_ensure_adapters()` lazy-import 패턴 모방, `_gemini_vision_enabled()` OFF 시 입력 반환. adapter Protocol 경계 — v2 거부권 본체는 DEFERRED(Phase 18 검증 후). dimensionScores 에 vision-flag 확장 여지(옵셔널 슬롯)만 남기되 v1 schema 변경 없음. graceful boundary(except Exception #noqa BLE001 + log.exception + skip) 박제.
    근거 카피 schema(build_mode3 옵셔널 키)는 3중 계약 정합 — 키 추가 시 analysis.ts/models.py/contract.md 옵셔널 동기화(dimensionExplanation? 패턴 박제). from __future__ import annotations, snake_case, 한국어 docstring(why), 이모지 금지.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_pipeline_mode3.py::test_unknown_move_gate tests/test_pipeline_mode3.py::test_vision_hook_passthrough -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_unknown_move_gate / test_vision_hook_passthrough GREEN
    - 미보유 동작이 raise 하지 않고 점수+근거 플래그를 반환 (fail-closed 금지 — 소스: _SAFE_DEFAULT_BRANCH 경로에 raise 없음)
    - build_mode3 근거 카피에 "% 일치"/"89%"/"정은지와 거의" 거짓 프레이밍 forbidden 가드 존재
    - _apply_vision_veto 가 OFF 시 입력 score_result 를 그대로 반환 (identity — 소스 확인)
    - not_pole 게이트(1812)는 MODE_EXPERT 전용 유지, get_previous_analysis(mode=MODE_SELF) 가드 불변
    - 3중 계약 — 근거 키 추가 시 옵셔널 선언 + analysis.ts/models.py/contract.md 동기화 (또는 키 미추가 시 schema 불변 명시)
    - 전체 backend suite green (pre-existing 격리 제외)
  </acceptance_criteria>
  <done>Mode3 미보유 게이트 + 근거 카피 + v2 hook pass-through 동작. fail-closed 금지. 거짓 프레이밍 가드. 회귀 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

본 plan 은 pipeline 오케스트레이션(SQS consumer, 기존 경로) + ML core 카피 조립 순수함수만 변경 — 신규 endpoint/auth/PII 표면 없음. v2 vision hook 은 pass-through 자리만(본체 DEFERRED) — Gemini 호출 없음(v1). Firestore flat 저장 불변.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-07 | Information disclosure / 신뢰성 | 미보유 동작 거짓 confident 점수("89% 일치") | mitigate | TRUST-03 유효성 게이트 + "기준 동작 없음" 근거 + BRANCH2_FORBIDDEN_PHRASES 거짓 프레이밍 가드 |
| T-19-08 | DoS | 미보유 동작 fail-closed raise → 분석 흐름 차단 | mitigate | _SAFE_DEFAULT_BRANCH 점수 주되 근거 명시(raise 금지), graceful boundary 박제 ([[motion-routing-generalize-principle]]) |
| T-19-09 | Tampering | vision hook 이 v1 에서 점수를 변경 (객관성 위반) | mitigate | _apply_vision_veto v1 = identity pass-through, test_vision_hook_passthrough 점수 불변 단언 |
| T-19-SC | Tampering | npm/pip installs | accept | 신규 패키지 설치 0 (numpy/기존 adapter lazy-import 재사용) — 19-RESEARCH "신규 외부 패키지 없음" |

내부 오케스트레이션/카피 변경 — 위협 표면 낮음 (19-RESEARCH Security Domain: 미보유 거짓 점수만 신뢰성 위협, TRUST-03 가 mitigate).
</threat_model>

<verification>
- `cd backend && python -m pytest tests/test_pipeline_mode3.py -x -q` GREEN (TRUST-01/03/05 신규 케이스)
- `cd backend && python -m pytest tests/ -q` 전체 suite green (pre-existing 격리 제외)
- D-05 anchor(test_anchor_known_answer.py) 는 GPU-skip — Pod 재개 후 방향검증 게이트
- schema 키 변경 시 EAS 재빌드 + sam build --use-container 명시 (근거 카피 옵셔널 키만이면 재배포 불요)
</verification>

<success_criteria>
- 표시 각도 = 점수 source (TRUST-01), Mode3 미보유 게이트 + 근거 (TRUST-03), v2 hook pass-through (TRUST-05)
- fail-closed 금지, 거짓 프레이밍 가드, 점수 객관성(vision identity) 유지
- 전체 backend suite green
</success_criteria>

<output>
Create `.planning/phases/19-vision-hybrid/19-04-SUMMARY.md` when done
</output>
