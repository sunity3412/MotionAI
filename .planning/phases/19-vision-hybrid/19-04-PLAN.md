---
phase: 19-vision-hybrid
plan: 04
type: execute
wave: 2
depends_on: [19-01, 19-02]
files_modified:
  - backend/functions/pipeline/app.py
  - backend/shared/python/sunity_shared/analysis/assemble.py
  - backend/tests/test_assemble.py
  - app/src/types/analysis.ts
  - backend/shared/python/sunity_shared/models.py
  - docs/contract.md
  - app/src/app/analysis/result.tsx
  - app/src/components/DimensionDetailModal.tsx
autonomous: true
requirements: [TRUST-01, TRUST-03, TRUST-05]
must_haves:
  truths:
    - "표시 각도값(현재/기준)이 점수 산출에 쓰는 DTW path-정렬 median 과 동일 source 다 (비대칭 제거)"
    - "scoringBasis 가 실제 채점 SOURCE 로 라벨링된다 — Mode3 first reference-free=reference_free_absolute, first recognized=recognized_motion_absolute, progress known=previous_analysis_plus_absolute, progress reference-free=previous_analysis_plus_reference_free_absolute (composite). Mode3 enum 에 reference_motion 부재 (Mode1 전용)"
    - "Mode1(MODE_EXPERT) 의 reference_motion basis 는 build_mode1 이 emit 하는 OPTIONAL Mode1Comparison 필드(scoringBasis/scoringBasisLabel)로 직렬화된다 — Mode3Comparison 을 통하지 않는다 (3중 계약)"
    - "MODE_SELF 미보유 동작은 is_reference_free_motion 으로 판정되어 절대트랙(line+stability) 채점 + reference-free basis 를 받는다 (posture 는 점수 차원 아님)"
    - "scoringBasis/scoringBasisLabel 이 result 화면 Mode3 헤더 + DimensionDetailModal 에 표시된다 (백엔드 only 아님)"
    - "contributesToOverall===false 차원은 DimensionDetailModal 에 보조지표 안내 카피가 표시된다"
    - "v1 vision hook 은 SAME-OBJECT identity pass-through (점수 불변)이고 v2 거부권 슬롯을 막지 않는다"
  artifacts:
    - path: "backend/functions/pipeline/app.py"
      provides: "_angles_to_dtw_median_dicts 정합 + branch_info early lookup + MODE_SELF 게이트 + scoring_basis 산출 + vision hook(identity)"
      contains: "_angles_to_dtw_median_dicts"
    - path: "backend/shared/python/sunity_shared/analysis/assemble.py"
      provides: "is_reference_free_motion + build_mode3 scoring_basis 카피 (옵셔널 — 미전달 시 옛 dict 보존, Mode3 4-value enum 위반 시 ValueError) + build_mode1 reference_motion basis emit (OPTIONAL)"
      contains: "is_reference_free_motion"
    - path: "app/src/types/analysis.ts"
      provides: "Mode1Comparison 에 OPTIONAL scoringBasis: 'reference_motion' + scoringBasisLabel + Mode3Comparison 4-value scoringBasis (reference_motion 부재)"
      contains: "scoringBasis"
    - path: "app/src/app/analysis/result.tsx"
      provides: "Mode3 scoringBasisLabel 헤더 렌더"
      contains: "scoringBasis"
    - path: "app/src/components/DimensionDetailModal.tsx"
      provides: "reference-free 시 하드코딩 '세계 심사 기준' 대신 basis 사용 + contributesToOverall=false 보조지표 카피"
      contains: "contributesToOverall"
  key_links:
    - from: "pipeline._process MODE_SELF"
      to: "assemble.is_reference_free_motion"
      via: "angleSource=='unavailable' + fixtureKey/ipsfCode None + officialName 없음 → reference_free (copyBranch 단독 분기 금지)"
      pattern: "is_reference_free_motion"
    - from: "assemble.build_mode1 scoringBasis"
      to: "app/src/types/analysis.ts Mode1Comparison"
      via: "Mode1 OPTIONAL scoringBasis='reference_motion' 직렬화 (Mode3Comparison 아님, 3중 계약 lockstep)"
      pattern: "scoringBasis"
    - from: "assemble.build_mode3 scoring_basis"
      to: "app/src/app/analysis/result.tsx"
      via: "scoringBasisLabel 헤더 표시 (TRUST-03 가시화)"
      pattern: "scoringBasis"
    - from: "pipeline._process 점수 산출 직후"
      to: "_apply_vision_veto"
      via: "v1 SAME-OBJECT identity hook (adapter Protocol 자리)"
      pattern: "_apply_vision_veto"
---

<objective>
파이프라인 표시·게이트·hook + 가시화를 마무리한다 — Wave 1 감점식 집계 위에서:
- **TRUST-01 (HIGH-2 iter-1):** `_angles_to_mean_dict`(whole-clip nanmean, user matched-window vs ref full-clip 비대칭)를 신규 `_angles_to_dtw_median_dicts(path, user_seg, ref_angles, joint_keys)` 로 교체 — path-aligned (user_frame, ref_frame) 쌍 per-joint finite median (현재/기준 양측). per_joint_deviation 은 abs-diff median 만 반환하므로 별도 helper 필요. Mode3-first 는 extension_deviation 과 동일 hold-window source 사용 (whole-clip mean 아님).
- **TRUST-03 (BLOCKER-1 iter-1 + ITER-2 HIGH-2/HIGH-3 + ITER-3 HIGH-2 + ITER-4 HIGH-1/HIGH-2):** branch_info lookup 을 recognize 직후로 이동, `is_reference_free_motion(info)` helper 로 미보유 판정(copyBranch 단독 불가: _SAFE_DEFAULT_BRANCH.copyBranch == 실 branch2 동일값). **scoringBasis 는 실제 채점 SOURCE 로 라벨링** — Mode3 first reference-free → `reference_free_absolute`, first recognized → `recognized_motion_absolute`, progress known → `previous_analysis_plus_absolute`, progress reference-free → `previous_analysis_plus_reference_free_absolute`(composite, HIGH-3). **Mode1 의 `reference_motion` basis 는 build_mode1 이 emit 하는 OPTIONAL Mode1Comparison 필드(scoringBasis/scoringBasisLabel)로 직렬화** — Mode3Comparison 을 통하지 않는다 (ITER-3 HIGH-2 + ITER-4 HIGH-1, 3중 계약 lockstep). **build_mode3 의 허용 enum = 정확히 4 Mode3 값 (reference_motion 부재)** — `reference_motion` 전달 시 ValueError. **Mode3 에 reference_motion 거짓 라벨 금지** (first-analysis 는 reference motion 비교 아님 — abs_dims + extension targets). build_mode3 가 scoringBasis/scoringBasisLabel emit → result.tsx + DimensionDetailModal 가 화면 표시.
- **TRUST-05 (ITER-2 MEDIUM-3):** 점수 산출 직후 `_apply_vision_veto` pass-through (v1 = **SAME-OBJECT identity** — `out is score_result`). v2 거부권 본체 DEFERRED.
- **ITER-2 MEDIUM-1:** contributesToOverall===false 차원(stability)은 DimensionDetailModal 에 "종합점수에는 직접 합산하지 않는 보조 지표입니다." 류 안내 — main row 깨끗, detail 정직.
- **ITER-3 MEDIUM-2:** reference-free 절대 트랙 = `line` + `stability` (dimensionScores 키, ScoreDimension/ABSOLUTE_DIMENSIONS 기준). **posture 는 점수 차원이 아니다** (ScoreDimension=angle/line/stability, absolute_dimension_scores 는 line+stability 만 반환) — 실행 채점 경로에서 posture 제거. posture 감점은 점수에 합산되지 않는 future/non-score evidence 레이어로만 (정식 3-way 계약으로 승급될 때 별도 plan).

**ITER-4 반영:**
- **HIGH-1 (Mode1 scoringBasis 계약화):** Mode1 의 reference_motion basis 가 테스트(test_mode1_scoring_basis_reference_motion)되지만 직렬화 계약이 없었다. **결정 = OPTIONAL 로 계약화** (Mode1 테스트 의미 유지 + legacy 호환). Mode1Comparison 에 `scoringBasis?: 'reference_motion'` + `scoringBasisLabel?: string` 추가, build_mode1 이 신규 Mode1 doc 에 항상 emit, contract.md + models.py lockstep. Mode1 테스트는 **직렬화된** comparison.scoringBasis == "reference_motion" 을 단언한다 (내부값 아님). Mode1 basis 는 항상 build_mode1 경유 — 절대 Mode3Comparison 을 통하지 않음.
- **HIGH-2 (reference_motion grep-zero 자기모순 제거):** "reference_motion 은 Mode1 전용" 이라는 설명 주석/문서가 그 리터럴을 포함하므로 raw-prose `reference_motion` grep 0 은 자기모순(false fail). **acceptance 를 ENUM/VALUE 체크로 교체:** TS = Mode3Comparison.scoringBasis union 이 정확히 4 Mode3 리터럴(+reference_motion 부재) / Python = `_MODE3_SCORING_BASES == {4 values}` / Docs = Mode3 테이블이 4 Mode3 값만 + reference_motion 이름의 row/value 없음. 설명 주석("Mode1 전용" 등)이 reference_motion 을 언급하는 것은 허용.

Purpose: 시각적으로 동일한데 19° 차이나는 표시 모순 제거, 미보유 동작 거짓 confident 점수 차단 + 근거를 **정확한 채점 source 로** 사용자가 화면에서 보게, v2 비전 거부권 슬롯 확보.
Output: pipeline/app.py(median helper + early branch_info + 게이트 + scoring_basis 결정 + identity hook), assemble.py(is_reference_free_motion + scoring_basis emit + build_mode1 reference_motion basis, Mode3 4-value enum 가드), 3중 계약(analysis.ts Mode1Comparison+Mode3Comparison / models.py / contract.md) + result.tsx + DimensionDetailModal.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-vision-hybrid/19-REVIEWS.md
@.planning/phases/19-vision-hybrid/19-PATTERNS.md
@.planning/phases/19-vision-hybrid/19-IPSF-DEDUCTION-NOTES.md
@.planning/phases/19-vision-hybrid/19-02-SUMMARY.md
@backend/functions/pipeline/app.py
@backend/shared/python/sunity_shared/analysis/assemble.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 표시-점수 정합 — _angles_to_dtw_median_dicts (TRUST-01 / HIGH-2 iter-1)</name>
  <files>backend/functions/pipeline/app.py</files>
  <behavior>
    - test_display_matches_score_source: user/ref 표시 각도가 동일 DTW path 정렬 구간의 per-joint finite median (whole-clip nanmean 비대칭 제거)
  </behavior>
  <read_first>
    - backend/functions/pipeline/app.py (_angles_to_mean_dict 1515-1538 교체, MODE_EXPERT 비대칭 1800-1801 = user_seg mean vs a_ref full-clip mean, _deviation_against 1557-1569 = match.path/user_seg/a_ref 반환, _mode3_comparison 1572-1630 = first-analysis abs_dims+extension_target 1586-1603 / progress prev_seg 1604-1630, ref_mean 1609 + mode3-first user_mean 1588 + extension_deviation 1598)
    - 19-REVIEWS.md (iter-1) HIGH-2 — per_joint_deviation 은 abs-diff median 만 반환, current/target angle median 미반환 → 전용 helper. Mode3-first 는 extension_deviation 과 동일 hold-window source
    - backend/shared/python/sunity_shared/analysis/motiondtw.py (per_joint_deviation 103-130 = path 따라 abs-diff median — 알고리즘 모방, 단 abs-diff 가 아닌 양측 angle median)
    - backend/shared/python/sunity_shared/analysis/dimensions.py (extension_deviation + _select_window — mode3-first hold-window source)
    - 19-RESEARCH.md Pattern 2 + 19-PATTERNS.md pipeline 섹션 TRUST-01
    - backend/tests/test_pipeline_mode3.py (test_display_matches_score_source — 통과 대상)
  </read_first>
  <action>
    신규 helper 추가: `_angles_to_dtw_median_dicts(path, user_seg, ref_angles, joint_keys) -> tuple[dict[str,float], dict[str,float]]`. per_joint_deviation(motiondtw 103-130) 의 path 순회 패턴을 모방하되 **abs-diff 가 아니라 양측 각도값을 수집**: path 의 각 (u, r) 쌍마다 `user_seg[u]` / `ref_angles[r]` 를 관절별로 모아 finite median 2개 dict 반환 (user_median_by_joint, ref_median_by_joint). NaN 무시(np.nanmedian 또는 finite 필터). 점수 경로(per_joint_deviation)와 **동일 path/구간** 사용 — 표시·점수 source 통일.
    MODE_EXPERT(1800-1801): `user_mean_mode1 = _angles_to_mean_dict(user_seg)` / `ref_mean_mode1 = _angles_to_mean_dict(a_ref)` 를 `_deviation_against` 가 이미 반환하는 `match`(=path) / `user_seg` / `a_ref` 로 `_angles_to_dtw_median_dicts(match.path, user_seg, a_ref, skeleton.JOINT_KEYS)` 호출로 교체. whole-clip nanmean 의 jitter/occlusion 민감성 + user matched-window vs ref full-clip 시간 비대칭 둘 다 제거.
    _mode3_comparison(1572-1630): progress 경로 ref_mean(1609) 를 동일 helper(_match.path/_user_seg/prev_seg 사용 — _deviation_against 반환 unpack 1605 에서 _match/_user_seg 를 실제 사용하도록 변경)로 교체. **mode3-first(1588-1603) user_mean** 은 whole-clip `_angles_to_mean_dict(angles)` 대신 extension_deviation(1598)이 쓰는 동일 hold-window(_select_window) source 의 median 으로 산출 (extension_deviation 과 정합). 별도 window 계산 금지 (drift 방지).
    `_angles_to_mean_dict`(1515-1538)는 호출처가 없어지면 제거 또는 helper 로 위임 — 표시·점수 동일 source 보장. 순수성 유지(numpy 만, AWS 무관). from __future__ import annotations, snake_case, 이모지 금지. graceful boundary(2312-2316 패턴) 유지.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_pipeline_mode3.py::test_display_matches_score_source -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_display_matches_score_source GREEN
    - `_angles_to_dtw_median_dicts` 가 path-aligned 양측 angle median 2 dict 반환 (소스: path 순회 + user/ref 각각 median)
    - 표시 각도가 점수 source(DTW path-정렬 median)와 동일 — grep: MODE_EXPERT 표시값 산출에 whole-clip `_angles_to_mean_dict`/`np.nanmean` 직접 호출 0건
    - mode3-first user 표시값이 extension_deviation 과 동일 hold-window source (소스: _select_window 공유)
    - 무관한 기존 pipeline 테스트 회귀 0 (pre-existing 격리 제외)
  </acceptance_criteria>
  <done>표시 각도 = 점수 산출 DTW path-정렬 median (양측). mode3-first hold-window 정합. 비대칭 제거. 회귀 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: branch_info early lookup + is_reference_free_motion + scoring_basis(source-based) + Mode3 게이트(line+stability) + build_mode3 4-value enum + build_mode1 reference_motion basis + backward-compat + vision identity (TRUST-03 / TRUST-05)</name>
  <files>backend/functions/pipeline/app.py, backend/shared/python/sunity_shared/analysis/assemble.py</files>
  <behavior>
    - test_mode1_scoring_basis_reference_motion (ITER-3 MEDIUM-1 + ITER-4 HIGH-1): MODE_EXPERT → build_mode1 이 emit 한 직렬화 Mode1Comparison.scoringBasis == "reference_motion" (Mode1 전용 — Mode3Comparison 아님). 이 케이스는 Mode3 게이트와 분리
    - test_unknown_move_gate (parametrize, ITER-3 MEDIUM-1 — Mode3 4-value 만): Mode3 first reference-free → reference_free_absolute / Mode3 first recognized → recognized_motion_absolute / Mode3 progress known → previous_analysis_plus_absolute / Mode3 progress reference-free → previous_analysis_plus_reference_free_absolute. 실 branch2 와 reference-free 가 copyBranch 동일값인데 scoringBasis 다름. raise 없음. **Mode3 어디에도 reference_motion 안 나옴 (명시 invariant)**
    - test_build_mode3_backward_compat: build_mode3(is_first=True) (basis 미전달) == {"mode":"mode3","isFirst":True} EXACT. scoring_basis 전달 시에만 scoringBasis/scoringBasisLabel 키 등장. **build_mode3(scoring_basis="reference_motion") → ValueError** (Mode3 4-value enum 위반)
    - test_vision_hook_passthrough: _apply_vision_veto OFF 시 `out is score_result` (SAME object identity) + 점수 mutation 0
  </behavior>
  <read_first>
    - backend/functions/pipeline/app.py (recognize 1733, abs_dims 1734, MODE_EXPERT 1740-1847, MODE_SELF 1839-1847, _mode3_comparison 1572-1630 = first-analysis abs_dims+extension_target(reference motion 비교 아님!) vs progress prev_seg, NOT_POLE 1812-1820 = MODE_EXPERT 전용, branch_info 현재 lookup 1945-1947 = recognize/scoring 보다 늦음 → 이동, coach_context 1948-1959, build_result 호출부 ~2045 = branch_info forward, get_previous_analysis mode=MODE_SELF 1842 Pitfall 5)
    - backend/shared/python/sunity_shared/analysis/assemble.py (MotionBranchInfo 39-53 = copyBranch/ipsfCode/officialName/angleSource/angleFixtureKey 필드, _SAFE_DEFAULT_BRANCH 68-77 copyBranch="branch2_eunji_reference"=실 branch2 동일값/angleSource="unavailable"/angleFixtureKey=None/ipsfCode=None/officialName="", lookup_motion_branch 91-113, build_mode1 520-537 = {"mode":"mode1","referenceMotionId","referenceMotionName","athleteName","similarity"} + segmentScores? — reference_motion basis 추가 위치, build_mode3 539-562 = `out: dict = {"mode":"mode3","isFirst":bool(is_first)}` + is_first 면 early return 552-553, _DIMENSION_BASELINES_* 119-187, _baselines_for_branch 175-187)
    - 19-REVIEWS.md (iter-4) HIGH-1 (15-39) build_mode1 reference_motion OPTIONAL 계약화 + Mode1 test 가 직렬화 필드 단언 / HIGH-2 (41-53) reference_motion acceptance = enum/value 체크
    - 19-REVIEWS.md (iter-3) HIGH-2 build_mode3 enum = 4 Mode3 값(reference_motion 제거), Mode1 basis 는 build_mode1 / MEDIUM-1 Mode1 케이스 분리 / MEDIUM-2 posture 점수 차원 아님 — 실행 경로에서 제거
    - 19-REVIEWS.md (iter-1) BLOCKER-1 is_reference_free_motion 정의 / (iter-2) HIGH-2 scoringBasis = 실제 source 라벨 / HIGH-3 composite basis / MEDIUM-2 build_mode3 backward-compat / MEDIUM-3 _apply_vision_veto SAME object identity
    - 19-RESEARCH.md Pattern 3 (3분기 + reference-free 절대트랙 + fail-closed 금지) + "v2 비전 hook 자리" + Pitfall 5
    - 19-IPSF-DEDUCTION-NOTES.md §B (reference-free 절대 트랙) — **단 dimensionScores 키는 line+stability 만** (ScoreDimension/ABSOLUTE_DIMENSIONS 기준; posture 는 점수 차원 아님)
    - app/src/types/analysis.ts:119 (ScoreDimension = angle|line|stability — posture 부재) + backend dimensions.py:155 (ABSOLUTE_DIMENSIONS=(DIM_LINE,DIM_STABILITY))
    - 19-PATTERNS.md pipeline TRUST-03/TRUST-05 + assemble 섹션
    - backend/tests/test_pipeline_mode3.py (test_mode1_scoring_basis_reference_motion, test_unknown_move_gate, test_build_mode3_backward_compat, test_vision_hook_passthrough — 통과 대상)
    - backend/tests/test_assemble.py (test_mode1_shape_and_clamp 58-66 = build_mode1 exact-dict 5키 단언 → build_mode1 always-emit 로 깨짐, 갱신 대상; test_mode1_segment_scores_included_only_when_given 69-79 = 부분 단언, 영향 없음 확인)
  </read_first>
  <action>
    **branch_info early lookup:** 현재 1945-1947 의 `branch_info = assemble.lookup_motion_branch(getattr(profile,"motion_id",None))` 를 `profile = recognizer.recognize(...)`(1733) **직후로 이동** → MODE_EXPERT/MODE_SELF 분기 전에 사용 가능. coach_context(1948-1959) / build_result(~2045) 는 이미 동일 branch_info 참조하므로 중복 lookup 제거 (single source). _mode3_comparison 시그니처에 branch_info 전달.
    **is_reference_free_motion:** assemble.py 에 helper 추가:
      `is_reference_free_motion(info: MotionBranchInfo | None) -> bool` = `info is None or (info.angleSource == "unavailable" and info.angleFixtureKey is None and info.ipsfCode is None and not info.officialName)`. copyBranch 로 판정 금지 (_SAFE_DEFAULT_BRANCH.copyBranch == 실 branch2 동일값 — 구분 불가). docstring 에 왜 copyBranch 단독 불가인지 명시.
    **scoring_basis 결정 (ITER-2 HIGH-2/HIGH-3 + ITER-3 HIGH-2/MEDIUM-1 — 실제 채점 SOURCE 기준):** scoringBasis 를 실제 점수 source 로 도출:
      - MODE_EXPERT (Mode1, reference 각도 실제 비교) → `reference_motion`. **이 값은 Mode3Comparison 을 통해 흐르지 않는다** — Mode1 표시 경로(build_mode1)에서만 set (아래 build_mode1 basis 참조). _mode3_comparison/build_mode3 에는 절대 전달 금지 (ITER-3 HIGH-2 + ITER-4 HIGH-1).
      - MODE_SELF first-analysis (abs_dims + extension targets — reference motion 비교 아님!):
        · is_reference_free_motion(branch_info) True → `reference_free_absolute`.
        · recognized profile (False) → `recognized_motion_absolute`.
      - MODE_SELF progress (previous-analysis angle 일관성 + abs_dims):
        · known motion → `previous_analysis_plus_absolute`.
        · reference-free motion → `previous_analysis_plus_reference_free_absolute` (composite — previous-analysis 각도 일관성 + reference-free 절대 line/stability 둘 다 점수에 들어감, HIGH-3 lossy 금지).
      Mode3 4값 enum = {reference_free_absolute, recognized_motion_absolute, previous_analysis_plus_absolute, previous_analysis_plus_reference_free_absolute}. **Mode3 어디에도 reference_motion 사용 금지.** scoringBasisLabel(user-facing) 은 위 source 의 정확한 한국어 — reference-free 류는 "기준 동작 없음 — 절대 자세 기준 평가", composite 는 "이전 분석 대비 + 절대 자세 기준" 류. _mode3_comparison 이 branch_info + is_first 로 Mode3 4값 중 하나를 산출해 build_mode3 에 전달.
    **Mode3 게이트 (ITER-3 MEDIUM-2 — line+stability 만):** MODE_SELF 분기(1839-1847) + _mode3_comparison 에서 branch_info 로 분기 — is_reference_free_motion True 면 reference-free 절대트랙으로 채점하되 **dimensionScores 키는 `line` + `stability` 만** (absolute_dimension_scores 가 이미 line+stability 반환; ScoreDimension/ABSOLUTE_DIMENSIONS 기준). **posture 는 dimensionScores 키로 추가하지 않는다** — posture 감점은 점수에 합산되지 않는 future/non-score evidence 레이어이며, 점수 차원으로 승급하려면 ScoreDimension + ABSOLUTE_DIMENSIONS + 3중 계약 + UI 라벨 + weighting + dimensionExplanation + 테스트를 모두 추가하는 별도 plan 필요 (이 phase 범위 밖). **fail-closed/raise 금지** ([[motion-routing-generalize-principle]]). not_pole 게이트(1812)는 MODE_EXPERT 전용 유지(이동 금지). get_previous_analysis(mode=MODE_SELF)(1842) Pitfall 5 가드 불변.
    **build_mode3 scoringBasis (ITER-2 MEDIUM-2 backward-compat + ITER-3 HIGH-2 4-value enum):** assemble.build_mode3(539-562) 시그니처에 `scoring_basis: str | None = None`, `scoring_basis_label: str | None = None` 추가. **basis 미전달(None) 시 기존 dict 정확히 보존** — is_first=True 면 `{"mode":"mode3","isFirst":True}` EXACT (552-553 early return 유지, basis None 이면 키 미추가). basis 전달 시에만 out dict 에 `scoringBasis` + `scoringBasisLabel` 추가 (is_first early-return 전에 set 하되 None 가드). **build_mode3 의 scoring_basis 허용값 = 정확히 4 Mode3 값** (`reference_free_absolute`|`recognized_motion_absolute`|`previous_analysis_plus_absolute`|`previous_analysis_plus_reference_free_absolute`) — 모듈 상수 frozenset `_MODE3_SCORING_BASES` 로 검증. **`reference_motion` 등 enum 밖 값 전달 시 ValueError** (free-form 문자열 금지). docstring 에 "reference_motion 은 Mode1 전용 — Mode3 에 들어오면 ValueError" 명시. 라벨 카피는 _DIMENSION_BASELINES_* 형식으로 상수화. BRANCH2_FORBIDDEN_PHRASES 패턴으로 "89% 일치"/"정은지와 거의 같음" 거짓 프레이밍 가드 ([[mode3-progress-not-similarity]]). _process 의 build_mode3 호출부(_mode3_comparison 1603/1624)에 Mode3 4값 scoring_basis/label 전달.
    **build_mode1 reference_motion basis (ITER-4 HIGH-1 — OPTIONAL 계약화):** assemble.build_mode1(520-537) 가 신규 Mode1 doc 에 `scoringBasis` + `scoringBasisLabel` 를 **항상 emit** — `out["scoringBasis"] = "reference_motion"`, `out["scoringBasisLabel"] = "<정은지 등 athleteName> 측정 각도 기준 비교"` 류 (athleteName 활용 한국어 카피). 이 두 필드는 Mode1Comparison schema 의 OPTIONAL 필드 (Task 3 에서 3중 계약 추가) — legacy doc 호환 위해 옵셔널이되 신규 doc 은 항상 채운다. **reference_motion 은 build_mode1 에서만 set** — _mode3_comparison/build_mode3 로 전달 금지. _process MODE_EXPERT 경로가 build_mode1 호출(기존 위치)에서 basis 가 흐르도록 확인 (별도 인자 불필요 — build_mode1 내부 상수). docstring 에 "Mode1 의 scoringBasis 는 항상 reference_motion (정은지 reference 각도 실제 비교) — Mode3 에는 이 값이 존재하지 않음" 명시.

    **기존 exact-dict 테스트 갱신 (ITER-5 — build_mode1 always-emit 회귀):** build_mode1 이 신규 doc 에 scoringBasis/scoringBasisLabel 을 항상 emit 하므로 기존 `backend/tests/test_assemble.py::test_mode1_shape_and_clamp`(58-66) 의 exact-dict 단언(`c == {...scoringBasis 없는 5키...}`)이 깨진다. 해당 기대 dict 에 `"scoringBasis": "reference_motion"` + `"scoringBasisLabel": <build_mode1 이 내는 정확한 문자열>` 을 추가해 갱신한다. `test_mode1_segment_scores_included_only_when_given`(69-79)은 키 접근(`c["segmentScores"]`)·부분 단언이라 깨지진 않지만, **scoringBasis 가 segmentScores 유무 양쪽 doc 에 모두 존재함을 추가 단언하도록 확장**한다 (segment 있는 경우 + 없는 단일 모션 경우 둘 다 `c["scoringBasis"]=="reference_motion"`) — 미래 리팩터가 단순 Mode1 에만 basis 를 빠뜨리는 것을 방어 (ITER-5 리뷰). 이 갱신은 "무관한 기존 케이스 회귀 0" 원칙의 예외가 아니라 **새 계약에 직접 영향받는 케이스의 정당한 갱신**이다 (BLOCKER-3 패턴과 동일).
    **TRUST-05 vision hook (ITER-2 MEDIUM-3 SAME-OBJECT identity):** 감점식 점수 산출 직후 `_apply_vision_veto(score_result, local_video_path, angles)` pass-through 자리 — **v1 = 동일 객체 반환 (`return score_result`, copy/deepcopy 금지)**. `_ensure_adapters()` lazy-import 패턴 모방, `_gemini_vision_enabled()` OFF 시 입력 객체 그대로 반환. v1 은 mutation 0 (객체 변경 금지) — v2 가 mutate 시작할 때 계약 의도적 전환. adapter Protocol 경계 — v2 본체 DEFERRED. graceful boundary(except Exception #noqa BLE001 + log.exception + skip) 박제. v1 schema 변경 없음. docstring 에 "v1 = same-object identity, v2 가 mutation 도입 시 계약 전환" 명시.
    from __future__ import annotations, snake_case, 한국어 docstring(why), 이모지 금지.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_pipeline_mode3.py::test_mode1_scoring_basis_reference_motion tests/test_pipeline_mode3.py::test_unknown_move_gate tests/test_pipeline_mode3.py::test_build_mode3_backward_compat tests/test_pipeline_mode3.py::test_vision_hook_passthrough tests/test_assemble.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_mode1_scoring_basis_reference_motion / test_unknown_move_gate / test_build_mode3_backward_compat / test_vision_hook_passthrough GREEN
    - is_reference_free_motion 이 angleSource/angleFixtureKey/ipsfCode/officialName 으로 판정 (grep: copyBranch 로 reference-free 판정 0건)
    - branch_info lookup 이 recognize(1733) 직후 1회, MODE_SELF/coach_context/build_result 가 동일 객체 참조 (중복 lookup 0)
    - **build_mode3 의 scoring_basis 허용 enum = 정확히 4 Mode3 값** — `_MODE3_SCORING_BASES` frozenset 정의가 정확히 {reference_free_absolute, recognized_motion_absolute, previous_analysis_plus_absolute, previous_analysis_plus_reference_free_absolute} (ENUM/VALUE 체크 — `_MODE3_SCORING_BASES` 멤버십이 4개이고 reference_motion ∉ frozenset; ITER-4 HIGH-2). reference_motion 전달 시 ValueError (test 단언)
    - scoringBasis 가 실제 채점 source 로 라벨링 — first reference-free=reference_free_absolute, first recognized=recognized_motion_absolute, progress known=previous_analysis_plus_absolute, progress reference-free=previous_analysis_plus_reference_free_absolute (composite). Mode3 경로(_mode3_comparison/build_mode3) 의 build_mode3 호출 인자 scoring_basis ∈ _MODE3_SCORING_BASES (reference_motion 미전달 — test 가 단언)
    - **Mode1 reference_motion basis (ITER-4 HIGH-1):** build_mode1 이 신규 doc 에 `scoringBasis="reference_motion"` + `scoringBasisLabel` 을 emit (소스: build_mode1 내부 상수). reference_motion 은 build_mode1 에서만 set — _mode3_comparison/build_mode3 호출 인자에 reference_motion 미전달 (test_unknown_move_gate invariant 가 단언)
    - build_mode3(is_first=True, basis 미전달) 가 정확히 `{"mode":"mode3","isFirst":True}` 반환 (test_build_mode3_backward_compat) — basis 전달 시에만 scoringBasis/scoringBasisLabel 등장
    - **기존 test_assemble.py 갱신 (ITER-5):** (a) `test_mode1_shape_and_clamp` 기대 dict 가 `scoringBasis="reference_motion"`/`scoringBasisLabel` 포함하도록 갱신, (b) `test_mode1_segment_scores_included_only_when_given` 이 segmentScores 유무 양쪽 doc 모두 `scoringBasis=="reference_motion"` 존재를 단언하도록 확장 (단순 Mode1 basis 누락 방어). `tests/test_assemble.py` GREEN (회귀 0)
    - **reference-free 절대트랙 dimensionScores 키 = line+stability 만** — posture 가 dimensionScores 키로 추가되지 않음 (grep: 신규 추가 채점 경로에 `"posture"`/`DIM_POSTURE` dimensionScores 키 0건; ABSOLUTE_DIMENSIONS 불변 = (DIM_LINE, DIM_STABILITY)) (ITER-3 MEDIUM-2)
    - 미보유 동작이 raise 하지 않고 점수+scoringBasis 반환 (fail-closed 금지)
    - build_mode3 근거 카피에 "% 일치"/"89%"/"정은지와 거의" forbidden 가드 존재
    - _apply_vision_veto OFF 시 `out is score_result` (SAME object) + 점수/필드 mutation 0 (test 가 identity + 값 불변 단언)
    - not_pole(1812) MODE_EXPERT 전용 유지, get_previous_analysis(mode=MODE_SELF) 가드 불변
    - 무관한 backend suite 회귀 0 (pre-existing 격리 제외)
  </acceptance_criteria>
  <done>branch_info early single-lookup + is_reference_free_motion + scoringBasis(source-based, Mode3 4-value enum frozenset, reference_motion=build_mode1 전용+ValueError 가드) + reference-free 트랙=line+stability(posture 점수 차원 아님) + build_mode3 backward-compat + build_mode1 reference_motion basis + 게이트 + vision SAME-object identity. fail-closed 금지. 거짓 프레이밍 가드. 회귀 0.</done>
</task>

<task type="auto">
  <name>Task 3: scoringBasis 3중 계약(Mode1 OPTIONAL + Mode3 4-value) + result.tsx 헤더 + DimensionDetailModal 가시화 + 보조지표 카피 (TRUST-03 / ITER-2 MEDIUM-1 / ITER-4 HIGH-1)</name>
  <files>app/src/types/analysis.ts, backend/shared/python/sunity_shared/models.py, docs/contract.md, app/src/app/analysis/result.tsx, app/src/components/DimensionDetailModal.tsx</files>
  <read_first>
    - 19-REVIEWS.md (iter-4) HIGH-1 (15-39) Mode1Comparison 에 scoringBasis?: 'reference_motion' + scoringBasisLabel? 추가, build_mode1/contract.md/models.py lockstep / HIGH-2 (41-53) reference_motion acceptance = enum/value/table 체크 (raw prose grep 0 금지)
    - 19-REVIEWS.md (iter-3) HIGH-2 — Mode3 enum = 4 값(reference_motion 제거), models.py/contract.md/analysis.ts 정렬 / MEDIUM-2 posture 점수 차원 아님
    - 19-REVIEWS.md (iter-2) HIGH-2 scoringBasis source / HIGH-3 composite enum / MEDIUM-1 contributesToOverall=false → DimensionDetailModal 보조지표 안내 카피
    - app/src/types/analysis.ts (ScoreDimension 119 = angle|line|stability(posture 부재), Mode1Comparison 239-247 = mode/referenceMotionId/referenceMotionName/athleteName/similarity/segmentScores? — **scoringBasis 부재, 추가 위치**, Mode3Comparison 249-256 = mode/isFirst/previousAnalysisId/deltaFromPrevious — scoringBasis 추가 위치, AnalysisMode 8, DimensionExplanation 263-267 contributesToOverall 옵셔널 = Plan 02 추가)
    - backend/shared/python/sunity_shared/analysis/assemble.py (build_mode1 520-537 = Mode1 dict — Task 2 가 scoringBasis emit, build_mode3 539-562)
    - backend/shared/python/sunity_shared/models.py (MODE_EXPERT/MODE_SELF 10-11, SCORE_DIMENSIONS 17, DIMENSION_EXPLANATION_KEYS 35 — mode3/mode1 comparison 키 상수/주석)
    - docs/contract.md (Mode1Comparison 정의 240-244, Mode3Comparison 248-256, dimensionScores 174-179, baseline 185-186)
    - app/src/app/analysis/result.tsx (헤더 sub 672-678 = mode3 sub 텍스트 677 아래가 basis 렌더 위치, cmp.mode mode3 분기 491-496, summary)
    - app/src/components/DimensionDetailModal.tsx (baselineFor 87-102 + formulaFor 59-83 하드코딩 "세계 심사 기준" 68/74/80/94 — reference-free 시 basis 사용, explanation prop 115/deficit 133, Props 111-121, DIMENSION_MEASUREMENT_KO 105-109)
    - 19-PATTERNS.md 3중 계약 섹션 + result.tsx 표시 컨벤션
  </read_first>
  <action>
    **3중 계약 — Mode1 scoringBasis OPTIONAL 추가 (ITER-4 HIGH-1, Task 2 build_mode1 emit 과 lockstep):**
    - app/src/types/analysis.ts `Mode1Comparison`(239-247)에 `scoringBasis?: 'reference_motion';` + `scoringBasisLabel?: string;` 추가 (OPTIONAL — legacy doc 호환, 신규 doc 은 build_mode1 이 항상 채움). 주석에 "Mode1 의 scoringBasis 는 항상 reference_motion (정은지 reference 각도 실제 비교) — Mode3Comparison 에는 이 값이 없음 (Mode1 전용)".
    - backend/shared/python/sunity_shared/models.py Mode1 comparison 키 상수/주석에 scoringBasis="reference_motion" (Mode1 전용) 반영.
    - docs/contract.md Mode1Comparison 정의(240-244)에 `scoringBasis? 'reference_motion'` + `scoringBasisLabel?` 추가 + "Mode1 전용 — Mode3 에 없음" 명시.
    **3중 계약 — Mode3 scoringBasis 4-value 추가 (Task 2 build_mode3 emit 과 lockstep, ITER-3 HIGH-2):**
    - app/src/types/analysis.ts `Mode3Comparison`(249-256)에 `scoringBasis?: 'reference_free_absolute' | 'recognized_motion_absolute' | 'previous_analysis_plus_absolute' | 'previous_analysis_plus_reference_free_absolute';` + `scoringBasisLabel?: string;` 추가 (옵셔널). **Mode3 union 에 reference_motion 미포함** (Mode3 는 reference motion 비교 아님 — Mode1 전용). 주석에 각 enum 의미 + composite(progress+reference-free) 설명 + "reference_motion 은 Mode1 전용이라 이 union 에 없음".
    - backend/shared/python/sunity_shared/models.py 의 mode3 comparison 키 상수/주석에 scoringBasis/scoringBasisLabel + **Mode3 4-value** 반영. **주석에 "5-enum" 표기 금지** — Mode3 = 4값, reference_motion 은 Mode1 전용이라 Mode3 comparison 에 미포함이라고 명시. (Mode1 basis 상수는 위 Mode1 섹션 / build_mode1 에 위치.)
    - docs/contract.md Mode3Comparison 정의(248-256)에 scoringBasis **4-value** enum(source-based) + label + "미보유 first=reference_free_absolute, progress reference-free=composite, **Mode3 에 reference_motion 없음(Mode1 전용)**, 거짓 % 일치 프레이밍 금지" 명시.
    **result.tsx 헤더 렌더 (가시화):** mode3 헤더 sub(677) 아래에 `cmp.scoringBasisLabel` 이 있으면 1줄 표시 (스타일은 기존 styles.sub/coachPositioning 토큰 재사용 — 색/스페이싱 하드코딩 금지, #FF4B33 직접 사용 금지, theme 토큰만). reference-free 일 때 "기준 동작 없음 — 절대 자세 기준 평가" 가 사용자에게 보인다. scoringBasisLabel 없을 때는 미렌더 (graceful). (mode1 도 build_mode1 이 scoringBasisLabel 을 채우므로 동일 렌더 로직이 mode1 헤더에도 적용 가능 — 단 기존 mode1 카피 유지가 우선, scoringBasisLabel 은 보조 1줄.)
    **DimensionDetailModal 하드코딩 교체 (가시화):** baselineFor(87-102) + formulaFor(59-83)의 하드코딩 "세계 심사 기준" 카피가 reference-free Mode3 에서는 거짓 — scoringBasis 가 reference-free 류('reference_free_absolute' 또는 'previous_analysis_plus_reference_free_absolute')일 때 basis(scoringBasisLabel 또는 절대트랙 카피)로 대체. Props 에 scoringBasis/scoringBasisLabel optional 전달 (result.tsx 가 cmp 에서 넘김). recognized/previous_analysis_plus_absolute 일 때는 기존 카피 유지. (reference_motion 은 Mode3 에 부재 — Mode1 모달 경로는 기존 카피 그대로.)
    **ITER-2 MEDIUM-1 — contributesToOverall=false 보조지표 카피:** DimensionDetailModal 에서 `explanation?.contributesToOverall === false` 일 때 detail 영역에 quiet 안내 1줄 표시 — 예 "종합점수에는 직접 합산하지 않는 보조 지표입니다." (theme 토큰 색/스페이싱, 하드코딩 금지). main score row 는 깨끗하게 유지 (이 안내는 모달 detail 에만). explanation 미존재(legacy) 또는 contributesToOverall !== false 면 미표시 (graceful — Plan 02 default true 와 정합).
    이모지 금지, 2-space indent, single quote, `import type`, theme 토큰만 (브랜드 컬러/스페이싱 하드코딩 금지 — app/CLAUDE.md).
  </action>
  <verify>
    <automated>cd app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && npm run typecheck` (tsc --noEmit) clean
    - **Mode1 계약 (ITER-4 HIGH-1):** analysis.ts Mode1Comparison 에 `scoringBasis?: 'reference_motion'` + `scoringBasisLabel?: string` 존재 (ENUM/VALUE 체크 — Mode1Comparison.scoringBasis 의 유일 리터럴이 'reference_motion'), models.py + contract.md Mode1 섹션 + build_mode1(Task 2) 에 동시 반영 (3중 계약 lockstep)
    - **Mode3 계약 (ENUM/VALUE 체크 — ITER-4 HIGH-2, raw prose grep 금지):** analysis.ts Mode3Comparison.scoringBasis union 이 정확히 4 Mode3 리터럴 {reference_free_absolute, recognized_motion_absolute, previous_analysis_plus_absolute, previous_analysis_plus_reference_free_absolute} 이고 reference_motion 이 union 멤버가 아님 (union 멤버 4개 + reference_motion ∉ Mode3Comparison.scoringBasis 정의). composite enum previous_analysis_plus_reference_free_absolute 포함 (HIGH-3)
    - **models.py / contract.md Mode3 테이블/키 체크 (ENUM/VALUE — ITER-4 HIGH-2):** Mode3 comparison 의 scoringBasis 허용값 목록(테이블/상수)이 정확히 4 Mode3 값이고 reference_motion 이라는 row/value 가 없음 + "5-enum" 표기 0건. (설명 주석에 "reference_motion 은 Mode1 전용" 처럼 리터럴이 등장하는 것은 허용 — raw `reference_motion` grep 0 을 요구하지 않음)
    - scoringBasis/scoringBasisLabel 이 Mode1(analysis.ts/models.py/contract.md/build_mode1) + Mode3(analysis.ts/models.py/contract.md/build_mode3) 양쪽에서 lockstep
    - result.tsx 가 mode3 + scoringBasisLabel 존재 시 헤더에 라벨 렌더 (grep: scoringBasisLabel 참조)
    - DimensionDetailModal 이 reference-free 시 하드코딩 "세계 심사 기준" 대신 basis 사용 (grep: scoringBasis 분기 존재)
    - DimensionDetailModal 이 contributesToOverall===false 시 보조지표 안내 카피 표시 (grep: contributesToOverall 분기 + "보조 지표" 카피)
    - theme 토큰만 사용 (grep: 신규 추가 라인에 `#` hex 색/매직 px 0건)
  </acceptance_criteria>
  <done>scoringBasis 3중 계약 (Mode1 OPTIONAL 'reference_motion' + Mode3 4-value, composite 포함) + result.tsx 헤더 + DimensionDetailModal basis 분기 + contributesToOverall=false 보조지표 카피. Mode3 에 reference_motion 부재(enum/value 체크), Mode1 에 OPTIONAL reference_motion. TRUST-03 근거 정확히 화면 표시. tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

본 plan 은 pipeline 오케스트레이션(기존 SQS consumer 경로) + ML core 조립 순수함수 + 앱 표시 카피 + 3중 계약만 변경 — 신규 endpoint/auth/PII 표면 없음. v2 vision hook 은 pass-through 자리만(본체 DEFERRED, Gemini 호출 없음). Firestore flat 저장 불변(scoringBasis = 옵셔널 키 추가).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-07 | Information disclosure / 신뢰성 | 미보유 동작 거짓 confident 점수("89% 일치") | mitigate | is_reference_free_motion 판정 + scoringBasis=reference_free_absolute + BRANCH2_FORBIDDEN_PHRASES + 화면 가시화 |
| T-19-08 | DoS | 미보유 동작 fail-closed raise → 분석 흐름 차단 | mitigate | _SAFE_DEFAULT_BRANCH 점수 주되 근거 명시(raise 금지) ([[motion-routing-generalize-principle]]) |
| T-19-09 | Tampering | vision hook 이 v1 에서 점수를 변경 (객관성 위반) | mitigate | _apply_vision_veto v1 = SAME-object identity, test_vision_hook_passthrough `out is score_result` + mutation 0 단언 (ITER-2 MEDIUM-3) |
| T-19-12 | Information disclosure | scoringBasis 가 reference-free 인데 모달이 하드코딩 "세계 심사 기준" 표시 (모순 카피) | mitigate | DimensionDetailModal basis 분기 |
| T-19-16 | Information disclosure | Mode3 first 가 reference_motion 라벨 → 거짓 reference 비교 함의 (신뢰 문제 재발) | mitigate | build_mode3 4-value enum frozenset (reference_motion ValueError 가드), Mode3 build_mode3 호출 인자 reference_motion 0, Mode1 전용 분리 (ITER-2 HIGH-2 + ITER-3 HIGH-2 + ITER-4 HIGH-1/HIGH-2) |
| T-19-18 | Information disclosure | posture 가 점수 차원으로 새서 미정의 dimensionScores 키 / 거짓 posture 점수 함의 | mitigate | reference-free 트랙 = line+stability 만, ABSOLUTE_DIMENSIONS 불변, posture dimensionScores 키 0 (ITER-3 MEDIUM-2) |
| T-19-21 | Information disclosure | Mode1 scoringBasis 가 테스트되나 직렬화 계약 부재 → 구현자 임의값/계약 drift | mitigate | Mode1Comparison OPTIONAL scoringBasis 계약화 + build_mode1 emit + 3중 lockstep, Mode1 test 가 직렬화 필드 단언 (ITER-4 HIGH-1) |
| T-19-SC | Tampering | npm/pip installs | accept | 신규 패키지 설치 0 (numpy/기존 adapter lazy-import 재사용) |

내부 오케스트레이션/카피/표시 변경 — 위협 표면 낮음.
</threat_model>

<verification>
- `cd backend && python -m pytest tests/test_pipeline_mode3.py -x -q` GREEN (TRUST-01/03/05 + Mode1 basis 직렬화 + backward-compat 케이스)
- `cd backend && python -m pytest tests/ -q` 전체 suite green (pre-existing 격리 제외)
- `cd app && npm run typecheck` clean
- D-05 anchor(test_anchor_known_answer.py) 는 RUN_PHASE19_ANCHORS env-gate — Pod 재개 후 방향검증
- scoringBasis schema 키 추가 → EAS 재빌드 (옵셔널 키, Mode1+Mode3)
</verification>

<success_criteria>
- 표시 각도 = 점수 DTW median source (TRUST-01), Mode3 미보유 게이트(line+stability) + scoringBasis(Mode3 4-value source 라벨, composite 처리) 화면 가시화 (TRUST-03), v2 hook SAME-object identity (TRUST-05)
- branch_info early single-lookup, fail-closed 금지, build_mode3 4-value enum frozenset + backward-compat + reference_motion ValueError 가드, build_mode1 reference_motion OPTIONAL basis, 거짓 프레이밍 가드, vision identity
- reference-free 트랙 = line+stability (posture 점수 차원 아님 — future evidence 레이어)
- 3중 계약 lockstep (Mode1 OPTIONAL reference_motion + Mode3 4-value enum, ENUM/VALUE 체크) + result.tsx + DimensionDetailModal(basis + 보조지표 카피)
- 전체 backend suite + tsc green
</success_criteria>

<output>
Create `.planning/phases/19-vision-hybrid/19-04-SUMMARY.md` when done — scoringBasis schema 키 추가(Mode1+Mode3)로 EAS 재빌드 필요 명시
</output>
</content>
</invoke>
