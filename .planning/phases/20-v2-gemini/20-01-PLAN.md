---
phase: 20-v2-gemini
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/shared/python/sunity_shared/analysis/vision_veto.py
  - backend/tests/test_vision_veto.py
autonomous: true
requirements: [SCORE-08, TRUST-08]
must_haves:
  truths:
    - "어떤 (overall, severity) 입력도 apply_downward_cap 출력이 입력보다 크지 않다 (하향 전용 — 비전은 절대 점수를 올리지 않음, D-01)"
    - "minor/None/미지 severity 는 점수를 깎지 않는다 (fault-free 정타 95~100 보존)"
    - "worst_pose_timestamp 가 key_moments(hold/peak) 재사용으로 지배 결함 pose 시점을 고른다 (신규 Gemini moment 호출 0, D-05)"
    - "SEVERITY_CAP 의 moderate/major 수치는 placeholder(None or 미확정)이며 6페어 curve-fit 이 아니다 — 실 수치는 20-04 eval 도출 (D-02)"
    - "SEVERITY_CAP_PROVENANCE 가 데이터(주석 아님)로 박제되어 도출 출처를 증명한다 — sensitivity_manifest_sha256 가 absent/TODO 인 동안 moderate/major cap 을 채우면 가드(테스트)가 거부한다 + phase18_pairs_used_for_derivation=False (HIGH-3 / D-02 fail-closed)"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/vision_veto.py"
      provides: "순수 SEVERITY_CAP 테이블 + SEVERITY_CAP_PROVENANCE(data) + apply_downward_cap(min-cap) + worst_pose_timestamp (numpy 외 의존 0, AWS/네트워크 무관)"
      contains: "SEVERITY_CAP_PROVENANCE"
    - path: "backend/tests/test_vision_veto.py"
      provides: "하향전용 property test + minor-no-cap + worst-pose 재사용 + curve-fit 가드 + provenance fail-closed 단위테스트"
      contains: "def test_downward_only_property"
  key_links:
    - from: "vision_veto.apply_downward_cap"
      to: "score_result['overallScore'] (20-03 _apply_vision_veto)"
      via: "terminal min() cap on already-min-of-core overall (dimensions.py:384 정합)"
      pattern: "min\\("
    - from: "vision_veto.worst_pose_timestamp"
      to: "TechniqueProfile.key_moments (technique 프로파일 hold/peak)"
      via: "moment_key=='hold' 우선, peak fallback (신규 moment 호출 0)"
      pattern: "moment_key"
    - from: "vision_veto.SEVERITY_CAP_PROVENANCE"
      to: "derive_caps.py (20-04) cap 도출 게이트"
      via: "sensitivity_manifest_sha256 부재/TODO 시 moderate/major 채움 금지 (fail-closed)"
      pattern: "sensitivity_manifest_sha256"
---

<objective>
v2 비전 거부권의 **순수 코어**(numpy 외 의존 0, AWS/네트워크 무관)를 TDD 로 만든다 — Gemini 호출/파이프라인 wiring 없이 단위테스트만으로 D-01 하향-전용 invariant 와 D-05 worst-pose 재사용을 코드로 못 박는다. 이 plan 은 **pod-free** 이며 phase 의 가장 중요한 안전 성질(비전은 점수를 못 올린다)을 가장 먼저 확정한다.

- **SCORE-08 (D-01 하향 전용):** `apply_downward_cap(overall, severity)` = severity→cap 테이블 lookup 후 `min(overall, cap)`. 어떤 입력도 출력 ≤ 입력 (property test). 가중블렌드/하한/`max()` 금지.
- **SCORE-08 (D-05 worst-pose):** `worst_pose_timestamp(profile)` = key_moments(hold/peak) 재사용으로 지배 결함 pose 시점 선택. IPSF phase 평균 금지(Phase 19 희석 버그 재발 차단).
- **D-02 curve-fit 금지 (HIGH-3 fail-closed):** SEVERITY_CAP 의 moderate/major 수치는 **placeholder**(None 또는 미확정 마커)로 두고, 실 수치는 20-04 의 generalization eval 에서 도출. 출처를 **주석이 아닌 데이터**(`SEVERITY_CAP_PROVENANCE` dict)로 박제한다 — `sensitivity_manifest_sha256` 가 absent/TODO 인 동안 moderate/major cap 을 채우면 가드 테스트가 거부하고, `phase18_pairs_used_for_derivation` 는 영구 `False`. 이 plan 은 수치를 6페어에 맞추지 않는다.

Purpose: 비전이 점수를 올려 위양성을 재발시키는 것(이 phase 최대 리스크)을 **구조적**으로 차단 — min() 으로 인코딩하면 코드가 invariant 를 강제한다. cap 출처도 데이터로 박제해 curve-fit 경로를 fail-closed 로 막는다.
Output: backend/shared/python/sunity_shared/analysis/vision_veto.py (순수 모듈 + SEVERITY_CAP_PROVENANCE) + backend/tests/test_vision_veto.py (property + 재사용 + curve-fit + provenance 가드).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/20-v2-gemini/20-CONTEXT.md
@.planning/phases/20-v2-gemini/20-RESEARCH.md
@.planning/phases/20-v2-gemini/20-VALIDATION.md
@backend/shared/python/sunity_shared/analysis/dimensions.py
@backend/shared/python/sunity_shared/analysis/technique.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 순수 vision_veto 코어 — apply_downward_cap(하향 전용) + SEVERITY_CAP(placeholder) + SEVERITY_CAP_PROVENANCE(data, fail-closed) + worst_pose_timestamp(key_moments 재사용)</name>
  <files>backend/shared/python/sunity_shared/analysis/vision_veto.py, backend/tests/test_vision_veto.py</files>
  <behavior>
    - test_downward_only_property: 모든 (overall ∈ 0..100, severity ∈ {None,'minor','moderate','major','unknown'}) 에 대해 apply_downward_cap(overall, severity) <= overall (절대 안 올림). minor/None/unknown → overall 불변(== overall).
    - test_minor_no_cap: severity='minor' 또는 None → 입력 그대로 반환 (정타 95~100 보존). SEVERITY_CAP['minor'] 가 cap 미적용(None or 100) 임을 단언.
    - test_cap_lowers_for_major: SEVERITY_CAP['major'] 가 수치로 확정되면 overall=100 입력이 그 값으로 내려감. 단 **수치 자체는 단언하지 않는다**(D-02) — placeholder(None) 일 때는 cap 미적용을 단언, 수치일 때는 min 동작만 단언.
    - test_severity_cap_no_curve_fit_marker: SEVERITY_CAP 의 moderate/major 가 명시적 placeholder(None) 이거나 SEVERITY_CAP_PROVENANCE 가 "phase20_sensitivity" 출처를 가진다는 것을 단언 (D-02 가드).
    - test_provenance_is_data_not_comment (HIGH-3): SEVERITY_CAP_PROVENANCE 가 module 상수 dict 로 존재하고 키 {source, sensitivity_manifest_sha256, phase18_pairs_used_for_derivation} 를 가진다. source=='phase20_sensitivity', phase18_pairs_used_for_derivation is False (영구). 주석 grep 이 아니라 import 한 dict 를 직접 검사.
    - test_cap_fill_requires_real_manifest_sha (HIGH-3 fail-closed): SEVERITY_CAP['moderate'] 또는 ['major'] 가 None 이 아닌 정수면, SEVERITY_CAP_PROVENANCE['sensitivity_manifest_sha256'] 는 absent/None/'TODO' 가 아닌 실 sha256 문자열이어야 한다(둘이 동시 만족). 모순(수치 cap + TODO/None sha) 시 테스트 FAIL — curve-fit fail-closed 인코딩. (현재 plan 상태: cap=None + sha=None → 일관되므로 PASS. 20-04 가 sha 채운 뒤에만 cap 채울 수 있음.)
    - test_worst_pose_prefers_hold: key_moments 에 hold/peak 혼재 시 hold 우선, hold 없으면 peak, 둘 다 없으면 첫 moment. key_moments None/빈 → None 반환 (graceful).
    - test_worst_pose_no_new_moment_call: worst_pose_timestamp 가 profile.key_moments 만 읽고 외부 호출 0 (순수). Gemini/네트워크 import 0.
  </behavior>
  <read_first>
    - backend/shared/python/sunity_shared/analysis/dimensions.py:384 (overall_from_dimensions = min-of-core(angle,line) — cap 이 이 위에 terminal min 으로 합성됨을 이해; CORE_DIMENSIONS 381)
    - backend/shared/python/sunity_shared/analysis/technique.py:40-71 (TechniqueProfile 데이터클래스 + key_moments 필드 71 = tuple[KeyMoment,...] | None)
    - backend/shared/python/sunity_shared/analysis/gemini_technique_recognizer.py:300-394 (KeyMoment 사용 — moment_key/timestamp_seconds 속성, KeyMoment(...) 생성 351-354, getattr 패턴 393-394)
    - 20-RESEARCH.md §"Pattern 1: Downward-only clamp" (SEVERITY_CAP + apply_downward_cap 예시 — 수치는 placeholder) + §"Code Examples: Worst-pose frame selection" (worst_pose_timestamp 예시) + §"Pitfall 3" (curve-fit 금지) + §"Pitfall 1" (averaging 금지)
    - 20-CONTEXT.md D-01 (하향 전용) / D-02 (curve-fit 금지) / D-05 (worst-pose, 평균 거부)
    - 20-DIRECT-REVIEW.md HIGH-3 (SEVERITY_CAP_PROVENANCE 데이터 박제 + manifest sha fail-closed)
    - backend/shared/python/sunity_shared/analysis/kismam.py (순수 함수 + from __future__ import annotations 컨벤션 모방 — numpy 만, boto3/네트워크 무관)
    - backend/tests/test_pipeline_mode3.py (테스트 스타일/import 경로 컨벤션 — PYTHONPATH=backend/shared/python)
  </read_first>
  <action>
    신규 순수 모듈 `backend/shared/python/sunity_shared/analysis/vision_veto.py` 생성. `from __future__ import annotations`. numpy 외 의존 금지 — boto3/Gemini/네트워크/firestore import 절대 0 (adapter 는 20-02 가 별도 모듈로).
    **SEVERITY_CAP 상수:** dict `SEVERITY_CAP: dict[str, int | None]` = `{"minor": None, "moderate": None, "major": None}`. 모든 값 **placeholder None**(cap 미적용). 모듈 헤더 docstring + 상수 위 주석에 한국어로 "이 수치는 20-04 의 generalization-tested eval(미보유+above-cutoff sensitivity 셋)에서 도출한다. 6페어에 curve-fit 절대 금지 (D-02 / [[scoring-redesign-must-generalize-no-overfit]])" 명시. minor 는 영구 None(정타 무캡 — D-01 95~100 보존). moderate/major 는 20-04 가 채울 자리.
    **SEVERITY_CAP_PROVENANCE 상수 (HIGH-3 — 주석 아닌 데이터):** module 상수 dict `SEVERITY_CAP_PROVENANCE: dict[str, object]` = `{"source": "phase20_sensitivity", "sensitivity_manifest_sha256": None, "phase18_pairs_used_for_derivation": False}`. `sensitivity_manifest_sha256` 는 20-04 derive_caps 가 sensitivity manifest 의 실 sha256 으로 채운다(현재 None=미도출). `phase18_pairs_used_for_derivation` 는 **영구 False**(6페어는 회귀 검증 전용, derive 입력 아님 — derive_caps 가 변경 금지). docstring 에 "cap 수치를 채우려면 sensitivity_manifest_sha256 가 실 sha 여야 한다(TODO/None 금지) — curve-fit fail-closed (HIGH-3)" 명시. 모듈에 `assert_cap_provenance_consistent()` 류 순수 헬퍼를 추가해도 좋으나, 강제는 test_cap_fill_requires_real_manifest_sha 단위테스트가 담당.
    **apply_downward_cap(overall: int, severity: str | None) -> int:** `cap = SEVERITY_CAP.get(severity)` (미지 키 → None). `cap is None` → `return overall` (불변). 아니면 `return min(overall, cap)`. **올림 경로 절대 금지** — max()/하한/가중블렌드 없음. docstring 에 "v1 overall 을 vision severity 로 하향만. None/미지/minor → 불변. invariant: 반환값 ≤ overall 항상" 명시.
    **worst_pose_timestamp(profile) -> float | None:** `getattr(profile, "key_moments", None) or ()` → hold(`moment_key=="hold"`) 우선, 없으면 peak, 둘 다 없으면 전체. `min(timestamp_seconds for chosen, default=None)`. getattr 방어(20-RESEARCH Code Examples 패턴). **신규 Gemini moment 호출 금지** (D-05 — key_moments 재사용만). fps 주석(9.0 frame_extractor 타깃) 박제.
    snake_case, 한국어 docstring(why), 이모지 금지, 4-space indent.
    **테스트 파일** `backend/tests/test_vision_veto.py`: behavior 8 케이스 구현. property test 는 0..100 × severity 전수(또는 hypothesis 없으면 명시 루프). worst-pose 는 가짜 profile(SimpleNamespace 또는 KeyMoment 모킹) 으로 hold/peak 우선순위 검증. curve-fit 가드 = SEVERITY_CAP['moderate']/['major'] is None OR provenance source=='phase20_sensitivity'. **provenance fail-closed 가드** = SEVERITY_CAP_PROVENANCE 를 import 한 dict 로 직접 검사(주석 grep 아님) + cap 수치 ↔ manifest sha 일관성 단언(test_provenance_is_data_not_comment + test_cap_fill_requires_real_manifest_sha). Gemini import 0 가드(모듈 소스에 'genai'/'requests' 문자열 0).
  </action>
  <verify>
    <automated>cd backend && PYTHONPATH=shared/python python -m pytest tests/test_vision_veto.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_vision_veto.py 8 테스트 GREEN
    - apply_downward_cap 의 property: ∀ (overall, severity) 출력 ≤ overall (test_downward_only_property GREEN) — 올림 경로 0 (grep: vision_veto.py 에 `max(` 또는 가중블렌드/`*` 점수연산 0건, `min(` 만)
    - minor/None/미지 severity → overall 불변 (test_minor_no_cap GREEN)
    - SEVERITY_CAP['moderate'] / ['major'] 가 placeholder None 이거나 provenance source=='phase20_sensitivity' (test_severity_cap_no_curve_fit_marker GREEN — D-02 가드)
    - SEVERITY_CAP_PROVENANCE 가 module dict(주석 아님)로 존재 + source='phase20_sensitivity' + phase18_pairs_used_for_derivation is False (test_provenance_is_data_not_comment GREEN — HIGH-3)
    - moderate/major cap 이 None 이 아니면 sensitivity_manifest_sha256 가 실 sha(absent/None/'TODO' 아님)임을 단언; 모순 시 FAIL (test_cap_fill_requires_real_manifest_sha GREEN — HIGH-3 fail-closed; 현재 cap=None + sha=None 일관 → PASS)
    - worst_pose_timestamp 가 hold>peak>전체 우선순위로 key_moments 재사용, None/빈 → None (test_worst_pose_prefers_hold GREEN)
    - vision_veto.py 가 numpy 외 무거운 의존 0 — grep: `import genai`/`google`/`requests`/`boto3`/`firestore` 0건 (순수 모듈)
    - 무관한 backend suite 회귀 0 (pre-existing 격리 제외)
  </acceptance_criteria>
  <done>순수 vision_veto.py (apply_downward_cap min-cap + SEVERITY_CAP placeholder + SEVERITY_CAP_PROVENANCE data fail-closed + worst_pose_timestamp key_moments 재사용) + 8 단위테스트 GREEN. 하향-전용 invariant 코드 강제. curve-fit provenance fail-closed. Gemini 의존 0. 회귀 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

본 plan 은 순수 알고리즘 모듈 + 단위테스트만 — 신규 endpoint/auth/PII/네트워크 표면 0. Gemini 호출·파이프라인 wiring·Firestore 저장 모두 후속 plan(20-02/20-03). 입력 = 정수 overall + severity 문자열 + 메모리상 profile 객체.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering / 신뢰성 | 비전이 점수를 **올려** 위양성 재발 (D-01 위반) | mitigate | apply_downward_cap = `min()` only, test_downward_only_property 가 ∀입력 출력≤입력 단언 + grep `max(` 0건 |
| T-20-02 | Tampering / 객관성 | SEVERITY_CAP 수치를 6페어에 curve-fit (D-02 위반) | mitigate | 수치 placeholder None + SEVERITY_CAP_PROVENANCE(data) + test_severity_cap_no_curve_fit_marker 가드. 실 수치는 20-04 eval 도출 |
| T-20-02b | Tampering / 객관성 | sensitivity 자산 없이 cap 을 채워 curve-fit (HIGH-3) | mitigate | SEVERITY_CAP_PROVENANCE.sensitivity_manifest_sha256 = None 동안 cap 채움 금지(test_cap_fill_requires_real_manifest_sha fail-closed) + phase18_pairs_used_for_derivation 영구 False |
| T-20-03 | Information disclosure | worst-pose 가 IPSF phase 평균으로 결함 희석 (D-05 위반, Phase 19 버그 재발) | mitigate | worst_pose_timestamp = 단일 지배 pose(hold/peak) 선택, 평균 경로 0 (key_moments 재사용) |
| T-20-SC | Tampering | npm/pip installs | accept | 신규 패키지 0 (numpy 기존). 20-RESEARCH Package Legitimacy Audit = N/A (신규 패키지 0) |

내부 순수 모듈 — 위협 표면 최소.
</threat_model>

<verification>
- `cd backend && PYTHONPATH=shared/python python -m pytest tests/test_vision_veto.py -x -q` GREEN
- `cd backend && PYTHONPATH=shared/python python -m pytest tests/ -q` 전체 suite 회귀 0 (pre-existing 격리 제외)
- 객관성 grep: vision_veto.py 에 점수 라벨/사람 점수 ground truth 0, `max(` 0건
- SEVERITY_CAP_PROVENANCE fail-closed: cap 수치 ↔ manifest sha 일관성 단위테스트 GREEN
- Pod 무관 (전부 pod-free 단위테스트)
</verification>

<success_criteria>
- apply_downward_cap 하향-전용 property 코드 강제 (SCORE-08 / D-01)
- SEVERITY_CAP 수치 placeholder + SEVERITY_CAP_PROVENANCE data fail-closed (SCORE-09 준비 / D-02 / HIGH-3)
- worst_pose_timestamp key_moments 재사용, 평균 거부 (SCORE-08 / D-05)
- 순수 모듈 (Gemini/AWS 의존 0), 단위테스트 GREEN, 회귀 0
</success_criteria>

<output>
Create `.planning/phases/20-v2-gemini/20-01-SUMMARY.md` when done — vision_veto.py 순수 코어 + SEVERITY_CAP placeholder + SEVERITY_CAP_PROVENANCE(20-04 가 sensitivity_manifest_sha256 채움, 그 전엔 cap 채움 금지) 명시.
</output>
