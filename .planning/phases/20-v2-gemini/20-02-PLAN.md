---
phase: 20-v2-gemini
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py
  - backend/tests/test_gemini_vision_scorer.py
autonomous: true
requirements: [SCORE-08, TRUST-06, TRUST-08]
user_setup:
  - service: gemini
    why: "비전 결함-심각도 판정 (이미 production 사용 중인 키 재사용 — 신규 시크릿 0)"
    env_vars:
      - name: GEMINI_API_KEY
        source: "SSM /sunity/motion/gemini-api-key (로컬 조회 --profile sunity-motion). 이미 recognizer/coach 가 사용 — 신규 발급 불필요"
    dashboard_config:
      - task: "신규 설정 없음 — 기존 Gemini 키/모델 재사용"
        location: "변경 없음"
must_haves:
  truths:
    - "Gemini 비전 어댑터 응답 스키마에 score/점수 필드가 0 — 출력은 결함 위치/종류/severity enum/기하 추정만 (객관성 hard gate, D-02). build_schema()['properties'] 내성검사(introspection)로 단언하며 spike 의 overall_qualitative 를 production 스키마에 복사 금지 (MEDIUM-1)"
    - "응답 텍스트에 점수 라벨(NN점 / NN/100 / NN%)이 누출되면 _SCORE_PATTERN 가드가 거부한다 (_SCORE_PATTERN 은 구현 leak-guard — 위반 아님)"
    - "temperature 0 + 전용 VisionVetoCache(video_hash, model, PROMPT_VERSION, SCHEMA_VERSION, at_seconds_bucket)로 같은 영상=같은 severity verdict (결정론, D-06 — temp 0 단독 아님, 캐시가 실 보장). PROMPT_VERSION/SCHEMA_VERSION 변경 시 stale verdict 무효화 (MEDIUM-2)"
    - "어댑터가 caller 의 local_video_path 만 쓰고 영상 재다운로드/RTMW 재실행 0 (B4 hard gate)"
    - "Gemini API 실패/키 부재 시 graceful — verdict=None 반환 (분석 흐름 안 막음) + 관측 가능"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py"
      provides: "Gemini 결함-심각도 어댑터 (Protocol, lazy-import, temp 0, PROMPT_VERSION/SCHEMA_VERSION + VisionVetoCache, 객관성 스키마(no overall) + _SCORE_PATTERN 가드)"
      contains: "def assess_fault_severity"
    - path: "backend/tests/test_gemini_vision_scorer.py"
      provides: "mocked-Gemini 단위테스트 — 스키마/dataclass 내성검사 no-score + _SCORE_PATTERN 누출 거부 + PROMPT_VERSION 캐시 무효화 + graceful 실패 (실 API/Pod 무관)"
      contains: "def test_schema_introspection_has_no_score_field"
  key_links:
    - from: "gemini_vision_scorer.assess_fault_severity"
      to: "build_schema (no-score, no-overall) + _SCORE_PATTERN guard"
      via: "spike 리팩터 — response_schema 점수/overall 필드 0 + 누출 정규식 거부"
      pattern: "_SCORE_PATTERN"
    - from: "gemini_vision_scorer (determinism)"
      to: "VisionVetoCache (video_hash + model + PROMPT_VERSION + SCHEMA_VERSION + at_seconds_bucket)"
      via: "같은 키 → 저장된 verdict 반환; PROMPT/SCHEMA 버전 바뀌면 무효화 (temp 0 단독 아님)"
      pattern: "PROMPT_VERSION"
---

<objective>
Gemini **결함-심각도 어댑터**(line 차원 아님 — overall cap 용 severity)를 spike 리팩터로 만든다. mocked-Gemini 단위테스트만으로 객관성(no-score, no-overall)·결정론(prompt/schema-versioned 캐시)·graceful 실패를 확정한다 — 실 Gemini API/Pod 무관(**pod-free**). 20-01 의 순수 cap 코어와 20-03 의 파이프라인 wiring 사이의 adapter 경계.

- **SCORE-08 (어댑터 출력):** `assess_fault_severity(local_video_path, at_seconds=None)` → `VisionVerdict(primary_fault: str, severity: 'minor'|'moderate'|'major', differences: list)`. **score 필드 절대 없음** (D-02 객관성). severity enum 만 20-01 의 apply_downward_cap 에 먹인다.
- **TRUST-08 (객관성 hard gate — MEDIUM-1 introspection):** 객관성을 **raw 파일 grep 이 아니라 스키마/데이터클래스 내성검사**로 강제. `build_schema()["properties"]`(중첩 포함)에 forbidden 이름 {score, overall, rating, 점수} 0; `VisionVerdict` 데이터클래스 필드 = 허용 집합 정확히. `_SCORE_PATTERN` 은 구현 leak-guard 로 **허용**(파일에 존재해도 위반 아님). spike 의 `overall_qualitative`(spike_vision_grounding_pair.py:217) 를 production 스키마에 복사하는 것을 **명시적으로 금지**(strict no-overall). 응답에 "NN점/NN/100/NN%" 누출 시 거부.
- **TRUST-06 (결정론 — MEDIUM-2 prompt/schema 버전):** `temperature=0.0`(spike 0.1→0) + **전용 `VisionVetoCache`**(recognizer 의 (video_hash, model, yaml_version) 키 재사용 금지) — 키 = (video_hash, model_name, PROMPT_VERSION, SCHEMA_VERSION, at_seconds_bucket/input_granularity). 같은 영상=같은 severity. **PROMPT_VERSION/SCHEMA_VERSION 변경 시 stale verdict 무효화** — 프롬프트/스키마 바뀌면 옛 verdict 가 살아남지 않는다.
- **B4 hard gate:** caller 의 `local_video_path` 만 사용 — 영상 재다운로드/RTMW 재실행 0.
- **graceful (Pitfall 5):** 키 부재/API 실패 → verdict=None + WARNING 로그 (silent no-op 차단은 20-03 이 audit 필드로 완성, 여기선 None 신호 + 로그).

Purpose: 비전 판단을 객관성·결정론 경계 안에 가둔다 — Gemini 는 결함 종류/심각도 자연어만, 점수는 절대 안 냄(점수는 20-01 의 결정론 테이블이 cap 으로 변환). 객관성은 구조적 내성검사로, 결정론은 prompt/schema-versioned 캐시로 강제.
Output: gemini_vision_scorer.py (Protocol 어댑터, lazy-import, PROMPT_VERSION/SCHEMA_VERSION, VisionVetoCache) + test_gemini_vision_scorer.py (mocked).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/20-v2-gemini/20-CONTEXT.md
@.planning/phases/20-v2-gemini/20-RESEARCH.md
@.planning/phases/20-v2-gemini/20-VALIDATION.md
@backend/research/spikes/spike_vision_grounding_pair.py
@backend/shared/python/sunity_shared/analysis/technique_cache.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gemini 결함-심각도 어댑터 — VisionVerdict 스키마(no-score, no-overall) + 내성검사 가드 + _SCORE_PATTERN + temp 0 + PROMPT_VERSION/SCHEMA_VERSION VisionVetoCache + graceful (mocked-Gemini 테스트)</name>
  <files>backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py, backend/tests/test_gemini_vision_scorer.py</files>
  <behavior>
    - test_schema_introspection_has_no_score_field (MEDIUM-1): build_schema()['properties'] (중첩 properties 재귀 포함) 의 키 집합에 forbidden {'score','overall','rating','점수'} 0. raw 파일 grep 아님 — import 한 schema dict 를 직접 순회. severity enum(minor/moderate/major) + primary_fault(str) + differences 만.
    - test_verdict_dataclass_fields_exact (MEDIUM-1): VisionVerdict 데이터클래스 필드 = 허용 집합 {primary_fault, severity, differences} 정확히(초과/score 필드 0). dataclasses.fields 로 단언.
    - test_no_overall_qualitative_copied (MEDIUM-1): build_schema()['properties'] 에 'overall_qualitative'(spike:217) 키 0 — strict no-overall, spike 스키마 복사 금지.
    - test_score_pattern_rejects_leak: 응답 텍스트에 "85점" 또는 "85/100" 또는 "85%" 가 있으면 _SCORE_PATTERN.search 가 매칭 → 어댑터가 거부/None (점수 누출 차단). (_SCORE_PATTERN 의 *존재* 는 위반 아님 — leak guard.)
    - test_assess_returns_severity_enum: mocked Gemini JSON(primary_fault + severity='major') → VisionVerdict.severity in {minor,moderate,major}, score 속성 부재(getattr 'score' 없음).
    - test_determinism_cache_same_key: 같은 (video_hash, model, PROMPT_VERSION, SCHEMA_VERSION, at_seconds_bucket) 2회 호출 → Gemini 호출 1회(2번째는 캐시) + verdict 동일. temperature=0.0 으로 호출됨을 단언(mock 호출 인자 검사).
    - test_prompt_version_invalidates_cache (MEDIUM-2): PROMPT_VERSION 변경(또는 SCHEMA_VERSION 변경) 시 같은 video_hash 라도 캐시 miss → Gemini 재호출. stale verdict 가 prompt/schema 변경을 넘어 살아남지 않음.
    - test_graceful_no_key: GEMINI_API_KEY 부재(또는 client 생성 실패) → assess_fault_severity 가 None 반환(raise 0) + WARNING 로그 1회.
    - test_no_video_redownload: 어댑터가 caller local_video_path 만 받고, boto3/S3 download/RTMW 재실행 호출 0 (mock 으로 S3/RTMW 미호출 단언).
  </behavior>
  <read_first>
    - backend/research/spikes/spike_vision_grounding_pair.py 전체 (MODEL=gemini-3.1-pro-preview 57, upload_and_wait 120 + ASCII workaround 100, build_schema 167-205, **overall_qualitative 217 = production 복사 금지 대상(MEDIUM-1)**, _SCORE_PATTERN 233, temperature=0.1 261-263 → 0.0 변경 대상, response_mime_type/thinking_config, 응답 파싱 289 누출 가드)
    - backend/shared/python/sunity_shared/analysis/technique_cache.py (compute_video_hash SHA256 42-62, compute_yaml_version 98, class TechniqueCache 166 + cache key tuple (video_hash, model_name, yaml_version) 172 — **재사용 금지(MEDIUM-2): severity verdict 는 prompt/schema 민감**. 전용 VisionVetoCache 의 키 패턴/Firestore-backed 저장 구조만 모방)
    - backend/shared/python/sunity_shared/analysis/gemini_technique_recognizer.py (lazy-import 패턴 + 어댑터 graceful fallback 137-141 api_failure, _adapter_reject_guard 54 객관성 double-defense, GEMINI_MODEL env, cache wiring 77/115-226)
    - backend/shared/python/sunity_shared/analysis/interfaces.py (Protocol 어댑터 경계 컨벤션 — FrameExtractor/PoseEstimator/CoachWriter typing.Protocol)
    - 20-RESEARCH.md §"Pattern 2: Objectivity-safe Gemini schema" + §"Pitfall 2"(temp 0 ≠ bit-deterministic, 캐시가 실 보장 + A1 temp 0.0 serialize 검증) + §"Pitfall 5"(graceful 무음실패) + §"Don't Hand-Roll"(upload_and_wait/ _SCORE_PATTERN 재사용; **단 cache 키는 전용**) + §"Security Domain"(키 로그 금지, PII video_hash)
    - 20-DIRECT-REVIEW.md MEDIUM-1(schema/dataclass introspection, overall_qualitative 금지) + MEDIUM-2(PROMPT_VERSION/SCHEMA_VERSION 캐시 무효화)
    - 20-CONTEXT.md D-06(temp 0 + 캐시) + 객관성 HARD(score 필드 0) + [[gemini-key-local-ssm-profile]](로컬 --profile sunity-motion) + [[gemini-latest-model-versions]](gemini-3.1-pro-preview -preview 필수)
    - backend/tests/ (mocked Gemini 테스트 스타일 — monkeypatch client, 실 API 미호출)
  </read_first>
  <action>
    신규 어댑터 `backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py` 생성. `from __future__ import annotations`. Gemini SDK(google-genai)는 **lazy-import**(`_ensure_client()` 류) — 모듈 top-level import 금지(coach_writer/recognizer 패턴). Protocol 경계.
    **버전 상수 (MEDIUM-2):** module 상수 `PROMPT_VERSION: str`(예 "v2.0") + `SCHEMA_VERSION: str`(예 "v2.0"). 프롬프트 문자열/스키마를 바꾸면 이 상수도 bump 해야 함을 docstring 에 명시 — 캐시 키에 들어가 stale verdict 무효화.
    **VisionVerdict 값객체:** `@dataclass(frozen=True) class VisionVerdict`: `primary_fault: str`, `severity: str`(minor/moderate/major), `differences: tuple[dict, ...]`. **score 필드 절대 추가 금지** (객관성). docstring 에 "score 없음 — severity enum 만 20-01 apply_downward_cap 에 먹인다. 사람 점수 라벨 ground truth 금지" 명시.
    **build_schema():** spike build_schema(167-205) 리팩터 — properties = motion/primary_fault/differences(body_part/correct_state/fault_state/approx_angle_deviation_deg/severity enum/ipsf_note)/extension_gaps. **score/overall/rating/점수 키 0 + spike 의 overall_qualitative(217) 복사 금지(MEDIUM-1 strict no-overall).** severity enum=["minor","moderate","major"]. response_mime_type="application/json" + response_schema + thinking_config(thinking_budget=-1).
    **_SCORE_PATTERN 가드:** spike line 233 정규식(`\b\d{1,3}\s*(점|/\s*100|/\s*10|%|퍼센트)`) 재사용. 응답 raw_text 에 매칭 시 → verdict 거부(None) + WARNING("점수 누출 — 객관성 위반, verdict 폐기"). recognizer `_adapter_reject_guard` double-defense 정신 정합. (이 상수의 *존재* 는 객관성 위반이 아니라 leak guard — 내성검사 테스트가 build_schema/dataclass 만 검사하므로 충돌 없음.)
    **VisionVetoCache (MEDIUM-2 — 전용, recognizer 캐시 재사용 금지):** 신규 클래스(또는 명시 namespace) — Firestore-backed(technique_cache 저장 구조 모방) 하되 키 = `(video_hash, model_name, PROMPT_VERSION, SCHEMA_VERSION, at_seconds_bucket)`. `at_seconds_bucket` = at_seconds 를 정수초(또는 명시 input_granularity)로 양자화(None → 'whole'). 키에 PROMPT_VERSION/SCHEMA_VERSION 가 들어가므로 프롬프트/스키마 bump 시 자동 cache-miss.
    **assess_fault_severity(local_video_path, at_seconds=None) -> VisionVerdict | None:** (1) `_gemini_vision_veto_enabled()`/키 확인 → 부재면 None + WARNING(graceful, Pitfall 5). (2) VisionVetoCache 키 조회 → hit 시 저장 verdict 반환(Gemini 호출 0, 결정론 D-06). (3) miss 시 upload_and_wait(spike 120, ASCII workaround 100) + generate_content(**temperature=0.0** — spike 0.1 에서 변경, D-06 + A1 검증) + response_schema. (4) _SCORE_PATTERN 가드 통과 후 VisionVerdict 파싱 → VisionVetoCache 저장 → 반환. **at_seconds 는 worst-pose 힌트**(20-01 worst_pose_timestamp 산출값) — Open Q1 = whole-video 업로드(spike 패턴, 안전 default) 채택, at_seconds 는 프롬프트 힌트 + cache 키 bucket 으로만(프레임 샘플링 최적화는 후속). **B4: caller local_video_path 만 사용 — S3 재다운로드/RTMW 재실행 0.** 키 로그 금지(spike never prints key). PII = video_hash 만(경로/원본 미로그).
    `_ensure_client()` lazy-import + 모듈 캐시 싱글톤(recognizer 패턴). graceful boundary(except → None + log.exception/warning). snake_case, 한국어 docstring(why), 이모지 금지.
    **테스트** `backend/tests/test_gemini_vision_scorer.py`: behavior 9 케이스. Gemini client 는 monkeypatch(가짜 generate_content 반환) — 실 API 미호출. 캐시는 in-memory fake 또는 monkeypatch. 객관성 가드는 build_schema()/dataclasses.fields 내성검사(raw grep 금지). PROMPT_VERSION 무효화는 상수 monkeypatch 후 cache-miss 단언. test_no_video_redownload 는 boto3/RTMW import/호출 감시(mock assert_not_called).
  </action>
  <verify>
    <automated>cd backend && PYTHONPATH=shared/python python -m pytest tests/test_gemini_vision_scorer.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_gemini_vision_scorer.py 9 테스트 GREEN (실 Gemini API/Pod 미호출 — 전부 mocked)
    - build_schema()['properties'] 내성검사(중첩 포함)에 score/overall/rating/점수 키 0 + overall_qualitative 0 (test_schema_introspection_has_no_score_field + test_no_overall_qualitative_copied GREEN — MEDIUM-1, raw grep 아님)
    - VisionVerdict 데이터클래스 필드 = {primary_fault, severity, differences} 정확 (test_verdict_dataclass_fields_exact GREEN — MEDIUM-1)
    - _SCORE_PATTERN 이 "NN점/NN/100/NN%" 누출을 거부 (test_score_pattern_rejects_leak GREEN — 객관성 hard gate; _SCORE_PATTERN 의 존재는 위반 아님)
    - 같은 캐시 키 2회 → Gemini 호출 1회 + verdict 동일, temperature=0.0 호출 인자 (test_determinism_cache_same_key GREEN — TRUST-06)
    - PROMPT_VERSION/SCHEMA_VERSION 변경 → cache-miss + Gemini 재호출 (test_prompt_version_invalidates_cache GREEN — MEDIUM-2, stale verdict 무효화)
    - 키 부재/API 실패 → None 반환 + raise 0 + WARNING (test_graceful_no_key GREEN — Pitfall 5)
    - caller local_video_path 만 사용, S3 download/RTMW 재실행 호출 0 (test_no_video_redownload GREEN — B4)
    - Gemini SDK lazy-import (grep: 모듈 top-level `import google`/`from google` 0건 — 함수 내부 lazy 만)
    - VisionVetoCache 키에 PROMPT_VERSION + SCHEMA_VERSION 포함, recognizer (video_hash, model, yaml_version) 키 재사용 0 (grep: 전용 캐시 키)
    - 신규 패키지 설치 0 (google-genai/numpy 기존 — 20-RESEARCH Package Legitimacy Audit N/A)
    - 무관한 backend suite 회귀 0 (pre-existing 격리 제외)
  </acceptance_criteria>
  <done>gemini_vision_scorer.py (VisionVerdict no-score + build_schema no-score/no-overall + 내성검사 가드 + _SCORE_PATTERN + temp 0 + PROMPT_VERSION/SCHEMA_VERSION VisionVetoCache 결정론·무효화 + graceful None + B4 no-redownload, lazy-import Protocol) + 9 mocked 테스트 GREEN. 객관성(introspection)·결정론(versioned cache) 경계 확정. 회귀 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

client→Gemini API: 영상(데이터, 명령 아님)이 외부 multimodal 모델로 나간다. pipeline→어댑터: severity verdict 가 채점 path 로 들어온다(20-03). 신규 secret 0(기존 GEMINI_API_KEY 재사용). PII = 영상 → video_hash 로만 로그.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-04 | Tampering / Repudiation | Gemini 가 사람/AI 점수 라벨을 ground truth 로 출력 (객관성 위반) | mitigate | response_schema 에 score/overall 필드 0 (build_schema 내성검사) + _SCORE_PATTERN 누출 가드 double-defense + VisionVerdict score 속성 부재. test 가 introspection 으로 단언 |
| T-20-04b | Tampering / 객관성 | spike 의 overall_qualitative 를 production 스키마에 복사 (no-overall 위반) | mitigate | test_no_overall_qualitative_copied 가 build_schema()['properties'] 에 overall_qualitative 0 단언 (MEDIUM-1) |
| T-20-05 | Tampering | 프롬프트 인젝션 (영상 메타/파일명) | mitigate | 파일명 ASCII temp copy 정화(spike 100), 프롬프트 고정 템플릿, 영상=데이터 |
| T-20-06 | Information disclosure | GEMINI_API_KEY 로그 노출 | mitigate | 키 절대 로그 금지(spike never prints), PII=video_hash 만(경로/원본 미로그) |
| T-20-07 | DoS / integrity | Gemini 실패 → silent gate bypass (위양성 통과) | mitigate | graceful None + WARNING 로그 (관측 가능). audit 필드 완성은 20-03 (visionVeto) |
| T-20-08 | Integrity | run 변동/프롬프트변경으로 같은 영상 다른(stale) severity (비결정론) | mitigate | temp 0 + 전용 VisionVetoCache 키(video_hash, model, PROMPT_VERSION, SCHEMA_VERSION, at_seconds_bucket). PROMPT/SCHEMA bump 시 stale 무효화. test_determinism_cache_same_key + test_prompt_version_invalidates_cache (MEDIUM-2) |
| T-20-SC | Tampering | npm/pip installs | accept | 신규 패키지 0 (google-genai 기존 production 사용 — Phase 5 이래). 20-RESEARCH Package Legitimacy Audit N/A |

신규 secret/endpoint 0 — 기존 Gemini 어댑터 경계 재사용.
</threat_model>

<verification>
- `cd backend && PYTHONPATH=shared/python python -m pytest tests/test_gemini_vision_scorer.py -x -q` GREEN (전부 mocked)
- `cd backend && PYTHONPATH=shared/python python -m pytest tests/ -q` 전체 회귀 0
- 객관성: build_schema()/dataclass 내성검사로 score/overall/rating/점수/overall_qualitative 0 (raw grep 아님 — MEDIUM-1)
- 결정론: 같은 키 hit 시 Gemini 호출 0 + PROMPT_VERSION 변경 시 무효화 (단위 검증, MEDIUM-2). 실 결정론(cache-warm byte-identity)은 20-04 Pod sweep
- Pod 무관 (mocked 단위테스트)
</verification>

<success_criteria>
- VisionVerdict/스키마 no-score + no-overall_qualitative + 내성검사 가드 (TRUST-08 객관성, MEDIUM-1)
- temp 0 + PROMPT_VERSION/SCHEMA_VERSION VisionVetoCache 결정론·무효화 (TRUST-06, MEDIUM-2)
- graceful None + WARNING (Pitfall 5), B4 no-redownload
- lazy-import Protocol 어댑터, 신규 패키지 0, 회귀 0
</success_criteria>

<output>
Create `.planning/phases/20-v2-gemini/20-02-SUMMARY.md` when done — 어댑터 severity enum 출력(score/overall 없음) + 내성검사 객관성 + PROMPT_VERSION/SCHEMA_VERSION VisionVetoCache 결정론 + 실 결정론은 20-04 Pod sweep 검증 명시.
</output>
