---
phase: 20-v2-gemini
plan: 03
type: execute
wave: 2
depends_on: [20-01, 20-02]
files_modified:
  - backend/functions/pipeline/app.py
  - backend/tests/test_pipeline_mode3.py
  - app/src/types/analysis.ts
  - backend/shared/python/sunity_shared/models.py
  - docs/contract.md
  - app/src/app/analysis/result.tsx
  - app/scripts/assert-result-score-suppression.mjs
autonomous: true
requirements: [SCORE-08, TRUST-06, TRUST-07, TRUST-08]
must_haves:
  truths:
    - "_apply_vision_veto 가 v1 identity(pass-through)에서 하향-전용 mutation 으로 전환된다 — capped < overallScore 일 때만 result['overallScore'] 를 낮추고 절대 안 올린다 (Mode1+Mode3 둘 다, 호출부 mode 분기 밖). cap-mutation 경로는 SEVERITY_CAP['major']=50 monkeypatch(임시 derived-cap fixture, production 캘리브레이션 아님)로 pod-free 증명되며 production cap 은 20-04 까지 placeholder None 유지 (iter2 HIGH-1 / D-02 무손상)"
    - "비전 veto 토글(_gemini_vision_veto_enabled)이 pipeline(app.py) 단독 소유다 (iter2 MEDIUM-1) — 어댑터(20-02)는 토글 미소유. ON 이면 기존 Phase17 vision 토글이 OFF 라도 keep_local_video 가 True 라 local_video_path 가 보존된다 — veto 가 local_video_path None 으로 무음 no-op 되지 않는다 (HIGH-1)"
    - "거부권 결과가 visionVeto audit 필드(status enum, severity, capApplied)로 직렬화된다 — status ∈ {applied, not_applicable, disabled, skipped_error, missing_local_video}. TS 는 discriminated union 으로 status='applied' 시 capApplied 컴파일-타임 강제 (iter2 non-blocking). visionVeto 부재가 'veto 가 돔'으로 읽히지 않고, terminal gate 가 status=applied 로 veto 실행을 증명할 수 있다 (HIGH-1 / 3-way lockstep)"
    - "Gemini/키 실패 시 v1 점수 graceful 통과 + WARNING 로그 + visionVeto.status=skipped_error 로 무음실패가 관측 가능하다 (silent no-op 차단, Pitfall 5)"
    - "Mode3 미보유(branch-3, is_reference_free) 시 isScoreSuppressed=True 면 점수카드 전체(OctagonScore + gradeBadge + score-derived summary + LevelBenchmark + scoreCaption + '점수를 확인해보세요' 헤더 카피)가 '기준 없음' state 로 대체된다 — octagon 만 숨기는 것은 D-08 미충족 (iter2 HIGH-2). dimensionScores/diagnostics 는 비권위 관측으로 남을 수 있음. **isScoreSuppressed 는 STRICTLY result.scoreSuppressed === true 단독 신호 — scoringBasis 폴백 금지 (iter3 HIGH-2)**"
    - "scoreSuppressed 가 visionVeto 와 동일하게 3-way 계약(analysis.ts + models.py + contract.md)에 lockstep 으로 박제된다 — 위치는 result.scoreSuppressed?(전체 결과 표시 제어) 채택. **scoreSuppressedReason(unheld | recognition_low_confidence)도 동일 3-way lockstep 으로 추가되며, scoreSuppressed=true 일 때 scoreSuppressedReason 는 REQUIRED 다 — discriminated suppression type(iter3 MEDIUM-1 / iter4 MEDIUM-1)**. scoringBasis 단독을 억제 신호로 쓰지 않는다(scoringBasis 는 source 라벨, suppression 은 display/trust 정책) (iter2 HIGH-3)"
    - "**Phase 20 result 가 reference_free_absolute scoringBasis 인데 scoreSuppressed 플래그가 누락되면 producer-contract FAILURE 로 fail-loud 처리된다 — UI 에서 scoringBasis 로 silent 추론하지 않는다 (iter3 HIGH-2 acceptance). 또한 scoreSuppressed=true 인데 scoreSuppressedReason 누락도 producer-contract FAILURE (iter4 MEDIUM-1)**"
    - "recognizer 가 같은 reference 에 cache-hit 으로 같은 category(=같은 line 차원 분류)를 반환해 run 간 line 차원 변동이 0 이다 — temp 0 단독 아닌 TechniqueCache 가 결정성 보장 (C-1 / D-06 / TRUST-06, pod-free 단위 검증)"
    - "**recognizer category 가 PROVENANCE 로 scoreSuppressedReason 을 결정한다 — 전용 resolver `_score_suppression_reason(profile, branch_info)` 가 (1) category=='low_confidence' → 'recognition_low_confidence' (motion_id=None 이 _SAFE_DEFAULT_BRANCH lookup 을 유발해도), (2) category=='unregistered' → 'unheld', (3) concrete branch metadata + unavailable basis → 'unheld'. _SAFE_DEFAULT_BRANCH(motion_id=None 유래)를 low_confidence 일 때 unheld 의 증거로 쓰지 않는다 (iter4 HIGH-1). 불일치는 한 분기 아래 숨기지 않고 정확히 하나의 structured 필드 `scoreSuppressionAudit {recognizerCategory, branchReferenceFree, resolvedReason}` 로 보고 — log.warning '또는' 대안 폐기, log 는 additive only (A2 '반드시 일치' → '명시 reason 으로 reconcile', iter5 MEDIUM-2)**"
    - "**suppression reason 이 모든 visible suppressed-state 텍스트(헤더 카피 AND scoringBasisLabel)를 소유한다 — recognition_low_confidence 억제 결과의 scoringBasisLabel 은 reference-free '기준 동작 없음'/'기준 없음' 을 절대 포함하지 않는다. scoringBasisLabel 은 suppression reason 이 존재하면 scoringBasis 단독에서 파생되지 않는다. low_confidence 결과가 동시에 '동작 인식 신뢰도 낮음' + '기준 동작 없음' 을 보이지 않는다 (iter5 HIGH-2)**"
  artifacts:
    - path: "backend/functions/pipeline/app.py"
      provides: "_gemini_vision_veto_enabled 토글(pipeline 단독 소유 — iter2 MEDIUM-1) + keep_local_video 게이트 확장(HIGH-1) + _apply_vision_veto 본체 swap (adapter 호출 + apply_downward_cap + visionVeto status enum audit + graceful 관측) + profile 인자 추가 + 단일 호출부 갱신 + Mode3 branch-3 억제(scoreSuppressed + scoreSuppressedReason) + 전용 resolver _score_suppression_reason(iter4 HIGH-1) + 단일 structured scoreSuppressionAudit reconcile 필드(iter5 MEDIUM-2)"
      contains: "_score_suppression_reason"
    - path: "app/src/types/analysis.ts"
      provides: "visionVeto discriminated union audit 필드(status='applied'→capApplied 강제) + ScoreSuppression discriminated type(scoreSuppressed:true→scoreSuppressedReason REQUIRED — iter4 MEDIUM-1) + AnalysisDoc/result 연결"
      contains: "scoreSuppressedReason"
    - path: "backend/shared/python/sunity_shared/models.py"
      provides: "VISION_VETO_STATUSES enum 상수 + visionVeto 키 + scoreSuppressed 키 + SCORE_SUPPRESSED_REASONS enum 상수(unheld | recognition_low_confidence) + scoreSuppressionAudit 키 상수(recognizerCategory/branchReferenceFree/resolvedReason — iter5 MEDIUM-2) + scoreSuppressed=true→scoreSuppressedReason REQUIRED 불변식 주석 (3-way lockstep — iter2 HIGH-3 / iter3 MEDIUM-1 / iter4 MEDIUM-1)"
      contains: "scoreSuppressedReason"
    - path: "docs/contract.md"
      provides: "visionVeto {status enum, severity?, capApplied?} + result.scoreSuppressed? + result.scoreSuppressedReason? enum 정의 + scoreSuppressionAudit {recognizerCategory, branchReferenceFree, resolvedReason} 정의(iter5 MEDIUM-2) + scoreSuppressed=true→scoreSuppressedReason REQUIRED 불변식(3-way lockstep — iter2 HIGH-3 / iter3 MEDIUM-1 / iter4 MEDIUM-1)"
      contains: "scoreSuppressedReason"
    - path: "app/src/app/analysis/result.tsx"
      provides: "isScoreSuppressed 단일 boolean(STRICTLY result.scoreSuppressed===true, scoringBasis 폴백 0 — iter3 HIGH-2) → Mode3 미보유 시 점수카드 전체('기준 없음' state) 대체 (OctagonScore + gradeBadge + summary + LevelBenchmark + scoreCaption + 헤더 카피 모두 미렌더 — iter2 HIGH-2) + scoreSuppressedReason 이 헤더 카피 AND scoringBasisLabel 둘 다 소유(reason-owns-copy: unheld vs recognition_low_confidence — iter3 MEDIUM-1, recognition_low_confidence 는 '기준 동작 없음' 미노출 — iter5 HIGH-2, reason 누락 default '기준 없음' 카피 금지 — iter4 MEDIUM-1)"
      contains: "isScoreSuppressed"
    - path: "app/scripts/assert-result-score-suppression.mjs"
      provides: "pod-free 정적 단언 스크립트(앱에 test runner 부재 — iter3 HIGH-1). cwd-stable(import.meta.url 로 result.tsx 경로 해석 — iter4 MEDIUM-2) + --self-test(guarded sample PASS / unguarded OctagonScore·header sample FAIL / low-confidence 분기에 legacy '기준 동작 없음' 라벨 렌더 sample FAIL — iter4 MEDIUM-2 / iter5 HIGH-2). result.tsx 소스를 파싱해 isScoreSuppressed 가 점수카드 전체(OctagonScore/gradeBadge/LevelBenchmark/scoreCaption/score-derived summary/'점수를 확인해보세요' 헤더 카피)를 비억제 분기 아래 두는지 + recognition_low_confidence 분기에서 scoringBasisLabel 이 reference-free 라벨로 떨어지지 않는지 단언 — 하나라도 비조건부/leak 이면 non-zero exit"
      contains: "self-test"
  key_links:
    - from: "pipeline keep_local_video gate"
      to: "_gemini_vision_veto_enabled() (pipeline 단독 소유 — iter2 MEDIUM-1)"
      via: "keep_local_video = _gemini_enabled() or _gemini_vision_enabled() or _gemini_vision_veto_enabled() (HIGH-1)"
      pattern: "_gemini_vision_veto_enabled"
    - from: "pipeline._apply_vision_veto"
      to: "vision_veto.apply_downward_cap + gemini_vision_scorer.assess_fault_severity"
      via: "worst_pose_timestamp(profile) → assess severity → apply_downward_cap(overallScore) → min cap + visionVeto status audit (cap-mutation 은 monkeypatch SEVERITY_CAP['major']=50 으로 증명 — iter2 HIGH-1)"
      pattern: "apply_downward_cap"
    - from: "pipeline visionVeto + scoreSuppressed + scoreSuppressedReason + scoreSuppressionAudit"
      to: "analysis.ts + models.py + contract.md (3-way lockstep — visionVeto AND scoreSuppressed AND scoreSuppressedReason AND scoreSuppressionAudit)"
      via: "optional visionVeto {status, severity, capApplied} (discriminated union) + ScoreSuppression discriminated type(scoreSuppressed:true→scoreSuppressedReason REQUIRED) + scoreSuppressionAudit {recognizerCategory, branchReferenceFree, resolvedReason} 직렬화 (iter2 HIGH-3 / iter3 MEDIUM-1 / iter4 MEDIUM-1 / iter5 MEDIUM-2)"
      pattern: "scoreSuppressedReason"
    - from: "pipeline._score_suppression_reason(profile, branch_info) (iter4 HIGH-1 전용 resolver)"
      to: "scoreSuppressedReason emit + scoreSuppressionAudit structured 필드 (provenance 우선순위: low_confidence → recognition_low_confidence, unregistered/concrete-unheld → unheld)"
      via: "recognizer category 를 PROVENANCE 로 — _SAFE_DEFAULT_BRANCH(motion_id=None 유래)를 low_confidence 일 때 unheld 증거로 쓰지 않음. 불일치는 단일 structured 필드로 보고(log 대안 폐기 — iter5 MEDIUM-2)"
      pattern: "_score_suppression_reason"
    - from: "result.tsx isScoreSuppressed (STRICTLY scoreSuppressed===true — iter3 HIGH-2)"
      to: "점수카드 전체 대체 + scoreSuppressedReason 이 헤더 AND scoringBasisLabel 소유(reason-owns-copy) + assert-result-score-suppression.mjs 정적 단언(cwd-stable + self-test — iter4 MEDIUM-2 / iter5 HIGH-2)"
      via: "scoreSuppressed===true → 점수카드(octagon+grade+summary+benchmark+caption+헤더) '기준 없음'으로 대체 + recognition_low_confidence 는 scoringBasisLabel 이 '기준 동작 없음' 미노출, mjs 가 정적으로 강제 (iter2 HIGH-2 / iter3 HIGH-1 / iter4 MEDIUM-2 / iter5 HIGH-2)"
      pattern: "isScoreSuppressed"
---

<objective>
20-01 의 순수 cap 코어 + 20-02 의 Gemini 어댑터를 **채점 path 에 통합**한다 — Phase 19 가 남긴 `_apply_vision_veto` identity hook 의 본체를 하향-전용 mutation 으로 swap. **pod-free**(adapter 는 mocked 단위테스트, cap 은 monkeypatch). Open Q2(visionVeto audit 필드) = 채택, status enum + discriminated union 으로 강화, 3-way lockstep(visionVeto AND scoreSuppressed AND scoreSuppressedReason AND scoreSuppressionAudit). Open Q4(Mode3 branch-3 억제) = 점수카드 전체 억제로 완성.

- **iter2 MEDIUM-1 (토글 ownership = pipeline 단독):** `_gemini_vision_veto_enabled()` 는 **pipeline(app.py)에만** 정의/사용 — 20-02 어댑터는 토글 미소유(어댑터는 키/캐시/응답만 게이트). 어댑터가 토글을 복제하면 drift → no-op 버그 재발. `_apply_vision_veto` 가 **유일한 feature-toggle 게이트**.
- **HIGH-1 (veto no-op 차단):** 신규 `_gemini_vision_veto_enabled()` 토글을 **추출 보존 게이트(app.py:1833)에 포함** — `keep_local_video = _gemini_enabled() or _gemini_vision_enabled() or _gemini_vision_veto_enabled()`. 기존 Phase17 vision 토글이 OFF 라도 veto 만 ON 이면 local_video_path 가 보존돼 veto 가 None 으로 무음 no-op 되지 않는다. pod-free 테스트로 (Phase17 OFF + veto ON → adapter 가 non-None local_video_path 수신) 단언.
- **iter2 HIGH-1 (cap-mutation 증명 — production cap 은 placeholder 유지):** 20-01 의 SEVERITY_CAP 은 production 에서 {minor:None, moderate:None, major:None} 로 20-04 까지 placeholder(provenance fail-closed). 그러나 cap-mutation 경로(_apply_vision_veto 가 overallScore 를 실제로 낮추는지)는 20-04 이전에 **증명**돼야 한다 — pipeline cap-mutation 테스트에서 `monkeypatch.setitem(vision_veto.SEVERITY_CAP, "major", 50)` 로 **임시 derived-cap fixture**(그 테스트에만 scoped, production 캘리브레이션 아님)를 주입해 severity='major' + overall=100 → capped=50 하향을 pod-free 로 증명. production caps 는 20-04 가 채울 때까지 None 유지 (D-02 무손상).
- **SCORE-08 (D-01/03/04/05 통합):** `_apply_vision_veto(score_result, local_video_path, angles, profile)` 본체 = worst_pose_timestamp(profile) → assess_fault_severity(local_video, at_seconds) → apply_downward_cap(score_result['overallScore'], severity) → capped < overall 일 때만 `min` 으로 낮춤. 호출부(app.py:2226)는 mode 분기 **밖** → Mode1+Mode3 둘 다 자동(D-03/04). 평균 금지(D-05 — worst-pose 단일).
- **TRUST-08 (status enum audit + 무음실패 방지 — HIGH-1):** veto 결과를 `visionVeto: {status, severity, capApplied}` 로 직렬화(3-way lockstep). **status ∈ {applied, not_applicable, disabled, skipped_error, missing_local_video}** — 단순 applied/absent 가 아니라 명시 enum. visionVeto 부재가 'veto 가 돔'으로 읽히지 않고, terminal gate(20-04)가 **status=applied 로 veto 실행을 증명**한다. 어댑터 실패 → v1 graceful 통과 + WARNING + status=skipped_error; local_video_path None → status=missing_local_video; veto OFF → status=disabled; cap 미적용(minor/None) → status=not_applicable. 기존 identity 테스트(`out is score_result`)를 하향-전용 + status property 테스트로 **전환**.
- **TRUST-07 (Mode3 미보유 억제 — iter2 HIGH-2/HIGH-3 + iter3 HIGH-2/MEDIUM-1 + iter4 HIGH-1/MEDIUM-1 + iter5 HIGH-2/MEDIUM-2):** Phase 19 가 라벨만 달았던 branch-3(is_reference_free) 에 **점수카드 전체 억제** 추가 — octagon 만이 아니라 score-derived UI 전부(gradeBadge, summary, LevelBenchmark, scoreCaption, '점수를 확인해보세요' 헤더)를 '기준 없음' state 로 대체. 억제 신호 `scoreSuppressed` 는 visionVeto 와 동일한 3-way 계약 lockstep. **iter3 HIGH-2: 프런트 `isScoreSuppressed` 는 STRICTLY `result.scoreSuppressed === true` 단독 신호 — scoringBasis 폴백 금지(scoringBasis 는 source 라벨, suppression 은 display/trust 정책). backend 가 reference_free_absolute 인데 scoreSuppressed 누락 시 producer-contract FAILURE 로 fail-loud(UI 에서 scoringBasis 로 silent 추론 0). iter3 MEDIUM-1: low_confidence 를 '기준 없음' 과 분리 — scoreSuppressedReason(unheld vs recognition_low_confidence) 추가.** **iter4 HIGH-1: reason 매핑은 전용 resolver `_score_suppression_reason(profile, branch_info)` 가 담당하고 recognizer category 를 PROVENANCE 로 본다 — category=='low_confidence' 면 motion_id=None 이 _SAFE_DEFAULT_BRANCH lookup(=is_reference_free True)을 유발해도 'recognition_low_confidence' 가 우선이며 절대 unheld 로 collapse 되지 않는다. _SAFE_DEFAULT_BRANCH(motion_id=None 유래)는 low_confidence 일 때 unheld 의 증거가 아니다("모르니까 안전 기본" ≠ "확정 미보유"). iter4 MEDIUM-1: scoreSuppressed=true 면 scoreSuppressedReason 는 REQUIRED(discriminated suppression type) — reason 누락도 producer-contract FAILURE, UI 는 reason 누락 시 default '기준 없음' 카피 금지.** **iter5 HIGH-2 (reason-owns-copy): suppression reason 이 모든 visible suppressed-state 카피를 소유한다 — 헤더뿐 아니라 scoringBasisLabel 도. recognition_low_confidence 억제 결과의 scoringBasisLabel 은 reference-free '기준 동작 없음'/'기준 없음' 을 절대 포함하지 않는다(같은 화면에 '신뢰도 낮음' + '기준 동작 없음' 동시 표시 차단). scoringBasisLabel 은 suppression reason 이 존재하면 scoringBasis 단독에서 파생되지 않는다. iter5 MEDIUM-2: A2 reconcile 불일치는 정확히 하나의 structured 필드 `scoreSuppressionAudit {recognizerCategory, branchReferenceFree, resolvedReason}` 로 보고 — log.warning '또는' 대안 폐기(log 는 additive only).** A2 는 '반드시 일치' 에서 '명시 reason 으로 reconcile' 로 전환 — 불일치는 단일 structured audit 필드로 보고.
- **iter3 HIGH-1 / iter4 MEDIUM-2 / iter5 HIGH-2 (앱 render test 자동화 — test runner 부재 + cwd-stable + self-test + reason-leak 가드):** 앱(`app/package.json`)에 `test` 스크립트/Jest/RN 테스트 스택이 **없다.** mid-phase 에 도입하지 않는다. RN render 테스트 대신 **pod-free 정적 단언 스크립트** `app/scripts/assert-result-score-suppression.mjs` 를 추가 — result.tsx 소스를 읽어 `isScoreSuppressed` 가 점수카드 전체(OctagonScore/gradeBadge/LevelBenchmark/scoreCaption/score-derived summary/'점수를 확인해보세요' 헤더 카피)를 비억제 분기 아래에만 두는지 + recognition_low_confidence 분기가 scoringBasisLabel 을 reference-free '기준 동작 없음' 라벨로 떨어뜨리지 않는지(iter5 HIGH-2) 정적 단언(하나라도 비조건부 렌더/leak 이면 non-zero exit). **iter4 MEDIUM-2: 스크립트는 cwd-independent — result.tsx 경로를 `new URL('../src/app/analysis/result.tsx', import.meta.url)` 로 해석하고, 모든 verify 커맨드를 `cd app && node scripts/assert-result-score-suppression.mjs` 로 표준화. `--self-test` 플래그로 in-memory fixture 자체검증(guarded sample PASS / unguarded OctagonScore sample FAIL / unguarded header copy sample FAIL / low-confidence 분기에 '기준 동작 없음' leak sample FAIL).** verify 에 **실제 실행 커맨드** `cd app && node scripts/assert-result-score-suppression.mjs --self-test && node scripts/assert-result-score-suppression.mjs && npm run typecheck` 명시(false-green 차단).

Purpose: 위양성(kip-up 100/100)을 채점 path 에서 죽이되 v1 변별·정타는 안 건드리고, 미보유 confident 97 을 **점수카드 통째로** 억제해 신뢰 회복. veto 가 **항상 실 영상으로 돌고**(HIGH-1) status enum 으로 실행이 증명되며, cap-mutation 경로가 pod-free monkeypatch 로 증명되고(iter2 HIGH-1), 억제 신호가 STRICTLY explicit flag 로 fail-loud(iter3 HIGH-2)이며, low_confidence 가 전용 resolver 로 unheld 로 collapse 되지 않고(iter4 HIGH-1), reason 이 suppressed 시 강제되며(iter4 MEDIUM-1), **suppression reason 이 scoringBasisLabel 까지 소유해 두 번째 UI 필드를 통한 reason leak 이 차단되고(iter5 HIGH-2), reconcile 불일치가 단일 structured audit 필드로 관측 가능하며(iter5 MEDIUM-2)**, 억제 자동검증이 **실제로 돌아가는** cwd-stable + self-test 정적 스크립트(iter3 HIGH-1 / iter4 MEDIUM-2)로 false-red/green 이 차단된다.
Output: app.py(_gemini_vision_veto_enabled pipeline 소유 + keep_local_video 게이트 + _apply_vision_veto 본체 + profile 인자 + 호출부 + branch-3 scoreSuppressed/scoreSuppressedReason + _score_suppression_reason resolver + scoreSuppressionAudit 단일 structured 필드), test_pipeline_mode3.py(identity→하향전용+status 전환 + keep_local_video + cap-mutation monkeypatch + audit + resolver provenance + missing-flag/missing-reason fail-loud + low_confidence scoringBasisLabel not-unheld), 3-way 계약(analysis.ts/models.py/contract.md visionVeto discriminated union + ScoreSuppression discriminated type + scoreSuppressionAudit), result.tsx(점수카드 전체 억제 UX + reason-owns-copy 헤더 AND scoringBasisLabel), assert-result-score-suppression.mjs(cwd-stable + self-test + reason-leak 가드 정적 억제 단언 — 실 실행).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/20-v2-gemini/20-CONTEXT.md
@.planning/phases/20-v2-gemini/20-RESEARCH.md
@.planning/phases/20-v2-gemini/20-VALIDATION.md
@backend/functions/pipeline/app.py
@backend/shared/python/sunity_shared/analysis/assemble.py
@backend/shared/python/sunity_shared/analysis/gemini_technique_recognizer.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 토글 pipeline 소유(MEDIUM-1) + keep_local_video 게이트 확장(HIGH-1) + _apply_vision_veto 본체 swap — adapter 호출 + apply_downward_cap + visionVeto status enum audit + profile 인자 + 호출부 갱신 + graceful 관측 + cap-mutation monkeypatch 증명(iter2 HIGH-1) (identity→하향전용 전환)</name>
  <files>backend/functions/pipeline/app.py, backend/tests/test_pipeline_mode3.py</files>
  <behavior>
    - test_keep_local_video_for_veto_only (HIGH-1): 기존 Phase17 vision 토글(_gemini_enabled / _gemini_vision_enabled) OFF + `GEMINI_VISION_VETO_ENABLED=1` ON → keep_local_video True 라 추출이 local 영상을 보존하고, _apply_vision_veto 가 adapter(assess_fault_severity)에 **non-None local_video_path** 를 전달한다(mock 으로 호출 인자 검사). veto 가 None 으로 무음 no-op 되지 않음.
    - test_toggle_owned_by_pipeline (iter2 MEDIUM-1): `_gemini_vision_veto_enabled` 가 app.py(pipeline)에 정의됨 + 어댑터 모듈(gemini_vision_scorer)에는 정의 0. _apply_vision_veto 가 유일한 feature-toggle 게이트(어댑터 assess_fault_severity 는 env 미참조 — 20-02 와 정합). grep: app.py 에 `def _gemini_vision_veto_enabled` 1건, gemini_vision_scorer 에 0건.
    - test_vision_veto_downward_only_with_cap (iter2 HIGH-1 — cap monkeypatch): **`monkeypatch.setitem(vision_veto.SEVERITY_CAP, "major", 50)`** (임시 derived-cap fixture, production 캘리브레이션 아님 — production cap 은 20-04 까지 None). adapter mock 이 severity='major' 반환 + overallScore=100 → out['overallScore'] == 50 (capped 하향 증명). severity='minor'/None → out['overallScore'] == 입력(불변). **어떤 경우도 overallScore 가 입력보다 크지 않다** (property — 올림 0). 테스트 docstring 에 "production cap 은 20-04 까지 placeholder None — 여기 50 은 mutation 경로 증명용 scoped fixture (D-02 무손상)" 명시.
    - test_vision_veto_placeholder_cap_no_mutation (iter2 HIGH-1 보강): SEVERITY_CAP['major']=None(production placeholder, monkeypatch 없음) + severity='major' → out['overallScore'] == 입력(불변) + visionVeto.status == 'not_applicable'. 즉 20-04 전에는 cap 미적용이 정상(provenance fail-closed 와 정합).
    - test_vision_veto_status_enum (HIGH-1): (major cap monkeypatch 하) cap 적용 시 out['visionVeto']['status'] == 'applied' + {severity, capApplied}. cap 미적용(minor/None/placeholder) → status == 'not_applicable'. veto OFF → status == 'disabled'. local_video_path None → status == 'missing_local_video'. adapter None(실패) → status == 'skipped_error'. status 가 veto 실행 여부를 명시 증명(부재 ≠ 실행).
    - test_vision_veto_graceful_observable: adapter 가 None(키부재/실패) → out['overallScore'] == v1 점수(불변, graceful) + WARNING 로그 1회 + visionVeto.status == 'skipped_error' (무음실패 관측 가능 — Pitfall 5).
    - test_vision_veto_called_both_modes: 호출부(app.py:2226)가 mode 분기 밖이라 Mode1(MODE_EXPERT) + Mode3(MODE_SELF) result 둘 다 _apply_vision_veto 통과 (D-03/04). profile 인자 전달 확인.
    - test_vision_veto_off_passthrough: _gemini_vision_veto_enabled() OFF → out['overallScore'] 불변 + adapter 미호출 + status == 'disabled'.
  </behavior>
  <read_first>
    - backend/functions/pipeline/app.py:1631-1661 (_apply_vision_veto 현재 def = v1 identity, docstring 1636-1655 = 'overallScore'/'dimensionScores' 키 박제 + graceful try/except 1656-1660, _gemini_vision_enabled 220-244, _VISION_ENV_DEFAULTS 209) + :2226 (단일 호출부 `result = _apply_vision_veto(result, local_video_path, angles)` — profile 인자 추가 대상; 이 줄이 mode 분기 밖임을 확인) + :2207 build_result 호출(result 산출) + **:1833 keep_local_video=_gemini_enabled() or _gemini_vision_enabled() (HIGH-1 게이트 확장 대상 — _gemini_vision_veto_enabled() 추가)** + recognize/profile 변수 위치(~1730)
    - backend/shared/python/sunity_shared/analysis/vision_veto.py (20-01 산출 — apply_downward_cap + worst_pose_timestamp + SEVERITY_CAP(production None placeholder) + SEVERITY_CAP_PROVENANCE)
    - backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py (20-02 산출 — assess_fault_severity → VisionVerdict | None, lazy-import. **iter2 MEDIUM-1: 어댑터는 _gemini_vision_veto_enabled 정의/사용 0 — 토글은 pipeline 소유. 정합 확인**)
    - 20-DIRECT-REVIEW.md HIGH-1 (keep_local_video 게이트 + visionVeto status enum + missing_local_video 명시)
    - 20-DIRECT-REVIEW-ITERATION2.md HIGH-1 (cap-mutation monkeypatch SEVERITY_CAP['major']=50, production placeholder 유지) + MEDIUM-1 (토글 pipeline 소유, 어댑터 import 금지)
    - 20-RESEARCH.md §"Code Examples: Adapter call inside _apply_vision_veto" (본체 swap 예시 — profile 인자, visionVeto audit, graceful) + §"Pitfall 1"(averaging 금지 — terminal min 만) + §"Pitfall 5"(무음실패 관측) + §"State of the Art"(out is score_result → downward property 전환)
    - 20-CONTEXT.md D-01/03/04/05 + 객관성(score 필드 0 — visionVeto 는 status/severity/capApplied 만, 점수 라벨 아님)
    - backend/tests/test_pipeline_mode3.py:229-246 (현재 test_vision_hook_passthrough = `out is score_result` identity 단언 → 하향-전용 + status property 로 전환 대상; _apply_vision_veto getattr 가드 231-232)
    - 20-VALIDATION.md V-6(하향전용 invariant) / V-9(무음실패 audit status) / V-10(veto no-op 차단/status enum). **모든 backend 커맨드는 `cd backend && PYTHONPATH=shared/python ...` 단일 shape (iter5 MEDIUM-1)**
  </read_first>
  <action>
    **_gemini_vision_veto_enabled() 토글 (HIGH-1 + iter2 MEDIUM-1 — pipeline 단독 소유):** _VISION_ENV_DEFAULTS 패턴으로 신규 토글 추가(env `GEMINI_VISION_VETO_ENABLED`, default 결정은 belle 운영 — 20-RESEARCH Runtime State 정합). **토글은 pipeline(app.py)에만 정의/사용 — 20-02 어댑터는 토글 미소유(중복 정의 금지, 어댑터는 키/캐시/응답만 게이트).** pipeline 이 keep_local_video 게이트와 _apply_vision_veto OFF 분기에서 사용. `_apply_vision_veto` 가 **유일한 feature-toggle 게이트**. 주석에 "iter2 MEDIUM-1 — 토글 pipeline 단독 소유, 어댑터 복제 금지(drift 방지)" 박제.
    **keep_local_video 게이트 확장 (app.py:1833, HIGH-1):** `keep_local_video = _gemini_enabled() or _gemini_vision_enabled() or _gemini_vision_veto_enabled()` 로 변경 — veto 만 ON 인 경우에도 local_video_path 가 보존됨. 주석에 "HIGH-1 — veto 가 local_video_path None 으로 무음 no-op 되지 않도록 보존 게이트에 veto 토글 포함" 박제.
    **_apply_vision_veto 본체 swap (app.py:1631):** 시그니처에 `profile: technique.TechniqueProfile | None = None` 추가. 본체를:
      (1) `not _gemini_vision_veto_enabled()` → `return _veto_passthrough(score_result, status="disabled")` (OFF 시 adapter 미호출). `local_video_path is None` → `return _veto_passthrough(score_result, status="missing_local_video")` (graceful, HIGH-1 — 부재 신호 명시).
      (2) `from sunity_shared.analysis import vision_veto, gemini_vision_scorer` lazy(또는 _ensure_adapters 패턴). `at = vision_veto.worst_pose_timestamp(profile)`. `verdict = gemini_vision_scorer.assess_fault_severity(local_video_path, at_seconds=at)`.
      (3) verdict None(실패/키부재) → `log.warning("vision veto 미실행 — verdict None, v1 점수 통과 (graceful, 무음실패 관측)")` + `return _veto_passthrough(score_result, status="skipped_error")` (Pitfall 5 관측 가능).
      (4) `capped = vision_veto.apply_downward_cap(score_result["overallScore"], verdict.severity)`. `if capped < score_result["overallScore"]:` → `score_result = {**score_result, "overallScore": capped, "visionVeto": {"status": "applied", "severity": verdict.severity, "capApplied": capped}}` (audit 직렬화). 아니면 status="not_applicable" 직렬화(점수 불변 — production placeholder cap=None 일 때 항상 이 경로, iter2 HIGH-1 보강과 정합).
      (5) `return score_result`. **올림 경로 절대 0** (apply_downward_cap 이 min 만). **평균 금지** — dimension_scores 재집계 없이 terminal cap 만(Pitfall 1). graceful boundary(except → WARNING + status='skipped_error' 반환) 유지. status enum 직렬화 헬퍼(`_veto_passthrough(result, status)`)로 5개 status 일관 박제. docstring 을 "v2 = 하향-전용 mutation (capped < overall 시에만 낮춤, 절대 안 올림) + visionVeto.status 가 veto 실행을 증명(부재≠실행). production SEVERITY_CAP 은 20-04 까지 placeholder None 라 cap 적용은 not_applicable — mutation 경로는 테스트가 monkeypatch 로 증명. v1 identity 계약은 의도적 전환(20-03)" 로 갱신.
    **호출부 갱신 (app.py:2226):** `result = _apply_vision_veto(result, local_video_path, angles, profile)` 로 profile 전달(A4 — 단일 호출부, grep 으로 다른 호출부 0 확인). 이 줄이 MODE_EXPERT/MODE_SELF 분기 **밖**이라 Mode1+Mode3 둘 다 적용됨을 주석으로 박제.
    **테스트 전환 (test_pipeline_mode3.py):** 기존 test_vision_hook_passthrough(229-246) 의 `out is score_result` identity 단언을 **삭제·전환** → behavior 8 케이스로 교체(adapter mock 으로 severity 주입, env monkeypatch 로 토글 제어, **SEVERITY_CAP monkeypatch 로 cap-mutation 증명**). 이는 "무관한 케이스 회귀 0" 예외가 아니라 **v2 계약 전환에 직접 영향받는 케이스의 정당한 전환**(20-RESEARCH State of the Art: identity→downward property 명시). adapter/_gemini_vision_scorer 는 monkeypatch(실 Gemini 미호출). test_keep_local_video_for_veto_only 는 추출 보존 경로(1833)와 adapter 호출 인자(non-None path)를 함께 단언. **test_vision_veto_downward_only_with_cap 은 `monkeypatch.setitem(vision_veto.SEVERITY_CAP, "major", 50)` 로 scoped 임시 cap 주입 — production cap=None 무손상(iter2 HIGH-1)**. test_vision_veto_placeholder_cap_no_mutation 은 monkeypatch 없이 cap=None → not_applicable 단언. test_toggle_owned_by_pipeline 은 app.py/gemini_vision_scorer 소스에서 토글 정의 위치 단언.
    from __future__ import annotations, snake_case, 한국어 docstring(why), 이모지 금지, graceful BLE001 패턴 박제.
  </action>
  <verify>
    <automated>cd backend && PYTHONPATH=shared/python python -m pytest tests/test_pipeline_mode3.py -k "vision_veto or keep_local_video or toggle_owned" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_pipeline_mode3.py vision_veto + keep_local_video + toggle_owned 케이스 8개 GREEN (전부 mocked adapter — 실 Gemini/Pod 미호출)
    - Phase17 vision OFF + veto ON → keep_local_video True + adapter 가 non-None local_video_path 수신 (test_keep_local_video_for_veto_only GREEN — HIGH-1; grep: app.py:1833 게이트에 `_gemini_vision_veto_enabled()` 포함)
    - **토글 pipeline 단독 소유: app.py 에 `def _gemini_vision_veto_enabled` 1건 + gemini_vision_scorer 에 0건 (test_toggle_owned_by_pipeline GREEN — iter2 MEDIUM-1); _apply_vision_veto 가 유일 feature-toggle 게이트**
    - **cap-mutation 증명: SEVERITY_CAP['major']=50 monkeypatch(scoped 임시 fixture) 하 severity='major'+overall=100 → out['overallScore']==50 (test_vision_veto_downward_only_with_cap GREEN — iter2 HIGH-1); 테스트 docstring 에 "production cap 은 20-04 까지 None — 50 은 mutation 증명용 scoped fixture, D-02 무손상" 명시. cap 적용 시에도 절대 안 올림 (grep: _apply_vision_veto 본체에 `max(`/가중블렌드 0건, apply_downward_cap 경유 min 만)**
    - **production placeholder cap=None(monkeypatch 없음) + severity='major' → overallScore 불변 + status='not_applicable' (test_vision_veto_placeholder_cap_no_mutation GREEN — iter2 HIGH-1; 20-04 전 cap 미적용이 정상, provenance fail-closed 정합)**
    - visionVeto.status enum ∈ {applied, not_applicable, disabled, skipped_error, missing_local_video} 직렬화 — 부재≠실행 (test_vision_veto_status_enum GREEN — HIGH-1). visionVeto 에 score/점수 필드 0 (status/severity enum + capApplied 정수만 — 객관성)
    - adapter None → v1 graceful 통과 + WARNING + visionVeto.status='skipped_error' (test_vision_veto_graceful_observable GREEN — Pitfall 5 무음실패 관측)
    - 호출부 단일(app.py:2226) + mode 분기 밖 → Mode1+Mode3 둘 다 통과, profile 전달 (test_vision_veto_called_both_modes GREEN — grep: `_apply_vision_veto(` 호출 1건 + profile 인자)
    - OFF 토글 시 adapter 미호출 + 불변 + status='disabled' (test_vision_veto_off_passthrough GREEN)
    - 무관한 backend suite 회귀 0 (pre-existing 격리 제외)
  </acceptance_criteria>
  <done>토글 pipeline 단독 소유(iter2 MEDIUM-1) + keep_local_video 게이트에 _gemini_vision_veto_enabled 포함(HIGH-1 — veto no-op 차단) + _apply_vision_veto v1 identity → 하향-전용 mutation 전환 (apply_downward_cap min cap + visionVeto status enum audit + worst_pose profile + graceful 관측 + 단일 호출부 Mode1/Mode3). cap-mutation 경로 monkeypatch SEVERITY_CAP['major']=50 으로 증명, production cap=None 무손상(iter2 HIGH-1). 올림 0, 평균 0. 8 mocked 테스트 GREEN. 회귀 0.</done>
</task>

<task type="auto">
  <name>Task 2: visionVeto(discriminated union) + ScoreSuppression discriminated type(scoreSuppressed=true→scoreSuppressedReason REQUIRED — iter4 MEDIUM-1) + scoreSuppressionAudit structured 필드(iter5 MEDIUM-2) 3-way 계약 lockstep (analysis.ts + models.py + contract.md) + Firestore flat 저장 검증 (iter2 HIGH-3 / iter3 MEDIUM-1 / iter4 MEDIUM-1 / iter5 MEDIUM-2)</name>
  <files>app/src/types/analysis.ts, backend/shared/python/sunity_shared/models.py, docs/contract.md</files>
  <read_first>
    - app/src/types/analysis.ts (AnalysisDoc.result 구조 + overallScore 위치 + scoringBasis Mode1/Mode3 239-272 패턴 = optional audit 필드 추가 컨벤션, import type, 문자열 union 컨벤션)
    - backend/shared/python/sunity_shared/models.py (SCORE_DIMENSIONS 20, MODE3_SCORING_BASES 62, comparison 키 상수/주석 패턴 — visionVeto status enum 상수 + scoreSuppressed + scoreSuppressedReason + scoreSuppressionAudit 키 추가 위치)
    - docs/contract.md:161 (overallScore 정의 = min-of-core) + :246-272 (Mode1/Mode3 Comparison optional 필드 패턴 — visionVeto/scoreSuppressed/scoreSuppressedReason/scoreSuppressionAudit 추가 위치) + result 구조 섹션
    - 20-DIRECT-REVIEW.md HIGH-1 (visionVeto status enum {applied, not_applicable, disabled, skipped_error, missing_local_video} — 3-way 계약 변경)
    - 20-DIRECT-REVIEW-ITERATION2.md HIGH-3 (scoreSuppressed 3-way lockstep, result.scoreSuppressed? 위치) + Non-Blocking(visionVeto discriminated union)
    - 20-DIRECT-REVIEW-ITERATION3.md MEDIUM-1 (scoreSuppressedReason: 'unheld' | 'recognition_low_confidence' — 3-way lockstep, low_confidence 를 '기준 없음' 과 분리)
    - 20-DIRECT-REVIEW-ITERATION4.md MEDIUM-1 (ScoreSuppression discriminated type: scoreSuppressed=true→scoreSuppressedReason REQUIRED, scoreSuppressed=false→reason never. producer-validation: suppressed without reason FAIL, not-suppressed with reason FAIL)
    - **20-DIRECT-REVIEW-ITERATION5.md MEDIUM-2 (scoreSuppressionAudit = 단일 필수 structured 필드 {recognizerCategory, branchReferenceFree, resolvedReason} — log.warning '또는' 대안 폐기)**
    - 20-RESEARCH.md §"Runtime State Inventory"(visionVeto {status, severity, capApplied} optional, 3-way lockstep 필요) + §"Open Questions Q2"(audit 필드 채택 권장)
    - backend/shared/python/sunity_shared/firestore_admin.py (complete_analysis flat 저장 + nested-array 금지 — visionVeto 는 scalar dict {status:str, severity:str, capApplied:int} + scoreSuppressed 는 bool + scoreSuppressedReason 는 str + scoreSuppressionAudit 는 scalar dict {recognizerCategory:str, branchReferenceFree:bool, resolvedReason:str} 이라 flat 정합; 검증 validator 패턴)
    - 20-CONTEXT.md 객관성(visionVeto severity/capApplied = 임계 수치 라벨링 OK, 사람 점수 라벨 아님) + D-08(미보유 confident 97 금지)
  </read_first>
  <action>
    **3-way lockstep #1 — visionVeto status enum audit 필드 (discriminated union — iter2 non-blocking):**
    - app/src/types/analysis.ts: AnalysisDoc 의 result(또는 overallScore 인접)에 visionVeto 를 **discriminated union** 으로 추가 — `visionVeto?: | { status: 'applied'; severity: 'minor' | 'moderate' | 'major'; capApplied: number } | { status: 'not_applicable'; severity?: 'minor' | 'moderate' | 'major'; capApplied?: never } | { status: 'disabled' | 'skipped_error' | 'missing_local_video'; severity?: never; capApplied?: never };` (OPTIONAL — legacy doc 호환). status='applied' 시 severity+capApplied 컴파일-타임 강제(iter2 non-blocking — status:'applied' without capApplied 차단). 주석에 "Phase 20 SCORE-08 — 비전 하향 거부권 audit. **status 가 veto 실행을 증명**(부재≠실행, HIGH-1). severity=결함 심각도, capApplied=하향된 종합점수. **점수 라벨 아님**(객관성 — enum + 임계 산출 정수). discriminated union 으로 applied→capApplied 강제(iter2)" 명시.
    - backend/shared/python/sunity_shared/models.py: visionVeto status enum 상수(VISION_VETO_STATUSES = ['applied','not_applicable','disabled','skipped_error','missing_local_video']) + severity/capApplied 키 상수/주석 추가. "Phase 20 — 비전 거부권 audit. status 가 실행 증명, applied 시에만 severity/capApplied" 주석.
    - docs/contract.md: result 구조(overallScore 161 인접)에 visionVeto optional `{ status, severity?, capApplied? }` 정의 + status enum 5값 + applied→severity/capApplied 강제 + "Phase 20 SCORE-08 — status 가 veto 실행 증명(부재≠실행), 객관성: 사람 점수 라벨 아님" 명시.
    **3-way lockstep #2+#3 — ScoreSuppression discriminated type (iter2 HIGH-3 + iter3 MEDIUM-1 + iter4 MEDIUM-1 — scoreSuppressed=true 면 reason REQUIRED):**
    - **위치 결정 = result-level discriminated suppression (전체 결과 표시를 제어하므로 comparison-local 아닌 result-level). 채택 근거 주석 박제.**
    - app/src/types/analysis.ts: **ScoreSuppression discriminated type 을 정의하고 AnalysisDoc.result 에 intersection 으로 합성** — `type ScoreSuppression = | { scoreSuppressed: true; scoreSuppressedReason: 'unheld' | 'recognition_low_confidence' } | { scoreSuppressed?: false; scoreSuppressedReason?: never };` (OPTIONAL 합성 — legacy 호환은 두 번째 멤버 `scoreSuppressed?: false`). 이렇게 하면 `scoreSuppressed: true` 객체가 `scoreSuppressedReason` 없으면 tsc 에러, `scoreSuppressed` false/부재인데 reason 있으면 tsc 에러(never). 주석에 "Phase 20 iter4 MEDIUM-1 — discriminated suppression: scoreSuppressed=true 면 scoreSuppressedReason REQUIRED, false/부재면 reason never. UI 카피가 reason-specific 이므로 reason 은 더 이상 optional 아님. 프런트 isScoreSuppressed 는 STRICTLY scoreSuppressed===true(scoringBasis 폴백 금지 — iter3 HIGH-2). 'unheld'=미보유(is_reference_free branch metadata)→'기준 없음' 카피, 'recognition_low_confidence'=recognizer 신뢰도 낮음(동작은 알 수 있어도 분류 불확실)→'동작 인식 신뢰도가 낮아 기준을 확정할 수 없어요' 카피. **iter5 HIGH-2: suppression reason 이 scoringBasisLabel 까지 소유 — recognition_low_confidence 면 scoringBasisLabel 이 '기준 동작 없음' 미노출**" 명시.
    - backend/shared/python/sunity_shared/models.py: SCORE_SUPPRESSED_REASONS = ['unheld','recognition_low_confidence'] 상수 + scoreSuppressed 키 상수 + 키 주석 추가. **불변식 주석 박제: "Phase 20 iter4 MEDIUM-1 — scoreSuppressed=True 면 scoreSuppressedReason 는 REQUIRED(producer-contract). scoreSuppressed=False/부재면 scoreSuppressedReason 없어야 함. unheld(branch metadata)=미보유, recognition_low_confidence(recognizer)=분류 불확실, 분리(iter3 MEDIUM-1)".**
    - docs/contract.md: result 구조에 scoreSuppressed optional bool + scoreSuppressedReason optional enum(unheld | recognition_low_confidence) 정의 + 각 사유의 UI 카피 + **불변식 명시: "scoreSuppressed=true → scoreSuppressedReason REQUIRED(iter4 MEDIUM-1). producer 가 reference_free_absolute 인데 scoreSuppressed 누락 = producer-contract FAILURE; scoreSuppressed=true 인데 reason 누락 = producer-contract FAILURE(fail-loud, UI silent 추론/ default 카피 금지 — iter3 HIGH-2 / iter4 MEDIUM-1). 프런트 억제 판정은 이 플래그 단독(scoringBasis 폴백 금지). iter5 HIGH-2 — suppression reason 이 모든 visible suppressed-state 카피(헤더 + scoringBasisLabel) 소유; recognition_low_confidence 의 scoringBasisLabel 은 '기준 동작 없음'/'기준 없음' 미포함".**
    **3-way lockstep #4 — scoreSuppressionAudit structured 필드 (iter5 MEDIUM-2 — 단일 필수 sink):**
    - app/src/types/analysis.ts: `scoreSuppressionAudit?: { recognizerCategory: string; branchReferenceFree: boolean; resolvedReason: 'unheld' | 'recognition_low_confidence' };` (OPTIONAL — reconcile 관측). 주석 "Phase 20 iter5 MEDIUM-2 — A2 reconcile audit. category↔branch 출처가 달라 불일치 시 단일 structured 필드로 보고(log.warning 대안 폐기). resolvedReason = resolver 의 최종 reason".
    - backend/shared/python/sunity_shared/models.py: SCORE_SUPPRESSION_AUDIT_KEYS = ['recognizerCategory','branchReferenceFree','resolvedReason'] 상수 + 주석 "iter5 MEDIUM-2 — A2 reconcile 단일 structured sink, log 는 additive only never alternative".
    - docs/contract.md: result 구조에 scoreSuppressionAudit optional `{ recognizerCategory, branchReferenceFree, resolvedReason }` 정의 + "iter5 MEDIUM-2 — recognizer category 와 branch is_reference_free 출처가 다름, resolver 가 단일 reason 결정하면서 이 필드로 reconcile 관측(log 대안 폐기)" 명시.
    **Firestore flat 검증:** visionVeto 는 scalar dict({status:str, severity:str, capApplied:int}), scoreSuppressed 는 bool, scoreSuppressedReason 는 str, scoreSuppressionAudit 는 scalar dict({recognizerCategory:str, branchReferenceFree:bool, resolvedReason:str}) 이라 nested-array 금지 정합(firestore_admin flat 저장 위반 0). 별도 nested validator 변경 불필요하나, complete_analysis 가 result 통째 저장 시 네 필드 모두 flat scalar 임을 주석으로 박제.
    이모지 금지, 2-space indent, single quote, import type, theme 무관(타입만).
  </action>
  <verify>
    <automated>cd app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && npm run typecheck` (tsc --noEmit) clean
    - visionVeto status enum optional 필드가 analysis.ts(discriminated union) + models.py + contract.md 3곳 동시 정의 (3-way lockstep — grep: 세 파일 모두 `visionVeto` + status enum 5값 + severity/capApplied)
    - **scoreSuppressed 가 analysis.ts + models.py + contract.md 3곳 동시 정의 (iter2 HIGH-3 3-way lockstep — grep: 세 파일 모두 `scoreSuppressed`); 위치 = result-level**
    - **scoreSuppressedReason('unheld' | 'recognition_low_confidence') 가 analysis.ts + models.py + contract.md 3곳 동시 정의 (iter3 MEDIUM-1 3-way lockstep — grep: 세 파일 모두 `scoreSuppressedReason` + 두 enum 값)**
    - **scoreSuppressionAudit({recognizerCategory, branchReferenceFree, resolvedReason}) 가 analysis.ts + models.py + contract.md 3곳 동시 정의 (iter5 MEDIUM-2 3-way lockstep — grep: 세 파일 모두 `scoreSuppressionAudit` + 세 키)**
    - **ScoreSuppression discriminated type — scoreSuppressed:true 는 scoreSuppressedReason 필수(iter4 MEDIUM-1; `{ scoreSuppressed: true }` without reason 객체가 tsc 에러), scoreSuppressed false/부재인데 reason 있으면 tsc 에러(never). grep: analysis.ts 에 `scoreSuppressedReason?: never` + models.py/contract.md 에 "scoreSuppressed=true → reason REQUIRED" 불변식 문구**
    - status enum = {applied, not_applicable, disabled, skipped_error, missing_local_video} 3곳 정합 (HIGH-1)
    - **TS visionVeto 가 discriminated union — status:'applied' 는 capApplied 필수(iter2 non-blocking; status:'applied' without capApplied 객체가 tsc 에러 유발하도록 union 구성)**
    - visionVeto 에 score/점수 필드 0 (status/severity enum + capApplied 정수만 — 객관성)
    - capApplied 타입 number(TS)/int(Python) + severity union/enum 정합 (Task 1 직렬화 shape 과 일치)
    - Firestore flat 정합 — visionVeto = scalar dict + scoreSuppressed = bool + scoreSuppressedReason = str + scoreSuppressionAudit = scalar dict (nested-array 0, firestore_admin 위반 0)
  </acceptance_criteria>
  <done>visionVeto {status enum(discriminated union), severity, capApplied} + ScoreSuppression discriminated type(scoreSuppressed=true→scoreSuppressedReason REQUIRED — iter4 MEDIUM-1, unheld|recognition_low_confidence) + scoreSuppressionAudit {recognizerCategory, branchReferenceFree, resolvedReason}(iter5 MEDIUM-2) optional audit 3-way 계약 lockstep + Firestore flat 정합. status 가 veto 실행 증명(HIGH-1), scoreSuppressed 가 visionVeto 와 동등 계약(iter2 HIGH-3), scoreSuppressedReason 으로 low_confidence 분리(iter3 MEDIUM-1) + suppressed 시 reason 강제(iter4 MEDIUM-1) + reconcile 단일 structured sink(iter5 MEDIUM-2), applied→capApplied 컴파일 강제(iter2 union). 객관성(점수 라벨 0). tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Mode3 미보유(branch-3) scoreSuppressed + scoreSuppressedReason 점수 억제 + 전용 resolver _score_suppression_reason(provenance 우선 — iter4 HIGH-1) + 단일 structured scoreSuppressionAudit reconcile(iter5 MEDIUM-2) + reason-owns-copy scoringBasisLabel(iter5 HIGH-2) + missing-flag/missing-reason fail-loud(iter3 HIGH-2 / iter4 MEDIUM-1) + result.tsx 점수카드 전체 억제 UX(STRICTLY flag — iter3 HIGH-2) + assert-result-score-suppression.mjs cwd-stable + self-test 정적 단언(iter3 HIGH-1 / iter4 MEDIUM-2)</name>
  <files>backend/functions/pipeline/app.py, backend/tests/test_pipeline_mode3.py, app/src/app/analysis/result.tsx, app/scripts/assert-result-score-suppression.mjs</files>
  <behavior>
    - test_branch3_score_suppressed: MODE_SELF + is_reference_free_motion True(미보유) → result['scoreSuppressed'] == True + result['scoreSuppressedReason'] == 'unheld' emit (+ scoringBasis=reference_free_absolute + scoringBasisLabel). 미보유에 confident 97 금지(D-08). scoringBasis 단독 아닌 명시 scoreSuppressed 플래그(iter2 HIGH-3).
    - **test_resolver_low_confidence_not_unheld (iter4 HIGH-1 — REGRESSION TEST, 핵심): `_score_suppression_reason(profile, branch_info)` 를 `category='low_confidence', motion_id=None, branch_info=_SAFE_DEFAULT_BRANCH`(즉 lookup_motion_branch(None) 결과, is_reference_free=True) 로 직접 호출 → 반드시 'recognition_low_confidence' 반환(절대 'unheld' 아님). _SAFE_DEFAULT_BRANCH 가 is_reference_free True 여도 category provenance 가 우선. 이것이 iter4 HIGH-1 의 정확한 회귀 단언.**
    - **test_resolver_priority (iter4 HIGH-1): resolver 우선순위 — (1) profile.category=='low_confidence' → 'recognition_low_confidence' (motion_id=None/safe-default 무관), (2) profile.category=='unregistered' → 'unheld', (3) concrete branch metadata(실 등재 아님, unavailable basis) → 'unheld'. recognized + 미보유 branch → 'unheld'. 각 케이스 직접 단위.**
    - test_low_confidence_suppressed_reason (iter3 MEDIUM-1): _process 경로에서 recognizer category=='low_confidence' (분류 불확실) → result['scoreSuppressed'] == True + result['scoreSuppressedReason'] == 'recognition_low_confidence' (점수 억제 유지하되 '기준 없음' 아님 — resolver 경유). branch-3 미보유와 다른 reason.
    - **test_low_confidence_scoring_basis_label_not_unheld (iter5 HIGH-2 — 핵심): recognizer category=='low_confidence' 로 억제된 result 의 visible scoringBasisLabel(또는 scoringBasisLabel 산출 경로)이 reference-free "기준 동작 없음"/"기준 없음" 문자열을 포함하지 않는다. scoringBasisLabel 은 suppression reason 이 존재하면 scoringBasis(reference_free_absolute) 단독에서 파생되지 않고 reason 이 카피 권위 — recognition_low_confidence 면 '동작 인식 신뢰도' 류 라벨. assert: result 의 user-facing suppressed-state 텍스트에 '기준 동작 없음' 부재.**
    - test_missing_suppression_flag_is_contract_failure (iter3 HIGH-2 acceptance): scoringBasis=='reference_free_absolute' 인데 scoreSuppressed 미emit 인 result 가 producer-contract 검사에서 FAILURE(명시 raise/assert) — UI 가 scoringBasis 로 silent 추론하지 않도록 backend producer 가 fail-loud.
    - **test_missing_reason_is_contract_failure (iter4 MEDIUM-1): result['scoreSuppressed']==True 인데 scoreSuppressedReason 누락이면 producer-contract 검사에서 FAILURE(명시 raise/assert). suppressed 면 reason 은 항상 동반(discriminated suppression invariant) — UI default '기준 없음' 카피 의존 차단.**
    - **test_recognizer_ipsf_map_reconcile (A2 → iter3 MEDIUM-1 / iter4 HIGH-1 / iter5 MEDIUM-2 reconcile — structured 필드만): resolver 가 is_reference_free + recognized → 'unheld'; low_confidence → 'recognition_low_confidence'. 둘이 불일치(low_confidence 인데 ipsf_map 은 등재)면 resolver 가 category provenance(recognition_low_confidence) 를 반환하면서 정확히 하나의 structured 필드 `result['scoreSuppressionAudit'] == {recognizerCategory: 'low_confidence', branchReferenceFree: True, resolvedReason: 'recognition_low_confidence'}` 로 보고 — log.warning '또는' 대안 0(단언이 정확한 키/값을 요구, log 는 additive 면 OK). 한 분기 아래 숨기지 않음. raise 0 (fail-closed 금지).**
    - test_branch3_no_raise: 미보유 동작이 raise 안 하고 점수+scoringBasisLabel+scoreSuppressed+scoreSuppressedReason 반환 (motion-routing-generalize-principle).
    - test_recognizer_line_dim_determinism (C-1 / D-06 / criterion #4 / TRUST-06): 같은 reference(동일 video-hash + model + yaml_version 캐시 키) 2회 recognizer 호출 → 2번째는 TechniqueCache hit(Gemini 호출 0) → 같은 category 반환 → 같은 line 차원 분류. run 간 line 차원 변동 0 (temp 0 단독 아님 — 캐시가 실 보장, Pitfall 2). mocked-Gemini, **pod-free**.
    - assert-result-score-suppression.mjs (iter3 HIGH-1 / iter4 MEDIUM-2 / iter5 HIGH-2 — RN render test 대체, cwd-stable + self-test + reason-leak 가드): **(a) cwd-stable**: result.tsx 경로를 `new URL('../src/app/analysis/result.tsx', import.meta.url)` 로 해석(repo root 또는 app/ 어디서 실행해도 동일). **(b) --self-test**: in-memory fixture — guarded sample(점수카드 6요소가 isScoreSuppressed 가드 아래) → PASS; unguarded OctagonScore sample(OctagonScore 가 가드 밖 무조건 렌더) → FAIL(non-zero); unguarded header copy sample('점수를 확인해보세요' 가 가드 밖) → FAIL; **low-confidence reason-leak sample(recognition_low_confidence 분기인데 scoringBasisLabel/카피가 '기준 동작 없음' 으로 떨어짐) → FAIL(iter5 HIGH-2)**. **(c) 기본 실행**: result.tsx 소스를 읽어 OctagonScore / gradeBadge / LevelBenchmark / scoreCaption / score-derived summary / '점수를 확인해보세요' 헤더 카피가 모두 비억제(!isScoreSuppressed) 분기 아래인지 + **recognition_low_confidence 분기가 reference-free '기준 동작 없음' 라벨로 떨어지지 않는지(reason-owns-copy — iter5 HIGH-2)** 정적 단언. 하나라도 비조건부/leak 이면 non-zero exit + 어떤 요소가 위반인지 출력. 통과 시 exit 0.
  </behavior>
  <read_first>
    - backend/functions/pipeline/app.py:1693-1790 (_mode3_scoring_basis 1693 = is_first/is_reference_free → basis, is_reference_free=assemble.is_reference_free_motion(branch_info) 1736, first_basis 1756, progress_basis 1789, MODE_SELF 게이트) + branch_info early lookup 위치
    - backend/shared/python/sunity_shared/analysis/assemble.py:106-128 (**lookup_motion_branch: motion_id None/falsy → _SAFE_DEFAULT_BRANCH 반환 — iter4 HIGH-1 핵심**) + :130-150 (is_reference_free_motion = angleSource/angleFixtureKey/ipsfCode/officialName 판정, **_SAFE_DEFAULT_BRANCH 가 is_reference_free True 를 반환 — 143-150**, branch_info None 도 True 143-144) + **:582-604 (_MODE3_SCORING_BASIS_LABELS: MODE3_SCORING_BASIS_REFERENCE_FREE_ABSOLUTE → "기준 동작 없음 — 절대 자세 기준 평가" 라벨 — iter5 HIGH-2 핵심 leak source. recognition_low_confidence 면 이 라벨로 떨어지면 안 됨)** + scoringBasis 상수 + build_mode3 scoring_basis 인자
    - backend/shared/python/sunity_shared/analysis/gemini_technique_recognizer.py:14-18 (category enum docstring: low_confidence 와 unregistered 는 SEPARATE 케이스 — iter3 MEDIUM-1 / iter4 HIGH-1 핵심 근거) + :163-182 (**low_confidence: mean conf < threshold → category='low_confidence', motion_id=None — recognizer 불확실, 미보유 아님. motion_id=None 이 assemble.lookup_motion_branch(None)=_SAFE_DEFAULT_BRANCH 를 유발한다는 점이 iter4 HIGH-1 의 collapse 원인**) + :184-206 (unregistered: scope_status=='unregistered' → category='unregistered', motion_id=None, 진짜 미등록) + :124-126 (category 4값 recognized/api_failure/low_confidence/unregistered) + cache wiring
    - backend/shared/python/sunity_shared/analysis/technique_cache.py (compute_video_hash SHA256, compute_yaml_version, TechniqueCache + cache key (video_hash, model_name, yaml_version) — recognizer 결정성 캐시, C-1. **주의: 이는 recognizer 캐시 — vision veto severity 캐시(20-02 VisionVetoCache)와 별개**)
    - 20-VALIDATION.md V-4(결정론) + V-8(Mode3 미보유 억제 + 점수카드 전체 + scoreSuppressedReason + reason-owns-copy + self-test 커맨드) + V-18(resolver provenance + 단일 structured audit) + V-21(scoringBasisLabel reason-leak 차단) + 20-RESEARCH.md §"Pitfall 2"(temp 0 ≠ bit-deterministic, 캐시가 실 보장). **모든 backend 커맨드 단일 shape `cd backend && PYTHONPATH=shared/python ...`(iter5 MEDIUM-1)**
    - 20-RESEARCH.md §"Pattern 4: Mode3 3-branch"(category enum + is_reference_free + scoringBasis 재사용, gap=score suppression) + §"Pitfall 4"(branch-3 confident 점수 차단) + §"Assumptions A2"(category↔ipsf_map 정합 — iter3 MEDIUM-1: '일치' → 'reason 으로 reconcile', iter5 MEDIUM-2: 단일 structured sink)
    - 20-CONTEXT.md D-07(3분기 recognizer) / D-08(억제 + 기준없음, confident 97 금지) + Claude's Discretion(억제 UX 강도 = plan 확정, belle = "점수 억제 + 기준없음")
    - 20-DIRECT-REVIEW-ITERATION2.md HIGH-2 (점수카드 전체 억제) + HIGH-3 (scoreSuppressed 신호)
    - 20-DIRECT-REVIEW-ITERATION3.md HIGH-1 (앱 test runner 부재 → 정적 mjs 단언) + HIGH-2 (isScoreSuppressed STRICTLY scoreSuppressed===true, scoringBasis 폴백 제거 + missing-flag fail-loud) + MEDIUM-1 (scoreSuppressedReason unheld vs recognition_low_confidence + A2 reconcile)
    - 20-DIRECT-REVIEW-ITERATION4.md HIGH-1 (전용 resolver _score_suppression_reason, category=PROVENANCE, low_confidence+motion_id=None+_SAFE_DEFAULT_BRANCH → recognition_low_confidence NOT unheld, 정확한 회귀 테스트) + MEDIUM-1 (suppressed→reason REQUIRED, missing-reason fail-loud) + MEDIUM-2 (mjs cwd-stable import.meta.url + --self-test)
    - **20-DIRECT-REVIEW-ITERATION5.md HIGH-2 (scoringBasisLabel reason-leak: recognition_low_confidence 가 '기준 동작 없음' 미노출, reason-owns-copy, scoringBasisLabel 이 scoringBasis 단독에서 파생 금지, test_low_confidence_scoring_basis_label_not_unheld + mjs low-confidence 분기 가드) + MEDIUM-2 (A2 audit = 단일 structured 필드 scoreSuppressionAudit {recognizerCategory, branchReferenceFree, resolvedReason}, log.warning 대안 폐기, test_recognizer_ipsf_map_reconcile 가 정확한 키/값 단언) + MEDIUM-1 (backend 커맨드 단일 shape)**
    - **app/src/app/analysis/result.tsx:443 (grade=scoreGrade(result.overallScore)) + :491-496 (mode3 summary = mode3Summary(result.overallScore,...)) + :677 ('점수를 확인해보세요' 헤더 카피) + :679-684 (scoringBasisLabel 렌더 — 이미 존재, iter5 HIGH-2 reason-leak 차단 대상) + :735-747 (점수카드 View: OctagonScore 737 / gradeBadge 739 / summary 740 / LevelBenchmark 742 / scoreCaption 744-746 — iter2 HIGH-2 전체 억제 대상)**
    - app/scripts/seed-reference-motions.mjs (기존 .mjs 스크립트 스타일 — node ESM, process.exit(1) on fail — assert-result-score-suppression.mjs 가 모방할 컨벤션. **단 cwd-stable 위해 fs.readFileSync 상대경로 대신 import.meta.url 기반 URL 사용 — iter4 MEDIUM-2**)
    - app/src/types/analysis.ts (Task 2 의 ScoreSuppression discriminated type + scoreSuppressionAudit + scoringBasis Mode3 4-value)
  </read_first>
  <action>
    **전용 resolver _score_suppression_reason (backend — iter4 HIGH-1, 핵심):** app.py 에 신규 순수 헬퍼 `_score_suppression_reason(profile, branch_info)` 를 정의 — recognizer category 를 **PROVENANCE** 로 다루는 우선순위:
      1. `profile is not None and profile.category == 'low_confidence'` → return `'recognition_low_confidence'` (**motion_id=None 이 lookup_motion_branch(None)=_SAFE_DEFAULT_BRANCH 를 유발해 is_reference_free=True 여도 무조건 우선** — "모르니까 안전 기본" ≠ "확정 미보유").
      2. `profile is not None and profile.category == 'unregistered'` → return `'unheld'` (진짜 미등록).
      3. `assemble.is_reference_free_motion(branch_info)` (concrete branch metadata 가 unavailable basis) → return `'unheld'`.
      4. 그 외(등재 동작) → return `None` (억제 아님).
    주석에 "iter4 HIGH-1 — recognizer category 가 provenance. low_confidence 는 motion_id=None→_SAFE_DEFAULT_BRANCH(is_reference_free True)를 유발하지만 그것은 미보유 증거가 아님. _SAFE_DEFAULT_BRANCH 를 unheld 의 proof 로 쓰지 않는다. test_resolver_low_confidence_not_unheld 가 이 경로를 박제" 박제.
    **branch-3 점수 억제 신호 (backend — iter2 HIGH-3 scoreSuppressed + iter3 MEDIUM-1 + iter4 HIGH-1 resolver):** _mode3_scoring_basis / MODE_SELF 경로에서 `reason = _score_suppression_reason(profile, branch_info)` 호출 → `reason is not None` 이면 result 에 **`scoreSuppressed: True` + `scoreSuppressedReason: reason`** emit(Task 2 의 discriminated suppression 계약) + scoringBasis=reference_free_absolute(unheld 시) + scoringBasisLabel(아래 reason-owns-copy 참조). **confident overallScore 를 0 으로 만들거나 raise 하지 않는다** — 점수는 산출하되 scoreSuppressed=True 로 화면이 점수카드 전체를 억제(result.tsx Task 3 후반). fail-closed/raise 금지([[motion-routing-generalize-principle]]). **scoringBasis 단독을 억제 신호로 쓰지 않고 명시 scoreSuppressed 플래그를 emit(iter2 HIGH-3). reason 매핑은 resolver 단일 진실(iter4 HIGH-1) — '둘 다 해당이면 unheld 우선' 류 로직 절대 사용 금지(low_confidence 가 unheld 로 collapse 되는 원인).**
    **reason-owns-copy scoringBasisLabel (backend — iter5 HIGH-2):** scoringBasisLabel 산출이 suppression reason 을 따른다 — `reason == 'unheld'` 일 때만 _MODE3_SCORING_BASIS_LABELS 의 reference-free "기준 동작 없음 — 절대 자세 기준 평가" 라벨(assemble.py:598) 적용. **`reason == 'recognition_low_confidence'` 일 때는 reference-free 라벨을 절대 쓰지 않고 별도 라벨(예 "동작 인식 신뢰도가 낮아 기준을 확정할 수 없어요" 류, 한국어)을 emit — scoringBasisLabel 이 scoringBasis(reference_free_absolute) 단독에서 파생되지 않는다(reason 이 카피 권위).** 주석에 "iter5 HIGH-2 — suppression reason 이 scoringBasisLabel 소유; recognition_low_confidence 는 '기준 동작 없음' 미노출(두 번째 UI 필드 reason leak 차단)" 박제. test_low_confidence_scoring_basis_label_not_unheld 가 leak 부재를 RED 로 강제.
    **producer-contract fail-loud (iter3 HIGH-2 + iter4 MEDIUM-1):** _process 의 result emit 경로에서 (a) scoringBasis=='reference_free_absolute' 이면 scoreSuppressed 가 **반드시** set 됨을 보장 — 누락 시 명시 raise/assert(producer-contract FAILURE). (b) **scoreSuppressed==True 이면 scoreSuppressedReason 가 반드시 set + SCORE_SUPPRESSED_REASONS 내 값임을 보장 — 누락/무효 시 명시 raise/assert(iter4 MEDIUM-1).** UI 가 scoringBasis 로 silent 추론하거나 reason 누락 시 default '기준 없음' 카피로 폴백하지 않도록 backend 가 fail-loud. 주석에 "iter3 HIGH-2 — reference_free_absolute 는 scoreSuppressed 동반 필수; iter4 MEDIUM-1 — scoreSuppressed=True 는 scoreSuppressedReason 동반 필수(discriminated suppression invariant), UI silent 추론/default 카피 금지" 박제. test_missing_suppression_flag_is_contract_failure + test_missing_reason_is_contract_failure 가 두 경로를 RED 로 강제.
    **A2 reconcile — 단일 structured sink (backend — iter3 MEDIUM-1 / iter4 HIGH-1 / iter5 MEDIUM-2):** recognizer category ↔ is_reference_free_motion(ipsf_map branch_info) 는 출처가 다름(하나는 recognizer 출력, 하나는 motion_ipsf_map.json). resolver 가 category provenance 로 reason 을 단일 결정하되, **불일치 시 정확히 하나의 관측 가능 structured 필드 `result['scoreSuppressionAudit'] = {'recognizerCategory': profile.category, 'branchReferenceFree': bool(is_reference_free), 'resolvedReason': reason}` 로 보고(iter5 MEDIUM-2 — log.warning '또는' 대안 폐기, log 는 additive only never alternative).** test_recognizer_ipsf_map_reconcile 가 이 structured 필드의 **정확한 키/값**(recognizerCategory, branchReferenceFree, resolvedReason)을 단언. 주석에 "A2(iter3 MEDIUM-1 / iter4 HIGH-1 / iter5 MEDIUM-2) — 두 신호 출처 다름, resolver category provenance 단일 결정 + 불일치는 단일 structured 필드 scoreSuppressionAudit 로 보고(log 대안 폐기)" 박제.
    **result.tsx 점수카드 전체 억제 UX + reason-owns-copy (app — iter2 HIGH-2 + iter3 HIGH-2 + iter4 MEDIUM-1 + iter5 HIGH-2):** **단일 boolean `const isScoreSuppressed = cmp.mode === 'mode3' && result.scoreSuppressed === true` 박제 — STRICTLY scoreSuppressed===true 단독, scoringBasis 폴백 절대 금지(iter3 HIGH-2).** (legacy tolerance 가 정말 필요하면 `isLegacyReferenceFreeWithoutSuppressionFlag` 로 **별도·가시적 legacy-only** 박제하고 V-8/3-way 계약에 절대 포함 안 함 — 단 본 phase 신규 경로는 이걸 쓰지 않음.) `isScoreSuppressed` 시:
      - 점수카드 View(735-747) **전체를 '기준 없음' state 로 대체** — OctagonScore(737) + gradeBadge(739) + score-derived summary(740) + LevelBenchmark(742) + scoreCaption(744-746) **모두 미렌더**.
      - 헤더 mode3 카피(677 '점수를 확인해보세요')를 억제 시 분기: **scoreSuppressedReason 별 카피** — 'unheld' → '기준 없음' 류(기존 D-08 카피), 'recognition_low_confidence' → '동작 인식 신뢰도가 낮아 기준을 확정할 수 없어요'(iter3 MEDIUM-1). **reason 누락 시 default '기준 없음' 카피로 폴백 금지(iter4 MEDIUM-1) — discriminated suppression 상 reason 은 항상 동반되므로 누락은 contract 위반(backend 가 fail-loud). 만약 방어적으로 처리하면 '기준 없음' 이 아닌 중립 카피로(오라벨 방지).** confident 유도 카피 제거.
      - **scoringBasisLabel 렌더(679-684, iter5 HIGH-2 reason-owns-copy):** 억제 시 scoringBasisLabel 은 backend 가 reason-aware 로 채운 값(unheld→'기준 동작 없음', recognition_low_confidence→'신뢰도 낮음' 류)을 그대로 표시 — UI 가 scoringBasis 로 '기준 동작 없음' 을 재구성하지 않는다. recognition_low_confidence 억제 결과의 scoringBasisLabel 이 '기준 동작 없음' 을 표시하지 않음(헤더 카피와 모순 0). 즉 backend reason-owns-copy 를 신뢰하되 UI 측에서 reference-free 라벨을 reason 무시하고 강제하지 않는다.
      - grade(443)/summary(491-496) 계산식은 남되 **억제 시 화면에 안 쓰임**(렌더 분기에서 제외).
      - dimensionScores/diagnostic 섹션은 **비권위 관측**으로 남길 수 있음(필요 시) — 단 점수카드 confident 표시는 전부 제거.
      억제 아닐 때(recognized/known) 기존 표시 전부 유지. theme 토큰만(색/스페이싱 하드코딩 금지, #FF4B33 직접 금지), graceful(필드 부재 시 기존 동작).
    **assert-result-score-suppression.mjs (app — iter3 HIGH-1 / iter4 MEDIUM-2 / iter5 HIGH-2, RN render test 대체, cwd-stable + self-test + reason-leak 가드):** `app/scripts/assert-result-score-suppression.mjs` 신규 — node ESM(seed-reference-motions.mjs 스타일).
      - **cwd-stable (iter4 MEDIUM-2):** result.tsx 경로를 상대 cwd 가 아니라 `const resultPath = new URL('../src/app/analysis/result.tsx', import.meta.url)` 로 해석 + `fs.readFileSync(resultPath, 'utf8')`. repo root 든 app/ 든 동일 동작.
      - **--self-test (iter4 MEDIUM-2 + iter5 HIGH-2):** `process.argv.includes('--self-test')` 면 in-memory fixture 문자열로 검사 로직을 자체검증 — (1) guarded sample(점수카드 6요소가 `!isScoreSuppressed &&` 또는 `isScoreSuppressed ? ... : (점수카드)` else 아래) → 검사 PASS 기대; (2) unguarded OctagonScore sample → FAIL 기대; (3) unguarded header copy sample → FAIL 기대; **(4) low-confidence reason-leak sample(recognition_low_confidence 분기인데 scoringBasisLabel/카피가 '기준 동작 없음' 으로 하드코딩 떨어짐) → FAIL 기대(iter5 HIGH-2)**. 셋 중 하나라도 기대와 다르면 self-test 자체가 non-zero exit + 어떤 fixture 가 어긋났는지 출력. 검사 핵심 로직을 함수(예: `assertSuppressionGuards(source) -> {ok, violations}`)로 분리해 self-test 와 실행이 동일 함수 공유.
      - **기본 실행:** `assertSuppressionGuards(result.tsx 소스)` 로 (a) 점수카드 6요소(OctagonScore, gradeBadge(styles.gradeBadge), LevelBenchmark, scoreCaption(styles.scoreCaption/캡션 텍스트), score-derived summary(styles.summary), '점수를 확인해보세요' 헤더 카피)가 **isScoreSuppressed 비억제 분기 가드** 아래인지 검사 + `isScoreSuppressed` 식별자 정의 존재; (b) **recognition_low_confidence 분기/카피 경로가 reference-free '기준 동작 없음' 문자열을 하드코딩 렌더하지 않는지(iter5 HIGH-2 reason-owns-copy)** 검사. 위반 시 `console.error('[suppression] <요소/leak> 가 가드 밖/reason leak — (iter2 HIGH-2 / iter5 HIGH-2 위반)')` + `process.exit(1)`. 전부 통과면 exit 0.
      (Jest/RN 스택 도입 금지 — iter3 HIGH-1.)
    이모지 금지, snake_case(py)/camelCase(ts/mjs), import type, 한국어 카피.
  </action>
  <verify>
    <automated>cd backend && PYTHONPATH=shared/python python -m pytest tests/test_pipeline_mode3.py -k "branch3 or reference_free or recognizer or line_dim_determinism or missing_suppression or missing_reason or low_confidence or resolver or basis_label" -x -q && cd ../app && node scripts/assert-result-score-suppression.mjs --self-test && node scripts/assert-result-score-suppression.mjs && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - test_pipeline_mode3.py branch-3 + resolver + reconcile + missing-flag + missing-reason + low_confidence + basis_label 케이스 GREEN + **`cd app && node scripts/assert-result-score-suppression.mjs --self-test && node scripts/assert-result-score-suppression.mjs` exit 0(self-test + 실 실행, iter3 HIGH-1 / iter4 MEDIUM-2 / iter5 HIGH-2)** + `cd app && npm run typecheck` clean
    - **전용 resolver: `_score_suppression_reason(category='low_confidence', motion_id=None, branch_info=_SAFE_DEFAULT_BRANCH)` → 'recognition_low_confidence' (절대 'unheld' 아님 — test_resolver_low_confidence_not_unheld GREEN, iter4 HIGH-1 정확한 회귀). grep: app.py 에 '둘 다 해당이면 unheld 우선' 류 collapse 로직 0건, resolver 가 단일 진실**
    - **resolver 우선순위: low_confidence→recognition_low_confidence(provenance) / unregistered→unheld / concrete unavailable branch→unheld (test_resolver_priority GREEN — iter4 HIGH-1)**
    - 미보유(is_reference_free True, recognized) → result['scoreSuppressed']==True + result['scoreSuppressedReason']=='unheld' emit + scoringBasisLabel '기준 동작 없음' (test_branch3_score_suppressed GREEN — D-08, confident 97 금지; scoringBasis 단독 아닌 명시 플래그 — iter2 HIGH-3)
    - **recognizer category=='low_confidence' → scoreSuppressed==True + scoreSuppressedReason=='recognition_low_confidence' (점수 억제 유지하되 '기준 없음' 아님 — test_low_confidence_suppressed_reason GREEN, iter3 MEDIUM-1, resolver 경유)**
    - **recognition_low_confidence 억제 result 의 visible scoringBasisLabel/카피가 reference-free '기준 동작 없음'/'기준 없음' 미포함 — scoringBasisLabel 이 reason 소유, scoringBasis 단독 파생 0 (test_low_confidence_scoring_basis_label_not_unheld GREEN — iter5 HIGH-2; grep: backend scoringBasisLabel 산출이 reason 분기, reference-free 라벨이 reason 무시하고 무조건 적용 0건)**
    - **scoringBasis=='reference_free_absolute' 인데 scoreSuppressed 누락 = producer-contract FAILURE (명시 raise/assert — test_missing_suppression_flag_is_contract_failure GREEN; UI silent 추론 0, iter3 HIGH-2)**
    - **scoreSuppressed==True 인데 scoreSuppressedReason 누락 = producer-contract FAILURE (명시 raise/assert — test_missing_reason_is_contract_failure GREEN; UI default '기준 없음' 카피 의존 차단, iter4 MEDIUM-1)**
    - **recognizer category ↔ is_reference_free_motion 가 resolver category provenance 로 reconcile + 불일치는 정확히 하나의 structured 필드 `result['scoreSuppressionAudit'] == {recognizerCategory, branchReferenceFree, resolvedReason}` 로 보고(log.warning '또는' 대안 0 — test_recognizer_ipsf_map_reconcile 가 정확한 키/값 단언, iter5 MEDIUM-2). 한 분기 아래 숨김 0, raise 0 (A2 → iter3 MEDIUM-1 / iter4 HIGH-1 / iter5 MEDIUM-2)**
    - 같은 reference 2회 recognizer 호출 → 캐시 hit(Gemini 호출 0) → 같은 category/line 분류 = run 간 line 차원 변동 0 (test_recognizer_line_dim_determinism GREEN — C-1 / D-06 / TRUST-06, pod-free)
    - 미보유가 raise 0 + 점수+scoringBasisLabel+scoreSuppressed+scoreSuppressedReason 반환 (test_branch3_no_raise GREEN — fail-closed 금지)
    - **result.tsx 의 `isScoreSuppressed` 가 STRICTLY `result.scoreSuppressed === true` 단독 (grep: result.tsx isScoreSuppressed 정의에 scoringBasis/reference_free 폴백 토큰 0건 — iter3 HIGH-2); 점수카드 분기에서 OctagonScore/gradeBadge/LevelBenchmark/scoreCaption/summary 모두 조건부, octagon-only 억제 아님**
    - **`cd app && node scripts/assert-result-score-suppression.mjs` 가 cwd-independent(repo root + app/ 둘 다 동일) + `--self-test` 가 guarded sample PASS / unguarded OctagonScore sample FAIL / unguarded header sample FAIL / low-confidence reason-leak sample FAIL 을 자체검증(iter4 MEDIUM-2 / iter5 HIGH-2; grep: 스크립트에 `import.meta.url` + `--self-test` 분기 + reason-leak 가드). 점수카드 6요소 가드 밖 또는 recognition_low_confidence 분기에 '기준 동작 없음' leak 이면 non-zero exit — 실제 돌아가는 정적 단언(false-green 차단; npm run typecheck 단독 아님)**
    - 억제 시 헤더 카피가 scoreSuppressedReason 별 분기('unheld'→'기준 없음', 'recognition_low_confidence'→'동작 인식 신뢰도가 낮아 기준을 확정할 수 없어요' — iter3 MEDIUM-1), reason 누락 default '기준 없음' 폴백 0(iter4 MEDIUM-1)
    - theme 토큰만 (grep: 신규 추가 라인에 `#` hex/매직 px 0건)
    - 무관한 backend suite 회귀 0 + tsc clean
  </acceptance_criteria>
  <done>전용 resolver _score_suppression_reason(provenance 우선 — low_confidence 가 _SAFE_DEFAULT_BRANCH 로 unheld collapse 안 됨, iter4 HIGH-1) + Mode3 미보유 scoreSuppressed=True + reason emit(명시 플래그 + reference_free_absolute + 기준없음 카피) + low_confidence='recognition_low_confidence' 분리(iter3 MEDIUM-1) + reason-owns-copy scoringBasisLabel(recognition_low_confidence 가 '기준 동작 없음' 미노출 — iter5 HIGH-2) + missing-flag/missing-reason producer-contract fail-loud(iter3 HIGH-2 / iter4 MEDIUM-1) + A2 단일 structured scoreSuppressionAudit reconcile(log 대안 폐기, raise 0 — iter5 MEDIUM-2) + recognizer line 차원 cache-hit 결정성(C-1/D-06/TRUST-06) + result.tsx **점수카드 전체 억제**(STRICTLY scoreSuppressed===true, scoringBasis 폴백 0 — iter3 HIGH-2; octagon+grade+summary+benchmark+caption+헤더, iter2 HIGH-2) + assert-result-score-suppression.mjs **cwd-stable + self-test + reason-leak 가드 정적 단언**(iter3 HIGH-1 / iter4 MEDIUM-2 / iter5 HIGH-2). confident 97 차단. backend 테스트 + mjs self-test/실행 exit 0 + tsc clean. 회귀 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

pipeline _process(SQS consumer, 인증 후 서버측) → Gemini 어댑터(20-02 경계) + ML core(20-01 순수) + Firestore flat 저장 + 앱 표시. 신규 endpoint/auth 0. visionVeto/scoreSuppressed/scoreSuppressedReason/scoreSuppressionAudit = optional scalar 필드(nested-array 0). 토글은 pipeline 단독 소유(어댑터 미소유). 앱 억제 자동검증 = pod-free cwd-stable + self-test 정적 .mjs(Jest/RN 스택 미도입).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-09 | Tampering / 신뢰성 | 비전이 채점 path 에서 점수를 **올림** (D-01 위반, 위양성 재발) | mitigate | _apply_vision_veto = apply_downward_cap(min) 경유, test_vision_veto_downward_only_with_cap ∀입력 출력≤입력 + grep `max(` 0건 |
| T-20-09b | Tampering / 신뢰성 | veto ON 인데 local_video_path 보존 안 돼 무음 no-op (위양성 생존, HIGH-1) | mitigate | keep_local_video 게이트에 _gemini_vision_veto_enabled() 포함 + test_keep_local_video_for_veto_only (non-None path) + visionVeto.status='missing_local_video' 신호 |
| T-20-09c | Tampering / 신뢰성 | **cap-mutation 경로가 20-04 전까지 미테스트 → mutation 이 실제로 안 내려가는 버그 잠복 (iter2 HIGH-1)** | mitigate | test_vision_veto_downward_only_with_cap 이 `monkeypatch.setitem(SEVERITY_CAP,'major',50)` scoped fixture 로 하향 증명(pod-free) + test_vision_veto_placeholder_cap_no_mutation 이 production None=not_applicable 증명. production cap 무손상(D-02) |
| T-20-09d | Architecture / Integrity | **토글 ownership drift (iter2 MEDIUM-1)** — 어댑터+pipeline 양쪽 토글 정의로 불일치 → no-op 재발 | mitigate | 토글 pipeline 단독 소유(test_toggle_owned_by_pipeline: app.py 1건/어댑터 0건), _apply_vision_veto 가 유일 게이트 |
| T-20-10 | DoS / integrity | Gemini 실패 → silent gate bypass (kip-up 위양성 prod 통과) | mitigate | graceful 통과 BUT WARNING 로그 + visionVeto.status='skipped_error' 신호 (관측 가능, Pitfall 5). 실 검출 = 20-04 eval cache-warm + status=applied 증명 |
| T-20-11 | Information disclosure / 신뢰성 | Mode3 미보유 confident 97 (기준 없음인데 권위적 숫자, 신뢰 파괴) | mitigate | branch-3 scoreSuppressed + **점수카드 전체 억제**(octagon만 아님 — iter2 HIGH-2) + scoringBasisLabel 기준없음 (Pitfall 4 / D-08) |
| T-20-11b | Information disclosure / 신뢰성 | **억제 시 octagon 만 숨기고 gradeBadge/summary/caption/헤더로 점수 confident 누출 (iter2 HIGH-2)** | mitigate | isScoreSuppressed 단일 boolean 으로 점수카드 전체 대체 + **assert-result-score-suppression.mjs 가 6요소 가드 정적 단언(실 실행 + self-test, false-green 차단 — iter3 HIGH-1 / iter4 MEDIUM-2)** |
| T-20-11c | Tampering / 계약 | **scoreSuppressed 가 비계약(uncontracted)으로 backend↔frontend drift (iter2 HIGH-3)** | mitigate | scoreSuppressed + scoreSuppressedReason + scoreSuppressionAudit 3-way lockstep(analysis.ts+models.py+contract.md), scoringBasis 단독 의존 금지 |
| T-20-11d | Tampering / 신뢰성 | **isScoreSuppressed 가 scoringBasis 폴백을 재도입 → label-drives-policy 회귀, backend 가 flag 누락해도 UI silent 추론 (iter3 HIGH-2)** | mitigate | isScoreSuppressed = STRICTLY result.scoreSuppressed===true (grep: 정의에 scoringBasis/reference_free 폴백 0건) + producer-contract fail-loud(reference_free_absolute 인데 flag 누락 → raise, test_missing_suppression_flag_is_contract_failure) |
| T-20-11e | Information disclosure / 신뢰성 | **low_confidence 를 '기준 없음' 으로 오라벨 → 아는 동작인데 미보유라 오인 (오해 UX, iter3 MEDIUM-1)** | mitigate | scoreSuppressedReason(unheld vs recognition_low_confidence) 분리 + UI 카피 분기 + test_low_confidence_suppressed_reason. branch metadata 가 미보유 증명할 때만 unheld |
| T-20-11f | Integrity / false-green-red | **억제 자동검증이 typecheck-only/cwd 의존이라 점수카드가 여전히 렌더돼도 green 또는 wrong-cwd 로 false-red (iter3 HIGH-1 / iter4 MEDIUM-2)** | mitigate | RN render test 가 아닌 **실행되는 정적 .mjs**가 verify 에 명시 + **cwd-stable(import.meta.url) + --self-test(guarded PASS/unguarded FAIL 자체검증)** — 6요소 가드 밖이면 non-zero, 검사 함수 자체를 self-test 가 증명. Jest/RN 스택 미도입 |
| T-20-11g | Tampering / 신뢰성 | **low_confidence 가 motion_id=None→_SAFE_DEFAULT_BRANCH(is_reference_free True)로 unheld 에 collapse → '아는데 불확실' 을 '미보유' 로 오라벨 (iter4 HIGH-1)** | mitigate | 전용 resolver `_score_suppression_reason` 가 category 를 PROVENANCE 로 — low_confidence 우선 'recognition_low_confidence', _SAFE_DEFAULT_BRANCH 를 unheld 증거로 안 씀. test_resolver_low_confidence_not_unheld 회귀(category=low_confidence+motion_id=None+_SAFE_DEFAULT_BRANCH→recognition_low_confidence). '둘 다 해당이면 unheld 우선' collapse 로직 제거 |
| T-20-11h | Tampering / 계약 | **scoreSuppressed=true 인데 reason 누락 → UI default '기준 없음' 폴백으로 오라벨 drift (iter4 MEDIUM-1)** | mitigate | ScoreSuppression discriminated type(suppressed→reason REQUIRED, tsc 강제) + producer-contract fail-loud(test_missing_reason_is_contract_failure) + UI reason 누락 default '기준 없음' 폴백 금지 |
| T-20-11i | Information disclosure / 신뢰성 | **low_confidence 억제가 두 번째 UI 필드(scoringBasisLabel)로 reference-free '기준 동작 없음' 을 누출 → 헤더는 '신뢰도 낮음' 인데 basis 라벨은 '기준 동작 없음' 동시 표시 (iter5 HIGH-2)** | mitigate | reason-owns-copy — scoringBasisLabel 이 suppression reason 존재 시 scoringBasis 단독에서 파생 안 됨, recognition_low_confidence 면 '기준 동작 없음' 미노출. test_low_confidence_scoring_basis_label_not_unheld + assert-result-score-suppression.mjs low-confidence 분기 reason-leak 가드(self-test sample) |
| T-20-11j | Integrity / observability | **A2 reconcile 불일치 sink 가 dict OR log.warning 으로 분기 가능 → 약한 sink 로 테스트가 통과하고 증거가 async/Pod log 에서 소실 (iter5 MEDIUM-2)** | mitigate | 정확히 하나의 structured 필드 `scoreSuppressionAudit {recognizerCategory, branchReferenceFree, resolvedReason}` 만 계약(log 는 additive only never alternative) + test_recognizer_ipsf_map_reconcile 가 정확한 키/값 단언 + 3-way lockstep |
| T-20-12 | DoS | 미보유 fail-closed raise → 분석 차단 | mitigate | OR reconcile 후 점수+근거 반환, raise 0 ([[motion-routing-generalize-principle]]) — 단 producer-contract(reference_free_absolute↔scoreSuppressed, suppressed↔reason) 누락은 의도적 fail-loud(iter3 HIGH-2 / iter4 MEDIUM-1) |
| T-20-13 | Tampering / 객관성 | visionVeto audit 에 사람 점수 라벨 누출 | mitigate | visionVeto = status/severity enum + capApplied(임계 산출 정수)만, score 필드 0 (3-way 계약 + grep) |
| T-20-14 | Integrity | recognizer category ↔ ipsf_map 불일치로 branch-3 오발 (A2 → iter3 MEDIUM-1 / iter4 HIGH-1 / iter5 MEDIUM-2 reconcile) | mitigate | resolver category provenance 단일 결정 + 불일치 단일 structured audit 보고 + test_recognizer_ipsf_map_reconcile |
| T-20-SC | Tampering | npm/pip installs | accept | 신규 패키지 0 (20-01/20-02 모듈 + 기존 adapter + node stdlib fs/.mjs). Package Legitimacy Audit N/A |

내부 오케스트레이션 + optional 계약 필드 + 표시 억제 — 위협 표면 낮음.
</threat_model>

<verification>
- `cd backend && PYTHONPATH=shared/python python -m pytest tests/test_pipeline_mode3.py -x -q` GREEN (vision_veto+keep_local_video+toggle_owned 전환 + branch-3 + resolver + reconcile(structured) + missing-flag + missing-reason + low_confidence + basis_label + cap-mutation monkeypatch, 전부 mocked adapter)
- `cd backend && PYTHONPATH=shared/python python -m pytest tests/ -q` 전체 회귀 0
- `cd app && node scripts/assert-result-score-suppression.mjs --self-test && node scripts/assert-result-score-suppression.mjs` exit 0 (self-test 자체검증 + 점수카드 6요소 isScoreSuppressed 가드 + low-confidence reason-leak 정적 단언 — iter3 HIGH-1 / iter4 MEDIUM-2 / iter5 HIGH-2, cwd-stable 실 실행 false-green/red 차단)
- `cd app && npm run typecheck` clean (visionVeto discriminated union + ScoreSuppression discriminated type + scoreSuppressionAudit)
- visionVeto(status enum, union)/scoreSuppressed/scoreSuppressedReason/scoreSuppressionAudit schema 키 추가 → EAS 재빌드 (optional 키)
- 실 거부권 동작(kip-up≤50) + visionVeto.status='applied' 증명은 20-04 Pod sweep 에서 정량 확정 — 본 plan 은 mocked 로직 + cap monkeypatch + 정적 억제 단언만
- **모든 backend 커맨드 단일 shape `cd backend && PYTHONPATH=shared/python ...`(iter5 MEDIUM-1)**
</verification>

<success_criteria>
- 토글 pipeline 단독 소유(iter2 MEDIUM-1) + keep_local_video 게이트에 veto 토글 포함 (HIGH-1 — veto no-op 차단) + _apply_vision_veto 하향-전용 mutation 통합 (SCORE-08 / D-01/03/04/05) — Mode1+Mode3, 올림 0, 평균 0
- cap-mutation 경로 monkeypatch 증명 + production cap=None 무손상 (iter2 HIGH-1 / D-02)
- visionVeto status enum audit 3-way lockstep(discriminated union) + ScoreSuppression discriminated type(scoreSuppressed=true→reason REQUIRED — iter2 HIGH-3 / iter3 MEDIUM-1 / iter4 MEDIUM-1) + scoreSuppressionAudit 단일 structured sink(iter5 MEDIUM-2) + graceful 무음실패 관측 (TRUST-08 / Pitfall 5 / HIGH-1)
- 전용 resolver _score_suppression_reason(provenance 우선, low_confidence unheld collapse 차단 — iter4 HIGH-1)
- Mode3 미보유 점수카드 전체 억제(iter2 HIGH-2) + STRICTLY scoreSuppressed===true(scoringBasis 폴백 0) + producer-contract fail-loud(flag + reason — iter3 HIGH-2 / iter4 MEDIUM-1) + low_confidence reason 분리(iter3 MEDIUM-1) + **reason-owns-copy scoringBasisLabel(recognition_low_confidence 가 '기준 동작 없음' 미노출 — iter5 HIGH-2)** + 기준없음 화면 + A2 단일 structured reconcile + raise 0 (TRUST-07 / D-08)
- 억제 자동검증이 cwd-stable + self-test + reason-leak 가드 실행 정적 .mjs(iter3 HIGH-1 / iter4 MEDIUM-2 / iter5 HIGH-2, false-green/red 차단) — typecheck-only 아님
- 전체 backend suite + mjs self-test/실행 exit 0 + tsc green, 회귀 0
</success_criteria>

<output>
Create `.planning/phases/20-v2-gemini/20-03-SUMMARY.md` when done — 토글 pipeline 소유(iter2 MEDIUM-1) + keep_local_video 게이트 확장(HIGH-1) + cap-mutation monkeypatch 증명(iter2 HIGH-1) + visionVeto(union)/scoreSuppressed/scoreSuppressedReason/scoreSuppressionAudit schema 키 3-way + ScoreSuppression discriminated type(iter4 MEDIUM-1) + 전용 resolver _score_suppression_reason(iter4 HIGH-1) + 단일 structured scoreSuppressionAudit reconcile(iter5 MEDIUM-2) + reason-owns-copy scoringBasisLabel(iter5 HIGH-2) + 점수카드 전체 억제(iter2 HIGH-2) + STRICTLY flag + producer-contract fail-loud(flag + reason, iter3 HIGH-2 / iter4 MEDIUM-1) + assert-result-score-suppression.mjs cwd-stable + self-test + reason-leak 가드 실 실행 단언(iter3 HIGH-1 / iter4 MEDIUM-2 / iter5 HIGH-2)(EAS 재빌드) + 실 정량 게이트(kip-up≤50, status=applied)는 20-04 Pod sweep 명시.
</output>
