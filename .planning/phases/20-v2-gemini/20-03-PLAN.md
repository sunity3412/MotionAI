---
phase: 20-v2-gemini
plan: 03
type: execute
wave: 2
depends_on: [20-01, 20-02]
files_modified:
  - backend/functions/pipeline/app.py
  - backend/tests/test_pipeline_mode3.py
  - app/src/types/analysis.ts
  - backend/shared/python/sunity_shared/models.py
  - docs/contract.md
  - app/src/app/analysis/result.tsx
autonomous: true
requirements: [SCORE-08, TRUST-06, TRUST-07, TRUST-08]
must_haves:
  truths:
    - "_apply_vision_veto 가 v1 identity(pass-through)에서 하향-전용 mutation 으로 전환된다 — capped < overallScore 일 때만 result['overallScore'] 를 낮추고 절대 안 올린다 (Mode1+Mode3 둘 다, 호출부 mode 분기 밖)"
    - "비전 veto 토글(_gemini_vision_veto_enabled)이 ON 이면 기존 Phase17 vision 토글이 OFF 라도 keep_local_video 가 True 라 local_video_path 가 보존된다 — veto 가 local_video_path None 으로 무음 no-op 되지 않는다 (HIGH-1)"
    - "거부권 결과가 visionVeto audit 필드(status enum, severity, capApplied, severityCapProvenance 참조)로 직렬화된다 — status ∈ {applied, not_applicable, disabled, skipped_error, missing_local_video}. visionVeto 부재가 'veto 가 돔'으로 읽히지 않고, terminal gate 가 status=applied 로 veto 실행을 증명할 수 있다 (HIGH-1 / 3-way lockstep)"
    - "Gemini/키 실패 시 v1 점수 graceful 통과 + WARNING 로그 + visionVeto.status=skipped_error 로 무음실패가 관측 가능하다 (silent no-op 차단, Pitfall 5)"
    - "Mode3 미보유(branch-3, is_reference_free) 시 confident 점수가 억제되고 scoringBasisLabel '기준 없음' 이 화면에 보인다 (TRUST-07/08)"
    - "recognizer 가 같은 reference 에 cache-hit 으로 같은 category(=같은 line 차원 분류)를 반환해 run 간 line 차원 변동이 0 이다 — temp 0 단독 아닌 TechniqueCache 가 결정성 보장 (C-1 / D-06 / TRUST-06, pod-free 단위 검증)"
    - "recognizer category(unregistered/low_confidence) ↔ is_reference_free_motion(ipsf_map) 정합이 검증된다 (A2)"
  artifacts:
    - path: "backend/functions/pipeline/app.py"
      provides: "_gemini_vision_veto_enabled 토글 + keep_local_video 게이트 확장(HIGH-1) + _apply_vision_veto 본체 swap (adapter 호출 + apply_downward_cap + visionVeto status enum audit + graceful 관측) + profile 인자 추가 + 단일 호출부 갱신 + Mode3 branch-3 억제"
      contains: "_gemini_vision_veto_enabled"
    - path: "app/src/types/analysis.ts"
      provides: "visionVeto optional audit 필드 (status enum, severity, capApplied) + AnalysisDoc/result 연결"
      contains: "visionVeto"
    - path: "app/src/app/analysis/result.tsx"
      provides: "Mode3 미보유 점수 억제 UX + scoringBasisLabel '기준 없음' 노출"
      contains: "scoringBasisLabel"
  key_links:
    - from: "pipeline keep_local_video gate"
      to: "_gemini_vision_veto_enabled()"
      via: "keep_local_video = _gemini_enabled() or _gemini_vision_enabled() or _gemini_vision_veto_enabled() (HIGH-1)"
      pattern: "_gemini_vision_veto_enabled"
    - from: "pipeline._apply_vision_veto"
      to: "vision_veto.apply_downward_cap + gemini_vision_scorer.assess_fault_severity"
      via: "worst_pose_timestamp(profile) → assess severity → apply_downward_cap(overallScore) → min cap + visionVeto status audit"
      pattern: "apply_downward_cap"
    - from: "pipeline visionVeto audit (status enum)"
      to: "analysis.ts + models.py + contract.md (3-way lockstep)"
      via: "optional visionVeto {status, severity, capApplied} 직렬화 — status 가 veto 실행 증명"
      pattern: "visionVeto"
    - from: "result.tsx Mode3 branch-3"
      to: "scoringBasisLabel + 점수 억제"
      via: "is_reference_free → confident octagon 억제 + '기준 없음' 노출"
      pattern: "scoringBasisLabel"
---

<objective>
20-01 의 순수 cap 코어 + 20-02 의 Gemini 어댑터를 **채점 path 에 통합**한다 — Phase 19 가 남긴 `_apply_vision_veto` identity hook 의 본체를 하향-전용 mutation 으로 swap. **pod-free**(adapter 는 mocked 단위테스트). Open Q2(visionVeto audit 필드) = 채택, status enum 으로 강화, 3-way lockstep. Open Q4(Mode3 branch-3 억제) 완성.

- **HIGH-1 (veto no-op 차단):** 신규 `_gemini_vision_veto_enabled()` 토글을 추가하고 **추출 보존 게이트(app.py:1833)에 포함** — `keep_local_video = _gemini_enabled() or _gemini_vision_enabled() or _gemini_vision_veto_enabled()`. 기존 Phase17 vision 토글이 OFF 라도 veto 만 ON 이면 local_video_path 가 보존돼 veto 가 None 으로 무음 no-op 되지 않는다. pod-free 테스트로 (Phase17 OFF + veto ON → adapter 가 non-None local_video_path 수신) 단언.
- **SCORE-08 (D-01/03/04/05 통합):** `_apply_vision_veto(score_result, local_video_path, angles, profile)` 본체 = worst_pose_timestamp(profile) → assess_fault_severity(local_video, at_seconds) → apply_downward_cap(score_result['overallScore'], severity) → capped < overall 일 때만 `min` 으로 낮춤. 호출부(app.py:2226)는 mode 분기 **밖** → Mode1+Mode3 둘 다 자동(D-03/04). 평균 금지(D-05 — worst-pose 단일).
- **TRUST-08 (status enum audit + 무음실패 방지 — HIGH-1):** veto 결과를 `visionVeto: {status, severity, capApplied}` 로 직렬화(3-way lockstep). **status ∈ {applied, not_applicable, disabled, skipped_error, missing_local_video}** — 단순 applied/absent 가 아니라 명시 enum. visionVeto 부재가 'veto 가 돔'으로 읽히지 않고, terminal gate(20-04)가 **status=applied 로 veto 실행을 증명**한다. 어댑터 실패 → v1 graceful 통과 + WARNING + status=skipped_error; local_video_path None → status=missing_local_video; veto OFF → status=disabled; cap 미적용(minor/None) → status=not_applicable. 기존 identity 테스트(`out is score_result`)를 하향-전용 + status property 테스트로 **전환**.
- **TRUST-07 (Mode3 미보유 억제):** Phase 19 가 라벨만 달았던 branch-3(is_reference_free) 에 **점수 억제** 추가 — confident octagon 억제 + scoringBasisLabel '기준 없음' 화면 노출. recognizer category(unregistered/low_confidence) ↔ is_reference_free_motion(ipsf_map) 정합 검증(A2).

Purpose: 위양성(kip-up 100/100)을 채점 path 에서 죽이되 v1 변별·정타는 안 건드리고, 미보유 confident 97 을 억제해 신뢰 회복. veto 가 **항상 실 영상으로 돌고**(HIGH-1) status enum 으로 실행이 증명되게 한다.
Output: app.py(_gemini_vision_veto_enabled + keep_local_video 게이트 + _apply_vision_veto 본체 + profile 인자 + 호출부 + branch-3 억제), test_pipeline_mode3.py(identity→하향전용+status 전환 + keep_local_video + audit + 정합), 3-way 계약(analysis.ts/models.py/contract.md visionVeto status enum), result.tsx(억제 UX).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/20-v2-gemini/20-CONTEXT.md
@.planning/phases/20-v2-gemini/20-RESEARCH.md
@.planning/phases/20-v2-gemini/20-VALIDATION.md
@backend/functions/pipeline/app.py
@backend/shared/python/sunity_shared/analysis/assemble.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: keep_local_video 게이트 확장(HIGH-1) + _apply_vision_veto 본체 swap — adapter 호출 + apply_downward_cap + visionVeto status enum audit + profile 인자 + 호출부 갱신 + graceful 관측 (identity→하향전용 전환)</name>
  <files>backend/functions/pipeline/app.py, backend/tests/test_pipeline_mode3.py</files>
  <behavior>
    - test_keep_local_video_for_veto_only (HIGH-1): 기존 Phase17 vision 토글(_gemini_enabled / _gemini_vision_enabled) OFF + `GEMINI_VISION_VETO_ENABLED=1` ON → keep_local_video True 라 추출이 local 영상을 보존하고, _apply_vision_veto 가 adapter(assess_fault_severity)에 **non-None local_video_path** 를 전달한다(mock 으로 호출 인자 검사). veto 가 None 으로 무음 no-op 되지 않음.
    - test_vision_veto_downward_only (전환 — 기존 test_vision_hook_passthrough 의 `out is score_result` identity 를 대체): adapter mock 이 severity='major'(cap<100) 반환 + overallScore=100 → out['overallScore'] <= 100 이고 capped 값. severity='minor'/None → out['overallScore'] == 입력(불변). **어떤 경우도 overallScore 가 입력보다 크지 않다** (property — 올림 0).
    - test_vision_veto_status_enum (HIGH-1): cap 적용 시 out['visionVeto']['status'] == 'applied' + {severity, capApplied}. cap 미적용(minor/None) → status == 'not_applicable'. veto OFF → status == 'disabled' (또는 visionVeto 부재 정책 명시). local_video_path None → status == 'missing_local_video'. adapter None(실패) → status == 'skipped_error'. status 가 veto 실행 여부를 명시 증명(부재 ≠ 실행).
    - test_vision_veto_graceful_observable: adapter 가 None(키부재/실패) → out['overallScore'] == v1 점수(불변, graceful) + WARNING 로그 1회 + visionVeto.status == 'skipped_error' (무음실패 관측 가능 — Pitfall 5).
    - test_vision_veto_called_both_modes: 호출부(app.py:2226)가 mode 분기 밖이라 Mode1(MODE_EXPERT) + Mode3(MODE_SELF) result 둘 다 _apply_vision_veto 통과 (D-03/04). profile 인자 전달 확인.
    - test_vision_veto_off_passthrough: _gemini_vision_veto_enabled() OFF → out['overallScore'] 불변 + adapter 미호출 + status == 'disabled'.
  </behavior>
  <read_first>
    - backend/functions/pipeline/app.py:1631-1661 (_apply_vision_veto 현재 def = v1 identity, docstring 1636-1655 = 'overallScore'/'dimensionScores' 키 박제 + graceful try/except 1656-1660, _gemini_vision_enabled 220-244, _VISION_ENV_DEFAULTS 209) + :2226 (단일 호출부 `result = _apply_vision_veto(result, local_video_path, angles)` — profile 인자 추가 대상; 이 줄이 mode 분기 밖임을 확인) + :2207 build_result 호출(result 산출) + **:1833 keep_local_video=_gemini_enabled() or _gemini_vision_enabled() (HIGH-1 게이트 확장 대상 — _gemini_vision_veto_enabled() 추가)** + recognize/profile 변수 위치(~1730)
    - backend/shared/python/sunity_shared/analysis/vision_veto.py (20-01 산출 — apply_downward_cap + worst_pose_timestamp + SEVERITY_CAP + SEVERITY_CAP_PROVENANCE)
    - backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py (20-02 산출 — assess_fault_severity → VisionVerdict | None, lazy-import, _gemini_vision_veto_enabled 토글 위치 확인 — pipeline 과 adapter 어느 쪽이 토글 소유인지 정합)
    - 20-DIRECT-REVIEW.md HIGH-1 (keep_local_video 게이트 + visionVeto status enum + missing_local_video 명시)
    - 20-RESEARCH.md §"Code Examples: Adapter call inside _apply_vision_veto" (본체 swap 예시 — profile 인자, visionVeto audit, graceful) + §"Pitfall 1"(averaging 금지 — terminal min 만) + §"Pitfall 5"(무음실패 관측) + §"State of the Art"(out is score_result → downward property 전환)
    - 20-CONTEXT.md D-01/03/04/05 + 객관성(score 필드 0 — visionVeto 는 status/severity/capApplied 만, 점수 라벨 아님)
    - backend/tests/test_pipeline_mode3.py:229-246 (현재 test_vision_hook_passthrough = `out is score_result` identity 단언 → 하향-전용 + status property 로 전환 대상; _apply_vision_veto getattr 가드 231-232)
    - 20-VALIDATION.md V-6(하향전용 invariant) / V-9(무음실패 audit status)
  </read_first>
  <action>
    **_gemini_vision_veto_enabled() 토글 (HIGH-1):** _VISION_ENV_DEFAULTS 패턴으로 신규 토글 추가(env `GEMINI_VISION_VETO_ENABLED`, default 결정은 belle 운영 — 20-RESEARCH Runtime State 정합). 토글 소유는 pipeline(app.py) 또는 20-02 adapter 중 한 곳 — 정합 확인(중복 정의 금지). pipeline 이 게이트와 _apply_vision_veto OFF 분기에서 사용.
    **keep_local_video 게이트 확장 (app.py:1833, HIGH-1):** `keep_local_video = _gemini_enabled() or _gemini_vision_enabled() or _gemini_vision_veto_enabled()` 로 변경 — veto 만 ON 인 경우에도 local_video_path 가 보존됨. 주석에 "HIGH-1 — veto 가 local_video_path None 으로 무음 no-op 되지 않도록 보존 게이트에 veto 토글 포함" 박제.
    **_apply_vision_veto 본체 swap (app.py:1631):** 시그니처에 `profile: technique.TechniqueProfile | None = None` 추가. 본체를:
      (1) `not _gemini_vision_veto_enabled()` → `return _veto_passthrough(score_result, status="disabled")` (OFF 시 adapter 미호출). `local_video_path is None` → `return _veto_passthrough(score_result, status="missing_local_video")` (graceful, HIGH-1 — 부재 신호 명시).
      (2) `from sunity_shared.analysis import vision_veto, gemini_vision_scorer` lazy(또는 _ensure_adapters 패턴). `at = vision_veto.worst_pose_timestamp(profile)`. `verdict = gemini_vision_scorer.assess_fault_severity(local_video_path, at_seconds=at)`.
      (3) verdict None(실패/키부재) → `log.warning("vision veto 미실행 — verdict None, v1 점수 통과 (graceful, 무음실패 관측)")` + `return _veto_passthrough(score_result, status="skipped_error")` (Pitfall 5 관측 가능).
      (4) `capped = vision_veto.apply_downward_cap(score_result["overallScore"], verdict.severity)`. `if capped < score_result["overallScore"]:` → `score_result = {**score_result, "overallScore": capped, "visionVeto": {"status": "applied", "severity": verdict.severity, "capApplied": capped}}` (audit 직렬화). 아니면 status="not_applicable" 직렬화(또는 정책상 visionVeto.status='not_applicable' 만 — 점수 불변).
      (5) `return score_result`. **올림 경로 절대 0** (apply_downward_cap 이 min 만). **평균 금지** — dimension_scores 재집계 없이 terminal cap 만(Pitfall 1). graceful boundary(except → WARNING + status='skipped_error' 반환) 유지. status enum 직렬화 헬퍼(`_veto_passthrough(result, status)`)로 5개 status 일관 박제. docstring 을 "v2 = 하향-전용 mutation (capped < overall 시에만 낮춤, 절대 안 올림) + visionVeto.status 가 veto 실행을 증명(부재≠실행). v1 identity 계약은 의도적 전환(20-03)" 로 갱신.
    **호출부 갱신 (app.py:2226):** `result = _apply_vision_veto(result, local_video_path, angles, profile)` 로 profile 전달(A4 — 단일 호출부, grep 으로 다른 호출부 0 확인). 이 줄이 MODE_EXPERT/MODE_SELF 분기 **밖**이라 Mode1+Mode3 둘 다 적용됨을 주석으로 박제.
    **테스트 전환 (test_pipeline_mode3.py):** 기존 test_vision_hook_passthrough(229-246) 의 `out is score_result` identity 단언을 **삭제·전환** → behavior 6 케이스로 교체(adapter mock 으로 severity 주입, env monkeypatch 로 토글 제어). 이는 "무관한 케이스 회귀 0" 예외가 아니라 **v2 계약 전환에 직접 영향받는 케이스의 정당한 전환**(20-RESEARCH State of the Art: identity→downward property 명시). adapter/_gemini_vision_scorer 는 monkeypatch(실 Gemini 미호출). test_keep_local_video_for_veto_only 는 추출 보존 경로(1833)와 adapter 호출 인자(non-None path)를 함께 단언.
    from __future__ import annotations, snake_case, 한국어 docstring(why), 이모지 금지, graceful BLE001 패턴 박제.
  </action>
  <verify>
    <automated>cd backend && PYTHONPATH=shared/python python -m pytest tests/test_pipeline_mode3.py -k "vision_veto or keep_local_video" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_pipeline_mode3.py vision_veto + keep_local_video 케이스 6개 GREEN (전부 mocked adapter — 실 Gemini/Pod 미호출)
    - Phase17 vision OFF + veto ON → keep_local_video True + adapter 가 non-None local_video_path 수신 (test_keep_local_video_for_veto_only GREEN — HIGH-1; grep: app.py:1833 게이트에 `_gemini_vision_veto_enabled()` 포함)
    - _apply_vision_veto 가 capped < overallScore 일 때만 낮추고 절대 안 올림 (test_vision_veto_downward_only GREEN — grep: _apply_vision_veto 본체에 `max(`/가중블렌드 0건, apply_downward_cap 경유 min 만)
    - visionVeto.status enum ∈ {applied, not_applicable, disabled, skipped_error, missing_local_video} 직렬화 — 부재≠실행 (test_vision_veto_status_enum GREEN — HIGH-1). visionVeto 에 score/점수 필드 0 (status/severity enum + capApplied 정수만 — 객관성)
    - adapter None → v1 graceful 통과 + WARNING + visionVeto.status='skipped_error' (test_vision_veto_graceful_observable GREEN — Pitfall 5 무음실패 관측)
    - 호출부 단일(app.py:2226) + mode 분기 밖 → Mode1+Mode3 둘 다 통과, profile 전달 (test_vision_veto_called_both_modes GREEN — grep: `_apply_vision_veto(` 호출 1건 + profile 인자)
    - OFF 토글 시 adapter 미호출 + 불변 + status='disabled' (test_vision_veto_off_passthrough GREEN)
    - 무관한 backend suite 회귀 0 (pre-existing 격리 제외)
  </acceptance_criteria>
  <done>keep_local_video 게이트에 _gemini_vision_veto_enabled 포함(HIGH-1 — veto no-op 차단) + _apply_vision_veto v1 identity → 하향-전용 mutation 전환 (apply_downward_cap min cap + visionVeto status enum audit + worst_pose profile + graceful 관측 + 단일 호출부 Mode1/Mode3). 올림 0, 평균 0. 6 mocked 테스트 GREEN. 회귀 0.</done>
</task>

<task type="auto">
  <name>Task 2: visionVeto status enum audit 필드 3-way 계약 lockstep (analysis.ts + models.py + contract.md) + Firestore flat 저장 검증</name>
  <files>app/src/types/analysis.ts, backend/shared/python/sunity_shared/models.py, docs/contract.md</files>
  <read_first>
    - app/src/types/analysis.ts (AnalysisDoc.result 구조 + overallScore 위치 + scoringBasis Mode1/Mode3 239-272 패턴 = optional audit 필드 추가 컨벤션, import type)
    - backend/shared/python/sunity_shared/models.py (SCORE_DIMENSIONS 20, MODE3_SCORING_BASES 62, comparison 키 상수/주석 패턴 — visionVeto status enum 상수 추가 위치)
    - docs/contract.md:161 (overallScore 정의 = min-of-core) + :246-272 (Mode1/Mode3 Comparison optional 필드 패턴 — visionVeto 추가 위치) + result 구조 섹션
    - 20-DIRECT-REVIEW.md HIGH-1 (visionVeto status enum {applied, not_applicable, disabled, skipped_error, missing_local_video} — 3-way 계약 변경)
    - 20-RESEARCH.md §"Runtime State Inventory"(visionVeto {status, severity, capApplied} optional, 3-way lockstep 필요) + §"Open Questions Q2"(audit 필드 채택 권장)
    - backend/shared/python/sunity_shared/firestore_admin.py (complete_analysis flat 저장 + nested-array 금지 — visionVeto 는 scalar dict {status:str, severity:str, capApplied:int} 라 flat 정합; 검증 validator 패턴)
    - 20-CONTEXT.md 객관성(visionVeto severity/capApplied = 임계 수치 라벨링 OK, 사람 점수 라벨 아님)
  </read_first>
  <action>
    **3-way lockstep — visionVeto status enum optional audit 필드:**
    - app/src/types/analysis.ts: AnalysisDoc 의 result(또는 overallScore 인접)에 `visionVeto?: { status: 'applied' | 'not_applicable' | 'disabled' | 'skipped_error' | 'missing_local_video'; severity?: 'minor' | 'moderate' | 'major'; capApplied?: number };` 추가(OPTIONAL — legacy doc 호환). status 는 항상 존재(veto 가 돌면), severity/capApplied 는 status='applied' 일 때만. 주석에 "Phase 20 SCORE-08 — 비전 하향 거부권 audit. **status 가 veto 실행을 증명**(부재≠실행, HIGH-1). severity=결함 심각도, capApplied=하향된 종합점수. **점수 라벨 아님**(객관성 — enum + 임계 산출 정수)" 명시.
    - backend/shared/python/sunity_shared/models.py: visionVeto status enum 상수(VISION_VETO_STATUSES = ['applied','not_applicable','disabled','skipped_error','missing_local_video']) + severity/capApplied 키 상수/주석 추가. "Phase 20 — 비전 거부권 audit. status 가 실행 증명, applied 시에만 severity/capApplied" 주석.
    - docs/contract.md: result 구조(overallScore 161 인접)에 visionVeto optional `{ status, severity?, capApplied? }` 정의 + status enum 5값 + "Phase 20 SCORE-08 — status 가 veto 실행 증명(부재≠실행), 객관성: 사람 점수 라벨 아님" 명시.
    **Firestore flat 검증:** visionVeto 는 scalar dict({status:str, severity:str, capApplied:int}) 라 nested-array 금지 정합(firestore_admin flat 저장 위반 0). 별도 nested validator 변경 불필요하나, complete_analysis 가 result 통째 저장 시 visionVeto 가 flat scalar dict 임을 주석으로 박제.
    이모지 금지, 2-space indent, single quote, import type, theme 무관(타입만).
  </action>
  <verify>
    <automated>cd app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && npm run typecheck` (tsc --noEmit) clean
    - visionVeto status enum optional 필드가 analysis.ts + models.py + contract.md 3곳 동시 정의 (3-way lockstep — grep: 세 파일 모두 `visionVeto` + status enum 5값 + severity/capApplied)
    - status enum = {applied, not_applicable, disabled, skipped_error, missing_local_video} 3곳 정합 (HIGH-1)
    - visionVeto 에 score/점수/rating 필드 0 (status/severity enum + capApplied 정수만 — 객관성)
    - capApplied 타입 number(TS)/int(Python) + severity union/enum 정합 (Task 1 직렬화 shape 과 일치)
    - Firestore flat 정합 — visionVeto = scalar dict (nested-array 0, firestore_admin 위반 0)
  </acceptance_criteria>
  <done>visionVeto {status enum, severity, capApplied} optional audit 3-way 계약 lockstep + Firestore flat 정합. status 가 veto 실행 증명(HIGH-1). 객관성(점수 라벨 0). tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Mode3 미보유(branch-3) 점수 억제 + recognizer↔ipsf_map 정합(A2) + result.tsx 억제 UX</name>
  <files>backend/functions/pipeline/app.py, backend/tests/test_pipeline_mode3.py, app/src/app/analysis/result.tsx</files>
  <behavior>
    - test_branch3_score_suppressed: MODE_SELF + is_reference_free_motion True(미보유) → result 가 confident overallScore 를 억제하는 신호(예 scoreSuppressed=True 또는 scoringBasis=reference_free_absolute + 억제 플래그) 를 emit. 미보유에 confident 97 금지(D-08).
    - test_recognizer_category_agrees_with_ipsf_map (A2): recognizer category ∈ {unregistered, low_confidence} 이면 is_reference_free_motion(branch_info) 도 True 와 정합(둘이 어긋나면 명시 reconcile). raise 0 (fail-closed 금지).
    - test_branch3_no_raise: 미보유 동작이 raise 안 하고 점수+scoringBasisLabel 반환 (motion-routing-generalize-principle).
    - test_recognizer_line_dim_determinism (C-1 / D-06 / criterion #4 / TRUST-06): 같은 reference(동일 video-hash + model + yaml_version 캐시 키) 2회 recognizer 호출 → 2번째는 TechniqueCache hit(Gemini 호출 0) → 같은 category 반환 → 같은 line 차원 분류. run 간 line 차원 변동 0 (temp 0 단독 아님 — 캐시가 실 보장, Pitfall 2). mocked-Gemini, **pod-free**.
  </behavior>
  <read_first>
    - backend/functions/pipeline/app.py:1693-1790 (_mode3_scoring_basis 1693 = is_first/is_reference_free → basis, is_reference_free=assemble.is_reference_free_motion(branch_info) 1736, first_basis 1756, progress_basis 1789, MODE_SELF 게이트) + branch_info early lookup 위치
    - backend/shared/python/sunity_shared/analysis/assemble.py:130-145 (is_reference_free_motion = angleSource/angleFixtureKey/ipsfCode/officialName 판정, copyBranch 금지) + scoringBasis 상수 576-617 + build_mode3 scoring_basis 인자
    - backend/shared/python/sunity_shared/analysis/gemini_technique_recognizer.py:15-18,124-141 (category enum recognized/api_failure/low_confidence/unregistered — A2 정합 대상: is_reference_free 는 ipsf_map branch_info 키, category 는 recognizer 출력 → 둘 reconcile) + cache wiring 77/115-226 (TechniqueCache hit → 같은 category, line 차원 결정성 C-1)
    - backend/shared/python/sunity_shared/analysis/technique_cache.py (compute_video_hash SHA256 42-62, compute_yaml_version 98, TechniqueCache 166 + cache key (video_hash, model_name, yaml_version) 172 — recognizer 결정성 캐시, C-1. **주의: 이는 recognizer 캐시 — vision veto severity 캐시(20-02 VisionVetoCache)와 별개**)
    - 20-VALIDATION.md V-4(결정론 — recognizer line 차원 cache-hit 결정성 단위 포함) + 20-RESEARCH.md §"Pitfall 2"(temp 0 ≠ bit-deterministic, 캐시가 실 보장)
    - 20-RESEARCH.md §"Pattern 4: Mode3 3-branch"(category enum + is_reference_free + scoringBasis 재사용, gap=score suppression) + §"Pitfall 4"(branch-3 confident 점수 차단) + §"Assumptions A2"(category↔ipsf_map 정합 — planner 검증 필수)
    - 20-CONTEXT.md D-07(3분기 recognizer) / D-08(억제 + 기준없음, confident 97 금지) + Claude's Discretion(억제 UX 강도 = plan 확정, belle = "점수 억제 + 기준없음")
    - app/src/app/analysis/result.tsx:443 (scoreGrade) + :679-683 (scoringBasisLabel 렌더 — 이미 존재) + :737 (OctagonScore overallScore — 억제 분기 위치) + :742 (LevelBenchmark) + :491-496 (mode3 분기)
    - app/src/types/analysis.ts (scoringBasis Mode3 4-value + scoreSuppressed 류 신규 optional 필드 추가 위치 — Task 2 의 visionVeto 와 별개)
    - 20-VALIDATION.md V-8(Mode3 미보유 억제 + 기준없음 단위테스트)
  </read_first>
  <action>
    **branch-3 점수 억제 (backend):** _mode3_scoring_basis / MODE_SELF 경로에서 is_reference_free_motion True(미보유) 일 때 result 에 **억제 신호** emit — Claude's Discretion 결정 = `scoreSuppressed: true`(또는 동등 플래그) optional 필드를 result/Mode3Comparison 에 추가하고, scoringBasis=reference_free_absolute + scoringBasisLabel "이 동작은 기준 데이터가 없어 정확한 점수를 드릴 수 없어요" 류(D-08 belle 카피 정합). **confident overallScore 를 0 으로 만들거나 raise 하지 않는다** — 점수는 산출하되 화면이 confident 표시를 억제(result.tsx Task 3 후반). fail-closed/raise 금지([[motion-routing-generalize-principle]]). 억제 플래그는 Task 2 의 3-way 계약 패턴으로 analysis.ts 에 optional 추가(scoreSuppressed?: boolean + 주석).
    **A2 정합 검증 (backend):** recognizer category(unregistered/low_confidence) ↔ is_reference_free_motion(ipsf_map branch_info) 이 어긋날 수 있음(하나는 recognizer 출력, 하나는 motion_ipsf_map.json) — _process 에서 둘을 reconcile: category ∈ {unregistered, low_confidence} 이거나 is_reference_free_motion True 이면 미보유 처리(OR 결합, 안전측). test_recognizer_category_agrees_with_ipsf_map 으로 정합/reconcile 단언. 주석에 "A2 — 두 신호 출처 다름, OR 로 미보유 안전 결합" 박제.
    **result.tsx 억제 UX (app):** mode3 + (scoreSuppressed 또는 scoringBasis reference-free) 일 때 OctagonScore(737)/LevelBenchmark(742) confident 표시를 억제 — 점수 octagon 을 회색/배너 처리 또는 숨김(belle = "점수 억제 + 기준 없음"). scoringBasisLabel(679-683 이미 렌더)이 "기준 없음" 카피를 노출. 억제 아닐 때(recognized/known) 기존 표시 유지. theme 토큰만(색/스페이싱 하드코딩 금지, #FF4B33 직접 금지), graceful(필드 부재 시 기존 동작).
    이모지 금지, snake_case(py)/camelCase(ts), import type, 한국어 카피.
  </action>
  <verify>
    <automated>cd backend && PYTHONPATH=shared/python python -m pytest tests/test_pipeline_mode3.py -k "branch3 or reference_free or recognizer_category or line_dim_determinism" -x -q && cd ../app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - test_pipeline_mode3.py branch-3 케이스 3개 GREEN + `cd app && npm run typecheck` clean
    - 미보유(is_reference_free True) → confident 점수 억제 신호 emit + scoringBasisLabel '기준 없음' (test_branch3_score_suppressed GREEN — D-08, confident 97 금지)
    - recognizer category(unregistered/low_confidence) ↔ is_reference_free_motion OR 결합으로 미보유 안전 처리 (test_recognizer_category_agrees_with_ipsf_map GREEN — A2 reconcile)
    - 같은 reference 2회 recognizer 호출 → 캐시 hit(Gemini 호출 0) → 같은 category/line 분류 = run 간 line 차원 변동 0 (test_recognizer_line_dim_determinism GREEN — C-1 / D-06 / TRUST-06, pod-free)
    - 미보유가 raise 0 + 점수+scoringBasisLabel 반환 (test_branch3_no_raise GREEN — fail-closed 금지)
    - result.tsx 가 mode3 미보유 시 OctagonScore confident 표시 억제 + scoringBasisLabel 노출 (grep: scoreSuppressed 또는 scoringBasis reference-free 분기 + OctagonScore 억제)
    - theme 토큰만 (grep: 신규 추가 라인에 `#` hex/매직 px 0건)
    - 무관한 backend suite 회귀 0 + tsc clean
  </acceptance_criteria>
  <done>Mode3 미보유 점수 억제(scoreSuppressed + reference_free_absolute + 기준없음 카피) + A2 category↔ipsf_map OR reconcile(raise 0) + recognizer line 차원 cache-hit 결정성(C-1/D-06/TRUST-06, line 변동 0) + result.tsx 억제 UX. confident 97 차단. 4 테스트 GREEN + tsc clean. 회귀 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

pipeline _process(SQS consumer, 인증 후 서버측) → Gemini 어댑터(20-02 경계) + ML core(20-01 순수) + Firestore flat 저장 + 앱 표시. 신규 endpoint/auth 0. visionVeto/scoreSuppressed = optional scalar 필드(nested-array 0).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-09 | Tampering / 신뢰성 | 비전이 채점 path 에서 점수를 **올림** (D-01 위반, 위양성 재발) | mitigate | _apply_vision_veto = apply_downward_cap(min) 경유, test_vision_veto_downward_only ∀입력 출력≤입력 + grep `max(` 0건 |
| T-20-09b | Tampering / 신뢰성 | veto ON 인데 local_video_path 보존 안 돼 무음 no-op (위양성 생존, HIGH-1) | mitigate | keep_local_video 게이트에 _gemini_vision_veto_enabled() 포함 + test_keep_local_video_for_veto_only (non-None path) + visionVeto.status='missing_local_video' 신호 |
| T-20-10 | DoS / integrity | Gemini 실패 → silent gate bypass (kip-up 위양성 prod 통과) | mitigate | graceful 통과 BUT WARNING 로그 + visionVeto.status='skipped_error' 신호 (관측 가능, Pitfall 5). 실 검출 = 20-04 eval cache-warm + status=applied 증명 |
| T-20-11 | Information disclosure / 신뢰성 | Mode3 미보유 confident 97 (기준 없음인데 권위적 숫자, 신뢰 파괴) | mitigate | branch-3 score suppression + scoringBasisLabel 기준없음 화면 노출 (Pitfall 4 / D-08) |
| T-20-12 | DoS | 미보유 fail-closed raise → 분석 차단 | mitigate | OR reconcile 후 점수+근거 반환, raise 0 ([[motion-routing-generalize-principle]]) |
| T-20-13 | Tampering / 객관성 | visionVeto audit 에 사람 점수 라벨 누출 | mitigate | visionVeto = status/severity enum + capApplied(임계 산출 정수)만, score 필드 0 (3-way 계약 + grep) |
| T-20-14 | Integrity | recognizer category ↔ ipsf_map 불일치로 branch-3 오발 (A2) | mitigate | OR 결합 reconcile + test_recognizer_category_agrees_with_ipsf_map 단언 |
| T-20-SC | Tampering | npm/pip installs | accept | 신규 패키지 0 (20-01/20-02 모듈 + 기존 adapter). Package Legitimacy Audit N/A |

내부 오케스트레이션 + optional 계약 필드 + 표시 억제 — 위협 표면 낮음.
</threat_model>

<verification>
- `cd backend && PYTHONPATH=shared/python python -m pytest tests/test_pipeline_mode3.py -x -q` GREEN (vision_veto+keep_local_video 전환 + branch-3 + A2, 전부 mocked adapter)
- `cd backend && PYTHONPATH=shared/python python -m pytest tests/ -q` 전체 회귀 0
- `cd app && npm run typecheck` clean
- visionVeto(status enum)/scoreSuppressed schema 키 추가 → EAS 재빌드 (optional 키)
- 실 거부권 동작(kip-up≤50) + visionVeto.status='applied' 증명은 20-04 Pod sweep 에서 정량 확정 — 본 plan 은 mocked 로직만
</verification>

<success_criteria>
- keep_local_video 게이트에 veto 토글 포함 (HIGH-1 — veto no-op 차단) + _apply_vision_veto 하향-전용 mutation 통합 (SCORE-08 / D-01/03/04/05) — Mode1+Mode3, 올림 0, 평균 0
- visionVeto status enum audit 3-way lockstep (부재≠실행) + graceful 무음실패 관측 (TRUST-08 / Pitfall 5 / HIGH-1)
- Mode3 미보유 점수 억제 + 기준없음 화면 + A2 reconcile + raise 0 (TRUST-07 / D-08)
- 전체 backend suite + tsc green, 회귀 0
</success_criteria>

<output>
Create `.planning/phases/20-v2-gemini/20-03-SUMMARY.md` when done — keep_local_video 게이트 확장(HIGH-1) + visionVeto status enum/scoreSuppressed schema 키 추가(EAS 재빌드) + 실 정량 게이트(kip-up≤50, status=applied)는 20-04 Pod sweep 명시.
</output>
