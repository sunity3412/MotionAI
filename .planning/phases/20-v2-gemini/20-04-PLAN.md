---
phase: 20-v2-gemini
plan: 04
type: execute
wave: 3
depends_on: [20-01, 20-02, 20-03]
files_modified:
  - backend/evals/phase20/derive_caps.py
  - backend/evals/phase20/sensitivity.yaml
  - backend/evals/phase20/assert_baseline_v2.py
  - backend/evals/phase20/README.md
  - backend/shared/python/sunity_shared/analysis/vision_veto.py
  - backend/evals/phase20/baseline/eval20_serial_baseline.json
autonomous: false
requirements: [SCORE-09, SCORE-08, TRUST-06]
must_haves:
  truths:
    - "SEVERITY_CAP 의 moderate/major 수치가 IPSF severity 의미 + sensitivity 셋(미보유+above-cutoff)에서 도출되고 6페어에 curve-fit 되지 않는다 (D-02 — 6페어는 known-answer 회귀, fit 타깃 아님)"
    - "Pod serial sweep 후 kip-up fault 영상이 ≤50 (known_false_positive → discriminate 전환, V-1)"
    - "변별 4쌍(power-spin/peter-pan/elbow-twist/pdshape) 퇴행 0 (fault<success margin>0 유지, V-2)"
    - "정은지 정타가 95~100 유지 (비전이 깨끗한 자세 안 깎음, V-3)"
    - "sensitivity 셋(미보유+above-cutoff) 위양성↔위음성 양방 정확 — above-cutoff 가 잘못 안 떨어진다 (V-5 일반화)"
    - "결정론 — cache-warm 재실행 byte-identical (temp 0 단독 아님, 캐시 보장, V-4 실 점수)"
  artifacts:
    - path: "backend/evals/phase20/derive_caps.py"
      provides: "SEVERITY_CAP 도출 방법론 스크립트 — IPSF severity 의미 + sensitivity 셋 양방 게이트 (6페어 curve-fit 금지, 도출 근거 박제)"
      contains: "def derive"
    - path: "backend/evals/phase20/assert_baseline_v2.py"
      provides: "v2 known-answer 게이트 — kip-up≤50 + 변별 4쌍 퇴행0 + 정타 95~100 + sensitivity 양방 + 결정론"
      contains: "kip_up"
    - path: "backend/evals/phase20/baseline/eval20_serial_baseline.json"
      provides: "v2 serial baseline 재스냅샷 (kip-up verdict flip + 4쌍 유지 + climb gate 유지)"
      contains: "discriminate"
  key_links:
    - from: "derive_caps.py"
      to: "vision_veto.SEVERITY_CAP (20-01 placeholder)"
      via: "도출 수치를 SEVERITY_CAP moderate/major 에 박제 (출처 주석 = sensitivity 셋, 6페어 아님)"
      pattern: "SEVERITY_CAP"
    - from: "sweep_phase15.py --mode mode1 --pair-sequential (Pod serial)"
      to: "assert_baseline_v2.py + eval20_serial_baseline.json"
      via: "serial sweep 산출 ↔ v2 baseline 대조 (kip-up flip + 4쌍 유지)"
      pattern: "pair-sequential"
---

<objective>
**[POD 의존 — TERMINAL GATE]** 이 plan 은 RunPod RTMW GPU 가 **필수**다(현재 down). 20-01~20-03 의 pod-free 코드가 전부 GREEN 인 뒤에만 진입한다. 두 가지를 한다: (1) **SEVERITY_CAP 수치를 generalization-tested eval 로 도출**(D-02 — 6페어 curve-fit 금지, 이 plan 이 placeholder 를 채우는 **유일한** 자리), (2) **serial Pod sweep 으로 belle 스펙 게이트 정량 검증**(kip-up≤50 + 변별 퇴행0 + 정타 95~100 + sensitivity 양방 + 결정론).

- **SCORE-09 (D-02 일반화 hard gate):** `derive_caps.py` = IPSF severity 의미(minor/moderate/major 결함 무효화 강도) + sensitivity 셋(미보유+above-cutoff, 고득점이어야 정상) 양방 게이트에서 cap 도출. 6페어는 **known-answer 회귀**(검증), fit 타깃 아님. 위양성(fault 하락)↔위음성(above-cutoff 유지) 둘 다 게이트. 도출 근거를 SEVERITY_CAP 출처 주석 + README 에 박제.
- **SCORE-08 정량 확정 (V-1/2/3):** Pod serial sweep → v2 baseline 재스냅샷. kip-up known_false_positive→discriminate, 4쌍 margin>0 유지, climb known_gate_blocked 유지, 정타 95~100.
- **TRUST-06 (V-4 실 점수 결정론):** cache-warm 재실행 byte-identical 게이트. cache-cold 변동은 정보성(Pitfall 2 + Open Q4 경계).
- **동시성 hard gate:** eval **순차만**(`--pair-sequential`) — _process 동시성 비안전, 동시 트리거=cross-contamination([[pipeline-not-concurrency-safe-eval-serial]]).

> ⚠️ **Pod silent-skip 금지.** Pod down 이면 Task 2(sweep)는 **blocking checkpoint** 로 멈춘다 — pod-free Task 1(도출 방법론 + 게이트 하니스 + sensitivity manifest)까지만 진행하고 belle/Pod 재개를 기다린다. Task 1 은 sweep 없이도 도출 *방법론* + 가드를 박제(수치 확정은 Pod 데이터 필요).

Purpose: belle "절대 실수 없게" — 비전 cap 을 6페어에 짜맞추면 일반화 실패(위양성↔위음성 한쪽만). sensitivity 양방 게이트가 유일한 안전 도출. Pod 정량 검증이 최종 신뢰 게이트.
Output: backend/evals/phase20/(derive_caps.py + sensitivity.yaml + assert_baseline_v2.py + README + baseline/eval20_serial_baseline.json) + vision_veto.py SEVERITY_CAP 수치 박제.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/20-v2-gemini/20-CONTEXT.md
@.planning/phases/20-v2-gemini/20-RESEARCH.md
@.planning/phases/20-v2-gemini/20-VALIDATION.md
@backend/evals/phase18/dataset/pairs.yaml
@backend/evals/phase18/baseline/eval18_serial_baseline.json
@backend/evals/phase18/assert_baseline.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: [POD-FREE] SEVERITY_CAP 도출 방법론(derive_caps.py) + sensitivity 셋 manifest + v2 게이트 하니스(assert_baseline_v2.py) + README — 수치 확정 전 구조/방법론 박제</name>
  <files>backend/evals/phase20/derive_caps.py, backend/evals/phase20/sensitivity.yaml, backend/evals/phase20/assert_baseline_v2.py, backend/evals/phase20/README.md</files>
  <read_first>
    - backend/evals/phase18/assert_baseline.py 전체 (게이트 의미론: expected↔baseline 정합, discriminate margin>0, known_false_positive/known_gate_blocked 명시 추적, exit 0/non-zero — v2 하니스가 확장할 패턴)
    - backend/evals/phase18/dataset/pairs.yaml 전체 (6페어 구조 + 객관성 하드가드 주석 + 일반화 경계 주석 + 동시성 주석 — sensitivity.yaml 이 모방할 fixture 형식)
    - backend/evals/phase18/baseline/eval18_serial_baseline.json (kip-up known_false_positive 100/100/margin0, 4쌍 discriminate margin 28/21/41/42, climb known_gate_blocked — v2 baseline 이 대조할 v1 스냅샷)
    - backend/shared/python/sunity_shared/analysis/vision_veto.py (20-01 — SEVERITY_CAP placeholder None + 출처 주석 자리; derive_caps 가 채울 대상)
    - 20-RESEARCH.md §"Pitfall 3"(curve-fit 금지 — IPSF severity 의미 + sensitivity 양방, 6페어는 verify) + §"Validation Architecture: Phase gate"(kip-up flip + 4쌍 유지 + climb 유지 + sensitivity) + §"Open Q3"(severity 3버킷 vs 4번째 severe — sensitivity 에서 도출) + §"Open Q4"(결정론 cache-warm/cold 경계)
    - 20-CONTEXT.md D-02(curve-fit 금지) + specifics(sensitivity 미보유+above-cutoff 양방) + [[scoring-redesign-must-generalize-no-overfit]] [[sensitivity-gate-not-just-elite-low]] [[calibration-source-hard-gate]](inherited evidence/repo artifact 만, 자기 sweep 재calibrate 금지)
    - .planning/phases/19-vision-hybrid/19-IPSF-DEDUCTION-NOTES.md (IPSF 감점 의미론 — severity→무효화 강도 도출 근거)
    - 20-VALIDATION.md V-5(sensitivity 일반화) / V-1/2/3 게이트 의미론
  </read_first>
  <action>
    신규 디렉터리 `backend/evals/phase20/`.
    **derive_caps.py (도출 방법론):** SEVERITY_CAP 의 moderate/major 수치를 **도출하는 절차**를 스크립트로 박제 — 입력 = (1) IPSF severity 의미론(19-IPSF-DEDUCTION-NOTES: major=요소 무효화 강도 → ≤50 류, moderate=부분 감점) + (2) sensitivity 셋 양방 게이트(미보유+above-cutoff). **6페어를 fit 타깃으로 쓰지 않는다** — 6페어는 도출된 cap 의 *검증*(회귀)에만. derive() 가 sensitivity 셋에서 "above-cutoff 가 안 떨어지는 최대 cap" + "fault 가 ≤50 되는 최소 강도" 를 양방 만족하는 cap 산출. **수치 확정은 Pod sensitivity 데이터 필요** — Pod 없으면 derive() 는 방법론 + placeholder 검증만(Task 2 가 실행). 출처 주석 강제: "cap 출처 = IPSF severity + sensitivity 셋, NOT 6페어 curve-fit ([[scoring-redesign-must-generalize-no-overfit]])". [[calibration-source-hard-gate]] 정합 — 자기 sweep 재calibrate 금지(inherited evidence/repo artifact 만).
    **sensitivity.yaml (manifest):** 미보유 동작 + above-cutoff(고득점이어야 정상) 케이스 manifest — pairs.yaml 형식 모방. **자산 수집은 Deferred**(20-CONTEXT) 이므로 이 파일은 *요구사항 manifest*(어떤 케이스가 필요한지 + 기대 verdict: above-cutoff→점수 안 떨어짐, 미보유→억제) 를 박제하고, 실 영상 키는 belle 수집 후 채움(TODO 마커 + 객관성 가드 주석). 사람 점수 라벨 금지(기대 = "떨어지면 안 됨/억제" 방향성만, 숫자 라벨 X).
    **assert_baseline_v2.py (게이트 하니스):** phase18 assert_baseline.py 확장 — (1) kip-up verdict known_false_positive→discriminate 검증, (2) 4쌍 discriminate margin>0 유지, (3) climb known_gate_blocked 유지, (4) 정타 95~100 범위, (5) sensitivity above-cutoff 가 cap 후에도 high 유지(위음성 0), (6) 결정론 cache-warm byte-identity. Pod baseline 부재 시 self-consistency(방법론/manifest 구조)만 검사 + "Pod sweep 대기" 명시 exit. exit 0/non-zero 게이트 의미론.
    **README.md:** 게이트 의미론 + 도출 근거(6페어=회귀, sensitivity=도출) + 동시성(순차만) + Pod 실행 절차(Task 2) 박제.
    from __future__ import annotations, snake_case, 한국어 docstring, 이모지 금지. 사람 점수 라벨 ground truth 영구 금지(객관성).
  </action>
  <verify>
    <automated>cd backend && PYTHONPATH=shared/python python evals/phase20/assert_baseline_v2.py; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - assert_baseline_v2.py 가 Pod baseline 부재 시 self-consistency(방법론/manifest/게이트 의미론) 검사 통과 + "Pod sweep 대기" 명시 (exit 의미 명확)
    - derive_caps.py 가 cap 을 sensitivity 양방 + IPSF severity 에서 도출하는 절차 (grep: derive_caps.py 에 "sensitivity" + "IPSF" 근거 + "curve-fit 금지"/"6페어 회귀" 주석; 6페어를 fit 입력으로 쓰는 경로 0)
    - sensitivity.yaml 이 미보유+above-cutoff 케이스 manifest + 기대 방향(above-cutoff 안 떨어짐 / 미보유 억제) + 객관성 가드 주석 (사람 점수 라벨 0 — grep: 숫자 점수 라벨 0)
    - assert_baseline_v2.py 게이트 = kip-up flip + 4쌍 margin>0 + climb gate + 정타 95~100 + sensitivity 양방 + 결정론 (6 게이트 코드 존재)
    - README 에 6페어=회귀/sensitivity=도출 + 순차만 + Pod 절차 박제
    - 무관한 backend suite 회귀 0
  </acceptance_criteria>
  <done>derive_caps.py(도출 방법론 — IPSF+sensitivity 양방, 6페어 fit 금지) + sensitivity.yaml manifest + assert_baseline_v2.py(6 게이트) + README. Pod 수치 확정 전 구조/방법론/객관성 박제. self-consistency GREEN.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: [POD 의존 — TERMINAL] RunPod RTMW 재개 + serial sweep → SEVERITY_CAP 수치 도출/박제 → v2 baseline 재스냅샷 → 게이트 검증 (kip-up≤50 + 변별 퇴행0 + 정타 95~100 + sensitivity + 결정론)</name>
  <action>POD 의존 blocking-human checkpoint — 실행 절차 전체는 아래 how-to-verify 1~6 단계 참조. 요약: Pod 재개 → serial sweep(--pair-sequential, 동시 금지) → derive_caps 로 SEVERITY_CAP 도출(sensitivity 양방, 6페어 fit 금지) → vision_veto.py 박제 → 재sweep → eval20 baseline 재스냅샷 → assert_baseline_v2 전 게이트 PASS. Pod down 지속 시 이 plan 은 여기서 멈춘다(silent-skip 금지). Claude 가 Pod 작업 실행([[pod-ops-claude-runs]]), belle 는 Pod 재개 승인 + 도메인 판단만.</action>
  <what-built>
    Task 1 이 pod-free 로 cap 도출 방법론(derive_caps.py) + sensitivity manifest + v2 게이트 하니스(assert_baseline_v2.py) + README 를 박제했다. 20-01~20-03 의 vision_veto 코어 + Gemini 어댑터 + 파이프라인 통합 + Mode3 억제 + 3-way 계약이 전부 pod-free 단위테스트 GREEN 이다. 남은 것 = **실 GPU 정량 게이트**(RTMW 포즈 + 실 Gemini 비전 거부권 + 실 점수). 이 단계는 RunPod RTMW GPU 가 필수이며 현재 down — silent-skip 금지, belle/Pod 재개까지 blocking.
  </what-built>
  <how-to-verify>
    **Claude(Pod 작업 = 내 몫 [[pod-ops-claude-runs]])가 실행, belle 는 Pod 재개 승인 + 도메인 판단만:**
    1. RunPod RTMW Pod 재개(또는 신규 — Network Storage 박제 [[next-pod-use-network-storage]]) → :8000 server UP + Lambda RUNPOD_ANALYZE_URL 동기화(proxy URL 변경 시) + Gemini 키/모델 Pod env 주입(GEMINI_API_KEY SSM, gemini-3.1-pro-preview) + GEMINI_VISION_VETO_ENABLED ON. git push 선행([[gsd-pod-work-push-first]]) → Pod git pull.
    2. **SERIAL sweep**(동시 금지 — [[pipeline-not-concurrency-safe-eval-serial]]): `cd backend && python scripts/sweep_phase15.py --keys-file scripts/phase15_keys.json --mode mode1 --pair-sequential` (Mode1 6페어 fault→success 순차). Mode3 sensitivity 셋은 belle 수집 후 동일 순차.
    3. **cap 도출:** sweep 산출 + sensitivity 셋으로 derive_caps.py 실행 → SEVERITY_CAP moderate/major 수치 산출(IPSF severity + sensitivity 양방, 6페어 fit 금지). 산출값을 vision_veto.py SEVERITY_CAP 에 박제 + 출처 주석. **6페어로 수치를 조정(재실행 반복)하면 curve-fit — 금지.** sensitivity 양방이 통과하는 cap 만.
    4. **재sweep + baseline 재스냅샷:** cap 박제 후 재sweep → backend/evals/phase20/baseline/eval20_serial_baseline.json 산출.
    5. **게이트 검증:** `python evals/phase20/assert_baseline_v2.py` → 전부 PASS 확인:
       - V-1: kip-up fault ≤50 (known_false_positive → discriminate flip)
       - V-2: 4쌍(power-spin/peter-pan/elbow-twist/pdshape) margin>0 유지 (퇴행 0)
       - V-3: 정은지 정타 success_overall 95~100 유지
       - climb known_gate_blocked 유지 (본 phase scope 아님 — 변동 0 확인)
       - V-5: sensitivity above-cutoff 안 떨어짐(위음성 0) + 미보유 억제(위양성 0) 양방
       - V-4: cache-warm 재실행 byte-identical (cache-cold 변동은 정보성 보고)
    6. 결과(sweep report + baseline + 게이트 PASS/FAIL)를 commit. RUNPOD_AUTH_TOKEN 절대 로그/evidence 출력 금지.
    **FAIL 시:** kip-up 이 ≤50 안 되거나 4쌍 퇴행 또는 sensitivity 위음성 → cap/severity 매핑 또는 어댑터 프롬프트 재검토(20-01/20-02 로 회귀). 6페어 짜맞춤으로 통과시키지 말 것(D-02).
  </how-to-verify>
  <resume-signal>belle: Pod 재개 승인 후 Claude 가 sweep+도출+게이트 실행 → 전 게이트 PASS 확인 시 "approved" 또는 FAIL 항목 기술. Pod down 지속 시 "대기" (이 plan TERMINAL — 여기서 멈춤).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

eval 하니스(repo) + Pod sweep(RTMW GPU + Gemini) → vision_veto.SEVERITY_CAP 수치 + baseline 스냅샷. 신규 endpoint 0. Pod env = 기존 secret(RUNPOD_AUTH_TOKEN/GEMINI_API_KEY/AWS/Firebase SA). 객관성: baseline=채점기 출력 스냅샷(라벨 아님).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-15 | Tampering / 객관성 | SEVERITY_CAP 을 6페어에 curve-fit (D-02 위반, 일반화 실패) | mitigate | derive_caps = sensitivity 양방 + IPSF severity 도출, 6페어=회귀만. [[calibration-source-hard-gate]] 자기 sweep 재calibrate 금지. README/주석 출처 박제 |
| T-20-16 | Integrity | 동시 sweep → 결과 cross-contamination (가짜 regression) | mitigate | --pair-sequential 순차만, 동시성 hard gate ([[pipeline-not-concurrency-safe-eval-serial]]) |
| T-20-17 | Information disclosure | RUNPOD_AUTH_TOKEN / GEMINI_API_KEY 로그·evidence 노출 | mitigate | 토큰 절대 로그 금지(sweep_phase15 redaction 선례), 키 미출력 |
| T-20-18 | Integrity / 신뢰성 | Pod silent-skip → 정량 게이트 미실행 (위양성 미확정 ship) | mitigate | blocking-human checkpoint, Task 2 TERMINAL — Pod down 시 멈춤(silent skip 금지) |
| T-20-19 | Tampering / 객관성 | baseline 점수를 사람 점수 라벨로 오용 | mitigate | baseline = 채점기 출력 스냅샷(라벨 아님), 사람 점수 ground truth 영구 금지 명시 |
| T-20-SC | Tampering | npm/pip installs | accept | 신규 패키지 0 (기존 sweep/eval 하니스 + numpy). Package Legitimacy Audit N/A |

Pod 운영 보안 = 기존 토큰 redaction 선례 재사용.
</threat_model>

<verification>
- [POD-FREE] `cd backend && PYTHONPATH=shared/python python evals/phase20/assert_baseline_v2.py` self-consistency GREEN (Pod 부재 시 "대기" exit)
- [POD] serial sweep → derive_caps → SEVERITY_CAP 박제 → 재sweep → eval20 baseline → assert_baseline_v2 전 게이트 PASS (V-1/2/3/4/5 + climb 유지)
- 동시 실행 금지 (--pair-sequential 순차만)
- curve-fit 가드: cap 출처 = sensitivity 양방 + IPSF severity (6페어 회귀만)
- 결정론: cache-warm byte-identity (cache-cold 정보성)
</verification>

<success_criteria>
- SEVERITY_CAP 수치가 sensitivity 양방 + IPSF severity 도출 (SCORE-09 / D-02 — 6페어 curve-fit 0)
- kip-up ≤50 (위양성 해소, V-1) + 변별 4쌍 퇴행 0 (V-2) + 정타 95~100 (V-3) + climb gate 유지
- sensitivity 위양성↔위음성 양방 정확 (V-5 일반화)
- cache-warm 결정론 byte-identity (TRUST-06 / V-4)
- Pod silent-skip 금지 — blocking-human TERMINAL gate
</success_criteria>

<output>
Create `.planning/phases/20-v2-gemini/20-04-SUMMARY.md` when done — SEVERITY_CAP 도출 수치 + 출처(sensitivity, NOT 6페어) + v2 baseline flip 결과(kip-up≤50) + 전 게이트 PASS/FAIL + Pod 환경 박제.
</output>
