---
phase: 20-v2-gemini
plan: 04
type: execute
wave: 3
depends_on: [20-01, 20-02, 20-03]
files_modified:
  - backend/evals/phase20/derive_caps.py
  - backend/evals/phase20/sensitivity.yaml
  - backend/evals/phase20/assert_baseline_v2.py
  - backend/evals/phase20/README.md
  - backend/shared/python/sunity_shared/analysis/vision_veto.py
  - backend/evals/phase20/baseline/eval20_serial_baseline.json
  - backend/evals/phase20/baseline/eval20_gate_report.json
autonomous: false
requirements: [SCORE-09, SCORE-08, TRUST-06]
must_haves:
  truths:
    - "SEVERITY_CAP 의 moderate/major 수치가 IPSF severity 의미 + sensitivity 셋(미보유+above-cutoff)에서 도출되고 6페어에 curve-fit 되지 않는다 (D-02 — 6페어는 known-answer 회귀, fit 타깃 아님)"
    - "derive_caps.py 가 sensitivity 항목이 TODO/누락 video key/최소개수 미달이면 fail-closed 한다 — 최소 2개 distinct bucket(fault/false-positive 가 떨어져야 하는 케이스 + above-cutoff 가 높게 유지돼야 하는 케이스) 없이는 cap 도출 거부. phase18 6페어는 derive 입력으로 금지(assert_baseline_v2 회귀 전용) (HIGH-3)"
    - "cap 박제 시 vision_veto.SEVERITY_CAP_PROVENANCE.sensitivity_manifest_sha256 가 실 sha256 으로 채워진다(TODO/None 금지) + phase18_pairs_used_for_derivation=False (HIGH-3)"
    - "Pod-free self-check(--self-check)는 phase gate 가 아니며 approval 로 기록될 수 없다. 기본/--phase-gate assert 는 Pod baseline 자산(eval20_serial_baseline.json) 부재 시 fail-closed(non-zero) 한다 (HIGH-2)"
    - "Pod serial sweep 후 kip-up fault 영상이 ≤50 (known_false_positive → discriminate 전환, V-1) — eval20_serial_baseline.json + eval20_gate_report.json terminal evidence 존재 + V-1..V-5 PASS 하에서만 --phase-gate 가 exit 0"
    - "변별 4쌍(power-spin/peter-pan/elbow-twist/pdshape) 퇴행 0 (fault<success margin>0 유지, V-2)"
    - "정은지 정타가 95~100 유지 (비전이 깨끗한 자세 안 깎음, V-3)"
    - "sensitivity 셋(미보유+above-cutoff) 위양성↔위음성 양방 정확 — above-cutoff 가 잘못 안 떨어진다 (V-5 일반화)"
    - "결정론 — cache-warm 재실행 byte-identical (temp 0 단독 아님, 캐시 보장, V-4 실 점수)"
  artifacts:
    - path: "backend/evals/phase20/derive_caps.py"
      provides: "SEVERITY_CAP 도출 방법론 스크립트 — IPSF severity 의미 + sensitivity 셋 양방 게이트 + fail-closed(TODO/min-count/2-bucket) + phase18 페어 derive 입력 금지 + provenance sha 기록 (6페어 curve-fit 금지)"
      contains: "def derive"
    - path: "backend/evals/phase20/assert_baseline_v2.py"
      provides: "v2 known-answer 게이트 — --self-check(구조/방법론, non-approval) vs --phase-gate(fail-closed, V-1..V-5). kip-up≤50 + 변별 4쌍 퇴행0 + 정타 95~100 + sensitivity 양방 + 결정론"
      contains: "--phase-gate"
    - path: "backend/evals/phase20/baseline/eval20_serial_baseline.json"
      provides: "v2 serial baseline 재스냅샷 (kip-up verdict flip + 4쌍 유지 + climb gate 유지) — terminal evidence"
      contains: "discriminate"
    - path: "backend/evals/phase20/baseline/eval20_gate_report.json"
      provides: "phase-gate 판정 evidence (V-1..V-5 PASS/FAIL + cap provenance sha + status=applied 증명) — terminal evidence, --phase-gate 산출"
      contains: "phase_gate"
  key_links:
    - from: "derive_caps.py"
      to: "vision_veto.SEVERITY_CAP + SEVERITY_CAP_PROVENANCE (20-01 placeholder)"
      via: "도출 수치를 SEVERITY_CAP moderate/major 에 박제 + sensitivity_manifest_sha256 기록 (출처=sensitivity 셋, 6페어 아님)"
      pattern: "sensitivity_manifest_sha256"
    - from: "sweep_phase15.py --mode mode1 --pair-sequential (Pod serial)"
      to: "assert_baseline_v2.py --phase-gate + eval20_serial_baseline.json + eval20_gate_report.json"
      via: "serial sweep 산출 ↔ v2 baseline 대조 (kip-up flip + 4쌍 유지), phase-gate fail-closed"
      pattern: "pair-sequential"
---

<objective>
**[POD 의존 — TERMINAL GATE]** 이 plan 은 RunPod RTMW GPU 가 **필수**다(현재 down). 20-01~20-03 의 pod-free 코드가 전부 GREEN 인 뒤에만 진입한다. 두 가지를 한다: (1) **SEVERITY_CAP 수치를 generalization-tested eval 로 도출**(D-02 — 6페어 curve-fit 금지, 이 plan 이 placeholder 를 채우는 **유일한** 자리, fail-closed), (2) **serial Pod sweep 으로 belle 스펙 게이트 정량 검증**(kip-up≤50 + 변별 퇴행0 + 정타 95~100 + sensitivity 양방 + 결정론, fail-closed phase-gate).

- **SCORE-09 (D-02 일반화 hard gate — HIGH-3 fail-closed):** `derive_caps.py` = IPSF severity 의미(minor/moderate/major 결함 무효화 강도) + sensitivity 셋(미보유+above-cutoff, 고득점이어야 정상) 양방 게이트에서 cap 도출. **sensitivity 항목이 TODO/누락 video key/최소개수 미달이면 cap 도출 거부(fail-closed)** — 최소 2개 distinct bucket(① fault/false-positive 가 ≤50 으로 떨어져야 하는 케이스 + ② above-cutoff 가 높게 유지돼야 하는 케이스) 충족 전에는 어떤 cap 도 안 채운다. **phase18 6페어는 derive 입력으로 금지**(assert_baseline_v2 회귀 검증 전용). cap 박제 시 vision_veto.SEVERITY_CAP_PROVENANCE.sensitivity_manifest_sha256 를 실 sha256 으로 기록.
- **SCORE-08 정량 확정 (V-1/2/3):** Pod serial sweep → v2 baseline 재스냅샷. kip-up known_false_positive→discriminate, 4쌍 margin>0 유지, climb known_gate_blocked 유지, 정타 95~100. visionVeto.status='applied' 로 veto 실 실행 증명(HIGH-1 연계).
- **TRUST-06 (V-4 실 점수 결정론):** cache-warm 재실행 byte-identical 게이트. cache-cold 변동은 정보성(Pitfall 2 + Open Q4 경계).
- **HIGH-2 (terminal gate fail-closed):** `assert_baseline_v2.py` 를 두 모드로 분리 — `--self-check`(Pod baseline 없이 구조/방법론만, **exit 0 가능하나 phase gate 아님, approval 로 기록 금지**) vs `--phase-gate`(**eval20_serial_baseline.json 존재 + V-1..V-5 PASS 아니면 non-zero**). 기본(인자 없음)=`--phase-gate` 의미(fail-closed). verify 커맨드에서 `; echo "exit=$?"` 제거(green 오인 차단). terminal evidence = eval20_serial_baseline.json + eval20_gate_report.json. 20-04-SUMMARY 'approved' 는 --phase-gate PASS 없이 불법.
- **동시성 hard gate:** eval **순차만**(`--pair-sequential`) — _process 동시성 비안전, 동시 트리거=cross-contamination([[pipeline-not-concurrency-safe-eval-serial]]).

> ⚠️ **Pod silent-skip 금지 + self-check ≠ approval.** Pod down 이면 Task 2(sweep)는 **blocking checkpoint** 로 멈춘다 — pod-free Task 1(도출 방법론 + 게이트 하니스 + sensitivity manifest)까지만 진행하고 belle/Pod 재개를 기다린다. Task 1 은 sweep 없이도 도출 *방법론* + 가드를 박제(수치 확정은 Pod 데이터 필요). **`--self-check` 가 GREEN 이어도 phase gate 통과가 아니다 — approval 로 기록 금지(HIGH-2).**

Purpose: belle "절대 실수 없게" — 비전 cap 을 6페어에 짜맞추면 일반화 실패(위양성↔위음성 한쪽만). sensitivity 양방 게이트가 유일한 안전 도출이고, fail-closed 가 자산 없는 curve-fit 을 막는다(HIGH-3). Pod 정량 phase-gate 가 최종 신뢰 게이트 — self-check 가 false-green 으로 ship 을 풀지 못하게 한다(HIGH-2).
Output: backend/evals/phase20/(derive_caps.py + sensitivity.yaml + assert_baseline_v2.py + README + baseline/eval20_serial_baseline.json + baseline/eval20_gate_report.json) + vision_veto.py SEVERITY_CAP 수치 + SEVERITY_CAP_PROVENANCE.sensitivity_manifest_sha256 박제.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/20-v2-gemini/20-CONTEXT.md
@.planning/phases/20-v2-gemini/20-RESEARCH.md
@.planning/phases/20-v2-gemini/20-VALIDATION.md
@backend/evals/phase18/dataset/pairs.yaml
@backend/evals/phase18/baseline/eval18_serial_baseline.json
@backend/evals/phase18/assert_baseline.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: [POD-FREE] SEVERITY_CAP 도출 방법론(derive_caps.py, fail-closed) + sensitivity 셋 manifest + v2 게이트 하니스(assert_baseline_v2.py, --self-check vs --phase-gate) + README — 수치 확정 전 구조/방법론 박제</name>
  <files>backend/evals/phase20/derive_caps.py, backend/evals/phase20/sensitivity.yaml, backend/evals/phase20/assert_baseline_v2.py, backend/evals/phase20/README.md</files>
  <read_first>
    - backend/evals/phase18/assert_baseline.py 전체 (게이트 의미론: expected↔baseline 정합, discriminate margin>0, known_false_positive/known_gate_blocked 명시 추적, exit 0/non-zero — v2 하니스가 확장할 패턴)
    - backend/evals/phase18/dataset/pairs.yaml 전체 (6페어 구조 + 객관성 하드가드 주석 + 일반화 경계 주석 + 동시성 주석 — sensitivity.yaml 이 모방할 fixture 형식)
    - backend/evals/phase18/baseline/eval18_serial_baseline.json (kip-up known_false_positive 100/100/margin0, 4쌍 discriminate margin 28/21/41/42, climb known_gate_blocked — v2 baseline 이 대조할 v1 스냅샷)
    - backend/shared/python/sunity_shared/analysis/vision_veto.py (20-01 — SEVERITY_CAP placeholder None + SEVERITY_CAP_PROVENANCE{source,sensitivity_manifest_sha256=None,phase18_pairs_used_for_derivation=False}; derive_caps 가 채울 대상)
    - 20-DIRECT-REVIEW.md HIGH-2(--self-check vs --phase-gate fail-closed, echo exit 제거, terminal evidence) + HIGH-3(derive_caps fail-closed: TODO/min-count/2-bucket, phase18 페어 derive 입력 금지, provenance sha)
    - 20-RESEARCH.md §"Pitfall 3"(curve-fit 금지 — IPSF severity 의미 + sensitivity 양방, 6페어는 verify) + §"Validation Architecture: Phase gate"(kip-up flip + 4쌍 유지 + climb 유지 + sensitivity) + §"Open Q3"(severity 3버킷 vs 4번째 severe — sensitivity 에서 도출) + §"Open Q4"(결정론 cache-warm/cold 경계)
    - 20-CONTEXT.md D-02(curve-fit 금지) + specifics(sensitivity 미보유+above-cutoff 양방) + [[scoring-redesign-must-generalize-no-overfit]] [[sensitivity-gate-not-just-elite-low]] [[calibration-source-hard-gate]](inherited evidence/repo artifact 만, 자기 sweep 재calibrate 금지)
    - .planning/phases/19-vision-hybrid/19-IPSF-DEDUCTION-NOTES.md (IPSF 감점 의미론 — severity→무효화 강도 도출 근거)
    - 20-VALIDATION.md V-5(sensitivity 일반화) / V-1/2/3 게이트 의미론 / V-1 phase-gate fail-closed
  </read_first>
  <action>
    신규 디렉터리 `backend/evals/phase20/`.
    **derive_caps.py (도출 방법론 + fail-closed, HIGH-3):** SEVERITY_CAP 의 moderate/major 수치를 **도출하는 절차**를 스크립트로 박제 — 입력 = (1) IPSF severity 의미론(19-IPSF-DEDUCTION-NOTES: major=요소 무효화 강도 → ≤50 류, moderate=부분 감점) + (2) sensitivity 셋 양방 게이트(미보유+above-cutoff). derive() 가 sensitivity 셋에서 "above-cutoff 가 안 떨어지는 최대 cap" + "fault 가 ≤50 되는 최소 강도" 를 양방 만족하는 cap 산출.
    **fail-closed 가드 (HIGH-3 — cap 도출 전 hard precondition):** derive() 진입 즉시 sensitivity manifest 검증 — (a) 어떤 항목이라도 `video_key` 가 TODO/빈값/누락이면 raise/non-zero(자산 미수집), (b) 항목 수가 최소개수(예 bucket 당 ≥1, 총 ≥2) 미달이면 거부, (c) **최소 2개 distinct bucket**(`must_drop`: fault/false-positive 가 ≤50 으로 떨어져야 함 + `must_stay_high`: above-cutoff 가 높게 유지) 둘 다 비어있지 않아야 cap 도출 진행. 미달 시 SEVERITY_CAP 을 **건드리지 않고** 명확한 에러로 종료("sensitivity 자산 미수집 — cap 도출 불가, curve-fit fail-closed"). **phase18 6페어는 derive 입력으로 절대 사용 금지** — 코드에 phase18 pairs 로딩 경로 0(grep 으로 강제), 6페어는 assert_baseline_v2 의 회귀 검증에만. cap 도출 성공 시 vision_veto.SEVERITY_CAP_PROVENANCE['sensitivity_manifest_sha256'] = 실 manifest sha256(hashlib) 기록 + phase18_pairs_used_for_derivation=False 유지. 출처 주석 강제: "cap 출처 = IPSF severity + sensitivity 셋, NOT 6페어 curve-fit ([[scoring-redesign-must-generalize-no-overfit]])". [[calibration-source-hard-gate]] 정합 — 자기 sweep 재calibrate 금지(inherited evidence/repo artifact 만).
    **sensitivity.yaml (manifest):** 미보유 동작 + above-cutoff(고득점이어야 정상) 케이스 manifest — pairs.yaml 형식 모방. **자산 수집은 Deferred**(20-CONTEXT) 이므로 이 파일은 *요구사항 manifest*(두 bucket: must_drop + must_stay_high, 각 항목 video_key=TODO 마커 + 기대 verdict 방향: above-cutoff→점수 안 떨어짐, 미보유→억제) 를 박제하고, 실 영상 키는 belle 수집 후 채움(TODO 마커 + 객관성 가드 주석). 사람 점수 라벨 금지(기대 = "떨어지면 안 됨/억제" 방향성만, 숫자 라벨 X). derive_caps 의 fail-closed 가 TODO 마커를 거부함을 주석에 박제.
    **assert_baseline_v2.py (게이트 하니스 — HIGH-2 두 모드):** phase18 assert_baseline.py 확장 + argparse `--self-check` / `--phase-gate`(기본=phase-gate 의미).
      - `--self-check`: Pod baseline 없이 구조/방법론/manifest/게이트 의미론만 검사. exit 0 가능. 출력에 명시: "SELF-CHECK ONLY — NOT a phase gate, must not be recorded as approval (HIGH-2)". approval 신호 절대 출력 금지.
      - `--phase-gate`(기본): `backend/evals/phase20/baseline/eval20_serial_baseline.json` 이 **존재하지 않으면 즉시 non-zero**(fail-closed) + "Pod baseline 자산 부재 — phase gate FAIL". 존재 시 6 게이트 검사: (1) kip-up verdict known_false_positive→discriminate(V-1), (2) 4쌍 discriminate margin>0 유지(V-2), (3) climb known_gate_blocked 유지, (4) 정타 95~100 범위(V-3), (5) sensitivity above-cutoff 가 cap 후에도 high 유지(V-5 위음성 0), (6) 결정론 cache-warm byte-identity(V-4) + visionVeto.status='applied' 증명. 전부 PASS 시에만 exit 0 + eval20_gate_report.json 기록. 하나라도 FAIL → non-zero. **`; echo "exit=$?"` 류 green-오인 출력 금지.**
    **README.md:** 게이트 의미론 + self-check≠approval(HIGH-2) + 도출 근거(6페어=회귀, sensitivity=도출, derive_caps fail-closed HIGH-3) + 동시성(순차만) + Pod 실행 절차(Task 2) 박제.
    from __future__ import annotations, snake_case, 한국어 docstring, 이모지 금지. 사람 점수 라벨 ground truth 영구 금지(객관성).
  </action>
  <verify>
    <automated>cd backend && PYTHONPATH=shared/python python evals/phase20/assert_baseline_v2.py --self-check</automated>
  </verify>
  <acceptance_criteria>
    - `assert_baseline_v2.py --self-check` 가 Pod baseline 부재 시 구조/방법론/manifest/게이트 의미론 검사 통과(exit 0) + "SELF-CHECK ONLY — NOT a phase gate / approval 금지" 명시 출력 (HIGH-2)
    - `assert_baseline_v2.py --phase-gate`(또는 인자 없음 기본) 가 eval20_serial_baseline.json 부재 시 **non-zero(fail-closed)** + "Pod baseline 자산 부재 — phase gate FAIL" (HIGH-2; grep: phase-gate 분기에 baseline 존재 검사 + non-zero exit)
    - derive_caps.py 가 sensitivity TODO/누락 video key/2-bucket 미달이면 cap 도출 거부(fail-closed) + SEVERITY_CAP 미변경 (HIGH-3; grep: derive() 에 manifest 검증 + must_drop/must_stay_high 2-bucket 가드 + raise/non-zero)
    - derive_caps.py 가 phase18 6페어를 derive 입력으로 쓰지 않음 (HIGH-3; grep: derive 경로에 phase18 pairs 로딩 0; 6페어는 assert_baseline_v2 회귀에만) + cap 도출 성공 시 SEVERITY_CAP_PROVENANCE.sensitivity_manifest_sha256 기록 + phase18_pairs_used_for_derivation=False
    - sensitivity.yaml 이 must_drop + must_stay_high 2 bucket manifest + 기대 방향(above-cutoff 안 떨어짐 / 미보유 억제) + TODO 마커 + 객관성 가드 주석 (사람 점수 라벨 0 — grep: 숫자 점수 라벨 0)
    - assert_baseline_v2.py phase-gate 게이트 = kip-up flip + 4쌍 margin>0 + climb gate + 정타 95~100 + sensitivity 양방 + 결정론(+status=applied) (6 게이트 코드 존재)
    - README 에 self-check≠approval(HIGH-2) + 6페어=회귀/sensitivity=도출/derive_caps fail-closed(HIGH-3) + 순차만 + Pod 절차 박제
    - 무관한 backend suite 회귀 0
  </acceptance_criteria>
  <done>derive_caps.py(도출 방법론 + fail-closed: TODO/2-bucket/min-count, phase18 derive 금지, provenance sha — HIGH-3) + sensitivity.yaml 2-bucket manifest + assert_baseline_v2.py(--self-check non-approval vs --phase-gate fail-closed, 6 게이트 — HIGH-2) + README. Pod 수치 확정 전 구조/방법론/객관성/fail-closed 박제. --self-check GREEN(approval 아님).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: [POD 의존 — TERMINAL] RunPod RTMW 재개 + serial sweep → SEVERITY_CAP 수치 도출/박제(fail-closed) → v2 baseline 재스냅샷 → --phase-gate 검증 (kip-up≤50 + 변별 퇴행0 + 정타 95~100 + sensitivity + 결정론, status=applied)</name>
  <action>POD 의존 blocking-human checkpoint — 실행 절차 전체는 아래 how-to-verify 1~6 단계 참조. 요약: Pod 재개 → serial sweep(--pair-sequential, 동시 금지) → sensitivity 셋(belle 수집) 채운 뒤 derive_caps 로 SEVERITY_CAP 도출(sensitivity 양방 fail-closed, 6페어 fit 금지) → vision_veto.py SEVERITY_CAP + SEVERITY_CAP_PROVENANCE.sensitivity_manifest_sha256 박제 → 재sweep → eval20_serial_baseline.json + eval20_gate_report.json 재스냅샷 → assert_baseline_v2 --phase-gate 전 게이트 PASS(fail-closed). Pod down 지속 시 이 plan 은 여기서 멈춘다(silent-skip 금지). **--self-check GREEN 은 approval 아님(HIGH-2).** Claude 가 Pod 작업 실행([[pod-ops-claude-runs]]), belle 는 Pod 재개 승인 + 도메인 판단만.</action>
  <what-built>
    Task 1 이 pod-free 로 cap 도출 방법론(derive_caps.py, fail-closed) + sensitivity manifest(2-bucket) + v2 게이트 하니스(assert_baseline_v2.py, --self-check/--phase-gate) + README 를 박제했다. 20-01~20-03 의 vision_veto 코어 + Gemini 어댑터 + 파이프라인 통합(keep_local_video 게이트 + visionVeto status enum) + Mode3 억제 + 3-way 계약이 전부 pod-free 단위테스트 GREEN 이다. 남은 것 = **실 GPU 정량 phase-gate**(RTMW 포즈 + 실 Gemini 비전 거부권 + 실 점수). 이 단계는 RunPod RTMW GPU 가 필수이며 현재 down — silent-skip 금지, self-check≠approval, belle/Pod 재개까지 blocking.
  </what-built>
  <how-to-verify>
    **Claude(Pod 작업 = 내 몫 [[pod-ops-claude-runs]])가 실행, belle 는 Pod 재개 승인 + 도메인 판단만:**
    1. RunPod RTMW Pod 재개(또는 신규 — Network Storage 박제 [[next-pod-use-network-storage]]) → :8000 server UP + Lambda RUNPOD_ANALYZE_URL 동기화(proxy URL 변경 시) + Gemini 키/모델 Pod env 주입(GEMINI_API_KEY SSM, gemini-3.1-pro-preview) + GEMINI_VISION_VETO_ENABLED ON. git push 선행([[gsd-pod-work-push-first]]) → Pod git pull.
    2. **SERIAL sweep**(동시 금지 — [[pipeline-not-concurrency-safe-eval-serial]]): `cd backend && python scripts/sweep_phase15.py --keys-file scripts/phase15_keys.json --mode mode1 --pair-sequential` (Mode1 6페어 fault→success 순차). Mode3 sensitivity 셋은 belle 수집 후 동일 순차.
    3. **cap 도출 (fail-closed):** sensitivity 셋(belle 수집, sensitivity.yaml TODO→실 video_key + 2-bucket must_drop/must_stay_high 충족)으로 derive_caps.py 실행 → SEVERITY_CAP moderate/major 수치 산출(IPSF severity + sensitivity 양방). **sensitivity 가 TODO/2-bucket 미달이면 derive_caps 가 fail-closed 로 거부 — 이때 cap 채우지 말 것(HIGH-3).** 산출값을 vision_veto.py SEVERITY_CAP 에 박제 + SEVERITY_CAP_PROVENANCE.sensitivity_manifest_sha256 = 실 manifest sha 기록. **6페어로 수치를 조정(재실행 반복)하면 curve-fit — 금지.** sensitivity 양방이 통과하는 cap 만.
    4. **재sweep + baseline 재스냅샷:** cap 박제 후 재sweep → backend/evals/phase20/baseline/eval20_serial_baseline.json 산출.
    5. **phase-gate 검증 (fail-closed):** `python evals/phase20/assert_baseline_v2.py --phase-gate` → 전부 PASS 확인(baseline 부재 시 자동 non-zero, self-check 와 혼동 금지):
       - V-1: kip-up fault ≤50 (known_false_positive → discriminate flip) + visionVeto.status='applied' (veto 실 실행 증명, HIGH-1)
       - V-2: 4쌍(power-spin/peter-pan/elbow-twist/pdshape) margin>0 유지 (퇴행 0)
       - V-3: 정은지 정타 success_overall 95~100 유지
       - climb known_gate_blocked 유지 (본 phase scope 아님 — 변동 0 확인)
       - V-5: sensitivity above-cutoff 안 떨어짐(위음성 0) + 미보유 억제(위양성 0) 양방
       - V-4: cache-warm 재실행 byte-identical (cache-cold 변동은 정보성 보고)
       → 전 게이트 PASS 시 eval20_gate_report.json 기록(terminal evidence).
    6. 결과(sweep report + eval20_serial_baseline.json + eval20_gate_report.json + phase-gate PASS/FAIL)를 commit. RUNPOD_AUTH_TOKEN 절대 로그/evidence 출력 금지.
    **FAIL 시:** kip-up 이 ≤50 안 되거나 4쌍 퇴행 또는 sensitivity 위음성 → cap/severity 매핑 또는 어댑터 프롬프트 재검토(20-01/20-02 로 회귀). 6페어 짜맞춤으로 통과시키지 말 것(D-02). **--phase-gate 가 PASS 안 하면 20-04-SUMMARY 를 'approved' 로 작성 금지(HIGH-2).**
  </how-to-verify>
  <resume-signal>belle: Pod 재개 승인 후 Claude 가 sweep+도출+--phase-gate 실행 → 전 게이트 PASS(fail-closed) 확인 시 "approved" 또는 FAIL 항목 기술. **--self-check GREEN 만으로는 approved 불가(HIGH-2).** Pod down 지속 시 "대기" (이 plan TERMINAL — 여기서 멈춤).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

eval 하니스(repo) + Pod sweep(RTMW GPU + Gemini) → vision_veto.SEVERITY_CAP 수치 + baseline 스냅샷. 신규 endpoint 0. Pod env = 기존 secret(RUNPOD_AUTH_TOKEN/GEMINI_API_KEY/AWS/Firebase SA). 객관성: baseline=채점기 출력 스냅샷(라벨 아님).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-15 | Tampering / 객관성 | SEVERITY_CAP 을 6페어에 curve-fit (D-02 위반, 일반화 실패) | mitigate | derive_caps = sensitivity 양방 + IPSF severity 도출, 6페어=회귀만. [[calibration-source-hard-gate]] 자기 sweep 재calibrate 금지. README/주석 + SEVERITY_CAP_PROVENANCE 출처 박제 |
| T-20-15b | Tampering / 객관성 | sensitivity 자산(TODO) 없이 cap 채움 → 사실상 6페어+IPSF curve-fit (HIGH-3) | mitigate | derive_caps fail-closed(TODO/누락key/2-bucket 미달 시 cap 거부) + phase18 페어 derive 입력 금지(grep) + provenance sha 기록 강제 |
| T-20-16 | Integrity | 동시 sweep → 결과 cross-contamination (가짜 regression) | mitigate | --pair-sequential 순차만, 동시성 hard gate ([[pipeline-not-concurrency-safe-eval-serial]]) |
| T-20-17 | Information disclosure | RUNPOD_AUTH_TOKEN / GEMINI_API_KEY 로그·evidence 노출 | mitigate | 토큰 절대 로그 금지(sweep_phase15 redaction 선례), 키 미출력 |
| T-20-18 | Integrity / 신뢰성 | Pod silent-skip 또는 self-check false-green → 정량 게이트 미실행인데 ship (위양성 미확정) | mitigate | --phase-gate fail-closed(baseline 부재→non-zero) + self-check≠approval 명시 + blocking-human TERMINAL + terminal evidence(eval20_serial_baseline.json + eval20_gate_report.json) (HIGH-2) |
| T-20-19 | Tampering / 객관성 | baseline 점수를 사람 점수 라벨로 오용 | mitigate | baseline = 채점기 출력 스냅샷(라벨 아님), 사람 점수 ground truth 영구 금지 명시 |
| T-20-SC | Tampering | npm/pip installs | accept | 신규 패키지 0 (기존 sweep/eval 하니스 + numpy + stdlib hashlib). Package Legitimacy Audit N/A |

Pod 운영 보안 = 기존 토큰 redaction 선례 재사용.
</threat_model>

<verification>
- [POD-FREE] `cd backend && PYTHONPATH=shared/python python evals/phase20/assert_baseline_v2.py --self-check` 구조 GREEN (NOT a phase gate / approval 금지 — HIGH-2)
- [POD-FREE] `cd backend && PYTHONPATH=shared/python python evals/phase20/assert_baseline_v2.py --phase-gate` → baseline 부재 시 **non-zero(fail-closed)** 확인
- [POD] serial sweep → derive_caps(fail-closed) → SEVERITY_CAP + provenance sha 박제 → 재sweep → eval20_serial_baseline.json + eval20_gate_report.json → assert_baseline_v2 --phase-gate 전 게이트 PASS (V-1/2/3/4/5 + climb 유지 + status=applied)
- 동시 실행 금지 (--pair-sequential 순차만)
- curve-fit 가드: cap 출처 = sensitivity 양방 + IPSF severity (6페어 회귀만), derive_caps fail-closed
- 결정론: cache-warm byte-identity (cache-cold 정보성)
</verification>

<success_criteria>
- SEVERITY_CAP 수치가 sensitivity 양방 + IPSF severity 도출, fail-closed(TODO/2-bucket) (SCORE-09 / D-02 / HIGH-3 — 6페어 curve-fit 0) + SEVERITY_CAP_PROVENANCE.sensitivity_manifest_sha256 박제
- kip-up ≤50 (위양성 해소, V-1, status=applied) + 변별 4쌍 퇴행 0 (V-2) + 정타 95~100 (V-3) + climb gate 유지
- sensitivity 위양성↔위음성 양방 정확 (V-5 일반화)
- cache-warm 결정론 byte-identity (TRUST-06 / V-4)
- Pod silent-skip 금지 + self-check≠approval — --phase-gate fail-closed TERMINAL gate + terminal evidence (HIGH-2)
</success_criteria>

<output>
Create `.planning/phases/20-v2-gemini/20-04-SUMMARY.md` when done — **'approved' 는 --phase-gate PASS 없이 불법(HIGH-2)**. SEVERITY_CAP 도출 수치 + 출처(sensitivity sha, NOT 6페어) + v2 baseline flip 결과(kip-up≤50, status=applied) + 전 게이트 PASS/FAIL + terminal evidence(eval20_serial_baseline.json + eval20_gate_report.json) + Pod 환경 박제.
</output>
