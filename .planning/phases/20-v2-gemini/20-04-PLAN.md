---
phase: 20-v2-gemini
plan: 04
type: execute
wave: 3
depends_on: [20-01, 20-02, 20-03]
files_modified:
  - backend/evals/phase20/derive_caps.py
  - backend/evals/phase20/sensitivity.yaml
  - backend/evals/phase20/assert_baseline_v2.py
  - backend/evals/phase20/README.md
  - backend/shared/python/sunity_shared/analysis/vision_veto.py
  - backend/tests/test_assert_baseline_v2.py
  - backend/evals/phase20/baseline/eval20_serial_baseline.json
  - backend/evals/phase20/baseline/eval20_gate_report.json
autonomous: false
requirements: [SCORE-09, SCORE-08, TRUST-06]
must_haves:
  truths:
    - "SEVERITY_CAP 의 moderate/major 수치가 IPSF severity 의미 + sensitivity 셋(미보유+above-cutoff)에서 도출되고 6페어에 curve-fit 되지 않는다 (D-02 — 6페어는 known-answer 회귀, fit 타깃 아님)"
    - "derive_caps.py 가 sensitivity 항목이 TODO/누락 video key/최소개수 미달이면 fail-closed 한다 — must_drop ≥2 videos across ≥2 motion ids + must_stay_high ≥3 videos across ≥2 motion ids + 가능 시 ≥2 distinct capture session/camera setup 없이는 cap 도출 거부 (iter2 MEDIUM-3 — 1+1 floor 폐기). phase18 6페어는 derive 입력으로 금지(assert_baseline_v2 회귀 전용) (HIGH-3)"
    - "cap 박제 시 vision_veto.SEVERITY_CAP_PROVENANCE.sensitivity_manifest_sha256 가 실 sha256 으로 채워진다(TODO/None 금지) + phase18_pairs_used_for_derivation=False (HIGH-3)"
    - "Pod-free self-check(--self-check)는 phase gate 가 아니며 approval 로 기록될 수 없다. 기본/--phase-gate assert 는 Pod baseline 자산(eval20_serial_baseline.json) 부재 시 fail-closed(non-zero) 하며, 이 fail-closed 동작이 pod-free pytest 로 자동 검증된다(temp empty baseline dir → non-zero) (iter2 MEDIUM-4 / HIGH-2)"
    - "**--phase-gate 가 EVERY eval row 의 visionVeto.status 를 DATA-DRIVEN(eval manifest 의 per-row expected_veto_status)으로 검사한다 (iter3 MEDIUM-2 — '모든 fault row=applied' 폐기): must_drop row(kip-up 포함)=status 'applied'+점수 상한; must_stay_high row=status 'not_applicable'+고점 하한; 이미 변별되는 regression fault row(4쌍)=status ∈ {applied, not_applicable} 허용하되 fault<success + skipped status 0 강제; ALL row=disabled/missing_local_video/skipped_error/visionVeto 부재 시 FAIL. eval20_gate_report.json 에 vision_veto_status_counts + expected_bucket_counts 기록 (D-04 를 코드-경로 주장에서 eval 산출물로 전환, curve-fit 압박 완화)**"
    - "Pod serial sweep 후 kip-up fault 영상이 ≤50 (known_false_positive → discriminate 전환, V-1) — eval20_serial_baseline.json + eval20_gate_report.json terminal evidence 존재 + V-1..V-5 PASS 하에서만 --phase-gate 가 exit 0"
    - "변별 4쌍(power-spin/peter-pan/elbow-twist/pdshape) 퇴행 0 (fault<success margin>0 유지, V-2)"
    - "정은지 정타가 95~100 유지 (비전이 깨끗한 자세 안 깎음, V-3)"
    - "sensitivity 셋(미보유+above-cutoff) 위양성↔위음성 양방 정확 — above-cutoff 가 잘못 안 떨어진다 (V-5 일반화)"
    - "결정론 — cache-warm 재실행 byte-identical (temp 0 단독 아님, 캐시 보장, V-4 실 점수)"
  artifacts:
    - path: "backend/evals/phase20/derive_caps.py"
      provides: "SEVERITY_CAP 도출 방법론 스크립트 — IPSF severity 의미 + sensitivity 셋 양방 게이트 + fail-closed(TODO/min-count/diversity floor: must_drop≥2/≥2ids, must_stay_high≥3/≥2ids — iter2 MEDIUM-3) + phase18 페어 derive 입력 금지 + provenance sha 기록 + diversity summary (6페어 curve-fit 금지)"
      contains: "def derive"
    - path: "backend/evals/phase20/sensitivity.yaml"
      provides: "sensitivity manifest — must_drop + must_stay_high 2 bucket, 각 row 에 motion_id + video_key(TODO) + expected_veto_status(applied | not_applicable) + 점수 방향(억제/유지) + diversity floor 주석. **expected_veto_status 는 row-local data (iter3 MEDIUM-2) — assert_baseline_v2 가 generic fault/clean 라벨이 아닌 이 필드를 읽는다**"
      contains: "expected_veto_status"
    - path: "backend/evals/phase20/assert_baseline_v2.py"
      provides: "v2 known-answer 게이트 — --self-check(구조/방법론, non-approval) vs --phase-gate(fail-closed, V-1..V-5 + **data-driven per-row visionVeto.status: manifest expected_veto_status 대조, regression fault row 는 applied|not_applicable 허용 — iter3 MEDIUM-2**). kip-up≤50 + 변별 4쌍 퇴행0 + 정타 95~100 + sensitivity 양방 + 결정론"
      contains: "--phase-gate"
    - path: "backend/tests/test_assert_baseline_v2.py"
      provides: "pod-free pytest — --phase-gate fail-closed(temp empty baseline → non-zero) + derive_caps floor 가드 + **data-driven per-row status 검사 로직 단위(must_drop→applied, must_stay_high→not_applicable, regression→둘 다 허용, disabled/skipped→FAIL — iter3 MEDIUM-2)** (iter2 MEDIUM-4 — V-11 자동화)"
      contains: "def test_phase_gate_fail_closed_empty_baseline"
    - path: "backend/evals/phase20/baseline/eval20_serial_baseline.json"
      provides: "v2 serial baseline 재스냅샷 (kip-up verdict flip + 4쌍 유지 + climb gate 유지 + per-row visionVeto.status + expected_bucket) — terminal evidence"
      contains: "discriminate"
    - path: "backend/evals/phase20/baseline/eval20_gate_report.json"
      provides: "phase-gate 판정 evidence (V-1..V-5 PASS/FAIL + cap provenance sha + vision_veto_status_counts + expected_bucket_counts + rows_without_veto_status + skipped_error_rows + diversity summary) — terminal evidence, --phase-gate 산출"
      contains: "vision_veto_status_counts"
  key_links:
    - from: "derive_caps.py"
      to: "vision_veto.SEVERITY_CAP + SEVERITY_CAP_PROVENANCE (20-01 placeholder)"
      via: "도출 수치를 SEVERITY_CAP moderate/major 에 박제 + sensitivity_manifest_sha256 기록 (출처=sensitivity 셋, 6페어 아님)"
      pattern: "sensitivity_manifest_sha256"
    - from: "sweep_phase15.py --mode mode1 --pair-sequential (Pod serial)"
      to: "assert_baseline_v2.py --phase-gate + eval20_serial_baseline.json + eval20_gate_report.json"
      via: "serial sweep 산출 ↔ v2 baseline 대조 (kip-up flip + 4쌍 유지 + data-driven per-row status), phase-gate fail-closed"
      pattern: "pair-sequential"
    - from: "sensitivity.yaml expected_veto_status (row-local — iter3 MEDIUM-2)"
      to: "assert_baseline_v2.py --phase-gate per-row 검사 + eval20_gate_report.json"
      via: "manifest 의 row-local expected_veto_status 가 generic fault/clean 라벨을 대체 — must_drop/must_stay_high/regression 별 검사, vision_veto_status_counts + expected_bucket_counts 기록"
      pattern: "expected_veto_status"
---

<objective>
**[POD 의존 — TERMINAL GATE]** 이 plan 은 RunPod RTMW GPU 가 **필수**다(현재 down). 20-01~20-03 의 pod-free 코드가 전부 GREEN 인 뒤에만 진입한다. 세 가지를 한다: (1) **SEVERITY_CAP 수치를 generalization-tested eval 로 도출**(D-02 — 6페어 curve-fit 금지, 이 plan 이 placeholder 를 채우는 **유일한** 자리, fail-closed with diversity floor), (2) **serial Pod sweep 으로 belle 스펙 게이트 정량 검증**(kip-up≤50 + 변별 퇴행0 + 정타 95~100 + sensitivity 양방 + 결정론 + **data-driven per-row visionVeto.status**, fail-closed phase-gate), (3) **fail-closed 동작을 pod-free pytest 로 자동 검증**(iter2 MEDIUM-4).

- **SCORE-09 (D-02 일반화 hard gate — HIGH-3 + iter2 MEDIUM-3 diversity floor):** `derive_caps.py` = IPSF severity 의미(minor/moderate/major 결함 무효화 강도) + sensitivity 셋(미보유+above-cutoff, 고득점이어야 정상) 양방 게이트에서 cap 도출. **sensitivity 항목이 TODO/누락 video key/diversity floor 미달이면 cap 도출 거부(fail-closed)** — floor = `must_drop` ≥2 videos across ≥2 motion ids + `must_stay_high` ≥3 videos across ≥2 motion ids + (가능 시) ≥2 distinct capture session/camera setup. 1+1 은 너무 약함(iter2 MEDIUM-3 — trust-critical score change 정당화 불가). **phase18 6페어는 derive 입력으로 금지**(assert_baseline_v2 회귀 검증 전용). cap 박제 시 vision_veto.SEVERITY_CAP_PROVENANCE.sensitivity_manifest_sha256 를 실 sha256 으로 기록 + diversity summary 를 eval20_gate_report.json 에 기록.
- **SCORE-08 정량 확정 (V-1/2/3 + iter3 MEDIUM-2 data-driven per-row status):** Pod serial sweep → v2 baseline 재스냅샷. kip-up known_false_positive→discriminate, 4쌍 margin>0 유지, climb known_gate_blocked 유지, 정타 95~100. **EVERY eval row 의 visionVeto.status 를 manifest 의 row-local expected_veto_status 로 DATA-DRIVEN 검사 (iter3 MEDIUM-2 — '모든 fault row=applied' 폐기):** must_drop row(kip-up 포함)→'applied'+점수 상한; must_stay_high row→'not_applicable'+고점 하한; **이미 변별되는 regression fault row(4쌍)→status ∈ {applied, not_applicable} 허용**(v1 점수만으로 fault<success 면 Gemini 가 minor/none → not_applicable 정당, 단 fault<success + skipped status 0 강제); disabled/missing_local_video/skipped_error/부재 row 는 항상 FAIL. D-04 "always call vision" 를 코드-경로 주장에서 **eval 산출물**로 전환하되, "fault row=무조건 cap" 압박을 제거해 curve-fit 을 완화한다.
- **TRUST-06 (V-4 실 점수 결정론):** cache-warm 재실행 byte-identical 게이트. cache-cold 변동은 정보성(Pitfall 2 + Open Q4 경계).
- **HIGH-2 + iter2 MEDIUM-4 (terminal gate fail-closed + 자동 검증):** `assert_baseline_v2.py` 를 두 모드로 분리 — `--self-check`(Pod baseline 없이 구조/방법론만, **exit 0 가능하나 phase gate 아님, approval 로 기록 금지**) vs `--phase-gate`(**eval20_serial_baseline.json 존재 + V-1..V-5 PASS + data-driven per-row status PASS 아니면 non-zero**). 기본(인자 없음)=`--phase-gate` 의미(fail-closed). verify 커맨드에서 `; echo "exit=$?"` 제거(green 오인 차단). terminal evidence = eval20_serial_baseline.json + eval20_gate_report.json. **fail-closed 동작은 acceptance 산문이 아니라 pod-free pytest 로 자동 검증** — temp empty baseline dir 에 `--phase-gate` 실행 → non-zero exit assert(iter2 MEDIUM-4 — V-11 자동화, 회귀 방지). 20-04-SUMMARY 'approved' 는 --phase-gate PASS 없이 불법.
- **동시성 hard gate:** eval **순차만**(`--pair-sequential`) — _process 동시성 비안전, 동시 트리거=cross-contamination([[pipeline-not-concurrency-safe-eval-serial]]).

> ⚠️ **Pod silent-skip 금지 + self-check ≠ approval.** Pod down 이면 Task 2(sweep)는 **blocking checkpoint** 로 멈춘다 — pod-free Task 1(도출 방법론 + 게이트 하니스 + sensitivity manifest + fail-closed pytest)까지만 진행하고 belle/Pod 재개를 기다린다. Task 1 은 sweep 없이도 도출 *방법론* + 가드 + fail-closed 자동 테스트를 박제(수치 확정은 Pod 데이터 필요). **`--self-check` 가 GREEN 이어도 phase gate 통과가 아니다 — approval 로 기록 금지(HIGH-2). fail-closed 는 pytest 가 증명(iter2 MEDIUM-4).**

Purpose: belle "절대 실수 없게" — 비전 cap 을 6페어에 짜맞추면 일반화 실패(위양성↔위음성 한쪽만). sensitivity 양방 게이트 + diversity floor 가 유일한 안전 도출이고(iter2 MEDIUM-3), fail-closed 가 자산 없는 curve-fit 을 막는다(HIGH-3). Pod 정량 phase-gate + **data-driven per-row status(iter3 MEDIUM-2)** 가 최종 신뢰 게이트 — "fault row=무조건 cap" 강제가 cap 을 불필요하게 깎는 curve-fit 압박을 만들지 않게, status 기대를 manifest row 에서 읽는다. self-check false-green 이 ship 을 풀지 못하게 pytest 가 fail-closed 를 증명한다(iter2 MEDIUM-4).
Output: backend/evals/phase20/(derive_caps.py + sensitivity.yaml(expected_veto_status row-local) + assert_baseline_v2.py + README + baseline/eval20_serial_baseline.json + baseline/eval20_gate_report.json) + backend/tests/test_assert_baseline_v2.py + vision_veto.py SEVERITY_CAP 수치 + SEVERITY_CAP_PROVENANCE.sensitivity_manifest_sha256 박제.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/20-v2-gemini/20-CONTEXT.md
@.planning/phases/20-v2-gemini/20-RESEARCH.md
@.planning/phases/20-v2-gemini/20-VALIDATION.md
@backend/evals/phase18/dataset/pairs.yaml
@backend/evals/phase18/baseline/eval18_serial_baseline.json
@backend/evals/phase18/assert_baseline.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: [POD-FREE] SEVERITY_CAP 도출 방법론(derive_caps.py, fail-closed + diversity floor) + sensitivity 셋 manifest(expected_veto_status row-local — iter3 MEDIUM-2) + v2 게이트 하니스(assert_baseline_v2.py, --self-check vs --phase-gate + data-driven per-row status) + fail-closed pytest + README — 수치 확정 전 구조/방법론 박제</name>
  <files>backend/evals/phase20/derive_caps.py, backend/evals/phase20/sensitivity.yaml, backend/evals/phase20/assert_baseline_v2.py, backend/evals/phase20/README.md, backend/tests/test_assert_baseline_v2.py</files>
  <read_first>
    - backend/evals/phase18/assert_baseline.py 전체 (게이트 의미론: expected↔baseline 정합, discriminate margin>0, known_false_positive/known_gate_blocked 명시 추적, exit 0/non-zero — v2 하니스가 확장할 패턴)
    - backend/evals/phase18/dataset/pairs.yaml 전체 (6페어 구조 + 객관성 하드가드 주석 + 일반화 경계 주석 + 동시성 주석 — sensitivity.yaml 이 모방할 fixture 형식; **각 row 에 expected_veto_status 추가할 위치 참고**)
    - backend/evals/phase18/baseline/eval18_serial_baseline.json (kip-up known_false_positive 100/100/margin0, 4쌍 discriminate margin 28/21/41/42, climb known_gate_blocked — v2 baseline 이 대조할 v1 스냅샷)
    - backend/shared/python/sunity_shared/analysis/vision_veto.py (20-01 — SEVERITY_CAP placeholder None + SEVERITY_CAP_PROVENANCE{source,sensitivity_manifest_sha256=None,phase18_pairs_used_for_derivation=False}; derive_caps 가 채울 대상)
    - 20-DIRECT-REVIEW.md HIGH-2(--self-check vs --phase-gate fail-closed, echo exit 제거, terminal evidence) + HIGH-3(derive_caps fail-closed: TODO/min-count/2-bucket, phase18 페어 derive 입력 금지, provenance sha)
    - 20-DIRECT-REVIEW-ITERATION2.md MEDIUM-2(per-row visionVeto.status 검사 도입) + MEDIUM-3(diversity floor: must_drop≥2/≥2ids, must_stay_high≥3/≥2ids, ≥2 sessions) + MEDIUM-4(fail-closed pod-free pytest, V-11 자동화)
    - 20-DIRECT-REVIEW-ITERATION3.md MEDIUM-2(**per-row expected status DATA-DRIVEN — manifest row-local expected_veto_status, '모든 fault row=applied' 폐기. must_drop→applied, must_stay_high→not_applicable, regression fault(4쌍)→applied|not_applicable 허용+fault<success, ALL→disabled/missing/skipped/부재 FAIL, expected_bucket_counts 기록**)
    - 20-RESEARCH.md §"Pitfall 3"(curve-fit 금지 — IPSF severity 의미 + sensitivity 양방, 6페어는 verify) + §"Validation Architecture: Phase gate"(kip-up flip + 4쌍 유지 + climb 유지 + sensitivity) + §"Open Q3"(severity 3버킷 vs 4번째 severe — sensitivity 에서 도출) + §"Open Q4"(결정론 cache-warm/cold 경계)
    - 20-CONTEXT.md D-02(curve-fit 금지) + specifics(sensitivity 미보유+above-cutoff 양방) + [[scoring-redesign-must-generalize-no-overfit]] [[sensitivity-gate-not-just-elite-low]] [[calibration-source-hard-gate]](inherited evidence/repo artifact 만, 자기 sweep 재calibrate 금지)
    - .planning/phases/19-vision-hybrid/19-IPSF-DEDUCTION-NOTES.md (IPSF 감점 의미론 — severity→무효화 강도 도출 근거)
    - backend/tests/test_pipeline_mode3.py (pytest 스타일/import — pod-free 단위 컨벤션, test_assert_baseline_v2.py 가 모방)
    - 20-VALIDATION.md V-5(sensitivity 일반화) / V-1/2/3 게이트 의미론 / V-1 phase-gate fail-closed / V-11(self-check≠approval + fail-closed 자동) / V-14(data-driven per-row status)
  </read_first>
  <action>
    신규 디렉터리 `backend/evals/phase20/`.
    **derive_caps.py (도출 방법론 + fail-closed + diversity floor, HIGH-3 + iter2 MEDIUM-3):** SEVERITY_CAP 의 moderate/major 수치를 **도출하는 절차**를 스크립트로 박제 — 입력 = (1) IPSF severity 의미론(19-IPSF-DEDUCTION-NOTES: major=요소 무효화 강도 → ≤50 류, moderate=부분 감점) + (2) sensitivity 셋 양방 게이트(미보유+above-cutoff). derive() 가 sensitivity 셋에서 "above-cutoff 가 안 떨어지는 최대 cap" + "fault 가 ≤50 되는 최소 강도" 를 양방 만족하는 cap 산출.
    **fail-closed 가드 (HIGH-3 + iter2 MEDIUM-3 diversity floor — cap 도출 전 hard precondition):** derive() 진입 즉시 sensitivity manifest 검증 — (a) 어떤 항목이라도 `video_key` 가 TODO/빈값/누락이면 raise/non-zero(자산 미수집), (b) **diversity floor (iter2 MEDIUM-3, 1+1 폐기):** `must_drop` ≥2 videos AND across ≥2 distinct motion ids; `must_stay_high` ≥3 videos AND across ≥2 distinct motion ids; 가능 시 ≥2 distinct capture session/camera setup. 미달 시 cap 도출 거부, (c) 두 bucket(must_drop + must_stay_high) 모두 비어있지 않아야 진행. 미달 시 SEVERITY_CAP 을 **건드리지 않고** 명확한 에러로 종료("sensitivity 자산 미수집/diversity floor 미달 — cap 도출 불가, curve-fit fail-closed (iter2 MEDIUM-3)"). **phase18 6페어는 derive 입력으로 절대 사용 금지** — 코드에 phase18 pairs 로딩 경로 0(grep 으로 강제). cap 도출 성공 시 vision_veto.SEVERITY_CAP_PROVENANCE['sensitivity_manifest_sha256'] = 실 manifest sha256(hashlib) 기록 + phase18_pairs_used_for_derivation=False 유지 + **diversity summary**(must_drop/must_stay_high video count + distinct motion id count + session count)를 반환/eval20_gate_report.json 기록용으로 산출. 출처 주석 강제: "cap 출처 = IPSF severity + sensitivity 셋(diversity floor), NOT 6페어 curve-fit ([[scoring-redesign-must-generalize-no-overfit]])". [[calibration-source-hard-gate]] 정합.
    **sensitivity.yaml (manifest + expected_veto_status row-local — iter3 MEDIUM-2):** 미보유 동작 + above-cutoff(고득점이어야 정상) 케이스 manifest — pairs.yaml 형식 모방. **자산 수집은 Deferred**(20-CONTEXT) 이므로 이 파일은 *요구사항 manifest*(두 bucket: must_drop[항목마다 motion_id + video_key=TODO + 기대 방향=떨어짐 + **expected_veto_status: applied**] + must_stay_high[motion_id + video_key=TODO + 기대 방향=유지 + **expected_veto_status: not_applicable**]) 를 박제하고, 실 영상 키는 belle 수집 후 채움(TODO 마커 + 객관성 가드 주석). **각 row 에 `expected_veto_status` 필드(applied | not_applicable)를 row-local data 로 박제 (iter3 MEDIUM-2) — assert_baseline_v2 가 generic fault/clean 라벨이 아니라 이 필드를 읽는다.** **diversity floor 요구를 manifest 헤더 주석에 명시** — must_drop ≥2/≥2ids, must_stay_high ≥3/≥2ids, ≥2 sessions(iter2 MEDIUM-3). 사람 점수 라벨 금지(기대 = "떨어지면 안 됨/억제" 방향성 + status enum 만, 숫자 점수 라벨 X). derive_caps 의 fail-closed 가 TODO 마커 + floor 미달을 거부함을 주석에 박제.
    **assert_baseline_v2.py (게이트 하니스 — HIGH-2 두 모드 + iter3 MEDIUM-2 data-driven per-row status):** phase18 assert_baseline.py 확장 + argparse `--self-check` / `--phase-gate`(기본=phase-gate 의미).
      - `--self-check`: Pod baseline 없이 구조/방법론/manifest/게이트 의미론만 검사. exit 0 가능. 출력에 명시: "SELF-CHECK ONLY — NOT a phase gate, must not be recorded as approval (HIGH-2)". approval 신호 절대 출력 금지.
      - `--phase-gate`(기본): `backend/evals/phase20/baseline/eval20_serial_baseline.json` 이 **존재하지 않으면 즉시 non-zero**(fail-closed) + "Pod baseline 자산 부재 — phase gate FAIL". 존재 시 게이트 검사: (1) kip-up verdict known_false_positive→discriminate(V-1), (2) 4쌍 discriminate margin>0 유지(V-2), (3) climb known_gate_blocked 유지, (4) 정타 95~100 범위(V-3), (5) sensitivity above-cutoff 가 cap 후에도 high 유지(V-5 위음성 0), (6) 결정론 cache-warm byte-identity(V-4), **(7) DATA-DRIVEN per-row visionVeto.status 검사 (iter3 MEDIUM-2): 각 row 의 기대 status 는 manifest 의 row-local `expected_veto_status` 에서 읽는다 (generic fault/clean 라벨로 도출하지 않음). must_drop row(kip-up 포함)→'applied' 강제 + 점수 상한; must_stay_high row→'not_applicable' 강제 + 고점 하한; phase18 regression fault row(4쌍)→`status ∈ {applied, not_applicable}` 허용하되 `fault < success` margin>0 + skipped status 0 강제(v1 변별 + Gemini minor/none 정당). ALL row→disabled/missing_local_video/skipped_error/visionVeto 부재 시 FAIL.** 전부 PASS 시에만 exit 0 + eval20_gate_report.json 기록. **eval20_gate_report.json 에 `vision_veto_status_counts`(status→count) + `expected_bucket_counts`(must_drop/must_stay_high/regression 별 row count + 충족 여부) + `rows_without_veto_status`(int) + `skipped_error_rows`(int) + diversity summary 기록.** 하나라도 FAIL → non-zero. **`; echo "exit=$?"` 류 green-오인 출력 금지.**
    **test_assert_baseline_v2.py (pod-free fail-closed pytest — iter2 MEDIUM-4 + iter3 MEDIUM-2):** `backend/tests/test_assert_baseline_v2.py`:
      - test_phase_gate_fail_closed_empty_baseline: temp **empty** baseline dir(또는 baseline 부재 경로)에 `--phase-gate` 를 subprocess/직접 호출 → **non-zero exit** assert(fail-closed). green-오인 차단.
      - test_self_check_not_approval: `--self-check` 가 exit 0 가능하나 출력에 "approval"/"approved" 신호 0 + "NOT a phase gate" 문구 present.
      - test_derive_caps_floor_fail_closed: derive_caps 가 floor 미달 manifest(must_drop 1개 또는 must_stay_high 2개 또는 single motion id) → cap 도출 거부 + SEVERITY_CAP 미변경 (iter2 MEDIUM-3 floor pod-free 검증).
      - **test_per_row_status_data_driven (iter3 MEDIUM-2): per-row status 검사 함수가 manifest expected_veto_status 를 읽어 — must_drop row 가 status!='applied' 면 FAIL, must_stay_high row 가 status!='not_applicable' 면 FAIL, regression fault row 는 status='not_applicable'(+fault<success) 면 PASS·status='applied' 면 PASS(둘 다 허용), 어떤 row 든 disabled/missing/skipped/부재면 FAIL. 합성 row dict + 합성 manifest 로 단위 검증(pod-free).** '모든 fault row=applied' 강제 아님을 단언(regression row 가 not_applicable 여도 fault<success 면 PASS).
    **README.md:** 게이트 의미론 + self-check≠approval(HIGH-2) + fail-closed pytest(iter2 MEDIUM-4) + 도출 근거(6페어=회귀, sensitivity=도출, derive_caps fail-closed + diversity floor HIGH-3/iter2 MEDIUM-3) + **data-driven per-row status(iter3 MEDIUM-2 — manifest expected_veto_status, regression fault row 는 applied|not_applicable 허용, "fault row=무조건 cap" 아님, curve-fit 압박 완화)** + 동시성(순차만) + Pod 실행 절차(Task 2) 박제.
    from __future__ import annotations, snake_case, 한국어 docstring, 이모지 금지. 사람 점수 라벨 ground truth 영구 금지(객관성).
  </action>
  <verify>
    <automated>cd backend && PYTHONPATH=shared/python python -m pytest tests/test_assert_baseline_v2.py -x -q && PYTHONPATH=shared/python python evals/phase20/assert_baseline_v2.py --self-check</automated>
  </verify>
  <acceptance_criteria>
    - **test_assert_baseline_v2.py 4 케이스 GREEN — 특히 test_phase_gate_fail_closed_empty_baseline 이 temp empty baseline → non-zero exit 자동 증명 (iter2 MEDIUM-4 — V-11 자동화, acceptance 산문 아닌 pytest)**
    - `assert_baseline_v2.py --self-check` 가 Pod baseline 부재 시 구조/방법론/manifest/게이트 의미론 검사 통과(exit 0) + "SELF-CHECK ONLY — NOT a phase gate / approval 금지" 명시 출력 (HIGH-2; test_self_check_not_approval GREEN)
    - `assert_baseline_v2.py --phase-gate`(또는 인자 없음 기본) 가 eval20_serial_baseline.json 부재 시 **non-zero(fail-closed)** + "Pod baseline 자산 부재 — phase gate FAIL" (HIGH-2; grep: phase-gate 분기에 baseline 존재 검사 + non-zero exit)
    - **derive_caps.py 가 diversity floor 미달(must_drop<2 또는 <2ids, must_stay_high<3 또는 <2ids) 또는 TODO/누락 video key 시 cap 도출 거부(fail-closed) + SEVERITY_CAP 미변경 (HIGH-3 + iter2 MEDIUM-3; test_derive_caps_floor_fail_closed GREEN; grep: derive() 에 must_drop≥2/≥2ids + must_stay_high≥3/≥2ids 가드 + raise/non-zero)**
    - derive_caps.py 가 phase18 6페어를 derive 입력으로 쓰지 않음 (HIGH-3; grep: derive 경로에 phase18 pairs 로딩 0) + cap 도출 성공 시 SEVERITY_CAP_PROVENANCE.sensitivity_manifest_sha256 기록 + phase18_pairs_used_for_derivation=False + diversity summary 산출
    - **sensitivity.yaml 이 must_drop + must_stay_high 2 bucket manifest + motion_id + 기대 방향 + `expected_veto_status`(applied/not_applicable) row-local 필드 + TODO 마커 + diversity floor 요구 주석 + 객관성 가드 주석 (사람 점수 라벨 0 — grep: 숫자 점수 라벨 0; grep: 각 row 에 expected_veto_status 존재 — iter3 MEDIUM-2)**
    - **assert_baseline_v2.py phase-gate 가 DATA-DRIVEN per-row visionVeto.status 검사 (iter3 MEDIUM-2) — manifest expected_veto_status 대조: must_drop→applied, must_stay_high→not_applicable, regression fault(4쌍)→applied|not_applicable 허용(+fault<success), disabled/missing/skipped/부재→FAIL (test_per_row_status_data_driven GREEN; grep: phase-gate 가 generic fault→applied 매핑이 아니라 manifest expected_veto_status 를 읽음 + eval20_gate_report 에 vision_veto_status_counts/expected_bucket_counts/rows_without_veto_status/skipped_error_rows 키)**
    - **assert_baseline_v2.py 가 '모든 fault row=applied' 를 강제하지 않음 — regression fault row 가 status='not_applicable' + fault<success 면 PASS (grep/단위: regression bucket 분기에 status ∈ {applied, not_applicable} 허용, applied-only 강제 0 — iter3 MEDIUM-2 curve-fit 압박 완화)**
    - assert_baseline_v2.py phase-gate 게이트 = kip-up flip + 4쌍 margin>0 + climb gate + 정타 95~100 + sensitivity 양방 + 결정론 + data-driven per-row status (7 게이트 코드 존재)
    - README 에 self-check≠approval(HIGH-2) + fail-closed pytest(iter2 MEDIUM-4) + 6페어=회귀/sensitivity=도출/diversity floor(iter2 MEDIUM-3) + data-driven per-row status(iter3 MEDIUM-2, regression 허용·"무조건 cap" 아님) + 순차만 + Pod 절차 박제
    - 무관한 backend suite 회귀 0
  </acceptance_criteria>
  <done>derive_caps.py(도출 방법론 + fail-closed + diversity floor: must_drop≥2/≥2ids·must_stay_high≥3/≥2ids — iter2 MEDIUM-3, phase18 derive 금지, provenance sha + diversity summary) + sensitivity.yaml 2-bucket manifest(+expected_veto_status row-local — iter3 MEDIUM-2) + assert_baseline_v2.py(--self-check non-approval vs --phase-gate fail-closed + **data-driven per-row status: manifest expected_veto_status, regression 허용 — iter3 MEDIUM-2**, 7 게이트) + test_assert_baseline_v2.py(fail-closed + data-driven status pytest — iter2 MEDIUM-4 / iter3 MEDIUM-2) + README. Pod 수치 확정 전 구조/방법론/객관성/fail-closed 박제. test + --self-check GREEN(approval 아님).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: [POD 의존 — TERMINAL] RunPod RTMW 재개 + serial sweep → SEVERITY_CAP 수치 도출/박제(fail-closed + diversity floor) → v2 baseline 재스냅샷 → --phase-gate 검증 (kip-up≤50 + 변별 퇴행0 + 정타 95~100 + sensitivity + 결정론 + data-driven per-row status)</name>
  <action>POD 의존 blocking-human checkpoint — 실행 절차 전체는 아래 how-to-verify 1~6 단계 참조. 요약: Pod 재개 → serial sweep(--pair-sequential, 동시 금지) → sensitivity 셋(belle 수집, diversity floor 충족) 채운 뒤 derive_caps 로 SEVERITY_CAP 도출(sensitivity 양방 fail-closed + floor, 6페어 fit 금지) → vision_veto.py SEVERITY_CAP + SEVERITY_CAP_PROVENANCE.sensitivity_manifest_sha256 박제 → 재sweep → eval20_serial_baseline.json + eval20_gate_report.json 재스냅샷 → assert_baseline_v2 --phase-gate 전 게이트 PASS(fail-closed + data-driven per-row status). Pod down 지속 시 이 plan 은 여기서 멈춘다(silent-skip 금지). **--self-check GREEN 은 approval 아님(HIGH-2). fail-closed 는 pytest 가 증명(iter2 MEDIUM-4).** Claude 가 Pod 작업 실행([[pod-ops-claude-runs]]), belle 는 Pod 재개 승인 + 도메인 판단만.</action>
  <what-built>
    Task 1 이 pod-free 로 cap 도출 방법론(derive_caps.py, fail-closed + diversity floor) + sensitivity manifest(2-bucket + expected_veto_status row-local) + v2 게이트 하니스(assert_baseline_v2.py, --self-check/--phase-gate + data-driven per-row status) + fail-closed pytest(test_assert_baseline_v2.py — iter2 MEDIUM-4 / iter3 MEDIUM-2) + README 를 박제했다. 20-01~20-03 의 vision_veto 코어 + Gemini 어댑터(토글 미소유) + 파이프라인 통합(토글 pipeline 소유 + keep_local_video 게이트 + visionVeto status enum + cap-mutation monkeypatch 증명) + Mode3 점수카드 전체 억제 + 3-way 계약(visionVeto union + scoreSuppressed + scoreSuppressedReason)이 전부 pod-free 단위테스트 GREEN 이다. 남은 것 = **실 GPU 정량 phase-gate**(RTMW 포즈 + 실 Gemini 비전 거부권 + 실 점수 + data-driven per-row status). 이 단계는 RunPod RTMW GPU 가 필수이며 현재 down — silent-skip 금지, self-check≠approval, belle/Pod 재개까지 blocking.
  </what-built>
  <how-to-verify>
    **Claude(Pod 작업 = 내 몫 [[pod-ops-claude-runs]])가 실행, belle 는 Pod 재개 승인 + 도메인 판단만:**
    1. RunPod RTMW Pod 재개(또는 신규 — Network Storage 박제 [[next-pod-use-network-storage]]) → :8000 server UP + Lambda RUNPOD_ANALYZE_URL 동기화(proxy URL 변경 시) + Gemini 키/모델 Pod env 주입(GEMINI_API_KEY SSM, gemini-3.1-pro-preview) + GEMINI_VISION_VETO_ENABLED ON. git push 선행([[gsd-pod-work-push-first]]) → Pod git pull.
    2. **SERIAL sweep**(동시 금지 — [[pipeline-not-concurrency-safe-eval-serial]]): `cd backend && python scripts/sweep_phase15.py --keys-file scripts/phase15_keys.json --mode mode1 --pair-sequential` (Mode1 6페어 fault→success 순차). Mode3 sensitivity 셋은 belle 수집 후 동일 순차.
    3. **cap 도출 (fail-closed + diversity floor):** sensitivity 셋(belle 수집, sensitivity.yaml TODO→실 video_key + expected_veto_status 채움 + diversity floor: must_drop ≥2/≥2ids, must_stay_high ≥3/≥2ids, ≥2 sessions 충족)으로 derive_caps.py 실행 → SEVERITY_CAP moderate/major 수치 산출(IPSF severity + sensitivity 양방). **sensitivity 가 TODO/floor 미달이면 derive_caps 가 fail-closed 로 거부 — 이때 cap 채우지 말 것(HIGH-3 + iter2 MEDIUM-3).** 산출값을 vision_veto.py SEVERITY_CAP 에 박제 + SEVERITY_CAP_PROVENANCE.sensitivity_manifest_sha256 = 실 manifest sha 기록. **6페어로 수치를 조정(재실행 반복)하면 curve-fit — 금지.** sensitivity 양방 + floor 가 통과하는 cap 만.
    4. **재sweep + baseline 재스냅샷:** cap 박제 후 재sweep → backend/evals/phase20/baseline/eval20_serial_baseline.json 산출(per-row visionVeto.status + expected_bucket 포함).
    5. **phase-gate 검증 (fail-closed + data-driven per-row status):** `python evals/phase20/assert_baseline_v2.py --phase-gate` → 전부 PASS 확인(baseline 부재 시 자동 non-zero, self-check 와 혼동 금지):
       - V-1: kip-up fault ≤50 (known_false_positive → discriminate flip) + visionVeto.status='applied' (must_drop bucket, veto 실 실행 증명, HIGH-1)
       - V-2: 4쌍(power-spin/peter-pan/elbow-twist/pdshape) margin>0 유지 (퇴행 0) + **regression bucket: status ∈ {applied, not_applicable} 허용(fault<success 면 not_applicable 도 PASS — iter3 MEDIUM-2)**
       - V-3: 정은지 정타 success_overall 95~100 유지 + clean(must_stay_high) row visionVeto.status='not_applicable'
       - climb known_gate_blocked 유지 (본 phase scope 아님 — 변동 0 확인)
       - V-5: sensitivity above-cutoff(must_stay_high) 안 떨어짐(위음성 0, status='not_applicable') + 미보유(must_drop) 억제(위양성 0, status='applied') 양방
       - V-4: cache-warm 재실행 byte-identical (cache-cold 변동은 정보성 보고)
       - **data-driven per-row status (iter3 MEDIUM-2): EVERY row 가 manifest expected_veto_status 와 정합 — must_drop=applied, must_stay_high=not_applicable, regression=applied|not_applicable(+fault<success). disabled/missing/skipped/부재 0. eval20_gate_report.json 의 vision_veto_status_counts + expected_bucket_counts + rows_without_veto_status=0 + skipped_error_rows=0 확인**
       → 전 게이트 PASS 시 eval20_gate_report.json 기록(terminal evidence + status counts + expected_bucket_counts + diversity summary).
    6. 결과(sweep report + eval20_serial_baseline.json + eval20_gate_report.json + phase-gate PASS/FAIL)를 commit. RUNPOD_AUTH_TOKEN 절대 로그/evidence 출력 금지.
    **FAIL 시:** kip-up 이 ≤50 안 되거나 4쌍 퇴행 또는 sensitivity 위음성 또는 per-row status 에 disabled/missing/skipped/부재 또는 must_drop row 가 not_applicable → cap/severity 매핑 또는 어댑터 프롬프트 또는 keep_local_video 게이트 재검토(20-01/20-02/20-03 로 회귀). **regression fault row 가 not_applicable 인 것은 fault<success 면 FAIL 아님(iter3 MEDIUM-2) — 거기 맞추려 cap 더 깎지 말 것(curve-fit).** 6페어 짜맞춤으로 통과시키지 말 것(D-02). **--phase-gate 가 PASS 안 하면 20-04-SUMMARY 를 'approved' 로 작성 금지(HIGH-2).**
  </how-to-verify>
  <resume-signal>belle: Pod 재개 승인 후 Claude 가 sweep+도출+--phase-gate 실행 → 전 게이트 PASS(fail-closed + data-driven per-row status) 확인 시 "approved" 또는 FAIL 항목 기술. **--self-check GREEN 만으로는 approved 불가(HIGH-2).** Pod down 지속 시 "대기" (이 plan TERMINAL — 여기서 멈춤).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

eval 하니스(repo) + Pod sweep(RTMW GPU + Gemini) → vision_veto.SEVERITY_CAP 수치 + baseline 스냅샷. 신규 endpoint 0. Pod env = 기존 secret(RUNPOD_AUTH_TOKEN/GEMINI_API_KEY/AWS/Firebase SA). 객관성: baseline=채점기 출력 스냅샷(라벨 아님).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-15 | Tampering / 객관성 | SEVERITY_CAP 을 6페어에 curve-fit (D-02 위반, 일반화 실패) | mitigate | derive_caps = sensitivity 양방 + IPSF severity 도출, 6페어=회귀만. [[calibration-source-hard-gate]] 자기 sweep 재calibrate 금지. README/주석 + SEVERITY_CAP_PROVENANCE 출처 박제 |
| T-20-15b | Tampering / 객관성 | sensitivity 자산(TODO) 없이 또는 약한 1+1 셋으로 cap 채움 → 사실상 curve-fit (HIGH-3 + iter2 MEDIUM-3) | mitigate | derive_caps fail-closed + **diversity floor(must_drop≥2/≥2ids, must_stay_high≥3/≥2ids, ≥2 sessions)** + phase18 페어 derive 입력 금지(grep) + provenance sha + diversity summary 기록. test_derive_caps_floor_fail_closed pod-free 증명 |
| T-20-15c | Tampering / 객관성 | **per-row 'fault row=무조건 applied' 강제가 implementer 를 over-cap(불필요 감점)으로 몰아 curve-fit 압박 (iter3 MEDIUM-2)** | mitigate | per-row 기대 status 를 manifest row-local expected_veto_status 로 data-driven, regression fault row 는 status ∈ {applied, not_applicable} 허용(fault<success 면 not_applicable PASS). test_per_row_status_data_driven 가 'applied-only 강제 아님' 증명 |
| T-20-16 | Integrity | 동시 sweep → 결과 cross-contamination (가짜 regression) | mitigate | --pair-sequential 순차만, 동시성 hard gate ([[pipeline-not-concurrency-safe-eval-serial]]) |
| T-20-17 | Information disclosure | RUNPOD_AUTH_TOKEN / GEMINI_API_KEY 로그·evidence 노출 | mitigate | 토큰 절대 로그 금지(sweep_phase15 redaction 선례), 키 미출력 |
| T-20-18 | Integrity / 신뢰성 | Pod silent-skip 또는 self-check false-green → 정량 게이트 미실행인데 ship (위양성 미확정) | mitigate | --phase-gate fail-closed(baseline 부재→non-zero) + **fail-closed pytest 자동 증명(test_phase_gate_fail_closed_empty_baseline — iter2 MEDIUM-4)** + self-check≠approval 명시 + blocking-human TERMINAL + terminal evidence (HIGH-2) |
| T-20-18b | Integrity / 신뢰성 | **per-row veto skip false-green (iter2 MEDIUM-2)** — vision 이 일부 row 에서 disabled/skipped 인데 점수만으로 PASS → clean-path invariant 미증명 | mitigate | data-driven per-row visionVeto.status 검사(every row expected_veto_status 정합, disabled/missing/skipped/부재 FAIL) + eval20_gate_report.json vision_veto_status_counts/expected_bucket_counts/rows_without_veto_status/skipped_error_rows. test_per_row_status_data_driven pod-free |
| T-20-19 | Tampering / 객관성 | baseline 점수를 사람 점수 라벨로 오용 | mitigate | baseline = 채점기 출력 스냅샷(라벨 아님), 사람 점수 ground truth 영구 금지 명시 |
| T-20-SC | Tampering | npm/pip installs | accept | 신규 패키지 0 (기존 sweep/eval 하니스 + numpy + stdlib hashlib). Package Legitimacy Audit N/A |

Pod 운영 보안 = 기존 토큰 redaction 선례 재사용.
</threat_model>

<verification>
- [POD-FREE] `cd backend && PYTHONPATH=shared/python python -m pytest tests/test_assert_baseline_v2.py -x -q` GREEN (fail-closed/floor/data-driven per-row status pod-free 자동 — iter2 MEDIUM-4 / iter3 MEDIUM-2)
- [POD-FREE] `cd backend && PYTHONPATH=shared/python python evals/phase20/assert_baseline_v2.py --self-check` 구조 GREEN (NOT a phase gate / approval 금지 — HIGH-2)
- [POD-FREE] `cd backend && PYTHONPATH=shared/python python evals/phase20/assert_baseline_v2.py --phase-gate` → baseline 부재 시 **non-zero(fail-closed)** 확인
- [POD] serial sweep → derive_caps(fail-closed + diversity floor) → SEVERITY_CAP + provenance sha 박제 → 재sweep → eval20_serial_baseline.json + eval20_gate_report.json → assert_baseline_v2 --phase-gate 전 게이트 PASS (V-1/2/3/4/5 + climb 유지 + data-driven per-row status: must_drop=applied/must_stay_high=not_applicable/regression=applied|not_applicable)
- 동시 실행 금지 (--pair-sequential 순차만)
- curve-fit 가드: cap 출처 = sensitivity 양방 + IPSF severity (6페어 회귀만), derive_caps fail-closed + diversity floor, per-row 'applied-only 강제' 0(iter3 MEDIUM-2)
- 결정론: cache-warm byte-identity (cache-cold 정보성)
</verification>

<success_criteria>
- SEVERITY_CAP 수치가 sensitivity 양방 + IPSF severity 도출, fail-closed(TODO/diversity floor — iter2 MEDIUM-3) (SCORE-09 / D-02 / HIGH-3 — 6페어 curve-fit 0) + SEVERITY_CAP_PROVENANCE.sensitivity_manifest_sha256 + diversity summary 박제
- kip-up ≤50 (위양성 해소, V-1, status=applied) + 변별 4쌍 퇴행 0 (V-2, regression status applied|not_applicable) + 정타 95~100 (V-3, status=not_applicable) + climb gate 유지
- sensitivity 위양성↔위음성 양방 정확 (V-5 일반화)
- cache-warm 결정론 byte-identity (TRUST-06 / V-4)
- **data-driven per-row visionVeto.status (manifest expected_veto_status, regression row 는 applied|not_applicable 허용 — iter3 MEDIUM-2) + status counts + expected_bucket_counts evidence**
- Pod silent-skip 금지 + self-check≠approval — --phase-gate fail-closed TERMINAL gate + fail-closed pytest 자동(iter2 MEDIUM-4) + terminal evidence (HIGH-2)
</success_criteria>

<output>
Create `.planning/phases/20-v2-gemini/20-04-SUMMARY.md` when done — **'approved' 는 --phase-gate PASS 없이 불법(HIGH-2)**. SEVERITY_CAP 도출 수치 + 출처(sensitivity sha + diversity summary, NOT 6페어) + v2 baseline flip 결과(kip-up≤50, status=applied) + data-driven per-row status counts(must_drop=applied, must_stay_high=not_applicable, regression=applied|not_applicable, rows_without_veto_status=0, skipped_error_rows=0) + 전 게이트 PASS/FAIL + terminal evidence(eval20_serial_baseline.json + eval20_gate_report.json) + Pod 환경 박제.
</output>
</output>
