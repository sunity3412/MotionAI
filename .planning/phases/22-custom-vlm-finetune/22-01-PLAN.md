---
phase: 22-custom-vlm-finetune
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/training/datagen/schema.py
  - backend/training/datagen/measure_error_profile.py
  - backend/training/datagen/rtmw_error_profile.json
  - backend/training/datagen/perturb.py
  - backend/training/datagen/__init__.py
  - backend/tests/phase22/conftest.py
  - backend/tests/phase22/test_schema.py
  - backend/tests/phase22/test_perturb.py
autonomous: true
requirements: [FT-03]
must_haves:
  truths:
    - "D-11 JSON 규격(Null 고정·키 알파벳 정렬·관절 사전 필터·3자리 정수 이산화)이 단일 owner 모듈로 존재하고 위반 입력이 테스트에서 잡힌다"
    - "D-01 통합 리포트 v1 스키마에 score 필드가 존재하지 않는다 (모델은 점수를 절대 내지 않음)"
    - "합성 교란 파라미터가 NLM 임의 수치가 아니라 실측 RTMW 오류 분포 artifact에서 샘플된다"
    - "교란 주입 전 원좌표가 정답 라벨로 보존된다 (자가 라벨, 교사 무관)"
  artifacts:
    - path: "backend/training/datagen/schema.py"
      provides: "D-11 규격 + D-01 통합 리포트 v1 스키마 단일 owner (REPORT_KEYS, discretize/undiscretize, frame 서브샘플 규칙)"
      min_lines: 120
    - path: "backend/training/datagen/perturb.py"
      provides: "합성 교란 순수 모듈 (numpy 단독, 3단 커리큘럼, 시간역전 함정 생성)"
      min_lines: 100
    - path: "backend/training/datagen/rtmw_error_profile.json"
      provides: "실 RTMW 오류 분포 히스토그램 artifact (교란 설계 입력, A3 해소)"
    - path: "backend/tests/phase22/test_schema.py"
      provides: "D-11 규격 + score 부재 + 이산화 왕복 검증"
    - path: "backend/tests/phase22/test_perturb.py"
      provides: "교란 순수성(네트워크 0) + 분포 샘플 + 정답 보존 검증"
  key_links:
    - from: "backend/training/datagen/perturb.py"
      to: "backend/training/datagen/rtmw_error_profile.json"
      via: "교란 파라미터 분포 로드"
      pattern: "rtmw_error_profile"
    - from: "backend/training/datagen/schema.py"
      to: "sunity_shared/analysis/vision_veto.py"
      via: "FAULT_CATEGORIES enum 단일 owner 재사용"
      pattern: "FAULT_CATEGORIES"
---

<objective>
D-16 Wave 0 데이터 엔진의 코어: D-11 JSON 규격 단일 owner 모듈(schema.py) + D-01 통합 리포트 v1 스키마 + 실 RTMW 오류 분포 측정 + 합성 교란 순수 모듈(perturb.py, D-10a 좌표 보정 자가 라벨 트랙)을 구축한다.

Purpose: 이 phase의 신규성은 학습 코드가 아니라 데이터 규격에 있다 (22-RESEARCH.md "Key insight"). schema.py가 학습 JSONL·bake-off·서빙 파서가 공유하는 유일한 규격 소스가 된다. 교란 파라미터는 실측 분포 기반 — NLM 수치(3~5프레임 Null, 5% 지터)는 소스 근거 없음이 자인됨(A3, 상충 #2)이므로 구조만 차용한다.

Output: backend/training/datagen/ 신설 (schema.py + perturb.py + measure_error_profile.py + 분포 artifact) + backend/tests/phase22/ 신설.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-custom-vlm-finetune/22-CONTEXT.md
@.planning/phases/22-custom-vlm-finetune/22-RESEARCH.md
@.planning/phases/22-custom-vlm-finetune/22-PATTERNS.md
@.planning/phases/22-custom-vlm-finetune/22-NLM-EXTRACT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: schema.py — D-11 규격 + D-01 통합 리포트 v1 스키마 단일 owner</name>
  <files>backend/training/datagen/__init__.py, backend/training/datagen/schema.py, backend/tests/phase22/conftest.py, backend/tests/phase22/test_schema.py</files>
  <read_first>
    - .planning/phases/22-custom-vlm-finetune/22-NLM-EXTRACT.md §2·§7·§8 (D-11 4철칙 + 이산화 + 학습 JSONL 원형)
    - .planning/phases/22-custom-vlm-finetune/22-RESEARCH.md Pattern 1 (9fps 인덱스 서브샘플) + Pattern 2 (3자리 정수 이산화)
    - backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py:75-76, 166-232 (PROMPT_VERSION/SCHEMA_VERSION 상수 + VisionVerdict score-부재 계약 + FAULT_CATEGORIES enum 단일 owner)
    - backend/shared/python/sunity_shared/analysis/vision_veto.py (FAULT_CATEGORIES 정의 위치 확인)
    - backend/evals/phase24/assert_gates.py:1-70 (importable check 함수 + docstring 독트린 형태)
  </read_first>
  <behavior>
    - Test 1: `REPORT_KEYS`가 정확히 `("coaching", "corrected_coords", "faults", "segments", "svg_spec", "time_anchors")` — 알파벳 오름차순, "score"/"overall"/"points" 키 부재를 명시 assert (하드 제약: 모델은 점수를 절대 출력하지 않는다)
    - Test 2: `normalize_report(raw)`가 결측 키를 삭제하지 않고 Null(None)로 고정하고, dict 키를 알파벳 정렬로 재방출한다 (D-11 철칙 1·2)
    - Test 3: `discretize(xy, width, height)` → 000~999 3자리 정수 그리드, `undiscretize` 왕복 오차 ≤ 그리드 1칸(1/1000) — Pattern 2 (loc_NNN 어휘 확장 아님, 정수 텍스트)
    - Test 4: `filter_joints(coords, task_joints)`가 태스크 무관 관절(얼굴 이목구비·손가락)을 입력 전 삭제 (D-11 철칙 — 관절 사전 필터)
    - Test 5: `select_frame_indices(total_frames, budget=64)`가 stride=ceil(T/64) 균등 서브샘플의 9fps 원 인덱스 리스트를 반환하고, 반환 인덱스가 전부 [0, T) 범위 (Pattern 1 — 원 영상 재추출 금지, frame 필드 = canonical time key, timestamp = frame/9.0)
    - Test 6: `faults[].fault_category`가 vision_veto.FAULT_CATEGORIES 밖 값이면 normalize에서 거부/None 처리 (enum 단일 owner — 새 분류 발명 금지)
  </behavior>
  <action>
    backend/training/datagen/schema.py 신설 (D-11, D-01 인용 한국어 모듈 docstring + from __future__ import annotations). 내용: (1) SCHEMA_VERSION = "ft-v1.0", PROMPT_VERSION = "ft-v1.0" 상수(gemini_vision_scorer 전례 — 변경 시 날짜+이유 주석), (2) REPORT_KEYS 튜플 — D-01 5출력 = coaching / corrected_coords / faults / segments / svg_spec / time_anchors (알파벳 정렬 순서 그대로), (3) normalize_report / discretize / undiscretize / filter_joints / select_frame_indices / bind_key_prompt(키 리스트 사전 바인딩용 시스템 프롬프트 조각 생성) 순수 함수, (4) TASK_IRRELEVANT_JOINTS 상수(RTMW 133 중 얼굴 68 + 손가락 42 키 프리픽스), (5) fault_category 검증은 sys.path.insert(0, BACKEND/"shared"/"python") 후 sunity_shared.analysis.vision_veto의 FAULT_CATEGORIES import (테스트에서만 무거우면 lazy). numpy 외 서드파티 의존 0, boto3/네트워크 0. svg_spec은 기하 데이터 스키마(target_angle_deg, force_vector, ideal_trajectory 필드 — 픽셀 생성 아님)만 정의. backend/tests/phase22/conftest.py는 shared layer sys.path 주입 fixture 제공.
  </action>
  <verify>
    <automated>python3 -m pytest backend/tests/phase22/test_schema.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m pytest backend/tests/phase22/test_schema.py -x -q` exit 0, 최소 6 tests
    - `grep -c '"score"' backend/training/datagen/schema.py` 결과가 score-부재 assert 테스트/주석 외 스키마 필드 정의에 0회 (REPORT_KEYS에 score 계열 키 없음)
    - schema.py 모듈 docstring에 D-11, D-01 인용 존재: `grep -n "D-11" backend/training/datagen/schema.py` 매치 ≥ 1
  </acceptance_criteria>
  <done>D-11 규격과 D-01 리포트 스키마가 단일 모듈로 존재하고, score 필드 부재·이산화 왕복·Null 고정·알파벳 정렬·관절 필터·프레임 서브샘플 규칙이 전부 테스트로 고정된다.</done>
</task>

<task type="auto">
  <name>Task 2: 실 RTMW 오류 분포 측정 → rtmw_error_profile.json artifact (A3 해소)</name>
  <files>backend/training/datagen/measure_error_profile.py, backend/training/datagen/rtmw_error_profile.json</files>
  <read_first>
    - .planning/phases/22-custom-vlm-finetune/22-RESEARCH.md Pitfall 4 (sim-to-real — 실분포 측정이 교란 설계의 1차 기준)
    - backend/shared/python/sunity_shared/firestore_admin.py (analyses/reference 문서의 keypoint·confidence·angles 저장 필드 확인 — flat 저장 + shape 키)
    - backend/scripts/upload_phase15_dataset.py:1-53 (스크립트 docstring/CLI 규율)
    - .planning/PILOT-FEEDBACK-2026-07-06.md (실증 케이스 목록 — 어떤 실사용 데이터가 있는지)
  </read_first>
  <action>
    backend/training/datagen/measure_error_profile.py 신설 — 실사용/보유 데이터에서 RTMW 오류 특성 3종 히스토그램 추출: (1) confidence 급락 run-length (연속 저신뢰 프레임 구간 길이 분포), (2) 관절별 프레임간 점프 크기(각도 도 단위 — Firestore analyses의 angles (T,J) flat 시계열에서 프레임간 절대차 분포, 관절키별), (3) 저신뢰 관절 빈도 순위. 데이터 소스 우선순위: Firestore `users/{uid}/analyses` 실사용 문서(angles + anglesJointKeys + anglesFrames 재구성) + `reference/{id}/versions/phase4_v1`의 keypointReport/joints3d(confidence 채널 보유분). Firestore Admin 접근은 firestore_admin 기존 클라이언트 재사용, 읽기 전용. 출력 = backend/training/datagen/rtmw_error_profile.json — 히스토그램(bin edges + counts) + `_meta` 블록(source 문서 수, captured_epoch, 측정 스크립트 버전). 이 artifact가 Task 3 교란 파라미터의 유일한 수치 근거 (NLM 임의 수치 하드코딩 금지 — [[scoring-redesign-must-generalize-no-overfit]] 정신: 보유 13영상 curve-fit 아님, 오류 '분포' 측정임을 docstring에 명시). CLI --dry-run(소스 카운트만)과 --limit N 지원.
  </action>
  <verify>
    <automated>python3 -c "import json; p=json.load(open('backend/training/datagen/rtmw_error_profile.json')); assert '_meta' in p and 'confidence_drop_run_length' in p and 'per_joint_jump_deg' in p; print('profile ok', p['_meta'].get('source_doc_count'))"</automated>
  </verify>
  <acceptance_criteria>
    - rtmw_error_profile.json이 존재하고 `_meta.source_doc_count` ≥ 30 (실사용 371건 중 유효분 — 30 미만이면 SUMMARY에 사유 기록 후 reference 데이터로 보강)
    - 히스토그램 키 3종(confidence_drop_run_length, per_joint_jump_deg, low_confidence_joint_rank) 존재
    - measure_error_profile.py에 하드코딩된 교란 수치(예: "0.05", "3~5프레임") 없음 — 측정만 수행
  </acceptance_criteria>
  <done>교란 설계의 수치 근거가 실측 artifact로 박제되어 A3(합성 교란 수치 무근거) 리스크가 해소된다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: perturb.py — 합성 교란 순수 모듈 (D-10a, 정답=원좌표 자가 라벨)</name>
  <files>backend/training/datagen/perturb.py, backend/tests/phase22/test_perturb.py</files>
  <read_first>
    - backend/training/datagen/schema.py (Task 1 산출 — Null 고정/관절 필터 규칙을 교란 출력이 준수해야 함)
    - backend/training/datagen/rtmw_error_profile.json (Task 2 산출 — 파라미터 분포)
    - .planning/phases/22-custom-vlm-finetune/22-NLM-EXTRACT.md Q3 (교란 종류 4종 + 커리큘럼 3단계 — 구조만 차용, 수치 금지)
    - backend/shared/python/sunity_shared/analysis/validation.py ("순수 함수(boto3/네트워크 무관)" 선언 + _as_tj shape ValueError 패턴)
  </read_first>
  <behavior>
    - Test 1: `perturb_sequence(coords, profile, stage, rng)` 출력에서 교란된 관절/프레임 목록과 원좌표(ground truth)가 함께 반환된다 — 정답 = 교란 전 원좌표 (D-10a 자가 라벨)
    - Test 2: 가려짐 시뮬레이션이 키를 삭제하지 않고 값을 Null + confidence 저값으로 덮어쓴다 (D-11 Null 바인딩 — schema.normalize_report 통과)
    - Test 3: 교란 구간 길이·지터 크기가 rtmw_error_profile.json 히스토그램에서 샘플된다 (분포 밖 상수 사용 시 실패하는 파라미터 출처 assert — profile 객체 없이 호출하면 TypeError)
    - Test 4: stage 1/2/3 커리큘럼 — stage1=비핵심 관절 소수·단프레임, stage2=연속 다프레임 핵심 관절 Null, stage3=L/R 스왑 + 복합 (구조 검증: stage3 출력에 좌우 스왑된 관절 쌍 존재)
    - Test 5: `make_temporal_trap(coords, rng)` 시간 역전/순서 셔플 함정 시퀀스 생성 (bake-off 언어-프라이어 shortcut 검출용, Pitfall 6)
    - Test 6: 같은 seed → 같은 출력 (재현성), numpy 외 import 없음 (모듈 소스 grep으로 boto3/requests/urllib 부재)
  </behavior>
  <action>
    backend/training/datagen/perturb.py 신설 — numpy 단독 순수 모듈 (분석 모듈 규율: boto3/네트워크 0, (T,J,C) shape 계약 ValueError). 함수: load_profile(path) / perturb_sequence(coords, profile, stage, rng) / make_temporal_trap(coords, rng) / swap_lr_joints(coords, joint_keys). 교란 4종 구조(NLM Q3 차용): 가려짐 Null run, 가우시안 지터, L/R 스왑, 프레임 드롭 — 전 수치 파라미터는 profile 히스토그램에서 rng.choice로 샘플. 커리큘럼 stage 상수는 구조(관절 범주·연속성·복합 여부)만 정의. 출력 dataclass PerturbResult(frozen): perturbed, original(정답), perturbed_joints, perturbed_frames, stage. docstring에 D-10a + Pitfall 4 인용, "정답=원좌표, 사람 점수 라벨 아님" 명시.
  </action>
  <verify>
    <automated>python3 -m pytest backend/tests/phase22/test_perturb.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m pytest backend/tests/phase22 -x -q` exit 0 (schema + perturb 전체)
    - `grep -E "import (boto3|requests|urllib)" backend/training/datagen/perturb.py` 매치 0
    - PerturbResult에 original(원좌표) 필드 존재 — `grep -n "original" backend/training/datagen/perturb.py` 매치 ≥ 1
  </acceptance_criteria>
  <done>좌표 보정 학습 트랙의 자가 라벨 생성기가 실측 분포 기반·재현 가능·순수 모듈로 완성된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firestore 읽기 → 로컬 artifact | 실사용 고객 데이터(analyses)가 측정 스크립트를 통과 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Info Disclosure | measure_error_profile.py → rtmw_error_profile.json | mitigate | artifact에 uid·영상 URL·식별자 미포함 — 히스토그램 집계값만 저장, `_meta`는 문서 수만 기록 (D-12 정신). 테스트로 artifact 키 화이트리스트 검증 |
| T-22-02 | Tampering | schema.py normalize_report | mitigate | 화이트리스트 키(REPORT_KEYS)만 통과, fault_category enum 강제 — 모델 출력 주입 방어의 규격 원점 |
| T-22-SC | Tampering | pip installs | accept | 이 plan은 신규 패키지 설치 없음 (numpy/pytest 기설치). RESEARCH Package Legitimacy Audit 6/6 Approved |
</threat_model>

<verification>
- `python3 -m pytest backend/tests/phase22 -x -q` GREEN (pod-free)
- `python3 -m pytest backend/tests -q` 기존 baseline FAILED diff IDENTICAL (회귀 0 규율)
- rtmw_error_profile.json 존재 + `_meta` 검증
</verification>

<success_criteria>
- FT-03 라벨링의 (a) 합성 교란 트랙이 실측 분포 기반으로 가동 가능
- D-11 규격 위반이 코드 리뷰가 아니라 테스트로 잡히는 상태
- 후속 plan(22-04 JSONL 조립, 22-05 bake-off, 22-08 서빙 파서)이 schema.py 단일 소스를 import
</success_criteria>

<output>
Create `.planning/phases/22-custom-vlm-finetune/22-01-SUMMARY.md` when done
</output>
