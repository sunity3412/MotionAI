---
phase: 22-custom-vlm-finetune
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/scripts/collect_phase22_youtube.py
  - backend/scripts/phase22_sources.yaml
  - backend/training/data/manifest.json
  - backend/training/datagen/anonymize.py
  - backend/training/LICENSE-AUDIT.md
  - backend/tests/phase22/test_manifest_consistency.py
  - backend/tests/phase22/test_provenance.py
autonomous: true
requirements: [FT-02, FT-06]
must_haves:
  truths:
    - "수집 영상이 전부 비-notified S3 prefix(fixtures/phase22/)에 적재되어 production 파이프라인 트리거가 발화하지 않는다"
    - "모든 수집 행에 provenance(출처 URL·채널·라이선스 근거·학습전용 표기)가 기록되고 사람 숫자 점수 라벨은 어디에도 없다(버킷만)"
    - "학습셋 매니페스트가 전 동작 균등이며 kip-up 편중이 게이트로 차단된다 (미보유 동작 포함)"
    - "2026-07-06 실증 케이스(A2 피터팬 위양성·A3 power-spin)가 hard-negative eval 전용으로 격리되어 학습셋에 오염되지 않는다"
    - "고객 영상(371건 트랙)은 가명처리(얼굴 블러) 후에만 학습 적재 경로에 오른다 (D-12)"
  artifacts:
    - path: "backend/scripts/collect_phase22_youtube.py"
      provides: "yt-dlp 수집 + info.json 사이드카 + S3 적재 + 매니페스트 행 생성 (D-09)"
      min_lines: 120
    - path: "backend/training/data/manifest.json"
      provides: "학습셋 provenance 원장 (시드 자산 + 유튜브 수집분, 균등 카운트 _meta)"
    - path: "backend/training/datagen/anonymize.py"
      provides: "얼굴 검출 + Gaussian blur 가명처리 (고객 영상 트랙 전용, D-12)"
    - path: "backend/training/LICENSE-AUDIT.md"
      provides: "모델/데이터 라이선스 감사 문서 (DD 대비, FT-06)"
  key_links:
    - from: "backend/scripts/collect_phase22_youtube.py"
      to: "s3://sunity-motion-pilot-videos/fixtures/phase22/"
      via: "비-notified prefix PUT (Content-Type video/mp4)"
      pattern: "fixtures/phase22"
    - from: "backend/tests/phase22/test_manifest_consistency.py"
      to: "backend/training/data/manifest.json"
      via: "매니페스트↔S3 적재분 정합 + 균등 카운트 게이트"
      pattern: "manifest"
---

<objective>
D-16 Wave 0 데이터 엔진의 수집 축: 유튜브 수집 파이프라인(D-09) + 보유 시드 자산 등재(D-08) + 고객 영상 가명처리(D-12) + provenance/라이선스 감사(FT-06)를 구축하고 균등 매니페스트를 박제한다.

Purpose: 학습셋의 법적 방어선(provenance + 학습전용 + 가명처리)과 품질 방어선(전 동작 균등, hard-negative 격리)을 데이터가 쌓이기 전에 게이트로 세운다. 가명처리는 적재 후 소급 불가이므로 순서가 생명이다.

Output: 수집 스크립트 + 매니페스트 원장 + 가명처리 모듈 + 라이선스 감사 문서 + 정합/균등 테스트.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-custom-vlm-finetune/22-CONTEXT.md
@.planning/phases/22-custom-vlm-finetune/22-RESEARCH.md
@backend/scripts/upload_phase15_dataset.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: collect_phase22_youtube.py — yt-dlp 수집 스크립트 + provenance/균등 테스트 (D-09)</name>
  <files>backend/scripts/collect_phase22_youtube.py, backend/tests/phase22/test_provenance.py, backend/tests/phase22/test_manifest_consistency.py</files>
  <read_first>
    - backend/scripts/upload_phase15_dataset.py (전체 — 비-notified prefix 규율, --dry-run self-check, Content-Type, KEYS_OUT 매니페스트 골격을 그대로 복사)
    - .planning/phases/22-custom-vlm-finetune/22-RESEARCH.md "유튜브 수집 + provenance" Code Example (MANIFEST_ROW 필드) + Environment Availability (yt-dlp 미설치)
    - backend/evals/phase18/assert_baseline.py:42-92 (fixture self-consistency 패턴 — 셋 일치 + silent-통과 금지)
    - backend/evals/phase24/run_sweep.py:195-207 (_meta provenance 블록 양식)
  </read_first>
  <action>
    `pip install yt-dlp` (로컬, RESEARCH Package Legitimacy Audit [OK] Approved, 2026.7.4 이상). backend/scripts/collect_phase22_youtube.py 신설: 입력 = backend/scripts/phase22_sources.yaml (스키마: url / motion / label_bucket(정타|fault) / segment(start·end 초 — 튜토리얼 "흔한 실수" 구간 트리밍) / notes; 큐레이션은 Task 3). 동작: yt-dlp `--write-info-json --no-playlist -f "bv*[height<=720]+ba/b"` 다운로드(플래그는 착수 시 `yt-dlp --help` 1회 재확인 — A1) → segment 지정 시 ffmpeg 트리밍 → S3 `fixtures/phase22/{motion}/{video_id}.mp4` PUT (BUCKET=sunity-motion-pilot-videos, Content-Type=video/mp4; uploads/ prefix 절대 금지 — ObjectCreated→SQS→pipeline 발화 차단) + info.json 사이드카 PUT → backend/training/data/manifest.json 행 추가: s3_key / motion / label_bucket / source_url / channel / license_evidence("info.json 보관") / usage("training-only-no-redistribution") / collected_at_ms / yt_dlp_version / anonymized(false — 유튜브 공개 영상은 D-12 가명처리 범위 밖, provenance만) / holdout(null). `--dry-run`(다운로드 없이 yaml 검증 + 키 스킴 self-check) 지원. AWS 자격증명 = sunity-motion 프로필.
    test_provenance.py: (a) 매니페스트 행 필수 필드(source_url·license_evidence·usage) 부재 시 FAIL, (b) label_bucket이 정타|fault 외 값(숫자 점수 포함) 거부 — 사람 점수 라벨 영구 금지, (c) 금지 모델 fence — backend/training/ + backend/evals/phase22/의 .py 파일에서 주석 제외 라인에 llava / internvl-u 모델 식별자 0 (grep -v '^\s*#' 필터 후 카운트 — grep gate hygiene).
    test_manifest_consistency.py: (a) 매니페스트 s3_key 중복 0 + 스키마 정합, (b) 균등 게이트 — 동작별 총계 max ≤ 2 x min(설정 가능 _meta.balance_ratio), 각 동작에 정타·fault 버킷 각각 ≥ 1(수집 진행 중엔 xfail 마커 대신 _meta.collection_complete 플래그로 게이트 활성화 — silent 통과 금지. 플래그 false/부재 시의 fail-closed 강제는 22-04 build_jsonl 진입 assert가 담당 (DR-06) — 매니페스트 정합 테스트만으로 충분하다고 간주 금지), (c) holdout=hard_negative_eval 행은 학습 카운트에서 제외됨을 검증, (d) `--check-s3` 옵션 함수로 매니페스트↔S3 적재분 셋 일치(assert_baseline self-consistency 패턴; 네트워크 필요 검사는 pytest에서 skip 마커, 스크립트 경로로 실행).
  </action>
  <verify>
    <automated>python3 backend/scripts/collect_phase22_youtube.py --dry-run && python3 -m pytest backend/tests/phase22/test_provenance.py backend/tests/phase22/test_manifest_consistency.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `--dry-run` exit 0, stdout의 키 스킴에 "uploads/" 미등장 + "fixtures/phase22/" 등장
    - test_provenance.py + test_manifest_consistency.py exit 0
    - `yt-dlp --version` ≥ 2026.7.4
  </acceptance_criteria>
  <done>수집 실행 전에 provenance·비-notified·점수라벨금지·균등 규율이 스크립트+테스트로 강제된다.</done>
</task>

<task type="auto">
  <name>Task 2: 시드 자산 등재 + 가명처리 모듈 + hard-negative 격리 (D-08, D-12)</name>
  <files>backend/training/datagen/anonymize.py, backend/training/data/manifest.json</files>
  <read_first>
    - .planning/phases/22-custom-vlm-finetune/22-CONTEXT.md D-08(시드 목록) + D-12(가명처리 3겹)
    - .planning/phases/22-custom-vlm-finetune/22-RESEARCH.md Open Question 3 (블러 = 기존 YOLO 인프라 + Gaussian blur 최소 경로) + Pitfall 2 (실증 케이스 = hard negative eval 격리)
    - backend/shared/python/sunity_shared/analysis/pose_estimator.py (ultralytics YOLO 로드 방식 — 재사용 지점)
    - .planning/PILOT-FEEDBACK-2026-07-06.md (A2 피터팬 위양성 / A3 power-spin 케이스 식별자)
    - backend/scripts/upload_phase15_dataset.py (시드 페어 S3 적재 시 동일 골격)
  </read_first>
  <action>
    (1) backend/training/datagen/anonymize.py 신설 — 얼굴 가명처리: ultralytics YOLO 얼굴 검출(신규 pip 패키지 추가 없이 기존 ultralytics로 얼굴 가중치 로드; 가중치 파일은 스크립트가 멱등 다운로드) + 검출 bbox Gaussian blur(numpy/PIL 순수 함수) → ffmpeg 재인코딩. 순수 blur 함수와 I/O 껍데기 분리(어댑터 경계 규율). docstring에 D-12 인용: "모델은 포즈/모션만 학습, 얼굴 픽셀 불필요. 고객 영상 트랙 전용(유튜브 공개 영상 범위 밖). 적재 후 소급 불가 — 적재 전 강제". 검출 실패 프레임은 보수적으로 상단 1/3 블러 폴백(단정 금지 정신).
    (2) manifest.json 시드 등재(D-08): 정은지 reference 11 + 일부러-실수 still 페어(기존 S3 fixtures/ 키 참조 또는 fixtures/phase22/pairs/ 적재), 실사용 371건은 video_hash + Firestore 문서 경로만 참조(uid 미기재, anonymized=false 상태로는 학습 JSONL 제외 — 22-04 build_jsonl이 anonymized 또는 internal=true만 소비), 2026-07-06 실증 케이스 A2 피터팬·A3 power-spin은 holdout="hard_negative_eval" 마킹(Pitfall 2 — 학습 오염 금지, eval 전용). 시드 행 provenance: source="internal", license_evidence="자사 촬영/파일럿 참가 동의서(D-12 1겹)".
  </action>
  <verify>
    <automated>python3 -m pytest backend/tests/phase22/test_manifest_consistency.py -x -q && python3 -c "import json; m=json.load(open('backend/training/data/manifest.json')); rows=m['rows']; hn=[r for r in rows if r.get('holdout')=='hard_negative_eval']; assert len(hn)>=2; print('rows', len(rows), 'hard_negative', len(hn))"</automated>
  </verify>
  <acceptance_criteria>
    - manifest.json에 hard_negative_eval 행 ≥ 2 (피터팬·power-spin) + 시드 행 ≥ 12 (reference 11 + still 페어)
    - anonymize.py에 D-12 인용 docstring 존재 (`grep -c "D-12" backend/training/datagen/anonymize.py` ≥ 1)
    - 매니페스트 어느 행에도 uid / 사용자 식별자 필드 없음 (test_provenance 화이트리스트 통과)
  </acceptance_criteria>
  <done>시드 자산이 provenance와 함께 원장에 오르고, 고객 영상은 가명처리 게이트 뒤에, 실증 케이스는 eval 전용 격리 뒤에 놓인다.</done>
</task>

<task type="auto">
  <name>Task 3: 유튜브 수집 실행 (균등 quota) + LICENSE-AUDIT.md (D-09, FT-06)</name>
  <files>backend/scripts/phase22_sources.yaml, backend/training/data/manifest.json, backend/training/LICENSE-AUDIT.md</files>
  <read_first>
    - backend/scripts/collect_phase22_youtube.py (Task 1 산출 — 소스 yaml 스키마)
    - .planning/phases/22-custom-vlm-finetune/22-CONTEXT.md D-04 (확정 라이선스 문구 — 재조사 금지) + D-09
    - .planning/phases/22-custom-vlm-finetune/22-NLM-EXTRACT.md §5·§11·§12 (라이선스 노트 3건 — 감사 문서의 근거 절)
    - .planning/phases/22-custom-vlm-finetune/22-RESEARCH.md Assumptions A9 (유튜브 학습 이용 법적 지위 — 법률 검토 이월 항목)
  </read_first>
  <action>
    (1) phase22_sources.yaml 큐레이션: 대회 영상=정타 버킷(IPSF 공식 아카이브/채널 1순위), 튜토리얼 "흔한 실수" 세그먼트=fault 버킷. 대상 동작 = 등록 5동작(kip-up, power-spin, peter-pan, elbow-twist-sister, pdshape) + climb + 미보유 동작 ≥ 2종(reference 라이브러리에 없는 동작 — 균등 검증 D-15용). 동작당 목표 quota: 정타 ≥ 3, fault ≥ 3 (초기 시드 규모 — kip-up이 최다가 되지 않게 균등 유지). (2) 수집 실행: --dry-run → 실 실행 → test_manifest_consistency의 --check-s3 경로로 매니페스트↔S3 셋 일치 확인 → _meta.collection_complete=true 설정으로 균등 게이트 활성화. (3) backend/training/LICENSE-AUDIT.md 작성 (FT-06, DD 대비): 모델 라이선스 확정표(D-04 belle 2026-07-06 확정 그대로 — Qwen 3.6 <35B Apache 2.0 / InternVL 3.5 ≤38B 코드 MIT+백본 Apache 2.0 / MMPose Apache 2.0; 재조사 금지 명시), 금지 목록(LLaVA 계열 상업 불가, InternVL-U 생성헤드 ScaleEdit-12M CC BY-NC-SA 오염 — 아키텍처 차용만), 데이터 정책(유튜브 수집 = 학습전용·재배포금지·provenance 원장 = manifest.json + info.json 사이드카), D-12 동의 3겹 구조 기록, 이월 항목(A9)에 다음을 명시: 유튜브/공개영상의 학습 이용 법적 지위는 파일럿 리서치 용도 유예이며, 본 감사 문서는 법률 검토(counsel) 서명 전까지 release-clean을 함의하지 않는다 — 상업 출시 gate 별도 (출시 전 법률 검토 1회에 포함).
  </action>
  <verify>
    <automated>python3 -m pytest backend/tests/phase22/test_manifest_consistency.py backend/tests/phase22/test_provenance.py -x -q && test -f backend/training/LICENSE-AUDIT.md && grep -c "Apache 2.0" backend/training/LICENSE-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - 균등 게이트 활성 상태(_meta.collection_complete=true)에서 test_manifest_consistency exit 0 — 동작별 max ≤ 2 x min, 미보유 동작 ≥ 2종 포함
    - `aws s3 ls s3://sunity-motion-pilot-videos/fixtures/phase22/ --recursive --profile sunity-motion | wc -l` ≥ 매니페스트 수집 행 수 (mp4 + info.json)
    - LICENSE-AUDIT.md에 "재배포" "training-only" "ScaleEdit" "LLaVA" 문자열 각 ≥ 1회 (금지 fence 문서화)
    - `grep -c "release-clean" backend/training/LICENSE-AUDIT.md` ≥ 1 (A9 유예 — 법률 검토 서명 전 release-clean 미함의 명시)
  </acceptance_criteria>
  <done>전 동작 균등 유튜브 시드가 provenance와 함께 S3에 적재되고, 라이선스 감사 문서가 DD 대비 상태가 된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 유튜브 → 로컬/S3 | 외부 신뢰불가 미디어가 수집 파이프라인 진입 |
| 고객 영상 → 학습 적재 경로 | PII(얼굴)가 학습 데이터로 유입될 수 있는 경계 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03 | Tampering | 유튜브 수집분(악의/저품질 세그먼트) | mitigate | provenance 매니페스트 + label_bucket 이원 + 22-04 judge/휴리스틱 필터 + 학습셋 버전 고정(재현 가능) |
| T-22-04 | Info Disclosure | 고객 영상 얼굴 PII | mitigate | anonymize.py 블러를 적재 전 강제 — build_jsonl이 anonymized=false 고객 행 소비 거부 (D-12). 매니페스트에 uid 필드 금지 |
| T-22-05 | Repudiation | 저작권 분쟁 시 출처 입증 불가 | mitigate | info.json 사이드카 보관 + manifest source_url/license_evidence 필수 필드 테스트. usage=training-only-no-redistribution 명문화 (재배포 금지) |
| T-22-06 | DoS/Tampering | 외부 미디어 파싱(ffmpeg/imageio) | mitigate | 로컬 최신 ffmpeg 유지, 파일명 ASCII-safe 정규화(기존 전례), 다운로드 실패는 행 skip + 로그 |
| T-22-SC | Tampering | pip install yt-dlp | mitigate | RESEARCH Package Legitimacy Audit: yt-dlp [OK] Approved (github.com/yt-dlp/yt-dlp, 초대규모). 추가 확인 불필요 |
</threat_model>

<verification>
- `python3 -m pytest backend/tests/phase22 -x -q` GREEN
- S3 fixtures/phase22/ 적재분 ↔ manifest.json 셋 일치
- production 파이프라인 미발화: 수집 후 SQS/analyses 신규 문서 0 (CloudWatch 또는 Firestore 확인)
</verification>

<success_criteria>
- FT-02: 시드 + 유튜브 수집 + 전 동작 균등 매니페스트 완성
- FT-06: provenance 원장 + 라이선스 감사 문서 + 금지 모델 fence 테스트
- D-12 가명처리 구조가 적재 경로에 강제됨
</success_criteria>

<output>
Create `.planning/phases/22-custom-vlm-finetune/22-02-SUMMARY.md` when done
</output>
