---
phase: 22-custom-vlm-finetune
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/scripts/collect_phase22_youtube.py
  - backend/scripts/collect_phase22_instagram.py
  - backend/scripts/phase22_sources.yaml
  - backend/training/datagen/curate_vision.py
  - backend/training/data/manifest.json
  - backend/training/datagen/anonymize.py
  - backend/training/LICENSE-AUDIT.md
  - backend/tests/phase22/test_manifest_consistency.py
  - backend/tests/phase22/test_provenance.py
  - backend/tests/phase22/test_harvest_filter.py
  - backend/tests/phase22/test_curate_vision.py
autonomous: false
requirements: [FT-02, FT-06]
must_haves:
  truths:
    - "수집 영상이 전부 비-notified S3 prefix(fixtures/phase22/)에 적재되어 production 파이프라인 트리거가 발화하지 않는다"
    - "모든 수집 행에 provenance(출처 URL·채널·라이선스 근거·학습전용 표기)가 기록되고 사람 숫자 점수 라벨은 어디에도 없다(버킷만)"
    - "학습셋 매니페스트가 전 동작 균등이며 kip-up 편중이 게이트로 차단된다 (미보유 동작 포함)"
    - "2026-07-06 실증 케이스(A2 피터팬 위양성·A3 power-spin)가 hard-negative eval 전용으로 격리되어 학습셋에 오염되지 않는다"
    - "고객 영상(371건 트랙)은 가명처리(얼굴 블러) 후에만 학습 적재 경로에 오른다 (D-12)"
    - "다운로드 전 Gemini Vision 선별 게이트가 깨끗한 단일인물 폴 여부·동작·버킷을 판정해 쓰레기 저장을 차단하고, 점수는 절대 내지 않는다"
  artifacts:
    - path: "backend/scripts/collect_phase22_youtube.py"
      provides: "yt-dlp 채널 harvest + 종목/길이 필터 + info.json 사이드카 + S3 적재 + 매니페스트 행 생성 (D-09)"
      min_lines: 140
    - path: "backend/training/datagen/curate_vision.py"
      provides: "Gemini Vision 선별 게이트 — 후보 URL/프레임 판정(keep·단일인물폴·동작·버킷), 순수 판정 함수/Gemini I/O 분리, verdict 캐시 (D-10 스트리밍 선별)"
      min_lines: 90
    - path: "backend/scripts/collect_phase22_instagram.py"
      provides: "gallery-dl 인스타 공개 릴스 수집 어댑터 (Tier-2, ToS 회색 명시)"
    - path: "backend/training/data/manifest.json"
      provides: "학습셋 provenance 원장 (시드 자산 + 수집분, 균등 카운트 _meta)"
    - path: "backend/training/datagen/anonymize.py"
      provides: "얼굴 검출 + Gaussian blur 가명처리 (고객 영상 트랙 전용, D-12)"
    - path: "backend/training/LICENSE-AUDIT.md"
      provides: "모델/데이터 라이선스 감사 문서 (DD 대비, FT-06)"
  key_links:
    - from: "backend/scripts/collect_phase22_youtube.py"
      to: "s3://sunity-motion-pilot-videos/fixtures/phase22/"
      via: "비-notified prefix PUT (Content-Type video/mp4)"
      pattern: "fixtures/phase22"
    - from: "backend/scripts/collect_phase22_youtube.py"
      to: "backend/training/datagen/curate_vision.py"
      via: "다운로드 전 Vision 선별 게이트 호출"
      pattern: "curate_vision"
    - from: "backend/tests/phase22/test_manifest_consistency.py"
      to: "backend/training/data/manifest.json"
      via: "매니페스트↔S3 적재분 정합 + 균등 카운트 게이트"
      pattern: "manifest"
---

<objective>
D-16 Wave 0 데이터 엔진의 수집 축: 티어별 채널 harvest(D-09) + Gemini Vision 다운로드-전 선별 게이트(belle 2026-07-09 채택) + 보유 시드 자산 등재(D-08) + 고객 영상 가명처리(D-12) + provenance/라이선스 감사(FT-06)를 구축하고 균등 매니페스트를 박제한다.

Purpose: 학습셋의 법적 방어선(provenance + 학습전용 + 가명처리)과 품질 방어선(전 동작 균등, hard-negative 격리, Vision 선별)을 데이터가 쌓이기 전에 게이트로 세운다. 가명처리는 적재 후 소급 불가이므로 순서가 생명이다.

**아키텍처 (belle 2026-07-09 확정, 원안 대비 변경):** 손으로 고른 개별 URL 목록(`--no-playlist`) → **티어별 공식/스튜디오 채널 harvest**(`--yes-playlist` + 종목/길이 필터). 소스·정책·caveat는 `22-DATA-SOURCES.md` + 메모리 `phase22-data-sourcing-policy-2026-07-09`가 단일 근거. 검증 안 된 third-party MCP는 보안상 미사용 — yt-dlp/gallery-dl(대형 검증 OSS) + 우리 Gemini만. "다운로드 없이 스트리밍 분석"은 학습 substrate가 못 됨(RTMW는 픽셀 필요, 코퍼스는 재현가능 저장 필요) → Vision은 **다운로드-전 선별 게이트** 자리에만 사용.

**belle 게이트(autonomous=false):** Task 1·2(툴링 빌드, 다운로드·과금 없음, --dry-run)는 지금 실행. Task 3(Gemini Vision 실선별 = API 과금 + 카피라이트 영상 prod S3 적재 = 외부·비가역)은 **belle greenlight 후에만** 실행.

Output: 채널 harvester + Vision 선별 모듈 + IG 어댑터 + 매니페스트 원장 + 가명처리 모듈 + 라이선스 감사 문서 + 정합/균등/필터 테스트.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-custom-vlm-finetune/22-CONTEXT.md
@.planning/phases/22-custom-vlm-finetune/22-RESEARCH.md
@.planning/phases/22-custom-vlm-finetune/22-DATA-SOURCES.md
@backend/scripts/upload_phase15_dataset.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 채널 harvester(collect_phase22_youtube.py) + Vision 선별 게이트(curate_vision.py) + provenance/균등/필터 테스트</name>
  <files>backend/scripts/collect_phase22_youtube.py, backend/training/datagen/curate_vision.py, backend/scripts/phase22_sources.yaml, backend/tests/phase22/test_provenance.py, backend/tests/phase22/test_manifest_consistency.py, backend/tests/phase22/test_harvest_filter.py, backend/tests/phase22/test_curate_vision.py</files>
  <read_first>
    - backend/scripts/upload_phase15_dataset.py (전체 — 비-notified prefix 규율, --dry-run self-check, Content-Type, KEYS_OUT 매니페스트 골격을 그대로 복사)
    - .planning/phases/22-custom-vlm-finetune/22-DATA-SOURCES.md (전체 — 채널 목록·티어·버킷·길이/종목 필터·caveat)
    - .planning/phases/22-custom-vlm-finetune/22-RESEARCH.md "유튜브 수집 + provenance" Code Example (MANIFEST_ROW 필드) + Environment Availability
    - backend/shared/python/sunity_shared/analysis/coach_writer.py (Gemini 클라이언트 lazy-import + graceful no-op 전례 — curate_vision Gemini 어댑터가 미러)
    - backend/evals/phase18/assert_baseline.py:42-92 (fixture self-consistency — 셋 일치 + silent-통과 금지)
    - backend/evals/phase24/run_sweep.py:195-207 (_meta provenance 블록 양식)
  </read_first>
  <action>
    (1) `pip install yt-dlp` (로컬, 2026.7.4 이상; 착수 시 `yt-dlp --help` 1회 재확인). **backend/scripts/phase22_sources.yaml = 채널/계정 레지스트리**(22-DATA-SOURCES.md 반영): 각 엔트리 = channel_url / platform(youtube|instagram) / tier(1_official|2_studio) / bucket(정타|fault|mixed) / discipline_filter(제목 정규식 — "폴|pole" 포함·"후프|hoop" 제외) / duration_window(min·max 초 — 예 120~400, 타이틀카드/블록 배제) / series_filter(선택 — 예 "폴인폴"·"Fall In Pole") / notes. (2) **backend/scripts/collect_phase22_youtube.py** 신설 — 파이프라인: `yt-dlp --flat-playlist`로 채널 열거 → discipline_filter·duration_window·series_filter로 후보 축소 → (선별) curate_vision.gate 호출 → (통과분만) `yt-dlp --write-info-json -f "bv*[height<=720]+ba/b"` 다운로드 → S3 `fixtures/phase22/{motion}/{video_id}.mp4` PUT (BUCKET=sunity-motion-pilot-videos, Content-Type=video/mp4; **uploads/ prefix 절대 금지** — ObjectCreated→SQS→pipeline 발화 차단) + info.json 사이드카 PUT → manifest.json 행 추가: s3_key / motion / label_bucket / source_url / channel / tier / license_evidence("info.json 보관") / usage("training-only-no-redistribution") / collected_at_ms / yt_dlp_version / vision_verdict(요약) / anonymized(false — 공개영상은 provenance만) / holdout(null). **동작 모드 3단**: `--dry-run`(열거+필터만, 다운로드·Vision 0), `--curate`(Vision 선별까지, 다운로드 0 — API 과금 발생 → Task 3 belle 게이트), `--collect`(통과분 다운로드+적재 — Task 3 belle 게이트). AWS 자격증명 = sunity-motion 프로필. 균등 quota는 채널·동작별 상한으로 harvest 단계에서 강제(kip-up 편중 방지, dump-all 금지).
    (3) **backend/training/datagen/curate_vision.py** 신설 — Gemini Vision 선별 게이트. 순수 판정 로직(`decide(verdict_json) -> KeepDecision`)과 Gemini I/O 어댑터(`gate(url_or_path)`) 분리(coach_writer lazy-import 전례). 유튜브 = Gemini에 **YouTube URL 직접 전달**(네이티브 인제스트, 다운로드 불필요); 로컬/IG = File API. 프롬프트: "깨끗한 단일인물 폴 클립인가 / 동작 추정 / 정타·fault 버킷 / 저장 가치 사유" 반환 — **점수·severity 절대 금지**(불변식: 모델은 짚기, 점수는 Phase 24). verdict schema = {keep:bool, single_person_pole:bool, move_guess:str|null, bucket:"정타"|"fault"|null, reason:str} — score 필드 부재. 과금 방어: verdict를 `backend/training/data/vision_verdicts.json`에 video_id 키로 캐시(재호출 0). 키 미설정 시 graceful — gate가 "unknown"(다운로드 보류) 반환, 크래시 금지.
    (4) test_provenance.py: (a) 매니페스트 행 필수 필드(source_url·license_evidence·usage) 부재 시 FAIL, (b) label_bucket이 정타|fault 외 값(숫자 점수 포함) 거부, (c) 금지 모델 fence — backend/training/ + backend/evals/phase22/의 .py에서 주석 제외 라인에 llava / internvl-u 식별자 0. test_manifest_consistency.py: (a) s3_key 중복 0 + 스키마 정합, (b) 균등 게이트(동작별 max ≤ 2×min, _meta.balance_ratio; 각 동작 정타·fault ≥1; _meta.collection_complete 플래그로 활성화 — silent 통과 금지, fail-closed는 22-04 진입 assert 담당 DR-06), (c) holdout=hard_negative_eval 행 학습 카운트 제외, (d) `--check-s3` 셋 일치(네트워크 검사는 skip 마커). test_harvest_filter.py: discipline_filter/duration_window/series_filter 순수 함수 단위 테스트(후프 제외·타이틀카드 배제·시리즈 매칭). test_curate_vision.py: verdict schema에 score/severity 부재 + bucket enum 강제 + decide() 순수성(Gemini 미호출) + 키 미설정 graceful.
  </action>
  <verify>
    <automated>python3 backend/scripts/collect_phase22_youtube.py --dry-run && python3 -m pytest backend/tests/phase22/test_provenance.py backend/tests/phase22/test_manifest_consistency.py backend/tests/phase22/test_harvest_filter.py backend/tests/phase22/test_curate_vision.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `--dry-run` exit 0, stdout 키 스킴에 "uploads/" 미등장 + "fixtures/phase22/" 등장, 다운로드·Gemini 호출 0
    - 4개 테스트 exit 0; curate_vision verdict에 score/severity 필드 부재(grep 0)
    - `yt-dlp --version` ≥ 2026.7.4
  </acceptance_criteria>
  <done>수집 실행 전에 provenance·비-notified·점수라벨금지·균등·Vision선별·종목/길이필터 규율이 스크립트+테스트로 강제된다. 다운로드·과금은 아직 0.</done>
</task>

<task type="auto">
  <name>Task 2: 시드 자산 등재 + 가명처리 모듈 + hard-negative 격리 (D-08, D-12)</name>
  <files>backend/training/datagen/anonymize.py, backend/training/data/manifest.json</files>
  <read_first>
    - .planning/phases/22-custom-vlm-finetune/22-CONTEXT.md D-08(시드 목록) + D-12(가명처리 3겹)
    - .planning/phases/22-custom-vlm-finetune/22-RESEARCH.md Open Question 3 (블러 = 기존 YOLO 인프라 + Gaussian blur 최소 경로) + Pitfall 2 (실증 케이스 = hard negative eval 격리)
    - backend/shared/python/sunity_shared/analysis/pose_estimator.py (ultralytics YOLO 로드 방식 — 재사용 지점)
    - .planning/PILOT-FEEDBACK-2026-07-06.md (A2 피터팬 위양성 / A3 power-spin 케이스 식별자)
    - backend/scripts/upload_phase15_dataset.py (시드 페어 S3 적재 시 동일 골격)
  </read_first>
  <action>
    (1) backend/training/datagen/anonymize.py 신설 — 얼굴 가명처리: ultralytics YOLO 얼굴 검출(신규 pip 없이 기존 ultralytics로 얼굴 가중치 로드; 가중치는 스크립트가 멱등 다운로드) + 검출 bbox Gaussian blur(numpy/PIL 순수 함수) → ffmpeg 재인코딩. 순수 blur 함수와 I/O 껍데기 분리. docstring에 D-12 인용: "모델은 포즈/모션만 학습, 얼굴 픽셀 불필요. 고객 영상 트랙 전용(유튜브 공개 영상 범위 밖). 적재 후 소급 불가 — 적재 전 강제". 검출 실패 프레임은 보수적으로 상단 1/3 블러 폴백.
    (2) manifest.json 시드 등재(D-08): 정은지 reference 11 + 일부러-실수 still 페어(기존 S3 fixtures/ 키 참조 또는 fixtures/phase22/pairs/), 실사용 371건은 video_hash + Firestore 문서 경로만 참조(uid 미기재; anonymized=false 상태로는 학습 JSONL 제외 — 22-04 build_jsonl이 anonymized 또는 internal=true만 소비), 2026-07-06 실증 케이스 A2 피터팬·A3 power-spin은 holdout="hard_negative_eval" 마킹(Pitfall 2). 시드 행 provenance: source="internal", license_evidence="자사 촬영/파일럿 참가 동의서(D-12 1겹)".
  </action>
  <verify>
    <automated>python3 -m pytest backend/tests/phase22/test_manifest_consistency.py -x -q && python3 -c "import json; m=json.load(open('backend/training/data/manifest.json')); rows=m['rows']; hn=[r for r in rows if r.get('holdout')=='hard_negative_eval']; assert len(hn)>=2; print('rows', len(rows), 'hard_negative', len(hn))"</automated>
  </verify>
  <acceptance_criteria>
    - manifest.json에 hard_negative_eval 행 ≥ 2 (피터팬·power-spin) + 시드 행 ≥ 12 (reference 11 + still 페어)
    - anonymize.py에 D-12 인용 docstring 존재 (`grep -c "D-12" backend/training/datagen/anonymize.py` ≥ 1)
    - 매니페스트 어느 행에도 uid / 사용자 식별자 필드 없음 (test_provenance 화이트리스트 통과)
  </acceptance_criteria>
  <done>시드 자산이 provenance와 함께 원장에 오르고, 고객 영상은 가명처리 게이트 뒤에, 실증 케이스는 eval 전용 격리 뒤에 놓인다.</done>
</task>

<task type="checkpoint">
  <name>Task 3 (belle 게이트): Vision 실선별 → 수집 실행(YouTube+IG) + LICENSE-AUDIT.md (D-09, FT-06)</name>
  <files>backend/scripts/collect_phase22_instagram.py, backend/scripts/phase22_sources.yaml, backend/training/data/manifest.json, backend/training/LICENSE-AUDIT.md</files>
  <checkpoint>
    이 태스크는 **belle greenlight 후에만** 실행한다. 이유: (1) Gemini Vision 실선별 = 수백 후보 API 과금, (2) 카피라이트 영상을 prod S3에 실제 적재 = 외부·비가역. Task 1·2 완료 후 belle에게 규모(채널별 후보 수·예상 다운로드 수·예상 Vision 호출 수)를 제시하고 승인받은 뒤 `--curate`→`--collect`를 순차 실행한다.
  </checkpoint>
  <read_first>
    - backend/scripts/collect_phase22_youtube.py (Task 1 산출 — 레지스트리 스키마·모드)
    - .planning/phases/22-custom-vlm-finetune/22-DATA-SOURCES.md (채널 목록·필터·caveat — 미성년 polesportkids·IG ToS)
    - .planning/phases/22-custom-vlm-finetune/22-CONTEXT.md D-04 (확정 라이선스 문구 — 재조사 금지) + D-09
    - .planning/phases/22-custom-vlm-finetune/22-NLM-EXTRACT.md §5·§11·§12 (라이선스 노트)
    - .planning/phases/22-custom-vlm-finetune/22-RESEARCH.md Assumptions A9 (유튜브 학습 이용 법적 지위 — 이월)
  </read_first>
  <action>
    (1) backend/scripts/collect_phase22_instagram.py 신설 — gallery-dl(또는 yt-dlp) 공개 릴스 수집 어댑터, YouTube harvester와 동일 매니페스트/provenance 계약 재사용, 로그인월·rate-limit 시 skip+로그, ToS 회색 명시 주석. (2) 수집 실행: `--curate`(Vision 선별, verdict 캐시) → belle에 통과 요약 → `--collect`(통과분 다운로드+적재) → `--check-s3` 셋 일치 → _meta.collection_complete=true로 균등 게이트 활성화. (3) backend/training/LICENSE-AUDIT.md 작성 (FT-06, DD 대비): 모델 라이선스 확정표(D-04 belle 2026-07-06 그대로 — Qwen 3.6 <35B Apache 2.0 / InternVL 3.5 ≤38B 코드 MIT+백본 Apache 2.0 / MMPose Apache 2.0; 재조사 금지), 금지 목록(LLaVA 상업 불가, InternVL-U 생성헤드 ScaleEdit-12M CC BY-NC-SA 오염 — 아키텍처 차용만), 데이터 정책(수집=학습전용·재배포금지·provenance 원장), D-12 동의 3겹, **IG ToS 회색·쿠키 인증 항목**, **@polesportkids 미성년 프라이버시 플래그**, 이월(A9): 공개영상 학습 이용 법적 지위는 파일럿 리서치 유예이며 본 감사 문서는 counsel 서명 전까지 release-clean을 함의하지 않는다 — 상업 출시 gate 별도.
  </action>
  <verify>
    <automated>python3 -m pytest backend/tests/phase22/test_manifest_consistency.py backend/tests/phase22/test_provenance.py -x -q && test -f backend/training/LICENSE-AUDIT.md && grep -c "Apache 2.0" backend/training/LICENSE-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - 균등 게이트 활성(_meta.collection_complete=true)에서 test_manifest_consistency exit 0 — 동작별 max ≤ 2×min, 미보유 동작 ≥ 2종 포함
    - `aws s3 ls s3://sunity-motion-pilot-videos/fixtures/phase22/ --recursive --profile sunity-motion | wc -l` ≥ 매니페스트 수집 행 수 (mp4 + info.json)
    - LICENSE-AUDIT.md에 "재배포" "training-only" "ScaleEdit" "LLaVA" "미성년" "ToS" 문자열 각 ≥ 1회
    - `grep -c "release-clean" backend/training/LICENSE-AUDIT.md` ≥ 1 (A9 유예)
  </acceptance_criteria>
  <done>Vision으로 선별된 전 동작 균등 시드가 provenance와 함께 S3에 적재되고, 라이선스 감사 문서가 DD 대비 상태가 된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 유튜브/인스타 → 로컬/S3 | 외부 신뢰불가 미디어가 수집 파이프라인 진입 |
| 고객 영상 → 학습 적재 경로 | PII(얼굴)가 학습 데이터로 유입될 수 있는 경계 |
| 후보 URL → Gemini Vision | 외부 URL을 외부 LLM에 전달(선별) — 과금·프롬프트 경계 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03 | Tampering | 수집분(악의/저품질 세그먼트) | mitigate | Vision 선별 게이트 + provenance 매니페스트 + label_bucket 이원 + 22-04 judge/휴리스틱 필터 + 학습셋 버전 고정 |
| T-22-04 | Info Disclosure | 고객 영상 얼굴 PII | mitigate | anonymize.py 블러를 적재 전 강제 — build_jsonl이 anonymized=false 고객 행 소비 거부 (D-12). 매니페스트 uid 필드 금지 |
| T-22-05 | Repudiation | 저작권 분쟁 시 출처 입증 불가 | mitigate | info.json 사이드카 + manifest source_url/license_evidence 필수 필드 테스트. usage=training-only-no-redistribution 명문화 |
| T-22-06 | DoS/Tampering | 외부 미디어 파싱(ffmpeg/imageio) | mitigate | 최신 ffmpeg, 파일명 ASCII-safe 정규화, 다운로드 실패는 행 skip + 로그 |
| T-22-07 | Cost/Info | Gemini Vision 대량 선별 과금 + 외부 URL 전달 | mitigate | verdict 캐시(재호출 0), belle 게이트(Task 3), --dry-run 기본 무과금. Gemini 키 미설정 graceful |
| T-22-08 | Compliance | 미성년(@polesportkids)·IG ToS | mitigate | LICENSE-AUDIT에 명시 플래그 + A9 법률검토 이월 항목, 학습전용·비재배포 |
| T-22-SC | Tampering | pip install yt-dlp / gallery-dl | mitigate | 대형 검증 OSS(yt-dlp/gallery-dl). 검증 안 된 third-party MCP 미사용(supply-chain 회피, belle 2026-07-09) |
</threat_model>

<verification>
- `python3 -m pytest backend/tests/phase22 -x -q` GREEN
- S3 fixtures/phase22/ 적재분 ↔ manifest.json 셋 일치 (Task 3 후)
- production 파이프라인 미발화: 수집 후 SQS/analyses 신규 문서 0 (CloudWatch 또는 Firestore 확인)
</verification>

<success_criteria>
- FT-02: 시드 + 채널 harvest + Vision 선별 + 전 동작 균등 매니페스트 완성
- FT-06: provenance 원장 + 라이선스 감사 문서 + 금지 모델 fence 테스트
- D-12 가명처리 구조가 적재 경로에 강제됨
- Vision 선별 게이트가 점수를 내지 않고 저장-전 품질/버킷 판정을 수행
</success_criteria>

<output>
Create `.planning/phases/22-custom-vlm-finetune/22-02-SUMMARY.md` when done
</output>
