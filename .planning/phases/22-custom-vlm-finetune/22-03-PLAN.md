---
phase: 22-custom-vlm-finetune
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/shared/python/sunity_shared/firestore_admin.py
  - backend/functions/pipeline/app.py
  - backend/tests/phase22/test_shadow_wiring.py
  - .planning/phases/22-custom-vlm-finetune/22-POD-VRAM.md
autonomous: false
requirements: [FT-03, FT-05]
must_haves:
  truths:
    - "프로덕션 분석 1건이 돌면 Gemini 판정(veto verdict·recognizer 프로파일·coach 출력)이 vlm_shadow 컬렉션에 복제 저장된다 (D-10c — 증류 라벨 적재 즉시 시작)"
    - "shadow 로깅은 절대 분석을 실패시키지 않는다 — 예외는 삼켜지고 status 필드로만 신호한다"
    - "shadow 로깅 토글은 pipeline app.py 단독 소유 env이며 Gemini 재호출은 0회다"
    - "현행 서빙 Pod의 분석 중 피크 VRAM 실측치가 기록되어 vLLM 동거 예산(D-14)의 입력이 된다"
    - "production Pod 변형은 blocking checkpoint 승인 뒤에만 실행되고 rollback 블록이 변형 전에 기록된다 (DR-02)"
  artifacts:
    - path: "backend/shared/python/sunity_shared/firestore_admin.py"
      provides: "store_vlm_shadow(video_hash, payload) helper — flat-dict 검증 + ms epoch"
      contains: "store_vlm_shadow"
    - path: "backend/functions/pipeline/app.py"
      provides: "VLM_SHADOW_LOG env 토글 + 판정 복제 저장 배선 (status enum)"
      contains: "VLM_SHADOW_LOG"
    - path: ".planning/phases/22-custom-vlm-finetune/22-POD-VRAM.md"
      provides: "분석 1건 중 nvidia-smi 피크 VRAM 실측 기록 (A5 해소)"
  key_links:
    - from: "backend/functions/pipeline/app.py"
      to: "backend/shared/python/sunity_shared/firestore_admin.py"
      via: "shadow 판정 복제 저장 호출"
      pattern: "store_vlm_shadow"
    - from: "backend/functions/pipeline/app.py"
      to: "Firestore vlm_shadow/{video_hash}"
      via: "top-level 컬렉션 set(merge=True)"
      pattern: "vlm_shadow"
---

<objective>
Gemini shadow 로깅을 지금 시작한다(D-10c — "프로덕션 판정 로그가 그대로 증류 라벨로 적재됨"). 기존 Gemini 판정 3역할(veto/recognizer/coach)의 출력을 소비 지점에서 복제 저장만 하고(재호출 0, 판정 경로 무변경), 동시에 서빙 Pod의 상주 VRAM을 실측해 Wave 3 vLLM 동거 판단(D-14)의 입력을 확보한다.

Purpose: shadow 로그는 시간이 지날수록 쌓이는 자산 — 이 plan이 빨리 가동될수록 22-04 증류 트랙(c)과 22-09 shadow 비교의 데이터가 늘어난다.

Output: firestore_admin helper + pipeline 배선 + Pod 스모크/VRAM 실측 artifact.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-custom-vlm-finetune/22-CONTEXT.md
@.planning/phases/22-custom-vlm-finetune/22-PATTERNS.md
@.planning/phases/22-custom-vlm-finetune/22-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: firestore_admin.store_vlm_shadow — flat-dict 검증 helper</name>
  <files>backend/shared/python/sunity_shared/firestore_admin.py, backend/tests/phase22/test_shadow_wiring.py</files>
  <read_first>
    - backend/shared/python/sunity_shared/firestore_admin.py:1423-1494 (store_gemini_cache의 top-level 컬렉션 + nested-array 사전검증 + ms epoch, record_unregistered_keyword의 merge=True 멱등 패턴 — 그대로 복사)
    - .planning/phases/22-custom-vlm-finetune/22-PATTERNS.md §4 (적용 지침 — vlm_shadow/{video_hash}, (T,J) flat 저장, index 면제 주의)
    - firestore.rules (클라이언트가 vlm_shadow에 접근 불가한지 — default deny 확인)
  </read_first>
  <behavior>
    - Test 1: store_vlm_shadow("hash", payload)가 vlm_shadow/{video_hash} 경로에 set(merge=True) + created_at(첫 기록 보존)/updated_at ms epoch 기록 (Firestore client는 mock/stub)
    - Test 2: payload 값에 list-of-list(nested array) 포함 시 TypeError — 저장 전 차단 ([[firestore-nested-array-flat]])
    - Test 3: payload에 uid·email 등 PII 키 포함 시 ValueError — 화이트리스트 키만 허용 (role, verdict, prompt_version, schema_version, model, status, video_hash, frame 인덱스류, 측정 스칼라)
    - Test 4: 같은 video_hash 재호출 시 role별 서브필드 merge — 기존 role 데이터 미파괴
  </behavior>
  <action>
    firestore_admin.py에 _VLM_SHADOW_COLLECTION = "vlm_shadow" (top-level, gemini_cache 형제) + store_vlm_shadow(video_hash: str, role: str, payload: dict) 추가. store_gemini_cache의 nested-array 사전검증을 helper로 재사용/복사, PII 키 거부 화이트리스트 검증 추가(D-12 — shadow 로그에 사용자 식별자 금지; analysis 참조는 video_hash로만). role은 ("veto", "recognizer", "coach") 검증. 문서 구조: { video_hash, created_at, updated_at, roles: { veto: {...}, recognizer: {...}, coach: {...} } } — set(merge=True)로 role별 누적. 대형 배열이 들어갈 경우 flat 저장 + shape 키 (index 면제 필요 시 SUMMARY에 기록 — [[firestore-index-entry-limit]]). docstring에 D-10c/D-13 인용.
  </action>
  <verify>
    <automated>python3 -m pytest backend/tests/phase22/test_shadow_wiring.py -x -q -k "store_vlm_shadow or helper"</automated>
  </verify>
  <acceptance_criteria>
    - test_shadow_wiring.py의 helper 테스트 4종 exit 0
    - `grep -n "store_vlm_shadow" backend/shared/python/sunity_shared/firestore_admin.py` 매치 ≥ 1, nested-array 검증 경로 존재
    - firestore.rules에서 vlm_shadow 클라이언트 접근이 차단됨 확인 (default deny 또는 명시 규칙 — SUMMARY에 근거 라인 기록)
  </acceptance_criteria>
  <done>shadow 판정 저장이 Firestore 규율(flat/ms epoch/merge/PII 금지)을 강제하는 단일 helper로 존재한다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: pipeline shadow 로깅 배선 — VLM_SHADOW_LOG env + status enum (무음실패 금지)</name>
  <files>backend/functions/pipeline/app.py, backend/tests/phase22/test_shadow_wiring.py</files>
  <read_first>
    - backend/functions/pipeline/app.py:244-248 (토글 pipeline 단독 소유 규율 주석), 2276-2311 (_apply_vision_veto status enum), 1058-1090 (_ensure_recognizer env-switch), 747-762 (coach writer 소비 지점), 3841 부근 (coach hook "cosmetic, 분석 절대 실패 안 함" 규율)
    - .planning/phases/22-custom-vlm-finetune/22-PATTERNS.md §2 (배선 지점 3곳 + 무음실패 금지 enum 복사 지침)
    - .planning/phases/22-custom-vlm-finetune/22-RESEARCH.md Pattern 5 (shadow는 복제 저장만 — Gemini 재호출 0)
  </read_first>
  <behavior>
    - Test 1: VLM_SHADOW_LOG 미설정(기본) → shadow 저장 호출 0회, 분석 결과 불변 (disabled)
    - Test 2: VLM_SHADOW_LOG=1 + veto verdict 존재 → store_vlm_shadow("veto", ...) 1회 호출, verdict에 prompt_version/schema_version/model 동봉
    - Test 3: store_vlm_shadow가 예외를 던져도 분석은 성공 완료 — log.exception + status="skipped_error" (분석 실패 절대 유발 금지)
    - Test 4: Gemini 클라이언트 호출 횟수가 shadow 켜기 전후 동일 (재호출 0 — mock call count)
  </behavior>
  <action>
    app.py에 VLM_SHADOW_LOG env 토글 신설(pipeline 단독 소유 — 어댑터 정의 금지, 기존 토글 주석 블록에 등재). 배선 3지점: (1) _apply_vision_veto가 verdict를 만든 직후 소비 지점에서 복제 저장(1순위 — 증류 가치 최대), (2) recognizer 결과(technique profile) 저장, (3) coach writer JSON 출력 저장. 각 지점은 try/except 전체 삼킴 + log.exception + status enum(disabled/skipped_error/applied) — TRUST-08 무음실패 방지. 저장 payload에 PROMPT_VERSION/SCHEMA_VERSION/모델 string 동봉(증류 라벨 provenance). video_hash는 기존 캐시 키 체계 재사용. RunPod BackgroundTasks 경로에서도 동일 코드가 돌므로 mutable 사이드카 금지(local-return만 — app.py:1124-1151 HIGH-1 규율).
  </action>
  <verify>
    <automated>python3 -m pytest backend/tests/phase22/test_shadow_wiring.py -x -q && python3 -m pytest backend/tests -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - test_shadow_wiring.py 전체 exit 0 (helper + 배선)
    - `grep -c "VLM_SHADOW_LOG" backend/functions/pipeline/app.py` ≥ 2 (정의 + 사용), 어댑터/공유 모듈에는 0
    - 전체 backend 테스트 full-suite 실 실행 후 HEAD baseline과 FAILED/ERROR 목록 diff IDENTICAL을 SUMMARY에 기록 (기존 54건 pre-existing failure 목록 대조 — collect-only 금지, DR-07)
  </acceptance_criteria>
  <done>프로덕션 판정 3역할의 shadow 복제 저장이 무음실패 방지 규율과 함께 배선된다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: production Pod 변형 승인 — canary-first + rollback 블록 (DR-02)</name>
  <what-built>Pod 변형 계획 제시 자료 — (a) clone/canary Pod 우선 절차(동일 이미지·Volume/스크립트 형상·라이브 트래픽 0)와 production 접근 전제 조건: canary에서 warmup OK + 분석 스모크 OK + Firestore write OK + nvidia-smi 예산 OK + rollback 커맨드 문서화 완료, (b) 현재 Pod(svn31pzja7uay0) 직접 변형이 불가피한 경우의 rollback 블록 — env revert 커맨드(VLM_SHADOW_LOG 제거), start_server.sh revert 경로(Volume 쪽 원본 백업), 서버 재기동 커맨드, /health 체크, Lambda RUNPOD_ANALYZE_URL/env sync 체크.</what-built>
  <how-to-verify>
    1. canary 경로(a)와 현재 Pod 직접 경로(b) 중 택일 — rollback 블록 완비 여부 확인
    2. 승인 후 실행은 Claude가 SSH로 수행 ([[pod-ops-claude-runs]])
  </how-to-verify>
  <resume-signal>"approved"(canary 경로) / "current-pod"(현재 Pod 직접, rollback 블록 전제) / 조정 지시</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Pod 배포 + shadow 스모크 + 피크 VRAM 실측 (A5)</name>
  <files>.planning/phases/22-custom-vlm-finetune/22-POD-VRAM.md</files>
  <read_first>
    - ~/.claude/projects/-Users-kimtaesung-Dev-SunityMotion/memory/current-pod-hbpvhedq2bu01i.md (Pod svn31pzja7uay0 SSH 접속 + start_server.sh 함정)
    - .planning/phases/22-custom-vlm-finetune/22-RESEARCH.md Pattern 4 step 1 (실측 먼저 — vLLM 예산의 입력)
    - backend/evals/phase24/run_sweep.py:22-29 (Pod 실행 env 헤더 — fixtures 분석 실행 방법)
  </read_first>
  <action>
    (0) rollback 블록 선기록 (DR-02): env revert / start_server.sh revert 경로(수정 전 원본을 Volume에 백업) / 재기동 커맨드 / health check / Lambda RUNPOD_ANALYZE_URL·env sync check를 SUMMARY 초안에 먼저 적고 나서 변형 시작. (1) 로컬 commit 후 즉시 push ([[gsd-pod-work-push-first]]) → Pod(svn31pzja7uay0, SSH root@213.173.102.233:12729)에서 git pull. (2) Pod env에 VLM_SHADOW_LOG=1 설정(start_server.sh 수정 시 Volume 쪽에 반영 — Pod 재생성 함정) 후 서버 재기동. (3) 스모크: fixtures 영상 1건 분석(run_sweep 단건 경로 또는 /analyze 직접 호출, X-RunPod-Token) → Firestore vlm_shadow/{video_hash} 문서 생성 확인(roles.veto 존재) + 분석 status=completed 불변. (4) 분석 진행 중 별도 SSH 세션에서 1초 간격 nvidia-smi 폴링으로 피크 VRAM 기록(NLF TorchScript + RTMW onnxruntime + YOLO 상주분 + 분석 피크). (5) .planning/phases/22-custom-vlm-finetune/22-POD-VRAM.md 작성: 총 VRAM(24GB), idle 상주, 분석 피크, vLLM 가용 예산 계산(예: 피크 12GB → gpu-memory-utilization 0.35~0.45 권장), 측정 일시/Pod ID. 이 수치가 22-08의 --gpu-memory-utilization 값 입력.
  </action>
  <verify>
    <automated>test -f .planning/phases/22-custom-vlm-finetune/22-POD-VRAM.md && grep -cE "peak|피크" .planning/phases/22-custom-vlm-finetune/22-POD-VRAM.md</automated>
  </verify>
  <acceptance_criteria>
    - Firestore에 vlm_shadow 문서 ≥ 1 (roles.veto 필드 포함) — `node app/scripts/verify-firebase.mjs` 류 또는 firestore_admin 읽기 스크립트로 확인, SUMMARY에 문서 경로 기록
    - 스모크 분석의 최종 status=completed (shadow가 분석을 깨지 않음 실증)
    - 22-POD-VRAM.md에 피크 VRAM 수치(GB)와 vLLM 예산 권고치가 숫자로 기록됨
    - rollback 블록이 변형 전에 기록됨 (SUMMARY 근거 — DR-02)
  </acceptance_criteria>
  <done>shadow 로깅이 프로덕션에서 실동하며 증류 라벨이 적재되기 시작하고, vLLM 동거 판단의 실측 입력이 확보된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline → Firestore vlm_shadow | 판정 로그(잠재 PII)가 신규 컬렉션에 축적 |
| 클라이언트 앱 → Firestore | vlm_shadow가 앱에서 읽히면 정보 노출 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-07 | Info Disclosure | vlm_shadow 로그 PII | mitigate | store_vlm_shadow 화이트리스트 검증 — uid/식별자 키 거부, video_hash 참조만. 테스트로 강제 (Task 1 Test 3) |
| T-22-08 | Elevation | 클라이언트의 vlm_shadow 읽기 | mitigate | firestore.rules default deny 확인 (Task 1 acceptance) — backend Admin SDK 전용 |
| T-22-09 | DoS | shadow 저장 실패가 분석 경로 전파 | mitigate | 예외 전체 삼킴 + status enum + log.exception — Task 2 Test 3으로 강제 |
| T-22-SC | Tampering | 신규 패키지 설치 | accept | 이 plan은 pip 설치 없음 (기존 firebase-admin/boto3 재사용) |
</threat_model>

<verification>
- `python3 -m pytest backend/tests/phase22/test_shadow_wiring.py -x -q` GREEN
- Pod 스모크: 분석 completed + vlm_shadow 문서 생성
- 22-POD-VRAM.md 실측 수치 존재
</verification>

<success_criteria>
- FT-03(c) shadow 로깅 즉시 가동 — 프로덕션 트래픽이 증류 라벨로 적재되기 시작
- FT-05의 shadow 저장 인프라(컬렉션/helper/토글) 선행 완성
- A5(VRAM 실측) 해소 — D-14 동거 판단 입력 확보
</success_criteria>

<output>
Create `.planning/phases/22-custom-vlm-finetune/22-03-SUMMARY.md` when done
</output>
