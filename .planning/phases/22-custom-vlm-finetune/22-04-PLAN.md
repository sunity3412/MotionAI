---
phase: 22-custom-vlm-finetune
plan: 04
type: execute
wave: 2
depends_on: ["22-01", "22-02", "22-03"]
files_modified:
  - backend/training/distill/__init__.py
  - backend/training/distill/gemini_teacher.py
  - backend/training/datagen/build_jsonl.py
  - backend/tests/phase22/test_build_jsonl.py
  - backend/tests/phase22/test_gemini_teacher.py
autonomous: false
requirements: [FT-03, FT-02]
must_haves:
  truths:
    - "Gemini 교사 증류 배치가 업로드→호출→즉시 삭제 규율을 지켜 File API 20GB 적체가 재발하지 않는다"
    - "LLM judge 7점 미만 라벨과 물리 불가 궤적/반복 루프 샘플이 학습셋에서 폐기된다 (D-10b)"
    - "학습 JSONL의 프레임이 기존 9fps 배열 인덱스 서브샘플이며 원 영상 재추출이 0회다 (Pattern 1)"
    - "학습 JSONL이 T3 순수 텍스트 시간추론 + 순수 텍스트 instruction을 혼합하고(D-11), 매니페스트↔S3↔JSONL 3자 정합이 테스트로 강제된다"
    - "hard_negative_eval 격리 행과 미가명처리 고객 행이 학습 JSONL에 포함되지 않는다 — 미등록/미가명 shadow는 text-only 라벨로 강등되고 media 참조 0 (DR-01)"
  artifacts:
    - path: "backend/training/distill/gemini_teacher.py"
      provides: "교사 증류 배치 (File API 생명주기 + judge/휴리스틱 필터)"
      min_lines: 120
    - path: "backend/training/datagen/build_jsonl.py"
      provides: "3트랙(교란/증류/shadow) JSONL 조립 + T3/텍스트 혼합 + train/val split"
      min_lines: 100
  key_links:
    - from: "backend/training/datagen/build_jsonl.py"
      to: "backend/training/datagen/schema.py"
      via: "리포트 스키마/이산화/프레임 서브샘플 규칙 import"
      pattern: "from .schema import|from schema import"
    - from: "backend/training/distill/gemini_teacher.py"
      to: "backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py"
      via: "File API 업로드→ACTIVE 폴링→삭제 헬퍼 재사용"
      pattern: "files.delete|_upload_video"
---

<objective>
D-10b 교사 증류 트랙 + 학습 JSONL 조립기를 구축하고 증류 배치를 실행해 SFT 학습셋(train/val JSONL)을 완성한다.

Purpose: 좌표 보정은 합성 교란(22-01, 교사 무관)이, 짚기/코칭은 Gemini 증류가 라벨을 공급한다. 증류는 상한이 교사 품질(Pitfall 2)이므로 judge/휴리스틱 필터와 hard-negative 격리가 함께 간다. JSONL 포맷의 원형은 belle 노트 §8 (system / user video+RTMW_Data / assistant thought+통합리포트).

Output: gemini_teacher.py + build_jsonl.py + 학습 JSONL(S3 training/phase22/jsonl/) + 3자 정합 테스트.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-custom-vlm-finetune/22-CONTEXT.md
@.planning/phases/22-custom-vlm-finetune/22-NLM-EXTRACT.md
@.planning/phases/22-custom-vlm-finetune/22-RESEARCH.md
@backend/training/datagen/schema.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: gemini_teacher.py — 교사 증류 배치 (File API 생명주기 + 필터)</name>
  <files>backend/training/distill/__init__.py, backend/training/distill/gemini_teacher.py, backend/tests/phase22/test_gemini_teacher.py</files>
  <read_first>
    - backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py:761-843, 1004, 1128 (클라이언트 싱글톤 + _upload_video ACTIVE 폴링 + files.delete 규율 — 20GB 적체 누수 fix 이력)
    - .planning/phases/22-custom-vlm-finetune/22-NLM-EXTRACT.md §6 (다단계 품질 필터링 — judge 0~10 7점 미만 폐기, 반복 탐지, 물리 불가 궤적 휴리스틱, 일관성 투표)
    - .planning/phases/22-custom-vlm-finetune/22-RESEARCH.md Pitfall 5 (크레딧/스토리지) + Pitfall 2 (교사 결함 상속)
    - backend/training/data/manifest.json (22-02 산출 — 증류 대상 행)
    - ~/.claude/projects/-Users-kimtaesung-Dev-SunityMotion/memory/gemini-latest-model-versions.md 요지: 교사=gemini-3.1-pro-preview, judge=gemini-3.5-flash, 2.5 계열 금지
  </read_first>
  <action>
    backend/training/distill/gemini_teacher.py 신설 — 배치 흐름: (1) 시작 전 크레딧/quota 확인(1건 시험 호출, 429 시 즉시 중단 — [[gemini-credits-depleted-2026-06-20]]) + client.files.list 잔량 로그, (2) manifest 행 순회(holdout=hard_negative_eval 제외, 고객 행은 anonymized=true만): S3 다운로드 → File API 업로드(ACTIVE 폴링, gemini_vision_scorer 헬퍼 재사용 또는 동일 패턴 복사) → 교사 모델 gemini-3.1-pro-preview에 schema.py bind_key_prompt 시스템 프롬프트 + RTMW 좌표(select_frame_indices 서브샘플 행만) 입력 → D-01 통합 리포트 JSON + thought 수신(교사 프롬프트는 faults[] 각 항목에 student_angle_deg/reference_angle_deg/measurement_basis/fault_category 를 그대로 산출하도록 요구 — 감점 엔진 계약 필드가 새 REPORT_KEYS 조립 시 탈락하면 채점 불가, schema faults ⊇ DEDUCTION_CONSUMED_KEYS 미충족 샘플 폐기) → 즉시 client.files.delete(name=...) (finally 블록 — 실패해도 삭제), (3) 필터: LLM judge(gemini-3.5-flash, 0~10 블라인드 채점) < 7 폐기 + 반복 루프 탐지(동일 n-gram 반복) + 물리 불가 궤적 휴리스틱(perturb.py의 속도/가속도 체크 로직 재사용) + 뼈길이 정합, (4) 출력 = 로컬 distill JSONL + 필터 통계 리포트(수락/폐기 사유별 카운트). video_hash 캐시로 재실행 시 재업로드 0. 시크릿 = env GEMINI_API_KEY 우선 → SSM /sunity/motion/gemini-api-key (--profile sunity-motion, 키 로그 금지). 사람 숫자 점수 라벨 필드 생성 금지 — judge 점수는 필터 임계로만 쓰고 라벨에 저장하지 않음. delete-in-finally는 정적 grep이 아니라 실제 unit test로 증명(DR-07): backend/tests/phase22/test_gemini_teacher.py에 fake Gemini client(업로드/생성/삭제 호출 기록 stub)를 만들어 — 생성(generate) 호출이 예외를 던져도 files.delete가 호출됨을 assert하는 테스트 작성.
  </action>
  <verify>
    <automated>python3 -c "import ast,sys; src=open('backend/training/distill/gemini_teacher.py').read(); assert 'files.delete' in src or 'delete(name=' in src; assert 'gemini-3.1-pro-preview' in src; assert 'gemini-2.5' not in src; print('teacher ok')" && python3 -m pytest backend/tests/phase22/test_gemini_teacher.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - 소스에 files.delete 호출이 finally/직후 경로에 존재 + 교사 모델 string이 gemini-3.1-pro-preview, judge가 gemini-3.5-flash (2.5 계열 0회)
    - judge 임계 7 미만 폐기 로직 존재 (`grep -n "7" 필터 함수` — 상수명 JUDGE_MIN_SCORE=7)
    - hard_negative_eval / anonymized=false 고객 행 제외 로직 존재 (grep으로 holdout·anonymized 필터 확인)
  </acceptance_criteria>
  <done>교사 증류가 스토리지 누수·크레딧 소진·교사 결함 상속 방어와 함께 배치 가능한 상태가 된다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: build_jsonl.py — 3트랙 조립 + T3/텍스트 혼합 + 3자 정합</name>
  <files>backend/training/datagen/build_jsonl.py, backend/tests/phase22/test_build_jsonl.py</files>
  <read_first>
    - backend/training/datagen/schema.py + backend/training/datagen/perturb.py (22-01 산출 — 규격과 교란 API)
    - .planning/phases/22-custom-vlm-finetune/22-NLM-EXTRACT.md §8 (학습 JSONL verbatim 원형 — messages 3롤 구조)
    - .planning/phases/22-custom-vlm-finetune/22-RESEARCH.md Pattern 1 (frame = 9fps 원 인덱스 canonical key) + Open Question 4 (T3 혼합비 = ablation 축, 수치 단언 금지)
    - backend/tests/phase22/test_manifest_consistency.py (22-02 산출 — 3자 정합 확장 지점)
    - backend/shared/python/sunity_shared/analysis/deduction_engine.py:181-247 (faults[] 가 상위집합이어야 하는 supported_differences 소비 계약 — F1)
  </read_first>
  <behavior>
    - Test 1: 조립된 샘플이 belle 노트 원형 구조 — messages[0].role=system(bind_key_prompt 산출), messages[1]=video 참조+RTMW_Data 텍스트(서브샘플 인덱스 행만, frame=9fps 원 인덱스), messages[2]=assistant(thought + REPORT_KEYS 알파벳 정렬 JSON)
    - Test 2: assistant JSON에 score 계열 키 0 (schema.normalize_report 통과 강제)
    - Test 3: hard_negative_eval 행과 anonymized=false 고객 행이 출력 JSONL에 0건
    - Test 4: 동작별 샘플 수 균등 게이트 (max ≤ 2 x min — 22-02와 동일 비율) + 트랙별(perturb/distill/shadow/text) 카운트가 _meta에 기록
    - Test 5: T3 순수 텍스트 시간추론 샘플과 순수 텍스트 instruction 샘플이 혼합비 config(기본 0.15, ablation 축으로 조정 가능)만큼 포함
    - Test 6: train/val split이 video_hash 단위 (같은 영상이 양쪽에 등장 0 — leakage 차단)
    - Test 7: assistant faults[] 항목이 deduction_engine 소비 계약(fault_category+student_angle_deg+reference_angle_deg+measurement_basis) 상위집합 — 미충족 샘플은 build_jsonl에서 폐기/제외 (schema faults ⊇ DEDUCTION_CONSUMED_KEYS lockstep 재사용, F1 계약 보존)
    - Test 8: svg_spec.target_angle_deg 가 reference/IPSF 각도에서 결정적으로 산출되고(교사 무관, corrected_coords 와 동일 원리), force_vector/ideal_trajectory 는 교사 증류분 또는 Null — svg_spec 전체가 REPORT_KEYS 스키마 유효(픽셀 생성 아님, D-01 v1 필수 출력, F2)
    - Test 9: 미등록 shadow video_hash(manifest row 부재 또는 anonymized != true) → video-referencing 샘플 0건 (text-only verdict 라벨은 허용 — DR-01)
    - Test 10: _meta.shadow_unregistered_dropped / _meta.shadow_text_only_count 카운터가 방출된다 (드롭률 은폐 불가 — DR-01)
    - Test 11: _meta.collection_complete=false manifest → build_jsonl 실패(--partial 없이), --partial run은 canonical S3 prefix(training/phase22/jsonl/)에 업로드 0 (DR-06)
  </behavior>
  <action>
    backend/training/datagen/build_jsonl.py 신설 — 진입점 assert(DR-06, fail-closed): 시작 시 manifest `_meta.collection_complete is true`를 assert하고, 명시적 `--partial` 플래그가 없으면 false/부재 시 즉시 실패. `--partial` run은 canonical prefix `training/phase22/jsonl/` 업로드 금지(별도 partial prefix 또는 업로드 자체 skip). 입력: manifest.json + perturb 트랙(22-01 perturb_sequence로 좌표보정 샘플 생성: 정답=원좌표를 assistant corrected_coords로) + distill JSONL(Task 1) + Firestore vlm_shadow 적재분(22-03 — firestore_admin 읽기, veto/coach 라벨화). shadow 유입 규칙(DR-01): 모든 shadow 유래 샘플은 manifest.json을 video_hash로 join해야 한다. manifest row가 없거나 anonymized != true면 text-only verdict 라벨만 허용(영상/프레임/media 참조 0). 프레임을 쓰려면 anonymized=true + manifest에 등록된 anonymized S3 key가 필수. _meta에 shadow_unregistered_dropped / shadow_text_only_count 카운터 방출(드롭률 은폐 불가). T3 시간추론 텍스트 합성(순수 텍스트로 프레임 순서/속도 추론 Q-A — 시각 없이 시간축 각성, D-11) + 순수 텍스트 instruction 혼합(언어능력 감퇴 방지). MIX_RATIOS dict config(기본 text 계열 합 0.15 — 수치는 ablation 축임을 주석 명시). svg_spec 라벨 조립(F2): target_angle_deg 는 reference/IPSF criteria 에서 결정적 산출(자가 라벨, 교사 무관), force_vector·ideal_trajectory 는 교사 증류분(judge 통과분)만, 셋 다 결측 시 Null 고정(D-11). faults[] 는 교사 differences[] 측정 필드(각도쌍/measurement_basis)를 보존해 감점 엔진 계약 상위집합 유지(F1) — 미충족 샘플 폐기. 출력: train.jsonl / val.jsonl (video_hash 단위 split 0.98/0.02 — video_hash 단위 split의 소유자는 build_jsonl.py 단독이다. SFT(22-07)는 val.jsonl을 명시적 val 파일로 소비하며 내부 재분할을 하지 않는다. ms-swift가 explicit val 파일 인자를 지원하지 않으면 val.jsonl을 발행하지 않고 held-out eval gate(22-07 assert_gates)가 validation owner임을 _meta와 22-07 스크립트 헤더에 문서화한다 (DR-04)) + _meta(트랙별/동작별 카운트, SCHEMA_VERSION, manifest 버전, runId). S3 업로드: training/phase22/jsonl/ (비-notified prefix). test_build_jsonl.py는 소형 fixture manifest로 조립 로직 검증(네트워크 0 — Firestore/S3는 주입 가능한 loader 인터페이스로 분리).
  </action>
  <verify>
    <automated>python3 -m pytest backend/tests/phase22/test_build_jsonl.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_build_jsonl.py ≥ 11 tests exit 0
    - build_jsonl.py가 imageio/ffmpeg 프레임 재추출을 호출하지 않음 — `grep -E "imageio|ffmpeg" backend/training/datagen/build_jsonl.py` 매치 0 (Pattern 1: 인덱스 서브샘플만)
    - `python3 -m pytest backend/tests/phase22 -x -q` 전체 GREEN
  </acceptance_criteria>
  <done>3트랙 학습셋 조립이 규격·격리·균등·leakage 방지와 함께 재현 가능한 코드로 존재한다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 증류 비용 checkpoint — full batch 승인 (DR-05)</name>
  <what-built>자동 준비 자료 — manifest 증류 대상 행 수, 예상 teacher(gemini-3.1-pro-preview) call 수, 예상 judge(gemini-3.5-flash) call 수, 현재 quota/credit probe 결과(1건 시험 호출 + client.files.list 잔량 — [[gemini-credits-depleted-2026-06-20]]), 첫 run max rows=10, abort threshold(예: 429 1회 또는 폐기율 임계) 제시.</what-built>
  <how-to-verify>
    1. 첫 배치 10 rows 실행 → 필터 통계(수락/폐기 사유별) + File API 잔여물(client.files.list) 검사 결과 확인
    2. "approved" 시 full batch(Task 4) 진행
  </how-to-verify>
  <resume-signal>"approved" 또는 조정 지시</resume-signal>
</task>

<task type="auto">
  <name>Task 4: 증류 배치 실행 + 학습셋 완성 + 3자 정합 확인</name>
  <files>backend/training/data/manifest.json</files>
  <read_first>
    - backend/training/distill/gemini_teacher.py + backend/training/datagen/build_jsonl.py (Task 1·2 산출)
    - .planning/phases/22-custom-vlm-finetune/22-RESEARCH.md Validation Architecture Wave 0 (매니페스트 3자 정합 + 균등 카운트)
  </read_first>
  <action>
    (1) 증류 배치 실행: gemini_teacher.py를 manifest 전 대상 행에 실행(재실행 안전 — video_hash 캐시). 종료 후 client.files.list 잔량 0 확인(누수 0). 필터 통계(수락률, 폐기 사유별)를 SUMMARY에 기록. (2) build_jsonl.py 실행 → train.jsonl/val.jsonl 생성 + S3 training/phase22/jsonl/ 업로드. (3) 3자 정합: test_manifest_consistency의 --check-s3 경로 + JSONL 카운트 대조(매니페스트 학습 대상 행 수 ↔ JSONL 영상 수). manifest _meta에 jsonl_run_id/생성 시각 기록. (4) 학습셋 통계 리포트(동작별 x 버킷별 x 트랙별 표)를 SUMMARY에 포함 — kip-up 편중 없음을 수치로 증명.
  </action>
  <verify>
    <automated>python3 -m pytest backend/tests/phase22/test_manifest_consistency.py backend/tests/phase22/test_build_jsonl.py -x -q && aws s3api head-object --bucket sunity-motion-pilot-videos --key training/phase22/jsonl/train.jsonl --profile sunity-motion</automated>
  </verify>
  <acceptance_criteria>
    - S3에 train.jsonl object key 존재(head-object exit 0 — prefix listing 아님, DR-07). val.jsonl은 DR-04 결정에 따라 발행 시에만 동일 head-object로 확인 — 미발행 옵션 선택 시 val 체크 대신 _meta의 validation-owner 문서화를 grep으로 확인
    - gemini files.list 잔량 0 (증류 후 — SUMMARY에 출력 기록)
    - 동작별 학습 샘플 카운트 표에서 max ≤ 2 x min, hard_negative 0건 포함
  </acceptance_criteria>
  <done>SFT가 소비할 학습셋이 S3에 완성되고 정합·균등·격리가 수치로 증명된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| S3 학습 영상 → Gemini File API | 학습 영상이 외부 API로 업로드 |
| Gemini 출력 → 학습 JSONL | 교사 모델 출력(신뢰 불가)이 학습 라벨로 유입 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-10 | Tampering | 교사 라벨 오염(교사 결함 상속) | mitigate | judge <7 폐기 + 물리 휴리스틱 + hard-negative eval 격리 + 합성 트랙은 교사 무관 (Pitfall 2 4중 방어) |
| T-22-11 | Info Disclosure | 고객 영상 외부 업로드 | mitigate | anonymized=true 고객 행만 증류 대상 (D-12) — Task 1 필터 + Task 2 Test 3 |
| T-22-12 | DoS | File API 20GB 적체 / 크레딧 소진 | mitigate | 즉시 삭제(finally) + 배치 전 크레딧 확인 + files.list 잔량 0 acceptance |
| T-22-13 | Info Disclosure | Gemini API 키 유출 | mitigate | SSM WithDecryption 단일 소스, 키 값 로그 금지(T-20-06 전례), JSONL/_meta에 키 미포함 |
| T-22-SC | Tampering | 신규 패키지 설치 | accept | 설치 없음 (google-genai/boto3 기존 의존 재사용) |
</threat_model>

<verification>
- `python3 -m pytest backend/tests/phase22 -x -q` GREEN
- S3 JSONL 존재 + 3자 정합 + 균등 통계
- Gemini 파일 잔량 0
</verification>

<success_criteria>
- FT-03 라벨 3경로(a 합성 / b 증류 / c shadow) 전부 학습셋에 합류
- FT-02 학습셋이 train/val JSONL로 완성 — 22-07 SFT의 직접 입력
- 사람 숫자 점수 라벨 0 (버킷·측정·짚기만)
</success_criteria>

<output>
Create `.planning/phases/22-custom-vlm-finetune/22-04-SUMMARY.md` when done
</output>
