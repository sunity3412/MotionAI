---
phase: 22-custom-vlm-finetune
plan: 05
type: execute
wave: 2
depends_on: ["22-01", "22-02"]
files_modified:
  - backend/evals/phase22/run_bakeoff.py
  - backend/evals/phase22/fixtures/manifest.yaml
  - backend/tests/phase22/test_bakeoff_harness.py
autonomous: true
requirements: [FT-01]
must_haves:
  truths:
    - "bake-off 4축(grounding L2 / 시계열 / JSON 준수 / 코칭 judge)이 계측 함수로 구현되고 pod-free 단위 테스트로 검증된다"
    - "grounding L2는 합성 교란 셋에서만 계측된다 — 실영상엔 진짜 정답 좌표가 없다 (Open Question 1 명시)"
    - "역재생/순서 섞기 함정 데이터가 미니셋에 포함되어 언어 프라이어 shortcut이 검출된다 (Pitfall 6)"
    - "eval 미니셋이 전 동작 균등 + 미보유 동작 + hard negative를 포함한다"
    - "하네스가 SERIAL 실행·EVAL_OUT_DIR repo-밖·_meta provenance·cold re-run 규율을 지킨다"
  artifacts:
    - path: "backend/evals/phase22/run_bakeoff.py"
      provides: "Wave 1 bake-off 하네스 (run_sweep 골격, 4축 계측, 양 백본 순차 평가)"
      min_lines: 150
    - path: "backend/evals/phase22/fixtures/manifest.yaml"
      provides: "eval 미니셋 매니페스트 (균등 + 함정 + hard negative + 코너케이스)"
    - path: "backend/tests/phase22/test_bakeoff_harness.py"
      provides: "계측 함수 pod-free 검증 (L2/EM/CER/함정 채점)"
  key_links:
    - from: "backend/evals/phase22/run_bakeoff.py"
      to: "backend/training/datagen/schema.py"
      via: "동일 시스템 프롬프트(bind_key_prompt) + normalize_report 파서"
      pattern: "bind_key_prompt|normalize_report"
    - from: "backend/evals/phase22/fixtures/manifest.yaml"
      to: "backend/training/datagen/perturb.py"
      via: "합성 교란/함정 데이터 생성"
      pattern: "perturb|trap"
---

<objective>
D-04 bake-off(Qwen 3.6-VL-8B vs InternVL 3.5-8B)의 하네스와 평가 미니셋을 로컬에서 완성한다. 실행(Pod)은 22-06.

Purpose: bake-off의 신뢰도는 하네스 설계에서 결정된다 — 언어 프라이어 함정, 동일 프롬프트 통제, 합성-only grounding, 결정성 재실행이 없으면 변별력이 허수가 된다(Pitfall 6). run_sweep 골격을 복사해 검증된 eval 규율을 승계한다.

Output: run_bakeoff.py + fixtures/manifest.yaml + 계측 pod-free 테스트.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-custom-vlm-finetune/22-CONTEXT.md
@.planning/phases/22-custom-vlm-finetune/22-NLM-EXTRACT.md
@.planning/phases/22-custom-vlm-finetune/22-RESEARCH.md
@backend/evals/phase24/run_sweep.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: eval 미니셋 매니페스트 — 균등 + 함정 + hard negative</name>
  <files>backend/evals/phase22/fixtures/manifest.yaml</files>
  <read_first>
    - backend/evals/phase18/pairs.yaml (매니페스트 전례 — 페어/known_issue 구조)
    - backend/training/data/manifest.json (22-02 산출 — 소스 풀, holdout 마킹)
    - .planning/phases/22-custom-vlm-finetune/22-NLM-EXTRACT.md Q7 (미니셋 구성 — 정상/미세결함/가려짐/조도·앵글 코너케이스 + 함정)
    - backend/training/datagen/perturb.py (make_temporal_trap API)
  </read_first>
  <action>
    backend/evals/phase22/fixtures/manifest.yaml 작성 — 항목 타입 4종: (a) real: 학습 매니페스트에서 동작별 균등 추출(등록 5동작 + climb + 미보유 ≥2종, 각 동작 정타/fault 균형; 학습 JSONL과 겹치는 영상은 eval_only=false 표기 — bake-off는 zero-shot이라 오염 아님을 주석 명시), (b) hard_negative: A2 피터팬 위양성·A3 power-spin (22-02 holdout 행 인용), (c) synthetic_grounding: perturb.py 교란 시퀀스 참조(정답=원좌표 — grounding L2의 유일한 계측 트랙, Open Question 1), (d) trap: make_temporal_trap 역재생/순서 셔플(함정 정답률이 정상과 동일하게 높으면 shortcut 경고). 각 행: id / type / motion / s3_key 또는 생성 시드 / expected(타입별 정답 스펙). `_meta`: 총 카운트, 동작별 카운트, 균등 비율, 생성 스크립트 버전. 규모: 초기 40~80 클립(동작당 균등 — NLM Q7의 100~500은 목표치, 시드 규모에 맞춰 축소하되 전 동작 커버 우선임을 _meta에 명시).
  </action>
  <verify>
    <automated>python3 -c "import yaml; m=yaml.safe_load(open('backend/evals/phase22/fixtures/manifest.yaml')); rows=m['items']; types={r['type'] for r in rows}; assert {'real','hard_negative','synthetic_grounding','trap'} <= types; motions={r.get('motion') for r in rows if r['type']=='real'}; assert len(motions)>=7; print('items', len(rows), 'motions', len(motions))"</automated>
  </verify>
  <acceptance_criteria>
    - 4개 타입 전부 존재, real 트랙 동작 ≥ 7종(등록5 + climb + 미보유 ≥ 2 중 7 이상)
    - hard_negative에 피터팬·power-spin 행 존재
    - _meta에 동작별 카운트 표 존재 (kip-up이 최다 아님)
  </acceptance_criteria>
  <done>bake-off와 이후 SFT 게이트가 공유할 균등·함정 포함 평가 미니셋이 박제된다.</done>
</task>

<task type="auto">
  <name>Task 2: run_bakeoff.py — 4축 계측 하네스 (run_sweep 골격)</name>
  <files>backend/evals/phase22/run_bakeoff.py</files>
  <read_first>
    - backend/evals/phase24/run_sweep.py (전체 — EVAL_OUT_DIR repo-밖 강제, SERIAL, _meta, cold re-run, ALLDONE 마커, Pod env 헤더 docstring을 그대로 복사)
    - .planning/phases/22-custom-vlm-finetune/22-NLM-EXTRACT.md Q7 (4축 지표 정의 + CircularEval + few-shot 2~3 + guided JSON)
    - backend/training/datagen/schema.py (bind_key_prompt / normalize_report / REPORT_KEYS)
    - .planning/phases/22-custom-vlm-finetune/22-RESEARCH.md "Don't Hand-Roll" (guided decoding — vLLM response_format)
  </read_first>
  <action>
    backend/evals/phase22/run_bakeoff.py 신설 (run_sweep 골격 복사). 구조: (1) 모델 백엔드 = vLLM OpenAI 호환 endpoint(--model 인자에 후보 지정, 한 번에 한 모델 — 학습 Pod에서 vllm serve로 후보를 순차 기동), 프롬프트 = schema.bind_key_prompt 기반 동일 시스템 프롬프트 + few-shot 2~3개(프레임+완성 JSON — 양 모델 동일), guided JSON(response_format json_schema) 사용해 포맷 변수 통제. (2) 4축 계측 함수(importable, 순수): score_grounding(pred, truth) = 관절별 L2 평균(synthetic_grounding 트랙만), score_temporal = 동작 순서/역재생 함정 정오(trap 트랙 CircularEval — 선택지 순서 전 조합 정답 시 1점), score_json = 파싱 성공률 + REPORT_KEYS Exact Match + CER(레벤슈타인), score_coaching = LLM judge(gemini-3.5-flash 블라인드 1~5 — 모델명 은닉). (3) 실행 규율: SERIAL(모델당 항목 순차), EVAL_OUT_DIR repo-밖 강제(_resolve_out_dir 복사), cold re-run 2회로 결정성(temp 0 + greedy) 비교, 리포트 JSON에 _meta(runId, model_id, prompt/schema version, "objectivity: 사람 점수 라벨 아님 — 합성 정답/함정 정오/포맷 계측") + 4축 요약 표 + ALLDONE 마커. (4) 프레임 입력: 파이프라인 9fps 배열 인덱스 서브샘플(select_frame_indices) — 원 영상 재추출 금지, 프레임당 448x448 리사이즈.
  </action>
  <verify>
    <automated>python3 -c "import importlib.util,sys; sys.path.insert(0,'backend/shared/python'); spec=importlib.util.spec_from_file_location('rb','backend/evals/phase22/run_bakeoff.py'); m=importlib.util.module_from_spec(spec); spec.loader.exec_module(m); assert all(hasattr(m,f) for f in ('score_grounding','score_temporal','score_json','score_coaching')); print('harness importable')"</automated>
  </verify>
  <acceptance_criteria>
    - 4축 계측 함수가 import 가능 (module-level GPU/네트워크 의존 없음 — lazy)
    - `grep -n "EVAL_OUT_DIR" backend/evals/phase22/run_bakeoff.py` 존재 + repo-밖 강제 로직 존재
    - Pod 실행 env 헤더 docstring 존재 (필요 env 전체 복붙 가능 형태)
  </acceptance_criteria>
  <done>양 백본을 동일 조건에서 4축으로 재는 하네스가 Pod 실행만 남은 상태로 완성된다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 계측 함수 pod-free 단위 테스트</name>
  <files>backend/tests/phase22/test_bakeoff_harness.py</files>
  <read_first>
    - backend/evals/phase22/run_bakeoff.py (Task 2 산출)
    - backend/evals/phase22/fixtures/manifest.yaml (Task 1 산출)
  </read_first>
  <behavior>
    - Test 1: score_grounding — 동일 좌표 입력 시 L2=0, 알려진 오프셋 주입 시 정확한 L2 값
    - Test 2: score_json — 유효 리포트 = 파싱 1.0 + EM 1.0, 키 누락/여분 키/정렬 위반 각각 감지
    - Test 3: score_temporal — 함정(역재생) 항목에 정방향 답 제출 시 0점, CircularEval 전 조합 일치 시만 1점
    - Test 4: score_coaching judge 프롬프트에 모델명/후보명 문자열 미포함 (블라인드 채점 보장)
    - Test 5: manifest.yaml 로드 → 4 타입 파싱 + synthetic_grounding 항목만 grounding 계측 대상으로 라우팅
  </behavior>
  <action>
    backend/tests/phase22/test_bakeoff_harness.py 작성 — run_bakeoff의 계측 함수를 직접 import(모델 호출 없음, 순수 로직만). 고정 fixture 입출력으로 4축 계산 검증. score_coaching은 judge 호출부를 주입 가능한 callable로 설계했는지 검증(mock judge).
  </action>
  <verify>
    <automated>python3 -m pytest backend/tests/phase22/test_bakeoff_harness.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_bakeoff_harness.py ≥ 5 tests exit 0, GPU/네트워크 0 (pod-free)
    - `python3 -m pytest backend/tests/phase22 -x -q` 전체 GREEN
  </acceptance_criteria>
  <done>bake-off 계측의 수학이 Pod 실행 전에 검증되어, Pod 시간이 순수 추론에만 쓰인다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 후보 모델 출력 → 계측 파서 | 신뢰 불가 모델 출력이 하네스에 유입 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-14 | Tampering | 모델 출력 파싱(스키마 밖 키/거대 배열) | mitigate | schema.normalize_report 화이트리스트 + guided JSON + 파싱 실패는 score_json 감점으로 계측(크래시 아님) |
| T-22-15 | Info Disclosure | eval 리포트에 시크릿/PII | mitigate | _meta 화이트리스트(runId/model_id/버전만), EVAL_OUT_DIR repo-밖 강제 |
| T-22-16 | Repudiation | bake-off 결과 재현 불능 | mitigate | _meta provenance + temp 0 + cold re-run 2회 + 미니셋 버전 고정 |
| T-22-SC | Tampering | 신규 패키지 설치 | accept | 로컬 설치 없음 (yaml/numpy 기존; Pod 설치는 22-06 스코프) |
</threat_model>

<verification>
- `python3 -m pytest backend/tests/phase22 -x -q` GREEN
- manifest.yaml 4 타입 + 균등 검증
- run_bakeoff.py import 가능 + 규율 grep 확인
</verification>

<success_criteria>
- FT-01의 계측기가 pod-free로 검증 완료 — 22-06은 실행과 판정만 수행
- grounding=합성-only 한계가 하네스와 문서에 명시 (실영상 좌표 정확도는 shadow 일치율로 대리)
</success_criteria>

<output>
Create `.planning/phases/22-custom-vlm-finetune/22-05-SUMMARY.md` when done
</output>
