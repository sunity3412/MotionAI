---
phase: 22-custom-vlm-finetune
plan: 06
type: execute
wave: 3
depends_on: ["22-05"]
files_modified:
  - backend/training/setup_training_pod.sh
  - .planning/phases/22-custom-vlm-finetune/22-BAKEOFF-RESULT.md
autonomous: false
requirements: [FT-01]
must_haves:
  truths:
    - "학습 Pod 임대 비용이 belle에게 사전 승인된다 (하드 제약 — 비용 발생 지점 belle 알림, Pod 운영 실행은 Claude)"
    - "Qwen 3.6-VL-8B와 InternVL 3.5-8B가 동일 하네스·동일 프롬프트·동일 미니셋으로 4축 계측된다"
    - "cold re-run 2회에서 결정성(같은 입력=같은 출력)이 확인된다"
    - "백본 선정이 4축 리포트 근거와 함께 belle 확인으로 확정되고 문서로 박제된다 (D-04 — 성능만으로 선정, 라이선스 재조사 금지)"
  artifacts:
    - path: "backend/training/setup_training_pod.sh"
      provides: "학습 Pod 멱등 셋업 (ms-swift/vllm/lmms-eval 설치 + 후보 모델 다운로드)"
    - path: ".planning/phases/22-custom-vlm-finetune/22-BAKEOFF-RESULT.md"
      provides: "4축 결과 표 + 함정 데이터 변별 + 선정 근거 + belle 확정 기록"
  key_links:
    - from: "backend/training/setup_training_pod.sh"
      to: "backend/evals/phase22/run_bakeoff.py"
      via: "Pod에서 SERIAL 실행"
      pattern: "run_bakeoff"
---

<objective>
학습 Pod를 임대(belle 승인 후)하고 bake-off를 실행해 백본을 확정한다 (D-04, D-16 Wave 1).

Purpose: 둘 다 라이선스 클린(belle 2026-07-06 확정)이므로 순수 성능 변별. 8B로 시작(D-05), 판정 신호는 NLM Q7 — Qwen 우세=시계열 순서/궤적(M-RoPE), InternVL 우세=OOM 적음/처리 속도/ViR 압축.

Output: 셋업 스크립트 + bake-off 리포트 + 백본 확정 문서.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-custom-vlm-finetune/22-CONTEXT.md
@.planning/phases/22-custom-vlm-finetune/22-RESEARCH.md
@backend/evals/phase22/run_bakeoff.py
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: 학습 Pod 임대 비용 belle 승인</name>
  <what-built>bake-off/SFT용 학습 Pod 임대 계획 — 서빙 Pod와 분리(VRAM·수명 주기, RESEARCH Architectural Responsibility Map). GPU 우선순위 = EU-RO-1 리전 4090 → RTX PRO 4500 → 5090 → PRO 6000 → A4500 ([[runpod-eu-ro1-gpu-priority]]), Network Storage 필수 ([[next-pod-use-network-storage]]), 디스크는 16-bit 8B 베이스(약 16GB+) x 후보 2종 + 데이터셋 여유로 Volume 100GB 이상.</what-built>
  <how-to-verify>
    1. 제시된 견적 확인: GPU 종류/시간당 요금, 예상 사용 시간(bake-off 수 시간 + 이후 22-07 SFT 재사용), Network Storage 용량/요금
    2. 임대는 Claude가 RunPod API/콘솔로 실행 (belle는 승인만 — [[pod-ops-claude-runs]])
  </how-to-verify>
  <action>임대 전 견적 산출을 자동화해 제시: RunPod EU-RO-1 가용 GPU/시간당 요금 조회, 예상 사용 시간(bake-off + 22-07 SFT 재사용), Network Storage 용량(100GB+) 견적. 승인 후 임대는 Task 2에서 Claude가 실행 (비용 발생 지점 belle 알림 — 하드 제약).</action>
  <resume-signal>"approved" 입력 시 임대 진행, 또는 GPU/예산 조정 지시</resume-signal>
</task>

<task type="auto">
  <name>Task 2: 학습 Pod 임대 + 셋업 + bake-off SERIAL 실행</name>
  <files>backend/training/setup_training_pod.sh</files>
  <read_first>
    - backend/runpod_inference/setup.sh (멱등 패턴 — set -euo pipefail, 존재 시 skip, 말미 env export 안내)
    - .planning/phases/22-custom-vlm-finetune/22-RESEARCH.md Standard Stack (버전: ms-swift 4.4.0 / vllm 0.24.0 / lmms-eval 0.7.2 — Wave 착수 시 버전 재확인, Valid until 2026-07-20) + Assumptions A6 (모델 ID는 ms-swift Supported Models 문서에서 확정)
    - ~/.claude/projects/-Users-kimtaesung-Dev-SunityMotion/memory/runpod-gpu-env.md 요지 (sunity3412 계정, Stop 금지 함정)
    - backend/evals/phase22/run_bakeoff.py docstring (실행 env 헤더)
  </read_first>
  <action>
    (1) RunPod 임대 실행(승인된 스펙, Network Storage) — Pod ID/SSH 정보를 SUMMARY와 메모리 후보로 기록, belle에게 임대 완료 알림 문구 포함. (2) backend/training/setup_training_pod.sh 작성(멱등): pip install "ms-swift[all]" vllm lmms-eval (RESEARCH Package Legitimacy Audit 전부 [OK] Approved), Qwen3.6-VL-8B / InternVL3.5-8B 정확 모델 ID를 ms-swift Supported Models 문서(swift.readthedocs.io)에서 확정 후 스크립트 상수로 박제(A6 해소 — 확정 ID를 22-BAKEOFF-RESULT.md에도 기록), HF 가중치 다운로드(Volume 캐시), 미니셋 S3 sync(aws s3 sync fixtures/phase22 + synthetic 트랙 생성), 말미에 필요한 env 전체 echo. (3) bake-off 실행: 후보 A vllm serve 기동 → run_bakeoff.py SERIAL 실행(zero-shot + few-shot) → cold re-run 2회 → 종료 후 후보 B 동일 절차(동시 서빙 금지 — VRAM). EVAL_OUT_DIR=/workspace/eval_out (repo-밖). 리포트 2벌 + ALLDONE 확인. push-first 규율: 로컬 스크립트 commit/push 후 Pod에서 pull.
  </action>
  <verify>
    <automated>bash -n backend/training/setup_training_pod.sh && grep -cE "ms-swift|vllm" backend/training/setup_training_pod.sh</automated>
  </verify>
  <acceptance_criteria>
    - Pod에서 후보 2종 각각 리포트 JSON 생성(4축 값 + _meta.model_id + cold re-run 일치 여부) — SUMMARY에 경로/요약 기록
    - 함정(trap) 트랙 점수가 리포트에 별도 표기 (정상 트랙과 동일하게 높으면 shortcut 경고 플래그)
    - setup_training_pod.sh 멱등 (2회 실행 무해) + bash -n 통과
  </acceptance_criteria>
  <done>양 백본의 4축 실측 리포트가 확보된다.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: 백본 확정 (belle) + 결과 박제</name>
  <decision>SFT 백본 선정 — Qwen 3.6-VL-8B vs InternVL 3.5-8B (D-04: 성능만으로, 라이선스 재조사 금지)</decision>
  <context>4축 결과 표(grounding L2 / 시계열·함정 / JSON 준수 / 코칭 judge) + 결정성(cold re-run) + 실행 특성(OOM/속도)을 제시. 판정 신호: 시계열·궤적 우세=Qwen(M-RoPE), 메모리·속도·미세 인식 우세=InternVL(ViR). 동률이면 시계열 축 우선(우리 태스크 = 모션 분석).</context>
  <options>
    <option id="qwen">
      <name>Qwen 3.6-VL-8B</name>
      <pros>Apache 2.0 단일, M-RoPE 시계열 강점, ms-swift 지원 성숙</pros>
      <cons>longest_edge 토큰 캡 관리 필요</cons>
    </option>
    <option id="internvl">
      <name>InternVL 3.5-8B</name>
      <pros>ViR 토큰 압축(-Flash), 타일 동적 해상도, v2 생성 헤드(D-03) 아키텍처 연속성</pros>
      <cons>코드 MIT+백본 Apache 이원 구조</cons>
    </option>
  </options>
  <action>결정 제시 전 자동화: 후보 2종 리포트에서 4축 비교 표 + 함정 변별 + cold re-run 결정성 요약을 생성해 제시. belle 선택 수신 후 .planning/phases/22-custom-vlm-finetune/22-BAKEOFF-RESULT.md에 4축 표 + 선정 근거 + belle 확정 일시 + 확정 모델 ID(A6)를 박제 — 22-07이 참조 (D-04: 성능만으로 선정).</action>
  <resume-signal>"qwen" 또는 "internvl" 선택 (또는 8B 재실험 지시)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HF/PyPI → 학습 Pod | 외부 가중치/패키지가 Pod에 설치 |
| 학습 Pod SSH | 신규 인프라 표면 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-17 | Tampering | pip/HF 공급망 | mitigate | RESEARCH Package Legitimacy Audit 6/6 [OK] Approved (ms-swift/vllm/lmms-eval — 공식 repo 확인). 모델은 공식 org(Qwen/OpenGVLab) HF 경로만, 스크립트에 ID 박제 |
| T-22-18 | Info Disclosure | Pod env 시크릿(AWS 키/Gemini 키) | mitigate | SSM/env 주입만, setup.sh에 키 값 하드코딩 금지, echo 안내는 변수명만 |
| T-22-19 | Elevation | 학습 Pod 포트 노출 | mitigate | bake-off vllm serve는 localhost 바인드 — 외부 프록시 미등록 (서빙 Pod와 달리 인바운드 불필요) |
| T-22-SC | Tampering | 신규 설치(ms-swift/vllm/lmms-eval) | mitigate | slopcheck 감사 완료(Approved) — 추가 검증 불필요, 버전 핀(4.4.0/0.24.0/0.7.2 기준, 착수 시 재확인) |
</threat_model>

<verification>
- 후보 2종 4축 리포트 + cold re-run 결정성 기록
- 22-BAKEOFF-RESULT.md에 belle 확정 기록
</verification>

<success_criteria>
- FT-01 완료: 백본이 실측 근거 + belle 확인으로 확정
- 학습 Pod가 22-07 SFT에 즉시 재사용 가능한 상태
</success_criteria>

<output>
Create `.planning/phases/22-custom-vlm-finetune/22-06-SUMMARY.md` when done
</output>
