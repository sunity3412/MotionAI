---
phase: 22-custom-vlm-finetune
plan: 07
type: execute
wave: 4
depends_on: ["22-04", "22-06"]
files_modified:
  - backend/training/sft/run_sft.sh
  - backend/training/export/merge_and_quant.sh
  - backend/evals/phase22/assert_gates.py
  - backend/tests/phase22/test_assert_gates.py
autonomous: false
requirements: [FT-04]
must_haves:
  truths:
    - "SFT는 ms-swift QLoRA(4-bit, 전 선형 레이어, rank 64~128)로 실행되고 학습 비용이 belle에게 사전 알림된다 (D-06)"
    - "어댑터 병합은 16-bit 원본 경유 후 AWQ 재양자화된다 — 4-bit 직접 병합 0회 (Pitfall 1)"
    - "SFT 게이트가 Phase 24 4종(추적성·단조성·결정성·일반화) 확장 + EVAL18 6페어 무회귀 + 전 동작 균등(미보유 포함) + 합성 held-out을 exit 코드로 판정한다 (D-15)"
    - "게이트 FAIL 시 27B 승급은 자동이 아니라 belle 논의를 거친다 (D-05)"
    - "eval은 SERIAL 실행이다"
  artifacts:
    - path: "backend/training/sft/run_sft.sh"
      provides: "ms-swift QLoRA SFT 실행 스크립트 (검증된 초기 파라미터)"
    - path: "backend/training/export/merge_and_quant.sh"
      provides: "16-bit 병합 → AWQ 재양자화 → vLLM 로드 검증"
    - path: "backend/evals/phase22/assert_gates.py"
      provides: "D-15 SFT 게이트 (importable check_*, ARTIFACT-GATED, exit 0=PASS)"
      min_lines: 120
  key_links:
    - from: "backend/training/export/merge_and_quant.sh"
      to: "s3://sunity-motion-pilot-videos/training/phase22/checkpoints/"
      via: "AWQ 체크포인트 적재 (비-notified prefix)"
      pattern: "training/phase22/checkpoints"
    - from: "backend/evals/phase22/assert_gates.py"
      to: "backend/evals/phase18/dataset/pairs.yaml"
      via: "EVAL18 6페어 무회귀 게이트"
      pattern: "phase18"
---

<objective>
bake-off 승자 8B에 QLoRA SFT(D-06)를 실행하고, 16-bit 병합→AWQ 파이프라인으로 배포 체크포인트를 만들고, D-15 게이트(assert_gates)로 출하 가능성을 판정한다 (D-16 Wave 2). v1은 SFT까지 — RL은 Wave 4 후속 plan (D-07).

Purpose: LoRA 어댑터 방식은 D-03의 v2 재사용 구조(같은 백본에 어댑터/헤드 추가)와 정합 — 백본 가중치를 보존한 채 도메인 능력을 얹는다. 게이트는 Phase 24 독트린을 확장하는 것이지 재발명이 아니다.

Output: run_sft.sh + merge_and_quant.sh + assert_gates.py + AWQ 체크포인트(S3) + 게이트 판정.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-custom-vlm-finetune/22-CONTEXT.md
@.planning/phases/22-custom-vlm-finetune/22-RESEARCH.md
@.planning/phases/22-custom-vlm-finetune/22-BAKEOFF-RESULT.md
@backend/evals/phase24/assert_gates.py
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: SFT 학습 비용 belle 알림/승인</name>
  <what-built>SFT 실행 계획 — 학습 Pod(22-06 임대분) 계속 사용. 예상: QLoRA 8B 수 시간~수십 시간(데이터 규모 의존) + 16-bit 베이스 디스크 16GB+ (Volume 여유 확인 완료 여부 보고). 하이퍼파라미터 초기값과 스윕 계획(LR 1e-5 기준, ViT 2e-6 — A4: 초기값+스윕으로만 사용) 제시.</what-built>
  <how-to-verify>
    1. 예상 GPU 시간/비용과 Volume 잔여 용량 확인
    2. 실행은 Claude가 SSH로 수행 ([[pod-ops-claude-runs]])
  </how-to-verify>
  <action>제시 자료 자동 준비: 학습셋 규모(JSONL 샘플 수)에서 예상 step/GPU 시간 산출, Pod Volume 잔여 용량 확인(df -h), 16-bit 베이스 디스크 16GB+ 확보 여부, 하이퍼파라미터 초기값 표(LR 1e-5 / ViT 2e-6 — A4 스윕 전제). 승인 후 학습 실행은 Task 2 (비용 발생 지점 belle 알림 — 하드 제약).</action>
  <resume-signal>"approved" 또는 조정 지시</resume-signal>
</task>

<task type="auto">
  <name>Task 2: run_sft.sh + 학습 실행 + merge_and_quant.sh (16-bit 병합 → AWQ)</name>
  <files>backend/training/sft/run_sft.sh, backend/training/export/merge_and_quant.sh</files>
  <read_first>
    - .planning/phases/22-custom-vlm-finetune/22-RESEARCH.md Code Examples "ms-swift QLoRA SFT" + "병합 → 재양자화 → 서빙" (검증된 파라미터 세트) + Pitfall 1 (4-bit 병합 함정) + Assumptions A2(autoawq)·A8(인자명)
    - .planning/phases/22-custom-vlm-finetune/22-NLM-EXTRACT.md §14·§18·§22·§26 (VRAM 최적화 + 전 선형 타겟 + 병합 수순) + Q1 (packing, aligner unfreeze, LR 분리)
    - .planning/phases/22-custom-vlm-finetune/22-BAKEOFF-RESULT.md (확정 백본 ID)
    - backend/evals/phase24/run_sweep.py:22-29 (Pod 실행 헤더 docstring 규율 — 셸 스크립트 헤더에 동일 적용)
  </read_first>
  <action>
    (1) backend/training/sft/run_sft.sh: swift sft — --model {bake-off 승자 ID} --dataset train.jsonl(S3 training/phase22/jsonl/에서 sync) --split_dataset_ratio 0.02 --train_type lora --lora_rank 64 --lora_alpha 128 --target_modules all-linear --quant_bits 4 --torch_dtype bfloat16 --gradient_checkpointing true --optim paged_adamw_8bit --packing true --max_length 32768 --per_device_train_batch_size 1 --gradient_accumulation_steps 16 --learning_rate 1e-5 (+ViT 분리 LR 인자는 지원 시 2e-6). 실행 전 `swift sft --help`로 인자명 재확인(A8 — 4.x 개편 이력) 후 스크립트에 실제 인자 박제. 헤더 docstring에 Pod env 전체 + 데이터 sync 명령 복붙 가능 형태. Aligner(프로젝터) unfreeze 설정(Q1 — 지원 인자 확인). (2) 학습 실행(Pod) — loss 곡선/val loss를 SUMMARY에 기록, 체크포인트는 Volume + S3 training/phase22/checkpoints/ 백업. (3) backend/training/export/merge_and_quant.sh: 16-bit(BF16) 원본 베이스 확보 → swift export --adapters {ckpt} --merge_lora true → swift export --model {merged} --quant_method awq --quant_bits 4 (awq 실패 시 GPTQ → llm-compressor 폴백 순서를 스크립트 분기로 — A2) → vllm serve 로드 스모크(짧은 추론 1건, guided JSON 파싱 성공) → AWQ 결과물 S3 training/phase22/checkpoints/{run_id}/awq/ 업로드. 4-bit 베이스 직접 병합 경로 없음을 주석으로 봉인 (Pitfall 1).
  </action>
  <verify>
    <automated>bash -n backend/training/sft/run_sft.sh && bash -n backend/training/export/merge_and_quant.sh && grep -c "merge_lora true" backend/training/export/merge_and_quant.sh</automated>
  </verify>
  <acceptance_criteria>
    - SFT 학습 완료: 최종 체크포인트 + train/val loss 수치가 SUMMARY에 기록
    - AWQ 체크포인트가 S3 training/phase22/checkpoints/에 존재 (`aws s3 ls ... --profile sunity-motion`)
    - vLLM 로드 스모크: AWQ 모델이 vllm serve로 기동되고 1건 추론이 REPORT_KEYS JSON을 반환 (파싱 성공 — SUMMARY에 출력 첨부)
    - merge_and_quant.sh에 4-bit 직접 병합 경로 없음 (16-bit 경유가 유일 경로)
  </acceptance_criteria>
  <done>배포 가능한 AWQ 체크포인트가 검증된 병합 수순으로 만들어진다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: assert_gates.py — D-15 SFT 게이트 작성 + Pod eval 실행 + 판정</name>
  <files>backend/evals/phase22/assert_gates.py, backend/tests/phase22/test_assert_gates.py</files>
  <read_first>
    - backend/evals/phase24/assert_gates.py (전체 — check_* importable 구조, ARTIFACT-GATED SKIPPED≠FAIL, 독트린 docstring: 사람 점수 라벨 금지 / determinism scope 분리 / 수치 밴드 미단언)
    - backend/evals/phase18/dataset/pairs.yaml + backend/evals/phase18/assert_baseline.py (EVAL18 6페어 baseline + self-consistency)
    - backend/evals/phase22/fixtures/manifest.yaml + backend/evals/phase22/run_bakeoff.py (미니셋/계측 재사용)
    - .planning/phases/22-custom-vlm-finetune/22-RESEARCH.md Validation Architecture (Wave 2 게이트 정의) + Pitfall 8 (결정성 범위 정직성)
  </read_first>
  <behavior>
    - Test 1: check_synthetic_holdout — 합성 교란 held-out에서 보정 L2가 무보정 대비 개선 (개선 없으면 FAIL; 절대 임계 밴드 단언 금지 — 상대 개선만)
    - Test 2: check_motion_balance — eval 결과가 전 동작 커버(미보유 포함) + 동작별 계측 존재, kip-up 단독 검증 시 FAIL
    - Test 3: check_eval18_no_regression — EVAL18 6페어에 대해 SFT 모델의 결함 짚기(veto 대체 관점) verdict가 baseline 변별(변별 4페어 + kip-up/climb known 처리)과 모순되지 않음 — 하네스 직접 추론(파이프라인 swap 아님, Wave 3 전 오프라인 재현임을 docstring 명시)
    - Test 4: check_determinism — 같은 입력 cold 2회 verdict 동일 (temp 0 + greedy; GPU bit-비결정과 verdict 결정성을 분리 서술)
    - Test 5: ARTIFACT-GATED — Pod artifact 없으면 각 check가 SKIPPED 반환(FAIL 아님), main() exit 코드 규약(0=PASS, 1=FAIL, SKIPPED만 있으면 0 + 경고)
    - Test 6: check_svg_spec_validity — SFT 모델 리포트의 svg_spec 이 스키마 유효(REPORT_KEYS 통과 + target_angle_deg 수치형) + 결함 프레임에서 non-null 비율 ≥ 기준(형식·존재 계측, 생물역학 정합 아님 — 정답 부재 도메인/no-overfit; 전량 null/garbage 시 FAIL). svg_spec 은 D-01 v1 필수 5출력 중 하나이므로 게이트 포함 (F2)
  </behavior>
  <action>
    backend/evals/phase22/assert_gates.py 신설 (phase24 assert_gates 골격 확장 — 재발명 금지). check_* 6종: synthetic_holdout / motion_balance / eval18_no_regression / determinism / traceability_and_monotonicity(Phase 24 4종 중 추적성·단조성은 모델이 점수를 안 내므로 "출력 리포트의 측정값이 좌표에서 역산 가능(추적성)" + "교란 강도 증가 시 검출 결함 수 비감소(단조성)" 형태로 번안 — 번안 근거를 docstring에 명시). check_svg_spec_validity: svg_spec 스키마 유효 + target_angle_deg 수치형 + 결함 프레임 non-null 비율 게이트(D-01 v1 SVG 스펙 필수 출력, 생물역학 정합이 아닌 형식·존재 계측 — F2). 입력 artifact = run_bakeoff.py를 SFT 모델로 재실행한 리포트(EVAL_OUT_DIR) — SERIAL. 실행: Pod에서 SFT AWQ 모델로 미니셋 + EVAL18 6페어 + 합성 held-out 순차 평가 → assert_gates 판정. 게이트 FAIL 시: 하이퍼파라미터 스윕 1회(LR/rank — A4) 재시도 → 여전히 FAIL이면 D-05 27B 승급 여부를 belle 논의 항목으로 SUMMARY에 기록(자동 승급 금지). test_assert_gates.py는 합성 artifact fixture로 check 함수 pod-free 검증.
  </action>
  <verify>
    <automated>python3 -m pytest backend/tests/phase22/test_assert_gates.py -x -q && python3 backend/evals/phase22/assert_gates.py; test $? -le 1</automated>
  </verify>
  <acceptance_criteria>
    - test_assert_gates.py exit 0 (pod-free, SKIPPED 의미론 포함)
    - Pod eval 실행 후 `python3 backend/evals/phase22/assert_gates.py` 판정 결과(PASS/FAIL/게이트별 상세)가 SUMMARY에 기록
    - FAIL 시 27B 승급 논의가 belle 항목으로 기록되고 임의 진행 없음
  </acceptance_criteria>
  <done>SFT 모델의 출하 가능성이 D-15 게이트의 exit 코드로 판정되고, Wave 3 진입 조건이 명확해진다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 학습셋(JSONL) → 모델 가중치 | 데이터 오염이 가중치로 각인되는 지점 |
| S3 체크포인트 | 배포 아티팩트 무결성 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-20 | Tampering | 학습 데이터 오염 → 가중치 | mitigate | 22-04 필터 체인 + 학습셋 버전 고정(manifest _meta.jsonl_run_id) — 재현 가능한 재학습 경로 확보 |
| T-22-21 | Tampering | 체크포인트 변조/혼동 | mitigate | S3 training/phase22/checkpoints/{run_id}/ 버전 경로 + SUMMARY에 sha256 기록, 비-notified prefix |
| T-22-22 | Info Disclosure | 학습 로그에 시크릿 | mitigate | run_sft.sh는 env 참조만, 키 값 echo 금지 |
| T-22-SC | Tampering | 신규 설치 | accept | 22-06 셋업 재사용 — 이 plan 신규 pip 0 (autoawq는 swift export 간접 의존, Audit Approved) |
</threat_model>

<verification>
- assert_gates 판정 기록 (PASS 또는 FAIL+후속 결정)
- AWQ 체크포인트 S3 존재 + vLLM 로드 스모크
- eval 전 과정 SERIAL 실행 로그
</verification>

<success_criteria>
- FT-04: SFT v1 모델 + 배포 체크포인트 + D-15 게이트 판정 완료
- 게이트 PASS 시 Wave 3(22-08 서빙) 진입 가능, FAIL 시 belle 논의 경로 명확
</success_criteria>

<output>
Create `.planning/phases/22-custom-vlm-finetune/22-07-SUMMARY.md` when done
</output>
