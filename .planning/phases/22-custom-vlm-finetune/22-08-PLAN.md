---
phase: 22-custom-vlm-finetune
plan: 08
type: execute
wave: 5
depends_on: ["22-07", "22-03"]
files_modified:
  - backend/shared/python/sunity_shared/analysis/interfaces.py
  - backend/shared/python/sunity_shared/analysis/vlm_judge.py
  - backend/runpod_inference/setup_vllm.sh
  - backend/runpod_inference/server.py
  - backend/tests/phase22/test_vlm_judge.py
autonomous: false
requirements: [FT-05]
must_haves:
  truths:
    - "자체 모델 어댑터가 Protocol 경계 뒤에 있고, vLLM 미기동 시 graceful no-op이다 (분석 크래시 0)"
    - "vLLM은 localhost 바인드 + 실측 기반 gpu-memory-utilization으로 기동되며 기존 분석 경로(NLF/RTMW)를 붕괴시키지 않는다 (Pitfall 3)"
    - "기동 순서 = 기존 파이프라인 웜업 후 vLLM (선점 순서 역전 시 NLF OOM)"
    - "동거 실패 판정 기준(OOM 또는 분석 지연 +20% 초과)이 수치로 박제되고, 초과 시 D-14 폴백(별도 pod)으로 전환한다"
    - "어댑터 출력 파서 화이트리스트가 schema.py REPORT_KEYS와 lockstep이다"
    - "production Pod 변형은 blocking checkpoint 승인 뒤에만 실행되고 rollback 블록이 변형 전에 기록된다 (DR-02)"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/vlm_judge.py"
      provides: "자체 VLM 어댑터 (vLLM HTTP, temp 0, 캐시, 방어 파싱, graceful)"
      min_lines: 100
    - path: "backend/shared/python/sunity_shared/analysis/interfaces.py"
      provides: "VlmJudge Protocol + FallbackVlmJudge"
      contains: "VlmJudge"
    - path: "backend/runpod_inference/setup_vllm.sh"
      provides: "서빙 Pod vLLM 멱등 셋업 + 기동 순서"
  key_links:
    - from: "backend/shared/python/sunity_shared/analysis/vlm_judge.py"
      to: "http://127.0.0.1:8001/v1"
      via: "vLLM OpenAI 호환 HTTP (urllib, localhost 전용)"
      pattern: "8001"
    - from: "backend/runpod_inference/server.py"
      to: "vLLM 프로세스"
      via: "/health vllm_loaded 필드"
      pattern: "vllm_loaded"
---

<objective>
자체 모델을 서빙 라인에 올린다: VlmJudge Protocol + 어댑터(vlm_judge.py) + 서빙 Pod vLLM 동거(AWQ 4-bit, D-14). 판정 경로는 아직 Gemini 그대로 — 이 plan은 인프라만 세우고 shadow 병행/swap은 22-09.

Purpose: D-14의 기본형(기존 4090 동거)을 실측 예산(22-03 VRAM artifact) 위에서 검증하고, 실패 기준 초과 시 별도 pod 폴백을 즉시 발동할 수 있는 상태를 만든다. 부수효과: swap 완료 시 Gemini File API 라운드트립 소멸 → Phase 27(속도 1분) 기여.

Output: Protocol + 어댑터 + vLLM 셋업/기동 + 동거 검증 기록.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-custom-vlm-finetune/22-CONTEXT.md
@.planning/phases/22-custom-vlm-finetune/22-PATTERNS.md
@.planning/phases/22-custom-vlm-finetune/22-RESEARCH.md
@.planning/phases/22-custom-vlm-finetune/22-POD-VRAM.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: VlmJudge Protocol + vlm_judge.py 어댑터 (graceful + 방어 파싱)</name>
  <files>backend/shared/python/sunity_shared/analysis/interfaces.py, backend/shared/python/sunity_shared/analysis/vlm_judge.py, backend/tests/phase22/test_vlm_judge.py</files>
  <read_first>
    - backend/shared/python/sunity_shared/analysis/interfaces.py:25-105 (PoseEngine Protocol 정의 패턴 — D-번호 docstring + direct-import 금지 선언 + Fallback 동봉)
    - backend/shared/python/sunity_shared/analysis/coach_writer.py:213-320 (graceful 어댑터 골격 + _normalize_entry 방어 파싱 — 그대로 복사할 수준)
    - backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py:744-780 (SSM 키 로딩 + 클라이언트 싱글톤)
    - backend/functions/pipeline/app.py:122-148 (urllib HTTP 위임 패턴 — requests 의존 0)
    - backend/training/datagen/schema.py (REPORT_KEYS — 파서 화이트리스트의 lockstep 대상)
  </read_first>
  <behavior>
    - Test 1: VlmJudge Protocol이 interfaces.py에 존재 — judge(frames_ref, rtmw_rows, context) -> dict|None 시그니처, docstring에 D-13 인용 + "score 필드 영구 부재" 명시, FallbackVlmJudge는 None 반환 무해 폴백
    - Test 2: VllmJudgeAdapter — VLLM_BASE_URL 미설정/접속 실패 시 예외 없이 None 반환 + log.exception (graceful — 분석 크래시 0)
    - Test 3: 응답 파싱 — REPORT_KEYS 화이트리스트 외 키 폐기, nested-array 거부, 거대 배열 크기 상한(프레임 수 x 관절 수 상한 초과 시 절단+경고), score 계열 키 유입 시 폐기 + 경고 로그
    - Test 4: 파서 화이트리스트 == schema.REPORT_KEYS lockstep (training.datagen.schema import 대조 — drift 시 FAIL)
    - Test 5: temp 0 + greedy 요청 파라미터 고정, video_hash 기반 캐시 재사용 (같은 입력 2회 → HTTP 1회)
  </behavior>
  <action>
    (1) interfaces.py에 VlmJudge Protocol + FallbackVlmJudge 추가 (PoseEngine 전례 형식 — 구현체 위치 명시, 무거운 SDK top-level import 0, TYPE_CHECKING lazy). recognizer 역할 swap은 기존 TechniqueRecognizer Protocol 재사용(신규 Protocol 불필요 — PATTERNS §1). (2) vlm_judge.py 신설: VllmJudgeAdapter — env VLLM_BASE_URL(기본 http://127.0.0.1:8001/v1), urllib.request POST /chat/completions (OpenAI 호환, guided JSON response_format), 시스템 프롬프트 = schema.bind_key_prompt와 동일 규격(서빙 프롬프트 상수 + PROMPT_VERSION), temperature 0 + greedy, 입력 = 파이프라인 9fps 프레임 배열 인덱스 서브샘플(select_frame_indices — 재추출 금지) + 해당 인덱스 RTMW 행, 캐시 = video_hash+PROMPT_VERSION 키(TechniqueCache 전례), 방어 파싱 = coach_writer._normalize_entry 수준 화이트리스트(REPORT_KEYS lockstep — schema를 직접 import할 수 없는 배포 환경 대비 상수 복사 + 테스트 lockstep으로 drift 차단). 전부 broad except → None + log.exception (경계에서만).
  </action>
  <verify>
    <automated>python3 -m pytest backend/tests/phase22/test_vlm_judge.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_vlm_judge.py ≥ 5 tests exit 0
    - `grep -n "import requests" backend/shared/python/sunity_shared/analysis/vlm_judge.py` 매치 0 (urllib만)
    - interfaces.py에 VlmJudge + FallbackVlmJudge 존재, 백본 SDK direct-import 0
    - 전체 backend 테스트 baseline diff IDENTICAL
  </acceptance_criteria>
  <done>자체 모델이 Protocol 경계 뒤 graceful 어댑터로 소비 가능해진다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 서빙 Pod vLLM 동거 승인 — canary-first + rollback 블록 (DR-02)</name>
  <what-built>vLLM 동거 계획 제시 자료 — 22-POD-VRAM.md 실측 예산, gpu-memory-utilization 제안값, (a) clone/canary Pod에서 먼저 vLLM 동거 검증(warmup OK + 분석 스모크 OK + Firestore write OK + nvidia-smi 예산 OK + rollback 커맨드 문서화 완료) 후 production 접근, (b) 현재 Pod 직접 진행 시 rollback 블록 — vLLM 프로세스 중지/제거 커맨드, start_server.sh revert 경로(Volume 쪽 원본 백업), 서버 재기동 커맨드, /health 체크(vllm_loaded 필드 회귀 무해 확인), Lambda RUNPOD_ANALYZE_URL/env sync 체크.</what-built>
  <how-to-verify>
    1. canary 경로(a)와 현재 Pod 직접 경로(b) 중 택일 — 실측 예산·rollback 블록 완비 여부 확인
    2. 승인 후 실행은 Claude가 SSH로 수행 ([[pod-ops-claude-runs]])
  </how-to-verify>
  <resume-signal>"approved"(canary 경로) / "current-pod"(현재 Pod 직접, rollback 블록 전제) / 조정 지시</resume-signal>
</task>

<task type="auto">
  <name>Task 3: 서빙 Pod vLLM 동거 — setup_vllm.sh + 기동 순서 + health</name>
  <files>backend/runpod_inference/setup_vllm.sh, backend/runpod_inference/server.py</files>
  <read_first>
    - .planning/phases/22-custom-vlm-finetune/22-RESEARCH.md Pattern 4 (동거 실행 순서 5단계 — 태스크 순서 그대로) + Pitfall 3
    - .planning/phases/22-custom-vlm-finetune/22-POD-VRAM.md (22-03 실측 — gpu-memory-utilization 입력값)
    - backend/runpod_inference/setup.sh (멱등 골격) + backend/runpod_inference/server.py:94-102, 139-192 (_verify_token, _warmup, /health)
    - ~/.claude/projects/-Users-kimtaesung-Dev-SunityMotion/memory/current-pod-hbpvhedq2bu01i.md (start_server.sh Volume 반영 함정)
  </read_first>
  <action>
    (0) rollback 블록 선기록 (DR-02): vLLM 프로세스 중지/제거 커맨드 / start_server.sh revert 경로(수정 전 원본을 Volume에 백업) / 재기동 커맨드 / health check / Lambda RUNPOD_ANALYZE_URL·env sync check를 SUMMARY 초안에 먼저 적고 나서 변형 시작. (1) setup_vllm.sh 신설(멱등): pip install vllm(버전 22-06과 동일 핀), AWQ 체크포인트 S3 training/phase22/checkpoints/{run_id}/awq/ → Volume 다운로드(존재 시 skip). (2) 기동 스크립트: 기존 파이프라인 웜업(NLF 로드) 완료 후 vLLM 기동 — `vllm serve {awq-model-path} --quantization awq --gpu-memory-utilization {22-POD-VRAM.md 권고치, 0.35~0.5 범위} --max-model-len 32768 --limit-mm-per-prompt '{"video":1}' --port 8001 --host 127.0.0.1` (localhost 바인드 — 외부 비공개, FastAPI가 X-RunPod-Token 인증 프록시 유지). 기동 순서 역전 금지 사유(vLLM 선점→NLF OOM)를 주석 명시. start_server.sh 수정분은 Volume 쪽에 반영(Pod 재생성 함정). (3) server.py /health에 vllm_loaded bool 필드 추가(8001 ping) — 기존 필드 패턴 유지. env VLLM_BASE_URL을 Pod env에 등록.
  </action>
  <verify>
    <automated>bash -n backend/runpod_inference/setup_vllm.sh && grep -c "127.0.0.1" backend/runpod_inference/setup_vllm.sh && grep -c "vllm_loaded" backend/runpod_inference/server.py</automated>
  </verify>
  <acceptance_criteria>
    - Pod에서 vLLM 기동 성공: `curl -s http://127.0.0.1:8001/v1/models` 응답 (SSH 내부에서만) + /health의 vllm_loaded=true
    - RunPod proxy 외부에서 8001 직접 접근 불가 (localhost 바인드 — SUMMARY에 확인 기록)
    - gpu-memory-utilization 값이 22-POD-VRAM.md 권고치와 일치 (하드코딩 0.9 금지)
  </acceptance_criteria>
  <done>자체 모델이 서빙 Pod에서 기존 분석과 동거 기동된다.</done>
</task>

<task type="auto">
  <name>Task 4: 동거 검증 — OOM/지연 기준 판정 + 분석 경로 생존 확인</name>
  <files>.planning/phases/22-custom-vlm-finetune/22-POD-VRAM.md</files>
  <read_first>
    - .planning/phases/22-custom-vlm-finetune/22-RESEARCH.md Pattern 4 step 5 (실패 판정 기준 수치) + Open Question 2 (shadow는 분석 완료 후 후순위 — 지연 영향 0 설계)
    - backend/evals/phase18/dataset/pairs.yaml (스모크 대상)
  </read_first>
  <action>
    Pod에서 3중 검증: (1) 분석 단독 1건(vLLM 상주 상태) — 기존 대비 소요시간/피크 VRAM 기록, (2) 분석 1건 + vlm_judge 추론 동시 — OOM 발생 여부 + 분석 지연 % 계측, (3) EVAL18 스모크 1회(6페어 SERIAL) — 분석 경로 생존 + verdict 무회귀. 판정: OOM 발생 또는 동시 시나리오 지연 +20% 초과 → D-14 폴백(별도 pod) 전환을 결정 기록으로 남기고 setup_vllm.sh를 별도 pod용으로 분기(이 경우 VLLM_BASE_URL이 별도 pod URL — X-RunPod-Token 헤더 인증 필수). 폴백 전환 시에도 production Pod 원복(rollback 블록 실행)을 선행한다 (DR-02). 결과를 22-POD-VRAM.md에 동거 판정 절로 추가(수치 표 + 판정 + 근거).
  </action>
  <verify>
    <automated>grep -cE "동거 판정|cohab" .planning/phases/22-custom-vlm-finetune/22-POD-VRAM.md</automated>
  </verify>
  <acceptance_criteria>
    - 22-POD-VRAM.md에 3중 검증 수치(단독/동시/EVAL18)와 판정(동거 유지 또는 별도 pod 전환) 기록
    - EVAL18 6페어 verdict가 vLLM 상주 상태에서 baseline과 일치 (변별 4 + known 2)
    - 판정 기준이 수치(OOM 0, 지연 ≤ +20%)로 명시 — 주관 판단 0
  </acceptance_criteria>
  <done>D-14 동거/분리 판단이 실측으로 확정되고 프로덕션 분석 경로의 생존이 증명된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 자체 모델 출력 → 파이프라인 | 신뢰 불가 모델 출력이 분석 데이터로 유입 |
| vLLM HTTP 엔드포인트 | 신규 추론 서비스 표면 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-23 | Elevation | vLLM 무인증 추론 탈취 | mitigate | 127.0.0.1 바인드 + 외부는 FastAPI X-RunPod-Token 프록시만 (server.py _verify_token 전례, 토큰 미설정=503). 별도 pod 폴백 시에도 토큰 헤더 필수 |
| T-22-24 | Tampering | 모델 출력 주입(스키마 밖 키/거대 배열/프롬프트 주입 영상 자막) | mitigate | REPORT_KEYS 화이트리스트 + nested-array 거부 + 크기 상한 + guided JSON — 파서 방어가 본체(guided는 보조). 완전 차단 불가분은 shadow 불일치로 검출 (V5) |
| T-22-25 | DoS | vLLM VRAM 선점 → 분석 전면 장애 | mitigate | 실측 예산 + 기동 순서 + +20%/OOM 수치 기준 + EVAL18 생존 스모크 (Pitfall 3) |
| T-22-26 | Info Disclosure | 어댑터 로그에 토큰/키 | mitigate | 키 값 로그 금지 규율, VLLM_BASE_URL/토큰은 env 참조만 |
| T-22-SC | Tampering | pip install vllm (서빙 Pod) | mitigate | Audit [OK] Approved + 22-06과 동일 버전 핀 |
</threat_model>

<verification>
- `python3 -m pytest backend/tests/phase22 -x -q` GREEN
- Pod: vllm_loaded=true + EVAL18 스모크 무회귀 + 동거 판정 기록
</verification>

<success_criteria>
- FT-05의 서빙 기반 완성: Protocol/어댑터/vLLM 동거가 실측 검증됨
- 22-09가 shadow 병행을 켜기만 하면 되는 상태
</success_criteria>

<output>
Create `.planning/phases/22-custom-vlm-finetune/22-08-SUMMARY.md` when done
</output>
