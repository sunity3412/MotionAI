---
phase: 22-custom-vlm-finetune
plan: 09
type: execute
wave: 6
depends_on: ["22-08"]
files_modified:
  - backend/functions/pipeline/app.py
  - backend/evals/phase22/shadow_report.py
  - backend/tests/phase22/test_shadow_wiring.py
autonomous: true
requirements: [FT-05]
must_haves:
  truths:
    - "같은 입력이 Gemini와 자체 모델 양쪽에 태워지고 verdict diff가 vlm_shadow에 병기 저장된다 (D-13)"
    - "shadow 추론은 분석 완료 후 후순위(BackgroundTasks)로 실행되어 사용자 체감 지연이 0이다"
    - "shadow는 절대 분석을 실패시키지 않는다 — status enum + 예외 삼킴"
    - "역할별(veto 먼저) 일치율/불일치 케이스 리포트가 생성되고 'Gemini 이상' 증명(hard negative에서 Gemini 오답을 자체 모델이 정답) 기준이 수치로 정의된다"
  artifacts:
    - path: "backend/functions/pipeline/app.py"
      provides: "VLM_JUDGE_SHADOW 병행 배선 + verdict diff 저장 (pipeline 단독 소유 env)"
      contains: "VLM_JUDGE_SHADOW"
    - path: "backend/evals/phase22/shadow_report.py"
      provides: "역할별 일치율/불일치 케이스 리포트 생성기"
      min_lines: 80
  key_links:
    - from: "backend/functions/pipeline/app.py"
      to: "backend/shared/python/sunity_shared/analysis/vlm_judge.py"
      via: "shadow 경로에서 VllmJudgeAdapter lazy 호출"
      pattern: "vlm_judge|VllmJudge"
    - from: "backend/evals/phase22/shadow_report.py"
      to: "Firestore vlm_shadow"
      via: "누적 diff 로그 집계"
      pattern: "vlm_shadow"
---

<objective>
D-13 shadow 병행을 켠다: 같은 입력을 Gemini(판정 경로, 불변)와 자체 모델(shadow) 양쪽에 태워 verdict diff를 축적하고, 역할별 swap 게이트 판정의 근거가 될 비교 리포트를 만든다.

Purpose: swap(22-10)의 전제는 "shadow 대비 Gemini 이상" 증명(D-15)이다. 이 plan이 그 증거를 생산한다. 판정 경로는 여기서도 무변경 — shadow는 관찰만 한다.

Output: 병행 배선 + shadow_report.py + 누적 비교 리포트.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-custom-vlm-finetune/22-CONTEXT.md
@.planning/phases/22-custom-vlm-finetune/22-PATTERNS.md
@.planning/phases/22-custom-vlm-finetune/22-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: shadow 병행 배선 — VLM_JUDGE_SHADOW + verdict diff 저장</name>
  <files>backend/functions/pipeline/app.py, backend/tests/phase22/test_shadow_wiring.py</files>
  <read_first>
    - backend/functions/pipeline/app.py (22-03이 넣은 VLM_SHADOW_LOG 배선 지점 + _apply_vision_veto + BackgroundTasks 경로)
    - backend/shared/python/sunity_shared/analysis/vlm_judge.py (22-08 산출 — 어댑터 API)
    - .planning/phases/22-custom-vlm-finetune/22-RESEARCH.md Pattern 5 + Open Question 2 (후순위 실행 — 분석 완료 후)
    - backend/shared/python/sunity_shared/firestore_admin.py (store_vlm_shadow — role별 merge)
  </read_first>
  <behavior>
    - Test 1: VLM_JUDGE_SHADOW=1 → 분석 완료 후 자체 모델 추론이 실행되고 vlm_shadow 문서의 roles.veto에 own/gemini/diff 3필드 병기 (diff = fault_category 집합 대칭차 + 측정대상 일치 여부 flat dict)
    - Test 2: 자체 모델 호출이 분석 완료 이후 시점 (mock 호출 순서 검증 — complete_analysis 후) — 사용자 체감 지연 0
    - Test 3: vlm_judge가 None 반환(vLLM 다운) 시 status="skipped_error", 분석 결과·Gemini 판정 불변
    - Test 4: VLM_JUDGE_SHADOW 미설정 → 자체 모델 호출 0회 (기본 off)
    - Test 5: env 토글이 app.py 단독 소유 — vlm_judge.py 내 토글 참조 0 (grep)
  </behavior>
  <action>
    app.py의 22-03 shadow 로깅 지점을 확장: VLM_JUDGE_SHADOW=1이면 분석 완료(complete_analysis) 후 후순위로 VllmJudgeAdapter(lazy 싱글톤, _ensure_* double-checked lock 패턴) 추론 실행 — 입력은 파이프라인이 이미 가진 9fps 프레임 배열 인덱스 서브샘플 + RTMW 행(재추출 0). Gemini verdict(이미 저장됨)와 diff 계산(순수 함수로 분리 — fault 집합 비교, 측정대상 교집합, 시간 앵커 근접도) 후 store_vlm_shadow(role, {own, gemini, diff, status, latency_ms}) 병기. VLM_JUDGE_BACKEND env는 22-10 swap용으로 이번에 정의만(기본 "gemini" — 판정 경로 분기 코드는 22-10). 3역할 중 veto를 우선 배선하고 recognizer/coach shadow는 같은 패턴으로 확장(coach는 Cerebras와 병행 비교 — D-02). RunPod BackgroundTasks 글로벌 싱글톤 규율(mutable 사이드카 금지) 준수.
  </action>
  <verify>
    <automated>python3 -m pytest backend/tests/phase22/test_shadow_wiring.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_shadow_wiring.py 확장분 포함 전체 exit 0
    - `grep -c "VLM_JUDGE_SHADOW" backend/functions/pipeline/app.py` ≥ 2, `grep -c "VLM_JUDGE_SHADOW" backend/shared/python/sunity_shared/analysis/vlm_judge.py` = 0
    - 전체 backend 테스트 baseline diff IDENTICAL
  </acceptance_criteria>
  <done>양 모델 병행 비교가 프로덕션 관찰 모드로 배선된다.</done>
</task>

<task type="auto">
  <name>Task 2: Pod 배포 + shadow 누적 실행 (전 동작 커버)</name>
  <files>backend/functions/pipeline/app.py</files>
  <read_first>
    - backend/evals/phase18/pairs.yaml + backend/evals/phase22/fixtures/manifest.yaml (누적 실행 대상 — 전 동작 균등)
    - .planning/phases/22-custom-vlm-finetune/22-POD-VRAM.md (동거 판정 — 동시 실행 제약)
  </read_first>
  <action>
    push → Pod pull → VLM_JUDGE_SHADOW=1 설정(Volume start_server.sh) → 재기동. 누적 실행: eval 미니셋 real 트랙 + EVAL18 6페어 + hard negative 2건을 SERIAL로 분석(동시성 비안전 — 순차 필수), 각 건이 vlm_shadow에 own/gemini/diff를 남기는지 확인. 전 동작(등록 5 + climb + 미보유) 커버 확인 — kip-up 편중 검증 금지 원칙. 목표 누적 N ≥ 20건(동작별 ≥ 2). 실행 중 분석 실패 0 + 지연 로그 수집.
  </action>
  <verify>
    <automated>python3 -c "print('shadow accumulation verified via Firestore query — see SUMMARY')"</automated>
  </verify>
  <acceptance_criteria>
    - Firestore vlm_shadow에 diff 병기 문서 ≥ 20건, 동작 커버 ≥ 7종 (집계 스크립트 출력 SUMMARY 첨부)
    - 누적 실행 중 분석 status=failed 0건
    - shadow 추론 평균 latency_ms 기록 (Phase 27 참고용)
  </acceptance_criteria>
  <done>swap 판정에 필요한 비교 데이터가 전 동작 균등으로 축적된다.</done>
</task>

<task type="auto">
  <name>Task 3: shadow_report.py — 역할별 일치율 + "Gemini 이상" 증명 리포트</name>
  <files>backend/evals/phase22/shadow_report.py</files>
  <read_first>
    - backend/evals/phase24/run_sweep.py:195-236 (_meta + 관찰 요약 테이블 양식)
    - .planning/phases/22-custom-vlm-finetune/22-RESEARCH.md Validation Architecture Wave 3 (일치율 + 불일치 케이스 belle 리뷰 + 역할별 veto 먼저)
    - backend/training/data/manifest.json (hard_negative 행 — Gemini 기지 오답 케이스)
  </read_first>
  <action>
    backend/evals/phase22/shadow_report.py 신설 — Firestore vlm_shadow 집계: 역할별(veto/recognizer/coach) (1) 전체 일치율(fault 집합 일치 / 측정대상 일치 / 시간 앵커 근접), (2) 불일치 케이스 목록(video_hash + 양쪽 verdict + 어느 쪽이 그럴듯한지 표기란 — 사람 점수 라벨 아님, 정오 판단은 hard negative 트랙만), (3) "Gemini 이상" 판정: hard negative 케이스(A2 피터팬 위양성 — Gemini가 틀린 것으로 알려진 입력)에서 자체 모델 정답 여부 + 일반 케이스 일치율 ≥ 기준(기본 0.8, config — 밴드 단언 아님, swap 논의 입력값임을 주석 명시). 출력 = Markdown 리포트(역할별 표 + 불일치 상세) — 22-10 swap checkpoint의 belle 제시 자료. EVAL_OUT_DIR 규율 + _meta.
  </action>
  <verify>
    <automated>python3 -c "import importlib.util,sys; sys.path.insert(0,'backend/shared/python'); spec=importlib.util.spec_from_file_location('sr','backend/evals/phase22/shadow_report.py'); m=importlib.util.module_from_spec(spec); spec.loader.exec_module(m); assert hasattr(m,'build_report'); print('report module ok')"</automated>
  </verify>
  <acceptance_criteria>
    - shadow_report.py 실행으로 역할별 일치율 표 + 불일치 케이스 + hard negative 정오가 담긴 리포트 생성 (SUMMARY에 요약 첨부)
    - 리포트에 사람 숫자 점수 라벨 필드 0 (버킷/집합 비교만)
    - veto 역할 리포트가 최우선 완성 (D-13 순서)
  </acceptance_criteria>
  <done>swap 게이트(22-10)가 소비할 "Gemini 이상" 증거 리포트가 생성된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 자체 모델 출력 → vlm_shadow | shadow 경로로 신뢰 불가 출력이 Firestore 유입 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-27 | Info Disclosure | shadow diff 로그 PII | mitigate | store_vlm_shadow 화이트리스트(22-03) 재사용 — uid 금지, video_hash 참조만 |
| T-22-28 | DoS | shadow 추론이 분석 지연/실패 유발 | mitigate | 분석 완료 후 후순위 실행 + 예외 삼킴 + status enum + 누적 실행 실패 0 acceptance |
| T-22-29 | Tampering | 자체 모델 출력의 Firestore 오염 | mitigate | vlm_judge 방어 파서(REPORT_KEYS/크기 상한) + store_vlm_shadow flat-dict 검증 2중 |
| T-22-SC | Tampering | 신규 설치 | accept | 설치 없음 |
</threat_model>

<verification>
- `python3 -m pytest backend/tests/phase22 -x -q` GREEN + baseline diff IDENTICAL
- vlm_shadow 누적 ≥ 20건 (전 동작) + 분석 실패 0
- 역할별 비교 리포트 생성
</verification>

<success_criteria>
- FT-05 shadow 병행 가동 — verdict 비교 로그 축적 체계 완성
- swap 판정(22-10)의 정량 근거(일치율 + hard negative 정오) 확보
</success_criteria>

<output>
Create `.planning/phases/22-custom-vlm-finetune/22-09-SUMMARY.md` when done
</output>
