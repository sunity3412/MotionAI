---
phase: 22-custom-vlm-finetune
plan: 10
type: execute
wave: 7
depends_on: ["22-09"]
files_modified:
  - backend/functions/pipeline/app.py
  - backend/tests/phase22/test_swap_toggle.py
  - .planning/phases/22-custom-vlm-finetune/22-SWAP-LOG.md
autonomous: false
requirements: [FT-05, FT-04]
must_haves:
  truths:
    - "swap은 역할별 순차(veto → recognizer → coach)이며 각 역할마다 belle 확인을 거친다 (D-13)"
    - "veto swap 후 EVAL18 6페어 재실행이 무회귀다 (D-15)"
    - "coach swap은 D-02 품질 게이트(측정 사실 앵커 준수 + LLM judge + belle 확인) 통과 전에는 실행되지 않는다 — 미통과 시 shadow 유지가 정상 종료 상태"
    - "swap 토글은 역할당 env 1개(one env per role, pipeline 단독 소유 3개)로 역할별 즉시 롤백 가능하다"
    - "Wave 4 Cascade RL은 후속 plan 경계로만 박제되고 이번 phase에서 구현되지 않는다 (D-07/D-16)"
  artifacts:
    - path: "backend/functions/pipeline/app.py"
      provides: "VLM_JUDGE_BACKEND=own|gemini 역할별 판정 경로 분기"
      contains: "VLM_JUDGE_BACKEND"
    - path: ".planning/phases/22-custom-vlm-finetune/22-SWAP-LOG.md"
      provides: "역할별 swap 판정 기록 + EVAL18 무회귀 증거 + Wave 4 RL 후속 경계"
  key_links:
    - from: "backend/functions/pipeline/app.py"
      to: "backend/shared/python/sunity_shared/analysis/vlm_judge.py"
      via: "VLM_JUDGE_BACKEND=own일 때 판정 경로가 자체 모델 소비"
      pattern: "VLM_JUDGE_BACKEND"
---

<objective>
D-15 출하 게이트를 통과한 역할부터 순차 swap한다: veto(결함 짚기) → recognizer(인식/구간) → coach(D-02 게이트 후). 각 swap은 env 토글 + EVAL18 무회귀 + belle 확인으로 진행하고, Wave 4 Cascade RL은 후속 plan 경계로 박제한다.

Purpose: phase의 최종 산출 — Gemini 의존 시각 판정이 우리가 가중치를 소유한 모델로 교체되기 시작한다. swap은 되돌릴 수 있어야 하며(역할당 env 1개), coach는 한국어 문체 리스크 때문에 게이트 앞에서 기다리는 것이 정상이다(D-02).

Output: 판정 경로 분기 + swap 실행 기록 + RL 후속 경계 문서.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-custom-vlm-finetune/22-CONTEXT.md
@.planning/phases/22-custom-vlm-finetune/22-RESEARCH.md
@backend/evals/phase22/shadow_report.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: VLM_JUDGE_BACKEND 역할별 판정 경로 분기 (즉시 롤백 가능)</name>
  <files>backend/functions/pipeline/app.py, backend/tests/phase22/test_swap_toggle.py</files>
  <read_first>
    - backend/functions/pipeline/app.py (22-09 배선 + _apply_vision_veto / _ensure_recognizer / coach 소비 지점 3곳)
    - backend/shared/python/sunity_shared/analysis/interfaces.py (VlmJudge Protocol + TechniqueRecognizer Protocol — recognizer swap은 기존 Protocol 뒤 구현체 교체)
    - .planning/phases/22-custom-vlm-finetune/22-PATTERNS.md §2 (env-switch + double-checked lock — 그대로 복사)
    - backend/shared/python/sunity_shared/analysis/deduction_engine.py:181-247 (변환된 differences[] 를 소비하는 감점 엔진 — F1 substrate 보존 검증 대상)
  </read_first>
  <behavior>
    - Test 1: VLM_JUDGE_BACKEND_VETO=own → veto 판정이 vlm_judge 출력 소비, =gemini(기본) → 기존 경로 byte-동일 (역할별 독립 env 3개: _VETO/_RECOGNIZER/_COACH)
    - Test 2: own 경로에서 vlm_judge가 None(장애) 반환 시 Gemini 경로로 graceful 폴백 + status 로그 (판정 공백 0 — FallbackRecognizer "모르면 깎지 않는다" 정신)
    - Test 3: own 경로 출력도 기존 verdict 계약(VisionVerdict 필드/FAULT_CATEGORIES enum)으로 정규화되어 하위 감점 엔진이 무수정 소비 (모델은 점수 안 냄 — 감점은 Phase 24 엔진 불변)
    - Test 4: 토글이 app.py 단독 소유 — 어댑터/공유 모듈에 env 참조 0
    - Test 5: own→VisionVerdict 변환 결과를 deduction_engine 에 태우면 각도쌍 보유 결함에서 감점이 비어있지 않게 산출된다 (F1 측정 substrate 보존 실증 — faults[] 상위집합이 실제 채점으로 이어짐; 변환은 score 필드를 생성하지 않음, 감점은 Phase 24 엔진)
  </behavior>
  <action>
    app.py에 역할별 env 3개(VLM_JUDGE_BACKEND_VETO / _RECOGNIZER / _COACH, 값 own|gemini, 기본 gemini) 분기 추가 — 22-09에서 정의만 한 것을 판정 경로 분기로 완성. veto: _apply_vision_veto의 verdict 소스 선택(own이면 vlm_judge 리포트의 faults[](fault_category·student_angle_deg·reference_angle_deg·measurement_basis·root_cause_hypothesis)를 VisionVerdict.differences[] 필드로 매핑하는 순수 변환 함수 — 감점 엔진이 라우팅·산술할 substrate 보존, 각도쌍 누락 결함은 measured-only 로 강등, score 필드 생성 금지(F1)). recognizer: _ensure_recognizer에 own 분기(vlm_judge 리포트의 인식/구간을 TechniqueRecognizer 계약으로). coach: own 분기 코드는 두되 D-02 게이트 통과 전 활성화 금지를 주석+테스트로 봉인. shadow 로깅(22-09)은 swap 후에도 유지(역방향 비교 — own이 판정, gemini가 shadow). 각 분기 double-checked lock 싱글톤 + lazy import.
  </action>
  <verify>
    <automated>python3 -m pytest backend/tests/phase22/test_swap_toggle.py backend/tests/phase22/test_shadow_wiring.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_swap_toggle.py ≥ 4 tests exit 0
    - 기본(env 미설정) 상태에서 전체 backend 테스트 baseline diff IDENTICAL (기존 경로 무변경 증명)
    - `grep -c "VLM_JUDGE_BACKEND" backend/shared/python/sunity_shared/analysis/vlm_judge.py` = 0
  </acceptance_criteria>
  <done>역할별 swap이 역할당 env 1개 토글로 켜지고 꺼지는 가역 구조가 된다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: veto swap 실행 belle 승인 → 토글 → EVAL18 무회귀</name>
  <what-built>veto 역할 swap 준비 완료 상태 — 제시 자료: (1) shadow_report.py의 veto 일치율/불일치 케이스/hard negative 정오(A2 피터팬 위양성에서 자체 모델 판정), (2) 22-07 assert_gates 판정, (3) 롤백 절차(역할당 env 1개 되돌림). D-15 "shadow 대비 Gemini 이상" 증명이 기준.</what-built>
  <how-to-verify>
    1. shadow 리포트에서 veto 일치율과 hard negative 개선 확인
    2. "approved" 시 Claude가 실행: Pod env VLM_JUDGE_BACKEND_VETO=own(Volume 반영) → 재기동 → EVAL18 6페어 SERIAL 재실행 → verdict 무회귀 확인(변별 4 + known 2) → 결과 보고. 회귀 발생 시 즉시 롤백 + 원인 기록
    3. recognizer 역할도 동일 절차 반복(veto 안정 확인 후) — recognizer shadow 리포트 제시 → 승인 → 토글 → EVAL18 재실행
  </how-to-verify>
  <action>승인 전 자동 준비: shadow_report.py veto 리포트(일치율/불일치/hard negative 정오) + assert_gates 판정 + 롤백 절차 1페이지 제시. 승인 후 Claude 실행: Pod env VLM_JUDGE_BACKEND_VETO=own (Volume start_server.sh 반영) → 재기동 → EVAL18 6페어 SERIAL 재실행 → verdict 무회귀(변별 4 + known 2) 확인 → 결과 보고, 회귀 시 즉시 env 롤백 + 원인 기록. recognizer 역할도 동일 절차 반복 (D-13 순차).</action>
  <resume-signal>"approved"(veto swap 진행) / "hold"(shadow 연장) / 역할별 개별 지시</resume-signal>
</task>

<task type="auto">
  <name>Task 3: coach D-02 게이트 판정 + 22-SWAP-LOG.md + Wave 4 RL 후속 경계 박제</name>
  <files>.planning/phases/22-custom-vlm-finetune/22-SWAP-LOG.md</files>
  <read_first>
    - .planning/phases/22-custom-vlm-finetune/22-CONTEXT.md D-02 (coach 게이트 3요건) + D-07 (RL 후속)
    - .planning/phases/22-custom-vlm-finetune/22-RESEARCH.md Pattern 3 (ms-swift MPO/GSPO 확정 경로 — 후속 plan 근거) + Code Examples GSPO
    - backend/evals/phase22/shadow_report.py 출력 (coach 병행 비교분)
  </read_first>
  <action>
    (1) coach D-02 게이트 판정: shadow의 coach 출력(자체 모델 vs Cerebras)에 대해 — 측정 사실 앵커 준수(코칭 문장이 실측 데이터에 없는 수치/결함을 언급하면 FAIL — 거짓 구체성 금지), LLM judge 비교 채점, belle 확인용 샘플 표 작성. 통과 시 Task 2와 동일 절차로 swap 제안, 미통과 시 shadow 유지가 phase 정상 종료 상태임을 명기(D-02 — 단계적 소비). (2) .planning/phases/22-custom-vlm-finetune/22-SWAP-LOG.md 박제: 역할별 swap 상태 표(veto/recognizer/coach — 일자/근거 리포트/EVAL18 결과/롤백 이력), D-15 게이트 최종 체크리스트(Phase24 4종 번안 + EVAL18 + 균등 + Gemini 이상). (3) Wave 4 Cascade RL 후속 경계(D-07/D-16 — 이번 phase 구현 금지): 후속 plan이 소비할 사실만 기록 — ms-swift 경로 확정(MPO = rlhf_type dpo + 복수 loss_type/loss_weights, GSPO = GRPO --importance_sampling_level sequence, vLLM rollout 백엔드), 보상 5종 설계 위치(NLM §24 — 시계열 물리/뼈길이/CoT 논리/JSON 포맷/[미래]이종센서), 진입 조건(SFT swap 안정 + shadow 롤아웃 풀 축적), 데이터 소스(vlm_shadow 롤아웃). ROADMAP Phase 22 항목에 Wave 4 후속 plan 표기 갱신.
  </action>
  <verify>
    <automated>test -f .planning/phases/22-custom-vlm-finetune/22-SWAP-LOG.md && grep -cE "GSPO|MPO" .planning/phases/22-custom-vlm-finetune/22-SWAP-LOG.md && grep -c "D-02" .planning/phases/22-custom-vlm-finetune/22-SWAP-LOG.md</automated>
  </verify>
  <acceptance_criteria>
    - 22-SWAP-LOG.md에 역할별 상태 표 + D-15 체크리스트 + Wave 4 경계 절 존재
    - coach 게이트 판정 결과(통과→swap 제안 / 미통과→shadow 유지 사유) 기록 — 어느 쪽이든 명시적 상태
    - RL 상세 구현 태스크가 이번 phase 산출물에 0개 (경계 문서만)
  </acceptance_criteria>
  <done>swap 이력과 잔여 상태가 단일 문서로 박제되고, Wave 4 RL이 깨끗한 후속 경계로 넘어간다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 자체 모델 verdict → 감점 엔진 | swap 후 신뢰 불가 모델 출력이 실제 판정을 결정 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-30 | Tampering | own 경로 verdict 오염(스키마 밖/환각 결함) | mitigate | VisionVerdict 계약 변환 함수의 enum/화이트리스트 강제 + score 생성 금지 + EVAL18 무회귀 게이트 + shadow 역방향 비교 유지 |
| T-22-31 | DoS | own 경로 장애 시 판정 공백 | mitigate | vlm_judge None → Gemini graceful 폴백 (Task 1 Test 2) + 역할당 env 1개 즉시 롤백 |
| T-22-32 | Repudiation | swap 결정 근거 소실 | mitigate | 22-SWAP-LOG.md 역할별 근거/일자/롤백 이력 박제 |
| T-22-33 | Info Disclosure | coach 출력의 거짓 구체성(환각 수치) | mitigate | D-02 측정 사실 앵커 게이트 — 실측에 없는 수치 언급 시 FAIL, 미통과 시 shadow 유지 |
| T-22-SC | Tampering | 신규 설치 | accept | 설치 없음 |
</threat_model>

<verification>
- 기본 env에서 전체 테스트 baseline diff IDENTICAL
- veto swap 시 EVAL18 6페어 무회귀 (변별 4 + known 2)
- 22-SWAP-LOG.md 완성
</verification>

<success_criteria>
- FT-05 완료: 역할별 순차 swap 체계 가동 (최소 veto swap 판정 완료, coach는 D-02 게이트 상태 명시)
- FT-04의 D-15 출하 게이트 최종 체크리스트 박제
- Wave 4 Cascade RL 후속 plan 경계 명확 (이번 phase 미구현)
</success_criteria>

<output>
Create `.planning/phases/22-custom-vlm-finetune/22-10-SUMMARY.md` when done
</output>
