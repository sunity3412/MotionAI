---
phase: 22-custom-vlm-finetune
plan: 11
type: execute
wave: 8
depends_on: ["22-02"]
files_modified:
  - backend/scripts/phase22_watch.py
  - backend/scripts/phase22_sources.yaml
  - backend/training/data/manifest.json
  - backend/tests/phase22/test_watch_collect.py
  - backend/training/FLYWHEEL-RUNBOOK.md
autonomous: true
requirements: [FT-02]

must_haves:
  truths:
    - "belle이 로컬 1커맨드(PHASE22_BELLE_GREENLIGHT=1 python3 backend/scripts/phase22_watch.py --run)로 watchlist 주기 수집을 실행하면 신규 영상만 큐레이션·과금되고 기존 수집분은 재과금 0 (s3_key 멱등 + verdict 캐시 재사용)"
    - "수집 실행 후에도 manifest _meta.collection_complete=true 가 보존되고, 무수정 build_jsonl.assert_collection_complete 가 통과한다 (마감 무결성 + 배치 증분 공존)"
    - "실행마다 신규 수집 수 / curate reject 수 / skip(기존) 수 / 누적 training 행 수 요약 리포트가 stdout + JSON 으로 방출된다 (은폐 금지)"
    - "기존 manifest 행(131행 마감분 + recollection_rounds 분)은 단 1행도 변형되지 않는다 (append-only)"
  artifacts:
    - path: "backend/scripts/phase22_watch.py"
      provides: "watch 러너 — 배치 원장 순수 헬퍼 + YT/IG 수집기 오케스트레이션 + 누적 리포트"
      exports: ["make_batch_entry", "register_batch", "assert_ledger_invariants", "summarize_run", "main"]
      min_lines: 150
    - path: "backend/scripts/phase22_sources.yaml"
      provides: "watch 옵트아웃 규약(watch: false) + 신규 채널 등재 형식 주석"
      contains: "watch"
    - path: "backend/training/data/manifest.json"
      provides: "_meta.collection_batches 배치 원장 (초기 빈 배열)"
      contains: "collection_batches"
    - path: "backend/tests/phase22/test_watch_collect.py"
      provides: "배치 원장 불변식 + 리포트 순수성 테스트"
      min_lines: 60
    - path: "backend/training/FLYWHEEL-RUNBOOK.md"
      provides: "쌓기(연속 수집) 운영 절차 — belle 1커맨드 + 스케줄 옵션"
      contains: "PHASE22_BELLE_GREENLIGHT"
  key_links:
    - from: "backend/scripts/phase22_watch.py"
      to: "backend/scripts/collect_phase22_youtube.py"
      via: "main(argv) import 재사용 (재발명 금지)"
      pattern: "collect_phase22_youtube"
    - from: "backend/scripts/phase22_watch.py"
      to: "backend/training/data/manifest.json _meta.collection_batches"
      via: "register_batch append-only 쓰기"
      pattern: "collection_batches"
    - from: "backend/training/data/manifest.json"
      to: "backend/training/datagen/build_jsonl.py"
      via: "assert_collection_complete truthiness 통과 (build_jsonl 무수정)"
      pattern: "collection_complete"
---

<objective>
데이터 플라이휠의 "쌓기" 동사를 상설화한다 (belle 확정 2026-07-16 — v7 SFT 성패와 무관하게
데이터를 계속 쌓아 "스포츠 모션 분석의 힉스필드"를 만든다). 22-02가 만든 수집기
(collect_phase22_youtube.py / collect_phase22_instagram.py, D-09)를 재발명 없이 재사용해,
YT 폴 채널 + 정은지 IG(eunji.poledancer, cap 60 등재 완료)를 belle 트리거 1커맨드로 주기
수집하는 watch 러너를 만든다. 수집+큐레이션만 — RTMW/교사 라벨 없음(비용 통제, 라벨링은 22-12).

핵심 설계 문제 = 마감된 manifest와의 정합: `_meta.collection_complete=true` +
`collection_closed`(2026-07-10 belle 승인, 131행 기준 마감)의 무결성을 깨지 않는 **배치 단위
증분 등재 규약**(`_meta.collection_batches[]`)을 세운다. build_jsonl.assert_collection_complete
는 `_meta.collection_complete` truthiness 단순 체크(build_jsonl.py:77-88)라 배치 추가로
영향받지 않음을 실측 확인했다 — build_jsonl 은 무접촉 파일이며, 규약이 이에 맞춘다(역이 아님).
manifest 의 기존 `recollection_rounds[]`(fault-yt-ig-260714)가 배치 등재의 선례다.

Purpose: FT-02(학습셋 데이터 엔진)의 "지속 운영" 확장 — D-08 균등·D-09 수집·D-12 동의 정책을
운영 루프에서도 유지. 22-12(공부 배치 루프)의 입력 배치를 생산한다.
Output: phase22_watch.py + collection_batches 원장 규약 + watchlist 규약 + FLYWHEEL-RUNBOOK §1.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-custom-vlm-finetune/22-CONTEXT.md
@.planning/phases/22-custom-vlm-finetune/22-02-SUMMARY.md
@backend/scripts/collect_phase22_youtube.py
@backend/scripts/collect_phase22_instagram.py
@backend/scripts/phase22_sources.yaml
</context>

<interface_contracts>
집행자가 탐색 없이 쓸 수 있는 확정 계약 (플래너가 소스 실측):

- `collect_phase22_youtube.main(argv=None) -> int` — argv 로 `--dry-run|--curate|--collect`,
  `--sources`, `--bucket`, `--only`, `--limit-per-channel`, `--max-candidates` 수용.
  `--collect` 멱등: manifest rows 의 s3_key 집합에 이미 있으면 skip(라인 469, 492),
  `--curate` 는 verdict 캐시로 재호출 0. 과금 모드는 `PHASE22_BELLE_GREENLIGHT=1` env 필수
  (`_require_greenlight`, SystemExit(2)).
- `collect_phase22_instagram.main(argv=None) -> int` — `--dry-run|--collect`, `--sources`,
  `--only`, `--cap-per-account`(기본 20, 레지스트리 `cap_per_account` 가 오버라이드 —
  eunji.poledancer=60 등재 완료 라인 197-206), `--max-candidates`.
- `collect_phase22_youtube._load_manifest() -> dict` / `MANIFEST_PATH` — manifest 로드 재사용.
- `build_jsonl.assert_collection_complete(manifest, partial)` — `_meta.collection_complete`
  truthiness 만 검사(build_jsonl.py:77-88). collection_batches 추가는 무영향. **build_jsonl 무수정.**
- manifest row 테스트는 필수 키 검사(ROW_REQUIRED_KEYS)이지 화이트리스트가 아님
  (test_manifest_consistency.py:25) — 신규 행에 `collection_batch` 필드 추가 안전.
- 균등 게이트(test_manifest_consistency.py:75-119): collection_complete=true 활성 상태에서
  위반은 `violations <= set(waiver.unmet)` 만족 필요. 현 waiver.unmet 은 3범주 전부
  (per_motion_jeongta_min1 / per_motion_fault_min1 / max_le_2min)라 정타 편중 증분도 커버됨.
- conftest(backend/tests/phase22/conftest.py)가 backend/scripts 를 sys.path 에 추가 —
  테스트에서 phase22_watch 직접 import 가능.
</interface_contracts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 배치 원장 규약 (_meta.collection_batches) + 순수 헬퍼 + 불변식 테스트</name>
  <files>backend/scripts/phase22_watch.py, backend/training/data/manifest.json, backend/tests/phase22/test_watch_collect.py</files>
  <read_first>
    - backend/training/datagen/build_jsonl.py (라인 75-90 assert_collection_complete — 호환 근거, 무수정 대상)
    - backend/training/data/manifest.json 의 _meta 블록만 (collection_closed / balance_waiver / recollection_rounds 현행 형태 — python -c 로 _meta 만 출력해 읽을 것, rows 전체 로드 금지)
    - backend/tests/phase22/test_manifest_consistency.py (ROW_REQUIRED_KEYS + 균등 게이트 waiver 로직 — 신규 규약이 깨지 말아야 할 기존 게이트)
    - backend/scripts/collect_phase22_youtube.py 의 _load_manifest / MANIFEST_PATH / build_manifest_row (라인 141-178, 438-445)
  </read_first>
  <behavior>
    - make_batch_entry(batch_id, trigger) 는 {batch_id, opened_at(UTC ISO), approved_by:"belle", trigger, sources:{youtube:0,instagram:0}, new_rows:0, curated_reject:0, skipped_existing:0, status:"open", cumulative_rows_after:None} 를 반환하고 여분 키가 없다
    - register_batch(manifest, entry) 는 _meta.collection_batches 에 append 만 하고 manifest 의 다른 모든 _meta 키(collection_complete/collection_closed/balance_waiver/recollection_rounds 등)와 rows 를 deep-equal 보존한다
    - batch_id 중복 register 는 ValueError (멱등 재실행은 기존 entry 갱신 경로로만)
    - assert_ledger_invariants(manifest_before, manifest_after) 는 (a) collection_complete 가 정확히 True 유지, (b) collection_closed/balance_waiver 무변형, (c) rows append-only(기존 s3_key 집합 보존 + 기존 행 내용 불변) 위반 시 AssertionError
    - 신규 행 규약: watch 수집 행은 build_manifest_row 산출에 collection_batch=batch_id 필드가 추가된다 (기존 행 무접촉 — 필드 부재 = 131행 마감분)
    - 헬퍼 전부 순수(파일 I/O 는 별도 load/save 껍데기) — 네트워크/boto3/yt-dlp import 0
  </behavior>
  <action>
    D-09 수집 원장의 지속 운영 확장. backend/scripts/phase22_watch.py 를 신설하고 위 behavior 의
    순수 헬퍼(make_batch_entry / register_batch / update_batch_entry / assert_ledger_invariants)를
    구현한다. 모듈 docstring 에 배치 등재 규약을 박제: collection_complete 의 의미는 "131행 초기
    수집 라운드 마감"으로 보존(build_jsonl DR-06 게이트가 이 플래그의 소유자 — 규약이 build_jsonl
    에 맞추고 역은 금지), 이후 증분은 collection_batches[] 배치 단위로만 등재, batch_id 형식은
    "watch-YYMMDD"(같은 날 2회차부터 "-2" 접미). recollection_rounds[](fault-yt-ig-260714 선례)는
    이력으로 동결하고 신규 등재는 collection_batches 로 일원화한다고 명시.

    backend/training/data/manifest.json 의 _meta 에 "collection_batches": [] 를 초기화한다
    (rows/기타 _meta 키 무접촉 — python 스크립트로 _meta 만 갱신, 수기 편집 금지).

    backend/tests/phase22/test_watch_collect.py 를 RED 먼저 작성: behavior 의 6개 항목 + "실 manifest
    로드 후 assert_collection_complete(manifest, partial=False) 가 예외 없이 통과"(build_jsonl
    무수정 호환 증명, git diff 로 무수정도 검증) + "실 manifest 의 collection_batches 가 list".
    이모지 금지, 주석 한국어.
  </action>
  <acceptance_criteria>
    - `python3 -m pytest backend/tests/phase22/test_watch_collect.py -q` exit 0
    - `python3 -m pytest backend/tests/phase22/test_manifest_consistency.py backend/tests/phase22/test_provenance.py -q` exit 0 (기존 게이트 무회귀)
    - `python3 -c "import json,sys; sys.path.insert(0,'backend/training'); from datagen.build_jsonl import assert_collection_complete; m=json.load(open('backend/training/data/manifest.json')); assert_collection_complete(m, False); print('OK')"` 가 OK 출력 + exit 0
    - `git diff --quiet backend/training/datagen/build_jsonl.py backend/training/datagen/schema.py` exit 0 (프로덕션 무접촉)
    - `python3 -c "import json; m=json.load(open('backend/training/data/manifest.json')); assert m['_meta']['collection_complete'] is True; assert m['_meta']['collection_batches']==[]"` exit 0
    - phase22_watch.py 에서 boto3/yt_dlp 의 모듈 최상위 import 0: `grep -v '^#' backend/scripts/phase22_watch.py | grep -cE '^import (boto3|yt_dlp)|^from (boto3|yt_dlp)'` 출력 0
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest backend/tests/phase22/test_watch_collect.py backend/tests/phase22/test_manifest_consistency.py -q</automated>
  </verify>
  <done>배치 원장 규약이 순수 헬퍼+테스트로 박제되고, 마감 무결성(collection_complete=true 보존 + build_jsonl 무수정 통과 + 기존 행 append-only)이 기계 검증된다</done>
</task>

<task type="auto">
  <name>Task 2: watch 오케스트레이션 (--dry-run/--run) + 레지스트리 watch 규약 + 누적 리포트</name>
  <files>backend/scripts/phase22_watch.py, backend/scripts/phase22_sources.yaml, backend/tests/phase22/test_watch_collect.py</files>
  <read_first>
    - backend/scripts/collect_phase22_youtube.py (main argv 계약 라인 243-260, _require_greenlight 라인 226, _run_collect 요약 카운터 라인 447-545)
    - backend/scripts/collect_phase22_instagram.py (main argv 계약 라인 106-116, account_cap, _ig_accounts 의 enabled 무관 IG 트랙 semantics)
    - backend/scripts/phase22_sources.yaml (defaults + 채널 entry 스키마 — 기존 스키마 준수, eunji.poledancer 라인 197-206 확인)
    - backend/tests/phase22/test_harvest_filter.py (필터 self-check 선례 — silent 통과 금지 패턴)
  </read_first>
  <action>
    phase22_watch.py 에 main(argv) 을 추가한다. 모드 2단:
    - `--dry-run`(기본): 레지스트리 로드 + watch 대상 채널/계정 목록 출력 + 배치 원장 불변식
      self-check(합성 manifest 로 register_batch 왕복) + 하위 수집기 dry-run 위임
      (collect_phase22_youtube.main(["--dry-run"]) / collect_phase22_instagram.main(["--dry-run"])).
      네트워크·과금 0, exit 0.
    - `--run`: PHASE22_BELLE_GREENLIGHT=1 없으면 SystemExit(2) (22-02 belle 게이트 관례 유지 —
      반복 실행 UX 는 "env 1개 + 커맨드 1줄"로 통일, 게이트 완화 금지). 흐름:
      (1) manifest 스냅샷(before) → make_batch_entry+register_batch(status:"open") 저장,
      (2) YT: collect_phase22_youtube.main(["--curate", ...]) 후 main(["--collect", ...]) —
      verdict 캐시+s3_key 멱등이 재과금을 차단, (3) IG: collect_phase22_instagram.main(["--collect", ...]),
      (4) manifest 재로드 → before/after 행 diff 로 신규 행에 collection_batch=batch_id 주입 +
      배치 entry 갱신(new_rows/skipped_existing/curated_reject/sources/status:"collected"/
      cumulative_rows_after), (5) assert_ledger_invariants(before, after) 통과 후에만 저장,
      (6) 수집 후 게이트 pytest 자동 실행: `python3 -m pytest backend/tests/phase22/test_manifest_consistency.py
      backend/tests/phase22/test_provenance.py -q` — FAIL 이면 exit 비0으로 표면화(은폐 금지).
      passthrough 플래그: --only / --limit-per-channel / --cap-per-account / --max-candidates.
    - 리포트(은폐 금지 원칙): 실행 말미에 stdout 요약(신규 N = yt/ig 분해, reject R, skip S,
      누적 training 행 T, 버킷 분해 정타/fault, 배치 id) + backend/training/data/watch_reports/
      {batch_id}.json 저장. summarize_run(before, after, batch_entry) 순수 함수로 조립(테스트 대상).

    phase22_sources.yaml 확장(기존 스키마 준수, 기존 entry 무변형): (a) 채널 entry 에 선택
    필드 `watch: false` 옵트아웃 규약 도입 — 부재=watch 대상(기본 전 enabled 채널 + IG 트랙),
    (b) 파일 헤더 주석에 신규 채널 등재 형식(필수 키: name/channel_url/platform/tier/bucket/notes,
    선택: discipline_include 오버라이드/duration_window/cap_per_account/watch)과 "등재 = belle
    승인 후" 규약을 문서화. eunji.poledancer 는 이미 등재(cap 60)이므로 추가 등재 없음 —
    watch 대상임을 주석으로만 명시.

    test_watch_collect.py 에 추가: watch 대상 선별 순수 함수(watch: false 제외) + summarize_run
    (합성 before/after 로 카운트 정확성) + --run 게이트(env 부재 SystemExit(2)) 테스트.
    실 수집 실행은 이 플랜에서 하지 않는다(과금 게이트 — 운영은 런북 절차).
  </action>
  <acceptance_criteria>
    - `python3 backend/scripts/phase22_watch.py --dry-run` exit 0, stdout 에 watch 대상 채널 수와 "network 0" 표기, 다운로드·Gemini 호출 0
    - `PHASE22_BELLE_GREENLIGHT= python3 backend/scripts/phase22_watch.py --run` exit 2 (게이트 차단)
    - `python3 -m pytest backend/tests/phase22/test_watch_collect.py -q` exit 0 (신규 테스트 포함)
    - `python3 -c "import yaml; r=yaml.safe_load(open('backend/scripts/phase22_sources.yaml')); assert any(c['name']=='eunji.poledancer' and c.get('cap_per_account')==60 for c in r['channels'])"` exit 0
    - `git diff backend/scripts/phase22_sources.yaml` 에 기존 entry 값 변경 0 (추가·주석만 — 실행자가 diff 육안+`git diff --stat` 로 삽입 위주 확인)
    - summarize_run 리포트에 new_rows/curated_reject/skipped_existing/cumulative 4필드 존재: `grep -c "cumulative" backend/scripts/phase22_watch.py` >= 1
  </acceptance_criteria>
  <verify>
    <automated>python3 backend/scripts/phase22_watch.py --dry-run && python3 -m pytest backend/tests/phase22/test_watch_collect.py -q</automated>
  </verify>
  <done>belle 1커맨드 watch 실행 경로가 게이트·멱등·리포트·원장 불변식과 함께 배선되고, dry-run 이 무과금으로 전 경로를 self-check 한다</done>
</task>

<task type="auto">
  <name>Task 3: FLYWHEEL-RUNBOOK §1(쌓기) + 전체 호환 리허설</name>
  <files>backend/training/FLYWHEEL-RUNBOOK.md</files>
  <read_first>
    - backend/scripts/phase22_watch.py (Task 1-2 산출 — 런북이 문서화할 실 커맨드/플래그)
    - .planning/phases/22-custom-vlm-finetune/22-CONTEXT.md 의 D-12 (동의 3겹 — 파일럿=학습동의 필수, belle 2026-07-16 확정 반영)
  </read_first>
  <action>
    backend/training/FLYWHEEL-RUNBOOK.md 를 신설하고 §1 "쌓기 (연속 수집)"를 작성한다. §2 는
    22-12 가 추가할 자리(헤더만 예약). 내용(이모지 금지, 한국어):
    - 목적 한 줄: v7 성패 무관 데이터 축적(힉스필드), 수집+큐레이션만 = 저비용(라벨링은 §2).
    - belle 트리거 기본 절차(정확한 커맨드 1줄):
      `PHASE22_BELLE_GREENLIGHT=1 AWS_PROFILE=sunity-motion python3 backend/scripts/phase22_watch.py --run`
      — 사전 조건(로컬 venv 에 yt-dlp/gallery-dl 기설치, Gemini 키 = SSM /sunity/motion/gemini-api-key,
      크레딧 확인) 체크리스트 포함.
    - 주기 권장: 주 1회 (정은지 IG 하루 ~1영상 페이스 기준 주당 ~7후보 + YT 신규 업로드).
      스케줄 옵션 문서화: launchd/cron 등재 예시를 적되 "기본은 belle 수동 트리거 — 과금
      게이트(PHASE22_BELLE_GREENLIGHT)는 스케줄에서도 유지" 명시(무인 과금 금지).
    - 리포트 읽는 법: watch_reports/{batch_id}.json 필드 설명 + "reject 급증 = 레지스트리
      필터 점검" 가이드.
    - 동의·라이선스 경계(D-12/D-09): 수집분 = training-only-no-redistribution, IG 는 공개
      릴스만(로그인/쿠키 미사용), 신규 채널 등재는 belle 승인 후 phase22_sources.yaml 형식 준수,
      내부(고객) 영상은 이 러너 스코프 밖(anonymize 경로 — 22-04 customer_track).
    - 배치 원장 규약 요약: collection_batches[] 의미, collection_complete 마감 의미 보존,
      22-12 가 배치를 소비하는 방식(collection_batch 필드로 신규분 식별).
    마지막으로 전체 리허설: phase22 테스트 스위트 전체 + dry-run + build_jsonl 호환 체크를
    한 번에 재실행해 플랜 종료 상태를 검증한다.
  </action>
  <acceptance_criteria>
    - backend/training/FLYWHEEL-RUNBOOK.md 존재, §1 에 실행 커맨드 1줄과 PHASE22_BELLE_GREENLIGHT 문자열 포함: `grep -c "PHASE22_BELLE_GREENLIGHT" backend/training/FLYWHEEL-RUNBOOK.md` >= 1
    - `grep -c "training-only-no-redistribution" backend/training/FLYWHEEL-RUNBOOK.md` >= 1 (D-09/D-12 경계 박제)
    - `python3 -m pytest backend/tests/phase22 -q` exit 0 (전 스위트 무회귀; skip 허용)
    - `python3 backend/scripts/phase22_watch.py --dry-run` exit 0
    - `git diff --quiet backend/training/datagen/build_jsonl.py backend/training/datagen/schema.py backend/evals/phase22/assert_gates.py` exit 0 (프로덕션 무접촉 최종 확인)
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest backend/tests/phase22 -q && python3 backend/scripts/phase22_watch.py --dry-run</automated>
  </verify>
  <done>belle 이 문서만 보고 주기 수집을 반복 실행할 수 있고, 플랜 전체가 기존 게이트·무접촉 제약과 함께 리허설 통과 상태로 종료된다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 외부 영상(YT/IG) → 로컬/S3 | 신뢰 불가 콘텐츠가 학습 원장에 유입되는 지점 |
| 로컬 러너 → S3 (sunity-motion 자격) | 쓰기 자격 증명이 스크립트 경로에 노출되는 지점 |
| 로컬 러너 → Gemini API | 과금 + 키 취급 지점 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-11-01 | Spoofing/Tampering | 악성·오분류 영상 유입 (제목 위장) | mitigate | 기존 3중 필터(discipline/duration/series) + curate_vision 다운로드-전 Gemini 게이트 재사용 — 우회 경로 신설 금지 (Task 2 는 기존 main 위임만) |
| T-22-11-02 | Tampering | manifest 마감 원장 훼손 (기존 행 변형/삭제) | mitigate | assert_ledger_invariants 가 append-only + collection_closed/balance_waiver 무변형을 실행마다 강제 (Task 1), 위반 시 저장 중단 |
| T-22-11-03 | Elevation | 무인 스케줄이 belle 과금 게이트를 우회 | mitigate | PHASE22_BELLE_GREENLIGHT env 게이트를 --run 진입에서 유지(SystemExit 2), 런북이 스케줄에서도 게이트 유지 명시 (Task 2/3) |
| T-22-11-04 | Information Disclosure | Gemini 키·AWS 자격 로그 유출 | mitigate | 키는 SSM/env 참조만, 값 echo/print 금지 (기존 curate_vision lazy 어댑터 관례), 리포트 JSON 에 자격·uid 필드 없음 |
| T-22-11-05 | Repudiation | 수집 이력 은폐 (몇 개를 언제 왜) | mitigate | collection_batches[] 원장 + watch_reports/{batch_id}.json 이중 기록, 실행 말미 pytest 게이트 자동 실행 |
| T-22-11-06 | Information Disclosure | IG 세션/쿠키 취급 | accept | 공개 릴스만 로그인 없이 접근(기존 collect_phase22_instagram 설계) — 쿠키 저장 자체가 없음. 런북에 "로그인 요구 시 계정 skip, 쿠키 도입 금지" 명시 |
| T-22-11-SC | Tampering | pip 공급망 | accept | 신규 pip 설치 0 — yt-dlp/gallery-dl/PyYAML 전부 기설치 재사용. 설치 태스크 부재로 legitimacy 체크포인트 불요 |
</threat_model>

<verification>
- `python3 -m pytest backend/tests/phase22 -q` — 전 스위트 pass (기존 156+ 무회귀 + 신규 test_watch_collect)
- `python3 backend/scripts/phase22_watch.py --dry-run` — exit 0, 네트워크·과금 0
- build_jsonl/schema/assert_gates `git diff --quiet` — 프로덕션 무접촉
- manifest: collection_complete=true 보존 + collection_batches=[] 초기화 + 기존 행 무변형
</verification>

<success_criteria>
- belle 1커맨드(--run, greenlight env)로 watchlist 주기 수집 실행 가능 — 신규만 과금, 기존 멱등 skip
- 배치 증분 등재 규약이 마감 무결성(build_jsonl 무수정 통과)과 공존함이 기계 검증됨
- 실행마다 신규/reject/누적 리포트 방출 (stdout + watch_reports JSON)
- FLYWHEEL-RUNBOOK §1 로 반복 운영 절차 박제 (스케줄 옵션 포함, 과금 게이트 유지)
</success_criteria>

<output>
Create `.planning/phases/22-custom-vlm-finetune/22-11-SUMMARY.md` when done
</output>
