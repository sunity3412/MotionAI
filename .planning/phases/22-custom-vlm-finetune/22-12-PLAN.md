---
phase: 22-custom-vlm-finetune
plan: 12
type: execute
wave: 9
depends_on: ["22-11", "22-04", "22-07"]
files_modified:
  - backend/training/sft/run_retrain_cycle.sh
  - backend/training/sft/promotion.py
  - backend/training/sft/promotion_ledger.json
  - backend/tests/phase22/test_promotion.py
  - backend/training/FLYWHEEL-RUNBOOK.md
autonomous: true
requirements: [FT-03, FT-04]

must_haves:
  truths:
    - "재학습 사이클 1커맨드(run_retrain_cycle.sh all)가 신규 배치 행만 라벨링하고(full_batch 행별 영속화 — 기존 accepted 재과금 0) 기존 학습셋과 병합 조립한다"
    - "S3 canonical training/phase22/jsonl/ 교체 전 반드시 백업 prefix 스냅샷(jsonl_backup_<ts>)이 선행된다 — 백업 실패 시 업로드 중단"
    - "게이트(assert_gates --require-pass) exit 0 일 때만 promotion ledger 의 current 포인터가 전진한다(단방향 래칫) — FAIL 은 attempt 기록만 남기고 기존 모델 유지"
    - "게이트 실행에 VLLM_USE_FLASHINFER_SAMPLER=0 + TORCH_CUDA_ARCH_LIST=12.0 이 Blackwell(compute cap >= 12) Pod 에서 자동 적용된다 (flashinfer 오탐 우회 — 2026-07-16 실증)"
    - "사이클마다 비용 관측치(라벨링 영상 수·Gemini 호출 추정·SFT 시간·게이트 판정)가 cycle report 로 방출된다"
  artifacts:
    - path: "backend/training/sft/run_retrain_cycle.sh"
      provides: "주기 재학습 사이클 러너 — preflight/label/assemble/train/gates/promote 순차 stage"
      contains: "VLLM_USE_FLASHINFER_SAMPLER"
      min_lines: 100
    - path: "backend/training/sft/promotion.py"
      provides: "게이트 판정 파싱 + 래칫 원장 순수 로직 + cycle report 조립"
      exports: ["parse_gate_verdict", "make_ledger_entry", "apply_ratchet", "make_cycle_report"]
      min_lines: 80
    - path: "backend/training/sft/promotion_ledger.json"
      provides: "승격 원장 (append-only entries + current 포인터)"
      contains: "current"
    - path: "backend/tests/phase22/test_promotion.py"
      provides: "래칫 단조성·판정 파싱·리포트 필드 테스트"
      min_lines: 60
    - path: "backend/training/FLYWHEEL-RUNBOOK.md"
      provides: "§2 공부하기 — belle 주 1회 트리거 절차 + Pod 생명주기 + flashinfer env 박제"
      contains: "TORCH_CUDA_ARCH_LIST"
  key_links:
    - from: "backend/training/sft/run_retrain_cycle.sh"
      to: "backend/training/distill/full_batch.py"
      via: "label/assemble stage CLI 호출 (--assemble --with-perturb --upload)"
      pattern: "full_batch"
    - from: "backend/training/sft/run_retrain_cycle.sh"
      to: "backend/training/sft/run_sft.sh + run_sft_gates.sh"
      via: "train/gates stage 순차 호출 (SERIAL)"
      pattern: "run_sft"
    - from: "backend/training/sft/promotion.py"
      to: "backend/training/sft/promotion_ledger.json"
      via: "apply_ratchet append-only 쓰기 (PASS 만 current 전진)"
      pattern: "current"
---

<objective>
데이터 플라이휠의 "공부하기" 동사를 배치 루프로 상설화한다 (belle 확정 2026-07-16). 22-11 이
쌓은 신규 배치를 주 1회급 belle 트리거로 몰아서: 라벨(D-10b Gemini 교사 증류, 신규분만 과금)
→ 병합 조립(D-10a perturb 포함 3트랙) → SFT(D-06/D-07 기존 러너) → D-15 게이트 → **통과 시만
승격(단방향 래칫)**. 전부 기존 자산 orchestration — full_batch.py(행별 영속화로 증분 라벨링
공짜), pod_coords(RTMW 캐시), build_jsonl/assemble(--assemble --with-perturb --upload),
run_sft.sh, merge_and_quant.sh, run_sft_gates.sh + assert_gates.py. 신규 코드는 경량 러너
셸 + 승격 원장 순수 로직 + 런북뿐이다.

실행 시점 제약: v7 SFT 가 현재 Pod(olnrvtj0f80pl4)에서 실행 중 — 이 플랜의 **작성·로컬 검증은
지금**, Pod 위 실 사이클 실행은 v7 종료 후(러너의 serial lock 이 이중 방어). 파이프라인 동시성
비안전 원칙에 따라 러너는 전 stage 순차 강제.

Purpose: FT-04(SFT 학습·평가 게이트)의 지속 운영 확장 + FT-03 라벨링 경로(b) 반복 가동.
게이트 FAIL 이 기본 상태여도(v4~v6 이력) 데이터는 계속 쌓이고, 통과하는 라운드에서만 모델이
전진하는 구조 — "쌓기"와 "공부"의 분리를 운영으로 완성한다.
Output: run_retrain_cycle.sh + promotion.py/ledger + FLYWHEEL-RUNBOOK §2.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-custom-vlm-finetune/22-CONTEXT.md
@.planning/phases/22-custom-vlm-finetune/22-04-SUMMARY.md
@.planning/phases/22-custom-vlm-finetune/22-07-SUMMARY.md
@backend/training/distill/full_batch.py
@backend/training/sft/run_sft.sh
@backend/training/sft/run_sft_gates.sh
</context>

<interface_contracts>
집행자가 탐색 없이 쓸 수 있는 확정 계약 (플래너가 소스 실측):

- `python3 -m training.distill.full_batch` CLI(라인 420-434): `--manifest`(기본 repo manifest),
  `--out-dir`(기본 /workspace/phase22_distill_out — Network Volume 에 reports 129 + accepted 109
  보존), `--cache-dir`(pod_coords 캐시), `--max-rows`, `--assemble`, `--with-perturb`,
  `--upload`(uploader 주입 시에만), `--validation-owner`(기본 explicit_val_jsonl), `--partial`.
  **행별 결과 파일이 진실 — 터미널 사유는 skip, 재실행 시 기존 행 Gemini 호출 0** (22-04 실증).
  즉 22-11 배치로 manifest 가 자라면 같은 커맨드가 신규 행만 라벨링한다(증분 공짜).
- `bash training/sft/run_sft.sh` — env: SFT_MODEL(기본 Qwen/Qwen3-VL-8B-Instruct), TRAIN_VENV
  (기본 /workspace/train_venv), HF_HOME. S3 JSONL(training/phase22/jsonl)을 로컬라이즈 후 학습.
  산출 /workspace/phase22_sft_out/{run_id}/checkpoint-*.
- `bash training/sft/run_sft_gates.sh <MERGED_OR_AWQ_DIR>` — 인자 1 필수. env: PROMPT_MODE
  (본판정=aligned), REPETITION_PENALTY(본판정 1.0). 내부에서 vLLM serve → run_bakeoff run1/run2
  (--skip-judge, Gemini 과금 0) → assert_gates 기본 + --require-pass 순차. SERIAL 명시.
- `backend/evals/phase22/assert_gates.py` — **무접촉**. exit 0=PASS / 비0=FAIL 이 래칫 입력.
- 병합: `training/export/merge_and_quant.sh` — bf16 병합본 `<ckpt>-merged` 규약(22-07).
  AWQ 는 llm-compressor 이슈로 미완 — 게이트는 bf16 병합본으로 실행(22-07 선례 유지).
- flashinfer 오탐 우회(2026-07-16 실증, Blackwell RTX PRO 6000 sm_120):
  `VLLM_USE_FLASHINFER_SAMPLER=0 TORCH_CUDA_ARCH_LIST=12.0`. **주의: TORCH_CUDA_ARCH_LIST=12.0
  은 sm_120 전용 — A100(sm_80) 등에서는 설정 금지.** compute cap 은
  `nvidia-smi --query-gpu=compute_cap --format=csv,noheader` 로 판별.
- S3 canonical: `s3://sunity-motion-pilot-videos/training/phase22/jsonl/`(train/val/_meta).
  백업 관례: 교체 전 스냅샷 prefix(선례 jsonl_v1_distill_only/) — 본 플랜은
  `training/phase22/jsonl_backup_<UTC ts>/` 로 표준화.
- Pod 운영: belle 콘솔 start/stop, Claude 가 SSH 실행(프록시 ssh.runpod.io, -tt + stdin 파이프).
  코드 반입 = git push 후 Pod 에서 pull (gsd-pod-work-push-first).
</interface_contracts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: promotion.py 래칫 원장 순수 로직 + ledger 초기화 + 테스트</name>
  <files>backend/training/sft/promotion.py, backend/training/sft/promotion_ledger.json, backend/tests/phase22/test_promotion.py</files>
  <read_first>
    - backend/evals/phase22/assert_gates.py (게이트 이름·exit 코드 규약·artifact JSON 구조 — 무접촉, 파싱 계약만 추출)
    - .planning/phases/22-custom-vlm-finetune/22-07-SUMMARY.md 의 게이트 표 (6개 게이트 명칭·판정 형식)
    - backend/training/distill/full_batch.py 의 aggregate_stats (라벨링 카운트 소스 — cycle report 입력)
  </read_first>
  <behavior>
    - parse_gate_verdict(require_pass_exit_code, gate_artifact_paths) 는 {"pass": bool, "gates": {게이트명: "PASS"|"FAIL"}, "artifacts": [...]} 를 반환 — exit 0 만 pass=True
    - make_ledger_entry(ckpt_path, verdict, data_snapshot={train_rows, val_rows, distill, perturb, text}, batch_ids, ts) 는 promoted 필드를 verdict.pass 로 설정
    - apply_ratchet(ledger, entry): entries 에 append-only, entry.promoted=True 일 때만 ledger["current"]={ckpt, ts} 갱신 — FAIL entry 후 current 는 직전 promoted 를 그대로 가리킴(단조성)
    - apply_ratchet 은 기존 entries 를 변형·삭제하지 않는다 (before deep-equal 보존)
    - current 가 None(초기)인 상태에서 FAIL 만 쌓이면 current 는 None 유지 — 미승격 모델이 current 가 되는 경로 부재
    - make_cycle_report(label_stats, sft_wall_seconds, verdict, promoted) 는 {new_labeled, accepted, rejected_judge, rejected_parse, rejected_contract, est_gemini_calls(=new_labeled*2 교사+judge), sft_wall_seconds, gates, promoted} 전 필드 포함 — 사람 점수 라벨 필드 없음
  </behavior>
  <action>
    backend/training/sft/promotion.py 를 신설한다 — 순수 함수(파일/네트워크 I/O 는 load/save
    껍데기 분리, boto3/torch import 0). D-15 게이트 판정을 승격 래칫으로 변환하는 단일 owner.
    모듈 docstring 에 래칫 원칙 박제: "게이트 PASS(assert_gates --require-pass exit 0)만 current
    를 전진시킨다. FAIL 은 attempt 로 기록만 — 서빙/swap(22-08~10 소관)은 current 포인터만 신뢰."
    judge 점수·사람 점수는 원장에 저장 금지(객관성 hard gate — 게이트 verdict 문자열만).

    backend/training/sft/promotion_ledger.json 초기화: {"schema": "promotion-ledger-v1",
    "current": null, "entries": []} — v7 이전 이력(v4/v5 FAIL)은 소급 기재하지 않는다(22-07
    SUMMARY 가 이력 소유). CLI 엔트리(main): `python3 -m training.sft.promotion --gate-exit N
    --ckpt PATH --stats-json PATH --ledger PATH --report-out PATH` 형태로 셸 러너(Task 2)가
    호출 — exit 0/1 을 promoted 여부로 반환해 셸이 분기 가능하게.

    backend/tests/phase22/test_promotion.py 를 RED 먼저 작성: behavior 6항목 전부 + "FAIL 연속
    2회 후 PASS 1회 시 current 가 PASS ckpt 로 전진하고 entries 는 3개" 시나리오. 이모지 금지.
  </action>
  <acceptance_criteria>
    - `python3 -m pytest backend/tests/phase22/test_promotion.py -q` exit 0
    - `python3 -c "import json; l=json.load(open('backend/training/sft/promotion_ledger.json')); assert l['current'] is None and l['entries']==[]"` exit 0
    - promotion.py 에 score 계열 저장 부재: `grep -v '^#' backend/training/sft/promotion.py | grep -ci "judge_score"` 출력 0
    - `git diff --quiet backend/evals/phase22/assert_gates.py` exit 0 (무접촉)
    - promotion.py 최상위에서 boto3/torch import 0: `grep -v '^#' backend/training/sft/promotion.py | grep -cE '^import (boto3|torch)|^from (boto3|torch)'` 출력 0
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest backend/tests/phase22/test_promotion.py -q</automated>
  </verify>
  <done>승격 래칫이 순수 로직+테스트로 박제되고, FAIL 이 current 를 절대 전진시키지 못함이 기계 검증된다</done>
</task>

<task type="auto">
  <name>Task 2: run_retrain_cycle.sh — 순차 stage 러너 (백업-선행 + serial lock + flashinfer 조건부 env + 비용 관측치)</name>
  <files>backend/training/sft/run_retrain_cycle.sh</files>
  <read_first>
    - backend/training/sft/run_sft.sh (env 계약·로컬라이즈 흐름·산출 경로 — 러너가 그대로 호출)
    - backend/training/sft/run_sft_gates.sh (인자·PROMPT_MODE·SERIAL 규약·vLLM serve 내부 처리)
    - backend/training/distill/full_batch.py 라인 415-440 (CLI 플래그 — label/assemble stage 호출 형태)
    - backend/training/export/merge_and_quant.sh (병합 규약 <ckpt>-merged)
    - backend/training/sft/promotion.py (Task 1 산출 — promote stage CLI 계약)
  </read_first>
  <action>
    backend/training/sft/run_retrain_cycle.sh 신설 (bash, set -euo pipefail, 주석 한국어,
    이모지 금지). 사용법: `bash run_retrain_cycle.sh [all|label|assemble|train|gates|promote]`
    (기본 all — belle "주 1회 이거 한 줄"). Pod 전용(경로 /workspace/*), nohup 무인 실행 전제.
    경로 앵커(plan-checker WARNING 1 해소): 러너 상단에서
    `ROOT="${SUNITY_ROOT:-/workspace/SunityMotion}"; cd "$ROOT/backend"` 로 cwd 를 단일
    고정하고, 이후 모든 파일 인자(ledger/stats/report)는 `$ROOT/backend/...` 절대경로로만
    전달 — 특히 promote stage `--ledger` 는 `$ROOT/backend/training/sft/promotion_ledger.json`.
    (아래 stage 예시의 `backend/...` 상대 표기를 문자 그대로 쓰지 말 것 — cwd=backend 에서
    `backend/backend/...` 로 깨져 SFT 완주 후 promote 가 실패한다.)

    Stage 구성 (전부 순차 — 파이프라인 동시성 비안전):
    1. preflight: (a) serial lock — `pgrep -f "swift sft|vllm serve|full_batch"` 결과 비면 진행,
       있으면 "다른 학습/서빙 프로세스 실행 중(v7 등) — 종료 후 재시도" 메시지와 exit 1
       (v7 비중첩 이중 방어). (b) `PHASE22_BELLE_GREENLIGHT=1` 확인(라벨 stage Gemini 과금 게이트
       — 없으면 exit 2). (c) /workspace 여유 디스크 >= 30GB 확인(볼륨 쿼터 사건 재발 방지).
       (d) `git -C /workspace/SunityMotion pull --ff-only` 로 최신 manifest/코드 동기화.
    2. label: `python3 -m training.distill.full_batch --out-dir /workspace/phase22_distill_out
       --cache-dir /workspace/phase22_coords_cache` — 행별 영속화가 기존 행 skip, 신규 배치
       (22-11 collection_batch 행)만 과금. 시작/종료 시각과 신규 처리 행 수를 변수로 캡처.
    3. assemble: **백업 선행 강제** — `aws s3 sync s3://sunity-motion-pilot-videos/training/phase22/jsonl/
       s3://sunity-motion-pilot-videos/training/phase22/jsonl_backup_$(date -u +%y%m%d-%H%M)/`
       가 exit 0 이어야만 다음 줄 진행(set -e + 명시 || exit). 이후
       `python3 -m training.distill.full_batch --assemble --with-perturb --upload` 로 기존+신규
       accepted 전체를 병합 재조립·canonical 교체.
    4. train: `bash training/sft/run_sft.sh` (foreground — 사이클 자체가 nohup). wall time 캡처.
       best ckpt 경로를 /workspace/phase22_sft_out 최신 run 에서 해석.
    5. gates: `bash training/export/merge_and_quant.sh` 로 bf16 병합 후, **flashinfer env 조건부
       적용** — `CAP=$(nvidia-smi --query-gpu=compute_cap --format=csv,noheader | head -1)` 이
       12.0 이상이면 `export VLLM_USE_FLASHINFER_SAMPLER=0 TORCH_CUDA_ARCH_LIST=12.0`
       (sm_120 Blackwell 오탐 우회, 2026-07-16 실증), 미만이면 미설정(A100 sm_80 에 12.0 강제
       금지). 이어서 `PROMPT_MODE=aligned bash training/sft/run_sft_gates.sh <merged_dir>` —
       exit 코드를 GATE_EXIT 로 캡처(set -e 예외 처리: `|| GATE_EXIT=$?`).
    6. promote: `python3 -m training.sft.promotion --gate-exit $GATE_EXIT --ckpt <merged_dir>
       --stats-json <full_batch stats> --ledger backend/training/sft/promotion_ledger.json
       --report-out /workspace/cycle_reports/cycle_$(date -u +%y%m%d-%H%M).json` — PASS 면
       ledger current 전진, FAIL 이면 attempt 기록만(러너는 FAIL 에서도 exit 0 로 정상 종료하되
       "NOT PROMOTED — 기존 모델 유지" 를 stdout 마지막 줄에 명시). ledger 변경은 git commit
       대상임을 stdout 으로 안내(Pod 작업 = push 한 단위).
    말미에 cycle report 요약을 stdout 방출: 라벨링 신규 N / accepted / reject 분해 /
    est_gemini_calls / SFT wall time / 게이트 판정 표 / promoted 여부 (은폐 금지).
    시크릿 echo 금지(키 값 출력 0 — env 이름만 로그).
  </action>
  <acceptance_criteria>
    - `bash -n backend/training/sft/run_retrain_cycle.sh` exit 0 (문법)
    - flashinfer env 박제 + 조건부: `grep -c "VLLM_USE_FLASHINFER_SAMPLER=0" backend/training/sft/run_retrain_cycle.sh` >= 1 그리고 `grep -c "compute_cap" backend/training/sft/run_retrain_cycle.sh` >= 1
    - 백업 선행: `grep -n "jsonl_backup_" backend/training/sft/run_retrain_cycle.sh` 의 첫 매치 라인이 `--assemble` 첫 매치 라인보다 앞 (실행자: grep -n 두 값 비교로 확인)
    - serial lock: `grep -cE "pgrep.*(swift|vllm|full_batch)" backend/training/sft/run_retrain_cycle.sh` >= 1
    - greenlight 게이트: `grep -c "PHASE22_BELLE_GREENLIGHT" backend/training/sft/run_retrain_cycle.sh` >= 1
    - 순차성: 스크립트에 백그라운드 병렬 실행 부재 — `grep -v '^#' backend/training/sft/run_retrain_cycle.sh | grep -cE '&[[:space:]]*$'` 출력 0
    - `git diff --quiet backend/training/sft/run_sft.sh backend/training/sft/run_sft_gates.sh backend/evals/phase22/assert_gates.py backend/training/datagen/build_jsonl.py` exit 0 (기존 러너·게이트 무접촉)
  </acceptance_criteria>
  <verify>
    <automated>bash -n backend/training/sft/run_retrain_cycle.sh && grep -q "VLLM_USE_FLASHINFER_SAMPLER=0" backend/training/sft/run_retrain_cycle.sh && grep -q "jsonl_backup_" backend/training/sft/run_retrain_cycle.sh</automated>
  </verify>
  <done>1커맨드 사이클 러너가 백업-선행·serial lock·조건부 flashinfer env·비용 관측치·래칫 연동을 갖추고 로컬 문법+계약 grep 검증을 통과한다 (실 Pod 실행은 v7 종료 후 운영 절차)</done>
</task>

<task type="auto">
  <name>Task 3: FLYWHEEL-RUNBOOK §2(공부하기) — belle 주 1회 절차 + Pod 생명주기 + 최종 리허설</name>
  <files>backend/training/FLYWHEEL-RUNBOOK.md</files>
  <read_first>
    - backend/training/FLYWHEEL-RUNBOOK.md (22-11 산출 §1 — 형식·톤 일치, §2 예약 자리)
    - backend/training/sft/run_retrain_cycle.sh (Task 2 산출 — 문서화할 실 커맨드·stage·exit 규약)
    - .planning/phases/22-custom-vlm-finetune/22-07-SUMMARY.md (게이트 FAIL 이 정상 상태일 수 있음 — 런북 기대치 설정)
  </read_first>
  <action>
    FLYWHEEL-RUNBOOK.md 의 §2 "공부하기 (주기 재학습 배치 루프)"를 작성한다 (§1 무변형).
    내용(이모지 금지, 한국어, belle 이 "주 1회 이거 한 줄" 수준으로 따라할 수 있게):
    - 전제 조건 체크리스트: (1) v7 등 진행 중 학습 없음(러너 lock 이 이중 방어하지만 belle 이
      먼저 확인), (2) Pod = belle 이 콘솔에서 start(생명주기는 belle, 실행은 Claude SSH —
      프록시 ssh.runpod.io, 재생성 시 SSH 엔드포인트 변경 주의), (3) 22-11 watch 수집이 1배치
      이상 쌓임(collection_batches 확인), (4) Gemini 크레딧 확인(신규 행 수 x 2콜 추정치 제시).
    - 실행 한 줄 (박제):
      `PHASE22_BELLE_GREENLIGHT=1 nohup bash training/sft/run_retrain_cycle.sh all > /workspace/cycle_$(date -u +%y%m%d).log 2>&1 &`
      (cd /workspace/SunityMotion/backend 에서). stage 재시작법: 중단 시 label 은 재실행 안전
      (행별 영속화 — 재과금 0), assemble 부터 개별 stage 지정 가능.
    - flashinfer env 박제 섹션: Blackwell(sm_120) 게이트 vLLM 오탐 =
      `VLLM_USE_FLASHINFER_SAMPLER=0 TORCH_CUDA_ARCH_LIST=12.0` 필요(2026-07-16 실증, 러너가
      compute cap >= 12 에서 자동 적용). A100 등 비-Blackwell 에서는 설정하지 않음(자동).
    - 래칫 해석: "게이트 FAIL = 실패 아님 — 데이터는 쌓였고 모델만 미승격. current 는
      promotion_ledger.json 이 진실, FAIL 로그는 다음 처방 근거". PASS 시 후속(22-08 서빙 swap)
      진입 조건이 됨을 명시.
    - 종료 절차: cycle report 확인 → ledger/manifest 변경 git commit+push(Pod 작업 = push 한
      단위) → belle 콘솔 Pod stop(과금 차단).
    - 비용 관측치 읽는 법: cycle_reports JSON 필드(new_labeled/est_gemini_calls/
      sft_wall_seconds/gates/promoted) 설명 + "est_gemini_calls 가 예상 밖으로 크면 label
      stage 중단 후 belle 확인" 가이드.
    최종 리허설: phase22 전체 테스트 + 22-11/22-12 신규 검증 커맨드 일괄 재실행으로 플랜 종료
    상태 확인.
  </action>
  <acceptance_criteria>
    - `grep -c "TORCH_CUDA_ARCH_LIST" backend/training/FLYWHEEL-RUNBOOK.md` >= 1 그리고 `grep -c "VLLM_USE_FLASHINFER_SAMPLER" backend/training/FLYWHEEL-RUNBOOK.md` >= 1 (flashinfer env 런북 박제)
    - `grep -c "run_retrain_cycle.sh" backend/training/FLYWHEEL-RUNBOOK.md` >= 1 (실행 한 줄 존재)
    - `grep -c "promotion_ledger" backend/training/FLYWHEEL-RUNBOOK.md` >= 1 (래칫 진실 소스 명시)
    - §1 무변형: `git diff backend/training/FLYWHEEL-RUNBOOK.md` 가 §2 추가만 포함 (실행자가 diff 로 §1 라인 삭제/변경 0 확인)
    - `python3 -m pytest backend/tests/phase22 -q` exit 0 (전 스위트 무회귀)
    - `bash -n backend/training/sft/run_retrain_cycle.sh` exit 0 (최종 재확인)
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest backend/tests/phase22 -q && bash -n backend/training/sft/run_retrain_cycle.sh && grep -q "TORCH_CUDA_ARCH_LIST" backend/training/FLYWHEEL-RUNBOOK.md</automated>
  </verify>
  <done>belle 이 런북만 보고 주 1회 재학습 사이클을 트리거·해석·마감할 수 있고, flashinfer env 와 래칫 규약이 문서에 박제된다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 러너 → S3 canonical 학습셋 | 단일 진실(train/val jsonl) 파괴 가능 지점 |
| 러너 → Gemini API | 배치 과금 지점 (신규 행 x 교사+judge) |
| 게이트 판정 → 승격 원장 | 미검증 모델이 current 가 되는 경로 차단 지점 |
| 로컬/Claude → Pod SSH | 자격·실행 경계 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-12-01 | Tampering/DoS | S3 canonical jsonl 파괴 (--upload 덮어쓰기) | mitigate | assemble stage 는 jsonl_backup_<ts>/ s3 sync 성공(exit 0) 후에만 --upload 진행 — 백업 실패 = 사이클 중단 (Task 2) |
| T-22-12-02 | Elevation | 게이트 FAIL 모델이 승격됨 | mitigate | apply_ratchet 이 promoted=True(=--require-pass exit 0)만 current 전진, 테스트가 FAIL 후 current 불변을 강제 (Task 1) |
| T-22-12-03 | DoS | 동시 실행이 파이프라인/학습을 오염 (v7 중첩 포함) | mitigate | preflight pgrep serial lock + 런북 전제 조건 + 전 stage 순차 (Task 2/3) |
| T-22-12-04 | Information Disclosure | Gemini/AWS/HF 키 로그 유출 (nohup 로그 장기 보존) | mitigate | 러너는 env 이름만 로그, 값 echo 0 (T-22-22 관례 승계) — grep 게이트는 아니지만 코드 리뷰 항목으로 acceptance 에 시크릿 echo 금지 명시 |
| T-22-12-05 | Repudiation | 과금·승격 이력 부재 (언제 몇 행 라벨링했나) | mitigate | cycle_reports/<ts>.json + promotion_ledger entries append-only 이중 원장, ledger 는 repo 커밋 |
| T-22-12-06 | Tampering | 잘못된 env(TORCH_CUDA_ARCH_LIST=12.0)가 비-Blackwell Pod 학습을 파괴 | mitigate | compute_cap 판별 조건부 적용 — sm_120 미만에서는 미설정 (Task 2) |
| T-22-12-SC | Tampering | pip 공급망 | accept | 신규 pip 설치 0 — ms-swift/vLLM/llm 스택 전부 Pod train_venv 기설치 재사용. 설치 태스크 부재로 legitimacy 체크포인트 불요 |
</threat_model>

<verification>
- `python3 -m pytest backend/tests/phase22 -q` — 전 스위트 pass (신규 test_promotion 포함)
- `bash -n backend/training/sft/run_retrain_cycle.sh` — 문법 exit 0
- grep 계약: VLLM_USE_FLASHINFER_SAMPLER=0 / compute_cap / jsonl_backup_ / pgrep lock / PHASE22_BELLE_GREENLIGHT 전부 존재
- `git diff --quiet` — run_sft.sh / run_sft_gates.sh / assert_gates.py / build_jsonl.py / schema.py 무접촉
- promotion_ledger.json — current=null, entries=[] 초기 상태
- 실 Pod 사이클 실행은 이 플랜 스코프 밖(운영 — v7 종료 후 런북 절차, belle 트리거)
</verification>

<success_criteria>
- belle 주 1회 1커맨드로 라벨(신규만 과금)→병합 조립→SFT→게이트→래칫 승격 사이클 실행 가능
- canonical 교체 전 백업 선행이 스크립트 구조로 강제됨
- 게이트 PASS 만 current 전진(단방향 래칫)이 순수 로직+테스트로 증명됨
- flashinfer 우회 env 가 러너(조건부 자동)와 런북(박제) 양쪽에 존재
- 사이클마다 비용 관측치(라벨 수/Gemini 추정/SFT 시간/판정) 방출
</success_criteria>

<output>
Create `.planning/phases/22-custom-vlm-finetune/22-12-SUMMARY.md` when done
</output>
