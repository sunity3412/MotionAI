---
phase: 23-mode-1-recall-still-frame-veto-dtw-key-frame
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py
  - backend/shared/python/sunity_shared/analysis/vision_veto.py
  - backend/shared/python/sunity_shared/analysis/skeleton.py
  - backend/functions/pipeline/app.py
  - backend/shared/python/sunity_shared/models.py
  - app/src/types/analysis.ts
  - docs/contract.md
  - backend/tests/test_gemini_vision_scorer.py
  - backend/tests/test_vision_veto.py
  - backend/tests/test_pipeline_vision_gate.py
autonomous: true
requirements: [VETO-01, VETO-02, VETO-03]

must_haves:
  truths:
    - "Mode 1 veto 입력이 whole-video 업로드에서 학생 worst-pose still + DTW-매칭 정은지 same-pose still 나란히 입력으로 교체된다 (D-01)"
    - "단일 호출 과소열거가 부위별(상체/하체/라인) 프롬프트 × 선별 key-frame fan-out 으로 보정되어 상체 결함(좌/우팔+고개)이 union 에 살아남는다 (D-05)"
    - "DTW 정렬 신뢰도(MotionMatch.distance)가 낮으면 ±윈도우 다중프레임 union 으로, 그래도 낮으면 거짓결함을 만들지 않고 보류 status 를 반환한다 (D-03)"
    - "정타/깨끗 still 은 none 유지 (severity median, 위양성 비증가) + 같은 입력=같은 verdict (캐시·temp 0 결정론)"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py"
      provides: "이미지 업로드 swap(_upload_image) + 부위별 key-frame fan-out → 기존 _union_differences 집계 + INPUT_GRANULARITY frame token + PROMPT/SCHEMA version bump"
      contains: "_upload_image"
    - path: "backend/shared/python/sunity_shared/analysis/vision_veto.py"
      provides: "worst-pose ±윈도우 key-frame 후보 list (순수) + 머리/목/그립 part keyword 확장"
      contains: "_HIGHLIGHT_KEYPOINTS"
    - path: "backend/functions/pipeline/app.py"
      provides: "_apply_vision_veto still-pair 경로 + DTW-confidence 게이팅 + low_alignment_confidence status"
      contains: "low_alignment_confidence"
    - path: "backend/shared/python/sunity_shared/models.py"
      provides: "VISION_VETO_STATUSES 에 low_alignment_confidence 추가 (3-way lockstep)"
      contains: "low_alignment_confidence"
  key_links:
    - from: "backend/functions/pipeline/app.py::_apply_vision_veto"
      to: "gemini_vision_scorer.assess_fault_severity"
      via: "still-frame 경로 호출 (student worst-pose frame + DTW-matched reference frame)"
      pattern: "assess_fault_severity"
    - from: "backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py"
      to: "_union_differences"
      via: "부위별 key-frame verdict fan-out 을 기존 union 집계에 투입"
      pattern: "_union_differences"
    - from: "backend/functions/pipeline/app.py"
      to: "fault_zoom._matched_ref_frame"
      via: "DTW match → 정은지 same-pose 프레임 선택 (재계산 금지)"
      pattern: "_matched_ref_frame"
---

<objective>
Mode 1 veto/비교의 **입력 granularity** 를 "영상 통째 Gemini 업로드"에서 "DTW worst-pose
**단일 key-frame(들)** + 부위별 프롬프트 × union + N-sample"으로 교체한다. whole-video VLM 이
프레임 독립 인코딩으로 구조적으로 놓치던 **상체 결함(팔-폴 갭/고개젖힘/팔꿈치) recall 을
복구**하되 위양성은 늘리지 않는다. DTW 정렬 신뢰도가 낮은 정상 Mode 1 케이스(시작점/템포 상이)
에서는 거짓결함을 fabricate 하지 않고 **보류 status** 로 graceful 처리한다.

Purpose: 핵심 가치(분석 정확도) 직격 — 스파이크로 입증된 "레버는 입력 granularity" 를 production
에 반영. 어댑터 경계는 이 swap 을 위해 이미 설계됨(`VisionVetoCache.build_key` 가
`input_granularity` 보유, `_aggregate_comparison_verdict` 가 N-sample + union 보유) — INPUT 만
교체, 집계기는 재사용/확장.

Output: still-pair veto 경로 + 부위별 key-frame fan-out + DTW-confidence 게이팅 + 신규
`low_alignment_confidence` status (3-way contract lockstep). 채점 수식·downward-cap 불변.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kimtaesung/Dev/SunityMotion/.planning/phases/23-mode-1-recall-still-frame-veto-dtw-key-frame/23-CONTEXT.md
@/Users/kimtaesung/Dev/SunityMotion/.planning/phases/23-mode-1-recall-still-frame-veto-dtw-key-frame/23-PATTERNS.md
@/Users/kimtaesung/Dev/SunityMotion/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: still-image 업로드 swap + 이미지 mime 분기 (D-01 입력 형태)</name>
  <files>backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py, backend/tests/test_gemini_vision_scorer.py</files>
  <read_first>
    - backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py (`_upload_video` 505-533, `_mime` 478-484, `_ascii_safe_path` 487-502, `_call_gemini_comparison` 799-828, `assess_fault_severity` 546-667, `_state_name` 536-540)
    - backend/tests/test_gemini_vision_scorer.py (기존 monkeypatch 패턴: client/files.upload/generate_content fake)
    - 23-PATTERNS.md "analysis/gemini_vision_scorer.py" 섹션 (swap point 분석)
  </read_first>
  <behavior>
    - Test: `_mime("frame.png")` → "image/png", `_mime("frame.jpg")`/`.jpeg` → "image/jpeg", 기존 동작(.mov→video/quicktime, .mp4→video/mp4) 불변.
    - Test: `_upload_image(client, path)` 가 `_upload_video` 와 동일한 ACTIVE-wait/PROCESSING poll/TimeoutError/ascii-safe-path/tmp os.unlink 디시플린을 따르되 mime 만 이미지 분기 (fake client 로 검증).
    - Test: `_call_gemini_comparison` contents 순서가 still 핸들에서도 `[기준 라벨, 기준 핸들, 학생 라벨, 학생 핸들, 프롬프트]` 로 유지 (fake generate_content 가 contents 인자 캡처 → 순서 assert).
  </behavior>
  <action>
    `_mime` 에 png/jpeg 이미지 분기 추가 (기존 video 분기 보존 — fall-through default 는 video/mp4 유지하되 이미지 확장자는 명시 분기). `_upload_video` 의 구조를 복사한 형제 함수 `_upload_image(client, local_image_path)` 신설: `client.files.upload(config=UploadFileConfig(mime_type=_mime(...)))` + PROCESSING poll loop + `_FILES_TIMEOUT_S` TimeoutError + ACTIVE 검사 + `_ascii_safe_path` + tmp `os.unlink`. `google.genai import types` 는 lazy(함수 내부) 유지. `_call_gemini_comparison` 의 contents 순서 contract `[기준 라벨, ref_uploaded, 학생 라벨, student_uploaded, 프롬프트]` 는 핸들이 image 든 video 든 동일하게 유지 — 이 함수 시그니처/순서는 변경 금지(핸들 타입만 무관). temperature=0.0 / response_schema / thinking_config / 180s timeout 동일(객관성·결정론 parity, D-06). File API 핸들은 호출자 `finally` 에서 `client.files.delete` 로 정리(48h TTL·20GB 누수 방어 — 기존 디시플린 동일, still 도 예외 없음).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && python -m pytest tests/test_gemini_vision_scorer.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "_upload_image" backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py` 가 함수 정의를 반환한다.
    - `_mime` 가 png→"image/png", jpg/jpeg→"image/jpeg" 를 반환하고 기존 video 분기는 불변 (테스트로 검증).
    - `_call_gemini_comparison` 의 contents 리스트 순서가 `[기준 라벨, ref, 학생 라벨, student, 프롬프트]` 로 유지된다 (테스트로 캡처·assert).
    - `from google.genai` import 는 함수 내부에만 존재 (`grep -nc "^from google" gemini_vision_scorer.py | grep -v '^#'` 가 top-level 0).
    - pytest tests/test_gemini_vision_scorer.py 통과.
  </acceptance_criteria>
  <done>이미지 업로드 형제 함수 + mime 이미지 분기 + 비교 contents 순서 유지가 테스트로 증명되고, 핸들 정리 디시플린이 still 경로에서도 동일하다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 부위별 key-frame fan-out → 기존 union 집계 + granularity/버전 bump (D-05) + 머리/목/그립 part 확장</name>
  <files>backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py, backend/shared/python/sunity_shared/analysis/vision_veto.py, backend/shared/python/sunity_shared/analysis/skeleton.py, backend/tests/test_gemini_vision_scorer.py, backend/tests/test_vision_veto.py</files>
  <read_first>
    - backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py (`_aggregate_comparison_verdict` 676-727, `_union_differences` 733-762, `VISION_VETO_SAMPLES` 81, `INPUT_GRANULARITY` 88, `PROMPT_VERSION` 70, `SCHEMA_VERSION` 71, `VisionVetoCache.build_key` 340-376, `_call_gemini_comparison` 799-828, `_COMPARISON_PROMPT` 246+)
    - backend/shared/python/sunity_shared/analysis/vision_veto.py (`worst_pose_timestamp` 241-278, `_PART_KEYWORDS` 142-156, `_HIGHLIGHT_KEYPOINTS` 133-138, `_keypoints_for_part` 170-201)
    - backend/shared/python/sunity_shared/analysis/skeleton.py (`JOINT_TO_PART` 70-79, `PART_UPPER/CORE/LOWER` 65-68, `JOINT_KEYS` 50)
    - 23-PATTERNS.md "N-sample + per-part union (D-05)" 분석
  </read_first>
  <behavior>
    - Test: 부위별(상체/하체/라인) 프롬프트 × N-sample 으로 생성된 여러 verdict 를 `_union_differences` 에 투입했을 때, 한 부위 프롬프트만 잡은 결함(예: 왼팔)이 다른 부위가 잡은 결함과 함께 union 에 모두 남는다(부위별 최고 severity/deviation 1개씩). 두 번째 aggregator 신설 금지 — 기존 `_union_differences` 재사용.
    - Test: severity 는 median 유지 — 정타(모든 부위 none) 입력 시 dominant severity='none' (정타 100 보존, 위양성 비증가).
    - Test: still 경로 cache 키가 whole-video 키와 충돌하지 않는다 (`build_key(input_granularity="frame_pair")` ≠ `build_key(input_granularity="whole")`).
    - Test: 머리/목/그립 키워드 body_part 가 `_keypoints_for_part` 에서 최근접 keypoint(머리/목→어깨, 그립→손)로 매핑된다 (`ankle→knee` 선례 패턴).
  </behavior>
  <action>
    `_aggregate_comparison_verdict` 의 per-call 루프를 **부위별 프롬프트 × 선별 key-frame** 으로 확장: 부위 토큰(상체/하체/라인 — `skeleton.PARTS` + 라인) 각각에 대해 generic 부위-집중 비교 프롬프트로 호출하고, 모든 파싱된 verdict 를 **기존** `_union_differences` 에 투입한다. **두 번째 aggregator 작성 금지** — sample set 만 넓힌다. severity 는 median 유지(정타 100 보존, union 은 differences 만 widen). 부위 프롬프트는 `_COMPARISON_PROMPT` 의 generic 톤을 유지하되 "이번에는 [상체/하체/라인] 부위에 집중해 관찰" 류의 부위-스코프만 추가 — **특정 동작명/기대답/curve-fit 금지(D-06)**. `INPUT_GRANULARITY` 분기용 token(예 `"frame_pair"`)을 still 경로에서 `build_key(input_granularity=...)` 에 전달(whole 충돌 방지). 프롬프트/스키마 변경에 맞춰 `PROMPT_VERSION` / `SCHEMA_VERSION` bump(헤더 주석 mandate — stale verdict 자동 무효화). `vision_veto._PART_KEYWORDS` 에 머리/목(("머리","고개","목","head","neck") → 최근접 어깨)·그립(("그립","grip","손바닥") → 손) 확장(`ankle→knee` 선례). `skeleton.JOINT_KEYS` 순서 불변(line 50 주석). 비용·레이턴시 상한(W1 — plan-checker): 호출수 = parts × samples × frame_top_k 를 **명시 상수 `MAX_VETO_CALLS` 로 캡**한다 — `_aggregate_comparison_verdict` 진입부에서 `parts × samples × frame_top_k <= MAX_VETO_CALLS` 를 강제(초과 시 frame_top_k 부터 축소). frame_top_k·parts·samples 기본값과 `MAX_VETO_CALLS` 를 모듈 상수로 명시하고 근거 주석(라이브 veto 경로 레이턴시 + 구독료 하한 비용). 일반 동작에서 호출수가 결정적(같은 입력=같은 호출수)이라 23-03 결정론·비용 재현 가능. (Claude 재량은 상수 값 선택까지 — 상한 존재 자체는 mandate, D-06 준수.)
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && python -m pytest tests/test_gemini_vision_scorer.py tests/test_vision_veto.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nc "def _union_differences" backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py` 가 정확히 1 (두 번째 aggregator 없음).
    - `PROMPT_VERSION` 과 `SCHEMA_VERSION` 값이 이전(v7.0/v5.0)에서 bump 되었다 (`grep -n "PROMPT_VERSION\|SCHEMA_VERSION" ...`).
    - still 경로 granularity token 이 "whole" 이 아니다 (테스트: `build_key(input_granularity="frame_pair") != build_key(input_granularity="whole")`).
    - 머리/그립 body_part 가 빈 list 가 아닌 최근접 keypoint 로 매핑된다 (테스트 assert).
    - 부위별 fan-out verdict 들의 union 에서 한 부위만 잡은 결함이 보존된다 (테스트 assert). 정타 입력 severity='none'.
    - `MAX_VETO_CALLS` 상수가 정의되고 still 경로 호출수(parts × samples × frame_top_k)가 그 값 이하임이 테스트로 assert 된다 (호출수 ceiling — W1).
    - pytest 두 파일 통과.
  </acceptance_criteria>
  <done>부위별 key-frame fan-out 이 기존 union 집계기에 흘러들어 상체 결함이 보존되고, granularity/버전 bump 로 still verdict 가 whole-video verdict 와 격리되며, 머리/그립 part 가 추가되었다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: pipeline still-pair 경로 + DTW-confidence 게이팅 + low_alignment_confidence status (D-03) + 3-way lockstep</name>
  <files>backend/functions/pipeline/app.py, backend/shared/python/sunity_shared/models.py, app/src/types/analysis.ts, docs/contract.md, backend/tests/test_pipeline_vision_gate.py</files>
  <read_first>
    - backend/functions/pipeline/app.py (`_apply_vision_veto` 1662-1767, `_veto_passthrough` 1651-1659, Mode1 wiring `reference_dtw_match` 2411-2414 / `reference_local_video_path` 2459-2480)
    - backend/shared/python/sunity_shared/analysis/fault_zoom.py (`_matched_ref_frame` 44-62, `_frame_index` 34-41)
    - backend/shared/python/sunity_shared/analysis/motiondtw.py (`MotionMatch.distance` 72-77, `dtw` 26-69)
    - backend/shared/python/sunity_shared/analysis/vision_veto.py (`worst_pose_timestamp` 241-278, `apply_downward_cap` 104-118 — cap 불변)
    - backend/shared/python/sunity_shared/models.py (`VISION_VETO_STATUSES` 74-82)
    - app/src/types/analysis.ts (`VisionVeto` union 350-353)
    - docs/contract.md (§4 visionVeto status 정의 — `grep -n "visionVeto\|VISION_VETO\|mode3_held" docs/contract.md`)
  </read_first>
  <behavior>
    - Test: `MotionMatch.distance` 가 임계 이하(정렬 양호)면 still-pair 단일 key-frame 경로로 verdict 산출 + 기존 applied/not_applicable 동작 유지.
    - Test: distance 가 1차 임계 초과(정렬 약함)면 worst-pose ±윈도우 다중프레임 union 경로로 전환된다.
    - Test: distance 가 2차 임계도 초과(정렬 실패)면 거짓결함을 만들지 않고 status='low_alignment_confidence' + overallScore 불변 (cap 미적용) 을 반환한다.
    - Test: status='low_alignment_confidence' 는 점수/severity 필드를 동반하지 않는다 (객관성 — `_veto_passthrough` 패턴, score 불변).
  </behavior>
  <action>
    `_apply_vision_veto` 에 still-pair 경로를 추가한다: 학생 worst-pose frame(`worst_pose_timestamp` → `_frame_index`) + `fault_zoom._matched_ref_frame(reference_dtw_match, user_frame, ref_n)` 로 얻은 정은지 same-pose frame 을 still 입력으로 `assess_fault_severity` 호출(DTW 재계산 금지 — 기존 `reference_dtw_match` 재사용). DTW 정렬 신뢰도는 `MotionMatch.distance` 임계 게이팅(threshold 만 — motiondtw 는 pure 유지): distance ≤ T1 → 단일 key-frame; T1 < distance ≤ T2 → worst-pose ±윈도우 다중프레임 union; distance > T2 → **fabricate 금지**, `_veto_passthrough(score_result, "low_alignment_confidence")` 반환(전체영상 폴백 채택 안 함 — 그게 상체 누락 원인). 임계 T1/T2 는 generic(특정 영상 튜닝 금지, D-06) — 상수로 명시하고 주석으로 근거. 신규 status 를 기존 enum 패턴에 추가: `models.VISION_VETO_STATUSES` 에 `"low_alignment_confidence"` + 주석, `app/src/types/analysis.ts` `VisionVeto` union 의 score-free 분기(`disabled|skipped_error|...`)에 추가, `docs/contract.md` §4 visionVeto status 정의 갱신 — **3-way lockstep 단일 commit**. cap 수식(`apply_downward_cap`)·downward-only·terminal min cap 불변. graceful try/except(BLE001 한국어 사유) 보존. D-01/D-03 결정 id 를 한국어 주석으로 인용.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && python -m pytest tests/test_pipeline_vision_gate.py -x -q && cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "low_alignment_confidence" backend/shared/python/sunity_shared/models.py app/src/types/analysis.ts docs/contract.md` 가 세 파일 모두 ≥1 (3-way lockstep).
    - `_apply_vision_veto` 가 `fault_zoom._matched_ref_frame` 를 호출하고 `reference_dtw_match` 를 재사용한다 (DTW 재계산 함수 호출 부재 — `grep -n "motiondtw.dtw\|find_action_segment" _apply_vision_veto 범위` 0).
    - distance > T2 경로가 `_veto_passthrough(..., "low_alignment_confidence")` 를 반환하고 overallScore 가 입력과 동일하다 (테스트 assert).
    - low_alignment_confidence verdict 가 severity/capApplied/primaryFault 를 동반하지 않는다 (TS union score-free 분기 + 테스트).
    - `apply_downward_cap` 본체 미변경 (`git diff` 에 vision_veto.py cap 수식 변화 0).
    - pytest tests/test_pipeline_vision_gate.py 통과 + `npm run typecheck` clean.
  </acceptance_criteria>
  <done>still-pair 경로 + 2단계 DTW-confidence 게이팅이 작동하고, 정렬 실패 시 거짓결함 대신 low_alignment_confidence status 가 score 불변으로 반환되며, 신규 status 가 3-way contract 에 lockstep 으로 반영되었다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline → Gemini Files API | 학생/정은지 still 이미지 핸들 업로드(외부 VLM). 업로드 핸들 = 누수 자원 |
| Gemini 응답 → VisionVerdict | 외부 모델 자유텍스트가 verdict 로 파싱됨 (점수 누출/거짓결함 위험) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Information Disclosure | `_load_api_key` / 로그 | mitigate | GEMINI_API_KEY 절대 로그 금지 — hash/길이만 로그(T-20-06 선례 유지). 신규 코드는 키를 변수로도 노출 안 함 |
| T-23-02 | Tampering | Gemini verdict → score | mitigate | `_SCORE_PATTERN` leak guard 유지 — still 샘플도 누출 시 폐기(객관성). VisionVerdict/build_schema score 필드 영구 부재 |
| T-23-03 | Denial of Service | Gemini Files API 20GB 누수 | mitigate | still 핸들도 `finally` 에서 `client.files.delete` (whole-video 디시플린 동일). 부위×frame fan-out 으로 호출수 증가 → 상수 bound 명시 |
| T-23-04 | Tampering | DTW 오정렬 → 거짓결함 fabrication | mitigate | distance > T2 → low_alignment_confidence 보류(score 불변), 절대 fabricate 안 함(D-03/D-06 위양성 게이트) |
| T-23-05 | Availability | still 추출/정렬 실패 → 파이프라인 crash | mitigate | graceful degradation — 모든 실패는 skipped_error/보류 status 로 swallow, v1 점수 경로 생존(Pitfall 5) |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/test_gemini_vision_scorer.py tests/test_vision_veto.py tests/test_pipeline_vision_gate.py tests/test_fault_zoom.py tests/test_motiondtw.py -q` 회귀 0.
- `cd app && npm run typecheck` clean (VisionVeto union 갱신 반영).
- `grep -rn "low_alignment_confidence" backend/shared/python/sunity_shared/models.py app/src/types/analysis.ts docs/contract.md` 3/3.
- 객관성 grep gate: `grep -nE "score|점수|/100" build_schema/VisionVerdict 정의 범위` 에 신규 score 필드 0.
</verification>

<success_criteria>
- Mode 1 veto 입력이 whole-video → DTW worst-pose still-pair 로 교체됨 (Success #1 입력 swap 부분).
- 부위별 × key-frame union 으로 상체 결함이 보존됨 (Success #1 recall 부분 — eval 은 23-03 에서 실증).
- DTW 정렬 신뢰도 게이팅 + 보류 status 로 거짓결함 비생성 (Success #3 코드 경로).
- 정타 none 유지 + 결정론 (Success #2 코드 경로 — eval 실증은 23-03).
</success_criteria>

<output>
Create `.planning/phases/23-mode-1-recall-still-frame-veto-dtw-key-frame/23-01-SUMMARY.md` when done
</output>
