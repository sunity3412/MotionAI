---
phase: 23-mode-1-recall-still-frame-veto-dtw-key-frame
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - backend/shared/python/sunity_shared/analysis/features.py
  - backend/shared/python/sunity_shared/analysis/dimensions.py
  - backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py
  - backend/shared/python/sunity_shared/analysis/vision_veto.py
  - backend/functions/pipeline/app.py
  - backend/shared/python/sunity_shared/models.py
  - app/src/types/analysis.ts
  - docs/contract.md
  - backend/tests/test_features.py
  - backend/tests/test_gemini_vision_scorer.py
  - backend/tests/test_pipeline_vision_gate.py
autonomous: true
requirements: [VETO-04, VETO-05]

must_haves:
  truths:
    - "결함 출력에 관절각 직접 수치(예: 무릎 145° vs 정은지 178°, 33° 더 굽음, ↑펴라)가 표시된다 — 스케일 무관 3D 각도 직접 (D-02)"
    - "거리 정량화는 절대 cm/m 가 아니라 몸-상대 칸/층 텍스트(정은지=N칸 기준, 학생을 비율로 환산)로 표시된다 (D-02)"
    - "코칭이 결함군을 보고 가능한 root cause(힘 부족/폴 밀착 풀림)를 기존 dual-coach 원인 섹션에서 '~로 보임' 가설로 묶는다 (D-04)"
    - "신규 정량화·원인 필드는 모두 DESCRIPTIVE 전용 — VisionVerdict/build_schema 는 score-free 유지(객관성 hard gate, D-06)"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/features.py"
      provides: "학생 vs DTW-매칭 정은지 프레임 per-joint 각도 델타 (순수, 스케일 무관)"
      contains: "compute_joint_angles"
    - path: "backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py"
      provides: "build_schema 에 몸-상대 notch 텍스트 등 DESCRIPTIVE 필드 추가 + 비교 프롬프트 원인 가설 섹션 (score 필드 0 유지)"
      contains: "build_schema"
    - path: "backend/functions/pipeline/app.py"
      provides: "정량화 텍스트/각도 델타를 applied 경로 audit 에 DESCRIPTIVE 로 attach (점수 아님)"
      contains: "visionVeto"
  key_links:
    - from: "backend/functions/pipeline/app.py"
      to: "features.compute_joint_angles"
      via: "학생·정은지 same-pose 프레임 per-joint 각도 델타 산출"
      pattern: "compute_joint_angles"
    - from: "backend/functions/pipeline/app.py"
      to: "body_normalization 프로파일"
      via: "몸-상대 칸/층 비율 텍스트 산출 (정은지 reach=100%=N칸)"
      pattern: "BodyNormalizationProfile|bodyNormalizationProfile"
    - from: "backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py"
      to: "_COMPARISON_PROMPT"
      via: "원인(힘/폴밀착) ~로 보임 가설 추론 지시 추가 (generic)"
      pattern: "_COMPARISON_PROMPT"
---

<objective>
결함 출력에 **기준선 정량화 레이어**와 **증상→root cause 묶음 코칭**을 더한다. (1) 정량화:
관절각은 스케일 무관 3D 각도로 **직접 수치**("무릎 145° vs 정은지 178°, 33° 더 굽음"),
거리는 절대 cm 금지·**몸-상대 칸/층 텍스트**("정은지 3칸, 너 2칸 ⅔" — 정은지 도달치를
100%=N칸으로 두고 학생을 비율 환산). (2) 원인: Gemini 가 결함군을 보고 가능한 root
cause(힘 부족/폴 밀착 풀림)를 기존 dual-coach "원인" 섹션에서 **"~로 보임" 가설**로 묶는다.

Purpose: "수치는 보조, 원인이 핵심"(PROJECT core value) — 사용자가 "무엇을 어디로 얼마나
고칠지" 직관화. 룰기반 매핑은 brittle → Gemini 추론(D-04). 단정 금지·사람-라벨 ground
truth 금지(D-06).

Output: per-joint 각도 델타(순수) + 몸-상대 칸/층 텍스트 + build_schema DESCRIPTIVE 필드 +
비교 프롬프트 원인 가설 섹션 + applied 경로 audit attach. **score 필드 영구 부재 유지.**
**시각 화살표/칸 오버레이(v1.1)는 본 phase 에서 구현 금지 — 텍스트+각도만.**
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kimtaesung/Dev/SunityMotion/.planning/phases/23-mode-1-recall-still-frame-veto-dtw-key-frame/23-CONTEXT.md
@/Users/kimtaesung/Dev/SunityMotion/.planning/phases/23-mode-1-recall-still-frame-veto-dtw-key-frame/23-PATTERNS.md
@/Users/kimtaesung/Dev/SunityMotion/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: per-joint 각도 델타 (학생 vs DTW-매칭 정은지) — 스케일 무관 직접 수치 (D-02 각도)</name>
  <files>backend/shared/python/sunity_shared/analysis/features.py, backend/shared/python/sunity_shared/analysis/dimensions.py, backend/tests/test_features.py</files>
  <read_first>
    - backend/shared/python/sunity_shared/analysis/features.py (`compute_joint_angles` 36-55 — (T,NUM_JOINTS) deg, 스케일 무관)
    - backend/shared/python/sunity_shared/analysis/dimensions.py (`JOINT_ANGLES`, `line_score`, `line_deficits_by_joint` ~320, `_select_window`+median 패턴)
    - backend/shared/python/sunity_shared/analysis/skeleton.py (`JOINT_ANGLES` 39-48 — 8 관절 left/right × elbow/shoulder/hip/knee, `JOINT_KEYS` 50)
    - 23-PATTERNS.md "features.py + dimensions.py" 분석 (cm 절대거리 금지)
  </read_first>
  <behavior>
    - Test: 학생 각도 (T_u, J) + DTW-매칭 정은지 각도 윈도우 (T_r, J) 입력 → 관절별 (student_deg, reference_deg, delta_deg, direction) 산출. direction = '더 굽음'/'더 펴짐' (delta 부호). 예: 무릎 student 145 / ref 178 → delta 33, '더 굽음'.
    - Test: NaN/finite 가드 — 한쪽이 NaN 인 관절은 결과에서 제외 (median + isfinite, RTMW jitter 흡수).
    - Test: 출력은 절대 cm/px 거리 0 — 각도(도)만 (cm/px 키 부재 assert).
    - Test: DTW path-정렬 median 사용 (whole-clip mean 아님 — 기존 `_angles_to_dtw_median_dicts`/`per_joint_deviation` median 선례와 정합).
  </behavior>
  <action>
    `features.py`(또는 `dimensions.py` — 8 JOINT_ANGLES 소비처에 가까운 곳)에 순수 함수 신설: 학생 각도 행렬과 DTW-매칭된 정은지 same-pose 윈도우 각도 행렬을 받아 관절별 `{joint, student_deg, reference_deg, delta_deg, direction}` 리스트를 반환. delta = student - reference, direction = delta<0 '더 굽음' / delta>0 '더 펴짐' (도메인: 작은 각=굽음). median 집계(`per_joint_deviation`/`_select_window` median 선례 재사용, jitter 흡수) + `np.isfinite` 가드(NaN 관절 제외). **절대 cm/px 거리 산출 금지(D-02 — 단일 카메라 스케일 모호, 140/150 깨짐)**. numpy-only 순수(어댑터/네트워크 무관). 신규 함수는 8 `skeleton.JOINT_ANGLES` 관절만 다룬다(머리/그립은 vision-only — Gemini differences 가 담당). 한국어 docstring + D-02 인용.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && python -m pytest tests/test_features.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - 신규 함수가 관절별 (student_deg, reference_deg, delta_deg, direction) 을 반환한다 (테스트 assert).
    - 출력에 cm/px/거리 키가 없다 (`grep -niE "cm|px|distance" 신규 함수 본문` 에 절대거리 산출 0 — 각도만).
    - NaN 관절이 결과에서 제외된다 (테스트 assert).
    - 신규 함수에 boto3/google.genai/requests import 0 (순수 — `grep`).
    - pytest tests/test_features.py 통과.
  </acceptance_criteria>
  <done>학생 vs DTW-매칭 정은지 per-joint 각도 델타가 스케일 무관 직접 수치로 산출되고, 절대 거리는 산출하지 않는다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: build_schema DESCRIPTIVE 정량화 필드 + 비교 프롬프트 원인 가설 (D-02 칸/층 + D-04 root cause)</name>
  <files>backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py, backend/shared/python/sunity_shared/analysis/vision_veto.py, backend/tests/test_gemini_vision_scorer.py</files>
  <read_first>
    - backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py (`build_schema` 131-196, `VisionVerdict` 109-125, `_COMPARISON_PROMPT` 246+, `_SCORE_PATTERN` 97, `PROMPT_VERSION`/`SCHEMA_VERSION` 70-71, `_parse_verdict` 831+)
    - backend/shared/python/sunity_shared/analysis/vision_veto.py (verdict.differences 소비 — `fault_joint_deficits_from_differences` 218-238)
    - 23-PATTERNS.md "no-score schema" + "Objectivity hard gate" 분석
  </read_first>
  <behavior>
    - Test: `build_schema()` 에 몸-상대 notch 텍스트 등 DESCRIPTIVE 필드가 추가되되 score/overall/rating/점수/100 키는 여전히 0 (객관성 grep gate).
    - Test: `VisionVerdict` dataclass 에 score 필드 부재 유지 (내성검사).
    - Test: 원인 가설 텍스트가 단정형이 아니라 "~로 보임" 가설형 — 프롬프트에 단정 금지 지시 존재 (프롬프트 문자열 assert).
    - Test: 점수 누출 텍스트("85점"/"89%")는 여전히 `_SCORE_PATTERN` 으로 폐기 (still 경로 포함).
  </behavior>
  <action>
    `build_schema()` 에 **DESCRIPTIVE 전용** 정량화 필드 추가: differences[] 항목에 몸-상대 거리 표현용 free-text 필드(예 `notch_note` — "정은지 3칸, 너 2칸 ⅔" 류)와 원인 가설용 free-text 필드(예 `root_cause_hypothesis` — "힘 부족으로 보임" 류). **normalized 0-100 score/overall/rating/숫자점수 필드 신설 절대 금지(D-06 객관성 hard gate)** — string/observational number 만. `_COMPARISON_PROMPT` 에 (a) 거리 표현은 절대 cm/m 금지·정은지 도달치를 기준(N칸)으로 한 몸-상대 비율 텍스트로, (b) 결함군을 보고 가능한 root cause(힘/폴 밀착)를 **"~로 보임" 가설형**(단정·사람라벨 ground truth 금지, D-04)으로 기술하라는 generic 지시 추가 — **특정 동작명/기대답/curve-fit 금지(D-06)**. 기준선은 동작별(kip-up=바닥, 공중=폴 수직/엉덩이 라인) 맥락을 generic 하게 언급. 프롬프트/스키마 변경 → `PROMPT_VERSION`/`SCHEMA_VERSION` bump(23-01 에서 이미 bump 했으면 추가 bump — stale 무효화). `VisionVerdict` 와 `_parse_verdict` 가 신규 필드를 DESCRIPTIVE 로 통과시키되 score 필드는 추가 안 함. `_SCORE_PATTERN` leak guard 유지(누출 폐기).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && python -m pytest tests/test_gemini_vision_scorer.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `build_schema()` 반환 dict 에 score/overall/rating/"점수"/"100" 속성이 없다 (테스트: 재귀 grep gate 0).
    - `VisionVerdict` 에 score 류 필드 부재 (dataclass 필드 내성검사 테스트 통과).
    - `_COMPARISON_PROMPT` 에 "~로 보임" 가설 + 몸-상대 칸/N칸 지시 문자열 존재, 절대 cm/m 금지 명시 (`grep`).
    - `_COMPARISON_PROMPT` 에 특정 동작명 하드코딩 0 (kip-up 등 known-answer 유도 부재 — generic, D-06).
    - `PROMPT_VERSION`/`SCHEMA_VERSION` 이 23-01 대비 추가 bump 되었거나 23-01 bump 가 본 변경을 포함 (캐시 무효화 보장).
    - pytest tests/test_gemini_vision_scorer.py 통과.
  </acceptance_criteria>
  <done>build_schema 에 DESCRIPTIVE 정량화·원인 필드가 score-free 로 추가되고, 비교 프롬프트가 몸-상대 칸/층 + "~로 보임" 원인 가설을 generic 하게 유도한다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 정량화 audit attach + 몸-상대 칸/층 산출 wiring (D-02) + 3-way lockstep</name>
  <files>backend/functions/pipeline/app.py, backend/shared/python/sunity_shared/models.py, app/src/types/analysis.ts, docs/contract.md, backend/tests/test_pipeline_vision_gate.py</files>
  <read_first>
    - backend/functions/pipeline/app.py (`_apply_vision_veto` applied 경로 audit 1727-1760, Mode1 body profile fetch 2360-2408 `compare_body_profiles`/`BodyNormalizationProfile`, `reference_dtw_match` 2411-2414)
    - backend/shared/python/sunity_shared/analysis/body_normalization.py + body_normalization_measurer.py (arm/leg/torso/height ratio 프로파일 — 학생·정은지 둘 다 저장)
    - app/src/types/analysis.ts (`VisionVeto` applied 분기 350-351 — primaryFault/faultJoints/faultJointDeficits 선례)
    - docs/contract.md §4 (visionVeto audit 필드 정의)
    - backend/shared/python/sunity_shared/models.py (vision veto audit 주석 83-88)
  </read_first>
  <behavior>
    - Test: applied 경로에서 per-joint 각도 델타 텍스트(Task 1 산출)와 몸-상대 칸/층 텍스트가 visionVeto audit 에 DESCRIPTIVE 필드로 동반된다 (score 아님).
    - Test: 몸-상대 칸/층 = 정은지 도달치를 100%(N칸)로 두고 학생을 비율 환산 — body_normalization 프로파일(arm/leg/torso ratio) 기반, 절대 cm 0.
    - Test: not_applicable/보류/disabled 경로에는 정량화 필드가 동반되지 않는다 (applied 시에만 — discriminated union).
    - Test: 신규 audit 필드가 3-way(TS union + models.py 주석/상수 + contract.md) 정합.
  </behavior>
  <action>
    `_apply_vision_veto` 의 applied 경로(`vision_veto_audit` 조립, 1746-1760)에 정량화 DESCRIPTIVE 필드를 추가한다: (a) Task 1 의 per-joint 각도 델타를 텍스트/구조로(예 `angleDeltas` — 각 항목 student/reference/delta/direction, **점수 아님**), (b) 몸-상대 칸/층 텍스트(예 `bodyRelativeNotches`) — Mode1 의 `body_normalization` 프로파일(학생·정은지 arm/leg/torso/height ratio, app.py 2360-2408 fetch)로 정은지 도달치=100%=N칸 기준 학생 비율 환산. **절대 cm/m 산출 금지(D-02)**. 기존 `primaryFault`/`faultJoints`/`faultJointDeficits` 와 동일하게 **applied 시에만** 동반(discriminated — not_applicable/보류/disabled 분기엔 부재). 3-way lockstep 단일 commit: `app/src/types/analysis.ts` `VisionVeto` applied 분기에 신규 optional 필드 추가(score 타입 금지 — string/number DESCRIPTIVE), `models.py` vision veto audit 주석/상수 갱신, `docs/contract.md` §4 갱신. Firestore nested-array 금지 — 텍스트/flat 구조만. graceful(산출 실패 시 필드 생략, 분석 흐름 유지). cap 수식·downward-only 불변. D-02 결정 id 한국어 주석 인용.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && python -m pytest tests/test_pipeline_vision_gate.py -x -q && cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - applied 경로 audit 에 각도 델타 + 몸-상대 칸/층 DESCRIPTIVE 필드가 동반된다 (테스트 assert).
    - 신규 필드에 normalized score/0-100 타입 0 — TS 에서 string/number(deg/ratio) DESCRIPTIVE (`npm run typecheck` + union 검토).
    - not_applicable/low_alignment_confidence/disabled 분기에 정량화 필드 부재 (discriminated union — `never` 강제).
    - 몸-상대 칸/층이 body_normalization 프로파일을 사용하고 절대 cm 0 (`grep -niE "cm|미터|meter" 신규 wiring` 0).
    - 신규 audit 필드 3-way 정합 (`grep` TS + models.py + contract.md 모두 존재).
    - 시각 오버레이(화살표/칸 그리기) 코드 부재 — fault_zoom.py 미변경 (v1.1 deferred, `git diff --stat` 에 fault_zoom.py 0).
    - pytest + typecheck 통과.
  </acceptance_criteria>
  <done>정량화(각도 직접 + 몸-상대 칸/층)와 원인 가설이 applied 경로 audit 에 DESCRIPTIVE 로 attach 되고 3-way contract 정합하며, 시각 오버레이는 구현하지 않는다(v1.1).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gemini 응답 → 정량화 필드 | 외부 모델 자유텍스트(칸/층·원인 가설)가 DESCRIPTIVE 필드로 저장됨 |
| pipeline → Firestore audit | 신규 정량화 필드가 analysis doc 에 기록됨 (score 누출 위험) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-06 | Tampering | 신규 정량화 필드 → score 누출 | mitigate | build_schema/VisionVerdict/visionVeto audit 에 normalized score 필드 영구 부재(객관성 hard gate). `_SCORE_PATTERN` leak guard 유지. TS union 이 DESCRIPTIVE 타입만 허용 |
| T-23-07 | Information Disclosure | 정량화/원인 텍스트에 PII/점수 | mitigate | 텍스트는 관절각·몸-상대 비율·동작 관찰만 — PII 0, 사람-라벨 원인 ground truth 0(D-04 "~로 보임" 가설형) |
| T-23-08 | Availability | 각도 델타/칸층 산출 실패 → crash | mitigate | graceful — 산출 실패 시 필드 생략, applied 경로/v1 점수 생존(Pitfall 5) |
| T-23-09 | Tampering | curve-fit 프롬프트로 일반화 붕괴 | mitigate | 프롬프트 generic 유지(특정 동작명·기대답 0). grep gate 로 known-answer 하드코딩 0 검증(D-06) |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/test_features.py tests/test_gemini_vision_scorer.py tests/test_pipeline_vision_gate.py -q` 회귀 0.
- `cd app && npm run typecheck` clean.
- 객관성 grep gate: build_schema/VisionVerdict/visionVeto audit 에 score/0-100 필드 0.
- `git diff --stat` 에 `fault_zoom.py` 0 (v1.1 시각 오버레이 미구현 확인).
- `_COMPARISON_PROMPT` 에 특정 동작명 하드코딩 0 (generic — D-06).
</verification>

<success_criteria>
- 결함 출력에 기준선 정량화(각도 직접 / 거리 몸-상대 칸·층)가 표시됨 (Success #4 — 시각 오버레이 제외 텍스트+각도).
- 코칭이 증상을 root cause(힘/폴밀착)에 "~로 보임" 가설로 묶음 (Success #5).
- VisionVerdict/스키마 score-free 유지 (객관성 D-06).
</success_criteria>

<output>
Create `.planning/phases/23-mode-1-recall-still-frame-veto-dtw-key-frame/23-02-SUMMARY.md` when done
</output>
