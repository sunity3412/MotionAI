---
phase: 23-mode-1-recall-still-frame-veto-dtw-key-frame
plan: 03
type: execute
wave: 3
depends_on: ["23-01", "23-02"]
files_modified:
  - backend/research/spikes/eval_stillframe_veto.py
  - backend/research/spikes/reports/eval_stillframe_veto_phase23.json
autonomous: false
requirements: [VETO-06]

must_haves:
  truths:
    - "kip-up known-answer 에서 상체 결함(좌/우팔+고개) recall 이 whole-video baseline 대비 복구됨이 Pod 에서 실측·박제된다 (스파이크 effect size 재현, Success #1)"
    - "깨끗 프레임/정타에서 위양성이 증가하지 않음(none 유지) + 같은 입력=같은 verdict(결정론)이 실측된다 (Success #2)"
    - "시작점/템포가 정은지와 다른 정상 Mode 1 케이스에서 프레임선별이 거짓결함을 만들지 않음(보류 status)이 실측된다 (Success #3)"
    - "recall 은 프로젝트 자체 라벨셋으로 측정되고 프롬프트는 generic(curve-fit/known-answer 유도 0)임이 박제된다 (D-06 일반화 hard gate)"
  artifacts:
    - path: "backend/research/spikes/eval_stillframe_veto.py"
      provides: "still-frame veto eval harness (Pod-bound, 순차 실행 — 동시성 비안전)"
      contains: "def main"
    - path: "backend/research/spikes/reports/eval_stillframe_veto_phase23.json"
      provides: "Pod 실측 결과 박제 (recall up / 위양성 0 / 결정론 / 정렬-약 케이스 보류)"
      contains: "kip-up"
  key_links:
    - from: "backend/research/spikes/eval_stillframe_veto.py"
      to: "gemini_vision_scorer.assess_fault_severity"
      via: "still-pair 경로 eval 호출 (23-01/23-02 구현 사용)"
      pattern: "assess_fault_severity"
    - from: "backend/research/spikes/eval_stillframe_veto.py"
      to: "spike 기준 산출물"
      via: "kip-up known-answer + whole-video baseline 비교"
      pattern: "spike_kipup_granularity_result|spike_vision_grounding_kip-up"
---

<objective>
23-01/23-02 의 still-frame veto 구현을 **Pod GPU 에서 실측**해 (1) kip-up 상체 결함 recall 이
whole-video baseline 대비 복구됨(스파이크 effect size 재현), (2) 깨끗 프레임/정타 위양성
비증가(특이도 보존) + 결정론, (3) 시작점/템포 상이 케이스에서 거짓결함 비생성(보류)을
증명·박제한다. recall 은 **프로젝트 자체 라벨셋**으로 측정하고 프롬프트는 generic 유지
(known-answer 유도/curve-fit 금지, D-06).

Purpose: 코드가 "동작" 하는 것과 "정확도 게이트를 통과" 하는 것은 다르다 — 핵심 가치(분석
정확도)는 Pod 실측으로만 검증됨([[pipeline-not-concurrency-safe-eval-serial]]). 구현(23-01/02)
은 Pod-free 였으나 본 plan 은 GPU·라이브 LLM·실 영상이 필수.

Output: eval harness + Pod 실측 결과 JSON 박제. 게이트 = recall up + 위양성 0 + 결정론 + 정렬-약
보류.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kimtaesung/Dev/SunityMotion/.planning/phases/23-mode-1-recall-still-frame-veto-dtw-key-frame/23-CONTEXT.md
@/Users/kimtaesung/Dev/SunityMotion/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: still-frame veto eval harness (Pod-bound, 순차)</name>
  <files>backend/research/spikes/eval_stillframe_veto.py</files>
  <read_first>
    - backend/research/spikes/reports/spike_vision_grounding_kip-up.json (whole-video baseline — 상체 0)
    - backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py (`assess_fault_severity` still-pair 경로 — 23-01/02 구현)
    - backend/functions/pipeline/app.py (`_apply_vision_veto` still-pair + low_alignment_confidence — 23-01)
    - 기존 spike/eval 스크립트 1개 (`ls backend/research/spikes/*.py` — 가장 가까운 harness 를 구조 analog 으로 복사: CLI args / 순차 루프 / JSON 결과 dump)
    - MEMORY [[pipeline-not-concurrency-safe-eval-serial]] (eval 반드시 순차)
  </read_first>
  <action>
    Pod 에서 실행할 eval harness 신설: kip-up known-answer + 정타/깨끗 케이스 + 시작점/템포 상이 케이스(정상 Mode 1)를 **순차**(동시 호출 금지 — 결과 오염 [[pipeline-not-concurrency-safe-eval-serial]])로 돌려, 각 케이스에 대해 (a) still-pair veto verdict 의 differences/부위/severity, (b) whole-video baseline 대비 상체 결함 recall(좌/우팔+고개 잡힘 여부), (c) 같은 입력 2회 호출 결과 동일성(결정론), (d) 정렬-약 케이스 status(`low_alignment_confidence` 보류 여부)를 수집해 JSON 으로 dump. 비교 기준 산출물 경로: `/workspace/spike_kipup_granularity_result.json`, `/workspace/spike_perframe_upper_result.json`(Pod), `backend/research/spikes/reports/spike_vision_grounding_kip-up.json`(repo). **프롬프트는 generic 유지** — harness 가 known-answer 에 맞춘 임계/프롬프트를 주입하지 않는다(D-06). recall 라벨은 프로젝트 자체 라벨셋(메모리 [[jeongeunji-success-fail-pair-dataset]] 정성 참고만, 점수 라벨 금지). CLI: 입력 영상 경로/reference id, 결과 출력 경로 인자. Gemini 키는 SSM/env(로그 금지, T-20-06).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && python -c "import ast,sys; ast.parse(open('research/spikes/eval_stillframe_veto.py').read()); print('parse ok')" && grep -nc "concurrent\|ThreadPool\|asyncio.gather" research/spikes/eval_stillframe_veto.py | grep -q '^0$' && echo "serial ok"</automated>
  </verify>
  <acceptance_criteria>
    - `eval_stillframe_veto.py` 가 `main`/CLI entry 를 가진다 (`grep -n "def main\|__main__"`).
    - 동시 실행 primitive 부재 (concurrent/ThreadPool/asyncio.gather 0 — 순차 강제).
    - harness 가 still-pair `assess_fault_severity` 경로를 호출하고 whole-video baseline JSON 을 비교 입력으로 읽는다 (`grep`).
    - known-answer 에 맞춘 임계/프롬프트 주입 0 (generic — D-06, 코드 리뷰 + grep "kip-up" 가 라벨/비교 용도일 뿐 프롬프트 주입 아님).
    - Gemini 키를 로그하지 않는다 (`grep -niE "log.*key|print.*key" research/spikes/eval_stillframe_veto.py` 에 키 노출 0).
    - python ast parse 통과.
  </acceptance_criteria>
  <done>순차 eval harness 가 still-pair veto recall/위양성/결정론/정렬-약 보류를 수집·dump 하고, 프롬프트는 generic 이며 키를 로그하지 않는다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: [POD terminal] Pod 실측 — recall 재현 + 위양성 0 + 결정론 + 정렬-약 보류</name>
  <action>
    [POD terminal] Claude 가 Pod SSH 로 `eval_stillframe_veto.py` 를 순차 실행해 23-01/02
    still-frame veto 구현의 정확도 게이트를 실측하고 결과 JSON 을 repo 에 박제한다. 구현은
    Pod-free 였으나 라이브 LLM·실 영상·GPU 가 eval 에 필수 — [[pod-ops-claude-runs]] 에 따라
    Claude 가 Pod 실행 주체, belle 는 도메인 판정·production 승인. 본 task 는 자동 실행 후
    belle 의 4 게이트 도메인 판정을 받는 blocking-human checkpoint 다.
  </action>
  <what-built>
    Task 1 의 `eval_stillframe_veto.py` 를 Pod GPU 에서 실행해 23-01/02 still-frame veto
    구현의 정확도 게이트를 실측한다. (구현은 Pod-free 였으나 라이브 LLM·실 영상·GPU 가
    eval 에 필수 — [[pod-ops-claude-runs]] 에 따라 Claude 가 Pod SSH/실행 주체, belle 는
    도메인 판정·production 승인.)
  </what-built>
  <how-to-verify>
    [POD terminal] (Claude 가 Pod SSH 로 실행, belle 는 결과 도메인 판정):
    1. Pod 살아있는지 확인 + `git pull` (로컬 23-01/02 commit push 선행 — [[gsd-pod-work-push-first]]).
    2. Gemini 키 충전 확인([[gemini-credits-depleted-2026-06-20]] — sweep 전 크레딧 잔량).
    3. `python backend/research/spikes/eval_stillframe_veto.py` 순차 실행 → `eval_stillframe_veto_phase23.json` 산출.
    4. 게이트 판정:
       - (recall, 정량 고정 — W2) 스파이크 effect size 를 그대로 재현해야 한다: whole-video baseline 상체 recall = **0**(다리만), still-pair 경로 상체 recall ⊇ **{왼팔, 오른팔, 고개·목}**(spike_perframe_upper_result.json 의 알려진 결과). 즉 알려진 상체 3종(좌팔/우팔/고개)이 모두 differences union 에 잡혀야 통과 — 정성("잡힘 여부")이 아니라 이 집합을 고정 기준으로 판정.
       - (특이도) 정타/깨끗 케이스 = none 유지 (위양성 0, 비증가).
       - (결정론) 같은 입력 2회 → 동일 verdict.
       - (정렬) 시작점/템포 상이 케이스 = 거짓결함 0, status=low_alignment_confidence 보류(해당 시).
    5. 결과 JSON 을 repo `backend/research/spikes/reports/eval_stillframe_veto_phase23.json` 로 커밋.
  </how-to-verify>
  <resume-signal>belle 가 "통과"(4 게이트 모두) 또는 회귀 항목을 보고하면 재개. recall 미복구/위양성 증가/결정론 깨짐/거짓결함 발생 시 → 23-01 또는 23-02 gap 으로 환류.</resume-signal>
  <acceptance_criteria>
    - `eval_stillframe_veto_phase23.json` 이 repo reports 디렉터리에 존재하고 kip-up·정타·정렬-약 케이스 결과를 포함한다.
    - kip-up 상체 recall 이 whole-video baseline(상체=0) 대비 복구됨이 JSON 에 기록 — 알려진 상체 집합 **{왼팔, 오른팔, 고개·목}** 가 모두 union 에 잡힘(spike_perframe_upper_result.json 기준 고정, W2). recall-presence 임계는 측정값이며 사람 점수 라벨이 아님([[analysis-objectivity-no-human-scores]]).
    - 정타/깨끗 케이스 severity=none (위양성 0).
    - 결정론: 동일 입력 반복 verdict 동일.
    - 정렬-약 케이스: 거짓결함 0 (보류 또는 none).
  </acceptance_criteria>
  <done>Pod 실측 4 게이트(recall 복구 / 위양성 0 / 결정론 / 정렬-약 보류)를 belle 가 판정 통과하고 결과 JSON 이 박제된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval harness → Gemini API | 라이브 LLM 호출 (크레딧 소모, 키 노출 위험) |
| Pod → repo (결과 박제) | 실측 JSON 이 repo 에 commit 됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-10 | Information Disclosure | eval harness Gemini 키 | mitigate | 키 SSM/env, 로그 금지(T-20-06). 결과 JSON 에 키/PII 0 |
| T-23-11 | Tampering | 동시 호출 결과 오염 | mitigate | harness 순차 강제([[pipeline-not-concurrency-safe-eval-serial]]) — concurrent primitive 0 |
| T-23-12 | Tampering | curve-fit eval (일반화 위반) | mitigate | 프롬프트 generic, recall 자체 라벨셋 측정, known-answer 임계 주입 0(D-06). belle 도메인 판정 게이트 |
| T-23-DOS | Availability | Gemini 크레딧 고갈 | accept | sweep 전 크레딧 확인([[gemini-credits-depleted-2026-06-20]]) — 운영 체크, 코드 위협 아님 |
</threat_model>

<verification>
- `eval_stillframe_veto.py` ast parse + 순차(동시 primitive 0) 검증.
- Pod 실측 4 게이트 belle 판정 통과 (Task 2 checkpoint).
- `eval_stillframe_veto_phase23.json` repo 박제.
</verification>

<success_criteria>
- kip-up 상체 결함 recall 복구 실측 (Success #1 effect size 재현).
- 위양성 0 유지 + 결정론 실측 (Success #2).
- 정렬-약 케이스 거짓결함 비생성 실측 (Success #3 검증 task).
- recall 자체 라벨셋 측정 + generic 프롬프트 (D-06 일반화 hard gate).
</success_criteria>

<output>
Create `.planning/phases/23-mode-1-recall-still-frame-veto-dtw-key-frame/23-03-SUMMARY.md` when done
</output>
