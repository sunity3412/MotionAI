---
phase: 23-mode-1-recall-still-frame-veto-dtw-key-frame
plan: 03
type: execute
wave: 3
depends_on: ["23-01", "23-02"]
files_modified:
  - backend/research/spikes/eval_stillframe_veto.py
  - backend/research/spikes/reports/eval_stillframe_veto_phase23.json
autonomous: false
requirements: [VETO-06]

must_haves:
  truths:
    - "eval 이 어댑터 직접호출이 아니라 FULL production 경로(_apply_vision_veto → 프레임선별 → 글로벌+로컬 DTW 게이팅 → 캐시)를 통과해 측정한다 (D-08 H5)"
    - "kip-up known-answer 상체 결함(좌/우팔+고개) recall 이 whole-video baseline 대비 복구됨이 Pod 에서 실측·박제된다 — baseline 은 동일 모델/버전으로 재실행한 apples-to-apples (Success #1, D-08 H5)"
    - "케이스 매트릭스가 확장된다: elite clean / imperfect clean / occluded / tempo-shifted / spinning / known-fault — 1 kip-up + 1 clean 으로는 위양성 비증가를 주장할 수 없음 (D-08 H5)"
    - "cold-cache 와 warm-cache 결정론이 분리 측정된다 (캐시 hit 로 결정론이 거짓 증명되지 않도록 cold-cache 반복 호출) + cost/latency 게이트 (D-08 H5)"
    - "결과 JSON 에 machine-checkable pass/fail 필드(call_count, upload_count, duration_ms, cache hit/miss, recall set, false_positive_count) 가 박제된다 (D-08 H5)"
    - "recall 은 프로젝트 자체 라벨셋으로 측정되고 프롬프트는 generic(curve-fit/known-answer 유도 0) (D-06 일반화 hard gate)"
  artifacts:
    - path: "backend/research/spikes/eval_stillframe_veto.py"
      provides: "still-frame veto eval harness (Pod-bound, 순차 실행, FULL production 경로 + direct-adapter 둘 다 + 동일-모델 baseline 재실행 + cold/warm 결정론 + cost/latency + machine-checkable 필드)"
      contains: "def main"
    - path: "backend/research/spikes/reports/eval_stillframe_veto_phase23.json"
      provides: "Pod 실측 결과 박제 (recall set / false_positive_count / call_count / upload_count / duration_ms / cache hit-miss / cold-warm 결정론 / 정렬-약 보류)"
      contains: "kip-up"
  key_links:
    - from: "backend/research/spikes/eval_stillframe_veto.py"
      to: "pipeline._apply_vision_veto"
      via: "FULL production 경로 통과 측정 (프레임선별·DTW 게이팅·캐시 포함 — 어댑터 직접호출 아님, H5)"
      pattern: "_apply_vision_veto"
    - from: "backend/research/spikes/eval_stillframe_veto.py"
      to: "동일-모델 whole-video baseline 재실행"
      via: "apples-to-apples (옛 baseline JSON 비교가 아니라 같은 모델/버전으로 재실행)"
      pattern: "whole|baseline"
---

<objective>
23-01/23-02 의 still-frame veto 구현을 **Pod GPU 에서 실측**하되, codex HIGH(H5)에 따라
**어댑터 직접호출이 아니라 FULL production 경로**(`_apply_vision_veto` → 프레임선별 →
글로벌+로컬 DTW 게이팅 → 캐시)를 통과해 측정한다. (1) kip-up 상체 결함 recall 이 whole-video
baseline 대비 복구됨(스파이크 effect size 재현) — baseline 은 **동일 모델/버전으로 재실행**한
apples-to-apples, (2) 확장 케이스 매트릭스(elite clean / imperfect clean / occluded /
tempo-shifted / spinning / known-fault)에서 위양성 비증가(특이도 보존) + cold/warm 분리
결정론, (3) 시작점/템포 상이 케이스에서 거짓결함 비생성(보류), (4) cost/latency 게이트 +
machine-checkable pass/fail 필드를 증명·박제한다. recall 은 **프로젝트 자체 라벨셋**으로
측정하고 프롬프트는 generic 유지(known-answer 유도/curve-fit 금지, D-06).

**스코프 (D-07): Mode 1 집중.** Mode 3 정량화/정렬 eval 은 defer(Backlog B-15a).

Purpose: 코드가 "동작" 하는 것과 "정확도 게이트를 통과" 하는 것은 다르다 — 핵심 가치(분석
정확도)는 Pod 실측으로만 검증됨([[pipeline-not-concurrency-safe-eval-serial]]). 그리고 스파이크
재현이 아니라 **실제 production 경로**가 같은 결과를 내는지가 게이트(H5). 구현(23-01/02)은
Pod-free 였으나 본 plan 은 GPU·라이브 LLM·실 영상이 필수.

Output: eval harness(FULL production 경로 + 동일-모델 baseline 재실행 + 확장 매트릭스 +
cold/warm 결정론 + cost/latency + machine-checkable 필드) + Pod 실측 결과 JSON 박제.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kimtaesung/Dev/SunityMotion/.planning/phases/23-mode-1-recall-still-frame-veto-dtw-key-frame/23-CONTEXT.md
@/Users/kimtaesung/Dev/SunityMotion/.planning/phases/23-mode-1-recall-still-frame-veto-dtw-key-frame/23-REVIEWS.md
@/Users/kimtaesung/Dev/SunityMotion/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: still-frame veto eval harness — FULL production 경로 + 동일-모델 baseline 재실행 + 확장 매트릭스 + cold/warm + machine-checkable (Pod-bound, 순차, H5)</name>
  <files>backend/research/spikes/eval_stillframe_veto.py</files>
  <read_first>
    - backend/research/spikes/reports/spike_vision_grounding_kip-up.json (whole-video baseline — 상체 0, 단 옛 모델 — H5: 동일 모델로 재실행 필요)
    - backend/functions/pipeline/app.py (`_apply_vision_veto` still-pair + 글로벌+로컬 DTW 게이팅 + low_alignment_confidence — 23-01. **eval 은 이 함수를 통과해야 함**)
    - backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py (`assess_fault_severity` still-pair + VisionVetoCache cold/warm + MAX_VETO_CALLS/UPLOADS/WALL_S — 23-01/02 구현)
    - 기존 spike/eval 스크립트 1개 (`ls backend/research/spikes/*.py` — 가장 가까운 harness 를 구조 analog 으로 복사: CLI args / 순차 루프 / JSON 결과 dump)
    - MEMORY [[pipeline-not-concurrency-safe-eval-serial]] (eval 반드시 순차) + [[jeongeunji-success-fail-pair-dataset]] (확장 매트릭스 영상 소스 — 정성 참고만, 점수 라벨 금지)
    - 23-REVIEWS.md MEDIUM "23-03 eval may bypass the production path" 전문 + Suggestions(eval telemetry/case matrix/cold-warm/re-baseline)
  </read_first>
  <action>
    Pod 에서 실행할 eval harness 신설. **순차**(동시 호출 금지 — 결과 오염 [[pipeline-not-concurrency-safe-eval-serial]]).
    **(A) FULL production 경로(H5):** eval 은 `gemini_vision_scorer.assess_fault_severity` 어댑터를 직접 호출하지 않고 **`pipeline._apply_vision_veto` 전체 경로**(프레임선별 → 글로벌+로컬 DTW 게이팅 → 캐시 → 어댑터)를 통과해 측정한다. (보조로 direct-adapter 경로도 별도 측정해 두 경로 비교 가능 — 단 게이트 판정은 production 경로 기준.)
    **(B) 동일-모델 baseline 재실행(H5):** whole-video baseline 을 옛 JSON 비교가 아니라 **현재와 동일한 모델/버전/프롬프트로 재실행**해 apples-to-apples 비교(옛 `spike_vision_grounding_kip-up.json` 은 참고만). still-pair recall ⊖ whole-video recall 을 같은 모델로 측정.
    **(C) 확장 케이스 매트릭스(H5):** elite clean / imperfect clean / occluded / tempo-shifted / spinning / known-fault(kip-up 등) 케이스를 포함 — 1 kip-up + 1 clean 으로는 위양성 비증가 주장 불가. 각 케이스 라벨은 프로젝트 자체 라벨셋([[jeongeunji-success-fail-pair-dataset]] 정성 참고만, 점수 라벨 금지).
    **(D) cold/warm 결정론 분리(H5):** 결정론은 캐시 hit 로 거짓 증명될 수 있으므로 — **cold-cache(캐시 비우고) 동일 입력 반복 호출**의 verdict 동일성과 warm-cache hit 동일성을 **분리 측정**·기록.
    **(E) cost/latency 게이트(H5):** 각 케이스의 call_count / upload_count / duration_ms 를 수집하고 23-01 의 MAX_VETO_CALLS/UPLOADS/WALL_S bound 이내인지 판정.
    **(F) machine-checkable pass/fail 필드(H5):** 결과 JSON 에 케이스별 `call_count`, `upload_count`, `duration_ms`, `cache_hit`/`cache_miss`, `recall_set`(잡힌 부위 집합), `false_positive_count`, `determinism_cold`/`determinism_warm`(bool), `alignment_status`(applied/low_alignment_confidence), 그리고 각 게이트 `pass`/`fail` bool 을 박제(사람이 읽어 판단하는 정성 텍스트가 아니라 기계 판정 가능 필드).
    **프롬프트는 generic 유지** — harness 가 known-answer 에 맞춘 임계/프롬프트를 주입하지 않는다(D-06). Gemini 키는 SSM/env(로그 금지, T-20-06). CLI: 입력 영상 경로/reference id, 결과 출력 경로, 케이스 매트릭스 manifest 인자.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && python -c "import ast; ast.parse(open('research/spikes/eval_stillframe_veto.py').read()); print('parse ok')" && grep -Eqc "concurrent|ThreadPool|asyncio.gather" research/spikes/eval_stillframe_veto.py && echo "FAIL concurrent found" || echo "serial ok"</automated>
  </verify>
  <acceptance_criteria>
    - `eval_stillframe_veto.py` 가 `main`/CLI entry 를 가진다 (`grep -n "def main\|__main__"`).
    - 동시 실행 primitive 부재 (`grep -E "concurrent|ThreadPool|asyncio.gather"` 0 — 순차 강제).
    - harness 가 **`_apply_vision_veto` production 경로**를 호출한다 (`grep -n "_apply_vision_veto"` 존재 — 어댑터 직접호출만이 아님, H5).
    - harness 가 whole-video baseline 을 **동일 모델로 재실행**한다 (옛 JSON 단순 비교가 아닌 재실행 호출 존재 — `grep`/코드 리뷰, H5).
    - 케이스 매트릭스가 elite clean/imperfect clean/occluded/tempo-shifted/spinning/known-fault 를 포함한다 (manifest/케이스 열거 — `grep`/코드 리뷰).
    - cold-cache 반복 결정론과 warm-cache 결정론이 분리 측정된다 (cold/warm 분기 코드 존재 — `grep -niE "cold|warm|cache"`).
    - 결과 dump 에 machine-checkable 필드(call_count/upload_count/duration_ms/cache hit-miss/recall_set/false_positive_count/determinism/pass-fail) 가 쓰인다 (`grep -niE "call_count|upload_count|duration_ms|false_positive|recall_set"`).
    - known-answer 에 맞춘 임계/프롬프트 주입 0 (generic — D-06, 코드 리뷰 + `grep "kip-up"` 가 라벨/비교 용도일 뿐 프롬프트 주입 아님).
    - Gemini 키를 로그하지 않는다 (`grep -niE "log.*key|print.*key" research/spikes/eval_stillframe_veto.py` 에 키 노출 0).
    - python ast parse 통과.
  </acceptance_criteria>
  <done>순차 eval harness 가 FULL production 경로(_apply_vision_veto)를 통과하고, 동일-모델 baseline 재실행 + 확장 매트릭스 + cold/warm 분리 결정론 + cost/latency 를 측정해 machine-checkable 필드로 dump 하며, 프롬프트는 generic 이고 키를 로그하지 않는다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: [POD terminal] Pod 실측 — production 경로 recall 재현 + 위양성 0(확장 매트릭스) + cold/warm 결정론 + 정렬-약 보류 + cost/latency</name>
  <action>
    [POD terminal] Claude 가 Pod SSH 로 `eval_stillframe_veto.py` 를 순차 실행해 23-01/02
    still-frame veto 구현의 정확도·자원 게이트를 **FULL production 경로**로 실측하고 결과 JSON 을
    repo 에 박제한다. 구현은 Pod-free 였으나 라이브 LLM·실 영상·GPU 가 eval 에 필수 —
    [[pod-ops-claude-runs]] 에 따라 Claude 가 Pod 실행 주체, belle 는 도메인 판정·production
    승인. 본 task 는 자동 실행 후 belle 의 게이트 도메인 판정을 받는 blocking-human checkpoint 다.
  </action>
  <what-built>
    Task 1 의 `eval_stillframe_veto.py` 를 Pod GPU 에서 실행해 23-01/02 still-frame veto
    구현의 정확도·자원 게이트를 `_apply_vision_veto` production 경로로 실측한다. (구현은
    Pod-free 였으나 라이브 LLM·실 영상·GPU 가 eval 에 필수 — [[pod-ops-claude-runs]] 에 따라
    Claude 가 Pod SSH/실행 주체, belle 는 도메인 판정·production 승인.)
  </what-built>
  <how-to-verify>
    [POD terminal] (Claude 가 Pod SSH 로 실행, belle 는 결과 도메인 판정):
    1. Pod 살아있는지 확인 + `git pull` (로컬 23-01/02 commit push 선행 — [[gsd-pod-work-push-first]]).
    2. Gemini 키 충전 확인([[gemini-credits-depleted-2026-06-20]] — sweep 전 크레딧 잔량).
    3. `python backend/research/spikes/eval_stillframe_veto.py` 순차 실행 → `eval_stillframe_veto_phase23.json` 산출.
    4. 게이트 판정 (machine-checkable 필드 기준 — 정성 아님):
       - (recall, production 경로, 정량 고정 — W2) `_apply_vision_veto` 경로 still-pair recall_set ⊇ **{왼팔, 오른팔, 고개·목}**(spike_perframe_upper_result.json 알려진 결과). 동일-모델 whole-video baseline recall_set 상체 = **∅/다리만**. 즉 같은 모델 비교에서 production 경로가 상체 3종을 모두 잡아야 통과.
       - (특이도, 확장 매트릭스) elite clean / imperfect clean / occluded / spinning / tempo-shifted 케이스 false_positive_count = 0 (위양성 비증가). 1 케이스가 아닌 매트릭스 전체.
       - (결정론 분리) cold-cache 반복 verdict 동일(determinism_cold=true) + warm-cache hit 동일(determinism_warm=true). 캐시 hit 로 거짓 증명 아님.
       - (정렬) tempo-shifted/시작점 상이 케이스 = 거짓결함 0, alignment_status=low_alignment_confidence 보류(해당 시).
       - (cost/latency) 각 케이스 call_count/upload_count/duration_ms 가 MAX_VETO_CALLS/UPLOADS/WALL_S bound 이내.
    5. 결과 JSON 을 repo `backend/research/spikes/reports/eval_stillframe_veto_phase23.json` 로 커밋.
  </how-to-verify>
  <resume-signal>belle 가 "통과"(전 게이트) 또는 회귀 항목을 보고하면 재개. recall 미복구/위양성 증가/결정론 깨짐/거짓결함/bound 초과 시 → 23-01 또는 23-02 gap 으로 환류.</resume-signal>
  <acceptance_criteria>
    - `eval_stillframe_veto_phase23.json` 이 repo reports 디렉터리에 존재하고 확장 매트릭스 전 케이스 결과 + machine-checkable 필드를 포함한다.
    - production 경로(_apply_vision_veto) recall_set 이 알려진 상체 집합 **{왼팔, 오른팔, 고개·목}** 를 포함하고, 동일-모델 whole-video baseline 상체 recall = ∅ (apples-to-apples, W2). recall-presence 는 측정값이며 사람 점수 라벨이 아님([[analysis-objectivity-no-human-scores]]).
    - 확장 매트릭스(elite/imperfect clean/occluded/spinning/tempo-shifted) false_positive_count = 0 (위양성 0).
    - determinism_cold=true 그리고 determinism_warm=true (cold/warm 분리 결정론).
    - 정렬-약 케이스: false positive 0 (보류 또는 none, alignment_status 기록).
    - 각 케이스 call_count/upload_count/duration_ms 가 23-01 bound 이내 (cost/latency 게이트 pass).
  </acceptance_criteria>
  <done>Pod 실측 게이트(production 경로 recall 복구 / 확장 매트릭스 위양성 0 / cold·warm 분리 결정론 / 정렬-약 보류 / cost·latency bound)를 belle 가 machine-checkable 필드 기준으로 판정 통과하고 결과 JSON 이 박제된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval harness → Gemini API | 라이브 LLM 호출 (크레딧 소모, 키 노출 위험) |
| Pod → repo (결과 박제) | 실측 JSON 이 repo 에 commit 됨 |
| eval → production 경로 | eval 이 어댑터 직접호출로 우회하면 게이트가 실제 경로를 검증 못 함 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-10 | Information Disclosure | eval harness Gemini 키 | mitigate | 키 SSM/env, 로그 금지(T-20-06). 결과 JSON 에 키/PII 0 |
| T-23-11 | Tampering | 동시 호출 결과 오염 | mitigate | harness 순차 강제([[pipeline-not-concurrency-safe-eval-serial]]) — concurrent primitive 0 |
| T-23-12 | Tampering | curve-fit eval (일반화 위반) | mitigate | 프롬프트 generic, recall 자체 라벨셋 측정, known-answer 임계 주입 0(D-06). belle 도메인 판정 게이트 |
| T-23-13 | Tampering | eval 이 production 경로 우회 → 거짓 통과 | mitigate | eval 이 `_apply_vision_veto` 전체 경로 통과(H5). 캐시 hit 로 결정론 거짓 증명 방지 위해 cold-cache 반복 분리 측정 |
| T-23-DOS | Availability | Gemini 크레딧 고갈 | accept | sweep 전 크레딧 확인([[gemini-credits-depleted-2026-06-20]]) — 운영 체크, 코드 위협 아님 |
| T-23-SC | Tampering | npm/pip 설치 | mitigate | 본 plan 신규 외부 패키지 설치 없음. 신규 설치 발생 시 slopcheck + blocking human checkpoint |
</threat_model>

<verification>
- `eval_stillframe_veto.py` ast parse + 순차(동시 primitive 0) 검증.
- harness 가 `_apply_vision_veto` production 경로를 통과하고 동일-모델 baseline 을 재실행 (코드 리뷰 + grep).
- Pod 실측 게이트 belle 판정 통과 (Task 2 checkpoint) — machine-checkable 필드 기준.
- `eval_stillframe_veto_phase23.json` repo 박제 (확장 매트릭스 + telemetry 필드).
</verification>

<success_criteria>
- production 경로 kip-up 상체 recall 복구 실측, 동일-모델 baseline 대비 (Success #1 effect size 재현, H5).
- 확장 매트릭스 위양성 0 + cold/warm 분리 결정론 실측 (Success #2/#6).
- 정렬-약 케이스 거짓결함 비생성 실측 (Success #3 검증 task).
- cost/latency 게이트 + machine-checkable pass/fail (Success #6, H5).
- recall 자체 라벨셋 측정 + generic 프롬프트 (D-06 일반화 hard gate).
</success_criteria>

<output>
Create `.planning/phases/23-mode-1-recall-still-frame-veto-dtw-key-frame/23-03-SUMMARY.md` when done
</output>
