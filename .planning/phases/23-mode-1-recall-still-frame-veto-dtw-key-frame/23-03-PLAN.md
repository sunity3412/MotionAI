---
phase: 23-mode-1-recall-still-frame-veto-dtw-key-frame
plan: 03
type: execute
wave: 3
depends_on: ["23-01", "23-02"]
files_modified:
  - backend/research/spikes/eval_stillframe_veto.py
  - backend/research/spikes/assert_stillframe_veto_gate.py
  - backend/research/spikes/freeze_stillframe_veto_manifest.py
  - backend/research/spikes/eval_stillframe_veto_manifest.json
  - backend/research/spikes/eval_stillframe_veto_manifest.lock.json
  - backend/research/spikes/reports/eval_stillframe_veto_phase23.json
  - backend/tests/test_assert_stillframe_veto_gate.py
autonomous: false
requirements: [VETO-06, SCORE-08, TRUST-06]

must_haves:
  truths:
    - "eval 이 어댑터 직접호출이 아니라 FULL production 경로(_collect_vision_fault_context(coach 전) → coach writers 소비 → build_result → _apply_vision_veto(vision_fault_context=) → 캐시)를 통과해 측정한다. 프레임선별·글로벌+로컬 DTW 게이팅은 collect 가, cap+audit attach 는 apply 가 수행하고 verdict Gemini 호출은 1회다 (D-08 H5 + D-10 HIGH-1)"
    - "kip-up known-answer 상체 결함(좌/우팔+고개) recall 이 whole-video baseline 대비 복구됨이 Pod 에서 실측·박제된다 — baseline 은 동일 모델/버전으로 재실행한 apples-to-apples (Success #1, D-08 H5)"
    - "케이스 매트릭스가 확장된다: elite clean / imperfect clean / occluded / tempo-shifted / spinning / known-fault — 1 kip-up + 1 clean 으로는 위양성 비증가를 주장할 수 없음 (D-08 H5)"
    - "위양성·alignment abstention·resource 미완(완료실패)이 **세 갈래로 분리** 측정된다 — clean case 가 전부 low_alignment_confidence 로 abstain 하거나 resource_limited 로 timeout 하면 false_positive_count=0 이 specificity 증명 없이 통과(분모 오류). JSON 에 clean_applied_fault_count/clean_true_negative_count/clean_abstention_count(=alignment 만)/resource_incomplete_count(=resource_limited/samplingComplete=false)/clean_evaluable_count + case 별 abstention_allowed + completion_pass 를 박고, case class 별 게이트 + coverage 임계로 '전부 abstain' 도 '전부 timeout' 도 통과 못 하게 한다 (D-11 HIGH-2 + D-12 HIGH-2)"
    - "`resource_limited`(예산소진/samplingComplete=false)는 abstention 이 아니라 **완료실패** 로 취급된다 — manifest 가 의도적 budget-stress 케이스로 표시 안 한 한 main Pod gate 의 completion/resource coverage 를 FAIL 시키고, true negative 도 alignment abstention 도 아니며 tempo-shift alignment abstention 과 그룹화하지 않는다. `completion_pass` = budget-stress 아닌 모든 케이스가 samplingComplete=true AND status != resource_limited. **(D-13 HIGH-2, Option A) main eval path 의 normal `applied` 는 samplingComplete=true 필수** — applied 인데 samplingComplete=false 는 clean success 로 카운트하지 않고 resource_incomplete 로 분류하며, cold·warm 이 completion-pass 분류를 바꾸지 못한다 (D-12 HIGH-2 + D-13 HIGH-2)"
    - "cold-cache 와 warm-cache 결정론이 분리 측정되고 **arm 간 캐시 격리**가 강제된다 — `arm` ∈ {production_still, direct_adapter_still, whole_video_baseline} 별/cold 반복별 cache_namespace/run_id; cold 첫 run cacheHit=false; warm 은 직전 cold 와 같은 키 + cacheHit=true; whole-video baseline 키 ≠ still production 키; direct-adapter 는 manifest 가 의도 표시 안 한 한 production gate run 을 pre-warm 금지 + cost/latency 게이트. **캐시 격리 비교는 같은 case 내 arm 간 cacheKey 비교다 (D-17 HIGH-2)** — production recall·특이도 게이트는 `arms.production_still` 만, baseline 비교는 `arms.whole_video_baseline` 만 읽는다 (D-08 H5 + D-12 MED-2 + D-17 HIGH-2)"
    - "**multi-arm result shape 가 고정된다 (D-17 HIGH-2):** result = `cases[]{row_id, arms:{production_still, whole_video_baseline, direct_adapter_still}}`(case×arm 평면 row 아님). manifest row = `{row_id, required_arms:[production_still, whole_video_baseline], optional_arms:[direct_adapter_still]}`. assert: manifest row_id set == result cases[].row_id set / 각 case 의 required_arms 정확히 1회 존재 / unknown arm 0 / production recall·특이도 게이트는 `arms.production_still` 만 / baseline recall 비교는 `arms.whole_video_baseline` / 캐시격리는 같은 case 내 arm 간 cacheKey 비교. (옛 `result row set == manifest row set` 평면 계약 폐기 — arm 다중에서 extra-rows false-fail 또는 baseline/캐시격리 silent-miss 방지.)"
    - "결과 JSON 에 machine-checkable pass/fail 필드(arm, cache_namespace/run_id, call_count, upload_count, duration_ms, cacheKey/cacheHit, recall set, false_positive_count + clean_*_count + resource_incomplete_count + completion_pass + abstention_allowed + manifest_lock_git_commit + manifest_sha256 + **run_git_commit(eval 시작 HEAD) + worktree_dirty_at_run + eval_command + result_generated_at (D-17 HIGH-1)** + **Phase 20-04 흡수(D-14): elite_clean_score_in_95_100 / fault_cap_holds / kipup_fault_moderate_le_75 / score_determinism_cold / score_determinism_warm / eval18_discrimination_regression_count + 4쌍 margin**) 가 박제되며, 런타임 trace 는 VisionFaultContext.to_trace_dict()(collection_status/cap_would_apply/geminiCallCount 등 — D-13 MED-2 와 정합) 로 직렬화된다 (D-08 H5 + D-11 MED-2 + D-12 HIGH-2/MED-2 + D-13 MED-2 + D-14 + D-16 HIGH-1 + D-17 HIGH-1)"
    - "recall 은 프로젝트 자체 라벨셋으로 측정되고 프롬프트는 generic(curve-fit/known-answer 유도 0). recall 기대치는 **canonical FaultKey(part_scope/side/keypoint_set/fault_kind) 로 비교**되고 한글 표시라벨이 아니다 — manifest `expected_recall_keys` 는 **`FaultKey.from_dict` 로 검증**되고 result `recall_set` 은 **`FaultKey.to_dict` 산출물**이며 assert 는 **정규화된 FaultKey 4-tuple** 로 대조한다(free-form dict 아님). `display_label_ko` 는 optional 문서용만. enum 어휘(part_scope/side/keypoint_set/fault_kind)는 23-01 Task 2 의 **단일 owner `FaultKey.to_dict`/`from_dict`** + `VisionFaultContext.to_trace_dict()` 가 방출하는 것과 동일하다 — manifest 의 unknown fault_kind 는 Pod 결과 수락 **전에** fail (D-06 일반화 hard gate + D-16 MED-1 + D-17 MED-3, 23-01 FaultKey 와 단일 owner 정합)"
    - "**terminal gate 가 운영 가능하다 (D-16):** (HIGH-1) manifest freeze 는 Pod 측정과 **별도 pre-Pod 단계** 다 — `freeze_stillframe_veto_manifest.py` 가 ① manifest(JSON) commit ② clean worktree 에서 freeze(uncommitted/dirty 면 거부) ③ lock.json commit ④ Pod 가 그 commit pull ⑤ Pod eval 은 result JSON 만 생성 순서를 강제하고, assert 는 `lock_git_commit` 이 현 HEAD 의 **ancestor** 인지 + dirty/dev 아닌지 검증한다(post-result relabel 차단). result JSON 은 `manifest_lock_git_commit` 을 기록한다. (HIGH-2) manifest 는 **JSON(`eval_stillframe_veto_manifest.json` + `.lock.json`)** 이고 freeze·assert 는 stdlib(json/hashlib/subprocess) 만 사용해 RunPod 의존을 추가하지 않는다 — PyYAML 은 runpod_inference/requirements.txt 에 없어 Pod-terminal assert 가 import 단계에서 죽지 않게 한다. (MED-2) assert 는 grep/AST 가 아니라 **mandatory behavioral test** (`backend/tests/test_assert_stillframe_veto_gate.py`)로 검증된다 — valid→exit0 / hash mismatch / missing row / extra row / `\"true\"` 문자열 / non-budget-stress resource_limited / kip-up major+50 / kip-up moderate+80 / EVAL18 fault≥success 각각 non-zero exit"
    - "**terminal gate 가 tamper-proof 다 (D-17):** (HIGH-1 run-commit ancestry) `eval_stillframe_veto.py` 는 lock 존재 + 현 manifest sha == lock.manifest_sha256 + lock_git_commit 이 현 run HEAD 의 ancestor + worktree clean(출력 경로 제외) 이 아니면 **실행을 거부**하고, result JSON 에 `run_git_commit`(eval 시작 HEAD)/`worktree_dirty_at_run`/`manifest_lock_git_commit`/`manifest_sha256`/`eval_command`/`result_generated_at` 를 기록한다. assert 는 `lock_git_commit` 이 **`result.run_git_commit`** 의 ancestor(현 HEAD 아님) + `result.run_git_commit` 이 현 HEAD 의 ancestor + `result.worktree_dirty_at_run==false` + `result.manifest_lock_git_commit==lock.lock_git_commit` + `result.manifest_sha256==lock.manifest_sha256` 를 검증한다(eval 먼저 돌리고→freeze→result 에 lock commit 끼워넣는 post-result freeze 사고/조작 차단). (MED-1 manifest-owned pair mapping) EVAL18 regression-pair 는 manifest top-level `regression_pairs:[{pair_id, fault_row_id, success_row_id, min_margin}]`(4쌍)이 소유하고, lock 이 snapshot, assert 는 이 mapping 에서만 margin 계산(row_id 네이밍/motion-id 추론 = hardcoded known-answer 재유입 금지). (MED-2 manifest-owned gate_policy) coverage 임계·gate grouping 은 manifest top-level `gate_policy:{clean_coverage_min, clean_gate_case_classes:[elite_clean,imperfect_clean,occluded,spinning], alignment_abstention_case_classes:[tempo_shifted], known_fault_case_classes:[known_fault], required_arms:[production_still,whole_video_baseline]}` 가 소유하고, lock 이 포함, assert 는 script 상수가 아니라 **locked policy 에서 coverage 재계산**(미래 변경이 frozen manifest 안 거치고 게이트 강도 못 바꾸게). (MED-3 FaultKey 단일 serializer) `FaultKey.to_dict()`/`from_dict()`(23-01 Task 2 소유, locked enum)로 manifest `expected_recall_keys` 검증 + result `recall_set` 산출 + assert 정규화 tuple 비교; behavioral test 에 `lock_git_commit ∉ ancestor(result.run_git_commit)` / `worktree_dirty_at_run==true` / missing required arm / unknown arm / unknown fault_kind 가 각각 non-zero exit 인 fixture 포함"
    - "**Phase 20-04 의 SEVERITY_CAP *regression subset* 게이트만 23-03 eval 이 still-frame veto 에서 OWN·검증한다 (D-14 amended + D-15, regression subset 만 supersedes Phase 20-04):** cap(50/75/90) 수식은 불변이되 veto 입력이 still-frame 으로 바뀌므로 20-04 를 옛 whole-video 로 따로 캘리브레이션하지 않고 본 eval 이 still-frame 경로에서 regression subset 의 네 게이트를 박는다 — (a) 정은지 정타 95~100 retained(비전이 깨끗한 자세 안 깎음, V-3), (b) kip-up fault = moderate, 점수 ≤75(belle ITERATION6 HIGH-3 — 무릎굽음/발끝/스트래들은 제대로 인식된 kip-up 의 moderate 실행결함이지 틀린 동작 아님; 20-04 evidence 75/moderate 와 일치; ≤50 억지 격상=curve-fit 금지, V-1), (c) determinism(같은 입력=같은 점수 — cold·warm 분리, V-4), (d) EVAL18 baseline 변별 4쌍(power-spin/peter-pan/elbow-twist/pdshape) margin>0 퇴행 0(V-2). 이 항목들이 frozen manifest·machine-checkable JSON 게이트 필드(`elite_clean_score_in_95_100`/`fault_cap_holds`/`kipup_fault_moderate_le_75`/`score_determinism_cold`/`score_determinism_warm`/`eval18_discrimination_regression_count`)·non-zero assert·acceptance 에 포함되고, ROADMAP Phase 20-04 regression subset = superseded-by-23-03 (D-14). **SCORE-09(일반화/sensitivity — 미보유+above-cutoff 양방검증, diversity floor)는 흡수하지 않는다 — 별도 pending 으로 Phase 20/후속에 잔류(D-14 amended HIGH-1). 본 eval 은 20-04 의 sensitivity manifest/diversity floor/above-cutoff 행을 import 하지 않으며, 흡수 게이트 = elite 95~100 + kip-up moderate≤75 + 결정론(cold+warm) + EVAL18 4쌍 퇴행0 으로 한정한다.**"
  artifacts:
    - path: "backend/research/spikes/eval_stillframe_veto.py"
      provides: "still-frame veto eval harness (Pod-bound, 순차 실행, FULL production 경로 + direct-adapter 둘 다 + 동일-모델 baseline 재실행 + arm 별 캐시 격리 cold/warm 결정론 + cost/latency + FP/alignment-abstention/resource-incomplete 3-way 분리 + completion_pass + case-class 게이트 + coverage 임계 + machine-checkable 필드 + multi-arm result shape(cases[].arms) + run-commit ancestry 강제(lock 부재/sha 불일치/lock∉ancestor(run HEAD)/dirty 면 실행 거부) + run_git_commit/worktree_dirty_at_run/manifest_lock_git_commit 기록 + to_trace_dict 직렬화 + FaultKey.to_dict 로 recall_set 산출 + **Phase 20-04 흡수 게이트(D-14): 정은지 정타 95~100 / fault cap(kip-up moderate≤75) / score 결정론(cold·warm) / EVAL18 변별 4쌍 퇴행 0**)"
      contains: "def main"
    - path: "backend/research/spikes/freeze_stillframe_veto_manifest.py"
      provides: "pre-Pod manifest freeze 단계 (D-16 HIGH-1) — stdlib(json/hashlib/subprocess) 만. clean worktree 에서만 실행(manifest uncommitted 또는 worktree dirty 면 REFUSE/non-zero exit), manifest JSON sha256 + 현 git commit(=lock_git_commit) + created timestamp(--created-at 주입 인자) + row ids + expected-policy 스냅샷 + **regression_pairs(D-17 MED-1) + gate_policy(D-17 MED-2) 스냅샷** 을 lock.json 으로 emit. Pod 측정 전 라벨 freeze 강제(post-result relabel 차단)"
      contains: "def main"
    - path: "backend/research/spikes/assert_stillframe_veto_gate.py"
      provides: "non-zero exit assert gate (D-15 HIGH-2 + D-16 HIGH-1/HIGH-2/MED-1 + D-17 HIGH-1/HIGH-2/MED-1/MED-2/MED-3) — frozen manifest(JSON)+lock 정합 검증 + **run-commit ancestry(lock_git_commit 이 result.run_git_commit 의 ancestor + result.run_git_commit 이 현 HEAD 의 ancestor + worktree_dirty_at_run==false + manifest_lock_git_commit/sha 정합, D-17 HIGH-1)** + **multi-arm result shape 검증(cases[].arms, required_arms 정확히 1회, unknown arm 0, D-17 HIGH-2)** + coverage_pass/completion_pass/determinism/arm 캐시격리/D-14 게이트를 row 에서 재계산(precomputed 불신) + **gate_policy/regression_pairs 를 locked manifest 에서 읽어 coverage·margin 재계산(D-17 MED-1/MED-2)** + recall 을 **FaultKey.to_dict/from_dict 정규화 4-tuple** 로 대조(한글 표시라벨 아님, D-17 MED-3) + kip-up moderate≤75 severity-misclassification vs cap-application 구분 메시지 + status 화이트리스트. stdlib(json/hashlib/subprocess) 만 — PyYAML 의존 0. 어떤 실패에서도 non-zero exit"
      contains: "def main"
    - path: "backend/tests/test_assert_stillframe_veto_gate.py"
      provides: "assert gate 의 mandatory behavioral test (D-16 MED-2 + D-17) — pytest. 필수 fixture: valid 최소→exit0 / manifest hash mismatch→non-zero / missing row→non-zero / extra unmanifested row→non-zero / bool 자리 `\"true\"` 문자열→non-zero / non-budget-stress row 의 resource_limited→non-zero / kip-up severity=major score=50→non-zero(severity 오분류) / kip-up severity=moderate score=80→non-zero(cap 적용 실패) / EVAL18 pair fault≥success→regression count↑ non-zero / **lock_git_commit ∉ ancestor(result.run_git_commit)→non-zero (D-17 HIGH-1) / worktree_dirty_at_run==true→non-zero (D-17 HIGH-1) / case 에 required arm 결측→non-zero (D-17 HIGH-2) / unknown arm 존재→non-zero (D-17 HIGH-2) / manifest unknown fault_kind→non-zero (D-17 MED-3)**. grep/AST 가 아니라 실제 exit code 로 검증"
      contains: "def test_"
    - path: "backend/research/spikes/eval_stillframe_veto_manifest.json"
      provides: "frozen manifest (D-15 MED-2 + D-16 HIGH-2/MED-1 + D-17 HIGH-2/MED-1/MED-2/MED-3, JSON — PyYAML 의존 0) — row 별 row_id/case_class/expected_bucket/allowed_veto_statuses/expected_severity/max_score/min_score/abstention_allowed/budget_stress/alignment_unverifiable/**required_arms:[production_still,whole_video_baseline]/optional_arms:[direct_adapter_still] (D-17 HIGH-2)**/expected_recall_keys(canonical FaultKey 객체 list — FaultKey.from_dict 검증). top-level **regression_pairs:[{pair_id, fault_row_id, success_row_id, min_margin}] (4쌍, D-17 MED-1)** + **gate_policy:{clean_coverage_min, clean_gate_case_classes, alignment_abstention_case_classes, known_fault_case_classes, required_arms} (D-17 MED-2)**. kip-up row = {expected_severity: moderate, max_score: 75, allowed_veto_statuses: [applied], expected_bucket: must_drop, expected_recall_keys: [좌/우팔 arm + head_neck canonical FaultKey]} (D-14 amended — ≤50/major 아님; D-16 MED-1 — 한글 표시라벨 아님). SCORE-09 sensitivity 행(미보유/above-cutoff) 부재"
      contains: "regression_pairs"
    - path: "backend/research/spikes/eval_stillframe_veto_manifest.lock.json"
      provides: "manifest lock (D-15 MED-2 + D-16 HIGH-1 + D-17 MED-1/MED-2) — manifest_sha256 + lock_git_commit(freeze 시점 HEAD — assert 가 result.run_git_commit 의 ancestor 인지 검증) + dirty-worktree flag + created timestamp(--created-at 주입 인자, Date.now 금지) + row ids + expected-policy 스냅샷 + **regression_pairs snapshot (D-17 MED-1)** + **gate_policy snapshot (D-17 MED-2)**. freeze_stillframe_veto_manifest.py 가 clean worktree 에서만 생성(Pod 측정 전 라벨 freeze, post-result relabel 차단)"
      contains: "manifest_sha256"
    - path: "backend/research/spikes/reports/eval_stillframe_veto_phase23.json"
      provides: "Pod 실측 결과 박제 (multi-arm result shape — top-level cases[]{row_id, arms:{production_still, whole_video_baseline, direct_adapter_still}}, D-17 HIGH-2 / arm 별 cache_namespace / run_id / recall set(FaultKey.to_dict) / false_positive_count / clean_applied_fault_count / clean_true_negative_count / clean_abstention_count / resource_incomplete_count / clean_evaluable_count / completion_pass / abstention_allowed / call_count / upload_count / duration_ms / cacheKey / cacheHit / manifest_lock_git_commit / manifest_sha256 / **run_git_commit / worktree_dirty_at_run / eval_command / result_generated_at (D-17 HIGH-1)** / cold-warm 결정론 / 정렬-약 보류 / contextCollectedBeforeCoach / contextReusedForAudit / geminiCallCount / **Phase 20-04 흡수(D-14): elite_clean_score_in_95_100 / fault_cap_holds(+kip-up moderate≤75) / score_determinism_cold / score_determinism_warm / eval18_discrimination_regression_count(=0) + 4쌍 margin**)"
      contains: "kip-up"
  key_links:
    - from: "backend/research/spikes/eval_stillframe_veto.py"
      to: "pipeline._collect_vision_fault_context + _apply_vision_veto"
      via: "FULL production 경로 통과 측정 — collect(프레임선별·DTW 게이팅·Gemini·캐시) → coach writers 소비 → build_result → apply(cap+audit, Gemini 미호출). 어댑터 직접호출 아님 (H5 + D-10 HIGH-1)"
      pattern: "_collect_vision_fault_context|_apply_vision_veto"
    - from: "backend/research/spikes/eval_stillframe_veto.py"
      to: "VisionFaultContext.to_trace_dict() + FaultKey.to_dict()"
      via: "런타임 trace 필드(entrypoint/selectorVersion/frame indices/alignmentConfidence/geminiCallCount/cacheKey/cacheHit 등)를 raw dict 가 아니라 typed serializer 로 직렬화하고 recall_set 은 FaultKey.to_dict 산출 (D-11 MED-2 + D-17 MED-3)"
      pattern: "to_trace_dict|FaultKey"
    - from: "backend/research/spikes/eval_stillframe_veto.py"
      to: "git rev-parse HEAD + worktree status (run-commit ancestry, D-17 HIGH-1)"
      via: "eval 시작 시 run_git_commit/worktree_dirty_at_run 을 기록하고, lock 부재/sha 불일치/lock∉ancestor(run HEAD)/dirty(출력 경로 제외) 면 실행 거부"
      pattern: "run_git_commit|worktree_dirty_at_run|rev-parse|is-ancestor"
    - from: "backend/research/spikes/eval_stillframe_veto.py"
      to: "동일-모델 whole-video baseline 재실행 (case.arms.whole_video_baseline, 격리된 cache_namespace)"
      via: "apples-to-apples (옛 baseline JSON 비교가 아니라 같은 모델/버전으로 재실행; still production arm 과 다른 캐시 키, multi-arm result shape D-17 HIGH-2)"
      pattern: "whole_video_baseline|arms|cache_namespace"
    - from: "backend/research/spikes/freeze_stillframe_veto_manifest.py"
      to: "eval_stillframe_veto_manifest.json → eval_stillframe_veto_manifest.lock.json"
      via: "pre-Pod freeze 단계 (D-16 HIGH-1) — clean worktree 에서만 실행, manifest sha256 + 현 git commit(lock_git_commit) + dirty flag + regression_pairs(D-17 MED-1) + gate_policy(D-17 MED-2) 를 lock 에 박제. uncommitted/dirty 면 REFUSE(non-zero). stdlib 만 (D-16 HIGH-2)"
      pattern: "freeze_stillframe_veto_manifest|lock_git_commit|regression_pairs|gate_policy"
    - from: "backend/research/spikes/assert_stillframe_veto_gate.py"
      to: "eval_stillframe_veto_phase23.json + eval_stillframe_veto_manifest.json + .lock.json"
      via: "non-zero exit assert (D-15 HIGH-2 + D-16 + D-17) — JSON+manifest+lock 받아 lock 정합·run-commit ancestry(lock∈ancestor(result.run_git_commit)∈ancestor(HEAD))·multi-arm shape·row 재계산·locked gate_policy/regression_pairs 에서 coverage·margin 재계산·FaultKey 정규화 recall·kip-up moderate≤75·status 화이트리스트 검증, stdlib 만, 어떤 실패에서도 non-zero exit. Task 3 Pod 가 JSON 수락 전 실행(exit 0 필수)"
      pattern: "assert_stillframe_veto_gate|--manifest|--results|is-ancestor|gate_policy|regression_pairs"
    - from: "backend/tests/test_assert_stillframe_veto_gate.py"
      to: "assert_stillframe_veto_gate.py"
      via: "mandatory behavioral test (D-16 MED-2 + D-17) — 실제 exit code 로 검증(valid→0, 각 손상 fixture→non-zero: hash/missing row/extra arm/string-bool/resource_limited/kip-up major+50/moderate+80/EVAL18 fault≥success/lock∉ancestor(run)/dirty/missing required arm/unknown arm/unknown fault_kind). grep/AST 우회 차단"
      pattern: "subprocess|assert.*returncode|pytest"
    - from: "backend/research/spikes/eval_stillframe_veto.py"
      to: "Phase 20-04 흡수 게이트 (D-14) — 정은지 정타 95~100 / fault cap kip-up moderate≤75 / score 결정론 / EVAL18 변별 4쌍 퇴행 0"
      via: "still-frame veto production 경로에서 cap(50/75/90 불변) 게이트를 본 eval 이 OWN — 20-04 를 옛 whole-video 로 별도 캘리브레이션하지 않음(supersedes Phase 20-04). EVAL18 4쌍 margin>0 + kip-up moderate≤75 + elite 95~100 + 결정론을 machine-checkable 필드로 박제"
      pattern: "fault_cap_holds|eval18_discrimination_regression_count|elite_clean_score_in_95_100"
---

<objective>
23-01/23-02 의 still-frame veto 구현을 **Pod GPU 에서 실측**하되, codex HIGH(H5)에 따라
**어댑터 직접호출이 아니라 FULL production 경로**(`_collect_vision_fault_context`(coach 전,
Gemini 소유) → coach writers 소비 → `build_result` → `_apply_vision_veto(vision_fault_context=)`
(cap+audit, Gemini 미호출) → 캐시)를 통과해 측정한다. 프레임선별·글로벌+로컬 DTW 게이팅은
collect 가, downward cap+audit attach 는 apply 가 수행하며 verdict Gemini 호출은 1회다
(D-10 HIGH-1). (1) kip-up 상체 결함 recall 이 whole-video
baseline 대비 복구됨(스파이크 effect size 재현) — baseline 은 **동일 모델/버전으로 재실행**한
apples-to-apples, (2) 확장 케이스 매트릭스(elite clean / imperfect clean / occluded /
tempo-shifted / spinning / known-fault)에서 **위양성·alignment abstention·resource 미완을 3-way
분리** 측정해 특이도를 evaluable 입력에서 증명(D-11 HIGH-2 + D-12 HIGH-2) + arm 별 격리 cold/warm
결정론, (3) 시작점/템포 상이 케이스에서 거짓결함 비생성(보류), (4) cost/latency 게이트 +
machine-checkable pass/fail 필드를 증명·박제한다. recall 은 **프로젝트 자체 라벨셋**으로
측정하고 프롬프트는 generic 유지(known-answer 유도/curve-fit 금지, D-06).

**FP ↔ alignment abstention ↔ resource 미완 3-way 분리 (D-11 HIGH-2 + D-12 HIGH-2):
false_positive_count=0 이 specificity 를 증명하지 않는다.** clean case 가 전부
`low_alignment_confidence` 로 abstain 하거나 `resource_limited` 로 timeout 하면 분모가 비어
false_positive_count=0 이 무의미해진다. 더구나 `low_alignment_confidence`(의미적 정렬 보류)와
`resource_limited`(예산소진/샘플링 미완 = **완료실패**)는 **운영적으로 다르다** — resource_limited
를 일반 abstention 으로 세면 under-budget/timeout 구현이 clean FP 를 회피하면서 recall 의무도
부분 회피할 수 있다. 따라서 outcome class 를 분리한다(D-12 HIGH-2):
- `clean_applied_fault_count` = clean 에서 fault 잡힘(false positive).
- `clean_true_negative_count` = evaluable non-fault(none/not_applicable).
- `clean_abstention_count` = **`low_alignment_confidence` 만**(의미적 정렬 보류).
- `resource_incomplete_count` = **`resource_limited` / samplingComplete=false**(완료실패 — abstention 아님).
- `clean_evaluable_count` = applied + true_negative.
- `completion_pass` = budget-stress 아닌 모든 케이스가 samplingComplete=true AND status != resource_limited.
**main Pod gate 에서 `resource_limited` 는** manifest 가 의도적 budget-stress 케이스로 표시 안 한
한 **completion/resource coverage 를 FAIL** 시킨다 — true negative 도 alignment abstention 도
아니며 tempo-shift alignment abstention 과 **그룹화 금지**. case class 별 게이트(elite/imperfect
clean/occluded/spinning 은 manifest 가 alignment-unverifiable 로 명시 안 한 한 evaluable
non-fault 요구; tempo-shifted 는 low_alignment_confidence 허용하되 **true negative 가 아닌
ALIGNMENT ABSTENTION 으로 카운트**) + **coverage 임계**(전부 abstain → fail) + `completion_pass`
(전부 timeout → fail)로 "evaluable 입력에서 특이도 증명" 을 강제한다. (case class 그룹·coverage
임계 자체는 D-17 MED-2 의 manifest `gate_policy` 가 소유한다 — 아래 참조.)

**arm 간 캐시 격리 (D-12 MED-2 + D-17 HIGH-2): cold/warm 이 서로 오염되지 않게.** production seam /
direct-adapter / whole-video baseline 이 캐시 namespace 를 공유하거나 서로 warm 시키면 cold/warm
결과가 오도된다(input_granularity / selector version / frame indices / path type 이 모두
바뀌므로 특히 중요). 따라서 `arm` ∈ {production_still, direct_adapter_still,
whole_video_baseline}; arm·cold 반복마다 `cache_namespace`/`run_id`; cold assertion = 각 arm
첫 cold run `cacheHit=false`; warm assertion = 직전 cold 와 같은 키 + `cacheHit=true`; cross-arm
assertion = whole-video baseline 키 ≠ still production 키, direct-adapter 는 manifest 가 의도
표시 안 한 한 production gate run 을 pre-warm 금지. **arm 은 case 의 nested 필드다(D-17 HIGH-2)** —
아래 multi-arm result shape 참조.

**multi-arm result shape 고정 (D-17 HIGH-2): result row 정체성을 arm 다중과 정합.** eval 은
production_still / whole_video_baseline / direct_adapter_still 세 arm 을 측정한다. result JSON 이
`(case, arm)` 마다 평면 row 면 result row 수가 manifest row 수를 초과해 valid 결과가 "extra row"
로 false-fail 한다. 반대로 arm 을 nested 로 숨기면서 "result row set == manifest row set" 만 보면
production row 만 검증하고 whole-video baseline 또는 direct-adapter 캐시격리 실패를 silent 하게
놓친다. 따라서 result shape 를 고정한다:
```
{ "cases": [ { "row_id": "...", "arms": { "production_still": {...}, "whole_video_baseline": {...}, "direct_adapter_still": {...} } } ] }
```
manifest row = `{ "row_id": "...", "required_arms": ["production_still", "whole_video_baseline"], "optional_arms": ["direct_adapter_still"] }`.
assert 규칙: manifest row_id set == result cases[].row_id set / 각 case 의 모든 required_arms 가
정확히 1회 존재 / unknown arm 0 / production recall·특이도 게이트는 `arms.production_still` 만 읽음 /
whole-video baseline recall 비교는 `arms.whole_video_baseline` / 캐시격리는 같은 case 내 arm 간
cacheKey 비교. (옛 평면 "result row set == manifest row set" 계약은 폐기.)

**타입 계약 (D-11 MED-2 + D-17 MED-3): to_trace_dict() + FaultKey 직렬화.** 런타임 trace 필드는
raw dict 가 아니라 23-01 Task 4 의 `VisionFaultContext.to_trace_dict()` 로 직렬화한다(키 drift
방지). recall_set 은 23-01 Task 2 의 단일 owner `FaultKey.to_dict()` 산출물이다(아래 D-17 MED-3).
arm/cache_namespace/cacheHit 등 eval-only 필드는 harness 가 trace dict 에 append.

**terminal gate 운영화 (D-16, ITERATION7): D-15 가 "설명만" 이 아니라 실제 terminal gate 로 작동하게.** D-15 의 frozen manifest + non-zero assert 는 방향은 맞으나 Pod 에서 운영 가능하려면 네 가지가 더 필요하다:
- **(HIGH-1) freeze 는 Pod 측정과 별도 pre-Pod 단계다.** manifest+lock 을 동시 생성하면 "결과 본 뒤 relabel→lock→commit" 가 여전히 가능하다(D-15 가 막으려던 것). lock 의 hash 일치는 "현 manifest=lock" 만 증명할 뿐 결과 전 freeze 를 증명 못 한다. 따라서 freeze 를 독립 스크립트 `freeze_stillframe_veto_manifest.py` 로 분리하고 워크플로를 강제한다: ① `eval_stillframe_veto_manifest.json` 작성 → ② **commit** → ③ **clean worktree 에서** freeze 실행(manifest uncommitted 또는 worktree dirty 면 REFUSE/non-zero) → ④ `eval_stillframe_veto_manifest.lock.json` **commit** → ⑤ Pod 가 그 commit pull → ⑥ Pod eval 은 result JSON 만 생성 → ⑦ assert 가 `lock_git_commit` 이 현 HEAD 의 **ANCESTOR** 인지 + dirty/dev-only 아닌지 검증. result JSON 은 `manifest_lock_git_commit` 을 기록한다. Task 3 가 "Pod run 은 manifest+lock 을 이미 포함한 commit 에서 시작" 을 명시한다.
- **(HIGH-2) manifest = JSON, stdlib only.** assert 가 YAML 을 파싱하면 PyYAML 이 필요한데 **PyYAML 은 Python stdlib 가 아니고 `backend/runpod_inference/requirements.txt` 에 없다**(`setup.sh` 는 그 requirements 만 설치). Task 3 는 Pod-terminal 이라 assert 가 JSON 수락 전 import 단계에서 죽으면 valid 한 eval 결과도 게이트 실패한다. 따라서 manifest 를 **`eval_stillframe_veto_manifest.json` + `.lock.json`** 으로 하고, freeze·assert 는 `json`/`hashlib`/`subprocess`(git) **stdlib 만** 사용한다 — RunPod 의존 추가 0. (옛 `.yaml` 참조는 전부 `.json` 으로 갱신.)
- **(MED-1) expected recall = canonical FaultKey, 표시라벨 아님.** manifest 가 `[왼팔,오른팔,고개·목]` 한글 표시라벨을 쓰면 23-01 의 canonical `FaultKey(part_scope, side, keypoint_set, fault_kind)` 계약과 drift 한다(표시카피 rename 이 게이트를 깨거나, 두 라벨이 한 fault 로 recall 을 부풀린다). 따라서 `expected_recall_keys` 를 canonical FaultKey 객체 list 로 박는다: `[{part_scope: upper_body, side: left, keypoint_set: arm, fault_kind: pole_gap_or_bent}, {…side: right…}, {part_scope: upper_body, side: unknown, keypoint_set: head_neck, fault_kind: extension_or_alignment}]`. `display_label_ko` 는 optional 문서용만. assert 는 `supported_differences[].fault_key`(또는 `VisionFaultContext.to_trace_dict()` 의 직렬화된 canonical key)와 비교하고 렌더 한글 텍스트와는 비교하지 않는다 — 이 키는 23-01 Task 2 의 FaultKey 스키마와 정합한다.
- **(MED-2) assert 는 mandatory behavioral test 로 검증된다.** grep/AST(소스에 단어 존재·`sys.exit` 존재)만으론 terminal gate 로 약하다 — precomputed bool 신뢰·`"true"` 문자열·missing row·hash mismatch·kip-up major≤50 오통과를 다 통과시킬 수 있다. 따라서 `backend/tests/test_assert_stillframe_veto_gate.py` 에 필수 failure fixture 를 두고 각각 정확한 exit code 를 assert 한다: valid 최소→exit 0 / manifest hash mismatch→non-zero / missing row→non-zero / extra unmanifested row→non-zero / bool 자리 `"true"` 문자열→non-zero / non-budget-stress row 의 resource_limited→non-zero / kip-up severity=major score=50→non-zero(severity 오분류) / kip-up severity=moderate score=80→non-zero(cap 적용 실패) / EVAL18 pair fault≥success→regression count↑ non-zero. Task 2 verify = `cd backend && python -m pytest tests/test_assert_stillframe_veto_gate.py -q`.

**terminal gate tamper-proof (D-17, ITERATION8, belle "마지막 점검"): D-16 으로 운영 가능해진 게이트를 조작/사고 불가능하게.** D-16 은 가장 큰 위험을 닫았으나, assert 가 여전히 구조를 추론하거나 잘못된 시점을 신뢰할 수 있는 다섯 곳이 남았다:
- **(HIGH-1) lock ancestry 를 result run commit 기준으로.** `lock_git_commit` 이 현 HEAD 의 ancestor 인 것만으론 "결과가 lock 이후 생성" 을 증명 못 한다 — eval 을 freeze 전에 돌리고, 결과를 보고, 그 다음 freeze→lock 을 만들고, result JSON 에 `manifest_lock_git_commit` 을 끼워넣으면 assert 가 통과한다(악의 없는 수동 워크플로 사고 포함: sweep 먼저 돌리고 lock 누락을 깨닫고 만들어 이어가기). **fix:** eval harness 가 시작 시 lock 상태를 강제·기록한다. `eval_stillframe_veto.py` 는 (a) `eval_stillframe_veto_manifest.lock.json` 존재, (b) 현 manifest sha == lock.manifest_sha256, (c) lock_git_commit 이 현 run HEAD 의 ancestor, (d) worktree 가 출력 경로 제외하고 clean — 이 넷이 아니면 **실행을 거부**한다. result JSON 은 `run_git_commit`(eval 시작 시 `git rev-parse HEAD`)/`worktree_dirty_at_run`(bool)/`manifest_lock_git_commit`(=lock.lock_git_commit)/`manifest_sha256`(=lock.manifest_sha256)/`eval_command`/`result_generated_at` 를 기록한다. assert 는 (1) `lock_git_commit` 이 **`result.run_git_commit`** 의 ancestor(현 HEAD 가 아니라 측정 시작 commit), (2) `result.run_git_commit` 이 현 HEAD 의 ancestor, (3) `result.worktree_dirty_at_run == false`, (4) `result.manifest_lock_git_commit == lock.lock_git_commit`, (5) `result.manifest_sha256 == lock.manifest_sha256` 를 검증한다. 수동 편집 JSON 을 완전 불가능하게 하지는 못하나, 의도된 워크플로를 기계적으로 감사 가능하게 하고 흔한 post-result freeze 사고를 잡는다.
- **(HIGH-2) multi-arm result shape 고정.** 위 "multi-arm result shape 고정" 문단 참조 — `cases[]{row_id, arms:{production_still, whole_video_baseline, direct_adapter_still}}` 로 lock 하고, manifest row 가 `required_arms`/`optional_arms` 를 명시하며, production 게이트는 `arms.production_still` 만, baseline 은 `arms.whole_video_baseline` 만 읽고, 캐시격리는 같은 case 내 arm 간 키 비교다.
- **(MED-1) EVAL18 regression-pair mapping 을 manifest 소유.** `eval18_discrimination_regression_count=0` 의 4쌍(power-spin/peter-pan/elbow-twist-sister/pdshape)을 assert 가 row_id 네이밍이나 motion id 로 추론하면 hardcoded known-answer 로직이 게이트에 재유입되고 row rename 이 위험해진다. **fix:** manifest top-level `regression_pairs: [{pair_id, fault_row_id, success_row_id, min_margin}]` (4쌍)을 박는다. lock 이 pair policy 를 snapshot 하고, assert 는 **이 mapping 에서만** margin(success_final_score − fault_final_score >= min_margin)을 계산한다.
- **(MED-2) coverage 임계·gate grouping 을 manifest 소유.** `clean_evaluable_count >= COVERAGE_MIN` 의 `COVERAGE_MIN` 이 script 상수면 미래 변경이 frozen manifest 를 거치지 않고 terminal-gate 강도를 바꿀 수 있고, occluded/spinning/tempo-shifted 가 같은 분모인지 다른 gate 그룹인지 모호해진다. **fix:** manifest top-level `gate_policy: {clean_coverage_min, clean_gate_case_classes:[elite_clean, imperfect_clean, occluded, spinning], alignment_abstention_case_classes:[tempo_shifted], known_fault_case_classes:[known_fault], required_arms:[production_still, whole_video_baseline]}` 를 박는다. lock 이 gate_policy 를 포함하고, assert 는 script 상수가 아니라 **locked policy 에서 coverage 를 재계산**한다.
- **(MED-3) canonical FaultKey 단일 serializer/validator.** D-16 이 recall 을 한글 표시라벨에서 canonical FaultKey 로 바꿨으나, manifest 의 값(`pole_gap_or_bent`/`extension_or_alignment`/`head_neck` 등)이 `VisionFaultContext.to_trace_dict()` 가 방출하는 enum 어휘와 다르면 시스템이 의도한 fault 를 찾아도 게이트가 fail 한다. **fix:** 23-01 Task 2 가 **단일 owner** `FaultKey.to_dict()`/`from_dict()` 를 part_scope/side/keypoint_set/fault_kind 의 **locked enum 값**으로 정의한다(동일 어휘를 `VisionFaultContext.to_trace_dict()` 가 방출). 23-03: manifest `expected_recall_keys` 는 `FaultKey.from_dict` 로 검증, result `recall_set` 은 `FaultKey.to_dict` 산출, assert 는 정규화된 FaultKey 4-tuple 로 비교(free-form dict 아님). behavioral test: manifest 의 unknown `fault_kind` 가 어떤 Pod 결과 수락 **전에** fail.

**Phase 20-04 흡수 (D-14 amended + D-15, regression subset 만 supersedes Phase 20-04): 본 eval 이 SEVERITY_CAP *regression subset* 게이트만 still-frame veto 에서 OWN 한다.** 23 은 cap(50/75/90) 수식을 그대로 쓰되 veto 입력을 still-frame 으로 바꾸므로, 20-04 의 still-frame regression subset 을 옛 whole-video 로 따로 캘리브레이션하면 23 이 다시 흔든다(중복·드리프트). 따라서 본 harness 가 still-frame 경로에서 regression subset 의 네 게이트를 박는다: (a) **정은지 정타 95~100 retained**(V-3) — elite clean case 의 최종 점수(cap 적용 후)가 95~100, 비전이 깨끗한 자세를 안 깎음, (b) **kip-up fault = moderate, 점수 ≤75**(V-1, belle ITERATION6 HIGH-3) — known fault row(kip-up)가 severity=moderate 로 인식되고 cap 적용으로 점수 ≤75. **무릎굽음/발끝/스트래들은 제대로 인식된 kip-up 의 moderate 실행결함이지 틀린 동작 아님 — 20-04 evidence 75/moderate 와 일치. ≤50 으로 억지 격상(major 강제)하면 known-answer curve-fit 이므로 금지. '잘못된 동작 ≤50' 스펙은 진짜 실패/틀린 동작용이지 이 moderate kip-up 용이 아님.** (c) **score 결정론**(V-4) — 같은 입력=같은 점수, cold·warm 분리(verdict 결정론과 별개로 최종 점수 byte-stable), (d) **EVAL18 baseline 변별 4쌍(power-spin/peter-pan/elbow-twist/pdshape) margin>0 퇴행 0**(V-2, manifest `regression_pairs` 가 pair 매핑 소유 — D-17 MED-1). frozen manifest·machine-checkable JSON 게이트(`elite_clean_score_in_95_100`/`fault_cap_holds`/`kipup_fault_moderate_le_75`/`score_determinism_cold`/`score_determinism_warm`/`eval18_discrimination_regression_count`)·non-zero assert·acceptance·POD checkpoint 에 포함. **SCORE-09(일반화/sensitivity — 미보유+above-cutoff 양방검증, diversity floor, policy/asset lock)는 흡수하지 않는다 — 별도 pending 으로 Phase 20/후속에 잔류한다(D-14 amended HIGH-1). 본 eval 은 20-04 의 sensitivity manifest/diversity floor/above-cutoff 행을 import 하지 않으며, frozen manifest(아래 Task 1 (I))는 still-frame regression subset(known-fault/clean/tempo-shift/occluded/spinning) 만 라벨링한다. Phase 23 을 SCORE-09 미처리로 닫거나 20-04 를 SCORE-09 채로 superseded 처리 금지.** (ROADMAP Phase 20-04 regression subset = superseded-by-23-03. belle: 21 은 23 뒤, 22·10 은 파일럿 후.)

**스코프 (D-07): Mode 1 집중.** Mode 3 정량화/정렬 eval 은 defer(Backlog B-15a).

Purpose: 코드가 "동작" 하는 것과 "정확도 게이트를 통과" 하는 것은 다르다 — 핵심 가치(분석
정확도)는 Pod 실측으로만 검증됨([[pipeline-not-concurrency-safe-eval-serial]]). 그리고 스파이크
재현이 아니라 **실제 production 경로**가 같은 결과를 내는지가 게이트(H5). 구현(23-01/02)은
Pod-free 였으나 본 plan 은 GPU·라이브 LLM·실 영상이 필수.

Output: eval harness(FULL production 경로 + 동일-모델 baseline 재실행 + 확장 매트릭스 + arm 별
격리 cold/warm 결정론 + multi-arm result shape + run-commit ancestry 강제 + cost/latency +
FP/alignment-abstention/resource-incomplete 3-way 분리 + completion_pass + case-class 게이트 +
coverage 임계 + machine-checkable 필드 + to_trace_dict 직렬화 + FaultKey.to_dict recall) +
pre-Pod freeze 스크립트(JSON manifest + regression_pairs + gate_policy snapshot, stdlib only) +
non-zero assert gate(stdlib only, run-commit ancestry + multi-arm shape + locked policy 재계산 +
FaultKey 정규화 recall) + mandatory behavioral test + Pod 실측 결과 JSON 박제.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kimtaesung/Dev/SunityMotion/.planning/phases/23-mode-1-recall-still-frame-veto-dtw-key-frame/23-CONTEXT.md
@/Users/kimtaesung/Dev/SunityMotion/.planning/phases/23-mode-1-recall-still-frame-veto-dtw-key-frame/23-REVIEWS.md
@/Users/kimtaesung/Dev/SunityMotion/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: still-frame veto eval harness — FULL production 경로 + 동일-모델 baseline 재실행 + 확장 매트릭스 + arm 별 격리 cold/warm + multi-arm result shape + run-commit ancestry 강제 + FP/alignment-abstention/resource-incomplete 3-way 분리 + machine-checkable (Pod-bound, 순차, H5, D-11 HIGH-2, D-12 HIGH-2/MED-2, D-17 HIGH-1/HIGH-2/MED-3)</name>
  <files>backend/research/spikes/eval_stillframe_veto.py</files>
  <read_first>
    - backend/research/spikes/reports/spike_vision_grounding_kip-up.json (whole-video baseline — 상체 0, 단 옛 모델 — H5: 동일 모델로 재실행 필요)
    - backend/functions/pipeline/app.py (`_collect_vision_fault_context`(Gemini 소유, coach 전, keyword overall_score) + `_apply_vision_veto(vision_fault_context=)`(cap+audit, Gemini 미호출) + 글로벌+로컬 DTW 게이팅 + low_alignment_confidence/resource_limited — 23-01 Task 3/4. **eval 은 collect→coach→build_result→apply seam 을 통과해야 함, verdict 1회**)
    - backend/shared/python/sunity_shared/analysis/vision_veto.py (`VisionFaultContext.to_trace_dict()` + `FaultKey.to_dict()`/`from_dict()` — 23-01 Task 4/2. eval trace 직렬화 + recall_set 산출은 이 serializer 사용)
    - backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py (`assess_fault_severity` still-pair + VisionVetoCache cold/warm + MAX_VETO_CALLS/UPLOADS/WALL_S — 23-01/02 구현. cache namespace/key 진입점)
    - 기존 spike/eval 스크립트 1개 (`ls backend/research/spikes/*.py` — 가장 가까운 harness 를 구조 analog 으로 복사: CLI args / 순차 루프 / JSON 결과 dump / git rev-parse)
    - MEMORY [[pipeline-not-concurrency-safe-eval-serial]] (eval 반드시 순차) + [[jeongeunji-success-fail-pair-dataset]] (확장 매트릭스 영상 소스 — 정성 참고만, 점수 라벨 금지)
    - 23-REVIEWS.md MEDIUM "23-03 eval may bypass the production path" 전문 + Suggestions(eval telemetry/case matrix/cold-warm/re-baseline)
    - 23-DIRECT-REVIEW-ITERATION3.md HIGH-2 전문 (FP↔abstention 분리 + coverage 임계)
    - 23-DIRECT-REVIEW-ITERATION4.md HIGH-2 + MED-2 전문 (resource_limited=완료실패, arm 별 캐시 격리)
    - 23-DIRECT-REVIEW-ITERATION5.md HIGH-2 전문 (main eval applied⟹samplingComplete=true, Option A)
    - 23-DIRECT-REVIEW-ITERATION8.md HIGH-1 + HIGH-2 전문 (run-commit ancestry, multi-arm result shape)
    - .planning/phases/20-v2-gemini/20-04-PLAN.md (흡수 대상 게이트 — V-1 kip-up moderate≤75 / V-2 변별 4쌍 퇴행0(power-spin/peter-pan/elbow-twist/pdshape margin>0) / V-3 정은지 정타 95~100 / V-4 결정론 cache-warm byte-identical — D-14)
    - backend/evals/phase18/baseline/eval18_serial_baseline.json (EVAL18 변별 4쌍 baseline margin 28/21/41/42, kip-up known_false_positive — D-14 회귀 대조)
    - 23-CONTEXT.md D-14 (Phase 20-04 흡수) + D-17 HIGH-1/HIGH-2/MED-3
  </read_first>
  <action>
    Pod 에서 실행할 eval harness 신설. **순차**(동시 호출 금지 — 결과 오염 [[pipeline-not-concurrency-safe-eval-serial]]).
    **(A) FULL production 경로(H5 + D-10 HIGH-1):** eval 은 `gemini_vision_scorer.assess_fault_severity` 어댑터를 직접 호출하지 않고 **production seam 전체**를 통과해 측정한다: `_collect_vision_fault_context`(coach 전, keyword overall_score, 프레임선별 → 글로벌+로컬 DTW 게이팅 → still 추출 → Gemini → support 게이트 → cap_would_apply → 캐시 → telemetry) → (coach writers 가 VisionFaultContext.to_coach_context() 소비) → `build_result` → `_apply_vision_veto(result, vision_fault_context=ctx)`(final cap 후 ctx.to_audit_dict 로 audit attach, Gemini 미호출). collect 가 Gemini 단독 소유 → verdict 호출은 **1회**. (보조로 direct-adapter 경로도 별도 arm 으로 측정해 비교 가능 — 단 게이트 판정은 production seam arm 기준.)
    **(B) 동일-모델 baseline 재실행(H5):** whole-video baseline 을 옛 JSON 비교가 아니라 **현재와 동일한 모델/버전/프롬프트로 재실행**해 apples-to-apples 비교(옛 `spike_vision_grounding_kip-up.json` 은 참고만). still-pair recall ⊖ whole-video recall 을 같은 모델로 측정. baseline 은 `arm="whole_video_baseline"` 로 격리된 cache_namespace 에서 실행(아래 D/E 참조).
    **(B2) multi-arm result shape(D-17 HIGH-2):** result JSON 을 **`cases[]{row_id, arms:{production_still, whole_video_baseline, direct_adapter_still}}`** 구조로 dump 한다 — `(case, arm)` 평면 row 가 아니라 case 당 하나의 객체에 arm 들을 nested. 각 arm 객체가 해당 arm 의 measurement(recall_set/alignment_status/score/cacheKey/cacheHit/call_count/upload_count/duration_ms 등)를 담는다. production recall·특이도 게이트 산출은 `arms.production_still` 만, whole-video baseline recall 비교는 `arms.whole_video_baseline` 만 사용. case 별로 manifest 의 `required_arms`(production_still+whole_video_baseline) 가 정확히 1회씩 채워지고 unknown arm 을 넣지 않는다.
    **(C) 확장 케이스 매트릭스 + case class(H5 + D-11 HIGH-2):** elite clean / imperfect clean / occluded / tempo-shifted / spinning / known-fault(kip-up 등) 케이스를 포함하고 **각 케이스에 case class + `abstention_allowed`(bool) + `budget_stress`(bool, 의도적 자원 압박 케이스 표시) 라벨**을 manifest 에 박는다 — 1 kip-up + 1 clean 으로는 위양성 비증가 주장 불가. tempo-shifted/start-offset 케이스만 `abstention_allowed=true`(low_alignment_confidence 허용), elite/imperfect clean/occluded/spinning 은 manifest 가 명시적으로 `alignment_unverifiable=true` 로 마킹하지 않는 한 `abstention_allowed=false`(evaluable non-fault 요구). 각 케이스 라벨은 프로젝트 자체 라벨셋([[jeongeunji-success-fail-pair-dataset]] 정성 참고만, 점수 라벨 금지). case class 그룹·coverage 임계는 manifest `gate_policy`(D-17 MED-2)가 소유.
    **(D) FP/alignment-abstention/resource-incomplete 3-way 분리 + case-class 게이트 + coverage 임계 + completion_pass(D-11 HIGH-2 + D-12 HIGH-2):** clean 케이스의 `arms.production_still` 결과를 분류한다 — `applied`(fault 잡힘=false positive), `none`/`not_applicable`(evaluable true negative), `low_alignment_confidence`(**alignment abstention**), `resource_limited`/`samplingComplete=false`(**resource 미완 = 완료실패, abstention 아님**). 집계 필드: `clean_applied_fault_count`, `clean_true_negative_count`(evaluable non-fault), `clean_abstention_count`(**low_alignment_confidence 만**), `resource_incomplete_count`(**resource_limited/samplingComplete=false**), `clean_evaluable_count`(=applied+true_negative). 판정:
    (i) **resource_limited 는 abstention 과 그룹화 금지** — manifest `budget_stress=true` 가 아닌 케이스가 resource_limited/samplingComplete=false 면 그 케이스 **completion/resource coverage FAIL**(true negative 도 alignment abstention 도 아님). `completion_pass` = budget_stress 아닌 모든 케이스가 samplingComplete=true AND status != resource_limited. **(D-13 HIGH-2, Option A) main eval path 의 normal `applied` 는 `samplingComplete=true` 를 필수로 한다** — applied 인데 samplingComplete=false 면 그 결과를 clean success 로 카운트하지 않고 resource_incomplete 로 분류(부분 샘플 applied 는 wall-clock/cache/latency 에 비결정). budget_stress fixture 만 graceful degradation 별도 검증.
    (ii) elite/imperfect clean/occluded/spinning(abstention_allowed=false; manifest `gate_policy.clean_gate_case_classes`) — **evaluable non-fault(none/not_applicable) 요구**, alignment abstention 이면 그 케이스 fail(manifest 가 alignment_unverifiable 로 명시한 경우만 면제).
    (iii) tempo-shifted(abstention_allowed=true; manifest `gate_policy.alignment_abstention_case_classes`) — low_alignment_confidence 허용하되 **true negative 가 아니라 ALIGNMENT ABSTENTION 으로 카운트**(분모에서 제외).
    **coverage 임계(D-17 MED-2 — manifest gate_policy 소유):** `clean_evaluable_count >= gate_policy.clean_coverage_min` 이고 그 evaluable 부분집합에서 `false_positive_count=0` **AND `completion_pass=true`** 여야 통과 — "전부 abstain" 은 coverage 미달로 fail, "전부 timeout" 은 completion_pass=false 로 fail(specificity·recall 미증명). false_positive_count=0 유지하되 evaluable 분모가 비거나 resource 미완이면 통과 불가. coverage 임계 상수는 script 가 아니라 manifest `gate_policy.clean_coverage_min` 에서 읽는다.
    **(E) arm 별 격리 cold/warm 결정론 분리(H5 + D-12 MED-2 + D-17 HIGH-2):** 결정론은 캐시 hit 로 거짓 증명될 수 있으므로 — **cold-cache(캐시 비우고) 동일 입력 반복 호출**의 verdict 동일성과 warm-cache hit 동일성을 **분리 측정**·기록한다. 그리고 **arm 간 캐시를 격리**한다: `arm` ∈ {production_still, direct_adapter_still, whole_video_baseline}; arm·cold 반복마다 고유 `cache_namespace`/`run_id`. cold assertion = 각 arm 첫 cold run `cacheHit=false`; warm assertion = 직전 cold 와 **같은 cacheKey** + `cacheHit=true`; cross-arm assertion = **같은 case 내** whole_video_baseline 의 cacheKey ≠ production_still 의 cacheKey(input_granularity/selector/frame indices/path type 차이 반영, D-17 HIGH-2); direct_adapter_still 은 manifest 가 의도 표시(`prewarm_intended=true`) 안 한 한 production_still gate run 을 **pre-warm 하지 않는다**(별도 namespace). 캐시격리 비교는 평면 row 가 아니라 같은 case 의 `arms` 간 비교다.
    **(F) cost/latency 게이트(H5):** 각 arm 의 call_count / upload_count / duration_ms 를 수집하고 23-01 의 MAX_VETO_CALLS/UPLOADS/WALL_S bound 이내인지 판정.
    **(G) machine-checkable pass/fail 필드(H5 + D-11 HIGH-2/MED-2 + D-12 HIGH-2/MED-2 + D-17):** result JSON top-level 에 `manifest_lock_git_commit`/`manifest_sha256`/`run_git_commit`/`worktree_dirty_at_run`/`eval_command`/`result_generated_at`(D-17 HIGH-1) + `cases[]`(D-17 HIGH-2). 각 case 의 각 arm 객체에 `arm`, `cache_namespace`, `run_id`, `call_count`, `upload_count`, `duration_ms`, `cacheKey`, `cacheHit`/`cache_miss`, `recall_set`(잡힌 결함 집합 — **`FaultKey.to_dict` 산출 객체 set**, 한글 표시라벨 아님 — D-16 MED-1 + D-17 MED-3), `alignment_status`(applied/none/not_applicable/low_alignment_confidence/resource_limited), `samplingComplete`. case-level 집계 + top-level 집계에 `false_positive_count`, `clean_applied_fault_count`/`clean_true_negative_count`/`clean_abstention_count`/`resource_incomplete_count`/`clean_evaluable_count`, `abstention_allowed`, `budget_stress`, `completion_pass`(bool), `determinism_cold`/`determinism_warm`(bool), 그리고 각 게이트 `pass`/`fail` bool + `coverage_pass` bool 을 박제(사람이 읽어 판단하는 정성 텍스트가 아니라 기계 판정 가능 필드). 런타임 trace 필드는 `VisionFaultContext.to_trace_dict()`(23-01 Task 4)로 직렬화(raw dict fork 금지, D-11 MED-2).
    **(H) Phase 20-04 흡수 게이트(D-14 — supersedes Phase 20-04):** still-frame veto production 경로(`arms.production_still`)에서 SEVERITY_CAP(50/75/90 불변) 게이트를 본 harness 가 OWN·검증한다(옛 whole-video 로 별도 캘리브레이션 금지). 케이스별 **최종 점수(cap 적용 후 overallScore)** 를 수집하고 네 machine-checkable 필드를 박는다: (a) `elite_clean_score_in_95_100`(bool — elite clean case 의 최종 점수가 95~100; 비전이 깨끗한 자세 안 깎음 — V-3), (b) `fault_cap_holds`(bool — known fault row 가 cap 적용으로 ≤상한; kip-up fault = moderate, 점수 ≤75 명시 필드 `kipup_fault_moderate_le_75` — V-1), (c) `score_determinism_cold`/`score_determinism_warm`(bool — 같은 입력=같은 최종 점수, cold·warm 분리; verdict 결정론과 별개로 최종 점수 byte-stable — V-4), (d) `eval18_discrimination_regression_count`(int — manifest `regression_pairs`(D-17 MED-1) 가 정의한 EVAL18 변별 4쌍 power-spin/peter-pan/elbow-twist/pdshape 의 success_final − fault_final < min_margin 인 쌍 수, **0 이어야 PASS**; 각 쌍 margin 도 기록 — V-2). EVAL18 4쌍은 `eval18_serial_baseline.json`(margin 28/21/41/42) 대비 회귀 0 — recall/특이도 게이트와 **같은 production 경로**로 측정. pair 매핑은 row_id/motion-id 추론이 아니라 manifest `regression_pairs` 에서만 읽는다(D-17 MED-1). 이 게이트는 23-03 가 OWN 하며 Phase 20-04 를 supersede(ROADMAP 표기). known-answer 라벨은 비교용일 뿐 프롬프트/임계 주입 아님(D-06).
    **(I) frozen manifest (D-15 MED-2 + D-16 HIGH-2/MED-1 + D-17 HIGH-2/MED-1/MED-2/MED-3 — JSON, stdlib only, Pod 측정 전 라벨 freeze):** `backend/research/spikes/eval_stillframe_veto_manifest.json` 신설 (YAML 아님 — PyYAML 은 runpod_inference/requirements.txt 에 없어 Pod-terminal assert 가 import 단계에서 죽음, D-16 HIGH-2). 각 row 에 `row_id` + `case_class`(elite_clean/imperfect_clean/occluded/tempo_shifted/spinning/known_fault) + `expected_bucket`(must_stay_high/must_drop/evaluable_non_fault) + `allowed_veto_statuses`(list) + `expected_severity` + `max_score` + `min_score`(elite 는 95) + `abstention_allowed`(bool) + `budget_stress`(bool) + `alignment_unverifiable`(bool) + **`required_arms`(["production_still","whole_video_baseline"]) + `optional_arms`(["direct_adapter_still"]) (D-17 HIGH-2)** + **`expected_recall_keys`(canonical FaultKey 객체 list — 한글 표시라벨 아님, D-16 MED-1; `FaultKey.from_dict` 로 검증 가능한 객체, D-17 MED-3)** 를 박는다. top-level 에 **`regression_pairs`(D-17 MED-1)** + **`gate_policy`(D-17 MED-2)** 를 박는다 — 아래 (K). `expected_recall_keys` 는 23-01 Task 2 의 단일 owner `FaultKey(part_scope, side, keypoint_set, fault_kind)` 와 정합하는 객체 list 다 — 예: `[{"part_scope": "upper_body", "side": "left", "keypoint_set": "arm", "fault_kind": "pole_gap_or_bent"}, {"part_scope": "upper_body", "side": "right", "keypoint_set": "arm", "fault_kind": "pole_gap_or_bent"}, {"part_scope": "upper_body", "side": "unknown", "keypoint_set": "head_neck", "fault_kind": "extension_or_alignment"}]`. `display_label_ko`("왼팔" 등)는 row 당 optional 문서 필드로만 둘 수 있고 게이트 비교에는 쓰지 않는다. **kip-up row 는 D-14 amended 대로 `{"row_id": "eval18-kip-up-fault", "case_class": "known_fault", "expected_bucket": "must_drop", "allowed_veto_statuses": ["applied"], "expected_severity": "moderate", "max_score": 75, "required_arms": ["production_still", "whole_video_baseline"], "expected_recall_keys": [좌팔 arm, 우팔 arm, head_neck 의 canonical FaultKey 3개 위 예시]}`** (≤50/major 아님 — belle ITERATION6 HIGH-3). 동봉 `eval_stillframe_veto_manifest.lock.json` 은 **별도 freeze 단계(Task 1b)가 생성** — 본 task 는 manifest.json + (eval 이 채울) result JSON 스키마만 소유. **라벨은 Pod 측정 전에 freeze** — 결과 본 뒤 hard case 를 budget_stress/alignment_unverifiable/out-of-scope 로 relabel 해 통과시키는 것을 차단. (SCORE-09 의 미보유+above-cutoff sensitivity 행은 manifest 에 넣지 않는다 — D-14 amended HIGH-1, regression subset 한정.)
    **(J) run-commit ancestry 강제 + result transcribe (D-17 HIGH-1 + D-16 HIGH-1):** eval harness 는 시작 시 (a) `eval_stillframe_veto_manifest.lock.json` 존재, (b) 현 manifest 파일 sha256 == lock.manifest_sha256, (c) `git merge-base --is-ancestor <lock.lock_git_commit> HEAD`(lock_git_commit 이 현 run HEAD 의 ancestor), (d) `git status --porcelain` 가 출력 결과 경로 제외하고 비어있음(worktree clean) — 이 넷이 아니면 **실행을 거부**(non-zero exit, 명확한 메시지). 그런 다음 `run_git_commit = git rev-parse HEAD`(eval 시작 시점)와 `worktree_dirty_at_run`(출력 경로 제외 dirty 여부) 를 캡처해 result JSON top-level 에 기록하고, lock 의 `lock_git_commit`/`manifest_sha256` 을 `manifest_lock_git_commit`/`manifest_sha256` 로 transcribe, `eval_command`(argv) 와 `result_generated_at`(주입 또는 ISO8601) 도 기록한다 — assert 가 lock↔result.run_git_commit↔HEAD ancestor 관계를 검증할 수 있게. (lock.json 자체는 freeze 스크립트가 생성하며, harness 는 그것을 읽어 강제·transcribe 만 한다.)
    **(K) manifest top-level regression_pairs + gate_policy (D-17 MED-1/MED-2):** manifest top-level 에 **`regression_pairs`** = `[{"pair_id": "eval18-power-spin", "fault_row_id": "eval18-power-spin-fault", "success_row_id": "eval18-power-spin-success", "min_margin": 1}, ...]`(4쌍: power-spin/peter-pan/elbow-twist-sister/pdshape) 를 박고, harness 의 `eval18_discrimination_regression_count` 산출은 row_id 네이밍/motion-id 추론이 아니라 **이 mapping 에서만** margin(success_final − fault_final >= min_margin)을 계산한다(D-17 MED-1). 그리고 **`gate_policy`** = `{"clean_coverage_min": <int>, "clean_gate_case_classes": ["elite_clean","imperfect_clean","occluded","spinning"], "alignment_abstention_case_classes": ["tempo_shifted"], "known_fault_case_classes": ["known_fault"], "required_arms": ["production_still","whole_video_baseline"]}` 를 박고, harness 의 coverage·case-class 게이트는 script 상수가 아니라 이 gate_policy 에서 읽는다(D-17 MED-2). freeze(Task 1b) 가 regression_pairs/gate_policy 를 lock 에 snapshot, assert(Task 2) 가 lock 에서 재계산.
    **프롬프트는 generic 유지** — harness 가 known-answer 에 맞춘 임계/프롬프트를 주입하지 않는다(D-06). Gemini 키는 SSM/env(로그 금지, T-20-06). CLI: 입력 영상 경로/reference id, 결과 출력 경로, 케이스 매트릭스 manifest 인자.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && python -c "import ast; ast.parse(open('research/spikes/eval_stillframe_veto.py').read()); print('parse ok')" && ! grep -Eq "concurrent|ThreadPool|asyncio.gather" research/spikes/eval_stillframe_veto.py && grep -Eq "run_git_commit|worktree_dirty_at_run" research/spikes/eval_stillframe_veto.py && echo "serial + run-commit ok"</automated>
  </verify>
  <acceptance_criteria>
    - `eval_stillframe_veto.py` 가 `main`/CLI entry 를 가진다 (`grep -n "def main\|__main__"`).
    - 동시 실행 primitive 부재 (`grep -E "concurrent|ThreadPool|asyncio.gather"` 0 — 순차 강제).
    - harness 가 **`_collect_vision_fault_context`(coach 전) → build_result → `_apply_vision_veto(vision_fault_context=)` production seam**을 호출하고, **소스 grep 이 아니라 런타임 trace 필드로 경로 통과를 증명한다(MED-2)**: 결과 JSON 의 각 case 의 각 arm 에 `entrypoint`, `selectorVersion`, `studentFrameIndices`, `referenceFrameIndices`, `alignmentConfidence`, `cacheKey`, `cacheHit`, `pathUsed`(single|window_union|low_alignment_confidence|resource_limited), 그리고 호출 소유권 trace `contextCollectedBeforeCoach`(true), `contextReusedForAudit`(true), `geminiCallCount`(1) 가 `VisionFaultContext.to_trace_dict()` 직렬화로 기록됨. 가능하면 `_process` 레벨 통합 1케이스(real 또는 fake-Firestore/fake-S3 로 같은 wiring 통과).
    - **(D-17 HIGH-2 multi-arm shape)** result JSON 이 `cases[]{row_id, arms:{production_still, whole_video_baseline, ...}}` 구조이고(`(case,arm)` 평면 row 아님), production 게이트 산출이 `arms.production_still` 만, baseline 비교가 `arms.whole_video_baseline` 만 사용한다 (`grep -nE '"arms"|"production_still"|"whole_video_baseline"|cases' research/spikes/eval_stillframe_veto.py` + 코드 리뷰).
    - harness 가 whole-video baseline 을 **동일 모델로 재실행**한다 (옛 JSON 단순 비교가 아닌 재실행 호출 존재 — `grep`/코드 리뷰, H5).
    - 케이스 매트릭스가 elite clean/imperfect clean/occluded/tempo-shifted/spinning/known-fault 를 포함하고 각 케이스에 case class + `abstention_allowed`(bool) + `budget_stress`(bool) 라벨이 manifest 에 있다 (`grep`/코드 리뷰 — D-11 HIGH-2 + D-12 HIGH-2).
    - **FP/alignment-abstention/resource-incomplete 3-way 분리:** harness 가 clean `arms.production_still` 결과를 applied(FP)/evaluable non-fault(true negative)/low_alignment_confidence(alignment abstention)/resource_limited(resource 미완)로 분류하고 `clean_applied_fault_count`/`clean_true_negative_count`/`clean_abstention_count`(alignment 만)/`resource_incomplete_count`(resource_limited/samplingComplete=false)/`clean_evaluable_count` 를 산출하며, `resource_limited` 가 abstention 과 그룹화되지 않고 budget_stress 아닌 케이스에서 completion/resource coverage 를 fail 시킨다 (`grep`/코드 리뷰 + 단위 판정 로직 — D-12 HIGH-2).
    - case-class 게이트(abstention_allowed=false 케이스는 evaluable non-fault 요구) + **coverage 임계(전부 abstain → fail; 임계는 manifest `gate_policy.clean_coverage_min` 에서 읽음 — D-17 MED-2)** + **`completion_pass`(budget_stress 아닌 모든 케이스 samplingComplete=true AND status!=resource_limited; 전부 timeout → fail)** 가 코드에 존재한다 (`grep -niE "gate_policy|clean_coverage_min|completion_pass"` + 코드 리뷰 — D-11 HIGH-2 + D-12 HIGH-2 + D-17 MED-2).
    - **arm 별 캐시 격리:** harness 가 `arm` ∈ {production_still, direct_adapter_still, whole_video_baseline} + arm·cold 반복별 `cache_namespace`/`run_id` 를 부여하고, cold 첫 run cacheHit=false / warm 직전 cold 와 같은 cacheKey + cacheHit=true / **같은 case 내** whole_video_baseline cacheKey ≠ production_still cacheKey / direct_adapter 가 production gate run 을 pre-warm 하지 않음(manifest prewarm_intended 만 예외)을 검증하는 코드가 존재한다 (`grep -niE "arm|cache_namespace|run_id|prewarm"` + 코드 리뷰 — D-12 MED-2 + D-17 HIGH-2).
    - cold-cache 반복 결정론과 warm-cache 결정론이 분리 측정된다 (cold/warm 분기 코드 존재 — `grep -niE "cold|warm|cache"`).
    - **(D-17 HIGH-1 run-commit ancestry)** harness 가 시작 시 lock 존재/현 manifest sha==lock.manifest_sha256/`git merge-base --is-ancestor <lock_git_commit> HEAD`/worktree clean 이 아니면 실행을 거부하고, result 에 `run_git_commit`/`worktree_dirty_at_run`/`manifest_lock_git_commit`/`manifest_sha256`/`eval_command`/`result_generated_at` 를 기록한다 (`grep -nE "run_git_commit|worktree_dirty_at_run|is-ancestor|rev-parse|manifest_sha256" research/spikes/eval_stillframe_veto.py` + 코드 리뷰).
    - 결과 dump 에 machine-checkable 필드(case 별 arm/cache_namespace/run_id/call_count/upload_count/duration_ms/cacheKey/cacheHit/recall_set + top-level false_positive_count/clean_*_count/resource_incomplete_count/abstention_allowed/budget_stress/completion_pass/coverage_pass/determinism/pass-fail + run-commit 필드) **+ to_trace_dict() trace 필드(entrypoint/selectorVersion/studentFrameIndices/referenceFrameIndices/alignmentConfidence/cacheKey/cacheHit/pathUsed + contextCollectedBeforeCoach/contextReusedForAudit/geminiCallCount)** 가 쓰인다 (MED-2 + D-10 HIGH-1 + D-11 HIGH-2 + D-12 HIGH-2/MED-2 + D-17 HIGH-1/HIGH-2).
    - **(D-14 Phase 20-04 흡수)** harness 가 still-frame production 경로(`arms.production_still`)에서 최종 점수(cap 적용 후) 기반 네 게이트 필드를 산출한다: `elite_clean_score_in_95_100`(정은지 정타 95~100, V-3) + `fault_cap_holds`/`kipup_fault_moderate_le_75`(kip-up fault = moderate, 점수 ≤75, V-1) + `score_determinism_cold`/`score_determinism_warm`(같은 입력=같은 최종 점수, V-4) + `eval18_discrimination_regression_count`(EVAL18 변별 4쌍 퇴행 수, 0 이어야 PASS, V-2 — manifest `regression_pairs` mapping 에서만 margin 계산, 4쌍 margin 도 기록) (`grep`/코드 리뷰 — D-14 + D-17 MED-1). 옛 whole-video 로 별도 캘리브레이션하지 않고 본 eval 이 OWN.
    - **(D-17 MED-1/MED-2 manifest-owned policy)** manifest top-level 에 `regression_pairs`(4쌍, pair_id/fault_row_id/success_row_id/min_margin) + `gate_policy`(clean_coverage_min/clean_gate_case_classes/alignment_abstention_case_classes/known_fault_case_classes/required_arms) 가 존재하고, harness 가 EVAL18 margin·coverage 를 이 manifest 정의에서만 읽는다 (`grep -nE '"regression_pairs"|"gate_policy"' backend/research/spikes/eval_stillframe_veto_manifest.json` + 코드 리뷰 — script 상수/row 추론 0).
    - known-answer 에 맞춘 임계/프롬프트 주입 0 (generic — D-06, 코드 리뷰 + `grep "kip-up"` 가 라벨/비교 용도일 뿐 프롬프트 주입 아님).
    - Gemini 키를 로그하지 않는다 (`grep -niE "log.*key|print.*key" research/spikes/eval_stillframe_veto.py` 에 키 노출 0).
    - **(D-15 MED-2 + D-16 HIGH-2/MED-1 + D-17 HIGH-2/MED-3 frozen manifest = JSON)** `eval_stillframe_veto_manifest.json`(YAML 아님 — JSON 만, D-16 HIGH-2) 가 존재하고 row 별 `row_id`/`case_class`/`expected_bucket`/`allowed_veto_statuses`/`expected_severity`/`max_score`/`min_score`/`abstention_allowed`/`budget_stress`/`alignment_unverifiable`/**`required_arms`/`optional_arms`(D-17 HIGH-2)**/**`expected_recall_keys`(canonical FaultKey 객체 list — 한글 표시라벨 아님, D-16 MED-1; FaultKey.from_dict 검증가능, D-17 MED-3)** 를 가지며, kip-up row = `{"expected_severity": "moderate", "max_score": 75, "allowed_veto_statuses": ["applied"], "expected_bucket": "must_drop", "expected_recall_keys": [좌/우팔 arm + head_neck canonical FaultKey]}` (≤50/major 아님 — `grep -n '"max_score": 75' backend/research/spikes/eval_stillframe_veto_manifest.json`; kip-up row 에 `"max_score": 50` 부재). `expected_recall_keys` 의 각 객체가 `part_scope`/`side`/`keypoint_set`/`fault_kind` 키를 가진다(23-01 Task 2 FaultKey 정합 — `grep -nE '"part_scope"|"keypoint_set"|"fault_kind"' backend/research/spikes/eval_stillframe_veto_manifest.json`). 한글 표시라벨(`왼팔`/`고개·목`)은 게이트 비교 필드가 아니라 optional `display_label_ko` 로만 등장. SCORE-09 sensitivity 행(미보유/above-cutoff)은 manifest 에 부재 (D-14 amended HIGH-1).
    - **(D-16 HIGH-1 freeze 는 별도 task)** lock.json 생성은 Task 1 이 아니라 신설 **Task 1b(freeze)** 가 소유한다 — 본 task 는 manifest.json + result JSON 스키마만 만들고, result JSON 에 `manifest_lock_git_commit`/`manifest_sha256`/`run_git_commit`/`worktree_dirty_at_run` 를 transcribe/캡처하는 코드 경로가 존재한다 (`grep -n "manifest_lock_git_commit\|run_git_commit" research/spikes/eval_stillframe_veto.py`).
    - python ast parse 통과.
  </acceptance_criteria>
  <done>순차 eval harness 가 FULL production 경로(collect→apply, to_trace_dict 직렬화)를 통과하고, multi-arm result shape(cases[].arms)로 dump 하며, run-commit ancestry(lock∈ancestor(run HEAD), worktree clean 아니면 실행 거부 + run_git_commit/worktree_dirty_at_run 기록)를 강제하고, 동일-모델 baseline 재실행 + 확장 매트릭스(case class+abstention_allowed+budget_stress) + arm 별 격리 cold/warm 결정론 + cost/latency 를 측정하며, FP/alignment-abstention/resource-incomplete 를 3-way 분리해 manifest gate_policy/regression_pairs(D-17 MED-1/MED-2)에서 coverage·margin 을 재계산하고 case-class 게이트 + coverage 임계 + completion_pass(main applied⟹samplingComplete=true) 로 evaluable 입력에서 특이도·완료를 증명하고, **Phase 20-04 흡수 게이트(정은지 95~100 / kip-up moderate≤75 / score 결정론 cold·warm / EVAL18 변별 4쌍 퇴행0, D-14)** 를 still-frame 경로에서 OWN 하며, recall_set 을 FaultKey.to_dict 로 산출하고, machine-checkable 필드로 dump 하고 프롬프트는 generic 이며 키를 로그하지 않는다.</done>
</task>

<task type="auto">
  <name>Task 1b: pre-Pod manifest freeze 스크립트 — clean worktree 강제 + lock.json 생성 + regression_pairs/gate_policy snapshot (D-16 HIGH-1/HIGH-2, D-17 MED-1/MED-2)</name>
  <files>backend/research/spikes/freeze_stillframe_veto_manifest.py</files>
  <read_first>
    - backend/research/spikes/eval_stillframe_veto_manifest.json (Task 1 (I)/(K) — freeze 가 sha256/row ids/expected-policy/regression_pairs/gate_policy 를 읽어 lock 으로 박제)
    - backend/research/spikes/eval_stillframe_veto_manifest.lock.json (Task 1 — freeze 가 생성하는 산출물 스키마)
    - 23-DIRECT-REVIEW-ITERATION7.md HIGH-1 전문 (freeze=별도 pre-Pod 단계, 워크플로 7단계, 수용 조건)
    - 23-DIRECT-REVIEW-ITERATION8.md MED-1 + MED-2 전문 (regression_pairs / gate_policy lock snapshot)
    - backend/runpod_inference/requirements.txt (PyYAML 부재 확인 — freeze 도 stdlib 만, D-16 HIGH-2)
    - 23-CONTEXT.md D-16 HIGH-1/HIGH-2 + D-17 MED-1/MED-2
  </read_first>
  <action>
    Manifest freeze 를 Pod 측정과 **별도 pre-Pod 단계** 로 분리하는 스크립트 신설 (D-16 HIGH-1). D-15 의 "manifest+lock 동시 생성" 워크플로는 "결과 본 뒤 relabel→lock→commit" 를 여전히 허용하므로(hash 일치는 결과 전 freeze 를 증명 못 함), freeze 를 독립 실행으로 강제한다.
    호출 계약(고정): `cd backend && python research/spikes/freeze_stillframe_veto_manifest.py --manifest research/spikes/eval_stillframe_veto_manifest.json --created-at <ISO8601> [--lock research/spikes/eval_stillframe_veto_manifest.lock.json]`.
    동작(전부 stdlib `json`/`hashlib`/`subprocess` 만 — PyYAML 등 외부 의존 0, D-16 HIGH-2):
    (1) **clean-worktree 강제 (REFUSE on dirty/uncommitted):** `git status --porcelain` 로 worktree 가 dirty 면(특히 manifest 가 uncommitted/modified 면) **non-zero exit 으로 거부** — manifest 가 commit 된 상태에서만 freeze 한다. 워크플로 강제: ① manifest.json 작성 → ② commit → ③ **이 freeze 를 clean worktree 에서 실행** → ④ lock.json commit → ⑤ Pod 가 그 commit pull.
    (2) **lock.json emit:** `manifest_sha256`(manifest.json 바이트의 sha256) + `lock_git_commit`(freeze 시점 `git rev-parse HEAD` — assert 가 result.run_git_commit 의 ancestor 인지 검증할 기준, D-17 HIGH-1) + `dirty_worktree`(false 여야 — true 면 애초에 (1)에서 거부) + `created_at`(**`--created-at` 주입 인자, `time.time()`/`datetime.now()` 직접 호출 금지** — 결정론·재현성) + `row_ids`(manifest row_id list) + `expected_policy`(row 별 expected_bucket/allowed_veto_statuses/expected_severity/max_score/min_score/abstention_allowed/budget_stress/alignment_unverifiable/required_arms/optional_arms/expected_recall_keys 스냅샷) + **`regression_pairs`(manifest top-level 4쌍 그대로 snapshot — D-17 MED-1)** + **`gate_policy`(manifest top-level gate_policy 그대로 snapshot — D-17 MED-2)**.
    (3) freeze 가 성공하면 lock.json 을 쓰고 exit 0, 거부/실패 시 명확한 메시지 + non-zero exit.
    **임계/프롬프트 주입 0** — freeze 는 라벨을 읽어 박제만 한다(생성·변경 아님, D-06).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && python -c "import ast; ast.parse(open('research/spikes/freeze_stillframe_veto_manifest.py').read()); print('parse ok')" && grep -nE "git status --porcelain|--created-at|hashlib|rev-parse|regression_pairs|gate_policy" research/spikes/freeze_stillframe_veto_manifest.py && ! grep -nE "import yaml|time\.time\(\)|datetime\.now\(\)" research/spikes/freeze_stillframe_veto_manifest.py && echo "freeze stdlib-only + clean-worktree + policy snapshot ok"</automated>
  </verify>
  <acceptance_criteria>
    - `freeze_stillframe_veto_manifest.py` 가 `--manifest`/`--created-at` CLI 인자 + `def main` 을 가진다 (`grep -nE "argparse|--manifest|--created-at|def main"`).
    - **clean-worktree 강제:** worktree 가 dirty 또는 manifest 가 uncommitted 면 **non-zero exit 으로 거부** 하는 코드가 존재한다 (`grep -nE "git status --porcelain|dirty|uncommitted"` + 코드 리뷰 — D-16 HIGH-1).
    - lock.json 이 `manifest_sha256` + `lock_git_commit`(rev-parse HEAD) + `dirty_worktree` + `created_at`(주입 인자) + `row_ids` + `expected_policy` + **`regression_pairs`(D-17 MED-1)** + **`gate_policy`(D-17 MED-2)** 스냅샷을 담도록 emit 한다 (`grep -nE "manifest_sha256|lock_git_commit|rev-parse|created_at|regression_pairs|gate_policy"`).
    - **stdlib only (D-16 HIGH-2):** `import yaml`/PyYAML 부재, `time.time()`/`datetime.now()` 직접 호출 0(created_at 은 주입), `json`/`hashlib`/`subprocess` 만 (`! grep -nE "import yaml|time\.time\(\)|datetime\.now\(\)"`).
    - ast parse 통과.
  </acceptance_criteria>
  <done>freeze 스크립트가 clean worktree 에서만 실행되어(dirty/uncommitted 면 거부) manifest sha256 + lock_git_commit(HEAD) + created_at(주입) + expected-policy + regression_pairs + gate_policy 를 lock.json 으로 박제하고, stdlib 만 쓰며, Pod 측정과 분리된 pre-Pod 단계로 동작한다 — post-result relabel 이 구조적으로 불가능해진다.</done>
</task>


<task type="auto">
  <name>Task 2: non-zero assert gate 스크립트 + mandatory behavioral test — frozen manifest(JSON)+lock 검증 + run-commit ancestry + multi-arm shape + locked gate_policy/regression_pairs 재계산 + canonical FaultKey recall + row 재계산 (D-15 HIGH-2 + MED-2, D-16 HIGH-1/HIGH-2/MED-1/MED-2, D-17 HIGH-1/HIGH-2/MED-1/MED-2/MED-3)</name>
  <files>backend/research/spikes/assert_stillframe_veto_gate.py, backend/tests/test_assert_stillframe_veto_gate.py</files>
  <read_first>
    - backend/research/spikes/eval_stillframe_veto.py (Task 1 — 결과 JSON 필드 스키마(cases[].arms, run-commit 필드) + manifest 인자. assert 가 이 JSON 을 검증)
    - backend/research/spikes/eval_stillframe_veto_manifest.json + .lock.json (Task 1 (I)/(K) — frozen manifest + lock(regression_pairs/gate_policy 포함); assert 가 hash/lock/row/policy 정합 검증)
    - backend/research/spikes/eval_stillframe_veto_manifest.lock.json (Task 1b — freeze 가 생성한 lock; assert 가 manifest sha256 정합 + lock_git_commit 이 result.run_git_commit 의 ancestor 인지 검증)
    - backend/research/spikes/freeze_stillframe_veto_manifest.py (Task 1b — lock 스키마 owner. assert 가 읽는 lock_git_commit/manifest_sha256/expected_policy/regression_pairs/gate_policy 출처)
    - 23-DIRECT-REVIEW-ITERATION6.md HIGH-2 + MED-2 전문 (non-zero assert 최소검사 목록 + lock 정합)
    - 23-DIRECT-REVIEW-ITERATION7.md HIGH-1(lock ancestor) + HIGH-2(stdlib only) + MED-1(canonical FaultKey recall) + MED-2(mandatory behavioral test) 전문
    - 23-DIRECT-REVIEW-ITERATION8.md HIGH-1(run-commit ancestry) + HIGH-2(multi-arm shape) + MED-1(regression_pairs) + MED-2(gate_policy) + MED-3(FaultKey single owner) 전문
    - .planning/phases/23-mode-1-recall-still-frame-veto-dtw-key-frame/23-01-PLAN.md Task 2 (canonical FaultKey(part_scope/side/keypoint_set/fault_kind) 단일 owner FaultKey.to_dict/from_dict — assert recall 비교 기준)
    - 23-CONTEXT.md D-14 amended (kip-up moderate≤75) + D-15 (assert 스크립트 / frozen manifest + lock) + D-16 (freeze 분리 / JSON / canonical FaultKey / behavioral test) + D-17 (run-commit ancestry / multi-arm shape / regression_pairs / gate_policy / FaultKey single owner)
    - .planning/phases/20-v2-gemini/20-04-EVAL-EVIDENCE.md (kip-up 75/moderate 근거 — assert 의 kip-up 게이트가 moderate≤75 로 도출됨을 확인)
  </read_first>
  <action>
    Phase 20-04 가 가졌던 terminal-gate 강제력을 still-frame eval 에 부여하는 **non-zero exit assert 스크립트** 신설(D-15 HIGH-2). "machine-checkable JSON" 만으론 약함 — 필드 누락/`"true"` 문자열/stale/row 실패 은닉/precomputed 신뢰가 통과해버린다. 본 스크립트는 결과 JSON + frozen manifest + lock 을 받아 **어떤 실패에서도 non-zero exit** 하고 compact gate report(통과/실패 게이트별 한 줄)를 stdout 에 emit 한다.
    호출 계약(고정): `cd backend && python research/spikes/assert_stillframe_veto_gate.py --results research/spikes/reports/eval_stillframe_veto_phase23.json --manifest research/spikes/eval_stillframe_veto_manifest.json` (lock 은 manifest 와 같은 디렉터리에서 `.lock.json` 자동 탐색 또는 `--lock` 인자). **stdlib 만 사용 (D-16 HIGH-2)** — `json`/`hashlib`/`subprocess`(git). `import yaml` 금지(PyYAML 은 RunPod requirements 에 없어 Pod-terminal assert 가 import 단계에서 죽음).
    **최소 검사(전부 — 하나라도 실패 시 non-zero):**
    (1) **lock 정합 + run-commit ancestry (MED-2 + D-16 HIGH-1 + D-17 HIGH-1):** manifest sha256 == lock.manifest_sha256 (불일치 시 fail), lock.dirty_worktree==true 면 gate 거부(fail), **run-commit ancestry chain 검증:** (a) `git merge-base --is-ancestor <lock.lock_git_commit> <result.run_git_commit>` — lock_git_commit 이 **result.run_git_commit** 의 ancestor 아니면 fail(eval 이 freeze 전에 돌았으면 lock 이 측정 commit 의 ancestor 가 아님 → post-result freeze 차단, D-17 HIGH-1); (b) `git merge-base --is-ancestor <result.run_git_commit> HEAD` — result.run_git_commit 이 현 HEAD 의 ancestor 아니면 fail; (c) `result.worktree_dirty_at_run == false` 아니면 fail; (d) `result.manifest_lock_git_commit == lock.lock_git_commit` (불일치 시 fail — Pod run 이 다른 lock 으로 측정); (e) `result.manifest_sha256 == lock.manifest_sha256` (불일치 시 fail). 모든 expected-policy 필드(expected_bucket/allowed_veto_statuses/expected_severity/max_score/min_score/abstention_allowed/budget_stress/alignment_unverifiable/required_arms/optional_arms/**expected_recall_keys**) 가 lock 스냅샷과 동일(누락·변경 시 fail).
    (2) **multi-arm result shape (D-17 HIGH-2):** result 가 `cases[]{row_id, arms:{...}}` 구조다(평면 row 아님). **manifest row_id set == result cases[].row_id set**(누락/잉여 모두 fail). 각 case 의 manifest `required_arms` 가 result `arms` 에 정확히 1회 존재(결측 시 fail). `arms` 에 manifest required+optional 밖의 unknown arm 이 있으면 fail. production recall/특이도/D-14 게이트는 `arms.production_still` 만, whole-video baseline 비교는 `arms.whole_video_baseline` 만 읽는다.
    (3) **타입/스키마:** 필수 top-level + case-level + arm-level 필드가 **정확한 bool/int/list 타입**으로 존재(문자열 `"true"` 는 bool 아님 → fail; `null`/누락 → fail).
    (4) **재계산(precomputed 불신):** `coverage_pass`/`completion_pass`/`determinism_cold`/`determinism_warm`/arm 캐시 격리(같은 case 내 cross-arm cacheKey 상이·direct-adapter pre-warm 금지)/D-14 게이트를 **JSON 의 precomputed bool 을 믿지 말고 cases/arms 에서 재계산**해 일치 확인(불일치 시 fail). **coverage·case-class 그룹·EVAL18 pair margin 은 locked manifest `gate_policy`/`regression_pairs` 에서 읽는다(D-17 MED-1/MED-2 — script 상수/row 추론 금지):** `clean_evaluable_count >= gate_policy.clean_coverage_min`; clean gate 케이스 = `gate_policy.clean_gate_case_classes`; alignment abstention 케이스 = `gate_policy.alignment_abstention_case_classes`; EVAL18 margin 은 `regression_pairs[].{fault_row_id, success_row_id, min_margin}` 에서 success_final − fault_final >= min_margin 로 계산.
    (5) **D-14 게이트를 실제 final score 에서 도출:** `kipup_fault_moderate_le_75`(kip-up row 의 `arms.production_still` final severity==moderate **AND** final score<=75 — manifest kip-up row 의 expected_severity/max_score 와 대조; **severity!=moderate 면 severity-misclassification 실패, severity==moderate 이나 score>75 면 cap-application 실패로 row-level 메시지를 구분 출력**, D-14 HIGH-3), `elite_clean_score_in_95_100`(elite row final score>=95 AND <=100), `eval18_discrimination_regression_count`(`regression_pairs` 4쌍 success−fault<min_margin 퇴행 쌍 수, !=0 면 fail) — 전부 결과 JSON 의 `arms.production_still` 실제 final score 에서 산출.
    (5b) **recall 을 canonical FaultKey 로 대조 (D-16 MED-1 + D-17 MED-3):** known-fault row(kip-up 등)의 recall 검증은 result 의 `arms.production_still.recall_set`(`FaultKey.to_dict` 산출 객체) ⊇ manifest row 의 `expected_recall_keys`(`FaultKey.from_dict` 로 검증·정규화한 객체) 로 한다 — `supported_differences[].fault_key`(또는 to_trace_dict() 의 직렬화된 canonical key)와 비교하고 **한글 표시라벨(display_label_ko)과는 비교하지 않는다**. FaultKey 동등성은 정규화된 (part_scope, side, keypoint_set, fault_kind) 4-tuple 일치(23-01 Task 2 단일 owner `FaultKey` 정합). **manifest `expected_recall_keys` 를 `FaultKey.from_dict` 로 검증하다 unknown fault_kind/part_scope/side/keypoint_set 가 나오면 어떤 Pod 결과를 보기 전에 fail**(D-17 MED-3). 표시카피 rename 이 게이트를 깨거나 두 라벨이 한 fault 로 recall 을 부풀리는 것을 차단.
    (6) **abstention/resource 분리:** `resource_limited` 또는 `samplingComplete=false` 인 case(`arms.production_still`)가 manifest `budget_stress=true` 가 아니면 fail(완료실패 — abstention 과 그룹화 금지, D-12 HIGH-2). `abstention_allowed=false` case 가 low_alignment_confidence 면 fail(manifest alignment_unverifiable 만 면제).
    (7) **status 화이트리스트:** result case 의 `arms.production_still` 에 `visionVeto` 누락 / `skipped_error` / `missing_local_video` / manifest allowed_veto_statuses 밖의 status 가 있으면 fail.
    (8) **coverage (D-17 MED-2):** evaluable 분모(clean_evaluable_count)가 **locked `gate_policy.clean_coverage_min`** 미달이면(전부 abstain) fail.
    스크립트는 numpy/PyYAML/외부 의존 없이 **stdlib(json/hashlib/subprocess for git) 만** 사용 (D-16 HIGH-2 — YAML 파싱 금지). FaultKey 정규화는 23-01 Task 2 의 `FaultKey.from_dict`/`to_dict` 를 import 해 쓰거나(권장 — 단일 owner), import 불가 시 동일 locked enum 으로 4-tuple 정규화(어휘 drift 금지). **임계/프롬프트 주입 0(generic, D-06)** — manifest 의 expected-policy/gate_policy/regression_pairs 만 읽고 known-answer 를 프롬프트에 넣지 않는다.
    **(MED-2) mandatory behavioral test 동반 — `backend/tests/test_assert_stillframe_veto_gate.py`:** grep/AST(단어 존재·`sys.exit` 존재)만으론 terminal gate 로 약하다(precomputed bool 신뢰·`"true"` 문자열·missing row·hash mismatch·kip-up major≤50·post-result freeze·unknown arm 오통과 가능). 따라서 pytest 로 assert 스크립트를 **실제 subprocess 실행해 exit code 를 assert** 하는 fixture 를 둔다(또는 import 해 main 의 SystemExit code 캡처). 각 fixture 는 최소 result JSON(cases[].arms 구조) + manifest(regression_pairs/gate_policy/required_arms 포함) + lock 을 임시 생성: ① valid 최소 fixture → **exit 0**; ② manifest hash mismatch → non-zero; ③ missing row(manifest row 가 result cases 에 없음) → non-zero; ④ extra unmanifested row(result cases 에 manifest 없는 row) → non-zero; ⑤ bool 자리 문자열 `"true"` → non-zero; ⑥ non-budget-stress row 의 resource_limited/samplingComplete=false → non-zero; ⑦ kip-up severity=major, score=50 → non-zero(severity 오분류); ⑧ kip-up severity=moderate, score=80 → non-zero(cap 적용 실패); ⑨ EVAL18 pair 가 fault>=success(margin<min_margin) → regression count↑ non-zero; ⑩ **lock_git_commit 이 result.run_git_commit 의 ancestor 아님 → non-zero(D-17 HIGH-1)**; ⑪ **worktree_dirty_at_run==true → non-zero(D-17 HIGH-1)**; ⑫ **case 에 required arm(whole_video_baseline) 결측 → non-zero(D-17 HIGH-2)**; ⑬ **unknown arm 존재 → non-zero(D-17 HIGH-2)**; ⑭ **manifest expected_recall_keys 에 unknown fault_kind → non-zero(D-17 MED-3, Pod 결과 보기 전 fail)**. 각 fixture 가 정확한 exit code 를 assert 한다. (git ancestry fixture 는 fake/monkeypatch 한 merge-base 또는 임시 commit 으로 구성.)
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && python -c "import ast; ast.parse(open('research/spikes/assert_stillframe_veto_gate.py').read()); print('parse ok')" && ! grep -nE "import yaml" research/spikes/assert_stillframe_veto_gate.py && grep -nE "sys.exit|raise SystemExit" research/spikes/assert_stillframe_veto_gate.py && python -m pytest tests/test_assert_stillframe_veto_gate.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `assert_stillframe_veto_gate.py` 가 `--results`/`--manifest` CLI 인자 + `def main` 을 가진다 (`grep -nE "argparse|--results|--manifest|def main"`).
    - 실패 시 **non-zero exit** 한다 (`grep -nE "sys.exit\(1\)|sys.exit\([^0]|raise SystemExit"` 존재 — 실패 경로가 exit 0 아님).
    - lock 정합 검사 존재: manifest sha256 != lock / dirty worktree / expected-policy 변경 시 fail (`grep -niE "sha256|lock|dirty|hashlib"`).
    - **(D-17 HIGH-1 run-commit ancestry)** lock_git_commit 이 **result.run_git_commit** 의 ancestor 인지 + result.run_git_commit 이 현 HEAD 의 ancestor 인지 `git merge-base --is-ancestor` 로 검증하고 아니면 fail; `result.worktree_dirty_at_run==false` + `result.manifest_lock_git_commit==lock.lock_git_commit` + `result.manifest_sha256==lock.manifest_sha256` 검증 (`grep -niE "merge-base|is-ancestor|run_git_commit|worktree_dirty_at_run|manifest_lock_git_commit"`).
    - **(D-17 HIGH-2 multi-arm shape)** result `cases[].row_id` set == manifest row_id set / 각 case 의 required_arms 정확히 1회 / unknown arm fail / production 게이트는 arms.production_still 만 읽는다 (`grep -niE "arms|required_arms|production_still|whole_video_baseline|cases"` + 코드 리뷰).
    - **(D-16 HIGH-2) stdlib only** — `import yaml`/PyYAML 부재 (`! grep -nE "import yaml" research/spikes/assert_stillframe_veto_gate.py`), `json`/`hashlib`/`subprocess` 만.
    - **(D-16 MED-1 + D-17 MED-3) recall 을 canonical FaultKey 단일 owner 로 대조** — result `arms.production_still.recall_set` ⊇ manifest `expected_recall_keys` 를 `FaultKey.from_dict`/`to_dict` 정규화 (part_scope, side, keypoint_set, fault_kind) 4-tuple 일치로 검증하고, manifest 의 unknown fault_kind 가 Pod 결과 보기 전 fail; 한글 표시라벨과는 비교하지 않는다 (`grep -niE "expected_recall_keys|fault_key|FaultKey|from_dict|part_scope"` + 코드 리뷰).
    - **(D-17 MED-1/MED-2) locked policy 재계산** — coverage 는 `gate_policy.clean_coverage_min`, case-class 그룹은 `gate_policy.*_case_classes`, EVAL18 margin 은 `regression_pairs[].{fault_row_id,success_row_id,min_margin}` 에서 읽어 재계산(script 상수/row_id 추론 0) (`grep -niE "gate_policy|regression_pairs|clean_coverage_min|min_margin"` + 코드 리뷰).
    - 게이트를 row/arm 에서 **재계산**(precomputed 필드 trust 안 함) 하는 코드 존재 — `coverage_pass`/`completion_pass`/`determinism`/D-14 게이트를 결과 cases/arms 순회로 재산출 (`grep`/코드 리뷰).
    - kip-up 게이트가 manifest 의 `expected_severity: moderate` + `max_score: 75` 와 대조되고, severity-misclassification vs cap-application 을 **구분하는 row-level 실패 메시지**가 존재한다 (`grep -niE "moderate|misclassif|cap"` + 코드 리뷰 — D-14 HIGH-3). ≤50/major 강제 0.
    - `resource_limited`/`samplingComplete=false` 가 budget_stress 아닌 case 에서 fail / 화이트리스트 밖 status fail / coverage 미달 fail 코드 존재.
    - **(D-16 MED-2 + D-17) mandatory behavioral test** `backend/tests/test_assert_stillframe_veto_gate.py` 가 존재하고 `python -m pytest tests/test_assert_stillframe_veto_gate.py -q` 가 통과하며, valid→exit0 / hash mismatch / missing row / extra row / `"true"` 문자열 / non-budget-stress resource_limited / kip-up major+50(severity 오분류) / kip-up moderate+80(cap 실패) / EVAL18 fault≥success / **lock∉ancestor(run_git_commit) / worktree_dirty_at_run==true / missing required arm / unknown arm / manifest unknown fault_kind** 각각 non-zero exit 를 assert 하는 fixture 를 가진다 (`grep -nE "def test_" backend/tests/test_assert_stillframe_veto_gate.py` 다수 + 각 fixture 가 exit code assert).
    - ast parse 통과.
  </acceptance_criteria>
  <done>stdlib-only non-zero exit assert 스크립트가 frozen manifest(JSON)+lock 정합 + **run-commit ancestry(lock∈ancestor(result.run_git_commit)∈ancestor(HEAD) + worktree_dirty_at_run==false, D-17 HIGH-1)** + **multi-arm result shape(cases[].arms, required_arms 1회, unknown arm 0, D-17 HIGH-2)** + **locked gate_policy/regression_pairs 에서 coverage·margin 재계산(D-17 MED-1/MED-2)** + **canonical FaultKey 단일 owner recall 대조(unknown fault_kind 사전 fail, D-17 MED-3)** + row/arm 재계산(precomputed 불신) + kip-up moderate≤75(severity/cap 구분 메시지)를 강제하고 어떤 실패에서도 non-zero exit 하며, mandatory behavioral test(pytest, 14+ failure fixture)가 실제 exit code 로 이를 검증한다 — JSON 이 "file exists" 로도 grep-pass 로도 post-result freeze 로도 통과하지 못한다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: [POD terminal] Pod 실측 — production 경로 recall 재현 + 위양성 0(evaluable 분모, 확장 매트릭스) + completion_pass + arm 격리 cold/warm 결정론 + 정렬-약 보류 + cost/latency + non-zero assert gate(exit 0) + Phase 20-04 regression subset 흡수 게이트(정은지 95~100 / kip-up moderate≤75 / 점수 결정론 / EVAL18 변별 4쌍 퇴행0, D-14 amended + D-15 + D-16 + D-17: run-commit ancestry / multi-arm shape / manifest-owned pairs+gate_policy / FaultKey single owner / freeze 분리 / JSON manifest / behavioral test)</name>
  <action>
    [POD terminal] Claude 가 Pod SSH 로 `eval_stillframe_veto.py` 를 순차 실행해 23-01/02
    still-frame veto 구현의 정확도·자원 게이트를 **FULL production 경로**로 실측하고 결과 JSON 을
    repo 에 박제한다. 구현은 Pod-free 였으나 라이브 LLM·실 영상·GPU 가 eval 에 필수 —
    [[pod-ops-claude-runs]] 에 따라 Claude 가 Pod 실행 주체, belle 는 도메인 판정·production
    승인. 본 task 는 자동 실행 후 belle 의 게이트 도메인 판정을 받는 blocking-human checkpoint 다.
  </action>
  <what-built>
    Task 1 의 `eval_stillframe_veto.py` 를 Pod GPU 에서 실행해 23-01/02 still-frame veto
    구현의 정확도·자원 게이트를 `_collect_vision_fault_context → _apply_vision_veto` production
    경로로 실측한다. (구현은 Pod-free 였으나 라이브 LLM·실 영상·GPU 가 eval 에 필수 —
    [[pod-ops-claude-runs]] 에 따라 Claude 가 Pod SSH/실행 주체, belle 는 도메인 판정·production
    승인.)
  </what-built>
  <how-to-verify>
    [POD terminal] (Claude 가 Pod SSH 로 실행, belle 는 결과 도메인 판정):
    1. **(D-16 HIGH-1 + D-17 HIGH-1) Pod run 은 manifest.json + lock.json 을 이미 포함한 commit 에서 시작한다.** 선행: ① `eval_stillframe_veto_manifest.json`(regression_pairs/gate_policy/required_arms 포함) 작성·commit → ② 로컬 clean worktree 에서 `freeze_stillframe_veto_manifest.py --created-at <ISO8601>` 실행해 `eval_stillframe_veto_manifest.lock.json` 생성 → ③ lock.json + 23-01/02 commit push ([[gsd-pod-work-push-first]]). 그 다음 Pod 살아있는지 확인 + `git pull` 로 그 commit 을 받는다(manifest+lock 이 측정 전 이미 frozen·committed). Pod eval 은 result JSON 만 생성하고 manifest/lock 을 수정하지 않는다. **eval harness 자체가 시작 시 lock 존재/sha 정합/lock∈ancestor(run HEAD)/worktree clean 을 강제**하므로(D-17 HIGH-1), freeze 전에 eval 을 돌리면 harness 가 실행을 거부한다.
    2. Gemini 키 충전 확인([[gemini-credits-depleted-2026-06-20]] — sweep 전 크레딧 잔량).
    3. `python backend/research/spikes/eval_stillframe_veto.py` 순차 실행 → `eval_stillframe_veto_phase23.json`(cases[].arms 구조 + run_git_commit/worktree_dirty_at_run 기록) 산출.
    3b. **non-zero assert gate 실행 (D-15 HIGH-2 + D-16 + D-17 — JSON 수락 전 필수):** `cd backend && python research/spikes/assert_stillframe_veto_gate.py --results research/spikes/reports/eval_stillframe_veto_phase23.json --manifest research/spikes/eval_stillframe_veto_manifest.json`. assert 는 stdlib 만 쓰므로 Pod 에서 PyYAML 없이 import·실행된다(D-16 HIGH-2). assert 가 lock.manifest_sha256 정합 + **run-commit ancestry(lock_git_commit ∈ ancestor(result.run_git_commit) ∈ ancestor(HEAD) + worktree_dirty_at_run==false, D-17 HIGH-1)** + **multi-arm shape(cases[].arms, required_arms, unknown arm 0, D-17 HIGH-2)** + **locked gate_policy/regression_pairs 재계산(D-17 MED-1/MED-2)** + canonical FaultKey single-owner recall(D-16 MED-1 + D-17 MED-3) + row/arm 재계산 + kip-up moderate≤75 를 검증한다. **이 스크립트가 exit 0 이어야만 JSON 을 수락**한다(exit !=0 면 게이트 실패 — 어느 row/필드/arm 이 깨졌는지 compact report 로 확인 후 23-01/02 환류 또는 eval 재실행). belle 의 게이트 판정은 **raw JSON 이 아니라 assert report** 를 읽는다.
    4. 게이트 판정 (assert gate exit 0 전제, machine-checkable 필드 기준 — 정성 아님):
       - (recall, production seam, 정량 고정 — W2) `arms.production_still` recall_set(apply 가 attach 한 audit, FaultKey.to_dict) ⊇ **{왼팔, 오른팔, 고개·목}** canonical FaultKey(spike_perframe_upper_result.json 알려진 결과). 동일-모델 `arms.whole_video_baseline` recall_set 상체 = **∅/다리만**. 즉 같은 모델 비교에서 production 경로가 상체 3종을 모두 잡아야 통과.
       - (특이도, FP↔alignment abstention↔resource 미완 분리 — D-11 HIGH-2 + D-12 HIGH-2) elite clean / imperfect clean / occluded / spinning 은 evaluable non-fault(none/not_applicable) 요구(abstention_allowed=false; manifest alignment_unverifiable 만 면제); tempo-shifted 는 low_alignment_confidence 허용하되 alignment abstention 으로 카운트(true negative 아님). evaluable 부분집합에서 false_positive_count=0 **AND coverage_pass=true**(전부 abstain → fail; 임계는 manifest gate_policy.clean_coverage_min) **AND completion_pass=true**(budget_stress 아닌 케이스가 resource_limited/samplingComplete=false 면 fail). 1 케이스가 아닌 매트릭스 전체.
       - (완료실패 분리 — D-12 HIGH-2) `resource_incomplete_count` 는 abstention 과 별도 집계되고, budget_stress 표시 안 한 케이스가 resource_limited 면 completion coverage fail.
       - (결정론·arm 격리 분리) cold-cache 반복 verdict 동일(determinism_cold=true) + warm-cache hit 동일(determinism_warm=true). 같은 case 내 whole_video_baseline cacheKey ≠ production_still cacheKey; direct_adapter 가 production gate run 을 pre-warm 안 함. 캐시 hit 로 거짓 증명 아님.
       - (정렬) tempo-shifted/시작점 상이 케이스 = 거짓결함 0, alignment_status=low_alignment_confidence 보류(alignment abstention 카운트).
       - (cost/latency) 각 arm call_count/upload_count/duration_ms 가 MAX_VETO_CALLS/UPLOADS/WALL_S bound 이내.
       - (호출 소유권, D-10 HIGH-1) contextCollectedBeforeCoach=true + contextReusedForAudit=true + geminiCallCount=1 (collect 가 coach 전 1회 호출, apply 재호출 0). Gemini 2회 호출 또는 collect-after-coach 면 fail.
       - (Phase 20-04 regression subset 흡수 게이트, D-14 amended — still-frame 경로에서 OWN, regression subset 만 supersedes 20-04; SCORE-09 별도 pending) 네 게이트 전부 PASS: elite_clean_score_in_95_100=true(정은지 정타 95~100, V-3) AND fault_cap_holds=true + kipup_fault_moderate_le_75=true(kip-up fault = moderate, 점수 ≤75, V-1) AND score_determinism_cold=true + score_determinism_warm=true(같은 입력=같은 최종 점수, V-4) AND eval18_discrimination_regression_count=0(manifest regression_pairs 4쌍 power-spin/peter-pan/elbow-twist/pdshape margin>=min_margin 퇴행 0, V-2). 어느 하나라도 어긋나면 fail → 23-01/02 gap 환류 또는 cap 재점검.
    5. 결과 JSON 을 repo `backend/research/spikes/reports/eval_stillframe_veto_phase23.json` 로 커밋.
  </how-to-verify>
  <resume-signal>belle 가 "통과"(전 게이트) 또는 회귀 항목을 보고하면 재개. recall 미복구/위양성 증가/coverage 미달(전부 abstain)/completion 미달(전부 timeout=resource_limited)/결정론 깨짐/arm 캐시 누수/거짓결함/bound 초과/**Phase 20-04 흡수 게이트 위반(정은지 정타 95~100 이탈·kip-up severity ≠ moderate 또는 점수 >75·최종점수 비결정·EVAL18 4쌍 변별 퇴행, D-14)**/assert gate 실패(run-commit ancestry·multi-arm shape·FaultKey·gate_policy 위반) 시 → 23-01 또는 23-02 gap 으로 환류.</resume-signal>
  <acceptance_criteria>
    - `eval_stillframe_veto_phase23.json` 이 repo reports 디렉터리에 존재하고 **multi-arm shape(cases[]{row_id, arms:{production_still, whole_video_baseline, ...}}, D-17 HIGH-2)** + 확장 매트릭스 전 케이스 결과 + machine-checkable 필드(clean_*_count/resource_incomplete_count/abstention_allowed/budget_stress/coverage_pass/completion_pass 포함) + **run_git_commit/worktree_dirty_at_run/manifest_lock_git_commit/manifest_sha256(D-17 HIGH-1)** + arm/cache_namespace/run_id + 런타임 trace 필드(entrypoint/selectorVersion/studentFrameIndices/referenceFrameIndices/alignmentConfidence/cacheKey/cacheHit/pathUsed + contextCollectedBeforeCoach/contextReusedForAudit/geminiCallCount, to_trace_dict 직렬화)를 포함한다.
    - **(D-15 HIGH-2 + D-16 + D-17 non-zero assert)** `assert_stillframe_veto_gate.py --results ... --manifest ...` 가 **exit 0** 으로 통과한다 (frozen manifest(JSON)+lock 정합 + **run-commit ancestry(lock∈ancestor(result.run_git_commit)∈ancestor(HEAD) + worktree_dirty_at_run==false, D-17 HIGH-1)** + **multi-arm shape(D-17 HIGH-2)** + **locked gate_policy/regression_pairs 재계산(D-17 MED-1/MED-2)** + canonical FaultKey single-owner recall(D-16 MED-1 + D-17 MED-3) + row/arm 재계산 게이트 + kip-up moderate≤75 severity/cap 구분 + status 화이트리스트 전부 PASS). assert 는 stdlib 만 써 Pod 에서 PyYAML 없이 실행(D-16 HIGH-2). assert report 가 belle 판정 입력 — raw JSON 아님.
    - production seam(collect→apply) `arms.production_still.recall_set`(apply attach audit, FaultKey.to_dict) 이 알려진 상체 집합 **{왼팔, 오른팔, 고개·목}** canonical FaultKey 를 포함하고, 동일-모델 `arms.whole_video_baseline` 상체 recall = ∅ (apples-to-apples, W2). recall-presence 는 측정값이며 사람 점수 라벨이 아님([[analysis-objectivity-no-human-scores]]).
    - 확장 매트릭스 evaluable 부분집합(elite/imperfect clean/occluded/spinning, abstention_allowed=false)에서 false_positive_count = 0 **AND coverage_pass=true**(전부 abstain 통과 불가; 임계 manifest gate_policy.clean_coverage_min) **AND completion_pass=true**(budget_stress 아닌 케이스 resource_limited 면 통과 불가 — D-12 HIGH-2). tempo-shifted 는 alignment abstention 카운트.
    - `resource_incomplete_count` 가 alignment abstention 과 분리 집계되고 budget_stress 표시 안 한 케이스에서 0 (완료실패 분리 — D-12 HIGH-2).
    - determinism_cold=true 그리고 determinism_warm=true (cold/warm 분리 결정론). 같은 case 내 whole_video_baseline cacheKey ≠ production_still cacheKey (arm 격리 — D-12 MED-2 + D-17 HIGH-2).
    - 정렬-약 케이스: false positive 0 (보류 또는 none, alignment_status 기록, alignment abstention 카운트).
    - 각 arm call_count/upload_count/duration_ms 가 23-01 bound 이내 (cost/latency 게이트 pass).
    - contextCollectedBeforeCoach=true + contextReusedForAudit=true + geminiCallCount=1 (collect-before-coach seam + verdict 1회 재사용 — D-10 HIGH-1).
    - **(Phase 20-04 regression subset 흡수, D-14 amended — regression subset 만 supersedes 20-04; SCORE-09 별도 pending)** elite_clean_score_in_95_100=true(정은지 정타 95~100) + fault_cap_holds=true + kipup_fault_moderate_le_75=true(kip-up fault = moderate, 점수 ≤75) + score_determinism_cold=true + score_determinism_warm=true(같은 입력=같은 최종 점수) + eval18_discrimination_regression_count=0(manifest regression_pairs 4쌍 margin>=min_margin 퇴행 0, 4쌍 margin 기록). still-frame production 경로에서 측정(옛 whole-video 별도 캘리브레이션 아님). SCORE-09 sensitivity 행 부재(흡수 안 함).
  </acceptance_criteria>
  <done>non-zero assert gate(exit 0, run-commit ancestry + multi-arm shape + locked policy + FaultKey single owner 전부 PASS) 통과 후 Pod 실측 게이트(production 경로 recall 복구 / evaluable 분모에서 위양성 0 + coverage_pass + completion_pass / arm 격리 cold·warm 분리 결정론 / 정렬-약 보류 / resource 미완 분리 / cost·latency bound / **Phase 20-04 regression subset 흡수: 정은지 95~100 + kip-up moderate≤75 + 최종점수 결정론 + EVAL18 변별 4쌍 퇴행 0, D-14 amended; SCORE-09 별도 pending**)를 belle 가 assert report 기준으로 판정 통과하고 결과 JSON 이 박제된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval harness → Gemini API | 라이브 LLM 호출 (크레딧 소모, 키 노출 위험) |
| Pod → repo (결과 박제) | 실측 JSON 이 repo 에 commit 됨 |
| eval → production 경로 | eval 이 어댑터 직접호출로 우회하면 게이트가 실제 경로를 검증 못 함 |
| clean case 결과 → 특이도 판정 | 전부 abstain 또는 전부 resource_limited 면 false_positive_count=0 이 특이도를 거짓 증명 (FP/alignment-abstention/resource-incomplete 3-way 분리 + coverage + completion_pass 로 차단) |
| arm 간 캐시 공유 → cold/warm 오도 | production/direct-adapter/baseline 이 캐시를 공유하거나 서로 warm 시키면 결정론·격리 판정 오염 (arm 별 cache_namespace/run_id 로 격리, 같은 case 내 arm 간 키 비교) |
| eval 실행 시점 → lock 시점 | eval 을 freeze 전에 돌리고 결과 본 뒤 lock 을 끼워넣으면 측정-이후-freeze 가 통과 (run-commit ancestry: lock∈ancestor(result.run_git_commit)∈ancestor(HEAD) + worktree_dirty_at_run==false 로 시간순 강제) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-10 | Information Disclosure | eval harness Gemini 키 | mitigate | 키 SSM/env, 로그 금지(T-20-06). 결과 JSON 에 키/PII 0 |
| T-23-11 | Tampering | 동시 호출 결과 오염 | mitigate | harness 순차 강제([[pipeline-not-concurrency-safe-eval-serial]]) — concurrent primitive 0 |
| T-23-12 | Tampering | curve-fit eval (일반화 위반) | mitigate | 프롬프트 generic, recall 자체 라벨셋 측정, known-answer 임계 주입 0(D-06). belle 도메인 판정 게이트 |
| T-23-13 | Tampering | eval 이 production 경로 우회 → 거짓 통과 | mitigate | eval 이 `_collect_vision_fault_context → _apply_vision_veto` 전체 경로 통과(H5). 캐시 hit 로 결정론 거짓 증명 방지 위해 arm 별 격리 cold-cache 반복 분리 측정 |
| T-23-14 | Tampering | 전부 abstain → 위양성 0 거짓 통과 | mitigate | FP↔alignment abstention 분리 카운트(clean_*_count) + case-class 게이트 + coverage 임계(전부 abstain → fail, manifest gate_policy.clean_coverage_min). evaluable 분모에서만 특이도 증명(D-11 HIGH-2) |
| T-23-15 | Tampering | resource_limited timeout → recall/FP 의무 회피 | mitigate | resource_limited 를 abstention 과 분리(resource_incomplete_count), budget_stress 아닌 케이스 resource_limited = completion coverage fail. completion_pass 게이트로 "전부 timeout" 통과 불가(D-12 HIGH-2) |
| T-23-16 | Tampering | arm 간 캐시 pre-warm → cold/warm 오도 | mitigate | arm 별 cache_namespace/run_id 격리; 같은 case 내 cross-arm cacheKey 상이 assertion; direct-adapter 가 production gate run pre-warm 금지(manifest prewarm_intended 만 예외) (D-12 MED-2 + D-17 HIGH-2) |
| T-23-17 | Tampering | still-frame swap 이 cap 게이트(20-04)를 흔듦 (정타 깎임/kip-up 통과/변별 퇴행) | mitigate | Phase 20-04 흡수(D-14) — 본 eval 이 still-frame 경로에서 정은지 95~100 + kip-up moderate≤75 + 점수 결정론 + EVAL18 4쌍 퇴행0 을 OWN·검증, 옛 whole-video 별도 캘리브레이션 금지(드리프트 차단) |
| T-23-DOS | Availability | Gemini 크레딧 고갈 | accept | sweep 전 크레딧 확인([[gemini-credits-depleted-2026-06-20]]) — 운영 체크, 코드 위협 아님 |
| T-23-18 | Tampering | machine-checkable JSON 이 file-exists 로 통과 (필드누락/문자열bool/stale/row 은닉) | mitigate | non-zero exit assert 스크립트(D-15 HIGH-2) — 게이트를 cases/arms 에서 재계산(precomputed 불신), Task 3 가 JSON 수락 전 exit 0 강제. belle 판정 입력 = assert report |
| T-23-19 | Tampering | post-result relabel (hard case 를 budget_stress/alignment_unverifiable 로 재라벨해 통과) | mitigate | **freeze 를 별도 pre-Pod 단계로 분리(D-16 HIGH-1)** — freeze 가 clean worktree(uncommitted/dirty REFUSE)에서만 lock 생성, assert 가 manifest sha256≠lock / dirty / lock_git_commit 시점 관계 / expected-policy 변경 시 fail |
| T-23-24 | Tampering | post-result freeze (eval 먼저 돌리고 결과 본 뒤 freeze→lock 끼워넣기, 수동 사고 포함) | mitigate | **run-commit ancestry(D-17 HIGH-1)** — eval harness 가 시작 시 lock∈ancestor(run HEAD)+worktree clean 강제(아니면 실행 거부), result 에 run_git_commit/worktree_dirty_at_run 기록, assert 가 lock_git_commit ∈ ancestor(result.run_git_commit) ∈ ancestor(HEAD) + worktree_dirty_at_run==false 로 시간순 강제. hash 일치만으론 결과 전 freeze 미증명 |
| T-23-25 | Tampering | multi-arm 결과를 평면 row 로 비교 → baseline/캐시격리 silent-miss 또는 valid 결과 extra-row false-fail | mitigate | **multi-arm result shape 고정(D-17 HIGH-2)** — cases[]{row_id, arms:{...}}, manifest required_arms/optional_arms, assert 가 required_arms 정확히 1회·unknown arm 0 검증, production 게이트는 arms.production_still·baseline 은 arms.whole_video_baseline 만, 캐시격리는 같은 case 내 arm 간 키 |
| T-23-26 | Tampering | EVAL18 pair/coverage 를 row_id 네이밍·script 상수로 추론 → hardcoded known-answer 재유입 또는 frozen 안 거친 게이트 강도 변경 | mitigate | **manifest-owned regression_pairs(D-17 MED-1) + gate_policy(D-17 MED-2)** — pair 매핑·coverage 임계·case-class 그룹을 manifest top-level 이 소유, lock 이 snapshot, assert 가 locked policy 에서만 margin·coverage 재계산(script 상수/row 추론 0) |
| T-23-21 | Tampering | YAML manifest → Pod-terminal assert 가 PyYAML 부재로 import 단계 사망(valid 결과도 게이트 실패) | mitigate | manifest = JSON, freeze·assert stdlib(json/hashlib/subprocess) 만 — RunPod requirements 의존 0 (D-16 HIGH-2) |
| T-23-22 | Tampering | recall 게이트가 한글 표시라벨 비교 또는 어휘 drift → 표시카피 rename 이 게이트 깸 / 두 라벨이 한 fault 로 recall 부풀림 / 의도한 fault 찾아도 fail | mitigate | recall 을 canonical FaultKey **단일 owner**(FaultKey.to_dict/from_dict, 23-01 Task 2)로 대조, manifest 는 expected_recall_keys 객체 list(from_dict 검증, unknown fault_kind 사전 fail), display_label_ko 는 비교 불사용 (D-16 MED-1 + D-17 MED-3) |
| T-23-23 | Tampering | assert 가 grep/AST 만 통과하고 실제로는 precomputed bool 신뢰/`"true"` 문자열/missing row/post-result freeze/unknown arm 오통과 | mitigate | mandatory behavioral test(D-16 MED-2 + D-17) — pytest 가 14+ failure fixture 를 실제 subprocess 실행해 exit code assert(hash/missing row/extra arm/string-bool/resource_limited/kip-up major+50/moderate+80/EVAL18/lock∉ancestor(run)/dirty/missing arm/unknown arm/unknown fault_kind). grep 우회 차단 |
| T-23-20 | Tampering | kip-up 을 ≤50/major 로 억지 격상해 known-answer 통과 (curve-fit) | mitigate | manifest kip-up row = moderate/≤75(D-14 amended, 20-04 evidence 일치), assert 가 severity-misclassification vs cap-application 구분 메시지로 검증. ≤50 강제 금지 |
| T-23-SC | Tampering | npm/pip 설치 | mitigate | 본 plan 신규 외부 패키지 설치 없음. 신규 설치 발생 시 slopcheck + blocking human checkpoint |
</threat_model>

<verification>
- `eval_stillframe_veto.py` ast parse + 순차(동시 primitive 0) + run-commit 필드(run_git_commit/worktree_dirty_at_run) 존재 검증.
- harness 가 `_collect_vision_fault_context`(coach 전) → coach → build_result → `_apply_vision_veto(vision_fault_context=)` seam 을 통과하고 동일-모델 baseline 을 재실행 (런타임 trace contextCollectedBeforeCoach/contextReusedForAudit/geminiCallCount via to_trace_dict + 코드 리뷰).
- multi-arm result shape(D-17 HIGH-2): result 가 cases[]{row_id, arms:{production_still, whole_video_baseline, direct_adapter_still}} 구조이고 production 게이트는 arms.production_still 만 읽는다 (코드 리뷰 + grep).
- run-commit ancestry(D-17 HIGH-1): harness 가 시작 시 lock∈ancestor(run HEAD)+worktree clean 강제(아니면 실행 거부) + run_git_commit/worktree_dirty_at_run 기록; assert 가 lock∈ancestor(result.run_git_commit)∈ancestor(HEAD)+worktree_dirty_at_run==false 검증 (코드 리뷰 + behavioral test fixture).
- FP/alignment-abstention/resource-incomplete 3-way 분리: clean_*_count + resource_incomplete_count + abstention_allowed + budget_stress + coverage_pass + completion_pass 필드 + case-class 게이트 코드 존재 (D-11 HIGH-2 + D-12 HIGH-2).
- arm 격리: arm/cache_namespace/run_id 필드 + 같은 case 내 cross-arm cacheKey 상이 + direct-adapter pre-warm 금지 코드 존재 (D-12 MED-2 + D-17 HIGH-2).
- manifest-owned policy(D-17 MED-1/MED-2): manifest top-level regression_pairs(4쌍) + gate_policy 존재, freeze 가 lock 에 snapshot, assert 가 locked policy 에서 coverage·margin 재계산 (grep + 코드 리뷰).
- non-zero assert gate `assert_stillframe_veto_gate.py` 신설 (D-15 HIGH-2 + D-16 + D-17) — stdlib only(PyYAML 0), frozen manifest(JSON)+lock 정합 + run-commit ancestry(D-17 HIGH-1) + multi-arm shape(D-17 HIGH-2) + locked gate_policy/regression_pairs 재계산(D-17 MED-1/MED-2) + canonical FaultKey single-owner recall(D-16 MED-1 + D-17 MED-3) + row/arm 재계산 + kip-up moderate≤75 severity/cap 구분 메시지, 어떤 실패에서도 non-zero exit. Task 3 가 JSON 수락 전 exit 0 강제.
- pre-Pod freeze 스크립트 `freeze_stillframe_veto_manifest.py` 신설 (D-16 HIGH-1) — clean worktree 강제(dirty/uncommitted REFUSE), lock_git_commit(HEAD)+sha256+created_at(주입)+expected_policy+regression_pairs+gate_policy 를 lock.json 으로 박제. stdlib only. freeze 가 Pod 측정과 분리된 별도 단계.
- frozen manifest = JSON `eval_stillframe_veto_manifest.json`(regression_pairs/gate_policy/required_arms 포함) + freeze 가 생성하는 `.lock.json` (D-15 MED-2 + D-16 HIGH-2 + D-17) — Pod 측정 전 라벨 freeze(post-result relabel 차단). kip-up row = moderate/≤75. expected_recall_keys = canonical FaultKey(D-16 MED-1 + D-17 MED-3).
- mandatory behavioral test `backend/tests/test_assert_stillframe_veto_gate.py` (D-16 MED-2 + D-17) — pytest 가 valid→exit0 / hash mismatch / missing row / extra row / `"true"` 문자열 / non-budget-stress resource_limited / kip-up major+50 / kip-up moderate+80 / EVAL18 fault≥success / lock∉ancestor(run_git_commit) / worktree_dirty_at_run==true / missing required arm / unknown arm / unknown fault_kind 각각 non-zero 를 실제 exit code 로 검증. `python -m pytest tests/test_assert_stillframe_veto_gate.py -q` 통과.
- Pod 실측 게이트 belle 판정 통과 (Task 3 checkpoint) — assert report 기준, evaluable 분모 위양성 0 + coverage_pass + completion_pass.
- `eval_stillframe_veto_phase23.json` repo 박제 (multi-arm shape + 확장 매트릭스 + telemetry + run-commit 필드).
- **Phase 20-04 regression subset 흡수 게이트(D-14 amended):** harness 가 still-frame 경로에서 elite_clean_score_in_95_100 + fault_cap_holds(kipup_fault_moderate_le_75=moderate/≤75) + score_determinism_cold/warm + eval18_discrimination_regression_count(=0, manifest regression_pairs 4쌍 margin 기록) 필드를 산출·판정하고 Pod 실측이 전부 PASS (옛 whole-video 별도 캘리브레이션 아님, regression subset 만 supersedes Phase 20-04). **SCORE-09(일반화/sensitivity)는 흡수 안 함 — 별도 pending(Phase 20/후속).**
- main eval applied⟹samplingComplete=true (D-13 HIGH-2 Option A) — applied+samplingComplete=false 는 resource_incomplete 로 분류, cold·warm 이 completion-pass 분류를 바꾸지 못함.
</verification>

<success_criteria>
- production seam(collect→coach→build_result→apply) kip-up 상체 recall 복구 실측, 동일-모델 baseline 대비 + verdict 1회 (Success #1 effect size 재현, H5 + D-10 HIGH-1).
- evaluable 분모에서 위양성 0 + coverage_pass + completion_pass + arm 격리 cold/warm 분리 결정론 실측 (Success #2/#6, D-11 HIGH-2 + D-12 HIGH-2/MED-2).
- 정렬-약 케이스 거짓결함 비생성 실측(alignment abstention 카운트) + resource 미완 분리 (Success #3 검증 task, D-12 HIGH-2).
- cost/latency 게이트 + machine-checkable pass/fail (Success #6, H5).
- recall 자체 라벨셋 측정 + generic 프롬프트 (D-06 일반화 hard gate).
- **terminal gate tamper-proof (D-17):** run-commit ancestry(post-result freeze 차단) + multi-arm result shape(baseline/캐시격리 silent-miss 차단) + manifest-owned regression_pairs+gate_policy(hardcoded known-answer/우회 차단) + canonical FaultKey 단일 owner recall(어휘 drift 차단) + behavioral test 가 전부 강제됨.
- **Phase 20-04 regression subset 흡수 게이트(D-14 amended + D-15, regression subset 만 supersedes Phase 20-04):** still-frame veto production 경로에서 정은지 정타 95~100 + kip-up fault = moderate, 점수 ≤75(≤50/major 억지 격상 금지) + 최종 점수 결정론(cold·warm) + EVAL18 변별 4쌍(power-spin/peter-pan/elbow-twist/pdshape) margin>0 퇴행 0 을 OWN·실측, **non-zero assert gate(stdlib-only, JSON manifest, 별도 freeze 단계, run-commit ancestry, multi-arm shape, manifest-owned policy, canonical FaultKey single owner recall, mandatory behavioral test — D-16 + D-17)로 강제**. **SCORE-09(일반화/sensitivity — 미보유+above-cutoff)는 흡수하지 않고 별도 pending(Phase 20/후속).**
</success_criteria>

<output>
Create `.planning/phases/23-mode-1-recall-still-frame-veto-dtw-key-frame/23-03-SUMMARY.md` when done
</output>
