---
phase: 24-transparent-deduction-scoring
plan: 02
type: execute
wave: 2
depends_on: [24-01]
files_modified:
  - backend/shared/python/sunity_shared/analysis/vision_veto.py
  - backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py
  - backend/functions/pipeline/app.py
  - backend/shared/python/sunity_shared/firestore_admin.py
  - app/src/types/analysis.ts
  - backend/shared/python/sunity_shared/models.py
  - docs/contract.md
  - backend/tests/test_pipeline_deduction_seam.py
  - backend/tests/test_vision_veto.py
  - backend/tests/test_pipeline_vision_gate.py
  - backend/tests/test_pipeline_mode3.py
  - backend/tests/test_gemini_vision_scorer.py
autonomous: true
requirements: [SCORE-10, SCORE-13, SCORE-15, SCORE-16, SCORE-09]
must_haves:
  truths:
    - "vision_veto.py no longer contains SEVERITY_CAP, apply_downward_cap, or SEVERITY_CAP_PROVENANCE; gemini_vision_scorer.py docstrings (L5/L129/L134) no longer describe severity as an apply_downward_cap input — they describe it as a criterion pointer (HIGH-5)"
    - "_process produces overallScore = deductionBreakdown.final via deduction_engine.tally at the old veto seam, fed the un-vetoed measured overallScore + a NAMED measured-deviation substrate built by _build_deduction_measured_deviations (HIGH-3) — never a dimension SCORE mistaken for a deviation, never 100-minus-Gemini"
    - "baseline_kind is DERIVED per-move by STRING-matching the technique profile's name/motion_id against an explicit floor-move / horizontal-move name list (NOT profile.category, which only encodes flexibility/strength/spin/transition/unknown) — computed ONCE at the _process seam (where profile is in scope) and threaded into _build_vision_quantification_result + the tally; kip-up=floor is exercised in production ONLY WHEN the move is named (Gemini/studio term 'kip-up'); for truly-unknown/unnamed moves baseline honestly defaults to hip_line (no universal per-move claim)"
    - "deductionBreakdown (the OBJECT {baseline, records, final, coverageGaps, fallback} from breakdown.to_dict()) is set on result['deductionBreakdown'] at the seam (visionVeto analog), validated inside complete_analysis before payload['result']=dict(result) by a scoped validator that accepts a top-level dict whose records/coverageGaps are lists of flat scalar dicts; NO complete_analysis kwarg added (HIGH-1)"
    - "Phase 20/23 working parts are preserved: _apply_score_suppression (Mode3), GEMINI_VISION_VETO_ENABLED toggle, Phase 23 still-pair fan-out + quantification ordering, Mode3 mode3_held passthrough, AND VisionFaultContext.cap_would_apply / eligible_for_coach — coach root-cause injection (app.py L3003) PRESERVED as band-free coach-root-cause eligibility (coachRootCauseEligible meaning): cap_would_apply = (severity in ('moderate','major')), a severity-only pointer that fires for moderate/major and not minor/none. This is band-free CONTINUITY, NOT byte-identical legacy behavior (the old cap_would_apply also depended on the score already being below the cap, so it was NOT identical — HIGH-6)."
    - "baseline_kind is threaded as a plain string into _apply_vision_veto / _apply_vision_veto_from_context (computed once at the _process seam where profile is in scope) — the engine never receives the technique profile, so there is no NameError surface in the apply paths"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/vision_veto.py"
      provides: "Band layer removed; quantification/FaultKey/baseline/worst-pose helpers preserved; to_audit_dict reshaped band-free (cap_applied retired, tally final/status emitted)"
      contains: "body_relative_notches"
    - path: "backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py"
      provides: "Docstrings reframed — severity is a criterion pointer (not an apply_downward_cap input); objectivity guards (_SCORE_PATTERN, score-free VisionVerdict) unchanged"
      contains: "VisionVerdict"
    - path: "backend/functions/pipeline/app.py"
      provides: "Seam replaced: deduction_engine.tally fed measured overallScore + _build_deduction_measured_deviations substrate → result['overallScore']=breakdown.final + result['deductionBreakdown']=breakdown.to_dict(); baseline_kind derived per-move"
      contains: "deduction_engine"
    - path: "backend/shared/python/sunity_shared/firestore_admin.py"
      provides: "complete_analysis validates result['deductionBreakdown'] (top-level dict; records/coverageGaps flat list-of-dicts) inside the existing result-dict flow, NO new kwarg"
      contains: "deductionBreakdown"
    - path: "app/src/types/analysis.ts"
      provides: "visionVeto.capApplied retired→tallyFinal (band retired); result.tsx reframe consumer updated to deductionBreakdown.final"
      contains: "tallyFinal"
    - path: "backend/tests/test_pipeline_deduction_seam.py"
      provides: "Seam integration: band-removed, final from tally over measured substrate, score-not-deviation regression, breakdown OBJECT set on result + validated, baseline_kind derived, Mode3 suppression preserved, toggle preserved"
      contains: "def test_"
  key_links:
    - from: "pipeline/app.py _process seam"
      to: "deduction_engine.tally"
      via: "replaces _apply_vision_veto band mutation; fed score_result['overallScore'] (un-vetoed measured) + _build_deduction_measured_deviations substrate"
      pattern: "deduction_engine\\.tally"
    - from: "pipeline/app.py _build_deduction_measured_deviations"
      to: "dimensions.extension_deviation / line_score + bodyRelativeNotches.delta_notches"
      via: "builds the NAMED measured-deviation substrate (HIGH-3) — never feeds a dimension SCORE as a deviation"
      pattern: "_build_deduction_measured_deviations"
    - from: "pipeline/app.py"
      to: "result['deductionBreakdown']"
      via: "breakdown.to_dict() (OBJECT) set on result dict (visionVeto analog), persisted via payload['result']=dict(result)"
      pattern: "deductionBreakdown"
    - from: "firestore_admin.complete_analysis"
      to: "result.get('deductionBreakdown') validator"
      via: "scoped top-level-dict validator (records/coverageGaps flat) before payload['result']=dict(result), mirroring _validate_recommended_exercises invocation but reading from result"
      pattern: "deductionBreakdown"
    - from: "pipeline/app.py _baseline_kind_for_profile (computed once at the _process seam)"
      to: "_build_vision_quantification_result AND _apply_vision_veto"
      via: "name/motion_id string-match → baseline string (floor/pole_vertical/hip_line); threaded into both quantification + apply paths (engine never gets the profile)"
      pattern: "_baseline_kind_for_profile"
---

<objective>
Wire the Plan 01 engine into the single production scoring seam and remove the band layer. Delete `SEVERITY_CAP` / `apply_downward_cap` / `SEVERITY_CAP_PROVENANCE` from `vision_veto.py` (keeping all measurement/FaultKey/baseline/worst-pose helpers) and reframe the `gemini_vision_scorer.py` docstrings (L5/L129/L134) that still describe severity as an `apply_downward_cap` input to describe it as a CRITERION POINTER (HIGH-5 — no code/schema change, objectivity guards unchanged). Replace the band mutation inside `_apply_vision_veto` / `_apply_vision_veto_from_context` / `_collect_vision_fault_context` (all currently call `apply_downward_cap`) with `deduction_engine.tally(...)`, FED the un-vetoed measured `score_result["overallScore"]` (assemble.py:736, the kismam/dimensions overall) + a NAMED measured-deviation substrate built by a new `_build_deduction_measured_deviations(...)` helper (HIGH-3 — so a dimension SCORE (0-100) is NEVER fed as a deviation (deg)). Set `result['overallScore'] = breakdown.final` and `result['deductionBreakdown'] = breakdown.to_dict()` (the OBJECT, HIGH-1) ON THE RESULT DICT (the `visionVeto` persistence analog — no `complete_analysis` kwarg). Derive `baseline_kind` per-move from the technique profile (BLOCKER B — floor for kip-up/floor moves named, hip_line honest default). Reshape `vision_veto.to_audit_dict` to be band-free (retire `cap_applied`/`capApplied`, emit tally final + status). Validate `result['deductionBreakdown']` (a top-level dict whose records/coverageGaps are flat) inside `complete_analysis` before `payload["result"]=dict(result)`. Preserve Phase 20/23 working parts unchanged: `_apply_score_suppression` (Mode3), the `GEMINI_VISION_VETO_ENABLED` toggle (reuse), the Phase 23 still-pair fan-out + collect→coach→quantification ordering, the `vision_fault_context is None` legacy fallback, AND the coach-injection trigger (`cap_would_apply` now a band-free severity-only pointer that fires for moderate/major — band-free CONTINUITY of today's trigger, NOT byte-identical legacy, HIGH-6).

Purpose: this is the ONE seam (분기 0, 코드 1벌) where the forbidden severity→fixed-ceiling band lived. Replacing it here — fed the measured-geometry substrate — makes the production overallScore a transparent tally output everywhere (Lambda + RunPod share `_process`) while keeping Phase 20's false-positive defense (a fault video still drops from measured geometry even if Gemini is silent or the reference pair is missing). The mandate is "replace only the band, preserve working parts" — coach injection is a PRESERVED working part, kept as band-free coach-root-cause eligibility for the same moderate/major candidate verdicts.

Output: band-free `vision_veto.py` + reframed `gemini_vision_scorer.py` docstrings, engine-wired `_process` with a named substrate builder + per-move baseline, breakdown OBJECT set on result + validated in `complete_analysis`, capApplied consumer migrated, and seam integration tests (incl. the broadened caller-test migration).

Deferred (out of scope, Phase 24): whether MINOR faults should ALSO trigger coach root-cause injection is a separate product decision — Phase 24 keeps the existing moderate/major trigger and does not widen or narrow it.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-transparent-deduction-scoring/24-CONTEXT.md
@.planning/phases/24-transparent-deduction-scoring/24-RESEARCH.md
@.planning/phases/24-transparent-deduction-scoring/24-PATTERNS.md
@.planning/phases/24-transparent-deduction-scoring/24-01-PLAN.md
@backend/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove band layer from vision_veto.py + reframe gemini_vision_scorer docstrings + reshape to_audit_dict band-free (keep all measurement helpers)</name>
  <files>backend/shared/python/sunity_shared/analysis/vision_veto.py, backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py</files>
  <read_first>
    - backend/shared/python/sunity_shared/analysis/vision_veto.py (lines 14-40 docstring band section; `SEVERITY_CAP` L65-70, `SEVERITY_CAP_PROVENANCE` L89-101, `apply_downward_cap` L104-118; lines 690-738 `to_audit_dict` (signature `(*, final_status, cap_applied=None, quantification=None)` at L690-693) — emits `audit["capApplied"] = cap_applied` at L704-705 when final_status=='applied' (RESHAPE this, see action); KEEP line 201 FaultKey, 233-242 _KEYPOINT_SET_BY_KEYWORD, 245 _keypoint_set_for, 266 fault_key_from_difference, 372 BASELINE_KINDS, 494 _NOTCH_REACH_KEYPOINTS, 497 body_relative_notches, 549 build_quantification_result, fault_joints_from_differences, worst_pose_timestamp)
    - backend/shared/python/sunity_shared/analysis/vision_veto.py (lines 643-646 `eligible_for_coach` property = `collection_status == "candidate_verdict" and cap_would_apply is True`; line 630 `cap_would_apply` dataclass field; lines 650-667 `to_coach_context` emits `capWouldApply`) — DO NOT change `eligible_for_coach`'s shape; `cap_would_apply` stays as a band-free pointer signal (see Task 2 coach-gate note — it becomes `severity in ('moderate','major')`, NOT a band). This property gates Phase 23 coach root-cause injection at app.py L3003 — a PRESERVED working part.
    - backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py (L5 docstring "severity 는 ... apply_downward_cap 에 먹여 하향 캡으로만 변환"; L129 VisionVerdict docstring "severity enum 만 20-01 의 apply_downward_cap 에 먹인다"; L134 field doc "severity: 'minor' | 'moderate' | 'major'. apply_downward_cap 입력") — THE three docstring strings to reframe to "criterion pointer" (HIGH-5). KEEP all code: `_SCORE_PATTERN` (L113), score-free `VisionVerdict` (L125-141), `build_schema` no-score (L147+) — objectivity guards unchanged.
    - 24-PATTERNS.md section "vision_veto.py (EDIT — remove band layer)" + "gemini_vision_scorer.py (EDIT — semantic reinterpret)" (exact removal/reframe targets)
    - 24-RESEARCH.md §"Old band cap to delete"
    - backend/functions/pipeline/app.py (call sites that will break: `vision_veto.apply_downward_cap(` at L1879, L1899, L2046, L2127; `to_audit_dict(... cap_applied=capped ...)` at L2136-2138 — do NOT edit app.py here, note for Task 2)
    - backend/tests/test_vision_veto.py (L44-105 — the WHOLE `apply_downward_cap` test suite, breaks when the function is removed; L115-191 SEVERITY_CAP/SEVERITY_CAP_PROVENANCE provenance tests, break; L511-528 `test_to_audit_dict_requires_final_args` calls `to_audit_dict(..., cap_applied=50, ...)` at L522, breaks on the signature reshape), backend/tests/test_pipeline_vision_gate.py (L285-296 `cap_would_apply` test with rows `("minor",88,False)`/`("moderate",92,True)`/`("none",95,False)`; L416 `test_applied_audit_serialized_via_to_audit_dict_...`), backend/tests/test_pipeline_mode3.py (L358-611 `SEVERITY_CAP` monkeypatch + `capApplied` asserts throughout), backend/tests/test_gemini_vision_scorer.py (L208-210 calls `vision_veto.apply_downward_cap`) — ALL break; they are in this plan's `files_modified` and rewritten in Task 4. Removing/rewriting these is mandatory (HIGH-5 broadened scope).
    - backend/shared/python/sunity_shared/models.py (line 111 `VISION_VETO_KEYS` includes `capApplied`) + app/src/types/analysis.ts (line 393 applied union requires `capApplied: number`; line 1029 result.tsx reads it) — capApplied is a LIVE contract field, see Task 3 for migration
  </read_first>
  <action>
    Delete the band layer from `vision_veto.py` per 24-CONTEXT ND-01 ([[scoring-must-be-transparent-deduction-tally]]): remove `SEVERITY_CAP` (L65-70), `SEVERITY_CAP_PROVENANCE` (L89-101), `apply_downward_cap` (L104-118), and trim the band-specific docstring section (L14-40) including any prose mentioning `apply_downward_cap`/`SEVERITY_CAP` so a call-site grep does not false-pass on comments. KEEP everything else verbatim: `worst_pose_timestamp`, `fault_joints_from_differences`, `fault_key_from_difference`, `FaultKey`, `_keypoint_set_for`, `_KEYPOINT_SET_BY_KEYWORD`, `_NOTCH_REACH_KEYPOINTS`, `body_relative_notches`, `build_quantification_result`, `FramePairMeasurementContext`, `BASELINE_KINDS`.

    Reframe the three `gemini_vision_scorer.py` docstrings (HIGH-5 — DOCSTRING ONLY, no code change): at L5 ("severity 는 20-01 의 apply_downward_cap 에 먹여 하향 캡으로만 변환된다"), L129 ("severity enum 만 20-01 의 apply_downward_cap 에 먹인다"), and L134 ("severity: 'minor' | 'moderate' | 'major'. apply_downward_cap 입력"), replace the `apply_downward_cap`/cap-input framing with the Phase 24 CRITERION-POINTER meaning: severity LOCATES which measured criterion is implicated (Phase 24 deduction_engine) and never produces a number — it is no longer a cap input. Keep the objectivity invariants verbatim (`_SCORE_PATTERN` leak guard, score-free VisionVerdict, no-score response_schema). No `apply_downward_cap` mention may remain in these docstrings.

    RESHAPE `to_audit_dict` (the band-free audit shape — exact new shape): change the signature from `(*, final_status, cap_applied=None, quantification=None)` to `(*, final_status, breakdown_final=None, quantification=None)`. When `final_status == "applied"`, emit `audit["tallyFinal"] = breakdown_final` (the deduction tally result int) INSTEAD of `audit["capApplied"] = cap_applied`. Keep `severity`/`primaryFault`/`rootCauseHypotheses`/`quantificationStatus`/`angleDeltas`/`bodyRelativeNotches`/`windowMedianAngleDeltas` emission unchanged. Update the docstring to ND-02 framing ("Gemini 은 점수를 절대 내지 않는다 — 측정대상/criterion 만 짚는다; cap 은 제거, tally final 만 audit") and drop the `capApplied` references in the to_audit_dict docstring/comments. Do NOT introduce any replacement ceiling constant. This task makes app.py temporarily fail to import `apply_downward_cap` — Task 2 fixes all callers in the same plan/wave.
  </action>
  <verify>
    <automated>grep -nE "SEVERITY_CAP|def apply_downward_cap|SEVERITY_CAP_PROVENANCE" backend/shared/python/sunity_shared/analysis/vision_veto.py; test $? -ne 0 && echo "BAND REMOVED"</automated>
    <automated>cd backend && python -c "from sunity_shared.analysis import vision_veto; assert hasattr(vision_veto, 'body_relative_notches') and hasattr(vision_veto, 'build_quantification_result') and hasattr(vision_veto, 'worst_pose_timestamp') and hasattr(vision_veto, 'BASELINE_KINDS'); assert not hasattr(vision_veto, 'apply_downward_cap') and not hasattr(vision_veto, 'SEVERITY_CAP')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "SEVERITY_CAP|apply_downward_cap|SEVERITY_CAP_PROVENANCE" backend/shared/python/sunity_shared/analysis/vision_veto.py` returns NOTHING (definitions AND comments removed — no false-pass strings).
    - `to_audit_dict` no longer takes/emits cap: `grep -nE "cap_applied|capApplied" backend/shared/python/sunity_shared/analysis/vision_veto.py` returns NOTHING; `grep -n "tallyFinal" backend/shared/python/sunity_shared/analysis/vision_veto.py` returns a hit.
    - gemini_vision_scorer docstrings reframed (HIGH-5): `grep -n "apply_downward_cap" backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py` returns NOTHING; `grep -nE "criterion|짚는다|측정대상|pointer" backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py` returns a hit in the severity docstrings; objectivity code unchanged (`grep -n "_SCORE_PATTERN" backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py` still returns a hit).
    - `vision_veto.body_relative_notches`, `build_quantification_result`, `worst_pose_timestamp`, `fault_key_from_difference`, `BASELINE_KINDS`, `FramePairMeasurementContext` all still importable (the python -c assertion exits 0).
    - No replacement ceiling: `grep -nE "_CAP =|min\\(overall|min\\(score" backend/shared/python/sunity_shared/analysis/vision_veto.py` returns nothing band-shaped.
  </acceptance_criteria>
  <done>vision_veto.py has zero band-cap code (defs and comment strings); gemini_vision_scorer.py severity docstrings reframed to criterion-pointer meaning (no apply_downward_cap mention, objectivity code unchanged); to_audit_dict reshaped band-free (tallyFinal, no capApplied); all Phase 23 measurement + FaultKey + baseline + worst-pose helpers remain importable.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the _process veto seam with deduction_engine.tally (fed measured substrate via _build_deduction_measured_deviations) + per-move baseline_kind + persist breakdown OBJECT on result</name>
  <files>backend/functions/pipeline/app.py, backend/shared/python/sunity_shared/firestore_admin.py</files>
  <read_first>
    - backend/functions/pipeline/app.py (lines 245-256 `_gemini_vision_veto_enabled` toggle — REUSE; lines 1753-1911 `_collect_vision_fault_context` incl. `apply_downward_cap` at L1879/L1899 + the `cap_would_apply = capped < overall_score` computation at L1880/L1900 + the cap_would_apply docstring at L1773-1774; lines 1914-1968 `_build_vision_quantification_result` — KEEP, baseline_kind param at L1924; lines 1971-2085 `_apply_vision_veto` incl. `overall = score_result["overallScore"]` at L2045 + `apply_downward_cap` at L2046 + Mode3 passthrough at L2020-2023 + visionVeto audit at L2064-2078; lines 2088-2155 `_apply_vision_veto_from_context` incl. `overall = score_result["overallScore"]` at L2126 + `apply_downward_cap` at L2127 + the `if capped < overall` gate at L2128 + `to_audit_dict(... cap_applied=` at L2136-2138; lines 3115-3149 `_process` seam call sites + `baseline_kind="hip_line"` hardcoded at L3128 + collect→coach→quantification ordering comment; L3156-3157 `_apply_score_suppression` — KEEP)
    - backend/functions/pipeline/app.py (L2980-3005 — the coach injection gated by `vision_fault_context.eligible_for_coach` at L3003; `to_coach_context` at L3002) — `eligible_for_coach` depends on `cap_would_apply`; PRESERVE this gate's firing behavior as band-free continuity (coach-gate HIGH-6, see action)
    - backend/shared/python/sunity_shared/analysis/assemble.py (line 736 — confirms `score_result["overallScore"]` is the un-vetoed measured-geometry overall = the tally substrate; `dimensionScores` present in the built result)
    - backend/shared/python/sunity_shared/analysis/dimensions.py (lines 240-267 `line_score`, lines 270-283 `extension_deviation` — the per-joint 180°-deficit vector + clean-lines source the substrate builder draws from, profile-gated on expects_extension) — THE deviation source (deg), NOT the 0-100 score
    - backend/shared/python/sunity_shared/analysis/vision_veto.py (L494 _NOTCH_REACH_KEYPOINTS; L497-546 body_relative_notches delta_notches — the reference_relative reach substrate) — the body_relative_reach input the builder forwards
    - backend/shared/python/sunity_shared/analysis/deduction_engine.py + ipsf_criteria.py (Plan 01 — tally signature takes dimension_overall/measured_deviations/dimension_scores/baseline_kind; criterion_for_fault_key; COVERAGE_GAP_KEYPOINT_SETS; to_dict() OBJECT)
    - backend/shared/python/sunity_shared/analysis/technique.py (TechniqueProfile fields: `name: str` (e.g. "미등록: kip-up"), `category: str` ∈ {flexibility,strength,spin,transition,unknown}, `motion_id: str | None`; FallbackRecognizer returns name="미상"/category="unknown"/motion_id=None at L106-113) — NOTE: `category` does NOT encode floor/aerial/horizontal, so baseline derivation MUST string-match `name`/`motion_id`, not `category`
    - backend/shared/python/sunity_shared/analysis/gemini_technique_recognizer.py (L198-206 — unregistered move e.g. kip-up returns name=f"미등록: {raw_motion_name}" / category="unregistered" / motion_id=None; L171-182 low_confidence returns motion_id=None) — confirms kip-up arrives as a NAMED-but-unregistered profile; the floor-move name list must match the substring "kip-up" inside name even for category=="unregistered"
    - backend/shared/python/sunity_shared/firestore_admin.py (L804 `def complete_analysis`; the `payload["result"] = dict(result)` assignment; L893-894 `_validate_recommended_exercises(recommended_exercises)` then `payload["result"]["recommendedExercises"]=...` — the INVOCATION analog (validate then place in result); `_validate_recommended_exercises` def at L495-526 reads a list[dict] and routes each item to `_validate_dict_only_scalars`; `_validate_flat_dict_no_nested_array` at L45 / `_validate_dict_only_scalars` at L104 — DO NOT modify these generic validator bodies (project-wide invariant), add a NEW scoped validator)
    - backend/shared/python/sunity_shared/models.py (DEDUCTION_RECORD_KEYS / DEDUCTION_BREAKDOWN_KEYS from Plan 01)
    - 24-PATTERNS.md sections "pipeline/app.py (EDIT — replace the seam)" + "models.py"
    - 24-RESEARCH.md §"Architecture Patterns" diagram (PHASE 24 REPLACES HERE block) + §"Runtime State Inventory" (toggle reuse A4)
  </read_first>
  <action>
    Replace the band mutation at the ONE seam, both the context path and the legacy path, preserving ordering and toggles. The base substrate is the un-vetoed `score_result["overallScore"]` (assemble.py:736 measured-geometry overall) plus a NAMED measured-deviation substrate (BLOCKER A + HIGH-3).

    SUBSTRATE BUILDER (HIGH-3 — explicit named shape, score-not-deviation safety): add a pure helper `_build_deduction_measured_deviations(*, angles, profile, assessments, dimension_scores, quantification)` returning a NAMED dict shape (NOT raw dimension scores):
      · `extension_deficits_by_joint`: from `dimensions.extension_deviation(angles, profile)` — per-joint 180°-deficit (deg), profile-gated; the leg_extension source.
      · `line_deficit` / `line_deficits_by_joint`: verified to draw from the SAME source as `dimensions.line_score` (the clean-lines 180°-deficit, deg) — NOT the 0-100 line dimension score; the `line` criterion source.
      · `split_angle`: the real split angle (deg) if measurable, else an explicit `unavailable` marker (never a fabricated number).
      · `body_relative_notches`: forwarded from `quantification.bodyRelativeNotches` (the delta_notches reach substrate) ONLY if present — the body_relative_reach (reference_relative) source.
    The builder MUST NOT place any 0-100 dimension SCORE where a deviation (deg/notch) is expected. Add a seam regression test (Task 4) asserting a dimension SCORE (0-100, e.g. 72) is NEVER fed as a deviation (72°) — this closes the HIGH-F gap at the seam, not just via the criterion tag.

    THREADING (no implicit `profile` name in scope of the apply paths): `_apply_vision_veto_from_context(score_result, ctx, quantification)` (L2088) and `_apply_vision_veto(...)` (L1971) do NOT have `profile` in scope, so calling the engine with a profile would NameError. The engine does NOT need the profile (Plan 01 Task 2). Thread the derived baseline as a plain string: add a keyword-only `baseline_kind: str = "hip_line"` parameter to BOTH `_apply_vision_veto` and `_apply_vision_veto_from_context` (and pass it through from `_apply_vision_veto` to `_apply_vision_veto_from_context` at the L2012-2015 delegation). The caller at the `_process` seam (where `profile` IS in scope, L3128) computes `baseline_kind=_baseline_kind_for_profile(profile)` ONCE and passes it into BOTH `_build_vision_quantification_result(..., baseline_kind=...)` and `_apply_vision_veto(..., baseline_kind=...)`. State this signature change in the SUMMARY.

    In `_apply_vision_veto` and `_apply_vision_veto_from_context`: where they currently compute `capped = vision_veto.apply_downward_cap(overall, verdict.severity)` and set `result['overallScore'] = capped` / build the cap audit (L2045-2078, L2124-2150), instead build the measured substrate via `_build_deduction_measured_deviations(...)` and call `deduction_engine.tally(quantification, ctx, dimension_overall=score_result["overallScore"], measured_deviations=<named substrate>, dimension_scores=score_result.get("dimensionScores"), baseline_kind=baseline_kind)` — passing the threaded `baseline_kind` STRING, NOT a profile. Set `result['overallScore'] = breakdown.final` and `result['deductionBreakdown'] = breakdown.to_dict()` (the OBJECT, HIGH-1). Use `verdict.severity` ONLY to LOCATE the Gemini-pointed fault's criterion via `ipsf_criteria.criterion_for_fault_key` (ND-02 — severity locates/adds, never a number, never a gate on measured deductions). Replace the `to_audit_dict(... cap_applied=capped ...)` call (L2136-2138) with `to_audit_dict(... breakdown_final=breakdown.final ...)` (Task 1 new signature). Keep the `visionVeto` audit field for observability/back-compat (now carries `tallyFinal` + status, not `capApplied`); emit a WARNING if the engine no-ops due to adapter failure (TRUST-08 no-silent-failure). Keep the early `_veto_passthrough` branches: `disabled`, `mode3_held` (L2020-2023 — Mode3 still absolute-delta, NO reference-anchored tally), `missing_local_video`, `missing_reference`, `skipped_error`. Note: the `_apply_vision_veto_from_context` `if capped < overall` gate (L2128) is REMOVED — the tally always runs on a candidate_verdict (measured deductions apply regardless of whether Gemini lowered anything); `not_applicable` now means "no measured deduction AND no Gemini-located criterion."

    In `_collect_vision_fault_context` (L1879/L1899): the two `apply_downward_cap` calls computed a provisional capped score for `cap_would_apply` in the ctx. Remove BOTH `apply_downward_cap` calls — collect still produces the verdict + supported_differences + FaultKeys (Phase 23 still-pair fan-out preserved); the deduction is computed once at the seam, not in collect.

    COACH-GATE PRESERVATION (HIGH-6 — band-free CONTINUITY, do NOT call it exact/strict parity): `cap_would_apply` is a dataclass field of `VisionFaultContext` (vision_veto.py L630) and `eligible_for_coach` (L643-646) = `collection_status == "candidate_verdict" AND cap_would_apply is True`. This property gates Phase 23 coach root-cause injection at app.py L3003 (a PRESERVED working part). The OLD `cap_would_apply = (apply_downward_cap(overall, severity) < overall)` depended on the score ALREADY being below the cap, so a moderate/major verdict on an already-low score could be False — it was NOT a pure severity function. Phase 24 replaces it with a band-free, severity-only pointer that means "Gemini pointed at a coach-worthy (moderate/major) fault" (`coachRootCauseEligible` meaning): at the two collect call sites set `cap_would_apply = (getattr(verdict, "severity", "none") in ("moderate", "major"))`. This is band-free CONTINUITY of today's trigger (moderate/major fire; minor/none don't) — NOT byte-identical legacy behavior. Do NOT use `severity != "none"` (that would WIDEN to minor — out of scope per the deferred note). Update the L1773-1774 docstring to remove the "production 동일 apply_downward_cap" / "cap_would_apply 계산(production 동일 cap)" wording and reframe `cap_would_apply` as "Gemini 이 coach-worthy (moderate/major) fault 를 짚었는지 — band-free coach-root-cause eligibility pointer; minor/none 미발화. (밴드 제거로 continuity 이며 과거 cap 산식과 byte-동일 아님, HIGH-6)". Do NOT change `eligible_for_coach`'s definition or `to_coach_context`'s `capWouldApply` emission. Add an acceptance criterion that coach injection fires for moderate/major and does NOT fire for minor/none.

    BASELINE_KIND DERIVATION (BLOCKER B — string-match the NAME, not category): add a small pure helper `_baseline_kind_for_profile(profile)` returning a `vision_veto.BASELINE_KINDS` member. Because `profile.category` ∈ {flexibility,strength,spin,transition,unknown,unregistered} does NOT encode floor/aerial/horizontal, derive by case-insensitive SUBSTRING matching against `profile.name` AND `profile.motion_id` (both may carry the move name, e.g. name="미등록: kip-up", motion_id="kip_up") using explicit keyword lists (STATE these in the helper): FLOOR_MOVE_KEYWORDS = ("kip-up","kip up","kip_up","킵업","floor","바닥","ground") -> "floor"; HORIZONTAL_MOVE_KEYWORDS = ("flag","plank","horizontal","수평","깃발") -> "pole_vertical"; otherwise (no name match, or name is None/"미상"/unknown) -> "hip_line" (the honest default — keypoint-only deterministic; correct for the common aerial/inversion case and the only safe default for truly-unknown moves). So kip-up=floor is exercised in production ONLY WHEN the move is named; there is NO universal per-move baseline claim. In `_process` (L3115-3149), compute `baseline_kind = _baseline_kind_for_profile(profile)` ONCE just before the `if vision_fault_context is not None` block (where `profile` is in scope), replace the hardcoded `baseline_kind="hip_line"` at L3128 with `baseline_kind=baseline_kind`, and pass the SAME `baseline_kind=baseline_kind` into the `_apply_vision_veto(...)` calls in BOTH the context branch (L3130-3139) and the legacy branch (L3142-3149). Keep the `if vision_fault_context is not None` / `else` structure and the `_build_vision_quantification_result(...)` call BEFORE the seam (Phase 23 ordering, KEEP). Keep `_gemini_vision_veto_enabled()` gating (REUSE `GEMINI_VISION_VETO_ENABLED`, no env rename). Keep `_apply_score_suppression` for `MODE_SELF` (L3156-3157, KEEP). After the seam sets `result['deductionBreakdown']`, ensure it flows to persistence.

    In `firestore_admin.complete_analysis` (PERSISTENCE = visionVeto analog, HIGH G + HIGH-1): do NOT add a `deduction_breakdown=` kwarg and do NOT mirror `force_signals_report=`. `result['deductionBreakdown']` is ALREADY inside the `result` dict (set at the seam, an OBJECT) and flows through `payload["result"] = dict(result)`. Add a SCOPED VALIDATOR call that reads from result before that line — mirror the `_validate_recommended_exercises` invocation pattern but reading `result.get('deductionBreakdown')`: define a NEW `_validate_deduction_breakdown(breakdown)` that accepts a TOP-LEVEL DICT and asserts its `records` AND `coverageGaps` are lists of flat scalar dicts (reuse `_validate_flat_dict_no_nested_array` / `_validate_dict_only_scalars` per element) so any accidental nested array raises (firestore-nested-array-flat). It accepts None/absent gracefully (legacy docs). `lazy import deduction_engine` at the seam following the existing lazy-import convention; the engine is pure (no adapter), so no `_ensure_adapters()` change.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_pipeline_deduction_seam.py -x -q</automated>
    <automated>grep -nE "vision_veto\.apply_downward_cap\(" backend/functions/pipeline/app.py; test $? -ne 0 && echo "NO CAP CALL SITES"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "vision_veto\.apply_downward_cap\(" backend/functions/pipeline/app.py` returns NOTHING (all CALL SITES replaced — grep scoped to the call, not comment prose).
    - `app.py` calls the engine fed the named measured substrate (HIGH-3): `grep -n "deduction_engine.tally" backend/functions/pipeline/app.py` returns a hit AND `grep -n "_build_deduction_measured_deviations" backend/functions/pipeline/app.py` returns ≥ 2 hits (def + call) AND `grep -nE "extension_deficits_by_joint|line_deficit|body_relative_notches" backend/functions/pipeline/app.py` returns hits in the builder.
    - `app.py` sets the breakdown OBJECT on result (HIGH-1): `grep -n "deductionBreakdown" backend/functions/pipeline/app.py` returns a hit AND `grep -n "breakdown.to_dict()" backend/functions/pipeline/app.py` returns a hit (NOT `to_records()`).
    - baseline_kind derived per-move, helper defined AND called at the seam: `grep -n "_baseline_kind_for_profile" backend/functions/pipeline/app.py` returns ≥ 2 hits; a baseline-branch test `test__baseline_kind_for_profile` (Task 4) asserts name="미등록: kip-up"→"floor", name="미상"/unknown→"hip_line". The seam no longer hardcodes the literal at the call site: confirm the L3115-3149 seam region passes `baseline_kind=baseline_kind` (variable), the only `"hip_line"` literal being the helper's own default branch.
    - baseline_kind threaded, not a profile, into the apply paths: both `_apply_vision_veto` and `_apply_vision_veto_from_context` gained a `baseline_kind` param; `grep -nE "technique_profile=|profile=profile" backend/functions/pipeline/app.py` returns NOTHING inside the tally call (no profile passed to the engine).
    - coach-gate preserved as band-free continuity (HIGH-6): `grep -n "cap_would_apply" backend/functions/pipeline/app.py` still returns hits (field set at collect call sites, now from `severity in ("moderate","major")`); `grep -nE 'severity.*in.*\("moderate"|moderate.*major' backend/functions/pipeline/app.py` returns a hit at a collect call site; `grep -n "eligible_for_coach" backend/functions/pipeline/app.py` still returns a hit at the coach-injection gate (L3003); the collect docstring no longer says "exact"/"production 동일 apply_downward_cap" — `grep -nE "production 동일 apply_downward_cap|exact parity|EXACTLY reproduces" backend/functions/pipeline/app.py` returns NOTHING; a Task-4 test asserts coach injection fires for moderate/major and does NOT fire for minor/none.
    - No reintroduced final band: `grep -nE "min\\(overall|min\\(100|SEVERITY_CAP|apply_downward_cap" backend/functions/pipeline/app.py` returns nothing.
    - to_audit_dict call migrated: `grep -nE "cap_applied=" backend/functions/pipeline/app.py` returns NOTHING; `grep -n "breakdown_final=" backend/functions/pipeline/app.py` returns a hit.
    - Preserved parts still referenced: `grep -nE "_apply_score_suppression|GEMINI_VISION_VETO_ENABLED|_build_vision_quantification_result|mode3_held" backend/functions/pipeline/app.py` returns hits for all four.
    - `complete_analysis` validates result['deductionBreakdown'] as a top-level dict WITHOUT a kwarg (HIGH-1): `grep -nE "def complete_analysis" backend/shared/python/sunity_shared/firestore_admin.py` line does NOT contain `deduction_breakdown`; `grep -nE "_validate_deduction_breakdown|deductionBreakdown" backend/shared/python/sunity_shared/firestore_admin.py` returns hits (validator reading from result, accepting a dict whose records/coverageGaps are flat).
    - `cd backend && python -m pytest tests/test_pipeline_deduction_seam.py -x -q` exits 0.
  </acceptance_criteria>
  <done>The single veto seam (context + legacy paths + collect) produces overallScore=breakdown.final via deduction_engine.tally fed the un-vetoed measured overallScore + a named _build_deduction_measured_deviations substrate (no dimension SCORE fed as a deviation — HIGH-3) + a threaded baseline_kind STRING (no profile passed to the engine); baseline_kind derived per-move by name/motion_id string-match (kip-up=floor when named, hip_line default), computed once at the seam and threaded into both quantification + apply paths; cap_would_apply preserved as band-free coach-root-cause eligibility (severity in moderate/major — continuity, not byte-identical legacy, HIGH-6) so eligible_for_coach + Phase 23 coach injection fire for moderate/major (minor/none do not); deductionBreakdown set as the OBJECT via to_dict() on result and validated as a top-level dict inside complete_analysis (no kwarg, visionVeto analog); to_audit_dict band-free; toggle/Mode3-suppression/quantification-ordering preserved; zero apply_downward_cap call sites remain.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate visionVeto.capApplied consumer + contract (band retired) — analysis.ts/models.py/result.tsx/contract.md</name>
  <files>app/src/types/analysis.ts, backend/shared/python/sunity_shared/models.py, app/src/app/analysis/result.tsx, docs/contract.md</files>
  <read_first>
    - app/src/types/analysis.ts (lines 340-341 capApplied compile-time-force comment; line 389-400 VisionVeto union — line 393 `applied` requires `capApplied: number`; the new DeductionBreakdown OBJECT from Plan 01) — capApplied is in the lockstep-edited contract
    - app/src/app/analysis/result.tsx (lines 1020-1041 — the ONLY runtime consumer: `result.visionVeto.capApplied` used as the reframe threshold for the angle dimension callout) — must migrate to `result.deductionBreakdown?.final`
    - backend/shared/python/sunity_shared/models.py (line 71-73 VISION_VETO lockstep comment; line 111 `VISION_VETO_KEYS` includes `capApplied`) — Python side of the same contract
    - docs/contract.md §4 VisionVeto note (L171 `visionVeto { status, severity?, capApplied? }`; L180 `capApplied? number`; L193, L202, L204, L209-211, L214, L218-219 — all reference `capApplied`; L218-219 the 3-way lockstep note) — the THIRD lockstep location; `capApplied`→`tallyFinal` here too (3-way convention)
    - 24-01-PLAN.md (DeductionBreakdown OBJECT contract — `final` is the new score-source the reframe should compare against)
  </read_first>
  <action>
    Migrate the retired `capApplied` band field explicitly (HIGH G — it is a LIVE contract field + runtime consumer). The band is gone; `visionVeto` audit now carries `tallyFinal` (Task 1). Choose REPURPOSE over silent-keep:

    In `app/src/types/analysis.ts`: in the `applied` member of the `VisionVeto` union (line 393), replace `capApplied: number` with `tallyFinal: number` (the deduction tally result, matching to_audit_dict). Keep the discriminated-union `capApplied?: never` on the non-applied members updated to `tallyFinal?: never`. Update the line 340-341 comment to state the band is retired and the audit carries the tally final (objectivity: still an int produced by measured rules, not a human label).

    In `backend/shared/python/sunity_shared/models.py`: update `VISION_VETO_KEYS` (line 111) replacing `"capApplied"` with `"tallyFinal"`, and update the lockstep comment (line 71-73) to cite the band retirement + DeductionBreakdown §10.

    In `docs/contract.md` §4 VisionVeto note (3-way lockstep): replace every `capApplied` with `tallyFinal` (L171 `visionVeto { status, severity?, tallyFinal? }`; L180 `tallyFinal? number  적용된 감점 tally 최종 점수`; L193/L202/L204/L209-211/L214) and update the prose to "밴드 제거 — applied 시 tallyFinal(감점 합산 최종) 동반; severity/tallyFinal" and the discriminated-union note to `status:'applied' without tallyFinal 는 tsc 에러`. Keep the 3-way lockstep note at L218-219 (now analysis.ts ↔ models.py ↔ §4, all carrying tallyFinal not capApplied). Note that the score itself is now the §10 DeductionBreakdown.final; visionVeto.tallyFinal mirrors it for audit.

    In `app/src/app/analysis/result.tsx` (lines 1024-1041): the reframe/contextNote logic compared the angle dimension score against `result.visionVeto.capApplied`. Repoint it to the new authoritative final: compare against `result.deductionBreakdown?.final` (the tally final, read off the OBJECT) with a fallback to `result.visionVeto?.status === 'applied' ? result.visionVeto.tallyFinal : (result.overallScore ?? 0)`. Keep the Korean copy unchanged. Ensure `cd app && npx tsc --noEmit` is clean.
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit</automated>
    <automated>grep -nE "capApplied" app/src/types/analysis.ts backend/shared/python/sunity_shared/models.py app/src/app/analysis/result.tsx docs/contract.md; test $? -ne 0 && echo "capApplied RETIRED"</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && npx tsc --noEmit` exits 0.
    - `capApplied` fully retired from the 3-way contract + consumer: `grep -nE "capApplied" app/src/types/analysis.ts backend/shared/python/sunity_shared/models.py app/src/app/analysis/result.tsx docs/contract.md` returns NOTHING.
    - `tallyFinal` present in all three lockstep locations: `grep -n "tallyFinal" app/src/types/analysis.ts backend/shared/python/sunity_shared/models.py docs/contract.md` returns hits in all three.
    - result.tsx reframe consumes the tally OBJECT: `grep -n "deductionBreakdown" app/src/app/analysis/result.tsx` returns a hit.
  </acceptance_criteria>
  <done>The retired band field capApplied is removed from all three visionVeto lockstep locations (analysis.ts ↔ models.py ↔ contract.md §4) and repurposed to tallyFinal; the result.tsx reframe consumer reads the deduction tally final off the OBJECT; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 4: Seam integration tests + migrate all broken band-contract caller tests + full-suite regression</name>
  <files>backend/tests/test_pipeline_deduction_seam.py, backend/tests/test_vision_veto.py, backend/tests/test_pipeline_vision_gate.py, backend/tests/test_pipeline_mode3.py, backend/tests/test_gemini_vision_scorer.py</files>
  <read_first>
    - backend/tests/test_kismam.py + backend/tests/conftest.py (test harness/imports)
    - backend/tests/test_vision_veto.py (L44-105 the `apply_downward_cap` test suite — MUST be removed since the function is gone; L115-191 SEVERITY_CAP/SEVERITY_CAP_PROVENANCE provenance tests — MUST be removed (the constants are gone); L511-528 `test_to_audit_dict_requires_final_args` calls `to_audit_dict(..., cap_applied=50, ...)` at L522 — UPDATE to the new `breakdown_final=` signature, assert `tallyFinal` emitted, no `capApplied`)
    - backend/tests/test_pipeline_vision_gate.py (L285-296 `test_collect_cap_would_apply_uses_production_cap` with rows `("minor",88,False)`/`("moderate",92,True)`/`("none",95,False)` — RENAME + REWRITE to assert `cap_would_apply == (severity in ("moderate","major"))` i.e. the SAME three booleans (minor→False, moderate→True, none→False) now sourced from the band-free severity-only pointer, NOT apply_downward_cap; drop the "production cap" framing; L416 `test_applied_audit_serialized_via_to_audit_dict_with_available_quant` — UPDATE for `breakdown_final=`/`tallyFinal` not `cap_applied`/`capApplied`)
    - backend/tests/test_pipeline_mode3.py (L358-611 — `monkeypatch.setitem(vision_veto.SEVERITY_CAP, ...)` + `out["visionVeto"]["capApplied"]==N` asserts throughout, e.g. L358/L374/L386/L409/L424/L435/L440/L466/L503/L528/L552/L577/L611 — HIGH-5 broadened scope: the SEVERITY_CAP monkeypatch is gone and capApplied is retired) — CONVERT these to tally/toggle/mode3-held asserts: assert the seam produces overallScore=deductionBreakdown.final (a tally over the measured substrate), Mode3 stays mode3_held passthrough, and the audit carries tallyFinal not capApplied; keep the over-penalization regression intent (clean form not punished) expressed via the tally (final not driven below by a clean-form fixture)
    - backend/tests/test_gemini_vision_scorer.py (L208-210 — `vision_veto.apply_downward_cap(100, verdict.severity)==100` / `(97,...)==97` in the none-verdict test) — REWRITE to assert score-free severity output (verdict.severity=='none', no score field) + the `_SCORE_PATTERN` guard, since apply_downward_cap is gone; the test's INTENT (clean form does not get punished) now belongs to the engine, so assert the verdict is severity='none' and carries no number, and (if convenient) that the engine routes 'none' to no Gemini-located criterion
    - existing pipeline integration tests (search `backend/tests/` for the file(s) that exercise `_apply_vision_veto` / `_process` with mocks — the analog for mocking the Gemini adapter + driving a synthetic verdict)
    - backend/functions/pipeline/app.py (the edited seam from Task 2 + `_baseline_kind_for_profile` + `_build_deduction_measured_deviations`)
    - backend/shared/python/sunity_shared/analysis/deduction_engine.py (Plan 01)
    - 24-RESEARCH.md §"Validation Architecture" (determinism gate intent — same input twice → identical breakdown) + §"Common Pitfalls" 5 (determinism)
  </read_first>
  <behavior>
    - Test (band removed end-to-end): a fault verdict drives overallScore via tally over the measured substrate, and overallScore is NOT one of the old fixed ceilings by construction — assert overallScore == breakdown.final and final has no constant floor.
    - Test (score-not-deviation regression — HIGH-3): drive the seam with a known dimension SCORE (e.g. dimensionScores angle=72) and assert the engine does NOT treat 72 as a 72° deviation — the measured substrate fed to tally is built by _build_deduction_measured_deviations from extension_deviation/line_score (deg), never the 0-100 score.
    - Test (measured substrate preserved — Phase 20): a fault video with a measured deviation but Gemini silent → overallScore < 100 (measured geometry still drives the drop; NOT auto-100).
    - Test (baseline_kind derived): `_baseline_kind_for_profile` returns 'floor' for a kip-up/floor named profile, 'hip_line' for an unknown/inversion/미상 profile — the seam passes the derived value into _build_vision_quantification_result.
    - Test (breakdown OBJECT set on result + validated flat): the result dict carries deductionBreakdown as the OBJECT {baseline, records, final, coverageGaps, fallback}; complete_analysis's `_validate_deduction_breakdown` accepts it and would reject a planted nested array inside records; NO complete_analysis deduction kwarg exists.
    - Test (Mode3 preserved): MODE_SELF path still returns mode3_held passthrough / absolute-delta — engine does NOT inject a reference-anchored band for Mode3; _apply_score_suppression still runs.
    - Test (toggle preserved): GEMINI_VISION_VETO_ENABLED off → veto/engine path disabled passthrough (no overallScore mutation).
    - Test (math-determinism): running the seam twice on the same stored quantification + cached verdict yields byte-identical deductionBreakdown (math-determinism, conditional on the cached verdict).
    - Test (severity not a score): swapping verdict.severity minor↔major with identical quantification yields identical overallScore.
    - Test (coach-gate band-free continuity — HIGH-6): a candidate_verdict ctx with severity in ("moderate","major") has `cap_would_apply == True` and `eligible_for_coach == True`, and the coach-injection path fires `coach_context["visionFault"]`; a severity=="minor" ctx and a severity=="none" ctx BOTH have `cap_would_apply == False` and `eligible_for_coach == False` (minor does NOT fire — continuity with the existing trigger).
  </behavior>
  <action>
    Create `test_pipeline_deduction_seam.py`. Mock the Gemini adapter / inject a synthetic `VisionFaultContext` + verdict + quantification (no Pod, no live Gemini — follow the existing pipeline-test mocking convention). Exercise `_apply_vision_veto` / `_apply_vision_veto_from_context` (and `_process` if feasible with mocks) for: Mode1 fault, Mode1 fault-free (high score result, not band-capped), Mode1 Gemini-silent-with-measured-deviation (final<100), Mode3 (mode3_held passthrough + suppression), toggle-off (disabled passthrough). Add the HIGH-3 score-not-deviation regression test. Unit-test `_baseline_kind_for_profile` for floor vs unknown. Add the math-determinism test and the severity-independence test at the seam level. Assert overallScore == deductionBreakdown.final, the breakdown is the OBJECT shape and serializes flat, and the complete_analysis validator accepts/rejects appropriately. Keep assertions directional/structural (`# 보유 sweep 수치 타깃 아님`). Add a coach-gate continuity test asserting `eligible_for_coach` (and `cap_would_apply`) is True for moderate AND major, and False for minor AND none.

    Migrate the FOUR broken caller test files in the same change (HIGH-5 broadened scope):
    - `test_vision_veto.py`: remove the `apply_downward_cap` suite (L44-105, function gone) AND the SEVERITY_CAP/SEVERITY_CAP_PROVENANCE provenance tests (L115-191, constants gone); update `test_to_audit_dict_requires_final_args` (L522) from `cap_applied=50` to `breakdown_final=50` asserting `tallyFinal` (no `capApplied`).
    - `test_pipeline_vision_gate.py`: rewrite `test_collect_cap_would_apply_uses_production_cap` (L285-296) to assert `cap_would_apply == (severity in ("moderate","major"))` keeping the same three booleans (minor→False, moderate→True, none→False) now sourced from the band-free severity-only pointer (rename away from "production_cap"); update the `to_audit_dict` test (L416) to the `breakdown_final`/`tallyFinal` shape.
    - `test_pipeline_mode3.py`: convert the `SEVERITY_CAP` monkeypatch + `capApplied` asserts (L358-611) to tally/toggle/mode3-held asserts (overallScore=deductionBreakdown.final, mode3_held passthrough, tallyFinal audit) preserving the over-penalization regression intent via the tally.
    - `test_gemini_vision_scorer.py`: rewrite the none-verdict test (L208-210) to assert score-free severity output (severity=='none', no number) + `_SCORE_PATTERN` guard, replacing the removed `apply_downward_cap` assertions.

    Acceptance grep (HIGH-5 — repo-wide, live code/tests = 0): `rg -n "apply_downward_cap|SEVERITY_CAP|capApplied|cap_applied" backend/shared/python backend/functions backend/tests app/src docs/contract.md` returns 0 (planning archive may be excluded).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_pipeline_deduction_seam.py -x -q</automated>
    <automated>cd backend && python -m pytest tests/ -q</automated>
    <automated>rg -n "apply_downward_cap|SEVERITY_CAP|capApplied|cap_applied" backend/shared/python backend/functions backend/tests app/src docs/contract.md; test $? -ne 0 && echo "BAND CONTRACT GONE REPO-WIDE"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_" backend/tests/test_pipeline_deduction_seam.py` ≥ 9.
    - `cd backend && python -m pytest tests/test_pipeline_deduction_seam.py -x -q` exits 0.
    - Full-suite regression: `cd backend && python -m pytest tests/ -q` exits 0 (all four broken caller test files updated to the band-free seam, no regressions).
    - Score-not-deviation regression present (HIGH-3): `grep -nE "score.not.deviation|72|_build_deduction_measured_deviations" backend/tests/test_pipeline_deduction_seam.py` returns a hit asserting a 0-100 score is not fed as a deviation.
    - Determinism test present and green: `grep -nE "determinis|twice|identical" backend/tests/test_pipeline_deduction_seam.py` returns a hit.
    - baseline_kind derivation + Phase-20 preservation asserted: tests reference `_baseline_kind_for_profile` and a gemini-silent-final<100 case.
    - Mode3 + toggle preservation asserted: tests reference mode3_held / disabled passthrough and _apply_score_suppression.
    - coach-gate band-free continuity (HIGH-6): a test asserts `eligible_for_coach == True` for a candidate_verdict with severity in ("moderate","major") and `False` for severity in ("minor","none").
    - ALL broken callers fixed repo-wide (HIGH-5): `rg -n "apply_downward_cap|SEVERITY_CAP|capApplied|cap_applied" backend/shared/python backend/functions backend/tests app/src docs/contract.md` returns 0; the rewritten `cap_would_apply` test asserts the band-free pointer reproduces the same three booleans (minor→False, moderate→True, none→False); test_pipeline_mode3.py + test_gemini_vision_scorer.py migrated band-free and pass under the full-suite run.
  </acceptance_criteria>
  <done>Seam integration tests prove band removal, measured-substrate tally (Gemini-silent still drops), score-not-deviation safety (HIGH-3), per-move baseline derivation (name string-match), flat-OBJECT-breakdown set-on-result + complete_analysis validation, coach-gate band-free continuity (eligible_for_coach fires for moderate/major, NOT minor/none), Mode3/toggle/suppression preservation, math-determinism, and severity-independence; ALL four broken caller test files (test_vision_veto / test_pipeline_vision_gate / test_pipeline_mode3 / test_gemini_vision_scorer) migrated band-free; the repo-wide band-contract grep returns 0; full backend suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQS event → _process | existing trigger; no change. |
| pipeline → Firestore Admin write | existing write path; deductionBreakdown added inside result dict as a top-level OBJECT whose records/coverageGaps are flat list-of-dicts, validated before payload['result']=dict(result). |
| Gemini verdict → seam | severity locates a fault's criterion; never a number, never gates measured deduction (ND-02). |

No new auth/network surface — the seam swaps an in-process band call for an in-process pure tally call. Lambda layer + RunPod Pod must be redeployed/pulled to drop the old vision_veto (Plan 03 / deploy step).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-04 | Tampering | malformed/NaN quantification reaching the seam | mitigate | engine's `np.isfinite` guard (Plan 01); unavailable → dimension_overall fallback (never 100, traceable record). |
| T-24-05 | Repudiation | silent no-op if the engine fails (TRUST-08 carryover) | mitigate | WARNING log + visionVeto audit field retained (tallyFinal); determinism test catches a silently-dropped breakdown. |
| T-24-06 | Tampering | nested array smuggled into deductionBreakdown crashing Firestore | mitigate | scoped `_validate_deduction_breakdown` on result['deductionBreakdown'] (top-level dict; records/coverageGaps flat) reusing `_validate_flat_dict_no_nested_array`; test plants a nested array and asserts rejection. |
| T-24-10 | Tampering | a dimension SCORE (0-100) silently fed as a deviation (deg) inflating/deflating a deduction (HIGH-F) | mitigate | `_build_deduction_measured_deviations` builds a NAMED deg/notch substrate from extension_deviation/line_score/delta_notches; Task-4 score-not-deviation regression test asserts a 0-100 score is never a deviation. |
</threat_model>

<verification>
- `grep -nE "vision_veto\.apply_downward_cap\(|SEVERITY_CAP" backend/functions/pipeline/app.py backend/shared/python/sunity_shared/analysis/vision_veto.py` returns NOTHING.
- `rg -n "apply_downward_cap|SEVERITY_CAP|capApplied|cap_applied" backend/shared/python backend/functions backend/tests app/src docs/contract.md` returns 0 (live code/tests band-contract gone, HIGH-5).
- `grep -n "deduction_engine.tally" backend/functions/pipeline/app.py` returns a hit; `grep -n "_build_deduction_measured_deviations" backend/functions/pipeline/app.py` returns ≥ 2 hits; `grep -n "deductionBreakdown" backend/shared/python/sunity_shared/firestore_admin.py` returns a hit.
- `grep -n "tallyFinal" app/src/types/analysis.ts backend/shared/python/sunity_shared/models.py docs/contract.md` returns hits in all three.
- `cd backend && python -m pytest tests/ -q` exits 0; `cd app && npx tsc --noEmit` exits 0.
- Preserved: `_apply_score_suppression`, `GEMINI_VISION_VETO_ENABLED`, `_build_vision_quantification_result`, `mode3_held` all still referenced in app.py.
</verification>

<success_criteria>
- Band layer fully removed (vision_veto.py defs+comments + gemini_vision_scorer docstrings + all app.py call sites + to_audit_dict reshaped + all four caller test files migrated band-free) (SCORE-10, HIGH-5).
- overallScore = deductionBreakdown.final at the one production seam, fed the un-vetoed measured overallScore + a named _build_deduction_measured_deviations substrate (no dimension SCORE as a deviation — HIGH-3), both context + legacy paths (SCORE-10, SCORE-16).
- baseline_kind derived per-move by name/motion_id string-match (NOT category), computed once at the seam and threaded as a plain string into both quantification + apply paths (kip-up=floor exercised when named; hip_line honest default) (SCORE-14).
- deductionBreakdown persisted as the OBJECT via to_dict() on result + validated as a top-level dict in complete_analysis (no kwarg, visionVeto analog) (SCORE-16, HIGH-1).
- Gemini severity locates-criterion-only, never scores, never gates measured deduction (SCORE-13); deferred-substrate faults surface as coverage gaps at the seam (SCORE-15).
- capApplied band field retired/repurposed to tallyFinal across all three lockstep locations (analysis.ts ↔ models.py ↔ contract.md §4) + result.tsx consumer.
- VisionFaultContext.cap_would_apply preserved as band-free coach-root-cause eligibility (severity in moderate/major — continuity, NOT byte-identical legacy, HIGH-6) so eligible_for_coach + Phase 23 coach root-cause injection fire for moderate/major (minor/none unchanged — no widening).
- Phase 20/23 working parts preserved (Mode3 suppression, toggle, still-pair quantification ordering); math-determinism holds (advances SCORE-09 determinism portion).
</success_criteria>

<output>
Create `.planning/phases/24-transparent-deduction-scoring/24-02-SUMMARY.md` when done.
</output>
</content>
