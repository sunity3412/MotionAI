---
phase: 24-transparent-deduction-scoring
plan: 02
type: execute
wave: 2
depends_on: [24-01]
files_modified:
  - backend/shared/python/sunity_shared/analysis/vision_veto.py
  - backend/functions/pipeline/app.py
  - backend/shared/python/sunity_shared/firestore_admin.py
  - app/src/types/analysis.ts
  - backend/shared/python/sunity_shared/models.py
  - docs/contract.md
  - backend/tests/test_pipeline_deduction_seam.py
  - backend/tests/test_vision_veto.py
  - backend/tests/test_pipeline_vision_gate.py
autonomous: true
requirements: [SCORE-10, SCORE-13, SCORE-15, SCORE-16, SCORE-09]
must_haves:
  truths:
    - "vision_veto.py no longer contains SEVERITY_CAP, apply_downward_cap, or SEVERITY_CAP_PROVENANCE"
    - "_process produces overallScore = deductionBreakdown.final via deduction_engine.tally at the old veto seam, fed the un-vetoed measured overallScore + measured deviations as substrate (one seam, both context + legacy paths)"
    - "baseline_kind is DERIVED per-move by STRING-matching the technique profile's name/motion_id against an explicit floor-move / horizontal-move name list (NOT profile.category, which only encodes flexibility/strength/spin/transition/unknown) — computed ONCE at the _process seam (where profile is in scope) and threaded into _build_vision_quantification_result + the tally; kip-up=floor is exercised in production ONLY WHEN the move is named (Gemini/studio term 'kip-up'); for truly-unknown/unnamed moves baseline honestly defaults to hip_line (no universal per-move claim)"
    - "deductionBreakdown (list-of-flat-dicts) is set on result['deductionBreakdown'] at the seam (visionVeto analog), validated inside complete_analysis before payload['result']=dict(result); NO complete_analysis kwarg added"
    - "Phase 20/23 working parts are preserved EXACTLY: _apply_score_suppression (Mode3), GEMINI_VISION_VETO_ENABLED toggle, Phase 23 still-pair fan-out + quantification ordering, Mode3 mode3_held passthrough, AND VisionFaultContext.cap_would_apply / eligible_for_coach — coach root-cause injection (app.py L3003) PRESERVED with STRICT PARITY: cap_would_apply = (severity in ('moderate','major')), a band-free pointer that EXACTLY reproduces today's coach-injection trigger (moderate/major fire; minor/none don't), matching test_pipeline_vision_gate.py's ('minor',88,False) expectation — no behavior change"
    - "baseline_kind is threaded as a plain string into _apply_vision_veto / _apply_vision_veto_from_context (computed once at the _process seam where profile is in scope) — the engine never receives the technique profile, so there is no NameError surface in the apply paths"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/vision_veto.py"
      provides: "Band layer removed; quantification/FaultKey/baseline/worst-pose helpers preserved; to_audit_dict reshaped to band-free (cap_applied retired, tally final/status emitted)"
      contains: "body_relative_notches"
    - path: "backend/functions/pipeline/app.py"
      provides: "Seam replaced: deduction_engine.tally fed measured overallScore+deviations → result['overallScore']=breakdown.final + result['deductionBreakdown']=breakdown.to_records(); baseline_kind derived per-move"
      contains: "deduction_engine"
    - path: "backend/shared/python/sunity_shared/firestore_admin.py"
      provides: "complete_analysis validates result['deductionBreakdown'] (flat list-of-dicts) inside the existing result-dict flow, NO new kwarg"
      contains: "deductionBreakdown"
    - path: "app/src/types/analysis.ts"
      provides: "visionVeto.capApplied deprecated/repurposed explicitly (band retired); result.tsx reframe consumer updated to deductionBreakdown.final"
      contains: "capApplied"
    - path: "backend/tests/test_pipeline_deduction_seam.py"
      provides: "Seam integration: band-removed, final from tally over measured substrate, breakdown set on result + validated, baseline_kind derived, Mode3 suppression preserved, toggle preserved"
      contains: "def test_"
  key_links:
    - from: "pipeline/app.py _process seam"
      to: "deduction_engine.tally"
      via: "replaces _apply_vision_veto band mutation; fed score_result['overallScore'] (un-vetoed measured) + measured deviations"
      pattern: "deduction_engine\\.tally"
    - from: "pipeline/app.py"
      to: "result['deductionBreakdown']"
      via: "breakdown.to_records() set on result dict (visionVeto analog), persisted via payload['result']=dict(result)"
      pattern: "deductionBreakdown"
    - from: "firestore_admin.complete_analysis"
      to: "result.get('deductionBreakdown') validator"
      via: "scoped flat-list validator before payload['result']=dict(result), mirroring _validate_recommended_exercises invocation but reading from result"
      pattern: "deductionBreakdown"
    - from: "pipeline/app.py _baseline_kind_for_profile (computed once at the _process seam)"
      to: "_build_vision_quantification_result AND _apply_vision_veto"
      via: "name/motion_id string-match → baseline string (floor/pole_vertical/hip_line); threaded into both quantification + apply paths (engine never gets the profile)"
      pattern: "_baseline_kind_for_profile"
---

<objective>
Wire the Plan 01 engine into the single production scoring seam and remove the band layer. Delete `SEVERITY_CAP` / `apply_downward_cap` / `SEVERITY_CAP_PROVENANCE` from `vision_veto.py` (keeping all measurement/FaultKey/baseline/worst-pose helpers). Replace the band mutation inside `_apply_vision_veto` / `_apply_vision_veto_from_context` / `_collect_vision_fault_context` (all currently call `apply_downward_cap`) with `deduction_engine.tally(...)`, FED the un-vetoed measured `score_result["overallScore"]` (assemble.py:736, the kismam/dimensions overall) + the measured per-criterion deviations as the deduction substrate (BLOCKER A — never a hardcoded 100-minus-Gemini). Set `result['overallScore'] = breakdown.final` and `result['deductionBreakdown'] = breakdown.to_records()` ON THE RESULT DICT (the `visionVeto` persistence analog — no `complete_analysis` kwarg). Derive `baseline_kind` per-move from the technique profile (BLOCKER B — floor for kip-up/floor moves, pole_vertical for horizontal, hip_line for inversions). Reshape `vision_veto.to_audit_dict` to be band-free (retire `cap_applied`/`capApplied`, emit tally final + status). Validate `result['deductionBreakdown']` inside `complete_analysis` before `payload["result"]=dict(result)`. Preserve Phase 20/23 working parts unchanged: `_apply_score_suppression` (Mode3), the `GEMINI_VISION_VETO_ENABLED` toggle (reuse), the Phase 23 still-pair fan-out + collect→coach→quantification ordering, the `vision_fault_context is None` legacy fallback, AND — with STRICT PARITY — the coach-injection trigger (`cap_would_apply` now a band-free pointer that fires for exactly the same moderate/major candidate verdicts as today, so `eligible_for_coach` and the app.py L3003 coach root-cause injection behave identically; minor/none still do not fire).

Purpose: this is the ONE seam (분기 0, 코드 1벌) where the forbidden severity→fixed-ceiling band lived. Replacing it here — fed the measured-geometry substrate — makes the production overallScore a transparent tally output everywhere (Lambda + RunPod share `_process`) while keeping Phase 20's false-positive defense (a fault video still drops from measured geometry even if Gemini is silent or the reference pair is missing). The mandate is "replace only the band, preserve working parts" — coach injection is a PRESERVED working part, so its firing set is reproduced exactly (band-free), not widened.

Output: band-free `vision_veto.py`, engine-wired `_process` with per-move baseline, breakdown set on result + validated in `complete_analysis`, capApplied consumer migrated, and seam integration tests.

Deferred (out of scope, Phase 24): whether MINOR faults should ALSO trigger coach root-cause injection is a separate product decision — Phase 24 preserves the existing moderate/major-only trigger exactly and does not widen or narrow it.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-transparent-deduction-scoring/24-CONTEXT.md
@.planning/phases/24-transparent-deduction-scoring/24-RESEARCH.md
@.planning/phases/24-transparent-deduction-scoring/24-PATTERNS.md
@.planning/phases/24-transparent-deduction-scoring/24-01-PLAN.md
@backend/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove band layer from vision_veto.py + reshape to_audit_dict band-free (keep all measurement helpers)</name>
  <files>backend/shared/python/sunity_shared/analysis/vision_veto.py</files>
  <read_first>
    - backend/shared/python/sunity_shared/analysis/vision_veto.py (lines 14-40 docstring band section; `SEVERITY_CAP` ≈L65, `SEVERITY_CAP_PROVENANCE` ≈L89, `apply_downward_cap` ≈L104; lines 690-738 `to_audit_dict` (signature `(*, final_status, cap_applied=None, quantification=None)` at L690-693) — emits `audit["capApplied"] = cap_applied` at L704-705 when final_status=='applied' (RESHAPE this, see action); KEEP line 201 FaultKey, 233-242 _KEYPOINT_SET_BY_KEYWORD, 245 _keypoint_set_for, 266 fault_key_from_difference, 372 BASELINE_KINDS, 494 body_relative_notches, 549 build_quantification_result, fault_joints_from_differences, worst_pose_timestamp)
    - backend/shared/python/sunity_shared/analysis/vision_veto.py (lines 643-646 `eligible_for_coach` property = `collection_status == "candidate_verdict" and cap_would_apply is True`; line 630 `cap_would_apply` dataclass field; lines 650-667 `to_coach_context` emits `capWouldApply`) — DO NOT change `eligible_for_coach`'s shape; `cap_would_apply` stays as a band-free pointer signal (see Task 2 coach-gate note — it becomes `severity in ('moderate','major')`, NOT a band). This property gates Phase 23 coach root-cause injection at app.py L3003 — a PRESERVED working part.
    - 24-PATTERNS.md section "vision_veto.py (EDIT — remove band layer)" (exact removal targets)
    - 24-RESEARCH.md §"Old band cap to delete"
    - backend/functions/pipeline/app.py (call sites that will break: `vision_veto.apply_downward_cap(` at L1879, L1899, L2046, L2127; `to_audit_dict(... cap_applied=capped ...)` at L2136-2138 — do NOT edit app.py here, note for Task 2)
    - backend/tests/test_vision_veto.py (L44-105 — the WHOLE `apply_downward_cap` test suite, breaks when the function is removed; L511-528 `test_to_audit_dict_requires_final_args` calls `to_audit_dict(..., cap_applied=50, ...)` at L522, breaks on the signature reshape) + backend/tests/test_pipeline_vision_gate.py (L285-296 `cap_would_apply` test with rows `("minor",88,False)`/`("moderate",92,True)`/`("none",95,False)`; L416 `test_applied_audit_serialized_via_to_audit_dict_...`) — BOTH break; they are in this plan's `files_modified` and updated in Task 4 (see Task 4 note). Removing/rewriting these is mandatory, not optional.
    - backend/shared/python/sunity_shared/models.py (line 111 `VISION_VETO_KEYS` includes `capApplied`) + app/src/types/analysis.ts (line 393 applied union requires `capApplied: number`; line 1029 result.tsx reads it) — capApplied is a LIVE contract field, see Task 3 for migration
  </read_first>
  <action>
    Delete the band layer from `vision_veto.py` per 24-CONTEXT ND-01 ([[scoring-must-be-transparent-deduction-tally]]): remove `SEVERITY_CAP` (≈L65-70), `SEVERITY_CAP_PROVENANCE` (≈L89-101), `apply_downward_cap` (≈L104-118), and trim the band-specific docstring section (≈L14-40) including any prose mentioning `apply_downward_cap`/`SEVERITY_CAP` so a call-site grep does not false-pass on comments. KEEP everything else verbatim: `worst_pose_timestamp`, `fault_joints_from_differences`, `fault_key_from_difference`, `FaultKey`, `_keypoint_set_for`, `_KEYPOINT_SET_BY_KEYWORD`, `body_relative_notches`, `build_quantification_result`, `FramePairMeasurementContext`, `BASELINE_KINDS`.

    RESHAPE `to_audit_dict` (the band-free audit shape — exact new shape): change the signature from `(*, final_status, cap_applied=None, quantification=None)` to `(*, final_status, breakdown_final=None, quantification=None)`. When `final_status == "applied"`, emit `audit["tallyFinal"] = breakdown_final` (the deduction tally result int) INSTEAD of `audit["capApplied"] = cap_applied`. Keep `severity`/`primaryFault`/`rootCauseHypotheses`/`quantificationStatus`/`angleDeltas`/`bodyRelativeNotches`/`windowMedianAngleDeltas` emission unchanged. Update the docstring to ND-02 framing ("Gemini 은 점수를 절대 내지 않는다 — 측정대상/criterion 만 짚는다; cap 은 제거, tally final 만 audit") and drop the `capApplied` references in the to_audit_dict docstring/comments. Do NOT introduce any replacement ceiling constant. This task makes app.py temporarily fail to import `apply_downward_cap` — Task 2 fixes all callers in the same plan/wave.
  </action>
  <verify>
    <automated>grep -nE "SEVERITY_CAP|def apply_downward_cap|SEVERITY_CAP_PROVENANCE" backend/shared/python/sunity_shared/analysis/vision_veto.py; test $? -ne 0 && echo "BAND REMOVED"</automated>
    <automated>cd backend && python -c "from sunity_shared.analysis import vision_veto; assert hasattr(vision_veto, 'body_relative_notches') and hasattr(vision_veto, 'build_quantification_result') and hasattr(vision_veto, 'worst_pose_timestamp') and hasattr(vision_veto, 'BASELINE_KINDS'); assert not hasattr(vision_veto, 'apply_downward_cap') and not hasattr(vision_veto, 'SEVERITY_CAP')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "SEVERITY_CAP|apply_downward_cap|SEVERITY_CAP_PROVENANCE" backend/shared/python/sunity_shared/analysis/vision_veto.py` returns NOTHING (definitions AND comments removed — no false-pass strings).
    - `to_audit_dict` no longer takes/emits cap: `grep -nE "cap_applied|capApplied" backend/shared/python/sunity_shared/analysis/vision_veto.py` returns NOTHING; `grep -n "tallyFinal" backend/shared/python/sunity_shared/analysis/vision_veto.py` returns a hit.
    - `vision_veto.body_relative_notches`, `build_quantification_result`, `worst_pose_timestamp`, `fault_key_from_difference`, `BASELINE_KINDS`, `FramePairMeasurementContext` all still importable (the python -c assertion exits 0).
    - No replacement ceiling: `grep -nE "_CAP =|min\\(overall|min\\(score" backend/shared/python/sunity_shared/analysis/vision_veto.py` returns nothing band-shaped.
  </acceptance_criteria>
  <done>vision_veto.py has zero band-cap code (defs and comment strings); to_audit_dict reshaped band-free (tallyFinal, no capApplied); all Phase 23 measurement + FaultKey + baseline + worst-pose helpers remain importable.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the _process veto seam with deduction_engine.tally (fed measured substrate) + per-move baseline_kind + persist breakdown on result</name>
  <files>backend/functions/pipeline/app.py, backend/shared/python/sunity_shared/firestore_admin.py</files>
  <read_first>
    - backend/functions/pipeline/app.py (lines 245-256 `_gemini_vision_veto_enabled` toggle — REUSE; lines 1753-1911 `_collect_vision_fault_context` incl. `apply_downward_cap` at L1879/L1899 + the cap_would_apply comment at L1773-1774; lines 1914-1968 `_build_vision_quantification_result` — KEEP, baseline_kind param at L1924; lines 1971-2085 `_apply_vision_veto` incl. `overall = score_result["overallScore"]` at L2045 + `apply_downward_cap` at L2046 + Mode3 passthrough at L2020-2023 + visionVeto audit at L2064-2078; lines 2088-2155 `_apply_vision_veto_from_context` incl. `overall = score_result["overallScore"]` at L2126 + `apply_downward_cap` at L2127 + `to_audit_dict(... cap_applied=` at L2136; lines 3115-3149 `_process` seam call sites + `baseline_kind="hip_line"` hardcoded at L3128 + collect→coach→quantification ordering comment; L3156-3157 `_apply_score_suppression` — KEEP)
    - backend/shared/python/sunity_shared/analysis/assemble.py (line 736 — confirms `score_result["overallScore"]` is the un-vetoed measured-geometry overall = the tally substrate)
    - backend/shared/python/sunity_shared/analysis/deduction_engine.py + ipsf_criteria.py (Plan 01 — tally signature takes dimension_overall/measured_deviations/dimension_scores/baseline_kind; criterion_for_fault_key; COVERAGE_GAP_KEYPOINT_SETS)
    - backend/shared/python/sunity_shared/analysis/technique.py (TechniqueProfile fields: `name: str` (e.g. "미등록: kip-up"), `category: str` ∈ {flexibility,strength,spin,transition,unknown}, `motion_id: str | None`; FallbackRecognizer returns name="미상"/category="unknown"/motion_id=None at L106-113) — NOTE: `category` does NOT encode floor/aerial/horizontal, so baseline derivation MUST string-match `name`/`motion_id`, not `category`
    - backend/shared/python/sunity_shared/analysis/gemini_technique_recognizer.py (L198-206 — unregistered move e.g. kip-up returns name=f"미등록: {raw_motion_name}" / category="unregistered" / motion_id=None; L171-182 low_confidence returns motion_id=None) — confirms kip-up arrives as a NAMED-but-unregistered profile; the floor-move name list must match the substring "kip-up" inside name even for category=="unregistered"
    - backend/functions/pipeline/app.py (L2985-3005 — the coach injection gated by `vision_fault_context.eligible_for_coach` at L3003; `to_coach_context` at L3002) — `eligible_for_coach` depends on `cap_would_apply`; PRESERVE this gate's firing behavior EXACTLY (coach-gate HIGH — STRICT PARITY, see action)
    - backend/shared/python/sunity_shared/firestore_admin.py (L804 `def complete_analysis`; the `payload["result"] = dict(result)` assignment; L893-894 `_validate_recommended_exercises(recommended_exercises)` then `payload["result"]["recommendedExercises"]=...` — the INVOCATION analog (validate then place in result); `_validate_recommended_exercises` def at L495-526 reads a list[dict] and routes each item to `_validate_dict_only_scalars`; `_validate_flat_dict_no_nested_array` at L45 / `_validate_dict_only_scalars` at L104 — DO NOT modify these generic validator bodies (project-wide invariant), add a NEW scoped validator like `_validate_recommended_exercises`)
    - backend/shared/python/sunity_shared/models.py (DEDUCTION_RECORD_KEYS / DEDUCTION_BREAKDOWN_KEYS from Plan 01)
    - 24-PATTERNS.md sections "pipeline/app.py (EDIT — replace the seam)" + "models.py"
    - 24-RESEARCH.md §"Architecture Patterns" diagram (PHASE 24 REPLACES HERE block) + §"Runtime State Inventory" (toggle reuse A4)
  </read_first>
  <action>
    Replace the band mutation at the ONE seam, both the context path and the legacy path, preserving ordering and toggles. The base substrate is the un-vetoed `score_result["overallScore"]` (assemble.py:736 measured-geometry overall) plus the measured per-criterion deviations available in the result (dimension_scores + per-joint deltas / quantification) — pass these to `tally` (BLOCKER A: the engine deducts from the measured layer; it does NOT recompute 100-minus-Gemini-only).

    THREADING (NEW-HIGH — no implicit `profile` name in scope of the apply paths): `_apply_vision_veto_from_context(score_result, ctx, quantification)` (L2088) and `_apply_vision_veto(...)` (L1971) do NOT have `profile` in scope, so calling `tally(..., technique_profile=profile)` inside them would NameError. The engine does NOT need the profile (Plan 01 Task 2 — criterion selection is driven by fault_context FaultKeys + measured_deviations; baseline is only a record LABEL). Thread the derived baseline as a plain string: add a keyword-only `baseline_kind: str = "hip_line"` parameter to BOTH `_apply_vision_veto` and `_apply_vision_veto_from_context` (and pass it through from `_apply_vision_veto` to `_apply_vision_veto_from_context` at the L2012-2015 delegation). The caller at the `_process` seam (where `profile` IS in scope, L3128) computes `baseline_kind=_baseline_kind_for_profile(profile)` ONCE and passes it into BOTH `_build_vision_quantification_result(..., baseline_kind=...)` and `_apply_vision_veto(..., baseline_kind=...)`. State this signature change explicitly in the SUMMARY.

    In `_apply_vision_veto` and `_apply_vision_veto_from_context`: where they currently compute `capped = vision_veto.apply_downward_cap(overall, verdict.severity)` and set `result['overallScore'] = capped` / build the cap audit (L2045-2078, L2124-2150), instead call `deduction_engine.tally(quantification, ctx, dimension_overall=score_result["overallScore"], measured_deviations=(per-criterion measured deviations from score_result/dimension_scores), dimension_scores=score_result.get("dimensionScores"), baseline_kind=baseline_kind)` — passing the threaded `baseline_kind` STRING, NOT a profile. Set `result['overallScore'] = breakdown.final` and `result['deductionBreakdown'] = breakdown.to_records()`. Use `verdict.severity` ONLY to LOCATE the Gemini-pointed fault's criterion via `ipsf_criteria.criterion_for_fault_key` (ND-02 — severity locates/adds, never a number, never a gate on measured deductions). Replace the `to_audit_dict(... cap_applied=capped ...)` call (L2136-2138) with `to_audit_dict(... breakdown_final=breakdown.final ...)` (Task 1 new signature). Keep the `visionVeto` audit field for observability/back-compat (now carries `tallyFinal` + status, not `capApplied`); emit a WARNING if the engine no-ops due to adapter failure (TRUST-08 no-silent-failure). Keep the early `_veto_passthrough` branches: `disabled`, `mode3_held` (L2020-2023 — Mode3 still absolute-delta, NO reference-anchored tally), `missing_local_video`, `missing_reference`, `skipped_error`. Note: the `_apply_vision_veto_from_context` `if capped < overall` gate (L2128) is REMOVED — the tally always runs on a candidate_verdict (the measured deductions apply regardless of whether Gemini lowered anything); `not_applicable` now means "no measured deduction AND no Gemini-located criterion." 

    In `_collect_vision_fault_context` (L1879/L1899): the two `apply_downward_cap` calls computed a provisional capped score for `cap_would_apply` in the ctx. Remove BOTH `apply_downward_cap` calls — collect still produces the verdict + supported_differences + FaultKeys (Phase 23 still-pair fan-out preserved); the deduction is computed once at the seam, not in collect.

    COACH-GATE PRESERVATION (NEW-HIGH — STRICT PARITY, do NOT widen): `cap_would_apply` is a dataclass field of `VisionFaultContext` (vision_veto.py L630) and `eligible_for_coach` (L643-646) = `collection_status == "candidate_verdict" AND cap_would_apply is True`. This property gates Phase 23 coach root-cause injection at app.py L3003 (a PRESERVED working part). Today (proven by test_pipeline_vision_gate.py L290 `("minor",88,False)`) `cap_would_apply` is True ONLY for moderate/major verdicts (minor→no-cap→False, none→False) because `apply_downward_cap` only lowered the score for moderate/major. The phase mandate is "replace only the band, preserve working parts" — so reproduce that firing set EXACTLY with a band-free pointer that no longer depends on cap arithmetic: at the two collect call sites set `cap_would_apply = (getattr(verdict, "severity", "none") in ("moderate", "major"))`. Do NOT use `severity != "none"` (that would WIDEN coach injection to include minor — a behavior change, out of scope per the deferred note in the objective). Update the L1773-1774 docstring to remove the "production 동일 apply_downward_cap" / "cap_would_apply 계산(production 동일 cap)" wording and reframe `cap_would_apply` as "Gemini 이 coach-worthy (moderate/major) fault 를 짚었는지 — band-free pointer, 오늘의 coach-injection trigger 와 동일(minor/none 미발화)". Do NOT change `eligible_for_coach`'s definition or `to_coach_context`'s `capWouldApply` emission. Add an acceptance criterion that coach injection fires for moderate/major and does NOT fire for minor/none (parity with the existing gate test).

    BASELINE_KIND DERIVATION (BLOCKER B — string-match the NAME, not category): add a small pure helper `_baseline_kind_for_profile(profile)` returning a `vision_veto.BASELINE_KINDS` member. Because `profile.category` ∈ {flexibility,strength,spin,transition,unknown,unregistered} does NOT encode floor/aerial/horizontal, derive by case-insensitive SUBSTRING matching against `profile.name` AND `profile.motion_id` (both may carry the move name, e.g. name="미등록: kip-up", motion_id="kip_up") using explicit keyword lists (STATE these in the helper): FLOOR_MOVE_KEYWORDS = ("kip-up","kip up","kip_up","킵업","floor","바닥","ground") -> "floor"; HORIZONTAL_MOVE_KEYWORDS = ("flag","plank","horizontal","수평","깃발") -> "pole_vertical"; otherwise (no name match, or name is None/"미상"/unknown) -> "hip_line" (the honest default — keypoint-only deterministic; correct for the common aerial/inversion case and the only safe default for truly-unknown moves). So kip-up=floor is exercised in production ONLY WHEN the move is named (Gemini named it / studio term present in name or motion_id); there is NO universal per-move baseline claim. In `_process` (L3115-3149), compute `baseline_kind = _baseline_kind_for_profile(profile)` ONCE just before the `if vision_fault_context is not None` block (where `profile` is in scope), replace the hardcoded `baseline_kind="hip_line"` at L3128 with `baseline_kind=baseline_kind`, and pass the SAME `baseline_kind=baseline_kind` into the `_apply_vision_veto(...)` calls in BOTH the context branch (L3130-3139) and the legacy branch (L3142-3149). Keep the `if vision_fault_context is not None` / `else` structure and the `_build_vision_quantification_result(...)` call BEFORE the seam (Phase 23 ordering, KEEP). Keep `_gemini_vision_veto_enabled()` gating (REUSE `GEMINI_VISION_VETO_ENABLED`, no env rename). Keep `_apply_score_suppression` for `MODE_SELF` (L3156-3157, KEEP). After the seam sets `result['deductionBreakdown']`, ensure it flows to persistence.

    In `firestore_admin.complete_analysis` (PERSISTENCE = visionVeto analog, HIGH G): do NOT add a `deduction_breakdown=` kwarg and do NOT mirror `force_signals_report=`. `result['deductionBreakdown']` is ALREADY inside the `result` dict (set at the seam) and flows through `payload["result"] = dict(result)`. Add a SCOPED VALIDATOR call that reads from result before that line — mirror the `_validate_recommended_exercises` invocation pattern but reading `result.get('deductionBreakdown')`: define/extend a validator that asserts `deductionBreakdown.records` is a list of flat scalar dicts (reuse `_validate_flat_dict_no_nested_array` / `_validate_dict_only_scalars`) so any accidental nested array raises (firestore-nested-array-flat). `lazy import deduction_engine` at the seam following the existing lazy-import convention; the engine is pure (no adapter), so no `_ensure_adapters()` change.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_pipeline_deduction_seam.py -x -q</automated>
    <automated>grep -nE "vision_veto\.apply_downward_cap\(" backend/functions/pipeline/app.py; test $? -ne 0 && echo "NO CAP CALL SITES"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "vision_veto\.apply_downward_cap\(" backend/functions/pipeline/app.py` returns NOTHING (all CALL SITES replaced — grep scoped to the call, not comment prose).
    - `app.py` calls the engine fed the measured substrate: `grep -n "deduction_engine.tally" backend/functions/pipeline/app.py` returns a hit AND `grep -nE "dimension_overall|measured_deviations" backend/functions/pipeline/app.py` returns hits.
    - `app.py` sets the breakdown on result: `grep -n "deductionBreakdown" backend/functions/pipeline/app.py` returns a hit.
    - baseline_kind derived per-move, helper defined AND called at the seam (LOW — presence+behavior, not a brittle whole-file string-absence count): `grep -n "_baseline_kind_for_profile" backend/functions/pipeline/app.py` returns ≥ 2 hits (the `def` AND at least one call at the L3128 seam); a baseline-branch test `test__baseline_kind_for_profile` (Task 4; satisfies RESEARCH's SCORE-13 `test_baseline_branch` intent — same behavior, seam-helper name) asserts name="미등록: kip-up"→"floor", name="미상"/unknown→"hip_line". The seam no longer hardcodes the literal at the call site: `grep -n 'baseline_kind="hip_line"' backend/functions/pipeline/app.py` shows the L3128 quantification call now reads `baseline_kind=baseline_kind` (a variable, NOT the literal) — the ONLY permitted `"hip_line"` literal is the helper's own internal default branch (`return "hip_line"`), so confirm any remaining `baseline_kind="hip_line"` hit is NOT at the L3128 seam call site (grep the seam region L3115-3149 returns the variable form, not the literal).
    - baseline_kind threaded, not a profile, into the apply paths: both `_apply_vision_veto` and `_apply_vision_veto_from_context` gained a `baseline_kind` param; `grep -nE "technique_profile=|profile=profile" backend/functions/pipeline/app.py` returns NOTHING inside the tally call (no profile passed to the engine — no NameError surface).
    - coach-gate preserved with STRICT PARITY (NEW-HIGH): `grep -n "cap_would_apply" backend/functions/pipeline/app.py` still returns hits (field still set at collect call sites, now from `severity in ("moderate","major")`); `grep -nE 'severity.*in.*\("moderate"|moderate.*major' backend/functions/pipeline/app.py` returns a hit at a collect call site; `grep -n "eligible_for_coach" backend/functions/pipeline/app.py` still returns a hit at the coach-injection gate (L3003); a Task-4 test asserts coach injection fires for moderate/major and does NOT fire for minor/none (parity).
    - No reintroduced final band: `grep -nE "min\\(overall|min\\(100|SEVERITY_CAP|apply_downward_cap" backend/functions/pipeline/app.py` returns nothing.
    - to_audit_dict call migrated: `grep -nE "cap_applied=" backend/functions/pipeline/app.py` returns NOTHING; `grep -n "breakdown_final=" backend/functions/pipeline/app.py` returns a hit.
    - Preserved parts still referenced: `grep -nE "_apply_score_suppression|GEMINI_VISION_VETO_ENABLED|_build_vision_quantification_result|mode3_held" backend/functions/pipeline/app.py` returns hits for all four.
    - `complete_analysis` validates result['deductionBreakdown'] WITHOUT a kwarg: `grep -nE "def complete_analysis" backend/shared/python/sunity_shared/firestore_admin.py` line does NOT contain `deduction_breakdown`; `grep -n "deductionBreakdown" backend/shared/python/sunity_shared/firestore_admin.py` returns a hit (validator reading from result).
    - `cd backend && python -m pytest tests/test_pipeline_deduction_seam.py -x -q` exits 0.
  </acceptance_criteria>
  <done>The single veto seam (context + legacy paths + collect) produces overallScore=breakdown.final via deduction_engine.tally fed the un-vetoed measured overallScore + deviations + a threaded baseline_kind STRING (no profile passed to the engine); baseline_kind derived per-move by name/motion_id string-match (kip-up=floor when named, hip_line default), computed once at the seam and threaded into both quantification + apply paths; cap_would_apply preserved with STRICT PARITY as severity in (moderate,major) so eligible_for_coach + Phase 23 coach injection fire for exactly the same verdicts as today (minor/none do not fire); deductionBreakdown set on result and validated inside complete_analysis (no kwarg, visionVeto analog); to_audit_dict band-free; toggle/Mode3-suppression/quantification-ordering preserved; zero apply_downward_cap call sites remain.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate visionVeto.capApplied consumer + contract (band retired) — analysis.ts/models.py/result.tsx</name>
  <files>app/src/types/analysis.ts, backend/shared/python/sunity_shared/models.py, app/src/app/analysis/result.tsx, docs/contract.md</files>
  <read_first>
    - app/src/types/analysis.ts (lines 340-341 capApplied compile-time-force comment; line 389-400 VisionVeto union — line 393 `applied` requires `capApplied: number`; the new DeductionBreakdown from Plan 01) — capApplied is in the lockstep-edited contract
    - app/src/app/analysis/result.tsx (lines 1020-1041 — the ONLY runtime consumer: `result.visionVeto.capApplied` used as the reframe threshold for the angle dimension callout) — must migrate to `result.deductionBreakdown?.final`
    - backend/shared/python/sunity_shared/models.py (line 71-73 VISION_VETO lockstep comment; line 111 `VISION_VETO_KEYS` includes `capApplied`) — Python side of the same contract
    - docs/contract.md §4 VisionVeto note (L171 `visionVeto { status, severity?, capApplied? }`; L180 `capApplied? number`; L193, L202, L204, L209-211, L214, L218-219 — all reference `capApplied`; L218-219 the 3-way lockstep note) — the THIRD lockstep location; `capApplied`→`tallyFinal` here too (MEDIUM — 3-way convention: analysis.ts ↔ models.py ↔ §4)
    - 24-01-PLAN.md (DeductionBreakdown contract — `final` is the new score-source the reframe should compare against)
  </read_first>
  <action>
    Migrate the retired `capApplied` band field explicitly (HIGH G — it is a LIVE contract field + runtime consumer, not just audit). The band is gone; `visionVeto` audit now carries `tallyFinal` (Task 1). Choose REPURPOSE over silent-keep:

    In `app/src/types/analysis.ts`: in the `applied` member of the `VisionVeto` union (line 393), replace `capApplied: number` with `tallyFinal: number` (the deduction tally result, matching to_audit_dict). Keep the discriminated-union `capApplied?: never` on the non-applied members updated to `tallyFinal?: never`. Update the line 340-341 comment to state the band is retired and the audit carries the tally final (objectivity: still an int produced by measured rules, not a human label).

    In `backend/shared/python/sunity_shared/models.py`: update `VISION_VETO_KEYS` (line 111) replacing `"capApplied"` with `"tallyFinal"`, and update the lockstep comment (line 71-73) to cite the band retirement + DeductionBreakdown §10.

    In `docs/contract.md` §4 VisionVeto note (MEDIUM — 3-way lockstep): replace every `capApplied` with `tallyFinal` (L171 `visionVeto { status, severity?, tallyFinal? }`; L180 `tallyFinal? number  적용된 감점 tally 최종 점수`; L193/L202/L204/L209-211/L214) and update the prose to "밴드 제거 — applied 시 tallyFinal(감점 합산 최종) 동반; severity/tallyFinal" and the discriminated-union note to `status:'applied' without tallyFinal 는 tsc 에러`. Keep the 3-way lockstep note at L218-219 (now analysis.ts ↔ models.py ↔ §4, all carrying tallyFinal not capApplied). Note that the score itself is now the §10 DeductionBreakdown.final; visionVeto.tallyFinal mirrors it for audit.

    In `app/src/app/analysis/result.tsx` (lines 1024-1041): the reframe/contextNote logic compared the angle dimension score against `result.visionVeto.capApplied`. Repoint it to the new authoritative final: compare against `result.deductionBreakdown?.final` (the tally final) with a fallback to `result.visionVeto?.status === 'applied' ? result.visionVeto.tallyFinal : (result.overallScore ?? 0)`. Keep the Korean copy unchanged. Ensure `cd app && npx tsc --noEmit` is clean.
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit</automated>
    <automated>grep -nE "capApplied" app/src/types/analysis.ts backend/shared/python/sunity_shared/models.py app/src/app/analysis/result.tsx docs/contract.md; test $? -ne 0 && echo "capApplied RETIRED"</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && npx tsc --noEmit` exits 0.
    - `capApplied` fully retired from the 3-way contract + consumer: `grep -nE "capApplied" app/src/types/analysis.ts backend/shared/python/sunity_shared/models.py app/src/app/analysis/result.tsx docs/contract.md` returns NOTHING.
    - `tallyFinal` present in all three lockstep locations: `grep -n "tallyFinal" app/src/types/analysis.ts backend/shared/python/sunity_shared/models.py docs/contract.md` returns hits in all three.
    - result.tsx reframe consumes the tally: `grep -n "deductionBreakdown" app/src/app/analysis/result.tsx` returns a hit.
  </acceptance_criteria>
  <done>The retired band field capApplied is removed from all three visionVeto lockstep locations (analysis.ts ↔ models.py ↔ contract.md §4) and repurposed to tallyFinal; the result.tsx reframe consumer reads the deduction tally final; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 4: Seam integration tests + full-suite regression</name>
  <files>backend/tests/test_pipeline_deduction_seam.py, backend/tests/test_vision_veto.py, backend/tests/test_pipeline_vision_gate.py</files>
  <read_first>
    - backend/tests/test_kismam.py + backend/tests/conftest.py (test harness/imports)
    - backend/tests/test_vision_veto.py (L44-105 the `apply_downward_cap` test suite — MUST be removed/rewritten since the function is gone; L511-528 `test_to_audit_dict_requires_final_args` calls `to_audit_dict(..., cap_applied=50, ...)` at L522 — UPDATE to the new `breakdown_final=` signature, assert `tallyFinal` emitted, no `capApplied`) — MEDIUM caller fix, in this plan's files_modified
    - backend/tests/test_pipeline_vision_gate.py (L285-296 `test_collect_cap_would_apply_uses_production_cap` with rows `("minor",88,False)`/`("moderate",92,True)`/`("none",95,False)` — REWRITE to assert `cap_would_apply == (severity in ("moderate","major"))` i.e. the SAME three expected booleans (minor→False, moderate→True, none→False) now sourced from the band-free pointer, not apply_downward_cap; L416 `test_applied_audit_serialized_via_to_audit_dict_with_available_quant` — UPDATE for `breakdown_final=`/`tallyFinal` not `cap_applied`/`capApplied`) — MEDIUM caller fix, in this plan's files_modified
    - existing pipeline integration tests (search `backend/tests/` for the file(s) that exercise `_apply_vision_veto` / `_process` with mocks — the analog for mocking the Gemini adapter + driving a synthetic verdict; e.g. test files referencing `_collect_vision_fault_context` or `visionVeto`)
    - backend/functions/pipeline/app.py (the edited seam from Task 2 + `_baseline_kind_for_profile`)
    - backend/shared/python/sunity_shared/analysis/deduction_engine.py (Plan 01)
    - 24-RESEARCH.md §"Validation Architecture" (determinism gate intent — same input twice → identical breakdown) + §"Common Pitfalls" 5 (determinism)
  </read_first>
  <behavior>
    - Test (band removed end-to-end): a fault verdict drives overallScore via tally over the measured substrate, and overallScore is NOT one of the old fixed ceilings by construction — assert overallScore == breakdown.final and final has no constant floor.
    - Test (measured substrate preserved — Phase 20): a fault video with a measured deviation but Gemini silent → overallScore < 100 (the measured geometry still drives the drop; NOT auto-100).
    - Test (baseline_kind derived): `_baseline_kind_for_profile` returns 'floor' for a kip-up/floor profile, 'hip_line' for an unknown/inversion profile — the seam passes the derived value into _build_vision_quantification_result.
    - Test (breakdown set on result + validated flat): the result dict carries deductionBreakdown as a list of flat dicts; complete_analysis's validator accepts it and would reject a planted nested array; NO complete_analysis deduction kwarg exists.
    - Test (Mode3 preserved): MODE_SELF path still returns mode3_held passthrough / absolute-delta — engine does NOT inject a reference-anchored band for Mode3; _apply_score_suppression still runs.
    - Test (toggle preserved): GEMINI_VISION_VETO_ENABLED off → veto/engine path disabled passthrough (no overallScore mutation).
    - Test (math-determinism): running the seam twice on the same stored quantification + cached verdict yields byte-identical deductionBreakdown (math-determinism, conditional on the cached verdict — see Plan 03 for criterion-selection determinism).
    - Test (severity not a score): swapping verdict.severity minor↔major with identical quantification yields identical overallScore.
    - Test (coach-gate STRICT PARITY — NEW-HIGH): a candidate_verdict ctx with severity in ("moderate","major") has `cap_would_apply == True` and `eligible_for_coach == True`, and the `_process` coach-injection path (or a direct `eligible_for_coach` assertion) fires `coach_context["visionFault"]`; a severity=="minor" ctx and a severity=="none" ctx BOTH have `cap_would_apply == False` and `eligible_for_coach == False` (minor does NOT fire — parity with test_pipeline_vision_gate.py's ("minor",...,False) expectation).
  </behavior>
  <action>
    Create `test_pipeline_deduction_seam.py`. Mock the Gemini adapter / inject a synthetic `VisionFaultContext` + verdict + quantification (no Pod, no live Gemini — follow the existing pipeline-test mocking convention). Exercise `_apply_vision_veto` / `_apply_vision_veto_from_context` (and `_process` if feasible with mocks) for: Mode1 fault, Mode1 fault-free (high score result, not band-capped), Mode1 Gemini-silent-with-measured-deviation (final<100), Mode3 (mode3_held passthrough + suppression), toggle-off (disabled passthrough). Unit-test `_baseline_kind_for_profile` for floor vs unknown. Add the math-determinism test (same input twice → identical breakdown) and the severity-independence test at the seam level. Assert overallScore == deductionBreakdown.final, the breakdown serializes flat, and the complete_analysis validator accepts/rejects appropriately. Keep assertions directional/structural (no curve-fit to specific held-video scores — `# 보유 sweep 수치 타깃 아님`). Add a coach-gate parity test asserting `eligible_for_coach` (and `cap_would_apply`) is True for moderate AND major, and False for minor AND none. ALSO fix the two broken caller test files in the same change: in `test_vision_veto.py` remove the `apply_downward_cap` suite (L44-105, function gone) and update `test_to_audit_dict_requires_final_args` (L522) from `cap_applied=50` to `breakdown_final=50` asserting `tallyFinal`; in `test_pipeline_vision_gate.py` rewrite `test_collect_cap_would_apply_uses_production_cap` (L285-296) to assert `cap_would_apply == (severity in ("moderate","major"))` keeping the same three expected booleans (minor→False, moderate→True, none→False) now sourced from the band-free pointer, and update the `to_audit_dict` test (L416) to the `breakdown_final`/`tallyFinal` shape.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_pipeline_deduction_seam.py -x -q</automated>
    <automated>cd backend && python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_" backend/tests/test_pipeline_deduction_seam.py` ≥ 8.
    - `cd backend && python -m pytest tests/test_pipeline_deduction_seam.py -x -q` exits 0.
    - Full-suite regression: `cd backend && python -m pytest tests/ -q` exits 0 (existing pipeline/vision tests updated to the band-free seam, no regressions).
    - Determinism test present and green: `grep -nE "determinis|twice|identical" backend/tests/test_pipeline_deduction_seam.py` returns a hit.
    - baseline_kind derivation + Phase-20 preservation asserted: tests reference `_baseline_kind_for_profile` and a gemini-silent-final<100 case.
    - Mode3 + toggle preservation asserted: tests reference mode3_held / disabled passthrough and _apply_score_suppression.
    - coach-gate STRICT PARITY (NEW-HIGH): a test asserts `eligible_for_coach == True` for a candidate_verdict with severity in ("moderate","major") and `False` for severity in ("minor","none") — minor does NOT fire (parity with the existing ("minor",...,False) gate expectation).
    - broken callers fixed (MEDIUM): `grep -nE "apply_downward_cap|cap_applied=" backend/tests/test_vision_veto.py backend/tests/test_pipeline_vision_gate.py` returns NOTHING; the rewritten `cap_would_apply` test asserts the band-free pointer reproduces the same three booleans (minor→False, moderate→True, none→False); both files updated to the `breakdown_final`/`tallyFinal` shape and pass under the full-suite run.
  </acceptance_criteria>
  <done>Seam integration tests prove band removal, measured-substrate tally (Gemini-silent still drops), per-move baseline derivation (name string-match), flat-breakdown set-on-result + complete_analysis validation, coach-gate STRICT PARITY (eligible_for_coach fires for moderate/major, NOT minor/none — exactly today's trigger), Mode3/toggle/suppression preservation, math-determinism, and severity-independence; the two broken caller test files (test_vision_veto.py / test_pipeline_vision_gate.py) are migrated band-free; full backend suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQS event → _process | existing trigger; no change. |
| pipeline → Firestore Admin write | existing write path; deductionBreakdown added inside result dict as flat list-of-dicts, validated before payload['result']=dict(result). |
| Gemini verdict → seam | severity locates a fault's criterion; never a number, never gates measured deduction (ND-02). |

No new auth/network surface — the seam swaps an in-process band call for an in-process pure tally call. Lambda layer + RunPod Pod must be redeployed/pulled to drop the old vision_veto (Plan 03 / deploy step).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-04 | Tampering | malformed/NaN quantification reaching the seam | mitigate | engine's `np.isfinite` guard (Plan 01); unavailable → dimension_overall fallback (never 100). |
| T-24-05 | Repudiation | silent no-op if the engine fails (TRUST-08 carryover) | mitigate | WARNING log + visionVeto audit field retained (tallyFinal); determinism test catches a silently-dropped breakdown. |
| T-24-06 | Tampering | nested array smuggled into deductionBreakdown crashing Firestore | mitigate | scoped validator on result['deductionBreakdown'] reusing `_validate_flat_dict_no_nested_array`; test plants a nested array and asserts rejection. |
</threat_model>

<verification>
- `grep -nE "vision_veto\.apply_downward_cap\(|SEVERITY_CAP" backend/functions/pipeline/app.py backend/shared/python/sunity_shared/analysis/vision_veto.py` returns NOTHING.
- `grep -n "deduction_engine.tally" backend/functions/pipeline/app.py` returns a hit; `grep -n "deductionBreakdown" backend/shared/python/sunity_shared/firestore_admin.py` returns a hit.
- `grep -nE "capApplied" app/src/types/analysis.ts backend/shared/python/sunity_shared/models.py app/src/app/analysis/result.tsx` returns NOTHING (band field retired).
- `cd backend && python -m pytest tests/ -q` exits 0; `cd app && npx tsc --noEmit` exits 0.
- Preserved: `_apply_score_suppression`, `GEMINI_VISION_VETO_ENABLED`, `_build_vision_quantification_result`, `mode3_held` all still referenced in app.py.
</verification>

<success_criteria>
- Band layer fully removed (vision_veto.py defs+comments + all app.py call sites + to_audit_dict reshaped) (SCORE-10).
- overallScore = deductionBreakdown.final at the one production seam, fed the un-vetoed measured overallScore + deviations as substrate, both context + legacy paths (SCORE-10, SCORE-16).
- baseline_kind derived per-move by name/motion_id string-match (NOT category), computed once at the seam and threaded as a plain string into both quantification + apply paths (kip-up=floor exercised when named; hip_line honest default) (SCORE-14).
- deductionBreakdown persisted flat via result dict + validated in complete_analysis (no kwarg, visionVeto analog) (SCORE-16).
- Gemini severity locates-criterion-only, never scores, never gates measured deduction (SCORE-13); deferred-substrate faults surface as coverage gaps at the seam (SCORE-15).
- capApplied band field retired/repurposed to tallyFinal across all three lockstep locations (analysis.ts ↔ models.py ↔ contract.md §4) + result.tsx consumer (HIGH G + MEDIUM contract lockstep).
- VisionFaultContext.cap_would_apply preserved with STRICT PARITY (now severity in moderate/major) so eligible_for_coach + Phase 23 coach root-cause injection fire for exactly the same verdicts as today (minor/none unchanged — no widening); the two broken caller test files migrated band-free (coach-gate HIGH + MEDIUM).
- Phase 20/23 working parts preserved (Mode3 suppression, toggle, still-pair quantification ordering); math-determinism holds (advances SCORE-09 determinism portion).
</success_criteria>

<output>
Create `.planning/phases/24-transparent-deduction-scoring/24-02-SUMMARY.md` when done.
</output>
