---
phase: 24-transparent-deduction-scoring
plan: 02
type: execute
wave: 2
depends_on: [24-01]
files_modified:
  - backend/shared/python/sunity_shared/analysis/vision_veto.py
  - backend/functions/pipeline/app.py
  - backend/shared/python/sunity_shared/firestore_admin.py
  - backend/tests/test_pipeline_deduction_seam.py
autonomous: true
requirements: [SCORE-10, SCORE-13, SCORE-15, SCORE-16, SCORE-09]
must_haves:
  truths:
    - "vision_veto.py no longer contains SEVERITY_CAP, apply_downward_cap, or SEVERITY_CAP_PROVENANCE"
    - "_process produces overallScore = deductionBreakdown.final via deduction_engine.tally at the old veto seam (one seam, both context + legacy paths)"
    - "deductionBreakdown (list-of-flat-dicts) is persisted to Firestore alongside the band-free overallScore"
    - "Phase 20/23 working parts are preserved: _apply_score_suppression (Mode3), GEMINI_VISION_VETO_ENABLED toggle, Phase 23 still-pair fan-out + quantification ordering"
    - "Mode3 still uses absolute-dimension delta (no reference anchor); engine does not introduce a reference-anchored band for Mode3"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/vision_veto.py"
      provides: "Band layer removed; quantification/FaultKey/baseline/worst-pose helpers preserved"
      contains: "body_relative_notches"
    - path: "backend/functions/pipeline/app.py"
      provides: "Seam replaced: deduction_engine.tally → result['overallScore']=breakdown.final + result['deductionBreakdown']=breakdown.to_records()"
      contains: "deduction_engine"
    - path: "backend/shared/python/sunity_shared/firestore_admin.py"
      provides: "complete_analysis stores deductionBreakdown (flat list-of-dicts, nested-array validator passes)"
      contains: "deductionBreakdown"
    - path: "backend/tests/test_pipeline_deduction_seam.py"
      provides: "Seam integration: band-removed, final from tally, breakdown persisted flat, Mode3 suppression preserved, toggle preserved"
      contains: "def test_"
  key_links:
    - from: "pipeline/app.py _process seam"
      to: "deduction_engine.tally"
      via: "replaces _apply_vision_veto band mutation at app.py:3130"
      pattern: "deduction_engine\\.tally"
    - from: "pipeline/app.py"
      to: "result['deductionBreakdown']"
      via: "breakdown.to_records() set on result, persisted by complete_analysis"
      pattern: "deductionBreakdown"
    - from: "firestore_admin.complete_analysis"
      to: "_validate_flat_dict_no_nested_array"
      via: "deductionBreakdown.records validated as flat list-of-dicts"
      pattern: "deductionBreakdown"
---

<objective>
Wire the Plan 01 engine into the single production scoring seam and remove the band layer. Delete `SEVERITY_CAP` / `apply_downward_cap` / `SEVERITY_CAP_PROVENANCE` from `vision_veto.py` (keeping all measurement/FaultKey/baseline/worst-pose helpers). Replace the band mutation inside `_apply_vision_veto` / `_apply_vision_veto_from_context` / `_collect_vision_fault_context` (all three currently call `apply_downward_cap`) with `deduction_engine.tally(...)`, setting `result['overallScore'] = breakdown.final` and `result['deductionBreakdown'] = breakdown.to_records()`. Persist the breakdown via `firestore_admin.complete_analysis` (flat list-of-dicts, nested-array validator passes). Preserve Phase 20/23 working parts unchanged: `_apply_score_suppression` (Mode3), the `GEMINI_VISION_VETO_ENABLED` toggle (reuse, no env rename), the Phase 23 still-pair fan-out + collect→coach→quantification ordering, and the `vision_fault_context is None` legacy fallback.

Purpose: this is the ONE seam (분기 0, 코드 1벌) where the forbidden severity→fixed-ceiling band lived. Replacing it here makes the production overallScore a transparent tally output everywhere (Lambda + RunPod share `_process`).

Output: band-free `vision_veto.py`, engine-wired `_process`, persisted `deductionBreakdown`, and seam integration tests.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-transparent-deduction-scoring/24-CONTEXT.md
@.planning/phases/24-transparent-deduction-scoring/24-RESEARCH.md
@.planning/phases/24-transparent-deduction-scoring/24-PATTERNS.md
@.planning/phases/24-transparent-deduction-scoring/24-01-PLAN.md
@backend/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove band layer from vision_veto.py (keep all measurement helpers)</name>
  <files>backend/shared/python/sunity_shared/analysis/vision_veto.py</files>
  <read_first>
    - backend/shared/python/sunity_shared/analysis/vision_veto.py (lines 1-41 docstring band section; lines 46-118 — `SEVERITY_CAP` L65, `SEVERITY_CAP_PROVENANCE` L89, `apply_downward_cap` L104; KEEP line 202 FaultKey, 266 fault_key_from_difference, 372 BASELINE_KINDS, 376 FramePairMeasurementContext, 497 body_relative_notches, 549 build_quantification_result, 786 fault_joints_from_differences, 934 worst_pose_timestamp)
    - 24-PATTERNS.md section "vision_veto.py (EDIT — remove band layer)" (exact removal targets + the exact code to delete)
    - 24-RESEARCH.md §"Old band cap to delete"
    - backend/functions/pipeline/app.py (lines 1879, 1899, 2046, 2127 — every `apply_downward_cap` call site that will break once removed; do NOT edit app.py in this task, just note the callers for Task 2)
  </read_first>
  <action>
    Delete the band layer from `vision_veto.py` per 24-CONTEXT ND-01 ([[scoring-must-be-transparent-deduction-tally]]): remove `SEVERITY_CAP` (lines ~65-70), `SEVERITY_CAP_PROVENANCE` (lines ~89-101), `apply_downward_cap` (lines ~104-118), and trim the band-specific docstring section (lines ~14-40 referencing the cap). KEEP everything else verbatim: `worst_pose_timestamp`, `fault_joints_from_differences`, `fault_key_from_difference`, `FaultKey`, `body_relative_notches`, `build_quantification_result`, `FramePairMeasurementContext`, `BASELINE_KINDS`, `_keypoint_set_for`. Preserve the file's score-free invariant in spirit — update the docstring to state ND-02 ("Gemini 은 점수를 절대 내지 않는다 — 측정대상/criterion 만 짚는다") rather than the old "비전은 점수를 올리지 않는다" cap framing. Do NOT introduce any replacement ceiling constant. This task makes app.py temporarily fail to import `apply_downward_cap` — Task 2 fixes all callers in the same plan/wave.
  </action>
  <verify>
    <automated>grep -nE "SEVERITY_CAP|apply_downward_cap|SEVERITY_CAP_PROVENANCE" backend/shared/python/sunity_shared/analysis/vision_veto.py; test $? -ne 0 && echo "BAND REMOVED"</automated>
    <automated>cd backend && python -c "from sunity_shared.analysis import vision_veto; assert hasattr(vision_veto, 'body_relative_notches') and hasattr(vision_veto, 'build_quantification_result') and hasattr(vision_veto, 'worst_pose_timestamp') and hasattr(vision_veto, 'BASELINE_KINDS'); assert not hasattr(vision_veto, 'apply_downward_cap') and not hasattr(vision_veto, 'SEVERITY_CAP')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "SEVERITY_CAP|apply_downward_cap|SEVERITY_CAP_PROVENANCE" backend/shared/python/sunity_shared/analysis/vision_veto.py` returns NOTHING.
    - `vision_veto.body_relative_notches`, `build_quantification_result`, `worst_pose_timestamp`, `fault_key_from_difference`, `BASELINE_KINDS`, `FramePairMeasurementContext` all still importable (the python -c assertion exits 0).
    - No replacement ceiling: `grep -nE "_CAP|min\\(overall|min\\(score" backend/shared/python/sunity_shared/analysis/vision_veto.py` returns nothing band-shaped.
  </acceptance_criteria>
  <done>vision_veto.py has zero band-cap code; all Phase 23 measurement + FaultKey + baseline + worst-pose helpers remain importable.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the _process veto seam with deduction_engine.tally + persist breakdown</name>
  <files>backend/functions/pipeline/app.py, backend/shared/python/sunity_shared/firestore_admin.py</files>
  <read_first>
    - backend/functions/pipeline/app.py (lines 245-256 `_gemini_vision_veto_enabled` toggle — REUSE; lines 1651 `_veto_passthrough`; 1753-1914 `_collect_vision_fault_context` incl. apply_downward_cap calls at 1879/1899; 1914-1971 `_build_vision_quantification_result` — KEEP; 1971-2155 `_apply_vision_veto` + `_apply_vision_veto_from_context` incl. apply_downward_cap at 2046/2127 + Mode3 passthrough at 2020-2023; 2454 `_apply_score_suppression` — KEEP; 3108-3157 `_process` seam call sites + collect→coach→quantification ordering comment)
    - backend/shared/python/sunity_shared/analysis/deduction_engine.py + ipsf_criteria.py (Plan 01 — tally signature, criterion_for_fault_key)
    - backend/shared/python/sunity_shared/firestore_admin.py (`complete_analysis` signature + `_validate_flat_dict_no_nested_array` / `_validate_dict_only_scalars` — the nested-array validator the breakdown must pass; precedent: how forceSignalsReport / visionVeto scoped validators were added)
    - backend/shared/python/sunity_shared/models.py (DEDUCTION_RECORD_KEYS / DEDUCTION_BREAKDOWN_KEYS from Plan 01)
    - 24-PATTERNS.md sections "pipeline/app.py (EDIT — replace the seam)" + "models.py"
    - 24-RESEARCH.md §"Architecture Patterns" diagram (PHASE 24 REPLACES HERE block) + §"Runtime State Inventory" (toggle reuse A4)
  </read_first>
  <action>
    Replace the band mutation at the ONE seam, both the context path and the legacy path, preserving ordering and toggles.

    In `_apply_vision_veto` and `_apply_vision_veto_from_context`: where they currently compute `capped = vision_veto.apply_downward_cap(overall, verdict.severity)` and set `result['overallScore'] = capped` / build the cap audit, instead call `deduction_engine.tally(quantification, vision_fault_context_or_ctx, baseline_kind=<from the FramePairMeasurementContext / quantification, default 'hip_line'>, technique_profile=profile)`. Set `result['overallScore'] = breakdown.final` and `result['deductionBreakdown'] = breakdown.to_records()`. Use `verdict.severity` ONLY to route the Gemini-pointed fault to its criterion via `ipsf_criteria.criterion_for_fault_key` (ND-02 — severity selects WHICH criterion, never a number). The `visionVeto` audit field stays for observability/back-compat but its `capApplied` semantics are retired (record the tally final + status instead; keep the field present so TRUST-08 audit/no-silent-failure still holds — emit a WARNING if the engine no-ops due to adapter failure). Keep the early `_veto_passthrough` branches: `disabled`, `mode3_held` (line 2020-2023 — Mode3 still absolute-delta, NO reference-anchored tally), `missing_local_video`, `missing_reference`, `skipped_error`.

    In `_collect_vision_fault_context` (lines 1879/1899): the two `apply_downward_cap` calls were computing a provisional capped score for the audit context. Replace with the band-free flow — collect still produces the verdict + supported_differences + FaultKeys (Phase 23 still-pair fan-out preserved); the actual deduction is computed once at the seam, not in collect. Remove the provisional cap; if a provisional score field is needed for the context, use the un-capped `overall_score` directly (no ceiling).

    In `_process` (3115-3149): keep the `if vision_fault_context is not None` / `else` structure and the `_build_vision_quantification_result(...)` call BEFORE the seam (Phase 23 ordering, KEEP). Keep `_gemini_vision_veto_enabled()` gating (REUSE `GEMINI_VISION_VETO_ENABLED`, no env rename — research A4, avoids Lambda+Pod env drift). Keep `_apply_score_suppression` for `MODE_SELF` (line 3156-3157, KEEP). After the seam sets `result['deductionBreakdown']`, ensure it flows to persistence.

    In `firestore_admin.complete_analysis`: add a `deduction_breakdown=` kwarg (mirror the `force_signals_report=` / `vision_veto` precedent) that writes `deductionBreakdown` to the analysis doc. Reuse / extend the existing nested-array validator so `deductionBreakdown.records` (list of flat dicts, scalar values) passes and any accidental nested array raises (firestore-nested-array-flat invariant). `lazy import deduction_engine` at the seam following the existing lazy-import-of-analysis-modules convention; the engine is pure (no adapter), so no `_ensure_adapters()` change.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_pipeline_deduction_seam.py -x -q</automated>
    <automated>grep -nE "apply_downward_cap" backend/functions/pipeline/app.py; test $? -ne 0 && echo "NO CAP CALLERS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "apply_downward_cap" backend/functions/pipeline/app.py` returns NOTHING (all callers replaced).
    - `app.py` calls the engine: `grep -n "deduction_engine.tally" backend/functions/pipeline/app.py` returns a hit.
    - `app.py` sets the breakdown: `grep -n "deductionBreakdown" backend/functions/pipeline/app.py` returns a hit.
    - No reintroduced final band: `grep -nE "min\\(overall|min\\(100|SEVERITY_CAP|_downward_cap" backend/functions/pipeline/app.py` returns nothing.
    - Preserved parts still referenced: `grep -nE "_apply_score_suppression|GEMINI_VISION_VETO_ENABLED|_build_vision_quantification_result|mode3_held" backend/functions/pipeline/app.py` returns hits for all four.
    - `firestore_admin.complete_analysis` accepts `deduction_breakdown=` and stores `deductionBreakdown`: `grep -n "deductionBreakdown\|deduction_breakdown" backend/shared/python/sunity_shared/firestore_admin.py` returns a hit.
    - `cd backend && python -m pytest tests/test_pipeline_deduction_seam.py -x -q` exits 0.
  </acceptance_criteria>
  <done>The single veto seam (context + legacy paths + collect) produces overallScore=breakdown.final via deduction_engine.tally; deductionBreakdown is persisted flat; toggle/Mode3-suppression/quantification-ordering preserved; zero apply_downward_cap callers remain.</done>
</task>

<task type="auto">
  <name>Task 3: Seam integration tests + full-suite regression</name>
  <files>backend/tests/test_pipeline_deduction_seam.py</files>
  <read_first>
    - backend/tests/test_kismam.py + backend/tests/conftest.py (test harness/imports)
    - existing pipeline integration tests (search `backend/tests/` for the file(s) that exercise `_apply_vision_veto` / `_process` with mocks — the analog for mocking the Gemini adapter + driving a synthetic verdict; e.g. test files referencing `_collect_vision_fault_context` or `visionVeto`)
    - backend/functions/pipeline/app.py (the edited seam from Task 2)
    - backend/shared/python/sunity_shared/analysis/deduction_engine.py (Plan 01)
    - 24-RESEARCH.md §"Validation Architecture" (determinism gate intent — same input twice → identical breakdown) + §"Common Pitfalls" 5 (determinism)
  </read_first>
  <behavior>
    - Test (band removed end-to-end): a fault verdict drives overallScore via tally, and overallScore is NOT one of the old fixed ceilings by construction (no 50/75/90 stamping) — assert overallScore == breakdown.final and final has no constant floor.
    - Test (breakdown persisted flat): the result dict carries deductionBreakdown as a list of flat dicts; the firestore nested-array validator accepts it (no nested array) and would reject a planted nested array.
    - Test (Mode3 preserved): MODE_SELF path still returns mode3_held passthrough / absolute-delta — engine does NOT inject a reference-anchored band for Mode3; _apply_score_suppression still runs.
    - Test (toggle preserved): GEMINI_VISION_VETO_ENABLED off → veto/engine path disabled passthrough (no overallScore mutation).
    - Test (determinism): running the seam twice on the same stored quantification + cached verdict yields byte-identical deductionBreakdown (Phase 18 exact-score drift resolved at the deterministic-geometry layer).
    - Test (severity not a score): swapping verdict.severity minor↔major with identical quantification yields identical overallScore.
  </behavior>
  <action>
    Create `test_pipeline_deduction_seam.py`. Mock the Gemini adapter / inject a synthetic `VisionFaultContext` + verdict + quantification (no Pod, no live Gemini — follow the existing pipeline-test mocking convention). Exercise `_apply_vision_veto` / `_apply_vision_veto_from_context` (and `_process` if feasible with mocks) for: Mode1 fault, Mode1 fault-free (high score result, not band-capped), Mode3 (mode3_held passthrough + suppression), toggle-off (disabled passthrough). Add the determinism test (same input twice → identical breakdown) and the severity-independence test at the seam level. Assert overallScore == deductionBreakdown.final and the breakdown serializes flat. Keep assertions directional/structural (no curve-fit to specific held-video scores — `# 보유 sweep 수치 타깃 아님`).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_pipeline_deduction_seam.py -x -q</automated>
    <automated>cd backend && python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_" backend/tests/test_pipeline_deduction_seam.py` ≥ 6.
    - `cd backend && python -m pytest tests/test_pipeline_deduction_seam.py -x -q` exits 0.
    - Full-suite regression: `cd backend && python -m pytest tests/ -q` exits 0 (existing pipeline/vision tests updated to the band-free seam, no regressions).
    - Determinism test present and green: `grep -n "determinis\|twice\|identical" backend/tests/test_pipeline_deduction_seam.py` returns a hit.
    - Mode3 + toggle preservation asserted: tests reference mode3_held / disabled passthrough and _apply_score_suppression.
  </acceptance_criteria>
  <done>Seam integration tests prove band removal, flat-breakdown persistence, Mode3/toggle/suppression preservation, determinism, and severity-independence; full backend suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQS event → _process | existing trigger; no change. |
| pipeline → Firestore Admin write | existing write path; deductionBreakdown added as flat list-of-dicts. |
| Gemini verdict → seam | severity routes a fault to a criterion; never a number (ND-02). |

No new auth/network surface — the seam swaps an in-process band call for an in-process pure tally call. Lambda layer + RunPod Pod must be redeployed/pulled to drop the old vision_veto (Plan 03 / deploy step).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-04 | Tampering | malformed/NaN quantification reaching the seam | mitigate | engine's `np.isfinite` guard (Plan 01); seam passes quantification straight to tally which skips non-finite criteria. |
| T-24-05 | Repudiation | silent no-op if the engine fails (TRUST-08 carryover) | mitigate | WARNING log + visionVeto audit field retained; determinism test catches a silently-dropped breakdown. |
| T-24-06 | Tampering | nested array smuggled into deductionBreakdown crashing Firestore | mitigate | reuse `_validate_flat_dict_no_nested_array`; test plants a nested array and asserts rejection. |
</threat_model>

<verification>
- `grep -nE "apply_downward_cap|SEVERITY_CAP" backend/functions/pipeline/app.py backend/shared/python/sunity_shared/analysis/vision_veto.py` returns NOTHING.
- `grep -n "deduction_engine.tally" backend/functions/pipeline/app.py` returns a hit; `grep -n "deductionBreakdown" backend/shared/python/sunity_shared/firestore_admin.py` returns a hit.
- `cd backend && python -m pytest tests/ -q` exits 0.
- Preserved: `_apply_score_suppression`, `GEMINI_VISION_VETO_ENABLED`, `_build_vision_quantification_result`, `mode3_held` all still referenced in app.py.
</verification>

<success_criteria>
- Band layer fully removed (vision_veto.py + all app.py callers) (SCORE-10).
- overallScore = deductionBreakdown.final at the one production seam, both context + legacy paths (SCORE-10, SCORE-16).
- deductionBreakdown persisted flat to Firestore (SCORE-16).
- Gemini severity routes-criterion-only, never scores (SCORE-13); unmapped faults surface as coverage gaps at the seam (SCORE-15).
- Phase 20/23 working parts preserved (Mode3 suppression, toggle, still-pair quantification ordering); determinism holds (advances SCORE-09 determinism portion).
</success_criteria>

<output>
Create `.planning/phases/24-transparent-deduction-scoring/24-02-SUMMARY.md` when done.
</output>
