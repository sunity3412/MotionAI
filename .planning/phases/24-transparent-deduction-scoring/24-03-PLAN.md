---
phase: 24-transparent-deduction-scoring
plan: 03
type: execute
wave: 3
depends_on: [24-01, 24-02]
files_modified:
  - backend/evals/phase24/assert_gates.py
  - backend/evals/phase24/README.md
  - backend/evals/phase18/assert_baseline.py
  - backend/tests/test_phase24_gates.py
autonomous: false
requirements: [SCORE-16, SCORE-09]
user_setup: []
must_haves:
  truths:
    - "phase24/assert_gates.py runs the 4 ND-07 gates (traceability, monotonicity, determinism, generalization-partial) pod-free and exits 0=PASS"
    - "Traceability gate proves final == 100 − Σ(records.points) and every record has non-null ruleId/criterion + finite measured/deviation"
    - "Monotonicity gate proves a synthetic deviation sweep yields a non-increasing final (zero inversions) per criterion"
    - "Determinism gate proves the same input yields a byte-identical breakdown across two runs"
    - "phase18/assert_baseline.py no longer asserts case-by-case expected-score/verdict bands (moderate≤75/major=50 retired); fault-label fixtures preserved"
    - "Generalization gate runs false-positive (coach stays high as a RESULT) + false-negative (faults drop) on the phase18 6 pairs pure-partial; unseen+above-cutoff sensitivity set is Pod-serial deferred"
  artifacts:
    - path: "backend/evals/phase24/assert_gates.py"
      provides: "Pod-free 4-gate checker replacing phase18 band asserts; exit 0 = PASS"
      contains: "traceability"
    - path: "backend/evals/phase18/assert_baseline.py"
      provides: "Band/expected-score asserts removed; fault-label regression source retained"
      contains: "pairs"
    - path: "backend/tests/test_phase24_gates.py"
      provides: "Unit-level coverage that assert_gates' traceability/monotonicity/determinism fail on a planted violation and pass on a valid breakdown"
      contains: "def test_"
  key_links:
    - from: "phase24/assert_gates.py"
      to: "deduction_engine.tally"
      via: "gates run the engine on synthetic + phase18-derived quantification"
      pattern: "deduction_engine|tally"
    - from: "phase24/assert_gates.py traceability gate"
      to: "DeductionBreakdown.records"
      via: "final == 100 − Σ points + non-null ruleId/criterion"
      pattern: "final|points|ruleId|ipsfAnchor"
---

<objective>
Replace the Phase 18 case-by-case expected-score band asserts (moderate≤75 / major=50 — the curve-fit manifest ND-07 retires) with the four ND-07 gates in a new `backend/evals/phase24/assert_gates.py` (pod-free, exit 0 = PASS): (1) traceability — every −point reverse-derivable, `final == 100 − Σ records.points`, each record has non-null ruleId + named criterion + finite measured/deviation; (2) monotonicity — synthetic deviation sweep per criterion yields a non-increasing final (zero inversions); (3) determinism — same stored input twice → byte-identical breakdown; (4) generalization (partial) — false-positive (coach/elite stays high as a RESULT, not a target) AND false-negative (fault videos drop) on the phase18 6 pairs, with the unseen + above-cutoff sensitivity-set portion explicitly Pod-serial deferred (asset not yet built — research Deferred). Retire only the band asserts in `phase18/assert_baseline.py`; keep `pairs.yaml` as the fault-label/regression source. Add a human-verify checkpoint for the Pod-serial generalization sweep (real-video, GPU, serial).

Purpose: ND-07 — "정은지 95~100 is a RESULT, not a target." The gates prove the tally is traceable/monotonic/deterministic without curve-fitting to held videos. This is SCORE-16's eval-gate body and SCORE-09's generalization-gate body.

Output: `phase24/assert_gates.py` + README, band-free `phase18/assert_baseline.py`, gate unit tests, and a Pod-serial generalization checkpoint.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-transparent-deduction-scoring/24-CONTEXT.md
@.planning/phases/24-transparent-deduction-scoring/24-RESEARCH.md
@.planning/phases/24-transparent-deduction-scoring/24-PATTERNS.md
@.planning/phases/24-transparent-deduction-scoring/24-01-PLAN.md
@.planning/phases/24-transparent-deduction-scoring/24-02-PLAN.md
@backend/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: phase24/assert_gates.py — 4 ND-07 gates (pod-free) + retire phase18 band asserts</name>
  <files>backend/evals/phase24/assert_gates.py, backend/evals/phase24/README.md, backend/evals/phase18/assert_baseline.py</files>
  <read_first>
    - backend/evals/phase18/assert_baseline.py (lines 1-75 — THE structure analog: pod-free self-consistency checker, `failures: list[str]`, `exit 0 = PASS`, YAML-import guard; lines 55-70 are the band asserts (`expected != verdict`, `f < s` margin) to RETIRE)
    - backend/evals/phase18/README.md + backend/evals/phase18/dataset (pairs.yaml) + backend/evals/phase18/baseline (the fault-label fixtures to KEEP as the generalization-gate input)
    - backend/shared/python/sunity_shared/analysis/deduction_engine.py + ipsf_criteria.py (Plan 01 — tally + CRITERION_GROUPS the gates exercise)
    - 24-RESEARCH.md §"Validation Architecture" → "The 4 ND-07 gates" (exact sampling/verification semantics) + §"Phase Requirements → Test Map"
    - 24-PATTERNS.md section "backend/evals/phase24/assert_gates.py" (the divergence: REMOVE expected-verdict/score asserts, REPLACE with 4 gates, keep pairs.yaml fault labels)
    - 24-VALIDATION.md (the Per-Task Verification Map to fill + the 4-gate sampling rate)
  </read_first>
  <action>
    Create `backend/evals/phase24/assert_gates.py` copying `phase18/assert_baseline.py:1-46` skeleton (shebang, pod-free docstring citing objectivity [[analysis-objectivity-no-human-scores]], `from __future__ import annotations`, json/pathlib/sys + YAML import guard, `_HERE`, `def main() -> int` accumulating `failures: list[str]`, `exit 0 = PASS`). Implement the four gates per 24-RESEARCH "The 4 ND-07 gates":

    (1) Traceability gate — for every breakdown in the fixture/synthetic set: assert `breakdown.final == max(0, round(100 - sum(-r.points for r in records)))` exactly AND each record has non-null `ruleId`, named `criterion`, finite `measuredValue`/`deviation`, and an `ipsfAnchor` (CoP citation or `engineering_interpretation`). belle's "명명백백하면 의외 점수도 OK" gate.

    (2) Monotonicity gate — synthetic sweep: for each criterion independently, feed increasing deviation (0,10,20,…,90°) holding others fixed through `deduction_engine.tally`; assert `final` is non-increasing (zero inversions). Pure, no Pod.

    (3) Determinism gate — run the same stored quantification through `tally()` twice; assert byte-identical breakdown (serialize both, compare). Replaces Phase 18 exact-score drift.

    (4) Generalization gate (partial) — using the phase18 `pairs.yaml` fault labels + stored quantification: assert false-negative direction (fault member of each discriminate pair produces a LOWER final than its success member) AND false-positive direction (the success/elite member stays high — assert a RESULT-shaped bound like `≥ ~95` framed as "emerges as a result", NOT a fitted target; use a directional/structural assert with a `# RESULT not target — curve-fit ban` comment). The unseen + above-cutoff sensitivity-set portion is NOT runnable here (asset deferred) — emit a clearly-labeled `SKIPPED (sensitivity set deferred — Pod-serial, see Task 3 checkpoint)` line, do NOT fail the gate for the missing asset, and do NOT fabricate it.

    Write `phase24/README.md` documenting the 4 gates, the pod-free vs Pod-serial split, and that 정은지 95~100 is a RESULT.

    Edit `backend/evals/phase18/assert_baseline.py`: REMOVE the case-by-case `expected != verdict` band assert and the `f < s` / margin score-band asserts (lines ~55-72). KEEP the pod-free skeleton + the `pairs.yaml` loading as a fault-label regression source (it now feeds the phase24 generalization gate). Add a docstring note that score-band asserts moved to `phase24/assert_gates.py` per ND-07 (curve-fit manifest retired).
  </action>
  <verify>
    <automated>cd backend && python evals/phase24/assert_gates.py; echo "exit=$?"</automated>
    <automated>grep -nE "expected != verdict|moderate|major.*50|f < s" backend/evals/phase18/assert_baseline.py; test $? -ne 0 && echo "BAND ASSERTS RETIRED"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/evals/phase24/assert_gates.py` exists and `cd backend && python evals/phase24/assert_gates.py` exits 0 (PASS) on the available pure + phase18-derived fixtures.
    - `assert_gates.py` implements all four gates: `grep -niE "traceab|monoton|determinis|generaliz" backend/evals/phase24/assert_gates.py` returns ≥ 4 distinct hits.
    - phase18 band asserts retired: `grep -nE "expected != verdict|f < s|moderate|major.*= ?50" backend/evals/phase18/assert_baseline.py` returns NOTHING.
    - phase18 fault-label fixtures preserved: `pairs.yaml` still loaded — `grep -n "pairs" backend/evals/phase18/assert_baseline.py` returns a hit; pairs.yaml file untouched.
    - Generalization gate does NOT fabricate the sensitivity set: `grep -n "SKIPPED\|deferred\|sensitivity" backend/evals/phase24/assert_gates.py` returns a hit (honest deferral, not a fake pass).
    - No curve-fit target: the false-positive bound carries a `# RESULT not target` style comment; manual review confirms no fitted-to-6-pairs score targets.
  </acceptance_criteria>
  <done>phase24/assert_gates.py runs the 4 ND-07 gates pod-free and passes; phase18 band/expected-score asserts retired while fault-label fixtures remain; sensitivity-set portion honestly deferred.</done>
</task>

<task type="auto">
  <name>Task 2: Gate unit tests — gates fail on planted violations, pass on valid breakdown</name>
  <files>backend/tests/test_phase24_gates.py</files>
  <read_first>
    - backend/evals/phase24/assert_gates.py (Task 1 — the gate functions to import/exercise; refactor each gate into an importable function returning failures so tests can call them directly)
    - backend/tests/test_kismam.py + conftest.py (harness)
    - backend/shared/python/sunity_shared/analysis/deduction_engine.py (Plan 01 — to build valid + planted-invalid breakdowns)
    - 24-RESEARCH.md §"The 4 ND-07 gates" (what a violation looks like for each gate)
  </read_first>
  <behavior>
    - test_traceability_gate_catches_mismatch: a breakdown where final ≠ 100 − Σ points → traceability gate reports a failure; a valid one → no failure.
    - test_traceability_gate_catches_null_rule: a record with ruleId=None and points<0 → failure (coverage-gap must be 0-deduction, not a null-rule penalty).
    - test_monotonicity_gate_catches_inversion: a synthetic engine/criterion that produces a non-monotonic sweep → failure; the real engine → pass.
    - test_determinism_gate_catches_drift: two non-identical breakdowns for the same input → failure; identical → pass.
    - test_generalization_partial_directional: fault member lower than success member passes; an inverted fixture fails. Sensitivity-set portion reports SKIPPED, not FAIL.
  </behavior>
  <action>
    Refactor `assert_gates.py` (if not already) so each gate is an importable function (e.g. `check_traceability(breakdowns) -> list[str]`) and `main()` composes them — this lets unit tests call gates with planted violations without running the full CLI. Create `test_phase24_gates.py` (copy `test_kismam.py` header/AWS-free convention) exercising each gate with (a) a valid breakdown built via `deduction_engine.tally` and (b) a planted-violation breakdown, asserting the gate catches the violation and passes the valid case. Assert the generalization gate's sensitivity portion returns a SKIPPED marker (not a failure) when the asset is absent.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_phase24_gates.py -x -q</automated>
    <automated>cd backend && python -m pytest tests/ -q && python evals/phase24/assert_gates.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_" backend/tests/test_phase24_gates.py` ≥ 5.
    - `cd backend && python -m pytest tests/test_phase24_gates.py -x -q` exits 0.
    - Each gate is importable as a function: `grep -nE "def check_|def gate_|def _gate" backend/evals/phase24/assert_gates.py` returns ≥ 3 hits.
    - Full regression + gate run: `cd backend && python -m pytest tests/ -q && python evals/phase24/assert_gates.py` exits 0.
    - Sensitivity-set deferral asserted as SKIPPED not FAIL in a test.
  </acceptance_criteria>
  <done>Gate functions are importable and unit-tested to catch traceability/monotonicity/determinism/generalization violations and pass valid breakdowns; full suite + gate runner green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Pod-serial generalization sweep (real-video, GPU) — belle verification</name>
  <read_first>
    - backend/scripts/sweep_phase15.py (the serial sweep entry; `--pair-sequential` flag — _process is not concurrency-safe, SERIAL only per [[pipeline-not-concurrency-safe-eval-serial]])
    - .planning/STATE.md (current Pod identity + RUNPOD_ANALYZE_URL sync; [[gemini-credits-depleted-2026-06-20]] — verify Gemini credits before sweep)
    - 24-RESEARCH.md §"Environment Availability" + §"Validation Architecture" (generalization gate is Pod-serial) + §"Runtime State Inventory" (Lambda layer + Pod must hold the NEW vision_veto — sam build --use-container + deploy; Pod git pull)
    - memory [[gsd-pod-work-push-first]] (push before Pod follows) + [[pod-ops-claude-runs]] (Claude runs the sweep; belle approves domain/production)
  </read_first>
  <what-built>
    Claude has, before this checkpoint, automated: (1) committed + pushed Plan 01/02/03 code; (2) `cd backend && sam build --use-container && sam deploy` to ship the band-free vision_veto + deduction_engine into the Lambda layer; (3) confirmed/started the RunPod Pod, `git pull`ed the new modules, restarted uvicorn, verified `/health`, and synced `RUNPOD_ANALYZE_URL`; (4) verified Gemini credits are non-zero; (5) run `backend/scripts/sweep_phase15.py --mode all --trigger direct-process --pair-sequential` SERIALLY over the phase18 6 pairs (+ any available unseen videos); (6) run `python backend/evals/phase24/assert_gates.py` against the live sweep output and captured the breakdown tallies.
  </what-built>
  <how-to-verify>
    1. Review the captured per-video deductionBreakdown tally lines (each "−X(criterion) −Y(criterion) … = final").
    2. Confirm 정은지 (선택 코치) correct videos land high (≈95-100) as a RESULT — verify it EMERGED from near-zero deductions, not a band stamp.
    3. Confirm fault videos (kip-up, pdshape, elbow-twist, etc.) DROP via named measured deductions (no band; the breakdown explains the score).
    4. Confirm no "보이는데 0감점" coverage-gap entry remains for a clearly-visible fault on the shipping path (ND-06) — if any coverage gap is logged, decide: add the measurement rule (back to Plan 01) or accept as known dev-gap.
    5. Confirm determinism: a re-run of the same video yields the identical breakdown.
    6. belle domain judgment: does each surprising score read as "명명백백" from its breakdown? (belle's core acceptance — not a target number, a traceable tally.)
  </how-to-verify>
  <resume-signal>Type "approved" to close the generalization gate, or list any coverage gaps / inversions / non-determinism to route back to Plan 01/02.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval runner → _process (Pod) | serial only; concurrent eval corrupts the global-shared `_process` ([[pipeline-not-concurrency-safe-eval-serial]]). |
| stored quantification fixtures → gates | pod-free gates consume stored/synthetic geometry; no live network. |

No new external surface — gates are read-only checkers; the Pod sweep uses the existing analyze path. Lambda layer + Pod must hold the NEW band-free module before the sweep (deploy/pull step in the checkpoint).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-07 | Repudiation | a fabricated/curve-fit sensitivity result passing the generalization gate | mitigate | sensitivity set honestly SKIPPED when absent (Task 1); false-positive bound framed as RESULT not target; objectivity gate ([[analysis-objectivity-no-human-scores]]) — no human score labels. |
| T-24-08 | DoS | concurrent eval corrupting shared `_process` | accept→mitigate | `--pair-sequential` enforced; checkpoint instructions mandate serial. |
| T-24-09 | Tampering | stale Lambda/Pod holding old band code passing the gate against new fixtures | mitigate | checkpoint requires sam build+deploy + Pod git pull + /health before sweep (research Runtime State canonical question). |
</threat_model>

<verification>
- `cd backend && python evals/phase24/assert_gates.py` exits 0 (4 gates pod-free).
- `cd backend && python -m pytest tests/ -q` exits 0 (gate unit tests + full regression).
- phase18 band asserts retired; pairs.yaml fault labels preserved.
- Pod-serial generalization sweep reviewed + belle-approved (checkpoint); coverage gaps = 0 on shipping path or explicitly accepted.
</verification>

<success_criteria>
- 4 ND-07 gates (traceability/monotonicity/determinism/generalization-partial) run pod-free and pass (SCORE-16 eval-gate body).
- phase18 case-by-case band/expected-score asserts retired; fault-label fixtures retained (ND-07).
- Generalization gate proves false-positive (coach high as RESULT) + false-negative (faults drop) on 6 pairs; sensitivity set honestly Pod-serial deferred (SCORE-09 generalization body).
- belle confirms each score is "명명백백" from its breakdown via the Pod-serial sweep (checkpoint).
</success_criteria>

<output>
Create `.planning/phases/24-transparent-deduction-scoring/24-03-SUMMARY.md` when done.
</output>
