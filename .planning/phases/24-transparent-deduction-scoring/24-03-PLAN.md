---
phase: 24-transparent-deduction-scoring
plan: 03
type: execute
wave: 3
depends_on: [24-01, 24-02]
files_modified:
  - backend/evals/phase24/assert_gates.py
  - backend/evals/phase24/README.md
  - backend/evals/phase18/assert_baseline.py
  - backend/tests/test_phase24_gates.py
autonomous: false
requirements: [SCORE-16, SCORE-09]
user_setup: []
must_haves:
  truths:
    - "phase24/assert_gates.py runs the pod-free ND-07 gates (traceability, monotonicity, determinism, criterion-selection) on SYNTHETIC fixtures and exits 0=PASS; the phase18 generalization check runs ONLY when an explicit Phase-24 breakdown artifact exists, else prints SKIPPED and acceptance accepts partial generalization skipped (HIGH-4)"
    - "Traceability gate proves final == max(0, round(100 + Σ(records.points))) (points signed-negative, HIGH-2) and every record has non-null ruleId/criterion + finite measured/deviation + a deviationSource + a numeric baselineValue; the quantification_unavailable fallback record (criterion='dimension_overall_fallback', unit='score_delta', deviationSource='dimension_overall', MEDIUM-1) PASSES this gate (100+Σpoints==final holds on the fallback path too — it is NOT an exception)"
    - "Monotonicity gate proves a synthetic deviation sweep yields a non-increasing final (zero inversions) per criterion, GIVEN A FIXED ACTIVATED CRITERION SET (the gate holds criterion selection constant)"
    - "Determinism gate scope is stated honestly: it is MATH-determinism (same input → byte-identical breakdown), conditional on the verdict cache keyed by frame-pair hash — NOT a claim that Gemini sampling is deterministic; a criterion-SELECTION-determinism assertion proves selection is a pure function of supported_differences"
    - "phase18/assert_baseline.py no longer asserts verdict-consistency (expected != verdict) or discriminate-margin (not (f < s) / margin) bands; fault-label fixtures (pairs.yaml) preserved"
    - "The pod-free generalization check is STRUCTURAL not numeric (MEDIUM-1): a success/elite member PASSES if its deductions are absent-or-within-tolerance for a TRACEABLE reason; a fault member PASSES if it shows a strictly larger shortfall in the same/named criterion than its success pair. The numeric high-score (≥~95) is an OBSERVATIONAL report in the Task 3 Pod checkpoint, NOT a pure-gate fail bound — it may be promoted to a hard gate only when an unseen+above-cutoff sensitivity set exists. The phase18 6-pair generalization runs ONLY against a real Phase-24 breakdown artifact (HIGH-4); the unseen sensitivity set is Pod-serial deferred"
    - "Mode3 deductionBreakdown is NOT silently expected: Success #2 breakdown is scoped to Mode1 (ROADMAP + Plan must_haves); Mode3 session-delta breakdown is an honest follow-up"
    - "The phase24 breakdown artifact (backend/evals/phase24/baseline/phase24_breakdowns.json) is GENERATED + COMMITTED by the Task 3 Pod-serial sweep (real generalization becomes real only after the Pod run), NOT synthesized in the pure gate; the term 'phase18-derived fixtures' is not used until that real artifact exists"
    - "The assert_gates verify command does NOT mask a nonzero gate exit (MEDIUM-3): the last shell command propagates the gate's exit code (no trailing `echo` that returns 0)"
  artifacts:
    - path: "backend/evals/phase24/assert_gates.py"
      provides: "Pod-free synthetic gate checker (traceability/monotonicity/determinism/criterion-selection) replacing phase18 verdict/margin asserts; exit 0 = PASS; importable gate functions; fallback-record-aware traceability; artifact-gated phase18 generalization (SKIPPED if the Phase-24 breakdown artifact is absent)"
      contains: "traceability"
    - path: "backend/evals/phase18/assert_baseline.py"
      provides: "Verdict-consistency + discriminate-margin asserts removed; pairs.yaml fault-label regression source retained"
      contains: "pairs"
    - path: "backend/tests/test_phase24_gates.py"
      provides: "Unit-level coverage that assert_gates' traceability/monotonicity/determinism/criterion-selection gates fail on a planted violation and pass on a valid breakdown (incl. the fallback record); the structural generalization check passes a success/fault pair structurally and reports SKIPPED when the artifact is absent"
      contains: "def test_"
  key_links:
    - from: "phase24/assert_gates.py"
      to: "deduction_engine.tally"
      via: "synthetic gates run the engine on synthetic quantification (NO phase18 tally inputs exist until the Pod artifact, HIGH-4)"
      pattern: "deduction_engine|tally"
    - from: "phase24/assert_gates.py traceability gate"
      to: "DeductionBreakdown.records"
      via: "final == max(0, round(100 + Σ points)) + non-null ruleId/criterion + deviationSource + numeric baselineValue; fallback record accepted"
      pattern: "final|points|ruleId|ipsfAnchor|deviationSource|baselineValue"
    - from: "phase24/assert_gates.py generalization check"
      to: "backend/evals/phase24/baseline/phase24_breakdowns.json"
      via: "artifact-gated — runs only if the Pod-generated breakdown artifact exists, else SKIPPED (HIGH-4)"
      pattern: "phase24_breakdowns|SKIPPED|artifact"
---

<objective>
Replace the Phase 18 case-by-case asserts (verdict-consistency `expected != verdict` ~L59 + discriminate-margin `not (f < s)` / `margin` ~L69-72 — the curve-fit-prone band manifest ND-07 retires) with the ND-07 gates in a new `backend/evals/phase24/assert_gates.py` (pod-free, exit 0 = PASS). The pure/synthetic gates are: (1) traceability — every −point reverse-derivable, `final == max(0, round(100 + Σ records.points))` (points signed-negative, HIGH-2), each record has non-null ruleId + named criterion + finite measured/deviation + a deviationSource + a numeric baselineValue, AND the quantification_unavailable fallback record (MEDIUM-1) PASSES (not an exception); (2) monotonicity — synthetic deviation sweep per criterion yields a non-increasing final (zero inversions), GIVEN A FIXED ACTIVATED CRITERION SET; (3) determinism — MATH-determinism (same stored input twice → byte-identical breakdown), explicitly conditional on the verdict cache, PLUS a criterion-SELECTION-determinism assertion (selection is a pure function of `supported_differences`). The phase18 generalization check is ARTIFACT-GATED (HIGH-4): it runs ONLY when a real Phase-24 breakdown artifact (`backend/evals/phase24/baseline/phase24_breakdowns.json`) exists, and it is STRUCTURAL not numeric (MEDIUM-1) — a success member PASSES if its deductions are absent-or-within-tolerance for a traceable reason; a fault member PASSES if it shows a strictly larger shortfall in the same named criterion than its success pair. If the artifact is absent it prints `SKIPPED (phase24 breakdown fixture absent)` and acceptance accepts "partial generalization skipped". Retire only the verdict/margin asserts in `phase18/assert_baseline.py`; keep `pairs.yaml` as the fault-label/regression source. The Task 3 Pod-serial sweep GENERATES + COMMITS the breakdown artifact (so generalization becomes real only after the Pod run), runs the numeric high-score observation (≥~95 is an OBSERVATIONAL report, not a pure fail-gate), and includes a cold-re-run criterion-selection check.

Purpose: ND-07 — "정은지 95~100 is a RESULT, not a target." The pure gates prove the tally is traceable/monotonic/deterministic on synthetic data without curve-fitting to held videos; the numeric high-score bound is NOT applied in the pure gate (MEDIUM-1 — a numeric ≥95 on the 6 정은지 pairs invites fitting slope/cap/tolerance, which are `[ASSUMED]`). This is SCORE-16's eval-gate body and SCORE-09's generalization-gate body. The determinism scope is stated honestly because the real Phase-18 drift was Gemini sampling (different fault set → different criterion selection), not tally math.

Output: `phase24/assert_gates.py` + README, band-free `phase18/assert_baseline.py`, gate unit tests, and a Pod-serial generalization checkpoint that generates the breakdown artifact.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-transparent-deduction-scoring/24-CONTEXT.md
@.planning/phases/24-transparent-deduction-scoring/24-RESEARCH.md
@.planning/phases/24-transparent-deduction-scoring/24-PATTERNS.md
@.planning/phases/24-transparent-deduction-scoring/24-01-PLAN.md
@.planning/phases/24-transparent-deduction-scoring/24-02-PLAN.md
@backend/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: phase24/assert_gates.py — pod-free synthetic gates (traceability/monotonicity/determinism/criterion-selection) + artifact-gated structural generalization + retire phase18 verdict/margin asserts</name>
  <files>backend/evals/phase24/assert_gates.py, backend/evals/phase24/README.md, backend/evals/phase18/assert_baseline.py</files>
  <read_first>
    - backend/evals/phase18/assert_baseline.py (the WHOLE file is ~120 lines — THE structure analog: pod-free self-consistency checker, `failures: list[str]`, `exit 0 = PASS`, YAML-import guard at L23-26, `_HERE`/`_PAIRS`/`_BASELINE` at L28-30, `def main() -> int` at L33. THE asserts to RETIRE are: the verdict-consistency assert `if expected != verdict:` at L59-62, and the discriminate-margin asserts `if f is None or s is None` / `elif not (f < s):` / `elif m != (s - f):` at L67-72. KEEP the `known_false_positive`/`known_gate_blocked` tracking at L76-94 and pairs loading. NOTE: there is NO `moderate`/`major`/`=50` string in this file — those live in vision_veto.SEVERITY_CAP, do not grep for them here.)
    - backend/evals/phase18/README.md + backend/evals/phase18/dataset/pairs.yaml (fault labels + S3 key ONLY, ~L41-119; `dominant_dimension: line` vs `angle` labels; NO Phase-24 tally inputs — no deductionBreakdown/quantification) + backend/evals/phase18/baseline/eval18_serial_baseline.json (OLD scorer snapshot: fault_overall/success_overall/margin/verdict ONLY ~L14-20 — NO tally/quantification/breakdown) — CONFIRMS the repo has NO Phase-24 generalization input today (HIGH-4): do NOT read the old overall as a tally result, do NOT call synthetic fixtures "phase18-derived"
    - backend/shared/python/sunity_shared/analysis/deduction_engine.py + ipsf_criteria.py (Plan 01 — tally + CRITERION_GROUPS + the criteria_for_fault router the gates exercise; the OBJECT to_dict() shape with baselineValue/baselineKind; the quantification_unavailable fallback record shape — criterion='dimension_overall_fallback', unit='score_delta', deviationSource='dimension_overall')
    - 24-RESEARCH.md §"Validation Architecture" → "The 4 ND-07 gates" (exact sampling/verification semantics) + §"Phase Requirements → Test Map"
    - 24-PATTERNS.md section "backend/evals/phase24/assert_gates.py" (the divergence: REMOVE verdict/margin asserts, REPLACE with the gates, keep pairs.yaml fault labels)
    - 24-VALIDATION.md (the Per-Task Verification Map to fill + the gate sampling rate)
  </read_first>
  <action>
    Create `backend/evals/phase24/assert_gates.py` copying `phase18/assert_baseline.py:1-33` skeleton (shebang, pod-free docstring citing objectivity [[analysis-objectivity-no-human-scores]], `from __future__ import annotations`, json/pathlib/sys + YAML import guard, `_HERE`, `def main() -> int` accumulating `failures: list[str]`, `exit 0 = PASS`). Implement each gate as an IMPORTABLE function (e.g. `check_traceability(breakdowns) -> list[str]`, `check_monotonicity() -> list[str]`, `check_determinism() -> list[str]`, `check_criterion_selection_determinism() -> list[str]`, `check_generalization(pairs, breakdowns) -> list[str]`) and compose them in `main()` (so Plan 03 Task 2 can unit-test gates directly). Implement per 24-RESEARCH "The 4 ND-07 gates":

    (1) Traceability gate — for every breakdown (the OBJECT {baseline, records, final, coverageGaps, fallback}) in the SYNTHETIC fixture set: assert `breakdown.final == max(0, round(100 + sum(r.points for r in records)))` exactly (points SIGNED NEGATIVE, HIGH-2 unified formula) AND each record has non-null `ruleId`, named `criterion`, finite `measuredValue`/`deviation`, a numeric `baselineValue`, a `deviationSource` ∈ {ipsf_absolute, reference_relative, dimension_overall}, and an `ipsfAnchor` (CoP citation or `engineering_interpretation`). The quantification_unavailable fallback record (criterion='dimension_overall_fallback', unit='score_delta', deviationSource='dimension_overall', baselineValue=100, MEDIUM-1) satisfies this gate by construction — explicitly include a fallback-path breakdown in the synthetic set and assert it PASSES (100+Σpoints==final on the fallback path), NOT that it is skipped. belle's "명명백백하면 의외 점수도 OK" gate.

    (2) Monotonicity gate — synthetic sweep: for each criterion independently, feed increasing deviation (0,10,20,…,90°) holding others fixed through `deduction_engine.tally`, HOLDING THE ACTIVATED CRITERION SET CONSTANT (same fault_context / supported_differences across the sweep so `criteria_for_fault` selection does not change); assert `final` is non-increasing (zero inversions). Pure, no Pod. Document inline that monotonicity is claimed only given a fixed criterion set (the per-criterion `min(raw, ipsf_cap)`-before-sum preserves global monotonicity for a fixed set — review-confirmed).

    (3) Determinism gate — TWO parts, scope stated honestly: (3a) MATH-determinism — run the same stored synthetic quantification + same supported_differences through `tally()` twice; assert byte-identical breakdown (serialize both via to_dict(), compare). State in the docstring this is conditional on the verdict cache keyed by frame-pair hash — it does NOT assert Gemini sampling is deterministic. (3b) CRITERION-SELECTION-determinism — assert the engine's criterion SELECTION (which criteria activate via `criteria_for_fault`) is a pure function of `supported_differences`: feed the same `supported_differences`/FaultKeys twice (varying only fields that must not matter, e.g. telemetry or severity) → identical activated criterion set. This is the gate that addresses the real Phase-18 drift (Gemini → different fault set → different selection); the cold-re-run-identical-selection check is in the Task 3 Pod checkpoint.

    (4) Generalization check (ARTIFACT-GATED + STRUCTURAL — HIGH-4 + MEDIUM-1) — `check_generalization(pairs, breakdowns)`: it runs the phase18 6-pair check ONLY when a real Phase-24 breakdown artifact `backend/evals/phase24/baseline/phase24_breakdowns.json` exists (the artifact maps each pair's correct/fault member to its real `deductionBreakdown` from the Pod sweep, Task 3). If the artifact is ABSENT, return a single `SKIPPED (phase24 breakdown fixture absent)` marker (NOT a failure) and do NOT fabricate inputs (do NOT read the old eval18 overall as a tally result, do NOT synthesize a breakdown). When the artifact IS present, the checks are STRUCTURAL not numeric (MEDIUM-1):
      · success/elite member PASSES if its deduction records are absent OR within-tolerance for a TRACEABLE reason (each present record's deviation ≤ its criterion tolerance, or zero records) — a structural property, NOT a number ≥ 95.
      · fault member PASSES if it shows a strictly LARGER shortfall in the SAME named criterion than its success pair (a structural relative check: same criterion id, fault.deviation > success.deviation) — false-negative direction.
    Do NOT apply a numeric `≥ ~95` bound in the pure gate (MEDIUM-1 — that invites curve-fitting the `[ASSUMED]` slope/cap/tolerance). The numeric high-score observation is an OBSERVATIONAL report in the Task 3 Pod checkpoint; it may be promoted to a hard fail-gate only once an unseen + above-cutoff sensitivity set exists. The unseen sensitivity-set portion stays Pod-serial deferred — emit `SKIPPED (sensitivity set deferred — Pod-serial, see Task 3 checkpoint)`.

    Write `phase24/README.md` documenting the gates, the determinism scope (math-determinism + criterion-selection-determinism, conditional on verdict cache — NOT a Gemini-sampling claim), the monotonicity-given-fixed-criterion-set scope, the traceability formula (final == max(0, round(100 + Σ points)) with signed-negative points, fallback record included), the STRUCTURAL artifact-gated generalization (HIGH-4 — runs only against the Pod-generated phase24_breakdowns.json; structural not numeric per MEDIUM-1; the ≥95 observation lives in the Pod checkpoint), the pod-free vs Pod-serial split, and that 정은지 95~100 is a RESULT.

    Edit `backend/evals/phase18/assert_baseline.py`: REMOVE the verdict-consistency assert (`if expected != verdict:` at L59-62) and the discriminate-margin asserts (`if f is None or s is None` / `elif not (f < s):` / `elif m != (s - f):` at L67-72, and the `discriminating += 1` / summary `discriminating_pairs` bookkeeping that depended on them). KEEP the pod-free skeleton, the pairs/baseline loading, the pair-set equality check, and the `known_false_positive`/`known_gate_blocked` known_issue tracking (L76-94) as a fault-label regression source (it now feeds the phase24 generalization gate). Add a docstring note that verdict/margin asserts moved to `phase24/assert_gates.py` per ND-07 (curve-fit-prone manifest retired).
  </action>
  <verify>
    <automated>cd backend && python evals/phase24/assert_gates.py</automated>
    <automated>grep -nE "expected != verdict|not \(f < s\)|m != \(s - f\)" backend/evals/phase18/assert_baseline.py; test $? -ne 0 && echo "VERDICT/MARGIN ASSERTS RETIRED"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/evals/phase24/assert_gates.py` exists and `cd backend && python evals/phase24/assert_gates.py` exits 0 (PASS) on the available SYNTHETIC gates (generalization SKIPPED when the artifact is absent). (MEDIUM-3 — the verify command is the bare gate invocation; its exit code is the gate's exit code, NOT masked by a trailing echo.)
    - `assert_gates.py` implements the gates as importable functions: `grep -nE "def check_traceab|def check_monoton|def check_determin|def check_criterion_selection|def check_generaliz" backend/evals/phase24/assert_gates.py` returns ≥ 5 hits.
    - Traceability uses the unified signed-negative formula + numeric baselineValue + accepts the fallback record (HIGH-2/MEDIUM-1): `grep -nE "100 \+ sum|sum\(.*points|dimension_overall_fallback|score_delta|baselineValue" backend/evals/phase24/assert_gates.py` returns hits (the gate adds signed-negative points, checks baselineValue, and includes the fallback-record path).
    - Generalization is ARTIFACT-GATED + STRUCTURAL (HIGH-4 + MEDIUM-1): `grep -nE "phase24_breakdowns|SKIPPED|artifact|absent" backend/evals/phase24/assert_gates.py` returns hits (the check skips when the artifact is absent); `grep -nE ">= ?95|>=95|>= ~95" backend/evals/phase24/assert_gates.py` returns NOTHING (no numeric high-score bound in the pure gate); the term "phase18-derived" is NOT used to describe a synthesized fixture — `grep -nE "phase18.derived|phase18-derived" backend/evals/phase24/assert_gates.py` returns NOTHING.
    - phase18 verdict/margin asserts retired: `grep -nE "expected != verdict|not \(f < s\)|m != \(s - f\)" backend/evals/phase18/assert_baseline.py` returns NOTHING.
    - phase18 fault-label fixtures preserved: `pairs.yaml` still loaded — `grep -n "pairs" backend/evals/phase18/assert_baseline.py` returns a hit; pairs.yaml file untouched; known_false_positive/known_gate_blocked tracking retained.
    - Determinism scope honest: `grep -niE "criterion.selection|selection.determin|math.determin|verdict cache" backend/evals/phase24/assert_gates.py backend/evals/phase24/README.md` returns hits (both math-determinism and criterion-selection-determinism present).
    - No curve-fit target: the structural generalization check carries a `# RESULT not target — curve-fit ban` style comment; manual review confirms no fitted-to-6-pairs score targets and no numeric high-score bound.
  </acceptance_criteria>
  <done>phase24/assert_gates.py runs the pod-free synthetic gates (importable functions) and passes; traceability uses the unified signed-negative formula + numeric baselineValue and accepts the quantification_unavailable fallback record (MEDIUM-1, not an exception); determinism gate scoped honestly (math + criterion-selection, conditional on verdict cache); monotonicity scoped to a fixed criterion set; the phase18 generalization check is artifact-gated (SKIPPED when phase24_breakdowns.json absent — HIGH-4) and STRUCTURAL not numeric (no ≥95 pure bound — MEDIUM-1); phase18 verdict/margin asserts retired while fault-label fixtures remain; sensitivity-set portion honestly deferred.</done>
</task>

<task type="auto">
  <name>Task 2: Gate unit tests — gates fail on planted violations, pass on valid breakdown (incl. fallback record), structural generalization + artifact-absent SKIPPED</name>
  <files>backend/tests/test_phase24_gates.py</files>
  <read_first>
    - backend/evals/phase24/assert_gates.py (Task 1 — the importable gate functions: check_traceability / check_monotonicity / check_determinism / check_criterion_selection_determinism / check_generalization)
    - backend/tests/test_kismam.py + conftest.py (harness)
    - backend/shared/python/sunity_shared/analysis/deduction_engine.py (Plan 01 — to build valid + planted-invalid breakdowns, incl. the quantification_unavailable fallback-record breakdown) + ipsf_criteria.py (criteria_for_fault for the selection test)
    - 24-RESEARCH.md §"The 4 ND-07 gates" (what a violation looks like for each gate)
  </read_first>
  <behavior>
    - test_traceability_gate_catches_mismatch: a breakdown where final ≠ max(0, round(100 + Σ points)) → traceability gate reports a failure; a valid one → no failure.
    - test_traceability_gate_passes_fallback_record (MEDIUM-1): a quantification_unavailable breakdown (one fallback record, points=dimension_overall-100, final=dimension_overall, baselineValue=100) → traceability gate reports NO failure (100+Σpoints==final holds; the fallback is not an exception).
    - test_traceability_gate_catches_null_rule: a record with ruleId=None and points<0 → failure (coverage-gap must be 0-deduction, not a null-rule penalty).
    - test_monotonicity_gate_catches_inversion: a synthetic engine/criterion that produces a non-monotonic sweep (with criterion set held fixed) → failure; the real engine → pass.
    - test_determinism_gate_catches_drift: two non-identical breakdowns for the same input → failure; identical → pass.
    - test_criterion_selection_determinism: identical supported_differences → identical activated criterion set passes; a fixture where selection varies on an irrelevant field (e.g. severity or telemetry) → failure.
    - test_generalization_structural_directional (MEDIUM-1): given a real-shaped breakdown artifact, the fault member showing a larger shortfall in the same named criterion than the success member passes; an inverted fixture (fault shortfall ≤ success) fails. Assert the check uses a structural same-criterion comparison, NOT a numeric ≥95 bound.
    - test_generalization_skipped_when_artifact_absent (HIGH-4): with no phase24_breakdowns.json present, check_generalization returns a SKIPPED marker (not a failure); the pure gate run still exits 0.
  </behavior>
  <action>
    Use the importable gate functions from `assert_gates.py` (Task 1 made each gate a function; `main()` composes them) so unit tests call gates with planted violations without running the full CLI. Create `test_phase24_gates.py` (copy `test_kismam.py` header/AWS-free convention) exercising each gate with (a) a valid breakdown built via `deduction_engine.tally`, (b) a planted-violation breakdown, and (c) the quantification_unavailable fallback-record breakdown (asserting traceability PASSES it — MEDIUM-1), asserting the gate catches the violation and passes the valid + fallback cases. Include the criterion-selection-determinism test (identical supported_differences → identical selection; irrelevant-field variation incl. severity must not change selection). Add the structural generalization test (fault shortfall > success shortfall in the same named criterion passes; inverted fails; assert NO numeric ≥95 bound) and the artifact-absent SKIPPED test (HIGH-4 — point check_generalization at a non-existent artifact path / empty breakdowns and assert it returns a SKIPPED marker, not a failure).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_phase24_gates.py -x -q</automated>
    <automated>cd backend && python -m pytest tests/ -q && python evals/phase24/assert_gates.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_" backend/tests/test_phase24_gates.py` ≥ 8.
    - `cd backend && python -m pytest tests/test_phase24_gates.py -x -q` exits 0.
    - Each gate is importable as a function: `grep -nE "def check_" backend/evals/phase24/assert_gates.py` returns ≥ 5 hits.
    - Fallback-record traceability tested (MEDIUM-1): `grep -n "fallback" backend/tests/test_phase24_gates.py` returns a hit asserting the unavailable fallback record passes traceability.
    - Criterion-selection-determinism unit-tested: `grep -nE "criterion_selection|selection_determinism" backend/tests/test_phase24_gates.py` returns a hit.
    - Structural generalization + artifact-absent SKIPPED tested (HIGH-4/MEDIUM-1): `grep -nE "structural|SKIPPED|artifact|shortfall" backend/tests/test_phase24_gates.py` returns hits; the test asserts no numeric ≥95 bound is applied.
    - Full regression + gate run: `cd backend && python -m pytest tests/ -q && python evals/phase24/assert_gates.py` exits 0.
  </acceptance_criteria>
  <done>Gate functions are importable and unit-tested to catch traceability/monotonicity/determinism/criterion-selection violations and pass valid breakdowns including the quantification_unavailable fallback record (MEDIUM-1); the generalization check is structural (same-criterion shortfall direction, no numeric ≥95 bound) and returns SKIPPED when the Phase-24 breakdown artifact is absent (HIGH-4); full suite + gate runner green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Pod-serial generalization sweep (real-video, GPU) — generate phase24_breakdowns.json artifact + belle verification</name>
  <read_first>
    - backend/scripts/sweep_phase15.py (the serial sweep entry; `--pair-sequential` flag — _process is not concurrency-safe, SERIAL only per [[pipeline-not-concurrency-safe-eval-serial]])
    - .planning/STATE.md (current Pod identity + RUNPOD_ANALYZE_URL sync; [[gemini-credits-depleted-2026-06-20]] — verify Gemini credits before sweep)
    - 24-RESEARCH.md §"Environment Availability" + §"Validation Architecture" (generalization gate is Pod-serial) + §"Runtime State Inventory" (Lambda layer + Pod must hold the NEW vision_veto — sam build --use-container + deploy; Pod git pull)
    - memory [[gsd-pod-work-push-first]] (push before Pod follows) + [[pod-ops-claude-runs]] (Claude runs the sweep; belle approves domain/production) + [[scoring-redesign-must-generalize-no-overfit]] (curve-fit ban — feedback may change COVERAGE or GROUPING, never re-fit slope/cap to a target)
  </read_first>
  <what-built>
    Claude has, before this checkpoint, automated: (1) committed + pushed Plan 01/02/03 code; (2) `cd backend && sam build --use-container && sam deploy` to ship the band-free vision_veto + deduction_engine into the Lambda layer; (3) confirmed/started the RunPod Pod, `git pull`ed the new modules, restarted uvicorn, verified `/health`, and synced `RUNPOD_ANALYZE_URL`; (4) verified Gemini credits are non-zero; (5) run `backend/scripts/sweep_phase15.py --mode all --trigger direct-process --pair-sequential` SERIALLY over the phase18 6 pairs (+ any available unseen videos); (6) CAPTURED each pair member's real `deductionBreakdown` into `backend/evals/phase24/baseline/phase24_breakdowns.json` (the artifact HIGH-4 requires) and COMMITTED it; (7) run `python backend/evals/phase24/assert_gates.py` against the live artifact (now the structural generalization check ACTUALLY runs, no longer SKIPPED) and captured the breakdown tallies; (8) run the numeric high-score OBSERVATION (정은지 correct ≈95-100) as a report line, NOT a pass/fail gate; (9) re-run ONE video a second time (cold) and captured its breakdown for the determinism/selection check.
  </what-built>
  <how-to-verify>
    1. Confirm `backend/evals/phase24/baseline/phase24_breakdowns.json` was generated + committed from the live sweep (HIGH-4 — the structural generalization check now runs against a REAL artifact, not a synthesized one).
    2. Review the captured per-video deductionBreakdown tally lines (each "−X(criterion) −Y(criterion) … = final").
    3. Confirm 정은지 (선택 코치) correct videos land high (≈95-100) as an OBSERVATION — verify it EMERGED from near-zero deductions, not a band stamp. (This is a report, NOT a numeric pure-gate fail bound — MEDIUM-1.)
    4. Confirm the STRUCTURAL generalization holds: each fault member shows a strictly larger shortfall in the SAME named criterion than its success pair (kip-up, pdshape, elbow-twist, etc.) — DROP via named measured deductions (no band; the breakdown explains the score). Confirm kip-up used baseline_kind='floor' WHEN the move is named (Gemini named it / studio term "kip-up" in profile.name or motion_id) — the only case the floor baseline is exercised. For a truly-unnamed/unknown move, baseline honestly defaults to hip_line (NOT a regression — no universal per-move baseline claim, BLOCKER B). If kip-up arrives unnamed (recognizer returned "미상"), note it as a recognizer-coverage follow-up, not a Phase 24 baseline failure.
    5. Confirm no "보이는데 0감점" coverage-gap entry remains for a clearly-visible fault on the shipping path (ND-06) — if any coverage gap is logged, decide: add the measurement rule (back to Plan 01, COVERAGE change only) or accept as a TRACKED known dev-gap. (Curve-fit ban: any feedback may change COVERAGE or GROUPING only — never re-fit the single slope or a cap to hit a target score, [[scoring-redesign-must-generalize-no-overfit]].)
    6. Confirm determinism: the cold re-run of the same video yields the identical breakdown AND the identical activated criterion SELECTION (the real Phase-18 drift check — selection is a pure function of supported_differences).
    7. belle domain judgment: does each surprising score read as "명명백백" from its breakdown? (belle's core acceptance — not a target number, a traceable tally.)
  </how-to-verify>
  <resume-signal>Type "approved" to close the generalization gate, or list any coverage gaps / inversions / non-determinism / selection-drift to route back to Plan 01/02. Any score-tuning request must be expressed as a COVERAGE or GROUPING change, never a slope/cap re-fit.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval runner → _process (Pod) | serial only; concurrent eval corrupts the global-shared `_process` ([[pipeline-not-concurrency-safe-eval-serial]]). |
| stored quantification fixtures → gates | pod-free gates consume synthetic geometry; no live network. The phase18 generalization check consumes ONLY a real Pod-generated breakdown artifact (HIGH-4), never a synthesized one. |

No new external surface — gates are read-only checkers; the Pod sweep uses the existing analyze path. Lambda layer + Pod must hold the NEW band-free module before the sweep (deploy/pull step in the checkpoint).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-07 | Repudiation | a fabricated/curve-fit sensitivity result passing the generalization gate, OR a synthetic fixture masquerading as phase18-derived (HIGH-4) | mitigate | generalization check is ARTIFACT-GATED (runs only against the real Pod-generated phase24_breakdowns.json; SKIPPED if absent) and STRUCTURAL not numeric (no ≥95 bound — MEDIUM-1); sensitivity set honestly SKIPPED when absent; objectivity gate ([[analysis-objectivity-no-human-scores]]); slope/cap re-fit forbidden (checkpoint resume-signal). |
| T-24-08 | DoS | concurrent eval corrupting shared `_process` | accept→mitigate | `--pair-sequential` enforced; checkpoint instructions mandate serial. |
| T-24-09 | Tampering | stale Lambda/Pod holding old band code passing the gate against new fixtures | mitigate | checkpoint requires sam build+deploy + Pod git pull + /health before sweep (research Runtime State canonical question). |
| T-24-11 | Repudiation | a failing gate reported green because the verify command's last token is a 0-returning echo (MEDIUM-3) | mitigate | the verify command is the bare `python evals/phase24/assert_gates.py` (its exit code propagates); no trailing `echo "exit=$?"` masks the gate. |
</threat_model>

<verification>
- `cd backend && python evals/phase24/assert_gates.py` exits 0 (synthetic gates pod-free; generalization SKIPPED until the Pod artifact exists; exit code not masked — MEDIUM-3).
- `cd backend && python -m pytest tests/ -q` exits 0 (gate unit tests + full regression).
- phase18 verdict/margin asserts retired; pairs.yaml fault labels preserved.
- Pod-serial generalization sweep reviewed + belle-approved (checkpoint); phase24_breakdowns.json generated + committed (HIGH-4); structural generalization holds against the real artifact; coverage gaps = 0 on shipping path or explicitly accepted; cold-re-run criterion selection identical.
</verification>

<success_criteria>
- Pod-free synthetic gates (traceability/monotonicity[fixed-set]/determinism[math+selection]) run and pass (SCORE-16 eval-gate body); traceability uses the unified signed-negative formula + numeric baselineValue and accepts the unavailable fallback record (HIGH-2/MEDIUM-1).
- Determinism gate scope honest: math-determinism (conditional on verdict cache) + criterion-selection-determinism (pure function of supported_differences) — not a Gemini-sampling claim (SCORE-09 determinism body).
- phase18 verdict-consistency + discriminate-margin asserts retired; fault-label fixtures retained (ND-07).
- Generalization check is ARTIFACT-GATED (runs only against the real Pod-generated phase24_breakdowns.json, SKIPPED if absent — HIGH-4) and STRUCTURAL not numeric (success member traceable-within-tolerance; fault member larger same-criterion shortfall; no ≥95 pure bound — MEDIUM-1); the numeric high-score is an OBSERVATION in the Pod checkpoint; sensitivity set honestly Pod-serial deferred (SCORE-09 generalization body).
- The assert_gates verify command does not mask a nonzero exit (MEDIUM-3).
- belle confirms each score is "명명백백" from its breakdown + identical cold-re-run selection via the Pod-serial sweep, and the phase24_breakdowns.json artifact is generated + committed (checkpoint).
</success_criteria>

<output>
Create `.planning/phases/24-transparent-deduction-scoring/24-03-SUMMARY.md` when done.
</output>
</content>
