---
phase: 24-transparent-deduction-scoring
plan: 04
type: execute
wave: 4
gap_closure: true
depends_on: [24-01, 24-02, 24-03]
files_modified:
  - backend/functions/pipeline/app.py
  - backend/shared/python/sunity_shared/analysis/vision_veto.py
  - backend/tests/test_pipeline_deduction_seam.py
  - backend/tests/test_vision_veto.py
autonomous: true
requirements: [SCORE-16]
user_setup: []
must_haves:
  truths:
    - "low_alignment_confidence is now TALLY-ELIGIBLE for the MEASURED-deviation seed only: when the seam routes a ctx with collection_status=low_alignment_confidence, deduction_engine.tally STILL runs on the seam-built measured_deviations (RTMW extension deficits + body-relative notches — objective, alignment-independent) so a measurable geometric deviation DEDUCTS even though Gemini alignment was low. Gemini vision-located faults stay ABSENT (the ctx carries supported_differences=[] so criteria_for_fault adds nothing — no fabricated faults, objectivity preserved [[analysis-objectivity-no-human-scores]])."
    - "The Phase 23 collect-side bail is UNCHANGED: _collect_vision_fault_context still returns _ctx('low_alignment_confidence', frame_pairs=[]) before the Gemini call (we do NOT call Gemini on poorly-aligned frames). The fix is ONLY at the apply/tally seam: measured deviations deduct; Gemini stays silent for this member."
    - "Audit transparency: when low_alignment_confidence yields measured deductions, the visionVeto audit emits status=applied AND preserves collectionStatus=low_alignment_confidence so the report shows the deduction was measurement-only (no Gemini-located fault). When the measured substrate is clean (no records), status=not_applicable with the breakdown attached (final unchanged). No silent failure (TRUST-08)."
    - "Objectivity hard line: the measured seed is RTMW extension_deviation (deg, 180-relative, profile.expects_extension gated) + quantification body-relative notches ONLY — never a 0-100 dimension SCORE as a deviation (HIGH-3 preserved). The measured deduction uses the SAME explicit dimensions.py rules as every other path (same slope, all videos) — no curve-fit, no per-video band ([[scoring-must-be-transparent-deduction-tally]] / [[scoring-redesign-must-generalize-no-overfit]])."
    - "Phase 20/23 working parts preserved: eligible_for_coach stays candidate_verdict-only (low_alignment does NOT inject coach root-cause — alignment was low, no Gemini cause to render); the GEMINI_VISION_VETO_ENABLED toggle, Mode3 mode3_held, the still-pair fan-out, and the candidate_verdict / no_fault tally paths are byte-unchanged. Only low_alignment_confidence moves from passthrough to measured-eligible."
    - "Pure unit gates GREEN: a low_alignment ctx + a non-trivial measured_deviations substrate -> tally produces measured records -> status=applied + final<100; a low_alignment ctx + clean substrate -> not_applicable + final unchanged + breakdown attached; objectivity test asserts NO criteria_for_fault activation (supported_differences=[] path adds zero Gemini-located records)."
  artifacts:
    - path: "backend/functions/pipeline/app.py"
      provides: "_apply_vision_veto_from_context: low_alignment_confidence added to the tally-eligible branch (measured seed only); audit preserves collectionStatus provenance; coach-eligibility + all other statuses unchanged"
      contains: "low_alignment_confidence"
    - path: "backend/tests/test_pipeline_deduction_seam.py"
      provides: "seam test: low_alignment ctx + measured substrate -> measured deduction applied (final<100, no Gemini-located records); clean substrate -> not_applicable"
      contains: "low_alignment"
  key_links:
    - from: "pipeline/app.py::_apply_vision_veto_from_context"
      to: "deduction_engine.tally"
      via: "low_alignment_confidence now routes measured_deviations into tally (Gemini differences empty)"
      pattern: "deduction_engine|tally"
---

<objective>
Close the Task 3 (24-03) real-video BLOCKER: the transparent deduction engine never engaged on
real fault video because `_apply_vision_veto_from_context` treated `low_alignment_confidence` as a
score-free passthrough — so the iter-5 measured-deviation seed (the Gemini-silent defense) could not
fire, every fault member got a null `deductionBreakdown`, and kip-up's false positive stayed at 100.

belle decision (2026-06-26, Option A — 측정-감점 분리): make `low_alignment_confidence`
TALLY-ELIGIBLE for the MEASURED seed only. The objective RTMW measured deviations (extension
deficits, body-relative notches — independent of student↔reference DTW alignment) must DEDUCT via
the same explicit `dimensions.py` rules; the Gemini vision-located faults stay deferred (we do not
trust Gemini's fault localization on poorly-aligned frames, and we never fabricate faults). This
realizes [[scoring-must-be-transparent-deduction-tally]]: "Gemini는 측정대상 짚기만, 점수는
측정+규칙" — measurement deducts regardless of whether Gemini could align.

This is a COVERAGE/gating fix, NOT a slope/cap re-fit ([[scoring-redesign-must-generalize-no-overfit]]).
</objective>

<context>
Root-cause evidence (committed): `backend/evals/phase24/baseline/phase24_sweep_report.json` +
`phase24_breakdowns.json` — 10/12 members returned visionVeto.status=low_alignment_confidence ->
null breakdown. Diagnosis: `assess_alignment_confidence` (vision_veto.py:743) gates on the
reference DTW match distance (> `_ALIGN_GLOBAL_T2`=25) AND weak local path; fault videos diverge
from the reference (the signal we want to score) -> read as "cannot align" -> the collect path
bails BEFORE Gemini, and the apply path then passthroughs without a tally.

Key code:
- `_collect_vision_fault_context` (app.py ~1753): on low alignment returns
  `_ctx("low_alignment_confidence", frame_pairs=[])` — ctx is non-None, supported_differences=[],
  verdict=None, cap_would_apply=False, eligible_for_coach=False. UNCHANGED by this plan.
- Seam (app.py ~3268-3318): `baseline_kind` + `measured_deviations` are built whenever
  `vision_fault_context is not None` (line 3269) — so for low_alignment the measured substrate
  (RTMW angles+profile) IS already built and passed to `_apply_vision_veto`.
- `_apply_vision_veto_from_context` (app.py ~2208-2306): line ~2232
  `if status not in ("candidate_verdict", "no_fault")` routes low_alignment into `passthrough_map`
  (no tally). THIS is the single seam to change.
- `deduction_engine.tally(quant, ctx, ..., measured_deviations=md, ...)`: with ctx.supported_differences=[]
  the `criteria_for_fault` router adds nothing; only `criteria_from_measured_deviations` (the measured
  seed) fires — exactly the desired measured-only behavior.
- `VisionFaultContext.to_audit_dict(final_status=...)` accepts only "applied"/"not_applicable" and
  already emits `collectionStatus` (the pre-final status) so low_alignment provenance is preserved.

Objectivity boundary: the measured seed is `dimensions.extension_deviation` (deg, profile-gated) +
quantification body-relative notches only — NEVER a 0-100 dimension SCORE as a deviation (HIGH-3).
No Gemini faults are added (empty supported_differences). No human score labels.
</context>

<tasks>

<task number="1">
  <name>Task 1: low_alignment_confidence -> tally-eligible (measured seed only) at the apply seam</name>
  <files>backend/functions/pipeline/app.py, backend/shared/python/sunity_shared/analysis/vision_veto.py</files>
  <steps>
  In `_apply_vision_veto_from_context` (app.py ~2228): extend the tally-eligible set so
  `collection_status == "low_alignment_confidence"` runs the tally on `measured_deviations`
  (the RTMW measured seed) instead of falling into `passthrough_map`. Concretely:
    - Change the gate at ~2232 from `if status not in ("candidate_verdict", "no_fault"):` to also
      admit `"low_alignment_confidence"` into the tally branch (keep low_alignment OUT of the
      passthrough_map). The remaining non-measuring statuses (low_alignment_confidence's siblings:
      resource_limited, disabled, mode3_held, missing_reference, missing_current_video,
      skipped_error) stay score-free passthrough exactly as today.
    - In the tally branch, the existing code already does the right thing for low_alignment because
      `ctx.supported_differences` is empty (criteria_for_fault adds nothing) and `quant` falls back
      to unavailable — so ONLY `criteria_from_measured_deviations` fires. Verify the `quant is None`
      fallback (lines ~2254-2260) still builds the unavailable VisionQuantificationResult for the
      low_alignment ctx (selected_frame_pairs=[] -> quantification was built with selected_pair=None
      upstream; if `quantification` arg is falsy here, the existing fallback handles it).
    - Audit honesty: when `has_deduction` is True for a low_alignment ctx, call
      `ctx.to_audit_dict(final_status="applied", breakdown_final=breakdown.final, quantification=quant)`
      — to_audit_dict already emits `collectionStatus="low_alignment_confidence"`, so the report
      transparently shows the deduction was measurement-only (no Gemini-located fault). Do NOT attach
      faultJoints/faultJointDeficits derived from Gemini differences when supported_differences is
      empty (they will be empty anyway via fault_joints_from_differences(())). When `has_deduction`
      is False, the existing `not_applicable` return (with breakdown attached) already applies.
  Do NOT touch `_collect_vision_fault_context` (the Phase 23 collect-side bail stays — no Gemini on
  poorly-aligned frames). Do NOT change `eligible_for_coach` (low_alignment must NOT inject coach
  root-cause — candidate_verdict-only stays). Do NOT change `assess_alignment_confidence` thresholds
  (no re-calibration — this is a routing/coverage fix, not a slope change).
  If `vision_veto.py` needs an enum/vocabulary note (e.g. documenting that low_alignment_confidence is
  now measured-eligible at the apply seam while remaining a collect-side bail), update the comment at
  the collection_status enum (~206-216) — comment/docstring only, no shape/enum change.
  </steps>
  <verify>
  `cd backend && PYTHONPATH=shared/python:. python3 -m pytest tests/test_vision_veto.py tests/test_pipeline_vision_gate.py -q` green;
  grep shows low_alignment_confidence removed from passthrough_map and present in the tally branch.
  </verify>
  <done>low_alignment_confidence routes the measured seed into deduction_engine.tally; Gemini faults stay absent; collect-side bail + coach-eligibility + thresholds unchanged.</done>
</task>

<task number="2">
  <name>Task 2: unit + seam tests — measured deduction fires under low alignment, no fabricated Gemini faults</name>
  <files>backend/tests/test_pipeline_deduction_seam.py, backend/tests/test_vision_veto.py</files>
  <steps>
  Add tests proving:
    (a) a `VisionFaultContext(collection_status="low_alignment_confidence", supported_differences=[],
        verdict=None)` + a non-trivial `measured_deviations` (e.g. `{"leg_extension": 35.0}`) routed
        through `_apply_vision_veto_from_context` yields `overallScore == breakdown.final < dimension_overall`,
        a non-empty `deductionBreakdown.records` whose records are ALL measured (deviationSource ∈
        {ipsf_absolute, reference_relative}, NO criteria_for_fault/Gemini-located records), and a
        visionVeto audit with status=applied + collectionStatus=low_alignment_confidence.
    (b) the SAME low_alignment ctx + a CLEAN measured substrate (`{}`) yields status=not_applicable,
        breakdown attached, final unchanged.
    (c) objectivity: assert that with supported_differences=[] NO record carries a Gemini fault
        criterion that only criteria_for_fault could have produced (i.e. records ⊆ measured-seed criteria).
    (d) regression: candidate_verdict and no_fault paths are unchanged (existing tests still green);
        the other passthrough statuses (resource_limited/disabled/missing_reference/...) still produce
        score-free passthrough (no deductionBreakdown).
  </steps>
  <verify>
  `cd backend && PYTHONPATH=shared/python:. python3 -m pytest tests/test_pipeline_deduction_seam.py tests/test_vision_veto.py tests/test_pipeline_vision_gate.py tests/test_deduction_engine.py -q` green (no regressions).
  </verify>
  <done>tests prove measured-only deduction under low alignment + no fabricated Gemini faults + unchanged sibling statuses.</done>
</task>

<task number="3">
  <name>Task 3: full targeted suite + band-grep regression</name>
  <files>backend/tests/test_pipeline_deduction_seam.py</files>
  <steps>
  Run the Phase 24 + Phase 20/23 targeted suites together to confirm no cross-regression:
  test_deduction_engine, test_pipeline_deduction_seam, test_vision_veto, test_pipeline_vision_gate,
  test_pipeline_mode3, test_gemini_vision_scorer. Re-run the band-free grep
  (`apply_downward_cap|SEVERITY_CAP|capApplied`) across backend/shared/python + backend/functions ->
  must stay 0. No production code beyond app.py/vision_veto.py changed.
  </steps>
  <verify>
  targeted suites green; band grep = 0.
  </verify>
  <done>no cross-regression; band stays removed; only the seam routing changed.</done>
</task>

</tasks>

<acceptance>
- low_alignment_confidence is tally-eligible for the measured seed; objective RTMW deviations deduct under low alignment while Gemini faults stay absent (objectivity preserved).
- Pure unit + seam tests green; band grep 0; Phase 20/23 working parts unchanged.
- The Pod re-sweep (orchestrator, post-merge) regenerates phase24_breakdowns.json and is re-presented to belle: fault members now carry measured deductionBreakdown records (transparent −X−Y−Z), kip-up FP re-checked, generalization gate runs against real (non-null) breakdowns. Re-sweep + belle verification is the Task 3 checkpoint resume (not part of this autonomous plan's code gates).
</acceptance>
