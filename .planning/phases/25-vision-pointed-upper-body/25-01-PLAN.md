---
phase: 25-vision-pointed-upper-body
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/shared/python/sunity_shared/analysis/vision_veto.py
  - backend/functions/pipeline/app.py
  - backend/tests/test_vision_pointed_mapper.py
  - backend/tests/test_deduction_seed_pointed_merge.py
autonomous: true
requirements: [SCORE-15]

must_haves:
  truths:
    - "Gemini 가 짚은(supported faultKey) 관절만 worst-window median 값으로 감점 seed 가 방출되고, Gemini-silent 관절은 기존 full-path DTW median 을 유지한다 (관절 단위 merge — 경로 either/or 아님)"
    - "라인/전신/torso/head_neck/grip 류 faultKey 는 window 승격 대상에서 제외된다 (broad 확장으로 vision 게이트 무력화 금지)"
    - "side 미명시(unknown) faultKey 는 양측 관절 모두 window-측정 eligible — 판정은 측정+tol 20° 게이트가 한다 (fail-closed 버리기 금지)"
    - "vision_fault_context 부재(legacy/mode3) 경로는 동작 불변 (pointed=None → 기존 fallback 전 관절)"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/vision_veto.py"
      provides: "좁은 pointed-joint 매퍼 pointed_joints_from_supported_differences (순수 함수)"
      contains: "def pointed_joints_from_supported_differences"
    - path: "backend/functions/pipeline/app.py"
      provides: "_build_deduction_measured_deviations 관절 단위 2-source merge + vision_pointed_joints kwarg + seed_audit_out 관측 파라미터"
      contains: "vision_pointed_joints"
    - path: "backend/tests/test_vision_pointed_mapper.py"
      provides: "매퍼 단위 테스트 (narrow/side/broad-제외)"
      min_lines: 40
    - path: "backend/tests/test_deduction_seed_pointed_merge.py"
      provides: "seed merge 단위 테스트 (pointed=window / silent=fallback / cross-exclusion)"
      min_lines: 60
  key_links:
    - from: "backend/functions/pipeline/app.py (~3507 vision_fault_context 분기)"
      to: "vision_veto.pointed_joints_from_supported_differences"
      via: "supported_differences → pointed joints 도출 후 _build_deduction_measured_deviations kwarg"
      pattern: "pointed_joints_from_supported_differences"
    - from: "_build_deduction_measured_deviations"
      to: "quantification.windowMedianAngleDeltas"
      via: "pointed ∩ wm 관절만 abs(delta_deg) 방출"
      pattern: "windowMedianAngleDeltas"
---

<objective>
Phase 25 핵심 배선 (Option A, seed-stage — 25-RESEARCH §2): "짚기=Gemini faultKey / 측정=짚인 관절만 worst-window median / 감점=기존 규칙(tol 20°+slope 1.2)" 역할 분리 아키텍처의 코드 본체. quick 260702-o0c(f513587, reverted) 의 경로 단위 either/or 를 **관절 단위 merge** 로 바꿔 재배선한다 — 260702-o0c FAIL 원인(Gemini-silent 관절까지 window 편향 표집 → success 위양성 4건)의 정확한 해소.

Orchestrator 결정 반영:
- OD-1 (side 해소): side=unknown 어깨 faultKey → 양측 어깨 모두 window eligible. 판정은 측정+tol 게이트. broad 확장 함정 방지 = **좁은 전용 매퍼** 신설 (line/torso/head_neck/grip 제외).
- 설계 지침 (locked): 엔진/라우터/criterion 무접촉 — `angle_vs_reference__{jk}` criterion(기존 tol 20°+slope 1.2, `_MEASURABLE_SEED_IDS` 등재) 재사용. 신규 튜닝 상수 0. split→hip 이중감점은 엔진 HIGH-5 기존 방어 재사용(재구현 금지). kip-up 특정 분기 금지(일반화).

Purpose: TestFlight 실증 갭(kip-up 어깨 Δ40.4°/31.0° 측정-표시되는데 감점 0) 해소의 측정·감점 축. SCORE-15("짚힌 결함은 기하 측정항으로 변환되어 감점, 보이는데 0감점 0") 배선.
Output: 좁은 매퍼(vision_veto.py 순수 함수) + seed merge(app.py) + 단위 테스트 2파일. Pod-free.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-vision-pointed-upper-body/25-RESEARCH.md
@.planning/phases/25-vision-pointed-upper-body/25-RESEARCH-IPSF.md
@backend/shared/python/sunity_shared/analysis/vision_veto.py
@backend/functions/pipeline/app.py

재사용 소스 (revert 된 커밋 — 그대로 복원 가능 부분 확인):
`git show f513587 -- backend/functions/pipeline/app.py` — `_emit_reference_relative` helper (JOINT_KEYS 검증 / NaN·0 skip / expects_extension cross-exclusion / md 방출 단일화) + wm 파싱(`abs(float(entry["delta_deg"]))`) + seed source 로그 패턴.
</context>

<interfaces>
사전 확인된 계약 (탐색 불필요):

- `vision_veto.FaultKey` (frozen dataclass): `part_scope / side / keypoint_set / fault_kind`. `FAULT_KEYPOINT_SETS = ("arm","shoulder","leg","hip","head_neck","grip","torso","line")`, `FAULT_SIDES` 에 "left"/"right"/"unknown".
- `ctx.supported_differences` (FaultContext): dict record list — `_faultKey`(FaultKey 인스턴스) / `_supportCount` / `_sourceIds` 메타 + body_part 등. app.py:3768 소비 패턴 참조.
- `skeleton.JOINT_KEYS` = 8관절: `left_elbow, right_elbow, left_shoulder, right_shoulder, left_hip, right_hip, left_knee, right_knee` (순서는 JOINT_ANGLES 정의 순 — 코드에서 import 해 사용, 하드코딩 금지).
- `quantification.windowMedianAngleDeltas`: dict `{"deltas": [{"joint": str, "delta_deg": float(SIGNED), ...}], "sourceFrameIndices": {...}}`. veto applied 경로(Mode1)에서만 존재.
- `_build_deduction_measured_deviations(angles, profile, assessments, dimension_scores, quantification, reference_dtw_match, reference_angles, split_deficit_deg)` — app.py:2043, 유일 호출부 app.py:3518 (vision_fault_context 분기 내).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 좁은 pointed-joint 매퍼 (vision_veto.py 순수 함수)</name>
  <files>backend/shared/python/sunity_shared/analysis/vision_veto.py, backend/tests/test_vision_pointed_mapper.py</files>
  <behavior>
    - keypoint_set="shoulder", side="left" → ("left_shoulder",)
    - keypoint_set="shoulder", side="unknown" → ("left_shoulder", "right_shoulder") — OD-1 양측 eligible
    - keypoint_set="arm", side="right" → ("right_elbow",) / "leg" → knee / "hip" → hip (명시 keypoint_set 4종 전부 매핑)
    - keypoint_set ∈ {"line","torso","head_neck","grip"} → 방출 0 (broad 확장 금지 — 리서치 함정 ⑥, 기존 fault_joints_from_differences 의 trunk/양다리 확장과 분리)
    - 복수 record union: JOINT_KEYS 순서 안정 + 중복 제거
    - `_faultKey` 부재/형상불량 record → skip (graceful, 방출 0)
    - 반환 관절은 전부 JOINT_KEYS 멤버 (spurious 키 방출 0)
  </behavior>
  <action>
    `vision_veto.py` 의 `fault_joints_from_differences`(표시용, line 721) 근처에 신규 순수 함수 `pointed_joints_from_supported_differences(supported) -> tuple[str, ...]` 를 추가한다 (OD-1 좁은 전용 매퍼 — 표시용 매퍼와 감점용 매퍼 분리, 리서치 §2·함정 ⑥).

    규칙:
    - 입력 = collect 산출 `supported_differences` record list. 각 record 의 `_faultKey`(FaultKey 인스턴스; dict 이면 `FaultKey.from_dict` 시도, 실패 시 skip) 만 본다 — body_part 자유텍스트 재파싱 금지 (canonical 키가 단일 owner).
    - keypoint_set → JOINT_KEYS base 매핑: `{"shoulder": "shoulder", "arm": "elbow", "leg": "knee", "hip": "hip"}`. 그 외(line/torso/head_neck/grip)는 방출 0 — line 하나가 양어깨 4관절을 "짚은" 것으로 만들면 vision 게이트가 무력화되므로 (기존 경로 = full-path median 유지).
    - side: "left"/"right" → 해당 측만, "unknown" → 양측 (OD-1 — 판정은 측정+tol 게이트가 하며, clean 쪽은 편차<20° 라 자연히 감점 0. fail-closed 버리기 금지 = motion-routing-generalize 원칙).
    - 출력: JOINT_KEYS 순서 안정 tuple, 중복 제거. JOINT_KEYS 외 문자열 방출 0.
    - 동작명/kip-up 특정 분기 0 (일반화 — locked).

    docstring 에 "감점용(pointed) 매퍼 — 표시용 fault_joints_from_differences 와 분리(리서치 함정 ⑥), broad(trunk/양다리) 확장 금지" 근거를 박제한다.

    테스트 `backend/tests/test_vision_pointed_mapper.py`: behavior 목록 전 케이스 + FaultKey enum 검증 정합(dict 입력 unknown enum → skip).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && PYTHONPATH=shared/python:. python3 -m pytest tests/test_vision_pointed_mapper.py -q</automated>
  </verify>
  <done>매퍼가 명시 keypoint_set 4종만 side 해소해 JOINT_KEYS 순서 안정 tuple 로 반환하고, line/torso/head_neck/grip 은 방출 0. 전 테스트 PASS.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: seed-stage 관절 단위 merge 배선 (_build_deduction_measured_deviations + 호출부)</name>
  <files>backend/functions/pipeline/app.py, backend/tests/test_deduction_seed_pointed_merge.py</files>
  <behavior>
    - pointed={"left_shoulder"} + wm deltas 에 left_shoulder Δ-40.4/right_shoulder Δ31.0 존재 + DTW fallback 가용 → md["angle_vs_reference__left_shoulder"] == 40.4 (window, abs), right_shoulder 는 DTW full-path median 값 (silent = fallback 유지)
    - pointed=None 또는 빈 tuple → 전 관절 DTW fallback (24-05/260626-e5k 기존 동작과 byte-동일 — 무회귀)
    - pointed 관절인데 wm 에 해당 joint 없음 → 그 관절은 DTW fallback (관절 단위 merge — 경로 either/or 아님)
    - profile.expects_extension(jk)=True 관절 → 양 경로 모두 미방출 (f513587 helper cross-exclusion 재사용, §3-2)
    - wm delta NaN/0/형상불량 entry → 그 관절 fallback 으로 강하 (honest skip)
    - seed_audit_out dict 전달 시 {"pointed": [...], "window_joints": [...], "fallback_joints": [...]} 기록, 미전달 시 부작용 0
  </behavior>
  <action>
    `_build_deduction_measured_deviations`(app.py:2043) 에 keyword-only 파라미터 2개 추가: `vision_pointed_joints=None`, `seed_audit_out=None` (둘 다 default None → 기존 호출/legacy 경로 무회귀).

    f513587 의 `_emit_reference_relative(jk, v)` helper 를 그대로 복원한다 (JOINT_KEYS 검증 / NaN·0·음수 skip / `profile.expects_extension` cross-exclusion / md 방출 단일화 — 이중감점 방어 재구현 금지, 리서치 함정 ⑦). 단 merge 로직은 f513587 의 경로 단위 either/or (wm 있으면 전 관절 window) 를 **관절 단위 선택**으로 교체:

    1. `wm_by_joint` = quantification.windowMedianAngleDeltas["deltas"] 파싱 → {joint: abs(float(delta_deg))} (f513587 파싱 패턴: SIGNED→magnitude, try/except (TypeError, ValueError) → skip).
    2. DTW fallback 편차 = 기존 per_joint_deviation 산출 (reference_dtw_match/reference_angles/angles 가용 시 1회 계산 — 기존 try/except honest-skip 유지).
    3. JOINT_KEYS 순회: `jk in vision_pointed_joints AND jk in wm_by_joint` → window 값 방출(source=window_median), 아니면 DTW 값 방출(source=dtw_median). pointed=None/빈 → 전 관절 DTW (기존과 동일).
    4. 관찰: `log.info("angle_vs_reference seed pointed=%d window=%d fallback=%d", ...)` + `seed_audit_out` 이 dict 이면 {"pointed", "window_joints", "fallback_joints"} 키로 기록 (25-04 eval harness 의 구조 게이트 입력 — production 호출부는 미전달이라 부작용 0. 스키마/Firestore 계약 변경 0 — record source 는 기존 geometry 표기 유지, window 집계 출처는 로그+eval audit 로만 관측).

    호출부(app.py:3518, vision_fault_context 분기): quantification 빌드 직후
    `vision_pointed_joints = pointed_joints_from_supported_differences(vision_fault_context.supported_differences)` (vision_veto 에서 import) 를 도출하고 kwarg 로 전달. legacy 분기(3542~)는 무접촉.

    테스트 `backend/tests/test_deduction_seed_pointed_merge.py`: behavior 전 케이스. 기존 phase24 seed 테스트 회귀 확인 포함 (`-k "deduction or seed"` 스코프로 로컬 전 통과).

    금지: 신규 튜닝 상수(tol/slope/window 정책 전부 기존 재사용 — window 정책은 features.window_median_angle_deltas 산출물을 소비만), 밴드, kip-up 특정 분기, motiondtw.py 접촉.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && PYTHONPATH=shared/python:. python3 -m pytest tests/test_deduction_seed_pointed_merge.py -q && PYTHONPATH=shared/python:. python3 -m pytest tests/ -k "deduction or vision or criteria" -q</automated>
  </verify>
  <done>pointed ∩ wm 관절만 window 값, 나머지 전부 DTW fallback. pointed=None 경로 byte-동일(무회귀). seed_audit_out 관측 동작. 기존 deduction/vision/criteria 테스트 회귀 0 (pre-existing fail 은 HEAD 과 diff 로 판단 — 25-RESEARCH Validation Architecture).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gemini API 응답 → supported_differences | 외부 모델 출력이 감점 대상 선택(pointed set)에 진입 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Tampering | pointed 매퍼 입력(supported_differences) | mitigate | `_faultKey` canonical enum 만 소비(`FaultKey.from_dict` enum 거부), body_part 자유텍스트 재파싱 금지, JOINT_KEYS 외 방출 0 |
| T-25-02 | Elevation (감점 과확장) | pointed 매퍼 broad 확장 | mitigate | line/torso/head_neck/grip 제외 하드코딩 + 단위 테스트, 감점은 tol 20° dead-zone 뒤에서만 |
| T-25-03 | DoS (NaN/형상불량) | wm delta 파싱 | mitigate | f513587 try/except + abs() + NaN skip 패턴 유지 |
| T-25-SC | Tampering | 패키지 설치 | accept | 신규 패키지 0 (감사 대상 없음 — 25-RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- `pytest tests/test_vision_pointed_mapper.py tests/test_deduction_seed_pointed_merge.py -q` PASS
- `pytest tests/ -k "deduction or vision or criteria" -q` — 신규 FAIL 0 (pre-existing 54 fail 은 HEAD 대비 diff IDENTICAL 로 판정)
- grep 게이트: `grep -v '^\s*#' backend/functions/pipeline/app.py | grep -c "SEVERITY_CAP\|apply_downward_cap"` == 0 (밴드 재도입 0)
</verification>

<success_criteria>
- Gemini-짚인 관절만 window seed, silent 관절 full-path median — 관절 단위 merge 가 단위 테스트로 증명됨
- legacy/mode3/pointed-없음 경로 무회귀 (기존 테스트 + byte-동일 케이스)
- 신규 튜닝 상수 0, 스키마 변경 0, 엔진/라우터/criterion 무접촉
</success_criteria>

<output>
Create `.planning/phases/25-vision-pointed-upper-body/25-01-SUMMARY.md` when done
</output>
