---
phase: 25-vision-pointed-upper-body
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py
  - backend/tests/test_gemini_vision_scorer.py
autonomous: true
requirements: [SCORE-15]

must_haves:
  truths:
    - "어깨 언급이 side/fault_kind fragment 로 support K=2 미달 drop 되던 것이 keypoint_set 단위 fold 로 살아남는다 (차단 1 해소)"
    - "upper_body scope 호출에서 관찰된 상체 편차가 primary_fault 서사에만 남지 않고 differences[] 항목(좌/우 명시)으로 구조화되도록 프롬프트가 강제한다"
    - "프롬프트/집계 변경 후 기존 whole_fanout rich 캐시는 전량 miss (stale-hit 재발 0 — kip-up 캐시충돌 이력)"
    - "프롬프트에 동작명/기대답 하드코딩 0 (D-06 curve-fit 밴 — generic 유지)"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py"
      provides: "keypoint_set fold 집계 + AGGREGATION_VERSION 캐시 marker + PROMPT_VERSION v10.0 + upper_body 구조화 지시"
      contains: "AGGREGATION_VERSION"
    - path: "backend/tests/test_gemini_vision_scorer.py"
      provides: "fold/캐시키/프롬프트 회귀 테스트 확장"
  key_links:
    - from: "_filter_supported_differences"
      to: "FaultKey.keypoint_set"
      via: "그룹 키 = keypoint_set 단독 (side/fault_kind fold)"
      pattern: "keypoint_set"
    - from: "VisionVetoCache.build_key"
      to: "AGGREGATION_VERSION"
      via: "키 component 로 folding — 집계 변경 시 자동 cache-miss"
      pattern: "AGGREGATION_VERSION"
---

<objective>
상체 faultKey 미산출의 차단 1(짚기 — 25-RESEARCH §1) 해소: Gemini 는 kip-up fault 의 상체 결함을 *보지만*(primaryFault "상체(어깨/목) 정렬 흐트러짐"), (a) side/fault_kind fragment 로 support K=2 미달 drop, (b) differences[] 미방출(서사에만 등장) 두 경우 모두에 처방한다 — 리서치 A1: artifact 로 두 경우 구분 불가하므로 둘 다(집계 fold + 프롬프트 보강) 적용.

설계 지침 (locked) 반영: 프롬프트 레버 = per-move 특정성이 아닌 **scope-집중 특정성**(flash-beats-pro 교훈 — "split 콕 집으니 reliable" 의 상체 대응 = upper_body scope 지시 강화). 동작명/기대답 하드코딩은 금지 유지(D-06 curve-fit 밴, 리서치 A2). 프롬프트/집계 변경 시 **캐시 버전 marker bump 필수** (kip-up stale-hit FP 이력 — 리서치 함정 ①②).

Purpose: 25-01 의 측정·감점 배선은 짚기(faultKey)가 살아남아야 발화한다 — 본 플랜이 짚기 커버리지를 상체까지 확보. SCORE-15 의 "Gemini 가 짚은 모든 결함" 입력측.
Output: gemini_vision_scorer.py 집계/프롬프트/캐시 marker + 테스트 확장. Pod-free.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-vision-pointed-upper-body/25-RESEARCH.md
@backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py
@backend/tests/test_gemini_vision_scorer.py
</context>

<interfaces>
사전 확인된 계약:

- `_filter_supported_differences(per_call_differences, *, part_scope_hint, min_support_k=VETO_SUPPORT_K)` — line 1227. 현행 그룹 키 = `tuple(sorted(fk.to_dict().items()))` (FaultKey 4필드 전체). severity 'none' 필터 / 대표 = 최고 severity rank → dev / `_supportCount`/`_faultKey`/`_sourceIds` 메타 / severity 내림차순 정렬.
- `VisionVetoCache.build_key` — line 398. 키 component 에 PROMPT_VERSION/SCHEMA_VERSION/VISION_VETO_SAMPLES 를 `globals()` 경유로 folding (monkeypatch 반영 설계).
- `PROMPT_VERSION = "v9.0"` (line 73), `SCHEMA_VERSION = "v7.0"` (line 74). rich 캐시(`store_rich`)는 support-게이트 **통과 후** supported_differences 를 저장 — 집계만 바꿔도 stale 이 살아남으므로 marker bump 필수 (리서치 함정 ②).
- `_call_gemini_comparison(..., part_scope)` — line 1457. part_scope 제공 시 `_PART_SCOPE_LABEL` 로 부위-집중 suffix (1480~1485). fanout = 3 scope(upper_body/lower_body/line) × 1 call (`_run_part_frame_fanout`, planned=3).
- 자원 bound: `MAX_VETO_WALL_S=120s` / `MAX_VETO_CALLS=9`, planned=3. **scope/call 수 불변** (늘리면 resource_limited fail-closed 위험 — 리서치 함정 ④).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: support 집계 fragment fold + AGGREGATION_VERSION 캐시 marker</name>
  <files>backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py, backend/tests/test_gemini_vision_scorer.py</files>
  <behavior>
    - 3 call 이 각각 "왼쪽 어깨 굽음"(side=left, kind=pole_gap_or_bent) / "어깨 정렬 흐트러짐"(side=unknown, kind=extension_or_alignment) 을 내면 — 현행은 각 support 1 로 drop, fold 후 keypoint_set=shoulder support 2 로 통과
    - 대표 record = 그룹 내 최고 severity rank → 최고 dev (기존 규칙 불변)
    - 대표 `_faultKey.side` = 그룹 내 명시(left/right) side 가 유일하면 그 side, 혼재·부재면 unknown (25-01 매퍼가 unknown → 양측 해소)
    - severity='none' 필터 / `_supportCount`/`_sourceIds` 메타 / 정렬 — 전부 기존과 동일
    - 서로 다른 keypoint_set (예: shoulder vs leg) 은 fold 되지 않는다 (그룹 분리 유지)
    - build_key 산출 키에 AGGREGATION_VERSION 문자열이 포함되고, marker 변경 시 다른 키 (기존 키 공간 재사용 0)
  </behavior>
  <action>
    `_filter_supported_differences` 의 그룹 키를 FaultKey 4필드 전체 → **`keypoint_set` 단독**으로 변경한다 (리서치 §1 처방 (a): side/fault_kind fragment fold — part_scope 는 hint 균일이라 원래 무변별). 대표 선택 규칙(최고 severity rank → dev)과 메타 부착은 불변. 대표의 `_faultKey` 는 대표 difference 의 `fault_key_from_difference` 결과에 side 만 그룹-해소 값으로 교체(`dataclasses.replace`): 그룹 내 명시 side 집합이 {left} 또는 {right} 단일이면 그 side, 그 외(혼재/전부 unknown)는 "unknown".

    캐시: 모듈 상수 `AGGREGATION_VERSION = "agg2"` 신설(집계 알고리즘 버전 marker — 튜닝 상수 아님) 후 `VisionVetoCache.build_key` 의 `":".join` component 에 `globals()["AGGREGATION_VERSION"]` 경유로 추가 (PROMPT_VERSION 과 동일한 monkeypatch-반영 패턴). docstring 에 "rich 캐시는 집계 후 supported_differences 를 저장하므로 집계 변경 = marker bump 필수 (kip-up whole/whole_fanout stale-hit FP 이력, 90d038f)" 근거 박제. 기존 키 공간 재사용 절대 금지 (리서치 함정 ①).

    테스트 확장 (test_gemini_vision_scorer.py): behavior 전 케이스 + "fold 로 union 부풀림 없음" 확인(같은 call 안에서 같은 keypoint_set 2건이어도 support 는 call-교차 카운트 로직 그대로 — 기존 diff_id 순회 의미 보존) + build_key 에 marker 포함/변경 테스트.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && PYTHONPATH=shared/python:. python3 -m pytest tests/test_gemini_vision_scorer.py -q</automated>
  </verify>
  <done>어깨 fragment 케이스가 K=2 통과. keypoint_set 분리 유지. build_key 가 AGGREGATION_VERSION 포함 — 기존 캐시 전량 miss 보장. 기존 scorer 테스트 회귀 0.</done>
</task>

<task type="auto">
  <name>Task 2: upper_body 짚기 프롬프트 구조화 강제 + PROMPT_VERSION bump</name>
  <files>backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py, backend/tests/test_gemini_vision_scorer.py</files>
  <action>
    `_call_gemini_comparison` 의 part_scope suffix 블록(1480~1485)에 구조화 강제 지시를 추가한다 (리서치 §1 처방 (b)):

    - 기존 suffix("특히 [{label}] 부위에 집중...") 뒤에: "이 부위에서 관찰한 각 편차는 반드시 differences[] 배열의 개별 항목으로 구조화하고, body_part 에는 좌/우를 명시하세요(예: '왼쪽 어깨'). primary_fault 서사에만 언급하고 differences 에서 누락하는 것은 금지입니다. 단 1·2번 규칙(정타/사소차/촬영조건은 결함 아님)은 그대로입니다."
    - generic 유지: 동작명·기대답·특정 관절 정답 하드코딩 0 (D-06 curve-fit 밴 — 리서치 A2. per-move 특정성은 scope-집중 + 구조화 강제로 달성, 동작명 주입 아님). 전 scope(upper/lower/line) 공통 적용 — 일반화 원칙 정합.
    - `PROMPT_VERSION` = "v9.0" → "v10.0" bump + 주석에 변경 사유(v10.0: part_scope 구조화 강제 — 서사-only 상체 결함의 differences[] 방출) 기록. 프롬프트 bump = 전 동작 회귀 표면 — 회귀 게이트는 25-04 의 6페어 full sweep (리서치 함정 ⑤) 임을 주석 박제.
    - fanout scope 수(3)/call 수/wall budget 불변 (리서치 함정 ④ — 자원 bound fail-closed).

    테스트: (1) PROMPT_VERSION != "v9.0" (bump 확인), (2) part_scope 제공 시 프롬프트에 "differences" 구조화 지시 포함, (3) 동작명 부재 grep-테스트 — 프롬프트 빌드 산출물에 REGISTERED_MOTIONS 동작명(kip-up/power-spin/peter-pan/elbow-twist/pdshape/climb 계열 문자열) 미포함 assert, (4) `_SCORE_PATTERN` 점수누출 폐기 로직 무접촉 확인(기존 테스트 통과로 갈음).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && PYTHONPATH=shared/python:. python3 -m pytest tests/test_gemini_vision_scorer.py -q && PYTHONPATH=shared/python:. python3 -m pytest tests/ -k "vision or gemini" -q</automated>
  </verify>
  <done>PROMPT_VERSION v10.0 + 구조화 지시 포함 + 동작명 0. 캐시 키가 자동 무효화(PROMPT_VERSION 이 키 component). vision/gemini 테스트 신규 FAIL 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gemini API 응답 → 집계/verdict | 외부 모델 JSON 이 support 게이트 입력 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-04 | Spoofing (환각 결함) | fold 후 support 게이트 | mitigate | K=2 게이트 유지(단발 drop) + severity none 필터 + tol 20° dead-zone 이 최종 심판 — fold 는 fragment 만 접합, 게이트 자체 완화 아님 |
| T-25-05 | Tampering (stale 캐시) | VisionVetoCache | mitigate | AGGREGATION_VERSION + PROMPT_VERSION 키 folding — 기존 키 공간 재사용 0 (90d038f 이력 재발 방지) |
| T-25-06 | Information Disclosure (점수 누출) | 프롬프트 변경 | mitigate | `_SCORE_PATTERN` 누출 샘플 폐기 유지, 프롬프트에 점수 요청 0 |
| T-25-SC | Tampering | 패키지 설치 | accept | 신규 패키지 0 |
</threat_model>

<verification>
- `pytest tests/test_gemini_vision_scorer.py -q` PASS (fold + marker + prompt 신규 테스트 포함)
- 프롬프트 동작명 grep 게이트: 테스트로 assert (grep -c 직접 사용 시 주석 제외 필터 적용)
- 짚기 커버리지의 실효(상체 faultKey 실제 산출)는 25-04 Pod sweep 이 최종 판정 — 본 플랜은 결정적 pure 함수/프롬프트 계층까지
</verification>

<success_criteria>
- 어깨 fragment 시나리오가 support 게이트 통과 (단위 테스트 증명)
- 캐시 stale-hit 구조적 불가능 (marker 2중: AGGREGATION_VERSION + PROMPT_VERSION)
- generic 유지 — 동작명/기대답/신규 튜닝 상수 0, scope/call 수 불변
</success_criteria>

<output>
Create `.planning/phases/25-vision-pointed-upper-body/25-02-SUMMARY.md` when done
</output>
