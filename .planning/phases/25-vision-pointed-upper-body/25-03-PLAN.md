---
phase: 25-vision-pointed-upper-body
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/shared/python/sunity_shared/analysis/fault_zoom.py
  - backend/tests/test_fault_zoom_relaxed_crop.py
autonomous: true
requirements: [TRUST-08]

must_haves:
  truths:
    - "reference(정은지) 측 keypoint 가 저신뢰여도 좌표가 유한하면 카드별로 다른 부위-중심 완화 crop 이 나온다 (카드마다 동일 전신 사진 반복 = '분석 안 한 것처럼 보임' 해소)"
    - "완화 crop 측은 앵커 동그라미를 생략한다 (좌표 불확실 — 엉뚱한 부위에 확정 표식 금지, 260702-sic belle 요구 3 정신 유지)"
    - "고신뢰 측 앵커 동그라미는 crop 중심이 아닌 결함 관절 좌표에 정확히 놓인다 (grouped bbox crop 에서도)"
    - "좌표 자체가 결측(NaN/부재)인 측은 기존 전신 폴백 유지 (완화 crop 은 저신뢰-유한 좌표에만)"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/fault_zoom.py"
      provides: "_side_crop 3단 강하 (valid → 완화 crop → 전신 폴백) + 앵커 관절-좌표 고정"
      contains: "relaxed"
    - path: "backend/tests/test_fault_zoom_relaxed_crop.py"
      provides: "합성 프레임/키포인트 단위 테스트 (3단 강하 + 카드별 차별화 + 앵커 위치)"
      min_lines: 60
  key_links:
    - from: "_side_crop"
      to: "_full_frame_fit"
      via: "완화 crop 불가(좌표 결측) 시에만 전신 폴백"
      pattern: "_full_frame_fit"
    - from: "_mark"
      to: "결함 관절 좌표"
      via: "circle 중심 = 관절 좌표 (crop 중심 아님)"
      pattern: "circle"
---

<objective>
Phase 25 동반 스코프 (ROADMAP locked — belle 2026-07-04 실기기 이관분): 확대 카드 정밀도. 현행 `fault_zoom.py` 는 저신뢰(<`_KP_CONF_MIN`=0.5) keypoint 측을 전신 폴백 처리(quick-260702-sic)하는데, reference(정은지) 측이 저신뢰인 kip-up 류에서 **카드마다 동일 전신 사진이 반복**돼 "분석 안 한 것처럼 보임". 해법 = 부위 bbox **완화 crop** + 카드별 차별화 + 앵커 정밀화. vision 확정 프레임(sourceFrameIndices median override)은 quick-260702-sic 에서 이미 배선 — 본 플랜은 fault_zoom.py 내부 crop/앵커 계층만.

Purpose: 감점 근거 시각화의 신뢰(TRUST-08 정신 — 근거 가시화). 25-01/02 로 상체 감점이 생기면 상체 카드가 늘어나므로 카드 품질 갭이 더 노출된다.
Output: fault_zoom.py 3단 crop 강하 + 앵커 정밀화 + 합성 프레임 단위 테스트. Pod-free (실 PNG 재생성·육안 확인은 25-04 sweep 재분석 후).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-vision-pointed-upper-body/25-RESEARCH.md
@backend/shared/python/sunity_shared/analysis/fault_zoom.py

현행 구조 (사전 확인): `_KP_CONF_MIN=0.5` 게이트(`_valid_kp_xy`) / `_BBOX_MARGIN=1.8` grouped bbox crop(`_side_crop`) / 전신 폴백 `_full_frame_fit`(260702-sic belle 요구 3 — "엉뚱한 부위 확대보다 전신") / advisory 티어(`select_advisory_joints`, 260704-fz4) / 앵커 `_mark`(circle=False 가 전신 폴백 측).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _side_crop 3단 강하 — 완화(relaxed) crop 신설</name>
  <files>backend/shared/python/sunity_shared/analysis/fault_zoom.py, backend/tests/test_fault_zoom_relaxed_crop.py</files>
  <behavior>
    - 관절 confidence >= _KP_CONF_MIN → 기존 valid crop (무회귀)
    - 관절 confidence < _KP_CONF_MIN 이지만 좌표 finite → 완화 crop: 그 좌표 중심, 변 = 기존 grouped 공식(max(bbox)*_BBOX_MARGIN, floor=_CROP_FRAC 줌 수준) × 신규 display 전용 상수 `_RELAXED_MARGIN = 2.0` — 부위가 crop 안에 남도록 넓게. **display 전용 신규 상수 1개 허용**: 채점 경로 무접촉이므로 calibration gate([[calibration-source-hard-gate]]) 대상 아님, "display 전용, 채점 무접촉" 주석 필수
    - 서로 다른 관절 그룹 2카드 → 완화 crop 중심이 서로 다름 (카드별 차별화)
    - 좌표 NaN/결측 → 기존 전신 폴백 (relaxed 아님)
    - 반환에 crop 종류(valid/relaxed/full) 구분 신호 포함 → _mark 가 앵커 표시 여부 결정
  </behavior>
  <action>
    `_side_crop` 을 3단 강하로 재구성한다: (1) `_valid_kp_xy` 통과 관절 → 기존 로직 그대로 (단일/grouped bbox). (2) valid 0개이지만 **좌표 finite 인 저신뢰 관절이 있으면** 신규 완화(relaxed) crop — 저신뢰 좌표 중심, 변 = 기존 `_BBOX_MARGIN`/`_CROP_FRAC` 공식 × `_RELAXED_MARGIN = 2.0`(신규, **display 전용 상수 — "display 전용, 채점 무접촉" 주석 필수**. 채점 경로 신규 튜닝 상수 금지는 그대로이며 본 상수는 채점 미진입이라 대상 아님). (3) 좌표 자체 결측 → 기존 `_full_frame_fit` 전신 폴백.

    반환 시그니처를 (이미지, 전신폴백 여부) → (이미지, crop_kind ∈ {"valid","relaxed","full"}) 로 확장하고 호출부(`build_fault_zoom_comparisons` 내) 정합 갱신. "양측 다 full" skip 규칙은 유지하되, relaxed 는 skip 대상이 아니다(부위 정보가 있으므로 카드 생성).

    docstring 에 근거 박제: "reference 저신뢰 전신 폴백이 카드마다 동일 전신 반복 → 부위-중심 완화 crop 으로 카드별 차별화 (Phase 25 동반 스코프, belle 2026-07-04 실기기). 오인 방지는 앵커 생략으로 유지(260702-sic 요구 3)."

    테스트 `backend/tests/test_fault_zoom_relaxed_crop.py`: numpy 합성 프레임 + 합성 keypointReport 로 behavior 전 케이스 (PIL 이미지 크기/중심 픽셀 좌표로 crop 중심 차별화 검증).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && PYTHONPATH=shared/python:. python3 -m pytest tests/test_fault_zoom_relaxed_crop.py -q</automated>
  </verify>
  <done>저신뢰-유한 좌표 측이 카드별로 다른 부위-중심 crop 을 얻고, 결측 측만 전신 폴백. 기존 valid 경로 무회귀.</done>
</task>

<task type="auto">
  <name>Task 2: 앵커 정밀화 — circle 을 관절 좌표에 고정 + relaxed 측 circle 생략</name>
  <files>backend/shared/python/sunity_shared/analysis/fault_zoom.py, backend/tests/test_fault_zoom_relaxed_crop.py</files>
  <action>
    `_mark` / `_crop_zoom` / `_side_crop` 의 앵커 배선 정리 (ROADMAP "동그라미 앵커 정밀화"):

    - valid crop (단일·grouped 모두): circle 중심 = **결함 관절의 crop-내 상대 좌표** (crop 중심 아님). grouped bbox 카드에서 관절이 중심을 벗어나도 circle 이 관절을 정확히 가리키도록, crop 시 관절→crop 상대좌표를 함께 산출해 `_mark` 에 전달한다. grouped(2관절+) 카드는 대표 관절(deficit 최대) 1개에 circle.
    - relaxed crop: circle 생략 (좌표 불확실 — 전신 폴백과 동일한 오인-방지 규칙, deficit 배지는 유지).
    - full(전신) 폴백: 기존 그대로 (circle=False).

    테스트 확장: (1) grouped crop 에서 circle 픽셀이 관절 상대좌표 근방에 그려짐(합성 프레임 색상 샘플링 또는 _mark 호출 인자 검증 — 호출 인자 검증이 단순하면 그쪽 선택), (2) relaxed/full 측 circle 미호출.

    앱/계약 무접촉 (PNG 산출물 내부 구도만 변경 — S3 키/Firestore 필드/faultZoom 계약 불변). 채점 경로 diff 0 을 grep 으로 확인: fault_zoom.py 는 deduction/criteria 모듈 import 부재 유지.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && PYTHONPATH=shared/python:. python3 -m pytest tests/test_fault_zoom_relaxed_crop.py tests/ -k "fault_zoom or zoom" -q</automated>
  </verify>
  <done>circle = 관절 좌표 고정(valid), relaxed/full = 생략. 기존 zoom 테스트 회귀 0. 계약/스키마 변경 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keypointReport(모델 산출) → crop 좌표 | 저신뢰/NaN 좌표가 이미지 연산에 진입 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-07 | DoS (NaN/범위밖 좌표) | relaxed crop 좌표 | mitigate | finite 검사 후에만 relaxed, crop 좌표 clamp(기존 _crop_zoom 경계 처리 재사용) |
| T-25-08 | Repudiation (오인 유발 표식) | relaxed 측 앵커 | mitigate | circle 생략 규칙 — 불확실 좌표에 확정 표식 금지 |
| T-25-SC | Tampering | 패키지 설치 | accept | 신규 패키지 0 |
</threat_model>

<verification>
- `pytest tests/test_fault_zoom_relaxed_crop.py -q` PASS + 기존 zoom 테스트 회귀 0
- 채점 무접촉: `git diff --stat` 이 fault_zoom.py + 테스트 파일만 (deduction_engine/dimensions/kismam 접촉 0)
- 실 PNG 육안 확인은 25-04 sweep 재분석 산출물로 belle 확인 (본 플랜 SUMMARY 에 follow-up 명시)
</verification>

<success_criteria>
- reference 저신뢰 케이스에서 카드별 차별화된 부위 crop (단위 테스트 증명)
- 앵커 circle 이 관절 좌표에 정확히 위치, 불확실 측은 생략
- 채점/계약/스키마 diff 0
</success_criteria>

<output>
Create `.planning/phases/25-vision-pointed-upper-body/25-03-SUMMARY.md` when done
</output>
