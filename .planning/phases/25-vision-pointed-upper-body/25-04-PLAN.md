---
phase: 25-vision-pointed-upper-body
plan: 04
type: execute
wave: 2
depends_on: [25-01, 25-02, 25-03]
files_modified:
  - backend/evals/phase25/run_sweep.py
  - backend/evals/phase25/assert_gates.py
  - backend/evals/phase25/README.md
  - backend/tests/test_phase25_eval_gates.py
  - .planning/phases/25-vision-pointed-upper-body/25-SWEEP-EVIDENCE.md
  - backend/evals/phase25/baseline/
autonomous: false
requirements: [SCORE-09, SCORE-15]

must_haves:
  truths:
    - "6페어 serial sweep 에서 success 6/6 == 100 (위양성 0 — 260702-o0c FAIL 재발 금지)"
    - "kip-up fault 는 88 미만으로 하락하고, window-sourced 감점은 vision-짚인 관절에서만 발생한다 (구조 assert — 특정 점수 assert 금지)"
    - "success 멤버의 Gemini collect status(짚은 관절 목록)가 처음으로 관측·기록된다 — 짚기-FP율은 관측 지표(게이트 아님, OD-2)"
    - "5 fault 멤버 score 가 phase24 baseline 이하 (방향 비교 무퇴행) + climb not_pole 유지"
    - "cold(캐시 전량 miss, 36 pro call) → warm 재실행 결정론 (동일 verdict/breakdown)"
  artifacts:
    - path: "backend/evals/phase25/run_sweep.py"
      provides: "phase24 harness 복제 + collectObservation/seedObservation tee 캡처"
      contains: "seed_audit_out"
    - path: "backend/evals/phase25/assert_gates.py"
      provides: "phase24 게이트 상속 + 신규 4 게이트 (success_100 / pointed_only_window / kipup_upper_structure / fault_no_regression)"
      contains: "check_success_100"
    - path: "backend/tests/test_phase25_eval_gates.py"
      provides: "신규 게이트 pod-free 단위 테스트 (합성 fixture)"
    - path: ".planning/phases/25-vision-pointed-upper-body/25-SWEEP-EVIDENCE.md"
      provides: "sweep 게이트 결과 + 짚기-FP 관측 + zoom 카드 follow-up 박제"
  key_links:
    - from: "run_sweep.py"
      to: "pipeline._build_deduction_measured_deviations"
      via: "tee wrapper 가 seed_audit_out dict 전달 → window/fallback 관절 캡처"
      pattern: "seed_audit_out"
    - from: "assert_gates.check_pointed_only_window"
      to: "sweep report seedObservation"
      via: "window_joints ⊆ pointed_joints 전 멤버 assert"
      pattern: "pointed"
---

<objective>
Phase 25 최종 게이트: 6페어 serial sweep (Pod, RTMW GPU + Gemini) — "fault 변별 유지·개선 AND success 6/6==100 AND 'vision 짚은 관절만 window 감점' 구조 assert AND 결정론 cold-rerun". OD-2 반영: success 멤버의 collect status 를 score-free 로 캡처해 "clean 영상 짚기 빈도"(짚기-FP율)를 처음으로 관측한다 — 게이트는 감점 결과가 지고, 짚기-FP율은 관측 지표로만 기록 (기존 na_audit 는 collect 산출을 버리므로 harness 가 tee 로 채움, 리서치 §3 관측 공백).

OD-3 (환경 정정): pod 는 살아있다 — **lsx9kedqsdk1e3, HEAD 0d11835+, 서버 가동 중. 재생성 불필요** (25-RESEARCH Environment Availability 의 "재생성 필요"는 stale). 필요한 것 = 로컬 커밋 push → pod git pull 동기화. Gemini 크레딧 잔량 확인은 sweep 전 belle 체크 항목 (cold sweep = 12 멤버 × 3 call = 36 pro call + 멤버당 영상 2 업로드, [[gemini-credits-depleted-2026-06-20]]).

금지 (locked): 특정 점수 assert (kip-up == X 짜맞추기), 사람 점수 라벨, 밴드, 병렬 실행 ([[pipeline-not-concurrency-safe-eval-serial]] — SERIAL 필수).

Purpose: SCORE-09(일반화 게이트) + SCORE-15(보이는데 0감점 0) 의 실측 판정. Phase 25 전체(25-01/02/03)의 유일한 실영상 회귀 게이트.
Output: evals/phase25 harness + 게이트 + Pod sweep evidence.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-vision-pointed-upper-body/25-RESEARCH.md
@backend/evals/phase24/run_sweep.py
@backend/evals/phase24/assert_gates.py
@backend/evals/phase24/baseline/phase24_sweep_report.json

Pod 실행 env 커맨드 = phase24 run_sweep.py 헤더 그대로 (aws_env.sh + RTMW/YOLOX onnx + GEMINI_API_KEY SSM). 로컬 SSM 조회는 `--profile sunity-motion` 필수 ([[gemini-key-local-ssm-profile]]).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: evals/phase25 harness + 게이트 (pod-free 코드)</name>
  <files>backend/evals/phase25/run_sweep.py, backend/evals/phase25/assert_gates.py, backend/evals/phase25/README.md, backend/tests/test_phase25_eval_gates.py</files>
  <behavior>
    - check_success_100: success 멤버 overallScore != 100 이 하나라도 있으면 FAIL (phase24 check_generalization 의 within-tolerance 허용보다 강함)
    - check_pointed_only_window: 어느 멤버든 seedObservation.window_joints ⊄ pointed_joints 면 FAIL
    - check_kipup_upper_structure: kip-up fault 멤버 — (a) overallScore < 88 (baseline 방향 비교, 고정 타깃 아님), (b) deductionBreakdown 에 pointed 상체 관절(shoulder/elbow)의 angle_vs_reference record ≥ 1, (c) 그 record 관절 ∈ pointed set. 셋 다 충족해야 PASS
    - check_fault_no_regression: 5 fault 멤버 각각 overallScore <= phase24 baseline(`evals/phase24/baseline/phase24_sweep_report.json` 의 해당 멤버 score) — kip-up <88 과 동일한 방향 비교 패턴(무퇴행, curve-fit 아님) + climb not_pole 유지. baseline artifact 부재 시 SKIPPED
    - 세 게이트 모두 artifact(sweep report) 부재 시 SKIPPED (실패 아님 — phase24 HIGH-4 패턴)
    - 짚기-FP 관측: success 멤버별 collectObservation(짚은 keypoint_set/관절 목록) 이 report 에 존재하면 요약 출력 (관측만, 게이트 아님 — OD-2)
  </behavior>
  <action>
    `backend/evals/phase24/` 를 `backend/evals/phase25/` 로 복제-확장한다 (리서치 §4 하네스 재사용 — phase18 6페어 both-member Mode1, serial in-process `_process`, pdshape cold-rerun, UID=phase25eval 로 변경, eval_keys.json 재사용 경로 확인).

    run_sweep.py 확장 (eval-only, production 코드 무접촉 — probe_kip_up_gemini.py 의 tee 패턴. **wrapper 는 read-only tee — collect 인자 변경 금지, 특히 at_seconds=None 불변** (리서치 함정 ③)):
    1. `_load_pipeline()` 직후 `pipeline._collect_vision_fault_context` 를 wrapper 로 감싸 멤버별 collect 산출(status, supported_differences 의 `_faultKey` dict 목록, pointed_joints_from_supported_differences 결과)을 캡처 — **success 멤버 포함 전 12 멤버** (OD-2 짚기-FP 관측. na_audit 가 버리던 것을 harness 가 채움 — production 스키마 무접촉, 리서치 Open Q2 의 eval-전용 권장).
    2. `pipeline._build_deduction_measured_deviations` 를 wrapper 로 감싸 `seed_audit_out` dict 를 주입(25-01 이 추가한 관측 파라미터)해 멤버별 {pointed, window_joints, fallback_joints} 캡처.
    3. sweep report JSON 에 멤버별 `collectObservation` + `seedObservation` 섹션 추가 + 짚기-FP율 요약(success 멤버 중 상체 keypoint_set 짚임 빈도).

    assert_gates.py: phase24 의 7 게이트(traceability/monotonicity/determinism/criterion-selection/generalization/clean_residual/sensitivity)를 import 재사용 또는 복제 유지하고, behavior 의 신규 4 게이트(check_success_100 / check_pointed_only_window / check_kipup_upper_structure / check_fault_no_regression)를 추가한다. 특정 점수 assert 금지 원칙을 모듈 docstring 에 박제 — kip-up "< 88" 은 baseline(88) 대비 방향 비교(fault 변별 개선 gate, ROADMAP locked)이지 고정 타깃이 아님을 명시. fault 변별 무퇴행 = check_fault_no_regression(fault 멤버별 score <= phase24 baseline 방향 비교) AND climb not_pole 유지 — phase24 generalization 구조 gate(fault < success) 위에 강화.

    README.md: 실행 커맨드(phase24 헤더 승계) + cold/warm 구분 + 크레딧 요건(36 pro call) 기록.

    테스트 `backend/tests/test_phase25_eval_gates.py`: 신규 4 게이트를 합성 report fixture 로 pod-free 검증 (phase24 의 21-test 패턴).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && PYTHONPATH=shared/python:. python3 -m pytest tests/test_phase25_eval_gates.py -q && PYTHONPATH=shared/python:. python3 evals/phase25/assert_gates.py; test $? -le 1</automated>
  </verify>
  <done>harness/게이트 코드 완성, 신규 게이트 단위 테스트 PASS, artifact 부재 시 SKIPPED 로 graceful (exit 코드 계약 = phase24 동일).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: belle 체크 — Gemini 크레딧 잔량 (sweep 선행 조건)</name>
  <what-built>25-01/02/03 코드 + phase25 eval harness 가 준비됨. cold sweep 은 캐시 전량 miss(PROMPT_VERSION v10.0 + AGGREGATION_VERSION bump)라 12 멤버 × 3 call = 36 Gemini pro call + 멤버당 영상 2 업로드가 발생한다.</what-built>
  <action>belle 에게 Gemini 크레딧 잔량 확인을 요청하고 blocking 대기한다 (OD-3: sweep 전 belle 체크 항목 — 자동화 불가, Google AI Studio 결제 대시보드는 belle 계정 전용). Claude 가 대신 할 수 있는 사전 점검(pod /health, 로컬 게이트 GREEN)은 요청 전에 완료해 둔다.</action>
  <how-to-verify>
    1. Google AI Studio (sunity3412@gmail.com 계정) 크레딧/결제 잔량 확인 — 2026-06-20 sweep 중 429 고갈 이력 ([[gemini-credits-depleted-2026-06-20]]).
    2. 36 pro call + 영상 업로드 분량이 충분한지 확인, 부족하면 충전.
  </how-to-verify>
  <resume-signal>"크레딧 OK" 입력 시 Task 3(Pod sweep) 진행. 부족하면 충전 후 알려주세요.</resume-signal>
  <verify>belle 응답 "크레딧 OK" 수신</verify>
  <done>Gemini 크레딧이 36 pro call + 영상 업로드 분량을 감당함이 belle 에 의해 확인됨</done>
</task>

<task type="auto">
  <name>Task 3: Pod 동기화 + 6페어 serial sweep (cold+warm) + 게이트 + evidence 박제</name>
  <files>.planning/phases/25-vision-pointed-upper-body/25-SWEEP-EVIDENCE.md, backend/evals/phase25/baseline/</files>
  <action>
    Pod 운영은 Claude 가 실행한다 ([[pod-ops-claude-runs]]). OD-3: pod lsx9kedqsdk1e3 살아있음 — 재생성/부트스트랩 불필요.

    1. 로컬 25-01~04 커밋을 GitHub push ([[gsd-pod-work-push-first]]).
    2. pod SSH → `/workspace/SunityMotion` git pull → HEAD 가 25-04 커밋인지 확인. `/health` 로 서버 가동 확인 (sweep 은 in-process `_process` 라 서버 재시작 불필요하나, 진행 중 분석과 겹치지 않게 확인 — SERIAL 원칙).
    3. cold sweep: phase25 README 커맨드로 `run_sweep.py` 실행 (반드시 순차 — [[pipeline-not-concurrency-safe-eval-serial]]). 429/resource_limited 발생 시 중단하고 belle 에 보고 (fail-closed).
    4. warm 재실행: 동일 커맨드 재실행 — 캐시 hit 로 Gemini 0 call, verdict/breakdown 동일성 = 결정론 게이트 (리서치 §4).
    5. `assert_gates.py` 실행 — 전 게이트 PASS 확인. FAIL 시: 게이트별 원인 분석을 evidence 에 기록하고 **점수 짜맞추기 수정 금지** — 구조 원인(짚기 미발화/과확장/캐시)만 후속 대상으로 박제 후 belle 판단 요청.
    6. sweep report + breakdowns JSON 을 `backend/evals/phase25/baseline/` 에 커밋 (phase24 패턴).
    7. `25-SWEEP-EVIDENCE.md` 박제: 게이트 결과표(6페어 score, kip-up 구조 assert 상세), 짚기-FP 관측(성공 멤버별 짚은 관절 + 빈도 — 최초 관측치), cold/warm 결정론, 캐시 miss→hit 확인, zoom 카드 재생성 여부 + belle 육안 확인 follow-up 항목(25-03 산출 PNG — S3 zoom_/zoom_adv_ 키), F(260704-fz4) advisory→confirmed 승격 관측(vision-확인 관절이 confirmed 티어로 나오는지).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && PYTHONPATH=shared/python:. python3 evals/phase25/assert_gates.py</automated>
  </verify>
  <done>success 6/6==100 + kip-up 구조 assert PASS + fault 변별 무퇴행 + cold/warm 결정론 — 전 게이트 GREEN, evidence/baseline 커밋 완료. FAIL 이면 원인 박제 + belle 에스컬레이션 (짜맞추기 수정 금지).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pod SSH / SSM | 시크릿(Gemini 키, AWS 키)이 sweep env 에 주입 |
| Gemini API | 외부 호출 36건 (비용/rate limit) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-09 | Information Disclosure | sweep env/로그 | mitigate | 키는 SSM 조회로만 주입(하드코딩 0), evidence/커밋에 키 미포함 |
| T-25-10 | DoS (크레딧 고갈) | cold sweep 36 call | mitigate | Task 2 blocking checkpoint(belle 크레딧 확인) + 429 시 fail-closed 중단 |
| T-25-11 | Repudiation (결과 오염) | 병렬 실행 | mitigate | SERIAL 강제 + cold/warm 결정론 게이트 |
| T-25-SC | Tampering | 패키지 설치 | accept | 신규 패키지 0 |
</threat_model>

<verification>
- assert_gates.py exit 0 (전 게이트 PASS, artifact 존재 상태)
- 25-SWEEP-EVIDENCE.md 에 게이트표 + 짚기-FP 관측 + follow-up 박제
- baseline artifacts 커밋 (phase25_sweep_report.json + breakdowns)
</verification>

<success_criteria>
- ROADMAP Phase 25 게이트 전부 실측 충족: success 6/6==100, kip-up < 88 + vision-짚은 관절만 window 감점(구조), fault 멤버별 score <= phase24 baseline 무퇴행 + climb not_pole, 결정론
- 짚기-FP율 최초 관측치 박제 (게이트 아님)
- belle follow-up 명시: zoom 카드 육안 확인 + kip-up 점수 도메인 판정(88~50 사이 자리 찾음 확인)
</success_criteria>

<output>
Create `.planning/phases/25-vision-pointed-upper-body/25-04-SUMMARY.md` when done
</output>
