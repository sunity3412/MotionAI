---
phase: 26-onboarding-upload-guide
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/app/tutorial.tsx
  - app/src/lib/onboarding.ts
  - app/src/app/index.tsx
autonomous: true
requirements: [ONBD-01]
must_haves:
  truths:
    - "첫 실행 게스트는 홈 진입 전에 튜토리얼을 1회 본다 (D-03)"
    - "튜토리얼은 건너뛰기 가능하고, 스킵/완료 후에는 다시 자동 노출되지 않는다 (D-03)"
    - "튜토리얼 내용이 기대설정 중심이다 — 무엇을 측정하고 무엇은 못 하는지 (D-04)"
    - "AsyncStorage 오류가 나도 앱 진입이 차단되지 않는다 (graceful)"
  artifacts:
    - path: "app/src/app/tutorial.tsx"
      provides: "Figma 튜토리얼 스와이프 화면 (dot 인디케이터 + 건너뛰기 + 시작하기 CTA)"
      min_lines: 80
    - path: "app/src/lib/onboarding.ts"
      provides: "첫 실행 1회 플래그 helper (hasSeenTutorial / markTutorialSeen)"
      contains: "@sunity:tutorial_seen"
  key_links:
    - from: "app/src/app/index.tsx"
      to: "app/src/lib/onboarding.ts"
      via: "hasSeenTutorial() 라우팅 분기"
      pattern: "hasSeenTutorial"
    - from: "app/src/app/tutorial.tsx"
      to: "app/src/lib/onboarding.ts"
      via: "markTutorialSeen() 완료/스킵 시"
      pattern: "markTutorialSeen"
---

<objective>
**시나리오 0단계 작업 (기대 설정 온보딩).** 첫 실행 1회 노출되는 Figma 튜토리얼 화면 + 첫 실행 감지 라우팅을 구현한다 (D-03/D-04).

Purpose: 사전 불신("AI는 일반 답변만") 캘리브레이션 — 수강생이 학원에서 혼자 앱을 켰을 때 무엇을 기대해야 하는지 첫 화면에서 설정한다. 파일럿 성공 기준("혼자 완료")의 시작점.
Output: `app/src/app/tutorial.tsx`(신규), `app/src/lib/onboarding.ts`(신규), `app/src/app/index.tsx`(라우팅 분기).

배포 노트: JS-only 변경 (native 모듈 추가 0 — AsyncStorage는 기설치). **OTA(expo-updates) 배포 가능.**
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-onboarding-upload-guide/26-CONTEXT.md
@.planning/phases/26-onboarding-upload-guide/26-UI-SPEC.md
@.planning/phases/26-onboarding-upload-guide/26-PATTERNS.md
@app/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: onboarding.ts 첫 실행 플래그 helper + index.tsx 라우팅 분기</name>
  <files>app/src/lib/onboarding.ts, app/src/app/index.tsx</files>
  <read_first>
    - app/src/app/analysis/result.tsx:966-990 (프로젝트 유일 AsyncStorage read/write 선례 — @sunity: prefix + 양방향 .catch graceful)
    - app/src/app/index.tsx 전체 (113줄 — onAuthStateChanged 라우팅 수렴점 :18-28, bootstrapping state :15)
    - app/src/lib/bodyProfile.ts:1-11 (데이터소스 격리 헤더 원칙 — 화면은 lib helper 경유)
    - .planning/phases/26-onboarding-upload-guide/26-PATTERNS.md §onboarding.ts
  </read_first>
  <action>
    (D-03 구현, 시나리오 0단계)

    1. `app/src/lib/onboarding.ts` 신설:
       - 상수 `const TUTORIAL_SEEN_KEY = '@sunity:tutorial_seen';` (`@sunity:` prefix 필수 — Firebase Auth backing store 충돌 회피, result.tsx T-12-03-T4 선례)
       - `export async function hasSeenTutorial(): Promise<boolean>` — `AsyncStorage.getItem(TUTORIAL_SEEN_KEY)` 결과 `=== 'true'`. **catch 시 `true` 반환** (읽기 실패가 홈 진입을 막거나 재노출 루프를 만들면 안 됨 — graceful 방향은 "본 것으로 간주").
       - `export function markTutorialSeen(): void` — `AsyncStorage.setItem(TUTORIAL_SEEN_KEY, 'true').catch(() => {})` (fire-and-forget, UI는 이미 진행).
       - 한국어 헤더 블록 주석: why + `D-03` / `26-UI-SPEC S1` 인용 (기존 컨벤션 — 스펙 섹션 인용).
    2. `app/src/app/index.tsx` 수정: onAuthStateChanged 콜백(:18-28)에서 `router.replace('/(tabs)')` 를 분기로 교체 —
       `hasSeenTutorial().then((seen) => router.replace(seen ? '/(tabs)' : '/tutorial'));`
       기존 bootstrapping state 패턴 유지 (플래그 로드가 비동기여도 CTA 깜빡임 없음). 미인증(else) 경로 불변.
    3. `import type` 규칙, 2-space/single-quote/semicolon 스타일, 이모지 금지 준수.
  </action>
  <verify>
    <automated>npm --prefix app run typecheck && grep -c "@sunity:tutorial_seen" app/src/lib/onboarding.ts && grep -c "hasSeenTutorial" app/src/app/index.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm --prefix app run typecheck` exits 0
    - `app/src/lib/onboarding.ts` 에 `@sunity:tutorial_seen` 키 + `hasSeenTutorial` + `markTutorialSeen` export 존재
    - `app/src/app/index.tsx` 가 `hasSeenTutorial` 을 import 하고 미시청 시 `/tutorial` 로 replace
    - `onboarding.ts` 의 getItem/setItem 모두 `.catch` graceful 처리 존재 (`grep -c "catch" app/src/lib/onboarding.ts` >= 2)
  </acceptance_criteria>
  <done>첫 실행 게스트는 /tutorial 로, 재실행 게스트는 /(tabs) 로 라우팅되고, AsyncStorage 오류 시에도 홈 진입이 차단되지 않는다.</done>
</task>

<task type="auto">
  <name>Task 2: tutorial.tsx — Figma 튜토리얼 스와이프 화면</name>
  <files>app/src/app/tutorial.tsx</files>
  <read_first>
    - Figma fileKey `jrdI7kp245HkPfLB0nclsz` — **Figma MCP 로 튜토리얼 프레임을 먼저 조회** (get_metadata 로 프레임 탐색 → get_screenshot / get_design_context 로 추출). belle 승인 디자인(2026-07-05)이 비주얼 계약 — 자체 디자인 금지 (D-04)
    - .planning/phases/26-onboarding-upload-guide/26-UI-SPEC.md §S1 + §Figma Contract + §실행자 확인 체크리스트
    - app/src/app/index.tsx:98-106 (CTA 버튼 스타일 선례 — layout.ctaHeight/radius.button/colors.brand)
    - app/src/app/(tabs)/analyze.tsx:338-346 (backBtn/건너뛰기 a11y Pressable 선례)
    - app/src/theme/typography.ts (letterSpacing 0 고정 — iOS 26 SIGABRT 회피 주석)
  </read_first>
  <action>
    (D-03/D-04 구현, 시나리오 0단계. **UI = Figma 우선** — 실행자는 구현 전 Figma MCP 로 튜토리얼 프레임을 조회하고, 프레임의 슬라이드 수/카피/레이아웃을 계약으로 삼는다. 아래는 구조 계약이며 Figma 실물과 다르면 Figma 승.)

    `app/src/app/tutorial.tsx` 신설:
    - 페이저: RN 코어 `ScrollView horizontal pagingEnabled showsHorizontalScrollIndicator={false}` + `onMomentumScrollEnd` 에서 `Math.round(contentOffset.x / width)` 로 활성 페이지 산출 (`useWindowDimensions` 의 width). **신규 라이브러리 도입 금지** (UI-SPEC Registry Safety — 코드베이스에 페이저 무아날로그, 신규 작성).
    - 슬라이드 데이터: 모듈 상수 배열 `SLIDES` (title/body/비주얼). 내용 = 기대설정 중심 — Figma 카피가 있으면 그대로, 없으면: (1) 무엇을 측정하는지(관절 각도·기준 모션 비교·투명한 감점 내역), (2) 무엇은 못 하는지(강사를 대체하지 않음 — AI는 보조 도구, 측정 밖 원인은 강사와 확인), (3) 정확한 분석을 위한 촬영·원본 영상 안내. "~해요" 체, 이모지 금지.
    - dot 인디케이터: 하단, 활성 dot = `colors.brand`, 비활성 = `colors.divider` 계열 토큰 (크기/간격은 Figma 추출값, 없으면 4의 배수).
    - 우상단 `건너뛰기`: Pressable, `accessibilityRole="button"` + `accessibilityLabel="튜토리얼 건너뛰기"` + `hitSlop={10}`, 40x40 이상 터치 타깃.
    - 마지막 슬라이드 `시작하기` CTA: `layout.ctaHeight`(54) + `radius.button`(13) + `colors.brand` filled + `typography.button` + `colors.textWhite` (index.tsx cta 선례) — Figma 프레임과 다르면 Figma 우선.
    - 종료 수렴 함수 `finish()` 단일: `markTutorialSeen()` 호출 후 `router.canGoBack() ? router.back() : router.replace('/(tabs)')` — 첫 실행(replace 진입)은 홈으로, FAQ 재진입(push)은 뒤로. 건너뛰기/시작하기 모두 `finish()` 경유.
    - 배경 `colors.bg` 라이트 전용 (다크 배경 금지). 색/간격/타이포는 theme 토큰만 — Figma 추출 hex 는 기존 토큰 매핑, 매핑 불가 시 colors.ts 토큰 신설(이 경우 files_modified 에 colors.ts 추가하고 Phase 26 주석 패턴으로 신설 — colors.ts:37-48 선례). 인라인 hex 금지.
    - 한국어 헤더 주석: `D-03/D-04, 26-UI-SPEC S1, Figma jrdI7kp245HkPfLB0nclsz` 인용.
  </action>
  <verify>
    <automated>npm --prefix app run typecheck && grep -c "pagingEnabled" app/src/app/tutorial.tsx && grep -c "건너뛰기" app/src/app/tutorial.tsx && grep -c "markTutorialSeen" app/src/app/tutorial.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm --prefix app run typecheck` exits 0
    - tutorial.tsx 에 `pagingEnabled` + `onMomentumScrollEnd` + `건너뛰기` + `시작하기` 존재
    - `markTutorialSeen` 이 스킵/완료 양 경로에서 호출됨 (단일 finish 수렴 — `grep -c "finish" app/src/app/tutorial.tsx` >= 3)
    - 인라인 hex 0: `! grep -nE "#[0-9A-Fa-f]{3,8}" app/src/app/tutorial.tsx` (색은 토큰만)
    - `package.json` diff 0 (신규 의존성 0 — OTA 안전)
  </acceptance_criteria>
  <done>Figma 디자인 기반 튜토리얼이 스와이프/스킵/시작하기로 동작하고, 완료 후 재노출되지 않으며, FAQ 재진입 경로(push)에서도 자연스럽게 복귀한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 앱 로컬 저장(AsyncStorage) | 튜토리얼 노출 플래그 — 신뢰 경계 내부(기기 로컬), 서버 무접촉 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Tampering | AsyncStorage tutorial_seen 플래그 | accept | 기기 소유자 본인만 변조 가능, 영향 = 튜토리얼 재노출/미노출 뿐 (보안 자산 아님) |
| T-26-02 | DoS | index.tsx 라우팅 (AsyncStorage 읽기 실패) | mitigate | hasSeenTutorial catch 시 true 반환 — 진입 차단/무한 재노출 불가 (Task 1 acceptance) |
| T-26-SC | Tampering | npm/pip 설치 | n/a | 이 플랜 패키지 설치 0 — Package Legitimacy Gate 해당 없음 |

백엔드 무접촉 — 서버측 위협은 이 phase 스코프 밖 (26-CONTEXT D-01).
</threat_model>

<verification>
- `npm --prefix app run typecheck` GREEN
- 신규 파일 2개 + index.tsx 수정만 — backend/ 디렉토리 diff 0
- 튜토리얼 비주얼의 belle 확인은 26-06 checkpoint 에서 일괄 수행
</verification>

<success_criteria>
- 첫 실행 1회 노출 + 스킵 가능 + 재노출 없음 (D-03)
- 내용 = 기대설정 중심, 비주얼 = Figma 승인 디자인 (D-04)
- JS-only, 신규 의존성 0 (OTA 가능)
</success_criteria>

<output>
Create `.planning/phases/26-onboarding-upload-guide/26-01-SUMMARY.md` when done
</output>
