---
phase: 26-onboarding-upload-guide
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/app/help.tsx
  - app/src/app/analysis/samples.tsx
  - app/src/lib/simulationWriter.ts
  - app/src/lib/simulatedResult.ts
  - app/src/app/analysis/result.tsx
autonomous: true
requirements: [ONBD-01]
must_haves:
  truths:
    - "샘플 결과 미리보기 화면이 이용방법/FAQ 화면으로 교체된다 (F2/D-05)"
    - "FAQ 에서 튜토리얼을 다시 볼 수 있다 (D-03 재진입)"
    - "FAQ 최소 항목: 무엇을 측정/못 하는지, 원본 업로드 이유, 촬영 거리·구도, 실패 대처, 보관·삭제 정책, 튜토리얼 다시 보기"
    - "시뮬레이션 샘플 코드(simulationWriter/simulatedResult)가 result.tsx dev 폴백 포함 잔존 참조 없이 제거된다"
  artifacts:
    - path: "app/src/app/help.tsx"
      provides: "이용방법/FAQ 화면 (samples.tsx 골격 재사용, 정적 콘텐츠)"
      min_lines: 100
  key_links:
    - from: "app/src/app/help.tsx"
      to: "/tutorial"
      via: "튜토리얼 다시 보기 진입점"
      pattern: "router\\.push\\('/tutorial'\\)"
---

<objective>
**시나리오 0단계 작업 (온보딩 여정 편입).** `analysis/samples.tsx`(샘플 결과 미리보기)를 이용방법/FAQ 화면으로 교체한다 (F2, D-05). FAQ 는 기대설정 콘텐츠의 상시 재접근 지점이자 튜토리얼 재진입점(D-03)이다.

Purpose: 파일럿 피드백 F2 — 샘플 미리보기는 시연·검토용이었고, 수강생에게 필요한 것은 "잘 쓰는 방법"이다. 온보딩(튜토리얼) 1회 노출 이후의 상시 기대설정 레이어.
Output: `app/src/app/help.tsx`(신규 FAQ 화면), samples.tsx + simulationWriter.ts + simulatedResult.ts 삭제 + result.tsx 의 getSimulatedResult dev 폴백 제거.

배포 노트: JS-only. **OTA 배포 가능.** analyze.tsx 의 진입 링크 교체(`/analysis/samples` → `/help`)는 26-03 이 수행한다 (파일 소유권 분리 — 같은 wave 파일 충돌 방지). result.tsx 는 다른 어느 플랜/wave 도 소유하지 않음 — 충돌 없음.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-onboarding-upload-guide/26-CONTEXT.md
@.planning/phases/26-onboarding-upload-guide/26-UI-SPEC.md
@.planning/phases/26-onboarding-upload-guide/26-PATTERNS.md
@app/src/app/analysis/samples.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: help.tsx — 이용방법/FAQ 화면 신설</name>
  <files>app/src/app/help.tsx</files>
  <read_first>
    - app/src/app/analysis/samples.tsx 전체 (191줄 — 골격 계약: backBtn 40x40 hitSlop 10 + heading + sub + ScrollView 카드 리스트, badge 스타일 :154-190)
    - .planning/phases/26-onboarding-upload-guide/26-UI-SPEC.md §S2 + §Copywriting(이용방법/FAQ 화면)
    - Figma fileKey `jrdI7kp245HkPfLB0nclsz` — Figma MCP 로 이용방법/FAQ 류 프레임을 **먼저 탐색** (있으면 그것이 계약, 없으면 samples 골격 재사용 — UI-SPEC S2)
    - app/src/app/(tabs)/analyze.tsx:618-623 (텍스트 링크 스타일 선례 — colors.brand + underline)
    - .planning/SCENARIO.md §불변 원칙 (강사 보조 포지셔닝 상시 — FAQ 카피 근거)
  </read_first>
  <action>
    (F2/D-05 구현, 시나리오 0단계. Figma 에 대응 프레임이 있으면 그것이 계약 — 실행자 선탐색.)

    `app/src/app/help.tsx` 신설 (톱레벨 라우트 — inquiry.tsx 와 동급 위치):
    - 골격: samples.tsx 구조 재사용 — `colors.bg` 컨테이너, backBtn(Ionicons chevron-back, 40x40, hitSlop 10), 헤딩 `이용 방법`(`typography.heading`), 부제 `Sunity AI 코치를 잘 쓰는 방법을 모아뒀어요.`(`typography.caption` + lineHeight 18), ScrollView.
    - FAQ = 아코디언 리스트: 모듈 상수 `FAQ_ITEMS: readonly { id, title, body }[]`. 항목 카드 = `colors.cardBg` + `layout.cardBorderWidth`(0.858) + `colors.divider` + `radius.card`(15) + `spacing.cardPadding`(16). 헤더 Pressable(`accessibilityRole="button"` + `accessibilityState={{ expanded }}` + hitSlop 6) 탭으로 본문 토글, 본문 = `typography.caption` + `colors.textMid` + lineHeight 18.
    - 항목 6개 (카피 세부는 재량이되 "~해요" 체, 최소 이 구성 — UI-SPEC §Copywriting):
      1. `무엇을 측정하나요?` — 관절 각도·기준 모션 비교·투명한 감점 내역 (수치는 보조, 원인·행동이 핵심)
      2. `AI 점수가 강사님 평가를 대체하나요?` — 아니요, 강사 보조 도구 (FEED-03 기대설정 — 측정 밖 원인은 강사와 확인)
      3. `왜 원본 영상을 올려야 하나요?` — 카톡 전달본은 압축돼 분석 실패 확률 높음, 원본/직접 촬영 안내
      4. `어떻게 촬영하면 좋나요?` — 몸 전체가 들어오는 거리(약 2~3m), 정면 기준
      5. `분석이 실패하면 어떻게 하나요?` — 촬영 구도/거리·화질 확인 후 재촬영, 문의하기 안내
      6. `영상은 어떻게 보관되나요?` — 분석에만 사용·안전 보관·언제든 삭제 요청 가능 (D-08 고지와 동일 취지)
    - 리스트 하단 `튜토리얼 다시 보기` 진입점 (D-03): 텍스트 링크 (`colors.brand` + underline, analyze.tsx link 선례) — `router.push('/tutorial')`.
    - 한국어 헤더 주석: `F2/D-05/D-03, 26-UI-SPEC S2` 인용. 이모지·인라인 hex 금지.
  </action>
  <verify>
    <automated>npm --prefix app run typecheck && grep -c "튜토리얼 다시 보기" app/src/app/help.tsx && grep -c "이용 방법" app/src/app/help.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm --prefix app run typecheck` exits 0
    - help.tsx 에 `router.push('/tutorial')` 존재 (D-03 재진입)
    - FAQ_ITEMS 항목 >= 6 (`grep -c "title:" app/src/app/help.tsx` >= 6)
    - 인라인 hex 0: `! grep -nE "#[0-9A-Fa-f]{3,8}" app/src/app/help.tsx`
  </acceptance_criteria>
  <done>이용방법/FAQ 화면이 기대설정 6항목 + 튜토리얼 재진입점을 갖추고 /help 라우트로 접근 가능하다.</done>
</task>

<task type="auto">
  <name>Task 2: 샘플 미리보기 코드 제거 (samples.tsx + 시뮬레이션 lib + result.tsx dev 폴백)</name>
  <files>app/src/app/analysis/samples.tsx, app/src/lib/simulationWriter.ts, app/src/lib/simulatedResult.ts, app/src/app/analysis/result.tsx</files>
  <read_first>
    - app/src/app/analysis/samples.tsx:1-25 (헤더 주석 "실 분석 파이프라인 켜지면 simulationWriter 와 함께 폐기 대상" — 삭제 근거)
    - app/src/app/analysis/result.tsx:42 (getSimulatedResult import) + :313-320 (헤더 주석 — 폴백은 dev 안전망, "실 분석 경로는 loading.tsx 가 doc 를 쓰므로 폴백이 활성화될 일은 없다") + :550-584 (useLocalSearchParams + useAnalysisDoc + `result` useMemo — 폴백 발동 지점 :573 과 referenceMotion 덮어쓰기 블록 :574-582)
    - app/src/lib/userAnalyses.ts (useAnalysisDoc 반환 형태 — loading/error/doc 상태 구분, 폴백 제거 후 상태 분기 근거)
    - .planning/phases/26-onboarding-upload-guide/26-PATTERNS.md §samples.tsx (진입 링크는 analyze.tsx:482-489 단일 — 26-03 이 /help 로 교체)
  </read_first>
  <action>
    (F2/D-05 잔여 정리, 시나리오 0단계)

    1. 삭제 전 잔존 참조 확인: `grep -rn "simulationWriter\|simulatedResult\|analysis/samples" app/src --include='*.ts' --include='*.tsx'` 실행 — **예상 소비자 3곳**: (a) samples.tsx 자신, (b) analyze.tsx 의 라우트 문자열(26-03 이 교체), (c) result.tsx 의 getSimulatedResult dev 폴백(이 태스크 step 3 이 제거). 이 3곳 밖의 예상 밖 소비자가 나오면 해당 파일은 삭제하지 말고 SUMMARY 에 보고 (fail-safe).
    2. `app/src/app/analysis/samples.tsx` 삭제 (라우트 `/analysis/samples` 소멸 — 대체 라우트 `/help` 는 Task 1).
    3. **result.tsx dev 폴백 제거** (샘플 시딩 경로가 죽으므로 폴백도 함께 제거가 정합 — 폴백의 존재 이유였던 "샘플 시드 누락" 케이스 자체가 소멸):
       - :42 `import { getSimulatedResult } from '../../lib/simulatedResult';` 제거.
       - :317-320 헤더 주석의 getSimulatedResult 폴백 단락을 "Firestore doc 단일 소스 — 시뮬 폴백은 Phase 26(F2/D-05)에서 샘플 경로와 함께 제거" 취지로 갱신 (:551 인라인 주석도 동일 갱신).
       - `result` useMemo(:571-584) 재작성: `storedDoc?.result` 부재 시 시뮬 폴백 대신 null 을 갖도록 하고, 점수 렌더 이전에 상태 분기 — `useAnalysisDoc` 의 loading/error/doc 상태를 활용해 (i) 로딩 중 = 기존 로딩 표현/빈 상태, (ii) 최종 부재 = 한국어 안내("분석 결과를 불러올 수 없어요" 취지, "~해요" 체) + 뒤로가기, 기존 에러 표시 계층 컨벤션 재사용. **시뮬 데이터 렌더 금지.** 폴백 제거로 고아가 되는 로컬(referenceMotionName 덮어쓰기 블록 :574-582 등)이 있으면 함께 정리하되, 다른 용처가 남은 param/로컬은 유지.
    4. `app/src/lib/simulationWriter.ts`, `app/src/lib/simulatedResult.ts` 삭제 (step 2·3 이후 소비자 0 — 헤더 주석의 예정된 폐기. simulationWriter 는 외부 소비자 없음 확인됨).
    5. typecheck 로 잔존 import 부재 증명.
  </action>
  <verify>
    <automated>npm --prefix app run typecheck && ! test -f app/src/app/analysis/samples.tsx && ! test -f app/src/lib/simulationWriter.ts && ! test -f app/src/lib/simulatedResult.ts && ! grep -rn "simulationWriter\|simulatedResult" app/src</automated>
  </verify>
  <acceptance_criteria>
    - `npm --prefix app run typecheck` exits 0 (dangling import 0)
    - `app/src/app/analysis/samples.tsx`, `app/src/lib/simulationWriter.ts`, `app/src/lib/simulatedResult.ts` 파일 부재
    - `grep -rn "simulationWriter\|simulatedResult" app/src` 매치 0 (result.tsx 포함 잔존 참조 0)
    - result.tsx: doc.result 부재 시 시뮬 데이터가 아닌 한국어 안내 상태 렌더 (getSimulatedResult 호출 0)
  </acceptance_criteria>
  <done>시뮬레이션 샘플 경로가 result.tsx dev 폴백 포함 코드베이스에서 완전히 제거되고 typecheck 가 깨끗하다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 없음 | 정적 콘텐츠 화면 + 데드코드 삭제 — 입력/저장/네트워크 0 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-03 | Information Disclosure | FAQ 보관·삭제 정책 카피 | mitigate | 카피가 실제 정책(분석 사용·보관·삭제 요청 가능)과 일치하도록 D-08 고지 문구와 동일 취지 고정 — 과장/허위 고지 금지 |
| T-26-04 | DoS | 시뮬레이션 lib 삭제 (잔존 참조) | mitigate | 삭제 전 grep 게이트 + typecheck — 알려진 소비자 3곳(samples/analyze/result)은 이 플랜·26-03 이 소유, 그 밖의 예상 밖 소비자 발견 시 삭제 중단·보고 (Task 2 action 1). result.tsx 폴백 제거는 doc 부재 안내 상태로 대체 (Task 2 action 3) |
| T-26-SC | Tampering | npm/pip 설치 | n/a | 패키지 설치 0 — Package Legitimacy Gate 해당 없음 |

백엔드 무접촉 — 서버측 위협 스코프 밖.
</threat_model>

<verification>
- `npm --prefix app run typecheck` GREEN
- backend/ diff 0
- `grep -rn "simulationWriter\|simulatedResult" app/src` 매치 0 (result.tsx 포함)
- 진입 링크(analyze.tsx) 교체는 26-03 검증 항목 — 이 플랜에서는 /help 라우트 존재만 보장
</verification>

<success_criteria>
- 샘플 미리보기 → 이용방법/FAQ 교체 완료 (F2/D-05)
- FAQ 에서 튜토리얼 재진입 가능 (D-03)
- 데드코드 잔존 0 (result.tsx dev 폴백 포함), JS-only (OTA 가능)
</success_criteria>

<output>
Create `.planning/phases/26-onboarding-upload-guide/26-02-SUMMARY.md` when done
</output>
