---
phase: 26-onboarding-upload-guide
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/types/analysis.ts
  - backend/shared/python/sunity_shared/models.py
  - docs/contract.md
  - app/src/app/analysis/loading.tsx
  - app/src/app/(tabs)/analyze.tsx
  - app/src/app/analysis/reference.tsx
autonomous: true
requirements: [ONBD-02, ONBD-03]
must_haves:
  truths:
    - "업로드 직전(영상 pick 전) 프라이버시 1줄 고지가 보인다 — 동의 버튼이 아닌 고지 (D-08)"
    - "학습활용 opt-in 체크가 별도로 존재하고 기본값 off 이며, 미체크로 업로드가 지연/차단되지 않는다 (D-08/D-09)"
    - "동의 여부가 분석 문서 learningOptIn(boolean) 으로 항상 기록돼 Phase 22 manifest 게이트가 동의한 영상만 학습 후보로 필터링할 수 있다 (D-09)"
    - "mode1 경로(reference.tsx 경유)에서도 동의값이 유실되지 않는다"
    - "라우트 param 과 계약 필드가 learningOptIn 단일 네이밍이다 (리뷰 LOW-1)"
    - "촬영 거리 안내(약 2~3m, 몸 전체)가 pick 전에 노출된다 (D-01-i)"
    - "not_pole 게이트/임계는 어떤 파일에서도 변경되지 않는다 (D-01)"
  artifacts:
    - path: "app/src/types/analysis.ts"
      provides: "AnalysisDoc.learningOptIn 계약 필드"
      contains: "learningOptIn"
    - path: "docs/contract.md"
      provides: "learningOptIn 계약 문서 미러"
      contains: "learningOptIn"
    - path: "backend/shared/python/sunity_shared/models.py"
      provides: "learningOptIn 계약 미러 (주석/문서 — 로직 무접촉)"
      contains: "learningOptIn"
  key_links:
    - from: "app/src/app/(tabs)/analyze.tsx"
      to: "app/src/app/analysis/loading.tsx"
      via: "라우터 param learningOptIn ('1' | 미포함) — lowQuality 선례, 계약 필드와 단일 네이밍 (리뷰 LOW-1)"
      pattern: "learningOptIn"
    - from: "app/src/app/analysis/reference.tsx"
      to: "app/src/app/analysis/loading.tsx"
      via: "mode1 경로 param pass-through"
      pattern: "learningOptIn"
    - from: "app/src/app/analysis/loading.tsx"
      to: "users/{uid}/analyses/{id}"
      via: "setDoc learningOptIn boolean 기록"
      pattern: "learningOptIn"
---

<objective>
**시나리오 0.5단계(프라이버시 안심) + 1단계(촬영 안내) 작업.** 업로드 직전 프라이버시 1줄 고지 + 학습활용 opt-in 체크(D-08/D-09) + 촬영 거리 안내(D-01-i)를 analyze.tsx 에 삽입하고, 동의값을 분석 문서 계약 필드 `learningOptIn` 으로 기록한다 (3-way lockstep — 이 phase 에서 허용된 유일한 계약 예외, 필드 추가만·로직 무접촉). 라우트 param 도 동일하게 `learningOptIn` 으로 명명 — 계약 필드와 단일 네이밍 (리뷰 LOW-1 반영, 구 명칭 trainingOptIn 사용 금지).

Purpose: 설문 노출 우려 2건 대응(0.5단계) + Phase 22 D-12 학습 플라이휠의 동의 근거 확보 — 동의한 영상만 학습 후보. 촬영 거리 안내는 not_pole 오반려(A1, torso ratio 17~36배)의 예방 레이어 (게이트 자체는 불변 — D-01).
Output: 계약 필드 3-way(analysis.ts + models.py + contract.md) + analyze.tsx opt-in UI/고지/거리 안내 + reference.tsx pass-through + loading.tsx Firestore 기록. 부수: 샘플 링크 → `/help` 교체 (F2 잔여, 26-02 연동).

배포 노트 (리뷰 LOW-2 문구 교정): **앱 런타임 변경은 OTA-safe — OTA 배포 가능.** backend 편집은 `models.py` **주석-only 계약 미러**로 Lambda 재배포 불필요 — 이 플랜은 Python 파일을 주석 수준에서 건드리므로 "JS-only" 로 기술하지 않는다.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-onboarding-upload-guide/26-CONTEXT.md
@.planning/phases/26-onboarding-upload-guide/26-UI-SPEC.md
@.planning/phases/26-onboarding-upload-guide/26-PATTERNS.md
@.planning/phases/22-custom-vlm-finetune/22-04-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: learningOptIn 계약 필드 3-way lockstep + loading.tsx Firestore 기록</name>
  <files>app/src/types/analysis.ts, backend/shared/python/sunity_shared/models.py, docs/contract.md, app/src/app/analysis/loading.tsx</files>
  <read_first>
    - app/src/types/analysis.ts:589-610 (AnalysisDoc.bodyProfile 옵셔널 필드 추가 선례 — why 주석 + 출처 인용 형식)
    - app/src/app/analysis/loading.tsx:124-137 (startAnalysisUpload setDoc — 조건부 spread 선례) + :270-294 (useLocalSearchParams lowQuality?: string '1' 규약)
    - backend/shared/python/sunity_shared/models.py:200-263 (bodyProfile 계약 미러 주석 블록 + normalize_body_profile — 미러 형식 선례) + :376 (analyses 경로 docstring)
    - docs/contract.md §3 (AnalysisDoc 필드 표, line ~86)
    - .planning/phases/22-custom-vlm-finetune/22-04-PLAN.md frontmatter+Task 2 (manifest 게이트가 학습 후보를 거르는 소비자 — **현 게이트는 anonymized/등록만 필터, learningOptIn 필터는 아직 없음.** learningOptIn 은 이 게이트가 후속으로 소비해야 할 동의 전제)
  </read_first>
  <action>
    (D-08/D-09 구현, 시나리오 0.5단계. 계약 필드 추가만 — 백엔드 로직/채점 무접촉.)

    1. `app/src/types/analysis.ts` — `AnalysisDoc` 에 옵셔널 필드 추가 (bodyProfile 선례 형식):
       `learningOptIn?: boolean;` + 한국어 주석: 업로드 시점 학습활용 opt-in 동의값 (D-08/D-09). 기본 off — 명시 체크만 true. 필드 부재(Phase 26 이전 문서) = 동의 없음. Phase 22 D-12 학습 플라이휠의 동의 근거 — **Phase 22 manifest 게이트는 `learningOptIn === true` 인 분석의 영상만 학습 후보로 삼아야 한다** (의무 계약 — 현 22-04 게이트는 anonymized/등록만 필터, learningOptIn 필터는 Phase 22 후속 반영 필요). 현재형 단정("조회한다") 금지 — 아직 게이트에 이 필터가 없다.
    2. `backend/shared/python/sunity_shared/models.py` — bodyProfile 미러 주석 블록(:200-218) 과 동일 형식으로 `learningOptIn` 미러 주석 추가: 앱이 분석 문서 생성 시 기록하는 boolean, 백엔드 파이프라인은 읽지도 쓰지도 않음(로직 무접촉), 소비 예정 = Phase 22 학습 manifest 게이트 (learningOptIn 필터는 Phase 22 후속 반영 필요). **함수/검증 로직 추가 금지** — 주석 미러만.
    3. `docs/contract.md` — §3 AnalysisDoc 섹션에 `learningOptIn?: boolean` 행 추가: 의미(학습활용 동의), 기록 주체(앱 loading.tsx), 기본(false/부재=미동의), 소비 예정(Phase 22 manifest 게이트 — learningOptIn 필터 후속 반영 필요), 출처(26-CONTEXT D-08/D-09). 라우트 param 도 동일 명칭 `learningOptIn` ('1' | 미포함) 임을 부기 — 단일 네이밍 (리뷰 LOW-1).
    4. `app/src/app/analysis/loading.tsx`:
       - `useLocalSearchParams` 타입에 `learningOptIn?: string` 추가 (lowQuality '1' 문자열 규약 동일 — 계약 필드와 같은 이름, 리뷰 LOW-1).
       - `startAnalysisUpload` 의 생성 setDoc 에 `learningOptIn: learningOptIn === '1',` 추가 — **항상 boolean 기록** (조건부 spread 아님: 부재≠false 를 없애 동의 증거를 명시적으로 남긴다. `=== '1'` 엄격 비교로 param 유실/오염 시 false 로 안전 강하 — fail-safe 방향 = 미동의).
       - 주석에 D-09 + Phase 22 소비 예정(후속 반영) 인용.
    5. 세 파일의 3-way lockstep 주석 상호 인용 유지 (기존 컨벤션 — "계약 변경 시 셋 동시 편집").
    6. **SUMMARY 에 Phase 22 후속 항목 명시 플래그:** "22-04 manifest 게이트에 `learningOptIn === true` 필터 추가 필요 — 현 게이트는 anonymized/등록만 필터하고 learningOptIn 을 아직 읽지 않음. 이 필터가 반영되기 전까지 D-09(동의한 영상만 학습 후보)는 계약상 의무일 뿐 미집행." 다음 Phase 22 작업/gap closure 가 픽업할 수 있게 SUMMARY 의 후속 작업 섹션에 기재.
  </action>
  <verify>
    <automated>npm --prefix app run typecheck && grep -c "learningOptIn" app/src/types/analysis.ts && grep -c "learningOptIn" backend/shared/python/sunity_shared/models.py && grep -c "learningOptIn" docs/contract.md && grep -c "learningOptIn" app/src/app/analysis/loading.tsx && cd backend && python3 -c "import ast; ast.parse(open('shared/python/sunity_shared/models.py').read()); print('models.py parse ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `npm --prefix app run typecheck` exits 0
    - `learningOptIn` 이 analysis.ts + models.py + contract.md + loading.tsx 4곳 모두 존재 (3-way lockstep + 기록점)
    - loading.tsx 에 `learningOptIn === '1'` 엄격 비교 존재
    - 계약 주석이 Phase 22 게이트 소비를 **의무형**("삼아야 한다"/"후속 반영 필요")으로 기술 — 현재형 단정("조회한다"/"필터한다") 0
    - models.py 는 python3 ast 파싱 통과 + 함수 시그니처 diff 0 (`git diff backend/shared/python/sunity_shared/models.py` 가 주석 추가만) — 로직 무접촉 증명
    - backend/functions/, backend/shared/python/sunity_shared/analysis/ diff 0 (채점/게이트 무접촉 — D-01)
    - `trainingOptIn` 문자열 0: `! grep -rn "trainingOptIn" app/src docs` (구 명칭 잔존 금지 — 리뷰 LOW-1)
    - SUMMARY 에 Phase 22 후속 항목(22-04 게이트 learningOptIn 필터 추가 필요) 플래그 존재
  </acceptance_criteria>
  <done>동의값이 분석 문서에 항상 boolean 으로 기록되고, param↔필드가 learningOptIn 단일 네이밍이며, 계약 3면(TS/Python/문서)이 Phase 22 게이트의 의무를 정확히(미집행 상태 포함) 기술하고, 백엔드 로직은 단 한 줄도 변하지 않는다.</done>
</task>

<task type="auto">
  <name>Task 2: analyze.tsx 프라이버시 1줄 + opt-in 체크 행 + 촬영 거리 안내 + param 배선 (순수 헬퍼)</name>
  <files>app/src/app/(tabs)/analyze.tsx, app/src/app/analysis/reference.tsx</files>
  <read_first>
    - app/src/app/(tabs)/analyze.tsx:136-181 (routeAfterPick — lowQuality param 선례: undefined 면 param 미포함) + :367-372, 575-581 (guidance 캡션 블록/스타일 — 삽입점) + :482-489 (샘플 링크 — /help 교체 대상)
    - app/src/app/analysis/reference.tsx:29-40, 85-95 (lowQuality pass-through 선례 — learningOptIn 동일 배선)
    - app/src/components/KeypointOverlayToggle.tsx:9-10, 34-46 (controlled + a11y 골격 — checkbox 변환 원형)
    - .planning/phases/26-onboarding-upload-guide/26-UI-SPEC.md §S3 + §S5 + §Copywriting (확정 카피 3종)
    - Figma fileKey `jrdI7kp245HkPfLB0nclsz` — S3 대응 프레임 유무 Figma MCP 선탐색 (있으면 우선)
  </read_first>
  <action>
    (D-08/D-09/D-01-i 구현, 시나리오 0.5/1단계)

    1. **프라이버시 1줄 (D-08):** 소스 선택 단계(`if (mode)` 블록, 기존 guidance 캡션 인근 — pick 직전 노출 보장)에 Text 추가:
       `영상은 분석에만 사용하고 안전하게 보관해요. 언제든 삭제를 요청할 수 있어요.`
       스타일 = 기존 guidance 동일 (`typography.caption` + `colors.textMid` + lineHeight 18). 동의 버튼 아님 — 고지만. 포괄 동의 문구 금지.
    2. **촬영 거리 안내 (D-01-i, S5):** 같은 영역에 guidance 캡션 추가:
       `몸 전체가 화면에 잘 들어오는 거리(약 2~3m)에서 촬영해 주세요. 너무 가깝거나 멀면 분석에 실패할 수 있어요.`
       기존 원본 화질 guidance(:369) 는 유지 — 이어지는 두 번째 안내로 배치. 주석에 `D-01-i / A1 예방` 인용.
    3. **opt-in 체크 행 (D-08/D-09):** analyze.tsx 인라인 구현 —
       - state: `const [learningOptIn, setLearningOptIn] = useState(false);` — **기본 off 고정, 세션 간 영속 없음** (매 방문 off — 동의는 매 업로드 명시적). 명칭은 계약 필드와 동일 `learningOptIn` (리뷰 LOW-1).
       - 행 전체 Pressable: `accessibilityRole="checkbox"` + `accessibilityState={{ checked: learningOptIn }}` + `accessibilityLabel` + hitSlop 8, 행 높이(라벨 포함) 44pt 이상.
       - 체크박스 비주얼: unchecked = `colors.inputBorder` 테두리 사각, checked = `colors.brand` 채움 + `Ionicons name="checkmark"` `colors.textWhite`.
       - 라벨: `분석 영상을 AI 개선(학습)에 활용하는 데 동의해요 (선택)` — `typography.caption` + `colors.textMid`. "(선택)" 표기 필수. 미체크가 업로드 흐름을 지연/차단하지 않음 (검증 로직 연결 금지).
       - 배치: 프라이버시 1줄 바로 아래.
    4. **param 배선 — 순수 헬퍼 추출 (리뷰 MEDIUM-2):** analyze.tsx 모듈 스코프에 순수 함수를 named export 로 추출:
       `export function buildOptInRouteParams(opts: { learningOptIn: boolean; lowQuality?: boolean }): { learningOptIn?: '1'; lowQuality?: '1' }`
       — true 인 키만 `'1'` 로 포함, false/undefined 키는 반환 객체에서 제외 (lowQuality 선례 — undefined/미포함이면 param 자체 미포함, 기존 흐름 불변). `routeAfterPick` 의 mode1/mode3 양쪽 `router.push` params 가 이 헬퍼 산출을 spread 하도록 배선 — 기존 lowQuality param 방출도 이 헬퍼로 이동 (규약 '1'|미포함 동작은 문자 그대로 불변). React/expo import 없는 순수 함수로 유지 — 추후 테스트 하니스 도입 시 단위테스트 대상 (현재 앱 정적 게이트는 typecheck 뿐 — 행위 증거는 26-06 실기기 기록이 담당).
    5. **reference.tsx pass-through:** `useLocalSearchParams` destructure + 타입에 `learningOptIn?: string` 추가, loading push params(:92 인근) 에 그대로 전달 — mode1 미선택 경로 유실 방지.
    6. **샘플 링크 교체 (F2 잔여, D-05):** :482-489 링크를 `router.push('/help')` + 라벨 `이용 방법 · FAQ` 로 교체, "시연·검토용 진입점" 주석을 F2/D-05 인용으로 갱신 (26-02 가 /help 를 신설·samples 를 삭제함 — 26-02 라우트 이관 결정: `/help` canonical, shim 없음).
  </action>
  <verify>
    <automated>npm --prefix app run typecheck && grep -c "learningOptIn" "app/src/app/(tabs)/analyze.tsx" && grep -c "learningOptIn" app/src/app/analysis/reference.tsx && grep -c "buildOptInRouteParams" "app/src/app/(tabs)/analyze.tsx" && grep -c "삭제를 요청" "app/src/app/(tabs)/analyze.tsx" && grep -c "2~3m" "app/src/app/(tabs)/analyze.tsx" && ! grep -rn "analysis/samples" "app/src/app/(tabs)/analyze.tsx" && ! grep -rn "trainingOptIn" app/src</automated>
  </verify>
  <acceptance_criteria>
    - `npm --prefix app run typecheck` exits 0
    - analyze.tsx 에 `accessibilityRole="checkbox"` + `useState(false)` 초기값 존재 (기본 off — D-09)
    - analyze.tsx / reference.tsx 양쪽에 learningOptIn 존재 (mode1 경로 유실 0), `trainingOptIn` 문자열 0 (리뷰 LOW-1)
    - `buildOptInRouteParams` 가 모듈 스코프 순수 함수(named export, React/expo import 무관)로 존재하고 routeAfterPick mode1/mode3 양쪽이 이 헬퍼를 경유 (리뷰 MEDIUM-2 — param 조립 단일점)
    - 프라이버시 1줄 + 거리 안내 카피가 UI-SPEC §Copywriting 문구와 일치
    - analyze.tsx 에 `/analysis/samples` 참조 0 + `/help` 링크 존재
    - opt-in 미체크 상태에서 업로드를 막는 조건문 0 (`learningOptIn` 이 validate/에러 경로에 등장하지 않음)
  </acceptance_criteria>
  <done>업로드 직전 고지·동의·촬영 안내가 노출되고, 체크 여부와 무관하게 업로드가 진행되며, 동의값이 단일 네이밍 learningOptIn param 으로 mode1/mode3 양 경로에서 loading 까지 도달한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 앱 → Firestore (분석 문서 생성) | 클라이언트가 쓰는 동의값 — Firestore rules 로 본인 문서만 write |
| 라우터 param (analyze → reference → loading) | 동의값 전달 경로 — 유실/오염 시 동의 오기록 위험 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-05 | Tampering | learningOptIn 동의값 무결성 (D-09) | mitigate | 기본 off + `=== '1'` 엄격 비교 + 항상-boolean 기록 + param 조립 순수 헬퍼 단일점 — param 유실/오염 시 false 로 강하 (미동의 방향 fail-safe). 필드 부재(구 문서) = 미동의로 Phase 22 게이트가 해석해야 함 (Task 1 주석 계약 — 게이트 필터 자체는 Phase 22 후속 반영) |
| T-26-06 | Repudiation | 동의 증거 | mitigate | 분석 문서에 업로드 시점 boolean 명시 기록 — Phase 22 manifest 게이트가 true 만 학습 후보로 필터해야 함 (후속 반영 항목 — Task 1 action 6 SUMMARY 플래그로 추적). 실기기 Firestore 증거 기록은 26-06 (리뷰 MEDIUM-2) |
| T-26-07 | Spoofing | 타 사용자 동의값 위조 | accept | Firestore rules 가 users/{uid}/** 본인 한정 — 기존 규칙 무변경, 잔여 위험 = 기기 소유자 본인뿐 |
| T-26-08 | Information Disclosure | 프라이버시 고지 우회(미노출) | mitigate | 고지를 pick 직전 소스 선택 단계에 고정 배치 + 26-06 실기기 checkpoint 에서 노출 확인 |
| T-26-SC | Tampering | npm/pip 설치 | n/a | 패키지 설치 0 — Package Legitimacy Gate 해당 없음 |

백엔드 파이프라인/게이트 무접촉 (models.py 주석 미러만) — 서버측 위협 스코프 밖.
</threat_model>

<verification>
- `npm --prefix app run typecheck` GREEN + models.py ast 파싱 OK
- `git diff backend/` 가 models.py 주석 추가만 (로직 diff 0) — not_pole 게이트/채점 불변 (D-01)
- `! grep -rn "trainingOptIn" app/src docs` — 구 명칭 잔존 0 (리뷰 LOW-1)
- Firestore rules 무변경 (기존 본인 한정 규칙이 동의값 보호를 커버)
- SUMMARY 에 Phase 22 후속 항목(게이트 learningOptIn 필터) 플래그 존재
</verification>

<success_criteria>
- D-08: 1줄 고지 + 별도 opt-in 체크, 포괄 동의 없음
- D-09: 기본 off, learningOptIn 이 Phase 22 manifest 게이트가 소비 가능한 분석 문서 필드로 기록 (게이트측 필터는 후속 반영 항목으로 SUMMARY 플래그)
- D-01-i: 촬영 거리 안내 pick 전 노출, 게이트 불변
- 라우트 param = 계약 필드 = `learningOptIn` 단일 네이밍, param 조립은 순수 헬퍼 단일점 (리뷰 LOW-1/MEDIUM-2)
- 앱 런타임 변경 OTA-safe (OTA 배포 가능); backend 는 models.py 주석-only 계약 미러 — Lambda 재배포 불필요 (리뷰 LOW-2)
</success_criteria>

<output>
Create `.planning/phases/26-onboarding-upload-guide/26-03-SUMMARY.md` when done
</output>
