---
phase: 26-onboarding-upload-guide
plan: 04
type: execute
wave: 2
depends_on: ["26-03"]
files_modified:
  - app/src/app/(tabs)/analyze.tsx
  - app/src/app/analysis/loading.tsx
  - app/src/theme/colors.ts
  - app/src/theme/index.ts
autonomous: true
requirements: [ONBD-03]
must_haves:
  truths:
    - "카톡 전달본(_talkv_ 파일명) pick 시 경고 다이얼로그가 뜨되 진행은 허용된다 — 하드 차단 금지 (D-06)"
    - "카톡 경고를 승인하고 진행한 영상이 not_pole 로 실패하면 화질 원인 안내가 우선 노출된다 (D-07 — lowQuality 플래그 공유)"
    - "카톡 경고와 저화질 경고가 같은 영상에 이중으로 뜨지 않는다 (게이트 직렬 설계)"
    - "플래그 없는 not_pole 실패 화면에 촬영 구도/거리 원인 + 재촬영 행동 안내가 추가된다 (D-01-ii)"
    - "not_pole 게이트/임계와 D-07 분기 우선순위 로직은 불변이다 (D-01)"
  artifacts:
    - path: "app/src/app/(tabs)/analyze.tsx"
      provides: "_talkv_ 감지 순수 헬퍼(isKakaoCompressedVideoName) + Figma 패턴 경고 다이얼로그 + 게이트 체인 (talkv → lowQuality → bodyProfile → route)"
      contains: "_talkv_"
  key_links:
    - from: "app/src/app/(tabs)/analyze.tsx"
      to: "app/src/app/analysis/loading.tsx"
      via: "talkv 승인 → lowQuality:true 플래그 → not_pole 화질 우선 분기 (D-07)"
      pattern: "lowQuality: true"
    - from: "app/src/app/analysis/loading.tsx"
      to: "not_pole 실패 화면"
      via: "isNotPole && !isLowQualityNotPole 분기에 구도 안내"
      pattern: "구도"
---

<objective>
**시나리오 1.5단계(원본 업로드 가이드) + 1.7단계(not_pole 실패 안내) 작업.** 카톡 압축본 `_talkv_` 파일명 감지 경고 다이얼로그(D-06, Figma Dialog Pattern) + 저화질 플로우 연동(D-07) + not_pole 실패 화면 구도 원인 안내(D-01-ii)를 구현한다.

Purpose: 실측(실사용자 371건) 실패 27건 중 카톡 압축본 15건 = 56% — 파일럿 실패의 최대 단일 원인을 pick 시점에 예방한다. not_pole 오반려(A1, torso ratio 불일치)는 실패 화면에서 원인+재촬영을 안내해 "AI가 이상하다" 오인을 줄인다. 게이트 자체는 불변 (D-01 — 위양성 리스크 0).
Output: analyze.tsx 카톡 경고 다이얼로그 + 게이트 체인 (감지는 순수 헬퍼 단일점 — 리뷰 MEDIUM-2), loading.tsx 구도 안내, 필요 시 colors.ts/theme index.ts Figma 토큰 신설.

배포 노트: JS-only. **OTA 배포 가능.** wave 2 — 26-03 이 analyze.tsx/loading.tsx 를 먼저 수정하므로 순차 (파일 소유권).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-onboarding-upload-guide/26-CONTEXT.md
@.planning/phases/26-onboarding-upload-guide/26-UI-SPEC.md
@.planning/phases/26-onboarding-upload-guide/26-PATTERNS.md
@.planning/phases/26-onboarding-upload-guide/26-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: analyze.tsx — _talkv_ 감지 순수 헬퍼 + Figma 패턴 경고 다이얼로그 + 게이트 직렬 체인</name>
  <files>app/src/app/(tabs)/analyze.tsx, app/src/theme/colors.ts, app/src/theme/index.ts</files>
  <read_first>
    - app/src/app/(tabs)/analyze.tsx:70-86 (lowQualityPicked/pendingPicked 보류-picked 상태머신) + :235-273 (handleResult 감지 삽입점 + continueLowQuality 승인 플래그) + :386-422 (lqCard Modal 구조 — native back/백드롭 = 영상 버림)
    - app/src/components/BodyProfilePromptModal.tsx:107-116 (보조 버튼 흰+보더 스타일 선례) + :17-23 (3-way dismiss 수렴 주석)
    - .planning/phases/26-onboarding-upload-guide/26-UI-SPEC.md §Figma Dialog Pattern + §S4 + §Copywriting(카톡 경고)
    - Figma fileKey `jrdI7kp245HkPfLB0nclsz` — **Figma MCP 로 오류 다이얼로그 프레임("용량이 너무 커요"/"지원할 수 없는 파일이에요" 류) 선탐색** (belle 실물 확인 2026-07-07 — 있으면 추출값이 계약)
    - app/src/theme/colors.ts:37-48, 65-68 (토큰 신설 주석 패턴 — Phase 표기 + 근거)
  </read_first>
  <action>
    (D-06/D-07 구현, 시나리오 1.5단계)

    1. **감지 — 순수 헬퍼 추출 (리뷰 MEDIUM-2):** 모듈 상수 `const KAKAO_COMPRESSED_MARKER = '_talkv_';` + analyze.tsx 모듈 스코프 순수 함수를 named export 로 추출:
       `export function isKakaoCompressedVideoName(source: string): boolean` — 구현 = `source.toLowerCase().includes(KAKAO_COMPRESSED_MARKER)` (대소문자 무시 — best-effort 파일명 감지, D-06). `handleResult`(:235) 에서 validate 통과 후 `const source = asset.fileName ?? asset.uri;` 에 대해 **이 헬퍼로만 검사** (인라인 includes 중복 금지 — 감지 로직 단일점). React/expo import 없는 순수 함수로 유지 — 추후 테스트 하니스 도입 시 단위테스트 대상 (현재 앱 정적 게이트는 typecheck 뿐 — 행위 증거는 26-06 실기기 기록이 담당). 주석에 실측 근거(실패의 56%) 인용.
    2. **게이트 직렬 체인 (이중 경고 금지):** handleResult 순서 = **talkv 검사 → (미해당 시) checkLowQuality → maybePromptBeforeRoute**. talkv 감지 시 `setTalkvPicked(picked)` 로 보류 후 return — 저화질 검사는 건너뛴다 (카톡 경고가 화질 경고를 포함·상회하는 메시지이므로 같은 영상에 두 모달 금지 — PATTERNS §비차단 경고 상태머신).
    3. **승인/취소 수렴 (D-07 핵심):**
       - `continueTalkv()`: 보류 picked 를 `{ ...p, lowQuality: true }` 로 **기존 lowQuality 승인 플래그를 그대로 심어** `maybePromptBeforeRoute()` 재개 — quick-260704-fwb 의 not_pole 화질 우선 분기가 추가 코드 0 으로 연동된다 (param 방출은 26-03 의 `buildOptInRouteParams` 헬퍼가 담당 — 플래그만 심으면 배선 불변). 주석에 D-07 인용.
       - `cancelTalkv()`: 보류 picked 폐기 (다른 영상 선택).
       - Modal `onRequestClose={cancelTalkv}` + 백드롭 = 취소 (기존 lq 모달과 동일 안전 동작 — 영상 버림).
    4. **다이얼로그 비주얼 (Figma Dialog Pattern):** Figma 프레임 추출값이 계약. 구조 = 연한 브랜드 틴트 라운드 카드(추출 hex → `colors.brandTint`/`brandSoft`/`brandBg` 매핑, 불일치 시 colors.ts 토큰 신설) + 중앙 상단 빨간 원형 느낌표 아이콘(`colors.brand` 계열 우선) + 타이틀 `카톡으로 받은 영상 같아요`(Bold, `colors.textPrimary`) + 본문 `카톡으로 전달된 영상은 압축돼 화질이 낮아져 분석에 실패할 확률이 높아요. 원본 영상을 올리거나 앱에서 직접 촬영하면 더 정확하게 분석할 수 있어요.`(`colors.textMid`) + **가로 2버튼**: 좌 보조 `이대로 계속`(흰 배경 + `colors.inputBorder` 보더 + `colors.textSecondary` — BodyProfilePromptModal secondary 선례) / 우 주액션 `다른 영상 선택`(`colors.brand` filled + `typography.button` + `colors.textWhite`). 각 버튼 a11y(role/label) + 높이 `layout.ctaHeight`. Figma radius 가 `radius.modal`(20) 초과면 `app/src/theme/index.ts` 에 radius 토큰 신설 후 사용 — 인라인 수치/hex 금지.
    5. **기존 lqCard 정렬 검토:** 저화질 경고 모달(lqCard, 세로 버튼)이 Figma 패턴과 다르면 같은 다이얼로그 컴포넌트/스타일로 정렬 — **단 D-07 로직(continueLowQuality 의 lowQuality:true 플래그와 게이트 판정 checkLowQuality)은 문자 그대로 불변.** 정렬이 회귀 위험을 키우면 스타일 정렬은 생략하고 SUMMARY 에 사유 보고 (재량).
    6. 토큰 신설 시 colors.ts 에 `── Phase 26 신설 토큰 (26-UI-SPEC §Figma Dialog Pattern) ──` 주석 블록 패턴 준수.
  </action>
  <verify>
    <automated>npm --prefix app run typecheck && grep -c "_talkv_" "app/src/app/(tabs)/analyze.tsx" && grep -c "isKakaoCompressedVideoName" "app/src/app/(tabs)/analyze.tsx" && grep -c "talkvPicked" "app/src/app/(tabs)/analyze.tsx" && grep -c "lowQuality: true" "app/src/app/(tabs)/analyze.tsx" && grep -c "다른 영상 선택" "app/src/app/(tabs)/analyze.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `npm --prefix app run typecheck` exits 0
    - `_talkv_` 감지가 순수 함수 `isKakaoCompressedVideoName`(모듈 스코프, named export, React/expo import 무관) 단일점 경유 — handleResult 에 인라인 `includes(KAKAO_COMPRESSED_MARKER)`/`includes('_talkv_')` 중복 0 (리뷰 MEDIUM-2)
    - handleResult 에서 talkv 검사가 checkLowQuality **앞**에 위치 (grep -n 으로 줄번호 비교 — 직렬 체인)
    - `continueTalkv` 경로에 `lowQuality: true` 존재 (D-07 플래그 공유)
    - talkv 다이얼로그에 하드 차단 없음 — `이대로 계속` 버튼 존재 + 진행 시 maybePromptBeforeRoute 재개
    - Modal onRequestClose = 취소 함수 (native back = 영상 버림)
    - 인라인 hex 신규 도입 0: analyze.tsx diff 에 `#` hex 리터럴 없음 (토큰만)
    - checkLowQuality 함수 본문 diff 0 (기존 저화질 판정 불변)
  </acceptance_criteria>
  <done>카톡 압축본 pick 시 Figma 패턴 경고가 뜨고, 감지는 순수 헬퍼 단일점을 경유하며, 진행 선택 시 lowQuality 승인 플래그가 심어져 D-07 분기가 공짜로 연동되고, 이중 경고가 없다.</done>
</task>

<task type="auto">
  <name>Task 2: loading.tsx — not_pole 실패 화면 구도/거리 원인 안내 (D-01-ii)</name>
  <files>app/src/app/analysis/loading.tsx</files>
  <read_first>
    - app/src/app/analysis/loading.tsx:332-338 (에러 표시 계층 불변 계약 — localError > Firestore > 기본) + :390-407 (isLowQualityNotPole 화질 우선 분기 — 불변) + :440-464 (tipCard 항목 패턴)
    - .planning/phases/26-onboarding-upload-guide/26-UI-SPEC.md §S6 + §Copywriting(not_pole 구도 안내)
    - Figma fileKey `jrdI7kp245HkPfLB0nclsz` — 대응 오류 화면/다이얼로그 프레임 유무 Figma MCP 선탐색 (있으면 그 구조·카피 우선)
  </read_first>
  <action>
    (D-01-ii 구현, 시나리오 1.7단계. 이 화면은 기존 네이비 예외 화면 — 신규 색 도입 금지, 기존 스타일 토큰(tipCard/tipItem/ERROR_RED) 그대로.)

    1. **분기 스코프:** 구도 안내는 `isNotPole && !isLowQualityNotPole` (플래그 없는 not_pole) 에만 추가. `isLowQualityNotPole` 화질 우선 분기와 그 판정 조건은 **문자 그대로 불변** (D-07).
    2. **본문 안내:** 기존 타이틀 `기준 동작과 너무 달라요` 유지. 해당 분기의 본문/설명 영역에 추가:
       `촬영 구도나 거리가 기준 영상과 많이 다르면 이렇게 나올 수 있어요. 몸 전체가 화면에 들어오는 거리에서 정면 기준으로 다시 촬영해 보세요.`
    3. **tipCard 항목 보강:** 기존 not_pole tipCard(:440-464) 항목에 구도/거리 항목 추가:
       `· 몸 전체가 화면에 들어오는 거리(약 2~3m)에서 정면으로 촬영했는지` — 기존 `tipItem` 스타일 그대로.
    4. 재촬영 유도는 기존 재시도/문의 버튼 구조 유지 — 버튼 신설 없이 본문 행동 지시로 충분 (재량 범위 내 최소 변경).
    5. 주석에 `D-01-ii / A1(torso ratio 17~36배 오반려 실측)` 인용. **게이트/임계 관련 백엔드 코드 무접촉** — 이 태스크는 loading.tsx 카피/렌더만.
  </action>
  <verify>
    <automated>npm --prefix app run typecheck && grep -c "구도" app/src/app/analysis/loading.tsx && grep -c "isLowQualityNotPole" app/src/app/analysis/loading.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm --prefix app run typecheck` exits 0
    - 구도 안내 카피가 `isNotPole && !isLowQualityNotPole` 스코프에만 렌더 (화질 우선 분기 조건식 diff 0)
    - `기준 동작과 너무 달라요` / `화질이 낮아 분석하지 못했을 수 있어요` 기존 타이틀 2종 문자열 불변
    - loading.tsx 에 신규 색상 토큰/hex 도입 0
    - backend/ diff 0 (not_pole 게이트·임계 불변 — D-01)
  </acceptance_criteria>
  <done>플래그 없는 not_pole 실패에서 수강생이 "구도/거리 때문일 수 있다 + 어떻게 다시 찍을지"를 화면에서 바로 알 수 있고, 화질 우선 분기는 기존 그대로 동작한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사용자 파일명 → 앱 감지 로직 | `_talkv_` 파일명은 사용자 제어 입력 — 감지는 advisory (보안 게이트 아님) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-09 | Spoofing | _talkv_ 감지 우회 (파일명 변경) | accept | 감지는 경고용 advisory — 우회해도 분석 파이프라인의 기존 검증(validate/게이트)이 그대로 적용. 하드 차단이 아니므로 우회 이득 없음 (D-06 설계 의도) |
| T-26-10 | Tampering | D-07 분기 회귀 (화질 우선 안내 파손) | mitigate | continueTalkv 가 기존 lowQuality 플래그를 재사용 (신규 분기 0) + isLowQualityNotPole 조건식 diff 0 acceptance 게이트 + 감지 순수 헬퍼 단일점 (리뷰 MEDIUM-2) |
| T-26-11 | DoS | 게이트 체인 데드락 (모달 이중 노출/보류 유실) | mitigate | 직렬 체인 (talkv → lowQuality → bodyProfile) + 모든 dismiss 경로 단일 수렴 함수 + native back = 영상 버림 (기존 안전 동작) |
| T-26-SC | Tampering | npm/pip 설치 | n/a | 패키지 설치 0 — Package Legitimacy Gate 해당 없음 |

백엔드 무접촉 — not_pole 게이트/임계는 서버측이며 이 플랜은 안내 카피만 (D-01).
</threat_model>

<verification>
- `npm --prefix app run typecheck` GREEN
- backend/ diff 0 — D-01 게이트 불변 증명
- 실기기 확인(카톡 실파일 pick → 경고 → 진행 → 실패 시 화질 우선 문구)은 26-06 checkpoint 항목 — transcript + 증거 기록 포함 (리뷰 MEDIUM-2)
</verification>

<success_criteria>
- D-06: `_talkv_` 감지 경고 + 진행 허용 (하드 차단 0) — 감지는 `isKakaoCompressedVideoName` 순수 헬퍼 단일점
- D-07: 승인 시 lowQuality 플래그 연동 — not_pole 화질 우선 분기 공유
- D-01-ii: 플래그 없는 not_pole 에 구도/거리 원인 + 재촬영 안내
- Figma Dialog Pattern 준수 (브랜드 틴트 카드 + 빨간 느낌표 + 가로 2버튼), JS-only (OTA 가능)
</success_criteria>

<output>
Create `.planning/phases/26-onboarding-upload-guide/26-04-SUMMARY.md` when done
</output>
