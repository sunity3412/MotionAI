---
phase: 26-onboarding-upload-guide
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/BodyProfileForm.tsx
  - app/src/lib/bodyProfile.ts
  - app/src/app/(tabs)/profile.tsx
  - app/src/app/(tabs)/index.tsx
autonomous: true
requirements: [ONBD-04]
must_haves:
  truths:
    - "통증 부위 선택지 마지막에 '기타' + 자유입력이 있다 (F3)"
    - "기타 통증 메모가 저장·재진입 시 복원되고, BodyProfile 계약 타입/normalize 는 변하지 않는다 (앱-로컬 필드 — 계약 무접촉)"
    - "홈 공지 배너와 인접 요소 간격이 최소 12 이상이고 배너 카피에서 불필요 문장이 제거된다 (F4)"
    - "backend/ 와 docs/contract.md 는 무접촉이다"
  artifacts:
    - path: "app/src/lib/bodyProfile.ts"
      provides: "savePainAreaNote helper + useBodyProfile painAreaNote 반환 (promptDismissedAt 선례)"
      contains: "painAreaNote"
    - path: "app/src/components/BodyProfileForm.tsx"
      provides: "기타 chip + 자유입력 필드 (design.md §5-3-1 입력 패턴)"
      contains: "기타"
  key_links:
    - from: "app/src/components/BodyProfileForm.tsx"
      to: "app/src/lib/bodyProfile.ts"
      via: "savePainAreaNote 저장 (dirty-guard)"
      pattern: "savePainAreaNote"
---

<objective>
**잡 UI 작업 (파일럿 피드백 F3/F4 — 시나리오 여정 보조).** (F3) 통증 부위 입력에 `기타` + 자유입력을 추가하고, (F4) 홈 공지 배너 간격/카피를 정리한다.

Purpose: 실증 피드백 F3("설문/입력에 기타 자유입력") — 문의 유형은 이미 `기타`+자유입력 완비(inquiry.tsx)이므로 잔여 대상은 통증 부위(BodyProfileForm, 닫힌 8개 enum). F4 — 공지 배너 밀집/불필요 문장.
Output: BodyProfileForm 기타 입력 + bodyProfile.ts helper + profile.tsx prefill + (tabs)/index.tsx 배너 정리.

설계 결정 (플래너): 기타 메모는 **앱-로컬 필드 `painAreaNote`** 로 저장 — `promptDismissedAt` 선례(bodyProfile map 안에 살지만 BodyProfile 계약 타입/normalize 밖, raw 에서 별도 read). PainArea enum/BodyProfile 계약을 건드리지 않아 3-way lockstep·백엔드 exercise_map 소비 경로가 무접촉이다 (phase 계약 예외는 26-03 의 learningOptIn 하나로 유지).

배포 노트: JS-only. **OTA 배포 가능.**
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-onboarding-upload-guide/26-CONTEXT.md
@.planning/phases/26-onboarding-upload-guide/26-UI-SPEC.md
@.planning/phases/26-onboarding-upload-guide/26-PATTERNS.md
@app/src/lib/bodyProfile.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: F3 — 통증 부위 '기타' chip + 자유입력 (앱-로컬 painAreaNote)</name>
  <files>app/src/components/BodyProfileForm.tsx, app/src/lib/bodyProfile.ts, app/src/app/(tabs)/profile.tsx</files>
  <read_first>
    - app/src/lib/bodyProfile.ts:109-210 (useBodyProfile 의 promptDismissedAt raw 별도 read 선례 R2 + saveBodyProfile WR-01 deep-merge 주의 + dismissBodyProfilePrompt merge-write 선례)
    - app/src/components/BodyProfileForm.tsx:60-62, 237-260, 412-423 (PAIN_AREA_OPTIONS chip 렌더 + chipSelected 스타일)
    - app/src/app/inquiry.tsx:311-326 (자유입력 필드 스타일 선례 — design.md §5-3-1)
    - app/src/app/(tabs)/profile.tsx:45-49, 153 (요약 라인 parts + BodyProfileForm 호출부)
    - .planning/phases/26-onboarding-upload-guide/26-UI-SPEC.md §S7(F3)
  </read_first>
  <action>
    (F3 구현. **BodyProfile 계약 타입 / normalizeBodyProfile / PainArea enum / analysis.ts / models.py / contract.md 전부 무접촉** — painAreaNote 는 promptDismissedAt 와 같은 앱-로컬 필드.)

    1. `app/src/lib/bodyProfile.ts`:
       - `export async function savePainAreaNote(note: string | null): Promise<void>` — dismissBodyProfilePrompt 패턴 그대로: `setDoc(doc(db,'users',uid), { bodyProfile: { painAreaNote: note } }, { merge: true })`. null = 비움 (WR-01: merge 는 키 명시로만 덮어씀 — null 명시 기록).
       - `useBodyProfile` 반환에 `painAreaNote: string | null` 추가 — promptDismissedAt 처럼 raw 에서 별도 read (`typeof raw.painAreaNote === 'string' && raw.painAreaNote.trim() !== ''` 일 때만 값, 아니면 null). **normalizeBodyProfile 은 무변경** (계약 필드 아님 — 분석 snapshot 에 자동 배제됨을 주석으로 명시).
       - 헤더/필드 주석에 `F3 / 26-UI-SPEC S7 / promptDismissedAt(R2) 선례` 인용.
    2. `app/src/components/BodyProfileForm.tsx`:
       - Props 에 `initialPainAreaNote?: string | null` 추가 (옵셔널 — analyze.tsx 호출부는 무변경, prefill 없는 첫-입력 맥락).
       - 통증 부위 chip 그룹 마지막에 `기타` chip 추가 — 기존 chip/chipSelected 스타일 재사용, 선택 상태 = 로컬 boolean `etcSelected` (초기값 = initialPainAreaNote 존재 여부). `accessibilityState={{ selected }}` 등 기존 chip a11y 동일.
       - `etcSelected` 일 때 TextInput 노출: placeholder `직접 입력해 주세요` + `placeholderTextColor={colors.textDisabled}`(UI-SPEC S7 — inquiry 의 textSecondary 아님), 보더 `colors.inputBorder`(포커스 시 `colors.brand`), `radius.button`, 높이 `layout.inputHeight`(54) 단일행. state `noteStr` 초기값 = initialPainAreaNote ?? ''.
       - 저장 시 **dirty-guard**: `noteStr.trim()`(기타 해제 시 '' 취급) 이 `initialPainAreaNote ?? ''` 와 다를 때만 `savePainAreaNote(trimmed === '' ? null : trimmed)` 호출 — prefill 이 없는 analyze 맥락에서 기존 메모를 빈 값으로 덮어쓰는 사고 방지. saveBodyProfile 호출과 같은 저장 흐름(try/catch/finally busy)에 합류.
    3. `app/src/app/(tabs)/profile.tsx`:
       - useBodyProfile 의 painAreaNote 를 받아 `<BodyProfileForm initialPainAreaNote={painAreaNote} ...>` 로 전달 (마이페이지 = 수정 진입 prefill).
       - 요약 라인(:45-49): painAreaNote 존재 시 `parts.push(\`기타: ${painAreaNote}\`)` 추가 (통증 부위 라벨 join 뒤).
  </action>
  <verify>
    <automated>npm --prefix app run typecheck && grep -c "savePainAreaNote" app/src/lib/bodyProfile.ts && grep -c "savePainAreaNote" app/src/components/BodyProfileForm.tsx && grep -c "initialPainAreaNote" "app/src/app/(tabs)/profile.tsx" && ! grep -n "painAreaNote" app/src/types/analysis.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm --prefix app run typecheck` exits 0
    - `app/src/types/analysis.ts`, `backend/`, `docs/contract.md` diff 0 (계약 무접촉 증명)
    - normalizeBodyProfile 함수 본문 diff 0 (`git diff` 확인 — raw 별도 read 는 useBodyProfile 쪽만)
    - BodyProfileForm 에 `기타` chip + placeholder `직접 입력해 주세요` + `colors.textDisabled` 존재
    - dirty-guard 존재: 저장 함수에서 initialPainAreaNote 비교 조건 확인 (무조건 savePainAreaNote 호출 아님)
    - analyze.tsx diff 0 (호출부 무변경 — wave 1 파일 충돌 방지)
  </acceptance_criteria>
  <done>통증 부위에 기타+자유입력이 생기고 마이페이지 재진입 시 복원되며, BodyProfile 계약·백엔드 소비 경로는 1바이트도 변하지 않는다.</done>
</task>

<task type="auto">
  <name>Task 2: F4 — 홈 공지 배너 간격 + 불필요 문장 제거</name>
  <files>app/src/app/(tabs)/index.tsx</files>
  <read_first>
    - app/src/app/(tabs)/index.tsx:83-119 (newsBanner 렌더 + 카피) + :315 (TOP_AREA_HEIGHT 240 — 간격 변경 시 연동 확인) + :337-356 (newsBanner 스타일, marginTop 16)
    - .planning/phases/26-onboarding-upload-guide/26-UI-SPEC.md §S7(F4) + §Spacing (인접 최소 간격 12)
  </read_first>
  <action>
    (F4 구현 — 잡 UI, 카피 세부 재량)

    1. **간격:** newsBanner 와 인접 요소(위 헤더/아래 콘텐츠) 간격을 각각 최소 12 확보 — 현재 marginTop 16 은 유지, 배너 **아래쪽** 간격이 12 미만이면 다음 요소와의 간격을 12 이상으로 조정. 간격 변경이 브랜드 그라디언트 상단 영역 높이에 영향을 주면 TOP_AREA_HEIGHT(:315) 를 함께 보정 (레이아웃 깨짐 방지 — 주석의 연동 지시).
    2. **카피:** `{newest.name} 기준모션이 추가되었어요.` 에서 불필요 어절 축약 — 예: `{newest.name} 기준모션 추가` (한 줄 numberOfLines={1} 내 이름 잘림 최소화가 목적). "~해요" 체 유지 여부는 배너 길이 실측으로 판단 (재량 — 긴 모션명에서 잘리지 않는 쪽 우선).
    3. 배너 자체 비주얼(brand pill, NEW 배지, rgba 배경) **불변** — 간격/카피만.
  </action>
  <verify>
    <automated>npm --prefix app run typecheck && grep -c "newsBanner" "app/src/app/(tabs)/index.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `npm --prefix app run typecheck` exits 0
    - newsBanner 스타일에서 인접 간격 값 >= 12 (스타일 diff 검토)
    - 배너 렌더 구조(NEW 배지 + numberOfLines={1}) 불변 — 카피 문자열만 단축
    - `기준모션이 추가되었어요` 원문 카피가 더 짧은 문구로 교체됨
  </acceptance_criteria>
  <done>공지 배너가 인접 요소와 12 이상 간격을 갖고 카피가 간결해지며, 배너 비주얼은 그대로다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 앱 → Firestore (users/{uid}.bodyProfile.painAreaNote) | 자유입력 텍스트 — 본인 문서 한정 (기존 rules) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-12 | Tampering | painAreaNote 자유 텍스트 주입 | accept | 앱-로컬 표시 전용 필드 — 백엔드/채점/LLM 프롬프트에 미소비 (normalizeBodyProfile 밖, 분석 snapshot 자동 배제). RN Text 렌더는 injection-safe |
| T-26-13 | Information Disclosure | 통증 메모 = 민감 건강 정보 | mitigate | 저장 위치 = 기존 bodyProfile(painAreas 와 동일 민감도·동일 rules 보호) 안 — 새 노출 경로 0, 화면 표시도 본인 마이페이지뿐 |
| T-26-14 | Tampering | dirty-guard 부재 시 메모 소실 | mitigate | initialPainAreaNote 비교 dirty-guard — prefill 없는 호출부에서 덮어쓰기 불가 (Task 1 acceptance) |
| T-26-SC | Tampering | npm/pip 설치 | n/a | 패키지 설치 0 — Package Legitimacy Gate 해당 없음 |

백엔드 무접촉 — 서버측 위협 스코프 밖.
</threat_model>

<verification>
- `npm --prefix app run typecheck` GREEN
- backend/, docs/contract.md, app/src/types/analysis.ts diff 0 — 계약·채점 무접촉
</verification>

<success_criteria>
- F3: 통증 부위 기타+자유입력, 저장/복원 동작, 계약 무접촉
- F4: 공지 간격 >= 12 + 카피 단축, 배너 비주얼 불변
- JS-only (OTA 가능)
</success_criteria>

<output>
Create `.planning/phases/26-onboarding-upload-guide/26-05-SUMMARY.md` when done
</output>
