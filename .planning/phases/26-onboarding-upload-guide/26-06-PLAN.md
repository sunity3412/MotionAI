---
phase: 26-onboarding-upload-guide
plan: 06
type: execute
wave: 3
depends_on: ["26-01", "26-02", "26-03", "26-04", "26-05"]
files_modified:
  - app/src/app/(tabs)/analyze.tsx
  - app/src/app/(tabs)/index.tsx
  - app/src/app/(tabs)/_layout.tsx
  - app/src/app/help.tsx
autonomous: false
requirements: [ONBD-01]
must_haves:
  truths:
    - "UI 화면 순서 재배치안이 목업 선제시(최악 데이터 케이스 포함)로 belle 에게 제시되고, belle 확정 후에만 구현된다 (CONTEXT Claude's Discretion)"
    - "belle 이 phase 26 전체 산출(튜토리얼/FAQ/카톡 경고/opt-in/거리 안내/not_pole 문구)을 실기기 또는 목업으로 확인한다"
    - "opt-in 기본 off(미체크→learningOptIn:false / 체크→true)와 talkv→lowQuality 우선순위가 기록된 증거(확인 transcript + Firestore 조회 결과)로 26-06-SUMMARY 에 남는다 (리뷰 MEDIUM-2)"
    - "belle 이 거부한 재배치안은 구현되지 않는다"
  artifacts:
    - path: ".planning/phases/26-onboarding-upload-guide/26-06-SUMMARY.md"
      provides: "belle 결정 기록 (재배치 채택안/거부안 + 실기기 확인 결과) + 확인 transcript + Firestore 동의값 증거 (analysisId·learningOptIn 값 — 리뷰 MEDIUM-2)"
  key_links:
    - from: "재배치 결정"
      to: "구현 파일"
      via: "belle 승인 범위 내 파일만 수정"
      pattern: "승인"
---

<objective>
**시나리오 0단계 마감 작업 (belle 요청 — UI 화면 순서 재배치 제안 + phase 산출 확인).** 재배치안을 목업으로 선제시(최악 데이터 케이스 포함)하고 belle 확정 후 구현하며, phase 26 전체 산출을 belle 이 실기기로 확인한다. 앱에 JS 테스트 하니스가 없어(정적 게이트 = typecheck 뿐) opt-in/talkv 우선순위의 **행위 증거는 이 플랜의 기록이 담당** — 확인 transcript 와 Firestore 증거를 SUMMARY 에 남긴다 (리뷰 MEDIUM-2 폴백).

Purpose: SCENARIO.md 0단계 비고 "gap 착수 시 화면 순서 재배치 제안 포함 (belle 요청)" + 기존 원칙(UI 변경 = 목업 선제시). belle 이 직접 보고 결정하고 싶어하는 항목 — 자율 확정 금지.
Output: belle 확정 재배치 구현(승인 범위 내) + 실기기 확인 기록(증거 포함).

배포 노트: 구현분은 JS-only 유지 (native 모듈 금지). **OTA 배포 가능** 범위 내에서만 재배치.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-onboarding-upload-guide/26-CONTEXT.md
@.planning/phases/26-onboarding-upload-guide/26-UI-SPEC.md
@.planning/SCENARIO.md
@.planning/phases/26-onboarding-upload-guide/26-01-SUMMARY.md
@.planning/phases/26-onboarding-upload-guide/26-02-SUMMARY.md
@.planning/phases/26-onboarding-upload-guide/26-03-SUMMARY.md
@.planning/phases/26-onboarding-upload-guide/26-04-SUMMARY.md
@.planning/phases/26-onboarding-upload-guide/26-05-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: UI 화면 순서 재배치안 목업 선제시 (belle 확정)</name>
  <read_first>
    - .planning/SCENARIO.md (단계 0/0.5/1/1.5 흐름 — 재배치안의 정합 근거)
    - .planning/phases/26-onboarding-upload-guide/26-01-SUMMARY.md ~ 26-05-SUMMARY.md (wave 1-2 산출 현황)
    - app/src/app/(tabs)/analyze.tsx, app/src/app/(tabs)/index.tsx, app/src/app/help.tsx (재배치 대상 현재 상태)
  </read_first>
  <decision>분석 이전 여정(홈 → 분석 탭 → 업로드)의 화면/요소 순서 재배치 — 온보딩·FAQ·프라이버시/동의·거리 안내가 여정 어디에 놓일지</decision>
  <context>
    Phase 26 이 요소를 여럿 추가해(튜토리얼, FAQ 진입점, 프라이버시 1줄, opt-in 체크, 거리 안내 2종) 분석 탭 밀도가 올라갔다. belle 은 재배치를 직접 보고 결정하길 원한다 (SCENARIO 0단계 비고 + CONTEXT Claude's Discretion). 실행자는 완성된 wave 1-2 산출을 기준으로 안을 만든다.
  </context>
  <action>
    선제시 절차 (belle 기존 지시 — 목업 먼저):
    1. wave 1-2 산출이 반영된 현행 화면 구조를 파악한다 (read_first 파일).
    2. AskUserQuestion preview 로 "현행 유지 + 재배치안 A/B" 를 목업으로 제시 — 텍스트 와이어프레임 + 가능하면 스크린샷. **최악 데이터 케이스 포함 필수**: 긴 모션명 공지 배너, 에러 문구 + guidance 3종 + opt-in 행이 모두 떠 있는 분석 탭, FAQ 전체 펼침.
    3. 각 안에 SCENARIO 단계 흐름(0 → 0.5 → 1 → 1.5) 과의 정합 근거를 표기한다.
    4. belle 의 선택/수정 지시를 기록하고 Task 2 의 구현 범위로 고정한다.
  </action>
  <options>
    <option id="keep-current">
      <name>현행 유지 (26-01~05 배치 그대로)</name>
      <pros>회귀 위험 0, 즉시 마감</pros>
      <cons>분석 탭에 안내 요소 밀집 — 가독성 저하 가능</cons>
    </option>
    <option id="proposal-a">
      <name>재배치안 A (실행 시 구체화 — 예: FAQ 를 마이페이지로, 안내 캡션 통합)</name>
      <pros>여정 단계별 밀도 분산</pros>
      <cons>파일 추가 수정 필요</cons>
    </option>
    <option id="proposal-b">
      <name>재배치안 B (실행 시 구체화 — 예: 홈에 온보딩 재진입 카드)</name>
      <pros>0단계 상시 노출 강화</pros>
      <cons>홈 레이아웃 변경 폭 큼</cons>
    </option>
  </options>
  <verify>belle 의 명시적 선택이 대화에 기록됨 (안 ID 또는 수정 지시)</verify>
  <done>재배치안이 목업(최악 케이스 포함)으로 제시되고 belle 이 하나를 확정했다 — "현행 유지" 도 유효한 결정.</done>
  <resume-signal>belle 이 안 하나를 선택하거나 수정 지시 — "현행 유지" 도 유효한 결정</resume-signal>
</task>

<task type="auto">
  <name>Task 2: 확정된 재배치안 구현</name>
  <files>app/src/app/(tabs)/analyze.tsx, app/src/app/(tabs)/index.tsx, app/src/app/(tabs)/_layout.tsx, app/src/app/help.tsx</files>
  <read_first>
    - Task 1 의 belle 확정안 (checkpoint 결과)
    - .planning/phases/26-onboarding-upload-guide/26-UI-SPEC.md (토큰/간격/타이포 계약 — 재배치에도 동일 적용)
    - 확정안이 건드리는 각 파일의 현재 상태 (wave 1-2 산출 반영본)
  </read_first>
  <action>
    belle 확정안을 **승인 범위 내 파일만** 수정해 구현한다. "현행 유지" 선택 시 이 태스크는 no-op (SUMMARY 에 결정만 기록). 규칙:
    - 승인되지 않은 요소/화면은 건드리지 않는다 (재배치 편승 리팩토링 금지)
    - D-01~D-09 결과물의 동작 로직(게이트 체인, 동의 기록, 첫 실행 플래그)은 재배치로 변형 금지 — 위치/순서만 이동
    - 테마 토큰·a11y·"~해요" 카피 컨벤션 유지, 이모지·인라인 hex 금지
    - files_modified 목록 밖 파일이 필요해지면 중단하고 belle 에게 범위 확인 (checkpoint 재개)
  </action>
  <verify>
    <automated>npm --prefix app run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npm --prefix app run typecheck` exits 0
    - 수정 파일이 belle 승인 범위와 일치 (`git status` 대조)
    - 26-01~05 의 must_haves truths 가 재배치 후에도 성립 (게이트 체인/동의 기록/첫 실행 플래그 grep 재확인: `_talkv_`, `learningOptIn`, `hasSeenTutorial` 존재)
  </acceptance_criteria>
  <done>belle 확정안이 그대로 구현되고 phase 26 기존 산출의 동작이 보존된다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: phase 26 전체 산출 실기기 확인 (belle) + 증거 기록</name>
  <read_first>
    - .planning/phases/26-onboarding-upload-guide/26-01-SUMMARY.md ~ 26-05-SUMMARY.md (확인 대상 산출 목록)
  </read_first>
  <what-built>온보딩 튜토리얼(Figma 디자인), 이용방법/FAQ, 프라이버시 1줄 + 학습활용 opt-in, 촬영 거리 안내, 카톡 압축본 경고 다이얼로그, not_pole 구도 안내, F3 기타 입력, F4 공지 정리, (선택 시) 화면 순서 재배치</what-built>
  <action>
    Claude 가 확인 환경을 준비한다 (belle 는 확인만 — 콘솔/CLI 떠넘기기 금지): Expo 개발 서버 기동 또는 EAS/TestFlight 빌드 준비, Firestore 동의값 조회 대행. 이후 아래 how-to-verify 절차를 belle 에게 안내하고 결과를 기다린다.

    **증거 기록 의무 (리뷰 MEDIUM-2 — 앱에 테스트 하니스가 없어 grep 을 못 넘는 행위 증거는 여기서 확보):** 확인 진행 중 Claude 가 아래를 수집해 `26-06-SUMMARY.md` 에 기록한다:
    - 시뮬레이터/실기기 확인 transcript (항목별 수행 스텝 + 관찰 결과 — 특히 4·5·6·7번)
    - Firestore 증거: 7번 항목의 두 업로드 각각에 대해 **analysisId + learningOptIn 값** (스크린샷 또는 콘솔 조회 결과 원문 — Claude 가 조회 대행)
    - 증거의 핵심 대상 2가지: (a) **opt-in 기본 off** — 미체크 업로드 문서가 `learningOptIn: false`, 체크 업로드 문서가 `learningOptIn: true`, (b) **talkv→lowQuality 우선순위** — `_talkv_` 영상 진행 후 not_pole 실패 시 화질 우선 문구가 구도 문구보다 우선 노출
  </action>
  <how-to-verify>
    1. 앱 데이터 초기화(재설치) 후 첫 실행 → 튜토리얼 1회 노출 확인, 건너뛰기/시작하기 동작, 재실행 시 미노출 (D-03)
    2. 튜토리얼 비주얼이 Figma 승인 디자인과 정합한지 (D-04)
    3. 분석 탭 → `이용 방법 · FAQ` 링크 → FAQ 6항목 + `튜토리얼 다시 보기` 재진입 (F2/D-05)
    4. 분석 탭 소스 선택 화면에서 프라이버시 1줄 + opt-in 체크(기본 off) + 거리 안내 노출 (D-08/D-09/D-01-i)
    5. 카톡에서 전달받은 실영상(`_talkv_` 파일명) pick → 경고 다이얼로그(Figma 패턴) → `이대로 계속` 진행 허용 확인 (D-06)
    6. (가능하면) 압축본 진행 → not_pole 실패 시 "화질이 낮아…" 우선 문구 / 원본 구도 불일치 실패 시 구도 안내 문구 (D-07/D-01-ii) — 결과를 transcript 로 기록
    7. **opt-in 양방향 확인 (증거 기록 대상):** (a) opt-in 미체크 업로드 → Firestore 해당 분석 문서 `learningOptIn: false` 확인, (b) opt-in 체크 업로드 → `learningOptIn: true` 확인 — 두 건 모두 Claude 가 조회 대행, analysisId + 값이 SUMMARY 증거로 기록됨
    8. 마이페이지 신체 프로필 → 통증 부위 `기타` 입력/저장/복원 (F3), 홈 공지 배너 간격/카피 (F4)
  </how-to-verify>
  <verify>belle 의 "approved" 또는 항목별 이슈 목록이 기록됨 + 확인 transcript 와 Firestore 증거(analysisId·learningOptIn 값)가 26-06-SUMMARY.md 에 기재됨</verify>
  <done>belle 이 8개 항목을 확인하고 approved 했거나 발견 이슈가 이 phase 내에서 수정 완료됐고, opt-in 기본 off + talkv→lowQuality 우선순위의 증거(transcript + Firestore 조회 결과)가 26-06-SUMMARY.md 에 기록됐다.</done>
  <resume-signal>"approved" 또는 항목별 이슈 기술 — 이슈는 이 phase 내 즉시 수정 (미완 이월 금지 원칙)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 재배치 구현 | 기존 보안-관련 동작(동의 기록, 게이트 체인)의 위치 이동 — 로직 변형 시 회귀 위험 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-15 | Tampering | 재배치 중 동의/게이트 로직 회귀 | mitigate | Task 2 acceptance 의 grep 재확인(_talkv_/learningOptIn/hasSeenTutorial) + Task 3 실기기 7번 항목(Firestore 동의값 양방향 확인 — 증거 기록, 리뷰 MEDIUM-2) |
| T-26-16 | Elevation of Privilege | 승인 범위 밖 변경 | mitigate | files_modified 밖 필요 시 중단·재확인 규칙 (Task 2 action) + git status 대조 |
| T-26-SC | Tampering | npm/pip 설치 | n/a | 패키지 설치 0 — Package Legitimacy Gate 해당 없음 |

백엔드 무접촉 — 서버측 위협 스코프 밖.
</threat_model>

<verification>
- belle 결정 2회(재배치 확정 + 최종 approved)가 SUMMARY 에 기록
- 확인 transcript + Firestore 증거(analysisId·learningOptIn 값, 미체크→false / 체크→true)가 SUMMARY 에 기록 (리뷰 MEDIUM-2)
- `npm --prefix app run typecheck` GREEN
- backend/ diff 0 (phase 전체 불변식)
- 라우트 이관 최종 봉인 (26-02 결정, 리뷰 MEDIUM-1): `grep -rn "analysis/samples" app/src docs` 매치 0 — `.planning` 이력 문서 매치는 의도적(검증 대상 아님)
</verification>

<success_criteria>
- 재배치는 목업 선제시 → belle 확정 → 구현 순서로만 진행 (자율 확정 0)
- phase 26 전체 산출이 belle 실기기 확인을 통과하고, opt-in 기본 off + talkv→lowQuality 우선순위 증거가 SUMMARY 에 남음
- 발견 이슈는 phase 내 수정 완료 (이월 0)
</success_criteria>

<output>
Create `.planning/phases/26-onboarding-upload-guide/26-06-SUMMARY.md` when done
</output>
