---
phase: 27-1-gemini-analysis-speed-1min
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/functions/pipeline/app.py
  - backend/tests/test_stage_timing.py
  - backend/tests/gemini/fake_genai.py
  - app/src/types/analysis.ts
  - docs/contract.md
autonomous: true
requirements: [SPD-01]
tags: [performance, instrumentation, gemini, pipeline]

must_haves:
  truths:
    - "mode1 분석 1회 실행 시 단계별 stage_timing 로그가 key=value 형식으로 방출된다"
    - "완료된 분석 doc의 result.timingsMs에 단계별 소요(ms)가 flat dict로 저장된다"
    - "timingsMs 계약이 analysis.ts + contract.md에 'backend/audit 전용, 사용자 비노출'로 lockstep 정의된다 — 암묵 result 필드 금지 (외부 리뷰 MEDIUM-1)"
    - "후속 wave의 병렬화/세션 테스트가 재사용할 fake genai fixture(업로드/삭제 카운터)가 존재한다"
  artifacts:
    - path: "backend/functions/pipeline/app.py"
      provides: "_stage 타이머 helper + _process 단계 경계 계측 + result.timingsMs 부착"
      contains: "stage_timing"
    - path: "app/src/types/analysis.ts"
      provides: "AnalysisResult.timingsMs?: Record<string, number> — backend/audit 전용 optional 필드 (3-way lockstep)"
      contains: "timingsMs"
    - path: "backend/tests/test_stage_timing.py"
      provides: "caplog 로그 라인 assert + timingsMs flat 검증"
      min_lines: 40
    - path: "backend/tests/gemini/fake_genai.py"
      provides: "FakeFiles(upload/get/delete 카운터) + FakeModels + patch_genai"
      exports: ["FakeFiles", "FakeModels", "patch_genai"]
  key_links:
    - from: "backend/functions/pipeline/app.py"
      to: "firestore_admin.complete_analysis"
      via: "result['timingsMs'] flat dict[str,int] (nested-array 금지 정합)"
      pattern: "timingsMs"
    - from: "app/src/types/analysis.ts"
      to: "docs/contract.md"
      via: "timingsMs 계약 lockstep 주석 상호 인용 (Phase 26 learningOptIn 선례)"
      pattern: "timingsMs"
    - from: "backend/tests/gemini/fake_genai.py"
      to: "backend/tests/gemini/test_client.py"
      via: "_FakeFiles/_FakeModels/_patch_genai 패턴 복제 + delete 카운터 확장"
      pattern: "delete_calls"
---

<objective>
Phase 27 Wave 0(계측-먼저): `_process`에 단계별 stage-timing 계측을 넣고 `result.timingsMs`로 저장(계약 lockstep 포함), 후속 wave 전체가 의존하는 fake genai fixture를 만든다. (시나리오 2 — 대기 경험. D-01 무회귀 게이트의 before/after 증명과 D-02 진행률 재배분의 실측 기반.)

현재 단계별 타이밍 로그가 없어(veto telemetry.durationMs, coach _meta.latency_ms만 존재) 152s vs 197s의 ~45s 미계상 구간이 있고(27-RESEARCH.md §152s vs 197s), 계측 없이는 레버 효과를 증명할 수 없다. 이 plan이 모든 최적화 wave의 전제다.

Purpose: D-01(정확도 무회귀 hard gate)의 before/after stage-timing 표 + D-02(대기 체감) 진행률 재배분의 실측 기반 확보.
Output: 계측 코드 + 계약 lockstep(analysis.ts/contract.md) + 단위 테스트 + fake genai fixture. OTA 무관(타입/문서만 — 앱 소비 0).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-1-gemini-analysis-speed-1min/27-CONTEXT.md
@.planning/phases/27-1-gemini-analysis-speed-1min/27-RESEARCH.md
@.planning/phases/27-1-gemini-analysis-speed-1min/27-PATTERNS.md
@.planning/phases/27-1-gemini-analysis-speed-1min/27-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _process 단계별 stage-timing 계측 + result.timingsMs flat 저장 + 계약 lockstep</name>
  <files>backend/functions/pipeline/app.py, backend/tests/test_stage_timing.py, app/src/types/analysis.ts, docs/contract.md</files>
  <read_first>
    - backend/functions/pipeline/app.py — `_process` 전체 골격(단계 경계 확인: 3088-3093 status write, 3111-3123 `_extract_video_analysis_inputs`, 3132-3135 scene_finder, 3534-3539 coach, 3995 zoom 부착, 4131-4153 complete_analysis, 4154 구조 로그, 4155-4166 outer finally)
    - app/src/types/analysis.ts:515-560 — `AnalysisResult` optional 필드 선례 (`visionVeto?`, `deductionBreakdown?` — "OPTIONAL, legacy doc 호환" 주석 스타일)
    - .planning/phases/27-1-gemini-analysis-speed-1min/27-PATTERNS.md §`backend/functions/pipeline/app.py` (a) stage-timing 계측 — 구조 로그/latency try-finally/telemetry dict 선례 3종
    - .planning/phases/27-1-gemini-analysis-speed-1min/27-RESEARCH.md §Pattern 6 (측정 단계 목록 + cold/warm 구분 요구)
  </read_first>
  <behavior>
    - Test 1: `_stage` contextmanager가 timings dict에 int ms를 누적하고 `stage_timing analysis_id=%s stage=%s elapsed_ms=%d` 로그 라인을 방출한다 (caplog assert, %-lazy 포맷)
    - Test 2: 예외가 stage 블록 안에서 발생해도 elapsed가 기록된 뒤 예외가 전파된다 (try/finally)
    - Test 3: timings dict는 `dict[str, int]` flat — `firestore_admin._validate_dict_only_scalars`에 통과한다 (nested-array/비scalar 없음)
  </behavior>
  <action>
    1. `backend/functions/pipeline/app.py`에 module-level 순수 helper 추가 (unit test 가능하도록 boto3/네트워크 무의존):
       - `@contextlib.contextmanager def _stage(timings: dict[str, int], analysis_id: str, name: str):` — `time.monotonic()` 시작, finally에서 `timings[name] = int((time.monotonic() - t0) * 1000)` + `log.info("stage_timing analysis_id=%s stage=%s elapsed_ms=%d", analysis_id, name, timings[name])`. 구조 로그 규율(PATTERNS Shared Pattern 3): %-lazy, f-string 금지, 시크릿/바이트 로그 금지.
    2. `_process` 초입에 `timings_ms: dict[str, int] = {}` 선언 후 다음 단계 경계를 `with _stage(...)` 로 감싼다 (27-RESEARCH Pattern 6 최소 목록): `s3_download`, `frame_extract`, `rtmw`, `ref_fetch_download`, `dtw_scoring`, `scene_finder`, `recognizer`, `veto_collect`, `coach_dual`, `assemble_misc`, `fault_zoom`, `firestore_complete`. 기존 로직 이동 금지 — 감싸기만. 후속 wave에서 prefetch로 이동하는 단계(scene_finder/recognizer)도 지금은 현 위치에서 감싼다 (wave 3~5가 위치만 옮김).
    3. complete_analysis 호출 직전(app.py:4131 부근)에 `result["timingsMs"] = timings_ms` 부착. `firestore_complete` 단계는 complete_analysis 호출 자체를 감싸므로 timingsMs에 포함 불가 — 로그 라인으로만 방출 (dict에는 미포함, 주석으로 사유 박제).
    4. **계약 lockstep (외부 리뷰 MEDIUM-1 — 암묵 result 필드 금지):**
       - `app/src/types/analysis.ts` — `AnalysisResult`(515-560)에 `timingsMs?: Record<string, number>` optional 추가. 주석: "Phase 27 SPD-01 — 단계별 소요(ms). backend/audit 전용, 사용자 비노출(UI 소비 금지). Python lockstep: pipeline app.py `_stage` + contract.md timingsMs 절. OPTIONAL — legacy doc 호환(부재 = 계측 이전 doc)." (Phase 26 learningOptIn 3-way lockstep 선례.)
       - `docs/contract.md` — timingsMs 절 추가: flat `dict[str, int]`(nested 금지 — firestore-nested-array-flat 규율), backend/audit 전용·사용자 비노출 명기, 단계 키 목록은 예시(비고정 — 키 추가는 비파괴), 부재 = legacy doc.
       - Python 쪽 정본은 pipeline `_stage` 주석 — models.py 상수 추가 불필요 (status enum 아님, 자유 키 dict).
    5. 주석에 근거 인용: `# Phase 27 SPD-01 — stage-timing 계측 (27-RESEARCH Pattern 6). D-01 before/after 표의 데이터 소스. 계약 = analysis.ts timingsMs? + contract.md (backend/audit 전용).`
    6. `backend/tests/test_stage_timing.py` 작성 — behavior 3건. Test 3은 `from sunity_shared import firestore_admin` 후 `firestore_admin._validate_dict_only_scalars(timings, path="result.timingsMs")` 직접 호출 (파일 상단 PYTHONPATH 주석은 기존 테스트 관례 따름).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && PYTHONPATH=shared/python:. python3 -m pytest tests/test_stage_timing.py -q && cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && PYTHONPATH=shared/python:. python3 -m pytest tests/test_stage_timing.py -q` exit 0, 최소 3 passed
    - `cd app && npm run typecheck` exit 0
    - `rtk grep -n "stage_timing" backend/functions/pipeline/app.py` 매치 ≥ 1 (helper 로그 라인)
    - `rtk grep -c "_stage(timings_ms" backend/functions/pipeline/app.py` ≥ 10 (단계 경계 10곳 이상 계측)
    - `rtk grep -n "timingsMs" backend/functions/pipeline/app.py` 매치 ≥ 1 (result 부착)
    - `rtk grep -c "timingsMs" app/src/types/analysis.ts docs/contract.md` 각 ≥ 1 (계약 lockstep 존재)
  </acceptance_criteria>
  <done>mode1 분석 경로의 단계 경계 10곳 이상이 계측되고, timingsMs flat dict가 result에 부착되며 계약이 analysis.ts/contract.md에 lockstep 정의되고, 단위 테스트 3건 + app typecheck가 통과한다.</done>
</task>

<task type="auto">
  <name>Task 2: fake genai fixture 모듈 — 업로드/삭제 카운터 + 순서 결정론 지원</name>
  <files>backend/tests/gemini/fake_genai.py</files>
  <read_first>
    - backend/tests/gemini/test_client.py:38-100 — `_FakeFiles`/`_FakeFile`/`_FakeModels`/`_patch_genai` 정본 (upload_state/get_sequence, monkeypatch로 genai.Client 교체 + time.sleep 박제)
    - backend/tests/gemini/test_client.py:190 — `monkeypatch.setattr(client_mod.time, "monotonic", lambda: next(seq))` timeout fake 패턴
    - .planning/phases/27-1-gemini-analysis-speed-1min/27-PATTERNS.md §테스트 신규 파일들
  </read_first>
  <action>
    `backend/tests/gemini/fake_genai.py` 신규 작성 — test_client.py의 stub 패턴을 재사용 가능한 모듈로 확장 (test_client.py 본체는 수정하지 않음):
    - `FakeFile(name, state)` — `.name`(`files/fake-N` 형식), `.state` 속성.
    - `FakeFiles(upload_state="ACTIVE", get_sequence=None)` — `upload_calls`/`get_calls`/`delete_calls` 카운터 + `uploaded_names: list[str]`/`deleted_names: list[str]` 기록. `upload(*, file, config=None)` (config kwargs 허용 — client.py:210-219의 UploadFileConfig 경로 대응), `get(*, name)`, `delete(*, name)`.
    - `FakeModels(responses=None, delay_by_index=None)` — `generate_content(*, model, contents, config=None)` 가 호출 인덱스별 응답을 반환. `delay_by_index: dict[int, float]` 지정 시 해당 인덱스 호출에서 `time.sleep(delay)` — wave 5 fan-out 순서 결정론 테스트용(늦게 끝나는 future가 있어도 집계 순서가 call_plan 인덱스 순인지 검증). `calls: list[dict]` 로 호출 인자 기록.
    - `patch_genai(monkeypatch, target_module, *, files=None, models=None)` — `target_module.genai.Client`(또는 lazy import 대상)를 fake ctor로 교체 + `target_module.time.sleep` 박제. test_client.py `_patch_genai` 시그니처 관례 준수.
    - 모듈 docstring에 용도 명시: "Phase 27 SPD-01 — D-04 업로드/삭제 카운트 + D-03 fan-out 순서 결정론 검증용 공용 stub (test_client.py:38-100 패턴 확장)".
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && PYTHONPATH=shared/python:. python3 -c "from tests.gemini.fake_genai import FakeFiles, FakeModels, patch_genai; f=FakeFiles(); f.upload(file='x.mp4'); f.delete(name='files/fake-1'); assert f.upload_calls==1 and f.delete_calls==1"</automated>
  </verify>
  <acceptance_criteria>
    - 위 import+카운터 one-liner exit 0
    - `rtk grep -n "delete_calls" backend/tests/gemini/fake_genai.py` 매치 ≥ 1
    - `cd backend && PYTHONPATH=shared/python:. python3 -m pytest tests/gemini/ -q` 기존 테스트 신규 FAILED/ERROR 0 (test_client.py 무수정 확인)
  </acceptance_criteria>
  <done>후속 wave(27-03 세션 누수 0, 27-05 fan-out 결정론) 테스트가 import만으로 재사용할 수 있는 fake genai 모듈이 존재한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline → Firestore | result.timingsMs 신규 필드가 앱이 구독하는 doc에 기록됨 |
| 계측 코드 → 분석 로직 | 타이머가 분석 경로에 삽입됨 — 계측 실패가 분석을 깨면 안 됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Tampering | `_stage` helper / result dict | mitigate | timingsMs는 `dict[str,int]` flat만 — `_validate_dict_only_scalars` 통과를 unit test로 고정. 계측은 기존 로직 이동 0(감싸기만), try/finally로 예외 시에도 기록 후 전파. 계약은 analysis.ts/contract.md lockstep — 암묵 필드 0 |
| T-27-02 | Information Disclosure | stage_timing 로그 | mitigate | 로그에 analysis_id/stage/elapsed_ms만 — 시크릿/영상 바이트/URL 금지 (기존 never-log-secrets 규율). timingsMs는 사용자 비노출(contract.md 명기) |
| T-27-SC | Tampering | 패키지 설치 | accept | 신규 패키지 0 (stdlib contextlib/time만) — 공급망 표면 없음 |
</threat_model>

<verification>
- `cd backend && PYTHONPATH=shared/python:. python3 -m pytest tests/test_stage_timing.py tests/gemini/ -q` — 신규 FAILED/ERROR 0
- `cd app && npm run typecheck` exit 0
- `rtk grep -c "_stage(timings_ms" backend/functions/pipeline/app.py` ≥ 10
- timingsMs 계약 3곳 존재 (analysis.ts / contract.md / pipeline 주석)
</verification>

<success_criteria>
- stage_timing 로그 + result.timingsMs 계측이 mode1 경로에 배선되고 단위 테스트 green
- timingsMs가 암묵 필드가 아니라 analysis.ts+contract.md lockstep 계약으로 정의됨 (backend/audit 전용 명기)
- fake_genai.py fixture가 존재하고 카운터/순서 지원 동작
- 기존 backend 테스트 기준선 대비 신규 실패 0
</success_criteria>

<output>
Create `.planning/phases/27-1-gemini-analysis-speed-1min/27-01-SUMMARY.md` when done
</output>
