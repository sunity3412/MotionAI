---
phase: 27-1-gemini-analysis-speed-1min
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - .planning/phases/27-1-gemini-analysis-speed-1min/27-TIMING-BEFORE.md
autonomous: false
requirements: [SPD-01, SPD-06]
tags: [pod, eval, baseline, gemini]

must_haves:
  truths:
    - "최적화 코드가 하나도 들어가기 전(27-01 계측 커밋 시점)의 cold-run 단계별 타이밍이 실측으로 박제되어 있다"
    - "before 실측은 cold run이다 — telemetry.cacheHit=false 확인 기록이 있다"
    - "실측은 분석 간 SERIAL로 수행되었다 (동시 실행 0)"
  artifacts:
    - path: ".planning/phases/27-1-gemini-analysis-speed-1min/27-TIMING-BEFORE.md"
      provides: "before 타이밍 표 (페어별 stage_timing + 총 소요 + cacheHit 증빙 + Pod/커밋 해시)"
      min_lines: 30
  key_links:
    - from: "27-TIMING-BEFORE.md"
      to: "27-09 phase gate의 before/after 표"
      via: "동일 페어·동일 단계 키(timingsMs)로 대조"
      pattern: "stage_timing|timingsMs"
---

<objective>
[POD] 최적화 착수 전 cold baseline 실측 — 27-01 계측 커밋을 Pod에 pin하고 EVAL18 페어를 SERIAL cold로 돌려 "before" 단계별 타이밍 표를 박제한다. (시나리오 2 — 대기 경험. D-01 hard gate의 before 수치.)

이 plan 없이는 phase 종료 시 "얼마나 빨라졌는지" 증명이 불가능하다 (27-RESEARCH A4: 152s 배분은 로그 재구성 추정 — 실측이 레버 우선순위를 교정). 반드시 wave 3+(핸들 세션/병렬화) 코드가 Pod에 배포되기 전에 실행한다.

Purpose: D-01 무회귀 게이트(27-09)의 before 데이터 + 레버 우선순위 실측 교정.
Output: 27-TIMING-BEFORE.md. Pod 실작업 — belle 승인 + Gemini 크레딧 확인 선행 (autonomous: false).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-1-gemini-analysis-speed-1min/27-RESEARCH.md
@.planning/phases/27-1-gemini-analysis-speed-1min/27-VALIDATION.md
@backend/evals/phase25/run_sweep.py
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: belle 승인 — Pod 실측 착수 + Gemini 크레딧 확인</name>
  <action>checkpoint — 아래 how-to-verify 항목(크레딧/범위/절차)을 belle에게 제시하고 승인 응답을 대기한다. 승인 전 후속 태스크 착수 금지 (Pod 실작업 = belle 승인 선행 규율).</action>
  <done>belle 승인 수신 후에만 Task 2 진행 — resume-signal 수신.</done>
  <what-built>27-01 계측 코드가 로컬 테스트 green 상태. Pod에서 EVAL18 cold sweep을 돌려 before 타이밍을 실측할 준비 완료.</what-built>
  <how-to-verify>
    1. Gemini 크레딧 잔액 확인 (고갈 이력 2026-06-20 — sweep 전 필수. AI Studio 콘솔 sunity3412 계정)
    2. Pod svn31pzja7uay0 가동 상태 확인 (SSH root@213.173.102.233:12729 — 재생성됐다면 proxy URL/Lambda env 동기화 상태 먼저 보고)
    3. 실측 범위 승인: 최소 1페어(power-spin — 3m17s 실측의 원본), 크레딧 여유 시 6페어 전체 (cold 1회씩, SERIAL)
  </how-to-verify>
  <resume-signal>"approved" (필요 시 페어 수 지정: "approved 1페어" / "approved 6페어")</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Pod pin + EVAL18 cold SERIAL 실측 + 27-TIMING-BEFORE.md 박제</name>
  <files>.planning/phases/27-1-gemini-analysis-speed-1min/27-TIMING-BEFORE.md</files>
  <read_first>
    - backend/evals/phase25/run_sweep.py:79-93 — env setdefault 박제 + SERIAL sweep 관례 (EVAL_OUT_DIR 리포 밖)
    - backend/scripts/sweep_phase15.py — `--pair-sequential` 사용법
    - .planning/phases/27-1-gemini-analysis-speed-1min/27-RESEARCH.md §Pitfall 1 (warm-hit 오염 방지), §Pitfall 6 (Pod env는 git 밖)
  </read_first>
  <action>
    Pod 작업은 Claude가 SSH로 직접 실행한다 (pod-ops 규율 — belle에게 콘솔 떠넘기기 금지). 순서:
    1. 로컬 27-01 커밋을 push (GSD Pod 작업 = push 한 단위 규율). Pod SSH 접속 → 리포 pull → **27-01 커밋 해시에 pin** (이후 wave 코드 유입 차단). 커밋 해시를 기록.
    2. Pod 현행 env 스냅샷 채집 (27-RESEARCH Open Q2): `start_server.sh`의 RECOGNIZER_BACKEND / GEMINI_VISION_VETO_ENABLED / GEMINI_MAX_VETO_WALL_S / GEMINI_*_MODEL override 유무를 기록 — 인벤토리 "업로드 4~5회" 가정 검증.
    3. cold 보장: 대상 페어의 VisionVetoCache/TechniqueCache warm-hit를 격리한다. **PROMPT_VERSION bump 금지**(프로덕션 캐시 오염). 우선순위: (a) sweep 하니스에 캐시 무효화 옵션이 있으면 사용, (b) 없으면 Firestore의 해당 캐시 문서만 임시 삭제(대상 hash pair 한정), (c) in-memory 캐시는 서버 재시작으로 초기화. 실행 후 각 run의 `telemetry.cacheHit=false` 확인·기록.
    4. `sweep_phase15.py --pair-sequential`로 승인된 페어 수만큼 SERIAL cold 실행 (EVAL_OUT_DIR 리포 밖, 분석 간 동시 실행 절대 금지 — pipeline-not-concurrency-safe invariant). stage_timing 로그 라인 + result.timingsMs 수집.
    5. `.planning/phases/27-1-gemini-analysis-speed-1min/27-TIMING-BEFORE.md` 작성: 페어별 표 (stage × elapsed_ms), 총 소요(벽시계), cacheHit 증빙, Pod ID/GPU/커밋 해시, env 스냅샷, 152s/197s 추정 대비 실측 배분 코멘트 (미계상 ~45s의 정체 — 27-RESEARCH Open Q1 답). 점수·verdict도 함께 기록 (27-09 무회귀 대조의 참고 사본 — 정본 baseline은 evals/phase18 기존 것).
  </action>
  <verify>
    <automated>test -f /Users/kimtaesung/Dev/SunityMotion/.planning/phases/27-1-gemini-analysis-speed-1min/27-TIMING-BEFORE.md && rtk grep -c "cacheHit" /Users/kimtaesung/Dev/SunityMotion/.planning/phases/27-1-gemini-analysis-speed-1min/27-TIMING-BEFORE.md</automated>
  </verify>
  <acceptance_criteria>
    - 27-TIMING-BEFORE.md 존재, `rtk grep -c "cacheHit" ...27-TIMING-BEFORE.md` ≥ 1 (cold 증빙)
    - `rtk grep -c "elapsed_ms\|timingsMs" ...27-TIMING-BEFORE.md` ≥ 1 (단계별 수치 존재)
    - 파일에 Pod 커밋 해시 기록 (`rtk grep -c "commit" ...27-TIMING-BEFORE.md` ≥ 1)
  </acceptance_criteria>
  <done>최소 1페어의 cold before 타이밍 표가 커밋 해시·cacheHit 증빙과 함께 박제되어 27-09 게이트가 대조할 기준이 존재한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로컬 → Pod (SSH) | 프로덕션 분석 서버에서 실측 실행 |
| eval run → 프로덕션 캐시 | cold 격리 조작이 프로덕션 캐시를 오염시킬 수 있음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-03 | Tampering | VisionVetoCache/TechniqueCache | mitigate | PROMPT_VERSION bump 절대 금지 — 대상 hash pair 문서만 삭제/하니스 옵션 사용. 조작 내역을 27-TIMING-BEFORE.md에 기록 |
| T-27-04 | DoS | 분석 파이프라인 (동시성 비안전) | mitigate | SERIAL 강제 (`--pair-sequential`), 실측 중 실사용자 분석과 겹치지 않게 belle에게 시간대 고지 |
| T-27-05 | DoS | Gemini 크레딧 | mitigate | Task 1 checkpoint에서 잔액 확인 후만 실행 (고갈 이력) |
| T-27-SC | Tampering | 패키지 설치 | accept | 신규 패키지 0 |
</threat_model>

<verification>
- 27-TIMING-BEFORE.md에 cold 증빙(cacheHit=false) + 단계별 수치 + 커밋 해시 존재
- 실측 중 코드 변경 0 (Pod pin 해시 = 27-01 커밋)
</verification>

<success_criteria>
- before 타이밍 표 박제 완료 — 27-09가 after와 대조할 기준 확보
- 미계상 ~45s 구간의 실측 배분이 기록되어 wave 3~6 레버 기대치가 교정됨
</success_criteria>

<output>
Create `.planning/phases/27-1-gemini-analysis-speed-1min/27-02-SUMMARY.md` when done
</output>
