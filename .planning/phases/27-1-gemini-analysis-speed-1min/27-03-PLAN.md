---
phase: 27-1-gemini-analysis-speed-1min
plan: 03
type: execute
wave: 3
depends_on: ["27-01", "27-02"]
files_modified:
  - backend/shared/python/sunity_shared/gemini/file_session.py
  - backend/shared/python/sunity_shared/gemini/client.py
  - backend/tests/gemini/test_file_session.py
autonomous: true
requirements: [SPD-02]
tags: [gemini, file-api, session, performance]

must_haves:
  truths:
    - "GeminiFileSession이 같은 로컬 영상 경로에 대해 File API 업로드를 1회만 수행하고 핸들을 재사용한다"
    - "세션 close() 시 세션이 업로드한 모든 핸들이 일괄 delete된다 — 예외 경로 포함 누수 0"
    - "GeminiVisionCall.call()에 preuploaded handle을 주면 업로드/폴링/delete를 건너뛰고 generate만 수행한다"
    - "핸들 미주입 시 기존 동작이 byte-동일하다 (기본값 무변경 — RunPod server.py 무수정)"
  artifacts:
    - path: "backend/shared/python/sunity_shared/gemini/file_session.py"
      provides: "GeminiFileSession — get_or_upload(락 보호) + close 일괄 delete + context manager"
      exports: ["GeminiFileSession"]
      min_lines: 80
    - path: "backend/shared/python/sunity_shared/gemini/client.py"
      provides: "GeminiVisionCall.call(video_path, *, preuploaded_handle=None)"
      contains: "preuploaded_handle"
    - path: "backend/tests/gemini/test_file_session.py"
      provides: "업로드 1회/일괄 delete/예외 경로/핸들 주입 skip 테스트"
      min_lines: 60
  key_links:
    - from: "backend/shared/python/sunity_shared/gemini/file_session.py"
      to: "google.genai files API"
      via: "lazy import + 업로드/ACTIVE 폴링 (client.py 정본 포팅)"
      pattern: "files\\.upload|files\\.delete"
    - from: "backend/shared/python/sunity_shared/gemini/client.py"
      to: "file_session 핸들"
      via: "preuploaded_handle 주입 시 Step 2~3 + finally delete skip"
      pattern: "preuploaded_handle"
---

<objective>
GeminiFileSession 코어 — 학생/기준 영상을 분석당 1회만 File API에 업로드하고 핸들을 전 모듈이 공유, 종료 시 일괄 delete하는 세션 객체를 만든다 (D-04 허용 레버: 파일 핸들 재사용). (시나리오 2 — 대기 경험.)

현재 학생 영상이 Gemini File API에 최대 4~5회 중복 업로드된다(scene_finder + moment extractor + veto + coach B, 각 15~20s 라운드트립 — 27-RESEARCH 호출 인벤토리). 핸들(`files/...` name)은 서버측 리소스(48h TTL)라 어느 Client에서든 재사용 가능 — veto fan-out 내부가 이미 이 패턴을 증명(vision_scorer.py:96).

Purpose: 비전 구간의 지배 요인(중복 업로드+폴링) 소멸의 기반. 소비처 배선은 27-04.
Output: file_session.py + client.py 핸들 주입 + 누수 0 단위 테스트. OTA 무관(백엔드).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-1-gemini-analysis-speed-1min/27-PATTERNS.md
@.planning/phases/27-1-gemini-analysis-speed-1min/27-RESEARCH.md
@backend/shared/python/sunity_shared/gemini/client.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GeminiFileSession — get_or_upload + close 일괄 delete (누수 0)</name>
  <files>backend/shared/python/sunity_shared/gemini/file_session.py, backend/tests/gemini/test_file_session.py</files>
  <read_first>
    - backend/shared/python/sunity_shared/gemini/client.py:210-279 — 업로드 + UploadFileConfig TypeError stub 폴백 + ACTIVE 폴링 state machine (상수 56-58: `_FILES_PROCESSING_TIMEOUT_S=120.0`, `_FILES_POLL_INTERVAL_S=2.0`)
    - backend/shared/python/sunity_shared/gemini/client.py:221-242 — delete 규율 + 20GB 적체 근거 주석 (주석까지 복사할 것 — 27-PATTERNS 지시)
    - backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py:797-843 — `_ascii_safe_path`(한글 파일명) + `_upload_video`(예외-raise형 업로드 변형)
    - backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py:1283-1336 — 다중 핸들 일괄 finally delete (close()의 정본)
    - backend/tests/gemini/fake_genai.py — 27-01이 만든 fixture (FakeFiles 카운터)
  </read_first>
  <behavior>
    - Test 1 (D-04 핵심): 같은 경로로 get_or_upload 3회 호출 → FakeFiles.upload_calls == 1, 반환 핸들 name 동일
    - Test 2: 서로 다른 경로 2개 업로드 후 close() → delete_calls == 2, deleted_names == uploaded_names (누수 0)
    - Test 3: context manager 안에서 예외 발생 → __exit__이 delete를 수행하고 예외 전파 (누수 0 불변)
    - Test 4: 업로드가 PROCESSING 타임아웃/FAILED → get_or_upload가 None 반환 (graceful — 분석 비차단), close()는 성공 업로드분만 delete
    - Test 5: delete가 예외를 던져도 close()는 나머지 핸들 delete를 계속하고 raise하지 않는다 (best-effort + log.warning)
  </behavior>
  <action>
    `backend/shared/python/sunity_shared/gemini/file_session.py` 신규 작성 (D-04 "파일 핸들 재사용" 레버 구현):
    - 모듈 헤더: `from __future__ import annotations` + docstring에 목적·근거 인용 ("Phase 27 SPD-02 / D-04 — 분석당 업로드 1회 + 종료 일괄 delete. 20GB 적체 실증 2026-07-06 → delete 규율 불변, 27-RESEARCH Pattern 1").
    - `class GeminiFileSession:` — 생성자 `(client_factory=None)` (lazy: 실제 genai.Client 생성은 첫 업로드 시, 테스트는 factory 주입). 내부 상태: `_handles: dict[str, object]` (path → uploaded file 객체), `_lock: threading.Lock` (wave 5 prefetch 스레드 대비 — get_or_upload 전체를 락으로 보호, 같은 경로 이중 업로드 방지).
    - `get_or_upload(self, video_path: str, *, mime_type: str = "video/mp4") -> object | None` — 캐시 hit 시 즉시 반환. miss 시 업로드+ACTIVE 폴링: client.py:210-279 정본을 포팅 (UploadFileConfig TypeError stub 폴백 포함, 폴링 상수는 client.py의 `_FILES_PROCESSING_TIMEOUT_S`/`_FILES_POLL_INTERVAL_S`를 import 재사용 — 신규 상수 발명 금지), 한글 파일명은 vision_scorer `_ascii_safe_path` 로직 포팅. 타임아웃/FAILED/APIError → log.warning + None 반환 (graceful 규율, PATTERNS Shared Pattern 1). None은 캐시에 넣지 않음(재시도 여지).
    - `close(self) -> None` — vision_scorer.py:1283-1336 일괄 delete 정본 복제: 각 핸들 `client.files.delete(name=...)`, 실패는 `except Exception: # noqa: BLE001 - 정리 실패는 분석을 막지 않는다` + log.warning, 계속 진행. client.py:221-242의 20GB 적체 근거 주석을 그대로 이식.
    - `__enter__`/`__exit__` — __exit__에서 close() (예외 경로 포함).
    - lazy import: `from google import genai` 는 함수 안에서 (Lambda 250MB 규율 — vision_scorer.py:772 스타일).
    - 테스트는 behavior 5건 — `tests/gemini/fake_genai.py`의 FakeFiles/patch_genai 사용.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && PYTHONPATH=shared/python:. python3 -m pytest tests/gemini/test_file_session.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/gemini/test_file_session.py -q` exit 0, 최소 5 passed
    - `rtk grep -c "files.delete\|files\\.delete" backend/shared/python/sunity_shared/gemini/file_session.py` ≥ 1
    - `rtk grep -c "20GB\|RESOURCE_EXHAUSTED" backend/shared/python/sunity_shared/gemini/file_session.py` ≥ 1 (근거 주석 이식)
    - `rtk grep -c "threading" backend/shared/python/sunity_shared/gemini/file_session.py` ≥ 1 (락 보호)
  </acceptance_criteria>
  <done>업로드 1회·일괄 delete·예외 경로 누수 0이 fake client로 기계 증명된 세션 객체가 존재한다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GeminiVisionCall.call() preuploaded_handle 주입 — 업로드/delete skip</name>
  <files>backend/shared/python/sunity_shared/gemini/client.py, backend/tests/gemini/test_file_session.py</files>
  <read_first>
    - backend/shared/python/sunity_shared/gemini/client.py:167-242 — `call()` 현행: Step 2 업로드 → Step 3 폴링 → Step 4 generate → finally delete
    - backend/shared/python/sunity_shared/gemini/client.py:306-417 — `_generate_with_retry` (321: `contents=[active_file, self.prompt]` — 핸들 객체 그대로 수용, 무수정)
    - backend/tests/gemini/test_client.py — 기존 call() 테스트 (기본 경로 무변경 확인용)
  </read_first>
  <behavior>
    - Test 6: `call(video_path, preuploaded_handle=handle)` → FakeFiles.upload_calls == 0, delete_calls == 0, generate는 주입 핸들로 수행
    - Test 7: `call(video_path)` (핸들 미주입) → 기존과 동일하게 upload 1회 + finally delete 1회 (기본값 무변경 회귀 가드)
  </behavior>
  <action>
    `client.py` `GeminiVisionCall.call()` 시그니처를 `call(self, video_path, *, preuploaded_handle=None)` 로 확장 (keyword-only, default None — 기존 호출부/RunPod server.py 무수정 원칙, 27-RESEARCH Pattern 1):
    - `preuploaded_handle is not None` 이면 Step 2(업로드)~Step 3(폴링) skip, `uploaded = preuploaded_handle` 로 Step 4 generate 진입. **finally delete도 skip** — 소유권은 세션 (주석: "핸들 소유권 = GeminiFileSession — 세션 close()가 일괄 delete. 여기서 지우면 같은 분석의 후속 호출이 404").
    - 미주입 경로는 diff 0 (기존 업로드/폴링/delete 코드 블록 이동·재포맷 금지).
    - Test 6/7을 test_file_session.py에 추가 (fake_genai patch_genai로 client_mod 패치).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && PYTHONPATH=shared/python:. python3 -m pytest tests/gemini/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/gemini/ -q` exit 0 — 신규 2건 포함 전체 green, 기존 test_client.py 신규 실패 0
    - `rtk grep -c "preuploaded_handle" backend/shared/python/sunity_shared/gemini/client.py` ≥ 2 (시그니처 + 분기)
  </acceptance_criteria>
  <done>핸들 주입 시 업로드/폴링/delete가 skip되고, 미주입 시 기존 동작이 보존됨이 테스트로 고정된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pod → Gemini File API | 사용자 영상이 제3자 스토리지(48h TTL)에 업로드됨 |
| 세션 핸들 → 다중 호출부 | 핸들 생명주기 소유권이 호출부에서 세션으로 이동 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-06 | DoS | Gemini Files 20GB 쿼터 (핸들 누수 → RESOURCE_EXHAUSTED, 2026-07-06 실증) | mitigate | close() 일괄 delete + __exit__ 예외 경로 + best-effort log.warning — Test 2/3/5로 기계 고정. delete가 "호출마다"→"분석마다"로 바뀔 뿐 누수 0 불변 |
| T-27-07 | Information Disclosure | 사용자 영상 File API 보존 | mitigate | 분석 종료 시 삭제 불변 (V8 데이터 보호 — 제3자 보존 최소화). 영상 바이트/경로 시크릿 로그 금지 |
| T-27-08 | Tampering | call() 이중 delete (세션 핸들을 호출부가 삭제 → 후속 호출 404) | mitigate | preuploaded_handle 주입 시 finally delete skip + 소유권 주석 박제 — Test 6으로 고정 |
| T-27-SC | Tampering | 패키지 설치 | accept | 신규 패키지 0 (google-genai 기존 핀 재사용) |
</threat_model>

<verification>
- `cd backend && PYTHONPATH=shared/python:. python3 -m pytest tests/gemini/ -q` — 전체 green, 기존 기준선 대비 신규 실패 0
</verification>

<success_criteria>
- GeminiFileSession 누수 0 + 업로드 1회가 fake client로 기계 증명
- client.py 핸들 주입 경로 동작 + 기본 경로 무변경 회귀 가드 green
</success_criteria>

<output>
Create `.planning/phases/27-1-gemini-analysis-speed-1min/27-03-SUMMARY.md` when done
</output>
