---
phase: 27-1-gemini-analysis-speed-1min
plan: 04
type: execute
wave: 4
depends_on: ["27-03"]
files_modified:
  - backend/shared/python/sunity_shared/gemini/scene_finder.py
  - backend/shared/python/sunity_shared/judging/gemini_moment_extractor.py
  - backend/shared/python/sunity_shared/gemini/coach_writer_v2.py
  - backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py
  - backend/functions/pipeline/app.py
  - backend/tests/gemini/test_session_wiring.py
autonomous: true
requirements: [SPD-02]
tags: [gemini, file-api, session, inline, performance]

must_haves:
  truths:
    - "D-04: 허용 레버는 모델·입력 불변(핸들 재사용·inline 전송)뿐 — 프레임/해상도 축소 없음, 픽셀 동일 (27-RESEARCH A6)"
    - "mode1 분석 1회에서 학생 영상의 File API 업로드가 1회다 (scene_finder/moment extractor/veto/coach B 전부 세션 핸들 공유)"
    - "coach B retry는 재업로드 없이 generate만 반복한다"
    - "veto still PNG 2장은 File API 업로드 없이 inline(Part.from_bytes)으로 전송된다"
    - "NoHuman/NotPole 조기 raise 경로에서도 세션 핸들이 delete된다"
    - "세션 업로드 실패(None) 시 각 모듈이 기존 자체 업로드 경로로 폴백해 분석이 비차단된다"
  artifacts:
    - path: "backend/functions/pipeline/app.py"
      provides: "_process 세션 생성 + outer finally session.close() + 핸들 전달 배선"
      contains: "GeminiFileSession"
    - path: "backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py"
      provides: "assess_fault_context_video preuploaded 핸들 kwargs + still PNG inline"
      contains: "Part.from_bytes"
    - path: "backend/shared/python/sunity_shared/gemini/coach_writer_v2.py"
      provides: "retry 재업로드 제거 (핸들 재사용)"
      contains: "preuploaded_handle"
  key_links:
    - from: "backend/functions/pipeline/app.py"
      to: "gemini/file_session.py"
      via: "_process try 초입 세션 생성 → outer finally close()"
      pattern: "session\\.close\\(\\)"
    - from: "backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py"
      to: "google.genai types.Part"
      via: "still PNG inline — 업로드/폴링/delete 0"
      pattern: "Part\\.from_bytes"
---

<objective>
핸들 세션 소비처 배선 — scene_finder·moment extractor·veto·coach B가 세션 핸들을 공유하게 하고, veto still PNG 2장을 inline 전송으로 전환한다 (D-04 허용 레버: 핸들 재사용 + inline 전송). (시나리오 2 — 대기 경험.)

이 plan 완료 시 학생 영상 업로드 4~5회 → 1회, 기준 영상 업로드 그대로 1회(veto), still PNG 업로드 2회 → 0회. 업로드+폴링 15~20s × 소멸 횟수만큼 절감 (27-RESEARCH 호출 인벤토리). 프레임/해상도/모델 불변 — D-04 준수, 픽셀 동일 (27-RESEARCH A6, 최종 방어 = 27-09 EVAL18).

Purpose: 비전 구간 지배 요인(중복 라운드트립) 소멸.
Output: 소비처 4곳 배선 + inline 전환 + 배선 단위 테스트. OTA 무관(백엔드).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-1-gemini-analysis-speed-1min/27-PATTERNS.md
@.planning/phases/27-1-gemini-analysis-speed-1min/27-RESEARCH.md
@backend/shared/python/sunity_shared/gemini/file_session.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 어댑터 핸들 주입 — scene_finder / moment extractor / coach B retry</name>
  <files>backend/shared/python/sunity_shared/gemini/scene_finder.py, backend/shared/python/sunity_shared/judging/gemini_moment_extractor.py, backend/shared/python/sunity_shared/gemini/coach_writer_v2.py</files>
  <read_first>
    - backend/shared/python/sunity_shared/gemini/scene_finder.py:160-184 — GeminiVisionCall 인스턴스화 + `call.call(video_path)` + latency try/finally + graceful None 분기 (latency/graceful 무변경)
    - backend/shared/python/sunity_shared/gemini/coach_writer_v2.py:415-429 (`_build_call`), 466-490 (`write()` retry loop — attempt마다 1회용 GeminiVisionCall 생성 = 재업로드 원인, 27-RESEARCH Pitfall 8)
    - backend/shared/python/sunity_shared/judging/gemini_moment_extractor.py:350-365 — 업로드/폴링 지점 (scene_finder와 동일 주입 패턴 — 27-PATTERNS role-match)
    - backend/shared/python/sunity_shared/gemini/client.py — 27-03이 넣은 `preuploaded_handle` 계약
  </read_first>
  <action>
    세 어댑터의 공개 진입 함수에 keyword-only optional `preuploaded_handle=None` (또는 세션 객체가 아닌 **핸들만** — 어댑터는 세션 존재를 모름, 결합 최소화)을 추가해 내부 GeminiVisionCall/업로드부로 전달:
    1. `scene_finder.py` — `find_scene_flags`(및 `_call_wave1_scene_finder`가 부르는 진입 함수)에 `preuploaded_handle` 추가 → `call.call(video_path, preuploaded_handle=preuploaded_handle)`. latency_ms try/finally(171-175)와 graceful None 분기(177-184) 무변경.
    2. `gemini_moment_extractor.py` — 업로드 경로(350-365)에 동일 주입: 핸들 제공 시 자체 업로드/폴링 skip + delete skip (소유권 주석 동일 박제). `TechniqueCache` 키/로직 무변경 (video_hash 키 — 캐시 의미 불변).
    3. `coach_writer_v2.py` — `write(coach_context, *, preuploaded_handle=None)`: `_build_call()`은 attempt마다 생성하되 `call.call(video_path, preuploaded_handle=...)` 로 핸들 전달 — retry는 generate만 반복, 재업로드 0 (Pitfall 8 해소). 핸들 None이면 기존 그대로 (attempt마다 업로드 — 폴백 경로).
    모든 시그니처는 default None — 기존 호출부 동작 byte-동일 (RunPod server.py 무수정 원칙).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && PYTHONPATH=shared/python:. python3 -m pytest tests/ -q -k "scene or coach or moment or extractor" --no-header</automated>
  </verify>
  <acceptance_criteria>
    - 위 pytest exit 0, 신규 FAILED/ERROR 0
    - `rtk grep -c "preuploaded_handle" backend/shared/python/sunity_shared/gemini/scene_finder.py backend/shared/python/sunity_shared/gemini/coach_writer_v2.py backend/shared/python/sunity_shared/judging/gemini_moment_extractor.py` 각 파일 ≥ 1
  </acceptance_criteria>
  <done>세 어댑터가 핸들 주입을 수용하고, 미주입 시 기존 동작이 보존된다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: vision_scorer — veto 핸들 kwargs + still PNG inline 전환</name>
  <files>backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py, backend/tests/gemini/test_session_wiring.py</files>
  <read_first>
    - backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py:1167-1176 — `assess_fault_context_video` 시그니처 (still_* keyword-only optional 선례: "셋 다 제공될 때만 활성, 일부만 = 미제공 취급" 1239-1251 — 동일 계약 스타일로 추가)
    - backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py:1283-1336 — 업로드 + finally 일괄 delete (세션-소유 핸들은 delete 대상에서 제외해야 함)
    - backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py:846-, 1294-1298 — `_upload_image` 호출부 (inline 대체 대상)
    - backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py:822, 856 — `from google.genai import types as genai_types  # lazy` 스타일
    - backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py:1253-1266 — `_build_fanout_key` 캐시 키 (inline 전환은 픽셀 불변 → granularity bump 불필요, 27-RESEARCH Pattern 3 판단)
  </read_first>
  <behavior>
    - Test 1: `preuploaded_student_handle`+`preuploaded_reference_handle` 둘 다 제공 → FakeFiles.upload_calls에 영상 업로드 0, finally delete가 주입 핸들을 지우지 않음 (deleted_names에 주입 핸들 name 부재)
    - Test 2: 둘 중 하나만 제공 → 미제공 취급 (기존 자체 업로드 경로 — still_* 계약 스타일과 동일)
    - Test 3: still PNG가 generate contents에 inline Part로 들어가고 `_upload_image` 미호출 (image 업로드 0, image delete 0)
  </behavior>
  <action>
    1. `assess_fault_context_video`에 `preuploaded_student_handle=None, preuploaded_reference_handle=None` keyword-only 추가 — **둘 다 제공될 때만 활성** (1239-1251 계약 스타일 복제, 주석 포함). 활성 시 student/ref 자체 업로드 skip, finally delete 루프(1283-1336)에서 주입 핸들 제외 (세션 소유 — 자기가 업로드한 것만 지움).
    2. still PNG inline (27-RESEARCH Pattern 3): `_upload_image` 호출부(1294-1298)를 `genai_types.Part.from_bytes(data=png_bytes, mime_type="image/png")` 로 교체, generate_content contents에 file 핸들 대신 Part 삽입. lazy import 스타일(822, 856) 준수. `MAX_VETO_UPLOADS=4` 관련 텔레메트리 `uploadCount` 산식이 있으면 실제 업로드 수 반영으로 갱신 (1685-1692 telemetry dict).
    3. 캐시 키 무변경 — 픽셀 동일이므로 granularity bump 불필요 (90d038f 사고와 구분: 입력 형태가 아닌 전송 방식만 변경. 주석으로 판단 근거 + "EVAL18이 최종 방어 (D-01)" 박제).
    4. `backend/tests/gemini/test_session_wiring.py` 신규 — behavior 3건 (fake_genai fixture 사용, vision_scorer 모듈 patch).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && PYTHONPATH=shared/python:. python3 -m pytest tests/gemini/test_session_wiring.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/gemini/test_session_wiring.py -q` exit 0, 최소 3 passed
    - `rtk grep -c "Part.from_bytes\|Part\\.from_bytes" backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py` ≥ 1
    - `rtk grep -c "preuploaded_student_handle" backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py` ≥ 2
  </acceptance_criteria>
  <done>veto가 세션 핸들을 공유하고 still PNG 업로드 2회+폴링+delete가 inline으로 소멸하며, 세션 핸들을 지우지 않음이 테스트로 고정된다.</done>
</task>

<task type="auto">
  <name>Task 3: _process 세션 생성 + outer finally close + 핸들 전달 배선</name>
  <files>backend/functions/pipeline/app.py, backend/tests/gemini/test_session_wiring.py</files>
  <read_first>
    - backend/functions/pipeline/app.py:3132-3135 (scene_finder 호출부), 3534-3539 (coach 호출부), veto collect 호출부(`_collect_vision_fault_context`), 4155-4166 (outer finally — unlink)
    - backend/shared/python/sunity_shared/gemini/file_session.py — get_or_upload/close 계약 (27-03)
    - .planning/phases/27-1-gemini-analysis-speed-1min/27-RESEARCH.md §Pitfall 2 (조기 raise 경로 finally 도달)
  </read_first>
  <action>
    1. `_process` try 초입(분석-로컬)에 `session = GeminiFileSession()` 생성 — import는 lazy (`_ensure_adapters` 계열 지점 또는 함수 내 import, Lambda 250MB 규율). 모듈 전역 캐시 금지 — 세션은 분석-로컬 (분석 간 상태 오염 0).
    2. outer finally(4155-4166)에 `session.close()` 추가 — **unlink보다 앞**, NoHuman/NotPole 조기 raise 경로 도달 확인 (Pitfall 2). 주석: "세션 delete = 분석당 1회 일괄 — 20GB 적체 재발 방지."
    3. 핸들 전달: scene_finder/recognizer(moment extractor)/veto/coach B 호출부에서 `session.get_or_upload(local_video_path)` (veto는 reference도 `session.get_or_upload(reference_local_video_path)`) 결과를 각 어댑터의 preuploaded kwargs로 전달. 핸들 None(업로드 실패) 시 kwargs 미전달 → 각 모듈 자체 업로드 폴백 (graceful, 분석 비차단). 이 wave에서는 **동기 호출** — get_or_upload를 각 호출 직전에 부름 (첫 소비자가 업로드, 이후 재사용). prefetch 겹치기는 27-05.
    4. Lambda CPU 폴백 경로(`RUNPOD_ANALYZE_URL` 미설정 분기)도 동일 코드 경로 — 분기 0 코드 1벌 불변.
    5. test_session_wiring.py에 통합 수준 테스트 1건 추가: fake genai로 scene→veto→coach 순 호출 시뮬레이션 (어댑터 함수 직접 호출 수준) — 학생 영상 upload_calls == 1, close 후 delete_calls == 업로드 수.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && PYTHONPATH=shared/python:. python3 -m pytest tests/gemini/ tests/test_stage_timing.py -q</automated>
  </verify>
  <acceptance_criteria>
    - 위 pytest exit 0, 신규 FAILED/ERROR 0
    - `rtk grep -c "session.close()" backend/functions/pipeline/app.py` ≥ 1 (outer finally)
    - `rtk grep -c "get_or_upload" backend/functions/pipeline/app.py` ≥ 2 (학생 + 기준)
    - `cd backend && PYTHONPATH=shared/python:. python3 -m pytest tests/ -q` — 기존 pre-existing failure 기준선 대비 신규 FAILED/ERROR 0
  </acceptance_criteria>
  <done>분석당 학생 영상 업로드 1회 + 종료 일괄 delete가 _process에 배선되고, 전체 backend 테스트 기준선이 유지된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pod → Gemini File API | 세션 핸들 공유로 delete 시점이 분석 종료로 이동 |
| _process 조기 raise 경로 → finally | NoHuman/NotPole raise 시 세션 정리 도달 보장 필요 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-09 | DoS | 세션 close 미도달 (조기 raise) → 20GB 적체 재발 | mitigate | outer finally 배치 + NoHuman/NotPole 경로 포함 — Task 3 배선 + 통합 테스트 delete 카운트로 고정. 48h TTL이 최후 안전망 |
| T-27-10 | Tampering | inline 전환이 Gemini 응답 분포 변경 (A6) | mitigate | 픽셀 동일(전송 방식만 변경) + 27-09 EVAL18 무회귀 게이트가 최종 판정 (D-01) |
| T-27-11 | Information Disclosure | still PNG 바이트 로그 유출 | mitigate | inline 바이트를 로그에 남기지 않음 (V8 — 기존 never-log 규율, 코드 리뷰 체크) |
| T-27-SC | Tampering | 패키지 설치 | accept | 신규 패키지 0 |
</threat_model>

<verification>
- `cd backend && PYTHONPATH=shared/python:. python3 -m pytest tests/ -q` — 기준선 대비 신규 실패 0
- 배선 grep 게이트 3종 (session.close / get_or_upload / Part.from_bytes)
</verification>

<success_criteria>
- 학생 영상 File API 업로드 분석당 1회 (fake client 통합 테스트로 증명)
- coach B retry 재업로드 0, still PNG 업로드 0
- 조기 raise 경로 포함 핸들 누수 0
</success_criteria>

<output>
Create `.planning/phases/27-1-gemini-analysis-speed-1min/27-04-SUMMARY.md` when done
</output>
