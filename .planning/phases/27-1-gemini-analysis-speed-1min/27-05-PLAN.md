---
phase: 27-1-gemini-analysis-speed-1min
plan: 05
type: execute
wave: 5
depends_on: ["27-04"]
files_modified:
  - backend/functions/pipeline/app.py
  - backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py
  - backend/tests/test_vision_fanout_parallel.py
  - backend/tests/test_stage_timing.py
  - backend/tests/test_pipeline_gemini_integration.py
  - backend/tests/pipeline/test_pipeline_phase8.py
  - backend/tests/pipeline/test_pipeline_phase9.py
  - backend/tests/test_pipeline_geminic_wiring.py
  - backend/evals/phase25/run_sweep.py
autonomous: true
requirements: [SPD-03, SPD-05]
tags: [parallelism, threadpool, gemini, pipeline, status]

must_haves:
  truths:
    - "prefetch 시작 seam이 실제 코드에 존재한다 — `_download_analysis_video`(로컬 영상 확보)와 `_extract_video_analysis_inputs_from_local`(프레임 추출+RTMW)이 분리되어, prefetch submit이 포즈 작업 이전에 발생한다 (외부 리뷰 HIGH-1: 현행 `_extract_video_analysis_inputs`는 다운로드~RTMW를 내부에서 일괄 수행해 caller-visible seam이 없음)"
    - "기존 wrapper-patch 테스트 20개소(4파일)가 `_download_analysis_video` + `_extract_video_analysis_inputs_from_local` 2-함수 patch로 마이그레이션되어 `_process` 2단 전환 후에도 실 S3 접근 없이 green이다 — stub 재배선만, assert·검증 로직 무변경"
    - "포즈 추정(RTMW) 진행 중 Gemini 업로드 prefetch·scene_finder·moment extractor가 백그라운드 스레드에서 겹쳐 실행된다 (D-03 — 단일 분석 내부만, 분석 간 SERIAL 불변)"
    - "prefetch submit이 frame_extract/rtmw보다 앞임이 stage 로그 순서로 기계 검증된다 (RTMW 후 submit이면 FAIL — 겹치기 수확 소멸)"
    - "veto fan-out 4콜이 병렬 실행되되 집계 입력 순서는 call_plan 인덱스 순으로 순차 실행과 byte-동일하다"
    - "부분 완료(completed < planned)는 여전히 fail-closed resource_limited다 — 부분 결과 verdict 금지"
    - "coach B(Gemini)와 Cerebras가 동시 실행된다"
    - "점수·verdict는 결과 시점에 동기 확정 — 사후 점수 변경 경로 0 (D-03)"
    - "status 갱신이 실제 단계 경계에서 write되어 앱 진행률이 실제와 근사한다 (D-02)"
    - "temp 파일 unlink는 모든 future join 이후에만 실행된다"
  artifacts:
    - path: "backend/functions/pipeline/app.py"
      provides: "seam 리팩터(_download_analysis_video + _extract_video_analysis_inputs_from_local, RTMW 1회 불변) + 분석-로컬 ThreadPoolExecutor prefetch + coach 동시화 + status 시점 교정 + finally join-then-unlink"
      contains: "_download_analysis_video"
    - path: "backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py"
      provides: "_run_part_frame_fanout 병렬화 (인덱스 순 join, fail-closed 보존)"
    - path: "backend/tests/test_vision_fanout_parallel.py"
      provides: "순서 결정론(지연 주입) + fail-closed(timeout→resource_limited) 테스트"
      min_lines: 50
    - path: "backend/tests/test_stage_timing.py"
      provides: "prefetch submit이 rtmw 이전임을 caplog 레코드 순서로 assert하는 테스트 (HIGH-1 검증)"
    - path: "backend/tests/test_pipeline_gemini_integration.py"
      provides: "stub factory 2-함수 patch 마이그레이션 (factory :154 + patch 4개소 — assert 무변경)"
  key_links:
    - from: "backend/functions/pipeline/app.py"
      to: "_extract_video_analysis_inputs_from_local"
      via: "_download_analysis_video 반환 직후 prefetch submit → 이후 포즈 추출 (seam)"
      pattern: "_download_analysis_video"
    - from: "backend/tests/pipeline/test_pipeline_phase8.py"
      to: "_download_analysis_video / _extract_video_analysis_inputs_from_local"
      via: "wrapper 단일 patch → 2-함수 patch 재배선 (_process 2단 전환 정합)"
      pattern: "_extract_video_analysis_inputs_from_local"
    - from: "backend/functions/pipeline/app.py"
      to: "recognizer.motion_query_hint"
      via: "WR-07 rebind가 recognize future submit 이전에 완료"
      pattern: "motion_query_hint"
    - from: "backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py"
      to: "per_call 집계"
      via: "future를 call_plan 인덱스 순 join (as_completed 금지)"
      pattern: "for idx"
---

<objective>
단일 분석 내부 병렬화 — (0) 로컬 영상 확보와 포즈 추출을 분리하는 seam 리팩터로 prefetch 시작점을 실제 코드에 만들고(외부 리뷰 HIGH-1), (a) 다운로드 직후 Gemini 업로드·scene_finder·moment extractor를 prefetch 스레드로 시작해 포즈 51s 그늘에 숨기고(D-03), (b) veto fan-out 4콜을 병렬 발사하되 집계 순서를 보존하고, (c) coach B∥Cerebras를 동시 실행하며, (d) status 갱신 시점을 실제 단계 경계로 교정한다(D-02). (시나리오 2 — 대기 경험.)

경계는 27-RESEARCH 겹치기 지도가 정본: 포즈 산출물 불필요(prefetch 가능) = 업로드/scene_finder/moment extractor(Gemini 경로)/기준 영상 다운로드. 포즈 산출물 필요(겹치기 불가) = DTW/점수/veto 판정/still 페어/coach context. veto 판정·점수 확정은 동기 그대로 — 병렬화 대상은 "업로드 준비"와 "상호독립 generate 콜"뿐 (D-03: veto 완전 비동기·점수 사후 보정 기각).

Purpose: 비전 구간 대기가 포즈 구간에 흡수 + fan-out/coach 벽시계 단축 + 진행률 실제화.
Output: seam 리팩터 + 기존 wrapper-patch 테스트 20개소 마이그레이션(stub 재배선만) + prefetch/병렬 배선 + 결정론·로그순서 테스트 + 신규 env 2종(setdefault 박제). OTA 무관(백엔드).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-1-gemini-analysis-speed-1min/27-RESEARCH.md
@.planning/phases/27-1-gemini-analysis-speed-1min/27-PATTERNS.md
@backend/tests/gemini/fake_genai.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: seam 리팩터 — _download_analysis_video / _extract_video_analysis_inputs_from_local 분리 + 기존 테스트 20개소 2-함수 patch 마이그레이션 (HIGH-1)</name>
  <files>backend/functions/pipeline/app.py, backend/tests/test_pipeline_gemini_integration.py, backend/tests/pipeline/test_pipeline_phase8.py, backend/tests/pipeline/test_pipeline_phase9.py, backend/tests/test_pipeline_geminic_wiring.py</files>
  <read_first>
    - backend/functions/pipeline/app.py:1281-1324 — `_extract_video_analysis_inputs` 현행: keep_local_video=True 분기가 tempfile 생성(1299-1302)→S3 다운로드(1304)→프레임 추출(1305)→RTMW(1306)를 **한 helper 안에서 일괄 수행** — "다운로드 완료, 포즈 미시작" 시점이 caller에 노출되지 않음 (HIGH-1의 실체). docstring의 T-06-02-06(double RTMW 금지) 근거 필독
    - backend/functions/pipeline/app.py:1310-1314 — keep_local_video=False 분기 (delete=True context manager — 추출 후 자동 정리 의미론)
    - backend/functions/pipeline/app.py:3111-3123 — `_process` 호출부: inputs 반환 후에야 local_video_path 획득(3122-3123)
    - backend/tests/test_pipeline_gemini_integration.py:154-194 — `_stub_extract_inputs` factory (wrapper 시그니처의 `_impl(bucket, key, default_pole, *, keep_local_video)` 반환, keep_local_video=True면 fake bytes write) + patch 4개소(:243/:278/:356/:430)에서 wrapper를 monkeypatch 후 `pipeline._process(...)` 직접 실행(:260) — **_process가 2단 호출로 전환되면 이 wrapper patch들이 전부 dead가 되는 마이그레이션 실체**
    - backend/tests/pipeline/test_pipeline_phase8.py:85-114 — 동형 factory + :177-179 patch 패턴 (동일 패턴 10개소)
    - backend/tests/pipeline/test_pipeline_phase9.py:81 factory + patch 5개소(:202/:224/:280/:304/:341), backend/tests/test_pipeline_geminic_wiring.py:47 `_stub_inputs` + patch 1개소(:169-175)
  </read_first>
  <action>
    `_extract_video_analysis_inputs`(1281-1324)를 두 함수로 분리 — 동작 byte-동일, 실행 순서·횟수 불변:
    1. `_download_analysis_video(bucket: str, key: str) -> str` — delete=False NamedTemporaryFile(suffix=".mp4") 생성 + `_s3.download_file`. 예외 시 unlink 후 raise (현행 1307-1309 정합). 반환 = 로컬 경로 문자열. **이 함수 반환 시점 = "다운로드 완료, 포즈 미시작" — Task 2 prefetch submit 지점** (주석 박제: "27-PLAN-REVIEW HIGH-1 — prefetch seam. 이 라인 뒤·`_extract_video_analysis_inputs_from_local` 앞이 Gemini prefetch 시작점").
    2. `_extract_video_analysis_inputs_from_local(local_video_path: str, default_pole: PoleAxis, *, keep_local_video: bool) -> _VideoAnalysisInputs` — `_ensure_adapters()` + `_FRAME_EXTRACTOR.extract` + `_RTMW_ENGINE.estimate` + 기존 후처리(profile R4 non-null / angles R6 / pole 검출 Plan 08-04) 전부 이식. keep_local_video=False면 추출 완료 후 unlink + `local_video_path=None` 반환 (현행 delete=True 의미론 보존), True면 caller unlink 책임 (현행 계약 유지). 예외 시 unlink 후 raise.
    3. 기존 `_extract_video_analysis_inputs(bucket, key, default_pole, *, keep_local_video)` 는 두 함수를 순서대로 부르는 **얇은 합성 wrapper로 유지** — 단 이는 **외부 호출부 호환용(시그니처·반환 불변)이지, 기존 테스트 통과를 보장하지 않는다**: `_process`가 4번 항목의 2단 호출로 전환되면 wrapper를 monkeypatch하던 기존 테스트 20개소(4파일)는 전부 dead patch가 되어 `_process`가 실제 S3 다운로드를 시도한다 → **6번 항목의 테스트 마이그레이션이 이 태스크의 필수 구성요소** ("wrapper 유지 = 기존 테스트 무수정 통과" 가정 금지). wrapper 존재만 확인하는 hasattr-only 테스트 2건(backend/tests/test_pipeline_recognizer_switch.py:246, backend/tests/test_pipeline_body_profile_injection.py:272)은 wrapper 유지로 **무접촉** — 수정 금지.
    4. `_process`(3111-3123)는 wrapper 대신 2단 호출로 전환: `local_video_path = _download_analysis_video(bucket, key)` → (Task 2가 여기 prefetch 삽입) → `inputs = _extract_video_analysis_inputs_from_local(local_video_path, default_pole, keep_local_video=...)`. keep_local_video 게이트 식(3113-3117)은 그대로 두 번째 호출로 이동.
    5. 불변 보장 (T-06-02-06 재발 금지): S3 다운로드 1회·프레임 추출 1회·**RTMW estimate 1회** — 분리 전후 호출 횟수 동일. docstring의 R3/R4/T-06-02-06 근거 주석을 새 함수로 이관.
    6. **기존 테스트 마이그레이션 — stub 재배선만:** wrapper 단일 patch 20개소를 2-함수 patch 쌍으로 기계적 치환. 각 개소의 `monkeypatch.setattr(pipeline, "_extract_video_analysis_inputs", stub)` 를 → (i) `monkeypatch.setattr(pipeline, "_download_analysis_video", <stub: fake 로컬 경로 문자열 반환 — 기존 factory의 tmp_video_path 재사용, gemini_integration처럼 fake bytes write가 필요한 경우 그 로직을 다운로드 stub으로 이동>)` + (ii) `monkeypatch.setattr(pipeline, "_extract_video_analysis_inputs_from_local", <기존 factory가 만들던 `_VideoAnalysisInputs` stub 그대로 — 내부 `_impl` 시그니처만 `(local_video_path, default_pole, *, keep_local_video)` 로 교체>)` 로 대체. 대상/개소 (grep 검증 완료): backend/tests/test_pipeline_gemini_integration.py — factory `_stub_extract_inputs`(:154) + patch 4개소(:243/:278/:356/:430); backend/tests/pipeline/test_pipeline_phase8.py — factory(:85) + patch 10개소(:177/:200/:272/:288/:327/:338/:354/:415/:438/:459); backend/tests/pipeline/test_pipeline_phase9.py — factory(:81) + patch 5개소(:202/:224/:280/:304/:341); backend/tests/test_pipeline_geminic_wiring.py — `_stub_inputs`(:47) 소비부 + patch 1개소(:169-175). factory는 파일당 1곳(2-함수 쌍 또는 두 stub을 반환하도록)만 고치고 patch 개소는 치환만. **제약 박제: 테스트의 assert·검증 로직 변경 금지, 신규 로직 추가 금지** — 마이그레이션이 기존 검증 의미를 바꾸거나 회귀를 가리면 안 됨 (재배선 diff만 허용).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && PYTHONPATH=shared/python:. python3 -m pytest tests/test_pipeline_gemini_integration.py tests/pipeline/test_pipeline_phase8.py tests/pipeline/test_pipeline_phase9.py tests/test_pipeline_geminic_wiring.py -q && PYTHONPATH=shared/python:. python3 -m pytest tests/ -q -k "pipeline or extract or stage_timing" --no-header</automated>
  </verify>
  <acceptance_criteria>
    - 마이그레이션 4파일 명시 pytest exit 0 — dead patch면 `_process`가 실 S3 다운로드를 시도해 즉사하므로, green = 2-함수 patch 정착의 기계 증명
    - 위 -k pytest exit 0, 신규 FAILED/ERROR 0 (기존 기준선 유지 — hasattr-only 2건 포함)
    - `rtk grep -c '"_extract_video_analysis_inputs"' backend/tests/test_pipeline_gemini_integration.py backend/tests/pipeline/test_pipeline_phase8.py backend/tests/pipeline/test_pipeline_phase9.py backend/tests/test_pipeline_geminic_wiring.py` — 각 파일 0 (setattr 문자열 잔존 0 = wrapper patch 전멸 확인. docstring 백틱 표기는 미매치)
    - `rtk grep -c "_download_analysis_video" backend/functions/pipeline/app.py` ≥ 2 (정의 + _process 호출)
    - `rtk grep -c "_extract_video_analysis_inputs_from_local" backend/functions/pipeline/app.py` ≥ 2 (정의 + 호출)
    - `rtk grep -c "_RTMW_ENGINE.estimate" backend/functions/pipeline/app.py` — 분리 전과 동일 횟수 (RTMW 1회 불변, executor가 diff로 확인)
  </acceptance_criteria>
  <done>"다운로드 완료·포즈 미시작" 지점이 `_process`에서 caller-visible해지고, 다운로드/추출/RTMW 각 1회 불변이 유지되며, wrapper-patch 기존 테스트 20개소가 2-함수 patch로 재배선(assert 무변경)되어 4파일 green이다.</done>
</task>

<task type="auto">
  <name>Task 2: prefetch 겹치기 — 분석-로컬 ThreadPoolExecutor + submit-순서 로그 검증 + join-then-unlink</name>
  <files>backend/functions/pipeline/app.py, backend/tests/test_stage_timing.py, backend/evals/phase25/run_sweep.py</files>
  <read_first>
    - Task 1이 만든 seam: `_download_analysis_video` 반환 직후 = prefetch 시작점 (RTMW 전 — HIGH-1 fix의 핵심)
    - backend/functions/pipeline/app.py:3142-3152 — WR-07 rebind 주석 (recognize submit은 rebind 이후 — 순서 제약의 정본. rebind 입력 recognizer/meta/mode는 모두 다운로드 시점에 이미 가용 — 포즈 산출물 무관)
    - backend/functions/pipeline/app.py:3132-3135 — scene_finder 현행 순차 호출부, 4155-4166 — outer finally unlink
    - .planning/phases/27-1-gemini-analysis-speed-1min/27-RESEARCH.md §Code Examples "업로드 prefetch + 순서보존 join", §공유 상태 오염 위험 표, §Pitfall 3 (future 생존 중 unlink 금지)
    - backend/evals/phase25/run_sweep.py:79-93 — env setdefault 박제 패턴
  </read_first>
  <action>
    1. `_process`에 분석-로컬 `ThreadPoolExecutor(max_workers=4, thread_name_prefix="gemini")` 도입 — `with` 컨텍스트 매니저, 분석 간 SERIAL 불변 유지 (executor는 분석마다 생성/폐기, 모듈 전역 금지). 코드베이스 production 선례 0 (27-PATTERNS No Analog) — 27-RESEARCH Code Example 골격이 설계 정본이며 이 파일이 향후 analog가 된다.
    2. env 게이트: `GEMINI_UPLOAD_PREFETCH` (기본 "1") — "0"이면 prefetch 전체 비활성 = 27-04의 동기 경로 그대로 (canary/rollback 레버, 27-09가 사용). `int(os.environ.get(...) or "1")` 소비.
    3. prefetch submit — **위치 = `_download_analysis_video` 반환 직후, `_extract_video_analysis_inputs_from_local`(프레임 추출/RTMW) 호출 전** (Task 1 seam — 여기가 아니면 겹치기 수확 소멸, HIGH-1): (a) `session.get_or_upload(local_video_path)` — 학생 영상 업로드+폴링, (b) mode1이면 기준 영상 S3 다운로드 + `session.get_or_upload(ref_path)`, (c) scene_finder (3132-3135 이동 — 입력 local_video_path/is_reference만, 포즈 무관), (d) moment extractor — **Gemini recognizer 경로만** (`_call_extractor(frames=video_path)` — angles 미소비. FallbackRecognizer env 분기는 prefetch 제외, 기존 순차 위치 유지). (d) submit은 WR-07 `motion_query_hint` rebind **완료 후** — rebind 블록(3146-3152)을 다운로드 직후·submit 블록 앞으로 이동 (rebind 입력 recognizer/meta/mode는 포즈 이전 가용 — read_first 근거) + 주석으로 순서 제약 박제.
    4. submit 시점 마커 로그: prefetch submit 직후 `log.info("stage_timing analysis_id=%s stage=gemini_upload_prefetch_submit elapsed_ms=%d", ...)` 방출 (27-01 `_stage` 규율과 동일 포맷 — dict 미기록, 마커 전용. 주석 박제). **검증 (HIGH-1):** `backend/tests/test_stage_timing.py`에 테스트 추가 — mock된 `_process` 축소 경로(또는 submit/rtmw 로그 방출 지점 단위 함수)에서 caplog 레코드 순서로 `gemini_upload_prefetch_submit` 레코드 인덱스 < `stage=rtmw` 완료 레코드 인덱스 assert. submit이 rtmw 완료 뒤면 FAIL. (프로덕션 로그 실측 확인은 27-09 sweep에서 동일 순서 조건으로 재검.)
    5. join 지점: 각 결과의 첫 소비처 직전 `future.result()` — scene/moment는 기존 소비 지점, 핸들은 veto/coach 직전. future 예외는 기존 graceful 규율로 흡수 (`except Exception: # noqa: BLE001` + log.warning → None 폴백, 분석 비차단 — PATTERNS Shared Pattern 1). fail 시 각 모듈 자체 업로드 폴백 경로(27-04, moment extractor는 HIGH-2 fix로 누수 0) 그대로.
    6. outer finally 재배치: **모든 future join(예외 무시 wait) → session.close() → unlink** 순서 (Pitfall 3 — prefetch future가 파일 읽는 중 unlink 금지). NotPole/NoHuman 조기 raise 경로에서도 이 순서 도달. RTMW estimate 1회 불변(Task 1)과 cleanup 순서가 이 태스크의 두 불변.
    7. `run_sweep.py`에 `os.environ.setdefault("GEMINI_UPLOAD_PREFETCH", "1")` 박제 (79-93 패턴 — pipeline 로드 전 module-level. 주석 포함). stage-timing: prefetch 이동한 단계는 `gemini_upload_prefetch`/`scene_finder`/`recognizer` 키로 스레드 내 계측 유지 (helper는 스레드-로컬 dict 충돌 없게 stage 키만 분리 — timings dict 접근은 GIL-원자 int 대입이라 안전, 주석 박제).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && PYTHONPATH=shared/python:. python3 -m pytest tests/ -q -k "pipeline or stage_timing or session" --no-header</automated>
  </verify>
  <acceptance_criteria>
    - 위 pytest exit 0, 신규 FAILED/ERROR 0 (prefetch-submit-before-rtmw 로그 순서 테스트 포함)
    - `rtk grep -c "ThreadPoolExecutor" backend/functions/pipeline/app.py` ≥ 1
    - `rtk grep -c "GEMINI_UPLOAD_PREFETCH" backend/functions/pipeline/app.py backend/evals/phase25/run_sweep.py` 각 ≥ 1
    - `rtk grep -c "gemini_upload_prefetch_submit" backend/functions/pipeline/app.py backend/tests/test_stage_timing.py` 각 ≥ 1 (마커 + 순서 테스트)
    - `rtk grep -n "motion_query_hint" backend/functions/pipeline/app.py` — rebind가 submit 블록보다 앞 라인 (rtk read로 육안 확인 + 주석 존재)
  </acceptance_criteria>
  <done>prefetch가 env 게이트 하에 seam(다운로드 직후·포즈 전)에서 시작되고, submit-before-rtmw가 로그 순서 테스트로 기계 증명되며, rebind→submit 순서·join-then-unlink가 구조적으로 보장된다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: veto fan-out 병렬화 — 인덱스 순 join + fail-closed 보존</name>
  <files>backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py, backend/tests/test_vision_fanout_parallel.py</files>
  <read_first>
    - backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py:1659-1683 — `_run_part_frame_fanout` 순차 루프 (보존 의미론 3종: fail-closed 1700-1708, per_call 순서 = call_plan 인덱스 = `primary = parsed_verdicts[0]` 1720, `clock` 주입 인자 1614)
    - .planning/phases/27-1-gemini-analysis-speed-1min/27-RESEARCH.md §Code Examples "fan-out 병렬 (순서 결정론 보존)", §Pitfall 4, §Pitfall 7 (429)
    - backend/tests/gemini/fake_genai.py — FakeModels delay_by_index (순서 결정론 테스트용)
  </read_first>
  <behavior>
    - Test 1 (순서 결정론): 4콜 중 인덱스 0에 인위 지연 주입(FakeModels delay_by_index) → per_call/parsed_verdicts 순서가 call_plan 인덱스 순과 동일 (as_completed였다면 뒤바뀌었을 상황)
    - Test 2 (fail-closed): future timeout으로 4콜 중 2콜만 완료 → completed < planned → resource_limited 의미론 산출 (부분 verdict 금지)
    - Test 3 (병렬도 env): `GEMINI_FANOUT_WORKERS=1` 이면 순차 실행과 동일 결과 (폴백 경로)
  </behavior>
  <action>
    1. `_run_part_frame_fanout`(1659-1683) 병렬화 — 27-RESEARCH Code Example 골격 그대로: `futures = [pool.submit(_one_call, call_plan[i]) for i in range(planned)]` 후 `for idx, fut in enumerate(futures): raw_text = fut.result(timeout=remaining_budget())` — **as_completed 금지**, 기존 per_call append/`_SCORE_PATTERN` 폐기 로직 루프 본체 그대로 이식.
    2. wall budget 가드 이식: "제출 전 elapsed 확인" → "future별 `result(timeout=remaining)`" — timeout/미완료 시 해당 콜 미완료 처리, `completed < planned → resource_limited` fail-closed 의미론 불변 (D-13 HIGH-2 Option A). `clock` 주입 인자(1614) 유지 — remaining 계산에 clock 사용.
    3. 병렬도 env: `GEMINI_FANOUT_WORKERS` (기본 4, 429 발생 시 2로 축소하는 운영 폴백 상수 — 27-09 canary 레버). 1이면 사실상 순차. executor는 함수-로컬 생성 (`with ThreadPoolExecutor(max_workers=workers) as pool`).
    4. Client 스레드 안전성 (27-RESEARCH A1 [ASSUMED]): 모듈 싱글톤 `_CLIENT`(761-780) 유지하되, 주석으로 "google-genai thread-safety 미문서 — 병렬 콜 산발 오류 시 스레드별 Client 생성으로 회피 (핸들은 name 문자열이라 Client 무관)" 폴백 방침 박제. 실측 검증은 27-09.
    5. 캐시: `VisionVetoCache` 키는 (hash pair, PROMPT_VERSION, SCHEMA_VERSION, granularity, n, fi)로 완전 결정 — 같은 분석 내 병렬 태스크는 서로 다른 키 네임스페이스 (27-RESEARCH 공유 상태 표 "안전" 판정). granularity/PROMPT_VERSION bump **불필요·금지** (실행 토폴로지만 변경, 입력 형태 불변 — 90d038f와 구분 주석).
    6. `backend/tests/test_vision_fanout_parallel.py` 신규 — behavior 3건. 병렬 테스트 선례는 tests/test_pipeline_body_profile_injection.py:218.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && PYTHONPATH=shared/python:. python3 -m pytest tests/test_vision_fanout_parallel.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_vision_fanout_parallel.py -q` exit 0, 최소 3 passed
    - `rtk grep -c "as_completed" backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py` == 0
    - `rtk grep -c "GEMINI_FANOUT_WORKERS" backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py` ≥ 1
    - `cd backend && PYTHONPATH=shared/python:. python3 -m pytest tests/ -q -k "vision or veto" --no-header` 신규 FAILED/ERROR 0
  </acceptance_criteria>
  <done>fan-out이 병렬 실행되되 집계 순서·fail-closed가 순차와 byte-동일함이 지연-주입 테스트로 기계 증명된다.</done>
</task>

<task type="auto">
  <name>Task 4: coach B∥Cerebras 동시화 + status 갱신 시점 교정 (D-02)</name>
  <files>backend/functions/pipeline/app.py</files>
  <read_first>
    - backend/functions/pipeline/app.py:3534-3539 — coach 순차 호출부 (`_call_coach_writer_with_retry` gemini → cerebras)
    - backend/functions/pipeline/app.py:3088-3093 — status 연속 write (85% 정지 오인의 원점), 3172-3174 — STATUS_COMPARISON write
    - .planning/phases/27-1-gemini-analysis-speed-1min/27-RESEARCH.md §Pitfall 5 (진행률 역행 방지 — 앱 Math.max 단조 로직 무변경 전제)
  </read_first>
  <action>
    1. coach 동시화: `gemini_future = pool.submit(_call_coach_writer_with_retry, "gemini", _ensure_gemini_coach_writer().write, coach_context)` (핸들 kwargs 포함 — 27-04 배선 유지) + Cerebras를 메인 스레드에서 실행 후 `gemini_result = gemini_future.result()` join. 두 writer는 같은 immutable coach_context dict 읽기만 (B3 정합 — 주석 박제). 예외는 기존 retry/graceful 규율 그대로.
    2. status 갱신 시점 교정 (D-02): 3088-3093의 연속 write를 분리 — `STATUS_FRAME_EXTRACTION`은 추출 시작 전(현행 유지, Task 1 분리 후에는 `_extract_video_analysis_inputs_from_local` 직전), `STATUS_POSE_ANALYSIS`는 **프레임 추출 완료 후**, `STATUS_COMPARISON`은 **RTMW 완료 후** (3172-3174 이동)로 각 단계 경계에 write. status enum/PIPELINE_SEQUENCE 추가 절대 금지 (3-way lockstep 비용 — 27-RESEARCH Alternatives 표). 갱신 함수 `firestore_admin.update_analysis_status` 재사용, 호출 위치만 이동.
    3. 앱 진행률 역행 없음 확인 근거를 주석으로: loading.tsx의 `Math.max` 단조 로직이 유지되는 한 시점 이동은 역행 불가 (Pitfall 5) — PROGRESS_PCT 재배분은 27-07.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && PYTHONPATH=shared/python:. python3 -m pytest tests/ -q --no-header 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - backend full pytest — 기존 pre-existing failure 기준선(node-ID diff) 대비 신규 FAILED/ERROR 0
    - `rtk grep -c "gemini_future" backend/functions/pipeline/app.py` ≥ 1
    - `rtk grep -n "STATUS_POSE_ANALYSIS" backend/functions/pipeline/app.py` — 3088-3093 연속 write 블록 해체 확인 (frame_extraction 직후 pose_analysis 연속 write 부재)
  </acceptance_criteria>
  <done>coach 듀얼트랙이 동시 실행되고 status가 실제 단계 경계에서 write되며 전체 테스트 기준선이 유지된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 메인 스레드 ↔ prefetch 스레드 | 공유 상태(모듈 캐시/전역/temp 파일) 접근 |
| 병렬 fan-out → 점수 집계 | 실행 순서가 점수 결정론에 영향 가능 |
| seam 리팩터 → 포즈 파이프라인 | 다운로드/추출/RTMW 분리가 실행 횟수를 바꾸면 T-06-02-06 재발 |
| seam 리팩터 → 기존 테스트 스위트 | wrapper patch가 dead가 되면 테스트가 실 S3에 도달 — 마이그레이션이 회귀를 가리면 안 됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-12 | Tampering | fan-out 집계 순서 (비결정 → 점수 변동) | mitigate | call_plan 인덱스 순 join 강제 (as_completed grep==0 게이트) + 지연-주입 결정론 테스트 + 27-09 cold/warm 결정론 게이트 |
| T-27-13 | Tampering | 부분 완료 verdict (병렬 예외 무음 삼킴) | mitigate | fail-closed resource_limited 의미론 보존 (Test 2) — future 예외는 graceful status 명시 매핑 |
| T-27-14 | Tampering | 공유 상태 오염 (`_RECOGNIZER.motion_query_hint` WR-07 / `_CLIENT` 싱글톤 / VisionVetoCache) | mitigate | rebind→submit 순서 박제, 캐시 키 완전 결정(네임스페이스 분리), Client 폴백 방침 주석 + 27-09 실측 |
| T-27-15 | DoS | 429 rate limit 표면화 → resource_limited 증가 = 정확도 회귀 전이 | mitigate | GEMINI_FANOUT_WORKERS 폴백 상수(4→2→1) + 27-09에서 completedCalls==plannedCalls 게이트 |
| T-27-16 | DoS | future 생존 중 temp unlink → 이상 실패 | mitigate | finally join-then-close-then-unlink 구조 보장 (Pitfall 3) |
| T-27-23 | Tampering | seam 리팩터로 RTMW/추출 이중 실행 (T-06-02-06 재발) 또는 prefetch가 RTMW 후 시작(수확 0 — HIGH-1) | mitigate | wrapper 합성 유지 + estimate 호출 횟수 동일 게이트(Task 1) + submit-before-rtmw 로그 순서 테스트(Task 2) |
| T-27-24 | Tampering | 테스트 마이그레이션이 assert 약화/회귀 은폐 | mitigate | "stub 재배선만, assert·로직 변경 금지" 제약 박제(Task 1-6) + setattr 문자열 잔존 0 grep 게이트 + 4파일 명시 pytest 게이트 |
| T-27-SC | Tampering | 패키지 설치 | accept | 신규 패키지 0 (stdlib concurrent.futures) |
</threat_model>

<verification>
- backend full pytest 기준선 유지 + fan-out 결정론/fail-closed 테스트 + prefetch submit-before-rtmw 로그 순서 테스트 green
- 마이그레이션 4파일(gemini_integration/phase8/phase9/geminic_wiring) 명시 pytest green + wrapper setattr 문자열 잔존 0
- as_completed 사용 0, env 게이트 2종(GEMINI_UPLOAD_PREFETCH/GEMINI_FANOUT_WORKERS) 존재
- seam 함수 2종(_download_analysis_video/_extract_video_analysis_inputs_from_local) 존재 + RTMW 1회 불변
</verification>

<success_criteria>
- prefetch 시작점이 실제 코드 seam(다운로드 직후·포즈 전)에 존재하고 로그 순서로 기계 검증됨 (HIGH-1 해소)
- 기존 wrapper-patch 테스트 20개소가 2-함수 patch로 마이그레이션(assert 무변경)되어 _process 2단 전환과 함께 green
- prefetch·fan-out·coach 병렬화가 배선되고 결정론·fail-closed·SERIAL 불변이 테스트/게이트로 고정
- status 갱신 시점이 실제 단계 경계로 이동 (enum 추가 0)
- 신규 env가 코드 default + eval setdefault로 이중 박제 (start_server.sh 반영은 27-09)
</success_criteria>

<output>
Create `.planning/phases/27-1-gemini-analysis-speed-1min/27-05-SUMMARY.md` when done
</output>
