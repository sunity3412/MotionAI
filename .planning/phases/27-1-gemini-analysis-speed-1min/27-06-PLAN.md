---
phase: 27-1-gemini-analysis-speed-1min
plan: 06
type: execute
wave: 6
depends_on: ["27-05"]
files_modified:
  - backend/shared/python/sunity_shared/models.py
  - backend/shared/python/sunity_shared/firestore_admin.py
  - backend/functions/pipeline/app.py
  - app/src/types/analysis.ts
  - docs/contract.md
  - backend/tests/test_fault_zoom_deferred.py
autonomous: true
requirements: [SPD-04]
tags: [fault-zoom, firestore, contract, postprocess, performance]

must_haves:
  truths:
    - "점수·verdict·감점 내역이 fault_zoom 렌더를 기다리지 않고 먼저 complete된다 (앱 onSnapshot 즉시 표시)"
    - "zoom PNG는 렌더 완료 후 result.faultZoomComparisons + faultZoomStatus='done' 부분 업데이트로 도착한다"
    - "zoom 렌더 실패 시 faultZoomStatus='failed' — 앱이 무한 로딩에 빠질 pending 고아 상태가 없다"
    - "complete 이후 zoom 외 어떤 result 필드도 변경되지 않는다 (D-03 경계)"
    - "학생 영상 ffmpeg 재디코딩이 3회→1회로 준다 (프레임 배열 재사용)"
    - "faultZoomStatus 계약이 analysis.ts + models.py + contract.md 3-way lockstep으로 정의된다"
  artifacts:
    - path: "backend/shared/python/sunity_shared/firestore_admin.py"
      provides: "update_analysis_fault_zoom — field-path 부분 업데이트 + scoped validator"
      exports: ["update_analysis_fault_zoom"]
    - path: "backend/shared/python/sunity_shared/models.py"
      provides: "FAULT_ZOOM_STATUS_PENDING/DONE/FAILED 상수 (PIPELINE_SEQUENCE 비추가)"
      contains: "FAULT_ZOOM_STATUS_PENDING"
    - path: "app/src/types/analysis.ts"
      provides: "faultZoomStatus?: 'pending' | 'done' | 'failed' optional 필드"
      contains: "faultZoomStatus"
    - path: "backend/tests/test_fault_zoom_deferred.py"
      provides: "부분 업데이트 shape + validator + pending→done/failed 전이 테스트"
      min_lines: 50
  key_links:
    - from: "backend/functions/pipeline/app.py"
      to: "firestore_admin.update_analysis_fault_zoom"
      via: "complete_analysis 이후 같은 BackgroundTask에서 zoom 렌더 → 부분 update"
      pattern: "update_analysis_fault_zoom"
    - from: "app/src/types/analysis.ts"
      to: "backend/shared/python/sunity_shared/models.py"
      via: "3-way lockstep 주석 상호 인용"
      pattern: "faultZoomStatus"
---

<objective>
fault_zoom 사후 분리 (D-06) — zoom PNG 렌더(~후처리 49s의 주요분)를 complete_analysis 이후로 옮겨 점수를 먼저 앱에 도착시키고, zoom은 faultZoomStatus pending→done/failed 부분 업데이트로 뒤따르게 한다. 동시에 ffmpeg 재디코딩 3회를 프레임 배열 재사용으로 제거한다. (시나리오 2 — 대기 경험.)

D-06 명시: zoom은 점수가 아닌 표현물 — D-03 "사후 변경 금지"와 충돌하지 않음 (CONTEXT 원문). zoom 외 필드의 사후 변경은 절대 금지. 분석 간 SERIAL 불변 — zoom 렌더는 같은 BackgroundTask 안에서 실행 (별도 태스크 불필요: 다음 분석은 어차피 이 task 종료 후, 27-RESEARCH Pattern 5).

Purpose: 사용자 체감 완료 시점을 ~수십 초 앞당김 + 후처리 디코딩 비용 소멸.
Output: 계약 3-way + persistence 함수 + _process 재배열 + 프레임 재사용. OTA 무관(백엔드 — 앱 소비는 27-07).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-1-gemini-analysis-speed-1min/27-PATTERNS.md
@.planning/phases/27-1-gemini-analysis-speed-1min/27-RESEARCH.md
@backend/shared/python/sunity_shared/firestore_admin.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 계약 3-way lockstep + update_analysis_fault_zoom persistence</name>
  <files>backend/shared/python/sunity_shared/models.py, backend/shared/python/sunity_shared/firestore_admin.py, app/src/types/analysis.ts, docs/contract.md, backend/tests/test_fault_zoom_deferred.py</files>
  <read_first>
    - backend/shared/python/sunity_shared/models.py:76 (`VISION_VETO_STATUSES` tuple 선례), 325-339 (`STATUS_*` 상수 블록)
    - backend/shared/python/sunity_shared/firestore_admin.py:37-42 (`update_analysis_status` — 부분 update 정본), 104-128 (`_validate_dict_only_scalars` — 본체 변경 영구 0), 288-305 (`_validate_safety_flags` — list[dict] scoped 검증 선례)
    - app/src/types/analysis.ts:515-525 (`faultZoomComparisons?` optional + lockstep 주석 선례), 451-459 (`tier?` — 하위호환 서술 모범)
    - .planning/phases/27-1-gemini-analysis-speed-1min/27-RESEARCH.md §Code Examples "D-06 zoom 부분 업데이트" 골격
  </read_first>
  <behavior>
    - Test 1: `update_analysis_fault_zoom(uid, id, comparisons, status="done")` → mock doc `.update()`가 `result.faultZoomComparisons` + `result.faultZoomStatus` + `updatedAt`(epoch ms int) 키로 호출됨 (field-path 형)
    - Test 2: comparisons item에 nested list/dict 포함 시 TypeError (scoped validator — firestore-nested-array-flat 규율)
    - Test 3: `status="failed"` + comparisons 빈 리스트 허용 — 실패 전이 shape
    - Test 4: status가 pending/done/failed 외 값이면 ValueError (models 상수 강제)
  </behavior>
  <action>
    1. `models.py` — `STATUS_*` 블록(325-339) 옆에 상수 3종 + tuple 추가 (D-06):
       `FAULT_ZOOM_STATUS_PENDING = "pending"` / `_DONE = "done"` / `_FAILED = "failed"` / `FAULT_ZOOM_STATUSES = (...)`. **PIPELINE_SEQUENCE/status enum에는 절대 추가 금지** (3-way lockstep 비용 — 27-RESEARCH Alternatives 표. 주석으로 박제).
    2. `firestore_admin.py` — 신규 함수 `update_analysis_fault_zoom(uid: str, analysis_id: str, comparisons: list[dict], status: str) -> None` (27-RESEARCH 골격 그대로): status를 `models.FAULT_ZOOM_STATUSES`로 검증(ValueError), 각 item을 `_validate_dict_only_scalars(c, path="faultZoomComparisons[]")` 루프 (Analog `_validate_safety_flags` 288-305 — validator 본체 무수정), `.update({"result.faultZoomComparisons": comparisons, "result.faultZoomStatus": status, "updatedAt": int(time.time() * 1000)})` field-path 형. docstring에 "contract.md faultZoomStatus 절 + D-06" 인용. field-path `.update()`는 firestore_admin 무선례지만 firebase-admin 표준 API (27-PATTERNS 부분-무선례 메모 — `set(merge=True)`는 dict 병합이라 배열 교체 의미가 모호, 명시적 field-path update 채택. 근거 주석 박제).
    3. `app/src/types/analysis.ts` — `AnalysisResult`의 `faultZoomComparisons?` 옆에 `faultZoomStatus?: 'pending' | 'done' | 'failed'` (string-literal union 관례) + 주석: "Python lockstep: models.py FAULT_ZOOM_STATUSES + contract.md. 하위호환: 필드 부재(legacy doc) = comparisons 유무로 판정 (tier? 451-459 서술 모범 준수)".
    4. `docs/contract.md` — faultZoomStatus 절 추가: 의미(pending=렌더 중/done=도착/failed=실패·카드 숨김), 부재=legacy, zoom 외 필드 사후 변경 금지(D-03 경계) 명시. CONTEXT가 3-way lockstep을 명시했으므로 contract.md 수정 확정 (27-PATTERNS의 저비용 주석-only 경로 대신).
    5. `backend/tests/test_fault_zoom_deferred.py` — behavior 4건 (firestore mock — 기존 firestore_admin 테스트의 mock 관례 따름).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && PYTHONPATH=shared/python:. python3 -m pytest tests/test_fault_zoom_deferred.py -q && cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_fault_zoom_deferred.py -q` exit 0, 최소 4 passed
    - `cd app && npm run typecheck` exit 0
    - `rtk grep -c "faultZoomStatus" app/src/types/analysis.ts backend/shared/python/sunity_shared/firestore_admin.py docs/contract.md` 각 ≥ 1 (3-way 존재)
    - `rtk grep -c "FAULT_ZOOM_STATUS_PENDING" backend/shared/python/sunity_shared/models.py` ≥ 1
    - `rtk grep -v "^#" backend/shared/python/sunity_shared/models.py | rtk grep -c "PIPELINE_SEQUENCE" | head -1` — PIPELINE_SEQUENCE에 zoom 상태 미추가 (기존 정의 그대로)
  </acceptance_criteria>
  <done>faultZoomStatus 계약이 3-way로 정의되고 부분 업데이트 함수가 validator와 함께 테스트로 고정된다.</done>
</task>

<task type="auto">
  <name>Task 2: _process 재배열 — complete 먼저, zoom 렌더는 이후 + unlink 재배치</name>
  <files>backend/functions/pipeline/app.py, backend/tests/test_fault_zoom_deferred.py</files>
  <read_first>
    - backend/functions/pipeline/app.py:2648 (`_attach_fault_zoom_comparisons` 정의 — graceful 가드 2665-2666), 3995 (현행 호출부 — complete 전 result 부착), 4131-4153 (complete_analysis 호출), 4155-4166 (outer finally unlink)
    - backend/shared/python/sunity_shared/firestore_admin.py — complete_analysis payload 조립부(914-918 부근)
    - .planning/phases/27-1-gemini-analysis-speed-1min/27-RESEARCH.md §Pattern 5 (temp 파일 생명주기 주의 포함)
  </read_first>
  <action>
    1. `_attach_fault_zoom_comparisons` 호출(3995)을 complete 전 경로에서 제거하고, complete 시점 pending 마커 규칙 적용: zoom 렌더 대상이 존재하면(기존 graceful 가드 2665-2666 조건 재사용 — 입력 없으면 대상 없음) `result["faultZoomStatus"] = models.FAULT_ZOOM_STATUS_PENDING`, 대상 없으면 **필드 자체 생략** (하위호환 판정 규칙 정합). scalar라 기존 complete 검증 통과 (27-PATTERNS complete_analysis 914-918 메모).
    2. `complete_analysis` 호출 직후 **같은 BackgroundTask 안에서** (분석 간 SERIAL 불변 — 다음 분석은 이 task 종료 후, 주석 박제): try로 zoom 렌더(기존 `_attach_fault_zoom_comparisons` 내부 렌더 로직 재사용 — result 부착 대신 comparisons 리스트 반환하도록 최소 분리) → `firestore_admin.update_analysis_fault_zoom(uid, analysis_id, comparisons, status=models.FAULT_ZOOM_STATUS_DONE)`. except → `update_analysis_fault_zoom(uid, analysis_id, [], status=FAULT_ZOOM_STATUS_FAILED)` + log.warning (graceful — 부가 기능 실패는 분석 비차단 규율). **failed write 자체가 실패하면 log.exception만** (분석은 이미 complete — 앱은 pending 고아 시 27-07의 시간 상한 폴백이 방어. 주석 박제).
    3. zoom 사후 렌더가 `local_video_path`/`reference_local_video_path`를 참조하므로 outer finally의 unlink는 zoom 렌더 **이후** 도달하도록 순서 확인 (27-05의 join→close→unlink 순서에 zoom이 complete 후·finally 전 본문에 위치 — 구조상 자동 충족, 조기 raise 경로에서 zoom은 실행되지 않음을 주석으로 명시).
    4. D-03 경계 박제: zoom update 이외의 어떤 `result.*` 필드도 complete 후 write하는 코드 경로 금지 — `update_analysis_fault_zoom`만 사용 (grep 게이트).
    5. stage-timing: `fault_zoom` 단계 키가 사후 경로로 이동해도 로그는 계속 방출 (timingsMs dict는 complete에 이미 실림 — 사후 zoom 소요는 로그 라인으로만. 주석 박제).
    6. test_fault_zoom_deferred.py에 오케스트레이션 테스트 추가: 렌더 성공 경로 = complete 후 update(done) 1회 / 렌더 예외 경로 = update(failed) 호출 (pipeline 모듈 함수 단위 mock — 기존 tests/test_pipeline_* mock 관례).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && PYTHONPATH=shared/python:. python3 -m pytest tests/test_fault_zoom_deferred.py tests/ -q -k "fault_zoom or pipeline" --no-header</automated>
  </verify>
  <acceptance_criteria>
    - 위 pytest exit 0, 신규 FAILED/ERROR 0
    - `rtk grep -c "update_analysis_fault_zoom" backend/functions/pipeline/app.py` ≥ 2 (done + failed 경로)
    - `rtk grep -c "FAULT_ZOOM_STATUS_PENDING" backend/functions/pipeline/app.py` ≥ 1
    - complete_analysis 호출 이후 라인에서 `result[` 직접 mutation grep 0 (zoom update 함수 경유만)
  </acceptance_criteria>
  <done>점수 먼저 complete → zoom 사후 부분 업데이트(성공 done/실패 failed) 흐름이 배선되고 pending 고아 방지 규칙이 박제된다.</done>
</task>

<task type="auto">
  <name>Task 3: 프레임 배열 재사용 — ffmpeg 재디코딩 3회→1회</name>
  <files>backend/functions/pipeline/app.py, backend/tests/test_fault_zoom_deferred.py</files>
  <read_first>
    - backend/functions/pipeline/app.py:1305-1314 (`_extract_video_analysis_inputs` — 1차 디코딩), 1713-1715 (`_build_selected_frame_pair` — 학생 재추출 + 기준 추출), 2571-2573 (`_render_fault_zoom` — 학생 3차 + 기준 재추출)
    - .planning/phases/27-1-gemini-analysis-speed-1min/27-RESEARCH.md §Pattern 7 (메모리 주의: ~190MB/영상, A5)
  </read_first>
  <action>
    1. `_extract_video_analysis_inputs`가 산출한 9fps/640px 프레임 배열을 분석-로컬로 보존 — `_VideoAnalysisInputs`(또는 반환 구조)에 frames 필드 추가 혹은 분석-로컬 dict `{video_path: frames}`. 동일 `FfmpegFrameExtractor(target_fps=9.0, max_side=640)` 파라미터 전제 — 파라미터가 다른 호출이 생기면 캐시 우회 (주석 박제).
    2. `_build_selected_frame_pair`(1713-1715)와 `_render_fault_zoom`(2571-2573)이 캐시된 프레임 재사용 — 학생 재디코딩 2회 소멸. 기준 영상은 최초 추출 1회 후 재사용하되, 메모리 부담(A5: ~190MB×2) 고려해 **학생 영상 캐시 우선, 기준 영상은 zoom 시점 1회 추출 타협 허용** (27-RESEARCH Pattern 7 명시 타협 — 실측 후 판단, 주석으로 선택 근거 기록).
    3. Lambda 폴백 경로(256MB~1GB MemorySize) 캐시 비활성 분기: RunPod 실행 여부(기존 env `RUNPOD_ANALYZE_URL` 소비 분기 또는 메모리 env)로 게이트 — Lambda에서는 기존 재추출 경로 유지.
    4. zoom 사후 분리(Task 2)와 정합: 캐시 수명은 zoom 렌더 완료까지 — finally 정리 전 참조 유지, zoom 후 명시 해제(`frames = None`)로 메모리 반환.
    5. **디코딩 횟수 behavior 테스트** (checker WARNING fix): test_fault_zoom_deferred.py에 카운팅 fake FrameExtractor(extract 호출마다 `extract_calls.append(path)` 기록) 주입 테스트 2건 — (a) RunPod 경로: 포즈→still-pair→zoom 전 구간에서 학생 영상 path의 extract 호출 수 == 1 assert, (b) Lambda 폴백 분기(action 3의 게이트 off): 기존 재추출 경로 유지 = 호출 수 > 1 assert. pipeline 모듈 함수 단위 mock — Task 2의 기존 test_pipeline_* mock 관례 따름.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && PYTHONPATH=shared/python:. python3 -m pytest tests/test_fault_zoom_deferred.py -q -k "decode or extract_calls" --no-header && PYTHONPATH=shared/python:. python3 -m pytest tests/ -q --no-header 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_fault_zoom_deferred.py -k "decode or extract_calls"` exit 0, 최소 2 passed — 학생 디코딩 1회(RunPod 경로) + 폴백 재추출 유지(Lambda 경로)를 카운팅 fake로 증명
    - backend full pytest — 기준선 대비 신규 FAILED/ERROR 0
    - `_build_selected_frame_pair`/`_render_fault_zoom` 호출 경로에 프레임 재사용 배선 존재: `rtk grep -c "frames" backend/functions/pipeline/app.py` 증가분에서 캐시 파라미터 전달 확인 (executor가 diff로 확인)
  </acceptance_criteria>
  <done>학생 영상 ffmpeg 디코딩이 분석당 1회로 줄고 Lambda 폴백 분기가 보존된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline → Firestore (complete 후 write) | 사후 부분 쓰기가 새로 열리는 표면 — 실패 시 앱 상태 불일치 |
| 앱 onSnapshot ← faultZoomStatus | pending 고아 = 앱 무한 로딩 위험 (planning_context 명시 위협) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-17 | DoS | zoom 부분 쓰기 실패 → 앱 무한 로딩 | mitigate | 렌더 실패 = failed write (Test), failed write 실패 = log.exception + 27-07 앱 시간 상한 폴백. pending은 대상 존재 시에만 마킹 |
| T-27-18 | Tampering | complete 후 점수 필드 사후 변경 (D-03 위반) | mitigate | 사후 write는 update_analysis_fault_zoom 단일 경로 — result.faultZoom* 외 field-path 금지 (grep 게이트 + scoped validator) |
| T-27-19 | Tampering | nested-array가 부분 update로 우회 유입 | mitigate | `_validate_dict_only_scalars` 루프 (본체 무수정, scoped 재사용) — Test 2로 고정 |
| T-27-20 | DoS | 프레임 캐시 메모리 (A5 ~190MB×2 → OOM) | mitigate | 학생만 캐시 타협 옵션 + Lambda 폴백 분기 캐시 비활성 + zoom 후 명시 해제 |
| T-27-SC | Tampering | 패키지 설치 | accept | 신규 패키지 0 |
</threat_model>

<verification>
- test_fault_zoom_deferred.py green (계약 + 오케스트레이션) + backend full pytest 기준선 유지
- 3-way lockstep grep 게이트 (analysis.ts / models.py+firestore_admin / contract.md)
- app typecheck exit 0
</verification>

<success_criteria>
- 점수 먼저 도착 → zoom 사후 도착 흐름이 계약·persistence·오케스트레이션 전 층에서 배선됨
- pending 고아/사후 점수 변경/nested-array 3대 위험이 기계 게이트로 차단됨
- ffmpeg 학생 재디코딩 1회화
</success_criteria>

<output>
Create `.planning/phases/27-1-gemini-analysis-speed-1min/27-06-SUMMARY.md` when done
</output>
