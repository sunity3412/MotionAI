---
phase: 27-1-gemini-analysis-speed-1min
plan: 07
type: execute
wave: 7
depends_on: ["27-06"]
files_modified:
  - app/src/app/analysis/result.tsx
  - app/src/app/analysis/loading.tsx
autonomous: true
requirements: [SPD-04, SPD-05]
tags: [app, ux, loading, result, ota]

must_haves:
  truths:
    - "faultZoomStatus='pending'일 때 결과 화면 확대카드 자리에 로딩 상태가 표시되고, zoom 도착(onSnapshot) 시 자동 전환된다 (D-06)"
    - "'failed'/필드 부재(legacy)는 기존 graceful 숨김 동작 그대로다"
    - "pending이 비정상적으로 오래 지속되면(시간 상한) 로딩 대신 숨김으로 폴백한다 — 무한 로딩 0"
    - "분석 대기 화면에 폴스포츠 팁/동작 소개 텍스트 로테이션이 표시된다 (D-07 v1)"
    - "진행률 배분이 실측 타이밍(27-TIMING-BEFORE.md) 기반으로 재배분되되 Math.max 단조 로직은 무변경이다 (D-02)"
  artifacts:
    - path: "app/src/app/analysis/result.tsx"
      provides: "zoom pending placeholder 분기 + 시간 상한 폴백"
      contains: "faultZoomStatus"
    - path: "app/src/app/analysis/loading.tsx"
      provides: "폴스포츠 팁 로테이터 + PROGRESS_PCT/CEIL 재배분"
      contains: "POLE_TIPS"
  key_links:
    - from: "app/src/app/analysis/result.tsx"
      to: "app/src/types/analysis.ts faultZoomStatus"
      via: "onSnapshot 구독(useAnalysisDoc) — 추가 폴링 0"
      pattern: "faultZoomStatus"
    - from: "app/src/app/analysis/loading.tsx"
      to: "PROGRESS_PCT 단조 로직"
      via: "값만 재배분, Math.max 로직 무변경 (Pitfall 5)"
      pattern: "Math\\.max"
---

<objective>
앱 대기 경험 (D-02/D-06/D-07) — 결과 화면의 zoom pending placeholder, 로딩 화면의 폴스포츠 팁 텍스트 로테이션, 실측 기반 진행률 재배분. (시나리오 2 — 대기 경험. 피드백의 본질 = "오래 걸리는데 아무 조치/변화가 없다"는 체감 — D-02.)

**전제: Phase 26 실행 완료 후 착수.** loading.tsx는 26-03/26-04와, result.tsx는 26-02(wrapper/child 분리 예정)와 파일이 겹친다 — Phase 26 미실행 상태로 착수 시 충돌 파일 상태를 재확인하고 orchestrator에 보고할 것 (CONTEXT canonical_refs 명시).

Purpose: 대기 중 "변화가 보이는" 체감 + 점수 선도착 UX 완성.
Output: JS-only 변경 — **OTA 가능** (네이티브 모듈 추가 0, EAS 재빌드 불필요).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-1-gemini-analysis-speed-1min/27-CONTEXT.md
@.planning/phases/27-1-gemini-analysis-speed-1min/27-PATTERNS.md
@.planning/phases/27-1-gemini-analysis-speed-1min/27-TIMING-BEFORE.md
@app/src/types/analysis.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: result.tsx — zoom pending placeholder + 시간 상한 폴백</name>
  <files>app/src/app/analysis/result.tsx</files>
  <read_first>
    - app/src/app/analysis/result.tsx:905-917 (`selectedZoom` useMemo — `result.faultZoomComparisons ?? []` graceful 소비), 1716 (`zoom={selectedZoom}` 소비처)
    - app/src/types/analysis.ts — 27-06이 넣은 `faultZoomStatus?: 'pending' | 'done' | 'failed'` + 하위호환 주석
    - .planning/phases/27-1-gemini-analysis-speed-1min/27-PATTERNS.md §result.tsx (Phase 26 26-02 파일 겹침 주의)
  </read_first>
  <action>
    1. **선행 확인:** Phase 26(26-02)의 result.tsx wrapper/child 분리 반영 상태 확인 — 분리됐다면 zoom 소비 child 컴포넌트에 작업, 미분리면 현행 구조에 작업 (라인 번호는 26 실행으로 이동됐을 수 있음 — `selectedZoom` 심볼 기준으로 위치 재탐색).
    2. pending 분기: `selectedRecord.result.faultZoomStatus === 'pending'`이면 확대카드 자리에 로딩 placeholder 렌더 — 기존 카드 컨테이너 스타일 재사용 + `ActivityIndicator` + 안내 카피 (예: '확대 비교 이미지를 준비하고 있어요') — 한국어, 이모지 금지, theme 토큰만 (colors/spacing/radius — 하드코딩 금지), 라이트 테마.
    3. 시간 상한 폴백 (pending 고아 방어 — T-27-17 짝): doc `updatedAt` 기준 경과가 상한(180초, 상수 `FAULT_ZOOM_PENDING_TIMEOUT_MS` — zoom 렌더 실측 상회 보수값, 주석에 근거)을 넘으면 placeholder 대신 기존 숨김 처리. 타이머 훅은 early return 이전 배치 (hook 규칙).
    4. 'failed'/'done'/필드 부재: 기존 동작 그대로 — `selectedZoom` null이면 숨김 (graceful, 코드 변경 최소). 'done'은 onSnapshot rerender로 자동 반영 — 추가 폴링 코드 금지 (안티패턴).
    5. 주석에 spec 인용: `// Phase 27 D-06 — zoom 사후 도착. contract.md faultZoomStatus 절.`
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && npm run typecheck` exit 0
    - `rtk grep -c "faultZoomStatus" app/src/app/analysis/result.tsx` ≥ 2 (pending 분기 + 상한 폴백)
    - `rtk grep -c "setInterval\|fetch(" app/src/app/analysis/result.tsx` 중 zoom 관련 신규 폴링 0 (onSnapshot만 — executor가 diff로 확인)
    - `rtk grep -c "#FF4B33\|#FFFFFF" app/src/app/analysis/result.tsx` 신규 하드코딩 증가 0 (theme 토큰만)
  </acceptance_criteria>
  <done>pending 로딩 표시 → zoom 자동 도착 전환 + 시간 상한 폴백이 배선되고 typecheck green이다.</done>
</task>

<task type="auto">
  <name>Task 2: loading.tsx — 폴스포츠 팁 로테이터 (D-07) + 진행률 재배분 (D-02)</name>
  <files>app/src/app/analysis/loading.tsx</files>
  <read_first>
    - app/src/app/analysis/loading.tsx:51-56 (`REASSURANCE_COPIES` 상수 + `COPY_ROTATE_MS`), 362-369 (로테이터 hook — 정본 복제 대상), 360-361 (hook 순서 규칙 주석), 62-86 (`PROGRESS_PCT`/`PROGRESS_CEIL`/`PROGRESS_CREEP_MS`), 374-388 (`Math.max` 단조 로직 — 무변경), 507-519 (분석 중 렌더 — `stepLine` 아래 삽입 지점), 40-41 (NAVY_TOP/NAVY_BOT — navy 예외 화면), 556-557 (음수 letterSpacing 금지 iOS SIGABRT 주석)
    - .planning/phases/27-1-gemini-analysis-speed-1min/27-TIMING-BEFORE.md — 실측 단계별 배분 (진행률 재배분 근거)
  </read_first>
  <action>
    1. **선행 확인:** Phase 26(26-03/26-04) 반영으로 라인 이동 가능 — `REASSURANCE_COPIES`/`PROGRESS_PCT` 심볼 기준 재탐색.
    2. D-07 팁 로테이터: `REASSURANCE_COPIES` 패턴의 **두 번째 인스턴스** — `const POLE_TIPS: readonly string[] = [...] as const` (폴스포츠 팁/동작 소개/재미 문구 8~12개, Claude 재량 작성 — D-07 명시 재량. 규칙: 한국어, 이모지 금지, 단정 금지(부상·근력 단정 카피 금지 — SAFE 규율 정합), 동작 소개는 학원 통용 명칭 사용) + `TIP_ROTATE_MS = 6000` (기존 4000과 어긋나게 — 두 로테이터 동시 점프 방지, 주석 박제) + 별도 `useState`/`useEffect` interval 훅. **모든 early return 이전에 훅 배치** (360-361 규칙). 렌더 삽입 지점 = 분석 중 렌더(507-519) `stepLine` 아래 — navy 배경 위 가독 색상은 기존 텍스트 스타일 준수, 음수 letterSpacing 금지.
    3. D-02 진행률 재배분: `PROGRESS_PCT`/`PROGRESS_CEIL` **값만** 27-TIMING-BEFORE.md 실측 배분 + wave 3~6 반영 후 기대 분포(비전 구간이 포즈 그늘로 숨음 → comparison 구간 체감 단축)로 조정 — 27-05의 status 시점 교정과 정합 (frame_extraction/pose_analysis/comparison 각 단계의 실제 길이 근사). `Math.max` 단조 로직(374-388)과 `PROGRESS_CREEP_MS` creep 메커니즘은 무변경 (85% 멈춤 fix 이력 보존 — Pitfall 5). 각 값 옆에 근거 주석 (`// 27-TIMING-BEFORE 실측: ...`).
    4. 스타일: theme 토큰만, Pretendard, navy 예외 화면 규칙 유지.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && npm run typecheck` exit 0
    - `rtk grep -c "POLE_TIPS" app/src/app/analysis/loading.tsx` ≥ 2 (상수 + 렌더)
    - `rtk grep -c "Math.max" app/src/app/analysis/loading.tsx` ≥ 1 (단조 로직 잔존)
    - POLE_TIPS 배열 항목 수 8 이상 (rtk read로 확인)
  </acceptance_criteria>
  <done>대기 화면에 팁 로테이션이 돌고 진행률이 실측 기반으로 재배분되며 단조 로직이 보존된다. belle 실기기 체감 확인은 phase 수동 검증 항목 (27-VALIDATION Manual-Only).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firestore doc → 앱 렌더 | faultZoomStatus 신규 필드 소비 — 악성/결손 doc에 대한 방어적 소비 필요 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-21 | DoS | pending 고아 → 앱 무한 로딩 (planning_context 명시 위협) | mitigate | FAULT_ZOOM_PENDING_TIMEOUT_MS 시간 상한 폴백 (updatedAt 기준) — Task 1 |
| T-27-22 | Tampering | 비정상 faultZoomStatus 값 (계약 외 문자열) | mitigate | string-literal union 밖 값은 기존 숨김 경로로 수렴 (pending 정확 일치만 placeholder) — 방어적 소비 |
| T-27-SC | Tampering | 패키지 설치 | accept | 신규 패키지 0 (기존 RN 컴포넌트만) — OTA 가능의 전제 |
</threat_model>

<verification>
- `cd app && npm run typecheck` exit 0
- grep 게이트: faultZoomStatus 분기 / POLE_TIPS / Math.max 잔존 / theme 하드코딩 0
- belle 실기기 확인(수동): pending→done 전이 체감, 팁 로테이션, 진행률 전진 — 27-09 phase gate 이후 27-VALIDATION Manual-Only 절차
</verification>

<success_criteria>
- 결과 화면: 점수 즉시 + zoom 뒤따라 도착 UX 완성 (무한 로딩 0)
- 로딩 화면: "변화가 보이는" 대기 (팁 로테이션 + 실측 진행률)
- JS-only — OTA 배포 가능
</success_criteria>

<output>
Create `.planning/phases/27-1-gemini-analysis-speed-1min/27-07-SUMMARY.md` when done
</output>
