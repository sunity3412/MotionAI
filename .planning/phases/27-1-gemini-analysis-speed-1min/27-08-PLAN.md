---
phase: 27-1-gemini-analysis-speed-1min
plan: 08
type: execute
wave: 8
depends_on: ["27-05", "27-06"]
files_modified:
  - .planning/phases/27-1-gemini-analysis-speed-1min/27-FLASH-DECISION.md
autonomous: false
requirements: [SPD-07]
tags: [pod, gemini, flash, eval, conditional]

must_haves:
  truths:
    - "Pro→Flash 전환은 EVAL18 순차 대조에서 verdict·점수·faults 동일할 때만 채택된다 (D-05) — 미통과 시 기각이 정상 종료"
    - "실험은 env override로만 수행되어 프로덕션 캐시를 오염시키지 않는다 (TechniqueCache 키에 model명 포함)"
    - "채택/기각 근거가 기록으로 박제된다 (D-05 명시: SUMMARY 기록)"
    - "veto fan-out은 기본 보류 — 점수 직결 경로라 완전 동일 증명 없이는 전환하지 않는다"
  artifacts:
    - path: ".planning/phases/27-1-gemini-analysis-speed-1min/27-FLASH-DECISION.md"
      provides: "후보별 대조 결과 + 채택/기각 판정 + 근거"
      min_lines: 20
  key_links:
    - from: "27-FLASH-DECISION.md"
      to: "Pod start_server.sh (채택 시)"
      via: "GEMINI_*_MODEL env override — 코드 변경 0"
      pattern: "gemini-3.5-flash"
---

<objective>
[POD] Pro→Flash 조건부 전환 실험 (D-05) — moment extractor(1순위)·coach B(2순위)를 env override로 Flash로 바꿔 EVAL18 순차 대조를 돌리고, verdict·점수·faults **동일**일 때만 채택한다. (시나리오 2 — 대기 경험. 조건부 별도 플랜 — 미통과 시 기각이 정상 종료.)

과거 실측 "video split 판정 Flash≈Pro"는 split 단일 판정 — 3-scope 짚기 전체의 등가 증거가 아니므로 veto fan-out은 기본 보류 (27-RESEARCH Flash 표). 모델 string은 확정값만: Pro=`gemini-3.1-pro-preview`, Flash=`gemini-3.5-flash` (2.5 계열 금지).

Purpose: 모델-측 레이턴시 추가 절감 — 단, D-01(무회귀)이 상위.
Output: 27-FLASH-DECISION.md + (채택 시) Pod env 반영. Pod 실작업 — belle 승인 선행 (autonomous: false). OTA 무관(백엔드/Pod).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-1-gemini-analysis-speed-1min/27-RESEARCH.md
@.planning/phases/27-1-gemini-analysis-speed-1min/27-TIMING-BEFORE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: belle 승인 — Flash 실험 착수 + 크레딧 확인</name>
  <action>checkpoint — 아래 how-to-verify 항목(크레딧/범위/절차)을 belle에게 제시하고 승인 응답을 대기한다. 승인 전 후속 태스크 착수 금지 (Pod 실작업 = belle 승인 선행 규율).</action>
  <done>belle 승인(또는 skip) 수신 후에만 Task 2 진행 — resume-signal 수신.</done>
  <what-built>wave 3~6 최적화가 로컬 green 상태. Flash 후보 2종(moment extractor, coach B)의 EVAL18 대조 실험 준비 완료.</what-built>
  <how-to-verify>
    1. Gemini 크레딧 잔액 확인 (대조 = 후보당 EVAL18 페어 세트 1회씩 추가 소요 — 고갈 이력)
    2. 실험 범위 승인: 1순위 moment extractor만 / 1+2순위(coach B 포함) 중 택
    3. veto fan-out은 이번 실험 제외(기본 보류)임을 확인 — 점수 직결 경로 (27-RESEARCH Flash 표)
  </how-to-verify>
  <resume-signal>"approved" (범위 지정: "approved 1순위만" / "approved 1+2순위") 또는 "skip" (실험 자체 보류 — plan 기각 종료)</resume-signal>
</task>

<task type="auto">
  <name>Task 2: env override EVAL18 대조 → 채택/기각 판정 박제</name>
  <files>.planning/phases/27-1-gemini-analysis-speed-1min/27-FLASH-DECISION.md</files>
  <read_first>
    - .planning/phases/27-1-gemini-analysis-speed-1min/27-RESEARCH.md §Flash 전환 대상 선별 (후보 순위 + 채택 절차 + 캐시 오염 0 근거)
    - backend/shared/python/sunity_shared/gemini/config.py — 모델 string 단일 출처 + `GEMINI_*_MODEL` env override 기존 패턴
    - backend/evals/phase18/assert_baseline.py + backend/scripts/sweep_phase15.py — 무회귀 대조 커맨드
  </read_first>
  <action>
    Pod SSH에서 Claude가 직접 실행 (pod-ops 규율 — belle 콘솔 떠넘기기 금지). 후보별 절차 (D-05 고정):
    1. 대상 env 확인: moment extractor/coach B의 모델 override env 키를 `gemini/config.py`에서 확정 — 기존 `GEMINI_*_MODEL` 패턴만 사용, 신규 env 발명 금지.
    2. 후보 1(moment extractor): Pod에서 해당 env를 `gemini-3.5-flash`로 override → EVAL18 SERIAL 순차 대조 (`sweep_phase15.py --pair-sequential`, EVAL_OUT_DIR 리포 밖, 분석 간 동시 실행 금지) → `assert_baseline.py`로 verdict·점수·faults 대조. TechniqueCache 키에 model명 포함 → 전환 실험 자동 cache-miss = 프로덕션 캐시 오염 0 (기록).
    3. 판정: **완전 동일 시만 채택** — 채택이면 Pod `start_server.sh`에 env export 라인 추가(setdefault 박제 규율) + 서버 재시작 + `/health` 확인. 하나라도 다르면 기각 — env 원복, 기각 사유(어느 페어·어느 필드 diff) 기록. **기각 = 정상 종료** (실패 아님, D-05).
    4. 후보 2(coach B, 승인 범위 내): 동일 절차 + 대조 항목에 코칭 문장 스키마/tone validator 통과 여부 포함 (Cerebras cross-fill 폴백 존재 — 회귀 표면 제한적이나 게이트는 동일).
    5. `27-FLASH-DECISION.md` 작성: 후보별 (모델 string, 대조 페어 수, verdict/점수/faults diff 결과, 레이턴시 델타, 채택/기각 + 근거). SUMMARY에도 요약 (D-05 명시 요구).
  </action>
  <verify>
    <automated>test -f /Users/kimtaesung/Dev/SunityMotion/.planning/phases/27-1-gemini-analysis-speed-1min/27-FLASH-DECISION.md && rtk grep -c "gemini-3.5-flash" /Users/kimtaesung/Dev/SunityMotion/.planning/phases/27-1-gemini-analysis-speed-1min/27-FLASH-DECISION.md</automated>
  </verify>
  <acceptance_criteria>
    - 27-FLASH-DECISION.md 존재, 후보별 판정(채택/기각) 라인 존재 (`rtk grep -c "채택\|기각" ...27-FLASH-DECISION.md` ≥ 1)
    - `rtk grep -c "gemini-2.5" ...27-FLASH-DECISION.md` == 0 (2.5 계열 금지)
    - 채택 시: Pod start_server.sh diff 기록 + /health ok 증빙이 파일에 포함
    - 기각 시: diff 필드/페어 명시 — "동일하지 않아 기각"의 기계 근거 존재
  </acceptance_criteria>
  <done>Flash 후보별 채택/기각이 EVAL18 완전 동일 기준으로 판정·박제되고, 채택분만 Pod env에 반영된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval 실험 → 프로덕션 Pod env | env override 실험이 프로덕션 설정에 잔류할 위험 |
| Flash 응답 → 점수 경로 | 모델 전환이 인식/코칭 품질에 회귀 유발 가능 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-23 | Tampering | 모델 전환 정확도 회귀 | mitigate | EVAL18 verdict·점수·faults 완전 동일 게이트 — 부분 유사 채택 금지 (D-05). veto는 기본 보류 |
| T-27-24 | Tampering | 실험 env 잔류 (기각 후 원복 누락) | mitigate | 기각 경로에 env 원복 명시 + start_server.sh diff를 DECISION 파일에 기록 |
| T-27-25 | Tampering | 실험이 프로덕션 캐시 오염 | mitigate | TechniqueCache 키에 model명 포함 — 자동 네임스페이스 분리 (근거 기록) |
| T-27-SC | Tampering | 패키지 설치 | accept | 신규 패키지 0 |
</threat_model>

<verification>
- 27-FLASH-DECISION.md에 후보별 판정 + 기계 근거 존재
- 기각 시 Pod env 원복 확인, 채택 시 start_server.sh 반영 + /health ok
</verification>

<success_criteria>
- D-05 절차(완전 동일 시만 채택)가 지켜지고 근거가 SUMMARY에 박제됨 — 채택/기각 어느 쪽도 성공
</success_criteria>

<output>
Create `.planning/phases/27-1-gemini-analysis-speed-1min/27-08-SUMMARY.md` when done
</output>
