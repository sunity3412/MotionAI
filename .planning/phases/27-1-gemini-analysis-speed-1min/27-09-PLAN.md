---
phase: 27-1-gemini-analysis-speed-1min
plan: 09
type: execute
wave: 9
depends_on: ["27-07", "27-08"]
files_modified:
  - .planning/phases/27-1-gemini-analysis-speed-1min/27-TIMING-AFTER.md
autonomous: false
requirements: [SPD-06]
tags: [pod, eval, gate, no-regression, deploy]

must_haves:
  truths:
    - "EVAL18 6페어 SERIAL cold sweep에서 점수·verdict·faults가 baseline 대비 무회귀다 (D-01 hard gate)"
    - "cold/warm 결정론이 성립한다 — 같은 입력 = 같은 출력 (병렬화 후에도)"
    - "before/after stage-timing 표가 존재하고 after는 cold run(cacheHit=false)이다"
    - "veto completedCalls==plannedCalls — 병렬화로 인한 429/부분완료가 정확도 회귀로 전이되지 않았다"
    - "학생 영상 File API 업로드가 분석당 1회임이 프로덕션 로그로 확인된다 (누수 0)"
    - "신규 env(GEMINI_UPLOAD_PREFETCH, GEMINI_FANOUT_WORKERS)가 Pod start_server.sh에 박제되고 canary/rollback 절차가 선기록되어 있다"
  artifacts:
    - path: ".planning/phases/27-1-gemini-analysis-speed-1min/27-TIMING-AFTER.md"
      provides: "after 타이밍 표 + before 대조 + 게이트 판정 증빙 (무회귀/결정론/completedCalls/업로드 카운트)"
      min_lines: 40
  key_links:
    - from: "27-TIMING-AFTER.md"
      to: "27-TIMING-BEFORE.md"
      via: "동일 페어·동일 단계 키 대조 표 (D-01 절감 증명)"
      pattern: "elapsed_ms|timingsMs"
---

<objective>
[POD] Phase gate — 전체 최적화를 Pod에 배포하고 D-01 hard gate를 판정한다: EVAL18 SERIAL cold sweep 무회귀 + cold/warm 결정론 + before/after stage-timing 표. (시나리오 2 — 대기 경험. 시간은 "가능한 범위 최대 절감"이 목표치 — 1분은 지향점이지 hard 아님, belle 원문 D-01.)

Phase 22 DR-02 교훈 준수: production Pod mutation은 canary/rollback 선기록 + belle 승인 뒤. 신규 env는 setdefault 박제 + start_server.sh 갱신 (git 밖 — Pitfall 6).

Purpose: phase 전체의 성패 판정 — 이 게이트 통과가 phase 완료 조건.
Output: 27-TIMING-AFTER.md + Pod 프로덕션 반영. Pod 실작업 — belle 승인 선행 (autonomous: false).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-1-gemini-analysis-speed-1min/27-VALIDATION.md
@.planning/phases/27-1-gemini-analysis-speed-1min/27-TIMING-BEFORE.md
@.planning/phases/27-1-gemini-analysis-speed-1min/27-FLASH-DECISION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: belle 승인 — 프로덕션 배포 + gate sweep (canary/rollback 선기록 제시)</name>
  <action>checkpoint — 아래 how-to-verify 항목(크레딧/범위/절차)을 belle에게 제시하고 승인 응답을 대기한다. 승인 전 후속 태스크 착수 금지 (Pod 실작업 = belle 승인 선행 규율).</action>
  <done>belle 승인 수신 후에만 Task 2/3 진행 — resume-signal 수신.</done>
  <what-built>wave 1~8 전체(계측/핸들 세션/inline/병렬화/zoom 사후 분리/앱 대기 경험/Flash 판정)가 로컬 green + full pytest 기준선 유지 상태.</what-built>
  <how-to-verify>
    1. Gemini 크레딧 잔액 확인 (6페어 × cold 2회 상당 — before는 27-02에 있으므로 after cold 1세트 + warm 재실행 1세트)
    2. canary/rollback 선기록 검토 (Phase 22 DR-02 교훈):
       - rollback 레버 1: `GEMINI_UPLOAD_PREFETCH=0` + `GEMINI_FANOUT_WORKERS=1` (env만으로 순차 경로 복귀 — 코드 롤백 불요)
       - rollback 레버 2: git revert 대상 커밋 해시 목록 (wave 3~6) + Pod pull + 서버 재시작 절차
       - canary: gate sweep 자체가 canary — 통과 전 실사용자 트래픽에 신 코드 노출 최소화 (배포 직후 sweep, 시간대 belle 합의)
    3. 배포 범위 승인: Pod pull 대상 커밋 (27-08까지 반영분)
  </how-to-verify>
  <resume-signal>"approved" 또는 이슈 서술</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Pod 배포 + 신규 env 박제 + 헬스 확인</name>
  <files>.planning/phases/27-1-gemini-analysis-speed-1min/27-TIMING-AFTER.md</files>
  <read_first>
    - backend/evals/phase25/run_sweep.py:79-93 — env setdefault 주석 관례 (start_server.sh 기재 형식 참고)
    - .planning/phases/27-1-gemini-analysis-speed-1min/27-RESEARCH.md §Pitfall 6 (start_server.sh는 git 밖 — 미주입 시 무음 비활성)
  </read_first>
  <action>
    1. 로컬 최종 커밋 push (GSD Pod 작업 = push 한 단위) → Pod SSH → pull → 승인 커밋에 checkout.
    2. `start_server.sh`에 신규 env 박제: `export GEMINI_UPLOAD_PREFETCH=1`, `export GEMINI_FANOUT_WORKERS=4` (+ 27-08 채택분 GEMINI_*_MODEL). 기존 VETO=1/GEMINI_MAX_VETO_WALL_S=300 박제 라인 무변경. 변경 diff를 27-TIMING-AFTER.md에 기록 (git 밖 파일 — 증빙은 문서로).
    3. 서버 재시작 → `/health` ok + `X-RunPod-Token` 인증 경로 정상 확인.
    4. 스모크 1건: 캐시 무관 짧은 분석 1회 실행 — stage_timing 로그 방출 + 세션 delete 로그(누수 0) + 오류 0 확인 후 gate로 진행.
  </action>
  <verify>
    <automated>test -f /Users/kimtaesung/Dev/SunityMotion/.planning/phases/27-1-gemini-analysis-speed-1min/27-TIMING-AFTER.md && rtk grep -c "GEMINI_UPLOAD_PREFETCH" /Users/kimtaesung/Dev/SunityMotion/.planning/phases/27-1-gemini-analysis-speed-1min/27-TIMING-AFTER.md</automated>
  </verify>
  <acceptance_criteria>
    - 27-TIMING-AFTER.md에 start_server.sh diff + /health ok + 배포 커밋 해시 기록
    - `rtk grep -c "GEMINI_UPLOAD_PREFETCH\|GEMINI_FANOUT_WORKERS" ...27-TIMING-AFTER.md` ≥ 2
  </acceptance_criteria>
  <done>신 코드 + env가 Pod에 반영되고 헬스/스모크가 확인된다.</done>
</task>

<task type="auto">
  <name>Task 3: D-01 gate sweep — 무회귀 + 결정론 + before/after 표</name>
  <files>.planning/phases/27-1-gemini-analysis-speed-1min/27-TIMING-AFTER.md</files>
  <read_first>
    - backend/scripts/sweep_phase15.py (`--pair-sequential`) + backend/evals/phase18/assert_baseline.py — 무회귀 하니스 (신규 발명 금지 — Don't Hand-Roll)
    - backend/evals/phase25/ — `check_cold_warm_determinism` 패턴 (phase25 계보)
    - .planning/phases/27-1-gemini-analysis-speed-1min/27-VALIDATION.md §Per-Task Verification Map + §불변 제약
  </read_first>
  <action>
    전부 SERIAL (분석 간 동시 실행 절대 금지 — invariant). 순서:
    1. **cold sweep**: 27-02와 동일한 cold 격리 절차(PROMPT_VERSION bump 금지)로 EVAL18 6페어 cold 실행 → `assert_baseline.py` 대조 — 점수·verdict·faults 무회귀 (D-01 hard gate). 각 run `telemetry.cacheHit=false` + `completedCalls==plannedCalls`(429/부분완료 검출 — Pitfall 7) 기록. 429 검출 시 `GEMINI_FANOUT_WORKERS=2`로 낮춰 재실행하고 최종 채택값 기록.
    2. **warm 재실행**: 같은 페어 warm 1세트 → cold/warm 최종 점수 결정론 대조 (phase25 `check_cold_warm_determinism` 패턴 재사용).
    3. **업로드 카운트**: sweep 로그에서 분석당 학생 영상 `files.upload` 1회 + 세션 delete 로그 확인 (SPD-02 누수 0의 프로덕션 증빙).
    4. **before/after 표**: 27-TIMING-BEFORE.md와 동일 페어·동일 단계 키로 대조 표 작성 — 단계별 델타 + 총 벽시계 델타 + zoom 사후 도착까지의 추가 시간. 절감이 미미하거나 특정 단계가 역행하면 원인 분석을 표에 병기 (게이트는 무회귀가 hard, 시간은 보고 — D-01).
    5. **판정 기록**: PASS/FAIL 명시. FAIL(무회귀 위반) 시 Task 1의 rollback 레버 즉시 적용(env 순차 복귀 우선) + 원인 gap을 SUMMARY에 박제하고 gap-closure로 회부 — 회귀 상태로 프로덕션 방치 금지 (fix-now 규율).
    6. belle 실기기 수동 확인 항목 안내를 SUMMARY에 포함 (27-VALIDATION Manual-Only: zoom pending→done 전이, 팁 로테이션, 진행률 전진 — /gsd-verify-work에서 수행).
  </action>
  <verify>
    <automated>rtk grep -c "cacheHit\|completedCalls" /Users/kimtaesung/Dev/SunityMotion/.planning/phases/27-1-gemini-analysis-speed-1min/27-TIMING-AFTER.md</automated>
  </verify>
  <acceptance_criteria>
    - 27-TIMING-AFTER.md에 6페어 무회귀 판정(assert_baseline 결과) + cold/warm 결정론 결과 + before/after 표 존재
    - `rtk grep -c "cacheHit" ...27-TIMING-AFTER.md` ≥ 1 (after cold 증빙), `rtk grep -c "completedCalls" ...` ≥ 1
    - `rtk grep -c "PASS\|FAIL" ...27-TIMING-AFTER.md` ≥ 1 (명시 판정)
    - FAIL 시: rollback 적용 기록 존재 — 회귀 상태 방치 0
  </acceptance_criteria>
  <done>D-01 hard gate(무회귀+결정론)가 판정·박제되고 before/after 절감이 표로 증명되며, FAIL 시 rollback이 즉시 적용된 상태다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로컬 코드 → 프로덕션 Pod | 실사용자 분석 경로에 최적화 일괄 반영 |
| gate 판정 → phase 완료 | 게이트 우회/약화 시 정확도 회귀가 파일럿에 노출 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-26 | Tampering | 최적화 누적 회귀 (점수·verdict·faults) | mitigate | EVAL18 6페어 cold 무회귀 hard gate + cold/warm 결정론 + FAIL 시 env rollback 레버 즉시 적용 (D-01) |
| T-27-27 | DoS | 429/부분완료가 resource_limited 증가로 전이 | mitigate | completedCalls==plannedCalls 게이트 + GEMINI_FANOUT_WORKERS 축소 폴백 재실행 |
| T-27-28 | DoS | 배포 직후 실사용자 노출 (canary 부재) | mitigate | 배포→즉시 gate sweep 시간대 belle 합의 + rollback 절차 선기록 (Phase 22 DR-02) |
| T-27-29 | DoS | 신규 env 미주입 무음 비활성 (start_server.sh git 밖) | mitigate | env 박제를 명시 태스크 + diff 문서 증빙 (Pitfall 6) |
| T-27-SC | Tampering | 패키지 설치 | accept | 신규 패키지 0 |
</threat_model>

<verification>
- 27-TIMING-AFTER.md: 무회귀 판정 + 결정론 + before/after 표 + cold 증빙 + 업로드 카운트
- PASS 시 phase gate 충족 → /gsd-verify-work (belle 실기기 manual 항목 포함)
</verification>

<success_criteria>
- D-01 hard gate PASS: EVAL18 6페어 점수·verdict·faults 무회귀 + cold/warm 결정론
- before/after 단계별 절감이 실측 표로 증명 (시간 목표는 "현실적 최대 절감" — 보고 항목)
- 신규 env Pod 박제 + rollback 레버 검증 완료
</success_criteria>

<output>
Create `.planning/phases/27-1-gemini-analysis-speed-1min/27-09-SUMMARY.md` when done
</output>
