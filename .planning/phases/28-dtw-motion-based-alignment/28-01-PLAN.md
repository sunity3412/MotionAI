---
phase: 28-dtw-motion-based-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/scripts/measure_reference_fps.py
  - backend/tests/test_motion_alignment.py
  - app/src/lib/alignmentWarp.ts
autonomous: true
requirements: [ALGN-01]
must_haves:
  truths:
    - "reference doc의 angles fps가 실측으로 확정된다 (A1 가정 해소 — 하드코딩 아님)"
    - "motion_alignment 모듈의 기대 동작이 구현 전에 테스트로 고정된다 (RED)"
    - "앱 워핑 수학(warpTime/segmentRate)이 typecheck 대상에 진입한다"
  artifacts:
    - path: "backend/scripts/measure_reference_fps.py"
      provides: "reference 11개 doc의 keypointReport.fps / frames / anglesFrames 실측 (1회성 Admin 읽기)"
    - path: "backend/tests/test_motion_alignment.py"
      provides: "build_motion_alignment 계약 테스트 (단조/초 단위/결정론/identity/fps 변환/tier 경계) — 구현 전 RED"
      min_lines: 80
    - path: "app/src/lib/alignmentWarp.ts"
      provides: "warpTime/segmentRate/normalizeMotionAlignment 순수 함수 (player 의존 0)"
      exports: ["warpTime", "segmentRate", "normalizeMotionAlignment"]
  key_links:
    - from: "backend/tests/test_motion_alignment.py"
      to: "sunity_shared.analysis.motion_alignment"
      via: "import (28-02 구현 전 RED = collection error 확인)"
      pattern: "from sunity_shared.analysis import motion_alignment"
---

<objective>
Phase 28 Wave 0 — 계측-먼저 검증 재료 3종. (1) reference doc fps 실측으로 RESEARCH A1 가정("활성 reference angles = 18fps") 해소 — 정렬 맵을 초 단위로 방출하는 계약(D-01/Claude 재량 계약설계)의 전제 검증. (2) `test_motion_alignment.py` — 28-02가 구현할 순수 모듈의 계약을 구현 전에 테스트로 고정 (RED 커밋). (3) `alignmentWarp.ts` — 앱측 워핑 수학 순수 모듈 골격+본체 (typecheck 진입).

시나리오 태그: 2단계(비교 재생 대기·싱크)+6단계(결과 비교) 작업의 기반.

**채점 무접촉:** 이 플랜은 채점 코드 0접촉 (신규 파일 + 1회성 스크립트만). veto still 경로(`_build_selected_frame_pair`, app.py:1720) 제외는 phase 전역 원칙 — 28-VALIDATION 불변 제약 참조 (belle 통지됨).

Purpose: fps 도메인 함정(RESEARCH Pitfall 1 — D2의 추가 근본 원인)을 실측으로 봉인하고, 이후 웨이브가 검증 재료 위에서만 구현하게 한다.
Output: 실측 로그 + RED 테스트 + typecheck 통과 워핑 모듈.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-dtw-motion-based-alignment/28-CONTEXT.md
@.planning/phases/28-dtw-motion-based-alignment/28-RESEARCH.md
@.planning/phases/28-dtw-motion-based-alignment/28-PATTERNS.md
@.planning/phases/28-dtw-motion-based-alignment/28-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: reference doc fps 실측 스크립트 — A1 가정 해소</name>
  <files>backend/scripts/measure_reference_fps.py</files>
  <read_first>
    - backend/scripts/backfill_reference_downstream.py:1-60 (Admin SA 초기화 + reference 컬렉션 읽기 선례, --target-fps 18.0 주석 :131)
    - backend/shared/python/sunity_shared/firestore_admin.py:1-60 (클라이언트 초기화 관례)
    - 28-RESEARCH.md Pitfall 1 (user 9fps vs ref 18fps) + Assumptions A1
  </read_first>
  <action>
    1회성 읽기 전용 스크립트 작성 (backfill_reference_downstream.py의 SA 초기화 선례 재사용, FIREBASE_SA_PATH=firebase-sa.json 로컬 SA — Pod 아님). `reference` 컬렉션 전 active doc(11개)에 대해 다음을 출력:
    - `id`, `activeVersion`, `keypointReport.fps`, `keypointReport.frames`, `anglesFrames`, 그리고 `anglesFrames / keypointReport.fps` (= angles 재생 길이 초).
    - 정합 판정 2건: (a) `anglesFrames == keypointReport.frames` (angles와 keypointReport가 같은 재처리 산출 — fault_zoom 표시 경로 fix의 clamp 도메인 전제), (b) `keypointReport.fps > 0`.
    - 판정 실패 doc이 하나라도 있으면 exit 1 + doc id 출력. 전부 통과면 "A1 RESOLVED: N docs, fps set={...}" 출력 후 exit 0.
    쓰기 작업 절대 금지 (read-only — get만 사용). 스크립트 헤더 docstring에 "Phase 28 Wave 0 1회성 실측 (28-RESEARCH A1), 재사용 가능" 명시. 실행하고 출력 전문을 SUMMARY에 박제. fps 실측값이 18.0이 아닌 doc이 있어도 초 단위 계약 설계는 유효(fps를 doc 메타에서 읽는 설계가 방어) — 결과만 기록, 판단은 (a)(b)만.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && PYTHONPATH=shared/python:. FIREBASE_SA_PATH=../firebase-sa.json python3 scripts/measure_reference_fps.py</automated>
  </verify>
  <acceptance_criteria>
    - 스크립트 exit 0, 출력에 "A1 RESOLVED" 포함 + 11개 doc 라인
    - `grep -c "set(" backend/scripts/measure_reference_fps.py` 무관 — 대신 `grep -c "\.set\|\.update\|\.delete" backend/scripts/measure_reference_fps.py` == 0 (read-only 보장)
  </acceptance_criteria>
  <done>reference 전 doc의 angles fps가 실측 기록되고 anglesFrames==keypointReport.frames 정합이 확인됨 (A1 해소)</done>
</task>

<task type="auto">
  <name>Task 2: test_motion_alignment.py — 구현 전 계약 고정 (RED)</name>
  <files>backend/tests/test_motion_alignment.py</files>
  <read_first>
    - backend/tests/test_dimensions.py:1-45 (순수 모듈 unit 표준형 — 합성 빌더 + None 검증, 28-PATTERNS "test_motion_alignment" 절)
    - backend/shared/python/sunity_shared/analysis/motiondtw.py:72-77 (MotionMatch frozen dataclass — 합성 입력 계약)
    - 28-RESEARCH.md Pattern 1 (앵커 생성 규칙) + Pattern 2 (tier 사다리) + Validation Architecture Test Map
  </read_first>
  <action>
    합성 `MotionMatch` 빌더 `_match(path, distance, start=0, end=None)` (motiondtw.MotionMatch 직접 생성)를 두고, 28-02가 구현할 `sunity_shared.analysis.motion_alignment.build_motion_alignment(match, *, user_fps, ref_fps)` 의 계약 테스트를 작성 (모듈 top-level import — 모듈 부재 동안 collection error = 의도된 RED):
    1. **None/빈 입력**: match=None 또는 path=[] → None 반환 (필드 미방출 = legacy).
    2. **초 단위 + fps 변환** (Pitfall 1 회귀 가드): user_fps=9.0, ref_fps=18.0, path=[(i, 2*i) for i in range(18)] (identity-in-time) → 모든 앵커 쌍 uSec≈rSec (허용오차 ≤ 1/9s), anchors 값이 인덱스가 아닌 초 (max ≤ 구간초+1).
    3. **단조성**: anchors u 좌표 strictly increasing, r 좌표 non-decreasing (비단조 쌍 제거됨).
    4. **결정론**: 동일 입력 2회 호출 → dict 동일 (==).
    5. **identity 기울기 1.0**: user_fps==ref_fps==9.0, path=[(i,i)] → 전 구간 기울기 1.0 ± 1e-6, tier=="warped" (distance=1.0).
    6. **tier 경계** (D-02/D-03): distance=8.0 → warped(경계 포함), 8.1 → trim_only(reason 존재), 25.0 → trim_only, 25.1 → disabled. 기울기 클램프 위반 경로: distance=1.0인데 ref가 user의 3배 속도 path → trim_only + reason=="rate_clamp_exceeded".
    7. **flat 계약**: 반환 dict 키 == {version, source, tier, anchors, anchorCount, distance} (+ optional reason), anchors는 flat list[float] (중첩 0), anchorCount == len(anchors)//2.
    8. **길이 상한**: 300초 합성 path → len(anchors) <= 512 (step 자동 확대 허용, 앵커는 여전히 단조).
    각 테스트 docstring에 대응 결정 인용 (28-CONTEXT D-01/D-02/D-03). 커밋 메시지 `test(28-01): ...` (RED — 모듈 미존재).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && PYTHONPATH=shared/python:. python3 -m pytest tests/test_motion_alignment.py -q; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - pytest 실행이 비영 exit (RED: ModuleNotFoundError/collection error — 구현 부재 확인)
    - `grep -c "def test_" backend/tests/test_motion_alignment.py` >= 8
    - `grep -c "build_motion_alignment" backend/tests/test_motion_alignment.py` >= 6
  </acceptance_criteria>
  <done>28-02 구현이 만족해야 할 계약이 테스트로 고정됨 (Nyquist Wave 0 충족)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: alignmentWarp.ts — 워핑 수학 순수 모듈 (typecheck 진입)</name>
  <files>app/src/lib/alignmentWarp.ts</files>
  <read_first>
    - app/src/lib/deductionLabels.ts:1-26 (순수 lib 모듈 형상 — 헤더 단일출처 선언 + named export, 28-PATTERNS "alignmentWarp" 절)
    - app/src/lib/userAnalyses.ts:42-58 (방어적 normalize 패턴 — malformed → null)
    - 28-RESEARCH.md Pattern 3 (warp 설계) + Code Examples (TS 스케치)
  </read_first>
  <behavior>
    - warpTime: anchors=[0,0, 1,2] (u,r 쌍), t=0.5 → 1.0 (구간 선형보간)
    - warpTime 범위 밖: t < u0 → r0 - (u0 - t) / t > uN → rN + (t - uN) (기울기 1.0 연장, 경계 특수분기 0)
    - segmentRate: 구간 기울기 2.0 → 2.0, 기울기 3.0 → 2.0 (클램프), 기울기 0.2 → 0.5 (클램프)
    - segmentRate: tier==='trim_only' → 항상 1.0 (D-02 "가변속도 끔")
    - warpTime: tier==='trim_only' → 오프셋만 (warp(t) = t - u0 + r0, 첫 앵커 기준 — D-02 트림+오프셋)
    - normalizeMotionAlignment: 비단조 u / NaN / 홀수 길이 / anchors < 4개 float / 512 초과 / tier 미등재값 → null (legacy 폴백, 크래시 0)
    - normalizeMotionAlignment: **tier==='disabled' + anchors===[] (degenerate 방출, 28-02 규칙 7) → null이 아니라 유효 객체 반환** — disabled 배지 안내가 소실되지 않아야 함 (체커 2회차 warning: 백엔드 validator는 빈 anchors 허용, 앱 normalize가 거부하면 비대칭). disabled인데 anchors가 비어있지 않으면 그대로 통과(빈 배열 강제 아님)
  </behavior>
  <action>
    순수 TS 모듈 신설 (player/react 의존 0 — `tsc --noEmit` 만으로 검증 가능):
    - `export type MotionAlignment = { version: string; source: 'dtw' | 'vlm'; tier: 'warped' | 'trim_only' | 'disabled'; reason?: string; anchors: number[]; anchorCount: number; distance: number; }` — **임시 로컬 정의**. 헤더 주석에 "28-03이 analysis.ts로 계약 이관 후 28-06에서 `import type`으로 전환" 명시 (임시가 아니라 이관 예정 — 계약 단일 출처는 analysis.ts).
    - `export const RATE_MIN = 0.5; export const RATE_MAX = 2.0;` — belle 고정값 출처 주석 (28-CONTEXT specifics, VideoCompare FULLSCREEN_ZOOM 승인값 주석 관례).
    - `export function warpTime(a: MotionAlignment, tStudent: number): number` — anchors 이진탐색(또는 선형 스캔 — 앵커 ≤256쌍이라 성능 무관) + 구간 선형보간, behavior 블록 규칙 전부. trim_only는 첫 앵커 오프셋만.
    - `export function segmentRate(a: MotionAlignment, tStudent: number): number` — 현재 구간 기울기 clamp(RATE_MIN, RATE_MAX), trim_only → 1.0, disabled → 1.0.
    - `export function normalizeMotionAlignment(value: unknown): MotionAlignment | null` — userAnalyses normalize 형상(방어적, null = legacy 폴백). Number.isFinite 전수 검사 + u strictly increasing + r non-decreasing + anchors.length 짝수·4 이상·512 이하 + tier/source 화이트리스트. **예외: tier==='disabled'는 anchors.length===0 허용** (degenerate 방출 형상 — 짝수·4 이상 규칙을 disabled에 한해 면제. 근거 주석: D-02 3단 안내가 배지로 살아야 함, 28-02 규칙 7과 대칭).
    헤더: "정렬 목표시각 계산의 단일 출처 — `rightPlayer.currentTime =` 는 전부 여기 경유 (28-RESEARCH Pitfall 7)". 이모지 금지, 주석 한국어 why + spec 인용 (28-CONTEXT D-01/D-02).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - typecheck exit 0
    - `grep -c "export function warpTime\|export function segmentRate\|export function normalizeMotionAlignment" app/src/lib/alignmentWarp.ts` == 3
    - `grep -c "RATE_MIN" app/src/lib/alignmentWarp.ts` >= 2 (상수 + 사용)
  </acceptance_criteria>
  <done>워핑 수학이 순수 모듈로 존재하고 typecheck green — 28-06은 배선만 수행</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로컬 스크립트 → Firestore Admin | SA 자격으로 reference 컬렉션 읽기 (읽기 전용이어야 함) |
| Firestore doc → 앱 normalize | 미래의 alignment 데이터가 앱 크래시를 못 일으키게 하는 방어 지점 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Tampering | measure_reference_fps.py | mitigate | read-only 강제 — `.set/.update/.delete` grep 0 게이트 (acceptance) |
| T-28-02 | DoS (앱 크래시) | normalizeMotionAlignment | mitigate | malformed anchors(NaN/비단조/홀수/초과) → null 폴백, 크래시 0 (Task 3 behavior) |
| T-28-03 | Information Disclosure | 스크립트 출력 | accept | fps/frames 메타만 출력 — PII/시크릿 없음. SA 경로는 env로만 |
| T-28-SC | Tampering | 패키지 설치 | accept | 신규 패키지 0 (RESEARCH Package Legitimacy Audit — slopcheck 대상 없음) |
</threat_model>

<verification>
- Task 1 스크립트 exit 0 + "A1 RESOLVED" (읽기 전용 grep 0)
- Task 2 pytest RED (비영 exit) — 계약 고정
- Task 3 `npm run typecheck` green
</verification>

<success_criteria>
- A1(reference fps) 실측 완료 — 이후 플랜의 fps는 전부 doc 메타/인자에서 (하드코딩 0)
- 28-02의 GREEN 대상 테스트 8+개 존재
- alignmentWarp.ts 순수 함수 3개 typecheck 통과
</success_criteria>

<output>
Create `.planning/phases/28-dtw-motion-based-alignment/28-01-SUMMARY.md` when done (fps 실측 출력 전문 포함)
</output>
