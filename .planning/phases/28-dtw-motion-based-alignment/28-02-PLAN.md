---
phase: 28-dtw-motion-based-alignment
plan: 02
type: execute
wave: 2
depends_on: [28-01]
files_modified:
  - backend/shared/python/sunity_shared/analysis/motion_alignment.py
  - backend/tests/test_motion_alignment.py
autonomous: true
requirements: [ALGN-01, ALGN-02]
must_haves:
  truths:
    - "MotionMatch가 초 단위 단조 앵커 + tier로 결정론적으로 변환된다 (D-01)"
    - "tier 사다리(warped/trim_only/disabled)가 프로덕션 임계 재사용으로 갈린다 (D-02/D-03, calibration-source-hard-gate)"
    - "degenerate 입력(앵커<2쌍·fps 결측 등 build 불가)은 None이 아니라 tier 'disabled'로 방출된다 — legacy(필드 부재)와 구분 (W3)"
    - "채점 코어와 완전 분리 — numpy 외 의존 0, 점수 경로 import 0"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/motion_alignment.py"
      provides: "build_motion_alignment(match, *, user_fps, ref_fps) -> dict | None 순수 함수 + tier 사다리"
      min_lines: 80
      contains: "def build_motion_alignment"
    - path: "backend/tests/test_motion_alignment.py"
      provides: "Wave 0 계약 테스트 GREEN + vision_veto 임계 lockstep 테스트"
  key_links:
    - from: "backend/shared/python/sunity_shared/analysis/motion_alignment.py"
      to: "vision_veto._ALIGN_GLOBAL_T1/T2"
      via: "임계 값 재사용 + 출처 주석 + lockstep 테스트"
      pattern: "_ALIGN_GLOBAL"
---

<objective>
`motion_alignment.py` 순수 모듈 구현 — 28-01이 고정한 계약 테스트를 GREEN으로. `reference_dtw_match`(MotionMatch)를 초 단위 앵커(0.5s 간격, flat float 쌍, ~120 float) + tier(D-02 사다리) + distance로 변환한다. 임계는 `vision_veto.py:913-914`의 `_ALIGN_GLOBAL_T1=8.0 / _ALIGN_GLOBAL_T2=25.0` 프로덕션 상수 재사용 (D-03 — 자기 sweep 재보정 아님, calibration-source-hard-gate 충족), 배속 클램프 0.5~2.0은 belle 고정값 (D-01).

시나리오 태그: 2단계+6단계 — 정렬 데이터의 산출 코어.

**채점 무접촉:** 이 모듈은 표현/재생 정렬 전용. 채점 경로(per_joint_deviation/kismam/dimensions) import 금지, veto still 경로 무접촉 (28-VALIDATION 불변 제약).

Purpose: 방출(28-04)과 소비(28-06)가 공유할 정렬 데이터의 단일 산출 지점.
Output: build_motion_alignment 순수 함수, 테스트 GREEN.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-dtw-motion-based-alignment/28-CONTEXT.md
@.planning/phases/28-dtw-motion-based-alignment/28-RESEARCH.md
@.planning/phases/28-dtw-motion-based-alignment/28-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: build_motion_alignment 본체 — Wave 0 테스트 GREEN</name>
  <files>backend/shared/python/sunity_shared/analysis/motion_alignment.py, backend/tests/test_motion_alignment.py</files>
  <read_first>
    - backend/tests/test_motion_alignment.py (28-01 산출 — 만족해야 할 계약 전체)
    - backend/shared/python/sunity_shared/analysis/vision_veto.py:908-983 (임계 상수 + MotionMatch 방어적 getattr 접근 패턴, 28-PATTERNS Analog 1)
    - backend/shared/python/sunity_shared/analysis/fault_zoom.py:250-268 (1:N median 안정화 선례)
    - backend/shared/python/sunity_shared/analysis/dimensions.py:1-26 (모듈 헤더 관례 — 설계결정 이력)
    - 28-RESEARCH.md Pattern 1 (앵커 생성 규칙) + Pattern 2 (사다리 매핑)
  </read_first>
  <behavior>
    - 28-01 test_motion_alignment.py의 9+개 테스트 전부 (None/degenerate disabled 3형/초단위·fps/단조/결정론/identity 1.0/tier 경계/flat 계약/길이 상한). **기대값 무수정 — 28-01이 degenerate=disabled 최종 계약으로 이미 고정했다 (리뷰 MEDIUM-2). 구현이 테스트에 맞춘다.**
  </behavior>
  <action>
    신규 순수 모듈 (`from __future__ import annotations` 첫 줄, import는 numpy·stdlib만 — typing/dataclasses 허용):

    **모듈 헤더 docstring** (dimensions.py 관례): (a) 무엇/왜 — "MotionMatch → 초 단위 정렬 앵커 + tier. 표현/재생 전용, 채점 무접촉 (28-CONTEXT D-01~D-03)", (b) fps 도메인 함정 명시 — "user angles 9fps vs ref angles 18fps(phase4_v1, 28-01 실측) — 인덱스 방출 금지, 초 단위 강제 (28-RESEARCH Pitfall 1)", (c) 임계 출처 — "distance 임계 = vision_veto H4 프로덕션 상수(_ALIGN_GLOBAL_T1/T2) 재사용, 자기 sweep 재보정 아님 (calibration-source-hard-gate). 경로 부분 붕괴(1:N 극단 런)는 별도 임계를 발명하지 않고 앵커 구간 기울기 클램프 위반으로 표면화시킨다 — belle 고정 클램프(0.5~2.0)가 구조적 기준 (D-02)".

    **상수** (전부 출처 주석):
    - `DISTANCE_T1 = 8.0`, `DISTANCE_T2 = 25.0` — vision_veto._ALIGN_GLOBAL_T1/T2 값 재사용 (cross-import 대신 값 복제 + Task 2 lockstep 테스트로 drift 차단 — vision_veto의 Gemini 인접 의존을 순수 모듈에 끌어오지 않기 위함).
    - `RATE_MIN = 0.5`, `RATE_MAX = 2.0` — belle 고정 (28-CONTEXT specifics). app/src/lib/alignmentWarp.ts와 값 lockstep 주석.
    - `ANCHOR_STEP_S = 0.5` — 학생 타임라인 앵커 간격 (Firestore 40k index-entry 회피 근거: 20s 영상 ~80 float, [[analyses-index-exemption-fix]] 운영 스텝 불필요).
    - `MAX_ANCHOR_FLOATS = 512` — validator(28-03) 상한과 lockstep.
    - `MOTION_ALIGNMENT_VERSION = "ma-v1"`.

    **`def build_motion_alignment(match, *, user_fps: float, ref_fps: float) -> dict | None`**:
    1. 방어적 접근 (vision_veto :932-940 getattr 패턴): match None → None (**유일한 None 케이스** — 정렬 컨텍스트 자체 부재 = 미방출). path 빈 / user_fps<=0 / ref_fps<=0 → **degenerate 방출 (W3)**: 규칙 7의 tier 'disabled' dict 반환 (None 금지 — 신규 분석의 build 불가를 legacy 필드 부재와 구분).
    2. 앵커 생성: 학생초 그리드 = match.start/user_fps 부터 (match.end-1)/user_fps 까지 ANCHOR_STEP_S 간격 + 양 끝점 포함 (전역 트림 내장). 그리드 초과 시(len*2 > MAX_ANCHOR_FLOATS) step을 정수배로 확대해 상한 내로. 각 그리드 시각 t → user_local = round(t*user_fps) - match.start → path에서 same-local ref idx들의 **median** (fault_zoom._matched_ref_frame :264-268 선례 규칙) → rSec = median_ref_idx / ref_fps. 해당 local에 대응 없으면 그 앵커 skip.
    3. 단조성 강제: u는 구성상 strictly increasing, r은 직전 r보다 작은 앵커 제거 (warp 역전 방지).
    4. 앵커 < 2쌍이면 규칙 7의 degenerate 'disabled' 방출 (W3 — None 금지).
    5. tier (D-02 사다리, 28-RESEARCH Pattern 2 매핑):
       - `distance <= DISTANCE_T1` AND 전 구간 기울기 (r[i+1]-r[i])/(u[i+1]-u[i]) ∈ [RATE_MIN, RATE_MAX] → `"warped"`
       - elif `distance <= DISTANCE_T2` → `"trim_only"` + reason: 기울기 위반이면 `"rate_clamp_exceeded"`, 아니면(distance만 T1 초과) `"low_global_confidence"` — 전체 길이비가 클램프 밖인 경우도 첫/끝 앵커 기울기 위반으로 잡힘 (`"length_extreme"` 별도 enum은 두되 전체길이비 (rN-r0)/(uN-u0) 클램프 밖일 때 부여)
       - else → `"disabled"` + reason `"low_global_confidence"`
    6. 반환 dict: `{"version": MOTION_ALIGNMENT_VERSION, "source": "dtw", "tier": ..., "anchors": [u0,r0,u1,r1,...] (float), "anchorCount": len//2, "distance": float(match.distance)}` + tier != "warped"일 때만 `"reason"`. flat scalar만 — 중첩 컨테이너 0 ([[firestore-nested-array-flat]]).
    7. **degenerate 반환 스펙 (W3 — checker 채택안, 28-01 RED 테스트가 이미 이 형상을 기대):** `{"version": MOTION_ALIGNMENT_VERSION, "source": "dtw", "tier": "disabled", "reason": "insufficient_anchors"(앵커<2쌍) | "invalid_fps"(fps 결측/비양수) | "empty_path"(path 빈), "anchors": [], "anchorCount": 0, "distance": float(getattr(match, "distance", 0.0) or 0.0)}`. tier enum은 기존 3단 명칭 그대로 — 새 tier 값 발명 금지 (D-02 사다리 3단 'disabled'=안내+절대시계가 원래 이 용도). reason은 계약상 자유 문자열(optional)이라 degenerate 사유 추가 가능. 빈 anchors는 28-03 validator의 disabled 예외를 통과하는 형상 (역불변식 — disabled만 빈 anchors 허용). 주석: "28-07 배너는 필드 부재(legacy)만 대상 — degenerate도 방출해 '재분석하면 적용' 과약속 루프 차단 (checker W3)".
    결정론: 정렬/median만 사용, 랜덤/시간 의존 0.

    **Wave 0 테스트 기대값 갱신 금지 (리뷰 MEDIUM-2):** 28-01이 degenerate=disabled를 포함한 최종 계약으로 RED 테스트를 고정했다. 이 플랜은 기존 테스트를 수정하지 않고 GREEN만 만든다 — 기대값을 고치고 싶어지면 구현이 계약을 어긴 것.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && PYTHONPATH=shared/python:. python3 -m pytest tests/test_motion_alignment.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Wave 0 테스트 전부 PASS (0 failed) — 기존 테스트 diff 0 (`git diff backend/tests/test_motion_alignment.py`에 기대값 변경 hunk 없음; Task 2의 신규 테스트 추가만 허용)
    - `grep -v '^#' backend/shared/python/sunity_shared/analysis/motion_alignment.py | grep -c "import"` 결과에서 numpy/stdlib 외 import 0 — 구체 게이트: `grep -c "from sunity_shared\|import kismam\|import dimensions\|import vision_veto\|import boto3\|import google" backend/shared/python/sunity_shared/analysis/motion_alignment.py` == 0
    - `grep -c "_ALIGN_GLOBAL" backend/shared/python/sunity_shared/analysis/motion_alignment.py` >= 1 (임계 출처 주석 — D-03 게이트)
  </acceptance_criteria>
  <done>28-01 계약 테스트 GREEN (degenerate=disabled 방출 포함, 기대값 무수정), 순수성(수치 코어 numpy-only) 보존</done>
</task>

<task type="auto">
  <name>Task 2: 임계 lockstep + 순수성 가드 테스트</name>
  <files>backend/tests/test_motion_alignment.py</files>
  <read_first>
    - backend/shared/python/sunity_shared/analysis/vision_veto.py:908-916 (재사용 원본 상수)
    - 28-RESEARCH.md Pattern 2 (calibration-source-hard-gate 준수 방법)
  </read_first>
  <action>
    test_motion_alignment.py에 추가 (기존 테스트 무수정):
    1. **임계 lockstep**: `from sunity_shared.analysis import vision_veto` (테스트에서만 import) — `motion_alignment.DISTANCE_T1 == vision_veto._ALIGN_GLOBAL_T1` and `DISTANCE_T2 == vision_veto._ALIGN_GLOBAL_T2` 단언. docstring: "값 복제 drift 차단 — 프로덕션 임계 단일 출처는 vision_veto (D-03)".
    2. **순수성 AST 가드**: motion_alignment.py 소스를 ast.parse 해 import 노드 화이트리스트(numpy, math, typing, dataclasses, __future__) 밖 import 0 단언 (채점/네트워크/모델 의존 유입 영구 차단 — 채점 무접촉 경계의 구조 가드).
    3. **경계 사다리 재확인**: distance=DISTANCE_T2 정확값 → trim_only (경계 포함 규칙 고정 — 28-01 테스트와 값 중복이어도 상수 참조형으로 재작성해 하드코딩 drift 차단).
    4. **degenerate 방출 계약 테스트 (W3)**: 앵커<2쌍 합성 match → tier == "disabled" + anchors == [] + anchorCount == 0 + reason 존재; ref_fps=0 → 동일 형상. None 반환은 match=None일 때만임을 함께 단언 (legacy 구분 계약 고정 — 28-01 degenerate 테스트와 값 중복이어도 상수/형상 참조형 재확인).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && PYTHONPATH=shared/python:. python3 -m pytest tests/test_motion_alignment.py -q</automated>
  </verify>
  <acceptance_criteria>
    - 전체 PASS (0 failed), 테스트 수 >= 12
    - `grep -c "ast.parse\|ast.walk" backend/tests/test_motion_alignment.py` >= 1
  </acceptance_criteria>
  <done>임계 drift·의존 오염·degenerate 방출 계약이 테스트로 영구 차단/고정됨</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 정렬 산출 → 채점 경계 | 정렬 모듈이 점수를 건드리면 안 됨 (phase 전역 hard 제약) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-04 | Tampering (채점 오염) | motion_alignment.py | mitigate | 순수성 AST 가드 테스트 — 채점/외부 의존 import 0 강제 (Task 2) |
| T-28-05 | DoS (대형 배열) | anchors 산출 | mitigate | MAX_ANCHOR_FLOATS=512 + step 자동 확대 (Task 1 규칙 2) |
| T-28-06 | Repudiation (임계 근거 소실) | tier 사다리 | mitigate | 출처 주석 grep 게이트 + vision_veto lockstep 테스트 (calibration-source-hard-gate) |
| T-28-17 | Spoofing (오도 안내 — legacy 혼동) | degenerate 반환 | mitigate | build 불가 = disabled 방출 (None 금지) — 28-07 배너가 legacy(필드 부재)만 판정 가능 (W3, Task 1 규칙 7 + Task 2 항목 4) |
| T-28-SC | Tampering | 패키지 설치 | accept | 신규 패키지 0 |
</threat_model>

<verification>
- `pytest tests/test_motion_alignment.py -q` 전부 GREEN (12+ 테스트)
- 기존 Wave 0 테스트 기대값 diff 0 (MEDIUM-2 — churn 금지)
- 순수성 grep + AST 게이트 통과
- degenerate 입력 → disabled 방출 (None은 match=None만)
</verification>

<success_criteria>
- build_motion_alignment이 결정론·단조·초 단위·상한을 테스트로 증명
- tier 사다리가 재사용 임계로만 동작 (신규 튜닝 상수 0 — RATE/STEP은 구조 상수)
- degenerate 방출이 legacy와 구분되는 계약으로 고정 (W3)
</success_criteria>

<output>
Create `.planning/phases/28-dtw-motion-based-alignment/28-02-SUMMARY.md` when done
</output>
