---
phase: 28-dtw-motion-based-alignment
plan: 03
type: execute
wave: 2
depends_on: [28-01]
files_modified:
  - app/src/types/analysis.ts
  - backend/shared/python/sunity_shared/models.py
  - docs/contract.md
  - backend/shared/python/sunity_shared/firestore_admin.py
  - backend/tests/test_motion_alignment_contract.py
autonomous: true
requirements: [ALGN-01, ALGN-03]
must_haves:
  truths:
    - "motionAlignment 계약이 3-way lockstep(analysis.ts + models.py + contract.md)으로 존재한다"
    - "FaultZoomComparison.refMatch 캡션 플래그가 계약에 존재한다 (D-04)"
    - "validator가 앵커 길이 상한·flat·단조성 + tier↔anchors 역불변식을 강제한다 — 빈 anchors는 tier=='disabled'만 허용, warped/trim_only는 2쌍 이상 (Firestore 40k 인덱스 방어 + 리뷰 MEDIUM-3: 모순 데이터 저장 전 거부)"
    - "RATE 클램프(0.5/2.0) Python↔TS lockstep이 텍스트 대조 테스트로 잠긴다 (W4)"
  artifacts:
    - path: "app/src/types/analysis.ts"
      provides: "MotionAlignment interface + AnalysisResult.motionAlignment? + FaultZoomComparison.refMatch?"
      contains: "motionAlignment"
    - path: "backend/shared/python/sunity_shared/models.py"
      provides: "MOTION_ALIGNMENT_KEYS / MOTION_ALIGNMENT_TIERS 계약 상수 블록"
      contains: "MOTION_ALIGNMENT_KEYS"
    - path: "docs/contract.md"
      provides: "§11 MotionAlignment 절"
    - path: "backend/shared/python/sunity_shared/firestore_admin.py"
      provides: "_validate_motion_alignment scoped validator + complete_analysis 훅"
      contains: "_validate_motion_alignment"
  key_links:
    - from: "backend/shared/python/sunity_shared/firestore_admin.py"
      to: "result['motionAlignment']"
      via: "complete_analysis 내 scoped validator 호출 (선례 :907-913 블록)"
      pattern: "_validate_motion_alignment"
---

<objective>
motionAlignment 계약 3-way lockstep + Firestore scoped validator. Phase 22 v1 시간 앵커 출력과 상위 호환(source: 'dtw' | 'vlm' 축 — 22-01 REPORT_KEYS time_anchors 동형, ROADMAP 명시)으로 설계하고, D-04 캡션용 `FaultZoomComparison.refMatch` scalar도 함께 계약화한다.

시나리오 태그: 2단계+6단계 — 정렬 데이터의 저장/소비 계약.

**선행 전제 (필수 — 착수 전 확인):** Phase 27의 27-06(faultZoomStatus 3-way lockstep + update_analysis_fault_zoom)이 analysis.ts / contract.md / firestore_admin.py를 먼저 수정한다. **27-06 실행 완료 후 착수.** Task 1의 심볼 선행 확인 스텝이 미실행 상태를 감지하면 중단하고 orchestrator에 보고한다 (라인 번호가 아닌 심볼 기준 — 27-07 선례).

**채점 무접촉:** 계약/validator 추가만 — 점수 산출 코드 0접촉.

Purpose: 방출(28-04)·소비(28-06/07)가 착수하기 전에 계약을 단일 커밋으로 고정 (한쪽만 수정 = 프로젝트 anti-pattern).
Output: 3-way 계약 + validator(역불변식 포함) + lockstep 테스트.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-dtw-motion-based-alignment/28-CONTEXT.md
@.planning/phases/28-dtw-motion-based-alignment/28-RESEARCH.md
@.planning/phases/28-dtw-motion-based-alignment/28-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 계약 3-way lockstep — analysis.ts + models.py + contract.md (atomic)</name>
  <files>app/src/types/analysis.ts, backend/shared/python/sunity_shared/models.py, docs/contract.md</files>
  <read_first>
    - app/src/types/analysis.ts:434-459 (FaultZoomComparison.region/tier optional 선례 + lockstep 주석 형식), :519-525 (AnalysisResult optional 필드 자리 관례)
    - backend/shared/python/sunity_shared/models.py:150-169 (DEDUCTION_RECORD_KEYS 3-way lockstep 블록 형식)
    - docs/contract.md §10 헤더 (:1420 부근 — 신규 절 형식)
    - .planning/phases/22-custom-vlm-finetune/22-01-PLAN.md:83,92 (time_anchors/segments — 상위 호환 근거)
    - 28-RESEARCH.md Pattern 1 (계약 필드 정의 + 옵션 비교표)
  </read_first>
  <action>
    **선행 확인 스텝 (심볼 기준):** `grep -c "faultZoomStatus" app/src/types/analysis.ts` >= 1 AND `grep -c "def update_analysis_fault_zoom" backend/shared/python/sunity_shared/firestore_admin.py` >= 1 확인. 하나라도 0이면 **작업 중단** — "27-06 미실행 — Phase 27 실행 후 재개" 보고 (파일 수정 0).

    단일 atomic 커밋으로 세 파일 동시 수정:
    1. **analysis.ts**: `MotionAlignment` interface 신설 — `version: string; source: 'dtw' | 'vlm'; tier: 'warped' | 'trim_only' | 'disabled'; reason?: string; anchors: number[]; anchorCount: number; distance: number;` JSDoc에 (a) anchors = flat [u0,r0,u1,r1,...] 학생초/기준초 쌍·단조 (firestore-nested-array-flat), (b) 부재(legacy doc) = 현행 절대시계 동작 (하위호환, tier 선례와 동일 규칙), (c) source 'vlm' = Phase 22 v1 time_anchors 상위 호환 축, (d) Python lockstep: models.py MOTION_ALIGNMENT_KEYS + contract.md §11, (e) 역불변식 — 빈 anchors는 tier 'disabled'(degenerate)만 (MEDIUM-3). `AnalysisResult`에 `motionAlignment?: MotionAlignment;` 추가 (:519-525 optional 관례 자리). `FaultZoomComparison`에 `refMatch?: 'dtw' | 'failed';` 추가 — JSDoc: "D-04 — 기준(정은지) 측 프레임 대응 provenance. 'failed'=DTW 대응 실패로 전신 폴백 (앱은 캡션 렌더), 부재(legacy)=캡션 없음".
    2. **models.py**: DEDUCTION_RECORD_KEYS 블록(:150-169) 형식으로 `MOTION_ALIGNMENT_KEYS = ("version", "source", "tier", "reason", "anchors", "anchorCount", "distance")`, `MOTION_ALIGNMENT_TIERS = ("warped", "trim_only", "disabled")`, `MOTION_ALIGNMENT_SOURCES = ("dtw", "vlm")`, `MOTION_ALIGNMENT_MAX_ANCHOR_FLOATS = 512` — 헤더 주석 "3-way lockstep: app/src/types/analysis.ts MotionAlignment + docs/contract.md §11. 상한은 motion_alignment.MAX_ANCHOR_FLOATS와 lockstep (Firestore 40k index-entry, [[firestore-index-entry-limit]])".
    3. **contract.md**: `## §11. MotionAlignment (Phase 28 신설 — ALGN-01 동작 기반 비교 정렬)` 절 추가 — 필드 표(키/타입/의미), tier 사다리 3단(D-02), 초 단위 근거(user 9fps vs ref 18fps fps 도메인 — 28-RESEARCH Pitfall 1), anchors flat 규칙 + 상한 512, **tier↔anchors 역불변식 (MEDIUM-3): anchors==[]는 tier 'disabled'에서만 유효 — 'warped'/'trim_only'는 최소 2쌍(4 float)**, legacy 하위호환(부재=현행 동작), Phase 22 source:'vlm' 상위 호환, refMatch 필드는 기존 FaultZoomComparison 절에 행 추가.
    커밋: `feat(28-03): motionAlignment + refMatch 계약 3-way lockstep`.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck && cd /Users/kimtaesung/Dev/SunityMotion && grep -c "motionAlignment" app/src/types/analysis.ts && grep -c "MOTION_ALIGNMENT_KEYS" backend/shared/python/sunity_shared/models.py && grep -c "MotionAlignment" docs/contract.md</automated>
  </verify>
  <acceptance_criteria>
    - typecheck exit 0
    - `grep -c "motionAlignment" app/src/types/analysis.ts` >= 2 (interface + AnalysisResult)
    - `grep -c "refMatch" app/src/types/analysis.ts` >= 1
    - `grep -c "MOTION_ALIGNMENT_KEYS" backend/shared/python/sunity_shared/models.py` >= 1
    - `grep -c "§11" docs/contract.md` >= 1
    - 세 파일이 같은 커밋에 포함 (3-way atomic)
  </acceptance_criteria>
  <done>계약이 세 곳에서 동일 형상으로 존재 — 이후 플랜은 소비만</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: firestore_admin scoped validator (역불변식 포함) + lockstep 테스트</name>
  <files>backend/shared/python/sunity_shared/firestore_admin.py, backend/tests/test_motion_alignment_contract.py</files>
  <read_first>
    - backend/shared/python/sunity_shared/firestore_admin.py:288-305 (_validate_safety_flags — scoped validator 선례 형식), :904-916 (complete_analysis 내 result 검증 블록 — "result 안으로 흐른다, 신규 kwarg 없음" 선례)
    - backend/shared/python/sunity_shared/models.py (Task 1 산출 상수)
    - app/src/lib/alignmentWarp.ts (28-01 산출 — RATE_MIN/RATE_MAX 텍스트 대조 대상 W4 + normalizeMotionAlignment disabled 예외와의 대칭 확인, MEDIUM-3)
    - 28-PATTERNS.md "firestore_admin.py [수정]" 절
  </read_first>
  <behavior>
    - 유효 dict (ma-v1, warped, anchors 4개 float 단조) → 통과 (raise 0)
    - None → 통과 (optional)
    - anchors에 list 중첩 → TypeError (nested-array 금지)
    - anchors 길이 513+ → ValueError (상한)
    - anchors u 비단조 → ValueError
    - tier 미등재값 / 키 화이트리스트 밖 → ValueError
    - anchors에 NaN/inf → ValueError
    - **역불변식 (리뷰 MEDIUM-3):**
      - tier=="disabled" + anchors==[] → 통과 (degenerate 방출 형상)
      - tier=="warped" + anchors==[] → ValueError (모순 데이터 — 앱이 조용히 폴백하는 대신 저장 전 거부)
      - tier=="trim_only" + anchors 1쌍(2 float) → ValueError (warped/trim_only는 len(anchors)>=4 AND anchorCount>=2 필수)
  </behavior>
  <action>
    1. `_validate_motion_alignment(payload, *, path: str = "motionAlignment") -> None` 신설 (_validate_safety_flags :288-305 형식) — None이면 return; dict 강제; 키는 models.MOTION_ALIGNMENT_KEYS 화이트리스트(reason optional); `anchors`는 flat list[숫자 scalar] + `len <= models.MOTION_ALIGNMENT_MAX_ANCHOR_FLOATS` + 짝수 + finite + u strictly increasing·r non-decreasing; tier ∈ MOTION_ALIGNMENT_TIERS, source ∈ MOTION_ALIGNMENT_SOURCES; anchorCount == len(anchors)//2. **tier↔anchors 역불변식 (MEDIUM-3):** `tier == "disabled"`일 때만 `anchors == []` 허용 (degenerate 방출 형상, 28-02 W3); `tier in ("warped", "trim_only")`이면 `len(anchors) >= 4 and anchorCount >= 2` 강제 — "warped인데 anchors 빈" 모순 상태를 저장 전에 거부해 앱 normalizer(같은 규칙으로 null 폴백)와 대칭 유지 + malformed Firestore 상태 진단 가능. docstring에 "Phase 28 (ALGN-01) — result['motionAlignment'] 단일 persistence path 전용, 신규 kwarg 없음 (safetyFlags 선례). 상한 = 40k index-entry 방어 (T-28-05). 역불변식 = 리뷰 MEDIUM-3".
    2. `complete_analysis`의 result 검증 블록(:907-913 — `_validate_deduction_breakdown` 호출부 인접)에 한 줄 추가: `_validate_motion_alignment((result or {}).get("motionAlignment"))`. **generic validator 본체와 다른 코드 무변경.** complete_analysis docstring에 Phase 28 이력 단락 1개 추가 (기존 Phase 6/8/10/24 단락 형식).
    3. `backend/tests/test_motion_alignment_contract.py` 신설 — behavior 블록 케이스 전부 (역불변식 3케이스 포함) + **3-way lockstep 테스트 (models/TS 텍스트 쪽 단독 — B1)**: (a) models.MOTION_ALIGNMENT_KEYS의 각 키가 app/src/types/analysis.ts 소스 텍스트에 존재 (파일 읽어 문자열 검사 — DEDUCTION 계열 lockstep 테스트 선례 형식이 있으면 그 형식 재사용), (b) **RATE 클램프 lockstep (W4)**: app/src/lib/alignmentWarp.ts 소스 텍스트에 `RATE_MIN = 0.5` / `RATE_MAX = 2.0` 문자열 존재 + 텍스트에서 파싱한 값이 0.5/2.0(belle 고정값 — 28-CONTEXT D-01)과 일치 단언 — analysis.ts 텍스트 대조와 동일 형식.
    **금지 (B1):** 이 테스트 파일에서 `sunity_shared.analysis.motion_alignment` import 금지 — 같은 wave 2의 28-02 산출물이라 병렬 실행 시 collection error. `motion_alignment.MAX_ANCHOR_FLOATS == models.MOTION_ALIGNMENT_MAX_ANCHOR_FLOATS` 단언은 **28-04의 test_pipeline_motion_alignment.py로 이동됨** (wave 3 — 28-02/28-03 완료 후 실행이 보장되는 자리). 여기서는 models/TS 텍스트 쪽 단독 검증만.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && PYTHONPATH=shared/python:. python3 -m pytest tests/test_motion_alignment_contract.py -q</automated>
  </verify>
  <acceptance_criteria>
    - 신규 테스트 전부 PASS (>= 11 tests — 역불변식 3케이스 포함)
    - `grep -c "_validate_motion_alignment" backend/shared/python/sunity_shared/firestore_admin.py` >= 2 (정의 + complete_analysis 훅)
    - `grep -c "def _validate_dict_only_scalars" backend/shared/python/sunity_shared/firestore_admin.py` == 1 (generic validator 본체 무변경 — 함수 중복 정의 없음)
    - `grep -c "RATE_MIN" backend/tests/test_motion_alignment_contract.py` >= 1 (W4 lockstep)
    - `grep -cE "^(from|import).*motion_alignment" backend/tests/test_motion_alignment_contract.py` == 0 (B1 — 같은 wave 산출물 import 금지)
  </acceptance_criteria>
  <done>malformed/초대형/모순(tier↔anchors) alignment가 저장 전에 차단됨, 계약 drift(keys + RATE 클램프)가 테스트로 잠김</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 파이프라인 → Firestore | result.motionAlignment 저장 (validator가 유일 관문) |
| 계약 3-way | 한쪽만 수정 시 앱-백엔드 해석 불일치 (오도 위험) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-05 | DoS (40k index-entry) | complete_analysis | mitigate | _validate_motion_alignment 길이 상한 512 + flat scalar 강제 (Task 2) |
| T-28-07 | Tampering (계약 drift) | 3-way 계약 | mitigate | atomic 커밋 + lockstep 테스트 (keys ↔ analysis.ts 텍스트 대조 + RATE 상수 ↔ alignmentWarp.ts 텍스트 대조 W4; MAX_ANCHOR_FLOATS lockstep은 28-04 소관 B1) |
| T-28-18 | Integrity (모순 데이터 저장) | tier↔anchors 불일치 | mitigate | 역불변식 — disabled만 빈 anchors, warped/trim_only는 2쌍 이상 강제 (MEDIUM-3, Task 2) — 앱 silent fallback 대신 저장 전 거부 |
| T-28-08 | Elevation of Privilege | Firestore rules | accept | motionAlignment는 본인 doc result 내부 — rules 무변경, 백엔드가 유일 writer (ASVS V4 해당 없음) |
| T-28-SC | Tampering | 패키지 설치 | accept | 신규 패키지 0 |
</threat_model>

<verification>
- 선행 확인: 27-06 심볼 존재 (미존재 시 중단·보고)
- typecheck green + 계약 grep 3-way 각 >= 1
- validator 테스트 GREEN (역불변식 3케이스 포함, motion_alignment.py import 0 — B1)
</verification>

<success_criteria>
- 계약 단일 커밋 lockstep, validator가 상한/flat/단조 + tier↔anchors 역불변식 강제
- legacy doc(필드 부재) 하위호환이 계약 문서에 명시
- RATE 클램프 lockstep이 텍스트 대조로 잠김 (W4)
</success_criteria>

<output>
Create `.planning/phases/28-dtw-motion-based-alignment/28-03-SUMMARY.md` when done
</output>
