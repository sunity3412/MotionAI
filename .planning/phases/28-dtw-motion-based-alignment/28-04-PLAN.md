---
phase: 28-dtw-motion-based-alignment
plan: 04
type: execute
wave: 3
depends_on: [28-02, 28-03]
files_modified:
  - backend/functions/pipeline/app.py
  - backend/tests/test_pipeline_motion_alignment.py
autonomous: true
requirements: [ALGN-01, ALGN-06]
must_haves:
  truths:
    - "mode1 분석 result에 motionAlignment가 complete_analysis 페이로드로 동승 방출된다 (사후 write 0 — 27-06 게이트 정합)"
    - "mode3 second+에도 prev 대비 정렬이 방출된다 (양측 9fps — Phase 29 D1 의존 대비, Open Q1 방출 포함 판단)"
    - "degenerate 입력(ref fps 결측 등)도 tier 'disabled'로 방출돼 legacy(필드 부재)와 구분된다 (W3)"
    - "alignment 방출 유무/실패와 무관하게 overallScore/deductionBreakdown이 동일하다 (채점 무접촉 hard)"
  artifacts:
    - path: "backend/functions/pipeline/app.py"
      provides: "_attach_motion_alignment helper + mode1/mode3 배선 + mode3 fault_zoom에 prev dtw_match 전달"
      contains: "_attach_motion_alignment"
    - path: "backend/tests/test_pipeline_motion_alignment.py"
      provides: "방출 단위 + 채점 무접촉 + graceful skip + 상한 lockstep(B1 이동분) 테스트"
      min_lines: 60
  key_links:
    - from: "backend/functions/pipeline/app.py"
      to: "sunity_shared.analysis.motion_alignment.build_motion_alignment"
      via: "EXPERT 분기 + mode3 분기 호출"
      pattern: "build_motion_alignment"
    - from: "backend/functions/pipeline/app.py"
      to: "firestore_admin.complete_analysis"
      via: "result['motionAlignment'] — complete 호출 전 주입"
      pattern: "motionAlignment"
---

<objective>
파이프라인 방출 배선 — `reference_dtw_match`(mode1, app.py:3307-3310)와 mode3 prev match(`_mode3_comparison` 내부에서 현재 `_match`로 버려지는 값, :3038)를 `build_motion_alignment`으로 변환해 `result["motionAlignment"]`로 **complete_analysis 호출 전에** 주입한다 (27-06 "complete 후 result.* write 금지" 게이트 정합 — 사후 업데이트 경로 신설 금지). ref_fps는 ref doc `keypointReport.fps`에서, user_fps는 frame_extractor의 기본 target_fps 상수 참조에서 (리터럴 하드코딩 금지 — I1 출처 단일화, 28-RESEARCH Pitfall 1/6).

mode3 방출 포함 판단 (RESEARCH Open Q1): **포함** — 양측 9fps로 변환 저비용, Phase 29 D1("Mode3에도 비교영상")이 이 산출물을 소비, 채점 무접촉이라 리스크 낮음. 같은 이유로 mode3 fault_zoom(`_attach_mode3_fault_zoom`)에 prev dtw_match를 전달해 28-05의 ratio 근사 제거가 mode3 카드 회귀를 만들지 않게 한다.

시나리오 태그: 2단계+6단계 — 정렬 데이터가 실제 분석 doc에 실리는 지점.

**선행 전제 (필수):** Phase 27 백엔드 플랜(27-01~27-06)이 `_process` 후반(zoom 호출/complete 순서)을 재배열한다. **27-06 실행 완료 후 착수.** 라인 번호 참조는 전부 심볼 기준 재탐색 (`reference_dtw_match`, `complete_analysis(`, `_mode3_comparison`, `_attach_mode3_fault_zoom`). Task 1 선행 확인 스텝이 미실행을 감지하면 중단·보고.

**채점 무접촉 hard:** 방출은 result에 키 1개 추가뿐. veto still 경로(`_build_selected_frame_pair`, app.py:1720) 접촉 금지 — 이 경로의 fps fix는 채점 인접이라 phase 제외 (belle 통지됨, 28-VALIDATION 불변 제약).

Purpose: 정렬 데이터가 새 분석부터 doc에 존재 (D-05 "새 분석부터"의 공급측).
Output: mode1+mode3 방출 배선 + 무접촉 증명 테스트.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-dtw-motion-based-alignment/28-CONTEXT.md
@.planning/phases/28-dtw-motion-based-alignment/28-RESEARCH.md
@.planning/phases/28-dtw-motion-based-alignment/28-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: mode1 EXPERT 분기 방출 — _attach_motion_alignment helper</name>
  <files>backend/functions/pipeline/app.py</files>
  <read_first>
    - backend/functions/pipeline/app.py — 심볼 기준: `reference_dtw_match = match` 대입부(EXPERT 분기), `reference_dtw_match = None` 초기화부, `firestore_admin.complete_analysis(` 호출부, `_FRAME_EXTRACTOR = FfmpegFrameExtractor()` (:1167 — user fps 출처)
    - backend/shared/python/sunity_shared/analysis/frame_extractor.py:1-30 (기본 target_fps 상수/생성자 기본값 — user_fps 단일 출처, I1)
    - 28-PATTERNS.md "functions/pipeline/app.py [수정]" 절 (result 주입 선례 "result 안으로 흐른다, 신규 kwarg 없음" + graceful skip 관례 :4125-4128)
    - backend/shared/python/sunity_shared/analysis/motion_alignment.py (28-02 산출 — 시그니처 + degenerate 'disabled' 방출 규칙 W3)
  </read_first>
  <action>
    **선행 확인 스텝:** `grep -c "faultZoomStatus\|update_analysis_fault_zoom" backend/functions/pipeline/app.py backend/shared/python/sunity_shared/firestore_admin.py` 로 27-06 실행 흔적 확인 — 0이면 중단, "27-06 미실행" 보고.

    1. helper 신설: `def _attach_motion_alignment(result: dict, match, *, user_fps: float, ref_fps: float, uid: str, analysis_id: str) -> None` — `build_motion_alignment` 호출, dict가 나오면 `result["motionAlignment"] = alignment` (그 외 result 무변경 — 키 1개 추가가 유일한 부작용). None(= match 자체 None — 정렬 컨텍스트 부재)이면 아무것도 안 함 (미방출 = legacy); degenerate 입력은 build가 tier 'disabled' dict로 방출하므로(28-02 W3) helper에 별도 분기 없음. 전체를 try/except로 감싸 실패 시 `log.exception("motion alignment 방출 실패 — graceful skip uid=%s analysis_id=%s", ...)` 후 통과 (:4125-4128 관례 — 분석 흐름 차단 0). docstring에 "표현/재생 전용 — 채점 무접촉 (28-CONTEXT). complete_analysis 호출 전에만 호출할 것 (27-06 게이트: complete 후 result.* write 금지)".
    2. mode1 배선: EXPERT 분기의 `reference_dtw_match = match` 직후(심볼 기준) — `ref_kp_fps = float((((ref or {}).get("keypointReport")) or {}).get("fps") or 0.0)` 을 지역 변수로 확보 (하드코딩 금지 — phase4_v1 18fps는 doc 메타가 정본, 28-01 실측 근거 주석). `_attach_motion_alignment`는 complete_analysis 호출 **직전 구간**(현재 result가 최종 조립된 뒤)에서 호출 — `_attach_motion_alignment(result, reference_dtw_match, user_fps=<frame_extractor 기본 target_fps>, ref_fps=ref_kp_fps, ...)`. **user_fps 출처 단일화 (I1):** 리터럴 9.0 금지 — frame_extractor의 기본 target_fps를 단일 출처로 참조 (심볼 재탐색: `_FRAME_EXTRACTOR`가 fps/target_fps 속성을 노출하면 그 값을, 아니면 frame_extractor 모듈의 기본값 상수를 import — frame_extractor.py:20 부근이 정본, 값 재복제 금지). ref_kp_fps <= 0이면 build가 degenerate `tier: 'disabled'` dict를 방출(28-02 W3)하므로 별도 분기 불필요 — 신규 분석은 build 불가여도 필드가 실려 legacy(필드 부재)와 구분된다.
    3. ref_kp_fps 변수가 complete 직전 스코프에 도달하도록 `reference_dtw_match` 초기화부 인접에 `reference_kp_fps = 0.0` 초기화 + EXPERT 분기에서 대입 (기존 `reference_dtw_match` 패턴과 동일한 수명 관리).
    코드에 fenced 예제 넣지 말고 위 명세대로 구현. 커밋 `feat(28-04): mode1 motionAlignment 방출 배선`.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && PYTHONPATH=shared/python:. python3 -m pytest tests/test_pipeline_dispatch.py tests/test_motion_alignment.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_attach_motion_alignment" backend/functions/pipeline/app.py` >= 2 (정의 + mode1 호출)
    - `grep -c "keypointReport" backend/functions/pipeline/app.py` 가 기존 대비 +1 이상 (ref fps doc 메타 읽기)
    - `grep -c "user_fps=9.0\|user_fps = 9.0" backend/functions/pipeline/app.py` == 0 (I1 — fps 리터럴 하드코딩 금지)
    - 기존 pipeline 테스트 신규 FAILED 0
  </acceptance_criteria>
  <done>mode1 새 분석 result에 motionAlignment가 complete 페이로드로 동승 (degenerate도 disabled로 실림)</done>
</task>

<task type="auto">
  <name>Task 2: mode3 second+ 방출 + mode3 fault_zoom에 prev match 전달</name>
  <files>backend/functions/pipeline/app.py</files>
  <read_first>
    - backend/functions/pipeline/app.py — 심볼 기준: `def _mode3_comparison(` (:2983, 내부 `deviation, _match, _user_seg, prev_seg = _deviation_against(` :3038), 단일 호출부 `= _mode3_comparison(` (:3385), `def _attach_mode3_fault_zoom(` 및 그 호출부 (mode3 분기, :4014 부근)
    - 28-RESEARCH.md Pitfall 6 (mode3 = 9fps/9fps — mode1 변환 규칙과 다름) + Open Question 1
  </read_first>
  <action>
    1. `_mode3_comparison` 반환 확장: `_match`로 버려지는 MotionMatch를 반환 tuple 5번째 원소 `prev_dtw_match`로 추가 (prev 부재/first 분석 경로는 None). docstring 반환 절 갱신. 단일 호출부(:3385 심볼 재탐색)에서 `assessments, dimension_scores, overall, comparison, prev_dtw_match = _mode3_comparison(...)` 로 수령.
    2. mode3 방출: complete 직전 구간에서 `_attach_motion_alignment(result, prev_dtw_match, user_fps=<frame_extractor 상수>, ref_fps=<동일 상수>, ...)` — 양측 모두 파이프라인 9fps angles 도메인 (prev doc angles는 자기 분석의 9fps 저장분 — Pitfall 6 주석 필수: "mode1의 18fps 변환을 여기 적용 금지, fps는 인자"). **I1:** 두 fps 모두 Task 1과 동일한 frame_extractor 기본 target_fps 참조 — 리터럴 9.0 금지 (mode3는 ref도 같은 상수). mode1과 mode3는 상호 배타 분기이므로 호출 지점은 mode 분기 내 각각 또는 complete 직전 단일 지점에서 mode별 fps 선택 — 후자 권장 (주입 지점 단일화).
    3. mode3 fault_zoom 회귀 방지 (28-05 ratio 근사 제거 대비): `_attach_mode3_fault_zoom` 시그니처에 `dtw_match=None` kwarg 추가 → 내부 `build_fault_zoom_comparisons(..., dtw_match=dtw_match)` 전달 (kwarg는 fault_zoom에 이미 존재) → 호출부에서 `dtw_match=prev_dtw_match` 전달. 주석: "28-05가 시간비례 근사를 제거(D-04) — mode3도 DTW 대응으로 같은-pose 프레임 (전신 폴백 회귀 방지)".
    커밋 `feat(28-04): mode3 second+ 정렬 방출 + prev match를 mode3 zoom에 전달`.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && PYTHONPATH=shared/python:. python3 -m pytest tests/ -q -k "mode3 or pipeline_dispatch" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "prev_dtw_match" backend/functions/pipeline/app.py` >= 3 (반환 + 수령 + 전달)
    - `grep -c "dtw_match" backend/functions/pipeline/app.py` 에 _attach_mode3_fault_zoom 경로 포함 (grep -n으로 확인 기록)
    - 기존 mode3/pipeline 테스트 신규 FAILED 0
  </acceptance_criteria>
  <done>mode3 second+ doc에도 motionAlignment 방출 + mode3 zoom이 DTW 대응 사용 가능</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 채점 무접촉 + graceful + 방출 단위 + 상한 lockstep 테스트</name>
  <files>backend/tests/test_pipeline_motion_alignment.py</files>
  <read_first>
    - backend/tests/test_pipeline_dispatch.py:1-60 (pipeline 모듈 로드/모킹 관례)
    - backend/functions/pipeline/app.py `_attach_motion_alignment` (Task 1 산출)
    - backend/shared/python/sunity_shared/models.py MOTION_ALIGNMENT_MAX_ANCHOR_FLOATS + backend/shared/python/sunity_shared/analysis/motion_alignment.py MAX_ANCHOR_FLOATS (B1 lockstep 단언 대상)
    - 28-VALIDATION.md Per-Task Verification Map "채점 무접촉" 행
  </read_first>
  <behavior>
    - 무접촉: result dict(overallScore/deductionBreakdown 포함 fixture)에 _attach_motion_alignment 호출 → motionAlignment 키 추가 외 **모든 기존 키·값 동일** (deepcopy 대조 — diff 0)
    - 방출: 합성 MotionMatch(user 9fps, ref 18fps) → result["motionAlignment"]["anchors"] 값이 초 단위 (max ≤ 구간초+1), tier ∈ 3종
    - degenerate 방출 (W3): build 불가 입력(ref_fps=0 합성) → result["motionAlignment"]["tier"] == "disabled" + anchors == [] (키 존재 — legacy와 구분)
    - graceful: build_motion_alignment monkeypatch로 raise → _attach_motion_alignment이 raise 없이 통과 + result 무변경
    - 미방출: match=None → result에 motionAlignment 키 부재 (legacy)
    - validator 통과: 방출 dict(정상+degenerate 양쪽)가 firestore_admin._validate_motion_alignment를 raise 없이 통과 (방출↔저장 계약 정합)
    - 상한 lockstep (B1 — 28-03에서 이동): motion_alignment.MAX_ANCHOR_FLOATS == models.MOTION_ALIGNMENT_MAX_ANCHOR_FLOATS
  </behavior>
  <action>
    `test_pipeline_motion_alignment.py` 신설 — pipeline 모듈을 test_pipeline_dispatch 관례로 로드해 `_attach_motion_alignment`를 직접 단위 테스트 (behavior 블록 7케이스). 무접촉 테스트가 이 phase의 hard 게이트임을 docstring에 명시: "채점 무접촉 (28-CONTEXT '점수·verdict 절대 불변') — alignment 방출의 유일 부작용 = motionAlignment 키 1개". **상한 lockstep 단언 (B1):** `motion_alignment.MAX_ANCHOR_FLOATS == models.MOTION_ALIGNMENT_MAX_ANCHOR_FLOATS` — 28-03의 test_motion_alignment_contract.py에서 이 파일로 이동된 단언 (같은 wave 2에서 28-02 산출물을 import하면 병렬 실행 collection error — 이 플랜은 wave 3, depends_on에 28-02/28-03 모두 있어 안전). 테스트 docstring에 이동 사유 주석.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && PYTHONPATH=shared/python:. python3 -m pytest tests/test_pipeline_motion_alignment.py -q</automated>
  </verify>
  <acceptance_criteria>
    - 7+ 테스트 전부 PASS
    - `grep -c "deepcopy" backend/tests/test_pipeline_motion_alignment.py` >= 1 (diff 0 대조 방식)
    - `grep -c "MOTION_ALIGNMENT_MAX_ANCHOR_FLOATS" backend/tests/test_pipeline_motion_alignment.py` >= 1 (B1 이동분)
  </acceptance_criteria>
  <done>채점 무접촉이 기계 판정으로 증명됨 (ALGN-06) + 상한 lockstep이 wave-안전한 자리에서 잠김 (B1)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 정렬 방출 → 채점 경계 | result의 점수 필드에 어떤 변경도 금지 (phase hard 제약) |
| 파이프라인 → Firestore | complete_analysis 단일 관문 (27-06 게이트: complete 후 result.* write 금지) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-04 | Tampering (채점 오염) | _attach_motion_alignment | mitigate | deepcopy diff-0 무접촉 테스트 (Task 3) + 키 1개 부작용 규칙 |
| T-28-09 | DoS (분석 실패 유발) | 방출 경로 예외 | mitigate | try/except graceful skip — 방출 실패가 분석을 못 죽임 (Task 1) |
| T-28-10 | Tampering (fps 오적용 → 오도 데이터) | ref_fps / user_fps 결정 | mitigate | ref는 doc 메타(keypointReport.fps)에서만, user는 frame_extractor 상수 단일 출처(I1) — 리터럴 하드코딩 금지 + fps<=0 → degenerate 'disabled' 방출 (W3 — 오도 앵커 0개, legacy와 구분) (Task 1) |
| T-28-11 | Tampering (veto still 경로 접촉) | app.py:1720 | mitigate | 이 플랜은 `_build_selected_frame_pair` 무접촉 — git diff 검토로 확인, 28-08 게이트에서 재확인 |
| T-28-SC | Tampering | 패키지 설치 | accept | 신규 패키지 0 |
</threat_model>

<verification>
- 선행 확인: 27-06 심볼 (미존재 시 중단)
- test_pipeline_motion_alignment.py GREEN + affected pipeline 테스트 신규 FAILED 0
- `git diff` 범위에 vision_veto.py / kismam.py / dimensions.py / deduction 계열 0
</verification>

<success_criteria>
- mode1/mode3 새 분석 doc에 motionAlignment 존재 (초 단위, tier 포함 — degenerate는 disabled)
- 채점 diff 0 기계 증명, graceful 실패
- fps 출처 단일화 (I1) + 상한 lockstep 이동 (B1)
</success_criteria>

<output>
Create `.planning/phases/28-dtw-motion-based-alignment/28-04-SUMMARY.md` when done
</output>
