---
phase: 28-dtw-motion-based-alignment
plan: 05
type: execute
wave: 3
depends_on: [28-03]
files_modified:
  - backend/shared/python/sunity_shared/analysis/fault_zoom.py
  - backend/tests/test_fault_zoom_ref_match.py
autonomous: true
requirements: [ALGN-03, ALGN-06]
must_haves:
  truths:
    - "fault_zoom의 시간비례 근사 fallback이 코드에서 사라진다 (D-04 — 엉뚱한 부위 확대 오도 0)"
    - "DTW 성공 경로가 ref 18fps 인덱스를 9fps frames 인덱스로 올바르게 변환한다 (D2 진짜 메커니즘 fix — 근사 제거만으론 D2 안 잡힘, Pitfall 4)"
    - "대응 실패 시 기준(정은지) 측은 전신 폴백 + refMatch='failed' 캡션 플래그, 학생 카드는 유지"
    - "veto still 경로(_build_selected_frame_pair)와 _matched_ref_frame 본체는 무변경 — 점수 이동 0"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/fault_zoom.py"
      provides: "표시 경로 전용 fps 정합 + ratio 근사 제거 + refMatch 방출"
      contains: "refMatch"
    - path: "backend/tests/test_fault_zoom_ref_match.py"
      provides: "fps 변환/전신 폴백/refMatch/근사 부재 회귀 가드"
      min_lines: 60
  key_links:
    - from: "backend/shared/python/sunity_shared/analysis/fault_zoom.py"
      to: "_to_rep_idx"
      via: "rep(=angles) 인덱스 → frames 인덱스 역변환 — 단일 공식 재사용 (quick-260705-ftn 관례)"
      pattern: "_to_rep_idx"
---

<objective>
D-04 — fault_zoom 시간비례 근사 제거 + fps 도메인 정합. 두 작업은 한 몸 (28-RESEARCH Pitfall 4): (a) `build_fault_zoom_comparisons`의 ratio 근사 else 블록(:848-859) 제거 → 대응 실패 시 기준 측 전신 폴백 + `refMatch: "failed"` scalar 방출 (앱이 캡션 렌더 — PNG에 굽지 않음, 카피 수정에 재분석 불필요), (b) DTW "성공" 경로의 fps 오독 fix — `_matched_ref_frame`이 반환하는 ref 인덱스는 **ref angles fps 공간**(phase4_v1=18fps, 28-01 실측)인데 현재 9fps frames 배열에 그대로 인덱싱(시간 2배 오독/끝프레임 클램프 = D2 그 자체).

**경계 (hard):** `_matched_ref_frame` **본체 로직 수정 금지** — veto still 경로(app.py:1720 `_build_selected_frame_pair`)가 같은 함수를 공유하며 그쪽 입력이 바뀌면 점수가 움직인다 (Gemini veto still = 채점 인접). fix는 fault_zoom 내부 **호출측**(clamp 인자 교정 + 반환값 도메인 변환)에만 — 표시 경로 전용 (28-RESEARCH Open Q2, belle 통지됨). docstring의 stale "9fps 절대" 서술 정정은 허용 (동작 무변경).

시나리오 태그: 6단계(결과 확대비교) — D2 재발 방지.

**선행 전제:** 28-03(refMatch 계약) 실행 후. fault_zoom.py는 Phase 26/27이 건드리지 않음 (RESEARCH 겹침 매트릭스) — Phase 27 선행 불요. 단 mode3 경로의 dtw_match 공급은 28-04 Task 2가 담당 (이 플랜은 fault_zoom.py만).

Purpose: "정은지 쪽이 비교 부위 아닌 곳 확대"(파일럿 D2) 영구 종결.
Output: fps 정합 + 근사 0 + 캡션 플래그.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-dtw-motion-based-alignment/28-CONTEXT.md
@.planning/phases/28-dtw-motion-based-alignment/28-RESEARCH.md
@.planning/phases/28-dtw-motion-based-alignment/28-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DTW 성공 경로 fps 정합 — 표시 경로 전용 변환</name>
  <files>backend/shared/python/sunity_shared/analysis/fault_zoom.py, backend/tests/test_fault_zoom_ref_match.py</files>
  <read_first>
    - backend/shared/python/sunity_shared/analysis/fault_zoom.py:177-187 (_to_rep_idx — 변환 공식 단일 출처, "중복 공식 금지" quick-260705-ftn), :250-268 (_matched_ref_frame — 본체 무수정 대상, docstring stale), :772-860 (build_fault_zoom_comparisons — u_rep_fps/r_rep_fps/r_rep_frames 지역 변수 + DTW 분기)
    - backend/tests/test_fault_zoom.py:1-50 (기존 테스트 관례 — 합성 report/frames fixture)
    - 28-RESEARCH.md Pattern 4 + Pitfall 1 (fps 도메인)
  </read_first>
  <behavior>
    - 합성: ref angles 18fps (r_rep_fps=18, r_rep_frames=2N), frames 9fps (r_n=N), path=[(i, 2*i)] → 반환 카드의 ref frames 인덱스가 round(2*i/18*9)=i (시간 동일 지점) — 기존 코드였다면 2*i로 오독됐을 케이스
    - 9fps/9fps (mode3 도메인): path=[(i,i)] → ref frames 인덱스 == i (변환이 identity — mode1 전용 규칙 하드코딩 아님을 증명, Pitfall 6)
    - clamp: ref angles 인덱스가 r_rep_frames-1 에서 클램프 (frames 수 r_n이 아니라 — 도메인 교정)
  </behavior>
  <action>
    `build_fault_zoom_comparisons`의 DTW 분기 (심볼: `r_matched = _matched_ref_frame(`) 수정 — **본체가 아닌 호출측**:
    1. clamp 도메인 교정: `_matched_ref_frame(dtw_match, u_idx, r_rep_frames)` — 세 번째 인자를 frames 수(r_n)에서 keypointReport frames 수(r_rep_frames = angles 도메인 수; anglesFrames==keypointReport.frames 는 28-01 실측 정합)로 교체. r_rep_frames<=0이면 DTW 분기 자체를 실패 취급 (Task 2 폴백 경로).
    2. 반환값 도메인 변환: `r_matched`는 ref angles(=rep) 인덱스이므로 `r_kp_idx = r_matched` (이미 rep 공간 — 추가 변환 불필요), `r_idx = _to_rep_idx(r_matched, r_rep_fps, frames_fps, r_n)` (rep→frames 역방향 — 같은 공식에 fps 인자 순서만 반대, 중복 공식 금지 관례 준수. 별도 helper를 만들지 말고 _to_rep_idx 재사용 + 호출부 주석 "rep→frames 역변환 (28-RESEARCH Pitfall 1, D2 fix)").
    3. `_matched_ref_frame` docstring 정정 (로직 무변경): "ref_idx = 기준 angles 9fps 절대"를 "ref_idx = 기준 angles 인덱스 — **ref doc의 keypointReport.fps 공간** (phase4_v1 재처리 18fps, 28-01 실측). 호출측이 clamp 상한과 frames 변환을 도메인에 맞게 책임진다. **본체 수정 금지** — veto still 경로(app.py `_build_selected_frame_pair`) 공유, 점수 인접 (28-RESEARCH Open Q2)"로 교체. build_fault_zoom_comparisons docstring의 "시간비례(ratio)로 근사" 문장도 Task 2 반영으로 갱신.
    4. behavior 케이스를 `test_fault_zoom_ref_match.py` 신설 파일에 작성 (RED→GREEN 커밋 분리: `test(28-05)` 후 `fix(28-05)`).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && PYTHONPATH=shared/python:. python3 -m pytest tests/test_fault_zoom_ref_match.py tests/test_fault_zoom.py tests/test_fault_zoom_relaxed_crop.py -q</automated>
  </verify>
  <acceptance_criteria>
    - 신규 + 기존 fault_zoom 테스트 전부 PASS (신규 FAILED 0)
    - `git diff --name-only`에 fault_zoom.py + 신규 테스트만 (app.py/vision_veto.py 0 — veto 경로 무접촉)
    - `_matched_ref_frame` 함수 본체 diff = docstring만 (`git diff -U0 backend/shared/python/sunity_shared/analysis/fault_zoom.py` 검토 기록)
  </acceptance_criteria>
  <done>DTW 성공 카드가 같은 시간 지점의 정은지 프레임을 잡음 (2배 오독 종결)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ratio 근사 제거 + 전신 폴백 + refMatch 방출 (D-04)</name>
  <files>backend/shared/python/sunity_shared/analysis/fault_zoom.py, backend/tests/test_fault_zoom_ref_match.py</files>
  <read_first>
    - backend/shared/python/sunity_shared/analysis/fault_zoom.py:848-859 (제거 대상 else 블록 — 28-PATTERNS 발췌), :819-821 (3단 강하 전신 폴백 선례 Phase 25-03 / 260702-sic), 방출 dict 조립부 (심볼: "region" scalar 추가 지점)
    - app/src/types/analysis.ts FaultZoomComparison.refMatch (28-03 산출 — 계약)
    - 28-CONTEXT.md D-04
  </read_first>
  <behavior>
    - dtw_match=None (또는 대응 없음) → 카드 생성됨(학생 측 유지) + ref 측은 전신 crop + item["refMatch"] == "failed"
    - DTW 대응 성공 → item["refMatch"] == "dtw"
    - ratio 근사 코드 부재: 소스에서 `ratio * (r_n - 1)` / `ratio * (r_rep_frames - 1)` 패턴 0 (주석 제외 — grep -v '^\s*#')
    - 방출 dict의 refMatch는 str scalar (nested 금지 정합 — 기존 region 선례와 동일)
  </behavior>
  <action>
    1. else 블록(ratio 근사) 삭제. 대응 실패(`r_matched is None` 또는 r_rep_frames<=0) 시: `r_idx`/`r_kp_idx`를 학생과 같은 시간비례로 잡는 대신 — **ref 측 전신 폴백 강제**: ref 측 crop을 3단 강하의 전신 폴백 단계로 직행시키는 지역 플래그 (예: `ref_match_failed = True`) 도입, `_side_crop` 호출 경로에서 ref 측만 전신 경로 사용 (기존 좌표-결측 전신 폴백 분기 재사용 — 새 렌더 금지, Don't Hand-Roll). ref 프레임 자체는 중앙 프레임(r_n//2) 사용 (전신이므로 어느 순간이든 오도 없음 — 주석에 근거). 학생 측 카드는 무변경 유지 (D-04 "학생 쪽 카드는 유지").
    2. 방출 dict에 scalar 추가: 매 item에 `"refMatch": "failed" if ref_match_failed else "dtw"` (region 선례 형식). override 경로(`ref_frame_idx is not None`)는 vision 측정 프레임 정합이 이미 보장되므로 `"dtw"` 취급 (주석 명시).
    3. docstring "시간비례 근사(held pose MVP)" 서술 제거 → "대응 실패 = ref 전신 폴백 + refMatch='failed' (D-04, 260702-sic confidence<0.5 전신 폴백과 일관 — 오도 0, 정보 보존)".
    4. behavior 케이스 테스트 추가 (같은 신규 파일).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && PYTHONPATH=shared/python:. python3 -m pytest tests/test_fault_zoom_ref_match.py tests/test_fault_zoom.py tests/test_fault_zoom_relaxed_crop.py -q && grep -v '^\s*#' backend/shared/python/sunity_shared/analysis/fault_zoom.py | grep -c "ratio \* (r_n - 1)" | grep -qx 0 && echo RATIO_REMOVED</automated>
  </verify>
  <acceptance_criteria>
    - 전체 fault_zoom 테스트 PASS + "RATIO_REMOVED" 출력 (주석 제외 grep 0 — grep 게이트 위생 준수)
    - `grep -c "refMatch" backend/shared/python/sunity_shared/analysis/fault_zoom.py` >= 2 (방출 + docstring)
    - 기존 test_fault_zoom*.py 신규 FAILED 0
  </acceptance_criteria>
  <done>엉뚱한 부위 확대 경로가 코드에서 소멸 — 실패는 정직한 전신+캡션 (D-04 완결)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 표시 경로 ↔ 채점 인접(veto still) 경계 | `_matched_ref_frame` 공유 — 본체 변경 시 점수 이동 |
| 서버 렌더 → 사용자 | 잘못된 프레임 페어 = 오도된 비교 노출 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-12 | Tampering (점수 이동) | _matched_ref_frame 본체 | mitigate | 본체 diff=docstring-only 게이트 + app.py 무접촉 (Task 1 acceptance) |
| T-28-13 | Integrity (오도된 비교) | ratio 근사 / fps 오독 | mitigate | 근사 제거 grep 0 + fps 변환 합성 테스트 + 실패 시 전신 폴백(오도 0) |
| T-28-14 | Repudiation (실패 은폐) | refMatch 플래그 | mitigate | scalar provenance 방출 — 앱 캡션으로 사용자에게 정직 고지 (28-07) |
| T-28-SC | Tampering | 패키지 설치 | accept | 신규 패키지 0 |
</threat_model>

<verification>
- fault_zoom 테스트 전체 GREEN + RATIO_REMOVED grep 게이트
- git diff 범위 = fault_zoom.py + 신규 테스트만
</verification>

<success_criteria>
- D2 메커니즘 2종(근사 폴백 + fps 오독) 모두 종결, 실패는 정직 표기
- 채점/veto 경로 diff 0
</success_criteria>

<output>
Create `.planning/phases/28-dtw-motion-based-alignment/28-05-SUMMARY.md` when done
</output>
