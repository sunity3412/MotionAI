---
phase: 28-dtw-motion-based-alignment
plan: 05
type: execute
wave: 4
depends_on: [28-03, 28-04]
files_modified:
  - backend/shared/python/sunity_shared/analysis/fault_zoom.py
  - backend/functions/pipeline/app.py
  - backend/tests/test_fault_zoom_ref_match.py
autonomous: true
requirements: [ALGN-03, ALGN-06]
must_haves:
  truths:
    - "fault_zoom의 시간비례 근사 fallback이 코드에서 사라진다 (D-04 — 엉뚱한 부위 확대 오도 0)"
    - "DTW 성공 경로가 ref 18fps 인덱스를 9fps frames 인덱스로 올바르게 변환한다 (D2 진짜 메커니즘 fix — 근사 제거만으론 D2 안 잡힘, Pitfall 4)"
    - "대응 실패 시 기준(정은지) 측은 전신 폴백 + refMatch='failed' 캡션 플래그, 학생 카드는 유지"
    - "refMatch가 app.py zoom result mapper(_render_fault_zoom 재조립 루프 :2625-2642)를 통과해 최종 result['faultZoomComparisons'][*].refMatch로 생존한다 (리뷰 HIGH-1 — fault_zoom.py 방출만으로는 mapper가 키를 버림)"
    - "veto still 경로(_build_selected_frame_pair)와 _matched_ref_frame 본체는 무변경 — 점수 이동 0"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/fault_zoom.py"
      provides: "표시 경로 전용 fps 정합 + ratio 근사 제거 + refMatch 방출"
      contains: "refMatch"
    - path: "backend/functions/pipeline/app.py"
      provides: "_render_fault_zoom 재조립 루프의 refMatch scalar pass-through (zoom result mapping 함수 한정 — 좁은 안전 게이트, HIGH-1)"
      contains: "refMatch"
    - path: "backend/tests/test_fault_zoom_ref_match.py"
      provides: "fps 변환/전신 폴백/refMatch/근사 부재 회귀 가드 + mapper-level refMatch 생존 테스트"
      min_lines: 60
  key_links:
    - from: "backend/shared/python/sunity_shared/analysis/fault_zoom.py"
      to: "_to_rep_idx"
      via: "rep(=angles) 인덱스 → frames 인덱스 역변환 — 단일 공식 재사용 (quick-260705-ftn 관례)"
      pattern: "_to_rep_idx"
    - from: "backend/functions/pipeline/app.py"
      to: "backend/shared/python/sunity_shared/analysis/fault_zoom.py"
      via: "_render_fault_zoom 재조립 루프가 c.get('refMatch') 검증 복사 (region pass-through :2640-2641 선례)"
      pattern: "refMatch"
---

<objective>
D-04 — fault_zoom 시간비례 근사 제거 + fps 도메인 정합 + refMatch가 Firestore doc까지 생존하는 pass-through. 세 작업은 한 몸: (a) `build_fault_zoom_comparisons`의 ratio 근사 else 블록(:848-859) 제거 → 대응 실패 시 기준 측 전신 폴백 + `refMatch: "failed"` scalar 방출 (앱이 캡션 렌더 — PNG에 굽지 않음, 카피 수정에 재분석 불필요), (b) DTW "성공" 경로의 fps 오독 fix — `_matched_ref_frame`이 반환하는 ref 인덱스는 **ref angles fps 공간**(phase4_v1=18fps, 28-01 실측)인데 현재 9fps frames 배열에 그대로 인덱싱(시간 2배 오독/끝프레임 클램프 = D2 그 자체), (c) **HIGH-1 (리뷰)**: app.py `_render_fault_zoom`(:2528, 재조립 루프 :2625-2642)이 선택 키(`joint/deficitDeg/imageUrl/tier/kind/region`)만 재조립하며 refMatch를 버림 — pass-through를 넣지 않으면 fault_zoom.py 테스트가 전부 통과해도 앱은 `FaultZoomComparison.refMatch`를 영원히 못 보고 D-04 캡션 경로(28-07)가 사망, 28-08 end-to-end 검사에서 늦게 터짐.

**경계 (hard):** `_matched_ref_frame` **본체 로직 수정 금지** — veto still 경로(app.py:1688 `_build_selected_frame_pair`, 호출 :1852)가 같은 함수를 공유하며 그쪽 입력이 바뀌면 점수가 움직인다 (Gemini veto still = 채점 인접). fps fix는 fault_zoom 내부 **호출측**(clamp 인자 교정 + 반환값 도메인 변환)에만 — 표시 경로 전용 (28-RESEARCH Open Q2, belle 통지됨). app.py 수정 허용 범위는 **zoom result mapping 함수만** (`_render_fault_zoom` 및 27-06 이후 zoom 방출이 사용하는 mapper) — `_build_selected_frame_pair` 무접촉 grep/diff 게이트 (Task 2 acceptance). docstring의 stale "9fps 절대" 서술 정정은 허용 (동작 무변경).

시나리오 태그: 6단계(결과 확대비교) — D2 재발 방지.

**선행 전제:** (1) 28-03(refMatch 계약) + **28-04(app.py 소유권 + mode3 prev dtw_match 배선) 실행 후** — 리뷰 권고 5는 "depends_on만(wave 이동 없이)"과 "mode3 일시 부재 허용 명시" 중 택일이었으나, HIGH-1 fix로 이 플랜이 app.py를 수정하게 되면서 같은 wave 3의 28-04와 files_modified 겹침 발생 → 같은 wave 병렬 실행 시 파일 소유권 충돌이라 wave 4로 이동 (option A 채택, DAG 영향: 28-07→wave 5, 28-08→wave 6). 부수 효과로 28-04의 mode3 prev match 전달이 항상 선행되므로 "mode3 refMatch 일시 부재" 상황 자체가 소멸. (2) **27-06 실행 완료 후 착수** — app.py zoom 방출이 post-complete `update_analysis_fault_zoom` 경로로 이동한 뒤이므로, Task 2의 심볼 선행 확인 스텝(`update_analysis_fault_zoom` 포함)이 미실행을 감지하면 중단·보고.

Purpose: "정은지 쪽이 비교 부위 아닌 곳 확대"(파일럿 D2) 영구 종결 + refMatch의 end-to-end 생존.
Output: fps 정합 + 근사 0 + 캡션 플래그 (Firestore doc까지 도달 증명).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-dtw-motion-based-alignment/28-CONTEXT.md
@.planning/phases/28-dtw-motion-based-alignment/28-RESEARCH.md
@.planning/phases/28-dtw-motion-based-alignment/28-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DTW 성공 경로 fps 정합 — 표시 경로 전용 변환</name>
  <files>backend/shared/python/sunity_shared/analysis/fault_zoom.py, backend/tests/test_fault_zoom_ref_match.py</files>
  <read_first>
    - backend/shared/python/sunity_shared/analysis/fault_zoom.py:177-187 (_to_rep_idx — 변환 공식 단일 출처, "중복 공식 금지" quick-260705-ftn), :250-268 (_matched_ref_frame — 본체 무수정 대상, docstring stale), :772-860 (build_fault_zoom_comparisons — u_rep_fps/r_rep_fps/r_rep_frames 지역 변수 + DTW 분기)
    - backend/tests/test_fault_zoom.py:1-50 (기존 테스트 관례 — 합성 report/frames fixture)
    - 28-RESEARCH.md Pattern 4 + Pitfall 1 (fps 도메인)
  </read_first>
  <behavior>
    - 합성: ref angles 18fps (r_rep_fps=18, r_rep_frames=2N), frames 9fps (r_n=N), path=[(i, 2*i)] → 반환 카드의 ref frames 인덱스가 round(2*i/18*9)=i (시간 동일 지점) — 기존 코드였다면 2*i로 오독됐을 케이스
    - 9fps/9fps (mode3 도메인): path=[(i,i)] → ref frames 인덱스 == i (변환이 identity — mode1 전용 규칙 하드코딩 아님을 증명, Pitfall 6)
    - clamp: ref angles 인덱스가 r_rep_frames-1 에서 클램프 (frames 수 r_n이 아니라 — 도메인 교정)
  </behavior>
  <action>
    `build_fault_zoom_comparisons`의 DTW 분기 (심볼: `r_matched = _matched_ref_frame(`) 수정 — **본체가 아닌 호출측**:
    1. clamp 도메인 교정: `_matched_ref_frame(dtw_match, u_idx, r_rep_frames)` — 세 번째 인자를 frames 수(r_n)에서 keypointReport frames 수(r_rep_frames = angles 도메인 수; anglesFrames==keypointReport.frames 는 28-01 실측 정합)로 교체. r_rep_frames<=0이면 DTW 분기 자체를 실패 취급 (Task 2 폴백 경로).
    2. 반환값 도메인 변환: `r_matched`는 ref angles(=rep) 인덱스이므로 `r_kp_idx = r_matched` (이미 rep 공간 — 추가 변환 불필요), `r_idx = _to_rep_idx(r_matched, r_rep_fps, frames_fps, r_n)` (rep→frames 역방향 — 같은 공식에 fps 인자 순서만 반대, 중복 공식 금지 관례 준수. 별도 helper를 만들지 말고 _to_rep_idx 재사용 + 호출부 주석 "rep→frames 역변환 (28-RESEARCH Pitfall 1, D2 fix)").
    3. `_matched_ref_frame` docstring 정정 (로직 무변경): "ref_idx = 기준 angles 9fps 절대"를 "ref_idx = 기준 angles 인덱스 — **ref doc의 keypointReport.fps 공간** (phase4_v1 재처리 18fps, 28-01 실측). 호출측이 clamp 상한과 frames 변환을 도메인에 맞게 책임진다. **본체 수정 금지** — veto still 경로(app.py `_build_selected_frame_pair`) 공유, 점수 인접 (28-RESEARCH Open Q2)"로 교체. build_fault_zoom_comparisons docstring의 "시간비례(ratio)로 근사" 문장도 Task 2 반영으로 갱신.
    4. behavior 케이스를 `test_fault_zoom_ref_match.py` 신설 파일에 작성 (RED→GREEN 커밋 분리: `test(28-05)` 후 `fix(28-05)`).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && PYTHONPATH=shared/python:. python3 -m pytest tests/test_fault_zoom_ref_match.py tests/test_fault_zoom.py tests/test_fault_zoom_relaxed_crop.py -q</automated>
  </verify>
  <acceptance_criteria>
    - 신규 + 기존 fault_zoom 테스트 전부 PASS (신규 FAILED 0)
    - 이 Task 시점 `git diff --name-only`에 fault_zoom.py + 신규 테스트만 (app.py 수정은 Task 2 소관 — vision_veto.py는 plan 전체에서 0)
    - `_matched_ref_frame` 함수 본체 diff = docstring만 (`git diff -U0 backend/shared/python/sunity_shared/analysis/fault_zoom.py` 검토 기록)
  </acceptance_criteria>
  <done>DTW 성공 카드가 같은 시간 지점의 정은지 프레임을 잡음 (2배 오독 종결)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ratio 근사 제거 + 전신 폴백 + refMatch 방출·mapper pass-through (D-04, HIGH-1)</name>
  <files>backend/shared/python/sunity_shared/analysis/fault_zoom.py, backend/functions/pipeline/app.py, backend/tests/test_fault_zoom_ref_match.py</files>
  <read_first>
    - backend/shared/python/sunity_shared/analysis/fault_zoom.py:848-859 (제거 대상 else 블록 — 28-PATTERNS 발췌), :819-821 (3단 강하 전신 폴백 선례 Phase 25-03 / 260702-sic), :925-935 (방출 item dict 조립부 — kind/region scalar 추가 선례)
    - backend/functions/pipeline/app.py:2528 (`def _render_fault_zoom` — Firestore-facing item 재조립 :2625-2642, region pass-through :2640-2641, `result["faultZoomComparisons"] = out` :2643-2644, 호출부 :2750/:2831), :1688 (`_build_selected_frame_pair` — 무접촉 대상)
    - backend/shared/python/sunity_shared/firestore_admin.py — 심볼: `def update_analysis_fault_zoom` (27-06 산출 — post-complete zoom 경로)
    - backend/tests/test_pipeline_dispatch.py:1-60 (pipeline 모듈 로드/모킹 관례 — mapper-level 테스트용)
    - app/src/types/analysis.ts FaultZoomComparison.refMatch (28-03 산출 — 계약)
    - 28-CONTEXT.md D-04
  </read_first>
  <behavior>
    - dtw_match=None (또는 대응 없음) → 카드 생성됨(학생 측 유지) + ref 측은 전신 crop + item["refMatch"] == "failed"
    - DTW 대응 성공 → item["refMatch"] == "dtw"
    - ratio 근사 코드 부재: 소스에서 `ratio * (r_n - 1)` / `ratio * (r_rep_frames - 1)` 패턴 0 (주석 제외 — grep -v '^\s*#')
    - 방출 dict의 refMatch는 str scalar (nested 금지 정합 — 기존 region 선례와 동일)
    - **mapper 생존 (HIGH-1)**: build_fault_zoom_comparisons가 refMatch 포함 카드를 반환하면, app.py `_render_fault_zoom`을 통과한 최종 `result["faultZoomComparisons"][0]["refMatch"]`가 동일 값으로 존재. refMatch 없는 카드(legacy 형상)는 최종 item에도 키 부재 (region 선례와 동일한 조건부 복사)
  </behavior>
  <action>
    **선행 확인 스텝 (심볼 기준 — 27-06):** `grep -c "def update_analysis_fault_zoom" backend/shared/python/sunity_shared/firestore_admin.py` >= 1 확인. 0이면 **작업 중단** — "27-06 미실행 — Phase 27 실행 후 재개" 보고 (파일 수정 0).

    1. else 블록(ratio 근사) 삭제. 대응 실패(`r_matched is None` 또는 r_rep_frames<=0) 시: `r_idx`/`r_kp_idx`를 학생과 같은 시간비례로 잡는 대신 — **ref 측 전신 폴백 강제**: ref 측 crop을 3단 강하의 전신 폴백 단계로 직행시키는 지역 플래그 (예: `ref_match_failed = True`) 도입, `_side_crop` 호출 경로에서 ref 측만 전신 경로 사용 (기존 좌표-결측 전신 폴백 분기 재사용 — 새 렌더 금지, Don't Hand-Roll). ref 프레임 자체는 중앙 프레임(r_n//2) 사용 (전신이므로 어느 순간이든 오도 없음 — 주석에 근거). 학생 측 카드는 무변경 유지 (D-04 "학생 쪽 카드는 유지").
    2. 방출 dict에 scalar 추가 (:925-935 item 조립부, region 선례 형식): 매 item에 `"refMatch": "failed" if ref_match_failed else "dtw"`. override 경로(`ref_frame_idx is not None`)는 vision 측정 프레임 정합이 이미 보장되므로 `"dtw"` 취급 (주석 명시).
    3. **app.py mapper pass-through (HIGH-1 — 이 스텝 없이는 refMatch가 Firestore에 도달 못 함):** `_render_fault_zoom`의 재조립 루프(:2625-2642)에서 region pass-through(:2640-2641) 직후에 검증 복사 추가: `if c.get("refMatch") in ("dtw", "failed"): item["refMatch"] = c["refMatch"]` — scalar str이라 _validate_dict_only_scalars flat 제약 통과, TS lockstep 주석(FaultZoomComparison.refMatch, 부재=legacy=캡션 없음) region 선례와 동일 형식. **27-06이 zoom 방출을 post-complete `update_analysis_fault_zoom` 경로로 옮긴 상태이므로**, 그 경로가 사용하는 item mapper를 심볼 재탐색으로 확인 — `_render_fault_zoom`을 재사용하면 위 1곳으로 충분, 별도 mapper가 있으면 그곳에도 동일 pass-through 적용. 확인 결과(재사용 여부 + 적용 지점)를 SUMMARY에 기록.
    4. **mapper-level unit test (HIGH-1 — build_fault_zoom_comparisons 단독 테스트로는 불충분):** test_pipeline_dispatch 관례로 pipeline 모듈 로드, `_s3.put_object`/`_signed_get` monkeypatch(스텁), **`sunity_shared.analysis.frame_extractor.FfmpegFrameExtractor`도 monkeypatch 필수** (체커 3회차 WARNING-1: `_render_fault_zoom`이 mock된 comps에 도달하기 전에 양쪽 영상 경로에 `ext.extract()`를 호출 — 함수 내 lazy import라 module-attr patch로 fake extractor(더미 프레임 반환) 주입), `fault_zoom.build_fault_zoom_comparisons`를 refMatch 포함 합성 카드(png bytes 포함) 반환으로 monkeypatch → `_render_fault_zoom` 호출 → 최종 `result["faultZoomComparisons"][0]["refMatch"]` 생존 단언 ("failed"/"dtw" 각 1케이스) + refMatch 미포함 카드 → 최종 item에 키 부재 단언.
    5. docstring "시간비례 근사(held pose MVP)" 서술 제거 → "대응 실패 = ref 전신 폴백 + refMatch='failed' (D-04, 260702-sic confidence<0.5 전신 폴백과 일관 — 오도 0, 정보 보존)".
    6. behavior 케이스 테스트 추가 (같은 신규 파일).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && PYTHONPATH=shared/python:. python3 -m pytest tests/test_fault_zoom_ref_match.py tests/test_fault_zoom.py tests/test_fault_zoom_relaxed_crop.py -q && grep -v '^\s*#' backend/shared/python/sunity_shared/analysis/fault_zoom.py | grep -c "ratio \* (r_n - 1)" | grep -qx 0 && git diff backend/functions/pipeline/app.py | grep -c "_build_selected_frame_pair" | grep -qx 0 && echo RATIO_REMOVED_MAPPER_SAFE</automated>
  </verify>
  <acceptance_criteria>
    - 전체 fault_zoom 테스트 + mapper-level 테스트 PASS + "RATIO_REMOVED_MAPPER_SAFE" 출력 (주석 제외 grep 0 — grep 게이트 위생 준수)
    - `grep -c "refMatch" backend/shared/python/sunity_shared/analysis/fault_zoom.py` >= 2 (방출 + docstring)
    - `grep -c "refMatch" backend/functions/pipeline/app.py` >= 1 (mapper pass-through — HIGH-1)
    - **좁은 안전 게이트 (기존 "app.py 0" diff 게이트 교체):** `git diff --name-only`에 fault_zoom.py + app.py + 신규 테스트만; `git diff -U0 backend/functions/pipeline/app.py`의 hunk가 zoom result mapping 함수(`_render_fault_zoom` 및 27-06 zoom 경로 mapper) 범위만 — `_build_selected_frame_pair` 무접촉 (verify의 diff grep 0), vision_veto.py 0
    - 기존 test_fault_zoom*.py 신규 FAILED 0
  </acceptance_criteria>
  <done>엉뚱한 부위 확대 경로가 코드에서 소멸 — 실패는 정직한 전신+캡션, refMatch는 Firestore doc까지 생존 (D-04 + HIGH-1 완결)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 표시 경로 ↔ 채점 인접(veto still) 경계 | `_matched_ref_frame` 공유 — 본체 변경 시 점수 이동. app.py 수정은 zoom mapper 함수 한정 |
| 서버 렌더 → 사용자 | 잘못된 프레임 페어 = 오도된 비교 노출 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-12 | Tampering (점수 이동) | _matched_ref_frame 본체 / _build_selected_frame_pair | mitigate | 본체 diff=docstring-only 게이트 + app.py 변경은 zoom mapper hunk 한정 + `_build_selected_frame_pair` diff grep 0 (Task 2 verify) |
| T-28-13 | Integrity (오도된 비교) | ratio 근사 / fps 오독 | mitigate | 근사 제거 grep 0 + fps 변환 합성 테스트 + 실패 시 전신 폴백(오도 0) |
| T-28-14 | Repudiation (실패 은폐) | refMatch 플래그 | mitigate | scalar provenance 방출 + **mapper pass-through 생존 unit test (HIGH-1)** — 앱 캡션으로 사용자에게 정직 고지 (28-07) |
| T-28-SC | Tampering | 패키지 설치 | accept | 신규 패키지 0 |
</threat_model>

<verification>
- fault_zoom 테스트 전체 GREEN + RATIO_REMOVED_MAPPER_SAFE 게이트 (근사 부재 + _build_selected_frame_pair 무접촉)
- mapper-level 테스트: result["faultZoomComparisons"][0]["refMatch"] 생존 (HIGH-1)
- git diff 범위 = fault_zoom.py + app.py(zoom mapper hunk 한정) + 신규 테스트만
</verification>

<success_criteria>
- D2 메커니즘 2종(근사 폴백 + fps 오독) 모두 종결, 실패는 정직 표기
- refMatch가 fault_zoom → app.py mapper → Firestore doc까지 end-to-end 생존 (28-07 캡션 경로 생존)
- 채점/veto 경로 diff 0
</success_criteria>

<output>
Create `.planning/phases/28-dtw-motion-based-alignment/28-05-SUMMARY.md` when done
</output>
