---
phase: 28-dtw-motion-based-alignment
plan: 06
type: execute
wave: 3
depends_on: [28-03]
files_modified:
  - app/src/lib/alignmentWarp.ts
  - app/src/components/VideoCompare.tsx
autonomous: true
requirements: [ALGN-04]
must_haves:
  truths:
    - "정은지(right) 재생이 학생(left) 타임라인에 워핑된다 — rate feedforward + tick 보정 seek feedback 이중 제어 (D-01)"
    - "rightPlayer.currentTime 직접 대입이 helper 2개 내부에만 존재 — setRightToStudentTime(warp 경유)/setBothAbsoluteTime(!alignmentActive 전용). 정렬 활성 경로의 tick/seekBoth/togglePlay/restart 호출부는 전부 setRightToStudentTime 경유 (Pitfall 7 + 리뷰 MEDIUM-1: togglePlay slowerTime 절대시간 대입이 활성 경로에 남는 것 차단)"
    - "tier=trim_only는 오프셋만+rate 1.0, tier=disabled/필드 부재는 현행 절대시계 100% 보존 (D-02 사다리 소비)"
    - "배지가 tier별 정직한 카피 3종을 표시 (가짜 수치 금지)"
  artifacts:
    - path: "app/src/lib/alignmentWarp.ts"
      provides: "analysis.ts 계약 import로 전환된 워핑 수학 (단일 출처)"
      contains: "import type { MotionAlignment }"
    - path: "app/src/components/VideoCompare.tsx"
      provides: "alignment prop + setRightToStudentTime/setBothAbsoluteTime helper 격리 + 구간 경계 rate + tier 배지"
      contains: "setRightToStudentTime"
  key_links:
    - from: "app/src/components/VideoCompare.tsx"
      to: "app/src/lib/alignmentWarp.ts"
      via: "warpTime/segmentRate/normalizeMotionAlignment import"
      pattern: "from '../lib/alignmentWarp'"
    - from: "app/src/components/VideoCompare.tsx"
      to: "rightPlayer.playbackRate"
      via: "구간 경계에서만 설정 (매 tick 금지)"
      pattern: "playbackRate"
---

<objective>
VideoCompare 워핑 소비 (D-01) — 학생(left)=master 시계 불변, 정은지(right)만 warp. 기존 100ms tick drift 보정 인프라(DRIFT_CORRECT_THRESHOLD_S=0.2, Build 16 UAT 산물 — 재학습 금지)는 그대로 두고 **목표값만** `cR ≈ cL`에서 `cR ≈ warpTime(alignment, cL)`로 교체. rate는 feedforward(구간 경계에서만 변경, 클램프 0.5~2.0), tick 보정 seek은 feedback 안전망 (expo-video rate 지연 미문서화 — A2, 이중 제어로 rate가 늦어도 동기 유지). alignment 부재/disabled = 현행 코드 100% 보존 (optional prop — faultZoomStatus/tier legacy 폴백 선례).

**코드 형태 규율 (리뷰 MEDIUM-1):** 기존 계획의 grep 게이트는 `slowerTime` 라인을 legacy 예외로 허용해, togglePlay(:398-400)의 `rightPlayer.currentTime = slowerTime` 절대시간 대입이 정렬 활성 경로에 남아도 통과할 수 있었다. 대신 코드 형태 자체를 검증 가능하게 바꾼다: right 쓰기는 helper 2개(`setRightToStudentTime`/`setBothAbsoluteTime`) 내부에만 존재, 직접 대입 총 2곳 — grep으로 기계 판정.

시나리오 태그: 2단계+6단계 — power-spin "자동구간맞춤 싱크 안 맞음"(A3)의 해소 본체.

**선행 전제:** 28-03(analysis.ts MotionAlignment 계약) 실행 후. VideoCompare.tsx는 Phase 26/27이 건드리지 않음 (RESEARCH 겹침 매트릭스) — 26/27 선행 불요. JS-only (OTA 가능, 신규 native 모듈 0).

**채점 무접촉:** 앱 표시/재생만 — 점수/판정 재계산·재해석 금지 (deductionLabels 헤더 원칙과 동일).

Purpose: 두 영상이 동작 기준으로 함께 흐르는 비교 경험.
Output: 워핑 재생 + tier 배지, typecheck+grep 게이트 green.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-dtw-motion-based-alignment/28-CONTEXT.md
@.planning/phases/28-dtw-motion-based-alignment/28-RESEARCH.md
@.planning/phases/28-dtw-motion-based-alignment/28-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 계약 import 전환 + setRightToStudentTime helper 격리 배선</name>
  <files>app/src/lib/alignmentWarp.ts, app/src/components/VideoCompare.tsx</files>
  <read_first>
    - app/src/lib/alignmentWarp.ts (28-01 산출 — 로컬 타입을 import로 전환할 대상)
    - app/src/types/analysis.ts MotionAlignment (28-03 산출 계약)
    - app/src/components/VideoCompare.tsx:150-160 (상수 DRIFT_CORRECT_THRESHOLD_S 등), :191-203 (props 목록), :280-350 (tick — drift 보정 :303-325 + 종료 판정 :334-342), :363-420 (togglePlay — isAtEnd reset :388-389, needsStartSync slowerTime :396-400 **← MEDIUM-1 실체: rightPlayer.currentTime = slowerTime 절대 대입** + restart :416-417), :424-442 (seekBoth :435-436)
    - 28-RESEARCH.md Pattern 3 (최소 변경 워핑 설계 1~5)
  </read_first>
  <action>
    1. **alignmentWarp.ts**: 로컬 `MotionAlignment` 타입 정의 제거 → `import type { MotionAlignment } from '../types/analysis'` + `export type { MotionAlignment }` 재수출 (기존 소비처 호환). 함수 본체 무변경.
    2. **VideoCompare.tsx — 상태/헬퍼 3종 신설 (MEDIUM-1 코드 형태 규율)**: props에 `alignment?: MotionAlignment | null` 추가 (:191-203 형식, JSDoc "28-CONTEXT D-01/D-02 — 부재/null = 현행 절대시계 100% 보존").
       - `const alignmentActive = !!alignment && alignment.tier !== 'disabled';`
       - `const targetRefTime = (t: number): number => (alignmentActive ? warpTime(alignment!, t) : t);` — 순수 계산.
       - `const setRightToStudentTime = (tStudent: number) => { if (rightPlayer) rightPlayer.currentTime = targetRefTime(tStudent); };` — **right 쓰기의 유일한 warp 경유 지점**.
       - `const setBothAbsoluteTime = (t: number) => { if (leftPlayer) leftPlayer.currentTime = t; if (rightPlayer) rightPlayer.currentTime = t; };` — **legacy 절대 동기 전용, 호출은 반드시 `!alignmentActive` 가드 분기 안에서만** (주석: "리뷰 MEDIUM-1 — 절대시간 대입이 정렬 활성 경로에 새는 것 구조적 차단").
       **기존 쓰기 지점 교체 매핑 (직접 대입은 helper 2개 내부의 2곳만 남김):**
       - seekBoth(:435-436): left는 `leftPlayer.currentTime = safe` 유지(master), right는 `setRightToStudentTime(safe)` (비활성 시 targetRefTime이 identity라 legacy와 동일 동작 — 분기 불필요).
       - restart(:416-417) / togglePlay isAtEnd reset(:388-389): left=0 대입 유지, right는 `setRightToStudentTime(0)` (활성 시 r0 오프셋 반영이 옳은 동작).
       - togglePlay needsStartSync(:396-400): **분기 분리** — `alignmentActive`면 left 유지 + `setRightToStudentTime(leftCurrent)` (slowerTime min 로직은 워핑 하에서 무의미 — right 타임라인이 다름); `!alignmentActive`면 기존 의미 보존: `setBothAbsoluteTime(slowerTime)`.
       leftPlayer 쓰기는 무변경 (setBothAbsoluteTime 내부 이동분 제외).
    3. tick 보정 로직 (:303-325): alignment 활성(warped/trim_only) 시 — left=master 로 left back-seek 없음, `Math.abs(cR - targetRefTime(cL)) > DRIFT_CORRECT_THRESHOLD_S`일 때만 `setRightToStudentTime(cL)`. alignment 비활성(부재/null/disabled) 시 기존 상호 back-seek 코드 그대로 (조건 분기로 보존 — 삭제 금지; 기존 코드의 직접 대입은 helper 경유로만 치환).
    4. 종료 판정 (:334-342 shorter 로직): alignment 활성 시 비교 재생 정의역 = `min(dL, uN)` (uN = 마지막 앵커 학생초; warp(uN) 이후는 기울기 1.0 연장이므로 dR 초과 시 기존 shorter pause 로직이 잡음). 기존 로직과 동일 형태 유지, 주석으로 정의역 근거.
    (Task 2와 같은 파일 연속 작업 — 커밋은 Task별 분리: `feat(28-06): setRightToStudentTime helper 격리 워핑 배선`)
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck && grep -v "^\s*//" src/components/VideoCompare.tsx | grep -c "rightPlayer.currentTime =" | grep -qx 2 && grep -v "^\s*//" src/components/VideoCompare.tsx | grep "rightPlayer.currentTime" | grep -c "slowerTime" | grep -qx 0 && echo WARP_ROUTED</automated>
  </verify>
  <acceptance_criteria>
    - typecheck exit 0 + "WARP_ROUTED" 출력 — `rightPlayer.currentTime =` 직접 대입이 **정확히 2곳** (setRightToStudentTime 내부 1 + setBothAbsoluteTime 내부 1, 주석 제외), rightPlayer 쓰기 라인에 slowerTime 0 (MEDIUM-1 게이트 — legacy 예외 grep 폐기)
    - `grep -c "setRightToStudentTime" app/src/components/VideoCompare.tsx` >= 5 (정의 + tick/seekBoth/togglePlay/restart·isAtEnd 호출부)
    - `grep -c "setBothAbsoluteTime" app/src/components/VideoCompare.tsx` >= 2 (정의 + !alignmentActive needsStartSync 분기 호출)
    - `grep -c "import type { MotionAlignment } from '../types/analysis'" app/src/lib/alignmentWarp.ts` == 1
    - `grep -c "alignment?" app/src/components/VideoCompare.tsx` >= 1
  </acceptance_criteria>
  <done>right 쓰기가 helper 2개로 격리 — 정렬 활성 경로에 절대시간 대입이 구조적으로 불가능, legacy 경로 100% 보존</done>
</task>

<task type="auto">
  <name>Task 2: rate feedforward + tier 배지 카피</name>
  <files>app/src/components/VideoCompare.tsx</files>
  <read_first>
    - app/src/components/VideoCompare.tsx:211-220 (player 초기화 콜백 — muted=true라 preservesPitch 무관), :735-758 (배지 — TODO(deferred-backend) 주석이 이 작업을 예고)
    - app/src/lib/alignmentWarp.ts segmentRate (28-01 산출)
    - 28-RESEARCH.md Pattern 3 항목 3·6 (rate 정책) + Anti-Patterns ("매 tick rate 재설정 금지")
  </read_first>
  <action>
    1. **rate feedforward**: tick 내에서 `segmentRate(alignment, cL)` 계산하되 **`rightPlayer.playbackRate` 대입은 값이 직전 설정값과 다를 때만** (ref로 직전 rate 기억 — 구간 경계에서만 실변경, 초당 최대 2회, 매 tick 재설정 금지 = 플랫폼 재버퍼 위험 회피). alignment 비활성/pause/unmount 시 rate 1.0 복원. 주석: "rate=feedforward, tick seek=feedback (A2 — expo-video rate 지연 미문서화, seek 안전망이 0.2s 내 동기 보장)". `preservesPitch`는 muted=true(:212)라 무접촉.
    2. **배지 tier 카피** (:735-758 정적 배지 교체): alignment 존재 시 tier별 카피 —
       - warped: "동작 기준으로 자동 구간을 맞췄어요"
       - trim_only: "동작 차이가 있어 시작점만 맞췄어요 (배속 조정은 꺼짐)"
       - disabled: "기준 동작과 차이가 커 자동 정렬을 껐어요" (D-02 3단 — belle 지정 톤)
       alignment 부재(legacy): 기존 정적 배지 그대로 (D-05 배너는 28-07 result.tsx 책임 — 배지/배너 책임 분리). 수치 표기는 하지 않음 — DTW distance 원값은 사용자 의미 없음, 가짜/무의미 수치 금지 (Phase 20 A4 규율; distance는 계약에 보존, 표기는 후속 재량). 기존 스타일 토큰(alignBadge*) 재사용, 하드코딩 색/간격 0.
    3. Phase 20 TODO(deferred-backend) 주석을 "Phase 28 해소 — tier 실데이터 기반" 으로 갱신.
    커밋: `feat(28-06): rate feedforward + tier 배지`.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck && grep -c "playbackRate" src/components/VideoCompare.tsx && grep -c "자동 정렬을 껐어요" src/components/VideoCompare.tsx</automated>
  </verify>
  <acceptance_criteria>
    - typecheck exit 0
    - `grep -c "playbackRate" app/src/components/VideoCompare.tsx` >= 2 (설정 + 복원)
    - tier 카피 3종 각 grep >= 1
    - `grep -c "#[0-9a-fA-F]\{6\}" app/src/components/VideoCompare.tsx` 기존 대비 증가 0 (하드코딩 색 금지)
  </acceptance_criteria>
  <done>rate가 구간 경계에서만 바뀌고, 배지가 tier를 정직하게 말함</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firestore doc → 앱 재생 제어 | malformed alignment가 재생 제어(rate/seek)를 오작동시킬 수 있음 |
| 워핑 표시 → 사용자 판단 | 오적용 워핑 = 잘못된 비교 노출 (오도) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-02 | Tampering/DoS (크래시) | alignment prop 소비 | mitigate | normalizeMotionAlignment 통과분만 소비 (28-07에서 result→prop 경로에 적용) + tier/anchors 방어 로직은 순수 함수에 격리 |
| T-28-15 | Integrity (오도 워핑) | warp/rate 적용 | mitigate | tier 사다리 — 신뢰 낮으면 trim_only/disabled 강등 + 배지 정직 고지 (Task 2) |
| T-28-16 | DoS (seek stutter) / Integrity (unwarped seek) | 보정 seek 연발·절대시간 대입 잔존 | mitigate | helper 2개 격리 — 직접 대입 정확히 2곳 grep 게이트 + setBothAbsoluteTime은 !alignmentActive 전용 (MEDIUM-1) + threshold 0.2s 유지 |
| T-28-SC | Tampering | 패키지 설치 | accept | 신규 패키지 0 (expo-video 기존분) |
</threat_model>

<verification>
- typecheck green + WARP_ROUTED grep 게이트 (직접 대입 == 2, slowerTime→rightPlayer 0) + tier 카피 3종 존재
- legacy 분기(alignment 부재) 코드 경로 보존 (조건 분기 — 삭제 0)
</verification>

<success_criteria>
- 워핑/rate/seek이 계약 데이터로만 구동, 부재 시 현행 동작
- 정렬 활성 경로에서 절대시간 seek이 구조적으로 불가능 (helper 격리)
- 실기기 체감(A2 rate 반응성)은 28-08 end-of-phase 항목
</success_criteria>

<output>
Create `.planning/phases/28-dtw-motion-based-alignment/28-06-SUMMARY.md` when done
</output>
