---
phase: 28-dtw-motion-based-alignment
plan: 07
type: execute
wave: 5
depends_on: [28-05, 28-06]
files_modified:
  - app/src/app/analysis/result.tsx
autonomous: true
requirements: [ALGN-04, ALGN-05]
must_haves:
  truths:
    - "result 화면이 motionAlignment를 normalize 경유로 VideoCompare에 전달한다"
    - "legacy doc(motionAlignment 부재)에서만 비교 카드에 재분석 유도 배너 + CTA가 뜬다 — tier 'disabled'(신규 degenerate 포함)는 배너 아님 (D-05, W3)"
    - "refMatch='failed' 카드에 '자동 대응 실패' 캡션이 뜬다 (D-04 앱측)"
  artifacts:
    - path: "app/src/app/analysis/result.tsx"
      provides: "alignment prop 전달 + legacy 배너 + refMatch 캡션"
      contains: "motionAlignment"
  key_links:
    - from: "app/src/app/analysis/result.tsx"
      to: "app/src/components/VideoCompare.tsx"
      via: "alignment={normalizeMotionAlignment(result.motionAlignment)} prop"
      pattern: "alignment="
    - from: "app/src/app/analysis/result.tsx"
      to: "/(tabs)/analyze"
      via: "배너 CTA router.replace (기존 재분석 플로우 재사용)"
      pattern: "router.replace\\('/\\(tabs\\)/analyze'\\)"
---

<objective>
result 화면 소비 배선 — (a) `result.motionAlignment`를 `normalizeMotionAlignment`(방어적 — malformed → null = legacy 폴백, ASVS V5) 경유로 VideoCompare `alignment` prop에 전달, (b) D-05 legacy 배너: `motionAlignment === undefined`(optional 부재 = legacy — faultZoomStatus/tier 선례 규칙)이고 두 영상 비교 카드가 있는 문맥에서 "다시 분석하면 자동 구간 맞춤이 적용돼요" 배너 + "다시 분석하기" CTA(기존 `router.replace('/(tabs)/analyze')` 플로우 재사용 — 신규 플로우 0, Pod 비용은 사용자가 선택할 때만). **W3 이후 신규 분석은 build 불가(degenerate)여도 tier 'disabled'로 필드가 항상 실리므로(28-02/28-04) undefined 판정 = 순수 legacy — "재분석하면 적용" 과약속 루프 없음. tier 'disabled' doc은 배너 대상 아님 — 안내 배지는 VideoCompare(28-06) 책임.** (c) D-04 앱측: `FaultZoomComparison.refMatch === 'failed'` 카드에 "같은 동작 순간을 찾지 못해 전신 화면으로 보여드려요" 캡션.

시나리오 태그: 2단계+6단계 — 소비의 마지막 이음새.

**선행 전제 (필수):** result.tsx는 **26-02(wrapper/child 분리)와 27-07(zoom pending placeholder)이 먼저 수정**한다. 두 플랜 실행 완료 후 착수. 라인 번호 참조 금지 — 심볼 기준 재탐색 (`<VideoCompare`, `faultZoomComparisons`, `다시 분석하기`). Task 1 선행 확인 스텝이 미실행을 감지하면 중단·보고.

**채점 무접촉:** 표시만 — 점수/판정 재계산 0.

Purpose: 사용자가 정렬을 체감하고(신규), legacy 사용자는 업그레이드 경로를 안다(D-05).
Output: prop 배선 + 배너 + 캡션, typecheck green. JS-only (OTA 가능).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-dtw-motion-based-alignment/28-CONTEXT.md
@.planning/phases/28-dtw-motion-based-alignment/28-RESEARCH.md
@.planning/phases/28-dtw-motion-based-alignment/28-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: alignment prop 전달 + refMatch 캡션</name>
  <files>app/src/app/analysis/result.tsx</files>
  <read_first>
    - app/src/app/analysis/result.tsx — 심볼 기준: `<VideoCompare` 호출부(:1286 부근이었음 — 26-02/27-07 이후 이동 가능), fault_zoom 카드 렌더부(`faultZoomComparisons` 소비), 조건부 배지/1줄 카피 관례(:1104-1135 — 28-PATTERNS 발췌)
    - app/src/lib/alignmentWarp.ts normalizeMotionAlignment (28-01/28-06 산출)
    - app/src/types/analysis.ts FaultZoomComparison.refMatch (28-03 계약)
  </read_first>
  <action>
    **선행 확인 스텝:** 26-02/27-07 실행 흔적 심볼 확인 — `grep -c "faultZoomStatus" app/src/app/analysis/result.tsx` >= 1 (27-07 placeholder). 0이면 중단, "27-07 미실행 — Phase 26/27 앱 플랜 실행 후 재개" 보고 (파일 수정 0). (26-02 분리 여부는 grep으로 특정 어려우면 `<VideoCompare` 호출부가 존재하는 파일을 기준으로 작업 — wrapper 분리 시 자식 파일 쪽 수정.)

    1. `<VideoCompare` 호출부에 `alignment={normalizeMotionAlignment(result?.motionAlignment)}` 전달 — normalize는 렌더마다 재계산 비용이 미미하나 useMemo(result 의존)로 감싸기 (관례). 주석: "28-CONTEXT D-01 — malformed/legacy → null = 현행 절대시계 (ASVS V5 방어 소비)".
    2. fault_zoom 카드 렌더부: `item.refMatch === 'failed'`일 때 카드 하단 캡션 텍스트 "같은 동작 순간을 찾지 못해 전신 화면으로 보여드려요" (theme 토큰 스타일, 기존 카드 캡션/힌트 스타일 재사용 — 신규 하드코딩 0). 부재(legacy)/'dtw'면 캡션 없음.
    커밋: `feat(28-07): alignment prop + refMatch 캡션`.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck && grep -c "normalizeMotionAlignment" src/app/analysis/result.tsx && grep -c "refMatch" src/app/analysis/result.tsx</automated>
  </verify>
  <acceptance_criteria>
    - typecheck exit 0
    - `grep -c "alignment=" app/src/app/analysis/result.tsx` >= 1
    - `grep -c "refMatch" app/src/app/analysis/result.tsx` >= 1
  </acceptance_criteria>
  <done>신규 doc은 워핑이 흐르고, 대응 실패 카드는 정직 캡션</done>
</task>

<task type="auto">
  <name>Task 2: D-05 legacy 재분석 유도 배너</name>
  <files>app/src/app/analysis/result.tsx</files>
  <read_first>
    - app/src/app/analysis/result.tsx — 심볼 기준: 재분석 CTA(`다시 분석하기` + `router.replace('/(tabs)/analyze')`), 조건부 1줄 카피 관례 (28-PATTERNS "result.tsx [수정, D-05]" 절)
    - 28-RESEARCH.md Pattern 6 (legacy 판정 + 배지/배너 책임 분리)
  </read_first>
  <action>
    비교 카드(두 영상 보유 — VideoCompare가 렌더되는 조건과 동일 문맥) 직상/직하에 legacy 배너 렌더: 조건 = `result != null && result.motionAlignment === undefined` (undefined 판정 — null 정규화 실패와 구분: normalize null은 데이터 있으나 malformed → 배너 아님, 필드 자체 부재만 legacy). **tier 판정 금지 (W3):** `tier === 'disabled'`(신규 분석의 degenerate/저신뢰 — 28-02가 build 불가 케이스도 disabled로 방출)는 배너 조건에 포함하지 말 것 — 재분석해도 같은 결과일 수 있어 "재분석하면 적용"이 과약속이 됨. disabled 안내는 VideoCompare 배지(28-06) 책임 (Pattern 6 배지=VideoCompare/배너=화면 레벨 책임 분리). 내용 = 1줄 안내 "다시 분석하면 자동 구간 맞춤이 적용돼요" + 인라인 CTA Pressable "다시 분석하기" → `router.replace('/(tabs)/analyze')` (기존 CTA와 동일 라우팅 — 신규 플로우 금지). `accessibilityRole="button"` + `hitSlop={8}` 필수 (관례). 스타일 = theme 토큰만 (brandTint 계열 배지 선례), 라이트 전용, 이모지 0. 주석: "28-CONTEXT D-05 — 정렬 데이터는 새 분석부터, legacy는 재분석 유도. W3: 신규 분석은 degenerate라도 disabled로 필드가 실리므로 undefined=순수 legacy (배지=VideoCompare/배너=화면 레벨 책임 분리)".
    커밋: `feat(28-07): D-05 legacy 재분석 유도 배너`.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck && grep -c "자동 구간 맞춤이 적용" src/app/analysis/result.tsx</automated>
  </verify>
  <acceptance_criteria>
    - typecheck exit 0
    - 배너 카피 grep >= 1 + `grep -c "accessibilityRole" app/src/app/analysis/result.tsx` 기존 대비 +1
    - `grep -c "#[0-9a-fA-F]\{6\}" app/src/app/analysis/result.tsx` 기존 대비 증가 0 (하드코딩 색 금지)
    - 배너 조건식에 tier 참조 0 (`grep -n "disabled" src/app/analysis/result.tsx` 로 배너 블록 내 tier 판정 부재 확인 기록 — W3)
  </acceptance_criteria>
  <done>legacy doc 사용자만 재분석 경로를 배너로 안내받는다 — 신규 degenerate doc은 배지(28-06)로 안내</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firestore doc → result 화면 | 신뢰 불가 doc 데이터의 방어적 소비 지점 (ASVS V5) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-02 | Tampering/DoS (크래시) | result→prop 경로 | mitigate | normalizeMotionAlignment 방어 소비 — malformed → null 폴백 (Task 1) |
| T-28-17 | Spoofing (오도 안내) | legacy 배너 조건 | mitigate | undefined 판정만 legacy (malformed null·tier 'disabled'와 구분) + W3: 신규 degenerate는 disabled로 방출되므로 undefined=legacy 판정이 구조적으로 성립 — "재분석하면 적용" 과약속 루프 차단 |
| T-28-14 | Repudiation (실패 은폐) | refMatch 캡션 | mitigate | 'failed' 정직 고지 캡션 (Task 1) |
| T-28-SC | Tampering | 패키지 설치 | accept | 신규 패키지 0 |
</threat_model>

<verification>
- 선행 확인: 27-07 심볼 (미존재 시 중단)
- typecheck green + 카피/캡션 grep
- 배너 조건 = 필드 부재(undefined)만 — tier 판정 0 (W3)
</verification>

<success_criteria>
- 신규 doc = 워핑 + tier 배지 (disabled 포함 — 배지 책임) / legacy doc = 배너 + CTA / failed 카드 = 캡션
- 실기기 UI 확인은 28-08 end-of-phase 항목
</success_criteria>

<output>
Create `.planning/phases/28-dtw-motion-based-alignment/28-07-SUMMARY.md` when done
</output>
