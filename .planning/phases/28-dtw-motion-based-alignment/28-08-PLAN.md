---
phase: 28-dtw-motion-based-alignment
plan: 08
type: execute
wave: 5
depends_on: [28-04, 28-05, 28-06, 28-07]
files_modified: []
autonomous: false
requirements: [ALGN-01, ALGN-02, ALGN-03, ALGN-04, ALGN-05, ALGN-06]
must_haves:
  truths:
    - "affected 스코프 pytest + typecheck + grep 게이트 전부 green (신규 FAILED 0)"
    - "Pod 재분석은 belle 승인 checkpoint 이후에만 실행된다 (W2)"
    - "실분석 1회로 새 doc에 motionAlignment(초 단위·tier)가 실재함이 read-only Firestore doc 검사로 기계 판정된다 (B2)"
    - "OTA는 preview 채널 선발행 → belle 실기기 확인(approved) → production 발행 순서로만 나간다 (W2)"
  artifacts: []
  key_links:
    - from: "로컬 게이트"
      to: "Pod 실분석"
      via: "push → Pod git pull → 재분석 1회 (gsd-pod-work-push-first)"
      pattern: "motionAlignment"
---

<objective>
Phase 28 gate — 로컬 게이트 일괄 + belle 승인 후 실분석 1회 확인 + preview 선발행 OTA → belle 실기기 확인 → production 발행. Pod 상시 작업 불필요 (28-VALIDATION — 정렬은 로컬 재현, 실분석 확인은 재분석 1회 수준). Pod 운영은 Claude가 실행 (pod-ops-claude-runs), 로컬 commit 후 즉시 push (gsd-pod-work-push-first). **autonomous: false — Pod 재분석 착수(Task 2)와 production 발행 전 실기기 확인(Task 5)이 blocking checkpoint (W2).**

시나리오 태그: 2단계+6단계 — 파일럿 체감 확인.

**채점 무접촉 최종 확인:** 재분석 doc의 overallScore/deductionBreakdown이 정렬 방출과 무관함을 phase 전체 diff 관점에서 재확인 (28-04 단위 증명의 실데이터 관측).

**phase 시작 커밋 (W1):** 이 플랜의 diff 게이트는 단일 커밋(`HEAD~1`)이 아니라 **phase 전체 범위**를 본다. `PHASE_START` = 28-01 SUMMARY에 박제된 첫 커밋 해시 — 없으면 `git log --reverse --format=%H --grep='(28-01)' | head -1` 로 확보. 이하 모든 diff 게이트는 `"$PHASE_START"..HEAD` 범위.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-dtw-motion-based-alignment/28-VALIDATION.md
@.planning/phases/28-dtw-motion-based-alignment/28-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 로컬 게이트 일괄 — affected pytest + typecheck + grep 게이트</name>
  <files>(수정 없음 — 게이트 실행만)</files>
  <read_first>
    - 28-VALIDATION.md Sampling Rate + Per-Task Verification Map (게이트 목록 정본)
    - .planning/phases/28-dtw-motion-based-alignment/28-01-SUMMARY.md (phase 첫 커밋 해시 — W1 diff 범위 기점)
  </read_first>
  <action>
    다음을 순서대로 실행하고 결과를 SUMMARY에 표로 박제 (실패 시 해당 플랜 결함으로 보고 — 이 플랜에서 임의 수정 금지, fix-now 원칙에 따라 결함 플랜 단위로 수정 후 재실행):
    1. `cd backend && PYTHONPATH=shared/python:. python3 -m pytest tests/ -q -k "alignment or fault_zoom or pipeline"` — 신규 FAILED/ERROR 0 (pre-existing ~54건은 full-suite 스코프 — affected 스코프 비교 판정, STATE.md 박제 기준).
    2. `cd app && npm run typecheck` — exit 0.
    3. grep 게이트 재확인: (a) ratio 근사 부재 (28-05 게이트 명령 재실행), (b) `rightPlayer.currentTime =` warp 경유 (28-06 WARP_ROUTED 명령 재실행), (c) 3-way lockstep `grep -c motionAlignment app/src/types/analysis.ts backend/shared/python/sunity_shared/models.py docs/contract.md` 각 >= 1, (d) 임계 출처 `grep -c "_ALIGN_GLOBAL" backend/shared/python/sunity_shared/analysis/motion_alignment.py` >= 1.
    4. 채점 무접촉 diff (W1 — phase 전체 범위): `PHASE_START` 확보(objective의 확보 로직 — 28-01 SUMMARY 해시 우선, 폴백 `git log --reverse --format=%H --grep='(28-01)' | head -1`) 후 `git diff --name-only "$PHASE_START"..HEAD` 에 vision_veto.py/kismam.py/dimensions.py/deduction_engine 계열/motiondtw.py 부재 확인 (fault_zoom.py는 표시 경로만 — `_matched_ref_frame` 본체 docstring-only는 28-05 SUMMARY 증빙 인용). 사용한 PHASE_START 해시를 SUMMARY에 박제 (Task 4의 JS-only 게이트가 동일 기점 재사용).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && PYTHONPATH=shared/python:. python3 -m pytest tests/ -q -k "alignment or fault_zoom or pipeline" 2>&1 | tail -3 && cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - affected 스코프 신규 FAILED/ERROR 0 + typecheck exit 0
    - grep 게이트 4종 전부 통과 (SUMMARY 표)
    - 채점 무접촉 diff가 phase 전체 범위(PHASE_START..HEAD)로 판정됨 + PHASE_START 해시 SUMMARY 박제 (W1)
  </acceptance_criteria>
  <done>phase 전체가 로컬 게이트 green</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: belle 승인 — Pod 재분석 착수 (W2)</name>
  <action>
    Pod SSH 재분석(production 데이터 1건)을 시작하기 전에 belle 확인/승인이 필요합니다:
    1. RunPod Pod svn31pzja7uay0 상태 확인 — 실행 중인지, 크레딧 잔량 (메모리 [[current-pod-hbpvhedq2bu01i]])
    2. Gemini 크레딧 잔량 확인 — 재분석 1건에 Gemini 호출 포함 (메모리 [[gemini-credits-depleted-2026-06-20]] 이력)
    3. production Firestore에 재분석 doc 1건 추가 실행 승인 (power-spin 학생 fixture, 1건·순차)
  </action>
  <context>
    Task 1 로컬 게이트가 전부 green인 상태에서만 이 checkpoint에 도달. Pod SSH·실데이터 접근은 belle 승인 없이 autonomous로 진행하지 않음 (checker W2).
  </context>
  <resume-signal>belle "승인" 회신 후 Task 3 착수. 미승인/보류 시 로컬 게이트 결과까지만 SUMMARY에 박제하고 orchestrator에 부분 완료 보고.</resume-signal>
  <done>belle 승인/보류 기록 확보 — 승인 시에만 Task 3 진행</done>
</task>

<task type="auto">
  <name>Task 3: push + Pod 동기화 + 재분석 1회 — motionAlignment 실데이터 확인</name>
  <files>(수정 없음 — 운영 확인)</files>
  <read_first>
    - 메모리 [[current-pod-hbpvhedq2bu01i]] (Pod svn31pzja7uay0, SSH root@213.173.102.233:12729, start_server.sh 함정), [[gsd-pod-work-push-first]], [[pipeline-not-concurrency-safe-eval-serial]] (재분석은 1건 순차)
  </read_first>
  <action>
    1. 로컬 커밋 push (orchestrator가 커밋한 phase 산출 포함 상태 확인 후).
    2. Pod SSH → `git pull` → 서버 재시작 (start_server.sh 관례 — VETO env 박제 유지) → `/health` ok.
    3. 앱 없이도 가능한 경로로 mode1 재분석 1건 트리거 (power-spin 학생 fixture — 기존 sweep/재분석 스크립트 선례 재사용, **1건만·순차**). 완료 후 재분석 doc의 uid/analysisId를 확보해 verify 스니펫의 `ALIGN_CHECK_UID`/`ALIGN_CHECK_ANALYSIS_ID` env로 전달 (B2 — 검사는 read-only, 28-01 fps 실측 스크립트와 동일한 로컬 SA firebase-admin 관례).
    4. `overallScore`가 직전 동일 영상 분석과 동일(±0 — 채점 무접촉 실데이터 관측; Gemini 비결정 요소로 차이 나면 breakdown 구조 비교로 판정하고 원인 기록).
    5. 결과(필드 값 발췌 + DOC_CHECK_OK 출력)를 SUMMARY에 박제.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && FIREBASE_SA_PATH=../firebase-sa.json ALIGN_CHECK_UID=<재분석 uid> ALIGN_CHECK_ANALYSIS_ID=<재분석 analysisId> python3 - <<'PY'
import os
import firebase_admin
from firebase_admin import credentials, firestore
firebase_admin.initialize_app(credentials.Certificate(os.environ["FIREBASE_SA_PATH"]))
db = firestore.client()
snap = db.collection("users").document(os.environ["ALIGN_CHECK_UID"]).collection("analyses").document(os.environ["ALIGN_CHECK_ANALYSIS_ID"]).get()
doc = snap.to_dict() or {}
r = doc.get("result") or {}
ma = r.get("motionAlignment")
assert isinstance(ma, dict), "motionAlignment 부재"
assert ma.get("tier") in ("warped", "trim_only", "disabled"), "tier 미등재: %r" % ma.get("tier")
a = ma.get("anchors")
assert isinstance(a, list) and len(a) % 2 == 0, "anchors 짝수 flat 위반"
assert ma.get("anchorCount") * 2 == len(a), "anchorCount 불일치"
us, rs = a[0::2], a[1::2]
assert all(us[i] < us[i + 1] for i in range(len(us) - 1)), "u 비단조"
assert all(rs[i] <= rs[i + 1] for i in range(len(rs) - 1)), "r 비단조"
max_sec = float(os.environ.get("ALIGN_CHECK_MAX_SEC", "300"))
assert all(0.0 <= float(v) <= max_sec for v in a), "초 단위 범위 위반 (인덱스 방출 의심)"
fz = r.get("faultZoomComparisons") or []
assert any("refMatch" in c for c in fz), "refMatch 부재"
print("DOC_CHECK_OK")
PY</automated>
  </verify>
  <acceptance_criteria>
    - verify 스니펫이 DOC_CHECK_OK 출력 (assert 실패 = exit 1) — motionAlignment 존재 / tier 3종 / anchors 초 단위·짝수·단조 / anchorCount*2==len(anchors) / refMatch 존재 (B2)
    - env 플레이스홀더(<재분석 uid>/<재분석 analysisId>)는 Task 3 항목 3에서 확보한 실제 값으로 치환해 실행 (read-only — write 호출 0)
    - 점수 필드 무이동 관측 기록 (또는 비결정 요소 원인 기록)
  </acceptance_criteria>
  <done>실분석 doc에서 phase 산출물이 end-to-end로 실재 — 기계 판정</done>
</task>

<task type="auto">
  <name>Task 4: JS-only 범위 확인 + preview 채널 OTA 선발행 (W1/W2)</name>
  <files>(수정 없음 — 배포)</files>
  <read_first>
    - 메모리 [[eas-build-gotchas]] + quick 260705-fx4 SUMMARY (OTA 발행 선례 — production+preview 채널, runtime 1.0.0)
  </read_first>
  <action>
    1. **JS-only 게이트 (W1 — phase 전체 범위):** Task 1에서 박제한 `PHASE_START` 재사용 — `git diff --name-only "$PHASE_START"..HEAD | grep -E '^app/(package\.json|package-lock\.json|app\.json|ios/|android/)'` 결과 0줄 확인 (신규 native 모듈 0 — expo-video 기존분. 단일 커밋 HEAD~1 검사 금지 — phase가 다수 커밋이므로 범위 diff만 유효).
    2. `eas update` 로 **preview 채널만** 선발행 (fx4 선례 명령 — production 채널 발행 금지, Task 5 approved 이후). belle 기기(TestFlight #27)가 preview 채널을 수신하는지 빌드 채널을 확인하고, preview 미수신 빌드라면 Task 5 checkpoint에서 belle에게 수신 가능 채널을 보고해 확인 방식을 조율 (production 직행은 어떤 경우에도 금지 — W2).
    3. 발행 로그(channel/runtime/update id)를 SUMMARY에 박제.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && PHASE_START=$(git log --reverse --format=%H --grep='(28-01)' | head -1) && test -n "$PHASE_START" && git diff --name-only "$PHASE_START"..HEAD | grep -cE '^app/(package\.json|package-lock\.json|app\.json|ios/|android/)' | grep -qx 0 && echo JS_ONLY && cd app && eas update:list --branch preview --limit 1 --non-interactive 2>&1 | head -8</automated>
  </verify>
  <acceptance_criteria>
    - JS_ONLY 출력 (phase 범위 diff에 package.json/package-lock.json/app.json/ios/android 0 — W1)
    - preview 채널 최신 update가 방금 발행분 (update:list 출력 SUMMARY 박제)
    - production 채널 발행 이력 없음 (Task 6 전까지)
  </acceptance_criteria>
  <done>belle 확인용 preview 번들이 나가 있고, production은 아직 미발행</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: belle 실기기 확인 — preview 채널 (W2)</name>
  <action>preview 채널 OTA 수신 상태에서 belle에게 아래 how-to-verify 5항목을 전달하고 확인 결과를 수집한다 — approved 전에는 production 발행 금지 (W2).</action>
  <what-built>Phase 28 전체 — DTW 동작 기반 비교 정렬 (워핑 재생 + tier 배지 + legacy 배너 + refMatch 캡션), preview 채널 OTA로 발행됨. 백엔드는 Pod에 배포·재분석 1건 검증 완료.</what-built>
  <how-to-verify>
    belle 실기기 (TestFlight, preview 채널 수신 빌드, 재분석된 power-spin doc):
    1. 비교 재생 — 정은지가 학생 동작을 중반 템포까지 따라옴 (D-01 체감), rate 구간 경계 stutter 없음 (A2)
    2. 스크럽/재시작 직후 동기 유지 (보정 seek 연발 없음 — Pitfall 7)
    3. 배지 tier 카피 표시 (warped/trim_only/disabled 중 해당)
    4. legacy doc(기존 분석) → "다시 분석하면 자동 구간 맞춤이 적용돼요" 배너 + CTA 라우팅 (D-05)
    5. 확대비교 카드 — 정은지 측이 같은 동작 순간 (D2 종결), 실패 카드는 "전신 화면" 캡션 (D-04)
  </how-to-verify>
  <resume-signal>"approved" 회신 시에만 Task 6(production 발행) 진행. 이슈 서술 시 결함 플랜 단위 수정 후 Task 4부터 재실행 (production 미발행 유지).</resume-signal>
  <done>belle approved 기록 — production 발행 조건 충족</done>
</task>

<task type="auto">
  <name>Task 6: production 채널 OTA 발행 + SUMMARY 취합</name>
  <files>(수정 없음 — 배포/기록)</files>
  <action>
    1. Task 5 approved 확인 후에만 `eas update` production 채널 발행 (fx4 선례 — preview와 동일 커밋 기준).
    2. SUMMARY에 취합: 게이트 표(Task 1) + doc 발췌·DOC_CHECK_OK(Task 3) + preview/production OTA 로그(Task 4/6) + belle 확인 결과(Task 5 5항목 체크리스트).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && eas update:list --branch production --limit 1 --non-interactive 2>&1 | head -8</automated>
  </verify>
  <acceptance_criteria>
    - production 채널 최신 update가 preview에서 belle이 approved한 것과 동일 커밋 기준
    - SUMMARY에 게이트 표 + doc 발췌 + OTA 로그(양 채널) + belle 체크리스트 전부 존재
  </acceptance_criteria>
  <done>belle 확인을 거친 번들만 production에 나감 — phase gate 종결</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로컬 → Pod 배포 | push 기반 동기화 — Pod env/서버 재시작 함정 (start_server.sh) |
| OTA → 사용자 기기 | 미검증 번들 배포 위험 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-18 | DoS (배포 사고) | Pod 재시작 | mitigate | belle 승인 checkpoint(Task 2) + /health 확인 + start_server.sh 박제 절차 준수 (Task 3) |
| T-28-19 | Integrity (미검증 OTA) | eas update | mitigate | 게이트 green 후 preview 선발행 → belle 실기기 approved 후에만 production (Task 4→5→6, W2) + JS-only phase 범위 diff (W1) |
| T-28-11 | Tampering (채점 이동) | phase 전체 diff | mitigate | 채점 코어 파일 diff 0 (PHASE_START..HEAD 범위 — W1) + 실데이터 점수 무이동 관측 (Task 1·3) |
| T-28-SC | Tampering | 패키지 설치 | accept | 신규 패키지 0 |
</threat_model>

<verification>
- affected pytest + typecheck + grep 게이트 전부 green
- belle 승인 후 실분석 doc read-only 기계 검사 DOC_CHECK_OK (B2)
- preview 선발행 → belle approved → production 발행 순서 준수 (W2)
</verification>

<success_criteria>
- 파일럿 신고 2건(A3 싱크, D2 크롭)의 해소가 실기기에서 확인 가능한 상태
- 채점 무접촉이 단위+실데이터 양쪽으로 증빙 (diff 범위 = phase 전체)
- Pod 재분석·production 배포가 belle 승인/확인 게이트를 통과한 기록 존재
</success_criteria>

<output>
Create `.planning/phases/28-dtw-motion-based-alignment/28-08-SUMMARY.md` when done (게이트 표 + doc 발췌 + OTA 로그 + belle 체크리스트)
</output>
