---
phase: 29-mode3-result-screen-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/InjuryRiskSection.tsx
  - .planning/phases/10-injury-risk-flags/10-UI-SPEC.md
autonomous: true
requirements: [D-14]

must_haves:
  truths:
    - "부상 위험 카드가 제목·이유 아래 '이렇게 해보세요' 권고 행을 flagType 4종(asymmetry/trunk_hyperextension/joint_hyperextension/level_mismatch) 모두에서 표시한다 (D-14)"
    - "카드·캡션 어디에도 부상을 확정하는 단정 표현이 없고, '강사와 점검' 톤 캡션이 존재한다 (시나리오 불변원칙)"
    - "legacy doc(기존 분석 결과)에서도 권고가 보인다 — 클라이언트 카피맵 확장이므로 계약·백엔드 변경 0, OTA-safe"
  artifacts:
    - path: "app/src/components/InjuryRiskSection.tsx"
      provides: "FLAG_COPY {title, why, recommendation} 4종 + 카드 recommendation 행 + 점검 캡션"
      contains: "recommendation"
    - path: ".planning/phases/10-injury-risk-flags/10-UI-SPEC.md"
      provides: "Copywriting Contract 동기화 (recommendation 행 추가분)"
  key_links:
    - from: "app/src/components/InjuryRiskSection.tsx FLAG_COPY"
      to: "InjuryRiskFlagCard 렌더"
      via: "copy.recommendation"
      pattern: "copy\\.recommendation"
---

<objective>
D-14 부상 대응법 노출 — SafetyFlag 카드에 "이렇게 해보세요" 권고 행을 추가한다.

**전제 보정 (RESEARCH Pitfall 2, 필수 준수):** `SafetyFlag.recommendation` 필드는 백엔드 dataclass(safety_flags.py:86-92)·TS interface(analysis.ts:1138-1146)·contract §9.13 어디에도 존재하지 않는다. SCENARIO.md 단계 9의 "데이터가 있는데 앱이 안 그림"은 서술 오류다. 올바른 구현 = 클라이언트 카피맵 `FLAG_COPY {title, why}` → `{title, why, recommendation}` 확장. 백엔드 필드 신설 금지 (3-way lockstep 비용 + 신규 분석에만 적용되는 열등안).

Purpose: 시나리오 9단계(부상 대응법) 파일럿 gap #1 해소. 즉시 가능한 첫 plan (ROADMAP 명시).
Output: InjuryRiskSection.tsx 권고 행 + 10-UI-SPEC Copywriting Contract 동기화. OTA-safe.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-mode3-result-screen-completion/29-CONTEXT.md
@.planning/phases/29-mode3-result-screen-completion/29-PATTERNS.md
@app/src/components/InjuryRiskSection.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: FLAG_COPY 4종 recommendation 확장 + 카드 권고 행 + 점검 캡션</name>
  <files>app/src/components/InjuryRiskSection.tsx</files>
  <read_first>
    - app/src/components/InjuryRiskSection.tsx (전체 — FLAG_COPY :29-46, InjuryRiskFlagCard :51-67, EXPERT_REFERRAL :25, 헤더 규칙 :1-14)
    - .planning/phases/29-mode3-result-screen-completion/29-PATTERNS.md (§InjuryRiskSection — 확장 대상 원문과 규칙)
    - app/src/theme/colors.ts (warnAmber 토큰 확인)
  </read_first>
  <action>
    D-14 구현. FLAG_COPY 타입을 `Record<SafetyFlagType, { title: string; why: string; recommendation: string }>`로 확장하고 flagType 4종(asymmetry, trunk_hyperextension, joint_hyperextension, level_mismatch) 전부에 recommendation 카피를 채운다. InjuryRiskFlagCard 에서 기존 why 행(cardWhy) 아래에 "이렇게 해보세요" 도입구 + recommendation 행을 추가하고, 카드 하단 또는 섹션 캡션으로 "정확한 진단은 강사님과 점검하세요" 톤 문구를 배치한다.

    카피 규칙 (D-14 + 컴포넌트 헤더 규칙 승계):
    - 사용자 카피는 "~해요" 체, 행동 지시형 (구체적 완화 행동: 보강 스트레칭, 그립 재점검, 강사 확인 등 — flagType 별 결이 다르게)
    - 부상 "확정" 단정 표현 금지 (부상 경고 = "강사와 점검", 위험 확정 아님 — 시나리오 불변원칙)
    - 금지어 "각도" 사용 금지 (29-CONTEXT D-05 근거 준용)
    - amber 시맨틱만 (colors.warnAmber / warnAmberBg) — 브랜드 레드 #FF4B33 금지
    - 기존 EXPERT_REFERRAL 푸터(:25 '정확한 판단은 강사 또는 전문가와 함께 확인해 주세요.')와 문구 중복 시 조정은 재량 (둘 중 하나를 흡수·통합 가능, 단 "강사와 점검" 톤은 최소 1곳 유지)
    - accessibilityLabel 에 recommendation 포함 갱신
    - 스타일은 StyleSheet.create 파일 하단, theme 토큰만 (하드코딩 색/spacing 금지)
    - 신규 코드 주석에 스펙 인용: `// 29-CONTEXT D-14 — ...`
    - 백엔드/계약 파일(models.py, analysis.ts SafetyFlag, contract.md §9.13)은 절대 건드리지 않는다 (Pitfall 2)
  </action>
  <verify>
    <automated>cd app && npm run typecheck && grep -c "recommendation" src/components/InjuryRiskSection.tsx && ! grep -n "각도" src/components/InjuryRiskSection.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "recommendation:" app/src/components/InjuryRiskSection.tsx` ≥ 4 (flagType 4종 전부 카피 보유)
    - `grep -n "각도" app/src/components/InjuryRiskSection.tsx` 매치 0
    - `grep -n "FF4B33\|colors.brand" app/src/components/InjuryRiskSection.tsx` 신규 추가분 0 (amber 시맨틱 유지)
    - `cd app && npm run typecheck` exit 0
    - git diff 에 backend/, app/src/types/analysis.ts, docs/contract.md 변경 0 (앱 컴포넌트 단독 변경)
    - 카드 렌더 경로에 copy.recommendation 소비 존재 (`grep -n "copy.recommendation" src/components/InjuryRiskSection.tsx` ≥ 1)
  </acceptance_criteria>
  <done>flagType 4종 카드 전부에서 제목→이유→권고("이렇게 해보세요") 순으로 렌더되고, "강사와 점검" 톤 캡션이 존재하며, typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: 10-UI-SPEC Copywriting Contract 동기화</name>
  <files>.planning/phases/10-injury-risk-flags/10-UI-SPEC.md</files>
  <read_first>
    - .planning/phases/10-injury-risk-flags/10-UI-SPEC.md (Copywriting Contract 섹션 — 기존 {title, why} 계약 서술)
    - app/src/components/InjuryRiskSection.tsx (Task 1 완료본 — 실제 채택 카피)
  </read_first>
  <action>
    InjuryRiskSection.tsx 헤더 주석이 명시한 규칙("변경 시 10-UI-SPEC Copywriting Contract 동시 갱신")을 이행한다. 10-UI-SPEC.md 의 Copywriting Contract 에 recommendation 행 4종의 최종 카피를 그대로 반영하고, 카드 구조 서술을 {title, why} → {title, why, recommendation} + 점검 캡션으로 갱신한다. 갱신 근거로 `29-CONTEXT D-14` 를 인용한다.
  </action>
  <verify>
    <automated>grep -c "recommendation" .planning/phases/10-injury-risk-flags/10-UI-SPEC.md</automated>
  </verify>
  <acceptance_criteria>
    - 10-UI-SPEC.md Copywriting Contract 에 recommendation 4종 카피가 컴포넌트 실카피와 문자열 일치
    - "D-14" 인용 존재 (`grep -c "D-14" .planning/phases/10-injury-risk-flags/10-UI-SPEC.md` ≥ 1)
  </acceptance_criteria>
  <done>스펙 문서와 컴포넌트 카피가 일치 — Copywriting Contract 규칙 위반 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firestore doc → 앱 렌더 | safetyFlags 배열이 클라이언트에 도달 — 이미 방어적 정규화 존재 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01-01 | Tampering | InjuryRiskSection 미지 flagType | mitigate | 기존 `if (!copy) return null` graceful skip 유지 — 계약 외 값 방어 (변경 금지) |
| T-29-01-02 | Information Disclosure | 부상 카피 (의료 단정) | mitigate | 부상 확정 단정 금지 카피 규칙 + "강사와 점검" 캡션 — 잘못된 의료 정보 전달 차단 |
| T-29-01-SC | Tampering | 패키지 설치 | accept | 이 plan 은 패키지 설치 0 — 공급망 표면 없음 |
</threat_model>

<verification>
- `cd app && npm run typecheck` exit 0
- 금지어 grep: "각도" 0건, 부상 확정 단정 문구 0건 (수동 카피 검토)
- 실기기 시각 확인은 29-08 HUMAN-UAT.md 적립 항목 (즉시 belle 호출 금지 — batch UAT 원칙)
</verification>

<success_criteria>
- D-14 완료: 카드 내 권고 행 + 점검 캡션, flagType 4종 전부, legacy doc 자동 커버 (클라이언트 카피맵), 계약 변경 0
- 10-UI-SPEC Copywriting Contract 동기화 완료
</success_criteria>

<output>
Create `.planning/phases/29-mode3-result-screen-completion/29-01-SUMMARY.md` when done
</output>
