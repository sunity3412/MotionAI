---
phase: 29-mode3-result-screen-completion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_mode3_tally_seam.py
  - backend/functions/pipeline/app.py
  - docs/contract.md
autonomous: true
requirements: [D-01, D-02, D-03]

must_haves:
  truths:
    - "mode3 등록 동작에서 md(ipsf_absolute measured seed)가 비어있지 않으면 deduction_engine.tally 가 실행되고 deductionBreakdown 이 방출된다 — Gemini 추가 호출 0 (D-01, mode3_held 불변)"
    - "mode3 breakdown 방출 doc 의 overallScore == deductionBreakdown.final — 100−Σ감점 항등식 유지 (D-02, 투명 감점 invariant)"
    - "md 빈 dict(미등록 동작 + 빈 criteria 4동작)는 breakdown 미방출 + overallScore byte-불변 — 기준 없는 감점 0=100 위양성 차단 (D-03)"
    - "mode1 채점·vision veto 경로 회귀 0 — TALLY-ELIGIBLE 3-status 로직 불변, 기존 seam 테스트 전부 green"
    - "visionVeto.status 는 'mode3_held' 유지 — 비전 미실행의 진실 신호 보존 (RESEARCH Pattern 1 옵션 a)"
  artifacts:
    - path: "backend/tests/test_mode3_tally_seam.py"
      provides: "D-01/D-02/D-03 매트릭스 unit 테스트 (mock-based, 실 Gemini/S3 0)"
      min_lines: 80
    - path: "backend/functions/pipeline/app.py"
      provides: "_apply_vision_veto_from_context 의 mode3_held tally 분기"
      contains: "mode3_held"
    - path: "docs/contract.md"
      provides: "§10 mode3 deductionBreakdown 방출 조건 서술 (신규 필드 0)"
  key_links:
    - from: "backend/functions/pipeline/app.py _apply_vision_veto_from_context"
      to: "deduction_engine.tally"
      via: "mode3_held + measured_deviations 분기"
      pattern: "deduction_engine\\.tally"
    - from: "backend/functions/pipeline/app.py"
      to: "complete_analysis deductionBreakdown"
      via: "breakdown.to_dict() 방출 (Firestore flat dict list — 기존 준수)"
      pattern: "deductionBreakdown"
---

<objective>
D-01/D-02/D-03 — Mode3 점수 내역의 백엔드 절개. `_apply_vision_veto_from_context` 의 mode3_held passthrough 를 "md 보유 시 tally-eligible" 로 확장한다 (24-04 low_alignment 선례 이식).

**전제 보정 (RESEARCH Pitfall 1, 필수 준수):** 등록 5동작 중 objective EXTEND criteria 를 가진 것은 power-spin 하나뿐. kip-up/peter-pan/elbow-twist-sister/pdshape 의 criteria yaml 은 2026-06-27 Pod 진단으로 비어 있다 (정타 form 이 무릎을 굽힘 — 신전기준 강요 금지). 이 4동작은 md 빈 dict → 미방출 + 점수 불변이 정상 동작이다. criteria 복원·신설 절대 금지 (deferred 트랙 + calibration-source-hard-gate).

**D-02 전환 조건:** overallScore = breakdown.final 전환 코드는 이 plan 에서 구현하되, production 노출(Pod 서버 재기동)은 29-05 sweep 게이트 PASS 후에만 일어난다 — 전환 조건(정은지 페어셋 mode3 sweep 통과)이 wave 구조로 강제된다.

Purpose: 시나리오 3단계(점수 근거) Mode3 gap #4 의 백엔드 절반. 비용·시간 증가 0 (Gemini 무호출).
Output: mode3 tally seam + Wave 0 테스트 + contract §10 서술 갱신.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-mode3-result-screen-completion/29-CONTEXT.md
@.planning/phases/29-mode3-result-screen-completion/29-RESEARCH.md
@.planning/phases/29-mode3-result-screen-completion/29-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — test_mode3_tally_seam.py (RED 먼저)</name>
  <files>backend/tests/test_mode3_tally_seam.py</files>
  <read_first>
    - backend/tests/test_pipeline_deduction_seam.py (Phase 24 seam 테스트 정본 — path 주입 :17-38, _profile/_ctx 헬퍼 :44-73, 헤더 docstring 관례 :1-15)
    - backend/tests/test_pipeline_mode3.py (prev doc 헬퍼 _as_prev — Firestore flat angles mirror)
    - backend/functions/pipeline/app.py (:2533-2631 _apply_vision_veto_from_context 현행 — passthrough_map 과 tally 경로, :2560-2583)
    - backend/shared/python/sunity_shared/analysis/deduction_engine.py (:166-201 tally 시그니처 + fallback 게이트 :196-201)
  </read_first>
  <action>
    D-01/D-02/D-03 검증 매트릭스를 mock-based unit 테스트로 작성한다 (29-VALIDATION Wave 0 요건). test_pipeline_deduction_seam.py 의 path 주입·_profile·_ctx 헬퍼 패턴을 승계하고, 헤더 docstring 에 "전부 mock-based — 실 Gemini/실 S3 0" + "수치 타깃 아님 — 방향/구조 단언만 (curve-fit 금지)" 문구를 승계한다.

    테스트 케이스 (모두 `_apply_vision_veto_from_context` 직접 호출, `_ctx("mode3_held")` 사용):
    1. **D-01 방출:** mode3_held + measured_deviations 비어있지 않음 (예: leg_extension seed) → 반환 dict 에 deductionBreakdown 존재, records 는 measured seed 파생, visionVeto.status == "mode3_held" 유지 ('applied' 아님)
    2. **D-02 항등:** 케이스 1 의 overallScore == deductionBreakdown["final"] AND final == 100 − Σ(records points) (엔진 to_dict 산술 그대로 — 재계산 조작 금지 확인)
    3. **D-03 미방출:** mode3_held + measured_deviations == {} → 반환 dict 에 deductionBreakdown 키 부재 + overallScore 는 입력 score_result["overallScore"] 와 동일 (byte-불변) + status "mode3_held"
    4. **clean tally:** mode3_held + md 보유 + 편차가 tolerance 이내(records 0) → breakdown 방출 + final == 100 (감점 0 = 100, 260630-l4e mode1 선례와 동일 원리)
    5. **Gemini 무호출:** mode3 경로에서 vision 판정 함수(assess/collect 계열)가 호출되지 않음 — mock/monkeypatch 로 호출 시 raise 하는 sentinel 설치 후 통과 확인
    6. **mode1 무회귀:** candidate_verdict / no_fault / low_alignment_confidence status 의 기존 동작이 변하지 않음 (기존 test_pipeline_deduction_seam.py 가 이미 커버 — 여기서는 passthrough_map 의 나머지 status(resource_limited 등)가 여전히 미방출인 것만 1케이스 추가)

    이 시점에서 케이스 1/2/4 는 FAIL(RED — seam 미구현), 3/5/6 은 PASS 가 정상. RED 상태를 커밋 메시지에 명시: `test(29-02): add failing mode3 tally seam tests`.
  </action>
  <verify>
    <automated>cd backend && PYTHONPATH=shared/python:. python3 -m pytest tests/test_mode3_tally_seam.py -q; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `backend/tests/test_mode3_tally_seam.py` 존재, 케이스 1~6 전부 구현 (≥ 6 test functions)
    - Task 2 이전 실행 시 케이스 1/2/4 FAIL (RED 확인) — pytest 종료코드 1
    - 파일 내 실 네트워크/S3/Gemini 클라이언트 import 0 (`grep -n "boto3\|genai\|google" backend/tests/test_mode3_tally_seam.py` 매치 0)
    - 점수 리터럴 하드코딩 단언 없음 — final==100−Σ 같은 산술 관계 단언만 (100 과 0 은 항등식 상수로 허용)
  </acceptance_criteria>
  <done>D-01/D-02/D-03 매트릭스 테스트가 존재하고 seam 미구현 상태에서 RED.</done>
</task>

<task type="auto">
  <name>Task 2: mode3_held tally 절개 구현 + contract §10 서술</name>
  <files>backend/functions/pipeline/app.py, docs/contract.md</files>
  <read_first>
    - backend/functions/pipeline/app.py (:2533-2631 from_context 전체 — passthrough_map :2560-2583, tally 실행+방출 :2598-2631, seam try/except :2526-2530; :2492-2498 legacy tally(None, None,...) 시그니처 선례; :4108-4174 veto 호출부 + md 빌더 :4137-4149)
    - backend/shared/python/sunity_shared/analysis/deduction_engine.py (:166-201 tally, fallback 게이트, PER_RECORD_CAP :43)
    - docs/contract.md (§10 deductionBreakdown 절 — 현행 방출 조건 서술)
    - .planning/phases/29-mode3-result-screen-completion/29-PATTERNS.md (§D-01/D-02 mode3 tally seam — 삽입 지점·복사 패턴 원문)
  </read_first>
  <action>
    `_apply_vision_veto_from_context` 의 mode3_held 분기 절개 (29-CONTEXT D-01/D-02/D-03):

    1. status == "mode3_held" 이고 measured_deviations 가 비어있지 않으면: `deduction_engine.tally(None, None, dimension_overall=score_result["overallScore"], measured_deviations=measured_deviations, dimension_scores=score_result.get("dimensionScores"), baseline_kind=baseline_kind)` 실행 (legacy 단일영상 경로 :2492-2498 의 quant=None/ctx=None 시그니처 선례).
    2. breakdown 이 fallback-only 가 아니면 (breakdown.fallback != "quantification_unavailable"): `deductionBreakdown: breakdown.to_dict()` 방출 + `overallScore: breakdown.final` (D-02 — records 0 인 clean 케이스도 final(=100) 채택, 260630-l4e 선례). visionVeto audit 의 status 는 "mode3_held" 유지 — 'applied' 재사용 금지 (result.tsx:711 vetoApplied 파생 의미 오염 방지, RESEARCH Pattern 1 옵션 a).
    3. measured_deviations 빈 dict 또는 fallback-only breakdown 이면: 현행 passthrough 그대로 (breakdown 미방출 + score_result 불변) — D-03 자연 방어 + D-02 무회귀 산술 보장.
    4. 전체를 기존 seam 스타일 try/except 로 감싼다 (:2526-2530 — `except Exception: log.exception → passthrough`). 채점 hook 실패가 분석 실패가 되면 안 된다. 로그는 key=value 구조 (`log.info("mode3 tally ... motion_id=%s records=%d", ...)`).
    5. mode1 TALLY-ELIGIBLE 3-status(candidate_verdict/no_fault/low_alignment_confidence) 로직과 collect 의 mode3_held 조기 bail(:1999-2000)은 byte-불변. md 빌더(:4137-4149)도 불변 — 인자 흐름(reference_dtw_match/split/pointed 전부 None)이 D-01 "ipsf_absolute 전용"을 이미 자동 보장한다.
    6. docs/contract.md §10: 신규 필드 0 (deductionBreakdown 은 이미 mode 무관 optional — RESEARCH Pattern 2). mode3 방출 조건 서술만 추가: "mode3 는 등록 동작의 ipsf_absolute measured seed 보유 시에만 방출, visionVeto.status 는 mode3_held 유지, overallScore == final 항등". analysis.ts/models.py 는 변경 0 이므로 3-way lockstep 커밋 불필요 — contract.md 단독 서술 갱신임을 커밋 메시지에 명시.
    7. 신규 주석: `# 29-CONTEXT D-01/D-02 — mode3_held tally-eligible (24-04 Option A 이식) ...` 형식.
  </action>
  <verify>
    <automated>cd backend && PYTHONPATH=shared/python:. python3 -m pytest tests/test_mode3_tally_seam.py tests/test_pipeline_deduction_seam.py -q && PYTHONPATH=shared/python:. python3 -m pytest tests/pipeline -q -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_mode3_tally_seam.py -q` 전 케이스 PASS (RED → GREEN)
    - `pytest tests/test_pipeline_deduction_seam.py -q` 기존 seam 테스트 전부 PASS (mode1 무회귀)
    - `pytest tests/pipeline -q -x` 신규 실패 0
    - app.py 의 mode1 tally-eligible status tuple("candidate_verdict", "no_fault", "low_alignment_confidence") 문자열 diff 0
    - visionVeto.status 값으로 'applied' 가 mode3 경로에서 방출되지 않음 (테스트 케이스 1 단언)
    - models.py / analysis.ts diff 0 (계약 필드 신설 없음)
    - contract.md 에 mode3 방출 조건 서술 존재 (`grep -c "mode3" docs/contract.md` 가 변경 전보다 증가)
  </acceptance_criteria>
  <done>mode3 tally seam GREEN + mode1 회귀 0 + contract §10 서술 갱신. production 전환은 29-05 sweep 게이트 대기 (Pod 미배포 상태).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 채점 seam → Firestore 저장 점수 | 점수는 백엔드 단일 seam 소유 — 오염 시 신뢰(핵심 가치) 직격 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-02-01 | Tampering | mode1 채점 경로 회귀 | mitigate | mode1 TALLY-ELIGIBLE tuple byte-불변 + test_pipeline_deduction_seam.py 전량 green 게이트 |
| T-29-02-02 | Denial of Service | seam 예외로 분석 전체 실패 | mitigate | 기존 seam try/except passthrough 스타일 승계 (:2526-2530) — hook 실패 ≠ 분석 실패 |
| T-29-02-03 | Repudiation | mode3 점수 근거 불투명 | mitigate | breakdown 방출 시 100−Σ=final 항등 테스트 단언 + deviationSource 'ipsf_absolute' provenance 유지 |
| T-29-02-SC | Tampering | 패키지 설치 | accept | 이 plan 은 패키지 설치 0 |
</threat_model>

<verification>
- backend: `pytest tests/test_mode3_tally_seam.py tests/test_pipeline_deduction_seam.py tests/pipeline -q` 신규 실패 0
- D-02 production 전환 게이트는 29-05 (Pod sweep SERIAL, cold=warm) — 이 plan 완료 시점에 Pod 는 구코드 유지
</verification>

<success_criteria>
- D-01: 등록 동작 md 보유 시 tally 실행·breakdown 방출, Gemini 무호출, mode3_held 불변
- D-02: overallScore == breakdown.final 항등 (코드 구현 완료, sweep 게이트는 29-05)
- D-03: md 빈 dict → 미방출 + 점수 불변 (4/5 빈 criteria 동작 + 미등록 동작 자연 방어)
- 계약 필드 신설 0, Firestore flat 규칙 기존 준수 유지
</success_criteria>

<output>
Create `.planning/phases/29-mode3-result-screen-completion/29-02-SUMMARY.md` when done
</output>
