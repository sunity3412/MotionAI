---
phase: 29-mode3-result-screen-completion
plan: 03
type: execute
wave: 2
depends_on: ["29-02"]
files_modified:
  - backend/tests/test_mode3_fault_zoom_selection.py
  - backend/functions/pipeline/app.py
autonomous: true
requirements: [D-08]

must_haves:
  truths:
    - "mode3 확대비교(zoom) 카드의 대상 선택 소스 = 이번 분석 deductionBreakdown 감점 record 의 criterion id → region 매핑 — |Δscore| top-2 소스 폐기 (D-08)"
    - "improved kind 카드는 방출되지 않는다 (개선 부위 축하 카드 = deferred)"
    - "감점 record 가 없으면 zoom 카드도 없다 — 의도된 동작 (4/5 빈 criteria 동작 자연 귀결, RESEARCH Open Q4)"
    - "record criterion → region 매핑(leg_extension→legs 등)이 29-04 앱 helper(projectDeductionRecordKeypoints) 매핑과 항목 동일해 앱 드릴다운 매칭(result.tsx:962-974, REGION_MEMBER_KEYPOINTS 교집합)이 성립한다 (Pitfall 6 + 29-PLAN-REVIEW HIGH-1)"
  artifacts:
    - path: "backend/functions/pipeline/app.py"
      provides: "_build_mode3_fault_zoom_comparisons 대상 선택 소스 교체 (criterion→region 매핑)"
      contains: "_build_mode3_fault_zoom_comparisons"
    - path: "backend/tests/test_mode3_fault_zoom_selection.py"
      provides: "D-08 criterion→region 소스·improved 억제 회귀 테스트"
      min_lines: 40
  key_links:
    - from: "backend/functions/pipeline/app.py _build_mode3_fault_zoom_comparisons"
      to: "deductionBreakdown records"
      via: "감점 record criterion id → region 파생"
      pattern: "deductionBreakdown|records"
---

<objective>
D-08 — Mode3 확대비교 카드를 "이번 분석 감점 부위만"으로 교체한다.

**전제 (RESEARCH Pitfall 6):** `_build_mode3_fault_zoom_comparisons`(app.py:2970-3039)는 이미 완전 구현체다 — 단 현행 관절 선택이 |Δscore| top-2 + kind=improved/worsened 로 D-08 과 다른 것을 만든다. "배선만 하면 됨"으로 읽고 앱 진입점만 열면 improved 카드가 노출된다 (deferred 결정 위반).

**리뷰 반영 (29-PLAN-REVIEW HIGH-1):** DeductionRecord 는 keypoint/관절 필드를 나르지 않는다 (deduction_engine.py — criterion·측정값·unit·source·deviationSource 뿐). mode3 감점 record 의 실제 criterion id 는 `leg_extension`/`arm_extension`/`line` 계열(ipsf_criteria._MEASURABLE_SEED_IDS)이므로, zoom 대상 파생은 **criterion id → region 매핑**으로 한다. 이 매핑이 29-04 앱 helper 매핑과 항목 동일해야 record 행 ↔ zoom 카드 드릴다운 매칭이 성립한다 (측당 1벌, cross-side 일치가 수용 기준).

Purpose: 시나리오 6단계(확대 비교) Mode3 확장 — mode1 줌과 동일 개념(결함 부위만).
Output: 대상 선택 소스 교체(criterion→region) + improved 억제 + 회귀 테스트. 29-02 의 mode3 breakdown 이 선택 소스이므로 wave 2.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-mode3-result-screen-completion/29-CONTEXT.md
@.planning/phases/29-mode3-result-screen-completion/29-PATTERNS.md
@.planning/phases/29-mode3-result-screen-completion/29-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — mode3 zoom criterion→region 소스 테스트 (기존 테스트 위치 확인 후 확장/신설)</name>
  <files>backend/tests/test_mode3_fault_zoom_selection.py</files>
  <read_first>
    - backend/functions/pipeline/app.py (:2970-3039 _build_mode3_fault_zoom_comparisons 전체 — _joint_scores/change_joints/kinds 현행 로직 :3000-3013, _render_fault_zoom 호출부 :3026-3039)
    - backend/shared/python/sunity_shared/analysis/deduction_engine.py (DeductionRecord dataclass — keypoint 필드 부재 확인, 읽기만)
    - backend/shared/python/sunity_shared/analysis/ipsf_criteria.py (_MEASURABLE_SEED_IDS — mode3 에 등장 가능한 criterion id 4종, 읽기만)
    - backend/tests/ 에서 `grep -rln "mode3_fault_zoom\|_build_mode3_fault_zoom" backend/tests/` 로 기존 테스트 위치 확인 — 존재하면 그 파일 확장, 없으면 신설 (29-VALIDATION Wave 0 지시)
    - backend/tests/test_pipeline_deduction_seam.py (mock 헬퍼·path 주입 관례)
    - app/src/app/analysis/result.tsx (:958-974 zoom 진입점 — record 투영 keypoint ∩ REGION_MEMBER_KEYPOINTS[region] 매칭 규칙, 읽기만)
  </read_first>
  <action>
    D-08 회귀 테스트를 작성한다 (기존 mode3 zoom 테스트가 있으면 확장하고 파일명을 files 에 맞춰 조정 — SUMMARY 에 기록). _render_fault_zoom 은 mock/monkeypatch 로 대체하고 호출 인자(region/관절 목록, kinds)를 캡처한다.

    케이스:
    1. **감점 record → region 소스:** result 에 deductionBreakdown records `[{criterion:'leg_extension', source:'geometry', ...}]` 스텁 존재 → _build_mode3_fault_zoom_comparisons 가 region 'legs' 로 _render_fault_zoom 호출 (|Δscore| 파생 관절 아님). `arm_extension` → 'arms' 케이스 포함.
    2. **improved 억제:** 어떤 입력에서도 kind == "improved" 가 방출 인자에 등장하지 않음
    3. **record 0 → 카드 0:** deductionBreakdown 부재 또는 records 빈 리스트 → _render_fault_zoom 미호출 (zoom 카드 0 = 의도된 동작)
    4. **비매핑 criterion → 카드 0:** records 가 `line` / `dimension_overall_fallback` 뿐이면 _render_fault_zoom 미호출 (line = collective 전신 criterion, 의도된 무투영 — 29-04 앱 helper 결정과 정합)
    5. **인자 보존:** dtw_match / dtw_ref_fps 인자가 현행과 동일하게 전달됨 (28-05/CR-01 무회귀)

    RED 확인 후 커밋: `test(29-03): add failing mode3 zoom criterion-source tests`.
  </action>
  <verify>
    <automated>cd backend && PYTHONPATH=shared/python:. python3 -m pytest tests/test_mode3_fault_zoom_selection.py -q; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - 케이스 1~5 전부 구현 (≥ 5 test functions), mock-based (실 S3/렌더 0)
    - Task 2 이전 실행 시 케이스 1/2/4 FAIL (RED)
    - 기존 mode3 zoom 테스트 존재 여부 확인 결과가 SUMMARY 에 기록됨
  </acceptance_criteria>
  <done>D-08 criterion→region 소스·improved 억제 테스트가 존재하고 현행 구현에서 RED.</done>
</task>

<task type="auto">
  <name>Task 2: 대상 선택 소스 교체 — deduction record criterion→region 매핑 + improved 억제</name>
  <files>backend/functions/pipeline/app.py</files>
  <read_first>
    - backend/functions/pipeline/app.py (:2970-3039 교체 대상 + :4658-4692 zoom 사후 렌더 게이트 — 호출 문맥)
    - .planning/phases/29-mode3-result-screen-completion/29-PATTERNS.md (§D-08 zoom joint 소스 교체 — 현행 로직 원문과 교체 방향)
    - backend/shared/python/sunity_shared/analysis/fault_zoom.py (mode3 fps 경로 — 28-05 완료분, 변경 금지 확인용. region 렌더 경로 _REGION_JOINTS 확인)
  </read_first>
  <action>
    `_build_mode3_fault_zoom_comparisons` 의 대상 산출을 교체한다 (29-CONTEXT D-08 + 29-PLAN-REVIEW HIGH-1):

    1. 현행 `curr_scores/prev_scores/common/change_joints(|Δscore|≥1.0 top-2)/kinds(improved|worsened)` 산출을 제거하고, 이번 분석 result 의 deductionBreakdown records 의 **criterion id → region 매핑**으로 zoom 대상을 파생한다. **DeductionRecord 에 keypoint 필드는 없다** — record 필드에서 관절을 읽으려 하지 말 것 (구 plan 문언 "records 가 keypoint 를 나르는 기존 구조"는 오류였음). 매핑 표 (모듈 상수, 29-04 앱 helper 매핑과 항목 동일 — 측당 1벌):
       - `leg_extension` → region `'legs'` / `split_angle` → region `'legs'` / `arm_extension` → region `'arms'`
       - `line` · `dimension_overall_fallback` · 기타 미등록 id → zoom 미방출 (line 은 collective 전신 criterion(joint_keys 빈 튜플) — 특정 부위 카드가 오도, 29-04 앱 무투영 결정과 정합. 주석으로 의도 명시)
       region 카드로 방출 (fault_zoom 의 기존 region 렌더 경로 재사용 — _REGION_JOINTS) → 앱 selectedZoom 이 REGION_MEMBER_KEYPOINTS[region] 교집합으로 매칭 (result.tsx:962-974). 같은 region 을 가리키는 중복 record 는 카드 1장으로 dedupe, 상한은 현행 top-2 카드 수 유지.
    2. kind 는 "deficit" 로 방출 — 이번 분석 결함 부위 카드 (mode1 줌과 동일 개념). "improved" 는 어떤 경로에서도 방출 금지 (deferred: 개선 부위 축하 카드).
    3. deductionBreakdown 부재/records 빈 리스트/전부 비매핑 criterion → 카드 0 개 (조기 return). 주석으로 의도 명시: `# 29-CONTEXT D-08 — 감점 record 없으면 zoom 카드 없음 = 의도된 동작 (4/5 빈 criteria 동작)`.
    4. `_render_fault_zoom(...)` 호출부의 dtw_match=prev_dtw_match, dtw_ref_fps=_pipeline_frame_fps() 등 나머지 인자는 그대로 유지 (28-05/CR-01) — 대상(region)·kinds 인자만 바뀐다.
    5. 에러 격리: 기존 seam 스타일 (`except Exception: log.exception → 카드 0`) 유지.
    6. **cross-side 정합 박제:** 사용한 criterion→region 매핑 표를 SUMMARY 에 그대로 기록 — 29-04 SUMMARY 의 앱 helper 매핑 표와 대조해 id 키·region 값 일치를 확인한다 (불일치 = D-08 드릴다운 실패, 수용 불가).
  </action>
  <verify>
    <automated>cd backend && PYTHONPATH=shared/python:. python3 -m pytest tests/test_mode3_fault_zoom_selection.py -q && PYTHONPATH=shared/python:. python3 -m pytest tests/ -k "fault_zoom" -q && PYTHONPATH=shared/python:. python3 -m pytest tests/pipeline -q -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_mode3_fault_zoom_selection.py -q` 전 케이스 PASS (RED → GREEN)
    - `pytest tests/ -k "fault_zoom" -q` 기존 fault_zoom 테스트 신규 실패 0 (mode1 zoom·28-05 경로 무회귀)
    - app.py 에서 mode3 zoom 경로의 "improved" 문자열 방출 코드 0 (`grep -n '"improved"' backend/functions/pipeline/app.py` 결과가 mode3 빌더 함수 내부에 없음)
    - mode3 빌더에 criterion→region 매핑 상수 존재: `grep -n "leg_extension" backend/functions/pipeline/app.py` 매치 ≥ 1 (zoom 빌더 매핑)
    - SUMMARY 에 criterion→region 매핑 표 박제 (29-04 SUMMARY 매핑 표와 대조용 — cross-side 일치가 D-08 수용 기준)
    - fault_zoom.py diff 0 (렌더러 무접촉)
  </acceptance_criteria>
  <done>mode3 zoom 카드 = 감점 record criterion→region 매핑만, improved 0, record 없으면 카드 없음, 기존 렌더 인자 보존, 앱 매핑과 항목 일치.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| zoom 렌더 → S3 업로드 → 앱 표시 | 사후 태스크가 PNG 를 S3 에 올리고 앱이 소비 — 기존 경로 재사용 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-03-01 | Tampering | mode1 zoom 경로 회귀 | mitigate | `pytest -k fault_zoom` 전량 green + fault_zoom.py 무접촉 |
| T-29-03-02 | Denial of Service | zoom 빌드 예외로 분석 실패 | mitigate | 기존 에러 격리(log.exception → 카드 0) 유지 — 사후 태스크 실패 ≠ 분석 실패 |
| T-29-03-03 | Information Disclosure | zoom PNG presigned URL | accept | 기존 mode1 zoom 과 동일 경로·TTL — 본인 분석 산출물, 신규 표면 0 |
| T-29-03-SC | Tampering | 패키지 설치 | accept | 이 plan 은 패키지 설치 0 |
</threat_model>

<verification>
- backend: 신규 테스트 + `-k fault_zoom` + tests/pipeline 신규 실패 0
- cross-side 매핑 일치: 29-03/29-04 SUMMARY 매핑 표 대조 (id 키·region 값 동일)
- 실데이터(PNG 재생성) 확인은 29-05 Pod sweep 산출물 검사 + 29-08 HUMAN-UAT 적립으로 커버
</verification>

<success_criteria>
- D-08 완료: mode3 zoom = 이번 분석 감점 부위만(criterion→region), improved 미방출, 앱 helper 매핑과 항목 일치로 드릴다운 매칭 성립
</success_criteria>

<output>
Create `.planning/phases/29-mode3-result-screen-completion/29-03-SUMMARY.md` when done
</output>
