---
phase: 29-mode3-result-screen-completion
plan: 04
type: execute
wave: 2
depends_on: ["29-02"]
files_modified:
  - app/src/lib/deductionLabels.ts
  - app/src/app/analysis/result.tsx
  - app/src/components/ScoreBreakdownSection.tsx
autonomous: true
requirements: [D-03, D-04, D-05, D-06, D-07, D-10]

must_haves:
  truths:
    - "mode3 doc 에 deductionBreakdown 이 있으면 점수 계산 내역 섹션·번호 마커·행동 라벨이 렌더된다 — mode1 게이트 3곳의 mode 무관화 (D-01 소비)"
    - "mode3 ipsf_absolute record(leg_extension/arm_extension/split_angle)가 마커·행동구·zoom 매칭으로 투영된다 — 투영 규칙은 deductionLabels 공용 helper 1벌 (29-PLAN-REVIEW HIGH-1)"
    - "mode3 감점 0(clean) 축하 카피는 mode1 문구(정은지 유사)를 재사용하지 않는다"
    - "미등록 동작(scoreSuppressed)은 '제공 불가' 통보가 아닌 행동 유도 안내를 보여준다 — 코치님 영상/이전 영상 비교 유도 (D-03)"
    - "legacy mode3 doc 은 재분석 유도 배너를 본다 — Phase 28 배너와 통합, motionAlignment undefined 판정 (D-04)"
    - "mode3 결과에 한계 고지 1줄이 항상 표시된다 — belle 승인 뼈대, 금지어 '각도' 0 (D-05)"
    - "mode3 비교 라벨은 지난 영상/이번 영상 계열이고, 첫 분석은 비교 섹션 숨김 + 안내 1줄 (D-06/D-07)"
    - "mode3 second+ 워핑은 신규 구현 없이 기존 mode 무관 경로로 흐른다 — 검증만 (D-10, Pitfall 7)"
  artifacts:
    - path: "app/src/lib/deductionLabels.ts"
      provides: "projectDeductionRecordKeypoints 공용 투영 helper + ipsf_absolute criterion 매핑"
      contains: "projectDeductionRecordKeypoints"
    - path: "app/src/app/analysis/result.tsx"
      provides: "게이트 3곳 mode 무관화 + D-03/04/06/07 카피·배너·라벨 + 로컬 투영 사본 제거"
      contains: "deductionBreakdown"
    - path: "app/src/components/ScoreBreakdownSection.tsx"
      provides: "D-05 한계 고지 optional prop (footnote 슬롯)"
  key_links:
    - from: "app/src/app/analysis/result.tsx showBreakdownSection"
      to: "result.deductionBreakdown"
      via: "mode 무관 게이트"
      pattern: "deductionBreakdown != null"
    - from: "app/src/app/analysis/result.tsx selectedZoom·actionPhraseForRecord"
      to: "deductionLabels projectDeductionRecordKeypoints"
      via: "공용 helper import (규칙 1벌)"
      pattern: "projectDeductionRecordKeypoints"
    - from: "app/src/app/analysis/result.tsx"
      to: "ScoreBreakdownSection 한계 고지 prop"
      via: "mode3 분기 전달"
      pattern: "ScoreBreakdownSection"
---

<objective>
Mode3 결과 화면 앱 소비 — D-03/D-04/D-05/D-06/D-07/D-10. 29-02 가 방출하는 mode3 deductionBreakdown 을 앱이 그리고, 미등록/legacy/첫 분석의 안내 UX 를 완성한다.

**전제 보정 (Pitfall 1 UX 귀결):** 등록 5동작 중 4동작은 criteria 가 비어 breakdown 이 방출되지 않는다 — "Mode3 내역"의 실질 콘텐츠는 power-spin 외에는 없고, D-05 한계 고지가 UX 의 본체다. breakdown 부재를 버그로 취급하지 않는다. belle 확답(2026-07-09): "Gemini 눈 없이 RTMW 자만" 구조가 사용자에게 설명돼야 함 — D-05 고지가 그 답.

**리뷰 반영 (29-PLAN-REVIEW HIGH-1):** 현행 앱 투영 규칙(angle_vs_reference__ prefix / source='vision' 만)은 mode3 의 실제 record `{criterion:'leg_extension', source:'geometry'}` 를 [] 로 투영해 마커·행동구·zoom 매칭이 전부 무음 실패한다 — 게이트만 열면 "행은 있는데 마커·사진이 없는" 반쪽 D-01/D-08 이 된다. Task 1 이 공용 투영 helper + ipsf_absolute 매핑으로 이 갭을 먼저 막는다.

Purpose: 시나리오 3단계(점수 근거) + 6단계(비교) 의 Mode3 앱 절반. 전부 OTA-safe (JS-only).
Output: deductionLabels 투영 helper + result.tsx 게이트·카피·라벨 + ScoreBreakdownSection 한계 고지 슬롯.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-mode3-result-screen-completion/29-CONTEXT.md
@.planning/phases/29-mode3-result-screen-completion/29-PATTERNS.md
@.planning/phases/29-mode3-result-screen-completion/29-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: HIGH-1 — criterion→keypoint 투영 공용 helper + ipsf_absolute 매핑</name>
  <files>app/src/lib/deductionLabels.ts, app/src/app/analysis/result.tsx</files>
  <read_first>
    - app/src/lib/deductionLabels.ts (:76-96 KEYPOINT_FROM_ANGLE_KEY·REGION_MEMBER_KEYPOINTS, :185-237 buildDeductionMarkers 내부 투영 블록 :200-216)
    - app/src/app/analysis/result.tsx (:284-320 recordProjectedKeypoints·actionPhraseForRecord, :958-974 selectedZoom 매칭)
    - backend/shared/python/sunity_shared/analysis/ipsf_criteria.py (:56-178 criterion id 카탈로그 — leg_extension/arm_extension/split_angle/line, _MEASURABLE_SEED_IDS 4종. 읽기만)
    - .planning/phases/29-mode3-result-screen-completion/29-PLAN-REVIEW.md (§HIGH-1)
  </read_first>
  <action>
    투영 규칙을 공용 helper 1벌로 승격하고 ipsf_absolute criterion 매핑을 추가한다 (29-PLAN-REVIEW HIGH-1 — D-01/D-08 소비 성립 조건):

    1. deductionLabels.ts 에 `export function projectDeductionRecordKeypoints(record: DeductionRecord, faultJoints: readonly KeypointName[] | undefined): KeypointName[]` 신설 — 현행 두 사본(buildDeductionMarkers 내부 :200-216, result.tsx recordProjectedKeypoints :288-305)의 규칙을 이 한 함수로 이관 (quick-260705-r6v 주석이 이미 "동일 규칙" 페어링을 명시 — 규칙 1벌 실현). 기존 규칙 보존: `dimension_overall_fallback`·unit `score_delta` → [] / `angle_vs_reference__{jk}` → KEYPOINT_FROM_ANGLE_KEY 단일 keypoint / source==='vision' → faultJoints 전체.
    2. ipsf_absolute criterion id 매핑 확장 — 모듈 상수 테이블(예: `CRITERION_REGION_KEYPOINTS`)로 선언: `leg_extension` → REGION_MEMBER_KEYPOINTS.legs / `arm_extension` → REGION_MEMBER_KEYPOINTS.arms / `split_angle` → REGION_MEMBER_KEYPOINTS.legs (ipsf_criteria._MEASURABLE_SEED_IDS 4종에 포함 — mode3 record 로 등장 가능, 기존 vision-split legs 투영과 동일 부위). `line` → 투영 없음 (**의도된 결정, 주석으로 문서화** — collective 전신 라인 criterion(joint_keys 빈 튜플)이라 특정 관절 마커가 오도, zoom region 도 없음 — 29-03 backend 무방출 결정과 정합). 그 외 미등록 id → [] 유지 (fabricate 0). **평가 순서 고정 (mode1 무회귀):** criterion 테이블은 기존 3규칙(fallback/score_delta → prefix `angle_vs_reference__` → source==='vision')에 **전부 비매치**인 record 에만 적용 — 분기 순서상 마지막. mode1 의 vision-sourced `split_angle` record 는 종전대로 vision 분기에서 faultJoints(vision 확정 부분집합)로 투영되고 테이블의 legs 고정 4관절로 절대 바뀌지 않는다 (테이블 분기를 vision 분기보다 앞에 두면 mode1 그룹 마커 멤버·centroid 가 조용히 드리프트 — 금지).
    3. buildDeductionMarkers 가 helper 를 소비하도록 교체 + 그룹(중점 1점) 마커 판정을 source==='vision' 한정에서 `projected.length >= 2` 로 일반화 — leg_extension 의 legs 4관절 투영이 개별 4점(전부 같은 번호)이 아닌 centroid 그룹 1점으로 표시된다 (quick-260705-r6v "번호 다 1" 근본원인과 동일 문제의 일반화). first-wins·번호 부여 규칙은 불변.
    4. result.tsx 로컬 `recordProjectedKeypoints` 함수 삭제 → helper import 로 대체. actionPhraseForRecord·selectedZoom(:962-974) 소비 경로는 인자 동일 — 호출부 무변경.
    5. **backend 정합 계약 (29-03 크로스체크):** 이 매핑 표(criterion id → legs/arms/무투영)는 29-03 backend zoom region 파생 표와 항목 동일해야 한다 — 측당 1벌, id 키·region 값 일치. SUMMARY 에 매핑 표를 박제해 29-03 SUMMARY 표와 대조.
    6. 스펙 인용 주석: `// 29-PLAN-REVIEW HIGH-1 / 29-CONTEXT D-01·D-08 — ...`.
  </action>
  <verify>
    <automated>cd app && npm run typecheck && grep -q "projectDeductionRecordKeypoints" src/lib/deductionLabels.ts && grep -q "projectDeductionRecordKeypoints" src/app/analysis/result.tsx && ! grep -q "function recordProjectedKeypoints" src/app/analysis/result.tsx && grep -q "leg_extension" src/lib/deductionLabels.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && npm run typecheck` exit 0
    - deductionLabels.ts 에 helper 정의 1개 + buildDeductionMarkers 내부 소비 (투영 규칙 사본 0 — result.tsx 로컬 함수 제거 grep 으로 확인)
    - fixture-level 소스 단언 (JS 러너 부재 — grep 으로 인코딩): `leg_extension`→legs / `arm_extension`→arms / `split_angle`→legs 매핑 라인 존재 + `line` 무투영 결정 주석 존재
    - 평가 순서 단언: helper 소스에서 criterion 테이블 조회 분기가 source==='vision' 분기 **뒤**에 위치 (소스 라인 순서 확인) — vision record 의 faultJoints 투영 경로 불변 (mode1 split_angle 무회귀)
    - mode3 record `{criterion:'leg_extension', source:'geometry'}` 가 (a) 내역 행 번호, (b) 그룹 마커, (c) region 'legs' zoom(selectedZoom) 매칭을 얻는 코드 경로가 성립 (helper → buildDeductionMarkers/selectedZoom 소비 확인) — 실기기 end-to-end 는 29-08 HUMAN-UAT power-spin 드릴다운 항목으로 적립 (SUMMARY 에 항목 후보 기록)
    - SUMMARY 에 criterion→투영 매핑 표 박제 (29-03 표와 대조용)
  </acceptance_criteria>
  <done>투영 규칙 helper 1벌 + ipsf_absolute 매핑 완성 — mode3 record 가 마커·행동구·zoom 매칭으로 투영된다.</done>
</task>

<task type="auto">
  <name>Task 2: mode1 게이트 3곳 mode 무관화 + D-03 행동 유도 안내</name>
  <files>app/src/app/analysis/result.tsx</files>
  <read_first>
    - app/src/app/analysis/result.tsx (:651-906 — scoreSuppressed/suppressedHeaderCopy :651-661, cleanPass :766-773, actionLabels hasBreakdown :812-814, timelineTicks :890-900, showBreakdownSection :902-906, breakdownBasisLine :789 부근, vetoApplied :711)
    - .planning/phases/29-mode3-result-screen-completion/29-PATTERNS.md (§result.tsx 게이트 확장 — 4곳 원문과 확장 방향)
    - app/src/types/analysis.ts (DeductionBreakdown/AnalysisDoc — 필드 확인, 변경 금지)
  </read_first>
  <action>
    RESEARCH Pattern 6 설계대로 게이트를 확장한다 (29-CONTEXT D-01 소비 + D-03):

    1. `showBreakdownSection`(:902-906): `cmp.mode === 'mode1' &&` 조건 제거 → `result.deductionBreakdown != null` 단독 (mode3 는 29-02 방출 시에만 필드 존재 → 미등록/legacy/빈 criteria 동작 자연 숨김). 기존 주석의 "mode1 전용" 근거를 29-CONTEXT D-01 인용으로 갱신.
    2. `cleanPass`(:766-773): breakdown 을 mode 무관 전달. 단 mode3 의 감점 0 축하 카피는 mode1 문구(정은지 유사 계열) 재사용 금지 — mode3 톤 별도 문구 신설 (발전/자세 형태 중심, "~해요" 체, "각도" 금지). cleanPass 카드 렌더부에서 mode 분기로 카피 선택.
    3. `actionLabels hasBreakdown`(:812-814): `cmp.mode === 'mode1' &&` 제거 → breakdown 보유로 일반화. mode3 는 windowMedianAngleDeltas 없음(veto 미실행) — 기존 2순위 JointScore.deltaDeg 경로가 커버하므로 소스 우선순위 로직은 무변경.
    4. `timelineTicks`(:890-900): 변경 금지 — mode3 는 window 시점이 없어 빈 배열 유지가 정직 (주석 근거 :890-891).
    5. D-03: `suppressedHeaderCopy`(:655) 의 mode3 미등록 케이스 문구를 행동 유도형으로 교체/보강 — "제공 불가" 통보가 아니라 "코치님(정은지) 영상과 비교하거나, 같은 동작을 새 영상으로 올려 이전 연습과 비교해보세요" 계열 전진형 유도 (belle 카피 방향: "더 연습하고 새로운 영상으로 같은 자세를 비교하면 본격 분석이 시작된다"). reason-owns-copy 구조(:653-661) 유지.
    6. `vetoApplied`(:711) 파생은 무변경 — 29-02 가 mode3 에서 status 'mode3_held' 를 유지하므로 오염 없음 (확인만).
    7. 신규 주석 스펙 인용: `// 29-CONTEXT D-01/D-03 — ...`.
  </action>
  <verify>
    <automated>cd app && npm run typecheck && grep -n "deductionBreakdown != null" src/app/analysis/result.tsx | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && npm run typecheck` exit 0
    - showBreakdownSection 과 actionLabels hasBreakdown 에서 `cmp.mode === 'mode1'` 조건이 제거됨 (grep 으로 두 게이트 위치에 mode1 조건 부재 확인)
    - timelineTicks 게이트 diff 0
    - mode3 clean 축하 카피가 mode1 카피와 다른 문자열 (정은지 언급 없음)
    - suppressedHeaderCopy 의 mode3 미등록 문구에 행동 유도(비교 유도) 표현 존재, "제공 불가/지원하지 않아요" 계열 단독 통보 없음
    - 신규 카피에 "각도" 0건 — result.tsx 는 기존 "각도" 26건 보유라 절대 grep 게이트 불가, 커밋 전 diff-scoped 로 판정: `! (git diff -U0 -- src/app/analysis/result.tsx | grep '^+' | sed 's/심각도//g' | grep -q "각도")` (추가 라인만, '심각도' substring 제외)
  </acceptance_criteria>
  <done>mode3 breakdown doc 이 내역 섹션·마커·행동 라벨을 렌더하고, 미등록 동작은 행동 유도 안내를 본다.</done>
</task>

<task type="auto">
  <name>Task 3: D-04 legacy 재분석 배너 통합 + D-05 한계 고지</name>
  <files>app/src/app/analysis/result.tsx, app/src/components/ScoreBreakdownSection.tsx</files>
  <read_first>
    - app/src/app/analysis/result.tsx (:1464-1484 Phase 28 alignUpsellBanner — 판정 규칙 주석 :1464-1468 "필드 자체 부재(undefined)만 순수 legacy")
    - app/src/components/ScoreBreakdownSection.tsx (전체 — optional prop 관례 :31-47, footnote 슬롯 :127-152, footnote 스타일 토큰 :209-213, 객관성 헤더 :13-14)
    - .planning/phases/29-mode3-result-screen-completion/29-PATTERNS.md (§ScoreBreakdownSection D-05 슬롯 + §result.tsx D-04 배너 원문)
  </read_first>
  <action>
    D-04 (Claude 재량 행사 — 배너 통합 채택): legacy mode3 doc 전용 배너를 신설하지 않고 Phase 28 alignUpsellBanner(:1471-1484, `motionAlignment === undefined` 판정)에 통합한다. 근거: "breakdown 부재 = legacy" 판정은 빈 criteria 4동작의 신선한 doc 에서도 참이 되어 "재분석하면 내역이 나와요"가 거짓 약속이 된다 (Pitfall 1). 통합 배너 카피를 "다시 분석하면 자동 구간 맞춤 등 최신 분석이 적용돼요" 계열로 일반화 (재분석 시 구간 맞춤 + 내역(등록 동작 한정)이 함께 따라옴 — 특정 기능 약속 없이 포괄). 판정 규칙 주석(:1464-1468) 승계 + `// 29-CONTEXT D-04 — 28 배너 통합 (재량)` 주석.

    D-05: 한계 고지 1줄.
    1. ScoreBreakdownSection 에 optional prop (예: `limitNotice?: string`) 신설 — 미전달 시 렌더 diff 0 (기존 optional prop 관례 :31-47 준수). 전달 시 footnote 슬롯(:127-152)에 기존 footnote 스타일 토큰으로 렌더.
    2. result.tsx 에서 mode3 이고 breakdown 표시 중이면 limitNotice 전달. breakdown 부재 mode3 doc 에는 같은 문구를 점수 카드 아래(또는 D-03 안내 인접) 독립 1줄로 렌더 — mode3 결과에는 한계 고지가 항상 1곳 존재해야 한다 (RESEARCH: "breakdown 부재/무감점 mode3 를 D-05 한계 고지 중심으로 렌더").
    3. 카피는 belle 승인 뼈대 사용: "카메라로 잰 자세 형태 기준이에요. 같은 동작을 새 영상으로 다시 올리면 이전 영상과 비교한 발전 분석이 본격 시작돼요. 그립·디테일 점검은 코치님 비교 분석을 이용해보세요." (세부 다듬기 재량, 뼈대·톤 유지)
    4. **금지어 "각도" 절대 금지** — 신규 카피 + 재사용하는 기존 deductionLabels 카피의 "각도" 포함 여부 grep 확인 (29-CONTEXT D-05 3중 근거: 사용자 미이해·mode3 angle 차원 용어 충돌·강사 철학 충돌).
    5. mode1 렌더 diff 0 (limitNotice 미전달).
  </action>
  <verify>
    <automated>cd app && npm run typecheck && ! (sed 's/심각도//g' src/components/ScoreBreakdownSection.tsx | grep -q "각도")</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && npm run typecheck` exit 0
    - ScoreBreakdownSection 신규 prop 은 optional — mode1 호출부 무변경으로 컴파일 통과 (미전달 시 렌더 diff 0)
    - ScoreBreakdownSection.tsx 와 신규 고지 카피에 "각도" 0건 — 실패형 게이트로 판정: `! (sed 's/심각도//g' src/components/ScoreBreakdownSection.tsx | grep -q "각도")` ('심각도' substring 오탐 제외, 항상-exit-0 인 `| wc -l` 형 금지)
    - result.tsx 에 mode3 전용 배너 신설 없음 — alignUpsellBanner 1곳 통합 (배너 렌더 블록 grep 1개)
    - 한계 고지 문구에 "자세 형태 기준" + "새 영상" + "코치님" 3요소 존재 (뼈대 보존 확인)
    - mode3 doc 렌더 경로에서 breakdown 유/무 양쪽 모두 한계 고지 1줄이 도달 (코드 경로 분기 확인)
  </acceptance_criteria>
  <done>legacy mode3 doc = 통합 배너, 모든 mode3 결과 = 한계 고지 1줄 ("각도" 0), mode1 무회귀.</done>
</task>

<task type="auto">
  <name>Task 4: D-06 비교 라벨 + D-07 첫 분석 안내 + D-10 워핑 경로 검증</name>
  <files>app/src/app/analysis/result.tsx</files>
  <read_first>
    - app/src/app/analysis/result.tsx (:1373-1486 비교 섹션 — 첫 분석 숨김 :1373, 라벨 :1388-1396, alignment prop 전달 :1387, prev 재발급 훅 :683-706)
    - app/src/components/VideoCompare.tsx (:263-292 props 소비부 — 읽기만, 변경 금지: 29-07 소유 파일)
  </read_first>
  <action>
    D-06: mode3 비교 라벨을 "지난 영상/이번 영상" 계열로 정렬 — rightLabel 현행 '지난 분석'(:1394)과 좌측 라벨을 함께 점검해 지난/이번 쌍이 명확하게 (정은지 언급 없음, 정확한 문자열 재량). mode1 라벨(`${cmp.athleteName} 선수`) 무변경.

    D-07: 첫 분석(cmp.mode === 'mode3' && cmp.isFirst) 은 현행대로 비교 섹션 전체 숨김 유지 + 그 자리에 안내 1줄 신설: "다음 분석부터 이전 영상과 비교해 드려요" 늬낌, D-05 고지와 톤 통일 ("~해요" 체, 전진형). 정은지 폴백 금지 (mode1 혼동 + 미보유 동작 reference 부재 — 29-CONTEXT D-07 기각 사유).

    D-10 (검증 태스크 — Pitfall 7: 신규 구현 금지): mode3 second+ 워핑은 백엔드 방출(28-04, mode1+mode3) + 앱 mode 무관 전달(:1387) + VideoCompare mode 무관 워핑으로 이미 흐른다.
    1. result.tsx :1387 의 alignment 전달이 mode 조건 없이 이루어짐을 확인하고, 확인 근거 주석 추가: `// 29-CONTEXT D-10 — mode3 워핑은 28-04 방출 + mode 무관 전달로 기흐름 (신뢰도 사다리·배속 클램프 28 D-02 동일 적용)`.
    2. "mode3 워핑 대규모 리팩터" 류 코드 변경 금지 — 코드 diff 는 주석 + 라벨 + 안내 1줄뿐이어야 한다.
    3. 실기기 mode3 second+ 워핑 확인 항목은 29-08 이 HUMAN-UAT.md 에 적립한다 (이 plan 은 SUMMARY 에 항목 후보만 기록).
  </action>
  <verify>
    <automated>cd app && npm run typecheck && grep -n "지난" src/app/analysis/result.tsx | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && npm run typecheck` exit 0
    - mode3 라벨 쌍이 지난/이번 계열 문자열 (정은지·선수 표현 0), mode1 라벨 diff 0
    - 첫 분석(mode3 isFirst) 분기에 안내 1줄 렌더 존재, 비교 섹션은 여전히 미렌더
    - VideoCompare.tsx diff 0 (29-07 파일 소유권 침범 없음)
    - alignment prop 전달부(:1387)에 mode 조건 추가 없음 (D-10 — 기존 mode 무관 유지)
  </acceptance_criteria>
  <done>mode3 라벨·첫 분석 안내 완성, D-10 은 코드 검증 + UAT 후보 기록으로 종결 (신규 워핑 코드 0).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firestore doc → 앱 렌더 | malformed doc 이 클라이언트에 도달 가능 — 방어적 정규화가 방벽 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-04-01 | Tampering | 앱에서 감점 재계산/재해석 | mitigate | 저장된 record 그대로 표기 원칙 유지 (ScoreBreakdownSection 헤더 :13-14) — 숫자 조작 코드 0, 투영은 표시 매핑만(값 무변경), 게이트만 확장 |
| T-29-04-02 | Denial of Service | malformed mode3 doc 크래시 | mitigate | normalize() undefined 접기 선례 유지 — 필드 부재 = 현행 렌더 자연 분기 (legacy 폴백). 미등록 criterion id → 투영 [] (crash 0) |
| T-29-04-03 | Spoofing | 잘못된 안내로 사용자 오신뢰 | mitigate | D-05 한계 고지 필수 렌더 (측정 범위 정직 고지) + D-04 거짓 약속 회피 (배너 카피 일반화) |
| T-29-04-SC | Tampering | 패키지 설치 | accept | 이 plan 은 패키지 설치 0 |
</threat_model>

<verification>
- `cd app && npm run typecheck` exit 0
- 투영 규칙 1벌: result.tsx 로컬 사본 제거 grep + deductionLabels helper grep
- 금지어 grep: 신규 카피 "각도" 0건
- cross-side 매핑 일치: 29-03/29-04 SUMMARY 매핑 표 대조
- 실기기 확인(내역 렌더·라벨·안내·power-spin 드릴다운)은 29-08 HUMAN-UAT.md 적립
</verification>

<success_criteria>
- HIGH-1 해소: criterion→keypoint 투영 helper 1벌 + ipsf_absolute 매핑 — mode3 record 마커·행동구·zoom 매칭 성립
- D-03/D-04/D-05/D-06/D-07 앱 UX 완성 (전부 OTA-safe)
- D-10 검증 종결 — 신규 워핑 구현 0, mode 무관 경로 확인
- mode1 렌더·게이트 회귀 0
</success_criteria>

<output>
Create `.planning/phases/29-mode3-result-screen-completion/29-04-SUMMARY.md` when done
</output>
