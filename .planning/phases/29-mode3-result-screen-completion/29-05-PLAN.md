---
phase: 29-mode3-result-screen-completion
plan: 05
type: execute
wave: 3
depends_on: ["29-02", "29-03"]
files_modified:
  - backend/evals/phase29/run_sweep.py
  - backend/evals/phase29/assert_gates.py
  - backend/evals/phase29/eval_keys.json
  - backend/evals/phase29/README.md
autonomous: true
requirements: [D-02]

must_haves:
  truths:
    - "정은지 페어셋 mode3 sweep 게이트가 Pod 에서 SERIAL 로 실행되고 assert_gates exit 0 — D-02 overallScore=tally 전환 조건 충족"
    - "게이트 기대치가 실체를 반영한다: success 멤버 무감점(final ≥ 기존), power-spin fault 만 leg_extension 감점 변별, 나머지 4동작 fallback 항등 (Pitfall 1)"
    - "cold == warm 결정성 (recognizer TechniqueCache — mode3 는 Gemini recognizer 1회뿐)"
    - "게이트 PASS 후에만 Pod 서버가 신코드로 재기동된다 — 전환 조건이 production 노출을 게이트"
    - "자기 sweep 재보정 금지 — 게이트 FAIL 시 임계/criteria 수정 없이 정지·보고 (calibration-source-hard-gate)"
    - "D-02 score-switch 가시성: 키별 old/new 점수 대조표가 SUMMARY 에 박제되고, clean doc 이 단일 협소 criterion 근거로 대폭 상승(→100 류)하면 Pod 재기동 전 belle 결정 체크포인트 — 무음 ship 금지 (29-PLAN-REVIEW MEDIUM-1)"
  artifacts:
    - path: "backend/evals/phase29/run_sweep.py"
      provides: "phase25 복제-확장 SERIAL in-process sweep (mode3 단독 분석)"
      min_lines: 60
    - path: "backend/evals/phase29/assert_gates.py"
      provides: "D-02 게이트 5종 (방향 비교만, 점수 리터럴 0)"
      min_lines: 60
    - path: "backend/evals/phase29/eval_keys.json"
      provides: "phase24 6페어의 mode3 변형 키"
  key_links:
    - from: "backend/evals/phase29/run_sweep.py"
      to: "pipeline _process"
      via: "in-process import (phase25 계보)"
      pattern: "_process|importlib"
    - from: "backend/evals/phase29/assert_gates.py"
      to: "EVAL_OUT_DIR 산출물"
      via: "artifact 판독 (baseline read-only)"
      pattern: "EVAL_OUT_DIR|sunity_eval_out"
---

<objective>
D-02 검증 게이트 — 정은지 페어셋 mode3 Pod sweep. 29-02(tally seam)/29-03(zoom 소스)의 백엔드 변경을 실영상으로 검증하고, PASS 시에만 Pod 서버를 재기동해 production 전환한다.

**게이트 설계 원칙 (locked):** 특정 점수 리터럴 assert 금지 + 자기 sweep 재보정 금지(calibration-source-hard-gate) + SERIAL 필수(파이프라인 동시성 비안전) + baseline read-only.

**리뷰 반영 (29-PLAN-REVIEW MEDIUM-1):** "clean tally → final 100 허용"은 게이트로는 정당하나, 이번 phase 의 criteria 커버리지는 매우 좁다(5동작 중 4동작 fallback, power-spin 만 유효 criteria). clean doc 이 단일 협소 criterion 통과 근거만으로 100 으로 뛰는 것은 게이트 PASS 여도 제품 결정 사안 — 키별 old/new 대조표를 가시화하고, 발동 시 Pod 재기동 전 belle 즉시 결정(결정형 = batch-UAT 아님)을 거친다.

Purpose: D-02 "sweep 게이트 통과가 전환 조건" 이행. 29-02 는 코드만 랜딩했고 Pod 는 구코드 — 이 plan 이 검증·전환을 담당.
Output: evals/phase29 하네스 + Pod sweep PASS 증거 + score-switch 대조표 + 서버 재기동.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-mode3-result-screen-completion/29-CONTEXT.md
@.planning/phases/29-mode3-result-screen-completion/29-RESEARCH.md
@.planning/phases/29-mode3-result-screen-completion/29-02-SUMMARY.md
@.planning/phases/29-mode3-result-screen-completion/29-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: evals/phase29 하네스 — phase25 복제-확장</name>
  <files>backend/evals/phase29/run_sweep.py, backend/evals/phase29/assert_gates.py, backend/evals/phase29/eval_keys.json, backend/evals/phase29/README.md</files>
  <read_first>
    - backend/evals/phase25/run_sweep.py (전체 — 골격 :56-94, 산출물 규율 :21-29, env setdefault 블록: RTMW_DETERMINISTIC/GEMINI_VISION_VETO_ENABLED/GEMINI_MAX_VETO_WALL_S, 실행 커맨드 헤더 :41-50)
    - backend/evals/phase25/assert_gates.py (헤더 계약 — phase24 게이트 import 재사용, SKIPPED 규칙, exit 코드)
    - backend/evals/phase24/eval_keys.json (6페어 키 — bucket sunity-motion-pilot-videos, fixtures/phase15)
    - backend/evals/phase25/README.md (Pod sweep 실행 블록)
  </read_first>
  <action>
    `backend/evals/phase29/` 신설 — phase25 를 복제-확장한다 (같은 계보 3대째, 29-CONTEXT canonical ref).

    run_sweep.py:
    - phase25 골격 그대로: path 주입, module-level env setdefault (RTMW_DETERMINISTIC=1, GEMINI_VISION_VETO_ENABLED=1, GEMINI_MAX_VETO_WALL_S=300 — production mirror, 260705-d64 박제), EVAL_OUT_DIR(기본 /tmp/sunity_eval_out) 하위에만 기록, `--tag warm` 재실행 → `*_warm.json`, repo 내 baseline/ read-only (2026-07-02 오염 사고 재발 금지).
    - **mode3 전용 조정 (RESEARCH Pattern 5):** eval_keys = phase24 6페어를 mode='mode3' **단독 분석**(prev 없음 — 절대 기준 채점이 D-02 본체)으로 재사용. SERIAL 실행.
    - 산출물에 visionVeto.status, deductionBreakdown(records/final), overallScore 캡처.

    assert_gates.py — 게이트 5종 + 보조 (전부 방향/구조 비교, 점수 리터럴 하드코딩 0):
    1. success 멤버: deduction record 0 AND overallScore ≥ 해당 키의 기존 baseline 점수 (무감점·비하락 — "≥ 기존" 은 clean tally 100 승격 허용)
    2. power-spin fault: leg_extension 계열 감점 record ≥ 1 발화 (유일한 criteria 보유 동작 — 변별 실증)
    3. kip-up/peter-pan/elbow-twist-sister/pdshape fault: deductionBreakdown 미방출 AND overallScore == baseline 항등 (fallback 무회귀 — Pitfall 1: 이 4동작 무감점은 정상, "감점 엔진 고장" 오판·임계 조정 금지)
    4. cold == warm: 전 키 overallScore/records 동일 (결정성)
    5. breakdown 방출 키: overallScore == final == 100 − Σ(records points) 항등 (D-02)
    - 보조: 전 키 visionVeto.status == "mode3_held" ('applied' 오염 0)
    - artifact 부재 = SKIPPED(실패 아님), exit 0 = PASS / 1 = FAIL. phase24 게이트 헬퍼는 import 재사용.
    - not_pole 게이트는 mode3 에 미적용 — climb 키 관련 mode1 게이트는 포함하지 않는다 (RESEARCH Pattern 5 판단 반영).

    README.md: Pod 실행 블록 — SERIAL 명시, cold → warm → assert 순서, Gemini 크레딧 사전 확인 항목 (mode3 도 recognizer 1회 호출), gsd-pod-work-push-first 규칙, score-switch 대조표·정지 조건 절차 (Task 2 5번 항목) 요약.
  </action>
  <verify>
    <automated>cd backend && python3 -c "import ast; [ast.parse(open('evals/phase29/'+m).read()) for m in ('run_sweep.py','assert_gates.py')]" && python3 -c "import json; json.load(open('evals/phase29/eval_keys.json'))" && PYTHONPATH=shared/python:. python3 evals/phase29/assert_gates.py</automated>
  </verify>
  <acceptance_criteria>
    - run_sweep.py / assert_gates.py 파싱 통과 + eval_keys.json 유효 JSON (6키, mode3 표기)
    - 로컬(artifact 없음)에서 assert_gates.py 가 SKIPPED 로 exit 0 (부재 ≠ 실패 규칙)
    - `grep -n "setdefault" backend/evals/phase29/run_sweep.py` 에 RTMW_DETERMINISTIC / GEMINI_VISION_VETO_ENABLED / GEMINI_MAX_VETO_WALL_S 3종 존재
    - assert_gates.py 에 점수 리터럴 비교(예: == 88, <= 75 류) 0 — baseline artifact 대비 방향 비교만 (100/0 은 항등식 상수로만 허용)
    - baseline/ 디렉터리 쓰기 코드 0 (`grep -n "baseline" evals/phase29/*.py` 검토 — read-only)
  </acceptance_criteria>
  <done>phase29 하네스가 phase25 규율(SERIAL·EVAL_OUT_DIR·baseline read-only·cold/warm)을 승계하고 로컬 구문·SKIPPED 검증 통과.</done>
</task>

<task type="auto">
  <name>Task 2: Pod sweep 실행 — cold/warm/게이트 + score-switch 가시성 → PASS 시 서버 재기동</name>
  <files>backend/evals/phase29/README.md</files>
  <read_first>
    - backend/evals/phase29/README.md (Task 1 산출 — 실행 블록)
    - backend/evals/phase25/README.md (Pod 실행 선례 — env·경로)
    - .planning/phases/29-mode3-result-screen-completion/29-RESEARCH.md (§Environment Availability — Pod s7gyvvlc6u7ktz, SSH root@213.173.102.162:26448, Gemini 크레딧 확인 항목)
  </read_first>
  <action>
    Pod 운영은 Claude 가 직접 실행한다 (pod-ops-claude-runs). 순서:

    1. **push 먼저** (gsd-pod-work-push-first): 29-02/29-03/29-05 커밋이 origin/main 에 있는지 확인 후 미push 분 push.
    2. **Gemini 크레딧 확인** (고갈 이력 2026-06-20): recognizer 1회×6키×2run 분량 잔액 확인 — 고갈이면 정지·보고 (sweep 강행 금지).
    3. SSH Pod (root@213.173.102.162 -p 26448): `cd /workspace/SunityMotion && git pull` 로 신코드 반영 (서버 프로세스는 아직 구코드로 서빙 중 — in-process sweep 은 새 코드 사용).
    4. SERIAL sweep: `cd backend && PYTHONPATH=shared/python:. python3 evals/phase29/run_sweep.py` (cold) → 동일 커맨드 `--tag warm` → `python3 evals/phase29/assert_gates.py`. 동시 실행 절대 금지 (파이프라인 동시성 비안전).
    5. **score-switch 가시성 표 + 정지 조건 (29-PLAN-REVIEW MEDIUM-1):** assert 후 PASS/FAIL 무관하게 29-05-SUMMARY.md 에 키별 대조표를 작성한다 — 컬럼: 키 / 기존 baseline overallScore / 신 tally.final / records 수 / criterion id 목록 / coverage·fallback 상태 / visionVeto.status / cold==warm. **정지 조건:** clean(무감점) doc 의 final 이 단일 협소 criterion 통과 근거만으로 기존 대비 대폭 상승(예: 기존 80대 → 100)하는 키가 있으면 — 게이트 PASS 여도 — Pod 재기동을 보류하고 **checkpoint:decision 으로 belle 에 즉시 제시** (결정형 체크포인트 = 즉시 호출, batch-UAT 원칙 대상 아님): "criterion-clean = 100 수용" 여부는 제품 결정이다. belle 가시 승인 후에만 6번(재기동) 진행하고, 승인 문구·표를 SUMMARY 에 박제. 무음 ship 금지.
    6. **PASS (exit 0, 정지 조건 미발동 또는 belle 승인 후):** Pod 서버 재기동 — start_server.sh 사용 (VETO 함정 영구 fix 경로, 임의 uvicorn 직기동 금지) → `/health` ok 확인 → production 이 신코드(D-01/D-02/D-08) 서빙.
    7. **FAIL (exit 1):** 정지. 임계·criteria·게이트 기대치 수정 금지 (calibration-source-hard-gate + Pitfall 1 warning signs). 서버 재기동 하지 않음 (구코드 유지 = 무회귀). FAIL 키·값·원인 가설을 SUMMARY 에 기록하고 blocker 로 보고 — belle 결정 대기.
    8. sweep 중 mode3 zoom 산출(D-08)도 함께 확인: fault 키의 faultZoomComparisons kind 에 improved 0 / 감점 record criterion→region 매핑과 일치(29-03) — 게이트 외 관찰 기록.
  </action>
  <verify>
    <automated>ssh -p 26448 root@213.173.102.162 "cd /workspace/SunityMotion/backend && PYTHONPATH=shared/python:. python3 evals/phase29/assert_gates.py"; test $? -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - Pod 에서 cold sweep + warm sweep + assert_gates 전부 실행 (SERIAL — 실행 로그에 동시 실행 없음)
    - assert_gates exit 0 (PASS) — 또는 FAIL 시 blocker 보고 + 서버 구코드 유지 + 재보정 0 (이 경우 plan 은 미완, gap 으로 반환)
    - SUMMARY 키별 대조표에 기존 overallScore / 신 tally.final / records 수 / criterion id 목록 / coverage·fallback 상태 / visionVeto.status / cold==warm 컬럼 전부 존재 (29-PLAN-REVIEW MEDIUM-1)
    - 정지 조건 판정 결과가 SUMMARY 에 명시 (발동 시: Pod 재기동 보류 + belle 결정 기록 존재 / 미발동 시: 근거 1줄) — 무음 ship 0
    - PASS 시: Pod `/health` ok + 서버가 신코드 커밋 해시로 서빙 (pull 해시 == origin/main HEAD)
    - repo 내 baseline/ 파일 diff 0 (오염 없음)
  </acceptance_criteria>
  <done>D-02 전환 조건 충족: sweep 게이트 PASS + score-switch 가시성 표 박제 + (정지 조건 발동 시 belle 승인 후) Pod production 신코드 서빙. FAIL 이면 정지·보고 (재보정 없음).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로컬 → Pod SSH | 운영자 키로 원격 실행 — 크리덴셜·env 노출 주의 |
| sweep → S3/Firestore | 실영상 다운로드 + 결과 기록 경로 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-05-01 | Information Disclosure | Pod env (AWS 키·Gemini 키·SA JSON) | mitigate | 로그·SUMMARY 에 env 값 출력 금지 — key=value 로그는 uid/키명만, 시크릿 값 0 |
| T-29-05-02 | Tampering | baseline 오염 (자기 sweep 재보정) | mitigate | EVAL_OUT_DIR 밖 기록 금지 + baseline read-only + FAIL 시 임계 수정 금지 게이트 |
| T-29-05-03 | Denial of Service | sweep 동시 실행 파이프라인 오염 | mitigate | SERIAL 강제 (README 명시 + 실행 절차 단일 커맨드 순차) |
| T-29-05-04 | Tampering | 미검증 코드 production 노출 | mitigate | 서버 재기동을 게이트 PASS 뒤에만 — FAIL 시 구코드 유지 + score-switch 정지 조건 시 belle 승인 전 재기동 보류 (MEDIUM-1) |
| T-29-05-SC | Tampering | 패키지 설치 | accept | 이 plan 은 패키지 설치 0 |
</threat_model>

<verification>
- Pod assert_gates exit 0 (cold/warm 완주 후)
- SUMMARY score-switch 대조표 + 정지 조건 판정 명시 (MEDIUM-1)
- backend full suite 는 wave merge 시 실행 (29-VALIDATION sampling rate): `cd backend && PYTHONPATH=shared/python:. python3 -m pytest tests/ -q` 신규 실패 0 (기존 54 failures 알려짐)
</verification>

<success_criteria>
- D-02 전환 조건 충족 — sweep 5게이트 PASS, production 전환 완료
- score-switch 가시성: 키별 old/new 대조표 박제 + clean 대폭 상승 시 belle 결정 경유 (무음 ship 0)
- 게이트 FAIL 시나리오에서 재보정 0·정지·보고 프로토콜 준수
</success_criteria>

<output>
Create `.planning/phases/29-mode3-result-screen-completion/29-05-SUMMARY.md` when done
</output>
