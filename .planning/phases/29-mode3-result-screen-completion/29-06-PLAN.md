---
phase: 29-mode3-result-screen-completion
plan: 06
type: execute
wave: 3
depends_on: ["29-04"]
files_modified:
  - backend/functions/playback-url/app.py
  - backend/tests/test_playback_url_reference.py
  - backend/template.yaml
  - app/src/lib/api.ts
  - app/src/app/analysis/result.tsx
  - docs/contract.md
autonomous: true
requirements: [D-09]

must_haves:
  truths:
    - "D1(Mode1 비교영상 안 뜸)의 근본 원인이 재현으로 확정되고 SUMMARY 에 증거와 함께 기록된다 — 선판단 금지, 재현→규명→fix (D-09)"
    - "fix 후: 7일+ 경과 mode1 doc 에서도 우측(정은지) 비교영상이 재생 가능하다"
    - "reference 재서명은 referenceMotionId → Firestore canonical `reference/{id}` doc 의 videoS3Key 화이트리스트 경유만 — 클라이언트가 임의 S3 키를 서명시킬 수 없고, inactive(isActive === false) doc·`reference/` prefix 밖 키는 404 로 차단된다 (ASVS V4 + 29-PLAN-REVIEW HIGH-2)"
    - "presigned TTL 은 7일 유지 — TTL 연장으로 풀지 않는다 (재발급 경로가 정답, RESEARCH 위협 패턴)"
  artifacts:
    - path: "backend/functions/playback-url/app.py"
      provides: "referenceMotionId 재서명 확장 (진단이 TTL 확정 시) — models.REFERENCE_MOTIONS_COLLECTION 경유 + isActive/prefix 가드"
    - path: "backend/tests/test_playback_url_reference.py"
      provides: "확장 핸들러 unit 테스트 (mock) — inactive 404·prefix 거부 포함"
      min_lines: 40
    - path: "app/src/app/analysis/result.tsx"
      provides: "mode1 rightUrl 재발급 훅 (mode3 prev 훅 미러)"
  key_links:
    - from: "app/src/app/analysis/result.tsx"
      to: "POST /playback-url"
      via: "api.ts 재발급 호출 (mode1 reference)"
      pattern: "playback-url|PlaybackUrl"
    - from: "backend/functions/playback-url/app.py"
      to: "Firestore reference doc videoS3Key"
      via: "models.reference_motion_path 화이트리스트 lookup + isActive/prefix 가드"
      pattern: "referenceMotionId|videoS3Key"
---

<objective>
D-09 — D1(파일럿 신고: Mode1 비교영상 안 뜸) 진단 태스크. 재현 → 원인 규명 → fix 를 이 순서로 수행한다. Phase 28 변경 연관 가능성도 점검한다.

**최유력 가설 (RESEARCH Pitfall 4, MEDIUM 확신 — 진단이 확정한다):** `result.referenceVideoUrl` 은 분석 시점 서명(_PLAYBACK_EXPIRES=7일, app.py:145)이고 reference videoS3Key 재서명 경로가 전무 — 7일 지난 doc 은 우측 영상이 확정적으로 죽는다. 폴백 refMotion.videoUrl 도 시드 시점 서명이라 사실상 항상 만료.

**Task 2 는 조건부:** TTL 가설이 확정되면 명세대로 fix. 다른 원인(videoS3Key 부재, Phase 28 워핑 회귀, expo-video 무음 실패)이 확정되면 그 원인을 이 phase 범위에서 fix 하되, 범위를 넘으면 blocker 로 보고 — 선판단 금지 (D-09 문언 그대로).

**리뷰 반영 (29-PLAN-REVIEW HIGH-2):** (a) canonical 컬렉션은 `reference` 다 (models.REFERENCE_MOTIONS_COLLECTION, models.py:443) — 구 plan 의 `reference_motions/{id}` 표기는 컬렉션 드리프트로 mock 테스트가 통과해도 production 조회가 실패한다. 리터럴 컬렉션 문자열 금지. (b) get_reference_motion 은 isActive 무필터로 아무 doc 이나 반환하고(firestore_admin.py:1522), 앱은 isActive === false 를 클라이언트에서 숨긴다(referenceMotions.ts normalize) — auto-registration 이 reject/G3 경로에서 inactive doc 을 실제로 만들므로, /playback-url 이 무가드로 서명하면 미승인 reference 영상이 id 추측만으로 노출된다 (EoP). isActive + videoS3Key prefix 가드가 필수 경계다.

Purpose: 파일럿 gap D1 해소 + Mode1 신뢰 회복 (비교영상은 Mode1 의 핵심 증거 화면).
Output: 진단 증거 + fix + 보안 경계 유지 (uid-scoped + reference 화이트리스트 + active/prefix 가드).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-mode3-result-screen-completion/29-CONTEXT.md
@.planning/phases/29-mode3-result-screen-completion/29-RESEARCH.md
@backend/functions/playback-url/app.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: D1 재현·규명 — Pitfall 4 체크리스트 실행</name>
  <files></files>
  <read_first>
    - .planning/phases/29-mode3-result-screen-completion/29-RESEARCH.md (§Pitfall 4 — 유력 순 원인 5개 + 진단 설계)
    - app/src/app/analysis/result.tsx (:1388-1396 rightUrl 조립 — referenceVideoUrl || refMotion?.videoUrl 폴백, :683-706 mode3 prev 재발급 훅 선례)
    - backend/functions/pipeline/app.py (:145 _PLAYBACK_EXPIRES, :1258/:3806 _signed_get referenceVideoUrl 서명 지점)
    - .planning/PILOT-FEEDBACK-2026-07-06.md (§D D1 원문 — 신고 정황)
  </read_first>
  <action>
    코드 변경 없이 진단만 수행한다 (읽기 + 원격 조회). 체크리스트 (유력 순):

    1. **TTL 분리 실험:** Firebase Admin(로컬 스크립트, firebase-sa.json + --profile sunity-motion)으로 mode1 analyses doc 2건 조회 — (a) 7일+ 경과 doc, (b) 최신 doc. 각 doc 의 result.referenceVideoUrl 을 curl HEAD 로 검사: (a)가 403(AccessDenied/expired) AND (b)가 200 이면 TTL 확정.
    2. refMotion 폴백 검사: Firestore reference doc 의 videoUrl 도 curl HEAD — 시드 시점 서명 만료 여부 확인 (프로젝트 메모리 선례상 만료 예상).
    3. videoS3Key 부재 케이스: reference doc 에 videoS3Key 필드 존재 확인 (부재면 referenceVideoUrl 자체 미방출 경로). 이때 videoS3Key 값의 prefix 도 함께 기록 (`reference/` 시작 여부 — Task 2 prefix 가드 설계 입력).
    4. Phase 28 워핑 회귀(낮음): 신선한 doc 의 motionAlignment 정상 + 앱 normalizeMotionAlignment 폴백 경로 검토 — "안 뜸" vs "이상 재생" 구분.
    5. 진단 결과를 SUMMARY 에 기록: 확정 원인, 증거(HTTP 상태·doc 나이·필드 유무), fix 방향 결정 (TTL 이면 Task 2 명세 그대로 / 다른 원인이면 Task 2 를 그 원인 fix 로 대체하고 deviation 기록 / 범위 초과면 blocker 보고).

    주의: presigned URL 전문을 SUMMARY/로그에 남기지 않는다 (서명 파라미터 = 자격 정보) — 상태코드·만료시각만 기록.
  </action>
  <verify>
    <automated>aws s3 ls s3://sunity-motion-pilot-videos --profile sunity-motion | head -1</automated>
  </verify>
  <acceptance_criteria>
    - 신선 doc vs 7일+ doc 의 referenceVideoUrl HTTP 상태가 실측 기록됨 (양쪽 값 명시)
    - refMotion.videoUrl 만료 여부 실측 기록
    - reference doc 의 videoS3Key prefix 실측 기록 (Task 2 allowlist 설계 입력)
    - 근본 원인 1개가 증거와 함께 확정 선언됨 ("추정" 아님) — 또는 재현 불가 시 그 사실과 관찰 조건 기록
    - SUMMARY 에 presigned URL 전문 0 (상태코드/만료시각만)
    - 코드 diff 0 (진단 전용 태스크)
  </acceptance_criteria>
  <done>D1 근본 원인 확정 + fix 방향 결정 — 재현→규명 완료 (D-09 전반부).</done>
</task>

<task type="auto">
  <name>Task 2: fix — /playback-url reference 재서명 확장 + 앱 mode1 재발급 훅 (진단 결과 조건부)</name>
  <files>backend/functions/playback-url/app.py, backend/tests/test_playback_url_reference.py, backend/template.yaml, app/src/lib/api.ts, app/src/app/analysis/result.tsx, docs/contract.md</files>
  <read_first>
    - backend/functions/playback-url/app.py (전체 69줄 — 현행 uid-scoped analysisId 서명, ext 화이트리스트, 에러 패턴)
    - backend/shared/python/sunity_shared/models.py (:436-443 reference_motion_path / REFERENCE_MOTIONS_COLLECTION = "reference" — canonical, 리터럴 금지 근거)
    - backend/shared/python/sunity_shared/firestore_admin.py (:1522-1529 get_reference_motion — isActive 무필터 반환 확인, :1406-1472 auto-registration inactive doc 생성 경로)
    - app/src/lib/referenceMotions.ts (:70-78 normalize — 앱의 isActive === false 숨김 = product/보안 상태 근거)
    - backend/template.yaml (:155-180 playback-url 함수 정의 — env/policy 에 FIREBASE_SA_PARAM 존재 여부 확인)
    - app/src/lib/api.ts (requestPlaybackUrl — 확장 원본)
    - app/src/app/analysis/result.tsx (:683-706 mode3 prev 재발급 훅 — SAFE_TTL_MS 6일 margin 패턴, 미러 대상)
    - docs/contract.md (playback-url 절 — 요청/응답 계약)
  </read_first>
  <action>
    Task 1 이 TTL(또는 refMotion 만료) 를 확정한 경우의 fix 명세 (다른 원인 확정 시 이 태스크를 해당 fix 로 대체하고 SUMMARY deviation 에 기록):

    백엔드 (backend/functions/playback-url/app.py):
    1. body 에 `referenceMotionId`(string) 를 analysisId 와 **상호 배타** 로 수용. referenceMotionId 경로: canonical `reference/{id}` doc 조회 — **models.REFERENCE_MOTIONS_COLLECTION / models.reference_motion_path 사용 필수, 리터럴 컬렉션 문자열("reference_motions" 등) 금지** (29-PLAN-REVIEW HIGH-2 컬렉션 드리프트 차단). firestore_admin.get_reference_motion 재사용. **경계 가드 4종 전부 통과해야 서명** (하나라도 실패 → 동일 404 not_found — inactive/부재/무영상 케이스가 응답으로 구분되지 않게, 숨김 doc 존재 leak 0):
       - (a) doc 존재
       - (b) `doc.get("isActive") is not False` — 앱 normalize 가 숨기는 미승인/reject/G3 자동등록 doc 은 재서명도 거부 (EoP 차단)
       - (c) `videoS3Key` 존재
       - (d) `videoS3Key.startswith("reference/")` — allowlist prefix (Task 1 에서 실측한 실제 prefix 로 확정; `reference/` 밖 키가 doc 에 들어 있어도 서명 거부)
    2. 인증(verify_request uid) 은 기존 그대로 필수 — reference 는 공용 자산이나 인증 사용자만 재서명 가능. analysisId 경로는 byte-불변 (uid-scoped build_upload_key 유지).
    3. _PLAYBACK_EXPIRES 7일 유지 — 연장 금지.
    4. referenceMotionId 형식 검증 (영숫자·하이픈 화이트리스트, path injection 방지 — 기존 analysisId 검증 스타일).
    5. template.yaml: playback-url 함수에 FIREBASE_SA_PARAM env/SSM 정책이 없으면 추가 (upload-url 함수 정의 참조). 변경 시 `sam build --use-container && sam deploy` (samconfig.toml 기본값, stack sunity-motion-pilot). **배포 순서 주의 (wave 3 에서 29-05 sweep 와 병행):** `sam deploy` 는 pipeline Lambda 도 29-02/03 코드로 함께 재배포하지만, production 채점 경로는 RunPod 위임(Lambda pipeline 은 delegation-only)이라 미검증 채점 코드가 노출되지 않는다 — Pod 는 29-05 게이트 PASS 전까지 구코드 서빙. 그래도 배포는 가능하면 29-05 Task 2 PASS 이후로 순서화하고, 먼저 배포한 경우 이 RunPod-위임 근거를 SUMMARY 에 기록한다.
    6. unit 테스트 (backend/tests/test_playback_url_reference.py, mock 기반) — 케이스 (29-PLAN-REVIEW HIGH-2 확장분 포함):
       - referenceMotionId 정상 재서명 (active doc + reference/ 키 → 200)
       - doc 부재 → 404
       - **inactive doc (isActive === False) → 404** (정상 케이스와 응답 구분 불가 확인)
       - **videoS3Key 가 `reference/` prefix 아님 (예: `uploads/...`) → 404 (서명 거부)**
       - videoS3Key 필드 부재 → 404
       - analysisId·referenceMotionId 동시 제공 → 400
       - 임의 S3 키 파라미터 무시 (요청 body 에 s3Key 류 키를 넣어도 서명에 미반영)
       - 기존 analysisId 경로 무회귀 (요청/응답 byte-호환)

    앱:
    7. api.ts: requestPlaybackUrl 을 referenceMotionId 지원으로 확장 (또는 requestReferencePlaybackUrl 신설 — 기존 함수 시그니처 호환 유지 쪽 선택).
    8. result.tsx: mode1 rightUrl 재발급 훅 — mode3 prev 훅(:683-706) 미러. result.createdAt 기준 SAFE_TTL_MS(6일 margin) 초과 시 referenceMotionId 로 재발급 → freshRefUrl state → rightUrl 조립(:1394-1396)에서 최우선 소비. 실패 시 기존 폴백 체인 유지 (__DEV__ warn 만).
    9. docs/contract.md: playback-url 절에 referenceMotionId 요청 변형 서술 추가 — 가드 4종(존재·active·videoS3Key·prefix)과 404 통합 응답 포함 (응답 스키마 불변 — playbackUrl/expiresInSec).
    10. 스펙 인용 주석: `// 29-CONTEXT D-09 — D1 fix (진단: <확정 원인>)` + `# 29-PLAN-REVIEW HIGH-2 — isActive/prefix 가드`.

    배포 후 스모크: 실제 referenceMotionId 1건으로 curl POST (유효 Firebase 토큰) → 200 + 재생 가능 URL (HEAD 200).
  </action>
  <verify>
    <automated>cd backend && PYTHONPATH=shared/python:. python3 -m pytest tests/test_playback_url_reference.py -q && cd ../app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_playback_url_reference.py -q` 전 케이스 PASS (임의 S3 키 서명 불가 + inactive 404 + prefix 거부 케이스 포함 — 보안 게이트)
    - `npm run typecheck` exit 0
    - 핸들러가 canonical 경로 헬퍼 사용: `grep -n "REFERENCE_MOTIONS_COLLECTION\|reference_motion_path\|get_reference_motion" backend/functions/playback-url/app.py` 매치 ≥ 1 AND `grep -n "reference_motions" backend/functions/playback-url/app.py` 매치 0 (리터럴 드리프트 0)
    - playback-url 핸들러에 클라이언트 S3 키 직접 수용 코드 0 (`grep -n "s3Key\|key.*body" backend/functions/playback-url/app.py` 검토 — build_upload_key/doc lookup 경유만)
    - isActive 가드 + startswith prefix 가드가 핸들러에 존재 (`grep -n "isActive" backend/functions/playback-url/app.py` ≥ 1)
    - _PLAYBACK_EXPIRES 값 diff 0 (7일 유지)
    - SAM deploy 성공 (변경 시) + 스모크 curl 200 기록
    - 기존 analysisId 경로 요청/응답 byte-호환 (mode3 prev 재발급 무회귀)
  </acceptance_criteria>
  <done>7일+ 경과 mode1 doc 에서 우측 비교영상 재생 가능 — D1 해소, 보안 경계(화이트리스트·uid 인증·active/prefix 가드·TTL 7일) 유지.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 앱 → POST /playback-url | 인증된 클라이언트 입력이 S3 서명 파라미터에 도달 — 이 phase 의 최대 보안 표면 |
| Lambda → S3 presign | 서명 권한 보유 코드 — 서명 대상 통제가 핵심 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-06-01 | Elevation of Privilege | /playback-url 임의 키 서명 | mitigate (HIGH — 차단 필수) | referenceMotionId → Firestore doc lookup 화이트리스트만 서명. 클라이언트 S3 키 직접 수용 금지 + unit 테스트로 고정 |
| T-29-06-02 | Information Disclosure | presigned URL 과다 TTL | mitigate | 7일 TTL 유지 (연장 금지) — 재발급 경로가 해법 |
| T-29-06-03 | Spoofing | 미인증 재서명 요청 | mitigate | verify_request(Firebase ID 토큰) 필수 유지 — 401 경로 무변경 |
| T-29-06-04 | Tampering | referenceMotionId path injection | mitigate | 형식 화이트리스트 검증 (기존 analysisId 검증 스타일) |
| T-29-06-05 | Information Disclosure | 진단 중 URL 전문 로그 유출 | mitigate | SUMMARY/로그에 presigned URL 전문 금지 — 상태코드·만료시각만 |
| T-29-06-06 | Elevation of Privilege | inactive/미승인 reference doc 재서명 우회 (id 추측으로 앱 active 필터 우회 — 29-PLAN-REVIEW HIGH-2) | mitigate | `isActive is not False` 가드 + `videoS3Key.startswith("reference/")` prefix 가드 → 전 실패 케이스 동일 404 (부재와 구분 불가, 숨김 doc 존재 leak 0) + unit 테스트 고정 |
| T-29-06-SC | Tampering | 패키지 설치 | accept | 이 plan 은 패키지 설치 0 |
</threat_model>

<verification>
- backend: 신규 테스트 PASS + `pytest tests/ -q` 신규 실패 0
- app: typecheck exit 0
- 실기기 D1 재확인(옛 doc 비교영상 재생)은 29-08 HUMAN-UAT.md 적립
</verification>

<success_criteria>
- D-09 완결: 재현 증거 → 원인 확정 → fix → 스모크 — 전 단계 SUMMARY 기록
- 보안 불변: 임의 키 서명 불가, inactive/비-prefix 키 404, uid 인증 유지, TTL 7일 유지
</success_criteria>

<output>
Create `.planning/phases/29-mode3-result-screen-completion/29-06-SUMMARY.md` when done
</output>
