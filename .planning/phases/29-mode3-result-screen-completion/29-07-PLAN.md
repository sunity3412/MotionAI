---
phase: 29-mode3-result-screen-completion
plan: 07
type: execute
wave: 3
depends_on: ["29-04"]
files_modified:
  - app/package.json
  - app/package-lock.json
  - app/app.json
  - app/src/components/VideoCompare.tsx
autonomous: true
requirements: [D-11, D-12]

must_haves:
  truths:
    - "새 빌드: 전체화면 비교 뷰어 진입 시 진짜 가로(LANDSCAPE) 전환, 닫으면 세로 복귀 — 앱 전체는 세로 고정 유지 (D-11)"
    - "구빌드(TestFlight 27): 같은 OTA 번들 로드 시 크래시 0 — 정적 import 0 + requireOptionalNativeModule 감지 + 90° 회전 핵 폴백 유지 (D-12)"
    - "expo-screen-orientation 은 SDK 54 번들 ~9.0.9 로 고정 설치 (latest 57.0.0 금지)"
    - "app.json version(1.0.0)/runtimeVersion policy 무변경 — 구빌드와 OTA 채널 공유 유지"
    - "FULLSCREEN_ZOOM=1.35 (belle 승인값) 무변경"
  artifacts:
    - path: "app/package.json"
      provides: "expo-screen-orientation ~9.0.9 의존성"
      contains: "expo-screen-orientation"
    - path: "app/app.json"
      provides: "expo-screen-orientation plugin 등록"
      contains: "expo-screen-orientation"
    - path: "app/src/components/VideoCompare.tsx"
      provides: "런타임 네이티브 감지 + 진짜 가로/회전 핵 분기"
      contains: "requireOptionalNativeModule"
  key_links:
    - from: "app/src/components/VideoCompare.tsx"
      to: "expo-screen-orientation"
      via: "함수 스코프 lazy require (정적 import 금지)"
      pattern: "require\\('expo-screen-orientation'\\)"
    - from: "app/src/components/VideoCompare.tsx openFullscreen/closeFullscreen"
      to: "lockAsync LANDSCAPE_RIGHT / PORTRAIT_UP"
      via: "네이티브 가용 분기"
      pattern: "lockAsync"
---

<objective>
D-11/D-12 — 전체화면 비교 뷰어의 진짜 가로 전환. D4(비율 이상)는 회전 핵 치수 계산 소멸로 근본 해소된다.

**치명 함정 (Pitfall 3, 절대 준수):** expo-screen-orientation 9.0.9 는 모듈 최상위에서 requireNativeModule 을 실행한다 — 파일 상단 정적 `import 'expo-screen-orientation'` 이 포함된 OTA 번들은 빌드 27 에서 번들 평가 시점에 앱 전체가 크래시한다 (가로 기능을 안 써도). requireOptionalNativeModule 감지 + 함수 스코프 lazy require 만 허용.

**패키지 정당성:** RESEARCH Package Legitimacy Audit — expo-screen-orientation = Expo 공식 모노레포, slopcheck [OK], Disposition Approved. 인간 확인 checkpoint 불요. 단 `npx expo install` 로 ~9.0.9 고정 (npm latest 57.0.0 = SDK 54 비호환).

Purpose: 파일럿 gap D4 근본 해소 + 시나리오 6단계 비교 UX. 네이티브 모듈 — 29-08 EAS 빌드에 동승.
Output: 설치 + plugin + VideoCompare 분기. OTA-safe wave(29-01~06) 완결 후 네이티브 wave (RESEARCH sequencing).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-mode3-result-screen-completion/29-CONTEXT.md
@.planning/phases/29-mode3-result-screen-completion/29-RESEARCH.md
@.planning/phases/29-mode3-result-screen-completion/29-PATTERNS.md
@app/src/components/VideoCompare.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: expo-screen-orientation 설치 + app.json plugin (버전·런타임 불변)</name>
  <files>app/package.json, app/package-lock.json, app/app.json</files>
  <read_first>
    - app/package.json (현행 deps — react-native-screens ~4.16.0 확인, 업그레이드 금지 대상)
    - app/app.json (plugins 배열 :44-57, version 1.0.0, runtimeVersion policy appVersion — 불변 대상)
    - .planning/phases/29-mode3-result-screen-completion/29-RESEARCH.md (§Standard Stack + §Package Legitimacy Audit)
  </read_first>
  <action>
    1. `cd app && npx expo install expo-screen-orientation` — SDK 54 번들 ~9.0.9 고정 설치. `npm install expo-screen-orientation@latest` 절대 금지 (57.0.0 = SDK 정렬 버저닝, 비호환).
    2. app.json plugins 배열에 `"expo-screen-orientation"` 문자열 항목 추가 (expo-mail-composer 등록 선례 :56 과 동일 — 옵션 불필요, initialOrientation 미지정).
    3. **version(1.0.0)·runtimeVersion policy(appVersion) 절대 무변경** — bump 시 구빌드(27)와 OTA 채널이 갈라진다 (D-12 요구 위반). autoIncrement 는 buildNumber 만 올리므로 기본 설정 유지가 정답 (appVersionSource=remote).
    4. react-native-screens ~4.16.0 유지 — ≥4.23 은 iOS orientation lock 무력화 swizzle (expo/expo#43802). lockfile diff 에서 rns 버전 변동 없는지 확인.
    5. package-lock.json diff 검토: expo-screen-orientation 및 그 직접 의존성 외 변동 최소 확인 (공급망 lockfile diff 게이트, A5).
  </action>
  <verify>
    <automated>cd app && node -e "const p=require('./package.json'); const v=p.dependencies['expo-screen-orientation']; if(!v||!v.includes('9.0')) process.exit(1)" && node -e "const a=require('./app.json'); if(!JSON.stringify(a.expo.plugins).includes('expo-screen-orientation')) process.exit(1); if(a.expo.version!=='1.0.0') process.exit(1)" && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - package.json 의 expo-screen-orientation 버전 문자열이 9.0.x 대 (~9.0.9)
    - app.json plugins 에 "expo-screen-orientation" 존재, expo.version === "1.0.0" 유지, runtimeVersion 블록 diff 0
    - package.json 의 react-native-screens 버전 문자열 diff 0 (~4.16.0)
    - `npm run typecheck` exit 0
  </acceptance_criteria>
  <done>~9.0.9 고정 설치 + plugin 등록, 버전/런타임/rns 불변 — OTA 채널 공유 유지.</done>
</task>

<task type="auto">
  <name>Task 2: VideoCompare 가로 전환 분기 — 런타임 감지 + 회전 핵 폴백</name>
  <files>app/src/components/VideoCompare.tsx</files>
  <read_first>
    - app/src/components/VideoCompare.tsx (전체 — open/close :268-292, 전체화면 Modal :954-994, fsRotated 치수 :1223-1229, VIDEO_ASPECT :195, FULLSCREEN_ZOOM :202)
    - .planning/phases/29-mode3-result-screen-completion/29-PATTERNS.md (§VideoCompare D-11/D-12 — 감지 코드 검증본 + 회전 핵 원문 + Modal supportedOrientations 주의)
    - .planning/phases/29-mode3-result-screen-completion/29-RESEARCH.md (§Pattern 3/4 — lazy require 안전 패턴 + 진입/이탈 시퀀스 + Pitfall 5)
  </read_first>
  <action>
    29-CONTEXT D-11/D-12 구현. PATTERNS §VideoCompare 의 검증본(expo-modules-core 3.0.29 실측) 을 그대로 사용:

    1. **감지:** 파일 상단에서 `requireOptionalNativeModule('ExpoScreenOrientation') != null` (expo-modules-core import — 기설치 3.0.29) 로 module-level const `hasNativeOrientation` 산출. **`import ... from 'expo-screen-orientation'` 정적 import 절대 금지** — 사용 지점마다 함수 스코프 `require('expo-screen-orientation')` (Metro 는 require 호출 시점 평가 → 구빌드에서 해당 함수 미호출이면 안전).
    2. **진입 시퀀스 (진짜 가로 분기):** openFullscreen → setFullscreen(true) → Modal 마운트 후 lockAsync(OrientationLock.LANDSCAPE_RIGHT). Modal 에 supportedOrientations={['portrait','landscape']} (네이티브 분기에서 — RN Modal 자체 orientation 허용 목록 필요, Pattern 4).
    3. **이탈 시퀀스:** lockAsync(PORTRAIT_UP) → setFullscreen(false) (역순이면 세로 복귀 전 Modal 닫혀 flicker). PORTRAIT_UP lock 후 unlock 여부는 재량 — 앱 전체 세로 고정(D-11) 유지가 기준. onRequestClose(Android back)에도 동일 시퀀스.
    4. **치수:** 진짜 가로 분기에서는 fsRotated transform(rotate 90deg)과 short/long 축 스왑 치수 계산을 생략하고 useWindowDimensions 값을 그대로 사용 (가로 상태의 window 치수). 폴백 분기(hasNativeOrientation === false)는 현행 회전 핵 코드 byte-보존 — 핵 제거는 deferred (파일럿 이후).
    5. FULLSCREEN_ZOOM=1.35 무변경 (belle 승인값 — 변경 시 근거 필수인데 이 plan 은 근거 없음 → 유지).
    6. 오버레이(KeypointOverlay)·라벨 배치가 양 분기에서 치수 파생을 올바르게 받도록 분기 변수 단일화 (기존 fsBoxH/fsBoxW 파생을 분기별로 산출).
    7. 주석: `// 29-CONTEXT D-11/D-12 — 진짜 가로 + 구빌드(27) 폴백. 정적 import = OTA 크래시 (RESEARCH Pitfall 3)`.
    8. 실기기 확인 항목(가로 전환·복귀·flicker·iPad 관찰·구빌드 무크래시 — A1/A2 가정 판정)은 29-08 이 HUMAN-UAT.md 로 적립 — 이 plan 은 SUMMARY 에 항목 후보 기록.
  </action>
  <verify>
    <automated>cd app && npm run typecheck && ! grep -n "^import.*expo-screen-orientation\|from 'expo-screen-orientation'" src/components/VideoCompare.tsx && grep -c "requireOptionalNativeModule" src/components/VideoCompare.tsx && grep -c "FULLSCREEN_ZOOM = 1.35" src/components/VideoCompare.tsx</automated>
  </verify>
  <acceptance_criteria>
    - VideoCompare.tsx 에 expo-screen-orientation 정적 import 0 (`grep "from 'expo-screen-orientation'"` 매치 0) — 크래시 게이트
    - `requireOptionalNativeModule('ExpoScreenOrientation')` 감지 존재 + lazy `require('expo-screen-orientation')` 는 함수 스코프에만
    - lockAsync 호출이 LANDSCAPE_RIGHT(진입)·PORTRAIT_UP(이탈) 양쪽 존재
    - Modal supportedOrientations 에 'landscape' 포함 (네이티브 분기)
    - 폴백 분기의 fsRotated transform 코드 보존 (`grep -c "rotate" src/components/VideoCompare.tsx` ≥ 현행)
    - FULLSCREEN_ZOOM === 1.35 유지
    - `npm run typecheck` exit 0
  </acceptance_criteria>
  <done>새 빌드 = 진짜 가로, 구빌드 = 회전 핵 폴백 + 크래시 0 — 런타임 분기 완성.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → 앱 의존성 | 이 phase 유일의 패키지 설치 지점 |
| OTA 번들 → 구빌드 런타임 | 잘못된 import 하나가 전 사용자 크래시 (가용성) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-07-SC | Tampering | expo-screen-orientation npm 설치 | mitigate | RESEARCH 감사 [OK]/Approved (Expo 공식 모노레포) + `npx expo install` 버전 고정 + lockfile diff 검토 (rns 포함 무관 패키지 변동 0 확인) |
| T-29-07-01 | Denial of Service | 구빌드 OTA 크래시 (정적 import) | mitigate | lazy require 패턴 + grep 게이트 (acceptance_criteria 고정) + 29-08 HUMAN-UAT 구빌드 항목 |
| T-29-07-02 | Tampering | rns ≥4.23 승급으로 lock 무력화 | mitigate | package.json rns 버전 diff 0 게이트 |
| T-29-07-03 | Denial of Service | 가로 lock 실패 (iPad/기기 설정) | accept | 파일럿 = iPhone 중심, iPad 는 HUMAN-UAT 관찰 항목만 (Pitfall 5, A1/A2 가정 로그) |
</threat_model>

<verification>
- `cd app && npm run typecheck` exit 0 + grep 게이트 3종 (정적 import 0 / 감지 존재 / ZOOM 유지)
- 실기기 회전 검증은 자동화 불가 (29-VALIDATION manual-only) — 29-08 HUMAN-UAT.md 적립
</verification>

<success_criteria>
- D-11: 뷰어 진입 가로/이탈 세로, 앱 전체 세로 고정 유지 (코드 완성 — 실기기는 UAT)
- D-12: 구빌드 폴백 분기 + OTA 크래시 게이트 통과
- ~9.0.9 고정 + version/runtimeVersion/rns 불변
</success_criteria>

<output>
Create `.planning/phases/29-mode3-result-screen-completion/29-07-SUMMARY.md` when done
</output>
