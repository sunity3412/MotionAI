---
phase: 29-mode3-result-screen-completion
plan: 08
type: execute
wave: 4
depends_on: ["29-05", "29-06", "29-07"]
files_modified:
  - .planning/phases/29-mode3-result-screen-completion/29-HUMAN-UAT.md
autonomous: true
requirements: [D-13]

must_haves:
  truths:
    - "backend full suite 신규 실패 0 (기존 54 failures 대비) + app typecheck clean — phase gate"
    - "OTA(production 채널) 발행 — 구빌드(27)에 도달해도 크래시 0 (29-07 lazy require 게이트 전제)"
    - "iOS production 빌드가 TestFlight 에 무인 제출 완료 (ASC 6772934567) + Android APK 빌드 완료 (D-13)"
    - "F1(문의하기): expo-mail-composer 네이티브가 새 빌드에 자동 포함 (plugins 기등록 — 코드 변경 0)"
    - "실기기 확인 항목 전부 29-HUMAN-UAT.md 적립 — 즉시 belle 호출 0 (batch UAT 원칙), 항목별 토큰 grep 으로 누락 0 검증 (29-PLAN-REVIEW MEDIUM-2)"
  artifacts:
    - path: ".planning/phases/29-mode3-result-screen-completion/29-HUMAN-UAT.md"
      provides: "phase 29 실기기 확인 항목 일괄 적립"
      min_lines: 20
  key_links:
    - from: "eas build production (iOS)"
      to: "TestFlight"
      via: "eas submit -p ios --latest --non-interactive"
      pattern: "eas submit"
---

<objective>
D-13 — Phase 29 마감: phase gate → OTA 발행 → 새 EAS 빌드·무인 제출(iOS TestFlight + Android APK) → HUMAN-UAT 적립.

F1(문의하기 안 눌림 = 빌드 27 에 mail-composer 네이티브 부재)은 이 빌드에 동승으로 자동 해소 — package.json ~15.0.8 + app.json plugins 기등록이라 코드 변경 0.

Purpose: 시나리오 gap 전체(29-01~07)의 사용자 도달. OTA 는 구빌드 즉시 개선, 새 빌드는 가로+F1 해소.
Output: OTA + 빌드 2종 + 제출 + 29-HUMAN-UAT.md.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-mode3-result-screen-completion/29-CONTEXT.md
@.planning/phases/29-mode3-result-screen-completion/29-04-SUMMARY.md
@.planning/phases/29-mode3-result-screen-completion/29-06-SUMMARY.md
@.planning/phases/29-mode3-result-screen-completion/29-07-SUMMARY.md
@app/eas.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: phase gate + OTA 발행</name>
  <files></files>
  <read_first>
    - .planning/phases/29-mode3-result-screen-completion/29-VALIDATION.md (phase gate 정의 — full suite + typecheck + sweep 완료 확인)
    - app/src/components/VideoCompare.tsx (29-07 완료본 — 정적 import 0 재확인, OTA 발행 전 최종 게이트)
  </read_first>
  <action>
    1. Phase gate: `cd backend && PYTHONPATH=shared/python:. python3 -m pytest tests/ -q` — 신규 실패 0 (기존 54 pre-existing failures = app-module-name-collision + gemini/knee env, HEAD baseline 과 FAILED/ERROR node-ID diff 로 비교). `cd app && npm run typecheck` exit 0. 29-05 sweep PASS 여부 SUMMARY 확인 (미PASS 면 이 plan 진행 불가 — blocker).
    2. **OTA 발행 전 크래시 게이트 재확인:** `grep -n "from 'expo-screen-orientation'" app/src/components/VideoCompare.tsx` 매치 0 (Pitfall 3 — 이 게이트 실패 시 OTA 발행 절대 금지).
    3. 미커밋 변경 0 확인 후 OTA 발행: `cd app && eas update --channel production --non-interactive` + `eas update --channel preview --non-interactive` (양 채널 선례 260702-q8q). runtime 1.0.0 공유 — 빌드 27 사용자에게 mode3 내역·부상 대응법·D1 fix·라벨이 즉시 도달, 가로 코드는 lazy require 라 무해.
  </action>
  <verify>
    <automated>cd backend && PYTHONPATH=shared/python:. python3 -m pytest tests/ -q --tb=no | tail -3 && cd ../app && npm run typecheck && ! grep -n "from 'expo-screen-orientation'" src/components/VideoCompare.tsx</automated>
  </verify>
  <acceptance_criteria>
    - backend full suite FAILED/ERROR node-ID 목록이 baseline(기존 54) 대비 신규 0
    - typecheck exit 0
    - eas update 출력에 production·preview 채널 update ID 2건 기록 (SUMMARY 에 박제)
    - OTA 발행 커밋 해시 == origin/main HEAD (미커밋 변경 0)
  </acceptance_criteria>
  <done>phase gate GREEN + OTA 양 채널 발행 — 구빌드 즉시 개선분 도달.</done>
</task>

<task type="auto">
  <name>Task 2: EAS 빌드 2종 + iOS 무인 제출 (F1 동승)</name>
  <files></files>
  <read_first>
    - app/eas.json (build 프로필 production / preview-android + submit 설정 — appleId, ascAppId 6772934567, teamId 8ZL3YL358P)
    - app/app.json (plugins 에 expo-mail-composer + expo-screen-orientation 등록 확인 — F1/D-11 네이티브 포함 전제)
  </read_first>
  <action>
    D-13 빌드 체인 (RESEARCH §D-13 커맨드, 전부 무인 — ASC 자동화 OK, eas-cli 20.1.0 확인됨):

    1. `cd app && eas build --profile production --platform ios --non-interactive` — 빌드 완료 대기 (autoIncrement 로 buildNumber 자동 상승, appVersionSource=remote 라 app.json 무변경).
    2. `eas submit -p ios --latest --non-interactive` — TestFlight 무인 제출 (ASC 6772934567).
    3. `eas build --profile preview-android --platform android --non-interactive` — 직원 설치용 APK.
    4. 빌드 아티팩트 URL·빌드 번호·제출 상태를 SUMMARY 에 기록. EAS 함정 주의: npm ci sync (lockfile 커밋 상태), git committed 상태에서만 빌드.
    5. F1 검증 전제 기록: 새 빌드 plugins 에 expo-mail-composer 포함 — 실기기 문의하기 동작은 Task 3 UAT 항목.
  </action>
  <verify>
    <automated>cd app && eas build:list --platform ios --limit 1 --non-interactive --json | head -50</automated>
  </verify>
  <acceptance_criteria>
    - iOS production 빌드 status finished + submit 완료 (ASC 업로드 성공 로그)
    - Android preview-android 빌드 status finished (APK 아티팩트 URL 확보)
    - 빌드 커밋 == origin/main HEAD (29-07 네이티브 변경 포함)
    - SUMMARY 에 iOS 빌드 번호·Android 빌드 번호·제출 상태 기록
  </acceptance_criteria>
  <done>TestFlight 새 빌드 제출 + Android APK 확보 — 가로/F1 네이티브 해소 빌드 출고.</done>
</task>

<task type="auto">
  <name>Task 3: 29-HUMAN-UAT.md 적립 (batch UAT — 즉시 belle 호출 금지)</name>
  <files>.planning/phases/29-mode3-result-screen-completion/29-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/28-dtw-motion-based-alignment/28-HUMAN-UAT.md (형식 선례 — 존재 시; 부재 시 항목/확인방법/기대결과 표 형식 재량)
    - .planning/phases/29-mode3-result-screen-completion/29-04-SUMMARY.md (D-10 UAT 후보 + HIGH-1 power-spin 드릴다운 항목 후보)
    - .planning/phases/29-mode3-result-screen-completion/29-07-SUMMARY.md (가로/구빌드 UAT 후보)
  </read_first>
  <action>
    phase 29 실기기 확인 항목을 29-HUMAN-UAT.md 로 일괄 적립한다 (batch-uat-after-phase-31 원칙 — 나중에 /gsd-audit-uat 한 번에, 즉시 belle 호출 금지). 최소 항목:

    1. **D-14:** 부상 위험 카드에 "이렇게 해보세요" 권고 행 + 점검 캡션 표시 (legacy doc 포함)
    2. **D-01~05:** power-spin mode3 분석 → 점수 내역 렌더 + overallScore==final / kip-up 등 4동작 → 내역 없음 + 한계 고지 + (미등록 동작) 행동 유도 안내 / legacy doc → 통합 배너
    3. **D-06/07:** mode3 비교 라벨 지난/이번 + 첫 분석 안내 1줄
    4. **D-08 + HIGH-1 end-to-end (power-spin 드릴다운):** power-spin mode3 감점 분석에서 (a) 결함 부위 확대 카드 표시 (improved 카드 없음), (b) 점수 내역 leg_extension 행에 번호 + 영상 위 그룹 마커 표시, (c) 행 탭 → 드릴다운 시트에 region 'legs' zoom 카드·행동구 매칭 — criterion→region 투영 전 구간 확인 (29-PLAN-REVIEW HIGH-1)
    5. **D-10:** mode3 second+ 비교영상 워핑 (신뢰도 배지·배속 자연스러움)
    6. **D-09:** 7일+ 경과 mode1 doc 에서 정은지 비교영상 재생 (D1 재발 확인)
    7. **D-11 (새 빌드):** 전체화면 뷰어 진입 시 진짜 가로 전환·닫으면 세로 복귀·flicker 없음 (A1/A2 가정 판정 — 실패 시 plugin initialOrientation/requireFullScreen 조정 후보 명기)
    8. **D-12 (구빌드 27):** OTA 수신 후 앱 정상 기동 + 전체화면 뷰어 = 기존 90° 회전 핵 동작 (크래시 0)
    9. **F1 (새 빌드):** 마이페이지 문의하기 → 메일 컴포저 열림
    10. **iPad 관찰 (참고):** 가로 lock 동작 여부 (Pitfall 5 — 파일럿 iPhone 중심, 실패해도 blocker 아님)

    각 항목에 확인 방법(화면 경로)·기대 결과를 표로. 헤더에 "batch UAT — /gsd-audit-uat 시 일괄, 즉시 belle 호출 금지" 명시. 각 항목의 요구 토큰(D-14, D-01, D-06, D-08, D-09, D-10, D-11, D-12, F1, iPad, power-spin)이 본문에 그대로 등장해야 verify 의 per-token grep 이 통과한다.
  </action>
  <verify>
    <automated>UAT=.planning/phases/29-mode3-result-screen-completion/29-HUMAN-UAT.md; test -f "$UAT" && for t in D-14 D-01 D-06 D-08 D-09 D-10 D-11 D-12 F1 iPad power-spin "belle 호출 금지"; do grep -q -- "$t" "$UAT" || { echo "missing: $t"; exit 1; }; done</automated>
  </verify>
  <acceptance_criteria>
    - 29-HUMAN-UAT.md 존재, 위 10개 항목 전부 포함 (D-14/D-01~05/D-06·07/D-08+power-spin/D-10/D-09/D-11/D-12/F1/iPad)
    - **per-token grep 게이트 (29-PLAN-REVIEW MEDIUM-2):** D-14·D-01·D-06·D-08·D-09·D-10·D-11·D-12·F1·iPad·power-spin 토큰 전부 개별 매치 — `grep -c "D-1"` 류 약한 집계 게이트 금지 (여러 항목 누락돼도 통과하는 함정)
    - 각 항목에 확인 방법 + 기대 결과 존재
    - "즉시 belle 호출 금지" batch 원칙 명시
  </acceptance_criteria>
  <done>실기기 확인 전 항목 적립 완료 — phase 29 마감, /gsd-verify-work 진입 가능.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로컬 → EAS/ASC | 빌드·제출 크리덴셜 사용 지점 |
| OTA 채널 → 전 사용자 기기 | 발행 즉시 구빌드 포함 전 파일럿 사용자 도달 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-08-01 | Denial of Service | OTA 로 구빌드 크래시 | mitigate | 발행 전 정적 import grep 게이트 (Task 1 blocking) + D-12 UAT 항목 |
| T-29-08-02 | Information Disclosure | 빌드 로그·SUMMARY 에 크리덴셜 | mitigate | ASC API Key·env 값 로그 금지 — 빌드 번호·URL 만 기록. 시크릿은 EAS/Parameter Store 소유 (.env 하드코딩 금지 불변) |
| T-29-08-03 | Tampering | 미커밋/불일치 코드 빌드 | mitigate | 빌드·OTA 커밋 == origin/main HEAD 게이트 (EAS git committed 요건) |
| T-29-08-SC | Tampering | 패키지 설치 | accept | 이 plan 은 패키지 설치 0 (29-07 에서 완료) |
</threat_model>

<verification>
- backend full suite 신규 실패 0 + typecheck clean
- OTA update ID 2건 + iOS submit 성공 + Android APK finished
- 29-HUMAN-UAT.md 10항목 적립 — per-token grep 전 토큰 통과 (MEDIUM-2)
</verification>

<success_criteria>
- D-13 완료: 새 빌드 TestFlight 무인 제출 + Android APK + F1 동승 + OTA 발행
- phase 29 전 산출물이 사용자 도달 경로에 실림 — 실기기 검증은 batch UAT 로 이월 (HIGH-1 power-spin 드릴다운 end-to-end 포함)
</success_criteria>

<output>
Create `.planning/phases/29-mode3-result-screen-completion/29-08-SUMMARY.md` when done
</output>
