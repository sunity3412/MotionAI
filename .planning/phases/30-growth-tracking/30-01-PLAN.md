---
phase: 30-growth-tracking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/lib/growthSelectors.ts
  - app/src/theme/colors.ts
  - app/scripts/assert-growth-selectors.mjs
autonomous: true
requirements: [E1, E2]   # ROADMAP Requirements=TBD — PILOT-FEEDBACK-2026-07-06 §E 항목 ID 사용

must_haves:
  truths:
    - "weeklyAverages가 주별 평균을 산출하고 분석 없는 주는 점 없이 건너뛴다 (D-01)"
    - "weeklyAverages는 mode 인자로 mode1/mode3를 분리 집계한다 — 혼합 평균 코드 경로가 존재하지 않는다 (D-02)"
    - "defaultGrowthMode는 마지막 분석 모드를 기본으로 하되, 그 모드 주별 점 2개 미만이면 데이터 있는 다른 모드로 폴백하고, 양쪽 다 부족하면 null을 반환한다 (D-03)"
    - "motionDeltas가 동작별 델타를 '최근 활동 주 평균 − 직전 활동 주 평균' 점수 포인트로 산출하고, 활동 주 1개뿐인 동작은 delta=null(첫 기록)로 반환한다 (D-05)"
    - "scoreSuppressed===true 분석(결과화면이 점수카드를 숨긴 mode3 결과)은 weeklyAverages·motionDeltas·defaultGrowthMode 폴백 후보에서 모두 제외된다 — 결과화면에서 숨긴 점수를 홈 성장 지표로 되살리지 않는다 (REVIEW HIGH-1, 신뢰 계약)"
    - "legacy 채점 체계 문서를 걸러내는 필터/특별 처리 코드가 selector에 없다 — 전 문서 포함 (D-07)"
    - "colors.ts에 하락 표시용 파란계열 토큰이 D-06 근거 주석과 함께 신설된다 (D-06)"
    - "assert-growth-selectors.mjs 시맨틱 테스트가 weekStart·모드 비혼합·폴백·scoreSuppressed 제외·그룹핑·point 델타를 fixture로 잠그고, acceptance 게이트가 이 스크립트를 실행한다 (REVIEW HIGH-2)"
  artifacts:
    - path: "app/src/lib/growthSelectors.ts"
      provides: "주별 평균·동작별 델타·기본 모드 순수 selector + hasUsableGrowthScore predicate (D-01/D-02/D-03/D-05/D-07 + HIGH-1)"
      exports: ["weeklyAverages", "defaultGrowthMode", "motionDeltas", "weekStartOf", "hasUsableGrowthScore"]
      min_lines: 90
    - path: "app/src/theme/colors.ts"
      provides: "하락 파란계열 토큰 (D-06)"
      contains: "declineBlue"
    - path: "app/scripts/assert-growth-selectors.mjs"
      provides: "selector 시맨틱 테스트 (Node assert, fixture 기반 — HIGH-2)"
      contains: "assert"
      min_lines: 60
  key_links:
    - from: "app/src/lib/growthSelectors.ts"
      to: "app/src/types/analysis.ts"
      via: "import type AnalysisDoc/AnalysisMode"
      pattern: "import type \\{.*AnalysisDoc.*\\} from '../types/analysis'"
---

<objective>
Phase 30 성장 추적의 데이터 계층 — 주별 평균(D-01)·모드 분리(D-02)·기본 모드 폴백(D-03)·동작별 점수 델타(D-05)를 순수 함수 selector 모듈로 신설하고, ▼ 하락 표시용 파란계열 테마 토큰(D-06)을 신설한다.

Purpose: 30-03(컴포넌트)·30-04(홈 카드 통합)가 소비할 계약(타입+함수)을 먼저 확정 — interface-first. 순수 함수 분리는 30-CONTEXT Integration Points의 명시 요구.
Output: `app/src/lib/growthSelectors.ts` (신규), `app/src/theme/colors.ts` 토큰 1개 추가.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-growth-tracking/30-CONTEXT.md
@.planning/phases/30-growth-tracking/30-PATTERNS.md
@.planning/PILOT-FEEDBACK-2026-07-06.md
@app/src/lib/alignmentWarp.ts
@app/src/lib/userAnalyses.ts
@app/src/types/analysis.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: growthSelectors.ts 신설 — 주별 평균·기본 모드·동작별 델타 순수 selector</name>
  <files>app/src/lib/growthSelectors.ts</files>
  <read_first>
    - app/src/lib/alignmentWarp.ts (모듈 헤더 순수성 선언 + 상수 출처 박제 + named export 패턴 — 이 모듈의 골격 analog, 30-PATTERNS)
    - app/src/types/analysis.ts (AnalysisDoc/AnalysisMode/Mode1Comparison.referenceMotionId·Name 정확한 필드명과 createdAt 타입 확인 — 가정 금지. :409-419 AnalysisResult scoreSuppressed/scoreSuppressedReason discriminated union — HIGH-1 제외 predicate가 소비할 계약)
    - app/src/app/analysis/result.tsx :647-658 (isScoreSuppressed = mode3 && result.scoreSuppressed===true → 점수카드 숨김. 홈 성장 지표도 동일 기준으로 제외해야 신뢰 계약 정합 — HIGH-1)
    - app/src/app/(tabs)/index.tsx :58-64 averageScore (filter((s): s is number => ...) 관용구) 및 :292-302 GrowthCard 현행 피드
    - app/src/app/(tabs)/history.tsx :23-32 motionLabel/modeBadge (동작 그룹핑 키·배지 카피 선례)
  </read_first>
  <action>
    `app/src/lib/growthSelectors.ts`를 신설한다. alignmentWarp.ts 패턴대로 모듈 헤더에 "순수 함수만 (react/firebase 의존 0 — tsc --noEmit 만으로 검증)" 선언 + 30-CONTEXT D-01/D-02/D-03/D-05/D-07 출처 인용 주석. import는 `import type { AnalysisDoc, AnalysisMode } from '../types/analysis'`만.

    내보낼 계약 (모두 named export):
    - `interface WeeklyPoint { weekStart: number; avg: number; count: number }` — weekStart = 해당 주 월요일 00:00 로컬시간 epoch millis.
    - `interface MotionDeltaRow { key: string; label: string; badge: '프로 비교' | '내 기록'; latestAvg: number; delta: number | null; lastAnalyzedAt: number }` — delta=null은 첫 기록(비교 대상 없음, D-05).
    - `weekStartOf(millis: number): number` — 월요일 시작 로컬 주 버킷 헬퍼 (테스트 가능성 위해 별도 export). createdAt이 Firestore Timestamp면 호출부에서 millis로 변환하는 내부 헬퍼를 두되, analysis.ts에서 실제 타입을 확인해 결정 (가정 금지).
    - `hasUsableGrowthScore(doc: AnalysisDoc, mode: AnalysisMode): boolean` — (HIGH-1) 성장 지표에 쓸 수 있는 분석인지 단일 판별 predicate. 조건 = `doc.status === 'done'` AND `doc.mode === mode` AND `result?.overallScore`가 `typeof === 'number' && Number.isFinite` AND **`result?.scoreSuppressed !== true`**. 이 predicate가 유일한 집계 자격 관문 — 아래 세 selector는 전부 이것만 통과한 문서를 쓴다. result.tsx가 mode3 suppressed에서 점수카드를 숨기는 것(:652)과 동일 기준. 주석에 "결과화면에서 숨긴 점수는 홈 성장 지표(평균·델타·기본모드)에도 쓰지 않는다 — HIGH-1 신뢰 계약" 박제.
    - `weeklyAverages(analyses: AnalysisDoc[], mode: AnalysisMode): WeeklyPoint[]` — (D-01/D-02) `hasUsableGrowthScore(doc, mode)` 통과 문서만 집계 (scoreSuppressed·NaN·비수치·모드불일치 전부 predicate가 배제). 주 버킷별 평균(내부 raw, 반환 avg는 Math.round), 분석 없는 주는 요소 자체가 없음(빈 주 0점 채움 금지), weekStart 오름차순 정렬.
    - `defaultGrowthMode(analyses: AnalysisDoc[]): AnalysisMode | null` — (D-03) 최신 문서(배열 첫 요소, useMyAnalyses는 최신순)의 mode를 기본으로 하되, 그 모드의 weeklyAverages 길이 < 2이면 다른 모드를 검사해 길이 ≥ 2일 때 폴백. 양쪽 다 < 2면 null (호출부가 GrowthLockedCard 처리). weeklyAverages가 predicate로 suppressed를 이미 배제하므로 폴백 후보에서도 자동 제외 — 별도 분기 불필요하되, "기본 모드 선정도 hasUsableGrowthScore 기반"임을 주석에 명시 (HIGH-1).
    - `motionDeltas(analyses: AnalysisDoc[]): MotionDeltaRow[]` — (D-04 화면층/D-05) 입력을 `hasUsableGrowthScore(doc, doc.mode)`로 먼저 거른 뒤 그룹핑: mode1은 `result.comparison.mode === 'mode1'`일 때 `referenceMotionId`를 key, `referenceMotionName`을 label로; mode3는 전부 key='self', label='내 기록' 단일 그룹 (history.tsx motionLabel 선례). badge는 modeBadge 카피 재사용('프로 비교'/'내 기록'). 그룹별로 주별 평균(raw)을 구해 delta = Math.round(최근 활동 주 평균 − 직전 활동 주 평균); 활동 주가 1개면 delta=null. latestAvg = Math.round(최근 활동 주 평균). 기본 정렬 = lastAnalyzedAt desc (표시 순서 세부는 30-03이 Figma 확인 후 재량 — 코드 주석에 명시).

    D-07: legacy 문서(mode1 Phase 24 이전 / mode3 Phase 29 tally 전환 이전) 필터링 코드를 넣지 않는다 — "전부 포함, 특별 처리 없음"을 주석으로 박제. 주의: scoreSuppressed 제외(HIGH-1)는 legacy 필터가 아니라 '결과화면과 동일한 표시 억제 정합'이므로 D-07과 무모순 — legacy는 채점 체계 세대 구분(포함), suppressed는 표시 억제 계약(제외). 델타/평균 어디에도 '%' 계산·문자열 없음 (mode3-progress-not-similarity invariant). deltaFromPrevious나 angle 유사도는 소비하지 않는다 — overallScore만 (mode3-overall-exclude-angle-similarity와 정합).

    게이트 폭 주의: 아래 verify의 '%' grep은 파일 전체(주석 포함) 0건 게이트로, invariant(등락률 퍼센트 금지)보다 의도적으로 넓게 잡았다 — 모듈로 연산자 `%`도 걸리므로 요일/주 버킷 계산은 `Date#getDay()` + millis 산술로 구현(모듈로 불필요)하고, 나머지 연산이 꼭 필요하면 `a - Math.floor(a / b) * b`로 대체한다. 퍼센트 표기·문자는 코드·주석 어디에도 쓰지 않는다.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck && grep -c "export function\|export interface" src/lib/growthSelectors.ts && grep -c "hasUsableGrowthScore" src/lib/growthSelectors.ts && grep -c "scoreSuppressed" src/lib/growthSelectors.ts && ! grep -n "useState\|useEffect\|firebase\|onSnapshot" src/lib/growthSelectors.ts && ! grep -n "%" src/lib/growthSelectors.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` (tsc --noEmit) 통과.
    - `grep -n "export" app/src/lib/growthSelectors.ts`에 weeklyAverages, defaultGrowthMode, motionDeltas, weekStartOf, hasUsableGrowthScore, WeeklyPoint, MotionDeltaRow 전부 존재.
    - `grep -n "scoreSuppressed" app/src/lib/growthSelectors.ts` 매치 ≥ 1건, 그리고 weeklyAverages/motionDeltas/defaultGrowthMode가 전부 hasUsableGrowthScore를 경유(별도 자격 필터 중복 정의 없음) — HIGH-1. 시맨틱 증명은 Task 3.
    - `grep -n "useState\|useEffect\|firebase\|onSnapshot" app/src/lib/growthSelectors.ts` 매치 0건 (순수성).
    - `grep -n "%" app/src/lib/growthSelectors.ts` 매치 0건 (파일 전체 — 주석 포함 % 문자 금지. 모듈로 연산자 포함 — Task 1 action의 대체 구현 규칙 참조).
    - `grep -n "Number.isFinite" app/src/lib/growthSelectors.ts` 매치 ≥ 1건 (NaN 방어).
    - legacy 제외 필터 부재 확인 (MEDIUM-2 — bare word grep 대신 property 접근 검사): `grep -nE "phaseId|milestone|createdAt[^)]*<|scoringBasis[^)]*(===|!==)|\\bversion\\b" app/src/lib/growthSelectors.ts`로 legacy 세대를 갈라내는 조건 분기(날짜/버전/basis 비교) 매치 0건. D-07 설명 주석의 'legacy'/'phase' 단어는 허용(주석은 필터가 아님) — 이 grep은 실제 필터링 property 접근만 겨냥.
  </acceptance_criteria>
  <done>순수 selector 모듈이 D-01/D-02/D-03/D-05/D-07 규칙 + HIGH-1 suppressed 제외를 타입 계약과 함께 제공하고 typecheck·순수성·%부재 게이트를 전부 통과.</done>
</task>

<task type="auto">
  <name>Task 2: colors.ts 하락 파란계열 토큰 신설 (D-06)</name>
  <files>app/src/theme/colors.ts</files>
  <read_first>
    - app/src/theme/colors.ts (:34 highlight '#006FFD', :45-46 progressGreen/progressRed, :77-83 advisoryOrange alias 신설 블록 — 형식 analog)
  </read_first>
  <action>
    colors.ts에 advisoryOrange 블록(:77-83) 형식으로 신규 토큰 블록을 추가한다: `declineBlue: '#006FFD'` (기존 `highlight` 값 alias). 블록 주석에 D-06 근거 박제: "30-CONTEXT D-06 — 주식창 관례: 상승 ▲=brand(#FF4B33 그대로, 신규 토큰 불필요) / 하락 ▼=파랑. 기존 progressGreen(+)/progressRed(−)는 부호-색이 D-06과 반대라 성장 카드 재사용 금지." brand 값 변경 0 (CLAUDE.md §4). 기존 토큰 수정·삭제 없음, 추가만.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck && grep -c "declineBlue" src/theme/colors.ts && grep -c "#FF4B33" src/theme/colors.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "declineBlue" app/src/theme/colors.ts` 매치 ≥ 1건, 값은 '#006FFD'.
    - 블록 주석에 "D-06" 문자열 존재.
    - `git diff app/src/theme/colors.ts`에 기존 라인 삭제/변경 없음 (추가만).
    - typecheck 통과.
  </acceptance_criteria>
  <done>▼ 표시에 쓸 declineBlue 토큰이 D-06 근거 주석과 함께 존재하고 기존 토큰 무손상.</done>
</task>

<task type="auto">
  <name>Task 3: growthSelectors 시맨틱 테스트 (Node assert, 신규 프레임워크 0 — HIGH-2)</name>
  <files>app/scripts/assert-growth-selectors.mjs</files>
  <read_first>
    - app/src/lib/growthSelectors.ts (Task 1 산출 — 검증 대상 함수 시그니처·계약)
    - app/scripts/verify-firebase.mjs (프로젝트 기존 .mjs 관리자 스크립트 관례 — 순수 Node 실행, devDep만 사용. import 스타일·실행 형식 analog)
    - app/src/types/analysis.ts (fixture AnalysisDoc 최소 형태 — status/mode/result.overallScore/result.scoreSuppressed/result.comparison 필드명 확인, 가정 금지)
  </read_first>
  <action>
    셀렉터 핵심 규칙(주간 bucket·모드 분리·폴백·suppressed 제외·그룹핑·point 델타)이 typecheck/grep을 넘어 실제 동작으로 잠기도록 시맨틱 테스트를 신설한다. **새 테스트 프레임워크·devDependency 도입 금지** — Node 24 내장 타입 스트리핑으로 growthSelectors.ts를 직접 import한다. 권장 실행: `node --experimental-strip-types app/scripts/assert-growth-selectors.mjs` (growthSelectors.ts는 `import type`만 쓰는 순수 모듈이라 런타임 의존 0 → 스트리핑으로 그대로 로드됨). Expo tsconfig path 문제로 스트리핑 로드가 실패하면 폴백: `npx tsc src/lib/growthSelectors.ts --outDir <tmp> --module esnext --moduleResolution bundler` 후 컴파일 산출물 import (둘 다 신규 상시 devDep 아님 — tsc는 이미 설치됨). 실제 실행 형식은 executor가 확인해 스크립트 상단 주석에 박제.

    fixture는 최소 AnalysisDoc 배열을 손으로 구성해 Node `assert`(strict)로 다음을 검증한다:
    - `weekStartOf`: 알려진 수요일 millis → 그 주 월요일 00:00 로컬 millis 반환 (경계 계산).
    - `weeklyAverages` 모드 비혼합: 같은 주에 mode1·mode3 문서가 섞여 있을 때 `weeklyAverages(docs,'mode1')`와 `'mode3'`가 서로의 점수를 평균에 넣지 않음 (D-02).
    - `defaultGrowthMode`: (a) 최신 모드 주별 점 ≥2 → 그 모드, (b) 최신 모드 <2인데 다른 모드 ≥2 → 폴백, (c) 양쪽 <2 → null (D-03).
    - `hasUsableGrowthScore`/HIGH-1: `scoreSuppressed:true`이고 `overallScore`가 있는 mode3 문서가 weeklyAverages·motionDeltas·defaultGrowthMode 후보에서 **모두 제외**됨.
    - `motionDeltas` 그룹핑: mode1은 referenceMotionId별 행, mode3는 '내 기록' 단일 행 (D-04 화면층).
    - `motionDeltas` 델타: delta가 퍼센트가 아닌 정수 point 차이, 활동 주 1개면 null (D-05).
    - NaN 방어: `overallScore: NaN` 문서가 평균/델타에 반영되지 않음.
    스크립트는 실패 시 non-zero exit (assert 던짐). 통과 시 "growth-selectors semantic checks passed" 출력.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && node --experimental-strip-types scripts/assert-growth-selectors.mjs && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && node --experimental-strip-types scripts/assert-growth-selectors.mjs`(또는 스크립트 주석에 박제된 폴백 실행 형식)가 exit 0으로 통과.
    - 스크립트에 최소 7개 assert 케이스(weekStart·모드비혼합·폴백 3분기·suppressed 제외·그룹핑·point 델타·NaN 방어) 존재 — `grep -c "assert" app/scripts/assert-growth-selectors.mjs` ≥ 7.
    - suppressed 제외 케이스가 명시적으로 존재: `grep -n "scoreSuppressed" app/scripts/assert-growth-selectors.mjs` ≥ 1건.
    - 신규 devDependency 0: `git diff app/package.json` 무변경.
  </acceptance_criteria>
  <done>셀렉터 규칙이 실행 가능한 fixture 테스트로 잠기고, scoreSuppressed 제외(HIGH-1)가 시맨틱으로 증명됨. acceptance 게이트가 typecheck에 더해 이 스크립트 실행을 포함.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firestore doc → selector | 서버가 쓴 문서지만 필드 타입/NaN 신뢰 불가 (CPU fallback NaN 이력, legacy 필드 부재) |
| 결과화면 표시 억제 → 홈 지표 | result.tsx가 숨긴 scoreSuppressed 점수가 홈 성장 지표로 새어나가면 신뢰 계약 위반 (HIGH-1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Tampering (malformed data) | growthSelectors 집계 입력 | mitigate | `typeof === 'number' && Number.isFinite` 필터로 NaN/비수치 점수 배제, comparison 필드는 mode 판별 후 string 타입 가드 |
| T-30-02 | DoS (렌더 붕괴) | weeklyAverages 빈/단일 데이터 | mitigate | 빈 배열·활동 주 0~1개에서 예외 없이 빈 배열/null 반환 (호출부 locked-card 분기) |
| T-30-04 | Information Disclosure (억제 우회) | scoreSuppressed 점수의 홈 지표 노출 | mitigate | hasUsableGrowthScore가 `scoreSuppressed !== true`를 단일 관문에서 강제, Task 3 시맨틱 테스트가 제외를 증명 (HIGH-1) |
| T-30-SC | Tampering | 패키지 설치 | accept | 이 phase 신규 패키지 설치 0건 — 해당 없음 (assert 스크립트는 Node 내장 실행) |
</threat_model>

<verification>
- `cd app && npm run typecheck` GREEN (프로젝트 정적 게이트).
- `cd app && node --experimental-strip-types scripts/assert-growth-selectors.mjs` GREEN — 셀렉터 시맨틱 테스트 (HIGH-2, 신규 devDep 0). typecheck+grep만으로는 못 잡는 회귀(weekStart·모드혼합·폴백·suppressed 포함·델타 부호)를 잠근다.
- growthSelectors.ts 순수성 grep 게이트 (react/firebase import 0).
- '%' 문자 grep 0건 (invariant 선제 차단).
- scoreSuppressed 제외 grep + 시맨틱 assert (HIGH-1).
</verification>

<success_criteria>
- D-01/D-02/D-03/D-05/D-06/D-07이 코드로 구현되고 각 결정 ID가 주석으로 인용됨.
- HIGH-1: scoreSuppressed===true 분석이 hasUsableGrowthScore 단일 관문으로 평균·델타·기본모드에서 제외되고 Task 3 시맨틱 테스트가 이를 증명.
- HIGH-2: assert-growth-selectors.mjs가 셀렉터 핵심 규칙을 fixture로 잠그고 acceptance 게이트에 포함.
- 30-03/30-04가 import할 타입·함수 계약이 확정됨 (WeeklyPoint, MotionDeltaRow, 3 selector + weekStartOf + hasUsableGrowthScore).
</success_criteria>

<output>
Create `.planning/phases/30-growth-tracking/30-01-SUMMARY.md` when done
</output>
