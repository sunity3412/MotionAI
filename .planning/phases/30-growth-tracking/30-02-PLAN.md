---
phase: 30-growth-tracking
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/types/analysis.ts
  - backend/shared/python/sunity_shared/models.py
  - docs/contract.md
  - backend/shared/python/sunity_shared/analysis/assemble.py
  - backend/functions/pipeline/app.py
  - backend/tests/test_assemble.py
  - backend/tests/test_pipeline_mode3.py
autonomous: true
requirements: [E2]   # ROADMAP Requirements=TBD — D-04(ii) 데이터 적립 층, PILOT-FEEDBACK §E2 계보

must_haves:
  truths:
    - "mode3 분석 완료 시 인식 성공(profile.motion_id 존재)이면 comparison에 recognizedMotionId/recognizedMotionName이 저장된다 — 첫 분석(is_first=True)에서도 (build_mode3 early return 앞 삽입, HIGH-3)"
    - "recognized 방출이 motion_id 있는 픽스처로 first·progress 두 파이프라인 경로에서 pytest로 증명된다 — grep 배선 확인만으로 통과하는 false-green 차단 (HIGH-3)"
    - "profile.motion_id가 None(인식 실패/FallbackRecognizer)이면 두 키가 아예 추가되지 않는다 — legacy 동형 보존, 기존 test_mode3_first_has_no_delta exact-dict assert가 무변경으로 통과"
    - "3-way lockstep: app/src/types/analysis.ts + backend models.py + docs/contract.md가 단일 atomic commit으로 동시 갱신 (D-04)"
    - "신규 필드는 comparison 내부 scalar string — Firestore nested-array 규칙 자동 준수, _validate_dict_only_scalars 본체 무변경"
    - "채점 로직 무접촉 — overallScore/breakdown 산출 코드 diff 0 (phase 불변 경계)"
  artifacts:
    - path: "app/src/types/analysis.ts"
      provides: "Mode3Comparison.recognizedMotionId?/recognizedMotionName? optional 필드 (D-04)"
      contains: "recognizedMotionId"
    - path: "backend/shared/python/sunity_shared/analysis/assemble.py"
      provides: "build_mode3 recognized_motion_id/recognized_motion_name kwargs (None → 키 미추가)"
      contains: "recognized_motion_id"
    - path: "backend/functions/pipeline/app.py"
      provides: "_mode3_comparison 두 분기에서 profile.motion_id/name 배선"
    - path: "docs/contract.md"
      provides: "Mode3Comparison 절 신규 필드 서술 + legacy 폴백 명시"
      contains: "recognizedMotionName"
    - path: "backend/tests/test_assemble.py"
      provides: "build_mode3 emit(is_first=True/progress)/미emit/ValueError 테스트 (backward-compat 게이트 포함, HIGH-3)"
    - path: "backend/tests/test_pipeline_mode3.py"
      provides: "motion_id 있는 픽스처로 first·progress recognized 방출 pytest 증명 (HIGH-3 false-green 차단)"
      contains: "ref-foo"
  key_links:
    - from: "backend/functions/pipeline/app.py"
      to: "backend/shared/python/sunity_shared/analysis/assemble.py"
      via: "build_mode3(recognized_motion_id=profile.motion_id, ...) 호출 2곳 (first/progress 분기)"
      pattern: "recognized_motion_id=profile\\.motion_id"
    - from: "backend/shared/python/sunity_shared/analysis/assemble.py"
      to: "docs/contract.md"
      via: "3-way lockstep 주석 상호 인용"
      pattern: "contract\\.md"
---

<objective>
D-04(ii): mode3 파이프라인이 이미 인식하고 있던 동작 id/명(`TechniqueProfile.motion_id`/`name`)을 분석 문서 comparison에 optional 필드로 방출 시작 — Phase 16(학원 명칭 카테고리 체계)이 소비할 데이터 적립. 3-way lockstep + legacy 무마이그레이션.

Purpose: 인식기는 이미 mode3에서 동작을 인식 중인데 저장만 안 되던 것 — 앞으로 쌓이는 분석부터 동작별 그룹핑이 가능해진다. 이번 phase 화면은 이 필드를 소비하지 않는다(mode3='내 기록' 단일 행) — 순수 데이터 적립.
Output: 계약 3파일 + assemble.py + pipeline app.py + 테스트, SAM 배포.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-growth-tracking/30-CONTEXT.md
@.planning/phases/30-growth-tracking/30-PATTERNS.md
@backend/shared/python/sunity_shared/analysis/assemble.py
@backend/functions/pipeline/app.py
@docs/contract.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 3-way 계약 + build_mode3 kwargs + 테스트 (단일 atomic commit)</name>
  <files>app/src/types/analysis.ts, backend/shared/python/sunity_shared/models.py, docs/contract.md, backend/shared/python/sunity_shared/analysis/assemble.py, backend/tests/test_assemble.py</files>
  <read_first>
    - app/src/types/analysis.ts (:258-277 Mode3Comparison + scoringBasis? optional 선례, :451-459 tier? 주석 서술 모범, :657-666 learningOptIn 3-way lockstep 문구 선례)
    - backend/shared/python/sunity_shared/models.py (:1-4 파일 헤더 lockstep 규칙, :45-67 MODE3_SCORING_BASES 섹션 주석 형식)
    - backend/shared/python/sunity_shared/analysis/assemble.py (build_mode3 :641-706 부근 — scoring_basis None→키 미추가 패턴이 그대로 복제 대상)
    - backend/tests/test_assemble.py (:60-121 — test_mode3_first_has_no_delta exact-dict assert = backward-compat 게이트, scoringBasis emit assert 스타일)
    - docs/contract.md (:401-427 Mode3Comparison scoringBasis 서술 블록 형식)
  </read_first>
  <action>
    D-04(ii) 계약을 한 번에 (단일 atomic commit — 3-way lockstep):

    1. `app/src/types/analysis.ts` Mode3Comparison에 optional 필드 2개 추가: `recognizedMotionId?: string;` + `recognizedMotionName?: string;`. 주석은 tier? 선례 형식으로: "Phase 30 (30-CONTEXT D-04) — mode3 인식 동작 데이터 적립(이번 phase 화면 미소비, Phase 16이 소비). 부재(legacy doc/인식 실패)=앱은 '내 기록' 단일 그룹. Python lockstep: assemble.build_mode3 + models.py + docs/contract.md §4와 동시 갱신."
    2. `backend/shared/python/sunity_shared/models.py`에 "── Phase 30 (D-04): comparison.recognizedMotionId/Name 명세 ──" 섹션 주석 추가 (MODE3_SCORING_BASES 형식). 값은 free string(인식기 canonical motion id/표시명)이라 enum tuple 불필요 — 주석으로 source(`TechniqueProfile.motion_id`/`name`)와 3중 계약 인용만.
    3. `assemble.py build_mode3`에 kwargs 2개 추가: `recognized_motion_id: str | None = None`, `recognized_motion_name: str | None = None`. 규칙 = scoring_basis 패턴 그대로: `recognized_motion_id`가 None이면 두 키 모두 미추가(legacy 동형 보존); str이 아니면 ValueError (T-30-03 완화 — LLM 유래 값 타입 강제); 전달 시 `out["recognizedMotionId"] = recognized_motion_id`, name은 truthy일 때만 `out["recognizedMotionName"]` emit. **삽입 위치 필수(REVIEW HIGH-3):** 이 emit 블록은 `scoringBasis` 블록과 같은 자리, 즉 `if is_first or not previous_analysis_id: return out` early return(현재 :673) **앞**에 둔다 — 그래야 첫 분석(is_first=True) mode3도 recognized 필드를 실어보낸다. early return 뒤에 붙이면 첫 분석에서 누락됨. docstring에 "Phase 30 D-04: None 미전달 시 기존 dict 정확히 보존; recognized 필드는 early return 전에 out에 추가(첫 분석도 포함)" 명시.
    4. `docs/contract.md` Mode3Comparison 절 코드블록에 `recognizedMotionId?` / `recognizedMotionName?` 행 추가 + "OPTIONAL (legacy doc 호환) — 부재 시 앱은 '내 기록' 단일 그룹. Phase 30 (Plan 30-02) 신설, Phase 16 소비 예정. 3-way lockstep: analysis.ts + models.py + assemble.build_mode3" 서술 (learningOptIn 절 각주 형식).
    5. `backend/tests/test_assemble.py`에 테스트 추가 (기존 scoringBasis emit 테스트 mirror): (a) `is_first=True`, `recognized_motion_id="ref-foo"`, `recognized_motion_name="Foo"` 전달 시 — early return 경로에서도 두 키가 out에 존재하고 값 일치함을 assert (HIGH-3: early return 앞 삽입 증명), (b) progress 케이스(`is_first=False`, `previous_analysis_id` 있음)에서도 두 키 emit + 값 일치, (c) None(기본값) 시 두 키 부재 — 기존 `test_mode3_first_has_no_delta`가 `assert c == {"mode":"mode3","isFirst":True}`로 무수정 통과함을 확인, (d) 비-str 전달 시 ValueError.

    앱 `userAnalyses.ts::normalize`는 변경하지 않는다 — comparison은 통째 신뢰가 기존 관례(:91 raw.result 그대로, recommendedExercises 주석 :166-169)이고 optional string이라 깊은 검증 불필요. firestore_admin.py도 변경 없음 — scalar string은 scoringBasis와 동일하게 전용 validator 없이 저장(30-PATTERNS), `_validate_dict_only_scalars` 본체 변경 영구 금지.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && python -m pytest tests/test_assemble.py -q && cd ../app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest backend/tests/test_assemble.py -q` 전건 통과 — 기존 `test_mode3_first_has_no_delta`의 `assert c == {"mode": "mode3", "isFirst": True}` 무수정 통과 (backward-compat 증명).
    - 신규 테스트 4건(is_first=True emit / progress emit / 미emit / ValueError) 존재·통과 — 특히 is_first=True 케이스가 early return 앞 삽입을 잠근다 (HIGH-3).
    - `grep -n "recognizedMotionId" app/src/types/analysis.ts backend/shared/python/sunity_shared/analysis/assemble.py docs/contract.md` 3파일 전부 매치 (lockstep).
    - `grep -n "recognizedMotionId" backend/shared/python/sunity_shared/firestore_admin.py` 매치 0건 + `git diff`에 firestore_admin.py/userAnalyses.ts 무변경.
    - 3-way + assemble + tests가 단일 커밋 (커밋 메시지에 D-04 인용).
  </acceptance_criteria>
  <done>계약 3파일+builder+테스트가 한 커밋으로 lockstep 갱신되고, None 경로가 legacy dict를 바이트 동일하게 보존.</done>
</task>

<task type="auto">
  <name>Task 2: pipeline _mode3_comparison 배선 — profile.motion_id/name 방출 + recognized 파이프라인 테스트 (HIGH-3)</name>
  <files>backend/functions/pipeline/app.py, backend/tests/test_pipeline_mode3.py</files>
  <read_first>
    - backend/functions/pipeline/app.py (:3233-3238 _mode3_comparison 시그니처 — profile: technique.TechniqueProfile 이미 도달, :3280-3288 first 분기 build_mode3 호출, :3319-3328 progress 분기 호출, :3175-3230 _apply_score_suppression 후처리)
    - backend/shared/python/sunity_shared/analysis/technique.py (:46 profile.name '미상' 가능, :64 profile.motion_id — source 필드 확인)
    - backend/tests/test_pipeline_mode3.py (:32-40 _profile() 픽스처 — 현재 motion_id 미설정(None), :56-73 _mode3_comparison 호출 패턴 — recognized 픽스처 추가 지점)
  </read_first>
  <action>
    (A) `_mode3_comparison`의 두 `assemble.build_mode3(...)` 호출(first 분기 + progress 분기) 모두에 `recognized_motion_id=profile.motion_id, recognized_motion_name=profile.name` kwarg를 추가한다. `profile.motion_id`가 None(FallbackRecognizer/인식 실패)이면 builder가 키를 안 만드는 게 계약이므로 호출부 조건 분기 불필요 — 단 `profile.name`이 '미상' placeholder일 수 있으니 motion_id가 None일 때 name도 의미 없음(builder가 id-None이면 name도 미emit — Task 1 규칙)을 배선 주석에 명시.

    저신뢰(`low_confidence`) 억제(`_apply_score_suppression`)와 독립적으로 emit한다 — D-04 목적이 데이터 적립이므로 표시 억제와 무관하게 저장(30-PATTERNS 플래너 판단 사항 — 이 플랜의 결정: emit 유지, 소비 시점인 Phase 16이 신뢰도 처리). `_mode3_comparison`의 순수성 선언(:3239 — 어댑터/S3/Firestore 불필요) 유지 — 신규 import·부수효과 0. 저장 경로는 기존 build_result → complete_analysis 자동 포함이라 추가 코드 없음.

    (B) HIGH-3 false-green 방어 — `test_pipeline_mode3.py`에 recognized 픽스처와 테스트를 추가한다. 기존 `_profile()`은 motion_id=None이라 방출 경로가 실테스트되지 않으므로: `TechniqueProfile(name="Foo", motion_id="ref-foo", ...)`(나머지 필드는 기존 _profile() 복제)를 넘기는 별도 픽스처/헬퍼를 두고, first 분석과 progress 분석 **둘 다**에서 반환 comparison에 `recognizedMotionId == "ref-foo"` AND `recognizedMotionName == "Foo"`가 있는지 assert. motion_id=None인 기존 _profile() 경로는 두 키 부재를 assert하는 negative test로 유지(기존 테스트가 이미 커버하면 명시 참조). grep 카운트는 배선 존재의 보조 증거로만 남기고, 방출의 주 증명은 이 pytest assertion으로 한다.

    회귀: pipeline 관련 기존 테스트 전건 + test_assemble 재실행.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && grep -c "recognized_motion_id=profile.motion_id" functions/pipeline/app.py && python -m pytest tests/test_pipeline_mode3.py tests/test_assemble.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "recognized_motion_id=profile.motion_id" backend/functions/pipeline/app.py` = 2 (first + progress 두 분기 — 배선 존재 보조 증거).
    - 주 증명(HIGH-3): `test_pipeline_mode3.py`에 motion_id="ref-foo" 픽스처로 first·progress 두 케이스가 comparison의 `recognizedMotionId`/`recognizedMotionName`을 assert하는 테스트 존재·통과 — `grep -n "ref-foo\|recognizedMotionId" backend/tests/test_pipeline_mode3.py` ≥ 2건.
    - motion_id=None 경로가 두 키 부재를 검증하는 negative test 유지·통과.
    - `python -m pytest backend/tests/test_pipeline_mode3.py backend/tests/test_assemble.py -q` 전건 통과, 기존 실패 0 신규 도입.
    - `git diff backend/functions/pipeline/app.py`가 _mode3_comparison 내부 build_mode3 호출 kwarg 추가 + 주석 외 변경 0 — 특히 채점 산출(overall/abs_dims/breakdown) 라인 무접촉.
  </acceptance_criteria>
  <done>인식 동작 id/명이 mode3 first/progress 양 분기에서 comparison으로 흘러 Firestore에 적립되기 시작하고, motion_id 있는 프로파일로 first·progress 방출이 pytest로 증명됨 (HIGH-3 false-green 차단).</done>
</task>

<task type="auto">
  <name>Task 3: SAM 배포 + 실효 런타임(Pod) 동기화 조건 기록</name>
  <files>.planning/phases/30-growth-tracking/30-HUMAN-UAT.md</files>
  <read_first>
    - backend/samconfig.toml (stack sunity-motion-pilot, region ap-northeast-2 — 배포 기본값)
  </read_first>
  <action>
    (1) `cd backend && sam build --use-container && sam deploy` 실행 (Mac native 바이너리 함정으로 --use-container 필수). Docker Desktop 미가동/자격 증명 실패 시 배포를 건너뛰고 그 사실을 SUMMARY deviation + 30-HUMAN-UAT.md ops 노트로 기록 (조용한 생략 금지).
    (2) `.planning/phases/30-growth-tracking/30-HUMAN-UAT.md` 생성(없으면) 후 ops 노트 적립: "D-04 방출 필드의 실효 런타임은 RunPod Pod(_process 실행처) — 현재 Pod OFF. 다음 Pod 재가동 시 git 최신 코드 pull로 자동 반영, 재가동 후 mode3 실분석 1건으로 comparison.recognizedMotionId 저장 확인." (batch UAT 원칙 — 즉시 사용자 호출 금지, 적립만.)
  </action>
  <verify>
    <automated>test -f /Users/kimtaesung/Dev/SunityMotion/.planning/phases/30-growth-tracking/30-HUMAN-UAT.md</automated>
    <conditional>배포를 실행한 경우에만 추가 게이트: `aws cloudformation describe-stacks --stack-name sunity-motion-pilot --region ap-northeast-2 --query 'Stacks[0].StackStatus' --output text | grep -E "UPDATE_COMPLETE|CREATE_COMPLETE"` — 배포 보류(Docker 미가동/자격 실패) 시 이 절은 생략하고 30-HUMAN-UAT.md 보류 사유 기록으로 대체 (acceptance_criteria '둘 중 하나' 규칙과 정합). 두 절을 ';' 연결하지 않고 분리 서술한 것은 의도 — CF 상태 확인 실패가 무조건 게이트를 조용히 통과하는 형태를 피하기 위함.</conditional>
  </verify>
  <acceptance_criteria>
    - 배포 성공 시: CloudFormation 스택 상태 UPDATE_COMPLETE. 배포 불가 시: SUMMARY deviation + 30-HUMAN-UAT.md에 배포 보류 사유 기록 (둘 중 하나는 반드시 충족).
    - 30-HUMAN-UAT.md에 "Pod 재가동 시 recognizedMotionId 저장 확인" 항목 존재.
  </acceptance_criteria>
  <done>Lambda 측 코드 동기화 완료(또는 보류 사유 박제) + Pod 동기화·실검증 조건이 batch UAT로 적립됨.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gemini 인식기 → TechniqueProfile → Firestore | LLM 유래 free string(motion_id/name)이 영속 저장으로 넘어가는 경계 |
| Firestore doc → 앱 렌더 | 저장된 string이 미래(Phase 16) 화면에 노출될 경계 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-03 | Tampering | build_mode3 신규 kwargs | mitigate | 비-str 입력 ValueError (builder에서 타입 강제), scalar string이라 nested-array validator 자동 준수 |
| T-30-04 | Repudiation/Integrity | 억제(low_confidence)와 emit 독립 | accept | 데이터 적립 목적 — 소비 시점(Phase 16)에 신뢰도 처리. 계약 주석에 명시 |
| T-30-05 | Info Disclosure | 동작명 저장 | accept | 동작명은 PII 아님, 소유자 본인 문서(Firestore rules 기존 경계 불변) |
| T-30-SC | Tampering | 패키지 설치 | accept | 신규 패키지 설치 0건 — 해당 없음 |
</threat_model>

<verification>
- `python -m pytest backend/tests/test_assemble.py -q` + `-k "mode3 or assemble"` 전건 GREEN, 기존 exact-dict 테스트 무수정 통과.
- `npm run typecheck` GREEN (analysis.ts 변경분).
- lockstep grep: recognizedMotionId가 analysis.ts/assemble.py/contract.md 3곳 존재, firestore_admin.py 0곳.
- 채점 무접촉: git diff에 dimensions/kismam/deduction 계열 파일 0건.
</verification>

<success_criteria>
- 인식 성공한 mode3 분석부터 동작 id/명이 comparison에 적립 (D-04 ii).
- legacy 문서·인식 실패 경로 바이트 동일 (마이그레이션 0).
- 3-way lockstep 단일 커밋.
</success_criteria>

<output>
Create `.planning/phases/30-growth-tracking/30-02-SUMMARY.md` when done
</output>
