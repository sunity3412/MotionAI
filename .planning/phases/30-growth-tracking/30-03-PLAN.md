---
phase: 30-growth-tracking
plan: 03
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - app/src/components/GrowthChart.tsx
  - app/src/components/GrowthMotionBars.tsx
  - app/src/app/(tabs)/index.tsx
autonomous: true
requirements: [E1, E2]   # ROADMAP Requirements=TBD — PILOT-FEEDBACK §E1/E2

must_haves:
  truths:
    - "GrowthChart가 raw 점수 6건 나열이 아니라 주별 평균 점(WeeklyPoint[])을 꺾은선으로 그린다 (D-01, E1)"
    - "동작별 리스트(GrowthMotionBars)가 mode1 동작 행들 + '내 기록' 행을 배지('프로 비교'/'내 기록')로 구분해 한 리스트로 그린다 (D-04 i, D-09)"
    - "증감은 '+N점'/'−N점' 점수 포인트 표기 — 델타/점수/라벨 표기에 '%' 없음 (D-05, mode3-progress-not-similarity invariant). 단 GrowthChart 기존 골격의 Svg 컨테이너 width=\"100%\" 속성(GrowthChart.tsx:42, 레이아웃 반응형)은 게이트 제외 — 픽셀 고정으로 바꾸지 말 것"
    - "모든 증감이 노이즈 임계 없이 ▲=colors.brand / ▼=colors.declineBlue로 노출된다 (D-06)"
    - "활동 주 1개뿐인 동작은 '첫 기록 N점 (비교 전)' 형태로 표시 (D-05)"
    - "색·간격 하드코딩 0 — 전부 테마 토큰"
    - "index.tsx 잠정 배선 TODO 주석이 교체 플랜 파일명(30-04-PLAN.md)을 명시한다 — 플랜 간 교체 계약 고정"
  artifacts:
    - path: "app/src/components/GrowthChart.tsx"
      provides: "주별 평균 꺾은선 (WeeklyPoint[] props)"
      contains: "WeeklyPoint"
      min_lines: 60
    - path: "app/src/components/GrowthMotionBars.tsx"
      provides: "동작별 ▲▼ 델타 리스트 (주식창식, D-05/D-06/D-09)"
      exports: ["GrowthMotionBars"]
      min_lines: 60
  key_links:
    - from: "app/src/components/GrowthMotionBars.tsx"
      to: "app/src/lib/growthSelectors.ts"
      via: "MotionDeltaRow props 타입 import"
      pattern: "MotionDeltaRow"
    - from: "app/src/components/GrowthChart.tsx"
      to: "app/src/lib/growthSelectors.ts"
      via: "WeeklyPoint props 타입 import"
      pattern: "WeeklyPoint"
    - from: "app/src/components/GrowthMotionBars.tsx"
      to: "app/src/theme"
      via: "colors.declineBlue / colors.brand"
      pattern: "declineBlue"
    # GrowthMotionBars → 화면 노출 배선(index.tsx [동작별] 탭 렌더)은 30-04 must_haves.key_links 소관
    # — 이 플랜 완료 시점에는 컴포넌트만 존재(의도된 wave 분리, 고아 아님: 30-04 depends_on 30-03).
---

<objective>
성장 카드의 두 보기용 프레젠테이션 컴포넌트 — (1) GrowthChart를 주별 평균 선(D-01)으로 전환, (2) 동작별 ▲▼ 델타 리스트 GrowthMotionBars 신설(D-05/D-06/D-09, 주식창식).

Purpose: E1(평균 기반)·E2(동작별 막대)의 화면 실체. 30-04가 이 두 컴포넌트를 홈 카드 토글 아래에 배치한다.
Output: GrowthChart.tsx 개편 + GrowthMotionBars.tsx 신설 + index.tsx 호출부 최소 배선(타입 정합 유지용 잠정 — 30-04-PLAN.md가 교체).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-growth-tracking/30-CONTEXT.md
@.planning/phases/30-growth-tracking/30-PATTERNS.md
@app/src/components/GrowthChart.tsx
@app/src/app/(tabs)/history.tsx
@app/src/lib/growthSelectors.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: GrowthChart 주별 평균 전환 + index.tsx 잠정 배선</name>
  <files>app/src/components/GrowthChart.tsx, app/src/app/(tabs)/index.tsx</files>
  <read_first>
    - app/src/components/GrowthChart.tsx (현행 88줄 전체 — viewBox 고정 좌표계+min/max 정규화+그라디언트 area+점 라벨 골격 그대로 재사용)
    - app/src/lib/growthSelectors.ts (30-01 산출 — WeeklyPoint 계약, weeklyAverages/defaultGrowthMode 시그니처)
    - app/src/app/(tabs)/index.tsx (:68 useMyAnalyses 구독, :292-313 GrowthCard/GrowthLockedCard 현행 호출부)
  </read_first>
  <action>
    세부 비주얼(주 라벨 포맷, 표시 주 수 상한, 점 강조)은 **Figma 우선 확인**: Figma MCP(get_design_context, fileKey jrdI7kp245HkPfLB0nclsz)로 성장 카드 노드를 확인하고, 미접근/미설계 시 design.md 규칙 + 기존 GrowthChart 비주얼 유지로 자체 판단 (선택 근거를 컴포넌트 주석에 기록 — ui-figma-first).

    GrowthChart props를 `{ scores: number[] }` → `{ points: WeeklyPoint[] }`로 교체 (D-01). 기존 svg 골격(W/H/PAD 상수, min/max 정규화, Defs+Polygon 그라디언트, 마지막 점 강조, SvgText 점수 라벨) 유지 — y값 소스만 point.avg. 골격의 `<Svg width="100%">` 컨테이너 속성(:42)은 그대로 유지 (반응형 폭 — % invariant 게이트에서 이 속성만 예외, 델타/라벨 표기에는 % 금지). 각 점 아래에 주 시작일 라벨(예: '6/29주' — 포맷은 Figma/재량, weekStart millis에서 파생) SvgText 추가. `points.length < 2`면 기존처럼 null 반환 (D-03 폴백 임계 2와 정합). 상단 블록 주석에 "30-CONTEXT D-01 — 주별 평균, 분석 없는 주는 점 없음(등간격 x는 활동 주 순번)" + Figma/design.md 출처 인용.

    index.tsx 호출부는 typecheck 정합만 유지하는 잠정 배선: `weeklyAverages(analyses, defaultGrowthMode(analyses) ?? 'mode3')` 결과를 points로 전달 (기존 slice(0,6) raw scores 제거). TODO 주석에 교체 플랜 파일명을 반드시 명시: `// TODO(30-04-PLAN.md): 잠정 배선 — 같은 phase wave 3의 30-04(depends_on 30-03)가 2층 토글 상태 기반 배선 + GrowthLockedCard 분기로 즉시 교체`.

    알려진 잠정 한계(플랜 간 계약으로 박제): defaultGrowthMode가 null(양 모드 주별 점 <2)인 경우 `?? 'mode3'` 폴백의 weeklyAverages 결과가 2점 미만 → GrowthChart null 반환으로 카드 내부가 비어 보일 수 있다. 이 빈 상태는 동일 phase 내 즉시 후속 실행되는 30-04가 D-03 null 분기(GrowthLockedCard 게이트 교체)로 해소하는 것이 계약이며, 이 태스크에서 GrowthLockedCard 분기 조건은 건드리지 않는다 (30-04 소관 — TODO 주석이 그 경계를 코드에 고정).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck && grep -c "WeeklyPoint" src/components/GrowthChart.tsx && grep -c "TODO(30-04" "src/app/(tabs)/index.tsx" && ! grep -n "scores: number\[\]" src/components/GrowthChart.tsx</automated>
  </verify>
  <acceptance_criteria>
    - typecheck 통과 (index.tsx 호출부 포함 전체 정합).
    - GrowthChart props에 `points: WeeklyPoint[]` 존재, `scores: number[]` 부재.
    - `grep -n '#[0-9A-Fa-f]\{3,6\}' app/src/components/GrowthChart.tsx` 매치 0건 (테마 토큰만).
    - index.tsx에 raw `slice(0, 6)` 점수 피드가 GrowthCard 경로에서 제거되고 weeklyAverages 결과가 전달됨.
    - index.tsx 잠정 배선 TODO 주석에 '30-04-PLAN.md' 문자열 존재 (교체 플랜 파일명 박제 — grep "TODO(30-04" ≥ 1건).
    - 컴포넌트 주석에 D-01 및 Figma 확인 결과(또는 미설계 자체판단 근거) 인용 존재.
  </acceptance_criteria>
  <done>홈 추이 차트의 데이터 의미가 raw 나열 → 주별 평균으로 전환되고 전체 typecheck GREEN, 잠정 배선의 교체 계약(30-04)이 코드에 고정됨.</done>
</task>

<task type="auto">
  <name>Task 2: GrowthMotionBars 신설 — 동작별 ▲▼ 델타 리스트 (주식창식)</name>
  <files>app/src/components/GrowthMotionBars.tsx</files>
  <read_first>
    - app/src/app/(tabs)/history.tsx (:86-122 행 구조 — 배지+제목+우측 수치, :152-160 rowBadge 스타일: colors.brand bg + colors.textWhite + borderRadius 8)
    - app/src/components/GrowthChart.tsx (svg 직접 렌더 패턴 — 막대 렌더에 부분 재사용)
    - app/src/lib/growthSelectors.ts (MotionDeltaRow 계약 — key/label/badge/latestAvg/delta/lastAnalyzedAt)
    - app/src/theme/colors.ts (declineBlue 토큰 — 30-01 산출)
  </read_first>
  <action>
    신규 컴포넌트 `GrowthMotionBars({ rows }: { rows: MotionDeltaRow[] })` (named export). 코드베이스에 정확한 analog 없는 유일 조각(30-PATTERNS No Analog) — 조합 규칙:
    - 행 구조 = history.tsx 행(배지 + 동작명 numberOfLines={1} + 우측 수치). 배지 카피는 row.badge 그대로('프로 비교'/'내 기록' — D-09 통합 리스트 구분).
    - 델타 표기 (D-05): `delta !== null`이면 `▲ +N점` / `▼ −N점` — ▲ 텍스트·막대 = colors.brand, ▼ = colors.declineBlue (D-06 주식창 관례, progressGreen/progressRed 사용 금지 — 부호-색 반대). delta === 0은 '±0점' 중립(colors.textSecondary). `delta === null`이면 `첫 기록 N점 (비교 전)` (N = latestAvg).
    - 임계·숨김 없음 — 전달된 모든 행의 증감 그대로 노출 (D-06).
    - 주식창식 막대: |delta| 비례 수평 막대(react-native-svg Rect 또는 View width, GrowthChart svg 패턴 재사용) — 시각 세부(막대 유무·두께·정렬 순서·동작 수 상한)는 **Figma 우선 확인**(fileKey jrdI7kp245HkPfLB0nclsz) 후 미설계 시 design.md 기반 자체 판단, 근거를 주석에 기록.
    - '%' 문자를 파일 어디에도(주석 포함) 쓰지 않는다 — mode3-progress-not-similarity invariant grep 게이트. 등락률·일치율 계산 금지, 입력 MotionDeltaRow 외 데이터 파생 금지. **게이트 폭 주의:** 이 grep은 invariant보다 의도적으로 넓어 모듈로 연산자 `%`와 RN 퍼센트 width 문자열('50%' 등)도 걸린다 — 막대 폭은 퍼센트 문자열 대신 계산된 픽셀 number(svg viewBox 좌표 또는 `width: n`) 또는 flex 비율로 구현하고, 나머지 연산이 필요하면 `a - Math.floor(a / b) * b`로 대체한다.
    - a11y: 각 행 View에 accessibilityLabel(예: "케미컬스핀, 프로 비교, 6점 상승") — BodyProfileForm 컨벤션.
    - 스타일 전부 테마 토큰(colors/spacing/radius), StyleSheet.create 하단 배치, 상단 Korean 블록 주석에 30-CONTEXT D-05/D-06/D-09 인용.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck && grep -c "declineBlue" src/components/GrowthMotionBars.tsx && ! grep -n "%" src/components/GrowthMotionBars.tsx && ! grep -n "progressGreen\|progressRed" src/components/GrowthMotionBars.tsx && ! grep -n '#[0-9A-Fa-f]\{3,6\}' src/components/GrowthMotionBars.tsx</automated>
  </verify>
  <acceptance_criteria>
    - typecheck 통과, `export function GrowthMotionBars` 존재.
    - `grep -n "%" app/src/components/GrowthMotionBars.tsx` 매치 0건 (invariant — 모듈로·퍼센트 width 포함, Task 2 action의 대체 구현 규칙 참조).
    - `grep -n "declineBlue" ...` ≥ 1건 + `grep -n "colors.brand" ...` ≥ 1건 (D-06 색 배선), progressGreen/progressRed 매치 0건.
    - `grep -n "첫 기록" ...` ≥ 1건 (D-05 첫 기록 카피), `grep -n "점" ...`에 델타 포인트 표기 존재.
    - hex 하드코딩 매치 0건.
    - 임계/숨김 조건 분기 없음 — delta 값으로 행을 필터링하는 코드 부재 (D-06).
  </acceptance_criteria>
  <done>동작별 상승/하락 포인트 리스트가 주식창 색 관례·포인트 표기·전량 노출 규칙으로 렌더 가능 상태.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firestore 유래 문자열 → 홈 카드 렌더 | referenceMotionName 등 서버 저장 문자열이 RN Text로 렌더 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-06 | Tampering (표시 왜곡) | GrowthMotionBars label | mitigate | RN `<Text>`는 마크업 미해석(주입 표면 inert) + `numberOfLines={1}`로 레이아웃 남용 차단 |
| T-30-07 | DoS (렌더 예외) | 델타/평균 수치 | mitigate | 입력은 30-01 selector가 isFinite 필터 완료 — 컴포넌트는 null 델타 분기만 (첫 기록) |
| T-30-SC | Tampering | 패키지 설치 | accept | 신규 패키지 0건 (react-native-svg 기존 설치분만 — 차트 라이브러리 도입 금지, CONTEXT 재량 확정) |
</threat_model>

<verification>
- `npm run typecheck` GREEN.
- invariant grep: GrowthMotionBars + selector에 '%' 0건; GrowthChart는 `grep -n '%' app/src/components/GrowthChart.tsx | grep -v 'width="100%"'` 0건 (Svg 컨테이너 width 속성만 예외). progressGreen/Red 0건, hex 0건.
- 잠정 배선 계약: index.tsx에 `TODO(30-04` 주석 ≥ 1건 (30-04가 소거 책임).
- 신규 패키지 0: `git diff app/package.json` 무변경.
</verification>

<success_criteria>
- E1 화면 실체(주별 평균 선)와 E2 화면 실체(동작별 ▲▼ 포인트 리스트)가 컴포넌트로 존재.
- D-01/D-04(i)/D-05/D-06/D-09 표시 규칙이 코드·주석으로 추적 가능.
- 잠정 배선의 30-04 교체 계약이 TODO 주석으로 코드에 고정.
</success_criteria>

<output>
Create `.planning/phases/30-growth-tracking/30-03-SUMMARY.md` when done
</output>
