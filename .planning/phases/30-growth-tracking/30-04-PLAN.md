---
phase: 30-growth-tracking
plan: 04
type: execute
wave: 3
depends_on: ["30-01", "30-02", "30-03"]
# 30-HUMAN-UAT.md는 30-02 Task 3(wave 1)가 생성 — 본 플랜은 append만.
# 파일 중복은 wave 순서(1→3)로 직렬화 보장 (동일 wave 충돌 없음).
files_modified:
  - app/src/app/(tabs)/index.tsx
  - .planning/phases/30-growth-tracking/30-HUMAN-UAT.md
autonomous: true
requirements: [E1, E2]   # ROADMAP Requirements=TBD — PILOT-FEEDBACK §E1/E2 사용자 노출 층 (E2가 사용자에게 실제로 보이는 유일한 플랜)

must_haves:
  truths:
    - "홈 성장 카드에 [추이]/[동작별] 보기 전환 탭이 있고, 두 보기에서 카드 높이가 일정하다 (D-08)"
    - "[추이] 보기에 '프로 비교'/'내 기록' 모드 토글이 있고 선택 모드의 주별 평균만 그려진다 — 혼합 평균 렌더 경로 없음 (D-02)"
    - "모드 토글 기본값이 defaultGrowthMode(마지막 분석 모드 + 주별 점 2개 미만 시 타 모드 폴백)를 따르고, 활성 탭이 브랜드색으로 명확히 표시된다 (D-03)"
    - "[동작별] 보기에서 모드 토글이 숨겨지고 GrowthMotionBars 통합 리스트(프로 비교 동작 행들 + '내 기록' 행)가 렌더된다 (D-09)"
    - "'이번주 성장 그래프' 라벨이 주별 평균 의미에 맞는 카피로 정정된다 (D-01 재량)"
    - "양쪽 모드 모두 주별 점 2개 미만이면 GrowthLockedCard가 주별 기준에 맞게 정정된 카피로 표시된다 (D-03 null 분기)"
    - "30-03의 잠정 배선(`?? 'mode3'` + TODO(30-04-PLAN.md) 주석)이 토글 상태 기반 배선으로 교체되어 TODO가 사라진다"
  artifacts:
    - path: "app/src/app/(tabs)/index.tsx"
      provides: "GrowthCard 2층 토글(모드×보기) + GrowthChart/GrowthMotionBars 배선 + GrowthLockedCard 분기 (D-01/D-02/D-03/D-08/D-09)"
      contains: "GrowthMotionBars"
  key_links:
    - from: "app/src/app/(tabs)/index.tsx"
      to: "app/src/components/GrowthMotionBars.tsx"
      via: "[동작별] 탭 선택 시 rows={motionDeltas(analyses)} 렌더 (30-03 고아 아티팩트 해소)"
      pattern: "GrowthMotionBars"
    - from: "app/src/app/(tabs)/index.tsx"
      to: "app/src/lib/growthSelectors.ts"
      via: "defaultGrowthMode(기본 토글) / weeklyAverages(추이) / motionDeltas(동작별) 호출"
      pattern: "defaultGrowthMode|motionDeltas"
    - from: "app/src/app/(tabs)/index.tsx"
      to: "app/src/components/GrowthChart.tsx"
      via: "[추이] 탭 선택 시 points={weeklyAverages(analyses, effectiveMode)} 렌더"
      pattern: "weeklyAverages"
---

<objective>
Phase 30의 사용자 노출 층 — 홈 성장 카드를 2층 토글(모드 D-02/D-03 × 보기 D-08/D-09) 구조로 재작업하고, 30-01 selector와 30-03 컴포넌트를 실제 화면에 배선한다. 라벨 카피 정정(D-01)·GrowthLockedCard 양모드 부족 분기(D-03 null)·홈 헤더 "(평균 N점)" 재량 정리 포함. 마지막으로 OTA 발행 + 실기기 확인 항목을 HUMAN-UAT.md에 적립한다.

Purpose: E1(주별 평균 추이)·E2(동작별 ▲▼ 포인트)가 이 플랜에서 처음 사용자에게 노출된다 — 30-01/30-03 산출물은 이 배선 없이는 고아. 30-03이 남긴 잠정 배선(TODO(30-04-PLAN.md))을 교체하는 것이 플랜 간 계약.
Output: `app/src/app/(tabs)/index.tsx` GrowthCard 영역 재작업, production OTA, 30-HUMAN-UAT.md 항목 적립.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-growth-tracking/30-CONTEXT.md
@.planning/phases/30-growth-tracking/30-PATTERNS.md
@app/src/app/(tabs)/index.tsx
@app/src/components/BodyProfileForm.tsx
@app/src/lib/growthSelectors.ts
@app/src/components/GrowthMotionBars.tsx
@app/src/components/GrowthChart.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: GrowthCard 2층 토글 재작업 — 모드/보기 토글 + GrowthMotionBars 배선 + locked 분기 + 라벨 정정</name>
  <files>app/src/app/(tabs)/index.tsx</files>
  <read_first>
    - app/src/app/(tabs)/index.tsx (:58-64 averageScore, :71-73 recent/avg/hasGrowth, :200-205 성장 그래프 섹션 렌더, :292-313 GrowthCard/GrowthLockedCard 현행 + 30-03이 남긴 잠정 배선, :440-466 growthCard/growthLocked 스타일)
    - app/src/components/BodyProfileForm.tsx (:460-499 제네릭 Segmented — 프로젝트 유일 단일선택 세그먼트 선례, :562 chipSelected 브랜드 활성 스타일: colors.brandTint bg + colors.brand border)
    - app/src/lib/growthSelectors.ts (30-01 산출 계약 — weeklyAverages/defaultGrowthMode/motionDeltas 시그니처, WeeklyPoint/MotionDeltaRow)
    - app/src/components/GrowthMotionBars.tsx + GrowthChart.tsx (30-03 산출 — props 계약: rows: MotionDeltaRow[] / points: WeeklyPoint[])
  </read_first>
  <action>
    카드 레이아웃 세부(탭 배치·높이·빈 상태 비주얼)는 **Figma 우선 확인**(get_design_context, fileKey jrdI7kp245HkPfLB0nclsz — 성장 카드 노드) 후 미설계 시 design.md §5-4 카드 규칙 기반 자체 판단, 근거를 주석에 기록 (ui-figma-first).

    `GrowthCard`를 2층 토글 구조로 재작업한다 (30-CONTEXT D-02/D-03/D-08/D-09):

    1. **토글 UI (D-02/D-08):** BodyProfileForm의 제네릭 `Segmented` analog(30-PATTERNS :460-499)를 index.tsx 내 소형 named function helper로 이식 (컨벤션 — RecentAnalysisCard 선례. BodyProfileForm import 금지, 스타일 결합 방지). 보기 탭 = [추이]/[동작별] (D-08), 모드 토글 = '프로 비교'/'내 기록' (history.tsx modeBadge 카피 재사용, D-02). 활성 탭 스타일 = chipSelected 선례(`colors.brandTint` bg + `colors.brand` border/text) — D-03 "활성 상태 브랜드색 명확 표시" 요구. a11y 필수 세트: accessibilityRole="button" + accessibilityState={{ selected }} + 한국어 accessibilityLabel + hitSlop (Segmented 선례 그대로).

    2. **상태 설계 (D-03):** `const [view, setView] = useState<'trend' | 'byMotion'>('trend')` + `const [modeOverride, setModeOverride] = useState<AnalysisMode | null>(null)`. 유효 모드 = `modeOverride ?? defaultGrowthMode(analyses)` — 파생값으로 계산 (useState 초기값에 defaultGrowthMode를 넣지 말 것: analyses가 비동기 도착이라 stale 초기화됨. override는 사용자가 토글을 누른 뒤에만 non-null). 사용자가 직접 선택한 모드의 주별 점이 2개 미만이면 빈 차트 대신 인라인 안내 카피(재량, 예: "이 모드는 주별 데이터가 아직 부족해요")를 같은 높이 영역에 표시 — D-03 폴백은 기본값에만 적용, 명시 선택은 존중하되 빈 화면 금지.

    3. **보기 배선 (D-08/D-09):** [추이] = `<GrowthChart points={weeklyAverages(analyses, effectiveMode)} />` + 모드 토글 노출. [동작별] = `<GrowthMotionBars rows={motionDeltas(analyses)} />` + **모드 토글 숨김** (D-09 — 통합 리스트가 배지로 모드 구분, 행 내부 계산이라 혼합 왜곡 없음). motionDeltas 빈 배열이면 같은 높이의 빈 상태 안내 카피(재량). 집계 호출은 useMemo로 감쌈 (index.tsx :72 avg 선례). 30-03이 남긴 잠정 배선(`?? 'mode3'`)과 `TODO(30-04-PLAN.md)` 주석을 이 배선으로 교체·삭제한다.

    4. **카드 높이 일정 (D-08):** 두 보기의 콘텐츠 영역 높이를 고정(minHeight 또는 고정 height — GrowthChart H=132 기준, [동작별] 행 수 상한과 맞춤. 정확한 값은 Figma/재량, 근거 주석). 홈 스크롤 부담 없도록 보기 전환 시 카드 바깥 레이아웃이 안 움직이는 것이 요구사항.

    5. **라벨 정정 (D-01 재량):** growthHeader "이번주 성장 그래프" → 주별 평균 의미에 맞는 카피로 교체 (예: "주별 평균 성장 그래프" — 최종 카피 재량, 주석에 D-01 인용).

    6. **GrowthLockedCard 분기 (D-03 null):** 렌더 게이트를 현행 `analyses.length >= 2`(hasGrowth)에서 `defaultGrowthMode(analyses) !== null`(양쪽 모드 다 주별 점 2개 미만이면 locked)로 교체 — useMemo 파생. locked 카피 "분석을 2번 이상 하면"은 주별 평균 기준과 불일치(같은 주 2건 = 점 1개)하므로 주별 기준에 맞는 카피로 정정 (재량, 30-CONTEXT 재량 절 명시 항목). 단 [동작별] 데이터만 있는 경우도 locked로 묶는 단순 규칙 채택 — 추이가 성장 카드의 기본 보기이고 파일럿 데이터 규모에서 분기 복잡도가 비용 대비 낮음 (판단 근거 주석 박제).

    7. **홈 헤더 "(평균 N점)" (재량):** averageScore(:58-64)는 모드 혼합 누적 평균 — 그래프(D-02 분리)와 성격이 다름을 인지하고 유지 또는 정리 결정. 기본 권고 = 유지 (헤더는 "전체 누적" 맥락이라 그래프와 역할 구분 가능, 삭제는 정보 손실). 어느 쪽이든 결정 근거를 주석으로 박제.

    공통: 신규 스타일 전부 테마 토큰(colors/spacing/radius/typography) — 신규 hex 하드코딩 금지 (기존 잔존 hex(#EFEFEF 등 Figma 박제분)는 무접촉 — 스코프 밖 정리 금지). 증감 표기·집계는 전부 30-01/30-03 산출물 경유 — index.tsx에서 델타·평균을 재계산하거나 '%' 표기를 새로 만들지 않는다 (invariant는 selector/컴포넌트 층 게이트가 담당).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck && grep -c "GrowthMotionBars" "src/app/(tabs)/index.tsx" && grep -c "defaultGrowthMode" "src/app/(tabs)/index.tsx" && grep -c "motionDeltas" "src/app/(tabs)/index.tsx" && ! grep -n "TODO(30-04" "src/app/(tabs)/index.tsx" && ! grep -n "이번주 성장 그래프" "src/app/(tabs)/index.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - typecheck 통과.
    - index.tsx에 GrowthMotionBars/weeklyAverages/defaultGrowthMode/motionDeltas 배선 존재 (key_links 4종 grep 매치).
    - 30-03 잠정 배선의 `TODO(30-04` 주석 매치 0건 (교체 완료 증명).
    - "이번주 성장 그래프" 문자열 매치 0건 (D-01 라벨 정정).
    - [동작별] 보기 렌더 경로에 모드 토글이 포함되지 않음 — view === 'byMotion' 분기에서 모드 Segmented 미렌더 (D-09, 코드 리뷰로 확인).
    - GrowthLockedCard 렌더 조건이 defaultGrowthMode null 파생값 기반 (analyses.length 단독 조건 아님).
    - 신규 추가 라인에 hex 하드코딩 0건 (`git diff`의 + 라인 기준), 기존 잔존 hex 라인 무변경.
    - 토글 helper에 a11y 세트(accessibilityRole/State/Label + hitSlop) 존재.
  </acceptance_criteria>
  <done>홈 성장 카드가 [추이](모드 토글+주별 평균 선)/[동작별](통합 ▲▼ 리스트) 2층 토글로 완성되어 E1/E2가 사용자에게 노출되고, 잠정 배선·구 라벨이 소거됨.</done>
</task>

<task type="auto">
  <name>Task 2: production OTA 발행 + 실기기 확인 항목 HUMAN-UAT 적립</name>
  <files>.planning/phases/30-growth-tracking/30-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/30-growth-tracking/30-HUMAN-UAT.md (30-02 Task 3 생성분 — append만, 기존 항목 보존)
  </read_first>
  <action>
    (1) 앱 변경분은 전부 JS-only(신규 native 모듈 0)이므로 OTA 발행: `cd app && npx eas-cli update --branch production --message "phase 30: growth card weekly avg + per-motion deltas"` (phase 마감 시 production OTA 발행 관례 — Phase 27/28 선례). EAS 인증 실패/네트워크 불가 시 발행을 건너뛰고 SUMMARY deviation + 30-HUMAN-UAT.md ops 노트로 기록 (조용한 생략 금지).

    (2) 30-HUMAN-UAT.md에 실기기 확인 항목 append (batch UAT 원칙 — 즉시 belle 호출 금지, 적립만):
    - 홈 성장 카드 [추이]/[동작별] 탭 전환 — 카드 높이 불변(홈 레이아웃 안 움직임), 활성 탭 브랜드색 식별 (D-08/D-03)
    - [추이] 모드 토글 기본값 = 마지막 분석 모드 (해당 모드 주별 점 부족 시 타 모드 폴백 동작) (D-03)
    - [추이] 선이 주별 평균 점 + 주 시작일 라벨로 그려짐 — raw 건별 나열 아님 (D-01)
    - [동작별] 리스트 — '프로 비교'/'내 기록' 배지 구분, ▲=브랜드레드/▼=파랑, '+N점/−N점' 포인트 표기, 첫 기록 동작은 "첫 기록 N점 (비교 전)" (D-05/D-06/D-09)
    - [동작별] 보기에서 모드 토글 미노출 (D-09)
    - 분석 부족 상태에서 정정된 locked 카피 표시 (D-03 null 분기)
  </action>
  <verify>
    <automated>test -f /Users/kimtaesung/Dev/SunityMotion/.planning/phases/30-growth-tracking/30-HUMAN-UAT.md && grep -c "동작별" /Users/kimtaesung/Dev/SunityMotion/.planning/phases/30-growth-tracking/30-HUMAN-UAT.md</automated>
    <conditional>OTA 발행을 실행한 경우에만 추가 게이트: `cd app && npx eas-cli update:list --branch production --limit 1`에 phase 30 메시지의 최신 업데이트 존재. 발행 보류(인증/네트워크 불가) 시 이 절은 생략하고 30-HUMAN-UAT.md 보류 사유 기록으로 대체 — 두 절을 분리 서술한 것은 의도(발행 확인 실패가 무조건 게이트를 조용히 통과하는 ';' 연결 형태 회피).</conditional>
  </verify>
  <acceptance_criteria>
    - OTA 발행 성공 시: update:list에 phase 30 업데이트 확인. 발행 불가 시: SUMMARY deviation + 30-HUMAN-UAT.md 보류 사유 기록 (둘 중 하나 반드시 충족).
    - 30-HUMAN-UAT.md에 위 6개 확인 항목 존재, 30-02가 적립한 기존 항목(Pod 재가동 recognizedMotionId 확인) 무손상.
  </acceptance_criteria>
  <done>변경분이 OTA로 배포(또는 보류 사유 박제)되고, phase 30 실기기 확인 항목이 batch UAT로 적립됨.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firestore 유래 문자열 → 홈 카드 렌더 | referenceMotionName 등 서버 저장 문자열이 홈 카드 RN Text로 렌더 (30-03 T-30-06과 동일 표면 — inert/text-only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-08 | Tampering (표시 왜곡) | 홈 카드 동작명/배지 렌더 | mitigate | RN `<Text>` 마크업 미해석(주입 표면 inert) + 라벨은 30-03 GrowthMotionBars의 numberOfLines={1} 경유 — index.tsx에서 raw 문자열 직접 조립 금지 |
| T-30-09 | DoS (렌더 예외) | 토글 상태 × 데이터 부족 조합 | mitigate | effectiveMode null → GrowthLockedCard, 명시 선택 모드 점 부족·motionDeltas 빈 배열 → 동일 높이 안내 카피 — 모든 조합에서 빈 화면/예외 없는 분기 명시 (Task 1 action 2·3·6) |
| T-30-SC | Tampering | 패키지 설치 | accept | 신규 패키지 설치 0건 — 기존 react-native-svg/expo 스택만 |
</threat_model>

<verification>
- `cd app && npm run typecheck` GREEN (프로젝트 유일 정적 게이트).
- key_links grep: index.tsx에 GrowthMotionBars/weeklyAverages/defaultGrowthMode/motionDeltas 전부 매치.
- 잠정 배선 소거: `TODO(30-04` 매치 0건 + "이번주 성장 그래프" 매치 0건.
- `git diff app/package.json` 무변경 (신규 패키지 0).
</verification>

<success_criteria>
- D-01(라벨)/D-02/D-03/D-08/D-09가 코드로 구현되고 각 결정 ID가 주석으로 추적 가능.
- E1/E2가 홈 화면에서 사용자에게 실제 노출 — 30-01 selector·30-03 컴포넌트의 고아 상태 해소.
- OTA 발행(또는 보류 사유) + 실기기 확인 항목 HUMAN-UAT 적립.
</success_criteria>

<output>
Create `.planning/phases/30-growth-tracking/30-04-SUMMARY.md` when done
</output>
