---
phase: 31-api-visual-correction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/31-api-visual-correction/smoke/image_smoke.py
  - .planning/phases/31-api-visual-correction/smoke/RESULTS.json
  - .planning/phases/31-api-visual-correction/31-ACCEPTANCE.md
autonomous: false
requirements: [D-02, D-03, D-05]
must_haves:
  truths:
    - "외부 벤더(DashScope) 전송·보존·고지·학습 페어 정책이 belle 승인 release gate 로 확정된다 (리뷰 H-04 — UI 결정이 아니라 release gate)"
    - "이미지 편집 모델이 실호출로 실측되고 결과가 RESULTS.json 에 박제된다 (D-03 — RESEARCH A1~A3 해소)"
    - "RESULTS.json.blocked=true 면 라이브 롤아웃이 차단된다 — 31-12 배포 게이트가 이 값을 읽어 feature flag OFF 를 유지한다 (리뷰 H-03)"
    - "측정 가능한 시각 품질 임계값과 라벨된 fixture 셋이 31-ACCEPTANCE.md 에 선언된다 (리뷰 Wave 0-4)"
    - "스모크 이미지가 추적되는 .planning 디렉터리에 저장되지 않는다 — scratch 전용, RESULTS.json 은 redacted 메트릭만 (리뷰 H-04)"
  artifacts:
    - path: ".planning/phases/31-api-visual-correction/smoke/RESULTS.json"
      provides: "chosen_model + endpoint + params + usage 실측 + blocked 판정"
      contains: "chosen_model"
    - path: ".planning/phases/31-api-visual-correction/31-ACCEPTANCE.md"
      provides: "판정 축·임계값(display/training 분리)·라벨 fixture 목록·privacy 결정 기록"
      contains: "Training"
  key_links:
    - from: ".planning/phases/31-api-visual-correction/smoke/RESULTS.json"
      to: "31-12 배포 게이트"
      via: "blocked=true → feature flag OFF 유지"
      pattern: "blocked"
---

<objective>
리뷰의 Wave 0 을 실행한다: (1) 외부 처리·개인정보 release gate (H-04) — DashScope 전송 고지/보존/DPA, consent 버전 체계, 페어 가명화(HMAC)/삭제 경로/얼굴 블러 여부를 belle checkpoint:decision 으로 확정. (2) 이미지 모델 스모크 실측 (D-03, fixture 입력만, 산출 이미지는 scratch 전용). (3) 측정 가능한 품질 임계값 + 라벨 fixture 셋 선언 — display 게이트와 training 적재 게이트를 분리(training 이 더 엄격, 리뷰 H-03). blocked=true 면 31-12 가 라이브 롤아웃을 차단한다.

D-10 재결정은 이미 완료(31-CONTEXT.md [AMENDED 2026-07-19] — 2D 비교 뷰어 확정, 리뷰 B-01 해소) — 재오픈 태스크 없음.

Purpose: 잘못된 가정 위에서 구현 wave 가 출발하는 것을 차단 (리뷰 최종 권고 — 첫 결정 = 외부 처리/privacy 계약).
Output: privacy 결정 기록 + smoke/RESULTS.json + 31-ACCEPTANCE.md.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-api-visual-correction/31-CONTEXT.md
@.planning/phases/31-api-visual-correction/31-RESEARCH.md
@.planning/spikes/004-gemini-omni-view-editing/README.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: 외부 처리·개인정보 release gate (리뷰 H-04)</name>
  <decision>DashScope 외부 전송 고지·보존 정책, 페어 적재 가명화/삭제/블러, 스모크 지출 승인</decision>
  <read_first>.planning/phases/31-api-visual-correction/31-REVIEWS.md 의 H-04 절 (요구 항목 원문), backend/training/datagen/enumerate_internal.py lines 87-110 (consent_allows — 현행 동의 게이트), .planning/spikes/004-gemini-omni-view-editing/README.md (spike 008 — DashScope 실사용 이력)</read_first>
  <context>D-05 는 분석마다 사용자 신체/얼굴 프레임을 DashScope 로 자동 전송한다 — learningOptIn 과 별개의 제3자 처리 목적 (리뷰 H-04: 학습 동의와 외부 처리 고지는 법적·제품적으로 다른 목적). 결정 전 Claude 가 WebFetch 로 Alibaba Model Studio 데이터 보존/학습 사용 정책을 확인해 요약 제시한다. 결정 항목: (a) 앱/약관 고지 문구 방향 (제3자 처리 벤더 명시 여부), (b) consent 기록 체계 = consentVersion 'pilot-optout-v1' + capturedAtMs 채택 여부, (c) 페어 저장 정책 — pairId = HMAC-SHA256(SSM 비밀키, uid:analysisId:joint) 가명화(원 analysisId/uid 를 S3 key/메타에 미기록) + 삭제 스크립트 + visual-input/ 1일 lifecycle + 처리 후 즉시 삭제, (d) 원본(before) 프레임 얼굴 블러 여부 (구 Open Q2), (e) 스모크 실호출 지출 승인 (최대 4콜, 정은지 fixture 프레임 사용 — 리뷰 L-01 비용 checkpoint).</context>
  <options>
    <option id="option-a">
      <name>권고안 일괄 승인 (블러 없음 + HMAC 가명화 + 1일 lifecycle + 삭제 스크립트 + 스모크 승인)</name>
      <pros>학습 품질 보존 + 가명화/삭제/단기보존으로 통제. 리뷰 H-04 요구 전부 충족</pros>
      <cons>원본 얼굴이 페어 저장소에 남음 (동의·가명화 범위 내)</cons>
    </option>
    <option id="option-b">
      <name>블러 적용 변형 (before 프레임 anonymize_batch 얼굴 블러 후 적재, 나머지 동일)</name>
      <pros>PII 최소화 일관 (내부 데이터 선례와 동일)</pros>
      <cons>블러 아티팩트가 phase 22 v2 생성 헤드 학습 품질 저하 가능</cons>
    </option>
  </options>
  <resume-signal>option-a 또는 option-b 선택 (+ 고지 문구 방향 지시가 있으면 함께)</resume-signal>
  <action>WebFetch 로 DashScope/Model Studio 데이터 정책 요약을 먼저 확보해 제시하고, (a)~(e) 를 AskUserQuestion 으로 belle 에게 결정받는다. 결정 내용은 Task 3 에서 31-ACCEPTANCE.md 의 "## Privacy Release Gate" 절에 기록한다 — 후속 플랜(31-07 pair_store, 31-10 lifecycle)의 구현 기준. 승인 전 Task 2 착수 금지 (스모크 지출이 이 게이트에 포함 — 리뷰 L-01).</action>
  <verify>
    <automated>N/A - checkpoint:decision (belle 응답이 게이트)</automated>
  </verify>
  <acceptance_criteria>
    - belle 이 option-a 또는 option-b 를 명시적으로 선택
    - DashScope 데이터 정책 요약(보존/학습 사용 여부)이 결정 제시에 포함됨
  </acceptance_criteria>
  <done>외부 처리·페어 저장 정책이 release gate 로 확정되어 31-07/31-10 의 구현 기준이 됨.</done>
</task>

<task type="auto">
  <name>Task 2: 이미지 편집 모델 스모크 실호출 (산출물은 scratch 전용)</name>
  <files>.planning/phases/31-api-visual-correction/smoke/image_smoke.py</files>
  <read_first>.planning/spikes/004-gemini-omni-view-editing/wan_gate_batch.py (http_json 헬퍼 lines 24-56 + 인증 헤더 + 비동기 task 패턴), .planning/spikes/004-gemini-omni-view-editing/README.md (spike 008 절 — 모더레이션 응답 형상, 입력 소재 위치)</read_first>
  <action>stdlib urllib 만 사용하는 단발 스크립트 작성 (requests 금지 — 프로젝트 관례). 키 로드 = `aws ssm get-parameter --name /sunity/motion/dashscope-api-key --with-decryption --profile sunity-motion` 로 env DASHSCOPE_API_KEY 주입 — 하드코딩·stdout 로깅 금지. 입력 = spike 004 디렉터리의 정은지/폴 동작 fixture 프레임 1장 (없으면 S3 fixtures/ 에서 1장, AWS_PROFILE=sunity-motion) → S3 임시 key `visual-input/_smoke/src.png` put + presign 1h. 후보: (1) `wan2.7-image-pro` (dashscope-intl — WebFetch 로 alibabacloud.com/help/en/model-studio/models 에서 이미지 편집 endpoint 확인), (2) `qwen-image-edit-plus` (동기 API — qwen-image-edit-guide, 출력 URL 24h). 프롬프트: "Correct the athlete's left knee to full extension (about 175 degrees). Keep the person's identity, clothing, the pole, and the background unchanged." 호출 상한 = 모델당 2회(1+재시도 1), 총 4회 — 스크립트 상수 MAX_CALLS = 4 로 고정. 출력 이미지는 반드시 세션 scratch 디렉터리(/tmp 계열)에만 저장 — .planning 하위 저장 금지 (리뷰 H-04: 추적 디렉터리에 실 인물 이미지 금지). 각 호출의 HTTP status·latency·usage·모더레이션 여부만 수집. 종료 시 S3 임시 key delete_object 정리.</action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('.planning/phases/31-api-visual-correction/smoke/image_smoke.py').read())"</automated>
  </verify>
  <acceptance_criteria>
    - image_smoke.py 존재·파싱 가능, `dashscope-api-key` SSM 이름 참조, 키 리터럴 0 (grep 으로 "sk-" 류 리터럴 0건)
    - 최소 1개 모델 실호출 수행 + HTTP status 기록
    - `find .planning/phases/31-api-visual-correction -name "*.png" -o -name "*.jpg"` 결과 0건 (인물 이미지 미추적)
    - 스크립트에 S3 임시 객체 delete_object 정리 호출 존재
  </acceptance_criteria>
  <done>두 후보 모델의 실측 데이터(또는 실패 증거)가 확보됨 — 이미지는 scratch 에만.</done>
</task>

<task type="auto">
  <name>Task 3: RESULTS.json 판정 + 31-ACCEPTANCE.md (임계값·fixture·privacy 기록)</name>
  <files>.planning/phases/31-api-visual-correction/smoke/RESULTS.json, .planning/phases/31-api-visual-correction/31-ACCEPTANCE.md</files>
  <read_first>Task 2 의 scratch 출력 이미지 (Read 툴로 시각 확인), .planning/phases/31-api-visual-correction/31-RESEARCH.md (§Standard Stack 판정 기준 A1~A3), .planning/spikes/004-gemini-omni-view-editing/wan_out/ (라벨 fixture 후보 — spike 산출 이미지)</read_first>
  <action>출력 이미지를 Read 로 직접 보고 판정: (a) 신원·복장·폴·배경 보존, (b) 지시 교정 반영, (c) 왜곡 여부. RESULTS.json 스키마: chosen_model(str), endpoint(str), request_params(dict), sync(bool), latency_s(number), usage(dict), moderation_blocked(bool), blocked(bool), notes(str), measured_at(str) — presigned URL·이미지 데이터·서명 포함 금지 (redacted 메트릭만). 둘 다 실패 시 blocked=true + 사유. notes 에 blocked=true 의 효력을 명시: 31-12 배포 게이트가 이 값을 읽어 VisualJobsEnabled flag OFF 유지 + phase 미완료 (리뷰 H-03 — mock-only 는 품질 게이트가 아님). 31-ACCEPTANCE.md 작성: (1) "## Display 게이트" — before/after judge 축(identity_ok/clothing_ok/background_ok/pole_ok/single_person_ok/no_extra_limbs/correction_visible) 전부 true + confidence >= 0.7 + 결정론 pose 게이트 |측정각−목표각| <= 15° (초기 선언값, env 로 조정 가능), (2) "## Training 적재 게이트" — display 게이트 + pose 오차 <= 8° + judge confidence >= 0.85 (training 이 더 엄격 — 리뷰 H-03), (3) "## 라벨 fixture 셋" — spike 004 wan_out 이미지 + 스모크 산출의 PASS/FAIL 라벨 표 (경로 참조만 — 신규 인물 이미지 복사 금지), (4) "## Privacy Release Gate" — Task 1 결정 전문 기록 (option/고지 방향/HMAC/블러/lifecycle/삭제 경로/consentVersion), (5) "## Hard Rejection" — 인물 부재/추가 인물·사지/폴 기하 붕괴/판정 불능 = 무조건 FAIL.</action>
  <verify>
    <automated>python -c "import json; d=json.load(open('.planning/phases/31-api-visual-correction/smoke/RESULTS.json')); assert set(['chosen_model','endpoint','request_params','sync','blocked']) <= set(d)" && grep -q "Training" .planning/phases/31-api-visual-correction/31-ACCEPTANCE.md</automated>
  </verify>
  <acceptance_criteria>
    - RESULTS.json 이 chosen_model/endpoint/request_params/sync/blocked 키 포함, dashscope 서명 URL 미포함
    - 31-ACCEPTANCE.md 에 Display/Training 두 게이트가 서로 다른 수치 임계값으로 선언됨
    - Privacy Release Gate 절에 Task 1 결정이 기록됨
    - 라벨 fixture 표에 PASS 와 FAIL 예시가 각 1개 이상
  </acceptance_criteria>
  <done>31-05(judge)/31-06(pose 게이트)/31-07(pair)/31-12(배포 게이트)가 미실측 가정 0 으로 구현 가능한 기준 확보.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로컬 → DashScope | fixture 프레임이 외부 벤더로 전송 (belle 승인 게이트 내) |
| SSM → 로컬 셸 | SecureString 키의 로컬 취급 |
| 산출 이미지 → git | 실 인물 이미지의 저장 위치 경계 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Information Disclosure | image_smoke.py 키 취급 | mitigate | 키는 env/SSM 경유만 — 스크립트·RESULTS.json 에 키/서명 URL 리터럴 금지 (acceptance grep) |
| T-31-02 | Information Disclosure | 스모크 산출 이미지 | mitigate | scratch 전용 저장 + .planning 하위 이미지 0 을 acceptance 로 고정 (리뷰 H-04) |
| T-31-03 | Denial of Service (과금) | 실호출 반복 | mitigate | MAX_CALLS=4 상수 + belle 지출 승인 checkpoint (리뷰 L-01) |
| T-31-04 | Privacy | 외부 처리 고지 부재 | mitigate | Task 1 release gate — 고지/보존/삭제 정책 승인 전 구현 wave 진입 금지 (리뷰 H-04) |
</threat_model>

<verification>
- RESULTS.json 스키마 assert 통과 + blocked 값 판정 기록
- 31-ACCEPTANCE.md 5개 절 존재
- .planning 하위 인물 이미지 0
</verification>

<success_criteria>
- 리뷰 Wave 0 항목 2·3·4 완료 (privacy gate, 스모크+blocked 규칙, 임계값+fixture)
- D-03 모델 확정 또는 blocked 박제 (폴백/차단 규칙 포함)
</success_criteria>

<output>
Create `.planning/phases/31-api-visual-correction/31-01-SUMMARY.md` when done
</output>
