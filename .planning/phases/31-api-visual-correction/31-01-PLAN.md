---
phase: 31-api-visual-correction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/31-api-visual-correction/smoke/image_smoke.py
  - .planning/phases/31-api-visual-correction/smoke/RESULTS.json
  - .planning/phases/31-api-visual-correction/smoke/CALIBRATION.json
  - .planning/phases/31-api-visual-correction/smoke/privacy_decision.json
  - .planning/phases/31-api-visual-correction/31-ACCEPTANCE.md
autonomous: false
requirements: [D-02, D-03, D-05]
must_haves:
  truths:
    - "외부 벤더(DashScope) 전송·보존·고지·학습 페어 정책이 belle 승인 release gate 로 확정된다 (리뷰 H-04)"
    - "privacy 결정이 machine-readable privacy_decision.json 으로 방출된다 — downstream(31-07/31-10)이 이 값을 assert, 하드코딩 drift 차단 (2차 리뷰 M2-05)"
    - "페어 보존기간이 숫자(일)로 확정된다 — 기본 제안 180일, deleteAfter lifecycle 대상 (2차 리뷰 H2-06)"
    - "privacy option B(블러) 선택 시 실행 위치는 Pod frame 생성 시점 — Lambda 내 blur 실행은 설계에서 배제 (2차 리뷰 H2-10, §8 gate 13)"
    - "이미지 편집 모델이 실호출로 실측되고 결과가 RESULTS.json 에 박제된다 (D-03)"
    - "display/training 임계값은 선언이 아니라 라벨된 calibration set 의 false accept/reject 결과표에서 선택된다 — calibration 미달도 blocked=true (2차 리뷰 H2-07, §8 gate 14)"
    - "RESULTS.json.blocked=true 면 라이브 롤아웃 차단 — 31-12 게이트가 flag OFF 유지 (리뷰 H-03)"
    - "스모크/calibration 이미지가 추적 디렉터리에 저장되지 않는다 — scratch 전용 (리뷰 H-04)"
  artifacts:
    - path: ".planning/phases/31-api-visual-correction/smoke/RESULTS.json"
      provides: "chosen_model + endpoint + params + usage/latency 실측 + blocked 판정"
      contains: "chosen_model"
    - path: ".planning/phases/31-api-visual-correction/smoke/CALIBRATION.json"
      provides: "라벨 fixture 별 judge/pose 측정 + false accept/reject 표 + 채택 임계값 (display/training)"
      contains: "chosen"
    - path: ".planning/phases/31-api-visual-correction/smoke/privacy_decision.json"
      provides: "consentVersion/blurOption/retentionDays/hmacKeyVersioning — downstream assert 대상"
      contains: "retentionDays"
    - path: ".planning/phases/31-api-visual-correction/31-ACCEPTANCE.md"
      provides: "판정 축·calibration 기반 임계값·라벨 fixture 목록·privacy 결정 기록"
      contains: "Training"
  key_links:
    - from: ".planning/phases/31-api-visual-correction/smoke/RESULTS.json"
      to: "31-12 배포 게이트"
      via: "blocked=true → feature flag OFF 유지"
      pattern: "blocked"
    - from: ".planning/phases/31-api-visual-correction/smoke/privacy_decision.json"
      to: "31-07 pair_store / 31-10 lifecycle dry-run"
      via: "machine-readable 결정값 assert (M2-05)"
      pattern: "retentionDays"
---

<objective>
리뷰 Wave 0 + 2차 리뷰 H2-06/H2-07/H2-10/M2-05 를 실행한다: (1) 외부 처리·개인정보 release gate (H-04) — 보존기간 숫자 확정(H2-06)과 option B 실행 위치(H2-10) 포함, 결정은 machine-readable privacy_decision.json 으로 방출(M2-05). (2) 이미지 모델 스모크 실측 (D-03). (3) **calibration 기반 임계값 결정 (H2-07)** — 관절/좌우/직립·도립/가림/모션블러를 포함한 라벨 fixture 셋에 judge/pose 측정을 돌려 false accept/false reject 결과표를 만들고, display(false accept 우선 최소화)/training(더 엄격) 임계값을 표에서 선택한다. 15°/8°/0.7/0.85 는 선언값이 아니라 calibration 이 대체·확정한다. calibration 미달(항목 부족/측정 불가)도 blocked=true.

D-10 재결정은 이미 완료(31-CONTEXT.md [AMENDED 2026-07-19]) — 재오픈 태스크 없음.

Purpose: 잘못된 가정 위에서 구현 wave 가 출발하는 것을 차단 — 임계값·privacy 값이 측정과 승인에서 나온 상태로.
Output: privacy_decision.json + smoke/RESULTS.json + CALIBRATION.json + 31-ACCEPTANCE.md.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-api-visual-correction/31-CONTEXT.md
@.planning/phases/31-api-visual-correction/31-RESEARCH.md
@.planning/spikes/004-gemini-omni-view-editing/README.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: 외부 처리·개인정보 release gate (H-04 + H2-06/H2-10)</name>
  <decision>DashScope 외부 전송 고지·보존 정책, 페어 적재 가명화/삭제/블러/보존일수, 스모크·calibration 지출 승인</decision>
  <read_first>.planning/phases/31-api-visual-correction/31-REVIEWS.md 의 H-04 절 + 31-PLAN-REVIEW-ITERATION2.md 의 H2-06/H2-10 절, backend/training/datagen/enumerate_internal.py lines 87-110 (consent_allows — 현행 동의 게이트), .planning/spikes/004-gemini-omni-view-editing/README.md (spike 008 — DashScope 실사용 이력)</read_first>
  <context>D-05 는 분석마다 사용자 신체/얼굴 프레임을 DashScope 로 자동 전송한다 — learningOptIn 과 별개의 제3자 처리 목적. 결정 전 Claude 가 WebFetch 로 Alibaba Model Studio 데이터 보존/학습 사용 정책을 확인해 요약 제시. 결정 항목: (a) 앱/약관 고지 문구 방향, (b) consent 기록 체계 = consentVersion 'pilot-optout-v1' + capturedAtMs 채택 여부, (c) 페어 저장 정책 — pairId = HMAC-SHA256 가명화(**versioned key set — SSM /sunity/motion/pair-id-hmac-keys JSON {"active","keys"}; 회전 후에도 retired key 로 삭제 가능, 2차 리뷰 H2-06**) + 삭제 스크립트 + visual-input/ 1일 lifecycle + 처리 후 즉시 삭제, **(c') 페어 보존일수 숫자 확정 — 기본 제안 180일 (H2-06: "phase 22 트랙 활성 기간" 은 retention policy 가 아님; lifecycle Expiration 으로 강제)**, (d) 원본(before) 프레임 얼굴 블러 여부 — **option-b 선택 시 blur 는 Pod 의 source frame 생성 시점에 별도 training-before artifact 로 수행 (Lambda 에서 anonymize_batch 실행 불가 — video runner + ultralytics/ffmpeg 의존, 2차 리뷰 H2-10). option-a 확정 시 downstream 의 B 분기 코드는 계획에서 제거**, (e) 스모크+calibration 실호출 지출 승인 (생성 최대 8콜 — 모델당 4, fixture 다양화; judge/pose calibration 호출은 생성 아닌 판정 비용).</context>
  <options>
    <option id="option-a">
      <name>권고안 일괄 승인 (블러 없음 + versioned HMAC + 보존 180일 + 1일 lifecycle + 삭제 스크립트 + 지출 승인)</name>
      <pros>학습 품질 보존 + 가명화/삭제/기한부 보존으로 통제. H-04 + H2-06 요구 전부 충족. downstream B 분기 제거로 단순화</pros>
      <cons>원본 얼굴이 페어 저장소에 보존기간 동안 남음 (동의·가명화·기한 내)</cons>
    </option>
    <option id="option-b">
      <name>블러 변형 (Pod frame 생성 시점 blur 로 training-before artifact 별도 생성, 나머지 동일)</name>
      <pros>PII 최소화 일관. Lambda 실행 불가 문제는 Pod 시점 blur 로 해소 (H2-10)</pros>
      <cons>블러 아티팩트가 phase 22 v2 학습 품질 저하 가능 + Pod 측 구현 추가</cons>
    </option>
  </options>
  <resume-signal>option-a 또는 option-b + 보존일수 확정(기본 180일) (+ 고지 문구 방향 지시가 있으면 함께)</resume-signal>
  <action>WebFetch 로 DashScope/Model Studio 데이터 정책 요약을 먼저 확보해 제시하고, (a)~(e) 를 AskUserQuestion 으로 belle 에게 결정받는다. **결정 즉시 `smoke/privacy_decision.json` 작성 (M2-05 — downstream 의 단일 기준): {"consentVersion": str, "blurOption": "none"|"pod_blur", "retentionDays": int, "hmacKeyParam": "/sunity/motion/pair-id-hmac-keys", "hmacKeyVersioning": true, "noticeDirection": str, "decidedAtMs": int}** — 31-07 은 CONSENT_VERSION 하드코딩 대신 이 파일 값과 일치를 테스트로 assert, 31-10 dry-run 은 retentionDays 로 lifecycle 규칙 생성. 결정 전문은 Task 3 에서 31-ACCEPTANCE.md "## Privacy Release Gate" 에도 기록. 승인 전 Task 2 착수 금지.</action>
  <verify>
    <automated>N/A - checkpoint:decision (belle 응답이 게이트)</automated>
  </verify>
  <acceptance_criteria>
    - belle 이 option 명시 선택 + 보존일수 숫자 확정
    - privacy_decision.json 이 consentVersion/blurOption/retentionDays 포함 (M2-05)
    - DashScope 데이터 정책 요약이 결정 제시에 포함됨
  </acceptance_criteria>
  <done>외부 처리·페어 저장 정책이 machine-readable release gate 로 확정 — 31-07/31-10/31-12 의 구현 기준.</done>
</task>

<task type="auto">
  <name>Task 2: 이미지 편집 모델 스모크 실호출 (산출물은 scratch 전용)</name>
  <files>.planning/phases/31-api-visual-correction/smoke/image_smoke.py</files>
  <read_first>.planning/spikes/004-gemini-omni-view-editing/wan_gate_batch.py (http_json 헬퍼 lines 24-56 + 인증 헤더 + 비동기 task 패턴), .planning/spikes/004-gemini-omni-view-editing/README.md (spike 008 절 — 모더레이션 응답 형상, 입력 소재 위치)</read_first>
  <action>stdlib urllib 만 사용하는 단발 스크립트 작성 (requests 금지). 키 로드 = `aws ssm get-parameter --name /sunity/motion/dashscope-api-key --with-decryption --profile sunity-motion` 로 env DASHSCOPE_API_KEY 주입 — 하드코딩·stdout 로깅 금지. 입력 = **fixture 프레임 4종 (H2-07 표본 확대: 직립 1·도립 1·좌/우 결함 각 1 — spike 004 디렉터리 또는 S3 fixtures/, AWS_PROFILE=sunity-motion)** → S3 임시 key `visual-input/_smoke/` put + presign 1h. 후보: (1) `wan2.7-image-pro` (dashscope-intl — WebFetch 로 이미지 편집 endpoint 확인), (2) `qwen-image-edit-plus` (동기 API, 출력 URL 24h). 프롬프트: "Correct the athlete's left knee to full extension (about 175 degrees). Keep the person's identity, clothing, the pole, and the background unchanged." (관절/방향은 fixture 별 치환). 호출 상한 = **모델당 4회, 총 8회 — MAX_CALLS = 8 상수 고정 (Task 1 승인 범위)**. sync 모델은 latency 를 콜마다 기록 (**B2-02 의 sync-only 허용 조건 = latency 실측 근거 — RESULTS.json 에 p95 추정 기록**). 출력 이미지는 세션 scratch 디렉터리에만 저장 — .planning 하위 금지. 각 호출의 HTTP status·latency·usage·모더레이션 여부만 수집. 종료 시 S3 임시 key delete_object 정리.</action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('.planning/phases/31-api-visual-correction/smoke/image_smoke.py').read())"</automated>
  </verify>
  <acceptance_criteria>
    - image_smoke.py 존재·파싱 가능, SSM 이름 참조, 키 리터럴 0 ("sk-" 류 grep 0건)
    - MAX_CALLS = 8 상수 + fixture 4종 입력 경로
    - `find .planning/phases/31-api-visual-correction -name "*.png" -o -name "*.jpg"` 0건
    - S3 임시 객체 delete_object 정리 호출 존재
  </acceptance_criteria>
  <done>두 후보 모델의 실측 데이터(다양화 표본 + latency)가 확보됨 — 이미지는 scratch 에만.</done>
</task>

<task type="auto">
  <name>Task 3: calibration 측정 + RESULTS/CALIBRATION.json + 31-ACCEPTANCE.md (H2-07)</name>
  <files>.planning/phases/31-api-visual-correction/smoke/RESULTS.json, .planning/phases/31-api-visual-correction/smoke/CALIBRATION.json, .planning/phases/31-api-visual-correction/31-ACCEPTANCE.md</files>
  <read_first>Task 2 scratch 출력 이미지 (Read 로 시각 확인), .planning/phases/31-api-visual-correction/31-RESEARCH.md (§Standard Stack 판정 기준 A1~A3), .planning/spikes/004-gemini-omni-view-editing/wan_out/ + kpts*/ (라벨 fixture 후보 — spike 산출 이미지와 keypoint 실측)</read_first>
  <action>(1) RESULTS.json: chosen_model/endpoint/request_params/sync/latency_s(콜별 배열 + p95 추정)/usage/moderation_blocked/blocked/notes/measured_at — presigned URL·이미지 데이터 금지. 둘 다 실패 시 blocked=true. notes 에 blocked 효력 명시 (31-12 게이트 → flag OFF 유지 + phase 미완료).
(2) **CALIBRATION.json (H2-07 — §8 gate 14)**: 라벨 fixture 셋 구성 — 최소 12항목: spike 004 wan_out/스모크 산출/정은지 fixture 에서 관절 2종 이상 × 좌/우 × 직립/도립 + 가림 1+ + 모션블러 1+ 를 커버. 각 항목: Claude 가 Read 로 시각 판정한 PASS/FAIL 라벨 + 기대 target 각도(육안+RTMW keypoint 실측 기준) 기록 → judge 프롬프트(31-05 와 동일 축)와 pose 측정(로컬 각도 계산 — Pod 불요 시 spike kpts 실측 재사용)을 돌려 항목별 (judge confidence, pose error) 수집 → **후보 임계값 격자(pose tol 8/10/15/20°, confidence 0.6/0.7/0.8/0.85)별 false accept/false reject 표 산출 → display = false accept 최소화 우선으로 선택, training = display 보다 엄격한 조합 선택. 채택값을 {"chosen": {"display_pose_tol_deg", "display_confidence", "training_pose_tol_deg", "training_confidence"}, "table": [...], "fixtures": [...]} 로 기록.** fixture 항목 부족/측정 불가 시 calibration 미달 → RESULTS.json blocked=true (H2-07: 모델 성공만으론 부족).
(3) 31-ACCEPTANCE.md: "## Display 게이트" — judge 7축 전부 true + CALIBRATION chosen 의 display 임계값, "## Training 적재 게이트" — display + chosen training 임계값 (training 이 더 엄격), "## Calibration 결과표" — false accept/reject 표 전문 + 선택 근거, "## 라벨 fixture 셋" — 경로 참조만 (신규 인물 이미지 복사 금지), "## Privacy Release Gate" — Task 1 결정 전문 (privacy_decision.json 값과 동일), "## Hard Rejection" — 인물 부재/추가 인물·사지/폴 기하 붕괴/판정 불능 = 무조건 FAIL.
31-05(judge 상수)/31-10(워커 env DISPLAY_POSE_TOL_DEG·TRAINING_POSE_TOL_DEG) 은 CALIBRATION.json chosen 값을 소비 — 대조 테스트가 31-05 에 존재.</action>
  <verify>
    <automated>python -c "import json; d=json.load(open('.planning/phases/31-api-visual-correction/smoke/RESULTS.json')); assert set(['chosen_model','endpoint','request_params','sync','blocked']) <= set(d); c=json.load(open('.planning/phases/31-api-visual-correction/smoke/CALIBRATION.json')); assert 'chosen' in c and 'table' in c; p=json.load(open('.planning/phases/31-api-visual-correction/smoke/privacy_decision.json')); assert set(['consentVersion','blurOption','retentionDays']) <= set(p)" && grep -q "Training" .planning/phases/31-api-visual-correction/31-ACCEPTANCE.md</automated>
  </verify>
  <acceptance_criteria>
    - RESULTS.json 스키마 + latency 배열, dashscope 서명 URL 미포함
    - CALIBRATION.json 에 chosen 4값 + false accept/reject 표 + fixture 12항목 이상 (미달 시 blocked=true 기록)
    - display 와 training 이 서로 다른 chosen 값 (training 더 엄격)
    - privacy_decision.json 3키 존재 + ACCEPTANCE Privacy 절과 값 일치
    - 라벨 fixture 표에 PASS/FAIL 각 1개 이상
  </acceptance_criteria>
  <done>31-05(judge)/31-06(pose 게이트)/31-07(pair)/31-10(env)/31-12(게이트)가 미실측 가정 0·측정 기반 임계값으로 구현 가능.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로컬 → DashScope | fixture 프레임 외부 전송 (belle 승인 게이트 내) |
| SSM → 로컬 셸 | SecureString 키의 로컬 취급 |
| 산출 이미지 → git | 실 인물 이미지의 저장 위치 경계 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Information Disclosure | image_smoke.py 키 취급 | mitigate | 키 env/SSM 경유만 — 리터럴·서명 URL 금지 (acceptance grep) |
| T-31-02 | Information Disclosure | 스모크/calibration 산출 이미지 | mitigate | scratch 전용 + .planning 하위 이미지 0 acceptance (H-04) |
| T-31-03 | Denial of Service (과금) | 실호출 반복 | mitigate | MAX_CALLS=8 상수 + belle 지출 승인 checkpoint (L-01) |
| T-31-04 | Privacy | 외부 처리 고지/보존 부재 | mitigate | Task 1 release gate + privacy_decision.json + retentionDays 숫자 (H-04 + H2-06 + M2-05) |
</threat_model>

<verification>
- RESULTS/CALIBRATION/privacy_decision 3 JSON 스키마 assert 통과
- 31-ACCEPTANCE.md 6개 절 존재
- .planning 하위 인물 이미지 0
</verification>

<success_criteria>
- 리뷰 Wave 0 + 2차 H2-06(보존 숫자)/H2-07(calibration 임계값)/H2-10(blur 실행 위치)/M2-05(machine-readable 결정) 완료
- D-03 모델 확정 또는 blocked 박제 — §8 gate 12(보존 숫자)·13(blur 실행 가능성)·14(calibration) 의 결정 지점
</success_criteria>

<output>
Create `.planning/phases/31-api-visual-correction/31-01-SUMMARY.md` when done
</output>
