---
phase: 31-api-visual-correction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/phase31/conftest.py
  - backend/tests/phase31/test_visual_gen.py
  - backend/tests/phase31/test_silhouette_flow.py
  - backend/tests/phase31/test_visual_request.py
  - backend/tests/phase31/test_pair_store.py
  - backend/shared/python/sunity_shared/models.py
  - backend/shared/python/sunity_shared/firestore_admin.py
  - app/src/types/analysis.ts
  - app/src/lib/userAnalyses.ts
  - docs/contract.md
autonomous: true
requirements: [D-02, D-05, D-06, D-08]
must_haves:
  truths:
    - "backend/tests/phase31/ 스캐폴드가 존재하고 전체 suite 가 green 을 유지한다 (Wave 0)"
    - "silhouetteStatus/rotationStatus 계약이 3면(analysis.ts / models.py / contract.md)에 동일하게 선언된다 (D-05/D-06 방출 기반)"
    - "앱 normalize 가 신규 필드를 drop 하지 않고, 부재(legacy doc) 시 undefined 로 조용히 통과한다 (D-08 하위호환)"
  artifacts:
    - path: "backend/tests/phase31/conftest.py"
      provides: "sys.path 주입 + _FakeDoc/_FakeSnapshot + DashScope urllib mock fixture"
    - path: "backend/shared/python/sunity_shared/models.py"
      provides: "SILHOUETTE_STATUSES / ROTATION_STATUSES 상수"
      contains: "SILHOUETTE_STATUSES"
    - path: "backend/shared/python/sunity_shared/firestore_admin.py"
      provides: "update_analysis_silhouette / update_analysis_rotation 부분 업데이트"
      contains: "def update_analysis_silhouette"
    - path: "app/src/types/analysis.ts"
      provides: "silhouetteStatus/rotationStatus optional 계약 필드"
      contains: "silhouetteStatus?"
  key_links:
    - from: "backend/shared/python/sunity_shared/firestore_admin.py"
      to: "backend/shared/python/sunity_shared/models.py"
      via: "SILHOUETTE_STATUSES / ROTATION_STATUSES 상수 검증"
      pattern: "models\\.(SILHOUETTE|ROTATION)_STATUSES"
    - from: "app/src/lib/userAnalyses.ts"
      to: "app/src/types/analysis.ts"
      via: "normalize 방어 파싱"
      pattern: "silhouetteStatus"
---

<objective>
Phase 31 의 Wave 0 테스트 스캐폴드와 계약 3면(TS/Python/contract.md)을 먼저 굳힌다. 실루엣(D-05)·회전 영상(D-06)의 Firestore 방출 필드와 상태 머신('pending'|'done'|'failed', 부재=legacy)을 faultZoomStatus 선례 그대로 신설하고, 조용한 폴백(D-08)의 기반인 "failed/부재 = 앱 숨김" 판정 규칙을 계약에 박제한다. D-02 의 VisualGenEngine 어댑터가 Wave 2 에서 바로 테스트 가능하도록 DashScope urllib mock fixture 를 스캐폴드에 포함한다.

Purpose: 후속 전 플랜(31-04~31-08)의 공유 기반. 계약을 먼저 굳혀야 backend 방출과 앱 소비가 병렬 진행 가능.
Output: backend/tests/phase31/ 5개 파일 + models.py 상수 + firestore_admin 부분 업데이트 함수 2종 + analysis.ts/userAnalyses.ts/contract.md 3면 동기.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-api-visual-correction/31-CONTEXT.md
@.planning/phases/31-api-visual-correction/31-PATTERNS.md
@.planning/phases/31-api-visual-correction/31-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: backend/tests/phase31/ Wave 0 스캐폴드 + DashScope mock fixture</name>
  <files>backend/tests/phase31/conftest.py, backend/tests/phase31/test_visual_gen.py, backend/tests/phase31/test_silhouette_flow.py, backend/tests/phase31/test_visual_request.py, backend/tests/phase31/test_pair_store.py</files>
  <read_first>backend/tests/phase22/conftest.py (sys.path 주입 패턴 19줄 전체), backend/tests/phase22/test_shadow_wiring.py lines 18-57 (_FakeSnapshot/_FakeDoc registry stub + "LOCAL ONLY" docstring 관례), .planning/spikes/004-gemini-omni-view-editing/wan_gate_batch.py lines 24-56 (mock 대상 urllib 형상)</read_first>
  <action>phase22 선례 그대로 conftest.py 작성: `_BACKEND = Path(__file__).resolve().parents[2]`, `_LAYER = _BACKEND / "shared" / "python"` sys.path 주입 + `_FakeDoc`/`_FakeSnapshot` registry stub fixture + 신규 `dashscope_urlopen_mock` fixture — `urllib.request.urlopen` 을 monkeypatch 하는 팩토리로, create 응답(`{"output": {"task_id": "..."}}`)·폴링 응답(`{"output": {"task_status": "SUCCEEDED", "video_url": "https://dashscope-result....mp4"}}`)·모더레이션 실패 응답을 시나리오별로 주입 가능하게 한다. 테스트 4파일은 모듈 docstring("실 Firestore/네트워크/Pod 미접촉 — LOCAL ONLY" 관례 + 채우는 플랜 번호 명시: test_visual_gen=31-04, test_silhouette_flow=31-01 Task2 일부+31-06, test_visual_request=31-07, test_pair_store=31-06) + 아직 없는 모듈은 `pytest.importorskip("sunity_shared.analysis.visual_gen")` 가드 + conftest fixture 동작을 확인하는 placeholder 테스트 1개씩(suite green 유지). 이모지 금지, 한국어 why-주석.</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31 -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/tests/phase31/conftest.py` contains `dashscope_urlopen_mock`
    - 4개 테스트 파일이 존재하고 각각 docstring 에 담당 플랜 번호가 명시됨
    - `python -m pytest backend/tests/phase31 -x -q` exits 0 (skip 허용, fail 0)
    - `python -m pytest backend/tests -q` exits 0 (기존 suite 무회귀)
  </acceptance_criteria>
  <done>phase31 테스트 디렉터리가 collection 에러 없이 green, DashScope mock 이 후속 플랜에서 재사용 가능.</done>
</task>

<task type="auto">
  <name>Task 2: models.py 상수 + firestore_admin 부분 업데이트 함수 2종 (D-05/D-06 방출 기반)</name>
  <files>backend/shared/python/sunity_shared/models.py, backend/shared/python/sunity_shared/firestore_admin.py, backend/tests/phase31/test_silhouette_flow.py</files>
  <read_first>backend/shared/python/sunity_shared/models.py lines 399-408 (FAULT_ZOOM_STATUS_* 선례 + lockstep 주석 스타일), backend/shared/python/sunity_shared/firestore_admin.py lines 1138-1187 (update_analysis_fault_zoom — 시그니처/검증/field-path update 전부 복제 대상), firestore_admin.py lines 104-128 (_validate_dict_only_scalars — 본체 무수정 재사용)</read_first>
  <action>models.py 에 FAULT_ZOOM 선례와 동형으로 상수 신설: `SILHOUETTE_STATUS_PENDING/DONE/FAILED` + tuple `SILHOUETTE_STATUSES`, `ROTATION_STATUS_PENDING/DONE/FAILED` + tuple `ROTATION_STATUSES` (값 'pending'/'done'/'failed') — 3면 lockstep 주석(analysis.ts + contract.md 절 인용 + D-05/D-06/D-08 근거) 포함. firestore_admin.py 에 update_analysis_fault_zoom 동형 함수 2종 신설: (1) `update_analysis_silhouette(uid, analysis_id, *, status, item=None)` — status 를 models.SILHOUETTE_STATUSES 로 강제(위반 시 ValueError), item dict 는 `_validate_dict_only_scalars` 통과 후 `result.silhouetteImageUrl`/`result.silhouetteKey`/`result.silhouetteJoint` field-path 로 기록(item None 이면 status 만), `result.silhouetteStatus` + `updatedAt`(ms epoch) 갱신; (2) `update_analysis_rotation(uid, analysis_id, *, status, video_url=None, s3_key=None, task_id=None)` — 동일 규율로 `result.rotationStatus` + 제공된 것만 `result.rotationVideoUrl`/`result.rotationVideoKey`/`result.rotationTaskId` 기록 (task_id 는 멱등 journal 겸용 — 31-07 worker 가 재개에 사용). nested list/dict 절대 금지 (flat scalar invariant). test_silhouette_flow.py 에 실 테스트 추가: 잘못된 status → ValueError, _FakeDoc 로 field-path 키 검증(`result.silhouetteStatus` 등 정확한 키 문자열), item 의 nested 값 → _validate_dict_only_scalars 거부, rotation task_id 저장 확인.</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31/test_silhouette_flow.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - models.py contains `SILHOUETTE_STATUSES` and `ROTATION_STATUSES` tuples with exactly ('pending', 'done', 'failed')
    - firestore_admin.py contains `def update_analysis_silhouette` and `def update_analysis_rotation`
    - 잘못된 status 인자로 호출 시 ValueError 발생을 테스트가 고정
    - `python -m pytest backend/tests/phase31/test_silhouette_flow.py -x -q` exits 0
  </acceptance_criteria>
  <done>backend 방출측 상태 머신이 상수·검증·부분 업데이트까지 테스트로 고정됨.</done>
</task>

<task type="auto">
  <name>Task 3: analysis.ts + userAnalyses.ts normalize + contract.md 3면 동기 (POST /visual/rotation 계약 포함)</name>
  <files>app/src/types/analysis.ts, app/src/lib/userAnalyses.ts, docs/contract.md</files>
  <read_first>app/src/types/analysis.ts lines 439-473 (FaultZoomComparison 인터페이스 — 필드별 JSDoc + flat 제약 서술 미러 대상), analysis.ts lines 563-572 (faultZoomStatus 주석 형식 — 값 의미/부재=legacy/Python lockstep 명시), app/src/lib/userAnalyses.ts (normalize() 전체 — 방어 파싱 위치), docs/contract.md (faultZoomStatus 절 — 신규 절 선례)</read_first>
  <action>analysis.ts 의 AnalysisResult(또는 faultZoomStatus 가 선언된 동일 타입)에 faultZoomStatus 주석 형식 그대로 optional 필드 추가: `silhouetteStatus?: 'pending' | 'done' | 'failed'`, `silhouetteImageUrl?: string`, `silhouetteKey?: string`, `silhouetteJoint?: string`, `rotationStatus?: 'pending' | 'done' | 'failed'`, `rotationVideoUrl?: string`, `rotationVideoKey?: string` — 각 필드에 'pending'=자리표시/'done'=카드 표시/'failed'·부재(legacy)=숨김(D-08 조용한 폴백) 의미와 Python lockstep(models.py SILHOUETTE_STATUSES 등 + firestore_admin 함수명 + contract.md 절) 주석. rotationTaskId 는 backend 내부 멱등 필드로 contract.md 에만 기술하고 TS 에는 미선언(주석으로 존재 명시). userAnalyses.ts normalize() 에 typeof 검증 추가 — status 값은 3값 literal 만 통과(그 외 undefined), URL/key/joint 는 string 검증, 부재 시 undefined 유지 (필드 drop 금지 — RESEARCH Pitfall 5). contract.md 에 (a) 신규 필드 절 (faultZoomStatus 절 선례 형식, 3면 lockstep 명시), (b) 신규 엔드포인트 절 `POST /visual/rotation`: request `{analysisId: string}`, 인증 Firebase ID token 필수, 응답 202 `{rotationStatus: 'pending'}` / 200 `{rotationStatus: 'done', rotationVideoUrl}` (이미 완료 시) / 401 unauthorized / 400 bad_request / 404 not_found (존재·소유 가드 합산 단일 응답 — leak 0) / 429 code `daily_limit` (D-07, 사용자당 일일 한도) 를 기술. 이 절이 31-07(backend)·31-08(app) 의 단일 기준이 된다.</action>
  <verify>
    <automated>cd app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - analysis.ts contains `silhouetteStatus?: 'pending' | 'done' | 'failed'` and `rotationStatus?: 'pending' | 'done' | 'failed'`
    - userAnalyses.ts normalize 가 silhouetteStatus/rotationStatus 를 3값 literal 검증으로 통과시킴 (grep: `silhouetteStatus`)
    - docs/contract.md contains `POST /visual/rotation` and `daily_limit`
    - `cd app && npm run typecheck` exits 0
  </acceptance_criteria>
  <done>계약 3면이 동기화되어 W2/W3 의 backend 방출·앱 소비가 서로를 기다리지 않고 병렬 진행 가능.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firestore doc → 앱 normalize | backend 방출 필드를 앱이 신뢰 없이 방어 파싱 |
| 계약 3면 경계 | TS/Python/contract.md 불일치 시 필드 drop·오파싱 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Tampering | userAnalyses.ts normalize | mitigate | status 는 3값 literal 화이트리스트만 통과, URL/key 는 typeof string 검증 — 임의 값은 undefined 강등 |
| T-31-02 | Information Disclosure | contract.md /visual/rotation 절 | mitigate | 존재·소유 가드를 단일 404 로 합산 명시 (playback-url guards_ok 선례) — 문서 오류가 곧 구현 leak 이 되므로 계약 단계에서 고정 |
| T-31-03 | Denial of Service | ROTATION 상태 계약 | mitigate | 429 daily_limit 을 계약에 명시해 31-07 한도 구현 누락을 계약 위반으로 검출 가능하게 함 |
</threat_model>

<verification>
- `python -m pytest backend/tests -q` green (전체 무회귀)
- `cd app && npm run typecheck` green
- grep -c "silhouetteStatus" 가 analysis.ts / userAnalyses.ts / contract.md / models.py 4곳 모두에서 1 이상
</verification>

<success_criteria>
- Wave 0 스캐폴드 5파일 존재 + suite green
- 상태 계약이 3면 동기 + 방출 함수 2종 테스트 고정
- POST /visual/rotation 계약 절이 contract.md 에 존재
</success_criteria>

<output>
Create `.planning/phases/31-api-visual-correction/31-01-SUMMARY.md` when done
</output>
