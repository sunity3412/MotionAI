---
phase: 31-api-visual-correction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/31-api-visual-correction/smoke/image_smoke.py
  - .planning/phases/31-api-visual-correction/smoke/RESULTS.json
  - .planning/phases/31-api-visual-correction/smoke/fixtures_manifest.json
  - .planning/phases/31-api-visual-correction/smoke/privacy_decision.json
  - .planning/phases/31-api-visual-correction/31-ACCEPTANCE.md
autonomous: false
requirements: [D-02, D-03, D-05]
must_haves:
  truths:
    - "외부 벤더(DashScope) 전송·보존·고지·학습 페어 정책이 belle 승인 release gate 로 확정된다 (리뷰 H-04)"
    - "privacy 결정이 machine-readable privacy_decision.json 으로 방출된다 — downstream(31-07/31-10)이 build-time 상수 대조로 assert, 런타임 .planning 읽기 금지 (2차 M2-05 + 3차 M3-02)"
    - "페어 보존기간이 숫자(일)로 확정된다 — 기본 제안 180일, deleteAfter lifecycle 대상 (2차 H2-06)"
    - "privacy option B(블러) 선택 시 실행 위치는 Pod frame 생성 시점 — Lambda 내 blur 배제 (2차 H2-10)"
    - "이미지 편집 모델이 실호출로 실측되고 결과가 RESULTS.json 에 박제된다 (D-03)"
    - "릴리스 게이트는 async taskId 지원 모델만 허용한다 — RESULTS.json.sync is True 이거나 taskId 부재면 blocked=true (v1 async-only, fetch 재-poll 불가 방지) (4차 리뷰 B4-02)"
    - "fixtures_manifest 는 before/after pair 계약이다 — 각 항목이 beforePath/beforeSha256/afterPath/afterSha256/label/jointKey/targetDeg/afterKeypointSource/failureAxes/category 를 갖는다. 31-13 harness 의 유일한 입력 (4차 리뷰 B4-04)"
    - "라벨 표본 하한 = PASS 4 / FAIL 8. FAIL 은 pole·identity/clothing·background·extra limbs/person·correction invisible·pose tolerance 를 각각 포함 (4차 리뷰 H4-10)"
    - "31-01 은 라벨 pair manifest 까지만 만든다 — 임계값 산출은 31-13 harness (3차 H3-02)"
    - "RESULTS.json.blocked=true 면 라이브 롤아웃 차단 — 31-12 게이트가 flag OFF 유지 (리뷰 H-03)"
    - "스모크 이미지가 추적 디렉터리에 저장되지 않는다 — scratch 전용 (리뷰 H-04)"
  artifacts:
    - path: ".planning/phases/31-api-visual-correction/smoke/RESULTS.json"
      provides: "chosen_model + endpoint + params + sync + usage/latency 실측 + blocked 판정(async-only 포함)"
      contains: "chosen_model"
    - path: ".planning/phases/31-api-visual-correction/smoke/fixtures_manifest.json"
      provides: "before/after pair 라벨 manifest — 31-13 calibration 의 입력 (B4-04)"
      contains: "fixtures"
    - path: ".planning/phases/31-api-visual-correction/smoke/privacy_decision.json"
      provides: "consentVersion/blurOption/retentionDays/hmacKeyVersioning — downstream build-time assert 대상"
      contains: "retentionDays"
    - path: ".planning/phases/31-api-visual-correction/31-ACCEPTANCE.md"
      provides: "판정 축·라벨 pair 목록·privacy 결정 + Display/Training 게이트 placeholder (31-13 기입)"
      contains: "Training"
  key_links:
    - from: ".planning/phases/31-api-visual-correction/smoke/RESULTS.json"
      to: "31-12 배포 게이트"
      via: "blocked=true (스모크 실패 또는 sync-only) → feature flag OFF 유지"
      pattern: "blocked"
    - from: ".planning/phases/31-api-visual-correction/smoke/fixtures_manifest.json"
      to: "backend/scripts/calibrate_visual_gates.py (31-13)"
      via: "before/after pair 라벨 입력 (B4-04/H3-02)"
      pattern: "fixtures"
---

<objective>
리뷰 Wave 0 + 2차 H2-06/H2-10/M2-05 + 3차 H3-02 + 4차 B4-02/B4-04/H4-10 을 실행한다: (1) 외부 처리·개인정보 release gate (H-04) — 보존기간 숫자(H2-06)·option B 실행 위치(H2-10)·machine-readable(M2-05). (2) 이미지 모델 스모크 실측 + **async-only 릴리스 게이트 (B4-02)**. (3) **before/after pair 계약 fixtures_manifest 작성까지만 (B4-04 + H3-02)** — 관절/좌우/직립·도립/가림/모션블러 커버, PASS 4/FAIL 8 하한(H4-10). 임계값 측정·CALIBRATION.json 산출은 31-13 harness.

D-10 재결정은 완료(31-CONTEXT [AMENDED 2026-07-19]) — 재오픈 없음.

Purpose: 잘못된 가정 위에서 구현 wave 출발 차단 — privacy 값은 승인에서, pair 라벨은 명시 기록에서, async 여부는 게이트에서.
Output: privacy_decision.json + smoke/RESULTS.json(async-only 게이트 포함) + fixtures_manifest.json(pair 계약) + 31-ACCEPTANCE.md.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-api-visual-correction/31-CONTEXT.md
@.planning/phases/31-api-visual-correction/31-RESEARCH.md
@.planning/spikes/004-gemini-omni-view-editing/README.md
@.planning/phases/31-api-visual-correction/31-PLAN-REVIEW-ITERATION4.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: 외부 처리·개인정보 release gate (H-04 + H2-06/H2-10)</name>
  <decision>DashScope 외부 전송 고지·보존 정책, 페어 적재 가명화/삭제/블러/보존일수, 스모크·calibration 지출 승인</decision>
  <read_first>.planning/phases/31-api-visual-correction/31-REVIEWS.md 의 H-04 절 + 31-PLAN-REVIEW-ITERATION2.md 의 H2-06/H2-10 절, backend/training/datagen/enumerate_internal.py lines 87-110 (consent_allows — 현행 동의 게이트), .planning/spikes/004-gemini-omni-view-editing/README.md (spike 008 — DashScope 실사용)</read_first>
  <context>D-05 는 분석마다 사용자 신체/얼굴 프레임을 DashScope 로 자동 전송한다 — learningOptIn 과 별개의 제3자 처리. 결정 전 Claude 가 WebFetch 로 Alibaba Model Studio 데이터 보존/학습 정책 요약. 결정: (a) 앱/약관 고지 방향, (b) consent 기록 = consentVersion 'pilot-optout-v1' + capturedAtMs, (c) 페어 저장 — pairId = HMAC-SHA256 가명화(versioned key set — SSM /sunity/motion/pair-id-hmac-keys JSON {"active","keys"}; retired key 삭제 가능 H2-06) + 삭제 스크립트 + visual-input/ 1일 lifecycle + 처리 후 즉시 삭제, (c') 보존일수 숫자(기본 180일 H2-06), (d) 원본 프레임 블러 여부 — option-b 는 Pod source frame 생성 시점 blur (Lambda 불가 H2-10); option-a 확정 시 downstream B 분기 제거, (e) 스모크+calibration 실호출 지출 — 생성 최대 8콜(모델당 4) + **31-13 calibration judge 최대 24콜 승인 (H3-02 harness 실행분)**.</context>
  <options>
    <option id="option-a">
      <name>권고안 일괄 승인 (블러 없음 + versioned HMAC + 보존 180일 + 1일 lifecycle + 삭제 스크립트 + 지출 승인)</name>
      <pros>학습 품질 보존 + 가명화/삭제/기한부 보존. H-04 + H2-06 충족. downstream B 분기 제거</pros>
      <cons>원본 얼굴이 페어 저장소에 보존기간 동안 남음 (동의·가명화·기한 내)</cons>
    </option>
    <option id="option-b">
      <name>블러 변형 (Pod frame 생성 시점 blur 로 training-before artifact 별도 생성, 나머지 동일)</name>
      <pros>PII 최소화 일관. Lambda 불가 문제는 Pod 시점 blur 로 해소 (H2-10)</pros>
      <cons>블러 아티팩트가 phase 22 v2 학습 품질 저하 가능 + Pod 구현 추가</cons>
    </option>
  </options>
  <resume-signal>option-a 또는 option-b + 보존일수 확정(기본 180일) (+ 고지 문구 방향)</resume-signal>
  <action>WebFetch 로 DashScope/Model Studio 데이터 정책 요약을 먼저 제시하고, (a)~(e) 를 AskUserQuestion 으로 결정받는다. 즉시 `smoke/privacy_decision.json` 작성 (M2-05): {"consentVersion": str, "blurOption": "none"|"pod_blur", "retentionDays": int, "hmacKeyParam": "/sunity/motion/pair-id-hmac-keys", "hmacKeyVersioning": true, "noticeDirection": str, "decidedAtMs": int} — 소비 방식 (M3-02): 31-07 의 CONSENT_VERSION/BLUR_OPTION 배포 상수가 이 JSON 과 build 테스트로 대조 (런타임 .planning 읽기 금지). 결정 전문은 Task 3 에서 31-ACCEPTANCE.md "## Privacy Release Gate" 기록. 승인 전 Task 2 착수 금지.</action>
  <verify>
    <automated>N/A - checkpoint:decision (belle 응답이 게이트)</automated>
  </verify>
  <acceptance_criteria>
    - belle option 명시 선택 + 보존일수 숫자 확정
    - privacy_decision.json 이 consentVersion/blurOption/retentionDays 포함 (M2-05)
    - DashScope 정책 요약이 결정 제시에 포함
    - calibration judge 지출(최대 24콜) 승인 포함 (31-13 전제)
  </acceptance_criteria>
  <done>외부 처리·페어 저장 정책이 machine-readable release gate 로 확정.</done>
</task>

<task type="auto">
  <name>Task 2: 이미지 편집 모델 스모크 실호출 (산출물 scratch 전용)</name>
  <files>.planning/phases/31-api-visual-correction/smoke/image_smoke.py</files>
  <read_first>.planning/spikes/004-gemini-omni-view-editing/wan_gate_batch.py (http_json lines 24-56 + 인증 헤더 + 비동기 task 패턴), .planning/spikes/004-gemini-omni-view-editing/README.md (spike 008 — 모더레이션 응답 형상, 입력 소재 위치)</read_first>
  <action>stdlib urllib 단발 스크립트 (requests 금지). 키 로드 = `aws ssm get-parameter --name /sunity/motion/dashscope-api-key --with-decryption --profile sunity-motion` → env DASHSCOPE_API_KEY (하드코딩·stdout 로깅 금지). 입력 = fixture 프레임 4종(직립1·도립1·좌/우 각1) → S3 임시 key `visual-input/_smoke/` put + presign 1h. 후보: (1) `wan2.7-image-pro` (WebFetch 로 이미지 편집 endpoint 확인), (2) `qwen-image-edit-plus`. 프롬프트: "Correct the athlete's left knee to full extension (about 175 degrees). Keep the person's identity, clothing, the pole, and the background unchanged." (fixture 별 치환). 호출 상한 = 모델당 4, 총 8 — MAX_CALLS=8 상수. **각 모델의 async 여부(taskId 반환 vs sync 즉시 URL)를 콜마다 기록 — RESULTS.json 의 sync 판정 근거 (B4-02); sync 모델은 latency 도 기록**. 출력 이미지는 세션 scratch 만 (.planning 하위 금지). 각 호출 HTTP status·latency·usage·모더레이션·async여부 수집. 종료 시 S3 임시 key delete_object.</action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('.planning/phases/31-api-visual-correction/smoke/image_smoke.py').read())"</automated>
  </verify>
  <acceptance_criteria>
    - image_smoke.py 파싱 가능, SSM 이름 참조, 키 리터럴 0 ("sk-" grep 0)
    - MAX_CALLS=8 + fixture 4종 + async여부(taskId) 기록 (B4-02)
    - `find .planning/phases/31-api-visual-correction -name "*.png" -o -name "*.jpg"` 0건
    - S3 임시 객체 delete_object 정리 존재
  </acceptance_criteria>
  <done>두 후보 모델 실측(표본 + latency + async여부) 확보 — 이미지는 scratch 만.</done>
</task>

<task type="auto">
  <name>Task 3: RESULTS.json(async-only 게이트) + before/after pair manifest(B4-04/H4-10) + 31-ACCEPTANCE.md 골격</name>
  <files>.planning/phases/31-api-visual-correction/smoke/RESULTS.json, .planning/phases/31-api-visual-correction/smoke/fixtures_manifest.json, .planning/phases/31-api-visual-correction/31-ACCEPTANCE.md</files>
  <read_first>Task 2 scratch 출력 이미지 (Read 로 시각 확인), .planning/phases/31-api-visual-correction/31-RESEARCH.md (§Standard Stack 판정 A1~A3), .planning/spikes/004-gemini-omni-view-editing/wan_out/ + kpts*/ (pair 후보 — spike 산출 이미지 + keypoint 실측), .planning/phases/31-api-visual-correction/31-PLAN-REVIEW-ITERATION4.md 의 B4-02/B4-04/H4-10 절</read_first>
  <action>(1) RESULTS.json: chosen_model/endpoint/request_params/**sync(bool — B4-02)**/latency_s(콜별 + p95 추정)/usage/moderation_blocked/blocked/notes/measured_at — presigned URL·이미지 데이터 금지. **blocked=true 조건: 둘 다 실패 OR chosen 이 sync 모델(sync is True 또는 async taskId 미지원) — async-only v1 이므로 sync-only 후보만 성공하면 blocked (B4-02)**. notes 에 blocked 효력(31-12 → flag OFF + phase 미완료) + async-only 사유 명시. **calibration 미달 blocked 는 31-13 이 판정·갱신.**
(2) **fixtures_manifest.json (B4-04 pair 계약 — 31-13 harness 의 유일 입력)**: 최소 12 pair — **PASS 4 / FAIL 8 (H4-10)**, 관절 2종 이상 × 좌/우 × 직립/도립 + 가림 1+ + 모션블러 1+. **FAIL 8 은 pole 붕괴·identity/clothing 변형·background 변형·extra limbs/person·correction invisible·pose tolerance failure 를 각각 포함 (H4-10 failure axis 다양성)**. 각 항목: {"id", "beforePath"(경로 참조 — 복사 금지), "beforeSha256", "afterPath", "afterSha256", "label": "PASS"|"FAIL"(Claude Read 시각 판정), "jointKey", "targetDeg"(육안+RTMW 실측), "afterKeypointSource": {"path"(spike kpts 파일 또는 "manual"+3점 좌표), "modelVersion"(estimator/model/version — M4-05 per-fixture)}, "failureAxes": [...pole/identity/clothing/background/extra_limbs/correction_invisible/pose_tolerance...], "category": [...upright/inverted/left/right/occlusion/motion_blur...]}.
(3) 31-ACCEPTANCE.md 골격: "## Display 게이트" — judge 7축 true + **임계값 placeholder: "chosen 값은 31-13 harness 산출 — CALIBRATION.json 참조" (선언값 금지 — H3-02)**, "## Training 적재 게이트" — 동일 placeholder + "training 이 display 보다 엄격" 규칙 선기록, "## 라벨 pair 셋" — manifest 요약표(PASS/FAIL 수 + failure axis 커버), "## Privacy Release Gate" — Task 1 결정 전문, "## Hard Rejection" — 인물 부재/추가 인물·사지/폴 기하 붕괴/판정 불능 = 무조건 FAIL, "## Calibration 결과표" — placeholder(31-13 기입).</action>
  <verify>
    <automated>python -c "import json; d=json.load(open('.planning/phases/31-api-visual-correction/smoke/RESULTS.json')); assert set(['chosen_model','endpoint','request_params','sync','blocked']) <= set(d); f=json.load(open('.planning/phases/31-api-visual-correction/smoke/fixtures_manifest.json')); fx=f['fixtures']; assert len(fx) >= 12 and all(set(['beforePath','beforeSha256','afterPath','afterSha256','label','jointKey','targetDeg','afterKeypointSource','failureAxes','category']) <= set(x) for x in fx); assert sum(1 for x in fx if x['label']=='PASS')>=4 and sum(1 for x in fx if x['label']=='FAIL')>=8; p=json.load(open('.planning/phases/31-api-visual-correction/smoke/privacy_decision.json')); assert set(['consentVersion','blurOption','retentionDays']) <= set(p)" && grep -q "Training" .planning/phases/31-api-visual-correction/31-ACCEPTANCE.md</automated>
  </verify>
  <acceptance_criteria>
    - RESULTS.json 스키마 + sync bool + async-only blocked 규칙 (B4-02), dashscope 서명 URL 미포함
    - fixtures_manifest.json pair 계약 10키 + PASS>=4/FAIL>=8 + failure axis 다양성 + category inverted/occlusion 포함 (B4-04/H4-10)
    - afterKeypointSource 에 per-fixture modelVersion (M4-05)
    - 31-ACCEPTANCE.md 임계값 선언 숫자 0 — placeholder 만 (H3-02)
    - privacy_decision.json 3키 + ACCEPTANCE Privacy 절 값 일치
  </acceptance_criteria>
  <done>31-05/31-06/31-07/31-13 이 미실측 가정 0 + async-only 게이트 + pair 정합 manifest 상태로 구현 가능.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로컬 → DashScope | fixture 프레임 외부 전송 (belle 승인 게이트 내) |
| SSM → 로컬 셸 | SecureString 키의 로컬 취급 |
| 산출 이미지 → git | 실 인물 이미지의 저장 위치 경계 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Information Disclosure | image_smoke.py 키 취급 | mitigate | 키 env/SSM 경유만 — 리터럴·서명 URL 금지 (acceptance grep) |
| T-31-02 | Information Disclosure | 스모크 산출 이미지 | mitigate | scratch 전용 + .planning 하위 이미지 0 (H-04) |
| T-31-03 | Denial of Service (과금) | 실호출 반복 | mitigate | MAX_CALLS=8 + belle 지출 승인 checkpoint (L-01) |
| T-31-04 | Privacy | 외부 처리 고지/보존 부재 | mitigate | Task 1 release gate + privacy_decision.json + retentionDays 숫자 (H-04 + H2-06 + M2-05) |
| T-31-65 | Denial of Service (과금) | sync-only 모델 채택 시 correctedPose 전량 fetch 실패 | mitigate | async-only 릴리스 게이트 — sync 후보 blocked (B4-02) |
</threat_model>

<verification>
- RESULTS/fixtures_manifest/privacy_decision 3 JSON 스키마 assert 통과 (pair 계약 + PASS4/FAIL8)
- 31-ACCEPTANCE.md 6개 절 존재 (calibration 관련 2절은 placeholder)
- .planning 하위 인물 이미지 0
</verification>

<success_criteria>
- 리뷰 Wave 0 + 2차 H2-06/H2-10/M2-05 + 3차 H3-02 + 4차 B4-02(async-only)/B4-04(pair 계약)/H4-10(표본 하한) 완료
- D-03 모델 확정 또는 blocked 박제 — D-05 자동 생성 정책의 privacy 전제 확정
</success_criteria>

<output>
Create `.planning/phases/31-api-visual-correction/31-01-SUMMARY.md` when done
</output>
</content>
</invoke>
