---
phase: 31-api-visual-correction
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/phase31/conftest.py
  - backend/tests/phase31/test_visual_jobs.py
  - backend/tests/phase31/test_visual_gen.py
  - backend/tests/phase31/test_pose_gate.py
  - backend/tests/phase31/test_pair_store.py
  - backend/tests/phase31/test_visual_worker.py
  - backend/tests/phase31/test_visual_dispatcher.py
  - backend/tests/phase31/test_visual_request.py
  - backend/tests/phase31/test_visual_url.py
  - backend/tests/phase31/test_visual_dispatch.py
  - backend/shared/python/sunity_shared/models.py
  - backend/shared/python/sunity_shared/firestore_admin.py
autonomous: true
requirements: [D-05, D-06, D-07, D-08]
must_haves:
  truths:
    - "실루엣(correctedPose)·회전(rotation)이 공유하는 단일 durable visual job 계약이 존재한다 — kind='correctedPose'|'rotation' (리뷰 H-01)"
    - "모든 nonterminal state 전이는 단일 Firestore transaction 으로 {현재 state/generation 검증 + 다음 state + nextAction + dispatchState='pending' + nextDispatchAtMs} 를 원자 기록한다 — CAS 와 다음 action 발행 사이 crash 가 고아를 만들 수 없다 (3차 리뷰 B3-01)"
    - "terminal 종결은 finalize_visual_job() 하나뿐이다 — job terminal(done|failed)+메타 와 analysis visual status 가 하나의 Firestore multi-document transaction 으로 갱신된다. analysis 문서 부재 = typed 'orphaned_analysis' (3차 리뷰 B3-02)"
    - "reserve transaction 이 job 예약 + analysis 'pending' 표시 + 초기 nextAction='create'/dispatchState='pending' 을 함께 기록한다 — 늦은 pending write 가 worker done 을 되감을 수 없다 (3차 리뷰 B3-03, §8-3 gate 5)"
    - "polling 상태는 taskId 없이 성립할 수 없다 — CAS validator 가 taskId 부재 전이를 ValueError 로 거부 (2차 리뷰 B2-02)"
    - "action 메시지·전이에 generation 이 포함된다 — 구세대 메시지는 새 재시도 job 을 건드리지 못한다 (B3-01)"
    - "moderation retry 는 transaction 에서 polling→retry_ready 전이 + nextAction='create' + dispatchState='pending' 동반 기록으로만 성립한다 (B3-01)"
    - "표시 필드 갱신은 전용 timestamp(correctedPoseUpdatedAtMs/rotationUpdatedAtMs)만 쓰고 분석 공용 updatedAt 을 건드리지 않는다 (리뷰 H-06)"
    - "Firestore 에는 canonical S3 key 만 저장 — presigned URL 필드 자체가 계약에 없다. outputUrl/signedUrl 필드도 없다 (리뷰 H-02 + 3차 리뷰 H3-01)"
    - "list_dispatch_pending 은 Firestore composite index 를 요구하지 않는다 — 단일 등가 쿼리(dispatchState=='pending') + in-memory nextDispatchAtMs 필터 (프로젝트 선례 STATE.md phase-33 3f6681f; 등가+범위 복합 쿼리는 index 필수라 금지 — mock 테스트로는 못 잡고 실 EventBridge 첫 실행에서 FAILED_PRECONDITION 붕괴하므로 구조로 회피)"
  artifacts:
    - path: "backend/shared/python/sunity_shared/models.py"
      provides: "VISUAL_STATUSES / VISUAL_JOB_KINDS / VISUAL_JOB_STATES(9종 — retry_ready 포함) / VISUAL_FAILURE_REASONS(10종) + visual_job_doc_path"
      contains: "VISUAL_JOB_STATES"
    - path: "backend/shared/python/sunity_shared/firestore_admin.py"
      provides: "reserve_visual_job (transaction: job+analysis pending+outbox) / transition_visual_job (CAS+validator+원자 outbox) / finalize_visual_job (job+analysis 원자 종결) / mark_visual_job_dispatched / list_dispatch_pending / update_analysis_visual(reconciler 전용)"
      contains: "def finalize_visual_job"
    - path: "backend/tests/phase31/conftest.py"
      provides: "sys.path 주입 + _FakeDoc/_FakeSnapshot/_FakeTransaction(경쟁 interleave 지원) + DashScope urllib mock fixture"
  key_links:
    - from: "backend/shared/python/sunity_shared/firestore_admin.py"
      to: "backend/shared/python/sunity_shared/models.py"
      via: "상수 검증"
      pattern: "models\\.VISUAL_"
---

<objective>
3차 리뷰 B3-01/B3-02/B3-03 의 재설계를 데이터 계약 층에서 구현한다. 핵심 원칙 3개:

1. **범용 nextAction outbox (B3-01):** 모든 state 전이가 단일 Firestore transaction 으로 {현재 state/generation 검증 + 다음 state + nextAction + dispatchState='pending' + nextDispatchAtMs} 를 원자 기록한다. worker 는 best-effort send 후 dispatchState 'sent' CAS (latency 최적화)만 하고, 실패/크래시 복구는 dispatcher(31-09 별도 Lambda — H3-09)가 담당한다. 2차 개정의 "CAS → send, send 실패는 예외 전파" 는 CAS commit 직후 crash 창 4개(polling/fetching/judging/pose_checking)를 남겼다 — 폐기.
2. **원자 finalize (B3-02):** job terminal 과 analysis visual status 는 `finalize_visual_job()` 하나의 multi-document transaction 으로만 갱신한다 (Firestore 는 다중 문서 transaction 지원). canonical S3 put 은 transaction 전 immutable key 로 (재실행 안전). analysis 문서 부재 = typed 'orphaned_analysis'.
3. **reserve = dispatch 가능 상태를 여는 transaction (B3-03):** reserve transaction 이 job 예약 + analysis 'pending' 표시 + 초기 nextAction='create'/dispatchState='pending' 을 함께 기록한다. correctedPose 호출측(31-10)은 S3 srcKey put/head 검증을 **먼저** 마친 뒤에만 reserve 를 호출한다 (upload-first — docstring 계약).

vendor create 의 durable lease('creating' + leaseOwner/leaseExpiresAt/requestKey)와 'create_unconfirmed' 수동 reconciliation(자동 재생성 금지 — DashScope idempotency 미지원, 2차 리뷰 B2-02)은 불변 유지. 상태 'dispatch_failed' 는 제거 — dispatch 실패는 별도 state 가 아니라 "dispatchState='pending' 이 남아 있는 상태" 이며 dispatcher 가 복구한다. 대신 moderation retry 용 'retry_ready' state 를 신설한다 (B3-01).

계약 필드명 (31-04 TS 측과 lockstep — 리뷰 L-02 로 correctedPose 채택):
- result.correctedPoseStatus / correctedPoseKey / correctedPoseJoint / correctedPoseUpdatedAtMs
- result.rotationStatus / rotationVideoKey / rotationUpdatedAtMs
- URL 필드 없음 (재서명은 playback-url asset 확장 — 31-10). job 문서에도 outputUrl/signedUrl 필드 없음 (H3-01 — taskId 만).

Purpose: 모든 구현 wave 가 공유하는 상태·멱등·quota·outbox·finalize 기반 — 3차 리뷰 §7 Step 1 의 선행 계약.
Output: 스캐폴드 10파일 + models.py 상수 + firestore_admin 함수 6종 + 테스트.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-api-visual-correction/31-CONTEXT.md
@.planning/phases/31-api-visual-correction/31-PATTERNS.md
@.planning/phases/31-api-visual-correction/31-PLAN-REVIEW-ITERATION3.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: backend/tests/phase31/ 스캐폴드 + mock fixture (경쟁 transaction 지원)</name>
  <files>backend/tests/phase31/conftest.py, backend/tests/phase31/test_visual_jobs.py, backend/tests/phase31/test_visual_gen.py, backend/tests/phase31/test_pose_gate.py, backend/tests/phase31/test_pair_store.py, backend/tests/phase31/test_visual_worker.py, backend/tests/phase31/test_visual_dispatcher.py, backend/tests/phase31/test_visual_request.py, backend/tests/phase31/test_visual_url.py, backend/tests/phase31/test_visual_dispatch.py</files>
  <read_first>backend/tests/phase22/conftest.py (sys.path 주입 패턴 19줄 전체), backend/tests/phase22/test_shadow_wiring.py lines 18-57 (_FakeSnapshot/_FakeDoc registry stub + "LOCAL ONLY" docstring 관례), .planning/spikes/004-gemini-omni-view-editing/wan_gate_batch.py lines 24-56 (mock 대상 urllib 형상)</read_first>
  <action>phase22 선례로 conftest.py 작성: sys.path 주입(_BACKEND.parents[2] + shared/python) + _FakeDoc/_FakeSnapshot registry stub + 신규 2종: (1) `_FakeTransaction` — firebase-admin `@firestore.transactional` 데코레이터 경로를 로컬에서 실행 가능하게 하는 stub. 요구 2가지: (a) transaction 함수가 read→write 순서로 호출되는지 기록, (b) **경쟁 시나리오 지원 (3차 리뷰 §6 — "순차 2회 모사" 폐기): 두 transaction 함수를 interleave 실행하는 `run_contended(fn_a, fn_b)` 헬퍼 — 둘 다 commit 전 상태를 read 한 뒤 fn_a 가 먼저 commit, fn_b 는 Firestore optimistic concurrency 모사로 자동 재시도되어 fn_a 의 commit 결과를 다시 read 한 상태에서 실행**. 추가로 `commit_lost=True` 모드 — transaction 함수는 성공 commit 하되 호출측에 예외를 던져 "commit 응답 유실" 을 모사 (B3-02/B3-03 테스트 전제). (2) `dashscope_urlopen_mock` — urllib.request.urlopen monkeypatch 팩토리로 create 응답({"output":{"task_id":...}})·폴링 응답(task_status SUCCEEDED/FAILED + video_url)·모더레이션 차단 응답·redirect(HTTP 302)·oversized body 시나리오 주입 가능. 테스트 9파일은 골격: 모듈 docstring("실 Firestore/네트워크/Pod 미접촉 — LOCAL ONLY" + 채우는 플랜 명시: test_visual_jobs=31-02, test_visual_gen=31-05, test_pose_gate=31-06, test_pair_store=31-07, test_visual_worker=31-09, test_visual_dispatcher=31-09, test_visual_request/test_visual_url/test_visual_dispatch=31-10) + 미존재 모듈 pytest.importorskip 가드 + fixture 동작 확인 placeholder 1개씩 (suite green 유지). 이모지 금지, 한국어 why-주석.</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31 -x -q</automated>
  </verify>
  <acceptance_criteria>
    - conftest.py 에 `dashscope_urlopen_mock` 과 `_FakeTransaction`(run_contended + commit_lost 지원) 존재
    - 9개 테스트 파일에 담당 플랜 번호 docstring 존재
    - `python -m pytest backend/tests/phase31 -x -q` exits 0 (skip 허용, fail 0)
    - `python -m pytest backend/tests -q` exits 0 (기존 suite 무회귀)
  </acceptance_criteria>
  <done>후속 전 플랜이 재사용하는 mock/경쟁 transaction 스캐폴드 완성.</done>
</task>

<task type="auto">
  <name>Task 2: models.py — 상태·typed 실패 상수 + 경로 (retry_ready / orphaned_analysis / judge_input_too_large)</name>
  <files>backend/shared/python/sunity_shared/models.py</files>
  <read_first>backend/shared/python/sunity_shared/models.py lines 399-408 (FAULT_ZOOM_STATUS_* 선례 + lockstep 주석 스타일 + analysis_doc_path 경로 함수 관례)</read_first>
  <action>FAULT_ZOOM 선례 동형으로 신설: `VISUAL_STATUS_PENDING/DONE/FAILED` + tuple `VISUAL_STATUSES` ('pending','done','failed') — correctedPose/rotation 표시 상태 공용; `VISUAL_JOB_KINDS = ("correctedPose", "rotation")`; **`VISUAL_JOB_STATES = ("reserved", "creating", "polling", "retry_ready", "fetching", "judging", "pose_checking", "done", "failed")` — 외부 side-effect 당 상태 1개 (2차 리뷰 B2-02): creating=vendor create 진행(leaseOwner/leaseExpiresAt/requestKey 필수), polling=taskId 확보 후 폴링, retry_ready=모더레이션 재시도 대기(nextAction='create' outbox 동반 — B3-01), fetching=taskId 재-poll 후 결과 다운로드/S3 전송(outputUrl 은 어디에도 저장하지 않음 — H3-01), judging=Gemini before/after 판정, pose_checking=결정론 pose 게이트 (rotation 은 fetching 에서 종결 — judging/pose_checking/retry 미사용). 'dispatch_failed' state 는 존재하지 않음 — dispatch 복구는 state 가 아니라 dispatchState='pending' + dispatcher 담당 (B3-01, 주석 박제)**; `VISUAL_FAILURE_REASONS = ("moderation", "timeout", "vendor_error", "invalid_output", "judge_failed", "judge_input_too_large", "pose_gate_failed", "pose_gate_unavailable", "create_unconfirmed", "orphaned_analysis")` 10종 — **'create_unconfirmed' (B2-02): creating lease 만료 + taskId 부재 = vendor 성공 여부 판별 불가 → 자동 재생성 금지, "중복 과금 회피를 위한 수동 판정" 대상 (정확한 복구 아님 — DashScope client tag 미지원, 3차 리뷰 M3-07); 'judge_input_too_large' (H3-03): 정규화 후에도 Gemini 요청 body 내부 상한 초과; 'orphaned_analysis' (B3-02): finalize 시 analysis 문서 부재**; `visual_job_id(uid, analysis_id, kind) -> str` = f"{uid}_{analysis_id}_{kind}" (멱등키 — 리뷰 H-06) + `visual_job_doc_path(job_id) -> str` = f"visualJobs/{job_id}" (백엔드 전용 컬렉션 — Firestore rules 미노출). 3면 lockstep 주석: analysis.ts 의 correctedPoseStatus/rotationStatus + contract.md 신규 절 + D-05/D-06/D-08 근거 인용. 표시 상태('pending'|'done'|'failed', 부재=legacy 숨김)와 job 상태(9값)는 별개 개념임을 주석으로 구분. outbox 필드 계약 주석 명시 (B3-01): `nextAction` ('create'|'poll'|'fetch'|'judge'|'pose_check'|None), `dispatchState` ('pending'|'sent'|None), `nextDispatchAtMs`, `generation` — nextAction/dispatchState 는 state 전이와 같은 transaction 에서만 기록됨.</action>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0,'backend/shared/python'); from sunity_shared import models; assert models.VISUAL_STATUSES==('pending','done','failed'); assert 'retry_ready' in models.VISUAL_JOB_STATES and 'dispatch_failed' not in models.VISUAL_JOB_STATES; assert 'create_unconfirmed' in models.VISUAL_FAILURE_REASONS and 'orphaned_analysis' in models.VISUAL_FAILURE_REASONS and 'judge_input_too_large' in models.VISUAL_FAILURE_REASONS; assert models.visual_job_id('u','a','rotation')=='u_a_rotation'"</automated>
  </verify>
  <acceptance_criteria>
    - VISUAL_JOB_STATES 9종 (retry_ready 포함, dispatch_failed 부재), VISUAL_JOB_KINDS == ('correctedPose','rotation')
    - VISUAL_FAILURE_REASONS 10종 — create_unconfirmed/judge_input_too_large/orphaned_analysis 포함
    - visual_job_id / visual_job_doc_path 함수 존재
    - outbox 필드 계약(nextAction/dispatchState/nextDispatchAtMs/generation) 주석 존재
  </acceptance_criteria>
  <done>side-effect 단위 상태·멱등키·outbox 필드 계약이 상수로 고정됨.</done>
</task>

<task type="auto">
  <name>Task 3: firestore_admin — 원자 reserve/transition/finalize + dispatcher 조회 + validator</name>
  <files>backend/shared/python/sunity_shared/firestore_admin.py, backend/tests/phase31/test_visual_jobs.py</files>
  <read_first>backend/shared/python/sunity_shared/firestore_admin.py lines 1138-1187 (update_analysis_fault_zoom — field-path update/검증 선례) + lines 104-128 (_validate_dict_only_scalars — 본체 무수정 재사용) + _doc 헬퍼, backend/tests/phase31/conftest.py (_FakeTransaction — run_contended/commit_lost)</read_first>
  <action>함수 6종 신설 (전부 모듈 docstring 에 리뷰 H-02/H-06/B-03 + 2차 B2-02 + 3차 B3-01/B3-02/B3-03/H3-01 근거 인용):
(1) `reserve_visual_job(uid, analysis_id, kind, *, date_key=None, user_limit=None, global_limit=None, payload: dict | None = None, allow_retry_failed: bool = False) -> dict` — firebase-admin `@firestore.transactional` 로 **읽기 전부 → 쓰기 전부** 순서: job 문서 read + **analysis 문서 read (부재 시 {"created": False, "reason": "analysis_missing"} — job 생성 0)** + (rotation 이고 limit 인자 제공 시) `visualQuota/{uid}_{date_key}` 와 `visualQuota/global_{date_key}` counter read. job 이미 존재하면 {"created": False, "job": {...전체 필드...}} 반환 — 호출측(31-10)이 state/dispatchState/sourceHash 로 분기하는 유일한 근거 (B2-03). 단 기존 state 'failed' + allow_retry_failed=True 만 재예약 허용 (state 'reserved' 리셋 + `generation` +1 + payload 재기록 + rotation quota 재소비 + 아래 outbox/analysis 쓰기 재수행). 한도 이상 → {"created": False, "reason": "daily_limit"} (증가 0). 신규 생성 쓰기 (전부 같은 transaction — B3-03): counter +1(rotation) + job 문서 {uid, analysisId, kind, state:'reserved', **nextAction:'create', dispatchState:'pending', nextDispatchAtMs: now** (초기 outbox — B3-01/B3-03), generation:1, attempt:0, retryCount:0, taskId:None, failureReason:None, requestedAtMs, updatedAtMs, quotaDateKey, payload 병합} + **analysis 문서 field-path 로 result.{kind}Status='pending' + result.{kind}UpdatedAtMs (전용 timestamp — H-06; 늦은 pending write 가 존재하지 않게 되는 유일한 구조 — B3-03/§8-3 gate 5)** → {"created": True, "job": ...}. payload = flat scalar dict 만 (`_validate_dict_only_scalars` 재사용). **docstring 계약 박제: correctedPose 호출측은 hash 기반 immutable srcKey 의 S3 put/head 검증을 먼저 마친 뒤에만 호출한다 (upload-first — B3-03 권장안 A; input 없는 reserved job 이 구조적으로 불가능).**
(2) `transition_visual_job(job_id, *, expect_states: tuple, updates: dict, next_action: str | None, expect_generation: int | None = None, next_dispatch_at_ms: int | None = None) -> bool` — 단일 transaction: 현재 state ∉ expect_states → False (stale no-op); **expect_generation 지정 시 generation 불일치 → False (구세대 메시지 차단 — B3-01)**; validator: updates['state']=='polling' 인데 비어있지 않은 taskId 없으면 ValueError; 'creating' 인데 leaseOwner/leaseExpiresAt/requestKey 미비면 ValueError; **updates['state'] 가 'done'|'failed' 면 ValueError ("terminal 은 finalize_visual_job 전용"); next_action=None 은 'creating' 전이에서만 허용 (creating 의 복구 계약은 dispatch 가 아닌 lease — 그 외 nonterminal 전이에 next_action 부재 → ValueError)**. 통과 시 updates + updatedAtMs + **nextAction=next_action + dispatchState='pending' + nextDispatchAtMs** 원자 기록 (creating 전이는 nextAction=None + dispatchState=None 클리어) → True.
(3) `finalize_visual_job(job_id, *, uid, analysis_id, kind, terminal_state: str, failure_reason: str | None, job_meta: dict, display_status: str, key: str | None = None, joint: str | None = None, expect_states: tuple, expect_generation: int | None = None) -> str` — **B3-02 핵심**: 단일 multi-document transaction — read: job(state ∈ expect_states + generation 검증; 불일치 → 'stale' 반환 no-op) + analysis 문서. write: job {state: terminal_state, failureReason, job_meta flat scalar 병합, nextAction:None, dispatchState:None, updatedAtMs} + analysis field-path result.{kind}Status/{kind}Key/{kind}Joint(correctedPose 만)/{kind}UpdatedAtMs. **analysis 문서 부재 → job 만 {state:'failed', failureReason:'orphaned_analysis'} 기록 후 'orphaned_analysis' 반환 (typed 운영 경로)**. display_status ∈ models.VISUAL_STATUSES 강제, key 는 "results/{uid}/{analysis_id}/" prefix 강제 (위반 ValueError — H-02), terminal_state ∈ ('done','failed') 강제. 분석 공용 top-level updatedAt 절대 미갱신. 정상 'finalized' 반환. docstring: canonical S3 put 은 이 함수 호출 **전** immutable key 로 완료 (재실행 안전 — B3-02).
(4) `mark_visual_job_dispatched(job_id, *, expect_generation: int | None = None) -> bool` — dispatchState 'pending' → 'sent' CAS (worker/dispatcher 가 SQS send 성공 후 호출 — best-effort 최적화일 뿐, sent 실패해도 dispatcher 재발행은 무해: worker CAS 멱등).
(5) `list_dispatch_pending(now_ms: int, *, limit: int = 20) -> list[dict]` — dispatcher 전용 (**reserved 만이 아니라 nextAction 이 있는 모든 nonterminal state 가 대상**, B3-01). **Firestore composite index 회피 (프로젝트 선례 — STATE.md phase-33 `3f6681f` "composite index 자동 생성 X — query 단순화 + in-memory filter"; [[firestore-index-entry-limit]]): 등가+범위 복합 쿼리(dispatchState=='pending' AND nextDispatchAtMs<=now)는 composite index 필수 → 금지. 대신 단일 등가 쿼리 `where('dispatchState','==','pending').limit(N)` (N = limit*3 여유분, 단일 필드 자동 인덱스만 사용) 후 `nextDispatchAtMs <= now_ms` 를 in-memory 필터 + now_ms 오름차순 정렬 → 상위 limit 반환.** dispatcher 는 rate(1분) 이고 pending 집합은 작으므로(bounded) in-memory 필터로 충분. 반환 dict 에 jobId/nextAction/generation/state 포함. **docstring 에 "복합 인덱스 금지 이유 + in-memory 필터 계약" 박제.**
(6) `update_analysis_visual(uid, analysis_id, kind, *, status, key=None, joint=None)` — **terminal reconciler(31-09 운영 스크립트) repair 전용 — production worker 경로 호출 금지 (docstring + 31-09 acceptance grep)**. status ∈ VISUAL_STATUSES 강제, kind 별 field-path 만, top-level updatedAt 미갱신, key prefix 강제, URL 파라미터 없음.
test_visual_jobs.py 실 테스트: **reserve — 신규 예약 1 transaction 에 job(state reserved + nextAction create + dispatchState pending) + analysis result.{kind}Status pending + 전용 UpdatedAtMs 동시 기록 (B3-03); analysis 부재 → created False + job 0; 동일 키 2회 → 2회째 created False + counter 1회 + 반환 job 에 state/dispatchState/sourceHash 포함; 한도 → daily_limit + 증가 0; failed + allow_retry_failed=False → no-op / True → generation 2 + outbox 재기록; run_contended 로 동시 reserve 2건 → job 1개 + counter +1 (경쟁 transaction — §6)**; **transition — 정상 전이 시 nextAction/dispatchState/nextDispatchAtMs 가 state 와 같은 transaction 기록; terminal state → ValueError; nonterminal + next_action=None ('creating' 외) → ValueError; polling taskId 부재 ValueError; creating lease 미비 ValueError; expect_generation 불일치 → False + 무변경**; **finalize — job terminal + analysis 표시가 한 transaction 에서 동시 갱신 + nextAction/dispatchState 클리어; analysis 부재 → job failed 'orphaned_analysis' + 반환값; commit_lost 모드 → 호출측 예외에도 두 문서 이미 일치 (재호출 시 'stale' no-op) (B3-02)**; mark_visual_job_dispatched pending→sent + 이미 sent → False; list_dispatch_pending 이 polling/fetching 등 nonterminal pending 전부 반환 + sent/terminal 제외 + **단일 등가 쿼리만 사용(mock 이 dispatchState=='pending' where 1회만 받음 assert — 복합 range 쿼리 미발생) + nextDispatchAtMs > now 인 문서는 in-memory 필터로 제외 (composite index 회피 검증)**; update_analysis_visual URL 파라미터 부재 + top-level updatedAt 미기록; key prefix ValueError; payload nested ValueError.</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31/test_visual_jobs.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - firestore_admin.py 에 reserve_visual_job / transition_visual_job / finalize_visual_job / mark_visual_job_dispatched / list_dispatch_pending / update_analysis_visual 6종 존재
    - reserve 가 job+analysis pending+초기 outbox 를 한 transaction 에 기록 (테스트 고정 — B3-03)
    - transition 이 state+nextAction+dispatchState 를 원자 기록 + terminal 거부 + generation 검증 (테스트 고정 — B3-01)
    - finalize 가 multi-document transaction + orphaned_analysis + commit_lost 일치 테스트 green (B3-02)
    - polling taskId / creating lease validator 테스트 green (B2-02)
    - list_dispatch_pending 이 모든 nonterminal pending 을 반환 (reserved 한정 아님 — B3-01)
    - `python -m pytest backend/tests/phase31/test_visual_jobs.py -x -q` exits 0
  </acceptance_criteria>
  <done>quota·멱등·범용 outbox·lease·원자 finalize·전용 timestamp·canonical key 규율이 함수 시그니처와 테스트로 강제됨.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 요청/워커 → visualJobs 문서 | 상태 전이의 유일한 진실 — transaction CAS+validator 로만 변경 |
| job terminal → analysis 표시 | finalize_visual_job 단일 transaction 로만 — 이중 쓰기 금지 |
| 표시 필드 → 앱 | canonical key 만 — URL 은 인증 재서명 경로로만 발급 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-05 | Denial of Service (과금) | quota 예약 | mitigate | transaction 원자 예약 — 동시 요청/SQS 실패에서 이중 소비·고아 quota 불가 (run_contended 테스트 고정, 리뷰 H-06) |
| T-31-06 | Tampering | stale/구세대 SQS 메시지의 상태 되감기 | mitigate | transition_visual_job CAS + generation 검증 + taskId/lease validator (B-03 + B2-02 + B3-01) |
| T-31-07 | Information Disclosure | Firestore 에 만료 URL 저장 | mitigate | finalize/update 에 URL 파라미터 부재 + key prefix ValueError + job 에 taskId 만 (H-02 + H3-01) |
| T-31-08 | Elevation of Privilege | visualJobs 컬렉션 노출 | mitigate | 백엔드 전용 컬렉션 (Firestore rules 미추가 — 기본 거부), 앱은 analysis 표시 필드만 구독 |
| T-31-52 | Denial of Service (과금) | creating crash 후 무조건 재생성 | mitigate | lease 만료 + taskId 부재 = 'create_unconfirmed' 수동 판정 — 자동 재생성 금지 (B2-02/M3-07) |
| T-31-56 | Denial of Service | CAS 후 send 유실로 영구 고아 | mitigate | 모든 전이가 nextAction outbox 원자 기록 — dispatcher 가 pending 전부 복구 (B3-01, 테스트 고정) |
| T-31-57 | Tampering | terminal job / analysis 영구 불일치 | mitigate | finalize_visual_job multi-document transaction + orphaned_analysis typed + reconciler 보조 (B3-02) |
</threat_model>

<verification>
- `python -m pytest backend/tests -q` green (전체 무회귀)
- firestore_admin 신규 함수가 _validate_dict_only_scalars 관례 준수 (nested 거부)
- 워커/요청 production 경로에서 update_analysis_visual 직접 호출 금지는 31-09/31-10 acceptance 가 grep 으로 이어받음
</verification>

<success_criteria>
- 단일 visual job 계약(9상태·retry_ready·lease·범용 nextAction outbox·generation·원자 finalize·멱등키·CAS validator·typed failure 10종·전용 timestamp·canonical key)이 테스트로 고정
- 3차 리뷰 §8-3 승인 조건 1(전이=outbox 원자)·3(finalize 원자)·5(늦은 pending 불가) 의 계약 지점
- 9개 테스트 파일 스캐폴드 + 경쟁 transaction mock 완성
</success_criteria>

<output>
Create `.planning/phases/31-api-visual-correction/31-02-SUMMARY.md` when done
</output>
