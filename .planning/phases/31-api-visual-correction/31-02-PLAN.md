---
phase: 31-api-visual-correction
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/phase31/conftest.py
  - backend/tests/phase31/test_visual_jobs.py
  - backend/tests/phase31/test_visual_gen.py
  - backend/tests/phase31/test_pose_gate.py
  - backend/tests/phase31/test_pair_store.py
  - backend/tests/phase31/test_visual_worker.py
  - backend/tests/phase31/test_visual_dispatcher.py
  - backend/tests/phase31/test_visual_request.py
  - backend/tests/phase31/test_visual_url.py
  - backend/tests/phase31/test_visual_dispatch.py
  - backend/shared/python/sunity_shared/models.py
  - backend/shared/python/sunity_shared/firestore_admin.py
autonomous: true
requirements: [D-05, D-06, D-07, D-08]
must_haves:
  truths:
    - "실루엣(correctedPose)·회전(rotation)이 공유하는 단일 durable visual job 계약이 존재한다 — kind='correctedPose'|'rotation' (리뷰 H-01)"
    - "모든 nonterminal state 전이는 단일 Firestore transaction 으로 {현재 state/generation 검증 + 다음 state + nextAction + dispatchState='pending' + nextDispatchAtMs + 새 outboxSeq} 를 원자 기록하고 갱신된 job snapshot 을 반환한다 — 후속 단계(send/mark/2차 전이/finalize)는 반환 snapshot 하나만 사용하며 인바운드 msg 값을 재사용하지 않는다 (3차 B3-01 + 4차 B4-01 + 5차 H5-03/H5-04)"
    - "outbox 는 generation 과 별개인 단조 증가 outboxSeq 로 action instance 를 식별한다 — mark_visual_job_dispatched CAS = generation==expected AND nextAction==expected_action AND outboxSeq==expected_outbox_seq AND dispatchState=='pending'. 이전 action 의 늦은 ACK 는 seq 불일치로 no-op 이라 다음 continuation 을 덮을 수 없다 (4차 리뷰 B4-01)"
    - "worker 는 외부 side-effect 전에 claim_visual_job_action() 으로 claim 하고 반환은 4상태다 — stale(generation/action/outboxSeq 불일치)·claimed(미claim 또는 same-seq lease 만료 → owner/lease 원자 갱신 후 실행)·busy(same-seq + lease 유효 → 정상 ACK 금지)·completed(이미 다음 outboxSeq/terminal). claimedOutboxSeq 만이 아니라 claimState/claimLeaseExpiresAt/claimOwner 를 검사하며, lease 는 action worst-case runtime 보다 길고 SQS visibility 1800s 보다 짧다 (VISUAL_CLAIM_LEASE_MS 단일 출처) (5차 리뷰 B5-01)"
    - "action 성공 transition/finalize 는 현재 claim owner+outboxSeq 도 CAS 한다 — lease 를 잃은(재claim 당한) 늦은 worker 의 결과 write 가 새 owner 의 진행을 덮지 못한다 (5차 리뷰 B5-01)"
    - "claim 직후 crash 는 lease 만료 뒤 반드시 재실행된다 — dispatcher 가 dispatchState=='sent' + lease 만료 + nonterminal job 을 복구 대상으로 재발행하고(durable 복구 주체), busy 메시지는 batchItemFailures 로 SQS visibility 뒤 재전달된다. '재전달 no-op 멱등' 단독은 복구 주체가 아니다 (5차 리뷰 B5-01)"
    - "terminal 종결은 finalize_visual_job() 하나뿐이다 — job terminal(done|failed)+메타 와 analysis visual status 가 하나의 Firestore multi-document transaction. uid/analysisId/kind 는 caller 인자가 아니라 job 문서에서 파생, done↔display done / failed↔display failed 고정, done=canonical key 필수·failed=canonical key 금지 (3차 B3-02 + 4차 H4-07)"
    - "correctedPose 는 성공/실패 어느 terminal intent 든 pose_checking 통과 여부와 무관하게 postprocessing state 를 거쳐 종결한다 — job 에 pendingTerminalState/pendingFailureReason 를 실어 postprocess action 이 exact-prefix version cleanup(+동의 시 pair store)을 durable 하게 수행한 뒤 한 번의 finalize 로 done/failed 를 기록한다. finalize 직후 crash 로 pair/cleanup 이 유실되거나 실패 경로가 durable cleanup 을 우회할 수 없다 (4차 H4-01 + 5차 H5-05)"
    - "reserve transaction 이 job 예약 + analysis 'pending' 표시 + 초기 nextAction='create'/dispatchState='pending'/outboxSeq=1 을 함께 기록한다 — 늦은 pending write 가 worker done 을 되감을 수 없다 (3차 B3-03, §8-3 gate 5)"
    - "polling 상태는 taskId 없이 성립할 수 없다 — CAS validator 가 taskId 부재 전이를 ValueError 로 거부 (2차 B2-02). retry_ready→creating validator 는 taskId is None 을 강제하고 generation 을 +1 승격하며, requestKey 를 그 새 generation 으로 같은 transaction 에서 생성한다 — requestKey suffix == persisted generation 을 validator 가 강제 (4차 B4-03 + 5차 H5-03)"
    - "moderation retry 는 polling→retry_ready transaction 에서 taskId=None·attempt=0·output/failure/lease metadata clear + retryCount+1 + nextAction='create' 을 동반 기록한다 — 이전 blocked task 로 되돌아갈 수 없다 (4차 리뷰 B4-03)"
    - "표시 필드 갱신은 전용 timestamp(correctedPoseUpdatedAtMs/rotationUpdatedAtMs)만 쓴다 (리뷰 H-06). Firestore 에는 canonical S3 key 만 저장 — outputUrl/signedUrl 필드가 계약에 없다 (H-02 + H3-01)"
    - "list_dispatch_pending 은 Firestore composite index 를 요구하지 않는다 — 단일 등가 쿼리(dispatchState=='pending') + __name__ cursor pagination + durable scan cursor(마지막 문서 위치를 Firestore 에 저장해 다음 invocation 이 이어서 스캔, 끝에서 wrap) 로 max_scan 초과 backlog 도 순환 전량 drain 한다. dispatchState=='sent' + lease 만료 + nonterminal job 도 별도 조회해 복구 대상에 포함. first-N slice starvation 없음, 초과 시 truncated 플래그/alarm(31-10) (프로젝트 선례 STATE.md phase-33 3f6681f; 4차 H4-08 + 5차 H5-06/B5-01)"
  artifacts:
    - path: "backend/shared/python/sunity_shared/models.py"
      provides: "VISUAL_STATUSES / VISUAL_JOB_KINDS / VISUAL_JOB_STATES(10종 — retry_ready·postprocessing 포함) / VISUAL_FAILURE_REASONS(11종) / VISUAL_CLAIM_LEASE_MS + outbox·claim·pending-terminal 필드 계약(outboxSeq/claimedOutboxSeq/claimState/claimOwner/claimLeaseExpiresAt/pendingTerminalState/pendingFailureReason/pairAttempt) + visual_job_doc_path + visual_dispatch_cursor_doc_path"
      contains: "VISUAL_JOB_STATES"
    - path: "backend/shared/python/sunity_shared/firestore_admin.py"
      provides: "reserve_visual_job / transition_visual_job(expect_outbox_seq + expect_claim_owner, snapshot 반환) / finalize_visual_job(job 문서 파생 + expect_claim_owner) / mark_visual_job_dispatched(action+seq CAS) / claim_visual_job_action(4상태) / list_dispatch_pending(durable cursor + lease-expired redispatch) / update_analysis_visual"
      contains: "def claim_visual_job_action"
    - path: "backend/tests/phase31/conftest.py"
      provides: "sys.path 주입 + _FakeDoc/_FakeSnapshot/_FakeTransaction(경쟁 interleave 지원) + DashScope urllib mock fixture"
  key_links:
    - from: "backend/shared/python/sunity_shared/firestore_admin.py"
      to: "backend/shared/python/sunity_shared/models.py"
      via: "상수 검증"
      pattern: "models\\.VISUAL_"
---

<objective>
3차 리뷰 B3-01/B3-02/B3-03 + 4차 B4-01/B4-03/H4-01/H4-07/H4-08 + 5차 B5-01/H5-03/H5-04/H5-05/H5-06 을 데이터 계약 층에서 구현한다. 핵심 원칙:

1. **outbox instance ownership + claim 4상태 (B3-01 + B4-01 + B5-01):** 모든 state 전이가 단일 Firestore transaction 으로 {현재 state/generation 검증 + 다음 state + nextAction + dispatchState='pending' + nextDispatchAtMs + **단조 증가 outboxSeq**} 를 원자 기록하고 **갱신된 job snapshot 을 반환**한다. 메시지 = {jobId, generation, action, outboxSeq}. worker 는 외부 side-effect **전에** `claim_visual_job_action` transaction 으로 claim 하며 반환은 4상태(stale/claimed/busy/completed)다 — **claimedOutboxSeq 만이 아니라 claimState/claimLeaseExpiresAt/claimOwner 로 same-seq active lease(busy) 와 same-seq expired lease(재claim=claimed) 를 구분**한다. busy 는 정상 ACK 금지. action 성공 transition/finalize 는 **현재 claim owner+outboxSeq 도 CAS** 해 lease 를 잃은 늦은 worker 의 write 를 차단한다.
2. **crash 복구 주체 (B5-01):** claim 직후 crash 는 lease 만료 뒤 반드시 재실행돼야 한다. `list_dispatch_pending` 이 **dispatchState=='sent' + lease 만료 + nonterminal** job 도 복구 대상으로 반환(durable 복구 주체)하고, worker 의 busy 응답은 batchItemFailures 로 SQS visibility(1800s) 뒤 재전달된다. lease(`VISUAL_CLAIM_LEASE_MS` 단일 출처)는 action worst-case runtime(300s Lambda timeout)+margin 보다 길고 visibility 1800s 보다 짧다.
3. **원자 finalize (B3-02 + H4-07):** job terminal 과 analysis visual status 는 `finalize_visual_job()` 하나의 multi-document transaction 으로만 갱신. **uid/analysisId/kind 는 caller 인자가 아니라 job 문서에서 파생**, done↔display done / failed↔display failed 고정, done 은 canonical key 필수·failed 는 canonical key 금지.
4. **postprocessing durable state — 성공+실패 전부 경유 (H4-01 + H5-05):** correctedPose 는 어느 terminal intent 든 `postprocessing` 을 거쳐 종결한다. job 에 `pendingTerminalState`/`pendingFailureReason` 를 실어 postprocess action 이 exact-prefix version cleanup(+동의 시 pair store)을 durable 하게 수행한 뒤 한 번의 finalize 로 done/failed 를 기록한다. (rotation 은 임시 입력·pair 가 없어 fetching 에서 직접 finalize.)
5. **reserve = dispatch 가능 상태를 여는 transaction (B3-03):** reserve transaction 이 job 예약 + analysis 'pending' + 초기 nextAction='create'/dispatchState='pending'/outboxSeq=1 을 함께 기록한다.
6. **fresh requestKey (H5-03):** retry_ready→creating 전이가 generation+1 승격 시 requestKey 를 그 새 generation 으로 같은 transaction 에서 생성하고 suffix==persisted generation 을 validator 가 강제한다.

vendor create 의 durable lease('creating' + leaseOwner/leaseExpiresAt/requestKey)와 'create_unconfirmed' 수동 reconciliation(자동 재생성 금지 — 2차 B2-02)은 불변. 상태 'dispatch_failed' 는 존재하지 않음 — dispatch 실패는 "dispatchState='pending' 이 남아 있는 상태" 이며 dispatcher 가 복구한다.

계약 필드명 (31-04 TS 측과 lockstep):
- result.correctedPoseStatus / correctedPoseKey / correctedPoseJoint / correctedPoseUpdatedAtMs
- result.rotationStatus / rotationVideoKey / rotationUpdatedAtMs
- URL 필드 없음. job 문서에도 outputUrl/signedUrl 없음 (H3-01 — taskId 만).

Purpose: 모든 구현 wave 가 공유하는 상태·멱등·quota·outbox instance·claim(4상태)·owner CAS·finalize·durable postprocessing·pagination-cursor 기반 — 5차 리뷰 §7 실행 허용 기준.
Output: 스캐폴드 10파일 + models.py 상수 + firestore_admin 함수 7종 + 테스트.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-api-visual-correction/31-CONTEXT.md
@.planning/phases/31-api-visual-correction/31-PATTERNS.md
@.planning/phases/31-api-visual-correction/31-PLAN-REVIEW-ITERATION5.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: backend/tests/phase31/ 스캐폴드 + mock fixture (경쟁 transaction 지원)</name>
  <files>backend/tests/phase31/conftest.py, backend/tests/phase31/test_visual_jobs.py, backend/tests/phase31/test_visual_gen.py, backend/tests/phase31/test_pose_gate.py, backend/tests/phase31/test_pair_store.py, backend/tests/phase31/test_visual_worker.py, backend/tests/phase31/test_visual_dispatcher.py, backend/tests/phase31/test_visual_request.py, backend/tests/phase31/test_visual_url.py, backend/tests/phase31/test_visual_dispatch.py</files>
  <read_first>backend/tests/phase22/conftest.py (sys.path 주입 패턴 19줄 전체), backend/tests/phase22/test_shadow_wiring.py lines 18-57 (_FakeSnapshot/_FakeDoc registry stub + "LOCAL ONLY" docstring 관례), .planning/spikes/004-gemini-omni-view-editing/wan_gate_batch.py lines 24-56 (mock 대상 urllib 형상)</read_first>
  <action>phase22 선례로 conftest.py 작성: sys.path 주입(_BACKEND.parents[2] + shared/python) + _FakeDoc/_FakeSnapshot registry stub + 신규 2종: (1) `_FakeTransaction` — firebase-admin `@firestore.transactional` 경로를 로컬 실행 가능하게 하는 stub. 요구: (a) transaction 함수가 read→write 순서로 호출되는지 기록, (b) **경쟁 시나리오 지원 (§6): 두 transaction 함수를 interleave 실행하는 `run_contended(fn_a, fn_b)` 헬퍼 — 둘 다 commit 전 상태를 read 한 뒤 fn_a 먼저 commit, fn_b 는 optimistic concurrency 모사로 자동 재시도되어 fn_a commit 결과를 다시 read 한 상태에서 실행**. (c) `commit_lost=True` 모드 — transaction 함수는 성공 commit 하되 호출측에 예외를 던져 "commit 응답 유실" 모사. (d) **시계 주입(`now_ms` 조작 가능한 fixture clock) — claim lease 만료 전/후 시간축을 결정론으로 재현 (5차 B5-01)**. (2) `dashscope_urlopen_mock` — urllib.request.urlopen monkeypatch 팩토리로 create 응답({"output":{"task_id":...}})·폴링 응답(task_status SUCCEEDED/FAILED + video_url)·모더레이션 차단 응답·redirect(HTTP 302)·oversized body 주입 가능. 테스트 9파일은 골격: 모듈 docstring("실 Firestore/네트워크/Pod 미접촉 — LOCAL ONLY" + 채우는 플랜: test_visual_jobs=31-02, test_visual_gen=31-05, test_pose_gate=31-06, test_pair_store=31-07, test_visual_worker=31-09, test_visual_dispatcher=31-09, test_visual_request/test_visual_url/test_visual_dispatch=31-10) + 미존재 모듈 pytest.importorskip 가드 + fixture 동작 확인 placeholder 1개씩. 이모지 금지, 한국어 why-주석.</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31 -x -q</automated>
  </verify>
  <acceptance_criteria>
    - conftest.py 에 `dashscope_urlopen_mock` 과 `_FakeTransaction`(run_contended + commit_lost + fixture clock 지원) 존재
    - 9개 테스트 파일에 담당 플랜 번호 docstring 존재
    - `python -m pytest backend/tests/phase31 -x -q` exits 0 (skip 허용, fail 0)
    - `python -m pytest backend/tests -q` exits 0 (기존 suite 무회귀)
  </acceptance_criteria>
  <done>후속 전 플랜이 재사용하는 mock/경쟁 transaction/시계 스캐폴드 완성.</done>
</task>

<task type="auto">
  <name>Task 2: models.py — 상태(retry_ready/postprocessing)·typed 실패·outbox/claim/pending-terminal 필드 계약</name>
  <files>backend/shared/python/sunity_shared/models.py</files>
  <read_first>backend/shared/python/sunity_shared/models.py lines 399-408 (FAULT_ZOOM_STATUS_* 선례 + lockstep 주석 스타일 + analysis_doc_path 관례)</read_first>
  <action>FAULT_ZOOM 선례 동형으로 신설: `VISUAL_STATUS_PENDING/DONE/FAILED` + tuple `VISUAL_STATUSES` ('pending','done','failed'); `VISUAL_JOB_KINDS = ("correctedPose", "rotation")`; **`VISUAL_JOB_STATES = ("reserved", "creating", "polling", "retry_ready", "fetching", "judging", "pose_checking", "postprocessing", "done", "failed")` — 외부 side-effect 당 상태 1개 (B2-02). 신규 state 추가 금지(상태 폭발 방지 — 5차): claim 상태·pending-terminal 은 별도 필드로 표현**: creating=vendor create 진행(leaseOwner/leaseExpiresAt/requestKey 필수), polling=taskId 확보 후 폴링, retry_ready=모더레이션 재시도 대기(taskId=None 강제 + nextAction='create' outbox 동반 — B4-03), fetching=taskId 재-poll 후 다운로드/S3 전송(outputUrl 미저장 — H3-01), judging=Gemini before/after 판정, pose_checking=결정론 pose 게이트, **postprocessing=correctedPose 전용 pre-terminal — 성공/실패 어느 terminal intent 든 여기서 exact-prefix version cleanup(+동의 시 pair store)을 durable 수행 후 finalize (H4-01 + 5차 H5-05)**. rotation 은 fetching 에서 직접 finalize (judging/pose_checking/postprocessing/retry 미사용). 'dispatch_failed' state 부재 — 복구는 dispatchState='pending' + dispatcher (B3-01, 주석 박제)**; `VISUAL_FAILURE_REASONS = ("moderation", "timeout", "vendor_error", "invalid_output", "judge_failed", "judge_input_too_large", "pose_gate_failed", "pose_gate_unavailable", "create_unconfirmed", "orphaned_analysis", "cleanup_blocked")` 11종 (cleanup_blocked = privacy cleanup 이 max retry 후에도 미완 시 명시적 privacy blocker — 5차 H5-02); `visual_job_id(uid, analysis_id, kind) -> str` = f"{uid}_{analysis_id}_{kind}" (멱등키) + `visual_job_doc_path(job_id) -> str` = f"visualJobs/{job_id}" (백엔드 전용 컬렉션) + **`visual_dispatch_cursor_doc_path() -> str` = "visualDispatch/scanCursor" (durable pagination cursor — 5차 H5-06)**. **`VISUAL_CLAIM_LEASE_MS = 360000` (=300s Lambda timeout + 60s margin; SQS visibility 1800s 보다 짧아 만료 후 재claim 가능 — 5차 B5-01; template 의 worker Timeout·VisualQueue VisibilityTimeout 과 단일 출처 근거 주석)**. 3면 lockstep 주석 + D-05/D-06/D-08 인용. 표시 상태('pending'|'done'|'failed', 부재=legacy 숨김)와 job 상태(10값)는 별개 개념임을 주석 구분. **outbox/claim/pending-terminal 필드 계약 주석 명시: `nextAction` ('create'|'poll'|'fetch'|'judge'|'pose_check'|'postprocess'|None), `dispatchState` ('pending'|'sent'|None), `nextDispatchAtMs`, `generation`, `outboxSeq` (transition 마다 단조 증가 — action instance 식별자, generation 과 독립), `claimedOutboxSeq`/`claimState`/`claimOwner`/`claimLeaseExpiresAt` (worker claim 4상태 판정 필드 — 5차 B5-01), `pendingTerminalState` ('done'|'failed'|None)/`pendingFailureReason` (postprocessing 진입 시 고정되는 종결 의도 — 5차 H5-05), `pairId`/`pairHmacKeyVersion` (postprocessing 진입 시 1회 고정 — 5차 B5-03), `pairAttempt`/`cleanupAttempt` (durable retry 카운터 — 5차 H5-02). nextAction/dispatchState/outboxSeq 는 state 전이와 같은 transaction 에서만 기록됨.**</action>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0,'backend/shared/python'); from sunity_shared import models; assert models.VISUAL_STATUSES==('pending','done','failed'); assert 'retry_ready' in models.VISUAL_JOB_STATES and 'postprocessing' in models.VISUAL_JOB_STATES and 'dispatch_failed' not in models.VISUAL_JOB_STATES and len(models.VISUAL_JOB_STATES)==10; assert 'create_unconfirmed' in models.VISUAL_FAILURE_REASONS and 'orphaned_analysis' in models.VISUAL_FAILURE_REASONS and 'cleanup_blocked' in models.VISUAL_FAILURE_REASONS; assert models.VISUAL_CLAIM_LEASE_MS < 1800000; assert models.visual_job_id('u','a','rotation')=='u_a_rotation'"</automated>
  </verify>
  <acceptance_criteria>
    - VISUAL_JOB_STATES 10종 (retry_ready + postprocessing 포함, dispatch_failed 부재, 신규 state 미추가)
    - VISUAL_FAILURE_REASONS 11종 — create_unconfirmed/judge_input_too_large/orphaned_analysis/cleanup_blocked 포함
    - VISUAL_CLAIM_LEASE_MS 존재 + < 1800000 (visibility 보다 짧음)
    - visual_job_id / visual_job_doc_path / visual_dispatch_cursor_doc_path 함수 존재
    - outbox/claim/pending-terminal 필드 계약(nextAction/dispatchState/nextDispatchAtMs/generation/outboxSeq/claimedOutboxSeq/claimState/claimOwner/claimLeaseExpiresAt/pendingTerminalState/pendingFailureReason/pairId/pairHmacKeyVersion/pairAttempt) 주석 존재
  </acceptance_criteria>
  <done>side-effect 단위 상태(postprocessing 포함)·멱등키·outbox instance·claim 4상태·pending-terminal·lease 상수 계약이 상수로 고정됨.</done>
</task>

<task type="auto">
  <name>Task 3: firestore_admin — outbox instance CAS + claim 4상태 + owner CAS finalize + durable-cursor dispatcher</name>
  <files>backend/shared/python/sunity_shared/firestore_admin.py, backend/tests/phase31/test_visual_jobs.py</files>
  <read_first>backend/shared/python/sunity_shared/firestore_admin.py lines 1138-1187 (update_analysis_fault_zoom — field-path update 선례) + lines 104-128 (_validate_dict_only_scalars 재사용) + _doc 헬퍼, backend/tests/phase31/conftest.py (_FakeTransaction — run_contended/commit_lost/clock)</read_first>
  <action>함수 7종 신설 (모듈 docstring 에 리뷰 근거 인용 — B3-01/B3-02/B3-03 + B4-01/B4-03 + H4-01/H4-07/H4-08 + 5차 B5-01/H5-03/H5-04/H5-05/H5-06):
(1) `reserve_visual_job(uid, analysis_id, kind, *, date_key=None, user_limit=None, global_limit=None, payload=None, allow_retry_failed=False) -> dict` — `@firestore.transactional` **읽기 전부 → 쓰기 전부**: job read + analysis read(부재 → {"created": False, "reason": "analysis_missing"}) + (rotation+limit 인자 시) quota counter read. job 존재 → {"created": False, "job": {...전체 필드...}} (호출측 분기 근거 — B2-03). state 'failed' + allow_retry_failed=True 만 재예약 (state 'reserved' 리셋 + generation+1 + payload 재기록 + rotation quota 재소비 + outbox/analysis 재기록 + claim/pending-terminal 필드 리셋). 한도 이상 → {"created": False, "reason": "daily_limit"}. 신규 쓰기 (같은 transaction): counter+1(rotation) + job {uid, analysisId, kind, state:'reserved', **nextAction:'create', dispatchState:'pending', nextDispatchAtMs: now, outboxSeq: 1, claimedOutboxSeq: 0, claimState: None, claimOwner: None, claimLeaseExpiresAt: 0** (초기 outbox+claim — B4-01/B5-01), generation:1, attempt:0, retryCount:0, taskId:None, failureReason:None, pendingTerminalState:None, pendingFailureReason:None, pairAttempt:0, cleanupAttempt:0, requestedAtMs, updatedAtMs, quotaDateKey, payload 병합} + **analysis field-path result.{kind}Status='pending' + result.{kind}UpdatedAtMs (H-06; 늦은 pending 불가 — B3-03)** → {"created": True, "job": ...}. payload = flat scalar dict (`_validate_dict_only_scalars`). docstring: correctedPose 호출측은 immutable srcKey S3 conditional put/head 검증을 먼저 마친 뒤에만 호출 (upload-first — B3-03).
(2) `transition_visual_job(job_id, *, expect_states, updates, next_action, expect_generation=None, expect_outbox_seq, expect_claim_owner=None, next_dispatch_at_ms=None) -> dict | None` — **단일 transaction, 갱신된 job snapshot 반환(실패=None) — 5차 H5-03/H5-04**: state ∉ expect_states → None; expect_generation 불일치 → None (구세대 차단 — B3-01); **expect_outbox_seq 불일치 → None (구 outbox instance 차단 — B4-01)**; **expect_claim_owner 지정 시 job.claimOwner != expect_claim_owner OR job.claimedOutboxSeq != expect_outbox_seq → None (lease 잃은 늦은 worker 차단 — 5차 B5-01)**. validator: updates['state']=='polling' 인데 비어있지 않은 taskId 없으면 ValueError; 'creating' 인데 leaseOwner/leaseExpiresAt 미비 ValueError; **from ∈ ('retry_ready',) & to 'creating' → taskId is None 강제(위반 ValueError) + generation+1 승격**; **updates['state']=='creating' 이면 헬퍼가 requestKey = f"{job_id}:gen{새_generation}" 를 같은 transaction 에서 생성(caller 제공 requestKey 무시) + suffix==persisted generation assert (5차 H5-03)**; updates['state'] ∈ ('done','failed') → ValueError ("terminal 은 finalize_visual_job 전용"); next_action=None 은 'creating' 전이에서만 허용 (그 외 nonterminal next_action 부재 → ValueError); to 'postprocessing' 은 next_action='postprocess' 필수. 통과 시 updates + updatedAtMs + **nextAction=next_action + dispatchState='pending'(creating 은 None 클리어) + nextDispatchAtMs + outboxSeq = 현재+1** 원자 기록 → **갱신된 job dict 반환(outboxSeq/generation/requestKey 포함)**.
(3) `claim_visual_job_action(job_id, *, generation, action, outbox_seq, owner, lease_ms, now_ms) -> str` — **B5-01 4상태 claim**: 단일 transaction — job read: (a) job.state ∈ ('done','failed') OR job.generation > generation OR job.outboxSeq > outbox_seq → **'completed'** (이미 다음 instance/terminal — 정상 ACK, 외부 0); (b) generation/nextAction/outboxSeq 가 인자와 불일치(위 조건 외) → **'stale'** (외부 0, 정상 ACK); (c) claimedOutboxSeq == outbox_seq AND claimOwner 존재 AND claimLeaseExpiresAt > now_ms → **'busy'** (same-seq active lease — 정상 ACK 금지); (d) 그 외(미claim 또는 same-seq lease 만료) → claimedOutboxSeq=outbox_seq + claimOwner=owner + claimLeaseExpiresAt=now_ms+lease_ms + claimState='claimed' 원자 기록 → **'claimed'** (실행). create 는 claim 미사용 — creating lease 특례 유지 (docstring). **claimState/claimOwner/claimLeaseExpiresAt 3필드로 4상태 판정 — claimedOutboxSeq 단독 판정 금지 박제.**
(4) `finalize_visual_job(job_id, *, terminal_state, failure_reason, job_meta, display_status, key=None, joint=None, expect_states, expect_generation=None, expect_outbox_seq=None, expect_claim_owner=None) -> str` — **B3-02 + H4-07 + 5차 B5-01 owner CAS**: 단일 multi-document transaction — read: job(state ∈ expect_states + generation/outbox_seq 검증; **expect_claim_owner 지정 시 owner+seq CAS — 불일치 → 'stale' no-op**) + **uid/analysisId/kind 를 job 문서에서 파생 (caller 인자 제거 — cross-analysis tampering 차단)** + analysis read. **모순 조합 거부: terminal_state=='done' → display_status=='done' & key 필수(ValueError otherwise); terminal_state=='failed' → display_status=='failed' & key is None 강제**; display_status ∈ VISUAL_STATUSES; key 는 job 문서 파생 uid/analysisId 로 "results/{uid}/{analysis_id}/" prefix 강제 (위반 ValueError — H-02); terminal_state ∈ ('done','failed'). write: job {state, failureReason, job_meta flat scalar 병합, nextAction:None, dispatchState:None, updatedAtMs} + analysis field-path result.{kind}Status/{kind}Key(done)/{kind}Joint(correctedPose done)/{kind}UpdatedAtMs. **analysis 부재 → job 만 {state:'failed', failureReason:'orphaned_analysis'} + 'orphaned_analysis' 반환**. 분석 공용 top-level updatedAt 미갱신. 정상 'finalized'. docstring: canonical S3 put 은 호출 **전** immutable key (재실행 안전).
(5) `mark_visual_job_dispatched(job_id, *, expect_generation=None, expect_action, expect_outbox_seq) -> bool` — **B4-01 CAS**: dispatchState=='pending' AND generation==expect_generation AND nextAction==expect_action AND outboxSeq==expect_outbox_seq → 'sent' (worker/dispatcher SQS send 성공 후 호출). 하나라도 불일치 → False (이전 sender 의 늦은 mark 는 seq/action 불일치로 다음 outbox 를 덮지 못함).
(6) `list_dispatch_pending(now_ms, *, limit=20, max_scan=1000) -> dict` — dispatcher 전용. **composite index 회피 + starvation 제거 (H4-08 옵션3 pagination) + durable cursor 순환 (5차 H5-06) + lease-expired 복구 (5차 B5-01):** (a) 단일 등가 쿼리 `where('dispatchState','==','pending').order_by('__name__')` 를 **durable scan cursor(visual_dispatch_cursor_doc_path 저장 위치)에서 이어서** max_scan 까지 cursor pagination — 끝 도달 시 처음으로 wrap + cursor 갱신 (등가+range 복합 쿼리 금지 — composite index 필수라 실 EventBridge FAILED_PRECONDITION; STATE.md phase-33 3f6681f 선례). 스캔 전량에서 nextDispatchAtMs<=now_ms 인 due 항목을 모아 nextDispatchAtMs 오름차순 정렬 → 상위 limit; (b) **별도 스캔: `where('dispatchState','==','sent')` 중 claimLeaseExpiresAt < now_ms AND state nonterminal 인 job 을 복구 대상으로 포함(재발행 — dispatchState 를 pending 으로 되돌리지 않고, 반환 items 에 실어 dispatcher 가 재send + mark — B5-01 crash 복구 주체)**. first-N slice 고정 금지. 반환 = {"items": [{jobId, nextAction, generation, outboxSeq, state}], "backlog_max_age_ms": due 항목 중 실제 최고 경과, "truncated": 스캔이 max_scan 에 도달했는가, "cursor_wrapped": bool}. docstring 에 "복합 인덱스 금지 이유 + durable cursor 순환 + lease-expired 복구 + age 는 실 backlog" 박제.
(7) `update_analysis_visual(uid, analysis_id, kind, *, status, key=None, joint=None)` — terminal reconciler(31-09 운영 스크립트) 전용 — production worker 경로 호출 금지 (docstring + 31-09 acceptance grep). status ∈ VISUAL_STATUSES, kind 별 field-path, top-level updatedAt 미갱신, key prefix 강제, URL 파라미터 없음.
test_visual_jobs.py 실 테스트: **reserve — 신규 예약 1 transaction 에 job(reserved + create + pending + outboxSeq 1 + claim 필드 초기값) + analysis pending 동시; analysis 부재 → created False + job 0; 동일 키 2회 → created False + counter 1회; 한도 → daily_limit; failed+allow_retry True → generation 2 + outbox/claim 재기록; run_contended 동시 reserve 2건 → job 1 + counter +1**; **transition — 정상 전이 시 outboxSeq 증가 + snapshot 반환(outboxSeq/generation/requestKey) + nextAction/dispatchState/nextDispatchAtMs state 와 동일 transaction; terminal state → ValueError; nonterminal next_action=None('creating' 외) → ValueError; polling taskId 부재 ValueError; creating lease 미비 ValueError; retry_ready→creating 에 taskId 있으면 ValueError·없으면 generation+1 + requestKey suffix==새 generation (H5-03); expect_generation 불일치 → None; expect_outbox_seq 불일치 → None + 무변경; expect_claim_owner 불일치(다른 owner 재claim 후) → None (B5-01 늦은 write 차단)**; **claim (B5-01 4상태) — 미claim 매칭 → claimed(claimState/owner/lease 기록); same-seq + lease 유효 → busy(무변경); same-seq + lease 만료 → claimed(재claim, 새 owner/lease); nextAction/generation/outboxSeq 불일치 → stale; job outboxSeq 이미 초과/terminal → completed; clock 주입으로 만료 전/후 결정론 재현**; **mark_visual_job_dispatched — generation/action/outboxSeq 3중 일치만 sent; 이전 action 의 늦은 mark(action 또는 seq 불일치) → False + pending 유지 (§6 clobber 테스트)**; **finalize — job terminal + analysis 표시 한 transaction 동시 + nextAction/dispatchState 클리어; uid/analysisId/kind 를 job 문서에서 파생(caller 가 틀린 analysisId 주입해도 job 값 사용); expect_claim_owner 불일치 → stale no-op(늦은 worker); done+display failed 또는 failed+key → ValueError; analysis 부재 → orphaned_analysis; commit_lost → 재호출 'stale' no-op + 두 문서 일치**; **list_dispatch_pending — durable cursor 이어스캔 + wrap + max_scan 초과 backlog 순환 drain(1,200 항목 여러 호출로 전량) + due 필터 + backlog_max_age_ms 는 slice 아닌 전체 최고령 + sent+lease-expired 복구 항목 포함 + 단일 등가 쿼리(range 미발생) + truncated 플래그**; update_analysis_visual URL 파라미터 부재 + top-level updatedAt 미기록; payload nested ValueError.</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31/test_visual_jobs.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - firestore_admin.py 에 reserve_visual_job / transition_visual_job / claim_visual_job_action / finalize_visual_job / mark_visual_job_dispatched / list_dispatch_pending / update_analysis_visual 7종 존재
    - transition 이 snapshot 반환 + outboxSeq 증가 + expect_outbox_seq/expect_claim_owner CAS + terminal 거부 + retry_ready→creating taskId None·generation+1·requestKey 새 generation (테스트 고정 — B4-01/B4-03/H5-03)
    - claim_visual_job_action 이 4상태(claimed/busy/stale/completed) + same-seq active=busy·expired=재claim + clock 주입 테스트 green (B5-01)
    - mark_visual_job_dispatched 가 action+outboxSeq 3중 CAS — 늦은 mark clobber 방지 (B4-01)
    - finalize 가 uid/analysisId/kind 를 job 문서 파생 + expect_claim_owner CAS + 모순 조합 ValueError + orphaned_analysis + commit_lost 일치 (B3-02 + H4-07 + B5-01)
    - list_dispatch_pending 이 durable cursor 순환 + 1,200 backlog 전량 drain + sent+lease-expired 복구 + 실 backlog age + 단일 등가 쿼리 (H4-08 + H5-06 + B5-01)
    - `python -m pytest backend/tests/phase31/test_visual_jobs.py -x -q` exits 0
  </acceptance_criteria>
  <done>quota·멱등·outbox instance CAS·claim 4상태·owner CAS·원자 finalize·durable postprocessing·durable-cursor dispatcher 규율이 함수 시그니처와 테스트로 강제됨.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 요청/워커 → visualJobs 문서 | 상태 전이의 유일한 진실 — transaction CAS+validator+outboxSeq+claim owner 로만 변경 |
| SQS 메시지 → worker 외부 side-effect | claim_visual_job_action claimed 상태만 — busy/stale/completed 는 외부 0 |
| job terminal → analysis 표시 | finalize_visual_job 단일 transaction 로만 — job 문서 파생 인자 + owner CAS |
| 표시 필드 → 앱 | canonical key 만 — URL 은 인증 재서명 경로로만 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-05 | Denial of Service (과금) | quota 예약 | mitigate | transaction 원자 예약 — 이중 소비·고아 quota 불가 (run_contended 테스트, H-06) |
| T-31-06 | Tampering | stale/구세대/구-outbox SQS 메시지의 되감기 | mitigate | transition CAS + generation + expect_outbox_seq + expect_claim_owner + taskId/lease validator (B-03 + B2-02 + B3-01 + B4-01 + B5-01) |
| T-31-07 | Information Disclosure | Firestore 에 만료 URL 저장 | mitigate | finalize/update 에 URL 파라미터 부재 + key prefix ValueError + taskId 만 (H-02 + H3-01) |
| T-31-08 | Elevation of Privilege | visualJobs 노출 / cross-analysis finalize | mitigate | 백엔드 전용 컬렉션 + finalize 의 uid/analysisId 를 job 문서 파생 (H4-07) |
| T-31-52 | Denial of Service (과금) | creating crash 후 무조건 재생성 | mitigate | lease 만료 + taskId 부재 = 'create_unconfirmed' 수동 판정 (B2-02/M3-07) |
| T-31-56 | Denial of Service | CAS 후 send 유실 / 늦은 mark clobber 로 continuation 유실 | mitigate | 모든 전이 outboxSeq 원자 기록 + mark action+seq CAS + dispatcher pending 복구 (B3-01 + B4-01) |
| T-31-57 | Tampering | terminal job / analysis 영구 불일치 | mitigate | finalize_visual_job multi-document transaction + orphaned_analysis + reconciler (B3-02) |
| T-31-62 | Denial of Service (과금) | standard SQS 중복 전달로 유료 action 중복 실행 | mitigate | claim 4상태 — busy 는 정상 ACK 금지, claimed 만 외부 호출 (B5-01) |
| T-31-63 | Tampering | finalize 직후 crash / 실패 경로 우회로 pair/cleanup 유실 | mitigate | postprocessing durable state — 성공+실패 전부 경유, exact-prefix cleanup 후 finalize (H4-01 + H5-05) |
| T-31-69 | Denial of Service | claim 직후 crash 로 job 영구 pending | mitigate | dispatcher 가 dispatchState='sent'+lease 만료+nonterminal 재발행 + busy→batchItemFailures (B5-01) |
| T-31-70 | Tampering | lease 잃은 늦은 worker 의 결과 write | mitigate | transition/finalize expect_claim_owner+seq CAS (B5-01) |
</threat_model>

<verification>
- `python -m pytest backend/tests -q` green (전체 무회귀)
- firestore_admin 신규 함수가 _validate_dict_only_scalars 관례 준수 (nested 거부)
- 워커/요청 production 경로에서 update_analysis_visual 직접 호출 금지는 31-09/31-10 acceptance grep 으로 이어받음
</verification>

<success_criteria>
- 단일 visual job 계약(10상태·retry_ready·postprocessing·lease·outbox instance(outboxSeq)·claim 4상태·owner CAS·generation·원자 finalize·멱등키·CAS validator·typed failure 11종·전용 timestamp·canonical key·durable-cursor dispatcher)이 테스트로 고정
- 5차 리뷰 §7 실행 허용 조건 중 claim 4상태·owner CAS·crash 복구 주체·durable postprocessing·fresh requestKey·cursor 순환의 계약 지점
- 9개 테스트 파일 스캐폴드 + 경쟁 transaction/시계 mock 완성
</success_criteria>

<output>
Create `.planning/phases/31-api-visual-correction/31-02-SUMMARY.md` when done
</output>
</content>
