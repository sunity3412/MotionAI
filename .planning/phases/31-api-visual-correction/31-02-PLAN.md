---
phase: 31-api-visual-correction
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/phase31/conftest.py
  - backend/tests/phase31/test_visual_jobs.py
  - backend/tests/phase31/test_visual_gen.py
  - backend/tests/phase31/test_pose_gate.py
  - backend/tests/phase31/test_pair_store.py
  - backend/tests/phase31/test_visual_worker.py
  - backend/tests/phase31/test_visual_dispatcher.py
  - backend/tests/phase31/test_visual_request.py
  - backend/tests/phase31/test_visual_url.py
  - backend/tests/phase31/test_visual_dispatch.py
  - backend/shared/python/sunity_shared/models.py
  - backend/shared/python/sunity_shared/firestore_admin.py
autonomous: true
requirements: [D-05, D-06, D-07, D-08]
must_haves:
  truths:
    - "실루엣(correctedPose)·회전(rotation)이 공유하는 단일 durable visual job 계약이 존재한다 — kind='correctedPose'|'rotation' (리뷰 H-01)"
    - "모든 nonterminal state 전이는 단일 Firestore transaction 으로 {현재 state/generation 검증 + 다음 state + nextAction + dispatchState='pending' + nextDispatchAtMs + 새 outboxSeq} 를 원자 기록하고 갱신된 job snapshot 을 반환한다 — 후속 단계(send/mark/2차 전이/finalize)는 반환 snapshot 하나만 사용하며 인바운드 msg 값을 재사용하지 않는다 (3차 B3-01 + 4차 B4-01 + 5차 H5-03/H5-04)"
    - "outbox 는 generation 과 별개인 단조 증가 outboxSeq 로 action instance 를 식별한다 — mark_visual_job_dispatched CAS = generation==expected AND nextAction==expected_action AND outboxSeq==expected_outbox_seq AND dispatchState=='pending'. 이전 action 의 늦은 ACK 는 seq 불일치로 no-op 이라 다음 continuation 을 덮을 수 없다 (4차 리뷰 B4-01)"
    - "worker 는 외부 side-effect 전에 claim_visual_job_action() 으로 claim 하고 반환은 dict `{status, job}` 다 — status ∈ stale(generation/action/outboxSeq 불일치)·claimed(미claim 또는 same-seq lease 만료 → owner/lease 원자 갱신 후 실행)·busy(same-seq + lease 유효 → 정상 ACK 금지)·completed(이미 다음 outboxSeq/terminal). `job` = 갱신된 snapshot(최소 {claimOwner, outboxSeq, claimLeaseExpiresAt, generation, state}). claimedOutboxSeq 만이 아니라 claimState/claimLeaseExpiresAt/claimOwner 를 검사하며, lease 는 action worst-case runtime 보다 길고 SQS visibility 1800s 보다 짧다 (VISUAL_CLAIM_LEASE_MS 단일 출처) (5차 B5-01 + 6차 B6-01)"
    - "claim 성공(status=='claimed') 호출자는 claim 이 반환한 snapshot 만 action 에 전달한다 — claim 이전 job 을 action 에 재사용하는 정적 코드 경로가 없어야 한다(31-09 grep). expect_claim_owner/expect_outbox_seq 는 claim snapshot 의 owner/seq 로 세팅 (6차 리뷰 B6-01)"
    - "action 성공 transition/finalize 는 현재 claim owner+outboxSeq+미만료 lease 를 함께 CAS 한다 — expect_claim_owner 지정 시 job.claimOwner==expect_claim_owner AND job.claimedOutboxSeq==expect_outbox_seq AND now_ms < job.claimLeaseExpiresAt 셋 다 통과해야 write. lease 를 잃은(재claim 당한/만료된) 늦은 worker 의 결과 write 가 새 owner 의 진행을 덮지 못한다 (5차 B5-01 + 6차 H6-07)"
    - "transition 이 새 outboxSeq 를 만들 때 다음 action 용 claim 필드를 원자 clear 한다 — claimState=None, claimOwner=None, claimLeaseExpiresAt=0. claimedOutboxSeq 는 audit 로 유지하되 새 outboxSeq 와 반드시 다르다(=이전 seq). 이로써 다음 action 의 첫 claim 이 새 owner 를 온전히 획득하고, retry_ready→create snapshot 에 이전 claim owner 잔존이 0 이다 (6차 리뷰 B6-01)"
    - "create action 은 claim CAS 를 사용하지 않는다 — state/outboxSeq + creating lease(leaseOwner/leaseExpiresAt) 로만 보호한다. reserve/retry_ready snapshot 의 claim 필드는 초기값/clear 상태라 create 전이가 claim owner CAS 로 막히지 않는다 (6차 리뷰 B6-01)"
    - "claim 직후 crash 는 lease 만료 뒤 반드시 재실행된다 — dispatcher 가 dispatchState=='sent' + `claimedOutboxSeq==outboxSeq AND claimState=='claimed' AND 0<claimLeaseExpiresAt<=now_ms` + nonterminal job 만 복구 대상으로 재발행하고(durable 복구 주체 — 미claim sent(lease 0)·구 seq claim 은 제외), busy 메시지는 batchItemFailures 로 SQS visibility 뒤 재전달된다. '재전달 no-op 멱등' 단독은 복구 주체가 아니다 (5차 B5-01 + 6차 H6-01)"
    - "terminal 종결은 finalize_visual_job() 하나뿐이다 — job terminal(done|failed)+메타 와 analysis visual status 가 하나의 Firestore multi-document transaction. uid/analysisId/kind 는 caller 인자가 아니라 job 문서에서 파생, done↔display done / failed↔display failed 고정, done=canonical key 필수·failed=canonical key 금지 (3차 B3-02 + 4차 H4-07)"
    - "correctedPose 는 성공/실패 어느 terminal intent 든 pose_checking 통과 여부와 무관하게 postprocessing state 를 거쳐 종결한다 — job 에 pendingTerminalState/pendingFailureReason 를 실어 postprocess action 이 임시 입력 삭제(+동의 시 pair store)를 durable 하게 수행한 뒤 한 번의 finalize 로 done/failed 를 기록한다 (4차 H4-01 + 5차 H5-05)"
    - "임시 생체 프레임(correctedPose source/staging)은 비-버저닝 전용 버킷(template VisualInputBucketName)에 있어 cleanup = 단일 delete_object(key) + head 존재 확인(0)이다 — version 열거/삭제가 불필요. 학습 페어(training/phase31/pairs/)만 기존 버저닝 버킷에 남아 version-aware 삭제(31-07) 대상이다 (6차 belle 아키텍처 조정)"
    - "finalize_visual_job 은 `cleanup_verified_at_ms` 를 **파라미터로 받아** 같은 transaction 안에서 inputSealed/privacyBlocker/terminal intent 검증 후 candidate job 에 병합하고 validate+write 한다 — job_meta 로만 전달해 validator 가 기존 문서(0)를 읽어 항상 ValueError 나던 순서 모순 해소. cleanup 시각 기록과 terminal 전이가 한 transaction (7차 리뷰 B7-03)"
    - "correctedPose finalize validator 는 **kind=='correctedPose' 이면 (inputSealed 값 무관·done|failed 무관) candidate inputSealed is True + candidate cleanupVerifiedAtMs>0 + candidate privacyBlocker is None 셋 다 unconditional 요구한다** — inputSealed==True 조건부 아님(8차 B8-02: inputSealed=False done/failed·cleanupVerified=0 done/failed 네 조합 전부 ValueError). done 은 추가로 key 필수+failure_reason None, failed 는 typed reason+key None. **cleanup_verified_at_ms>0 호출은 같은 transaction 에서 privacyBlocker=None 을 원자 clear 해 cleanup_blocked→terminal 을 완성한다(8차 B8-03). job_meta 로 privacyBlocker/cleanupVerifiedAtMs 지정 금지(dedicated parameter 만).** model/finalizer/worker 3면 'correctedPose terminal(done|failed) 공통' lockstep (M8-03) (6차 B6-04 + 7차 B7-04 + 8차 B8-02/B8-03)"
    - "cleanup 미완은 terminal 이 아니다 — remainingObject(head 존재)>0 이면 state 는 postprocessing 유지 + privacyBlocker='cleanup_blocked' + nextAction='postprocess' + 장기 backoff. cleanup_blocked 는 VISUAL_PRIVACY_BLOCKERS 값(비-terminal)이며 VISUAL_FAILURE_REASONS(terminal)이 아니다. 해결 후 같은 postprocess 가 재확인 0(+cleanupVerifiedAtMs 기록) 후에만 원래 pendingTerminalState 로 finalize 한다 (6차 리뷰 B6-04)"
    - "표시 필드 갱신은 전용 timestamp(correctedPoseUpdatedAtMs/rotationUpdatedAtMs)만 쓴다 (리뷰 H-06). Firestore 에는 canonical S3 key 만 저장 — outputUrl/signedUrl 필드가 계약에 없다 (H-02 + H3-01)"
    - "list_dispatch_pending 은 Firestore composite index 를 요구하지 않는다 — pending 스캔(dispatchState=='pending')과 sent-recovery 스캔(dispatchState=='sent')이 각각 독립 durable cursor(visual_dispatch_cursor / visual_dispatch_sent_cursor) 로 순환 pagination 한다. sent-recovery 는 H6-01 정확 필터(same-seq expired claim)만 재발행. max_scan 초과 backlog 도 순환 전량 drain, 초과 시 truncated 플래그/alarm(31-10), metric 은 스캔 window 최고령임을 명시(ScannedOutboxMaxAgeMs) (프로젝트 선례 STATE.md phase-33 3f6681f; 4차 H4-08 + 5차 H5-06/B5-01 + 6차 H6-01/M6-01)"
  artifacts:
    - path: "backend/shared/python/sunity_shared/models.py"
      provides: "VISUAL_STATUSES / VISUAL_JOB_KINDS / VISUAL_JOB_STATES(10종 — retry_ready·postprocessing 포함) / VISUAL_FAILURE_REASONS(10종 terminal) / VISUAL_PRIVACY_BLOCKERS(비-terminal, cleanup_blocked) / VISUAL_CLAIM_LEASE_MS + outbox·claim·pending-terminal·privacy 필드 계약(outboxSeq/claimedOutboxSeq/claimState/claimOwner/claimLeaseExpiresAt/pendingTerminalState/pendingFailureReason/pairAttempt/cleanupAttempt/inputSealed/privacyBlocker/cleanupVerifiedAtMs) + visual_job_doc_path + visual_dispatch_cursor_doc_path + visual_dispatch_sent_cursor_doc_path + VISUAL_RESERVATION_STATES/VISUAL_ORPHAN_STATES/VISUAL_INPUT_RESERVATION_TTL_MS + visual_input_reservation_doc_path/visual_orphan_doc_path + reservation/orphan cursor path + visual_input_object_doc_path + VISUAL_OBJECT_STATES(active|deleting) + concrete TTL/lease/skew 상수(10차 H10-03) (8차 B8-05/B8-06/H8-01 + 10차 B10-02/H10-03)"
      contains: "VISUAL_JOB_STATES"
    - path: "backend/shared/python/sunity_shared/firestore_admin.py"
      provides: "reserve_visual_job(reservation claim B8-06) / transition_visual_job / claim_visual_job_action / begin_visual_job_create(B8-01 create CAS) / finalize_visual_job(unconditional correctedPose gate B8-02 + privacyBlocker 원자 clear B8-03) / read_visual_job / mark_visual_job_dispatched / list_dispatch_pending / update_analysis_visual + reservation/orphan state machine(claim lease 복구) + key-ownership active|deleting delete-fence state machine(acquire fence/_promote_..._tx/release/claim_key_for_delete self-ref 소비, 10차 B10-01~03/H10-01/H10-04) helpers"
      contains: "def claim_visual_job_action"
    - path: "backend/tests/phase31/conftest.py"
      provides: "sys.path 주입 + _FakeDoc/_FakeSnapshot/_FakeTransaction(경쟁 interleave 지원 + fixture clock) + DashScope urllib mock fixture"
  key_links:
    - from: "backend/shared/python/sunity_shared/firestore_admin.py"
      to: "backend/shared/python/sunity_shared/models.py"
      via: "상수 검증"
      pattern: "models\\.VISUAL_"
---

<objective>
3차 리뷰 B3-01/B3-02/B3-03 + 4차 B4-01/B4-03/H4-01/H4-07/H4-08 + 5차 B5-01/H5-03/H5-04/H5-05/H5-06 + 6차 B6-01/B6-04/H6-01/H6-07/M6-01 + 7차 B7-03/B7-04/H7-03 + 8차 B8-01/B8-02/B8-03/B8-05/B8-06/H8-03 + 9차 B9-01/B9-02/H9-01/H9-03/H9-04/H9-06/H9-07 + 10차 B10-01(self-ref 소비)/B10-02(active·deleting delete fence)/B10-03(전 종료경로 ref release)/H10-01(promote 내부tx)/H10-02(expireAt timestamp+TTL policy)/H10-03(concrete TTL+trusted clock)/H10-04(typed/bounded refs) 을 데이터 계약 층에서 구현한다. 핵심 원칙:

1. **outbox instance ownership + claim dict 4상태 (B3-01 + B4-01 + B5-01 + 6차 B6-01):** 모든 state 전이가 단일 Firestore transaction 으로 {현재 state/generation 검증 + 다음 state + nextAction + dispatchState='pending' + nextDispatchAtMs + **단조 증가 outboxSeq**} 를 원자 기록하고 **갱신된 job snapshot 을 반환**한다. 메시지 = {jobId, generation, action, outboxSeq}. worker 는 외부 side-effect **전에** `claim_visual_job_action` transaction 으로 claim 하며 **반환은 dict `{status, job}`(status 4상태 + 갱신 snapshot)** 다 — **claimedOutboxSeq 만이 아니라 claimState/claimLeaseExpiresAt/claimOwner 로 same-seq active lease(busy) 와 same-seq expired lease(재claim=claimed) 를 구분**한다. busy 는 정상 ACK 금지. **claim 성공 호출자는 claim 이 반환한 snapshot 만 action·후속 CAS 에 사용하고(pre-claim job 재사용 금지 — 정적 검사)**, action 성공 transition/finalize 는 **현재 claim owner+outboxSeq+미만료 lease 를 함께 CAS**(expect_claim_owner + now_ms < claimLeaseExpiresAt)해 lease 를 잃은 늦은 worker 의 write 를 차단한다.
2. **새 outboxSeq 시 claim 필드 원자 clear (B6-01):** 전이가 새 outboxSeq 를 만들 때 claimState=None/claimOwner=None/claimLeaseExpiresAt=0 을 같은 transaction 에서 clear 한다. claimedOutboxSeq(audit)는 새 seq 와 달라야 한다. **create action 은 claim CAS 미사용** — reserve/retry_ready snapshot 의 claim 필드가 clear 상태라 create 전이가 owner CAS 로 막히지 않는다.
3. **crash 복구 주체 (B5-01 + 6차 H6-01):** claim 직후 crash 는 lease 만료 뒤 반드시 재실행돼야 한다. `list_dispatch_pending` 이 **dispatchState=='sent' + `claimedOutboxSeq==outboxSeq AND claimState=='claimed' AND 0<claimLeaseExpiresAt<=now_ms` + nonterminal** job 만 복구 대상으로 반환(durable 복구 주체 — 미claim sent(lease 0)·구 seq claim 은 제외)하고, worker 의 busy 응답은 batchItemFailures 로 SQS visibility 뒤 재전달된다. lease(`VISUAL_CLAIM_LEASE_MS` 단일 출처)는 action worst-case runtime(300s Lambda timeout)+margin 보다 길고 visibility 1800s 보다 짧다(31-10 template build gate 로 강제 — H6-07).
4. **원자 finalize + privacy validator (B3-02 + H4-07 + 6차 B6-04 + 7차 B7-03/B7-04):** job terminal 과 analysis visual status 는 `finalize_visual_job()` 하나의 multi-document transaction 으로만 갱신. **uid/analysisId/kind 는 caller 인자가 아니라 job 문서에서 파생**, done↔display done / failed↔display failed 고정, done 은 canonical key 필수·failed 는 canonical key 금지. **`cleanup_verified_at_ms` 를 파라미터로 받아 같은 transaction 에서 candidate 병합 후 validate(B7-03 순서 모순 해소)**, **correctedPose 는 (inputSealed 값·done|failed 무관) unconditional 로 candidate inputSealed is True + cleanup_verified_at_ms>0 + privacyBlocker None 셋 다 validator 가 요구(8차 B8-02, 9차 M9-01: 'inputSealed==True면' 조건부 아님)**. remainingObject>0(cleanup 미완) job 은 terminal finalize 불가.
5. **postprocessing durable state — 성공+실패 전부 경유 + cleanup_blocked 비-terminal (H4-01 + H5-05 + 6차 B6-04):** correctedPose 는 어느 terminal intent 든 `postprocessing` 을 거쳐 종결한다. job 에 `pendingTerminalState`/`pendingFailureReason` 를 실어 postprocess action 이 **비-버저닝 visual-input 버킷의 단일 delete_object cleanup**(+동의 시 pair store)을 durable 수행한 뒤 한 번의 finalize 로 done/failed 를 기록한다. cleanup 미완이면 privacyBlocker='cleanup_blocked'(비-terminal) 로 postprocessing 유지 + 장기 backoff. (rotation 은 임시 입력·pair 가 없어 fetching 에서 직접 finalize.)
6. **reserve = dispatch 가능 상태를 여는 transaction (B3-03):** reserve transaction 이 job 예약 + analysis 'pending' + 초기 nextAction='create'/dispatchState='pending'/outboxSeq=1 + claim/privacy 필드 초기값을 함께 기록한다.
7. **fresh requestKey (H5-03):** retry_ready→creating 전이가 generation+1 승격 시 requestKey 를 그 새 generation 으로 같은 transaction 에서 생성하고 suffix==persisted generation 을 validator 가 강제한다.

vendor create 의 durable lease('creating' + leaseOwner/leaseExpiresAt/requestKey)와 'create_unconfirmed' 수동 reconciliation(자동 재생성 금지 — 2차 B2-02)은 불변. 상태 'dispatch_failed' 는 존재하지 않음 — dispatch 실패는 "dispatchState='pending' 이 남아 있는 상태" 이며 dispatcher 가 복구한다.

계약 필드명 (31-04 TS 측과 lockstep):
- result.correctedPoseStatus / correctedPoseKey / correctedPoseJoint / correctedPoseUpdatedAtMs
- result.rotationStatus / rotationVideoKey / rotationUpdatedAtMs
- URL 필드 없음. job 문서에도 outputUrl/signedUrl 없음 (H3-01 — taskId 만).

Purpose: 모든 구현 wave 가 공유하는 상태·멱등·quota·outbox instance·claim(dict 4상태+owner/lease CAS+새 seq clear)·finalize(privacy validator)·durable postprocessing·pagination-cursor 기반 — 6차 리뷰 §7 실행 허용 기준.
Output: 스캐폴드 10파일 + models.py 상수(+reservation/orphan path/state/TTL) + firestore_admin 핵심 함수 9종(begin_visual_job_create·read_visual_job 포함) + reservation(5)·orphan(3) state machine helpers + 테스트.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-api-visual-correction/31-CONTEXT.md
@.planning/phases/31-api-visual-correction/31-PATTERNS.md
@.planning/phases/31-api-visual-correction/31-PLAN-REVIEW-ITERATION10.md
@.planning/phases/31-api-visual-correction/31-PLAN-REVIEW-ITERATION9.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: backend/tests/phase31/ 스캐폴드 + mock fixture (경쟁 transaction 지원)</name>
  <files>backend/tests/phase31/conftest.py, backend/tests/phase31/test_visual_jobs.py, backend/tests/phase31/test_visual_gen.py, backend/tests/phase31/test_pose_gate.py, backend/tests/phase31/test_pair_store.py, backend/tests/phase31/test_visual_worker.py, backend/tests/phase31/test_visual_dispatcher.py, backend/tests/phase31/test_visual_request.py, backend/tests/phase31/test_visual_url.py, backend/tests/phase31/test_visual_dispatch.py</files>
  <read_first>backend/tests/phase22/conftest.py (sys.path 주입 패턴 19줄 전체), backend/tests/phase22/test_shadow_wiring.py lines 18-57 (_FakeSnapshot/_FakeDoc registry stub + "LOCAL ONLY" docstring 관례), .planning/spikes/004-gemini-omni-view-editing/wan_gate_batch.py lines 24-56 (mock 대상 urllib 형상)</read_first>
  <action>phase22 선례로 conftest.py 작성: sys.path 주입(_BACKEND.parents[2] + shared/python) + _FakeDoc/_FakeSnapshot registry stub + 신규 2종: (1) `_FakeTransaction` — firebase-admin `@firestore.transactional` 경로를 로컬 실행 가능하게 하는 stub. 요구: (a) transaction 함수가 read→write 순서로 호출되는지 기록, (b) **경쟁 시나리오 지원 (§6): 두 transaction 함수를 interleave 실행하는 `run_contended(fn_a, fn_b)` 헬퍼 — 둘 다 commit 전 상태를 read 한 뒤 fn_a 먼저 commit, fn_b 는 optimistic concurrency 모사로 자동 재시도되어 fn_a commit 결과를 다시 read 한 상태에서 실행**. (c) `commit_lost=True` 모드 — transaction 함수는 성공 commit 하되 호출측에 예외를 던져 "commit 응답 유실" 모사. (d) **시계 주입(`now_ms` 조작 가능한 fixture clock) — claim lease 만료 전/후 시간축을 결정론으로 재현 (5차 B5-01)**. (2) `dashscope_urlopen_mock` — urllib.request.urlopen monkeypatch 팩토리로 create 응답({"output":{"task_id":...}})·폴링 응답(task_status SUCCEEDED/FAILED + video_url)·모더레이션 차단 응답·redirect(HTTP 302)·oversized body 주입 가능. 테스트 9파일은 골격: 모듈 docstring("실 Firestore/네트워크/Pod 미접촉 — LOCAL ONLY" + 채우는 플랜: test_visual_jobs=31-02, test_visual_gen=31-05, test_pose_gate=31-06, test_pair_store=31-07, test_visual_worker=31-09, test_visual_dispatcher=31-09, test_visual_request/test_visual_url/test_visual_dispatch=31-10) + 미존재 모듈 pytest.importorskip 가드 + fixture 동작 확인 placeholder 1개씩. 이모지 금지, 한국어 why-주석.</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31 -x -q</automated>
  </verify>
  <acceptance_criteria>
    - conftest.py 에 `dashscope_urlopen_mock` 과 `_FakeTransaction`(run_contended + commit_lost + fixture clock 지원) 존재
    - 9개 테스트 파일에 담당 플랜 번호 docstring 존재
    - `python -m pytest backend/tests/phase31 -x -q` exits 0 (skip 허용, fail 0)
    - `python -m pytest backend/tests -q` exits 0 (기존 suite 무회귀)
  </acceptance_criteria>
  <done>후속 전 플랜이 재사용하는 mock/경쟁 transaction/시계 스캐폴드 완성.</done>
</task>

<task type="auto">
  <name>Task 2: models.py — 상태(retry_ready/postprocessing)·typed 실패·privacy blocker·outbox/claim/pending-terminal/privacy 필드 계약</name>
  <files>backend/shared/python/sunity_shared/models.py</files>
  <read_first>backend/shared/python/sunity_shared/models.py lines 399-408 (FAULT_ZOOM_STATUS_* 선례 + lockstep 주석 스타일 + analysis_doc_path 관례)</read_first>
  <action>FAULT_ZOOM 선례 동형으로 신설: `VISUAL_STATUS_PENDING/DONE/FAILED` + tuple `VISUAL_STATUSES` ('pending','done','failed'); `VISUAL_JOB_KINDS = ("correctedPose", "rotation")`; **`VISUAL_JOB_STATES = ("reserved", "creating", "polling", "retry_ready", "fetching", "judging", "pose_checking", "postprocessing", "done", "failed")` — 외부 side-effect 당 상태 1개 (B2-02). 신규 state 추가 금지(상태 폭발 방지 — 5차/6차): claim 상태·pending-terminal·privacy blocker 는 별도 필드로 표현**: creating=vendor create 진행(leaseOwner/leaseExpiresAt/requestKey 필수), polling=taskId 확보 후 폴링, retry_ready=모더레이션 재시도 대기(taskId=None 강제 + nextAction='create' outbox 동반 — B4-03), fetching=taskId 재-poll 후 다운로드/S3 전송(outputUrl 미저장 — H3-01), judging=Gemini before/after 판정, pose_checking=결정론 pose 게이트, **postprocessing=correctedPose 전용 pre-terminal — 성공/실패 어느 terminal intent 든 여기서 임시 입력 cleanup(+동의 시 pair store)을 durable 수행 후 finalize; cleanup 미완이면 privacyBlocker='cleanup_blocked' 로 postprocessing 유지(비-terminal — 6차 B6-04)**. rotation 은 fetching 에서 직접 finalize (judging/pose_checking/postprocessing/retry 미사용). 'dispatch_failed' state 부재 — 복구는 dispatchState='pending' + dispatcher (B3-01, 주석 박제)**; **`VISUAL_FAILURE_REASONS = ("moderation", "timeout", "vendor_error", "invalid_output", "judge_failed", "judge_input_too_large", "pose_gate_failed", "pose_gate_unavailable", "create_unconfirmed", "orphaned_analysis")` 10종 terminal (6차 B6-04: cleanup_blocked 를 여기서 제거 — 이제 비-terminal privacyBlocker)**; **`VISUAL_PRIVACY_BLOCKERS = ("cleanup_blocked",)` — privacy cleanup 이 max retry 후에도 미완인 비-terminal blocker. job 은 postprocessing 유지·terminal finalize 금지, 운영 복구 대상 (6차 리뷰 B6-04)**; `visual_job_id(uid, analysis_id, kind) -> str` = f"{uid}_{analysis_id}_{kind}" (멱등키) + `visual_job_doc_path(job_id) -> str` = f"visualJobs/{job_id}" (백엔드 전용 컬렉션) + **`visual_dispatch_cursor_doc_path() -> str` = "visualDispatch/scanCursor" + `visual_dispatch_sent_cursor_doc_path() -> str` = "visualDispatch/sentCursor" (pending·sent 독립 durable cursor — 5차 H5-06 + 6차 H6-01)**. **8차+9차 신규 상수: `VISUAL_RESERVATION_STATES = ("open","claimed_by_job","claimed_by_janitor","closed")` + `VISUAL_ORPHAN_STATES=("open","claimed","closed")` + `visual_input_reservation_doc_path(job_id, reservation_id)`(per-invocation immutable) + `visual_orphan_doc_path(orphan_id)` + **`visual_input_object_doc_path(bucket, key) -> str` = f"visualInputObjects/{hash(bucket,key)}" + `VISUAL_OBJECT_STATES = ("active","deleting")`(10차 B10-02 delete fence)** + **concrete 숫자 상수(10차 H10-03/H10-06): `VISUAL_INPUT_RESERVATION_TTL_MS`(=PipelineFunction Timeout×1000 + 최대 보상구간 + margin, 실제 숫자) + `VISUAL_JANITOR_CLAIM_LEASE_MS` + `VISUAL_OBJECT_DELETE_LEASE_MS` + `VISUAL_CLOCK_SKEW_MS`(모든 만료 비교 = trusted UTC epoch ms 단일 clock) + `VISUAL_CLOSED_DOC_RETENTION_MS`(closed 문서 expireAt = timestamp, H10-02 TTL policy 대상). alarm threshold(31-10)는 이 숫자에서 파생(H10-06)** + `visual_reservation_cursor_doc_path()`/`visual_orphan_cursor_doc_path()`(H8-01). reserve 반환 reason 에 'reservation_lost' 추가.** **`VISUAL_CLAIM_LEASE_MS = 360000` (=300s Lambda timeout + 60s margin; SQS visibility 1800s 보다 짧아 만료 후 재claim 가능 — 5차 B5-01; template 의 worker Timeout·VisualQueue VisibilityTimeout 과 단일 출처 근거 주석 + 31-10 build gate `WorkerTimeout*1000 < VISUAL_CLAIM_LEASE_MS < VisibilityTimeout*1000` — 6차 H6-07)**. 3면 lockstep 주석 + D-05/D-06/D-08 인용. 표시 상태('pending'|'done'|'failed', 부재=legacy 숨김)와 job 상태(10값)는 별개 개념임을 주석 구분. **outbox/claim/pending-terminal/privacy 필드 계약 주석 명시: `nextAction` ('create'|'poll'|'fetch'|'judge'|'pose_check'|'postprocess'|None), `dispatchState` ('pending'|'sent'|None), `nextDispatchAtMs`, `generation`, `outboxSeq` (transition 마다 단조 증가 — action instance 식별자, generation 과 독립), `claimedOutboxSeq`/`claimState`/`claimOwner`/`claimLeaseExpiresAt` (worker claim 4상태 판정 필드; 전이가 새 outboxSeq 생성 시 claimState/claimOwner/claimLeaseExpiresAt 원자 clear·claimedOutboxSeq audit 유지 — 6차 B6-01), `pendingTerminalState` ('done'|'failed'|None)/`pendingFailureReason` (postprocessing 진입 시 고정되는 종결 의도 — 5차 H5-05), `pairId`/`pairHmacKeyVersion` (postprocessing 진입 시 1회 고정 — 5차 B5-03), `pairAttempt`/`cleanupAttempt` (durable retry 카운터 — 5차 H5-02), **`inputSealed` (bool — pose_checking→postprocessing 진입 시 True 기록; producer(31-10) hard gate — 6차 B6-03), `privacyBlocker` ('cleanup_blocked'|None — 비-terminal cleanup 미완 marker; finalizer 가 cleanup_verified_at_ms>0 호출 시 원자 clear — 8차 B8-03), `cleanupVerifiedAtMs` (number — cleanup remainingObject==0 확인 시점; **correctedPose terminal(done|failed) 공통 finalize validator 요구 — 8차 B8-02/M8-03, 'done 전용' 아님**)**. nextAction/dispatchState/outboxSeq 는 state 전이와 같은 transaction 에서만 기록됨.**</action>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0,'backend/shared/python'); from sunity_shared import models; assert models.VISUAL_STATUSES==('pending','done','failed'); assert 'retry_ready' in models.VISUAL_JOB_STATES and 'postprocessing' in models.VISUAL_JOB_STATES and 'dispatch_failed' not in models.VISUAL_JOB_STATES and len(models.VISUAL_JOB_STATES)==10; assert 'create_unconfirmed' in models.VISUAL_FAILURE_REASONS and 'orphaned_analysis' in models.VISUAL_FAILURE_REASONS and 'cleanup_blocked' not in models.VISUAL_FAILURE_REASONS and len(models.VISUAL_FAILURE_REASONS)==10; assert models.VISUAL_PRIVACY_BLOCKERS==('cleanup_blocked',); assert models.VISUAL_CLAIM_LEASE_MS < 1800000; assert models.visual_job_id('u','a','rotation')=='u_a_rotation'; assert models.visual_dispatch_sent_cursor_doc_path()"</automated>
  </verify>
  <acceptance_criteria>
    - VISUAL_JOB_STATES 10종 (retry_ready + postprocessing 포함, dispatch_failed 부재, 신규 state 미추가)
    - VISUAL_FAILURE_REASONS 10종 terminal — create_unconfirmed/judge_input_too_large/orphaned_analysis 포함, cleanup_blocked 부재 (6차 B6-04)
    - VISUAL_PRIVACY_BLOCKERS == ('cleanup_blocked',) (비-terminal — 6차 B6-04)
    - VISUAL_CLAIM_LEASE_MS 존재 + < 1800000 (visibility 보다 짧음)
    - visual_job_id / visual_job_doc_path / visual_dispatch_cursor_doc_path / visual_dispatch_sent_cursor_doc_path 함수 존재
    - outbox/claim/pending-terminal/privacy 필드 계약() 주석 존재
  </acceptance_criteria>
  <done>side-effect 단위 상태(postprocessing 포함)·멱등키·outbox instance·claim 4상태·pending-terminal·privacy blocker·lease 상수 계약이 상수로 고정됨.</done>
</task>

<task type="auto">
  <name>Task 3: firestore_admin — outbox instance CAS + claim dict 4상태(+새 seq clear) + owner/lease CAS finalize(privacy validator) + 이중 durable-cursor dispatcher</name>
  <files>backend/shared/python/sunity_shared/firestore_admin.py, backend/tests/phase31/test_visual_jobs.py</files>
  <read_first>backend/shared/python/sunity_shared/firestore_admin.py lines 1138-1187 (update_analysis_fault_zoom — field-path update 선례) + lines 104-128 (_validate_dict_only_scalars 재사용) + _doc 헬퍼, backend/tests/phase31/conftest.py (_FakeTransaction — run_contended/commit_lost/clock), .planning/phases/31-api-visual-correction/31-PLAN-REVIEW-ITERATION6.md 의 B6-01/B6-04/H6-01/H6-07/M6-01 절</read_first>
  <action>핵심 함수 9종 + reservation/orphan helpers 신설 (모듈 docstring 근거 — B3~B6 + 5차 B5-01/H5-03~06 + 6차 B6-01/B6-04/H6-01/H6-07 + 7차 B7-03/B7-04/H7-03 + 8차 B8-01/B8-02/B8-03/B8-05/B8-06/H8-03):
(1) `reserve_visual_job(uid, analysis_id, kind, *, date_key=None, user_limit=None, global_limit=None, payload=None, allow_retry_failed=False, reservation_id=None, reservation_owner=None) -> dict` — `@firestore.transactional` **읽기 전부 → 쓰기 전부**: job read + analysis read(부재 → {"created": False, "reason": "analysis_missing"}) + (rotation+limit 인자 시) quota counter read + **(correctedPose + reservation_id 지정 시) 자기 reservation read**. **★B8-06/9차 H9-04: correctedPose 신규 쓰기 직전 같은 reserve transaction 안에서 내부 함수 `_claim_reservation_for_job_tx(transaction, job_id, reservation_id, owner=reservation_owner, now_ms)`(nested @transactional 금지) 성공 + 각 srcKey(+trainingSrcKey)에 `_promote_key_ownership_to_job_tx(transaction, bucket, key, reservation_ref, job_ref)`(10차 H10-01 — 같은 transaction 인자 공유)를 함께 수행해야만 job 생성. **read-all(job+reservation+두 object doc)→write-all 순서. 두 key 중 conflict/retry/commit-loss 에서 전부 이전 또는 전부 승격만 허용.** 'claimed_by_janitor'/부재/만료 → {"created": False, "reason": "reservation_lost"}(새 preflight 재시도). job+reservation+key-ownership 이 단일 commit/commit-loss 단위.** job 존재 → {"created": False, "job": {...전체 필드...}} (호출측 분기 근거 — B2-03). state 'failed' + allow_retry_failed=True 만 재예약 (state 'reserved' 리셋 + generation+1 + payload 재기록 + rotation quota 재소비 + outbox/analysis 재기록 + claim/pending-terminal/privacy 필드 리셋). 한도 이상 → {"created": False, "reason": "daily_limit"}. 신규 쓰기 (같은 transaction): counter+1(rotation) + job {uid, analysisId, kind, state:'reserved', **nextAction:'create', dispatchState:'pending', nextDispatchAtMs: now, outboxSeq: 1, claimedOutboxSeq: 0, claimState: None, claimOwner: None, claimLeaseExpiresAt: 0** (초기 outbox+claim clear — B4-01/B5-01/B6-01), generation:1, attempt:0, retryCount:0, taskId:None, failureReason:None, pendingTerminalState:None, pendingFailureReason:None, pairAttempt:0, cleanupAttempt:0, **inputSealed:False, privacyBlocker:None, cleanupVerifiedAtMs:0** (privacy 초기값 — B6-03/B6-04), requestedAtMs, updatedAtMs, quotaDateKey, payload 병합} + **analysis field-path result.{kind}Status='pending' + result.{kind}UpdatedAtMs (H-06; 늦은 pending 불가 — B3-03)** → {"created": True, "job": ...}. payload = flat scalar dict (`_validate_dict_only_scalars`). docstring: correctedPose 호출측은 immutable srcKey S3 conditional put/preflight 를 먼저 마친 뒤에만 호출 (upload-first — B3-03; 비-버저닝 VisualInputBucket).
(2) `transition_visual_job(job_id, *, expect_states, updates, next_action, expect_generation=None, expect_outbox_seq, expect_claim_owner=None, now_ms=None, next_dispatch_at_ms=None) -> dict | None` — **단일 transaction, 갱신된 job snapshot 반환(실패=None) — 5차 H5-03/H5-04 + 6차 B6-01/H6-07**: state ∉ expect_states → None; expect_generation 불일치 → None (구세대 차단 — B3-01); **expect_outbox_seq 불일치 → None (구 outbox instance 차단 — B4-01)**; **expect_claim_owner 지정 시 (job.claimOwner != expect_claim_owner) OR (job.claimedOutboxSeq != expect_outbox_seq) OR (now_ms is not None AND now_ms >= job.claimLeaseExpiresAt) → None (lease 잃은/만료된 늦은 worker 차단 — 5차 B5-01 + 6차 H6-07)**. validator: updates['state']=='polling' 인데 비어있지 않은 taskId 없으면 ValueError; 'creating' 인데 leaseOwner/leaseExpiresAt 미비 ValueError; **from ∈ ('retry_ready',) & to 'creating' → taskId is None 강제(위반 ValueError) + generation+1 승격**; **updates['state']=='creating' 이면 헬퍼가 requestKey = f"{job_id}:gen{새_generation}" 를 같은 transaction 에서 생성(caller 제공 requestKey 무시) + suffix==persisted generation assert (5차 H5-03)**; updates['state'] ∈ ('done','failed') → ValueError ("terminal 은 finalize_visual_job 전용"); next_action=None 은 'creating' 전이에서만 허용 (그 외 nonterminal next_action 부재 → ValueError); to 'postprocessing' 은 next_action='postprocess' 필수. 통과 시 updates + updatedAtMs + **nextAction=next_action + dispatchState='pending'(creating 은 None 클리어) + nextDispatchAtMs + outboxSeq = 현재+1 + claim 필드 clear(claimState=None, claimOwner=None, claimLeaseExpiresAt=0; claimedOutboxSeq 는 유지=이전 seq — 새 outboxSeq 와 다름, 6차 B6-01)** 원자 기록 → **갱신된 job dict 반환(outboxSeq/generation/requestKey/claim clear 반영)**.
(3) `claim_visual_job_action(job_id, *, generation, action, outbox_seq, owner, lease_ms, now_ms) -> dict` — **B5-01 + 6차 B6-01 dict 4상태 claim**: 단일 transaction — job read 후 status 판정 + 갱신 snapshot 을 dict `{"status": ..., "job": {...}}` 로 반환(최소 job = {state, generation, outboxSeq, claimOwner, claimedOutboxSeq, claimLeaseExpiresAt, claimState}): (a) job.state ∈ ('done','failed') OR job.generation > generation OR job.outboxSeq > outbox_seq → **status 'completed'** (이미 다음 instance/terminal — 정상 ACK, 외부 0; job=현재 snapshot); (b) generation/nextAction/outboxSeq 가 인자와 불일치(위 조건 외) → **status 'stale'** (외부 0, 정상 ACK; job=현재 snapshot); (c) claimedOutboxSeq == outbox_seq AND claimOwner 존재 AND claimLeaseExpiresAt > now_ms → **status 'busy'** (same-seq active lease — 정상 ACK 금지; job=현재 snapshot, 남은 lease = claimLeaseExpiresAt - now_ms 계산 가능); (d) 그 외(미claim 또는 same-seq lease 만료) → claimedOutboxSeq=outbox_seq + claimOwner=owner + claimLeaseExpiresAt=now_ms+lease_ms + claimState='claimed' 원자 기록 → **status 'claimed'** (job=갱신 snapshot with new owner/lease — 호출자는 이 snapshot 만 action·CAS 에 사용). create 는 claim 미사용 — creating lease 특례 유지 (docstring). **claimState/claimOwner/claimLeaseExpiresAt 3필드로 4상태 판정 — claimedOutboxSeq 단독 판정 금지 박제. 반환 dict 의 job snapshot 이 owner handoff 의 유일 출처 (6차 B6-01).**
(4) `finalize_visual_job(job_id, *, terminal_state, failure_reason, job_meta, display_status, key=None, joint=None, cleanup_verified_at_ms=None, expect_states, expect_generation=None, expect_outbox_seq=None, expect_claim_owner=None, now_ms=None) -> str` — **B3-02 + H4-07 + 5차 B5-01 + 6차 B6-04 + 7차 B7-03/B7-04 owner/lease CAS + privacy validator(파라미터 병합)**: 단일 multi-document transaction — read: job(state ∈ expect_states + generation/outbox_seq 검증; **expect_claim_owner 지정 시 owner+seq+미만료 lease CAS(now_ms) — 불일치/만료 → 'stale' no-op**) + **uid/analysisId/kind 를 job 문서에서 파생 (caller 인자 제거 — cross-analysis tampering 차단)** + analysis read. **cleanup_verified_at_ms 파라미터를 candidate job 에 병합 후 validate(7차 B7-03 — job_meta 로만 전달하던 순서 모순 제거)**. **모순 조합 거부: terminal_state=='done' → display_status=='done' & key 필수 & failure_reason is None (ValueError otherwise); terminal_state=='failed' → display_status=='failed' & key is None & failure_reason ∈ VISUAL_FAILURE_REASONS (ValueError otherwise)**. **★correctedPose privacy gate (8차 B8-02 unconditional — inputSealed 값 무관): `if kind=='correctedPose':` done/failed 공통으로 candidate `inputSealed is True` AND candidate `cleanupVerifiedAtMs > 0` AND candidate `privacyBlocker is None` 셋 다 필수(하나라도 위반 ValueError). inputSealed==True 조건부 금지 — inputSealed=False 인 correctedPose 도 done/failed terminal 거부(초기 reserved job 직접 finalize 우회 차단)**. **★cleanup_blocked 회복 (8차 B8-03): `cleanup_verified_at_ms>0` 로 호출되면 finalizer 가 같은 transaction 에서 candidate 에 `privacyBlocker=None` + `cleanupVerifiedAtMs=cleanup_verified_at_ms` 를 원자 반영해 clear+terminal 을 한 transaction 으로 완성한다. caller 가 `job_meta` 로 privacyBlocker 를 지우는 것은 금지(validator 가 job_meta 내 privacyBlocker/cleanupVerifiedAtMs 키를 ValueError 로 거부 — dedicated parameter 로만). cleanup_verified_at_ms 미전달인데 기존 privacyBlocker 존재 → 거부(재확인 없는 clear 금지)**; display_status ∈ VISUAL_STATUSES; key 는 job 문서 파생 uid/analysisId 로 "results/{uid}/{analysis_id}/" prefix 강제 (위반 ValueError — H-02; canonical 은 기존 VideoBucket). write: job {state, failureReason, **cleanupVerifiedAtMs = cleanup_verified_at_ms(있으면) + privacyBlocker=None(clear)**, job_meta flat scalar 병합(privacyBlocker/cleanupVerifiedAtMs 키 제외), nextAction:None, dispatchState:None, updatedAtMs} + analysis field-path result.{kind}Status/{kind}Key(done)/{kind}Joint(correctedPose done)/{kind}UpdatedAtMs. **analysis 부재 → job 만 {state:'failed', failureReason:'orphaned_analysis'} + 'orphaned_analysis' 반환**. 분석 공용 top-level updatedAt 미갱신. 정상 'finalized'. docstring: canonical S3 put 은 호출 **전** immutable key (재실행 안전).
(4b) `read_visual_job(job_id) -> dict | None` — 단순 문서 get(side-effect 0). 31-10 producer preflight(B6-03) 와 janitor(H8-01)가 사용. 존재하면 전체 필드 dict, 부재 None. (7차 H7-03)
(4c) **`begin_visual_job_create(job_id, *, expect_generation, expect_outbox_seq, expect_next_action='create', now_ms, owner, lease_ms) -> dict` — 8차 B8-01 create 전용 CAS(9번째 함수, 단일 transaction)**: job read → `{"status": ..., "job": snapshot}` 반환. **(a) state ∈ ('reserved','retry_ready') AND generation==expect_generation AND nextAction==expect_next_action AND outboxSeq==expect_outbox_seq AND nextDispatchAtMs<=now_ms → creating 전이. ★H9-03 full write set(고정): state='creating', leaseOwner=owner, leaseExpiresAt=now+lease_ms, requestKey=f"{job_id}:gen{새_generation}"(retry_ready 는 generation+1, taskId=None 강제), **outboxSeq=현재+1, nextAction=None, dispatchState=None, nextDispatchAtMs=0, claim 필드(claimState/claimOwner/claimLeaseExpiresAt) clear** → status 'acquired'. acquired snapshot 의 새 outboxSeq 를 creating→polling CAS 에 사용하고, old create message 는 즉시 stale(호출자는 future lease 를 다시 busy 판정하지 않고 즉시 vendor create 1회)**; (b) state=='creating' AND leaseExpiresAt>now_ms AND leaseOwner!=owner → 'busy'(다른 유효 owner); (c) state=='creating' AND leaseExpiresAt<=now_ms AND taskId is None → 'unconfirmed'(수동 판정 — B2-02 자동 재생성 금지); (d) state=='creating' AND taskId 존재 → 'resume'(polling 재개); (e) generation/outboxSeq/nextAction 불일치 또는 state terminal/그 외 → 'stale'(외부 0 정상 ACK). **acquired 반환 snapshot 이 vendor create·creating→polling CAS 의 유일 입력 — `_advance(creating)` 2차 호출 없음(B8-01 자기모순 제거).**
(5) `mark_visual_job_dispatched(job_id, *, expect_generation=None, expect_action, expect_outbox_seq) -> bool` — **B4-01 CAS**: dispatchState=='pending' AND generation==expect_generation AND nextAction==expect_action AND outboxSeq==expect_outbox_seq → 'sent' (worker/dispatcher SQS send 성공 후 호출). 하나라도 불일치 → False (이전 sender 의 늦은 mark 는 seq/action 불일치로 다음 outbox 를 덮지 못함).
(6) `list_dispatch_pending(now_ms, *, limit=20, max_scan=1000) -> dict` — dispatcher 전용. **composite index 회피 + starvation 제거 (H4-08 옵션3 pagination) + 이중 durable cursor 순환 (5차 H5-06 + 6차 H6-01) + lease-expired 복구 (5차 B5-01 + 6차 H6-01):** (a) 단일 등가 쿼리 `where('dispatchState','==','pending').order_by('__name__')` 를 **durable pending cursor(visual_dispatch_cursor_doc_path 저장 위치)에서 이어서** max_scan 까지 cursor pagination — 끝 도달 시 처음으로 wrap + cursor 갱신 (등가+range 복합 쿼리 금지 — composite index 필수라 실 EventBridge FAILED_PRECONDITION; STATE.md phase-33 3f6681f 선례). 스캔 전량에서 nextDispatchAtMs<=now_ms 인 due 항목을 모아 nextDispatchAtMs 오름차순 정렬 → 상위 limit; (b) **별도 sent-recovery 스캔: `where('dispatchState','==','sent').order_by('__name__')` 를 독립 durable sent cursor(visual_dispatch_sent_cursor_doc_path)에서 이어서 순환, 그 중 `claimedOutboxSeq==outboxSeq AND claimState=='claimed' AND 0<claimLeaseExpiresAt<=now_ms AND state nonterminal` 만 복구 대상(H6-01 정확 필터 — 미claim sent(lease 0)·구 seq claim 제외, 정상 sent 재발행 0). 복구 항목은 반환 items 에 실어 dispatcher 가 재send + mark (dispatchState 를 pending 으로 되돌리지 않음 — B5-01 crash 복구 주체)**. first-N slice 고정 금지. 반환 = {"items": [{jobId, nextAction, generation, outboxSeq, state}], "scanned_outbox_max_age_ms": due 항목 중 실제 최고 경과(스캔 window 기준 — 전체 backlog 아님 명시, M6-01), "truncated": pending 스캔이 max_scan 에 도달했는가, "cursor_wrapped": bool}. docstring 에 "복합 인덱스 금지 이유 + pending/sent 독립 cursor 순환 + H6-01 정확 필터 + age 는 스캔 window 최고령(M6-01)" 박제.
(7) `update_analysis_visual(uid, analysis_id, kind, *, status, key=None, joint=None)` — terminal reconciler(31-09 운영 스크립트) 전용 — production worker 경로 호출 금지 (docstring + 31-09 acceptance grep). status ∈ VISUAL_STATUSES, kind 별 field-path, top-level updatedAt 미갱신, key prefix 강제, URL 파라미터 없음.
(8) **per-invocation upload reservation state machine (8차 B8-05/B8-06/H8-03 + 9차 B9-01/H9-01/H9-04/H9-06 — pipeline/dispatcher 는 이 helper 만 호출). path `visualInputReservations/{jobId}_{reservationId}`. 필드 {jobId, owner, sourceHash, expectedKeys[], createdKeys[], state ∈ VISUAL_RESERVATION_STATES('open'|'claimed_by_job'|'claimed_by_janitor'|'closed'), leaseExpiresAt, **claimOwner, claimLeaseExpiresAt, claimAttempt(9차 B9-01), expireAt: timestamp(10차 H10-02 — Firestore TTL 대상은 timestamp 타입 필수, millisecond number 아님)**}. **10차 H10-03: `VISUAL_INPUT_RESERVATION_TTL_MS`·`VISUAL_JANITOR_CLAIM_LEASE_MS`·`VISUAL_CLOCK_SKEW_MS` 를 models 에 concrete 숫자로 고정(PipelineFunction Timeout+최대 재시도/보상 구간+margin). 모든 만료 비교는 trusted service clock 하나(UTC epoch ms)만 사용 — Firestore server timestamp 는 문서 저장용, lease 계산은 caller trusted now_ms. 31-10 build gate `PipelineTimeout*1000 + 최대보상구간 + margin <= TTL`.**:
  - `create_input_reservation(job_id, reservation_id, *, owner, source_hash, expected_keys, now_ms) -> dict` — create-only precondition(부재 시만; 존재 시 no-op) (B8-05). state='open', leaseExpiresAt=now+TTL.
  - `record_reservation_keys(job_id, reservation_id, *, created_keys, owner) -> bool` — owner CAS 후 createdKeys 갱신.
  - **`_claim_reservation_for_job_tx(transaction, job_id, reservation_id, *, owner, now_ms) -> bool` (9차 H9-04 — nested transaction 금지: transaction object 를 인자로 받는 내부 함수). reserve_visual_job 과 public `claim_reservation_for_job` wrapper 가 이 하나를 공유해 read-all-before-write 로 job+reservation 을 단일 commit/commit-loss 단위로 처리.** 자기 reservation state=='open' AND owner 일치 AND leaseExpiresAt>now → 'claimed_by_job' CAS(True). 'claimed_by_janitor'/만료 → False(reserve 실패, 새 preflight 재시도) (B8-06).
  - `claim_reservation_for_janitor(job_id, reservation_id, *, owner, now_ms) -> bool` — **janitor: (H9-02 정확 predicate) 이 reservation 의 expectedKeys∪createdKeys 를 소유한 nonterminal unsealed job 이 없음(terminal/inputSealed/mismatched payload job 은 삭제 허용) AND (state=='open' AND leaseExpiresAt<=now) OR (state=='claimed_by_janitor' AND claimLeaseExpiresAt<=now, 9차 B9-01 claim crash 재claim) → claimOwner=janitor owner + claimLeaseExpiresAt=now+claim_lease + claimAttempt+1 로 CAS. 'claimed_by_job' 은 False(유효 job 입력 보존). 이 CAS 성공 후에만 S3 delete.**
  - `close_reservation(job_id, reservation_id, *, owner=None, now_ms=None)` — 'closed' + **expireAt=now+VISUAL_CLOSED_DOC_RETENTION(timestamp, H10-02 TTL policy 대상) + expectedKeys∪createdKeys 각 key `release_key_ownership(ref=reservationId)` 를 같은 transaction 원자 수행(B10-03)**. claimed_by_job 잔존 reconciliation: 31-09 운영 스크립트가 terminal job 과 대조 close.
(9) **durable orphan registry (H8-01/H8-03/M8-05 + 9차 B9-01/H9-07). path `visualOrphans/{hash(bucket,key)}`. 필드 {bucket, key, state ∈ VISUAL_ORPHAN_STATES('open'|'claimed'|'closed'), generation(H9-07 재발 incident), attempt, nextRetryAtMs, lastError, createdAtMs, claimOwner, claimLeaseExpiresAt, expireAt: timestamp(10차 H10-02)}.**
  - `upsert_visual_orphan(bucket, key, *, now_ms, reason)` — 보상 delete 실패 시 필수 기록. **closed 동일 key 재발 시 (H9-07): closed→open + generation+1 + attempt=0 + timestamps reset 을 CAS(같은 deterministic hash key 재-orphan 을 다시 처리).**
  - `claim_visual_orphan(orphan_id, *, owner, now_ms) -> dict|None` — **(state=='open' AND nextRetryAtMs<=now) OR (state=='claimed' AND claimLeaseExpiresAt<=now, 9차 B9-01 claim crash 재claim) → claimOwner=owner + claimLeaseExpiresAt=now+lease + attempt+1 CAS + 반환**.
  - `close_visual_orphan(orphan_id, *, now_ms)` — 삭제+HEAD404 후 'closed' + expireAt(timestamp H10-02) + `release_key_ownership(ref=orphanId)`(B10-03). `bump_visual_orphan(orphan_id, *, next_retry_at_ms, last_error)` — 실패 backoff.
(10) **key-level ownership = `active|deleting` delete-fence 상태 머신 (9차 B9-02 + 10차 B10-01/B10-02/B10-03/H10-01/H10-04 — 동일 deterministic S3 key 의 cross-reservation 삭제 경쟁·self-ref deadlock·delete fence·ref release 를 모두 닫음). path `visualInputObjects/{hash(bucket,key)}`. 필드 {bucket, key, **state ∈ ('active','deleting')**, **refs: bounded map `{refId: {kind:'reservation'|'job', generation, expireAt:timestamp}}`(10차 H10-04 — typed/bounded, string 배열 금지·max-ref invariant)**, **deleteOwner, deleteLeaseExpiresAt, generation(acquire/delete CAS 에 실제 사용 — H10-04)**, updatedAtMs}. transaction 으로만 변경, S3 delete 는 이 문서 CAS 밖에서 하되 반드시 delete claim 성공 후:**
  - `acquire_key_ownership(bucket, key, *, ref, kind, now_ms) -> bool` — producer 가 PUT/reuse **전** 호출. **state=='deleting' AND deleteLeaseExpiresAt>now → False(delete fence — B10-02: janitor 가 삭제 중인 key 를 새 producer 가 획득 불가). 그 외 → refs[ref] 원자 추가(+expireAt) + state='active' 유지, True. deleting+lease 만료면 회수 후 active 로 acquire.**
  - **`_promote_key_ownership_to_job_tx(transaction, bucket, key, *, reservation_ref, job_ref)` (10차 H10-01 — reservation claim 과 동형 내부 tx 함수, transaction 인자 명시). reserve transaction 안에서 reservation ref 를 job ref 로 원자 교체(refs 에서 reservation_ref 소비·job_ref 추가). 두 key 중 하나 conflict/retry/commit-loss 시 job·reservation·두 object doc 이 전부 이전 또는 전부 승격만 허용.**
  - **`release_key_ownership(bucket, key, *, ref)` — 상태 전이의 필수 부분(B10-03: 호출자 선택 아님). refs[ref] 제거. release 후 refs 비고 state!='deleting' 이면 object doc 즉시 삭제(H10-02 metadata retention 0). worker cleanup·producer 보상·reservation/orphan close 의 terminal transition 에 lockstep.**
  - **`claim_key_for_delete(bucket, key, *, deleting_ref, owner, lease_ms, now_ms) -> bool` (10차 B10-01/B10-02 핵심): 단일 transaction — (a) refs 에서 **deleting_ref 자신을 소비/제거**(B10-01 self-ref deadlock 해소) → (b) 남은 refs 중 미만료 live ref 가 0 이 아니면 False(B9-02: A-expired 가 K 공유 live B 입력 삭제 불가) → (c) 0 이면 state='deleting' + deleteOwner=owner + deleteLeaseExpiresAt=now+lease + generation+1 CAS, True. state 이미 'deleting'+lease 미만료+타 owner → False; deleting+lease 만료 → 새 owner 회수. delete claim 성공 후에만 janitor 가 외부 S3 delete; delete+HEAD404 후 `release_key_ownership`/object doc 제거로 close.**
  - **janitor(reservation·orphan 공용): expired reservation/orphan claim(B9-01 lease) → 각 expectedKeys∪createdKeys 에 `claim_key_for_delete(deleting_ref=reservationId/orphanId)` 성공한 key 만 S3 delete → HEAD404 → release/close. deleting claim 실패(live ref 존재)면 그 key skip(다른 live owner 가 정리). B10-02 barrier: janitor claim commit → producer acquire 는 deleting fence 로 실패 → delete close 후에만 acquire 성공.**
test_visual_jobs.py 실 테스트: **reserve — 신규 예약 1 transaction 에 job(reserved + create + pending + outboxSeq 1 + claim/privacy 초기값(inputSealed False, privacyBlocker None)) + analysis pending 동시; analysis 부재 → created False + job 0; 동일 키 2회 → created False + counter 1회; 한도 → daily_limit; failed+allow_retry True → generation 2 + outbox/claim/privacy 재기록; run_contended 동시 reserve 2건 → job 1 + counter +1**; **transition — 정상 전이 시 outboxSeq 증가 + snapshot 반환(outboxSeq/generation/requestKey) + nextAction/dispatchState/nextDispatchAtMs state 와 동일 transaction + claim 필드 clear(claimState/claimOwner None, claimLeaseExpiresAt 0, claimedOutboxSeq==이전 seq≠새 seq — 6차 B6-01); terminal state → ValueError; nonterminal next_action=None('creating' 외) → ValueError; polling taskId 부재 ValueError; creating lease 미비 ValueError; retry_ready→creating 에 taskId 있으면 ValueError·없으면 generation+1 + requestKey suffix==새 generation (H5-03); expect_generation 불일치 → None; expect_outbox_seq 불일치 → None + 무변경; expect_claim_owner 불일치(다른 owner 재claim 후) → None; now_ms >= claimLeaseExpiresAt(만료) 인데 owner 일치 → None (6차 H6-07 lease unexpired CAS)**; **claim dict 4상태 (B5-01 + 6차 B6-01) — 미claim 매칭 → {status:'claimed', job: owner/lease 갱신 snapshot}; same-seq + lease 유효 → {status:'busy', job: 무변경 snapshot}; same-seq + lease 만료 → {status:'claimed', 새 owner/lease}; nextAction/generation/outboxSeq 불일치 → {status:'stale'}; job outboxSeq 이미 초과/terminal → {status:'completed'}; clock 주입으로 만료 전/후 결정론 재현; 반환 job snapshot 의 claimOwner/outboxSeq/claimLeaseExpiresAt 가 handoff 값과 일치**; **mark_visual_job_dispatched — generation/action/outboxSeq 3중 일치만 sent; 이전 action 의 늦은 mark(action 또는 seq 불일치) → False + pending 유지 (§6 clobber 테스트)**; **finalize — job terminal + analysis 표시 한 transaction 동시 + nextAction/dispatchState 클리어; uid/analysisId/kind 를 job 문서에서 파생(caller 가 틀린 analysisId 주입해도 job 값 사용); expect_claim_owner 불일치/만료 lease → stale no-op(늦은 worker); done+display failed 또는 failed+key 또는 done+failure_reason not None → ValueError; **8차 B8-02 finalizer unconditional: correctedPose 의 (done|failed)×(inputSealed False)×(cleanupVerified 0) 네 조합 전부 ValueError; inputSealed True+cleanupVerified>0+privacyBlocker None 만 통과**; **8차 B8-03 회복: privacyBlocker='cleanup_blocked' + cleanup_verified_at_ms>0 호출 → 같은 transaction 에서 privacyBlocker None clear + terminal 성공; cleanup_verified_at_ms 미전달 + 기존 blocker → ValueError; job_meta 에 privacyBlocker/cleanupVerifiedAtMs 키 → ValueError**; **8차 B8-01 begin_visual_job_create: 최초 reserved(gen/seq/action/due 일치) → acquired + creating+lease+requestKey; acquired snapshot 이 future lease 여도 즉시 vendor create(no-op 아님); creating+유효 타 owner → busy; creating+만료+taskId None → unconfirmed; creating+taskId → resume; 불일치 → stale; concurrent same-seq 2건 → acquired 1·busy/stale 1·vendor create total 1(M8-04)**; **8차 B8-05/B8-06 reservation: create_input_reservation create-only; _claim_reservation_for_job_tx(open+owner+미만료→claimed_by_job, claimed_by_janitor→False); ★barrier: janitor 확인 → producer reserve → janitor claim 실패 → delete 0; reserve↔janitor 배타 CAS commit_lost 일치**; **★9차 B9-01 janitor claim crash 복구: claim_reservation_for_janitor CAS commit 직후 delete 전 SIGKILL → claimLeaseExpiresAt 전 재실행 0 → lease 후 새 owner 재claim(claimed_by_janitor+expired→재claim) → delete+HEAD404+close; orphan claim 직후 crash 동형**; **★10차 ownership 6 barrier(§7): (B10-01) A-only expired → claim_key_for_delete(deleting_ref=res_A) 가 자기 ref 소비 후 다른 live ref 0 → deleting CAS 성공 → delete → object doc 제거; (B9-02) A-expired + B-live → claim_key_for_delete(A) 가 B live ref 발견 → False(delete skip); (B10-02 fence) janitor claim_key_for_delete commit(state='deleting') → producer acquire_key_ownership 는 deleting fence 로 False → delete close 후에만 acquire True; (B10-01/B10-02 crash) deleting claim 직후·delete 성공 직후 crash → deleteLease 만료 후 재claim → 최종 S3 0/object doc 정리; (B10-03) worker cleanup 후 job ref 0+object doc 제거, producer 보상 성공 후 reservation ref 0; (H10-01) reserve+두 key promote+job = 단일 commit/commit-loss 전부-또는-전무**; **9차 H9-02 predicate 표: terminal/inputSealed/mismatched payload job → janitor delete 허용; matching nonterminal unsealed → 보존**; **9차 H9-04 nested tx: reserve+reservation claim+key promote 가 단일 commit; commit_lost → 재실행 일관**; **9차 H9-07 orphan reopen: closed 동일 key 재발 → upsert closed→open+generation+1+attempt 0**; **9차 H9-01 TTL: VISUAL_INPUT_RESERVATION_TTL_MS 존재 + PipelineTimeout 관계 상수 검사**; read_visual_job 존재/부재 반환; analysis 부재 → orphaned_analysis; commit_lost → 재호출 'stale' no-op + 두 문서 일치**; **list_dispatch_pending — pending durable cursor 이어스캔 + wrap + max_scan 초과 backlog 순환 drain(1,200 항목 여러 호출로 전량) + due 필터 + scanned_outbox_max_age_ms 는 스캔 window 최고령(M6-01) + sent-recovery: same-seq expired claim(H6-01) 만 복구·미claim sent(lease 0)·구 seq claim 재발행 0 + sent 독립 cursor 순환 + 단일 등가 쿼리(range 미발생) + truncated 플래그**; update_analysis_visual URL 파라미터 부재 + top-level updatedAt 미기록; payload nested ValueError.</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31/test_visual_jobs.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - firestore_admin.py 에 reserve_visual_job(reservation_id claim) / transition_visual_job / claim_visual_job_action / begin_visual_job_create(acquired/busy/resume/unconfirmed/stale) / finalize_visual_job / read_visual_job / mark_visual_job_dispatched / list_dispatch_pending / update_analysis_visual 9종 + reservation helpers(create_input_reservation/record_reservation_keys/_claim_reservation_for_job_tx+public wrapper/claim_reservation_for_janitor(lease+key-ref 0)/close_reservation) + orphan helpers(upsert(reopen)/claim(lease 재claim)/close/bump) + key-ownership helpers(acquire/promote/release/live_ref_count) 존재 (8차 B8-05/B8-06 + 9차 B9-01/B9-02/H9-04/H9-07)
    - transition 이 snapshot 반환 + outboxSeq 증가 + expect_outbox_seq/expect_claim_owner/now_ms lease CAS + 새 seq 시 claim clear + terminal 거부 + retry_ready→creating taskId None·generation+1·requestKey 새 generation (테스트 고정 — B4-01/B4-03/H5-03/B6-01/H6-07)
    - claim_visual_job_action 이 dict `{status, job}` 4상태(claimed/busy/stale/completed) + same-seq active=busy·expired=재claim + 반환 snapshot handoff + clock 주입 테스트 green (B5-01/B6-01)
    - mark_visual_job_dispatched 가 action+outboxSeq 3중 CAS — 늦은 mark clobber 방지 (B4-01)
    - finalize 가 correctedPose done|failed unconditional inputSealed True+cleanupVerified>0+privacyBlocker None 요구(inputSealed=False 우회 0 — B8-02) + cleanup_verified_at_ms>0 시 privacyBlocker 원자 clear(cleanup_blocked→terminal, B8-03) + job_meta privacyBlocker 키 거부 + owner/lease CAS + orphaned_analysis + commit_lost 일치 (B3-02/H4-07/B5-01/B6-04/7차 B7-03/B7-04/8차 B8-02/B8-03)
    - begin_visual_job_create 최초 reserved → acquired → vendor create 정확 1회; concurrent same-seq 둘 중 acquired 1·나머지 busy/stale; creating future lease no-op 없음(B8-01); reservation create-only(overwrite 0, B8-05) + claim_for_job/claim_for_janitor CAS 배타 + producer reserve↔janitor delete 선형화(B8-06)
    - read_visual_job(단순 get, side-effect 0) 8번째 함수 존재 — preflight/janitor 계약 (7차 H7-03)
    - list_dispatch_pending 이 pending/sent 이중 durable cursor 순환 + 1,200 backlog 전량 drain + H6-01 same-seq expired claim 만 복구 + scanned_outbox_max_age_ms + 단일 등가 쿼리 (H4-08 + H5-06 + B5-01 + H6-01 + M6-01)
    - `python -m pytest backend/tests/phase31/test_visual_jobs.py -x -q` exits 0
  </acceptance_criteria>
  <done>quota·멱등·outbox instance CAS·claim dict 4상태·owner/lease CAS·원자 finalize(privacy validator)·durable postprocessing·이중-cursor dispatcher 규율이 함수 시그니처와 테스트로 강제됨.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 요청/워커 → visualJobs 문서 | 상태 전이의 유일한 진실 — transaction CAS+validator+outboxSeq+claim owner/lease 로만 변경 |
| SQS 메시지 → worker 외부 side-effect | claim_visual_job_action 'claimed' 상태만 — busy/stale/completed 는 외부 0 |
| job terminal → analysis 표시 | finalize_visual_job 단일 transaction 로만 — job 문서 파생 인자 + owner/lease CAS + privacy validator |
| 표시 필드 → 앱 | canonical key 만 — URL 은 인증 재서명 경로로만 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-05 | Denial of Service (과금) | quota 예약 | mitigate | transaction 원자 예약 — 이중 소비·고아 quota 불가 (run_contended 테스트, H-06) |
| T-31-06 | Tampering | stale/구세대/구-outbox SQS 메시지의 되감기 | mitigate | transition CAS + generation + expect_outbox_seq + expect_claim_owner + taskId/lease validator (B-03 + B2-02 + B3-01 + B4-01 + B5-01) |
| T-31-07 | Information Disclosure | Firestore 에 만료 URL 저장 | mitigate | finalize/update 에 URL 파라미터 부재 + key prefix ValueError + taskId 만 (H-02 + H3-01) |
| T-31-08 | Elevation of Privilege | visualJobs 노출 / cross-analysis finalize | mitigate | 백엔드 전용 컬렉션 + finalize 의 uid/analysisId 를 job 문서 파생 (H4-07) |
| T-31-52 | Denial of Service (과금) | creating crash 후 무조건 재생성 | mitigate | lease 만료 + taskId 부재 = 'create_unconfirmed' 수동 판정 (B2-02/M3-07) |
| T-31-56 | Denial of Service | CAS 후 send 유실 / 늦은 mark clobber 로 continuation 유실 | mitigate | 모든 전이 outboxSeq 원자 기록 + mark action+seq CAS + dispatcher pending 복구 (B3-01 + B4-01) |
| T-31-57 | Tampering | terminal job / analysis 영구 불일치 | mitigate | finalize_visual_job multi-document transaction + orphaned_analysis + reconciler (B3-02) |
| T-31-62 | Denial of Service (과금) | standard SQS 중복 전달로 유료 action 중복 실행 | mitigate | claim 4상태 — busy 는 정상 ACK 금지, claimed 만 외부 호출 (B5-01) |
| T-31-63 | Tampering | finalize 직후 crash / 실패 경로 우회로 pair/cleanup 유실 | mitigate | postprocessing durable state — 성공+실패 전부 경유, 단일 delete cleanup 후 finalize (H4-01 + H5-05) |
| T-31-69 | Denial of Service | claim 직후 crash 로 job 영구 pending | mitigate | dispatcher 가 dispatchState='sent'+H6-01 필터(same-seq expired claim) 재발행 + busy→batchItemFailures (B5-01 + H6-01) |
| T-31-70 | Tampering | lease 잃은/만료된 늦은 worker 의 결과 write | mitigate | transition/finalize expect_claim_owner+seq+미만료 lease CAS (B5-01 + H6-07) |
| T-31-74 | Information Disclosure | cleanup 미완인데 terminal done 으로 PII 잔존 | mitigate | finalize validator cleanupVerifiedAtMs/inputSealed 요구 + privacyBlocker terminal 금지 (B6-04) |
| T-31-75 | Tampering | claim owner 가 실행 snapshot 으로 이어지지 않아 정상 전이 실패 | mitigate | claim dict `{status, job}` 반환 + 호출자 snapshot-only 사용 + 새 seq claim clear (B6-01) |
| T-31-85 | Denial of Service | 정상 create 가 creating no-op 로 멈춤 | mitigate | begin_visual_job_create acquired→즉시 vendor create 1회, 2차 _advance 없음 (8차 B8-01) |
| T-31-86 | Information Disclosure | inputSealed=False correctedPose 가 cleanup 우회 terminal | mitigate | finalizer unconditional kind gate — inputSealed/cleanupVerified/privacyBlocker 3필수 (8차 B8-02) |
| T-31-87 | Repudiation | cleanup_blocked 영구 비-terminal(회복 불능) | mitigate | finalizer cleanup_verified>0 시 privacyBlocker 원자 clear+terminal 한 transaction (8차 B8-03) |
| T-31-88 | Information Disclosure | concurrent producer reservation overwrite / janitor 유효 입력 삭제 | mitigate | per-invocation immutable reservation create-only + claim_for_job↔claim_for_janitor 배타 CAS (8차 B8-05/B8-06) |
| T-31-89 | Denial of Service | self-ref 로 janitor delete 영구 정지(cleanup liveness 붕괴) | mitigate | claim_key_for_delete 가 자기 ref 소비 후 다른 live ref 0 판정 (10차 B10-01) |
| T-31-90 | Information Disclosure | count 확인 후 delete 전 producer acquire → live input 삭제 | mitigate | object active|deleting delete-fence lease — deleting 획득 후에만 외부 delete, acquire 는 fence 로 실패 (10차 B10-02) |
| T-31-91 | Denial of Service | 종료 경로 ref 미release → ownership doc 영구 잔존, 후속 key cleanup skip | mitigate | release 를 worker cleanup·producer 보상·close 의 원자 상태전이로 강제 + object doc 제거 (10차 B10-03) |
| T-31-92 | Repudiation | closed 문서 metadata 무한 retention | mitigate | expireAt timestamp + collection group Firestore TTL policy (10차 H10-02) |
</threat_model>

<verification>
- `python -m pytest backend/tests -q` green (전체 무회귀)
- firestore_admin 신규 함수가 _validate_dict_only_scalars 관례 준수 (nested 거부)
- 워커/요청 production 경로에서 update_analysis_visual 직접 호출 금지는 31-09/31-10 acceptance grep 으로 이어받음
</verification>

<success_criteria>
- 단일 visual job 계약(10상태·retry_ready·postprocessing·lease·outbox instance(outboxSeq)·claim dict 4상태·owner/lease CAS·새 seq claim clear·generation·원자 finalize(privacy validator)·멱등키·CAS validator·typed failure 10종·privacy blocker·전용 timestamp·canonical key·이중-cursor dispatcher)이 테스트로 고정
- 6차 리뷰 §7 실행 허용 조건 중 claim owner handoff·owner/lease CAS·crash 복구 주체(H6-01)·cleanup_blocked 비-terminal(B6-04)·fresh requestKey·cursor 순환의 계약 지점
- 8차 B8-01~06 + 9차 B9-01/B9-02/H9-01/H9-03/H9-04/H9-06/H9-07 + **10차 B10-01(self-ref 소비)/B10-02(active|deleting delete fence)/B10-03(전 종료경로 ref release)/H10-01(promote 내부tx)/H10-03(concrete TTL+trusted clock)/H10-04(typed/bounded refs)** 계약·테스트 고정
- 9개 테스트 파일 스캐폴드 + 경쟁 transaction/시계 mock 완성 + ownership 6 barrier(A-only/A+B/fence/crash/release/nested tx)
</success_criteria>

<output>
Create `.planning/phases/31-api-visual-correction/31-02-SUMMARY.md` when done
</output>
