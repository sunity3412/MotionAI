---
phase: 31-api-visual-correction
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/31-api-visual-correction/smoke/image_smoke.py
  - .planning/phases/31-api-visual-correction/smoke/RESULTS.json
autonomous: true
requirements: [D-02, D-03]
must_haves:
  truths:
    - "교정 실루엣용 이미지 편집 모델이 실호출 1건 이상으로 실측 검증된다 (D-03 — RESEARCH A1~A3 해소)"
    - "선정 모델 ID·endpoint·파라미터·비용·지연이 RESULTS.json 에 기록되어 31-04 어댑터가 미실측 가정 없이 구현된다"
    - "D-02 Wan2.7 계열 정합이 우선 검토된다 (wan2.7-image-pro 1순위, qwen-image-edit-plus 폴백)"
  artifacts:
    - path: ".planning/phases/31-api-visual-correction/smoke/RESULTS.json"
      provides: "chosen_model + endpoint + params + usage 실측"
      contains: "chosen_model"
    - path: ".planning/phases/31-api-visual-correction/smoke/image_smoke.py"
      provides: "재실행 가능한 스모크 스크립트 (stdlib urllib)"
  key_links:
    - from: ".planning/phases/31-api-visual-correction/smoke/image_smoke.py"
      to: "SSM /sunity/motion/dashscope-api-key"
      via: "aws ssm get-parameter --profile sunity-motion"
      pattern: "dashscope-api-key"
---

<objective>
D-03 의 Claude 재량 항목인 "이미지 생성 모델 선정" 을 실호출 스모크로 확정한다. RESEARCH 가 미실측([ASSUMED] A1~A3)으로 남긴 `wan2.7-image-pro` 의 이미지 편집 모드 지원 여부·파라미터·비용·지연·모더레이션 특성을 각 1건(+재시도 1건 상한) 호출로 실측하고, 실패 시 폴백 `qwen-image-edit-plus`(동기 API)로 판정한다. 결과는 RESULTS.json 으로 박제되어 31-04 의 WanImageAdapter 구현 기준이 된다 (D-02: Wan 계열 정합 우선, 엔진 스왑 가능 추상화 유지).

Purpose: 미실측 파라미터를 계약·어댑터에 굳히기 전 검증 (RESEARCH 권고 "plan 첫 태스크 스모크").
Output: smoke/image_smoke.py + smoke/RESULTS.json + 출력 이미지 파일.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-api-visual-correction/31-RESEARCH.md
@.planning/spikes/004-gemini-omni-view-editing/README.md
@.planning/spikes/004-gemini-omni-view-editing/wan_gate_batch.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 이미지 편집 스모크 스크립트 작성 + 실호출 (wan2.7-image-pro → qwen-image-edit-plus)</name>
  <files>.planning/phases/31-api-visual-correction/smoke/image_smoke.py</files>
  <read_first>.planning/spikes/004-gemini-omni-view-editing/wan_gate_batch.py (http_json 헬퍼 lines 24-56 + 인증 헤더 + 비동기 task 패턴 — 그대로 차용), .planning/spikes/004-gemini-omni-view-editing/README.md (spike 008 절 — 모더레이션 판정 응답 형상, 입력 프레임 소재 위치)</read_first>
  <action>stdlib urllib 만 사용하는 단발 스크립트 작성 (requests 금지 — 프로젝트 관례). 키 로드 = `aws ssm get-parameter --name /sunity/motion/dashscope-api-key --with-decryption --profile sunity-motion --query Parameter.Value --output text` (subprocess 또는 사전 셸 실행 후 env `DASHSCOPE_API_KEY` — 하드코딩·stdout 로깅 금지). 입력 이미지 = spike 004 디렉터리의 정은지/폴 동작 원본 프레임 1장 (README 에서 입력 소재 경로 확인; 없으면 S3 `fixtures/` 에서 1장 다운로드 — AWS_PROFILE=sunity-motion). 입력 이미지는 S3 임시 key 에 put 후 presign 1h URL 로 전달. 호출 전 Alibaba Model Studio 문서를 WebFetch 로 확인해 정확한 endpoint 를 결정: (1) `wan2.7-image-pro` — dashscope-intl 이미지 생성/편집 endpoint, 이미지+지시 편집 모드 (https://www.alibabacloud.com/help/en/model-studio/models), (2) `qwen-image-edit-plus` — 동기 API (https://www.alibabacloud.com/help/en/model-studio/qwen-image-edit-guide, 입력 1~3장, 출력 URL 24h, watermark 기본 off). 프롬프트 예: "Correct the athlete's left knee to full extension (about 175 degrees). Keep the person's identity, clothing, the pole, and the background unchanged." 모델당 최대 2회(1 + 모더레이션/오류 재시도 1) 호출 상한 — 총 4회 초과 금지 (비용 가드). 각 호출의 HTTP status·latency 초·usage/과금 필드·모더레이션 여부·출력 URL 을 기록하고 출력 이미지는 24h 만료 전 즉시 smoke/ 에 다운로드 저장.</action>
  <verify>
    <automated>test -f .planning/phases/31-api-visual-correction/smoke/image_smoke.py && python -c "import ast; ast.parse(open('.planning/phases/31-api-visual-correction/smoke/image_smoke.py').read())"</automated>
  </verify>
  <acceptance_criteria>
    - image_smoke.py 가 존재하고 파싱 가능하며 `dashscope-api-key` SSM 이름을 참조 (하드코딩 키 없음 — grep 으로 `sk-` 류 리터럴 0)
    - 최소 1개 모델에 대해 실호출이 수행되고 HTTP status 가 기록됨
    - 출력 이미지가 생성된 경우 smoke/ 하위에 저장됨
  </acceptance_criteria>
  <done>두 후보 모델의 실측 데이터(또는 실패 증거)가 확보됨.</done>
</task>

<task type="auto">
  <name>Task 2: 판정 + RESULTS.json 박제 (31-04 어댑터 구현 기준)</name>
  <files>.planning/phases/31-api-visual-correction/smoke/RESULTS.json</files>
  <read_first>.planning/phases/31-api-visual-correction/31-RESEARCH.md (§Standard Stack 모델 선정 권고 + Assumptions A1~A3 — 판정 기준), Task 1 이 저장한 출력 이미지 (Read 툴로 시각 확인)</read_first>
  <action>출력 이미지를 Read 툴로 직접 보고 판정: (a) 인물 신원·복장·폴·배경 보존 여부, (b) 지시된 자세 교정 반영 여부, (c) 명백한 왜곡(얼굴 붕괴 등) 여부. 선정 규칙: `wan2.7-image-pro` 가 이미지 편집 입력을 받고 (a)(b) 를 충족하면 1순위 채택 (D-02 Wan 계열 정합); 실패/미지원이면 `qwen-image-edit-plus` 채택; 둘 다 실패면 chosen_model 에 qwen 폴백을 기록하되 `blocked: true` + 사유를 남겨 31-04 가 mock-only 로 어댑터를 구현하고 실검증을 31-09 배포 후로 이월하게 한다. RESULTS.json 스키마(31-04 가 읽는 계약): `{"chosen_model": str, "endpoint": str, "request_params": dict, "sync": bool, "latency_s": number, "usage": dict, "moderation_blocked": bool, "blocked": bool, "notes": str, "measured_at": "2026-MM-DD"}`. 비용 실측치(usage)로 RESEARCH A2 의 "수백원 내외" 가정을 확정/기각 기록.</action>
  <verify>
    <automated>python -c "import json; d=json.load(open('.planning/phases/31-api-visual-correction/smoke/RESULTS.json')); assert 'chosen_model' in d and 'endpoint' in d and 'request_params' in d"</automated>
  </verify>
  <acceptance_criteria>
    - RESULTS.json 이 chosen_model/endpoint/request_params/sync/latency_s/usage 키를 포함
    - chosen_model 값이 `wan2.7-image-pro` 또는 `qwen-image-edit-plus` 중 하나
    - notes 에 신원 보존·교정 반영·왜곡 판정 결과가 서술됨
  </acceptance_criteria>
  <done>31-04 가 미실측 가정 0 으로 WanImageAdapter 를 구현할 수 있는 실측 계약 확보.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로컬 → DashScope | 사용자 유사 프레임이 외부 벤더로 전송 (D-01/D-05 결정에 내재 — fixture 프레임만 사용) |
| SSM → 로컬 셸 | SecureString 키의 로컬 취급 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-04 | Information Disclosure | image_smoke.py 키 취급 | mitigate | 키는 env/SSM 경유만, 스크립트·RESULTS.json·로그에 키 리터럴 금지 (acceptance 에 grep 검증) |
| T-31-05 | Information Disclosure | 입력 프레임 presign | mitigate | presign TTL 1h 한정, 스모크 전용 S3 key prefix 사용 후 정리 |
| T-31-06 | Denial of Service (과금) | 실호출 반복 | mitigate | 모델당 2회·총 4회 호출 상한을 스크립트 상수로 고정 |
</threat_model>

<verification>
- RESULTS.json 스키마 assert 통과
- 스모크 스크립트에 API 키 리터럴 없음
</verification>

<success_criteria>
- D-03 이미지 모델이 실측 기반으로 확정 (또는 blocked 사유 박제 + 폴백 지정)
- 31-04 입력 계약(RESULTS.json) 완성
</success_criteria>

<output>
Create `.planning/phases/31-api-visual-correction/31-02-SUMMARY.md` when done
</output>
