---
phase: 31-api-visual-correction
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/phase31/conftest.py
  - backend/tests/phase31/test_visual_jobs.py
  - backend/tests/phase31/test_visual_gen.py
  - backend/tests/phase31/test_pose_gate.py
  - backend/tests/phase31/test_pair_store.py
  - backend/tests/phase31/test_visual_worker.py
  - backend/tests/phase31/test_visual_request.py
  - backend/tests/phase31/test_visual_url.py
  - backend/tests/phase31/test_visual_dispatch.py
  - backend/shared/python/sunity_shared/models.py
  - backend/shared/python/sunity_shared/firestore_admin.py
autonomous: true
requirements: [D-05, D-06, D-07, D-08]
must_haves:
  truths:
    - "실루엣(correctedPose)·회전(rotation)이 공유하는 단일 durable visual job 계약이 존재한다 — kind='correctedPose'|'rotation' (리뷰 H-01)"
    - "job 상태가 외부 side-effect 단위로 분해된다 — reserved → creating(lease/requestKey) → polling(taskId 필수) → fetching → judging → pose_checking → done|failed (2차 리뷰 B2-02)"
    - "polling 상태는 taskId 없이 성립할 수 없다 — CAS validator 가 taskId 부재 전이를 ValueError 로 거부 (2차 리뷰 B2-02)"
    - "quota 예약과 job 생성이 하나의 Firestore transaction 으로 원자적이며, 같은 transaction 이 outbox(dispatchState='pending')를 함께 기록한다 — SQS 발행 실패가 복구 가능한 상태로 남는다 (리뷰 H-06 + 2차 리뷰 H2-02)"
    - "job 상태 전이는 job 문서 기준 CAS(compare-and-set) — stale 메시지는 no-op (리뷰 B-03 멱등 기반)"
    - "표시 필드 갱신은 전용 timestamp(correctedPoseUpdatedAtMs/rotationUpdatedAtMs)만 쓰고 분석 공용 updatedAt 을 건드리지 않는다 (리뷰 H-06)"
    - "Firestore 에는 canonical S3 key 만 저장 — presigned URL 필드 자체가 계약에 없다 (리뷰 H-02)"
  artifacts:
    - path: "backend/shared/python/sunity_shared/models.py"
      provides: "VISUAL_STATUSES / VISUAL_JOB_KINDS / VISUAL_JOB_STATES(9종) / VISUAL_FAILURE_REASONS(9종) + visual_job_doc_path"
      contains: "VISUAL_JOB_STATES"
    - path: "backend/shared/python/sunity_shared/firestore_admin.py"
      provides: "reserve_visual_job (transaction+outbox) / transition_visual_job (CAS+validator) / mark_visual_job_dispatched / mark_visual_job_dispatch_failed / update_analysis_visual"
      contains: "def reserve_visual_job"
    - path: "backend/tests/phase31/conftest.py"
      provides: "sys.path 주입 + _FakeDoc/_FakeSnapshot/_FakeTransaction + DashScope urllib mock fixture"
  key_links:
    - from: "backend/shared/python/sunity_shared/firestore_admin.py"
      to: "backend/shared/python/sunity_shared/models.py"
      via: "상수 검증"
      pattern: "models\\.VISUAL_"
---

<objective>
리뷰가 요구한 "single durable visual-job system" 의 데이터 기반을 만든다 (H-01/H-06/B-03 + 2차 리뷰 B2-02/H2-02). visualJobs 백엔드 전용 컬렉션(문서 id = "{uid}_{analysisId}_{kind}" — 자연 멱등키), **외부 side-effect 단위 상태 기계** (reserved → creating → polling → fetching → judging → pose_checking → done|failed), quota+job+outbox 원자 예약 transaction, taskId 필수 CAS validator, 전용 timestamp 표시 필드 갱신 함수를 신설하고, phase31 테스트 스캐폴드(DashScope mock 포함)를 깐다.

2차 리뷰 B2-02 핵심 반영: "메시지를 다시 보낼 수 있음" 은 "유료 작업이 멱등" 이 아니다. vendor create 전후 crash 를 구분 가능하게 하려면 create 라는 외부 side-effect 자체가 durable state(`creating` + leaseOwner/leaseExpiresAt/requestKey)를 가져야 하고, DashScope 는 idempotency token 을 지원하지 않으므로 lease 만료 후 무조건 재생성은 금지 — 'create_unconfirmed' typed 실패로 수동 reconciliation 대상이 된다 (중복 과금 가능성은 운영 계약으로 명시, 31-09 스크립트가 목록화).

계약 필드명 (31-04 TS 측과 lockstep — 리뷰 L-02 로 "silhouette" 대신 correctedPose 채택):
- result.correctedPoseStatus / correctedPoseKey / correctedPoseJoint / correctedPoseUpdatedAtMs
- result.rotationStatus / rotationVideoKey / rotationUpdatedAtMs
- URL 필드 없음 (재서명은 playback-url asset 확장 — 31-10)

Purpose: 모든 구현 wave 가 공유하는 상태·멱등·quota·outbox 기반 — 2차 리뷰 §7 Step 2 의 선행 계약.
Output: 스캐폴드 9파일 + models.py 상수 + firestore_admin 함수 5종 + 테스트.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-api-visual-correction/31-CONTEXT.md
@.planning/phases/31-api-visual-correction/31-PATTERNS.md
@.planning/phases/31-api-visual-correction/31-PLAN-REVIEW-ITERATION2.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: backend/tests/phase31/ 스캐폴드 + mock fixture</name>
  <files>backend/tests/phase31/conftest.py, backend/tests/phase31/test_visual_jobs.py, backend/tests/phase31/test_visual_gen.py, backend/tests/phase31/test_pose_gate.py, backend/tests/phase31/test_pair_store.py, backend/tests/phase31/test_visual_worker.py, backend/tests/phase31/test_visual_request.py, backend/tests/phase31/test_visual_url.py, backend/tests/phase31/test_visual_dispatch.py</files>
  <read_first>backend/tests/phase22/conftest.py (sys.path 주입 패턴 19줄 전체), backend/tests/phase22/test_shadow_wiring.py lines 18-57 (_FakeSnapshot/_FakeDoc registry stub + "LOCAL ONLY" docstring 관례), .planning/spikes/004-gemini-omni-view-editing/wan_gate_batch.py lines 24-56 (mock 대상 urllib 형상)</read_first>
  <action>phase22 선례로 conftest.py 작성: sys.path 주입(_BACKEND.parents[2] + shared/python) + _FakeDoc/_FakeSnapshot registry stub + 신규 2종: (1) `_FakeTransaction` — firebase-admin `@firestore.transactional` 데코레이터 경로를 로컬에서 실행 가능하게 하는 stub (transaction 함수가 실제로 read→write 순서로 호출되는지 기록, 동시성 시나리오는 순차 2회 호출로 모사), (2) `dashscope_urlopen_mock` — urllib.request.urlopen monkeypatch 팩토리로 create 응답({"output":{"task_id":...}})·폴링 응답(task_status SUCCEEDED/FAILED + video_url)·모더레이션 차단 응답·redirect(HTTP 302)·oversized body 시나리오 주입 가능. 테스트 8파일은 골격: 모듈 docstring("실 Firestore/네트워크/Pod 미접촉 — LOCAL ONLY" + 채우는 플랜 명시: test_visual_jobs=31-02, test_visual_gen=31-05, test_pose_gate=31-06, test_pair_store=31-07, test_visual_worker=31-09, test_visual_request/test_visual_url/test_visual_dispatch=31-10) + 미존재 모듈 pytest.importorskip 가드 + fixture 동작 확인 placeholder 1개씩 (suite green 유지). 이모지 금지, 한국어 why-주석.</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31 -x -q</automated>
  </verify>
  <acceptance_criteria>
    - conftest.py 에 `dashscope_urlopen_mock` 과 `_FakeTransaction` 존재
    - 8개 테스트 파일에 담당 플랜 번호 docstring 존재
    - `python -m pytest backend/tests/phase31 -x -q` exits 0 (skip 허용, fail 0)
    - `python -m pytest backend/tests -q` exits 0 (기존 suite 무회귀)
  </acceptance_criteria>
  <done>후속 전 플랜이 재사용하는 mock/transaction 스캐폴드 완성.</done>
</task>

<task type="auto">
  <name>Task 2: models.py — side-effect 단위 상태·typed 실패 상수 + 경로</name>
  <files>backend/shared/python/sunity_shared/models.py</files>
  <read_first>backend/shared/python/sunity_shared/models.py lines 399-408 (FAULT_ZOOM_STATUS_* 선례 + lockstep 주석 스타일 + analysis_doc_path 경로 함수 관례)</read_first>
  <action>FAULT_ZOOM 선례 동형으로 신설: `VISUAL_STATUS_PENDING/DONE/FAILED` + tuple `VISUAL_STATUSES` ('pending','done','failed') — correctedPose/rotation 표시 상태 공용; `VISUAL_JOB_KINDS = ("correctedPose", "rotation")`; **`VISUAL_JOB_STATES = ("reserved", "creating", "polling", "fetching", "judging", "pose_checking", "done", "failed", "dispatch_failed")` — 외부 side-effect 당 상태 1개 (2차 리뷰 B2-02): creating=vendor create 진행(leaseOwner/leaseExpiresAt/requestKey 필수), polling=taskId 확보 후 폴링, fetching=결과 다운로드/S3 전송, judging=Gemini before/after 판정, pose_checking=결정론 pose 게이트 (rotation 은 fetching 에서 종결 — judging/pose_checking 미사용)**; `VISUAL_FAILURE_REASONS = ("moderation", "timeout", "vendor_error", "invalid_output", "judge_failed", "pose_gate_failed", "pose_gate_unavailable", "dispatch_failed", "create_unconfirmed")` 9종 — **'create_unconfirmed' 신설 (B2-02): creating lease 만료 + taskId 부재 = vendor 성공 여부 판별 불가 → 자동 재생성 금지, 수동 reconciliation 대상 (DashScope idempotency token 미지원 — 중복 과금 방지 운영 계약)**; `visual_job_id(uid, analysis_id, kind) -> str` = f"{uid}_{analysis_id}_{kind}" (멱등키 — 리뷰 H-06) + `visual_job_doc_path(job_id) -> str` = f"visualJobs/{job_id}" (백엔드 전용 컬렉션 — Firestore rules 미노출). 3면 lockstep 주석: analysis.ts 의 correctedPoseStatus/rotationStatus + contract.md 신규 절 + D-05/D-06/D-08 근거 인용. 표시 상태('pending'|'done'|'failed', 부재=legacy 숨김)와 job 상태(9값)는 별개 개념임을 주석으로 구분. outbox 필드 `dispatchState` ('pending'|'sent') 는 state 와 직교하는 발행 여부 필드임을 주석 명시 (2차 리뷰 H2-02).</action>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0,'backend/shared/python'); from sunity_shared import models; assert models.VISUAL_STATUSES==('pending','done','failed'); assert 'creating' in models.VISUAL_JOB_STATES and 'fetching' in models.VISUAL_JOB_STATES; assert 'create_unconfirmed' in models.VISUAL_FAILURE_REASONS; assert models.visual_job_id('u','a','rotation')=='u_a_rotation'"</automated>
  </verify>
  <acceptance_criteria>
    - VISUAL_JOB_STATES 9종 (creating/fetching/judging/pose_checking 포함), VISUAL_JOB_KINDS == ('correctedPose','rotation')
    - VISUAL_FAILURE_REASONS 9종 — create_unconfirmed 포함
    - visual_job_id / visual_job_doc_path 함수 존재
  </acceptance_criteria>
  <done>side-effect 단위 상태·멱등키 계약이 상수로 고정됨.</done>
</task>

<task type="auto">
  <name>Task 3: firestore_admin — 원자 예약+outbox transaction + CAS validator 전이 + 표시 필드 갱신</name>
  <files>backend/shared/python/sunity_shared/firestore_admin.py, backend/tests/phase31/test_visual_jobs.py</files>
  <read_first>backend/shared/python/sunity_shared/firestore_admin.py lines 1138-1187 (update_analysis_fault_zoom — field-path update/검증 선례) + lines 104-128 (_validate_dict_only_scalars — 본체 무수정 재사용) + _doc 헬퍼, backend/tests/phase31/conftest.py (_FakeTransaction)</read_first>
  <action>함수 5종 신설 (전부 모듈 docstring 에 리뷰 H-02/H-06/B-03 + 2차 리뷰 B2-02/B2-03/H2-02 근거 인용):
(1) `reserve_visual_job(uid, analysis_id, kind, *, date_key=None, user_limit=None, global_limit=None, payload: dict | None = None, allow_retry_failed: bool = False) -> dict` — firebase-admin `@firestore.transactional` 로: job 문서(models.visual_job_doc_path) read → 이미 존재하면 **{"created": False, "job": {...전체 필드...}} 반환 — 호출측(31-10)이 state/dispatchState/sourceHash 로 분기하는 유일한 근거 (2차 리뷰 B2-03: reserve 반환이 replay 분기의 단일 기준)**. 단 기존 state 가 'failed' 이고 allow_retry_failed=True 인 경우만 재예약 허용 (state 'reserved' 리셋 + `generation` int 필드 +1 + payload 재기록 + rotation 은 quota 재소비) — allow_retry_failed=False(자동 correctedPose 경로)면 failed 도 created False no-op (모더레이션 무한 재시도 차단). kind=='rotation' 이고 limit 인자 제공 시 `visualQuota/{uid}_{date_key}` 와 `visualQuota/global_{date_key}` 두 counter read → 둘 중 하나라도 한도 이상이면 {"created": False, "reason": "daily_limit"} (증가 0); 통과 시 두 counter +1 + job 문서 생성 {uid, analysisId, kind, state:'reserved', **dispatchState:'pending' (outbox — 같은 transaction 원자 기록, 2차 리뷰 H2-02)**, generation:1, attempt:0, taskId:None, failureReason:None, requestedAtMs, updatedAtMs, nextDispatchAtMs:None, quotaDateKey, **payload 병합**} → {"created": True, "job": ...}. payload = flat scalar dict 만 (`_validate_dict_only_scalars` 재사용, nested/비스칼라 ValueError, 예약어 키 충돌 ValueError; 용도 = correctedPose 의 CorrectedPoseTarget 스칼라 — srcKey/sourceHash/jointKey/targetDeg/userFrameIdx/refFrameIdx/confidence/provenanceVersion, 31-10 T3 기록 → 31-09 소비). 읽기 전부 → 쓰기 전부 순서 (Firestore transaction 규칙).
(2) `transition_visual_job(job_id, *, expect_states: tuple, updates: dict) -> bool` — transaction 으로 현재 state 가 expect_states 에 없으면 False (stale SQS 메시지 no-op — 리뷰 B-03 멱등), 있으면 updates + updatedAtMs 갱신 후 True. **CAS validator (2차 리뷰 B2-02): updates['state']=='polling' 인데 updates 또는 기존 문서에 비어있지 않은 taskId 가 없으면 ValueError ("polling 은 taskId 없이 성립 불가"); updates['state']=='creating' 인데 leaseOwner/leaseExpiresAt/requestKey 셋 중 하나라도 updates 에 없으면 ValueError.**
(3) `mark_visual_job_dispatched(job_id) -> bool` — dispatchState 'pending' → 'sent' CAS (outbox 발행 확인 — dispatcher/sweeper 가 SQS send 성공 후 호출, 2차 리뷰 H2-02).
(4) `mark_visual_job_dispatch_failed(job_id, *, next_dispatch_at_ms: int | None = None)` — state 'dispatch_failed' + failureReason 'dispatch_failed' + nextDispatchAtMs CAS 기록 (sweeper 재발행 기준 — 2차 리뷰 H2-02).
(5) `update_analysis_visual(uid, analysis_id, kind, *, status, key=None, joint=None)` — status 를 models.VISUAL_STATUSES 로 강제(위반 ValueError). kind=='correctedPose' 면 field-path 로 result.correctedPoseStatus/result.correctedPoseKey/result.correctedPoseJoint/result.correctedPoseUpdatedAtMs 만, kind=='rotation' 이면 result.rotationStatus/result.rotationVideoKey/result.rotationUpdatedAtMs 만 갱신. **분석 공용 top-level updatedAt 절대 미갱신** (리뷰 H-06). key 값은 "results/{uid}/{analysis_id}/" prefix 강제 (위반 ValueError — H-02). URL 파라미터 자체가 없음.
test_visual_jobs.py 실 테스트: 동일 (uid,analysisId,kind) 2회 예약 → 2회째 created False + counter 1회만 증가 + **반환 job 에 state/dispatchState/sourceHash(payload) 포함 (B2-03 분기 근거)**; 한도 도달 → reason daily_limit + 증가 0; failed + allow_retry_failed=False → created False (재예약 0); failed + True → 재예약 + generation 2; **outbox: 신규 예약 문서에 dispatchState 'pending' 존재; mark_visual_job_dispatched 로 'sent' CAS + 이미 sent 면 False**; CAS expect 불일치 → False + 무변경; **polling 전이에 taskId 부재 → ValueError; creating 전이에 lease 필드 부재 → ValueError**; update_analysis_visual 이 top-level updatedAt 키를 쓰지 않음; key prefix 위반 ValueError; status 위반 ValueError; payload flat scalar 기록/nested ValueError/예약어 ValueError.</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31/test_visual_jobs.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - firestore_admin.py 에 reserve_visual_job / transition_visual_job / mark_visual_job_dispatched / mark_visual_job_dispatch_failed / update_analysis_visual 5종 존재
    - reserve 가 outbox dispatchState='pending' 을 같은 transaction 에 기록 (테스트 고정 — H2-02)
    - polling 전이 taskId 필수 validator + creating 전이 lease 필수 validator 테스트 green (B2-02)
    - allow_retry_failed 분기 (자동 경로 failed no-op / 사용자 경로 generation+1) 테스트 green
    - update_analysis_visual 시그니처에 URL 파라미터 없음 + top-level updatedAt 미기록 테스트 green
    - `python -m pytest backend/tests/phase31/test_visual_jobs.py -x -q` exits 0
  </acceptance_criteria>
  <done>quota·멱등·outbox·lease·전용 timestamp·canonical key 규율이 함수 시그니처와 테스트로 강제됨.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 요청/워커 → visualJobs 문서 | 상태 전이의 유일한 진실 — CAS+validator 로만 변경 |
| 표시 필드 → 앱 | canonical key 만 — URL 은 인증 재서명 경로로만 발급 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-05 | Denial of Service (과금) | quota 예약 | mitigate | transaction 원자 예약 — 동시 요청/SQS 실패에서 이중 소비·고아 quota 불가 (테스트 고정, 리뷰 H-06) |
| T-31-06 | Tampering | stale SQS 메시지의 상태 되감기 | mitigate | transition_visual_job CAS + taskId/lease validator (리뷰 B-03 + B2-02) |
| T-31-07 | Information Disclosure | Firestore 에 만료 URL 저장 | mitigate | update_analysis_visual 에 URL 파라미터 부재 + key prefix ValueError (리뷰 H-02) |
| T-31-08 | Elevation of Privilege | visualJobs 컬렉션 노출 | mitigate | 백엔드 전용 컬렉션 (Firestore rules 미추가 — 기본 거부), 앱은 analysis 표시 필드만 구독 |
| T-31-52 | Denial of Service (과금) | creating crash 후 무조건 재생성 | mitigate | lease 만료 + taskId 부재 = 'create_unconfirmed' 수동 reconciliation — 자동 재생성 금지 (B2-02, DashScope idempotency 미지원) |
</threat_model>

<verification>
- `python -m pytest backend/tests -q` green (전체 무회귀)
- firestore_admin 신규 함수가 _validate_dict_only_scalars 관례 준수 (nested 거부)
</verification>

<success_criteria>
- 단일 visual job 계약(side-effect 단위 9상태·lease·outbox·멱등키·CAS validator·typed failure 9종·전용 timestamp·canonical key)이 테스트로 고정
- 8개 테스트 파일 스캐폴드 + mock 완성
</success_criteria>

<output>
Create `.planning/phases/31-api-visual-correction/31-02-SUMMARY.md` when done
</output>
