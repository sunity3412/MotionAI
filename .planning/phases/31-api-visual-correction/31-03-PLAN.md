---
phase: 31-api-visual-correction
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/shared/python/sunity_shared/analysis/fault_zoom.py
  - backend/functions/pipeline/app.py
  - backend/tests/test_fault_zoom_arrow.py
autonomous: true
requirements: [D-10, D-11, D-12]
must_haves:
  truths:
    - "목표 각도 화살표는 3점(proximal/vertex/distal) + DTW 대응 reference 좌표 기하로만 산출된다 — direction 어휘·measuredValue/baselineValue 로 화면 기하를 만들지 않는다 (리뷰 B-02)"
    - "명시적 renderer 매핑(ARROW_JOINT_MAP)이 선언된 관절만 화살표를 만들고, ref 대응 실패·저신뢰·미매핑·미세 차이는 화살표 생략 (리뷰 B-02 omission 규칙)"
    - "reference_relative deviation record 는 화살표 기하에 사용될 수 없다 — 기하가 record 수치를 아예 읽지 않음이 테스트로 고정 (리뷰 B-02)"
    - "화살표 미사용(legacy) 경로는 기존 줌 렌더와 바이트 동일 (무회귀)"
    - "결함 줌 item 에 DTW 대응 프레임 쌍(userFrameIdx/refFrameIdx/refMatched)이 방출된다 — 2D 비교 뷰어(amended D-10)의 프레임 정합 소스 + 자동 정렬 테스트 (리뷰 B-01 frame-pair alignment test)"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/fault_zoom.py"
      provides: "TargetArrowSpec dataclass + ARROW_JOINT_MAP + _build_arrow_spec + _draw_target_arrow"
      contains: "TargetArrowSpec"
    - path: "backend/tests/test_fault_zoom_arrow.py"
      provides: "기하 golden 테스트 (좌/우, 도립, 미러, 저신뢰, ref 실패, 미세 delta, edge clamp)"
      min_lines: 120
    - path: "backend/functions/pipeline/app.py"
      provides: "줌 item 에 userFrameIdx/refFrameIdx/refMatched scalar 방출"
  key_links:
    - from: "backend/shared/python/sunity_shared/analysis/fault_zoom.py"
      to: "_to_crop_px"
      via: "좌표 변환 단일 출처"
      pattern: "_to_crop_px"
    - from: "backend/functions/pipeline/app.py"
      to: "backend/shared/python/sunity_shared/analysis/fault_zoom.py"
      via: "build_fault_zoom_comparisons 반환 item 의 프레임 쌍 passthrough"
      pattern: "refFrameIdx"
---

<objective>
리뷰 BLOCKER B-02 를 해소하는 방식으로 1단 오버레이(D-11/D-12)를 구현한다. 기각된 설계(limb 1개 + direction 어휘 회전 + unit=='deg' 절대각 가정)를 버리고, DTW 대응 reference pose 좌표 기반 TargetArrowSpec 을 도입한다: 사용자의 (proximal, vertex) 세그먼트에 reference 의 (proximal, vertex) 를 정합(2D similarity: 회전+스케일)시킨 변환을 reference distal 에 적용해 목표 endpoint 를 얻는다 — 감점 record 의 measuredValue/baselineValue/direction 은 기하에 일절 사용하지 않는다 (reference_relative 의 "measuredValue=편차일 수 있음" 함정이 원천 차단됨).

부가로 줌 item 에 DTW 대응 프레임 쌍 scalar 를 방출한다 — 31-08 의 2D 비교 뷰어(amended D-10)가 소비하는 프레임 정합 소스이자 리뷰 B-01 이 요구한 자동 frame-pair alignment 테스트의 대상.

Purpose: 비용 0 의 1단 시각 교정 — 단, 틀린 방향 화살표(오코칭)가 구조적으로 불가능한 기하로.
Output: fault_zoom.py 스펙/드로잉 + pipeline item 방출 + 기하 테스트.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-api-visual-correction/31-CONTEXT.md
@.planning/phases/31-api-visual-correction/31-PATTERNS.md
@.planning/phases/31-api-visual-correction/31-REVIEWS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TargetArrowSpec + ARROW_JOINT_MAP + 정합 기하 + 드로잉</name>
  <files>backend/shared/python/sunity_shared/analysis/fault_zoom.py, backend/tests/test_fault_zoom_arrow.py</files>
  <read_first>backend/shared/python/sunity_shared/analysis/fault_zoom.py (전체 — _to_crop_px lines 468-484, _draw_leg_angle lines 615-664 의 계약: in-place draw + bool 반환 + degenerate 가드 + _BRAND + 한글 금지, _member_pts 의 신뢰/relaxed 좌표 계약, build_fault_zoom_comparisons lines 779-940 의 u_kp_idx/r_kp_idx/ref_match_failed 산출), backend/shared/python/sunity_shared/analysis/skeleton.py (COCO-17 keypoint 이름), backend/shared/python/sunity_shared/analysis/deduction_engine.py lines 47-70 (DeductionRecord — deviation_source 필드: 이 record 값들을 기하에 쓰지 않음을 확인하는 근거)</read_first>
  <action>fault_zoom.py 에 신설 (모듈 내 docstring 에 리뷰 B-02 근거 + "display 전용, 채점 무접촉" 명시):
(1) `@dataclass(frozen=True) class TargetArrowSpec` — 필드: vertex/proximal/distal (keypoint 이름 str), user_vertex_px/user_proximal_px/user_distal_px/target_endpoint_px (crop-px tuple), source_kind ('reference_pose' 고정 — ipsf_absolute 구축은 검증 전이므로 v1 미사용, 필드만 예약), confidence (float), omit_reason (str | None — spec 생성 실패 사유).
(2) 모듈 상수 `ARROW_JOINT_MAP: dict[str, tuple[str, str, str]]` — 화살표를 그릴 수 있는 관절만 (proximal, vertex, distal) COCO-17 이름으로 명시 선언: left_knee=(left_hip,left_knee,left_ankle), right_knee 동형, left_elbow=(left_shoulder,left_elbow,left_wrist), right_elbow 동형, left_hip=(left_shoulder,left_hip,left_knee), right_hip 동형. 미선언 관절(집합/모호 criterion) = 화살표 없음 (리뷰 B-02: 선언된 매핑만).
(3) `_build_arrow_spec(joint_key, user_report, u_kp_idx, ref_report, r_kp_idx, ref_match_failed, crop_ctx) -> TargetArrowSpec` — 규칙: ref_match_failed → omit_reason='ref_match_failed'; joint_key 미매핑 → 'unmapped_joint'; 6개 keypoint(사용자 3 + reference 3) 중 하나라도 신뢰 좌표 아님(기존 _member_pts 신뢰 기준 재사용) → 'low_confidence'; 정상이면 정규화 이미지 공간에서 2D similarity 변환 T (reference (proximal,vertex) → 사용자 (proximal,vertex) 로 회전+스케일+평행이동) 를 구해 reference distal 에 적용 = 후보 endpoint A; (proximal-vertex 축 반사를 추가한 변환) 적용 = 후보 endpoint B; 사용자 현재 distal 에 더 가까운 후보 채택 (small-correction prior — 미러 영상 좌우 오류 방어, 리뷰 B-02 mirrored 케이스); |target − user_distal| < 최소 픽셀 상수(_MIN_ARROW_DELTA_PX, _MIN_LEG_VEC_PX 준용) → 'negligible_delta'; crop-px 변환은 전부 _to_crop_px 경유.
(4) `_draw_target_arrow(img, spec: TargetArrowSpec) -> bool` — _draw_leg_angle 계약 준수: omit_reason 있으면 즉시 False (드로잉 0 — 오인 방지); 사용자 distal → target endpoint 직선 + 삼각 화살촉(_BRAND, width 4) + target endpoint 에 목표 위치 점선 마커(width 3); 라벨은 숫자/기호만 (한글·이모지 금지); 캔버스 밖 좌표 [0,_OUT-1] clamp; 성공 True.
test_fault_zoom_arrow.py (신규): 합성 keypoint fixture 로 기하 수치 assert — (a) 좌/우 무릎 각각 endpoint 가 기대 좌표 ±2px, (b) 도립(pose 상하 반전) 입력에서 endpoint 가 반전 기하 기대값, (c) 미러(reference 좌우 반전) 입력에서 반사 후보가 선택돼 endpoint 가 사용자측 근방(오측 endpoint 대비 가까움 assert), (d) ref_match_failed/미매핑/저신뢰/negligible → omit_reason 설정 + _draw_target_arrow False + 이미지 픽셀 무변화, (e) edge 좌표 clamp, (f) **record 비의존 고정**: _build_arrow_spec 시그니처/본문이 DeductionRecord 를 받지 않음 — 모듈 소스에 measuredValue/baselineValue 참조가 화살표 경로에 없음을 grep assert (리뷰 B-02 reference_relative-omission 케이스의 구조적 해소), (g) 정상 spec → 드로잉 True + 픽셀 diff 발생 + diff bbox 가 기대 영역 내.</action>
  <verify>
    <automated>python -m pytest backend/tests/test_fault_zoom_arrow.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - fault_zoom.py 에 class TargetArrowSpec / ARROW_JOINT_MAP / def _build_arrow_spec / def _draw_target_arrow 존재
    - _build_arrow_spec 이 DeductionRecord/measured/baseline/direction 값을 인자로 받지 않음
    - 좌/우·도립·미러·저신뢰·ref실패·미세delta·clamp 7계열 테스트 green
    - `python -m pytest backend/tests/test_fault_zoom_arrow.py -x -q` exits 0
  </acceptance_criteria>
  <done>화살표 기하가 reference 좌표 정합 기반으로 테스트 고정 — 방향 오류가 구조적으로 불가능.</done>
</task>

<task type="auto">
  <name>Task 2: 줌 렌더 배선 + 프레임 쌍 방출 + 무회귀</name>
  <files>backend/shared/python/sunity_shared/analysis/fault_zoom.py, backend/functions/pipeline/app.py, backend/tests/test_fault_zoom_arrow.py</files>
  <read_first>backend/shared/python/sunity_shared/analysis/fault_zoom.py lines 842-940 (build_fault_zoom_comparisons — u_kp_idx/r_kp_idx 산출부와 card 루프, 반환 item 형상 {joint, deficitDeg, png}), backend/functions/pipeline/app.py lines 2851-2879 (_render_fault_zoom item 조립 — scalar 필드 방출 관례 + _validate_dict_only_scalars 주석 스타일)</read_first>
  <action>(1) build_fault_zoom_comparisons 확장 — optional 파라미터 `draw_arrows: bool = False` (기본 False → 기존 호출 전부 바이트 동일 무회귀). True 면 카드 루프에서 unit 멤버 중 ARROW_JOINT_MAP 에 있는 관절에 대해 _build_arrow_spec 을 만들고 사용자측 crop 이미지에만 _draw_target_arrow (reference 측 이미지는 무접촉 — 게이트 B 선례와 일관). (2) 반환 item 에 scalar 3종 추가: `userFrameIdx` = u_kp_idx (user keypointReport 프레임 공간 int), `refFrameIdx` = r_kp_idx (ref keypointReport 프레임 공간 int), `refMatched` = not ref_match_failed (bool) — 항상 방출 (draw_arrows 와 무관; 2D 비교 뷰어의 프레임 정합 소스, amended D-10). (3) pipeline/app.py _render_fault_zoom: draw_arrows=True 전달 + item 조립부에 userFrameIdx/refFrameIdx/refMatched passthrough (scalar 라 _validate_dict_only_scalars 통과 — TS lockstep 주석: analysis.ts FaultZoomComparison optional 필드, 31-04 담당 명시). 채점 값 mutation 절대 금지 — 읽기도 프레임 인덱스 외 불필요. (4) 테스트 추가: draw_arrows=False (기본) 시 기존 fixture 출력과 바이트 동일; True 시 사용자측 PNG 만 diff 발생; item 에 프레임 쌍 3키 존재 + **정렬 테스트 (리뷰 B-01)**: 합성 dtw_match 로 u_kp_idx→r_kp_idx 대응이 _matched_ref_frame 결과와 일치함을 item 방출값으로 assert; ref_match_failed 시 refMatched False + 화살표 0.</action>
  <verify>
    <automated>python -m pytest backend/tests/test_fault_zoom_arrow.py -x -q && python -m pytest backend/tests -q</automated>
  </verify>
  <acceptance_criteria>
    - build_fault_zoom_comparisons 반환 item 에 userFrameIdx/refFrameIdx/refMatched 존재 (테스트 고정)
    - draw_arrows 기본 False + False 경로 바이트 동일 무회귀 테스트 green
    - pipeline _render_fault_zoom 에 draw_arrows=True 와 프레임 쌍 passthrough 존재 (grep: refFrameIdx)
    - `python -m pytest backend/tests -q` exits 0 (기존 fault_zoom 테스트 포함 무회귀)
  </acceptance_criteria>
  <done>실분석 경로에서 화살표+프레임 쌍이 방출되고 legacy 무회귀가 증명됨.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 채점 산출물 → 렌더러 | 프레임 인덱스만 읽기 — record 수치는 기하에 미사용 |
| 렌더 산출물 → 사용자 | 틀린 화살표 = 오코칭 (신뢰 손상) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-09 | Repudiation (오코칭) | 화살표 방향/위치 오류 | mitigate | reference 좌표 정합 기하 + 반사 후보 선택 + omission 규칙 + 7계열 golden 테스트 (리뷰 B-02) |
| T-31-10 | Tampering | 채점 데이터 경계 | mitigate | 기하 함수가 record 를 인자로 받지 않음 (구조적 차단) + result mutation 0 |
| T-31-11 | Repudiation (오정합) | 프레임 쌍 오방출 | mitigate | B-01 frame-pair alignment 테스트 — _matched_ref_frame 결과와 방출값 일치 고정 |
</threat_model>

<verification>
- `python -m pytest backend/tests -q` green (무회귀)
- fault_zoom.py 화살표 경로에 dimensions/kismam import 없음
- 실 fixture 시각 게이트는 31-12 E2E 에서 (Pod 필요 — 리뷰 B-02 rollout 전 시각 확인)
</verification>

<success_criteria>
- D-11 화살표가 B-02 해소 설계(3점+reference 좌표)로 구현, D-12 기존 줌 카드 재사용 (앱 파일 변경 0)
- 프레임 쌍 방출 + B-01 정렬 테스트 green
</success_criteria>

<output>
Create `.planning/phases/31-api-visual-correction/31-03-SUMMARY.md` when done
</output>
