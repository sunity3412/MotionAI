---
phase: 31-api-visual-correction
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/shared/python/sunity_shared/analysis/fault_zoom.py
  - backend/functions/pipeline/app.py
  - backend/tests/test_fault_zoom_arrow.py
autonomous: true
requirements: [D-05, D-10, D-11, D-12]
must_haves:
  truths:
    - "목표 각도 화살표는 3점(proximal/vertex/distal) + DTW 대응 reference 좌표 기하로만 산출된다 — direction 어휘·measuredValue/baselineValue 로 화면 기하를 만들지 않는다 (리뷰 B-02)"
    - "미러 parity 는 per-joint 거리 선택이 아니라 full-body topology 로 프레임 단위 1회 결정되어 provenance 에 기록되고, 불명확하면 화살표를 생략한다 (2차 리뷰 H2-03)"
    - "correctedPose 생성의 joint/targetDeg/src frame 은 CorrectedPoseTarget 단일 immutable 계약으로 묶인다 — targetDeg 는 DTW matched reference 3점 내각에서만 계산, 감점 record 수치는 기하에 미유입 (2차 리뷰 B2-01)"
    - "감점 record 는 후보 우선순위에만 쓰이며 정렬은 명시적으로 abs(points) 내림차순 + 동률 시 criterion key 오름차순 (결정론 tie-break — 3차 M3-06). signed-negative 함정 차단. criterion→joint 선언 매핑이 없는 항목·collective·low-confidence·ref-match-fail 은 correctedPose 생략 = legacy 숨김 (2차 리뷰 B2-01)"
    - "HISTORICAL_JOINT_REGISTRY(append-only)가 과거 joint 이름을 안정 보존한다 — 31-07 페어 삭제의 pairId 재계산 근거, ARROW_JOINT_MAP 변경에도 삭제 가능 (3차 M3-05)"
    - "명시적 renderer 매핑(ARROW_JOINT_MAP)이 선언된 관절만 화살표를 만들고, ref 대응 실패·저신뢰·미매핑·미세 차이는 화살표 생략 (리뷰 B-02 omission 규칙)"
    - "화살표 미사용(legacy) 경로는 기존 줌 렌더와 바이트 동일 (무회귀)"
    - "D-11: 1단 오버레이는 목표 각도 화살표+목표선부터 — 이상 궤적 선·힘 벡터는 후속 (deferred)"
    - "D-12: 화살표 적용 위치 = 기존 결함 줌 카드 위 — 기존 동선/컴포넌트 재사용, 신규 화면 0"
    - "결함 줌 item 에 DTW 대응 프레임 쌍(userFrameIdx/refFrameIdx/refMatched)이 방출된다 — 2D 비교 뷰어(amended D-10)의 프레임 정합 소스 + 자동 정렬 테스트 (리뷰 B-01)"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/fault_zoom.py"
      provides: "TargetArrowSpec + CorrectedPoseTarget + ARROW_JOINT_MAP + CRITERION_JOINT_MAP + joint_inner_angle_deg + _frame_mirror_parity + _build_arrow_spec + build_corrected_pose_target + _draw_target_arrow"
      contains: "CorrectedPoseTarget"
    - path: "backend/tests/test_fault_zoom_arrow.py"
      provides: "기하 golden + parity adversarial + CorrectedPoseTarget provenance 테스트"
      min_lines: 160
    - path: "backend/functions/pipeline/app.py"
      provides: "줌 item 에 userFrameIdx/refFrameIdx/refMatched scalar 방출"
  key_links:
    - from: "backend/shared/python/sunity_shared/analysis/fault_zoom.py"
      to: "_to_crop_px"
      via: "좌표 변환 단일 출처"
      pattern: "_to_crop_px"
    - from: "backend/shared/python/sunity_shared/analysis/fault_zoom.py"
      to: "backend/shared/python/sunity_shared/analysis/deduction_engine.py"
      via: "후보 우선순위만 — 기하값 미사용 (abs(points) 정렬)"
      pattern: "abs"
    - from: "backend/functions/pipeline/app.py"
      to: "backend/shared/python/sunity_shared/analysis/fault_zoom.py"
      via: "build_fault_zoom_comparisons 반환 item 의 프레임 쌍 passthrough"
      pattern: "refFrameIdx"
---

<objective>
리뷰 BLOCKER B-02 와 2차 리뷰 BLOCKER B2-01·HIGH H2-03 을 해소하는 방식으로 1단 오버레이(D-11/D-12)와 correctedPose target 계약을 구현한다.

(1) TargetArrowSpec: 사용자의 (proximal, vertex) 세그먼트에 reference 의 (proximal, vertex) 를 정합(2D similarity: 회전+스케일)시킨 변환을 reference distal 에 적용해 목표 endpoint 를 얻는다 — 감점 record 의 measuredValue/baselineValue/direction 은 기하에 일절 사용하지 않는다. 미러 반사 후보의 선택은 per-joint "가까운 쪽" 휴리스틱을 쓰지 않는다 (2차 리뷰 H2-03: 큰 교정에서 오방향) — full-body topology 기반 프레임 단위 parity 판정 1회로 결정하고, 불명확하면 생략한다.

(2) CorrectedPoseTarget (2차 리뷰 B2-01, §7 Step 1 — 이 계약이 고정되기 전에 correctedPose worker/prompt/pose gate 구현 착수 금지): joint, targetDeg, source frame 을 따로 계산하지 않고 하나의 immutable 계약으로 묶는다. targetDeg 는 TargetArrowSpec 과 동일한 DTW matched reference 3점 내각에서만 계산한다 (`reference_relative.measuredValue` = 편차일 수 있음 함정 원천 차단 — deduction_engine.py:47-70 의 record 에는 joint_key 도 없다). 감점 record 는 후보 우선순위(abs(points) 명시 정렬)에만 쓴다. 31-10 enqueue 는 이 객체의 스칼라만 payload 로 기록하고, 31-09 의 프롬프트와 pose gate 는 동일 provenance 를 소비한다.

(3) 줌 item 에 DTW 대응 프레임 쌍 scalar 방출 — 31-08 뷰어(amended D-10) 프레임 정합 소스.

Purpose: 비용 0 의 1단 시각 교정 + 자동 교정의 신뢰 가능한 target provenance — 틀린 방향/틀린 관절/편차값 목표각이 구조적으로 불가능한 기하로.
Output: fault_zoom.py 스펙/target/드로잉 + pipeline item 방출 + 기하 테스트.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-api-visual-correction/31-CONTEXT.md
@.planning/phases/31-api-visual-correction/31-PATTERNS.md
@.planning/phases/31-api-visual-correction/31-PLAN-REVIEW-ITERATION2.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TargetArrowSpec + ARROW_JOINT_MAP + parity 판정 + 정합 기하 + 드로잉</name>
  <files>backend/shared/python/sunity_shared/analysis/fault_zoom.py, backend/tests/test_fault_zoom_arrow.py</files>
  <read_first>backend/shared/python/sunity_shared/analysis/fault_zoom.py (전체 — _to_crop_px lines 468-484, _draw_leg_angle lines 615-664 의 계약: in-place draw + bool 반환 + degenerate 가드 + _BRAND + 한글 금지, _member_pts 의 신뢰/relaxed 좌표 계약, build_fault_zoom_comparisons lines 779-940 의 u_kp_idx/r_kp_idx/ref_match_failed 산출), backend/shared/python/sunity_shared/analysis/skeleton.py (COCO-17 keypoint 이름), backend/shared/python/sunity_shared/analysis/deduction_engine.py lines 40-70 (DeductionRecord — joint_key 부재 + points signed-negative 확인 근거)</read_first>
  <action>fault_zoom.py 에 신설 (모듈 내 docstring 에 리뷰 B-02 + 2차 리뷰 H2-03 근거 + "display 전용, 채점 무접촉" 명시):
(1) `joint_inner_angle_deg(proximal_xy, vertex_xy, distal_xy) -> float` — vertex 내각 (arccos of normalized dot). **각도 산출의 단일 출처 — CorrectedPoseTarget(Task 2)·pose_gate(31-06)가 동일 함수를 import (계산 이원화 금지).**
(2) `@dataclass(frozen=True) class TargetArrowSpec` — 필드: vertex/proximal/distal (keypoint 이름 str), user_vertex_px/user_proximal_px/user_distal_px/target_endpoint_px (crop-px tuple), source_kind ('reference_pose' 고정 — ipsf_absolute 는 필드만 예약), mirror_parity ('same'|'mirrored' — 프레임 판정 결과 provenance), confidence (float), omit_reason (str | None).
(3) 모듈 상수 `ARROW_JOINT_MAP: dict[str, tuple[str, str, str]]` — (proximal, vertex, distal) COCO-17 명시 선언: left_knee=(left_hip,left_knee,left_ankle), right_knee 동형, left_elbow=(left_shoulder,left_elbow,left_wrist), right_elbow 동형, left_hip=(left_shoulder,left_hip,left_knee), right_hip 동형. 미선언 관절 = 화살표/target 없음.
(4) `_frame_mirror_parity(user_kps, ref_kps) -> str` — **per-joint 거리 선택 금지 (2차 리뷰 H2-03).** full-body topology 로 프레임 단위 1회 판정: 사용자·reference 각각 (left_shoulder→right_shoulder) 벡터와 (left_hip→right_hip) 벡터의 방향 부호(수평 성분 부호 + 어깨/힙 교차곱 방향 일관성)를 비교 — 두 프레임의 좌우 방향이 일치하면 'same', 반대면 'mirrored', 어깨/힙 keypoint 저신뢰·부호 불일치(어깨와 힙이 서로 다른 판정)면 'unknown'. 'unknown' 이면 호출측이 화살표/target 생략.
(5) `_build_arrow_spec(joint_key, user_report, u_kp_idx, ref_report, r_kp_idx, ref_match_failed, crop_ctx) -> TargetArrowSpec` — 규칙: ref_match_failed → omit_reason='ref_match_failed'; joint_key 미매핑 → 'unmapped_joint'; 6개 keypoint 신뢰 미달 → 'low_confidence'; `_frame_mirror_parity` == 'unknown' → 'parity_unknown' (생략 — H2-03: 가까운 후보 fallback 을 사용자 지시에 쓰지 않음); 정상이면 parity 가 'mirrored' 일 때만 reference 좌표에 좌우 반사를 적용한 뒤, 2D similarity 변환 T (reference (proximal,vertex) → 사용자 (proximal,vertex)) 를 구해 reference distal 에 적용 = 목표 endpoint (후보 2개 중 선택 로직 자체가 없음 — parity 가 유일 결정자); |target − user_distal| < _MIN_ARROW_DELTA_PX → 'negligible_delta'; crop-px 변환은 전부 _to_crop_px 경유.
(6) `_draw_target_arrow(img, spec) -> bool` — _draw_leg_angle 계약 준수: omit_reason 있으면 즉시 False (드로잉 0); 사용자 distal → target endpoint 직선 + 삼각 화살촉(_BRAND, width 4) + target endpoint 점선 마커(width 3); 라벨 숫자/기호만; [0,_OUT-1] clamp; 성공 True.
test_fault_zoom_arrow.py (신규): 합성 keypoint fixture 로 — (a) 좌/우 무릎 endpoint ±2px, (b) 도립 반전 기하, (c) parity: 사용자·reference 좌우 반대 프레임에서 'mirrored' 판정 + 반사 적용 endpoint 정확, **(c') adversarial (H2-03 요구): 목표 교정이 커서 "잘못된 반사 후보가 현재 distal 에 더 가까운" fixture — 구현이 거리 기반이면 실패하고 parity 기반이면 통과하는 golden**, (c'') 어깨/힙 판정 불일치 fixture → 'parity_unknown' 생략, (d) ref_match_failed/미매핑/저신뢰/negligible → omit + draw False + 픽셀 무변화, (e) edge clamp, (f) record 비의존: _build_arrow_spec 시그니처가 DeductionRecord 를 받지 않음 + 모듈 화살표 경로에 measuredValue/baselineValue 참조 없음 grep assert, (g) 정상 spec → draw True + diff bbox 기대 영역.</action>
  <verify>
    <automated>python -m pytest backend/tests/test_fault_zoom_arrow.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - fault_zoom.py 에 TargetArrowSpec / ARROW_JOINT_MAP / joint_inner_angle_deg / _frame_mirror_parity / _build_arrow_spec / _draw_target_arrow 존재
    - _build_arrow_spec 에 "가까운 후보 선택" 로직 부재 — parity 판정이 유일 반사 결정자 (adversarial 테스트 green — H2-03)
    - parity 'unknown' → 생략 테스트 green
    - 좌/우·도립·미러·저신뢰·ref실패·미세delta·clamp 계열 테스트 green
  </acceptance_criteria>
  <done>화살표 기하가 reference 좌표 정합 + topology parity 기반으로 테스트 고정 — 방향 오류가 구조적으로 불가능.</done>
</task>

<task type="auto">
  <name>Task 2: CorrectedPoseTarget — 자동 교정 target 단일 계약 (2차 리뷰 B2-01)</name>
  <files>backend/shared/python/sunity_shared/analysis/fault_zoom.py, backend/tests/test_fault_zoom_arrow.py</files>
  <read_first>backend/shared/python/sunity_shared/analysis/deduction_engine.py lines 40-95 (DeductionRecord 필드 — joint_key 부재·points signed-negative·deviation_source 3종 확인), backend/shared/python/sunity_shared/analysis/fault_zoom.py 의 build_fault_zoom_comparisons (기존 criterion/unit → 줌 대상 관절 매핑 로직 — CRITERION_JOINT_MAP 의 이관 원천)</read_first>
  <action>fault_zoom.py 에 신설 (docstring: 2차 리뷰 B2-01 전문 인용 — "joint/targetDeg/src frame 을 따로 계산하지 않는다"):
(1) `@dataclass(frozen=True) class CorrectedPoseTarget` — 필드: joint_key (str), proximal_key/vertex_key/distal_key (str), user_frame_idx (int), ref_frame_idx (int), source_kind ('reference_pose' 고정 — v1), source_frame_hash (str | None — 프레임 PNG sha256, 방출 시점에 pipeline 이 채움), target_deg (float — **DTW matched reference 3점의 joint_inner_angle_deg 로만 계산, record 수치 미유입**), confidence (float), provenance_version ('cp-target-v1' 상수). `to_payload() -> dict` — 31-02 reserve_visual_job payload 계약의 flat scalar (srcKey/sourceHash 는 pipeline 이 병합): {"jointKey", "targetDeg", "userFrameIdx", "refFrameIdx", "sourceKind", "confidence", "provenanceVersion"}. **hash 필드명 단일 계약 (checker W1): payload key = "sourceHash" = 프레임 PNG sha256 full hex (pipeline 31-10 이 산출·병합 — dataclass 의 source_frame_hash 와 동일 값), S3 srcKey 세그먼트 = sourceHash[:16].**
(2) 모듈 상수 `CRITERION_JOINT_MAP: dict[str, str]` — 감점 criterion 이름 → ARROW_JOINT_MAP joint_key 의 **명시 선언 매핑** (기존 build_fault_zoom_comparisons 의 criterion→줌 관절 로직에서 확인되는 항목만 이관·선언; collective/모호 criterion 은 선언하지 않음 = 후보 제외). **`HISTORICAL_JOINT_REGISTRY: frozenset[str]` 도 신설 (M3-05) — 지금까지 존재한 모든 joint_key 의 append-only 집합 (ARROW_JOINT_MAP 키 ⊆ 이 집합). ARROW_JOINT_MAP 에서 joint 가 제거/rename 돼도 이 레지스트리는 항목을 지우지 않는다 — 31-07 페어 삭제가 과거 pairId(uid:analysisId:joint HMAC)를 재계산하려면 과거 joint 이름이 안정적으로 남아야 하기 때문. 주석에 "삭제 전용 안정 레지스트리 — 절대 항목 제거 금지" 박제.**
(3) `build_corrected_pose_target(records: list[dict], user_report, ref_report, dtw_pairs, ref_match_failed) -> CorrectedPoseTarget | None` — 규칙 (B2-01 해결방향 전항): (a) 후보 = CRITERION_JOINT_MAP 에 선언된 criterion 의 record 만; (b) 우선순위 정렬 = **`abs(points)` 내림차순 → 동률 시 criterion key 오름차순 (결정론 tie-break — M3-06: 같은 감점 record 순서가 입력/직렬화에 따라 바뀌면 target 도 달라지므로 고정)** (points 는 signed-negative — max(points) 는 최소 감점을 고르는 함정, 주석으로 박제); (c) top 후보의 joint_key 에 대해 DTW 대응 프레임 쌍(줌 카드와 동일 u_kp_idx/r_kp_idx 산출 로직 재사용)에서 reference 3점 신뢰 좌표 확보 → target_deg = joint_inner_angle_deg(reference 3점); (d) ref_match_failed / 3점 저신뢰 / `_frame_mirror_parity`=='unknown' / 후보 0 → **None 반환 (correctedPose 생성 생략 = legacy 숨김 — 잘못된 target 으로 생성하지 않는다)**; (e) user_frame_idx = 해당 후보의 u_kp_idx — **source frame 과 target 이 같은 순간을 가리킴을 계약으로 강제** (top-1 감점과 fault zoom top-1 이 다른 기준을 고르더라도 이 객체 내부는 항상 자기 일관); (f) record 의 measuredValue/baselineValue/deviation 은 어떤 산술에도 미사용 (우선순위 비교 외 record 필드 접근 금지).
테스트 추가: (h) **reference_relative record 의 measuredValue 를 극단값(예: 999)으로 오염시켜도 target_deg 불변** (B2-01 §8 gate 2 — record 수치 미유입 증명), (i) points 정렬: [-5, -30, -12] 에서 -30 record 의 joint 선택 (abs 정렬 — §8 gate 3), (j) unmapped criterion 만 있으면 None (§8 gate 3), (k) ref_match_failed → None, (l) 반환 target 의 user_frame_idx 가 선택 후보의 u_kp_idx 와 일치 (source/target frame 일치 — B2-01), (m) to_payload 가 flat scalar 만 방출 + provenanceVersion 'cp-target-v1'.</action>
  <verify>
    <automated>python -m pytest backend/tests/test_fault_zoom_arrow.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - CorrectedPoseTarget dataclass + build_corrected_pose_target + CRITERION_JOINT_MAP 존재
    - record 수치 오염 → target_deg 불변 테스트 green (reference_relative 미유입 — §8 gate 2)
    - abs(points) 정렬 + unmapped 생략 테스트 green (§8 gate 3)
    - target_deg 가 joint_inner_angle_deg(reference 3점) 산출 — TargetArrowSpec 과 동일 geometry helper (단일 소스)
    - 생략 조건(None) 4계열 테스트 green
  </acceptance_criteria>
  <done>자동 교정의 joint/targetDeg/frame provenance 가 단일 immutable 계약으로 고정 — 31-10 enqueue 와 31-09 prompt/pose gate 가 이 객체만 소비.</done>
</task>

<task type="auto">
  <name>Task 3: 줌 렌더 배선 + 프레임 쌍 방출 + 무회귀</name>
  <files>backend/shared/python/sunity_shared/analysis/fault_zoom.py, backend/functions/pipeline/app.py, backend/tests/test_fault_zoom_arrow.py</files>
  <read_first>backend/shared/python/sunity_shared/analysis/fault_zoom.py lines 842-940 (build_fault_zoom_comparisons — u_kp_idx/r_kp_idx 산출부와 card 루프, 반환 item 형상 {joint, deficitDeg, png}), backend/functions/pipeline/app.py lines 2851-2879 (_render_fault_zoom item 조립 — scalar 필드 방출 관례 + _validate_dict_only_scalars 주석 스타일)</read_first>
  <action>(1) build_fault_zoom_comparisons 확장 — optional 파라미터 `draw_arrows: bool = False` (기본 False → 기존 호출 전부 바이트 동일 무회귀). True 면 카드 루프에서 unit 멤버 중 ARROW_JOINT_MAP 관절에 대해 _build_arrow_spec → 사용자측 crop 이미지에만 _draw_target_arrow (reference 측 무접촉). (2) 반환 item 에 scalar 3종 추가: `userFrameIdx` = u_kp_idx, `refFrameIdx` = r_kp_idx, `refMatched` = not ref_match_failed — 항상 방출 (draw_arrows 무관; 2D 비교 뷰어 프레임 정합 소스, amended D-10). (3) pipeline/app.py _render_fault_zoom: draw_arrows=True 전달 + item 조립부에 3종 passthrough (scalar — TS lockstep 주석: analysis.ts FaultZoomComparison optional 필드, 31-04 담당 명시). 채점 값 mutation 절대 금지. (4) 테스트 추가: draw_arrows=False 기본 바이트 동일; True 시 사용자측 PNG 만 diff; item 3키 존재 + 정렬 테스트 (리뷰 B-01): 합성 dtw_match 로 u_kp_idx→r_kp_idx 대응이 _matched_ref_frame 결과와 일치; ref_match_failed 시 refMatched False + 화살표 0.</action>
  <verify>
    <automated>python -m pytest backend/tests/test_fault_zoom_arrow.py -x -q && python -m pytest backend/tests -q</automated>
  </verify>
  <acceptance_criteria>
    - build_fault_zoom_comparisons 반환 item 에 userFrameIdx/refFrameIdx/refMatched 존재 (테스트 고정)
    - draw_arrows 기본 False + False 경로 바이트 동일 무회귀 테스트 green
    - pipeline _render_fault_zoom 에 draw_arrows=True 와 프레임 쌍 passthrough 존재 (grep: refFrameIdx)
    - `python -m pytest backend/tests -q` exits 0 (기존 fault_zoom 테스트 포함 무회귀)
  </acceptance_criteria>
  <done>실분석 경로에서 화살표+프레임 쌍이 방출되고 legacy 무회귀가 증명됨.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 채점 산출물 → 렌더러/target | 우선순위(abs points)만 읽기 — record 수치는 기하에 미사용 |
| 렌더 산출물 → 사용자 | 틀린 화살표/틀린 target = 오코칭 (신뢰 손상) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-09 | Repudiation (오코칭) | 화살표 방향/위치 오류 | mitigate | reference 좌표 정합 기하 + topology parity 단일 결정 + omission 규칙 + adversarial golden (리뷰 B-02 + H2-03) |
| T-31-10 | Tampering | 채점 데이터 경계 | mitigate | 기하 함수가 record 를 인자로 받지 않음 + target_deg 에 record 수치 미유입 테스트 (B-02 + B2-01) |
| T-31-11 | Repudiation (오정합) | 프레임 쌍 오방출 | mitigate | B-01 frame-pair alignment 테스트 — _matched_ref_frame 결과와 방출값 일치 고정 |
| T-31-53 | Repudiation (오코칭) | 잘못된 관절/편차값 목표각으로 생성 | mitigate | CorrectedPoseTarget 단일 계약 — 선언 매핑만 후보, reference 3점 내각만 target, 불확실 = None 생략 (B2-01) |
</threat_model>

<verification>
- `python -m pytest backend/tests -q` green (무회귀)
- fault_zoom.py 화살표/target 경로에 dimensions/kismam import 없음
- 실 fixture 시각 게이트는 31-12 E2E 에서 (Pod 필요 — 리뷰 B-02 rollout 전 시각 확인)
</verification>

<success_criteria>
- D-11 화살표가 B-02+H2-03 해소 설계(3점+reference 좌표+topology parity)로 구현, D-12 기존 줌 카드 재사용 (앱 파일 변경 0)
- CorrectedPoseTarget 계약이 §8 gate 1·2·3 테스트로 고정 — 31-09/31-10 은 이 계약 확정 후에만 correctedPose 경로 구현 (리뷰 §7 Step 1)
- 프레임 쌍 방출 + B-01 정렬 테스트 green
</success_criteria>

<output>
Create `.planning/phases/31-api-visual-correction/31-03-SUMMARY.md` when done
</output>
