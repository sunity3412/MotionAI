---
phase: 31-api-visual-correction
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/shared/python/sunity_shared/analysis/fault_zoom.py
  - backend/tests/test_fault_zoom_arrow.py
  - backend/functions/pipeline/app.py
autonomous: true
requirements: [D-11, D-12]
must_haves:
  truths:
    - "결함 줌 카드의 사용자측 이미지 위에 목표 각도 화살표와 목표선이 렌더된다 (D-11: '여기까지 올려야 함' 화살표 + 목표선)"
    - "화살표는 기존 결함 줌 카드 위에 그려진다 — 신규 화면 0, 앱 변경 0 (D-12: backend 합성 PNG)"
    - "화살표 스펙 미제공(legacy 호출) 시 기존 줌 렌더와 픽셀 동일 (무회귀)"
    - "드로잉은 display 전용 — 채점 모듈(dimensions/deduction_engine/kismam) 무접촉 (점수 반영 금지 invariant)"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/fault_zoom.py"
      provides: "_draw_target_arrow 드로잉 함수 (bool 반환, degenerate 가드)"
      contains: "_draw_target_arrow"
    - path: "backend/tests/test_fault_zoom_arrow.py"
      provides: "화살표 순수 함수 단위 테스트"
      min_lines: 60
    - path: "backend/functions/pipeline/app.py"
      provides: "_render_fault_zoom 이 DeductionRecord/JointScore.direction 기반 arrow_specs 를 전달"
  key_links:
    - from: "backend/functions/pipeline/app.py"
      to: "backend/shared/python/sunity_shared/analysis/fault_zoom.py"
      via: "arrow_specs 파라미터"
      pattern: "arrow_specs"
    - from: "backend/shared/python/sunity_shared/analysis/fault_zoom.py"
      to: "_to_crop_px"
      via: "좌표 변환 단일 출처"
      pattern: "_to_crop_px"
---

<objective>
1단 기하 오버레이(D-11)를 구현한다: 결함 관절의 현재 각도에서 목표 각도까지 "여기까지 올려야 함" 을 보여주는 화살표 + 목표선을 기존 결함 줌 카드 PNG 위에 PIL 로 렌더한다 (D-12: 기존 동선·컴포넌트 재사용, 신규 화면 0). 데이터는 전부 기존 산출물 — DeductionRecord 의 measuredValue(현재 각)/baselineValue(목표 각)/unit='deg' + JointScore.direction ('extend'|'flex'|'raise'|'lower'|'open'|'close') — 신규 측정 0. 화살표는 앱 SVG 가 아니라 backend 합성이다 (앱은 crop-px 좌표를 모름 — RESEARCH 안티패턴).

Purpose: 비용 0·즉시 가능한 1단 시각 교정 — "왜 안 되는지 + 무엇이 필요한지" 를 눈으로 보여주는 첫 층.
Output: fault_zoom.py 화살표 드로잉 + pipeline 배선 + 단위 테스트.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-api-visual-correction/31-CONTEXT.md
@.planning/phases/31-api-visual-correction/31-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: _draw_target_arrow 드로잉 함수 + 단위 테스트</name>
  <files>backend/shared/python/sunity_shared/analysis/fault_zoom.py, backend/tests/test_fault_zoom_arrow.py</files>
  <read_first>backend/shared/python/sunity_shared/analysis/fault_zoom.py (전체 — 특히 _to_crop_px lines 468-484, _draw_leg_angle lines 615-664 의 계약: in-place draw + bool 반환 + degenerate 가드 + _BRAND 색·width 4 + 한글 금지, _mark lines 605-611 배지 스타일, _deficit_label), backend/shared/python/sunity_shared/analysis/skeleton.py (JOINT_KEYS 관절 인접 구조 — 화살표의 limb 벡터 산출), 기존 backend/tests/test_fault_zoom*.py 1개 (테스트 픽스처 관례)</read_first>
  <action>fault_zoom.py 에 `_draw_target_arrow(img, anchor_px, limb_px, direction, measured_deg, target_deg) -> bool` 신설 — _draw_leg_angle 을 모델로: (1) anchor(결함 관절 px)→limb(자식 관절 px) 현재 벡터를 signed delta = target_deg − measured_deg 만큼 anchor 중심으로 회전시켜 목표 endpoint 산출 (회전 방향 부호는 direction 어휘로 결정: 'extend'/'raise'/'open' = 목표각 커지는 방향, 'flex'/'lower'/'close' = 작아지는 방향 — 값 관계와 모순 시 delta 부호 우선), (2) 목표 위치에 점선 목표선(_BRAND, width 3) + 현재 endpoint 에서 목표 endpoint 로 호를 따라가는 화살표(직선+삼각 화살촉, _BRAND, width 4), (3) _mark 스타일 배지로 목표각 라벨 — 숫자/기호만 (예: "-> 175", 한글·이모지 금지 — 폰트 회피 관례), (4) degenerate 가드: 벡터 길이 < _MIN_LEG_VEC_PX 유사 상수·delta 절대값 < 3° 이면 드로잉 생략 False 반환, 캔버스 밖 좌표는 [0,_OUT-1] clamp (_to_crop_px 관례). 좌표 변환은 반드시 _to_crop_px 경유 (중복 공식 금지). docstring 에 "display 전용, 채점 무접촉" + D-11/D-12 근거 명시. test_fault_zoom_arrow.py (신규 파일 — 기존 fault_zoom 테스트 파일과 소유 분리): direction 어휘 6종 각각의 회전 부호, degenerate(짧은 벡터/미세 delta) → False + 이미지 무변화, 정상 입력 → True + 드로잉 전후 픽셀 diff 0 초과, target endpoint 가 캔버스 내 clamp, 채점 모듈 import 없음(모듈 소스 grep 로 dimensions/kismam import 부재 assert).</action>
  <verify>
    <automated>python -m pytest backend/tests/test_fault_zoom_arrow.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - fault_zoom.py contains `def _draw_target_arrow` returning bool
    - 좌표 산출이 `_to_crop_px` 를 경유 (자체 변환 공식 없음)
    - `python -m pytest backend/tests/test_fault_zoom_arrow.py -x -q` exits 0
    - 테스트가 degenerate → False + 픽셀 무변화를 고정
  </acceptance_criteria>
  <done>화살표 드로잉이 순수 함수로 테스트 고정됨.</done>
</task>

<task type="auto">
  <name>Task 2: pipeline arrow_specs 배선 (DeductionRecord → 줌 렌더)</name>
  <files>backend/functions/pipeline/app.py, backend/shared/python/sunity_shared/analysis/fault_zoom.py, backend/tests/test_fault_zoom_arrow.py</files>
  <read_first>backend/functions/pipeline/app.py lines 2764-2879 (_render_fault_zoom — deficits 전달 현행 형상 + cached_user_frames 파라미터), backend/shared/python/sunity_shared/analysis/fault_zoom.py (렌더 엔트리 시그니처 — deficits 수신부와 사용자측 이미지 합성 지점), backend/shared/python/sunity_shared/models.py (DeductionRecord 필드: criterion/measuredValue/baselineValue/deviation/unit + JointScore.direction 어휘)</read_first>
  <action>fault_zoom 렌더 엔트리에 optional 파라미터 `arrow_specs: dict[str, dict] | None = None` 추가 (기본 None → 화살표 생략, 기존 호출 전부 무회귀). spec 형상: `{joint_key: {"direction": str, "measured_deg": float, "target_deg": float}}`. 렌더 내부에서 결함 관절의 사용자측 crop 이미지에 한해 _draw_target_arrow 호출 — anchor = 결함 관절 keypoint, limb = skeleton 인접 자식 관절 keypoint (둘 다 기존 keypoint 소스에서 _to_crop_px 변환). 실패(False)는 조용히 생략 (오인 방지 관례). pipeline `_render_fault_zoom` 에서 deduction records 를 순회해 unit=='deg' 인 결함 관절만 arrow_specs 를 구성 (measuredValue→measured_deg, baselineValue→target_deg, JointScore.direction→direction) 후 렌더에 전달 — 채점 값 읽기 전용, mutation 절대 금지. test_fault_zoom_arrow.py 에 배선 테스트 추가: arrow_specs 전달 시 사용자측 PNG 가 미전달 대비 픽셀 diff 발생, None 전달 시 기존 출력과 바이트 동일(무회귀), unit!='deg' 레코드는 spec 제외.</action>
  <verify>
    <automated>python -m pytest backend/tests/test_fault_zoom_arrow.py backend/tests/test_fault_zoom*.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - pipeline/app.py 의 _render_fault_zoom 경로에 `arrow_specs` 구성 코드 존재 (grep: `arrow_specs`)
    - arrow_specs=None 무회귀가 테스트로 고정 (기존 fault_zoom 테스트 전부 green)
    - `python -m pytest backend/tests -q` exits 0
  </acceptance_criteria>
  <done>실분석 경로에서 결함 줌 카드에 화살표가 실리고, legacy 경로 무회귀가 증명됨.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 채점 산출물 → 렌더러 | DeductionRecord 값을 읽기 전용으로만 소비 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-07 | Tampering | 채점 데이터 경계 | mitigate | arrow_specs 는 records 의 read-only 파생 — 렌더 경로에서 result/breakdown mutation 금지, 테스트로 채점 모듈 import 부재 고정 |
| T-31-08 | Repudiation (오인) | 잘못 그려진 화살표 | mitigate | degenerate/미세 delta 는 드로잉 생략(False) — 틀린 시각 지시로 인한 신뢰 손상 방지 (_draw_leg_angle 선례) |
</threat_model>

<verification>
- `python -m pytest backend/tests -q` green (기존 fault_zoom 테스트 포함 무회귀)
- fault_zoom.py 에 dimensions/kismam import 없음
</verification>

<success_criteria>
- D-11 목표각 화살표 + 목표선이 backend PNG 합성으로 구현
- D-12 기존 줌 카드 재사용 — 앱 파일 변경 0
- legacy 호출 무회귀 픽셀 동일 테스트 고정
</success_criteria>

<output>
Create `.planning/phases/31-api-visual-correction/31-03-SUMMARY.md` when done
</output>
