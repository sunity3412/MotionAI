---
phase: 31-api-visual-correction
plan: 04
type: execute
wave: 2
depends_on: [31-01, 31-02]
files_modified:
  - backend/shared/python/sunity_shared/analysis/interfaces.py
  - backend/shared/python/sunity_shared/analysis/visual_gen.py
  - backend/tests/phase31/test_visual_gen.py
autonomous: true
requirements: [D-02, D-03, D-08]
must_haves:
  truths:
    - "생성 엔진이 Protocol 로 추상화되어 GEN3C 재평가(deferred) 시 어댑터 스왑만으로 교체 가능하다 (D-02 VisualGenEngine 류)"
    - "확정 엔진 = Wan2.7 계열 (D-02) — 회전 영상은 wan2.7-videoedit, 실루엣 이미지는 31-02 RESULTS.json 의 chosen_model"
    - "DashScope 키는 env(DASHSCOPE_API_KEY, SSM 주입) 로만 로드되고 로그·예외 메시지에 노출되지 않는다"
    - "모더레이션 차단은 재시도 1회 상한 후 실패 반환 — 무한 재시도 없음 (D-08 조용한 폴백의 전제)"
    - "24h 만료 벤더 URL 은 즉시 S3 이관 대상으로만 취급되고 Firestore 에 저장되지 않는다"
    - "Gemini judge 가 얼굴/배경 왜곡·자세 타당성을 판정하고, 판정 불가 시 보수적으로 FAIL 처리한다"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/interfaces.py"
      provides: "SilhouetteGenEngine / RotationGenEngine Protocol"
      contains: "class SilhouetteGenEngine"
    - path: "backend/shared/python/sunity_shared/analysis/visual_gen.py"
      provides: "Wan 어댑터 2종 + judge_silhouette + _http_json"
      contains: "wan2.7-videoedit"
      min_lines: 150
    - path: "backend/tests/phase31/test_visual_gen.py"
      provides: "HTTP mock 기반 어댑터·judge 단위 테스트"
      min_lines: 100
  key_links:
    - from: "backend/shared/python/sunity_shared/analysis/visual_gen.py"
      to: "dashscope-intl.aliyuncs.com"
      via: "stdlib urllib _http_json"
      pattern: "dashscope-intl"
    - from: "backend/shared/python/sunity_shared/analysis/visual_gen.py"
      to: "backend/shared/python/sunity_shared/gemini/client.py"
      via: "GeminiVisionCall 구조화 judge"
      pattern: "GeminiVisionCall"
---

<objective>
D-02 의 생성 엔진 계층을 구현한다: interfaces.py 에 Protocol 선언(선례: FrameExtractor/PoseEstimator/CoachWriter), visual_gen.py 에 Wan 어댑터 2종(실루엣 이미지 = 31-02 chosen_model, 회전 영상 = wan2.7-videoedit — spike 008 실증 파라미터) + Gemini 3.5-flash 품질 judge. 전부 stdlib urllib (신규 패키지 0). GEN3C 는 deferred — Protocol 경계가 스왑 지점이다.

Purpose: 31-06(실루엣 사후 훅)과 31-07(회전 워커)이 공유하는 유일한 외부 API 코드 경로 — 코드 1벌 원칙.
Output: Protocol + 어댑터 + judge + mock 테스트.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-api-visual-correction/31-PATTERNS.md
@.planning/phases/31-api-visual-correction/smoke/RESULTS.json
@.planning/spikes/004-gemini-omni-view-editing/wan_gate_batch.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Protocol 선언 + Wan 어댑터 2종 (visual_gen.py)</name>
  <files>backend/shared/python/sunity_shared/analysis/interfaces.py, backend/shared/python/sunity_shared/analysis/visual_gen.py, backend/tests/phase31/test_visual_gen.py</files>
  <read_first>backend/shared/python/sunity_shared/analysis/interfaces.py (전체 — Protocol 선언 + Fallback 구현체 + "구현은 형제 모듈" docstring 규칙), .planning/spikes/004-gemini-omni-view-editing/wan_gate_batch.py (http_json lines 24-56 + create body lines 87-128 + task 폴링/종료 상태/모더레이션 응답 형상 — 2026-07-17 실측), .planning/phases/31-api-visual-correction/smoke/RESULTS.json (chosen_model/endpoint/request_params — 이미지 어댑터의 실측 계약), backend/tests/phase31/conftest.py (dashscope_urlopen_mock)</read_first>
  <action>interfaces.py 에 Protocol 2종 추가 (docstring 에 D-02 근거 + "구현은 visual_gen.py" 명시): `SilhouetteGenEngine` — `generate(self, image_url: str, prompt: str) -> dict | None` (성공 시 {"url": str, "usage": dict}, 모더레이션/실패 시 None); `RotationGenEngine` — `create_task(self, video_url: str, prompt: str) -> str | None` (task_id) + `poll(self, task_id: str) -> dict` ({"status": 'SUCCEEDED'|'FAILED'|'PENDING'|'BLOCKED', "video_url": str | None}). visual_gen.py 신설: 모듈 docstring 에 D-02/D-08 + spike 008 인용. `_http_json(url, key, body=None, async_header=False)` — wan_gate_batch 형상 그대로 (Authorization Bearer, X-DashScope-Async: enable, timeout 60, 방어적 JSON 파싱). `BASE = "https://dashscope-intl.aliyuncs.com"`. `WanImageAdapter` — RESULTS.json 의 chosen_model/endpoint/request_params 를 모듈 상수로 반영 (sync=false 면 task 폴링 포함), 재시도 1회 상한. `WanVideoEditAdapter` — model 'wan2.7-videoedit', create endpoint `/api/v1/services/aigc/video-generation/video-synthesis`, parameters {"resolution": "720P", "watermark": False, "prompt_extend": False, "seed": 42} (spike 검증값 유지), poll endpoint `/api/v1/tasks/{task_id}`, 모더레이션 응답을 'BLOCKED' 로 매핑. 공통: 키는 인자 주입(호출측이 env DASHSCOPE_API_KEY 로드) — 키를 로그·예외 문자열에 절대 포함 금지. 반환 URL 은 호스트 화이트리스트(aliyuncs.com 계열 도메인) 검증 함수 `_is_allowed_output_url(url) -> bool` 제공. test_visual_gen.py 를 실 테스트로 채움 (importorskip 제거): dashscope_urlopen_mock 으로 create→task_id, 폴링 SUCCEEDED→video_url, 모더레이션→BLOCKED 매핑, 재시도 1회 상한(호출 횟수 카운트), 화이트리스트 거부, caplog 에 키 문자열 부재 assert.</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31/test_visual_gen.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - interfaces.py contains `class SilhouetteGenEngine(Protocol)` and `class RotationGenEngine(Protocol)`
    - visual_gen.py contains `wan2.7-videoedit` and `watermark` param False
    - 테스트가 재시도 1회 상한을 호출 횟수로 고정하고 exits 0
    - visual_gen.py 에 `requests` import 없음 (stdlib urllib only)
  </acceptance_criteria>
  <done>엔진 계층이 Protocol 경계 뒤에 mock 테스트로 고정됨.</done>
</task>

<task type="auto">
  <name>Task 2: Gemini 3.5-flash 실루엣 품질 judge</name>
  <files>backend/shared/python/sunity_shared/analysis/visual_gen.py, backend/tests/phase31/test_visual_gen.py</files>
  <read_first>backend/shared/python/sunity_shared/gemini/client.py (GeminiVisionCall line 137~ + _generate_with_retry line 319 + 실패 시 None 반환 계약 + Pydantic response_schema 사용법)</read_first>
  <action>visual_gen.py 에 `judge_silhouette(image_bytes: bytes, context: dict) -> bool` 추가 — GeminiVisionCall 재사용 (자체 CV 코드 금지, Don't Hand-Roll). 모델 string 'gemini-3.5-flash' (메모리 [[gemini-latest-model-versions]] — 2.5 금지). response_schema: {face_ok: bool, background_ok: bool, pose_plausible: bool, verdict: bool, reason: str}. 프롬프트 판정 축: (1) 얼굴/신원 붕괴·왜곡 없음, (2) 배경·폴 왜곡 없음, (3) context 로 전달된 교정 지시(관절·목표각)가 그럴듯하게 반영. 보수 규칙: GeminiVisionCall 이 None 반환(키 미설정·재시도 소진·파싱 실패)이거나 예외 → False (왜곡 이미지 노출은 신뢰 손상이 미노출보다 크다 — core value 분석 정확도=신뢰; 미노출은 D-08 조용한 폴백이 흡수). 사용자 노출 게이트와 페어 적재 게이트는 동일 verdict 사용 (RESEARCH A5 분리 여지는 31-06 에서 판단). test_visual_gen.py 에 추가: schema 파싱 성공→verdict 그대로, None→False, 예외→False.</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31/test_visual_gen.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - visual_gen.py contains `def judge_silhouette` and `gemini-3.5-flash`
    - judge 실패(None/예외) → False 가 테스트로 고정
    - `python -m pytest backend/tests/phase31 -q` exits 0
  </acceptance_criteria>
  <done>실루엣 품질 게이트가 HTTP-only(Pod 무관)로 동작하며 보수적 실패 규칙이 고정됨.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend → DashScope | 사용자 프레임/영상 presign URL 이 외부 벤더로 전송 |
| DashScope 응답 → backend | 외부 JSON/URL 을 신뢰 없이 파싱·검증 |
| backend → Gemini | 생성 이미지 판정 요청 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-09 | Information Disclosure | DASHSCOPE_API_KEY | mitigate | 키 인자 주입 + 로그/예외 문자열 포함 금지 (caplog 테스트 고정), SSM SecureString 만 |
| T-31-10 | Injection / SSRF | DashScope 응답 video_url | mitigate | `_is_allowed_output_url` 호스트 화이트리스트 통과 후에만 다운로드 (테스트 고정) |
| T-31-11 | Denial of Service (과금) | 모더레이션 재시도 | mitigate | 재시도 1회 상한 상수 — 호출 횟수 테스트 고정 (spike 실측: 결정적 차단은 재시도 무의미) |
| T-31-12 | Tampering | 왜곡 생성물 사용자 노출 | mitigate | judge 보수 규칙 (불확실 = FAIL) — D-08 조용한 폴백이 미노출 비용을 흡수 |
</threat_model>

<verification>
- `python -m pytest backend/tests/phase31 -q` green
- `python -m pytest backend/tests -q` green (무회귀)
- visual_gen.py 에 API 키 리터럴·`requests` import 없음
</verification>

<success_criteria>
- Protocol 추상화 + Wan 어댑터 2종 + judge 가 mock 테스트로 고정
- 31-06/31-07 이 이 모듈만 import 하면 되는 상태
</success_criteria>

<output>
Create `.planning/phases/31-api-visual-correction/31-04-SUMMARY.md` when done
</output>
