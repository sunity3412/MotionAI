---
phase: 31-api-visual-correction
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/types/analysis.ts
  - app/src/lib/userAnalyses.ts
  - docs/contract.md
autonomous: true
requirements: [D-05, D-06, D-07, D-08, D-10]
must_haves:
  truths:
    - "correctedPose*/rotation* 계약이 3면(analysis.ts / models.py / contract.md)에 동일하게 선언된다 — URL 필드 없음, canonical key + 전용 timestamp 만 (리뷰 H-02/H-06)"
    - "앱 normalize 가 신규 필드를 drop 하지 않고 부재(legacy) 시 undefined 로 조용히 통과한다 (D-08 하위호환)"
    - "POST /visual/rotation 과 playback-url asset 확장 계약이 contract.md 에 단일 기준으로 존재한다 — daily_limit(KST 자정 리셋 명시, 리뷰 M-06)·feature_disabled 포함"
    - "FaultZoomComparison 에 userFrameIdx/refFrameIdx/refMatched optional 필드가 선언된다 (amended D-10 뷰어의 프레임 소스)"
  artifacts:
    - path: "app/src/types/analysis.ts"
      provides: "correctedPoseStatus/rotationStatus 등 optional 계약 필드"
      contains: "correctedPoseStatus"
    - path: "docs/contract.md"
      provides: "/visual/rotation + playback-url asset 확장 + visual 필드 절"
      contains: "daily_limit"
  key_links:
    - from: "app/src/lib/userAnalyses.ts"
      to: "app/src/types/analysis.ts"
      via: "normalize 방어 파싱"
      pattern: "correctedPoseStatus"
---

<objective>
계약 3면의 TS/문서 측을 확정한다 (Python 측 상수는 31-02 가 동시 확정 — 필드명은 본 플랜과 31-02 objective 에 동일하게 박제된 아래 명세가 단일 기준):

result 내 optional 필드 (전부 flat scalar, 부재 = legacy 숨김):
- correctedPoseStatus?: 'pending'|'done'|'failed' / correctedPoseKey?: string / correctedPoseJoint?: string / correctedPoseUpdatedAtMs?: number
- rotationStatus?: 'pending'|'done'|'failed' / rotationVideoKey?: string / rotationUpdatedAtMs?: number
- FaultZoomComparison 에 userFrameIdx?: number / refFrameIdx?: number / refMatched?: boolean

URL 필드는 계약에 존재하지 않는다 (리뷰 H-02 — 표시 URL 은 playback-url asset 재서명으로만). 표시 상태의 pending 타임아웃 기준은 전용 *UpdatedAtMs (리뷰 H-06 — 공용 updatedAt 아님). 명칭은 리뷰 L-02 에 따라 correctedPose (실루엣 아님 — 사용자 카피도 "교정된 자세").

Purpose: backend 방출(31-09/31-10)과 앱 소비(31-08/31-11)가 서로를 기다리지 않고 병렬 진행 가능한 단일 기준.
Output: analysis.ts + userAnalyses.ts normalize + contract.md 절.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-api-visual-correction/31-CONTEXT.md
@.planning/phases/31-api-visual-correction/31-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: analysis.ts 필드 + userAnalyses.ts normalize</name>
  <files>app/src/types/analysis.ts, app/src/lib/userAnalyses.ts</files>
  <read_first>app/src/types/analysis.ts lines 439-473 (FaultZoomComparison — optional 필드 추가 지점 + JSDoc 관례) + lines 563-572 (faultZoomStatus 주석 형식 — 값 의미/부재=legacy/Python lockstep 명시) + lines 630-636 (joints3d 트리플릿 — 뷰어 소스 확인), app/src/lib/userAnalyses.ts (normalize() 전체 — 방어 파싱 위치와 typeof 검증 관례)</read_first>
  <action>analysis.ts: objective 명세의 correctedPose*/rotation* 7필드를 faultZoomStatus 주석 형식으로 추가 — 각 필드에 'pending'=자리표시/'done'=카드 표시/'failed'·부재(legacy)=숨김(D-08) 의미, "URL 은 저장하지 않음 — playback-url asset 재서명으로 발급 (리뷰 H-02)", "타임아웃/dedupe 기준은 이 UpdatedAtMs (공용 updatedAt 아님 — 리뷰 H-06)", Python lockstep(models.py VISUAL_STATUSES + firestore_admin.update_analysis_visual + contract.md 절) 주석. FaultZoomComparison 에 userFrameIdx?/refFrameIdx?/refMatched? 추가 (JSDoc: DTW 대응 프레임 쌍 — keypointReport 프레임 공간, 2D 비교 뷰어 소스, 31-03 방출). userAnalyses.ts normalize(): status 2종은 3값 literal 화이트리스트만 통과(그 외 undefined), key/joint 는 typeof string, *UpdatedAtMs 는 typeof number 유한값, comparison 의 idx 2종은 Number.isInteger && >= 0, refMatched 는 typeof boolean — 부재 시 undefined 유지 (필드 drop 금지). key 값 방어: correctedPoseKey/rotationVideoKey 가 'results/' 로 시작하지 않으면 undefined (오염 데이터 조용 강등).</action>
  <verify>
    <automated>cd app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - analysis.ts 에 correctedPoseStatus?: 'pending' | 'done' | 'failed' 와 rotationStatus?: 'pending' | 'done' | 'failed' 존재, *Url 신규 필드 0
    - FaultZoomComparison 에 userFrameIdx/refFrameIdx/refMatched optional 선언
    - normalize 가 correctedPoseKey 비-'results/' prefix 를 undefined 로 강등 (코드 분기 존재)
    - `cd app && npm run typecheck` exits 0
  </acceptance_criteria>
  <done>앱 소비 계약이 방어 파싱까지 완결.</done>
</task>

<task type="auto">
  <name>Task 2: contract.md — 필드 절 + 엔드포인트 2절</name>
  <files>docs/contract.md</files>
  <read_first>docs/contract.md (faultZoomStatus 절 — 신규 절 형식 선례 + 엔드포인트 절 형식), backend/functions/playback-url/app.py lines 97-136 (기존 요청/응답 형상 — asset 확장의 베이스)</read_first>
  <action>3개 절 신설/확장 (각 절에 3면 lockstep 명시):
(1) "visual 교정 시각물 필드" 절 — objective 의 7필드 + FaultZoomComparison 확장 3필드 정의, 상태 3값 + 부재=legacy, URL 비저장 원칙(리뷰 H-02)과 전용 timestamp 의미(리뷰 H-06), visualJobs 백엔드 전용 컬렉션과 quotaDateKey 존재를 내부 구현 노트로 기술 (앱 비노출).
(2) "POST /visual/rotation" 절 — request {analysisId: string}, Firebase ID token 필수. 응답: 202 {"rotationStatus":"pending"} (신규/재시도 수락) / 200 {"rotationStatus":"done"} (이미 완료 — URL 미포함, 표시 URL 은 playback-url asset 으로) / 401 unauthorized / 400 bad_request / 404 not_found (존재·소유·상태 가드 합산 단일 응답 — leak 0) / 429 code 'daily_limit' (**한도 명세 명시: 사용자당 3건/일 + 전역 30건/일, 일 경계 = KST(UTC+9) 자정 리셋 — 파일럿 사용자 전원 한국, 리뷰 M-06 명시화**) / 503 code 'feature_disabled' (VisualJobsEnabled flag OFF — 앱은 조용히 버튼 비활성, D-08).
(3) "POST /playback-url asset 확장" 절 — 기존 body 에 optional `asset?: 'correctedPose' | 'rotation'` 추가. asset 지정 시 서버가 분석 문서(token uid 스코프)에서 correctedPoseKey/rotationVideoKey 를 읽어 `results/{uid}/{analysisId}/` prefix 정확 검증 후 1시간 presign 발급 — 클라이언트 임의 key 수신 없음 (리뷰 H-02: server-selected asset type). 응답 {playbackUrl, expiresInSec: 3600}. 필드 부재/상태 미완료 = 404 단일 응답. asset 미지정 = 기존 동작 100% 보존.</action>
  <verify>
    <automated>grep -q "daily_limit" docs/contract.md && grep -q "feature_disabled" docs/contract.md && grep -q "correctedPose" docs/contract.md</automated>
  </verify>
  <acceptance_criteria>
    - contract.md 에 /visual/rotation 절 (202/200/401/400/404/429/503 전부) 존재
    - KST 자정 리셋 문구가 daily_limit 정의에 포함 (리뷰 M-06)
    - playback-url asset 확장 절에 "클라이언트 임의 key 없음 + 1시간 presign" 명시
    - visual 필드 절에 URL 비저장 원칙 명시
  </acceptance_criteria>
  <done>31-10(backend)·31-11(app) 이 이 절만 보고 구현 가능한 단일 기준 완성.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firestore doc → 앱 normalize | backend 방출 필드를 앱이 신뢰 없이 방어 파싱 |
| 계약 3면 경계 | 불일치 시 필드 drop·오파싱 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-12 | Tampering | normalize | mitigate | status literal 화이트리스트 + key prefix 검증 + typeof 검증 — 임의 값 undefined 강등 |
| T-31-13 | Information Disclosure | /visual/rotation 계약 | mitigate | 존재·소유·상태 가드 단일 404 명시 — 문서 단계에서 leak 차단 (playback-url guards_ok 선례) |
| T-31-14 | Information Disclosure | asset 재서명 계약 | mitigate | server-selected key 만 — 클라이언트 key 수신 자체가 계약에 없음 (리뷰 H-02/H-05) |
</threat_model>

<verification>
- `cd app && npm run typecheck` green
- grep 으로 correctedPoseStatus 가 analysis.ts / userAnalyses.ts / contract.md 3곳 모두 존재
</verification>

<success_criteria>
- 계약 3면 동기 (Python 측은 31-02) — URL 비저장·전용 timestamp·KST 한도까지 문서화
- 신규 엔드포인트 2절이 단일 기준으로 존재
</success_criteria>

<output>
Create `.planning/phases/31-api-visual-correction/31-04-SUMMARY.md` when done
</output>
