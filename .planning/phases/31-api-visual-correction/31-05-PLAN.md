---
phase: 31-api-visual-correction
plan: 05
type: execute
wave: 2
depends_on: [31-01, 31-02]
files_modified:
  - backend/shared/python/sunity_shared/analysis/interfaces.py
  - backend/shared/python/sunity_shared/analysis/visual_gen.py
  - backend/tests/phase31/test_visual_gen.py
autonomous: true
requirements: [D-02, D-03, D-04, D-08]
must_haves:
  truths:
    - "생성 엔진이 Protocol + typed 반환 모델로 추상화된다 — dict|None 금지, typed 오류 카테고리(moderation/timeout/vendor_error/invalid_output) (D-02 + 리뷰 M-08)"
    - "회전은 wan2.7-videoedit (amended D-04 — 회전의 유일 수단), 이미지는 31-01 RESULTS.json 의 chosen_model"
    - "벤더 다운로드는 HTTPS·정확 호스트 경계·redirect 거부·사설 IP 차단·content-type 허용목록·바이트 상한·타임아웃이 전부 테스트로 고정된다 (리뷰 H-05)"
    - "품질 judge 는 before/after 두 이미지를 함께 받아 typed 축으로 판정한다 — 생성 이미지 단독 판정 금지 (리뷰 H-03)"
    - "키는 로그·예외에 노출되지 않고, 모더레이션 재시도는 1회 상한"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/visual_gen.py"
      provides: "typed 결과 모델 + Wan 어댑터 2종 + download_vendor_asset + judge_corrected_pose"
      contains: "download_vendor_asset"
      min_lines: 200
    - path: "backend/shared/python/sunity_shared/analysis/interfaces.py"
      provides: "ImageEditEngine / VideoEditEngine Protocol"
      contains: "class ImageEditEngine"
    - path: "backend/tests/phase31/test_visual_gen.py"
      provides: "어댑터·다운로드 경계·judge 테스트"
      min_lines: 150
  key_links:
    - from: "backend/shared/python/sunity_shared/analysis/visual_gen.py"
      to: "dashscope-intl.aliyuncs.com"
      via: "stdlib urllib _http_json"
      pattern: "dashscope-intl"
    - from: "backend/shared/python/sunity_shared/analysis/visual_gen.py"
      to: "backend/shared/python/sunity_shared/gemini/client.py"
      via: "GeminiVisionCall before/after judge"
      pattern: "GeminiVisionCall"
---

<objective>
외부 생성 API 계층을 구현한다: typed 결과 모델(리뷰 M-08), Protocol 2종(D-02 스왑 지점 — GEN3C deferred), Wan 어댑터 2종(이미지 = 31-01 chosen_model, 영상 = wan2.7-videoedit spike 008 실증 파라미터), H-05 전면 강화 다운로드 경계, H-03 before/after judge. 전부 stdlib urllib (신규 패키지 0 — RESEARCH Package Legitimacy Audit 유지).

Purpose: 31-09 워커가 공유하는 유일한 외부 API 코드 경로 — 코드 1벌 + 테스트 고정된 보안 경계.
Output: interfaces.py Protocol + visual_gen.py + mock 테스트.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-api-visual-correction/31-PATTERNS.md
@.planning/phases/31-api-visual-correction/31-ACCEPTANCE.md
@.planning/phases/31-api-visual-correction/smoke/RESULTS.json
@.planning/spikes/004-gemini-omni-view-editing/wan_gate_batch.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: typed 결과 모델 + Protocol + Wan 어댑터 2종</name>
  <files>backend/shared/python/sunity_shared/analysis/interfaces.py, backend/shared/python/sunity_shared/analysis/visual_gen.py, backend/tests/phase31/test_visual_gen.py</files>
  <read_first>backend/shared/python/sunity_shared/analysis/interfaces.py (전체 — Protocol 선언 + "구현은 형제 모듈" docstring 규칙), .planning/spikes/004-gemini-omni-view-editing/wan_gate_batch.py (http_json lines 24-56 + create body lines 87-128 + 폴링/종료 상태/모더레이션 응답 형상), .planning/phases/31-api-visual-correction/smoke/RESULTS.json (chosen_model/endpoint/request_params), backend/shared/python/sunity_shared/models.py (VISUAL_FAILURE_REASONS — 오류 카테고리 단일 출처), backend/tests/phase31/conftest.py (dashscope_urlopen_mock)</read_first>
  <action>visual_gen.py 신설 (docstring: D-02/D-08 + spike 008 + 리뷰 M-08/H-05 인용). typed 모델 (frozen dataclass — 리뷰 M-08 "dict|None 금지"): `VendorTaskCreated(task_id: str, request_id: str | None)`, `VendorPollResult(state: str, output_url: str | None, failure_reason: str | None)` — state 는 'pending'|'succeeded'|'failed'|'blocked' literal, failure_reason 은 models.VISUAL_FAILURE_REASONS 값만, `VendorImageResult(output_url: str | None, failure_reason: str | None, usage: dict | None)`. interfaces.py 에 Protocol 2종 (docstring: D-02 + "구현은 visual_gen.py"): `ImageEditEngine.generate(self, image_url: str, prompt: str) -> VendorImageResult`; `VideoEditEngine.create_task(self, video_url: str, prompt: str) -> VendorTaskCreated | VendorPollResult` (생성 즉시 실패 시 poll 결과형으로 typed 실패 반환) + `VideoEditEngine.poll(self, task_id: str) -> VendorPollResult`. `_http_json(url, key, body=None, async_header=False)` — wan_gate_batch 형상 (Authorization Bearer, X-DashScope-Async: enable, timeout 60, 방어적 JSON 파싱; 예외 문자열에 키 미포함). BASE = "https://dashscope-intl.aliyuncs.com". `WanImageAdapter` — RESULTS.json 의 chosen_model/endpoint/request_params 반영 (sync=false 면 내부 폴링 포함, 모더레이션 → failure_reason 'moderation'); RESULTS.json blocked=true 인 경우에도 어댑터는 구현하되 모듈 상수 `IMAGE_ENGINE_BLOCKED = True` 방출 (31-12 게이트 소비). `WanVideoEditAdapter` — model 'wan2.7-videoedit', create endpoint /api/v1/services/aigc/video-generation/video-synthesis, parameters resolution 720P / watermark False / prompt_extend False / seed 42 (spike 검증값), poll endpoint /api/v1/tasks/{task_id}, 모더레이션 → state 'blocked'. 재시도 1회 상한 상수 `MODERATION_RETRY_MAX = 1` 은 호출측(워커) 소비용으로 방출. 키는 인자 주입 (호출측 env DASHSCOPE_API_KEY 로드). test_visual_gen.py: create→task_id, SUCCEEDED→output_url, 모더레이션→'blocked'/'moderation' typed 매핑, 예외/비정상 JSON→'vendor_error', caplog 에 키 문자열 부재, `requests` import 부재 (모듈 소스 assert).</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31/test_visual_gen.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - interfaces.py 에 class ImageEditEngine(Protocol) / class VideoEditEngine(Protocol) 존재
    - visual_gen.py 반환형이 전부 frozen dataclass (dict 반환 0), failure_reason 값이 models.VISUAL_FAILURE_REASONS 부분집합임을 테스트 고정
    - wan2.7-videoedit + watermark False 존재, requests import 0
    - `python -m pytest backend/tests/phase31/test_visual_gen.py -x -q` exits 0
  </acceptance_criteria>
  <done>엔진 계층이 typed 계약 + mock 테스트로 고정됨.</done>
</task>

<task type="auto">
  <name>Task 2: download_vendor_asset — H-05 전면 강화 다운로드 경계</name>
  <files>backend/shared/python/sunity_shared/analysis/visual_gen.py, backend/tests/phase31/test_visual_gen.py</files>
  <read_first>.planning/phases/31-api-visual-correction/31-REVIEWS.md 의 H-05 절 (요구 목록 원문 — 테스트 케이스의 단일 기준)</read_first>
  <action>`download_vendor_asset(url: str, *, max_bytes: int, allowed_content_types: tuple[str, ...], timeout_s: int = 60) -> bytes` 신설 — 실패는 typed 예외 `VendorDownloadError(reason)` (reason 은 'bad_scheme'|'bad_host'|'redirect'|'private_ip'|'bad_content_type'|'too_large'|'timeout'). 규칙 (리뷰 H-05 전항목): (1) urlparse scheme == 'https' 강제; (2) 호스트 경계 — `_ALLOWED_HOST_SUFFIXES = ("aliyuncs.com",)` 에 대해 hostname == suffix 또는 hostname.endswith("." + suffix) 정확 매칭 (단순 substring/endswith("aliyuncs.com") 금지 — evilaliyuncs.com 차단); (3) socket.getaddrinfo 로 해석된 모든 IP 가 ipaddress 모듈 기준 사설/링크로컬/루프백/예약 대역이면 거부; (4) redirect 전면 거부 — HTTPRedirectHandler 를 제거한 opener 로 3xx 응답을 VendorDownloadError('redirect') 처리; (5) Content-Type 헤더가 allowed_content_types 접두 매칭 아니면 거부; (6) 스트리밍 read(64KB 청크) 로 max_bytes 초과 즉시 중단; (7) timeout 강제; (8) URL 의 query/서명을 로그에 남기지 않음. 기존 화이트리스트 함수가 있으면 이 함수로 대체·일원화. test_visual_gen.py 추가 (dashscope_urlopen_mock 확장): https 아님 거부, evilaliyuncs.com 거부, sub.aliyuncs.com 허용, 302 redirect 거부, 사설 IP(127.0.0.1/10.x 해석 mock) 거부, content-type text/html 거부, max_bytes 초과 중단, 정상 다운로드 bytes 반환.</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31/test_visual_gen.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - download_vendor_asset 존재 + VendorDownloadError typed reason 7종
    - evilaliyuncs.com / redirect / oversized / wrong content-type / private IP 5케이스 테스트 green
    - 로그 포맷 문자열에 URL query 미포함 (호스트만 로깅)
  </acceptance_criteria>
  <done>벤더 다운로드 경계가 리뷰 H-05 전 항목 테스트로 고정됨.</done>
</task>

<task type="auto">
  <name>Task 3: judge_corrected_pose — before/after typed judge (리뷰 H-03)</name>
  <files>backend/shared/python/sunity_shared/analysis/visual_gen.py, backend/tests/phase31/test_visual_gen.py</files>
  <read_first>backend/shared/python/sunity_shared/gemini/client.py (GeminiVisionCall line 137~ + _generate_with_retry + 실패 시 None 계약 + Pydantic response_schema — 다중 이미지 입력 지원 형태 확인), .planning/phases/31-api-visual-correction/31-ACCEPTANCE.md (Display/Training 게이트 축·임계값 — 단일 기준)</read_first>
  <action>`@dataclass(frozen=True) class JudgeVerdict` — identity_ok/clothing_ok/background_ok/pole_ok/single_person_ok/no_extra_limbs/correction_visible (bool 7축, 31-ACCEPTANCE 축과 동일 명명) + confidence(float) + reason(str) + `display_pass` / `training_pass` property (ACCEPTANCE 임계값 반영: display = 7축 전부 true AND confidence >= 0.7, training = display AND confidence >= 0.85 — pose 각도 게이트는 31-06 pose_gate 소관, judge 는 시각 보존축만). `judge_corrected_pose(before_png: bytes, after_png: bytes, context: dict) -> JudgeVerdict | None` — GeminiVisionCall 재사용 (자체 CV 금지), 모델 'gemini-3.5-flash' ([[gemini-latest-model-versions]] — 2.5 금지), **두 이미지를 순서 라벨(BEFORE=원본, AFTER=생성)과 함께 전달** — 원본 없이 보존 판정 불가 (리뷰 H-03 핵심). 프롬프트: BEFORE 대비 AFTER 의 신원/복장/배경/폴 보존 + 단일 인물 + 사지 수 정상 + context 의 교정(관절·목표각) 가시 반영을 각 축 bool 로. Hard rejection (ACCEPTANCE §Hard Rejection): 인물 미검출/복수 인물/사지 이상 축은 다른 축과 무관하게 display_pass False. GeminiVisionCall None(키 미설정·재시도 소진·파싱 실패)/예외 → None 반환 — 호출측(워커)은 None 을 'judge_failed' typed 실패로 처리 (보수 규칙: 불확실 = 미노출, D-08 이 흡수). 테스트: schema 파싱 → 축별 property 판정 (경계값: confidence 0.69 display False / 0.7 True / 0.84 training False), hard rejection 축 우선, None/예외 → None.</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31 -x -q</automated>
  </verify>
  <acceptance_criteria>
    - judge_corrected_pose 시그니처가 before_png 와 after_png 둘 다 필수
    - JudgeVerdict.display_pass / training_pass 가 서로 다른 임계값 (테스트 경계값 고정)
    - None/예외 → None (판정 불능은 통과가 아님) 테스트 green
    - `python -m pytest backend/tests -q` exits 0
  </acceptance_criteria>
  <done>보존 판정이 원본 대조 + typed 축 + display/training 분리로 고정됨.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend → DashScope | presign URL 전송 + 응답 신뢰 불가 |
| DashScope 응답 URL → backend | SSRF/오버사이즈/오호스트 위험 지점 |
| backend → Gemini | 두 이미지 판정 요청 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-15 | Information Disclosure | DASHSCOPE_API_KEY | mitigate | 키 인자 주입 + 로그/예외 문자열 미포함 (caplog 테스트) + SSM SecureString 만 |
| T-31-16 | Injection / SSRF | 벤더 응답 URL | mitigate | download_vendor_asset — https/정확 호스트/redirect 거부/사설 IP 차단 전부 테스트 고정 (리뷰 H-05) |
| T-31-17 | Denial of Service | 오버사이즈 응답 | mitigate | 스트리밍 바이트 상한 + timeout (리뷰 H-05 — 512MB Lambda 메모리 보호) |
| T-31-18 | Tampering | 왜곡 생성물 통과 | mitigate | before/after 대조 judge + hard rejection + 불확실=None (리뷰 H-03) |
| T-31-19 | Denial of Service (과금) | 모더레이션 재시도 | mitigate | MODERATION_RETRY_MAX=1 상수 (spike 실측: 결정적 차단 재시도 무의미) |
</threat_model>

<verification>
- `python -m pytest backend/tests -q` green
- visual_gen.py 에 API 키 리터럴·requests import 0
</verification>

<success_criteria>
- typed 엔진 계층 + H-05 다운로드 경계 + H-03 before/after judge 가 전부 mock 테스트로 고정
- 31-09 워커가 이 모듈만 import 하면 되는 상태
</success_criteria>

<output>
Create `.planning/phases/31-api-visual-correction/31-05-SUMMARY.md` when done
</output>
