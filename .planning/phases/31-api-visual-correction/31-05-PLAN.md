---
phase: 31-api-visual-correction
plan: 05
type: execute
wave: 2
depends_on: [31-01]
files_modified:
  - app/src/lib/referenceMotions.ts
  - app/src/components/PoseViewer3D.tsx
  - app/src/components/ReferenceCornerSection.tsx
autonomous: false
requirements: [D-04, D-09, D-10]
must_haves:
  truths:
    - "R3F 3D 뷰어가 내 자세(실측 스켈레톤) + 목표 자세 반투명 중첩을 손가락 드래그 회전으로 보여준다 (D-10)"
    - "회전 참고코너 기본 = 수학 R3F 뷰어 (환각 0·비용 0·즉시) + Wan 합성 영상은 옵션 카드 (D-04 하이브리드)"
    - "참고코너 섹션은 채점과 시각적으로 분리된 '참고하세요' 카드로 구성되고 점수 수치를 표시하지 않는다 (D-09 + 점수 비반영 invariant)"
    - "UI 구현 전 최악 데이터 케이스 목업이 belle 에게 선제시된다 ([[ux-propose-user-centric-screens-first]])"
    - "참고코너 어디에도 다각도 촬영 유도 카피가 없다 (단일 카메라 UX invariant)"
  artifacts:
    - path: "app/src/components/ReferenceCornerSection.tsx"
      provides: "참고코너 섹션 (실루엣 카드 + 3D 뷰어 + 회전 영상 카드/버튼)"
      min_lines: 120
    - path: "app/src/components/PoseViewer3D.tsx"
      provides: "referenceJoints 활성화 + 반투명 중첩 SkeletonMesh"
      contains: "referenceJoints"
    - path: "app/src/lib/referenceMotions.ts"
      provides: "reference 문서 joints3d 방어적 normalize"
      contains: "joints3d"
  key_links:
    - from: "app/src/components/ReferenceCornerSection.tsx"
      to: "app/src/components/PoseViewer3D.tsx"
      via: "referenceJoints prop"
      pattern: "PoseViewer3D"
    - from: "app/src/lib/referenceMotions.ts"
      to: "Firestore reference/{id} joints3d"
      via: "flat + jointKeys + frames reshape"
      pattern: "joints3d"
---

<objective>
D-04/D-10 의 R3F 하이브리드 뷰어와 D-09 참고코너 섹션 컴포넌트를 구현한다. PoseViewer3D 는 이미 배포된 컴포넌트 — 예약석 referenceJoints prop 활성화 + 반투명 중첩만 남았다. referenceMotions.ts 가 reference 문서의 joints3d(flat + 메타)를 방어적으로 normalize 해 흘리고, 신규 ReferenceCornerSection 이 실루엣 카드·3D 뷰어·회전 영상 카드/버튼을 "참고하세요" 로 묶는다. UI 작업 전 최악 케이스 목업 선제시(checkpoint) — RESEARCH Open Q3(실루엣 카드 위치)도 이때 확정.

Purpose: 온디맨드 회전 영상 대기 중에도 즉시 쓸 수 있는 비용 0 대체재 + 참고코너의 시각적 뼈대.
Output: 컴포넌트 2종 수정/신설 + normalize 확장. result.tsx 통합은 31-08.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-api-visual-correction/31-CONTEXT.md
@.planning/phases/31-api-visual-correction/31-PATTERNS.md
@.planning/spikes/005-frontend-3d-viewer/README.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: 참고코너 최악 케이스 목업 선제시 + 실루엣 카드 위치 확정</name>
  <decision>참고코너 레이아웃과 실루엣 카드 소속 (RESEARCH Open Q3)</decision>
  <context>[[ux-propose-user-centric-screens-first]] — UI 변경은 최악 데이터 케이스 목업을 먼저 belle 에게 보여야 한다. 제시할 최악 케이스: (1) 실루엣 failed → 카드 자체 숨김(참고코너에 뷰어만 남음), (2) mode3 + recognizedMotionId 부재 → 뷰어가 내 자세 단독으로 강등(중첩 없음), (3) rotationStatus pending 6~7분 대기 카드, (4) joints3d 부재 reference → 뷰어 안내 문구. 각 케이스의 화면 구성을 텍스트 목업/구성도로 제시. 카피는 전부 한국어, "다른 각도로 찍어보세요" 류 다각도 유도 문구 금지, 점수 수치 미표시 (D-09 채점 분리).</context>
  <options>
    <option id="option-a">
      <name>실루엣 카드를 참고코너 소속으로 (권장 — RESEARCH Open Q3 권고)</name>
      <pros>비채점 시각물 일관 배치 (D-09 원칙이 레이아웃으로 드러남), 섹션 1곳 신설로 끝</pros>
      <cons>결함 줌 캐러셀과 실루엣이 화면상 떨어짐</cons>
    </option>
    <option id="option-b">
      <name>실루엣 카드를 결함 줌 캐러셀 옆에</name>
      <pros>교정물이 결함 지적 바로 옆 — 동선 자연스러움</pros>
      <cons>채점 영역(감점 내역 인접)에 비채점 생성물이 섞임 — 점수 비반영 원칙 흐려짐</cons>
    </option>
  </options>
  <resume-signal>option-a 또는 option-b 선택 + 목업 승인 (레이아웃 수정 요청 시 반영 후 재제시)</resume-signal>
  <action>최악 데이터 케이스 4종(실루엣 failed 숨김 / mode3 단독 강등 / rotation pending 대기 / joints3d 부재 안내)의 텍스트 목업을 구성해 AskUserQuestion 으로 belle 에게 선제시하고, 실루엣 카드 위치(option-a/b)를 확정받는다. 승인 전 Task 2/3 착수 금지.</action>
  <verify>
    <automated>N/A - checkpoint:decision (belle 응답이 게이트)</automated>
  </verify>
  <acceptance_criteria>
    - belle 이 option-a 또는 option-b 를 명시적으로 선택
    - 최악 케이스 4종 목업이 제시되었음이 대화 기록으로 확인 가능
  </acceptance_criteria>
  <done>레이아웃·실루엣 카드 소속이 belle 승인으로 확정되어 Task 2/3 의 구현 기준이 됨.</done>
</task>

<task type="auto">
  <name>Task 2: referenceMotions.ts joints3d normalize + PoseViewer3D 반투명 중첩</name>
  <files>app/src/lib/referenceMotions.ts, app/src/components/PoseViewer3D.tsx</files>
  <read_first>app/src/lib/referenceMotions.ts (전체 — referenceKeypointReport null-guard idiom lines 103-130, deriveMeanAngles flat reshape lines 42-68, line 137 의 40k index-entry 주석), app/src/components/PoseViewer3D.tsx (전체 — 예약 prop lines 356-373, SkeletonMeshProps line 182, 빈 상태 패턴 lines 379-401, line 77 의 RTMW y≈0 축 재매핑 주석)</read_first>
  <action>referenceMotions.ts: reference 문서의 joints3d(flat number[]) + jointKeys + frames 메타를 referenceKeypointReport idiom 그대로 방어 파싱 — 길이가 frames×jointKeys.length×3 과 불일치하면 undefined (조용한 강등), reference 문서에 신규 필드 쓰기 금지 (40k index-entry 한도 — 읽기 전용). 노출 형태는 PoseViewer3D 의 joints prop 계약과 동일한 (T, J, 3) reshape 결과 또는 기존 reshapePose3dData 재사용 — PoseViewer3D 소비 형상에 맞춘다. PoseViewer3D.tsx: `referenceJoints: _referenceJoints` 의도적 미사용 destructure 를 실사용으로 전환 — SkeletonMesh 2벌 렌더: 내 자세 = 기존 스타일 그대로, 목표 자세 = 반투명(material opacity 0.3~0.4, transparent, depthWrite false 로 z-fighting 회피) + theme 보조색 (하드코딩 금지 — src/theme 토큰만, 브랜드 #FF4B33 은 내 자세 유지). line 77 주석의 축 재매핑(수직 분산 = Z 축)을 reference 데이터에도 동일 적용. referenceJoints null/undefined 시 기존 단독 렌더와 diff 0 (무회귀 — "미전달 시 렌더 diff 0" 관례). 빈 상태는 기존 안내 문구 패턴 유지 (빈 회색 박스 금지 — belle 디바이스 finding #3).</action>
  <verify>
    <automated>cd app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - PoseViewer3D.tsx 에서 `_referenceJoints` 미사용 이름이 사라지고 referenceJoints 가 렌더 경로에 사용됨
    - 중첩 메쉬가 transparent + opacity 값(0.3~0.4)으로 선언됨 (grep: `opacity`)
    - referenceMotions.ts 가 joints3d 길이 불일치 시 undefined 반환 (주석으로 조용한 강등 명시)
    - `cd app && npm run typecheck` exits 0
  </acceptance_criteria>
  <done>뷰어가 내 자세 + 목표 반투명 중첩을 지원하고, 데이터 부재 시 조용히 강등된다.</done>
</task>

<task type="auto">
  <name>Task 3: ReferenceCornerSection.tsx 신설 (실루엣 카드 + 뷰어 + 회전 영상 카드/버튼)</name>
  <files>app/src/components/ReferenceCornerSection.tsx</files>
  <read_first>app/src/components/ScoreBreakdownSection.tsx lines 1-75 (섹션 컴포넌트 표준형 — named export + inline prop 타입 + 헤더 주석 + StyleSheet 하단 + theme 토큰), app/src/types/analysis.ts (31-01 이 추가한 silhouette*/rotation* 필드), app/src/components/PoseViewer3D.tsx (props 계약)</read_first>
  <action>ScoreBreakdownSection 표준형으로 신설. 헤더 주석에 D-04/D-08/D-09/D-10 + 단일 카메라 invariant 인용. Props(inline 타입): silhouetteStatus/silhouetteImageUrl/silhouetteJoint, rotationStatus/rotationVideoUrl, myJoints(뷰어용), referenceJoints(옵션 — 부재 시 단독 뷰어 강등), onRequestRotation(옵션 콜백), rotationRequestBusy, limitNotice(옵션 — 429 시 한국어 안내). 렌더 구성 (Task 1 승인 목업 기준): (1) 섹션 타이틀 "참고하세요" + 서브카피 "점수에는 반영되지 않아요" (채점 분리 명시 — D-09), (2) 교정 실루엣 카드 — 'done'+imageUrl 만 표시(expo-image/Image), 'pending' 은 자리표시, 'failed'/부재 는 렌더 자체 생략 (에러 배너·토스트 절대 금지 — D-08), (3) PoseViewer3D 중첩 뷰어 + 짧은 안내 "드래그해서 돌려보세요", (4) 회전 참고 영상 카드 — 'done' 이면 expo-video 재생, 'pending' 이면 "생성 중이에요 (몇 분 걸려요)" 자리표시, 부재/failed 면 [회전 영상 만들기] 버튼(onRequestRotation, busy 시 비활성, accessibilityRole/Label/hitSlop 관례). 점수 수치·다각도 촬영 유도 문구 0. 전부 theme 토큰 (colors/spacing/radius/typography), 이모지 금지, 라이트 전용.</action>
  <verify>
    <automated>cd app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - ReferenceCornerSection.tsx 가 named export 로 존재
    - 'failed' 상태에서 실루엣/회전 카드가 렌더되지 않음이 코드 분기로 확인 가능 (에러 배너 문자열 0)
    - grep 으로 하드코딩 hex 색상 0 (#FF4B33 포함 — theme 토큰만)
    - grep 으로 "각도로 찍" 류 다각도 유도 문구 0
    - `cd app && npm run typecheck` exits 0
  </acceptance_criteria>
  <done>참고코너 섹션이 상태 3값 + 부재를 조용히 처리하는 완결 컴포넌트로 존재.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firestore 필드 → 섹션 렌더 | presigned URL/상태 값을 방어적으로 소비 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-13 | Information Disclosure | 실루엣/회전 URL | accept | presigned 7일 URL 은 기존 zoom/영상과 동일 경계 (results/{uid}/ 하위) — 신규 노출면 없음 |
| T-31-14 | Repudiation (신뢰 인상) | 실패 상태 노출 | mitigate | failed = 카드 미렌더 (D-08) — 에러 배너/토스트 금지를 acceptance 로 고정 |
| T-31-15 | Tampering | 점수 비반영 invariant | mitigate | 섹션에 점수 수치 미표시 + "점수에는 반영되지 않아요" 명시 카피 |
</threat_model>

<verification>
- `cd app && npm run typecheck` green
- 하드코딩 색상·다각도 유도 카피·에러 배너 grep 0
</verification>

<success_criteria>
- D-10 중첩 뷰어 동작 (typecheck 수준 — 실기기 렌더는 HUMAN-UAT 적립)
- D-09 참고코너 컴포넌트 완성 (배치는 31-08)
- 목업 선제시 checkpoint 통과 (belle 승인)
</success_criteria>

<output>
Create `.planning/phases/31-api-visual-correction/31-05-SUMMARY.md` when done
</output>
