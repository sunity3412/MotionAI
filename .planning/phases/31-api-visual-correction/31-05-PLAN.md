---
phase: 31-api-visual-correction
plan: 05
type: execute
wave: 2
depends_on: [31-01, 31-02]
files_modified:
  - backend/shared/python/sunity_shared/analysis/interfaces.py
  - backend/shared/python/sunity_shared/analysis/visual_gen.py
  - backend/tests/phase31/test_visual_gen.py
autonomous: true
requirements: [D-02, D-03, D-04, D-08]
must_haves:
  truths:
    - "생성 엔진이 Protocol + typed 반환 모델로 추상화된다 — dict|None 금지, typed 오류 카테고리 (D-02 + 리뷰 M-08)"
    - "이미지 어댑터도 create_task/poll 을 노출한다 — adapter 내부 장시간 폴링 금지, taskId journal 없는 async 처리 불가 (2차 리뷰 B2-02, §8 gate 5)"
    - "회전은 wan2.7-videoedit (amended D-04 — 회전의 유일 수단), 이미지는 31-01 RESULTS.json 의 chosen_model"
    - "redirect 차단은 HTTPRedirectHandler subclass 명시 전달 + redirect_request/http_error_30x typed raise + ProxyHandler({}) 로 구현되고, 실 302 를 발생시키는 local HTTP server 통합 테스트가 존재한다 (2차 리뷰 H2-04, §8 gate 10)"
    - "벤더 다운로드는 /tmp 파일 streaming (누적 cap + sha256) — 전체 bytes 메모리 적재 금지, 대용량 스트림 테스트에서 peak RSS 실측 (2차 리뷰 H2-05, §8 gate 11)"
    - "품질 judge 는 before/after 두 이미지를 함께 받아 typed 축으로 판정한다 — 생성 이미지 단독 판정 금지 (리뷰 H-03)"
    - "judge 런타임은 Lambda 에서 실행 가능하다 — google-genai/pydantic SDK 미반입, stdlib urllib Gemini REST 만"
    - "키는 로그·예외에 노출되지 않고, 모더레이션 재시도는 1회 상한"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/visual_gen.py"
      provides: "typed 결과 모델 + Wan 어댑터 2종(create_task/poll) + download_vendor_asset(파일 스트리밍) + judge_corrected_pose"
      contains: "download_vendor_asset"
      min_lines: 220
    - path: "backend/shared/python/sunity_shared/analysis/interfaces.py"
      provides: "ImageEditEngine / VideoEditEngine Protocol (양쪽 create_task/poll)"
      contains: "class ImageEditEngine"
    - path: "backend/tests/phase31/test_visual_gen.py"
      provides: "어댑터·다운로드 경계(실 302 서버 포함)·judge 테스트"
      min_lines: 180
  key_links:
    - from: "backend/shared/python/sunity_shared/analysis/visual_gen.py"
      to: "dashscope-intl.aliyuncs.com"
      via: "stdlib urllib _http_json"
      pattern: "dashscope-intl"
    - from: "backend/shared/python/sunity_shared/analysis/visual_gen.py"
      to: "generativelanguage.googleapis.com"
      via: "stdlib urllib judge REST 호출 (google-genai SDK 금지 — Lambda 250MB)"
      pattern: "generativelanguage"
---

<objective>
외부 생성 API 계층을 구현한다: typed 결과 모델(리뷰 M-08), Protocol 2종 — **이미지·영상 모두 create_task/poll 노출** (2차 리뷰 B2-02: 이미지 adapter 의 내부 폴링은 Lambda crash 시 재개 식별자가 없는 고아 유료 task 를 만든다), Wan 어댑터 2종, H-05+H2-04 전면 강화 다운로드 경계 (파일 스트리밍 — H2-05), H-03 before/after judge. 전부 stdlib urllib (신규 패키지 0).

31-01 RESULTS.json 의 chosen_model 이 sync 모델(단발 HTTP 응답)인 경우: create_task 가 그 1회 sync 호출을 수행하고 succeeded VendorPollResult 를 즉시 반환하는 형태로 허용 — 단 31-01 스모크가 latency 실측(p95 추정 근거)을 RESULTS.json 에 기록한 경우에만 (B2-02 의 sync-only 허용 조건). 이 경우에도 워커(31-09)의 'creating' state 안에서 단 1회 외부 호출로 끝난다.

Purpose: 31-09 워커가 공유하는 유일한 외부 API 코드 경로 — 코드 1벌 + 테스트 고정된 보안 경계.
Output: interfaces.py Protocol + visual_gen.py + mock/통합 테스트.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-api-visual-correction/31-PATTERNS.md
@.planning/phases/31-api-visual-correction/31-ACCEPTANCE.md
@.planning/phases/31-api-visual-correction/smoke/RESULTS.json
@.planning/spikes/004-gemini-omni-view-editing/wan_gate_batch.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: typed 결과 모델 + Protocol(create_task/poll 통일) + Wan 어댑터 2종</name>
  <files>backend/shared/python/sunity_shared/analysis/interfaces.py, backend/shared/python/sunity_shared/analysis/visual_gen.py, backend/tests/phase31/test_visual_gen.py</files>
  <read_first>backend/shared/python/sunity_shared/analysis/interfaces.py (전체 — Protocol 선언 + "구현은 형제 모듈" docstring 규칙), .planning/spikes/004-gemini-omni-view-editing/wan_gate_batch.py (http_json lines 24-56 + create body lines 87-128 + 폴링/종료 상태/모더레이션 응답 형상), .planning/phases/31-api-visual-correction/smoke/RESULTS.json (chosen_model/endpoint/request_params/sync/latency_s), backend/shared/python/sunity_shared/models.py (VISUAL_FAILURE_REASONS — 오류 카테고리 단일 출처), backend/tests/phase31/conftest.py (dashscope_urlopen_mock)</read_first>
  <action>visual_gen.py 신설 (docstring: D-02/D-08 + spike 008 + 리뷰 M-08/H-05 + 2차 리뷰 B2-02 인용). typed 모델 (frozen dataclass): `VendorTaskCreated(task_id: str, request_id: str | None)`, `VendorPollResult(state: str, output_url: str | None, failure_reason: str | None)` — state 는 'pending'|'succeeded'|'failed'|'blocked' literal, failure_reason 은 models.VISUAL_FAILURE_REASONS 값만. interfaces.py 에 Protocol 2종 — **양쪽 모두 create_task/poll 형태 (2차 리뷰 B2-02: "이미지 async API 도 video 와 동일하게 create_task/poll 로 노출, adapter 내부 장시간 polling 금지")**: `ImageEditEngine.create_task(self, image_url: str, prompt: str) -> VendorTaskCreated | VendorPollResult` (async 모델 = taskId 반환 → 워커가 journal 후 poll; sync 모델 = 1회 호출 결과를 succeeded/failed VendorPollResult 로 즉시 반환 — RESULTS.json sync=true + latency 실측 존재가 전제, docstring 에 근거 인용) + `ImageEditEngine.poll(self, task_id: str) -> VendorPollResult`; `VideoEditEngine.create_task(self, video_url: str, prompt: str) -> VendorTaskCreated | VendorPollResult` + `VideoEditEngine.poll(self, task_id: str) -> VendorPollResult`. `_http_json(url, key, body=None, async_header=False)` — wan_gate_batch 형상 (Authorization Bearer, X-DashScope-Async: enable, timeout 60, 방어적 JSON 파싱; 예외 문자열에 키 미포함). BASE = "https://dashscope-intl.aliyuncs.com". `WanImageAdapter` — RESULTS.json 의 chosen_model/endpoint/request_params 반영, **내부 while/폴링 루프 금지 (모듈 소스 assert 대상)**, 모더레이션 → 'blocked'/'moderation'; RESULTS.json blocked=true 여도 어댑터는 구현하되 `IMAGE_ENGINE_BLOCKED = True` 방출 (31-12 게이트 소비). `WanVideoEditAdapter` — model 'wan2.7-videoedit', create endpoint /api/v1/services/aigc/video-generation/video-synthesis, parameters resolution 720P / watermark False / prompt_extend False / seed 42 (spike 검증값), poll endpoint /api/v1/tasks/{task_id}, 모더레이션 → 'blocked'. `MODERATION_RETRY_MAX = 1` 상수 방출. 키는 인자 주입. test_visual_gen.py: create→task_id, SUCCEEDED→output_url, 모더레이션 typed 매핑, 예외/비정상 JSON→'vendor_error', caplog 키 부재, `requests` import 부재, **이미지 어댑터 모듈 소스에 sleep/폴링 루프 부재 assert (B2-02 — §8 gate 5)**, sync 분기: sync 응답 → VendorPollResult succeeded 즉시 반환.</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31/test_visual_gen.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - interfaces.py 에 ImageEditEngine/VideoEditEngine 둘 다 create_task + poll 선언 (이미지 단발 generate 메서드 부재)
    - WanImageAdapter 에 내부 폴링 루프 0 (time.sleep 부재 assert — B2-02)
    - visual_gen.py 반환형 전부 frozen dataclass, failure_reason ⊆ models.VISUAL_FAILURE_REASONS 테스트 고정
    - wan2.7-videoedit + watermark False 존재, requests import 0
  </acceptance_criteria>
  <done>엔진 계층이 taskId journal 가능한 typed 계약으로 고정됨.</done>
</task>

<task type="auto">
  <name>Task 2: download_vendor_asset — H-05+H2-04+H2-05 전면 강화 다운로드 경계</name>
  <files>backend/shared/python/sunity_shared/analysis/visual_gen.py, backend/tests/phase31/test_visual_gen.py</files>
  <read_first>.planning/phases/31-api-visual-correction/31-REVIEWS.md 의 H-05 절 + .planning/phases/31-api-visual-correction/31-PLAN-REVIEW-ITERATION2.md 의 H2-04/H2-05 절 (요구 목록 원문 — 테스트 케이스의 단일 기준)</read_first>
  <action>`@dataclass(frozen=True) class DownloadedAsset(path: str, sha256: str, size_bytes: int, content_type: str)` + `download_vendor_asset(url: str, dest_path: str, *, max_bytes: int, allowed_content_types: tuple[str, ...], timeout_s: int = 60) -> DownloadedAsset` 신설 — **전체 bytes 반환 금지 (2차 리뷰 H2-05): dest_path(/tmp) 로 64KB 청크 streaming write + hashlib.sha256 누적 + 누적 byte cap 초과 즉시 중단·파일 삭제.** 실패는 typed `VendorDownloadError(reason)` — 'bad_scheme'|'bad_host'|'redirect'|'private_ip'|'bad_content_type'|'too_large'|'timeout'. 규칙: (1) urlparse scheme == 'https'; (2) `_ALLOWED_HOST_SUFFIXES = ("aliyuncs.com",)` — hostname == suffix 또는 hostname.endswith("." + suffix) 정확 매칭 (evilaliyuncs.com 차단); (3) socket.getaddrinfo 해석 IP 전부 ipaddress 기준 사설/링크로컬/루프백/예약 대역 거부; (4) **redirect 차단 (2차 리뷰 H2-04 — 기존 "핸들러 제거" 방식은 build_opener 가 기본 HTTPRedirectHandler 를 자동 추가하므로 무효): `class _NoRedirectHandler(urllib.request.HTTPRedirectHandler)` 를 정의해 `redirect_request` 와 `http_error_301/302/303/307/308` 전부에서 즉시 `VendorDownloadError('redirect')` raise, `build_opener(_NoRedirectHandler(), urllib.request.ProxyHandler({}))` 로 subclass 명시 전달 + proxy auto-detection 차단**; (5) Content-Type 허용목록 접두 매칭; (6) timeout 강제; (7) URL query/서명 비로깅 (호스트만). 테스트 전용 내부 kwargs `_test_allowed_hosts: tuple | None = None`, `_test_allow_private: bool = False` (docstring: local 통합 테스트 전용 — production 호출부 사용 금지, grep 로 워커 소스에 부재 확인은 31-09 acceptance). 테스트: (mock 계열) https 아님/evilaliyuncs.com/사설 IP/content-type/max_bytes 초과/정상 파일 스트리밍+sha256 일치; **(통합 — H2-04 §8 gate 10) http.server ThreadingHTTPServer 를 127.0.0.1 에 띄워 실 301/302/303/307/308 응답 5종 각각을 실제 opener 로 요청 → 전부 VendorDownloadError('redirect') (_test_allowed_hosts/_test_allow_private 로 host/IP 게이트 통과시켜 redirect 계층만 검증)**; **(RSS 실측 — H2-05 §8 gate 11) 200MB 급 합성 스트림(청크 반복 mock 또는 로컬 서버 generator)을 다운로드하며 resource.getrusage(RUSAGE_SELF).ru_maxrss 증가분이 전체 크기 대비 소폭(< 64MB)임을 assert + 측정값을 테스트 출력에 기록 — 31-10 IaC 의 워커 MemorySize 결정 근거 (SUMMARY 에 실측값 박제)**.</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31/test_visual_gen.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - download_vendor_asset 가 파일 경로 스트리밍 시그니처 (bytes 반환 아님) + DownloadedAsset(sha256/size) 반환
    - _NoRedirectHandler subclass 가 build_opener 에 명시 전달 + ProxyHandler({}) 존재 (H2-04)
    - 실 302 local server 통합 테스트 5종(30x 전부) green (§8 gate 10)
    - 대용량 스트림 peak RSS assert + 측정 기록 (§8 gate 11)
    - evilaliyuncs.com / oversized / wrong content-type / private IP 케이스 green
  </acceptance_criteria>
  <done>벤더 다운로드 경계가 리뷰 H-05 + 2차 H2-04/H2-05 전 항목 테스트로 고정됨.</done>
</task>

<task type="auto">
  <name>Task 3: judge_corrected_pose — before/after typed judge, stdlib urllib Gemini REST (리뷰 H-03)</name>
  <files>backend/shared/python/sunity_shared/analysis/visual_gen.py, backend/tests/phase31/test_visual_gen.py</files>
  <read_first>backend/functions/pipeline/requirements.txt (2026-06-12 박제 주석 전체 — google-genai transitive ~100MB 로 Lambda 250MB 초과 실측 262MB → 배제), backend/shared/python/sunity_shared/gemini/client.py lines 1-30 (Pod 전용 확인 — 프롬프트/스키마 관례 참고만, 재사용 금지), .planning/phases/31-api-visual-correction/31-ACCEPTANCE.md (Display/Training 게이트 축·**calibration 으로 확정된 임계값** — 단일 기준, 2차 리뷰 H2-07)</read_first>
  <action>`@dataclass(frozen=True) class JudgeVerdict` — identity_ok/clothing_ok/background_ok/pole_ok/single_person_ok/no_extra_limbs/correction_visible (bool 7축) + confidence(float) + reason(str) + `display_pass` / `training_pass` property — **임계값 상수는 31-ACCEPTANCE.md 의 calibration 결과표에서 채택된 값을 인용 주석과 함께 선언 (H2-07: 선언값이 아니라 측정 기반 — 31-01 CALIBRATION.json 의 chosen 값과 일치해야 하며 불일치 시 테스트 실패하도록 CALIBRATION.json 을 읽어 대조하는 테스트 포함)**. `judge_corrected_pose(before_png: bytes, after_png: bytes, context: dict, *, api_key: str | None = None) -> JudgeVerdict | None` — stdlib urllib 로 POST `https://generativelanguage.googleapis.com/v1beta/models/gemini-3.5-flash:generateContent` ('gemini-3.5-flash' 고정 — [[gemini-latest-model-versions]]). 키 = api_key 인자 우선, 미제공 시 env `GEMINI_API_KEY`; `x-goog-api-key` 헤더 (URL query 금지). body: contents.parts = [text "BEFORE (원본)", inline_data base64(before_png), text "AFTER (생성)", inline_data base64(after_png), 판정 프롬프트] — 두 이미지 순서 라벨 필수 (리뷰 H-03). generationConfig: responseMimeType "application/json" + responseSchema (7 bool + confidence number + reason string — dict 수동 검증, pydantic 불사용). timeout 60s + 5xx/timeout 1회 재시도. fail-closed: 오류/파싱 실패/스키마 위반/키 미설정/예외 → None (호출측 'judge_failed'). Hard rejection (ACCEPTANCE §Hard Rejection): 인물 미검출/복수 인물/사지 이상 축은 다른 축과 무관하게 display_pass False. 키 비로깅. 테스트: 정상 JSON → 축별 property 판정 (경계값 — ACCEPTANCE 채택 임계값 기준 ±0.01), hard rejection 우선, HTTP 500 → 1회 재시도 후 None, malformed/축 누락 → None, 키 미설정 → None (네트워크 0), 요청 URL 키 부재 + caplog 키 부재, visual_gen.py 소스에 google.genai/google-genai/pydantic import 부재 assert.</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31 -x -q</automated>
  </verify>
  <acceptance_criteria>
    - judge_corrected_pose 시그니처가 before_png 와 after_png 둘 다 필수
    - display_pass / training_pass 임계값이 31-01 CALIBRATION.json 채택값과 일치 (대조 테스트 green — H2-07)
    - None/예외 → None (판정 불능은 통과가 아님) 테스트 green
    - visual_gen.py 에 google.genai / pydantic import 0
    - 모델 문자열 'gemini-3.5-flash' + 키는 x-goog-api-key 헤더
    - `python -m pytest backend/tests -q` exits 0
  </acceptance_criteria>
  <done>보존 판정이 원본 대조 + typed 축 + calibration 기반 임계값 + Lambda 실행 가능으로 고정됨.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend → DashScope | presign URL 전송 + 응답 신뢰 불가 |
| DashScope 응답 URL → backend | SSRF/redirect/오버사이즈/오호스트 위험 지점 |
| backend → Gemini REST | 두 이미지 판정 요청 (stdlib urllib — SDK 미반입) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-15 | Information Disclosure | DASHSCOPE_API_KEY / GEMINI_API_KEY | mitigate | 키 인자/env 주입 + x-goog-api-key 헤더 + 로그/예외 미포함 (caplog 테스트) + SSM SecureString 만 |
| T-31-16 | Injection / SSRF | 벤더 응답 URL | mitigate | https/정확 호스트/_NoRedirectHandler(30x 전부 raise)/ProxyHandler({})/사설 IP 차단 — 실 302 통합 테스트 (H-05 + H2-04) |
| T-31-17 | Denial of Service | 오버사이즈 응답 | mitigate | /tmp 파일 스트리밍 + 누적 cap + peak RSS 실측 (H-05 + H2-05 — 512MB Lambda 보호) |
| T-31-18 | Tampering | 왜곡 생성물 통과 | mitigate | before/after 대조 judge + hard rejection + 불확실=None (리뷰 H-03) |
| T-31-19 | Denial of Service (과금) | 모더레이션 재시도 / adapter 내부 폴링 고아 | mitigate | MODERATION_RETRY_MAX=1 + create_task/poll 통일 (내부 폴링 금지 — B2-02) |
</threat_model>

<verification>
- `python -m pytest backend/tests -q` green
- visual_gen.py 에 API 키 리터럴·requests import·google.genai import·pydantic import·내부 폴링 루프 0
</verification>

<success_criteria>
- typed 엔진 계층(create_task/poll 통일) + H-05/H2-04/H2-05 다운로드 경계 + H-03 judge 가 전부 mock/통합 테스트로 고정
- §8 gate 5·10·11 이 이 플랜의 테스트로 추적 가능
- 31-09 워커가 이 모듈만 import 하면 되는 상태
</success_criteria>

<output>
Create `.planning/phases/31-api-visual-correction/31-05-SUMMARY.md` when done
</output>
