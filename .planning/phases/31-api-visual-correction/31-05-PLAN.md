---
phase: 31-api-visual-correction
plan: 05
type: execute
wave: 2
depends_on: [31-01, 31-02]
files_modified:
  - backend/shared/python/sunity_shared/analysis/interfaces.py
  - backend/shared/python/sunity_shared/analysis/visual_gen.py
  - backend/tests/phase31/test_visual_gen.py
autonomous: true
requirements: [D-02, D-03, D-04, D-08]
must_haves:
  truths:
    - "생성 엔진이 Protocol + typed 반환 모델로 추상화된다 — dict|None 금지, typed 오류 카테고리 (D-02 + 리뷰 M-08)"
    - "이미지 어댑터도 create_task/poll 을 노출한다 — adapter 내부 장시간 폴링 금지 (2차 리뷰 B2-02)"
    - "회전은 wan2.7-videoedit (amended D-04 — 회전의 유일 수단), 이미지는 31-01 RESULTS.json 의 chosen_model"
    - "redirect 차단은 HTTPRedirectHandler subclass 명시 전달 + ProxyHandler({}) 로 구현되고, self-signed TLS local server + 테스트 전용 SSL context 주입으로 실제 301/302/303/307/308 응답이 redirect handler 에 도달하는 통합 테스트가 존재한다 — production 에 allow_http 인자 추가 금지 (2차 H2-04 + 3차 H3-05)"
    - "벤더 다운로드는 /tmp 파일 streaming (누적 cap + sha256) — 대용량 RSS 측정은 fresh subprocess + 플랫폼 단위 정규화 + 구조화 JSON (2차 H2-05 + 3차 H3-06)"
    - "품질 judge 는 before/after 두 이미지를 함께 받아 typed 축으로 판정한다 — 판정 임계값은 인자 주입 (calibration 은 31-13 이 수행 — 3차 H3-02)"
    - "judge 요청은 정규화 파생 이미지 (EXIF 제거·최대 변 2048px·bounded JPEG) 로 만들어지고 POST 전 body byte 측정 내부 상한 16MB 를 강제한다 — 초과 = typed JudgeInputTooLargeError (3차 H3-03, Gemini 공식 20MB 총 요청 상한)"
    - "judge 런타임은 Lambda 에서 실행 가능하다 — google-genai/pydantic SDK 미반입, stdlib urllib Gemini REST 만"
    - "키는 로그·예외에 노출되지 않고, 모더레이션 재시도는 1회 상한"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/visual_gen.py"
      provides: "typed 결과 모델 + Wan 어댑터 2종(create_task/poll) + download_vendor_asset(파일 스트리밍) + prepare_judge_payload(정규화+16MB) + judge_corrected_pose(threshold 인자식) + PROMPT_VERSION"
      contains: "prepare_judge_payload"
      min_lines: 240
    - path: "backend/shared/python/sunity_shared/analysis/interfaces.py"
      provides: "ImageEditEngine / VideoEditEngine Protocol (양쪽 create_task/poll)"
      contains: "class ImageEditEngine"
    - path: "backend/tests/phase31/test_visual_gen.py"
      provides: "어댑터·다운로드 경계(실 30x TLS 서버 포함)·judge·정규화 테스트"
      min_lines: 200
  key_links:
    - from: "backend/shared/python/sunity_shared/analysis/visual_gen.py"
      to: "dashscope-intl.aliyuncs.com"
      via: "stdlib urllib _http_json"
      pattern: "dashscope-intl"
    - from: "backend/shared/python/sunity_shared/analysis/visual_gen.py"
      to: "generativelanguage.googleapis.com"
      via: "stdlib urllib judge REST 호출 (google-genai SDK 금지 — Lambda 250MB)"
      pattern: "generativelanguage"
---

<objective>
외부 생성 API 계층을 구현한다: typed 결과 모델(리뷰 M-08), Protocol 2종 — 이미지·영상 모두 create_task/poll 노출 (B2-02), Wan 어댑터 2종, H-05+H2-04+H3-05 전면 강화 다운로드 경계 (파일 스트리밍 — H2-05/H3-06), H-03+H3-03 before/after judge. 전부 stdlib urllib (신규 패키지 0).

3차 리뷰 반영 3건:
- **H3-02:** judge 판정 임계값(confidence)은 인자 주입식 — `display_pass(verdict, *, min_confidence)` / `training_pass(verdict, *, min_confidence)` 함수형. 임계값 자체는 31-13 calibration harness 가 이 함수를 grid 로 돌려 선택한다. CALIBRATION.json 대조는 31-13 소관 (이 플랜에서 선언값·대조 테스트 금지).
- **H3-03:** `prepare_judge_payload` — judge 전용 파생 이미지 정규화 (PIL 재인코딩으로 EXIF 제거 + 최대 변 2048px 다운스케일 + JPEG quality 85 bounded) 후 최종 직렬화 body byte 를 측정해 내부 상한 `JUDGE_BODY_MAX_BYTES = 16MB` 강제 (Gemini 공식 총 요청 20MB 대비 여유) — 초과는 조용한 'judge_failed' 가 아닌 typed `JudgeInputTooLargeError`. canonical 결과/pose 입력은 별개 (판정용 압축이 사용자 결과 불변).
- **H3-05/H3-06:** redirect 통합 테스트는 self-signed TLS local server (production 은 https 계약 유지 — allow_http 인자 금지), RSS 는 fresh subprocess 실측.

31-01 RESULTS.json 의 chosen_model 이 sync 모델인 경우: create_task 가 1회 sync 호출을 수행하고 succeeded VendorPollResult 를 즉시 반환 — RESULTS.json 의 latency 실측이 전제 (B2-02).

Purpose: 31-09 워커가 공유하는 유일한 외부 API 코드 경로 — 코드 1벌 + 테스트 고정된 보안·payload 경계.
Output: interfaces.py Protocol + visual_gen.py + mock/통합 테스트.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-api-visual-correction/31-PATTERNS.md
@.planning/phases/31-api-visual-correction/31-ACCEPTANCE.md
@.planning/phases/31-api-visual-correction/smoke/RESULTS.json
@.planning/spikes/004-gemini-omni-view-editing/wan_gate_batch.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: typed 결과 모델 + Protocol(create_task/poll 통일) + Wan 어댑터 2종</name>
  <files>backend/shared/python/sunity_shared/analysis/interfaces.py, backend/shared/python/sunity_shared/analysis/visual_gen.py, backend/tests/phase31/test_visual_gen.py</files>
  <read_first>backend/shared/python/sunity_shared/analysis/interfaces.py (전체 — Protocol 선언 + "구현은 형제 모듈" docstring 규칙), .planning/spikes/004-gemini-omni-view-editing/wan_gate_batch.py (http_json lines 24-56 + create body lines 87-128 + 폴링/종료 상태/모더레이션 응답 형상), .planning/phases/31-api-visual-correction/smoke/RESULTS.json (chosen_model/endpoint/request_params/sync/latency_s), backend/shared/python/sunity_shared/models.py (VISUAL_FAILURE_REASONS — 오류 카테고리 단일 출처), backend/tests/phase31/conftest.py (dashscope_urlopen_mock)</read_first>
  <action>visual_gen.py 신설 (docstring: D-02/D-08 + spike 008 + 리뷰 M-08/H-05 + 2차 B2-02 + 3차 H3-01 인용 — "poll 결과의 output URL 은 반환값으로만 흐른다: 호출측이 Firestore/메시지/로그에 기록 금지"). typed 모델 (frozen dataclass): `VendorTaskCreated(task_id: str, request_id: str | None)`, `VendorPollResult(state: str, output_url: str | None, failure_reason: str | None)` — state 는 'pending'|'succeeded'|'failed'|'blocked' literal, failure_reason 은 models.VISUAL_FAILURE_REASONS 값만. interfaces.py 에 Protocol 2종 — 양쪽 모두 create_task/poll (B2-02): `ImageEditEngine.create_task(self, image_url: str, prompt: str) -> VendorTaskCreated | VendorPollResult` (async 모델 = taskId 반환 → 워커 journal 후 poll; sync 모델 = 1회 호출 결과를 succeeded/failed VendorPollResult 로 즉시 반환 — RESULTS.json sync=true + latency 실측 전제, docstring 근거 인용) + `poll(self, task_id: str) -> VendorPollResult`; `VideoEditEngine` 동형. `_http_json(url, key, body=None, async_header=False)` — wan_gate_batch 형상 (Authorization Bearer, X-DashScope-Async: enable, timeout 60, 방어적 JSON 파싱; 예외 문자열에 키 미포함). BASE = "https://dashscope-intl.aliyuncs.com". `WanImageAdapter` — RESULTS.json 의 chosen_model/endpoint/request_params 반영, 내부 while/폴링 루프 금지 (모듈 소스 assert 대상), 모더레이션 → 'blocked'/'moderation'; RESULTS.json blocked=true 여도 어댑터는 구현하되 `IMAGE_ENGINE_BLOCKED = True` 방출 (31-12 게이트 소비). `WanVideoEditAdapter` — model 'wan2.7-videoedit', create endpoint /api/v1/services/aigc/video-generation/video-synthesis, parameters resolution 720P / watermark False / prompt_extend False / seed 42 (spike 검증값), poll endpoint /api/v1/tasks/{task_id}, 모더레이션 → 'blocked'. `MODERATION_RETRY_MAX = 1` 상수 방출. 키는 인자 주입. test_visual_gen.py: create→task_id, SUCCEEDED→output_url, 모더레이션 typed 매핑, 예외/비정상 JSON→'vendor_error', caplog 키 부재, `requests` import 부재, 이미지 어댑터 모듈 소스에 sleep/폴링 루프 부재 assert (B2-02), sync 분기 즉시 반환.</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31/test_visual_gen.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - interfaces.py 에 ImageEditEngine/VideoEditEngine 둘 다 create_task + poll 선언 (이미지 단발 generate 메서드 부재)
    - WanImageAdapter 에 내부 폴링 루프 0 (time.sleep 부재 assert — B2-02)
    - visual_gen.py 반환형 전부 frozen dataclass, failure_reason ⊆ models.VISUAL_FAILURE_REASONS 테스트 고정
    - wan2.7-videoedit + watermark False 존재, requests import 0
  </acceptance_criteria>
  <done>엔진 계층이 taskId journal 가능한 typed 계약으로 고정됨.</done>
</task>

<task type="auto">
  <name>Task 2: download_vendor_asset — 다운로드 경계 + TLS redirect 통합 테스트 + subprocess RSS 실측</name>
  <files>backend/shared/python/sunity_shared/analysis/visual_gen.py, backend/tests/phase31/test_visual_gen.py</files>
  <read_first>.planning/phases/31-api-visual-correction/31-REVIEWS.md 의 H-05 절 + 31-PLAN-REVIEW-ITERATION2.md 의 H2-04/H2-05 절 + 31-PLAN-REVIEW-ITERATION3.md 의 H3-05/H3-06 절 (요구 목록 원문 — 테스트 케이스의 단일 기준)</read_first>
  <action>`@dataclass(frozen=True) class DownloadedAsset(path: str, sha256: str, size_bytes: int, content_type: str)` + `download_vendor_asset(url: str, dest_path: str, *, max_bytes: int, allowed_content_types: tuple[str, ...], timeout_s: int = 60) -> DownloadedAsset` 신설 — dest_path(/tmp) 로 64KB 청크 streaming write + hashlib.sha256 누적 + 누적 byte cap 초과 즉시 중단·파일 삭제 (H2-05). 실패는 typed `VendorDownloadError(reason)` — 'bad_scheme'|'bad_host'|'redirect'|'private_ip'|'bad_content_type'|'too_large'|'timeout'. 규칙: (1) urlparse scheme == 'https' — **production 우회 인자(allow_http 류) 절대 추가 금지 (H3-05)**; (2) `_ALLOWED_HOST_SUFFIXES = ("aliyuncs.com",)` — hostname == suffix 또는 endswith("." + suffix) 정확 매칭; (3) socket.getaddrinfo 해석 IP 전부 ipaddress 기준 사설/링크로컬/루프백/예약 대역 거부; (4) redirect 차단 (H2-04): `class _NoRedirectHandler(urllib.request.HTTPRedirectHandler)` — `redirect_request` 와 `http_error_301/302/303/307/308` 전부 즉시 `VendorDownloadError('redirect')` raise, `build_opener(_NoRedirectHandler(), urllib.request.ProxyHandler({}))` 명시 전달; (5) Content-Type 허용목록 접두 매칭; (6) timeout 강제; (7) URL query/서명 비로깅 (호스트만). 테스트 전용 내부 kwargs: `_test_allowed_hosts`, `_test_allow_private`, **`_test_ssl_context: ssl.SSLContext | None = None` (H3-05 — 통합 테스트가 self-signed 인증서 신뢰 context 를 주입; production 호출부 사용 금지, 31-09 acceptance grep)**.
테스트: (mock 계열) https 아님/evilaliyuncs.com/사설 IP/content-type/max_bytes 초과/정상 파일 스트리밍+sha256 일치; **(통합 — H3-05, §8-3 gate 9): 테스트가 임시 self-signed 인증서 생성 (cryptography 미반입 — `ssl` + 사전 생성 PEM fixture 를 테스트 디렉터리에 커밋하거나 openssl 서브프로세스; 인물 데이터 아님) → ThreadingHTTPServer socket 을 ssl wrap → `https://127.0.0.1:{port}` URL 로 실 301/302/303/307/308 응답 5종 각각 요청 (_test_allowed_hosts/_test_allow_private + _test_ssl_context 주입) → 전부 VendorDownloadError('redirect') — 최초 scheme 검사가 아니라 redirect handler 계층에 실제 도달함을 증명**; **(RSS 실측 — H3-06): 대용량(200MB 급 합성 스트림) 다운로드를 `subprocess.run([sys.executable, '-c', <측정 스크립트>])` fresh subprocess 로 실행 — 스크립트는 다운로드 전후 `resource.getrusage(RUSAGE_SELF).ru_maxrss` 를 읽어 **플랫폼 정규화 (sys.platform darwin = bytes / linux = KiB → bytes 통일)** 후 {"baseline_bytes", "peak_bytes", "delta_bytes", "platform"} 구조화 JSON 을 stdout 방출 → 부모 테스트가 파싱해 delta < 64MB assert + 측정값 기록. 같은 pytest 프로세스 내 ru_maxrss delta 사용 금지 (high-water mark 오염 — H3-06). SUMMARY 에 실측값 박제 + "IaC MemorySize 는 Linux/container 측정값 기준 (31-12 배포 전 컨테이너 실측로 재확인)" 명시**.</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31/test_visual_gen.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - download_vendor_asset 가 파일 경로 스트리밍 시그니처 + DownloadedAsset(sha256/size) 반환
    - _NoRedirectHandler subclass + ProxyHandler({}) 존재, production allow_http 인자 부재 (H3-05)
    - TLS local server 30x 5종 통합 테스트 green — redirect handler 실제 도달 (H3-05)
    - RSS 측정이 fresh subprocess + 플랫폼 단위 정규화 + 구조화 JSON (동일 프로세스 delta 코드 0 — H3-06)
    - evilaliyuncs.com / oversized / wrong content-type / private IP 케이스 green
  </acceptance_criteria>
  <done>벤더 다운로드 경계가 H-05 + H2-04/H2-05 + H3-05/H3-06 전 항목 테스트로 고정됨.</done>
</task>

<task type="auto">
  <name>Task 3: prepare_judge_payload + judge_corrected_pose — 정규화·16MB 상한·threshold 인자식 (H-03 + H3-02/H3-03)</name>
  <files>backend/shared/python/sunity_shared/analysis/visual_gen.py, backend/tests/phase31/test_visual_gen.py</files>
  <read_first>backend/functions/pipeline/requirements.txt (2026-06-12 박제 주석 전체 — google-genai transitive ~100MB 로 Lambda 250MB 초과 실측 262MB → 배제), backend/shared/python/sunity_shared/gemini/client.py lines 1-30 (Pod 전용 확인 — 프롬프트/스키마 관례 참고만, 재사용 금지), .planning/phases/31-api-visual-correction/31-PLAN-REVIEW-ITERATION3.md 의 H3-02/H3-03 절</read_first>
  <action>(1) **`prepare_judge_payload(before_png: bytes, after_png: bytes, prompt: str) -> bytes` (H3-03)**: 각 이미지를 PIL 로 열어 재인코딩 — EXIF/메타 제거 (재인코딩으로 자연 제거), 최대 변 2048px 초과 시 LANCZOS 다운스케일, JPEG quality 85 저장 (파생 판정용 — canonical 결과 불변, 원본 인자 미변형). 정규화 후 Gemini generateContent body dict (contents.parts = [text "BEFORE (원본)", inline_data base64 jpeg, text "AFTER (생성)", inline_data base64 jpeg, prompt] + generationConfig responseMimeType/responseSchema) 를 json.dumps 로 직렬화 → **len(body_bytes) > `JUDGE_BODY_MAX_BYTES = 16 * 1024 * 1024` 면 typed `class JudgeInputTooLargeError(Exception)` raise (Gemini 공식 inline 총 요청 20MB 대비 내부 여유 상한 — docstring 에 공식 문서 URL 인용)**. 정상 시 직렬화 bytes 반환.
(2) `judge_corrected_pose(before_png: bytes, after_png: bytes, context: dict, *, api_key: str | None = None) -> JudgeVerdict | None` — 내부에서 prepare_judge_payload 사용 (JudgeInputTooLargeError 는 전파 — 호출측 31-09 가 'judge_input_too_large' 로 종결; None 으로 뭉개지 않음). stdlib urllib POST `https://generativelanguage.googleapis.com/v1beta/models/gemini-3.5-flash:generateContent` ('gemini-3.5-flash' 고정 — [[gemini-latest-model-versions]]). 키 = api_key 인자 우선, 미제공 시 env `GEMINI_API_KEY`; `x-goog-api-key` 헤더 (URL query 금지). responseSchema = 7 bool + confidence number + reason string (dict 수동 검증, pydantic 불사용). timeout 60s + 5xx/timeout 1회 재시도. fail-closed: 오류/파싱 실패/스키마 위반/키 미설정 → None. `PROMPT_VERSION = "judge-v1"` 상수 방출 (31-13 meta 기록용 — H3-02 재현성).
(3) **`@dataclass(frozen=True) class JudgeVerdict`** — identity_ok/clothing_ok/background_ok/pole_ok/single_person_ok/no_extra_limbs/correction_visible (bool 7축) + confidence(float) + reason(str). **판정 함수는 threshold 인자식 (H3-02 — 선언 상수 금지): `judge_display_pass(verdict, *, min_confidence: float) -> bool` — hard rejection (인물 미검출/복수 인물/사지 이상) 은 confidence 무관 False, 그 외 7축 전부 true AND confidence >= min_confidence; `judge_training_pass(verdict, *, min_confidence: float) -> bool` — display 조건 + 더 높은 min_confidence (호출측 주입). 모듈에 임계값 숫자 리터럴 선언 금지 — 31-13 calibration 이 grid 로 이 함수들을 돌려 chosen 을 선택하고, 31-09 는 env 로 주입.**
테스트: prepare_judge_payload — 대형(각 12MB) 입력 → 정규화 후 16MB 이하 통과, 극단 케이스(정규화 후에도 초과하도록 mock) → JudgeInputTooLargeError; EXIF 포함 입력 → 출력 payload 이미지에 EXIF 부재; 최대 변 2048 다운스케일 확인; 정상 JSON → 축별 판정 (min_confidence 인자 경계 ±0.01), hard rejection 우선, HTTP 500 → 1회 재시도 후 None, malformed/축 누락 → None, 키 미설정 → None (네트워크 0), 요청 URL 키 부재 + caplog 키 부재, 모듈 소스에 google.genai/pydantic import 부재 + 임계값 숫자 상수(0.7/0.85 류 판정 리터럴) 부재 assert.</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31 -x -q</automated>
  </verify>
  <acceptance_criteria>
    - prepare_judge_payload 존재 — 정규화(EXIF 0·2048px·JPEG bounded) + 16MB 직렬화 상한 + JudgeInputTooLargeError (H3-03)
    - judge_display_pass/judge_training_pass 가 threshold 인자식 — 모듈 내 판정 임계값 리터럴 0 (H3-02)
    - JudgeInputTooLargeError 가 None 으로 뭉개지지 않고 전파 (테스트 고정)
    - visual_gen.py 에 google.genai / pydantic import 0, 모델 문자열 'gemini-3.5-flash' + x-goog-api-key 헤더
    - PROMPT_VERSION 상수 존재 (31-13 재현성 meta)
    - `python -m pytest backend/tests -q` exits 0
  </acceptance_criteria>
  <done>보존 판정이 원본 대조 + typed 축 + payload 경계 + 인자식 임계값 + Lambda 실행 가능으로 고정 — 31-13 calibration 과 31-09 워커가 같은 함수를 소비.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend → DashScope | presign URL 전송 + 응답 신뢰 불가 |
| DashScope 응답 URL → backend | SSRF/redirect/오버사이즈/오호스트 위험 지점 |
| backend → Gemini REST | 정규화 파생 이미지 판정 요청 (stdlib urllib — SDK 미반입) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-15 | Information Disclosure | DASHSCOPE_API_KEY / GEMINI_API_KEY | mitigate | 키 인자/env 주입 + x-goog-api-key 헤더 + 로그/예외 미포함 (caplog 테스트) + SSM SecureString 만 |
| T-31-16 | Injection / SSRF | 벤더 응답 URL | mitigate | https/정확 호스트/_NoRedirectHandler(30x 전부 raise)/ProxyHandler({})/사설 IP 차단 — 실 TLS 30x 통합 테스트 (H-05 + H2-04 + H3-05) |
| T-31-17 | Denial of Service | 오버사이즈 응답/요청 | mitigate | /tmp 파일 스트리밍 + 누적 cap + subprocess RSS 실측 + judge 16MB 상한 (H2-05 + H3-03/H3-06) |
| T-31-18 | Tampering | 왜곡 생성물 통과 | mitigate | before/after 대조 judge + hard rejection + 불확실=None (리뷰 H-03) |
| T-31-19 | Denial of Service (과금) | 모더레이션 재시도 / adapter 내부 폴링 고아 | mitigate | MODERATION_RETRY_MAX=1 + create_task/poll 통일 (내부 폴링 금지 — B2-02) |
</threat_model>

<verification>
- `python -m pytest backend/tests -q` green
- visual_gen.py 에 API 키 리터럴·requests import·google.genai import·pydantic import·내부 폴링 루프·판정 임계값 리터럴 0
</verification>

<success_criteria>
- typed 엔진 계층(create_task/poll 통일) + 다운로드 경계(H3-05 TLS 통합/H3-06 subprocess RSS) + judge(H3-02 인자식/H3-03 정규화·16MB) 가 전부 mock/통합 테스트로 고정
- 3차 리뷰 §8-3 gate 8·9 가 이 플랜의 테스트로 추적 가능
- 31-09 워커와 31-13 calibration 이 이 모듈만 import 하면 되는 상태
</success_criteria>

<output>
Create `.planning/phases/31-api-visual-correction/31-05-SUMMARY.md` when done
</output>
