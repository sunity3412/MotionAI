---
phase: 31-api-visual-correction
plan: 05
type: execute
wave: 2
depends_on: [31-01, 31-02]
files_modified:
  - backend/shared/python/sunity_shared/analysis/interfaces.py
  - backend/shared/python/sunity_shared/analysis/visual_gen.py
  - backend/tests/phase31/test_visual_gen.py
autonomous: true
requirements: [D-02, D-03, D-04, D-08]
must_haves:
  truths:
    - "생성 엔진이 Protocol + typed 반환 모델로 추상화된다 — dict|None 금지, typed 오류 카테고리 (D-02 + 리뷰 M-08)"
    - "이미지 어댑터도 create_task/poll 을 노출한다 — adapter 내부 장시간 폴링 금지 (2차 리뷰 B2-02)"
    - "회전은 wan2.7-videoedit (amended D-04 — 회전의 유일 수단), 이미지는 31-01 RESULTS.json 의 chosen_model"
    - "v1 은 async-only 계약이다 — create_task 가 sync succeeded 를 즉시 반환하면 production 미허용 (async taskId 필수). 어댑터는 sync 여부를 IMAGE_ENGINE_SYNC 로 방출하고, 릴리스 게이트(31-01/13)가 sync-only 후보를 blocked 처리한다 (4차 리뷰 B4-02)"
    - "redirect 차단은 HTTPRedirectHandler subclass 명시 전달 + ProxyHandler({}) 로 구현되고, self-signed TLS local server + 테스트 전용 SSL context 주입으로 실제 301/302/303/307/308 응답이 redirect handler 에 도달하는 통합 테스트가 존재한다 — production allow_http 인자 금지 (2차 H2-04 + 3차 H3-05)"
    - "벤더 다운로드는 /tmp 파일 streaming (누적 cap + sha256) — 대용량 RSS 측정은 fresh subprocess + 플랫폼 단위 정규화 + 구조화 JSON (2차 H2-05 + 3차 H3-06)"
    - "judge/pose 공용 safe_decode_image 가 format allowlist + MAX_IMAGE_PIXELS + width/height/pixel cap 을 decode 전/직후 검사한다 — 압축 20MB 이하 대해상도 bomb 도 decode 전 거부, judge 는 typed 실패로 종결 (4차 리뷰 H4-06)"
    - "품질 judge 는 before/after 두 이미지를 함께 받아 typed 축으로 판정한다 — 판정 임계값은 인자 주입 (calibration 은 31-13 — 3차 H3-02). prepare_judge_payload 는 judge_corrected_pose 내부에서만 호출되고 worker 는 JudgeInputTooLargeError 만 catch 한다 (4차 리뷰 M4-02)"
    - "judge 요청은 정규화 파생 이미지(safe_decode_image → EXIF 제거·최대 변 2048px·bounded JPEG)로 만들어지고 POST 전 body byte 내부 상한 16MB 를 강제한다 — 초과 = typed JudgeInputTooLargeError (3차 H3-03, Gemini 공식 20MB)"
    - "judge_display_pass / judge_training_pass 는 각각 min_confidence 인자를 받는다 — 31-09 가 DISPLAY_JUDGE_CONFIDENCE / TRAINING_JUDGE_CONFIDENCE env 로 별개 주입 (4차 리뷰 B4-05)"
    - "judge 런타임은 Lambda 에서 실행 가능하다 — google-genai/pydantic SDK 미반입, stdlib urllib Gemini REST 만"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/visual_gen.py"
      provides: "typed 결과 모델 + Wan 어댑터 2종(create_task/poll) + safe_decode_image(공용 bomb 방어) + download_vendor_asset + prepare_judge_payload(정규화+16MB) + judge_corrected_pose(threshold 인자식) + judge_display_pass/judge_training_pass + PROMPT_VERSION"
      contains: "def safe_decode_image"
      min_lines: 260
    - path: "backend/shared/python/sunity_shared/analysis/interfaces.py"
      provides: "ImageEditEngine / VideoEditEngine Protocol (양쪽 create_task/poll)"
      contains: "class ImageEditEngine"
    - path: "backend/tests/phase31/test_visual_gen.py"
      provides: "어댑터·다운로드 경계(실 30x TLS 서버 포함)·safe_decode_image·judge·정규화 테스트"
      min_lines: 220
  key_links:
    - from: "backend/shared/python/sunity_shared/analysis/visual_gen.py"
      to: "dashscope-intl.aliyuncs.com"
      via: "stdlib urllib _http_json"
      pattern: "dashscope-intl"
    - from: "backend/shared/python/sunity_shared/analysis/visual_gen.py"
      to: "generativelanguage.googleapis.com"
      via: "stdlib urllib judge REST 호출 (google-genai SDK 금지 — Lambda 250MB)"
      pattern: "generativelanguage"
---

<objective>
외부 생성 API 계층을 구현한다: typed 결과 모델(M-08), Protocol 2종(이미지·영상 모두 create_task/poll — B2-02), Wan 어댑터 2종, 다운로드 경계(파일 스트리밍 — H2-05/H3-06), before/after judge(H-03/H3-03). 전부 stdlib urllib (신규 패키지 0).

4차 리뷰 반영:
- **B4-02 (async-only v1):** create_task 가 sync 모델일 때 succeeded VendorPollResult 를 즉시 반환하면 taskId 가 없어 fetch 가 재-poll 로 fresh URL 을 얻을 수 없다. v1 정책 = **async taskId 지원 모델만 production 허용**. 어댑터는 `IMAGE_ENGINE_SYNC` bool 을 방출하고(RESULTS.json.sync 반영), 릴리스 게이트(31-01/13)가 sync-only 후보를 blocked 처리한다. 계약 docstring 박제: "sync succeeded 즉시반환 = production 미허용 — create action 은 async VendorTaskCreated(taskId) 만 정상 경로로 취급 (sync 지원은 future phase: same-invocation staging 완료 후 judging outbox — v1 미포함)."
- **H4-06 (공용 bomb 방어):** `safe_decode_image(data: bytes, *, allowed_formats, max_decoded_bytes, max_pixels, max_edge) -> PIL.Image` — decode 전 len(data) cap + PIL `Image.MAX_IMAGE_PIXELS` 설정으로 open, open 직후 format allowlist + width/height/pixel cap 재검사. judge(prepare_judge_payload)와 pose(31-06 _normalize_for_pose)가 공용 import — bomb 은 Gemini/Pod 호출 전에 typed 로 거부.
- **H3-02:** judge 임계값은 인자 주입식 — `judge_display_pass(verdict, *, min_confidence)` / `judge_training_pass(verdict, *, min_confidence)`. 임계값 자체는 31-13 harness 가 grid 로 선택. 모듈 내 판정 리터럴 금지.
- **H3-03 + M4-02:** `prepare_judge_payload` 는 safe_decode_image → 재인코딩(EXIF 제거 + 최대 변 2048px + JPEG q85) 후 직렬화 body 를 측정해 `JUDGE_BODY_MAX_BYTES = 16MB` 강제 — 초과는 typed `JudgeInputTooLargeError`. **prepare_judge_payload 는 judge_corrected_pose 내부에서만 호출 (worker 이중 decode 금지 — M4-02).**
- **H3-05/H3-06:** redirect 통합 테스트 self-signed TLS, RSS fresh subprocess 실측.

Purpose: 31-09 워커가 공유하는 유일한 외부 API 코드 경로 — 코드 1벌 + 테스트 고정된 보안·payload 경계.
Output: interfaces.py Protocol + visual_gen.py + mock/통합 테스트.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-api-visual-correction/31-PATTERNS.md
@.planning/phases/31-api-visual-correction/31-ACCEPTANCE.md
@.planning/phases/31-api-visual-correction/smoke/RESULTS.json
@.planning/spikes/004-gemini-omni-view-editing/wan_gate_batch.py
---
@.planning/phases/31-api-visual-correction/31-PLAN-REVIEW-ITERATION4.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: typed 결과 모델 + Protocol(create_task/poll 통일) + Wan 어댑터 2종 + async-only 방출</name>
  <files>backend/shared/python/sunity_shared/analysis/interfaces.py, backend/shared/python/sunity_shared/analysis/visual_gen.py, backend/tests/phase31/test_visual_gen.py</files>
  <read_first>backend/shared/python/sunity_shared/analysis/interfaces.py (전체 — Protocol 선언 + "구현은 형제 모듈" docstring 규칙), .planning/spikes/004-gemini-omni-view-editing/wan_gate_batch.py (http_json lines 24-56 + create body lines 87-128 + 폴링/종료 상태/모더레이션 응답 형상), .planning/phases/31-api-visual-correction/smoke/RESULTS.json (chosen_model/endpoint/request_params/sync/latency_s), backend/shared/python/sunity_shared/models.py (VISUAL_FAILURE_REASONS — 오류 카테고리 단일 출처), backend/tests/phase31/conftest.py (dashscope_urlopen_mock)</read_first>
  <action>visual_gen.py 신설 (docstring: D-02/D-08 + spike 008 + M-08/H-05 + B2-02 + H3-01 + 4차 B4-02 인용 — "poll 결과 output URL 은 반환값으로만 흐른다: 호출측이 Firestore/메시지/로그 기록 금지"). typed 모델 (frozen dataclass): `VendorTaskCreated(task_id: str, request_id: str | None)`, `VendorPollResult(state: str, output_url: str | None, failure_reason: str | None)` — state 는 'pending'|'succeeded'|'failed'|'blocked', failure_reason 은 models.VISUAL_FAILURE_REASONS 값만. interfaces.py Protocol 2종 — 양쪽 create_task/poll (B2-02): `ImageEditEngine.create_task(self, image_url, prompt) -> VendorTaskCreated | VendorPollResult` (**async 모델 = VendorTaskCreated(taskId) 반환 → 워커 journal 후 poll; sync 모델 = VendorPollResult 즉시 반환 — 그러나 v1 production 미허용, docstring 박제 (B4-02)**) + `poll(self, task_id) -> VendorPollResult`; `VideoEditEngine` 동형. `_http_json(url, key, body=None, async_header=False)` — wan_gate_batch 형상 (Authorization Bearer, X-DashScope-Async: enable, timeout 60, 방어적 JSON, 예외 문자열에 키 미포함). BASE = "https://dashscope-intl.aliyuncs.com". `WanImageAdapter` — RESULTS.json chosen_model/endpoint/request_params 반영, 내부 while/폴링 루프 금지(모듈 소스 assert), 모더레이션 → 'blocked'/'moderation'; **`IMAGE_ENGINE_SYNC = bool(RESULTS.json.sync)` + `IMAGE_ENGINE_BLOCKED = True` (RESULTS.json blocked=true 또는 sync-only 시) 방출 — 31-12/31-13 게이트 소비 (B4-02)**. `WanVideoEditAdapter` — model 'wan2.7-videoedit', create /api/v1/services/aigc/video-generation/video-synthesis, parameters resolution 720P / watermark False / prompt_extend False / seed 42, poll /api/v1/tasks/{task_id}, 모더레이션 → 'blocked'. `MODERATION_RETRY_MAX = 1` 상수. 키 인자 주입. test_visual_gen.py: create→task_id, SUCCEEDED→output_url, 모더레이션 typed 매핑, 예외/비정상 JSON→'vendor_error', caplog 키 부재, `requests` import 부재, 이미지 어댑터 소스에 sleep/폴링 루프 부재 assert (B2-02), **IMAGE_ENGINE_SYNC 가 RESULTS.json.sync 반영 + sync 후보에서 IMAGE_ENGINE_BLOCKED True (B4-02)**.</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31/test_visual_gen.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - interfaces.py 에 ImageEditEngine/VideoEditEngine 둘 다 create_task + poll 선언 (이미지 단발 generate 메서드 부재)
    - WanImageAdapter 에 내부 폴링 루프 0 (time.sleep 부재 assert — B2-02)
    - IMAGE_ENGINE_SYNC / IMAGE_ENGINE_BLOCKED 방출 + sync-only → blocked 테스트 고정 (B4-02)
    - visual_gen.py 반환형 전부 frozen dataclass, failure_reason ⊆ models.VISUAL_FAILURE_REASONS 테스트 고정
    - wan2.7-videoedit + watermark False 존재, requests import 0
  </acceptance_criteria>
  <done>엔진 계층이 taskId journal 가능한 typed 계약 + async-only 게이트 방출로 고정됨.</done>
</task>

<task type="auto">
  <name>Task 2: safe_decode_image + download_vendor_asset — 공용 bomb 방어 + TLS redirect 통합 + subprocess RSS</name>
  <files>backend/shared/python/sunity_shared/analysis/visual_gen.py, backend/tests/phase31/test_visual_gen.py</files>
  <read_first>.planning/phases/31-api-visual-correction/31-PLAN-REVIEW-ITERATION4.md 의 H4-05/H4-06 절 + 31-PLAN-REVIEW-ITERATION2.md 의 H2-04/H2-05 절 + 31-PLAN-REVIEW-ITERATION3.md 의 H3-05/H3-06 절 (요구 목록 원문 — 테스트 케이스의 단일 기준)</read_first>
  <action>(1) **`safe_decode_image(data: bytes, *, allowed_formats: tuple[str,...], max_decoded_bytes: int, max_pixels: int, max_edge: int) -> PIL.Image.Image` (H4-06 공용 방어)**: len(data) > max_decoded_bytes → typed `ImageDecodeError('too_large')`; `Image.MAX_IMAGE_PIXELS = max_pixels` 설정 후 `Image.open(BytesIO(data))` — DecompressionBombError/UnidentifiedImageError → ImageDecodeError('bomb'|'unreadable'); open 직후 img.format not in allowed_formats → ImageDecodeError('bad_format'); max(w,h) > max_edge 또는 w*h > max_pixels → ImageDecodeError('bad_dimension'). judge/pose(31-06) 공용. (2) `@dataclass(frozen=True) class DownloadedAsset(path, sha256, size_bytes, content_type)` + `download_vendor_asset(url, dest_path, *, max_bytes, allowed_content_types, timeout_s=60) -> DownloadedAsset` — dest_path(/tmp) 64KB 청크 streaming write + hashlib.sha256 누적 + cap 초과 즉시 중단·파일 삭제 (H2-05). 실패 typed `VendorDownloadError(reason)` — 'bad_scheme'|'bad_host'|'redirect'|'private_ip'|'bad_content_type'|'too_large'|'timeout'. 규칙: (1) scheme=='https' — **production allow_http 인자 금지 (H3-05)**; (2) `_ALLOWED_HOST_SUFFIXES=("aliyuncs.com",)` 정확 매칭; (3) getaddrinfo IP 전부 사설/링크로컬/루프백/예약 거부; (4) redirect 차단 (H2-04): `class _NoRedirectHandler(urllib.request.HTTPRedirectHandler)` — redirect_request + http_error_301/302/303/307/308 전부 즉시 raise, `build_opener(_NoRedirectHandler(), ProxyHandler({}))` 명시 전달; (5) Content-Type 허용목록 접두; (6) timeout; (7) URL query/서명 비로깅. 테스트 전용 kwargs: `_test_allowed_hosts`, `_test_allow_private`, `_test_ssl_context` (H3-05 — 통합 테스트 self-signed context 주입; production 미사용, 31-09 acceptance grep).
테스트: (mock) https 아님/evilaliyuncs.com/사설 IP/content-type/max_bytes 초과/정상 스트리밍+sha256; **(통합 — H3-05): self-signed 인증서(사전 PEM fixture 또는 openssl subprocess) → ThreadingHTTPServer ssl wrap → https 127.0.0.1 로 301/302/303/307/308 각각 요청 (_test_ssl_context 주입) → 전부 VendorDownloadError('redirect') — redirect handler 실제 도달**; **(safe_decode_image — H4-06): 압축 작지만 20000×20000 PNG bomb → ImageDecodeError('bomb'|'bad_dimension') + PIL open 이후 추가 처리 0; bad_format(GIF)·oversize decoded·과대 edge 각각 typed**; **(RSS — H3-06): 200MB 급 다운로드를 fresh subprocess(`subprocess.run([sys.executable,'-c',...])`)로 실행 — ru_maxrss 를 플랫폼 정규화(darwin bytes / linux KiB→bytes) 후 {"baseline_bytes","peak_bytes","delta_bytes","platform"} JSON stdout → 부모가 파싱 delta<64MB assert. 동일 프로세스 ru_maxrss delta 금지. SUMMARY 에 실측값 + "IaC MemorySize 는 Linux/container 측정 기준" 박제**.</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31/test_visual_gen.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - safe_decode_image 가 decode 전 size cap + MAX_IMAGE_PIXELS + format/dimension cap — bomb typed 거부 테스트 green (H4-06)
    - download_vendor_asset 가 파일 경로 스트리밍 + DownloadedAsset(sha256/size) 반환
    - _NoRedirectHandler subclass + ProxyHandler({}) 존재, production allow_http 부재 (H3-05)
    - TLS local server 30x 5종 통합 테스트 green — redirect handler 실제 도달 (H3-05)
    - RSS 측정이 fresh subprocess + 플랫폼 정규화 + 구조화 JSON (동일 프로세스 delta 0 — H3-06)
  </acceptance_criteria>
  <done>공용 bomb 방어 + 벤더 다운로드 경계가 H4-06 + H-05 + H2-04/H2-05 + H3-05/H3-06 전 항목 테스트로 고정됨.</done>
</task>

<task type="auto">
  <name>Task 3: prepare_judge_payload + judge_corrected_pose — safe_decode·16MB·threshold 인자식 (H-03 + H3-02/H3-03 + M4-02)</name>
  <files>backend/shared/python/sunity_shared/analysis/visual_gen.py, backend/tests/phase31/test_visual_gen.py</files>
  <read_first>backend/functions/pipeline/requirements.txt (2026-06-12 박제 주석 — google-genai transitive ~100MB Lambda 250MB 초과 → 배제), backend/shared/python/sunity_shared/gemini/client.py lines 1-30 (Pod 전용 확인 — 프롬프트/스키마 관례 참고만, 재사용 금지), .planning/phases/31-api-visual-correction/31-PLAN-REVIEW-ITERATION4.md 의 H4-06/M4-02 절 + 31-PLAN-REVIEW-ITERATION3.md 의 H3-02/H3-03 절</read_first>
  <action>(1) **`prepare_judge_payload(before_png: bytes, after_png: bytes, prompt: str) -> bytes` (H3-03 + H4-06)**: 각 이미지를 **safe_decode_image(allowed_formats=('PNG','JPEG'), max_decoded_bytes=20MB, max_pixels=16M, max_edge=8192)** 로 decode(bomb 사전 차단) → 최대 변 2048px 초과 시 LANCZOS 다운스케일 + EXIF 제거 재인코딩 + JPEG q85 (파생 판정용 — canonical 결과 불변). Gemini generateContent body dict (contents.parts = [text "BEFORE (원본)", inline_data base64 jpeg, text "AFTER (생성)", inline_data base64 jpeg, prompt] + generationConfig responseMimeType/responseSchema) json.dumps → **len(body_bytes) > `JUDGE_BODY_MAX_BYTES = 16*1024*1024` → typed `class JudgeInputTooLargeError(Exception)` (Gemini 공식 20MB 대비 여유 — docstring URL 인용)**. safe_decode 실패(ImageDecodeError)도 JudgeInputTooLargeError 로 승격(호출측이 'judge_input_too_large' 로 종결 — bomb·과대 입력 동일 취급). 정상 시 직렬화 bytes.
(2) `judge_corrected_pose(before_png, after_png, context, *, api_key=None) -> JudgeVerdict | None` — **내부에서 prepare_judge_payload 를 단일 호출 (M4-02 — worker 는 prepare 를 선호출하지 않는다; JudgeInputTooLargeError 만 전파, None 으로 뭉개지 않음)**. stdlib urllib POST `https://generativelanguage.googleapis.com/v1beta/models/gemini-3.5-flash:generateContent`. 키 = api_key 우선, 미제공 시 env GEMINI_API_KEY; `x-goog-api-key` 헤더 (URL query 금지). responseSchema = 7 bool + confidence number + reason string (dict 수동 검증, pydantic 불사용). timeout 60s + 5xx/timeout 1회 재시도. fail-closed: 오류/파싱/스키마/키 미설정 → None. `PROMPT_VERSION = "judge-v1"` 상수 (31-13 meta 재현성).
(3) **`@dataclass(frozen=True) class JudgeVerdict`** — identity_ok/clothing_ok/background_ok/pole_ok/single_person_ok/no_extra_limbs/correction_visible(bool 7축) + confidence + reason. **threshold 인자식 (H3-02 — 리터럴 금지): `judge_display_pass(verdict, *, min_confidence) -> bool` — hard rejection(인물 미검출/복수/사지 이상) confidence 무관 False, 그 외 7축 true AND confidence>=min_confidence; `judge_training_pass(verdict, *, min_confidence) -> bool` — display 조건 + 더 높은 min_confidence. 31-09 가 DISPLAY_JUDGE_CONFIDENCE / TRAINING_JUDGE_CONFIDENCE env 로 각각 주입 (B4-05).**
테스트: prepare_judge_payload — 대형(각 12MB) → 정규화 후 16MB 이하 통과, 극단(정규화 후 초과 mock) → JudgeInputTooLargeError, bomb 입력 → JudgeInputTooLargeError(safe_decode 승격) + Gemini POST 0; EXIF 입력 → 출력 EXIF 부재; 2048 다운스케일 확인; 정상 JSON → 축별 판정 (min_confidence 경계 ±0.01) + display/training 다른 임계값 동일 verdict 에 다른 결과, hard rejection 우선, HTTP 500 → 1회 재시도 후 None, malformed/축 누락/키 미설정 → None(네트워크 0), 요청 URL 키 부재 + caplog 키 부재, 모듈 소스에 google.genai/pydantic import 부재 + 판정 임계값 리터럴(0.7/0.85 류) 부재 assert.</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31 -x -q</automated>
  </verify>
  <acceptance_criteria>
    - prepare_judge_payload 가 safe_decode_image 경유 + 정규화 + 16MB 상한 + JudgeInputTooLargeError (H3-03 + H4-06)
    - judge_corrected_pose 가 prepare 를 내부 단일 호출 — worker 선호출 경로 부재 (M4-02)
    - judge_display_pass/judge_training_pass 가 min_confidence 인자식 — 모듈 판정 리터럴 0 (H3-02/B4-05)
    - JudgeInputTooLargeError 가 None 으로 뭉개지지 않고 전파
    - visual_gen.py 에 google.genai / pydantic import 0, 'gemini-3.5-flash' + x-goog-api-key
    - `python -m pytest backend/tests -q` exits 0
  </acceptance_criteria>
  <done>보존 판정이 공용 safe_decode + 단일 prepare 호출 + typed 축 + payload 경계 + 인자식 임계값(display/training 분리)으로 고정 — 31-13 calibration 과 31-09 워커가 같은 함수 소비.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend → DashScope | presign URL 전송 + 응답 신뢰 불가 |
| DashScope 응답 URL → backend | SSRF/redirect/오버사이즈/오호스트 위험 지점 |
| 외부 이미지 bytes → PIL decode | decompression bomb / 위장 포맷 방어 (safe_decode_image) |
| backend → Gemini REST | 정규화 파생 이미지 판정 (stdlib urllib — SDK 미반입) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-15 | Information Disclosure | DASHSCOPE/GEMINI_API_KEY | mitigate | 키 인자/env + x-goog-api-key 헤더 + 로그/예외 미포함 + SSM SecureString |
| T-31-16 | Injection / SSRF | 벤더 응답 URL | mitigate | https/정확 호스트/_NoRedirectHandler/ProxyHandler({})/사설 IP 차단 — 실 TLS 30x 통합 (H-05 + H2-04 + H3-05) |
| T-31-17 | Denial of Service | 오버사이즈 응답/요청/bomb | mitigate | /tmp 스트리밍 cap + safe_decode_image + subprocess RSS + judge 16MB (H2-05 + H3-03/H3-06 + H4-06) |
| T-31-18 | Tampering | 왜곡 생성물 통과 | mitigate | before/after judge + hard rejection + 불확실=None (H-03) |
| T-31-19 | Denial of Service (과금) | 모더레이션 재시도 / adapter 내부 폴링 / sync taskId 부재 고아 | mitigate | MODERATION_RETRY_MAX=1 + create_task/poll 통일 + async-only 게이트 (B2-02 + B4-02) |
</threat_model>

<verification>
- `python -m pytest backend/tests -q` green
- visual_gen.py 에 API 키 리터럴·requests·google.genai·pydantic·내부 폴링 루프·판정 임계값 리터럴 0
</verification>

<success_criteria>
- typed 엔진 계층(create_task/poll + async-only 방출) + 공용 safe_decode + 다운로드 경계(H3-05/H3-06) + judge(H3-02 인자식/H3-03 정규화·16MB/M4-02 단일 호출) 전부 mock/통합 테스트 고정
- 4차 리뷰 §8 gate 4(async-only) 의 어댑터 방출 지점 + §6 bomb 테스트
- 31-09 워커와 31-13 calibration 이 이 모듈만 import
</success_criteria>

<output>
Create `.planning/phases/31-api-visual-correction/31-05-SUMMARY.md` when done
</output>
</content>
</invoke>
