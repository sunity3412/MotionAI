---
phase: 31-api-visual-correction
plan: 06
type: execute
wave: 2
depends_on: [31-02, 31-03]
files_modified:
  - backend/runpod_inference/server.py
  - backend/shared/python/sunity_shared/analysis/pose_gate.py
  - backend/tests/phase31/test_pose_gate.py
autonomous: true
requirements: [D-03, D-05]
must_haves:
  truths:
    - "생성 이미지에 결정론 pose 재측정 게이트가 존재한다 — 생성형 judge 만으로 목표각 반영을 판정하지 않는다 (리뷰 H-03)"
    - "pose 게이트 불가(Pod 미가용 등) 시 fail-closed — typed 실패 'pose_gate_unavailable' 로 미노출 처리 (D-08 조용한 폴백이 흡수)"
    - "Pod 엔드포인트는 기존 X-RunPod-Token 인증·기존 pose estimator 를 재사용한다 — 신규 모델/패키지 0"
  artifacts:
    - path: "backend/runpod_inference/server.py"
      provides: "POST /pose-image — 단일 이미지 keypoint 추정"
      contains: "pose-image"
    - path: "backend/shared/python/sunity_shared/analysis/pose_gate.py"
      provides: "measure_generated_pose — 관절각 재측정 + 허용오차 판정"
      contains: "pose_gate_unavailable"
    - path: "backend/tests/phase31/test_pose_gate.py"
      provides: "각도 산출·허용오차·불가 fail-closed 테스트"
      min_lines: 60
  key_links:
    - from: "backend/shared/python/sunity_shared/analysis/pose_gate.py"
      to: "backend/runpod_inference/server.py"
      via: "RUNPOD_ANALYZE_URL 파생 /pose-image HTTP 호출"
      pattern: "pose-image"
---

<objective>
리뷰 H-03 의 결정론 백스톱을 구현한다: 생성된 교정 이미지의 목표 관절각을 실제 pose 추정으로 재측정해 선언된 허용오차(31-ACCEPTANCE — display 15° / training 8°)와 대조하는 게이트. 추정은 Pod 의 기존 estimator 를 얇은 엔드포인트로 재사용한다 (Lambda 에 GPU/onnx 반입 금지 — 신규 패키지 0 유지). correctedPose 잡은 신규 분석 직후(Pod 가동 중)에만 발생하므로 Pod 의존이 가용성 제약을 새로 만들지 않는다. Pod 미가용 시 fail-closed: typed 'pose_gate_unavailable' → 이미지 미노출 (D-08).

ARROW_JOINT_MAP 은 31-03(fault_zoom.py)이 신설하는 단일 출처를 import 재사용 — depends_on 31-03 (checker 지적 반영).

Purpose: "그럴듯하지만 기하가 틀린" 생성 이미지의 사용자 노출·학습 적재를 결정론적으로 차단.
Output: Pod /pose-image 엔드포인트 + pose_gate.py + 테스트.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-api-visual-correction/31-ACCEPTANCE.md
@.planning/phases/31-api-visual-correction/31-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pod POST /pose-image 엔드포인트</name>
  <files>backend/runpod_inference/server.py</files>
  <read_first>backend/runpod_inference/server.py (전체 — X-RunPod-Token 인증 패턴, pipeline 모듈 로딩 lock, Pydantic request 모델 관례, /analyze·/health 형상), backend/shared/python/sunity_shared/analysis/interfaces.py (PoseEstimator Protocol — 입력 프레임 배열 계약), backend/shared/python/sunity_shared/analysis/pose_estimator.py (estimator 의 단일 프레임 처리 가능 형태)</read_first>
  <action>server.py 에 `POST /pose-image` 추가 — 기존 토큰 인증 데코레이터/검사 재사용. Request(Pydantic): {imageB64: str} (PNG/JPEG base64, 크기 상한 8MB — 초과 413). 처리: base64 decode → PIL open → 단일 프레임 배열 (1,H,W,3) 구성 → pipeline 모듈의 기존 pose estimator 싱글턴으로 keypoint 추정 (모듈 로딩 lock 기존 패턴 준수 — 분석과 동시 실행 시 GPU 경합은 순차 처리로 수용, BackgroundTask 불필요: 동기 응답). Response: {ok: bool, keypoints: [[x,y,score],...] COCO-17 정규화 좌표, error: str | null}. 인체 미검출/추정 실패 = ok false + error (500 아님 — 게이트 판정용 정상 응답). 로깅 key=value, 이미지 바이트 미로깅. 이 엔드포인트는 채점 경로와 완전 무접촉 (display 게이트 전용 — 점수 반영 금지 invariant 주석).</action>
  <verify>
    <automated>python -c "import ast; src=open('backend/runpod_inference/server.py').read(); ast.parse(src); assert 'pose-image' in src and 'imageB64' in src"</automated>
  </verify>
  <acceptance_criteria>
    - server.py 에 /pose-image 라우트 + 토큰 인증 재사용 존재
    - 인체 미검출이 ok:false 정상 응답 (예외 전파 아님)
    - 채점 모듈 호출 0 (estimator 만)
  </acceptance_criteria>
  <done>Pod 가 단일 이미지 keypoint 를 반환하는 얇은 게이트 백엔드 완성 (실기동 검증은 31-12 Pod 재생성 후).</done>
</task>

<task type="auto">
  <name>Task 2: pose_gate.py — 관절각 재측정 + fail-closed 판정</name>
  <files>backend/shared/python/sunity_shared/analysis/pose_gate.py, backend/tests/phase31/test_pose_gate.py</files>
  <read_first>backend/shared/python/sunity_shared/analysis/fault_zoom.py 의 ARROW_JOINT_MAP (31-03 — proximal/vertex/distal 트리플의 단일 출처: 동일 매핑 재사용, 중복 선언 금지), backend/shared/python/sunity_shared/analysis/skeleton.py (COCO-17 이름), backend/tests/phase31/conftest.py (HTTP mock)</read_first>
  <action>신규 pose_gate.py (docstring: 리뷰 H-03 + 31-ACCEPTANCE 임계값 인용): `@dataclass(frozen=True) class PoseGateResult(passed: bool, measured_deg: float | None, error_deg: float | None, reason: str | None)` — reason 은 None|'pose_gate_unavailable'|'pose_gate_failed'|'no_person'. `measure_generated_pose(image_bytes: bytes, *, joint_key: str, target_deg: float, tolerance_deg: float, pose_url: str, token: str, timeout_s: int = 60) -> PoseGateResult`: (1) pose_url 로 stdlib urllib POST (X-RunPod-Token 헤더, imageB64) — 연결 실패/타임아웃/비200 → passed False + reason 'pose_gate_unavailable' (fail-closed, 리뷰 H-03); (2) ok false → 'no_person' + False; (3) fault_zoom.ARROW_JOINT_MAP 의 (proximal, vertex, distal) 로 keypoint 3점 추출 (미매핑 joint → 'pose_gate_failed'), 3점 중 score < 0.3 존재 → 'pose_gate_failed'; (4) numpy 로 vertex 내각 계산 (arccos of normalized dot) → error_deg = |measured − target_deg| → passed = error_deg <= tolerance_deg. pose_url 파생 헬퍼 `derive_pose_url(analyze_url: str) -> str` — 기존 env RUNPOD_ANALYZE_URL 의 path 를 /pose-image 로 치환 (신규 SSM 파라미터 불필요). test_pose_gate.py: 합성 keypoint 응답으로 각도 산출 정확성 (90°/180° 케이스 ±0.5°), tolerance 경계 (오차 15.1° fail / 14.9° pass), 연결 실패 → 'pose_gate_unavailable' + passed False, no person → False, 저신뢰 keypoint → 'pose_gate_failed', derive_pose_url 치환 검증.</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31/test_pose_gate.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - pose_gate.py 가 fault_zoom.ARROW_JOINT_MAP 을 import 재사용 (자체 트리플 선언 0)
    - 게이트 불가 → passed False + 'pose_gate_unavailable' 테스트 green (fail-closed)
    - 각도 산출·tolerance 경계 테스트 green
    - `python -m pytest backend/tests/phase31 -q` exits 0
  </acceptance_criteria>
  <done>결정론 pose 게이트가 typed·fail-closed 로 고정 — 31-09 워커가 display/training 허용오차로 각각 호출.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker → Pod /pose-image | 토큰 인증 내부 호출 — 이미지 바이트 전송 |
| pose 응답 → 게이트 판정 | 저신뢰/결측 keypoint 방어 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-20 | Spoofing | /pose-image 접근 | mitigate | 기존 X-RunPod-Token 인증 재사용 — 무토큰 401 |
| T-31-21 | Tampering | 기하 틀린 생성물 통과 | mitigate | 결정론 각도 재측정 + tolerance — judge 단독 판정 금지 (리뷰 H-03) |
| T-31-22 | Denial of Service | 대형 이미지 업로드 | mitigate | imageB64 8MB 상한 413 |
| T-31-23 | Repudiation | 게이트 불가 시 통과 | mitigate | fail-closed 'pose_gate_unavailable' — 불가는 통과가 아님 (테스트 고정) |
</threat_model>

<verification>
- `python -m pytest backend/tests -q` green
- pose_gate.py 에 채점 모듈(dimensions/kismam) import 0
</verification>

<success_criteria>
- 결정론 백스톱이 Pod 재사용·신규 패키지 0 으로 구현, fail-closed 규칙 테스트 고정
- ARROW_JOINT_MAP 단일 출처 유지
</success_criteria>

<output>
Create `.planning/phases/31-api-visual-correction/31-06-SUMMARY.md` when done
</output>
