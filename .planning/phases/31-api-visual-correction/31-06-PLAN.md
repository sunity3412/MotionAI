---
phase: 31-api-visual-correction
plan: 06
type: execute
wave: 3
depends_on: [31-01, 31-03, 31-04]
files_modified:
  - backend/shared/python/sunity_shared/analysis/pair_store.py
  - backend/functions/pipeline/app.py
  - backend/tests/phase31/test_pair_store.py
  - backend/tests/phase31/test_silhouette_flow.py
autonomous: false
requirements: [D-01, D-03, D-05, D-08]
must_haves:
  truths:
    - "분석 완료 시 top-1 결함 부위의 교정 실루엣 이미지가 자동 생성된다 (D-05 — 점수 확정을 블록하지 않는 사후 단계)"
    - "실루엣은 정지 이미지 1장 — 결함 순간 프레임에서 고친 폼 (D-03, 영상 승격은 deferred)"
    - "모더레이션 차단/judge FAIL 시 silhouetteStatus='failed' 만 기록되고 분석은 영향 없다 (D-08 조용한 폴백 — 재raise 0)"
    - "[틀린 폼→고쳐진 폼] 페어가 learningOptIn===true 사용자에 한해 S3 training/phase31/pairs/ 에 적재된다 (D-01 부산물 = phase 22 플라이휠 원료; false/부재 = 적재 금지)"
    - "결함 0 분석은 실루엣 생성을 건너뛰고 상태 필드를 남기지 않는다 (부재=legacy 숨김)"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/pair_store.py"
      provides: "consent-gated 학습 페어 S3 적재 helper"
      contains: "learningOptIn"
    - path: "backend/functions/pipeline/app.py"
      provides: "_run_deferred_silhouette 사후 훅"
      contains: "_run_deferred_silhouette"
    - path: "backend/tests/phase31/test_pair_store.py"
      provides: "동의 게이트 단위 테스트 (false/None/true 3분기)"
      min_lines: 50
  key_links:
    - from: "backend/functions/pipeline/app.py"
      to: "backend/shared/python/sunity_shared/analysis/visual_gen.py"
      via: "SilhouetteGenEngine 어댑터 + judge_silhouette"
      pattern: "visual_gen"
    - from: "backend/functions/pipeline/app.py"
      to: "backend/shared/python/sunity_shared/firestore_admin.py"
      via: "update_analysis_silhouette 부분 업데이트"
      pattern: "update_analysis_silhouette"
    - from: "backend/shared/python/sunity_shared/analysis/pair_store.py"
      to: "S3 training/phase31/pairs/"
      via: "boto3 put_object (consent 통과 시만)"
      pattern: "training/phase31/pairs"
---

<objective>
D-05 실루엣 자동 생성을 `_process` 사후 단계로 구현한다 — `_run_deferred_fault_zoom` 선례 복제 (점수 완료 후, 어떤 경로도 재raise 하지 않음). 흐름: top-1 결함 관절의 순간 프레임(cached_user_frames + 기존 프레임 선택 경로) → S3 임시 put + presign 1h → 31-04 SilhouetteGenEngine.generate → Gemini judge → PASS 면 S3 `results/{uid}/{analysis_id}/silhouette_{joint}.png` + silhouetteStatus='done', FAIL/차단이면 'failed' (D-08 조용한 폴백). PASS + learningOptIn===true 면 pair_store 로 [before→after] 페어 적재 (D-01 플라이휠 원료). 얼굴 블러 여부는 checkpoint:decision (RESEARCH Open Q2).

Purpose: 첫 분석의 "전문가 수준" 인상 (core value 정합) + phase 22 재도전 학습 데이터 제조.
Output: pipeline 사후 훅 + pair_store 모듈 + 테스트.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-api-visual-correction/31-PATTERNS.md
@.planning/phases/31-api-visual-correction/31-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: 페어 적재 원본측 프레임 얼굴 블러 여부 (RESEARCH Open Q2)</name>
  <decision>learningOptIn=true 신규 페어의 원본(before) 프레임에 얼굴 블러(anonymize) 강제 여부</decision>
  <context>anonymize_batch.py 얼굴 블러 선례는 내부 데이터 LICENSE-AUDIT 에 적용됐다. 신규 opt-in 페어에도 강제할지 미결 — 블러하면 생성 헤드(phase 22 v2) 학습 품질에 영향 가능. 파일럿 학습 동의는 opt-out 방식으로 이미 확정([[learning-consent-pilot-mandatory]]) — 이 결정은 동의 여부가 아니라 동의된 데이터의 가공 수준.</context>
  <options>
    <option id="option-a">
      <name>블러 없이 적재 (권장 — RESEARCH Open Q2 권고)</name>
      <pros>생성 헤드 학습 품질 보존. S3 prefix 접근을 학습 파이프라인으로 한정 + manifest 에 consent provenance 기록으로 통제</pros>
      <cons>원본 얼굴이 학습 저장소에 남음 (동의 범위 내이나 가공 없음)</cons>
    </option>
    <option id="option-b">
      <name>anonymize_batch 얼굴 블러 후 적재</name>
      <pros>PII 최소화 일관 (내부 데이터 선례와 동일)</pros>
      <cons>블러 아티팩트가 v2 생성 헤드 학습 품질 저하 가능</cons>
    </option>
  </options>
  <resume-signal>option-a 또는 option-b 선택</resume-signal>
  <action>RESEARCH Open Q2 의 두 선택지(블러 없이 적재[권장] vs anonymize 블러)를 근거와 함께 belle 에게 제시하고 결정을 받는다. 결정 전 Task 2 의 pair_store 구현 착수 금지 (가공 수준이 구현을 바꿈).</action>
  <verify>
    <automated>N/A - checkpoint:decision (belle 응답이 게이트)</automated>
  </verify>
  <acceptance_criteria>
    - belle 이 option-a 또는 option-b 를 명시적으로 선택
  </acceptance_criteria>
  <done>페어 적재 가공 수준이 확정되어 Task 2 구현 기준이 됨.</done>
</task>

<task type="auto">
  <name>Task 2: pair_store.py — consent-gated 학습 페어 적재 helper</name>
  <files>backend/shared/python/sunity_shared/analysis/pair_store.py, backend/tests/phase31/test_pair_store.py</files>
  <read_first>backend/training/datagen/enumerate_internal.py lines 87-110 (consent_allows — strict 분기 형상), backend/training/datagen/build_jsonl.py lines 40-41 (S3 prefix 상수 관례), backend/tests/phase31/conftest.py (mock fixture)</read_first>
  <action>신규 모듈 pair_store.py (docstring: D-01 플라이휠 + [[learning-consent-pilot-mandatory]] + 2026-07-13 컷오프 이후 strict 인용). `PAIRS_PREFIX = "training/phase31/pairs/"` 상수. `store_training_pair(s3_client, bucket, *, uid, analysis_id, joint, before_png: bytes, after_png: bytes, learning_opt_in, meta: dict) -> bool`: 게이트 = `learning_opt_in is True` 만 통과 (False/None/그 외 타입 전부 즉시 False 반환, S3 호출 0 — consent_allows 의 strict 분기와 동형, bulk_approval 경로 사용 금지: phase 31 페어는 전부 신규 생성물). 통과 시 `{PAIRS_PREFIX}{analysis_id}_{joint}/` 하위에 before.png / after.png / meta.json 3객체 put — meta.json 에 consent provenance (learningOptIn true, analysisId, joint, model id, judge verdict, ts ms) 기록, uid 는 meta 에 넣지 않음 (S3 key 에도 uid 미포함 — 학습 저장소에서 사용자 역추적 최소화). Task 1 결정이 option-b 면 before_png 에 anonymize_batch 의 얼굴 블러 helper 적용 (import 재사용, 실패 시 적재 포기 False). 실패는 전부 False 반환 + log.warning (재raise 금지 — 적재는 부가 기능). test_pair_store.py 채움: False→S3 put 0회, None→0회, 문자열 "true"→0회, True→put 3회 + key prefix/메타 검증, put 예외→False.</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31/test_pair_store.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - pair_store.py contains `is True` strict 검사 (truthy 검사 금지)
    - learningOptIn False/None/비-bool 에서 S3 호출 0회가 테스트로 고정
    - S3 key 와 meta.json 에 uid 미포함
    - `python -m pytest backend/tests/phase31/test_pair_store.py -x -q` exits 0
  </acceptance_criteria>
  <done>동의 우회가 단위 테스트로 차단된 페어 적재 helper 완성.</done>
</task>

<task type="auto">
  <name>Task 3: pipeline _run_deferred_silhouette 사후 훅 (D-05/D-08)</name>
  <files>backend/functions/pipeline/app.py, backend/tests/phase31/test_silhouette_flow.py</files>
  <read_first>backend/functions/pipeline/app.py lines 3116-3158 (_run_deferred_fault_zoom — 골격 복제 대상 + 재raise 0 규율), lines 2764-2879 (_render_fault_zoom — cached_user_frames·프레임 선택·S3 put·_signed_get 패턴), backend/shared/python/sunity_shared/analysis/fault_zoom.py (select_confident_frame + vision sourceFrameIndices 경로 — 재추출 금지), backend/shared/python/sunity_shared/analysis/visual_gen.py (31-04 어댑터/judge 시그니처)</read_first>
  <action>pipeline/app.py 에 `_run_deferred_silhouette(*, uid, analysis_id, ...)` 신설 — _run_deferred_fault_zoom 과 동형 골격 (try 전체, 어떤 경로도 재raise 하지 않음), `_run_deferred_fault_zoom` 호출 직후에 실행. 내부 흐름: (1) top-1 결함 관절 결정 = 감점 records 중 최대 감점(points) 관절 1개 — 결함 0 이면 즉시 return (silhouetteStatus 미기록, 부재=숨김), (2) `update_analysis_silhouette(status='pending')` 선기록, (3) 결함 프레임 = cached_user_frames 에서 기존 fault_zoom 프레임 선택 경로 재사용 (재추출 금지 — Don't Hand-Roll), (4) before PNG 를 S3 임시 key `results/{uid}/{analysis_id}/_silhouette_src.png` put + presign 1h (전용 짧은 TTL — _PLAYBACK_EXPIRES 아님), (5) 교정 프롬프트 구성 — 관절명·direction·measured/target 각도 기반 영어 프롬프트 (31-02 스모크 프롬프트 형식, 신원·복장·폴·배경 보존 지시 포함), (6) `SilhouetteGenEngine.generate` (키 = env `DASHSCOPE_API_KEY` — Lambda 는 template resolve:ssm, Pod 는 env 주입) → None 이면 'failed', (7) 출력 URL 화이트리스트 검증 후 즉시 다운로드 → `judge_silhouette` → False 면 'failed', (8) PASS: S3 `results/{uid}/{analysis_id}/silhouette_{joint}.png` put (ContentType image/png) → `update_analysis_silhouette(status='done', item={"joint": joint, "imageUrl": _signed_get(...), "s3Key": skey})` — item 키명은 31-01 계약(silhouetteImageUrl/silhouetteKey/silhouetteJoint 매핑) 준수, (9) PASS 시에만 분석 문서의 learningOptIn 을 읽어 pair_store.store_training_pair 호출 (적재 실패도 status 에 영향 0). 전 실패 경로 = 'failed' 기록 시도 + log.warning (D-08 — 사용자 대면 에러 0). RunPod-delegate 경로 주의: 이 훅은 `_process` 내부이므로 Lambda/Pod 공용 — RunPod 서버측 신규 코드 0. test_silhouette_flow.py 에 추가: 결함 0 → 상태 미기록, generate None → failed, judge False → failed + 페어 미적재, PASS → done + 페어 적재 호출 (learningOptIn true 시만), 예외 발생 → 재raise 없음 + failed 마킹.</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31/test_silhouette_flow.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - pipeline/app.py contains `def _run_deferred_silhouette`
    - 결함 0 분석에서 silhouetteStatus 미기록이 테스트로 고정
    - judge False → 'failed' + store_training_pair 미호출이 테스트로 고정
    - 훅 내부 예외가 밖으로 전파되지 않음이 테스트로 고정
    - `python -m pytest backend/tests -q` exits 0
  </acceptance_criteria>
  <done>실루엣 자동 생성이 점수 비차단·조용한 폴백·동의 게이트까지 테스트로 고정됨.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사용자 프레임 → DashScope | 서비스 기능 수행 목적 전송 (D-01/D-05 내재) — presign 1h 최소 TTL |
| 생성물 → 학습 저장소 | 동의 게이트 통과분만 |
| DashScope 출력 → S3 | 화이트리스트 검증 후 다운로드 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-16 | Privacy | 페어 적재 동의 우회 | mitigate | `learning_opt_in is True` strict + false/None/비-bool S3 호출 0 테스트 고정 (파일럿 학습 동의 invariant) |
| T-31-17 | Information Disclosure | 입력 프레임 presign | mitigate | 전용 1h TTL presign (7일 _PLAYBACK_EXPIRES 미사용), 임시 key 는 results/{uid}/ 경계 내 |
| T-31-18 | Information Disclosure | 학습 저장소 역추적 | mitigate | pairs key/meta 에 uid 미포함, prefix 접근은 학습 파이프라인 한정 + provenance manifest |
| T-31-19 | Tampering | 왜곡 실루엣 노출 | mitigate | judge PASS 필수 — 불확실 = failed (31-04 보수 규칙), D-08 조용한 폴백 |
| T-31-SC | Tampering | 신규 패키지 설치 | accept | 이 phase 신규 패키지 0 (RESEARCH Package Legitimacy Audit — 설치 태스크 부재) |
</threat_model>

<verification>
- `python -m pytest backend/tests -q` green
- pipeline 훅이 fail_analysis 를 호출하지 않음 (부분 업데이트만 — grep 확인)
</verification>

<success_criteria>
- D-05 자동 생성 사후 훅 + D-08 조용한 폴백 + D-01 페어 적재가 전부 mock 테스트로 고정
- 블러 결정(checkpoint) 반영
- 실 E2E 는 Pod 재생성 후 (31-09 에서 HUMAN-UAT 적립 — Pod OFF 는 구현을 막지 않음)
</success_criteria>

<output>
Create `.planning/phases/31-api-visual-correction/31-06-SUMMARY.md` when done
</output>
