---
phase: 31-api-visual-correction
plan: 07
type: execute
wave: 2
depends_on: [31-01, 31-02]
files_modified:
  - backend/shared/python/sunity_shared/analysis/pair_store.py
  - backend/scripts/delete_training_pair.py
  - backend/tests/phase31/test_pair_store.py
autonomous: true
requirements: [D-01]
must_haves:
  truths:
    - "[틀린 폼→고쳐진 폼] 페어가 learningOptIn is True 사용자에 한해 적재된다 — false/부재/비-bool 은 S3 호출 0 (D-01 + [[learning-consent-pilot-mandatory]])"
    - "페어 저장은 commit-marker 방식 — before/after 먼저 put, meta.json 이 마지막 commit marker. 부분 실패 시 이번 호출 생성 object 를 best-effort 삭제하고, consumer 는 marker 있는 pair 만 연다 (3차 리뷰 H3-07)"
    - "consumer helper 가 코드로 강제한다 — list_committed_pairs/load_committed_pair 가 meta.json 존재+schema 검증을 단일 경로로 수행하고, meta 없는 before/after prefix 는 enumeration 에서 제외한다 (4차 리뷰 H4-11)"
    - "페어 ID 는 HMAC 가명 — S3 key·메타 어디에도 uid/analysisId 원문이 없다 (리뷰 H-04)"
    - "HMAC key set 은 strict schema validator 하나가 검증한다 — active in keys, bytes.fromhex 32바이트, version ID 형식; store/delete/dry-run(31-10) 공용 (3차 리뷰 H3-11)"
    - "rotation 은 기존 key 삭제 없이 active 추가만 — 삭제 전 inventory 의 hmacKeyVersion 전부가 key set 에 존재하는지 gate (H3-11)"
    - "삭제 스크립트는 versioned bucket 에서도 완전 삭제한다 — list_object_versions 로 Versions/DeleteMarkers 전부 versionId 삭제 + current/noncurrent 검증 (3차 리뷰 H3-08)"
    - "삭제 대상 joint 집합은 append-only historical registry 상수 (3차 리뷰 M3-05)"
    - "consentVersion/blurOption 은 배포 상수 — privacy_decision.json 과 build 테스트 대조, 런타임 .planning 읽기 0 (M2-05 + 3차 M3-02)"
    - "blur option B 는 Lambda 에서 blur 를 실행하지 않는다 — Pod 산출 training-before artifact 소비만 (2차 H2-10)"
    - "HMAC 키 미설정/스키마 위반 시 적재 자체를 거부한다 (fail-closed)"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/pair_store.py"
      provides: "consent-gated·versioned-HMAC·commit-marker 적재 + validate_hmac_key_set + list_committed_pairs/load_committed_pair(H4-11) + HISTORICAL_PAIR_JOINTS + BLUR_OPTION"
      contains: "def list_committed_pairs"
    - path: "backend/scripts/delete_training_pair.py"
      provides: "활성+retired 전 키 × historical registry 재계산 + versionId 완전 삭제"
    - path: "backend/tests/phase31/test_pair_store.py"
      provides: "동의 3분기·가명화·commit marker·부분 실패·validator·consumer helper·rotation 삭제·decision 일치"
      min_lines: 140
  key_links:
    - from: "backend/shared/python/sunity_shared/analysis/pair_store.py"
      to: "S3 training/phase31/pairs/"
      via: "boto3 put_object (consent+HMAC 통과 시만, meta.json = commit marker)"
      pattern: "training/phase31/pairs"
    - from: "backend/tests/phase31/test_pair_store.py"
      to: ".planning/phases/31-api-visual-correction/smoke/privacy_decision.json"
      via: "CONSENT_VERSION/BLUR_OPTION 상수 build-time 대조 (M2-05/M3-02)"
      pattern: "privacy_decision"
---

<objective>
D-01 플라이휠 부산물(학습 페어)을 H-04 + 2차 H2-06/H2-10/M2-05 + 3차 H3-07/H3-08/H3-11/M3-02/M3-05 + 4차 H4-11 대로 구현한다:

- **commit marker (H3-07):** before.png → after.png → meta.json 순 put, **meta.json 이 commit marker**. 예외 시 이번 호출 생성 object best-effort 삭제(versionId 기록 시 포함). lifecycle 이중 방어.
- **consumer helper (H4-11):** `list_committed_pairs(s3_client, bucket) -> list[dict]` — PAIRS_PREFIX 하위를 열거하되 **meta.json 이 존재+schema 검증 통과한 pair prefix 만 반환** (marker 없는 before/after prefix 제외); `load_committed_pair(s3_client, bucket, pair_id) -> dict | None` — meta.json 로드+schema 검증 후 {before_key, after_key, meta}; 둘 다 meta 검증을 단일 경로로. **docstring: "phase 22 소비 plan 은 반드시 이 helper 만 사용 — before/after prefix 직접 list 금지 (partial pair 방지)". (phase 22 소비 배선은 미래 plan — 여기선 helper 제공까지.)**
- **strict HMAC validator (H3-11):** `validate_hmac_key_set(obj) -> dict | None` — {"active","keys"} 만, active in keys, key 는 bytes.fromhex 성공+32바이트, version `^v[0-9]+$`. store/delete/31-10 dry-run 공용. rotation 은 key 삭제 없이 active 추가만 — 삭제 전 inventory gate.
- **historical joint registry (M3-05):** `HISTORICAL_PAIR_JOINTS: tuple` append-only.
- **versioned 완전 삭제 (H3-08):** list_object_versions 로 Versions+DeleteMarkers versionId 삭제.
- **배포 상수 (M3-02):** `CONSENT_VERSION` + `BLUR_OPTION` 배포 상수, privacy_decision.json 대조.

Purpose: phase 22 원료를 privacy 통제 하에 제조 — 동의 우회·역추적·partial orphan·삭제 불능·미커밋 소비가 구조적으로 차단.
Output: pair_store.py + 삭제 스크립트 + 테스트.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-api-visual-correction/31-ACCEPTANCE.md
@.planning/phases/31-api-visual-correction/smoke/privacy_decision.json
@.planning/phases/31-api-visual-correction/31-PATTERNS.md
@.planning/phases/31-api-visual-correction/31-PLAN-REVIEW-ITERATION4.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: pair_store.py — validator + 동의 게이트 + commit-marker 적재 + consumer helper</name>
  <files>backend/shared/python/sunity_shared/analysis/pair_store.py, backend/tests/phase31/test_pair_store.py</files>
  <read_first>backend/training/datagen/enumerate_internal.py lines 87-110 (consent_allows — strict 분기; phase 31 페어는 신규 생성물이라 bulk_approval 경로 금지), backend/training/datagen/build_jsonl.py lines 40-41 (S3 prefix 상수 관례), .planning/phases/31-api-visual-correction/smoke/privacy_decision.json (31-01 결정), backend/tests/phase31/conftest.py, .planning/phases/31-api-visual-correction/31-PLAN-REVIEW-ITERATION4.md 의 H4-11 절</read_first>
  <action>신규 pair_store.py (docstring: D-01 + [[learning-consent-pilot-mandatory]] + H-04 + H2-06/H2-10 + H3-07/H3-11 + 4차 H4-11 인용). 상수: `PAIRS_PREFIX = "training/phase31/pairs/"`, `CONSENT_VERSION`(privacy_decision.json consentVersion — 테스트 대조 M2-05), **`BLUR_OPTION`('none'|'pod_blur' — privacy_decision.json blurOption, 31-10 유일 분기 기준, 런타임 JSON 읽기 금지 주석 M3-02)**, **`HISTORICAL_PAIR_JOINTS: tuple[str,...]`(초기 = ARROW_JOINT_MAP 6 관절; docstring "append-only — 제거/rename 금지 M3-05")**. **`validate_hmac_key_set(obj) -> dict | None`(H3-11)**: dict 아님/top-level {"active","keys"} 외/active 부재/keys 비-dict·빈 → None; active not in keys → None; version `^v[0-9]+$` 미매치 → None; 값 bytes.fromhex 실패 또는 !=32바이트 → None; 통과 시 {"active": str, "keys": {version: bytes}}. `_load_hmac_keys()` — env `PAIR_ID_HMAC_KEYS` JSON → validate_hmac_key_set 위임(fail-closed). `pair_id(uid, analysis_id, joint, *, hmac_key: bytes) -> str` — hmac.new(key, f"{uid}:{analysis_id}:{joint}", sha256).hexdigest()[:24]. **`store_training_pair(s3_client, bucket, *, uid, analysis_id, joint, before_png, after_png, learning_opt_in, consent_captured_at_ms, quality: dict) -> bool`(H3-07)**: (1) `learning_opt_in is True` strict — 비통과 즉시 False + S3 0; (2) validator 실패 → False + log.warning; (3) active 키로 pair_id; (4) blur 미실행 — before_png 는 호출측(31-09)이 BLUR_OPTION 에 따라 로드한 바이트 (H2-10); (5) **put 순서 before.png → after.png → meta.json (마지막=commit marker), 각 put VersionId 수집, 예외 시 이번 호출 put 성공 object best-effort delete(VersionId 지정) 후 False + log.warning (재raise 금지)**; meta.json: {consentVersion, consentCapturedAtMs, purpose "phase22-v2-generation-head", joint, model_id, judge_confidence, pose_error_deg, hmacKeyVersion(H2-06), blurApplied + anonymizerVersion(H2-10), storedAtMs} — uid/analysisId 원문 미포함. **`list_committed_pairs(s3_client, bucket) -> list[dict]`(H4-11): PAIRS_PREFIX 열거 → 각 pair prefix 에 meta.json HEAD/GET 시도 + validate schema → 통과분만 {pair_id, before_key, after_key, meta} 반환 (marker 없는 prefix 제외).** **`load_committed_pair(s3_client, bucket, pair_id) -> dict | None`: meta.json 로드+schema 검증 실패 → None; 통과 → {before_key, after_key, meta}.** 두 helper 가 meta 검증을 단일 내부 경로(`_read_valid_meta`)로. 모듈 docstring 에 consumer 계약 박제: **"consumer(phase 22 datagen)는 list_committed_pairs/load_committed_pair 만 사용 — before/after prefix 직접 list 금지 (H4-11)"** + 보존 정책("보존 = privacy_decision.json retentionDays — lifecycle Expiration + NoncurrentVersionExpiration, 31-12 적용"). test_pair_store.py: False/None/"true" → put 0; True+키 → put 3(순서 assert) + pair_id key + meta uid/analysisId 부재 + hmacKeyVersion; **부분 실패 3종(H3-07): 1번째 실패 → object 0; 2번째 → before 삭제 후 0; 3번째(meta) → before/after 삭제 후 0 — 각 False + 삭제 검증**; **validator(H3-11): unknown key/비-hex/31바이트/33바이트/vX 위반/active not in keys → None + 적재 거부**; **consumer helper(H4-11): meta.json 있는 pair 만 list_committed_pairs 반환 + before/after 만 있는 prefix 제외 + load_committed_pair meta 없으면 None**; **CONSENT_VERSION + BLUR_OPTION == privacy_decision.json 대조 (M2-05/M3-02)**; HISTORICAL_PAIR_JOINTS ⊇ ARROW_JOINT_MAP 6 관절; 소스에 anonymize/ultralytics import 부재(H2-10) + '.planning' 문자열 부재(M3-02).</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31/test_pair_store.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `is True` strict (truthy 0)
    - commit marker 순서 + 부분 실패 3종 정리 green (H3-07)
    - list_committed_pairs/load_committed_pair 가 meta marker 없는 pair 제외 green (H4-11)
    - validate_hmac_key_set 단일 validator + 스키마 케이스 green (H3-11)
    - CONSENT_VERSION/BLUR_OPTION 대조 + 런타임 .planning 참조 0 (M2-05/M3-02)
    - HISTORICAL_PAIR_JOINTS append-only (M3-05) + meta uid/analysisId 부재, blur 코드 0
  </acceptance_criteria>
  <done>동의·역추적·partial orphan·무가명·미커밋 소비가 단위 테스트로 차단된 적재/소비 helper 완성.</done>
</task>

<task type="auto">
  <name>Task 2: 삭제 스크립트 — 전 키 버전 × historical registry × versioned 완전 삭제</name>
  <files>backend/scripts/delete_training_pair.py, backend/tests/phase31/test_pair_store.py</files>
  <read_first>backend/shared/python/sunity_shared/analysis/pair_store.py (Task 1 의 pair_id/validate_hmac_key_set/HISTORICAL_PAIR_JOINTS — import 재사용), backend/scripts/ 하위 기존 스크립트 1개 (argparse/AWS_PROFILE 관례), .planning/phases/31-api-visual-correction/31-PLAN-REVIEW-ITERATION3.md 의 H3-08 절 (versioned 삭제 규칙)</read_first>
  <action>delete_training_pair.py — argparse: --uid --analysis-id (--joint 옵션, 미지정 시 **HISTORICAL_PAIR_JOINTS 순회 — ARROW_JOINT_MAP 금지 M3-05**) --profile 기본 sunity-motion --dry-run. 동작: (1) SSM 로드 → **validate_hmac_key_set(실패 시 중단)**; (2) **inventory gate(H3-11): 후보 prefix meta.json 의 hmacKeyVersion 이 전부 key set 에 존재하는지 확인 — 미존재 시 중단**; (3) keys 전 버전(활성+retired) × HISTORICAL_PAIR_JOINTS 로 pair_id 재계산 → 각 prefix 에 **get-bucket-versioning → enabled/suspended 면 list_object_versions 로 Versions+DeleteMarkers 전부 versionId delete_objects; 아니면 일반 list/delete (H3-08)**; (4) **삭제 후 list_objects + list_object_versions 재조회로 current/noncurrent/delete-marker 0 확인 — 잔존 시 비정상 종료**; dry-run 목록만. 버전별 삭제 수 stdout. test_pair_store.py 추가(boto3 mock): **key rotation — v1 적재 → v2 추가 → v1 재계산 삭제**; **versioned bucket(H3-08): list_object_versions 가 Versions 2 + DeleteMarkers 1 → 3건 versionId 삭제 + 재조회 0**; **inventory gate: meta v3 인데 key set v1/v2 → 중단**; historical registry 순회(ARROW_JOINT_MAP 참조 부재 grep); dry-run delete 0.</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31/test_pair_store.py -x -q && python -c "import ast; ast.parse(open('backend/scripts/delete_training_pair.py').read())"</automated>
  </verify>
  <acceptance_criteria>
    - delete_training_pair.py 가 pair_id/validate_hmac_key_set/HISTORICAL_PAIR_JOINTS import (재구현·ARROW_JOINT_MAP 순회 0)
    - versioned bucket 완전 삭제 + 재조회 0 green (H3-08)
    - inventory gate + rotation 전후 삭제 green (H2-06/H3-11)
    - dry-run/실삭제 분기 green
  </acceptance_criteria>
  <done>삭제/철회 경로가 키 회전·joint rename·bucket versioning 전부에 안전한 실행 가능 스크립트로 존재.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 생성물 → 학습 저장소 | 동의+품질 통과분만, 가명 ID 로만, commit marker 로만 확정 |
| 학습 저장소 → consumer | list_committed_pairs 만 — marker 없는 partial pair 제외 |
| 학습 저장소 → 역추적 | HMAC 단방향 — 키 보유자만 재계산 |
| 키 회전 / bucket versioning | retired 버전 + inventory gate + noncurrent 완전 삭제 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-24 | Privacy | 동의 우회 | mitigate | learning_opt_in is True strict + 비통과 S3 0 |
| T-31-25 | Information Disclosure | 저장소 역추적 | mitigate | HMAC 가명 + uid/analysisId 원문 미기록 (H-04) |
| T-31-26 | Repudiation | 삭제 요청 이행 불능 | mitigate | versioned key set + inventory gate + historical registry + versionId 완전 삭제 + 재조회 (H2-06 + H3-08/H3-11/M3-05) |
| T-31-27 | Information Disclosure | 무가명/부분 적재 orphan / 미커밋 소비 | mitigate | strict validator + commit marker + 부분 실패 정리 + consumer helper marker 강제 (H3-07/H3-11/H4-11) |
</threat_model>

<verification>
- `python -m pytest backend/tests -q` green
- pair_store 상수(CONSENT_VERSION/BLUR_OPTION)가 privacy_decision.json 과 테스트 대조 (M2-05/M3-02)
</verification>

<success_criteria>
- D-01 페어 적재가 strict 동의·versioned 가명화·commit marker·단일 validator·consumer helper·historical registry·versioned 완전 삭제까지 테스트 고정 — 3차 §8-3 gate 10 + 4차 H4-11 추적
</success_criteria>

<output>
Create `.planning/phases/31-api-visual-correction/31-07-SUMMARY.md` when done
</output>
</content>
</invoke>
