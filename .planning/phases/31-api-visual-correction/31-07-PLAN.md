---
phase: 31-api-visual-correction
plan: 07
type: execute
wave: 2
depends_on: [31-01, 31-02]
files_modified:
  - backend/shared/python/sunity_shared/analysis/pair_store.py
  - backend/scripts/delete_training_pair.py
  - backend/tests/phase31/test_pair_store.py
autonomous: true
requirements: [D-01]
must_haves:
  truths:
    - "[틀린 폼→고쳐진 폼] 페어가 learningOptIn is True 사용자에 한해 적재된다 — false/부재/비-bool 은 S3 호출 0 (D-01 + [[learning-consent-pilot-mandatory]])"
    - "페어 저장은 commit-marker 방식 — before/after 를 먼저 put 하고 meta.json 이 마지막 commit marker. 부분 실패 시 이번 호출 생성 object 를 best-effort 삭제하고, consumer 는 marker 있는 pair 만 연다 (3차 리뷰 H3-07)"
    - "페어 ID 는 HMAC 가명 — S3 key·메타 어디에도 uid/analysisId 원문이 없다 (리뷰 H-04)"
    - "HMAC key set 은 strict schema validator 하나가 검증한다 — active in keys, bytes.fromhex 32바이트, version ID 형식; store/delete/dry-run(31-10) 공용 (3차 리뷰 H3-11)"
    - "rotation 은 기존 key 삭제 없이 active 추가만 — 삭제 전 inventory 의 hmacKeyVersion 전부가 key set 에 존재하는지 gate (H3-11)"
    - "삭제 스크립트는 versioned bucket 에서도 완전 삭제한다 — list_object_versions 로 Versions/DeleteMarkers 전부 versionId 삭제 + current/noncurrent 개수 검증 (3차 리뷰 H3-08)"
    - "삭제 대상 joint 집합은 append-only historical registry 상수 — ARROW_JOINT_MAP 변경/rename 후에도 과거 pair ID 재계산 가능 (3차 리뷰 M3-05)"
    - "consentVersion/blurOption 은 배포 상수 — privacy_decision.json 과 build 테스트 대조, 런타임 .planning 읽기 0 (M2-05 + 3차 M3-02)"
    - "blur option B 는 Lambda 에서 blur 를 실행하지 않는다 — Pod 산출 training-before artifact 를 소비만 (2차 리뷰 H2-10)"
    - "HMAC 키 미설정/스키마 위반 시 적재 자체를 거부한다 (fail-closed)"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/pair_store.py"
      provides: "consent-gated·versioned-HMAC·commit-marker 페어 적재 + validate_hmac_key_set 단일 validator + HISTORICAL_PAIR_JOINTS + BLUR_OPTION 상수"
      contains: "validate_hmac_key_set"
    - path: "backend/scripts/delete_training_pair.py"
      provides: "활성+retired 전 키 버전 × historical registry 재계산 + versionId 완전 삭제 스크립트"
    - path: "backend/tests/phase31/test_pair_store.py"
      provides: "동의 3분기·가명화·commit marker·부분 실패·validator·rotation 삭제·decision 일치 테스트"
      min_lines: 120
  key_links:
    - from: "backend/shared/python/sunity_shared/analysis/pair_store.py"
      to: "S3 training/phase31/pairs/"
      via: "boto3 put_object (consent+HMAC 통과 시만, meta.json = commit marker)"
      pattern: "training/phase31/pairs"
    - from: "backend/tests/phase31/test_pair_store.py"
      to: ".planning/phases/31-api-visual-correction/smoke/privacy_decision.json"
      via: "CONSENT_VERSION/BLUR_OPTION 상수 build-time 대조 assert (M2-05/M3-02)"
      pattern: "privacy_decision"
---

<objective>
D-01 의 플라이휠 부산물(학습 페어)을 리뷰 H-04 + 2차 H2-06/H2-10/M2-05 + 3차 H3-07/H3-08/H3-11/M3-02/M3-05 요구대로 구현한다:

- **commit marker (H3-07):** 3차 개정의 순차 put + 예외 시 False 반환은 두 번째/세 번째 put 실패 시 PII object orphan 을 남겼다. 이번 계약: before.png → after.png → meta.json 순 put, **meta.json 이 commit marker** — consumer(phase 22 datagen)는 meta.json 있는 pair prefix 만 연다. 예외 시 이번 호출에서 만든 object 를 best-effort 삭제 (versionId 기록 시 versionId 포함). lifecycle 이 partial 잔존을 이중 방어.
- **strict HMAC validator 단일 소유 (H3-11):** `validate_hmac_key_set(obj) -> dict | None` — JSON 스키마: top-level 은 {"active","keys"} 만 (unknown key 거부), active in keys, 각 key 는 bytes.fromhex 성공 + 정확히 32바이트, version ID 는 `^v[0-9]+$` 형식. store/delete/31-10 dry-run 이 전부 이 함수를 import. rotation 은 key 삭제 없이 active 추가만 — 삭제 스크립트는 inventory(meta.json 의 hmacKeyVersion 스캔) 전부가 key set 에 존재해야 진행 (gate).
- **historical joint registry (M3-05):** `HISTORICAL_PAIR_JOINTS: tuple` — append-only 상수 (docstring: "제거/rename 절대 금지 — 과거 pair ID 재계산 가능성의 유일한 보장; ARROW_JOINT_MAP 은 렌더 계약이라 바뀔 수 있음"). 삭제 스크립트는 ARROW_JOINT_MAP 이 아니라 이 registry 를 순회.
- **versioned 완전 삭제 (H3-08):** versioning-enabled/suspended bucket 에서 일반 delete 는 delete marker 만 만든다. 삭제 스크립트는 `list_object_versions` 로 Versions + DeleteMarkers 전부 versionId 지정 삭제 후 재조회로 current/noncurrent 0 검증.
- **배포 상수 (M3-02):** `CONSENT_VERSION` + `BLUR_OPTION` ('none'|'pod_blur') 을 배포 코드 상수로 선언 — 테스트가 privacy_decision.json 과 대조 (drift 시 실패). 런타임 .planning 읽기 0. 31-10 pipeline 이 BLUR_OPTION 을 import.

Purpose: phase 22 재도전 원료를 privacy 통제 하에 제조 — 동의 우회·역추적·partial orphan·삭제 불능(키 회전·joint rename·bucket versioning 포함)이 구조적으로 차단된 형태.
Output: pair_store.py + 삭제 스크립트 + 테스트.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-api-visual-correction/31-ACCEPTANCE.md
@.planning/phases/31-api-visual-correction/smoke/privacy_decision.json
@.planning/phases/31-api-visual-correction/31-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: pair_store.py — strict validator + 동의 게이트 + commit-marker 적재</name>
  <files>backend/shared/python/sunity_shared/analysis/pair_store.py, backend/tests/phase31/test_pair_store.py</files>
  <read_first>backend/training/datagen/enumerate_internal.py lines 87-110 (consent_allows — strict 분기 형상; phase 31 페어는 전부 신규 생성물이라 bulk_approval 경로 사용 금지), backend/training/datagen/build_jsonl.py lines 40-41 (S3 prefix 상수 관례), .planning/phases/31-api-visual-correction/smoke/privacy_decision.json (31-01 belle 결정 — consentVersion/blurOption/retentionDays), backend/tests/phase31/conftest.py</read_first>
  <action>신규 pair_store.py (docstring: D-01 + [[learning-consent-pilot-mandatory]] + H-04 + H2-06/H2-10 + H3-07/H3-11 인용). 상수: `PAIRS_PREFIX = "training/phase31/pairs/"`, `CONSENT_VERSION` (privacy_decision.json 의 consentVersion 과 동일 값 — 테스트 대조, M2-05), **`BLUR_OPTION` ('none'|'pod_blur' — privacy_decision.json blurOption 과 동일 값, 테스트 대조 — M3-02: 31-10 pipeline 의 유일한 분기 기준, 런타임 JSON 읽기 금지 주석)**, **`HISTORICAL_PAIR_JOINTS: tuple[str, ...]` — 초기값 = 현재 ARROW_JOINT_MAP 의 6 관절; docstring "append-only — 제거/rename 금지 (M3-05: 과거 pair ID 재계산 보장; ARROW_JOINT_MAP 과 독립)"**. **`validate_hmac_key_set(obj) -> dict | None` (H3-11 단일 validator)**: dict 아님/top-level 키 {"active","keys"} 외 존재/active 부재/keys 비-dict·빈 dict → None; active not in keys → None; 각 version ID 가 `^v[0-9]+$` 미매치 → None; 각 값 bytes.fromhex 실패 또는 != 32바이트 → None; 통과 시 {"active": str, "keys": {version: bytes}} 반환. `_load_hmac_keys()` — env `PAIR_ID_HMAC_KEYS` JSON 파싱 후 validate_hmac_key_set 위임 (fail-closed). `pair_id(uid, analysis_id, joint, *, hmac_key: bytes) -> str` — hmac.new(key, f"{uid}:{analysis_id}:{joint}", sha256).hexdigest()[:24]. **`store_training_pair(s3_client, bucket, *, uid, analysis_id, joint, before_png: bytes, after_png: bytes, learning_opt_in, consent_captured_at_ms, quality: dict) -> bool` (H3-07 commit-marker)**: (1) `learning_opt_in is True` strict — 비통과 즉시 False + S3 0; (2) validator 실패 → False + log.warning; (3) active 키로 pair_id; (4) blur 미실행 — before_png 는 호출측(31-09)이 BLUR_OPTION 에 따라 원본 또는 Pod 산출 blur artifact 에서 로드한 바이트 (H2-10); (5) **put 순서 고정: before.png → after.png → meta.json (마지막 = commit marker). 각 put 의 응답 VersionId 를 수집. 어느 단계든 예외 → 이번 호출에서 put 성공한 object 들을 best-effort delete (VersionId 있으면 지정 삭제) 후 False + log.warning (재raise 금지)**; meta.json: {consentVersion, consentCapturedAtMs, purpose: "phase22-v2-generation-head", joint, model_id, judge_confidence, pose_error_deg, hmacKeyVersion (H2-06), blurApplied: bool + anonymizerVersion (H2-10), storedAtMs} — uid/analysisId 원문 미포함. 모듈 docstring 에 consumer 계약 박제: **"consumer 는 meta.json 이 존재하는 pair prefix 만 열거한다 — before/after 만 있는 prefix 는 미커밋 잔존물 (lifecycle 이 정리)" (H3-07)** + 보존 정책 ("보존 = privacy_decision.json retentionDays — lifecycle Expiration + NoncurrentVersionExpiration, 31-12 적용"). test_pair_store.py: False/None/"true" → put 0; True + 키 → put 3 (순서 before→after→meta assert) + pair_id 기반 key + meta 에 uid/analysisId 부재 + hmacKeyVersion 존재; **부분 실패 주입 3종 (H3-07): 1번째 put 실패 → object 0 잔존; 2번째 실패 → before 삭제 시도 후 object 0; 3번째(meta) 실패 → before/after 삭제 시도 후 object 0 — 각각 False 반환 + 삭제 호출 검증 (삭제 자체 실패 시 log 후 진행 = lifecycle 방어)**; **validator (H3-11): unknown top-level key/비-hex/31바이트/64자 아닌 33바이트/`vX` 형식 위반/active not in keys → 전부 None + 적재 거부**; **CONSENT_VERSION + BLUR_OPTION == privacy_decision.json 대조 (M2-05/M3-02)**; HISTORICAL_PAIR_JOINTS 가 ARROW_JOINT_MAP 6 관절 포함 superset assert; 모듈 소스에 anonymize/ultralytics import 부재 (H2-10) + '.planning' 경로 문자열 부재 (M3-02).</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31/test_pair_store.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `is True` strict 검사 존재 (truthy 0)
    - commit marker 순서 + 부분 실패 3종 정리 테스트 green (H3-07)
    - validate_hmac_key_set 단일 validator + 스키마 케이스 전부 green (H3-11)
    - CONSENT_VERSION/BLUR_OPTION 배포 상수 + JSON 대조 + 런타임 .planning 참조 0 (M2-05/M3-02)
    - HISTORICAL_PAIR_JOINTS append-only registry 존재 (M3-05)
    - meta 에 hmacKeyVersion + uid/analysisId 원문 부재, blur 실행 코드 0
  </acceptance_criteria>
  <done>동의 우회·역추적·partial orphan·무가명 적재·버전 불명 키가 단위 테스트로 차단된 적재 helper 완성.</done>
</task>

<task type="auto">
  <name>Task 2: 삭제 스크립트 — 전 키 버전 × historical registry × versioned 완전 삭제</name>
  <files>backend/scripts/delete_training_pair.py, backend/tests/phase31/test_pair_store.py</files>
  <read_first>backend/shared/python/sunity_shared/analysis/pair_store.py (Task 1 의 pair_id/validate_hmac_key_set/HISTORICAL_PAIR_JOINTS — import 재사용, 재구현 금지), backend/scripts/ 하위 기존 스크립트 1개 (argparse/AWS_PROFILE 관례), .planning/phases/31-api-visual-correction/31-PLAN-REVIEW-ITERATION3.md 의 H3-08 절 (AWS 공식 문서 링크 — versioned 삭제 규칙)</read_first>
  <action>delete_training_pair.py — argparse: --uid --analysis-id (--joint 옵션, 미지정 시 **pair_store.HISTORICAL_PAIR_JOINTS 전 관절 순회 — ARROW_JOINT_MAP 순회 금지 (M3-05)**) --profile (기본 sunity-motion) --dry-run. 동작: (1) SSM `/sunity/motion/pair-id-hmac-keys` 로드 → **validate_hmac_key_set 검증 (H3-11 — 실패 시 즉시 중단, 삭제 진행 금지)**; (2) **inventory gate (H3-11): 대상 후보 prefix 들의 meta.json 을 먼저 스캔해 등장하는 hmacKeyVersion 이 전부 key set 에 존재하는지 확인 — 미존재 버전 발견 시 중단 + "key set 에 없는 hmacKeyVersion — rotation 시 key 삭제 금지 위반" 오류**; (3) keys 의 모든 버전(활성+retired) × HISTORICAL_PAIR_JOINTS 로 pair_id 재계산 (H2-06) → 각 후보 `training/phase31/pairs/{pair_id}/` 에 대해 **`get-bucket-versioning` 조회 → versioning enabled/suspended 면 `list_object_versions` 로 Versions + DeleteMarkers 전부 열거해 versionId 지정 delete_objects; 아니면 일반 list/delete (H3-08)**; (4) **삭제 후 검증: list_objects + list_object_versions 재조회로 current/noncurrent/delete-marker 0 확인 — 잔존 시 비정상 종료 코드 + 목록 출력**; dry-run 은 목록만. 버전별 매칭/삭제 객체 수(current/noncurrent 구분) stdout 요약. test_pair_store.py 추가 (boto3 mock): **key rotation 시나리오 — v1 적재 → v2 추가·active=v2 → v1 재계산으로 pair 발견·삭제**; **versioned bucket 시나리오 (H3-08, §6 추가 게이트): fake list_object_versions 가 Versions 2 + DeleteMarkers 1 반환 → 3건 전부 versionId 지정 삭제 + 재조회 0 검증**; **inventory gate: meta 에 v3 이 있는데 key set 에 v1/v2 만 → 중단**; historical registry 순회 (ARROW_JOINT_MAP 참조 부재 grep); dry-run delete 0.</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31/test_pair_store.py -x -q && python -c "import ast; ast.parse(open('backend/scripts/delete_training_pair.py').read())"</automated>
  </verify>
  <acceptance_criteria>
    - delete_training_pair.py 가 pair_store.pair_id/validate_hmac_key_set/HISTORICAL_PAIR_JOINTS import (자체 재구현·ARROW_JOINT_MAP 순회 0)
    - versioned bucket 완전 삭제 (Versions+DeleteMarkers versionId 삭제) + 재조회 0 검증 테스트 green (H3-08)
    - inventory hmacKeyVersion gate + rotation 전후 삭제 테스트 green (H2-06/H3-11)
    - dry-run/실삭제 분기 테스트 green
  </acceptance_criteria>
  <done>삭제/철회 경로가 키 회전·joint rename·bucket versioning 전부에 안전한 실행 가능 스크립트로 존재 — H-04 + H2-06 + H3-08/H3-11/M3-05 충족.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 생성물 → 학습 저장소 | 동의+품질 통과분만, 가명 ID 로만, commit marker 로만 확정 |
| 학습 저장소 → 역추적 | HMAC 단방향 — 키 보유자만 재계산 가능 |
| 키 회전 | retired 버전 보존 + inventory gate — 삭제 이행 능력 유지 |
| bucket versioning | noncurrent version 까지 완전 삭제 경계 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-24 | Privacy | 동의 우회 | mitigate | learning_opt_in is True strict + 비통과 S3 0 테스트 고정 |
| T-31-25 | Information Disclosure | 저장소 역추적 | mitigate | HMAC 가명 + uid/analysisId 원문 미기록 (테스트 고정, H-04) |
| T-31-26 | Repudiation | 삭제 요청 이행 불능 (키 회전·joint rename·versioning 포함) | mitigate | versioned key set + inventory gate + historical registry + versionId 완전 삭제 + 재조회 검증 (H2-06 + H3-08/H3-11/M3-05) |
| T-31-27 | Information Disclosure | 무가명/부분 적재 orphan | mitigate | strict validator fail-closed + commit marker + 부분 실패 정리 + lifecycle 이중 방어 (H3-07/H3-11) |
</threat_model>

<verification>
- `python -m pytest backend/tests -q` green
- pair_store 상수(CONSENT_VERSION/BLUR_OPTION)가 privacy_decision.json 과 테스트 대조 (M2-05/M3-02)
</verification>

<success_criteria>
- D-01 페어 적재가 strict 동의·versioned 가명화·commit marker·단일 validator·historical registry·versioned 완전 삭제까지 테스트로 고정 — 3차 리뷰 §8-3 gate 10 추적 가능
</success_criteria>

<output>
Create `.planning/phases/31-api-visual-correction/31-07-SUMMARY.md` when done
</output>
