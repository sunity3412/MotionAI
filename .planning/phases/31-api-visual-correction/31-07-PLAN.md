---
phase: 31-api-visual-correction
plan: 07
type: execute
wave: 2
depends_on: [31-01, 31-02]
files_modified:
  - backend/shared/python/sunity_shared/analysis/pair_store.py
  - backend/scripts/delete_training_pair.py
  - backend/tests/phase31/test_pair_store.py
autonomous: true
requirements: [D-01]
must_haves:
  truths:
    - "[틀린 폼→고쳐진 폼] 페어가 learningOptIn is True 사용자에 한해 적재된다 — false/부재/비-bool 은 S3 호출 0 (D-01 + [[learning-consent-pilot-mandatory]])"
    - "페어 ID 는 HMAC 가명 — S3 key·메타 어디에도 uid/analysisId 원문이 없다 (리뷰 H-04)"
    - "HMAC 키는 versioned — meta 에 hmacKeyVersion 기록, 키 회전 후에도 retired key 로 기존 페어 삭제 가능 (2차 리뷰 H2-06, §8 gate 12)"
    - "consentVersion/보존일수는 31-01 privacy_decision.json 값과 일치한다 — 하드코딩 drift 를 테스트가 차단 (2차 리뷰 M2-05)"
    - "blur option B 는 Lambda 에서 blur 를 실행하지 않는다 — Pod 가 생성한 training-before artifact 를 소비만 (2차 리뷰 H2-10, §8 gate 13)"
    - "HMAC 키 미설정 시 적재 자체를 거부한다 (fail-closed)"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/pair_store.py"
      provides: "consent-gated·versioned-HMAC 가명화 페어 적재"
      contains: "is True"
    - path: "backend/scripts/delete_training_pair.py"
      provides: "활성+retired 전 키 버전 재계산 삭제 스크립트"
    - path: "backend/tests/phase31/test_pair_store.py"
      provides: "동의 3분기·가명화·fail-closed·key rotation 삭제·decision 일치 테스트"
      min_lines: 90
  key_links:
    - from: "backend/shared/python/sunity_shared/analysis/pair_store.py"
      to: "S3 training/phase31/pairs/"
      via: "boto3 put_object (consent+HMAC 통과 시만)"
      pattern: "training/phase31/pairs"
    - from: "backend/tests/phase31/test_pair_store.py"
      to: ".planning/phases/31-api-visual-correction/smoke/privacy_decision.json"
      via: "consentVersion/retentionDays 일치 assert (M2-05)"
      pattern: "privacy_decision"
---

<objective>
D-01 의 플라이휠 부산물(학습 페어)을 리뷰 H-04 + 2차 리뷰 H2-06/H2-10/M2-05 요구대로 구현한다: strict 동의 게이트, **versioned HMAC** (env `PAIR_ID_HMAC_KEYS` = SSM `/sunity/motion/pair-id-hmac-keys` 의 JSON {"active": "v1", "keys": {"v1": hex}} — 키 회전/유실 시에도 retired 버전으로 기존 페어 삭제 가능), meta 에 hmacKeyVersion 기록, consentVersion·보존일수는 privacy_decision.json 단일 소스, 삭제 스크립트는 활성+retired 전 버전 순회. blur 는 31-01 결정이 option-b 여도 이 모듈이 실행하지 않는다 — Pod 산출 training-before artifact 바이트를 받는 소비자일 뿐 (H2-10: anonymize_batch 는 Pod 용 video runner — Lambda 실행 불가).

Purpose: phase 22 재도전 원료를 privacy 통제 하에 제조 — 동의 우회·역추적·삭제 불능(키 회전 포함)이 구조적으로 차단된 형태.
Output: pair_store.py + 삭제 스크립트 + 테스트.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-api-visual-correction/31-ACCEPTANCE.md
@.planning/phases/31-api-visual-correction/smoke/privacy_decision.json
@.planning/phases/31-api-visual-correction/31-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: pair_store.py — strict 동의 + versioned HMAC 가명화 적재</name>
  <files>backend/shared/python/sunity_shared/analysis/pair_store.py, backend/tests/phase31/test_pair_store.py</files>
  <read_first>backend/training/datagen/enumerate_internal.py lines 87-110 (consent_allows — strict 분기 형상; phase 31 페어는 전부 신규 생성물이라 bulk_approval 경로 사용 금지), backend/training/datagen/build_jsonl.py lines 40-41 (S3 prefix 상수 관례), .planning/phases/31-api-visual-correction/smoke/privacy_decision.json (31-01 belle 결정 — consentVersion/blurOption/retentionDays), backend/tests/phase31/conftest.py</read_first>
  <action>신규 pair_store.py (docstring: D-01 + [[learning-consent-pilot-mandatory]] + H-04 + H2-06/H2-10 인용). 상수 `PAIRS_PREFIX = "training/phase31/pairs/"`, `CONSENT_VERSION` — **값은 privacy_decision.json 의 consentVersion 과 동일해야 하며, 테스트가 JSON 을 읽어 상수와 대조 (M2-05 — drift 시 테스트 실패)**. `_load_hmac_keys() -> dict` — env `PAIR_ID_HMAC_KEYS` JSON 파싱 ({"active": str, "keys": {version: hex}}); 파싱 실패/active 부재/빈 keys → None (fail-closed). `pair_id(uid, analysis_id, joint, *, hmac_key: bytes) -> str` — hmac.new(key, f"{uid}:{analysis_id}:{joint}", sha256).hexdigest()[:24]. `store_training_pair(s3_client, bucket, *, uid, analysis_id, joint, before_png: bytes, after_png: bytes, learning_opt_in, consent_captured_at_ms, quality: dict) -> bool`: (1) `learning_opt_in is True` strict 게이트 (truthy 금지) — 비통과 즉시 False + S3 0; (2) versioned 키 로드 실패 → False + log.warning (fail-closed); (3) active 키로 pair_id 계산; (4) **blur: 이 함수는 blur 를 수행하지 않음 — before_png 는 호출측(31-09)이 privacy 결정에 따라 원본(srcKey) 또는 Pod 산출 blur artifact(trainingSrcKey) 에서 로드한 바이트 (H2-10 주석: Lambda 내 anonymize 실행 금지)**; (5) `{PAIRS_PREFIX}{pair_id}/before.png`, `after.png`, `meta.json` put — meta.json: {consentVersion, consentCapturedAtMs, purpose: "phase22-v2-generation-head", joint, model_id, judge_confidence, pose_error_deg, **hmacKeyVersion (H2-06 — 삭제 시 어느 버전으로 재계산할지)**, **blurApplied: bool + anonymizerVersion (H2-10 — option-b 추적)**, storedAtMs} — uid/analysisId 원문 미포함; (6) 실패 전부 False + log.warning (재raise 금지). 모듈 docstring 에 보존 정책 박제: "보존 = privacy_decision.json retentionDays (lifecycle Expiration — 31-12 적용), 삭제 요청 = delete_training_pair.py". test_pair_store.py: False/None/"true" → put 0; True + 키 → put 3 + key 가 pair_id 기반 + meta 에 uid/analysisId 부재 + hmacKeyVersion 존재; 키 미설정/JSON 오염 → 0 + False; put 예외 → False; **CONSENT_VERSION == privacy_decision.json consentVersion 대조 (M2-05)**; 모듈 소스에 anonymize/ultralytics import 부재 assert (H2-10).</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31/test_pair_store.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `is True` strict 검사 존재 (truthy 0)
    - meta 에 hmacKeyVersion 기록 + uid/analysisId 원문 부재 테스트 고정 (H2-06)
    - versioned 키 로드 실패 → fail-closed 테스트 green
    - CONSENT_VERSION 이 privacy_decision.json 과 대조되는 테스트 존재 (M2-05)
    - blur 실행 코드 0 (소비만 — H2-10)
  </acceptance_criteria>
  <done>동의 우회·역추적·무가명 적재·버전 불명 키가 단위 테스트로 차단된 적재 helper 완성.</done>
</task>

<task type="auto">
  <name>Task 2: 삭제 스크립트 (활성+retired 전 버전) + key rotation 테스트</name>
  <files>backend/scripts/delete_training_pair.py, backend/tests/phase31/test_pair_store.py</files>
  <read_first>backend/shared/python/sunity_shared/analysis/pair_store.py (Task 1 의 pair_id/_load_hmac_keys — import 재사용, 재구현 금지), backend/scripts/ 하위 기존 스크립트 1개 (argparse/AWS_PROFILE 관례)</read_first>
  <action>delete_training_pair.py — argparse: --uid --analysis-id (--joint 옵션, 미지정 시 ARROW_JOINT_MAP 전 관절 순회) --profile (기본 sunity-motion) --dry-run. 동작: SSM 에서 `/sunity/motion/pair-id-hmac-keys` versioned JSON 로드 → **keys 의 모든 버전(활성+retired)에 대해 pair_store.pair_id 재계산 (H2-06: 회전 후에도 구 버전 페어 삭제 가능)** → 각 후보 `training/phase31/pairs/{pair_id}/` list + delete (dry-run 목록만). 삭제 결과(버전별 매칭/삭제 객체 수) stdout 요약. test_pair_store.py 추가 (boto3 mock): **key rotation 시나리오 (§8 gate 12) — v1 키로 적재 → keys 에 v2 추가·active=v2 회전 → 삭제 스크립트 로직이 v1 재계산으로 해당 pair 를 찾고 삭제**; 재계산 pair_id 일치; dry-run delete 0; 실행 시 3객체 delete.</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31/test_pair_store.py -x -q && python -c "import ast; ast.parse(open('backend/scripts/delete_training_pair.py').read())"</automated>
  </verify>
  <acceptance_criteria>
    - delete_training_pair.py 가 pair_store.pair_id/_load_hmac_keys import (자체 HMAC 재구현 0)
    - 전 키 버전 순회 삭제 + key rotation 전후 삭제 테스트 green (H2-06 — §8 gate 12)
    - dry-run/실삭제 분기 테스트 green
    - 보존 정책 문구(privacy_decision.json retentionDays 참조)가 pair_store.py docstring 에 존재
  </acceptance_criteria>
  <done>삭제/철회 경로가 키 회전에도 안전한 실행 가능 스크립트로 존재 — H-04 + H2-06 충족.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 생성물 → 학습 저장소 | 동의+품질 통과분만, 가명 ID 로만 |
| 학습 저장소 → 역추적 | HMAC 단방향 — 키 보유자만 재계산 가능 |
| 키 회전 | retired 버전 보존 — 삭제 이행 능력 유지 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-24 | Privacy | 동의 우회 | mitigate | learning_opt_in is True strict + 비통과 S3 0 테스트 고정 |
| T-31-25 | Information Disclosure | 저장소 역추적 | mitigate | HMAC 가명 + uid/analysisId 원문 미기록 (테스트 고정, H-04) |
| T-31-26 | Repudiation | 삭제 요청 이행 불능 (키 회전 포함) | mitigate | versioned key set + hmacKeyVersion meta + 전 버전 순회 삭제 + rotation 테스트 (H-04 + H2-06) |
| T-31-27 | Information Disclosure | 무가명 적재 | mitigate | 키 미설정/JSON 오염 시 적재 거부 fail-closed |
</threat_model>

<verification>
- `python -m pytest backend/tests -q` green
- pair_store 가 privacy_decision.json 결정(consentVersion/blurOption)을 반영 (테스트 대조)
</verification>

<success_criteria>
- D-01 페어 적재가 strict 동의·versioned 가명화·decision 단일 소스·rotation-safe 삭제까지 테스트로 고정 — §8 gate 12·13 추적 가능
</success_criteria>

<output>
Create `.planning/phases/31-api-visual-correction/31-07-SUMMARY.md` when done
</output>
