---
phase: 31-api-visual-correction
plan: 07
type: execute
wave: 3
depends_on: [31-01, 31-04]
files_modified:
  - backend/functions/visual-request/app.py
  - backend/functions/visual-request/requirements.txt
  - backend/functions/visual-worker/app.py
  - backend/functions/visual-worker/requirements.txt
  - backend/shared/python/sunity_shared/firestore_admin.py
  - backend/template.yaml
  - backend/tests/phase31/test_visual_request.py
autonomous: true
requirements: [D-04, D-06, D-07]
must_haves:
  truths:
    - "회전 합성 영상은 온디맨드 — 버튼 요청 시 202 즉답 후 백그라운드 생성, 완료는 Firestore onSnapshot 카드 갱신 (D-06, push 인프라 없음)"
    - "요청 엔드포인트는 Firebase 토큰 인증 + 자기 분석 문서만 (uid 스코프) + 사용자당 일일 한도를 강제한다 (D-07)"
    - "생성 워커는 Pod 무관 순수 HTTP Lambda — 기존 분석 문서에 대해 Pod OFF 상태에서도 동작한다"
    - "DashScope 24h URL 은 즉시 S3 이관 후 자체 presign 으로 방출된다 (Firestore 에 벤더 URL 저장 금지)"
    - "task_id 가 Firestore 에 저장되어 워커 재시도 시 재과금 없이 폴링을 재개한다 (journal 멱등)"
  artifacts:
    - path: "backend/functions/visual-request/app.py"
      provides: "POST /visual/rotation — auth·한도·SQS 발행·202"
      contains: "def lambda_handler"
    - path: "backend/functions/visual-worker/app.py"
      provides: "SQS 소비 → Wan task → 폴링 → S3 이관 → rotationStatus"
      contains: "def lambda_handler"
    - path: "backend/template.yaml"
      provides: "VisualRequestFunction + VisualWorkerFunction + VisualQueue/DLQ + LogGroup 2종"
      contains: "VisualQueue"
    - path: "backend/shared/python/sunity_shared/firestore_admin.py"
      provides: "increment_visual_quota 일일 한도 counter"
      contains: "def increment_visual_quota"
  key_links:
    - from: "backend/functions/visual-request/app.py"
      to: "VisualQueue (SQS)"
      via: "sqs.send_message 후 202"
      pattern: "send_message"
    - from: "backend/functions/visual-worker/app.py"
      to: "backend/shared/python/sunity_shared/analysis/visual_gen.py"
      via: "RotationGenEngine (WanVideoEditAdapter)"
      pattern: "visual_gen"
    - from: "backend/template.yaml"
      to: "SSM /sunity/motion/dashscope-api-key"
      via: "resolve:ssm dynamic reference"
      pattern: "dashscope-api-key"
---

<objective>
D-06 회전 참고 영상 온디맨드 경로를 구현한다: 신규 HTTP Lambda(visual-request — auth→검증→D-07 한도→SQS→202 즉답, API GW 30s 한도 때문에 동기 생성 금지)와 SQS 워커 Lambda(visual-worker — Wan2.7-VideoEdit task 생성→15s 폴링(6~7분)→24h URL 즉시 S3 이관→rotationStatus='done'). 완료 알림 = 앱 onSnapshot 카드 갱신이 전부. 워커는 Pod 무관 순수 HTTP — 기존 분석 문서에 Pod 없이 동작 (D-04 하이브리드의 옵션 트랙).

Purpose: R3F 뷰어(기본)의 프리미엄 보완 — 실사 회전 참고 영상 (비채점, 참고코너 소속).
Output: Lambda 2종 + firestore_admin 한도 counter + template.yaml IaC + 테스트. 배포는 31-09 checkpoint.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-api-visual-correction/31-PATTERNS.md
@docs/contract.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: visual-request Lambda + increment_visual_quota (D-07)</name>
  <files>backend/functions/visual-request/app.py, backend/functions/visual-request/requirements.txt, backend/shared/python/sunity_shared/firestore_admin.py, backend/tests/phase31/test_visual_request.py</files>
  <read_first>backend/functions/playback-url/app.py (전체 — imports/auth/analysisId alnum+len>=16 가드/guards_ok 단일 404/로깅 패턴: 가장 근접한 analog), docs/contract.md 의 POST /visual/rotation 절 (31-01 이 정의한 응답 계약 — 단일 기준), backend/shared/python/sunity_shared/firestore_admin.py (_doc 헬퍼 + models.analysis_doc_path + update_analysis_rotation)</read_first>
  <action>firestore_admin.py 에 `increment_visual_quota(uid: str, *, date_key: str, user_limit: int, global_limit: int) -> bool` 신설 — backend 전용 컬렉션 `visualQuota` (문서 `{uid}_{date_key}` + `global_{date_key}`, 사용자 문서 하위 아님 — Firestore rules 노출면 0), firebase-admin `@firestore.transactional` 로 두 counter 를 읽고 둘 다 한도 미만이면 +1 후 True, 아니면 False (증가 없음). date_key = KST 기준 YYYYMMDD (UTC+9 — 파일럿 사용자 전원 한국). visual-request/app.py 신설 — playback-url 골격 그대로: verify_request→401, parse_json_body, analysisId 검증(alnum+len>=16)→400, Firestore 분석 문서 read — 존재·소유(uid 경로 자체가 스코프)·status=='done' 가드를 guards_ok 하나로 합산해 단일 404 (leak 0). 멱등 분기: rotationStatus=='done' → 200 `responses.ok({"rotationStatus": "done", "rotationVideoUrl": ...})`; 'pending' 이고 updatedAt 이 20분 이내 → 202 dedupe (SQS 미발행); 그 외 → `increment_visual_quota(user_limit=int(env ROTATION_DAILY_LIMIT, 기본 3), global_limit=int(env ROTATION_GLOBAL_DAILY_LIMIT, 기본 30))` — False 면 429 `responses.error("daily_limit", "오늘 생성 한도에 도달했어요. 내일 다시 시도해 주세요.", status=429)` (D-07: 파일럿 규모 사실상 무제한 + 남용 방지 + 전역 안전핀) → 통과 시 `update_analysis_rotation(status='pending')` + `sqs.send_message(QueueUrl=env VISUAL_QUEUE_URL, MessageBody=json {uid, analysisId})` → 202 `{"rotationStatus": "pending"}`. requirements.txt = firebase-admin>=6,<7 (boto3 는 런타임 제공). test_visual_request.py 채움: 무토큰 401, analysisId 형식 400, 타인/부재 문서 404 단일 응답, done 멱등 200, pending dedupe 202 + SQS 미발행, 한도 초과 429 daily_limit, 정상 202 + SQS 발행 1회 + pending 마킹 (SQS/Firestore 전부 mock).</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31/test_visual_request.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - visual-request/app.py 의 존재·소유·status 가드가 guards_ok 단일 404 로 응답 (구분 불가)
    - 한도 초과 시 code `daily_limit` + status 429 가 테스트로 고정
    - increment_visual_quota 가 transaction 데코레이터를 사용하고 사용자·전역 두 counter 를 모두 검사
    - `python -m pytest backend/tests/phase31/test_visual_request.py -x -q` exits 0
  </acceptance_criteria>
  <done>온디맨드 요청 경로가 auth·한도·멱등·202 까지 테스트로 고정됨.</done>
</task>

<task type="auto">
  <name>Task 2: visual-worker Lambda (Wan task 생성→폴링→S3 이관)</name>
  <files>backend/functions/visual-worker/app.py, backend/functions/visual-worker/requirements.txt, backend/tests/phase31/test_visual_request.py</files>
  <read_first>backend/functions/pipeline/app.py lines 4816-4867 (SQS 레코드 루프 + 예외 매핑 골격) + lines 1258-1263 (_signed_get 7일 presign), backend/shared/python/sunity_shared/analysis/visual_gen.py (RotationGenEngine/WanVideoEditAdapter/_is_allowed_output_url 시그니처), .planning/spikes/004-gemini-omni-view-editing/wan_gate_batch.py (회전 PROMPT 원문 — 검증된 카메라 orbit 프롬프트 재사용)</read_first>
  <action>visual-worker/app.py 신설 — SQS 레코드 루프(자체 메시지 파싱: {uid, analysisId}, 방어적 — 파싱 실패 레코드는 log 후 skip). 레코드당 흐름: (1) 분석 문서 read — 부재/미완료면 log 후 skip (방어), (2) 멱등: 문서에 rotationTaskId 존재 + status pending 이면 새 task 생성 없이 그 task_id 폴링 재개 (재과금 0 — journal 패턴), (3) 신규면 myVideoKey 를 presign 1h (전용 발급 — 7일 아님, 트림 없이 원본 전송: RESEARCH Open Q4 권고, ffmpeg 의존 회피) → `WanVideoEditAdapter.create_task(video_url, prompt)` — prompt 는 wan_gate_batch.py 의 검증 PROMPT 상수 이식 → `update_analysis_rotation(status='pending', task_id=...)` 즉시 기록, (4) 15s 간격 폴링 상한 ~780s (Lambda timeout 900 내), (5) SUCCEEDED: `_is_allowed_output_url` 통과 확인 → 즉시 다운로드 → S3 `results/{uid}/{analysisId}/rotation.mp4` put (ContentType video/mp4) → 7일 presign → `update_analysis_rotation(status='done', video_url=..., s3_key=..., task_id=...)`, (6) BLOCKED(모더레이션): 재시도 1회 (새 task) → 재차단이면 'failed' (D-08 — fail_analysis 절대 호출 금지, 분석 문서는 이미 done), (7) FAILED/UNKNOWN: 'failed', (8) 폴링 상한 도달: pending + task_id 유지한 채 return — SQS 재전달(maxReceiveCount 3)이 (2) 멱등 경로로 재개. 키 로드 = env DASHSCOPE_API_KEY (template resolve:ssm). requirements.txt = firebase-admin>=6,<7. 로깅 key=value (uid/analysis_id/task_id), 키·presign URL 전문 로깅 금지. test_visual_request.py 에 워커 테스트 추가 (dashscope_urlopen_mock + boto3/Firestore mock): task_id 재개 시 create 미호출, SUCCEEDED → S3 put + done + 벤더 URL 이 Firestore 에 미저장 (rotationVideoUrl 이 자체 presign 형식), BLOCKED → 재시도 1회 후 failed, 폴링 상한 → pending 유지.</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31/test_visual_request.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - worker 가 fail_analysis 를 호출하지 않음 (grep: `fail_analysis` 0건)
    - rotationTaskId 존재 시 create_task 미호출(재과금 0)이 테스트로 고정
    - Firestore 에 저장되는 rotationVideoUrl 에 `dashscope` 문자열 부재가 테스트로 고정
    - `python -m pytest backend/tests/phase31 -q` exits 0
  </acceptance_criteria>
  <done>생성 워커가 멱등·조용한 폴백·URL 이관까지 테스트로 고정됨.</done>
</task>

<task type="auto">
  <name>Task 3: template.yaml — 함수 2종 + VisualQueue/DLQ + LogGroup + route</name>
  <files>backend/template.yaml</files>
  <read_first>backend/template.yaml (전체 — PlaybackUrlFunction lines 161-185 의 HttpApi 이벤트+명시 SSM Statement, PipelineFunction lines 259-294 의 Timeout 900+resolve:ssm env+SQS 이벤트 BatchSize 1, AnalysisQueue/DLQ lines 83-97 의 VisibilityTimeout 960+maxReceiveCount 3, LogGroup lines 297-324 — WR-05 교훈: 신규 함수마다 LogGroup 필수)</read_first>
  <action>4개 리소스군 추가: (1) `VisualQueue` + `VisualDLQ` — AnalysisQueue 선례: VisibilityTimeout 960 (worker Timeout 900 초과 주석 유지), RedrivePolicy maxReceiveCount 3. S3 QueuePolicy 불필요 (발행자가 Lambda). (2) `VisualRequestFunction` — PlaybackUrlFunction 선례: Runtime python3.12/arm64/Timeout 10, HttpApi 이벤트 `POST /visual/rotation`, env: FIREBASE_SA_PARAM, VISUAL_QUEUE_URL(!Ref VisualQueue), ROTATION_DAILY_LIMIT "3", ROTATION_GLOBAL_DAILY_LIMIT "30"; Policies: SSM GetParameter 명시 Statement (SAM SSMParameterReadPolicy leading-slash 버그 주석 유지) + `sqs:SendMessage` on !GetAtt VisualQueue.Arn. (3) `VisualWorkerFunction` — PipelineFunction 선례: Timeout 900, MemorySize 512, SQS 이벤트 !GetAtt VisualQueue.Arn BatchSize 1, env: VIDEO_BUCKET, FIREBASE_SA_PARAM, `DASHSCOPE_API_KEY: "{{resolve:ssm:/sunity/motion/dashscope-api-key}}"`; Policies: SSM 명시 Statement + s3 GetObject(uploads/* — presign 유효성)·PutObject/GetObject(results/*). (4) LogGroup 2종 30일 retention (선례 형식). 공용 Layer(sunity_shared) 참조는 기존 함수 선언과 동일하게. `sam validate --lint` 로 검증 (배포는 31-09 — sam deploy 는 belle 승인 checkpoint).</action>
  <verify>
    <automated>cd backend && sam validate --lint</automated>
  </verify>
  <acceptance_criteria>
    - template.yaml contains `VisualQueue`, `VisualRequestFunction`, `VisualWorkerFunction`, `resolve:ssm:/sunity/motion/dashscope-api-key`
    - VisualQueue VisibilityTimeout 이 960 (worker Timeout 900 초과)
    - LogGroup 2종이 RetentionInDays 30 으로 존재
    - `cd backend && sam validate --lint` exits 0
  </acceptance_criteria>
  <done>IaC 가 배포 가능 상태 (실배포는 31-09 checkpoint).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 앱 → POST /visual/rotation | 미신뢰 입력 + 과금 유발 요청 |
| SQS 메시지 → worker | 메시지 위조/변형 대비 방어 파싱 + 문서 재검증 |
| 사용자 영상 presign → DashScope | 외부 벤더 전송 |
| DashScope URL → S3 | 외부 URL 다운로드 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-20 | Spoofing | visual-request 인증 | mitigate | verify_request Firebase ID token 필수 → AuthError 401 (기존 경로 재사용) |
| T-31-21 | Elevation of Privilege | 타인 analysisId 요청 | mitigate | uid 토큰 박제 + users/{uid}/analyses/{id} 경로 스코프 + guards_ok 단일 404 (leak 0) |
| T-31-22 | Denial of Service (과금 공격) | 온디맨드 생성 (~$0.8/건) | mitigate | D-07 transaction 한도 (사용자 3/일 + 전역 30/일 env 조정) + pending dedupe + done 멱등 — 테스트 고정 |
| T-31-23 | Information Disclosure | 사용자 영상 presign | mitigate | 워커 내 전용 1h TTL 발급 (7일 presign 미사용), URL 전문 로깅 금지 |
| T-31-24 | Injection / SSRF | DashScope video_url 다운로드 | mitigate | _is_allowed_output_url 화이트리스트 통과 후에만 다운로드 (31-04 공용) |
| T-31-25 | Information Disclosure | DASHSCOPE_API_KEY | mitigate | SSM SecureString resolve:ssm 만 — 코드/로그 리터럴 0 |
| T-31-26 | Tampering | SQS 메시지 위조 | mitigate | 워커가 메시지의 uid/analysisId 로 문서 존재·상태를 재검증 (defense in depth), 큐 접근은 IAM 한정 |
</threat_model>

<verification>
- `python -m pytest backend/tests -q` green
- `cd backend && sam validate --lint` green
- 계약 준수: 응답 코드가 docs/contract.md /visual/rotation 절과 일치
</verification>

<success_criteria>
- D-06 온디맨드 202 + 백그라운드 생성 + onSnapshot 갱신 경로 완성 (mock 검증)
- D-07 일일 한도 (3/일 + 전역 30/일) 테스트 고정
- Pod 무관 — GPU/RunPod 참조 0
</success_criteria>

<output>
Create `.planning/phases/31-api-visual-correction/31-07-SUMMARY.md` when done
</output>
