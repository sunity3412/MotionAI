---
phase: 31-api-visual-correction
plan: 07
type: execute
wave: 2
depends_on: [31-01, 31-02]
files_modified:
  - backend/shared/python/sunity_shared/analysis/pair_store.py
  - backend/scripts/delete_training_pair.py
  - backend/tests/phase31/test_pair_store.py
autonomous: true
requirements: [D-01]
must_haves:
  truths:
    - "[틀린 폼→고쳐진 폼] 페어가 learningOptIn is True 사용자에 한해 적재된다 — false/부재/비-bool 은 S3 호출 0 (D-01 + [[learning-consent-pilot-mandatory]])"
    - "store_training_pair 는 caller(31-09 postprocess)가 고정한 pairId/keyVersion 을 받고 먼저 meta 를 읽어 진짜 멱등하다 — meta 존재+beforeSha256/afterSha256/consentVersion/joint/model/provenance 일치 → PUT 0 idempotent success; 존재하나 불일치 → overwrite 금지 typed conflict; 부재 → 조건부 PUT/marker-last. postprocess crash + HMAC active key rotation 이 겹쳐도 동일 분석에 pair 1개·before/after/meta version 각 1개·재PUT 0 (5차 리뷰 B5-03)"
    - "meta 부재(부분 pair 재개) 경로에서 before/after 조건부 PUT 이 412(존재) 를 만나면 skip 전에 기존 payload 를 검증한다 — HEAD size + GET sha256 이 이번 expected before/afterSha256/size 와 일치하면 resume(재PUT 0), 불일치하면 'conflict' 로 중단하고 meta 는 쓰지 않는다. before-only crash·after-only crash·변조된 preexisting payload 가 조용히 학습쌍으로 커밋될 수 없다 (6차 리뷰 H6-04)"
    - "페어 저장은 commit-marker 방식 — before/after 먼저 put, meta.json 이 마지막 commit marker. 부분 실패 시 이번 호출 생성 object 를 best-effort 삭제하고, consumer 는 marker 있는 pair 만 연다 (3차 리뷰 H3-07)"
    - "consumer helper 가 marker 뿐 아니라 실제 payload 존재+hash 를 검증한다 — list_committed_pairs/load_committed_pair 가 meta 의 beforeSha256/afterSha256/size 로 before/after 존재·hash 를 HEAD/GET 확인한 pair 만 반환하고, 불일치/부재는 metric+quarantine 으로 분리한다. version listing pagination 은 continuation token 끝까지 (4차 H4-11 + 5차 H5-08/M5-05)"
    - "페어 ID 는 HMAC 가명 — S3 key·메타 어디에도 uid/analysisId 원문이 없다 (리뷰 H-04)"
    - "HMAC key set 은 strict schema validator 하나가 검증한다 — active in keys, bytes.fromhex 32바이트, version ID 형식; store/delete/dry-run(31-10) 공용 (3차 리뷰 H3-11)"
    - "rotation 은 기존 key 삭제 없이 active 추가만 — 삭제 전 inventory 의 hmacKeyVersion 전부가 key set 에 존재하는지 gate (H3-11)"
    - "삭제 스크립트는 versioned bucket 에서도 완전 삭제한다 — list_object_versions 를 boto3 paginator(또는 NextKeyMarker+NextVersionIdMarker 쌍 명시)로 끝까지 열거해 Versions/DeleteMarkers 전부 versionId 삭제 + current/noncurrent 검증. 단일 key 에 version 이 페이지 경계를 넘겨도 누락/반복 0 (3차 H3-08 + 6차 리뷰 H6-06)"
    - "삭제 대상 joint 집합은 append-only historical registry 상수 (3차 리뷰 M3-05)"
    - "consentVersion/blurOption 은 배포 상수 — privacy_decision.json 과 build 테스트 대조, 런타임 .planning 읽기 0 (M2-05 + 3차 M3-02)"
    - "blur option B 는 Lambda 에서 blur 를 실행하지 않는다 — Pod 산출 training-before artifact 소비만 (2차 H2-10)"
    - "HMAC 키 미설정/스키마 위반 시 적재 자체를 거부한다 (fail-closed)"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/pair_store.py"
      provides: "consent-gated·versioned-HMAC·caller-fixed-pairId·meta-read-idempotent commit-marker 적재 + validate_hmac_key_set + payload-verifying paginated list_committed_pairs/load_committed_pair(H5-08) + HISTORICAL_PAIR_JOINTS + BLUR_OPTION"
      contains: "def list_committed_pairs"
    - path: "backend/scripts/delete_training_pair.py"
      provides: "활성+retired 전 키 × historical registry 재계산 + versionId 완전 삭제"
    - path: "backend/tests/phase31/test_pair_store.py"
      provides: "동의 3분기·가명화·caller-fixed pairId·meta-read 멱등·partial 재개 payload hash 검증(H6-04)·rotation crash·commit marker·부분 실패·validator·payload-verify consumer helper·marker-pair pagination(H6-06)·rotation 삭제·decision 일치"
      min_lines: 170
  key_links:
    - from: "backend/shared/python/sunity_shared/analysis/pair_store.py"
      to: "S3 training/phase31/pairs/"
      via: "boto3 put_object (consent+HMAC+meta-read 통과 시만, meta.json = commit marker)"
      pattern: "training/phase31/pairs"
    - from: "backend/tests/phase31/test_pair_store.py"
      to: ".planning/phases/31-api-visual-correction/smoke/privacy_decision.json"
      via: "CONSENT_VERSION/BLUR_OPTION 상수 build-time 대조 (M2-05/M3-02)"
      pattern: "privacy_decision"
---

<objective>
D-01 플라이휠 부산물(학습 페어)을 H-04 + 2차 H2-06/H2-10/M2-05 + 3차 H3-07/H3-08/H3-11/M3-02/M3-05 + 4차 H4-11 + 5차 B5-03/H5-08/M5-05 + 6차 H6-04/H6-06 대로 구현한다:

- **caller-fixed pairId + meta-read 멱등 (B5-03 + 6차 H6-04):** `store_training_pair` 는 active HMAC key 를 스스로 재선택하지 않는다. caller(31-09 postprocess)가 postprocessing 진입 transaction 에서 1회 고정한 `pair_id` + `hmac_key_version` 을 인자로 받고, **먼저 meta.json 을 읽어** (1) 존재+hash/consent/joint/provenance 일치 → PUT 0 idempotent success; (2) 존재+불일치 → overwrite 금지 typed conflict; (3) 부재 → 조건부 PUT/marker-last, **단 before/after 조건부 PUT 이 412 를 만나면 HEAD size + GET sha256 으로 기존 payload 가 이번 expected hash/size 와 일치하는지 검증 후에만 skip, 불일치 → 'conflict' 중단(meta 미기록) — H6-04**. → postprocess crash + key rotation 이 겹쳐도 pair 1개.
- **commit marker (H3-07):** before.png → after.png → meta.json 순 put, **meta.json 이 commit marker**. 예외 시 이번 호출 생성 object best-effort 삭제(versionId 기록 시 포함). lifecycle 이중 방어.
- **payload-verifying paginated consumer (H4-11 + H5-08 + M5-05):** `list_committed_pairs`/`load_committed_pair` 가 meta schema 뿐 아니라 **meta 의 beforeSha256/afterSha256/size 로 before/after 실 존재·hash 를 HEAD/GET 확인** — 불일치/부재는 metric+quarantine 으로 분리, 정상만 반환. version/object listing 은 **continuation token 끝까지**.
- **strict HMAC validator (H3-11):** `validate_hmac_key_set(obj) -> dict | None` — {"active","keys"} 만, active in keys, key 는 bytes.fromhex 성공+32바이트, version `^v[0-9]+$`. store/delete/31-10 dry-run 공용.
- **historical joint registry (M3-05):** `HISTORICAL_PAIR_JOINTS: tuple` append-only.
- **versioned 완전 삭제 (H3-08 + 6차 H6-06):** list_object_versions 를 paginator(또는 KeyMarker+VersionIdMarker 쌍)로 끝까지 열거해 Versions+DeleteMarkers versionId 삭제 — 단일 key 다중 version 페이지 경계 누락 0.
- **배포 상수 (M3-02):** `CONSENT_VERSION` + `BLUR_OPTION` 배포 상수, privacy_decision.json 대조.

Purpose: phase 22 원료를 privacy 통제 하에 제조 — 동의 우회·역추적·partial orphan·삭제 불능·미커밋/변조 소비·crash-rotation 중복이 구조적으로 차단.
Output: pair_store.py + 삭제 스크립트 + 테스트.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-api-visual-correction/31-ACCEPTANCE.md
@.planning/phases/31-api-visual-correction/smoke/privacy_decision.json
@.planning/phases/31-api-visual-correction/31-PATTERNS.md
@.planning/phases/31-api-visual-correction/31-PLAN-REVIEW-ITERATION6.md
@.planning/phases/31-api-visual-correction/31-PLAN-REVIEW-ITERATION5.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: pair_store.py — validator + 동의 게이트 + caller-fixed pairId + meta-read 멱등 commit-marker + payload-verify consumer helper</name>
  <files>backend/shared/python/sunity_shared/analysis/pair_store.py, backend/tests/phase31/test_pair_store.py</files>
  <read_first>backend/training/datagen/enumerate_internal.py lines 87-110 (consent_allows — strict 분기; phase 31 페어는 신규 생성물이라 bulk_approval 경로 금지), backend/training/datagen/build_jsonl.py lines 40-41 (S3 prefix 상수 관례), .planning/phases/31-api-visual-correction/smoke/privacy_decision.json (31-01 결정), backend/tests/phase31/conftest.py, .planning/phases/31-api-visual-correction/31-PLAN-REVIEW-ITERATION6.md 의 H6-04 절 + ITERATION5 의 B5-03/H5-08 절</read_first>
  <action>신규 pair_store.py (docstring: D-01 + [[learning-consent-pilot-mandatory]] + H-04 + H2-06/H2-10 + H3-07/H3-11 + 4차 H4-11 + 5차 B5-03/H5-08 인용). 상수: `PAIRS_PREFIX = "training/phase31/pairs/"`, `CONSENT_VERSION`(privacy_decision.json consentVersion — 테스트 대조 M2-05), **`BLUR_OPTION`('none'|'pod_blur' — privacy_decision.json blurOption, 31-10 유일 분기 기준, 런타임 JSON 읽기 금지 주석 M3-02)**, **`HISTORICAL_PAIR_JOINTS: tuple[str,...]`(초기 = ARROW_JOINT_MAP 6 관절; docstring "append-only — 제거/rename 금지 M3-05")**. **`validate_hmac_key_set(obj) -> dict | None`(H3-11)**: dict 아님/top-level {"active","keys"} 외/active 부재/keys 비-dict·빈 → None; active not in keys → None; version `^v[0-9]+$` 미매치 → None; 값 bytes.fromhex 실패 또는 !=32바이트 → None; 통과 시 {"active": str, "keys": {version: bytes}}. `_load_hmac_keys()` — env `PAIR_ID_HMAC_KEYS` JSON → validate_hmac_key_set 위임(fail-closed). `pair_id(uid, analysis_id, joint, *, hmac_key: bytes) -> str` — hmac.new(key, f"{uid}:{analysis_id}:{joint}", sha256).hexdigest()[:24]. **`store_training_pair(s3_client, bucket, *, pair_id: str, hmac_key_version: str, joint, before_png, after_png, learning_opt_in, consent_captured_at_ms, quality: dict) -> str`(B5-03 + H3-07)** — **caller 가 pair_id/hmac_key_version 을 고정해 넘긴다(내부에서 active key 재선택 금지 — 5차 B5-03)**: (1) `learning_opt_in is True` strict — 비통과 즉시 'skipped_consent' + S3 0; (2) beforeSha256=sha256(before_png)/afterSha256=sha256(after_png) 계산; (3) **meta.json 먼저 GET 시도: 존재하면 validate schema 후 beforeSha256/afterSha256/consentVersion/joint/model_id/purpose/hmacKeyVersion 이 이번 값과 전부 일치 → put 0 반환 'committed'(idempotent); 하나라도 불일치 → overwrite 금지 + log.error + 반환 'conflict'; 부재하면 조건부 PUT 진행**; (4) blur 미실행 — before_png 는 호출측(31-09)이 BLUR_OPTION 에 따라 로드한 바이트 (H2-10); (5) **put 순서 before.png → after.png → meta.json (마지막=commit marker), 각 조건부 put_object IfNoneMatch='*' + VersionId 수집. 412(PreconditionFailed=이미 존재) 면 skip 전에 기존 payload 검증 (6차 H6-04): head_object(size) + get_object(sha256) 가 이번 expected beforeSha256/afterSha256/size 와 일치 → resume(재PUT 0); 불일치 → log.error + 반환 'conflict'(meta 미기록, 이번 호출 put 성공분 best-effort delete). 예외 시 이번 호출 put 성공 object best-effort delete(VersionId 지정) 후 'failed' + log.warning (재raise 금지)**; meta.json: {consentVersion, consentCapturedAtMs, purpose "phase22-v2-generation-head", joint, model_id, judge_confidence, pose_error_deg, hmacKeyVersion(H2-06), **beforeSha256, afterSha256, beforeSize, afterSize (H5-08 payload 검증용)**, sourceGeneration, provenance, blurApplied + anonymizerVersion(H2-10), storedAtMs} — uid/analysisId 원문 미포함. **`list_committed_pairs(s3_client, bucket) -> dict`(H4-11 + H5-08 + M5-05): PAIRS_PREFIX 열거(continuation token 끝까지) → 각 pair prefix 에 `_read_valid_meta` → 통과 시 meta.beforeSha256/afterSha256/beforeSize/afterSize 로 before/after HEAD(size) + 필요 시 GET(hash) 확인 → 전부 일치만 {pair_id, before_key, after_key, meta} 를 "pairs" 에, 부재/불일치는 "quarantine"(사유 코드) 에 분리 + quarantine 카운트 metric. 반환 {"pairs":[...], "quarantine":[...]}.** **`load_committed_pair(s3_client, bucket, pair_id) -> dict | None`: meta.json 로드+schema 검증 + before/after 존재·hash 확인 실패 → None; 통과 → {before_key, after_key, meta}.** 두 helper 가 meta+payload 검증을 단일 내부 경로(`_read_valid_meta` + `_verify_payload`)로. 모듈 docstring 에 consumer 계약 박제: **"consumer(phase 22 datagen)는 list_committed_pairs/load_committed_pair 만 사용 — before/after prefix 직접 list 금지, marker+payload hash 검증분만 학습 소비 (H4-11/H5-08)"** + 보존 정책("보존 = privacy_decision.json retentionDays — lifecycle Expiration + NoncurrentVersionExpiration, 31-12 적용"). test_pair_store.py: skipped_consent(False/None/"true") → put 0; True+고정 pair_id/keyVersion → put 3(순서 assert) + pair_id key + meta uid/analysisId 부재 + hmacKeyVersion + before/afterSha256; **B5-03 meta-read 멱등: 동일 pair_id/keyVersion 재호출(meta 존재+일치) → put 0 + 'committed'; before/after hash 불일치 meta 존재 → 'conflict' + overwrite 0; crash+rotation 모사(v1 로 커밋 후 caller 가 여전히 v1 pair_id 재전달) → put 0 재적재 없음**; **부분 실패 3종(H3-07): 1번째 실패 → object 0; 2번째 → before 삭제 후 'failed'; 3번째(meta) → before/after 삭제 후 'failed' — 각 삭제 검증**; **H6-04 partial 재개: meta 부재 + before 만 존재(before crash) → before 412 이나 hash 일치 → resume 후 after/meta put; meta 부재 + before 변조(preexisting hash 불일치) → 'conflict' + meta put 0**; **validator(H3-11): unknown key/비-hex/31바이트/33바이트/vX 위반/active not in keys → None + 적재 거부**; **consumer helper(H5-08): before/after+meta 온전 → pairs; before 누락/after hash 변조 → quarantine(pairs 미포함) + 사유; meta 없는 prefix 제외; pagination — 1,000+ 페어 fixture 로 continuation 끝까지 누락 0 (M5-05)**; **CONSENT_VERSION + BLUR_OPTION == privacy_decision.json 대조 (M2-05/M3-02)**; HISTORICAL_PAIR_JOINTS ⊇ ARROW_JOINT_MAP 6 관절; 소스에 anonymize/ultralytics import 부재(H2-10) + '.planning' 문자열 부재(M3-02).</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31/test_pair_store.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `is True` strict (truthy 0)
    - caller-fixed pair_id/hmac_key_version 인자 + 내부 active key 재선택 0 (grep) (B5-03)
    - meta-read 멱등: 일치 재호출 put 0 'committed' / 불일치 'conflict' overwrite 0 / crash+rotation 재PUT 0 (B5-03)
    - partial 재개 412: 기존 before/after payload hash 일치 시만 resume, 불일치 'conflict' + meta 미기록 (H6-04)
    - commit marker 순서 + 부분 실패 3종 정리 green (H3-07)
    - consumer helper 가 payload hash 검증 + quarantine 분리 + pagination green (H4-11/H5-08/M5-05)
    - validate_hmac_key_set 단일 validator + 스키마 케이스 green (H3-11)
    - CONSENT_VERSION/BLUR_OPTION 대조 + 런타임 .planning 참조 0 (M2-05/M3-02)
    - HISTORICAL_PAIR_JOINTS append-only (M3-05) + meta uid/analysisId 부재, blur 코드 0
  </acceptance_criteria>
  <done>동의·역추적·partial orphan·무가명·미커밋/변조 소비·crash-rotation 중복이 단위 테스트로 차단된 적재/소비 helper 완성.</done>
</task>

<task type="auto">
  <name>Task 2: 삭제 스크립트 — 전 키 버전 × historical registry × versioned 완전 삭제</name>
  <files>backend/scripts/delete_training_pair.py, backend/tests/phase31/test_pair_store.py</files>
  <read_first>backend/shared/python/sunity_shared/analysis/pair_store.py (Task 1 의 pair_id/validate_hmac_key_set/HISTORICAL_PAIR_JOINTS — import 재사용), backend/scripts/ 하위 기존 스크립트 1개 (argparse/AWS_PROFILE 관례), .planning/phases/31-api-visual-correction/31-PLAN-REVIEW-ITERATION3.md 의 H3-08 절 (versioned 삭제 규칙) + ITERATION6 의 H6-06 절 (marker-pair pagination)</read_first>
  <action>delete_training_pair.py — argparse: --uid --analysis-id (--joint 옵션, 미지정 시 **HISTORICAL_PAIR_JOINTS 순회 — ARROW_JOINT_MAP 금지 M3-05**) --profile 기본 sunity-motion --dry-run. 동작: (1) SSM 로드 → **validate_hmac_key_set(실패 시 중단)**; (2) **inventory gate(H3-11): 후보 prefix meta.json 의 hmacKeyVersion 이 전부 key set 에 존재하는지 확인 — 미존재 시 중단**; (3) keys 전 버전(활성+retired) × HISTORICAL_PAIR_JOINTS 로 pair_id 재계산 → 각 prefix 에 **get-bucket-versioning → enabled/suspended 면 list_object_versions 를 boto3 paginator(또는 NextKeyMarker+NextVersionIdMarker 쌍 명시)로 끝까지 열거해 Versions+DeleteMarkers 전부 versionId delete_objects; 아니면 일반 list/delete (H3-08 + 6차 H6-06)**; (4) **삭제 후 list_objects + list_object_versions 재조회로 current/noncurrent/delete-marker 0 확인 — 잔존 시 비정상 종료**; dry-run 목록만. 버전별 삭제 수 stdout. test_pair_store.py 추가(boto3 mock): **key rotation — v1 적재 → v2 추가 → v1 재계산 삭제**; **versioned bucket(H3-08 + H6-06): 단일 key 에 Versions 2 + DeleteMarkers 1 이 페이지 경계로 나뉘어도(KeyMarker+VersionIdMarker paginator) 3건 전부 versionId 삭제 + 재조회 0 — marker 쌍 누락/반복 0**; **inventory gate: meta v3 인데 key set v1/v2 → 중단**; historical registry 순회(ARROW_JOINT_MAP 참조 부재 grep); dry-run delete 0.</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31/test_pair_store.py -x -q && python -c "import ast; ast.parse(open('backend/scripts/delete_training_pair.py').read())"</automated>
  </verify>
  <acceptance_criteria>
    - delete_training_pair.py 가 pair_id/validate_hmac_key_set/HISTORICAL_PAIR_JOINTS import (재구현·ARROW_JOINT_MAP 순회 0)
    - versioned bucket 완전 삭제 + 재조회 0 green + marker-pair paginator 경계 누락 0 (H3-08/H6-06)
    - inventory gate + rotation 전후 삭제 green (H2-06/H3-11)
    - dry-run/실삭제 분기 green
  </acceptance_criteria>
  <done>삭제/철회 경로가 키 회전·joint rename·bucket versioning 전부에 안전한 실행 가능 스크립트로 존재.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 생성물 → 학습 저장소 | 동의+품질 통과분만, 가명 ID 로만, caller-fixed pairId + meta-read 멱등 commit marker 로만 확정 |
| 학습 저장소 → consumer | list_committed_pairs 만 — marker+payload hash 검증분만, 변조/부분은 quarantine |
| 학습 저장소 → 역추적 | HMAC 단방향 — 키 보유자만 재계산 |
| 키 회전 / bucket versioning | retired 버전 + inventory gate + noncurrent 완전 삭제 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-24 | Privacy | 동의 우회 | mitigate | learning_opt_in is True strict + 비통과 S3 0 |
| T-31-25 | Information Disclosure | 저장소 역추적 | mitigate | HMAC 가명 + uid/analysisId 원문 미기록 (H-04) |
| T-31-26 | Repudiation | 삭제 요청 이행 불능 | mitigate | versioned key set + inventory gate + historical registry + versionId 완전 삭제 + 재조회 (H2-06 + H3-08/H3-11/M3-05) |
| T-31-27 | Information Disclosure | 무가명/부분 적재 orphan / 미커밋·변조 소비 | mitigate | strict validator + commit marker + 부분 실패 정리 + payload-hash consumer helper (H3-07/H3-11/H4-11/H5-08) |
| T-31-71 | Tampering | postprocess crash + HMAC rotation 로 중복 학습쌍 | mitigate | caller-fixed pairId + meta-read 멱등(일치 put 0 / 불일치 conflict) (B5-03) |
| T-31-78 | Tampering | before-only/after-only crash 후 변조 payload 가 조용히 학습쌍 커밋 | mitigate | meta 부재 재개 시 412 payload HEAD/GET hash 검증 후에만 resume, 불일치 conflict (H6-04) |
</threat_model>

<verification>
- `python -m pytest backend/tests -q` green
- pair_store 상수(CONSENT_VERSION/BLUR_OPTION)가 privacy_decision.json 과 테스트 대조 (M2-05/M3-02)
- store_training_pair 내부에 active key 재선택 코드 부재 (grep — caller-fixed 강제 B5-03)
</verification>

<success_criteria>
- D-01 페어 적재가 strict 동의·versioned 가명화·caller-fixed pairId·meta-read 멱등(partial 재개 payload hash 검증 — H6-04)·commit marker·단일 validator·payload-verify paginated consumer helper·historical registry·versioned 완전 삭제(marker-pair pagination — H6-06)까지 테스트 고정 — 3차 §8-3 gate 10 + 4차 H4-11 + 5차 B5-03/H5-08 + 6차 H6-04/H6-06 추적
</success_criteria>

<output>
Create `.planning/phases/31-api-visual-correction/31-07-SUMMARY.md` when done
</output>
