---
phase: 31-api-visual-correction
plan: 07
type: execute
wave: 2
depends_on: [31-01, 31-02]
files_modified:
  - backend/shared/python/sunity_shared/analysis/pair_store.py
  - backend/scripts/delete_training_pair.py
  - backend/tests/phase31/test_pair_store.py
autonomous: true
requirements: [D-01]
must_haves:
  truths:
    - "[틀린 폼→고쳐진 폼] 페어가 learningOptIn is True 사용자에 한해 적재된다 — false/부재/비-bool 은 S3 호출 0 (D-01 + [[learning-consent-pilot-mandatory]])"
    - "페어 ID 는 HMAC 가명 — S3 key·메타 어디에도 uid/analysisId 원문이 없다 (리뷰 H-04)"
    - "메타에 consentVersion/capturedAtMs/처리 목적/판정 메트릭이 기록된다 (versioned consent — 리뷰 H-04)"
    - "삭제 경로가 존재한다 — analysisId 로 HMAC 재계산해 페어를 삭제하는 스크립트 (리뷰 H-04 deletion path)"
    - "HMAC 키 미설정 시 적재 자체를 거부한다 (fail-closed — 가명화 없는 적재 불가)"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/pair_store.py"
      provides: "consent-gated·HMAC 가명화 페어 적재"
      contains: "is True"
    - path: "backend/scripts/delete_training_pair.py"
      provides: "HMAC 재계산 삭제 스크립트"
    - path: "backend/tests/phase31/test_pair_store.py"
      provides: "동의 3분기·가명화·fail-closed·삭제 테스트"
      min_lines: 70
  key_links:
    - from: "backend/shared/python/sunity_shared/analysis/pair_store.py"
      to: "S3 training/phase31/pairs/"
      via: "boto3 put_object (consent+HMAC 통과 시만)"
      pattern: "training/phase31/pairs"
---

<objective>
D-01 의 플라이휠 부산물(학습 페어)을 리뷰 H-04 privacy 요구대로 구현한다: strict 동의 게이트(learningOptIn is True), HMAC-SHA256 가명 pairId (uid/analysisId 원문 미기록 — 삭제는 HMAC 재계산으로 가능해 역추적 맵 자체가 불필요), versioned consent 메타, 삭제 스크립트, 31-01 Privacy Release Gate 결정(블러 여부 포함) 반영. 31-09 워커가 training 게이트(judge training_pass + pose 오차 <= 8°) 통과분만 이 모듈로 적재한다.

Purpose: phase 22 재도전 원료를 privacy 통제 하에 제조 — 동의 우회·역추적·삭제 불능이 구조적으로 차단된 형태.
Output: pair_store.py + 삭제 스크립트 + 테스트.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-api-visual-correction/31-ACCEPTANCE.md
@.planning/phases/31-api-visual-correction/31-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: pair_store.py — strict 동의 + HMAC 가명화 적재</name>
  <files>backend/shared/python/sunity_shared/analysis/pair_store.py, backend/tests/phase31/test_pair_store.py</files>
  <read_first>backend/training/datagen/enumerate_internal.py lines 87-110 (consent_allows — strict 분기 형상; phase 31 페어는 전부 신규 생성물이라 bulk_approval 경로 사용 금지), backend/training/datagen/build_jsonl.py lines 40-41 (S3 prefix 상수 관례), .planning/phases/31-api-visual-correction/31-ACCEPTANCE.md 의 Privacy Release Gate 절 (31-01 belle 결정 — 블러 여부 포함), backend/tests/phase31/conftest.py</read_first>
  <action>신규 pair_store.py (docstring: D-01 + [[learning-consent-pilot-mandatory]] + 리뷰 H-04 + 31-ACCEPTANCE Privacy Release Gate 인용). 상수 `PAIRS_PREFIX = "training/phase31/pairs/"`, `CONSENT_VERSION = "pilot-optout-v1"`. `pair_id(uid: str, analysis_id: str, joint: str, *, hmac_key: bytes) -> str` — hmac.new(hmac_key, f"{uid}:{analysis_id}:{joint}".encode(), hashlib.sha256).hexdigest()[:24]. `store_training_pair(s3_client, bucket, *, uid, analysis_id, joint, before_png: bytes, after_png: bytes, learning_opt_in, consent_captured_at_ms: int | None, quality: dict) -> bool`: (1) 게이트 = `learning_opt_in is True` 만 통과 — False/None/문자열 "true"/그 외 전부 즉시 False + S3 호출 0 (truthy 검사 금지); (2) HMAC 키 = env `PAIR_ID_HMAC_KEY` (SSM `/sunity/motion/pair-id-hmac-key` 주입 — 31-10 IaC) — 미설정/빈값이면 적재 거부 False + log.warning (fail-closed: 가명화 불가 시 저장 불가); (3) 31-01 결정이 블러(option-b)면 before_png 에 anonymize_batch 얼굴 블러 helper 적용 (실패 시 적재 포기 False) — option-a 면 원본; (4) `{PAIRS_PREFIX}{pair_id}/before.png`, `after.png`, `meta.json` 3객체 put — meta.json: {consentVersion, consentCapturedAtMs, purpose: "phase22-v2-generation-head", joint, model_id, judge_confidence, pose_error_deg, storedAtMs} — **uid/analysisId 원문은 key·메타 어디에도 미포함** (역추적은 HMAC 재계산으로만, 리뷰 H-04); (5) 모든 실패 False + log.warning (재raise 금지 — 부가 기능). test_pair_store.py: False/None/"true" → put 0회; True + 키 존재 → put 3회 + key 가 pair_id 기반 + meta 에 uid/analysisId 문자열 부재 assert; HMAC 키 미설정 → 0회 + False; put 예외 → False; consentVersion 기록 검증.</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31/test_pair_store.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - pair_store.py 에 `is True` strict 검사 존재 (truthy 검사 0)
    - meta.json/S3 key 에 uid·analysisId 원문 부재가 테스트로 고정
    - HMAC 키 미설정 → 적재 거부 fail-closed 테스트 green
    - `python -m pytest backend/tests/phase31/test_pair_store.py -x -q` exits 0
  </acceptance_criteria>
  <done>동의 우회·역추적·무가명 적재가 단위 테스트로 차단된 적재 helper 완성.</done>
</task>

<task type="auto">
  <name>Task 2: 삭제 스크립트 + 보존 정책 기록</name>
  <files>backend/scripts/delete_training_pair.py, backend/tests/phase31/test_pair_store.py</files>
  <read_first>backend/shared/python/sunity_shared/analysis/pair_store.py (Task 1 의 pair_id — 동일 함수 import 재사용, 재구현 금지), backend/scripts/ 하위 기존 스크립트 1개 (argparse/AWS_PROFILE 관례)</read_first>
  <action>delete_training_pair.py — argparse: --uid --analysis-id (--joint 옵션, 미지정 시 ARROW_JOINT_MAP 전 관절 후보 순회) --profile (기본 sunity-motion) --dry-run. 동작: SSM 에서 PAIR_ID_HMAC_KEY 로드 → pair_store.pair_id 재계산 → `training/phase31/pairs/{pair_id}/` 하위 객체 list + delete (dry-run 은 목록만 출력). 삭제 결과(삭제 객체 수) stdout 요약 — 사용자 삭제 요청/동의 철회 대응 경로 (리뷰 H-04). pair_store.py 모듈 docstring 에 보존 정책 1줄 박제: "보존 = phase 22 재도전 트랙 활성 기간, 삭제 요청 시 delete_training_pair.py — 31-ACCEPTANCE Privacy Release Gate 참조". test_pair_store.py 에 삭제 로직 테스트 추가 (boto3 mock): 재계산 pair_id 가 적재 pair_id 와 일치, dry-run 은 delete 0회, 실행 시 3객체 delete.</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31/test_pair_store.py -x -q && python -c "import ast; ast.parse(open('backend/scripts/delete_training_pair.py').read())"</automated>
  </verify>
  <acceptance_criteria>
    - delete_training_pair.py 가 pair_store.pair_id 를 import (자체 HMAC 재구현 0)
    - dry-run/실삭제 분기 테스트 green
    - 보존 정책 문구가 pair_store.py docstring 에 존재
  </acceptance_criteria>
  <done>삭제/철회 경로가 실행 가능한 스크립트로 존재 — H-04 deletion path 충족.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 생성물 → 학습 저장소 | 동의+품질 통과분만, 가명 ID 로만 |
| 학습 저장소 → 역추적 | HMAC 단방향 — 키 보유자만 재계산 가능 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-24 | Privacy | 동의 우회 | mitigate | learning_opt_in is True strict + 비통과 S3 호출 0 테스트 고정 |
| T-31-25 | Information Disclosure | 저장소 역추적 | mitigate | HMAC 가명 pairId + uid/analysisId 원문 미기록 (테스트 고정, 리뷰 H-04) |
| T-31-26 | Repudiation | 삭제 요청 이행 불능 | mitigate | HMAC 재계산 삭제 스크립트 + dry-run (리뷰 H-04 deletion path) |
| T-31-27 | Information Disclosure | 무가명 적재 | mitigate | HMAC 키 미설정 시 적재 거부 fail-closed |
</threat_model>

<verification>
- `python -m pytest backend/tests -q` green
- pair_store 가 31-01 Privacy Release Gate 결정(블러 여부)을 반영
</verification>

<success_criteria>
- D-01 페어 적재가 strict 동의·가명화·versioned consent·삭제 경로까지 테스트로 고정
</success_criteria>

<output>
Create `.planning/phases/31-api-visual-correction/31-07-SUMMARY.md` when done
</output>
