---
phase: 31-api-visual-correction
plan: 08
type: execute
wave: 2
depends_on: [31-04]
files_modified:
  - app/src/lib/referenceMotions.ts
  - app/src/components/PoseCompareViewer.tsx
  - app/src/components/ReferenceCornerSection.tsx
autonomous: false
requirements: [D-04, D-08, D-09, D-10]
must_haves:
  truths:
    - "비교 뷰어는 2D 카메라 평면 고정 — 회전/orbit 없음, '3D'/'회전' 문구 0 (amended D-10, 리뷰 B-01 해소)"
    - "뷰어는 내 자세 + 목표 자세 반투명 중첩을 DTW 대응 결함 프레임 1장으로 보여준다 (amended D-10 v1)"
    - "회전 니즈는 Wan2.7 합성 영상 카드가 전담한다 (amended D-04 — 유일 회전 수단)"
    - "참고코너는 점수 내역과 시각 분리된 '참고하세요' 섹션이고 점수 수치를 표시하지 않는다 (D-09 + 점수 비반영 invariant)"
    - "reference pose 는 단일 문서 구독으로 로드된다 — 전체 컬렉션 구독에 대형 배열 확장 금지 (리뷰 M-02)"
    - "UI 구현 전 최악 데이터 케이스 목업이 belle 에게 선제시된다 ([[ux-propose-user-centric-screens-first]])"
  artifacts:
    - path: "app/src/components/PoseCompareViewer.tsx"
      provides: "SVG 2D 중첩 뷰어 (react-native-svg — R3F/GL 미사용)"
      min_lines: 80
    - path: "app/src/components/ReferenceCornerSection.tsx"
      provides: "참고코너 섹션 (교정 자세 카드 + 2D 뷰어 + 회전 영상 카드/버튼)"
      min_lines: 120
    - path: "app/src/lib/referenceMotions.ts"
      provides: "useReferenceMotionDoc — 단일 문서 구독 + joints3d 방어 파싱"
      contains: "useReferenceMotionDoc"
  key_links:
    - from: "app/src/components/ReferenceCornerSection.tsx"
      to: "app/src/components/PoseCompareViewer.tsx"
      via: "userPose/refPose props"
      pattern: "PoseCompareViewer"
    - from: "app/src/lib/referenceMotions.ts"
      to: "Firestore reference/{id}"
      via: "doc onSnapshot (컬렉션 구독 아님)"
      pattern: "joints3d"
---

<objective>
amended D-10 의 정직한 계약을 구현한다: 2D 자세 비교 뷰어 — 카메라 평면 고정, 회전 없음, react-native-svg 기반 (PoseViewer3D/R3F 는 사용하지 않음 — result.tsx:1191 의 제거 결정과 정합, 리뷰 B-01 해소). 내 자세(브랜드색) + 목표 자세(반투명 보조색)를 DTW 대응 결함 프레임 1장(31-03 이 방출하는 userFrameIdx/refFrameIdx)으로 중첩한다. 참고코너 섹션(D-09)은 교정 자세 카드·뷰어·회전 영상 카드/버튼을 "참고하세요"로 묶는다. reference 로드는 단일 문서 구독 훅 신설 (리뷰 M-02 — 컬렉션 전체 구독 확장 금지).

프레임 스크럽/팬줌은 amended D-10 에서 "허용" 항목 — v1 은 정적 fit 렌더로 시작 (locked 요구는 중첩·무회전·정직 카피이며 축소 아님; 제스처는 후속 폴리시).

Purpose: 회전 영상 대기 중에도 즉시 쓸 수 있는 비용 0 비교 시각물 + 참고코너 뼈대 — 거짓 3D 없이.
Output: 컴포넌트 2종 신설 + 단일 문서 훅. result.tsx 통합은 31-11.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-api-visual-correction/31-CONTEXT.md
@.planning/phases/31-api-visual-correction/31-PATTERNS.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: 참고코너 최악 케이스 목업 선제시 + 교정 자세 카드 위치 확정</name>
  <decision>참고코너 레이아웃·교정 자세 카드 소속 (구 Open Q3) + 카피 확정</decision>
  <read_first>.planning/phases/31-api-visual-correction/31-CONTEXT.md 의 D-09/D-10 [AMENDED] 절 (2D 뷰어 재결정 원문 — 목업 표현의 기준), app/src/app/analysis/result.tsx lines 1419-1440 (섹션 스택 현행 순서)</read_first>
  <context>[[ux-propose-user-centric-screens-first]] — 최악 데이터 케이스 목업 선제시. Figma 우선 (CLAUDE.md §4, fileKey jrdI7kp245HkPfLB0nclsz): 결과 화면 프레임에서 참고코너/교정 이미지 설계 유무를 먼저 조회 — 있으면 그 프레임 기준, 없으면 design.md 자체 구성임을 명시. 제시할 최악 케이스 5종: (1) correctedPose failed → 카드 숨김 (섹션에 뷰어만), (2) mode3 → 비교 뷰어 자체 숨김 (mode1 전용 — DTW 대응이 mode1 reference 에만 성립, Pitfall 6; 숨김이 단독 스켈레톤 표시보다 정직), (3) rotationStatus pending 6~7분 대기 카드, (4) 만료 URL → 재서명 로딩 상태, (5) refMatched false → 뷰어 숨김. 카피 규칙: "3D"/"회전"/"돌려보세요"/"실루엣" 금지 — 뷰어는 "자세 비교" 계열, 생성 이미지는 "교정된 자세" 계열 (리뷰 B-01/L-02), 다각도 촬영 유도 0, 점수 수치 0, 전부 한국어.</context>
  <options>
    <option id="option-a">
      <name>교정 자세 카드를 참고코너 소속으로 (권고)</name>
      <pros>비채점 시각물 일관 배치 — D-09 원칙이 레이아웃으로 드러남, 섹션 1곳 신설</pros>
      <cons>결함 줌 캐러셀과 화면상 떨어짐</cons>
    </option>
    <option id="option-b">
      <name>교정 자세 카드를 결함 줌 캐러셀 옆에</name>
      <pros>교정물이 결함 지적 바로 옆 — 동선 자연스러움</pros>
      <cons>채점 영역에 비채점 생성물이 섞임 — 점수 비반영 원칙 흐려짐</cons>
    </option>
  </options>
  <resume-signal>option-a 또는 option-b + 목업 승인 (수정 요청 시 반영 후 재제시)</resume-signal>
  <action>Figma 조회 → 최악 케이스 5종 텍스트 목업 구성 → AskUserQuestion 으로 belle 에게 선제시 + 카드 위치 확정. 승인 전 Task 2/3 착수 금지.</action>
  <verify>
    <automated>N/A - checkpoint:decision (belle 응답이 게이트)</automated>
  </verify>
  <acceptance_criteria>
    - belle 이 option 을 명시 선택 + 목업 5종 제시가 대화 기록으로 확인 가능
    - Figma 조회 결과가 목업 제시에 명시됨
  </acceptance_criteria>
  <done>레이아웃·카피·카드 소속이 belle 승인으로 확정.</done>
</task>

<task type="auto">
  <name>Task 2: useReferenceMotionDoc 단일 문서 훅 + PoseCompareViewer 2D 뷰어</name>
  <files>app/src/lib/referenceMotions.ts, app/src/components/PoseCompareViewer.tsx</files>
  <read_first>app/src/lib/referenceMotions.ts (전체 — referenceKeypointReport null-guard idiom lines 103-130, 기존 useReferenceMotion/useReferenceMotions 구조 — 컬렉션 구독 확장 금지 확인), app/src/components/PoseViewer3D.tsx lines 60-90 (RTMW joints3d 축 주석 — 수직 분산이 어느 축인지: 2D 투영 축 선택의 단일 근거; 컴포넌트 자체는 미사용), backend/scripts/reprocess_reference_motions_phase4.py lines 76-125 (_validate_payload_schema — reference 방출 스키마: joints3d/joints3dKeys/joints3dFrames, coordDim=3), app/src/components/OctagonScore.tsx (react-native-svg 사용 관례)</read_first>
  <action>referenceMotions.ts: 신규 훅 `useReferenceMotionDoc(referenceMotionId: string | null): { joints3d: number[][][] | null; frames: number; loading: boolean }` — **doc(db, 'reference', id) 단일 문서 onSnapshot** (컬렉션 구독 아님 — 리뷰 M-02, 주석으로 금지 사유 명시). id null 이면 구독 없이 즉시 null (훅은 항상 호출 — 리뷰 M-04 대비). joints3d flat 배열을 joints3dKeys(len 17)·joints3dFrames 와 대조해 길이 = frames×17×3 검증, 불일치/부재 → null (조용한 강등). reshape (T,17,3). reference 문서 쓰기 금지 (40k index-entry — 읽기 전용 주석). PoseCompareViewer.tsx 신설 — react-native-svg 만 사용 (three/@react-three/fiber import 금지): Props { userPose: number[][] | null; refPose: number[][] | null; jointKeys: string[] } — 각 pose 는 단일 프레임 (17,3). 렌더: (1) 투영 = 카메라 평면 2축 (PoseViewer3D 축 주석 기준 — 수평 x + 수직 축 선택, 깊이 축 폐기), (2) 정규화 = 양 pose 각각 hip 중점 원점 이동 + 어깨중점~hip중점 몸통 길이 스케일 → 뷰박스 fit, (3) 스켈레톤 = COCO-17 인접 bone 선분 (skeleton 연결 상수는 컴포넌트 내 선언) — 내 자세 colors 브랜드 토큰 stroke width 3, 목표 자세 보조색 + opacity 0.4, (4) userPose 또는 refPose null → null 반환 (렌더 가드는 caller 소유 관례 — 섹션이 숨김), (5) 파일 헤더 주석: amended D-10 + 리뷰 B-01 인용 + "회전/orbit/3D 문구 금지" 명시. 하드코딩 색상 0 (theme 토큰만), 이모지 0.</action>
  <verify>
    <automated>cd app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - referenceMotions.ts 에 useReferenceMotionDoc 존재 + 내부가 doc() 단일 구독 (collection() 신규 구독 0)
    - PoseCompareViewer.tsx 에 three/@react-three import 0, react-native-svg 사용
    - 컴포넌트/주석/문자열에 "3D"·"회전" 문구 0 (grep 으로 확인)
    - joints3d 길이 불일치 → null 강등 분기 존재
    - `cd app && npm run typecheck` exits 0
  </acceptance_criteria>
  <done>정직한 2D 비교 뷰어와 저비용 reference 로드 경로 완성.</done>
</task>

<task type="auto">
  <name>Task 3: ReferenceCornerSection.tsx — 참고코너 섹션</name>
  <files>app/src/components/ReferenceCornerSection.tsx</files>
  <read_first>app/src/components/ScoreBreakdownSection.tsx lines 1-75 (섹션 컴포넌트 표준형 — named export + inline prop 타입 + 헤더 주석 + StyleSheet 하단 + theme 토큰), app/src/types/analysis.ts (31-04 의 correctedPose*/rotation* 필드), app/src/components/PoseCompareViewer.tsx (Task 2 props)</read_first>
  <action>ScoreBreakdownSection 표준형으로 신설. 헤더 주석에 amended D-04/D-08/D-09/D-10 + 단일 카메라 invariant + Task 1 승인 레이아웃 인용. Props(inline): correctedPoseState: 'hidden'|'pending'|'loading'|'ready', correctedPoseImageUrl?: string (재서명 fresh URL — caller 가 playback-url asset 으로 발급, 31-11), onCorrectedPoseImageError?: () => void (만료/403 시 재발급 트리거), rotationState: 'hidden'|'pending'|'ready'|'requestable', rotationVideoUrl?: string, onRequestRotation?: () => void, rotationRequestBusy?: boolean, limitNotice?: string, userPose/refPose/jointKeys (뷰어 전달용). 렌더 (Task 1 승인 목업 기준): (1) 타이틀 "참고하세요" + 서브카피 "점수에는 반영되지 않아요" (D-09), (2) 교정된 자세 카드 — 'ready'+URL 만 Image 표시 + onError 에서 onCorrectedPoseImageError 호출 (만료 복구 — 리뷰 H-02), 'pending'/'loading' 자리표시, 'hidden' 렌더 생략 (에러 배너·토스트 절대 금지 — D-08), (3) PoseCompareViewer — 양 pose 유효 시만, 안내 카피는 "내 자세와 목표 자세를 겹쳐 보여드려요" 계열 (회전 지시 문구 금지), (4) 회전 참고 영상 카드 — 'ready' 면 expo-video 재생, 'pending' 이면 "생성 중이에요. 몇 분 걸려요" 자리표시, 'requestable' 이면 [회전 영상 만들기] 버튼 (busy 비활성, accessibilityRole/Label/hitSlop), 'hidden' 생략; limitNotice 존재 시 버튼 인근 인라인 텍스트 (배너/토스트 아님). 점수 수치·다각도 유도 문구·"3D"/"실루엣" 문구 0. theme 토큰만, 라이트 전용, 이모지 0.</action>
  <verify>
    <automated>cd app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - named export ReferenceCornerSection 존재
    - 'hidden' 상태에서 해당 카드 렌더 생략이 코드 분기로 확인 가능 + Alert/Toast 호출 0
    - 파일 내 하드코딩 hex 색상 0, "각도로 찍" 류 다각도 유도 문구 0, "3D" 문구 0
    - `cd app && npm run typecheck` exits 0
  </acceptance_criteria>
  <done>참고코너가 상태 구동 + 조용한 폴백 + 만료 복구 훅까지 갖춘 완결 컴포넌트로 존재.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firestore 필드 → 섹션 렌더 | normalize 통과 필드만 방어적으로 소비 |
| 재서명 URL → Image/Video | 만료 시 onError 복구 경로 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-28 | Repudiation (오인) | 거짓 3D 표현 재도입 | mitigate | R3F import 0 + "3D"/"회전" 문구 0 을 acceptance grep 으로 고정 (리뷰 B-01) |
| T-31-29 | Repudiation (신뢰 인상) | 실패 상태 노출 | mitigate | hidden = 미렌더 + Alert/Toast 0 (D-08) |
| T-31-30 | Information Disclosure | 만료 URL 소비 | mitigate | onError → 재서명 콜백 계약 (리뷰 H-02 앱 측) |
| T-31-31 | Denial of Service (읽기 비용) | reference 전체 컬렉션 대형 배열 구독 | mitigate | 단일 문서 구독 훅 강제 (리뷰 M-02) |
</threat_model>

<verification>
- `cd app && npm run typecheck` green
- 하드코딩 색상·다각도 유도·"3D" 문구 grep 0
- 컴포넌트 상태 분기 테스트는 31-11 (jest) 에서
</verification>

<success_criteria>
- amended D-10 뷰어 + D-09 참고코너가 정직 계약(무회전·2D)으로 완성
- 목업 checkpoint 통과 (belle 승인)
</success_criteria>

<output>
Create `.planning/phases/31-api-visual-correction/31-08-SUMMARY.md` when done
</output>
