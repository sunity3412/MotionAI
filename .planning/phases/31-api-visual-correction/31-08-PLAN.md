---
phase: 31-api-visual-correction
plan: 08
type: execute
wave: 3
depends_on: [31-01, 31-05]
files_modified:
  - app/src/lib/api.ts
  - app/src/app/analysis/result.tsx
autonomous: true
requirements: [D-06, D-08, D-09, D-10]
must_haves:
  truths:
    - "결과 화면 점수 내역 아래에 '참고하세요' 참고코너 섹션이 렌더된다 (D-09 — 채점과 시각적 분리)"
    - "실루엣/회전 카드가 silhouetteStatus/rotationStatus 를 onSnapshot 으로 구독해 자동 갱신된다 (D-06 완료 알림 = 카드 갱신, 폴링 0)"
    - "failed/부재(legacy) 상태는 카드 숨김 — 에러 배너·토스트 없음 (D-08 조용한 폴백)"
    - "[회전 영상 만들기] 버튼이 POST /visual/rotation 을 호출하고 429 는 한국어 한도 안내로 표시된다 (D-06/D-07)"
    - "mode1 = referenceMotionId 의 joints3d 중첩, mode3 = recognizedMotionId 있으면 그 reference, 없으면 내 자세 단독 강등 (D-10 + Pitfall 6)"
  artifacts:
    - path: "app/src/lib/api.ts"
      provides: "requestRotationVideo 클라이언트"
      contains: "visual/rotation"
    - path: "app/src/app/analysis/result.tsx"
      provides: "참고코너 섹션 삽입 + 상태 소비 effect"
      contains: "ReferenceCornerSection"
  key_links:
    - from: "app/src/app/analysis/result.tsx"
      to: "app/src/components/ReferenceCornerSection.tsx"
      via: "점수 내역(ScoreBreakdownSection) 아래 조건부 렌더"
      pattern: "ReferenceCornerSection"
    - from: "app/src/lib/api.ts"
      to: "POST /visual/rotation"
      via: "인증 fetch"
      pattern: "visual/rotation"
    - from: "app/src/app/analysis/result.tsx"
      to: "result.silhouetteStatus"
      via: "onSnapshot 구독 + pending 시간 상한 폴백"
      pattern: "silhouetteStatus"
---

<objective>
참고코너를 결과 화면에 통합한다: D-09 배치(점수 내역 아래, 31-05 목업 승인 레이아웃), 상태 소비(faultZoomStatus pending-고아 방어 effect 복제 — silhouette 는 짧은 상한, rotation 은 6~7분 생성에 맞는 긴 상한), 회전 영상 요청 클라이언트(202/429 처리), mode1/mode3 의 목표 자세 데이터 해석(D-10 + 강등 규칙). 추가 폴링 0 — onSnapshot 구독이 완료 알림이다.

Purpose: 사용자가 실제로 보는 최종 통합 — "어떻게 고쳐야 하는지" 가 결과 화면에 나타나는 지점.
Output: api.ts 클라이언트 + result.tsx 통합.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-api-visual-correction/31-PATTERNS.md
@docs/contract.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: api.ts requestRotationVideo 클라이언트</name>
  <files>app/src/lib/api.ts</files>
  <read_first>app/src/lib/api.ts (전체 — 인증 헤더 부착·에러 포맷 `${method} ${path} ${status}: ${text.slice(0,200)}`·API_BASE_URL 관례), docs/contract.md 의 POST /visual/rotation 절 (요청/응답/에러 코드 — 단일 기준)</read_first>
  <action>`requestRotationVideo(analysisId: string): Promise<{ rotationStatus: 'pending' | 'done'; rotationVideoUrl?: string }>` 추가 — 기존 인증 fetch 관례로 `POST /visual/rotation` body `{analysisId}`. 202/200 = 응답 파싱 후 반환. 429 는 특수 처리: 에러 코드 `daily_limit` 판별 가능한 형태로 throw (기존 Error 포맷 유지하되 message 에 `daily_limit` 포함 — 호출측이 includes('daily_limit') 로 분기). 그 외 상태는 기존 에러 포맷 그대로 throw. 주석에 contract.md 절 인용 + D-06/D-07 근거.</action>
  <verify>
    <automated>cd app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - api.ts contains `requestRotationVideo` and `visual/rotation`
    - 429 응답이 `daily_limit` 판별 가능하게 throw 됨
    - `cd app && npm run typecheck` exits 0
  </acceptance_criteria>
  <done>회전 요청 클라이언트가 계약 3면과 정합.</done>
</task>

<task type="auto">
  <name>Task 2: result.tsx 참고코너 통합 (섹션 삽입 + 상태 소비 + 목표 자세 해석)</name>
  <files>app/src/app/analysis/result.tsx</files>
  <read_first>app/src/app/analysis/result.tsx (전체 — 특히 lines 1056-1083 zoom pending effect 복제 대상, lines 1419-1432 섹션 삽입 지점, 기존 PoseViewer3D 사용부와 joints3d 소스, useReferenceMotion 사용부, mode1/mode3 분기와 recognizedMotionId 필드), app/src/components/ReferenceCornerSection.tsx (31-05 props 계약), app/src/lib/referenceMotions.ts (joints3d normalize 노출 형태)</read_first>
  <action>(1) 섹션 삽입: 31-05 Task 1 승인 레이아웃대로 — 기본은 ScoreBreakdownSection 렌더 블록 직후, sectionTitle + 근거 주석 블록(D-09 + 31-CONTEXT 인용) 형식. status=='done' 분석에서만 렌더. (2) 상태 소비: zoom pending effect (lines 1056-1083) 를 복제해 상수 2종 신설 — `SILHOUETTE_PENDING_TIMEOUT_MS = 180000` (이미지 수초~수십초 + 여유), `ROTATION_PENDING_TIMEOUT_MS = 1200000` (6~7분 생성 + 재시도 여유 — 20분). timeout 초과 pending 은 failed 와 동일하게 숨김 (고아 방어). 추가 폴링·setInterval 금지 — onSnapshot 재렌더가 전부 (안티패턴 주석 관례 유지). (3) 목표 자세 데이터: mode1 = 기존 useReferenceMotion(referenceMotionId) 결과의 joints3d normalize 산출을 referenceJoints 로 전달; mode3 = recognizedMotionId (Phase 30 신설 필드) 존재 시 그 reference 문서로 useReferenceMotion, 부재/실패 시 referenceJoints 생략 (내 자세 단독 강등 — 빈 화면 금지, Pitfall 6). myJoints = 기존 결과 joints3d reshape 경로 재사용. (4) 회전 버튼 핸들러: busy state + requestRotationVideo 호출, 성공 시 즉시 로컬 상태로 pending 표시 (onSnapshot 이 곧 동기화), catch 에서 message 에 `daily_limit` 포함 시 limitNotice 로 "오늘 생성 한도에 도달했어요. 내일 다시 시도해 주세요." 를 섹션에 전달 (버튼 인근 인라인 — 배너/토스트 아님), 그 외 에러는 조용히 무시 + 버튼 원복 (D-08 — 기능 불안 인상 방지). try/catch/finally 로 busy 해제.</action>
  <verify>
    <automated>cd app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - result.tsx contains `ReferenceCornerSection` and `ROTATION_PENDING_TIMEOUT_MS`
    - setInterval/추가 폴링 코드 0 (grep: `setInterval` 신규 0건)
    - mode3 + recognizedMotionId 부재 경로에서 referenceJoints 가 전달되지 않는 분기 존재
    - 회전 요청 실패(비-429)에 Alert/토스트/에러 배너 호출 0
    - `cd app && npm run typecheck` exits 0
  </acceptance_criteria>
  <done>결과 화면에서 참고코너가 상태 구동으로 동작하고, 모든 실패가 조용히 처리된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firestore doc → 화면 | normalize 통과 필드만 소비 (31-01 방어 파싱) |
| 앱 → /visual/rotation | 인증 토큰 부착, 응답 방어 파싱 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-27 | Spoofing | 회전 요청 | mitigate | 기존 인증 fetch 관례 (Firebase ID token) — 무토큰 경로 없음 |
| T-31-28 | Denial of Service | 버튼 연타 | mitigate | busy state 비활성 + 서버측 pending dedupe (31-07) 이중 방어 |
| T-31-29 | Repudiation (신뢰 인상) | 생성 실패 노출 | mitigate | 비-429 실패 조용히 무시 + 카드 숨김 (D-08) — acceptance 로 Alert/토스트 0 고정 |
</threat_model>

<verification>
- `cd app && npm run typecheck` green
- 결과 화면 실기기 확인은 HUMAN-UAT.md 적립 (31-09 — 즉시 belle 호출 금지)
</verification>

<success_criteria>
- D-09 배치 + D-08 조용한 폴백 + D-06 온디맨드 버튼 + D-10 mode 별 중첩/강등이 코드로 완결
- 추가 폴링 0 (onSnapshot 만)
</success_criteria>

<output>
Create `.planning/phases/31-api-visual-correction/31-08-SUMMARY.md` when done
</output>
