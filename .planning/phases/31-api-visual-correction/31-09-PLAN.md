---
phase: 31-api-visual-correction
plan: 09
type: execute
wave: 4
depends_on: [31-06, 31-07, 31-08]
files_modified:
  - .planning/phases/31-api-visual-correction/31-HUMAN-UAT.md
  - plan.md
autonomous: false
requirements: [D-01, D-06]
must_haves:
  truths:
    - "신규 Lambda 2종 + VisualQueue 가 실 스택(sunity-motion-pilot)에 배포된다 (belle 승인 — sam deploy 는 auto-mode 차단)"
    - "배포 후 무토큰 POST /visual/rotation 이 401 을 반환한다 (인증 실경로 검증)"
    - "회전 워커 실호출 1건이 rotationStatus 를 done 또는 failed(재시도 1회 후)로 전이시킨다 — 배선 실증 (D-06)"
    - "앱 변경(참고코너)이 OTA 로 발행된다 (phase 마감 OTA 관례)"
    - "실기기 확인 항목이 HUMAN-UAT.md 에 적립된다 (즉시 belle 호출 금지 — [[batch-uat-after-phase-31]])"
    - "1단+2단 풀통합(D-01)의 배포 상태가 SUMMARY 에 박제된다 (실루엣 자동생성 E2E 는 Pod 재생성 후 — DASHSCOPE_API_KEY Pod env 필요 기록)"
  artifacts:
    - path: ".planning/phases/31-api-visual-correction/31-HUMAN-UAT.md"
      provides: "배치 UAT 적립 항목 (D-09/D-10/회전 카드 실기기)"
      contains: "D-10"
  key_links:
    - from: "backend/template.yaml"
      to: "sunity-motion-pilot 스택"
      via: "sam build --use-container + belle sam deploy"
      pattern: "sunity-motion-pilot"
---

<objective>
Phase 31 통합 마감: 전체 suite green 확인 → sam build → belle 배포 승인(checkpoint — auto-mode classifier 가 sam deploy 를 차단하므로 belle 이 `!` 프리픽스로 실행, 2026-07-19 선례) → 배포 후 실검증(401 + 워커 실호출 1건 ~$0.8) → OTA 발행 → HUMAN-UAT 적립. Pod OFF 이므로 실루엣 자동 생성의 실 E2E 는 Pod 재생성 후로 이월하고, Pod env 체크리스트에 DASHSCOPE_API_KEY 추가 필요를 박제한다.

Purpose: D-01 풀통합 산출물을 실제로 라이브에 올리고 검증 잔여를 명시적으로 적립.
Output: 배포된 스택 + OTA + 31-HUMAN-UAT.md + plan.md 갱신.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-api-visual-correction/31-CONTEXT.md
@backend/samconfig.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: 전체 검증 + sam build</name>
  <files>(빌드 산출물만 — 소스 변경 없음)</files>
  <read_first>backend/samconfig.toml (stack sunity-motion-pilot / region ap-northeast-2 / Stage=pilot 배포 기본값)</read_first>
  <action>순서대로: (1) `python -m pytest backend/tests -q` 전체 green 확인, (2) `cd app && npm run typecheck` green 확인, (3) `cd backend && sam build --use-container` (Mac native binary 함정 — --use-container 필수, 메모리 [[sam-build-native-deps]]), (4) `sam validate --lint` 재확인. 실패 시 원인 수정은 해당 플랜 파일 소유 원칙 준수 — 이 플랜에서 소스 수정이 필요하면 최소 diff 로 수정 후 SUMMARY 에 기록.</action>
  <verify>
    <automated>cd backend && sam validate --lint && python -m pytest backend/tests -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest backend/tests -q` exits 0
    - `cd app && npm run typecheck` exits 0
    - `sam build --use-container` exits 0 (.aws-sam/build 에 VisualRequestFunction/VisualWorkerFunction 존재)
  </acceptance_criteria>
  <done>배포 가능한 빌드 산출물 준비 완료.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: belle 배포 실행 (sam deploy — auto-mode 차단 우회)</name>
  <what-built>VisualRequestFunction(POST /visual/rotation) + VisualWorkerFunction + VisualQueue/DLQ + LogGroup 2종이 포함된 sam build 완료 상태. sam deploy 는 auto-mode classifier 가 차단하므로 belle 의 수동 실행이 필요 (2026-07-19 선례: belle `!` 프리픽스 실행으로 성공).</what-built>
  <how-to-verify>
    1. 터미널에서 `!cd backend && sam deploy` 실행 (samconfig.toml 기본값 — stack sunity-motion-pilot, ap-northeast-2)
    2. changeset 에 VisualRequestFunction / VisualWorkerFunction / VisualQueue 3종이 Add 로 표시되는지 확인 후 승인
    3. 완료 로그 "Successfully created/updated stack - sunity-motion-pilot" 확인
  </how-to-verify>
  <resume-signal>"deployed" 입력 (실패 시 에러 로그 붙여넣기)</resume-signal>
  <action>belle 에게 how-to-verify 순서를 안내하고 sam deploy 수동 실행을 요청한다 (auto-mode classifier 가 sam deploy 를 차단하므로 Claude 직접 실행 금지 — 2026-07-19 선례). 응답 후 Task 3 으로 진행.</action>
  <verify>
    <automated>N/A - checkpoint:human-action (belle 배포 완료가 게이트) — 배포 결과 실검증은 Task 3</automated>
  </verify>
  <acceptance_criteria>
    - belle 이 "deployed" 를 확인하고 스택 업데이트 성공 로그가 존재
  </acceptance_criteria>
  <done>sunity-motion-pilot 스택에 신규 리소스가 반영됨.</done>
</task>

<task type="auto">
  <name>Task 3: 배포 후 실검증 + OTA + HUMAN-UAT 적립 + 마감 기록</name>
  <files>.planning/phases/31-api-visual-correction/31-HUMAN-UAT.md, plan.md</files>
  <read_first>.planning/phases/30-growth-tracking/ 의 최신 SUMMARY (phase 30 OTA 발행 커맨드 선례 — eas update 채널/옵션 그대로 재사용), .planning/phases/28-dtw-alignment/ 또는 최근 phase 의 HUMAN-UAT.md 1개 (적립 형식 선례)</read_first>
  <action>(1) 실검증: 무토큰 `curl -X POST {ApiBaseUrl}/visual/rotation` → 401 확인. (2) 워커 실호출 1건 (비용 ~$0.8, 6~7분 — D-06 배선 실증): 기존 완료 분석 문서 1개(fixture 계정)를 골라 `aws lambda invoke` 로 VisualWorkerFunction 에 SQS 이벤트 형상({uid, analysisId} Records 래핑) 직접 투입 → 완료 후 Firestore 문서의 rotationStatus 가 'done'(S3 rotation.mp4 존재 + rotationVideoUrl 자체 presign) 또는 'failed'(모더레이션 재시도 1회 후) 인지 확인 — 둘 다 배선 실증으로 인정 (spike 실측 차단율 ~10%). AWS_PROFILE=sunity-motion. (3) OTA 발행: phase 30 SUMMARY 의 eas update 커맨드 선례 그대로 실행 — 참고코너는 backend 필드 부재 시 자동 숨김이라 앱 선발행 안전. (4) 31-HUMAN-UAT.md 작성 (선례 형식): D-09 참고코너 배치·"참고하세요" 분리 확인, D-10 반투명 중첩 + 드래그 회전 제스처, mode3 강등 화면, 회전 영상 카드 재생, 실루엣 카드 (Pod 재생성 후 신규 분석 필요 명시) — 즉시 belle 호출 금지, 배치 UAT. (5) plan.md 에 phase 31 완료 상태 갱신. (6) SUMMARY 박제 항목: Pod 재생성 시 env 체크리스트에 `DASHSCOPE_API_KEY` 추가 필요 (실루엣 사후 훅이 Pod 에서 실행됨 — [[current-pod-hbpvhedq2bu01i]] 셋업 절차에 추가), 실루엣 실 E2E 는 Pod 재생성 후 이월.</action>
  <verify>
    <automated>test -f .planning/phases/31-api-visual-correction/31-HUMAN-UAT.md && grep -q "D-10" .planning/phases/31-api-visual-correction/31-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - 무토큰 POST /visual/rotation 이 HTTP 401 반환 (curl 출력 기록)
    - 워커 실호출 후 대상 문서 rotationStatus 가 'done' 또는 'failed' (pending 고아 아님)
    - 'done' 인 경우 rotationVideoUrl 에 `dashscope` 문자열 부재 (자체 presign)
    - OTA 발행 로그 존재 (update id 기록)
    - 31-HUMAN-UAT.md 에 D-09/D-10/회전 카드/실루엣(Pod 이월) 항목 존재
    - plan.md 에 phase 31 상태 갱신
  </acceptance_criteria>
  <done>라이브 배포 + 실배선 증명 + 검증 잔여의 명시적 적립까지 완료.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 배포 파이프라인 | IaC 변경이 실 스택에 반영되는 지점 — belle 승인 게이트 |
| 실검증 호출 | 실 과금·실 데이터 접촉 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-30 | Elevation of Privilege | 무단 배포 | mitigate | sam deploy 는 checkpoint:human-action — belle 수동 실행만 (auto-mode 차단 유지) |
| T-31-31 | Spoofing | 배포된 엔드포인트 | mitigate | 배포 직후 무토큰 401 실검증을 acceptance 로 고정 |
| T-31-32 | Denial of Service (과금) | 실검증 호출 | mitigate | 워커 실호출 1건 상한 (~$0.8) — fixture 문서 1개만 |
</threat_model>

<verification>
- 스택 업데이트 성공 + 401 실검증 + 워커 상태 전이 확인
- OTA 발행 + HUMAN-UAT 적립
</verification>

<success_criteria>
- D-01 풀통합이 라이브 (실루엣 E2E 만 Pod 이월 명시)
- 검증 잔여가 전부 명시적 적립 (침묵 이월 0)
</success_criteria>

<output>
Create `.planning/phases/31-api-visual-correction/31-09-SUMMARY.md` when done
</output>
