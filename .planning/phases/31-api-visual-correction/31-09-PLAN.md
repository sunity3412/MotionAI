---
phase: 31-api-visual-correction
plan: 09
type: execute
wave: 3
depends_on: [31-05, 31-06, 31-07]
files_modified:
  - backend/functions/visual-worker/app.py
  - backend/functions/visual-worker/requirements.txt
  - backend/scripts/list_stuck_visual_jobs.py
  - backend/tests/phase31/test_visual_worker.py
autonomous: true
requirements: [D-03, D-04, D-05, D-06, D-08]
must_haves:
  truths:
    - "워커는 Lambda 를 장시간 폴링으로 붙잡지 않는다 — 1회 폴링 후 delayed SQS 메시지로 다음 폴링을 스케줄한다 (리뷰 B-03 state-machine poller)"
    - "정상 return 이 job 을 고아로 만들 수 없다 — 모든 정상 return 은 종결 상태이거나 지연 continuation 메시지를 이미 발행한 상태 (리뷰 B-03)"
    - "예상치 못한 예외는 partial batch failure(batchItemFailures)로 보고되어 SQS 가 재전달한다 (리뷰 B-03)"
    - "모든 전이는 job 문서 CAS 로 멱등 — stale/중복 메시지는 no-op, 재과금 0 (리뷰 B-03/H-06)"
    - "correctedPose 잡: judge(before/after) + 결정론 pose 게이트 통과분만 done, 입력 임시 객체는 finally 삭제 (리뷰 H-03/H-04)"
    - "종결 시 judge·poseGate 판정 메타가 job 문서에 flat scalar 로 남는다 — 31-12 E2E-A 가 게이트 통과 근거를 job 문서에서 검증 가능"
    - "벤더 URL 은 Firestore 에 저장되지 않는다 — S3 canonical key 만 (리뷰 H-02)"
    - "실패는 typed failureReason 으로 기록된다 — generic 실패와 모더레이션이 구분 가능 (리뷰 H-07 전제)"
  artifacts:
    - path: "backend/functions/visual-worker/app.py"
      provides: "kind 분기 state machine 워커 (run/poll 액션)"
      contains: "batchItemFailures"
    - path: "backend/scripts/list_stuck_visual_jobs.py"
      provides: "운영자용 stuck job 조회 (리뷰 L-04)"
    - path: "backend/tests/phase31/test_visual_worker.py"
      provides: "continuation·멱등·typed 실패·정리 테스트"
      min_lines: 150
  key_links:
    - from: "backend/functions/visual-worker/app.py"
      to: "backend/shared/python/sunity_shared/analysis/visual_gen.py"
      via: "typed 어댑터 + download_vendor_asset"
      pattern: "visual_gen"
    - from: "backend/functions/visual-worker/app.py"
      to: "backend/shared/python/sunity_shared/firestore_admin.py"
      via: "transition_visual_job CAS + update_analysis_visual"
      pattern: "transition_visual_job"
---

<objective>
리뷰 BLOCKER B-03 을 해소하는 워커를 구현한다. 기각된 설계(~780s 폴링 후 정상 return → SQS 재전달 기대 — 성공 return 은 메시지를 삭제하므로 영구 pending 고아)를 버리고 state-machine poller 로 전환한다:

메시지 = {"jobId": str, "action": "run"|"poll"}. run = 벤더 task 생성(또는 sync 이미지 호출) 후 즉시 1회 폴링, pending 이면 DelaySeconds=60 의 poll 메시지를 자기 큐에 발행하고 정상 return. poll = 1회 폴링 + 동일 규칙. 폴링 횟수 상한 MAX_POLLS=20 (attempt 필드) 초과 → typed 'timeout' 실패. 예상 외 예외 = batchItemFailures 보고 (ReportBatchItemFailures — IaC 는 31-10). Lambda Timeout 은 300s 면 충분 (장시간 대기 없음).

kind 분기: correctedPose (이미지 생성 → download_vendor_asset → judge_corrected_pose + pose_gate → canonical key put → 상태 done → visual-input 삭제 → training 게이트 통과+동의 시 pair_store), rotation (원본 영상 presign 1h → wan2.7-videoedit task → 폴링 → 다운로드 → rotation.mp4 put → done).

Purpose: 과금 잡이 고아가 될 수 없는 비동기 실행기 — 실루엣·회전이 같은 substrate 를 공유 (리뷰 H-01).
Output: visual-worker Lambda + 운영자 조회 스크립트 + 테스트.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-api-visual-correction/31-PATTERNS.md
@.planning/phases/31-api-visual-correction/31-ACCEPTANCE.md
@.planning/phases/31-api-visual-correction/31-REVIEWS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 워커 골격 — state machine + continuation + partial batch failure</name>
  <files>backend/functions/visual-worker/app.py, backend/functions/visual-worker/requirements.txt, backend/tests/phase31/test_visual_worker.py</files>
  <read_first>backend/functions/pipeline/app.py lines 4816-4867 (SQS 레코드 루프 관례 — 단 재전달 정책은 본 플랜이 교체) + lines 1258-1263 (_signed_get), backend/functions/pipeline/requirements.txt + backend/functions/playback-url/requirements.txt (pyyaml deploy fix 주석 2건 원문 — 신규 requirements.txt 주석에 인용), backend/shared/python/sunity_shared/firestore_admin.py (reserve_visual_job/transition_visual_job/update_analysis_visual/mark_visual_job_dispatch_failed 시그니처 — 31-02), backend/shared/python/sunity_shared/models.py (VISUAL_JOB_STATES/VISUAL_FAILURE_REASONS), .planning/phases/31-api-visual-correction/31-REVIEWS.md 의 B-03 절 (해소 요건 원문)</read_first>
  <action>visual-worker/app.py 신설 (모듈 docstring: 리뷰 B-03 해소 설계 + "정상 return = 종결 or continuation 발행 완료" 불변식 명시). lambda_handler: SQS Records 루프 — 각 레코드 try: 메시지 파싱({jobId, action} — 파싱 실패는 log 후 skip: 위조/오염 메시지는 재전달해도 무의미), `_handle_job(job_id, action)` 호출; except Exception → log.exception + `batchItemFailures` 리스트에 messageId 추가. 반환 = {"batchItemFailures": [...]} (partial batch failure — 리뷰 B-03; IaC 의 FunctionResponseTypes 는 31-10). _handle_job: job 문서 로드 (부재 → log 후 종료 — 재전달 무의미), kind 별 핸들러 위임 전 공통 규칙: action 'run' 은 expect_states ('reserved','dispatch_failed') CAS 로 'polling' 전이 (실패 = stale/중복 → no-op 종료, 재과금 0 — 리뷰 B-03 멱등); taskId 가 이미 있으면 벤더 create 호출 생략하고 폴링 재개 (journal 멱등). 폴링 공통 함수 `_poll_and_continue(job, adapter)`: poll 1회 → 'succeeded'/'failed'/'blocked' 는 종결 처리 위임, 'pending' 은 attempt+1 CAS 후 attempt >= MAX_POLLS(20) 면 typed 'timeout' 실패 종결, 아니면 `sqs.send_message(QueueUrl=env VISUAL_QUEUE_URL, MessageBody={"jobId":..., "action":"poll"}, DelaySeconds=60)` 발행 후 정상 return — **continuation 발행 실패는 예외 전파(batchItemFailures 경유 재전달)로 고아 방지**. 종결 공통: 실패 = transition_visual_job(state 'failed', failureReason typed) + update_analysis_visual(status 'failed') (D-08 — fail_analysis 절대 호출 금지, 분석은 이미 done); 'blocked'(모더레이션) 는 attempt 별개 카운터 retryCount 로 visual_gen.MODERATION_RETRY_MAX(1) 내 재생성 1회, 초과 시 'moderation' 실패. **requirements.txt = `firebase-admin>=6,<7` + `pyyaml>=6.0,<7.0` — pyyaml 주석에 두 선례 deploy fix 인용 필수: "PyYAML — sunity_shared.firestore_admin 의 transitive import (analysis/force_signals.py module-level yaml import). 2026-06-12 deploy fix (pipeline/upload-url/reference-api 500 ImportError) + 2026-07-17 deploy fix (playback-url 29-06 배포 직후 500 Runtime.ImportModuleError) 동일 패턴 — 누락 시 배포 직격". google-genai 는 절대 추가 금지 (pipeline/requirements.txt 2026-06-12 박제: transitive ~100MB → 250MB 한도 초과 실측 262MB; judge 는 visual_gen 의 stdlib urllib REST — 31-05)**. 로깅 key=value(job_id/kind/task_id), 키·서명 URL 미로깅. test_visual_worker.py: pending → DelaySeconds 60 continuation 발행 + 정상 return (mock sqs 검증); MAX_POLLS 도달 → 'timeout' failed (pending 고아 불가); continuation send 예외 → batchItemFailures 포함; stale 메시지(state 'done') → 벤더 호출 0 + no-op; taskId 존재 시 create 미호출; 모더레이션 → 재시도 1회 후 'moderation'.</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31/test_visual_worker.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - lambda_handler 가 batchItemFailures 를 반환 (grep + 테스트)
    - "pending 정상 return 전에 continuation 발행" 불변식이 테스트로 고정 (발행 실패 = 재전달)
    - fail_analysis 호출 0 (모듈 소스 검사로 고정)
    - MAX_POLLS 초과 → failureReason 'timeout' 테스트 green
    - requirements.txt 에 firebase-admin + pyyaml 두 줄 존재, pyyaml 주석에 2026-06-12/2026-07-17 deploy fix 인용, google-genai 부재
  </acceptance_criteria>
  <done>고아 불가능 state machine 골격 완성.</done>
</task>

<task type="auto">
  <name>Task 2: correctedPose 잡 핸들러 (judge + pose 게이트 + 정리 + 페어 + 판정 메타 기록)</name>
  <files>backend/functions/visual-worker/app.py, backend/tests/phase31/test_visual_worker.py</files>
  <read_first>backend/shared/python/sunity_shared/analysis/visual_gen.py (WanImageAdapter/judge_corrected_pose/download_vendor_asset — 31-05 시그니처; judge 는 env GEMINI_API_KEY 소비), backend/shared/python/sunity_shared/analysis/pose_gate.py (measure_generated_pose/derive_pose_url — 31-06), backend/shared/python/sunity_shared/analysis/pair_store.py (store_training_pair — 31-07), .planning/phases/31-api-visual-correction/31-ACCEPTANCE.md (display 15° / training 8° 허용오차)</read_first>
  <action>`_handle_corrected_pose(job)` — 흐름: (1) 입력 key 검증 = job 문서의 srcKey/joint/targetDeg 읽기 (**31-10 T3 이 reserve_visual_job 의 payload 파라미터로 기록한 flat scalar — 31-02 계약**; 셋 중 하나라도 부재 → 'invalid_output' 실패) 후 srcKey 가 정확히 f"visual-input/{uid}/{analysis_id}/src.png" 인지 equality 검증 — 불일치 즉시 'invalid_output' 실패 (리뷰 H-05 key 경계), (2) S3 get 으로 before_png 바이트 확보 + presign 1h (전용 TTL — _PLAYBACK_EXPIRES 7일 미사용), (3) WanImageAdapter.generate(presigned, prompt) — prompt 는 job 의 joint/targetDeg 로 구성 (31-01 스모크 프롬프트 형식: 교정 지시 + identity/clothing/pole/background unchanged), typed 실패는 그대로 종결, (4) download_vendor_asset(output_url, max_bytes=20MB, allowed_content_types=('image/png','image/jpeg')) → after_png, (5) judge_corrected_pose(before_png, after_png, context) — None → 'judge_failed', display_pass False → 'judge_failed', (6) pose_gate.measure_generated_pose(after_png, joint_key, target_deg, tolerance=display 15°, pose_url=derive_pose_url(env RUNPOD_ANALYZE_URL), token=env RUNPOD_AUTH_TOKEN) — passed False → reason 그대로 typed 실패 ('pose_gate_failed'/'pose_gate_unavailable'), (7) 전부 통과: S3 put `results/{uid}/{analysis_id}/corrected_pose_{joint}.png` (ContentType image/png) → transition 'done' + update_analysis_visual(kind 'correctedPose', status 'done', key=skey, joint=joint), (8) **페어 적재 (성공시에만)**: 분석 문서 learningOptIn 읽기 + training 게이트(judge training_pass AND pose error <= 8°) 통과 시 pair_store.store_training_pair — 적재 실패는 status 에 영향 0, (9) **finally: visual-input src.png delete_object 시도** (성공/실패 모든 경로 — 리뷰 H-04; 삭제 실패는 log.warning, lifecycle 1일이 이중 방어). **판정 메타 기록 (31-12 E2E-A 검증 대상 — checker WARNING 해소)**: 종결 transition_visual_job 의 updates 에 도달한 게이트의 판정 요약을 flat scalar 로 포함 — judge 도달 시 `judgeDisplayPass: bool` / `judgeTrainingPass: bool` / `judgeConfidence: float`, pose 게이트 도달 시 `poseGatePassed: bool` / `poseGateErrorDeg: float | None` / `poseGateReason: str | None`. 성공(done)·게이트 실패(failed) 모든 종결 경로에서 도달한 단계까지의 메타를 기록 (미도달 단계는 미기록 — 부재 = 미도달). nested dict 금지 (Firestore flat 규율 — _validate_dict_only_scalars 통과 형태). 테스트: PASS 전체 경로 → done + canonical key 저장 + **job 문서에 judgeDisplayPass/judgeConfidence/poseGatePassed/poseGateErrorDeg 기록됨 (fake doc 검증)** + Firestore 기록값에 'dashscope'/'aliyuncs' 문자열 부재 + src 삭제 호출; judge display fail → 'judge_failed' + judgeDisplayPass False 기록 + poseGate 메타 부재(미도달) + 페어 미적재 + src 삭제; pose_gate_unavailable → typed 실패 + poseGateReason 기록 + 미노출; training 게이트 미달(display 는 통과) → done 이지만 페어 0 + judgeTrainingPass False 기록; learningOptIn False → done + 페어 0; cross-key(타 uid srcKey) → 'invalid_output' + 벤더 호출 0.</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31/test_visual_worker.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - display(15°)와 training(8°) 게이트가 서로 다른 tolerance 로 호출됨 (테스트 고정)
    - 모든 종결 경로에서 visual-input 삭제 시도가 호출됨 (finally — 테스트 고정)
    - 종결 job 문서에 judge(judgeDisplayPass/judgeTrainingPass/judgeConfidence)·poseGate(poseGatePassed/poseGateErrorDeg/poseGateReason) 메타가 flat scalar 로 기록됨 (테스트 고정 — 31-12 E2E-A 전제)
    - Firestore 기록에 벤더 URL 문자열 부재 테스트 green
    - cross-UID 입력 key → 벤더 호출 0 테스트 green
  </acceptance_criteria>
  <done>D-05 자동 교정 이미지가 이중 게이트·정리·동의 페어·판정 메타까지 테스트로 고정됨.</done>
</task>

<task type="auto">
  <name>Task 3: rotation 잡 핸들러 + 운영자 stuck 조회 스크립트</name>
  <files>backend/functions/visual-worker/app.py, backend/scripts/list_stuck_visual_jobs.py, backend/tests/phase31/test_visual_worker.py</files>
  <read_first>backend/shared/python/sunity_shared/s3keys.py (build_upload_key — canonical upload key 형식의 단일 출처), .planning/spikes/004-gemini-omni-view-editing/wan_gate_batch.py (회전 PROMPT 원문 — 검증된 카메라 orbit 프롬프트 이식)</read_first>
  <action>`_handle_rotation(job)` — 흐름: (1) 분석 문서의 myVideoKey 를 s3keys.build_upload_key(uid, analysis_id, ext) 후보(mp4/mov)와 equality 검증 — 불일치 'invalid_output' (리뷰 H-05: 임의 key 로 타 객체 벤더 전송 차단), (2) presign 1h (트림 없이 원본 — ffmpeg 의존 회피, 구 Open Q4 결정 유지), (3) WanVideoEditAdapter.create_task(video_url, PROMPT — wan_gate_batch 검증 프롬프트 상수 이식) → taskId CAS 저장 (재과금 0 journal), (4) Task 1 의 _poll_and_continue 로 폴링/continuation, (5) succeeded: download_vendor_asset(output_url, max_bytes=200MB, allowed_content_types=('video/mp4',)) → S3 put `results/{uid}/{analysis_id}/rotation.mp4` (ContentType video/mp4) → 'done' + update_analysis_visual(kind 'rotation', status 'done', key=skey). list_stuck_visual_jobs.py (리뷰 L-04 — 앱의 pending 숨김은 표시 폴백일 뿐 서버 잡을 취소하지 않음): argparse --profile 기본 sunity-motion, --stale-minutes 기본 30 — visualJobs 컬렉션에서 state in ('reserved','polling','dispatch_failed') 이고 updatedAtMs 가 stale 기준 초과인 잡을 표로 출력 (jobId/kind/state/attempt/failureReason/경과분). 테스트: rotation 성공 경로 → rotation.mp4 canonical key + 벤더 URL 미저장; myVideoKey 불일치 → 'invalid_output' + create 0회; taskId 재개 → create 미호출.</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31/test_visual_worker.py -x -q && python -m pytest backend/tests -q</automated>
  </verify>
  <acceptance_criteria>
    - myVideoKey canonical equality 검증 존재 + 불일치 테스트 green
    - rotation 'done' 시 rotationVideoKey 만 기록 (URL 필드 자체 없음 — 31-02 시그니처가 강제)
    - list_stuck_visual_jobs.py 존재 + 파싱 가능
    - `python -m pytest backend/tests -q` exits 0
  </acceptance_criteria>
  <done>D-06 회전 잡이 같은 substrate 에서 멱등·경계 검증·정리까지 완결. 운영자 가시성 확보.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQS 메시지 → 워커 | 위조/중복/stale 메시지 방어 (CAS + 문서 재검증) |
| 사용자 객체 → 벤더 | canonical key equality 통과분만 전송 |
| 벤더 응답 → S3 | download_vendor_asset 경계 통과분만 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-32 | Denial of Service | 폴링 상한 고아 | mitigate | continuation 발행 or 종결 불변식 + MAX_POLLS→'timeout' (리뷰 B-03, 테스트 고정) |
| T-31-33 | Tampering | 중복/stale 메시지 재과금 | mitigate | CAS 전이 + taskId journal — create 재호출 0 테스트 고정 |
| T-31-34 | Information Disclosure | 타 사용자 객체 벤더 전송 | mitigate | upload key/입력 key equality 검증 (리뷰 H-05) |
| T-31-35 | Information Disclosure | 원본 프레임 잔존 | mitigate | finally 삭제 + lifecycle 이중 방어 (리뷰 H-04) |
| T-31-36 | Tampering | 저품질 생성물 노출/적재 | mitigate | before/after judge + 결정론 pose 게이트 이중 + training 별도 임계 (리뷰 H-03) — 판정 메타 job 기록으로 E2E 검증 가능 |
| T-31-37 | Repudiation | generic 실패로 뭉개짐 | mitigate | typed failureReason 8종 — H-07 E2E 가 모더레이션과 구현 오류를 구분 가능 |
</threat_model>

<verification>
- `python -m pytest backend/tests -q` green
- 워커 모듈에 requests import·키 리터럴·fail_analysis 호출·google.genai import 0
- requirements.txt 에 pyyaml 존재 (deploy fix 선례 2건 인용 주석)
</verification>

<success_criteria>
- B-03 해소 (고아 불가능 poller) + H-01 (단일 substrate 두 kind) + H-03 이중 게이트 + H-04 정리/페어 전부 테스트 고정
- 종결 job 문서의 judge/poseGate 메타로 31-12 E2E-A 검증 가능
</success_criteria>

<output>
Create `.planning/phases/31-api-visual-correction/31-09-SUMMARY.md` when done
</output>
