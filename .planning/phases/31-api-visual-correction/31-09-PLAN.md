---
phase: 31-api-visual-correction
plan: 09
type: execute
wave: 4
depends_on: [31-02, 31-05, 31-06, 31-07, 31-13]
files_modified:
  - backend/functions/visual-worker/app.py
  - backend/functions/visual-worker/requirements.txt
  - backend/functions/visual-dispatch/app.py
  - backend/functions/visual-dispatch/requirements.txt
  - backend/scripts/list_stuck_visual_jobs.py
  - backend/tests/phase31/test_visual_worker.py
  - backend/tests/phase31/test_visual_dispatcher.py
autonomous: true
requirements: [D-03, D-04, D-05, D-06, D-08]
must_haves:
  truths:
    - "worker 는 state action 1개만 수행하고 durable next action(nextAction+outboxSeq)을 transaction 에 남긴다 — SQS send 는 best-effort, 복구는 dispatcher (3차 B3-01 + 4차 B4-01)"
    - "메시지 = {jobId, generation, action, outboxSeq}. worker 는 외부 side-effect 전에 claim_visual_job_action 으로 claim 하고 반환 dict `{status, job}` 로 분기한다 — claimed 만 실행; stale/completed 는 외부 0 + 정상 ACK; busy(same-seq active lease)는 정상 ACK 금지 → change_message_visibility(남은 lease+jitter) + batchItemFailures 로 재전달 (5차 B5-01 + 6차 M6-03)"
    - "claim status=='claimed' 시 worker 는 claim 이 반환한 job snapshot 만 action·후속 CAS 에 사용한다 — claim 이전 inbound job 을 action 에 재사용하는 정적 코드 경로가 없다(grep). expect_claim_owner=snap.claimOwner, expect_outbox_seq=snap.outboxSeq, now_ms 를 transition/finalize 에 함께 전달 (6차 리뷰 B6-01)"
    - "각 CAS commit 직후 crash (polling/fetching/judging/pose_checking/postprocessing) 에서 dispatcher 가 lease 만료(H6-01 same-seq expired claim 필터) 후 정확한 다음 action 을 1회 이상 발행하고 외부 create 중복 0 으로 terminal 에 도달한다 — '재전달 no-op 멱등' 은 PASS 기준이 아니다 (B3-01 + 5차 B5-01 + 6차 H6-01)"
    - "action 성공 transition/finalize 는 owner+outboxSeq+미만료 lease CAS(expect_claim_owner + now_ms < claimLeaseExpiresAt) 를 거친다 — lease 를 잃고 재claim 당한/만료된 늦은 worker 의 결과 write 는 새 owner 진행을 덮지 못한다 (5차 B5-01 + 6차 H6-07)"
    - "correctedPose 는 성공/실패 모든 terminal intent 가 pose_checking(또는 실패 직전 state)→postprocessing 을 거친다 — 그 진입 transition 이 inputSealed=True 를 기록(producer hard gate — 6차 B6-03)하고, job 에 pendingTerminalState/pendingFailureReason 를 실어 postprocess action 이 임시 입력 삭제(+동의 시 pair store)를 durable 수행한 뒤 한 번 finalize 한다. 실패 경로도 durable cleanup 을 우회하지 않는다 (4차 H4-01 + 5차 H5-05 + 6차 B6-03)"
    - "임시 입력 cleanup 은 비-버저닝 VisualInputBucket 의 exact prefix(visual-input/{uid}/{analysisId}/) 를 list_objects_v2(continuation 끝까지)로 열거·delete_objects 후 재조회 count 로 remainingObject 를 얻는다 — 비-버저닝이라 version 열거/삭제 불필요, delete 1회 완전 소거. delete_objects 응답 per-object Errors(code/key) 기록 + failed count 를 cleanupAttempt 판단에 포함(HTTP 200 만 신뢰 금지 — 6차 M6-05)"
    - "cleanup remainingObject>0 이면 절대 terminal finalize 금지 — postprocessing 유지 + privacyBlocker='cleanup_blocked' + nextAction='postprocess' + 장기 backoff + VisualCleanupBlocked alarm + 운영 스크립트. remainingObject==0 확인 시 finalize_visual_job 에 `cleanup_verified_at_ms=now` 파라미터 전달(job_meta 아님 — 7차 B7-03 순서 모순 해소). validator 는 correctedPose inputSealed True 면 done·failed 공통 cleanup_verified_at_ms>0 요구(7차 B7-04). cleanup_blocked 해결 후 같은 postprocess 가 재확인 0 후에만 원래 pendingTerminalState finalize (6차 B6-04 + 7차 B7-03/B7-04)"
    - "pairEligible=False → pairId/pairHmacKeyVersion None + display(correctedPose) 경로 계속. eligible=True 인데 HMAC key config invalid → pairStoreStatus='failed_config' + pair PUT 0 + 결과는 cleanup 후 done 허용 — display correctedPose 가 HMAC env 오류로 막히지 않는다 (6차 리뷰 H6-05)"
    - "staging/canonical deterministic key 가 존재하고 hash 불일치면 overwrite 금지 — typed invalid_output/integrity conflict 로 차단. 새 generation 명시 선택 시만 generation 포함 새 key (6차 리뷰 H6-03)"
    - "postprocess 는 S3 PUT 직전 analysis 의 현재 learningOptIn is True + consentVersion + capturedAt/revokedAt 를 재read 한다 — 철회면 PUT 0 + pairStoreStatus='skipped_consent' (5차 리뷰 H5-01)"
    - "pairId/pairHmacKeyVersion 은 pose_checking→postprocessing transition 에서 1회 계산해 job 에 고정 — postprocess 재시도가 active key 를 다시 선택하지 않는다 (5차 리뷰 B5-03)"
    - "cleanup 재시도만 postprocessing self-loop(cleanupAttempt + nextDispatchAtMs + 새 outboxSeq)로 durable 하다. **pair 는 부산물이라 사용자 표시·cleanup·finalize 의 critical path 를 막지 않는다 — pair 'failed'(network)는 pairStoreStatus='failed' 즉시 기록 후 cleanup 진행(self-loop 0), 'conflict'는 즉시 quarantine/alarm, 재처리는 별도 운영 (7차 H7-07)** (5차 H5-02 + 6차 M6-04 + 7차 H7-07)"
    - "vendor create 는 async taskId 경로만 정상 처리한다 — create_task 가 sync succeeded 를 반환하면 typed 실패로 종결(v1 async-only, B4-02). rotation 도 async"
    - "moderation retry 는 transition 이 polling→retry_ready 에서 taskId=None·attempt=0·lease/failure clear + retryCount+1 + nextAction='create' 을 원자 기록하고, retry_ready→creating 이 generation+1 로 승격하며 requestKey 를 그 새 generation 으로 transaction 안에서 생성한다 (4차 B4-03 + 5차 H5-03)"
    - "_action_create 의 모든 후속 경로는 첫 transition 이 반환한 갱신 job snapshot 하나만 사용한다 — inbound msg 값을 재사용하지 않아 creating 잔존 0 (5차 리뷰 H5-04)"
    - "job 문서에 outputUrl 을 저장하지 않는다 — taskId 만. fetch invocation 이 taskId 재-poll 로 fresh URL 을 얻어 같은 invocation 에서 즉시 streaming download 한다 (3차 리뷰 H3-01)"
    - "다운로드된 correctedPose 이미지는 safe_decode_image + 실제 PNG 재인코딩(EXIF 제거)으로 정규화된다 — staging key 는 deterministic + HEAD/full-hash reuse. decode 오류는 size/pixel 초과=judge_input_too_large, corrupt/unsupported/mismatch=invalid_output 로 분리 (4차 H4-05/H4-06 + 5차 M5-01)"
    - "dispatcher 는 별도 소형 Lambda(reserved concurrency 1) — dispatchState='pending' 발행 + sent+H6-01 필터 복구 재발행 + 스캔 window age/truncated metric (3차 H3-09 + 4차 H4-08 + 5차 B5-01/H5-06 + 6차 H6-01/M6-01)"
    - "같은 dispatcher 가 privacy janitor 를 부수 수행한다 — reservation/orphan durable cursor bounded. reservation delete 는 `claim_reservation_for_janitor` CAS 성공 후에만 S3 delete(유효 job 입력 보존 B8-06). **9~11차 강화: (B9-01) janitor claim crash-recoverable; (B9-03) expectedKeys∪createdKeys; (H9-02) predicate; (B10-01/B10-02) claim_key_for_delete(자기 ref 소비+live ref 0→deleting CAS); ★11차 (B11-01) `commit_key_delete(token)` 를 외부 delete **직전** 재검증(owner/generation/미만료 lease)해 lease 만료 후 살아난 이전 claimant 의 late delete 차단 + producer 는 deleting 이면 만료여도 acquire 항상 False; (B11-04) claim_key_for_delete 가 job ref 는 항상 live 로 계산해 nonterminal job input 을 orphan janitor 가 삭제 못 함; (B10-03) delete 후 release. delete lease>dispatcher 생존 build gate**. TTL 은 PipelineTimeout+최대보상+margin 보다 커 정상 producer 오판 금지(31-10 build gate H9-01/H10-03). OpenCount/OldestAgeMs/SweepFailed/SweepDeleted metric + oldest-age/failure alarm (8차 B8-06/H8-01/M8-05 + 9차 B9-01/B9-02/B9-03/H9-02)"
    - "worker 는 action=iam_probe 를 외부 side-effect 없이 소비한다 — 31-12 IAM canary send-only 전용 (3차 H3-10 + 4차 H4-02)"
  artifacts:
    - path: "backend/functions/visual-worker/app.py"
      provides: "action 단위 state machine 워커 (create/poll/fetch/judge/pose_check/postprocess + iam_probe) — claim dict 4상태 + snapshot handoff + owner/lease-CAS transaction outbox + all-terminal-via-postprocessing + inputSealed + 비-버저닝 단일 delete cleanup 소비"
      contains: "claim_visual_job_action"
    - path: "backend/functions/visual-dispatch/app.py"
      provides: "dispatcher Lambda — outbox 발행(pending + sent H6-01) + dispatched CAS + **privacy janitor(claim lease B9-01 + claim_key_for_delete+commit_key_delete fencing token B10-02/B11-01 + job ref 무기한 live B11-04 + delete 후 release B10-03, bounded cursor)** + ScannedOutboxMaxAge/OrphanOpen/OldestAge/SweepFailed metric"
      contains: "list_dispatch_pending"
    - path: "backend/scripts/list_stuck_visual_jobs.py"
      provides: "운영자용 stuck 조회 + terminal reconciler + create_unconfirmed 수동 판정 + cleanup_blocked/orphan/expired-reservation 목록·재구동"
      contains: "reconcile"
    - path: "backend/tests/phase31/test_visual_worker.py"
      provides: "claim dict 4상태(busy change_visibility+batchItemFailures)·snapshot handoff·crash-injection(전이 5지점 + create 계열 + postprocess durable)·owner/lease CAS·consent race·all-terminal-postprocessing·비-버저닝 단일 delete cleanup·cleanup_blocked nonterminal·hash conflict·pair failed_config·JPEG→PNG·decode split·typed 실패 테스트"
      min_lines: 300
  key_links:
    - from: "backend/functions/visual-worker/app.py"
      to: "backend/shared/python/sunity_shared/firestore_admin.py"
      via: "claim_visual_job_action(dict 4상태) + transition_visual_job(snapshot+owner/lease CAS) + finalize_visual_job(owner/lease CAS + privacy validator)"
      pattern: "finalize_visual_job"
    - from: "backend/functions/visual-dispatch/app.py"
      to: "backend/shared/python/sunity_shared/firestore_admin.py"
      via: "list_dispatch_pending + mark_visual_job_dispatched"
      pattern: "mark_visual_job_dispatched"
    - from: "backend/functions/visual-worker/app.py"
      to: "backend/shared/python/sunity_shared/analysis/visual_gen.py"
      via: "create_task/poll + download_vendor_asset + safe_decode_image + judge_corrected_pose"
      pattern: "visual_gen"
---

<objective>
6~8차 리뷰 §7 실행 허용 조건을 구현한다: worker/dispatcher 분리 + claim dict 4상태 + snapshot handoff + owner/lease CAS + 모든 terminal intent 의 postprocessing 경유 + inputSealed + 비-버저닝 단일 delete cleanup + cleanup_blocked nonterminal + cleanup_verified_at_ms 파라미터 handoff + done|failed 공통 validator + **8차: begin_visual_job_create status 분기(acquired→즉시 vendor create, B8-01) + janitor claim CAS 선형화(B8-06) + bounded cursor/metric(H8-01/M8-05) + pair 단일 시도(cleanup 만 durable, H8-02)** + consent 재확인.

- **worker (VisualWorkerFunction):** 메시지 {jobId, generation, action, outboxSeq} 수신 → **외부 side-effect 전에 `claim_visual_job_action` 으로 claim → dict `{status, job}` 분기 — claimed 만 실행(claim 반환 snapshot 만 사용, pre-claim job 재사용 금지 — B6-01); stale/completed → 외부 0 + 정상 ACK; busy → change_message_visibility(남은 lease+jitter) + batchItemFailures(재전달 — B5-01/M6-03)** → 자기 외부 side-effect 1개 → `transition_visual_job(expect_outbox_seq=snap.outboxSeq, expect_claim_owner=snap.claimOwner, now_ms=now)` 가 다음 state + nextAction + dispatchState='pending' + 새 outboxSeq 를 **한 transaction** 기록하고 **갱신 snapshot 반환** → best-effort `sqs.send_message(...)` + `mark_visual_job_dispatched(...)`. send/mark 실패는 삼키고 log — durable outbox 를 dispatcher 가 복구.
- **dispatcher (VisualDispatchFunction — H3-09 + B5-01 + 6차 H6-01):** 별도 Lambda(reserved concurrency 1, EventBridge rate(1분)). `list_dispatch_pending` 으로 pending nonterminal + **sent+H6-01 필터(same-seq expired claim)** 복구 대상을 조회해 정확한 nextAction 발행 + `mark_visual_job_dispatched` + ScannedOutboxMaxAge/truncated metric.
- **종결 — correctedPose 는 성공/실패 전부 postprocessing 경유 (H4-01 + H5-05 + 6차 B6-03/B6-04):** 각 실패는 finalize 를 직접 하지 않고 job 에 `pendingTerminalState`/`pendingFailureReason` 을 실은 뒤 (현재 state)→postprocessing 전이(**진입 transition 이 inputSealed=True 기록**). postprocess action 이 비-버저닝 단일 delete cleanup(+동의+eligible 시 pair store)을 durable 수행 후, **remainingObject==0(+cleanupVerifiedAtMs) 확인 후에만** 한 번의 finalize. remainingObject>0 이면 privacyBlocker='cleanup_blocked'(비-terminal) 로 유지. rotation 은 fetching→finalize(임시 입력·pair 없음).
- **URL 비저장 (H3-01):** poll succeeded 시 outputUrl 미기록. fetch 가 taskId 재-poll 로 fresh URL 획득 후 같은 invocation streaming download.
- **async-only (B4-02):** create_task 가 sync succeeded 를 반환하면 typed 실패로 종결.
- **moderation retry (B4-03 + H5-03):** transition 이 polling→retry_ready clear + retry_ready→creating generation+1 승격 + requestKey 새 generation 생성.

judge 입력은 31-05 `judge_corrected_pose`(내부 prepare_judge_payload safe_decode+16MB — M4-02), pose 게이트 입력은 31-06 정규화(H3-04). display/training 임계값·tolerance 는 31-13 CALIBRATION 채택값을 env 로 소비 (DISPLAY_JUDGE_CONFIDENCE / TRAINING_JUDGE_CONFIDENCE / DISPLAY_POSE_TOL_DEG / TRAINING_POSE_TOL_DEG — B4-05).

Purpose: 유료 잡이 고아도 중복 과금도 영구 불일치도 pair/cleanup 유실도 임시 생체 프레임 잔존도 될 수 없는 비동기 실행기.
Output: visual-worker Lambda + visual-dispatch Lambda + 운영자 스크립트(reconciler + cleanup_blocked) + crash/claim/consent/cleanup 테스트.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-api-visual-correction/31-PATTERNS.md
@.planning/phases/31-api-visual-correction/31-ACCEPTANCE.md
@.planning/phases/31-api-visual-correction/31-PLAN-REVIEW-ITERATION11.md
@.planning/phases/31-api-visual-correction/31-PLAN-REVIEW-ITERATION10.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 워커 골격 — action state machine + claim dict 4상태(snapshot handoff) + owner/lease-CAS outbox + creating lease + busy change_visibility+batchItemFailures</name>
  <files>backend/functions/visual-worker/app.py, backend/functions/visual-worker/requirements.txt, backend/tests/phase31/test_visual_worker.py</files>
  <read_first>backend/functions/pipeline/app.py lines 4816-4867 (SQS 레코드 루프) + lines 1258-1263 (_signed_get), backend/functions/pipeline/requirements.txt + backend/functions/playback-url/requirements.txt (pyyaml deploy fix 주석 2건 원문), backend/shared/python/sunity_shared/firestore_admin.py (31-02 함수 7종 — claim_visual_job_action(dict 4상태) / transition(snapshot+expect_outbox_seq+expect_claim_owner+now_ms) / finalize / mark(action+seq)), backend/shared/python/sunity_shared/models.py (VISUAL_JOB_STATES 10종 + VISUAL_CLAIM_LEASE_MS + VISUAL_PRIVACY_BLOCKERS + inputSealed/privacyBlocker/cleanupVerifiedAtMs), .planning/phases/31-api-visual-correction/31-PLAN-REVIEW-ITERATION6.md 의 B6-01/B6-03/B6-04/H6-05/M6-03 절</read_first>
  <action>visual-worker/app.py 신설 (docstring: 6차 B6-01/B6-03/B6-04/H6-05 + 5차 B5-01/H5-04/B4-02/B4-03/H4-01/H5-05 불변식 — "외부 side-effect 당 invocation 1개, claim dict 통과분(claimed)의 반환 snapshot 만 사용, 모든 전이 = transaction 원자 outbox+snapshot 반환, owner/lease CAS, send best-effort, correctedPose terminal = 전부 postprocessing 경유(inputSealed 기록)·비-버저닝 단일 delete cleanup·remainingObject 0 후 finalize, 복구 = dispatcher"). lambda_handler: SQS Records 루프 — 각 레코드 try: 파싱 {jobId, generation, action, outboxSeq} — 파싱/스키마 위반 log.error + batchItemFailures (M2-02); **action=='iam_probe' → log.info 후 정상 소비(외부 0 — H4-02)**; `_handle_job(record)` — **반환값이 'busy' 면 sqs.change_message_visibility(record receipt, VisibilityTimeout=남은 lease 초 + jitter) 후 batchItemFailures 에 추가 (재전달 near lease-expiry — M6-03; duplicate busy 가 maxReceiveCount 로 DLQ 오염 최소화)**; except → log.exception + batchItemFailures. 반환 {"batchItemFailures":[...]}.
**claim 규율 (B5-01 dict 4상태 + B6-01 snapshot handoff):** 외부 side-effect 가 있는 action(poll/fetch/judge/pose_check/postprocess)은 side-effect **전** `res = claim_visual_job_action(job_id, generation=msg.generation, action=msg.action, outbox_seq=msg.outboxSeq, owner=aws_request_id, lease_ms=models.VISUAL_CLAIM_LEASE_MS, now_ms=now)` 호출 — `res['status']`: 'claimed' → **`job = res['job']`(claim 반환 snapshot)로만 실행 — inbound record 의 사전 job 재구성 금지(정적 grep)**; 'stale'/'completed' → 외부 0 + 정상 소비(재발행 불필요); **'busy' → 외부 0 + `_handle_job` 이 'busy' + res['job'].claimLeaseExpiresAt 반환 → handler 가 change_visibility+batchItemFailures**. create 는 claim 미사용(creating lease 특례).
공통 헬퍼 `_advance(job, next_state, updates, next_action, delay_s=0) -> dict|None`: `snap = transition_visual_job(원자 outbox, expect_generation=job['generation'], expect_outbox_seq=job['outboxSeq'], expect_claim_owner=job.get('claimOwner'), now_ms=now)` → **snap(갱신 job)이 있고 `next_action is not None` 일 때만 best-effort `sqs.send_message({jobId, generation: snap['generation'], action: next_action, outboxSeq: snap['outboxSeq']}, DelaySeconds=delay_s)` + mark_visual_job_dispatched(expect_action=next_action, expect_outbox_seq=snap['outboxSeq']) — send/mark 실패 삼키고 log.warning. ⚠ H7-01: next_action==None(creating internal transition)은 SQS send/mark 를 절대 호출하지 않는다(action null 메시지=schema fail→DLQ 오염 방지) — snap.dispatchState/nextAction 둘 다 None assert. internal transition 은 같은 invocation 이 이어서 create_task 를 실행하므로 재발행 불필요**; snap None(CAS 실패=lease 상실/만료/구세대) → log.warning + 실행 종료(재발행 불필요). **snap 반환 → 반환 (H5-04: 후속 2전이는 이 snap 을 job 으로 사용)**. **⚠ invocation 내 2전이 (checker): `_action_create` 처럼 (reserved/retry_ready→creating) 후 (creating→polling) 2전이는 반드시 첫 `_advance` 가 반환한 snap 을 두 번째 `_advance`/finalize 의 `job` 으로 사용 — msg.* / claim snapshot 재사용 금지(H5-04). 첫 claim snapshot 은 claimed action 1회만.**
action 핸들러:
- `_action_create(job)`: **8차 B8-01 — create 는 claim 미사용, `begin_visual_job_create`(31-02 9번째 함수) 로 typed status 를 받아 분기한다: `res = begin_visual_job_create(job_id, expect_generation=msg.generation, expect_outbox_seq=msg.outboxSeq, expect_next_action='create', now_ms=now, owner=aws_request_id, lease_ms=VISUAL_CLAIM_LEASE_MS)`; `snap = res['job']`**. **`res['status']` 분기 — 'stale' → 외부 0 정상 ACK(old seq/future-due/재claim); 'busy' → 외부 0 정상 ACK(다른 유효 owner 진행중); 'unconfirmed'(creating+lease 만료+taskId None) → **_finalize_create_failure('create_unconfirmed')**(kind 분기 — 자동 재생성 금지 B2-02); 'resume'(creating+taskId) → polling 재개(_advance(snap, polling, {taskId}, 'poll') or 이미 polling 이면 no-op); 'acquired' → ★즉시 kind 어댑터 create_task(requestKey=snap['requestKey']) 1회 → VendorTaskCreated → _advance(snap, polling, {taskId}, 'poll', delay_s=60)**. **`_advance(creating)` 2차 호출 없음(B8-01).** **VendorPollResult succeeded (sync) → async-only → **_finalize_create_failure('vendor_error')**(9차 H9-09 kind 분기 — rotation 은 direct finalize, correctedPose 는 postprocessing) + log("sync 미허용 B4-02")**; typed 실패 → _finalize_create_failure(snap 기준, kind 분기).
- `_action_poll(job)`: claim → expect polling — 1회 poll: succeeded → _advance(fetching, {}, 'fetch') (URL 미기록 — H3-01); pending → attempt+1 >= MAX_POLLS(20) 면 _finalize_correctedpose_intent('timeout'), 아니면 transition polling→polling {attempt+1} + next_action='poll' + next_dispatch_at_ms now+60s + best-effort send; failed → _finalize_correctedpose_intent('vendor_error'); blocked → retryCount < MODERATION_RETRY_MAX 면 **transition polling→retry_ready (31-02 가 clear + generation+1 준비 — B4-03)** + best-effort send, 아니면 _finalize_correctedpose_intent('moderation').
- **`_finalize_create_failure(job, reason)` (9차 H9-09 — kind 분기: correctedPose 와 rotation 을 명확히 나눔): `if job.kind=='correctedPose'` → `_finalize_correctedpose_intent(job, reason)` (transition (현재 state)→postprocessing {pendingTerminalState:'failed', pendingFailureReason:reason, inputSealed:True, 실패 메타} + next_action='postprocess' → postprocess 단일 delete cleanup 후 finalize — H5-05/B6-03); `else`(rotation) → **finalize_visual_job 직접**(임시 입력·pair 없어 postprocessing/cleanup 미경유 — correctedPose 계약으로 잘못 흐르지 않게 공통 helper 가 kind 분기). create/unconfirmed/sync/typed failure 는 모두 이 helper 경유.**
- 종결 공통: **transition 으로 done/failed 금지 (31-02 validator ValueError). fail_analysis 절대 금지 (D-08) + update_analysis_visual 직접 호출 금지(reconciler 전용 — grep).**
requirements.txt = `firebase-admin>=6,<7` + `pyyaml>=6.0,<7.0` (주석에 두 선례 deploy fix 인용 원문). google-genai 금지. 로깅 key=value, 키·서명 URL 미로깅.
test_visual_worker.py — **claim dict 4상태 (B5-01 + B6-01): 같은 {action,outboxSeq} 메시지 2개 → 1개 'claimed'(외부 1회)·나머지 'busy' → change_visibility 호출 + batchItemFailures(정상 ACK 아님) — 외부 호출 총 1회; claimed 시 worker 가 res['job'] snapshot 만 사용(pre-claim record job 미참조 정적 grep — B6-01); lease 만료 후 재전달 → 'claimed' 재실행(정확히 1 worker); job outboxSeq 초과 → 'completed' 정상 ACK; 구식 seq → 'stale' 정상 ACK no-op**; **owner/lease CAS: 재claim 당한/만료된 늦은 worker 의 _advance snap None → 결과 write 0 (B5-01/H6-07)**; **B5-01 crash: create→polling / poll→fetching CAS 직후·send 전 crash → (a) nextAction/dispatchState='pending'+새 outboxSeq 잔존 + claim 필드 clear, (b) dispatcher 가 lease 만료(H6-01) 후 발행 시 정확한 다음 action 1회+, (c) terminal 도달, (d) 외부 create 1회**; **H5-04: create 계열 재시도에서 vendor typed failure / sync result / taskId 성공 각각 첫 transition snapshot 사용 → creating 잔존 0**; create 직전 crash → lease 만료 후 'create_unconfirmed'(postprocessing 경유 finalize) + create 재호출 0; taskId write 직후 crash → polling 재개 + create 0; **generation 불일치 → no-op + 외부 0; sync succeeded (B4-02) → 'vendor_error'(postprocessing 경유) + polling 진입 0**; **H7-01 creating internal transition: reserved/retry_ready→creating(_advance next_action=None) 은 SQS send/mark 0 assert(action null 메시지 0)**; **8차 B8-01/M8-04 begin_visual_job_create status 분기: 최초 reserved acquired → 즉시 vendor create 1회(creating no-op 아님); concurrent same-seq 2건 → acquired 1·busy/stale 1·vendor create total 1; creating+만료+taskId None → unconfirmed(create 0); creating+taskId → resume(create 0); old seq/future-due → stale(create 0)**; moderation blocked → retry_ready → 재 create 로 재생성 1회(generation+1, requestKey suffix==새 gen — H5-03) + 2회째 blocked → 'moderation'; malformed → batchItemFailures; iam_probe → 외부 0 + batchItemFailures 미포함; send 실패 주입 → 예외 미전파 + outbox pending 잔존; **직렬화 (H3-01+M3-04): 전 시나리오 종료 후 job/analysis 문서 json.dumps 에 'http://'/'https://'/'outputUrl'/'signedUrl' 부재 + SQS body 동일 부재 + caplog log record 부재 assert**.</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31/test_visual_worker.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - 외부 side-effect action 이 claim_visual_job_action 선행 — claimed 만 실행(res['job'] snapshot only, pre-claim job 재사용 grep 0 — B6-01), busy → change_visibility+batchItemFailures(M6-03), stale/completed 정상 ACK
    - _advance 가 transition snapshot 사용 + owner/lease CAS(expect_claim_owner + now_ms) + 2전이 시 snap 재사용(H5-04) 테스트 green
    - 모든 전이가 transition_visual_job 경유 — done/failed transition 0 (finalize 만), correctedPose 실패도 postprocessing 경유(inputSealed 기록 — H5-05/B6-03)
    - crash-injection: CAS 직후 crash → dispatcher lease 만료(H6-01) 후 다음 action 발행 + terminal + create 중복 0 (B5-01)
    - sync succeeded → 'vendor_error' 종결 테스트 green (B4-02)
    - moderation retry → generation+1 + requestKey 새 gen 테스트 green (B4-03/H5-03)
    - 직렬화 http/https/outputUrl/signedUrl 부재 3면 assert green (H3-01/M3-04)
    - requirements.txt pyyaml 주석 2건 + google-genai 부재
  </acceptance_criteria>
  <done>고아·중복 과금·구세대/구-outbox·lease 상실 write·moderation stale taskId 가 불가능한 claim-dict-4상태 action state machine 골격 완성.</done>
</task>

<task type="auto">
  <name>Task 2: correctedPose fetch/judge/pose_check + postprocessing — JPEG→PNG(staging hash-conflict) + display/training env + consent 재확인 + inputSealed + 비-버저닝 단일 delete cleanup(cleanup_blocked nonterminal) + pair failed_config 후 finalize</name>
  <files>backend/functions/visual-worker/app.py, backend/tests/phase31/test_visual_worker.py</files>
  <read_first>backend/shared/python/sunity_shared/analysis/visual_gen.py (WanImageAdapter.create_task/poll, judge_corrected_pose, JudgeInputTooLargeError, safe_decode_image, download_vendor_asset — 31-05), backend/shared/python/sunity_shared/analysis/pose_gate.py (measure_generated_pose/derive_pose_url — 31-06), backend/shared/python/sunity_shared/analysis/pair_store.py (store_training_pair caller-fixed pairId + validate_hmac_key_set + BLUR_OPTION — 31-07), .planning/phases/31-api-visual-correction/smoke/CALIBRATION.json (31-13 채택 임계값), .planning/phases/31-api-visual-correction/31-PLAN-REVIEW-ITERATION6.md 의 B6-03/B6-04/H6-03/H6-05/M6-04/M6-05 절 + ITERATION5 의 B5-03/H5-01/H5-02/M5-01/M5-03 절</read_first>
  <action>모듈 상단 1회 `from PIL import Image; Image.MAX_IMAGE_PIXELS = <상한>` 고정 (M5-03). VISUAL_INPUT_BUCKET = env(비-버저닝). correctedPose 핸들러 (fetch/judge/pose_check/postprocess — 전부 claim 선행, claim 반환 snapshot 만 사용):
(1) `_action_fetch_corrected(job)`: expect fetching — **taskId 재-poll (H3-01): adapter.poll(taskId) fresh URL (succeeded 아니면 상태별: pending→poll backoff, failed/blocked→_finalize_correctedpose_intent)** → srcKey equality 검증 (payload srcKey — 불일치 'invalid_output') + head_object(VISUAL_INPUT_BUCKET) → download_vendor_asset(fresh_url, /tmp, max_bytes=20MB, allowed_content_types=('image/png','image/jpeg')) → **JPEG→PNG 정규화 (H4-05/H4-06 + M5-01): safe_decode_image 로 format/dimension 검증 + EXIF 제거 + PNG 재인코딩; decode 오류 분리 — size/pixel 초과 → 'judge_input_too_large', corrupt/unsupported/format mismatch → 'invalid_output'** → sha256 은 정규화 PNG 기준 afterHash → **staging key deterministic `visual-input/{uid}/{analysisId}/{sourceHash}_after.png` (VisualInputBucket) — head_object: 부재 → put(ContentType image/png, Metadata sha256=afterHash); 존재+Metadata sha256==afterHash → 재사용(재PUT 0); 존재+hash 불일치 → overwrite 금지 + _finalize_correctedpose_intent('invalid_output') (H6-03 integrity conflict — deterministic key hash 불일치는 tampering/비결정성 신호)** → _advance(judging, {stagingKey, afterHash}, 'judge'). URL 은 로컬 변수만.
(2) `_action_judge(job)`: expect judging — S3 get before(srcKey, sha256==sourceHash 재검증 — 불일치 'invalid_output')/after(stagingKey) → **judge_corrected_pose(before_png, after_png, context={joint, targetDeg}) 단일 호출; JudgeInputTooLargeError → _finalize_correctedpose_intent('judge_input_too_large'); decode/format 오류 → 'invalid_output'**; None → _finalize_correctedpose_intent('judge_failed'); **display = judge_display_pass(verdict, min_confidence=float(env DISPLAY_JUDGE_CONFIDENCE)); training = judge_training_pass(verdict, min_confidence=float(env TRAINING_JUDGE_CONFIDENCE)) (B4-05)**; display 실패 → _finalize_correctedpose_intent('judge_failed', 판정 메타); display 통과 → _advance(pose_checking, {judgeDisplayPass:True, judgeTrainingPass, judgeConfidence}, 'pose_check').
(3) `_action_pose_check(job)`: expect pose_checking — S3 get after(정규화 PNG) → measure_generated_pose(joint_key, target_deg, tolerance=float(env DISPLAY_POSE_TOL_DEG), pose_url=derive_pose_url(env RUNPOD_ANALYZE_URL), token=env RUNPOD_AUTH_TOKEN) — passed False → _finalize_correctedpose_intent(typed 'pose_gate_failed'/'pose_gate_unavailable', 메타); 통과 → **canonical put 먼저 (B3-02 + M5-04 + H6-03): canonical key `results/{uid}/{analysis_id}/corrected_pose_{joint}.png` (VideoBucket) head → 부재 → **copy_object(staging→canonical, MetadataDirective='REPLACE', Metadata={'sha256': afterHash}, ContentType='image/png') — REPLACE 로 sha256 metadata 를 명시 보존(7차 H7-08: default COPY 나 REPLACE+sha256 누락 시 다음 replay 가 정상 객체를 integrity conflict 처리) → HEAD 로 sha256/content-type/size 검증 후 진행**; 존재+Metadata sha256==afterHash → 재사용(재copy 0); 존재+hash 불일치 → overwrite 금지 + _finalize_correctedpose_intent('invalid_output') (H6-03); **copy response loss 재실행도 put/copy 0(HEAD 재사용)** → **pairEligible/pairId/pairHmacKeyVersion 계산 (H6-05 비차단): pairEligible = (judgeTrainingPass AND poseError<=float(env TRAINING_POSE_TOL_DEG)); pairEligible False → pairId/pairHmacKeyVersion = None, pairStoreStatus 미정; pairEligible True → validate_hmac_key_set(env PAIR_ID_HMAC_KEYS) — 유효하면 pairId=pair_store.pair_id(...), pairHmacKeyVersion=active, invalid/부재하면 pairId/keyVersion None + pairPreStatus='failed_config'(display 계속 — HMAC env 오류가 correctedPose done 을 막지 않음)** → **transition pose_checking→postprocessing {canonicalKey, poseGatePassed, poseGateErrorDeg, poseGateReason, judge 메타, inputSealed:True(producer hard gate — B6-03), pendingTerminalState:'done', pairEligible, pairId, pairHmacKeyVersion, pairPreStatus?} + next_action='postprocess' (H4-01/H5-05/B5-03).**
(4) **`_action_postprocess(job)` (H4-01/H5-05/B6-03/B6-04 durable): expect postprocessing — claim 선행. (a) pairEligible True AND pairId AND pendingTerminalState=='done' 이면: S3 PUT 직전 analysis 재read — 현재 learningOptIn is True + consentVersion + capturedAt/revokedAt (H5-01); 철회 → pair 0 + pairStoreStatus='skipped_consent'; 유효하면 store_training_pair(pair_id=job.pairId, hmac_key_version=job.pairHmacKeyVersion, before = BLUR_OPTION 'pod_blur' 면 trainingSrcKey 아니면 srcKey, after=canonical/staging PNG) — **pair 는 부산물이라 사용자 표시·cleanup·finalize 의 critical path 를 막지 않는다 (7차 H7-07)**: 반환 'committed'→pairStoreStatus='done'; 'conflict'→pairStoreStatus='conflict'+quarantine/alarm(재시도 0 — M6-04); **'failed'(network/5xx)→pairStoreStatus='failed' 즉시 기록하고 그대로 (b) cleanup 으로 진행(pair 때문에 self-loop 로 cleanup/finalize 지연 금지). pair 재처리는 별도 운영 재구동만(pairAttempt self-loop 제거)**. (job.pairPreStatus=='failed_config' 면 pair 0 + pairStoreStatus='failed_config' 진행 — H6-05). (b) **비-버저닝 단일 delete cleanup (B6-03 + M6-05): `_cleanup_visual_input(s3, VISUAL_INPUT_BUCKET, uid, analysis_id) -> int` — list_objects_v2 exact prefix `visual-input/{uid}/{analysisId}/`(ContinuationToken 끝까지) → delete_objects batch → **응답 per-object Errors(code/key) 기록 + failed count metric(M6-05)** → 재조회 list_objects_v2 count = remainingObject 반환. **★10차 B10-03: delete 성공 후 job payload 의 모든 expected key(srcKey/trainingSrcKey)에 `release_key_ownership(bucket, key, ref=jobRef)` 를 수행해 job liveRef 를 소거한다(정상 worker cleanup 도 ownership ref 를 반드시 release — 미release 시 후속 동일-key cleanup 영구 skip). terminal gate = S3 remainingObject==0 AND ownership liveRefs 0(object doc 제거) 둘 다.** remainingObject>0 또는 delete Errors>0 이면 cleanupAttempt+1 후 max(=5) 미만이면 postprocessing self-loop 재개, max 도달 시 transition {privacyBlocker:'cleanup_blocked'} + 장기 backoff + VisualCleanupBlocked metric/alarm (B6-04)**. (c) **remainingObject==0 AND ownership refs 0 확인 시 finalize_visual_job 에 `cleanup_verified_at_ms=now` 를 파라미터로 전달**(7차 B7-03 — job_meta 로만 넘기던 순서 모순 제거; finalize 가 같은 transaction 에서 candidate 병합+validate+write) finalize_visual_job(pendingTerminalState, pendingFailureReason, cleanup_verified_at_ms=now, job_meta={pairStoreStatus, poseGate/judge 메타}, display_status(done↔done/failed↔failed), key=(done 이면 canonicalKey else None), joint, expect_claim_owner=owner, now_ms=now) — 31-02 validator 가 correctedPose inputSealed True 면 done·failed 공통 cleanup_verified_at_ms>0 요구(B7-04). **privacyBlocker 가 세팅돼 있으면 finalize 는 remainingObject 0 재확인 후에만(finalize 가 privacyBlocker None 을 요구하므로 clear 는 finalize candidate 병합에서 처리)**. 재전달(crash 재개)이면 (a)(b)(c) idempotent (pair meta-read 멱등 B5-03, delete 이미 삭제분 무해, finalize 'stale' no-op).**
판정 메타 전부 flat scalar. 테스트: PASS 체인 (fetch→judge→pose_check→postprocess invocation 분리) → done + canonical key + 판정 메타 + inputSealed True + exact-prefix visual-input objects 0(list_objects_v2) + cleanupVerifiedAtMs 기록 + 페어 적재(동의 시) + **job/analysis 항상 함께 done**; **H5-05 실패 postprocessing: judge_failed/pose_gate_failed/invalid_output/timeout/moderation/create_unconfirmed 각각 postprocessing 경유(inputSealed True) → 단일 delete cleanup 후 failed finalize (직접 finalize 0)**; **B6-04 cleanup_blocked: delete 후 remainingObject 5회 연속 >0 → privacyBlocker='cleanup_blocked' + postprocessing 유지 + terminal finalize 0 + VisualCleanupBlocked metric; 이후 delete 성공(remaining 0) → cleanup_verified_at_ms 파라미터로 finalize**; **B7-03 handoff: remaining 0 → finalize_visual_job(cleanup_verified_at_ms=now) 호출 assert(job_meta 로 cleanupVerifiedAtMs 미전달) → done 성공 + job 문서 cleanupVerifiedAtMs 기록**; **B7-04 failed cleanup: failed intent(judge_failed 등) + inputSealed True → cleanup 후 cleanup_verified_at_ms 전달 finalize 만 성공; cleanup 미검증 failed finalize 시도 → ValueError**; **H7-07 pair off critical path: pair store 'failed'(network) → self-loop 0 + pairStoreStatus='failed' + 즉시 cleanup/finalize 진행(사용자 done 지연 0)**; **H7-08 copy metadata: canonical copy_object 가 MetadataDirective REPLACE + Metadata sha256 → HEAD sha256 일치; copy response loss 재실행 → copy 0**; **H4-01 durable: canonical copy 성공 후 postprocess 전 crash → 재개 두 문서 일치; pair 후·finalize 전 crash → pair meta-read 멱등 재PUT 0 + finalize; finalize commit_lost → 재전달 'stale' no-op 두 문서 일치**; **H6-03: staging/canonical deterministic key 존재+hash 불일치 → overwrite 0 + 'invalid_output'(postprocessing 경유 cleanup)**; **H6-05: pairEligible False → pairId None + done + 페어 0; HMAC env invalid + eligible True → pairStoreStatus='failed_config' + pair PUT 0 + correctedPose done(display 안 막힘)**; **H5-01 consent race: pose_check 시 opt-in, postprocess 직전 revoke → pair PUT 0 + skipped_consent + cleanup/finalize 정상**; **B5-03: pairId/keyVersion pose_checking→postprocessing 고정 + postprocess active key 재선택 0(grep) + crash 재PUT 0**; **H8-02: pair 'conflict' → 즉시 quarantine/alarm + 재시도 0; 'failed'(network) → pairStoreStatus='failed' + self-loop 0 + 즉시 cleanup/finalize 진행(pair durable self-loop 없음 — cleanup 만 durable, 플라이휠 손실 수용 amended)**; **M6-05: delete_objects Errors 주입 → failed count 반영 + cleanupAttempt 증가**; **B4-05: DISPLAY 통과·TRAINING 미달 → done + 페어 0**; fetch 재-poll fresh URL (소스 grep 'outputUrl' 저장/판독 0); **staging reuse: 재실행 같은 normalized PNG → staging/canonical head 재사용, put/copy 재호출 0**.</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31/test_visual_worker.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - fetch 가 taskId 재-poll + JPEG→PNG 재인코딩 + staging deterministic HEAD/hash reuse + hash 불일치 overwrite 0(H6-03) (H4-05/H3-01)
    - decode 오류 분리: size/pixel=judge_input_too_large, corrupt/mismatch=invalid_output (M5-01) + MAX_IMAGE_PIXELS 모듈 1회 (M5-03)
    - 모든 correctedPose terminal(성공+실패) postprocessing 경유(inputSealed True) + 비-버저닝 단일 delete cleanup 후 remainingObject 0 확인 후 finalize (H5-05/B6-03)
    - cleanup remainingObject>0 → privacyBlocker='cleanup_blocked' + terminal finalize 0 + alarm (B6-04)
    - postprocess consent 재read (H5-01) + caller-fixed pairId(B5-03) + pair failed_config 비차단(H6-05) + conflict 즉시 quarantine(M6-04) + delete Errors 반영(M6-05)
    - postprocessing durable: canonical 후 crash / pair 후 crash / cleanup 재개 / commit_lost 각각 두 문서 일치 (H4-01)
    - display/training 4 env 분리 소비 (B4-05)
  </acceptance_criteria>
  <done>D-05 자동 교정이 URL 비저장·PNG 정규화·hash conflict·inputSealed·consent 재확인·비-버저닝 단일 delete cleanup·cleanup_blocked nonterminal·성공+실패 postprocessing 경유까지 테스트로 고정됨.</done>
</task>

<task type="auto">
  <name>Task 3: rotation fetch + dispatcher Lambda(이중 cursor/H6-01 lease-expired/ScannedOutboxMaxAge) + 운영자 스크립트(reconciler + cleanup_blocked)</name>
  <files>backend/functions/visual-worker/app.py, backend/functions/visual-dispatch/app.py, backend/functions/visual-dispatch/requirements.txt, backend/scripts/list_stuck_visual_jobs.py, backend/tests/phase31/test_visual_worker.py, backend/tests/phase31/test_visual_dispatcher.py</files>
  <read_first>backend/shared/python/sunity_shared/s3keys.py (build_upload_key — canonical upload key 단일 출처), .planning/spikes/004-gemini-omni-view-editing/wan_gate_batch.py (회전 PROMPT 원문 — 카메라 orbit 이식), backend/shared/python/sunity_shared/firestore_admin.py (list_dispatch_pending 반환 {items,scanned_outbox_max_age_ms,truncated,cursor_wrapped} / mark_visual_job_dispatched(action+seq) / update_analysis_visual), .planning/phases/31-api-visual-correction/31-PLAN-REVIEW-ITERATION6.md 의 B6-01/B6-04/H6-01/M6-01/M6-03 절</read_first>
  <action>(1) `_action_create` rotation 분기: myVideoKey 를 s3keys.build_upload_key 후보(mp4/mov) equality 검증 — 불일치 finalize 'invalid_output' (H-05), presign 1h → WanVideoEditAdapter.create_task(video_url, PROMPT 상수); **sync 반환 시 async-only 이므로 'vendor_error' (B4-02, rotation 도 async)**. (2) `_action_fetch_rotation(job)`: claim → expect fetching (claim 반환 snapshot 사용) → **taskId 재-poll fresh URL (H3-01)** → download_vendor_asset(fresh_url, /tmp, max_bytes=200MB, **allowed_content_types=('video/mp4',) — Content-Type 정규화 exact membership: `split(';',1)[0].strip().lower()` (M5-02)**) — /tmp streaming + boto3 upload_file(multipart) canonical `results/{uid}/{analysis_id}/rotation.mp4` (VideoBucket, 전체 bytes 메모리 적재 0 — H2-05) → **finalize_visual_job(done, display_status='done', key, expect_claim_owner=owner, now_ms=now) — job+analysis 원자 (rotation 은 temp 입력·pair 없음 → postprocessing 미경유, inputSealed 불요)**.
(3) **visual-dispatch/app.py 신설 (H3-09 + H4-08 + B5-01 + H5-06 + 6차 H6-01/M6-01)**: lambda_handler(EventBridge) — `res = list_dispatch_pending(now_ms, limit=20)` → `res["items"]`(pending nonterminal + sent+H6-01 필터 복구 대상 혼합) 각 항목 `sqs.send_message(env VISUAL_QUEUE_URL, {"jobId","generation","action": nextAction,"outboxSeq"})` → `mark_visual_job_dispatched(expect_action=nextAction, expect_outbox_seq=outboxSeq)`. **creating 항목은 결과에 없음(outbox 미기록)이 정상 → 방어적 skip. metric (M6-01): res["scanned_outbox_max_age_ms"] 를 cloudwatch put_metric_data(Namespace 'SunityVisual', MetricName 'ScannedOutboxMaxAgeMs') 방출; res["truncated"] True 면 metric 'OutboxScanTruncated'=1 방출.** **janitor 부수 작업 (8차 B8-06/H8-01/M8-05 + 9차 B9-01/B9-02/B9-03/H9-02 — bounded cursor + crash-recoverable claim lease + key ownership): (i) reservation sweep: `visualInputReservations` durable cursor 로 bounded 순환. **claim_reservation_for_janitor(job_id, reservation_id, owner=janitor_owner, now_ms)** — (9차 H9-02 predicate) expectedKeys∪createdKeys 를 소유한 nonterminal unsealed job 부재(terminal/inputSealed/mismatched → 삭제 허용) AND [(open+leaseExpiresAt<=now) OR (claimed_by_janitor+claimLeaseExpiresAt<=now = 9차 B9-01 claim crash 재claim)] 를 같은 transaction 확인·claimOwner/claimLease CAS. **★10차 B10-01/B10-02 + 11차 B11-01/B11-04: 성공 후 각 key(expectedKeys∪createdKeys, B9-03) 에 `claim_key_for_delete(deleting_ref=reservationId, owner=janitor_owner, lease_ms, now_ms)` → token 획득(자기 ref 소비 + live ref 0[**job ref 는 항상 live·reservation ref 만 만료 판정 — B11-04**] + state='deleting' CAS). **★외부 delete_object **직전** `commit_key_delete(token)` 재검증(owner/generation/미만료 lease CAS) 통과한 key 만 delete → HEAD404(s3:GetObject) → release_key_ownership + object doc 제거/close (B11-01: J1 이 lease 만료 후 살아나도 generation 불일치로 commit 실패 → late delete 0)**. claim None(live ref)이면 skip; delete/claim 직후 crash → deleteLease 만료 후 J2 재claim(deleting 회수는 janitor 만). producer acquire 는 state=='deleting' 이면 만료여도 항상 fence(B11-01)**; delete 실패 → upsert_visual_orphan. **(ii) orphan sweep(동일 fence+token): `claim_visual_orphan` → 각 key `claim_key_for_delete(deleting_ref=orphanId)` → commit_key_delete → delete → release + close. ★orphan 도 nonterminal job ref 는 live 로 계산해 삭제 안 함(B11-04).** **TTL(VISUAL_INPUT_RESERVATION_TTL_MS)는 PipelineTimeout+margin 보다 커 정상 producer 오판 금지(31-10 build gate H9-01). metric(M8-05): VisualOrphanSweepDeleted/OpenCount/OldestAgeMs/SweepFailed + reservation 등가; oldest-age/failure alarm(31-10 template 정의).** 로그 key=value. requirements.txt = firebase-admin + pyyaml (deploy fix 주석 2건).
(4) list_stuck_visual_jobs.py: argparse --profile 기본 sunity-motion, --stale-minutes 기본 30, --redispatch, **--reconcile**: (a) 조회 — nonterminal stale 표(jobId/kind/state/dispatchState/nextAction/outboxSeq/attempt/failureReason/privacyBlocker/경과분); (b) --redispatch — pending 항목 SQS 재발행(action+outboxSeq 포함); (c) --reconcile — terminal job(done|failed)과 analysis 표시 불일치 → update_analysis_visual repair (이 스크립트가 유일 정당 호출자 — 31-02); (d) 'create_unconfirmed' 항목은 별도 섹션 "수동 판정 필요 — 중복 과금 회피" (M3-07); (e) **privacyBlocker=='cleanup_blocked' 항목 별도 섹션 "임시 생체 프레임 잔존 — cleanup 재구동 필요": exact prefix list_objects_v2 잔존 표시 + --redispatch-cleanup 로 postprocess outbox 재발행(운영 remediation — B6-04)**; (f) **visualOrphans open 문서 + expired visualInputReservations 조회/재구동 섹션 (7차 H7-04 — janitor 미처리분 운영자 가시성)**.
테스트 — test_visual_worker.py: rotation 성공 → 재-poll + upload_file(put_object bytes 아님) + finalize 원자(owner/lease CAS) + URL 미저장 + postprocessing 미경유; myVideoKey 불일치 → 'invalid_output' + create 0; rotation sync → 'vendor_error'; **M5-02: content-type 'video/mp4; charset=x' 통과 / 'video/mp4foo' 거부**. test_visual_dispatcher.py: pending 항목(polling/fetching/retry_ready 혼합) 전부 발행 + 각 action==nextAction + outboxSeq 포함 + mark(action+seq CAS); **sent+H6-01 필터(same-seq claimState='claimed' expired) 복구 항목 발행; sent+미claim(lease 0) 재발행 0; sent+구 seq claim 재발행 0 (H6-01)**; sent+lease-유효 미발행; terminal 미발행; **B5-01 종합: 워커 CAS 직후 crash → dispatcher lease 만료 후 발행 → 체인 재개 → terminal**; 상한 20건; **8차 janitor barrier(B8-06) + 1,200 drain(H8-01)**; **★9차 B9-01 janitor claim crash: claim_reservation_for_janitor CAS commit 직후 delete 전 crash → claimLease 전 재실행 0 → 만료 후 재claim → delete+HEAD404+close; orphan claim 직후 crash 동형**; **★10차+11차 ownership barrier: (B10-01) A-only expired→delete; (B9-02) A-expired+B-live→delete 0; (B10-03) worker cleanup 후 job ref 0+doc 제거; ★(B11-01) J1 claim(token gen=7)→lease 만료→P acquire=False(deleting 회수 안 함)→J2 재claim(gen=8)→commit_key_delete(J2)통과 delete→close→P acquire=True; J1 이 만료 후 commit_key_delete(gen=7) 시도→generation 불일치→DeleteObject 0; ★(B11-04) expired reservation ref + nonterminal job ref + orphan due → claim_key_for_delete 가 job ref live 로 계산 → None + DeleteObject 0; worker explicit release 후에만 orphan delete**; **9차 B9-03 multi-object: src record→trainingSrc 2xx→record 전 crash → expectedKeys∪createdKeys 로 두 key 모두 삭제**; **9차 H9-02 predicate 표: terminal/inputSealed/mismatched → 삭제, matching nonterminal unsealed → 보존**; **9차 H9-09 create failure kind: correctedPose acquired→typed failure/unconfirmed/sync → postprocessing 경유; rotation 동일 → direct finalize(postprocessing 미경유) 각각 별도**; VisualOrphanOldestAge/SweepFailed metric 방출.</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31/test_visual_worker.py backend/tests/phase31/test_visual_dispatcher.py -x -q && python -m pytest backend/tests -q</automated>
  </verify>
  <acceptance_criteria>
    - rotation fetch 가 재-poll + upload_file 기반 + content-type exact membership (M5-02) + postprocessing 미경유 (H2-05/H3-01)
    - visual-dispatch/app.py 가 pending + sent H6-01 필터(same-seq expired claim) 복구 발행 + ScannedOutboxMaxAge age + truncated metric (H3-09/H4-08/B5-01/H6-01/M6-01)
    - dispatcher 연동 (crash → lease 만료 → dispatcher → terminal) green (B5-01)
    - list_stuck_visual_jobs.py 에 --reconcile + M3-07 문구 + cleanup_blocked remediation 섹션 (B6-04)
    - `python -m pytest backend/tests -q` exits 0
  </acceptance_criteria>
  <done>D-06 회전 잡 + 독립 dispatcher(이중 cursor/H6-01 lease-expired/age) 복구 + reconciler + create_unconfirmed/cleanup_blocked 가시성 완결.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQS 메시지 → 워커 외부 side-effect | claim dict 'claimed' 만(반환 snapshot 사용) — busy/stale/completed/malformed 는 외부 0 |
| 사용자 객체 → 벤더 | canonical/hash key equality 통과분만 |
| 벤더 응답 → S3 | taskId 재-poll fresh URL + download 스트리밍 + JPEG→PNG 정규화 + hash conflict 차단 + content-type exact 통과분만 |
| worker 결과 → analysis 표시 | finalize_visual_job 단일 transaction (owner/lease CAS + postprocessing durable 경유 + cleanupVerified/inputSealed) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-32 | Denial of Service | 전이 후 발행 유실 고아 | mitigate | 전이=원자 outbox(seq)+snapshot + dispatcher 독립 복구(H6-01 lease-expired) + crash 기대값 terminal (B3-01/B4-01/B5-01) |
| T-31-33 | Tampering | 중복/stale/구세대/구-outbox/lease 상실 write | mitigate | claim dict 4상태 + snapshot handoff + creating lease + CAS + generation + outboxSeq + expect_claim_owner+미만료 lease (B2-02/B3-01/B4-01/B5-01/B6-01/H6-07) |
| T-31-34 | Information Disclosure | 타 사용자 객체 벤더 전송 | mitigate | upload/hash key equality (H-05 + B2-03) |
| T-31-35 | Information Disclosure | 원본 프레임/서명 URL 잔존 | mitigate | 비-버저닝 단일 delete cleanup(remainingObject 0) + taskId 만 + 직렬화 URL 부재 (H-04/H3-01/B6-03) |
| T-31-36 | Tampering | 저품질/포맷불일치 생성물 노출·적재 | mitigate | before/after judge + pose 게이트 + JPEG→PNG 정규화 + hash conflict + decode 오류 분리 + calibration (H-03/H3-03/H3-04/H4-05/H6-03/M5-01) |
| T-31-37 | Repudiation | generic 실패/조용 삭제/영구 불일치/pair 유실 | mitigate | typed 10종 + malformed→DLQ + finalize 원자 + 성공+실패 postprocessing durable + reconciler (M2-02/B3-02/H4-01/H5-05) |
| T-31-58 | Denial of Service | 복구 경로 concurrency 굶김 | mitigate | dispatcher 별도 Lambda concurrency 1 + 이중 durable cursor + ScannedOutboxMaxAge metric/alarm (H3-09/H4-08/H5-06/M6-01) |
| T-31-62 | Denial of Service (과금) | standard SQS 중복으로 유료 action 중복 | mitigate | claim busy → change_visibility+batchItemFailures, claimed 만 외부 (B5-01/M6-03) |
| T-31-71 | Tampering | postprocess crash + HMAC rotation 중복 학습쌍 | mitigate | pairId/keyVersion pose_checking→postprocessing 고정 + store meta-read 멱등 (B5-03) |
| T-31-72 | Privacy | 저장 직전 opt-out 무시 | mitigate | postprocess S3 PUT 직전 consent 재read → skipped_consent (H5-01) |
| T-31-73 | Information Disclosure | terminal/sealed 앞 임시 생체 프레임 잔존 | mitigate | postprocessing inputSealed 기록(producer hard gate) + 비-버저닝 단일 delete cleanup + remainingObject 0 후 finalize + cleanup_blocked nonterminal (B6-03/B6-04) |
| T-31-77 | Availability | HMAC env 오류가 display correctedPose 차단 | mitigate | pairEligible/key config 실패 = pair 비차단(failed_config) + display done (H6-05) |
</threat_model>

<verification>
- `python -m pytest backend/tests -q` green
- 워커/디스패처 모듈에 requests·키 리터럴·fail_analysis·update_analysis_visual·google.genai·시간 폴링 루프·outputUrl 기록·list_object_versions(비-버저닝 — 버전 열거 금지) 0
- postprocess 가 store_training_pair 에 caller-fixed pairId/keyVersion 전달 (active key 재선택 0 — grep) + claim 반환 snapshot 만 사용(pre-claim job 재사용 0 — grep, B6-01)
- requirements.txt 2종 pyyaml 존재 (deploy fix 선례 2건 인용)
</verification>

<success_criteria>
- B5-01(claim dict 4상태 + busy change_visibility+batchItemFailures + owner/lease CAS + H6-01 복구) + B6-01(snapshot handoff) + B6-03(inputSealed + 비-버저닝 단일 delete cleanup) + B6-04(cleanup_blocked nonterminal) + H6-03(hash conflict) + H6-05(pair 비차단) + H5-01(consent 재확인) + **cleanup 만 durable self-loop(H5-02 는 cleanup 한정으로 amend) + pair 단일 시도(H8-02 — network 실패 시 self-loop 0, 플라이휠 손실 belle 결정 위임) + conflict 즉시 quarantine(M6-04)** + M6-05(delete Errors) + M5-01/M5-02/M5-03 + B4-02/B4-03/H4-05/B4-05/H3-01/H5-04/H5-05 전부 테스트/텍스트 고정
- 7차 B7-03(cleanup proof 파라미터 handoff)/B7-04(done|failed 공통 cleanup validator)/H7-01(internal transition send 0)/H7-02(create CAS)/H7-04(janitor 소비)/H7-07(pair off critical path)/H7-08(copy metadata) 반영
- 8차 B8-01(begin status 분기)/B8-03(cleanup_blocked clear finalize)/B8-06(janitor claim CAS 선형화)/H8-01(janitor bounded cursor)/H8-02(pair 단일 시도)/M8-04(create status 테스트)/M8-05(janitor metric) 반영
- 9차 B9-01(janitor claim crash 복구)/B9-02(key ownership ref)/B9-03(expected∪created)/H9-02(predicate 표)/H9-09(create failure kind 분기) 반영
- 10차 B10-01(self-ref 소비)/B10-02(delete fence)/B10-03(worker cleanup+보상 ref release) 반영 — reservation·orphan janitor 공통 claim_key_for_delete
- 11차 B11-01(commit_key_delete fencing token — late claimant 차단)/B11-04(job ref 무기한 live) 반영
- 11차 리뷰 §7 실행 허용 조건 중 worker/janitor 측 항목 전부 이 플랜 테스트로 추적 가능
</success_criteria>

<output>
Create `.planning/phases/31-api-visual-correction/31-09-SUMMARY.md` when done
</output>
