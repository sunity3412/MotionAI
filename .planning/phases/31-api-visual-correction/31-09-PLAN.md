---
phase: 31-api-visual-correction
plan: 09
type: execute
wave: 4
depends_on: [31-02, 31-05, 31-06, 31-07, 31-13]
files_modified:
  - backend/functions/visual-worker/app.py
  - backend/functions/visual-worker/requirements.txt
  - backend/functions/visual-dispatch/app.py
  - backend/functions/visual-dispatch/requirements.txt
  - backend/scripts/list_stuck_visual_jobs.py
  - backend/tests/phase31/test_visual_worker.py
  - backend/tests/phase31/test_visual_dispatcher.py
autonomous: true
requirements: [D-03, D-04, D-05, D-06, D-08]
must_haves:
  truths:
    - "worker 는 state action 1개만 수행하고 durable next action(nextAction+outboxSeq)을 transaction 에 남긴다 — SQS send 는 best-effort, 복구는 dispatcher (3차 B3-01 + 4차 B4-01)"
    - "메시지 = {jobId, generation, action, outboxSeq}. worker 는 외부 side-effect 전에 claim_visual_job_action(jobId, generation, action, outboxSeq, owner, lease) 로 claim 하고, already_claimed/stale 이면 외부 호출 없이 no-op 한다 — standard SQS 중복/무순서 전달에서 유료 action 중복 실행 0 (4차 리뷰 B4-01)"
    - "각 CAS commit 직후 crash (polling/fetching/judging/pose_checking/postprocessing) 에서 dispatcher 가 정확한 다음 action 을 1회 이상 발행하고 외부 create 중복 0 으로 terminal 에 도달한다 — '재전달 no-op 멱등' 은 PASS 기준이 아니다 (B3-01)"
    - "correctedPose 성공/실패 종결은 pose_checking→postprocessing 을 거친다 — postprocess action 이 idempotent commit-marker pair store(성공+동의 시) + exact-version temp cleanup 을 수행한 뒤 finalize_visual_job 한다. finalize 직후 crash 로 pair/cleanup 이 유실되지 않는다 (4차 리뷰 H4-01)"
    - "vendor create 는 async taskId 경로만 정상 처리한다 — create_task 가 sync succeeded 를 반환하면 typed 실패로 종결(v1 async-only, B4-02). rotation 도 async"
    - "moderation retry 는 transition 이 polling→retry_ready 에서 taskId=None·attempt=0·lease/failure clear + retryCount+1 + nextAction='create' 을 원자 기록하고, retry_ready→creating 이 generation+1 로 승격한다 — crash 시 과거 blocked task 재개 0 (4차 리뷰 B4-03)"
    - "job 문서에 outputUrl 을 저장하지 않는다 — taskId 만. fetch invocation 이 taskId 재-poll 로 fresh URL 을 얻어 같은 invocation 에서 즉시 streaming download 한다 (3차 리뷰 H3-01)"
    - "다운로드된 correctedPose 이미지는 safe_decode_image + 실제 PNG 재인코딩(EXIF 제거)으로 정규화된다 — staging/canonical 은 PNG bytes 만, ContentType image/png, sha256 은 정규화 PNG 기준. JPEG 벤더 출력이 .png key/MIME 와 불일치할 수 없다 (4차 리뷰 H4-05/H4-06)"
    - "judge 는 prepare_judge_payload(safe_decode+16MB) 를 judge_corrected_pose 내부에서 단일 호출하고 worker 는 JudgeInputTooLargeError 만 catch → 'judge_input_too_large' (3차 H3-03 + 4차 M4-02)"
    - "judge action 은 같은 raw verdict 로 judge_display_pass(...DISPLAY_JUDGE_CONFIDENCE) 와 judge_training_pass(...TRAINING_JUDGE_CONFIDENCE) 를 각각 호출한다 — 두 confidence 는 별개 env (4차 리뷰 B4-05)"
    - "temp 입력 cleanup 은 source/staging put 의 VersionId 를 지정 삭제한다 — versioned bucket 에서도 noncurrent version 잔존 0 (4차 리뷰 H4-03)"
    - "dispatcher 는 별도 소형 Lambda(reserved concurrency 1) — dispatchState='pending' 인 모든 nonterminal state 를 발행하고 실 backlog pending age metric 을 방출한다 (3차 H3-09 + 4차 H4-08)"
    - "worker 는 action=iam_probe 를 외부 side-effect 없이 소비한다 — 31-12 IAM canary send-only 전용 (3차 H3-10 + 4차 H4-02)"
  artifacts:
    - path: "backend/functions/visual-worker/app.py"
      provides: "action 단위 state machine 워커 (create/poll/fetch/judge/pose_check/postprocess + iam_probe) — claim + transaction outbox + finalize 소비"
      contains: "claim_visual_job_action"
    - path: "backend/functions/visual-dispatch/app.py"
      provides: "dispatcher Lambda — list_dispatch_pending 발행(outboxSeq 포함) + dispatched CAS(action+seq) + 실 backlog age metric"
      contains: "list_dispatch_pending"
    - path: "backend/scripts/list_stuck_visual_jobs.py"
      provides: "운영자용 stuck 조회 + job/analysis terminal reconciler + create_unconfirmed 수동 판정 목록"
      contains: "reconcile"
    - path: "backend/tests/phase31/test_visual_worker.py"
      provides: "crash-injection(전이 5지점 + create 계열 + finalize/postprocess)·claim 중복·직렬화 URL 부재·JPEG→PNG·typed 실패 테스트"
      min_lines: 240
  key_links:
    - from: "backend/functions/visual-worker/app.py"
      to: "backend/shared/python/sunity_shared/firestore_admin.py"
      via: "claim_visual_job_action + transition_visual_job(원자 outbox) + finalize_visual_job(원자 종결)"
      pattern: "finalize_visual_job"
    - from: "backend/functions/visual-dispatch/app.py"
      to: "backend/shared/python/sunity_shared/firestore_admin.py"
      via: "list_dispatch_pending + mark_visual_job_dispatched"
      pattern: "mark_visual_job_dispatched"
    - from: "backend/functions/visual-worker/app.py"
      to: "backend/shared/python/sunity_shared/analysis/visual_gen.py"
      via: "create_task/poll + download_vendor_asset + safe_decode_image + judge_corrected_pose"
      pattern: "visual_gen"
---

<objective>
4차 리뷰 §7 Step 2·4 를 구현한다: worker/dispatcher 분리 + outbox instance claim + postprocessing durable.

- **worker (VisualWorkerFunction):** 메시지 {jobId, generation, action, outboxSeq} 수신 → **외부 side-effect 전에 `claim_visual_job_action` 으로 claim (already_claimed/stale → 외부 호출 없이 no-op — B4-01 중복 방어)** → 자기 외부 side-effect 1개 → `transition_visual_job(expect_outbox_seq=메시지 seq)` 가 다음 state + nextAction + dispatchState='pending' + 새 outboxSeq 를 **한 transaction** 기록 → best-effort `sqs.send_message({jobId, generation, action: nextAction, outboxSeq: 새 seq})` + `mark_visual_job_dispatched(expect_action, expect_outbox_seq)`. **send/mark 실패는 삼키고 log** — durable outbox 를 dispatcher 가 복구.
- **dispatcher (VisualDispatchFunction — H3-09):** 별도 Lambda(reserved concurrency 1, EventBridge rate(1분)). `list_dispatch_pending` 으로 pending 인 모든 nonterminal state 를 조회(outboxSeq 포함)해 정확한 nextAction 발행 + `mark_visual_job_dispatched(action+seq CAS)` + 실 backlog age metric.
- **종결:** correctedPose 는 pose_checking→postprocessing→(postprocess: pair store + versioned cleanup)→finalize (H4-01). rotation 은 fetching→finalize. 전부 `finalize_visual_job` (B3-02). canonical put 은 finalize 전 immutable key.
- **URL 비저장 (H3-01):** poll succeeded 시 outputUrl 미기록. fetch 가 taskId 재-poll 로 fresh URL 획득 후 같은 invocation streaming download.
- **async-only (B4-02):** create_task 가 sync succeeded 를 반환하면 v1 은 async 미지원이므로 typed 실패로 종결 (release gate 가 sync 후보를 애초에 blocked).
- **moderation retry (B4-03):** transition 이 polling→retry_ready 에서 taskId=None·attempt=0·lease/failure clear + retryCount+1 + nextAction='create', retry_ready→creating 이 generation+1 승격. create action 은 'reserved'|'retry_ready' 처리.

judge 입력은 31-05 `judge_corrected_pose`(내부 prepare_judge_payload safe_decode+16MB — M4-02), pose 게이트 입력은 31-06 정규화(H3-04). display/training 임계값·tolerance 는 31-13 CALIBRATION 채택값을 env 로 소비 (DISPLAY_JUDGE_CONFIDENCE / TRAINING_JUDGE_CONFIDENCE / DISPLAY_POSE_TOL_DEG / TRAINING_POSE_TOL_DEG — B4-05).

Purpose: 유료 잡이 고아도 중복 과금도 영구 불일치도 pair/cleanup 유실도 될 수 없는 비동기 실행기.
Output: visual-worker Lambda + visual-dispatch Lambda + 운영자 스크립트(reconciler) + crash/claim 테스트.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-api-visual-correction/31-PATTERNS.md
@.planning/phases/31-api-visual-correction/31-ACCEPTANCE.md
@.planning/phases/31-api-visual-correction/31-PLAN-REVIEW-ITERATION4.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 워커 골격 — action state machine + claim + 범용 outbox(seq) 소비 + creating lease + partial batch failure</name>
  <files>backend/functions/visual-worker/app.py, backend/functions/visual-worker/requirements.txt, backend/tests/phase31/test_visual_worker.py</files>
  <read_first>backend/functions/pipeline/app.py lines 4816-4867 (SQS 레코드 루프) + lines 1258-1263 (_signed_get), backend/functions/pipeline/requirements.txt + backend/functions/playback-url/requirements.txt (pyyaml deploy fix 주석 2건 원문), backend/shared/python/sunity_shared/firestore_admin.py (31-02 함수 7종 — claim_visual_job_action / transition(expect_outbox_seq) / finalize / mark(action+seq)), backend/shared/python/sunity_shared/models.py (VISUAL_JOB_STATES 10종), .planning/phases/31-api-visual-correction/31-PLAN-REVIEW-ITERATION4.md 의 B4-01/B4-02/B4-03 절</read_first>
  <action>visual-worker/app.py 신설 (docstring: B4-01/B4-02/B4-03/H4-01 불변식 — "외부 side-effect 당 invocation 1개, claim 후에만 외부 호출, 모든 전이 = transaction 원자 outbox(seq), send best-effort, terminal = finalize 단독, 복구 = dispatcher"). lambda_handler: SQS Records 루프 — 각 레코드 try: 파싱 {jobId, generation, action, outboxSeq} — 파싱/스키마 위반 log.error + batchItemFailures (M2-02); **action=='iam_probe' → log.info 후 정상 소비(외부 0 — H4-02 canary send-only 전용)**; `_handle_job(...)`; except → log.exception + batchItemFailures. 반환 {"batchItemFailures":[...]}.
**claim 규율 (B4-01):** 외부 side-effect 가 있는 action(poll/fetch/judge/pose_check/postprocess)은 side-effect **전** `claim_visual_job_action(job_id, generation=msg.generation, action=msg.action, outbox_seq=msg.outboxSeq, owner=aws_request_id, lease_ms=액션별)` 호출 — 'stale'/'already_claimed' → 외부 호출 0 + 정상 소비(재발행 불필요). create 는 claim 미사용(creating lease 특례).
공통 헬퍼 `_advance(job, next_state, updates, next_action, delay_s=0)`: transition_visual_job(원자 outbox, expect_generation=msg.generation, expect_outbox_seq=msg.outboxSeq) → True 면 새 outboxSeq 를 읽어 best-effort `sqs.send_message({jobId, generation, action: next_action, outboxSeq: 새 seq}, DelaySeconds=delay_s)` + mark_visual_job_dispatched(expect_action=next_action, expect_outbox_seq=새 seq) — **send/mark 실패 삼키고 log.warning (dispatcher 복구; 예외 전파로 batchItemFailures 넣지 않음)**.
action 핸들러:
- `_action_create(job)`: state 'creating' 이면 lease 검사 — leaseExpiresAt 미래 = no-op, 과거+taskId None = **finalize 'create_unconfirmed' (자동 재생성 금지 — B2-02; 문구 "중복 과금 회피 수동 판정")**, 과거+taskId 존재 = polling 재개. state ∈ ('reserved','retry_ready') → transition → creating {leaseOwner, leaseExpiresAt now+600_000ms, requestKey f"{jobId}:gen{generation}"} (retry_ready→creating 은 31-02 validator 가 taskId None 강제 + generation+1) → kind 어댑터 create_task 1회 → VendorTaskCreated → _advance(polling, {taskId}, 'poll', delay_s=60); **VendorPollResult succeeded (sync) → v1 async-only 이므로 finalize 'vendor_error'(async taskId 부재) + log("sync image production 미허용 — B4-02")**; typed 실패 → finalize.
- `_action_poll(job)`: claim → expect polling — 1회 poll: succeeded → _advance(fetching, {}, 'fetch') (URL 미기록 — H3-01); pending → attempt+1 >= MAX_POLLS(20) 면 finalize 'timeout', 아니면 transition polling→polling {attempt+1} + next_action='poll' + next_dispatch_at_ms now+60s (새 outboxSeq 로 old poll message 무력화 — B4-01) + best-effort send; failed → finalize 'vendor_error'; blocked → retryCount < MODERATION_RETRY_MAX 면 **transition polling→retry_ready (31-02 가 taskId=None·attempt=0·lease/failure clear + retryCount+1 + next_action='create' 원자 — B4-03)** + best-effort send, 아니면 finalize 'moderation'.
- 종결 공통: correctedPose 는 `_action_pose_check` 통과 후 postprocessing 경유(Task 2). 그 외 실패는 finalize_visual_job. **transition 으로 done/failed 금지 (31-02 validator ValueError). fail_analysis 절대 금지 (D-08) + update_analysis_visual 직접 호출 금지(reconciler 전용 — grep).**
requirements.txt = `firebase-admin>=6,<7` + `pyyaml>=6.0,<7.0` (주석에 두 선례 deploy fix 인용 원문). google-genai 금지. 로깅 key=value, 키·서명 URL 미로깅.
test_visual_worker.py — **claim 중복 (B4-01): 같은 {action,outboxSeq} 메시지 2개 → claim 1개만 'claimed'·나머지 'already_claimed' → 외부(vendor/Gemini) 호출 총 1회**; **old poll seq 재전달 (self-loop 후): state/generation 동일하나 outboxSeq 구식 → claim 'stale' + no-op + attempt/nextAction 불변**; **B4-01 crash: create→polling / poll→fetching CAS 직후·send 전 crash → (a) nextAction/dispatchState='pending'+새 outboxSeq 잔존, (b) dispatcher 발행 시 정확한 다음 action 1회+, (c) terminal 도달, (d) 외부 create 1회**; create 계열: create 직전 crash → lease 만료 후 'create_unconfirmed' + create 재호출 0; vendor 2xx 직후·taskId CAS 전 crash → 동일; taskId write 직후 crash → polling 재개 + create 0; **generation 불일치 → no-op + 외부 0; sync succeeded (B4-02) → 'vendor_error' + polling 진입 0**; moderation blocked → retry_ready(taskId None·attempt 0) + nextAction='create' → 재 create 로 재생성 1회(generation+1) + 2회째 blocked → 'moderation'; malformed → batchItemFailures; iam_probe → 외부 0 + batchItemFailures 미포함; send 실패 주입 → 예외 미전파 + outbox pending 잔존; **직렬화 (H3-01+M3-04): 전 시나리오 종료 후 job/analysis 문서 json.dumps 에 'http://'/'https://'/'outputUrl'/'signedUrl' 부재 + SQS body(= {jobId,generation,action,outboxSeq} 만) 동일 부재 + caplog log record 부재 assert**.</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31/test_visual_worker.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - 외부 side-effect action 이 claim_visual_job_action 선행 — already_claimed/stale no-op + 외부 1회 테스트 green (B4-01)
    - 모든 전이가 transition_visual_job(expect_outbox_seq) 경유 — done/failed transition 0 (finalize 만)
    - crash-injection: CAS 직후 crash → dispatcher 다음 action 발행 + terminal + create 중복 0 (B4-01)
    - sync succeeded → 'vendor_error' 종결 테스트 green (B4-02)
    - moderation retry → taskId None·attempt 0·generation+1 재생성 테스트 green (B4-03)
    - 직렬화 http/https/outputUrl/signedUrl 부재 3면 assert green (H3-01/M3-04)
    - requirements.txt pyyaml 주석 2건 + google-genai 부재
  </acceptance_criteria>
  <done>고아·중복 과금·구세대/구-outbox 간섭·moderation stale taskId 가 불가능한 action state machine 골격 완성.</done>
</task>

<task type="auto">
  <name>Task 2: correctedPose fetch/judge/pose_check + postprocessing — JPEG→PNG 정규화 + display/training env + durable pair/cleanup 후 finalize</name>
  <files>backend/functions/visual-worker/app.py, backend/tests/phase31/test_visual_worker.py</files>
  <read_first>backend/shared/python/sunity_shared/analysis/visual_gen.py (WanImageAdapter.create_task/poll, judge_corrected_pose, JudgeInputTooLargeError, safe_decode_image, download_vendor_asset — 31-05), backend/shared/python/sunity_shared/analysis/pose_gate.py (measure_generated_pose/derive_pose_url — 31-06), backend/shared/python/sunity_shared/analysis/pair_store.py (store_training_pair commit-marker + BLUR_OPTION — 31-07), .planning/phases/31-api-visual-correction/smoke/CALIBRATION.json (31-13 채택 임계값), .planning/phases/31-api-visual-correction/31-PLAN-REVIEW-ITERATION4.md 의 H4-01/H4-03/H4-05/B4-05/M4-02 절</read_first>
  <action>correctedPose 핸들러 4개 (fetch/judge/pose_check/postprocess — 전부 claim 선행):
(1) `_action_fetch_corrected(job)`: expect fetching — **taskId 재-poll (H3-01): adapter.poll(taskId) fresh URL (succeeded 아니면 상태별: pending→poll backoff, failed/blocked→finalize typed)** → srcKey equality 검증 (f"visual-input/{uid}/{analysis_id}/{payload sourceHash[:16]}.png" — 불일치 'invalid_output') + S3 head → download_vendor_asset(fresh_url, /tmp, max_bytes=20MB, allowed_content_types=('image/png','image/jpeg')) → **JPEG→PNG 정규화 (H4-05/H4-06): safe_decode_image 로 열어 format/dimension 검증 + EXIF 제거 + 실제 PNG 재인코딩 → staging 은 PNG bytes 만, sha256 은 정규화 PNG 기준** → staging `visual-input/{uid}/{analysisId}/{sourceHash}_after.png` upload_file(ContentType image/png) → **upload 후 head_object 로 stagingVersionId 확보 (H4-03 — versioned cleanup 대상)** → _advance(judging, {stagingKey, afterHash(정규화 PNG), stagingVersionId}, 'judge'). URL 은 로컬 변수만.
(2) `_action_judge(job)`: expect judging — S3 get before(srcKey, sha256 == sourceHash 재검증 — 불일치 'invalid_output')/after(stagingKey) → **judge_corrected_pose(before_png, after_png, context={joint, targetDeg} — CorrectedPoseTarget provenance) 를 단일 호출; JudgeInputTooLargeError 만 catch → finalize 'judge_input_too_large' (M4-02 — worker 는 prepare 선호출 안 함; H3-03 원본 canonical 불변)**; None → finalize 'judge_failed'; **display = judge_display_pass(verdict, min_confidence=float(env DISPLAY_JUDGE_CONFIDENCE)); training = judge_training_pass(verdict, min_confidence=float(env TRAINING_JUDGE_CONFIDENCE)) — 각각 별개 env (B4-05)**; display 실패 → finalize 'judge_failed'(판정 메타); display 통과 → _advance(pose_checking, {judgeDisplayPass:True, judgeTrainingPass, judgeConfidence}, 'pose_check').
(3) `_action_pose_check(job)`: expect pose_checking — S3 get after(정규화 PNG) → measure_generated_pose(joint_key, target_deg, tolerance=float(env DISPLAY_POSE_TOL_DEG), pose_url=derive_pose_url(env RUNPOD_ANALYZE_URL), token=env RUNPOD_AUTH_TOKEN) — passed False → finalize typed('pose_gate_failed'/'pose_gate_unavailable') + 메타; 통과 → **canonical put 먼저 (B3-02): S3 copy staging→canonical `results/{uid}/{analysis_id}/corrected_pose_{joint}.png` (immutable, ContentType image/png, PNG bytes) → head_object 로 canonicalVersionId** → transition pose_checking→**postprocessing** {canonicalKey, poseGatePassed, poseGateErrorDeg, poseGateReason, judge 메타, pairEligible = (learningOptIn AND judgeTrainingPass AND poseError<=float(env TRAINING_POSE_TOL_DEG))} + next_action='postprocess' (H4-01).
(4) **`_action_postprocess(job)` (H4-01 durable): expect postprocessing — claim 선행. (a) pairEligible True 면 pair_store.store_training_pair (commit-marker — before 바이트 = BLUR_OPTION 'pod_blur' 면 trainingSrcKey, 아니면 srcKey; 적재 실패 = 제한 retry 후 pairStoreStatus='failed' 메타 + metric — 제품 상태 미차단); (b) exact-version temp cleanup: srcKey/stagingKey(+trainingSrcKey) 를 payload 의 VersionId(sourceVersionId/stagingVersionId) 지정 delete_object (H4-03 — versioned bucket noncurrent 잔존 0; VersionId 없으면 일반 delete + log); (c) finalize_visual_job(done, meta={pairStoreStatus, poseGate/judge 메타}, display_status='done', key=canonicalKey, joint) — job+analysis 원자. 재전달(crash 재개)이면 (a)(b)(c) 전부 idempotent (pair commit-marker 존재 시 skip, cleanup 이미 삭제분 무해, finalize 'stale' no-op).**
실패 종결의 cleanup (H4-01 durable): correctedPose 실패 finalize 경로(fetch/judge/pose_check 실패, create_unconfirmed 포함)는 finalize 시 srcKey/stagingKey 를 VersionId 지정 best-effort delete + versioned lifecycle(NoncurrentVersionExpiration — 31-10) 이중 방어. (create/poll 단계 실패는 stagingKey 미생성 — srcKey 만 대상.)
판정 메타 전부 flat scalar. 테스트: PASS 체인 (fetch→judge→pose_check→postprocess invocation 분리) → done + canonical key + 판정 메타 + src/staging versionId 삭제 + 페어 적재(동의 시) + **job/analysis 항상 함께 done (finalize 원자)**; **H4-01: canonical copy 성공 후 postprocess 전 crash → 재전달 postprocess 재개 (pair/cleanup/finalize idempotent) 두 문서 일치; postprocess 중 pair store 후·finalize 전 crash → 재전달이 pair skip + finalize; finalize commit_lost → 재전달 'stale' no-op 두 문서 일치**; **H4-05: JPEG 벤더 출력 → staging/canonical 이 PNG magic bytes + ContentType image/png (S3 put 인자 assert) + sha256 정규화 PNG 기준**; **B4-05: DISPLAY_JUDGE_CONFIDENCE 통과·TRAINING_JUDGE_CONFIDENCE 미달 → done + 페어 0 (training 별개 env 소비 assert)**; judge fail → 'judge_failed' + poseGate 메타 부재 + 페어 0 + srcKey versionId cleanup; oversized/bomb before/after → 'judge_input_too_large' + Gemini POST 0; pose_gate_unavailable → typed + 미노출; learningOptIn False → done + 페어 0; **H4-03: cleanup 이 delete_object 에 VersionId 인자 전달 (versioned 잔존 방지) assert**; srcKey/hash 불일치 → 'invalid_output'; fetch 가 재-poll fresh URL (소스 grep 'outputUrl' 저장/판독 0).</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31/test_visual_worker.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - fetch 가 taskId 재-poll + JPEG→PNG 재인코딩(safe_decode) — staging/canonical PNG bytes + ContentType image/png (H4-05/H3-01)
    - postprocessing durable: canonical 후 crash / pair 후 crash / commit_lost 각각 재개 두 문서 일치 (H4-01)
    - judge 가 judge_corrected_pose 단일 호출 + JudgeInputTooLargeError 만 catch (M4-02)
    - display/training 이 DISPLAY_JUDGE_CONFIDENCE/TRAINING_JUDGE_CONFIDENCE + DISPLAY_POSE_TOL_DEG/TRAINING_POSE_TOL_DEG 4 env 분리 소비 (B4-05)
    - temp cleanup 이 VersionId 지정 삭제 (H4-03)
    - 모든 종결 경로 임시 객체 삭제 + 판정 메타 flat scalar
  </acceptance_criteria>
  <done>D-05 자동 교정이 URL 비저장·PNG 정규화·durable postprocess·versioned cleanup·display/training 분리·동의 페어까지 테스트로 고정됨.</done>
</task>

<task type="auto">
  <name>Task 3: rotation fetch + dispatcher Lambda(pagination/age) + 운영자 스크립트(reconciler)</name>
  <files>backend/functions/visual-worker/app.py, backend/functions/visual-dispatch/app.py, backend/functions/visual-dispatch/requirements.txt, backend/scripts/list_stuck_visual_jobs.py, backend/tests/phase31/test_visual_worker.py, backend/tests/phase31/test_visual_dispatcher.py</files>
  <read_first>backend/shared/python/sunity_shared/s3keys.py (build_upload_key — canonical upload key 단일 출처), .planning/spikes/004-gemini-omni-view-editing/wan_gate_batch.py (회전 PROMPT 원문 — 카메라 orbit 이식), backend/shared/python/sunity_shared/firestore_admin.py (list_dispatch_pending 반환 스키마 {items,backlog_max_age_ms,truncated} / mark_visual_job_dispatched(action+seq) / update_analysis_visual)</read_first>
  <action>(1) `_action_create` rotation 분기: myVideoKey 를 s3keys.build_upload_key 후보(mp4/mov) equality 검증 — 불일치 finalize 'invalid_output' (H-05), presign 1h → WanVideoEditAdapter.create_task(video_url, PROMPT 상수); **sync 반환 시 async-only 이므로 'vendor_error' (B4-02, rotation 도 async)**. (2) `_action_fetch_rotation(job)`: claim → expect fetching — **taskId 재-poll fresh URL (H3-01)** → download_vendor_asset(fresh_url, /tmp, max_bytes=200MB, allowed_content_types=('video/mp4',)) — /tmp streaming + boto3 upload_file(multipart) canonical `results/{uid}/{analysis_id}/rotation.mp4` (전체 bytes 메모리 적재 0 — H2-05) → **finalize_visual_job(done, display_status='done', key) — job+analysis 원자 (rotation 은 temp 입력·pair 없음 → postprocessing 미경유)**.
(3) **visual-dispatch/app.py 신설 (H3-09 + H4-08)**: lambda_handler(EventBridge) — `res = list_dispatch_pending(now_ms, limit=20)` → `res["items"]` 각 항목 `sqs.send_message(env VISUAL_QUEUE_URL, {"jobId","generation","action": nextAction,"outboxSeq"})` → `mark_visual_job_dispatched(expect_action=nextAction, expect_outbox_seq=outboxSeq)`. **모든 nonterminal pending 발행 (B3-01)**; creating 항목은 결과에 없음(outbox 미기록)이 정상 → 방어적 skip. **pending age metric (H4-08): res["backlog_max_age_ms"] (slice 아닌 실 backlog) 를 cloudwatch put_metric_data(Namespace 'SunityVisual', MetricName 'OutboxPendingAgeMs') 방출 (0건이면 0); res["truncated"] True 면 별도 metric 'OutboxScanTruncated'=1 방출**. 로그 key=value. requirements.txt = firebase-admin + pyyaml (deploy fix 주석 2건).
(4) list_stuck_visual_jobs.py: argparse --profile 기본 sunity-motion, --stale-minutes 기본 30, --redispatch, **--reconcile (B3-02 보조)**: (a) 조회 — nonterminal stale 표(jobId/kind/state/dispatchState/nextAction/outboxSeq/attempt/failureReason/경과분); (b) --redispatch — pending 항목 SQS 재발행(action+outboxSeq 포함); (c) --reconcile — terminal job(done|failed)과 analysis 표시 불일치 → update_analysis_visual repair (이 스크립트가 유일 정당 호출자 — 31-02); (d) 'create_unconfirmed' 항목은 별도 섹션 "수동 판정 필요 — 중복 과금 회피 (DashScope client tag 미지원, '정확한 복구' 아님)" (M3-07).
테스트 — test_visual_worker.py: rotation 성공 → 재-poll + upload_file(put_object bytes 아님) + finalize 원자 + URL 미저장 + postprocessing 미경유; myVideoKey 불일치 → 'invalid_output' + create 0; rotation sync → 'vendor_error'. test_visual_dispatcher.py: pending 항목(polling/fetching/retry_ready 혼합) 전부 발행 + 각 action==nextAction + outboxSeq 포함 + mark(action+seq CAS); sent/terminal 미발행; **B4-01 종합: 워커 CAS 직후 crash 로 남은 pending 을 dispatcher 발행 → 체인 재개 → terminal (worker 핸들러 연동)**; backlog_max_age_ms metric 방출 mock 검증 + truncated metric; 상한 20건.</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31/test_visual_worker.py backend/tests/phase31/test_visual_dispatcher.py -x -q && python -m pytest backend/tests -q</automated>
  </verify>
  <acceptance_criteria>
    - rotation fetch 가 재-poll + upload_file 기반 (put_object(Body=bytes) 부재) + postprocessing 미경유 (H2-05/H3-01)
    - visual-dispatch/app.py 가 모든 nonterminal pending 발행 + 실 backlog age + truncated metric (H3-09/H4-08)
    - dispatcher 연동 (crash → dispatcher → terminal) green (B4-01)
    - list_stuck_visual_jobs.py 에 --reconcile + M3-07 문구 존재
    - `python -m pytest backend/tests -q` exits 0
  </acceptance_criteria>
  <done>D-06 회전 잡 + 독립 dispatcher(pagination age) 복구 + reconciler + 수동 판정 가시성 완결.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQS 메시지 → 워커 외부 side-effect | claim_visual_job_action 통과분만 — 위조/중복/stale/구세대/구-outbox/malformed 방어 |
| 사용자 객체 → 벤더 | canonical/hash key equality 통과분만 |
| 벤더 응답 → S3 | taskId 재-poll fresh URL + download 스트리밍 + JPEG→PNG 정규화 통과분만 |
| worker 결과 → analysis 표시 | finalize_visual_job 단일 transaction (postprocessing durable 경유) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-32 | Denial of Service | 전이 후 발행 유실 고아 | mitigate | 전이=원자 outbox(seq) + dispatcher 독립 복구 + crash 기대값 terminal (B3-01/B4-01) |
| T-31-33 | Tampering | 중복/stale/구세대/구-outbox 메시지 재과금 | mitigate | claim + creating lease + taskId journal + CAS + generation + outboxSeq (B2-02/B3-01/B4-01) |
| T-31-34 | Information Disclosure | 타 사용자 객체 벤더 전송 | mitigate | upload/hash key equality (H-05 + B2-03) |
| T-31-35 | Information Disclosure | 원본 프레임/서명 URL 잔존 | mitigate | postprocess versionId cleanup + taskId 만 + 직렬화 URL 부재 (H-04/H3-01/H4-03) |
| T-31-36 | Tampering | 저품질/포맷불일치 생성물 노출·적재 | mitigate | before/after judge(safe_decode+16MB) + pose 게이트 + JPEG→PNG 정규화 + calibration 임계값 (H-03/H3-03/H3-04/H4-05) |
| T-31-37 | Repudiation | generic 실패/조용 삭제/영구 불일치/pair 유실 | mitigate | typed 10종 + malformed→DLQ + finalize 원자 + postprocessing durable + reconciler (M2-02/B3-02/H4-01) |
| T-31-58 | Denial of Service | 복구 경로 concurrency 굶김 | mitigate | dispatcher 별도 Lambda concurrency 1 + 실 backlog age metric/alarm (H3-09/H4-08) |
| T-31-62 | Denial of Service (과금) | standard SQS 중복으로 유료 action 중복 | mitigate | claim_visual_job_action already_claimed no-op (B4-01) |
</threat_model>

<verification>
- `python -m pytest backend/tests -q` green
- 워커/디스패처 모듈에 requests·키 리터럴·fail_analysis·update_analysis_visual·google.genai·시간 폴링 루프·outputUrl 기록 0
- requirements.txt 2종 pyyaml 존재 (deploy fix 선례 2건 인용)
</verification>

<success_criteria>
- B4-01(claim + outboxSeq + crash 기대값 terminal·create 중복 0) + B4-02(async-only) + B4-03(moderation stale taskId clear) + H4-01(postprocessing durable) + H4-03(versioned cleanup) + H4-05(JPEG→PNG) + B4-05(4 env 분리) + H3-01(URL 비저장) 전부 테스트/텍스트 고정
- 4차 리뷰 §8 승인 조건 1·2·3·4·5·6·8·11 이 이 플랜 테스트로 추적 가능
</success_criteria>

<output>
Create `.planning/phases/31-api-visual-correction/31-09-SUMMARY.md` when done
</output>
</content>
</invoke>
