---
phase: 31-api-visual-correction
plan: 09
type: execute
wave: 4
depends_on: [31-02, 31-05, 31-06, 31-07, 31-13]
files_modified:
  - backend/functions/visual-worker/app.py
  - backend/functions/visual-worker/requirements.txt
  - backend/functions/visual-dispatch/app.py
  - backend/functions/visual-dispatch/requirements.txt
  - backend/scripts/list_stuck_visual_jobs.py
  - backend/tests/phase31/test_visual_worker.py
  - backend/tests/phase31/test_visual_dispatcher.py
autonomous: true
requirements: [D-03, D-04, D-05, D-06, D-08]
must_haves:
  truths:
    - "worker 는 state action 1개만 수행하고 durable next action(nextAction+outboxSeq)을 transaction 에 남긴다 — SQS send 는 best-effort, 복구는 dispatcher (3차 B3-01 + 4차 B4-01)"
    - "메시지 = {jobId, generation, action, outboxSeq}. worker 는 외부 side-effect 전에 claim_visual_job_action 으로 claim 하고 4상태로 분기한다 — claimed 만 실행; stale/completed 는 외부 0 + 정상 ACK; busy(same-seq active lease)는 정상 ACK 금지 → batchItemFailures 로 SQS visibility 뒤 재전달 (5차 리뷰 B5-01)"
    - "각 CAS commit 직후 crash (polling/fetching/judging/pose_checking/postprocessing) 에서 dispatcher 가 lease 만료 후 정확한 다음 action 을 1회 이상 발행하고 외부 create 중복 0 으로 terminal 에 도달한다 — '재전달 no-op 멱등' 은 PASS 기준이 아니다 (B3-01 + 5차 B5-01)"
    - "action 성공 transition/finalize 는 owner+outboxSeq CAS(expect_claim_owner) 를 거친다 — lease 를 잃고 재claim 당한 늦은 worker 의 결과 write 는 새 owner 진행을 덮지 못한다 (5차 리뷰 B5-01)"
    - "correctedPose 는 성공/실패 모든 terminal intent 가 pose_checking 통과 여부와 무관하게 pose_checking(또는 실패 직전 state)→postprocessing 을 거친다 — job 에 pendingTerminalState/pendingFailureReason 를 실어 postprocess action 이 exact-prefix version cleanup(+동의 시 pair store)을 durable 수행한 뒤 한 번 finalize 한다. 실패 경로도 durable cleanup 을 우회하지 않는다 (4차 H4-01 + 5차 H5-05)"
    - "postprocess 는 S3 PUT 직전 analysis 의 현재 learningOptIn is True + consentVersion + capturedAt/revokedAt 를 재read 한다 — 철회면 PUT 0 + pairStoreStatus='skipped_consent' (5차 리뷰 H5-01)"
    - "pairId/pairHmacKeyVersion 은 pose_checking→postprocessing transition 에서 1회 계산해 job 에 고정 — postprocess 재시도가 active key 를 다시 선택하지 않는다 (5차 리뷰 B5-03)"
    - "postprocess pair/cleanup 재시도는 postprocessing→postprocessing self-loop(pairAttempt/cleanupAttempt + nextDispatchAtMs + 새 outboxSeq)로 durable 하다 — cleanup 은 개인정보 삭제라 비차단 취급 금지: 성공(remainingVersionCount==0) 또는 명시적 privacy blocker(cleanup_blocked) 전까지 terminal finalize 금지. pair 는 max 초과 후에만 pairStoreStatus='failed' done 허용 (5차 리뷰 H5-02)"
    - "vendor create 는 async taskId 경로만 정상 처리한다 — create_task 가 sync succeeded 를 반환하면 typed 실패로 종결(v1 async-only, B4-02). rotation 도 async"
    - "moderation retry 는 transition 이 polling→retry_ready 에서 taskId=None·attempt=0·lease/failure clear + retryCount+1 + nextAction='create' 을 원자 기록하고, retry_ready→creating 이 generation+1 로 승격하며 requestKey 를 그 새 generation 으로 transaction 안에서 생성한다 — crash 시 과거 blocked task 재개 0, requestKey suffix==persisted generation (4차 B4-03 + 5차 H5-03)"
    - "_action_create 의 모든 후속 경로(taskId 성공/sync typed 실패/vendor typed 실패 finalize-intent)는 첫 transition 이 반환한 갱신 job snapshot 하나만 사용한다 — inbound msg 값을 재사용하지 않아 creating 잔존 0 (5차 리뷰 H5-04)"
    - "job 문서에 outputUrl 을 저장하지 않는다 — taskId 만. fetch invocation 이 taskId 재-poll 로 fresh URL 을 얻어 같은 invocation 에서 즉시 streaming download 한다 (3차 리뷰 H3-01)"
    - "다운로드된 correctedPose 이미지는 safe_decode_image + 실제 PNG 재인코딩(EXIF 제거)으로 정규화된다 — staging key 는 deterministic + HEAD/full-hash reuse 라 재실행이 같은 normalized PNG 로 새 version 을 만들지 않는다. decode 오류는 size/pixel 초과=judge_input_too_large, corrupt/unsupported/mismatch=invalid_output 로 분리 (4차 H4-05/H4-06 + 5차 B5-02/M5-01)"
    - "temp 입력 cleanup 은 visual-input/{uid}/{analysisId}/ exact prefix 의 모든 Versions+DeleteMarkers 를 page 끝까지 열거·삭제해 job 단일 VersionId 미신뢰로도 versioned noncurrent 잔존 0 을 보장한다 (4차 H4-03 + 5차 B5-02)"
    - "dispatcher 는 별도 소형 Lambda(reserved concurrency 1) — dispatchState='pending' 인 모든 nonterminal 발행 + dispatchState='sent'+lease 만료+nonterminal 복구 재발행 + 실 backlog pending age metric + truncated metric (3차 H3-09 + 4차 H4-08 + 5차 B5-01/H5-06)"
    - "worker 는 action=iam_probe 를 외부 side-effect 없이 소비한다 — 31-12 IAM canary send-only 전용 (3차 H3-10 + 4차 H4-02)"
  artifacts:
    - path: "backend/functions/visual-worker/app.py"
      provides: "action 단위 state machine 워커 (create/poll/fetch/judge/pose_check/postprocess + iam_probe) — claim 4상태 + owner-CAS transaction outbox + all-terminal-via-postprocessing + exact-prefix version cleanup 소비"
      contains: "claim_visual_job_action"
    - path: "backend/functions/visual-dispatch/app.py"
      provides: "dispatcher Lambda — list_dispatch_pending 발행(outboxSeq 포함, pending+lease-expired) + dispatched CAS(action+seq) + 실 backlog age/truncated metric"
      contains: "list_dispatch_pending"
    - path: "backend/scripts/list_stuck_visual_jobs.py"
      provides: "운영자용 stuck 조회 + job/analysis terminal reconciler + create_unconfirmed 수동 판정 목록"
      contains: "reconcile"
    - path: "backend/tests/phase31/test_visual_worker.py"
      provides: "claim 4상태(busy batchItemFailures)·crash-injection(전이 5지점 + create 계열 + postprocess durable)·owner CAS·consent race·all-terminal-postprocessing·exact-prefix cleanup·JPEG→PNG·decode split·typed 실패 테스트"
      min_lines: 280
  key_links:
    - from: "backend/functions/visual-worker/app.py"
      to: "backend/shared/python/sunity_shared/firestore_admin.py"
      via: "claim_visual_job_action(4상태) + transition_visual_job(snapshot+owner CAS) + finalize_visual_job(owner CAS)"
      pattern: "finalize_visual_job"
    - from: "backend/functions/visual-dispatch/app.py"
      to: "backend/shared/python/sunity_shared/firestore_admin.py"
      via: "list_dispatch_pending + mark_visual_job_dispatched"
      pattern: "mark_visual_job_dispatched"
    - from: "backend/functions/visual-worker/app.py"
      to: "backend/shared/python/sunity_shared/analysis/visual_gen.py"
      via: "create_task/poll + download_vendor_asset + safe_decode_image + judge_corrected_pose"
      pattern: "visual_gen"
---

<objective>
5차 리뷰 §7 실행 허용 조건을 구현한다: worker/dispatcher 분리 + claim 4상태 + owner CAS + 모든 terminal intent 의 postprocessing 경유 + exact-prefix cleanup + durable pair/cleanup retry + consent 재확인.

- **worker (VisualWorkerFunction):** 메시지 {jobId, generation, action, outboxSeq} 수신 → **외부 side-effect 전에 `claim_visual_job_action` 으로 claim(4상태) — claimed 만 실행; stale/completed → 외부 0 + 정상 ACK; busy → 정상 ACK 금지 → batchItemFailures(SQS visibility 뒤 재전달 — B5-01)** → 자기 외부 side-effect 1개 → `transition_visual_job(expect_outbox_seq=메시지 seq, expect_claim_owner=owner)` 가 다음 state + nextAction + dispatchState='pending' + 새 outboxSeq 를 **한 transaction** 기록하고 **갱신 snapshot 반환** → best-effort `sqs.send_message(...)` + `mark_visual_job_dispatched(...)`. send/mark 실패는 삼키고 log — durable outbox 를 dispatcher 가 복구.
- **dispatcher (VisualDispatchFunction — H3-09 + B5-01):** 별도 Lambda(reserved concurrency 1, EventBridge rate(1분)). `list_dispatch_pending` 으로 pending nonterminal + **sent+lease-만료+nonterminal 복구 대상**을 조회해 정확한 nextAction 발행 + `mark_visual_job_dispatched` + 실 backlog age/truncated metric.
- **종결 — correctedPose 는 성공/실패 전부 postprocessing 경유 (H4-01 + H5-05):** 각 실패는 finalize 를 직접 하지 않고 job 에 `pendingTerminalState`/`pendingFailureReason` 을 실은 뒤 pose_checking(또는 실패 직전 state)→postprocessing 전이. postprocess action 이 exact-prefix version cleanup(+동의+eligible 시 pair store)을 durable 수행 후 한 번의 finalize. rotation 은 fetching→finalize(임시 입력·pair 없음).
- **URL 비저장 (H3-01):** poll succeeded 시 outputUrl 미기록. fetch 가 taskId 재-poll 로 fresh URL 획득 후 같은 invocation streaming download.
- **async-only (B4-02):** create_task 가 sync succeeded 를 반환하면 typed 실패로 종결.
- **moderation retry (B4-03 + H5-03):** transition 이 polling→retry_ready clear + retry_ready→creating generation+1 승격 + requestKey 새 generation 생성.

judge 입력은 31-05 `judge_corrected_pose`(내부 prepare_judge_payload safe_decode+16MB — M4-02), pose 게이트 입력은 31-06 정규화(H3-04). display/training 임계값·tolerance 는 31-13 CALIBRATION 채택값을 env 로 소비 (DISPLAY_JUDGE_CONFIDENCE / TRAINING_JUDGE_CONFIDENCE / DISPLAY_POSE_TOL_DEG / TRAINING_POSE_TOL_DEG — B4-05).

Purpose: 유료 잡이 고아도 중복 과금도 영구 불일치도 pair/cleanup 유실도 versioned PII 잔존도 될 수 없는 비동기 실행기.
Output: visual-worker Lambda + visual-dispatch Lambda + 운영자 스크립트(reconciler) + crash/claim/consent/cleanup 테스트.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-api-visual-correction/31-PATTERNS.md
@.planning/phases/31-api-visual-correction/31-ACCEPTANCE.md
@.planning/phases/31-api-visual-correction/31-PLAN-REVIEW-ITERATION5.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 워커 골격 — action state machine + claim 4상태 + owner-CAS outbox(snapshot) + creating lease + busy batchItemFailures</name>
  <files>backend/functions/visual-worker/app.py, backend/functions/visual-worker/requirements.txt, backend/tests/phase31/test_visual_worker.py</files>
  <read_first>backend/functions/pipeline/app.py lines 4816-4867 (SQS 레코드 루프) + lines 1258-1263 (_signed_get), backend/functions/pipeline/requirements.txt + backend/functions/playback-url/requirements.txt (pyyaml deploy fix 주석 2건 원문), backend/shared/python/sunity_shared/firestore_admin.py (31-02 함수 7종 — claim_visual_job_action(4상태) / transition(snapshot+expect_outbox_seq+expect_claim_owner) / finalize / mark(action+seq)), backend/shared/python/sunity_shared/models.py (VISUAL_JOB_STATES 10종 + VISUAL_CLAIM_LEASE_MS + pending-terminal 필드), .planning/phases/31-api-visual-correction/31-PLAN-REVIEW-ITERATION5.md 의 B5-01/B4-02/B4-03/H5-04 절</read_first>
  <action>visual-worker/app.py 신설 (docstring: 5차 B5-01/H5-04 + B4-02/B4-03/H4-01/H5-05 불변식 — "외부 side-effect 당 invocation 1개, claim 4상태 통과분(claimed)만 외부 호출, 모든 전이 = transaction 원자 outbox+snapshot 반환, owner CAS, send best-effort, correctedPose terminal = 전부 postprocessing 경유 finalize, 복구 = dispatcher"). lambda_handler: SQS Records 루프 — 각 레코드 try: 파싱 {jobId, generation, action, outboxSeq} — 파싱/스키마 위반 log.error + batchItemFailures (M2-02); **action=='iam_probe' → log.info 후 정상 소비(외부 0 — H4-02)**; `_handle_job(...)` — **반환값이 'busy' 면 batchItemFailures 에 넣어 SQS visibility 뒤 재전달 (B5-01 — 정상 ACK 금지)**; except → log.exception + batchItemFailures. 반환 {"batchItemFailures":[...]}.
**claim 규율 (B5-01 4상태):** 외부 side-effect 가 있는 action(poll/fetch/judge/pose_check/postprocess)은 side-effect **전** `claim_visual_job_action(job_id, generation=msg.generation, action=msg.action, outbox_seq=msg.outboxSeq, owner=aws_request_id, lease_ms=models.VISUAL_CLAIM_LEASE_MS, now_ms=now)` 호출 — 반환: 'claimed' → 실행; 'stale'/'completed' → 외부 0 + 정상 소비(재발행 불필요); **'busy' → 외부 0 + `_handle_job` 이 'busy' 반환 → handler 가 batchItemFailures**. create 는 claim 미사용(creating lease 특례).
공통 헬퍼 `_advance(job, next_state, updates, next_action, delay_s=0) -> dict|None`: `snap = transition_visual_job(원자 outbox, expect_generation=job.generation, expect_outbox_seq=job.outboxSeq, expect_claim_owner=job.get('claimOwner'))` → **snap(갱신 job)이 있으면 best-effort `sqs.send_message({jobId, generation: snap.generation, action: next_action, outboxSeq: snap.outboxSeq}, DelaySeconds=delay_s)` + mark_visual_job_dispatched(expect_action=next_action, expect_outbox_seq=snap.outboxSeq) — send/mark 실패 삼키고 log.warning**; snap None(CAS 실패=lease 상실/구세대) → log.warning + 실행 종료(재발행 불필요). **snap 반환 → 반환 (H5-04: 후속 2전이는 이 snap 을 job 으로 사용)**. **⚠ invocation 내 2전이 (checker): `_action_create` 처럼 (reserved/retry_ready→creating) 후 (creating→polling) 2전이는 반드시 첫 `_advance` 가 반환한 snap 을 두 번째 `_advance`/finalize 의 `job` 으로 사용 — msg.* 재사용 금지(H5-04). 첫 claim 은 msg.outboxSeq 로 1회만.**
action 핸들러:
- `_action_create(job)`: state 'creating' 이면 lease 검사 — leaseExpiresAt 미래 = no-op, 과거+taskId None = **_finalize_correctedpose_intent('create_unconfirmed') (자동 재생성 금지 — B2-02; 문구 "중복 과금 회피 수동 판정")**, 과거+taskId 존재 = polling 재개. state ∈ ('reserved','retry_ready') → **snap = _advance(creating, {leaseOwner, leaseExpiresAt now+VISUAL_CLAIM_LEASE_MS}, None)** (requestKey 는 transition 이 새 generation 으로 생성 — H5-03; retry_ready→creating 은 31-02 validator 가 taskId None 강제 + generation+1) → kind 어댑터 create_task(requestKey=snap.requestKey) 1회 → VendorTaskCreated → _advance(snap, polling, {taskId}, 'poll', delay_s=60); **VendorPollResult succeeded (sync) → v1 async-only 이므로 _finalize_correctedpose_intent('vendor_error')(async taskId 부재, snap 기준 — H5-04) + log("sync image production 미허용 — B4-02")**; typed 실패 → _finalize_correctedpose_intent(snap 기준).
- `_action_poll(job)`: claim → expect polling — 1회 poll: succeeded → _advance(fetching, {}, 'fetch') (URL 미기록 — H3-01); pending → attempt+1 >= MAX_POLLS(20) 면 _finalize_correctedpose_intent('timeout'), 아니면 transition polling→polling {attempt+1} + next_action='poll' + next_dispatch_at_ms now+60s (새 outboxSeq 로 old poll message 무력화 — B4-01) + best-effort send; failed → _finalize_correctedpose_intent('vendor_error'); blocked → retryCount < MODERATION_RETRY_MAX 면 **transition polling→retry_ready (31-02 가 clear + generation+1 준비 — B4-03)** + best-effort send, 아니면 _finalize_correctedpose_intent('moderation').
- **`_finalize_correctedpose_intent(job, reason)` (H5-05): correctedPose 실패는 finalize 를 직접 하지 않는다 — transition (현재 state)→postprocessing {pendingTerminalState:'failed', pendingFailureReason: reason, 실패 메타} + next_action='postprocess' → postprocess 가 exact-prefix cleanup 후 한 번 finalize. (rotation 실패는 임시 입력·pair 없어 finalize_visual_job 직접 — 단 srcKey 미생성이라 cleanup 대상 없음.)**
- 종결 공통: **transition 으로 done/failed 금지 (31-02 validator ValueError). fail_analysis 절대 금지 (D-08) + update_analysis_visual 직접 호출 금지(reconciler 전용 — grep).**
requirements.txt = `firebase-admin>=6,<7` + `pyyaml>=6.0,<7.0` (주석에 두 선례 deploy fix 인용 원문). google-genai 금지. 로깅 key=value, 키·서명 URL 미로깅.
test_visual_worker.py — **claim 4상태 (B5-01): 같은 {action,outboxSeq} 메시지 2개 → 1개 'claimed'(외부 1회)·나머지 'busy' → batchItemFailures(정상 ACK 아님) — 외부 호출 총 1회; lease 만료 후 재전달 → 'claimed' 재실행(정확히 1 worker); job outboxSeq 초과 → 'completed' 정상 ACK; 구식 seq → 'stale' 정상 ACK no-op**; **owner CAS: 재claim 당한 늦은 worker 의 _advance snap None → 결과 write 0 (B5-01)**; **B5-01 crash: create→polling / poll→fetching CAS 직후·send 전 crash → (a) nextAction/dispatchState='pending'+새 outboxSeq 잔존, (b) dispatcher 가 lease 만료 후 발행 시 정확한 다음 action 1회+, (c) terminal 도달, (d) 외부 create 1회**; **H5-04: create 계열 재시도(retry generation)에서 vendor typed failure / sync result / taskId 성공 각각 첫 transition snapshot 사용 → creating 잔존 0**; create 직전 crash → lease 만료 후 'create_unconfirmed'(postprocessing 경유 finalize) + create 재호출 0; taskId write 직후 crash → polling 재개 + create 0; **generation 불일치 → no-op + 외부 0; sync succeeded (B4-02) → 'vendor_error'(postprocessing 경유) + polling 진입 0**; moderation blocked → retry_ready → 재 create 로 재생성 1회(generation+1, requestKey suffix==새 gen — H5-03) + 2회째 blocked → 'moderation'; malformed → batchItemFailures; iam_probe → 외부 0 + batchItemFailures 미포함; send 실패 주입 → 예외 미전파 + outbox pending 잔존; **직렬화 (H3-01+M3-04): 전 시나리오 종료 후 job/analysis 문서 json.dumps 에 'http://'/'https://'/'outputUrl'/'signedUrl' 부재 + SQS body 동일 부재 + caplog log record 부재 assert**.</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31/test_visual_worker.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - 외부 side-effect action 이 claim_visual_job_action 선행 — claimed 만 실행, busy → batchItemFailures, stale/completed 정상 ACK (B5-01)
    - _advance 가 transition snapshot 사용 + owner CAS(expect_claim_owner) + 2전이 시 snap 재사용(H5-04) 테스트 green
    - 모든 전이가 transition_visual_job 경유 — done/failed transition 0 (finalize 만), correctedPose 실패도 postprocessing 경유 (H5-05)
    - crash-injection: CAS 직후 crash → dispatcher lease 만료 후 다음 action 발행 + terminal + create 중복 0 (B5-01)
    - sync succeeded → 'vendor_error' 종결 테스트 green (B4-02)
    - moderation retry → generation+1 + requestKey 새 gen 테스트 green (B4-03/H5-03)
    - 직렬화 http/https/outputUrl/signedUrl 부재 3면 assert green (H3-01/M3-04)
    - requirements.txt pyyaml 주석 2건 + google-genai 부재
  </acceptance_criteria>
  <done>고아·중복 과금·구세대/구-outbox·lease 상실 write·moderation stale taskId 가 불가능한 claim-4상태 action state machine 골격 완성.</done>
</task>

<task type="auto">
  <name>Task 2: correctedPose fetch/judge/pose_check + postprocessing — JPEG→PNG(staging reuse) + display/training env + consent 재확인 + durable pair/exact-prefix cleanup 후 finalize</name>
  <files>backend/functions/visual-worker/app.py, backend/tests/phase31/test_visual_worker.py</files>
  <read_first>backend/shared/python/sunity_shared/analysis/visual_gen.py (WanImageAdapter.create_task/poll, judge_corrected_pose, JudgeInputTooLargeError, safe_decode_image, download_vendor_asset — 31-05), backend/shared/python/sunity_shared/analysis/pose_gate.py (measure_generated_pose/derive_pose_url — 31-06), backend/shared/python/sunity_shared/analysis/pair_store.py (store_training_pair caller-fixed pairId + BLUR_OPTION — 31-07), .planning/phases/31-api-visual-correction/smoke/CALIBRATION.json (31-13 채택 임계값), .planning/phases/31-api-visual-correction/31-PLAN-REVIEW-ITERATION5.md 의 B5-02/B5-03/H5-01/H5-02/H5-05/M5-01/M5-03 절</read_first>
  <action>모듈 상단 1회 `from PIL import Image; Image.MAX_IMAGE_PIXELS = <상한>` 고정 — request 마다 전역 변경 금지, concurrent decode 안전 (M5-03). correctedPose 핸들러 (fetch/judge/pose_check/postprocess — 전부 claim 선행):
(1) `_action_fetch_corrected(job)`: expect fetching — **taskId 재-poll (H3-01): adapter.poll(taskId) fresh URL (succeeded 아니면 상태별: pending→poll backoff, failed/blocked→_finalize_correctedpose_intent)** → srcKey equality 검증 (f"visual-input/{uid}/{analysis_id}/{payload sourceHash[:16]}.png" — 불일치 'invalid_output') + S3 head → download_vendor_asset(fresh_url, /tmp, max_bytes=20MB, allowed_content_types=('image/png','image/jpeg')) → **JPEG→PNG 정규화 (H4-05/H4-06 + M5-01): safe_decode_image 로 열어 format/dimension 검증 + EXIF 제거 + 실제 PNG 재인코딩; decode 오류 분리 — size/pixel 초과 → 'judge_input_too_large', corrupt/unsupported/format mismatch → 'invalid_output' (reason 별 log/metric)** → sha256 은 정규화 PNG 기준 afterHash → **staging key deterministic `visual-input/{uid}/{analysisId}/{sourceHash}_after.png` — B5-02: head_object 로 존재 확인, 존재+Metadata full sha256 == afterHash 면 재사용(재PUT 0), 아니면 put_object(ContentType image/png, Metadata={'sha256': afterHash})** → head_object 로 stagingVersionId 확보 → _advance(judging, {stagingKey, afterHash, stagingVersionId}, 'judge'). URL 은 로컬 변수만.
(2) `_action_judge(job)`: expect judging — S3 get before(srcKey, sha256 == sourceHash 재검증 — 불일치 'invalid_output')/after(stagingKey) → **judge_corrected_pose(before_png, after_png, context={joint, targetDeg}) 를 단일 호출; JudgeInputTooLargeError 만 catch → _finalize_correctedpose_intent('judge_input_too_large') (M4-02); decode/format 오류 → 'invalid_output' (M5-01)**; None → _finalize_correctedpose_intent('judge_failed'); **display = judge_display_pass(verdict, min_confidence=float(env DISPLAY_JUDGE_CONFIDENCE)); training = judge_training_pass(verdict, min_confidence=float(env TRAINING_JUDGE_CONFIDENCE)) — 각각 별개 env (B4-05)**; display 실패 → _finalize_correctedpose_intent('judge_failed', 판정 메타); display 통과 → _advance(pose_checking, {judgeDisplayPass:True, judgeTrainingPass, judgeConfidence}, 'pose_check').
(3) `_action_pose_check(job)`: expect pose_checking — S3 get after(정규화 PNG) → measure_generated_pose(joint_key, target_deg, tolerance=float(env DISPLAY_POSE_TOL_DEG), pose_url=derive_pose_url(env RUNPOD_ANALYZE_URL), token=env RUNPOD_AUTH_TOKEN) — passed False → _finalize_correctedpose_intent(typed 'pose_gate_failed'/'pose_gate_unavailable', 메타); 통과 → **canonical put 먼저 (B3-02 + M5-04): canonical key `results/{uid}/{analysis_id}/corrected_pose_{joint}.png` head → 존재+Metadata sha256==afterHash 면 재사용(재copy 0), 아니면 S3 copy staging→canonical(immutable, ContentType image/png, PNG bytes) → head_object 로 canonicalVersionId** → **transition pose_checking→postprocessing {canonicalKey, poseGatePassed, poseGateErrorDeg, poseGateReason, judge 메타, pendingTerminalState:'done', pairEligible = (judgeTrainingPass AND poseError<=float(env TRAINING_POSE_TOL_DEG)), pairId + pairHmacKeyVersion = pair_store 로 계산해 job 고정(B5-03 — active key 를 여기서 1회 선택, postprocess 재선택 금지)} + next_action='postprocess' (H4-01/H5-05).**
(4) **`_action_postprocess(job)` (H4-01/H5-05 durable): expect postprocessing — claim 선행. (a) pairEligible True 이고 pendingTerminalState=='done' 이면: **S3 PUT 직전 analysis 재read — 현재 learningOptIn is True + consentVersion + capturedAt/revokedAt 확인 (H5-01); 철회 → pair 0 + pairStoreStatus='skipped_consent'**; 유효하면 pair_store.store_training_pair(pair_id=job.pairId, hmac_key_version=job.pairHmacKeyVersion, before 바이트 = BLUR_OPTION 'pod_blur' 면 trainingSrcKey 아니면 srcKey, after=canonical/staging PNG); 반환 'committed'→pairStoreStatus='done', 'conflict'/'failed'→pairAttempt+1 후 max(=3) 미만이면 transition postprocessing→postprocessing {pairAttempt+1} + next_action='postprocess' + nextDispatchAtMs backoff + best-effort send 후 return(재개 — H5-02), max 도달 시 pairStoreStatus='failed' 로 진행. (b) **exact-prefix version cleanup (B5-02): `_cleanup_visual_input_versions(s3, bucket, uid, analysis_id)` — list_object_versions 로 `visual-input/{uid}/{analysisId}/` exact prefix 의 Versions+DeleteMarkers 를 continuation 끝까지 열거·VersionId 지정 삭제 → remainingVersionCount 재조회 반환. 0 아니면 cleanupAttempt+1 후 max(=5) 미만이면 postprocessing self-loop 재개(H5-02 — cleanup 은 비차단 취급 금지, terminal finalize 금지), max 도달 시 pendingFailureReason='cleanup_blocked' 로 승격**. (c) cleanup remainingVersionCount==0(또는 cleanup_blocked 명시 blocker) 후에만 finalize_visual_job(pendingTerminalState, pendingFailureReason, meta={pairStoreStatus, poseGate/judge 메타}, display_status(done↔done/failed↔failed), key=(done 이면 canonicalKey else None), joint, expect_claim_owner=owner) — job+analysis 원자. 재전달(crash 재개)이면 (a)(b)(c) 전부 idempotent (pair meta-read 멱등 B5-03, cleanup 이미 삭제분 무해, finalize 'stale' no-op).**
판정 메타 전부 flat scalar. 테스트: PASS 체인 (fetch→judge→pose_check→postprocess invocation 분리) → done + canonical key + 판정 메타 + exact-prefix versions 0 + 페어 적재(동의 시) + **job/analysis 항상 함께 done (finalize 원자)**; **H5-05 실패 postprocessing: judge_failed/pose_gate_failed/invalid_output/timeout/moderation/create_unconfirmed 각각 postprocessing 경유 → exact-prefix cleanup 후 failed finalize (직접 finalize 0)**; **H4-01 durable: canonical copy 성공 후 postprocess 전 crash → 재개 두 문서 일치; postprocess 중 pair 후·finalize 전 crash → pair meta-read 멱등 재PUT 0 + finalize; cleanup 일시 실패 → cleanupAttempt self-loop 재개 후 0 확인 → finalize (H5-02); finalize commit_lost → 재전달 'stale' no-op 두 문서 일치**; **B5-02: 동시 source/staging 여러 version 있어도 exact-prefix cleanup 후 list-object-versions 0 (page 끝까지, 단일 VersionId 미신뢰)**; **H5-01 consent race: pose_check 시 opt-in, postprocess 직전 revoke → pair PUT 0 + skipped_consent + cleanup/finalize 정상**; **B5-03: pairId/keyVersion 이 pose_checking→postprocessing 에서 job 고정 + postprocess 가 active key 재선택 안 함(grep) + crash+rotation 재PUT 0**; **H4-05/M5-01: JPEG 벤더 출력 → PNG magic bytes + ContentType image/png; oversize/bomb → 'judge_input_too_large', corrupt/format mismatch → 'invalid_output'**; **B4-05: DISPLAY_JUDGE_CONFIDENCE 통과·TRAINING_JUDGE_CONFIDENCE 미달 → done + 페어 0**; pose_gate_unavailable → typed + 미노출; learningOptIn False → done + 페어 0; fetch 가 재-poll fresh URL (소스 grep 'outputUrl' 저장/판독 0); **staging reuse (B5-02): 재실행 같은 normalized PNG → staging/canonical head 재사용, put/copy 재호출 0**.</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31/test_visual_worker.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - fetch 가 taskId 재-poll + JPEG→PNG 재인코딩 + staging deterministic HEAD/hash reuse (H4-05/B5-02/H3-01)
    - decode 오류 분리: size/pixel=judge_input_too_large, corrupt/mismatch=invalid_output (M5-01) + MAX_IMAGE_PIXELS 모듈 1회 고정 (M5-03)
    - 모든 correctedPose terminal(성공+실패) postprocessing 경유 + exact-prefix version cleanup 후 finalize (H5-05/B5-02)
    - postprocess 가 consent 재read (H5-01) + caller-fixed pairId(B5-03) + durable pair/cleanup self-loop retry (H5-02)
    - postprocessing durable: canonical 후 crash / pair 후 crash / cleanup 실패 재개 / commit_lost 각각 두 문서 일치 (H4-01)
    - display/training 4 env 분리 소비 (B4-05)
  </acceptance_criteria>
  <done>D-05 자동 교정이 URL 비저장·PNG 정규화·staging reuse·consent 재확인·durable pair/exact-prefix cleanup·성공+실패 postprocessing 경유까지 테스트로 고정됨.</done>
</task>

<task type="auto">
  <name>Task 3: rotation fetch + dispatcher Lambda(cursor/lease-expired/age) + 운영자 스크립트(reconciler)</name>
  <files>backend/functions/visual-worker/app.py, backend/functions/visual-dispatch/app.py, backend/functions/visual-dispatch/requirements.txt, backend/scripts/list_stuck_visual_jobs.py, backend/tests/phase31/test_visual_worker.py, backend/tests/phase31/test_visual_dispatcher.py</files>
  <read_first>backend/shared/python/sunity_shared/s3keys.py (build_upload_key — canonical upload key 단일 출처), .planning/spikes/004-gemini-omni-view-editing/wan_gate_batch.py (회전 PROMPT 원문 — 카메라 orbit 이식), backend/shared/python/sunity_shared/firestore_admin.py (list_dispatch_pending 반환 {items,backlog_max_age_ms,truncated,cursor_wrapped} / mark_visual_job_dispatched(action+seq) / update_analysis_visual), .planning/phases/31-api-visual-correction/31-PLAN-REVIEW-ITERATION5.md 의 B5-01/H5-06/M5-02 절</read_first>
  <action>(1) `_action_create` rotation 분기: myVideoKey 를 s3keys.build_upload_key 후보(mp4/mov) equality 검증 — 불일치 finalize 'invalid_output' (H-05), presign 1h → WanVideoEditAdapter.create_task(video_url, PROMPT 상수); **sync 반환 시 async-only 이므로 'vendor_error' (B4-02, rotation 도 async)**. (2) `_action_fetch_rotation(job)`: claim → expect fetching — **taskId 재-poll fresh URL (H3-01)** → download_vendor_asset(fresh_url, /tmp, max_bytes=200MB, **allowed_content_types=('video/mp4',) — Content-Type 정규화 exact membership: `split(';',1)[0].strip().lower()` (M5-02 — 'video/mp4foo' 거부)**) — /tmp streaming + boto3 upload_file(multipart) canonical `results/{uid}/{analysis_id}/rotation.mp4` (전체 bytes 메모리 적재 0 — H2-05) → **finalize_visual_job(done, display_status='done', key, expect_claim_owner=owner) — job+analysis 원자 (rotation 은 temp 입력·pair 없음 → postprocessing 미경유)**.
(3) **visual-dispatch/app.py 신설 (H3-09 + H4-08 + B5-01 + H5-06)**: lambda_handler(EventBridge) — `res = list_dispatch_pending(now_ms, limit=20)` → `res["items"]`(pending nonterminal + sent+lease-만료+nonterminal 복구 대상 혼합) 각 항목 `sqs.send_message(env VISUAL_QUEUE_URL, {"jobId","generation","action": nextAction,"outboxSeq"})` → `mark_visual_job_dispatched(expect_action=nextAction, expect_outbox_seq=outboxSeq)`. **creating 항목은 결과에 없음(outbox 미기록)이 정상 → 방어적 skip. pending age metric (H4-08): res["backlog_max_age_ms"] 를 cloudwatch put_metric_data(Namespace 'SunityVisual', MetricName 'OutboxPendingAgeMs') 방출; res["truncated"] True 면 metric 'OutboxScanTruncated'=1 방출 (H5-06 — cursor 순환에도 backlog 가 max_scan 초과 지속 시 경보).** 로그 key=value. requirements.txt = firebase-admin + pyyaml (deploy fix 주석 2건).
(4) list_stuck_visual_jobs.py: argparse --profile 기본 sunity-motion, --stale-minutes 기본 30, --redispatch, **--reconcile (B3-02 보조)**: (a) 조회 — nonterminal stale 표(jobId/kind/state/dispatchState/nextAction/outboxSeq/attempt/failureReason/경과분); (b) --redispatch — pending 항목 SQS 재발행(action+outboxSeq 포함); (c) --reconcile — terminal job(done|failed)과 analysis 표시 불일치 → update_analysis_visual repair (이 스크립트가 유일 정당 호출자 — 31-02); (d) 'create_unconfirmed' 항목은 별도 섹션 "수동 판정 필요 — 중복 과금 회피" (M3-07).
테스트 — test_visual_worker.py: rotation 성공 → 재-poll + upload_file(put_object bytes 아님) + finalize 원자(owner CAS) + URL 미저장 + postprocessing 미경유; myVideoKey 불일치 → 'invalid_output' + create 0; rotation sync → 'vendor_error'; **M5-02: content-type 'video/mp4; charset=x' 통과 / 'video/mp4foo' 거부**. test_visual_dispatcher.py: pending 항목(polling/fetching/retry_ready 혼합) 전부 발행 + 각 action==nextAction + outboxSeq 포함 + mark(action+seq CAS); **sent+lease-만료+nonterminal 복구 항목 발행 (B5-01)**; sent+lease-유효 미발행; terminal 미발행; **B5-01 종합: 워커 CAS 직후 crash 로 남은 pending 을 dispatcher lease 만료 후 발행 → 체인 재개 → terminal**; backlog_max_age_ms metric + truncated metric 방출 mock 검증; 상한 20건.</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31/test_visual_worker.py backend/tests/phase31/test_visual_dispatcher.py -x -q && python -m pytest backend/tests -q</automated>
  </verify>
  <acceptance_criteria>
    - rotation fetch 가 재-poll + upload_file 기반 + content-type exact membership (M5-02) + postprocessing 미경유 (H2-05/H3-01)
    - visual-dispatch/app.py 가 pending + sent-lease-expired 복구 발행 + 실 backlog age + truncated metric (H3-09/H4-08/B5-01/H5-06)
    - dispatcher 연동 (crash → lease 만료 → dispatcher → terminal) green (B5-01)
    - list_stuck_visual_jobs.py 에 --reconcile + M3-07 문구 존재
    - `python -m pytest backend/tests -q` exits 0
  </acceptance_criteria>
  <done>D-06 회전 잡 + 독립 dispatcher(cursor/lease-expired/age) 복구 + reconciler + 수동 판정 가시성 완결.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQS 메시지 → 워커 외부 side-effect | claim 4상태 claimed 만 — busy/stale/completed/malformed 는 외부 0 |
| 사용자 객체 → 벤더 | canonical/hash key equality 통과분만 |
| 벤더 응답 → S3 | taskId 재-poll fresh URL + download 스트리밍 + JPEG→PNG 정규화 + content-type exact 통과분만 |
| worker 결과 → analysis 표시 | finalize_visual_job 단일 transaction (owner CAS + postprocessing durable 경유) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-32 | Denial of Service | 전이 후 발행 유실 고아 | mitigate | 전이=원자 outbox(seq)+snapshot + dispatcher 독립 복구(lease-expired) + crash 기대값 terminal (B3-01/B4-01/B5-01) |
| T-31-33 | Tampering | 중복/stale/구세대/구-outbox/lease 상실 write | mitigate | claim 4상태 + creating lease + taskId journal + CAS + generation + outboxSeq + expect_claim_owner (B2-02/B3-01/B4-01/B5-01) |
| T-31-34 | Information Disclosure | 타 사용자 객체 벤더 전송 | mitigate | upload/hash key equality (H-05 + B2-03) |
| T-31-35 | Information Disclosure | 원본 프레임/서명 URL 잔존 | mitigate | exact-prefix version cleanup(page 끝까지) + taskId 만 + 직렬화 URL 부재 (H-04/H3-01/H4-03/B5-02) |
| T-31-36 | Tampering | 저품질/포맷불일치 생성물 노출·적재 | mitigate | before/after judge + pose 게이트 + JPEG→PNG 정규화 + decode 오류 분리 + calibration 임계값 (H-03/H3-03/H3-04/H4-05/M5-01) |
| T-31-37 | Repudiation | generic 실패/조용 삭제/영구 불일치/pair 유실 | mitigate | typed 11종 + malformed→DLQ + finalize 원자 + 성공+실패 postprocessing durable + reconciler (M2-02/B3-02/H4-01/H5-05) |
| T-31-58 | Denial of Service | 복구 경로 concurrency 굶김 | mitigate | dispatcher 별도 Lambda concurrency 1 + durable cursor + 실 backlog age metric/alarm (H3-09/H4-08/H5-06) |
| T-31-62 | Denial of Service (과금) | standard SQS 중복으로 유료 action 중복 | mitigate | claim busy → batchItemFailures, claimed 만 외부 (B5-01) |
| T-31-71 | Tampering | postprocess crash + HMAC rotation 중복 학습쌍 | mitigate | pairId/keyVersion pose_checking→postprocessing 고정 + store meta-read 멱등 (B5-03) |
| T-31-72 | Privacy | 저장 직전 opt-out 무시 | mitigate | postprocess S3 PUT 직전 consent 재read → skipped_consent (H5-01) |
| T-31-73 | Information Disclosure | 단일 VersionId 미신뢰로 noncurrent PII 잔존 | mitigate | exact-prefix 전 Versions/DeleteMarkers 열거·삭제 + remainingVersionCount==0 후 finalize (B5-02) |
</threat_model>

<verification>
- `python -m pytest backend/tests -q` green
- 워커/디스패처 모듈에 requests·키 리터럴·fail_analysis·update_analysis_visual·google.genai·시간 폴링 루프·outputUrl 기록 0
- postprocess 가 store_training_pair 에 caller-fixed pairId/keyVersion 전달 (active key 재선택 0 — grep)
- requirements.txt 2종 pyyaml 존재 (deploy fix 선례 2건 인용)
</verification>

<success_criteria>
- B5-01(claim 4상태 + busy batchItemFailures + owner CAS + lease-expired 복구) + B5-02(exact-prefix version cleanup + staging reuse) + B5-03(pairId 고정) + H5-01(consent 재확인) + H5-02(durable pair/cleanup retry) + H5-04(create snapshot) + H5-05(성공+실패 postprocessing) + M5-01(decode 분리) + M5-02(content-type exact) + M5-03(MAX_IMAGE_PIXELS) + B4-02/B4-03/H4-05/B4-05/H3-01 전부 테스트/텍스트 고정
- 5차 리뷰 §7 실행 허용 조건 중 worker 측 항목 전부 이 플랜 테스트로 추적 가능
</success_criteria>

<output>
Create `.planning/phases/31-api-visual-correction/31-09-SUMMARY.md` when done
</output>
