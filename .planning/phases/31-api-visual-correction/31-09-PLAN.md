---
phase: 31-api-visual-correction
plan: 09
type: execute
wave: 3
depends_on: [31-02, 31-05, 31-06, 31-07]
files_modified:
  - backend/functions/visual-worker/app.py
  - backend/functions/visual-worker/requirements.txt
  - backend/scripts/list_stuck_visual_jobs.py
  - backend/tests/phase31/test_visual_worker.py
autonomous: true
requirements: [D-03, D-04, D-05, D-06, D-08]
must_haves:
  truths:
    - "외부 side-effect 는 invocation 당 1개 — create/poll/fetch/judge/pose_check 각 액션이 자기 외부 단계 1개만 수행하고 다음 SQS action 을 발행한다. 300초 안에 다중 외부 timeout 이 누적될 수 없다 (2차 리뷰 B2-02)"
    - "vendor create 전후 crash 에서 고아·무제한 중복 과금이 생기지 않는다 — create 는 creating(lease/requestKey) 상태에서만, 2xx 직후 taskId CAS, lease 만료+taskId 부재 = 'create_unconfirmed' 수동 reconciliation (자동 재생성 금지 — DashScope idempotency 미지원) (2차 리뷰 B2-02, §8 gate 4)"
    - "정상 return 이 job 을 고아로 만들 수 없다 — 모든 정상 return 은 종결 상태이거나 다음 action 메시지를 이미 발행한 상태 (리뷰 B-03)"
    - "예상치 못한 예외와 malformed 메시지는 partial batch failure(batchItemFailures)로 보고되어 DLQ 증거가 남는다 — log+skip 조용 삭제 금지 (2차 리뷰 M2-02)"
    - "모든 전이는 job 문서 CAS 로 멱등 — stale/중복 메시지는 no-op, 재과금 0 (리뷰 B-03/H-06)"
    - "rotation fetch 는 /tmp streaming + upload_file — 전체 bytes 메모리 적재 금지, fetching 은 별도 invocation (2차 리뷰 H2-05)"
    - "correctedPose: judge(before/after) + 결정론 pose 게이트 통과분만 done, 입력 임시 객체는 종결 시 삭제 (리뷰 H-03/H-04)"
    - "종결 시 judge·poseGate 판정 메타가 job 문서에 flat scalar 로 남는다 — 31-12 E2E-A 검증 가능"
    - "주기적 sweeper 가 outbox(dispatchState pending)·dispatch_failed 를 재발행한다 — 사용자 재시도는 보조 경로 (2차 리뷰 H2-02, §8 gate 7)"
    - "벤더 URL 은 Firestore 에 저장되지 않는다 — S3 canonical key 만 (리뷰 H-02)"
  artifacts:
    - path: "backend/functions/visual-worker/app.py"
      provides: "action 단위 state machine 워커 (create/poll/fetch/judge/pose_check) + sweeper 루틴"
      contains: "batchItemFailures"
    - path: "backend/scripts/list_stuck_visual_jobs.py"
      provides: "운영자용 stuck 조회 + --redispatch 재발행 + create_unconfirmed reconciliation 목록 (리뷰 L-04 + B2-02/H2-02)"
    - path: "backend/tests/phase31/test_visual_worker.py"
      provides: "crash-injection 6지점·continuation·멱등·typed 실패·정리 테스트"
      min_lines: 200
  key_links:
    - from: "backend/functions/visual-worker/app.py"
      to: "backend/shared/python/sunity_shared/analysis/visual_gen.py"
      via: "create_task/poll 어댑터 + download_vendor_asset(파일)"
      pattern: "visual_gen"
    - from: "backend/functions/visual-worker/app.py"
      to: "backend/shared/python/sunity_shared/firestore_admin.py"
      via: "transition_visual_job CAS(+validator) + update_analysis_visual"
      pattern: "transition_visual_job"
---

<objective>
리뷰 B-03 과 2차 리뷰 B2-02 를 해소하는 워커를 구현한다. 1차 개정의 "run 에서 create+즉시 폴링" 구조는 create 전후 crash window 를 남겼다 (B2-02: reserved→polling 선전이 후 create 크래시 = 영구 고아 or 재시도 시 중복 과금). 이번 구조는 **외부 side-effect 당 invocation 1개**:

메시지 = {"jobId": str, "action": "create"|"poll"|"fetch"|"judge"|"pose_check"}.
- create: CAS reserved|dispatch_failed → creating (leaseOwner=aws_request_id, leaseExpiresAt=now+10분, requestKey — 31-02 validator 필수 필드) → vendor create_task 1회 → 2xx 즉시 CAS creating → polling(taskId 필수 validator) → poll 메시지 발행(DelaySeconds 60; sync 이미지 모델이 succeeded 즉시 반환 시 fetching 으로 직행 + fetch 메시지). **재전달이 creating 을 발견하면: lease 유효 = no-op(동시 실행 방지), lease 만료 + taskId 부재 = 'create_unconfirmed' typed 실패 종결 (자동 재생성 금지 — 수동 reconciliation, B2-02).**
- poll: polling 상태에서 1회 폴링 — succeeded → CAS polling→fetching(outputUrl 기록) + fetch 메시지; pending → attempt+1, attempt>=MAX_POLLS(20) 면 'timeout' 종결, 아니면 poll 메시지 재발행; failed/blocked → 종결 (blocked 는 retryCount < MODERATION_RETRY_MAX 면 creating 부터 재생성 1회).
- fetch: download_vendor_asset 로 /tmp streaming (H2-05) → correctedPose = S3 staging key `visual-input/{uid}/{analysisId}/{sourceHash}_after.png` upload_file + CAS fetching→judging(stagingKey/afterHash 기록) + judge 메시지; rotation = canonical `results/{uid}/{analysisId}/rotation.mp4` upload_file(multipart) + 종결 done.
- judge (correctedPose 전용): S3 에서 before(srcKey)/after(stagingKey) 로드 → judge_corrected_pose → display_pass 만 CAS judging→pose_checking(판정 메타 기록) + pose_check 메시지; 실패 → 'judge_failed' 종결.
- pose_check (correctedPose 전용): pose gate → 통과 시 staging → canonical `corrected_pose_{joint}.png` copy + done + 페어 적재 + 정리; 실패 → typed 종결.

정상 return 불변식(B-03)·partial batch failure·CAS 멱등은 유지. continuation 발행 실패는 예외 전파로 재전달.

Purpose: 유료 잡이 고아도 중복 과금도 될 수 없는 비동기 실행기 — §8 gate 4·5·7·11 의 실행 지점.
Output: visual-worker Lambda + sweeper + 운영자 스크립트 + crash-injection 테스트.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-api-visual-correction/31-PATTERNS.md
@.planning/phases/31-api-visual-correction/31-ACCEPTANCE.md
@.planning/phases/31-api-visual-correction/31-PLAN-REVIEW-ITERATION2.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 워커 골격 — action 단위 state machine + creating lease + partial batch failure + sweeper</name>
  <files>backend/functions/visual-worker/app.py, backend/functions/visual-worker/requirements.txt, backend/tests/phase31/test_visual_worker.py</files>
  <read_first>backend/functions/pipeline/app.py lines 4816-4867 (SQS 레코드 루프 관례) + lines 1258-1263 (_signed_get), backend/functions/pipeline/requirements.txt + backend/functions/playback-url/requirements.txt (pyyaml deploy fix 주석 2건 원문 — 신규 requirements.txt 에 인용), backend/shared/python/sunity_shared/firestore_admin.py (31-02 함수 5종 시그니처 — creating/polling validator 포함), backend/shared/python/sunity_shared/models.py (VISUAL_JOB_STATES 9종/VISUAL_FAILURE_REASONS 9종), .planning/phases/31-api-visual-correction/31-PLAN-REVIEW-ITERATION2.md 의 B2-02 절 (해소 요건 원문)</read_first>
  <action>visual-worker/app.py 신설 (모듈 docstring: B-03 + B2-02 설계 불변식 — "외부 side-effect 당 invocation 1개, 정상 return = 종결 or 다음 action 발행 완료, creating lease 만료 = 수동 reconciliation"). lambda_handler: **EventBridge scheduled event 감지 (event 에 'source'=='aws.events' 또는 Records 부재) → `_sweep_dispatch()` 실행 후 return (Task 3 sweeper — H2-02)**; SQS Records 루프 — 각 레코드 try: 메시지 파싱({jobId, action}) — **파싱 실패/스키마 위반은 log.error(metric=visual_worker_malformed key=value) 후 batchItemFailures 에 추가 (2차 리뷰 M2-02: log+skip 조용 삭제 금지 — maxReceiveCount 소진 후 DLQ 에 증거 보존)**; `_handle_job(job_id, action, aws_request_id)` 호출; except Exception → log.exception + batchItemFailures. 반환 {"batchItemFailures": [...]}. _handle_job: job 문서 로드 (부재 → log 후 종료), action dispatch:
- `_action_create(job)`: 현재 state 'creating' 이면 lease 검사 — leaseExpiresAt 미래 = no-op return (동시 방지), 과거 + taskId None = **'create_unconfirmed' 종결 (자동 재생성 금지 — B2-02, 운영 계약: 중복 과금 가능성 명시 주석 + list_stuck 스크립트가 목록화)**, 과거 + taskId 존재 = polling 재개. state 'reserved'|'dispatch_failed' 면 CAS → creating {leaseOwner: aws_request_id, leaseExpiresAt: now+600_000ms, requestKey: f"{jobId}:gen{generation}"} → kind 어댑터 create_task 1회 → VendorTaskCreated 면 CAS creating→polling {taskId} (validator 가 taskId 강제) + poll 메시지(DelaySeconds 60); VendorPollResult succeeded (sync 이미지) 면 CAS creating→fetching {outputUrl} + fetch 메시지; typed 실패면 종결.
- `_action_poll(job)`: expect polling — 1회 poll → 위 objective 규칙 (succeeded→fetching+fetch 메시지 / pending→attempt·MAX_POLLS·재발행 / failed·blocked→종결·모더레이션 1회 재생성은 retryCount CAS 후 creating 재진입 아닌 **create 메시지 발행**).
- 공통: 다음 action 메시지 발행은 `sqs.send_message(env VISUAL_QUEUE_URL, {"jobId", "action"}, DelaySeconds)` — **발행 실패는 예외 전파 (batchItemFailures → 재전달 — 고아 방지)**. 종결 공통 `_finalize(job, state, failure_reason, meta)`: transition_visual_job CAS + update_analysis_visual (실패 시 'failed') — **fail_analysis 절대 호출 금지 (D-08)**.
requirements.txt = `firebase-admin>=6,<7` + `pyyaml>=6.0,<7.0` — pyyaml 주석에 두 선례 deploy fix 인용 필수 ("PyYAML — sunity_shared.firestore_admin transitive import (analysis/force_signals.py module-level yaml). 2026-06-12 deploy fix (pipeline/upload-url/reference-api 500 ImportError) + 2026-07-17 deploy fix (playback-url 29-06 배포 직후 500 Runtime.ImportModuleError)"). google-genai 절대 추가 금지. 로깅 key=value, 키·서명 URL 미로깅.
test_visual_worker.py — **crash-injection 6지점 (B2-02 필수, §8 gate 4): (1) create 직전 crash (creating 상태로 남음) → 재전달 시 lease 만료 후 'create_unconfirmed' + vendor create 재호출 0; (2) vendor 2xx 직후·taskId CAS 전 crash → 재전달 동일 규칙 (재생성 0 — 중복 과금 차단); (3) taskId write 직후 crash → 재전달이 polling 재개 (create 0회); (4) continuation send 직전 crash → batchItemFailures 재전달로 해당 action 재실행 (CAS no-op 멱등); (5) S3 put 직후·상태 CAS 전 crash (fetch) → 재전달이 fetching 재실행 (동일 dest 덮어쓰기 멱등, 벤더 호출 0); (6) terminal write 직전 crash → 재전달이 종결 재시도 (외부 호출 0)**; malformed 메시지 → batchItemFailures 포함 (M2-02); pending → DelaySeconds 60 poll 발행 + 정상 return; MAX_POLLS → 'timeout'; stale(state done) → no-op + 벤더 0; 모더레이션 → 재시도 1회 후 'moderation'.</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31/test_visual_worker.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - action 5종 (create/poll/fetch/judge/pose_check) dispatch 존재 + 각 action 이 외부 호출 최대 1개 (테스트에서 mock 호출 수 고정)
    - crash-injection 6지점 테스트 전부 green (B2-02 — §8 gate 4)
    - creating lease 만료 + taskId 부재 → 'create_unconfirmed' + 자동 재생성 0 테스트 green
    - malformed 메시지 → batchItemFailures (조용 삭제 0 — M2-02)
    - fail_analysis 호출 0 (모듈 소스 검사)
    - requirements.txt pyyaml 주석 2건 인용 + google-genai 부재
  </acceptance_criteria>
  <done>고아·중복 과금이 불가능한 action 단위 state machine 골격 완성.</done>
</task>

<task type="auto">
  <name>Task 2: correctedPose fetch/judge/pose_check 핸들러 (이중 게이트 + 정리 + 페어 + 판정 메타)</name>
  <files>backend/functions/visual-worker/app.py, backend/tests/phase31/test_visual_worker.py</files>
  <read_first>backend/shared/python/sunity_shared/analysis/visual_gen.py (WanImageAdapter.create_task/poll, judge_corrected_pose, download_vendor_asset 파일 시그니처 — 31-05), backend/shared/python/sunity_shared/analysis/pose_gate.py (measure_generated_pose/derive_pose_url — 31-06), backend/shared/python/sunity_shared/analysis/pair_store.py (store_training_pair — 31-07), .planning/phases/31-api-visual-correction/31-ACCEPTANCE.md (calibration 확정 display/training 허용오차 — H2-07)</read_first>
  <action>correctedPose 핸들러 3개:
(1) `_action_fetch_corrected(job)`: expect fetching — job payload 의 srcKey 가 정확히 f"visual-input/{uid}/{analysis_id}/{payload sourceHash 의 [:16]}.png" 인지 equality 검증 (**immutable hash key — 2차 리뷰 B2-03; 단일 계약 = payload "sourceHash" 는 full sha256 hex, 키 세그먼트는 그 [:16] — 31-10 T3/31-03 to_payload 동일 문구; 불일치 즉시 'invalid_output'**) + S3 head 로 src 존재 확인, download_vendor_asset(outputUrl, /tmp 경로, max_bytes=20MB, allowed_content_types=('image/png','image/jpeg')) → staging `visual-input/{uid}/{analysisId}/{sourceHash}_after.png` upload_file → CAS fetching→judging {stagingKey, afterHash} + judge 메시지.
(2) `_action_judge(job)`: expect judging — S3 get before(srcKey, sha256 재검증 == sourceHash — 불일치 'invalid_output')/after(stagingKey) → judge_corrected_pose(before, after, context={joint, targetDeg} — **CorrectedPoseTarget payload 의 동일 provenance (B2-01: prompt 와 gate 가 같은 target 소비)**) — None → 'judge_failed', display_pass False → 'judge_failed' (판정 메타 기록), 통과 → CAS judging→pose_checking {judgeDisplayPass, judgeTrainingPass, judgeConfidence} + pose_check 메시지.
(3) `_action_pose_check(job)`: expect pose_checking — S3 get after → pose_gate.measure_generated_pose(after, joint_key, target_deg, tolerance=**env DISPLAY_POSE_TOL_DEG (31-01 CALIBRATION 채택값 — 31-10 IaC 주입)**, pose_url=derive_pose_url(env RUNPOD_ANALYZE_URL), token=env RUNPOD_AUTH_TOKEN) — passed False → typed 실패 ('pose_gate_failed'/'pose_gate_unavailable') + 메타 기록; 통과 → S3 copy staging→canonical `results/{uid}/{analysis_id}/corrected_pose_{joint}.png` (ContentType image/png) → _finalize done {poseGatePassed, poseGateErrorDeg, poseGateReason} + update_analysis_visual('done', key, joint) → **페어 적재 (성공시에만)**: 분석 문서 learningOptIn + training 게이트(judgeTrainingPass AND pose error <= env TRAINING_POSE_TOL_DEG) 통과 시 pair_store.store_training_pair (before 바이트 = 31-01 privacy 결정이 option-b 면 payload 의 trainingSrcKey 객체, option-a 면 srcKey — H2-10: Lambda 내 blur 실행 금지, blur 아티팩트는 Pod 가 생성) — 적재 실패는 status 영향 0 → **정리: srcKey/stagingKey(+trainingSrcKey) delete_object 시도 (모든 종결 경로 공통 — 실패 log.warning, lifecycle 1일 이중 방어)**.
판정 메타는 전부 flat scalar (미도달 단계 미기록 = 부재). 프롬프트는 payload 의 jointKey/targetDeg 로 구성 (31-01 스모크 형식). 테스트: PASS 전체 체인 (fetch→judge→pose_check 각 invocation 분리 실행) → done + canonical key + 판정 메타 (fake doc) + Firestore 에 'dashscope'/'aliyuncs' 부재 + src/staging 삭제; judge fail → 'judge_failed' + poseGate 메타 부재 + 페어 0 + 정리; pose_gate_unavailable → typed + 미노출; training 미달 → done + 페어 0 + judgeTrainingPass False; learningOptIn False → done + 페어 0; srcKey 불일치/hash 불일치 → 'invalid_output' + 벤더/Gemini 호출 0; download_vendor_asset 의 _test_* kwargs 사용 부재 (grep — 31-05 테스트 전용 인자 워커 미사용).</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31/test_visual_worker.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - fetch/judge/pose_check 가 각각 별도 invocation (한 invocation 에 직렬 수행 0 — 테스트 고정, B2-02/H2-05)
    - display(env DISPLAY_POSE_TOL_DEG)와 training(env TRAINING_POSE_TOL_DEG) 이 서로 다른 값으로 소비 (테스트 고정)
    - 모든 종결 경로에서 임시 객체 삭제 시도 (테스트 고정)
    - 종결 job 문서 판정 메타 flat scalar 기록 (테스트 고정 — 31-12 E2E-A 전제)
    - srcKey hash 재검증 + cross-key → 벤더 호출 0 테스트 green
  </acceptance_criteria>
  <done>D-05 자동 교정이 invocation 분리·이중 게이트·정리·동의 페어·판정 메타까지 테스트로 고정됨.</done>
</task>

<task type="auto">
  <name>Task 3: rotation fetch + sweeper 루틴 + 운영자 스크립트 (--redispatch/reconciliation)</name>
  <files>backend/functions/visual-worker/app.py, backend/scripts/list_stuck_visual_jobs.py, backend/tests/phase31/test_visual_worker.py</files>
  <read_first>backend/shared/python/sunity_shared/s3keys.py (build_upload_key — canonical upload key 형식의 단일 출처), .planning/spikes/004-gemini-omni-view-editing/wan_gate_batch.py (회전 PROMPT 원문 — 검증된 카메라 orbit 프롬프트 이식)</read_first>
  <action>(1) `_action_create` rotation 분기: 분석 문서 myVideoKey 를 s3keys.build_upload_key 후보(mp4/mov)와 equality 검증 — 불일치 'invalid_output' (H-05), presign 1h → WanVideoEditAdapter.create_task(video_url, PROMPT 상수). (2) `_action_fetch_rotation(job)`: expect fetching — download_vendor_asset(outputUrl, /tmp, max_bytes=200MB, allowed_content_types=('video/mp4',)) — **/tmp streaming + boto3 upload_file(multipart) 로 `results/{uid}/{analysis_id}/rotation.mp4` (전체 bytes 메모리 적재 0 — H2-05, §8 gate 11)** → done + update_analysis_visual. (3) `_sweep_dispatch()` (H2-02, §8 gate 7): visualJobs 에서 state in ('reserved','dispatch_failed') 쿼리 → dispatchState=='pending' 또는 nextDispatchAtMs 경과 항목에 대해 create 메시지 재발행 + mark_visual_job_dispatched CAS (sweep 당 상한 20건, 로그 key=value) — **사용자 재시도는 보조 경로라는 원칙의 자동 주 경로**. 아울러 state=='creating' + lease 만료 + taskId 부재 항목은 재발행하지 않고 'create_unconfirmed' 카운트만 로깅 (수동 reconciliation 대상 — B2-02). (4) list_stuck_visual_jobs.py: argparse --profile 기본 sunity-motion, --stale-minutes 기본 30, **--redispatch (기본 조회만)** — state in ('reserved','polling','creating','fetching','judging','pose_checking','dispatch_failed') stale 항목 표 출력 (jobId/kind/state/dispatchState/attempt/failureReason/경과분); --redispatch 시 reserved|dispatch_failed 만 SQS create 재발행; **'create_unconfirmed' failed 항목은 별도 섹션 "수동 reconciliation 필요 (DashScope 콘솔 task 목록 대조 — 중복 과금 방지)" 로 출력 (B2-02 운영 계약)**. 테스트: rotation 성공 경로 → upload_file 호출 (put_object bytes 아님) + canonical key + 벤더 URL 미저장; myVideoKey 불일치 → 'invalid_output' + create 0; taskId 재개 → create 미호출; sweeper — pending outbox 재발행 + sent CAS + creating 만료 미재발행; scheduled event → sweeper 분기.</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31/test_visual_worker.py -x -q && python -m pytest backend/tests -q</automated>
  </verify>
  <acceptance_criteria>
    - rotation fetch 가 upload_file 기반 (put_object(Body=bytes) 부재 — H2-05)
    - _sweep_dispatch 가 outbox pending/dispatch_failed 재발행 + create_unconfirmed 미재발행 테스트 green (H2-02/B2-02)
    - list_stuck_visual_jobs.py --redispatch + reconciliation 섹션 존재
    - `python -m pytest backend/tests -q` exits 0
  </acceptance_criteria>
  <done>D-06 회전 잡 + outbox 자동 복구 + 수동 reconciliation 가시성 완결.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQS 메시지 → 워커 | 위조/중복/stale/malformed 메시지 방어 (CAS + validator + DLQ 증거) |
| 사용자 객체 → 벤더 | canonical/hash key equality 통과분만 전송 |
| 벤더 응답 → S3 | download_vendor_asset 파일 스트리밍 경계 통과분만 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-32 | Denial of Service | 폴링/전이 고아 | mitigate | action 단위 continuation or 종결 불변식 + MAX_POLLS 'timeout' + crash-injection 6지점 테스트 (B-03 + B2-02) |
| T-31-33 | Tampering | 중복/stale 메시지 재과금 | mitigate | creating lease + taskId journal + CAS validator — create 재호출 0, lease 만료 = 수동 reconciliation (B2-02) |
| T-31-34 | Information Disclosure | 타 사용자 객체 벤더 전송 | mitigate | upload/hash key equality 검증 (H-05 + B2-03) |
| T-31-35 | Information Disclosure | 원본 프레임 잔존 | mitigate | 종결 시 src/staging 삭제 + lifecycle 이중 방어 (H-04) |
| T-31-36 | Tampering | 저품질 생성물 노출/적재 | mitigate | before/after judge + 결정론 pose 게이트 + calibration 임계값 (H-03 + H2-07) — 판정 메타 job 기록 |
| T-31-37 | Repudiation | generic 실패/조용 삭제 | mitigate | typed failureReason 9종 + malformed → DLQ 증거 (H-07 + M2-02) |
</threat_model>

<verification>
- `python -m pytest backend/tests -q` green
- 워커 모듈에 requests import·키 리터럴·fail_analysis 호출·google.genai import·시간 폴링 루프 0
- requirements.txt pyyaml 존재 (deploy fix 선례 2건 인용)
</verification>

<success_criteria>
- B2-02 해소 (side-effect 당 invocation 1개 + creating lease + crash-injection 6지점) + B-03 (고아 불가) + H2-02 sweeper + H2-05 streaming + M2-02 DLQ 증거 전부 테스트 고정
- §8 gate 4·6(워커 측 no-op)·7·11 이 이 플랜의 테스트로 추적 가능
</success_criteria>

<output>
Create `.planning/phases/31-api-visual-correction/31-09-SUMMARY.md` when done
</output>
