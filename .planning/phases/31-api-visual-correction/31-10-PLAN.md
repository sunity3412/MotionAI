---
phase: 31-api-visual-correction
plan: 10
type: execute
wave: 4
depends_on: [31-02, 31-03, 31-04, 31-05, 31-09]
files_modified:
  - backend/shared/python/sunity_shared/validation.py
  - backend/functions/playback-url/app.py
  - backend/functions/visual-request/app.py
  - backend/functions/visual-request/requirements.txt
  - backend/functions/pipeline/app.py
  - backend/template.yaml
  - backend/scripts/visual_infra_dryrun.py
  - backend/tests/phase31/test_visual_url.py
  - backend/tests/phase31/test_visual_request.py
  - backend/tests/phase31/test_visual_dispatch.py
autonomous: true
requirements: [D-05, D-06, D-07, D-08]
must_haves:
  truths:
    - "pipeline enqueue 는 reserve_visual_job 반환이 유일한 분기 기준 — created=True 만 src put+pending+dispatch, existing done/polling 은 완전 no-op (S3/표시 상태 불변), replay 가 완료 상태를 pending 으로 되돌릴 수 없다 (2차 리뷰 B2-03, §8 gate 6)"
    - "source key 는 immutable — visual-input/{uid}/{analysisId}/{sourceHash}.png, 고정 src.png 덮어쓰기 없음 (2차 리뷰 B2-03)"
    - "SQS 발행은 outbox 기반 — 예약 transaction 의 dispatchState='pending' 을 발행 성공 후 'sent' CAS, 실패는 sweeper 가 복구 (사용자 재시도는 보조) (2차 리뷰 H2-02, §8 gate 7)"
    - "queue visibility >= 6 × worker timeout, maxReceiveCount >= 5 — 배포 테스트가 template 파싱으로 assert (2차 리뷰 H2-01, §8 gate 9)"
    - "PipelineFunction role 에 VisualQueue ARN sqs:SendMessage 명시 — RunPod credential 검증은 31-12 (2차 리뷰 H2-02, §8 gate 8)"
    - "31-10 은 라이브 인프라를 변경하지 않는다 — lifecycle merge/SSM 키는 dry-run artifact 생성까지만, 실제 적용은 31-12 checkpoint (2차 리뷰 H2-09, §8 gate 15)"
    - "playback-url asset guard 는 exact basename + status done + content-type — stale key 남은 failed 문서는 404 (2차 리뷰 M2-01)"
    - "VisualJobsEnabled feature flag 기본 OFF, quota env fail-closed, analysisId 공유 validator (H-07/M-06/L-03)"
    - "D-05: 교정 이미지(correctedPose)는 분석 완료 시 자동 생성 — top-1 결함 부위, _process 는 enqueue 만 (H-01 재구조 후에도 자동 생성 결정 유지)"
    - "D-07: 비용 가드 = 사용자당 일일 생성 한도(회전 3/일) + 전역 안전핀(30/일) — transaction 예약, fail-closed env"
  artifacts:
    - path: "backend/functions/visual-request/app.py"
      provides: "POST /visual/rotation — auth·flag·transaction·outbox dispatch·202"
      contains: "def lambda_handler"
    - path: "backend/functions/playback-url/app.py"
      provides: "asset 재서명 확장 (exact basename + done guard, 1h)"
      contains: "asset"
    - path: "backend/functions/pipeline/app.py"
      provides: "_enqueue_corrected_pose_job — CorrectedPoseTarget 소비, replay-safe"
      contains: "_enqueue_corrected_pose_job"
    - path: "backend/template.yaml"
      provides: "VisualQueue(visibility 1800/maxReceive 5)/DLQ alarm/함수 2종/ReportBatchItemFailures/sweeper schedule/flag 파라미터"
      contains: "VisualJobsEnabled"
    - path: "backend/scripts/visual_infra_dryrun.py"
      provides: "lifecycle merge dry-run artifact + SSM 키 존재 점검 (라이브 mutation 0)"
  key_links:
    - from: "backend/functions/pipeline/app.py"
      to: "backend/shared/python/sunity_shared/analysis/fault_zoom.py"
      via: "build_corrected_pose_target — target 계약 유일 소비 (B2-01)"
      pattern: "build_corrected_pose_target"
    - from: "backend/functions/visual-request/app.py"
      to: "backend/shared/python/sunity_shared/firestore_admin.py"
      via: "reserve_visual_job transaction + mark_visual_job_dispatched"
      pattern: "reserve_visual_job"
    - from: "backend/template.yaml"
      to: "SSM /sunity/motion/dashscope-api-key"
      via: "resolve:ssm dynamic reference"
      pattern: "dashscope-api-key"
---

<objective>
HTTP 표면·분석측 dispatch·IaC 를 완성한다 — 31-09 워커 코드가 존재한 뒤 실행되는 wave 4 (2차 리뷰 H2-08: template 의 VisualWorkerFunction CodeUri 가 31-09 산출 디렉터리를 가리키므로 직렬 의존).

(1) playback-url asset 확장 — H-02 재서명 + M2-01 exact guard. (2) visual-request Lambda — H-06 원자 예약 + H2-02 outbox dispatch. (3) pipeline enqueue 훅 재설계 — **B2-03 해소: reserve 반환 분기 강제 + immutable hash source key + outbox. 1차 개정의 "S3 put → reserve → 무조건 pending+send" 순서는 replay 시 done 카드를 pending 으로 되돌리고 실행 중 job 의 입력을 교체했다 — 폐기.** target 은 31-03 `build_corrected_pose_target` 의 CorrectedPoseTarget 만 소비 (B2-01 — 감점 record 로 joint/targetDeg 를 직접 계산하지 않음). (4) template.yaml IaC — H2-01 공식 권고 반영 (visibility 1800 = 6×300, maxReceiveCount 5, DLQ alarm, reserved concurrency), sweeper schedule, PipelineFunction send 권한 (H2-02). (5) **라이브 mutation 금지 (H2-09): lifecycle merge JSON 과 SSM versioned HMAC 키 생성은 dry-run artifact 스크립트까지만 — 실제 put 은 31-12 checkpoint:human-action.**

Purpose: 요청·자동생성 양 진입점이 동일 durable substrate 로 흐르는 마지막 backend 배선 — replay 에도 안전하게.
Output: Lambda 2종 수정/신설 + pipeline 훅 + IaC + dry-run 스크립트 + 테스트 3파일.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-api-visual-correction/31-PATTERNS.md
@docs/contract.md
@.planning/phases/31-api-visual-correction/31-PLAN-REVIEW-ITERATION2.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 공유 validator + playback-url asset 재서명 (exact basename + done guard — M2-01)</name>
  <files>backend/shared/python/sunity_shared/validation.py, backend/functions/playback-url/app.py, backend/tests/phase31/test_visual_url.py</files>
  <read_first>backend/functions/playback-url/app.py (전체 — analysisId alnum+len>=16 가드, guards_ok 단일 404, _sign_get, _PLAYBACK_EXPIRES), backend/shared/python/sunity_shared/validation.py (기존 validator 관례 — 순수 함수·boto3 무관), docs/contract.md 의 playback-url asset 확장 절 (31-04 — 단일 기준)</read_first>
  <action>validation.py 에 `validate_analysis_id_format(analysis_id) -> bool` 신설 (isalnum and len>=16 이관 — 리뷰 L-03). playback-url/app.py: 인라인 검사를 공유 validator 로 교체 + optional `asset` 처리 — 값이 'correctedPose'|'rotation' 외면 400; asset 지정 시 (2차 리뷰 M2-01 전면): 분석 문서 read (token uid 스코프) → **(a) 해당 status 필드(correctedPoseStatus/rotationStatus)가 정확히 'done' 인지 — 'failed'/'pending'/부재 는 stale key 가 남아 있어도 404 (M2-01: failed 문서의 잔존 key 서명 금지)**, (b) key 가 **exact 계약과 equality**: correctedPose = f"results/{uid}/{analysis_id}/corrected_pose_{correctedPoseJoint}.png" (correctedPoseJoint 부재 시 404), rotation = f"results/{uid}/{analysis_id}/rotation.mp4" — prefix startswith 가 아니라 전체 문자열 일치, (c) 확장자별 ResponseContentType 지정(png=image/png, mp4=video/mp4) presign ExpiresIn=3600 (`_ASSET_EXPIRES`) → {playbackUrl, expiresInSec: 3600}. 문서 부재/타인/조건 위반 전부 guards_ok 단일 404. asset 미지정 = 기존 동작 바이트 동일. test_visual_url.py: fresh URL 재서명 (H-02), 타 uid 404, 필드 부재 404, **status failed + key 잔존 → 404 (M2-01)**, **basename 불일치 (results prefix 는 맞지만 다른 파일명) → 404 (M2-01)**, joint 부재 404, 잘못된 asset 400, 미지정 무회귀, 무토큰 401.</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31/test_visual_url.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - key 검증이 exact equality (startswith 단독 아님 — M2-01 테스트 고정)
    - status done 강제 — failed 문서 stale key 404 테스트 green
    - validation.py 공유 validator + playback-url import (인라인 중복 0)
    - asset 경로가 클라이언트 key 를 받지 않음
  </acceptance_criteria>
  <done>H-02 재서명 + M2-01 exact guard 완성.</done>
</task>

<task type="auto">
  <name>Task 2: visual-request Lambda — flag·원자 예약·outbox dispatch</name>
  <files>backend/functions/visual-request/app.py, backend/functions/visual-request/requirements.txt, backend/tests/phase31/test_visual_request.py</files>
  <read_first>backend/functions/playback-url/app.py (auth/파싱/guards_ok 골격), backend/functions/playback-url/requirements.txt + backend/functions/pipeline/requirements.txt (pyyaml deploy fix 주석 2건 원문), backend/shared/python/sunity_shared/firestore_admin.py (reserve_visual_job/mark_visual_job_dispatched/mark_visual_job_dispatch_failed — 31-02 시그니처), docs/contract.md 의 POST /visual/rotation 절</read_first>
  <action>visual-request/app.py 신설 — 흐름: (1) env `VISUAL_JOBS_ENABLED` != "true" → 503 'feature_disabled', (2) verify_request → 401, (3) validate_analysis_id_format → 400, (4) 분석 문서 존재·소유·status=='done' → guards_ok 단일 404, (5) rotationStatus=='done' → 200 멱등, (6) rotationStatus=='pending' + rotationUpdatedAtMs 20분 이내 → 202 dedupe (SQS 미발행 — 전용 timestamp, H-06), (7) `_parse_limit` fail-closed (파싱 불가 → 0 = 전부 429, M-06) + KST date_key, (8) reserve_visual_job(uid, analysis_id, 'rotation', date_key, user_limit 기본 3, global_limit 기본 30, **allow_retry_failed=True — 사용자 명시 재시도는 generation+1 재예약 (B2-03 failed 분기)**) — daily_limit → 429; created False + 기존 job state 가 'polling'|'creating'|'fetching' → 202 no-op (진행 중 멱등), (9) **outbox dispatch (H2-02): created True 또는 기존 state 'reserved'|'dispatch_failed' (dispatchState 'pending') → update_analysis_visual('pending') + sqs.send_message({"jobId", "action":"create"}) → 성공 시 mark_visual_job_dispatched CAS; send 실패 → mark_visual_job_dispatch_failed(nextDispatchAtMs=now+60s) + 202 반환 (500 아님 — sweeper 가 자동 복구하므로 사용자에겐 수락으로 응답, 재요청도 무-quota 재발행 보조 경로)** → 202. requirements.txt = firebase-admin + pyyaml (deploy fix 주석 2건 인용). test_visual_request.py: flag OFF 503 + SQS 0; 401/400/404; done 멱등 200; pending 20분 내 202 + SQS 0; 429 daily_limit; env "abc" → 429 fail-closed; **SQS send 실패 → dispatch_failed + nextDispatchAtMs 기록 + 202 + 재요청 시 quota 증가 0 + 재발행 + dispatched CAS**; 동시 요청 (reserve 2회 순차) → 두 번째 created False + SQS 1회; failed job 재요청 → generation 2 + quota 재소비.</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31/test_visual_request.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - dedupe/타임아웃 판정이 rotationUpdatedAtMs 만 사용
    - outbox: send 성공 → dispatched CAS / 실패 → dispatch_failed + nextDispatchAtMs (sweeper 계약 — H2-02 테스트 고정)
    - env fail-closed 429 + flag OFF 503 테스트 green
    - requirements.txt pyyaml 주석 2건 인용
  </acceptance_criteria>
  <done>온디맨드 진입점이 원자 예약·outbox 복구·flag·KST 한도까지 테스트 고정.</done>
</task>

<task type="auto">
  <name>Task 3: pipeline enqueue 재설계 (B2-03) + template.yaml IaC (H2-01/H2-02) + dry-run 스크립트 (H2-09)</name>
  <files>backend/functions/pipeline/app.py, backend/template.yaml, backend/scripts/visual_infra_dryrun.py, backend/tests/phase31/test_visual_dispatch.py</files>
  <read_first>backend/functions/pipeline/app.py lines 3116-3158 (_run_deferred_fault_zoom — 비차단 규율) + 4760-4800 (fault-zoom 조건부 위치 — 훅은 조건부 밖; 31-03 이 같은 파일을 먼저 수정하므로 최신 상태 기준 재확인), backend/shared/python/sunity_shared/analysis/fault_zoom.py (build_corrected_pose_target/CorrectedPoseTarget.to_payload — 31-03), backend/template.yaml (전체 — 함수/큐/LogGroup 선례 + SSM Statement 주석), .planning/phases/31-api-visual-correction/31-PLAN-REVIEW-ITERATION2.md 의 B2-03/H2-01/H2-02/H2-09 절</read_first>
  <action>(A) pipeline/app.py `_enqueue_corrected_pose_job(*, uid, analysis_id, target: CorrectedPoseTarget, src_png: bytes) -> None` — complete_analysis 이후 fault-zoom 조건부 밖 배치 (H-01). **target 은 31-03 build_corrected_pose_target 반환 — None 이면 호출 자체 생략 (상태 미기록 = legacy 숨김; 감점 record 로 joint/targetDeg 직접 계산 금지 — B2-01)**. 내부 (B2-03 순서): env flag OFF → return; **sourceHash = sha256(src_png).hexdigest() (full hex — payload 기록값), keySeg = sourceHash[:16]** (checker W1 단일 계약: payload "sourceHash" = full hex, S3 키 세그먼트 = 그 [:16]), srcKey = f"visual-input/{uid}/{analysis_id}/{keySeg}.png" (**immutable — 고정 src.png 금지**); 31-01 privacy 결정 option-b 면 **blur 생성 = pipeline(_process, Pod 런타임)이 RTMW 얼굴 keypoint(nose/eyes/ears) bbox 로 PIL GaussianBlur 를 src_png 사본에 적용** (검출 실패 시 보수적 head-region blur — checker W2) 해 trainingSrcKey = f"...{keySeg}_blur.png" 로 함께 put (H2-10: Lambda blur 금지; option-a 확정 시 이 분기 제거, privacy_decision.json 값으로 분기); **reserve_visual_job(kind 'correctedPose', allow_retry_failed=False, payload = target.to_payload() + {"srcKey": srcKey, "sourceHash": sourceHash(+trainingSrcKey)}) 호출 → 반환 분기가 유일 기준: created True → S3 put(srcKey, 존재 시 put 생략 — hash 동일 내용) → sqs send({"jobId","action":"create"}) → mark_visual_job_dispatched → update_analysis_visual('pending'); existing done → 완전 no-op (S3 put 0·상태 write 0·SQS 0 — §8 gate 6); existing polling|creating|fetching|judging|pose_checking → no-op; existing reserved|dispatch_failed → job 의 sourceHash 와 신규 hash 비교 — 동일하면 SQS 재발행만 (S3 재put 불필요), 다르면 log 후 no-op (실행 중 job 입력 교체 금지 — B2-03); existing failed → no-op (자동 경로 재시도 없음)**. send 실패 → mark_visual_job_dispatch_failed(nextDispatchAtMs) — sweeper 복구 (H2-02). 전체 try 재raise 0 (비차단). DashScope/Gemini/pose 호출 0. Pod 런타임 전제 주석: Pod env VISUAL_QUEUE_URL/VISUAL_JOBS_ENABLED + **Pod AWS credential 의 VisualQueue sqs:SendMessage 필요 — 31-12 simulate/canary 검증 (H2-02)**.
(B) template.yaml: `VisualQueue` — **VisibilityTimeout 1800 (= 6 × worker Timeout 300 — AWS Lambda SQS 공식 권고, H2-01)** + `VisualDLQ` RedrivePolicy **maxReceiveCount 5 (H2-01)**; `VisualDLQAlarm` (CloudWatch Alarm — DLQ ApproximateNumberOfMessagesVisible >= 1, 기존 alarm 선례 없으면 AWS::CloudWatch::Alarm 직접 선언, 액션 없이 상태 가시화만); `VisualRequestFunction` (Timeout 10, POST /visual/rotation, env FIREBASE_SA_PARAM/VISUAL_QUEUE_URL/VISUAL_JOBS_ENABLED(!Ref)/ROTATION_DAILY_LIMIT "3"/ROTATION_GLOBAL_DAILY_LIMIT "30", Policies: SSM + VisualQueue ARN sqs:SendMessage); `VisualWorkerFunction` (Timeout 300, **ReservedConcurrentExecutions 2 (H2-01 — vendor rate 와 동시 처리 상한)**, MemorySize = **31-05 RSS 실측 기반 결정 (스트리밍 peak < 64MB 실측 시 512 유지, 근거를 template 주석에 인용 — H2-05: 실측 없이 여유 주장 금지)**, SQS 이벤트 BatchSize 1 + FunctionResponseTypes [ReportBatchItemFailures], **Events 에 SweepSchedule (Schedule rate(15 minutes)) 추가 — 31-09 _sweep_dispatch 트리거 (H2-02)**, env VIDEO_BUCKET/FIREBASE_SA_PARAM/VISUAL_QUEUE_URL/DASHSCOPE_API_KEY resolve:ssm/PAIR_ID_HMAC_KEYS resolve:ssm(/sunity/motion/pair-id-hmac-keys — versioned JSON, H2-06)/RUNPOD_ANALYZE_URL/RUNPOD_AUTH_TOKEN/GEMINI_API_KEY resolve:ssm(워커 전용)/DISPLAY_POSE_TOL_DEG·TRAINING_POSE_TOL_DEG (31-01 CALIBRATION 채택값), Policies: SSM + s3 Get(uploads/*, visual-input/*, results/*) + Put/Delete(visual-input/*) + Put(results/*, training/*) + 자기 큐 sqs:SendMessage); **PipelineFunction — env VISUAL_QUEUE_URL/VISUAL_JOBS_ENABLED 추가 + Policies 에 VisualQueue ARN sqs:SendMessage 명시 (H2-02: Lambda fallback 경로 실권한 — §8 gate 8)**; Parameters `VisualJobsEnabled` Default "false"; LogGroup 2종 RetentionInDays 30.
(C) **visual_infra_dryrun.py (H2-09 — 라이브 mutation 0)**: (1) `aws s3api get-bucket-lifecycle-configuration` 읽기(read-only) → 기존 규칙 + visual-input/ 1일 Expiration + training/phase31/pairs/ **privacy_decision.json 의 retentionDays** Expiration 을 merge 한 결과 JSON 을 `.planning/phases/31-api-visual-correction/infra/lifecycle_merged.json` 과 before 원본 `lifecycle_before.json` 으로 저장 (rollback artifact); (2) SSM `/sunity/motion/pair-id-hmac-keys` 존재 여부 get 시도(read-only)만 — 부재 시 생성할 versioned JSON 형식({"active":"v1","keys":{"v1":"<hex32>"}})을 문서화한 지시문 출력. **put-bucket-lifecycle-configuration / ssm put-parameter 호출 코드 자체를 이 스크립트에 두지 않음 (31-12 checkpoint 에서 별도 실행 — H2-09).**
(D) test_visual_dispatch.py: flag OFF → S3/SQS 0; target None → 호출 생략; created True 정상 → hash key put + payload 에 target 스칼라 전부 + SQS 1 + dispatched CAS + pending; **duplicate-after-done: 기존 done job → S3 put 0·pending write 0·SQS 0 (B2-03 — §8 gate 6)**; **duplicate-while-polling → 완전 no-op**; **concurrent 2 enqueue (reserve 순차 2회 모사) → S3 put 1회·SQS 1회**; **reserve 성공 후 send crash → dispatch_failed + nextDispatchAtMs (sweeper 복구 가능 상태)**; **S3 put 후 process crash 모사 → 재실행이 reserved 분기에서 동일 hash 재발행 (입력 교체 0)**; sourceHash 불일치 reserved → no-op; 훅 내 visual_gen import 부재; **template 파싱 assert (H2-01 — §8 gate 9): yaml 로 template.yaml 파싱 → VisualQueue VisibilityTimeout >= 6 × VisualWorkerFunction Timeout + MaximumBatchingWindowInSeconds(미설정 시 0 — 향후 drift 차단), RedrivePolicy maxReceiveCount >= 5, ReportBatchItemFailures 존재, PipelineFunction policy 에 VisualQueue SendMessage 존재**.

실행 커밋 권장 순서 (checker W3 — Task 3 밀도): (A/D pipeline 훅+dispatch 테스트) 커밋 → (B template IaC+파싱 assert) 커밋 → (C dryrun 스크립트) 커밋 — 3분할 atomic.</action>
  <verify>
    <automated>(cd backend && sam validate --lint) && python -m pytest backend/tests/phase31/test_visual_dispatch.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - _enqueue_corrected_pose_job 이 build_corrected_pose_target 반환만 소비 (record 직접 계산 0 — B2-01)
    - reserve 반환 분기 5종 (created/done/진행중/reserved·dispatch_failed/failed) 전부 테스트 고정 (B2-03)
    - duplicate-after-done 완전 no-op + concurrent + crash 시나리오 테스트 green (§8 gate 6)
    - template 파싱 assert: visibility >= 6×timeout + maxReceiveCount >= 5 + ReportBatchItemFailures + Pipeline SendMessage (§8 gate 8·9)
    - visual_infra_dryrun.py 에 put-bucket-lifecycle / put-parameter 호출 0 (H2-09 — read-only grep 확인)
    - visual_infra_dryrun.py 1회 실행 완료 — infra/lifecycle_before.json + lifecycle_merged.json 2파일 존재 (31-12 T2 read_first 전제)
    - VisualWorkerFunction 에만 GEMINI_API_KEY, SweepSchedule 존재, VisualJobsEnabled Default "false"
    - `(cd backend && sam validate --lint)` && `python -m pytest backend/tests -q` exits 0
  </acceptance_criteria>
  <done>분석 경로가 replay-safe enqueue-only 로 배선되고, IaC 가 공식 권고 충족 + 라이브 mutation 0 상태로 배포 준비 완료 (실배포·인프라 적용은 31-12 checkpoint).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 앱 → POST /visual/rotation | 미신뢰 입력 + 과금 유발 |
| 앱 → playback-url asset | key 는 서버 선택 exact 계약 — 클라이언트 key 수신 0 |
| _process → SQS | 분석 경로는 reserve 분기 + enqueue 만 — 외부 API 무접촉 |
| IaC → 실 스택 | flag 기본 OFF + 라이브 mutation 은 31-12 checkpoint 로 격리 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-38 | Spoofing | visual-request 인증 | mitigate | verify_request Firebase ID token → 401 |
| T-31-39 | Elevation of Privilege | 타인 analysisId | mitigate | uid 토큰 경로 스코프 + guards_ok 단일 404 |
| T-31-40 | Denial of Service (과금) | 온디맨드 남용 | mitigate | transaction 한도(KST) + dedupe + done 멱등 + fail-closed env (H-06/M-06) |
| T-31-41 | Information Disclosure | 만료/임의/stale key 서명 | mitigate | exact basename + status done + content-type + 1h TTL (H-02 + M2-01) |
| T-31-42 | Denial of Service | dispatch 실패 고아 quota | mitigate | outbox dispatchState + nextDispatchAtMs + sweeper 자동 복구 (H2-02, 테스트 고정) |
| T-31-43 | Information Disclosure | DASHSCOPE/GEMINI/HMAC 키 | mitigate | resolve:ssm SecureString 만, GEMINI_API_KEY 워커 전용 |
| T-31-44 | Tampering | 미검증 기능 전면 노출 | mitigate | VisualJobsEnabled 기본 false (H-07) |
| T-31-54 | Tampering | replay 가 완료 상태 되감기/입력 교체 | mitigate | reserve 반환 유일 분기 + immutable hash source key + no-op 테스트 (B2-03) |
| T-31-55 | Tampering | 라이브 lifecycle 교체로 기존 규칙 소실 | mitigate | 31-10 은 dry-run merge artifact 만 — put 은 31-12 checkpoint + before JSON rollback + put 후 get 검증 (H2-09) |
</threat_model>

<verification>
- `python -m pytest backend/tests -q` green + `(cd backend && sam validate --lint)` green
- 응답 코드가 contract.md /visual/rotation 절과 일치
- 라이브 AWS 변경 0 (스크립트 read-only — H2-09)
</verification>

<success_criteria>
- B2-03(replay-safe enqueue)/H2-01(공식 권고 IaC+파싱 assert)/H2-02(outbox+IAM 명시)/M2-01(exact guard)/H2-09(dry-run 격리) 전부 구현+테스트
- §8 gate 6·7·8·9·15(전반부) 가 이 플랜의 텍스트로 추적 가능
</success_criteria>

<output>
Create `.planning/phases/31-api-visual-correction/31-10-SUMMARY.md` when done
</output>
