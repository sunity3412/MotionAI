---
phase: 31-api-visual-correction
plan: 10
type: execute
wave: 5
depends_on: [31-02, 31-03, 31-04, 31-05, 31-09, 31-13]
files_modified:
  - backend/shared/python/sunity_shared/validation.py
  - backend/functions/playback-url/app.py
  - backend/functions/visual-request/app.py
  - backend/functions/visual-request/requirements.txt
  - backend/functions/pipeline/app.py
  - backend/template.yaml
  - backend/scripts/visual_infra_dryrun.py
  - backend/tests/phase31/test_visual_url.py
  - backend/tests/phase31/test_visual_request.py
  - backend/tests/phase31/test_visual_dispatch.py
autonomous: true
requirements: [D-05, D-06, D-07, D-08]
must_haves:
  truths:
    - "correctedPose enqueue 는 upload-first — (1) hash immutable srcKey S3 put/head 검증 먼저, (2) 단일 transaction 으로 reserve + analysis 'pending' + nextAction='create'/dispatchState='pending'/outboxSeq=1 동시 기록, (3) SQS send best-effort. input 없는 create action 이 순서상 불가능 (3차 B3-03)"
    - "기존 srcKey head 재사용은 object metadata 의 full sha256 을 신규 sourceHash 와 exact 비교해서만 허용된다 — S3 key 는 16 hex 뿐이므로 불일치=collision/tampering block (4차 리뷰 M4-04)"
    - "source put 의 VersionId 를 reserve payload 에 기록한다 — 31-09 postprocess/실패 cleanup 이 versionId 지정 삭제로 versioned bucket noncurrent 잔존 0 (4차 리뷰 H4-03)"
    - "SQS send 후 analysis pending write 가 존재하지 않는다 — pending 은 reserve transaction 안에서만 (B3-03, §8-3 gate 5)"
    - "send 실패 = dispatcher 복구 — dispatchState='pending' durable + 별도 VisualDispatchFunction(reserved concurrency 1) 발행 (3차 H3-09)"
    - "메시지는 {jobId, generation, action, outboxSeq} — visual-request/pipeline 이 reserve 반환 outboxSeq 로 best-effort send + mark_visual_job_dispatched(action+seq CAS) (4차 리뷰 B4-01)"
    - "queue visibility >= 6 × worker timeout, maxReceiveCount >= 5 — template 파싱 assert (2차 H2-01)"
    - "template 이 4 게이트 env(DISPLAY_JUDGE_CONFIDENCE/TRAINING_JUDGE_CONFIDENCE/DISPLAY_POSE_TOL_DEG/TRAINING_POSE_TOL_DEG)를 CALIBRATION chosen 값으로 주입하고, build gate 가 TRAINING_JUDGE_CONFIDENCE > DISPLAY_JUDGE_CONFIDENCE AND TRAINING_POSE_TOL_DEG < DISPLAY_POSE_TOL_DEG 를 강제한다 (4차 리뷰 B4-05/M4-03)"
    - "dispatcher pending outbox age metric alarm 존재 — template 파싱 assert (H3-09)"
    - "dry-run 이 bucket versioning + Object Lock 을 조회하고, Object Lock enabled+default retention 이면 민감 prefix 별도 bucket 요구+blocked / enabled·no-default 면 canary retention·delete 가능성 검증, versioning enabled/suspended 면 NoncurrentVersionExpiration + expired delete marker 규칙 merge 포함 (4차 리뷰 H4-03/H4-04 + 3차 H3-08)"
    - "privacy 결정값은 배포 config/상수 — .planning JSON 런타임 읽기 0, build 테스트가 JSON 대조 (3차 M3-02)"
    - "playback-url asset guard 는 exact basename + status done + content-type — stale key failed 문서는 404 (2차 M2-01)"
    - "VisualJobsEnabled 기본 OFF, quota env fail-closed, analysisId 공유 validator (H-07/M-06/L-03)"
    - "D-05: 교정 이미지(correctedPose)는 분석 완료 시 자동 생성 — top-1 결함 부위, _process 는 enqueue 만"
    - "D-07: 비용 가드 = 사용자당 일일 생성 한도(회전 3/일) + 전역 안전핀(30/일) — transaction 예약, fail-closed env"
  artifacts:
    - path: "backend/functions/visual-request/app.py"
      provides: "POST /visual/rotation — auth·flag·transaction(reserve=pending+outbox)·best-effort dispatch(outboxSeq)·202"
      contains: "def lambda_handler"
    - path: "backend/functions/playback-url/app.py"
      provides: "asset 재서명 확장 (exact basename + done guard, 1h)"
      contains: "asset"
    - path: "backend/functions/pipeline/app.py"
      provides: "_enqueue_corrected_pose_job — upload-first + full-sha256 head 대조 + VersionId 기록 + CorrectedPoseTarget, replay-safe"
      contains: "_enqueue_corrected_pose_job"
    - path: "backend/template.yaml"
      provides: "VisualQueue/DLQ alarm/VisualRequestFunction/VisualWorkerFunction/VisualDispatchFunction/OutboxAge alarm/4 게이트 env/flag"
      contains: "VisualDispatchFunction"
    - path: "backend/scripts/visual_infra_dryrun.py"
      provides: "versioning+ObjectLock-aware lifecycle merge dry-run artifact + SSM 키 점검 (라이브 mutation 0)"
  key_links:
    - from: "backend/functions/pipeline/app.py"
      to: "backend/shared/python/sunity_shared/analysis/fault_zoom.py"
      via: "build_corrected_pose_target — target 계약 유일 소비 (B2-01)"
      pattern: "build_corrected_pose_target"
    - from: "backend/functions/visual-request/app.py"
      to: "backend/shared/python/sunity_shared/firestore_admin.py"
      via: "reserve_visual_job transaction(pending+outbox) + mark_visual_job_dispatched"
      pattern: "reserve_visual_job"
    - from: "backend/template.yaml"
      to: "SSM /sunity/motion/dashscope-api-key"
      via: "resolve:ssm dynamic reference"
      pattern: "dashscope-api-key"
---

<objective>
HTTP 표면·분석측 dispatch·IaC 를 완성한다 — 31-09 워커/디스패처 코드가 존재한 뒤 실행되는 wave 5 (H2-08).

(1) playback-url asset 확장 (H-02 재서명 + M2-01 exact guard). (2) visual-request Lambda — reserve transaction 이 analysis pending + 초기 outbox 를 함께 기록하므로 send 는 best-effort(outboxSeq 포함). (3) pipeline enqueue 훅 — **B3-03 upload-first + 4차 M4-04 full-sha256 head 대조 + H4-03 VersionId 기록**. target 은 31-03 CorrectedPoseTarget 만 소비 (B2-01). (4) template.yaml — H2-01 + **VisualDispatchFunction 분리 (H3-09)** + **4 게이트 env + build gate strict ordering (B4-05)** — worker sweeper schedule 부재. (5) 라이브 mutation 금지 (H2-09) + **dry-run 의 versioning + Object Lock 인지 (H4-03/H4-04/H3-08)**. (6) privacy 값 build-time 상수 (M3-02).

Purpose: 요청·자동생성 양 진입점이 동일 durable substrate 로 흐르는 마지막 backend 배선.
Output: Lambda 3종 + pipeline 훅 + IaC + dry-run 스크립트 + 테스트 3파일.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-api-visual-correction/31-PATTERNS.md
@docs/contract.md
@.planning/phases/31-api-visual-correction/31-PLAN-REVIEW-ITERATION4.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 공유 validator + playback-url asset 재서명 (exact basename + done guard — M2-01)</name>
  <files>backend/shared/python/sunity_shared/validation.py, backend/functions/playback-url/app.py, backend/tests/phase31/test_visual_url.py</files>
  <read_first>backend/functions/playback-url/app.py (전체 — analysisId alnum+len>=16 가드, guards_ok 단일 404, _sign_get, _PLAYBACK_EXPIRES), backend/shared/python/sunity_shared/validation.py (기존 validator 관례 — 순수 함수·boto3 무관), docs/contract.md 의 playback-url asset 확장 절 (31-04)</read_first>
  <action>validation.py 에 `validate_analysis_id_format(analysis_id) -> bool` 신설 (isalnum and len>=16 이관 — L-03). playback-url/app.py: 인라인 검사를 공유 validator 로 교체 + optional `asset` 처리 — 'correctedPose'|'rotation' 외 400; asset 지정 시 (M2-01): 분석 문서 read (token uid 스코프) → (a) status 필드 정확히 'done' (failed/pending/부재는 stale key 남아도 404), (b) key exact equality: correctedPose = f"results/{uid}/{analysis_id}/corrected_pose_{correctedPoseJoint}.png" (joint 부재 404), rotation = f"results/{uid}/{analysis_id}/rotation.mp4" — 전체 문자열 일치, (c) 확장자별 ResponseContentType presign ExpiresIn=3600 → {playbackUrl, expiresInSec:3600}. 위반 전부 guards_ok 단일 404. asset 미지정 = 기존 동작 바이트 동일. test_visual_url.py: fresh 재서명, 타 uid 404, 필드 부재 404, status failed+key 잔존 404, basename 불일치 404, joint 부재 404, 잘못된 asset 400, 미지정 무회귀, 무토큰 401.</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31/test_visual_url.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - key 검증이 exact equality (M2-01 테스트 고정)
    - status done 강제 — failed stale key 404 green
    - validation.py 공유 validator + playback-url import (인라인 중복 0)
    - asset 경로가 클라이언트 key 미수신
  </acceptance_criteria>
  <done>H-02 재서명 + M2-01 exact guard 완성.</done>
</task>

<task type="auto">
  <name>Task 2: visual-request Lambda — flag·원자 예약(pending+outbox)·best-effort dispatch(outboxSeq)</name>
  <files>backend/functions/visual-request/app.py, backend/functions/visual-request/requirements.txt, backend/tests/phase31/test_visual_request.py</files>
  <read_first>backend/functions/playback-url/app.py (auth/파싱/guards_ok 골격), backend/functions/playback-url/requirements.txt + backend/functions/pipeline/requirements.txt (pyyaml deploy fix 주석 2건), backend/shared/python/sunity_shared/firestore_admin.py (reserve_visual_job 반환 job(outboxSeq 포함) / mark_visual_job_dispatched(action+seq) — 31-02), docs/contract.md 의 POST /visual/rotation 절</read_first>
  <action>visual-request/app.py 신설 — 흐름: (1) env VISUAL_JOBS_ENABLED != "true" → 503 'feature_disabled', (2) verify_request → 401, (3) validate_analysis_id_format → 400, (4) 분석 문서 존재·소유·status=='done' → guards_ok 단일 404, (5) rotationStatus=='done' → 200 멱등, (6) rotationStatus=='pending' + rotationUpdatedAtMs 20분 이내 → 202 dedupe(SQS 미발행 — 전용 timestamp H-06), (7) `_parse_limit` fail-closed(파싱 불가→0=전부 429 M-06) + KST date_key, (8) reserve_visual_job(uid, analysis_id, 'rotation', date_key, user_limit 기본 3, global_limit 기본 30, allow_retry_failed=True) — **reserve transaction 이 job + analysis rotationStatus 'pending' + nextAction='create'/dispatchState='pending'/outboxSeq=1 원자 (B3-03)**; daily_limit → 429; created False + 기존 진행중('creating'|'polling'|'retry_ready'|'fetching') → 202 no-op, (9) **best-effort dispatch: created True 또는 기존 dispatchState=='pending' → sqs.send_message({"jobId","generation","action":"create","outboxSeq": reserve 반환 job 의 outboxSeq}) → 성공 시 mark_visual_job_dispatched(expect_action='create', expect_outbox_seq=outboxSeq); send 실패 → log.warning + 202 (500 아님 — dispatcher 복구 H3-09)** → 202. requirements.txt = firebase-admin + pyyaml (주석 2건). test_visual_request.py: flag OFF 503 + SQS 0; 401/400/404; done 멱등 200; pending 20분 내 202 + SQS 0; 429 daily_limit; env "abc" → 429; **SQS send 실패 → 202 + dispatchState 'pending' 잔존 + 재요청 quota 0 + 재발행**; **메시지에 outboxSeq 포함 + mark 가 action+seq CAS (B4-01) assert**; **reserve 가 analysis pending 을 transaction 에 기록 — Lambda 소스에 update_analysis_visual/별도 pending write 0 (grep)**; run_contended 동시 → job 1 + quota +1 + SQS<=2; failed job 재요청 → generation 2 + quota 재소비.</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31/test_visual_request.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - pending 기록이 reserve transaction 내부뿐 — Lambda 별도 write 0 (grep)
    - 메시지 outboxSeq 포함 + mark(action+seq) 테스트 green (B4-01)
    - send 실패 → 202 + outbox pending 잔존 green (H3-09)
    - dedupe/타임아웃이 rotationUpdatedAtMs 만 사용 + env fail-closed 429 + flag OFF 503
    - requirements.txt pyyaml 주석 2건
  </acceptance_criteria>
  <done>온디맨드 진입점이 원자 예약(pending+outbox seq)·dispatcher 복구·flag·KST 한도까지 테스트 고정.</done>
</task>

<task type="auto">
  <name>Task 3: pipeline enqueue(upload-first+full-hash+VersionId) + template(H2-01/H3-09/B4-05) + versioning/ObjectLock dry-run(H4-03/H4-04)</name>
  <files>backend/functions/pipeline/app.py, backend/template.yaml, backend/scripts/visual_infra_dryrun.py, backend/tests/phase31/test_visual_dispatch.py</files>
  <read_first>backend/functions/pipeline/app.py lines 3116-3158 (_run_deferred_fault_zoom 비차단) + 4760-4800 (fault-zoom 조건부 위치 — 31-03 이 먼저 수정하므로 최신 재확인), backend/shared/python/sunity_shared/analysis/fault_zoom.py (build_corrected_pose_target/CorrectedPoseTarget.to_payload — 31-03), backend/shared/python/sunity_shared/analysis/pair_store.py (BLUR_OPTION — 31-07, M3-02), backend/template.yaml (전체 — 함수/큐/LogGroup 선례 + SSM Statement 주석), .planning/phases/31-api-visual-correction/31-PLAN-REVIEW-ITERATION4.md 의 B4-05/H4-03/H4-04/M4-04 절 + smoke/CALIBRATION.json (4 게이트 값)</read_first>
  <action>(A) pipeline/app.py `_enqueue_corrected_pose_job(*, uid, analysis_id, target: CorrectedPoseTarget, src_png: bytes) -> None` — complete_analysis 이후 fault-zoom 조건부 밖 (H-01). target None → 생략 (B2-01). 내부 (**B3-03 순서 계약**): env flag OFF → return; sourceHash = sha256(src_png).hexdigest() (full hex), keySeg = sourceHash[:16], srcKey = f"visual-input/{uid}/{analysis_id}/{keySeg}.png". **(1) upload-first + M4-04 대조: srcKey head_object 시도 → 존재하면 object Metadata['sha256'] (full hex) 를 읽어 신규 full sourceHash 와 exact 비교 — 불일치 → log.error("srcKey collision/tampering — 16hex 충돌 또는 변조") + return (job 0·vendor 0); 일치하면 재사용. 부재하면 put_object(srcKey, Body=src_png, ContentType image/png, Metadata={'sha256': full_hex}). put/head 실패 → log.warning + return (분석 비차단). put/head 응답 또는 후속 head_object 에서 `sourceVersionId` 확보 (H4-03 — versioned cleanup 대상)**; blur option-b 면 trainingSrcKey put(+ trainingVersionId) (Pod blur — H2-10; 분기 = pair_store.BLUR_OPTION 상수, .planning 런타임 읽기 0 — M3-02); **(2) reserve_visual_job(kind 'correctedPose', allow_retry_failed=False, payload = target.to_payload() + {"srcKey","sourceHash"(full),"sourceVersionId"(+trainingSrcKey/trainingVersionId)}) — job + analysis correctedPoseStatus 'pending' + nextAction='create'/dispatchState='pending'/outboxSeq=1 원자 (B3-03)**; **(3) created True → best-effort sqs send({"jobId","generation","action":"create","outboxSeq": job.outboxSeq}) + mark_visual_job_dispatched(expect_action='create', expect_outbox_seq=outboxSeq) — send 실패 log 만**. created False 분기: existing done → no-op (srcKey 재put 은 head 대조 재사용); existing 진행중(creating|polling|retry_ready|fetching|judging|pose_checking|postprocessing) → no-op; existing reserved + dispatchState 'pending' → sourceHash 동일하면 best-effort 재발행(현 outboxSeq), 다르면 log 후 no-op (실행 중 입력 교체 금지 — B2-03); existing failed → no-op. 전체 try 재raise 0. DashScope/Gemini/pose 호출 0. Pod 런타임 전제 주석: Pod env VISUAL_QUEUE_URL/VISUAL_JOBS_ENABLED + credential sqs:SendMessage — 31-12 검증.
(B) template.yaml: `VisualQueue` VisibilityTimeout 1800(=6×300 H2-01) + `VisualDLQ` maxReceiveCount 5 + `VisualDLQAlarm`; `VisualRequestFunction`(Timeout 10, POST /visual/rotation, env FIREBASE_SA_PARAM/VISUAL_QUEUE_URL/VISUAL_JOBS_ENABLED(!Ref)/ROTATION_DAILY_LIMIT "3"/ROTATION_GLOBAL_DAILY_LIMIT "30", Policies SSM + VisualQueue sqs:SendMessage); `VisualWorkerFunction`(Timeout 300, ReservedConcurrentExecutions 2, MemorySize = 31-05 subprocess RSS 실측(Linux/container) 근거 주석, SQS 이벤트 BatchSize 1 + FunctionResponseTypes [ReportBatchItemFailures], **Schedule 이벤트 없음 — H3-09**, env VIDEO_BUCKET/FIREBASE_SA_PARAM/VISUAL_QUEUE_URL/DASHSCOPE_API_KEY resolve:ssm/PAIR_ID_HMAC_KEYS resolve:ssm/RUNPOD_ANALYZE_URL/RUNPOD_AUTH_TOKEN/GEMINI_API_KEY resolve:ssm(워커 전용)/**DISPLAY_JUDGE_CONFIDENCE·TRAINING_JUDGE_CONFIDENCE·DISPLAY_POSE_TOL_DEG·TRAINING_POSE_TOL_DEG (4 게이트 — 31-13 CALIBRATION chosen 값, 주석에 CALIBRATION.json 경로 + B4-05 인용)**, Policies SSM + s3 Get(uploads/*, visual-input/*, results/*) + Put/Delete(visual-input/*) + Put(results/*, training/*) + 자기 큐 sqs:SendMessage); **`VisualDispatchFunction` (H3-09): CodeUri visual-dispatch/, Timeout 60, MemorySize 256, ReservedConcurrentExecutions 1, Events Schedule rate(1 minute), env FIREBASE_SA_PARAM/VISUAL_QUEUE_URL, Policies SSM + VisualQueue sqs:SendMessage + cloudwatch:PutMetricData**; **`VisualOutboxAgeAlarm` Namespace 'SunityVisual' MetricName 'OutboxPendingAgeMs' >= 900000 ALARM**; PipelineFunction — env VISUAL_QUEUE_URL/VISUAL_JOBS_ENABLED + Policies VisualQueue sqs:SendMessage (H2-02); Parameters VisualJobsEnabled Default "false"; LogGroup 3종 Retention 30.
(C) visual_infra_dryrun.py (H2-09 + **H4-03/H4-04/H3-08**): (1) **`get-bucket-versioning` + `get-object-lock-configuration` 조회(read-only) — 결과 JSON 기록**; **Object Lock fail-closed 분기 (H4-04): ObjectLockEnabled=='Enabled' + default Retention 존재 → 결과에 {"blocked": true, "reason": "object_lock_default_retention — 민감 prefix 는 별도 non-locked bucket 필요"} 기록(민감 prefix 를 이 bucket 에 쓰지 않도록 지시); Enabled + default retention 없음 → 결과에 canary 검증 필요 플래그(dry-run 은 조회만, 실 canary version retention/delete 는 31-12 checkpoint)**; (2) `get-bucket-lifecycle-configuration` → 기존 규칙 + visual-input/ 1일 Expiration + training/phase31/pairs/ retentionDays Expiration merge — **versioning enabled/suspended 면 두 민감 prefix 에 NoncurrentVersionExpiration(visual-input NoncurrentDays 1, pairs NoncurrentDays retentionDays) + ExpiredObjectDeleteMarker 규칙 추가 (H3-08)**; 결과를 `.planning/phases/31-api-visual-correction/infra/lifecycle_merged.json` + before `lifecycle_before.json` 저장; (3) SSM `/sunity/motion/pair-id-hmac-keys` get 시도(read-only) — 존재 시 31-07 validate_hmac_key_set 검증, 부재 시 생성 지시문. put 계열 코드 없음(31-12 전용). retentionDays 는 privacy_decision.json 로컬 읽기(로컬 dev 도구 — M3-02 위반 아님 주석).
(D) test_visual_dispatch.py: flag OFF → S3/SQS 0; target None → 생략; **B3-03 순서: S3 head/put 이 reserve 보다 먼저 (call order assert)**; **M4-04: 기존 srcKey head 의 Metadata sha256 == 신규 → 재사용; 불일치 → block(reserve 0·SQS 0·log)**; created True → put(Metadata sha256) + sourceVersionId payload + reserve + SQS 1(outboxSeq 포함) + mark(action+seq); **pending write reserve 내부뿐 (grep)**; **S3 put 예외 → reserve 0·SQS 0·비차단**; reserve 후 send crash → dispatchState 'pending' 잔존 + dispatcher 발행 → vendor create 1회; reserve commit_lost → 재실행 created False + 일관; worker done 후 producer 재실행 → done 유지(write 0); duplicate-while-progress → no-op; run_contended → job 1·SQS<=2; sourceHash 불일치 reserved → no-op; 훅 내 visual_gen import 부재; **template 파싱 assert (H2-01/H3-09/B4-05): VisualQueue VisibilityTimeout >= 6×Worker Timeout, maxReceiveCount >= 5, ReportBatchItemFailures, PipelineFunction SendMessage, VisualDispatchFunction ReservedConcurrentExecutions==1 + Schedule rate(1 minute), VisualWorkerFunction Schedule 부재, VisualOutboxAgeAlarm, 4 게이트 env 존재; build gate: template env 의 TRAINING_JUDGE_CONFIDENCE > DISPLAY_JUDGE_CONFIDENCE AND TRAINING_POSE_TOL_DEG < DISPLAY_POSE_TOL_DEG (CALIBRATION chosen 값 파싱해 strict ordering assert — 위반 시 fail, M4-03: stricter grid 없으면 calibration 이 애초 blocked)**.
실행 커밋 순서: (A/D) → (B template) → (C dryrun) 3분할.</action>
  <verify>
    <automated>(cd backend && sam validate --lint) && python -m pytest backend/tests/phase31/test_visual_dispatch.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - S3 head/put → reserve → send 순서 + full-sha256 Metadata 대조(불일치 block) + sourceVersionId payload (B3-03/M4-04/H4-03)
    - send 후 analysis pending write 부재 (grep) + worker-done 경쟁 green (gate 5)
    - reserve 분기(done/진행중/reserved/failed) + commit_lost + run_contended green
    - template 파싱: visibility/redrive + DispatchFunction concurrency 1 + worker schedule 부재 + OutboxAge alarm + 4 게이트 env + build gate strict ordering (H2-01/H3-09/B4-05)
    - dry-run 에 put 호출 0 + versioning/ObjectLock 조회 + ObjectLock fail-closed 분기 + NoncurrentVersionExpiration merge (H2-09/H4-03/H4-04/H3-08)
    - visual_infra_dryrun.py 1회 실행 완료 — infra/lifecycle_before.json + lifecycle_merged.json 존재 (31-12 T2 전제)
    - `(cd backend && sam validate --lint)` && `python -m pytest backend/tests -q` exits 0
  </acceptance_criteria>
  <done>분석 경로가 upload-first + full-hash 대조 + versioned cleanup 배선, IaC 가 dispatcher 분리·alarm·4 게이트 strict ordering·ObjectLock 인지까지 배포 준비 완료.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 앱 → POST /visual/rotation | 미신뢰 입력 + 과금 유발 |
| 앱 → playback-url asset | key 는 서버 선택 exact 계약 — 클라이언트 key 0 |
| _process → SQS | upload-first + full-hash 대조 + reserve 분기 + enqueue 만 |
| IaC → 실 스택 | flag 기본 OFF + 라이브 mutation 은 31-12 checkpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-38 | Spoofing | visual-request 인증 | mitigate | verify_request Firebase ID token → 401 |
| T-31-39 | Elevation of Privilege | 타인 analysisId | mitigate | uid 토큰 스코프 + guards_ok 단일 404 |
| T-31-40 | Denial of Service (과금) | 온디맨드 남용 | mitigate | transaction 한도(KST) + dedupe + done 멱등 + fail-closed env (H-06/M-06) |
| T-31-41 | Information Disclosure | 만료/임의/stale key 서명 | mitigate | exact basename + status done + content-type + 1h TTL (H-02 + M2-01) |
| T-31-42 | Denial of Service | dispatch 실패 고아 quota | mitigate | durable outbox(seq) + VisualDispatchFunction 복구 + OutboxAge alarm (B4-01 + H3-09) |
| T-31-43 | Information Disclosure | DASHSCOPE/GEMINI/HMAC 키 | mitigate | resolve:ssm SecureString, GEMINI_API_KEY 워커 전용 |
| T-31-44 | Tampering | 미검증 기능 전면 노출 | mitigate | VisualJobsEnabled 기본 false (H-07) |
| T-31-54 | Tampering | replay 완료 되감기/입력 교체/16hex 충돌 | mitigate | upload-first + reserve 내 pending + immutable hash key + full-sha256 head 대조 (B2-03 + B3-03 + M4-04) |
| T-31-55 | Tampering | 라이브 lifecycle 교체 규칙 소실 / noncurrent PII 잔존 / Object Lock 삭제 불능 | mitigate | dry-run merge + versioning 조회 + NoncurrentVersionExpiration + Object Lock fail-closed 분기 — put 은 31-12 (H2-09 + H4-03 + H4-04) |
</threat_model>

<verification>
- `python -m pytest backend/tests -q` green + `(cd backend && sam validate --lint)` green
- 응답 코드가 contract.md /visual/rotation 절과 일치
- 라이브 AWS 변경 0 (스크립트 read-only — H2-09)
- Lambda/pipeline 코드에 .planning 경로 참조 0 (M3-02 — dry-run 로컬 스크립트 제외)
</verification>

<success_criteria>
- B3-03(upload-first + reserve 내 pending)/M4-04(full-hash 대조)/H4-03(VersionId cleanup)/B4-01(outboxSeq 메시지)/B4-05(4 게이트 env + strict ordering build gate)/H4-04(ObjectLock fail-closed)/H3-08/H3-09/M3-02/H2-01/M2-01/H2-09 전부 구현+테스트
- 4차 리뷰 §8 승인 조건 4·5·7 이 이 플랜 테스트로 추적 가능
</success_criteria>

<output>
Create `.planning/phases/31-api-visual-correction/31-10-SUMMARY.md` when done
</output>
</content>
</invoke>
