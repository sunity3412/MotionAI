---
phase: 31-api-visual-correction
plan: 10
type: execute
wave: 5
depends_on: [31-02, 31-03, 31-04, 31-05, 31-09, 31-13]
files_modified:
  - backend/shared/python/sunity_shared/validation.py
  - backend/functions/playback-url/app.py
  - backend/functions/visual-request/app.py
  - backend/functions/visual-request/requirements.txt
  - backend/functions/pipeline/app.py
  - backend/template.yaml
  - backend/scripts/visual_infra_dryrun.py
  - backend/tests/phase31/test_visual_url.py
  - backend/tests/phase31/test_visual_request.py
  - backend/tests/phase31/test_visual_dispatch.py
autonomous: true
requirements: [D-05, D-06, D-07, D-08]
must_haves:
  truths:
    - "correctedPose enqueue 는 upload-first — (1) hash immutable srcKey S3 put/head 검증 먼저, (2) 그 다음 단일 transaction 으로 reserve + analysis 'pending' + nextAction='create'/dispatchState='pending' 동시 기록, (3) SQS send 는 그 후 best-effort. input 없는 create action 이 순서상 불가능하다 (3차 리뷰 B3-03 권장안 A)"
    - "SQS send 후 analysis pending write 가 존재하지 않는다 — pending 은 reserve transaction 안에서만 기록되므로 늦은 pending 이 worker done 을 되감을 수 없다 (B3-03, §8-3 gate 5)"
    - "send 실패 = dispatcher 복구 — dispatchState='pending' 이 durable 하게 남아 있고 별도 VisualDispatchFunction(reserved concurrency 1)이 발행한다 (3차 리뷰 H3-09)"
    - "reserve 반환이 유일한 분기 기준 — existing done/진행중 은 상태 write 0·SQS 0 (duplicate done 의 동일 hash object 재확인/재put 은 허용 — 1일 lifecycle 정리) (2차 리뷰 B2-03 + B3-03)"
    - "queue visibility >= 6 × worker timeout, maxReceiveCount >= 5 — 배포 테스트가 template 파싱으로 assert (2차 리뷰 H2-01)"
    - "dispatcher pending outbox age metric 에 alarm 이 존재한다 — template 파싱 assert (H3-09)"
    - "31-10 은 라이브 인프라를 변경하지 않는다 — lifecycle merge/SSM 키는 dry-run artifact 생성까지만, 실제 적용은 31-12 checkpoint (2차 리뷰 H2-09)"
    - "dry-run 이 bucket versioning/Object Lock 을 조회하고, versioning enabled/suspended 면 민감 prefix 에 NoncurrentVersionExpiration + expired delete marker 정리 규칙을 merge 에 포함한다 (3차 리뷰 H3-08)"
    - "privacy 결정값은 배포 config/상수로 컴파일된다 — .planning JSON 런타임 읽기 0, build 테스트가 JSON 과 일치 검사 (3차 리뷰 M3-02)"
    - "playback-url asset guard 는 exact basename + status done + content-type — stale key 남은 failed 문서는 404 (2차 리뷰 M2-01)"
    - "VisualJobsEnabled feature flag 기본 OFF, quota env fail-closed, analysisId 공유 validator (H-07/M-06/L-03)"
    - "D-05: 교정 이미지(correctedPose)는 분석 완료 시 자동 생성 — top-1 결함 부위, _process 는 enqueue 만"
    - "D-07: 비용 가드 = 사용자당 일일 생성 한도(회전 3/일) + 전역 안전핀(30/일) — transaction 예약, fail-closed env"
  artifacts:
    - path: "backend/functions/visual-request/app.py"
      provides: "POST /visual/rotation — auth·flag·transaction(reserve=pending+outbox)·best-effort dispatch·202"
      contains: "def lambda_handler"
    - path: "backend/functions/playback-url/app.py"
      provides: "asset 재서명 확장 (exact basename + done guard, 1h)"
      contains: "asset"
    - path: "backend/functions/pipeline/app.py"
      provides: "_enqueue_corrected_pose_job — upload-first + CorrectedPoseTarget 소비, replay-safe"
      contains: "_enqueue_corrected_pose_job"
    - path: "backend/template.yaml"
      provides: "VisualQueue/DLQ alarm/VisualRequestFunction/VisualWorkerFunction/VisualDispatchFunction(concurrency 1+schedule)/OutboxAge alarm/flag 파라미터"
      contains: "VisualDispatchFunction"
    - path: "backend/scripts/visual_infra_dryrun.py"
      provides: "versioning-aware lifecycle merge dry-run artifact + SSM 키 존재 점검 (라이브 mutation 0)"
  key_links:
    - from: "backend/functions/pipeline/app.py"
      to: "backend/shared/python/sunity_shared/analysis/fault_zoom.py"
      via: "build_corrected_pose_target — target 계약 유일 소비 (B2-01)"
      pattern: "build_corrected_pose_target"
    - from: "backend/functions/visual-request/app.py"
      to: "backend/shared/python/sunity_shared/firestore_admin.py"
      via: "reserve_visual_job transaction(pending+outbox) + mark_visual_job_dispatched"
      pattern: "reserve_visual_job"
    - from: "backend/template.yaml"
      to: "SSM /sunity/motion/dashscope-api-key"
      via: "resolve:ssm dynamic reference"
      pattern: "dashscope-api-key"
---

<objective>
HTTP 표면·분석측 dispatch·IaC 를 완성한다 — 31-09 워커/디스패처 코드가 존재한 뒤 실행되는 wave 5 (H2-08: template CodeUri 의존).

(1) playback-url asset 확장 — H-02 재서명 + M2-01 exact guard. (2) visual-request Lambda — reserve transaction 이 analysis pending + 초기 outbox 를 함께 기록하므로 send 는 순수 best-effort. (3) pipeline enqueue 훅 — **B3-03 해소 (권장안 A upload-first): 3차 개정의 "reserve(outbox 활성) → S3 put → send → pending write" 순서는 (a) input 없는 reserved job 을 dispatcher 가 정상 발행하고 (b) 늦은 pending write 가 worker done 을 되감는 두 결함을 남겼다 — 폐기.** 새 순서 = S3 put/head 먼저 → reserve transaction(job+pending+outbox 원자) → best-effort send. target 은 31-03 `build_corrected_pose_target` 의 CorrectedPoseTarget 만 소비 (B2-01). (4) template.yaml IaC — H2-01 권고 + **VisualDispatchFunction 별도 Lambda (H3-09: 전용 reserved concurrency 1 + rate(1 minute) schedule + OutboxPendingAgeMs alarm)** — worker 의 sweeper schedule 은 존재하지 않는다. (5) 라이브 mutation 금지 (H2-09) + **dry-run 의 versioning 인지 (H3-08)**. (6) privacy 결정값은 build-time 상수/template env 로만 — 런타임 .planning 읽기 금지 (M3-02).

Purpose: 요청·자동생성 양 진입점이 동일 durable substrate 로 흐르는 마지막 backend 배선 — replay·crash·경쟁에도 안전하게.
Output: Lambda 3종 수정/신설 + pipeline 훅 + IaC + dry-run 스크립트 + 테스트 3파일.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-api-visual-correction/31-PATTERNS.md
@docs/contract.md
@.planning/phases/31-api-visual-correction/31-PLAN-REVIEW-ITERATION3.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 공유 validator + playback-url asset 재서명 (exact basename + done guard — M2-01)</name>
  <files>backend/shared/python/sunity_shared/validation.py, backend/functions/playback-url/app.py, backend/tests/phase31/test_visual_url.py</files>
  <read_first>backend/functions/playback-url/app.py (전체 — analysisId alnum+len>=16 가드, guards_ok 단일 404, _sign_get, _PLAYBACK_EXPIRES), backend/shared/python/sunity_shared/validation.py (기존 validator 관례 — 순수 함수·boto3 무관), docs/contract.md 의 playback-url asset 확장 절 (31-04 — 단일 기준)</read_first>
  <action>validation.py 에 `validate_analysis_id_format(analysis_id) -> bool` 신설 (isalnum and len>=16 이관 — 리뷰 L-03). playback-url/app.py: 인라인 검사를 공유 validator 로 교체 + optional `asset` 처리 — 값이 'correctedPose'|'rotation' 외면 400; asset 지정 시 (M2-01 전면): 분석 문서 read (token uid 스코프) → (a) 해당 status 필드(correctedPoseStatus/rotationStatus)가 정확히 'done' 인지 — 'failed'/'pending'/부재는 stale key 가 남아 있어도 404, (b) key 가 exact 계약과 equality: correctedPose = f"results/{uid}/{analysis_id}/corrected_pose_{correctedPoseJoint}.png" (joint 부재 시 404), rotation = f"results/{uid}/{analysis_id}/rotation.mp4" — prefix startswith 아닌 전체 문자열 일치, (c) 확장자별 ResponseContentType(png=image/png, mp4=video/mp4) presign ExpiresIn=3600 (`_ASSET_EXPIRES`) → {playbackUrl, expiresInSec: 3600}. 문서 부재/타인/조건 위반 전부 guards_ok 단일 404. asset 미지정 = 기존 동작 바이트 동일. test_visual_url.py: fresh URL 재서명 (H-02), 타 uid 404, 필드 부재 404, status failed + key 잔존 → 404 (M2-01), basename 불일치 404, joint 부재 404, 잘못된 asset 400, 미지정 무회귀, 무토큰 401.</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31/test_visual_url.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - key 검증이 exact equality (startswith 단독 아님 — M2-01 테스트 고정)
    - status done 강제 — failed 문서 stale key 404 테스트 green
    - validation.py 공유 validator + playback-url import (인라인 중복 0)
    - asset 경로가 클라이언트 key 를 받지 않음
  </acceptance_criteria>
  <done>H-02 재서명 + M2-01 exact guard 완성.</done>
</task>

<task type="auto">
  <name>Task 2: visual-request Lambda — flag·원자 예약(pending+outbox)·best-effort dispatch</name>
  <files>backend/functions/visual-request/app.py, backend/functions/visual-request/requirements.txt, backend/tests/phase31/test_visual_request.py</files>
  <read_first>backend/functions/playback-url/app.py (auth/파싱/guards_ok 골격), backend/functions/playback-url/requirements.txt + backend/functions/pipeline/requirements.txt (pyyaml deploy fix 주석 2건 원문), backend/shared/python/sunity_shared/firestore_admin.py (reserve_visual_job — analysis pending+outbox 원자 / mark_visual_job_dispatched — 31-02 시그니처), docs/contract.md 의 POST /visual/rotation 절</read_first>
  <action>visual-request/app.py 신설 — 흐름: (1) env `VISUAL_JOBS_ENABLED` != "true" → 503 'feature_disabled', (2) verify_request → 401, (3) validate_analysis_id_format → 400, (4) 분석 문서 존재·소유·status=='done' → guards_ok 단일 404, (5) rotationStatus=='done' → 200 멱등, (6) rotationStatus=='pending' + rotationUpdatedAtMs 20분 이내 → 202 dedupe (SQS 미발행 — 전용 timestamp, H-06), (7) `_parse_limit` fail-closed (파싱 불가 → 0 = 전부 429, M-06) + KST date_key, (8) reserve_visual_job(uid, analysis_id, 'rotation', date_key, user_limit 기본 3, global_limit 기본 30, allow_retry_failed=True — 사용자 명시 재시도는 generation+1 재예약) — **reserve transaction 이 job + analysis rotationStatus 'pending' + nextAction='create'/dispatchState='pending' 을 원자 기록 (B3-03: 이 Lambda 에는 별도 pending write 가 없다)**; daily_limit → 429; created False + 기존 state 진행중('creating'|'polling'|'retry_ready'|'fetching') → 202 no-op 멱등, (9) **best-effort dispatch: created True 또는 기존 dispatchState=='pending' → sqs.send_message({"jobId","action":"create","generation"}) → 성공 시 mark_visual_job_dispatched; send 실패 → log.warning 후 그대로 202 (500 아님 — durable outbox 를 VisualDispatchFunction 이 복구, H3-09; 'dispatch_failed' state/함수는 존재하지 않음)** → 202. requirements.txt = firebase-admin + pyyaml (deploy fix 주석 2건 인용). test_visual_request.py: flag OFF 503 + SQS 0; 401/400/404; done 멱등 200; pending 20분 내 202 + SQS 0; 429 daily_limit; env "abc" → 429 fail-closed; **SQS send 실패 → 202 + dispatchState 'pending' 잔존 (dispatcher 복구 가능) + 재요청 시 quota 증가 0 + 재발행**; **reserve 가 analysis pending 을 같은 transaction 에 기록 — Lambda 소스에 update_analysis_visual/별도 pending write 호출 0 (grep)**; run_contended 동시 요청 → job 1개 + quota +1 + SQS <= 2 (중복 send 무해 — worker CAS); failed job 재요청 → generation 2 + quota 재소비.</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31/test_visual_request.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - pending 기록이 reserve transaction 내부뿐 — Lambda 소스에 별도 analysis write 0 (B3-03 grep 고정)
    - send 실패 → 202 + outbox pending 잔존 테스트 green (H3-09 복구 계약)
    - dedupe/타임아웃 판정이 rotationUpdatedAtMs 만 사용
    - env fail-closed 429 + flag OFF 503 테스트 green
    - requirements.txt pyyaml 주석 2건 인용
  </acceptance_criteria>
  <done>온디맨드 진입점이 원자 예약(pending+outbox)·dispatcher 복구·flag·KST 한도까지 테스트 고정.</done>
</task>

<task type="auto">
  <name>Task 3: pipeline enqueue upload-first (B3-03) + template.yaml IaC (H2-01/H3-09) + versioning-aware dry-run (H2-09/H3-08)</name>
  <files>backend/functions/pipeline/app.py, backend/template.yaml, backend/scripts/visual_infra_dryrun.py, backend/tests/phase31/test_visual_dispatch.py</files>
  <read_first>backend/functions/pipeline/app.py lines 3116-3158 (_run_deferred_fault_zoom — 비차단 규율) + 4760-4800 (fault-zoom 조건부 위치 — 훅은 조건부 밖; 31-03 이 같은 파일을 먼저 수정하므로 최신 상태 기준 재확인), backend/shared/python/sunity_shared/analysis/fault_zoom.py (build_corrected_pose_target/CorrectedPoseTarget.to_payload — 31-03), backend/shared/python/sunity_shared/analysis/pair_store.py (BLUR_OPTION 상수 — 31-07, M3-02), backend/template.yaml (전체 — 함수/큐/LogGroup 선례 + SSM Statement 주석), .planning/phases/31-api-visual-correction/31-PLAN-REVIEW-ITERATION3.md 의 B3-03/H3-08/H3-09 절</read_first>
  <action>(A) pipeline/app.py `_enqueue_corrected_pose_job(*, uid, analysis_id, target: CorrectedPoseTarget, src_png: bytes) -> None` — complete_analysis 이후 fault-zoom 조건부 밖 배치 (H-01). target = 31-03 build_corrected_pose_target 반환 — None 이면 호출 생략 (감점 record 직접 계산 금지 — B2-01). 내부 (**B3-03 권장안 A 순서 — 이 순서 자체가 계약**): env flag OFF → return; sourceHash = sha256(src_png).hexdigest() (full hex), keySeg = sourceHash[:16], srcKey = f"visual-input/{uid}/{analysis_id}/{keySeg}.png" (immutable); **(1) S3 put(srcKey) 먼저 — 이미 존재하면 head 검증으로 대체 (동일 hash = 동일 내용, 재put 무해); put/head 실패 → log.warning 후 return (job 0·vendor 0 — 일시 S3 실패가 기능 실패로 승격되지 않고 분석은 비차단)**; blur option-b 면 trainingSrcKey = f"...{keySeg}_blur.png" 동시 put (Pod 런타임 blur — H2-10; **분기 기준 = pair_store.BLUR_OPTION 배포 상수, .planning JSON 런타임 읽기 0 — M3-02**); **(2) reserve_visual_job(kind 'correctedPose', allow_retry_failed=False, payload = target.to_payload() + {"srcKey", "sourceHash"(+trainingSrcKey)}) — 이 transaction 이 job + analysis correctedPoseStatus 'pending' + nextAction='create'/dispatchState='pending' 을 원자 기록 (B3-03: 이 함수에 별도 pending write 없음)**; **(3) created True → best-effort sqs send({"jobId","action":"create","generation"}) + mark_visual_job_dispatched — send 실패는 log 만 (dispatcher 복구)**. created False 분기: existing done → 완전 no-op (상태 write 0·SQS 0 — srcKey put 은 이미 일어났으나 동일 hash object 재확인/재put 은 허용, 1일 lifecycle 정리 — B3-03 리뷰 명시); existing 진행중(creating|polling|retry_ready|fetching|judging|pose_checking) → no-op; existing reserved + dispatchState 'pending' → sourceHash 동일하면 best-effort 재발행만, 다르면 log 후 no-op (실행 중 job 입력 교체 금지 — B2-03); existing failed → no-op. 전체 try 재raise 0 (비차단). DashScope/Gemini/pose 호출 0. Pod 런타임 전제 주석: Pod env VISUAL_QUEUE_URL/VISUAL_JOBS_ENABLED + Pod credential 의 sqs:SendMessage — 31-12 검증.
(B) template.yaml: `VisualQueue` — VisibilityTimeout 1800 (= 6 × worker Timeout 300 — H2-01) + `VisualDLQ` RedrivePolicy maxReceiveCount 5 + `VisualDLQAlarm`; `VisualRequestFunction` (Timeout 10, POST /visual/rotation, env FIREBASE_SA_PARAM/VISUAL_QUEUE_URL/VISUAL_JOBS_ENABLED(!Ref)/ROTATION_DAILY_LIMIT "3"/ROTATION_GLOBAL_DAILY_LIMIT "30", Policies: SSM + VisualQueue sqs:SendMessage); `VisualWorkerFunction` (Timeout 300, ReservedConcurrentExecutions 2, MemorySize = 31-05 subprocess RSS 실측(Linux/container 기준 — H3-06) 근거 주석 인용, SQS 이벤트 BatchSize 1 + FunctionResponseTypes [ReportBatchItemFailures], **Schedule 이벤트 없음 (sweeper 는 worker 소관이 아님 — H3-09)**, env VIDEO_BUCKET/FIREBASE_SA_PARAM/VISUAL_QUEUE_URL/DASHSCOPE_API_KEY resolve:ssm/PAIR_ID_HMAC_KEYS resolve:ssm/RUNPOD_ANALYZE_URL/RUNPOD_AUTH_TOKEN/GEMINI_API_KEY resolve:ssm(워커 전용)/DISPLAY_POSE_TOL_DEG·TRAINING_POSE_TOL_DEG·DISPLAY_JUDGE_CONFIDENCE (31-13 CALIBRATION 채택값 — 주석에 CALIBRATION.json 경로 인용), Policies: SSM + s3 Get(uploads/*, visual-input/*, results/*) + Put/Delete(visual-input/*) + Put(results/*, training/*) + 자기 큐 sqs:SendMessage); **`VisualDispatchFunction` 신설 (H3-09): CodeUri visual-dispatch/, Timeout 60, MemorySize 256, ReservedConcurrentExecutions 1 (전용 — worker 와 미공유), Events: Schedule rate(1 minute), env FIREBASE_SA_PARAM/VISUAL_QUEUE_URL, Policies: SSM + VisualQueue sqs:SendMessage + cloudwatch:PutMetricData**; **`VisualOutboxAgeAlarm` — Namespace 'SunityVisual' MetricName 'OutboxPendingAgeMs' >= 900000 (15분) 시 ALARM (액션 없이 가시화)**; PipelineFunction — env VISUAL_QUEUE_URL/VISUAL_JOBS_ENABLED 추가 + Policies 에 VisualQueue sqs:SendMessage 명시 (H2-02); Parameters `VisualJobsEnabled` Default "false"; LogGroup 3종 RetentionInDays 30.
(C) visual_infra_dryrun.py (H2-09 + **H3-08**): (1) **`get-bucket-versioning` + `get-object-lock-configuration` 조회 (read-only) — 상태를 결과 JSON 에 기록**; (2) `get-bucket-lifecycle-configuration` 읽기 → 기존 규칙 + visual-input/ 1일 Expiration + training/phase31/pairs/ retentionDays Expiration 을 merge — **versioning 이 enabled/suspended 면 두 민감 prefix 에 NoncurrentVersionExpiration(visual-input NoncurrentDays 1, pairs NoncurrentDays retentionDays) + ExpiredObjectDeleteMarker 정리 규칙 추가 (H3-08: current Expiration 은 noncurrent 를 지우지 않음 — AWS 공식)**; 결과를 `.planning/phases/31-api-visual-correction/infra/lifecycle_merged.json` + before 원본 `lifecycle_before.json` 으로 저장; (3) SSM `/sunity/motion/pair-id-hmac-keys` 존재 여부 get 시도(read-only) — 존재 시 **31-07 `validate_hmac_key_set` 공유 validator 로 형식 검증 (H3-11 단일 소유)**, 부재 시 생성할 versioned JSON 형식 지시문 출력. put 계열 호출 코드 자체를 이 스크립트에 두지 않음 (31-12 checkpoint 전용 — H2-09). retentionDays 값은 **스크립트 실행 시 privacy_decision.json 을 로컬에서 읽어 쓰되 (로컬 dev 도구 — Lambda 런타임 아님, M3-02 위반 아님을 주석 명시)**.
(D) test_visual_dispatch.py: flag OFF → S3/SQS 0; target None → 생략; **B3-03 순서 고정: 정상 경로에서 S3 put 이 reserve 보다 먼저 호출됨 (mock call order assert)**; created True → put + reserve(payload 스칼라 전부) + SQS 1 + dispatched CAS — **pending write 는 reserve transaction 내부뿐 (pipeline 소스에 update_analysis_visual/별도 pending write 0 — grep)**; **S3 put 예외 → reserve 0·SQS 0·분석 비차단 (B3-03: input 없는 job 이 구조적으로 불가능)**; **reserve 성공 후 send crash → dispatchState 'pending' 잔존 + dispatcher(31-09 list_dispatch_pending 연동)가 create 발행 → vendor create 총 1회**; **reserve transaction commit_lost → 재실행 created False + 문서 일관 (job 1·pending 1)**; **worker done 후 producer 재실행 경쟁 → done 유지 (상태 write 0 — 되감기 없음), srcKey 재put 은 허용**; duplicate-after-done → 상태/SQS 0; duplicate-while-polling → no-op; run_contended 동시 enqueue → job 1·SQS<=2·put 허용 중복; sourceHash 불일치 reserved → no-op; 훅 내 visual_gen import 부재; **template 파싱 assert (H2-01/H3-09): VisualQueue VisibilityTimeout >= 6×Worker Timeout, maxReceiveCount >= 5, ReportBatchItemFailures 존재, PipelineFunction SendMessage 존재, VisualDispatchFunction ReservedConcurrentExecutions == 1 + Schedule rate(1 minute) 존재, VisualWorkerFunction 에 Schedule 이벤트 부재, VisualOutboxAgeAlarm 존재 (Namespace/MetricName 일치)**.
실행 커밋 권장 순서: (A/D pipeline 훅+dispatch 테스트) 커밋 → (B template IaC+파싱 assert) 커밋 → (C dryrun 스크립트) 커밋 — 3분할 atomic.</action>
  <verify>
    <automated>(cd backend && sam validate --lint) && python -m pytest backend/tests/phase31/test_visual_dispatch.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - S3 put → reserve → send 순서가 mock call order 로 고정 + S3 실패 시 job 0 (B3-03)
    - send 후 analysis pending write 부재 — grep + worker-done 경쟁 테스트 green (§8-3 gate 5)
    - reserve 반환 분기 (done/진행중/reserved/failed) + commit_lost + run_contended 테스트 green
    - template 파싱 assert: visibility/redrive + DispatchFunction concurrency 1 + worker schedule 부재 + OutboxAge alarm (H2-01/H3-09)
    - visual_infra_dryrun.py 에 put 계열 호출 0 + versioning/Object Lock 조회 + NoncurrentVersionExpiration merge 분기 존재 (H2-09/H3-08)
    - visual_infra_dryrun.py 1회 실행 완료 — infra/lifecycle_before.json + lifecycle_merged.json 존재 (31-12 T2 전제)
    - VisualWorkerFunction 에만 GEMINI_API_KEY, VisualJobsEnabled Default "false"
    - `(cd backend && sam validate --lint)` && `python -m pytest backend/tests -q` exits 0
  </acceptance_criteria>
  <done>분석 경로가 upload-first replay-safe 로 배선되고, IaC 가 dispatcher 분리·alarm·versioning 인지까지 배포 준비 완료 (실배포·인프라 적용은 31-12 checkpoint).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 앱 → POST /visual/rotation | 미신뢰 입력 + 과금 유발 |
| 앱 → playback-url asset | key 는 서버 선택 exact 계약 — 클라이언트 key 수신 0 |
| _process → SQS | 분석 경로는 upload-first + reserve 분기 + enqueue 만 — 외부 API 무접촉 |
| IaC → 실 스택 | flag 기본 OFF + 라이브 mutation 은 31-12 checkpoint 로 격리 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-38 | Spoofing | visual-request 인증 | mitigate | verify_request Firebase ID token → 401 |
| T-31-39 | Elevation of Privilege | 타인 analysisId | mitigate | uid 토큰 경로 스코프 + guards_ok 단일 404 |
| T-31-40 | Denial of Service (과금) | 온디맨드 남용 | mitigate | transaction 한도(KST) + dedupe + done 멱등 + fail-closed env (H-06/M-06) |
| T-31-41 | Information Disclosure | 만료/임의/stale key 서명 | mitigate | exact basename + status done + content-type + 1h TTL (H-02 + M2-01) |
| T-31-42 | Denial of Service | dispatch 실패 고아 quota | mitigate | durable outbox + VisualDispatchFunction 독립 복구 + OutboxAge alarm (B3-01 + H3-09) |
| T-31-43 | Information Disclosure | DASHSCOPE/GEMINI/HMAC 키 | mitigate | resolve:ssm SecureString 만, GEMINI_API_KEY 워커 전용 |
| T-31-44 | Tampering | 미검증 기능 전면 노출 | mitigate | VisualJobsEnabled 기본 false (H-07) |
| T-31-54 | Tampering | replay 가 완료 상태 되감기/입력 교체/입력 부재 create | mitigate | upload-first + reserve 내 pending + 반환 유일 분기 + immutable hash key (B2-03 + B3-03) |
| T-31-55 | Tampering | 라이브 lifecycle 교체로 기존 규칙 소실 / noncurrent PII 잔존 | mitigate | dry-run merge artifact + versioning 조회 + NoncurrentVersionExpiration — put 은 31-12 checkpoint (H2-09 + H3-08) |
</threat_model>

<verification>
- `python -m pytest backend/tests -q` green + `(cd backend && sam validate --lint)` green
- 응답 코드가 contract.md /visual/rotation 절과 일치
- 라이브 AWS 변경 0 (스크립트 read-only — H2-09)
- Lambda/pipeline 코드에 .planning 경로 참조 0 (M3-02 — dry-run 로컬 스크립트 제외)
</verification>

<success_criteria>
- B3-03(upload-first + reserve 내 pending + 늦은 pending 불가)/H3-09(dispatcher IaC 분리 + alarm)/H3-08(versioning-aware dry-run)/M3-02(빌드타임 상수)/H2-01/M2-01/H2-09 전부 구현+테스트
- 3차 리뷰 §8-3 gate 4·5 가 이 플랜의 테스트로 추적 가능
</success_criteria>

<output>
Create `.planning/phases/31-api-visual-correction/31-10-SUMMARY.md` when done
</output>
