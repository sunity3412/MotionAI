---
phase: 31-api-visual-correction
plan: 10
type: execute
wave: 3
depends_on: [31-02, 31-03, 31-04, 31-05]
files_modified:
  - backend/shared/python/sunity_shared/validation.py
  - backend/functions/playback-url/app.py
  - backend/functions/visual-request/app.py
  - backend/functions/visual-request/requirements.txt
  - backend/functions/pipeline/app.py
  - backend/template.yaml
  - backend/tests/phase31/test_visual_url.py
  - backend/tests/phase31/test_visual_request.py
  - backend/tests/phase31/test_visual_dispatch.py
autonomous: true
requirements: [D-05, D-06, D-07, D-08]
must_haves:
  truths:
    - "만료된 표시 URL 은 인증 재서명 경로로 복구된다 — playback-url asset 확장, 서버 선택 key 만 (리뷰 H-02)"
    - "회전 요청은 auth → 검증 → transaction (quota+job 원자) → SQS → 202 — SQS 실패는 dispatch_failed 로 남아 재요청이 복구한다 (리뷰 H-06)"
    - "분석 경로(_process)는 correctedPose 를 인라인 생성하지 않는다 — 소스 프레임 저장 + 잡 enqueue 만, fault-zoom 조건부 밖 배치 (리뷰 H-01)"
    - "VisualJobsEnabled feature flag 기본 OFF — OFF 면 enqueue 스킵·요청 503 feature_disabled (리뷰 H-07 canary)"
    - "quota env 는 fail-closed — 파싱 불가 시 한도 0 (거부) (리뷰 M-06)"
    - "analysisId 형식 검증은 공유 validator 단일 소유 (리뷰 L-03)"
  artifacts:
    - path: "backend/functions/visual-request/app.py"
      provides: "POST /visual/rotation — auth·flag·transaction·dispatch·202"
      contains: "def lambda_handler"
    - path: "backend/functions/playback-url/app.py"
      provides: "asset 재서명 확장 (correctedPose/rotation, 1h)"
      contains: "asset"
    - path: "backend/functions/pipeline/app.py"
      provides: "_enqueue_corrected_pose_job 사후 훅"
      contains: "_enqueue_corrected_pose_job"
    - path: "backend/template.yaml"
      provides: "VisualQueue/DLQ + 함수 2종 + ReportBatchItemFailures + LogGroup + flag 파라미터"
      contains: "VisualJobsEnabled"
  key_links:
    - from: "backend/functions/visual-request/app.py"
      to: "backend/shared/python/sunity_shared/firestore_admin.py"
      via: "reserve_visual_job transaction"
      pattern: "reserve_visual_job"
    - from: "backend/functions/pipeline/app.py"
      to: "VisualQueue"
      via: "sqs.send_message (enqueue-only — 인라인 생성 금지)"
      pattern: "VISUAL_QUEUE_URL"
    - from: "backend/template.yaml"
      to: "SSM /sunity/motion/dashscope-api-key"
      via: "resolve:ssm dynamic reference"
      pattern: "dashscope-api-key"
---

<objective>
HTTP 표면·분석측 dispatch·IaC 를 완성한다. (1) playback-url asset 확장 — 리뷰 H-02 의 재서명 경로 (서버가 분석 문서에서 key 를 선택, 1h presign, 만료 fixture 테스트). (2) visual-request Lambda — 리뷰 H-06 의 원자 예약 (31-02 reserve_visual_job) + dispatch 실패 복구 + KST fail-closed quota (리뷰 M-06). (3) pipeline 의 correctedPose enqueue 훅 — 리뷰 H-01: _process 는 DashScope/Gemini 를 인라인 호출하지 않고 소스 프레임 저장 + 잡 생성 + SQS 발행만, fault-zoom 조건부 밖에 핀 고정 (pipeline/app.py 는 31-03 이 먼저 수정하는 공유 파일 — depends_on 31-03). (4) template.yaml IaC — VisualQueue(+DLQ, ReportBatchItemFailures), 함수 2종, LogGroup, VisualJobsEnabled flag 기본 false (리뷰 H-07 canary), visual-input/ lifecycle.

Purpose: 요청·자동생성 양 진입점이 동일 durable substrate 로 흐르게 하는 마지막 backend 배선.
Output: Lambda 2종 수정/신설 + pipeline 훅 + IaC + 테스트 3파일.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-api-visual-correction/31-PATTERNS.md
@docs/contract.md
@.planning/phases/31-api-visual-correction/31-REVIEWS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 공유 validator + playback-url asset 재서명 확장</name>
  <files>backend/shared/python/sunity_shared/validation.py, backend/functions/playback-url/app.py, backend/tests/phase31/test_visual_url.py</files>
  <read_first>backend/functions/playback-url/app.py (전체 — analysisId alnum+len>=16 가드, guards_ok 단일 404, _sign_get, _PLAYBACK_EXPIRES), backend/shared/python/sunity_shared/validation.py (기존 validator 관례 — 순수 함수·boto3 무관), docs/contract.md 의 playback-url asset 확장 절 (31-04 — 단일 기준)</read_first>
  <action>validation.py 에 `validate_analysis_id_format(analysis_id) -> bool` 신설 — playback-url 의 현행 규칙(isalnum and len>=16)을 이관한 단일 소유 (리뷰 L-03). playback-url/app.py: 기존 인라인 검사를 공유 validator 호출로 교체 + body 의 optional `asset` 처리 — 값이 'correctedPose'|'rotation' 외면 400; asset 지정 시: 분석 문서 read (token uid 경로 스코프) → kind 별 필드(correctedPoseKey/rotationVideoKey) 취득 → 값 존재 + `results/{uid}/{analysis_id}/` prefix 로 시작하는지 정확 검증 (startswith equality — 리뷰 H-02/H-05) → 통과 시 ExpiresIn=3600 (`_ASSET_EXPIRES` 상수 — 7일 _PLAYBACK_EXPIRES 와 별개) presign 반환 {playbackUrl, expiresInSec: 3600}; 문서 부재/타인/필드 부재/prefix 위반 전부 guards_ok 단일 404 (leak 0). asset 미지정 = 기존 동작 바이트 동일. test_visual_url.py: **만료 URL 시나리오 fixture — Firestore 에 URL 이 없고 key 만 있는 문서에서 재서명이 항상 fresh URL 을 반환** (리뷰 H-02 요구 테스트), 타 uid 문서 404, 필드 부재 404, prefix 위반(예: results/otherUid/...) 404, 잘못된 asset 400, asset 미지정 기존 경로 무회귀, 무토큰 401.</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31/test_visual_url.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - validation.py 에 validate_analysis_id_format 존재 + playback-url 이 이를 import (인라인 중복 규칙 0)
    - asset 경로가 클라이언트 key 를 받지 않음 (body 에 key 파라미터 자체 없음)
    - prefix 위반/타 uid/필드 부재가 전부 단일 404 테스트 green
    - `python -m pytest backend/tests/phase31/test_visual_url.py -x -q` exits 0
  </acceptance_criteria>
  <done>H-02 재서명 경로 완성 — done 카드가 영구히 fresh URL 로 복구 가능.</done>
</task>

<task type="auto">
  <name>Task 2: visual-request Lambda — flag·원자 예약·dispatch 복구</name>
  <files>backend/functions/visual-request/app.py, backend/functions/visual-request/requirements.txt, backend/tests/phase31/test_visual_request.py</files>
  <read_first>backend/functions/playback-url/app.py (auth/파싱/guards_ok 골격 — 가장 근접 analog), backend/functions/playback-url/requirements.txt + backend/functions/pipeline/requirements.txt (pyyaml deploy fix 주석 2건 원문 — 신규 requirements.txt 주석에 인용), backend/shared/python/sunity_shared/firestore_admin.py (reserve_visual_job/mark_visual_job_dispatch_failed — 31-02 시그니처), docs/contract.md 의 POST /visual/rotation 절 (응답 계약 단일 기준)</read_first>
  <action>visual-request/app.py 신설 — 흐름: (1) env `VISUAL_JOBS_ENABLED` != "true" → 503 responses.error("feature_disabled", "지금은 사용할 수 없어요.", status=503) (flag 기본 OFF — 리뷰 H-07), (2) verify_request → 401, (3) analysisId validate_analysis_id_format → 400, (4) 분석 문서 read — 존재·소유(uid 경로)·status=='done' 을 guards_ok 단일 404, (5) rotationStatus=='done' → 200 {"rotationStatus":"done"} (멱등 — URL 미포함, 재서명은 playback-url asset), (6) rotationStatus=='pending' 이고 rotationUpdatedAtMs 기준 20분 이내 → 202 dedupe (SQS 미발행; **전용 timestamp 기준 — 공용 updatedAt 금지, 리뷰 H-06**), (7) quota env 파싱 = `_parse_limit(name, default)` — int 변환 실패/음수/비정상 → 0 반환 + log.error (**fail-closed: 한도 0 = 전부 429, 리뷰 M-06**); date_key = KST(UTC+9) YYYYMMDD, (8) reserve_visual_job(uid, analysis_id, 'rotation', date_key, user_limit=env ROTATION_DAILY_LIMIT 기본 3, global_limit=env ROTATION_GLOBAL_DAILY_LIMIT 기본 30) — reason daily_limit → 429 code 'daily_limit' 한국어 안내 "오늘 생성 한도에 도달했어요. 내일 다시 시도해 주세요.", created False + 기존 job 진행 중 → 202 (중복 요청 멱등), (9) created True (또는 dispatch_failed 재예약) → update_analysis_visual(kind 'rotation', status 'pending') + sqs.send_message({"jobId":..., "action":"run"}) — **send 실패 시 mark_visual_job_dispatch_failed + 500 반환 (quota 는 소비됐지만 다음 요청이 dispatch_failed 를 새 quota 없이 재발행 — 복구 경로, 리뷰 H-06)** → 202 {"rotationStatus":"pending"}. dispatch_failed 상태 job 재요청 = quota 재소비 없이 SQS 재발행 (reserve_visual_job 의 기존 job 반환 + state 검사). **requirements.txt = `firebase-admin>=6,<7` + `pyyaml>=6.0,<7.0` — pyyaml 주석에 두 선례 deploy fix 인용 필수 (2026-06-12 pipeline/upload-url/reference-api 500 ImportError + 2026-07-17 playback-url 29-06 배포 직후 500 Runtime.ImportModuleError — sunity_shared.firestore_admin → analysis/force_signals.py module-level yaml transitive import)**. test_visual_request.py: flag OFF → 503 + 벤더/SQS 0; 무토큰 401; 형식 400; 타인/부재/미완료 단일 404; done 멱등 200; pending 20분 내 202 + SQS 0 (rotationUpdatedAtMs 기준 — 공용 updatedAt 갱신으로는 dedupe 연장 안 됨을 함께 고정); 한도 초과 429 daily_limit; env "abc" → 전부 429 (fail-closed); SQS send 실패 → dispatch_failed 마킹 + 재요청 시 quota 증가 0 + 재발행; **동시 요청 시나리오: reserve 2회 순차 호출로 두 번째 created False → SQS 1회만** (리뷰 H-06 concurrent 테스트).</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31/test_visual_request.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - dedupe/타임아웃 판정이 rotationUpdatedAtMs 만 사용 (공용 updatedAt 참조 0)
    - SQS 실패 → dispatch_failed → 재요청 무-quota 복구 테스트 green
    - env 파싱 불가 → 429 fail-closed 테스트 green
    - flag OFF → 503 feature_disabled 테스트 green
    - requirements.txt 에 firebase-admin + pyyaml 두 줄 존재 (deploy fix 선례 2건 인용 주석)
  </acceptance_criteria>
  <done>온디맨드 진입점이 원자 예약·복구·flag·KST 한도까지 테스트 고정.</done>
</task>

<task type="auto">
  <name>Task 3: pipeline enqueue 훅 + template.yaml IaC + visual-input lifecycle</name>
  <files>backend/functions/pipeline/app.py, backend/template.yaml, backend/tests/phase31/test_visual_dispatch.py</files>
  <read_first>backend/functions/pipeline/app.py lines 3116-3158 (_run_deferred_fault_zoom — 비차단 규율) + 4760-4800 (fault-zoom 조건부 위치 — 훅은 이 조건부 **밖**; 31-03 이 같은 파일을 먼저 수정하므로 최신 상태 기준으로 위치 재확인), backend/template.yaml (전체 — PlaybackUrlFunction/PipelineFunction/AnalysisQueue/LogGroup 선례 + SSM 명시 Statement 주석), .planning/phases/31-api-visual-correction/31-REVIEWS.md 의 H-01 절</read_first>
  <action>(A) pipeline/app.py 에 `_enqueue_corrected_pose_job(*, uid, analysis_id, joint, target_deg, src_png: bytes) -> None` — _process 의 complete_analysis 이후, **fault-zoom 조건부 밖에서 무조건 호출되는 지점에 배치** (리뷰 H-01 핀 고정; top-1 결함 관절은 감점 records 최대 감점 기준, 결함 0 이면 호출 자체 생략 — 상태 미기록 = legacy 숨김). 내부: env VISUAL_JOBS_ENABLED != "true" → 즉시 return (flag); S3 put `visual-input/{uid}/{analysis_id}/src.png`; **reserve_visual_job(kind 'correctedPose', 한도 인자 없음 — 분석당 1 자동이 한도, `payload={"srcKey": skey, "joint": joint, "targetDeg": target_deg}` — 31-02 의 payload 파라미터로 job 문서에 flat scalar 기록, 31-09 T2 가 소비)**; update_analysis_visual(status 'pending'); sqs.send_message({"jobId", "action":"run"}) — send 실패는 mark_visual_job_dispatch_failed + update_analysis_visual('failed') (조용). 전체 try — 어떤 경로도 재raise 0 (fault_zoom 훅과 동일 비차단 규율). **DashScope/Gemini/pose 호출 0 — 인라인 생성 금지가 이 함수의 존재 이유** (리뷰 H-01: RunPod background task 를 외부 API 로 지연시키지 않음). Pod 런타임 전제 주석: Pod env 에 VISUAL_QUEUE_URL/VISUAL_JOBS_ENABLED 필요 — 31-12 Pod 체크리스트에 반영. (B) template.yaml: `VisualQueue`+`VisualDLQ` (VisibilityTimeout 360 — worker Timeout 300 초과, RedrivePolicy maxReceiveCount 3); `VisualRequestFunction` (python3.12/arm64/Timeout 10, HttpApi POST /visual/rotation, env FIREBASE_SA_PARAM/VISUAL_QUEUE_URL/VISUAL_JOBS_ENABLED(!Ref 파라미터)/ROTATION_DAILY_LIMIT "3"/ROTATION_GLOBAL_DAILY_LIMIT "30", Policies: SSM 명시 Statement + sqs:SendMessage — **GEMINI_API_KEY 미주입: judge 는 워커 전용**); `VisualWorkerFunction` (Timeout 300, MemorySize 512, SQS 이벤트 BatchSize 1 + **FunctionResponseTypes: [ReportBatchItemFailures]** — 리뷰 B-03 partial batch failure 전제, env VIDEO_BUCKET/FIREBASE_SA_PARAM/VISUAL_QUEUE_URL/DASHSCOPE_API_KEY="{{resolve:ssm:/sunity/motion/dashscope-api-key}}"/PAIR_ID_HMAC_KEY="{{resolve:ssm:/sunity/motion/pair-id-hmac-key}}"/RUNPOD_ANALYZE_URL(기존 resolve 선례)/RUNPOD_AUTH_TOKEN + **GEMINI_API_KEY="{{resolve:ssm:/sunity/motion/gemini-api-key}}" — visual-worker 에만 주입 (31-05 judge 의 stdlib urllib Gemini REST 용; checker BLOCKER 해소 — google-genai SDK 미반입)**, Policies: SSM + s3 Get(uploads/*, visual-input/*, results/*) + Put/Delete(visual-input/*) + Put(results/*, training/*) + sqs:SendMessage(자기 큐 — continuation)); Template Parameters 에 `VisualJobsEnabled` (Type String, Default "false", AllowedValues true/false) + PipelineFunction env 에 VISUAL_QUEUE_URL/VISUAL_JOBS_ENABLED 추가; LogGroup 2종 RetentionInDays 30. (C) visual-input lifecycle: 버킷은 SAM 외부 관리 — `aws s3api get-bucket-lifecycle-configuration --bucket sunity-motion-pilot-videos --profile sunity-motion` 로 기존 규칙(uploads/ 만료) 읽어 **기존 규칙 보존 + visual-input/ prefix 1일 Expiration 규칙 추가** 를 put-bucket-lifecycle-configuration 으로 적용 (덮어쓰기 주의 — 기존 규칙 미포함 put 금지, 실행 전 기존 JSON 을 SUMMARY 에 기록). (D) SSM `/sunity/motion/pair-id-hmac-key` 부재 시 `aws ssm put-parameter --type SecureString` 으로 랜덤 32바이트 hex 생성·등록 (기존 존재 시 무변경). test_visual_dispatch.py: flag OFF → S3/SQS 0; 결함 0 → 호출 생략 + 상태 미기록; 정상 → src put + **reserve_visual_job payload 에 srcKey/joint/targetDeg 전달 (mock 호출 인자 검증)** + pending + SQS 1회; SQS 실패 → dispatch_failed + failed 마킹 + 재raise 0; 훅 내 DashScope 호출 0 (visual_gen import 부재 assert).</action>
  <verify>
    <automated>(cd backend && sam validate --lint) && python -m pytest backend/tests/phase31/test_visual_dispatch.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - _enqueue_corrected_pose_job 이 visual_gen/judge 를 import 하지 않음 (enqueue-only — 테스트 고정, 리뷰 H-01)
    - reserve_visual_job 호출에 payload={srcKey, joint, targetDeg} 전달 (테스트 고정 — 31-09 소비 계약)
    - template.yaml 에 FunctionResponseTypes + ReportBatchItemFailures 존재 (리뷰 B-03)
    - VisualWorkerFunction 에만 GEMINI_API_KEY resolve:ssm 존재 (VisualRequestFunction 에 부재)
    - VisualJobsEnabled Default "false" (리뷰 H-07)
    - VisualQueue VisibilityTimeout(360) > worker Timeout(300)
    - lifecycle 적용 시 기존 규칙 보존 (get 후 merge — SUMMARY 에 before/after 기록)
    - `(cd backend && sam validate --lint)` exits 0 && `python -m pytest backend/tests -q` exits 0
  </acceptance_criteria>
  <done>분석 경로가 enqueue-only 로 배선되고 IaC 가 flag OFF 기본으로 배포 가능 상태 (실배포는 31-12 checkpoint).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 앱 → POST /visual/rotation | 미신뢰 입력 + 과금 유발 |
| 앱 → playback-url asset | key 는 서버 선택 — 클라이언트 key 수신 0 |
| _process → SQS | 분석 경로는 enqueue 만 — 외부 API 무접촉 |
| IaC → 실 스택 | flag 기본 OFF + belle 배포 게이트 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-38 | Spoofing | visual-request 인증 | mitigate | verify_request Firebase ID token → 401 (기존 경로) |
| T-31-39 | Elevation of Privilege | 타인 analysisId | mitigate | uid 토큰 경로 스코프 + guards_ok 단일 404 |
| T-31-40 | Denial of Service (과금) | 온디맨드 남용 | mitigate | transaction 한도(3/일+전역 30/일 KST) + dedupe + done 멱등 + fail-closed env (리뷰 H-06/M-06) |
| T-31-41 | Information Disclosure | 만료/임의 key 서명 | mitigate | 서버 선택 key + prefix equality + 1h TTL (리뷰 H-02/H-05) |
| T-31-42 | Denial of Service | dispatch 실패 고아 quota | mitigate | dispatch_failed 상태 + 무-quota 재발행 복구 (리뷰 H-06, 테스트 고정) |
| T-31-43 | Information Disclosure | DASHSCOPE/GEMINI/HMAC 키 | mitigate | resolve:ssm SecureString 만 — 코드/로그 리터럴 0, GEMINI_API_KEY 는 워커 함수에만 주입 (최소 권한) |
| T-31-44 | Tampering | 미검증 기능 전면 노출 | mitigate | VisualJobsEnabled 기본 false — E2E 통과 후 belle 이 ON (리뷰 H-07) |
</threat_model>

<verification>
- `python -m pytest backend/tests -q` green + `(cd backend && sam validate --lint)` green (리뷰 M-07 경로 수정 반영)
- 응답 코드가 contract.md /visual/rotation 절과 일치
</verification>

<success_criteria>
- H-01(enqueue-only)/H-02(재서명)/H-06(원자·복구)/M-06(KST fail-closed)/B-03 IaC 전제 전부 구현+테스트
- flag OFF 기본의 배포 가능 IaC + Lambda 신규 2함수 pyyaml 박제 (deploy fix 재발 방지)
</success_criteria>

<output>
Create `.planning/phases/31-api-visual-correction/31-10-SUMMARY.md` when done
</output>
