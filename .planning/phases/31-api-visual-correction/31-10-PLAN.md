---
phase: 31-api-visual-correction
plan: 10
type: execute
wave: 5
depends_on: [31-02, 31-03, 31-04, 31-05, 31-09, 31-13]
files_modified:
  - backend/shared/python/sunity_shared/validation.py
  - backend/functions/playback-url/app.py
  - backend/functions/visual-request/app.py
  - backend/functions/visual-request/requirements.txt
  - backend/functions/pipeline/app.py
  - backend/template.yaml
  - backend/scripts/visual_infra_dryrun.py
  - backend/tests/phase31/test_visual_url.py
  - backend/tests/phase31/test_visual_request.py
  - backend/tests/phase31/test_visual_dispatch.py
autonomous: true
requirements: [D-05, D-06, D-07, D-08]
must_haves:
  truths:
    - "임시 생체 프레임(correctedPose source/staging)은 신규 비-버저닝 전용 버킷(template Parameter VisualInputBucketName = sunity-motion-pilot-visual-input, SAM 외부 생성)에 저장된다 — 비-버저닝이라 delete_object(key) 1회 = 완전 소거, version 열거/삭제 불필요. correctedPose canonical 결과(results/...)는 기존 VideoBucket 유지, 학습 페어(training/phase31/pairs/)도 기존 버저닝 버킷 유지 (6차 belle 아키텍처 조정)"
    - "correctedPose enqueue 는 upload-first + producer preflight — (0) read-only preflight: visualJobs job 이 어떤 state 로든 존재하면 source PUT 0(기존 outbox 처리만, 최종 권위는 reserve), (1) job 부재면 조건부 immutable srcKey put_object(IfNoneMatch='*') 로 VisualInputBucket 에 원자 생성, (2) 단일 transaction reserve + analysis 'pending' + nextAction='create'/dispatchState='pending'/outboxSeq=1, (3) SQS send best-effort. input 없는 create action 이 순서상 불가능 (3차 B3-03 + 6차 B6-03)"
    - "source 최초 생성은 put_object(IfNoneMatch='*') 조건부 write — 412(PreconditionFailed) 또는 409(concurrent) 면 bounded retry 후 HEAD 하여 object metadata full sha256 을 신규 sourceHash 와 exact 비교해 기존 재사용, 불일치=collision/tampering block. 두 invocation 동시 HEAD/PUT 이 입력을 교체할 수 없다 (4차 M4-04 + 5차 B5-02 + 6차 M6-02)"
    - "producer 는 inputSealed hard gate 를 따른다 — reserve 결과 existing job 의 state ∈ ('done','failed') 이거나 inputSealed==True 이고 createdThisInvocation True 면 이번에 만든 source object 를 즉시 delete_object(key) 삭제한다(비-버저닝 = 완전 소거). postprocess 가 cleanup 후 finalize 하기 직전 producer replay 가 만든 새 입력이 terminal 뒤 잔존할 수 없다 (6차 리뷰 B6-03)"
    - "reserve commit 전 실패/analysis_missing/commit response loss 시 orphan 보상 — job 재read: job 부재면 created_keys 전부 즉시 delete(역순), job 존재+input 소유(payload.srcKey 일치)면 유지. 보상 delete 실패는 VisualOrphanSourceObject metric + **필수 visualOrphans/{hash} 문서**(H7-04, janitor 소비) 기록 (6차 H6-02 + 7차 H7-04)"
    - "source key 를 reserve payload 에 기록한다 — 31-09 postprocess 의 단일 delete cleanup 이 head 존재 0 으로 잔존 없음을 확인한다(비-버저닝 = version 열거 불필요) (6차 B6-03)"
    - "SQS send 후 analysis pending write 가 존재하지 않는다 — pending 은 reserve transaction 안에서만 (B3-03, §8-3 gate 5)"
    - "send 실패 = dispatcher 복구 — dispatchState='pending' durable + 별도 VisualDispatchFunction(reserved concurrency 1) 발행 + sent+lease-만료(H6-01 필터) 재발행 (3차 H3-09 + 5차 B5-01 + 6차 H6-01)"
    - "메시지는 {jobId, generation, action, outboxSeq} — visual-request/pipeline 이 reserve 반환 outboxSeq 로 best-effort send + mark_visual_job_dispatched(action+seq CAS) (4차 리뷰 B4-01)"
    - "queue visibility >= 6 × worker timeout, maxReceiveCount >= 5 — template 파싱 assert. lease 정합 build gate: WorkerTimeout*1000 < models.VISUAL_CLAIM_LEASE_MS < VisualQueue VisibilityTimeout*1000 (2차 H2-01 + 6차 H6-07)"
    - "template 이 4 게이트 env(DISPLAY_JUDGE_CONFIDENCE/TRAINING_JUDGE_CONFIDENCE/DISPLAY_POSE_TOL_DEG/TRAINING_POSE_TOL_DEG)를 CALIBRATION chosen 값으로 주입하고, build gate 가 TRAINING_JUDGE_CONFIDENCE > DISPLAY_JUDGE_CONFIDENCE AND TRAINING_POSE_TOL_DEG < DISPLAY_POSE_TOL_DEG 를 강제한다 (4차 리뷰 B4-05/M4-03)"
    - "worker IAM 은 VisualInputBucket **bucket ARN 에 s3:ListBucket(Condition s3:prefix=visual-input/*)** + object ARN 에 GetObject/PutObject/DeleteObject 다 — cleanup 의 list_objects_v2/재조회가 bucket-level ListBucket 을 요구(7차 B7-01). pipeline(producer) 은 object ARN Get/Put/DeleteObject(+orphan 보상 재read). 둘 다 버전 액션(s3:DeleteObjectVersion/ListBucketVersions) 부재(비-버저닝). 페어 버킷 version-aware 삭제 권한은 Lambda 아닌 페어 삭제 스크립트(31-07) credential 에만 — 31-12 iam simulate + 실 role list-objects-v2 canary 로 검증 (6차 B6-02 + 7차 B7-01)"
    - "worker·pipeline 은 cloudwatch:PutMetricData(Namespace 'SunityVisual' 제한) 권한을 갖는다 — VisualCleanupBlocked/VisualPairConflict(worker)·VisualOrphanSourceObject(pipeline) metric 방출 주체. template 에 VisualCleanupBlockedAlarm·VisualPairConflictAlarm·VisualOrphanSourceObjectAlarm 존재. metric call 실패는 business path 에서 삼키고 log (7차 리뷰 H7-03)"
    - "upload-first 하드 크래시 orphan 은 durable 주체가 있다 — producer 는 srcKey PUT **전** **per-invocation immutable reservation** `visualInputReservations/{jobId}_{reservationId}`(reservationId=이번 invocation 고유)를 **create-only 로 생성(다른 invocation 문서를 overwrite 불가 — 8차 B8-05)**, PUT 성공분을 record_reservation_keys 로 createdKeys 기록. reserve_visual_job 이 **자기 reservation 만 claim_reservation_for_job CAS(open+owner+미만료→claimed_by_job)** 성공 시에만 job 생성(8차 B8-06). PUT 직후 SIGKILL·option-b 실패에도 reservation 이 남아 janitor 가 claim_reservation_for_janitor CAS 성공 후 lifecycle 대기 없이 삭제. createdThisInvocation 단일 bool 금지 — key 별 소유권. 단일 {jobId} 문서 overwrite 로 concurrent invocation 의 key 를 잃지 않는다 (7차 B7-05 + 8차 B8-05/B8-06)"
    - "동일 deterministic S3 key 삭제 경쟁은 `visualInputObjects/{hash}` **active|deleting delete-fence 상태 머신**으로 봉쇄한다 — producer 는 PUT/reuse 전 acquire_key_ownership(state=='deleting'이면 fence 로 실패), reserve 성공 시 _promote_..._to_job_tx, **모든 종료 경로(worker cleanup·producer 보상·terminal-replay·close)에서 release_key_ownership 원자 수행(B10-03)**. janitor 는 `claim_key_for_delete`(자기 ref 소비+다른 live ref 0+state='deleting' CAS) 성공 후에만 외부 delete → count 확인 후 delete 전 producer acquire 는 fence 로 차단(9차 B9-02 + 10차 B10-01/B10-02/B10-03)"
    - "chosen bucket name 은 `infra/visual_input_bucket.json` 단일 출처 — dry-run/lifecycle/IAM/Pod/E2E/SAM deploy override 가 모두 이 값에서 파생, 실행 action 에 기본 이름 하드코딩 0. **10차 H10-05: template `VisualInputBucketName` Parameter 는 Default 를 제거(fail-open 금지) — override 누락 시 SAM deploy 가 실패. repo-root helper 가 JSON schema/region/name 검증해 SAM override 를 구성, checkpoint 는 그 단일 script 호출(빈 값 차단)** (9차 B9-05 + 10차 H10-05)"
    - "보상/실패한 삭제는 필수 durable `visualOrphans/{hash(bucket,key)}` 문서({bucket, key, state, attempt, nextRetryAtMs, lastError})로 기록되고 janitor 가 재시도·삭제+HEAD 404 후 문서를 닫는다 — 옵션 로그가 아니라 실제 소비 주체가 있는 계약 (7차 리뷰 H7-04)"
    - "dispatcher scan-window age metric alarm(ScannedOutboxMaxAgeMs) + scan truncated alarm 존재 — template 파싱 assert (H3-09 + 5차 H5-06 + 6차 M6-01)"
    - "dry-run 이 VisualInputBucket 의 versioning 을 **get-bucket-versioning 응답에 Status key 자체가 없음(Never-versioned)** 으로만 통과시킨다 — Status=='Enabled' 또는 =='Suspended' 는 둘 다 blocked(Suspended 의 단순 delete 는 과거 version 완전 삭제 미보장 — 7차 B7-02). 추가로 사용 전 list_object_versions prefix read-only 로 Versions/DeleteMarkers 0 기록(checkpoint 주체만, worker 런타임엔 version 권한 미부여) + Object Lock 미설정 확인(위반 blocked). 페어 버킷(VideoBucket)은 versioning + Object Lock 을 조회해 default retention 이면 blocked / enabled·no-default 면 canary 를 31-12 로 넘기고, pairs prefix 에 NoncurrentVersionExpiration + expired delete marker 규칙 merge (4차 H4-03/H4-04 + 5차 H5-07 + 3차 H3-08 + 6차 아키텍처 + 7차 B7-02)"
    - "두 버킷 lifecycle 은 버킷당 독립 파일이다 — dry-run 이 `infra/visual_input_lifecycle_{before,merged}.json` + `infra/video_lifecycle_{before,merged}.json` 4파일을 산출하고, 각 파일은 AWS API 가 바로 받는 `{\"Rules\":[...]}` 단일-버킷 형상(멀티버킷 wrapper 금지). VisualInputBucket merged = visual-input/ 1일 Expiration + Filter{Prefix} 만(NoncurrentVersionExpiration 없음 — 비-버저닝). 31-12 가 파일↔버킷 1:1 put + 교차 rollback (7차 리뷰 B7-07 + H7-08)"
    - "privacy 결정값은 배포 config/상수 — .planning JSON 런타임 읽기 0, build 테스트가 JSON 대조 (3차 M3-02)"
    - "playback-url asset guard 는 exact basename + status done + content-type — stale key failed 문서는 404 (2차 M2-01)"
    - "VisualJobsEnabled 기본 OFF, quota env fail-closed, analysisId 공유 validator (H-07/M-06/L-03)"
    - "D-05: 교정 이미지(correctedPose)는 분석 완료 시 자동 생성 — top-1 결함 부위, _process 는 enqueue 만"
    - "D-07: 비용 가드 = 사용자당 일일 생성 한도(회전 3/일) + 전역 안전핀(30/일) — transaction 예약, fail-closed env"
  artifacts:
    - path: "backend/functions/visual-request/app.py"
      provides: "POST /visual/rotation — auth·flag·transaction(reserve=pending+outbox)·best-effort dispatch(outboxSeq)·202"
      contains: "def lambda_handler"
    - path: "backend/functions/playback-url/app.py"
      provides: "asset 재서명 확장 (exact basename + done guard, 1h)"
      contains: "asset"
    - path: "backend/functions/pipeline/app.py"
      provides: "_enqueue_corrected_pose_job — preflight(read_visual_job) + durable reservation + full-64hex srcKey + upload-first IfNoneMatch/409 + created_keys 별 inputSealed/terminal-replay/orphan 삭제 + 필수 visualOrphans 문서 + CorrectedPoseTarget, replay-safe (Never-versioned VisualInputBucket)"
      contains: "_enqueue_corrected_pose_job"
    - path: "backend/template.yaml"
      provides: "VisualInputBucketName Parameter + worker s3:ListBucket/PutMetricData IAM + VisualQueue/DLQ + 5 alarm(ScannedOutboxMaxAge/OutboxScanTruncated/Orphan/CleanupBlocked/PairConflict) + Dispatch janitor IAM + 4 게이트 env + lease build gate/flag"
      contains: "VisualInputBucketName"
    - path: "backend/scripts/visual_infra_dryrun.py"
      provides: "VisualInputBucket Never-versioned(Status 부재)만 통과·Suspended blocked + 버킷당 4 lifecycle 파일(visual_input/video × before/merged) shape-validated + 페어 버킷 ObjectLock-aware merge + SSM 키 점검 (라이브 mutation 0)"
  key_links:
    - from: "backend/functions/pipeline/app.py"
      to: "backend/shared/python/sunity_shared/analysis/fault_zoom.py"
      via: "build_corrected_pose_target — target 계약 유일 소비 (B2-01)"
      pattern: "build_corrected_pose_target"
    - from: "backend/functions/visual-request/app.py"
      to: "backend/shared/python/sunity_shared/firestore_admin.py"
      via: "reserve_visual_job transaction(pending+outbox) + mark_visual_job_dispatched"
      pattern: "reserve_visual_job"
    - from: "backend/template.yaml"
      to: "SSM /sunity/motion/dashscope-api-key"
      via: "resolve:ssm dynamic reference"
      pattern: "dashscope-api-key"
---

<objective>
HTTP 표면·분석측 dispatch·IaC 를 완성한다 — 31-09 워커/디스패처 코드가 존재한 뒤 실행되는 wave 5 (H2-08).

(1) playback-url asset 확장 (H-02 재서명 + M2-01 exact guard). (2) visual-request Lambda — reserve transaction 이 analysis pending + 초기 outbox 를 함께 기록하므로 send 는 best-effort(outboxSeq 포함). (3) pipeline enqueue 훅 — **B3-03 upload-first + 6차 B6-03 producer preflight + inputSealed hard gate + terminal-replay/orphan 즉시 삭제 + H6-02 orphan 보상 + full-sha256 대조 (비-버저닝 VisualInputBucket)**. target 은 31-03 CorrectedPoseTarget 만 소비 (B2-01). (4) template.yaml — H2-01 + **신규 VisualInputBucketName Parameter** + VisualDispatchFunction 분리 (H3-09) + 4 게이트 env + build gate strict ordering (B4-05) + **lease 정합 build gate (H6-07)** + **ScannedOutboxMaxAge/OutboxScanTruncated alarm (M6-01/H5-06)** + **worker/pipeline IAM = VisualInputBucket Get/Put/Delete(버전 액션 없음 — B6-02)** — worker sweeper schedule 부재. (5) 라이브 mutation 금지 (H2-09) + dry-run 의 **VisualInputBucket 비-버저닝/no-lock 확인** + 페어 버킷 versioning + Object Lock 인지 (H4-03/H4-04) + **canary 가능성 판정 → 실 canary delete 는 31-12 (H5-07)**. (6) privacy 값 build-time 상수 (M3-02).

Purpose: 요청·자동생성 양 진입점이 동일 durable substrate 로 흐르는 마지막 backend 배선 — 비-버저닝 전용 버킷으로 임시 생체 프레임 잔존 근원(version enumeration) 소멸.
Output: Lambda 3종 + pipeline 훅 + IaC(신규 버킷 Parameter) + dry-run 스크립트 + 테스트 3파일.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-api-visual-correction/31-PATTERNS.md
@docs/contract.md
@.planning/phases/31-api-visual-correction/31-PLAN-REVIEW-ITERATION10.md
@.planning/phases/31-api-visual-correction/31-PLAN-REVIEW-ITERATION9.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 공유 validator + playback-url asset 재서명 (exact basename + done guard — M2-01)</name>
  <files>backend/shared/python/sunity_shared/validation.py, backend/functions/playback-url/app.py, backend/tests/phase31/test_visual_url.py</files>
  <read_first>backend/functions/playback-url/app.py (전체 — analysisId alnum+len>=16 가드, guards_ok 단일 404, _sign_get, _PLAYBACK_EXPIRES), backend/shared/python/sunity_shared/validation.py (기존 validator 관례 — 순수 함수·boto3 무관), docs/contract.md 의 playback-url asset 확장 절 (31-04)</read_first>
  <action>validation.py 에 `validate_analysis_id_format(analysis_id) -> bool` 신설 (isalnum and len>=16 이관 — L-03). playback-url/app.py: 인라인 검사를 공유 validator 로 교체 + optional `asset` 처리 — 'correctedPose'|'rotation' 외 400; asset 지정 시 (M2-01): 분석 문서 read (token uid 스코프) → (a) status 필드 정확히 'done' (failed/pending/부재는 stale key 남아도 404), (b) key exact equality: correctedPose = f"results/{uid}/{analysis_id}/corrected_pose_{correctedPoseJoint}.png" (joint 부재 404), rotation = f"results/{uid}/{analysis_id}/rotation.mp4" — 전체 문자열 일치(canonical 은 기존 VideoBucket), (c) 확장자별 ResponseContentType presign ExpiresIn=3600 → {playbackUrl, expiresInSec:3600}. 위반 전부 guards_ok 단일 404. asset 미지정 = 기존 동작 바이트 동일. test_visual_url.py: fresh 재서명, 타 uid 404, 필드 부재 404, status failed+key 잔존 404, basename 불일치 404, joint 부재 404, 잘못된 asset 400, 미지정 무회귀, 무토큰 401.</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31/test_visual_url.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - key 검증이 exact equality (M2-01 테스트 고정)
    - status done 강제 — failed stale key 404 green
    - validation.py 공유 validator + playback-url import (인라인 중복 0)
    - asset 경로가 클라이언트 key 미수신
  </acceptance_criteria>
  <done>H-02 재서명 + M2-01 exact guard 완성.</done>
</task>

<task type="auto">
  <name>Task 2: visual-request Lambda — flag·원자 예약(pending+outbox)·best-effort dispatch(outboxSeq)</name>
  <files>backend/functions/visual-request/app.py, backend/functions/visual-request/requirements.txt, backend/tests/phase31/test_visual_request.py</files>
  <read_first>backend/functions/playback-url/app.py (auth/파싱/guards_ok 골격), backend/functions/playback-url/requirements.txt + backend/functions/pipeline/requirements.txt (pyyaml deploy fix 주석 2건), backend/shared/python/sunity_shared/firestore_admin.py (reserve_visual_job 반환 job(outboxSeq 포함) / mark_visual_job_dispatched(action+seq) — 31-02), docs/contract.md 의 POST /visual/rotation 절</read_first>
  <action>visual-request/app.py 신설 — 흐름: (1) env VISUAL_JOBS_ENABLED != "true" → 503 'feature_disabled', (2) verify_request → 401, (3) validate_analysis_id_format → 400, (4) 분석 문서 존재·소유·status=='done' → guards_ok 단일 404, (5) rotationStatus=='done' → 200 멱등, (6) rotationStatus=='pending' + rotationUpdatedAtMs 20분 이내 → 202 dedupe(SQS 미발행 — 전용 timestamp H-06), (7) `_parse_limit` fail-closed(파싱 불가→0=전부 429 M-06) + KST date_key, (8) reserve_visual_job(uid, analysis_id, 'rotation', date_key, user_limit 기본 3, global_limit 기본 30, allow_retry_failed=True) — **reserve transaction 이 job + analysis rotationStatus 'pending' + nextAction='create'/dispatchState='pending'/outboxSeq=1 원자 (B3-03; rotation 은 임시 입력·pair 없음 → inputSealed/privacy 필드 무관)**; daily_limit → 429; created False + 기존 진행중('creating'|'polling'|'retry_ready'|'fetching') → 202 no-op, (9) **best-effort dispatch: created True 또는 기존 dispatchState=='pending' → sqs.send_message({"jobId","generation","action":"create","outboxSeq": reserve 반환 job 의 outboxSeq}) → 성공 시 mark_visual_job_dispatched(expect_action='create', expect_outbox_seq=outboxSeq); send 실패 → log.warning + 202 (500 아님 — dispatcher 복구 H3-09)** → 202. requirements.txt = firebase-admin + pyyaml (주석 2건). test_visual_request.py: flag OFF 503 + SQS 0; 401/400/404; done 멱등 200; pending 20분 내 202 + SQS 0; 429 daily_limit; env "abc" → 429; **SQS send 실패 → 202 + dispatchState 'pending' 잔존 + 재요청 quota 0 + 재발행**; **메시지에 outboxSeq 포함 + mark 가 action+seq CAS (B4-01) assert**; **reserve 가 analysis pending 을 transaction 에 기록 — Lambda 소스에 update_analysis_visual/별도 pending write 0 (grep)**; run_contended 동시 → job 1 + quota +1 + SQS<=2; failed job 재요청 → generation 2 + quota 재소비.</action>
  <verify>
    <automated>python -m pytest backend/tests/phase31/test_visual_request.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - pending 기록이 reserve transaction 내부뿐 — Lambda 별도 write 0 (grep)
    - 메시지 outboxSeq 포함 + mark(action+seq) 테스트 green (B4-01)
    - send 실패 → 202 + outbox pending 잔존 green (H3-09)
    - dedupe/타임아웃이 rotationUpdatedAtMs 만 사용 + env fail-closed 429 + flag OFF 503
    - requirements.txt pyyaml 주석 2건
  </acceptance_criteria>
  <done>온디맨드 진입점이 원자 예약(pending+outbox seq)·dispatcher 복구·flag·KST 한도까지 테스트 고정.</done>
</task>

<task type="auto">
  <name>Task 3: pipeline enqueue(preflight+upload-first+inputSealed gate+terminal-replay/orphan 삭제) + template(신규 버킷/H2-01/H3-09/B4-05/H6-07/M6-01) + versioning/ObjectLock dry-run(비-버저닝 확인/H4-03/H4-04/H5-07)</name>
  <files>backend/functions/pipeline/app.py, backend/template.yaml, backend/scripts/visual_infra_dryrun.py, backend/tests/phase31/test_visual_dispatch.py</files>
  <read_first>backend/functions/pipeline/app.py lines 3116-3158 (_run_deferred_fault_zoom 비차단) + 4760-4800 (fault-zoom 조건부 위치 — 31-03 이 먼저 수정하므로 최신 재확인), backend/shared/python/sunity_shared/analysis/fault_zoom.py (build_corrected_pose_target/CorrectedPoseTarget.to_payload — 31-03), backend/shared/python/sunity_shared/analysis/pair_store.py (BLUR_OPTION — 31-07, M3-02), backend/shared/python/sunity_shared/models.py (VISUAL_CLAIM_LEASE_MS — H6-07 build gate 근거), backend/template.yaml (전체 — 함수/큐/LogGroup 선례 + VideoBucketName Parameter 관례 + SSM Statement 주석), .planning/phases/31-api-visual-correction/31-PLAN-REVIEW-ITERATION7.md 의 B7-01/B7-02/B7-05/B7-07/H7-03/H7-04/M7-02 절 + ITERATION6 의 B6-02/B6-03/H6-02 절 + smoke/CALIBRATION.json (4 게이트 값)</read_first>
  <action>(A) pipeline/app.py `_enqueue_corrected_pose_job(*, uid, analysis_id, target: CorrectedPoseTarget, src_png: bytes) -> None` — complete_analysis 이후 fault-zoom 조건부 밖 (H-01). target None → 생략 (B2-01). 내부: env flag OFF → return; sourceHash = sha256(src_png).hexdigest() (**full 64 hex — 7차 M7-02, prefix 절단 금지=불필요 collision block 제거**), srcKey = f"visual-input/{uid}/{analysis_id}/{sourceHash}.png", bucket = env VISUAL_INPUT_BUCKET (**비-버저닝 전용 버킷 — delete 1회 완전 소거, VideoBucket 아님 주석 박제**). `created_keys: set[str]` = 이번 invocation PUT 성공 key object 별 추적(7차 B7-05 — createdThisInvocation 단일 bool 금지). **(0) B6-03 read-only preflight: `read_visual_job(job_id)`(31-02 신설 8번째 함수 — 단순 doc get, H7-03) — job 이 어떤 state 로든 존재하면 source PUT 하지 않는다(기존 outbox 처리만: dispatchState=='pending' + payload.srcKey==srcKey 면 best-effort resend + mark, 아니면 no-op) → return. 최종 권위는 reserve.** **(0.5) per-invocation reservation + key ownership (7차 B7-05 + 8차 B8-05 + 9차 B9-02): reservation_id = 이번 invocation 고유값(aws_request_id). srcKey PUT **전** ① `create_input_reservation(job_id, reservation_id, owner, source_hash=sourceHash, expected_keys=[srcKey(+trainingSrcKey)], now_ms)`(create-only B8-05) ② **각 expected key 에 `acquire_key_ownership(bucket, key, ref=reservation_id, now_ms)` 로 key-level liveRef 원자 추가(9차 B9-02 — 동일 deterministic key 를 쓰는 다른 live reservation 이 있으면 janitor 가 이 key 를 삭제하지 못하게)**. PUT 성공분은 `record_reservation_keys(created_keys=..., owner)`.** **(1) job 부재 → upload-first 조건부 생성 (B5-02 + M6-02): `put_object(bucket, srcKey, Body=src_png, ContentType image/png, Metadata={'sha256': sourceHash}, IfNoneMatch='*')` — 성공 시 created_keys.add(srcKey). 412(PreconditionFailed) 또는 409(concurrent, bounded retry 후에도) → HEAD 후 Metadata['sha256'] (full hex) 를 신규 full sourceHash 와 exact 비교 — 불일치 → log.error("srcKey collision/tampering") + **release_key_ownership(각 acquire 한 key, ref=reservation_id) + close_reservation** + return (job 0·vendor 0); 일치 → 재사용. put/head 그 외 실패 → **release + close_reservation** + log.warning + return**; blur option-b 면 trainingSrcKey 도 동일 조건부 put → 성공 시 created_keys.add(trainingSrcKey); **option-b 두번째 PUT 실패 시 첫 object(created_keys) 역순 delete 보상 + release_key_ownership + close_reservation, 실패 시 visualOrphans 등록 (7차 B7-05)**; **★10차 B10-03 blanket: acquire_key_ownership 한 key 는 모든 종료 경로(collision·put 실패·option-b 보상·analysis_missing·reserve 예외)에서 release_key_ownership + reservation close 를 원자 수행 — S3 delete 와 ownership ref release 는 한 종료 단위(미release 시 후속 동일-key cleanup 영구 skip)**; **(2) reserve_visual_job(kind 'correctedPose', allow_retry_failed=False, reservation_id=reservation_id, reservation_owner=aws_request_id, payload = target.to_payload() + {"srcKey","sourceHash"(full 64)(+trainingSrcKey)}) — job + analysis correctedPoseStatus 'pending' + 초기 outbox + **자기 reservation claim_reservation_for_job CAS** 를 한 transaction (B3-03 + 8차 B8-06). 반환 reason=='reservation_lost'(janitor 가 이미 claim) → created_keys 는 janitor 소유이므로 producer delete 0 + return(새 preflight 재시도). 성공 시 close_reservation(자기 것). try/except: reserve commit 전 예외 → H6-02 orphan 보상(아래 4).**; **(3) inputSealed hard gate + terminal-replay 즉시 삭제 (B6-03): reserve 결과 created False 이고 existing job(res.job) 의 (state ∈ ('done','failed')) OR (inputSealed==True) 이고 created_keys 비어있지 않으면 created_keys 전부 `delete_object(bucket, key)` 즉시 삭제 + `release_key_ownership(bucket, key, ref=reservation_id)`(9차 B9-02) + close_reservation + log (완료/sealed job 앞 새 source PII 0 — 비-버저닝이라 VersionId 불필요)**; **(4) H6-02 orphan 보상: reserve 가 reason=='analysis_missing' 또는 예외/commit loss → job 재read: job 부재면 created_keys 전부 즉시 delete_object + `release_key_ownership(ref=reservation_id)` + close_reservation(10차 B10-03); job 존재+input 소유(payload.srcKey==srcKey)면 유지. 보상 delete 실패 → VisualOrphanSourceObject put_metric_data + **필수 visualOrphans/{hash} 문서 기록**(옵션 아님 7차 H7-04) + return**; **(5) created True → best-effort sqs send({"jobId","generation","action":"create","outboxSeq": job.outboxSeq}) + mark_visual_job_dispatched(expect_action='create', expect_outbox_seq=outboxSeq) — send 실패 log 만**. created False 그 외 분기: existing 진행중(creating|polling|retry_ready|fetching|judging|pose_checking|postprocessing) & inputSealed False → no-op (진행 중 job 의 입력이라 삭제 금지 — 동일 hash 재사용); existing reserved + dispatchState 'pending' → sourceHash 동일하면 best-effort 재발행(현 outboxSeq), 다르면 log 후 no-op (실행 중 입력 교체 금지 — B2-03). 전체 try 재raise 0. DashScope/Gemini/pose 호출 0. Pod 런타임 전제 주석: Pod env VISUAL_QUEUE_URL/VISUAL_JOBS_ENABLED/VISUAL_INPUT_BUCKET + credential sqs:SendMessage + s3:PutObject/DeleteObject(VisualInputBucket) — 31-12 검증.
(B) template.yaml: **`Parameters.VisualInputBucketName` (Type String, **Default 없음 — 10차 H10-05: override 누락 시 deploy 실패(fail-open 금지). AllowedPattern 으로 bucket 명명 규칙 검증**)**; `VisualQueue` VisibilityTimeout 1800(=6×300 H2-01) + `VisualDLQ` maxReceiveCount 5 + `VisualDLQAlarm`; `VisualRequestFunction`(Timeout 10, POST /visual/rotation, env FIREBASE_SA_PARAM/VISUAL_QUEUE_URL/VISUAL_JOBS_ENABLED(!Ref)/ROTATION_DAILY_LIMIT "3"/ROTATION_GLOBAL_DAILY_LIMIT "30", Policies SSM + VisualQueue sqs:SendMessage); `VisualWorkerFunction`(Timeout 300, ReservedConcurrentExecutions 2, MemorySize = 31-05 subprocess RSS 실측 근거 주석, SQS 이벤트 BatchSize 1 + FunctionResponseTypes [ReportBatchItemFailures], **Schedule 이벤트 없음 — H3-09**, env VIDEO_BUCKET/**VISUAL_INPUT_BUCKET(!Ref VisualInputBucketName)**/FIREBASE_SA_PARAM/VISUAL_QUEUE_URL/DASHSCOPE_API_KEY resolve:ssm/PAIR_ID_HMAC_KEYS resolve:ssm/RUNPOD_ANALYZE_URL/RUNPOD_AUTH_TOKEN/GEMINI_API_KEY resolve:ssm(워커 전용)/**4 게이트 env(31-13 CALIBRATION chosen 값, 주석 CALIBRATION.json 경로 + B4-05)**, **Policies SSM + s3 Get(VideoBucket uploads/*, results/*) + Put(VideoBucket results/*, training/*) + 자기 큐 sqs:SendMessage + VisualInputBucket **bucket ARN s3:ListBucket(Condition s3:prefix=visual-input/*) — cleanup list_objects_v2/재조회(7차 B7-01)** + VisualInputBucket object ARN GetObject/PutObject/DeleteObject (visual-input/*) — 버전 액션 없음(비-버저닝) + **cloudwatch:PutMetricData(Condition cloudwatch:namespace=SunityVisual — VisualCleanupBlocked/VisualPairConflict 방출, 7차 H7-03)**(B6-02 + 7차 B7-01/H7-03)**); **`VisualDispatchFunction` (H3-09): CodeUri visual-dispatch/, Timeout 60, MemorySize 256, ReservedConcurrentExecutions 1, Events Schedule rate(1 minute), env FIREBASE_SA_PARAM/VISUAL_QUEUE_URL/**VISUAL_INPUT_BUCKET(!Ref — janitor visualOrphans/reservation sweep, 7차 H7-04)**, Policies SSM + VisualQueue sqs:SendMessage + cloudwatch:PutMetricData + **VisualInputBucket bucket ARN s3:ListBucket(Condition s3:prefix=visual-input/* — H8-04 prefix-scoped) + object ARN s3:DeleteObject + s3:GetObject(janitor delete 후 HEAD 404 검증 = GetObject 권한, `s3:HeadObject` 는 존재하지 않는 action — 8차 B8-04) (visual-input/*)**; **`VisualScannedOutboxAgeAlarm` Namespace 'SunityVisual' MetricName 'ScannedOutboxMaxAgeMs' >= 900000 ALARM (M6-01)** + **`VisualOutboxScanTruncatedAlarm` MetricName 'OutboxScanTruncated' >= 1 (H5-06)** + **`VisualOrphanSourceObjectAlarm` MetricName 'VisualOrphanSourceObject' >= 1 (H6-02)** + **`VisualCleanupBlockedAlarm` MetricName 'VisualCleanupBlocked' >= 1 + `VisualPairConflictAlarm` MetricName 'VisualPairConflict' >= 1 (7차 H7-03)** + **`VisualOrphanOldestAgeAlarm` Namespace 'SunityVisual' MetricName 'VisualOrphanOldestAgeMs' — **10차 H10-06: Threshold 는 Python 상수 표현식(×2) 금지, template Parameter `VisualOrphanOldestAgeThresholdMs`(concrete numeric Default) 로 정의하고 build test 가 models.VISUAL_INPUT_RESERVATION_TTL_MS×2 와 exact 일치 검사. Threshold/EvaluationPeriods/Period/DatapointsToAlarm/TreatMissingData(notBreaching) 전부 명시** + `VisualOrphanSweepFailedAlarm` MetricName 'VisualOrphanSweepFailed' >= 1(동일 concrete 정의) — 9차 H9-05/M8-05 실제 resource**; **build gate 3 (9차 H9-01): PipelineFunction Timeout×1000 + margin <= models.VISUAL_INPUT_RESERVATION_TTL_MS (janitor 가 정상 producer 를 expired 오판 금지 — YAML/model 단일 출처 assert)**; PipelineFunction — env VISUAL_QUEUE_URL/VISUAL_JOBS_ENABLED/**VISUAL_INPUT_BUCKET(!Ref)** + Policies VisualQueue sqs:SendMessage + **VisualInputBucket object ARN s3:PutObject/DeleteObject/GetObject(visual-input/*) — 버전 액션 없음 + cloudwatch:PutMetricData(namespace SunityVisual — VisualOrphanSourceObject, H7-03)**(H2-02 + B6-02 + B6-03 + 7차 H7-03); Parameters VisualJobsEnabled Default "false"; LogGroup 3종 Retention 30.
(C) visual_infra_dryrun.py (H2-09 + H4-03/H4-04/H3-08 + H5-07 + 6차 아키텍처 + 7차 B7-02/B7-07/H7-08 + **9차 B9-05**): **(0) bucket name = `infra/visual_input_bucket.json` 의 bucketName 필수 입력(하드코딩 기본값 금지 — 9차 B9-05; 파일 부재 시 STOP). 이하 모든 조회/lifecycle 대상은 이 값에서 파생.** (1) **VisualInputBucket 검사 (7차 B7-02): `get-bucket-versioning` 응답에 **`Status` key 자체가 없음(Never-versioned)** 만 통과 — Status=='Enabled' 또는 =='Suspended' → {"blocked": true, "reason": "visual_input_must_be_never_versioned — Suspended 도 단순 delete 로 과거 version 완전 삭제 미보장"}. `get-object-lock-configuration` 미설정 확인(위반 blocked). 사용 전 `list_object_versions` prefix read-only 로 Versions/DeleteMarkers 0 기록(checkpoint 주체만)**; (2) **페어 버킷(VideoBucket) 검사: `get-bucket-versioning` + `get-object-lock-configuration`(read-only) — Object Lock fail-closed(H4-04): ObjectLockEnabled=='Enabled' + default Retention → {"blocked": true, "reason": "object_lock_default_retention"}; Enabled + no-default → {"canary_required": true}(실 canary 는 31-12 — H5-07). `get-bucket-lifecycle-configuration` → 기존 규칙 전부 보존 + training/phase31/pairs/ retentionDays Expiration merge — versioning enabled/suspended 면 pairs prefix 에 NoncurrentVersionExpiration + ExpiredObjectDeleteMarker (H3-08)**; **(3) 버킷당 독립 4파일 산출 (7차 B7-07): `infra/visual_input_lifecycle_before.json`·`infra/visual_input_lifecycle_merged.json`(= visual-input/ 1일 Expiration + Filter{Prefix:"visual-input/"} 만, NoncurrentVersionExpiration 없음 — 비-버저닝 H7-08)·`infra/video_lifecycle_before.json`·`infra/video_lifecycle_merged.json`(VideoBucket 전체 규칙). 각 파일은 AWS API 가 바로 받는 `{"Rules":[...]}` 단일-버킷 형상(멀티버킷 wrapper 금지). dry-run 이 각 merged JSON 을 botocore shape validation + 기존 규칙 보존 bucket 별 assert**; (4) SSM `/sunity/motion/pair-id-hmac-keys` get 시도(read-only) — 존재 시 31-07 validate_hmac_key_set, 부재 시 생성 지시문. put 계열 코드 없음(31-12 전용). retentionDays 는 privacy_decision.json 로컬 읽기(M3-02 위반 아님 주석).
(D) test_visual_dispatch.py: flag OFF → S3/SQS 0; target None → 생략; **B6-03 preflight: job 이미 존재(어떤 state) → source PUT 0(기존 outbox 처리만) — put_object 호출 0 assert**; **B3-03 순서(job 부재 시): S3 put(IfNoneMatch) 이 reserve 보다 먼저**; **B5-02/M6-02/M7-02: srcKey = full 64 hex(prefix 절단 0 grep); put IfNoneMatch='*' 성공 → created_keys 추가; 412/409 → HEAD Metadata sha256 == 신규 → 재사용(재put 0), 불일치 → block(reserve 0·SQS 0·log)**; **B7-05/B8-05 per-invocation reservation: PUT 전 create_input_reservation(visualInputReservations/{jobId}_{reservationId}) create-only assert(call order: reservation → PUT → reserve); ★concurrent A/B: A create_input_reservation(id_A, keys=[A]) → B create_input_reservation(id_B, keys=[B]) → A 문서 overwrite 0(별도 doc) → A key PUT → A SIGKILL → janitor 가 id_A reservation 으로 A key 삭제(B8-05 key 유실 0); reserve 성공 → close_reservation(자기 것)**; **B8-06: reserve 가 claim_reservation_for_job CAS 성공해야 job 생성; reservation_lost(janitor 선점) → created_keys producer delete 0 + return**; **★10차 B10-02/B10-03: PUT 전 acquire_key_ownership(deleting fence — state='deleting'면 실패) + reserve 성공 시 _promote_..._to_job_tx; 모든 종료 경로(collision·put 실패·option-b 보상·analysis_missing·reserve 예외·terminal-replay) 후 release_key_ownership + close_reservation → ownership liveRefs 0/object doc 제거 assert; janitor deleting fence 중 producer acquire False barrier**; **9차 B9-03 multi-object: src PUT+record → trainingSrc PUT 2xx → record 전 crash 재현 시 expectedKeys 에 두 key 유지(createdKeys=[src]만) — janitor(31-09 test)가 expectedKeys∪createdKeys 로 둘 다 삭제함을 계약으로 명시**; **9차 B9-05: dry-run 이 infra/visual_input_bucket.json bucketName 읽음(기본값 하드코딩 grep 0), 파일 부재 STOP, alternative 이름 fixture 로 dry-run 통과**; **B6-03 inputSealed/terminal-replay: reserve done 또는 inputSealed True + created_keys 비어있지 않음 → created_keys 전부 delete_object 즉시(비-버저닝); 진행중 job(inputSealed False) → 삭제 0**; **H6-02/H7-04 orphan: reserve analysis_missing → created_keys delete; reserve 예외 → job 재read 부재면 delete·존재+소유면 유지; 보상 delete 실패 → VisualOrphanSourceObject metric + **필수 visualOrphans/{hash} 문서 기록**(옵션 아님 assert)**; created True → reserve + SQS 1(outboxSeq 포함) + mark(action+seq); **pending write reserve 내부뿐 (grep)**; **S3 put 예외(412/409 외) → reserve 0·SQS 0·비차단**; reserve 후 send crash → dispatchState 'pending' 잔존 + dispatcher 발행 → vendor create 1회; reserve commit_lost → 재실행 preflight job 존재 → PUT 0·created False + 일관; worker done 후 producer 재실행 → preflight job 존재 → PUT 0 (terminal-replay 삭제 대상도 없음); duplicate-while-progress → no-op; run_contended → job 1·SQS<=2; sourceHash 불일치 reserved → no-op; 훅 내 visual_gen import 부재; **H7-08 canary shape: visual_input_lifecycle_merged.json·video_lifecycle_merged.json 각각 `{"Rules":[...]}` botocore shape validation 통과 + VisualInput 규칙에 NoncurrentVersionExpiration 부재·Filter.Prefix 존재; 멀티버킷 wrapper JSON 부재**; **B7-02: dry-run 이 Status=='Suspended' fixture → blocked, Status key 부재 fixture → 통과, Status=='Enabled' → blocked**; **template 파싱 assert (H2-01/H3-09/B4-05/H6-07/M6-01/B6-02/7차 B7-01/H7-03): VisualInputBucketName Parameter 존재, VisualQueue VisibilityTimeout >= 6×Worker Timeout, maxReceiveCount >= 5, ReportBatchItemFailures, PipelineFunction SendMessage + VisualInputBucket s3 PutObject/DeleteObject + cloudwatch:PutMetricData, **VisualWorkerFunction VisualInputBucket bucket ARN s3:ListBucket(prefix condition 존재) + object Get/Put/Delete + cloudwatch:PutMetricData (s3:DeleteObjectVersion/ListBucketVersions 부재 assert — B7-01/B6-02)**, VisualDispatchFunction ReservedConcurrentExecutions==1 + Schedule rate(1 minute) + **VisualInputBucket bucket ARN ListBucket(prefix condition 존재 — H8-04) + object ARN DeleteObject + GetObject; 전 template 에 `s3:HeadObject` 문자열 0 assert(존재하지 않는 action — 8차 B8-04)**, VisualWorkerFunction Schedule 부재, **alarm 8종 logical ID 표 정합(9차 M9-03): VisualDLQAlarm + VisualScannedOutboxAgeAlarm + VisualOutboxScanTruncatedAlarm + VisualOrphanSourceObjectAlarm + VisualCleanupBlockedAlarm + VisualPairConflictAlarm + VisualOrphanOldestAgeAlarm + VisualOrphanSweepFailedAlarm — 각 resource 의 Namespace/MetricName/threshold 정의 존재(H9-05)**, 4 게이트 env 존재; build gate 1(H6-07): WorkerFunction Timeout*1000 < models.VISUAL_CLAIM_LEASE_MS < VisualQueue VisibilityTimeout*1000; build gate 2(B4-05/M4-03): strict ordering; **build gate 3(9차 H9-01): PipelineFunction Timeout*1000 + margin <= models.VISUAL_INPUT_RESERVATION_TTL_MS**.
실행 커밋 순서: (A/D) → (B template) → (C dryrun) 3분할.</action>
  <verify>
    <automated>(cd backend && sam validate --lint) && python -m pytest backend/tests/phase31/test_visual_dispatch.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - preflight + **per-invocation immutable reservation create-only(B8-05) + reserve claim_reservation_for_job CAS(B8-06)** + full-64hex + created_keys 별 inputSealed/terminal-replay/orphan delete + 필수 visualOrphans (B3-03/B6-03/H6-02/M6-02/7차 B7-05/8차 B8-05/B8-06)
    - send 후 analysis pending write 부재 (grep) + worker-done 경쟁 green (gate 5)
    - reserve 분기(done/진행중/reserved/failed/analysis_missing) + commit_lost + run_contended green
    - template: VisualInputBucketName Parameter + IAM 버전 액션 부재(B6-02) + visibility/redrive + DispatchFunction concurrency 1 + worker schedule 부재 + ScannedOutboxMaxAge/OutboxScanTruncated/Orphan alarm + 4 게이트 env + lease build gate(H6-07) + strict ordering build gate (H2-01/H3-09/B4-05/M6-01/H6-07)
    - dry-run 에 put 호출 0 + VisualInputBucket Never-versioned(Status key 부재)만 통과·Suspended blocked(B7-02) + 버킷당 4 lifecycle 파일 shape validation(B7-07/H7-08) + 페어 버킷 fail-closed 분기 + pairs NoncurrentVersionExpiration merge (H2-09/H4-03/H4-04/H5-07/H3-08/7차 B7-02/B7-07)
    - template: worker s3:ListBucket(prefix)+PutMetricData + **dispatcher prefix-scoped ListBucket + GetObject(HeadObject 문자열 0 — B8-04/H8-04)** + 5 alarm + M8-05 age/failure alarm + 버전 액션 부재 (7차 B7-01/H7-03 + 8차 B8-04/H8-04/M8-05)
    - visual_infra_dryrun.py 1회 실행 완료 — infra/visual_input_lifecycle_{before,merged}.json + video_lifecycle_{before,merged}.json 4파일 존재, 각 `{"Rules":[...]}` shape validation (M8-01 — 31-12 T2 전제)
    - `(cd backend && sam validate --lint)` && `python -m pytest backend/tests -q` exits 0
  </acceptance_criteria>
  <done>분석 경로가 preflight + per-invocation reservation(B8-05) + reserve claim CAS(B8-06) + terminal-replay/orphan 삭제 배선, IaC 가 신규 버킷·dispatcher 분리·**5 alarm + M8-05 age/failure alarm·worker ListBucket/dispatcher GetObject(HeadObject 부재 B8-04)·prefix-scoped ListBucket(H8-04)**·버킷당 4 lifecycle 파일(M8-01)·lease/strict ordering build gate 까지 배포 준비 완료.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 앱 → POST /visual/rotation | 미신뢰 입력 + 과금 유발 |
| 앱 → playback-url asset | key 는 서버 선택 exact 계약 — 클라이언트 key 0 |
| _process → SQS | preflight + upload-first(IfNoneMatch) + inputSealed gate + reserve 분기 + terminal-replay/orphan 삭제 + enqueue 만 |
| IaC → 실 스택 | flag 기본 OFF + 신규 비-버저닝 버킷 + 라이브 mutation 은 31-12 checkpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-38 | Spoofing | visual-request 인증 | mitigate | verify_request Firebase ID token → 401 |
| T-31-39 | Elevation of Privilege | 타인 analysisId | mitigate | uid 토큰 스코프 + guards_ok 단일 404 |
| T-31-40 | Denial of Service (과금) | 온디맨드 남용 | mitigate | transaction 한도(KST) + dedupe + done 멱등 + fail-closed env (H-06/M-06) |
| T-31-41 | Information Disclosure | 만료/임의/stale key 서명 | mitigate | exact basename + status done + content-type + 1h TTL (H-02 + M2-01) |
| T-31-42 | Denial of Service | dispatch 실패 고아 quota | mitigate | durable outbox(seq) + VisualDispatchFunction 복구(H6-01 필터) + ScannedOutboxMaxAge alarm (B4-01 + B5-01 + H3-09) |
| T-31-43 | Information Disclosure | DASHSCOPE/GEMINI/HMAC 키 | mitigate | resolve:ssm SecureString, GEMINI_API_KEY 워커 전용 |
| T-31-44 | Tampering | 미검증 기능 전면 노출 | mitigate | VisualJobsEnabled 기본 false (H-07) |
| T-31-54 | Tampering | replay 완료 되감기/입력 교체/16hex 충돌 | mitigate | preflight + upload-first(IfNoneMatch) + reserve 내 pending + immutable hash key + full-sha256 대조 (B2-03 + B3-03 + M4-04) |
| T-31-73 | Information Disclosure | terminal/sealed 앞 새 source PII 잔존 | mitigate | inputSealed hard gate + terminal-replay created_keys delete + orphan 보상 (B6-03/H6-02) |
| T-31-76 | Information Disclosure | 임시 생체 프레임 version 잔존 | mitigate | 전용 Never-versioned 버킷 — delete 1회 완전 소거, Suspended 오판 차단(Status 부재만 통과) (6차 아키텍처 + 7차 B7-02) |
| T-31-80 | Information Disclosure | PUT 직후 하드 크래시 orphan 이 lifecycle 대기까지 잔존 | mitigate | PUT 전 durable visualInputReservations + created_keys + janitor(즉시 삭제) + 필수 visualOrphans 문서 (7차 B7-05/H7-04) |
| T-31-81 | Denial of Service | cleanup 403(ListBucket 누락)으로 전 job cleanup_blocked | mitigate | worker bucket ARN s3:ListBucket(prefix) + 실 role canary (7차 B7-01) |
| T-31-55 | Tampering | 라이브 lifecycle 교체 규칙 소실 / Object Lock 삭제 불능(페어) | mitigate | dry-run merge + versioning 조회 + pairs NoncurrentVersionExpiration + Object Lock fail-closed(canary_required) — put·canary 는 31-12 (H2-09 + H4-03 + H4-04 + H5-07) |
</threat_model>

<verification>
- `python -m pytest backend/tests -q` green + `(cd backend && sam validate --lint)` green
- 응답 코드가 contract.md /visual/rotation 절과 일치
- 라이브 AWS 변경 0 (스크립트 read-only — H2-09)
- Lambda/pipeline 코드에 .planning 경로 참조 0 (M3-02 — dry-run 로컬 스크립트 제외)
- IAM 에 visual-input 버전 액션(s3:DeleteObjectVersion/ListBucketVersions) 부재 grep (비-버저닝 — B6-02)
</verification>

<success_criteria>
- 7~9차 축 + **10차 B10-02(active|deleting fence)/B10-03(전 종료경로 release)/H10-05($VIB fail-closed·Default 제거)/H10-06(alarm threshold concrete Parameter)** 전부 구현+테스트
- 6차 리뷰 §7 실행 허용 조건 중 enqueue/IaC 측 항목이 이 플랜 테스트로 추적 가능
</success_criteria>

<output>
Create `.planning/phases/31-api-visual-correction/31-10-SUMMARY.md` when done
</output>
