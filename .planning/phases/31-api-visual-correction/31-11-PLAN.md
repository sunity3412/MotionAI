---
phase: 31-api-visual-correction
plan: 11
type: execute
wave: 3
depends_on: [31-04, 31-08]
files_modified:
  - app/package.json
  - app/package-lock.json
  - app/jest.config.js
  - app/src/lib/api.ts
  - app/src/lib/visualCards.ts
  - app/src/lib/__tests__/visualCards.test.ts
  - app/src/components/__tests__/ReferenceCornerSection.test.tsx
  - app/src/app/analysis/result.tsx
autonomous: false
requirements: [D-06, D-08, D-09, D-10]
must_haves:
  truths:
    - "결과 화면 점수 내역 아래 '참고하세요' 섹션이 상태 구동으로 렌더된다 (D-09). **D-06 '완료 알림' 의 실체(백그라운드 push 알림+deep link vs 화면 열림 중 onSnapshot 실시간 갱신)는 Task 1c belle 결정에 따른다 (7차 H7-06) — 현재 계획은 onSnapshot 갱신(화면 열림 중), push 인프라(expo-notifications) 미설치. 결정이 push 이면 별도 알림 구현, 실시간 갱신이면 D-06 amended 로 CONTEXT 정정**"
    - "표시 URL 은 mount 시 재서명으로 발급되고 만료/403 시 재발급된다 — Firestore URL 의존 0 (리뷰 H-02)"
    - "API 오류는 typed ApiError(code) 로 분기된다 — message.includes 파싱 금지 (리뷰 M-05)"
    - "훅은 무조건 호출 — mode 분기 안에서 훅 호출 금지 (리뷰 M-04)"
    - "pending 타임아웃은 전용 correctedPoseUpdatedAtMs/rotationUpdatedAtMs 기준이며 표시 폴백일 뿐 서버 잡을 취소하지 않음이 주석으로 명시된다 (리뷰 H-06/L-04)"
    - "상태 분기·프레임 매핑이 typecheck 를 넘어 jest 컴포넌트/유닛 테스트로 검증된다 (리뷰 M-03)"
  artifacts:
    - path: "app/src/lib/visualCards.ts"
      provides: "상태 파생 순수 로직 (카드 상태·타임아웃·프레임 매핑·reference 선택)"
      contains: "rotationUpdatedAtMs"
    - path: "app/src/lib/api.ts"
      provides: "ApiError + requestRotationVideo + fetchVisualAssetUrl"
      contains: "class ApiError"
    - path: "app/src/app/analysis/result.tsx"
      provides: "참고코너 통합 (섹션 삽입 + URL 재서명 + 회전 요청)"
      contains: "ReferenceCornerSection"
  key_links:
    - from: "app/src/app/analysis/result.tsx"
      to: "app/src/components/ReferenceCornerSection.tsx"
      via: "점수 내역 아래 조건부 렌더 (31-08 승인 레이아웃)"
      pattern: "ReferenceCornerSection"
    - from: "app/src/lib/api.ts"
      to: "POST /visual/rotation + POST /playback-url(asset)"
      via: "인증 fetch + ApiError"
      pattern: "visual/rotation"
---

<objective>
앱 통합을 완성한다: typed ApiError (리뷰 M-05), 재서명 URL 클라이언트 (리뷰 H-02), 상태 파생 순수 로직 모듈 + result.tsx 통합 (무조건 훅 — 리뷰 M-04, 전용 timestamp 타임아웃 — 리뷰 H-06), 그리고 리뷰 M-03 이 요구한 typecheck 이상의 검증 — jest-expo 기반 유닛/컴포넌트 테스트 (신규 devDependency 는 package legitimacy checkpoint 통과 후 설치).

Purpose: 사용자가 실제로 보는 최종 통합 — 만료·실패·legacy·mode3 전 분기가 테스트로 고정된 상태.
Output: api.ts + visualCards.ts + result.tsx 통합 + jest 테스트.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-api-visual-correction/31-PATTERNS.md
@docs/contract.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: jest-expo 패키지 정당성 확인 + 설치</name>
  <what-built>리뷰 M-03(typecheck 은 상태 분기 검증에 불충분)을 충족하기 위한 앱 테스트 러너 도입 준비. RESEARCH 의 Package Legitimacy Audit 는 "신규 패키지 0" 기준이었으므로 신규 devDependency 3종은 [ASSUMED] — 설치 전 belle 확인 게이트 (auto-approve 불가).</what-built>
  <how-to-verify>
    1. Claude 가 제시하는 npmjs.com 확인 결과 검토: jest-expo (Expo 공식, expo org 퍼블리시), jest, @testing-library/react-native — 각 주간 다운로드/메인테이너/버전이 Expo SDK 54 호환인지
    2. devDependency 로만 추가되어 앱 번들에 미포함임을 확인
    3. 승인 시 "approved" 입력
  </how-to-verify>
  <resume-signal>"approved" 입력 (거부 시 대안 지시 — 거부되면 M-03 은 순수 로직 node 검증으로 축소하고 SUMMARY 에 deviation 기록)</resume-signal>
  <read_first>app/package.json (현행 의존성 — 테스트 러너 부재 확인), .planning/phases/31-api-visual-correction/31-REVIEWS.md 의 M-03 절</read_first>
  <action>npmjs.com/package/jest-expo, /jest, /@testing-library/react-native 를 WebFetch 로 확인해 퍼블리셔·다운로드·SDK 54 호환 버전을 표로 제시 → belle 승인 후 `npm install --save-dev jest-expo jest @testing-library/react-native` (Expo SDK 54 호환 버전) + package.json 에 "test": "jest" 스크립트 + jest.config.js (preset 'jest-expo', testMatch __tests__ 하위). 설치 후 빈 스모크 테스트 1개로 러너 기동 확인.</action>
  <verify>
    <automated>cd app && npx jest --listTests</automated>
  </verify>
  <acceptance_criteria>
    - belle "approved" 후에만 설치 수행
    - jest-expo 가 devDependencies 에만 존재 (dependencies 아님)
    - `cd app && npx jest --listTests` exits 0
  </acceptance_criteria>
  <done>테스트 러너가 정당성 확인을 거쳐 동작.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1c: D-06 '완료 알림' 실체 결정 (7차 H7-06 — 제품 목표 gap)</name>
  <context>31-CONTEXT.md 는 D-06 회전 영상(~6-7분·과금)을 "온디맨드 + 완료 알림 — 버튼 → 백그라운드 생성 → 카드 갱신" 으로 결정했다. 현재 31-11 계획은 결과 화면이 열려 있는 동안의 onSnapshot 카드 갱신만 제공하고, 백그라운드 push/local notification 등록·전송·권한·deep link 가 없다(app 에 expo-notifications 미설치 확인). 사용자가 6-7분 대기 중 화면을 닫으면 완료를 인지할 경로가 없다 — phase goal 축소 위험이라 belle 결정 필요.</context>
  <options>
    <option>
      <name>A: 백그라운드 완료 알림 구현 (expo-notifications + terminal rotation finalize outbox → push + result deep link)</name>
      <tradeoff>D-06 원안 충족. 앱 알림 권한 UX + 백엔드 push 발송(토큰 등록/저장) 신규 구현 비용. 파일럿 범위 증가.</tradeoff>
    </option>
    <option>
      <name>B: D-06 를 "결과 화면 열림 중 실시간 갱신" 으로 amended (CONTEXT 정정 + 현재 계획 유지)</name>
      <tradeoff>구현 비용 최소(현행 유지). 사용자는 결과 화면을 다시 열어 확인 — 완료 즉시 인지 불가. 파일럿 단계에서 수용 가능한지 belle 판단.</tradeoff>
    </option>
  </options>
  <resume-signal>belle 선택(A/B). A → 알림 구현 서브태스크 추가; B → 31-CONTEXT.md D-06 [AMENDED] 박제 + 현행 유지</resume-signal>
  <acceptance_criteria>
    - belle 가 A/B 중 선택하고, 선택이 CONTEXT/plan 에 반영됨 (A=구현 계획, B=amended decision 문구)
  </acceptance_criteria>
  <done>D-06 완료 알림의 실체가 belle 결정으로 확정 — 침묵 축소 0.</done>
</task>

<task type="auto">
  <name>Task 2: api.ts ApiError + 클라이언트 2종 + visualCards.ts 순수 로직</name>
  <files>app/src/lib/api.ts, app/src/lib/visualCards.ts, app/src/lib/__tests__/visualCards.test.ts</files>
  <read_first>app/src/lib/api.ts (전체 — 인증 헤더·에러 포맷·API_BASE_URL 관례), docs/contract.md 의 /visual/rotation + playback-url asset 절 (31-04 — 응답/에러 code 단일 기준), app/src/types/analysis.ts (correctedPose*/rotation* + FaultZoomComparison idx 필드)</read_first>
  <action>api.ts: `class ApiError extends Error { status: number; code: string }` 신설 — 응답 봉투 {error:{code,message}} 를 파싱해 code 보존 (파싱 실패 시 code 'unknown'), 기존 throw 지점을 ApiError 로 이관하되 기존 message 포맷 유지 (다른 소비처 무회귀 — 리뷰 M-05: message.includes 분기 금지의 대체). `requestRotationVideo(analysisId): Promise<{rotationStatus: 'pending' | 'done'}>` — POST /visual/rotation, 202/200 파싱 (rotationStatus 필드 존재 검증 — 스키마 방어), 그 외 ApiError throw. `fetchVisualAssetUrl(analysisId, asset: 'correctedPose' | 'rotation'): Promise<string>` — POST /playback-url body {analysisId, asset}, playbackUrl string 검증 후 반환. visualCards.ts 신설 (순수 함수만 — React/RN import 0, jest 검증 대상): `visualCardState(status, updatedAtMs, nowMs, timeoutMs) -> 'hidden'|'pending'|'done'` — status 부재/'failed' → hidden, 'pending'+경과>timeout → hidden (**주석: 표시 폴백일 뿐 서버 잡 취소 아님 — 운영 가시성은 backend list_stuck_visual_jobs.py, 리뷰 L-04**), 'pending' → pending, 'done' → done; 상수 `CORRECTED_POSE_PENDING_TIMEOUT_MS = 180000`, `ROTATION_PENDING_TIMEOUT_MS = 1500000` (worker 폴링 상한 20분 + 여유 — 31-09 MAX_POLLS 정합); `mapFrameIdx(idx, fromFrames, toFrames) -> number | null` — 프레임 공간 비율 매핑 + clamp, 비정상 입력 null; `pickCompareFrames(comparisons) -> {userIdx, refIdx} | null` — top-1 comparison 의 userFrameIdx/refFrameIdx, refMatched !== true 면 null (뷰어 숨김); `isDailyLimit(e) -> boolean` — e instanceof ApiError && e.code === 'daily_limit'; `isFeatureDisabled(e)` 동형. __tests__/visualCards.test.ts: 상태 전이표 전 케이스 (legacy 부재/failed/pending 신선/pending 만료/done), 타임아웃 경계값, mapFrameIdx 비율·clamp·비정상, pickCompareFrames refMatched false → null, ApiError code 분기.</action>
  <verify>
    <automated>cd app && npx jest src/lib/__tests__/visualCards.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - api.ts 에 class ApiError 존재 + requestRotationVideo/fetchVisualAssetUrl 이 코드 분기 가능한 ApiError throw
    - visualCards.ts 에 React import 0 (순수 모듈)
    - 상태 전이표·프레임 매핑 테스트 green
    - `cd app && npm run typecheck` exits 0
  </acceptance_criteria>
  <done>클라이언트·상태 로직이 typed + 테스트 고정.</done>
</task>

<task type="auto">
  <name>Task 3: result.tsx 통합 + 컴포넌트 테스트</name>
  <files>app/src/app/analysis/result.tsx, app/src/components/__tests__/ReferenceCornerSection.test.tsx</files>
  <read_first>app/src/app/analysis/result.tsx (전체 — lines 1056-1083 zoom pending effect, lines 1419-1440 섹션 삽입 지점, mode1/mode3 분기·referenceMotionId·recognizedMotionId 사용부, faultZoomComparisons 소비부, result.joints3d reshape 경로), app/src/components/ReferenceCornerSection.tsx + PoseCompareViewer.tsx (31-08 props), app/src/lib/referenceMotions.ts (useReferenceMotionDoc), app/src/lib/visualCards.ts (Task 2)</read_first>
  <action>result.tsx 통합: (1) 섹션 삽입 = 31-08 Task 1 승인 레이아웃 (기본: ScoreBreakdownSection 직후, sectionTitle + D-09 근거 주석). status=='done' 분석에서만. (2) 카드 상태 = visualCards.visualCardState 를 correctedPose(전용 correctedPoseUpdatedAtMs)/rotation(rotationUpdatedAtMs) 각각 적용 — 공용 updatedAt 사용 금지 (리뷰 H-06), setInterval/폴링 신설 금지 (onSnapshot 재렌더가 전부 — D-06). (3) URL 재서명: correctedPose 'done' 시 useEffect 로 fetchVisualAssetUrl('correctedPose') 1회 발급 → state 보관, Image onError/403 시 1회 재발급 (재시도 상한 1 — 루프 방지), rotation 'done' 도 동형 — **Firestore 의 URL 필드 소비 0** (계약에 없음, 리뷰 H-02). (4) 비교 뷰어 데이터: **훅은 전부 무조건 호출** (리뷰 M-04) — `const targetRefId = mode === 'mode1' ? referenceMotionId : null` 파생 후 useReferenceMotionDoc(targetRefId) 항상 호출 (mode3 = null → 뷰어 숨김, Pitfall 6 정직 강등 — 주석 명시); userPose = result.joints3d reshape 의 pickCompareFrames userIdx 프레임 (mapFrameIdx 로 joints3d 공간 매핑), refPose = reference joints3d 의 refIdx 매핑 프레임; pickCompareFrames null → 뷰어 숨김. (5) 회전 버튼: busy state + requestRotationVideo — 성공 시 로컬 pending 반영 (onSnapshot 이 동기화), catch 에서 isDailyLimit → limitNotice 한국어 안내 전달 (인라인 — 배너/토스트 금지), isFeatureDisabled/기타 → 조용히 버튼 원복 (D-08); try/catch/finally busy 해제. __tests__/ReferenceCornerSection.test.tsx (@testing-library/react-native): 'hidden' 상태 카드 미렌더, 'pending' 자리표시 렌더, 'ready' 이미지 렌더 + onError 시 onCorrectedPoseImageError 호출, limitNotice 인라인 표시, 'requestable' 버튼 disabled=busy, 다각도/3D 문구 부재 (queryByText 0). 필요 시 expo-video/expo-image 는 jest.config moduleNameMapper 로 mock.</action>
  <verify>
    <automated>cd app && npx jest && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - result.tsx 에 ReferenceCornerSection 렌더 + correctedPoseUpdatedAtMs/rotationUpdatedAtMs 기반 상태 존재, 신규 setInterval 0
    - 훅 호출이 조건문/mode 분기 내부에 없음 (grep + 코드 확인 — 리뷰 M-04)
    - Firestore URL 필드 참조 0 (silhouetteImageUrl/rotationVideoUrl 문자열 0)
    - 컴포넌트 테스트 5케이스 이상 green
    - `cd app && npx jest && npm run typecheck` exits 0
  </acceptance_criteria>
  <done>참고코너가 재서명·전용 timestamp·조용한 폴백·정직 강등까지 테스트 고정으로 통합됨.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firestore doc → 화면 | normalize 통과 필드만 소비 |
| 앱 → /visual/rotation·playback-url | 인증 fetch + typed 오류 분기 |
| npm registry → devDependency | 패키지 정당성 게이트 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-45 | Spoofing | 회전 요청 | mitigate | 기존 인증 fetch (Firebase ID token) — 무토큰 경로 없음 |
| T-31-46 | Denial of Service | 버튼 연타 | mitigate | busy state + 서버 dedupe(31-10) 이중 방어 |
| T-31-47 | Repudiation (신뢰 인상) | 실패 노출 | mitigate | 비-한도 오류 조용 처리 + 카드 숨김 (D-08) — Alert/토스트 0 acceptance |
| T-31-SC | Tampering | npm 설치 (jest-expo/jest/@testing-library) | mitigate | [ASSUMED] 3종 — Task 1 blocking-human 게이트에서 npmjs 확인 후 설치 (auto-approve 불가) |
</threat_model>

<verification>
- `cd app && npx jest && npm run typecheck` green
- 실기기 렌더/제스처는 31-12 HUMAN-UAT 적립 ([[batch-uat-after-phase-31]])
</verification>

<success_criteria>
- D-06/D-08/D-09 통합 + H-02 재서명 + M-03/M-04/M-05/H-06 앱 측 전부 구현·테스트 고정
</success_criteria>

<output>
Create `.planning/phases/31-api-visual-correction/31-11-SUMMARY.md` when done
</output>
