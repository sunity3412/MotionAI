---
phase: 31-api-visual-correction
plan: 12
type: execute
wave: 6
depends_on: [31-03, 31-09, 31-10, 31-11]
files_modified:
  - .planning/phases/31-api-visual-correction/31-HUMAN-UAT.md
  - .planning/phases/31-api-visual-correction/infra/lifecycle_before.json
  - plan.md
autonomous: false
requirements: [D-01, D-05, D-06]
must_haves:
  truths:
    - "배포 게이트는 RESULTS.json 과 CALIBRATION.json 을 각각 읽어 둘 다 blocked false + chosen 4값 존재 + strict ordering(training 더 엄격) + template env 4값이 chosen 일치를 검사한다 — 하나라도 위반이면 라이브 롤아웃 차단, flag OFF 유지 (4차 리뷰 H4-09)"
    - "build 산출물 acceptance 가 .aws-sam/build/VisualDispatchFunction + 세 함수 LogGroup/alarm 을 확인한다 (4차 리뷰 H4-09/H4-02)"
    - "라이브 인프라 mutation(bucket lifecycle merge put + SSM versioned HMAC 키)은 belle 승인 checkpoint 에서만 — before JSON rollback + put 후 get 검증. lifecycle apply 경로는 repo-root 기준 .planning/phases/31-api-visual-correction/infra/lifecycle_merged.json (4차 리뷰 M4-01) (2차 H2-09)"
    - "Object Lock 이 개인정보 retention/delete 약속과 충돌하면 배포 차단 — 삭제/lifecycle 스크립트가 403(retention/legal hold) 만나면 성공 진행 금지 + retention/hold 출력 + 비정상 종료 (4차 리뷰 H4-04)"
    - "IAM 실권한 검증은 simulate-principal-policy 또는 iam_probe send-only canary — production queue receive/delete 금지 (4차 리뷰 H4-02) (2차 H2-02)"
    - "phase 완료 필요조건 = 실 인증 E2E 2종 통과: (1) correctedPose done + fresh URL + 입력 정리 + versioned 잔존 0 + 비동의 학습 0, (2) 회전 요청 → quota → SQS → dispatcher/worker → done + fresh URL + 2차 멱등"
    - "E2E 후 visual-input prefix 는 list-object-versions 로 Versions/DeleteMarkers 전부 0 을 검사한다 — 단순 aws s3 ls 는 noncurrent PII 를 놓친다 (4차 리뷰 H4-03)"
    - "generic 실패는 성공이 아니다 — failed 종결은 typed 'moderation' 만 배선 실증으로 인정 (리뷰 H-07)"
    - "Pod/워커 runtime 미가용이면 phase 미완료 — 침묵 이월 금지"
    - "실기기 확인 항목은 HUMAN-UAT.md 적립 ([[batch-uat-after-phase-31]])"
  artifacts:
    - path: ".planning/phases/31-api-visual-correction/31-HUMAN-UAT.md"
      provides: "배치 UAT 적립 (D-09/D-10 2D 뷰어/교정 이미지/회전 카드/화살표 시각 게이트)"
      contains: "D-10"
    - path: ".planning/phases/31-api-visual-correction/infra/lifecycle_before.json"
      provides: "lifecycle 적용 전 rollback artifact (H2-09)"
  key_links:
    - from: "backend/template.yaml"
      to: "sunity-motion-pilot 스택"
      via: "sam build --use-container + belle sam deploy (flag OFF → E2E 후 ON)"
      pattern: "sunity-motion-pilot"
---

<objective>
fail-closed 배포 게이트를 실행한다. 순서: 전체 게이트(테스트+빌드+**RESULTS/CALIBRATION/template env 3중 검사 H4-09**) → **라이브 인프라 mutation checkpoint (H2-09 + M4-01 경로 + H4-04 Object Lock fail gate)** → belle 배포(flag OFF) + 배포 후 보안 + **IAM 실권한(H4-02 send-only iam_probe)** → Pod 재생성 + flag ON canary → 실 E2E 2종(**H4-03 versioned 잔존 검사**) → OTA + HUMAN-UAT.

이 플랜이 끝나지 않으면 phase 31 은 완료가 아니다.

Purpose: 실제 사용자 여정 증명 전 "완료" 선언 차단 + 라이브 인프라 변경을 승인·rollback·Object Lock 안전 경로로 격리.
Output: 적용된 lifecycle/SSM(승인분) + 배포 스택(canary) + E2E 증적 + OTA + 31-HUMAN-UAT.md + plan.md.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-api-visual-correction/31-CONTEXT.md
@.planning/phases/31-api-visual-correction/smoke/RESULTS.json
@.planning/phases/31-api-visual-correction/smoke/CALIBRATION.json
@backend/samconfig.toml
@.planning/phases/31-api-visual-correction/31-PLAN-REVIEW-ITERATION4.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 전체 게이트 — 테스트·빌드·RESULTS/CALIBRATION/template 3중 blocked·ordering 검사 (H4-09)</name>
  <files>(빌드 산출물만 — 소스 변경 없음)</files>
  <read_first>backend/samconfig.toml (stack sunity-motion-pilot / ap-northeast-2 / Stage=pilot), .planning/phases/31-api-visual-correction/smoke/RESULTS.json (blocked/sync), .planning/phases/31-api-visual-correction/smoke/CALIBRATION.json (blocked + chosen 4값), backend/template.yaml (VisualWorkerFunction 4 게이트 env), .planning/phases/31-api-visual-correction/31-PLAN-REVIEW-ITERATION4.md 의 H4-09 절</read_first>
  <action>순서 실행 (repo 루트 기준 — `cd backend` 후 `backend/tests` 참조 금지): (1) `python -m pytest backend/tests -q`, (2) `cd app && npm run typecheck`, (3) `cd app && npx jest`, (4) `(cd backend && sam validate --lint)`, (5) `(cd backend && sam build --use-container)` ([[sam-build-native-deps]]), (6) **blocked/ordering 게이트 (H4-09): RESULTS.json 과 CALIBRATION.json 을 각각 읽어 — (a) RESULTS.blocked==false (async-only 통과 포함 — B4-02), (b) CALIBRATION.blocked==false + chosen 4값(display_pose_tol_deg/display_confidence/training_pose_tol_deg/training_confidence) 존재 + strict ordering(training_confidence > display_confidence AND training_pose_tol_deg < display_pose_tol_deg), (c) template.yaml VisualWorkerFunction 의 DISPLAY_JUDGE_CONFIDENCE/TRAINING_JUDGE_CONFIDENCE/DISPLAY_POSE_TOL_DEG/TRAINING_POSE_TOL_DEG env 4값이 CALIBRATION chosen 과 exact 일치. 하나라도 위반 → STOP: 인프라만(flag OFF 고정), E2E-A 불가 → phase 미완료 SUMMARY + belle 보고**, (7) **build 산출물 acceptance (H4-02/H4-09): `.aws-sam/build/` 에 VisualRequestFunction + VisualWorkerFunction + VisualDispatchFunction 존재 + template 에 세 함수 LogGroup + VisualOutboxAgeAlarm + VisualDLQAlarm 존재 확인**. 실패 수정 필요 시 최소 diff + SUMMARY.</action>
  <verify>
    <automated>python -m pytest backend/tests -q && (cd backend && sam validate --lint)</automated>
  </verify>
  <acceptance_criteria>
    - backend suite + app typecheck + jest 전부 exits 0
    - RESULTS/CALIBRATION 둘 다 blocked false + chosen 4값 + strict ordering + template env 일치 (H4-09) — 위반 시 STOP 기록
    - `.aws-sam/build` 에 VisualRequestFunction/VisualWorkerFunction/VisualDispatchFunction 존재 + 세 LogGroup/alarm (H4-02/H4-09)
  </acceptance_criteria>
  <done>배포 가능 상태 + 3중 blocked/ordering 게이트 + dispatcher 산출물 판정 완료.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: 라이브 인프라 mutation — lifecycle merge + SSM versioned HMAC 키 (H2-09 + M4-01 + H4-04)</name>
  <what-built>31-10 visual_infra_dryrun.py 가 만든 artifact: (1) lifecycle_before.json (rollback) + lifecycle_merged.json (기존 규칙 보존 + visual-input/ 1일 + training/phase31/pairs/ retentionDays + versioning 시 NoncurrentVersionExpiration/ExpiredObjectDeleteMarker), (2) SSM versioned JSON 지시문, (3) **Object Lock 조회 결과 (H4-04 — enabled+default retention 이면 dry-run 이 blocked 플래그)**. put-bucket-lifecycle-configuration 은 기존 lifecycle 전체를 교체하므로 적용은 이 checkpoint 로 격리.</what-built>
  <how-to-verify>
    1. Claude 가 제시하는 before/merged JSON diff 검토 — 기존 규칙 전부 보존 + 신규 규칙(visual-input 1일, pairs retentionDays, NoncurrentVersionExpiration)만 추가 확인
    2. **Object Lock 결과 확인 (H4-04): dry-run 이 blocked=true(default retention 존재)면 여기서 STOP — 민감 prefix 를 이 bucket 에 두지 않는 대안(별도 non-locked bucket)을 belle 과 결정. blocked=false 여야 진행**
    3. SSM 신규 키 계획(versioned JSON, SecureString) 확인
    4. 승인 시 "approved" → Claude 적용 + 검증
  </how-to-verify>
  <resume-signal>"approved" (수정 요청 시 merged JSON 재생성 후 재제시)</resume-signal>
  <read_first>.planning/phases/31-api-visual-correction/infra/lifecycle_before.json + lifecycle_merged.json (31-10 산출), .planning/phases/31-api-visual-correction/smoke/privacy_decision.json (retentionDays), 31-10 dry-run 의 Object Lock 결과 JSON, .planning/phases/31-api-visual-correction/31-PLAN-REVIEW-ITERATION4.md 의 H4-04/M4-01 절</read_first>
  <action>belle 승인 후 Claude 실행 (AWS_PROFILE=sunity-motion): (0) **Object Lock 게이트 (H4-04): dry-run 결과가 blocked(default retention) 이면 put 금지 — belle 결정(별도 bucket) 없이 진행 불가**; (1) lifecycle_before.json rollback artifact 보존 확인; (2) **`aws s3api put-bucket-lifecycle-configuration --bucket sunity-motion-pilot-videos --lifecycle-configuration file://.planning/phases/31-api-visual-correction/infra/lifecycle_merged.json` — repo-root 기준 정확 경로 (M4-01, `file://lifecycle_merged.json` 상대경로 금지)**; (3) **put 후 get 검증 (H2-09): get-bucket-lifecycle-configuration 재조회 → 규칙 ID/개수/prefix + NoncurrentVersionExpiration 규칙이 merged 와 일치 assert — 소실 시 즉시 before JSON rollback put**; (4) SSM `/sunity/motion/pair-id-hmac-keys` 부재 시 `aws ssm put-parameter --type SecureString` versioned JSON({"active":"v1","keys":{"v1":"<랜덤 32바이트 hex>"}}) 생성(기존 존재 시 무변경) + get 형식 검증(값 비로깅). **삭제/lifecycle 관련 403(Object Lock retention/legal hold) 발생 시 성공 진행 금지 + retention/hold 출력 + 중단 (H4-04)**. 결과(규칙 개수 before/after, NoncurrentVersionExpiration 존재, SSM 확인)를 SUMMARY.</action>
  <verify>
    <automated>N/A - checkpoint:human-action (belle 승인이 게이트; put 후 get 검증은 resume 후 Claude 실행·기록)</automated>
  </verify>
  <acceptance_criteria>
    - Object Lock default retention blocked 아님 확인 후에만 진행 (H4-04)
    - belle "approved" 후에만 put 실행 + lifecycle_merged.json repo-root 정확 경로 (M4-01)
    - put 후 get 에서 기존 규칙 전부 + 신규 규칙 + NoncurrentVersionExpiration 확인 (개수/ID/prefix 기록)
    - lifecycle_before.json rollback artifact 보존 + SSM versioned JSON 존재 (값 미로깅)
  </acceptance_criteria>
  <done>라이브 인프라 변경이 승인·rollback·Object Lock 안전 경로로 적용됨.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: belle 배포 (flag OFF) + 배포 후 보안·IAM 실검증 (H4-02 send-only)</name>
  <what-built>VisualRequestFunction(POST /visual/rotation) + VisualWorkerFunction(**Schedule 없음 — H3-09/H4-02**) + **VisualDispatchFunction(reserved concurrency 1 + rate(1 minute))** + VisualQueue(visibility 1800/maxReceive 5)/DLQ+Alarm + VisualOutboxAgeAlarm + playback-url asset + PipelineFunction SendMessage 정책이 포함된 sam build. VisualJobsEnabled 기본 false. sam deploy 는 belle 수동(`!` 프리픽스).</what-built>
  <how-to-verify>
    1. `!cd backend && sam deploy` (samconfig 기본 — VisualJobsEnabled Default "false")
    2. changeset 에 VisualRequestFunction/VisualWorkerFunction/**VisualDispatchFunction**/VisualQueue Add + PipelineFunction 정책 Modify 확인 후 승인
    3. "Successfully created/updated stack - sunity-motion-pilot" 확인
  </how-to-verify>
  <resume-signal>"deployed" (실패 시 에러 로그)</resume-signal>
  <read_first>backend/samconfig.toml, .planning/phases/31-api-visual-correction/31-PLAN-REVIEW-ITERATION4.md 의 H4-02 절</read_first>
  <action>belle "deployed" 후 Claude 실검증 (AWS_PROFILE=sunity-motion): (1) 무토큰 POST /visual/rotation → 401, (2) fixture 유효 토큰 → 503 'feature_disabled' (flag OFF), (3) 유효 토큰 + asset='rotation' 필드 없는 문서 → 404 단일, **(4) IAM 실권한 (H4-02 — receive/delete 금지): `aws iam simulate-principal-policy` 로 PipelineFunction role + RunPod IAM user 각각의 VisualQueue ARN sqs:SendMessage allowed 확인. simulate 불가 시 대체 = RunPod credential 로 iam_probe canary **send 만** (`{jobId:"iam-probe", action:"iam_probe", generation:0, outboxSeq:0}` schema) → VisualWorker CloudWatch 로그에서 해당 probe ID 소비(외부 side-effect 0) 확인 — production queue receive/delete 절대 금지. RunPod user 권한 부재 시 최소 정책 JSON 제시 + belle 승인 하 attach 후 재검증**. 결과 4종을 SUMMARY.</action>
  <verify>
    <automated>N/A - checkpoint:human-action (belle 배포가 게이트; 검증 4종은 resume 후 Claude 실행·기록)</automated>
  </verify>
  <acceptance_criteria>
    - 스택 업데이트 성공 로그 + changeset 에 VisualDispatchFunction Add 확인
    - 무토큰 401 / flag OFF 503 / asset 부재 404 실측
    - IAM SendMessage allowed 실증 — iam_probe send-only canary(worker 로그 소비 확인), production queue receive/delete 0 (H4-02)
  </acceptance_criteria>
  <done>인프라가 기능 OFF 로 라이브 — 보안 경계·producer 권한 실검증(send-only) 완료.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Pod 재생성 + flag ON canary → 실 E2E 2종(versioned 잔존 검사) → OTA + UAT</name>
  <what-built>E2E 전제 2가지가 인간 액션: (1) RunPod Pod 생성 (belle 콘솔), (2) flag ON 배포 (`!cd backend && sam deploy --parameter-overrides VisualJobsEnabled=true`). Pod 셋업은 Claude SSH ([[pod-ops-claude-runs]]) — env 신규: VISUAL_JOBS_ENABLED=true, VISUAL_QUEUE_URL (+기존 AWS/FIREBASE/GEMINI) + [[current-pod-hbpvhedq2bu01i]] 절차 + SSM runpod-analyze-url/Lambda env 재동기화.</what-built>
  <how-to-verify>
    1. RunPod 콘솔 Pod 생성 (Network Storage — [[next-pod-use-network-storage]]) 후 SSH 전달
    2. Claude Pod 셋업 + 헬스체크(+/pose-image 스모크) 완료 보고
    3. `!cd backend && sam deploy --parameter-overrides VisualJobsEnabled=true`
  </how-to-verify>
  <resume-signal>"pod ready + deployed"</resume-signal>
  <read_first>.planning/phases/30-growth-tracking/ 최신 SUMMARY (OTA eas update 선례), 최근 phase HUMAN-UAT.md 1개 (적립 형식), backend/scripts/list_stuck_visual_jobs.py (E2E 잡 관찰), .planning/phases/31-api-visual-correction/31-PLAN-REVIEW-ITERATION4.md 의 H4-03 절</read_first>
  <action>resume 후 Claude 실 E2E 2종 (fixture 계정, AWS_PROFILE=sunity-motion):

**E2E-A 자동 correctedPose (D-05):** fixture 신규 분석 1건 업로드 → 분석 done 후 correctedPoseStatus 관찰 → 판정: (a) 'done' = playback-url asset='correctedPose' fresh URL + HTTP 200 이미지 로드 + job 문서 판정 메타(judgeDisplayPass/judgeTrainingPass/judgeConfidence + poseGatePassed/poseGateErrorDeg/poseGateReason + pairStoreStatus) 존재 + **`aws s3api list-object-versions --bucket sunity-motion-pilot-videos --prefix visual-input/{uid}/{analysisId}/` 의 Versions + DeleteMarkers 전부 0 (H4-03 — 단순 s3 ls 아님; versioned noncurrent PII 잔존 검사)** + learningOptIn 상태별 training/phase31/pairs/ 정합(비동의 = 신규 0) — PASS; (b) 'failed' + failureReason=='moderation' = 배선 실증 별도 기록(성공 아님) — 다른 fixture 1회 재시도; (c) 그 외 failed/pending 고아/'create_unconfirmed' = FAIL — 원인 수정 전 phase 미완료.

**E2E-B 회전 온디맨드 (D-06):** fixture 토큰 POST /visual/rotation → 202 + visualQuota +1 → 실 SQS 경유 **dispatcher/worker 관찰 (list_stuck_visual_jobs.py + CloudWatch — action 체인 create→poll→fetch 로그 + VisualDispatchFunction 발행 로그 확인)** → rotationStatus 'done' → asset='rotation' fresh URL 로드 → 2차 요청 → 200 done 멱등 + 벤더 task 신규 0(taskId 불변) + quota 불변.

완료 처리 (두 E2E PASS 시에만): (1) OTA 발행 (phase 30 선례; 참고코너 필드 부재 시 자동 숨김 — 선발행 안전), (2) 31-HUMAN-UAT.md 적립: D-09 참고코너 배치/"참고하세요" 분리, D-10 2D 비교 뷰어("3D" 아님 확인), 교정 이미지 카드, 회전 영상 재생, 결함 줌 화살표(E2E-A zoom PNG Claude Read 로 화살표 방향 선검토 — B-02 rollout 전 시각 게이트), (3) plan.md 갱신, (4) SUMMARY 에 Pod env 체크리스트 갱신분 박제. **어느 한 E2E 라도 미통과면 phase 31 미완료 명시 — OTA/완료 마킹 금지.**</action>
  <verify>
    <automated>test -f .planning/phases/31-api-visual-correction/31-HUMAN-UAT.md && grep -q "D-10" .planning/phases/31-api-visual-correction/31-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - E2E-A: 'done' + fresh URL 200 + visual-input list-object-versions Versions/DeleteMarkers 0 + 비동의 training 0 (H4-03 실측)
    - E2E-B: 'done' + fresh URL + 2차 멱등(taskId·quota 불변) + dispatcher/worker 로그 실측
    - failed 종결은 'moderation' 만 배선 실증
    - 두 E2E PASS 후에만 OTA update id 존재
    - 31-HUMAN-UAT.md 에 D-09/D-10/교정 이미지/회전/화살표 항목 존재
  </acceptance_criteria>
  <done>실 사용자 여정 2종 증명 + versioned 잔존 0 + canary ON — phase 31 완료 조건 충족 (미통과 시 미완료 명시).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 배포 파이프라인 | belle 수동 게이트 + flag 기본 OFF |
| 라이브 bucket lifecycle | put = 전체 교체 — 승인 + before rollback + get 검증 + Object Lock fail gate (H2-09/H4-04) |
| IAM 실검증 | iam_probe send-only — production queue receive/delete 0 (H4-02) |
| 실검증 호출 | 실 과금(~$0.8/건)·실 데이터 접촉 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-48 | Elevation of Privilege | 무단 배포/flag ON/인프라 mutation | mitigate | checkpoint:human-action 3회 (mutation·OFF·ON 각각 belle) |
| T-31-49 | Spoofing | 라이브 엔드포인트 | mitigate | 무토큰 401 + flag OFF 503 실검증 |
| T-31-50 | Denial of Service (과금) | E2E 호출 | mitigate | fixture 계정·회전 1건+멱등 2차 |
| T-31-51 | Repudiation | 검증 없는 완료 선언 | mitigate | 실 E2E 2종 PASS = 완료 필요조건 — Pod 미가용 = 미완료 (H-07) |
| T-31-55 | Tampering | lifecycle 교체 규칙 소실 / noncurrent PII 잔존 | mitigate | before rollback + put 후 get + list-object-versions 잔존 0 (H2-09/H4-03) |
| T-31-66 | Repudiation | 측정 미달 임계값으로 라이브 롤아웃 | mitigate | RESULTS/CALIBRATION/template 3중 blocked·ordering 게이트 (H4-09) |
| T-31-67 | Information Disclosure | iam canary 가 실 메시지 receive/delete | mitigate | send-only iam_probe + worker 로그 소비 확인 — receive/delete 금지 (H4-02) |
| T-31-68 | Tampering | Object Lock retention 하 삭제 약속 불이행 | mitigate | 403 시 진행 금지 + default retention blocked (H4-04) |
</threat_model>

<verification>
- 3중 blocked/ordering 게이트 + 인프라 mutation put 후 get + 스택 2단 배포(OFF→ON) + 401/503/404/IAM send-only + E2E 2종 + list-object-versions 잔존 0 실측 기록
- OTA + HUMAN-UAT 적립은 E2E PASS 조건부
</verification>

<success_criteria>
- 4차 리뷰 H4-09(blocked/build gate)·H4-02(dispatcher/iam_probe send-only)·H4-03(versioned E2E)·H4-04(Object Lock)·M4-01(lifecycle 경로) + 2차 gate 8/15/16 전부 실측
- D-01 풀통합 라이브 (조건 미충족 시 미완료 명시 — 침묵 이월 0)
</success_criteria>

<output>
Create `.planning/phases/31-api-visual-correction/31-12-SUMMARY.md` when done
</output>
</content>
</invoke>
