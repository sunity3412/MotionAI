---
phase: 31-api-visual-correction
plan: 12
type: execute
wave: 6
depends_on: [31-03, 31-09, 31-10, 31-11]
files_modified:
  - .planning/phases/31-api-visual-correction/31-HUMAN-UAT.md
  - .planning/phases/31-api-visual-correction/infra/lifecycle_before.json
  - plan.md
autonomous: false
requirements: [D-01, D-05, D-06]
must_haves:
  truths:
    - "라이브 인프라 mutation(bucket lifecycle merge put + SSM versioned HMAC 키 생성)은 belle 승인 checkpoint 에서만 수행된다 — before JSON rollback artifact 보관 + put 후 get 검증 (2차 리뷰 H2-09, §8 gate 15)"
    - "PipelineFunction role 과 RunPod producer credential 의 VisualQueue sqs:SendMessage 실권한이 검증된다 — simulate-principal-policy 또는 canary send/delete (2차 리뷰 H2-02, §8 gate 8)"
    - "배포는 fail-closed — RESULTS.json.blocked=true 면 라이브 롤아웃 차단, flag 기본 OFF 로만 배포 (리뷰 H-03/H-07)"
    - "phase 완료의 필요조건 = 실 인증 E2E 2종 통과 (§8 gate 16): (1) 신규 분석 → correctedPose done + fresh URL + 입력 정리 + 비동의 학습 기록 0, (2) 회전 요청 → quota → 실 SQS → 워커 → done + fresh URL + 2차 요청 멱등"
    - "generic 실패는 성공이 아니다 — failed 종결은 typed failureReason 'moderation' 만 배선 실증으로 인정 (리뷰 H-07)"
    - "Pod/워커 runtime 미가용이면 phase 미완료로 남는다 — 침묵 이월 금지"
    - "실기기 확인 항목은 HUMAN-UAT.md 적립 — 즉시 belle 호출 금지 ([[batch-uat-after-phase-31]])"
  artifacts:
    - path: ".planning/phases/31-api-visual-correction/31-HUMAN-UAT.md"
      provides: "배치 UAT 적립 (D-09/D-10 2D 뷰어/교정 이미지/회전 카드/화살표 시각 게이트)"
      contains: "D-10"
    - path: ".planning/phases/31-api-visual-correction/infra/lifecycle_before.json"
      provides: "lifecycle 적용 전 rollback artifact (H2-09)"
  key_links:
    - from: "backend/template.yaml"
      to: "sunity-motion-pilot 스택"
      via: "sam build --use-container + belle sam deploy (flag OFF → E2E 후 ON)"
      pattern: "sunity-motion-pilot"
---

<objective>
fail-closed 배포 게이트를 실행한다. 순서: 전체 게이트(테스트+빌드+blocked 검사) → **라이브 인프라 mutation checkpoint (H2-09 — 31-10 dry-run artifact 를 belle 승인 후 적용)** → belle 배포 (flag OFF) + 배포 후 보안 실검증 + **IAM 실권한 검증 (H2-02)** → Pod 재생성 + flag ON canary → 실 인증 E2E 2종 → OTA + HUMAN-UAT 적립.

이 플랜이 끝나지 않으면 phase 31 은 완료가 아니다.

Purpose: 실제 사용자 여정이 증명되기 전에 "완료" 가 선언되는 것을 구조적으로 차단 + 라이브 인프라 변경을 승인·rollback 가능 경로로 격리.
Output: 적용된 lifecycle/SSM(승인분) + 배포된 스택(canary) + E2E 증적 + OTA + 31-HUMAN-UAT.md + plan.md 갱신.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-api-visual-correction/31-CONTEXT.md
@.planning/phases/31-api-visual-correction/smoke/RESULTS.json
@backend/samconfig.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: 전체 게이트 — 테스트·빌드·blocked 검사</name>
  <files>(빌드 산출물만 — 소스 변경 없음)</files>
  <read_first>backend/samconfig.toml (stack sunity-motion-pilot / ap-northeast-2 / Stage=pilot), .planning/phases/31-api-visual-correction/smoke/RESULTS.json (blocked 값)</read_first>
  <action>순서 실행 (repo 루트 기준 — `cd backend` 후 `backend/tests` 참조 금지): (1) `python -m pytest backend/tests -q`, (2) `cd app && npm run typecheck`, (3) `cd app && npx jest`, (4) `(cd backend && sam validate --lint)`, (5) `(cd backend && sam build --use-container)` ([[sam-build-native-deps]]), (6) **blocked 게이트**: RESULTS.json blocked=true 면 STOP — Task 2/3 진행하되 인프라만(flag OFF 고정), E2E-A 수행 불가 → phase 미완료 SUMMARY 명시 + belle 보고. 실패 수정 필요 시 최소 diff + SUMMARY 기록.</action>
  <verify>
    <automated>python -m pytest backend/tests -q && (cd backend && sam validate --lint)</automated>
  </verify>
  <acceptance_criteria>
    - backend suite + app typecheck + jest 전부 exits 0
    - `.aws-sam/build` 에 VisualRequestFunction/VisualWorkerFunction 존재
    - RESULTS.json blocked 값이 SUMMARY 판정에 인용됨
  </acceptance_criteria>
  <done>배포 가능 상태 + blocked 게이트 판정 완료.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: 라이브 인프라 mutation — lifecycle merge + SSM versioned HMAC 키 (H2-09)</name>
  <what-built>31-10 의 visual_infra_dryrun.py 가 만든 dry-run artifact: (1) lifecycle_before.json (현행 bucket 규칙 원본 — rollback artifact) + lifecycle_merged.json (기존 규칙 보존 + visual-input/ 1일 + training/phase31/pairs/ retentionDays — privacy_decision.json 값), (2) SSM `/sunity/motion/pair-id-hmac-keys` versioned JSON 형식 지시문. put-bucket-lifecycle-configuration 은 **기존 lifecycle 전체를 교체**하므로 merge 실수 시 uploads retention 규칙이 소실된다 — 그래서 적용은 이 checkpoint 로 격리 (2차 리뷰 H2-09).</what-built>
  <how-to-verify>
    1. Claude 가 제시하는 before/merged JSON diff 검토 — 기존 규칙(uploads 만료 등)이 merged 에 전부 보존되고 신규 2 규칙(visual-input 1일, pairs retentionDays)만 추가됐는지 확인
    2. SSM 신규 키 생성 계획(versioned JSON, SecureString) 확인
    3. 승인 시 "approved" 입력 → Claude 가 적용 + 검증 실행
  </how-to-verify>
  <resume-signal>"approved" 입력 (수정 요청 시 merged JSON 재생성 후 재제시)</resume-signal>
  <read_first>.planning/phases/31-api-visual-correction/infra/lifecycle_before.json + lifecycle_merged.json (31-10 산출), .planning/phases/31-api-visual-correction/smoke/privacy_decision.json (retentionDays — 31-01 결정)</read_first>
  <action>belle 승인 후 Claude 가 실행 (AWS_PROFILE=sunity-motion): (1) lifecycle_before.json 을 커밋 대상 rollback artifact 로 보존 확인, (2) `aws s3api put-bucket-lifecycle-configuration --bucket sunity-motion-pilot-videos --lifecycle-configuration file://lifecycle_merged.json`, (3) **put 후 get 검증 (H2-09): get-bucket-lifecycle-configuration 재조회 → 규칙 ID 목록·개수·prefix 가 merged 와 일치 assert — 기존 규칙 소실 시 즉시 before JSON 으로 rollback put**, (4) SSM `/sunity/motion/pair-id-hmac-keys` 부재 시 `aws ssm put-parameter --type SecureString` 으로 versioned JSON({"active":"v1","keys":{"v1":"<랜덤 32바이트 hex>"}}) 생성 (기존 존재 시 무변경) + get 으로 형식 검증 (값 비로깅). 결과(규칙 개수 before/after, SSM 존재 확인)를 SUMMARY 에 기록.</action>
  <verify>
    <automated>N/A - checkpoint:human-action (belle 승인이 게이트; put 후 get 검증은 resume 후 Claude 실행·기록)</automated>
  </verify>
  <acceptance_criteria>
    - belle "approved" 후에만 put 실행
    - put 후 get 재조회에서 기존 규칙 전부 + 신규 2 규칙 확인 (개수/ID/prefix 기록)
    - lifecycle_before.json rollback artifact 가 phase 디렉터리에 보존
    - SSM versioned JSON 존재 (값 미로깅)
  </acceptance_criteria>
  <done>라이브 인프라 변경이 승인·rollback 가능 경로로 적용됨 (§8 gate 15 후반부).</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: belle 배포 (flag OFF) + 배포 후 보안·IAM 실검증</name>
  <what-built>VisualRequestFunction(POST /visual/rotation) + VisualWorkerFunction(+SweepSchedule) + VisualQueue(visibility 1800/maxReceive 5)/DLQ+Alarm + playback-url asset 확장 + PipelineFunction SendMessage 정책이 포함된 sam build. VisualJobsEnabled 기본 false — 기능 비활성 인프라 선배포 (canary 1단계). sam deploy 는 auto-mode classifier 차단으로 belle 수동 실행 (`!` 프리픽스 선례).</what-built>
  <how-to-verify>
    1. 터미널에서 `!cd backend && sam deploy` (samconfig 기본값 — VisualJobsEnabled Default "false")
    2. changeset 에 VisualRequestFunction/VisualWorkerFunction/VisualQueue Add + PipelineFunction 정책 Modify 확인 후 승인
    3. "Successfully created/updated stack - sunity-motion-pilot" 확인
  </how-to-verify>
  <resume-signal>"deployed" 입력 (실패 시 에러 로그 붙여넣기)</resume-signal>
  <read_first>backend/samconfig.toml</read_first>
  <action>belle "deployed" 후 Claude 가 즉시 실검증 (AWS_PROFILE=sunity-motion): (1) 무토큰 POST /visual/rotation → 401, (2) fixture 계정 유효 토큰 → 503 'feature_disabled' (flag OFF 실증), (3) 유효 토큰 + asset='rotation' 필드 없는 문서 → 404 단일 응답, **(4) IAM 실권한 검증 (H2-02, §8 gate 8): `aws iam simulate-principal-policy` 로 PipelineFunction role 과 RunPod 용 IAM user(sunity-motion 키 주체) 각각의 VisualQueue ARN sqs:SendMessage allowed 확인 — simulate 불가(권한 부족) 시 RunPod credential 로 canary send + 즉시 receive/delete 로 대체 실측. RunPod user 에 권한 부재 판명 시 필요한 최소 정책 JSON 을 제시하고 belle 승인 하에 attach 후 재검증**. 결과 4종을 SUMMARY 에 기록.</action>
  <verify>
    <automated>N/A - checkpoint:human-action (belle 배포가 게이트; 검증 4종은 resume 후 Claude 실행·기록)</automated>
  </verify>
  <acceptance_criteria>
    - 스택 업데이트 성공 로그 존재
    - 무토큰 401 / flag OFF 503 / asset 부재 404 실측 기록
    - PipelineFunction role + RunPod credential 양쪽 SendMessage allowed 실증 (simulate 또는 canary — §8 gate 8)
  </acceptance_criteria>
  <done>인프라가 기능 OFF 상태로 라이브 — 보안 경계·producer 권한 실검증 완료.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Pod 재생성 + flag ON canary → 실 E2E 2종 → OTA + UAT 적립</name>
  <what-built>E2E 전제 2가지가 인간 액션: (1) RunPod Pod 생성 (belle 콘솔), (2) flag ON 배포 (`!cd backend && sam deploy --parameter-overrides VisualJobsEnabled=true`). Pod 셋업은 Claude 가 SSH 수행 ([[pod-ops-claude-runs]]) — env 체크리스트 신규: VISUAL_JOBS_ENABLED=true, VISUAL_QUEUE_URL (+기존 AWS 키/FIREBASE/GEMINI) + [[current-pod-hbpvhedq2bu01i]] 절차 + SSM runpod-analyze-url/Lambda env 재동기화.</what-built>
  <how-to-verify>
    1. RunPod 콘솔에서 Pod 생성 (Network Storage attach — [[next-pod-use-network-storage]]) 후 SSH 정보 전달
    2. Claude 의 Pod 셋업 + 헬스체크(+/pose-image 스모크) 완료 보고 확인
    3. `!cd backend && sam deploy --parameter-overrides VisualJobsEnabled=true` 실행
  </how-to-verify>
  <resume-signal>"pod ready + deployed" 입력</resume-signal>
  <read_first>.planning/phases/30-growth-tracking/ 최신 SUMMARY (OTA eas update 커맨드 선례), 최근 phase HUMAN-UAT.md 1개 (적립 형식), backend/scripts/list_stuck_visual_jobs.py (E2E 중 잡 상태 관찰)</read_first>
  <action>resume 후 Claude 가 실 E2E 2종 실행 (fixture 계정, AWS_PROFILE=sunity-motion — §8 gate 16):

**E2E-A 자동 correctedPose (D-05):** fixture 계정 신규 분석 1건 업로드 → 분석 done 후 correctedPoseStatus 관찰 → 판정: (a) 'done' = playback-url asset='correctedPose' fresh URL + HTTP 200 이미지 로드 + job 문서 판정 메타(judgeDisplayPass/judgeTrainingPass/judgeConfidence + poseGatePassed/poseGateErrorDeg/poseGateReason) 존재 + `aws s3 ls .../visual-input/{uid}/{analysisId}/` 0건 (입력·staging 정리) + learningOptIn 상태별 training/phase31/pairs/ 정합 (비동의 = 신규 객체 0) — PASS; (b) 'failed' + failureReason=='moderation' = 배선 실증 별도 기록 (성공 아님) — 다른 fixture 로 1회 재시도; (c) 그 외 failed/pending 고아/'create_unconfirmed' = FAIL — 원인 수정 전 phase 미완료.

**E2E-B 회전 온디맨드 (D-06):** fixture 토큰 POST /visual/rotation → 202 + visualQuota +1 → 실 SQS 경유 워커 관찰 (list_stuck_visual_jobs.py + CloudWatch — action 체인 create→poll→fetch 로그 확인) → rotationStatus 'done' → asset='rotation' fresh URL 로드 → 2차 요청 → 200 done 멱등 + 벤더 task 신규 0 (taskId 불변) + quota 불변.

완료 처리 (두 E2E PASS 시에만): (1) OTA 발행 (phase 30 선례; 참고코너는 필드 부재 시 자동 숨김 — 선발행 안전), (2) 31-HUMAN-UAT.md 적립: D-09 참고코너 배치/"참고하세요" 분리, D-10 2D 비교 뷰어 ("3D" 아님 확인), 교정 이미지 카드, 회전 영상 재생, 결함 줌 화살표 시각 확인 (E2E-A zoom PNG 를 Claude 가 다운로드해 Read 로 화살표 방향 선검토 — 리뷰 B-02 rollout 전 시각 게이트), (3) plan.md 갱신, (4) SUMMARY 에 Pod env 체크리스트 갱신분 박제. **어느 한 E2E 라도 미통과면 phase 31 미완료 명시 — OTA/완료 마킹 금지.**</action>
  <verify>
    <automated>test -f .planning/phases/31-api-visual-correction/31-HUMAN-UAT.md && grep -q "D-10" .planning/phases/31-api-visual-correction/31-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - E2E-A: 'done' + fresh URL 200 + visual-input 잔존 0 + 비동의 training 기록 0 (실측 기록)
    - E2E-B: 'done' + fresh URL + 2차 멱등 (taskId·quota 불변) 실측 기록
    - failed 종결은 'moderation' 만 배선 실증 (성공 아님)
    - 두 E2E PASS 후에만 OTA update id 존재
    - 31-HUMAN-UAT.md 에 D-09/D-10/교정 이미지/회전/화살표 항목 존재
  </acceptance_criteria>
  <done>실 사용자 여정 2종 증명 + canary ON — phase 31 완료 조건 충족 (미통과 시 미완료 명시).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 배포 파이프라인 | belle 수동 게이트 + flag 기본 OFF |
| 라이브 bucket lifecycle | put = 전체 교체 — 승인 + before rollback + get 검증으로만 (H2-09) |
| 실검증 호출 | 실 과금(~$0.8/건)·실 데이터 접촉 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-48 | Elevation of Privilege | 무단 배포/flag ON/인프라 mutation | mitigate | checkpoint:human-action 3회 (mutation·OFF 배포·ON 배포 각각 belle) |
| T-31-49 | Spoofing | 라이브 엔드포인트 | mitigate | 무토큰 401 + flag OFF 503 실검증 acceptance 고정 |
| T-31-50 | Denial of Service (과금) | E2E 호출 | mitigate | fixture 계정·회전 1건+멱등 2차 상한 |
| T-31-51 | Repudiation | 검증 없는 완료 선언 | mitigate | 실 E2E 2종 PASS = phase 완료 필요조건 — Pod 미가용 = 미완료 (H-07) |
| T-31-55 | Tampering | lifecycle 교체로 기존 규칙 소실 | mitigate | before JSON rollback + put 후 get 재조회 규칙 ID/개수/prefix assert (H2-09) |
</threat_model>

<verification>
- 인프라 mutation put 후 get 검증 + 스택 2단 배포(OFF→ON) + 401/503/404/IAM 실검증 + E2E 2종 실측 기록
- OTA + HUMAN-UAT 적립은 E2E PASS 조건부
</verification>

<success_criteria>
- 2차 리뷰 §8 gate 8(IAM 실검증)·15(live mutation checkpoint)·16(실 E2E 2종) 전부 실측
- D-01 풀통합 라이브 (조건 미충족 시 미완료 명시 — 침묵 이월 0)
</success_criteria>

<output>
Create `.planning/phases/31-api-visual-correction/31-12-SUMMARY.md` when done
</output>
