---
phase: 31-api-visual-correction
plan: 12
type: execute
wave: 4
depends_on: [31-03, 31-09, 31-10, 31-11]
files_modified:
  - .planning/phases/31-api-visual-correction/31-HUMAN-UAT.md
  - plan.md
autonomous: false
requirements: [D-01, D-05, D-06]
must_haves:
  truths:
    - "배포는 fail-closed — RESULTS.json.blocked=true 면 라이브 롤아웃 차단, flag 기본 OFF 로만 배포 (리뷰 H-03/H-07)"
    - "phase 완료의 필요조건 = 실 인증 E2E 2종 통과: (1) 신규 분석 → correctedPose done + fresh URL + 입력 정리 + 비동의 학습 기록 0, (2) 회전 요청 → quota → 실 SQS → 워커 → done + fresh URL + 2차 요청 멱등 (리뷰 H-07)"
    - "generic 실패는 성공이 아니다 — failed 로 끝나는 경우 typed failureReason 'moderation' 만 배선 실증으로 인정 (리뷰 H-07)"
    - "Pod/워커 runtime 이 미가용이면 phase 는 미완료로 남는다 — 'Pod 재생성 후' 를 완료로 기록하지 않음 (리뷰 H-07)"
    - "실기기 확인 항목은 HUMAN-UAT.md 적립 — 즉시 belle 호출 금지 ([[batch-uat-after-phase-31]])"
  artifacts:
    - path: ".planning/phases/31-api-visual-correction/31-HUMAN-UAT.md"
      provides: "배치 UAT 적립 (D-09/D-10 2D 뷰어/교정 이미지/회전 카드/화살표 시각 게이트)"
      contains: "D-10"
  key_links:
    - from: "backend/template.yaml"
      to: "sunity-motion-pilot 스택"
      via: "sam build --use-container + belle sam deploy (flag OFF → E2E 후 ON)"
      pattern: "sunity-motion-pilot"
---

<objective>
리뷰 Wave 5 의 fail-closed 배포 게이트를 실행한다. 순서: 전체 게이트(테스트+빌드+blocked 검사) → belle 배포 (VisualJobsEnabled=false 기본) + 배포 후 보안 실검증 → Pod 재생성 + flag ON canary → **실 인증 E2E 2종** (자동 correctedPose 성공 + 회전 온디맨드 성공 — 리뷰 H-07 의 phase 완료 필요조건) → OTA + HUMAN-UAT 적립.

구 계획의 기각된 검증(워커 직접 invoke 로 배선 성공 처리, 자동 실루엣 E2E 이월 후 완료 마킹)을 대체한다: 이 플랜이 끝나지 않으면 phase 31 은 완료가 아니다.

Purpose: 실제 사용자 여정이 증명되기 전에 "완료" 가 선언되는 것을 구조적으로 차단.
Output: 배포된 스택(canary) + E2E 증적 + OTA + 31-HUMAN-UAT.md + plan.md 갱신.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-api-visual-correction/31-CONTEXT.md
@.planning/phases/31-api-visual-correction/smoke/RESULTS.json
@backend/samconfig.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: 전체 게이트 — 테스트·빌드·blocked 검사</name>
  <files>(빌드 산출물만 — 소스 변경 없음)</files>
  <read_first>backend/samconfig.toml (stack sunity-motion-pilot / ap-northeast-2 / Stage=pilot), .planning/phases/31-api-visual-correction/smoke/RESULTS.json (blocked 값)</read_first>
  <action>순서 실행 (리뷰 M-07 경로 수정 반영 — `cd backend` 후 `backend/tests` 참조 금지): (1) `python -m pytest backend/tests -q` (repo 루트), (2) `cd app && npm run typecheck`, (3) `cd app && npx jest`, (4) `(cd backend && sam validate --lint)`, (5) `(cd backend && sam build --use-container)` ([[sam-build-native-deps]]), (6) **blocked 게이트**: RESULTS.json 의 blocked 가 true 면 여기서 STOP — Task 2 로 진행하되 배포는 인프라만(flag OFF 고정), Task 3 의 correctedPose E2E 는 수행 불가 → phase 미완료 상태로 SUMMARY 에 명시하고 belle 에게 보고 (리뷰 H-03: blocked=true 는 라이브 기능 활성화를 막는다). 실패 수정이 필요하면 최소 diff + SUMMARY 기록.</action>
  <verify>
    <automated>python -m pytest backend/tests -q && (cd backend && sam validate --lint)</automated>
  </verify>
  <acceptance_criteria>
    - backend suite + app typecheck + jest 전부 exits 0
    - `.aws-sam/build` 에 VisualRequestFunction/VisualWorkerFunction 존재
    - RESULTS.json blocked 값이 SUMMARY 판정에 인용됨
  </acceptance_criteria>
  <done>배포 가능 상태 + blocked 게이트 판정 완료.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: belle 배포 (flag OFF) + 배포 후 보안 실검증</name>
  <what-built>VisualRequestFunction(POST /visual/rotation) + VisualWorkerFunction + VisualQueue/DLQ(ReportBatchItemFailures) + playback-url asset 확장 + LogGroup 이 포함된 sam build. VisualJobsEnabled 파라미터 기본 false — 기능 비활성 상태의 인프라 선배포 (리뷰 H-07 canary 1단계). sam deploy 는 auto-mode classifier 차단으로 belle 수동 실행 (2026-07-19 선례: `!` 프리픽스).</what-built>
  <how-to-verify>
    1. 터미널에서 `!cd backend && sam deploy` (samconfig 기본값 — VisualJobsEnabled 는 template Default "false")
    2. changeset 에 VisualRequestFunction/VisualWorkerFunction/VisualQueue Add 확인 후 승인
    3. "Successfully created/updated stack - sunity-motion-pilot" 확인
  </how-to-verify>
  <resume-signal>"deployed" 입력 (실패 시 에러 로그 붙여넣기)</resume-signal>
  <read_first>backend/samconfig.toml</read_first>
  <action>belle 에게 배포를 안내하고 "deployed" 후 Claude 가 즉시 실검증 (AWS_PROFILE=sunity-motion): (1) 무토큰 `curl -s -o /dev/null -w "%{http_code}" -X POST {ApiBaseUrl}/visual/rotation` → 401 확인, (2) fixture 계정 유효 토큰으로 동일 호출 → 503 body code 'feature_disabled' 확인 (flag OFF 실증 — 리뷰 H-07 canary), (3) 유효 토큰 + asset='rotation' 인데 필드 없는 문서로 playback-url → 404 단일 응답 확인. 결과 3종을 SUMMARY 에 기록.</action>
  <verify>
    <automated>N/A - checkpoint:human-action (belle 배포가 게이트; 검증 커맨드 3종은 resume 후 Claude 실행·기록)</automated>
  </verify>
  <acceptance_criteria>
    - 스택 업데이트 성공 로그 존재
    - 무토큰 401 / flag OFF 503 feature_disabled / asset 부재 404 — 3종 실측 기록
  </acceptance_criteria>
  <done>인프라가 기능 OFF 상태로 라이브 — 보안 경계 실검증 완료.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Pod 재생성 + flag ON canary → 실 E2E 2종 → OTA + UAT 적립</name>
  <what-built>E2E 실행의 전제 2가지가 인간 액션이다: (1) RunPod Pod 생성 (belle 콘솔 — RunPod 생성은 belle 계정 권한), (2) flag ON 배포 (`!cd backend && sam deploy --parameter-overrides VisualJobsEnabled=true`). Pod 셋업(bootstrap→start_server→env 주입)은 Claude 가 SSH 로 수행 ([[pod-ops-claude-runs]]) — env 체크리스트에 신규 3종 추가: VISUAL_JOBS_ENABLED=true, VISUAL_QUEUE_URL, (기존) AWS 키/FIREBASE/GEMINI + [[current-pod-hbpvhedq2bu01i]] 절차 + SSM runpod-analyze-url/Lambda env 재동기화.</what-built>
  <how-to-verify>
    1. RunPod 콘솔에서 Pod 생성 (Network Storage 볼륨 attach — [[next-pod-use-network-storage]]) 후 SSH 정보 전달
    2. Claude 의 Pod 셋업 + 헬스체크 완료 보고 확인
    3. `!cd backend && sam deploy --parameter-overrides VisualJobsEnabled=true` 실행
  </how-to-verify>
  <resume-signal>"pod ready + deployed" 입력</resume-signal>
  <read_first>.planning/phases/30-growth-tracking/ 최신 SUMMARY (OTA eas update 커맨드 선례), 최근 phase HUMAN-UAT.md 1개 (적립 형식), backend/scripts/list_stuck_visual_jobs.py (E2E 중 잡 상태 관찰용)</read_first>
  <action>resume 후 Claude 가 실 E2E 2종 실행 (fixture 계정, AWS_PROFILE=sunity-motion — 리뷰 H-07 필요조건):

**E2E-A 자동 correctedPose (D-05):** fixture 계정으로 신규 분석 1건 업로드 (기존 E2E 업로드 경로) → 분석 done 후 correctedPoseStatus 관찰 → 판정: (a) 'done' = playback-url asset='correctedPose' 로 fresh URL 발급 + HTTP 200 이미지 로드 + job 문서에 judge/poseGate 메타 존재 + `aws s3 ls .../visual-input/{uid}/{analysisId}/` 결과 0 (입력 정리) + 해당 계정 learningOptIn 상태에 따른 training/phase31/pairs/ 기록 정합 (비동의면 신규 객체 0) — PASS; (b) 'failed' 인데 failureReason=='moderation' = 조용한 폴백 배선 실증으로 별도 기록하되 **성공 E2E 로 인정하지 않음** — 다른 fixture 영상으로 1회 재시도; (c) 그 외 failed/pending 고아 = FAIL — 원인 수정 전 phase 미완료.

**E2E-B 회전 온디맨드 (D-06):** fixture 계정 인증 토큰으로 POST /visual/rotation → 202 + visualQuota counter +1 확인 → 실 SQS 경유 워커 처리 관찰 (list_stuck_visual_jobs.py + CloudWatch) → rotationStatus 'done' → asset='rotation' fresh URL 로드 확인 → **2차 요청 → 200 done 멱등 + 신규 벤더 task 0 (job 문서 taskId 불변) + quota 불변** 확인. 모더레이션 차단 시 E2E-A (b) 와 동일 규율.

완료 처리: 두 E2E PASS 시에만 — (1) OTA 발행 (phase 30 SUMMARY 의 eas update 선례; 참고코너는 필드 부재 시 자동 숨김이라 선발행 안전), (2) 31-HUMAN-UAT.md 적립 (즉시 belle 호출 금지): D-09 참고코너 배치/"참고하세요" 분리, D-10 2D 비교 뷰어 렌더 ("3D" 아님 확인 포함), 교정 이미지 카드, 회전 영상 재생, 결함 줌 화살표 시각 확인 (E2E-A 분석의 zoom PNG 를 Claude 가 다운로드해 Read 로 화살표 방향 선검토 — 리뷰 B-02 rollout 전 시각 게이트 — 후 실기기 항목으로 적립), (3) plan.md 갱신, (4) SUMMARY 에 Pod env 체크리스트 갱신분(VISUAL_JOBS_ENABLED/VISUAL_QUEUE_URL) 박제. **어느 한 E2E 라도 미통과면 phase 31 미완료로 SUMMARY 에 명시 — OTA/완료 마킹 금지 (리뷰 H-07).**</action>
  <verify>
    <automated>test -f .planning/phases/31-api-visual-correction/31-HUMAN-UAT.md && grep -q "D-10" .planning/phases/31-api-visual-correction/31-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - E2E-A: correctedPoseStatus 'done' + fresh URL 200 + visual-input 잔존 0 + 비동의 training 기록 0 (전부 실측 기록)
    - E2E-B: 'done' + fresh URL + 2차 요청 멱등 (taskId 불변·quota 불변) 실측 기록
    - failed 종결은 failureReason=='moderation' 일 때만 배선 실증으로 별도 기록 (성공 아님)
    - 두 E2E PASS 후에만 OTA update id 존재
    - 31-HUMAN-UAT.md 에 D-09/D-10/교정 이미지/회전/화살표 항목 존재
  </acceptance_criteria>
  <done>실 사용자 여정 2종이 증명되고 canary ON — phase 31 완료 조건 충족 (미통과 시 미완료 명시).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 배포 파이프라인 | belle 수동 게이트 + flag 기본 OFF |
| 실검증 호출 | 실 과금(~$0.8/건)·실 데이터 접촉 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-48 | Elevation of Privilege | 무단 배포/flag ON | mitigate | sam deploy = checkpoint:human-action 2회 (OFF 배포·ON 배포 각각 belle) |
| T-31-49 | Spoofing | 라이브 엔드포인트 | mitigate | 무토큰 401 + flag OFF 503 실검증을 acceptance 로 고정 |
| T-31-50 | Denial of Service (과금) | E2E 호출 | mitigate | fixture 계정·회전 E2E 1건+멱등 2차 (신규 과금 0 증명이 목적) 상한 |
| T-31-51 | Repudiation | 검증 없는 완료 선언 | mitigate | 두 실 E2E PASS 를 phase 완료 필요조건으로 고정 — Pod 미가용 = 미완료 (리뷰 H-07) |
</threat_model>

<verification>
- 스택 2단 배포(OFF→ON) + 401/503/404 실검증 + E2E 2종 실측 기록
- OTA + HUMAN-UAT 적립은 E2E PASS 조건부
</verification>

<success_criteria>
- 리뷰 "Minimum acceptance gates" 의 배포 관련 항목 전부 실측: 자동 correctedPose 성공 + 인증 회전 성공 + 임시 객체 정리 + fresh URL 재열람 + 멱등 + blocked 차단
- D-01 풀통합 라이브 (조건 미충족 시 미완료 명시 — 침묵 이월 0)
</success_criteria>

<output>
Create `.planning/phases/31-api-visual-correction/31-12-SUMMARY.md` when done
</output>
