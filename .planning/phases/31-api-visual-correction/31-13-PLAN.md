---
phase: 31-api-visual-correction
plan: 13
type: execute
wave: 3
depends_on: [31-01, 31-05, 31-06]
files_modified:
  - backend/scripts/calibrate_visual_gates.py
  - .planning/phases/31-api-visual-correction/smoke/CALIBRATION.json
  - .planning/phases/31-api-visual-correction/31-ACCEPTANCE.md
autonomous: true
requirements: [D-03, D-05]
must_haves:
  truths:
    - "calibration 은 31-05 judge 와 31-06 pose 측정의 실제 구현 코드를 실행하는 재현 가능 harness 로 수행된다 — 구현 전 선언값·수기 측정 금지 (3차 리뷰 H3-02)"
    - "manifest 항목은 before/after pair 계약이다 — harness 가 before/after 각 sha256 을 검증하고, pose error 는 after keypoint 로만 계산하며, pair provenance(생성 call/model/prompt version)를 기록한다 (4차 리뷰 B4-04)"
    - "chosen 은 4값 전부 산출한다 — display_pose_tol_deg/display_confidence/training_pose_tol_deg/training_confidence. training 이 display 보다 엄격하고, 더 엄격한 grid 조합이 없으면 arbitrary fallback 이 아니라 blocked (4차 리뷰 B4-05/M4-03)"
    - "최소 표본 = PASS 4 / FAIL 8. FAIL 에 pole·identity/clothing·background·extra limbs/person·correction invisible·pose tolerance failure 를 각각 포함하고 category confusion table + 표본 수를 기록한다 — 미달이면 chosen 미생성 blocked (4차 리뷰 H4-10)"
    - "display/training 임계값은 라벨 fixture 셋의 grid false accept/false reject 결과표에서 선택된다 — calibration 미달도 blocked=true (2차 H2-07)"
    - "CALIBRATION.json 에 fixture hash·judge model/prompt version·per-fixture pose model version + 전체 version set·실행 시각이 기록된다 (H3-02 + 4차 M4-05)"
    - "harness 는 threshold 를 인자로 받아 raw verdict/error 를 수집한다 — 임계값이 측정 코드에 하드코딩되지 않는다 (H3-02)"
  artifacts:
    - path: "backend/scripts/calibrate_visual_gates.py"
      provides: "실행 가능 calibration harness — pair 별(before/after) raw judge/pose 측정 + grid FA/FR + confusion table"
      contains: "def main"
    - path: ".planning/phases/31-api-visual-correction/smoke/CALIBRATION.json"
      provides: "pair 별 raw 결과 + grid + confusion table + chosen 4종 + per-fixture pose version set + 재현성 메타"
      contains: "chosen"
    - path: ".planning/phases/31-api-visual-correction/31-ACCEPTANCE.md"
      provides: "Display/Training 게이트 chosen 값 + calibration 결과표 + confusion table"
      contains: "Training"
  key_links:
    - from: "backend/scripts/calibrate_visual_gates.py"
      to: "backend/shared/python/sunity_shared/analysis/visual_gen.py"
      via: "judge_corrected_pose + judge_display_pass/judge_training_pass 실제 구현 호출 (threshold 인자식)"
      pattern: "judge_corrected_pose"
    - from: ".planning/phases/31-api-visual-correction/smoke/CALIBRATION.json"
      to: "backend/template.yaml (31-10)"
      via: "chosen 4값 → env DISPLAY_JUDGE_CONFIDENCE/TRAINING_JUDGE_CONFIDENCE/DISPLAY_POSE_TOL_DEG/TRAINING_POSE_TOL_DEG"
      pattern: "chosen"
---

<objective>
3차 리뷰 H3-02 + 4차 리뷰 B4-04/B4-05/H4-10/M4-03/M4-05 를 해소한다: 31-05/31-06 구현이 끝난 wave 3 에서 **실제 측정 코드를 호출하는 `calibrate_visual_gates.py`** 로 calibration 을 수행한다.

- 입력: 31-01 이 만든 **pair 계약 manifest** (`smoke/fixtures_manifest.json` — 각 항목 = {id, beforePath, beforeSha256, afterPath, afterSha256, label PASS|FAIL, jointKey, targetDeg, afterKeypointSource{path, modelVersion}, failureAxes[], category[]}).
- 측정: pair 별 (a) `visual_gen.judge_corrected_pose(before, after, context)` 실호출 (threshold 무관 raw 7축+confidence), (b) pose error — **after keypoint 로만** (`afterKeypointSource` 의 kpts 또는 수기 라벨) `fault_zoom.joint_inner_angle_deg` 실측 → error = |measured − targetDeg|.
- grid: pose tol {8,10,15,20}° × confidence {0.6,0.7,0.8,0.85} 격자별 FA/FR (판정 = 31-05 judge_display_pass 재사용 — 재구현 금지).
- chosen: display = FA 최소화 우선(동률 FR 최소); training = display 보다 pose tol 작고 confidence 높은 조합 중 FA 0 우선. **더 엄격한 training grid 조합이 없으면(display 가 이미 최엄격 8°/0.85) chosen 대신 blocked=true (M4-03 — arbitrary fallback 금지).**
- 소비: 31-09 워커 env(4 게이트) + 31-10 template + 31-12 blocked 게이트.

Purpose: 임계값이 pair-정합 측정에서 나오고, 그 측정이 언제든 재실행·재현 가능한 상태.
Output: harness + CALIBRATION.json + 31-ACCEPTANCE.md calibration 절.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-api-visual-correction/31-ACCEPTANCE.md
@.planning/phases/31-api-visual-correction/smoke/RESULTS.json
@.planning/phases/31-api-visual-correction/smoke/fixtures_manifest.json
@.planning/phases/31-api-visual-correction/31-PLAN-REVIEW-ITERATION4.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: calibrate_visual_gates.py — pair 계약 재현 harness</name>
  <files>backend/scripts/calibrate_visual_gates.py</files>
  <read_first>backend/shared/python/sunity_shared/analysis/visual_gen.py (judge_corrected_pose + judge_display_pass/judge_training_pass — threshold 인자식, PROMPT_VERSION), backend/shared/python/sunity_shared/analysis/fault_zoom.py (joint_inner_angle_deg — 단일 각도 출처), .planning/phases/31-api-visual-correction/smoke/fixtures_manifest.json (31-01 pair 스키마), .planning/spikes/004-gemini-omni-view-editing/kpts*/ (재사용 keypoint), .planning/phases/31-api-visual-correction/31-PLAN-REVIEW-ITERATION4.md 의 B4-04/H4-10/M4-05 절</read_first>
  <action>argparse: --manifest (기본 fixtures_manifest.json), --out (기본 smoke/CALIBRATION.json), --max-judge-calls (기본 24 — 초과 시 중단·부분 결과), --dry-run (측정 없이 manifest 검증만). 동작: (1) manifest 로드 + **각 pair 의 beforePath/afterPath 파일 sha256 를 beforeSha256/afterSha256 과 각각 재검증 — 불일치 항목 'hash_mismatch' 제외 기록 (B4-04)**; (2) **pair provenance 검증: afterKeypointSource.path 가 after 이미지 pair 에 속하는지(대상 일치) + generation call/model/prompt version 기록 (B4-04)**; (3) pair 별 raw 측정: judge = `judge_corrected_pose(before, after, context={jointKey, targetDeg})` 실호출 (env GEMINI_API_KEY — SSM 프로필 주석; 키 리터럴 금지) → 7축+confidence raw; pose = **afterKeypointSource 에서 3점 로드 → `joint_inner_angle_deg` → measured_deg → error_deg = |measured − targetDeg| (after keypoint 로만 — B4-04)**; (4) grid — pose tol {8,10,15,20} × confidence {0.6,0.7,0.8,0.85} 별 label 대비 FA/FR (판정 = judge_display_pass 재사용); (5) chosen — display = FA 최소(동률 FR 최소), training = display 보다 pose tol 작고 confidence 높은 조합 중 FA 0 우선; **더 엄격 조합 없음 → blocked=true, reason 'no_stricter_training_grid' (M4-03)**; (6) **confusion table (H4-10): category × (predicted pass/fail) 집계 + failure axis 별 검출율 + PASS/FAIL 표본 수**; (7) 출력 JSON: {"chosen": {display_pose_tol_deg, display_confidence, training_pose_tol_deg, training_confidence}, "table": [...격자...], "confusion": {...category confusion + 표본 수...}, "pairs": [...항목별 raw + label + before/after hash + failureAxes + category...], "meta": {"judge_model": 'gemini-3.5-flash', "prompt_version": visual_gen PROMPT_VERSION, **"pose_model_versions": {fixtureId: afterKeypointSource.modelVersion} + "pose_model_version_set": [고유 version 전체] (M4-05 — 단일 문자열 금지)**, "ran_at": ISO8601, "manifest_sha256": 전체 manifest hash}} (H3-02 + B4-04 + M4-05). **표본 미달 (PASS < 4 또는 FAIL < 8 또는 측정 불가 >25%) → chosen 대신 {"blocked": true, "reason": ...} (H4-10)**. 이미지 바이트/서명 URL JSON 미포함. .planning 하위 인물 이미지 복사 금지(경로 참조만).</action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('backend/scripts/calibrate_visual_gates.py').read())" && python backend/scripts/calibrate_visual_gates.py --dry-run --manifest .planning/phases/31-api-visual-correction/smoke/fixtures_manifest.json</automated>
  </verify>
  <acceptance_criteria>
    - harness 가 visual_gen.judge_corrected_pose/judge_display_pass / fault_zoom.joint_inner_angle_deg 실제 구현 import (재구현 0 — H3-02)
    - pair 별 before/after 각 sha256 재검증 + pose error 는 after keypoint 로만 (B4-04)
    - PASS<4/FAIL<8 → blocked + confusion table 코드 존재 (H4-10)
    - training 더 엄격 조합 없음 → blocked 'no_stricter_training_grid' (M4-03)
    - meta 에 per-fixture pose_model_versions + version_set (M4-05) + judge model/prompt version/실행 시각
    - threshold grid 인자 — 하드코딩 0, --max-judge-calls 상한, 키 리터럴 0, --dry-run 네트워크 0
  </acceptance_criteria>
  <done>calibration 이 pair 정합·표본 하한·재현 가능한 실측 harness 로 존재.</done>
</task>

<task type="auto">
  <name>Task 2: calibration 실행 + CALIBRATION.json + 31-ACCEPTANCE.md 기입</name>
  <files>.planning/phases/31-api-visual-correction/smoke/CALIBRATION.json, .planning/phases/31-api-visual-correction/31-ACCEPTANCE.md</files>
  <read_first>.planning/phases/31-api-visual-correction/31-ACCEPTANCE.md (31-01 골격 — placeholder 절), .planning/phases/31-api-visual-correction/smoke/RESULTS.json (chosen_model + sync — async-only 전제)</read_first>
  <action>(1) harness 실행 (`AWS_PROFILE=sunity-motion` SSM Gemini 키) → CALIBRATION.json. (2) 검증: chosen 4값 존재 + display ≠ training (training 더 엄격) + table 16조합 FA/FR + confusion table + PASS>=4/FAIL>=8 (미달 시 blocked=true + **RESULTS.json.blocked 를 true 로 갱신** — 31-12 flag OFF). (3) 31-ACCEPTANCE.md placeholder 기입: "## Display 게이트" — judge 7축 true + chosen display(pose_tol/confidence), "## Training 적재 게이트" — display + chosen training(더 엄격) + **build gate 명시: TRAINING_JUDGE_CONFIDENCE > DISPLAY_JUDGE_CONFIDENCE AND TRAINING_POSE_TOL_DEG < DISPLAY_POSE_TOL_DEG (31-10 강제 — B4-05)**, "## Calibration 결과표" — FA/FR 표 + confusion table + 표본 수 + 선택 근거 + 재현 커맨드 + meta. (4) SUMMARY 에 chosen 4값 ↔ 31-10 env(4 게이트) exact mapping 명시.</action>
  <verify>
    <automated>python -c "import json; c=json.load(open('.planning/phases/31-api-visual-correction/smoke/CALIBRATION.json')); assert (('chosen' in c and all(k in c['chosen'] for k in ('display_pose_tol_deg','display_confidence','training_pose_tol_deg','training_confidence')) and 'table' in c and 'confusion' in c and 'meta' in c) or c.get('blocked') is True); m=c.get('meta',{}); assert c.get('blocked') is True or all(k in m for k in ('judge_model','prompt_version','ran_at','pose_model_version_set'))" && grep -q "Training" .planning/phases/31-api-visual-correction/31-ACCEPTANCE.md</automated>
  </verify>
  <acceptance_criteria>
    - CALIBRATION.json 에 chosen 4값 + 격자표 + confusion table + pairs raw + meta(pose_model_version_set) (또는 blocked=true + RESULTS.json 동기화)
    - display 와 training 이 서로 다른 chosen (training 더 엄격) — 없으면 blocked
    - 31-ACCEPTANCE.md 에 결과표 + confusion table + build gate ordering + 재현 커맨드
    - 서명 URL/이미지 데이터 JSON 미포함
  </acceptance_criteria>
  <done>31-09(env 소비)/31-10(template 주입 + build gate)/31-12(blocked 게이트)가 pair 정합·표본 하한 측정 기반 임계값으로 진행 가능 — H2-07 + H3-02 + B4-04/B4-05/H4-10 충족.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로컬 → Gemini | pair 이미지 판정 호출 (31-01 belle 승인 범위 내) |
| 산출 JSON → git | 서명 URL/이미지 데이터 미포함 경계 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-59 | Denial of Service (과금) | 판정 호출 반복 | mitigate | --max-judge-calls 상한 + 31-01 지출 승인 범위 |
| T-31-60 | Tampering | 재현 불가 임계값 drift / pair 오정렬 | mitigate | before/after 각 hash 재검증 + after keypoint provenance + meta version set + 재현 커맨드 (H3-02 + B4-04 + M4-05) |
| T-31-61 | Information Disclosure | 키/서명 URL/인물 이미지 | mitigate | SSM 키 주입만 + JSON URL/바이트 미포함 + .planning 이미지 복사 금지 |
| T-31-64 | Tampering | 표본 부족 threshold 과신 | mitigate | PASS 4/FAIL 8 하한 + confusion table + 미달 blocked (H4-10) |
</threat_model>

<verification>
- CALIBRATION.json 스키마 assert 통과 + ACCEPTANCE 절 기입
- harness 재실행 시 동일 manifest 에서 동일 raw (judge 는 API 변동 허용 — meta drift 추적)
</verification>

<success_criteria>
- H3-02 + B4-04(pair 계약) + B4-05(4값 소비) + H4-10(표본 하한/confusion) + M4-03(엄격 조합 없으면 blocked) + M4-05(per-fixture version) 해소 — 4차 리뷰 §8 승인 조건 6·7
- 31-09/31-10/31-12 임계값 소비 경로가 pair 정합 측정 근거를 가짐
</success_criteria>

<output>
Create `.planning/phases/31-api-visual-correction/31-13-SUMMARY.md` when done
</output>
</content>
</invoke>
