---
phase: 31-api-visual-correction
plan: 13
type: execute
wave: 3
depends_on: [31-01, 31-05, 31-06]
files_modified:
  - backend/scripts/calibrate_visual_gates.py
  - .planning/phases/31-api-visual-correction/smoke/CALIBRATION.json
  - .planning/phases/31-api-visual-correction/31-ACCEPTANCE.md
autonomous: true
requirements: [D-03, D-05]
must_haves:
  truths:
    - "calibration 은 31-05 judge 와 31-06 pose 측정의 실제 구현 코드를 실행하는 재현 가능한 harness 로 수행된다 — 구현 전 선언값·수기 측정 금지 (3차 리뷰 H3-02)"
    - "display/training 임계값은 라벨 fixture 셋의 grid false accept/false reject 결과표에서 선택된다 — calibration 미달도 blocked=true (2차 리뷰 H2-07)"
    - "CALIBRATION.json 에 fixture hash·judge model/prompt version·pose model version·실행 시각이 기록된다 — 재현성과 drift 추적 (H3-02)"
    - "harness 는 threshold 를 인자로 받아 raw verdict/error 를 수집한다 — 임계값이 측정 코드에 하드코딩되지 않는다 (H3-02)"
  artifacts:
    - path: "backend/scripts/calibrate_visual_gates.py"
      provides: "실행 가능한 calibration harness — fixture 별 raw judge/pose 측정 + grid FA/FR 표 산출"
      contains: "def main"
    - path: ".planning/phases/31-api-visual-correction/smoke/CALIBRATION.json"
      provides: "fixture 별 raw 결과 + grid 표 + chosen 임계값 4종 + 재현성 메타"
      contains: "chosen"
    - path: ".planning/phases/31-api-visual-correction/31-ACCEPTANCE.md"
      provides: "Display/Training 게이트 chosen 값 + calibration 결과표 절 (31-01 골격에 기입)"
      contains: "Training"
  key_links:
    - from: "backend/scripts/calibrate_visual_gates.py"
      to: "backend/shared/python/sunity_shared/analysis/visual_gen.py"
      via: "judge_corrected_pose 실제 구현 호출 (threshold 인자식)"
      pattern: "judge_corrected_pose"
    - from: ".planning/phases/31-api-visual-correction/smoke/CALIBRATION.json"
      to: "backend/template.yaml (31-10)"
      via: "chosen 값 → env DISPLAY_POSE_TOL_DEG/TRAINING_POSE_TOL_DEG/DISPLAY_JUDGE_CONFIDENCE"
      pattern: "chosen"
---

<objective>
3차 리뷰 H3-02 를 해소한다: 3차 개정까지 calibration(31-01 Task 3)은 judge(31-05)·pose 측정(31-06)이 존재하기 전에 실행되어 "31-05 와 동일한 프롬프트" 라는 보장이 구조적으로 불가능했고, 재실행 가능한 harness 파일도 없었다. 이 플랜은 31-05/31-06 구현이 끝난 wave 3 에서 **실제 측정 코드를 호출하는 실행 가능한 `calibrate_visual_gates.py`** 로 calibration 을 수행한다.

- 입력: 31-01 이 만든 라벨 fixture manifest (`smoke/fixtures_manifest.json` — 경로/hash/PASS·FAIL 라벨/기대 target 각도/카테고리).
- 측정: fixture 별 (a) `visual_gen.judge_corrected_pose` 실호출 (threshold 무관 raw 7축+confidence 수집), (b) pose error — `fault_zoom.joint_inner_angle_deg` 로 keypoint 실측 (spike kpts 재사용 가능 fixture 는 재사용, 신규 산출 fixture 는 로컬 RTMW 또는 명시 수기 keypoint 라벨 — manifest 에 출처 기록).
- 산출: 후보 임계값 격자(pose tol 8/10/15/20°, confidence 0.6/0.7/0.8/0.85)별 false accept/false reject 표 → display = FA 최소화 우선, training = display 보다 엄격.
- 소비: 31-09 워커 env(DISPLAY_POSE_TOL_DEG/TRAINING_POSE_TOL_DEG/DISPLAY_JUDGE_CONFIDENCE — 31-10 template 주입)와 31-12 blocked 게이트.

D-03(교정 이미지 품질 게이트)·D-05(자동 생성의 "전문가 수준" 인상)의 임계값 근거 지점.

Purpose: 임계값이 측정에서 나오고, 그 측정이 언제든 재실행·재현 가능한 상태.
Output: harness 스크립트 + CALIBRATION.json + 31-ACCEPTANCE.md calibration 절.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-api-visual-correction/31-ACCEPTANCE.md
@.planning/phases/31-api-visual-correction/smoke/RESULTS.json
@.planning/phases/31-api-visual-correction/smoke/fixtures_manifest.json
@.planning/phases/31-api-visual-correction/31-PLAN-REVIEW-ITERATION3.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: calibrate_visual_gates.py — 재현 가능 harness</name>
  <files>backend/scripts/calibrate_visual_gates.py</files>
  <read_first>backend/shared/python/sunity_shared/analysis/visual_gen.py (judge_corrected_pose 시그니처 — threshold 인자식, prepare_judge_payload), backend/shared/python/sunity_shared/analysis/fault_zoom.py (joint_inner_angle_deg — 단일 각도 출처), .planning/phases/31-api-visual-correction/smoke/fixtures_manifest.json (31-01 산출 — 항목 스키마), .planning/spikes/004-gemini-omni-view-editing/kpts*/ (재사용 가능 keypoint 실측)</read_first>
  <action>argparse 스크립트 신설: --manifest (기본 fixtures_manifest.json 경로), --out (기본 smoke/CALIBRATION.json), --max-judge-calls (기본 24 — 비용 상한, 초과 시 중단·부분 결과 기록), --dry-run (측정 없이 manifest 검증만). 동작: (1) manifest 로드 + 각 fixture 파일 sha256 재계산 — manifest hash 와 불일치 시 해당 항목 'hash_mismatch' 로 제외 기록 (H3-02 재현성); (2) fixture 별 raw 측정: judge = `judge_corrected_pose(before, after, context)` 실호출 (env GEMINI_API_KEY — SSM 프로필 지시 주석; 키 리터럴 금지) → 7축+confidence raw 기록, pose = manifest 의 keypoint 출처(spike kpts 파일 경로 또는 수기 라벨)에서 3점 로드 → `joint_inner_angle_deg` 로 measured_deg → error_deg = |measured − 기대 target|; (3) grid 계산 — pose tol {8,10,15,20} × confidence {0.6,0.7,0.8,0.85} 격자별로 라벨 대비 false accept / false reject 집계 (판정 로직 = 31-05 의 threshold 인자식 display 판정 함수 재사용 — 하드코딩 재구현 금지); (4) chosen 선택 — display = FA 최소화 우선(동률 시 FR 최소), training = display 보다 pose tol 작고 confidence 높은 조합 중 FA 0 우선; (5) 출력 JSON: {"chosen": {"display_pose_tol_deg","display_confidence","training_pose_tol_deg","training_confidence"}, "table": [...격자 전체...], "fixtures": [...항목별 raw + 라벨 + hash...], "meta": {"judge_model": 'gemini-3.5-flash', "prompt_version": visual_gen 의 프롬프트 버전 상수, "pose_model_version": manifest keypoint 출처 표기, "ran_at": ISO8601, "manifest_sha256": 전체 manifest hash}} (H3-02 메타 4종 필수). fixture 항목 < 12 또는 측정 불가 항목 과다(>25%) → chosen 대신 {"blocked": true, "reason": ...} 기록. 이미지 바이트/서명 URL 은 JSON 에 미포함. 신규 인물 이미지의 .planning 하위 복사 금지 (경로 참조만).</action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('backend/scripts/calibrate_visual_gates.py').read())" && python backend/scripts/calibrate_visual_gates.py --dry-run --manifest .planning/phases/31-api-visual-correction/smoke/fixtures_manifest.json</automated>
  </verify>
  <acceptance_criteria>
    - harness 가 visual_gen.judge_corrected_pose / fault_zoom.joint_inner_angle_deg 실제 구현을 import (자체 재구현 0 — H3-02)
    - threshold 는 grid 인자 — 측정 코드에 하드코딩 0
    - fixture hash 재검증 + meta 4종 (judge model/prompt version/pose 출처/실행 시각) 기록 코드 존재
    - --max-judge-calls 상한 + 키 리터럴 0
    - --dry-run 이 manifest 검증만 수행 (네트워크 0)
  </acceptance_criteria>
  <done>calibration 이 언제든 재실행 가능한 실측 harness 로 존재.</done>
</task>

<task type="auto">
  <name>Task 2: calibration 실행 + CALIBRATION.json + 31-ACCEPTANCE.md 기입</name>
  <files>.planning/phases/31-api-visual-correction/smoke/CALIBRATION.json, .planning/phases/31-api-visual-correction/31-ACCEPTANCE.md</files>
  <read_first>.planning/phases/31-api-visual-correction/31-ACCEPTANCE.md (31-01 골격 — "Display 게이트/Training 적재 게이트" placeholder 절), .planning/phases/31-api-visual-correction/smoke/RESULTS.json (chosen_model — judge 호출 전제)</read_first>
  <action>(1) harness 실행 (`AWS_PROFILE=sunity-motion` 으로 SSM Gemini 키 주입) → CALIBRATION.json 생성. (2) 결과 검증: chosen 4값 존재 + display ≠ training (training 이 더 엄격 — pose tol 이하 & confidence 이상) + table 격자 16조합 전부 FA/FR 기록 + fixtures >= 12 (미달 시 blocked=true 기록 + **RESULTS.json 의 blocked 를 true 로 갱신** — 31-12 게이트가 flag OFF 유지). (3) 31-ACCEPTANCE.md 의 placeholder 절 기입: "## Display 게이트" — judge 7축 전부 true + chosen display 임계값, "## Training 적재 게이트" — display + chosen training 임계값, "## Calibration 결과표" — FA/FR 표 전문 + 선택 근거 + 재현 커맨드 (`python backend/scripts/calibrate_visual_gates.py ...`) + meta(실행 시각/model/prompt version). (4) SUMMARY 에 chosen 값과 31-10 env 주입 대상(DISPLAY_POSE_TOL_DEG/TRAINING_POSE_TOL_DEG/DISPLAY_JUDGE_CONFIDENCE) 매핑 명시.</action>
  <verify>
    <automated>python -c "import json; c=json.load(open('.planning/phases/31-api-visual-correction/smoke/CALIBRATION.json')); assert ('chosen' in c and 'table' in c and 'meta' in c) or c.get('blocked') is True; m=c.get('meta',{}); assert c.get('blocked') is True or all(k in m for k in ('judge_model','prompt_version','ran_at'))" && grep -q "Training" .planning/phases/31-api-visual-correction/31-ACCEPTANCE.md</automated>
  </verify>
  <acceptance_criteria>
    - CALIBRATION.json 에 chosen 4값 + 격자표 + fixtures raw + meta (또는 blocked=true + RESULTS.json blocked 동기화)
    - display 와 training 이 서로 다른 chosen 값 (training 더 엄격)
    - 31-ACCEPTANCE.md 에 결과표 + 재현 커맨드 존재
    - 서명 URL/이미지 데이터 JSON 미포함
  </acceptance_criteria>
  <done>31-09(env 소비)/31-10(template 주입)/31-12(blocked 게이트)가 측정 기반 임계값으로 진행 가능 — H2-07 + H3-02 충족.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로컬 → Gemini | fixture 이미지 판정 호출 (31-01 belle 승인 범위 내) |
| 산출 JSON → git | 서명 URL/이미지 데이터 미포함 경계 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-59 | Denial of Service (과금) | 판정 호출 반복 | mitigate | --max-judge-calls 상한 + 31-01 Task 1 지출 승인 범위 |
| T-31-60 | Tampering | 재현 불가 임계값 drift | mitigate | fixture hash 재검증 + meta 4종 + 재현 커맨드 박제 (H3-02) |
| T-31-61 | Information Disclosure | 키/서명 URL/인물 이미지 | mitigate | SSM 키 주입만 + JSON 에 URL/바이트 미포함 + .planning 이미지 복사 금지 |
</threat_model>

<verification>
- CALIBRATION.json 스키마 assert 통과 + ACCEPTANCE 절 기입
- harness 재실행 시 동일 manifest 에서 동일 raw 산출 (judge 는 API 변동 허용 — meta 로 drift 추적)
</verification>

<success_criteria>
- H3-02 해소: calibration 이 31-05/31-06 실제 코드 기반 재현 가능 harness 로 실행되고, 임계값이 grid FA/FR 표에서 선택됨 — 3차 리뷰 §8-3 gate 7
- 31-09/31-10/31-12 의 임계값 소비 경로가 측정 근거를 가짐
</success_criteria>

<output>
Create `.planning/phases/31-api-visual-correction/31-13-SUMMARY.md` when done
</output>
