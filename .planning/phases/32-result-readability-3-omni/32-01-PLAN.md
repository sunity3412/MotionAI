---
phase: 32-result-readability-3-omni
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/shared/python/sunity_shared/analysis/motion_alignment.py
  - backend/shared/python/sunity_shared/analysis/fault_zoom.py
  - backend/tests/phase32/conftest.py
  - backend/tests/phase32/test_motion_alignment_ladder.py
  - backend/tests/phase32/test_fault_zoom_crop_parity.py
autonomous: true
requirements: [D-16, D-20]
must_haves:
  truths:
    - "저신뢰(distance>T2) DTW 결과가 disabled 대신 trim_only + reason='low_global_confidence'로 방출되고 anchors가 보존된다 (D-16)"
    - "진짜 degenerate 3종(invalid_fps/empty_path/insufficient_anchors)만 disabled로 남는다 (D-16)"
    - "fault_zoom relaxed 경로의 프레이밍 배율이 valid 경로와 동일(1.8배)해지고, 마커 생략 게이트는 현행 유지된다 (D-20)"
    - "채점·veto 로직은 바이트 단위 무접촉 — 점수 불변"
  artifacts:
    - path: "backend/tests/phase32/conftest.py"
      provides: "phase32 테스트 스캐폴드 (sys.path 주입)"
    - path: "backend/tests/phase32/test_motion_alignment_ladder.py"
      provides: "사다리 재배치 단위 테스트"
    - path: "backend/tests/phase32/test_fault_zoom_crop_parity.py"
      provides: "relaxed 프레이밍 parity 단위 테스트"
  key_links:
    - from: "backend/shared/python/sunity_shared/analysis/motion_alignment.py"
      to: "firestore_admin._validate_motion_alignment"
      via: "trim_only 방출이 validator 역불변식(anchors ≥ 2쌍) 충족"
      pattern: "trim_only"
---

<objective>
Wave-1 즉시 수리 3건 중 백엔드 2건: D-16 동작 비교 초 맞춤(motion_alignment 사다리 재배치 — 계약 변경 0 경로)과 D-20 확대비교 크롭 배율 통일(fault_zoom relaxed 프레이밍 분리 수정). phase32 테스트 스캐폴드를 함께 신설한다(Wave 0 요구 — 이후 모든 백엔드 플랜이 이 conftest를 재사용).

Purpose: belle 최우선 지목 수리(D-16)의 백엔드 절반 + 결정 불요 백엔드 수리(D-20). 새 분석부터 적용.
Output: 사다리 재배치된 motion_alignment.py, 프레이밍 분리된 fault_zoom.py, backend/tests/phase32/ 스캐폴드 + 테스트 2벌.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-result-readability-3-omni/32-CONTEXT.md
@.planning/phases/32-result-readability-3-omni/32-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: phase32 테스트 스캐폴드 + motion_alignment 사다리 재배치 (D-16)</name>
  <files>backend/tests/phase32/conftest.py, backend/tests/phase32/test_motion_alignment_ladder.py, backend/shared/python/sunity_shared/analysis/motion_alignment.py</files>
  <read_first>
    - backend/shared/python/sunity_shared/analysis/motion_alignment.py (수리 대상 — tier 사다리 :167-180, _disabled 헬퍼 :47-62, degenerate 호출처 :90-95/:147-148, 모듈 헤더 docstring 관례 :1-22)
    - backend/tests/phase31/conftest.py (sys.path 주입 관례 :36-41 — phase32 conftest가 복제)
    - backend/tests/test_motion_alignment.py, backend/tests/test_motion_alignment_contract.py (기존 방출 기대값 — low_global_confidence→disabled를 assert하는 테스트가 있으면 D-16 새 방출로 갱신 필요)
    - backend/shared/python/sunity_shared/firestore_admin.py:308-407 (_validate_motion_alignment 역불변식 — trim_only는 anchors ≥ 2쌍 요구, 빈 anchors는 disabled만 허용. 수정 후에도 이 불변식이 자동 충족됨을 테스트로 증명)
  </read_first>
  <behavior>
    - Test 1: distance > DISTANCE_T2인 입력(pairs ≥ 2)에서 방출 tier == 'trim_only', reason == 'low_global_confidence', anchors 비어 있지 않음
    - Test 2: empty_path / invalid_fps / insufficient_anchors 각각의 degenerate 입력에서 tier == 'disabled' + anchors == [] 유지
    - Test 3: distance ≤ T1 ∧ slopes_ok → 'warped' / T1 < distance ≤ T2 → 'trim_only' 기존 사다리 무회귀
    - Test 4: trim_only 방출(신규 저신뢰 경로 포함)이 firestore_admin._validate_motion_alignment를 예외 없이 통과
  </behavior>
  <action>
    per D-16. (1) backend/tests/phase32/conftest.py 신설 — phase31/conftest.py의 sys.path 주입 블록(_BACKEND/_LAYER/_SCRIPTS 3경로 insert)을 복제. fake_firestore는 이 플랜에서 불필요하므로 넣지 않는다(32-06에서 phase31 것 재사용). (2) test_motion_alignment_ladder.py에 위 behavior 4건을 작성. (3) motion_alignment.py 사다리의 else 분기(현행: _disabled(distance, "low_global_confidence") 호출)를 tier="trim_only" + reason="low_global_confidence" + anchors 보존 방출로 교체 — trim_only는 MOTION_ALIGNMENT_TIERS enum에 이미 존재하므로 models.py/analysis.ts/contract.md/validator 전부 무변경(계약 변경 0 경로, 32-RESEARCH §수리 #1 확정). _disabled 헬퍼와 degenerate 3개 호출처(invalid_fps :91 / empty_path :95 / insufficient_anchors :148)는 그대로 둔다. 모듈 docstring에 D-16 각주 1줄 추가(관례 :1-22). (4) 기존 test_motion_alignment*.py에서 저신뢰→disabled를 기대하는 assert가 있으면 새 방출로 갱신하되 degenerate 3종 기대는 불변 확인. 채점 코드(dimensions/kismam/deduction) 무접촉.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/phase32/test_motion_alignment_ladder.py tests/test_motion_alignment.py tests/test_motion_alignment_contract.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - backend/tests/phase32/conftest.py가 존재하고 sys.path 3경로 주입 코드를 포함
    - test_motion_alignment_ladder.py의 behavior 4건 전부 green
    - motion_alignment.py에서 `grep -c '_disabled(' backend/shared/python/sunity_shared/analysis/motion_alignment.py` 결과가 정의 1 + degenerate 호출 3 = 4 이하 (저신뢰 분기의 _disabled 호출 제거됨)
    - 기존 motion_alignment 테스트 전부 green (갱신분 포함)
  </acceptance_criteria>
  <done>저신뢰 DTW가 anchors를 보존한 trim_only로 방출되고, degenerate 3종만 disabled로 남으며, 계약·validator 무변경으로 전 테스트 green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: fault_zoom relaxed 프레이밍 분리 수정 (D-20)</name>
  <files>backend/shared/python/sunity_shared/analysis/fault_zoom.py, backend/tests/phase32/test_fault_zoom_crop_parity.py</files>
  <read_first>
    - backend/shared/python/sunity_shared/analysis/fault_zoom.py (수리 대상 — _side_crop/_box_for :525-587, 상수 _BBOX_MARGIN=1.8 :86 / _RELAXED_MARGIN=2.0 :95)
    - backend/tests/test_fault_zoom_relaxed_crop.py (relaxed crop 전용 기존 테스트 — 기대값 갱신 대상 확인)
    - .planning/phases/32-result-readability-3-omni/32-PATTERNS.md §A-4 (수리 지점 발췌 — relaxed 경로 margin 1.0 전환, crop_kind="relaxed" 유지로 마커 생략 게이트 보존)
  </read_first>
  <behavior>
    - Test 1: 동일 bbox 입력에서 valid 경로와 relaxed 경로의 crop side 길이가 동일 (프레이밍 parity — 양측 1.8배)
    - Test 2: relaxed 경로의 반환 crop_kind가 "relaxed" 유지, anchor_px는 None 유지 (마커 생략 게이트 현행 보존)
    - Test 3: valid 경로 산출은 수정 전과 바이트 동일 (무회귀)
  </behavior>
  <action>
    per D-20. fault_zoom.py::_side_crop의 relaxed 분기에서 _box_for(relaxed_pts, _RELAXED_MARGIN) 호출을 _box_for(relaxed_pts, 1.0)으로 변경 — 프레이밍은 좌표 오차에 둔감하므로 양측 동일 배율(1.8), 마커는 민감하므로 relaxed의 anchor_px=None 생략 게이트는 현행 유지(신뢰도 게이트가 프레이밍과 마커를 동시에 결정하던 구조의 분리 — 메모리 faultzoom-reference-crop-2x-wider-diagnosed 합의안). _RELAXED_MARGIN 상수는 삭제하지 말고 미사용 시 주석으로 사유 명기(향후 마커 게이트 튜닝 참조용) 또는 제거 — 제거 시 import/참조 전수 확인. display 전용 수정 — 채점·veto 코드 무접촉(점수 불변). PNG는 분석 시 생성·저장이므로 새 분석부터 적용, 기존 doc 재처리 금지(같은 RTMW 엔진이라 재처리해도 신뢰도 동일 — 메모리 박제 근거). Pod 실측 육안 검증은 32-03(1건)과 32-15(전수)에서 수행하므로 이 태스크는 단위 테스트까지.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/phase32/test_fault_zoom_crop_parity.py tests/test_fault_zoom_relaxed_crop.py -x -q && python -m pytest tests -q -k "fault_zoom" 2>/dev/null | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - test_fault_zoom_crop_parity.py의 behavior 3건 green
    - fault_zoom.py의 relaxed 분기에서 `_box_for(relaxed_pts, 1.0)` 형태 호출 확인 (grep "_box_for(relaxed_pts" 결과에 _RELAXED_MARGIN 미포함)
    - 기존 fault_zoom 계열 테스트 전부 green (기대값 갱신분 포함)
    - 채점 모듈(dimensions.py/kismam.py/deduction 계열) diff 0 — `rtk git diff --stat`에 해당 파일 부재
  </acceptance_criteria>
  <done>relaxed 프레이밍이 valid와 동일 배율로 통일되고 마커 게이트는 보존, 채점 무접촉이 diff로 증명됨</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline → Firestore | motion_alignment 방출값이 doc에 저장되어 앱이 소비 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Tampering | motion_alignment 방출 | mitigate | 기존 _validate_motion_alignment 화이트리스트·enum·finite 검사가 trim_only 방출에 그대로 적용됨을 Task 1 behavior 4로 증명 |
| T-32-02 | DoS(품질) | fault_zoom crop | accept | display 전용 수정, 채점 무접촉 diff 0으로 점수 오염 불가. 신규 외부 입력 없음 |
</threat_model>

<verification>
- cd backend && python -m pytest tests/phase32 -x -q (신설 테스트 전부 green)
- 기존 motion_alignment/fault_zoom 계열 테스트 green (baseline 초과 실패 0)
</verification>

<success_criteria>
- D-16 백엔드부: 저신뢰 trim_only 방출 + anchors 보존, 계약 변경 0
- D-20: relaxed 프레이밍 parity, 마커 게이트 보존, 채점 무접촉
- backend/tests/phase32/ 스캐폴드 가동 (이후 플랜 재사용 가능)
</success_criteria>

<output>
Create `.planning/phases/32-result-readability-3-omni/32-01-SUMMARY.md` when done
</output>
