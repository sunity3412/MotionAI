---
phase: 32-result-readability-3-omni
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/lib/manualOffset.ts
  - app/src/lib/__tests__/manualOffset.test.ts
  - app/src/components/VideoCompare.tsx
  - app/src/app/analysis/result.tsx
autonomous: true
requirements: [D-16, D-03]
must_haves:
  truths:
    - "저신뢰 정렬(trim_only + low_global_confidence)에서 '대략 맞춤' 정직 라벨이 표시되고 '자동 정렬 꺼짐' 배지는 폐지된다 (D-16)"
    - "사용자가 ±초 수동 미세조정 슬라이더로 두 영상의 시작 오프셋을 조정할 수 있다 — 전 tier 공통 (D-16)"
    - "legacy doc(disabled + 빈 anchors)에서도 faultZoomComparisons 프레임 인덱스로 대략 오프셋이 자동 적용된다 (D-16)"
    - "참고 지표 카드 장문('동작 흐름'/'안정성')이 줄겹침 없이 렌더된다 (D-03 최소 수리)"
  artifacts:
    - path: "app/src/lib/manualOffset.ts"
      provides: "수동 오프셋 합성·legacy 폴백 오프셋 산출 순수 함수 (player/react 의존 0)"
      exports: ["composeRefTarget", "legacyOffsetFromCompareFrames"]
    - path: "app/src/lib/__tests__/manualOffset.test.ts"
      provides: "node --test 순수 로직 테스트"
  key_links:
    - from: "app/src/components/VideoCompare.tsx"
      to: "app/src/lib/manualOffset.ts"
      via: "clampRefTarget 단일 warp 경유 지점에서 합성 함수 호출"
      pattern: "composeRefTarget"
---

<objective>
Wave-1 즉시 수리 3건 중 앱 2건: D-16 동작 비교 초 맞춤의 앱 절반(수동 ±초 슬라이더 + "대략 맞춤" 배지 + legacy doc 폴백)과 D-03 참고 지표 겹침 버그의 최소 수리(lineHeight). 표현 전면 수정은 목업 게이트(32-04) 이후이므로 여기서는 겹침 해소만.

Purpose: belle 최우선 지목 "초를 좀만 맞추면 구간이 맞아떨어진다" 실현 + 실기기 재현 확정된 프로덕션 겹침 버그 해소.
Output: 슬라이더·배지·폴백이 들어간 VideoCompare, 겹침 수리된 result.tsx, 순수 함수 lib + node --test.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-result-readability-3-omni/32-CONTEXT.md
@.planning/phases/32-result-readability-3-omni/32-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: manualOffset 순수 함수 + node --test</name>
  <files>app/src/lib/manualOffset.ts, app/src/lib/__tests__/manualOffset.test.ts</files>
  <read_first>
    - app/src/lib/alignmentWarp.ts (warpTime 시그니처·trim_only 산식 warp(t)=t−u0+r0 :49-72, normalize :100-166, "순수 함수만 player/react 의존 0" 헤더 관례 :1-3)
    - app/src/lib/pickerFailure.test.ts (node --test 헤더 관례 — .ts 확장자 import 명시, node:test/node:assert만, 신규 러너 금지 사유 주석 :1-18)
    - app/src/types/analysis.ts:473-487 (FaultZoomComparison의 userFrameIdx/refFrameIdx/refMatched — legacy 폴백 입력 형상)
  </read_first>
  <behavior>
    - Test 1: composeRefTarget(rawWarpTarget, manualOffsetSec, durationRef) — 합성 후 [0, durationRef] 클램프. 오프셋 ±로 경계 초과 시 경계값 반환
    - Test 2: durationRef가 0/음수/NaN일 때 안전 (max(0, raw) 폴백 — 크래시 0)
    - Test 3: legacyOffsetFromCompareFrames({userFrameIdx, refFrameIdx}, userFps, refFps) = refFrameIdx/refFps − userFrameIdx/userFps. fps는 인자로만 받는다(9/18 하드코딩 금지)
    - Test 4: refMatched=false 또는 프레임 인덱스 부재 시 null 반환 (폴백 불가 케이스 — 슬라이더만 제공)
  </behavior>
  <action>
    per D-16. app/src/lib/manualOffset.ts 신설 — alignmentWarp.ts 헤더 관례대로 "순수 함수만, player/react 의존 0" 선언. export 2개: composeRefTarget(raw: number, manualOffsetSec: number, durationRef: number): number / legacyOffsetFromCompareFrames(frames: {userFrameIdx?: number; refFrameIdx?: number; refMatched?: boolean} | null, userFps: number, refFps: number): number | null. fps 인자는 호출부가 각자의 keypointReport.fps에서 읽어 넘긴다(SP-6 — result.tsx:2105 패턴이 정본, 32-RESEARCH Pitfall 2). 테스트는 pickerFailure.test.ts 관례(node:test/node:assert, .ts import) 복제.
  </action>
  <verify>
    <automated>node --test app/src/lib/__tests__/manualOffset.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - manualOffset.ts가 composeRefTarget/legacyOffsetFromCompareFrames를 export하고 react/expo import 0
    - node --test 결과 behavior 4건 전부 pass
    - 파일 내 리터럴 9/18 fps 하드코딩 0 (fps는 인자 전용)
  </acceptance_criteria>
  <done>수동 오프셋 합성과 legacy 폴백 산출이 순수 함수로 분리되어 node --test green</done>
</task>

<task type="auto">
  <name>Task 2: VideoCompare 수동 슬라이더 + "대략 맞춤" 배지 + legacy 폴백 배선 (D-16)</name>
  <files>app/src/components/VideoCompare.tsx, app/src/app/analysis/result.tsx</files>
  <read_first>
    - app/src/components/VideoCompare.tsx (수정 대상 — clampRefTarget/setRightToStudentTime 단일 warp 경유 지점 :366-383 "right 쓰기의 유일한 warp 경유 지점" 주석, ref 미러 패턴 :352-356, tick 루프 :392-453, alignBadgeCopy 분기 :921-934)
    - .planning/phases/32-result-readability-3-omni/32-PATTERNS.md §A-2 (삽입 지점 발췌 전문 — manualOffsetSec 합성 위치·배지 분기·수치 비표기 주석)
    - app/src/app/analysis/result.tsx:855-858 (pickCompareFrames — legacy 폴백 소스), :2105-2135 (fps 환산 정본 — result.keypointReport?.fps || 9 / referenceKeypointReport?.fps || 18)
    - app/src/theme/index.ts (slider·배지 스타일에 쓸 토큰 — 하드코딩 금지)
  </read_first>
  <action>
    per D-16. (1) VideoCompare에 manualOffsetSec 상태 + ref 미러(alignmentRef 관례 복제 — tick 클로저 stale 회피) 추가. clampRefTarget 내부에서 composeRefTarget(raw, manualOffsetRef.current, dR) 호출로 합성 — 이 함수가 유일한 warp 경유 지점이므로 drift 보정 tick·togglePlay·seek 전부 자동 반영. (2) 수동 ±초 미세조정 슬라이더 UI: D-16 원문은 슬라이더 — 신규 npm 의존성 0으로 PanResponder(RN 내장 Gesture Responder) 기반 커스텀 드래그 슬라이더(트랙+썸 View)로 구현한다. 범위 ±3초·스냅 단위(예: 0.1초)는 재량, 현재 오프셋 라벨(예: "+1.5초") + 리셋 버튼 동반. 접근성: accessibilityRole="adjustable" + accessibilityActions(increment/decrement)로 드래그 불가 사용자 폴백. 세션 상태만(영속화는 실물 게이트 후 판단 — D-17). 전 tier 공통 노출. (3) alignBadgeCopy에 tier==='trim_only' ∧ reason==='low_global_confidence' 분기 신설 — "대략 맞춤" 정직 라벨(예: title '대략 맞춤', hint '동작 차이가 커 시작점만 대략 맞췄어요 — 아래 버튼으로 미세조정할 수 있어요'). "자동 정렬 꺼짐" 배지 문구는 진짜 degenerate(disabled)에만 남긴다. DTW distance 수치 비표기 주석(:922-924) 유지 — D-09 정합. (4) legacy doc 폴백: result.tsx에서 legacyOffsetFromCompareFrames(compareFrames, userKrFps, refKrFps)를 산출해 VideoCompare에 initialOffsetSec prop(신규, 미전달 시 0 — 렌더 diff 0 opt-in 관례)으로 전달. VideoCompare는 alignment가 disabled(legacy)일 때 이 값을 manualOffsetSec 초기값으로 사용. fps는 각자의 keypointReport.fps에서만(9/18 하드코딩 금지 — Pitfall 2).
  </action>
  <verify>
    <automated>cd app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - VideoCompare.tsx에 composeRefTarget import + clampRefTarget 내 합성 호출 존재 (grep "composeRefTarget" 결과 ≥ 2)
    - PanResponder 기반 커스텀 슬라이더 존재 (grep "PanResponder" ≥ 1) + 신규 npm 의존성 0 (package.json diff 0)
    - alignBadgeCopy에 low_global_confidence 분기 존재, '자동 정렬 꺼짐' 문구는 disabled 분기에만 잔존
    - VideoCompare에 initialOffsetSec?: number prop 추가, 미전달 시 기존 렌더 diff 0
    - result.tsx의 오프셋 산출에 리터럴 9/18 하드코딩 없음 (`|| 9`/`|| 18` 폴백 패턴은 기존 정본 형태만 허용)
    - typecheck clean
  </acceptance_criteria>
  <done>수동 오프셋이 단일 warp 경유 지점으로 합성되고, 대략 맞춤 배지·legacy 폴백이 동작하며 typecheck green</done>
</task>

<task type="auto">
  <name>Task 3: 참고 지표 겹침 최소 수리 (D-03) + 시뮬레이터 확인</name>
  <files>app/src/app/analysis/result.tsx</files>
  <read_first>
    - app/src/app/analysis/result.tsx:2545-2550 (diagSentence 스타일 — 겹침 근본 원인: typography.body(fontSize 25) 상속 + lineHeight 21 명시), :508-533 (DimensionDiagnosisRow — 렌더 소비처)
    - app/src/theme/typography.ts (토큰 스케일 — body 25, lineHeight ≥ fontSize×1.3 규칙은 32-RESEARCH Pitfall 3)
  </read_first>
  <action>
    per D-03 wave-1 최소 수리. diagSentence 스타일의 lineHeight를 35(fontSize 25 × 1.4)로 상향 — 또는 장문에 body(25)가 과대하므로 더 작은 기존 토큰으로 교체할 경우에도 lineHeight ≥ fontSize×1.3을 명시. 표현 전면 수정(심사 정보 코너 전환)은 목업 게이트(32-04) 이후 32-11 소관이므로 여기서는 겹침 해소만. 수리 후 시뮬레이터에서 결과 화면 진입 → 참고 지표 카드('동작 흐름'/'안정성' 장문) 렌더를 스크린샷으로 확인(ios-simulator MCP 또는 xcrun simctl — 이 맥에 Xcode 26.6 설치됨). Expo Go 폰트 폴백이 이 계열을 가리는 이력이 있으므로(SEED §10) 시뮬레이터 확인은 1차 게이트, 실기기 최종 확인은 32-03 실물 게이트에서.
  </action>
  <verify>
    <automated>cd app && npm run typecheck && grep -A4 "diagSentence" src/app/analysis/result.tsx | grep -c "lineHeight"</automated>
  </verify>
  <acceptance_criteria>
    - diagSentence 스타일의 lineHeight 값 ≥ 해당 fontSize × 1.3 (소스 assert)
    - typecheck clean
    - 시뮬레이터 결과 화면 스크린샷에서 참고 지표 장문 줄겹침 없음 (SUMMARY에 스크린샷 경로 기록)
  </acceptance_criteria>
  <done>겹침 원인(lineHeight < fontSize)이 소스에서 제거되고 시뮬레이터 렌더로 확인됨</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firestore doc → 앱 렌더 | legacy doc의 프레임 인덱스를 오프셋 계산에 사용 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-03 | Tampering | legacy 폴백 입력 | mitigate | legacyOffsetFromCompareFrames가 비정수·음수·부재 입력에 null 반환(Test 4) — userAnalyses normalizeFrameIdx가 이미 상류 방어 |
| T-32-04 | DoS(크래시) | 수동 오프셋 합성 | mitigate | composeRefTarget가 durationRef 0/NaN에 안전(Test 2) — 재생 루프 크래시 0 |
</threat_model>

<verification>
- node --test app/src/lib/__tests__/manualOffset.test.ts 전부 pass
- cd app && npm run typecheck clean
- 시뮬레이터 결과 화면 진입 확인 (동작 비교 배지·슬라이더·참고 지표)
</verification>

<success_criteria>
- D-16 앱부: 대략 맞춤 라벨 + 수동 미세조정 + legacy 폴백 — "자동 정렬 꺼짐"으로 포기하는 경로 소멸
- D-03 수리부: 겹침 해소 (표현 전면 수정은 게이트 후)
- OTA는 이 플랜에서 발행하지 않음 — 32-03이 발행(빌드 경로 청결 + 롤백 준비 포함)
</success_criteria>

<output>
Create `.planning/phases/32-result-readability-3-omni/32-02-SUMMARY.md` when done
</output>
