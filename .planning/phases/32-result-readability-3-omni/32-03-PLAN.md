---
phase: 32-result-readability-3-omni
plan: 03
type: execute
wave: 2
depends_on: ["32-01", "32-02"]
files_modified:
  - .planning/phases/32-result-readability-3-omni/32-GATE-DECISIONS.md
autonomous: false
model: fable
requirements: [D-17, D-16, D-20, D-23]
must_haves:
  truths:
    - "wave-1 백엔드 수리가 Pod에 배포되고 fixture 6동작 전수 스윕(순차)으로 실측된다 — trim_only 방출·크롭 parity·동작별 점수 diff 0 (D-16/D-20/D-23)"
    - "6동작 점수·verdict가 배포 전 직전 기준선과 diff 0 — 32-01 '채점 무접촉'의 실측 증명 (D-23 웨이브 게이트)"
    - "wave-1 앱 수리가 OTA로 belle 실기기에 도달한다 (빌드 경로 청결 + 롤백 준비)"
    - "belle이 수리된 실물로 동작 비교 형태·자세 비교 카드 존폐·재생 중 큐 밀도를 그 자리에서 확정하고 결정이 32-GATE-DECISIONS.md에 적재된다 (D-17)"
    - "D-23 스윕 게이트의 phase 32 운영 매핑(배포 시점마다 6동작 전수 + UI-only 웨이브는 6동작 doc 렌더 검증)이 belle에게 제시·확인된다"
  artifacts:
    - path: ".planning/phases/32-result-readability-3-omni/32-GATE-DECISIONS.md"
      provides: "실물 게이트 확정 사항 (후속 플랜 32-08/32-11의 입력) + D-23 매핑 확인"
      contains: "실물 게이트"
  key_links:
    - from: "32-GATE-DECISIONS.md"
      to: "32-08/32-11 (큐 밀도·자세 카드·비교 형태 구현)"
      via: "게이트 확정 기록"
      pattern: "D-17"
---

<objective>
D-17 실물 게이트: "고장난 현행 위에서 판단 불가"였던 3가지(동작 비교 형태 / 자세 비교 카드 존폐 / 재생 중 큐 밀도)를 wave-1 수리가 반영된 실물을 belle과 실기기로 보며 그 자리에서 확정한다. 선행으로 백엔드 수리를 Pod에 배포하고 **fixture 6동작 전수 스윕**(D-23 — 리뷰 반영: 1건 축소 금지)으로 실측하며, 앱 수리를 OTA 발행한다.

Purpose: 4대 미결 게이트 중 실물 게이트를 지정된 방식(수리 후 실기기 리뷰)으로만 닫는다 + D-23 웨이브 게이트의 첫 실행.
Output: Pod 배포 + 6동작 스윕 실측, OTA 발행, 32-GATE-DECISIONS.md의 D-17 확정 3건 + D-23 매핑 확인.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-result-readability-3-omni/32-CONTEXT.md
@.planning/phases/32-result-readability-3-omni/32-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pod 상태 확인 → wave-1 백엔드 수리 배포 → fixture 6동작 전수 스윕 (D-23)</name>
  <files>(코드 수정 없음 — Pod 운영 + 스윕 기록)</files>
  <read_first>
    - .planning/phases/32-result-readability-3-omni/32-RESEARCH.md §Common Pitfalls 9/10 (스윕 순차 강제 + Pod 운영 — /health 200 확인 전 진행 금지, pkill 자기 세션 함정, 재생성 시 proxy URL → SSM+Lambda 재동기화)
    - ~/.claude/projects/-Users-kimtaesung-Dev-SunityMotion/memory/current-pod-hbpvhedq2bu01i.md (Pod 셋업 절차 — bootstrap→start_server→VETO env→X-RunPod-Token 스모크→SSM+Lambda 재동기화)
  </read_first>
  <action>
    per D-16/D-20 실측 + D-23 웨이브 게이트. (1) 현재 Pod 가동 여부 확인 — Lambda env의 RUNPOD_ANALYZE_URL 기준 `/health` 호출. 미가동이면 **여기서 멈추고** belle에게 Pod 재생성 승인을 AskUserQuestion으로 요청(GPU 우선순위: 4090 1순위 — 메모리 pod-request-include-gpu-model 관례. Network Storage 볼륨 재사용) — 승인 후 메모리 셋업 절차로 기동, proxy URL 변경 시 SSM + Lambda env 재동기화까지 완료. `/health` 200 확인 전 다음 단계 진행 금지. (2) 배포 전 기준선 확보: 보유 fixture 6동작의 직전 점수·verdict 기준선(가장 최근 스윕 기록 또는 Firestore 기존 doc)을 표로 정리 — 없으면 배포 전 스윕 1회로 기준선 생성. (3) wave-1 커밋(32-01)을 push 후 Pod에서 git pull + 서버 재시작 + `/health` 200 재확인 (gsd-pod-work-push-first 관례). (4) fixture 6동작 전수 스윕(순차 — 동시 호출 금지, kip-up 편중 금지 — D-23. 리뷰 반영: 1 fixture 축소는 D-23 위반이라 폐기): 각 결과 doc에서 (a) motionAlignment 방출 — 저신뢰 케이스는 trim_only + reason=low_global_confidence + anchors 비어있지 않음, degenerate만 disabled, (b) faultZoomComparisons 신규 PNG의 내/정은지 줌 배율 — 육안 비교 + Pod 로그의 crop side 길이 수치(32-01 Task 2 로그)로 user/ref side 비 0.8~1.25 판정, (c) **동작별 점수·verdict가 기준선 대비 diff 0** — 32-01 "채점 무접촉"의 실측 증명. 6동작 중 저신뢰 정렬·relaxed crop 재현 케이스가 각 1건 이상 포함되는지 확인 — 자연 발생 없으면 저신뢰 재현 가능한 추가 fixture(파워스핀 계열 선례)로 보강. (5) 스윕 결과 표(동작 × tier/reason × crop side 비 × 점수 diff)를 SUMMARY에 기록.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" "$RUNPOD_ANALYZE_URL/health" | grep -q 200 && echo POD_OK</automated>
  </verify>
  <acceptance_criteria>
    - Pod /health 200 (배포 후 재확인 포함)
    - 6동작 전수 스윕 표 존재 — 동작별 tier/reason + 점수 diff 0 (기준선 대비)
    - 저신뢰 케이스 ≥ 1건에서 result.motionAlignment.tier == "trim_only" + anchors 보존 실측
    - crop parity — 육안 + 로그 수치(user/ref side 비 0.8~1.25) 양쪽 기록 (스크린샷/파일 경로 SUMMARY 기록)
  </acceptance_criteria>
  <done>수리된 백엔드가 프로덕션 Pod에 반영되고 6동작 전수 스윕으로 D-16/D-20 방출과 채점 불변이 증명됨 (D-23 웨이브 게이트 통과)</done>
</task>

<task type="auto">
  <name>Task 2: wave-1 앱 수리 OTA 발행 (빌드 경로 청결 + 롤백 준비)</name>
  <files>(코드 수정 없음 — OTA 운영)</files>
  <read_first>
    - .planning/phases/32-result-readability-3-omni/32-RESEARCH.md §Common Pitfalls 4 (시뮬레이터 한계 + OTA 절차 — 빌드 경로 청결, republish 롤백, expo-updates 완전 종료 2회째 적용)
  </read_first>
  <action>
    32-02의 시뮬레이터 확인이 완료된 상태를 전제로 OTA 발행. (1) 발행 전 현재 정상 update group id를 기록(롤백 대상 — `npx eas update:republish --group <id>` 1분 절차 준비). (2) 정상 경로에서 빌드(임시 worktree·node_modules 심볼릭 링크 금지 — 2026-07-20 앨범 알림창 기동 불가 사고의 유력 원인 계열). (3) production+preview 채널 OTA 발행. (4) 발행된 group id를 SUMMARY에 기록. (참고: 이 시점의 앱은 native 변경 0 — runtime 1.0.0 대상 OTA. 오디오 모듈이 들어가는 32-12부터는 새 EAS build+TestFlight 경로로 전환된다.)
  </action>
  <verify>
    <automated>cd app && npx eas update:list --branch production --limit 1 --non-interactive 2>/dev/null | head -5</automated>
  </verify>
  <acceptance_criteria>
    - 신규 update group 발행 확인 (eas update:list 최신 항목이 이번 발행)
    - 직전 정상 group id가 SUMMARY에 롤백 커맨드와 함께 기록됨
  </acceptance_criteria>
  <done>wave-1 앱 수리분이 OTA로 발행되고 롤백 경로가 준비됨</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: belle 실기기 리뷰 — D-17 3건 그 자리 확정 + D-23 매핑 확인</name>
  <what-built>wave-1 수리 3건이 반영된 실물: 동작 비교 초 맞춤(대략 맞춤 배지 + ±초 미세조정 + legacy 폴백), 참고 지표 겹침 해소, (새 분석 한정) 확대비교 크롭 parity — 6동작 전수 스윕 통과 상태</what-built>
  <how-to-verify>
    1. 실기기(TestFlight)에서 앱 완전 종료 2회 재실행 (expo-updates는 2회째 적용)
    2. 기존 분석 결과 화면 진입 → 참고 지표 카드 장문 줄겹침 없음 확인
    3. 동작 비교 재생 → "대략 맞춤" 배지 + 슬라이더 드래그로 미세조정 체감 확인 (기존 doc이면 legacy 폴백 오프셋 동작)
    4. 가능하면 새 분석 1건 업로드 → 확대비교 줌 쌍 배율 확인
    5. 그 자리에서 3건 확정 (D-17):
       - 동작 비교 형태 — 제안: 기본 양옆 동시 + 탭하면 한쪽 확대 + 가로 유지
       - 자세 비교 카드 존폐 — 방향 매칭 프로토(몸 방향 비슷한 정은지 프레임 짝짓기, 실패 시 카드 숨김) 포함 여부
       - 재생 중 큐 밀도 — 결함 구간당 1개 vs 더 촘촘히
    6. D-23 스윕 게이트 운영 매핑 확인 (1분): "백엔드가 파이프라인에 배포되는 웨이브(32-03/32-09/32-12 B안/32-13/32-14/32-15)마다 6동작 전수 스윕, 백엔드 무배포 UI 웨이브는 재분석 대신 6동작 doc 전수로 결과 화면 렌더 검증(kip-up 편중 금지의 UI 적용)" — 이 매핑대로 갈지 확인. 거부 시 매 웨이브 재분석 스윕으로 전환(비용: Pod 상시 가동 + Gemini 콜 증가).
  </how-to-verify>
  <action>belle에게 how-to-verify 절차를 안내하고 실기기 리뷰를 진행한다. 확정된 3건(동작 비교 형태·자세 비교 카드 존폐·큐 밀도)과 D-23 매핑 확인 결과를 32-GATE-DECISIONS.md의 "## 실물 게이트 (D-17)" 섹션 + "## D-23 스윕 매핑" 섹션에 결정·일자·belle 원문 요지로 적재한다.</action>
  <verify>
    <automated>grep -c "실물 게이트" .planning/phases/32-result-readability-3-omni/32-GATE-DECISIONS.md</automated>
  </verify>
  <acceptance_criteria>
    - 32-GATE-DECISIONS.md에 "## 실물 게이트 (D-17)" 섹션 + 3건 확정 내용 존재
    - 32-GATE-DECISIONS.md에 "## D-23 스윕 매핑" 섹션 + belle 확인 결과 존재
  </acceptance_criteria>
  <done>D-17 3건이 belle 실기기 리뷰로 확정되고 D-23 운영 매핑이 확인되어 기록됨</done>
  <resume-signal>3건 확정 + D-23 매핑 확인을 답하면 Claude가 32-GATE-DECISIONS.md에 적재 후 계속</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로컬 → Pod SSH/proxy | 배포 명령·헬스체크가 프로덕션 GPU 서버에 도달 |
| OTA → 사용자 기기 | 발행 즉시 전 사용자 앱 코드 교체 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-05 | DoS | Pod 배포 | mitigate | /health 200 확인 전 다음 단계 진행 금지(31 사고 재발 방지) + pkill 자기 세션 함정 회피(원격 명령 문자열에 uvicorn 패턴 미포함) + Pod 재생성은 belle 승인 후만 |
| T-32-06 | DoS | OTA 발행 | mitigate | 직전 정상 group 기록 + republish 롤백 1분 경로 준비, 빌드 경로 청결(worktree·symlink 금지) |
| T-32-07 | Spoofing | Pod proxy 호출 | mitigate | X-RunPod-Token 인증 유지(기존 셋업 절차), URL은 SSM 동기화 경로만 |
</threat_model>

<verification>
- Pod /health 200 + 6동작 전수 스윕 표 (trim_only 방출 + crop parity 수치 + 점수 diff 0)
- OTA group 발행 + 롤백 준비 기록
- 32-GATE-DECISIONS.md에 D-17 확정 3건 + D-23 매핑 적재
</verification>

<success_criteria>
- 실물 게이트가 지정된 방식(수리 후 실기기)으로 닫힘 — 후속 32-08(큐)·32-11(비교 형태·자세 카드 반영)의 입력 확보
- D-23 웨이브 게이트가 6동작 전수로 첫 실행되고 운영 매핑이 belle 확인으로 고정됨
</success_criteria>

<output>
Create `.planning/phases/32-result-readability-3-omni/32-03-SUMMARY.md` when done
</output>
