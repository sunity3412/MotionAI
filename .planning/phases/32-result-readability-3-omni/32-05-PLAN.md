---
phase: 32-result-readability-3-omni
plan: 05
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - backend/data/phrasebook.json
  - backend/data/terminology_map.json
  - backend/shared/python/sunity_shared/analysis/phrasebook.py
  - backend/tests/phase32/test_phrasebook_forbidden.py
  - backend/tests/phase32/test_phrasebook_assembly.py
  - backend/tests/phase32/test_terminology_lockstep.py
  - app/src/lib/terminologyMap.ts
  - .planning/phases/32-result-readability-3-omni/32-PHRASEBOOK-REVIEW.md
autonomous: false
requirements: [D-11, D-12, D-08, D-06, D-13, D-09, D-14]
must_haves:
  truths:
    - "동작×결함별 고정 문구집이 상태문(몸 말)·이유문(감점·위험 1줄)·행동문(외부 큐)·코치 질문 완성문·운동 연결(exerciseId+이유) 슬롯으로 존재한다 (D-08/D-11/D-13/D-28)"
    - "커버리지 매트릭스(등록 6동작 × 실존 criterion + safetyFlags enum)가 작성되고, 매 셀이 {전용 entry | criterion 공통 entry | fail-closed}로 분류된다 — entries 개수가 아니라 매트릭스가 커버리지 증명 (리뷰 반영)"
    - "미지원 조합은 일반론 조언 대신 fail-closed — 행동문 없이 '정확한 방법은 강사님과 확인' + 코치 질문으로 유도 (D-11 일반론 금지의 폴백 적용)"
    - "safetyFlags 각 유형에 대응하는 안전 문구 entry(차분한 안전 톤 — D-14)가 존재한다 — 32-09 안전 질문 조립의 매핑 원천"
    - "금지어 grep 게이트가 D-09 위반 패턴(%환산·유사도·수치 헤드라인)과 일반론 문구를 차단하고 sanity(추출 0건 방지) 테스트를 동반한다 (D-09/D-11)"
    - "용어 맵의 단일 출처가 backend/data/terminology_map.json이고, 앱 terminologyMap.ts와의 정합이 lockstep 테스트로 강제된다 (D-12 — 주석 정합이 아니라 테스트 정합)"
    - "belle이 문구집 전량 목록과 용어 맵을 감수·승인한다 (D-11 승인 선출시 경로)"
  artifacts:
    - path: "backend/data/phrasebook.json"
      provides: "동작×결함 고정 문구집 fixture (+_meta 커버리지 매트릭스·근거 출처)"
    - path: "backend/data/terminology_map.json"
      provides: "측정 용어 → 심사 언어 매핑 단일 출처 (TS/Python 양쪽 lockstep)"
    - path: "backend/shared/python/sunity_shared/analysis/phrasebook.py"
      provides: "조립 순수 함수 (boto3/네트워크 0, lazy 캐시, graceful, fail-closed)"
    - path: "app/src/lib/terminologyMap.ts"
      provides: "terminology_map.json 미러 (lockstep 테스트 대상)"
  key_links:
    - from: "backend/shared/python/sunity_shared/analysis/phrasebook.py"
      to: "backend/data/phrasebook.json"
      via: "lazy 캐시 로드 (exercise_map 관례)"
      pattern: "phrasebook.json"
    - from: "backend/tests/phase32/test_terminology_lockstep.py"
      to: "app/src/lib/terminologyMap.ts + backend/data/terminology_map.json"
      via: "텍스트 대조 lockstep (contract lockstep 테스트 선례)"
      pattern: "terminology_map"
---

<objective>
번역 레이어의 심장인 고정 문구집(D-11)과 용어 맵(D-12)을 만든다. LLM이 골격을 소유하는 경로를 차단하고("무릎을 더 펴세요" 일반론 = 현장 이탈 실증), 문구는 Claude가 IPSF·폴스포츠 지식 문서·NotebookLM 기반으로 초안 → belle 감수. D-09 금지어 게이트를 테스트로 강제한다. 커버리지는 개수가 아니라 실존 방출값 매트릭스로 증명하고, 못 받치는 조합은 fail-closed로 코치에게 보낸다.

Purpose: 32-09(파이프라인 방출)와 32-10(감점 카드 렌더)의 데이터 원천. 문구 품질이 이 phase의 성패.
Output: phrasebook.json + terminology_map.json + phrasebook.py + 금지어/조립/lockstep 테스트 + terminologyMap.ts + belle 승인.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-result-readability-3-omni/32-CONTEXT.md
@.planning/phases/32-result-readability-3-omni/32-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 실존 방출값 전수 조사 → 커버리지 매트릭스 → phrasebook.json 초안 작성</name>
  <files>backend/data/phrasebook.json</files>
  <read_first>
    - app/src/types/analysis.ts:523-534 (DeductionRecord — criterion/ruleId/unit/ipsfAnchor/source 실존 필드. 문구집 키는 이 방출값으로만)
    - backend/shared/python/sunity_shared/analysis/keypoint_frame.py:51-81 (좌표 표시층 8개 — 어깨2·엉덩이2·무릎2·손2. 발목·팔꿈치·손목 좌표 기반 표현은 32-14 확장 전 금지 — Pitfall 1)
    - backend/shared/python/sunity_shared/analysis/skeleton.py:39-62 (각도 측정층 JOINT_ANGLES 8관절 + JOINT_LABEL_KO)
    - backend/shared/python/sunity_shared/models.py (safetyFlags 유형 enum 실측 — 안전 entry 키의 원천)
    - docs/research/폴스포츠-지식.md (IPSF 감점 분류 — 회당 −0.2 정렬/라인/신장, −0.5 나쁜 각도/트랜지션, 20° 허용 오차, Flow/Shades — 이유문·심사 언어의 도메인 소스)
    - backend/data/corrective_exercises.json (fixture 구조 선례 + 운동 목록 — exerciseId 슬롯이 참조할 실존 운동 키)
    - backend/shared/python/sunity_shared/analysis/copy_templates.py:95-107, :247-265 (canned dict 구조 + FORBIDDEN_PHRASES 선례)
  </read_first>
  <action>
    per D-11/D-08/D-13/D-14/D-28. (1) 실존 방출값 전수 조사: deduction 엔진이 방출하는 criterion/ruleId 목록을 grep으로 수집(backend/shared/python/sunity_shared/analysis/에서 criterion 문자열 리터럴·CRITERION_GROUPS·angle_vs_reference__ 패턴 등), safetyFlags 유형 enum, keypointSet enum, 등록 동작 6종(REGISTERED_MOTIONS 실측) + 미등재 폴백 키를 확정. (2) **커버리지 매트릭스 작성(리뷰 HIGH 반영 — entries 개수는 커버리지 증명이 아님):** 행 = 등록 6동작 + 미등재, 열 = 조사된 criterion 전체 + safetyFlags 유형. 각 셀을 {전용 entry | criterion 공통 entry | fail-closed}로 분류해 `_meta.coverageMatrix`에 기록. 목표: 실측 방출 빈도 상위 조합(최근 스윕 doc 실측 — 32-03 스윕 결과 활용 가능 시 대조)은 전용 또는 공통 entry, 나머지는 fail-closed 명시 — 빈 셀 금지(전 셀 분류). (3) phrasebook.json 스키마: 최상위 `{_meta, entries, safetyEntries}` — entries는 "동작키.criterion키" + 매칭 우선순위(동작×criterion → criterion 공통 → fail-closed)를 지원하는 구조. 각 entry 슬롯: statusLine(상태 — 몸 말, 관절명 허용), whyLine(왜 — 감점·위험 이유 1줄, IPSF 심사 언어), cueLine(행동 — 외부 큐, 예: "무릎으로 가슴을 끌어안듯"·"발끝으로 천장을 밀어내듯"), coachQuestion(강사님께 물어볼 완성문), exerciseId(corrective_exercises.json 실존 키 — 리뷰 반영: ID 연결로 D-13 결함→운동 관계 고정), exerciseReason(결함→운동 연결 이유 1줄). safetyEntries는 safetyFlags 유형별 {statusLine, whyLine(왜 위험한지), coachQuestion} — cueLine·게임 요소 없음(D-14 차분한 안전 톤, 32-09 안전 질문 조립의 매핑 원천). (4) **fail-closed 원칙(리뷰 HIGH 반영):** 미지원 조합의 generic 폴백은 조언을 만들지 않는다 — cueLine 없음(렌더는 행동문 생략), whyLine은 안전한 사실만("심사 기준과 차이가 측정된 부분이에요"), coachQuestion은 "이 부분을 어떻게 고치면 좋을지 강사님께 확인해보세요" 계열 — 일반론 교정 지시 생성 경로 차단. (5) 문구 원칙: 수치는 슬롯 텍스트에 넣지 않는다 — 수치는 렌더 시 소형 신뢰 배지가 별도 표시(D-09). % 환산 표현 금지. 톤 = 친숙하되 장난스럽지 않게(D-12), 응원 톤이되 근거 없는 칭찬 금지(D-6). (6) 근거 추적성(리뷰 반영): `_meta.keySources`(criterion/ruleId 조사 출처) + `_meta.evidence`(entry 그룹별 도메인 근거 — 폴스포츠-지식.md 섹션명·NotebookLM 노트 제목 스냅샷, notebook-lm-pole-sports 병용) 기록.
  </action>
  <verify>
    <automated>cd backend && python -c "import json; d=json.load(open('data/phrasebook.json')); assert '_meta' in d and 'entries' in d and 'safetyEntries' in d and 'coverageMatrix' in d['_meta']; print('PHRASEBOOK_OK', len(d['entries']), len(d['safetyEntries']))"</automated>
  </verify>
  <acceptance_criteria>
    - phrasebook.json이 유효 JSON — entries 각각에 statusLine/whyLine/cueLine/coachQuestion/exerciseId/exerciseReason 키, safetyEntries 각각에 statusLine/whyLine/coachQuestion 키 존재
    - _meta.coverageMatrix가 6동작+미등재 × 전 criterion+safetyFlags 유형을 커버 — 빈 셀 0 (전 셀 {전용|공통|fail-closed} 분류)
    - entries의 exerciseId 전부가 corrective_exercises.json 실존 키 (파이썬 1줄 대조 스크립트로 확인)
    - _meta.keySources + _meta.evidence 기록 존재
    - 발목·팔꿈치·손목 "좌표" 기반 시각 표현 요구 문구 0건 (텍스트 큐는 신체 부위 언급 자체는 허용 — 좌표 의존 아님)
    - 전 슬롯 텍스트에 '%', '유사도', '박제' 부재
  </acceptance_criteria>
  <done>실존 키와 대조된 문구집 초안이 커버리지 매트릭스·안전 entry·fail-closed 폴백 구조로 존재</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: phrasebook.py 조립 함수 + 금지어 게이트 + 용어 맵 단일 출처(JSON) + lockstep</name>
  <files>backend/shared/python/sunity_shared/analysis/phrasebook.py, backend/tests/phase32/test_phrasebook_forbidden.py, backend/tests/phase32/test_phrasebook_assembly.py, backend/tests/phase32/test_terminology_lockstep.py, backend/data/terminology_map.json, app/src/lib/terminologyMap.ts</files>
  <read_first>
    - backend/shared/python/sunity_shared/analysis/exercise_map.py (정본 아날로그 — lazy 캐시 :29-84, 순수성 docstring :7-17, graceful skip :120-128, fabrication 금지 cap :220-224)
    - backend/tests/phase07/test_copy_templates_no_forbidden.py:72-91 (금지어 게이트 + sanity 테스트 형태)
    - backend/tests/phase32/ 기존 lockstep 테스트 선례 탐색 (models.py ↔ analysis.ts 텍스트 대조 형태 — 예: tests/test_motion_alignment_contract.py 계열을 읽고 파일 텍스트 대조 방식 복제)
    - app/src/lib/deductionLabels.ts (용어 맵 전신 — JOINT_LABEL_KO 이중 키 공간 :28-42, ANGLE_MEANING_KO :62-71, cross-side 계약 주석 관례 :108-110)
  </read_first>
  <behavior>
    - Test (assembly) 1: 등록 동작+전용 entry 존재 criterion 입력 → 슬롯 dict 반환 (plain camelCase scalar dict, exerciseId 포함)
    - Test (assembly) 2: 미등재 동작/미등재 criterion → fail-closed dict 반환 — cueLine 부재(None/키 생략), coachQuestion은 강사 확인 유도문, 크래시 0 (graceful)
    - Test (assembly) 3: 반환 dict에 nested array/dict 없음 (Firestore flat 저장 가능)
    - Test (assembly) 4: safetyEntries 조회 — safety_flag 유형 입력 → 안전 슬롯 dict (cueLine·게임 관련 키 없음)
    - Test (forbidden) 1: phrasebook.json + terminology_map.json 전체 string 순회에서 FORBIDDEN 패턴("%일치", "유사도", "박제", 정규식 `\d+\s*%`, 정규식 `\d+(\.\d+)?\s*(도|°)만큼` 류 수치-지시 일반론) 0건
    - Test (forbidden) 2: sanity — 추출 string 수 ≥ 50 (0-string 게이트 무의미 회귀 차단)
    - Test (lockstep): terminology_map.json의 전 키·값이 app/src/lib/terminologyMap.ts 파일 텍스트에 존재 (양방향 — TS에만 있는 매핑도 실패)
  </behavior>
  <action>
    per D-11/D-12/D-09. (1) phrasebook.py — exercise_map 관례 복제: _PHRASEBOOK_PATH(Path 상대 구성)+lazy 캐시, 공개 함수 assemble_phrases(motion_key: str|None, criterion: str, rule_id: str|None) -> dict (camelCase 슬롯: statusLine/whyLine/cueLine/coachQuestion/exerciseId/exerciseReason — fail-closed 시 cueLine/exerciseId 생략) + assemble_safety_phrases(flag_type: str) -> dict (safetyEntries 조회, 미등재 유형은 generic 안전 유도문 — 역시 조언 생성 없음). 매칭 우선순위: 동작×criterion → criterion 공통 → fail-closed. 순수성 docstring(boto3/네트워크 0, 3-way lockstep 언급) 복제. FORBIDDEN_PHRASES_PHRASEBOOK 튜플을 이 모듈에 정의(copy_templates 확장이 아니라 신규 모듈 소유 — 파일 충돌 회피). (2) **용어 맵 단일 출처(리뷰 HIGH 반영 — 주석 정합은 단일 출처가 아님):** backend/data/terminology_map.json 신설 — {측정 용어 키('stability'/'flow'/'angle' 등 차원·지표 용어 전체): 심사 언어 사람 말}. terminologyMap.ts는 이 JSON의 미러(값 동일 — TS는 as const 객체로 전사, 헤더 주석에 "단일 출처 = backend/data/terminology_map.json, lockstep 테스트로 강제" 명시). phrasebook.py의 whyLine 심사 용어도 이 JSON을 참조 가능하게 로드 헬퍼 제공. (3) lockstep 테스트 — pytest가 terminology_map.json을 로드하고 app/src/lib/terminologyMap.ts 파일 텍스트를 읽어 전 키·값 문자열 존재를 양방향 대조(기존 계약 lockstep 텍스트 대조 선례 복제). (4) 금지어 게이트 테스트 — JSON 2개 전체 string 순회(iterate) + parametrize + sanity. (5) 실제 라벨 문자열 확정은 Task 3 belle 감수에서 조정될 수 있음을 주석으로.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/phase32/test_phrasebook_forbidden.py tests/phase32/test_phrasebook_assembly.py tests/phase32/test_terminology_lockstep.py -x -q && cd ../app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - phrasebook.py에 def assemble_phrases( + def assemble_safety_phrases( 존재, boto3/requests import 0
    - 금지어 게이트 + sanity + lockstep 테스트 green
    - backend/data/terminology_map.json 존재 + terminologyMap.ts가 전 항목 미러 (lockstep green이 증명)
    - assemble_phrases 반환이 flat scalar dict (assembly Test 3 green) + fail-closed 시 cueLine 부재 (Test 2 green)
  </acceptance_criteria>
  <done>조립 순수 함수(fail-closed 포함)·금지어 게이트·용어 맵 단일 출처+lockstep이 테스트와 함께 성립</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: belle 문구 감수 — 문구집 전량 + 용어 맵 승인 (D-11/D-12)</name>
  <what-built>동작×결함 고정 문구집(전 entry + 안전 entry + fail-closed 폴백)과 용어 맵(측정 용어→심사 언어, JSON 단일 출처)</what-built>
  <how-to-verify>
    1. Claude가 감수용 전량 목록을 생성해 제시: 32-PHRASEBOOK-REVIEW.md — 전 entry를 동작×결함 표(상태→왜→행동 3단이 실제 카드에서 읽히는 형태) + 안전 entry + fail-closed 문구 + 용어 맵 전량 + 커버리지 매트릭스 요약 (리뷰 반영 — 대표 3~5개만으로는 corpus 승인이 아님)
    2. 대표 entry 3~5개는 카드 목업 형태로 먼저 보여 감 잡기용, 판정은 전량 목록 기준
    3. 판단 기준: ① 톤 — 친숙하되 장난스럽지 않게 ② 행동문이 일반론("무릎을 더 펴세요")이 아니라 외부 큐인가 ③ 심사 언어가 실제 IPSF 기준과 맞는가 ④ 코치 질문이 강사에게 그대로 보여줄 수 있는 완성문인가 ⑤ fail-closed 문구가 어색하지 않은가
    4. 수정 지시는 항목 단위로 — Claude가 반영 후 금지어 게이트 재실행
  </how-to-verify>
  <action>32-PHRASEBOOK-REVIEW.md(전량 표 — 감수용 산출물)를 생성해 belle에게 제시하고 감수를 받는다. 수정 지시는 항목 단위로 반영 후 금지어·lockstep 게이트를 재실행하고, 승인 결과를 SUMMARY에 기록한다(32-GATE-DECISIONS.md는 wave 2에서 32-03이 소유 — 같은 웨이브 파일 충돌 회피, 감수 기록의 정본은 SUMMARY).</action>
  <verify>
    <automated>cd backend && python -m pytest tests/phase32/test_phrasebook_forbidden.py tests/phase32/test_terminology_lockstep.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - 32-PHRASEBOOK-REVIEW.md 전량 표 존재 (전 entry + 용어 맵 + 매트릭스 요약)
    - belle 승인 기록 (SUMMARY — 32-GATE-DECISIONS.md는 32-03 소유라 이 플랜 미접촉)
    - 수정 반영분 포함 금지어·lockstep 게이트 green
  </acceptance_criteria>
  <done>문구집·용어 맵이 belle 전량 감수 승인 상태로 확정됨 (D-11 승인 선출시 충족)</done>
  <resume-signal>"승인" 또는 항목별 수정 지시 (수정 반영 후 재감수 또는 승인으로 종료)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 문구집 fixture → 사용자 화면 | 고정 문구가 코칭 문장으로 그대로 노출 |
| LLM(Cerebras) → 문구 가변부 | 32-09에서 배선 — 이 플랜은 골격만 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-10 | Tampering(신뢰) | 문구집 내용 | mitigate | 금지어 grep 게이트(D-09 패턴+일반론) + sanity + fail-closed 폴백(미지원 조합 조언 생성 차단) + belle 전량 감수 선출시(D-11) |
| T-32-11 | Info Disclosure | 문구 내 PII | mitigate | 문구집은 측정 수치·동작명·신체 부위만 — 개인 식별 정보 슬롯 자체가 없음 (acceptance의 고정 스키마) |
</threat_model>

<verification>
- cd backend && python -m pytest tests/phase32 -x -q green
- cd app && npm run typecheck clean
- belle 승인 기록 (SUMMARY) + 32-PHRASEBOOK-REVIEW.md 전량 표
</verification>

<success_criteria>
- 문구집·용어 맵이 커버리지 매트릭스 + 금지어 게이트 + lockstep + belle 전량 감수의 4중 보증으로 성립 — 32-09 방출·32-10 렌더의 데이터 원천 확보
- 미지원 조합은 fail-closed로 코치 출구에 연결 — 일반론 재유입 경로 0
</success_criteria>

<output>
Create `.planning/phases/32-result-readability-3-omni/32-05-SUMMARY.md` when done
</output>
