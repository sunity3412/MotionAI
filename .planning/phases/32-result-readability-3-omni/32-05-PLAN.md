---
phase: 32-result-readability-3-omni
plan: 05
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - backend/data/phrasebook.json
  - backend/shared/python/sunity_shared/analysis/phrasebook.py
  - backend/tests/phase32/test_phrasebook_forbidden.py
  - backend/tests/phase32/test_phrasebook_assembly.py
  - app/src/lib/terminologyMap.ts
autonomous: false
requirements: [D-11, D-12, D-08, D-06, D-13, D-09]
must_haves:
  truths:
    - "동작×결함별 고정 문구집이 상태문(몸 말)·이유문(감점·위험 1줄)·행동문(외부 큐)·코치 질문 완성문·운동 연결 이유 5슬롯으로 존재한다 (D-08/D-11/D-13/D-28)"
    - "문구집 키가 실존 방출값(criterion/ruleId/keypointSet enum)과 대조되어 작성된다 — 31 화살표 함정(§6-5) 재발 방지"
    - "금지어 grep 게이트가 D-09 위반 패턴(%환산·유사도·수치 헤드라인)과 일반론 문구를 차단하고 sanity(추출 0건 방지) 테스트를 동반한다 (D-09/D-11)"
    - "용어 맵('안정성'→심사 언어 등)이 앱 단일 출처 모듈로 신설되고 톤은 '친숙하되 장난스럽지 않게'다 (D-12)"
    - "belle이 문구집·용어 맵을 감수·승인한다 (D-11 승인 선출시 경로)"
  artifacts:
    - path: "backend/data/phrasebook.json"
      provides: "동작×결함 고정 문구집 fixture"
    - path: "backend/shared/python/sunity_shared/analysis/phrasebook.py"
      provides: "조립 순수 함수 (boto3/네트워크 0, lazy 캐시, graceful)"
    - path: "app/src/lib/terminologyMap.ts"
      provides: "측정 용어 → 심사 언어 상위 매핑 (단일 출처)"
  key_links:
    - from: "backend/shared/python/sunity_shared/analysis/phrasebook.py"
      to: "backend/data/phrasebook.json"
      via: "lazy 캐시 로드 (exercise_map 관례)"
      pattern: "phrasebook.json"
    - from: "backend/tests/phase32/test_phrasebook_forbidden.py"
      to: "phrasebook.json 전체 string"
      via: "JSON 순회 금지어 게이트 + sanity"
      pattern: "FORBIDDEN"
---

<objective>
번역 레이어의 심장인 고정 문구집(D-11)과 용어 맵(D-12)을 만든다. LLM이 골격을 소유하는 경로를 차단하고("무릎을 더 펴세요" 일반론 = 현장 이탈 실증), 문구는 Claude가 IPSF·폴스포츠 지식 문서·NotebookLM 기반으로 초안 → belle 감수. D-09 금지어 게이트를 테스트로 강제한다.

Purpose: 32-09(파이프라인 방출)와 32-10(감점 카드 렌더)의 데이터 원천. 문구 품질이 이 phase의 성패.
Output: phrasebook.json + phrasebook.py + 금지어/조립 테스트 + terminologyMap.ts + belle 승인.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-result-readability-3-omni/32-CONTEXT.md
@.planning/phases/32-result-readability-3-omni/32-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 실존 방출값 전수 조사 + phrasebook.json 초안 작성</name>
  <files>backend/data/phrasebook.json</files>
  <read_first>
    - app/src/types/analysis.ts:523-534 (DeductionRecord — criterion/ruleId/unit/ipsfAnchor/source 실존 필드. 문구집 키는 이 방출값으로만)
    - backend/shared/python/sunity_shared/analysis/keypoint_frame.py:51-81 (좌표 표시층 8개 — 어깨2·엉덩이2·무릎2·손2. 발목·팔꿈치·손목 좌표 기반 표현은 32-14 확장 전 금지 — Pitfall 1)
    - backend/shared/python/sunity_shared/analysis/skeleton.py:39-62 (각도 측정층 JOINT_ANGLES 8관절 + JOINT_LABEL_KO)
    - docs/research/폴스포츠-지식.md (IPSF 감점 분류 — 회당 −0.2 정렬/라인/신장, −0.5 나쁜 각도/트랜지션, 20° 허용 오차, Flow/Shades — 이유문·심사 언어의 도메인 소스)
    - backend/data/corrective_exercises.json (fixture 구조 선례 + 운동 목록 — 운동 연결 이유 슬롯이 참조할 실존 운동 키)
    - backend/shared/python/sunity_shared/analysis/copy_templates.py:95-107, :247-265 (canned dict 구조 + FORBIDDEN_PHRASES 선례)
  </read_first>
  <action>
    per D-11/D-08/D-13/D-28. (1) 실존 방출값 전수 조사: deduction 엔진이 방출하는 criterion/ruleId 목록을 grep으로 수집(backend/shared/python/sunity_shared/analysis/에서 criterion 문자열 리터럴·CRITERION_GROUPS·angle_vs_reference__ 패턴 등), keypointSet enum, 등록 동작 6종(kip-up/power-spin/peter-pan/elbow-twist-sister/pdshape/sliding-spin 계열 — REGISTERED_MOTIONS 실측) + 미등재 폴백 키를 확정. 조사 결과를 phrasebook.json의 `_meta.keySources`에 기록(키 출처 추적성). (2) phrasebook.json 스키마: 최상위 `{_meta, entries}` — entries는 "동작키.criterion키" 또는 매칭 우선순위(동작×criterion → criterion 공통 → generic 폴백)를 지원하는 구조. 각 entry 5슬롯: statusLine(상태 — 몸 말, 관절명 허용), whyLine(왜 — 감점·위험 이유 1줄, IPSF 심사 언어), cueLine(행동 — 외부 큐, 예: "무릎으로 가슴을 끌어안듯"·"발끝으로 천장을 밀어내듯"), coachQuestion(강사님께 물어볼 완성문), exerciseReason(결함→운동 연결 이유 1줄 — corrective_exercises.json 실존 운동과 연결). (3) 문구 원칙: 수치는 슬롯 텍스트에 넣지 않는다 — 수치는 렌더 시 소형 신뢰 배지가 별도 표시(D-09). % 환산 표현 금지. 톤 = 친숙하되 장난스럽지 않게(D-12), 응원 톤이되 근거 없는 칭찬 금지(D-6). NotebookLM 폴스포츠 노트북을 병용해 동작별 감점 포인트를 교차 확인(notebook-lm-pole-sports). 커버리지: 등록 6동작 × 주요 criterion + generic 폴백 — 실측 방출 빈도가 높은 조합 우선, 전 조합 강제 아님(폴백이 받치므로 fabrication 금지).
  </action>
  <verify>
    <automated>cd backend && python -c "import json; d=json.load(open('data/phrasebook.json')); assert '_meta' in d and 'entries' in d and len(d['entries'])>=10; print('PHRASEBOOK_OK', len(d['entries']))"</automated>
  </verify>
  <acceptance_criteria>
    - phrasebook.json이 유효 JSON이고 entries ≥ 10, 각 entry에 statusLine/whyLine/cueLine/coachQuestion/exerciseReason 5키 존재
    - _meta.keySources에 criterion/ruleId 조사 출처 기록
    - 발목·팔꿈치·손목 "좌표" 기반 시각 표현 요구 문구 0건 (텍스트 큐는 신체 부위 언급 자체는 허용 — 좌표 의존 아님)
    - 전 슬롯 텍스트에 '%', '유사도', '박제' 부재
  </acceptance_criteria>
  <done>실존 키와 대조된 문구집 초안이 5슬롯 구조로 존재</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: phrasebook.py 조립 함수 + 금지어 게이트 + terminologyMap.ts</name>
  <files>backend/shared/python/sunity_shared/analysis/phrasebook.py, backend/tests/phase32/test_phrasebook_forbidden.py, backend/tests/phase32/test_phrasebook_assembly.py, app/src/lib/terminologyMap.ts</files>
  <read_first>
    - backend/shared/python/sunity_shared/analysis/exercise_map.py (정본 아날로그 — lazy 캐시 :29-84, 순수성 docstring :7-17, graceful skip :120-128, fabrication 금지 cap :220-224)
    - backend/tests/phase07/test_copy_templates_no_forbidden.py:72-91 (금지어 게이트 + sanity 테스트 형태)
    - app/src/lib/deductionLabels.ts (용어 맵 전신 — JOINT_LABEL_KO 이중 키 공간 :28-42, ANGLE_MEANING_KO :62-71, cross-side 계약 주석 관례 :108-110)
    - app/src/lib/userAnalyses.ts:60-62 (방어 파싱 원칙 주석 — 참고)
  </read_first>
  <behavior>
    - Test (assembly) 1: 등록 동작+실존 criterion 입력 → 5슬롯 dict 반환 (plain camelCase scalar dict)
    - Test (assembly) 2: 미등재 동작/미등재 criterion → generic 폴백 반환, 크래시 0 (graceful)
    - Test (assembly) 3: 반환 dict에 nested array/dict 없음 (Firestore flat 저장 가능)
    - Test (forbidden) 1: phrasebook.json 전체 string 순회에서 FORBIDDEN 패턴("%일치", "유사도", "박제", 정규식 `\d+\s*%`, 정규식 `\d+(\.\d+)?\s*(도|°)만큼` 류 수치-지시 일반론) 0건
    - Test (forbidden) 2: sanity — 추출 string 수 ≥ 50 (0-string 게이트 무의미 회귀 차단)
  </behavior>
  <action>
    per D-11/D-12/D-09. (1) phrasebook.py — exercise_map 관례 복제: _PHRASEBOOK_PATH(Path 상대 구성)+lazy 캐시, 공개 함수 assemble_phrases(motion_key: str|None, criterion: str, rule_id: str|None) -> dict (5슬롯 camelCase: statusLine/whyLine/cueLine/coachQuestion/exerciseReason). 매칭 우선순위: 동작×criterion → criterion 공통 → generic. 순수성 docstring(boto3/네트워크 0, 3-way lockstep 언급) 복제. FORBIDDEN_PHRASES_PHRASEBOOK 튜플을 이 모듈에 정의(copy_templates 확장이 아니라 신규 모듈 소유 — 파일 충돌 회피). (2) 금지어 게이트 테스트 — JSON 전체 string 순회(iterate) + parametrize + sanity. (3) terminologyMap.ts — deductionLabels.ts 관례로 신설: DIMENSION_JUDGE_KO(예: 'stability'→심사 언어 기반 사람 말, 'flow'/'angle' 등 차원·지표 용어 전부), 기존 라벨의 교체가 아니라 상위 매핑. cross-side 계약 주석으로 "백엔드 phrasebook.py 심사 용어와 정합" 명시. 톤 = 친숙하되 장난스럽지 않게. 실제 라벨 문자열 확정은 Task 3 belle 감수에서 조정될 수 있음을 주석으로.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/phase32/test_phrasebook_forbidden.py tests/phase32/test_phrasebook_assembly.py -x -q && cd ../app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - phrasebook.py에 def assemble_phrases( 존재, boto3/requests import 0
    - 금지어 게이트 + sanity 테스트 green
    - terminologyMap.ts가 export const 단일 출처 맵 + cross-side 주석 포함, typecheck clean
    - assemble_phrases 반환이 flat scalar dict (assembly Test 3 green)
  </acceptance_criteria>
  <done>조립 순수 함수·금지어 게이트·용어 맵이 테스트와 함께 성립</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: belle 문구 감수 — 문구집 + 용어 맵 승인 (D-11/D-12)</name>
  <what-built>동작×결함 고정 문구집(5슬롯 × 전 entry)과 용어 맵(측정 용어→심사 언어)</what-built>
  <how-to-verify>
    1. Claude가 요약 제시: 동작별 대표 entry 3~5개(상태→왜→행동 3단이 실제 카드에서 읽히는 형태로), 용어 맵 전량(항목 수 적음), 폴백 문구
    2. 전량은 backend/data/phrasebook.json에서 확인 가능 (경로 안내)
    3. 판단 기준: ① 톤 — 친숙하되 장난스럽지 않게 ② 행동문이 일반론("무릎을 더 펴세요")이 아니라 외부 큐인가 ③ 심사 언어가 실제 IPSF 기준과 맞는가 ④ 코치 질문이 강사에게 그대로 보여줄 수 있는 완성문인가
    5. 수정 지시는 항목 단위로 — Claude가 반영 후 금지어 게이트 재실행
  </how-to-verify>
  <action>문구집 대표 entry 3~5개(3단이 카드에서 읽히는 형태)와 용어 맵 전량, 폴백 문구를 belle에게 제시하고 감수를 받는다. 수정 지시는 항목 단위로 반영 후 금지어 게이트를 재실행하고, 승인 결과를 32-GATE-DECISIONS.md에 1줄 기록한다.</action>
  <verify>
    <automated>cd backend && python -m pytest tests/phase32/test_phrasebook_forbidden.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - belle 승인 기록 (SUMMARY + 32-GATE-DECISIONS.md 문구 감수 1줄)
    - 수정 반영분 포함 금지어 게이트 green
  </acceptance_criteria>
  <done>문구집·용어 맵이 belle 승인 상태로 확정됨 (D-11 승인 선출시 충족)</done>
  <resume-signal>"승인" 또는 항목별 수정 지시 (수정 반영 후 재감수 또는 승인으로 종료)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 문구집 fixture → 사용자 화면 | 고정 문구가 코칭 문장으로 그대로 노출 |
| LLM(Cerebras) → 문구 가변부 | 32-09에서 배선 — 이 플랜은 골격만 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-10 | Tampering(신뢰) | 문구집 내용 | mitigate | 금지어 grep 게이트(D-09 패턴+일반론) + sanity + belle 승인 선출시(D-11) — 비측정 주장·자의적 수치 생성 경로 차단 |
| T-32-11 | Info Disclosure | 문구 내 PII | mitigate | 문구집은 측정 수치·동작명·신체 부위만 — 개인 식별 정보 슬롯 자체가 없음 (acceptance의 5슬롯 고정 스키마) |
</threat_model>

<verification>
- cd backend && python -m pytest tests/phase32 -x -q green
- cd app && npm run typecheck clean
- belle 승인 기록 (SUMMARY + 32-GATE-DECISIONS.md에 감수 결과 1줄)
</verification>

<success_criteria>
- 문구집·용어 맵이 실존 키 대조 + 금지어 게이트 + belle 승인의 3중 보증으로 성립 — 32-09 방출·32-10 렌더의 데이터 원천 확보
</success_criteria>

<output>
Create `.planning/phases/32-result-readability-3-omni/32-05-SUMMARY.md` when done
</output>
