---
phase: 32-result-readability-3-omni
plan: 06
type: execute
wave: 3
depends_on: ["32-01", "32-05"]
files_modified:
  - backend/shared/python/sunity_shared/analysis/mission.py
  - backend/shared/python/sunity_shared/models.py
  - backend/shared/python/sunity_shared/firestore_admin.py
  - docs/contract.md
  - app/src/types/analysis.ts
  - app/src/lib/userAnalyses.ts
  - backend/tests/phase32/test_mission_rules.py
  - backend/tests/phase32/test_mission_contract_lockstep.py
autonomous: true
model: fable
requirements: [D-19, D-26, D-27, D-28, D-08, D-29, D-14]
must_haves:
  truths:
    - "미션이 ①위험 결함(안전 안내 — 게임·에스컬레이션 제외) > ②반복 미개선(faultKey 동일성) > ③감점 최대 우선순위의 순수 함수로 선정된다 (D-19)"
    - "동일 결함 판별이 criterion 단독이 아니라 안정 faultKey(motionId+ruleId+criterion — 좌우 관절 구분)로 이뤄진다 (리뷰 blocker 1)"
    - "미션에 baseline 측정값(baselinePoints/baselineDeviation/targetValue/unit/motionId)이 저장되어 다음 분석이 개선량을 계산할 수 있다 (D-26 계산 가능성 — 리뷰 blocker 1)"
    - "prev.mission의 motionId가 현재 분석과 다르면 미션 체인이 리셋된다 — 다른 동작 간 streak 오염 0 (리뷰 반영)"
    - "missionOutcome이 수치 필드만 담는다(improved/deltaPoints/deltaDeviation 등) — 사람 문장은 phrasebook 조립(32-09)·앱 소비층 소관 (리뷰 반영)"
    - "isSafety=true 미션은 streak 1 고정 + escalation 'none' 강제 — D-14(미션화·게임 금지)와 D-19 ①(안전 안내 우선)의 정합 해석이 코드로 강제된다"
    - "DeductionRecord에 안정 recordId가 계약으로 정의된다 — 정렬·필터·숨김에 안전한 조인 키 (리뷰 blocker 5 계약부)"
    - "summaryPraise(잘한 점 후보 — 백엔드 산출)가 계약 3면에 정의된다 — 앱·스팟체크가 같은 원천을 소비 (리뷰 blocker 5 계약부)"
    - "코치 질문 자동 등재 목록이 recordId 기반으로 방출 스키마에 있다 (D-28/D-29)"
    - "감점 카드 3단 문구 필드(statusLine/whyLine/cueLine/coachQuestion/exerciseId/exerciseReason)와 tolerance(게이지 재료)가 계약 3면에 정의된다 (D-08/D-10)"
    - "신규 필드 전부 flat scalar(coachQuestions/verdicts류 list-of-scalar-dict는 safetyFlags 선례로 허용) — nested array 0, complete_analysis 신규 kwarg 0 ('result 안으로 흐른다')"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/mission.py"
      provides: "select_mission/derive_mission_outcome/faultKey 판정 순수 함수"
      exports: ["select_mission", "derive_mission_outcome", "build_fault_key"]
    - path: "backend/tests/phase32/test_mission_rules.py"
      provides: "우선순위·faultKey·streak·에스컬레이션·outcome 수치 단위 테스트"
  key_links:
    - from: "backend/shared/python/sunity_shared/models.py"
      to: "app/src/types/analysis.ts + docs/contract.md"
      via: "3-way lockstep (MISSION_KEYS 블록 + interface + 계약 절)"
      pattern: "MISSION_KEYS"
    - from: "backend/shared/python/sunity_shared/firestore_admin.py"
      to: "result.mission / result.missionOutcome / result.summaryPraise / records 확장 키"
      via: "scoped validator (화이트리스트+enum+finite+상한)"
      pattern: "_validate_mission"
---

<objective>
미션 루프의 엔진(선정 규칙·faultKey·streak 체인·에스컬레이션·수치 outcome)과 phase 32 신규 필드의 계약 3면(contract.md + analysis.ts + models.py) + scoped validator + 앱 normalize를 한 번에 정의한다. 파이프라인 배선은 32-09가 담당 — 이 플랜은 순수 함수와 계약만.

리뷰 blocker 1 반영: 이전 미션에 baseline 측정값이 없으면 개선량을 계산할 수 없고, criterion 단독 판별은 좌우·동작을 병합한다 — faultKey + baseline 필드로 재설계. blocker 5 계약부 반영: recordId(안정 조인 키)와 summaryPraise(백엔드 산출 잘한 점 후보)를 이 웨이브에서 계약에 올려 32-09가 방출하고 32-13이 교차검증할 수 있게 한다.

Purpose: 미션→연습→확인 루프(D-19/D-26/D-27)와 번역 레이어 방출(D-08)의 데이터 계약 확립. 계약 파일은 이 플랜이 이 웨이브에서 독점 소유.
Output: mission.py + 계약 3면 확장 + validator + normalize + 테스트 2벌.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-result-readability-3-omni/32-CONTEXT.md
@.planning/phases/32-result-readability-3-omni/32-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: mission.py 순수 함수 — faultKey·선정·streak·에스컬레이션·수치 outcome (D-19/D-26/D-27/D-14)</name>
  <files>backend/shared/python/sunity_shared/analysis/mission.py, backend/tests/phase32/test_mission_rules.py</files>
  <read_first>
    - backend/shared/python/sunity_shared/analysis/exercise_map.py:141-224 (순수 함수 형태 정본 — plain dict 입출력, ordered 병합, dedup, cap, fabrication 금지)
    - backend/shared/python/sunity_shared/firestore_admin.py:1532-1561 (get_previous_analysis — 같은 mode만 in-memory 필터, motion 미필터 [실측 확인됨]. 이 플랜은 호출하지 않고 prev dict를 인자로 받는 설계 — motion 가드는 순수 함수 안에서)
    - app/src/types/analysis.ts:523-534 (DeductionRecord 실존 필드 — criterion/ruleId/points/measured/target/unit이 선정·baseline 입력)
    - .planning/phases/32-result-readability-3-omni/32-RESEARCH.md §미션 이력 스키마 (권장 구조 — streak 전파, N개 문서 쿼리 없이 직전 1건)
  </read_first>
  <behavior>
    - Test 1 (faultKey): build_fault_key(motion_id, rule_id, criterion) — 'kip-up::ipsf_absolute::angle_vs_reference__left_knee' 형태 결정적 문자열. 좌/우 관절 criterion이 서로 다른 faultKey를 생성 (left_knee ≠ right_knee)
    - Test 2 (우선순위 ① — D-14 정합): safetyFlags 존재 시 mission.selectedBy == 'safety' + isSafety == true + **streak == 1 고정 + escalation == 'none' 강제** — 안전은 안내이지 게임·에스컬레이션 대상이 아님 (D-14 미션화 금지 + D-19 ① 안전 안내 우선의 정합 해석)
    - Test 3 (우선순위 ②): 안전 없음 + prev_mission.faultKey와 동일 faultKey 결함이 이번에도 잔존 → selectedBy == 'repeat', streak == prev.streak + 1. 단 prev_mission.motionId != 현재 motion_id면 prev 무시 → streak == 1 (다른 동작 체인 오염 차단)
    - Test 4 (우선순위 ③): 안전·반복 없음 → 감점 최대(records의 |points| 최대) 결함, selectedBy == 'max_deduction', streak == 1
    - Test 5 (baseline 저장): 선정된 미션 dict에 baselinePoints(선정 record의 |points|), baselineDeviation(|measured−target|, 둘 다 있을 때만 아니면 None), targetValue, unit, motionId 포함
    - Test 6 (에스컬레이션): streak == 2 → escalation == 'exercise_detour', streak >= 3 → escalation == 'coach_card' (D-27) — isSafety=true는 항상 'none'
    - Test 7 (outcome 수치): prev_mission의 faultKey 결함이 이번 records에서 소멸 → missionOutcome.improved == true + currentPoints == 0 + deltaPoints == baselinePoints. 잔존하되 |points| 감소 → improved == true + delta 수치 정확. 증가/동일 → improved == false. prev 없음/mode1/motionId 불일치 → None. 반환에 사람 문장 필드 없음 (수치·키만)
    - Test 8 (flat): 반환 dict 전부 flat scalar (nested array/dict 0) + camelCase 키
  </behavior>
  <action>
    per D-19/D-26/D-27/D-14. mission.py 신설 — exercise_map 순수성 관례(boto3/네트워크 0, plain dict 입출력, docstring에 D-19 우선순위와 32-CONTEXT 인용 + D-14 정합 해석 각주: "안전은 미션 선정 1순위 자리를 차지하되 게임·streak·에스컬레이션에서 제외 — 안내와 코치 유도만"). 공개 함수 3개: (1) build_fault_key(motion_id: str|None, rule_id: str|None, criterion: str) -> str — f"{motion_id or 'unknown'}::{rule_id or 'na'}::{criterion}" 결정적 조합(criterion이 관절·좌우를 내장하는 angle_vs_reference__{joint} 형태라 좌우 구분이 faultKey에 승계됨 — 리뷰 blocker 1). (2) select_mission(deduction_records: list[dict], safety_flags: list[dict] | None, prev_mission: dict | None, motion_id: str | None) -> dict — 반환 {faultKey, criterion, ruleId, recordId(선정 record의 recordId — 부재 시 None), selectedBy('safety'|'repeat'|'max_deduction'), streak(int), isSafety(bool), escalation('none'|'exercise_detour'|'coach_card'), motionId, baselinePoints(float), baselineDeviation(float|None), targetValue(float|None), unit(str|None)}. prev_mission 사용 전 motionId 가드: prev_mission.get("motionId") != motion_id면 prev 무시(streak 리셋 — get_previous_analysis가 motion을 필터하지 않는 실측 사실의 순수 함수 측 방어). '미개선' 판정 = prev_mission.faultKey와 동일 faultKey record가 current에 잔존. isSafety=true면 streak=1·escalation='none' 강제. cueText는 phrasebook이 소유하므로 여기 넣지 않음(32-09 조립 시 결합). (3) derive_mission_outcome(prev_mission: dict | None, current_records: list[dict], mode: str, motion_id: str | None) -> dict | None — 반환 {improved(bool), faultKey, criterion, baselinePoints, currentPoints(잔존 시 |points|, 소멸 시 0), deltaPoints(baseline−current), baselineDeviation(float|None), currentDeviation(float|None), deltaDeviation(float|None)} — 전부 수치·키. improved = 소멸 or currentPoints < baselinePoints − 0.01(부동소수 여유). mode != mode3(self) 또는 prev 없음/motionId 불일치 → None. **사람 문장(deltaSummary 류)은 생성하지 않는다** — 문장은 32-09가 phrasebook·summaryPraise 조립에서, 앱이 렌더에서 만든다(계산/카피 책임 분리 — 리뷰 반영).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/phase32/test_mission_rules.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - mission.py에 def build_fault_key( / def select_mission( / def derive_mission_outcome( 존재, import에 boto3/firestore 부재
    - behavior 8건 전부 green
    - 반환 키 전부 camelCase, nested 컨테이너 0 (Test 8)
    - isSafety=true 경로에서 escalation 'none'·streak 1 강제 (Test 2 green)
    - 사람 문장 생성 코드 0 — 반환 dict에 Summary/문장 계열 문자열 조립 없음 (수치·enum·키만)
  </acceptance_criteria>
  <done>faultKey 기반 미션 선정·streak·에스컬레이션·수치 outcome이 순수 함수 + 테스트로 성립 — 개선량 계산 가능</done>
</task>

<task type="auto">
  <name>Task 2: 계약 3면 확장 + scoped validator + 앱 normalize (D-08/D-28/D-29 + recordId/summaryPraise/tolerance)</name>
  <files>backend/shared/python/sunity_shared/models.py, docs/contract.md, app/src/types/analysis.ts, app/src/lib/userAnalyses.ts, backend/shared/python/sunity_shared/firestore_admin.py, backend/tests/phase32/test_mission_contract_lockstep.py</files>
  <read_first>
    - backend/shared/python/sunity_shared/models.py:185-198 (MOTION_ALIGNMENT_KEYS 블록 — 신규 상수 블록이 복제할 형태 + lockstep 주석)
    - app/src/types/analysis.ts:489-508 (MotionAlignment interface — 하위호환 서술 관례 "(b) 부재(legacy doc) = 현행 동작")
    - backend/shared/python/sunity_shared/firestore_admin.py:288-407 (_validate_safety_flags list[dict] 라우팅 + _validate_motion_alignment 화이트리스트/enum/finite 뼈대 — "result 안으로 흐른다, complete_analysis 신규 kwarg 없음")
    - app/src/lib/userAnalyses.ts:63-94 (normalize 방어 파싱 3종 — enum 강등/키 prefix/정수 검증. "임의 값은 undefined 강등, 필드 drop 없음" 원칙)
    - docs/contract.md (§11 motionAlignment 절 — 신규 절 서술 모범)
  </read_first>
  <action>
    per D-08/D-19/D-26/D-27/D-28/D-29 + 리뷰 blocker 1·5 계약부. 계약 3면 + validator + normalize를 단일 커밋으로(atomic — 계약 3면 동시 수정 원칙). (1) models.py 상수 블록(각각 3-way lockstep 주석): MISSION_KEYS = ("faultKey","criterion","ruleId","recordId","selectedBy","streak","isSafety","escalation","motionId","baselinePoints","baselineDeviation","targetValue","unit"), MISSION_SELECTED_BY = ("safety","repeat","max_deduction"), MISSION_ESCALATIONS = ("none","exercise_detour","coach_card"), MISSION_OUTCOME_KEYS = ("improved","faultKey","criterion","baselinePoints","currentPoints","deltaPoints","baselineDeviation","currentDeviation","deltaDeviation"), DEDUCTION_PHRASE_KEYS = ("statusLine","whyLine","cueLine","coachQuestion","exerciseId","exerciseReason"), SUMMARY_PRAISE_KEYS = ("source","headline","evidenceValue","evidenceUnit"), SUMMARY_PRAISE_SOURCES = ("mission_improved","clean_dimension","criteria_met"), COACH_QUESTION_SOURCES = ("safety","mission_stuck","unmeasured","user"). (2) analysis.ts: Mission/MissionOutcome/SummaryPraise interface + DeductionRecord에 optional 키 — recordId?: string("방출 시 1회 각인되는 안정 조인 키 — 형식 'r{index:02d}:{criterion}', 정렬·필터·숨김에 index 대신 사용. 부재=legacy doc" 주석), statusLine?/whyLine?/cueLine?/coachQuestion?/exerciseId?/exerciseReason?(전부 string), tolerance?: number("규칙 상수 유래 허용 오차 — 게이지 스케일 재료, 자의적 수치 아님" 주석) + coachQuestions?: {text: string; source: 'safety'|'mission_stuck'|'unmeasured'|'user'; recordId?: string}[] — 주의: coachQuestions는 list[dict-of-scalars]라 Firestore 허용(nested array 아님, safetyFlags 선례). AnalysisDoc.result에 mission?/missionOutcome?/summaryPraise?/coachQuestions? 추가. (3) contract.md: §신설 — 미션 루프(faultKey·baseline 의미)·3단 문구·recordId 조인 규칙·summaryPraise(잘한 점 후보의 단일 원천 = 백엔드, 앱은 소비+legacy 폴백)·코치 질문 필드 정의(방출은 32-09부터임을 명시). (4) firestore_admin.py: _validate_mission(payload, *, path="mission") — None graceful, dict 강제, MISSION_KEYS 화이트리스트, selectedBy/escalation enum, streak int 1..99, baselinePoints finite ≥ 0, motionId/unit str|None. _validate_mission_outcome 동형(수치 finite). _validate_summary_praise(source enum, headline 비어있지 않은 str ≤ 200자, evidenceValue finite|None). coachQuestions는 _validate_safety_flags 형태(각 항목 scalar-only dict + source enum + text ≤ 200자, 항목 수 ≤ 10 — 리뷰 상한 반영). **source='user'는 클라이언트 로컬 전용(사용자 담기 — 백엔드 방출·validator에 도달하지 않음)** — 이 경계를 contract.md 절과 analysis.ts 주석에 1줄씩 명시. records 확장 키(recordId/문구 5종/tolerance)는 기존 record 검증 경로가 화이트리스트형이면 키 추가, dict-only-scalars형이면 자동 통과 — 실제 코드를 읽고 최소 침습으로. complete_analysis 시그니처 무변경(SP-1 — result 안으로 흐른다). (5) userAnalyses.ts normalize: mission/missionOutcome/summaryPraise/coachQuestions 방어 파싱(enum 강등·정수/유한 검증·문자열 검증 — 기존 3종 헬퍼 관례), 부재 시 undefined(legacy). records의 recordId/문구/tolerance는 통과 파싱(string/finite 검증). (6) lockstep 테스트: models.py 상수 ↔ analysis.ts 문자열 존재 대조(기존 lockstep 테스트 패턴 — 예: test_motion_alignment_contract.py 형태를 읽고 복제).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/phase32/test_mission_contract_lockstep.py -x -q && cd ../app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - models.py에 MISSION_KEYS/MISSION_SELECTED_BY/MISSION_ESCALATIONS/MISSION_OUTCOME_KEYS/DEDUCTION_PHRASE_KEYS/SUMMARY_PRAISE_KEYS/SUMMARY_PRAISE_SOURCES/COACH_QUESTION_SOURCES 존재
    - analysis.ts에 interface Mission/MissionOutcome/SummaryPraise + DeductionRecord optional recordId/문구/tolerance + coachQuestions(recordId 기반) 존재, typecheck clean
    - contract.md에 신규 절 존재 (grep "recordId" docs/contract.md ≥ 1 + grep "summaryPraise" ≥ 1)
    - firestore_admin.py에 _validate_mission( + _validate_summary_praise( 존재 + streak/문항수/문자열 상한 검사 + complete_analysis 시그니처 diff 0 (kwarg 추가 없음)
    - lockstep 테스트 green + 기존 firestore_admin 테스트 무회귀
  </acceptance_criteria>
  <done>phase 32 신규 필드의 계약 3면·validator·normalize가 atomic하게 성립 — 방출(32-09)·소비(32-07/32-10/32-11)·교차검증(32-13) 준비 완료</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 백엔드 → Firestore doc | 신규 필드(mission/outcome/praise/questions/3단 문구) 영속 |
| Firestore doc → 앱 | normalize가 신뢰 경계의 방어선 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-12 | Tampering | 신규 필드 쓰기 | mitigate | scoped validator 화이트리스트+enum+finite+상한(streak 99·문항 10·문자열 200) — motionAlignment 선례 뼈대 복제, 클라이언트 직접 쓰기는 firestore.rules default-deny로 기차단 |
| T-32-13 | Elevation | complete_analysis 표면 확장 | mitigate | 신규 kwarg 0 — "result 안으로 흐른다" 강제 (acceptance에 시그니처 diff 0) |
| T-32-14 | DoS(크래시) | legacy doc 소비 | mitigate | 앱 normalize 부재=undefined 강등 + 필드 drop 없음 — 구 doc 하위호환 |
</threat_model>

<verification>
- cd backend && python -m pytest tests/phase32 -x -q green
- cd app && npm run typecheck clean
- 기존 백엔드 테스트 baseline 초과 실패 0
</verification>

<success_criteria>
- D-19/D-26/D-27 미션 엔진이 faultKey·baseline·수치 outcome으로 개선량 계산 가능하게 검증 완료 (리뷰 blocker 1 해소)
- D-14/D-19 정합(안전 = 안내 우선·게임 제외)이 코드 강제로 성립
- recordId·summaryPraise·tolerance가 계약에 올라 32-09 방출·32-13 교차검증의 단일 원천 확보 (리뷰 blocker 5 계약부 해소)
</success_criteria>

<output>
Create `.planning/phases/32-result-readability-3-omni/32-06-SUMMARY.md` when done
</output>
