---
phase: 32-result-readability-3-omni
plan: 06
type: execute
wave: 3
depends_on: ["32-01", "32-05"]
files_modified:
  - backend/shared/python/sunity_shared/analysis/mission.py
  - backend/shared/python/sunity_shared/models.py
  - backend/shared/python/sunity_shared/firestore_admin.py
  - docs/contract.md
  - app/src/types/analysis.ts
  - app/src/lib/userAnalyses.ts
  - backend/tests/phase32/test_mission_rules.py
  - backend/tests/phase32/test_mission_contract_lockstep.py
autonomous: true
model: fable
requirements: [D-19, D-26, D-27, D-28, D-08, D-29]
must_haves:
  truths:
    - "미션이 ①위험 결함 > ②반복 미개선(mode3 이력) > ③감점 최대 우선순위의 순수 함수로 선정된다 (D-19)"
    - "streak가 문서 내 체인(직전 doc의 mission 참조)으로 전파되어 2회차/3회차 에스컬레이션이 직전 1건 조회만으로 판정된다 (D-27)"
    - "missionOutcome(지난 미션 개선 여부+델타 요약)이 산출되어 mode3 잘한 점 1순위 소스가 된다 (D-26)"
    - "코치 질문 자동 등재 목록(위험 결함 항상 + 3회 미개선 + 못 잰 것)이 방출 스키마에 있다 (D-28/D-29)"
    - "감점 카드 3단 문구 필드(statusLine/whyLine/cueLine/coachQuestion/exerciseReason)가 계약 3면에 정의된다 (D-08)"
    - "신규 필드 전부 flat scalar — nested array 0, complete_analysis 신규 kwarg 0 ('result 안으로 흐른다')"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/mission.py"
      provides: "select_mission/derive_mission_outcome/escalation 판정 순수 함수"
      exports: ["select_mission", "derive_mission_outcome"]
    - path: "backend/tests/phase32/test_mission_rules.py"
      provides: "우선순위·streak·에스컬레이션 단위 테스트"
  key_links:
    - from: "backend/shared/python/sunity_shared/models.py"
      to: "app/src/types/analysis.ts + docs/contract.md"
      via: "3-way lockstep (MISSION_KEYS 블록 + interface + 계약 절)"
      pattern: "MISSION_KEYS"
    - from: "backend/shared/python/sunity_shared/firestore_admin.py"
      to: "result.mission / result.missionOutcome / records 확장 키"
      via: "scoped validator (화이트리스트+enum+finite)"
      pattern: "_validate_mission"
---

<objective>
미션 루프의 엔진(선정 규칙·streak 체인·에스컬레이션·outcome)과 phase 32 신규 필드의 계약 3면(contract.md + analysis.ts + models.py) + scoped validator + 앱 normalize를 한 번에 정의한다. 파이프라인 배선은 32-09가 담당 — 이 플랜은 순수 함수와 계약만.

Purpose: 미션→연습→확인 루프(D-19/D-26/D-27)와 번역 레이어 방출(D-08)의 데이터 계약 확립. 계약 파일은 이 플랜이 이 웨이브에서 독점 소유.
Output: mission.py + 계약 3면 확장 + validator + normalize + 테스트 2벌.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-result-readability-3-omni/32-CONTEXT.md
@.planning/phases/32-result-readability-3-omni/32-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: mission.py 순수 함수 — 선정·streak·에스컬레이션·outcome (D-19/D-26/D-27)</name>
  <files>backend/shared/python/sunity_shared/analysis/mission.py, backend/tests/phase32/test_mission_rules.py</files>
  <read_first>
    - backend/shared/python/sunity_shared/analysis/exercise_map.py:141-224 (순수 함수 형태 정본 — plain dict 입출력, ordered 병합, dedup, cap, fabrication 금지)
    - backend/shared/python/sunity_shared/firestore_admin.py:1532-1561 (get_previous_analysis — prev chain 읽기. 이 플랜은 호출하지 않고 prev dict를 인자로 받는 설계)
    - app/src/types/analysis.ts:523-534 (DeductionRecord 실존 필드 — criterion/ruleId/points가 선정 입력)
    - .planning/phases/32-result-readability-3-omni/32-RESEARCH.md §미션 이력 스키마 (권장 구조 — streak 전파, N개 문서 쿼리 없이 직전 1건)
  </read_first>
  <behavior>
    - Test 1 (우선순위 ①): safetyFlags 존재 시 mission.selectedBy == 'safety', 해당 결함이 미션. 단 D-14에 따라 안전 미션은 게임 프레임 제외 표식(isSafety: true)
    - Test 2 (우선순위 ②): 안전 없음 + prev.mission과 동일 결함(criterion 일치)이 이번에도 미개선 → selectedBy == 'repeat', streak == prev.streak + 1
    - Test 3 (우선순위 ③): 안전·반복 없음 → 감점 최대(records의 |points| 최대) 결함, selectedBy == 'max_deduction', streak == 1
    - Test 4 (에스컬레이션): streak == 2 → escalation == 'exercise_detour' (보완 운동 우회 제안), streak >= 3 → escalation == 'coach_card' (코치 카드 전면 승격 — D-27)
    - Test 5 (outcome): prev.mission의 결함이 이번 분석에서 개선(해당 criterion 감점 소멸 또는 편차 감소) → missionOutcome.improved == true + deltaSummary 산출. prev 없음/mode1 → missionOutcome None
    - Test 6 (flat): 반환 dict 전부 flat scalar (nested array/dict 0) + camelCase 키
  </behavior>
  <action>
    per D-19/D-26/D-27. mission.py 신설 — exercise_map 순수성 관례(boto3/네트워크 0, plain dict 입출력, docstring에 D-19 우선순위와 32-CONTEXT 인용). 공개 함수 2개: (1) select_mission(deduction_records: list[dict], safety_flags: list[dict] | None, prev_mission: dict | None, prev_improved: bool | None) -> dict — 반환 {faultKey, criterion, ruleId, selectedBy('safety'|'repeat'|'max_deduction'), streak(int), isSafety(bool), escalation('none'|'exercise_detour'|'coach_card')}. cueText는 phrasebook이 소유하므로 여기 넣지 않음(32-09 조립 시 결합). (2) derive_mission_outcome(prev_mission: dict | None, current_records: list[dict], mode: str) -> dict | None — {improved(bool), criterion, deltaSummary(str — 사람 말 요약은 32-09에서 phrasebook과 결합하므로 여기서는 측정 요약만)}. "미개선" 판정 = prev_mission.criterion이 current_records에 여전히 존재(감점 잔존). 개선 = 소멸. 편차 감소 세부 판정은 records의 points 절대값 비교로 단순화(v1 — 과설계 금지). 안전 미션은 streak 1 고정(안전은 반복 카운트 대상이 아니라 항상 최우선 — D-14 미션화 금지와 정합: isSafety=true 미션은 렌더 시 게임 프레임 제외).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/phase32/test_mission_rules.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - mission.py에 def select_mission( / def derive_mission_outcome( 존재, import에 boto3/firestore 부재
    - behavior 6건 전부 green
    - 반환 키 전부 camelCase, nested 컨테이너 0 (Test 6)
  </acceptance_criteria>
  <done>미션 선정·streak·에스컬레이션·outcome이 순수 함수 + 테스트로 성립</done>
</task>

<task type="auto">
  <name>Task 2: 계약 3면 확장 + scoped validator + 앱 normalize (D-08/D-28/D-29 스키마)</name>
  <files>backend/shared/python/sunity_shared/models.py, docs/contract.md, app/src/types/analysis.ts, app/src/lib/userAnalyses.ts, backend/shared/python/sunity_shared/firestore_admin.py, backend/tests/phase32/test_mission_contract_lockstep.py</files>
  <read_first>
    - backend/shared/python/sunity_shared/models.py:185-198 (MOTION_ALIGNMENT_KEYS 블록 — 신규 상수 블록이 복제할 형태 + lockstep 주석)
    - app/src/types/analysis.ts:489-508 (MotionAlignment interface — 하위호환 서술 관례 "(b) 부재(legacy doc) = 현행 동작")
    - backend/shared/python/sunity_shared/firestore_admin.py:288-407 (_validate_safety_flags list[dict] 라우팅 + _validate_motion_alignment 화이트리스트/enum/finite 뼈대 — "result 안으로 흐른다, complete_analysis 신규 kwarg 없음")
    - app/src/lib/userAnalyses.ts:63-94 (normalize 방어 파싱 3종 — enum 강등/키 prefix/정수 검증. "임의 값은 undefined 강등, 필드 drop 없음" 원칙)
    - docs/contract.md (§11 motionAlignment 절 — 신규 절 서술 모범)
  </read_first>
  <action>
    per D-08/D-19/D-26/D-27/D-28/D-29. 계약 3면 + validator + normalize를 단일 커밋으로(atomic — 계약 3면 동시 수정 원칙). (1) models.py: MISSION_KEYS = ("faultKey","criterion","ruleId","selectedBy","streak","isSafety","escalation"), MISSION_SELECTED_BY = ("safety","repeat","max_deduction"), MISSION_ESCALATIONS = ("none","exercise_detour","coach_card"), MISSION_OUTCOME_KEYS = ("improved","criterion","deltaSummary"), DEDUCTION_PHRASE_KEYS = ("statusLine","whyLine","cueLine","coachQuestion","exerciseReason"), COACH_QUESTION_SOURCES = ("safety","mission_stuck","unmeasured","user") — 각 블록에 3-way lockstep 주석. (2) analysis.ts: Mission/MissionOutcome interface + DeductionRecord에 optional 5키(statusLine?/whyLine?/cueLine?/coachQuestion?/exerciseReason? — 전부 string, 부재=legacy doc 하위호환 서술) + coachQuestions?: {text: string; source: 'safety'|'mission_stuck'|'unmeasured'|'user'; recordIndex?: number}[] — 주의: coachQuestions는 list[dict-of-scalars]라 Firestore 허용(nested array 아님, safetyFlags 선례). AnalysisDoc.result에 mission?/missionOutcome?/coachQuestions? 추가. (3) contract.md: §신설 — 미션 루프·3단 문구·코치 질문 필드 정의(방출은 32-09부터임을 명시). (4) firestore_admin.py: _validate_mission(payload, *, path="mission") — None graceful, dict 강제, MISSION_KEYS 화이트리스트, selectedBy/escalation enum, streak int≥1 finite. _validate_mission_outcome 동형. coachQuestions는 _validate_safety_flags 형태(각 항목 _validate_dict_only_scalars 라우팅 + source enum 검사). records 확장 5키는 기존 record 검증 경로가 화이트리스트형이면 키 추가, dict-only-scalars형이면 자동 통과 — 실제 코드를 읽고 최소 침습으로. complete_analysis 시그니처 무변경(SP-1 — result 안으로 흐른다). (5) userAnalyses.ts normalize: mission/missionOutcome/coachQuestions 방어 파싱(enum 강등·정수 검증·문자열 검증 — 기존 3종 헬퍼 관례), 부재 시 undefined(legacy). (6) lockstep 테스트: models.py 상수 ↔ analysis.ts 문자열 존재 대조(기존 lockstep 테스트 패턴 — 예: test_motion_alignment_contract.py 형태를 읽고 복제).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/phase32/test_mission_contract_lockstep.py -x -q && cd ../app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - models.py에 MISSION_KEYS/MISSION_SELECTED_BY/MISSION_ESCALATIONS/DEDUCTION_PHRASE_KEYS/COACH_QUESTION_SOURCES 존재
    - analysis.ts에 interface Mission/MissionOutcome + DeductionRecord optional 5키 + coachQuestions 존재, typecheck clean
    - contract.md에 신규 절 존재 (grep "mission" docs/contract.md ≥ 1)
    - firestore_admin.py에 _validate_mission( 존재 + complete_analysis 시그니처 diff 0 (kwarg 추가 없음)
    - lockstep 테스트 green + 기존 firestore_admin 테스트 무회귀
  </acceptance_criteria>
  <done>phase 32 신규 필드의 계약 3면·validator·normalize가 atomic하게 성립, 방출(32-09) 준비 완료</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 백엔드 → Firestore doc | 신규 필드(mission/outcome/questions/3단 문구) 영속 |
| Firestore doc → 앱 | normalize가 신뢰 경계의 방어선 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-12 | Tampering | 신규 필드 쓰기 | mitigate | scoped validator 화이트리스트+enum+finite (V5 — motionAlignment 선례 뼈대 복제), 클라이언트 직접 쓰기는 firestore.rules default-deny로 기차단 |
| T-32-13 | Elevation | complete_analysis 표면 확장 | mitigate | 신규 kwarg 0 — "result 안으로 흐른다" 강제 (acceptance에 시그니처 diff 0) |
| T-32-14 | DoS(크래시) | legacy doc 소비 | mitigate | 앱 normalize 부재=undefined 강등 + 필드 drop 없음 — 구 doc 하위호환 |
</threat_model>

<verification>
- cd backend && python -m pytest tests/phase32 -x -q green
- cd app && npm run typecheck clean
- 기존 백엔드 테스트 baseline 초과 실패 0
</verification>

<success_criteria>
- D-19/D-26/D-27 미션 엔진이 순수 함수로 검증 완료
- 신규 필드 계약이 3면 atomic + validator + normalize로 성립 — 32-09(방출)·32-07/32-10/32-11(소비)의 계약 기반 확보
</success_criteria>

<output>
Create `.planning/phases/32-result-readability-3-omni/32-06-SUMMARY.md` when done
</output>
