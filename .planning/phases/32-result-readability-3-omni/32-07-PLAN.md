---
phase: 32-result-readability-3-omni
plan: 07
type: execute
wave: 3
depends_on: ["32-04"]
files_modified:
  - app/src/theme/typography.ts
  - app/src/components/SummaryCard.tsx
  - app/src/components/ResultCoachmarks.tsx
  - app/src/lib/coachmark.ts
  - app/src/lib/summarySource.ts
  - app/src/lib/__tests__/summarySource.test.ts
autonomous: true
requirements: [D-01, D-05, D-06, D-07, D-09, D-26]
must_haves:
  truths:
    - "요약 카드 1장 컴포넌트가 잘한 점 1 + 오늘 고칠 것 1 + 다음 행동 1 + 점수 소형 보조 구조로 존재한다 (D-01)"
    - "잘한 점 소스가 측정 근거에서만 선정된다 — mode3 지난 미션 개선 1순위 → 감점 0 차원 → 기준 충족 폴백, 근거 없으면 정직 고지 (D-06/D-26)"
    - "헤드라인에 수치가 없고 수치는 소형 신뢰 배지 1곳뿐이다 (D-09)"
    - "결과 화면용 신규 타이포 토큰이 추가되고(기본 크기 상향 — 옵션 b) lineHeight ≥ fontSize×1.3, letterSpacing 0이다 (D-05)"
    - "첫 1회 코치마크 2개('오늘 고칠 건 하나만'/'자세히는 펼쳐요')가 존재하고 재노출되지 않는다 (D-07)"
  artifacts:
    - path: "app/src/components/SummaryCard.tsx"
      provides: "요약 카드 1장 (props 로컬 타입 — doc 결합은 32-11)"
    - path: "app/src/lib/summarySource.ts"
      provides: "잘한 점/오늘 고칠 것/다음 행동 선정 순수 함수 (구조적 타이핑 입력)"
      exports: ["deriveSummaryContent"]
    - path: "app/src/lib/coachmark.ts"
      provides: "첫 1회 플래그 (@sunity: prefix AsyncStorage)"
  key_links:
    - from: "app/src/components/SummaryCard.tsx"
      to: "app/src/theme/typography.ts"
      via: "신규 토큰만 사용 (하드코딩 0)"
      pattern: "typography\\."
---

<objective>
결과 화면 첫인상의 본체 — 요약 카드 1장(D-01)과 그 선정 로직(D-06/D-26), 타이포 토큰 상향(D-05), 첫 진입 코치마크(D-07)를 컴포넌트 단위로 만든다. result.tsx 배선은 32-11이 담당하므로 이 플랜은 result.tsx 무접촉(interface-first — props 계약을 여기서 확정).

Purpose: 32-04 목업 게이트 확정(강조 체계·게임 프레임 범위)을 코드로 옮기는 첫 플랜.
Output: SummaryCard + summarySource 순수 함수 + 신규 토큰 + 코치마크 lib/컴포넌트 + node --test.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-result-readability-3-omni/32-CONTEXT.md
@.planning/phases/32-result-readability-3-omni/32-PATTERNS.md
@.planning/phases/32-result-readability-3-omni/32-GATE-DECISIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 타이포 신규 토큰 (D-05) + 코치마크 lib/컴포넌트 (D-07)</name>
  <files>app/src/theme/typography.ts, app/src/lib/coachmark.ts, app/src/components/ResultCoachmarks.tsx</files>
  <read_first>
    - .planning/phases/32-result-readability-3-omni/32-GATE-DECISIONS.md (목업 게이트 확정 — 강조 체계·토큰 스케일. 이 파일의 확정값이 토큰 수치의 단일 출처)
    - app/src/theme/typography.ts (수정 대상 — dialogTitle/dialogBody 신설 선례 :30-38, track()=0 규칙 :2-6 letterSpacing 음수 = iOS 26+ SIGABRT)
    - app/src/lib/onboarding.ts (코치마크 lib 정본 아날로그 — @sunity: prefix, 읽기 실패='본 것으로 간주' graceful, 화면은 helper 경유만)
    - app/src/components/AccuracyLimitBadge.tsx (소형 오버레이 컴포넌트 전형 — visible prop, null 자체 숨김, 토큰 스타일, accessibility)
  </read_first>
  <action>
    per D-05/D-07. (1) typography.ts에 결과 화면용 신규 토큰 추가 — 32-GATE-DECISIONS.md의 확정 스케일을 따르되, 미세 확정이 없으면 RESEARCH 권장 기본(bodySm 17 / bodyMd 19, 필요 시 emphasize용 bodyMdBold)을 채택하고 GATE-DECISIONS에 채택값 기록. 전역 토큰 값 상향은 금지(옵션 a 리스크 — 전 화면 파급), 신규 단계 추가만(옵션 b). 각 토큰 lineHeight ≥ fontSize×1.3 명시(Pitfall 3), letterSpacing은 track()=0, as const 유지. (2) coachmark.ts — onboarding.ts 구조 복제: 키 '@sunity:result_coachmark_seen', hasSeenResultCoachmark()/markResultCoachmarkSeen(), 읽기 실패 = true(재노출 루프 방지). (3) ResultCoachmarks.tsx — 코치마크 2개 오버레이(카피: "오늘 고칠 건 하나만 보여드려요" / "자세한 내용은 펼쳐서 볼 수 있어요" — 재량 조정 가능하되 D-07 취지 유지), visible prop + onDismiss 콜백, 탭하면 닫힘, 재노출 없음(1회 플래그는 호출부가 lib로 기록). 토큰만 사용, accessibility props 관례.
  </action>
  <verify>
    <automated>cd app && npm run typecheck && grep -c "letterSpacing" src/theme/typography.ts</automated>
  </verify>
  <acceptance_criteria>
    - typography.ts 신규 토큰의 lineHeight가 각 fontSize×1.3 이상 (소스 확인)
    - 신규 토큰에 음수 letterSpacing 0건
    - coachmark.ts가 '@sunity:' prefix 키 사용 + catch 시 true 반환
    - ResultCoachmarks.tsx에 accessibilityRole/accessibilityLabel 존재
    - typecheck clean
  </acceptance_criteria>
  <done>확정된 강조 체계가 토큰으로, 코치마크가 1회 플래그 구조로 성립</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: summarySource 순수 함수 — 잘한 점·오늘 고칠 것·다음 행동 선정 (D-06/D-26)</name>
  <files>app/src/lib/summarySource.ts, app/src/lib/__tests__/summarySource.test.ts</files>
  <read_first>
    - app/src/lib/pickerFailure.test.ts (node --test 관례)
    - app/src/types/analysis.ts (dimensionScores/deductionBreakdown 실존 형상 — 단 이 모듈은 구조적 타이핑 로컬 인터페이스로 입력을 받아 32-06 타입에 비의존)
    - .planning/phases/32-result-readability-3-omni/32-CONTEXT.md D-06/D-26 (선정 규칙 원문)
  </read_first>
  <behavior>
    - Test 1 (D-26 1순위): missionOutcome.improved=true 입력 → praise.source == 'mission_improved', 헤드라인은 사람 말 문자열(수치 미포함 — 정규식 \d+(\.\d+)?\s*(도|°|점|%) 부재), 수치는 evidenceBadge 필드로 분리
    - Test 2 (D-06 폴백): 개선 없음 + 감점 0 차원 존재 → praise.source == 'clean_dimension'. 기준 충족만 → 'criteria_met'
    - Test 3 (근거 없음): 측정 근거 전무 → praise == null (근거 없는 칭찬 금지 — 렌더는 정직 고지 후 오늘 고칠 것으로)
    - Test 4 (오늘 고칠 것): mission 입력이 있으면 mission 기반, isSafety=true면 gameFrame=false 표식 (D-14 정합)
    - Test 5 (다음 행동): cueLine 존재 시 cue 기반, 없으면 보완 운동 이유 기반 폴백
  </behavior>
  <action>
    per D-06/D-26. summarySource.ts 신설 — "순수 함수만, react 의존 0" 헤더. 입력은 구조적 타이핑 로컬 인터페이스(SummaryInput: {mode, missionOutcome?: {improved, deltaSummary...}, mission?: {...cueLine?}, dimensionScores?, deductionRecords?: {criterion, points, statusLine?, cueLine?}[], safetyFlagCount?})로 정의해 32-06의 계약 타입에 컴파일 의존하지 않는다(같은 웨이브 병렬 — TS 구조적 타이핑 활용, 32-11 배선 시 실 doc 필드와 자연 결합). export deriveSummaryContent(input) -> {praise: {source, headline, evidenceBadge?} | null, todayFix: {headline, criterion, gameFrame: boolean} | null, nextAction: {text} | null}. 헤드라인 문자열에 수치를 넣지 않는 것을 함수 계약으로(수치는 evidenceBadge로만 — D-09). deltaSummary(측정 요약)를 사람 말 헤드라인으로 바꾸는 건 phrasebook 방출값(cueLine/statusLine) 우선, 부재 시 generic 문구.
  </action>
  <verify>
    <automated>node --test app/src/lib/__tests__/summarySource.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - summarySource.ts가 react/expo import 0, deriveSummaryContent export
    - behavior 5건 전부 pass — 특히 Test 1의 헤드라인 수치 부재 정규식 검사
    - 32-06 산출 타입 import 0 (구조적 로컬 인터페이스만 — grep "from '../types/analysis'" 0건)
  </acceptance_criteria>
  <done>요약 카드 3요소의 선정 규칙이 순수 함수 + 테스트로 성립</done>
</task>

<task type="auto">
  <name>Task 3: SummaryCard 컴포넌트 (D-01/D-09)</name>
  <files>app/src/components/SummaryCard.tsx</files>
  <read_first>
    - app/src/components/ScoreBreakdownSection.tsx (카드 스타일 정본 :169-176 + opt-in prop 확장 패턴 :31-55)
    - .planning/phases/32-result-readability-3-omni/32-PATTERNS.md §B-1/B-2 (카드·배지 아날로그 발췌)
    - app/src/components/OctagonScore.tsx (점수 소형 보조 표시가 참조할 기존 점수 렌더)
    - .planning/phases/32-result-readability-3-omni/32-GATE-DECISIONS.md (게임 프레임 적용 범위 — 요약 카드 내 게임 요소 유무)
  </read_first>
  <action>
    per D-01/D-09. SummaryCard.tsx 신설 — props는 deriveSummaryContent 반환 형상 + score(number) + onPressTodayFix/onPressExpand 콜백(요약→해당 카드 점프 동선, Claude 재량 영역). 구조: (1) 잘한 점 1(praise null이면 정직 고지 문구 + 오늘 고칠 것으로 시선 유도 — D-06), (2) 오늘 고칠 것 1(탭하면 상세 카드로 점프), (3) 다음 행동 1(행동 큐 문장), (4) 점수는 소형 보조(모서리 소형 배지 — 헤드라인 아님, D-09. mode3 헤드라인=발전 델타 invariant는 praise가 담당). evidenceBadge는 소형 스타일(신규 토큰 caption 계열)로 카드당 1곳. 신규 토큰(Task 1) 사용, 카드 스타일은 ScoreBreakdownSection card 정본 복제, accessibility 관례. 게임 프레임 요소(배지 등)는 32-GATE-DECISIONS의 D-10 확정 범위를 따름 — 요약 카드가 범위 밖이면 미포함.
  </action>
  <verify>
    <automated>cd app && npm run typecheck && grep -c "accessibilityRole" src/components/SummaryCard.tsx</automated>
  </verify>
  <acceptance_criteria>
    - SummaryCard.tsx가 praise/todayFix/nextAction/score props를 받고 typecheck clean
    - 스타일에 색·간격 하드코딩 0 (colors/spacing/radius/typography 토큰만 — grep으로 '#' 리터럴 0건, 단 토큰 파일 참조 제외)
    - accessibilityRole ≥ 2 (탭 가능 요소)
    - praise null 케이스 렌더 존재 (정직 고지 분기)
  </acceptance_criteria>
  <done>요약 카드 1장이 계약된 props로 성립 — 32-11 배선 대기</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doc 데이터 → 요약 헤드라인 | 선정 로직이 측정 근거 없는 주장 생성 금지 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-15 | Tampering(신뢰) | 잘한 점 생성 | mitigate | 소스 enum(mission_improved/clean_dimension/criteria_met) 밖 생성 불가 + 근거 전무 시 null (Test 3) — 근거 없는 칭찬 구조 차단 |
| T-32-16 | Info Disclosure | 코치마크 플래그 | accept | AsyncStorage 로컬 boolean — 민감 정보 아님 |
</threat_model>

<verification>
- node --test summarySource.test.ts 전부 pass
- cd app && npm run typecheck clean
</verification>

<success_criteria>
- D-01 요약 카드·D-06/D-26 선정 규칙·D-05 토큰·D-07 코치마크가 result.tsx 무접촉으로 완성 — 32-11이 배선만 하면 되는 상태
</success_criteria>

<output>
Create `.planning/phases/32-result-readability-3-omni/32-07-SUMMARY.md` when done
</output>
