---
phase: 32-result-readability-3-omni
plan: 08
type: execute
wave: 4
depends_on: ["32-03", "32-04", "32-05"]
files_modified:
  - app/src/lib/cueTrack.ts
  - app/src/lib/__tests__/cueTrack.test.ts
  - app/src/components/VideoCompare.tsx
  - .planning/phases/32-result-readability-3-omni/samples/
  - .planning/phases/32-result-readability-3-omni/32-GATE-DECISIONS.md
  - .planning/phases/32-result-readability-3-omni/32-16-PLAN.md (B안 조건부 — W-2 분리 산출)
autonomous: false
requirements: [D-18, D-21, D-09]
must_haves:
  truths:
    - "동작 비교 재생 중 결함 구간에 진입하면 자막 큐(외부 큐 문장)가 오버레이로 표시된다 (D-18 자막부)"
    - "큐 밀도는 실물 게이트(D-17) 확정값을 따르고, 큐 타이밍은 기존 데이터(faultZoom 프레임 인덱스 + 각자 fps)로만 산출된다 — 신규 타이머 0, 기존 tick 재사용"
    - "큐 윈도우가 recordId를 보유해 fault zoom·오디오(32-12 B안)와 안정 조인된다 (리뷰 반영)"
    - "A안 청취 샘플이 belle 실기기(iPhone)의 실제 시스템 TTS 엔진(AVSpeechSynthesizer — expo-speech가 감싸는 동일 엔진)으로 제작된다 — macOS say는 참고 녹음으로만 (리뷰 blocker 3 샘플 유효성)"
    - "어느 방식이 확정되든 신규 native 모듈 추가 = 새 EAS build + TestFlight 필수(OTA 단독 불가)라는 사실이 결정 재료에 명시된다 (리뷰 blocker 3)"
    - "B안 확정 시 후속 조치가 게이트 기록에 남는다: 32-12 Task 2(백엔드 TTS 스테이지+SAM 표면)를 별도 플랜 32-16으로 물리 분리한 뒤 wave 7 진입 — 분리는 게이트 확정 직후 수행, D-18 phase 내 완결 유지 (W-2)"
    - "인체 일러스트 샘플이 생성 방식·사용권·정은지 likeness 회피 기록과 함께 제작되어 belle 품질 게이트를 통과하면 도입, 미달이면 실프레임+텍스트 폴백 확정 (D-21)"
  artifacts:
    - path: "app/src/lib/cueTrack.ts"
      provides: "결함 구간 → 큐 윈도우 산출 순수 함수 (fps 인자 기반, recordId 보유)"
      exports: ["buildCueWindows", "activeCue"]
    - path: ".planning/phases/32-result-readability-3-omni/samples/"
      provides: "TTS 샘플 2종(실기기 절차 포함) + 일러스트 샘플 + provenance README"
  key_links:
    - from: "app/src/components/VideoCompare.tsx"
      to: "app/src/lib/cueTrack.ts"
      via: "기존 tick 루프(100ms)에서 activeCue 판정"
      pattern: "activeCue"
---

<objective>
재생 중 큐(D-18)의 자막 절반을 구현하고("보고서를 코칭받는 재생으로" — SEED §4-④), 오디오 방식·일러스트 품질을 결정할 샘플 게이트를 belle에게 제출한다. 오디오 구현은 게이트 확정 후 32-12가 담당.

리뷰 blocker 3 반영: A안(기기 TTS) 샘플은 macOS say가 아니라 belle iPhone의 iOS 단축어 '텍스트 말하기'(AVSpeechSynthesizer — expo-speech가 감싸는 동일 시스템 엔진·음성)로 청취해 실기기 대표성을 확보한다. 또한 어느 확정이든 native 모듈 1개 추가 = 새 EAS build+TestFlight 경로가 필요함을 결정 재료에 명시한다. 체커 W-2 반영: B안 확정 시 32-12 Task 2(백엔드 TTS 스테이지+SAM 표면)를 별도 플랜 32-16으로 물리 분리하는 후속 조치를 이 게이트의 산출로 기록한다.

Purpose: 4대 게이트의 마지막(샘플 게이트)을 닫고, 자막 큐를 실물 게이트 확정 밀도로 출시 준비.
Output: cueTrack 순수 함수 + VideoCompare 자막 오버레이 + samples/ 2벌 + 32-GATE-DECISIONS 샘플 게이트 확정(+B안 시 32-16 분리 후속 조치).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-result-readability-3-omni/32-CONTEXT.md
@.planning/phases/32-result-readability-3-omni/32-PATTERNS.md
@.planning/phases/32-result-readability-3-omni/32-GATE-DECISIONS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: cueTrack 순수 함수(recordId 조인) + VideoCompare 자막 오버레이 (D-18 자막)</name>
  <files>app/src/lib/cueTrack.ts, app/src/lib/__tests__/cueTrack.test.ts, app/src/components/VideoCompare.tsx</files>
  <read_first>
    - app/src/components/VideoCompare.tsx (tick 루프 :392-453 — cL(학생 currentTime) 기존 계산. 신규 타이머 금지, 이 tick 재사용. 32-02가 수정한 최신 상태를 읽을 것)
    - app/src/app/analysis/result.tsx:2105-2135 (fps 환산 정본 — 프레임→초는 각자의 keypointReport.fps로만, 9/18 하드코딩 금지)
    - app/src/types/analysis.ts:473-487 (FaultZoomComparison userFrameIdx — kr 프레임 공간) + :523-534 (DeductionRecord.recordId — 32-06 계약)
    - .planning/phases/32-result-readability-3-omni/32-GATE-DECISIONS.md §실물 게이트 (D-17 확정 큐 밀도 — 이 값이 기본 밀도)
  </read_first>
  <behavior>
    - Test 1: buildCueWindows(comparisons[{userFrameIdx, text, recordId?}], userFps, windowSec) → [{startSec, endSec, text, recordId?}] — startSec = userFrameIdx/userFps − windowSec/2 (0 클램프), recordId는 입력에서 그대로 승계
    - Test 2: activeCue(windows, currentSec) → 해당 구간의 큐 1개 또는 null. 겹침 시 시작이 늦은(더 정확한) 큐 우선
    - Test 3: 밀도 제한 — maxCues 인자 초과 입력 시 감점 큰 순 우선 상위 N개만 (D-17 확정 밀도)
    - Test 4: fps 0/NaN·프레임 인덱스 부재 입력에 빈 배열 (크래시 0)
  </behavior>
  <action>
    per D-18 자막 + D-17 확정 밀도. (1) cueTrack.ts 신설 — 순수 함수(react/player 의존 0). 큐 텍스트 소스: 32-09 방출 전이므로 입력 인터페이스는 {userFrameIdx, text, points?, recordId?}의 구조적 타입 — 배선(32-11)에서 records의 cueLine(문구집 행동문)을 text로, records의 recordId를 recordId로 주입하고, cueLine 부재 legacy doc은 기존 buildDeductionMarkers 행동구로 폴백한다는 계획을 파일 헤더 주석에 명시. recordId는 fault zoom 카드 점프·오디오 mp3 조인(32-12 B안 — cueId=recordId)의 안정 키(리뷰 반영 — 정렬·필터 무관). (2) VideoCompare — 기존 tick 루프에서 activeCue(cueWindowsRef.current, cL) 판정 → 자막 오버레이 state 갱신(변경 시에만 setState — 렌더 churn 방지). cueWindows prop(옵셔널, 미전달 시 렌더 diff 0 — opt-in 관례). 오버레이는 영상 하단 자막 스타일(토큰만, 수치 미포함 — D-09), accessibility 고려. 큐 표시 중 다음 큐로 전환 시 교체.
  </action>
  <verify>
    <automated>node --test app/src/lib/__tests__/cueTrack.test.ts && cd app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - cueTrack.ts가 buildCueWindows/activeCue export, react import 0, fps 하드코딩 0
    - behavior 4건 pass (recordId 승계 포함)
    - VideoCompare에 cueWindows 옵셔널 prop — 미전달 시 기존 렌더 diff 0 (기존 소비처 무변경)
    - 신규 setInterval/타이머 0 (기존 tick 재사용 — grep "setInterval" 개수 수정 전후 동일)
  </acceptance_criteria>
  <done>자막 큐가 기존 tick 위에서 recordId 조인 가능 상태로 성립, 배선 대기</done>
</task>

<task type="auto">
  <name>Task 2: TTS 샘플 2종(실기기 대표성) + 일러스트 샘플 제작 (D-18/D-21 게이트 재료)</name>
  <files>.planning/phases/32-result-readability-3-omni/samples/</files>
  <read_first>
    - .planning/phases/32-result-readability-3-omni/32-RESEARCH.md §재생 중 큐 + TTS (두 방식 비교표 + Polly CLI 커맨드)
    - .planning/phases/32-result-readability-3-omni/32-CONTEXT.md D-21 (일러스트 기준선 — "형태감 있는 고품질 인체 일러스트", 캐릭터 X·졸라맨 X)
  </read_first>
  <action>
    per D-18/D-21 + 리뷰 blocker 3(샘플 유효성). (1) 같은 코칭 문장(문구집 32-05의 실제 cueLine 1개 — 예: 행동문 + 이유문 조합 2문장)으로 TTS 샘플 2종 준비: **A안(기기 TTS) = belle iPhone 실기기 청취 절차** — iOS 단축어(Shortcuts) 앱의 '텍스트 말하기(Speak Text)' 액션에 같은 문장을 넣고 시스템 한국어 음성(유나 계열 — 설정>손쉬운 사용>음성 콘텐츠에서 선택)으로 재생. 근거: iOS Shortcuts의 Speak Text와 expo-speech 모두 AVSpeechSynthesizer 동일 엔진·동일 시스템 음성을 사용하므로 실기기 출력을 대표함(macOS say -v Yuna는 다른 데스크톱 음성 — 참고 녹음 `samples/tts_device_approx.m4a`로만 보관하고 README에 '비대표' 라벨). 단축어 설정 절차(스크린샷 포함 단계)를 samples/README.md에 기록해 belle이 1분 내 재생 가능하게. (2) B안 = `aws polly synthesize-speech --engine neural --voice-id Seoyeon --output-format mp3 --text "<같은 문장>" --profile sunity-motion samples/tts_polly.mp3` (ap-northeast-2, 비용 무시 가능 수준) — belle 실기기로 파일 전달(카톡/AirDrop)해 같은 기기 스피커로 비교 청취. 파일명·생성 커맨드를 README에 기록. (3) 일러스트 샘플 — 폴스포츠 자세 1종(예: 무릎 접기 외부 큐 상황)의 "형태감 있는 인체 일러스트" 샘플을 이미지 생성 도구로 제작(사용 가능한 생성 MCP 활용, 단순 선화 금지·캐릭터화 금지 기준 명시 프롬프트). 2~3안 변형 제작. **provenance 기록(리뷰 반영):** README에 각 샘플의 생성 도구·프롬프트 요지·사용권(생성물 상업 사용 가능 여부)·**정은지 선수 likeness 회피 확인**(실존 인물 사진·이름 미사용 프롬프트) + "정적 에셋 후보 — 런타임 생성 AI 0 invariant, 제작만 오프라인" 명기.
  </action>
  <verify>
    <automated>ls .planning/phases/32-result-readability-3-omni/samples/ | grep -cE "(tts_|illust|README)" | awk '{if($1>=3) print "SAMPLES_OK"}'</automated>
  </verify>
  <acceptance_criteria>
    - samples/에 A안 실기기 청취 절차(README 단계) + 참고 녹음 + B안 Polly mp3 + 일러스트 이미지 ≥ 1 + README.md 존재
    - 두 TTS 방식이 동일 문장 (README에 문장 원문 기록)
    - README에 재생성 커맨드 + 일러스트 provenance(도구·사용권·likeness 회피) 기록
    - macOS say 산출물에 '비대표(참고용)' 라벨 명시
  </acceptance_criteria>
  <done>belle이 실기기에서 유효하게 청취·검수할 샘플 세트 완비</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: belle 샘플 게이트 — TTS 방식 + 일러스트 도입 확정 (D-18/D-21) + B안 시 32-16 분리 후속 조치</name>
  <decision>오디오 큐 방식(기기 TTS vs 클라우드 TTS) + 일러스트 도입 여부</decision>
  <context>
    A안은 belle iPhone에서 단축어 '텍스트 말하기'로 직접 청취(실제 기기 TTS 엔진 — README 절차), B안은 Polly mp3를 같은 기기에서 재생해 비교. 비교축은 RESEARCH 표 기준: 기기 TTS(비용 0·구현 최소·계약 변경 0·음질 기기 의존) vs Polly(음질 통제·백엔드 TTS 단계+S3+계약 확장 필요). **공통 전제(리뷰 blocker 3 — 결정 재료):** 어느 쪽이든 Expo 오디오 native 모듈 1개 추가가 필요하고, native 모듈은 OTA로 기존 TestFlight 바이너리에 배포할 수 없다 — 32-12에서 앱 버전 bump(1.0.0→1.1.0, runtimeVersion policy=appVersion) + 새 EAS build + TestFlight 제출 + belle 재설치가 수반된다(1회성 빌드 비용). 모듈은 expo-speech ~14.0.8 또는 expo-audio ~1.1.1, 둘 다 expo 공식(github.com/expo/expo)·SDK 54 번들 매니페스트 등재·Package Legitimacy Audit [OK] Approved — D-18 예외 승인 항목. 일러스트는 품질 기준(형태감) 미달 시 실프레임+텍스트 정직 폴백(D-21). A/B 어느 쪽이 확정되든 오디오는 이번 phase 안(32-12)에서 출시 완결된다 — B안 선택 = 이연 아님(구현 비용 차이는 아래 cons 그대로가 결정 재료). **후속 조치(W-2 스코프 통제):** B안 확정 시 32-12 Task 2(백엔드 TTS 스테이지+SAM 표면 — 파일 9개)는 게이트 확정 직후 별도 플랜 **32-16**으로 물리 분리한 뒤 wave 7에 진입한다(단일 플랜 파일 17개의 컨텍스트 예산 초과 방지 — 분리는 실행 단위 분할일 뿐, D-18 phase 내 완결 유지). A안 확정 시 분리 불요.
  </context>
  <options>
    <option id="tts-device">
      <name>A. 기기 TTS (expo-speech)</name>
      <pros>비용 0, 오프라인, 구현 최소, 계약 변경 0 (native build 1회는 공통 비용)</pros>
      <cons>음질이 기기·OS 의존</cons>
    </option>
    <option id="tts-cloud">
      <name>B. 클라우드 TTS (Polly Seoyeon + expo-audio)</name>
      <pros>음질 통제 (neural/generative), 재생 즉시(사전 생성+prefetch)</pros>
      <cons>백엔드 합성 스테이지 + S3 + 계약 확장 + IAM 권한 + SAM 배포 필요 (native build 1회는 공통 비용) — 확정 시 32-12 Task 2를 32-16으로 분리 실행</cons>
    </option>
    <option id="illust">
      <name>일러스트: 도입 / 실프레임 폴백</name>
      <pros>도입 시 시각 이해 보조 (정적 에셋 — 런타임 생성 0)</pros>
      <cons>품질 미달이면 볼품없음 — 폴백이 정직</cons>
    </option>
  </options>
  <action>samples/의 A안 실기기 청취 절차와 B안 mp3를 belle에게 전달·안내하고 일러스트 샘플을 검수하도록 한다. 확정된 TTS 방식(A/B)과 일러스트(도입/폴백)를 32-GATE-DECISIONS.md의 "## 샘플 게이트 (D-18/D-21)" 섹션에 적재한다 — native build 필요 사실 인지 포함. **B안 확정 시 같은 섹션에 후속 조치를 기록하고 즉시 수행한다: 32-12 Task 2 스펙(백엔드 TTS 스테이지+playback asset+SAM 배포+6동작 스윕)을 별도 플랜 32-16-PLAN.md(wave 7, depends_on 32-08·32-11)으로 물리 분리 생성 — 게이트 확정 직후, wave 7 진입 전 (W-2). A안 확정 시 '분리 불요' 1줄 기록.**</action>
  <verify>
    <automated>grep -c "샘플 게이트" .planning/phases/32-result-readability-3-omni/32-GATE-DECISIONS.md</automated>
  </verify>
  <acceptance_criteria>
    - 32-GATE-DECISIONS.md에 "## 샘플 게이트 (D-18/D-21)" 섹션 + TTS 방식·일러스트 확정 + native build 필요 인지 기록 존재
    - (B 확정 시) 32-16 분리 후속 조치 기록 + 32-16-PLAN.md 생성 (wave 7, 32-12 Task 2 스펙 이관) / (A 확정 시) '분리 불요' 기록
  </acceptance_criteria>
  <done>D-18/D-21이 belle 실기기 청취·검수로 확정되어 기록되고, B안이면 32-16 분리까지 완료됨</done>
  <resume-signal>TTS 방식(A/B)과 일러스트(도입/폴백)를 답하면 Claude가 32-GATE-DECISIONS.md에 "## 샘플 게이트 (D-18/D-21)" 섹션으로 적재(+B안 시 32-16 분리 수행) — 32-12(및 B안 시 32-16)가 이 확정을 구현</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 재생 시각 → 큐 표시 | 타이밍 오산 시 엉뚱한 큐 노출 (신뢰 훼손) |
| AWS CLI → Polly | 샘플 제작 1회 호출 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-17 | Tampering(신뢰) | 큐 타이밍 | mitigate | fps는 각자의 keypointReport.fps 인자로만(Pitfall 2) + 부재 시 빈 배열(Test 4) — 잘못된 시점 큐 방지. recordId 조인으로 큐↔카드 불일치 차단 |
| T-32-18 | Info Disclosure | TTS 샘플 문장 | mitigate | 문구집 고정 문장만 사용(PII 0), 샘플은 .planning/ 내 보관(홈 디렉터리 임시보관 금지) |
| T-32-SC | Tampering | npm/pip 설치 | mitigate | 이 플랜 설치 0건 — 모듈 설치는 32-12에서 npx expo install로만 (audit [OK] 2종 한정) |
</threat_model>

<verification>
- node --test cueTrack.test.ts pass + typecheck clean
- samples/ 완비(실기기 절차 + provenance) + 32-GATE-DECISIONS.md 샘플 게이트 확정 적재(+B안 시 32-16 분리)
</verification>

<success_criteria>
- 자막 큐가 재생 중 결함 구간에서 동작할 준비 완료 (배선 32-11, recordId 조인 가능)
- 4대 게이트 전부 닫힘 (리서치·목업 32-04, 실물 32-03, 샘플 32-08) — 샘플이 실기기 TTS를 대표
- B안 확정 시 32-16 분리로 wave 7 스코프가 통제됨 (W-2)
</success_criteria>

<output>
Create `.planning/phases/32-result-readability-3-omni/32-08-SUMMARY.md` when done
</output>
