---
phase: 32-result-readability-3-omni
plan: 08
type: execute
wave: 4
depends_on: ["32-03", "32-04", "32-05"]
files_modified:
  - app/src/lib/cueTrack.ts
  - app/src/lib/__tests__/cueTrack.test.ts
  - app/src/components/VideoCompare.tsx
  - .planning/phases/32-result-readability-3-omni/samples/
  - .planning/phases/32-result-readability-3-omni/32-GATE-DECISIONS.md
autonomous: false
requirements: [D-18, D-21, D-09]
must_haves:
  truths:
    - "동작 비교 재생 중 결함 구간에 진입하면 자막 큐(외부 큐 문장)가 오버레이로 표시된다 (D-18 자막부)"
    - "큐 밀도는 실물 게이트(D-17) 확정값을 따르고, 큐 타이밍은 기존 데이터(faultZoom 프레임 인덱스 + 각자 fps)로만 산출된다 — 신규 타이머 0, 기존 tick 재사용"
    - "같은 코칭 문장의 TTS 음성 샘플 2종(기기 vs 클라우드)이 제작되어 belle이 청취 후 방식을 확정한다 (D-18 샘플 게이트)"
    - "인체 일러스트 샘플이 제작되어 belle 품질 게이트를 통과하면 도입, 미달이면 실프레임+텍스트 폴백 확정 (D-21)"
  artifacts:
    - path: "app/src/lib/cueTrack.ts"
      provides: "결함 구간 → 큐 윈도우 산출 순수 함수 (fps 인자 기반)"
      exports: ["buildCueWindows", "activeCue"]
    - path: ".planning/phases/32-result-readability-3-omni/samples/"
      provides: "TTS 샘플 2종 + 일러스트 샘플"
  key_links:
    - from: "app/src/components/VideoCompare.tsx"
      to: "app/src/lib/cueTrack.ts"
      via: "기존 tick 루프(100ms)에서 activeCue 판정"
      pattern: "activeCue"
---

<objective>
재생 중 큐(D-18)의 자막 절반을 구현하고("보고서를 코칭받는 재생으로" — SEED §4-④), 오디오 방식·일러스트 품질을 결정할 샘플 게이트를 belle에게 제출한다. 오디오 구현은 게이트 확정 후 32-12가 담당.

Purpose: 4대 게이트의 마지막(샘플 게이트)을 닫고, 자막 큐를 실물 게이트 확정 밀도로 출시 준비.
Output: cueTrack 순수 함수 + VideoCompare 자막 오버레이 + samples/ 2벌 + 32-GATE-DECISIONS 샘플 게이트 확정.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-result-readability-3-omni/32-CONTEXT.md
@.planning/phases/32-result-readability-3-omni/32-PATTERNS.md
@.planning/phases/32-result-readability-3-omni/32-GATE-DECISIONS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: cueTrack 순수 함수 + VideoCompare 자막 오버레이 (D-18 자막)</name>
  <files>app/src/lib/cueTrack.ts, app/src/lib/__tests__/cueTrack.test.ts, app/src/components/VideoCompare.tsx</files>
  <read_first>
    - app/src/components/VideoCompare.tsx (tick 루프 :392-453 — cL(학생 currentTime) 기존 계산. 신규 타이머 금지, 이 tick 재사용. 32-02가 수정한 최신 상태를 읽을 것)
    - app/src/app/analysis/result.tsx:2105-2135 (fps 환산 정본 — 프레임→초는 각자의 keypointReport.fps로만, 9/18 하드코딩 금지)
    - app/src/types/analysis.ts:473-487 (FaultZoomComparison userFrameIdx — kr 프레임 공간)
    - .planning/phases/32-result-readability-3-omni/32-GATE-DECISIONS.md §실물 게이트 (D-17 확정 큐 밀도 — 이 값이 기본 밀도)
  </read_first>
  <behavior>
    - Test 1: buildCueWindows(comparisons[{userFrameIdx, text}], userFps, windowSec) → [{startSec, endSec, text}] — startSec = userFrameIdx/userFps − windowSec/2 (0 클램프)
    - Test 2: activeCue(windows, currentSec) → 해당 구간의 큐 1개 또는 null. 겹침 시 시작이 늦은(더 정확한) 큐 우선
    - Test 3: 밀도 제한 — maxCues 인자 초과 입력 시 감점 큰 순 우선 상위 N개만 (D-17 확정 밀도)
    - Test 4: fps 0/NaN·프레임 인덱스 부재 입력에 빈 배열 (크래시 0)
  </behavior>
  <action>
    per D-18 자막 + D-17 확정 밀도. (1) cueTrack.ts 신설 — 순수 함수(react/player 의존 0). 큐 텍스트 소스: 32-09 방출 전이므로 입력 인터페이스는 {userFrameIdx, text, points?}의 구조적 타입 — 배선(32-11)에서 records의 cueLine(문구집 행동문)을 text로 주입하고, cueLine 부재 legacy doc은 기존 buildDeductionMarkers 행동구로 폴백한다는 계획을 파일 헤더 주석에 명시. (2) VideoCompare — 기존 tick 루프에서 activeCue(cueWindowsRef.current, cL) 판정 → 자막 오버레이 state 갱신(변경 시에만 setState — 렌더 churn 방지). cueWindows prop(옵셔널, 미전달 시 렌더 diff 0 — opt-in 관례). 오버레이는 영상 하단 자막 스타일(토큰만, 수치 미포함 — D-09), accessibility 고려. 큐 표시 중 다음 큐로 전환 시 교체.
  </action>
  <verify>
    <automated>node --test app/src/lib/__tests__/cueTrack.test.ts && cd app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - cueTrack.ts가 buildCueWindows/activeCue export, react import 0, fps 하드코딩 0
    - behavior 4건 pass
    - VideoCompare에 cueWindows 옵셔널 prop — 미전달 시 기존 렌더 diff 0 (기존 소비처 무변경)
    - 신규 setInterval/타이머 0 (기존 tick 재사용 — grep "setInterval" 개수 수정 전후 동일)
  </acceptance_criteria>
  <done>자막 큐가 기존 tick 위에서 성립, 배선 대기 상태</done>
</task>

<task type="auto">
  <name>Task 2: TTS 샘플 2종 + 일러스트 샘플 제작 (D-18/D-21 게이트 재료)</name>
  <files>.planning/phases/32-result-readability-3-omni/samples/</files>
  <read_first>
    - .planning/phases/32-result-readability-3-omni/32-RESEARCH.md §재생 중 큐 + TTS (두 방식 비교표 + 샘플 게이트 제작법 — macOS say -v Yuna 근사 / aws polly synthesize-speech CLI)
    - .planning/phases/32-result-readability-3-omni/32-CONTEXT.md D-21 (일러스트 기준선 — "형태감 있는 고품질 인체 일러스트", 캐릭터 X·졸라맨 X)
  </read_first>
  <action>
    per D-18/D-21. (1) 같은 코칭 문장(문구집 32-05의 실제 cueLine 1개 — 예: 행동문 + 이유문 조합 2문장)으로 TTS 샘플 2종 제작: A안 기기 TTS 근사 = `say -v Yuna -o samples/tts_device.aiff "<문장>"` 후 mp3/m4a 변환(afconvert), B안 클라우드 = `aws polly synthesize-speech --engine neural --voice-id Seoyeon --output-format mp3 --text "<같은 문장>" --profile sunity-motion samples/tts_polly.mp3` (ap-northeast-2, 비용 무시 가능 수준). 파일명·생성 커맨드를 samples/README.md에 기록. (2) 일러스트 샘플 — 폴스포츠 자세 1종(예: 무릎 접기 외부 큐 상황)의 "형태감 있는 인체 일러스트" 샘플을 이미지 생성 도구로 제작(사용 가능한 생성 MCP 활용, 단순 선화 금지·캐릭터화 금지 기준 명시 프롬프트). 생성물은 정적 에셋 후보(런타임 생성 AI 0 invariant — 제작만 오프라인)임을 README에 명기. 2~3안 변형 제작.
  </action>
  <verify>
    <automated>ls .planning/phases/32-result-readability-3-omni/samples/ | grep -cE "(tts_|illust)" | awk '{if($1>=3) print "SAMPLES_OK"}'</automated>
  </verify>
  <acceptance_criteria>
    - samples/에 tts A안·B안 오디오 파일 + 일러스트 이미지 ≥ 1 + README.md 존재
    - 두 TTS 샘플이 동일 문장 (README에 문장 원문 기록)
    - README에 재생성 커맨드 기록
  </acceptance_criteria>
  <done>belle이 청취·검수할 샘플 세트 완비</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: belle 샘플 게이트 — TTS 방식 + 일러스트 도입 확정 (D-18/D-21)</name>
  <decision>오디오 큐 방식(기기 TTS vs 클라우드 TTS) + 일러스트 도입 여부</decision>
  <context>
    samples/의 두 오디오를 belle이 직접 청취(파일 전달). 비교축은 RESEARCH 표 기준: 기기 TTS(비용 0·구현 최소·계약 변경 0·음질 기기 의존) vs Polly(음질 통제·백엔드 TTS 단계+S3+계약 확장 필요). 어느 쪽이든 Expo 오디오 모듈 1개 추가가 신규 의존성 금지 원칙의 예외 승인 항목(D-18 원문) — expo-speech ~14.0.8 또는 expo-audio ~1.1.1, 둘 다 expo 공식(github.com/expo/expo)·SDK 54 번들 매니페스트 등재·Package Legitimacy Audit [OK] Approved. 일러스트는 품질 기준(형태감) 미달 시 실프레임+텍스트 정직 폴백(D-21). A/B 어느 쪽이 확정되든 오디오는 이번 phase 안(32-12)에서 출시 완결된다 — B안 선택 = 이연 아님(구현 비용 차이는 아래 cons 그대로가 결정 재료).
  </context>
  <options>
    <option id="tts-device">
      <name>A. 기기 TTS (expo-speech)</name>
      <pros>비용 0, 오프라인, 구현 최소, 계약 변경 0</pros>
      <cons>음질이 기기·OS 의존</cons>
    </option>
    <option id="tts-cloud">
      <name>B. 클라우드 TTS (Polly Seoyeon + expo-audio)</name>
      <pros>음질 통제 (neural/generative), 재생 즉시</pros>
      <cons>백엔드 합성 스테이지 + S3 + 계약 확장 + IAM 권한 필요</cons>
    </option>
    <option id="illust">
      <name>일러스트: 도입 / 실프레임 폴백</name>
      <pros>도입 시 시각 이해 보조 (정적 에셋 — 런타임 생성 0)</pros>
      <cons>품질 미달이면 볼품없음 — 폴백이 정직</cons>
    </option>
  </options>
  <action>samples/의 TTS 2종을 belle이 청취하고 일러스트 샘플을 검수하도록 파일을 전달·안내한다. 확정된 TTS 방식(A/B)과 일러스트(도입/폴백)를 32-GATE-DECISIONS.md의 "## 샘플 게이트 (D-18/D-21)" 섹션에 적재한다.</action>
  <verify>
    <automated>grep -c "샘플 게이트" .planning/phases/32-result-readability-3-omni/32-GATE-DECISIONS.md</automated>
  </verify>
  <acceptance_criteria>
    - 32-GATE-DECISIONS.md에 "## 샘플 게이트 (D-18/D-21)" 섹션 + TTS 방식·일러스트 확정 존재
  </acceptance_criteria>
  <done>D-18/D-21이 belle 청취·검수로 확정되어 기록됨</done>
  <resume-signal>TTS 방식(A/B)과 일러스트(도입/폴백)를 답하면 Claude가 32-GATE-DECISIONS.md에 "## 샘플 게이트 (D-18/D-21)" 섹션으로 적재 — 32-12가 이 확정을 구현</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 재생 시각 → 큐 표시 | 타이밍 오산 시 엉뚱한 큐 노출 (신뢰 훼손) |
| AWS CLI → Polly | 샘플 제작 1회 호출 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-17 | Tampering(신뢰) | 큐 타이밍 | mitigate | fps는 각자의 keypointReport.fps 인자로만(Pitfall 2) + 부재 시 빈 배열(Test 4) — 잘못된 시점 큐 방지 |
| T-32-18 | Info Disclosure | TTS 샘플 문장 | mitigate | 문구집 고정 문장만 사용(PII 0), 샘플은 .planning/ 내 보관(홈 디렉터리 임시보관 금지) |
| T-32-SC | Tampering | npm/pip 설치 | mitigate | 이 플랜 설치 0건 — 모듈 설치는 32-12에서 npx expo install로만 (audit [OK] 2종 한정) |
</threat_model>

<verification>
- node --test cueTrack.test.ts pass + typecheck clean
- samples/ 완비 + 32-GATE-DECISIONS.md 샘플 게이트 확정 적재
</verification>

<success_criteria>
- 자막 큐가 재생 중 결함 구간에서 동작할 준비 완료 (배선 32-11)
- 4대 게이트 전부 닫힘 (리서치·목업 32-04, 실물 32-03, 샘플 32-08)
</success_criteria>

<output>
Create `.planning/phases/32-result-readability-3-omni/32-08-SUMMARY.md` when done
</output>
