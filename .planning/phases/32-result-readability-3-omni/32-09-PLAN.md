---
phase: 32-result-readability-3-omni
plan: 09
type: execute
wave: 4
depends_on: ["32-05", "32-06"]
files_modified:
  - backend/functions/pipeline/app.py
  - backend/shared/python/sunity_shared/analysis/coach_writer.py
  - backend/tests/phase32/test_pipeline_emission.py
autonomous: true
model: fable
requirements: [D-08, D-11, D-19, D-26, D-27, D-28, D-29]
must_haves:
  truths:
    - "새 분석의 deductionBreakdown.records에 문구집 3단(statusLine/whyLine/cueLine)+coachQuestion+exerciseReason이 조립되어 방출된다 (D-08/D-11)"
    - "mission(선정·streak·에스컬레이션)과 missionOutcome이 직전 분석 체인으로 방출된다 (D-19/D-26/D-27)"
    - "coachQuestions가 자동 수집(위험 결함 항상 + 3회 미개선 + 못 잰 것)으로 방출된다 (D-28/D-29)"
    - "LLM(Cerebras)은 가변부만 — 전체 실패 시에도 문구집 골격만으로 3단이 성립한다 (D-11 graceful)"
    - "방출은 전부 result 안으로 흐르고 scoped validator를 통과한다 — complete_analysis kwarg 0"
  artifacts:
    - path: "backend/tests/phase32/test_pipeline_emission.py"
      provides: "방출 통합 테스트 (fake_firestore — LLM 무키 환경에서 골격 성립 증명 포함)"
  key_links:
    - from: "backend/functions/pipeline/app.py"
      to: "phrasebook.assemble_phrases + mission.select_mission"
      via: "_process 결과 조립부에서 호출"
      pattern: "assemble_phrases|select_mission"
    - from: "backend/functions/pipeline/app.py"
      to: "firestore_admin.get_previous_analysis"
      via: "prev chain (mode3 기존 경로 재사용)"
      pattern: "get_previous_analysis"
---

<objective>
32-05(문구집)와 32-06(미션 엔진·계약)을 파이프라인 `_process`에 배선해 새 분석부터 3단 문구·미션·코치 질문이 방출되게 한다. Cerebras는 가변부 슬롯만 채우고, 실패 시 문구집 골격이 그대로 나간다.

Purpose: 번역 레이어·미션 루프의 데이터가 실제 doc에 흐르기 시작 — 앱 배선(32-11)의 선행.
Output: pipeline 방출 배선 + coach_writer 가변부 한정 + 통합 테스트.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-result-readability-3-omni/32-CONTEXT.md
@.planning/phases/32-result-readability-3-omni/32-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: _process 방출 배선 — 3단 문구·미션·코치 질문 (D-08/D-19/D-26/D-27/D-28/D-29)</name>
  <files>backend/functions/pipeline/app.py</files>
  <read_first>
    - backend/functions/pipeline/app.py (수정 대상 — _process의 result dict 조립부와 firestore_complete 스테이지 :5236 부근. deductionBreakdown이 result에 실리는 지점과 mode3 get_previous_analysis 기존 호출 위치를 실측)
    - backend/shared/python/sunity_shared/analysis/phrasebook.py (32-05 산출 — assemble_phrases 시그니처)
    - backend/shared/python/sunity_shared/analysis/mission.py (32-06 산출 — select_mission/derive_mission_outcome 시그니처)
    - backend/shared/python/sunity_shared/firestore_admin.py (32-06 산출 — _validate_mission 계열이 검증하는 result 경로)
    - backend/shared/python/sunity_shared/models.py (COACH_QUESTION_SOURCES enum)
  </read_first>
  <action>
    per D-08/D-19/D-26/D-27/D-28/D-29. _process의 result 조립부(firestore_complete 이전, 동기 경로 내 순수 계산 — 외부 호출 아님)에 배선: (1) records 각각에 assemble_phrases(motion_key, criterion, ruleId) 5슬롯을 병합(기존 키 무변경, 신규 키 추가만 — 기존 소비처 무회귀). (2) prev = 기존 mode3 경로의 get_previous_analysis 결과 재사용(신규 쿼리 금지 — composite index 함정 주석 준수). prev.get("result",{}).get("mission")을 prev_mission으로 select_mission 호출 → result["mission"]. derive_mission_outcome → result["missionOutcome"](mode3만, mode1은 None 생략). (3) coachQuestions 자동 수집: safetyFlags 각각(source='safety') + mission.escalation=='coach_card'인 미션(source='mission_stuck') + 커버리지 갭(deductionBreakdown의 coverage_gap 계열 실존 신호 — 코드 실측해 사용, source='unmeasured') → 문구집 coachQuestion 완성문으로 조립(D-28 "질문은 문구집 스타일 완성문"). recordIndex를 붙여 앱 점프 지원. (4) 전부 result dict 안으로 — complete_analysis 호출 시그니처 무변경. 방출 실패는 분석을 죽이지 않는다(SP-3 — try/except + log.exception + 해당 필드 생략, motion_alignment "방출 실패 비차단" 교훈). 스펙 인용 주석(32-CONTEXT D-19 식) 관례.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/phase32 -x -q</automated>
  </verify>
  <acceptance_criteria>
    - pipeline/app.py에 assemble_phrases/select_mission/derive_mission_outcome 호출 존재
    - 신규 Firestore 쿼리 0 (get_previous_analysis 기존 경로 재사용 — where 절 추가 없음)
    - complete_analysis 호출부 kwarg diff 0
    - 방출 블록이 try/except로 감싸져 실패 시 분석 status done 유지
  </acceptance_criteria>
  <done>새 분석 result에 3단 문구·미션·질문이 흐르고 실패해도 분석은 완주</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: coach_writer 가변부 한정 (D-11) + 방출 통합 테스트</name>
  <files>backend/shared/python/sunity_shared/analysis/coach_writer.py, backend/tests/phase32/test_pipeline_emission.py</files>
  <read_first>
    - backend/shared/python/sunity_shared/analysis/coach_writer.py (수정 대상 — graceful 초기화 :217-229, 실패 {} 반환 :246-279, "주입된 실측 데이터만, 임의 수치 생성 금지" 시스템 프롬프트 :39-41)
    - backend/tests/phase31/conftest.py:346-370 (fake_firestore fixture — 방출 통합 테스트 재사용)
    - backend/tests/phase32/test_phrasebook_forbidden.py (32-05 산출 금지어 목록 — LLM 출력 게이트에 재사용)
  </read_first>
  <behavior>
    - Test 1: Cerebras 무키 환경(_client None)에서 record의 statusLine/whyLine/cueLine이 문구집 골격 그대로 방출 (LLM 없이 3단 성립 — D-11)
    - Test 2: 미션 방출 — safetyFlags 있는 입력 doc에서 result.mission.selectedBy == 'safety' + coachQuestions에 source='safety' 항목 존재
    - Test 3: prev chain — prev.mission과 동일 criterion 잔존 입력에서 streak 증가 + 3회차에 coachQuestions source='mission_stuck' 등재
    - Test 4: 방출 전체가 32-06 scoped validator를 예외 없이 통과 (fake_firestore 경유 저장 성공)
  </behavior>
  <action>
    per D-11. (1) coach_writer.py — 시스템 프롬프트를 가변부 슬롯 한정으로 강화: LLM의 역할은 문구집 골격 문장에 상황 수치 연결·조사 자연화·응원 톤 조정만(허용 슬롯 명시), 새 교정 지시·일반론 조언 생성 금지를 프롬프트에 명문화("주입 실측만" 기존 문구 유지·강화). 출력이 문구집 골격을 대체하지 않도록 조립 순서 = 골격 먼저, LLM 산출은 지정 슬롯 병합만. LLM 출력에 금지어(FORBIDDEN_PHRASES_PHRASEBOOK) 포함 시 해당 출력 폐기 후 골격 사용(사후 필터 — grep 게이트의 런타임 판). {} 폴백 관례 유지. (2) test_pipeline_emission.py — phase31 fake_firestore로 behavior 4건. LLM 어댑터는 무키 환경 자연 no-op 활용.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/phase32/test_pipeline_emission.py -x -q && python -m pytest tests/phase32 -q | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - behavior 4건 green
    - coach_writer.py 시스템 프롬프트에 슬롯 한정·일반론 금지 문구 존재 (grep "일반" 또는 명시 금지 조항)
    - LLM 출력 런타임 금지어 필터 존재 (FORBIDDEN 참조 grep ≥ 1)
    - 기존 coach_writer 테스트 무회귀
  </acceptance_criteria>
  <done>LLM이 골격을 소유할 수 없는 구조가 코드+테스트로 성립</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cerebras LLM → 사용자 문장 | 외부 LLM 출력이 코칭 문장 가변부에 유입 |
| pipeline → Firestore | 신규 필드 영속 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-19 | Tampering(신뢰) | LLM 가변부 | mitigate | 골격=문구집 소유 + 슬롯 한정 프롬프트 + 런타임 금지어 필터 + 전체 실패 시 골격 폴백 (Test 1) — 일반론·자의 수치 유입 경로 3중 차단 |
| T-32-20 | DoS | 방출 실패 | mitigate | 방출 블록 try/except 비차단 — 완료된 분석을 표시 부가물이 fail시키지 않음 (SP-3) |
| T-32-21 | Tampering | Firestore 쓰기 | mitigate | 32-06 scoped validator 경유 (Test 4) + 신규 쿼리·kwarg 0 |
</threat_model>

<verification>
- cd backend && python -m pytest tests/phase32 -x -q 전부 green
- 백엔드 전체 baseline diff 초과 실패 0
</verification>

<success_criteria>
- 새 분석 doc에 3단 문구·미션·코치 질문이 방출됨 (Pod 배포는 32-13 엔진 웨이브에서 일괄 — 그 전 앱 배선 32-11은 로컬/기존 doc 폴백으로 검증)
- LLM 실패 내성이 테스트로 증명됨
</success_criteria>

<output>
Create `.planning/phases/32-result-readability-3-omni/32-09-SUMMARY.md` when done
</output>
