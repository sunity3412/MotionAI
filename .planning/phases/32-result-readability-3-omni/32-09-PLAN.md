---
phase: 32-result-readability-3-omni
plan: 09
type: execute
wave: 4
depends_on: ["32-05", "32-06"]
files_modified:
  - backend/functions/pipeline/app.py
  - backend/shared/python/sunity_shared/analysis/coach_writer.py
  - backend/shared/python/sunity_shared/analysis/phrasebook.py
  - backend/tests/phase32/test_pipeline_emission.py
autonomous: true
model: fable
requirements: [D-08, D-11, D-19, D-26, D-27, D-28, D-29, D-23]
must_haves:
  truths:
    - "새 분석의 deductionBreakdown.records에 안정 recordId + 문구집 3단(statusLine/whyLine/cueLine)+coachQuestion+exerciseId/exerciseReason+tolerance가 조립되어 방출된다 (D-08/D-11 + 리뷰 blocker 5 계약부)"
    - "mission(faultKey·baseline 포함)과 수치 missionOutcome이 직전 분석 체인(동일 motion 가드)으로 방출된다 (D-19/D-26/D-27 + 리뷰 blocker 1)"
    - "summaryPraise(잘한 점 후보 — phrasebook 조립 사람 말 헤드라인 + 수치 분리)가 백엔드에서 방출된다 — 앱·스팟체크의 단일 원천 (리뷰 blocker 5)"
    - "coachQuestions가 자동 수집(위험 결함 항상 + 3회 미개선 + 못 잰 것)으로 recordId 조인과 함께 방출된다 (D-28/D-29)"
    - "record별 문구 조립 실패가 다른 record·mission·praise 조립을 죽이지 않는다 — 항목 단위 격리 (리뷰 반영)"
    - "LLM(Cerebras)은 가변부만 — 전체 실패 시에도 문구집 골격만으로 3단이 성립한다 (D-11 graceful)"
    - "방출은 전부 result 안으로 흐르고 scoped validator를 통과한다 — complete_analysis kwarg 0"
    - "방출 코드가 프로덕션(Pod)에 배포되고 fixture 6동작 전수 스윕으로 실측된다 — 32-11/32-12 UI 검증이 실제 신규 계약 doc을 갖게 됨 (리뷰 blocker 2 + D-23 웨이브 게이트)"
  artifacts:
    - path: "backend/tests/phase32/test_pipeline_emission.py"
      provides: "방출 통합 테스트 (fake_firestore — LLM 무키 환경에서 골격 성립 증명 포함)"
  key_links:
    - from: "backend/functions/pipeline/app.py"
      to: "phrasebook.assemble_phrases/assemble_safety_phrases/assemble_praise + mission.select_mission"
      via: "_process 결과 조립부에서 호출"
      pattern: "assemble_phrases|select_mission"
    - from: "backend/functions/pipeline/app.py"
      to: "firestore_admin.get_previous_analysis"
      via: "prev chain (mode3 기존 경로 재사용 + 순수 함수 측 motion 가드)"
      pattern: "get_previous_analysis"
---

<objective>
32-05(문구집)와 32-06(미션 엔진·계약)을 파이프라인 `_process`에 배선해 새 분석부터 recordId·3단 문구·미션·summaryPraise·코치 질문이 방출되게 한다. Cerebras는 가변부 슬롯만 채우고, 실패 시 문구집 골격이 그대로 나간다. 마지막으로 Pod에 배포하고 6동작 전수 스윕으로 실측한다 — 이후 UI 웨이브(32-11/32-12)가 실제 신규 계약 doc으로 검증할 수 있게 하는 순서 수정(리뷰 blocker 2).

Purpose: 번역 레이어·미션 루프의 데이터가 실제 doc에 흐르기 시작 — 앱 배선(32-11)의 선행.
Output: pipeline 방출 배선 + coach_writer 가변부 한정 + 통합 테스트 + Pod 배포·6동작 스윕.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-result-readability-3-omni/32-CONTEXT.md
@.planning/phases/32-result-readability-3-omni/32-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: _process 방출 배선 — recordId·3단 문구·미션·summaryPraise·코치 질문 (항목 단위 격리)</name>
  <files>backend/functions/pipeline/app.py, backend/shared/python/sunity_shared/analysis/phrasebook.py</files>
  <read_first>
    - backend/functions/pipeline/app.py (수정 대상 — _process의 result dict 조립부와 firestore_complete 스테이지 :5236 부근. deductionBreakdown이 result에 실리는 지점과 mode3 get_previous_analysis 기존 호출 위치, 인식된 motion key(recognizer/technique profile)가 존재하는 변수를 실측)
    - backend/shared/python/sunity_shared/analysis/phrasebook.py (32-05 산출 — assemble_phrases/assemble_safety_phrases 시그니처. 이 태스크가 assemble_praise를 추가)
    - backend/shared/python/sunity_shared/analysis/mission.py (32-06 산출 — build_fault_key/select_mission/derive_mission_outcome 시그니처)
    - backend/shared/python/sunity_shared/firestore_admin.py (32-06 산출 — _validate_mission/_validate_summary_praise 계열이 검증하는 result 경로)
    - backend/shared/python/sunity_shared/models.py (COACH_QUESTION_SOURCES/SUMMARY_PRAISE_SOURCES enum)
  </read_first>
  <action>
    per D-08/D-19/D-26/D-27/D-28/D-29 + 리뷰 blocker 1·2·5. _process의 result 조립부(firestore_complete 이전, 동기 경로 내 순수 계산 — 외부 호출 아님)에 배선: (1) **recordId 각인:** records 각각에 recordId = f"r{i:02d}:{criterion}" (방출 시 1회 각인 — 이후 앱 조인·스팟체크·질문 점프의 안정 키. 32-06 계약 형식). (2) **record별 문구 병합 — 항목 단위 격리(리뷰 반영):** records 각각에 assemble_phrases(motion_key, criterion, ruleId) 슬롯 병합을 **record 단위 try/except**로 감싼다 — 한 record 실패는 그 record만 문구 없이 통과(다른 record·미션·praise 무영향). tolerance는 CRITERION_GROUPS 등 실존 규칙 상수에서 record별로 방출(있을 때만 — 자의 수치 생성 금지, 32-10 게이지 재료). 기존 키 무변경, 신규 키 추가만(기존 소비처 무회귀). (3) **미션 체인:** prev = 기존 mode3 경로의 get_previous_analysis 결과 재사용(신규 쿼리 금지 — composite index 함정 주석 준수. 같은 mode만 필터하는 실측 사실은 mission.py의 motionId 가드가 방어). 인식된 motion key를 motion_id 인자로 전달해 select_mission(records, safety_flags, prev.get("result",{}).get("mission"), motion_id) → result["mission"]. derive_mission_outcome(prev_mission, records, mode, motion_id) → result["missionOutcome"](mode3만, mode1/None은 생략). (4) **summaryPraise 방출(리뷰 blocker 5 — 잘한 점 단일 원천):** phrasebook.py에 assemble_praise(outcome: dict | None, clean_dimensions: list[str], criteria_met: bool) -> dict | None 순수 함수 추가 — 우선순위 mission_improved(outcome.improved=true) → clean_dimension(감점 0 차원 — 측정 존재분만, 호출부가 커버리지 갭 차원 제외 후 전달) → criteria_met → None. 반환 {source, headline(사람 말 — 수치 미포함, 문구집 톤), evidenceValue(float|None — 예: deltaPoints), evidenceUnit(str|None)}. 파이프라인이 result["summaryPraise"]로 방출(None이면 생략). (5) **coachQuestions 자동 수집:** safetyFlags 각각 → assemble_safety_phrases(flag_type).coachQuestion (source='safety') + mission.escalation=='coach_card'인 미션 → 해당 record의 coachQuestion (source='mission_stuck') + 커버리지 갭 → source='unmeasured'. 커버리지 갭 신호는 명시 adapter 함수 collect_unmeasured_signals(records, dimension_data) -> list[str]로 고정(리뷰 반영 — 실존 신호를 코드 실측: records의 coverage_gap 계열 criterion/ruleId + dimensionExplanation 미측정 서술. 발견 필드를 함수 docstring에 기록) + fixture 테스트. 각 질문에 recordId 조인(해당 record 있을 때). (6) 전부 result dict 안으로 — complete_analysis 호출 시그니처 무변경. 방출 블록 전체도 상위 try/except(전체 실패 시 분석은 완주 — SP-3), 내부는 record/미션/praise/질문 단위 격리. 스펙 인용 주석(32-CONTEXT D-19 식) 관례.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/phase32 -x -q</automated>
  </verify>
  <acceptance_criteria>
    - pipeline/app.py에 assemble_phrases/assemble_safety_phrases/assemble_praise/select_mission/derive_mission_outcome 호출 존재
    - records 방출에 recordId 각인 (형식 r{i:02d}:{criterion})
    - record별 try/except 격리 존재 (한 record 예외 주입 테스트에서 나머지 record 문구 잔존)
    - collect_unmeasured_signals adapter 함수 존재 + 실존 신호 필드가 docstring에 기록
    - 신규 Firestore 쿼리 0 (get_previous_analysis 기존 경로 재사용 — where 절 추가 없음)
    - complete_analysis 호출부 kwarg diff 0
    - 방출 블록이 try/except로 감싸져 실패 시 분석 status done 유지
  </acceptance_criteria>
  <done>새 분석 result에 recordId·3단 문구·미션·praise·질문이 항목 격리로 흐르고 실패해도 분석은 완주</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: coach_writer 가변부 한정 (D-11) + 방출 통합 테스트</name>
  <files>backend/shared/python/sunity_shared/analysis/coach_writer.py, backend/tests/phase32/test_pipeline_emission.py</files>
  <read_first>
    - backend/shared/python/sunity_shared/analysis/coach_writer.py (수정 대상 — graceful 초기화 :217-229, 실패 {} 반환 :246-279, "주입된 실측 데이터만, 임의 수치 생성 금지" 시스템 프롬프트 :39-41)
    - backend/tests/phase31/conftest.py:346-370 (fake_firestore fixture — 방출 통합 테스트 재사용)
    - backend/tests/phase32/test_phrasebook_forbidden.py (32-05 산출 금지어 목록 — LLM 출력 게이트에 재사용)
  </read_first>
  <behavior>
    - Test 1: Cerebras 무키 환경(_client None)에서 record의 statusLine/whyLine/cueLine이 문구집 골격 그대로 방출 (LLM 없이 3단 성립 — D-11)
    - Test 2: 미션 방출 — safetyFlags 있는 입력 doc에서 result.mission.selectedBy == 'safety' + isSafety == true + escalation == 'none' + coachQuestions에 source='safety' 항목 존재 (D-14 정합)
    - Test 3: prev chain — prev.mission과 동일 faultKey 잔존 입력에서 streak 증가 + 3회차에 coachQuestions source='mission_stuck' 등재. prev motionId 불일치 입력 → streak 1 리셋
    - Test 4: summaryPraise — outcome.improved=true 입력에서 result.summaryPraise.source == 'mission_improved' + headline에 수치 부재(정규식) + evidenceValue 수치 분리
    - Test 5: record 격리 — 한 record에 조립 예외를 주입해도 다른 record 문구·mission·summaryPraise 방출 잔존
    - Test 6: 방출 전체가 32-06 scoped validator를 예외 없이 통과 (fake_firestore 경유 저장 성공)
  </behavior>
  <action>
    per D-11. (1) coach_writer.py — 시스템 프롬프트를 가변부 슬롯 한정으로 강화: LLM의 역할은 문구집 골격 문장에 상황 수치 연결·조사 자연화·응원 톤 조정만(허용 슬롯 명시), 새 교정 지시·일반론 조언 생성 금지를 프롬프트에 명문화("주입 실측만" 기존 문구 유지·강화). 출력이 문구집 골격을 대체하지 않도록 조립 순서 = 골격 먼저, LLM 산출은 지정 슬롯 병합만. LLM 출력에 금지어(FORBIDDEN_PHRASES_PHRASEBOOK) 포함 시 해당 출력 폐기 후 골격 사용(사후 필터 — grep 게이트의 런타임 판). {} 폴백 관례 유지. (2) test_pipeline_emission.py — phase31 fake_firestore로 behavior 6건. LLM 어댑터는 무키 환경 자연 no-op 활용.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/phase32/test_pipeline_emission.py -x -q && python -m pytest tests/phase32 -q | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - behavior 6건 green
    - coach_writer.py 시스템 프롬프트에 슬롯 한정·일반론 금지 문구 존재 (grep "일반" 또는 명시 금지 조항)
    - LLM 출력 런타임 금지어 필터 존재 (FORBIDDEN 참조 grep ≥ 1)
    - 기존 coach_writer 테스트 무회귀
  </acceptance_criteria>
  <done>LLM이 골격을 소유할 수 없는 구조가 코드+테스트로 성립</done>
</task>

<task type="auto">
  <name>Task 3: Pod 배포 + fixture 6동작 전수 스윕 — 신규 계약 doc 실측 (리뷰 blocker 2 + D-23)</name>
  <files>(코드 수정 없음 — Pod 운영 + 스윕 기록. 발견 결함 즉시 수리 커밋 허용)</files>
  <read_first>
    - .planning/phases/32-result-readability-3-omni/32-RESEARCH.md §Common Pitfalls 9/10 (스윕 순차 강제·Pod 운영 함정)
    - .planning/phases/32-result-readability-3-omni/32-03-SUMMARY.md (Pod 상태·배포 절차·직전 스윕 기준선)
  </read_first>
  <action>
    per 리뷰 blocker 2(배포 순서 수정) + D-23 웨이브 게이트. 아키텍처 사실(플랜 명시): pipeline/app.py·sunity_shared는 RunPod 서버가 모듈로 import하는 코드 — 프로덕션 분석 경로의 배포 = Pod git pull(Lambda 쪽 pipeline 함수는 CPU 폴백/위임 래퍼라 SAM 배포는 Lambda-전용 표면 변경 시에만 — 32-12 B안의 template.yaml/playback-url이 그 경우). (1) Pod /health 200 확인(미가동 시 32-03 절차 재사용 — 재생성은 belle 승인) → 32-05·32-06·32-09 백엔드 커밋 push → Pod git pull + 서버 재시작 + /health 200 재확인. (2) fixture 6동작 전수 스윕(순차 — 동시 호출 금지, kip-up 편중 금지): 각 결과 doc에서 (a) records에 recordId·3단 문구·tolerance 방출 확인, (b) mission(faultKey·baseline 필드 포함)·coachQuestions 방출 + validator 통과, (c) mode3 케이스 1건 이상에서 missionOutcome·summaryPraise 방출(mode3 fixture 재분석 2회 연쇄로 prev 체인 생성 — 첫 분석 후 같은 영상 재분석으로 outcome 실측), (d) 점수·verdict가 32-03 스윕 기준선 대비 diff 0(방출은 additive — 채점 무접촉 실증), (e) 동기 경로 timingsMs가 직전 실측 수준(방출 조립은 순수 계산 — 유의미 증가 없어야 함). (3) 결과 표(동작 × 방출 필드 유무 × 점수 diff × 타이밍)를 SUMMARY에 기록. 이 스윕이 만든 신규 계약 doc들이 32-11 시뮬레이터·32-12 실기기 검증의 실데이터가 된다 — doc id 목록을 SUMMARY에 남길 것.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" "$RUNPOD_ANALYZE_URL/health" | grep -q 200 && echo POD_OK && cd backend && python -m pytest tests/phase32 -q | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - 6동작 스윕 표 존재 — 전 doc에 recordId·3단 문구 방출 + validator 통과
    - mode3 연쇄 케이스에서 mission→missionOutcome·summaryPraise 실측 기록
    - 점수·verdict 기준선(32-03) 대비 diff 0
    - 동기 경로 타이밍 유의미 회귀 0 (직전 실측 대비)
    - 신규 계약 doc id 목록이 SUMMARY에 기록 (32-11/32-12 검증 입력)
  </acceptance_criteria>
  <done>방출 코드가 프로덕션에 반영되고 6동작 실측으로 증명됨 — UI 웨이브가 실데이터로 검증 가능 (blocker 2 해소)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cerebras LLM → 사용자 문장 | 외부 LLM 출력이 코칭 문장 가변부에 유입 |
| pipeline → Firestore | 신규 필드 영속 |
| 로컬 → Pod | 프로덕션 배포 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-19 | Tampering(신뢰) | LLM 가변부 | mitigate | 골격=문구집 소유 + 슬롯 한정 프롬프트 + 런타임 금지어 필터 + 전체 실패 시 골격 폴백 (Test 1) — 일반론·자의 수치 유입 경로 3중 차단 |
| T-32-20 | DoS | 방출 실패 | mitigate | 상위 try/except + record/미션/praise 항목 단위 격리(Test 5) — 완료된 분석을 표시 부가물이 fail시키지 않음 (SP-3) |
| T-32-21 | Tampering | Firestore 쓰기 | mitigate | 32-06 scoped validator 경유 (Test 6) + 신규 쿼리·kwarg 0 |
| T-32-40 | DoS | Pod 배포 | mitigate | /health 200 게이트 + 6동작 스윕 점수 diff 0 + 순차 실행 |
</threat_model>

<verification>
- cd backend && python -m pytest tests/phase32 -x -q 전부 green
- 백엔드 전체 baseline diff 초과 실패 0
- Pod 6동작 스윕 표 + 점수 diff 0 + 신규 계약 doc id 목록
</verification>

<success_criteria>
- 새 분석 doc에 recordId·3단 문구·미션(baseline)·summaryPraise·코치 질문이 프로덕션에서 실측 방출됨 — 32-11/32-12가 실데이터로 검증 (리뷰 blocker 2 해소)
- LLM 실패 내성·항목 격리가 테스트로 증명됨
</success_criteria>

<output>
Create `.planning/phases/32-result-readability-3-omni/32-09-SUMMARY.md` when done
</output>
