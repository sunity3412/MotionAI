---
phase: 32-result-readability-3-omni
plan: 10
type: execute
wave: 5
depends_on: ["32-04", "32-07"]
files_modified:
  - app/src/components/DeductionCard.tsx
  - app/src/components/GoalGaugeBar.tsx
  - app/src/components/MissionBadge.tsx
  - app/src/components/DeductionDetailSheet.tsx
  - app/src/components/InjuryRiskSection.tsx
  - app/src/lib/gaugeGeometry.ts
  - app/src/lib/__tests__/gaugeGeometry.test.ts
autonomous: true
requirements: [D-08, D-09, D-10, D-14, D-15, D-20]
must_haves:
  truths:
    - "감점 카드가 상태(몸 말)→왜(감점 이유 1줄)→행동(외부 큐) 3단으로 렌더된다 (D-08)"
    - "카드 안에 줌 쌍(내 줌 vs 정은지 줌)이 인라인 — 상태+증거+게이지+미션이 카드 하나에 완결, 이미지 로딩/실패/만료 상태 처리 포함 (D-20)"
    - "게이지의 수학적 의미가 정의·테스트된다 — 스케일 = [min(현재,목표)−tol, max(현재,목표)+tol], tol은 백엔드 방출 규칙 상수(record.tolerance). 표현 불가 record(tolerance 부재·비지원 unit)는 게이지 없이 수치 배지+텍스트 폴백 (리뷰 HIGH — 자의적 시각 비율 금지)"
    - "게이지 표시 텍스트는 실측 단위 그대로(% 환산 금지), 수치는 카드당 소형 신뢰 배지 1곳 (D-09/D-10)"
    - "위험 결함 카드는 게임 프레임 제외 — 차분한 안전 톤 + 왜 위험한지 + 강사와 함께 보기 유도 (D-14)"
    - "카드 상호작용(물어보기·점프)이 안정 recordId를 사용한다 — index 조인은 legacy 폴백만 (리뷰 반영)"
    - "드릴다운 시트가 같은 3단+문구집 원칙을 따른다 (D-15)"
  artifacts:
    - path: "app/src/components/DeductionCard.tsx"
      provides: "3단+인라인 줌+게이지+미션 완결 카드 (props 로컬 타입)"
    - path: "app/src/lib/gaugeGeometry.ts"
      provides: "게이지 스케일·채움 비율 순수 계산 (의미 정의 + node --test)"
      exports: ["computeGaugeGeometry"]
    - path: "app/src/components/GoalGaugeBar.tsx"
      provides: "실측 단위 목표 게이지 (트랙+채움 View, % 환산 없음, 표현 불가 시 null)"
    - path: "app/src/components/MissionBadge.tsx"
      provides: "오늘의 미션·mode3 기록 갱신 배지"
  key_links:
    - from: "app/src/components/DeductionCard.tsx"
      to: "app/src/components/GoalGaugeBar.tsx + MissionBadge.tsx"
      via: "카드 내 합성"
      pattern: "GoalGaugeBar"
    - from: "app/src/components/GoalGaugeBar.tsx"
      to: "app/src/lib/gaugeGeometry.ts"
      via: "채움 비율·마커 위치 계산"
      pattern: "computeGaugeGeometry"
---

<objective>
번역 레이어의 핵심 표면 — 감점 카드를 3단 문장+인라인 줌+게이지+미션의 완결형으로 재구성한다(D-08/D-20/D-10). 위험 결함 카드는 게임 프레임을 빼고 안전 톤으로(D-14), 드릴다운 시트는 같은 3단 원칙(D-15). result.tsx 배선은 32-11 — 이 플랜은 컴포넌트만(interface-first).

리뷰 HIGH 반영: 게이지는 정의된 수학(스케일·마커·채움 비율)을 순수 함수로 분리해 테스트하고, 규칙 상수(tolerance)가 없는 record는 게이지를 그리지 않는다 — D-09가 막으려던 자의적 숫자를 시각 비율로 재생산하지 않기 위함.

Purpose: "현재 94° → 기준 71°로 바꾸라"가 해석 불가라는 belle 지적(OPTIMAL 악순환 지점)의 직접 해소.
Output: DeductionCard/GoalGaugeBar/MissionBadge/gaugeGeometry 신규 + DeductionDetailSheet·InjuryRiskSection 개편.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-result-readability-3-omni/32-CONTEXT.md
@.planning/phases/32-result-readability-3-omni/32-PATTERNS.md
@.planning/phases/32-result-readability-3-omni/32-GATE-DECISIONS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: gaugeGeometry 순수 함수 + GoalGaugeBar + MissionBadge (D-10/D-09)</name>
  <files>app/src/lib/gaugeGeometry.ts, app/src/lib/__tests__/gaugeGeometry.test.ts, app/src/components/GoalGaugeBar.tsx, app/src/components/MissionBadge.tsx</files>
  <read_first>
    - .planning/phases/32-result-readability-3-omni/32-GATE-DECISIONS.md §목업 게이트 (D-10 확정 강도·범위 — 이 확정이 배지 노출 범위의 단일 출처)
    - app/src/app/analysis/result.tsx:536-545 (SegmentRow 트랙+채움 View 방식 — 게이지 1차 아날로그. SVG는 눈금/목표 마커 필요 시만)
    - app/src/components/AccuracyLimitBadge.tsx (배지 전형 — visible prop/null 숨김/카피 상수 분리/토큰/accessibility)
    - app/src/types/analysis.ts:523-534 (DeductionRecord — measured/target/unit/tolerance 32-06 계약 확장분)
    - app/src/theme/typography.ts (32-07 신규 토큰 — 소형 배지용 caption 계열)
  </read_first>
  <behavior>
    - Test 1 (스케일 정의): computeGaugeGeometry(current, target, tolerance) → {ratio, targetRatio, tolBandStart, tolBandEnd} — 스케일 도메인 = [min(current,target)−tolerance, max(current,target)+tolerance]. current/target/tol=94/71/20이면 도메인 [51,114], 각 비율이 도메인 내 선형 위치와 일치
    - Test 2 (경계): current == target → ratio == targetRatio (거리 0). tolerance 0/음수/NaN → null 반환 (게이지 불가)
    - Test 3 (불가 케이스): current/target 비유한(NaN/Infinity/부재) → null 반환 — 호출측이 게이지 생략
    - Test 4 (방향 무가정): current < target(늘려야)과 current > target(줄여야) 양쪽에서 비율이 대칭적으로 유효 (방향은 시각 표기만 다름)
  </behavior>
  <action>
    per D-10/D-09 + 리뷰 HIGH(게이지 의미 정의). (1) gaugeGeometry.ts 신설 — "순수 함수만, react 의존 0" 헤더. export computeGaugeGeometry(current: number, target: number, tolerance: number): {ratio: number; targetRatio: number; tolBandStart: number; tolBandEnd: number} | null. 의미(파일 헤더에 명문화): 도메인 = [min(current,target)−tolerance, max(current,target)+tolerance] — 현재와 목표를 모두 포함하고 규칙 허용 오차만큼 여유를 둔 실측 스케일. 모든 비율은 이 도메인의 선형 위치(내부 계산 전용 — 사용자 노출은 단위 원문). tolerance는 백엔드가 규칙 상수에서 방출한 record.tolerance만 사용(임의 기본값 생성 금지 — 없으면 null = 게이지 없음. D-09 자의 수치 금지의 시각 적용). (2) GoalGaugeBar.tsx — props {current: number, target: number, tolerance?: number, unit?: string, direction?: 'increase'|'decrease'}. computeGaugeGeometry가 null이면 **컴포넌트가 null 반환**(게이지 미표시 — 호출측 DeductionCard가 수치 배지+텍스트만 렌더). 렌더: 트랙+채움 View(SegmentRow 방식) + 목표 마커 + 허용 오차 밴드 표시 + 현재 위치. 표시 텍스트는 % 금지 — 단위 원문 소형 표기만(unit 문자열 그대로 병기 — 'deg'는 '°' 표기 매핑, 그 외 unit은 원문. 렌더 지원 unit 화이트리스트 밖이어도 게이지 자체는 기하 유효 시 표시하되 단위 라벨은 원문 — 리뷰 반영: enum 강제로 실존 unit을 배제하지 않음). 소형 수치 배지 1곳(예: "94°→71°" — belle 원문 "측정기준 94도→71도 이런 식으로 작게나마") — 카드에서 이 게이지의 배지가 유일한 수치 노출점이 되도록 주석 명시. (3) MissionBadge.tsx — variant: 'mission'(오늘의 미션) | 'record'(mode3 기록 갱신). AccuracyLimitBadge 전형 복제(visible/null/토큰/accessibility). 카피는 상수 분리, 톤 = 친숙하되 장난스럽지 않게(D-12). 게이지·미션·기록 갱신 배지(record variant)는 D-10 확정 3요소이므로 항상 구현(강등·미노출 금지 — 단 게이지는 데이터 불충분 record에서 정직하게 생략) — GATE-DECISIONS 확정은 적용 범위(감점 카드 밖 어느 표면까지 확장하는지)만 반영하고 채택 범위를 파일 헤더 주석에 인용.
  </action>
  <verify>
    <automated>node --test app/src/lib/__tests__/gaugeGeometry.test.ts && cd app && npm run typecheck && grep -c "%" src/components/GoalGaugeBar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - gaugeGeometry behavior 4건 pass — 스케일 도메인 정의가 테스트로 고정
    - GoalGaugeBar가 geometry null에서 null 반환 (게이지 불가 = 미표시)
    - GoalGaugeBar 표시 텍스트에 % 문자 0 (스타일 width 계산 내부 제외 — 사용자 노출 문자열 기준, grep 확인 후 SUMMARY 기록)
    - MissionBadge가 visible=false에서 null 반환
    - 두 컴포넌트 스타일 전부 토큰 참조 (색 리터럴 0)
    - typecheck clean
  </acceptance_criteria>
  <done>게이지가 정의된 수학+규칙 상수 기반으로만 그려지고, 불가 케이스는 정직 생략 — D-09 준수로 성립</done>
</task>

<task type="auto">
  <name>Task 2: DeductionCard — 3단+인라인 줌(상태 처리)+게이지+미션 완결 카드 (D-08/D-20)</name>
  <files>app/src/components/DeductionCard.tsx</files>
  <read_first>
    - app/src/components/ScoreBreakdownSection.tsx (opt-in prop 확장·record 행·Pressable 드릴다운 진입 :114-129·카드 스타일 :169-176 — 정본 아날로그)
    - app/src/app/analysis/result.tsx (faultZoomComparisons 이미지 렌더 기존 방식 — 줌 쌍 URI 소비 형태 실측; pickCompareFrames :855)
    - app/src/types/analysis.ts:523-534 (DeductionRecord — recordId/statusLine 등 32-06 계약 확장분)
    - .planning/phases/32-result-readability-3-omni/32-GATE-DECISIONS.md (실물 게이트 D-17 자세 카드·목업 게이트 D-10 범위)
  </read_first>
  <action>
    per D-08/D-20/D-09. DeductionCard.tsx 신설 — props 로컬 타입 {record: {recordId?, label, statusLine?, whyLine?, cueLine?, points, measured?, target?, unit?, tolerance?}, zoomPair?: {userUri, refUri}, mission?: {isMission: boolean, isSafety: boolean}, expanded?: boolean, onToggle?, onAskCoach?: (recordId: string | null) => void}. 렌더 순서: 상태문(몸 말 헤드라인 — 수치 0) → 이유문 1줄 → 인라인 줌 쌍(내/정은지 나란히, zoomPair 있을 때만 — D-20 카드 완결) → GoalGaugeBar(record의 measured/target/tolerance/unit로 조립 — 소형 수치 배지 이 1곳. tolerance 부재 등 게이지 불가면 GoalGaugeBar가 null → 수치 배지("94°→71°" 소형)+텍스트만 폴백 렌더) → 행동문(외부 큐 — 시각 강조, 목업 확정 강조 체계. cueLine 부재(fail-closed record)면 행동문 생략 + '강사님께 물어보기' 유도) → MissionBadge(mission.isMission && !mission.isSafety — D-14 안전 미션 게임 제외) → '강사님께 물어보기' 버튼(onAskCoach(record.recordId ?? null) — D-28 사용자 담기 진입점, **recordId 기반 조인** — 리뷰 반영: index 전달 금지). **줌 이미지 상태 처리(리뷰 반영):** 로딩 중 placeholder(토큰 배경 skeleton View), onError 시 해당 이미지 숨김+"이미지를 불러오지 못했어요" 소형 캡션(만료 presigned URL 대응 — 크래시·빈 깨짐 0). statusLine 부재(legacy doc) 폴백: 기존 라벨+행동구 조합으로 강등 렌더(크래시 0). 접힘 모드(expanded=false)는 상태문 1줄+펼침 유도만(순서안 #5 — 나머지 카드 기본 접힘). ScoreBreakdownSection 카드 스타일·accessibility 관례. 신규 토큰 사용.
  </action>
  <verify>
    <automated>cd app && npm run typecheck && grep -cE "(statusLine|cueLine)" src/components/DeductionCard.tsx && grep -c "onError" src/components/DeductionCard.tsx</automated>
  </verify>
  <acceptance_criteria>
    - DeductionCard가 3단 순서(statusLine→whyLine→cueLine) 렌더 + zoomPair 인라인 + GoalGaugeBar 합성
    - 줌 이미지 로딩 placeholder + onError 폴백 존재 (grep onError ≥ 2 — 양측 이미지)
    - statusLine 부재 시 legacy 폴백 분기 + cueLine 부재(fail-closed) 시 행동문 생략 분기 존재
    - onAskCoach 시그니처가 recordId 기반 (grep "recordId" ≥ 2)
    - 헤드라인 텍스트 조립에 수치 삽입 0 (수치는 게이지 배지 또는 폴백 수치 배지 1곳 경유만 — 소스 주석으로 명시)
    - isSafety=true에서 MissionBadge 미렌더 (조건 분기 grep)
    - typecheck clean
  </acceptance_criteria>
  <done>상태+증거(상태 처리 포함)+게이지+미션이 카드 하나에 완결 — recordId 조인으로 배선 대기</done>
</task>

<task type="auto">
  <name>Task 3: DeductionDetailSheet 3단화 (D-15) + InjuryRiskSection 안전 톤 (D-14)</name>
  <files>app/src/components/DeductionDetailSheet.tsx, app/src/components/InjuryRiskSection.tsx</files>
  <read_first>
    - app/src/components/DeductionDetailSheet.tsx (수정 대상 — 현행 실측 근거 렌더 구조 실측)
    - app/src/components/InjuryRiskSection.tsx (수정 대상 — 현행 카피·구조)
    - app/src/lib/deductionLabels.ts (라벨·의미 사전 — 시트가 import 중인 기존 층)
    - app/src/lib/terminologyMap.ts (32-05 산출 — 용어 심사 언어 적용)
  </read_first>
  <action>
    per D-15/D-14. (1) DeductionDetailSheet — 상단에 같은 3단(상태→왜→행동, record의 statusLine/whyLine/cueLine 소비, 부재 시 기존 렌더 유지) 추가, 그 아래 기존 투명 감점 내역(측정값·기준·편차·규칙 — scoring-must-be-transparent-deduction-tally 불변, 수치 삭제 금지·계층화만)을 "자세한 측정 내역" 블록으로 유지. 용어는 terminologyMap 적용. (2) InjuryRiskSection — 카피를 차분한 안전 톤으로 개정: 위험 결함명 + 왜 위험한지 1줄(문구집 safetyEntries whyLine 소비 가능 시) + "혼자 고치지 말고 강사와 이 화면을 함께 보세요" 코치 유도 문구. 게임 프레임 요소(게이지·미션·배지) 미포함 — D-14 미션화 금지. 기존 스타일 관례 유지, 카피 상수 분리.
  </action>
  <verify>
    <automated>cd app && npm run typecheck && grep -c "강사" src/components/InjuryRiskSection.tsx</automated>
  </verify>
  <acceptance_criteria>
    - DeductionDetailSheet에 3단 블록 + 기존 측정 내역 블록 공존 (수치 내역 삭제 0 — 기존 편차표 렌더 잔존 grep)
    - InjuryRiskSection에 코치 유도 문구 존재 + GoalGaugeBar/MissionBadge import 0
    - typecheck clean
  </acceptance_criteria>
  <done>드릴다운·위험 카드가 각자의 원칙(3단 계층화 / 안전 톤 게임 제외)으로 개편됨</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doc 필드 → 카드 렌더 | 방출 문구·줌 URI를 그대로 표시 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-22 | Tampering(신뢰) | 카드 헤드라인·게이지 | mitigate | 수치 삽입 경로를 게이지/수치 배지 1곳으로 구조 격리(D-09) + 게이지 스케일은 규칙 상수(tolerance) 기반 정의 수학만 — 자의 비율 생성 불가(geometry null = 미표시) |
| T-32-23 | DoS(크래시) | legacy doc·이미지 렌더 | mitigate | statusLine 부재 폴백 + zoomPair 옵셔널 + 이미지 onError 폴백 — 구 doc·만료 URL에서 크래시 0 |
| T-32-24 | Info Disclosure | 줌 이미지 URI | mitigate | 기존 presigned 소비 경로 재사용(신규 서명 표면 0) — userAnalyses normalizeResultKey 방어 기유지 |
</threat_model>

<verification>
- node --test gaugeGeometry.test.ts pass
- cd app && npm run typecheck clean
- 컴포넌트 grep 게이트 (acceptance 항목)
</verification>

<success_criteria>
- 감점 카드 표면 전체가 3단+완결형으로 준비 — 32-11 배선만 남음
- 게이지 의미가 테스트로 고정되고 불가 케이스는 정직 생략 — D-09/D-14 invariant가 컴포넌트 구조로 강제됨
</success_criteria>

<output>
Create `.planning/phases/32-result-readability-3-omni/32-10-SUMMARY.md` when done
</output>
