---
phase: 32-result-readability-3-omni
plan: 11
type: execute
wave: 6
depends_on: ["32-03", "32-09", "32-10"]
files_modified:
  - app/src/app/analysis/result.tsx
  - app/src/components/RecommendedExerciseModal.tsx
autonomous: true
requirements: [D-01, D-02, D-03, D-08, D-13, D-14, D-17, D-26, D-27, D-28]
must_haves:
  truths:
    - "결과 화면이 게이트 확정 순서(요약 카드 → 위험 → 오늘 고칠 것 상세 → 동작 비교 → 나머지 접힘 카드 → 성장/지난 미션 → 보완 운동 → 코치 질문 → 심사 정보 → 참고코너)로 재배치된다 (D-01/D-02)"
    - "요약 카드가 첫 화면이고 mode3 잘한 점 헤드라인 = 지난 미션 개선(사람 말) — mode3 헤드라인=발전 델타 invariant 유지 (D-26)"
    - "top-1 감점 카드가 DeductionCard 완결형으로, 나머지는 기본 접힘으로 렌더된다 (D-08 소비)"
    - "보완 운동이 top-1 연결 1개+이유 1줄 전면, '다른 운동 보기' 탭 시 가로 펼침 최대 3개 (D-13)"
    - "코치 질문 목록이 자동 수집+사용자 담기로 구성되고 탭하면 해당 카드로 점프한다 (D-28)"
    - "2회차 미개선 시 같은 미션 유지+보완 운동 우회 제안, 3회차 미개선 시 코치 카드가 전면 승격된다 (D-27)"
    - "참고 지표가 게이트 확정 형태(심사 정보 코너 또는 흡수)로 전환되고, 참고코너는 채점 표면 뒤 유지 (D-03 / 31 D-09 invariant)"
    - "자세 비교 카드·동작 비교 형태가 실물 게이트(D-17) 확정대로 반영된다"
  artifacts:
    - path: "app/src/app/analysis/result.tsx"
      provides: "재배치된 결과 화면 본체"
      min_lines: 2000
  key_links:
    - from: "app/src/app/analysis/result.tsx"
      to: "SummaryCard/DeductionCard/summarySource/cueTrack"
      via: "doc 필드 → props 조립"
      pattern: "SummaryCard"
    - from: "app/src/app/analysis/result.tsx"
      to: "ReferenceCornerSection"
      via: "채점 표면 뒤 배치 유지 (31 D-09)"
      pattern: "ReferenceCornerSection"
---

<objective>
result.tsx 대배선 — 지금까지 만든 컴포넌트(SummaryCard/DeductionCard/게이지/배지/코치마크/cueTrack)와 방출 필드(3단 문구·미션·질문)를 게이트 확정 순서로 통합한다. 보완 운동 개편(D-13)·코치 질문 강화(D-28)·참고 지표 표현 전환(D-03)·실물 게이트 확정 반영(D-17)을 포함한다.

Purpose: phase 32 UI 본체의 결합점 — "어떻게 읽으라는 건지 모르겠다"(belle §10)를 끝내는 화면.
Output: 재배치된 result.tsx + 개편된 RecommendedExerciseModal.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-result-readability-3-omni/32-CONTEXT.md
@.planning/phases/32-result-readability-3-omni/32-PATTERNS.md
@.planning/phases/32-result-readability-3-omni/32-GATE-DECISIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 상부 재배치 — 요약 카드·위험 승격·top-1 상세·동작 비교 상향·코치마크 (D-01/D-02/D-14/D-26/D-07/D-17)</name>
  <files>app/src/app/analysis/result.tsx</files>
  <read_first>
    - app/src/app/analysis/result.tsx (수정 대상 전체 — 현행 섹션 순서 실측: 점수 게이지→부상 위험→성공 축하→점수 계산 내역→구간별→동작 비교→코칭 팁→강사 확인→보완 운동→참고코너→참고 지표. isCleanPass 게이트·buildDeductionMarkers·pickCompareFrames 기존 로직 위치 파악)
    - .planning/phases/32-result-readability-3-omni/32-GATE-DECISIONS.md (D-02 확정 순서 + D-17 확정: 동작 비교 형태·자세 카드 존폐·큐 밀도 — 이 파일이 배치의 단일 출처)
    - app/src/lib/summarySource.ts + app/src/components/SummaryCard.tsx (32-07 산출 — props 계약)
    - app/src/lib/userAnalyses.ts (32-06 normalize — mission/missionOutcome/coachQuestions 소비 형상)
  </read_first>
  <action>
    per D-01/D-02/D-14/D-26/D-07/D-17. 게이트 확정 순서로 상부 재구성: (1) 최상단 = SummaryCard — deriveSummaryContent(doc 필드 조립: missionOutcome/mission/dimensionScores/records/safetyFlags 수)로 props 생성. mode3 잘한 점 헤드라인 = 지난 미션 개선(사람 말), 수치는 evidenceBadge(D-09). 기존 점수 게이지(OctagonScore)는 요약 카드 내 소형 보조 또는 게이트 확정 위치로 강등. mode1/mode3 차별화 세부는 재량이되 mode3 헤드라인=발전 델타 invariant. (2) 위험 결함(InjuryRiskSection 개편본)을 요약 카드 바로 아래 승격(있을 때만 — D-14 트리아지). (3) top-1 감점 카드 = DeductionCard 완결형(expanded, zoomPair는 faultZoomComparisons에서 해당 record 매칭, gauge는 record의 측정값·기준·unit로 조립, mission 표식은 result.mission과 criterion 일치로) — 순서안 #3. (4) 동작 비교(VideoCompare)를 #4로 상향 + cueWindows 배선(records의 cueLine + faultZoom userFrameIdx + 각자 fps — cueTrack.buildCueWindows, cueLine 부재 legacy는 기존 행동구 폴백) + D-17 확정 형태(비교 레이아웃·자세 비교 카드 존폐 — 존치 확정 시 기존 PoseCompareFrames 유지, 제거 확정 시 렌더 제거) 반영. (5) 첫 1회 코치마크 — hasSeenResultCoachmark 체크 후 ResultCoachmarks 표시+mark. isCleanPass(감점 0) 게이트 기존 로직과 정합 유지(축하 케이스에서 요약 카드가 자연스럽게 — 목업 최악 케이스 확정안대로).
  </action>
  <verify>
    <automated>cd app && npm run typecheck && grep -n "SummaryCard" src/app/analysis/result.tsx | head -3</automated>
  </verify>
  <acceptance_criteria>
    - result.tsx 렌더 트리에서 SummaryCard가 첫 콘텐츠 섹션 (JSX 순서 확인)
    - InjuryRiskSection이 SummaryCard 직후 조건 렌더
    - VideoCompare 호출부에 cueWindows prop 전달 + initialOffsetSec(32-02) 유지
    - 코치마크 1회 플래그 배선 (hasSeenResultCoachmark grep)
    - typecheck clean
  </acceptance_criteria>
  <done>첫 화면이 요약 1장→위험→오늘 고칠 것→증거(비교) 흐름으로 재편됨</done>
</task>

<task type="auto">
  <name>Task 2: 하부 재배치 — 접힘 카드·성장/미션 확인·보완 운동 가로·코치 질문·참고 지표 전환 (D-03/D-13/D-27/D-28)</name>
  <files>app/src/app/analysis/result.tsx, app/src/components/RecommendedExerciseModal.tsx</files>
  <read_first>
    - app/src/app/analysis/result.tsx:2009-2028 (강사에게 확인할 점 기존 섹션 — D-28 강화 대상), :2082-2088 (참고코너 배치 invariant 주석 — 31 D-09 위반 금지), :2139 부근 (참고 지표 섹션 — D-03 전환 대상)
    - app/src/components/RecommendedExerciseModal.tsx (수정 대상 — 현행 5개 세로 나열 구조)
    - .planning/phases/32-result-readability-3-omni/32-GATE-DECISIONS.md §목업 게이트 (D-03 확정 형태 — 심사 코너 vs 흡수)
    - docs/research/폴스포츠-지식.md (심사 정보 코너 콘텐츠 소스 — IPSF 감점 분류 실존 수치)
  </read_first>
  <action>
    per D-03/D-13/D-27/D-28. (1) 나머지 감점 카드 = DeductionCard 접힘 모드 목록(기본 접힘, 탭 펼침 — 순서안 #5. 기존 ScoreBreakdownSection의 투명 내역은 펼침 상세/드릴다운 경로에 유지 — 수치 삭제 금지). (2) 성장·지난 미션 확인 섹션(mode3) — missionOutcome 표시(개선/미개선 정직, 미션→연습→확인 루프 폐쇄 — 헤드라인은 요약 카드가 담당하므로 여기는 상세). 3회 미개선(mission.escalation=='coach_card') 시 코치 카드 전면 승격 렌더("혼자 안 되는 건 자세가 아니라 방법 문제일 수 있어요" 톤 + 물어볼 질문 연결 — D-27). (3) 보완 운동 — 전면 top-1 결함 연결 운동 1개 + exerciseReason 1줄 필수, '다른 운동 보기' 탭 시 가로 스크롤 최대 3개(RecommendedExerciseModal 개편 또는 인라인 전환 — 5개 세로 나열 폐지, D-13). mission.escalation=='exercise_detour'(2회차 미개선)면 같은 미션 유지 + 이 섹션 상단에 보완 운동 우회 제안 카피("이 운동부터 해보면 쉬워져요" 톤 — 카피 상수 분리)를 표시하는 분기 추가 — D-27 2회차. (4) 코치 질문 목록 — result.coachQuestions(자동 수집분) + 사용자 담기(DeductionCard onAskCoach → 로컬 state 추가, source='user') 통합 렌더, 질문 탭 → 해당 카드로 scrollTo 점프(recordIndex 활용). 기존 openQuestionsForCoach 섹션을 이 구조로 강화. (5) 참고 지표 — 게이트 확정 형태로 전환: 심사 정보 코너 확정 시 "실제 심사는 이렇게 파악한다" 콘텐츠(폴스포츠-지식.md 실존 수치)로 재작성 + terminologyMap 적용, 흡수 확정 시 섹션 제거 후 top-1 카드에 통합. 어느 쪽이든 '안정성' 류 추상 용어 표면 제거(D-12 일괄 적용). (6) 참고코너(ReferenceCornerSection)는 채점 표면 뒤 위치 불변(31 D-09 — 주석 유지).
  </action>
  <verify>
    <automated>cd app && npm run typecheck && grep -c "coachQuestions" src/app/analysis/result.tsx</automated>
  </verify>
  <acceptance_criteria>
    - 보완 운동 전면 렌더가 1개+이유 1줄, 확장 시 가로(horizontal) 최대 3 (5개 세로 나열 코드 제거)
    - coachQuestions 소비 + 질문 탭 점프 핸들러 존재
    - escalation=='coach_card' 분기 렌더 존재
    - escalation=='exercise_detour' 분기(보완 운동 우회 제안 카피) 렌더 존재
    - ReferenceCornerSection 위치가 채점 표면 뒤 유지 (주석 블록 잔존 grep "31 (D-09)" 또는 동등)
    - '안정성' 리터럴이 사용자 노출 문자열에서 제거 (terminologyMap 경유 — grep "'안정성'" 직접 리터럴 감소 확인)
    - typecheck clean
  </acceptance_criteria>
  <done>하부 전 섹션이 게이트 확정 형태로 전환되고 미션 루프·코치 출구가 화면에서 닫힘</done>
</task>

<task type="auto">
  <name>Task 3: 최악 데이터 케이스 렌더 확인 (시뮬레이터)</name>
  <files>(코드 수정 최소 — 발견 결함 즉시 수리)</files>
  <read_first>
    - .planning/phases/32-result-readability-3-omni/32-VALIDATION.md §Manual-Only Verifications (시뮬레이터 확인 절차)
  </read_first>
  <action>
    시뮬레이터에서 결과 화면을 데이터 케이스별로 확인: (1) 정상 mode1(감점 다수) — 요약→위험→top-1→비교 흐름, (2) 감점 0(isCleanPass — 축하 케이스), (3) legacy doc(3단 문구·미션 부재 — 폴백 렌더 크래시 0), (4) mode3(missionOutcome 표시). Firestore 실 doc 또는 시뮬레이터 내 기존 분석 활용(firebase MCC/에뮬레이션 불필요 — 기존 계정 doc). 발견 결함은 이 태스크에서 즉시 수리(fix-now 원칙). 각 케이스 스크린샷을 SUMMARY에 기록. OTA는 이 플랜에서 발행하지 않음(32-12 마감에서 일괄 — 오디오·실패 UX 포함 후).
  </action>
  <verify>
    <automated>cd app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - 4개 데이터 케이스 스크린샷 확보 (SUMMARY 경로 기록)
    - legacy doc 케이스에서 크래시·빈 화면 0
    - typecheck clean 유지
  </acceptance_criteria>
  <done>재배치된 화면이 최악 케이스 포함으로 시뮬레이터 검증됨</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doc → 화면 전체 | 신규 필드 대량 소비 — legacy 하위호환이 방어선 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-25 | DoS(크래시) | legacy doc 렌더 | mitigate | normalize undefined 강등(32-06) + 컴포넌트 폴백 분기(32-10) + Task 3 legacy 케이스 실렌더 확인 |
| T-32-26 | Tampering(신뢰) | 요약 헤드라인 | mitigate | summarySource 소스 enum 밖 생성 불가 + mode3 헤드라인=발전 델타 invariant 배선 확인 |
| T-32-27 | Repudiation | 31 결정 위반 | mitigate | 참고코너 뒤 배치·"3D/회전" 문구 금지 유지 — acceptance에 주석 잔존 확인 |
</threat_model>

<verification>
- cd app && npm run typecheck clean
- 시뮬레이터 4케이스 스크린샷
- node --test 기존 lib 테스트 무회귀 (summarySource/cueTrack/manualOffset)
</verification>

<success_criteria>
- 결과 화면이 게이트 확정 순서·형태로 완전 재편 — belle §10 "어떻게 읽으라는 건지"의 구조적 해소
- 미션→연습→확인 루프와 코치 출구가 화면에서 닫힘
</success_criteria>

<output>
Create `.planning/phases/32-result-readability-3-omni/32-11-SUMMARY.md` when done
</output>
