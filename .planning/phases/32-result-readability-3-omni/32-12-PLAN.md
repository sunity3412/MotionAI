---
phase: 32-result-readability-3-omni
plan: 12
type: execute
wave: 7
depends_on: ["32-08", "32-11"]
files_modified:
  - app/package.json
  - app/package-lock.json
  - app/app.json
  - app/src/lib/audioCue.ts
  - app/src/components/VideoCompare.tsx
  - app/src/app/analysis/loading.tsx
  - app/src/app/analysis/result.tsx
  - app/assets/illustrations/
  - backend/functions/pipeline/app.py
  - backend/functions/playback-url/app.py
  - backend/template.yaml
  - backend/shared/python/sunity_shared/firestore_admin.py
  - backend/shared/python/sunity_shared/models.py
  - docs/contract.md
  - app/src/types/analysis.ts
  - app/src/lib/userAnalyses.ts
  - backend/tests/phase32/test_coach_audio.py
autonomous: false
requirements: [D-18, D-21, D-29, D-30, D-09, D-23]
must_haves:
  truths:
    - "재생 중 큐가 자막+오디오로 제공되고 오디오는 설정 on/off 가능하다 — 방식은 샘플 게이트 확정안이며, A/B 어느 확정이든 오디오가 이번 phase 안에서 출시 완결된다 (D-18 동시 출시)"
    - "native 모듈 추가가 올바른 배포 경로로 출시된다: 설치 → app 버전 bump(1.0.0→1.1.0, runtimeVersion=appVersion 정합) → EAS build → TestFlight 제출 → belle 새 빌드 설치 → 신규 runtime 대상 OTA — 기존 바이너리에 OTA 단독 배포 금지 (리뷰 blocker 3)"
    - "wave 7이 D-23 비준 매핑(32-03)을 자기 게이트로 충족한다: B안(백엔드 변경) 확정 시 배포 후 fixture 6동작 전수 스윕, A/B 공통으로 6동작 doc 전수 렌더 확인 (W-1)"
    - "B안 확정 시 백엔드 변경(template.yaml IAM·playback-url)이 SAM 배포로, 파이프라인 합성 스테이지가 Pod 배포로 프로덕션에 도달하고 6동작 스윕+playback 스모크가 OTA 전에 수행된다 (리뷰 blocker 2/3) — B안 백엔드 태스크는 게이트 확정 직후 별도 플랜 32-16으로 물리 분리 실행 (W-2 스코프 통제)"
    - "B안 오디오는 cueId(=recordId)로 mp3와 조인되고, 재생 URL은 화면 진입 시 prefetch·캐시된다 — 큐 시점 네트워크 지연으로 자막·음성이 어긋나지 않음 (리뷰 반영)"
    - "부분 실패 시 측정 커버리지가 정직 고지되고 잰 범위 내 미션+재촬영 팁 1줄이 표시된다 (D-29)"
    - "전체 실패 화면(not_pole·no_human 등) 카피가 친숙·응원 톤+다음 행동 1개로 정비되고 재업로드 동선이 점검된다 (D-30)"
    - "일러스트는 게이트 확정대로 도입 또는 실프레임+텍스트 폴백 (D-21)"
    - "전 화면 D-09 검증(파일 목록 기반 grep 1차 + 화면별 수동 점검표 2차) 통과 후 새 TestFlight 빌드에서 belle 실기기 확인 — 확인 대상에 신규 계약 실 doc 포함"
  artifacts:
    - path: "app/src/lib/audioCue.ts"
      provides: "오디오 큐 어댑터 (확정 방식 — cue 객체 시그니처·발화 중단·설정 게이트·prefetch 포함)"
      exports: ["speakCue", "stopCue", "prefetchCueAudio", "isAudioCueEnabled", "setAudioCueEnabled"]
  key_links:
    - from: "app/src/components/VideoCompare.tsx"
      to: "app/src/lib/audioCue.ts"
      via: "자막 큐 표시 시점과 동일 트리거 (cue 객체 전달)"
      pattern: "speakCue"
---

<objective>
UI 본체 마감 — 샘플 게이트 확정 방식으로 오디오 큐를 구현하고(D-18), 실패 UX(D-29/D-30)를 정비하고, 전 화면 D-09 준수를 검증한 뒤 **올바른 native 배포 경로(버전 bump → EAS build → TestFlight → 신규 runtime OTA)**로 belle 실기기 확인까지 완료한다.

리뷰 blocker 3 반영: expo-speech/expo-audio는 native 모듈 — 기존 TestFlight 바이너리(runtime 1.0.0)에 OTA만으로 배포 불가. app.json version bump로 새 runtime을 만들고 EAS build+TestFlight 제출을 이 플랜의 필수 단계로 넣는다. blocker 2 반영: B안의 Lambda-전용 표면(template.yaml/playback-url)은 SAM 배포 + 스모크를 OTA 전에 수행.

체커 W-1 반영: wave 7은 32-03에서 belle이 비준하는 D-23 매핑의 적용 대상 — B안(백엔드 변경) 확정 시 배포 후 6동작 전수 스윕(Task 2), A/B 공통으로 6동작 doc 전수 렌더 확인(Task 4)을 이 플랜의 게이트로 포함한다. 체커 W-2 반영: B안 확정 시 Task 2(백엔드 TTS 스테이지+SAM 표면)는 컨텍스트 예산 통제를 위해 게이트 확정 직후 별도 플랜 **32-16**으로 물리 분리해 wave 7에서 실행한다(32-08 게이트 산출 — 분리는 실행 단위 분할일 뿐, D-18 phase 내 완결 불변).

Purpose: phase 32 UI 크리티컬 패스의 출시 상태 완성. 엔진 레버(32-13~15)는 이 뒤.
Output: 오디오 큐 + native build/TestFlight + 실패 UX 카피 + D-09 게이트 + belle 확인.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-result-readability-3-omni/32-CONTEXT.md
@.planning/phases/32-result-readability-3-omni/32-GATE-DECISIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 오디오 큐 구현 + native 빌드 파이프라인 (설치→버전 bump→EAS build→TestFlight) (D-18/D-21)</name>
  <files>app/package.json, app/package-lock.json, app/app.json, app/src/lib/audioCue.ts, app/src/components/VideoCompare.tsx, app/assets/illustrations/</files>
  <read_first>
    - .planning/phases/32-result-readability-3-omni/32-GATE-DECISIONS.md §샘플 게이트 (D-18 확정 A/B + D-21 일러스트 확정 — 구현 분기의 단일 출처)
    - .planning/phases/32-result-readability-3-omni/32-RESEARCH.md §재생 중 큐 + TTS (A안: Speech.speak + 이전 발화 stop / B안: Polly 사전 생성 + expo-audio 재생 + playback-url asset 확장)
    - app/app.json (version 1.0.0 + runtimeVersion policy appVersion 실측 — bump 대상), app/eas.json (build 프로필·appVersionSource remote — buildNumber는 원격 관리)
    - app/src/components/VideoCompare.tsx (32-08 자막 큐 트리거 지점 — 오디오는 같은 트리거, cue 객체에 recordId 있음)
    - app/src/lib/coachmark.ts (AsyncStorage 플래그 관례 — 오디오 on/off 저장 동일 구조)
  </read_first>
  <action>
    per D-18/D-21 + 리뷰 blocker 3. (1) 확정 모듈 설치 — 반드시 `cd app && npx expo install expo-speech`(A 확정 시) 또는 `npx expo install expo-audio`(B 확정 시). `npm install` 직접 실행 금지(npm dist-tag latest는 SDK 55 라인 — RESEARCH 경고). 설치 후 package.json 버전이 SDK 54 호환(~14.0.8 또는 ~1.1.1)인지 확인. **package-lock.json 변경을 커밋에 포함**(리뷰 반영 — EAS npm ci sync 함정). Package Legitimacy Audit: 두 후보 모두 [OK] Approved(expo 공식, github.com/expo/expo, SDK 54 번들 매니페스트 등재 — 32-RESEARCH 감사표). (2) **app.json version 1.0.0 → 1.1.0 bump**(runtimeVersion policy=appVersion → 새 runtime "1.1.0" 성립 — 기존 1.0.0 바이너리는 이후 신규 OTA를 받지 않아 native 모듈 부재 크래시로부터 보호됨. 이 정책을 SUMMARY에 기록: 이후 phase 32 앱 변경(32-13 포함)의 OTA는 1.1.0 runtime 대상). (3) audioCue.ts 어댑터 — 시그니처는 cue 객체 기반(리뷰 반영 — text 단독 금지): speakCue(cue: {cueId: string | null, text: string})/stopCue()/prefetchCueAudio(cues: {cueId, text}[]): Promise<void>/isAudioCueEnabled()/setAudioCueEnabled(bool). A안이면 Speech.speak(cue.text, {language:'ko-KR'}) + 새 큐 발화 전 이전 발화 stop 처리(prefetchCueAudio는 no-op). B안이면 expo-audio 재생 어댑터 — prefetchCueAudio가 화면 진입 시 cueId별 playback-url presigned URL을 일괄 발급받아 메모리 캐시(재생 시점 네트워크 0 — 자막·음성 동기), speakCue(cue)는 캐시된 cueId URL 재생. 백엔드 합성·저장·asset 확장은 Task 2 스펙 — **B안 확정 시 별도 플랜 32-16으로 분리 실행(W-2)**. 어느 확정이든 오디오는 이번 phase 안에서 출시 완결(D-18 동시 출시 — 이연 금지). 설정 키 '@sunity:audio_cue_enabled'(AsyncStorage, 기본 off — 학원 소음 환경), 토글 UI는 동작 비교 영역 내 소형 버튼(토큰·accessibility 관례). (4) VideoCompare — 자막 큐 전환 시점과 동일 지점에서 isAudioCueEnabled() 확인 후 speakCue({cueId: cue.recordId ?? null, text: cue.text}), 재생 정지·seek·언마운트 시 stopCue. 화면 진입(마운트+오디오 on) 시 prefetchCueAudio 호출. (5) 일러스트 — 게이트 확정 '도입'이면 승인 샘플을 app/assets/illustrations/에 번들하고 드릴다운 시트 표시 지점에 정적 렌더 추가(런타임 생성 0), '폴백'이면 코드 무산출(결과만 SUMMARY 기록). (6) **EAS build + TestFlight 제출:** typecheck·시뮬레이터 스모크 후 `eas build --platform ios --profile production` → `eas submit`(ASC 무인 자동화 — asc-app-id-and-api-key 관례). Android 직원 APK 재빌드는 파일럿 iOS 우선으로 이월 기록(known limitation — belle 필요 시 별도). 빌드·제출 결과(빌드 번호·제출 상태)를 SUMMARY에 기록.
  </action>
  <verify>
    <automated>cd app && npm run typecheck && grep -cE '"(expo-speech|expo-audio)"' package.json && grep -c '"version": "1.1.0"' app.json</automated>
  </verify>
  <acceptance_criteria>
    - package.json에 expo-speech 또는 expo-audio 정확히 1개 추가 (둘 다 금지) + package-lock.json 동반 커밋
    - app.json version == "1.1.0" (runtime bump)
    - audioCue.ts가 5개 export + cue 객체 시그니처(cueId 포함) + 이전 발화 stop 처리 포함
    - VideoCompare에서 speakCue/stopCue/prefetchCueAudio 호출 존재 + 오디오 off 시 자막만 동작
    - EAS build 완료 + TestFlight 제출 기록 (빌드 번호 SUMMARY)
    - typecheck clean
  </acceptance_criteria>
  <done>확정 방식의 오디오 큐가 설정 게이트·prefetch와 함께 동작하고 새 native 빌드가 TestFlight에 제출됨</done>
</task>

<task type="auto">
  <name>Task 2 (B안 조건부 — 확정 시 32-16으로 물리 분리 실행): Polly 합성 사후 스테이지 + S3 저장 + playback asset 확장 + SAM/Pod 배포·6동작 스윕 (D-18/D-23)</name>
  <files>backend/functions/pipeline/app.py, backend/functions/playback-url/app.py, backend/template.yaml, backend/shared/python/sunity_shared/firestore_admin.py, backend/shared/python/sunity_shared/models.py, docs/contract.md, app/src/types/analysis.ts, app/src/lib/userAnalyses.ts, backend/tests/phase32/test_coach_audio.py</files>
  <read_first>
    - .planning/phases/32-result-readability-3-omni/32-GATE-DECISIONS.md §샘플 게이트 (A 확정이면 이 태스크 전체 skip — skip 사유만 SUMMARY에 1줄 기록하고 백엔드 무접촉. **B 확정이면 이 태스크는 32-08 게이트 산출대로 별도 플랜 32-16으로 분리돼 있음 — 32-16이 이 스펙을 실행하고, 본 플랜에서는 중복 실행 금지·상호 참조만**)
    - backend/functions/playback-url/app.py:77-124 (_handle_asset H-02 정본 — 서버가 key 구성 + 저장 key exact 비교. 오디오 asset이 복제)
    - backend/functions/pipeline/app.py:5236-5320 (사후 스테이지 자리 — fault_zoom 패턴, 32-09가 수정한 최신 상태)
    - backend/shared/python/sunity_shared/firestore_admin.py:1138-1187 (update_analysis_fault_zoom — 사후 부분 업데이트 helper 뼈대)
    - .planning/phases/32-result-readability-3-omni/32-PATTERNS.md §E-3 (오디오 asset 확장 지침 — canonical key·_ASSET_CONTENT_TYPE mp3) + §SP-1/SP-3
    - backend/samconfig.toml (SAM deploy 기본값 — 스택 sunity-motion-pilot)
  </read_first>
  <action>
    per D-18 동시 출시 + D-23 (B안 확정 시에만 실행 — A 확정이면 skip. 이 스펙이 있으므로 어느 확정이든 오디오는 이번 phase 안에서 완결되고, B안 선택이 이연이 되지 않는다). **W-2 분리 노트: B안 확정 시 이 태스크의 스펙 전체는 게이트 확정 직후 신설되는 별도 플랜 32-16(wave 7, depends_on 32-08·32-11)으로 옮겨 실행한다 — 파일 17개 단일 플랜의 컨텍스트 예산 초과 방지. 본 플랜 실행 시점에 32-16이 존재하면 이 태스크는 '32-16에서 수행'으로 skip 기록.** (1) IaC: backend/template.yaml의 pipeline 함수 IAM에 polly:SynthesizeSpeech 1줄 추가 — 신규 시크릿 0(boto3 client("polly"), Seoyeon neural, ap-northeast-2 — RESEARCH 실계정 확인). (2) pipeline 사후 스테이지: status done 확정 후 with _stage(timings_ms, analysis_id, "coach_audio") 블록에서 records의 cueLine 문장들을 Polly 합성 → S3 `results/{uid}/{analysisId}/coach_audio_{recordId}.mp3` 저장(**키에 recordId 사용** — 리뷰 반영: cueId=recordId 조인) → 신규 helper update_analysis_coach_audio(fault_zoom 뼈대 복제 — "result.coachAudio" 단일 field-path)로 {status('done'|'failed'), items(list[{recordId, key}] — scalar dict 배열, safetyFlags 선례)} 부분 갱신. 합성 실패 = graceful(로그 후 통과 — 자막만 제공, 분석 무훼손 SP-3). 동기 경로 호출 금지(속도 예산). (3) playback-url _handle_asset에 오디오 asset 종류 확장 — 서버가 canonical key 구성 + 저장 items의 key exact 비교(H-02, prefix 부분일치 불가) + _ASSET_CONTENT_TYPE에 "mp3": "audio/mpeg". 기존 asset 경로 바이트 불변. (4) 계약 3면(COACH_AUDIO_KEYS 상수 + analysis.ts coachAudio? + contract.md 절 — **문서화 각주(리뷰 MEDIUM): 클라우드 TTS는 분석 시점 사전 생성으로, CONTEXT '런타임 신규 생성 AI 0'은 시각 생성(교정 이미지·회전 영상) OFF를 지칭 — belle이 D-18에서 명시 승인한 음성 합성 예외임을 1줄 기록**) + userAnalyses normalize(key는 results/ prefix 강등 — normalizeResultKey 재사용) atomic 커밋. (5) Task 1의 audioCue.ts prefetch가 playback-url 경유 presigned URL을 cueId별로 받아 캐시 재생 연결. (6) 테스트: 합성 스테이지 graceful(무자격/실패 시 done 유지) + asset guard(stale key·타 uid 404)를 backend/tests/phase32/test_coach_audio.py로. (7) **배포·게이트(리뷰 blocker 2/3 + W-1 — OTA 전 필수):** `sam build --use-container && sam deploy`(template.yaml IAM + playback-url 변경은 Lambda 표면 — Pod git pull로는 배포 안 됨) + Pod git pull·재시작(pipeline 합성 스테이지) + /health 200 → **fixture 6동작 전수 스윕(순차 — kip-up 편중 금지, D-23 배포 웨이브 게이트 — 32-03 비준 매핑상 B안 wave 7은 배포 웨이브)**: 각 doc의 coachAudio items(recordId 조인) 방출·validator 통과 + 대표 키 playback-url mp3 presigned 200 스모크 + 점수·verdict가 직전 기준선(32-09 또는 최신 스윕) 대비 diff 0(합성 스테이지는 사후 — 채점 무접촉 실증) + 동기 경로 timingsMs 회귀 0. 스윕 표를 SUMMARY에 기록.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/phase32 -x -q && cd ../app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - (B 확정 시) template.yaml에 polly:SynthesizeSpeech 존재 + _handle_asset에 mp3 content-type + exact 비교 가드 + 계약 3면 동시 수정(런타임 생성 예외 각주 포함)
    - (B 확정 시) coachAudio 키가 recordId 조인 형식 (coach_audio_{recordId}.mp3)
    - (B 확정 시) SAM deploy + Pod 배포 + **6동작 전수 스윕 표(전 doc coachAudio 방출 + 점수·verdict diff 0 + 동기 경로 회귀 0)** + playback mp3 스모크 200 기록 — OTA/실기기 확인 전 완료 (W-1)
    - (B 확정 시) 합성 실패 케이스에서도 status done 유지 + 자막 경로 무영향 (graceful 테스트 green)
    - (B 확정 시, 32-16 분리 실행) 본 플랜 SUMMARY에 '32-16에서 수행' 상호 참조 기록 — 중복 실행 0
    - (A 확정 시) 이 태스크 skip 사유 SUMMARY 1줄 + 이 태스크의 백엔드 파일 diff 0
  </acceptance_criteria>
  <done>B안 확정 시에도 오디오가 SAM/Pod 배포·6동작 스윕·스모크까지 이번 phase 안에서 완결 출시 — D-18 동시 출시 + D-23 배포 게이트 충족, 이연 없음</done>
</task>

<task type="auto">
  <name>Task 3: 실패 UX — 부분 실패 정직 고지 (D-29) + 전체 실패 카피 정비 (D-30)</name>
  <files>app/src/app/analysis/result.tsx, app/src/app/analysis/loading.tsx</files>
  <read_first>
    - app/src/app/analysis/loading.tsx (수정 대상 — 실패 카피 위치·에러 코드 분기(not_pole_motion/no_human/server_error)·재업로드 동선 실측. 에러 우선순위 로직 :303 부근)
    - app/src/app/analysis/result.tsx (부분 실패 고지 삽입 지점 — 32-09 collect_unmeasured_signals가 방출한 coachQuestions source='unmeasured'와 coverage 신호 소비. ScoreBreakdownSection footnote 정직 고지 패턴 :150-159가 아날로그)
    - backend/shared/python/sunity_shared/analysis/phrasebook.py (재촬영 팁·실패 카피 톤 참조 — 카피는 앱 상수)
  </read_first>
  <action>
    per D-29/D-30. (1) result.tsx 부분 실패 고지 — 커버리지 갭 존재 시(32-09가 고정한 실존 신호 — coachQuestions source='unmeasured' 항목 또는 records coverage_gap 계열) 요약 카드 아래 정직 고지 1블록: "이번엔 {잰 부위}만 확실히 잴 수 있었어요" 톤 + 재촬영 팁 1줄(온보딩 가이드 연결 — 기존 온보딩 화면 route 링크) — 잰 범위 내 미션 구성은 32-09 방출이 이미 담당(못 잰 것은 coachQuestions 자동 등재 — 표시 확인만). (2) loading.tsx 전체 실패 카피 — not_pole_motion/no_human/server_error 각 분기의 카피를 새 언어로: 친숙·응원 톤 + 다음 행동 1개(재촬영 가이드 또는 재업로드 버튼) — 구조 재설계 금지(D-30, 카피·톤만). 재업로드 동선이 실제로 동작하는지 점검(버튼 → analyze 화면 라우팅). 저화질 우선 안내 분기(기존 260704-fwb) 유지. 카피 상수 분리 + 스펙 인용 주석(32-CONTEXT D-30).
  </action>
  <verify>
    <automated>cd app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - result.tsx에 커버리지 정직 고지 조건 렌더 + 재촬영 팁 1줄 존재
    - loading.tsx 실패 분기 카피가 "다음 행동 1개" 구조 (각 분기에 행동 버튼/안내 1개)
    - 재업로드 동선 라우팅 확인 (코드 경로 추적 결과 SUMMARY 기록)
    - typecheck clean
  </acceptance_criteria>
  <done>부분·전체 실패가 정직 고지+다음 행동 구조로 정비됨</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: D-09 전 화면 검증 + 6동작 doc 전수 렌더 + 새 TestFlight 빌드 belle 실기기 확인 → 신규 runtime OTA 정착</name>
  <what-built>phase 32 UI 본체 전체 — 요약 카드·3단 감점 카드·게이지·미션 루프·코치 질문·재생 중 큐(자막+오디오)·실패 UX — 새 native 빌드(runtime 1.1.0)로 출시</what-built>
  <how-to-verify>
    Claude 선행 자동 절차(이 체크포인트 도달 전 완료):
    1. D-09 게이트 2단(리뷰 반영 — grep 단독의 오탐/미탐 보완): (a) 1차 grep — 결과 화면 계열 파일 목록(result.tsx/SummaryCard/DeductionCard/GoalGaugeBar/DeductionDetailSheet/InjuryRiskSection/VideoCompare/loading.tsx)에서 % 환산·헤드라인 수치 패턴 스캔, (b) 2차 수동 점검표 — 화면별(요약/top-1/접힘/드릴다운/위험/비교/심사 정보/실패) "헤드라인 수치 0·카드당 수치 1곳·% 0"을 시뮬레이터 스크린샷으로 체크. 백엔드 문구집 금지어 테스트 재실행
    2. 시뮬레이터 5케이스(신규 계약 doc/정상/감점0/legacy/mode3) 재확인 + 오디오 토글 동작 + **6동작 doc 전수 렌더 확인(W-1 — D-23 UI 웨이브 게이트, 32-03 비준 매핑): 32-09(및 B안이면 이번 스윕) 산출 6동작 doc 각각 결과 화면 진입·크래시 0·요약 카드 성립, kip-up 편중 금지**
    3. B안이면 Task 2(32-16 분리 실행분)의 배포·6동작 스윕·스모크 완료 확인. TestFlight 새 빌드(1.1.0)가 처리 완료 상태인지 확인
    4. 신규 runtime(1.1.0) 대상 OTA 발행(production+preview — 이후 JS 수정의 전달 경로 확립), 직전 정상 group id 기록
    belle 실기기 확인:
    5. TestFlight에서 **새 빌드(1.1.0) 설치** → 앱 실행 (기존 빌드에서의 확인은 무효 — native 모듈 부재)
    6. 확인 항목: 요약 카드 1장 첫인상 / 오늘 고칠 것 카드의 3단 문장이 읽히는가 / 게이지·미션이 D-10 확정 강도인가 / 재생 중 자막(+오디오 on 시 음성) 큐 / 코치 질문 목록 / 실패 화면(가능 시) — **확인 doc에 32-09 스윕 산출 신규 계약 doc 1건 포함**(리뷰: mock/legacy가 아닌 실데이터로 승인)
    7. 성공 판정 프레임(SEED §9): ① 자기 말로 문제 설명 가능한가 ② 다음 행동 하나를 말할 수 있는가 ③ 강사에게 물어볼 것이 생기는가
  </how-to-verify>
  <action>선행 자동 절차(D-09 2단 게이트 → 시뮬레이터 5케이스 + 6동작 doc 전수 렌더 → B안 배포·스윕 확인 → TestFlight 빌드 확인 → 신규 runtime OTA + 롤백 group 기록)를 완료한 뒤 belle 실기기 확인을 진행한다. 승인 또는 이슈를 수집하고, 이슈는 즉시 수리 후 재확인한다. 잔여 실기기 항목은 32-HUMAN-UAT.md에 적립한다.</action>
  <verify>
    <automated>cd app && npm run typecheck && npx eas update:list --branch production --limit 1 --non-interactive 2>/dev/null | head -3</automated>
  </verify>
  <acceptance_criteria>
    - D-09 1차 grep 위반 0 + 2차 화면별 점검표 완료 기록
    - 6동작 doc 전수 렌더 확인 기록 (동작별 진입 OK — W-1)
    - TestFlight 새 빌드(1.1.0) 제출·처리 + OTA 발행 group id + 롤백 대상 group id 기록
    - belle 실기기 승인 기록 — 신규 계약 실 doc 확인 포함 (또는 이슈 수리 후 승인)
  </acceptance_criteria>
  <done>UI 본체가 새 native 빌드+신규 runtime OTA로 6동작 렌더 게이트 포함 belle 실기기 승인을 받음</done>
  <resume-signal>"승인" 또는 발견 이슈 목록 (이슈는 즉시 수리 후 재확인 — fix-now 원칙)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm 레지스트리 → 앱 의존성 | 오디오 모듈 1개 설치 (예외 승인 항목) |
| EAS build/TestFlight → 사용자 기기 | 새 native 바이너리 배포 |
| OTA → 사용자 기기 | 신규 runtime 대상 JS 교체 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-SC | Tampering | npm install | mitigate | npx expo install만 사용 + 대상은 audit [OK] 2종 중 확정 1개 한정 + package.json/lock diff로 1개만 추가 확인 (acceptance) |
| T-32-28 | DoS | 배포 사고 | mitigate | runtime bump로 구 바이너리 보호(1.0.0은 native 의존 OTA 미수신) + 시뮬레이터 5케이스+6동작 렌더 + 빌드 경로 청결 + 직전 group 롤백 준비 + B안 6동작 스윕·스모크 선행 |
| T-32-29 | Info Disclosure | TTS 발화 내용 | mitigate | 발화 텍스트 = 문구집 큐 문장만(PII 0), 로컬 발화(A) 또는 서버 구성 key + exact 비교 presigned(B — Task 2 H-02) |
</threat_model>

<verification>
- cd app && npm run typecheck clean + node --test 기존 lib 테스트 무회귀
- D-09 grep+점검표 위반 0 + 문구집 금지어 테스트 green
- (B안) SAM/Pod 배포 + 6동작 전수 스윕 + 스모크 → EAS build/TestFlight 제출 + OTA 발행 + 6동작 doc 전수 렌더 + belle 실기기 승인
</verification>

<success_criteria>
- UI 크리티컬 패스 완결 — 자막+오디오 큐, 실패 UX, D-09 전 화면 준수, 새 native 빌드로 belle 실기기 승인 (리뷰 blocker 3 해소)
- wave 7이 D-23 비준 매핑 게이트(B안 스윕 + 6동작 렌더)를 자체 충족 (W-1)
- runtime 정책 확립: 이후 앱 OTA는 1.1.0 대상 (32-13이 이 경로 사용)
- HUMAN-UAT 적립 항목이 있으면 32-HUMAN-UAT.md에 기록 (batch-uat 관례)
</success_criteria>

<output>
Create `.planning/phases/32-result-readability-3-omni/32-12-SUMMARY.md` when done
</output>
