---
phase: 32-result-readability-3-omni
plan: 12
type: execute
wave: 7
depends_on: ["32-08", "32-11"]
files_modified:
  - app/package.json
  - app/src/lib/audioCue.ts
  - app/src/components/VideoCompare.tsx
  - app/src/app/analysis/loading.tsx
  - app/src/app/analysis/result.tsx
  - backend/functions/pipeline/app.py
  - backend/functions/playback-url/app.py
  - backend/template.yaml
  - backend/shared/python/sunity_shared/firestore_admin.py
  - backend/shared/python/sunity_shared/models.py
  - docs/contract.md
  - app/src/types/analysis.ts
  - app/src/lib/userAnalyses.ts
  - backend/tests/phase32/test_coach_audio.py
autonomous: false
requirements: [D-18, D-21, D-29, D-30, D-09]
must_haves:
  truths:
    - "재생 중 큐가 자막+오디오로 제공되고 오디오는 설정 on/off 가능하다 — 방식은 샘플 게이트 확정안이며, A/B 어느 확정이든 오디오가 이번 phase 안에서 출시 완결된다 (D-18 동시 출시)"
    - "부분 실패 시 측정 커버리지가 정직 고지되고 잰 범위 내 미션+재촬영 팁 1줄이 표시된다 (D-29)"
    - "전체 실패 화면(not_pole·no_human 등) 카피가 친숙·응원 톤+다음 행동 1개로 정비되고 재업로드 동선이 점검된다 (D-30)"
    - "일러스트는 게이트 확정대로 도입 또는 실프레임+텍스트 폴백 (D-21)"
    - "전 화면 D-09 검증(헤드라인 수치·% 환산 금지) 통과 후 시뮬레이터 확인 → OTA → belle 실기기 확인"
  artifacts:
    - path: "app/src/lib/audioCue.ts"
      provides: "오디오 큐 어댑터 (확정 방식 — 발화 중단·설정 게이트 포함)"
      exports: ["speakCue", "stopCue", "isAudioCueEnabled", "setAudioCueEnabled"]
  key_links:
    - from: "app/src/components/VideoCompare.tsx"
      to: "app/src/lib/audioCue.ts"
      via: "자막 큐 표시 시점과 동일 트리거"
      pattern: "speakCue"
---

<objective>
UI 본체 마감 — 샘플 게이트 확정 방식으로 오디오 큐를 구현하고(D-18), 실패 UX(D-29/D-30)를 정비하고, 전 화면 D-09 준수를 검증한 뒤 OTA로 belle 실기기 확인까지 완료한다.

Purpose: phase 32 UI 크리티컬 패스의 출시 상태 완성. 엔진 레버(32-13~15)는 이 뒤.
Output: 오디오 큐 + 실패 UX 카피 + D-09 게이트 + OTA + belle 확인.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-result-readability-3-omni/32-CONTEXT.md
@.planning/phases/32-result-readability-3-omni/32-GATE-DECISIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 오디오 큐 구현 — 게이트 확정 방식 + 설정 on/off (D-18) + 일러스트 확정 반영 (D-21)</name>
  <files>app/package.json, app/src/lib/audioCue.ts, app/src/components/VideoCompare.tsx</files>
  <read_first>
    - .planning/phases/32-result-readability-3-omni/32-GATE-DECISIONS.md §샘플 게이트 (D-18 확정 A/B + D-21 일러스트 확정 — 구현 분기의 단일 출처)
    - .planning/phases/32-result-readability-3-omni/32-RESEARCH.md §재생 중 큐 + TTS (A안: Speech.speak + 이전 발화 stop / B안: Polly 사전 생성 + expo-audio 재생 + playback-url asset 확장)
    - app/src/components/VideoCompare.tsx (32-08 자막 큐 트리거 지점 — 오디오는 같은 트리거)
    - app/src/lib/coachmark.ts (AsyncStorage 플래그 관례 — 오디오 on/off 저장 동일 구조)
  </read_first>
  <action>
    per D-18/D-21. (1) 확정 모듈 설치 — 반드시 `cd app && npx expo install expo-speech`(A 확정 시) 또는 `npx expo install expo-audio`(B 확정 시). `npm install` 직접 실행 금지(npm dist-tag latest는 SDK 55 라인 — RESEARCH 경고). slopcheck install 사용 금지(홈 디렉터리 오설치 사고 이력). 설치 후 package.json 버전이 SDK 54 호환(~14.0.8 또는 ~1.1.1)인지 확인. Package Legitimacy Audit: 두 후보 모두 [OK] Approved(expo 공식, github.com/expo/expo, SDK 54 번들 매니페스트 등재 — 32-RESEARCH 감사표). (2) audioCue.ts 어댑터 — speakCue(text: string)/stopCue()/isAudioCueEnabled()/setAudioCueEnabled(bool). A안이면 Speech.speak(text, {language:'ko-KR'}) + 새 큐 발화 전 이전 발화 stop 처리. B안이면 expo-audio 재생 어댑터(playback-url 경유 presigned mp3 재생)를 구현하고 백엔드 합성·저장·asset 확장은 Task 2(같은 플랜 — B안 조건부)가 담당. 어느 확정이든 오디오는 이 플랜 안에서 출시 완결(D-18 동시 출시 — 이연 금지). 설정 키 '@sunity:audio_cue_enabled'(AsyncStorage, 기본 off — 학원 소음 환경), 토글 UI는 동작 비교 영역 내 소형 버튼(토큰·accessibility 관례). (3) VideoCompare — 자막 큐 전환 시점과 동일 지점에서 isAudioCueEnabled() 확인 후 speakCue(동일 텍스트), 재생 정지·seek·언마운트 시 stopCue. (4) 일러스트 — 게이트 확정 '도입'이면 승인 샘플을 app/assets/illustrations/에 번들하고 드릴다운 시트 표시 지점에 정적 렌더 추가(런타임 생성 0), '폴백'이면 코드 무산출(결과만 SUMMARY 기록).
  </action>
  <verify>
    <automated>cd app && npm run typecheck && grep -cE '"(expo-speech|expo-audio)"' package.json</automated>
  </verify>
  <acceptance_criteria>
    - package.json에 expo-speech 또는 expo-audio 정확히 1개 추가 (둘 다 금지)
    - audioCue.ts가 4개 export + 이전 발화 stop 처리 포함
    - VideoCompare에서 speakCue/stopCue 호출 존재 + 오디오 off 시 자막만 동작
    - typecheck clean
  </acceptance_criteria>
  <done>확정 방식의 오디오 큐가 설정 게이트와 함께 동작</done>
</task>

<task type="auto">
  <name>Task 2 (B안 조건부): Polly 합성 사후 스테이지 + S3 저장 + playback asset 확장 (D-18)</name>
  <files>backend/functions/pipeline/app.py, backend/functions/playback-url/app.py, backend/template.yaml, backend/shared/python/sunity_shared/firestore_admin.py, backend/shared/python/sunity_shared/models.py, docs/contract.md, app/src/types/analysis.ts, app/src/lib/userAnalyses.ts, backend/tests/phase32/test_coach_audio.py</files>
  <read_first>
    - .planning/phases/32-result-readability-3-omni/32-GATE-DECISIONS.md §샘플 게이트 (A 확정이면 이 태스크 전체 skip — skip 사유만 SUMMARY에 1줄 기록하고 백엔드 무접촉)
    - backend/functions/playback-url/app.py:77-124 (_handle_asset H-02 정본 — 서버가 key 구성 + 저장 key exact 비교. 오디오 asset이 복제)
    - backend/functions/pipeline/app.py:5236-5320 (사후 스테이지 자리 — fault_zoom 패턴, 32-09가 수정한 최신 상태)
    - backend/shared/python/sunity_shared/firestore_admin.py:1138-1187 (update_analysis_fault_zoom — 사후 부분 업데이트 helper 뼈대)
    - .planning/phases/32-result-readability-3-omni/32-PATTERNS.md §E-3 (오디오 asset 확장 지침 — canonical key·_ASSET_CONTENT_TYPE mp3) + §SP-1/SP-3
  </read_first>
  <action>
    per D-18 동시 출시 (B안 확정 시에만 실행 — A 확정이면 skip. 이 태스크가 있으므로 어느 확정이든 오디오는 이번 phase 안에서 완결되고, B안 선택이 이연이 되지 않는다). (1) IaC: backend/template.yaml의 pipeline 함수 IAM에 polly:SynthesizeSpeech 1줄 추가 — 신규 시크릿 0(boto3 client("polly"), Seoyeon neural, ap-northeast-2 — RESEARCH 실계정 확인). (2) pipeline 사후 스테이지: status done 확정 후 with _stage(timings_ms, analysis_id, "coach_audio") 블록에서 records의 cueLine 문장들을 Polly 합성 → S3 results/{uid}/{analysisId}/coach_audio_{i}.mp3 저장 → 신규 helper update_analysis_coach_audio(fault_zoom 뼈대 복제 — "result.coachAudio" 단일 field-path)로 {status('done'|'failed'), keys(flat list[str])} 부분 갱신. 합성 실패 = graceful(로그 후 통과 — 자막만 제공, 분석 무훼손 SP-3). 동기 경로 호출 금지(속도 예산). (3) playback-url _handle_asset에 오디오 asset 종류 확장 — 서버가 canonical key 구성 + 저장 keys exact 비교(H-02, prefix 부분일치 불가) + _ASSET_CONTENT_TYPE에 "mp3": "audio/mpeg". 기존 asset 경로 바이트 불변. (4) 계약 3면(COACH_AUDIO_KEYS 상수 + analysis.ts coachAudio? + contract.md 절) + userAnalyses normalize(keys는 results/ prefix 강등 — normalizeResultKey 재사용) atomic 커밋. (5) Task 1의 audioCue.ts가 playback-url 경유 presigned URL을 받아 expo-audio로 재생 연결. (6) 테스트: 합성 스테이지 graceful(무자격/실패 시 done 유지) + asset guard(stale key·타 uid 404)를 backend/tests/phase32/test_coach_audio.py로.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/phase32 -x -q && cd ../app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - (B 확정 시) template.yaml에 polly:SynthesizeSpeech 존재 + _handle_asset에 mp3 content-type + exact 비교 가드 + 계약 3면 동시 수정
    - (B 확정 시) 합성 실패 케이스에서도 status done 유지 + 자막 경로 무영향 (graceful 테스트 green)
    - (A 확정 시) 이 태스크 skip 사유 SUMMARY 1줄 + 이 태스크의 백엔드 파일 diff 0
  </acceptance_criteria>
  <done>B안 확정 시에도 오디오가 이번 phase 안에서 완결 출시 — D-18 동시 출시 충족, 이연 없음</done>
</task>

<task type="auto">
  <name>Task 3: 실패 UX — 부분 실패 정직 고지 (D-29) + 전체 실패 카피 정비 (D-30)</name>
  <files>app/src/app/analysis/result.tsx, app/src/app/analysis/loading.tsx</files>
  <read_first>
    - app/src/app/analysis/loading.tsx (수정 대상 — 실패 카피 위치·에러 코드 분기(not_pole_motion/no_human/server_error)·재업로드 동선 실측. 에러 우선순위 로직 :303 부근)
    - app/src/app/analysis/result.tsx (부분 실패 고지 삽입 지점 — coverage_gap 신호 소비. ScoreBreakdownSection footnote 정직 고지 패턴 :150-159가 아날로그)
    - backend/shared/python/sunity_shared/analysis/phrasebook.py (재촬영 팁·실패 카피 톤 참조 — 카피는 앱 상수)
  </read_first>
  <action>
    per D-29/D-30. (1) result.tsx 부분 실패 고지 — 커버리지 갭 존재 시(기존 coverage_gap/미측정 신호 실측 소비) 요약 카드 아래 정직 고지 1블록: "이번엔 {잰 부위}만 확실히 잴 수 있었어요" 톤 + 재촬영 팁 1줄(온보딩 가이드 연결 — 기존 온보딩 화면 route 링크) — 잰 범위 내 미션 구성은 32-09 방출이 이미 담당(못 잰 것은 coachQuestions 자동 등재 — 표시 확인만). (2) loading.tsx 전체 실패 카피 — not_pole_motion/no_human/server_error 각 분기의 카피를 새 언어로: 친숙·응원 톤 + 다음 행동 1개(재촬영 가이드 또는 재업로드 버튼) — 구조 재설계 금지(D-30, 카피·톤만). 재업로드 동선이 실제로 동작하는지 점검(버튼 → analyze 화면 라우팅). 저화질 우선 안내 분기(기존 260704-fwb) 유지. 카피 상수 분리 + 스펙 인용 주석(32-CONTEXT D-30).
  </action>
  <verify>
    <automated>cd app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - result.tsx에 커버리지 정직 고지 조건 렌더 + 재촬영 팁 1줄 존재
    - loading.tsx 실패 분기 카피가 "다음 행동 1개" 구조 (각 분기에 행동 버튼/안내 1개)
    - 재업로드 동선 라우팅 확인 (코드 경로 추적 결과 SUMMARY 기록)
    - typecheck clean
  </acceptance_criteria>
  <done>부분·전체 실패가 정직 고지+다음 행동 구조로 정비됨</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: D-09 전 화면 검증 + 시뮬레이터 → OTA → belle 실기기 확인</name>
  <what-built>phase 32 UI 본체 전체 — 요약 카드·3단 감점 카드·게이지·미션 루프·코치 질문·재생 중 큐(자막+오디오)·실패 UX</what-built>
  <how-to-verify>
    Claude 선행 자동 절차(이 체크포인트 도달 전 완료):
    1. D-09 게이트 — 앱 사용자 노출 문자열에서 % 환산·헤드라인 수치 패턴 grep 스캔(결과 화면 계열 파일 대상, 위반 0 확인) + 백엔드 문구집 금지어 테스트 재실행
    2. 시뮬레이터 4케이스(정상/감점0/legacy/mode3) 재확인 + 오디오 토글 동작
    3. 빌드 경로 청결 확인 후 OTA 발행(production+preview), 직전 정상 group id 기록(롤백: npx eas update:republish --group)
    belle 실기기 확인:
    4. 완전 종료 2회 재실행 → 결과 화면 진입
    5. 확인 항목: 요약 카드 1장 첫인상 / 오늘 고칠 것 카드의 3단 문장이 읽히는가 / 게이지·미션이 D-10 확정 강도인가 / 재생 중 자막(+오디오 on 시) 큐 / 코치 질문 목록 / 실패 화면(가능 시)
    6. 성공 판정 프레임(SEED §9): ① 자기 말로 문제 설명 가능한가 ② 다음 행동 하나를 말할 수 있는가 ③ 강사에게 물어볼 것이 생기는가
  </how-to-verify>
  <action>선행 자동 절차(D-09 grep 스캔 → 시뮬레이터 4케이스 → 빌드 경로 청결 OTA 발행 + 롤백 group 기록)를 완료한 뒤 belle 실기기 확인을 진행한다. 승인 또는 이슈를 수집하고, 이슈는 즉시 수리 후 재확인한다. 잔여 실기기 항목은 32-HUMAN-UAT.md에 적립한다.</action>
  <verify>
    <automated>cd app && npm run typecheck && npx eas update:list --branch production --limit 1 --non-interactive 2>/dev/null | head -3</automated>
  </verify>
  <acceptance_criteria>
    - D-09 grep 스캔 위반 0 기록 + OTA 발행 group id + 롤백 대상 group id 기록
    - belle 실기기 승인 기록 (또는 이슈 수리 후 승인)
  </acceptance_criteria>
  <done>UI 본체가 OTA로 belle 실기기 승인을 받음</done>
  <resume-signal>"승인" 또는 발견 이슈 목록 (이슈는 즉시 수리 후 재확인 — fix-now 원칙)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm 레지스트리 → 앱 의존성 | 오디오 모듈 1개 설치 (예외 승인 항목) |
| OTA → 사용자 기기 | UI 본체 전체 교체 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-SC | Tampering | npm install | mitigate | npx expo install만 사용 + 대상은 audit [OK] 2종 중 확정 1개 한정 + package.json diff로 1개만 추가 확인 (acceptance) |
| T-32-28 | DoS | OTA 사고 | mitigate | 시뮬레이터 4케이스 + 빌드 경로 청결 + 직전 group 롤백 준비 + belle 확인 전 광고 없음 |
| T-32-29 | Info Disclosure | TTS 발화 내용 | mitigate | 발화 텍스트 = 문구집 큐 문장만(PII 0), 로컬 발화(A) 또는 서버 구성 key + exact 비교 presigned(B — Task 2 H-02) |
</threat_model>

<verification>
- cd app && npm run typecheck clean + node --test 기존 lib 테스트 무회귀
- D-09 grep 스캔 위반 0 + 문구집 금지어 테스트 green
- OTA 발행 + belle 실기기 승인
</verification>

<success_criteria>
- UI 크리티컬 패스 완결 — 자막+오디오 큐, 실패 UX, D-09 전 화면 준수, belle 실기기 승인
- HUMAN-UAT 적립 항목이 있으면 32-HUMAN-UAT.md에 기록 (batch-uat 관례)
</success_criteria>

<output>
Create `.planning/phases/32-result-readability-3-omni/32-12-SUMMARY.md` when done
</output>
