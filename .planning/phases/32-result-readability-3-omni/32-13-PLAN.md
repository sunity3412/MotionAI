---
phase: 32-result-readability-3-omni
plan: 13
type: execute
wave: 8
depends_on: ["32-09", "32-12"]
files_modified:
  - backend/shared/python/sunity_shared/analysis/spot_check.py
  - backend/functions/pipeline/app.py
  - backend/shared/python/sunity_shared/firestore_admin.py
  - backend/shared/python/sunity_shared/models.py
  - docs/contract.md
  - app/src/types/analysis.ts
  - app/src/lib/userAnalyses.ts
  - app/src/app/analysis/result.tsx
  - backend/tests/phase32/test_spot_check.py
autonomous: true
model: fable
requirements: [D-22, D-23]
must_haves:
  truths:
    - "omni가 분석 완료 후 '감점 카드 문장↔영상 일치'를 스팟체크하고 불일치 카드는 숨김+로그 적재된다 — 틀린 말을 내보내느니 안 보여줌 (D-23)"
    - "검증 결과가 안정 recordId 기반이다 — hiddenRecordIds(list[str]) + verdicts(recordId·verdict·reason) 감사 저장. index 기반 숨김 폐지 (리뷰 blocker 5)"
    - "같은 스팟체크 호출이 result.summaryPraise.headline(백엔드 방출 — 32-09)을 교차검증한다 — 앱이 렌더하는 바로 그 문장 (리뷰 blocker 5: 단일 원천 성립)"
    - "표시 정책이 계약에 명문화된다: spotCheck 부재/pending = 전 카드 표시(현행 동일 — D-23의 숨김은 '확정 불일치'만), status failed = fail-open + 로그 (제품 정책·근거 명기)"
    - "스팟체크는 status=done 확정 이후 사후 스테이지에서만 실행 — 동기 경로 신규 외부 호출 0 (분석 속도 회귀 금지)"
    - "스팟체크 실패(키 부재·API 오류)는 no-op — 완료된 분석을 훼손하지 않는다 (graceful)"
    - "모델은 실제 fixture 영상+records 멀티모달 스모크 1콜로 확정 — strict JSON 응답 스키마, 분석당 1콜·record 상한·타임아웃 명시 (리뷰 반영: 더미 텍스트 스모크 폐지)"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/spot_check.py"
      provides: "스팟체크 어댑터 (lazy client + PROMPT_VERSION + env 모델 주입 + 비용 상한)"
    - path: "backend/tests/phase32/test_spot_check.py"
      provides: "graceful·recordId 검증·카드 숨김 단위 테스트"
  key_links:
    - from: "backend/functions/pipeline/app.py"
      to: "spot_check.py"
      via: "fault_zoom 사후 스테이지 패턴의 신규 _stage 블록 (status done 이후)"
      pattern: "spot_check"
    - from: "app/src/app/analysis/result.tsx"
      to: "result.spotCheck.hiddenRecordIds"
      via: "recordId 맵 기반 감점 카드 숨김 필터"
      pattern: "hiddenRecordIds"
---

<objective>
엔진 레버 1/3 — omni 짚기·검수(D-22)의 운영 자가검증 형태(D-23): 분석별로 감점 카드 문장과 영상의 일치를 스팟체크하고 불일치 카드를 숨긴다. 같은 호출로 summaryPraise 헤드라인(백엔드 방출 — 앱이 렌더하는 그 문장)도 교차검증한다(D-22 "잘한 점 교차검증" — 추가 API 콜 0). D-22의 "짚기 정밀화"는 원문상 분석 속도 예산 내 지점 선별 재량이며, phase 27 1분대 달성 회귀 금지에 따라 이 플랜의 선별 = 사후 스테이지 한정 검수·교차검증(동기 경로 신규 호출 0)이다. fault_zoom 사후 분리 패턴을 재사용해 속도 예산을 구조적으로 보호한다.

리뷰 blocker 5 반영: 검증 대상 identity를 recordId로 통일(index 폐지), praise 교차검증은 32-09가 방출한 summaryPraise.headline(단일 원천)을 대상으로 하며, pending/failed 표시 정책을 계약에 명문화한다.

Purpose: "분석이 정말 잘 되고 있는지 중간중간 끊임없이 판단"(belle MVP 방향)의 운영 실체.
Output: spot_check.py + 사후 스테이지 + spotCheck 계약 + 앱 숨김 소비 + Pod 배포·스윕·OTA.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-result-readability-3-omni/32-CONTEXT.md
@.planning/phases/32-result-readability-3-omni/32-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 모델 멀티모달 스모크 + spot_check.py 어댑터 (recordId·상한·strict 스키마)</name>
  <files>backend/shared/python/sunity_shared/analysis/spot_check.py, backend/tests/phase32/test_spot_check.py</files>
  <read_first>
    - backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py (정본 아날로그 — _ensure_client lazy 싱글톤 :763-782, PROMPT_VERSION :77 캐시 규율, DEFAULT_VISION_MODEL env 주입 :104, 영상/프레임 입력 경로 실측 — 스팟체크가 같은 미디어 경로 재사용)
    - .planning/spikes/004-gemini-omni-view-editing/README.md (omni 실측 — 검증된 것은 영상 출력뿐, 텍스트 판정은 미검증 [ASSUMED A1])
    - backend/shared/python/sunity_shared/analysis/coach_writer.py:217-279 (graceful 실패 관례)
  </read_first>
  <action>
    per D-22/D-23 + 리뷰 반영. (1) **멀티모달 스모크 1콜(더미 텍스트 금지 — 리뷰 HIGH):** belle Gemini 키(SSM 경유, 크레딧 확인)로 gemini-omni-flash-preview에 **실제 짧은 fixture 클립(또는 분석에 쓰인 프레임 서브셋) + 실제 record 문장 2~3건**을 넣은 판정 프롬프트 1건 호출 — 영상+텍스트 멀티모달 판정 능력·응답 형식·비용을 실측. 성공 시 기본 모델 채택, 실패·형식 불충족 시 gemini-3.1-pro-preview 폴백 확정(같은 어댑터·검증된 경로) — 결과·비용을 SUMMARY와 spot_check.py 헤더 주석에 기록. 모델 ID는 env GEMINI_SPOTCHECK_MODEL(기본값 = 스모크 확정값)로 주입. (2) spot_check.py — gemini_vision_scorer 관례 복제: lazy client 싱글톤(top-level import 금지), SPOTCHECK_PROMPT_VERSION 상수(프롬프트 변경 시 bump — Pitfall 8), temp 0, **strict JSON 응답 스키마(response_schema — 자유 텍스트 파싱 금지)**. 공개 함수 run_spot_check(video_ref, records: list[dict], praise_headline: str | None) -> dict — 각 record(recordId 필수 — 없으면 해당 record 스킵)의 statusLine/cueLine 문장과 영상 일치 + praise_headline 1문장(D-22 잘한 점 교차검증 — 같은 호출에 문장 1개 추가, 추가 API 콜 0)을 판정해 {status('done'|'skipped'|'failed'), hiddenRecordIds(list[str] — 불일치 카드), verdicts(list[{recordId, verdict('match'|'mismatch'|'uncertain'), reason(str ≤120자)}] — 감사용, 리뷰 반영), praiseMismatch(bool), model, promptVersion} 반환. **비용·상한(리뷰 반영):** 분석당 1콜 고정, records 상한 8건(감점 큰 순 — 초과분은 'uncertain' 미판정 통과), 영상 입력은 분석에 이미 쓰인 9fps 프레임 중 record window 대표 프레임 서브셋(record당 최대 2 — gemini_vision_scorer 미디어 경로 실측 재사용), 타임아웃은 기존 vision wall 예산 env 재사용. 판정 기준은 보수적으로(불확실='uncertain'=일치 취급 — 과숨김 방지, 숨김은 명백 'mismatch'만). 키 부재·API 실패 = status 'skipped'/'failed' + hiddenRecordIds 빈 배열(no-op — 카드 전부 표시 유지, SP-3). 테스트: 무키 환경 no-op(praiseMismatch False 포함) / 반환 형상(verdicts scalar dict) / recordId 없는 record 스킵 / 상한 절삭.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/phase32/test_spot_check.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - spot_check.py에 run_spot_check + SPOTCHECK_PROMPT_VERSION + env GEMINI_SPOTCHECK_MODEL 주입 + records 상한 8 + response_schema 사용 존재
    - top-level에 google.genai import 부재 (lazy만 — grep으로 함수 내부 import 확인)
    - 무키 환경 테스트에서 no-op(빈 hiddenRecordIds + 비예외) green
    - 멀티모달 스모크 1콜 결과(채택 모델·판정 형식·비용)가 SUMMARY에 기록
  </acceptance_criteria>
  <done>스팟체크 어댑터가 실측 검증된 모델·strict 스키마·상한과 함께 graceful하게 성립</done>
</task>

<task type="auto">
  <name>Task 2: 사후 스테이지 배선 + spotCheck 계약(표시 정책 명문화) + 앱 recordId 숨김 소비</name>
  <files>backend/functions/pipeline/app.py, backend/shared/python/sunity_shared/firestore_admin.py, backend/shared/python/sunity_shared/models.py, docs/contract.md, app/src/types/analysis.ts, app/src/lib/userAnalyses.ts, app/src/app/analysis/result.tsx</files>
  <read_first>
    - backend/functions/pipeline/app.py:5236-5320 (firestore_complete → fault_zoom 사후 스테이지 — 스팟체크가 복제할 자리. _stage 컨텍스트 매니저 :122-140. 32-09/32-12가 수정한 최신 상태)
    - backend/shared/python/sunity_shared/firestore_admin.py:1138-1187 (update_analysis_fault_zoom — 사후 부분 업데이트 helper 뼈대. "그 외 result.* 사후 변경 금지" 경계 주석)
    - backend/shared/python/sunity_shared/models.py (32-06이 확장한 상수 블록 형태)
    - app/src/lib/userAnalyses.ts (32-06 normalize 헬퍼 3종 관례)
    - app/src/app/analysis/result.tsx (감점 카드 목록 렌더 — 32-11 배선 상태, buildRecordMaps recordId 맵)
  </read_first>
  <action>
    per D-23 + 리뷰 blocker 5. (1) 계약: models.py에 SPOT_CHECK_KEYS = ("status","hiddenRecordIds","verdicts","praiseMismatch","model","promptVersion") + SPOT_CHECK_STATUSES = ("done","skipped","failed") + SPOT_CHECK_VERDICTS = ("match","mismatch","uncertain") — lockstep 주석. analysis.ts에 SpotCheck interface(hiddenRecordIds: string[], verdicts?: {recordId: string; verdict: 'match'|'mismatch'|'uncertain'; reason?: string}[] — scalar dict 배열, safetyFlags 선례) + result.spotCheck?. contract.md 절 추가 — **표시 정책 명문화(리뷰 HIGH):** ① spotCheck 부재(legacy)·미도착(pending) = 전 카드 표시(현행 프로덕션과 동일 — D-23의 숨김 규칙은 '확정 불일치'에만 적용되고, 검수는 비차단 부가 레이어이므로 사전 숨김은 하지 않는다. onSnapshot 실시간 구독이라 늦은 숨김은 자연 반영 — 짧은 노출 후 숨김은 수용하는 트레이드오프임을 명기), ② status='failed' = fail-open(전 카드 표시) + 로그 — 근거: 검수 레이어 장애가 제품 전체를 비우는 것은 '비차단 부가 기능' 원칙 위반이며, 숨김 권한은 '명백 불일치 판정'에만 있음. (2) firestore_admin에 update_analysis_spot_check(uid, analysis_id, spot_check: dict) — fault_zoom helper 뼈대 복제: status/verdict enum 검사 + hiddenRecordIds str 검증 + verdicts scalar dict 검증(항목 ≤ 8, reason ≤ 120자) + field-path 부분 갱신("result.spotCheck") + updatedAt. (3) pipeline — fault_zoom 사후 스테이지 다음에 with _stage(timings_ms, analysis_id, "spot_check") 블록: run_spot_check(video_ref, records, result.get("summaryPraise",{}).get("headline")) 호출 → update_analysis_spot_check. **praise 교차검증 대상 = 32-09가 방출한 summaryPraise.headline — 앱이 렌더하는 바로 그 문장(단일 원천 성립, 리뷰 blocker 5).** 실패는 log.exception 후 통과(분석 훼손 금지). 동기 경로(firestore_complete 이전)에 호출 금지 — 구조로 속도 예산 보호. 불일치 시 구조 로그 적재(D-23 "로그 적재" — analysis_id/recordId/criterion/reason). (4) 앱 — userAnalyses normalize에 spotCheck 방어 파싱(string 배열·verdict enum 검증), result.tsx 감점 카드 목록에서 hiddenRecordIds 포함 record는 **recordId 맵(32-11 buildRecordMaps) 기반** 카드 숨김(펼침 상세의 투명 내역에는 유지 — 채점 tally 불변, 숨김은 카드 표면만. 이 경계를 주석으로 명시). recordId 없는 legacy record는 숨김 대상에서 제외(조인 불가 = 표시 유지 — fail-open 정합). praiseMismatch=true면 요약 카드 praise를 summarySource 폴백 체인의 다음 소스로 강등(32-07 Test 2 경로 — 근거 미검증 칭찬 노출 방지). (5) 계약 3면 atomic 커밋.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/phase32 -x -q && cd ../app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - pipeline에 spot_check _stage 블록이 firestore_complete **이후** 위치 (코드 순서 확인)
    - run_spot_check 호출 인자에 summaryPraise.headline 전달 (grep 확인)
    - update_analysis_spot_check가 "result.spotCheck" field-path 부분 갱신 + enum/문자열/상한 검증
    - contract.md에 pending=표시·failed=fail-open 정책 서술 존재 (grep "fail-open" 또는 동등 서술)
    - result.tsx 카드 숨김 필터가 recordId 맵 경유 + ScoreBreakdownSection 투명 내역은 미필터 (grep 확인)
    - 계약 3면 + normalize 동시 수정 (단일 커밋)
    - typecheck + phase32 테스트 green
  </acceptance_criteria>
  <done>스팟체크가 사후 스테이지로 흐르고 불일치 카드가 recordId 기반으로 표면에서만 숨겨지며 표시 정책이 계약에 고정됨</done>
</task>

<task type="auto">
  <name>Task 3: Pod 배포 + fixture 6동작 순차 스윕 + 앱 변경 OTA (D-23 웨이브 게이트)</name>
  <files>(코드 수정 없음 — Pod 운영 + 스윕 기록 + OTA)</files>
  <read_first>
    - .planning/phases/32-result-readability-3-omni/32-RESEARCH.md §Common Pitfalls 9/10 (스윕 순차 강제·Pod 운영 함정)
    - .planning/phases/32-result-readability-3-omni/32-09-SUMMARY.md, 32-12-SUMMARY.md (직전 스윕 기준선·runtime 1.1.0 OTA 경로)
  </read_first>
  <action>
    per D-23. 배포 아키텍처 사실(리뷰 H7 정정): pipeline/app.py·firestore_admin·spot_check.py는 RunPod 서버가 import하는 코드 — **프로덕션 분석 경로의 배포 = Pod git pull**(Lambda pipeline 함수는 CPU 폴백/위임 래퍼 — SAM 배포는 Lambda 전용 표면 변경 시에만이며 이 플랜엔 없음. Lambda 레이어 drift는 위임 경로에서 미실행 코드라 수용, 필요 시 phase 마감 후 verify-work에서 SAM 동기화 기록). (1) Pod /health 200 확인 → 이 플랜 백엔드 커밋 push+pull+재시작+/health 재확인. GEMINI_SPOTCHECK_MODEL env 주입(+ production start_server.sh 박제 mirror). (2) fixture 6동작 전수 스윕(순차 — 동시 호출 금지, kip-up 편중 금지): 각 분석에서 (a) spotCheck.status done + verdicts recordId 정합(방출 records의 recordId와 일치), (b) 불일치 숨김·praiseMismatch 빈도 확인(과숨김·과강등 = 프롬프트 보수화 재조정), (c) timingsMs에서 동기 경로(사용자 체감 = status done까지) 소요가 직전 실측(32-09 스윕 기준) 대비 회귀 없음 — spot_check 스테이지는 사후이므로 영향 0이어야 함, (d) 점수·verdict 기준선 무회귀. (3) **앱 변경 OTA:** result.tsx/userAnalyses 숨김 소비 변경을 runtime 1.1.0 대상 OTA로 발행(32-12 확립 경로 — 시뮬레이터 확인 선행 + 롤백 group 기록). 32-15 belle 최종 확인이 이 상태를 실기기로 본다. (4) 결과 표(동작×방출·숨김·praise 판정·타이밍)를 SUMMARY에 기록. 과숨김·오숨김 발견 시 프롬프트 조정 후 SPOTCHECK_PROMPT_VERSION bump하여 재스윕.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/phase32 -q | tail -2 && curl -s -o /dev/null -w "%{http_code}" "$RUNPOD_ANALYZE_URL/health" | grep -q 200 && echo POD_OK</automated>
  </verify>
  <acceptance_criteria>
    - 6동작 스윕 결과 표 (spotCheck status + hiddenRecordIds 개수 + praiseMismatch + 동기 경로 timingsMs)
    - 동기 경로 소요 회귀 0 (fault_zoom 이전 스테이지 합계 기준, 직전 실측 대비)
    - 명백한 오숨김(정타 카드 숨김) 0 — 발견 시 조정·재스윕 기록
    - runtime 1.1.0 OTA 발행 group id + 롤백 group 기록
  </acceptance_criteria>
  <done>스팟체크가 프로덕션 Pod에서 6동작 검증되고 속도 예산이 지켜지며 앱 소비층이 기기에 도달함</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gemini API → 카드 숨김 판정 | 외부 LLM 판정이 표시 여부를 결정 |
| Pod 배포 | 프로덕션 파이프라인 코드 교체 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-30 | Tampering(신뢰) | 스팟체크 판정 | mitigate | 판정 권한 = 숨김만(점수·tally 무접촉 — 투명 내역 유지), 보수 기준(명백 mismatch만, uncertain=표시), temp 0 + strict 스키마 + PROMPT_VERSION 캐시 규율, verdicts reason 감사 저장 |
| T-32-31 | DoS | 사후 스테이지 실패 | mitigate | graceful no-op(무키/실패 = 전 카드 표시 유지 — fail-open 정책 계약 명문화) + _stage try/finally — 완료 분석 훼손 0 |
| T-32-32 | Info Disclosure | 영상 → Gemini | accept | 기존 vision 경로(veto/짚기)와 동일 데이터 경계 — 신규 노출면 없음. 키는 SSM만 |
| T-32-33 | Elevation | 사후 쓰기 표면 | mitigate | update helper가 result.spotCheck 단일 field-path만 갱신 (fault_zoom "그 외 사후 변경 금지" 경계 복제) |
</threat_model>

<verification>
- cd backend && python -m pytest tests/phase32 -x -q green + 전체 baseline 초과 0
- cd app && npm run typecheck clean
- Pod 6동작 스윕 표 + 속도 회귀 0 + OTA 발행
</verification>

<success_criteria>
- D-23 운영 자가검증 가동 — recordId 기반 불일치 카드 숨김+감사 로그, 표시 정책 계약 고정, 속도 예산 무회귀
- praise 교차검증이 백엔드 단일 원천(summaryPraise.headline)에 대해 성립 (리뷰 blocker 5 해소)
</success_criteria>

<output>
Create `.planning/phases/32-result-readability-3-omni/32-13-SUMMARY.md` when done
</output>
