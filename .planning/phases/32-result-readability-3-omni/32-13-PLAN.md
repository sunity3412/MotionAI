---
phase: 32-result-readability-3-omni
plan: 13
type: execute
wave: 8
depends_on: ["32-09", "32-12"]
files_modified:
  - backend/shared/python/sunity_shared/analysis/spot_check.py
  - backend/functions/pipeline/app.py
  - backend/shared/python/sunity_shared/firestore_admin.py
  - backend/shared/python/sunity_shared/models.py
  - docs/contract.md
  - app/src/types/analysis.ts
  - app/src/lib/userAnalyses.ts
  - app/src/app/analysis/result.tsx
  - backend/tests/phase32/test_spot_check.py
autonomous: true
model: fable
requirements: [D-22, D-23]
must_haves:
  truths:
    - "omni가 분석 완료 후 '감점 카드 문장↔영상 일치'를 스팟체크하고 불일치 카드는 숨김+로그 적재된다 — 틀린 말을 내보내느니 안 보여줌 (D-23)"
    - "같은 스팟체크 호출이 요약 카드 잘한 점(praise) 헤드라인도 교차검증한다 — 불일치 시 praise 폴백 강등 (D-22 잘한 점 교차검증)"
    - "스팟체크는 status=done 확정 이후 사후 스테이지에서만 실행 — 동기 경로 신규 외부 호출 0 (분석 속도 회귀 금지)"
    - "스팟체크 실패(키 부재·API 오류)는 no-op — 완료된 분석을 훼손하지 않는다 (graceful)"
    - "모델은 스모크 1콜로 확정 — omni 텍스트 판정 가능 시 채택, 불가 시 gemini-3.1-pro-preview 폴백 (RESEARCH A1)"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/spot_check.py"
      provides: "스팟체크 어댑터 (lazy client + PROMPT_VERSION + env 모델 주입)"
    - path: "backend/tests/phase32/test_spot_check.py"
      provides: "graceful·플래그 쓰기·카드 숨김 단위 테스트"
  key_links:
    - from: "backend/functions/pipeline/app.py"
      to: "spot_check.py"
      via: "fault_zoom 사후 스테이지 패턴의 신규 _stage 블록 (status done 이후)"
      pattern: "spot_check"
    - from: "app/src/app/analysis/result.tsx"
      to: "result.spotCheck.hiddenRecordIndices"
      via: "감점 카드 숨김 필터"
      pattern: "hiddenRecordIndices"
---

<objective>
엔진 레버 1/3 — omni 짚기·검수(D-22)의 운영 자가검증 형태(D-23): 분석별로 감점 카드 문장과 영상의 일치를 스팟체크하고 불일치 카드를 숨긴다. 같은 호출로 잘한 점(praise) 헤드라인도 교차검증한다(D-22 "잘한 점 교차검증" — 추가 API 콜 0). D-22의 "짚기 정밀화"는 원문상 분석 속도 예산 내 지점 선별 재량이며, phase 27 1분대 달성 회귀 금지에 따라 이 플랜의 선별 = 사후 스테이지 한정 검수·교차검증(동기 경로 신규 호출 0)이다. fault_zoom 사후 분리 패턴을 재사용해 속도 예산을 구조적으로 보호한다.

Purpose: "분석이 정말 잘 되고 있는지 중간중간 끊임없이 판단"(belle MVP 방향)의 운영 실체.
Output: spot_check.py + 사후 스테이지 + spotCheck 계약 + 앱 숨김 소비 + Pod 배포·스윕 검증.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-result-readability-3-omni/32-CONTEXT.md
@.planning/phases/32-result-readability-3-omni/32-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 모델 스모크 1콜 + spot_check.py 어댑터</name>
  <files>backend/shared/python/sunity_shared/analysis/spot_check.py, backend/tests/phase32/test_spot_check.py</files>
  <read_first>
    - backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py (정본 아날로그 — _ensure_client lazy 싱글톤 :763-782, PROMPT_VERSION :77 캐시 규율, DEFAULT_VISION_MODEL env 주입 :104)
    - .planning/spikes/004-gemini-omni-view-editing/README.md (omni 실측 — 검증된 것은 영상 출력뿐, 텍스트 판정은 미검증 [ASSUMED A1])
    - backend/shared/python/sunity_shared/analysis/coach_writer.py:217-279 (graceful 실패 관례)
  </read_first>
  <action>
    per D-22/D-23. (1) 스모크 1콜 — belle Gemini 키(SSM 경유, 크레딧 확인)로 gemini-omni-flash-preview에 텍스트 판정 프롬프트 1건(짧은 더미) 호출해 응답 형식·비용을 확인. 성공 시 기본 모델 채택, 실패·형식 불충족 시 gemini-3.1-pro-preview 폴백 확정 — 결과를 SUMMARY와 spot_check.py 헤더 주석에 기록. 모델 ID는 env GEMINI_SPOTCHECK_MODEL(기본값 = 스모크 확정값)로 주입. (2) spot_check.py — gemini_vision_scorer 관례 복제: lazy client 싱글톤(top-level import 금지), SPOTCHECK_PROMPT_VERSION 상수(프롬프트 변경 시 bump — Pitfall 8), temp 0. 공개 함수 run_spot_check(video_ref, records: list[dict], praise_headline: str | None) -> dict — 각 record의 statusLine/cueLine 문장과 영상 일치 + praise 헤드라인 1문장(D-22 잘한 점 교차검증 — 같은 호출에 문장 1개 추가, 추가 API 콜 0)을 판정해 {status('done'|'skipped'|'failed'), hiddenRecordIndices(list[int] — 불일치 카드), praiseMismatch(bool), model, promptVersion} 반환. 프롬프트 설계는 재량(Claude's Discretion — "스팟체크 프롬프트 설계")이되 판정 기준을 보수적으로(불확실=일치 취급 — 과숨김 방지, 숨김은 명백 불일치만). 키 부재·API 실패 = status 'skipped'/'failed' + hiddenRecordIndices 빈 배열(no-op — 카드 전부 표시 유지, SP-3). 테스트: 무키 환경 no-op(praiseMismatch False 포함) / 반환 형상 flat / 불일치 인덱스 정수 검증.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/phase32/test_spot_check.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - spot_check.py에 run_spot_check + SPOTCHECK_PROMPT_VERSION + env GEMINI_SPOTCHECK_MODEL 주입 존재
    - top-level에 google.genai import 부재 (lazy만 — grep으로 함수 내부 import 확인)
    - 무키 환경 테스트에서 no-op(빈 hiddenRecordIndices + 비예외) green
    - 스모크 1콜 결과(채택 모델·근거)가 SUMMARY에 기록
  </acceptance_criteria>
  <done>스팟체크 어댑터가 검증된 모델로 graceful하게 성립</done>
</task>

<task type="auto">
  <name>Task 2: 사후 스테이지 배선 + spotCheck 계약 + 앱 숨김 소비</name>
  <files>backend/functions/pipeline/app.py, backend/shared/python/sunity_shared/firestore_admin.py, backend/shared/python/sunity_shared/models.py, docs/contract.md, app/src/types/analysis.ts, app/src/lib/userAnalyses.ts, app/src/app/analysis/result.tsx</files>
  <read_first>
    - backend/functions/pipeline/app.py:5236-5320 (firestore_complete → fault_zoom 사후 스테이지 — 스팟체크가 복제할 자리. _stage 컨텍스트 매니저 :122-140)
    - backend/shared/python/sunity_shared/firestore_admin.py:1138-1187 (update_analysis_fault_zoom — 사후 부분 업데이트 helper 뼈대. "그 외 result.* 사후 변경 금지" 경계 주석)
    - backend/shared/python/sunity_shared/models.py (32-06이 확장한 상수 블록 형태)
    - app/src/lib/userAnalyses.ts (32-06 normalize 헬퍼 3종 관례)
    - app/src/app/analysis/result.tsx (감점 카드 목록 렌더 — 32-11 배선 상태)
  </read_first>
  <action>
    per D-23. (1) 계약: models.py에 SPOT_CHECK_KEYS = ("status","hiddenRecordIndices","praiseMismatch","model","promptVersion") + SPOT_CHECK_STATUSES = ("done","skipped","failed") — lockstep 주석. analysis.ts에 SpotCheck interface(hiddenRecordIndices: number[] — flat 정수 배열, nested 아님) + result.spotCheck?. contract.md 절 추가. (2) firestore_admin에 update_analysis_spot_check(uid, analysis_id, spot_check: dict) — fault_zoom helper 뼈대 복제: status enum 검사 + hiddenRecordIndices 정수 검증 + field-path 부분 갱신("result.spotCheck") + updatedAt. (3) pipeline — fault_zoom 사후 스테이지 다음에 with _stage(timings_ms, analysis_id, "spot_check") 블록: run_spot_check 호출 → update_analysis_spot_check. 실패는 log.exception 후 통과(분석 훼손 금지). 동기 경로(firestore_complete 이전)에 호출 금지 — 구조로 속도 예산 보호. 로그에 불일치 시 record 내용 구조 로그 적재(D-23 "로그 적재" — analysis_id/criterion/사유). (4) 앱 — userAnalyses normalize에 spotCheck 방어 파싱(정수 배열 검증), result.tsx 감점 카드 목록에서 hiddenRecordIndices 포함 record는 카드 숨김(펼침 상세의 투명 내역에는 유지 — 채점 tally 불변, 숨김은 카드 표면만. 이 경계를 주석으로 명시). praiseMismatch=true면 요약 카드 praise를 summarySource 폴백 체인의 다음 소스로 강등(근거 미검증 칭찬 노출 방지 — D-06 정합). (5) 계약 3면 atomic 커밋.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/phase32 -x -q && cd ../app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - pipeline에 spot_check _stage 블록이 firestore_complete **이후** 위치 (코드 순서 확인)
    - update_analysis_spot_check가 "result.spotCheck" field-path 부분 갱신 + enum/정수 검증
    - result.tsx 카드 숨김 필터 존재 + ScoreBreakdownSection 투명 내역은 미필터 (grep 확인)
    - 계약 3면 + normalize 동시 수정 (단일 커밋)
    - typecheck + phase32 테스트 green
  </acceptance_criteria>
  <done>스팟체크가 사후 스테이지로 흐르고 불일치 카드가 표면에서만 숨겨짐</done>
</task>

<task type="auto">
  <name>Task 3: Pod 배포 + fixture 6동작 순차 스윕 (D-23 웨이브 게이트)</name>
  <files>(코드 수정 없음 — Pod 운영 + 스윕 기록)</files>
  <read_first>
    - .planning/phases/32-result-readability-3-omni/32-RESEARCH.md §Common Pitfalls 9/10 (스윕 순차 강제·Pod 운영 함정)
    - .planning/phases/32-result-readability-3-omni/32-03-SUMMARY.md (Pod 상태·배포 절차 기록)
  </read_first>
  <action>
    per D-23. (1) Pod /health 200 확인(미가동 시 32-03 절차 재사용) → 32-09~32-13 백엔드 커밋 push+pull+재시작+/health 재확인. GEMINI_SPOTCHECK_MODEL env 주입. (2) fixture 6동작 전수 스윕(순차 — 동시 호출 금지, kip-up 편중 금지): 각 분석에서 (a) 3단 문구·미션·coachQuestions 방출 확인(32-09 실측 — 이 웨이브가 첫 Pod 반영), (b) spotCheck.status done + 불일치 숨김·praiseMismatch 빈도 확인(과숨김·과강등 = 프롬프트 보수화 재조정), (c) timingsMs에서 동기 경로(사용자 체감 = status done까지) 소요가 직전 실측(124.7s 수준) 대비 회귀 없음 — spot_check 스테이지는 사후이므로 영향 0이어야 함. (3) 결과 표(동작×방출·숨김·타이밍)를 SUMMARY에 기록. 과숨김·오숨김 발견 시 프롬프트 조정 후 SPOTCHECK_PROMPT_VERSION bump하여 재스윕.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/phase32 -q | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - 6동작 스윕 결과 표 (spotCheck status + hidden 개수 + 동기 경로 timingsMs)
    - 동기 경로 소요 회귀 0 (fault_zoom 이전 스테이지 합계 기준, 직전 실측 대비)
    - 명백한 오숨김(정타 카드 숨김) 0 — 발견 시 조정·재스윕 기록
  </acceptance_criteria>
  <done>스팟체크가 프로덕션 Pod에서 6동작 검증되고 속도 예산이 지켜짐</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gemini API → 카드 숨김 판정 | 외부 LLM 판정이 표시 여부를 결정 |
| Pod 배포 | 프로덕션 파이프라인 코드 교체 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-30 | Tampering(신뢰) | 스팟체크 판정 | mitigate | 판정 권한 = 숨김만(점수·tally 무접촉 — 투명 내역 유지), 보수 기준(명백 불일치만), temp 0 + PROMPT_VERSION 캐시 규율 |
| T-32-31 | DoS | 사후 스테이지 실패 | mitigate | graceful no-op(무키/실패 = 전 카드 표시 유지) + _stage try/finally — 완료 분석 훼손 0 |
| T-32-32 | Info Disclosure | 영상 → Gemini | accept | 기존 vision 경로(veto/짚기)와 동일 데이터 경계 — 신규 노출면 없음. 키는 SSM만 |
| T-32-33 | Elevation | 사후 쓰기 표면 | mitigate | update helper가 result.spotCheck 단일 field-path만 갱신 (fault_zoom "그 외 사후 변경 금지" 경계 복제) |
</threat_model>

<verification>
- cd backend && python -m pytest tests/phase32 -x -q green + 전체 baseline 초과 0
- cd app && npm run typecheck clean
- Pod 6동작 스윕 표 + 속도 회귀 0
</verification>

<success_criteria>
- D-23 운영 자가검증 가동 — 불일치 카드 숨김+로그, 속도 예산 무회귀
- 32-09 방출(3단 문구·미션)이 프로덕션 Pod에서 첫 실측 확인됨
</success_criteria>

<output>
Create `.planning/phases/32-result-readability-3-omni/32-13-SUMMARY.md` when done
</output>
