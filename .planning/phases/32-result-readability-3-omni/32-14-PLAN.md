---
phase: 32-result-readability-3-omni
plan: 14
type: execute
wave: 9
depends_on: ["32-13"]
files_modified:
  - backend/shared/python/sunity_shared/analysis/keypoint_frame.py
  - backend/shared/python/sunity_shared/analysis/assemble.py
  - backend/shared/python/sunity_shared/firestore_admin.py
  - backend/shared/python/sunity_shared/models.py
  - docs/contract.md
  - app/src/types/analysis.ts
  - app/src/lib/deductionLabels.ts
  - backend/tests/phase32/test_keypoint_report_expansion.py
autonomous: true
model: fable
requirements: [D-22]
must_haves:
  truths:
    - "keypointReport.joints가 8→12로 확장되어(+발목2·팔꿈치2) 새 분석부터 좌표가 영속된다 — 표시 승격 1단 (D-22)"
    - "감점 반영은 하지 않는다 — 신규 관절은 표시·측정만, 감점 편입은 신뢰도 실측 게이트(32-15 스윕 conf 분포) 통과 후 별도 (fail-safe)"
    - "기존 8관절 doc과 신규 12관절 doc을 앱이 joints 배열 길이로 하위호환 소비한다"
    - "fixture 6동작 전수 스윕(순차)으로 방출·validator 변경의 동작별 무회귀가 이 웨이브 안에서 검증된다 (D-23)"
    - "18fps reference(≈540프레임×12×2)의 Firestore 1MB 한도·인덱스 면제가 계산·확인된다 (RESEARCH A7)"
    - "화살표(ARROW_JOINT_MAP)를 되살리는 방향의 작업 금지 — 외부 큐 문장이 대체 (SEED ⑪)"
  artifacts:
    - path: "backend/tests/phase32/test_keypoint_report_expansion.py"
      provides: "12관절 방출·validator·하위호환(8관절 doc) 테스트"
  key_links:
    - from: "backend/shared/python/sunity_shared/analysis/keypoint_frame.py"
      to: "assemble.build_keypoint_report + firestore_admin._validate_keypoint_report"
      via: "_KEYPOINT_NAMES 단일 출처 파생 (len() 자동 추종)"
      pattern: "_KEYPOINT_NAMES"
---

<objective>
엔진 레버 2/3 — RTMW 측정층 확장 1단(D-22): 백본이 이미 검출하는 발목·팔꿈치 좌표를 표시층(keypointReport.joints 8→12)으로 승격한다. 감점 반영(2단)은 관절별 신뢰도 실측 게이트 뒤로 — 이 플랜은 표시·측정 승격과 하위호환만.

Purpose: "데이터는 있는데 영속만 안 되는 상태"(31 화살표 원인의 구조)를 해소해 오버레이·번역 레이어의 좌표 재료를 넓힌다.
Output: 12관절 방출 + 계약 3면 + validator + 앱 하위호환 + 용량 검증.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-result-readability-3-omni/32-CONTEXT.md
@.planning/phases/32-result-readability-3-omni/32-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: keypoint_frame 8→12 + assemble + validator + 계약 3면</name>
  <files>backend/shared/python/sunity_shared/analysis/keypoint_frame.py, backend/shared/python/sunity_shared/analysis/assemble.py, backend/shared/python/sunity_shared/firestore_admin.py, backend/shared/python/sunity_shared/models.py, docs/contract.md, app/src/types/analysis.ts, backend/tests/phase32/test_keypoint_report_expansion.py</files>
  <read_first>
    - backend/shared/python/sunity_shared/analysis/keypoint_frame.py:51-100 (_KEYPOINT_NAMES 8개 + JOINT_KEY_TO_ANGLE_KEY loose 매핑 + NUM_KEYPOINTS_PHASE12 len() 파생 + data flat T×J×2)
    - backend/shared/python/sunity_shared/analysis/assemble.py:863 부근 (build_keypoint_report — 방출 조립)
    - backend/shared/python/sunity_shared/firestore_admin.py:716 부근 (_validate_keypoint_report — len==8 강제부 실측)
    - backend/shared/python/sunity_shared/analysis/skeleton.py:39-48 (각도층 JOINT_ANGLES 8관절 — 무릎 각이 발목 키포인트를 이미 소비 = RTMW 데이터 실존 증거. 각도층은 이 플랜에서 무변경)
    - .planning/phases/32-result-readability-3-omni/32-RESEARCH.md §RTMW 측정층 확장 (확장 절차 5단계 + 이름공간 함정)
  </read_first>
  <behavior>
    - Test 1: 확장 후 build_keypoint_report 방출 joints == 12 (+left/right_ankle, +left/right_elbow) + data 길이 T×12×2 정합
    - Test 2: validator가 12관절 신규 doc과 8관절 legacy doc 형상을 모두 통과 (len 강제가 {8,12} 허용 또는 joints 길이 파생 검증으로 전환)
    - Test 3: confidence 배열도 T×J 정합 (J=12)
    - Test 4: JOINT_KEY_TO_ANGLE_KEY에 ankle/elbow 1:1 매핑 추가 확인
  </behavior>
  <action>
    per D-22 1단. (1) keypoint_frame.py — _KEYPOINT_NAMES에 left_ankle/right_ankle/left_elbow/right_elbow 4개 추가(8→12, wrist는 hand로 기존재). JOINT_KEY_TO_ANGLE_KEY에 1:1 추가. NUM_KEYPOINTS_PHASE12는 len() 파생이라 자동 추종 — 하드코딩 12 금지. (2) assemble.build_keypoint_report — RTMW COCO-17 출력에서 신규 4관절 좌표·confidence 추출 배선(백본은 이미 검출 — pose 추론 코드 무변경, 추출만 확장). (3) firestore_admin._validate_keypoint_report — len==8 강제부를 len(joints) 파생 검증(joints ∈ {8,12} 허용 + data/confidence 길이 = T×J 정합)으로 전환. (4) 계약 3면: models.py 관련 상수(존재 시) + analysis.ts KeypointReport joints 서술("8=legacy, 12=phase32 확장 — 배열 길이로 판별" 하위호환 주석) + contract.md. atomic 커밋. (5) **감점 무유입 가드**: 신규 관절이 감점 경로(deduction/kismam/dimensions)에 유입되지 않음을 증명 — 각도층 JOINT_ANGLES 무변경(diff 0) + 감점 모듈 diff 0. 화살표 ARROW_JOINT_MAP 수정 금지(대체 대상 — SEED ⑪).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/phase32/test_keypoint_report_expansion.py -x -q && rtk git diff --stat -- backend/shared/python/sunity_shared/analysis/skeleton.py backend/shared/python/sunity_shared/analysis/dimensions.py backend/shared/python/sunity_shared/analysis/kismam.py</automated>
  </verify>
  <acceptance_criteria>
    - behavior 4건 green
    - skeleton.py/dimensions.py/kismam.py diff 0 (감점·각도층 무접촉)
    - fault_zoom.py의 ARROW_JOINT_MAP diff 0
    - 계약 3면 동시 수정 단일 커밋
  </acceptance_criteria>
  <done>12관절 방출이 하위호환·감점 무유입으로 성립</done>
</task>

<task type="auto">
  <name>Task 2: 앱 하위호환 — 하드코딩 8 전수 제거 + 라벨 추가</name>
  <files>app/src/types/analysis.ts, app/src/lib/deductionLabels.ts</files>
  <read_first>
    - app/src/lib/deductionLabels.ts:28-42 (JOINT_LABEL_KO — ankle/elbow 라벨 추가 지점)
    - app/src/components/KeypointOverlay.tsx + app/src/components/PoseCompareFrames.tsx + app/src/components/PoseCompareViewer.tsx + app/src/app/analysis/result.tsx (joints 소비처 — 하드코딩 8/인덱스 가정 전수 grep 대상)
  </read_first>
  <action>
    per D-22 1단. (1) 앱 전체에서 keypointReport.joints 길이 8 가정을 전수 grep(리터럴 8 비교, 고정 인덱스 접근, NUM_KEYPOINTS 류 상수) — 소비처는 joints 리스트 기반이므로 길이 가정 검사만 제거/파생화. 발견·수정 목록을 SUMMARY에 기록(0건이면 0건 증명). (2) deductionLabels.JOINT_LABEL_KO에 left/right_ankle('왼쪽 발목'/'오른쪽 발목')·left/right_elbow('왼쪽 팔꿈치'/'오른쪽 팔꿈치') 추가 — 이중 키 공간 커버 관례 유지. (3) 12관절 doc과 8관절 doc 각각의 오버레이 렌더가 배열 길이로 자연 분기됨을 typecheck+코드 추적으로 확인.
  </action>
  <verify>
    <automated>cd app && npm run typecheck && grep -c "발목" src/lib/deductionLabels.ts</automated>
  </verify>
  <acceptance_criteria>
    - JOINT_LABEL_KO에 ankle/elbow 4항목 존재
    - joints 길이 8 하드코딩 가정 잔존 0 (grep 결과 SUMMARY 기록)
    - typecheck clean
  </acceptance_criteria>
  <done>앱이 8/12 doc을 길이 판별로 하위호환 소비</done>
</task>

<task type="auto">
  <name>Task 3: 용량·인덱스 검증 + Pod 배포 + fixture 6동작 전수 스윕 (D-23)</name>
  <files>(코드 수정 없음 — 계산·운영 검증)</files>
  <read_first>
    - .planning/phases/32-result-readability-3-omni/32-RESEARCH.md §Assumptions A7 (18fps reference 1MB 한도 계산 필요) + §RTMW 확장 3 (인덱스 면제 — reference 기면제, analyses 쪽 확인)
  </read_first>
  <action>
    per D-22 1단 + A7. (1) 용량 계산 — 신규 12관절 doc: 사용자 9fps 30초=270프레임×12×2 float + confidence 270×12, reference 18fps≈540프레임×12×2 — JSON 직렬화 근사 바이트로 1MB 한도 여유 계산(초과 위험 시 fps 다운샘플/분리 저장 옵션을 SUMMARY에 제안하고 배포 보류). (2) 인덱스 — analyses 컬렉션의 keypointReport 인덱스 면제 상태 확인(기존 fix 확인 — analyses-index-exemption-fix), 신규 필드가 면제 범위에 드는지 gcloud로 확인(owner 계정 필요 시 belle 안내). (3) Pod 배포(push→pull→재시작→/health 200) 후 fixture 6동작 전수 스윕(순차 — kip-up 편중 금지, verify-all-fixtures-not-kipup-only 원칙. 이 플랜은 방출·validator 변경이라 동작별 회귀를 이 웨이브 안에서 잡아야 함 — D-23 웨이브 게이트): 각 doc의 joints 12 방출·confidence 채움·validator 통과 + 점수/verdict가 직전 스윕(32-13) 기준선 대비 무회귀임을 확인하고, 앱 오버레이 정상 렌더(시뮬레이터 1건 이상) 실측. 신규 관절 conf 분포(발목·팔꿈치 — 동작별)를 추출해 SUMMARY에 기록 — 32-15 신뢰도 게이트 재료.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" "$RUNPOD_ANALYZE_URL/health" | grep -q 200 && echo POD_OK</automated>
  </verify>
  <acceptance_criteria>
    - 1MB 계산 결과(바이트 근사·여유율) SUMMARY 기록
    - 6동작 전 doc의 keypointReport.joints 길이 == 12 실측 + 점수/verdict 기준선(32-13 스윕) 대비 무회귀 표
    - 신규 관절 conf 분포 수치 기록 — 동작별 (2단 게이트 재료)
    - 시뮬레이터 오버레이 렌더 정상 (스크린샷)
  </acceptance_criteria>
  <done>12관절이 용량 안전 확인 후 6동작 전수 스윕으로 동작별 무회귀까지 검증됨</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pod → Firestore | 확장 doc 영속 (용량·인덱스 리스크) |
| 신규 관절 → 채점 | 위양성 감점 유입 위험 (가림 많은 폴 동작) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-34 | Tampering(점수) | 신규 관절 감점 유입 | mitigate | 1단 = 표시·측정만 — 각도층·감점 모듈 diff 0을 acceptance로 강제, 2단은 conf 실측 게이트 후 별도 |
| T-32-35 | DoS | Firestore 1MB/인덱스 | mitigate | 배포 전 용량 계산 + 인덱스 면제 확인 — 초과 위험 시 배포 보류 절차 명시 |
| T-32-36 | DoS(크래시) | legacy 8관절 doc | mitigate | validator {8,12} 허용 + 앱 길이 판별 하위호환 (Test 2 + Task 2) |
</threat_model>

<verification>
- cd backend && python -m pytest tests/phase32 -x -q green + baseline 초과 0
- cd app && npm run typecheck clean
- Pod 실측 1건 (joints 12 + conf 분포)
</verification>

<success_criteria>
- 측정층 1단 승격 완료 — 표시 재료 확장, 감점 무유입, 하위호환
- 2단(감점 편입) 판단 재료(conf 분포)가 축적됨
</success_criteria>

<output>
Create `.planning/phases/32-result-readability-3-omni/32-14-SUMMARY.md` when done
</output>
