---
phase: 32-result-readability-3-omni
plan: 15
type: execute
wave: 10
depends_on: ["32-14"]
files_modified:
  - backend/shared/python/sunity_shared/analysis/inversion_warp.py
  - backend/shared/python/sunity_shared/analysis/pose_estimator.py
  - backend/tests/phase32/test_inversion_warp.py
autonomous: false
model: fable
requirements: [D-22, D-23]
must_haves:
  truths:
    - "PR(PersPose 위상회전) 보정이 2-pass 조건부 아키텍처로 확정된다: 1차 추론(기존 경로) → 1차 keypoint로 인버전 검출 → 참일 때만 워프+2차 추론 → 좌표 H⁻¹ 역변환으로 원본 프레임 공간 복원 — '추론 전 keypoint 검출' 순환 의존 해소 (리뷰 blocker 6)"
    - "워프 좌표의 원본 공간 복원(forward→inverse round-trip)이 단위 테스트로 증명된다 — 오버레이·crop·채점 좌표계 불변"
    - "검출 휴리스틱이 spike 실측 keypoint 시퀀스(.planning/spikes/004 kpts/ — .npz)로 검증된다 — invert 계열 TP + 비인버전(power-spin·sideway 등) FP 0 (리뷰: 단일 fixture 불충분 보완)"
    - "게이트 수치가 고정된다: invert fixture boneCV 개선 ≥30%(spike −58% 근거의 보수 하한) / 비인버전 5동작 detect False → 워프 미적용 = 기존 경로 바이트 동일 → 점수·verdict diff 0 / invert 분석 latency 증가 ≤ +60s (2차 추론 비용 상한)"
    - "PR_INVERSION_ENABLED 코드 기본값 off — 제한 통합 게이트 통과 후에만 Pod env로 on (게이트 실패 시 off 유지가 phase 안전 마감 경로)"
    - "fixture 6동작 최종 전수 스윕(순차, kip-up 편중 금지)에서 invert 개선 + 비인버전 5동작 무회귀(특히 power-spin)가 양방 검증된다 (D-23)"
    - "phase 32 엔진 웨이브 전체(스팟체크·12관절·PR)의 최종 전수 스윕이 통과하고 belle이 결과를 확인한다 — D-22 '짚기 정밀화'를 스팟체크 사후 검수로 갈음한 해석의 belle 확인 포함 (W-3)"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/inversion_warp.py"
      provides: "인버전 검출 휴리스틱(순수) + 호모그래피 forward/inverse + 조건부 워프 어댑터 (spike pr_warp_pod 편입)"
    - path: "backend/tests/phase32/test_inversion_warp.py"
      provides: "검출(실데이터 벡터)·round-trip·조건부 게이트 단위 테스트"
  key_links:
    - from: "backend/shared/python/sunity_shared/analysis/pose_estimator.py"
      to: "inversion_warp.py"
      via: "1차 추론 결과 → 검출 → 조건부 2차 추론 훅 (lazy import, Protocol 어댑터 관례)"
      pattern: "inversion"
---

<objective>
엔진 레버 3/3 — PR 인버전 보정(D-22): spike 006 실측(−58%)을 근거로 거꾸로 동작 한정 좌표 품질 보정을 Pod 추론 경로에 넣는다. 이어 phase 32 엔진 웨이브 전체의 fixture 6동작 최종 전수 스윕(D-23 게이트)으로 phase를 마감한다.

리뷰 blocker 6 반영(아키텍처 확정): spike의 pr_warp_pod.py는 워프 중심을 "RTMW kpts 보간+평활"로 구했다 — 즉 keypoint가 선행한다. 프로덕션 정합 형태는 **2-pass 조건부**: ① 1차 추론 = 기존 경로 그대로(항상 실행 — 이것이 곧 분석), ② 1차 keypoint 시퀀스로 인버전 검출(순수 numpy), ③ 참일 때만 1차 kpts 평활 중심으로 H=K·R·K⁻¹ 워프 → 워프 프레임 2차 추론 → 예측 좌표를 프레임별 H⁻¹로 원본 영상 좌표계에 역변환해 교체(신뢰도는 2차 것). 거짓이면 1차 결과 그대로(바이트 동일). 이 구조는 순환 의존이 없고, 좌표는 항상 원본 프레임 공간이라 오버레이·crop·채점 좌표계가 불변이다. 비용은 인버전 분석에만 2차 추론이 추가된다(상한 +60s — RTMW ~50fps에서 270프레임 추론 수 초 + 워프 I/O 보수 예산). 게이트 통과 전 기본 off — 실패 시 off 유지로 phase는 안전 마감(전면 재설계는 후속 결정).

Purpose: 인버전 좌표 품질 개선 + 무회귀 양방 증명 + phase 게이트.
Output: inversion_warp 어댑터(2-pass) + 제한 통합 게이트 + 최종 스윕 결과 + belle 확인.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-result-readability-3-omni/32-CONTEXT.md
@.planning/phases/32-result-readability-3-omni/32-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 검출 휴리스틱(실데이터 벡터) + 호모그래피 forward/inverse round-trip (로컬 순수)</name>
  <files>backend/shared/python/sunity_shared/analysis/inversion_warp.py, backend/tests/phase32/test_inversion_warp.py</files>
  <read_first>
    - .planning/spikes/004-gemini-omni-view-editing/README.md §Spike 006 (PR 실측 — invert boneCV 1.16→0.489, power-spin 전면 적용 1.03→7.0 파괴, "중심=RTMW kpts 9fps 보간+평활" — 2-pass 근거)
    - .planning/spikes/004-gemini-omni-view-editing/pr_warp_pod.py (spike 산출물 원본 — Rodrigues 회전 → H=K·R·K⁻¹ 호모그래피 워프. 편입 원본)
    - .planning/spikes/004-gemini-omni-view-editing/kpts/ (spike가 저장한 실측 keypoint 시퀀스 — **.npz(numpy) 포맷**: invert.npz·Chair-spin.npz·kip-up.npz 등 — 검출 테스트 벡터로 사용 — 리뷰: 합성 시퀀스 단독 검증 불충분 보완)
    - backend/shared/python/sunity_shared/analysis/pose_estimator.py (어댑터 관례 — Protocol, lazy import, CUDA 전제. 다음 태스크의 훅 지점 파악만)
  </read_first>
  <behavior>
    - Test 1 (검출 — 합성): 엉덩이 y < 어깨 y가 지속 구간(윈도우 비율 임계)인 keypoint 시퀀스 입력 → is_inverted True. 정립 시퀀스 → False. 짧은 순간 역전(점프 프레임)만으로는 False (지속성 요구 — 고속 스핀 오검출 방지)
    - Test 2 (검출 — spike 실데이터): kpts/ .npz 시퀀스 로드 — invert 계열(invert·straddle-invert) → True, 비인버전(power-spin·sideway-spin·chair 등 가용 전부) → False (TP ≥ 2 / FP == 0)
    - Test 3 (round-trip): 임의 좌표 p에 H 적용 후 H⁻¹ 적용 → p 복원 (eps ≤ 1e-6). 프레임 경계 좌표·경계 밖 투영 좌표도 유한 복원
    - Test 4 (fail-safe): 역변환 결과가 NaN/비유한이거나 원본 프레임 범위를 크게 벗어나는 프레임 → 해당 프레임은 1차(원본) 좌표 유지 표식 반환 (프레임 단위 폴백 — 전체 폐기 아님)
    - Test 5 (순수성): detect_inversion·호모그래피 계산에 torch import 0 (numpy만)
  </behavior>
  <action>
    per D-22 + 리뷰 blocker 6. inversion_warp.py 신설 — 3부 구성: (a) detect_inversion(keypoints_seq) 순수 휴리스틱(numpy만 — 엉덩이 y < 어깨 y 지속 구간 비율 임계. 임계값은 spike kpts 실데이터로 캘리브레이션한 보수 상수 — 상수 명시+근거 주석: invert 계열 전부 검출되고 비인버전 전부 미검출인 여유 있는 값). (b) 호모그래피 유틸: build_homography(center_seq, K 근사 — pr_warp_pod.py의 Rodrigues→H=K·R·K⁻¹ 로직 편입, 중심은 1차 kpts 보간+평활 — spike 방식 그대로) + warp_points(H, pts) + unwarp_points(H, pts)(H⁻¹ — **2차 추론 좌표를 원본 프레임 공간으로 복원하는 핵심**, 리뷰 지적 '역변환 절차 부재' 해소). (c) 프레임 워프 적용부(영상 프레임 H 워프)는 GPU/Pod 경로 — lazy import 어댑터 관례. Test 4의 프레임 단위 fail-safe(비유한/범위 이탈 → 원본 좌표 유지)를 unwarp 반환 계약에 포함. 테스트는 순수 부분만 로컬(GPU 워프 실행은 Task 2). spike kpts **.npz(numpy — np.load)** 로드는 테스트 픽스처 헬퍼로(경로 존재 없으면 해당 테스트 skip 마킹 — CI 환경 안전).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/phase32/test_inversion_warp.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - behavior 5건 green (spike 실데이터 TP ≥ 2 / FP 0 포함)
    - unwarp_points(H⁻¹ 역변환) 함수 존재 + round-trip eps 테스트 green
    - detect_inversion·호모그래피에 torch import 0
    - 임계값 상수에 spike 근거(캘리브레이션 데이터·여유) 주석
  </acceptance_criteria>
  <done>검출(실데이터 검증)과 좌표 왕복(원본 공간 복원)이 순수 함수 + 테스트로 성립</done>
</task>

<task type="auto">
  <name>Task 2: pose_estimator 2-pass 조건부 훅 + 제한 통합 게이트 (invert + power-spin, env 기본 off)</name>
  <files>backend/shared/python/sunity_shared/analysis/pose_estimator.py, backend/shared/python/sunity_shared/analysis/inversion_warp.py</files>
  <read_first>
    - backend/shared/python/sunity_shared/analysis/pose_estimator.py (수정 대상 — 추론 파이프라인 구조 실측: 프레임 입력→keypoint 출력 지점. 2-pass 훅은 1차 추론 **후** 삽입)
    - backend/shared/python/sunity_shared/analysis/interfaces.py (Protocol 계약 형태)
    - .planning/phases/32-result-readability-3-omni/32-14-SUMMARY.md (직전 스윕 기준선 — 게이트 비교 대상)
  </read_first>
  <action>
    per D-22 + 리뷰 blocker 6. (1) pose_estimator.py — **2-pass 조건부 훅**(1차 추론 완료 지점 뒤): PR_INVERSION_ENABLED env(**코드 기본 off** — 게이트 전 프로덕션 무영향)일 때, detect_inversion(1차 kpts) 참이면 1차 kpts 평활 중심으로 프레임 워프 → 2차 추론 → unwarp_points로 원본 좌표 복원(프레임 단위 fail-safe 포함) → 결과 교체. 거짓이면 1차 결과 그대로(기존 경로 바이트 동일 — 비인버전 무회귀의 구조 보장). 로그: is_inverted/적용 여부/2차 추론 소요를 key=value로. (2) Pod 배포(push→pull→재시작→/health 200 — env off 상태라 프로덕션 무변화). (3) **제한 통합 게이트(리뷰 권고 'bounded integration spike'의 phase 내 실행):** Pod에서 env on을 명시한 수동 실행 2건만 — (a) invert fixture: boneCV(또는 spike 동일 좌표 기질 지표) 1차 대비 **≥30% 개선** + 역변환 좌표가 원본 프레임 범위 내(오버레이 PNG 육안 정합 포함) + 분석 latency 증가 **≤ +60s**, (b) power-spin fixture: detect False 로그 확인 + 점수·verdict가 기준선(32-14 스윕) 대비 **diff 0**(바이트 동일 경로 증명). (4) 게이트 판정: 양쪽 통과 → Task 3에서 env on 전수 스윕. 실패 → env off 유지(프로덕션 무영향 = phase 안전 마감 경로), 원인·수치를 SUMMARY에 기록하고 Task 4에서 belle에게 "PR off 마감 + 후속 재설계" 옵션으로 보고 — 플랜이 임의로 재설계를 확장하지 않는다.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/phase32/test_inversion_warp.py -x -q && curl -s -o /dev/null -w "%{http_code}" "$RUNPOD_ANALYZE_URL/health" | grep -q 200 && echo POD_OK</automated>
  </verify>
  <acceptance_criteria>
    - pose_estimator 훅이 1차 추론 후 검출→조건부 2차 구조 (grep으로 detect_inversion 호출 위치 확인) + env 기본 off
    - 제한 게이트 실측 기록: invert 개선 %·latency 증가·power-spin diff 0 (수치 — SUMMARY)
    - detect False 경로에서 기존 코드 경로 무변경 (diff/로그 증명)
    - 게이트 실패 시 env off 유지 + 사유 기록 (해당 시)
  </acceptance_criteria>
  <done>2-pass 조건부 보정이 안전판(기본 off)과 제한 게이트 실측으로 성립 — 순환 의존·좌표계 훼손 없음</done>
</task>

<task type="auto">
  <name>Task 3: fixture 6동작 최종 전수 스윕 — 엔진 레버 3종 통합 게이트 (D-23 phase 게이트)</name>
  <files>(코드 수정 없음 — 스윕 실행·기록. 실패 시 조정 커밋 허용)</files>
  <read_first>
    - .planning/phases/32-result-readability-3-omni/32-RESEARCH.md §PR 인버전 보정 (게이트 기준 — invert 개선 + 비인버전 5동작 무회귀 양방)
    - .planning/phases/32-result-readability-3-omni/32-13-SUMMARY.md, 32-14-SUMMARY.md (직전 스윕 기준선 수치)
  </read_first>
  <action>
    per D-22/D-23. Task 2 게이트 통과를 전제(실패 시 이 스윕은 env off 상태로 실행 — 레버 2종(스팟체크·12관절)의 최종 확인만 수행하고 PR 항목은 '게이트 미통과·off 마감'으로 기록). (1) env on(게이트 통과 시 — production start_server.sh 박제 mirror 포함) 상태로 fixture 6동작 전수 스윕 — 순차 강제(pipeline-not-concurrency-safe), kip-up 편중 금지: (a) invert 동작 — 좌표 품질 지표 개선 ≥30% 유지 확인, (b) 비인버전 5동작(특히 power-spin) — detect False + 점수·verdict·좌표 지표가 32-13/32-14 스윕 기준선 대비 **diff 0**, (c) 12관절 conf 분포(32-14 재료 누적) + 스팟체크 정상 동작 + 크롭 PNG 전수 육안(32-01 수리분 최종 확인), (d) invert 분석 latency가 예산(+60s) 내. (2) **결정론 확인(리뷰 MEDIUM 스코프 정정):** cold 재실행 1건의 동일성 판정은 **포즈·측정 기질(keypoint·각도·감점 수치)에 한정** — Gemini 텍스트 산출(코칭 문장·스팟체크)은 캐시 히트 시 동일, 미스 시 제외(temp 0 + PROMPT_VERSION 캐시가 실보장 — TRUST-06. 판정 범위를 표에 명기). (3) 회귀 발견 시: PR_INVERSION_ENABLED off로 즉시 복구 → 원인 조정 → 재스윕(조정 커밋 기록). (4) 최종 결과 표(6동작 × 점수/verdict/좌표 지표/latency/스팟체크/joints)를 SUMMARY에 작성.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/phase32 -q | tail -2 && curl -s -o /dev/null -w "%{http_code}" "$RUNPOD_ANALYZE_URL/health" | grep -q 200 && echo POD_OK</automated>
  </verify>
  <acceptance_criteria>
    - 6동작 스윕 표 완성 (invert 개선 수치 ≥30% 또는 off-마감 기록 + 비인버전 5동작 diff 0)
    - power-spin 점수·verdict 기준선 대비 변동 0 (또는 원인 규명·복구 기록)
    - invert latency 예산(+60s) 내 기록
    - 크롭 PNG 전수 육안 결과 기록
    - cold 재실행 결정론 확인 (포즈·측정 기질 한정 — 판정 범위 명기)
  </acceptance_criteria>
  <done>엔진 레버 3종이 반영된 파이프라인이 전수 스윕 양방 게이트를 수치 기준으로 통과 (또는 PR off 안전 마감)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: belle 최종 확인 — 엔진 웨이브 스윕 결과 + phase 32 마감</name>
  <what-built>엔진 레버 3종: omni 스팟체크(recordId 기반 불일치 카드 숨김), RTMW 12관절 표시 승격, PR 인버전 2-pass 조건부 보정(게이트 통과 시 on / 미통과 시 off 마감) — 전수 스윕 결과 포함</what-built>
  <how-to-verify>
    1. 스윕 결과 표 검토 — invert 개선 수치(≥30% 게이트), 비인버전 무회귀(power-spin diff 0), invert latency(+60s 내), 스팟체크 숨김·praise 판정 빈도, 12관절 conf 분포. PR이 off 마감이면 게이트 실측 수치와 후속 옵션(재설계 트랙) 보고
    2. 실기기에서 새 분석 1건(가능 시) — 스팟체크·12관절 오버레이·크롭 parity가 실사용에서 정상인지
    3. 2단(신규 관절 감점 편입) 판단 — conf 분포 재료 기준: 지금 게이트 열지 / 파일럿 데이터 더 본 뒤 판단할지 (후속 결정, 이 phase 범위 밖)
    4. PR on/off 최종 상태 확인 — on이면 유지 승인, off면 후속 트랙 여부
    5. D-22 '짚기 정밀화' 해석 확인 (W-3): 이번 phase에서 짚기 정밀화 = 스팟체크 사후 검수(동기 경로 신규 호출 0 — phase 27 속도 예산 보호)로 갈음한 해석이 맞는지 belle 확인 — 추가 짚기 정밀화(동기 경로 지점 선별)가 필요하면 후속 트랙으로 기록
    6. 잔여 실기기 확인 항목은 32-HUMAN-UAT.md 적립 (batch-uat 관례)
  </how-to-verify>
  <action>최종 스윕 결과 표(invert 개선·비인버전 무회귀·latency·스팟체크 빈도·conf 분포 + PR on/off 상태)를 belle에게 제시하고 실기기 확인(가능 시 새 분석 1건)을 진행한다. D-22 짚기 정밀화 = 사후 검수 갈음 해석의 확인을 명시적으로 받는다(W-3 — 32-13 objective 해석의 비준 지점). 2단 감점 편입 판단은 후속 결정으로 기록만 하고, 잔여 실기기 항목은 32-HUMAN-UAT.md에 적립한다.</action>
  <verify>
    <automated>cd backend && python -m pytest tests/phase32 -q | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - 스윕 결과 표가 SUMMARY에 존재 + belle 승인 기록
    - D-22 짚기 정밀화=스팟체크 사후 검수 갈음 해석의 belle 확인 기록 (W-3)
    - PR on/off 최종 상태 + 2단(감점 편입) 판단 상태가 GATE-DECISIONS 또는 SUMMARY에 기록
  </acceptance_criteria>
  <done>엔진 웨이브가 belle 승인으로 마감되고 phase 32 실행 종료</done>
  <resume-signal>"승인" 시 phase 32 실행 완료 — /gsd-verify-work 진행. 이슈 발견 시 즉시 수리 (fix-now)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 2차 추론·역변환 → 좌표 품질 | 잘못된 워프/역변환이 좌표를 훼손하면 오버레이·채점까지 오염 |
| Pod 배포 | 프로덕션 추론 경로 변형 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-37 | Tampering(품질) | 오검출 워프 적용 | mitigate | 지속성 요구 휴리스틱 + spike 실데이터 FP 0 검증(Test 2) + detect False = 기존 경로 바이트 동일 + 비인버전 5동작 diff 0 스윕 + env 기본 off·제한 게이트 선행 |
| T-32-41 | Tampering(좌표계) | 역변환 누락·오류 | mitigate | unwarp round-trip 테스트(Test 3) + 프레임 단위 fail-safe(Test 4) + 오버레이 육안 정합을 제한 게이트에 포함 — 워프 공간 좌표가 doc에 실리는 경로 차단 |
| T-32-38 | DoS | 스윕 중 프로덕션 오염 | mitigate | 순차 실행 강제(동시 호출 = 오염 — pipeline-not-concurrency-safe) + 스윕은 fixture 계정 한정 + latency 예산 상한 |
| T-32-39 | Repudiation | 게이트 통과 주장 | mitigate | 수치 게이트(≥30%·diff 0·+60s)·기준선 diff·결정론(범위 명기)을 SUMMARY에 수치로 기록 — belle 확인 checkpoint로 종결 |
</threat_model>

<verification>
- cd backend && python -m pytest tests/phase32 -x -q green + 전체 baseline 초과 0
- 제한 통합 게이트(2건) 수치 기록 → 6동작 전수 스윕 양방 게이트 통과 (표 기록)
- belle 승인 (D-22 짚기 정밀화 해석 확인 포함)
</verification>

<success_criteria>
- D-22 레버 3종 전부 반영(PR은 수치 게이트 통과 시 on — 미통과 시 off 안전 마감 + 후속 옵션 보고) + D-23 개발 게이트(전수 스윕) 통과
- 좌표계 불변(역변환 증명)·순환 의존 0 (리뷰 blocker 6 해소)
- D-22 짚기 정밀화 해석이 belle 명시 확인으로 비준됨 (W-3)
- phase 32 실행 종료 — 다음: /gsd-verify-work 32
</success_criteria>

<output>
Create `.planning/phases/32-result-readability-3-omni/32-15-SUMMARY.md` when done
</output>
