---
phase: 32-result-readability-3-omni
plan: 15
type: execute
wave: 9
depends_on: ["32-14"]
files_modified:
  - backend/shared/python/sunity_shared/analysis/inversion_warp.py
  - backend/shared/python/sunity_shared/analysis/pose_estimator.py
  - backend/tests/phase32/test_inversion_warp.py
autonomous: false
model: fable
requirements: [D-22, D-23]
must_haves:
  truths:
    - "PR(PersPose 위상회전) 보정이 인버전(거꾸로) 검출 시에만 조건부 적용된다 — 전면 적용 금지 (spike 실측: invert −58% 개선 / power-spin 전면 적용 시 7.0 파괴) (D-22)"
    - "fixture 6동작 전수 스윕(순차, kip-up 편중 금지)에서 invert 개선 + 비인버전 5동작 무회귀(특히 power-spin)가 양방 검증된다 (D-23)"
    - "phase 32 엔진 웨이브 전체(스팟체크·12관절·PR)의 최종 전수 스윕이 통과하고 belle이 결과를 확인한다"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/inversion_warp.py"
      provides: "인버전 검출 휴리스틱(순수) + 조건부 워프 어댑터 (spike pr_warp_pod 편입)"
    - path: "backend/tests/phase32/test_inversion_warp.py"
      provides: "검출 휴리스틱·조건부 게이트 단위 테스트"
  key_links:
    - from: "backend/shared/python/sunity_shared/analysis/pose_estimator.py"
      to: "inversion_warp.py"
      via: "추론 전처리 조건부 훅 (lazy import, Protocol 어댑터 관례)"
      pattern: "inversion"
---

<objective>
엔진 레버 3/3 — PR 인버전 보정(D-22): spike 006 실측(−58%)을 근거로 거꾸로 동작 한정 좌표 품질 보정을 Pod 추론 경로에 조건부로 넣는다. 이어 phase 32 엔진 웨이브 전체의 fixture 6동작 최종 전수 스윕(D-23 게이트)으로 phase를 마감한다.

Purpose: 인버전 좌표 품질 개선 + 무회귀 양방 증명 + phase 게이트.
Output: inversion_warp 어댑터 + 조건부 훅 + 최종 스윕 결과 + belle 확인.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-result-readability-3-omni/32-CONTEXT.md
@.planning/phases/32-result-readability-3-omni/32-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 인버전 검출 휴리스틱 + 조건부 워프 어댑터 편입</name>
  <files>backend/shared/python/sunity_shared/analysis/inversion_warp.py, backend/shared/python/sunity_shared/analysis/pose_estimator.py, backend/tests/phase32/test_inversion_warp.py</files>
  <read_first>
    - .planning/spikes/004-gemini-omni-view-editing/README.md (PR 실측 — invert boneCV 1.16→0.489, power-spin 전면 적용 1.03→7.0 파괴, sideway-spin/peter-pan 동반 악화 — 조건부 근거)
    - .planning/spikes/004-gemini-omni-view-editing/ 디렉터리의 pr_warp_pod.py (spike 산출물 원본 — Rodrigues 회전 → H=K·R·K⁻¹ 호모그래피 워프. Pod 볼륨 사본 존재)
    - backend/shared/python/sunity_shared/analysis/pose_estimator.py (어댑터 관례 — Protocol, lazy import, CUDA 전제. 전처리 훅 삽입 지점 실측)
    - backend/shared/python/sunity_shared/analysis/interfaces.py (Protocol 계약 형태)
  </read_first>
  <behavior>
    - Test 1: 검출 휴리스틱 — 엉덩이 y < 어깨 y가 지속 구간(윈도우 비율 임계)인 keypoint 시퀀스 입력 → is_inverted True. 정립 시퀀스 → False
    - Test 2: 경계 — 짧은 순간 역전(점프 프레임)만으로는 False (지속성 요구 — 고속 스핀 오검출 방지)
    - Test 3: 조건부 게이트 — is_inverted False면 워프 미적용 경로(입력 그대로 통과) 보장
    - Test 4: 검출 함수는 순수(numpy만 — torch/GPU 의존 0)
  </behavior>
  <action>
    per D-22. (1) inversion_warp.py 신설 — 2부 구성: (a) detect_inversion(keypoints_seq) 순수 휴리스틱(numpy만 — 엉덩이 y < 어깨 y 지속 구간 비율 임계. 임계값은 spike 실측 기반 보수 설정, 상수 명시+근거 주석), (b) 워프 적용부는 pr_warp_pod.py의 Rodrigues→호모그래피 로직을 어댑터 관례(lazy import, GPU 경로)로 편입. recognizer/technique 분류를 2차 신호로 병용할지는 재량(휴리스틱 우선 — 분류 실패 시에도 동작). (2) pose_estimator.py — 추론 전처리에 조건부 훅: detect_inversion 참일 때만 프레임 워프 적용, 거짓이면 기존 경로 바이트 동일. env 스위치(PR_INVERSION_ENABLED, 기본 on — 스윕 실패 시 즉시 off 가능한 안전판). (3) 테스트는 검출·게이트의 순수 부분만 로컬(GPU 워프 실행은 Pod 스윕에서).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/phase32/test_inversion_warp.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - behavior 4건 green (로컬 — GPU 불요 부분)
    - pose_estimator.py 훅이 조건부(detect 거짓 = 기존 경로 무변경) + env 스위치 존재
    - detect_inversion에 torch import 0
    - 임계값 상수에 spike 근거 주석
  </acceptance_criteria>
  <done>인버전 한정 조건부 보정이 안전판과 함께 코드로 성립</done>
</task>

<task type="auto">
  <name>Task 2: Pod 배포 + fixture 6동작 최종 전수 스윕 (D-23 phase 게이트)</name>
  <files>(코드 수정 없음 — 스윕 실행·기록. 실패 시 조정 커밋 허용)</files>
  <read_first>
    - .planning/phases/32-result-readability-3-omni/32-RESEARCH.md §PR 인버전 보정 (게이트 기준 — invert 개선 + 비인버전 5동작 무회귀 양방)
    - .planning/phases/32-result-readability-3-omni/32-13-SUMMARY.md, 32-14-SUMMARY.md (직전 스윕 기준선 수치)
  </read_first>
  <action>
    per D-22/D-23. (1) Pod 배포(push→pull→재시작→/health 200). (2) fixture 6동작 전수 스윕 — 순차 강제(pipeline-not-concurrency-safe), kip-up 편중 금지: (a) invert 동작 — boneCV/좌표 품질 지표 개선 확인(spike 기준 −58% 방향), (b) 비인버전 5동작(특히 power-spin) — 점수·verdict·좌표 지표가 32-13/32-14 스윕 기준선 대비 무회귀, (c) 12관절 conf 분포(32-14 재료 누적) + 스팟체크 정상 동작 + 크롭 PNG 전수 육안(32-01 수리분 최종 확인). (3) 회귀 발견 시: PR_INVERSION_ENABLED off로 즉시 복구 → 원인 조정 → 재스윕(조정 커밋 기록). (4) 최종 결과 표(6동작 × 점수/verdict/좌표 지표/타이밍)를 SUMMARY에 작성.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/phase32 -q | tail -2 && curl -s -o /dev/null -w "%{http_code}" "$RUNPOD_ANALYZE_URL/health" | grep -q 200 && echo POD_OK</automated>
  </verify>
  <acceptance_criteria>
    - 6동작 스윕 표 완성 (invert 개선 수치 + 비인버전 5동작 무회귀 diff)
    - power-spin 점수·verdict 기준선 대비 변동 0 (또는 원인 규명·복구 기록)
    - 크롭 PNG 전수 육안 결과 기록
    - cold 재실행 결정론 확인 (동일 입력 동일 출력)
  </acceptance_criteria>
  <done>엔진 레버 3종이 반영된 파이프라인이 전수 스윕 양방 게이트를 통과</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: belle 최종 확인 — 엔진 웨이브 스윕 결과 + phase 32 마감</name>
  <what-built>엔진 레버 3종 전부: omni 스팟체크(불일치 카드 숨김), RTMW 12관절 표시 승격, PR 인버전 조건부 보정 — 전수 스윕 통과 상태</what-built>
  <how-to-verify>
    1. 스윕 결과 표 검토 — invert 개선 수치, 비인버전 무회귀(power-spin 포함), 스팟체크 숨김 빈도, 12관절 conf 분포
    2. 실기기에서 새 분석 1건(가능 시) — 스팟체크·12관절 오버레이·크롭 parity가 실사용에서 정상인지
    3. 2단(신규 관절 감점 편입) 판단 — conf 분포 재료 기준: 지금 게이트 열지 / 파일럿 데이터 더 본 뒤 판단할지 (후속 결정, 이 phase 범위 밖)
    4. 잔여 실기기 확인 항목은 32-HUMAN-UAT.md 적립 (batch-uat 관례)
  </how-to-verify>
  <action>최종 스윕 결과 표(invert 개선·비인버전 무회귀·스팟체크 빈도·conf 분포)를 belle에게 제시하고 실기기 확인(가능 시 새 분석 1건)을 진행한다. 2단 감점 편입 판단은 후속 결정으로 기록만 하고, 잔여 실기기 항목은 32-HUMAN-UAT.md에 적립한다.</action>
  <verify>
    <automated>cd backend && python -m pytest tests/phase32 -q | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - 스윕 결과 표가 SUMMARY에 존재 + belle 승인 기록
    - 2단(감점 편입) 판단 상태가 GATE-DECISIONS 또는 SUMMARY에 기록
  </acceptance_criteria>
  <done>엔진 웨이브가 belle 승인으로 마감되고 phase 32 실행 종료</done>
  <resume-signal>"승인" 시 phase 32 실행 완료 — /gsd-verify-work 진행. 이슈 발견 시 즉시 수리 (fix-now)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 전처리 워프 → 좌표 품질 | 잘못된 조건부 판정이 좌표를 훼손하면 채점까지 오염 |
| Pod 배포 | 프로덕션 추론 경로 변형 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-37 | Tampering(품질) | 오검출 워프 적용 | mitigate | 지속성 요구 휴리스틱(Test 2 — 고속 스핀 오검출 방지) + 비인버전 5동작 무회귀 스윕 양방 게이트 + env 안전판 즉시 off |
| T-32-38 | DoS | 스윕 중 프로덕션 오염 | mitigate | 순차 실행 강제(동시 호출 = 오염 — pipeline-not-concurrency-safe) + 스윕은 fixture 계정 한정 |
| T-32-39 | Repudiation | 게이트 통과 주장 | mitigate | 스윕 표·기준선 diff·결정론 재실행을 SUMMARY에 수치로 기록 — belle 확인 checkpoint로 종결 |
</threat_model>

<verification>
- cd backend && python -m pytest tests/phase32 -x -q green + 전체 baseline 초과 0
- 6동작 전수 스윕 양방 게이트 통과 (표 기록)
- belle 승인
</verification>

<success_criteria>
- D-22 레버 3종 전부 반영 + D-23 개발 게이트(전수 스윕) 통과
- phase 32 실행 종료 — 다음: /gsd-verify-work 32
</success_criteria>

<output>
Create `.planning/phases/32-result-readability-3-omni/32-15-SUMMARY.md` when done
</output>
