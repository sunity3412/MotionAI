---
phase: 32-result-readability-3-omni
plan: 16
type: execute
wave: 7
depends_on: ["32-08", "32-11"]
files_modified:
  - backend/template.yaml
  - backend/functions/pipeline/app.py
  - backend/functions/playback-url/app.py
  - backend/shared/python/sunity_shared/firestore_admin.py
  - backend/shared/python/sunity_shared/models.py
  - docs/contract.md
  - app/src/types/analysis.ts
  - app/src/lib/userAnalyses.ts
  - backend/tests/phase32/test_coach_audio.py
autonomous: false
requirements: [D-18, D-23, D-09]
must_haves:
  truths:
    - "B안(클라우드 TTS) 확정에 따라 백엔드가 분석 사후 스테이지에서 records 의 cueLine 문장을 AWS Polly(neural)로 합성해 S3 results/{uid}/{analysisId}/coach_audio_{recordId}.mp3 로 저장하고, doc 에 coachAudio(status/items) 를 부분 갱신한다 — 채점·verdict 무접촉(사후 스테이지)"
    - "오디오는 cueId(=recordId)로 mp3 와 조인된다 — 키에 recordId 를 사용해 32-12 audioCue prefetch(cueId별 presigned) 와 안정 조인 (리뷰 반영)"
    - "playback-url 이 오디오 asset 을 서버 구성 canonical key + 저장 key exact 비교(H-02)로만 발급한다 — prefix 부분일치·타 uid 접근 차단, 기존 asset(교정 이미지·회전 영상) 경로 바이트 불변"
    - "계약 3면(models COACH_AUDIO_KEYS + analysis.ts coachAudio? + contract.md) 동시 수정 + firestore_admin scoped validator + 앱 normalize 대칭 — 부재(legacy doc)=오디오 표면 미렌더 하위호환 (tier? 선례)"
    - "클라우드 TTS(사전 생성)는 belle 이 D-18 에서 명시 승인한 음성 합성 예외임을 계약 문서 각주로 1줄 기록 — CONTEXT '런타임 신규 생성 AI 0' 은 시각 생성(교정 이미지·회전) OFF 를 지칭 (리뷰 MEDIUM)"
    - "합성 실패는 graceful — status done 유지, 자막 경로 무영향, 분석 무훼손 (SP-3). 동기 경로(채점) timingsMs 회귀 0"
    - "최종 Polly 음성(voice)은 구현 후 한국어 음성 후보 샘플을 belle 청취로 확정한다 (belle 부속 결정 — 기본 후보 Seoyeon neural 잠정)"
    - "SAM 배포(template.yaml IAM + playback-url) + Pod 배포(pipeline 합성 스테이지) + fixture 6동작 전수 스윕(순차) + playback mp3 스모크 200 을 OTA/실기기(32-12 Task 4) 전에 완료 — D-23 배포 웨이브 게이트 (32-03 비준 매핑), 점수·verdict diff 0 실증"
  artifacts:
    - path: "backend/shared/python/sunity_shared/firestore_admin.py"
      provides: "update_analysis_coach_audio — result.coachAudio 부분 갱신 helper (fault_zoom 뼈대 복제)"
      exports: ["update_analysis_coach_audio"]
    - path: "backend/functions/playback-url/app.py"
      provides: "오디오 asset 종류 확장 (mp3 content-type + exact key 가드)"
  key_links:
    - from: "backend/functions/pipeline/app.py"
      to: "backend/shared/python/sunity_shared/firestore_admin.py"
      via: "사후 _stage(coach_audio) → update_analysis_coach_audio(items[{recordId,key}])"
      pattern: "update_analysis_coach_audio"
    - from: "app/src/lib/audioCue.ts (32-12 Task 1 소비처)"
      to: "backend/functions/playback-url/app.py"
      via: "prefetchCueAudio 가 cueId별 오디오 asset presigned URL 발급"
      pattern: "coach_audio"
---

<objective>
**B안 확정(클라우드 TTS) 백엔드 분리 플랜.** 32-08 샘플 게이트에서 belle 이 오디오 방식을 **B안(AWS Polly neural)** 으로 확정함에 따라(32-GATE-DECISIONS.md §샘플 게이트 결정), 32-12 Task 2(백엔드 TTS 스테이지+playback asset+SAM/Pod 배포+6동작 스윕)를 컨텍스트 예산 통제를 위해 이 플랜으로 **물리 분리**해 wave 7 에서 실행한다(체커 W-2 — 분리는 실행 단위 분할일 뿐, D-18 phase 내 완결 불변).

**분리 규약(파일 소유권 충돌 방지):**
- **32-12-PLAN.md 본문은 미수정.** 32-12 실행 executor 는 32-GATE-DECISIONS 를 읽고 Task 2 를 '32-16 에서 수행'으로 skip 한다.
- 계약 3면 중 앱 측(`app/src/types/analysis.ts` coachAudio? + `app/src/lib/userAnalyses.ts` normalize)은 **이 플랜(32-16)이 소유**한다. 32-12 는 그 계약을 소비만(audioCue.ts B-branch prefetch).
- **실행 순서:** 32-16 은 32-12 의 `audioCue.ts` prefetch(B-branch)가 소비하는 **playback-url 엔드포인트 + coachAudio 계약을 제공**하므로, **32-12 의 오디오 배선보다 먼저** 실행/배포되어야 한다(둘 다 wave 7 — 오케스트레이터 순서 통제).
- **해부학 검수 게이트와 무관** — 이 플랜은 백엔드 오디오 전용. 일러스트 해부학 검수는 32-12 일러스트 태스크 소관.

Purpose: B안 오디오가 SAM/Pod 배포·6동작 스윕·스모크까지 이번 phase 안에서 완결 출시되도록 백엔드+계약을 분리 실행 (D-18 동시 출시 + D-23 배포 게이트).
Output: Polly 합성 사후 스테이지 + coachAudio 계약 3면 + playback asset 확장 + belle 음성 확정 + 배포·스윕.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-result-readability-3-omni/32-CONTEXT.md
@.planning/phases/32-result-readability-3-omni/32-GATE-DECISIONS.md
@.planning/phases/32-result-readability-3-omni/32-PATTERNS.md
@.planning/phases/32-result-readability-3-omni/32-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polly IAM + 사후 합성 스테이지 + coachAudio 계약 3면 + normalize (D-18)</name>
  <files>backend/template.yaml, backend/functions/pipeline/app.py, backend/shared/python/sunity_shared/firestore_admin.py, backend/shared/python/sunity_shared/models.py, docs/contract.md, app/src/types/analysis.ts, app/src/lib/userAnalyses.ts</files>
  <read_first>
    - .planning/phases/32-result-readability-3-omni/32-GATE-DECISIONS.md §샘플 게이트 결정 (B안 확정 + Seoyeon neural 잠정 + 음성 belle 확정 부속)
    - .planning/phases/32-result-readability-3-omni/32-RESEARCH.md §재생 중 큐 + TTS (B안: Polly 사전 생성 + playback-url asset 확장 패턴 — 31 correctedPose/rotation 선례, H-02)
    - .planning/phases/32-result-readability-3-omni/32-PATTERNS.md §E-3 (오디오 asset 확장 — canonical key·_ASSET_CONTENT_TYPE mp3) + §SP-1/SP-3
    - backend/functions/pipeline/app.py (사후 스테이지 자리 — fault_zoom 패턴, 32-09 최신 상태. status done 확정 후 _stage 블록)
    - backend/shared/python/sunity_shared/firestore_admin.py (update_analysis_fault_zoom — 사후 부분 업데이트 helper 뼈대 복제 원본)
    - backend/shared/python/sunity_shared/models.py (계약 상수 패턴 — *_KEYS 선례) + app/src/types/analysis.ts (DeductionRecord.recordId 형식 — coach_audio 키 조인 근거) + app/src/lib/userAnalyses.ts (normalizeResultKey — key results/ prefix 강등 재사용)
  </read_first>
  <action>
    per D-18 동시 출시 + B안. (1) **IaC:** template.yaml 의 pipeline 함수 IAM 정책에 `polly:SynthesizeSpeech` 1줄 추가 — 신규 시크릿 0(boto3 `client("polly")`, ap-northeast-2). 음성은 상수(예 `_POLLY_VOICE_ID = "Seoyeon"`, `_POLLY_ENGINE = "neural"`)로 두고 **Task 4 belle 확정 후 최종값 고정**(잠정 표시 주석). (2) **pipeline 사후 스테이지:** status done 확정 **후** `with _stage(timings_ms, analysis_id, "coach_audio")` 블록에서 records 의 cueLine 문장들을 Polly 합성 → S3 `results/{uid}/{analysisId}/coach_audio_{recordId}.mp3` 저장(**키에 recordId** — cueId=recordId 조인) → 신규 helper `update_analysis_coach_audio`(fault_zoom 뼈대 복제 — `result.coachAudio` 단일 field-path)로 `{status: 'done'|'failed', items: list[{recordId, key}]}`(scalar dict 배열 — safetyFlags 선례, nested-array 금지) 부분 갱신. 합성 실패 = **graceful**(log.exception 후 통과 — 자막만 제공, status done 유지, 분석 무훼손 SP-3). **동기 채점 경로에서 호출 금지**(속도 예산 — 사후만). (3) **계약 3면 동시 수정:** models.py `COACH_AUDIO_KEYS`(+ item 키 검증) + firestore_admin scoped validator(_validate_dict_only_scalars 경로 — items 는 scalar dict 배열) + analysis.ts `coachAudio?: { status: 'done'|'failed'; items: { recordId: string; key: string }[] }` (부재=legacy 하위호환, tier? 서술) + contract.md 절 신설. **★계약 문서 각주(리뷰 MEDIUM):** "클라우드 TTS 는 분석 시점 사전 생성으로, CONTEXT '런타임 신규 생성 AI 0' 은 시각 생성(교정 이미지·회전 영상) OFF 를 지칭 — belle 이 D-18 에서 명시 승인한 음성 합성 예외" 1줄 기록. (4) **normalize:** userAnalyses.ts 가 coachAudio.items[].key 를 results/ prefix 로 강등(normalizeResultKey 재사용), 형상 malformed 는 방어적으로 드롭.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/phase32 -k coach_audio -q; cd ../app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - template.yaml 에 polly:SynthesizeSpeech 존재 (신규 pip/시크릿 0)
    - pipeline 사후 스테이지가 coach_audio_{recordId}.mp3 저장 + update_analysis_coach_audio 호출 (동기 채점 경로 무접촉)
    - 계약 3면(COACH_AUDIO_KEYS + coachAudio? + contract.md 절) 동시 수정 + 런타임 생성 예외 각주 포함
    - firestore_admin scoped validator + userAnalyses normalize 대칭 (부재=하위호환)
    - typecheck clean
  </acceptance_criteria>
  <done>Polly 합성 사후 스테이지가 recordId 조인 키로 coachAudio 를 부분 갱신하고 계약 3면이 정합</done>
</task>

<task type="auto">
  <name>Task 2: playback-url 오디오 asset 확장 (mp3 content-type + exact key 가드, H-02)</name>
  <files>backend/functions/playback-url/app.py, backend/tests/phase32/test_coach_audio.py</files>
  <read_first>
    - backend/functions/playback-url/app.py:77-124 (_handle_asset H-02 정본 — 서버가 canonical key 구성 + 저장 key exact 비교. 오디오 asset 이 이 패턴 복제)
    - .planning/phases/32-result-readability-3-omni/32-PATTERNS.md §E-3 (_ASSET_CONTENT_TYPE 에 mp3 추가 지침)
  </read_first>
  <action>
    per H-02. playback-url `_handle_asset` 에 오디오 asset 종류 확장 — 서버가 `results/{uid}/{analysisId}/coach_audio_{recordId}.mp3` canonical key 를 **서버 측에서 구성**하고 저장 items 의 key 와 **exact 비교**(prefix 부분일치 불가 — 타 객체 열람 차단) 후에만 presigned 발급. `_ASSET_CONTENT_TYPE` 에 `"mp3": "audio/mpeg"` 추가. 기존 asset 경로(교정 이미지·회전 영상) 바이트 불변. 요청 파라미터로 recordId(=cueId)를 받아 key 를 결정 — 클라이언트가 임의 key 를 넘기지 못한다(H-02 핵심).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/phase32 -k "coach_audio or asset" -q</automated>
  </verify>
  <acceptance_criteria>
    - _handle_asset 에 mp3 content-type + 서버 구성 canonical key + exact 비교 가드
    - 기존 asset 종류 diff 0 (바이트 불변)
    - stale key·타 uid recordId → 404 (asset guard 테스트 green)
  </acceptance_criteria>
  <done>오디오 재생 URL 이 H-02 exact 비교로만 발급되고 기존 asset 경로 무회귀</done>
</task>

<task type="auto">
  <name>Task 3: 합성 graceful + asset guard 테스트</name>
  <files>backend/tests/phase32/test_coach_audio.py</files>
  <read_first>
    - backend/tests/phase32/ (기존 phase32 테스트 스타일 — pytest, 사후 스테이지·validator 검증 관례)
    - backend/functions/pipeline/app.py (Task 1 합성 스테이지 — graceful 분기 확인)
  </read_first>
  <action>
    backend/tests/phase32/test_coach_audio.py 로: (1) 합성 스테이지 **graceful** — Polly 무자격/합성 실패 mock 시 status done 유지 + 자막 경로(records cueLine) 무영향 + coachAudio.status='failed' 방출. (2) **asset guard** — stale key·타 uid recordId 요청이 404(H-02 exact 비교). (3) coachAudio items 형상 validator 통과(scalar dict 배열) + normalize 대칭. 채점 무접촉(사후) 회귀 가드 1건.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/phase32 -x -q</automated>
  </verify>
  <acceptance_criteria>
    - graceful 테스트 green (실패 시 done 유지)
    - asset guard 테스트 green (stale/타 uid 404)
    - phase32 전체 pytest green
  </acceptance_criteria>
  <done>합성 실패·asset 접근 경계가 테스트로 고정됨</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 4: belle Polly 음성 확정 (belle 부속 결정 — 구현 후 한국어 음성 샘플 청취)</name>
  <decision>재생 중 큐 오디오의 최종 Polly 한국어 음성(voice/engine)</decision>
  <context>
    belle 부속 결정(32-GATE-DECISIONS §샘플 게이트): "최종 음성 선택은 구현 후 Polly 한국어 음성 샘플을 belle 에게 들려주고 확정". Task 1 구현은 잠정 Seoyeon neural 로 두었고, 이 게이트에서 belle 청취로 최종 고정한다. 후보 = ap-northeast-2 실계정 확인 음성: Seoyeon(neural), Jihye(neural), (가용 시 generative 엔진). 같은 코칭 문장(32-08 문구집 leg_extension cueLine+whyLine)으로 샘플 mp3 를 생성해 belle iPhone/Mac 청취 → 확정.
  </context>
  <options>
    <option id="seoyeon-neural">
      <name>Seoyeon (neural)</name>
      <pros>ap-northeast-2 검증·안정, 32-08 B안 샘플과 동일 음성 (belle 이미 1차 청취)</pros>
      <cons>—</cons>
    </option>
    <option id="jihye-neural">
      <name>Jihye (neural)</name>
      <pros>다른 톤 후보 (belle 취향 비교)</pros>
      <cons>미청취 — 이 게이트에서 확인</cons>
    </option>
    <option id="generative">
      <name>generative 엔진 (가용 시)</name>
      <pros>더 자연스러운 억양 가능</pros>
      <cons>비용·지연 상향, 가용성 확인 필요</cons>
    </option>
  </options>
  <action>같은 코칭 문장으로 후보 음성 mp3 샘플(2~3종)을 `aws polly synthesize-speech --profile sunity-motion --region ap-northeast-2` 로 생성해 `.planning/phases/32-result-readability-3-omni/samples/voice/` 에 저장·커밋하고, belle 에게 청취 안내(파일 경로·재생법). 확정된 voice/engine 을 `pipeline/app.py` 의 `_POLLY_VOICE_ID`/`_POLLY_ENGINE` 상수에 고정하고 32-GATE-DECISIONS 에 1줄 기록. (배경 executor 는 대화형 불가 — 샘플 커밋 후 checkpoint report 반환, 오케스트레이터 relay.)</action>
  <verify>
    <automated>ls .planning/phases/32-result-readability-3-omni/samples/voice/ | grep -c "mp3"</automated>
  </verify>
  <acceptance_criteria>
    - 후보 음성 샘플 mp3 ≥ 2 커밋 (동일 문장)
    - belle 확정 voice/engine 이 pipeline 상수에 고정 + GATE-DECISIONS 1줄 기록
  </acceptance_criteria>
  <done>최종 Polly 음성이 belle 청취로 확정되어 코드 상수에 고정됨</done>
  <resume-signal>음성(Seoyeon/Jihye/generative)을 답하면 Claude 가 pipeline 상수 고정 + GATE-DECISIONS 기록 후 배포(Task 5) 진행</resume-signal>
</task>

<task type="auto">
  <name>Task 5: SAM/Pod 배포 + fixture 6동작 전수 스윕 + playback mp3 스모크 (D-23 배포 게이트)</name>
  <files>backend/template.yaml, backend/functions/pipeline/app.py</files>
  <read_first>
    - backend/samconfig.toml (SAM deploy 기본값 — 스택 sunity-motion-pilot, region ap-northeast-2)
    - .planning/phases/32-result-readability-3-omni/32-GATE-DECISIONS.md §D-23 스윕 매핑 (배포 웨이브 = 6 fixture 전수 재분석, 순차 필수)
    - .planning/phases/32-result-readability-3-omni/32-03-SWEEP.md (스윕 실적 포맷 — SERIAL, 점수 diff 대조 기준선)
  </read_first>
  <action>
    per D-23 배포 게이트(리뷰 blocker 2/3 + W-1 — 32-12 OTA/실기기 전 필수). (1) `sam build --use-container && sam deploy`(template.yaml IAM + playback-url 변경은 Lambda 표면 — Pod git pull 로는 배포 안 됨). (2) Pod git pull·재시작(pipeline 합성 스테이지) + /health 200. (3) **fixture 6동작 전수 스윕(순차 — kip-up 편중 금지 [[verify-all-fixtures-not-kipup-only]], [[pipeline-not-concurrency-safe-eval-serial]]):** 각 doc 의 coachAudio items(recordId 조인) 방출·validator 통과 + 대표 키 playback-url mp3 presigned **200 스모크** + **점수·verdict 가 직전 기준선(32-09 또는 최신 스윕) 대비 diff 0**(합성 스테이지는 사후 — 채점 무접촉 실증) + 동기 경로 timingsMs 회귀 0. (4) 스윕 표를 `32-16-SWEEP.md` + SUMMARY 에 기록. **belle Pod 승인 필요 시 [[pod-request-include-gpu-model]] 관례로 요청** — 기존 프로덕션 Pod 재사용이면 승인 불요(git pull·재시작만).
  </action>
  <verify>
    <automated>cd backend && sam build --use-container 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - SAM deploy 성공(polly IAM + playback-url 반영) + Pod 배포 + /health 200
    - 6동작 전수 스윕 표: 전 doc coachAudio 방출 + 점수·verdict diff 0 + 동기 경로 회귀 0
    - 대표 키 playback-url mp3 presigned 200 스모크
    - 스윕 표 32-16-SWEEP.md 기록 (OTA/실기기 전 완료 — W-1)
  </acceptance_criteria>
  <done>B안 오디오가 SAM/Pod 배포·6동작 스윕·스모크까지 완결 — D-18 동시 출시 + D-23 배포 게이트 충족, 32-12 오디오 배선의 백엔드 선행 완료</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline → AWS Polly | 사후 합성 (분석당 N 문장, boto3 IAM 1줄) |
| playback-url → 사용자 기기 | 오디오 asset presigned URL 발급 (H-02 경계) |
| SAM/Pod 배포 → 프로덕션 | Lambda 표면 + 파이프라인 스테이지 변경 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-29 | Info Disclosure | TTS 발화 내용 | mitigate | 합성 텍스트 = 문구집 큐 문장만(PII 0). 재생은 서버 구성 canonical key + exact 비교 presigned (H-02) |
| T-32-30 | Tampering(경계) | playback-url asset key | mitigate | 클라이언트가 recordId 만 지정, key 는 서버 구성 + 저장 items exact 비교 — 임의 key·타 uid 열람 차단 |
| T-32-31 | DoS | 합성 스테이지 실패 | mitigate | graceful(사후, done 유지) + 동기 채점 경로 무접촉(timingsMs 회귀 0) + 6동작 스윕이 배포 전 회귀 검출 |
| T-32-SC | Tampering | 패키지 설치 | n/a | 이 플랜 신규 pip/npm 설치 0 (Polly=boto3 Lambda 제공, expo-audio 설치는 32-12 소관) |
</threat_model>

<verification>
- cd backend && python -m pytest tests/phase32 green + cd app && npm run typecheck clean
- 계약 3면 정합 + firestore validator/normalize 대칭 + 런타임 생성 예외 각주
- SAM/Pod 배포 + 6동작 전수 스윕(coachAudio 방출 + 점수 diff 0 + 회귀 0) + playback mp3 스모크 200 → 32-12 OTA/실기기 전 완료
</verification>

<success_criteria>
- B안 오디오 백엔드가 계약 3면·H-02 asset·graceful·6동작 스윕까지 완결 출시 (D-18 동시 출시 + D-23 배포 게이트, 이연 없음)
- belle 최종 Polly 음성 확정이 코드 상수에 고정됨
- 32-12 오디오 배선(audioCue.ts B-branch)의 백엔드 선행(playback-url + coachAudio 계약) 제공 완료
</success_criteria>

<output>
Create `.planning/phases/32-result-readability-3-omni/32-16-SUMMARY.md` when done
</output>
