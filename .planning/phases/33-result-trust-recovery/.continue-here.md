# Continue Here — Phase 33 §9 수리 사이클: **앱 수리 + D-1 완료**, 다음 = §C-4 Pod (갱신 2026-07-31)

## 한 줄 상태

belle 확인 ② 반려(12건) → 수리 사이클. **§C-1 백엔드 + §C-2 앱 4단위 + D-1 전부 완료.**
남은 것 = **§C-4 Pod 전수 재생성 → 일괄 OTA → belle 확인 ③**.
스펙 = 승인 목업 7R(`mockups/index.html`) + EVIDENCE §9. 정본 대조표 = **`33-G-MOCKUP-DIFF.md`**.
결정 = `33-CONTEXT.md` D-39~D-45.

## 다음에 할 것 (순서)

| # | 항목 | 크기 | 비고 |
|---|---|---|---|
| 1 | **§C-4 Pod** — 아래 §C-4 상세 | 큼 | GPU 필요 |
| 2 | **일괄 OTA → belle 확인 ③ 1회** (D-45) | — | 그전에 배포 금지 |

~~D-1 다리 사이각~~ = **완료** (quick-260731-f5h, main `f05bc98d`). deferred 2안 —
`_leg_line_pts(in_crop=)` 술어로 다리 끝을 `ankle→knee` 순회하며 conf 게이트 AND crop 포함인
것만 채택(측별 독립). crop 배율 무변경 → 32-03 parity 무이동. 등재 10동작 스위프 **0/10 → 10/10**,
ankle 이 crop 안인 형상은 PNG byte-불변, 변경 카드 = `split_angle` 뿐, legacy 9케이스 해시 불변,
pytest node ID diff 0. **단 기준 좌표가 합성** — 실 12관절 doc 판정은 아래 §C-4 5번에서.

## §C-4 상세 (Pod 필요)

1. **기준 12관절 표시 보고서** — 무릎·팔꿈치 각도를 켜는 유일한 길. **점수 무접촉 경로 확립됨**:
   채점은 `angles`만 읽고 표시용 `referenceKeypointReport`는 **항상 최상위 필드**에서 읽힌다
   (`firestore_admin._REFERENCE_CONSUMER_FIELDS`에 미포함). 최상위 그 필드만 **18fps**로 갈아
   끼우면 `angles`·`activeVersion`·릴리스 포인터 무접촉 = **33-07 flip 보류와 독립**.
   - **18.0 fps 필수** (9fps 채택 시 뒤쪽 절반이 한 프레임으로 붕괴 — 28-RESEARCH D2 기왕 이력).
   - go/no-go **완료 = GO**: candidate `phase33-cm3-run1`에 11/11 12관절 보고서 존재, ankle/elbow
     게이트 통과율 0.36~0.97 (약함 = pdshape·elbow-twist-sister). 증거 = `evidence-refkp/`.
   - 작업 = 신규 ~50줄 추출 스크립트(`extract_reference_keypoint_reports.py` 선례) + 무접촉
     증명 ~15줄(`_release_doc_hash`로 채점 8필드 불변) + **flip 방어 1줄**(아래).
2. **crop·각도 베이크 전수 재생성** (§C-1·§C-2 산출 반영).
3. **어깨·팔꿈치 일러스트 신규 생성** (belle 07-31 지시). 현재 에셋 6장 전부 `leg` 단독이라
   어깨·팔 항목은 전 동작 미부착. **에셋+메타만 채우면 코드 변경 없이 부착 복귀**(판정 코드 완비).
   ⚠ 구도도 같이 잡을 것 — 현 에셋은 82~89%가 빈 배경이라 belle #11 "빈 프레임"으로 읽힌다.
   토큰은 **에셋 실물 열람으로만** 부여(P-4). 상세 = quick-260731-2jt `deferred-items.md` D-2.
4. 9모션 앵커 주석은 위 1번이 들어오면 **대부분 불필요**(팔꿈치 좌표가 실재하게 됨).
5. **S10 12관절 doc 실측 판정** — 코드 수리는 끝났고(quick-260731-f5h) 남은 것은 **실 doc 위에서**
   다리 사이각이 그려지는지 확인하는 것뿐. 재스위프 산출 PNG 에서 `split_angle` 카드를 직접 열어
   골반→양다리 선 2개 + 호를 볼 것. 합성 스위프에서 못 본 것 = 계약 4·5·6행(ankle·knee 둘 다 crop
   밖 / 골반 crop 밖 / 좌우 비대칭)과 **선의 해부학적 정합**(합성 좌표라 인물과 정렬 안 됨).
   판정 재료 = `.planning/quick/260731-f5h-.../` (하네스 `sweep_leg_angle.py` 재사용 가능).
   · 보류 2건(S5·S22) 판정.

## ⚠ 33-07 flip 잠복 버그 (선행 방어)

`reprocess_reference_motions_phase4._flip_active_pointer`의 `mirror_fields`에
`referenceKeypointReport`가 **빠져 있다**. 지금 flip 하면 `angles`는 9fps로 가는데 표시 보고서는
최상위 낡은 18fps가 남아 기준 패널이 **의도 시점의 약 절반**을 보여준다. 한 줄 추가로 방어.
flip 여부와 무관하게 선행.

**flip 자체는 Phase 34에서 판단** — "기준을 바꿔도 같은 자세면 같은 점수"를 검사할 수 있게 된 뒤.
지금은 그것만의 효과를 분리한 자료가 없다.

## 미검증 7건 (조용히 PASS 처리 금지)

| 항목 | 왜 못 봤나 | 어디서 볼 것 |
|---|---|---|
| S19 pulse·선/원 · S2 참고 점선 · S23 illu-float | **시뮬이 재생 중 화면을 캡처에 못 담음** (20프레임 바이트 동일) | belle 실기기 확인 ③ |
| F-3 참고코너 페어 · S6 paircap 초 | 렌더 doc 4건이 §C-1 이전 산출이라 `userVideoSec`/`refVideoSec` 필드 부재 | §C-4 재산출 doc |
| F-5 게이지 라벨 · F-7 전환 · S12 시트 용어줄 | 화면까지 도달 못 함(정지 화면이라 **다음 세션에서 확인 가능**) | 시뮬 |
| S7 facing · S1 병합 배지 | 해당 doc/위치 도달 불가 | §C-4 재산출 doc |

**F-6(실기기 음성 무음) = FAIL 유지, 원인 미상.** 세션 경합을 파일:줄로 실증했으나 같은 증거가
반증도 했다(양쪽 카테고리 `.playback` 수렴 → 무음 스위치 축 설명 불가). 추측 수리 금지.
belle 실기기 분기 절차 = quick-260731-cum SUMMARY (첫 분기 = "큐 시점에 영상이 멈추는가").

## 시뮬 검증 요령 (다음 세션 시간 절약)

- **LogBox 배너가 하단 버튼 탭을 가로챈다.** 우측 X 먼저 눌러 치울 것. (배너 정체 =
  `expo-video allowsFullscreen` deprecation 2건, 기존·무관.)
- **AX 프레임의 y가 화면(874) 밖이면 그 탭은 엉뚱한 데 간다.** 스크롤로 화면 안에 넣고 탭할 것.
  이걸 몰라서 "칩→엉뚱한 시트", "감점 병합 결함" 오판을 각각 한 번씩 했다.
- 앱 uid = `fvcNXzEqKjgqVxRPVSj1iwFnIpn2`, 렌더 doc 4건 = 파워스핀 80(팔·어깨·다리 각 1감점,
  확대카드 5) / 킵업 79(팔·어깨) / pdshape 100(감점 0 = 헤드라인 확인용) / 엘보 60(8감점, 카드 0).
- Metro는 8081에 detached로 떠 있다.

## Critical Anti-Patterns

| Anti-pattern | severity | 설명 |
|---|---|---|
| single-motion-fixation | blocking | 규칙은 동작별 데이터(record·criterion·jointKeys·technique)로 키잉, 동작명 분기 0, 등재 10동작 확인. memory: fix-generalize-beyond-discussed-motion |
| code-only-verification | blocking | 코드 통과 ≠ 완료. 33-G 항목별 재채점(승인 목업 대조)까지가 검증(D-40). 산출물을 직접 열 것. memory: verify-against-approved-mockup-not-just-code |
| repair-rediscussion | blocking | 수리 사이클에서 **취향·설계**를 재논의하지 말 것(D-39). 단 **데이터가 결정과 안 맞으면 그건 재논의가 아니라 보고** — 반드시 올린다. memory: repair-cycle-no-rediscussion · evidence-outranks-prior-decisions |
| over-generalize-breaks-approved | blocking | 한 승인 자산의 규칙을 다른 종류로 확대해 이미 PASS인 항목을 깨지 말 것(§C-1 L-10 실발생) |
| 근거 없는 의심 | blocking | 반박할 **측정치 없이** 검증된 작업을 흔들지 말 것. 거짓 안심과 거짓 불안은 같은 실패. 07-31 실발생. memory: evidence-outranks-prior-decisions |

## 완료분 요약 (재조사 금지)

- **§C-1 백엔드** (quick-260730-l7t): S9 PASS · S8 부분 · F-3 백엔드 PASS · S10 PASS→PARTIAL.
  근본원인 2건 확정 — belle #7·#9 "팔꿈치인데 손" = `_KISMAM_TO_KEYPOINT` elbow→hand 인접 대입 /
  F-3 = 앱이 `refIdx / rep.fps`로 초 추정(rep 18fps ↔ video 9fps 불일치).
- **§C-2 1단위** (260730-py1): S6 부분 PASS(부위 단위 시트, 한 부위 2감점 = 시트 1개·블록 2개 실증) ·
  S7 PARTIAL(번호 헤더·basis·method·numnote. **proof 3컷·basis 구간 축은 의도적 fail-closed** —
  백엔드가 카드당 1장만 주고 3컷 분류는 doc에 없는 판단이라 만들면 날조) · F-3 앱 코드 PASS.
- **§C-2 2단위** (260730-szk): F-8 PASS(상시 마커 제거, 안 보이는 탭 0) · S1 부분 PASS(그룹 경계+
  번호 배지, 개별 관절 원 0) · S3 PASS(부위 칩 = 감점 부위 수 일치).
- **§C-2 3단위** (260731-2jt): S13/S25 PASS(어깨 미부착·다리 부착 양방향) · S26 = 에셋 구도 귀결 ·
  P-14 illu-float 기준면 교정(row→패널).
- **§C-2 4단위** (260731-cum): F-4 PASS(헤드라인, **근본원인이 백엔드 phrasebook 조립**) ·
  S12 코드 게이트 PASS(3곳이 아니라 7파일 16곳 + 백엔드 게이트 스코프 확장).

## 재개 방법

`/clear` 후 이렇게 주면 됩니다:

```
33 수리 이어서. .planning/phases/33-result-trust-recovery/.continue-here.md 읽고
D-1(다리 사이각)부터. 재논의 없음, 승인 목업이 스펙.
```

Pod가 필요한 §C-4로 바로 가려면 `D-1 건너뛰고 §C-4부터` 라고 하면 됩니다.
