# Continue Here — Phase 33 §9 수리 사이클: **§C-4 A-트랙까지 완료**, 다음 = 일러스트 → OTA (갱신 2026-07-31)

## 한 줄 상태

belle 확인 ② 반려(12건) → 수리 사이클. **§C-1 백엔드 + §C-2 앱 4단위 + D-1 + §C-4 A-트랙 완료.**
남은 것 = **§C-4 3번(어깨·팔꿈치 일러스트) → 일괄 OTA → belle 확인 ③**.
스펙 = 승인 목업 7R(`mockups/index.html`) + EVIDENCE §9. 정본 대조표 = **`33-G-MOCKUP-DIFF.md`**.
결정 = `33-CONTEXT.md` D-39~D-45.

## 다음에 할 것 (순서)

| # | 항목 | 크기 | 비고 |
|---|---|---|---|
| 1 | **belle 확인 ③** — OTA 발행 완료, 실기기 확인 대기 | — | 아래 확인 목록 |

## OTA 발행 완료 (2026-07-31, 커밋 `092c312`)

runtime **1.1.0** (앱 버전과 동일 — 재빌드 불필요). 신규 에셋 3장(어깨·팔 일러스트) 포함.
**앱 완전 종료 2회째 적용** ([[verify-ui-on-simulator-before-ota]]).

| 채널 | update group |
|---|---|
| production | `d1b97758-6007-44e9-9f1d-fc226637d588` |
| preview | `385bfa4d-a523-4fc7-b4c3-e3c6517d0c18` |
| development | `6d95fa4f-23ed-43bb-827c-9957a015f735` |

**롤백** (1분) — `npx eas update:republish --group <직전 group>`
· production 직전 = `ad955f54-a971-4d50-bb04-f72328d2d41c` (33-16)
· preview 직전 = `666ca0e0-59d7-42c9-a691-ece8f3171730` (32-13)
· development 직전 = `b23e9a68-3529-4cad-8343-c142e0ad2071` (IN-01)

## 일러스트 슬롯 건 — 종결 (오측이었음)

belle 확인 ② #11 인접으로 올렸던 "슬롯이 에셋을 잘라낸다"는 **오케스트레이터 오측**이었다.
카드는 `bff3a477` 이후 이미 **362x484.7pt** 로 에셋 전체를 담고 있었고, 내가 잰 260 은
시트 ScrollView 뷰포트(512)에 **보이던 조각**이었다(최하단 스크롤 상태).
**자체 반증** — 그때 보인 것은 그림의 아래쪽(정강이·폴 받침)이었다. 카드가 260 이고 위에서
잘렸다면 위쪽(골반·허벅지)이 보였어야 한다. 방향이 반대였다.
debug 세션 = `.planning/debug/illustration-slot-crop.md` (계기 2종 실측 + 계기 제거 후 재산출).
곁가지 확인 — RN 0.81.5 + New Architecture 의 `aspectRatio` 는 문서대로 width/height.

**다만 belle 지시로 적응형은 넣었다** (`0b865d39`). 카드 높이 상한 = 시트가 실측한 스크롤
뷰포트 x 0.5. 넘으면 비율 유지한 채 폭을 줄여 가운데 정렬. 이유 = 카드가 뷰포트에 들어가도
**뒤 요소(bullets·강사 연결·AI 고지) 때문에 자연스러운 위치에서 절반만 보였다**. 이제 두 시트
모두 일러스트 전신 + 본문 + CTA 가 한 화면에 들어온다(시뮬 화면 확인). 요소 삭제·순서 변경 0.
⚠ **SE 급은 시뮬로 안 재봤다** — 산수상 해소되지만 미확인.

## ★ §C-4 A-트랙 완료 (quick-260731-iis, main `0485a2be`)

기준 11동작 top-level `referenceKeypointReport` 를 **18.0fps 12관절**로 교체했다.
오케스트레이터가 Firestore 를 직접 조회해 재확인한 값:

- **11/11** `fps==18.0` · `joints==12` · **`frames == anglesFrames`** · `activeVersion` = `phase4_v1` 불변 ·
  `reference/_release` 문서 여전히 **부재**(릴리스 포인터 미설정 = 무접촉 전제 성립).
- 델타는 fps 가 아니라 **관절 8 → 12** 였다(교체 전에도 top-level 은 이미 18fps).
- **S8 켜짐** — `omitted:ref_gate` **39 → 0**, `drawn` 21 → 79(팔꿈치 0→20·무릎 0→20·어깨 1→19).
  원인 = `ANGLE_BAKE_MAP` 이 요구하는 elbow·ankle 이 8관절 기준에 아예 없어 fail-closed 였던 것.
  → **§C-4 4번(9모션 앵커 주석) 잔여 0** (주석 없이 성립).
- **S10 실 doc PASS** — kip-up `zoom_split_angle` PNG 를 직접 열어 **선이 실제 두 다리와 정합**함을 확인
  (f5h 합성 좌표에서 못 봤던 해부학적 정합).
- flip 방어 = `_flip_active_pointer.mirror_fields` 에 `referenceKeypointReport` 추가. **1줄이 아니었다** —
  `_reprocess_one` payload 에 그 키가 없어(REQUIRED_KEYS 밖) candidate 문서 폴백 + fail-closed 경고까지
  넣어야 실제로 실린다. **flip 실행은 여전히 Phase 34 몫.**
- 롤백 경로 실증 = `refkp_backup.json` + ref-kip-up `--restore`→`--write` 왕복(채점 해시 3상태 동일).
- 회귀 = pytest FAILED/ERROR node ID 집합 **diff 0**(58 pre-existing).

~~D-1 다리 사이각~~ = **완료** (quick-260731-f5h, main `f05bc98d`). deferred 2안 —
`_leg_line_pts(in_crop=)` 술어로 다리 끝을 `ankle→knee` 순회하며 conf 게이트 AND crop 포함인
것만 채택(측별 독립). crop 배율 무변경 → 32-03 parity 무이동. 등재 10동작 스위프 **0/10 → 10/10**,
ankle 이 crop 안인 형상은 PNG byte-불변, 변경 카드 = `split_angle` 뿐, legacy 9케이스 해시 불변,
pytest node ID diff 0. **단 기준 좌표가 합성** — 실 12관절 doc 판정은 아래 §C-4 5번에서.

## §C-4 상세

~~1. 기준 12관절 표시 보고서~~ · ~~2. crop·각도 베이크 전수 재생성~~ · ~~4. 앵커 주석~~ ·
~~5. S10 실 doc 판정~~ = **A-트랙으로 완료** (위 절 참조).

3. **어깨·팔꿈치 일러스트 신규 생성** (belle 07-31 지시) — **남은 유일한 §C-4 항목. Pod 불필요.**
   현재 에셋 6장 전부 `leg` 단독이라
   어깨·팔 항목은 전 동작 미부착. **에셋+메타만 채우면 코드 변경 없이 부착 복귀**(판정 코드 완비).
   ⚠ 구도도 같이 잡을 것 — 현 에셋은 82~89%가 빈 배경이라 belle #11 "빈 프레임"으로 읽힌다.
   토큰은 **에셋 실물 열람으로만** 부여(P-4). 상세 = quick-260731-2jt `deferred-items.md` D-2.

## ⚠ Phase 34 로 넘기는 것 (A-트랙이 새로 만든 관측)

**33-07 flip 방어는 들어갔다**(`_flip_active_pointer` 에 `referenceKeypointReport` + candidate 폴백).
**flip 실행 자체는 여전히 Phase 34** — "기준을 바꿔도 같은 자세면 같은 점수"를 검사할 수 있게 된 뒤.

★ **재산출로 점수가 움직였다 (Phase 34 의 정확한 관측 대상).** 같은 영상·같은 채점 코드인데
`power-spin 80 → 60`(−20) · `elbow-twist 60 → 63`(카드 0→5) · kip-up 79→79 · pdshape 100→100.
기준 표시 보고서는 채점 무접촉이 **증명**됐으므로(GATE-B 11/11) 이동분은 표시 교체 탓이 아니다.
후보 = 학생 RTMW 재추론 편차(**Pod `/health` `RTMW_DETERMINISTIC: false`**) · Gemini 재실행 편차 ·
직전 doc 산출 이후 누적된 코드 변경. **원인 분리 안 했다** — 그것이 Phase 34 4겹 중 "불변식 스위트".

★ **스위프 parity 게이트 24건 발화** — 임계 조정 안 했다. `user_side_px` 는 f5h 와 완전 동일하고
`ref_side_px` 만 230(합성 고정) → 151~253(실 해부)으로 이동, 151 은 `floor_side` 클램프.
합성 학생 × 실 기준이라는 **프로덕션에 없는 조합**의 산물로 보이나, **실 doc 에서도 배율 상이 카드를
1건 봤다**(elbow-twist 팔꿈치) → 안심으로 닫지 말고 **실 doc parity 전수 확인**이 잔여.

## 미검증 7건 (조용히 PASS 처리 금지)

| 항목 | 왜 못 봤나 | 어디서 볼 것 |
|---|---|---|
| S19 pulse·선/원 · S2 참고 점선 · S23 illu-float | **시뮬이 재생 중 화면을 캡처에 못 담음** (20프레임 바이트 동일) | belle 실기기 확인 ③ |
| F-3 참고코너 페어 · S6 paircap 초 | 렌더 doc 4건이 §C-1 이전 산출이라 `userVideoSec`/`refVideoSec` 필드 부재 | §C-4 재산출 doc |
| F-5 게이지 라벨 · F-7 전환 · S12 시트 용어줄 | 화면까지 도달 못 함(정지 화면이라 **다음 세션에서 확인 가능**) | 시뮬 |
| S7 facing · S1 병합 배지 | 해당 doc/위치 도달 불가 | §C-4 재산출 doc |

**F-6(실기기 음성 무음) = FAIL 유지, 원인 미상.** 세션 경합을 파일:줄로 실증했으나 같은 증거가
반증도 했다(양쪽 카테고리 `.playback` 수렴 → 무음 스위치 축 설명 불가). 추측 수리 금지.
belle 실기기 분기 절차 = quick-260731-cum SUMMARY (첫 분기 = "큐 시점에 영상이 멈추는가").

## 시뮬 검증 요령 (다음 세션 시간 절약)

- **LogBox 배너가 하단 버튼 탭을 가로챈다.** 우측 X 먼저 눌러 치울 것. (배너 정체 =
  `expo-video allowsFullscreen` deprecation 2건, 기존·무관.)
- **AX 프레임의 y가 화면(874) 밖이면 그 탭은 엉뚱한 데 간다.** 스크롤로 화면 안에 넣고 탭할 것.
  이걸 몰라서 "칩→엉뚱한 시트", "감점 병합 결함" 오판을 각각 한 번씩 했다.
- 앱 uid = `fvcNXzEqKjgqVxRPVSj1iwFnIpn2`. **렌더 doc 4건은 §C-4 A-트랙에서 재산출됐다** —
  파워스핀 **60**(카드 4) / 킵업 79(카드 2) / pdshape 100(카드 1) / 엘보 **63**(카드 **5**).
  (구 값 = 80/79/100/60, 카드 5/3/2/0. 이동 사유 미분리 → Phase 34.)
  ⚠ **엘보가 이제 확대카드 5장을 가진다** — 종전 "카드 0"이라 도달 못 했던 시트·facing·S7 항목을
  이 doc 으로 확인할 수 있다.
- Metro는 8081에 detached로 떠 있다.

## Critical Anti-Patterns

| Anti-pattern | severity | 설명 |
|---|---|---|
| single-motion-fixation | blocking | 규칙은 동작별 데이터(record·criterion·jointKeys·technique)로 키잉, 동작명 분기 0, 등재 10동작 확인. memory: fix-generalize-beyond-discussed-motion |
| code-only-verification | blocking | 코드 통과 ≠ 완료. 33-G 항목별 재채점(승인 목업 대조)까지가 검증(D-40). 산출물을 직접 열 것. memory: verify-against-approved-mockup-not-just-code |
| repair-rediscussion | blocking | 수리 사이클에서 **취향·설계**를 재논의하지 말 것(D-39). 단 **데이터가 결정과 안 맞으면 그건 재논의가 아니라 보고** — 반드시 올린다. memory: repair-cycle-no-rediscussion · evidence-outranks-prior-decisions |
| over-generalize-breaks-approved | blocking | 한 승인 자산의 규칙을 다른 종류로 확대해 이미 PASS인 항목을 깨지 말 것(§C-1 L-10 실발생) |
| 근거 없는 의심 | blocking | 반박할 **측정치 없이** 검증된 작업을 흔들지 말 것. 거짓 안심과 거짓 불안은 같은 실패. 07-31 실발생. memory: evidence-outranks-prior-decisions |

## 완료분 요약 (재조사 금지)

- **§C-1 백엔드** (quick-260730-l7t): S9 PASS · S8 부분 · F-3 백엔드 PASS · S10 PASS→PARTIAL.
  근본원인 2건 확정 — belle #7·#9 "팔꿈치인데 손" = `_KISMAM_TO_KEYPOINT` elbow→hand 인접 대입 /
  F-3 = 앱이 `refIdx / rep.fps`로 초 추정(rep 18fps ↔ video 9fps 불일치).
- **§C-2 1단위** (260730-py1): S6 부분 PASS(부위 단위 시트, 한 부위 2감점 = 시트 1개·블록 2개 실증) ·
  S7 PARTIAL(번호 헤더·basis·method·numnote. **proof 3컷·basis 구간 축은 의도적 fail-closed** —
  백엔드가 카드당 1장만 주고 3컷 분류는 doc에 없는 판단이라 만들면 날조) · F-3 앱 코드 PASS.
- **§C-2 2단위** (260730-szk): F-8 PASS(상시 마커 제거, 안 보이는 탭 0) · S1 부분 PASS(그룹 경계+
  번호 배지, 개별 관절 원 0) · S3 PASS(부위 칩 = 감점 부위 수 일치).
- **§C-2 3단위** (260731-2jt): S13/S25 PASS(어깨 미부착·다리 부착 양방향) · S26 = 에셋 구도 귀결 ·
  P-14 illu-float 기준면 교정(row→패널).
- **§C-2 4단위** (260731-cum): F-4 PASS(헤드라인, **근본원인이 백엔드 phrasebook 조립**) ·
  S12 코드 게이트 PASS(3곳이 아니라 7파일 16곳 + 백엔드 게이트 스코프 확장).

## 재개 방법

`/clear` 후 이렇게 주면 됩니다:

```
33 수리 이어서. .planning/phases/33-result-trust-recovery/.continue-here.md 읽고
D-1(다리 사이각)부터. 재논의 없음, 승인 목업이 스펙.
```

Pod가 필요한 §C-4로 바로 가려면 `D-1 건너뛰고 §C-4부터` 라고 하면 됩니다.
