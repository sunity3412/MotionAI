# Continue Here — Phase 33 §9 수리 사이클: §C-1(백엔드) 완료, §C-2(앱) 착수 대기 (갱신 2026-07-30)

## 현재 위치

- **belle 확인 ② 반려(12건)** → 수리 사이클 진행 중. belle 확정: **수리는 재논의 없음** — 스펙 = 승인
  목업 7R(`mockups/index.html`) + `33-PHASE-GATE-EVIDENCE.md` §9. 결정 = 33-CONTEXT.md **D-39~D-45**.
- **정본 작업 목록 = `33-G-MOCKUP-DIFF.md`** (26 스펙 + §9-2 6건). §C-1 재채점 반영 완료.
- **§C-1 백엔드 완료** — quick-260730-l7t (`c9b0609` 코드 6커밋 + `57e35c1` docs).
  S9 FAIL→PASS · S8 FAIL→부분 PASS · F-3 FAIL→백엔드 PASS · S15 각도 축 PASS ·
  **S10 PASS→PARTIAL**(pre-existing 결함 발견). 자체 도출 결정 L-1~L-11 = 그 SUMMARY.

## §C-1이 확정한 근본원인 2건 (재조사 금지 — 실증 완료)

| 반려 | 근본원인 | 상태 |
|---|---|---|
| belle #7·#9 "팔꿈치인데 손을 집고 있음" | `CRITERION_REGION` 이 아니라 `pipeline/app.py` `_KISMAM_TO_KEYPOINT` 의 **elbow→hand 인접 대입**. 32-14로 keypointReport가 12관절이 되어 elbow 직접 표기 가능 | 백엔드 + 앱 미러(`deductionLabels.ts` `KEYPOINT_FROM_ANGLE_KEY`) 동시 제거 완료 |
| F-3 "자세 비교 페어가 다른 순간" | 앱이 `result.tsx:3066` 에서 `refIdx / rep.fps` 로 초를 추정 → rep(18fps 329f) ↔ video(9fps 220f) 타임베이스 불일치를 그대로 먹음. 백엔드는 `ref_display_frame_index` 로 이미 보정 중 | 백엔드가 `userVideoSec`/`refVideoSec` 방출(3-way lockstep). **앱 렌더가 §C-2 잔여** |

## 확정 순서 (2026-07-30, belle "진행하자 순서대로")

| # | 항목 | 상태 |
|---|---|---|
| 1 | **§C-2 앱** — 반려 대부분. 의존 없음 | 착수 |
| 2 | **무릎·팔꿈치 각도 트랙** — go/no-go 측정 **완료=GO**. 아래 §무릎·팔꿈치 참조 | 대기 |
| 3 | **D-1 (다리 사이각)** — 독립. 끝점을 카드 crop 내 관절로 제한(deferred 2안, crop 배율 불변) | 대기 |
| 4 | **§C-4 Pod** — crop·각도 전수 재생성 + 잔여 저신뢰 순간 앵커 주석 | 대기 |
| 5 | **일괄 OTA → belle 확인 ③ 1회** (D-45) | 대기 |

## 무릎·팔꿈치 각도 — 점수 무접촉 경로 (검증 완료, flip 불필요)

**핵심**: 채점은 `angles`(8 각도관절)만 읽고, 표시용 `referenceKeypointReport` 는 **버전 문서가
아니라 항상 최상위 필드에서** 읽힌다(`firestore_admin.py` `_REFERENCE_CONSUMER_FIELDS` 에 미포함).
그래서 최상위 그 필드만 12관절로 갈아 끼우면 `angles`·`activeVersion`·릴리스 포인터를 전혀
건드리지 않는다 = **33-07 flip 보류와 무관하게 진행 가능**. Firestore 인덱스 면제는 33-04에서
이미 provisioned.

- **go/no-go 측정 완료 (GPU 0, 읽기 전용)** — candidate `phase33-cm3-run1` 에 11/11 모션 12관절
  `referenceKeypointReport` 존재. ankle/elbow 게이트(conf≥0.5·유효좌표) 통과율:
  sideway-spin 0.95~0.97 / kip-up 0.91~0.96 / climb 0.81~0.90 / peter-pan 0.70~0.81 /
  foxtop-split 0.69~0.73 / invert·foxtop·combo 0.61~0.70 / power-spin 0.56~0.77 /
  **pdshape 0.40~0.64 · elbow-twist-sister 0.36~0.52**(약함 → 일부 순간 fail-closed, 틀린 선 아님).
- 작업 = ① 신규 ~50줄 스크립트: 11 기준 영상 **18fps** 재추출 → **최상위 `referenceKeypointReport`
  만** 갱신 (`extract_reference_keypoint_reports.py` 가 near-exact 선례. GPU 1패스 ~5분) →
  ② 무접촉 증명 ~15줄(읽기 전용): `_release_doc_hash` 로 채점 8필드 해시 불변 +
  `activeVersion==phase4_v1` 11/11 + `reference/_release` 부재 → ③ flip 방어 1줄.
- **18.0 fps 필수** — phase4_v1 angles 가 18fps 이고 `fault_zoom` 이 `_dtw_ref_fps` 를 표시
  보고서 fps 에서 파생한다. 9fps candidate 를 표시용으로 채택하면 뒤쪽 절반이 한 프레임으로
  붕괴한다(28-RESEARCH D2 기왕 수정 이력). phase4_v1 = 18.0 은 11/11 실측 확인됨.
- **"비트 동일"은 불가** — phase4_v1 은 `RTMW_DETERMINISTIC` 이전 산출이고 RTMW 는 재현 시
  각도가 흔들린다(기록 23.43°→0.193°). 그래서 숫자 증명 대신 **`angles` 를 아예 쓰지 않는**
  구조적 보증을 택한다.
- **§C-4 축소 효과**: 12관절 기준이 들어오면 **팔꿈치 좌표가 실재**하게 되어 어깨 각도가 직접
  성립한다 → 9모션 수동 주석이 대부분 불필요해지고, §C-1의 `reference_anchors` 기구는
  **저신뢰 순간 폴백**으로 격하된다(헛일 아님, 주력→보조).

## ⚠ 보류 중인 33-07 flip 에 잠복 버그 (발견 2026-07-30)

`reprocess_reference_motions_phase4._flip_active_pointer` 의 `mirror_fields` 에
`referenceKeypointReport` 가 **빠져 있다**. 지금 flip 하면 `angles` 는 9fps candidate 로 가는데
표시 보고서는 최상위 낡은 18fps 가 남아 `_dtw_ref_fps=18` vs 9fps DTW 공간 → 기준 패널이
**의도 시점의 약 절반**을 보여준다. 한 줄 추가로 방어(위 트랙 작업 ③). flip 여부와 무관하게 선행.

## 정정 기록 (앞선 판단 오류 2건)

- ~~"D-1을 flip 전에 고쳐야"~~ → **틀림.** D-1은 **이미 프로덕션 발화 중**(수강생 보고서가
  32-14 이후 12관절). split 은 both-or-neither ref-first 라 기준(8관절)은 무릎 폴백으로 통과하고
  수강생(12관절)이 발목을 잡아 실패 → 양쪽 취소. 무릎·팔꿈치 각도는 별 분기(`build_angle_bake_spec`)
  라 **D-1과 무관, 선행조건 아님**.
- ~~"12관절 재처리는 점수를 움직인다"~~ → **부정확.** 관절 개수는 채점 무관(`skeleton.JOINT_ANGLES`
  8개 불변, `angle_vs_reference__*` criteria 도 8개 그대로). 점수를 움직인 건 fps(18→9)와
  PR 인버전(off→on)이고, 그 둘을 안 바꾸면 안 움직인다.

## §C-2 앱 세부 (현재 착수분)

`33-G-MOCKUP-DIFF.md` §C 2번 순서대로. 재논의 없음(D-39), 승인 목업이 스펙.

1. **S7 시트 블록 요소** — 번호 헤더("고칠 것 N — 항목 (−N점)") · basis "어디서 재나요" ·
   method 정직 라벨 · proof 증거 3컷 · facing "두 사진이 달라 보이는 이유". 현재 앱에 **전무**(FAIL).
2. **S6 부위 단위 재구성** — record 단위 시트 → 부위 단위. paircap 초 = **백엔드가 이미 방출하는
   `userVideoSec`/`refVideoSec` 소비**(직접 재계산 금지 — 그게 F-3 원인이었다). onecap 추가.
   ⚠ paircap 텍스트 초를 넣으면 PNG 베이크 `_stamp_time` 과 **이중 표기**가 된다 → 어느 쪽을
   남길지 승인 목업 기준으로 판정(목업은 paircap 텍스트).
3. **S19 강조 선/원 분기 + pulse 1.4s** (M-3) → **S13 일러스트 장면일치 fail-closed** (M-5) →
   **S23 illu-float** → **S1~S3 그룹 마커·부위 칩** → **S12 어휘 잔재 3곳** →
   **F-4~F-8** (F-8 = 상시 마커 제거, D-42).
4. **F-3 앱분** — 참고코너(`result.tsx:3026`) 페어를 `refVideoSec` 기반으로.
5. 재채점: 33-G 표 + **등재 10동작 일반화 확인** + 시뮬 렌더(보류 S5·S22 판정).

## 그 다음: §C-4 (Pod) — §C-2 후

- 9모션 × criterion **앵커 주석 값** 채우기. 절차 = `backend/judging_data/reference_anchors/README.md`.
- **무릎·팔꿈치류 각도는 주석으로 복귀 불가** — 기준 라이브러리 **12kp 재처리**가 조건.
  (스위프 실증: 무릎 사지 방향점 = ankle 이 8kp에 없고 대체 불가, 팔꿈치는 `elbow←hand` 시 degenerate.)
- S10 12관절 doc 판정 + 수리(후보 2안 = quick-260730-l7t `deferred-items.md` D-1).
- crop·각도 베이크 전수 재생성 → **일괄 OTA → belle 확인 ③ 1회**(D-45).

## ⚠ 시뮬 검증 능력 한계 (2026-07-31 실측 — 다시 시도해 시간 쓰지 말 것)

**재생은 걸린다.** 실패 원인은 앱이 아니라 **LogBox 경고 배너가 재생 버튼 탭을 가로챈 것**이었다.
배너 X(우측)를 먼저 눌러 치우면 재생 버튼이 눌리고 AX 라벨이 `재생 → 일시정지` 로 바뀐다.
(배너 내용 = `expo-video allowsFullscreen` deprecation 2건, 기존·무관.)

**그런데 재생 중 화면 캡처가 진행을 못 담는다.** `일시정지` 상태(=재생 중) 확인 후
`xcrun simctl io screenshot` 20프레임을 0.3초 간격으로 떠도 **전 프레임이 바이트 동일**
(픽셀 변화 0.0, 노이즈조차 0). 시뮬레이터가 영상 레이어의 프레임 진행을 캡처에 반영하지 않는다.
MCP `screenshot` 도 동일. 정지 UI 변화는 정상 캡처되므로 캡처 자체의 문제는 아니다.

**귀결 — 재생 중에만 보이는 것은 시뮬로 검증 불가:**
S19(pulse 1.4s·선/원 분기) · S2(참고 점선) · S23(illu-float) · S18 회귀 · S22(멈춤 컷).
→ **belle 실기기 확인 ③ 로 넘긴다.** 33-G 에 "시뮬 검증 불가, 실기기 확인 필요"로 명시할 것.
조용히 PASS 처리 금지.

**남은 수단(다음에 필요하면):** `mcp__ios-simulator__record_video` 미시도 · 실기기 연결 ·
정지 상태를 코드로 만들 수 있는 진입점 신설(= 새 범위, 수리 사이클에서 금지).

## Critical Anti-Patterns

| Anti-pattern | severity | 설명 |
|---|---|---|
| single-motion-fixation | blocking | 한 동작 매몰 수정 금지 — 모든 fix·규칙은 동작별 데이터 소스(record.jointKeys·criterion·33-A1 표·technique)로 키잉, 완료 전 등재 10동작 일반화 확인. memory: fix-generalize-beyond-discussed-motion |
| code-only-verification | blocking | 코드 통과·렌더 확인 ≠ 완료 — 33-G 표 항목별 재채점(승인 목업 대조)까지가 검증(D-40). 산출물을 직접 열 것. memory: verify-against-approved-mockup-not-just-code |
| repair-rediscussion | blocking | 수리 사이클에서 belle에게 재논의 질문 금지 — 승인본+§9가 스펙, 잔여 판단은 "승인본이 정답/수리에 새 범위 금지" 원칙으로 자체 도출·문서화(D-39). memory: repair-cycle-no-rediscussion |
| over-generalize-breaks-approved | blocking | 한 승인 자산에서 뽑은 규칙을 다른 카드 종류로 확대해 **이미 PASS인 항목을 깨뜨리지 말 것** — §C-1에서 실제 발생(220px 정중앙 crop을 split 카드에 적용하니 S10 렌더 소멸 → L-10으로 범위 환원) |

## 남은 정리 (선택)

- `refs/stash@{0}`(`c165c7e`) — §C-1 중 사고성 `git stash` 잔재. 내용은 작업 트리에 복원·검증
  완료(사용자 선행 수정 2건 byte-동일)이므로 `git stash drop` 안전. 방치해도 무해.

## 재개 방법

`/clear` 후: `33-G-MOCKUP-DIFF.md §C 2번(앱)부터 수리 실행. .continue-here.md 참조. 재논의 없음.`
