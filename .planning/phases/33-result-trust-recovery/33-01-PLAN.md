---
phase: 33-result-trust-recovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/phase33/conftest.py
  - backend/scripts/dump_analysis_doc.py
  - .planning/phases/33-result-trust-recovery/33-A0-EVIDENCE.md
autonomous: true
requirements: [D-03, D-04, D-18, D-19, D-20]
must_haves:
  truths:
    - "Claude can dump belle's real analysis doc (uid csKW… / analysis 071df…) and open its 3 crop PNGs by value, not by success-return (D-19)"
    - "A-0 produces a 6-motion 전수 대조 table of 짚은 부위(pointed/crop joint + activated criterion) vs 실측 결함 부위(measured window/fallback joints) from EXISTING committed data — no new Pod inference (D-04)"
    - "A-0 emits an explicit D-04 branch verdict: 어긋남 큼 → HALT + re-plan to pull C+M3 into phase 33 / 어긋남 작음 → proceed to expression layer; the verdict is measured, not guessed (D-04)"
    - "No scoring formula or threshold is touched — A-0 is read-only observation (D-20)"
  artifacts:
    - path: "backend/tests/phase33/conftest.py"
      provides: "sys.path scaffold for phase33 backend tests (copy of phase32 conftest)"
      contains: "_LAYER"
    - path: "backend/scripts/dump_analysis_doc.py"
      provides: "Firestore doc dump keyed by uid/analysisId; prints faultJoints/records[].criterion/faultZoomComparisons[]; downloads crop PNGs to disk"
      min_lines: 40
    - path: ".planning/phases/33-result-trust-recovery/33-A0-EVIDENCE.md"
      provides: "pointed vs measured vs shown divergence table (6 motions) + D-04 branch verdict with evidence"
      contains: "어긋남"
  key_links:
    - from: "backend/scripts/dump_analysis_doc.py"
      to: "sunity_shared.firestore_admin.get_analysis"
      via: "reuse existing accessor, no hand-rolled firebase client"
      pattern: "get_analysis"
    - from: "33-A0-EVIDENCE.md"
      to: "backend/evals/phase25/baseline/phase25_sweep_report.json"
      via: "read faultJoints / seedObservation.window_joints,fallback_joints / activatedCriteria"
      pattern: "phase25_sweep_report"
---

<objective>
Build the Wave 0 substrate (phase33 test scaffold + a Firestore doc-dump helper — neither exists today) and run the A-0 분석 출력 정확성 확인 BLOCKING gate (D-04). A-0 compares, for all 6 fixture motions, "what the analysis currently points at" (Gemini-pointed joints + activated criteria that become on-screen items + crop joints) against "the actually-measured defect location" (window/fallback joints), using ONLY committed data (`phase25_sweep_report.json`) plus a dump of belle's real doc — no new Pod inference.

Purpose: The spine of this phase is "모든 표현은 정확한 분석의 열매" (D-03). If the analysis output itself points at the wrong body part (substrate contamination, esp. inversion motions), fixing the screen only makes "틀린 것을 예쁘게 보여주는 것." A-0 decides, by measurement not opinion, whether phase 33 is expression-only or must first pull the C+M3 substrate track in.

Output: `backend/tests/phase33/conftest.py`, `backend/scripts/dump_analysis_doc.py`, and `33-A0-EVIDENCE.md` (divergence table + D-04 branch verdict). This plan HALTS the phase if the verdict is "어긋남 큼."
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-result-trust-recovery/33-CONTEXT.md
@.planning/phases/33-result-trust-recovery/33-PLANNING-APPROACH.md
@.planning/phases/33-result-trust-recovery/33-RESEARCH.md
@.planning/phases/33-result-trust-recovery/33-PATTERNS.md
@backend/tests/phase32/conftest.py
@backend/evals/phase24/read_veto_status.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 substrate — phase33 test conftest + Firestore doc-dump helper</name>
  <read_first>
    - backend/tests/phase32/conftest.py (22-line sys.path scaffold — copy verbatim, change phase number in docstring)
    - backend/evals/phase24/read_veto_status.py (near-exact analog: reads a doc by id, prints fields, observation-only, Pod run header)
    - 33-PATTERNS.md §"backend/scripts/dump_analysis_doc.py" (path bootstrap + firestore client excerpt, lines 104-136)
    - backend/shared/python/sunity_shared/firestore_admin.py:1979 (get_analysis accessor) + :21-30 (lazy _db singleton via _auth._ensure_firebase)
  </read_first>
  <action>
    Create backend/tests/phase33/conftest.py by copying phase32/conftest.py verbatim, changing only the phase number in the docstring. Create backend/scripts/dump_analysis_doc.py: a CLI keyed by `--uid` and `--analysis-id` (defaulting to belle's target uid csKWYvI3WCPYPysNQ9KkWecaUvq1 / analysis 071df9f894d64d1696f106e613f51f5c) that calls sunity_shared.firestore_admin.get_analysis(uid, analysis_id) — never hand-roll a firebase-admin client. From the returned doc dump to stdout as readable JSON: result.visionVeto.faultJoints, result.visionVeto.faultJointDeficits, every result.deductionBreakdown.records[].criterion (the activatedCriteria set), and every result.faultZoomComparisons[] item (joint, region, deficitDeg, userFrameIdx, refFrameIdx, refMatch, tier, kind). Add a `--download-pngs <dir>` flag that fetches each faultZoomComparisons[].png presigned URL to disk so the operator can open them (D-19). Path bootstrap: HERE=scripts dir, BACKEND=parent, insert BACKEND/shared/python and BACKEND on sys.path. Creds via FIREBASE_SA_PATH (firebase-sa.json exists locally) — never hardcode. Print the run header (creds env expectation) analog to read_veto_status.py. Do NOT modify any scoring or analysis code (D-20).
  </action>
  <verify>
    <automated>cd backend && python -c "import ast,sys; ast.parse(open('scripts/dump_analysis_doc.py').read()); ast.parse(open('tests/phase33/conftest.py').read()); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - backend/tests/phase33/conftest.py exists and injects shared/python onto sys.path (grep `_LAYER`).
    - dump_analysis_doc.py imports `firestore_admin` and calls `get_analysis` (grep both) — zero hand-rolled `firebase_admin.initialize_app` in the script.
    - Running `python backend/scripts/dump_analysis_doc.py --uid csKWYvI3WCPYPysNQ9KkWecaUvq1 --analysis-id 071df9f894d64d1696f106e613f51f5c --download-pngs /tmp/a0_pngs` prints the faultJoints + records[].criterion + faultZoomComparisons[] VALUES to stdout and writes the crop PNG files to /tmp/a0_pngs (opened-artifact proof, D-19). If faultJoints or window is empty, the dump PRINTS the empty value — it is not swallowed as an error (D-18).
    - 틀리면 걸리는 장치 (D-18): the script prints raw stored values; a wrong/empty provenance surfaces as a visible empty/mismatched field in stdout, not a silent pass. No `except: pass` swallowing.
    - 무엇을 열어서 확인했는가 (D-19): the 3 crop PNGs are downloaded to disk and the JSON emit-values are printed — not "script ran successfully."
  </acceptance_criteria>
  <done>conftest + dump helper exist; belle's real doc dumps by value and its crop PNGs land on disk for eyeballing.</done>
</task>

<task type="auto">
  <name>Task 2: A-0 gate — 짚은 부위 vs 실측 결함 부위 6-motion 전수 대조 + D-04 branch verdict</name>
  <read_first>
    - backend/evals/phase25/baseline/phase25_sweep_report.json (12 members: results[].visionVeto.faultJoints / seedObservation.window_joints,fallback_joints / activatedCriteria)
    - 33-RESEARCH.md §"A-0 Gate Feasibility (concrete field map)" + §"Baseline extract (2026-07-05 run7, fault members)" + Pitfall 3 (empty-window is itself a divergence signal, not agreement)
    - 33-RESEARCH.md §Open Questions Q1 (does A-0 conclude 어긋남 큼?)
    - .planning/debug/ref-student-substrate-gap.md (the C+M3 track that gets pulled in on "어긋남 큼")
    - the /tmp/a0_pngs crop images + belle doc dump from Task 1
  </read_first>
  <action>
    Write .planning/phases/33-result-trust-recovery/33-A0-EVIDENCE.md. For each of the 6 fixture motions (power-spin, peter-pan, elbow-twist-sister, pdshape, kip-up, climb — both correct & fault members where present), build a table with 4 columns: pointed (visionVeto.faultJoints + crop joints), shown (activatedCriteria that become deductionBreakdown records), measured (seedObservation.window_joints primary → fallback_joints), and eyeball verdict (open the actual crop PNG from belle's dump + the matched ref video frame; confirm whether the crop joint corresponds to the shown criterion). Explicitly confirm the defect-5 case: a `source==='vision'` split_angle record projecting to a shoulder crop. Treat empty window_joints (baseline shows 11/12 empty) as a divergence SIGNAL favoring C+M3, per Pitfall 3 — do not count empty-window as "aligned." Then render the D-04 branch verdict: "어긋남 큼" or "어긋남 작음", with the table + PNG observations as the cited evidence. Judge by measurement, not by belle (D-04). Do NOT run any new Pod analysis; do NOT touch scoring code (D-20).
  </action>
  <verify>
    <automated>test -f .planning/phases/33-result-trust-recovery/33-A0-EVIDENCE.md && grep -qE "어긋남 (큼|작음)" .planning/phases/33-result-trust-recovery/33-A0-EVIDENCE.md && echo "VERDICT PRESENT"</automated>
  </verify>
  <acceptance_criteria>
    - 33-A0-EVIDENCE.md contains a per-motion table covering ALL 6 fixture motions (전수, D-18/D-23) with pointed/shown/measured/eyeball columns filled from real values — no motion left blank; if a value is empty in source, the cell says "empty (source)" not fabricated.
    - The D-04 verdict line reads exactly "어긋남 큼" or "어긋남 작음" and cites the table rows + opened PNG observations as evidence.
    - 틀리면 걸리는 장치 (D-18): the divergence is proven by concrete joint-set comparison + opened crop PNGs, not asserted; if the three joint sets disagree or window is empty, that disagreement is recorded, not smoothed over.
    - 무엇을 열어서 확인했는가 (D-19): the doc references the specific crop PNG files opened (전수) and the sweep-report fields read by value.
    - 이 산출물이 틀렸다면 어떻게 알았을까: answered in the doc — the pointed/shown/measured sets are printed side by side, so a wrong pointing is visible as a set mismatch against the opened PNG.
  </acceptance_criteria>
  <done>
    A-0 verdict is documented with evidence. BRANCH CONTROL: if verdict = "어긋남 큼", this phase HALTS here — do NOT proceed to 33-02+; return to the planner to insert the C+M3 substrate track (ref-student-substrate-gap.md) into phase 33 before any expression work (D-04). If verdict = "어긋남 작음", the expression-layer plans (33-02 onward) proceed as planned. The branch is NOT a belle question (D-01/D-04).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| script → Firestore (read) | dump helper reads a user's analysis doc with admin creds; read-only, no write |
| S3 presigned → local disk | crop PNG download over TTL-bound presigned URL |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Information disclosure | dump_analysis_doc.py credentials | mitigate | creds via FIREBASE_SA_PATH / Parameter Store only; never hardcode (CLAUDE.md); local dev script, not deployed |
| T-33-02 | Tampering | read-only accessor | accept | script calls get_analysis (read); no write path; no scoring mutation (D-20) |
| T-33-SC | Tampering | package installs | accept | no npm/pip installs in this plan (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `python -c "ast.parse(...)"` on both new python files passes.
- dump helper produces real values + downloads PNGs from belle's doc.
- 33-A0-EVIDENCE.md has all 6 motions + an explicit 어긋남 verdict.
- No diff to any file under backend/shared/.../analysis/ scoring math (D-20): `git diff --stat` shows only tests/phase33, scripts/, and .planning changes.
</verification>

<success_criteria>
Substrate exists and A-0 has rendered a measured D-04 branch verdict with opened-artifact evidence. The phase either proceeds (어긋남 작음) or halts for C+M3 re-plan (어긋남 큼).
</success_criteria>

<output>
Create `.planning/phases/33-result-trust-recovery/33-01-SUMMARY.md` when done, including the A-0 verdict verbatim and, if "어긋남 큼", the HALT + re-plan recommendation.
</output>
