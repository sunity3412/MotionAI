---
phase: 33-result-trust-recovery
plan: 02
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - backend/scripts/backup_reference_docs.py
  - .gitignore
  - .planning/debug/backups/
autonomous: true
requirements: [D-11, D-18, D-19, D-25, D-26, D-27, D-31]
user_setup: []
must_haves:
  truths:
    - "A full backup of all 11 reference/{id} docs (top-level, every field, ~7.4 MiB single file) exists BEFORE any reprocess write — no write happens without it (SEED Task 0 / D-31)"
    - "The backup integrity gate proves restorability at backup time: 11/11 docs present + isActive=True + activeVersion='phase4_v1'; per-doc anglesFrames==joints3dFrames==keypointReport.frames; array-length == frames×keys×dim; per-doc SHA-256 in header (SEED 롤백 설계 4-check)"
    - "The backup is stored locally AND to S3 (dual copy) and is git-ignored — never committed (D-31)"
    - "If any integrity check fails, the file is marked FAILED and downstream reprocess (33-03) MUST NOT start — the gate blocks (D-31/D-18)"
  artifacts:
    - path: "backend/scripts/backup_reference_docs.py"
      provides: "read-only Firestore dump of 11 reference docs + 4-check integrity gate + SHA-256 header + S3 upload"
      contains: "sha256"
    - path: ".planning/debug/backups/"
      provides: "reference-11-preC-{ts}.json local backup (git-ignored) — rollback source paired with S3 mirror"
      contains: ""
  key_links:
    - from: "backend/scripts/backup_reference_docs.py"
      to: "firestore_admin.get_analysis / reference reader (read-only get)"
      via: "reuse existing Firestore admin client; no direct firebase-admin init"
      pattern: "firestore_admin|get_reference|reference"
    - from: "backup_reference_docs.py"
      to: "s3://sunity-motion-pilot-videos/backups/reference-11-preC-{ts}.json"
      via: "boto3 put_object dual copy"
      pattern: "backups/reference-11-preC"
---

<objective>
Task 0 of the C+M3 substrate track (SEED `## 인계` / `## 롤백 설계`): write and run a read-only backup of all 11 reference motion docs before any reprocess touches them. This is the unconditional precondition — SEED states "백업 JSON 이 없으면 어떤 write 도 금지." The backup proves restorability at capture time via a 4-check integrity gate, is stored locally + S3, and is git-ignored (7.4 MiB, binary-ish). A-0 verdict "어긋남 큼" (33-01, D-26) put this substrate track in scope; this is its root wave.

Purpose: Rollback safety (D-31). If reprocess (33-03) or flip (33-07) goes wrong, this backup is the source-of-truth restore. The integrity gate blocks the whole track if the backup is not provably complete.

Output: backup_reference_docs.py (new, read-only) + reference-11-preC-{ts}.json (local + S3) + integrity gate PASS record.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-result-trust-recovery/33-CONTEXT.md
@.planning/debug/ref-student-substrate-gap.md
@backend/scripts/dump_analysis_doc.py
@backend/scripts/measure_reference_fps.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: write backup_reference_docs.py (read-only dump + 4-check integrity gate + SHA-256) + gitignore</name>
  <read_first>
    - backend/scripts/dump_analysis_doc.py (read-only Firestore pattern: reuse firestore_admin, no direct firebase-admin init; argparse --uid/--analysis-id; PYTHONPATH=shared/python:. FIREBASE_SA_PATH=../firebase-sa.json)
    - backend/scripts/measure_reference_fps.py (reference doc reader — how the 11 reference/{id} docs are fetched read-only; the 11 motion ids)
    - .planning/debug/ref-student-substrate-gap.md `## 롤백 설계 / 백업` (target = reference/{id} 11 doc 전량 top-level, 7.4 MiB single file, path `.planning/debug/backups/reference-11-preC-{YYYYMMDD-HHMMSS}.json`, dual S3 `s3://sunity-motion-pilot-videos/backups/`, 4-check gate)
    - MEMORY reference-library-phase4-all11 (the 11 motion ids — do NOT use the 5-subset default)
  </read_first>
  <action>
    Create backend/scripts/backup_reference_docs.py as a READ-ONLY dump (no Firestore write, no Pod). It accepts `--motions` (the 11 ids: ref-climb ref-foxtop ref-foxtop-split ref-invert ref-sideway-spin ref-combo ref-elbow-twist-sister ref-kip-up ref-pdshape ref-peter-pan ref-power-spin) and `--out`. Fetch each reference/{id} doc's FULL top-level (every field, not a subset) via the existing firestore_admin reader (reuse the get pattern from dump_analysis_doc.py — do NOT init firebase-admin directly). Serialize all 11 into one JSON with a header block that records, per doc, a SHA-256 hash of its serialized content. Implement the 4-check integrity gate (SEED 롤백 설계): (1) 11/11 docs present AND isActive=True AND activeVersion='phase4_v1'; (2) per-doc anglesFrames == joints3dFrames == keypointReport.frames; (3) per-doc len(angles)/len(joints3d) == frames × len(keys) × dim; (4) SHA-256 written to header per doc. On ANY check failure the script exits non-zero and marks the output FAILED (so 33-03 cannot proceed). Add `.planning/debug/backups/` to .gitignore (7.4 MiB binary-ish, git 커밋 금지 per D-31). Do NOT touch scoring/analysis code (D-20 spirit — read-only).
  </action>
  <verify>
    <automated>cd backend && python3 -c "import ast; ast.parse(open('scripts/backup_reference_docs.py').read()); print('parse ok')" && grep -qE "sha256|hashlib" scripts/backup_reference_docs.py && grep -q "backups" ../.gitignore && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - backup_reference_docs.py exists, parses, is read-only (grep: no set(/update(/write to Firestore; only get/read).
    - The 4-check integrity gate is implemented and returns non-zero on any failure (grep the checks: isActive, anglesFrames==joints3dFrames, sha256).
    - `.planning/debug/backups/` is git-ignored (grep .gitignore).
    - 틀리면 걸리는 장치 (D-18): the gate is the device — a partial/corrupt backup exits non-zero, blocking 33-03. The task states which check would catch a truncated dump.
  </acceptance_criteria>
  <done>Read-only backup script with a blocking 4-check integrity gate exists; backups dir git-ignored.</done>
</task>

<task type="auto">
  <name>Task 2: run backup (local + S3 dual) + prove integrity gate PASS + open the artifact</name>
  <read_first>
    - .planning/debug/ref-student-substrate-gap.md `## 롤백 설계 / 백업 명령` (the exact invocation shape + dual S3 path)
    - MEMORY aws-keys-and-bucket (bucket sunity-motion-pilot-videos, motion-ai keys) + gemini-key-local-ssm-profile (--profile sunity-motion for AWS)
  </read_first>
  <action>
    Run backup_reference_docs.py against all 11 reference ids, writing `.planning/debug/backups/reference-11-preC-{YYYYMMDD-HHMMSS}.json` locally, then upload the identical file to `s3://sunity-motion-pilot-videos/backups/reference-11-preC-{ts}.json` (dual copy, D-31). Confirm the 4-check integrity gate returns PASS (exit 0). Then OPEN the artifact (D-19): print the header, list all 11 doc ids with their SHA-256, and dump each doc's frames/anglesFrames/joints3dFrames/keypointReport.frames triple to prove the equality check held — do NOT accept "script returned 0" as verification. Record the local path + S3 URI + per-doc SHA-256 in the SUMMARY. If any check fails, STOP and mark 33-03 blocked (do not proceed to reprocess).
  </action>
  <verify>
    <automated>ls .planning/debug/backups/reference-11-preC-*.json >/dev/null 2>&1 && python3 -c "import json,glob; d=json.load(open(sorted(glob.glob('.planning/debug/backups/reference-11-preC-*.json'))[-1])); print('docs:', len(d.get('docs', d.get('seedPayload', d))) )" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Local backup file exists AND the same bytes are uploaded to S3 (state both paths in SUMMARY); 11 docs present.
    - Integrity gate PASS (exit 0) — all 4 checks green; the SUMMARY lists the 4 results, not just "passed."
    - 무엇을 열어서 확인했는가 (D-19): the SUMMARY shows the opened header + per-doc SHA-256 + the frames-equality triple per doc, proving the dump is real and complete.
    - 이 산출물이 틀렸다면 어떻게 알았을까: answered — a truncated/partial backup would fail check (1) or (2) and exit non-zero, and the opened frames triple would show a mismatch.
  </acceptance_criteria>
  <done>Backup written locally + S3, integrity gate PASS proven by opening the artifact; rollback source armed for the whole track.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Firestore reference/{id} → local JSON + S3 | read-only dump of production reference docs; no write back |
| local backup → S3 backups/ | boto3 put_object; TTL-free archival copy |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-20 | Tampering | truncated/partial backup masquerading as complete | mitigate | 4-check integrity gate + per-doc SHA-256; non-zero exit blocks 33-03 (D-31) |
| T-33-21 | Information disclosure | reference doc dump on disk / S3 | accept | reference coords are not PII; local path git-ignored; S3 in existing private bucket |
| T-33-22 | Repudiation | which backup restores which state | mitigate | timestamped filename + SHA-256 header records exact capture |
| T-33-SC | Tampering | package installs | accept | no new packages (reuse boto3, firestore_admin, hashlib stdlib) |
</threat_model>

<verification>
- backup_reference_docs.py exists, read-only, 4-check gate + SHA-256.
- reference-11-preC-{ts}.json exists locally AND in S3; 11 docs; gate PASS.
- Opened artifact: header + SHA-256 + frames-equality triple per doc dumped (D-19).
</verification>

<success_criteria>
A provably-complete, integrity-gated backup of all 11 reference docs exists locally and in S3 before any reprocess write, with the gate blocking the track on failure — the rollback foundation (D-31) for the entire substrate wave.
</success_criteria>

<output>
Create `.planning/phases/33-result-trust-recovery/33-02-SUMMARY.md` when done, recording the local path, S3 URI, per-doc SHA-256, and the 4 integrity-check results.
</output>
