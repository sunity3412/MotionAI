---
phase: 33-result-trust-recovery
plan: 02
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - backend/scripts/backup_reference_docs.py
  - .gitignore
  - .planning/debug/backups/
autonomous: true
requirements: [D-11, D-18, D-19, D-25, D-26, D-27, D-31]
user_setup: []
must_haves:
  truths:
    - "A full backup of all 11 reference/{id} docs (top-level, every field, ~7.4 MiB) exists BEFORE any reprocess write — no write happens without it (SEED Task 0 / D-31)"
    - "The backup is written to a TEMP file first and only renamed to the final backup path AFTER the 4-check integrity gate passes; a FAILED dump is written to a *.FAILED name that does NOT match the backup glob (codex concern 10 — no FAILED output masquerading as a backup)"
    - "S3 recovery integrity is PROVEN, not assumed: a whole-file SHA-256 is computed, uploaded as S3 object metadata, then the object is re-downloaded and byte-compared to the local file (codex suggestion 8 / D-31)"
    - "A RESTORE rehearsal proves round-trip: the backup JSON is restored into an ISOLATED collection (or emulator), and the restored docs byte-match the source — done BEFORE 33-03 runs (codex suggestion 8)"
    - "The 4-check integrity gate proves restorability: 11/11 docs + isActive + activeVersion='phase4_v1'; anglesFrames==joints3dFrames==keypointReport.frames; array-length==frames×keys×dim; per-doc SHA-256 in header (SEED 롤백 설계)"
    - "The backup is stored locally AND S3 (dual) and git-ignored (D-31)"
  artifacts:
    - path: "backend/scripts/backup_reference_docs.py"
      provides: "read-only dump → temp file → 4-check gate → whole-file SHA-256 → S3 upload w/ checksum metadata → re-download byte-compare → restore rehearsal"
      contains: "sha256"
    - path: ".planning/debug/backups/"
      provides: "reference-11-preC-{ts}.json (git-ignored) local backup, S3-mirrored, PASS-manifest-gated"
      contains: ""
  key_links:
    - from: "backup_reference_docs.py"
      to: "s3://sunity-motion-pilot-videos/backups/reference-11-preC-{ts}.json"
      via: "boto3 put_object with SHA-256 as object metadata + re-download byte-compare"
      pattern: "backups/reference-11-preC|Metadata|sha256"
    - from: "backup_reference_docs.py restore rehearsal"
      to: "isolated collection / emulator"
      via: "round-trip restore + byte-compare before 33-03"
      pattern: "restore|rehearsal|isolated"
---

<objective>
Task 0 of the C+M3 substrate track (SEED `## 롤백 설계`), hardened per codex concern 10 / suggestion 8. Write and run a read-only backup of all 11 reference docs before any reprocess write, and PROVE the recovery copy is byte-faithful and restorable: temp-file-then-atomic-rename (no FAILED output matching the backup glob), whole-file SHA-256 uploaded as S3 metadata, re-download byte-compare, and a restore rehearsal into an isolated collection/emulator — all before 33-03. SEED: "백업 JSON 이 없으면 어떤 write 도 금지."

Purpose: Rollback safety (D-31). Per-document hashes inside the JSON only detect later edits; they do NOT prove the S3 object holds identical bytes nor that Firestore values round-trip. This plan closes that gap so the rollback source is provably usable, not just present.

Output: backup_reference_docs.py (hardened) + reference-11-preC-{ts}.json (local + S3, checksum-verified) + a PASS manifest + a restore-rehearsal record.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-result-trust-recovery/33-CONTEXT.md
@.planning/phases/33-result-trust-recovery/33-REVIEWS.md
@.planning/debug/ref-student-substrate-gap.md
@backend/scripts/dump_analysis_doc.py
@backend/scripts/measure_reference_fps.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: write backup_reference_docs.py — temp-then-rename, 4-check gate, whole-file SHA-256, S3 metadata, re-download byte-compare, restore rehearsal + gitignore</name>
  <read_first>
    - backend/scripts/dump_analysis_doc.py (read-only Firestore pattern: reuse firestore_admin, no direct firebase-admin init; PYTHONPATH=shared/python:. FIREBASE_SA_PATH=../firebase-sa.json)
    - backend/scripts/measure_reference_fps.py (how the 11 reference/{id} docs are fetched read-only + the 11 motion ids)
    - .planning/debug/ref-student-substrate-gap.md `## 롤백 설계 / 백업` (target = reference/{id} 11 doc top-level, 7.4 MiB, path .planning/debug/backups/reference-11-preC-{ts}.json, dual S3, 4-check gate) + `## 복원 절차` (set(merge=False) whole-doc restore — the rehearsal must exercise this)
    - 33-REVIEWS.md codex concern 10 (medium) + suggestion 8 (temp file, separate PASS manifest, whole-file SHA-256, S3 metadata checksum, re-download byte-compare, restore rehearsal into isolated collection/emulator)
    - MEMORY reference-library-phase4-all11 (the 11 ids) + aws-keys-and-bucket + gemini-key-local-ssm-profile (--profile sunity-motion)
  </read_first>
  <action>
    Create backend/scripts/backup_reference_docs.py, read-only (no reference write; get/read only). Accept `--motions` (11 ids) and `--out`. Fetch each reference/{id} FULL top-level via the existing firestore_admin reader (no direct firebase-admin init). Write the serialized dump to a TEMP file (`{out}.tmp`), compute per-doc SHA-256 into a header AND a whole-file SHA-256. Run the 4-check integrity gate: (1) 11/11 docs + isActive + activeVersion=='phase4_v1'; (2) anglesFrames==joints3dFrames==keypointReport.frames; (3) len(angles)/len(joints3d)==frames×keys×dim; (4) per-doc + whole-file SHA-256 recorded. On PASS: atomically rename `{out}.tmp` → `{out}` (the only name matching the backup glob `reference-11-preC-*.json`) and write a SEPARATE `{out}.MANIFEST.json` = PASS manifest. On FAIL: write `{out}.FAILED` (a name that does NOT match the backup glob) and exit non-zero — a failed dump can never masquerade as a backup (codex concern 10). Add an S3 step: put_object with the whole-file SHA-256 as object Metadata, then re-download the S3 object and byte-compare to the local file (fail if different). Add a `--rehearse-restore` mode: restore the backup JSON into an ISOLATED collection (e.g. `reference_restore_rehearsal/{id}` via set(merge=False)) OR the Firestore emulator, read back, and byte-compare the restored docs to the source (round-trip proof) — never touch the real reference collection. Add `.planning/debug/backups/` to .gitignore. Do NOT touch scoring/analysis code (D-20 spirit).
  </action>
  <verify>
    <automated>cd backend && python3 -c "import ast; ast.parse(open('scripts/backup_reference_docs.py').read()); print('parse ok')" && grep -qE "sha256|hashlib" scripts/backup_reference_docs.py && grep -qiE "\.tmp|rename|FAILED" scripts/backup_reference_docs.py && grep -qiE "Metadata|re-download|byte.compare|rehears" scripts/backup_reference_docs.py && grep -q "backups" ../.gitignore && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Read-only (grep: no reference set/update; only get/read).
    - Temp-then-atomic-rename: only a PASSED backup lands at the `reference-11-preC-*.json` glob; a FAILED dump goes to `*.FAILED` (grep) — no FAILED output matching the backup glob (concern 10).
    - Whole-file SHA-256 uploaded as S3 object Metadata; re-download + byte-compare implemented (grep).
    - `--rehearse-restore` restores into an isolated collection/emulator and byte-compares round-trip (grep).
    - `.planning/debug/backups/` git-ignored.
    - 틀리면 걸리는 장치 (D-18): a truncated dump fails check (1)/(2) → *.FAILED + non-zero; a corrupted S3 upload fails the re-download byte-compare; a non-round-trippable value fails the rehearsal.
  </acceptance_criteria>
  <done>Read-only backup script with atomic PASS-gating, whole-file S3 checksum verification, and a restore rehearsal exists; backups dir git-ignored.</done>
</task>

<task type="auto">
  <name>Task 2: run backup (local + S3, checksum-verified) + restore rehearsal + open the artifact</name>
  <read_first>
    - .planning/debug/ref-student-substrate-gap.md `## 롤백 설계 / 백업 명령` (exact invocation + dual S3 path) + `## 복원 절차` (the restore path the rehearsal mirrors)
    - MEMORY aws-keys-and-bucket (bucket sunity-motion-pilot-videos) + gemini-key-local-ssm-profile (--profile sunity-motion for AWS)
  </read_first>
  <action>
    Run backup_reference_docs.py for all 11 ids → local `.planning/debug/backups/reference-11-preC-{ts}.json` (only after PASS) + `{...}.MANIFEST.json`, upload to `s3://sunity-motion-pilot-videos/backups/reference-11-preC-{ts}.json` with the SHA-256 as object metadata, and confirm the re-download byte-compare passes. Then run `--rehearse-restore` and confirm the round-trip byte-compare passes into the isolated collection/emulator (never the real reference). Then OPEN the artifact (D-19): print the PASS manifest, list all 11 doc ids + per-doc SHA-256 + the whole-file SHA-256, dump each doc's frames/anglesFrames/joints3dFrames/keypointReport.frames triple, and print the S3 object's stored metadata checksum next to the local one (they must match). Record local path + S3 URI + whole-file SHA-256 + rehearsal result in the SUMMARY. If ANY check fails, STOP and mark 33-03 blocked.
  </action>
  <verify>
    <automated>ls .planning/debug/backups/reference-11-preC-*.json >/dev/null 2>&1 && python3 -c "import json,glob; f=sorted(glob.glob('.planning/debug/backups/reference-11-preC-*.json'))[-1]; d=json.load(open(f)); assert not f.endswith('.FAILED'); print('backup docs present')" && ls .planning/debug/backups/reference-11-preC-*.MANIFEST.json >/dev/null 2>&1 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Local backup at the PASS glob (never *.FAILED) + PASS manifest present; same bytes uploaded to S3; re-download byte-compare PASS (SUMMARY states both paths + the matching checksums).
    - Restore rehearsal round-trip byte-compare PASS into an isolated collection/emulator (SUMMARY records it) — the real reference untouched.
    - Integrity gate PASS — all 4 checks green + whole-file SHA-256 (SUMMARY lists the 4 results + the hash).
    - 무엇을 열어서 확인했는가 (D-19): opened PASS manifest + per-doc SHA-256 + whole-file SHA-256 + frames-triple + S3-vs-local checksum match are shown, not "script returned 0".
    - 이 산출물이 틀렸다면 어떻게 알았을까: answered — a truncated/partial backup fails a gate check + goes to *.FAILED; an S3 corruption fails the re-download compare; a non-round-trippable value fails the rehearsal.
  </acceptance_criteria>
  <done>Backup written + checksum-verified in S3 + restore-rehearsed + opened; the rollback source is provably usable, arming the whole track.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Firestore reference/{id} → local JSON + S3 | read-only dump of production reference docs; no write back |
| local backup → S3 backups/ | boto3 put_object w/ checksum metadata; re-download byte-compare |
| backup JSON → isolated restore rehearsal | round-trip proof into a non-production collection/emulator |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-20 | Tampering | truncated/partial backup masquerading as complete | mitigate | 4-check gate + temp-then-rename; FAILED → *.FAILED off-glob; non-zero blocks 33-03 (concern 10) |
| T-33-21 | Tampering | S3 copy differs from local bytes | mitigate | whole-file SHA-256 as S3 metadata + re-download byte-compare (suggestion 8) |
| T-33-22 | Tampering | backup not actually restorable | mitigate | restore rehearsal round-trip byte-compare into isolated collection/emulator (suggestion 8) |
| T-33-23 | Information disclosure | reference dump on disk / S3 | accept | reference coords not PII; local git-ignored; S3 private bucket |
| T-33-SC | Tampering | package installs | accept | reuse boto3/firestore_admin/hashlib stdlib |
</threat_model>

<verification>
- backup_reference_docs.py: read-only, temp-then-rename, 4-check + whole-file SHA-256, S3 metadata checksum + re-download byte-compare, restore rehearsal.
- reference-11-preC-{ts}.json at PASS glob (+ MANIFEST) local AND S3; checksums match; rehearsal round-trip PASS.
- Opened artifact: PASS manifest + per-doc/whole-file SHA-256 + frames triple + S3-vs-local checksum (D-19).
</verification>

<success_criteria>
A provably-complete AND provably-restorable backup of all 11 reference docs exists locally and in S3 with a verified whole-file checksum and a passed restore rehearsal, gated by temp-then-rename so no FAILED output can pose as a backup — the rollback foundation (D-31) hardened per codex concern 10.
</success_criteria>

<output>
Create `.planning/phases/33-result-trust-recovery/33-02-SUMMARY.md` when done, recording the local path, S3 URI, whole-file + per-doc SHA-256, the S3-vs-local checksum match, the 4 integrity results, and the restore-rehearsal round-trip result.
</output>
</content>
