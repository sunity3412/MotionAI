---
phase: 33-result-trust-recovery
plan: 03
type: execute
wave: 3
depends_on: ["33-02", "33-17", "33-18"]
files_modified:
  - .planning/phases/33-result-trust-recovery/33-S1-REPROCESS-EVIDENCE.md
autonomous: false
requirements: [D-18, D-19, D-20, D-25, D-26, D-27, D-29, D-30, D-31]
user_setup: []
must_haves:
  truths:
    - "All 11 reference motions are reprocessed at --target-fps 9.0 --no-flip to an IMMUTABLE candidate version --version phase33-cm3-run1 (and a second run under phase33-cm3-run2) — writes reference/{id}/versions/{candidate}, never versions/phase4_v1; refuse-overwrite + candidate!=active hold (codex concern 1 / 33-17 / D-31)"
    - "The Pod is deployed at an EXACT pinned commit: start_server sets SUNITY_COMMIT_SHA and /health returns that SHA + the model-init canary + PR/deterministic flags (33-18); the reprocess only proceeds after the /health canary confirms the pinned commit + model parity (codex concerns 5/6 / D-30)"
    - "Traffic isolation: reprocess runs on a DEDICATED eval Pod OR with Lambda production traffic drained — 'SERIAL' governs planned calls only, so external analyses must not run concurrently with re-extraction (codex concern 14 / D-30)"
    - "A release manifest row is created for each candidate: candidate id, per-doc hashes, commit SHA, targetFps=9.0, PR=1, deterministic=1, derivedFieldSchemaVersion (33-18 release_manifest.py)"
    - "detect_inversion logs are collected vs the 재처리 위험도 표 (R-1); ref-combo 2-run variance recorded (R-4); power-spin/peter-pan do NOT newly detect inversion (R-2); no threshold refit (D-20/D-29)"
    - "The 33-02 backup + restore-rehearsal are PASS before this plan starts (D-31)"
  artifacts:
    - path: ".planning/phases/33-result-trust-recovery/33-S1-REPROCESS-EVIDENCE.md"
      provides: "candidate ids + /health commit-canary + traffic-isolation record + detect_inversion vs risk table + ref-combo 2-run variance + manifest rows"
      contains: "phase33-cm3"
  key_links:
    - from: "reprocess ... --version phase33-cm3-run1 --target-fps 9.0 --no-flip"
      to: "reference/{id}/versions/phase33-cm3-run1 (candidate != active phase4_v1)"
      via: "33-17 candidate-id write + refuse-overwrite + candidate!=active"
      pattern: "phase33-cm3|versions|no-flip"
    - from: "Pod /health commit SHA + canary"
      to: "SUNITY_COMMIT_SHA pinned deploy (33-18)"
      via: "canary confirms source/model parity before reprocess"
      pattern: "commit|canary|SUNITY_COMMIT_SHA"
---

<objective>
Task 1 of the C+M3 substrate track (SEED Task 1), corrected per codex concerns 1/5/6/14. Reprocess all 11 reference motions at 9.0 fps with PR inversion correction, writing to an IMMUTABLE candidate version (`phase33-cm3-run1`, plus `run2` for comparison) via the 33-17 versioning primitives — NEVER to `versions/phase4_v1` (which is the active name). Deploy the Pod at an EXACT pinned commit and confirm source/model parity via the 33-18 `/health` commit-SHA + canary before touching data. Isolate from production traffic (dedicated eval Pod or drained Lambda). Record the candidate rows into the release manifest. Pod required (all terminated — D-30); autonomous:false at the GPU greenlight and canary gates.

Purpose: The reference substrate is the root of the A-0 "어긋남 큼" verdict. This aligns reference density (M1) + inversion handling (M6) to the student path — but only as an isolated candidate that cannot leak into the active pointer (concern 1) and only on provably-pinned code (concerns 5/6), with production analyses drained so the serial eval isn't contaminated (concern 14). No scoring math changes (D-20/D-29).

Output: 33-S1-REPROCESS-EVIDENCE.md (candidate ids, /health canary, traffic-isolation record, inversion logs vs risk table, ref-combo variance, manifest rows). Active pointer NOT flipped (flip is 33-07).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-result-trust-recovery/33-CONTEXT.md
@.planning/phases/33-result-trust-recovery/33-REVIEWS.md
@.planning/debug/ref-student-substrate-gap.md
@backend/scripts/reprocess_reference_motions_phase4.py
@backend/scripts/release_manifest.py
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: belle GPU greenlight — dedicated eval Pod + traffic isolation (D-30 / D-32ⓐ ops exception)</name>
  <read_first>
    - 33-CONTEXT.md D-25/D-30 (all Pods terminated; reboot precondition; eval SERIAL) + 33-REVIEWS.md codex concern 14 (dedicated eval Pod OR drain Lambda during gates — 'SERIAL' only governs planned calls)
    - MEMORY runpod-eu-ro1-gpu-priority + pod-request-include-gpu-model + next-pod-use-network-storage
  </read_first>
  <action>
    PAUSE for belle's operational greenlight to spin up a GPU Pod for the reference reprocess (2 runs, 11 motions). Request the GPU explicitly: EU-RO-1, 4090 priority, Network Storage Pod. CRUCIAL (codex concern 14): request this as a DEDICATED eval Pod — production Lambda must NOT be resynced to it — OR, if the same Pod is reused, plan to DRAIN Lambda production traffic while the reprocess/sweep run (external analyses must not hit the pipeline concurrently with re-extraction). This is an ops/cost greenlight (D-32ⓐ), not a design touchpoint. Also confirm the 33-02 backup + restore-rehearsal are PASS (D-31 — no write without a proven-restorable backup).
  </action>
  <what-built>Nothing yet — requests belle to greenlight a dedicated eval GPU Pod (or a Lambda drain window) so re-extraction runs without production contamination.</what-built>
  <how-to-verify>
    1. Approve GPU/credits for a dedicated eval Pod (or a maintenance drain window).
    2. Confirm the 33-02 backup + restore rehearsal passed.
  </how-to-verify>
  <verify>
    <automated>echo "checkpoint — belle GPU greenlight + traffic-isolation decision (ops, not a design touchpoint)"</automated>
  </verify>
  <resume-signal>Type "go" (dedicated eval Pod) or "drain" (reuse + drain Lambda), or hold.</resume-signal>
  <done>belle greenlit a dedicated eval Pod or a drain window; 33-02 backup confirmed PASS.</done>
</task>

<task type="auto">
  <name>Task 2: Pod bootstrap at pinned commit + /health commit-SHA + model-init canary + traffic isolation confirmed</name>
  <read_first>
    - MEMORY current-pod-hbpvhedq2bu01i (bootstrap → start_server → VETO env → X-RunPod-Token smoke → SSM+Lambda resync) + rtmw-blackwell-lean-bootstrap + next-pod-use-network-storage
    - backend/runpod_inference/server.py (/health now returns commit SHA + canary per 33-18; env PR_INVERSION_ENABLED, RTMW_DETERMINISTIC, SUNITY_COMMIT_SHA)
    - 33-REVIEWS.md codex concerns 5/6/14 (deploy the exact commit + expose SHA in health; canary not just /health 200; traffic isolation)
  </read_first>
  <action>
    Claude sets up the belle-greenlit Pod: check out/deploy the EXACT phase-33 commit, start_server with env PR_INVERSION_ENABLED=1 (M6), RTMW_DETERMINISTIC=1 (R-4), and SUNITY_COMMIT_SHA set to the deployed commit. Hit /health (X-RunPod-Token) and CONFIRM the canary: returned commit SHA == the intended commit AND the model-init canary is green AND the env flags read PR=1/deterministic=1 (33-18) — a bare 200 is NOT sufficient (concern 6). Traffic isolation (concern 14): if a dedicated eval Pod, do NOT resync production Lambda to it; if reusing, drain Lambda first and record the drain. Do NOT flip any reference. Record Pod id, proxy URL, the /health canary body (commit + canary + flags), and the isolation mode in 33-S1-REPROCESS-EVIDENCE.md.
  </action>
  <verify>
    <automated>test -f .planning/phases/33-result-trust-recovery/33-S1-REPROCESS-EVIDENCE.md && grep -qiE "commit|canary" .planning/phases/33-result-trust-recovery/33-S1-REPROCESS-EVIDENCE.md && grep -qiE "dedicated|drain|isolation" .planning/phases/33-result-trust-recovery/33-S1-REPROCESS-EVIDENCE.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - /health canary confirms the deployed commit SHA + green model-init canary + PR=1/deterministic=1 (recorded body), not just 200 (concern 6).
    - Traffic isolation recorded: dedicated eval Pod (Lambda NOT resynced) OR a documented Lambda drain window (concern 14).
    - 무엇을 열어서 확인했는가 (D-19): the /health canary body is pasted, not "Pod started."
  </acceptance_criteria>
  <done>Pod up at the pinned commit, canary-confirmed, production isolated — ready to reprocess.</done>
</task>

<task type="auto">
  <name>Task 3: reprocess 11 refs @9fps to candidate versions (run1+run2) + manifest rows + detect_inversion vs risk table + ref-combo variance</name>
  <read_first>
    - backend/scripts/reprocess_reference_motions_phase4.py (33-17 added --version + refuse-overwrite + candidate!=active; :418 --motions, :424 --target-fps default 18.0, :435 --no-flip)
    - backend/scripts/release_manifest.py (create a manifest row per candidate — candidate id + per-doc hashes + commit + fps + PR/deterministic flags, 33-18)
    - .planning/debug/ref-student-substrate-gap.md `## 인계` Task 1 (flags + env) + `## 재처리 위험도 표` (R-1 the 6-of-11 inversion prediction; R-2 power-spin/peter-pan must NOT newly detect; R-4 ref-combo variance)
    - MEMORY reference-library-phase4-all11 + reference-v1-pinned-force-config
  </read_first>
  <action>
    Run `reprocess_reference_motions_phase4.py --motions <11 ids> --target-fps 9.0 --no-flip --version phase33-cm3-run1` with env PR_INVERSION_ENABLED=1 RTMW_DETERMINISTIC=1, SERIALLY, then a second run `--version phase33-cm3-run2`. This writes reference/{id}/versions/phase33-cm3-run1 (and run2) — NEVER versions/phase4_v1 (33-17 refuse-overwrite + candidate!=active enforce this). --target-fps 9.0 is the M1 fix. --no-flip keeps the active pointer on the old phase4_v1 (flip is 33-07). Create a release_manifest.py row for each candidate (candidate id, per-doc hashes of the versioned docs, the deployed commit SHA, targetFps=9.0, prInversionEnabled=true, rtmwDeterministic=true, derivedFieldSchemaVersion). Collect the detect_inversion first-inference logs and compare which of the 11 fire vs the risk table's predicted 6 (R-1); verify R-2 (power-spin/peter-pan do NOT newly cross run≥5 at 9fps); record ref-combo's run1-vs-run2 angle variance (R-4) within P99_EPSILON_DEG. Do NOT refit any threshold (D-20/D-29); do NOT flip (D-31). Record everything + the candidate ids + manifest rows in 33-S1-REPROCESS-EVIDENCE.md.
  </action>
  <verify>
    <automated>grep -qE "phase33-cm3" .planning/phases/33-result-trust-recovery/33-S1-REPROCESS-EVIDENCE.md && grep -qiE "detect_inversion|inversion" .planning/phases/33-result-trust-recovery/33-S1-REPROCESS-EVIDENCE.md && grep -qiE "combo|variance" .planning/phases/33-result-trust-recovery/33-S1-REPROCESS-EVIDENCE.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All 11 written to versions/phase33-cm3-run1 (+ run2); NEVER versions/phase4_v1; active pointer unchanged (evidence shows the old activeVersion top-level still phase4_v1); refuse-overwrite + candidate!=active honored (codex concern 1).
    - A release manifest row exists per candidate (candidate id + per-doc hashes + commit + fps + PR/deterministic flags).
    - detect_inversion logs vs risk table (6-of-11 confirmed or discrepancy explained, R-1); power-spin/peter-pan NOT newly inverted (R-2); ref-combo run1-vs-run2 variance within P99_EPSILON_DEG (R-4).
    - No threshold refit (D-20/D-29): only pose re-extraction + PR env changed.
    - 틀리면 걸리는 장치 (D-18): a mispredicted inversion or a combo variance > epsilon is a rollback trigger; the doc states which motions fired and whether any R-2/R-4 tripwire triggered.
    - 무엇을 열어서 확인했는가 (D-19): actual detect_inversion log lines + run1-vs-run2 combo deltas + the manifest rows are pasted, not "reprocess succeeded."
  </acceptance_criteria>
  <done>11 refs reprocessed @9fps to immutable candidate versions (run1+run2) with manifest rows, inversion logs confirm the prediction, ref-combo variance within epsilon, active pointer untouched, no refit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Pod (GPU) → reference/{id}/versions/{candidate} | candidate write; active pointer (phase4_v1) untouched (33-17 asserts candidate!=active) |
| Pod deploy → pinned commit | /health canary proves source/model parity before data touch |
| production Lambda | must be a dedicated eval Pod or drained — no concurrent analyses during re-extraction |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-24 | Tampering | candidate write clobbers active phase4_v1 | mitigate | 33-17 candidate-id + refuse-overwrite + candidate!=active (concern 1) |
| T-33-25 | Spoofing | Pod runs stale code | mitigate | /health commit SHA + model-init canary before reprocess (concerns 5/6) |
| T-33-26 | Tampering | production traffic contaminates serial eval | mitigate | dedicated eval Pod OR drained Lambda (concern 14) |
| T-33-27 | Tampering | non-deterministic ref-combo extraction | mitigate | RTMW_DETERMINISTIC=1 + run1-vs-run2 variance vs P99_EPSILON_DEG (R-4) |
| T-33-28 | Tampering | false inversion detection destroys power-spin | mitigate | R-2 check: power-spin/peter-pan stay below run≥5 at 9fps |
| T-33-SC | Tampering | package installs on Pod | accept | existing lean bootstrap; no new packages |
</threat_model>

<verification>
- 11 refs reprocessed @9fps to versions/phase33-cm3-run1 (+run2), never phase4_v1; active pointer unchanged.
- /health canary confirmed pinned commit + model parity; traffic isolated (dedicated Pod or drained Lambda).
- Manifest row per candidate; detect_inversion vs risk table (6-of-11); ref-combo run1-vs-run2 variance within epsilon; no refit (D-20/D-29).
</verification>

<success_criteria>
All 11 reference motions are re-extracted at 9fps with PR correction into immutable candidate versions on provably-pinned code with production isolated, the inversion prediction is confirmed against real logs, ref-combo determinism is proven, and a release-manifest row binds each candidate — with the active pointer untouched and scoring unchanged (codex concerns 1/5/6/14).
</success_criteria>

<output>
Create `.planning/phases/33-result-trust-recovery/33-03-SUMMARY.md` when done, with the candidate ids, the /health canary body, the traffic-isolation mode, the detect_inversion-vs-risk-table comparison, the ref-combo run1-vs-run2 variance, the manifest rows, and an explicit "active pointer NOT flipped" statement.
</output>
