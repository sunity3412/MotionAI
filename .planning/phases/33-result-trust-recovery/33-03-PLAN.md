---
phase: 33-result-trust-recovery
plan: 03
type: execute
wave: 3
depends_on: ["33-02"]
files_modified:
  - .planning/phases/33-result-trust-recovery/33-S1-REPROCESS-EVIDENCE.md
autonomous: false
requirements: [D-18, D-19, D-20, D-25, D-26, D-27, D-29, D-30, D-31]
user_setup: []
must_haves:
  truths:
    - "All 11 reference motions are reprocessed at --target-fps 9.0 --no-flip (versioned write only, active pointer NOT flipped) — M1 density mismatch resolved at the reference side (SEED Task 1 / D-27)"
    - "Reprocess runs with env PR_INVERSION_ENABLED=1 (M6) and RTMW_DETERMINISTIC=1 (R-4); detect_inversion logs are collected and compared to the 재처리 위험도 표 (R-1) — the 6-of-11 inversion prediction is confirmed against real logs, not the proxy"
    - "Reprocess is run TWICE and ref-combo run-to-run variance is recorded (R-4, 931-frame clip, prior 23.43°→0.193° history)"
    - "The backup integrity gate (33-02) is PASS before this plan starts; if not, reprocess does not run (D-31)"
    - "No scoring/threshold change — reprocess only re-extracts pose at 9fps with PR on; tol 20°/slope 1.2/cap 90/epsilons untouched (D-20/D-29)"
  artifacts:
    - path: ".planning/phases/33-result-trust-recovery/33-S1-REPROCESS-EVIDENCE.md"
      provides: "detect_inversion logs vs risk table + ref-combo 2-run variance + versioned-write confirmation (no flip)"
      contains: "detect_inversion"
  key_links:
    - from: "backend/scripts/reprocess_reference_motions_phase4.py --motions <11> --target-fps 9.0 --no-flip"
      to: "reference/{id}/versions/phase4_v1 (versioned write, activeVersion NOT changed)"
      via: "--no-flip keeps active pointer; _flip_active_pointer skipped"
      pattern: "no-flip|versions"
    - from: "detect_inversion logs"
      to: ".planning/debug/ref-student-substrate-gap.md 재처리 위험도 표 (R-1..R-6)"
      via: "6-of-11 inversion prediction confirmed against real first-inference logs"
      pattern: "detect_inversion|inversion"
---

<objective>
Task 1 of the C+M3 substrate track (SEED `## 인계` Task 1): reprocess all 11 reference motions at 9.0 fps with PR inversion correction on, writing versioned docs only (--no-flip). This resolves M1 (target_fps 18 vs 9 density mismatch) at the reference side and M6 (reference PR uncorrected) for the 6 inversion motions. Pod required (all Pods terminated — D-30); the plan is autonomous:false and documents the reboot precondition. detect_inversion logs are collected to confirm the proxy risk table (R-1), and the run is repeated 2x to record ref-combo variance (R-4).

Purpose: The reference substrate is the root of the A-0 "어긋남 큼" verdict — mode1 score is dominated by ref↔student extraction-substrate difference. This plan aligns the reference density and inversion handling to the student path. No scoring math changes (D-20/D-29) — this is pose re-extraction.

Output: 33-S1-REPROCESS-EVIDENCE.md (detect_inversion logs vs risk table + ref-combo 2-run variance) + versioned reference docs (active pointer NOT flipped — flip is Task 5/33-07).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-result-trust-recovery/33-CONTEXT.md
@.planning/debug/ref-student-substrate-gap.md
@backend/scripts/reprocess_reference_motions_phase4.py
@backend/scripts/measure_reference_fps.py
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: belle GPU greenlight — Pod reboot request (D-30 / D-32ⓐ ops exception)</name>
  <action>
    PAUSE for belle's operational greenlight to spin up a GPU Pod. All Pods are terminated (D-25/D-30, belle 2026-07-23 — prior "volume alive" record is corrected). Request the GPU model explicitly: EU-RO-1, 4090 priority (per MEMORY runpod-eu-ro1-gpu-priority + pod-request-include-gpu-model), Network Storage Pod (next-pod-use-network-storage). This is an operational/cost greenlight (D-32 exception ⓐ), NOT a design question — it does not count against the 2 design touchpoints. Also confirm the 33-02 backup integrity gate is PASS before proceeding (D-31 — no write without backup).
  </action>
  <what-built>Nothing yet — this gate requests belle to greenlight GPU cost so Claude can reboot the Pod and run the reference reprocess.</what-built>
  <how-to-verify>
    1. Confirm you want to spend GPU/credits for the reference reprocess (2 runs, 11 motions).
    2. Confirm the 33-02 backup exists (local + S3) and its integrity gate passed.
  </how-to-verify>
  <verify>
    <automated>echo "checkpoint — belle GPU greenlight (ops, not a design touchpoint)"</automated>
  </verify>
  <resume-signal>Type "go" to greenlight the Pod reboot + reprocess, or hold.</resume-signal>
  <done>belle greenlit GPU spin-up; 33-02 backup confirmed PASS.</done>
</task>

<task type="auto">
  <name>Task 2: Pod bootstrap + start_server + VETO env + X-RunPod-Token smoke + SSM/Lambda resync</name>
  <read_first>
    - MEMORY current-pod-hbpvhedq2bu01i (bootstrap → start_server → VETO env → X-RunPod-Token smoke → SSM + Lambda RUNPOD_ANALYZE_URL resync — proxy URL changes on new Pod) + rtmw-blackwell-lean-bootstrap
    - MEMORY next-pod-use-network-storage + runpod-gpu-env (SSH/setup gotchas, Stop 금지)
    - backend/runpod_inference/server.py (start_server env: RUNPOD_AUTH_TOKEN, PR_INVERSION_ENABLED, RTMW_DETERMINISTIC, AWS/Firebase creds)
  </read_first>
  <action>
    Claude runs the Pod setup on the belle-greenlit GPU: bootstrap (rtmw lean, sm_120 JIT cache), start_server with env PR_INVERSION_ENABLED=1 (M6 resolution) and RTMW_DETERMINISTIC=1 (R-4), X-RunPod-Token /health smoke, then resync SSM Parameter Store + Lambda RUNPOD_ANALYZE_URL to the new proxy URL (proxy changes on reboot). Do NOT flip any reference yet. Record the Pod id, proxy URL, and smoke result in 33-S1-REPROCESS-EVIDENCE.md. eval/reprocess must be SERIAL (pipeline not concurrency-safe, D-30).
  </action>
  <verify>
    <automated>test -f .planning/phases/33-result-trust-recovery/33-S1-REPROCESS-EVIDENCE.md && grep -qiE "pod|proxy|health|smoke" .planning/phases/33-result-trust-recovery/33-S1-REPROCESS-EVIDENCE.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Pod rebooted with PR_INVERSION_ENABLED=1 + RTMW_DETERMINISTIC=1; /health X-RunPod-Token smoke returns 200 (recorded).
    - SSM + Lambda RUNPOD_ANALYZE_URL resynced to the new proxy (recorded); production analysis path documented as not-yet-consuming new reference (still --no-flip).
    - 무엇을 열어서 확인했는가 (D-19): the smoke response + env echo are recorded, not "Pod started."
  </acceptance_criteria>
  <done>Pod up with correct env, smoke green, SSM/Lambda resynced — ready to reprocess.</done>
</task>

<task type="auto">
  <name>Task 3: reprocess 11 refs @9fps --no-flip (2 runs) + detect_inversion logs vs risk table + ref-combo variance</name>
  <read_first>
    - backend/scripts/reprocess_reference_motions_phase4.py:54 MOTION_IDS (5-subset — MUST override with --motions 11), :418 --motions, :424 --target-fps (default 18.0), :435 --no-flip, :339 _flip_active_pointer (skipped under --no-flip), :272 build_keypoint_report(fps=target_fps)
    - .planning/debug/ref-student-substrate-gap.md `## 인계` Task 1 (exact flags + env) + `## 재처리 위험도 표` (R-1..R-6, the 6-of-11 inversion prediction) + R-4 (ref-combo variance) + R-2 (power-spin/peter-pan must NOT newly detect inversion)
    - MEMORY reference-library-phase4-all11 + reference-v1-pinned-force-config
  </read_first>
  <action>
    Run `python scripts/reprocess_reference_motions_phase4.py --motions ref-climb ref-foxtop ref-foxtop-split ref-invert ref-sideway-spin ref-combo ref-elbow-twist-sister ref-kip-up ref-pdshape ref-peter-pan ref-power-spin --target-fps 9.0 --no-flip` with env PR_INVERSION_ENABLED=1 RTMW_DETERMINISTIC=1, SERIALLY, TWICE. --target-fps 9.0 is the M1 fix (align to student FfmpegFrameExtractor default). --no-flip writes versioned docs only (active pointer stays phase4_v1 top-level — flip is 33-07). Collect the `detect_inversion` first-inference logs (R-1: the proxy used stored pole_aligned coords; the real judgment is only via these logs) and compare which of the 11 fire inversion vs the risk table's predicted 6 (pdshape, foxtop-split, foxtop, elbow-twist-sister, combo, invert). Verify R-2: power-spin (run 1) and peter-pan (run 1) do NOT newly cross the run≥5 threshold at 9fps (a false inversion detection destroys power-spin tracking, boneCV 1.03→7.0). Record ref-combo's 2-run angle variance (R-4) and confirm it is within P99_EPSILON_DEG. Do NOT refit any threshold (D-20/D-29); do NOT flip (D-31). Record everything in 33-S1-REPROCESS-EVIDENCE.md.
  </action>
  <verify>
    <automated>grep -qiE "detect_inversion|inversion" .planning/phases/33-result-trust-recovery/33-S1-REPROCESS-EVIDENCE.md && grep -qiE "combo|variance|run" .planning/phases/33-result-trust-recovery/33-S1-REPROCESS-EVIDENCE.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All 11 reprocessed at 9.0 fps with versioned write; active pointer NOT flipped (grep the reference docs still show activeVersion top-level unchanged; --no-flip honored).
    - detect_inversion logs collected and tabulated against the risk table — the 6-of-11 prediction confirmed or the discrepancy explained (R-1); power-spin/peter-pan confirmed NOT newly inverted (R-2).
    - ref-combo 2-run variance recorded and within P99_EPSILON_DEG (R-4); RTMW_DETERMINISTIC=1 used.
    - No threshold refit (D-20/D-29): SUMMARY confirms tol/slope/cap/epsilons untouched; only pose re-extraction + PR env changed.
    - 틀리면 걸리는 장치 (D-18): a mispredicted inversion (e.g. power-spin newly firing) or combo variance > epsilon is caught here and is a rollback trigger — the doc states which motions fired and whether any R-2/R-4 tripwire triggered.
    - 무엇을 열어서 확인했는가 (D-19): actual detect_inversion log lines + the 2-run combo deltas are pasted, not "reprocess succeeded."
  </acceptance_criteria>
  <done>11 refs reprocessed @9fps versioned (no flip), inversion logs confirm the 6-of-11 prediction, ref-combo variance recorded within epsilon, no threshold refit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Pod (GPU) → S3/Firestore versioned reference write | reprocess writes versions/phase4_v1; active pointer untouched (--no-flip) |
| student production analysis path | still consumes OLD active reference during this plan (no flip) — production not disrupted |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-23 | Tampering | reprocess overwrites active reference prematurely | mitigate | --no-flip enforced; _flip_active_pointer skipped; flip is 33-07 after 33-06 PASS (D-31) |
| T-33-24 | Tampering | non-deterministic ref-combo extraction | mitigate | RTMW_DETERMINISTIC=1 + 2-run variance vs P99_EPSILON_DEG (R-4) |
| T-33-25 | Tampering | false inversion detection destroys power-spin | mitigate | R-2 check: power-spin/peter-pan must stay below run≥5 at 9fps; tripwire = rollback |
| T-33-26 | Tampering | Pod env drift / stale RUNPOD_ANALYZE_URL | mitigate | X-RunPod-Token smoke + SSM/Lambda resync before reprocess (D-30) |
| T-33-SC | Tampering | package installs on Pod bootstrap | accept | existing rtmw lean bootstrap runbook; no new packages |
</threat_model>

<verification>
- 11 refs reprocessed @9fps, versioned, active pointer NOT flipped (--no-flip).
- detect_inversion logs vs risk table (6-of-11 confirmed); power-spin/peter-pan not newly inverted.
- ref-combo 2-run variance within P99_EPSILON_DEG; no threshold refit (D-20/D-29).
</verification>

<success_criteria>
All 11 reference motions are re-extracted at 9fps with PR inversion correction, written as versioned docs (no active flip), the inversion risk table is confirmed against real detect_inversion logs, and ref-combo determinism is proven — the reference density/inversion substrate is aligned to the student path with scoring untouched.
</success_criteria>

<output>
Create `.planning/phases/33-result-trust-recovery/33-03-SUMMARY.md` when done, with the detect_inversion-vs-risk-table comparison, the ref-combo 2-run variance, and an explicit "active pointer NOT flipped" statement.
</output>
