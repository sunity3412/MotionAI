---
phase: 33-result-trust-recovery
plan: 04
type: execute
wave: 4
depends_on: ["33-01", "33-02"]
files_modified:
  - .planning/phases/33-result-trust-recovery/33-A3-ZOOM-DESIGN.md
autonomous: true
requirements: [D-07, D-12, D-18, D-19, D-24]
must_haves:
  truths:
    - "The 6 judgment criteria (D-07) are fixed in writing BEFORE any option is drawn — no taste debate (D-24)"
    - "Options are grounded in three evidence strands: (a) domain (how a pole instructor points out posture), (b) prior-art comparison idioms, (c) backend data limits — an option that draws what the backend cannot give is invalid (D-12/D-24)"
    - "The seam #1 fix location is decided with rationale: backend criterion-keyed crops (Pod regen) vs app joint-exact join (OTA-only) — not left ambiguous for A-5"
    - "2-3 surviving options only; the design doc records why each was kept or dropped against D-07 (D-18)"
  artifacts:
    - path: ".planning/phases/33-result-trust-recovery/33-A3-ZOOM-DESIGN.md"
      provides: "fixed D-07 criteria + 3-strand rationale + 2-3 options + seam #1 fix-location decision"
      contains: "판정 기준"
  key_links:
    - from: "33-A3-ZOOM-DESIGN.md seam decision"
      to: "backend fault_zoom.py OR app deductionLabels.ts/result.tsx:1215"
      via: "resolves RESEARCH Open Q2 before A-5 implements"
      pattern: "criterion-keyed|joint-exact"
    - from: "33-A3-ZOOM-DESIGN.md data-limit strand"
      to: "fault_zoom.py crop/frame capability + coordinate confidence"
      via: "options constrained to deliverable backend output"
      pattern: "fault_zoom|confidence"
---

<objective>
Investigate the 확대비교 (fault-zoom) redesign before any implementation (D-24). First fix the 6 judgment criteria (D-07) in writing so the design is not a taste debate. Then gather three evidence strands: (a) how pole instructors actually point out posture (domain, from A-1 + NotebookLM/field research), (b) prior-art posture-comparison idioms (side-by-side / semi-transparent overlay / slider wipe / ghost silhouette / angle arc / trajectory line) with what each shows well and poorly, (c) the backend's actual data limits (coordinate accuracy, missing spans, crop range, temporal-alignment precision). Narrow to 2-3 options, and decide where seam #1 (item↔crop mismatch) gets fixed: backend criterion-keyed crops (Pod regen) vs app joint-exact join (OTA-only), resolving RESEARCH Open Q2 with rationale.

Purpose: The current 확대비교 broke because it was built without this investigation (D-24). Fixing the criteria and the data limits first prevents drawing a mockup the backend cannot deliver ("못 주는 것을 그린 목업은 무효").

Output: 33-A3-ZOOM-DESIGN.md — fixed criteria + 3-strand rationale + 2-3 options + seam decision.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-result-trust-recovery/33-A1-MOTION-STANDARDS.md
@.planning/phases/33-result-trust-recovery/33-A0-EVIDENCE.md
@.planning/phases/33-result-trust-recovery/33-RESEARCH.md
@.planning/phases/33-result-trust-recovery/33-PLANNING-APPROACH.md
@backend/shared/python/sunity_shared/analysis/fault_zoom.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: fix D-07 criteria + three evidence strands (domain / prior-art / data-limit)</name>
  <read_first>
    - 33-CONTEXT.md D-07 (the 6 criteria verbatim) + D-08 (worst-case data list)
    - 33-SEED.md section 표기 설계 조사 방법 (단계 1-3: 판정기준 → 근거 3갈래)
    - 33-A1-MOTION-STANDARDS.md (query 4: 강사가 어디를 보라 하는가 — the domain strand)
    - backend/shared/python/sunity_shared/analysis/fault_zoom.py (_side_crop 3-tier, _to_rep_idx:195 fps spaces, _mark:606 baked badge) — the data-limit strand
    - 33-RESEARCH.md Defect-8 root-cause map + Pitfall 2 (fps mismatch) + Pitfall 4 (region-first join)
  </read_first>
  <action>
    Write 33-A3-ZOOM-DESIGN.md. Section 1: restate the 6 D-07 criteria verbatim as the fixed pass line (fix before options, per D-24). Section 2 (a) domain: from A-1 query-4, list what body-part/moment an instructor points at per motion, as screen metaphors (mirror / hand-pointing / side-by-side / demo) — the app copies the studio idiom, invents none. Section 2 (b) prior-art: tabulate the comparison idioms (side-by-side, semi-transparent overlay, slider wipe, ghost silhouette, angle arc, trajectory) with strengths/weaknesses for "same-moment/same-scale posture diff." Section 2 (c) data-limit: from fault_zoom.py, state concretely what the backend can and cannot deliver — crop bbox reliability under low keypoint confidence, the three fps spaces (9fps frames / 18fps ref keypointReport / mode3 upsample) that must route through _to_rep_idx, DTW match-failure fallback behavior, and coordinate-missing spans. Mark any idiom that requires data the backend cannot give as INVALID. Honor D-05: options must reduce or align, not add labels.
  </action>
  <verify>
    <automated>test -f .planning/phases/33-result-trust-recovery/33-A3-ZOOM-DESIGN.md && grep -qE "판정 기준|D-07" .planning/phases/33-result-trust-recovery/33-A3-ZOOM-DESIGN.md && grep -qE "fps|confidence" .planning/phases/33-result-trust-recovery/33-A3-ZOOM-DESIGN.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The 6 D-07 criteria are written verbatim as the fixed pass line before any option appears (D-24 ordering).
    - The data-limit strand cites concrete fault_zoom.py constraints (fps spaces, confidence gating, DTW fallback) — not generic caveats; at least one idiom is marked INVALID for exceeding backend capability.
    - 틀리면 걸리는 장치 (D-18): the data-limit section is the device — an option drawing un-deliverable data is caught here, before the mockup.
    - 무엇을 열어서 확인했는가 (D-19): the task cites the specific fault_zoom.py functions read to establish the limits.
  </acceptance_criteria>
  <done>Criteria fixed and three evidence strands documented with at least one INVALID idiom flagged.</done>
</task>

<task type="auto">
  <name>Task 2: narrow to 2-3 options + decide seam #1 fix location</name>
  <read_first>
    - 33-RESEARCH.md Open Questions Q2 (backend criterion-keyed crops vs app joint-exact join) + Pattern 1/2 (seam #1 mechanics)
    - 33-A0-EVIDENCE.md (whether crops are built from the wrong joint set — informs backend vs app fix)
    - app/src/lib/deductionLabels.ts:221-240 (projectDeductionRecordKeypoints, source==='vision' branch) + app/src/app/analysis/result.tsx:1215 (selectedZoom first-match)
  </read_first>
  <action>
    Extend 33-A3-ZOOM-DESIGN.md. Section 3: present 2-3 concrete options (e.g. side-by-side same-scale vs semi-transparent overlay), each drawn on the SAME real data + the D-08 worst cases, noting the split points (나란히 vs 겹침 / 크기 기준 몸·관절·폴 / 차이 표시 원·화살표·각도호·유령). Section 4: decide where seam #1 gets fixed — backend criterion-keyed crops (removes mismatch at source, requires Pod regen per D-25) vs app joint-exact join (OTA-only, but only as good as the joint the crop carries). Use the A-0 evidence: if crops are built from the wrong joint set, prefer the backend fix; else the app join. State the decision and rationale unambiguously so A-5 implements without re-deciding. Section 5: for each option, pre-record the expected D-07 verdict (this is Claude's self-filter before belle).
  </action>
  <verify>
    <automated>grep -qE "criterion-keyed|joint-exact" .planning/phases/33-result-trust-recovery/33-A3-ZOOM-DESIGN.md && grep -cE "안 ?[0-9]|Option" .planning/phases/33-result-trust-recovery/33-A3-ZOOM-DESIGN.md</automated>
  </verify>
  <acceptance_criteria>
    - 2-3 options are recorded, each on the same real data + worst cases (D-08), not good-case-only.
    - The seam #1 fix location (backend vs app) is decided WITH rationale citing A-0 evidence — A-5 does not re-open this question.
    - Each option carries a pre-recorded D-07 self-verdict (survivors only will go to the A-4 mockup).
    - 이 산출물이 틀렸다면 어떻게 알았을까: answered — an option that fails D-07 or exceeds data limits is dropped here, in writing, before the mockup.
  </acceptance_criteria>
  <done>2-3 D-07-surviving options + an unambiguous seam #1 fix-location decision recorded for A-4/A-5.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (documentation-only) | design rationale doc; no code, no runtime |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-06 | Tampering | option vs backend capability | mitigate | data-limit strand flags un-deliverable idioms INVALID before mockup (D-12/D-24) |
| T-33-SC | Tampering | package installs | accept | no installs (documentation) |
</threat_model>

<verification>
- 33-A3-ZOOM-DESIGN.md exists with fixed D-07 criteria, 3 evidence strands, 2-3 options, and a seam #1 decision.
- At least one idiom marked INVALID for data limits; seam fix-location decided with rationale.
</verification>

<success_criteria>
The 확대비교 redesign is investigated and narrowed to 2-3 deliverable, D-07-surviving options with the seam #1 fix location decided — ready for the A-4 mockup, no implementation started (D-24).
</success_criteria>

<output>
Create `.planning/phases/33-result-trust-recovery/33-04-SUMMARY.md` when done.
</output>
