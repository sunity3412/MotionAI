---
phase: 33-result-trust-recovery
plan: 04
type: execute
wave: 4
depends_on: ["33-03"]
files_modified:
  - backend/scripts/backfill_reference_downstream.py
  - backend/scripts/extract_reference_keypoint_reports.py
  - .planning/phases/33-result-trust-recovery/33-S2-BACKFILL-EVIDENCE.md
autonomous: false
requirements: [D-18, D-19, D-20, D-25, D-27, D-29, D-30]
user_setup: []
must_haves:
  truths:
    - "The warm Pod from 33-03 is confirmed live (X-RunPod-Token /health 200) BEFORE backfill runs — bodyNormalizationProfile/forceDirectionPattern derive from live RTMW pose_frames (compute_reference_downstream:285-289: measure_body_profile needs raw keypoints_3d not in flat phase4_v1), so a cold Pod cannot silently no-op; if cold, HALT and request belle GPU greenlight (D-30/D-32ⓐ), do not proceed (D-18: no silent failure)"
    - "All fields derived from the new angles are re-derived so nothing is stale: meanAngles, techniqueProfile, bodyNormalizationProfile, forceDirectionPattern, keypointReport, referenceKeypointReport, bodyComparisonSourcePose (SEED Task 2 / D-27)"
    - "REFERENCE_TARGET_FPS is changed 18.0→9.0 in backfill_reference_downstream.py so the stored-vs-rerun angle gate does not abort on frame-count mismatch; keypointReport.fps label updates 18.0→9.0 for 11/11 (SEED Task 2)"
    - "REFERENCE_V1_FORCE_CONFIG (pinned) is KEPT — not this change's axis; MEAN_EPSILON_DEG=0.1 / P99_EPSILON_DEG=1.0 are NOT refit — a gate trip is investigated, not thresholded away (SEED Task 2 / D-29)"
    - "bodyComparisonSourcePose is present on all 11 docs after backfill — a missing one breaks the comparison screen (SEED Task 2 table)"
    - "No scoring math touched (D-20)"
  artifacts:
    - path: "backend/scripts/backfill_reference_downstream.py"
      provides: "downstream re-derivation with REFERENCE_TARGET_FPS 9.0; epsilons unchanged"
      contains: "REFERENCE_TARGET_FPS"
    - path: ".planning/phases/33-result-trust-recovery/33-S2-BACKFILL-EVIDENCE.md"
      provides: "warm-Pod health confirmation + per-doc downstream field values dumped + integrity-gate PASS + fps label 9.0 confirmation"
      contains: "keypointReport"
  key_links:
    - from: "backend/scripts/backfill_reference_downstream.py REFERENCE_TARGET_FPS"
      to: "stored-vs-rerun angle gate (MEAN_EPSILON_DEG / P99_EPSILON_DEG)"
      via: "fps 9.0 keeps frame-count parity so gate does not abort; epsilons untouched"
      pattern: "REFERENCE_TARGET_FPS|EPSILON"
    - from: "33-03 warm Pod (RUNPOD_ANALYZE_URL) /health"
      to: "compute_reference_downstream live pose_frames (bodyNormalizationProfile/forceDirectionPattern)"
      via: "warm-Pod health gate precedes backfill; cold → belle greenlight (D-30)"
      pattern: "health|RUNPOD_ANALYZE_URL|pose_frames"
    - from: "extract_reference_keypoint_reports.py"
      to: "reference keypointReport.fps label"
      via: "fps label 18.0→9.0 on 11/11"
      pattern: "keypointReport|fps"
---

<objective>
Task 2 of the C+M3 substrate track (SEED Task 2): re-derive every downstream field that the new 9fps angles made stale. Task 1 (33-03) changed angles/joints3d; meanAngles, techniqueProfile, bodyNormalizationProfile, forceDirectionPattern, keypointReport, referenceKeypointReport, and bodyComparisonSourcePose are all now stale and must be recomputed from the new source. Change REFERENCE_TARGET_FPS 18.0→9.0 (line 138) so the stored-vs-rerun angle gate keeps frame-count parity, and update the keypointReport fps label 18.0→9.0. Keep REFERENCE_V1_FORCE_CONFIG (pinned) and do NOT refit the gate epsilons.

Explicit GPU precondition (WARNING 2 fix): bodyNormalizationProfile and forceDirectionPattern do NOT derive from stored flat phase4_v1 — `compute_reference_downstream` (:285-289) calls `measure_body_profile(pose_frames)` and `infer_force_direction_pattern` on LIVE RTMW pose_frames (raw keypoints_3d + confidence, absent from the flat store). So this plan has a hard warm-Pod dependency on the 33-03 Pod session. This plan is therefore autonomous:false: it first confirms the 33-03 Pod is warm (X-RunPod-Token /health 200); if the Pod was stopped between 33-03 and 33-04, it HALTS and requests belle's GPU greenlight (D-30/D-32ⓐ ops exception) rather than failing silently or emitting NaN. meanAngles/techniqueProfile derive from STORED angles (CPU) and do not need the Pod. No scoring change (D-20/D-29).

Output: warm-Pod health confirmation + backfill with REFERENCE_TARGET_FPS=9.0 + 33-S2-BACKFILL-EVIDENCE.md (per-doc field dump + gate PASS + fps label 9.0).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-result-trust-recovery/33-CONTEXT.md
@.planning/debug/ref-student-substrate-gap.md
@.planning/phases/33-result-trust-recovery/33-03-SUMMARY.md
@backend/scripts/backfill_reference_downstream.py
@backend/scripts/extract_reference_keypoint_reports.py
</context>

<tasks>

<task type="auto">
  <name>Task 0: warm-Pod precondition gate — confirm 33-03 Pod live before backfill (D-30)</name>
  <read_first>
    - 33-03-SUMMARY.md (Pod id, proxy URL, X-RunPod-Token /health smoke result — the warm session this plan depends on)
    - backend/scripts/backfill_reference_downstream.py:242-289 compute_reference_downstream (bodyNormalizationProfile/forceDirectionPattern from live pose_frames — the reason a warm Pod is required; meanAngles/techniqueProfile are CPU-from-STORED and do NOT need the Pod)
    - MEMORY current-pod-hbpvhedq2bu01i (X-RunPod-Token /health smoke + RUNPOD_ANALYZE_URL) + 33-CONTEXT.md D-30 (Pod all terminated → reboot procedure) + D-32ⓐ (Pod greenlight is an ops exception, not a design touchpoint)
  </read_first>
  <action>
    Before touching the backfill, confirm the 33-03 Pod is still warm: read the proxy URL from 33-03-SUMMARY.md and hit /health with the X-RunPod-Token header, expecting 200. If warm → proceed to Task 1 autonomously (the 33-03 greenlight already covers this session). If the Pod is COLD/stopped (health fails or URL stale), HALT: do NOT run the backfill (it would NaN or no-op the live-pose fields silently). Request belle's GPU greenlight (D-30/D-32ⓐ ops exception — EU-RO-1 4090 priority, Network Storage), then reboot + resync SSM/Lambda RUNPOD_ANALYZE_URL per the 33-03 Task 2 procedure, re-smoke /health, and only then proceed. Record the /health response (warm or the reboot trail) in 33-S2-BACKFILL-EVIDENCE.md. This is the explicit precondition that replaces the silent-failure risk (D-18).
  </action>
  <verify>
    <automated>test -f .planning/phases/33-result-trust-recovery/33-S2-BACKFILL-EVIDENCE.md && grep -qiE "health|warm|pod|proxy" .planning/phases/33-result-trust-recovery/33-S2-BACKFILL-EVIDENCE.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The 33-03 Pod /health X-RunPod-Token smoke is recorded as 200 (warm), OR a documented reboot+resync trail is recorded (cold path, belle greenlit per D-30).
    - The backfill did NOT start against a cold Pod (no silent NaN/no-op on bodyNormalizationProfile/forceDirectionPattern) — D-18.
    - 무엇을 열어서 확인했는가 (D-19): the actual /health response body/status is pasted, not "Pod assumed warm."
  </acceptance_criteria>
  <done>Warm-Pod confirmed live (or rebooted+resynced with belle greenlight); the live-pose dependency is explicit, not silent.</done>
</task>

<task type="auto">
  <name>Task 1: set REFERENCE_TARGET_FPS 18→9 + keypointReport fps label + run backfill for 11 refs</name>
  <read_first>
    - backend/scripts/backfill_reference_downstream.py:138 REFERENCE_TARGET_FPS=18.0, :127-128 MEAN_EPSILON_DEG=0.1/P99_EPSILON_DEG=1.0 (do NOT refit), :107 REFERENCE_V1_FORCE_CONFIG (keep pinned), :242-289 downstream derivation (meanAngles/techniqueProfile from STORED angles; bodyNormalizationProfile/forceDirectionPattern from live pose_frames — requires the warm Pod confirmed in Task 0)
    - backend/scripts/extract_reference_keypoint_reports.py (keypointReport/referenceKeypointReport + fps label — currently 18.0 on 11/11)
    - .planning/debug/ref-student-substrate-gap.md `## 인계` Task 2 table (7 fields + source per field + REFERENCE_TARGET_FPS 동반 수정 + keep REFERENCE_V1_FORCE_CONFIG + do NOT refit epsilons + bodyComparisonSourcePose 재확인)
  </read_first>
  <action>
    (Precondition: Task 0 confirmed the warm Pod.) Change REFERENCE_TARGET_FPS 18.0→9.0 in backfill_reference_downstream.py:138 (so the stored-vs-rerun angle gate keeps frame-count parity and does not abort). Keep REFERENCE_V1_FORCE_CONFIG and the epsilons (0.1/1.0) exactly (D-29 — a gate trip is investigated, not thresholded away). Run backfill for all 11 reference ids to re-derive: meanAngles (nanmean of NEW stored angles), techniqueProfile (FallbackRecognizer on new angles), bodyNormalizationProfile (measure_body_profile on live pose_frames — needs the warm Pod), forceDirectionPattern (infer_force_direction_pattern on live frames — needs the warm Pod), keypointReport + referenceKeypointReport (new frames + fps label 9.0 via extract_reference_keypoint_reports.py), and confirm/refresh bodyComparisonSourcePose on all 11 (a missing one breaks the comparison screen). Do NOT touch scoring math (D-20).
  </action>
  <verify>
    <automated>cd backend && grep -qE "REFERENCE_TARGET_FPS\s*=\s*9\.0" scripts/backfill_reference_downstream.py && grep -qE "MEAN_EPSILON_DEG\s*=\s*0\.1" scripts/backfill_reference_downstream.py && grep -qE "P99_EPSILON_DEG\s*=\s*1\.0" scripts/backfill_reference_downstream.py && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - REFERENCE_TARGET_FPS == 9.0; MEAN_EPSILON_DEG still 0.1 and P99_EPSILON_DEG still 1.0 (grep — no refit, D-29); REFERENCE_V1_FORCE_CONFIG unchanged (git diff shows the constant untouched).
    - Backfill ran for all 11 refs against the warm Pod; the 7 downstream fields are re-derived from the new source (not the stale 18fps angles); bodyNormalizationProfile/forceDirectionPattern are non-NaN (proves the live Pod was used, not a cold no-op).
    - keypointReport.fps label updates 18.0→9.0 for the reprocessed docs.
    - No scoring file changed (git diff limited to scripts/backfill + scripts/extract + evidence doc), D-20.
  </acceptance_criteria>
  <done>Downstream fields re-derived at 9fps against the warm Pod with epsilons and force-config pinned; keypointReport fps label = 9.0.</done>
</task>

<task type="auto">
  <name>Task 2: prove integrity gate PASS without refit + dump downstream field values (open the artifact)</name>
  <read_first>
    - backend/scripts/backfill_reference_downstream.py:120-138 (integrity gate: meanAngleDelta > MEAN_EPSILON_DEG OR p99AngleDelta > P99_EPSILON_DEG → abort)
    - .planning/debug/ref-student-substrate-gap.md R-4 (ref-combo determinism) + Task 2 note (gate 걸리면 임계 올리지 말고 원인 조사)
  </read_first>
  <action>
    Run the backfill integrity gate and confirm it PASSES for all 11 with the epsilons unchanged. If it aborts on any doc, do NOT raise the epsilon — investigate the cause (record it, and if it is a genuine substrate divergence beyond epsilon this is a rollback signal per D-31). Then OPEN the artifact (D-19): dump each doc's meanAngles summary, techniqueProfile, bodyNormalizationProfile presence (and non-NaN — proves the warm Pod produced it), forceDirectionPattern, keypointReport.fps (must read 9.0), referenceKeypointReport presence, and bodyComparisonSourcePose presence — 11/11. Record the meanAngleDelta / p99AngleDelta per doc to show they are within epsilon. Write 33-S2-BACKFILL-EVIDENCE.md with the per-doc table.
  </action>
  <verify>
    <automated>test -f .planning/phases/33-result-trust-recovery/33-S2-BACKFILL-EVIDENCE.md && grep -qE "keypointReport|fps|9\.0" .planning/phases/33-result-trust-recovery/33-S2-BACKFILL-EVIDENCE.md && grep -qiE "bodyComparisonSourcePose|meanAngleDelta" .planning/phases/33-result-trust-recovery/33-S2-BACKFILL-EVIDENCE.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Integrity gate PASS for 11/11 with epsilons unchanged; any near-miss delta recorded (D-29 — no refit).
    - 무엇을 열어서 확인했는가 (D-19): per-doc downstream field VALUES dumped (meanAngles/technique/bodyNorm(non-NaN)/force/keypointReport.fps=9.0/referenceKeypointReport/bodyComparisonSourcePose) — not "backfill succeeded"; bodyComparisonSourcePose present on 11/11.
    - 이 산출물이 틀렸다면 어떻게 알았을까: answered — a stale field (still 18fps-derived), a NaN bodyNorm (cold-Pod no-op), or a missing bodyComparisonSourcePose shows up in the opened dump; a divergence beyond epsilon aborts the gate.
  </acceptance_criteria>
  <done>Gate PASS without refit; every downstream field opened and confirmed at 9fps; bodyNorm non-NaN; bodyComparisonSourcePose present 11/11.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| 33-03 warm Pod → live pose_frames | bodyNormalizationProfile/forceDirectionPattern require a warm RTMW session; a cold Pod would silently NaN |
| new stored angles → derived reference fields | downstream re-derivation writes versioned reference fields; active pointer still not flipped |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-27 | Tampering | stale downstream field (18fps-derived) shipped with 9fps angles | mitigate | re-derive all 7 fields from new source; open-value dump catches a stale field (D-19) |
| T-33-28 | Tampering | epsilon refit to force a gate pass | mitigate | epsilons pinned; gate trip → investigate/rollback, never raise threshold (D-29/D-31) |
| T-33-29 | Denial of service | missing bodyComparisonSourcePose breaks compare screen | mitigate | presence check on 11/11 in the opened dump |
| T-33-30 | Denial of service | cold Pod between 33-03 and 33-04 → silent NaN bodyNorm/force | mitigate | Task 0 warm-Pod /health gate; cold → HALT + belle greenlight (D-30); non-NaN check in dump |
| T-33-SC | Tampering | package installs | accept | no new packages (reuse existing backfill/extract scripts) |
</threat_model>

<verification>
- 33-03 Pod confirmed warm (/health 200) before backfill, or rebooted+resynced with belle greenlight (D-30).
- REFERENCE_TARGET_FPS=9.0; epsilons 0.1/1.0 unchanged; REFERENCE_V1_FORCE_CONFIG kept.
- 7 downstream fields re-derived; bodyNorm/force non-NaN (warm Pod used); keypointReport.fps=9.0; bodyComparisonSourcePose on 11/11.
- Integrity gate PASS without refit; per-doc deltas within epsilon (opened dump).
</verification>

<success_criteria>
The reprocessed reference is internally consistent at 9fps — the warm Pod confirmed live before the live-pose fields are derived, every derived field recomputed from the new angles, keypointReport labeled 9.0, bodyComparisonSourcePose intact — with the gate passing on unchanged epsilons and scoring untouched.
</success_criteria>

<output>
Create `.planning/phases/33-result-trust-recovery/33-04-SUMMARY.md` when done, with the warm-Pod /health confirmation, the per-doc downstream-field dump, and the meanAngleDelta/p99 values proving no epsilon refit.
</output>
</output>
