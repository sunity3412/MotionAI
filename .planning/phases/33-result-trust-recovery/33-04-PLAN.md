---
phase: 33-result-trust-recovery
plan: 04
type: execute
wave: 4
depends_on: ["33-03", "33-17"]
files_modified:
  - backend/scripts/backfill_reference_downstream.py
  - backend/scripts/extract_reference_keypoint_reports.py
  - .planning/phases/33-result-trust-recovery/33-S2-BACKFILL-EVIDENCE.md
autonomous: false
requirements: [D-18, D-19, D-20, D-25, D-27, D-29, D-30]
user_setup: []
must_haves:
  truths:
    - "The backfill reads the CANDIDATE version (reference/{id}/versions/phase33-cm3-run1), NOT top-level — today backfill_reference_downstream.py reads top-level via get_reference_motion:461 and 'NEVER writes Firestore' (line 44); this plan rewrites it to source from the candidate and MERGE the derived fields back into the SAME candidate only (codex concern 2 / suggestion 3)"
    - "All fields derived from the new angles are re-derived FROM the candidate: meanAngles, techniqueProfile, bodyNormalizationProfile, forceDirectionPattern, keypointReport, referenceKeypointReport, bodyComparisonSourcePose (SEED Task 2)"
    - "bodyComparisonSourcePose has a REAL, named producer defined (not 'reconfirm') — codex flagged it as having no concrete producer; all 11 candidate docs carry it after backfill"
    - "fps is read from candidate metadata or a CLI arg — NOT the global REFERENCE_TARGET_FPS=18.0 hardcode (backfill:138); keypointReport.fps label = 9.0 on 11/11 (codex concern 2)"
    - "Task-1 (33-03) pose/keypoint output is reused where possible instead of re-inferring; a fresh RTMW pass happens only for fields that genuinely need live pose_frames (bodyNorm/force)"
    - "REFERENCE_V1_FORCE_CONFIG kept; MEAN_EPSILON_DEG=0.1 / P99_EPSILON_DEG=1.0 NOT refit — a gate trip is investigated (D-29); no scoring math (D-20)"
  artifacts:
    - path: "backend/scripts/backfill_reference_downstream.py"
      provides: "candidate-sourced re-derivation that MERGES into versions/{candidate}; fps from candidate/CLI; real bodyComparisonSourcePose producer; epsilons unchanged"
      contains: "versions"
    - path: ".planning/phases/33-result-trust-recovery/33-S2-BACKFILL-EVIDENCE.md"
      provides: "warm-Pod canary + per-candidate-doc field dump + fps=9.0 + bodyComparisonSourcePose 11/11 + gate PASS"
      contains: "bodyComparisonSourcePose"
  key_links:
    - from: "backfill_reference_downstream.py"
      to: "reference/{id}/versions/phase33-cm3-run1 (read + merge)"
      via: "candidate-sourced derivation, merge back into the SAME candidate (not top-level)"
      pattern: "versions|phase33-cm3|SUNITY_SHADOW_REFERENCE_VERSION"
    - from: "backfill fps"
      to: "candidate metadata / CLI arg (not global 18.0)"
      via: "keypointReport.fps label 9.0 on 11/11"
      pattern: "target.fps|fps|9\\.0"
---

<objective>
Task 2 of the C+M3 substrate track (SEED Task 2), rewritten per codex concern 2 / suggestion 3. The current `backfill_reference_downstream.py` cannot do what 33-04 needs: it reads TOP-LEVEL angles (`get_reference_motion`:461 — the OLD active data, not the candidate), it states "NEVER writes Firestore" (line 44) so it only emits a seed JSON, its `REFERENCE_TARGET_FPS` is hardcoded 18.0 (line 138), and it produces neither `keypointReport` nor `bodyComparisonSourcePose`. This plan rewrites it to: source from the CANDIDATE version, re-derive every stale field, MERGE them back into the SAME candidate only, define a REAL `bodyComparisonSourcePose` producer, read fps from candidate metadata/CLI, and reuse the 33-03 pose output where possible. Warm-Pod required (live pose_frames for bodyNorm/force) — the cold path is a BLOCKING checkpoint (codex concern 15). No scoring math (D-20/D-29).

Purpose: If the backfill derives from the old top-level and never persists into the candidate, 33-06 verifies an incomplete or wrong-sourced candidate (codex concern 2). This makes the candidate internally consistent at 9fps so the verification in 33-06 is meaningful.

Output: candidate-sourced backfill (merges into versions/{candidate}) + real bodyComparisonSourcePose producer + fps=9.0 + 33-S2-BACKFILL-EVIDENCE.md.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-result-trust-recovery/33-CONTEXT.md
@.planning/phases/33-result-trust-recovery/33-REVIEWS.md
@.planning/debug/ref-student-substrate-gap.md
@.planning/phases/33-result-trust-recovery/33-03-SUMMARY.md
@backend/scripts/backfill_reference_downstream.py
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 0: warm-Pod canary OR belle GPU greenlight (blocking) — no silent cold-Pod no-op (D-30 / codex concern 15)</name>
  <read_first>
    - 33-03-SUMMARY.md (Pod id, proxy URL, /health commit-canary — the warm session this plan depends on)
    - backend/scripts/backfill_reference_downstream.py:285-289 (bodyNormalizationProfile/forceDirectionPattern from live pose_frames — the reason a warm Pod is required; meanAngles/techniqueProfile derive from candidate angles, CPU)
    - 33-REVIEWS.md codex concern 6 (warm-Pod canary, not just /health 200) + concern 15 (33-04 cold path must be a blocking checkpoint, not an inline auto request)
  </read_first>
  <action>
    BLOCKING checkpoint (converted from the old inline auto-request per codex concern 15). Confirm the 33-03 Pod is warm AND running the pinned commit: hit /health (X-RunPod-Token) and verify the 33-18 canary — commit SHA == 33-03's deployed commit + model-init canary green + PR=1/deterministic=1 (a bare 200 is insufficient, concern 6). If warm+canary-green → proceed. If COLD or the canary shows a DIFFERENT commit/stale model → HALT and request belle's GPU greenlight (D-30/D-32ⓐ), reboot at the pinned commit, resync, re-canary. Do NOT run the backfill against a cold or wrong-commit Pod (it would NaN/no-op the live-pose fields silently — D-18). Record the /health canary body (warm or the reboot trail) in 33-S2-BACKFILL-EVIDENCE.md.
  </action>
  <what-built>Nothing yet — this gate confirms the warm Pod runs the pinned commit before deriving live-pose fields, or requests a reboot.</what-built>
  <how-to-verify>
    1. /health canary shows the pinned commit + green model canary + PR=1/deterministic=1.
    2. If cold/wrong-commit, approve a reboot at the pinned commit.
  </how-to-verify>
  <verify>
    <automated>test -f .planning/phases/33-result-trust-recovery/33-S2-BACKFILL-EVIDENCE.md && grep -qiE "canary|commit|health" .planning/phases/33-result-trust-recovery/33-S2-BACKFILL-EVIDENCE.md && echo OK</automated>
  </verify>
  <resume-signal>Type "go" if canary green, or approve reboot at the pinned commit.</resume-signal>
  <done>Warm Pod confirmed at the pinned commit (canary green), or rebooted+re-canaried with belle greenlight — the live-pose dependency is explicit, not silent.</done>
</task>

<task type="auto">
  <name>Task 1: rewrite backfill to source from the candidate + merge back into versions/{candidate} + fps from candidate/CLI + real bodyComparisonSourcePose producer</name>
  <read_first>
    - backend/scripts/backfill_reference_downstream.py:44 ("NEVER writes Firestore"), :138 REFERENCE_TARGET_FPS=18.0, :127-128 epsilons (do NOT refit), :107 REFERENCE_V1_FORCE_CONFIG (keep), :232-343 compute_reference_downstream (meanAngles/technique from angles arg; bodyNorm/force from live pose_frames), :456-479 _read_stored_angles (reads TOP-LEVEL get_reference_motion — change to candidate), :628-766 _run_backfill (currently emits split JSON, no Firestore write)
    - backend/shared/python/sunity_shared/firestore_admin.py get_reference_motion (33-17 added SUNITY_SHADOW_REFERENCE_VERSION overlay — use it OR a direct versions/{candidate} read) + a write path to merge fields into versions/{candidate}
    - backend/scripts/extract_reference_keypoint_reports.py (keypointReport/referenceKeypointReport + fps label — currently 18.0)
    - .planning/debug/ref-student-substrate-gap.md `## 인계` Task 2 table (7 fields + source per field + bodyComparisonSourcePose 재확인 + keep FORCE_CONFIG + do NOT refit epsilons)
    - 33-REVIEWS.md codex concern 2 + suggestion 3 (read versions/{candidate}; derive from candidate; merge back into candidate only; REAL bodyComparisonSourcePose producer; reuse Task-1 pose output; fps from candidate metadata/CLI not global hardcode)
  </read_first>
  <action>
    Rewrite the backfill so it (a) SOURCES from the candidate: read `reference/{id}/versions/{candidate}` (via SUNITY_SHADOW_REFERENCE_VERSION=phase33-cm3-run1 or a direct versions read) for the new angles/joints3d — never top-level; (b) reads fps from the candidate's stored metadata or a `--target-fps`/`--reference-version` CLI arg instead of the global `REFERENCE_TARGET_FPS=18.0` (remove/param the hardcode); (c) reuses 33-03's pose/keypoint output where possible (the candidate already holds re-extracted joints3d/keypointReport from the reprocess) and only re-infers live pose_frames for the fields that genuinely need them (bodyNormalizationProfile, forceDirectionPattern); (d) MERGES the derived fields back into the SAME candidate `versions/{candidate}` (this replaces "NEVER writes Firestore" for the candidate path — top-level and activeVersion stay untouched, flip is 33-07); (e) defines a REAL `bodyComparisonSourcePose` producer (name the exact function/derivation — e.g. the candidate's to_coco17 pose at the comparison keyframe — codex: it has no concrete producer today) and writes it on all 11; (f) updates keypointReport/referenceKeypointReport fps label to 9.0 via extract_reference_keypoint_reports.py sourced from the candidate. Keep REFERENCE_V1_FORCE_CONFIG; do NOT refit MEAN_EPSILON_DEG/P99_EPSILON_DEG (D-29). No scoring math (D-20).
  </action>
  <verify>
    <automated>cd backend && python3 -c "import ast; ast.parse(open('scripts/backfill_reference_downstream.py').read())" && grep -qiE "versions|SUNITY_SHADOW_REFERENCE_VERSION|reference-version" scripts/backfill_reference_downstream.py && grep -qiE "bodyComparisonSourcePose" scripts/backfill_reference_downstream.py && grep -qE "MEAN_EPSILON_DEG\s*=\s*0\.1" scripts/backfill_reference_downstream.py && grep -qE "P99_EPSILON_DEG\s*=\s*1\.0" scripts/backfill_reference_downstream.py && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Sources from versions/{candidate} (not top-level) and MERGES derived fields back into the SAME candidate only (grep + git diff shows the versions read/write; top-level/activeVersion untouched) — codex concern 2.
    - fps comes from candidate metadata / CLI, not the global 18.0 hardcode; keypointReport.fps label becomes 9.0.
    - A REAL bodyComparisonSourcePose producer is named and implemented; all 11 candidate docs get it (codex).
    - Epsilons unchanged (0.1/1.0), REFERENCE_V1_FORCE_CONFIG kept (grep — no refit, D-29); no scoring file changed (D-20).
    - 틀리면 걸리는 장치 (D-18): a field still derived from top-level (18fps) or a missing bodyComparisonSourcePose is caught in Task 2's opened per-candidate dump.
  </acceptance_criteria>
  <done>Backfill sources from and merges into the candidate at candidate-fps, with a real bodyComparisonSourcePose producer and epsilons pinned.</done>
</task>

<task type="auto">
  <name>Task 2: run candidate backfill (warm Pod) + integrity gate PASS w/o refit + open per-candidate field dump</name>
  <read_first>
    - backend/scripts/backfill_reference_downstream.py:519-575 (stored-vs-rerun angle gate: mean/p99 epsilons → abort; do NOT raise) + `## 인계` Task 2 note (gate 걸리면 임계 올리지 말고 원인 조사)
    - .planning/debug/ref-student-substrate-gap.md R-4 (ref-combo determinism)
  </read_first>
  <action>
    Run the candidate backfill for all 11 against the warm Pod (Task 0 confirmed). Confirm the integrity gate PASSES with epsilons unchanged; if it aborts on any doc, do NOT raise the epsilon — investigate and, if a genuine substrate divergence beyond epsilon, treat as a rollback signal (D-31). Then OPEN the artifact (D-19): for each of the 11 candidate docs dump meanAngles summary, techniqueProfile, bodyNormalizationProfile (non-NaN — proves the warm Pod produced it), forceDirectionPattern, keypointReport.fps (must read 9.0), referenceKeypointReport presence, and bodyComparisonSourcePose presence — 11/11. Confirm these were written into versions/{candidate}, not top-level (dump the candidate doc, show top-level unchanged). Record meanAngleDelta/p99AngleDelta per doc within epsilon. Write 33-S2-BACKFILL-EVIDENCE.md with the per-candidate-doc table.
  </action>
  <verify>
    <automated>test -f .planning/phases/33-result-trust-recovery/33-S2-BACKFILL-EVIDENCE.md && grep -qE "keypointReport|9\.0" .planning/phases/33-result-trust-recovery/33-S2-BACKFILL-EVIDENCE.md && grep -qiE "bodyComparisonSourcePose|meanAngleDelta" .planning/phases/33-result-trust-recovery/33-S2-BACKFILL-EVIDENCE.md && grep -qiE "versions|phase33-cm3" .planning/phases/33-result-trust-recovery/33-S2-BACKFILL-EVIDENCE.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Integrity gate PASS for 11/11 with epsilons unchanged; any near-miss delta recorded (D-29 — no refit).
    - Per-candidate-doc VALUES dumped (meanAngles/technique/bodyNorm(non-NaN)/force/keypointReport.fps=9.0/referenceKeypointReport/bodyComparisonSourcePose) — 11/11 (D-19); written into versions/{candidate}, top-level unchanged.
    - 이 산출물이 틀렸다면 어떻게 알았을까: answered — a stale (top-level-derived) field, a NaN bodyNorm (cold-Pod), or a missing bodyComparisonSourcePose shows in the opened candidate dump; a divergence beyond epsilon aborts the gate.
  </acceptance_criteria>
  <done>Gate PASS without refit; every candidate downstream field opened at 9fps; bodyNorm non-NaN; bodyComparisonSourcePose present 11/11; top-level untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| warm Pod (pinned commit) → live pose_frames | bodyNorm/force need a warm RTMW at the pinned commit; a cold/stale Pod NaNs |
| candidate versions/{candidate} → derived fields | re-derivation merges into the SAME candidate; top-level/activeVersion untouched |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-29 | Tampering | fields derived from old top-level (18fps) shipped as candidate | mitigate | source from versions/{candidate}; open-value dump catches a stale field (concern 2) |
| T-33-30 | Denial of service | missing bodyComparisonSourcePose breaks compare screen | mitigate | real producer + 11/11 presence check |
| T-33-31 | Spoofing | cold/stale-commit Pod → silent NaN | mitigate | Task 0 blocking canary checkpoint; non-NaN check in dump (concerns 6/15) |
| T-33-32 | Tampering | epsilon refit to force a gate pass | mitigate | epsilons pinned; gate trip → investigate/rollback (D-29/D-31) |
| T-33-SC | Tampering | package installs | accept | reuse existing backfill/extract scripts |
</threat_model>

<verification>
- Warm Pod confirmed at the pinned commit (canary) before backfill; cold/wrong-commit → blocking checkpoint.
- Backfill sources from + merges into versions/{candidate}; fps from candidate/CLI; keypointReport.fps=9.0; real bodyComparisonSourcePose on 11/11.
- Integrity gate PASS without refit (epsilons 0.1/1.0); top-level/activeVersion untouched; no scoring (D-20/D-29).
</verification>

<success_criteria>
The candidate reference is internally consistent at 9fps — sourced from and merged back into versions/{candidate}, every derived field recomputed including a real bodyComparisonSourcePose, keypointReport labeled 9.0 — with the gate passing on unchanged epsilons, the cold path gated by a blocking canary, and top-level/scoring untouched (codex concerns 2/6/15).
</success_criteria>

<output>
Create `.planning/phases/33-result-trust-recovery/33-04-SUMMARY.md` when done, with the warm-Pod canary body, the per-candidate downstream-field dump, the bodyComparisonSourcePose producer name, and the meanAngleDelta/p99 values proving no epsilon refit.
</output>
