---
phase: 33-result-trust-recovery
plan: 05
type: execute
wave: 5
depends_on: ["33-04"]
files_modified:
  - backend/tests/test_motiondtw.py
  - backend/shared/python/sunity_shared/analysis/motiondtw.py
  - backend/tests/phase33/test_safety_flags_m3_regression.py
autonomous: true
requirements: [D-02, D-18, D-19, D-20, D-27, D-29]
user_setup: []
must_haves:
  truths:
    - "find_action_segment actually fires when nu < nr — the nu<=nr short-circuit (motiondtw.py:83) that disabled segment search in 12/12 members is removed/reworked so the 준비/대기 removal step runs (SEED Task 3 / D-27)"
    - "The 3 production call sites are accounted for: pipeline/app.py:1770 (mode1 scoring), :4015, safety_flags.py:245 — a safety-flag regression check is mandatory because safety_flags recomputes the same motion_dtw (per the ref-student-substrate-gap.md ripple analysis)"
    - "test_motiondtw.py:39,45 (which pinned the old behavior) are updated to assert the fixed behavior; a safety-flag regression test is added (SEED Task 3)"
    - "M3 is an alignment-substrate fix, NOT a scoring change — tol 20°/slope 1.2/cap 90 and per_joint_deviation math untouched (D-20/D-29)"
    - "The fix is global, not tuned to any one motion (D-02)"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/motiondtw.py"
      provides: "find_action_segment that fires for nu<nr (segment search restored)"
      contains: "find_action_segment"
    - path: "backend/tests/phase33/test_safety_flags_m3_regression.py"
      provides: "asserts safety_flags motion_dtw alignment does not regress under the M3 change"
      contains: "safety_flags"
  key_links:
    - from: "backend/shared/python/sunity_shared/analysis/motiondtw.py:83"
      to: "pipeline/app.py:1770, pipeline/app.py:4015, safety_flags.py:245"
      via: "single motion_dtw path — fixing find_action_segment ripples to all 3 call sites"
      pattern: "find_action_segment|motion_dtw"
    - from: "backend/tests/test_motiondtw.py:39,45"
      to: "fixed find_action_segment behavior"
      via: "tests updated from old-behavior pin to new-behavior assertion"
      pattern: "find_action_segment|motion_dtw"
---

<objective>
Task 3 of the C+M3 substrate track (SEED Task 3): fix M3 — the `find_action_segment` short-circuit at motiondtw.py:83 (`if nu <= nr: return 0, nu`) that made the 준비/대기-removal step no-op in 12/12 members. Task 1 aligned density so nu ≈ nr; without M3 the boundary case still disables segment search, so this is a separate code fix. Because the same motion_dtw is reused by safety_flags (ref-student-substrate-gap.md ripple analysis), a safety-flag regression check is mandatory. This is an alignment-substrate fix, NOT a scoring change (D-20/D-29): the deduction formula, tolerances, and per_joint_deviation math are untouched. Code-only, no Pod.

Purpose: Without M3, reference and student stay misaligned at the phase boundary — the reason C alone (density) was rejected in the debug session. M3 must go WITH C (33-03/04). The catch device is the safety-flag regression test (M3 ripple).

Output: reworked find_action_segment + updated test_motiondtw.py + a safety-flag regression test.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-result-trust-recovery/33-CONTEXT.md
@.planning/debug/ref-student-substrate-gap.md
@backend/shared/python/sunity_shared/analysis/motiondtw.py
@backend/shared/python/sunity_shared/analysis/safety_flags.py
@backend/tests/test_motiondtw.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — update test_motiondtw.py to assert fixed find_action_segment + add safety-flags M3 regression test</name>
  <read_first>
    - backend/tests/test_motiondtw.py:35-50 (test_segment_search_finds_embedded_motion asserts s/e; test_motion_dtw_trims_and_aligns) — these pin behavior; :39,:45 are the segment/dtw assertions
    - backend/shared/python/sunity_shared/analysis/motiondtw.py:80-98 (find_action_segment def, :83 nu<=nr short-circuit, :98 motion_dtw uses it)
    - backend/shared/python/sunity_shared/analysis/safety_flags.py:226-245 (deliberately recomputes the same motion_dtw — ref-student-substrate-gap.md ripple analysis)
    - backend/tests/phase33/conftest.py (phase33 sys.path scaffold from 33-01)
  </read_first>
  <behavior>
    - test_motiondtw.py: a case where nu < nr (reference denser/longer) must show find_action_segment CUTTING a segment (준비/대기 removed), not returning the whole user clip. Update the :39/:45 assertions to express the fixed behavior.
    - test_safety_flags_m3_regression.py: for a synthetic pose sequence, safety_flags' motion_dtw alignment produces no new false-positive / false-negative flag under the M3 change vs a captured baseline (safety_flags ripple).
  </behavior>
  <action>
    Update backend/tests/test_motiondtw.py so the segment-search assertions (:39,:45) express the FIXED behavior: when nu < nr, find_action_segment fires (trims 준비/대기) instead of returning (0, nu) whole-clip. This should be RED until Task 2. Create backend/tests/phase33/test_safety_flags_m3_regression.py asserting safety_flags' alignment does not regress (no new FP/FN) under the M3 change — pure synthetic, no Pod/Firestore. Do NOT change scoring math or tolerances (D-20).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_motiondtw.py tests/phase33/test_safety_flags_m3_regression.py -q; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - test_motiondtw.py segment assertion is RED before Task 2 (exercises the missing segment-fire, not a no-op).
    - test_safety_flags_m3_regression.py exists and captures the safety-flag baseline it will re-check after the fix.
    - 틀리면 걸리는 장치 (D-18): the safety-flags regression test is the M3-ripple catch — a new FP/FN turns it red.
  </acceptance_criteria>
  <done>Tests updated/added and RED for the right reason.</done>
</task>

<task type="auto">
  <name>Task 2: rework find_action_segment (nu<=nr disablement) + verify 3 ripple sites + scoring untouched</name>
  <read_first>
    - backend/shared/python/sunity_shared/analysis/motiondtw.py:80-98 (find_action_segment + the nu<=nr branch at :83)
    - .planning/debug/ref-student-substrate-gap.md `## 인계` Task 3 (fix direction: when nu<nr, window the reference to the student length OR two-way sliding by the shorter side — the 준비/대기 removal must actually fire) + `## 재현 검증` (12/12 무력화)
    - backend/functions/pipeline/app.py:1770 (mode1 scoring motion_dtw), :4015 (second call site)
    - backend/shared/python/sunity_shared/analysis/safety_flags.py:245 (recomputed motion_dtw — ref-student-substrate-gap.md ripple analysis)
  </read_first>
  <action>
    Rework find_action_segment so the nu<=nr short-circuit no longer disables segment search: when nu < nr, window the reference to the student length (or slide by the shorter side) so 준비/대기 removal actually fires — Claude picks the implementation per the SEED direction (planner discretion within the SEED Task 3 spec). Verify all 3 ripple sites: pipeline/app.py:1770 (mode1 scoring — the main path), :4015, and safety_flags.py:245 (recomputes the same motion_dtw — the safety-flag regression test must stay green). Do NOT change the deduction formula, tol 20°, slope 1.2, cap 90, or per_joint_deviation math (D-20/D-29) — grep-confirm those files are untouched. Global, not motion-specific (D-02). Run the full backend suite to confirm no regression beyond the intentionally-updated motiondtw assertions.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_motiondtw.py tests/phase33/test_safety_flags_m3_regression.py -q && git diff --stat -- shared/python/sunity_shared/analysis/dimensions.py shared/python/sunity_shared/analysis/kismam.py | grep -q . && echo "SCORING CHANGED — FAIL" || echo "scoring untouched OK"</automated>
  </verify>
  <acceptance_criteria>
    - find_action_segment fires for nu<nr; test_motiondtw.py passes with the updated assertions; the segment-removal step is no longer no-op.
    - Safety-flags M3 regression test PASSES (no new FP/FN) — the safety_flags ripple is contained.
    - No scoring math changed: git diff shows dimensions.py / kismam.py / deduction scoring UNCHANGED (D-20/D-29); only motiondtw.py + tests changed.
    - 무엇을 열어서 확인했는가 (D-19): the SUMMARY shows a before/after of a synthetic nu<nr case where the segment is now trimmed (values), plus the safety-flag baseline diff = 0.
  </acceptance_criteria>
  <done>M3 fixed (segment search fires), all 3 ripple sites verified, safety flags no-regression, scoring untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| motion_dtw (single path) → mode1 scoring + safety_flags | one alignment function feeds 3 call sites; a change ripples to safety flags |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-30 | Tampering | M3 change silently regresses safety flags | mitigate | mandatory safety-flags regression test (safety_flags.py:245 ripple) |
| T-33-31 | Tampering | M3 mistaken for a scoring change | mitigate | grep-gate: dimensions/kismam/deduction UNCHANGED; only motiondtw + tests (D-20/D-29) |
| T-33-SC | Tampering | package installs | accept | no new packages (numpy only) |
</threat_model>

<verification>
- find_action_segment fires for nu<nr; test_motiondtw.py updated + green.
- Safety-flags M3 regression test green (no new FP/FN).
- git diff: motiondtw.py + tests only; no scoring-math file touched (D-20/D-29).
</verification>

<success_criteria>
The 준비/대기-removal segment search actually fires (M3 no longer disabled), all 3 motion_dtw call sites are verified with the safety-flag ripple contained, and scoring math is untouched — the alignment substrate is restored to pair with the density/inversion fix.
</success_criteria>

<output>
Create `.planning/phases/33-result-trust-recovery/33-05-SUMMARY.md` when done, showing the nu<nr before/after trim and the safety-flag baseline diff = 0.
</output>
