---
phase: 33-result-trust-recovery
plan: 05
type: execute
wave: 5
depends_on: ["33-04", "33-19"]
files_modified:
  - backend/tests/test_motiondtw.py
  - backend/shared/python/sunity_shared/analysis/motiondtw.py
  - backend/tests/phase33/test_m3_alignment_only.py
  - backend/tests/phase33/test_safety_flags_m3_regression.py
autonomous: true
requirements: [D-02, D-18, D-19, D-20, D-27, D-29]
user_setup: []
must_haves:
  truths:
    - "M3 is implemented EXACTLY to the locked 33-M3-SPEC.md (33-19): MotionMatch carries a paired user+reference range; find_action_segment fires for nu<nr; the reference-phase coverage floor is enforced; ambiguous alignment fails closed — no spec deviation without re-locking 33-19 first (codex concern 4)"
    - "Alignment-only invariants hold as TESTS: already-aligned inputs → byte-identical per_joint_deviation; identical inputs → zero deviation; NO motion-key branches (D-02); no reference phase removed below the coverage floor; tol 20°/slope 1.2/cap 90/epsilons + per_joint_deviation formula hash-identical (D-20/D-29)"
    - "The 3 ripple sites (pipeline/app.py:1770, :4015, safety_flags.py:245) are verified; a safety-flag regression test proves no new FP/FN (safety_flags recomputes the same motion_dtw)"
    - "The scoring-untouched check is a JSON/exit-code DATA-GATE, NOT a shell expression whose trailing `|| echo` can absorb a pytest failure (codex concern 8 — the 33-05 gate bug is fixed)"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/motiondtw.py"
      provides: "find_action_segment + MotionMatch with paired user+reference range, coverage floor, fail-closed — per 33-M3-SPEC.md"
      contains: "find_action_segment"
    - path: "backend/tests/phase33/test_m3_alignment_only.py"
      provides: "byte-identical-deviation + zero-on-identity + coverage-floor + no-motion-key-branch tests"
      contains: "byte"
  key_links:
    - from: "33-M3-SPEC.md (33-19)"
      to: "motiondtw.py find_action_segment / MotionMatch"
      via: "implement the locked paired-range + coverage-floor contract verbatim"
      pattern: "MotionMatch|coverage|ref_start"
    - from: "motiondtw.py:83"
      to: "pipeline/app.py:1770, :4015, safety_flags.py:245"
      via: "single motion_dtw path — fix ripples to all 3 sites"
      pattern: "find_action_segment|motion_dtw"
---

<objective>
Task 3 of the C+M3 substrate track (SEED Task 3): implement M3 EXACTLY to the locked `33-M3-SPEC.md` (33-19). The spec resolves what the old plan left to executor discretion (codex concern 4): `find_action_segment`/`MotionMatch` gain a paired reference range, the reference-phase coverage floor forbids silent phase-dropping, and ambiguous alignment fails closed. Prove alignment-only behavior with byte-identical-deviation + zero-on-identity tests (not "scoring files unchanged", which codex showed is insufficient), verify the 3 ripple sites with a safety-flag regression test, and gate the scoring-untouched claim with a JSON/exit-code DATA-GATE — fixing the 33-05 shell bug where a trailing `|| echo ... OK` absorbed a pytest failure (codex concern 8). Code-only, no Pod.

Purpose: Segment selection changes the deduction INPUT even with an unchanged formula. The spec + the byte-identical invariants are what make M3 provably alignment-only and non-inflating. Without M3, reference and student stay misaligned at the phase boundary (why C alone was rejected in the debug session).

Output: spec-conformant find_action_segment/MotionMatch + updated test_motiondtw.py + alignment-only tests + safety-flag regression test.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-result-trust-recovery/33-CONTEXT.md
@.planning/phases/33-result-trust-recovery/33-M3-SPEC.md
@.planning/debug/ref-student-substrate-gap.md
@backend/shared/python/sunity_shared/analysis/motiondtw.py
@backend/shared/python/sunity_shared/analysis/safety_flags.py
@backend/tests/test_motiondtw.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — encode 33-M3-SPEC.md invariants as tests (byte-identical deviation, zero-on-identity, coverage floor, no-motion-key, safety-flag regression)</name>
  <read_first>
    - .planning/phases/33-result-trust-recovery/33-M3-SPEC.md (the LOCKED contract — paired ranges, coverage floor value, boundary nu<nr, fail-closed, invariants, the exact tests this plan must add)
    - backend/tests/test_motiondtw.py:35-50 (the assertions pinning old whole-clip behavior at :39/:45)
    - backend/shared/python/sunity_shared/analysis/motiondtw.py:72-100 (MotionMatch fields, find_action_segment nu<=nr, motion_dtw, per_joint_deviation identity→0)
    - backend/shared/python/sunity_shared/analysis/safety_flags.py:226-245 (recomputes the same motion_dtw — the ripple)
  </read_first>
  <behavior>
    - test_m3_alignment_only.py: already-aligned user/ref → per_joint_deviation BYTE-IDENTICAL to the pre-M3 result on that aligned input; identical user==ref → all-zero deviation; a synthetic case where nu<nr shows find_action_segment CUTTING 준비/대기 (not whole-clip) while keeping reference coverage ≥ the spec floor; a below-floor window is rejected/fails-closed; no motion-key branch exists (parametrized across motions gives identical code path).
    - test_motiondtw.py :39/:45 updated to the fixed nu<nr behavior with the paired reference range.
    - test_safety_flags_m3_regression.py: safety_flags' motion_dtw alignment yields no new FP/FN vs a captured baseline.
  </behavior>
  <action>
    Write backend/tests/phase33/test_m3_alignment_only.py encoding the 33-M3-SPEC invariants (byte-identical deviation on aligned input, zero-on-identity, coverage-floor enforcement, fail-closed on ambiguity, no-motion-key-branch). Update backend/tests/test_motiondtw.py :39/:45 to assert the spec's fixed nu<nr behavior (segment fires + paired reference range). Create backend/tests/phase33/test_safety_flags_m3_regression.py capturing the safety-flag baseline to re-check post-fix. All RED until Task 2. Do NOT change scoring math/tolerances (D-20).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_motiondtw.py tests/phase33/test_m3_alignment_only.py tests/phase33/test_safety_flags_m3_regression.py -q; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - The alignment-only tests exist and are RED for the right reason (they exercise the missing paired-range + coverage-floor behavior, not a no-op).
    - The safety-flag regression test captures its baseline.
    - 틀리면 걸리는 장치 (D-18): byte-identical-deviation + zero-on-identity + coverage-floor + safety-flag tests are the scoring-leak catches per the spec.
  </acceptance_criteria>
  <done>Spec invariants encoded as RED tests.</done>
</task>

<task type="auto">
  <name>Task 2: implement M3 per 33-M3-SPEC.md + verify 3 ripple sites + JSON data-gate for scoring-untouched (fix the || echo bug)</name>
  <read_first>
    - .planning/phases/33-result-trust-recovery/33-M3-SPEC.md (implement verbatim — paired ranges, coverage floor, fail-closed)
    - backend/shared/python/sunity_shared/analysis/motiondtw.py:72-100 (MotionMatch + find_action_segment + motion_dtw to modify) + per_joint_deviation:103-130 (consumes the windowed reference — keep identity→0)
    - backend/shared/python/sunity_shared/analysis/segments.py (ref_boundary_frame/split_path_by_ref depend on ref index space — keep consistent with the windowed reference per the spec)
    - backend/functions/pipeline/app.py:1770 (mode1 scoring), :4015 (2nd call site) + safety_flags.py:245 (recomputes motion_dtw)
    - backend/scripts/gate_check.py (33-18 — the JSON data-gate replacing the `|| echo` scoring-untouched shell expression; --scoring-constants-match)
  </read_first>
  <action>
    Implement M3 to the locked spec: extend MotionMatch with the reference range, rework find_action_segment so nu<nr windows the reference (or two-way slides by the shorter side) with the coverage floor enforced and fail-closed on ambiguity, and thread the paired range through motion_dtw + per_joint_deviation + segments.py consistently. Verify all 3 ripple sites (pipeline:1770/:4015, safety_flags:245); the safety-flag regression test must stay green. GATE the scoring-untouched claim with two devices: (a) PRIMARY — gate_check.py --scoring-constants-match against the pinned scoring-constant manifest (tests/phase33/scoring_constants_pinned.json: tol 20/slope 1.2/cap 90/MEAN_EPSILON_DEG/P99_EPSILON_DEG), and (b) a plain `git diff --quiet -- <exact pinned formula files: dimensions.py, kismam.py, and any other pinned scoring file>` exit-code assertion. Do NOT overload --require-all-pass with a filename list (it asserts item.status==PASS, not file-unchanged), and do NOT use a shell expression whose trailing `|| echo ... OK` can swallow a failure (codex concern 8). Global, not motion-specific (D-02). Run the full backend suite.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_motiondtw.py tests/phase33/test_m3_alignment_only.py tests/phase33/test_safety_flags_m3_regression.py -q && python3 scripts/gate_check.py --scoring-constants-match tests/phase33/scoring_constants_pinned.json 2>/dev/null && git diff --quiet -- shared/python/sunity_shared/analysis/dimensions.py shared/python/sunity_shared/analysis/kismam.py; echo "gate_exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - M3 implemented per the locked spec (paired ranges, coverage floor, fail-closed); test_motiondtw.py + test_m3_alignment_only.py green; byte-identical-deviation + zero-on-identity hold.
    - Safety-flag regression test green (no new FP/FN) — the ripple is contained across all 3 sites.
    - Scoring-untouched proven by gate_check.py --scoring-constants-match (pinned constants manifest) PLUS a `git diff --quiet` exit-code on the exact scoring files — NOT --require-all-pass overloaded with a filename list, NOT a `|| echo` absorber (concern 8); dimensions.py/kismam.py unchanged.
    - No motion-key branch (D-02); only motiondtw.py + tests changed among code files.
    - 무엇을 열어서 확인했는가 (D-19): the SUMMARY shows a before/after synthetic nu<nr trim (values), the byte-identical-deviation proof on an aligned input, and the safety-flag baseline diff = 0.
  </acceptance_criteria>
  <done>M3 implemented to spec; segment fires; 3 ripple sites verified; alignment-only proven by tests + JSON data-gate; scoring untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| motion_dtw (single path) → mode1 scoring + safety_flags | one alignment fn feeds 3 sites; a change ripples to safety flags |
| M3 window → deduction input | a below-floor window would inflate scores by dropping hard phases |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-33 | Tampering | M3 change leaks into scoring | mitigate | byte-identical-deviation + zero-on-identity tests + gate_check scoring-constants (concern 4/8) |
| T-33-34 | Tampering | reference window drops hard phases → inflation | mitigate | coverage floor enforced per spec; fail-closed on ambiguity |
| T-33-35 | Tampering | safety flags silently regress | mitigate | mandatory safety-flag regression test (safety_flags:245 ripple) |
| T-33-36 | Repudiation | a pytest failure absorbed by `|| echo ... OK` | mitigate | JSON/exit-code data-gate replaces the shell absorber (concern 8) |
| T-33-SC | Tampering | package installs | accept | numpy only |
</threat_model>

<verification>
- M3 conforms to 33-M3-SPEC.md (paired ranges, coverage floor, fail-closed); alignment-only tests green (byte-identical, zero-on-identity, no-motion-key).
- 3 ripple sites verified; safety-flag regression green.
- Scoring-untouched via JSON data-gate (no `|| echo` absorber); dimensions/kismam unchanged (D-20/D-29).
</verification>

<success_criteria>
M3 is implemented exactly to the locked spec — the 준비/대기-removal fires with a paired reference range and a coverage floor that forbids silent phase-dropping — proven alignment-only by byte-identical-deviation tests and gated by a JSON data-gate, with the safety-flag ripple contained and scoring math untouched (codex concerns 4, 8).
</success_criteria>

<output>
Create `.planning/phases/33-result-trust-recovery/33-05-SUMMARY.md` when done, showing the nu<nr before/after trim, the byte-identical-deviation proof on an aligned input, the coverage-floor enforcement, and the safety-flag baseline diff = 0.
</output>
