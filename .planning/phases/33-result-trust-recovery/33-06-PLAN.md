---
phase: 33-result-trust-recovery
plan: 06
type: execute
wave: 6
depends_on: ["33-05"]
files_modified:
  - .planning/phases/33-result-trust-recovery/33-S4-VERIFY-EVIDENCE.md
autonomous: false
requirements: [D-18, D-19, D-20, D-23, D-25, D-27, D-28, D-29, D-30, D-31, D-32]
user_setup: []
must_haves:
  truths:
    - "SEED 8-item success gate ALL PASS (this is the presentation-track entry gate, D-28): (1) all 6-fixture success-margins positive incl. pdshape neg→pos; (2) fault/success separation ≥ current; (3) inversion vs non-inversion 편차 바닥 gap reduced; (4) M8 consumer-side no regression (original-5 overlay/fault-zoom); (5) M3 actually fires (no longer 12/12 disabled); (6) safety flags no new FP/FN; (7) pdshape 2-run stability better than ±30; (8) threshold refit = 0"
    - "Verification is on the versioned (not-yet-active) reprocessed reference; 6-fixture equal frequency, eval SERIAL (D-23/D-30)"
    - "Fixture-less 4 (foxtop, foxtop-split, invert, combo) verified via verify_self_comparison.py (reference-as-student → near-full expected), no silent skip (R-3/D-23)"
    - "If elbow-twist success margin < +2.0° after reprocess, the single belle visual-identity question is raised (D-32ⓑ, no scores/angles) — else report only"
    - "Any rollback trigger (negative margin / M8 regression / combo variance / new safety flag) → immediate restore, presentation track does NOT start (D-31)"
  artifacts:
    - path: ".planning/phases/33-result-trust-recovery/33-S4-VERIFY-EVIDENCE.md"
      provides: "8-item gate verdict + per-fixture margins + self-comparison for fixture-less 4 + M8 simulator eyeball + rollback-trigger evaluation"
      contains: "여유"
  key_links:
    - from: "33-S4-VERIFY-EVIDENCE.md"
      to: "backend/evals/phase25/run_sweep.py + assert_gates.py"
      via: "6-fixture SERIAL re-sweep on rebooted Pod against versioned reference"
      pattern: "run_sweep|assert_gates"
    - from: "fixture-less 4 verification"
      to: "backend/scripts/verify_self_comparison.py"
      via: "reference-as-student self-comparison (R-3 substitute)"
      pattern: "verify_self_comparison|self.comparison"
---

<objective>
Task 4 of the C+M3 substrate track (SEED Task 4): full re-verification against the SEED 8-item success gate. This is the entry gate for the entire presentation track (D-28 — no crop/copy/overlay/illustration work starts until all 8 PASS). Re-sweep the 6 fixtures serially (equal frequency, D-23) on the reprocessed (still versioned, not-yet-active) reference; verify the fixture-less 4 via self-comparison (R-3); do the M8 consumer-side simulator eyeball on original-5 overlay/fault-zoom (R-5); evaluate rollback triggers; and handle the elbow-twist conditional per D-32ⓑ. Pod required (D-30) — autonomous:false. Threshold refit = 0 (D-20/D-29, gate item #8).

Purpose: The A-0 verdict was "어긋남 큼"; this proves the substrate is actually fixed before any presentation work builds on it. The gate is margin-based (not absolute score) per R-6 — pdshape's dead-zone is mitigated, not eliminated.

Output: 33-S4-VERIFY-EVIDENCE.md (8-item verdict + margins + self-comparison + M8 eyeball) — ALL PASS unblocks 33-07 flip; any fail → rollback.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-result-trust-recovery/33-CONTEXT.md
@.planning/debug/ref-student-substrate-gap.md
@backend/evals/phase25/run_sweep.py
@backend/scripts/verify_self_comparison.py
@backend/scripts/dump_analysis_doc.py
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: belle GPU greenlight — Pod for re-verification sweep (D-30 / D-32ⓐ ops exception)</name>
  <action>
    PAUSE for belle's operational greenlight to spin up a GPU Pod for the 6-fixture re-verification sweep (SERIAL). Request EU-RO-1 4090-priority Network Storage Pod (pod-request-include-gpu-model). This is an ops/cost greenlight (D-32 exception ⓐ), not a design touchpoint. If the 33-03 Pod is still warm, reuse it and skip the reboot. Confirm 33-03/04/05 all completed (versioned reference + downstream backfill + M3 fix) before sweeping.
  </action>
  <what-built>Nothing yet — requests belle to greenlight GPU cost for the substrate re-verification sweep.</what-built>
  <how-to-verify>
    1. Confirm GPU spend for the 6-fixture SERIAL re-sweep + self-comparison for 4 fixture-less motions.
    2. Confirm 33-03 (reprocess), 33-04 (backfill), 33-05 (M3) are done.
  </how-to-verify>
  <verify>
    <automated>echo "checkpoint — belle GPU greenlight for re-verification (ops, not a design touchpoint)"</automated>
  </verify>
  <resume-signal>Type "go" to run the re-verification sweep, or hold.</resume-signal>
  <done>belle greenlit GPU; substrate Tasks 1-3 confirmed complete.</done>
</task>

<task type="auto">
  <name>Task 2: 6-fixture SERIAL re-sweep + self-comparison for fixture-less 4 + collect margins</name>
  <read_first>
    - backend/evals/phase25/run_sweep.py (SERIAL in-process _process; VETO/RTMW_DETERMINISTIC env) + assert_gates.py + MEMORY phase25-run-sweep-veto-env (setdefault VETO=1)
    - backend/scripts/verify_self_comparison.py (reference-as-student self-comparison for fixture-less motions — R-3)
    - .planning/debug/ref-student-substrate-gap.md `## 성공 판정 기준` (8 items, current-baseline margins: kip-up +15.8 / peter-pan +10.5 / power-spin +9.0 / elbow-twist +0.2 / pdshape −1.2) + `## 재처리 위험도 표` R-2/R-3/R-6 + MEMORY verify-all-fixtures-not-kipup-only + pipeline-not-concurrency-safe-eval-serial
  </read_first>
  <action>
    Point the sweep at the versioned reprocessed reference (still not-active — verify against the new versions/phase4_v1, not the old active). Run the 6-fixture re-sweep SERIALLY (equal frequency across power-spin/peter-pan/elbow-twist-sister/pdshape/kip-up/climb — kip-up 편중 금지; no parallel /analyze). Run assert_gates.py. For each success member compute the 여유 = tol(20°) − max(관절 편차). For the 4 fixture-less motions (foxtop, foxtop-split, invert, combo) run verify_self_comparison.py (reference-as-student → near-full expected) as the R-3 substitute — do NOT silent-skip (D-23). Run pdshape twice to measure 2-run stability (R-6/item #7). Record all margins + separations + self-comparison results in 33-S4-VERIFY-EVIDENCE.md. Do NOT refit any threshold (item #8 / D-20/D-29).
  </action>
  <verify>
    <automated>test -f .planning/phases/33-result-trust-recovery/33-S4-VERIFY-EVIDENCE.md && grep -qE "여유|margin" .planning/phases/33-result-trust-recovery/33-S4-VERIFY-EVIDENCE.md && grep -qiE "self.comparison|verify_self" .planning/phases/33-result-trust-recovery/33-S4-VERIFY-EVIDENCE.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - 6-fixture SERIAL re-sweep run against the versioned reprocessed reference; per-fixture success 여유 recorded (all 6, equal frequency).
    - Fixture-less 4 verified via verify_self_comparison.py (recorded, no silent skip, D-23).
    - pdshape 2-run stability measured (item #7).
    - No threshold refit (item #8) — SUMMARY confirms tol/slope/cap/epsilons unchanged.
    - 무엇을 열어서 확인했는가 (D-19): actual margin numbers + self-comparison scores pasted, not "sweep passed."
  </acceptance_criteria>
  <done>6-fixture margins + fixture-less self-comparison + pdshape stability collected; no refit.</done>
</task>

<task type="auto">
  <name>Task 3: judge SEED 8-item gate + M8 simulator eyeball + rollback-trigger eval + elbow-twist conditional (D-32ⓑ)</name>
  <read_first>
    - .planning/debug/ref-student-substrate-gap.md `## 성공 판정 기준` (all 8 items verbatim) + `## 롤백 설계 / 롤백 트리거` (negative margin / M8 regression / combo variance / new safety flag) + `## elbow-twist 처방 경로` (< +2.0° → single belle question; branch pre-decided)
    - MEMORY verify-ui-on-simulator-before-ota (Xcode 26.6 + Simulator on this Mac) — for the M8 original-5 overlay/fault-zoom eyeball (R-5)
    - backend/scripts/dump_analysis_doc.py (--download-pngs to open regenerated crops for the M8 eyeball)
  </read_first>
  <action>
    Judge every one of the SEED 8-item gate against the Task 2 evidence and record PASS/FAIL each: (1) all 6 success 여유 positive incl. pdshape neg→pos; (2) fault/success separation ≥ current (power-spin 45/peter-pan 21/elbow-twist 35/pdshape 53/kip-up 20); (3) inversion vs non-inversion 편차 바닥 gap reduced from 3×; (4) M8 consumer-side no regression — render original-5 (climb/foxtop/foxtop-split/invert/sideway-spin) overlay + fault-zoom crops in the iOS Simulator (axis layout (x,0,y)→(x,y,0) transition, pickVerticalAxis absorption) and eyeball 전수, screenshots (R-5); (5) M3 fires (segment trimmed in ≥1 member, no longer 12/12 disabled); (6) safety flags no new FP/FN; (7) pdshape 2-run edit < ±30; (8) threshold refit = 0. Evaluate the 4 rollback triggers — if ANY trips, do NOT flip; execute the 33-07 rollback path and stop. ELBOW-TWIST CONDITIONAL (D-32ⓑ): if elbow-twist success 여유 < +2.0° after reprocess, raise the ONE belle question ("정은지 정상 영상과 기준 영상이 나란히 — 같은 동작으로 보이십니까?", no scores/angles/numbers) and follow the pre-decided branch (같아→track technical cause; 달라→re-register ref-elbow-twist-sister); if ≥ +2.0°, report result only, no belle question. Write the full 8-item verdict + M8 screenshots + rollback evaluation in 33-S4-VERIFY-EVIDENCE.md.
  </action>
  <verify>
    <automated>grep -qiE "PASS|FAIL" .planning/phases/33-result-trust-recovery/33-S4-VERIFY-EVIDENCE.md && grep -qiE "simulator|screenshot|M8" .planning/phases/33-result-trust-recovery/33-S4-VERIFY-EVIDENCE.md && grep -qiE "rollback|trigger" .planning/phases/33-result-trust-recovery/33-S4-VERIFY-EVIDENCE.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All 8 SEED gate items have an explicit PASS/FAIL with the evidence value cited; ALL PASS is required to unblock 33-07 (D-28).
    - M8 original-5 overlay/fault-zoom rendered in the Simulator with screenshots (전수, R-5) — no axis-transition regression, or the regression is a rollback trigger.
    - Rollback triggers evaluated explicitly; if any tripped, the flip is blocked and the rollback path is invoked (D-31).
    - Elbow-twist handled per D-32ⓑ: belle question raised ONLY if margin < +2.0° (SUMMARY states margin + whether the question was raised) — no extra belle questions manufactured.
    - 이 산출물이 틀렸다면 어떻게 알았을까: answered — a negative margin, an M8 crop regression, a combo variance overrun, or a new safety flag would each turn an item FAIL and trip rollback; the doc states none did OR names which did.
  </acceptance_criteria>
  <done>8-item gate judged with evidence, M8 eyeball done, rollback triggers clear (or invoked), elbow-twist handled per D-32ⓑ — ALL PASS gates the presentation track.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Pod → versioned reference sweep | re-analysis against not-yet-active reprocessed reference; production still on old active |
| regenerated crop PNG → Simulator | M8 consumer-side eyeball on original-5 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-32 | Tampering | passing the gate by refitting a threshold | mitigate | gate item #8 = refit 0; grep-confirm tol/slope/cap/epsilons unchanged (D-29) |
| T-33-33 | Tampering | M8 axis transition silently breaks original-5 crops | mitigate | Simulator 전수 eyeball on original-5 (R-5); regression = rollback trigger |
| T-33-34 | Repudiation | passing on kip-up alone | mitigate | 6-fixture equal frequency + fixture-less self-comparison (D-23) |
| T-33-35 | Denial of service | concurrency pollution of serial sweep | mitigate | SERIAL /analyze only (D-30) |
| T-33-SC | Tampering | package installs on Pod | accept | existing runbook; no new packages |
</threat_model>

<verification>
- SEED 8-item gate ALL PASS (margins positive incl. pdshape, separation ≥ current, gap reduced, M8 no regression, M3 fires, safety flags clean, pdshape stable, refit 0).
- Fixture-less 4 self-comparison recorded; 6-fixture equal frequency SERIAL.
- Rollback triggers clear (or rollback invoked); elbow-twist per D-32ⓑ.
</verification>

<success_criteria>
The reprocessed reference passes all 8 SEED success criteria on margins (not absolute scores), the M8 consumer path shows no regression, M3 fires, safety flags are clean, and no threshold was refit — the substrate is proven fixed and the presentation track is unblocked (or, on any failure, the rollback path is invoked and presentation stays blocked).
</success_criteria>

<output>
Create `.planning/phases/33-result-trust-recovery/33-06-SUMMARY.md` when done, with the 8-item PASS/FAIL table, per-fixture margins, M8 screenshots, and the elbow-twist margin + belle-question decision.
</output>
