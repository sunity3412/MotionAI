---
phase: 33-result-trust-recovery
plan: 06
type: execute
wave: 6
depends_on: ["33-05", "33-17", "33-18", "33-20"]
files_modified:
  - .planning/phases/33-result-trust-recovery/33-S4-VERIFY-EVIDENCE.md
autonomous: false
requirements: [D-18, D-19, D-20, D-23, D-25, D-27, D-28, D-29, D-30, D-31, D-32]
user_setup: []
must_haves:
  truths:
    - "Verification runs against the CANDIDATE version via the shadow-reference resolver (--reference-version phase33-cm3-run1 / SUNITY_SHADOW_REFERENCE_VERSION), proving which candidate hash was consumed WITHOUT flipping production top-level — no undocumented temporary flip (codex concern 3 / 33-17 / D-31)"
    - "The SEED 8-item gate is judged via a JSON DATA-GATE (gate_check.py): all 8 items must be exactly PASS, no rollback trigger true, all 11 candidate docs share the expected hash/version, scoring constants match the pinned manifest — a document merely containing 'PASS' cannot pass (codex concern 8 / D-18)"
    - "Every previously non-decidable gate has a PREDECLARED numeric boundary + oracle: 'gap significantly reduced' → a stated ratio/degree threshold; pdshape 2-run stability → a stated max edit < a fixed degree; safety 'no new FP/FN' → an explicit FP/FN count oracle (codex concern 12)"
    - "Coverage follows the single 33-COVERAGE-MATRIX.md (33-20): 6-fixture equal frequency + the fixture-less motions verified by their named substitute; no divergent inventory (codex suggestion 10 / D-23)"
    - "Warm-Pod canary confirms the pinned commit before sweeping (33-18); traffic isolated (dedicated eval Pod or drained Lambda, codex concern 14); eval SERIAL (D-30)"
    - "If elbow-twist margin < +2.0°, this plan records the HALT and ROUTES to 33-21 (formal HALT loop) — it does NOT resolve inline and does NOT unblock 33-07 (codex concern 13 / D-32)"
    - "No threshold refit (D-20/D-29, gate item #8)"
  artifacts:
    - path: ".planning/phases/33-result-trust-recovery/33-S4-VERIFY-EVIDENCE.md"
      provides: "shadow-candidate proof + JSON 8-item gate verdict + predeclared-boundary evaluation + per-fixture margins + self-comparison + M8 eyeball + rollback + elbow-twist route"
      contains: "phase33-cm3"
  key_links:
    - from: "run_sweep.py --reference-version phase33-cm3-run1"
      to: "reference/{id}/versions/phase33-cm3-run1 (shadow read, no flip)"
      via: "33-17 resolver; evidence records the consumed candidate hash"
      pattern: "reference-version|SUNITY_SHADOW|phase33-cm3"
    - from: "gate_check.py 8-item + hash + rollback + scoring-constants"
      to: "33-S4-VERIFY-EVIDENCE.json (machine-gate sidecar)"
      via: "JSON data-gate, non-zero on any non-PASS"
      pattern: "gate_check|require-all-pass|require-hashes"
---

<objective>
Task 4 of the C+M3 substrate track (SEED Task 4), corrected per codex concerns 3/8/12/13/14. Re-verify the CANDIDATE against the SEED 8-item gate — but consume the candidate via the 33-17 shadow-reference resolver (no undocumented flip), judge the gate with the 33-18 JSON data-gate (not grep-for-PASS), replace every non-decidable success gate with a predeclared numeric boundary, follow the single 33-COVERAGE-MATRIX.md, isolate production traffic, confirm the warm-Pod canary, and ROUTE the elbow-twist branch to the formal HALT plan (33-21) instead of resolving inline. ALL 8 PASS is the presentation-track entry gate (D-28); any rollback trigger blocks the flip (D-31). Pod required (D-30) — autonomous:false. Threshold refit = 0 (item #8).

Purpose: The A-0 verdict was "어긋남 큼"; this proves the substrate is actually fixed BEFORE any presentation work builds on it — and proves it on the candidate the flip will activate, judged by machine gates with predeclared boundaries, not prose.

Output: 33-S4-VERIFY-EVIDENCE.md (shadow-candidate proof + JSON 8-item verdict + predeclared-boundary eval + margins + self-comparison + M8 eyeball + elbow-twist route) — ALL PASS + no HALT unblocks 33-21→33-07; any fail → rollback.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-result-trust-recovery/33-CONTEXT.md
@.planning/phases/33-result-trust-recovery/33-REVIEWS.md
@.planning/phases/33-result-trust-recovery/33-COVERAGE-MATRIX.md
@.planning/debug/ref-student-substrate-gap.md
@backend/evals/phase25/run_sweep.py
@backend/scripts/verify_self_comparison.py
@backend/scripts/gate_check.py
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: belle GPU greenlight + warm-Pod canary + traffic isolation (D-30 / D-32ⓐ / codex concerns 6,14)</name>
  <read_first>
    - 33-03-SUMMARY.md (Pod id, proxy, deployed commit) + 33-18 /health canary shape
    - 33-REVIEWS.md codex concern 6 (canary not just /health 200) + concern 14 (dedicated eval Pod OR drain Lambda) + MEMORY pod-request-include-gpu-model / pipeline-not-concurrency-safe-eval-serial
  </read_first>
  <action>
    PAUSE for belle's ops greenlight for the 6-fixture SERIAL re-verification sweep. Request EU-RO-1 4090-priority Network Storage Pod. If the 33-03 Pod is still warm, hit /health and confirm the 33-18 canary (commit SHA == 33-05's committed code + model canary green + PR=1/deterministic=1) — reuse only if the canary matches the CURRENT commit including the M3 fix (concern 6). Traffic isolation (concern 14): dedicated eval Pod (Lambda NOT resynced) OR drain Lambda for the sweep window. Confirm 33-03/04/05 complete (candidate reprocessed + backfilled + M3). This is an ops greenlight (D-32ⓐ), not a design touchpoint.
  </action>
  <what-built>Nothing yet — requests belle to greenlight the GPU for the substrate re-verification sweep on an isolated Pod at the pinned commit.</what-built>
  <how-to-verify>
    1. Approve GPU for the SERIAL 6-fixture sweep + fixture-less self-comparison.
    2. Confirm the /health canary matches the current commit (incl. M3) and traffic is isolated.
  </how-to-verify>
  <verify>
    <automated>echo "checkpoint — belle GPU greenlight + warm-Pod canary + traffic isolation (ops)"</automated>
  </verify>
  <resume-signal>Type "go" once the canary matches the current commit and traffic is isolated, or hold.</resume-signal>
  <done>belle greenlit GPU; warm-Pod canary matches the M3 commit; traffic isolated.</done>
</task>

<task type="auto">
  <name>Task 2: shadow-candidate SERIAL re-sweep + fixture-less self-comparison (per coverage matrix) + collect margins</name>
  <read_first>
    - backend/evals/phase25/run_sweep.py (33-17 added --reference-version → SUNITY_SHADOW_REFERENCE_VERSION; SERIAL in-process _process; VETO/RTMW_DETERMINISTIC env) + assert_gates.py
    - backend/scripts/verify_self_comparison.py (33-17 added --reference-version — reference-as-student self-comparison, R-3)
    - .planning/phases/33-result-trust-recovery/33-COVERAGE-MATRIX.md (the single per-motion coverage source — which are 6-fixture, which are self-comparison, climb/sideway-spin/combo resolution)
    - .planning/debug/ref-student-substrate-gap.md `## 성공 판정 기준` (8 items, current-baseline margins: kip-up +15.8 / peter-pan +10.5 / power-spin +9.0 / elbow-twist +0.2 / pdshape −1.2) + R-2/R-3/R-6
  </read_first>
  <action>
    Point the sweep at the CANDIDATE via the shadow resolver: run run_sweep.py --reference-version phase33-cm3-run1 (33-17) so the candidate is consumed by hash WITHOUT flipping production top-level; the evidence must record the consumed candidate anglesHash (proving concern 3 is closed). Run the 6-fixture re-sweep SERIALLY (equal frequency per the coverage matrix — no kip-up bias, no parallel /analyze). Run assert_gates.py. For each success member compute 여유 = tol(20°) − max(관절 편차). For the fixture-less motions named in 33-COVERAGE-MATRIX.md, run verify_self_comparison.py --reference-version phase33-cm3-run1 (R-3 substitute) — no silent skip (D-23). Run pdshape twice for 2-run stability (R-6/item #7). Record the human-readable narrative in 33-S4-VERIFY-EVIDENCE.md AND emit a companion machine-gate input 33-S4-VERIFY-EVIDENCE.json (the 8-item PASS/FAIL map + numeric margin/separation/2-run-stability values + the consumed candidate hash) that gate_check.py can json.load — the .json is the gate input, the .md stays prose. Do NOT refit any threshold (item #8 / D-20/D-29).
  </action>
  <verify>
    <automated>test -f .planning/phases/33-result-trust-recovery/33-S4-VERIFY-EVIDENCE.md && grep -qE "phase33-cm3|SUNITY_SHADOW|reference-version" .planning/phases/33-result-trust-recovery/33-S4-VERIFY-EVIDENCE.md && grep -qE "여유|margin" .planning/phases/33-result-trust-recovery/33-S4-VERIFY-EVIDENCE.md && grep -qiE "self.comparison|verify_self" .planning/phases/33-result-trust-recovery/33-S4-VERIFY-EVIDENCE.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Sweep consumed the candidate via the shadow resolver; the consumed candidate anglesHash is recorded; production top-level NOT flipped (codex concern 3).
    - 6-fixture SERIAL equal frequency per the coverage matrix; per-fixture 여유 recorded; fixture-less motions self-comparison-verified per the matrix (no silent skip, D-23).
    - pdshape 2-run stability measured (item #7); the machine-gate input is emitted as 33-S4-VERIFY-EVIDENCE.json (json.load-parseable), separate from the .md narrative.
    - No threshold refit (item #8) — tol/slope/cap/epsilons unchanged.
    - 무엇을 열어서 확인했는가 (D-19): actual margin numbers + self-comparison scores + the candidate hash pasted, not "sweep passed."
  </acceptance_criteria>
  <done>Candidate margins + fixture-less self-comparison + pdshape stability collected via the shadow resolver into structured JSON; no refit.</done>
</task>

<task type="auto">
  <name>Task 3: JSON data-gate on the SEED 8 items (predeclared boundaries) + M8 eyeball + rollback eval + elbow-twist → 33-21 route</name>
  <read_first>
    - backend/scripts/gate_check.py (33-18 — --require-all-pass, --require-hashes 11, --no-rollback-trigger, --scoring-constants-match)
    - .planning/debug/ref-student-substrate-gap.md `## 성공 판정 기준` (8 items) + `## 롤백 트리거` + `## elbow-twist 처방 경로`
    - 33-REVIEWS.md codex concern 12 (predeclared numeric boundaries + oracle for 'gap significantly reduced' / pdshape '< ±30' / 'no new FP/FN') + concern 13 (elbow-twist HALT routes to a gap-closure plan, not inline; 33-07 stays blocked)
    - MEMORY verify-ui-on-simulator-before-ota (Xcode 26.6 Simulator) + backend/scripts/dump_analysis_doc.py (--download-pngs for the M8 eyeball)
  </read_first>
  <action>
    Judge the SEED 8-item gate with predeclared boundaries encoded as JSON gate fields, then run gate_check.py over the evidence JSON: (1) all 6 success 여유 > 0 incl. pdshape neg→pos; (2) fault/success separation ≥ current baselines (power-spin 45/peter-pan 21/elbow-twist 35/pdshape 53/kip-up 20); (3) inversion vs non-inversion 편차 바닥 gap reduced — PREDECLARE the boundary (e.g. the current 3× ratio must drop below a stated ratio/degree, name the oracle) per concern 12; (4) M8 consumer-side no regression — render the original-5 (per coverage matrix) overlay + fault-zoom crops in the iOS Simulator (axis (x,0,y)→(x,y,0), pickVerticalAxis), eyeball 전수 + screenshots (R-5); (5) M3 fires (segment trimmed in ≥1 member); (6) safety flags — PREDECLARE the oracle: new FP count == 0 AND new FN count == 0 vs the captured baseline; (7) pdshape 2-run edit < a PREDECLARED degree (state it, well below the old ±30); (8) threshold refit == 0 via gate_check --scoring-constants-match. Run gate_check.py --require-all-pass (8 items) --require-hashes 11 (candidate docs match the manifest) --no-rollback-trigger --scoring-constants-match; the gate's EXIT CODE is the verdict (not a grep). Evaluate the 4 rollback triggers; if ANY trips, do NOT flip — invoke the 33-07 rollback path and stop. ELBOW-TWIST (concern 13 / D-32ⓑ): if elbow-twist 여유 < +2.0°, RECORD the HALT and route to 33-21 (the formal HALT loop) — do NOT raise the belle question here, do NOT resolve inline, 33-07 stays blocked; if ≥ +2.0°, record the margin and note 33-21 will no-op. Write the machine 8-item PASS/FAIL verdict + numeric values into 33-S4-VERIFY-EVIDENCE.json (the gate_check input); write the narrative + M8 screenshots + rollback eval + elbow-twist route into 33-S4-VERIFY-EVIDENCE.md.
  </action>
  <verify>
    <automated>cd backend && python3 scripts/gate_check.py --file ../.planning/phases/33-result-trust-recovery/33-S4-VERIFY-EVIDENCE.json --require-all-pass gate1 gate2 gate3 gate4 gate5 gate6 gate7 gate8 --no-rollback-trigger 2>/dev/null; echo "gate_exit=$?"; grep -qiE "simulator|screenshot|M8" ../.planning/phases/33-result-trust-recovery/33-S4-VERIFY-EVIDENCE.md && grep -qiE "elbow-twist|33-21|HALT" ../.planning/phases/33-result-trust-recovery/33-S4-VERIFY-EVIDENCE.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All 8 SEED items judged with PREDECLARED numeric boundaries + oracles (concern 12); gate_check.py exit 0 required to unblock (JSON data-gate, not grep — concern 8); --require-hashes 11 confirms candidate docs match the manifest.
    - M8 original-5 overlay/fault-zoom rendered in the Simulator with screenshots (전수, R-5); a regression is a rollback trigger.
    - Rollback triggers evaluated via --no-rollback-trigger; any trip blocks the flip and invokes rollback (D-31).
    - Elbow-twist: if < +2.0°, the HALT is recorded and routed to 33-21 (no inline resolution, no belle question here, 33-07 blocked — concern 13); if ≥ +2.0°, the margin is recorded and 33-21 will no-op.
    - 이 산출물이 틀렸다면 어떻게 알았을까: answered — a negative margin, an M8 crop regression, a combo variance overrun, a new safety flag, or a candidate-hash mismatch each fails the gate_check exit code; the doc names any that did.
  </acceptance_criteria>
  <done>8-item gate judged by JSON data-gate with predeclared boundaries, M8 eyeball done, rollback clear (or invoked), elbow-twist routed to 33-21 — ALL PASS + no HALT is the presentation-track gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Pod → candidate shadow read | re-analysis against the candidate by hash; production top-level not flipped |
| production Lambda | dedicated eval Pod or drained during the serial sweep |
| regenerated crop PNG → Simulator | M8 consumer-side eyeball on original-5 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-37 | Tampering | verification runs against the OLD reference (no resolver) | mitigate | shadow --reference-version consumes the candidate by hash (concern 3) |
| T-33-38 | Repudiation | a FAIL doc passes a grep gate | mitigate | gate_check.py exit-code JSON data-gate (concern 8) |
| T-33-39 | Repudiation | non-decidable success gates | mitigate | predeclared numeric boundaries + oracles (concern 12) |
| T-33-40 | Tampering | production traffic contaminates the serial sweep | mitigate | dedicated eval Pod or drained Lambda (concern 14) |
| T-33-41 | Tampering | elbow-twist resolved inline while 33-07 flips | mitigate | route to 33-21 HALT loop; 33-07 depends_on 33-21 (concern 13) |
| T-33-SC | Tampering | package installs on Pod | accept | existing runbook |
</threat_model>

<verification>
- Sweep consumed the candidate via the shadow resolver (hash recorded); no production flip.
- 8-item SEED gate judged by gate_check.py (predeclared boundaries, --require-hashes 11, --no-rollback-trigger, --scoring-constants-match) — exit 0 required.
- 6-fixture equal frequency + fixture-less self-comparison per the coverage matrix; M8 Simulator eyeball; elbow-twist routed to 33-21; no refit (D-20/D-29).
</verification>

<success_criteria>
The candidate passes all 8 SEED criteria — consumed by hash via the shadow resolver, judged by a JSON data-gate with predeclared boundaries, covered per the single matrix, M8 clean, traffic isolated, and no threshold refit — with the elbow-twist branch routed to a formal HALT loop; ALL PASS + no HALT unblocks the presentation track, any failure invokes rollback (codex concerns 3, 8, 12, 13, 14).
</success_criteria>

<output>
Create `.planning/phases/33-result-trust-recovery/33-06-SUMMARY.md` when done, with the 8-item JSON PASS/FAIL, the consumed candidate hash, per-fixture margins, the predeclared boundaries used, M8 screenshots, and the elbow-twist margin + 33-21 route decision.
</output>
