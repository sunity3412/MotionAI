---
phase: 33-result-trust-recovery
plan: 07
type: execute
wave: 7
depends_on: ["33-06"]
files_modified:
  - .planning/phases/33-result-trust-recovery/33-S5-FLIP-EVIDENCE.md
autonomous: false
requirements: [D-18, D-19, D-21, D-25, D-27, D-28, D-30, D-31, D-32]
user_setup: []
must_haves:
  truths:
    - "The active reference pointer flips to the reprocessed 9fps+PR version ONLY after 33-06's 8-item gate is ALL PASS (SEED Task 5 / D-31)"
    - "Production activation is verified: PR_INVERSION_ENABLED=1 stays on, SSM/Lambda in sync, and a post-flip re-analysis of ≥1 fixture confirms production consumes the new substrate (D-19)"
    - "The rollback path is armed and left ready: rollback_reference_motions_phase4.py + the 33-02 backup + PR_INVERSION_ENABLED=0 kill switch (D-31)"
    - "This flip is the last substrate step; all presentation plans (33-08+) depend on it — no presentation work runs before this PASS (D-28)"
    - "belle flip greenlight is an operational/production-activation approval (pinned reference overwrite policy), not a design touchpoint (D-32ⓐ family)"
  artifacts:
    - path: ".planning/phases/33-result-trust-recovery/33-S5-FLIP-EVIDENCE.md"
      provides: "flip confirmation + post-flip re-analysis proof + rollback-armed record"
      contains: "activeVersion"
  key_links:
    - from: "backend/scripts/reprocess_reference_motions_phase4.py _flip_active_pointer (or flip step)"
      to: "reference/{id}.activeVersion + top-level mirror"
      via: "flip only after 33-06 PASS; --no-flip released"
      pattern: "_flip_active_pointer|activeVersion"
    - from: "33-S5-FLIP-EVIDENCE.md"
      to: "backend/scripts/rollback_reference_motions_phase4.py + 33-02 backup"
      via: "rollback path armed and documented"
      pattern: "rollback"
---

<objective>
Task 5 of the C+M3 substrate track (SEED Task 5): flip the active reference pointer to the reprocessed 9fps+PR version and activate it in production — ONLY after 33-06's 8-item gate is ALL PASS. This is the last substrate step and the precondition for the entire presentation track (D-28). The rollback path (rollback_reference_motions_phase4.py + the 33-02 backup + PR kill switch) is armed and kept ready (D-31). Pod + belle greenlight (D-30/D-32) — autonomous:false.

Purpose: Until the flip, production still scores against the old misaligned reference. This makes the fixed substrate live so all downstream presentation work (crops, copy, overlays, illustrations) builds on a correct root — the척추 principle. No scoring math change (D-20).

Output: 33-S5-FLIP-EVIDENCE.md (flip confirmation + post-flip re-analysis proof + rollback-armed record).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-result-trust-recovery/33-CONTEXT.md
@.planning/debug/ref-student-substrate-gap.md
@.planning/phases/33-result-trust-recovery/33-06-SUMMARY.md
@backend/scripts/reprocess_reference_motions_phase4.py
@backend/scripts/rollback_reference_motions_phase4.py
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: belle flip greenlight — production reference activation (D-32ⓐ ops / pinned-overwrite policy)</name>
  <action>
    PAUSE for belle's operational greenlight to flip the active reference pointer (production reference overwrite requires belle approval per pinned policy reference-v1-pinned-force-config). Present the 33-06 8-item gate verdict (ALL PASS required) and the elbow-twist margin outcome. This is a production-activation/ops greenlight (D-32ⓐ family — like Pod greenlight), NOT a design question; it does not count against the 2 design touchpoints. Do NOT flip if any 33-06 gate item is FAIL or any rollback trigger tripped — in that case invoke rollback instead.
  </action>
  <what-built>The reprocessed reference is written as versioned docs (33-03/04) and passed the 8-item gate (33-06); this gate requests belle to greenlight making it the ACTIVE production reference.</what-built>
  <how-to-verify>
    1. Review the 33-06 8-item PASS/FAIL table — all 8 must be PASS.
    2. Confirm no rollback trigger tripped; confirm you approve overwriting the production active reference.
  </how-to-verify>
  <verify>
    <automated>echo "checkpoint — belle flip greenlight (ops/production activation, not a design touchpoint)"</automated>
  </verify>
  <resume-signal>Type "flip" to activate the new reference, or "rollback" to restore.</resume-signal>
  <done>belle greenlit the flip after confirming 33-06 ALL PASS.</done>
</task>

<task type="auto">
  <name>Task 2: flip active pointer + production activation + verify + arm rollback</name>
  <read_first>
    - backend/scripts/reprocess_reference_motions_phase4.py:336-360 _flip_active_pointer (backs up current top-level to versions/pre_phase4, sets activeVersion, mirrors top-level 11 fields; flip-blocked unless all completed)
    - backend/scripts/rollback_reference_motions_phase4.py (--to-version pre_phase4 restore) + .planning/debug/ref-student-substrate-gap.md `## 롤백 설계 / 복원 절차` (6-step: PR kill → active pointer → top-level mirror from 33-02 backup set(merge=False) → measure_reference_fps → rescore → M3 code revert)
    - MEMORY current-pod-hbpvhedq2bu01i (SSM + Lambda RUNPOD_ANALYZE_URL resync) + reference-v1-pinned-force-config
  </read_first>
  <action>
    After belle greenlight and 33-06 ALL PASS: release --no-flip / run the flip step (_flip_active_pointer or the flip script) so reference/{id}.activeVersion + top-level mirror point to the reprocessed 9fps+PR version for all 11. Keep PR_INVERSION_ENABLED=1 in production Pod env; confirm SSM + Lambda RUNPOD_ANALYZE_URL are in sync. VERIFY activation (D-19): re-analyze ≥1 fixture (e.g. pdshape) through the production path and confirm it consumes the new active reference (dump the doc, confirm activeVersion + the new margin). ARM ROLLBACK: confirm rollback_reference_motions_phase4.py --to-version pre_phase4 is available, the 33-02 backup (local + S3) is intact, and the PR_INVERSION_ENABLED=0 kill switch procedure is documented — leave the rollback path ready (D-31). Note that no app-code OTA is needed for substrate (the app consumes reference via backend); the presentation-layer OTA is 33-16. Record everything in 33-S5-FLIP-EVIDENCE.md. Do NOT touch scoring math (D-20).
  </action>
  <verify>
    <automated>test -f .planning/phases/33-result-trust-recovery/33-S5-FLIP-EVIDENCE.md && grep -qiE "activeVersion|flip" .planning/phases/33-result-trust-recovery/33-S5-FLIP-EVIDENCE.md && grep -qiE "rollback" .planning/phases/33-result-trust-recovery/33-S5-FLIP-EVIDENCE.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Active pointer flipped for 11/11 (activeVersion + top-level mirror to the reprocessed version); flip ran only after 33-06 ALL PASS (D-31).
    - Production activation verified: a post-flip fixture re-analysis dumps the new activeVersion + the expected new margin (D-19) — not "flip script returned 0".
    - Rollback path armed: rollback script available + 33-02 backup intact (local + S3) + PR kill-switch documented (D-31).
    - No scoring math changed (D-20); SUMMARY states no app OTA needed for substrate (presentation OTA = 33-16).
    - 무엇을 열어서 확인했는가 (D-19): the post-flip re-analysis doc dump (activeVersion + margin) is pasted.
  </acceptance_criteria>
  <done>Reference activated in production after gate PASS, activation verified by re-analysis, rollback armed — substrate track complete, presentation track unblocked (D-28).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| flip → production active reference | activeVersion + top-level mirror overwrite; all new analyses consume it |
| Pod env → production analysis | PR_INVERSION_ENABLED stays on; SSM/Lambda RUNPOD_ANALYZE_URL sync |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-36 | Tampering | flip before gate PASS / bad reference goes live | mitigate | flip only after 33-06 ALL PASS + belle greenlight; flip-blocked unless all completed (D-31) |
| T-33-37 | Denial of service | flipped reference regresses production | mitigate | rollback armed (script + 33-02 backup + PR kill switch); post-flip re-analysis verify (D-31) |
| T-33-38 | Tampering | stale RUNPOD_ANALYZE_URL after Pod cycle | mitigate | SSM/Lambda resync confirmed before activation |
| T-33-SC | Tampering | package installs | accept | existing scripts; no new packages |
</threat_model>

<verification>
- Active pointer flipped 11/11 after 33-06 ALL PASS + belle greenlight.
- Production activation verified by post-flip fixture re-analysis (activeVersion + margin dumped).
- Rollback armed (script + 33-02 backup + PR kill switch); scoring untouched.
</verification>

<success_criteria>
The reprocessed 9fps+PR reference is live in production only after the 8-item gate PASS and belle greenlight, activation is proven by re-analysis, and the rollback path is armed — the substrate root is restored and every presentation plan can now build on a correct analysis (D-28).
</success_criteria>

<output>
Create `.planning/phases/33-result-trust-recovery/33-07-SUMMARY.md` when done, recording the flip, the post-flip re-analysis proof, and the armed rollback path.
</output>
