---
phase: 33-result-trust-recovery
plan: 07
type: execute
wave: 8
depends_on: ["33-06", "33-21", "33-17", "33-18"]
files_modified:
  - .planning/phases/33-result-trust-recovery/33-S5-FLIP-EVIDENCE.md
  - .planning/phases/33-result-trust-recovery/33-DEPLOYMENT-STATUS.md
autonomous: false
requirements: [D-18, D-19, D-21, D-25, D-27, D-28, D-30, D-31, D-32]
user_setup: []
must_haves:
  truths:
    - "C+M3 is activated as ONE release tuple {9fps candidate reference, PR=1, M3 code commit, derived fields}: activation verifies {commit SHA, candidate id, env} TOGETHER against the release manifest (33-18) — the M3 commit is deployed to the production Pod and its /health canary matches before the reference is made active (codex concern 5 / D-31)"
    - "The 11-doc flip is atomic-at-the-pointer + idempotent (33-17): the global release pointer reference/_release.activeCandidate is set, top-level mirrored with idempotent per-doc writes, versions/pre_phase4 NEVER overwritten, and ALL eleven docs are re-read post-write and hash-verified 11/11 against the manifest — a partial flip is detected, not shipped (codex concern 7)"
    - "Activation happens in a maintenance window with a deployment status document (33-DEPLOYMENT-STATUS.md) recording the tuple, the window, and the resumable step state — activation is resumable + auditable (codex suggestion 7 / belle full-scope)"
    - "Rollback restores the COMPATIBLE tuple (code+data+env) with NO mixed intermediate (never old-18fps-data + new-M3): PR kill switch + pointer restore + top-level restore from the 33-02 immutable backup + M3 code revert, all bound by the manifest (codex concern 5)"
    - "Flip runs ONLY after 33-06 ALL PASS AND 33-21 resolved (no HALT pending); presentation track (33-08+) gates on this (D-28); no scoring math (D-20)"
  artifacts:
    - path: ".planning/phases/33-result-trust-recovery/33-S5-FLIP-EVIDENCE.md"
      provides: "tuple-verified activation + 11/11 post-write hash + post-flip re-analysis + rollback-armed record"
      contains: "activeCandidate"
    - path: ".planning/phases/33-result-trust-recovery/33-DEPLOYMENT-STATUS.md"
      provides: "the deployment status document — release tuple, maintenance window, resumable step state"
      contains: "maintenance"
  key_links:
    - from: "release_manifest.py publish + 33-17 atomic flip"
      to: "reference/_release.activeCandidate + 11-doc top-level mirror"
      via: "one global pointer flip + idempotent mirror + 11/11 hash verify; pre_phase4 immutable"
      pattern: "activeCandidate|_release|pre_phase4"
    - from: "activation verify"
      to: "{commit SHA, candidate id, env} == manifest"
      via: "tuple checked together before + after flip"
      pattern: "commit|manifest|canary"
---

<objective>
Task 5 of the C+M3 substrate track (SEED Task 5), corrected per codex concerns 5/7 + suggestion 7 (belle full-scope). Activate C+M3 as ONE release tuple, not just a data flip: deploy the M3 commit to the PRODUCTION Pod and confirm its /health canary, then flip via the 33-17 atomic idempotent helper (global release pointer reference/_release + 11/11 post-write hash verify + immutable pre_phase4), inside a maintenance window with a deployment status document, and verify {commit SHA, candidate id, env} together against the release manifest (33-18). Rollback restores the compatible tuple with no mixed intermediate. ONLY after 33-06 ALL PASS AND 33-21 resolved. Pod + belle greenlight (D-30/D-32) — autonomous:false. No scoring math (D-20).

Purpose: The old plan flipped reference data but never deployed the M3 commit to production and allowed rollback to a mixed state (old-18fps-data + new-M3). C+M3 is valid ONLY as the full tuple; this makes activation and rollback operate on that tuple atomically — the척추 root goes live correct or not at all.

Output: 33-S5-FLIP-EVIDENCE.md (tuple-verified activation + 11/11 hash + post-flip re-analysis + rollback-armed) + 33-DEPLOYMENT-STATUS.md.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-result-trust-recovery/33-CONTEXT.md
@.planning/phases/33-result-trust-recovery/33-REVIEWS.md
@.planning/debug/ref-student-substrate-gap.md
@.planning/phases/33-result-trust-recovery/33-06-SUMMARY.md
@backend/scripts/reprocess_reference_motions_phase4.py
@backend/scripts/release_manifest.py
@backend/scripts/rollback_reference_motions_phase4.py
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: belle flip greenlight — production activation (D-32ⓐ ops / pinned-overwrite policy)</name>
  <read_first>
    - 33-06-SUMMARY.md (8-item JSON gate ALL PASS + elbow-twist margin + 33-21 route) + 33-21-SUMMARY.md (no-op or completed re-verification — must be resolved)
    - MEMORY reference-v1-pinned-force-config (production reference overwrite needs belle approval)
    - 33-REVIEWS.md codex concern 5 (deploy M3 commit + verify tuple) + concern 7 (atomic flip)
  </read_first>
  <action>
    PAUSE for belle's operational greenlight to activate the new reference (production overwrite requires belle approval per the pinned policy). Present: the 33-06 8-item JSON gate (ALL PASS required, verified by gate_check exit 0 against 33-S4-VERIFY-EVIDENCE.json — the machine sidecar, not the .md), the elbow-twist outcome, and 33-21's resolution (no-op OR completed re-verification — a pending HALT blocks this). This is a production-activation/ops greenlight (D-32ⓐ family), NOT a design question. Do NOT flip if any 33-06 gate item is FAIL, any rollback trigger tripped, or 33-21 is unresolved — invoke rollback instead.
  </action>
  <what-built>The candidate reference (33-03/04) passed the 8-item gate (33-06) and the elbow-twist HALT is resolved (33-21); this gate requests belle to greenlight making the tuple the ACTIVE production release.</what-built>
  <how-to-verify>
    1. Confirm 33-06 gate_check exit 0 against 33-S4-VERIFY-EVIDENCE.json (all 8 PASS) + no rollback trigger; release_manifest.py verify runs against the manifest JSON (data-gates read JSON, never a .md).
    2. Confirm 33-21 is resolved (no-op or re-verified); approve overwriting the production active reference.
  </how-to-verify>
  <verify>
    <automated>echo "checkpoint — belle flip greenlight (ops/production activation, not a design touchpoint)"</automated>
  </verify>
  <resume-signal>Type "flip" to activate the tuple, or "rollback" to restore.</resume-signal>
  <done>belle greenlit the flip after confirming 33-06 ALL PASS and 33-21 resolved.</done>
</task>

<task type="auto">
  <name>Task 2: deploy the M3 commit to production Pod + maintenance window + atomic tuple activation + 11/11 hash verify</name>
  <read_first>
    - backend/scripts/release_manifest.py (verify + publish the tuple to reference/_release; 33-18) + backend/scripts/reprocess_reference_motions_phase4.py _flip_active_pointer (33-17 made it idempotent: pre_phase4 immutable, 11/11 post-write hash, global pointer)
    - 33-18 /health canary (commit SHA + model canary) — the production Pod must match the M3 commit
    - .planning/debug/ref-student-substrate-gap.md `## 롤백 설계` (pre_phase4 = rollback source; must survive) + MEMORY current-pod (SSM + Lambda RUNPOD_ANALYZE_URL resync)
    - 33-REVIEWS.md codex concern 5 (tuple deploy) + concern 7 (atomic 11-doc flip) + suggestion 7 (maintenance window + deployment status doc + resumable + never overwrite pre_phase4)
  </read_first>
  <action>
    Open a maintenance window and write 33-DEPLOYMENT-STATUS.md recording the release tuple (candidate id, commit SHA, targetFps=9.0, PR=1, deterministic=1, derived schema version) + the window + a resumable step checklist. (a) DEPLOY the M3 commit to the PRODUCTION Pod: check out the exact commit (the one from 33-05), start_server with PR_INVERSION_ENABLED=1 + SUNITY_COMMIT_SHA, resync SSM + Lambda RUNPOD_ANALYZE_URL, and CONFIRM /health canary == the M3 commit + model green (concern 5 — the tuple's code half). (b) release_manifest.py verify (11 candidate docs match manifest hashes) then publish (write reference/_release.activeCandidate = the candidate + the full tuple). (c) Run the 33-17 idempotent atomic flip: set the global pointer, mirror top-level with idempotent per-doc writes, NEVER overwrite versions/pre_phase4, then re-read ALL 11 docs and hash-verify 11/11 against the manifest — abort/rollback on any mismatch (partial flip caught). (d) Verify the tuple TOGETHER: {production /health commit SHA, reference/_release.activeCandidate, PR/deterministic env} all match the manifest. Update 33-DEPLOYMENT-STATUS.md to "activated" only after 11/11 verify. Do NOT touch scoring (D-20). Record in 33-S5-FLIP-EVIDENCE.md.
  </action>
  <verify>
    <automated>test -f .planning/phases/33-result-trust-recovery/33-S5-FLIP-EVIDENCE.md && test -f .planning/phases/33-result-trust-recovery/33-DEPLOYMENT-STATUS.md && grep -qiE "activeCandidate|_release" .planning/phases/33-result-trust-recovery/33-S5-FLIP-EVIDENCE.md && grep -qiE "11/11|hash" .planning/phases/33-result-trust-recovery/33-S5-FLIP-EVIDENCE.md && grep -qiE "commit|canary" .planning/phases/33-result-trust-recovery/33-S5-FLIP-EVIDENCE.md && grep -qiE "maintenance|resumable" .planning/phases/33-result-trust-recovery/33-DEPLOYMENT-STATUS.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The M3 commit is deployed to the PRODUCTION Pod and /health canary matches it (the tuple's code half — concern 5), before the reference is made active.
    - Flip is atomic-at-the-pointer + idempotent: reference/_release.activeCandidate set, top-level mirrored idempotently, pre_phase4 NOT overwritten, 11/11 post-write hash verified against the manifest (concern 7).
    - {commit SHA, candidate id, env} verified TOGETHER against the manifest; 33-DEPLOYMENT-STATUS.md records the tuple + window + resumable state and is marked activated only after 11/11.
    - No scoring math changed (D-20); no app OTA needed for substrate (presentation OTA = 33-16).
    - 무엇을 열어서 확인했는가 (D-19): the 11/11 hash-verify output + the tuple-match (commit/candidate/env) are pasted.
  </acceptance_criteria>
  <done>C+M3 activated as one tuple: M3 commit live, global pointer flipped, 11/11 hash-verified, tuple-checked, deployment status recorded.</done>
</task>

<task type="auto">
  <name>Task 3: post-flip re-analysis proof + arm tuple-consistent rollback (no mixed intermediate)</name>
  <read_first>
    - backend/scripts/rollback_reference_motions_phase4.py (--to-version pre_phase4 restore) + .planning/debug/ref-student-substrate-gap.md `## 복원 절차` (PR kill → pointer restore → top-level from 33-02 backup set(merge=False) → measure_reference_fps → rescore → M3 code revert)
    - backend/scripts/dump_analysis_doc.py (post-flip re-analysis dump) + 33-02-SUMMARY.md (backup local+S3 + restore rehearsal)
    - 33-REVIEWS.md codex concern 5 (rollback restores the compatible tuple; NO mixed intermediate like old-18fps-data + new-M3)
  </read_first>
  <action>
    VERIFY activation by re-analysis (D-19): re-analyze ≥1 fixture (pdshape) through the production path and dump the doc — confirm it resolved reference/_release.activeCandidate (new candidate) + the expected new margin from 33-06. ARM the tuple-consistent rollback (concern 5): document the ONE rollback runbook that restores the COMPATIBLE tuple together — PR_INVERSION_ENABLED=0 kill switch + reference/_release pointer restore + top-level restore from the 33-02 immutable backup (set(merge=False)) + M3 code revert — with an explicit rule that NO mixed intermediate is allowed (never old-18fps-data with new-M3; the manifest binds code+data+env). Confirm rollback_reference_motions_phase4.py + the 33-02 backup (local+S3, rehearsal-proven) + the kill switch are all ready. Update 33-DEPLOYMENT-STATUS.md with the armed-rollback state and close the maintenance window. Record in 33-S5-FLIP-EVIDENCE.md. No scoring (D-20).
  </action>
  <verify>
    <automated>grep -qiE "activeCandidate|re-analy|margin" .planning/phases/33-result-trust-recovery/33-S5-FLIP-EVIDENCE.md && grep -qiE "rollback" .planning/phases/33-result-trust-recovery/33-S5-FLIP-EVIDENCE.md && grep -qiE "mixed|tuple|kill switch|PR_INVERSION_ENABLED=0" .planning/phases/33-result-trust-recovery/33-S5-FLIP-EVIDENCE.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Post-flip re-analysis dumps the resolved active candidate + the expected new margin (D-19) — not "flip returned 0".
    - Tuple-consistent rollback runbook armed: PR kill + pointer restore + top-level from 33-02 immutable backup + M3 code revert, with the no-mixed-intermediate rule stated (concern 5).
    - rollback script + 33-02 backup (local+S3, rehearsal-proven) + kill switch all confirmed ready; deployment status closed.
    - 이 산출물이 틀렸다면 어떻게 알았을까: answered — a post-flip doc still on the old candidate, or a rollback path that could leave old-data+new-code, would show here; the tuple binding + re-analysis catch it.
  </acceptance_criteria>
  <done>Activation proven by re-analysis; the tuple-consistent rollback is armed with no mixed-intermediate path — substrate track complete, presentation unblocked (D-28).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| release tuple → production | {code commit, candidate data, env} activated together; verified before + after |
| global pointer + 11-doc mirror | atomic-at-the-pointer flip; pre_phase4 immutable; 11/11 hash-verified |
| rollback → compatible tuple | restore code+data+env together; no mixed intermediate |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-42 | Tampering | reference flipped but M3 code not deployed (partial tuple) | mitigate | deploy M3 commit + /health canary match before flip; verify {commit,candidate,env} together (concern 5) |
| T-33-43 | Tampering | non-atomic 11-doc flip leaves mixed library | mitigate | idempotent flip + 11/11 post-write hash + immutable pre_phase4 (concern 7) |
| T-33-44 | Denial of service | rollback to old-18fps-data + new-M3 | mitigate | tuple-consistent rollback runbook; no mixed intermediate (concern 5) |
| T-33-45 | Tampering | stale RUNPOD_ANALYZE_URL after Pod cycle | mitigate | SSM/Lambda resync + /health canary before activation |
| T-33-SC | Tampering | package installs | accept | existing scripts |
</threat_model>

<verification>
- M3 commit deployed to production Pod, /health canary matches; tuple {commit, candidate, env} verified against the manifest.
- Atomic idempotent flip: global pointer + idempotent mirror + immutable pre_phase4 + 11/11 post-write hash verify; maintenance window + deployment status doc; resumable.
- Post-flip re-analysis proves the active candidate; tuple-consistent rollback armed (no mixed intermediate); scoring untouched (D-20).
</verification>

<success_criteria>
C+M3 goes live only after 33-06 ALL PASS and 33-21 resolved, as one verified tuple {9fps candidate, PR=1, M3 commit, derived fields} activated via an atomic global-pointer flip with 11/11 hash verification and an immutable rollback preimage, inside an audited maintenance window — activation proven by re-analysis and a tuple-consistent rollback armed with no mixed intermediate (codex concerns 5, 7; suggestion 7).
</success_criteria>

<output>
Create `.planning/phases/33-result-trust-recovery/33-07-SUMMARY.md` when done, recording the tuple activation ({commit, candidate, env} match), the 11/11 hash verify, the post-flip re-analysis proof, the deployment status, and the armed tuple-consistent rollback.
</output>
