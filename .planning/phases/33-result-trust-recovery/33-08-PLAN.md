---
phase: 33-result-trust-recovery
plan: 08
type: execute
wave: 8
depends_on: ["33-07"]
files_modified:
  - .planning/phases/33-result-trust-recovery/33-A1-MOTION-STANDARDS.md
autonomous: true
requirements: [D-09, D-18, D-19, D-23, D-28]
must_haves:
  truths:
    - "This plan runs only after the substrate track (33-07) is PASS — the presentation track's first plan gates on the flipped reference (D-28)"
    - "Every registered motion has a fixed 4-질의 row (완성 기준 / 흔한 실패 / 강사 교정 어휘 / 강사가 보라는 부위·순간) — not free-form IPSF reading (D-09)"
    - "The table covers the 6 fixture motions first, then extends to the 11 registered motions; fixture-less motions are marked with their substitute evidence source (D-09/D-23)"
    - "Each 완성 기준 / 방향 claim is cross-checked against the actual (reprocessed) reference-motion frame — a claim that contradicts the ref frame is corrected or dropped, not shipped (D-18)"
    - "The table uses the studio's existing vocabulary — the app invents no new notation (D-09)"
  artifacts:
    - path: ".planning/phases/33-result-trust-recovery/33-A1-MOTION-STANDARDS.md"
      provides: "동작 × 4질의 공통 재료 표 — feeds coaching copy (A-2/33-09), crop target selection (A-3/33-10), illustration subject (A-7/33-14)"
      contains: "완성 기준"
  key_links:
    - from: "33-A1-MOTION-STANDARDS.md"
      to: "NotebookLM 폴스포츠 노트북 + field-research-stakeholders"
      via: "domain sourcing per query, not whole-IPSF read"
      pattern: "완성 기준|흔한 실패"
    - from: "33-A1-MOTION-STANDARDS.md"
      to: "REGISTERED_MOTIONS (gemini_motion_classifier.py) + activatedCriteria per motion"
      via: "row per registered motion, scoped to emitted criteria"
      pattern: "ref-power-spin|kip-up"
---

<objective>
Produce the 동작별 기준 자세 조사 표 (D-09): for each registered motion, answer exactly 4 fixed queries — (1) 완성 기준 (what counts as done well) → drives coaching copy + crop target, (2) 흔한 실패 → coaching copy + illustration subject, (3) 강사가 교정할 때 쓰는 말 → coaching vocabulary, (4) 강사가 어디를(부위) 어느 순간에 보라 하는가 → what the fault-zoom crop must cut. This is the first presentation plan and gates on the substrate track (33-07) being PASS (D-28). The table is the shared material consumed by A-2 (phrasebook/33-09), A-3 (crop design/33-10), and A-7 (illustration/33-14).

Purpose: D-24 forbids implementing before investigating. The current 확대비교/phrasebook broke precisely because they were built without a domain-truth substrate. This plan fixes that at the fact layer — and it is the cheapest, code-change-0 root for the "왜 천장으로 뻗으라 하나" trust break (belle's power-spin question). Cross-checks now use the REPROCESSED reference frames (post-flip), so the facts align to the corrected substrate.

Output: `33-A1-MOTION-STANDARDS.md` — one table, 6 fixture motions first, extended to 11 registered.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-result-trust-recovery/33-CONTEXT.md
@.planning/phases/33-result-trust-recovery/33-PLANNING-APPROACH.md
@.planning/phases/33-result-trust-recovery/33-RESEARCH.md
@.planning/phases/33-result-trust-recovery/33-07-SUMMARY.md
@backend/evals/phase25/baseline/phase25_sweep_report.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: 6 fixture motions — 4질의 표 + reprocessed-reference-frame cross-check</name>
  <read_first>
    - 33-PLANNING-APPROACH.md §1 (the 4 queries + sourcing rule) + §"코칭 문구는 방법이 다르다" (facts first)
    - 33-07-SUMMARY.md (confirm substrate is flipped/active — cross-checks use the reprocessed reference)
    - backend/shared/python/sunity_shared/analysis/gemini_motion_classifier.py (REGISTERED_MOTIONS) — the motion keys
    - backend/shared/python/sunity_shared/analysis/ipsf_criteria.py (CRITERION_GROUPS) — the criteria vocabulary
    - backend/evals/phase25/baseline/phase25_sweep_report.json (per-motion activatedCriteria — scope which criteria each motion actually emits)
    - MEMORY notebook-lm-pole-sports + field-research-stakeholders (domain sources)
  </read_first>
  <action>
    For the 6 fixture motions (power-spin, peter-pan, elbow-twist-sister, pdshape, kip-up, climb), write one row each answering the 4 fixed D-09 queries. Source domain facts from the NotebookLM 폴스포츠 노트북 (IPSF Code of Points lookup) + field-research notes — query per motion, do NOT read all of IPSF. For query 1 (완성 기준) and query 4 (어디를 보라), state the joint/body-part and the direction (e.g. power-spin: legs extend to the SIDES, not toward the ceiling) so the coaching copy and crop can consume it. Scope each row's criteria to what that motion actually emits (read activatedCriteria from the sweep report). CRITICAL cross-check: for every 완성 기준/direction claim, open the actual REPROCESSED reference-motion frame for that motion (via dump_analysis_doc.py / reference doc) and confirm the claim matches what 정은지 actually does — if it contradicts, correct the claim or mark it UNVERIFIED (do not ship a contradicting fact). Use only vocabulary the studio already uses (D-09) — invent no new notation.
  </action>
  <verify>
    <automated>test -f .planning/phases/33-result-trust-recovery/33-A1-MOTION-STANDARDS.md && grep -c "완성 기준" .planning/phases/33-result-trust-recovery/33-A1-MOTION-STANDARDS.md</automated>
  </verify>
  <acceptance_criteria>
    - All 6 fixture motions have a complete 4-query row; power-spin's row explicitly records legs-to-the-SIDE (the belle "천장" contradiction fixed at the fact layer).
    - Each row's criteria are scoped to that motion's actual activatedCriteria (cited from sweep report), not the full criterion set.
    - 틀리면 걸리는 장치 (D-18): every direction claim carries a "ref frame confirms" or "UNVERIFIED" tag — a claim that contradicts the opened reprocessed reference frame is caught and not asserted as fact.
    - 무엇을 열어서 확인했는가 (D-19): the task cites which reprocessed reference-motion frames were opened per motion to verify direction.
  </acceptance_criteria>
  <done>6 fixture motions have verified 4-query rows with direction confirmed against opened reprocessed ref frames.</done>
</task>

<task type="auto">
  <name>Task 2: extend to 11 registered motions + name fixture-less substitute evidence</name>
  <read_first>
    - REGISTERED_MOTIONS (gemini_motion_classifier.py) — the full registered list
    - 33-RESEARCH.md Assumption A4 (fixture-less registered motions: ref-foxtop, ref-foxtop-split, ref-invert, ref-sideway-spin — no paired fixture)
    - 33-VALIDATION.md §"Wave 0 Requirements" (fixture-less motions need named substitute verification per D-23)
  </read_first>
  <action>
    Extend the table to cover the remaining registered motions up to the 11-motion registered set, including the 4 fixture-less ones (ref-foxtop, ref-foxtop-split, ref-invert, ref-sideway-spin). For fixture-less motions, source the 4 queries from domain material + the reprocessed reference-motion doc and EXPLICITLY mark the substitute evidence used ("no fixture mp4 — verified against reprocessed reference doc {id}"), because D-23 forbids silent skips. Where a motion cannot be verified even against a reference doc, mark that row UNVERIFIED and note what could not be checked (D-18 전수 불가 시 무엇을 못 봤는지 명시).
  </action>
  <verify>
    <automated>grep -qE "ref-foxtop|ref-invert|ref-sideway-spin" .planning/phases/33-result-trust-recovery/33-A1-MOTION-STANDARDS.md && echo "REGISTERED EXTENDED"</automated>
  </verify>
  <acceptance_criteria>
    - The 4 fixture-less registered motions each have a row with a named substitute evidence source (reprocessed reference doc id) OR an explicit UNVERIFIED tag stating what was not checkable (no silent skip, D-23/D-18).
    - The table's coverage equals the registered-motion set; any gap is visible, not implicit.
    - 이 산출물이 틀렸다면 어떻게 알았을까: answered — every direction claim is tagged verified/UNVERIFIED against an opened frame or a named reason it could not be opened.
  </acceptance_criteria>
  <done>Registered-motion coverage complete; fixture-less rows carry named substitute proof or explicit UNVERIFIED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none — documentation-only) | This plan writes a domain-fact table; no code, no runtime, no data write |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-39 | Tampering | authored domain facts vs reality | mitigate | every direction claim cross-checked against opened reprocessed reference frame; UNVERIFIED tagging (D-18) |
| T-33-SC | Tampering | package installs | accept | no installs (documentation task) |
</threat_model>

<verification>
- 33-A1-MOTION-STANDARDS.md exists with all 6 fixture rows + registered extension.
- power-spin row records legs-to-side (belle contradiction resolved at fact layer).
- fixture-less motions carry named substitute evidence or explicit UNVERIFIED.
</verification>

<success_criteria>
One 동작 × 4질의 table exists as the verified shared material for A-2/A-3/A-7, with every direction claim confirmed against a real (reprocessed) reference frame or explicitly flagged unverified.
</success_criteria>

<output>
Create `.planning/phases/33-result-trust-recovery/33-08-SUMMARY.md` when done.
</output>
