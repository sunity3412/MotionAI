---
phase: 33-result-trust-recovery
plan: 09
type: execute
wave: 10
depends_on: ["33-08", "33-20"]
files_modified:
  - backend/tests/phase33/test_phrasebook_motion_specific.py
  - backend/data/phrasebook.json
autonomous: true
requirements: [D-02, D-14, D-18, D-19, D-20, D-23]
must_haves:
  truths:
    - "Registered motion x emitted criterion combos resolve a motion-specific cueLine BEFORE __common__ — power-spin no longer speaks the ceiling cue (D-14)"
    - "The fix is data-only: phrasebook.json gains motion_key.criterion entries; phrasebook.py is unchanged (code-change-0, D-14)"
    - "Every authored cueLine is direction-checked against the reprocessed ref-motion frame (전수); a contradicting cue is not authored — it stays fail-closed (D-14/D-18)"
    - "The same cueLine is what Polly speaks and the subtitle shows — fixing the data fixes voice + subtitle at once (RESEARCH Pattern 4)"
    - "The fix is global across all registered motions, not power-spin-specific (D-02); no scoring touched (D-20)"
  artifacts:
    - path: "backend/data/phrasebook.json"
      provides: "motion-specific entries with the same 6-slot schema; _meta.coverageMatrix updated in lockstep"
      contains: "ref-power-spin."
    - path: "backend/tests/phase33/test_phrasebook_motion_specific.py"
      provides: "asserts motion-specific override wins over __common__ + forbidden digit/percent gate"
      contains: "assemble_phrases"
  key_links:
    - from: "backend/data/phrasebook.json"
      to: "backend/shared/python/sunity_shared/analysis/phrasebook.py:143"
      via: "motion_key.criterion lookup already supported; data fills it"
      pattern: "motion_key"
    - from: "phrasebook cueLine"
      to: "pipeline/app.py _synthesize_coach_audio_items"
      via: "voice reads the same authored string"
      pattern: "cueLine"
---

> **[2026-07-28 수정 — belle 결정: A-트랙 먼저, flip 이연]** depends_on 에서 33-07 제거.
> 방향 일치 게이트의 기준 프레임 = **staged candidate** reference (phase33-cm3-*, 33-17
> shadow resolver) — flip 후 활성화될 데이터와 동일하므로 재작업 없음. flip 은 33-16 전 완료 조건.

<objective>
Author motion-specific coaching copy in phrasebook.json (D-14). Today all 13 entries are __common__.{criterion}, so every motion gets the same cue — power-spin (legs to the side) is told to push toward the ceiling because it falls through to the common leg_extension cue. The lookup machine (phrasebook.py:143) already resolves motion_key.criterion then __common__ then fail-closed; the data is simply empty. This plan fills the data, scoped to the combos each motion actually emits, with a hard direction-consistency gate against the REPROCESSED reference frames (post-flip substrate).

Purpose: The cheapest, highest-trust-value root fix (D-11 orders it first in the presentation track) — code-change-0, and it corrects the most damaging trust break (a wrong cue is spoken aloud by Polly). The hard gate (D-14): a cue that contradicts the reference motion's actual direction is not authored — fail-closed beats a confident wrong cue.

Output: phrasebook.json motion-specific entries + a RED-first test asserting override precedence + the forbidden-digit gate.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-result-trust-recovery/33-A1-MOTION-STANDARDS.md
@.planning/phases/33-result-trust-recovery/33-COVERAGE-MATRIX.md
@.planning/phases/33-result-trust-recovery/33-RESEARCH.md
@.planning/phases/33-result-trust-recovery/33-PATTERNS.md
@backend/data/phrasebook.json
@backend/shared/python/sunity_shared/analysis/phrasebook.py
@backend/tests/phase32/test_phrasebook_assembly.py
@backend/tests/phase32/test_phrasebook_forbidden.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — test_phrasebook_motion_specific.py</name>
  <read_first>
    - backend/tests/phase32/test_phrasebook_assembly.py:11-20 (import + _is_flat_scalar_dict helper)
    - backend/tests/phase32/test_phrasebook_forbidden.py:19-24,46-55 (forbidden walker + sanity nonzero companion)
    - 33-PATTERNS.md section test_phrasebook_motion_specific.py (priority assertion skeleton)
    - backend/shared/python/sunity_shared/analysis/phrasebook.py:143-151 (lookup order)
  </read_first>
  <behavior>
    - test_motion_specific_overrides_common: assemble_phrases for a registered motion+criterion returns a cueLine differing from an unregistered motion + same criterion (override resolves before __common__).
    - returned entry is a flat scalar dict.
    - forbidden gate: no authored cueLine/statusLine/whyLine has a raw digit or percent; sanity companion asserts more than zero strings walked.
  </behavior>
  <action>
    Create backend/tests/phase33/test_phrasebook_motion_specific.py from the phase32 analogs. Add test_motion_specific_overrides_common (RED until Task 2) and reuse the forbidden-copy gate plus its non-zero sanity companion verbatim from test_phrasebook_forbidden.py. Do NOT modify phrasebook.py.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/phase33/test_phrasebook_motion_specific.py -q -k overrides_common</automated>
  </verify>
  <acceptance_criteria>
    - The override test FAILS before Task 2 (RED) — it exercises the missing data, not a no-op green.
    - The forbidden-copy gate and non-zero sanity companion are present (grep sanity).
    - 틀리면 걸리는 장치 (D-18): override test red if data missing; forbidden gate red if a digit/percent appears.
  </acceptance_criteria>
  <done>Test file exists and is RED for the right reason.</done>
</task>

<task type="auto">
  <name>Task 2: author motion-specific entries + direction hard-gate (against reprocessed reference)</name>
  <read_first>
    - backend/data/phrasebook.json:77-84 (6-slot schema) + _meta.coverageMatrix
    - .planning/phases/33-result-trust-recovery/33-A1-MOTION-STANDARDS.md (완성 기준 + direction per motion)
    - backend/evals/phase25/baseline/phase25_sweep_report.json (activatedCriteria per motion — scope, not all 198 cells)
    - backend/data/corrective_exercises.json (exerciseId must be an existing defect key)
    - backend/shared/python/sunity_shared/analysis/phrasebook.py:105-115 (_fail_closed_slots shape)
  </read_first>
  <action>
    Add motion_key.criterion keys to phrasebook.json entries using the SAME 6-slot schema. Author copy from the A-1 table's verified direction — power-spin leg cue says 옆으로, not 천장. Scope authoring to the combos each motion actually emits (activatedCriteria from the sweep report). exerciseId references an existing corrective_exercises.json key. All slot copy digit/percent-free. Update _meta.coverageMatrix in lockstep. HARD GATE (D-14): for EVERY new cueLine open the REPROCESSED reference-motion frame and confirm direction matches what 정은지 does (전수 대조 via dump helper / reference doc); any contradicting cue is DELETED and its combo left fail-closed (statusLine/whyLine/coachQuestion only). Global across all registered motions (D-02). Do NOT touch scoring (D-20) or phrasebook.py. Fixture-less registered motions (ref-foxtop, ref-foxtop-split, ref-invert, ref-sideway-spin): author from A-1 substitute rows and tag with the reference doc used (D-23 no silent skip).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/phase33/test_phrasebook_motion_specific.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Override test now PASSES; forbidden-digit/percent gate passes.
    - phrasebook.json has non-__common__ entry keys (grep ref-power-spin.); _meta.coverageMatrix override count no longer 0.
    - phrasebook.py byte-unchanged; git diff --stat limited to data/phrasebook.json + tests/phase33 (scoring untouched, D-20).
    - 틀리면 걸리는 장치 (D-18): direction 전수 대조 against reprocessed reference caught contradictions and dropped them to fail-closed; SUMMARY lists every combo authored vs fail-closed.
    - 무엇을 열어서 확인했는가 (D-19): emitted cueLine VALUES were printed and each opened against its reprocessed reference frame — not "test passed."
  </acceptance_criteria>
  <done>Motion-specific copy authored, direction-verified 전수 against reprocessed reference, contradictions fail-closed; voice + subtitle inherit the corrected cueLine on next analysis.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| phrasebook.json data → pipeline assembly → Polly/subtitle | authored copy becomes spoken voice + subtitle |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-40 | Tampering/Repudiation | authored cueLine vs reference reality | mitigate | direction 전수 대조 vs reprocessed ref frame; contradiction → fail-closed (D-14) |
| T-33-41 | Information disclosure | none new | accept | data-file edit only; no secrets/endpoint |
| T-33-SC | Tampering | package installs | accept | no installs (data + test) |
</threat_model>

<verification>
- pytest backend/tests/phase33/test_phrasebook_motion_specific.py passes.
- git diff --stat limited to data/phrasebook.json + tests/phase33 (phrasebook.py + scoring untouched).
- Emit-value dump of authored cueLines opened against reprocessed reference frames (전수), documented in SUMMARY.
</verification>

<success_criteria>
Every registered motion x emitted criterion resolves a direction-correct, digit-free motion-specific cue (or fail-closed), power-spin no longer says 천장, change is data-only, voice + subtitle inherit the fix on next analysis.
</success_criteria>

<output>
Create `.planning/phases/33-result-trust-recovery/33-09-SUMMARY.md` when done, listing each authored combo vs each fail-closed combo.
</output>
