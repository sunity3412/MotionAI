---
phase: 33-result-trust-recovery
plan: 10
type: execute
wave: 10
depends_on: ["33-06", "33-07", "33-08", "33-09"]
files_modified:
  - .planning/phases/33-result-trust-recovery/33-PHASE-GATE-EVIDENCE.md
autonomous: false
requirements: [D-01, D-19, D-21, D-23, D-25]
user_setup: []
must_haves:
  truths:
    - "All 6 fixture motions are re-analyzed serially on the Pod (rebooted from OFF) — regenerating stored crops AND coach voice from the new phrasebook (D-19/D-23/D-25)"
    - "Every regenerated crop PNG and coach mp3 is OPENED/played (전수) — code-pass is not verification (D-19)"
    - "Fixture-less registered motions are verified via their named substitute (belle real docs), no silent skip (D-23)"
    - "Every changed screen renders in the iOS Simulator without crash/overlap, screenshot captured, BEFORE any OTA (D-21)"
    - "belle runs the device UAT and the 5 questions are answered by the screen itself — touchpoint 2 of 2 (D-01)"
  artifacts:
    - path: ".planning/phases/33-result-trust-recovery/33-PHASE-GATE-EVIDENCE.md"
      provides: "6-motion re-sweep results + opened-PNG/played-mp3 log + Simulator screenshots + fixture-less substitute proof"
      contains: "전수"
  key_links:
    - from: "33-PHASE-GATE-EVIDENCE.md"
      to: "backend/evals/phase25/run_sweep.py + assert_gates.py"
      via: "serial 6-motion re-sweep on rebooted Pod"
      pattern: "run_sweep|assert_gates"
    - from: "regenerated crops/voice"
      to: "opened PNG files + played mp3"
      via: "전수 eyeball/ear per D-19"
      pattern: "faultZoomComparisons|coachAudio"
---

<objective>
The phase gate. Reboot the Pod (rbpnmxhbfoeg35, OFF, volume alive — belle-gated operational start per D-25), re-analyze all 6 fixture motions serially (pipeline not concurrency-safe), regenerating stored crops (from A-5) and coach voice (from A-2 phrasebook). Then OPEN every regenerated crop PNG and PLAY every coach mp3 (전수, D-19), verify fixture-less registered motions via their named substitute (belle real docs, D-23), render every changed screen in the iOS Simulator (no crash/overlap, screenshot) BEFORE any OTA (D-21), and take belle 확인 ② — the device UAT (touchpoint 2 of 2, D-01).

Purpose: Editing code does nothing to stored crop PNGs / coach mp3 (RESEARCH Runtime State) — only re-analysis regenerates them. This gate is where the "눈으로 확인 의무" is discharged 전수, so belle receives a verified build, not a code-passed one.

Output: 33-PHASE-GATE-EVIDENCE.md (sweep + opened-artifact log + screenshots) + belle UAT approval.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-result-trust-recovery/33-CONTEXT.md
@.planning/phases/33-result-trust-recovery/33-VALIDATION.md
@.planning/phases/33-result-trust-recovery/33-RESEARCH.md
@backend/evals/phase25/run_sweep.py
@backend/scripts/dump_analysis_doc.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pod reboot + 6-motion serial re-sweep + 전수 open regenerated crops/voice</name>
  <files>.planning/phases/33-result-trust-recovery/33-PHASE-GATE-EVIDENCE.md</files>
  <read_first>
    - 33-CONTEXT.md D-25 (Pod rbpnmxhbfoeg35 OFF, volume alive; reboot precondition, belle-gated operational start) + MEMORY current-pod runbook (bootstrap→start_server→VETO env→X-RunPod-Token smoke→SSM+Lambda resync)
    - backend/evals/phase25/run_sweep.py (serial in-process _process, Pod GPU+Gemini) + assert_gates.py
    - backend/scripts/dump_analysis_doc.py (dump + --download-pngs to open regenerated artifacts)
    - 33-RESEARCH.md Runtime State (stored crops/mp3 only regenerate on re-analysis) + Environment Availability (Pod missing, no fallback)
  </read_first>
  <action>
    PRECONDITION (D-25): reboot Pod rbpnmxhbfoeg35 (volume alive) — belle greenlights the operational spin-up (cost/GPU), then Claude runs bootstrap → start_server → set VETO env → X-RunPod-Token smoke → resync SSM + Lambda RUNPOD_ANALYZE_URL (proxy URL changes on reboot). Run the 6-motion re-sweep SERIALLY via run_sweep.py (pipeline not concurrency-safe — no parallel /analyze) so crops regenerate from the A-5 fault_zoom fix and coach voice regenerates from the A-2 phrasebook. Run assert_gates.py. Then OPEN every regenerated faultZoomComparisons[].png (download via dump_analysis_doc --download-pngs, 전수 across all 6 motions) and PLAY every coachAudio mp3 — confirm same-moment/same-scale pairs, joint-exact item↔crop, ref side drawn, and correct spoken direction. For the 4 fixture-less registered motions (ref-foxtop, ref-foxtop-split, ref-invert, ref-sideway-spin), verify via belle real docs (named substitute, D-23) — do not silent-skip. Record everything in 33-PHASE-GATE-EVIDENCE.md. Do NOT touch scoring (D-20).
  </action>
  <verify>
    <automated>test -f .planning/phases/33-result-trust-recovery/33-PHASE-GATE-EVIDENCE.md && grep -qE "전수|re-sweep|assert_gates" .planning/phases/33-result-trust-recovery/33-PHASE-GATE-EVIDENCE.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All 6 fixture motions re-analyzed serially on the rebooted Pod; assert_gates.py result recorded.
    - Every regenerated crop PNG opened (전수) and every coach mp3 played — the evidence doc lists each file opened, not "sweep passed" (D-19).
    - Fixture-less registered motions verified via named belle-doc substitute (D-23 no silent skip).
    - 틀리면 걸리는 장치 (D-18): a mismatched pair / wrong-direction voice / shoulder-on-split crop would be visible in the opened artifact; the doc states none remain OR lists what does and blocks belle.
    - 이 산출물이 틀렸다면 어떻게 알았을까: answered — the 전수 open/play is the catch; a single un-opened artifact means the gate is incomplete.
  </acceptance_criteria>
  <done>6-motion re-sweep done, all regenerated crops/voice opened 전수, fixture-less motions substitute-verified, evidence recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Simulator 전수 render of every changed screen + screenshots (before OTA)</name>
  <files>.planning/phases/33-result-trust-recovery/33-PHASE-GATE-EVIDENCE.md</files>
  <read_first>
    - MEMORY verify-ui-on-simulator-before-ota (Xcode 26.6 + Simulator on this Mac; typecheck blind to render crash; expo-updates applies on 2nd relaunch)
    - 33-06/07/08/09 SUMMARYs (the changed screens + the deferred Simulator sequences: keypoint toggle, voice-cue pause/highlight/resume, illustration render, safe-area/number relocation)
    - 33-CONTEXT.md D-21 (Simulator render before OTA)
  </read_first>
  <action>
    Launch the app in the iOS Simulator and render every changed result screen with a re-analyzed doc: the 확대비교 cards (joint-exact, same-scale, ref side drawn), the video overlay (keypoints hidden by default + toggle, red marker self-answer, voice-cue pause→region highlight→resume), the wired illustrations (silent-hidden for unverified), and the Wave B screens (numbers in 내역, no contradiction, no safe-area overlap). Capture a screenshot per changed screen. Confirm no crash and no status-bar overlap. Only after the Simulator 전수 render passes is an OTA permissible (D-21). Record screenshots + pass/fail in 33-PHASE-GATE-EVIDENCE.md.
  </action>
  <verify>
    <automated>grep -qiE "simulator|screenshot" .planning/phases/33-result-trust-recovery/33-PHASE-GATE-EVIDENCE.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Every changed screen rendered in the Simulator with a screenshot captured (전수) — no crash, no status-bar overlap.
    - The A-6 voice-cue pause/highlight/resume sequence and the keypoint toggle are confirmed by Simulator recording/screenshots (the deferred eyeballs from 33-07).
    - 무엇을 열어서 확인했는가 (D-19): the evidence doc embeds/links the Simulator screenshots — "typecheck/build passed" is explicitly NOT accepted (D-21).
    - OTA is gated on this render passing; the SUMMARY notes expo-updates applies on the 2nd relaunch.
  </acceptance_criteria>
  <done>All changed screens render cleanly in the Simulator with screenshots; OTA unblocked only after this passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: belle 확인 ② — device UAT (touchpoint 2 of 2)</name>
  <files>.planning/phases/33-result-trust-recovery/33-PHASE-GATE-EVIDENCE.md</files>
  <action>
    After the 6-motion re-sweep (전수 opened) and the Simulator render (전수 screenshots) pass and the OTA is published, present the build to belle for the device UAT and PAUSE. This is belle touchpoint 2 of 2 (D-01). Provide the 5-question + phase-32 3-question checklist. Do not close the phase until belle approves; on findings, route to gap closure.
  </action>
  <what-built>The full phase-33 build: 확대비교 (same-moment/scale/marking, joint-exact, ref side drawn), 영상 위 표시 (keypoints opt-in, self-answering markers, voice-cue pause+highlight+resume), 동작별 coaching copy (direction-correct, power-spin no longer 천장), wired illustrations, and Wave B tidy-up — verified 전수 on 6 motions + Simulator, deployed via OTA.</what-built>
  <how-to-verify>
    On the device, open a fresh analysis and check belle's 5 questions are answered by the screen itself:
    1. 확대사진 — 무엇을 견주는지 알겠고 두 사진이 같은 조건인가?
    2. 키포인트 — 무엇을 잡는지 알거나, 안 보이는가?
    3. 붉은 표시 — 무엇이 문제고 뭘 하라는지 붙어 있는가?
    4. 바 마커·음성 — 무엇을 가리키는지 알고, 음성 중 화면이 그 지점을 짚는가?
    5. 코칭 문구 — 그 동작의 실제 기준과 일치하는가 (파워스핀이 옆으로 뻗으라 하는가)?
    Plus phase-32 3문항: 자기 말로 설명 / 다음 행동 / 강사 질문이 서는가? 그리고 화면이 전보다 단순한가?
  </how-to-verify>
  <verify>
    <automated>echo "checkpoint — belle device UAT; blocks until approval"</automated>
  </verify>
  <resume-signal>Type "approved" to close the phase, or list findings for gap closure. This is belle touchpoint 2 of 2.</resume-signal>
  <done>belle has run the device UAT; phase closes on approval, or findings are captured for gap closure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Pod (rebooted) → S3/Firestore | re-analysis regenerates stored crops/voice; Lambda RUNPOD_ANALYZE_URL resync on proxy change |
| regenerated presigned URLs → app | TTL-bound crop/audio URLs |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-17 | Tampering | Pod env drift / stale RUNPOD_ANALYZE_URL after reboot | mitigate | X-RunPod-Token smoke + SSM/Lambda resync per runbook before sweep (D-25) |
| T-33-18 | Information disclosure | regenerated presigned crop/audio URLs | mitigate | canonical S3 key + TTL-bound presign; playback-url recordId whitelist |
| T-33-19 | Tampering | concurrency pollution of serial sweep | mitigate | serial /analyze only (pipeline not concurrency-safe) |
| T-33-SC | Tampering | package installs | accept | no new packages; Pod bootstrap uses existing runbook |
</threat_model>

<verification>
- 6-motion serial re-sweep green (assert_gates.py); fixture-less motions substitute-verified.
- Every regenerated crop PNG opened + coach mp3 played (전수); Simulator screenshots per changed screen.
- OTA only after Simulator render passes (D-21); belle UAT checkpoint reached (autonomous:false).
</verification>

<success_criteria>
On a rebooted Pod, all 6 motions re-analyze cleanly; every regenerated crop/voice and every changed screen is opened/rendered 전수 and verified; belle confirms the 5 questions are answered by the screen — the phase closes on belle's approval (touchpoint 2 of 2).
</success_criteria>

<output>
Create `.planning/phases/33-result-trust-recovery/33-10-SUMMARY.md` when done, recording belle's UAT verdict and any gap-closure items.
</output>
