---
phase: 33-result-trust-recovery
plan: 11
type: execute
wave: 12
depends_on: ["33-09", "33-10"]
files_modified:
  - .planning/phases/33-result-trust-recovery/mockups/index.html
autonomous: false
requirements: [D-01, D-07, D-08, D-10, D-18, D-19]
user_setup: []
must_haves:
  truths:
    - "The mockup shows 확대비교 AND 영상 위 표시 together in ONE HTML page belle opens on her phone (D-10)"
    - "It uses belle's REAL doc data (uid csKW… / analysis 071df…), no mock/synthetic; phase25eval real docs only if the target is thin (D-10)"
    - "Crop assets are DOWNLOADED locally into the mockup dir with a provenance note (source doc id + S3 key + retrieval time), NOT embedded as expiring presigned URLs — a phone review delayed past the URL TTL must not read as a design failure (codex concern 16, still D-10-compliant)"
    - "It includes the fixed worst-case set (가림/인버전/모션블러/프레임밖/좌표결측), not a good case only (D-08)"
    - "Only the D-07 survivors reach belle — Claude pre-filters; belle is asked exactly ONCE for design (D-01/D-10 — this is design touchpoint 1 of 2)"
    - "The video-overlay panel shows the 3 state-transition still frames: 재생 중 → 음성 중(정지+부위 강조) → 재개 (D-10)"
  artifacts:
    - path: ".planning/phases/33-result-trust-recovery/mockups/index.html"
      provides: "single self-contained HTML: 2-3 zoom options + video-overlay state frames, real data, worst cases"
      contains: "071df9f894d64d1696f106e613f51f5c"
  key_links:
    - from: "mockups/index.html"
      to: "backend/scripts/dump_analysis_doc.py output (belle real doc)"
      via: "real crop URLs + records rendered, mock forbidden"
      pattern: "faultZoomComparisons|deductionBreakdown"
    - from: "mockups/index.html"
      to: "33-A3-ZOOM-DESIGN.md surviving options"
      via: "only D-07 survivors rendered as tabs"
      pattern: "안|Option"
---

<objective>
Build the belle mockup (A-4) and take belle 확인 ① — the FIRST of exactly two DESIGN belle touchpoints (D-01; the Pod/flip greenlights in the substrate track are ops exceptions, not design touchpoints). One self-contained HTML page shows the 확대비교 options (A-3 survivors) and the 영상 위 표시 behavior together, so belle confirms both in a single review (D-11). Data is belle's REAL analysis doc dumped via dump_analysis_doc.py (D-10) — mock/synthetic forbidden. It includes the fixed D-08 worst-case set, and only the D-07-surviving options appear (Claude pre-filters, per D-18/§6.5 "belle 에게 불량이 도달하면 게이트 실패").

Purpose: The mockup is the gate before implementation (W1 Task 1 = 목업). belle confirms the direction once; A-5/A-6 then implement the confirmed design without further belle questions.

Output: mockups/index.html (real data, worst cases, 3 state frames) + belle approval.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-result-trust-recovery/33-A3-ZOOM-DESIGN.md
@.planning/phases/33-result-trust-recovery/33-A1-MOTION-STANDARDS.md
@.planning/phases/33-result-trust-recovery/33-CONTEXT.md
@.planning/phases/32-result-readability-3-omni/mockups/index.html
@backend/scripts/dump_analysis_doc.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: build the mockup HTML (real data + worst cases + 3 state frames) and D-07 self-filter</name>
  <files>.planning/phases/33-result-trust-recovery/mockups/index.html</files>
  <read_first>
    - .planning/phases/32-result-readability-3-omni/mockups/index.html (single self-contained HTML analog: :root tokens --brand #FF4B33 변경금지, --teal for safety tone, letter-spacing:0 iOS SIGABRT note, sticky topbar tabs, shared phone/card shell)
    - 33-PATTERNS.md section Mockup (structure to copy + phase-33 novelty: 3 state-transition frames, real doc, worst cases)
    - 33-A3-ZOOM-DESIGN.md (the 2-3 surviving options + their pre-recorded D-07 verdicts)
    - 33-CONTEXT.md D-10 (real doc ids, mock forbidden) + D-08 (worst-case list)
    - backend/scripts/dump_analysis_doc.py (run it to get belle's real crop URLs + records)
  </read_first>
  <action>
    Run dump_analysis_doc.py against belle's real doc (uid csKWYvI3WCPYPysNQ9KkWecaUvq1 / analysis 071df9f894d64d1696f106e613f51f5c) — re-analyze first if needed so the crops reflect the flipped substrate — and, if that doc is thin on worst cases, supplement with phase25eval fixture REAL docs (합성 금지). Copy the phase-32 mockup shell (tokens, topbar tabs, phone/card, letter-spacing:0). Build one HTML page with two panels: (1) 확대비교 — the 2-3 A-3 surviving options as switchable tabs, each drawn on the SAME real data plus the D-08 worst cases (가림/인버전/모션블러/프레임밖/좌표결측); (2) 영상 위 표시 — three still frames showing 재생 중 → 음성 중(정지+부위 강조) → 재개 (D-10, static state transition, no animation). DOWNLOAD the real crop assets locally into the mockup dir and reference those local files, with a small provenance note (source doc id + S3 key + retrieval time) — do NOT embed expiring presigned URLs (codex concern 16), so a delayed phone review never reads as a design failure; still real data, mock forbidden (D-10). No placeholder pixels. Honor D-05: reduce or align, at most 1 new line of copy per screen — no new labels/badges/legends. Before finalizing, run the D-07 6-criteria self-filter and DROP any option that fails; the completion note records the drop rationale (only survivors ship to belle, D-18). Brand #FF4B33, light-only, theme-consistent (D-22).
  </action>
  <verify>
    <automated>test -f .planning/phases/33-result-trust-recovery/mockups/index.html && grep -q "071df9f894d64d1696f106e613f51f5c" .planning/phases/33-result-trust-recovery/mockups/index.html && grep -qE "재생 중|음성 중|재개" .planning/phases/33-result-trust-recovery/mockups/index.html && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - mockups/index.html is a single self-contained page opening the 확대비교 options + the 3 video-overlay state frames.
    - It renders belle's REAL doc data (the analysis id string appears; crop URLs are real presigned/real doc URLs) — no mock/synthetic pixels (D-10).
    - The D-08 worst cases are present as their own cases, not just a good case (D-08).
    - Only D-07 survivors appear; the SUMMARY records which options were dropped and why (D-18 Claude self-gate).
    - 무엇을 열어서 확인했는가 (D-19): Claude opened the rendered HTML in a browser/preview and confirmed the real crops load and the state frames read — before sending to belle.
    - Brand #FF4B33 + light theme + letter-spacing:0 (D-22, iOS SIGABRT guard).
  </acceptance_criteria>
  <done>Mockup renders real data with worst cases and 3 state frames; only D-07 survivors present; Claude has opened it first.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: belle 확인 ① — mockup review (design touchpoint 1 of 2)</name>
  <files>.planning/phases/33-result-trust-recovery/mockups/index.html</files>
  <action>
    Present the finished mockup (mockups/index.html) to belle and PAUSE for review. Do not proceed to A-5/A-6 until belle approves. This is belle DESIGN touchpoint 1 of 2 (D-01). Claude has already pre-filtered by D-07 — only survivors are shown. Serve/share the link so belle can open it on her phone.
  </action>
  <what-built>A single HTML mockup page (mockups/index.html) showing the 확대비교 redesign options and the 영상 위 표시 state-transition behavior, on belle's real analysis data (reflecting the flipped substrate), including the worst-case set. Only D-07-surviving options are shown.</what-built>
  <how-to-verify>
    1. Open the mockup link on your phone (or ask Claude to serve it).
    2. 확대비교 panel: for each option tab, check — 두 사진이 같은 순간·같은 배율·같은 표시로 보이나? 무엇을 견주는지 3초 안에 읽히나? 최악 케이스(가림/거꾸로/블러/프레임밖/결측)에서도 무너지지 않나?
    3. 영상 위 표시 panel: 재생 중 → 음성 중(정지+부위 강조) → 재개 흐름이 "아하"를 유발하나?
    4. 화면이 전보다 단순한가 (요소·문장이 늘지 않았나)?
  </how-to-verify>
  <verify>
    <automated>echo "checkpoint — belle mockup review; blocks until approval"</automated>
  </verify>
  <resume-signal>Type "approved" (또는 어느 안으로 갈지) to unblock A-5/A-6, or describe what to change. This is belle design touchpoint 1 of 2.</resume-signal>
  <done>belle has reviewed the mockup and either approved a direction or given change requests; A-5/A-6 remain blocked until approval.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| belle real doc → static HTML | read-only render of a real analysis doc; presigned crop URLs are TTL-bound |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-43 | Availability | expiring presigned crop URLs break a delayed phone review | mitigate | download crop assets locally + provenance note; no embedded TTL URLs (codex concern 16, D-10) |
| T-33-44 | Tampering | mock data masquerading as real | mitigate | D-10 gate — real doc id must appear; synthetic forbidden; verify grep |
| T-33-SC | Tampering | package installs | accept | no installs (static HTML) |
</threat_model>

<verification>
- mockups/index.html exists, renders real doc data + worst cases + 3 state frames.
- Claude opened it before belle; only D-07 survivors present.
- belle checkpoint reached (autonomous:false); approval unblocks A-5/A-6.
</verification>

<success_criteria>
belle confirms the 확대비교 + 영상 표시 direction in one review on real data; the surviving design is locked for implementation. This is the first of exactly two design belle touchpoints.
</success_criteria>

<output>
Create `.planning/phases/33-result-trust-recovery/33-11-SUMMARY.md` when done, recording belle's decision and the dropped-option rationale.
</output>
