---
phase: 33-result-trust-recovery
plan: 12
type: execute
wave: 12
depends_on: ["33-11"]
files_modified:
  - backend/tests/phase33/test_zoom_join_joint_exact.py
  - backend/shared/python/sunity_shared/analysis/fault_zoom.py
  - backend/functions/pipeline/app.py
  - app/src/lib/deductionLabels.ts
  - app/src/app/analysis/result.tsx
  - app/src/types/analysis.ts
  - backend/shared/python/sunity_shared/models.py
  - docs/contract.md
autonomous: true
requirements: [D-02, D-03, D-05, D-06, D-12, D-16, D-18, D-19, D-20, D-21, D-22, D-23]
must_haves:
  truths:
    - "The card shown for an item is joint-exact to that item's criterion — a 다리 스플릿 item never attaches a shoulder crop (defect #5 root, D-12)"
    - "User and ref crops are same-moment / same-scale / same-marking, or the card is dropped — never shipped mismatched (D-12)"
    - "The ref (정은지) side is drawn (defect #1), phase spread across cards (defect #3), 국면 aligned via _to_rep_idx not hardcoded fps (defect #4), crop stays inside the person (defect #7), advisory reachable in the sheet (defect #8)"
    - "The angle number is NOT baked into the crop PNG (defect #6 resolved HERE): _mark's deficit badge and _draw_leg_angle's angle text no longer draw the number — it lives ONLY in 점수 내역 (D-16). Brand circle/arc geometry (the same-marking visual) is kept; the NUMBER is removed. Rendering change only, no scoring (D-20/D-29)"
    - "Crop provenance (which joint/frame) is recorded for INTERNAL review only — never rendered as an on-screen label (D-06/D-05)"
    - "The fix is the seam decided in A-3 (backend criterion-keyed crops OR app joint-exact join); no scoring math touched (D-20); global not power-spin-specific (D-02)"
  artifacts:
    - path: "backend/tests/phase33/test_zoom_join_joint_exact.py"
      provides: "asserts an emitted/joined card's joint matches the shown criterion"
      contains: "fault_zoom"
    - path: "backend/shared/python/sunity_shared/analysis/fault_zoom.py"
      provides: "crop provenance/phase/scale fixes (defects 1,2,3,4,7) + angle number un-baked from crop PNG (defect #6: _mark/_draw_leg_angle draw.text removed, circle/arc kept)"
      contains: "_to_rep_idx"
    - path: "app/src/lib/deductionLabels.ts"
      provides: "joint-exact projection (defect #5) if app seam chosen"
      contains: "projectDeductionRecordKeypoints"
  key_links:
    - from: "app/src/app/analysis/result.tsx:1215 selectedZoom"
      to: "app/src/lib/deductionLabels.ts:236 projectDeductionRecordKeypoints"
      via: "joint-exact join replaces region-first first-match"
      pattern: "selectedZoom|projectDeductionRecordKeypoints"
    - from: "backend/shared/python/sunity_shared/analysis/fault_zoom.py item shape"
      to: "app/src/types/analysis.ts + models.py + docs/contract.md"
      via: "3-way contract lockstep if item shape changes; scalar-only"
      pattern: "faultZoomComparisons"
---

<objective>
Implement the 확대비교 fix (A-5) per the seam decided in A-3 and the belle-approved A-4 mockup. Resolve the 8 measured defects at their roots: joint-exact item↔crop correspondence (#5), same-moment/same-scale/same-marking or drop the card (#2/#3), ref-side drawn (#1), 국면 aligned via _to_rep_idx (#4), crop inside person (#7), advisory reachable (#8). Defect #6 (baked angle badge) is resolved HERE, not deferred: this plan already edits fault_zoom.py and crops regenerate at the 33-16 Pod re-sweep, so this is the correct owner. Stop baking the angle number into the crop PNG — remove/guard the `_mark` deficit-badge text and the `_draw_leg_angle` angle-number text so the number lives ONLY in 점수 내역 (D-16), never twice (baked + 내역). Keep the brand circle/arc geometry (the same-marking visual). This is a rendering change, not scoring (D-20/D-29). Crop provenance is recorded internally only, never as an on-screen label (D-06/D-05). The crops now sit on the flipped substrate (33-07), so the join is correct at the root.

Purpose: This is the belle-#1-priority fix ("이것만 되도 볼만할거다"). It is provenance/data correction, not new machinery (D-05) — the disease is misalignment. Un-baking the number is part of the same crop-rendering surface (D-16) — an app-only Wave B plan (33-15) structurally cannot un-bake a backend PNG, so ownership sits here.

Output: joint-exact join + crop provenance fixes + angle-number un-bake (defect #6) + contract lockstep (if item shape changes) + a RED-first joint-exact test.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-result-trust-recovery/33-A3-ZOOM-DESIGN.md
@.planning/phases/33-result-trust-recovery/33-11-SUMMARY.md
@.planning/phases/33-result-trust-recovery/33-RESEARCH.md
@.planning/phases/33-result-trust-recovery/33-PATTERNS.md
@backend/shared/python/sunity_shared/analysis/fault_zoom.py
@app/src/lib/deductionLabels.ts
@app/src/app/analysis/result.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — joint-exact join test</name>
  <read_first>
    - backend/tests/phase32/test_fault_zoom_crop_parity.py:24-31 (synthetic @dataclass fixture, pure PIL/numpy, no S3/firestore)
    - 33-PATTERNS.md section test_zoom_join_joint_exact.py (two options per seam: backend criterion-keyed emit OR app-side; app has no JS runner — document the coverage gap explicitly per D-18)
    - app/src/lib/deductionLabels.ts:221-240 (projectDeductionRecordKeypoints, source==='vision' → ALL faultJoints)
  </read_first>
  <behavior>
    - If seam is backend: build_fault_zoom_comparisons emits an item whose joint/region matches the criterion that will be shown (extend the parity fixture).
    - If seam is app: assert the joint-exact projection returns joint-matching keypoints for a split_angle record (NOT the whole faultJoints set). If no JS test runner exists, record the missing automated coverage explicitly (D-18 전수 불가 시 무엇을 못 봤는지 명시) and cover via tsc --noEmit + the D-19 PNG eyeball.
    - Angle-number un-bake (defect #6): assert the rendered crop no longer contains the baked deficit/angle number — e.g. the _mark/_draw_leg_angle path draws circle/arc but the number-text branch is gone (the test asserts the un-baked path, RED before Task 2).
  </behavior>
  <action>
    Create backend/tests/phase33/test_zoom_join_joint_exact.py per the seam decided in A-3, copying the phase32 parity synthetic-fixture skeleton. Add an assertion that the crop-render path no longer bakes the angle number (defect #6) — assert against the _mark/_draw_leg_angle number-text branch being removed/guarded while circle/arc geometry stays. If the seam is app-side (TS), still create a backend-side assertion where possible (crop carries the right joint), and document in the test header what CANNOT be automatically asserted (no JS runner), naming the D-19 eyeball as the substitute proof.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/phase33/test_zoom_join_joint_exact.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Test is RED before Task 2 (asserts the mismatch that currently exists AND the baked-number that currently exists).
    - If app-side seam, the test header names the un-automatable portion + the D-19 substitute (no silent gap, D-18).
  </acceptance_criteria>
  <done>Joint-exact + un-baked-number test exists and is RED for the right reason.</done>
</task>

<task type="auto">
  <name>Task 2: implement crop provenance / join fix (the A-3 seam) + un-bake angle number (defect #6) + contract lockstep</name>
  <read_first>
    - 33-A3-ZOOM-DESIGN.md section 4 (seam #1 fix-location decision + rationale)
    - backend/shared/python/sunity_shared/analysis/fault_zoom.py:1287 build_fault_zoom_comparisons, :1533-1557 item emit (scalar-only), :195 _to_rep_idx, :1529 ref-side gate B (defect #1), _side_crop 3-tier (defect #7)
    - backend/shared/python/sunity_shared/analysis/fault_zoom.py:606-633 _mark (deficit badge `draw.text` + rectangle — bakes "40°" into PNG, defect #6) and :636-685 _draw_leg_angle (draws lines + arc + `draw.text` angle number — the split-angle number bake, defect #6)
    - backend/functions/pipeline/app.py:2907 _build_fault_zoom_comparisons, :2936 fault_joints source (seam #1 root)
    - app/src/lib/deductionLabels.ts:221-240 + app/src/app/analysis/result.tsx:1215-1229 (selectedZoom first-match + advisory skip defect #8)
    - backend/shared/python/sunity_shared/firestore_admin.py:1418-1435 (_validate_dict_only_scalars — items stay flat)
    - app/src/types/analysis.ts + backend/shared/python/sunity_shared/models.py + docs/contract.md (3-way lockstep if item shape changes)
    - 33-CONTEXT.md D-16 (수치는 점수 내역 자리로 이동, 투명 공개 유지) — the number's single home is 내역, not the crop PNG
  </read_first>
  <action>
    Implement the seam fix decided in A-3. If BACKEND: make crop selection consume the same criterion set that becomes deductionBreakdown.records (criterion-keyed crops), draw the ref side (remove/relax gate B so 정은지 side gets a marking, defect #1), spread the phase across cards instead of one worst_seconds for all (defect #3), route ALL frame indices through _to_rep_idx (defect #4, never hardcode 9/18), and keep the crop inside the person via the confidence fallback (defect #7). If APP: make projectDeductionRecordKeypoints joint-exact (split_angle → leg keypoints, not ALL faultJoints) and selectedZoom joint-exact instead of region-first, and surface advisory tier in the sheet (defect #8). In BOTH cases, when a user/ref pair cannot be made same-moment/same-scale, DROP the card (do not ship mismatched, D-12). Record crop provenance (joint + frame) to an INTERNAL review log/report only — never render it as an on-screen label (D-06/D-05). UN-BAKE the angle number (defect #6): in fault_zoom.py, remove/guard the number-drawing branch in `_mark` (the `_deficit_label` `draw.text` + its brand rectangle, ~:626-632) and in `_draw_leg_angle` (the `angle_deg` `draw.text` + rectangle, ~:671-684) so NO angle number is drawn onto the crop PNG — keep the brand circle and the leg lines/arc (the same-marking geometry, D-12), only the NUMBER goes. The number's single home is 점수 내역 (D-16); it must not appear twice. This is rendering-only — do NOT touch scoring/deficit math (D-20/D-29); `deficit_deg`/`angle_deg` still flow to the item/내역, they just stop being painted on the PNG. Keep items scalar (Firestore flat); if the item shape changes, update analysis.ts + models.py + contract.md in one lockstep commit. Global, not power-spin-specific (D-02).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/phase33/test_zoom_join_joint_exact.py -q && cd ../app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Joint-exact test PASSES; app tsc --noEmit clean.
    - No scoring file changed: git diff shows no edit to dimensions/kismam/motiondtw/deduction scoring (D-20). The fault_zoom.py edit is rendering-only (crop selection/drawing), not deficit computation.
    - If item shape changed, analysis.ts + models.py + contract.md all changed in lockstep (grep faultZoomComparisons in all three); items remain scalar-only (no nested array/dict).
    - Angle number NOT baked into the crop (defect #6 resolved): grep confirms the number-text `draw.text` branches in `_mark` and `_draw_leg_angle` are removed/guarded; circle/arc geometry retained; the number appears only in 내역 (D-16, cross-checked against 33-15 which relocates the coaching-card number — no double-render).
    - 틀리면 걸리는 장치 (D-18): the joint-exact test + the internal provenance log catch an item↔crop mismatch; a mismatched pair is dropped, not rendered; the un-bake assertion catches a re-appearing baked number.
    - 무엇을 열어서 확인했는가 (D-19): this task's code change alone does NOT regenerate stored crops — the actual PNG eyeball (전수, 6 motions) happens at the phase gate (33-16) after Pod re-sweep, confirming BOTH joint-exactness AND that no number is baked; the SUMMARY states this explicitly (RESEARCH Runtime State).
  </acceptance_criteria>
  <done>Item↔crop is joint-exact and same-moment/scale-or-dropped, ref side drawn, phase spread, fps routed, advisory reachable, angle number un-baked from the PNG (defect #6); contract in lockstep; scoring untouched; PNG regeneration deferred to 33-16 Pod re-sweep.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| pipeline → Firestore faultZoomComparisons[] | server-written crop items; app renders them |
| S3 presigned crop PNG → app | TTL-bound presigned URL |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-45 | Tampering | malformed faultZoomComparisons[] item | mitigate | keep scalar-only; _validate_dict_only_scalars (firestore_admin.py:1418) rejects nested; contract lockstep |
| T-33-46 | Information disclosure | crop PNG presigned URL over-exposure | mitigate | canonical S3 key server-built; TTL-bound presign (s3keys single source) |
| T-33-47 | Tampering | baked angle number re-appears on crop (double-render vs 내역) | mitigate | un-bake test asserts _mark/_draw_leg_angle number-text branch removed; 33-16 PNG eyeball confirms; D-16 single-home |
| T-33-SC | Tampering | package installs | accept | no new packages (RESEARCH Package Legitimacy Audit: none); reuse Pillow/numpy/react-native-svg |
</threat_model>

<verification>
- pytest backend/tests/phase33/test_zoom_join_joint_exact.py passes; app npm run typecheck clean.
- git diff: no scoring-math file touched (D-20); fault_zoom.py change is rendering-only (crop selection/drawing + number un-bake); contract 3-way lockstep if item shape changed.
- Actual PNG 전수 eyeball deferred to 33-16 (Pod re-sweep regenerates stored crops) — confirms joint-exactness AND no baked number.
</verification>

<success_criteria>
The shown card is joint-exact and same-condition-or-dropped, the ref side is drawn, phases spread, alignment via _to_rep_idx, advisory reachable, and the angle number is un-baked from the crop (lives only in 내역) — all at the root the A-3 seam chose, with scoring untouched and provenance internal-only.
</success_criteria>

<output>
Create `.planning/phases/33-result-trust-recovery/33-12-SUMMARY.md` when done, stating which seam was implemented, that the angle number is un-baked from the crop (defect #6, D-16), and that stored-crop regeneration is pending the 33-16 Pod re-sweep.
</output>
</output>
