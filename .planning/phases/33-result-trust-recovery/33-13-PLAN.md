---
phase: 33-result-trust-recovery
plan: 13
type: execute
wave: 13
depends_on: ["33-12"]
files_modified:
  - app/src/app/analysis/result.tsx
  - app/src/components/KeypointOverlay.tsx
  - app/src/components/VideoCompare.tsx
  - app/src/lib/deductionLabels.ts
autonomous: true
requirements: [D-02, D-05, D-13, D-18, D-19, D-21, D-22]
must_haves:
  truths:
    - "Keypoints are hidden by default and turned on only on demand — no unexplained 12 dots (D-13)"
    - "A red marker on the video answers itself: 여기 — 무엇이 문제 → 이렇게, as one unit, or it is not rendered (D-13/불변식)"
    - "Bar markers reveal they are the flagged items and jump to that item on tap (D-13)"
    - "When a voice cue starts, the video pauses and the relevant region is highlighted; on cue end it resumes — the phase's representative UX pattern (D-13)"
    - "Every displayed marker has a two-way match to a flagged item; an orphan marker (no matching item) is NOT rendered (D-18)"
  artifacts:
    - path: "app/src/components/VideoCompare.tsx"
      provides: "voice-cue pause + region highlight + resume sync"
      contains: "cue"
    - path: "app/src/components/KeypointOverlay.tsx"
      provides: "hide-by-default keypoints + opt-in toggle"
      contains: "react-native-svg"
  key_links:
    - from: "app/src/components/VideoCompare.tsx cue tick"
      to: "app/src/lib/cueTrack.ts / audioCue.ts"
      via: "reuse existing 100ms cue-window timing, no new timer loop"
      pattern: "cueWindow|audioCue"
    - from: "video marker"
      to: "deductionBreakdown record (flagged item)"
      via: "two-way match via buildDeductionMarkers single source"
      pattern: "buildDeductionMarkers"
---

<objective>
Implement the 영상 위 표시 fix (A-6), app-only, per the belle-approved A-4 mockup. Make every on-video display answer itself or disappear (the phase 불변식): keypoints hidden by default + opt-in (D-13); red markers carry 여기 → 무엇 → 이렇게 as one unit or are not rendered; bar markers reveal they are flagged items and jump on tap; and the representative UX pattern — when a voice cue starts, pause the video + highlight the region, resume on cue end. Every marker has a two-way match to a flagged item; orphan markers are not rendered (D-18).

Purpose: belle's questions 2-4 ("뭘 잡은거지 / 붉은 표시가 뭘 어쩌라는 / 바 마커·음성이 뭐 어쩌라는") are answered by the display answering itself, not by adding captions (D-05).

Output: app render changes only — no backend, no scoring, no new library.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-result-trust-recovery/33-11-SUMMARY.md
@.planning/phases/33-result-trust-recovery/33-RESEARCH.md
@.planning/phases/33-result-trust-recovery/33-PATTERNS.md
@app/src/components/KeypointOverlay.tsx
@app/src/components/VideoCompare.tsx
@app/src/app/analysis/result.tsx
@app/src/lib/deductionLabels.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: keypoints hide-by-default + red marker self-answer + bar-marker jump (two-way match)</name>
  <read_first>
    - app/src/components/KeypointOverlay.tsx (renders keypoints + axis polyline, react-native-svg, tokens only; action-text pills already removed) + KeypointOverlayToggle.tsx (async-storage persistence analog)
    - app/src/lib/deductionLabels.ts:242 buildDeductionMarkers (single source for markers + cues + zoom)
    - app/src/app/analysis/result.tsx (marker render + bar markers + isRecordHidden:1604 spot-check exclusion)
    - 33-SEED.md W2 table (per-display target behavior)
  </read_first>
  <action>
    Make KeypointOverlay hidden by default with an opt-in toggle (reuse KeypointOverlayToggle async-storage pattern; no new library). Ensure each red marker renders as one unit that includes 여기 + 무엇이 문제 + 이렇게 (a short action phrase from the flagged record; digit-free per project convention) — a marker with no matching flagged record is NOT rendered (two-way match via buildDeductionMarkers as the single source). Make bar markers reveal they are the flagged items and, on tap, jump to that item (existing scroll-to-item pattern). Honor D-05: reduce/answer, do NOT add new labels/badges/legends; at most 1 new line of copy. Theme tokens only, brand #FF4B33 (D-22). Global, not power-spin-specific (D-02). No scoring, no backend.
  </action>
  <verify>
    <automated>cd app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Keypoints hidden by default; a toggle turns them on (grep the toggle wiring).
    - buildDeductionMarkers is the single source; no marker renders without a matching flagged record (two-way match — the D-18 catch device against orphan markers).
    - Bar marker tap jumps to the flagged item (grep the scroll/jump handler).
    - No new label/badge/legend component added (D-05); theme tokens only, no hardcoded color/spacing (D-22).
    - tsc --noEmit clean.
  </acceptance_criteria>
  <done>Keypoints opt-in, red markers self-answer with two-way match, bar markers jump — no added captions.</done>
</task>

<task type="auto">
  <name>Task 2: voice-cue pause + region highlight + resume (representative UX pattern)</name>
  <read_first>
    - app/src/components/VideoCompare.tsx (expo-video playback + audio/cue) + app/src/lib/cueTrack.ts (buildCueWindows) + app/src/lib/audioCue.ts (audio trigger, resign/prefetch)
    - app/src/app/analysis/result.tsx:1601 cueWindows (subtitle = rec.cueLine, isRecordHidden excluded), :1641 coachAudioAnalysisId gate
    - 33-SEED.md W2 (belle 제안: 음성 시작 시 영상 일시정지 + 부위 강조, 끝나면 재생) + D-13
  </read_first>
  <action>
    Wire the representative UX pattern: on a voice-cue start (existing cueTrack 100ms tick / audioCue trigger — reuse, do NOT add a new timer loop), pause the video and highlight the region tied to that cue's flagged item; on cue end, resume playback. Make "적용 중입니다" apply while paused, then resume (멈춘 상태에서 적용 → 적용 후 재생). Region highlight uses the existing marker/region mapping (two-way match — a cue with no matching item does not pause/highlight). Theme tokens only (D-22). No new library, no backend, no scoring.
  </action>
  <verify>
    <automated>cd app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - On cue start the video pauses + the region highlights; on cue end it resumes (grep the pause/resume handler tied to cue transitions).
    - Reuses cueTrack/audioCue timing — no new timer loop introduced.
    - A cue with no matching flagged item does not trigger pause/highlight (D-18 orphan guard).
    - 무엇을 열어서 확인했는가 (D-19): this behavior is timing/interaction — its real verification is a Simulator recording of the pause→highlight→resume sequence, performed at the phase gate (33-16) before OTA (D-21); the SUMMARY names this as the pending eyeball, not "typecheck passed."
    - tsc --noEmit clean; theme tokens only.
  </acceptance_criteria>
  <done>Voice cue pauses + highlights + resumes via reused timing; Simulator eyeball of the sequence deferred to 33-16.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| stored cue/audio → app render | app renders stored frame indices + presigned audio; pure render layer |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-47 | Tampering | orphan marker/cue with no backing item | mitigate | two-way match via buildDeductionMarkers; unmatched display not rendered (D-18) |
| T-33-48 | Denial of service | render crash from bad field | mitigate | app-side normalize + tsc; Simulator render before OTA (D-21) |
| T-33-SC | Tampering | package installs | accept | no new packages (reuse expo-video, react-native-svg, async-storage) |
</threat_model>

<verification>
- app npm run typecheck clean (both tasks).
- No backend/scoring file changed (git diff limited to app/).
- Simulator render of keypoint toggle + voice-cue pause/highlight/resume deferred to 33-16 before OTA (D-21).
</verification>

<success_criteria>
Every on-video display answers itself or disappears: keypoints opt-in, red markers self-explain with two-way match, bar markers jump, and voice cues pause+highlight+resume — with no added captions and no scoring touched.
</success_criteria>

<output>
Create `.planning/phases/33-result-trust-recovery/33-13-SUMMARY.md` when done, naming the Simulator sequence to record at 33-16.
</output>
