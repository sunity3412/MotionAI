---
phase: 33-result-trust-recovery
plan: 14
type: execute
wave: 15
depends_on: ["33-09", "33-13"]
files_modified:
  - app/assets/illustrations/
  - app/src/components/DefectIllustration.tsx
  - app/src/app/analysis/result.tsx
autonomous: true
requirements: [D-02, D-05, D-15, D-18, D-19, D-21, D-22]
must_haves:
  truths:
    - "Illustrations are generated in the belle-confirmed 준실사 2안 style — Claude does NOT re-ask belle to confirm the style (D-01/D-15)"
    - "Every generated illustration passes anatomy 전수 검수 (limb count, joint direction); any single failure keeps that whole set unwired (D-15/D-18)"
    - "Illustration subject/direction matches the A-1 완성 기준 + the A-2 coaching direction — the picture shows the correct fix, not a wrong one prettily (D-03)"
    - "A set that fails review renders nothing (silent hidden), never ships a wrong illustration (D-18)"
    - "Wiring uses theme tokens + brand #FF4B33, light-only; no new image library (D-22)"
  artifacts:
    - path: "app/assets/illustrations/"
      provides: "anatomy-verified illustration assets bundled into the app"
      contains: ""
    - path: "app/src/components/DefectIllustration.tsx"
      provides: "presentational component with silent 'hidden' fallback (ReferenceCornerSection standard)"
      contains: "hidden"
  key_links:
    - from: "app/src/components/DefectIllustration.tsx"
      to: "app/src/app/analysis/result.tsx (defect → illustration mapping)"
      via: "wired per flagged defect; unverified sets stay hidden"
      pattern: "DefectIllustration"
    - from: "illustration subject"
      to: "33-A1-MOTION-STANDARDS.md + phrasebook cueLine direction"
      via: "picture matches the corrected coaching direction"
      pattern: "완성 기준"
---

<objective>
Generate the defect illustration sets (A-7) in the belle-confirmed 준실사 2안 style, run anatomy 전수 검수, and wire them into the app. belle already decided the style (2안, after rejecting 1안 for a 3-legged anatomy error) — do NOT re-ask (D-01/D-15). The illustration is the phase's "말 없이 뭘 하라는지 보여주는" device, so its subject and direction must match the A-1 완성 기준 and the corrected coaching direction (D-03) — a picture of the wrong fix is worse than none.

Purpose: The phase disease is "표시가 뭘 하라는지 말을 안 함"; illustrations show it wordlessly (belle promoted this to Wave A). It depends on A-2 phrasebook (33-09, so we know what to draw) and follows A-6 (33-13, result.tsx wiring order).

Output: anatomy-verified assets in app/assets/illustrations/ + a DefectIllustration component with silent-hidden fallback + result.tsx wiring.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-result-trust-recovery/33-A1-MOTION-STANDARDS.md
@.planning/phases/33-result-trust-recovery/33-09-SUMMARY.md
@.planning/phases/33-result-trust-recovery/33-PATTERNS.md
@app/src/components/ReferenceCornerSection.tsx
@.planning/phases/32-result-readability-3-omni/samples/illust_variant2_pro.jpg
</context>

<tasks>

<task type="auto">
  <name>Task 1: generate 준실사 2안 illustration sets + anatomy 전수 검수</name>
  <read_first>
    - .planning/phases/32-result-readability-3-omni/samples/illust_variant2_pro.jpg (the belle-confirmed 2안 준실사 style reference)
    - 33-A1-MOTION-STANDARDS.md (흔한 실패 + 완성 기준 per motion — the illustration subjects)
    - 33-09-SUMMARY.md (which defects have a corrected coaching direction to illustrate)
    - 33-SEED.md W6 (2안 confirmed, do not re-ask; 해부학 전수 검수; samples/ has 3, app wiring 0)
  </read_first>
  <action>
    Generate defect illustration sets in the 2안 준실사 style (matching illust_variant2_pro.jpg), one illustration per defect subject drawn from the A-1 흔한 실패 + the corrected coaching direction (so the picture shows the RIGHT fix — power-spin legs to the side, not ceiling). Claude decides set composition/count (D-15 discretion). Then run anatomy 전수 검수: open EVERY generated illustration and check limb count and joint direction against human anatomy AND against the motion's correct posture. Mark each illustration PASS/FAIL with the specific check. Save only anatomy-PASS assets under app/assets/illustrations/. Do NOT re-ask belle to confirm the style (D-01/D-15). No new image library.
  </action>
  <verify>
    <automated>ls app/assets/illustrations/ 2>/dev/null | grep -c . </automated>
  </verify>
  <acceptance_criteria>
    - Illustrations exist in the 2안 준실사 style; belle was NOT re-asked to confirm the style (D-01/D-15).
    - Anatomy 전수 검수 is recorded: every illustration has an opened-and-checked PASS/FAIL with the specific limb-count/joint-direction check (D-18/D-19).
    - Only anatomy-PASS assets are saved; any FAIL keeps its whole set out of app/assets/illustrations/ (D-15).
    - 무엇을 열어서 확인했는가 (D-19): each illustration file was OPENED and viewed (전수) — generation-success return does not count.
    - 이 산출물이 틀렸다면 어떻게 알았을까: answered — a 3-legged/backwards-joint illustration is caught by the 전수 anatomy open, exactly the 1안 error belle caught.
  </acceptance_criteria>
  <done>Anatomy-verified illustration sets exist; failed images excluded; belle not re-asked.</done>
</task>

<task type="auto">
  <name>Task 2: DefectIllustration component + result.tsx wiring (silent-hidden fallback)</name>
  <read_first>
    - app/src/components/ReferenceCornerSection.tsx:25-70 (ScoreBreakdownSection 표준형: named export, inline prop type, header decision-comment, StyleSheet at bottom, theme tokens only, silent 'hidden' state = render nothing)
    - app/src/app/analysis/result.tsx (where per-defect content renders — the wiring point)
    - 33-PATTERNS.md section Illustration wiring component (RN Image or expo-image, no new library; assets must move into app/)
  </read_first>
  <action>
    Create app/src/components/DefectIllustration.tsx following the ReferenceCornerSection standard: named export, inline destructured prop type, header comment citing D-15, StyleSheet at bottom, theme tokens only (no hardcoded color/spacing/radius, brand #FF4B33). Render the anatomy-PASS illustration for a given defect via RN Image (or expo-image if already transitive — verify, do not add a new library). A defect with no verified illustration renders NOTHING (silent 'hidden' state — no Alert/Toast/error banner). Wire it into result.tsx mapping each flagged defect to its illustration; unverified/failed sets stay hidden (D-15). Honor D-05 (the illustration replaces words, it does not add labels). Light-only, brand-consistent (D-22).
  </action>
  <verify>
    <automated>cd app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - DefectIllustration.tsx exists with a silent 'hidden' fallback (grep hidden); named export + theme tokens only (no hardcoded hex/spacing).
    - result.tsx wires defect → illustration; a defect without a verified asset renders nothing (D-15/D-18 silent-hidden, never a wrong picture).
    - No new image library added (grep package.json diff empty for image libs).
    - tsc --noEmit clean; Simulator render of the wired illustration deferred to 33-16 before OTA (D-21).
  </acceptance_criteria>
  <done>Illustrations wired with silent-hidden fallback; unverified defects show nothing; render eyeball deferred to 33-16.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| bundled asset → app render | static illustration assets rendered by a presentational component |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-49 | Tampering | wrong-anatomy or wrong-direction illustration | mitigate | anatomy 전수 검수; FAIL → set unwired; silent-hidden fallback (D-15/D-18) |
| T-33-50 | Denial of service | missing asset → render crash | mitigate | silent 'hidden' state renders nothing; tsc + Simulator before OTA |
| T-33-SC | Tampering | package installs | accept | no new image library (reuse RN Image / transitive expo-image) |
</threat_model>

<verification>
- app npm run typecheck clean.
- Anatomy 전수 검수 log: every illustration opened + PASS/FAIL recorded; only PASS assets bundled.
- No new package; git diff limited to app/assets/illustrations + DefectIllustration.tsx + result.tsx.
</verification>

<success_criteria>
Anatomy-verified illustrations in the belle-confirmed 2안 style are wired per defect with a silent-hidden fallback, showing the correct fix wordlessly; belle was not re-asked; failed images never ship.
</success_criteria>

<output>
Create `.planning/phases/33-result-trust-recovery/33-14-SUMMARY.md` when done, with the anatomy 검수 PASS/FAIL table.
</output>
