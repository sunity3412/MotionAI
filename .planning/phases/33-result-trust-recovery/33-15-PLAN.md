---
phase: 33-result-trust-recovery
plan: 15
type: execute
wave: 15
depends_on: ["33-12", "33-13", "33-14"]
files_modified:
  - app/src/app/analysis/result.tsx
  - app/src/components/DeductionDetailSheet.tsx
  - app/src/components/ReferenceCornerSection.tsx
  - app/src/lib/resultSections.ts
  - app/src/theme/typography.ts
autonomous: true
requirements: [D-05, D-16, D-17, D-18, D-19, D-20, D-21, D-22]
must_haves:
  truths:
    - "Angle numbers leave the coaching/explanation cards (action language only) and live in the 점수 내역 slot — transparency preserved, not deleted (D-16)"
    - "The contradiction copy is fixed: 점수에 반영되지 않아요 vs 점수에 반영된 비교 순간 no longer coexist (D-16)"
    - "Headline-sized numbers (−17.4, −20, 51점) are demoted below headline scale (D-16)"
    - "The one real bug — body overlapping the status bar (safe-area) — is fixed; the rest is tidy-up, not new features (D-17)"
    - "This wave is not pulled before Wave A, and touches no scoring math (D-20)"
  artifacts:
    - path: "app/src/theme/typography.ts"
      provides: "type-scale consistency sweep; number demotion tokens"
      contains: "typography"
    - path: "app/src/app/analysis/result.tsx"
      provides: "safe-area fix + numbers moved to 내역 + 참고하세요 guidance"
      contains: "SafeArea"
  key_links:
    - from: "app/src/app/analysis/result.tsx"
      to: "app/src/theme/typography.ts tokens"
      via: "number sizes reference demoted tokens, no hardcoded font sizes"
      pattern: "typography\\."
    - from: "coaching card"
      to: "점수 내역 section"
      via: "angle numbers relocated, action language stays in coaching"
      pattern: "deductionBreakdown|내역"
---

<objective>
Wave B tidy-up: move angle numbers out of coaching/explanation cards into the 점수 내역 slot (D-16, keeping the transparent-tally principle), fix the copy contradiction (점수에 반영되지 않아요 vs 반영된 비교 순간), add guidance to the 참고하세요 section, sentence-ify the dash lists, demote headline-sized numbers, and clean up typography/layout — including the ONE real bug: body content overlapping the top status bar (safe-area). Everything else is tidy, not new features (D-17).

Purpose: belle said "디자인은 계속 수정하면 된다" — Wave B follows Wave A and is not pulled forward. It removes number-noise from the explanation layer while keeping the numbers visible where they belong (transparency).

Output: app render/theme changes only; no backend, no scoring, no new UI surface.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-result-trust-recovery/33-CONTEXT.md
@.planning/phases/33-result-trust-recovery/33-PATTERNS.md
@app/src/app/analysis/result.tsx
@app/src/components/DeductionDetailSheet.tsx
@app/src/components/ReferenceCornerSection.tsx
@app/src/theme/typography.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: numbers to 내역 + copy contradiction fix + 참고하세요 guidance + demotion (D-16)</name>
  <read_first>
    - app/src/app/analysis/result.tsx (coaching/explanation cards + 점수 내역 section + 참고하세요/ReferenceCorner)
    - app/src/components/DeductionDetailSheet.tsx (detail sheet copy) + app/src/lib/resultSections.ts (section assembly)
    - 33-CONTEXT.md D-16 (numbers to 내역, contradiction fix, 참고하세요 guidance, dash→sentence, headline demotion)
    - MEMORY scoring-must-be-transparent-deduction-tally (numbers stay visible in 내역, not deleted)
  </read_first>
  <action>
    Remove angle numbers from coaching/explanation cards (action language only) and ensure they appear in the 점수 내역 slot (transparency preserved — move, don't delete). Fix the contradiction copy so 점수에 반영되지 않아요 and 점수에 반영된 비교 순간 no longer coexist for the same content. Add a short guidance line to the 참고하세요 section stating what to look at and why (≤1 new line, D-05). Convert dash lists (이 지표 — … / 확인하기 — …) to sentences. Demote headline-sized numbers (−17.4, −20, 51점) below headline scale using demoted typography tokens (no hardcoded font sizes). No scoring change (D-20).
  </action>
  <verify>
    <automated>cd app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Angle numbers are gone from coaching/explanation cards and present in 점수 내역 (grep: numbers not in coaching copy path; present in deductionBreakdown render).
    - The two contradicting copy strings no longer both apply to the same section.
    - Headline numbers use demoted typography tokens, not hardcoded sizes (D-22).
    - 무엇을 열어서 확인했는가 (D-19): Simulator render of the result screen confirms numbers moved + no contradiction — deferred to 33-16 before OTA; SUMMARY names it.
    - tsc clean; no scoring file changed.
  </acceptance_criteria>
  <done>Numbers relocated to 내역, contradiction fixed, guidance added (≤1 line), headline numbers demoted.</done>
</task>

<task type="auto">
  <name>Task 2: safe-area bug fix + type-scale sweep + 자세히보기 toggle/scroll + margins (D-17)</name>
  <read_first>
    - app/src/app/analysis/result.tsx (top container safe-area + 자세히보기 toggle/scroll + 오늘 고칠 것 scroll affordance + 좌우 여백)
    - app/src/theme/typography.ts:23-65 (existing token set; track() returns 0 — negative letterSpacing → iOS SIGABRT)
    - 33-CONTEXT.md D-17 (safe-area = the only real bug; rest is tidy)
    - MEMORY verify-ui-on-simulator-before-ota (typecheck blind to render crash)
  </read_first>
  <action>
    Fix the body↔status-bar overlap via safe-area handling (react-native-safe-area-context, already a dep) — this is the one real bug. Do a type-scale consistency sweep so screens use typography tokens uniformly (extend tokens, never inline). Fix 자세히보기: re-tap collapses it, and it scrolls to the correct position (not an errant stop). Add a scroll affordance so the extra 오늘 고칠 것 items are discoverable. Unify left/right margins via layout tokens. Keep letterSpacing at 0 (track() → 0, iOS SIGABRT guard). No new UI surface, no scoring, no backend (D-17/D-20).
  </action>
  <verify>
    <automated>cd app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Safe-area overlap fixed (grep SafeArea usage on the top container); the fix is verified by Simulator render at 33-16 (typecheck cannot catch overlap, D-21).
    - Type scale uses tokens uniformly; no hardcoded font sizes/colors/spacing introduced (D-22).
    - 자세히보기 re-tap collapses + scrolls correctly; scroll affordance present.
    - 틀리면 걸리는 장치 (D-18): the safe-area/overlap and toggle/scroll are render-time — their catch is the Simulator eyeball at 33-16, named here as the required proof (not typecheck).
    - tsc clean.
  </acceptance_criteria>
  <done>Safe-area bug fixed, type scale consistent, 자세히보기 toggle/scroll fixed, margins unified — render eyeball deferred to 33-16.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| stored doc → app render | pure render/typography layer; no data write |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-51 | Denial of service | negative letterSpacing → iOS SIGABRT | mitigate | track() returns 0; letterSpacing 0 kept; Simulator render before OTA (D-21) |
| T-33-52 | Tampering | number moved but contradiction persists | mitigate | Simulator eyeball at 33-16 confirms both; D-18 |
| T-33-SC | Tampering | package installs | accept | no new packages (reuse safe-area-context) |
</threat_model>

<verification>
- app npm run typecheck clean (both tasks).
- No scoring/backend file changed (git diff limited to app/).
- Safe-area + number-relocation + toggle/scroll verified by Simulator render at 33-16 before OTA.
</verification>

<success_criteria>
Numbers live in 내역 (not explanations), copy contradiction gone, headline numbers demoted, safe-area bug fixed, typography consistent — tidy-up only, Wave A not disturbed, scoring untouched.
</success_criteria>

<output>
Create `.planning/phases/33-result-trust-recovery/33-15-SUMMARY.md` when done, naming the Simulator checks to run at 33-16.
</output>
