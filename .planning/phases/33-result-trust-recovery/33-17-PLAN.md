---
phase: 33-result-trust-recovery
plan: 17
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - backend/scripts/reprocess_reference_motions_phase4.py
  - backend/shared/python/sunity_shared/firestore_admin.py
  - backend/evals/phase25/run_sweep.py
  - backend/scripts/verify_self_comparison.py
  - backend/tests/phase33/test_candidate_staging.py
autonomous: true
requirements: [D-18, D-20, D-27, D-29, D-30, D-31]
user_setup: []
must_haves:
  truths:
    - "reprocess_reference_motions_phase4.py accepts --version and writes reference/{id}/versions/{candidate}; it REFUSES to overwrite an existing version dir and ASSERTS candidate != current activeVersion — so --no-flip can no longer clobber the active-pointed backing doc (codex concern 1 / D-31)"
    - "A shadow-reference resolver lets eval read a candidate version WITHOUT flipping production: get_reference_motion honors SUNITY_SHADOW_REFERENCE_VERSION (reads reference/{id}/versions/{v}, overlays onto top-level meta) and logs which version+hash was consumed; run_sweep.py and verify_self_comparison.py expose --reference-version (codex concern 3 / D-31)"
    - "The 11-doc activation flip is idempotent and never overwrites versions/pre_phase4: the pre-flip snapshot is written only if absent, and after writing all 11 the flip re-reads all eleven docs and verifies 11/11 activeVersion + per-doc hash against the release manifest (codex concern 7 / D-31)"
    - "A single global release pointer reference/_release.activeCandidate is the authoritative active version; get_reference_motion resolves through it (falling back to top-level activeVersion for backward compat) so activation is one atomic pointer write (codex suggestion 7 / D-31)"
    - "No scoring math changed — versioning/resolver/flip are data-plumbing only; tol/slope/cap/epsilons and per_joint_deviation untouched (D-20/D-29)"
  artifacts:
    - path: "backend/scripts/reprocess_reference_motions_phase4.py"
      provides: "--version candidate-id support, refuse-overwrite, candidate!=active assertion"
      contains: "--version"
    - path: "backend/shared/python/sunity_shared/firestore_admin.py"
      provides: "shadow-reference version resolver + global release pointer resolution + idempotent atomic flip helper"
      contains: "SUNITY_SHADOW_REFERENCE_VERSION"
    - path: "backend/tests/phase33/test_candidate_staging.py"
      provides: "asserts refuse-overwrite, candidate!=active, resolver overlay, flip idempotency + pre_phase4 immutability"
      contains: "activeCandidate"
  key_links:
    - from: "reprocess_reference_motions_phase4.py --version"
      to: "reference/{id}/versions/{candidate}"
      via: "candidate-id write, refuse-overwrite, candidate!=active assert"
      pattern: "--version|refuse|activeVersion"
    - from: "get_reference_motion"
      to: "reference/{id}/versions/{v} + reference/_release.activeCandidate"
      via: "SUNITY_SHADOW_REFERENCE_VERSION overlay OR global-pointer resolution"
      pattern: "SUNITY_SHADOW_REFERENCE_VERSION|_release|activeCandidate"
---

<objective>
Fix the three release-mechanics defects codex flagged in the reference-versioning primitives so the substrate track can stage a genuinely isolated candidate, verify it without touching production, and activate it atomically. This is pure data-plumbing (no Pod, no scoring): (1) immutable candidate version IDs in the reprocess script (codex concern 1); (2) a shadow-reference resolver so 33-06 can sweep the candidate without an undocumented flip (codex concern 3); (3) an idempotent, hash-verified, pre_phase4-preserving 11-doc atomic flip driven by a single global release pointer (codex concerns 7 + suggestion 7). Belle chose full release engineering — the global release pointer is realized here.

Purpose: Without these, `--no-flip` overwrites the active-pointed doc (concern 1), 33-06 tests the OLD reference (concern 3), and a crashed flip leaves a mixed 11-doc library with the rollback snapshot destroyed (concern 7). These are production-data + score-trust risks, not doc polish.

Output: reprocess `--version` support + shadow resolver + global-release-pointer resolution + idempotent atomic flip helper + a phase33 test locking the invariants.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-result-trust-recovery/33-CONTEXT.md
@.planning/phases/33-result-trust-recovery/33-REVIEWS.md
@.planning/debug/ref-student-substrate-gap.md
@backend/scripts/reprocess_reference_motions_phase4.py
@backend/shared/python/sunity_shared/firestore_admin.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: immutable candidate version IDs in reprocess script (--version, refuse-overwrite, candidate!=active)</name>
  <read_first>
    - backend/scripts/reprocess_reference_motions_phase4.py:64 PIPELINE_VERSION="phase4_v1" (the collision), :79-127 _validate_payload_schema (asserts payload.pipelineVersion == PIPELINE_VERSION), :314-333 _write_versioned (writes versions/phase4_v1), :339-404 _flip_active_pointer (sets activeVersion=PIPELINE_VERSION, backs up to versions/pre_phase4), :418-446 argparse (--motions/--target-fps/--no-flip), :195,:301 payload pipelineVersion set
    - 33-REVIEWS.md codex concern 1 + suggestion 1 (candidate id format phase33-cm3-{run_id}, refuse overwrite, run1/run2 separate, assert candidate != active)
    - backend/tests/phase33/conftest.py (sys.path scaffold from 33-01)
  </read_first>
  <behavior>
    - reprocess with --version phase33-cm3-run1 writes reference/{id}/versions/phase33-cm3-run1 and stamps payload.pipelineVersion=phase33-cm3-run1 (schema validator now compares to the resolved version arg, not the frozen constant).
    - Running --version against an id whose versions/{candidate} already exists ABORTS non-zero (refuse-overwrite) — run1 and run2 stay distinct.
    - If the resolved candidate == the doc's current activeVersion, ABORT non-zero (candidate!=active assertion) — prevents the phase4_v1 collision.
  </behavior>
  <action>
    Thread a resolved version through the script instead of the frozen PIPELINE_VERSION constant: add `--version` (default keeps "phase4_v1" for backward-compat callers; phase-33 always passes `phase33-cm3-run1` / `phase33-cm3-run2`). Replace the internal uses so `_reprocess_one` stamps `pipelineVersion=<resolved>`, `_validate_payload_schema` compares against `<resolved>`, and `_write_versioned`/`_flip_active_pointer` use `versions/<resolved>` and `activeVersion=<resolved>`. Add a refuse-overwrite guard in `_write_versioned`: if `reference/{id}/versions/{resolved}` already exists, raise/abort non-zero (do not clobber run1 with run2). Add a candidate!=active assertion before write: read each doc's current `activeVersion`; if it equals `<resolved>`, abort non-zero. Do NOT touch scoring math (D-20). Write backend/tests/phase33/test_candidate_staging.py covering refuse-overwrite + candidate!=active (pure-function / mocked Firestore, no Pod).
  </action>
  <verify>
    <automated>cd backend && python3 -c "import ast; ast.parse(open('scripts/reprocess_reference_motions_phase4.py').read())" && grep -qE "\-\-version" scripts/reprocess_reference_motions_phase4.py && grep -qiE "already exists|refuse|overwrite" scripts/reprocess_reference_motions_phase4.py && python -m pytest tests/phase33/test_candidate_staging.py -q; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `--version` exists; the resolved version (not the frozen constant) is used in _reprocess_one/_validate/_write/_flip (grep + test).
    - Refuse-overwrite: writing to an existing versions/{candidate} aborts non-zero (test asserts).
    - candidate!=active: resolved==current activeVersion aborts non-zero (test asserts).
    - No scoring file changed: git diff limited to the reprocess script + test (D-20/D-29).
    - 틀리면 걸리는 장치 (D-18): the refuse-overwrite + candidate!=active tests are the devices — a phase4_v1 collision or a run1/run2 clobber turns them red.
  </acceptance_criteria>
  <done>reprocess writes to an immutable candidate id, refuses overwrite, and asserts candidate != active — the concern-1 collision is closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: shadow-reference resolver + global release pointer resolution in get_reference_motion (+ eval flags)</name>
  <read_first>
    - backend/shared/python/sunity_shared/firestore_admin.py:1989 get_reference_motion (reads reference/{id} top-level only — the missing resolver), models.reference_motion_path
    - backend/evals/phase25/run_sweep.py:236-252 _run_member (calls pipeline._process which reads the reference), :322 main/argparse
    - backend/scripts/verify_self_comparison.py (reference-as-student path — needs the same version injection, R-3)
    - 33-REVIEWS.md codex concern 3 + suggestions 4,7 (--reference-version / injected provider; prove which candidate hash consumed WITHOUT modifying top-level; prefer a single global release pointer)
  </read_first>
  <behavior>
    - With SUNITY_SHADOW_REFERENCE_VERSION=phase33-cm3-run1 set, get_reference_motion("ref-pdshape") returns the versions/phase33-cm3-run1 fields overlaid on the top-level meta, and logs the version + a content hash of the angles consumed — the top-level doc is NOT modified.
    - With no env and no global pointer, behavior is unchanged (top-level read) — backward compatible.
    - With reference/_release.activeCandidate set (and no shadow env), get_reference_motion resolves through the pointer (reads versions/{activeCandidate}); absent pointer falls back to top-level activeVersion.
    - run_sweep.py --reference-version X and verify_self_comparison.py --reference-version X set the shadow env for the child _process calls.
  </behavior>
  <action>
    In get_reference_motion, add resolution order: (1) if `SUNITY_SHADOW_REFERENCE_VERSION` is set, read `reference/{id}/versions/{v}`, overlay its consumer fields (angles/anglesJointKeys/anglesFrames/joints3d*/keypointReport + downstream derived fields) onto the top-level meta dict, log `resolved shadow reference {id} version={v} anglesHash={sha8}`, and return WITHOUT writing anything (proves which candidate hash was consumed without touching production — codex concern 3 / suggestion 4); (2) else if `reference/_release` doc exists with `activeCandidate`, resolve through that global pointer (read versions/{activeCandidate}); (3) else current top-level behavior (backward compat). Add `--reference-version` to run_sweep.py and verify_self_comparison.py that sets `SUNITY_SHADOW_REFERENCE_VERSION` for the in-process _process calls (SERIAL — do not change concurrency). Extend test_candidate_staging.py to assert the overlay returns candidate angles + logs the hash + leaves top-level untouched, and that absence of env+pointer is unchanged. Do NOT touch scoring (D-20).
  </action>
  <verify>
    <automated>cd backend && grep -qE "SUNITY_SHADOW_REFERENCE_VERSION" shared/python/sunity_shared/firestore_admin.py && grep -qE "activeCandidate|_release" shared/python/sunity_shared/firestore_admin.py && grep -qE "reference-version|reference_version" evals/phase25/run_sweep.py && grep -qE "reference-version|reference_version" scripts/verify_self_comparison.py && python -m pytest tests/phase33/test_candidate_staging.py -q; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - get_reference_motion resolves shadow env → global pointer → top-level, in that order (grep + test); shadow path overlays candidate fields and logs the version+hash WITHOUT writing top-level.
    - run_sweep.py and verify_self_comparison.py both expose --reference-version wired to the shadow env (grep).
    - Backward compat: no env + no pointer returns the unchanged top-level doc (test).
    - No scoring math touched (D-20); the resolver is read-only overlay.
    - 무엇을 열어서 확인했는가 (D-19): the SUMMARY shows a shadow-read log line proving version+hash was consumed and top-level was untouched.
  </acceptance_criteria>
  <done>Eval can consume any candidate version by hash without flipping production; the global release pointer is the authoritative resolver.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: idempotent atomic 11-doc flip helper (immutable pre_phase4, 11/11 hash verify, global-pointer flip)</name>
  <read_first>
    - backend/scripts/reprocess_reference_motions_phase4.py:339-404 _flip_active_pointer (current: sequential writes, backs up to versions/pre_phase4 on EVERY run → overwrites the rollback snapshot; no post-write verification)
    - 33-REVIEWS.md codex concern 7 + suggestion 7 (idempotent per-doc writes, 11/11 post-write hash check, never overwrite pre_phase4, prefer a single global release pointer, resumable/auditable)
    - .planning/debug/ref-student-substrate-gap.md `## 롤백 설계` (pre_phase4 = rollback source; must survive reruns)
    - backend/shared/python/sunity_shared/firestore_admin.py (client accessor _db)
  </read_first>
  <behavior>
    - _flip_active_pointer backs up current top-level to versions/pre_phase4 ONLY IF that doc does not already exist (immutable preimage) — a re-run does not replace the rollback snapshot.
    - After mirroring all 11 top-level docs + setting reference/_release.activeCandidate, the helper re-reads all eleven docs and verifies 11/11 activeVersion == candidate AND per-doc content hash == the manifest's perDocHashes; any mismatch raises (partial-flip detected).
    - Re-running the flip after a crash is safe (idempotent) and converges to 11/11 without corrupting pre_phase4.
  </behavior>
  <action>
    Rework _flip_active_pointer to be idempotent and atomic-at-the-pointer: (a) for each of the 11 docs, write versions/pre_phase4 ONLY if it does not already exist (never overwrite the rollback preimage — codex concern 7); (b) mirror the candidate consumer fields to top-level with idempotent set/merge; (c) set the global release pointer `reference/_release.activeCandidate = candidate` (single authoritative flip — activation is one pointer write); (d) POST-WRITE VERIFY: re-read all 11 docs and assert 11/11 have the candidate active + per-doc hash matches the passed manifest hashes, raising on any mismatch (partial activation is caught, not silently shipped). Make it resumable (safe to re-run). Accept the release-manifest dict (from 33-18) as the hash source. Do NOT touch scoring (D-20). Extend test_candidate_staging.py: pre_phase4 not overwritten on 2nd run; a doctored 11th-doc hash makes the post-write verify raise.
  </action>
  <verify>
    <automated>cd backend && grep -qiE "exists|already" scripts/reprocess_reference_motions_phase4.py && grep -qE "pre_phase4" scripts/reprocess_reference_motions_phase4.py && python -m pytest tests/phase33/test_candidate_staging.py -q; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - pre_phase4 is written only when absent (immutable preimage) — test: a 2nd flip run leaves the first pre_phase4 bytes unchanged.
    - Post-write verify reads ALL 11 docs and fails on any hash/version mismatch (test: doctored hash → raise), not one fixture (codex concern 7).
    - Flip is idempotent/resumable and drives the global release pointer reference/_release.activeCandidate.
    - No scoring math touched (D-20/D-29).
    - 이 산출물이 틀렸다면 어떻게 알았을까: answered — a partial 11-doc flip or an overwritten pre_phase4 turns the post-write verify / immutability test red.
  </acceptance_criteria>
  <done>The flip is idempotent, preserves the rollback preimage, verifies 11/11 by hash, and activates via one global pointer — the concern-7 non-atomic flip is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| reprocess script → reference/{id}/versions/{candidate} | staged write must not touch the active-pointed doc |
| eval → shadow version read | candidate consumed read-only; production top-level untouched |
| flip → global release pointer + 11-doc mirror | atomic activation; pre_phase4 preimage immutable |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-60 | Tampering | candidate write clobbers active-pointed doc | mitigate | candidate!=active assert + refuse-overwrite (concern 1) |
| T-33-61 | Tampering | eval flips top-level to test candidate | mitigate | shadow resolver overlay is read-only; logs version+hash (concern 3) |
| T-33-62 | Tampering | crashed flip leaves mixed 11-doc library / destroys pre_phase4 | mitigate | idempotent flip + immutable pre_phase4 + 11/11 post-write hash verify (concern 7) |
| T-33-SC | Tampering | package installs | accept | no new packages (stdlib hashlib, existing firebase-admin) |
</threat_model>

<verification>
- reprocess --version writes versions/{candidate}, refuses overwrite, asserts candidate!=active.
- get_reference_motion resolves shadow env → global pointer → top-level; shadow read logs version+hash, no top-level write.
- flip idempotent, pre_phase4 immutable, 11/11 hash-verified, driven by reference/_release pointer.
- No scoring math touched (D-20/D-29); test_candidate_staging.py green.
</verification>

<success_criteria>
The reference-versioning primitives support a genuinely isolated candidate, a read-only shadow verification path, and an atomic hash-verified activation via a single global release pointer — closing codex concerns 1, 3, and 7 with scoring untouched.
</success_criteria>

<output>
Create `.planning/phases/33-result-trust-recovery/33-17-SUMMARY.md` when done, recording the candidate-id format, the shadow-read log proving read-only consumption, and the flip idempotency/hash-verify evidence.
</output>
</content>
</invoke>
