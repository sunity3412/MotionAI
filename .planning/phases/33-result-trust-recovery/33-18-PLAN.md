---
phase: 33-result-trust-recovery
plan: 18
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - backend/scripts/release_manifest.py
  - backend/scripts/gate_check.py
  - backend/runpod_inference/server.py
  - backend/tests/phase33/test_release_manifest.py
autonomous: true
requirements: [D-18, D-20, D-27, D-29, D-30, D-31]
user_setup: []
must_haves:
  truths:
    - "A release manifest ties the substrate together as ONE tuple: candidate reference id, per-document hashes, code commit SHA, target fps (9.0), PR_INVERSION_ENABLED flag, RTMW_DETERMINISTIC flag, derived-field schema version, verification result — both activation (33-07) and rollback operate on this tuple (codex suggestion 2 / D-31)"
    - "release_manifest.py can create/update/verify the manifest and publishes it to the global release pointer reference/_release (the same doc 33-17's resolver reads) + a local JSON — a partial or mismatched tuple fails verification (D-31)"
    - "The RunPod server exposes its code commit SHA + a model-init canary in /health so a warm-Pod reuse can prove source revision + model parity, not just liveness (codex concern 6 / D-30)"
    - "gate_check.py is a JSON data-gate: it parses a JSON evidence file and exits non-zero unless caller-named assertions hold (all-PASS, no rollback trigger, 11/11 hash match, scoring-constant manifest match) — replacing grep-for-a-word gates (codex concern 8 / D-18)"
    - "No scoring math changed — manifest/gate/health are release plumbing (D-20/D-29)"
  artifacts:
    - path: "backend/scripts/release_manifest.py"
      provides: "create/update/verify the release tuple; publish to reference/_release + local JSON"
      contains: "commitSha"
    - path: "backend/scripts/gate_check.py"
      provides: "JSON data-gate — parse + fail unless named assertions hold (no grep-for-word)"
      contains: "argparse"
    - path: "backend/runpod_inference/server.py"
      provides: "/health exposes commit SHA + model-init canary"
      contains: "commit"
  key_links:
    - from: "release_manifest.py"
      to: "reference/_release (global release pointer) + local manifest JSON"
      via: "the tuple both activation and rollback consume"
      pattern: "_release|commitSha|perDocHashes"
    - from: "gate_check.py"
      to: "33-06 / 33-07 evidence JSON"
      via: "parse + assert; non-zero on any non-PASS"
      pattern: "gate_check|assert|PASS"
---

<objective>
Build the release-engineering tooling belle scoped in full: (1) a release manifest that binds candidate reference id + per-doc hashes + code commit SHA + fps + PR/deterministic flags + derived schema version + verification result into one tuple (codex suggestion 2), published to the global release pointer `reference/_release` that 33-17's resolver reads; (2) a commit-SHA + model-init canary in the RunPod `/health` so warm-Pod reuse proves source/model parity, not just liveness (codex concern 6); (3) `gate_check.py`, a JSON data-gate that replaces the grep-for-a-word gates codex flagged (concern 8). Code-only, no Pod, no scoring.

Purpose: C+M3 is valid ONLY as the tuple {9fps reference, PR=1, M3 code, derived fields}. The manifest makes that tuple explicit so activation and rollback operate on it atomically; the health canary stops a stale-code sweep; the data-gate stops a "document containing the word PASS" from passing.

Output: release_manifest.py + gate_check.py + /health commit-SHA+canary + a phase33 test locking the manifest verify + gate semantics.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-result-trust-recovery/33-CONTEXT.md
@.planning/phases/33-result-trust-recovery/33-REVIEWS.md
@.planning/debug/ref-student-substrate-gap.md
@backend/runpod_inference/server.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: release_manifest.py — create/update/verify the substrate release tuple + publish to reference/_release</name>
  <read_first>
    - 33-REVIEWS.md codex suggestion 2 (manifest fields: candidate reference id, per-document hashes, code commit SHA, target fps, PR flag, deterministic flag, derived-field schema version, verification result) + suggestion 7 (global release pointer)
    - backend/scripts/reprocess_reference_motions_phase4.py (candidate id + payload hashes — the manifest's per-doc hash source; 33-17 adds --version)
    - backend/shared/python/sunity_shared/firestore_admin.py (_db client accessor + the reference/_release pointer 33-17 resolves through)
    - backend/tests/phase33/conftest.py (sys.path scaffold)
  </read_first>
  <behavior>
    - `release_manifest.py create --candidate phase33-cm3-run1 --commit <sha> ...` writes a manifest JSON with the 8 tuple fields (candidate, perDocHashes {11 ids}, commitSha, targetFps=9.0, prInversionEnabled=true, rtmwDeterministic=true, derivedFieldSchemaVersion, verificationResult=null) locally.
    - `release_manifest.py verify --manifest <path>` re-reads the 11 candidate docs, recomputes per-doc hashes, and exits non-zero unless all 11 match the manifest + the tuple is complete.
    - `release_manifest.py publish --manifest <path>` writes the tuple to reference/_release (the global pointer 33-17 reads) — activation source.
  </behavior>
  <action>
    Create backend/scripts/release_manifest.py with create/update/verify/publish subcommands over the tuple {candidateVersion, perDocHashes(11), commitSha, targetFps, prInversionEnabled, rtmwDeterministic, derivedFieldSchemaVersion, verificationResult, updatedAt}. `verify` recomputes each candidate doc's content hash from reference/{id}/versions/{candidate} and fails non-zero on any mismatch or missing field (this is the tuple integrity gate). `publish` writes the tuple to reference/_release.activeCandidate + the full manifest (the global release pointer 33-17's resolver + 33-07's flip consume). Reuse firestore_admin._db (never hand-roll a client). Keep it read/verify-only except the explicit `publish`. Do NOT touch scoring (D-20). Write backend/tests/phase33/test_release_manifest.py covering create→verify (match), verify (mismatched hash → non-zero), and the tuple-completeness check.
  </action>
  <verify>
    <automated>cd backend && python3 -c "import ast; ast.parse(open('scripts/release_manifest.py').read())" && grep -qE "commitSha|perDocHashes|verificationResult" scripts/release_manifest.py && grep -qE "_release|activeCandidate" scripts/release_manifest.py && python -m pytest tests/phase33/test_release_manifest.py -q; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - release_manifest.py has create/update/verify/publish over the 8-field tuple (grep + test).
    - verify recomputes per-doc hashes and fails on any mismatch / missing field (test asserts non-zero).
    - publish writes reference/_release.activeCandidate + the manifest (the global pointer 33-17 resolves).
    - No scoring file changed (D-20/D-29).
    - 틀리면 걸리는 장치 (D-18): a doctored candidate doc or an incomplete tuple makes verify exit non-zero — the manifest is the substrate's single source of truth for activation/rollback.
  </acceptance_criteria>
  <done>The substrate release tuple exists as a verifiable manifest and a global release pointer.</done>
</task>

<task type="auto">
  <name>Task 2: RunPod /health exposes commit SHA + model-init canary (warm-Pod stale-code guard)</name>
  <read_first>
    - backend/runpod_inference/server.py (GET /health handler + startup model-init; X-RunPod-Token auth) — the endpoint that currently proves liveness only
    - 33-REVIEWS.md codex concern 6 + suggestion 11 (warm-Pod reuse can run stale code; expose commit SHA in health; commit/env/model-init canary, not just /health 200)
    - MEMORY current-pod-hbpvhedq2bu01i (start_server env) + rtmw-blackwell-lean-bootstrap
  </read_first>
  <action>
    Extend the RunPod server /health response to include: the code commit SHA (read from a build-time env var e.g. SUNITY_COMMIT_SHA set by start_server, or `git rev-parse HEAD` captured at boot), the key env flags (PR_INVERSION_ENABLED, RTMW_DETERMINISTIC), and a model-init canary (model loaded + a cheap deterministic self-check result). This lets 33-04/33-06/33-07 confirm the warm Pod runs the EXACT commit + model, not just that it is alive. Keep the endpoint token-gated; do not log secrets (T-04-W5-01). No scoring change (D-20). This is a server-code change only — the actual deploy/canary check happens in the substrate plans that reuse the Pod.
  </action>
  <verify>
    <automated>cd backend && python3 -c "import ast; ast.parse(open('runpod_inference/server.py').read())" && grep -qiE "commit|SUNITY_COMMIT_SHA|rev-parse" runpod_inference/server.py && grep -qE "PR_INVERSION_ENABLED|RTMW_DETERMINISTIC" runpod_inference/server.py && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - /health returns commit SHA + env flags + a model-init canary field (grep + code read).
    - Token gating preserved; no secret values logged/returned.
    - No scoring math touched (D-20).
    - 무엇을 열어서 확인했는가 (D-19): the SUMMARY shows a sample /health body with the commit + canary fields.
  </acceptance_criteria>
  <done>/health proves source revision + model parity, enabling the warm-Pod stale-code guard in 33-04/06/07.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: gate_check.py — JSON data-gate replacing grep-for-a-word gates</name>
  <read_first>
    - 33-REVIEWS.md codex concern 8 + suggestion 9 (parse JSON, fail unless: all eight 33-06 items exactly PASS; no rollback trigger true; all eleven candidate docs share expected hash/version; immutable scoring constants + formula files match a pre-phase manifest) + concern re: 33-05 trailing `|| echo ... OK`
    - .planning/debug/ref-student-substrate-gap.md `## 성공 판정 기준` (the 8 items the gate encodes)
    - backend/tests/phase33/conftest.py
  </read_first>
  <behavior>
    - `gate_check.py --file evidence.json --require-all-pass items` exits 0 only if every listed item's status is exactly "PASS"; a file that merely CONTAINS the substring PASS but has a FAIL item exits non-zero (fixing concern 8).
    - `gate_check.py --file manifest.json --require-hashes 11` exits non-zero unless all 11 perDocHashes are present and match.
    - `gate_check.py --file evidence.json --no-rollback-trigger` exits non-zero if any rollback-trigger flag is true.
    - A scoring-constants mode compares tol/slope/cap/epsilon values in a JSON against a pinned pre-phase manifest and fails on drift.
  </behavior>
  <action>
    Create backend/scripts/gate_check.py: a JSON parser gate with modes `--require-all-pass <keys>` (every named item.status == "PASS", non-zero otherwise), `--require-hashes <n>` (n perDocHashes present + match), `--no-rollback-trigger` (all rollback-trigger flags false), and `--scoring-constants-match <pinned.json>` (tol 20 / slope 1.2 / cap 90 / MEAN_EPSILON_DEG / P99_EPSILON_DEG equal a pre-phase manifest — the D-20/D-29 immutability check as data, not grep). It reads structured JSON, never greps prose. Exit code is the gate. Write backend/tests/phase33/test_gate_check.py: a doc with one FAIL item exits non-zero even though it contains the word PASS; a matching all-PASS doc exits 0; a scoring-constant drift exits non-zero. No scoring change (D-20).
  </action>
  <verify>
    <automated>cd backend && python3 -c "import ast; ast.parse(open('scripts/gate_check.py').read())" && grep -qE "require-all-pass|no-rollback-trigger|scoring-constants" scripts/gate_check.py && python -m pytest tests/phase33/test_gate_check.py -q; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - gate_check.py parses JSON and exits non-zero unless the named assertions hold; a doc containing "PASS" but with a FAIL item still fails (test asserts — closes concern 8).
    - Modes exist for all-PASS, hash count, no-rollback-trigger, scoring-constants-match (grep + test).
    - The scoring-constants mode is a data comparison against a pinned manifest — the D-20/D-29 immutability check (test).
    - 이 산출물이 틀렸다면 어떻게 알았을까: answered — test_gate_check.py proves a grep-passable-but-failing doc is rejected.
  </acceptance_criteria>
  <done>Downstream verification uses a JSON data-gate; grep-for-a-word gates are retired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| manifest tuple → reference/_release | the authoritative activation/rollback source; verify before publish |
| Pod /health → warm-reuse callers | commit SHA + canary proves source/model parity |
| evidence JSON → gate_check | machine gate; no prose grep |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-63 | Tampering | incomplete/mismatched release tuple activated | mitigate | release_manifest verify (per-doc hash + completeness) before publish (concern 2) |
| T-33-64 | Spoofing | warm Pod runs stale code | mitigate | /health commit SHA + model-init canary (concern 6) |
| T-33-65 | Tampering | a FAIL doc passes a grep gate | mitigate | gate_check JSON data-gate — exact PASS on every named item (concern 8) |
| T-33-SC | Tampering | package installs | accept | no new packages (stdlib json/hashlib/subprocess) |
</threat_model>

<verification>
- release_manifest.py create/verify/publish over the 8-field tuple; verify fails on hash mismatch.
- /health exposes commit SHA + env flags + model-init canary.
- gate_check.py rejects a grep-passable-but-failing doc; scoring-constants mode is data-based.
- No scoring math touched (D-20/D-29); phase33 tests green.
</verification>

<success_criteria>
The substrate is representable and verifiable as one release tuple, warm-Pod reuse can prove source/model parity, and downstream gates parse JSON instead of grepping prose — realizing belle's full-scope release engineering (codex suggestions 2, 7, 9; concerns 6, 8).
</success_criteria>

<output>
Create `.planning/phases/33-result-trust-recovery/33-18-SUMMARY.md` when done, with a sample manifest tuple, a sample /health body (commit + canary), and the gate_check reject-a-failing-doc proof.
</output>
</content>
