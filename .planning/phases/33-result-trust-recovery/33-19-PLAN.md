---
phase: 33-result-trust-recovery
plan: 19
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - .planning/phases/33-result-trust-recovery/33-M3-SPEC.md
autonomous: true
requirements: [D-02, D-18, D-20, D-27, D-29]
user_setup: []
must_haves:
  truths:
    - "The M3 alignment change is fully specified in 33-M3-SPEC.md BEFORE any M3 code is written — 33-05 (M3 code) is BLOCKED on this locked spec (codex concern 4 / suggestion 5 / D-27)"
    - "The spec defines a PAIRED user AND reference range: find_action_segment / MotionMatch must carry a reference (start,end) too — today MotionMatch only holds a user (start,end) and F_ref is consumed whole (motiondtw.py:73-100), so 'window the reference when nu<nr' cannot be expressed without an API change"
    - "The spec fixes a minimum reference-phase COVERAGE FLOOR so a best-match reference window can NEVER silently drop hard reference phases (the inflated-score risk codex named); it defines boundary behavior for nu<nr and fail-closed behavior when alignment is ambiguous"
    - "The spec pins alignment-only invariants: already-aligned inputs → byte-identical per_joint_deviation; identical inputs → zero deviation; NO motion-key branches (D-02); no reference phase removed below the coverage floor; formula/constants hash-identical (D-20/D-29)"
    - "The spec names the 3 ripple sites (pipeline/app.py:1770, :4015, safety_flags.py:245) and the safety-flag regression obligation"
  artifacts:
    - path: ".planning/phases/33-result-trust-recovery/33-M3-SPEC.md"
      provides: "the locked M3 alignment spec — paired ranges, coverage floor, boundary/fail-closed, invariants, ripple sites, test obligations"
      contains: "coverage floor"
  key_links:
    - from: "33-M3-SPEC.md"
      to: "backend/shared/python/sunity_shared/analysis/motiondtw.py (find_action_segment / MotionMatch)"
      via: "the paired-range API + coverage-floor contract 33-05 must implement"
      pattern: "MotionMatch|find_action_segment|coverage"
    - from: "33-M3-SPEC.md invariants"
      to: "per_joint_deviation byte-identical / zero-on-identity"
      via: "alignment-only proof obligations for 33-05 tests"
      pattern: "byte-identical|identity|deviation"
---

<objective>
Write and lock the M3 alignment spec BEFORE M3 code (codex concern 4 — the concern that most directly touches Core Value / score trust). codex's finding is precise: `find_action_segment` returns only a user `(start,end)`, and `MotionMatch` (motiondtw.py:72-77) carries no reference range, so `motion_dtw` consumes `F_ref` whole (`dtw(F_user[s:e], F_ref)`). The SEED's "window the reference when nu<nr" therefore CANNOT be expressed by the current API without carrying a reference range — and a naive best-match reference window could silently remove difficult reference phases and systematically inflate scores. This plan produces `33-M3-SPEC.md`: the paired-range API, a reference-phase coverage floor, boundary/fail-closed rules, and alignment-only invariants. 33-05 is blocked on it. Doc-only, no code, no Pod.

Purpose: "Scoring files unchanged" does NOT prove alignment-only behavior (codex). Segment selection changes the deduction INPUT even with an unchanged formula. The spec is the contract that makes the M3 code provably alignment-only and non-inflating.

Output: 33-M3-SPEC.md — the locked spec 33-05 implements and tests against.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-result-trust-recovery/33-CONTEXT.md
@.planning/phases/33-result-trust-recovery/33-REVIEWS.md
@.planning/debug/ref-student-substrate-gap.md
@backend/shared/python/sunity_shared/analysis/motiondtw.py
@backend/shared/python/sunity_shared/analysis/segments.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: author 33-M3-SPEC.md — paired-range API, coverage floor, boundary/fail-closed, invariants, ripple + tests</name>
  <read_first>
    - backend/shared/python/sunity_shared/analysis/motiondtw.py:72-100 (MotionMatch has start/end/distance/path but NO reference range; find_action_segment returns (int,int) user-only; nu<=nr → (0,nu) whole; motion_dtw calls dtw(F_user[s:e], F_ref) — F_ref whole) + per_joint_deviation:103-130 (uses A_ref full, path (u,r); identity path → median 0)
    - backend/shared/python/sunity_shared/analysis/segments.py (per_joint_deviation consumer; ref_boundary_frame/split_path_by_ref depend on ref index space — a reference window shifts ref indices, so segments must stay consistent)
    - .planning/debug/ref-student-substrate-gap.md M3 rows (`nu<=nr` 12/12 무력화; density made nu≈nr; "window reference to student length OR two-way sliding by the shorter side; the 준비/대기 removal must actually fire") + `## 성공 판정 기준` item #5 (M3 fires) + ripple 3곳 (pipeline:1770/:4015, safety_flags:245)
    - 33-REVIEWS.md codex concern 4 + suggestion 5 (paired user+reference ranges; minimum reference-phase coverage; boundary nu<nr; fail-closed on ambiguity; invariants: already-aligned→byte-identical deviations, identical→zero, no motion-key branches, no reference phase below floor, formula/constants hash-identical)
  </read_first>
  <action>
    Author 33-M3-SPEC.md with these locked sections: (1) API — extend MotionMatch to carry a reference range (ref_start, ref_end) alongside the user (start,end), and define find_action_segment's new signature returning both ranges; specify how motion_dtw + per_joint_deviation + segments.py consume the windowed reference consistently (ref index space must stay aligned with split_path_by_ref/ref_boundary_frame). (2) COVERAGE FLOOR — a minimum fraction of reference phases/frames that ANY chosen reference window must retain (define the floor value and its rationale from the SEED density facts); a window that would drop reference phases below the floor is rejected. (3) BOUNDARY nu<nr — the exact rule for windowing the reference to the student length (or two-way slide by the shorter side) so the 준비/대기 removal actually fires, and the rule for nu≈nr after density alignment. (4) FAIL-CLOSED — when alignment is ambiguous (multiple near-equal windows / coverage floor violated), the defined safe behavior (e.g. fall back to full-reference alignment, never a silently-truncated one). (5) INVARIANTS — already-aligned inputs produce byte-identical per_joint_deviation; identical inputs produce zero; NO motion-key branches (D-02); no reference phase silently removed below the floor; tol 20°/slope 1.2/cap 90/epsilons + per_joint_deviation formula hash-identical (D-20/D-29). (6) RIPPLE + TEST OBLIGATIONS — the 3 call sites (pipeline:1770/:4015, safety_flags:245) and the exact tests 33-05 must add (byte-identical-deviation test, zero-on-identity test, coverage-floor test, nu<nr fires test, safety-flag no-FP/FN regression). Mark the doc LOCKED — 33-05 implements it verbatim; deviations require re-locking here first.
  </action>
  <verify>
    <automated>test -f .planning/phases/33-result-trust-recovery/33-M3-SPEC.md && grep -qiE "coverage floor" .planning/phases/33-result-trust-recovery/33-M3-SPEC.md && grep -qiE "ref_start|reference range|reference (start|window)" .planning/phases/33-result-trust-recovery/33-M3-SPEC.md && grep -qiE "byte-identical|fail-closed" .planning/phases/33-result-trust-recovery/33-M3-SPEC.md && grep -qE "1770|4015|safety_flags" .planning/phases/33-result-trust-recovery/33-M3-SPEC.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The spec defines a paired user+reference range API for MotionMatch/find_action_segment (the API gap codex named) and how segments.py stays consistent.
    - A numeric coverage floor is specified with rationale; a below-floor window is rejected (no silent phase drop → no score inflation).
    - Boundary nu<nr, nu≈nr, and fail-closed-on-ambiguity behaviors are each specified.
    - The alignment-only invariants are pinned (byte-identical deviations / zero-on-identity / no motion-key branches / constants hash-identical) with the exact 33-05 test obligations, and the 3 ripple sites are named.
    - 틀리면 걸리는 장치 (D-18): the spec names the tests that will fail if M3 leaks into scoring (byte-identical + zero-on-identity + coverage-floor + safety-flag regression) — these are 33-05's RED tests.
  </acceptance_criteria>
  <done>33-M3-SPEC.md is a locked, testable contract; 33-05 cannot start until it exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| M3 spec → M3 code (33-05) | the spec is the contract; code without spec risks a scoring leak |
| reference window → deduction input | a below-floor window inflates scores by dropping hard phases |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-66 | Tampering | reference window silently drops hard phases → inflated scores | mitigate | coverage floor + fail-closed spec; below-floor window rejected (concern 4) |
| T-33-67 | Tampering | M3 change leaks into scoring behavior | mitigate | byte-identical-deviation + zero-on-identity invariants as 33-05 tests (concern 4 / D-20) |
| T-33-68 | Repudiation | ambiguous alignment resolved arbitrarily | mitigate | fail-closed rule specified (fall back to full-reference, never silent truncation) |
| T-33-SC | Tampering | package installs | accept | doc-only, no code |
</threat_model>

<verification>
- 33-M3-SPEC.md exists and is LOCKED with: paired user+reference range API, coverage floor + rationale, boundary nu<nr, fail-closed rule, alignment-only invariants, 3 ripple sites, 33-05 test obligations.
- No code changed (doc-only); 33-05 depends_on this plan.
</verification>

<success_criteria>
The M3 alignment change is fully specified and locked before any code — paired ranges, a coverage floor that forbids silent phase-dropping, fail-closed ambiguity handling, and byte-identical-deviation invariants — so the eventual M3 implementation is provably alignment-only and non-inflating (codex concern 4, the Core-Value-critical one).
</success_criteria>

<output>
Create `.planning/phases/33-result-trust-recovery/33-19-SUMMARY.md` when done, confirming the spec is locked and listing the invariants + coverage-floor value that 33-05 must satisfy.
</output>
