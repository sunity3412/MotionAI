---
phase: 33-result-trust-recovery
plan: 20
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - .planning/phases/33-result-trust-recovery/33-COVERAGE-MATRIX.md
autonomous: true
requirements: [D-18, D-23, D-27]
user_setup: []
must_haves:
  truths:
    - "ONE canonical 11-motion coverage matrix exists — every coverage plan (33-06, 33-08, 33-09, 33-16) cites it instead of each carrying a conflicting inventory (codex suggestion 10 / D-23)"
    - "The matrix resolves the conflicts codex found: 11−6 = 5 non-paired motions (not 4); 33-06's four excluded sideway-spin while 33-16's four included it but dropped combo; REGISTERED_MOTIONS has 10 (combo unregistered); climb lacks the mode1 margin/separation substrate — each of climb, sideway-spin, combo is explicitly resolved before execution"
    - "For each of the 11 references the matrix records: registered/unregistered, paired fixture (yes/no), success/fault availability, self-comparison substitute, M8 visual-check inclusion, presentation coverage — no silent skip (D-18/D-23)"
    - "Fixture-less motions have a NAMED substitute proof (self-comparison / belle real doc); the matrix states what each verification plan actually checks per motion"
  artifacts:
    - path: ".planning/phases/33-result-trust-recovery/33-COVERAGE-MATRIX.md"
      provides: "the single 11-motion coverage table + explicit climb/sideway-spin/combo resolution"
      contains: "coverage"
  key_links:
    - from: "33-COVERAGE-MATRIX.md"
      to: "REGISTERED_MOTIONS (10) + phase25 fixtures (6) + reference library (11)"
      via: "reconciled inventory of what is registered / paired / substitute-verified"
      pattern: "registered|fixture|self-comparison"
    - from: "33-COVERAGE-MATRIX.md"
      to: "33-06 / 33-08 / 33-09 / 33-16 coverage claims"
      via: "single cited source for per-motion verification"
      pattern: "33-06|33-08|33-09|33-16"
---

<objective>
Produce the ONE canonical 11-motion coverage matrix codex asked for (suggestion 10 / D-23). Today the coverage inventories conflict across 33-06/08/09/16: 11−6 leaves FIVE non-paired motions (not four); 33-06's four exclude sideway-spin while 33-16's four include it but drop combo; `REGISTERED_MOTIONS` actually has TEN (combo is unregistered); and climb lacks the mode1 margin/separation substrate 33-06 assumes. This plan reconciles all of that into a single table that every coverage plan cites. Doc-only, no code, no Pod.

Purpose: Without one matrix, a motion can silently fall through the gaps of four disagreeing inventories — a D-18 silent-skip and a D-23 "6동작 전수" violation. The matrix makes per-motion verification explicit and forces climb/sideway-spin/combo to be resolved before execution.

Output: 33-COVERAGE-MATRIX.md — the single source cited by 33-06, 33-08, 33-09, 33-16.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-result-trust-recovery/33-CONTEXT.md
@.planning/phases/33-result-trust-recovery/33-REVIEWS.md
@.planning/debug/ref-student-substrate-gap.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: reconcile the inventories + author 33-COVERAGE-MATRIX.md (climb/sideway-spin/combo resolved)</name>
  <read_first>
    - .planning/debug/ref-student-substrate-gap.md `## 재처리 위험도 표` (the authoritative 11-motion list + which are fixtures vs fixture-less + climb = mode1 comparison-gate only, no score) + R-3 (fixture-less foxtop/foxtop-split/invert/combo → self-comparison substitute)
    - 33-REVIEWS.md codex suggestion 10 + the coverage-conflict finding (5 not 4; 33-06 vs 33-16 disagreement; REGISTERED_MOTIONS = 10, combo unregistered; climb lacks mode1 substrate)
    - backend source of truth for REGISTERED_MOTIONS (grep the registered-motion constant + the 6 phase25 fixtures) — verify the actual counts, do not trust prose
    - 33-06 / 33-08 / 33-09 / 33-16 PLAN.md coverage claims (the inventories to reconcile)
  </read_first>
  <action>
    First VERIFY the raw counts against code (grep the registered-motion constant and the phase25 fixture set) so the matrix is grounded, not asserted. Then author 33-COVERAGE-MATRIX.md with one row per the 11 references, columns: motionId, registered (yes/no — combo currently no), paired fixture (yes/no), success available, fault available, self-comparison substitute (yes/no + script), M8 visual-check included (yes/no), presentation coverage (which of A-1/A-2/A-5 apply), verification owner plan (33-06 sweep vs 33-16 gate vs self-comparison). Explicitly RESOLVE the three ambiguous motions in prose: (a) climb — how mode1 comparison-gate-only (no score) is verified given it lacks margin/separation substrate; (b) sideway-spin — its verification path (fixture-less → self-comparison); (c) combo — that it is UNREGISTERED, whether that is intended, and how it is still substrate-verified (self-comparison, R-4 determinism). State the reconciled non-paired count (5) and name each. Mark the doc CANONICAL — 33-06/08/09/16 must cite it and MUST NOT carry a divergent inventory.
  </action>
  <verify>
    <automated>test -f .planning/phases/33-result-trust-recovery/33-COVERAGE-MATRIX.md && grep -qiE "climb" .planning/phases/33-result-trust-recovery/33-COVERAGE-MATRIX.md && grep -qiE "sideway-spin" .planning/phases/33-result-trust-recovery/33-COVERAGE-MATRIX.md && grep -qiE "combo" .planning/phases/33-result-trust-recovery/33-COVERAGE-MATRIX.md && grep -qiE "self.comparison|self comparison" .planning/phases/33-result-trust-recovery/33-COVERAGE-MATRIX.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The matrix has one row per 11 references with the named columns; the non-paired count is stated as 5 with each named.
    - climb, sideway-spin, and combo are each explicitly resolved (registered status, verification path, substitute) — no ambiguity left for execution.
    - REGISTERED_MOTIONS count and the 6-fixture set are grounded against code (grep), not prose.
    - Each fixture-less motion has a named substitute proof (self-comparison script), no silent skip (D-18/D-23).
    - 이 산출물이 틀렸다면 어떻게 알았을까: answered — a motion missing a verification owner or a substitute is a visible blank cell, caught before 33-06 runs.
  </acceptance_criteria>
  <done>One canonical coverage matrix exists; climb/sideway-spin/combo resolved; 33-06/08/09/16 have a single source to cite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| four plan inventories → one matrix | conflicting coverage claims reconciled into a single cited source |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-69 | Repudiation | a motion silently unverified across disagreeing inventories | mitigate | single matrix with a verification owner + substitute per motion (suggestion 10 / D-23) |
| T-33-70 | Tampering | combo (unregistered) assumed registered | mitigate | matrix records registered=no + its substrate-verification path explicitly |
| T-33-SC | Tampering | package installs | accept | doc-only |
</threat_model>

<verification>
- 33-COVERAGE-MATRIX.md exists; 11 rows; non-paired = 5 named; climb/sideway-spin/combo resolved; counts grounded against code.
- Every fixture-less motion has a named substitute; each motion has a verification owner plan.
</verification>

<success_criteria>
A single canonical coverage matrix resolves the four-way inventory conflict and forces climb, sideway-spin, and combo to explicit resolution — the D-23 전수 obligation is now checkable against one source (codex suggestion 10).
</success_criteria>

<output>
Create `.planning/phases/33-result-trust-recovery/33-20-SUMMARY.md` when done, listing the reconciled counts and the climb/sideway-spin/combo resolutions.
</output>
</content>
