---
phase: 33-result-trust-recovery
plan: 21
type: execute
wave: 7
depends_on: ["33-06"]
gap_closure: true
conditional: true
files_modified:
  - .planning/phases/33-result-trust-recovery/33-S4B-ELBOW-TWIST-HALT.md
autonomous: false
requirements: [D-18, D-20, D-27, D-29, D-30, D-31, D-32]
user_setup: []
must_haves:
  truths:
    - "This plan is the formal HALT loop for the elbow-twist branch (codex concern 13 / D-32): it runs ONLY if 33-06 recorded elbow-twist success margin < +2.0° after reprocess; if 33-06 recorded ≥ +2.0°, this plan is a NO-OP (mark complete with a one-line SUMMARY citing 33-06's margin)"
    - "On HALT, the branch returns THROUGH the substrate loop — reprocess (33-03) / backfill (33-04) / M3 (33-05) / re-verify (33-06) as needed — it does NOT resolve inline and does NOT unblock 33-07 until re-verification passes (codex concern 13)"
    - "The single belle question (D-32ⓑ) is asked only here and only when margin < +2.0°: 정은지 정상 영상 vs 기준 영상 나란히 — 같은 동작으로 보이십니까? (no scores/angles/numbers); the pre-decided branch runs without a second belle question"
    - "33-07 (flip) is BLOCKED on this plan's completion — either the no-op skip (margin ok) or the completed re-verification (margin recovered / reference re-registered)"
    - "No threshold refit — margin recovery comes from re-extraction, never from raising tol (D-20/D-29)"
  artifacts:
    - path: ".planning/phases/33-result-trust-recovery/33-S4B-ELBOW-TWIST-HALT.md"
      provides: "HALT decision record: 33-06 margin, no-op vs HALT, belle question+answer (if asked), branch executed, re-verification result"
      contains: "margin"
  key_links:
    - from: "33-06 elbow-twist margin < +2.0°"
      to: "belle single question (D-32ⓑ) → re-register OR technical trace"
      via: "SEED elbow-twist 처방 경로, pre-decided branches"
      pattern: "elbow-twist|margin|처방"
    - from: "33-21 completion"
      to: "33-07 flip unblock"
      via: "no-op skip (margin ok) OR completed re-verification"
      pattern: "re-verif|flip|unblock"
---

<objective>
Turn the elbow-twist branch into a formal HALT loop (codex concern 13 / suggestion 12) so a `< +2.0°` margin CANNOT be resolved inline while 33-07 unblocks. codex: "33-07 must remain blocked rather than treating the question as an inline subtask... route to a defined gap-closure plan that returns through reprocess/backfill/reverification." This plan is that defined route. It is CONDITIONAL: if 33-06's elbow-twist success margin ≥ +2.0° (no HALT), it is a no-op skip; if < +2.0°, it executes the SEED's pre-decided elbow-twist 처방 경로 — the single belle visual-identity question (D-32ⓑ) and the branch (technical trace OR reference re-registration) that returns through the substrate loop and re-verifies before 33-07 may flip. Pod/belle possible — autonomous:false.

Purpose: elbow-twist is the one motion no option fixed (success margin +0.2°→+0.3°). belle's standing instruction was "stop confirming, decide the prescription." The prescription is pre-decided; this plan makes it a blocking gate, not an inline aside.

Output: 33-S4B-ELBOW-TWIST-HALT.md (no-op record OR HALT decision + branch + re-verification proof).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-result-trust-recovery/33-CONTEXT.md
@.planning/debug/ref-student-substrate-gap.md
@.planning/phases/33-result-trust-recovery/33-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: read 33-06 elbow-twist margin — no-op if ≥ +2.0°, else declare HALT</name>
  <read_first>
    - 33-06-SUMMARY.md (the elbow-twist success 여유/margin recorded by the 8-item gate + whether the belle question was flagged)
    - .planning/debug/ref-student-substrate-gap.md `## elbow-twist 처방 경로` (belle 원문; margin ≥ +2.0° → 해소 report-only; < +2.0° → single belle question + pre-decided branch; no second question) + `## 성공 판정 기준` item #1 (margin > 0)
    - 33-CONTEXT.md D-32ⓑ (the ONE conditional belle question wording; no scores/angles/numbers)
  </read_first>
  <action>
    Read the elbow-twist success margin from 33-06-SUMMARY.md. If margin ≥ +2.0°: this plan is a NO-OP — write 33-S4B-ELBOW-TWIST-HALT.md stating "no HALT: 33-06 elbow-twist margin = {value} ≥ +2.0°, branch not triggered, 33-07 proceeds" and complete (do not manufacture a belle question — D-32 2-touchpoint rule). If margin < +2.0°: declare HALT — record that 33-07 is blocked, and proceed to Task 2 (the single belle question). Do NOT raise any threshold to force the margin (D-20/D-29). Record the decision + the 33-06 margin value.
  </action>
  <verify>
    <automated>test -f .planning/phases/33-result-trust-recovery/33-S4B-ELBOW-TWIST-HALT.md && grep -qiE "margin|여유" .planning/phases/33-result-trust-recovery/33-S4B-ELBOW-TWIST-HALT.md && grep -qiE "no HALT|HALT" .planning/phases/33-result-trust-recovery/33-S4B-ELBOW-TWIST-HALT.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The 33-06 elbow-twist margin value is read and recorded; the no-op vs HALT decision is explicit.
    - No-op path: no belle question is raised (D-32 touchpoint discipline); 33-07 is noted as free to proceed.
    - HALT path: 33-07 recorded as blocked; no threshold refit (D-20/D-29).
    - 틀리면 걸리는 장치 (D-18): a margin < +2.0° that tried to proceed inline would violate this gate — the recorded decision is the device.
  </acceptance_criteria>
  <done>The branch decision is made from 33-06's real margin — no-op skip or declared HALT.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: (HALT only) single belle visual-identity question (D-32ⓑ) — skip entirely if Task 1 was no-op</name>
  <action>
    ONLY IF Task 1 declared HALT (margin < +2.0°): ask belle exactly ONE question, the SEED-locked wording — 정은지 정상 영상과 기준 영상을 나란히 놓고, 같은 동작으로 보이십니까? Present ONLY the two videos side by side. Do NOT show scores, angles, or numbers (D-32ⓑ). Do NOT ask a second question. If Task 1 was a no-op, SKIP this task entirely (no belle contact). This is the D-32ⓑ exception, not a design touchpoint expansion.
  </action>
  <what-built>Nothing new — this presents정은지's normal video and the reference video side by side so belle judges visual sameness (not technique).</what-built>
  <how-to-verify>
    Look at the two videos side by side and answer only: 같은 동작으로 보이나요? (같아 / 달라)
  </how-to-verify>
  <verify>
    <automated>echo "checkpoint — HALT-only single belle visual-identity question (skipped if no-op)"</automated>
  </verify>
  <resume-signal>Answer "같아" (technical trace branch) or "달라" (re-register reference), or N/A if no-op.</resume-signal>
  <done>belle answered the single visual-identity question, OR the task was skipped because Task 1 was a no-op.</done>
</task>

<task type="auto">
  <name>Task 3: (HALT only) execute the pre-decided branch through the substrate loop + re-verify before unblocking 33-07</name>
  <read_first>
    - .planning/debug/ref-student-substrate-gap.md `## elbow-twist 처방 경로` step 3 (같아 → technical trace: PR twist-component hypothesis; 달라 → re-register ref-elbow-twist-sister) + Task 1-4 (reprocess/backfill/M3/re-verify order)
    - 33-03/33-04/33-05/33-06 PLAN.md (the substrate loop this returns through) + 33-17 candidate versioning + 33-18 release manifest
    - MEMORY jeongeunji-deliberate-mistake-refs / reference-library-phase4-all11 (re-registration source if 달라)
  </read_first>
  <action>
    ONLY IF HALT: execute the branch belle's answer selected. 같아 → technical trace (the PR twist-component hypothesis: PR's phase-rotation model H=K·R·K⁻¹ may not absorb the twist), and if a fix results, return THROUGH the substrate loop (new candidate version via 33-17 → reprocess 33-03 → backfill 33-04 → re-verify 33-06) — NOT inline. 달라 → re-register ref-elbow-twist-sister from a re-selected 정은지 normal clip, then return through the same substrate loop (new candidate id, backup preimage preserved, re-verify). Either branch produces a NEW candidate version and a re-run of 33-06's 8-item gate; only when re-verification PASSES (elbow-twist margin ≥ +2.0° or the gate otherwise green) is 33-07 unblocked. No threshold refit (D-20/D-29). Record the branch, the new candidate id, and the re-verification result. If Task 1 was a no-op, skip.
  </action>
  <verify>
    <automated>test -f .planning/phases/33-result-trust-recovery/33-S4B-ELBOW-TWIST-HALT.md && grep -qiE "no HALT|re-verif|같아|달라|branch" .planning/phases/33-result-trust-recovery/33-S4B-ELBOW-TWIST-HALT.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - HALT path: the branch returns through the substrate loop (new candidate version, reprocess/backfill/re-verify) — NOT resolved inline (codex concern 13).
    - 33-07 is unblocked ONLY after re-verification passes; the SUMMARY states the new candidate id + re-verify result.
    - No-op path: this task is skipped; the record cites 33-06's ≥ +2.0° margin.
    - No threshold refit (D-20/D-29).
    - 이 산출물이 틀렸다면 어떻게 알았을까: answered — an inline "fix" that unblocked 33-07 without a new candidate + re-verify would leave this record blank; the gate requires the re-verify PASS.
  </acceptance_criteria>
  <done>Either the branch completed with a re-verified new candidate, or the plan was a no-op — 33-07 remains blocked until this resolves.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| 33-06 margin → HALT decision | a below-floor margin must block flip, not proceed inline |
| re-registration / re-trace → new candidate | branch must re-enter the substrate loop, not patch top-level |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-71 | Tampering | elbow-twist resolved inline while 33-07 flips | mitigate | formal HALT gate; 33-07 depends_on this plan (concern 13) |
| T-33-72 | Tampering | margin forced by threshold refit | mitigate | no refit (D-20/D-29); recovery via re-extraction only |
| T-33-73 | Repudiation | second belle question manufactured | mitigate | exactly one D-32ⓑ question; branches pre-decided |
| T-33-SC | Tampering | package installs | accept | reuses existing substrate scripts; no new packages |
</threat_model>

<verification>
- No-op if 33-06 elbow-twist margin ≥ +2.0° (recorded); else HALT with the single D-32ⓑ question + pre-decided branch.
- HALT branch returns through the substrate loop (new candidate, reprocess/backfill/re-verify); 33-07 unblocked only on re-verify PASS.
- No threshold refit (D-20/D-29).
</verification>

<success_criteria>
The elbow-twist branch is a blocking HALT loop, not an inline aside: a `< +2.0°` margin routes through one belle question and a full substrate re-verification before 33-07 can flip, and a `≥ +2.0°` margin is a clean no-op — closing codex concern 13.
</success_criteria>

<output>
Create `.planning/phases/33-result-trust-recovery/33-21-SUMMARY.md` when done, recording the no-op-vs-HALT decision, the 33-06 margin, and (if HALT) the branch + new candidate + re-verification result.
</output>
