---
phase: 33-result-trust-recovery
plan: 22
type: execute
wave: 21
depends_on: []
files_modified:
  - backend/tests/test_deduction_two_track.py
  - backend/shared/python/sunity_shared/analysis/deduction_engine.py
  - backend/shared/python/sunity_shared/models.py
  - app/src/types/analysis.ts
  - docs/contract.md
autonomous: true
requirements: [D-33, D-34, D-35, D-36, D-37, D-38, R1, R2, R3, R4]
user_setup: []
must_haves:
  truths:
    - "A video with execution faults only (Σ|execution| far exceeds 40) scores exactly 60, not 0 — the elbow-twist anchor (8 execution records, Σ points ≈ −111.4) yields tally().final == 60 (INV-7, INV-2, D-33, D-34)"
    - "The critical track is structurally implemented but DORMANT — no active criterion carries split_fail_threshold_deg (D-35); a SYNTHETIC critical record bypasses BOTH the −40 aggregate execution cap AND the −20 per-record clamp and pulls the score below 60 down to the absolute floor 25 (INV-3, INV-8, D-36)"
    - "overallScore is reconstructable from deductionBreakdown alone via final = max(25, 100 − min(40, Σ|execution|) − Σ|critical|) for every record set (INV-6, R4)"
    - "Existing thresholds are byte-unchanged — tol 20°, slope 1.2, PER_RECORD_DEDUCTION_CAP 20, per-criterion ipsf_cap 90, MEAN/P99 epsilons all untouched; only EXECUTION_DEDUCTION_CAP=40, SCORE_FLOOR=25, and the track split are ADDED (D-29 residual, D-35, R3)"
    - "The deductionBreakdown contract is mirrored across all three files (app/src/types/analysis.ts + models.py + docs/contract.md) with additive-optional fields (track, executionRawTotal, executionCappedTotal, criticalTotal, executionCap, scoreFloor); legacy docs without them still parse (D-37, R4)"
    - "deduction_engine stays numpy-pure and deterministic — no boto3/Gemini/network/firestore imports added (R4 constraint)"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/deduction_engine.py"
      provides: "Two-track final: EXECUTION_DEDUCTION_CAP=40 aggregate cap on execution track + critical track bypassing caps + SCORE_FLOOR=25 absolute floor; DeductionRecord.track + DeductionBreakdown aggregate fields"
      contains: "EXECUTION_DEDUCTION_CAP"
    - path: "backend/tests/test_deduction_two_track.py"
      provides: "Synthetic numpy-pure unit tests: INV-3 critical descent, INV-6 reconstructability, INV-7 elbow-twist anchor→60, INV-8 absolute floor 25, INV-2 execution floor 60, no-cap-under-40"
      contains: "def test_"
    - path: "backend/shared/python/sunity_shared/models.py"
      provides: "Additive contract keys: track (record optional) + executionRawTotal/executionCappedTotal/criticalTotal/executionCap/scoreFloor (breakdown optional)"
      contains: "executionCap"
    - path: "app/src/types/analysis.ts"
      provides: "DeductionRecord.track? + DeductionBreakdown aggregate fields (additive optional, lockstep with models.py + contract.md)"
      contains: "executionCappedTotal"
  key_links:
    - from: "deduction_engine.tally"
      to: "two-track final via _two_track_final helper"
      via: "execution records summed → min(40,·) cap; critical records summed separately bypassing caps; final = max(25, 100 − capped_exec − Σcritical)"
      pattern: "EXECUTION_DEDUCTION_CAP|SCORE_FLOOR|_two_track_final"
    - from: "deduction_engine to_dict emission"
      to: "models.py DEDUCTION_* keys + analysis.ts + contract.md §10"
      via: "3-way additive-optional lockstep (D-37)"
      pattern: "track|executionCappedTotal|criticalTotal"
---

<objective>
Wave R scoring redesign (33-SPEC.md, D-33~D-38): replace `deduction_engine.tally`'s unbounded per-joint deduction accumulation — which collapses multi-joint execution faults to 0 (elbow-twist Σ ≈ −111.4 → final 0, while its own angle dimension = 58) — with a two-track IPSF-anchored deduction model. Execution-track deductions (line/angle deviations) are aggregated then capped at −40 (floor 60); a separate critical track (required full-extension not met = element non-recognition) bypasses BOTH the −40 aggregate cap AND the −20 per-record clamp, pulling below the execution floor down to an absolute score floor of 25. Final: `final = max(25, 100 − min(40, Σ|execution|) − Σ|critical|)`. The critical track is STRUCTURALLY implemented but DORMANT (D-35): no criterion carries an active full-extension 0-fail trigger today, so INV-3/INV-8 are verified by SYNTHETIC unit tests only. Emit the additive breakdown shape (D-37) and mirror the contract across all three files atomically.

Purpose: 0 is excessive for a video that reached scoring (it passed the not_pole_motion / no_human gates = the athlete attempted the move). IPSF total-caps execution deductions and separates element non-recognition; going IPSF restores 공감 and stops the 0-point pileup that causes 이탈 ([[scoring-ipsf-deduction-cap-no-zero-pileup]]). The change is a single scoring seam (분기 0, 코드 1벌) so Lambda and RunPod both inherit it.

Output: two-track `deduction_engine.tally` + synthetic unit tests + 3-file contract mirror.

Explicitly OUT of scope (do NOT touch): wiring split 0-fail (per-move `expects_split` + activating `split_fail_threshold_deg` — would re-trigger the resolved kip-up split FP [[split-measurement-doesnt-discriminate-kipup]] and has no clean fixture, D-35 deferred); re-fitting tol 20° / slope 1.2 / per-record −20 / per-criterion 90 / epsilons (D-29 — only the NEW aggregate cap + track + floor are added); gate2 separation floor (discarded); substrate re-extraction / backfill / M3 (33-03/04/05, verified good); flip 33-07 and the 표현 track (33-08~16, blocked); per-fixture target scores (banned as overfit [[scoring-redesign-must-generalize-no-overfit]]).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-result-trust-recovery/33-SPEC.md
@.planning/phases/33-result-trust-recovery/33-CONTEXT.md
@.planning/phases/33-result-trust-recovery/33-SCORING-REDESIGN-HANDOFF.md
@backend/shared/python/sunity_shared/analysis/deduction_engine.py
@backend/shared/python/sunity_shared/analysis/ipsf_criteria.py
@backend/shared/python/sunity_shared/models.py
@app/src/types/analysis.ts
@docs/contract.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — synthetic numpy-pure unit tests for the two-track formula (INV-3/6/7/8 + INV-2 + no-cap-under-40 + elbow-twist anchor)</name>
  <files>backend/tests/test_deduction_two_track.py</files>
  <read_first>
    - .planning/phases/33-result-trust-recovery/33-SPEC.md (Requirements R1/R2/R3; Acceptance Criteria INV-1~INV-8; the locked formula final = max(25, 100 − min(40, Σexec) − Σcritical))
    - .planning/phases/33-result-trust-recovery/33-CONTEXT.md (decisions D-34 formula, D-35 dormant critical, D-36 critical bypasses both caps + floor 25, D-38 verification = unit test for dormant INV-3/INV-8)
    - .planning/phases/33-result-trust-recovery/33-SCORING-REDESIGN-HANDOFF.md (the elbow-twist 8-joint breakdown table: points −18.1/−19.3/−17.2/−8.2/−16.3/−8.1/−12.5/−11.7 = −111.4; the "딴소리 5개" — do NOT fixate on elbow)
    - backend/shared/python/sunity_shared/analysis/deduction_engine.py (DeductionRecord dataclass fields + the final line 349 `final = max(0, round(_BASELINE + sum(r.points for r in records)))` being replaced; the `_two_track_final` helper Task 2 will add)
    - backend/tests/test_deduction_engine.py (existing test conventions for constructing DeductionRecord / calling tally / asserting breakdown)
  </read_first>
  <behavior>
    - INV-7 elbow-twist anchor: 8 execution-track records with points summing to −111.4 (each already ≤ −20 per-record) → the two-track final == 60 exactly (min(40, 111.4) = 40 → 100 − 40).
    - INV-2 execution floor: any set of execution-only records with Σ|points| > 40 → final == 60; Σ|points| == 40 → final == 60; and never below 60 for execution-only.
    - no-cap-under-40: execution-only records with Σ|points| == 15 (below the cap) → final == 85 (unchanged from pre-cap arithmetic; cap not reached).
    - INV-3 critical descent (SYNTHETIC/dormant): one critical-track record with |points| = 90 (bypasses the −20 per-record clamp) and no execution → final pulled below 60; a milder critical (|points| = 50) → final between 25 and 60.
    - INV-8 absolute floor: a critical record with |points| = 90 combined with execution Σ|points| = 40 → 100 − 40 − 90 = −30 → final == 25 (floored); any record set whose raw arithmetic goes below 25 → final == 25.
    - INV-6 reconstructability: for arbitrary mixed records, final == max(25, round(100 − min(40, Σ|execution|) − Σ|critical|)) and the emitted breakdown exposes executionRawTotal / executionCappedTotal / criticalTotal / executionCap(40) / scoreFloor(25) so the number is reconstructable from the dict alone.
    - Elite/near-zero guard: records with Σ|points| ≈ 0 → final ≈ 100 (the cap must not move near-zero-deduction scores — INV-1 structural anchor).
  </behavior>
  <action>
    Create backend/tests/test_deduction_two_track.py. Construct synthetic DeductionRecord instances (or invoke the `_two_track_final` pure helper Task 2 will add) tagged with `track='execution'` / `track='critical'` and assert the behaviors above. Use the elbow-twist points from the HANDOFF table verbatim for the INV-7 anchor (do NOT reconstruct them from deviation math — anchor on the recorded values). Do NOT construct per-fixture target scores or import any Pod/GPU/fixture data — these are pure numpy in-memory records ([[scoring-redesign-must-generalize-no-overfit]], [[judgment-must-not-fixate-on-recent-fixture]]). Assert the module imports remain numpy-only (no boto3/Gemini/network). All tests RED until Task 2. Name constants EXECUTION_DEDUCTION_CAP and SCORE_FLOOR exactly (import them from deduction_engine so the test fails cleanly with ImportError until Task 2 defines them).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_deduction_two_track.py -q; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - backend/tests/test_deduction_two_track.py exists and is RED for the right reason (references EXECUTION_DEDUCTION_CAP / SCORE_FLOOR / the two-track final that Task 2 has not yet implemented — not a no-op pass).
    - Test file contains an explicit INV-7 case asserting `final == 60` for the elbow-twist Σ = −111.4 execution records, an INV-8 case asserting `final == 25`, and an INV-6 reconstruction assertion.
    - 틀리면 걸리는 장치 (D-18): the synthetic INV-3/INV-8 tests are the ONLY verification of the dormant critical track (the 6 fixtures never trigger it, D-35) — if they are absent or trivially green, the critical-track structure is unverified.
  </acceptance_criteria>
  <done>Two-track invariants encoded as RED synthetic unit tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: implement the two-track cap + absolute floor + track field in deduction_engine.tally</name>
  <files>backend/shared/python/sunity_shared/analysis/deduction_engine.py</files>
  <read_first>
    - .planning/phases/33-result-trust-recovery/33-SPEC.md (Requirements R1/R2/R3; the locked formula; Boundaries "In scope" / "Out of scope")
    - .planning/phases/33-result-trust-recovery/33-CONTEXT.md (D-34 formula, D-35 dormant critical = structure only + active criterion 0, D-36 critical bypasses −40 aggregate AND −20 per-record + floor 25, D-29 existing thresholds unchanged)
    - backend/shared/python/sunity_shared/analysis/deduction_engine.py (tally final line ~349; DeductionRecord ~46-91 incl. to_dict; DeductionBreakdown ~94-119 incl. to_dict; _criterion_deduction ~368-411 where the split_fail_threshold_deg 0-fail branch classifies critical; PER_RECORD_DEDUCTION_CAP=20 at line 43 — UNCHANGED)
    - backend/shared/python/sunity_shared/analysis/ipsf_criteria.py:32-47,96-97 (split_fail_threshold_deg is unwired/dormant — the 0-fail branch is the ONLY critical source and it never fires today; _ANGLE_CAP=90 / _SLOPE=1.2 UNCHANGED)
    - backend/tests/test_deduction_two_track.py (Task 1 RED tests to turn green)
    - backend/tests/test_deduction_engine.py (existing tally tests that must stay green — no regression on execution-only-under-40 cases)
  </read_first>
  <action>
    Add two named project-level constants near PER_RECORD_DEDUCTION_CAP: `EXECUTION_DEDUCTION_CAP = 40.0` with an IPSF-provenance comment (IPSF execution-deduction total-cap −25 on the ~60-pt IPSF execution scale ≈ 42% of range → −40 on our 0–100; single project-level constant, NOT re-fit per fixture or per criterion — cite [[scoring-redesign-must-generalize-no-overfit]]) and `SCORE_FLOOR = 25.0` (belle: a scored video passed not_pole_motion/no_human = the move was attempted, 0 is unjust; fully-unrelated video is rejected before scoring). Add a `track: str` field to DeductionRecord (default `'execution'` so all existing construction sites stay byte-compatible) and emit it in to_dict via the additive-optional pattern (only include the key on emit — mirror the rawPoints/capApplied approach; legacy docs lack it). Classify a record `track='critical'` ONLY on the existing full-extension 0-fail path in `_criterion_deduction` (the `fail_thr is not None and measured_value < fail_thr` big_over branch) — which is DORMANT (no criterion carries split_fail_threshold_deg today, D-35); every real record today is `'execution'`. Do NOT wire split 0-fail (D-35 deferred). Extract a pure `_two_track_final(records)` helper: sum execution `points` → `execution_raw_total`; `execution_capped_total = -min(EXECUTION_DEDUCTION_CAP, abs(execution_raw_total))`; sum critical `points` (bypass the −40 cap AND keep their ipsf_cap-90 magnitude — critical records are NOT −20 clamped when built on the 0-fail path) → `critical_total`; `final = max(SCORE_FLOOR, round(_BASELINE + execution_capped_total + critical_total))`. Replace the line-349 final computation with this helper. Add aggregate fields to DeductionBreakdown (`execution_raw_total`, `execution_capped_total`, `critical_total`, `execution_cap`, `score_floor`) and emit them in to_dict as additive-optional camelCase (`executionRawTotal`/`executionCappedTotal`/`criticalTotal`/`executionCap`/`scoreFloor`). Apply the absolute floor to the `dimension_overall_fallback` early-return path too (deduction_engine.py:285): change its final from `max(0, round(dim))` to `max(SCORE_FLOOR, round(dim))` so INV-8 (no scored video below 25) holds on EVERY scored path, not only the two-track tally — D-36's rationale (a video that passes not_pole_motion/no_human was attempted, so sub-25 is unjust) is path-independent. This shifts the contract.md §10.5 fallback invariant from `final == dimension_overall` to `final == max(SCORE_FLOOR, round(dimension_overall))`; Task 3 updates §10.5 to match (3-way lockstep). Add a one-line comment on the fallback final citing INV-8 + D-36. Do NOT leave this to executor discretion — the floor is unconditional. Keep the engine numpy-pure. Do NOT change tol / slope / PER_RECORD_DEDUCTION_CAP / ipsf_cap / epsilons (D-29). Run the full deduction test suite.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_deduction_two_track.py tests/test_deduction_engine.py tests/test_pipeline_deduction_seam.py tests/test_deduction_seed_pointed_merge.py -q && python -c "import ast,sys; src=open('shared/python/sunity_shared/analysis/deduction_engine.py').read(); mods={n.module.split('.')[0] for n in ast.walk(ast.parse(src)) if isinstance(n,ast.ImportFrom) and n.level==0 and n.module}|{a.name.split('.')[0] for n in ast.walk(ast.parse(src)) if isinstance(n,ast.Import) for a in n.names}; bad={'boto3','requests','google','firebase','firestore','cerebras','urllib'}&mods; print('numpy-pure' if not bad else 'IMPURE:'+str(bad)); sys.exit(1 if bad else 0)"; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - deduction_engine.py contains `EXECUTION_DEDUCTION_CAP = 40.0` (with IPSF-provenance comment) and `SCORE_FLOOR = 25.0`; `_two_track_final` exists and is the single place `final` is computed for the two-track path.
    - Task 1 tests all GREEN; test_deduction_engine.py + test_pipeline_deduction_seam.py + test_deduction_seed_pointed_merge.py stay GREEN (no regression on execution-only-under-40 or existing seam behavior).
    - elbow-twist synthetic execution records Σ = −111.4 → `tally().final == 60`; a synthetic critical record |90| + exec 40 → `final == 25`.
    - grep confirms tol 20 / slope 1.2 / PER_RECORD_DEDUCTION_CAP 20 / ipsf_cap 90 are byte-unchanged (D-29) — `git diff` shows only additive lines in ipsf_criteria.py (ideally zero) and the cap/floor/track additions in deduction_engine.py.
    - numpy-purity gate passes (no boto3/Gemini/network/firestore import added).
    - the `dimension_overall_fallback` early-return path uses `max(SCORE_FLOOR, round(dim))` (not `max(0, …)`) so a synthetic fallback input with `dimension_overall < 25` yields `final == 25` (INV-8 holds on the fallback path; §10.5 updated in Task 3).
    - 무엇을 열어서 확인했는가 (D-19): the SUMMARY prints the actual `tally().to_dict()` for the elbow-twist synthetic input showing final=60 + executionRawTotal≈−111.4 + executionCappedTotal=−40 + criticalTotal=0, and a dormant-critical synthetic showing final=25.
  </acceptance_criteria>
  <done>Two-track cap + absolute floor + track classification implemented; execution-only floors at 60, synthetic critical descends to 25, existing thresholds untouched, engine numpy-pure.</done>
</task>

<task type="auto">
  <name>Task 3: mirror the additive deductionBreakdown contract across all three files (analysis.ts + models.py + contract.md)</name>
  <files>backend/shared/python/sunity_shared/models.py, app/src/types/analysis.ts, docs/contract.md</files>
  <read_first>
    - .planning/phases/33-result-trust-recovery/33-CONTEXT.md (D-37 — additive shape, 계약 3파일 동시 갱신, rawPoints/capApplied additive pattern 계승, INV-6 reconstructability)
    - backend/shared/python/sunity_shared/models.py:166-181 (DEDUCTION_RECORD_KEYS, DEDUCTION_RECORD_OPTIONAL_KEYS = ('rawPoints','capApplied'), DEDUCTION_BREAKDOWN_KEYS — the exact tuples to extend)
    - app/src/types/analysis.ts:652-711 (DeductionRecord interface incl. the rawPoints?/capApplied? optional block + the anti-word-colon comment warning; DeductionBreakdown interface baseline/records/final/coverageGaps)
    - docs/contract.md:1649-1700 (§10 DeductionBreakdown + §10.2 DeductionRecord table + the 3-way lockstep note)
    - backend/shared/python/sunity_shared/analysis/deduction_engine.py (the to_dict emission from Task 2 — the source of truth for the new keys)
    - backend/tests/test_deduction_engine.py (test_contract_lockstep, if present — the regex field-extraction guard that cross-checks analysis.ts vs models.py; keep it green)
  </read_first>
  <action>
    Update all three contract mirrors together (D-37), following the existing additive-optional pattern exactly. (1) models.py: add `'track'` to DEDUCTION_RECORD_OPTIONAL_KEYS (present only on new emits, legacy docs lack it), and add a new tuple `DEDUCTION_BREAKDOWN_OPTIONAL_KEYS = ('executionRawTotal','executionCappedTotal','criticalTotal','executionCap','scoreFloor')` (or extend the documented breakdown-optional set) with a comment citing 33-SPEC.md INV-6 + D-37; leave DEDUCTION_RECORD_KEYS (required 11) and DEDUCTION_BREAKDOWN_KEYS (required 5) untouched so legacy docs stay valid. (2) analysis.ts: add `track?: 'execution' | 'critical';` to DeductionRecord and `executionRawTotal?`, `executionCappedTotal?`, `criticalTotal?`, `executionCap?`, `scoreFloor?` (all `number`) to DeductionBreakdown, matching the emitted camelCase — respect the in-file warning that interface comment blocks must not contain word-colon or brace patterns (they break the lockstep regex). (3) contract.md §10 / §10.2: document the two-track final `max(25, 100 − min(40, Σexecution) − Σcritical)`, the `track` record field, and the five aggregate breakdown fields as additive-optional (legacy-compat, no migration), and update the §10.1 `final` description from `max(0, round(100 + Σ points))` to the two-track formula. Also update §10.5: the fallback-path invariant becomes `final == max(SCORE_FLOOR, round(dimension_overall))` (was `final == dimension_overall`) so the absolute floor 25 (INV-8, D-36) is honored on the fallback passthrough too — keep this lockstep with the Task 2 fallback-floor change. Keep all three semantically identical (3-way lockstep). Do NOT change the required-key sets or any existing field.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_deduction_engine.py tests/test_deduction_two_track.py -q && cd ../app && npm run typecheck; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - models.py: `track` in DEDUCTION_RECORD_OPTIONAL_KEYS; the five aggregate keys present in a breakdown-optional tuple; required key sets (DEDUCTION_RECORD_KEYS 11, DEDUCTION_BREAKDOWN_KEYS 5) unchanged.
    - analysis.ts: DeductionRecord has `track?: 'execution' | 'critical'`; DeductionBreakdown has the five `number` aggregate optionals; `npm run typecheck` (tsc --noEmit) passes with no new errors (app runtime code needs no change — the fields are additive optional; UI presentation of cap/track is deferred to the blocked 표현 track per D-05/D-16, NOT added here).
    - contract.md §10.1 `final` reads the two-track formula; §10.2 documents `track`; §10 documents the five aggregate fields as additive-optional legacy-compat.
    - test_contract_lockstep (if present) stays green — analysis.ts and models.py agree on the new field names.
    - 틀리면 걸리는 장치 (D-18): a doc emitted by Task 2's engine round-trips through the models.py validator without rejection AND typechecks against analysis.ts — divergence between the three files is caught by the lockstep test + typecheck, not left silent.
    - 무엇을 열어서 확인했는가 (D-19): the SUMMARY shows the diff of all three files side-by-side and the passing lockstep test + typecheck output.
  </acceptance_criteria>
  <done>deductionBreakdown contract mirrored across analysis.ts + models.py + contract.md with additive-optional two-track fields; required keys unchanged; typecheck + lockstep green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| deduction_engine.tally (single scoring seam) → Lambda + RunPod _process | one formula feeds every scored analysis; a wrong cap/floor mis-scores all videos |
| engine to_dict emission → Firestore write (firestore_admin validator) | new keys not mirrored in models.py would be rejected at write time (Pod re-verify) |
| new aggregate cap → existing execution-fault discrimination | an over-aggressive cap flattens distinct videos to the same floor (INV-4 risk) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-40 | Tampering | cap re-fit per fixture (overfit) | mitigate | EXECUTION_DEDUCTION_CAP is a single IPSF-provenance constant; tests use synthetic records not fixture targets ([[scoring-redesign-must-generalize-no-overfit]]) |
| T-33-41 | Tampering | critical track accidentally activated (kip-up split FP) | mitigate | split 0-fail wiring explicitly NOT done (D-35); critical path stays dormant; verified by grep that no criterion gains split_fail_threshold_deg |
| T-33-42 | Tampering | contract divergence across the 3 mirror files | mitigate | Task 3 lockstep test + npm typecheck; additive-optional keeps legacy docs valid |
| T-33-43 | Repudiation | existing thresholds silently re-fit under cover of the redesign | mitigate | git diff shows only additive lines; tol/slope/caps/epsilons byte-unchanged (D-29) |
| T-33-44 | Information disclosure | engine gains a network/Gemini import | mitigate | numpy-purity AST gate in Task 2 verify |
| T-33-SC | Tampering | package installs | accept | no installs — numpy only |
</threat_model>

<verification>
- Two-track final implemented in the single scoring seam: execution capped at −40 (floor 60), critical bypasses both caps to absolute floor 25 (INV-2, INV-3, INV-7, INV-8 by synthetic unit test).
- Reconstructability holds: final == max(25, 100 − min(40, Σexec) − Σcritical) from the emitted breakdown (INV-6).
- Existing thresholds byte-unchanged (D-29); engine numpy-pure (AST gate).
- Contract mirrored across analysis.ts + models.py + contract.md; typecheck + lockstep green; legacy docs still parse (D-37).
- Critical track DORMANT — no criterion wired to split 0-fail (D-35); grep confirms no split_fail_threshold_deg added to any criterion.
</verification>

<success_criteria>
The scoring seam no longer collapses multi-joint execution faults to 0 — an execution-only video (elbow-twist Σ ≈ −111.4) scores exactly 60, a near-perfect video stays ≥ 95, and a SYNTHETIC critical fault descends below 60 to the absolute floor 25 — all reconstructable from the emitted deductionBreakdown, with existing thresholds untouched, the critical track structurally present but dormant, and the contract mirrored across all three files. INV-1/2/4/5 across the real 6 fixtures are deferred to 33-23 (Pod re-verification).
</success_criteria>

<output>
Create `.planning/phases/33-result-trust-recovery/33-22-SUMMARY.md` when done, showing: the elbow-twist synthetic `tally().to_dict()` (final=60 + executionRawTotal/executionCappedTotal/criticalTotal), a dormant-critical synthetic (final=25), the git diff proving tol/slope/caps/epsilons byte-unchanged, and the 3-file contract diff + passing typecheck/lockstep.
</output>
