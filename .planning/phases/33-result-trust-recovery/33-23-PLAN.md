---
phase: 33-result-trust-recovery
plan: 23
type: execute
wave: 22
depends_on: ["33-22"]
files_modified:
  - .planning/phases/33-result-trust-recovery/33-SCORING-REVERIFY.md
autonomous: false
requirements: [D-38, R5]
user_setup: []
must_haves:
  truths:
    - "All 6 fixtures are re-analyzed SERIALLY (concurrent /analyze contaminates [[pipeline-not-concurrency-safe-eval-serial]]) against the NEW candidate substrate (phase33-cm3-run1, shadow — NOT the active pointer; flip 33-07 stays blocked) with the two-track engine from 33-22"
    - "Judgment is against the 8 STRUCTURAL invariants ONLY — never per-fixture target scores ([[judgment-must-not-fixate-on-recent-fixture]]): INV-1 elite ≥95, INV-2 execution floor ≥60, INV-4 discrimination preserved, INV-5 monotone deduction, INV-6 reconstructability across all 6 fixtures (INV-3/INV-7/INV-8 already proven by 33-22 synthetic unit tests; the dormant critical track is not triggered by any fixture, D-35)"
    - "INV-4 is EXPLICITLY checked above the cap: the −40 cap flattens execution-fault videos at floor 60 (baseline power-spin 57→60, kip-up 47→60 both converge to 60), so re-verification confirms discrimination between execution-fault videos is preserved ABOVE the floor, not lost to cap-flattening (D-38 caution)"
    - "The Pod is a fresh EU-RO-1 4090 Network Storage Pod created only after belle GPU greenlight (all Pods terminated, D-30), bootstrapped per the standing procedure, torn down / reported after the serial sweep"
    - "The re-verification records whether all applicable structural invariants hold simultaneously; a PASS unblocks flip (33-07) and the 표현 track, a FAIL is reported to belle with the failing invariant (no silent pass)"
  artifacts:
    - path: ".planning/phases/33-result-trust-recovery/33-SCORING-REVERIFY.md"
      provides: "Per-fixture two-track breakdown dump + structural-invariant PASS/FAIL table (INV-1/2/4/5/6) + explicit INV-4 above-cap discrimination check, no per-fixture target scores"
      contains: "INV-4"
  key_links:
    - from: "33-22 two-track engine (candidate substrate)"
      to: "serial /analyze of 6 fixtures on fresh Pod"
      via: "shadow re-analysis (candidate reference, active pointer unchanged)"
      pattern: "serial|shadow|candidate"
    - from: "6-fixture breakdowns"
      to: "structural invariants INV-1/2/4/5/6"
      via: "PASS/FAIL table with explicit above-cap discrimination"
      pattern: "INV-1|INV-4|discrimination"
---

<objective>
Wave R re-verification (33-SPEC.md R5, D-38): re-analyze all 6 fixtures with the two-track engine (33-22) and judge them against the 8 structural invariants — never per-fixture target scores. INV-3 (critical descent), INV-7 (elbow-twist anchor → 60), and INV-8 (absolute floor 25) are already proven by 33-22's synthetic unit tests (the critical track is DORMANT — no fixture triggers it, D-35), so this plan verifies INV-1 (elite ≥95), INV-2 (execution floor ≥60, never below), INV-4 (discrimination preserved), INV-5 (monotone deduction), and INV-6 (reconstructability) across the real 6 fixtures. The re-analysis runs against the NEW candidate substrate (phase33-cm3-run1, produced by the verified-good 33-03/04/05) in SHADOW mode — the active reference pointer is NOT flipped (flip 33-07 stays blocked on this verification). Re-analysis is SERIAL per fixture (concurrent /analyze contaminates) on a fresh EU-RO-1 4090 Network Storage Pod created after belle GPU greenlight (all Pods terminated, D-30).

Purpose: R5 generalization gate — the redesign must hold across all fixtures simultaneously without curve-fitting. The critical caution (D-38): the −40 cap flattens execution-fault videos at floor 60 (baseline power-spin 57→60, kip-up 47→60 both converge), so this plan must EXPLICITLY confirm discrimination is preserved ABOVE the cap and not lost to flattening. A PASS unblocks flip (33-07) and the 표현 track; a FAIL is reported to belle with the failing invariant.

Output: 33-SCORING-REVERIFY.md — per-fixture two-track breakdown dump + structural-invariant PASS/FAIL table.

Explicitly OUT of scope: flipping the active reference pointer (33-07, blocked); per-fixture target scores / expected-range calibration (banned as overfit); re-running substrate (33-03/04/05, verified good); any engine/threshold change (that is 33-22 — this plan only measures).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-result-trust-recovery/33-SPEC.md
@.planning/phases/33-result-trust-recovery/33-CONTEXT.md
@.planning/phases/33-result-trust-recovery/33-SCORING-REDESIGN-HANDOFF.md
@.planning/phases/33-result-trust-recovery/33-22-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: belle GPU greenlight + fresh Pod spec (all Pods terminated, D-30)</name>
  <files>(no code files — operational gate; Claude sets up the Pod after greenlight)</files>
  <read_first>
    - .planning/phases/33-result-trust-recovery/33-CONTEXT.md (D-30 Pod procedure, D-25 all Pods terminated, D-32 belle 접점 예외 = Pod greenlight is an operational action, not a design question)
    - .planning/phases/33-result-trust-recovery/33-SCORING-REDESIGN-HANDOFF.md (Pod status: k508k3lut0o3f1 terminated; Network Storage Volume/MooseFS survives with code + weights + the s4 re-design data at /workspace)
  </read_first>
  <decision>Greenlight a fresh GPU Pod for the 6-fixture two-track re-verification, and confirm the GPU model.</decision>
  <context>
    All Pods are terminated (D-25/D-30) to save cost; the Network Storage Volume (MooseFS) with code + weights survives. Re-verification needs GPU (NLF/RTMW diverge/NaN on CPU). Per [[pod-request-include-gpu-model]] the proposed model is EU-RO-1 4090 (priority order: 4090 → PRO 4500 → 5090 → PRO 6000 → A4500). After greenlight, Claude creates the Network Storage Pod, bootstraps (bootstrap_full → start_server → VETO env → X-RunPod-Token /health canary → SSM + Lambda re-sync per the standing Pod-ops procedure), runs the serial sweep, and reports. This is an operational gate, not a design question (D-32 exception ⓐ).
  </context>
  <options>
    <option id="greenlight-4090">
      <name>Greenlight EU-RO-1 4090 Network Storage Pod</name>
      <pros>Highest priority / lowest cost per [[runpod-eu-ro1-gpu-priority]]; reuses surviving volume; standing bootstrap procedure applies verbatim</pros>
      <cons>4090 availability varies — may need fallback to next priority</cons>
    </option>
    <option id="greenlight-other">
      <name>Greenlight a different GPU model</name>
      <pros>belle may prefer a specific model or region</pros>
      <cons>May diverge from the cost-optimal priority order</cons>
    </option>
    <option id="hold">
      <name>Hold — do not spin up a Pod yet</name>
      <pros>Zero GPU spend; 33-22 engine + unit tests already prove INV-3/6/7/8 in isolation</pros>
      <cons>INV-1/2/4/5 across the real 6 fixtures stay unverified; flip 33-07 + 표현 track stay blocked</cons>
    </option>
  </options>
  <action>PAUSE for belle. Present the proposed EU-RO-1 4090 Network Storage Pod (per [[pod-request-include-gpu-model]] / [[runpod-eu-ro1-gpu-priority]]) and wait for greenlight. This is an operational gate (D-32 exception ⓐ), not a design question — do NOT re-ask any locked design decision. On greenlight, Claude proceeds to Task 2 (create Pod, bootstrap, re-sync SSM+Lambda, run the serial sweep). On "hold", stop the wave (33-22 already proved INV-3/6/7/8 in isolation).</action>
  <verify><human-check>belle has selected greenlight-4090, greenlight-other (with a named model), or hold.</human-check></verify>
  <resume-signal>Select: greenlight-4090, greenlight-other (name the model), or hold</resume-signal>
  <done>belle greenlight recorded (GPU model confirmed) or wave held.</done>
</task>

<task type="auto">
  <name>Task 2: serial 6-fixture shadow re-analysis + structural-invariant PASS/FAIL (explicit INV-4 above-cap check)</name>
  <files>.planning/phases/33-result-trust-recovery/33-SCORING-REVERIFY.md</files>
  <read_first>
    - .planning/phases/33-result-trust-recovery/33-SPEC.md (Acceptance Criteria INV-1~INV-8; "structural invariants only, no per-fixture target scores")
    - .planning/phases/33-result-trust-recovery/33-CONTEXT.md (D-38 verification protocol + INV-4 caution: cap flattens power-spin 57→60 / kip-up 47→60; D-23 all 6 fixtures same frequency; D-30 serial eval; D-19 open the artifact)
    - .planning/phases/33-result-trust-recovery/33-SCORING-REDESIGN-HANDOFF.md (Pod bootstrap procedure reference; candidate = phase33-cm3-run1 shadow; /workspace survivors)
    - .planning/phases/33-result-trust-recovery/33-22-SUMMARY.md (the two-track engine behavior + the synthetic anchors already proven, so the fixture sweep only needs INV-1/2/4/5/6)
    - Memory: [[current-pod-k508k3lut0o3f1]] (bootstrap_full → start_server → /health canary; access info dead, procedure valid), [[pipeline-not-concurrency-safe-eval-serial]], [[verify-all-fixtures-not-kipup-only]]
  </read_first>
  <action>
    After greenlight (Task 1), create the Network Storage Pod on the confirmed GPU, bootstrap it (bootstrap_full → start_server → VETO env → X-RunPod-Token /health canary), and re-sync SSM + Lambda env (RUNPOD_ANALYZE_URL / proxy URL) per the standing Pod-ops procedure. Deploy the 33-22 two-track engine to the Pod code path (numpy-pure engine change; single seam _process). Run /analyze SERIALLY (one fixture at a time — never concurrent, [[pipeline-not-concurrency-safe-eval-serial]]) for ALL 6 fixtures at equal frequency (D-23 — do NOT sweep kip-up only, [[verify-all-fixtures-not-kipup-only]]) against the NEW candidate substrate (phase33-cm3-run1) in SHADOW mode — do NOT flip the active reference pointer (33-07 blocked). For each fixture, dump the full deductionBreakdown (final + executionRawTotal + executionCappedTotal + criticalTotal + records with track). Then judge against the 5 fixture-applicable STRUCTURAL invariants ONLY (no per-fixture target scores, [[judgment-must-not-fixate-on-recent-fixture]]): INV-1 elite/well-executed ≥95, INV-2 execution-fault-only ≥60 and never below 60, INV-4 discrimination preserved, INV-5 monotone deduction (larger measured deviation never scores higher), INV-6 reconstructability (final == max(25, 100 − min(40, Σexec) − Σcritical) from the dumped breakdown). For INV-4, run an EXPLICIT above-cap discrimination check: identify which fixtures the −40 cap flattens to 60, and confirm that videos NOT at the cap retain a monotone ordering consistent with eyeball severity — if two genuinely different-severity execution-fault videos both flatten to exactly 60, note it as an INV-4 caution/finding (the cap trades some discrimination for the floor; report whether the residual discrimination above the cap is acceptable, do NOT curve-fit a per-fixture floor to restore it — gate2 was discarded). Write 33-SCORING-REVERIFY.md with the per-fixture breakdown dump + the PASS/FAIL table + the INV-4 finding. Report wave quality to belle before proceeding ([[report-wave-quality-before-next-wave]]). Tear down or hand the Pod back per belle preference after the sweep. If any invariant FAILS, STOP and report the failing invariant to belle with the breakdown evidence (no silent pass, D-18); do NOT flip 33-07.
  </action>
  <verify>
    <human-check>33-SCORING-REVERIFY.md exists with all 6 fixtures dumped at equal frequency, a PASS/FAIL row per fixture for INV-1/2/4/5/6, and the explicit INV-4 above-cap discrimination finding. For every fixture, final is arithmetically reconstructed from the dumped breakdown (INV-6). No per-fixture target score appears as a pass criterion.</human-check>
  </verify>
  <acceptance_criteria>
    - All 6 fixtures re-analyzed SERIALLY against the candidate substrate in shadow (active pointer unchanged — grep/confirm no flip); equal frequency (D-23).
    - Structural invariants INV-1/2/4/5/6 evaluated per fixture; the report states whether ALL hold simultaneously (R5). INV-3/7/8 are cited as already proven by 33-22 synthetic tests (dormant critical, D-35).
    - INV-4 above-cap discrimination is EXPLICITLY addressed: the report names which fixtures flatten to 60 and whether discrimination is preserved above the floor (D-38 caution) — no per-fixture floor curve-fit ([[judgment-must-not-fixate-on-recent-fixture]]).
    - 틀리면 걸리는 장치 (D-18): the 6-fixture serial dump is the catch — a silent regression (rank inversion, elite below 95, execution below 60) surfaces as a FAIL row; a FAIL stops the wave and is reported to belle, flip 33-07 not performed.
    - 무엇을 열어서 확인했는가 (D-19): 33-SCORING-REVERIFY.md shows the actual dumped breakdown values (opened artifact), not "the sweep passed".
  </acceptance_criteria>
  <done>All 6 fixtures re-verified against structural invariants only; INV-4 above-cap discrimination explicitly confirmed; PASS unblocks flip 33-07 + 표현 track, FAIL reported to belle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| fresh Pod → SSM/Lambda env re-sync | a stale RUNPOD_ANALYZE_URL routes analysis to a dead/wrong Pod |
| candidate shadow substrate → active reference pointer | accidentally flipping the pointer ships unverified scoring to production |
| serial eval → concurrent contamination | concurrent /analyze calls corrupt breakdowns ([[pipeline-not-concurrency-safe-eval-serial]]) |
| −40 cap → INV-4 discrimination | cap flattening masks real severity differences (power-spin/kip-up both → 60) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-45 | Tampering | active reference pointer flipped prematurely | mitigate | shadow-only re-analysis; active pointer unchanged; flip 33-07 explicitly blocked until this PASSes |
| T-33-46 | Tampering | concurrent eval contaminates breakdowns | mitigate | strictly serial /analyze, one fixture at a time (D-30) |
| T-33-47 | Repudiation | a fixture regression silently passed | mitigate | per-fixture PASS/FAIL table; FAIL stops the wave + reports to belle (D-18) |
| T-33-48 | Tampering | per-fixture floor curve-fit to restore INV-4 | mitigate | gate2 discarded; INV-4 judged as a finding, no per-fixture floor added ([[judgment-must-not-fixate-on-recent-fixture]]) |
| T-33-49 | Denial of service | Pod left running (cost) / stale env | accept | tear down or hand back per belle after sweep; re-sync SSM+Lambda on teardown |
| T-33-SC | Tampering | Pod bootstrap package installs | accept | standing bootstrap procedure; no new packages beyond the pinned Pod image |
</threat_model>

<verification>
- 6 fixtures re-analyzed serially against the candidate substrate in shadow; active pointer unchanged.
- Structural invariants INV-1/2/4/5/6 hold simultaneously (R5); INV-3/7/8 already proven by 33-22 synthetic tests.
- INV-4 above-cap discrimination explicitly confirmed (D-38 caution); no per-fixture curve-fit.
- Result reported to belle; PASS unblocks flip 33-07 + 표현 track, FAIL halts with the failing invariant.
</verification>

<success_criteria>
All 6 fixtures, re-analyzed serially on a fresh belle-greenlit Pod against the new candidate substrate in shadow, satisfy the structural invariants simultaneously (elite ≥95, execution ≥60, monotone, discrimination preserved above the cap, reconstructable) — judged with no per-fixture target scores — clearing the R5 generalization gate that unblocks flip 33-07 and the 표현 track.
</success_criteria>

<output>
Create `.planning/phases/33-result-trust-recovery/33-23-SUMMARY.md` when done, and the verification artifact `33-SCORING-REVERIFY.md` (per-fixture two-track breakdown dump + INV-1/2/4/5/6 PASS/FAIL table + explicit INV-4 above-cap discrimination finding). State clearly whether the redesign PASSES (unblocks 33-07) or FAILS (halts, reported to belle).
</output>
