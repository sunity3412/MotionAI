---
phase: 38-supplier-link
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/shared/python/sunity_shared/s3keys.py
  - backend/tests/test_s3keys.py
  - backend/shared/python/sunity_shared/models.py
  - backend/tests/test_registration_contract.py
  - app/src/types/analysis.ts
  - docs/contract.md
  - docs/reference-motions.md
  - backend/shared/python/sunity_shared/validation.py
  - backend/tests/test_validation.py
autonomous: true
requirements: [REQ-38-1, REQ-38-3, REQ-38-6, REQ-38-7]
must_haves:
  truths:
    - "D-04/D-19: `reference/{uid}/{refId}.{ext}` 키가 build→parse 라운드트립되고, 기존 평면 `reference/ref-*.mp4` 와 `reference/_archive/...` 는 parse_reference_key 가 None 을 돌려 새 경로에 절대 들어오지 않는다"
    - "D-05/D-09: 등록 상태·실패 코드가 AnalysisStatus/PIPELINE_SEQUENCE 와 **별개 enum** 으로 models.py · analysis.ts · contract.md 세 벌에 같은 문자열로 존재한다"
    - "D-07/D-08/D-14: 폼 메타(name·athleteName·level·선언 4)·동의(필수 3 = true, training 기본 false)·길이(5~30초, 콤보 60초)를 순수 함수 하나가 검증하고 서 있는 시작 아니오는 거부한다"
    - "D-17: s3keys.py 헤더에 '30일' 문장이 없고 영구 보관이 적혀 있다"
    - "D-12: SUPPLIER_UIDS 파서가 `uid[:CODE]` 항목을 dict 로 돌려주고 잘못된 코드는 uid 만 남긴다"
    - "D-10: 자기 재현성 분석 doc 표식 `selfCheckForReference` 가 analysis.ts `AnalysisDoc` · models.py 상수 `ANALYSIS_FIELD_SELF_CHECK_FOR_REFERENCE` · contract.md §3 세 벌에 같은 문자열로 존재한다 — 38-07 writer · 38-08 reader 는 그 상수만 쓴다(리터럴 금지)"
  artifacts:
    - path: "backend/shared/python/sunity_shared/s3keys.py"
      provides: "build_reference_upload_key · ParsedReferenceKey · parse_reference_key"
      contains: "parse_reference_key"
    - path: "backend/shared/python/sunity_shared/models.py"
      provides: "REGISTRATION_STATUS_* · REGISTRATION_ERROR_MESSAGE · REFERENCE_LEVELS · parse_supplier_uids · ANALYSIS_FIELD_SELF_CHECK_FOR_REFERENCE"
      contains: "REGISTRATION_ERROR_MESSAGE"
    - path: "app/src/types/analysis.ts"
      provides: "ReferenceRegistrationStatus · ReferenceRegistrationErrorCode · ReferenceUploadUrlRequest/Response · SupplierProbeResponse · AnalysisDoc.selfCheckForReference"
      contains: "ReferenceRegistrationStatus"
    - path: "backend/shared/python/sunity_shared/validation.py"
      provides: "ReferenceUploadRequest · validate_reference_upload_request"
      contains: "validate_reference_upload_request"
    - path: "docs/contract.md"
      provides: "POST /reference/upload-url 절 + ReferenceMotion 등록 필드 + REGISTRATION_ERROR_MESSAGE"
      contains: "POST /reference/upload-url"
  key_links:
    - from: "backend/tests/test_registration_contract.py"
      to: "app/src/types/analysis.ts"
      via: "파일 텍스트 대조 — REGISTRATION_ERROR_MESSAGE 문자열이 TS 에 verbatim 존재"
      pattern: "analysis.ts"
    - from: "backend/shared/python/sunity_shared/validation.py"
      to: "backend/shared/python/sunity_shared/models.py"
      via: "REFERENCE_LEVELS · REGISTRATION_ERROR_MESSAGE · REFERENCE_*_DURATION_SEC import"
      pattern: "models\\.REFERENCE_"
---

<objective>
Phase 38 의 **계약과 순수 함수**를 먼저 박는다 — `reference/` 업로드 키(D-04·D-19), 등록 상태·실패 코드 enum(D-05·D-09, 계약 3벌 lockstep), 공급자 폼 검증(D-07·D-08·D-14·REQ-38-6 서버 2차 방어), `s3keys.py` 영구 보관 주석(D-17), 강사 코드가 붙는 `SUPPLIER_UIDS` 파서(D-12). 이 플랜의 산출물이 뒤 플랜(38-05 판정·38-06 writer/Lambda·38-07 파이프라인·38-10/12 페이지)의 **인터페이스**다 — 여기서 정한 이름을 뒤에서 바꾸지 않는다.

Purpose: 뒤쪽 자동화가 "각도 자동 추출"보다 먼저 막히는 곳은 키 파싱(`s3keys.py:18` 이 `reference/` 를 거부)과 계약 부재다(RESEARCH 관측 Q2·Q3). 순수 함수라 AWS/Pod 없이 pytest 로 닫힌다.
Output: 위 files_modified 9개. 새 테스트 2개(`test_registration_contract.py`, `test_validation.py` 확장) + `test_s3keys.py` 확장.

**Phase 38 전체 전제(플랜 01 머리말 — 모든 플랜에 적용):**
1. **인터럽트:** 정은지 새 영상이 도착하면 이 phase 의 어떤 작업보다 **시험 영상 2차**(`backend/scripts/sealed_test.py seal → run → grade`)가 먼저다. 사람 영상은 `manifest.json` 에 넣지 않는다(D-21, `intake_clips.py` → clips.jsonl).
2. **push:** 플랜 하나가 끝날 때마다 `git push origin main`(belle 규칙 — Pod 가 origin 만 본다). 커밋은 task 단위, 메시지 접두 `feat(38-01)` / `test(38-01)` / `docs(38-01)`.
3. **홈 디렉터리 금지:** `~/` 는 PII 가 든 별도 git 저장소다. `npm`·`pip`·임시 파일을 `~` 에 두지 않는다(임시 = scratchpad).
4. **보고 규칙(CLAUDE.md §7 ★):** SUMMARY 의 모든 주장에 `[확인]`/`[미확인]`, 관측과 진단을 다른 절에, 숫자는 소비처까지 따라간 뒤에.
5. **테스트 실행은 반드시 `cd backend && .venv/bin/python -m pytest ...`** (worktree 에 .venv 가 없으면 다른 인터프리터로 돈다 — 메모리 dont-trust-subagent-gate-numbers). 앱은 `cd app && npm run typecheck`, node 테스트는 `node --test <file>`(Node 24 type stripping, 신규 의존성 0).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-supplier-link/38-CONTEXT.md
@.planning/phases/38-supplier-link/38-RESEARCH.md
@.planning/phases/38-supplier-link/38-PATTERNS.md
@.planning/phases/38-supplier-link/38-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: `reference/{uid}/{refId}.{ext}` 키 build/parse + s3keys 헤더 정정(D-17) + 거부 테스트</name>
  <files>backend/shared/python/sunity_shared/s3keys.py, backend/tests/test_s3keys.py</files>
  <read_first>
    - backend/shared/python/sunity_shared/s3keys.py (전체 — `_UPLOAD_KEY_RE` :18 · `build_upload_key` · `ParsedUploadKey` · `parse_upload_key` · `build_coach_audio_key` 의 "단일 출처" docstring 어법 :47-55)
    - backend/tests/test_s3keys.py (전체 — `test_rejects_foreign_keys` 목록 어법)
    - .planning/phases/38-supplier-link/38-PATTERNS.md "### `backend/shared/python/sunity_shared/s3keys.py`" 절 + RESEARCH "## Code Examples" A
    - .planning/phases/38-supplier-link/38-CONTEXT.md D-04 · D-17 · D-19 · "Claude's Discretion — `reference/` 키 체계"
  </read_first>
  <action>
    `s3keys.py` 에 기존 4종 세트를 미러해 추가한다(REFERENCE_PREFIX 상수 :15 재사용, 새 상수 금지):
    - `_REFERENCE_UPLOAD_KEY_RE = re.compile(r"^reference/(?P<uid>[A-Za-z0-9]+)/(?P<ref_id>[A-Za-z0-9]+)\.(?P<ext>mp4|mov)$")` — uid 세그먼트를 영숫자로 제한하는 이유(평면 `reference/ref-*.mp4` 11개와 `reference/_archive/` 배제 = 기존 객체·손 업로드가 새 경로에 못 들어온다, D-19)를 주석에 적는다.
    - `build_reference_upload_key(uid: str, ref_id: str, ext: str) -> str` → `f"{REFERENCE_PREFIX}/{uid}/{ref_id}.{ext}"`. docstring 은 `build_coach_audio_key` 의 "**단일 출처**" 어법으로: 발급(`reference-upload-url` Lambda) · 역파싱(`pipeline.lambda_handler` · `runpod_inference/server.py /register-reference`) · 재개(`requeue_reference_registrations.py`)가 이 함수 하나를 공유한다. Phase 38 D-04 인용.
    - `@dataclass(frozen=True) class ParsedReferenceKey: uid: str; ref_id: str; ext: str`
    - `parse_reference_key(key: str) -> ParsedReferenceKey | None` — 형식 불일치면 None(예외 금지).
    - 헤더 docstring :3-4 의 "수명주기 정책으로 30일 후 자동 삭제 (backend_CLAUDE.md 비용 관리)" 문장을 **삭제**하고 "원본 영상은 uploads/ 프리픽스에 **영구 보관**(2026-09-26 belle 결정, Phase 38 D-17 — 30일 수명주기 규칙 해제). 분석 결과/기준 모션은 별도 프리픽스. 기준 모션 키는 두 모양: 기존 11개 평면 `reference/{motionId}.mp4`(손 등록, 무접촉) · 공급자 링크 `reference/{uid}/{refId}.{ext}`(Phase 38)." 로 바꾼다.
    `test_s3keys.py` 확장: (a) `test_reference_key_roundtrip` — `build_reference_upload_key("uid42","0123456789abcdef0123456789abcdef","mov")` → `parse_reference_key` → uid/ref_id/ext 일치 (b) `test_parse_reference_key_rejects_legacy_and_foreign` — 다음 전부 None: `reference/ref-kip-up.mp4`, `reference/_archive/ref-kip-up.mp4`, `reference/u1/a1.avi`, `reference/u-1/a1.mp4`, `reference/u1/a1/b.mp4`, `reference/u1/a1.mp4.bak`, `uploads/u1/a1.mp4`, `results/u1/a1.mp4`, `random` (c) `test_upload_parser_rejects_reference_keys` — `parse_upload_key("reference/u1/a1.mp4") is None` (d) 기존 테스트 무수정 통과.
    NEVER: `_UPLOAD_KEY_RE` 나 `parse_upload_key` 본문을 건드리지 않는다(학생 경로 byte-무접촉 — Success ④).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && .venv/bin/python -m pytest tests/test_s3keys.py -q && grep -c '30일' shared/python/sunity_shared/s3keys.py | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && .venv/bin/python -m pytest tests/test_s3keys.py -q` exits 0 with ≥ 4 tests collected.
    - `grep -c 'def parse_reference_key\|def build_reference_upload_key\|class ParsedReferenceKey' backend/shared/python/sunity_shared/s3keys.py` == 3.
    - `grep -c '30일' backend/shared/python/sunity_shared/s3keys.py` == 0 and `grep -c '영구 보관' backend/shared/python/sunity_shared/s3keys.py` ≥ 1.
    - `git diff backend/shared/python/sunity_shared/s3keys.py` shows no change to the `_UPLOAD_KEY_RE` line or `parse_upload_key` body.
  </acceptance_criteria>
  <done>reference 키 4종 세트가 있고, legacy/`_archive`/uploads 키가 전부 None 이며, 헤더에 영구 보관이 적혀 있다.</done>
</task>

<task type="auto">
  <name>Task 2: 등록 상태·실패 코드·레벨·SUPPLIER_UIDS 파서 + `AnalysisDoc.selfCheckForReference`(D-10) — 계약 3벌 lockstep(models.py · analysis.ts · contract.md · reference-motions.md) + 대조 테스트</name>
  <files>backend/shared/python/sunity_shared/models.py, app/src/types/analysis.ts, docs/contract.md, docs/reference-motions.md, backend/tests/test_registration_contract.py</files>
  <read_first>
    - backend/shared/python/sunity_shared/models.py :1-12 (헤더·MODES) · :692-713 (STATUS_*, PIPELINE_SEQUENCE) · :746-768 (`COACH_STATUS_*` 별도 enum 선례 + "PIPELINE_SEQUENCE 추가 금지" 주석) · :800-836 (ERROR_MESSAGE · 경로 헬퍼) · :590-606 (learningOptIn 3-way lockstep 주석 블록 — 새 상수 `ANALYSIS_FIELD_SELF_CHECK_FOR_REFERENCE` 의 자리·어법)
    - app/src/types/analysis.ts :76-92 (UploadUrlRequest/Response) · :94-109 (AnalysisStatus/AnalysisErrorCode — 여기에 추가하지 않는다) · :1236-1270 (`AnalysisDoc` — `learningOptIn` 주석 어법, 새 필드 `selfCheckForReference` 자리) · :1274-1329 (SkillLevel · ClipRange · ReferenceMotion) · :2220-2227 (ERROR_MESSAGE)
    - docs/contract.md :48-72 (§2 엔드포인트 어법) · :243-300 (§3 `AnalysisDoc` 의 `learningOptIn?` 행 + 그 아래 `> Phase 26 …` 인용문 어법 · ReferenceMotion) · :800-826 (§5 문구)
    - docs/reference-motions.md :40-92 (§3 스키마 — 필드 옆 `// seed` `// reprocess` `// backfill` 출처 주석 어법)
    - .planning/phases/38-supplier-link/38-PATTERNS.md "### `backend/shared/python/sunity_shared/models.py`" · "### `app/src/types/analysis.ts`" · "Shared Patterns §6" 절
    - .planning/phases/38-supplier-link/38-UI-SPEC.md "**row (A-3 행 · A-3b · A-3c)**" 표 (실패 4형 문구 정본)
  </read_first>
  <action>
    **models.py** (COACH_STATUS 블록 바로 아래, 같은 어법의 머리 주석 "PIPELINE_SEQUENCE / status enum 에는 절대 추가 금지 — 기준 doc 의 별도 필드 `registrationStatus`" + `Phase 38 D-05·D-09` 인용):
    - `REGISTRATION_STATUS_REGISTERING="registering"`, `_QUEUED="queued"`, `_PROCESSING="processing"`, `_FAILED="failed"`, `_ACTIVE="active"`, `REGISTRATION_STATUSES` 튜플(그 순서).
    - `SELF_CHECK_STATUS_PENDING="pending"`, `_QUEUED="queued"`, `_DONE="done"`, `_FAILED="failed"`, `SELF_CHECK_STATUSES`.
    - `REG_ERR_NO_HUMAN="no_human"`, `REG_ERR_MULTIPLE_PEOPLE="multiple_people"`, `REG_ERR_NO_STANDING_START="no_standing_start"`, `REG_ERR_LOW_CONFIDENCE="low_confidence"`, `REG_ERR_TOO_SHORT="too_short"`, `REG_ERR_TOO_LONG="too_long"`, `REG_ERR_SERVER_ERROR="server_error"`, `REGISTRATION_ERROR_CODES` 튜플(그 순서).
    - `REGISTRATION_ERROR_MESSAGE` dict — 글자 단위 정본. **조인 규칙(38-03 `supplierCopy.test.ts` 가 이 규칙으로 대조한다):** 6개 코드는 UI-SPEC `row.fail.<code>.title` + `'. '` + `row.fail.<code>.body` 를 그대로 이은 문자열(제목 끝 마침표 없음, 본문은 마침표로 끝난다); `no_human` 만 D-09 대로 기존 `ERROR_MESSAGE.no_human` 재사용 — 페이지 `row.fail.no_human`(Figma `1:479` 문구)과 **다른 것이 의도된 예외**이고 contract.md §5 에 그렇게 적는다:
      `no_human` → `ERROR_MESSAGE[ERR_NO_HUMAN]` (객체 그대로 참조, 문자열 복사 금지) ·
      `multiple_people` → `영상에 여러 사람이 나와요. 한 사람만 나오게 다시 촬영해 주세요.` ·
      `no_standing_start` → `서 있는 자세로 시작하지 않았어요. 서 있는 자세에서 시작해 주세요. 폴 옆에 서서 1초쯤 있다가 동작을 시작하면 돼요.` ·
      `low_confidence` → `일부 관절을 못 읽었어요. 잘 안 보인 부위: {joints}. 밝은 곳에서, 옷과 배경이 구분되게 다시 촬영해 주세요.` (`{joints}` 는 파이프라인이 `str.replace` 로 치환 — `str.format` 금지, 주석에 명시) ·
      `too_short` → `영상이 너무 짧아요. 기준 동작은 5초 이상이어야 해요. 동작 전체가 담기게 다시 올려주세요.` ·
      `too_long` → `영상이 너무 길어요. 동작 하나는 30초 이내로 올려주세요. 콤보는 60초까지예요.` ·
      `server_error` → `등록 중 문제가 생겼어요. 잠시 후 다시 올려주세요. 계속 그러면 운영팀에 알려주세요.`
    - `REFERENCE_LEVELS = ("basic", "intermediate", "advanced")` (앱 `SkillLevel` 미러, picker `normalize()` 가 버리지 않는 값 — D-07/D-19).
    - `REFERENCE_MIN_DURATION_SEC = 5.0`, `REFERENCE_MAX_DURATION_SEC = 30.0`, `REFERENCE_COMBO_MAX_DURATION_SEC = 60.0`, `REFERENCE_NAME_MAX_LEN = 30`, `CONSENT_VERSION = "2026-09-26"` (기획안 §6 문안 판), `SUPPLIER_CODE_RE = re.compile(r"^[A-Z0-9]{3,8}$")`, `SUPPLIER_UIDS_PARAM_DEFAULT = "/sunity/motion/supplier-uids"`.
    - `parse_supplier_uids(value: str | None) -> dict[str, str | None]` — 순수 함수. 쉼표 구분, 각 항목 `uid` 또는 `uid:CODE`, 공백 strip, 빈 항목 무시; CODE 가 `SUPPLIER_CODE_RE` 에 안 맞으면 **uid 는 유지하고 code 는 None**(운영 오타가 페이지 전체를 잠그지 않게 — 호출측이 warning 로그). None/빈 문자열 → `{}`. 예: `"abc:EUNJI, def, ghi:bad code"` → `{"abc":"EUNJI","def":None,"ghi":None}`.
    - `ANALYSIS_FIELD_SELF_CHECK_FOR_REFERENCE = "selfCheckForReference"` — learningOptIn 주석 블록(:590-606) 바로 아래, 같은 어법의 주석: "Phase 38 D-10 — 자기 재현성 분석 doc 표식(값 = 기준 refId). 쓰기 = `pipeline._trigger_self_check`(38-07) 의 `create_analysis_doc` payload 키, 읽기 = `_record_self_score_if_needed`/`_mark_self_check_failed_if_needed`(38-08). 앱은 읽지도 쓰지도 않는다(`userAnalyses.normalize` 무접촉). 3-way lockstep: analysis.ts `AnalysisDoc.selfCheckForReference` + contract.md §3". 문자열 리터럴은 이 한 곳에만 — 38-07/38-08 은 상수만 쓴다.
    **analysis.ts** (ReferenceMotion 위·아래, `// Phase 38 — 공급자 링크 등록 계약 (models.py REGISTRATION_* 미러, contract.md §2/§3/§5)` 주석):
    - `export type ReferenceRegistrationStatus = 'registering' | 'queued' | 'processing' | 'failed' | 'active';`
    - `export type SelfCheckStatus = 'pending' | 'queued' | 'done' | 'failed';`
    - `export type ReferenceRegistrationErrorCode = 'no_human' | 'multiple_people' | 'no_standing_start' | 'low_confidence' | 'too_short' | 'too_long' | 'server_error';`
    - `export interface ReferenceRegistrationError { code: ReferenceRegistrationErrorCode; message: string; joints?: string[] }` (joints = `KEYPOINT_LABEL_KO` 한국어 부위명 목록, 38-05 에서 채움)
    - `export interface ReferenceConsent { portrait: boolean; usage: boolean; silent: boolean; training: boolean; version: string; at: number; uid: string }`
    - `export interface ReferenceUploadUrlRequest { name: string; athleteName: string; level: SkillLevel; techniqueRefId?: string | null; isCombo: boolean; isSplit: boolean; hasHold: boolean; standingStart: boolean; clipRange?: ClipRange | null; consent: { portrait: boolean; usage: boolean; silent: boolean; training: boolean }; format: VideoFormat; fileSizeBytes: number; durationSec?: number | null }`
    - `export interface ReferenceUploadUrlResponse { refId: string; uploadUrl: string; s3Key: string; expiresInSec: number }`
    - `export interface SupplierProbeResponse { probe: true; uid: string; supplierCode: string | null }`
    - `ReferenceMotion` 에 optional 필드 추가(각 줄 끝 `// register` 출처 주석): `supplierUid?`, `supplierCode?`, `source?: 'supplier-link'`, `techniqueRefId?: string | null`, `isCombo?`, `isSplit?`, `hasHold?`, `standingStart?`, `consent?: ReferenceConsent`, `registrationStatus?: ReferenceRegistrationStatus`, `registrationError?: ReferenceRegistrationError | null`, `queuedReason?: string | null`, `registrationUpdatedAt?: number`, `selfScore?: number | null`, `selfCheckStatus?: SelfCheckStatus`, `selfCheckAnalysisId?: string | null`, `anglesRealFps?: number`, `createdAt?: number`. `referenceSplitAngle?`·`referenceKeypointReport?` 가 이미 있으면 그대로(없으면 추가). **AnalysisStatus/AnalysisErrorCode/ERROR_MESSAGE 는 무접촉.**
    - `AnalysisDoc`(:1236-1270) 의 `learningOptIn?: boolean;` 바로 아래에 `selfCheckForReference?: string | null;` — 주석(learningOptIn 어법): "Phase 38 D-10 — 이 분석이 기준 `reference/{refId}` 의 자기 재현성 분석임을 표시(값 = refId). 서버(38-07 `_trigger_self_check`)만 쓰고 38-08 훅이 읽는다; 앱은 읽지도 쓰지도 않는다(`userAnalyses.normalize` 무접촉). 3-way lockstep: models.py `ANALYSIS_FIELD_SELF_CHECK_FOR_REFERENCE` + contract.md §3".
    - `export const REGISTRATION_ERROR_MESSAGE: Record<ReferenceRegistrationErrorCode, string>` — models.py 와 **글자 단위 동일**(no_human 은 `ERROR_MESSAGE.no_human` 참조).
    **docs/contract.md**: §2 에 `### POST /reference/upload-url` 절(인증: Firebase ID 토큰 + **공급자 uid 화이트리스트**(SSM `/sunity/motion/supplier-uids` ∪ `BELLE_UID`, env 미설정 = 403) · 요청 `ReferenceUploadUrlRequest` 필드 표 · `{"probe": true}` 변형 = 화이트리스트 확인만 → `SupplierProbeResponse` · 응답 `ReferenceUploadUrlResponse` · 오류 401 `unauthorized` / 403 `forbidden` / 400 `bad_request`·`unsupported_format`·`size_exceeded`·`too_short`·`too_long` / 500 `server_error` · 부작용 = `reference/{refId}` doc `create()` 선작성 `registrationStatus:'registering'`, `isActive:false`); §3 `ReferenceMotion` 에 등록 필드 표(위 TS 와 같은 이름·타입, `registrationStatus` 는 **AnalysisStatus 와 별개**라고 명시); §3 `AnalysisDoc` 블록의 `learningOptIn?` 행 아래에 `selfCheckForReference? string | null` 행(설명: Phase 38 D-10 자기 재현성 표식 · 기록 주체 = 파이프라인 `_trigger_self_check`(38-07) · 소비 = selfScore 훅(38-08) · 앱 무접촉 · 3-way lockstep) + learningOptIn 인용문 어법의 `> Phase 38 (Plan 38-01) — selfCheckForReference 신설 …` 한 단락; §5 에 `REGISTRATION_ERROR_MESSAGE` 7줄 + 표 아래 한 줄: "`no_human` 은 기존 `ERROR_MESSAGE.no_human` 재사용(D-09) — 공급자 페이지 `row.fail.no_human` 제목/본문(Figma `1:479`)과 다르다, 의도된 예외. 나머지 6개 = `row.fail.<code>.title + '. ' + body`". 기존 절은 무접촉.
    **docs/reference-motions.md** §3 인터페이스 블록에 같은 필드를 `// register` 주석으로 추가하고, 헤더 문장 "주석의 seed / reprocess / backfill 은 …" 에 `register`(= Phase 38 공급자 링크 `reference-upload-url` + `pipeline._register_reference`) 를 덧붙인다.
    **backend/tests/test_registration_contract.py**(신규): (1) `set(REGISTRATION_ERROR_MESSAGE) == set(REGISTRATION_ERROR_CODES)` (2) `REGISTRATION_ERROR_MESSAGE["no_human"] is ERROR_MESSAGE["no_human"]` (3) `"registering" not in PIPELINE_SEQUENCE` and `STATUS_*`/`ANALYSIS_ERROR_CODES` 길이 불변(현재 값을 상수로 박아 단언) (4) `parse_supplier_uids` 4 케이스(None → {}, 정상, 잘못된 코드 → None, 공백/빈 항목) (5) **3벌 대조**: `Path(__file__).resolve().parents[2] / "app/src/types/analysis.ts"` 텍스트에 `REGISTRATION_ERROR_MESSAGE` 의 no_human 을 제외한 6개 문자열이 verbatim 으로 들어 있고, 7개 코드 문자열 `'multiple_people'` 등이 `ReferenceRegistrationErrorCode` 선언 안에 있으며, `docs/contract.md` 텍스트에 `POST /reference/upload-url` 과 6개 문구가 있다. (6) **selfCheckForReference 3벌**: `models.ANALYSIS_FIELD_SELF_CHECK_FOR_REFERENCE == "selfCheckForReference"` 이고, 그 문자열이 `analysis.ts` 의 `export interface AnalysisDoc {` 이후 첫 `}` 앞 구간에 있으며, `docs/contract.md` 의 `## 3. Firestore 문서` 와 `## 4.` 제목 사이에도 있다.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && .venv/bin/python -m pytest tests/test_registration_contract.py -q && cd ../app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && .venv/bin/python -m pytest tests/test_registration_contract.py -q` exits 0 (≥ 6 tests).
    - `cd app && npm run typecheck` exits 0.
    - `grep -c "registering" backend/shared/python/sunity_shared/models.py` ≥ 1 and the `PIPELINE_SEQUENCE` tuple line is unchanged in `git diff`.
    - `grep -c "### POST /reference/upload-url" docs/contract.md` == 1; `grep -c "REGISTRATION_ERROR_MESSAGE" docs/contract.md app/src/types/analysis.ts backend/shared/python/sunity_shared/models.py` → each ≥ 1.
    - `grep -c "// register" docs/reference-motions.md` ≥ 10.
    - `grep -n "AnalysisStatus =" -A8 app/src/types/analysis.ts` contains no `registering`/`active`.
    - `grep -c "selfCheckForReference" app/src/types/analysis.ts docs/contract.md backend/shared/python/sunity_shared/models.py` → each ≥ 1; `grep -c "^ANALYSIS_FIELD_SELF_CHECK_FOR_REFERENCE = " backend/shared/python/sunity_shared/models.py` == 1.
  </acceptance_criteria>
  <done>세 벌(+reference-motions.md)에 같은 enum·문구가 있고, 파이썬 테스트가 TS·contract 텍스트와 대조해 통과한다.</done>
</task>

<task type="auto">
  <name>Task 3: `validate_reference_upload_request` 순수 검증(폼 메타 · 선언 4 · 동의 · 5~30초/콤보 60) + 거부 테스트</name>
  <files>backend/shared/python/sunity_shared/validation.py, backend/tests/test_validation.py</files>
  <read_first>
    - backend/shared/python/sunity_shared/validation.py (전체 — `ValidationError` · `validate_analysis_id_format` · `UploadRequest` · `validate_upload_request` 의 bool-위장-int 거부 :74 어법)
    - backend/tests/test_validation.py (전체 — `OK_MODE3` + parametrize 거부 어법)
    - backend/shared/python/sunity_shared/models.py (Task 2 결과 — REFERENCE_LEVELS · REGISTRATION_ERROR_MESSAGE · REFERENCE_*_DURATION_SEC · REFERENCE_NAME_MAX_LEN)
    - .planning/phases/38-supplier-link/38-CONTEXT.md D-06 · D-07 · D-08 · D-14 · 기획안 §6 동의 문안(CONTEXT canonical_refs)
    - .planning/phases/38-supplier-link/38-PATTERNS.md "### `backend/shared/python/sunity_shared/validation.py`" 절
  </read_first>
  <action>
    `validation.py` 에 추가(기존 `validate_upload_request` 무접촉):
    - `@dataclass(frozen=True) class ReferenceUploadRequest`: `name: str`, `athlete_name: str`, `level: str`, `technique_ref_id: str | None`, `is_combo: bool`, `is_split: bool`, `has_hold: bool`, `standing_start: bool`, `clip_range: tuple[float, float] | None`, `consent_training: bool`, `fmt: str`, `file_size_bytes: int`, `duration_sec: float | None`. (필수 동의 3 은 검증 통과 = 전부 True 이므로 필드로 안 둔다 — docstring 에 명시.)
    - `validate_reference_upload_request(body: dict) -> ReferenceUploadRequest` — 순서·코드 고정:
      body 가 dict 아님 → `bad_request`; `name` str·strip 비공백·`len ≤ models.REFERENCE_NAME_MAX_LEN` 아니면 `bad_request` "동작 이름을 고르거나 입력해주세요."; `athleteName` 같은 규칙 "선수 이름을 입력해주세요."; `level ∉ models.REFERENCE_LEVELS` → `bad_request` "레벨을 고르세요."; `techniqueRefId` 는 None 또는 `^[A-Za-z0-9_-]{1,64}$` 문자열(아니면 `bad_request`); `isCombo` 부재 = False, 있으면 bool 만; `isSplit`·`hasHold`·`standingStart` 는 `isinstance(v, bool)` 필수(int 위장 거부) "답을 골라주세요."; `standingStart is False` → `bad_request` "서 있는 자세에서 시작한 영상만 등록할 수 있어요."(D-09 사전 차단); `clipRange` 는 None 또는 dict `{execStartS, execEndS}` 실수(bool 제외) `0 ≤ start < end` → 튜플, 아니면 `bad_request`(D-06 선택 입력); `consent` dict 필수 — `portrait`·`usage`·`silent` 가 `is True` 아니면 `bad_request` "필수 동의 3가지에 체크해주세요.", `training` 부재 = False, 있으면 bool 만(D-08 기본 꺼짐); `format ∉ models.VIDEO_FORMATS` → `ValidationError(models.ERR_UNSUPPORTED_FORMAT, models.ERROR_MESSAGE[...])`; `fileSizeBytes` int(bool 제외) > 0 아니면 `bad_request`, `> models.MAX_VIDEO_BYTES` → `ERR_SIZE_EXCEEDED`; `durationSec` 는 None(웹이 metadata 를 못 읽은 경우 fail-open, RESEARCH A12) 또는 양의 실수 — 있으면 `< REFERENCE_MIN_DURATION_SEC` → `ValidationError(models.REG_ERR_TOO_SHORT, models.REGISTRATION_ERROR_MESSAGE[...])`, `> (REFERENCE_COMBO_MAX_DURATION_SEC if is_combo else REFERENCE_MAX_DURATION_SEC)` → `REG_ERR_TOO_LONG`. 문자열은 strip 해 반환.
    `test_validation.py` 확장: `OK_REFERENCE` 정상 body(전 필드) + `test_reference_ok_normalizes`(strip·기본값 training False·isCombo False·clip_range 튜플) + parametrize 거부 ≥ 12 케이스(name 공백·31자·level 'pro'·isSplit 1(int)·standingStart False·consent.usage False·consent 누락·training "yes"·format 'avi' → code `unsupported_format`·fileSizeBytes True·durationSec 4.9 → code `too_short`·durationSec 31 → `too_long`·durationSec 45 + isCombo True → 통과·durationSec 61 + isCombo True → `too_long`·clipRange {execStartS:5, execEndS:3}). 각 거부는 `pytest.raises(ValidationError)` + `e.value.code` 단언.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && .venv/bin/python -m pytest tests/test_validation.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && .venv/bin/python -m pytest tests/test_validation.py -q` exits 0 with ≥ 14 new tests passing and all pre-existing tests unchanged.
    - `grep -c "def validate_reference_upload_request\|class ReferenceUploadRequest" backend/shared/python/sunity_shared/validation.py` == 2.
    - `git diff backend/shared/python/sunity_shared/validation.py` shows `validate_upload_request` body untouched.
    - `validation.py` imports nothing from boto3/firebase (`grep -c "boto3\|firebase" validation.py` == 0).
  </acceptance_criteria>
  <done>폼 검증이 순수 함수로 존재하고 선언 4·동의·길이 규칙이 코드로 거부된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 클라이언트 JSON → `validate_reference_upload_request` | 폰 브라우저가 보낸 폼 본문(신뢰 불가) |
| S3 이벤트 키 → `parse_reference_key` | 버킷에 누구든 손으로 올릴 수 있는 객체 키(경로 조작 가능) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01-1 | Tampering | `parse_reference_key` | mitigate | uid·refId 세그먼트 영숫자 정규식 — `..`·`_archive`·`ref-*` 매치 불가, 형식 불일치 = None(Task 1 거부 테스트) |
| T-38-01-2 | Spoofing | `ReferenceUploadUrlRequest` | mitigate | 본문에 uid/refId 필드를 두지 않는다 — 키는 서버가 토큰 uid + 서버 생성 refId 로만 구성(38-06) |
| T-38-01-3 | Tampering | `validate_reference_upload_request` | mitigate | enum·bool(`isinstance` 위장 int 거부)·길이·크기·형식 검사, 실패 = `ValidationError(code)` (Task 3) |
| T-38-01-4 | Repudiation | consent | mitigate | 클라이언트 동의값은 True 만 통과, 시각·버전·uid 는 서버(38-06 writer)가 붙인다 — 본문의 `at`/`version` 은 무시 |
| T-38-01-5 | Elevation | `parse_supplier_uids` | accept | 잘못된 CODE 는 uid 유지 + code None — 화이트리스트 판정은 uid 만 쓰므로 권한 상승 없음 |
| T-38-SC | Tampering | npm/pip installs | accept | 이 플랜은 패키지 설치 0 |
</threat_model>

<verification>
- `cd backend && .venv/bin/python -m pytest tests/test_s3keys.py tests/test_validation.py tests/test_registration_contract.py -q` → 0 failed.
- `cd app && npm run typecheck` → exit 0.
- `git diff --stat` 에 위 9개 파일만.
- `git log --oneline origin/main..HEAD` 가 비어 있다(플랜 끝 push).
</verification>

<success_criteria>
- 계약 3벌(+reference-motions.md)에 등록 상태·실패 코드·문구가 같은 문자열로 존재하고 파이썬 테스트가 그것을 대조한다.
- `reference/{uid}/{refId}.{ext}` 키가 라운드트립되고 legacy 키는 새 경로에 못 들어온다.
- 폼 검증 순수 함수가 D-07·D-08·D-14 규칙을 코드로 강제한다.
</success_criteria>

<output>
Create `.planning/phases/38-supplier-link/38-01-SUMMARY.md` when done (관측/진단 분리, `[확인]`/`[미확인]` 표식).
</output>
