---
phase: 38-supplier-link
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/app/(tabs)/analyze.tsx
  - app/src/lib/pickerFailure.ts
  - app/src/lib/pickerFailure.test.ts
  - app/src/lib/videoDuration.ts
  - app/src/lib/videoDuration.test.ts
  - app/src/app/analysis/loading.tsx
  - app/src/app/help.tsx
  - docs/ia.md
  - docs/reference-capture-guide.md
  - app/src/app/(tabs)/profile.tsx
  - app/src/constants/profileCopy.ts
  - .planning/phases/38-supplier-link/38-02-simulator-profile.png
  - .planning/phases/38-supplier-link/38-02-simulator-tips.png
autonomous: true
requirements: [REQ-38-5, REQ-38-6]
must_haves:
  truths:
    - "D-14 (리뷰 R15c): 순수 함수 `classifyDurationMs` 가 duration 이 유효할 때 3초 미만 → `tooShort`, 90초 초과 → `tooLong`, 없거나 0 이면 `null`(fail-open)을 돌려주고 앱 `validate()` 가 그것을 쓴다; 카메라는 `videoMaxDuration: 90`. **보장 범위는 클라이언트 검사뿐** — 수요자 `uploads/` 경로에 서버 측 길이 상한은 없다(범위 밖, 위협표·주석·SUMMARY 에 같은 문장)"
    - "D-15/D-16: 앱·문서 어디에도 '측면 45°'·'2~3m'·'정면 우선'·'정면으로 …'·'정면을 기준' 이 남지 않고 전부 '기준 영상처럼 찍으세요(폴 전체와 전신이 들어오는 거리, 세로, 고정)' 계열로 바뀐다"
    - "D-12/D-13: 마이 탭 계정 카드 아래에 `강사 코드 · 미입력` 한 줄이 보이고 탭 동작이 없다(입력·귀속·크레딧 지급은 범위 밖, 크레딧 숫자·결제 없음)"
  artifacts:
    - path: "app/src/lib/pickerFailure.ts"
      provides: "PickFailureKind 'tooShort' | 'tooLong' + copy"
      contains: "tooShort"
    - path: "app/src/lib/videoDuration.ts"
      provides: "classifyDurationMs(durationMs) → 'tooShort' | 'tooLong' | null + MIN/MAX_DURATION_MS (순수, RN import 0)"
      contains: "classifyDurationMs"
    - path: "app/src/constants/profileCopy.ts"
      provides: "profileCopy.instructorCode {label,value,hint,a11y}"
      contains: "instructorCode"
    - path: "app/src/app/(tabs)/profile.tsx"
      provides: "강사 코드 InfoRow 한 줄"
      contains: "profileCopy.instructorCode"
    - path: ".planning/phases/38-supplier-link/38-02-simulator-profile.png"
      provides: "마이 탭 강사 코드 행 — 시뮬레이터 실물(typecheck 가 못 잡는 렌더 크래시 확인)"
  key_links:
    - from: "app/src/app/(tabs)/analyze.tsx"
      to: "app/src/lib/videoDuration.ts"
      via: "validate() → classifyDurationMs(asset.duration)"
      pattern: "classifyDurationMs"
    - from: "app/src/app/(tabs)/analyze.tsx"
      to: "app/src/lib/pickerFailure.ts"
      via: "validate() → describePickFailure(kind)"
      pattern: "tooShort|tooLong"
    - from: "app/src/app/(tabs)/profile.tsx"
      to: "app/src/constants/profileCopy.ts"
      via: "import profileCopy"
      pattern: "profileCopy"
---

<objective>
앱 쪽 호스팅-무관 작업 셋: (1) 수요자 영상 길이 3~90초 즉시 검사 + 문구(D-14, REQ-38-6) (2) 촬영 문구 정정 14자리 — 앱 3파일 + 문서 2파일(D-15·D-16, UI-SPEC §C 표가 글자 단위 정본) (3) 마이 탭 "강사 코드" 한 줄(D-12·D-13, REQ-38-5, 표시만).

Purpose: "측면 45°, 2~3m" 는 실물(09-26 S3 기준 영상)과 안 맞는 낡은 문구이고, 길이 하한은 IA 3-3 에 있는데 미구현이었다(RESEARCH Q9). 강사 코드 칸은 공급자 페이지 "내 코드" 카드의 수요자 쪽 자리다.
Output: 위 11개 파일 + 시뮬레이터 스크린샷 2장(`38-02-simulator-profile.png` · `38-02-simulator-tips.png`, phase 디렉터리). node 테스트 1개 확장 + 1개 신설(`videoDuration.test.ts` — 리뷰 R15c: duration 분기 자체를 검사), 정정 grep 게이트 0건, typecheck clean, 시뮬레이터 실물 확인.

**리뷰 R15c(2026-09-26) 정합:** 길이 검사의 보장은 **클라이언트 fail-open 하나**다. duration 이 없는 픽(웹·일부 갤러리)은 통과하고, 수요자 `uploads/` 경로에는 서버 측 길이 상한이 없다(범위 밖 — 공급자 경로만 38-01/38-07 이 서버 2차 방어). 이 문장을 action 주석·위협표·SUMMARY 에 같은 뜻으로 적는다("서버가 2차 방어" 라고 쓰지 않는다).

**선언된 이월(오케스트레이터 결정 2026-09-26):** 이 플랜의 앱 변경(REQ-38-5/6)은 **실기기에 배포하지 않는다** — OTA(`eas update`)/EAS 빌드는 phase 뒤 belle 이 정한다(ROADMAP 38 "Not in scope" 에 명시). 이 phase 안의 확인은 시뮬레이터까지이고(Task 3), SUMMARY 에 "실기기 미반영(OTA/EAS 미실행 — belle 결정 대기)" 한 줄을 적는다. 다른 플랜의 체크포인트에 이 확인을 미루지 않는다.

Phase 38 전제(38-01 머리말 1~5)가 그대로 적용된다 — 시험 영상 2차 인터럽트 · 플랜 끝 push · `~` 금지 · 보고 규칙 · `.venv`/typecheck 경로.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/38-supplier-link/38-CONTEXT.md
@.planning/phases/38-supplier-link/38-UI-SPEC.md
@.planning/phases/38-supplier-link/38-PATTERNS.md
@app/CLAUDE.md
@design.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `videoDuration.ts` 순수 분기(`classifyDurationMs`, node 테스트) + `validate()` 길이 3~90초(fail-open, 클라이언트 보장뿐) + `tooShort`/`tooLong` 문구 + `videoMaxDuration: 90`</name>
  <files>app/src/lib/videoDuration.ts, app/src/lib/videoDuration.test.ts, app/src/app/(tabs)/analyze.tsx, app/src/lib/pickerFailure.ts, app/src/lib/pickerFailure.test.ts</files>
  <behavior>
    - `classifyDurationMs(undefined)` → `null`; `classifyDurationMs(null)` → `null`; `classifyDurationMs(0)` → `null`(fail-open — 값 없음/0 은 판정 불가)
    - `classifyDurationMs(2000)` → `'tooShort'`; `classifyDurationMs(3000)` → `null`(경계 포함); `classifyDurationMs(90000)` → `null`; `classifyDurationMs(91000)` → `'tooLong'`
    - `MIN_DURATION_MS === 3000`, `MAX_DURATION_MS === 90000` (export — `analyze.tsx` 는 이 이름만 쓴다)
  </behavior>
  <read_first>
    - app/src/app/(tabs)/analyze.tsx :44-48 (MAX_BYTES·ALLOWED 상수) · :204-236 (`validate` · `checkLowQuality` 의 `asset.duration != null && asset.duration > 0` 가드) · :357-365 (`handleResult` 배선) · :468-474 (`launchCameraAsync` 옵션)
    - app/src/lib/pickerFailure.ts (전체 — `PickFailureKind` :20-27 · `copyFor` switch :51-130 · exhaustiveness 게이트 :125-130 · REPICK_LABEL)
    - app/src/lib/pickerFailure.test.ts (전체 — `node --test` 실행법 :1-14, Figma 확정 문구 불변 단언 어법)
    - .planning/phases/38-supplier-link/38-UI-SPEC.md "### C 앱 촬영 문구 정정 + 길이 검사" · "**C 길이 검사 문구 — `pickerFailure.ts copyFor` 신규 2건**" 표
    - docs/ia.md :198-201 (AC-VID-003-1L 원문)
    - .planning/phases/38-supplier-link/38-PATTERNS.md "### `app/src/app/(tabs)/analyze.tsx` (screen — `validate()` duration)" · "### `app/src/lib/pickerFailure.ts` (copy map — `tooShort` / `tooLong`)" 절 + Shared Patterns §10·§11
    - .planning/phases/38-supplier-link/38-REVIEWS.md "### R15" 셋째 항목(38-02 duration 의 실제 보장 범위 + 분기 테스트)
  </read_first>
  <action>
    RED 먼저: `app/src/lib/videoDuration.test.ts`(`node --test`, `node:test`/`node:assert` 만, `.ts` import 명시 — `pickerFailure.test.ts` 어법) 에 위 behavior 7 케이스 → 실패 확인 → 구현.
    `app/src/lib/videoDuration.ts`(신규, 순수 — `react`/`expo`/`react-native` import 0): `export const MIN_DURATION_MS = 3 * 1000; // IA 3-3 AC-VID-003-1L — 3초 미만 실패 (Phase 38 D-14)`, `export const MAX_DURATION_MS = 90 * 1000; // Phase 38 D-14 — 수요자 상한 90초`, `export type DurationFailure = 'tooShort' | 'tooLong'`, `export function classifyDurationMs(durationMs: number | null | undefined): DurationFailure | null` — `durationMs == null || !(durationMs > 0)` → `null`; `< MIN` → `'tooShort'`; `> MAX` → `'tooLong'`; 그 외 `null`. 헤더 주석(리뷰 R15c 문장 그대로): "보장 범위 = 클라이언트 검사뿐. duration 을 안 주는 픽(웹·일부 갤러리, RESEARCH Pitfall 5)은 통과(fail-open)하고, 수요자 `uploads/` 경로에는 서버 측 길이 상한이 없다(Phase 38 범위 밖 — 공급자 경로만 서버 2차 방어). 이 fail-open 은 의도된 수용이다."
    `analyze.tsx`: `MAX_BYTES` 옆 리터럴 상수를 두지 않고 `import { classifyDurationMs } from '../../lib/videoDuration'`; `validate()` 에서 `format`·`tooLarge` 검사 뒤에 `const durationKind = classifyDurationMs(asset.duration); if (durationKind) return durationKind;` — 주석은 videoDuration.ts 헤더를 가리키는 한 줄("서버 2차 방어" 라는 말을 쓰지 않는다). `launchCameraAsync` 옵션에 `videoMaxDuration: 90` 추가(주석 D-14).
    `pickerFailure.ts`: `PickFailureKind` 에 `'tooShort' | 'tooLong'` 추가; `copyFor` 에 두 case(§"Figma 확정 문구" 블록 **밖**, 신규 블록 주석 `// ── Phase 38 D-14 길이 검사 (양식 = 1:499 다이얼로그: 제목 + 2줄 + 다른 파일 선택) ──`):
      `tooShort` → title `영상이 너무 짧아요` · lines `['동작 전체가 담긴 영상이 필요해요.', '3초 이상인 영상을 다시 선택해주세요.']` · `primaryLabel: REPICK_LABEL` · `primaryAction: 'repick'`;
      `tooLong` → title `영상이 너무 길어요` · lines `['90초 이내 영상만 분석할 수 있어요.', '동작이 담긴 부분만 잘라서 다시 선택해주세요.']` · 같은 버튼.
    `pickerFailure.test.ts` 확장: 두 kind 의 title/lines 가 위 문자열과 **글자 단위 동일**(기존 Figma 불변 단언 어법), `primaryAction === 'repick'`, 모든 kind 의 lines 가 비어 있지 않다(기존 축 2 유지).
    NEVER: 기존 `tooLarge`/`format` 문구·순서 변경, `validate` 의 format/tooLarge 분기 순서 변경.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && node --test app/src/lib/videoDuration.test.ts app/src/lib/pickerFailure.test.ts && cd app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `node --test app/src/lib/videoDuration.test.ts` exits 0 with ≥ 7 assertions (absent/null/0 → null, 2000 → tooShort, 3000 → null, 90000 → null, 91000 → tooLong).
    - `node --test app/src/lib/pickerFailure.test.ts` exits 0 with ≥ 2 new assertions for tooShort/tooLong.
    - `cd app && npm run typecheck` exits 0 (exhaustiveness 게이트가 두 kind 매핑을 요구).
    - `grep -c "classifyDurationMs" "app/src/app/(tabs)/analyze.tsx"` ≥ 2 (import + 호출) and `grep -c "videoMaxDuration: 90" "app/src/app/(tabs)/analyze.tsx"` == 1; `grep -c "3 \* 1000\|90 \* 1000" "app/src/app/(tabs)/analyze.tsx"` == 0 (상수는 videoDuration.ts 에만).
    - `grep -c "'tooShort'\|'tooLong'" app/src/lib/pickerFailure.ts` ≥ 4.
    - `grep -c "서버가 2차 방어\|서버 2차 방어" "app/src/app/(tabs)/analyze.tsx" app/src/lib/videoDuration.ts` → 0 for analyze.tsx; videoDuration.ts 헤더에는 `서버 측 길이 상한이 없다` 문장이 있다(`grep -c '서버 측 길이 상한이 없다' app/src/lib/videoDuration.ts` == 1).
  </acceptance_criteria>
  <done>3초 미만·90초 초과가 순수 함수로 판정되고 그 분기가 node 테스트로 잠기며, 다이얼로그 문구가 단일점에 있고 duration 부재는 통과한다(보장 범위 문장이 코드·플랜에 같다).</done>
</task>

<task type="auto">
  <name>Task 2: 촬영 문구 정정 14자리(앱 3파일 + docs 2파일) + 정제된 grep 게이트 0건</name>
  <files>app/src/app/analysis/loading.tsx, app/src/app/(tabs)/analyze.tsx, app/src/app/help.tsx, docs/ia.md, docs/reference-capture-guide.md</files>
  <read_first>
    - .planning/phases/38-supplier-link/38-UI-SPEC.md "### C 앱 문구 정정 — 자리별 최종 문자열" 표 (14행 — 백틱 안이 글자 단위 정본) + 표 아래 "정정 확인 게이트" 명령
    - app/src/app/analysis/loading.tsx :486-492 · :514-522 · :543-548 (원문 verbatim)
    - app/src/app/(tabs)/analyze.tsx :593-602 (주석 :593-596 + 안내 문단 :597-602)
    - app/src/app/help.tsx :43-47
    - docs/ia.md :185-201 (§3-2·§3-3 표) · docs/reference-capture-guide.md :23-28 · :45-49 (§1 표 · §2 정면성)
    - .planning/phases/38-supplier-link/38-CONTEXT.md D-15 · D-16
    - .planning/phases/38-supplier-link/38-PATTERNS.md "### `app/src/app/analysis/loading.tsx` (문구 3자리 정정)" · "### `docs/supplier-guide.md` (신규) · docs 4종 정정"(ia.md · reference-capture-guide 정정 행 원문 · `help.tsx` 톤) 절 + File Classification 의 `help.tsx`·`docs/ia.md`·`docs/reference-capture-guide.md` 행 + Shared Patterns §12(주석 어법)
  </read_first>
  <action>
    UI-SPEC §C 표의 "최종" 열을 **그대로** 옮긴다(창작 금지). 자리별:
    - `loading.tsx:490` → `'촬영 구도나 거리가 기준 영상과 많이 다르면 이렇게 나올 수 있어요. 기준 영상처럼 찍으세요(폴 전체와 전신이 들어오는 거리, 세로, 고정).'`
    - `loading.tsx:519` → `· 기준 영상처럼 찍으세요(폴 전체와 전신이 들어오는 거리, 세로, 고정)` · `:520` → `· 밝은 실내, 역광 없이, 한 사람만` · `:521` → `· 3초 이상 90초 이내, 동작 전체 포함`
    - `loading.tsx:546` → `· 기준 영상처럼 찍었는지(폴 전체와 전신이 들어오는 거리, 세로, 고정)` — 체크리스트 "~했는지" 문법에 맞춘 **의도된 변형**(UI-SPEC §Decisions ui-checker flags 표 5행): SUMMARY 에 "deliberate deviation from D-15 sentence (grammar only)" 로 기록.
    - `analyze.tsx:593-596` 주석: `2~3m` 가 남지 않게 **다시 쓴다**(UI-SPEC §Decisions ui-checker flags 표 6행 — §C 표의 ":594 주석 끝에 정정 문구 추가" 만으로는 게이트에 `2~3m` 가 남는다) — 예: `(#20 입력 화질: 원본 화질/카톡 압축 + Phase 26 D-01-i: 촬영 거리 안내)` 로 바꾸고 줄 끝에 ` Phase 38 D-15 로 "기준 영상처럼" 문구로 정정.` 추가.
    - `analyze.tsx:600-601` 본문: `몸 전체가 화면에 잘 들어오는 거리(약 2~3m)에서 촬영해 주세요. 너무 가깝거나 멀면 분석에 실패할 수 있어요.` → `기준 영상처럼 찍으세요(폴 전체와 전신이 들어오는 거리, 세로, 고정). 너무 가까우면 폴 전체가 안 들어와 분석에 실패할 수 있어요.`
    - `help.tsx:46` body → `기준 영상처럼 찍으세요(폴 전체와 전신이 들어오는 거리, 세로, 고정). 너무 가까우면 폴 전체가 안 들어와 분석에 실패할 수 있어요. 영상 길이는 3초 이상 90초 이내예요.`
    - `docs/ia.md:189` Condition 열 → `기준 영상처럼(폴 전체+전신이 들어오는 거리, 세로, 고정)`; `:191` → `밝은 실내 / 폴 전체와 전신이 들어오는 거리(약 4~5m)`; §3-3 표에 새 행 `| AC-VID-003-1X | Duration check | Fail | 90초 초과 | 영상이 너무 길어요. 동작이 담긴 부분만 잘라서 다시 선택해주세요. | 재업로드 |` 를 AC-VID-003-1L 바로 아래에.
    - `docs/reference-capture-guide.md:27` → `| 기본 시점 | **단일시점 1개** (기준 영상과 같은 시작 방향 — 고정 각도 없음) | 학생 입력과 동일 조건. \`captureViews=1\` 로 등록. |`; `:28` 의 `(정면 + 측면 권장)` → `(실증 범위 밖 — 단일시점)`; §2 `:47-48` "**정면성:** …" 두 줄 → `- **시작 방향:** 고정 각도(정면·45°)를 두지 않는다 — 시작 방향은 동작마다 다르다(2026-09-26 실물). 학생 안내는 "기준 영상처럼 찍으세요(폴 전체와 전신이 들어오는 거리, 세로, 고정)" 하나로 통일한다. 한 영상 안에서 카메라를 움직이지 않는다.` §6 운영자 절차는 무접촉(D-18).
    게이트(정제판 — RESEARCH 의 `45°` 는 `145°`, `정면` 은 3D 뷰어 주석에 오탐): `grep -rn '측면 45°\|2~3m\|정면 우선\|정면으로 다시\|정면으로 촬영\|정면을 기준' app/src docs` → **0건**. 시각 변경 0(스타일 무접촉).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && test "$(grep -rn '측면 45°\|2~3m\|정면 우선\|정면으로 다시\|정면으로 촬영\|정면을 기준' app/src docs | wc -l | tr -d ' ')" = "0" && cd app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn '측면 45°\|2~3m\|정면 우선\|정면으로 다시\|정면으로 촬영\|정면을 기준' app/src docs | wc -l` == 0.
    - `grep -c '기준 영상처럼 찍으세요(폴 전체와 전신이 들어오는 거리, 세로, 고정)' app/src/app/analysis/loading.tsx "app/src/app/(tabs)/analyze.tsx" app/src/app/help.tsx` → 2, 1, 1 respectively (loading :490 + :519; :546 is the "찍었는지" variant).
    - `grep -c 'AC-VID-003-1X' docs/ia.md` == 1.
    - `grep -c '시작 방향:' docs/reference-capture-guide.md` == 1 and §6 section text unchanged in `git diff`.
    - `cd app && npm run typecheck` exits 0; `git diff --stat` shows only the 5 files of this task.
  </acceptance_criteria>
  <done>14자리가 UI-SPEC 최종 문자열로 바뀌고 정제 게이트가 0건이다.</done>
</task>

<task type="auto">
  <name>Task 3: 마이 탭 "강사 코드 · 미입력" InfoRow 한 줄 + `profileCopy.ts`(D-12 표시만) + Figma 실측(D-22) + 시뮬레이터 실물 확인(스크린샷 2장 · UI 트리)</name>
  <files>app/src/app/(tabs)/profile.tsx, app/src/constants/profileCopy.ts, .planning/phases/38-supplier-link/38-02-simulator-profile.png, .planning/phases/38-supplier-link/38-02-simulator-tips.png</files>
  <read_first>
    - app/src/app/(tabs)/profile.tsx :28 (theme import) · :137-176 (계정 카드 블록) · :178-200 ("내 몸 정보") · :210-224 (`infoList` map) · :295-310 (`InfoRow`) · :348-353 (`guestHint`) · :399-416 (styles)
    - app/src/constants/authCopy.ts :1-12 (문구 상수 파일 규율 — 화면 리터럴 금지)
    - .planning/phases/38-supplier-link/38-UI-SPEC.md "### B 앱 마이 탭 — 강사 코드 한 줄" · "### B 앱 마이 탭 — `profileCopy.instructorCode`" 표 · "## Figma 참조 (필수)" B 행 · "Open for belle 4"
    - .planning/phases/38-supplier-link/38-CONTEXT.md D-12 · D-13 · D-22
    - app/CLAUDE.md (테마 토큰 하드코딩 금지)
    - .planning/phases/38-supplier-link/38-PATTERNS.md "### `app/src/app/(tabs)/profile.tsx` (강사 코드 한 줄)" 절 + Shared Patterns §10
    - /Users/kimtaesung/.claude/projects/-Users-kimtaesung-Dev-SunityMotion/memory/verify-ui-on-simulator-before-ota.md (typecheck 는 렌더 크래시를 못 잡는다 — 시뮬 확인이 필수인 이유)
  </read_first>
  <action>
    D-22 절차 먼저: `figma:figma-design-to-code` 스킬을 로드하고 Figma MCP `get_design_context`(fileKey `jrdI7kp245HkPfLB0nclsz`, nodeId `1:754` "마이" 탭)로 마이 탭이 Figma 에 본체 화면 없이 탭만 있음을 확인한다(UI-SPEC B: 신규 시각 0, 기존 `InfoRow` 재사용). Figma 원격 이미지 URL 은 코드에 넣지 않는다. 실측 결과(있는 것/없는 것)를 SUMMARY 관측 절에 `[확인]` 으로.
    `app/src/constants/profileCopy.ts`(신규, `authCopy.ts` 규율 미러, `as const`): `export const profileCopy = { instructorCode: { label: '강사 코드', value: '미입력', hint: '수업에서 받은 코드를 넣는 자리예요. 입력은 다음 업데이트에서 열려요.', a11y: '강사 코드 미입력. 입력은 다음 업데이트에서 열려요' } } as const;` 헤더 주석에 Phase 38 D-12(표시만 — 입력·귀속·크레딧 지급은 다음, D-13 결제 없음) 인용.
    `profile.tsx`: 계정 카드 블록(:137-176) **바로 아래**, "내 몸 정보" 위에 1행 카드 — `<View style={styles.infoList} accessible accessibilityLabel={profileCopy.instructorCode.a11y}>` 안에 `<InfoRow label={profileCopy.instructorCode.label} value={profileCopy.instructorCode.value} isLast />`, 카드 아래 `<Text style={styles.guestHint}>{profileCopy.instructorCode.hint}</Text>`. 기존 스타일만 재사용(신규 StyleSheet 항목 0, 색·간격 리터럴 0). 탭 동작 없음(Pressable 금지). 게스트·회원 모두 표시.
    **시뮬레이터 실물 확인(필수 — 메모리 verify-ui-on-simulator-before-ota: typecheck 는 렌더 크래시를 못 잡는다; 다른 플랜으로의 이월 금지):** (1) `xcrun simctl list devices booted` 에 부팅된 iPhone 이 없으면 `xcrun simctl boot "<설치된 최신 iPhone 시뮬레이터>"`. (2) `xcrun simctl listapps booted | grep -c com.sunity.aicoach` 가 0 이면 개발 빌드가 없다 → `cd app && npx expo run:ios`(첫 빌드, 오래 걸려도 한다); 있으면 `cd app && npx expo start --ios`. (3) 마이 탭으로 이동(`mcp__ios-simulator__ui_tap` 으로 탭 바의 "마이" 텍스트; 못 찾으면 `mcp__ios-simulator__ui_describe_all` 로 좌표를 찾는다) → `mcp__ios-simulator__ui_find_element`(텍스트 "강사 코드") 출력에 그 요소가 있어야 한다 → `xcrun simctl io booted screenshot .planning/phases/38-supplier-link/38-02-simulator-profile.png`. (4) 정정 문구 확인: 분석 탭의 안내 문단(`analyze.tsx:600-601`) 또는 도움말 화면(`help.tsx:46`) 중 화면에 뜨는 쪽으로 이동 → `ui_find_element`(텍스트 "기준 영상처럼 찍으세요") 출력에 있어야 한다 → `xcrun simctl io booted screenshot .planning/phases/38-supplier-link/38-02-simulator-tips.png`. (5) 두 스크린샷을 **눈으로 열어** 행/문구가 보이는지 본 뒤 SUMMARY 관측 절에 경로 + `ui_find_element` 출력 발췌를 `[확인]` 으로. 시뮬레이터·개발 빌드를 못 만들면 이 task 는 미완이다 — 가짜 `[확인]` 도, 다른 플랜 체크포인트로의 이월도 금지(원인·오류 원문을 적고 멈춘다). OTA(`eas update`)/EAS 빌드는 실행하지 않는다 — 실기기 반영은 phase 뒤 belle 결정(objective "선언된 이월").
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck && grep -c "profileCopy.instructorCode" "src/app/(tabs)/profile.tsx" | grep -qv '^0$' && test -s ../.planning/phases/38-supplier-link/38-02-simulator-profile.png && test -s ../.planning/phases/38-supplier-link/38-02-simulator-tips.png</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && npm run typecheck` exits 0.
    - `grep -c "profileCopy.instructorCode" "app/src/app/(tabs)/profile.tsx"` ≥ 3 (label·value·hint 또는 a11y).
    - `grep -c "'강사 코드'" app/src/constants/profileCopy.ts` == 1 and `grep -c "강사 코드" "app/src/app/(tabs)/profile.tsx"` == 0 (화면 리터럴 금지).
    - `git diff "app/src/app/(tabs)/profile.tsx"` adds no new `StyleSheet` entries and no hex color literal (`grep -c "#[0-9A-Fa-f]\{6\}"` on the diff additions == 0).
    - `.planning/phases/38-supplier-link/38-02-simulator-profile.png` and `38-02-simulator-tips.png` exist and are non-empty (`file` reports `PNG image data`).
    - `mcp__ios-simulator__ui_find_element` (or `ui_describe_all`) output pasted in SUMMARY contains the text `강사 코드` (profile row) and `기준 영상처럼 찍으세요` (corrected tip); no `[미확인]` for the simulator check.
    - SUMMARY records the Figma `get_design_context 1:754` result and states `실기기 미반영(OTA/EAS 미실행 — belle 결정 대기)`.
  </acceptance_criteria>
  <done>마이 탭에 강사 코드 한 줄이 기존 InfoRow 양식으로 보이고 문구는 상수 파일에만 있다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 갤러리/카메라 asset → `validate()` | OS 가 주는 메타(duration·fileSize)는 0/누락일 수 있다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-02-1 | DoS | `classifyDurationMs` / `validate()` | accept(부분 mitigate) | 90초 상한으로 duration 이 있는 픽은 앱에서 차단(75초=352초 분석급 입력 차단). **duration 부재 픽은 통과(fail-open)하고 수요자 `uploads/` 경로에 서버 측 길이 상한은 없다** — 범위 밖(38-01 validation 은 공급자 경로만). 위험 수용을 SUMMARY 에 같은 문장으로(리뷰 R15c) |
| T-38-02-2 | Information disclosure | `profileCopy` | accept | 표시 전용 문자열, 데이터 없음 |
| T-38-02-3 | Tampering | 문구 정정 | mitigate | grep 게이트 0건 + UI-SPEC 문자열 대조로 창작·누락 차단 |
| T-38-SC | Tampering | npm/pip installs | accept | 설치 0 (node --test 는 표준 모듈만) |
</threat_model>

<verification>
- `node --test app/src/lib/pickerFailure.test.ts` 통과 · `cd app && npm run typecheck` clean · grep 게이트 0건.
- `git log --oneline origin/main..HEAD` 비어 있음(플랜 끝 push).
</verification>

<success_criteria>
- 3~90초 즉시 검사가 있고 문구가 단일점에 있다.
- 낡은 촬영 문구가 앱·문서에서 사라졌다(게이트 0건).
- 마이 탭에 강사 코드 한 줄이 표시만 된다.
</success_criteria>

<output>
Create `.planning/phases/38-supplier-link/38-02-SUMMARY.md` when done (loading.tsx:546 변형은 "deliberate deviation" 으로 기록; 시뮬레이터 스크린샷 2장 경로 + `ui_find_element` 발췌 `[확인]`; "실기기 미반영(OTA/EAS 미실행 — belle 결정 대기)" 한 줄).
</output>
