---
phase: 38-supplier-link
plan: 03
type: execute
wave: 2
depends_on: ["38-01"]
files_modified:
  - docs/supplier-guide.md
  - app/src/constants/supplierCopy.ts
  - app/src/constants/supplierCopy.test.ts
  - app/src/lib/supplierFixtures.ts
  - app/src/lib/supplierRules.ts
  - app/src/lib/supplierRules.test.ts
  - app/src/lib/supplierForm.ts
  - app/src/lib/supplierForm.test.ts
autonomous: true
requirements: [REQ-38-8, REQ-38-4, REQ-38-6, REQ-38-3]
must_haves:
  truths:
    - "D-18: `docs/supplier-guide.md` 가 7항목(촬영 조건 = 정은지 영상 조건 · 동작 1개=영상 1개 · 선언 4 이유 · 등록 뒤 보는 것(재현성·실패 4형) · 권리/동의 · 내 코드 알리는 법 · 자주 틀리는 것) 을 쉬운 말로 담고 페이지 문구와 글자 단위로 같다"
    - "D-15/D-16: 가이드와 페이지 문구 어디에도 '45°'·'2~3m' 고정 각도/거리가 없고 '기준 영상처럼 찍으세요(폴 전체와 전신이 들어오는 거리, 세로, 고정)' 가 있다"
    - "D-09: 실패 4형 각각의 제목·본문·TIP 이 페이지 문구 단일점(`supplierCopy.row.fail.*`)에 있다"
    - "D-12/D-13: '내 코드' 카드 문구가 있고 크레딧·결제 문구는 없다"
    - "D-09 / 계약 3벌: `row.fail.<code>` 7개(no_human 제외, `too_large` 포함)의 `title + '. ' + body` 가 `analysis.ts` `REGISTRATION_ERROR_MESSAGE`(= models.py = contract.md §5)와 글자 단위로 같다 — 실패 문구 출처 셋이 하나로 묶인다"
    - "리뷰 R7/R11: 선언 helper·가이드 ③ 은 '등록 정보로 보관, 지금은 기본 비교 경로' 를 말하고, 자기 점수 높은 분기 문구는 '자기 영상 재분석 N점 — 추출·저장이 일관돼요' + 고지 '동작 정확도는 시험 영상으로 따로 봐요' 다(UI-SPEC 리뷰판 행과 글자 단위 동일)"
    - "리뷰 R14: 두 호스팅 후보가 공유하는 순수 규칙(`supplierRules.ts` 행 상태·자기 점수 분기·실패 문구·오류 매핑·업로드 결과 전이, `supplierForm.ts` 필수 개수·파일 검증·동의·요청 조립·프리필)이 이 플랜(호스팅 결정 전, 항상 실행)에 있고 공용 fixture 1벌로 node 테스트된다 — 38-10/38-11 은 import 만, 38-12 는 type-strip 빌드로 같은 파일을 ESM 으로 쓴다"
  artifacts:
    - path: "docs/supplier-guide.md"
      provides: "공급자 가이드 정본 7항목"
      min_lines: 60
    - path: "app/src/constants/supplierCopy.ts"
      provides: "supplierCopy — common·login·noAccess·home·row·form·dialog·guide 전 키"
      contains: "supplierCopy"
    - path: "app/src/lib/supplierRules.ts"
      provides: "normalizeRegistration · rowSubtitle · selfCheckLine · failCopy · hasDetail · sortNewestFirst · mapPresignFailure · uploadOutcomeNext · SELF_SCORE_OK_MIN · LEVEL_LABEL_KO (순수, firebase/react import 0)"
      contains: "SELF_SCORE_OK_MIN"
    - path: "app/src/lib/supplierForm.ts"
      provides: "remainingRequired · validateFile · consentAllRequired · buildRequest · parsePrefill (순수)"
      contains: "buildRequest"
    - path: "app/src/lib/supplierFixtures.ts"
      provides: "두 트랙·두 테스트가 공유하는 fixture 1벌 — raw 등록 doc 7상태 + 폼 상태 + 파일 메타 경계값"
      contains: "supplierFixtures"
  key_links:
    - from: "app/src/constants/supplierCopy.test.ts"
      to: "docs/supplier-guide.md"
      via: "fs 로 md 를 읽어 guide.* 문자열 verbatim 포함 단언"
      pattern: "supplier-guide.md"
    - from: "app/src/lib/supplierRules.test.ts"
      to: "app/src/lib/supplierFixtures.ts"
      via: "같은 fixture 로 행 상태·오류 매핑·전이 단언(38-12 rules.test.mjs 도 같은 fixture)"
      pattern: "supplierFixtures"
    - from: "app/src/constants/supplierCopy.test.ts"
      to: "app/src/types/analysis.ts"
      via: "fs 로 TS 텍스트를 읽어 row.fail 7코드(too_large 포함)의 title + '. ' + body 가 REGISTRATION_ERROR_MESSAGE 에 verbatim 존재(no_human 제외 — 의도된 예외)"
      pattern: "REGISTRATION_ERROR_MESSAGE"
---

<objective>
공급자 가이드 문서 정본(D-18, REQ-38-8)과 페이지 문구 단일점 `supplierCopy`(UI-SPEC §Copywriting 전 표 — 호스팅 결정과 무관한 **데이터**), 그리고 **리뷰 R14 로 옮겨 온 호스팅 무관 순수 규칙**(`supplierRules.ts`·`supplierForm.ts` + 공용 fixture `supplierFixtures.ts`)을 먼저 만든다. 페이지 플랜(38-10·38-11 Expo / 38-12 단일 HTML)은 이 파일들을 그대로 소비한다 — 문구·규칙을 화면 파일에 다시 쓰지 않는다. 38-04 호스팅 결정(wave 3)이 어느 쪽이든 이 플랜은 실행된다.

**리뷰 반영(2026-09-26):** R7(선언 문구 정직 — `form.sec2.name.helper`·`guide.s3.items`), R11(자기 점수 = 재분석 일관성 진단 — `row.self.ok` + 고지 `row.self.note`), R4(`row.status.expired`·`row.expired.*`), R9(`row.fail.too_large`), R14(순수 규칙 이동 + fixture 1벌). UI-SPEC §Copywriting 의 해당 행은 리뷰판으로 고쳐졌고 §Decisions 로그 23~26행이 그 기록이다 — 이 플랜은 그 행을 글자 단위로 옮긴다.

Purpose: 문구가 두 곳에 있으면 갈린다(메모리 llm-tips: 문장 결함은 소비처 표부터). 가이드 md 와 화면 문구가 글자 단위로 같아야 belle 이 한 곳만 본다.
Output: `docs/supplier-guide.md`, `app/src/constants/supplierCopy.ts`, `supplierCopy.test.ts`, `app/src/lib/{supplierFixtures,supplierRules,supplierRules.test,supplierForm,supplierForm.test}.ts`(전부 node --test).

wave 2(38-01 뒤)인 이유: `supplierCopy.test.ts` 가 38-01 이 쓴 `analysis.ts` `REGISTRATION_ERROR_MESSAGE` 를 텍스트로 대조한다(실패 문구 출처 셋 — models.py · analysis.ts · `row.fail` — 을 한 규칙으로 묶는다).

Phase 38 전제(38-01 머리말 1~5) 적용.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/38-supplier-link/38-CONTEXT.md
@.planning/phases/38-supplier-link/38-UI-SPEC.md
@.planning/phases/38-supplier-link/38-PATTERNS.md
@.planning/phases/38-supplier-link/38-01-SUMMARY.md
@design.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: `docs/supplier-guide.md` — UI-SPEC `guide.*` 표 7항목을 글자 단위로 옮긴 공급자 가이드 정본</name>
  <files>docs/supplier-guide.md</files>
  <read_first>
    - .planning/phases/38-supplier-link/38-UI-SPEC.md "**guide (A-6 = `docs/supplier-guide.md` 정본, D-18 7항목)**" 표 전체(리뷰판 — `guide.s3.items` 첫 항목 R7 문구 · `guide.s4.items` 넷째 항목 R11 고지 · `guide.s4.tip` 셋째 항목의 R6 "서 있는 자세인지는 사람이 확인해 주세요" 문장) + 표 아래 "D-18 ⑦ … `약 3m 안쪽`" 문장 + "## Decisions & Deviations Log" 23~26행
    - .planning/phases/38-supplier-link/38-CONTEXT.md D-16 · D-18 · "Specific Ideas"(실물 프레임 조건)
    - docs/reference-capture-guide.md :1-19 (헤더 인용문·§0 어법 — 구조만; §6 운영자 절차는 그대로 두고 링크만)
    - app/assets/motion-thumbs/ (ref-kip-up.jpg · ref-power-spin.jpg 존재 확인 — 예시 사진 2장)
    - .planning/phases/38-supplier-link/38-PATTERNS.md "### `docs/supplier-guide.md` (신규) · docs 4종 정정" 절(헤더 인용문·§0·표 어법 발췌 + 쉬운 말 톤) + File Classification `docs/supplier-guide.md` 행
  </read_first>
  <action>
    새 문서 구조: 제목 `# 촬영과 등록 가이드 (공급자용)` → 인용 헤더 2줄(Phase 38 D-18 산출물 · 공급자(강사·선수)용 쉬운 말 · 운영자 절차는 `docs/reference-capture-guide.md` §6 · 페이지 화면 A-6 과 글자 단위로 같다 — 바꾸면 `app/src/constants/supplierCopy.ts` `guide.*` 도 같이) → `guide.sub` 문장 → 섹션 7: `## 1. 어떻게 찍나요` … `## 7. 자주 틀리는 것`, 각 섹션 제목 = `guide.s{n}.h` 문자열, 본문 = `guide.s{n}.items` 를 `- ` 목록으로 **한 글자도 바꾸지 않고**. §1 끝에 예시 사진 2장(`![정은지 선수 기준 영상 프레임 — kip-up](../app/assets/motion-thumbs/ref-kip-up.jpg)`, `![… power-spin](../app/assets/motion-thumbs/ref-power-spin.jpg)`) + 캡션 `guide.s1.caption`. §4 안에 TIP 블록: `**등록이 안 되는 4가지**` + `guide.s4.tip` 4줄. §7 의 "너무 가까이" 항목은 UI-SPEC 대로 `약 3m 안쪽`(게이트 `2~3m` 회피). 이모지 0, 기술 용어(RTMW·DTW·conf) 0, '~요' 체.
    금지: D-18 7항목 밖 내용 창작, 나쁜 예 사진 생성(자산 없음), 수치 목표(재현성 문턱 숫자) 기재 — "100에 가까워야 정상이에요" 문장만(UI-SPEC 원문).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && test "$(grep -c '^## ' docs/supplier-guide.md)" = "7" && test "$(grep -c '2~3m\|45°' docs/supplier-guide.md)" = "0" && grep -q '기준 영상처럼\|정은지 선수 기준 영상' docs/supplier-guide.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## ' docs/supplier-guide.md` == 7 with headings exactly `어떻게 찍나요`·`영상 하나에 동작 하나`·`올릴 때 왜 4가지를 묻나요`·`올리면 무엇이 보이나요`·`권리와 동의`·`내 코드를 수강생에게 알려주는 법`·`자주 틀리는 것` (numbered).
    - `grep -c '2~3m\|45°\|측면' docs/supplier-guide.md` == 0; `grep -c '약 3m 안쪽' docs/supplier-guide.md` == 1.
    - `grep -c 'ref-kip-up.jpg\|ref-power-spin.jpg' docs/supplier-guide.md` == 2 and both asset files exist (`ls app/assets/motion-thumbs/ref-kip-up.jpg app/assets/motion-thumbs/ref-power-spin.jpg`).
    - `grep -c '등록이 안 되는 4가지' docs/supplier-guide.md` == 1 and the 4 tip lines follow.
    - `grep -c '등록 정보로 보관' docs/supplier-guide.md` ≥ 1 (R7 — s3 항목 셋이 같은 문장을 쓴다) and `grep -c '동작 정확도는 시험 영상으로 따로 봐요' docs/supplier-guide.md` == 1 (R11) and `grep -c '서 있는 자세인지는 사람이 확인해 주세요' docs/supplier-guide.md` == 1 (R6 — 바닥 규칙은 PROXY); `grep -c '채점 규칙(펴야 하는 관절, 실수 문장)이 같이 붙어요' docs/supplier-guide.md` == 0.
    - No emoji characters in the file (`grep -cP '[\x{1F300}-\x{1FAFF}]' docs/supplier-guide.md` == 0).
  </acceptance_criteria>
  <done>가이드 정본이 7항목·쉬운 말·예시 2장으로 존재한다.</done>
</task>

<task type="auto">
  <name>Task 2: `app/src/constants/supplierCopy.ts` — UI-SPEC §Copywriting 전 키(+UI-SPEC §Decisions ui-checker flags 반영) + md·analysis.ts 대조 node 테스트</name>
  <files>app/src/constants/supplierCopy.ts, app/src/constants/supplierCopy.test.ts</files>
  <read_first>
    - .planning/phases/38-supplier-link/38-UI-SPEC.md "## Copywriting Contract" 전체(common · login · noAccess · home · row · form · 검증 다이얼로그 · guide · B — 리뷰판: `row.status.expired` · `row.expired.title/body` · `row.self.ok` 새 문구 · `row.self.note` · `row.fail.too_large` · `form.sec2.name.helper` 새 문구) + "## Decisions & Deviations Log"(23~26행 = 리뷰 R7/R11/R4/R13 기록) + "## Open for belle"
    - app/src/constants/authCopy.ts (전체 — `as const` 중첩 객체 규율, 헤더 주석 어법)
    - app/src/lib/pickerFailure.test.ts :1-14 (node --test 실행법)
    - docs/supplier-guide.md (Task 1 산출물)
    - .planning/phases/38-supplier-link/38-CONTEXT.md D-08(동의 문안 §6 그대로) · D-09 · D-12 · D-13 · D-15
    - app/src/types/analysis.ts (38-01 산출 — `REGISTRATION_ERROR_MESSAGE` 7줄 · `ReferenceRegistrationErrorCode`; 조인 규칙은 38-01 Task 2 action)
    - .planning/phases/38-supplier-link/38-PATTERNS.md "### 공급자 페이지 — 후보 (1) Expo Router 라우트 `app/src/app/supplier/index.tsx` · `guide.tsx`" 절(문구 소비처 어법) + Shared Patterns §11(문구는 단일점 + exhaustive 게이트)
  </read_first>
  <action>
    `supplierCopy.ts`: `export const supplierCopy = { common, login, noAccess, home, row, form, dialog, guide } as const;` 헤더 주석에 "Phase 38 UI-SPEC §Copywriting 글자 단위 정본 · 화면 파일에 리터럴 금지 · Figma 문자열은 그대로(D-22) · guide.* 는 docs/supplier-guide.md 와 동일" 인용. 키 이름은 UI-SPEC 표의 점 표기를 중첩 객체로(`row.fail.no_human.title` → `row.fail.no_human.title`; `form.sec1.items` 는 배열; `row.tip` 배열; `dialog` = 검증 다이얼로그 표 5 kind `{title, lines:[2]}`; `guide.s1..s7` = `{h, items[]}` + `guide.title`·`guide.sub`·`guide.s1.caption`·`guide.s4.tipHead`·`guide.s4.tip[]`). 치환 자리는 `{score}` `{joints}` `{name}` `{email}` `{n}` `{durationSec}` `{sizeMB}` `{pct}` `{sha}` 그대로 두고 소비처가 `String.replace` 한다.
    UI-SPEC §Decisions "ui-checker flags (2026-09-26)" 표의 적용값을 **값으로** 반영(그 표가 §Copywriting 원문보다 우선, 주석에 `// ui-checker flag #n` 표시): `common.copy` → `코드 복사`; `common.cancel` → `올리기 취소`(A-5 취소 링크); `home.codePending` → `코드 준비 중 · 운영팀이 코드를 넣으면 여기에 보여요`; `home.upload` 는 `동작 올리기 >` 유지. `SELF_SCORE_OK_MIN` 같은 숫자 상수는 이 파일에 두지 않는다(38-10 `lib/supplierMotions.ts`).
    `supplierCopy.test.ts`(node --test, `node:test`/`node:assert` 만, `.ts` 확장자 import): (1) `docs/supplier-guide.md` 를 `fs.readFileSync` 로 읽어 `guide.s1..s7.h` 7개와 모든 `items`·`tip`·`sub`·`caption` 문자열이 md 텍스트에 verbatim 포함 (2) 전 문자열을 재귀 순회해 `45°`·`2~3m`·`측면` 0건, 이모지 0건 (3) `row.tip[0]` 과 `form.sec4.pill[0]` 에 D-15 문장 `기준 영상처럼 찍으세요(폴 전체와 전신이 들어오는 거리, 세로, 고정)` 포함 (4) `row.fail` 의 8 코드(`no_human`·`multiple_people`·`no_standing_start`·`low_confidence`·`too_short`·`too_long`·`too_large`·`server_error`) 각각 `title`·`body` 비공백 + `row.status.expired`·`row.expired.title`·`row.expired.body`·`row.self.note` 비공백(R4/R11) (5) `form.sec5` 필수 3 문안이 기획안 §6 원문과 동일: `초상·성명 사용 동의(앱 내 표시·재생)` · `영상 이용 허락 — 분석 기준 사용 · 프레임 추출·표시 · 썸네일` · `무음 영상 확인(음악·안무 라이선스 회피)` · `학습 사용 동의` (6) `common.copy === '코드 복사'`, `common.cancel === '올리기 취소'`, `home.codePending` 에 `운영팀` 포함 (7) 크레딧·결제 단어(`크레딧`·`결제`·`원`) 가 값 어디에도 없다(D-13) (8) **실패 문구 3원 일치(no_human 제외):** `app/src/types/analysis.ts` 를 `fs.readFileSync` 로 읽어(import 금지 — 타입 파일) `multiple_people`·`no_standing_start`·`low_confidence`·`too_short`·`too_long`·`too_large`·`server_error` 각각 `row.fail.<code>.title + '. ' + row.fail.<code>.body` 문자열이 verbatim 으로 들어 있다(= 38-01 조인 규칙; `{joints}` 자리는 치환 전 원문 그대로). (9) **R7/R11 정직 문구:** `form.sec2.name.helper` 와 `guide.s3.items[0]` 에 `등록 정보로 보관` 이 있고 `채점 규칙(펴야 하는 관절, 실수 문장)이 같이 붙어요` 가 값 어디에도 없다; `row.self.ok` 에 `추출·저장이 일관돼요` 가 있고 `기준으로 쓸 수 있어요` 가 값 어디에도 없다; `row.self.note === '동작 정확도는 시험 영상으로 따로 봐요.'`; `row.self.low` 에 `다시 찍어 주세요` 유지. `no_human` 은 D-09 예외(기존 `ERROR_MESSAGE.no_human` 재사용, Figma `1:479` 문구와 다름)라 대조하지 않고 그 사실을 테스트 주석에 적는다.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && node --test app/src/constants/supplierCopy.test.ts && cd app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `node --test app/src/constants/supplierCopy.test.ts` exits 0 (≥ 8 tests).
    - `grep -c "analysis.ts" app/src/constants/supplierCopy.test.ts` ≥ 1 and `grep -c "from '../types/analysis" app/src/constants/supplierCopy.test.ts` == 0 (텍스트 대조, import 아님).
    - `cd app && npm run typecheck` exits 0.
    - `grep -c "as const" app/src/constants/supplierCopy.ts` ≥ 1; `grep -c "코드 복사\|올리기 취소" app/src/constants/supplierCopy.ts` == 2.
    - `grep -c "no_human\|multiple_people\|no_standing_start\|low_confidence\|too_large" app/src/constants/supplierCopy.ts` ≥ 5; `grep -c "기준으로 쓸 수 있어요\|채점 규칙(펴야 하는 관절" app/src/constants/supplierCopy.ts` == 0.
    - `grep -c "크레딧\|결제" app/src/constants/supplierCopy.ts` == 0.
  </acceptance_criteria>
  <done>페이지 문구 단일점이 존재하고 가이드 md 와 글자 단위로 같음이 테스트로 잠긴다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 호스팅 무관 순수 규칙(리뷰 R14) — `supplierFixtures.ts`(공용 fixture 1벌) · `supplierRules.ts`(행 상태·자기 점수 분기·실패 문구·오류 매핑·업로드 전이) · `supplierForm.ts`(필수 개수·파일 검증·동의·요청 조립·프리필) + node 테스트 2개</name>
  <files>app/src/lib/supplierFixtures.ts, app/src/lib/supplierRules.ts, app/src/lib/supplierRules.test.ts, app/src/lib/supplierForm.ts, app/src/lib/supplierForm.test.ts</files>
  <behavior>
    - `normalizeRegistration('r1', raw)` → `supplierUid`·`name`·`athleteName`·`level` 없으면 null; 있으면 `SupplierMotion{motionId, name, athleteName, level, isActive, registrationStatus('registering' 기본), queuedReason|null, selfScore|null, selfCheckStatus|null, createdAt, videoS3Key|null, uploadExpiresAt|null}` — 공개 doc 필드만(R13: 동의·실패 상세·선언은 `normalizePrivate(raw)` → `SupplierMotionPrivate{registrationError|null, techniqueRefId, isCombo, isSplit, hasHold, standingStart}` 별도 함수)
    - `rowSubtitle(m)`: registering → `기본기 올린 영상 확인 중`(레벨 라벨 + 상태어, `supplierCopy.row.status.*` 치환) · processing · queued · **expired → `row.status.expired`**(R4) · active+selfCheck pending/queued → `새로 추가됨` · active+selfScore≥SELF_SCORE_OK_MIN → `재현성 97점` · active+selfScore<min → `재현성 61점 · 다시 찍어 주세요` · failed → `실패 · 눌러서 확인` — fixture 7상태 전부(`supplierFixtures.rawDocs`)
    - `selfCheckLine(m)` → `row.self.pending|queued|ok|low|failed` 의 문자열(`{score}` 치환, `Math.round`); `ok` 문구는 R11 판(`추출·저장이 일관돼요`); `selfCheckNote()` → `row.self.note`
    - `failCopy('low_confidence', ['왼쪽 발목','오른쪽 발목'])` → `{title, body}` 에 `{joints}` 가 ` · ` 로 이어져 치환; `failCopy('too_large')` 존재; 미지 코드 → `server_error` 문구; `expiredCopy()` → `row.expired.title/body`
    - `hasDetail(m)` → active/failed/expired 만 true; `sortNewestFirst` createdAt desc
    - `mapPresignFailure({status, code})`(R14 행동 테스트 축): `401` → `'sessionExpired'`, `403` → `'forbidden'`, `status 0`/네트워크 → `'offline'`, 그 외(5xx·4xx) → `'presignFail'`
    - `uploadOutcomeNext(outcome)`: `'aborted'` → `'step2'`(입력 유지·취소 링크), `'ok'` → `'home'`, `'failed'` → `'failPanel'`(재시도 = 새 presign 부터) — 두 트랙이 같은 전이표를 쓴다
    - `SELF_SCORE_OK_MIN === 90` 기본(`[ASSUMED RESEARCH A9]`; 38-04 T3 에서 belle 이 다른 숫자를 주면 그 커밋이 이 상수 하나만 고친다 — 다른 파일에 리터럴 90 금지, pass/fail 기준으로 쓰지 않는다)
    - `remainingRequired(step1State)` → 미완 필수 개수(체크 확인·동작 이름·선수 이름·레벨·선언 3·파일 = 최대 8), 전부 차면 0; `standingStart === 'no'` 면 `blocked: true`(개수와 별개)
    - `validateFile({ name, sizeBytes, durationSec, isCombo })` → `'format'`(확장자 ∉ mp4/mov) · `'tooLarge'`(>100MB) · `'tooShort'`(<5) · `'tooLong'`(>30, 콤보 >60) · `null`; 경계 `4.9→tooShort`, `5→null`, `30→null`, `30.1→tooLong`, `60+콤보→null`, `60.1+콤보→tooLong`; `durationSec == null` → 길이 검사 건너뜀(fail-open)
    - `consentAllRequired(consent)` → portrait·usage·silent 전부 true 일 때만 true(training 무관 — D-08)
    - `buildRequest(step1, consent, file)` → `ReferenceUploadUrlRequest` 정확한 형상(`techniqueRefId` = 사전 선택 시 그 motionId, 새 이름이면 null; `durationSec` null 허용; `isCombo` bool; `clipRange` 없음)
    - `parsePrefill(params)` → 38-10/38-12 가 보내는 `name/athleteName/level/isSplit('1'|'0')/hasHold/standingStart` 를 STEP 01 상태로
  </behavior>
  <read_first>
    - app/src/types/analysis.ts (38-01 산출 — `ReferenceRegistrationStatus`(expired 포함) · `ReferenceRegistrationErrorCode`(too_large 포함) · `ReferenceRegistrationPrivate` · `ReferenceUploadUrlRequest` · `SkillLevel`)
    - app/src/lib/referenceMotions.ts :60-80 (`normalize` · `LEVEL_ORDER` — 방어적 normalize 어법) · app/src/app/analysis/reference.tsx :21-25 (`TABS` 레벨 라벨 문자열)
    - app/src/app/(tabs)/analyze.tsx :204-236 (`validate` — 종류 반환 규율) · app/src/lib/pickerFailure.test.ts :1-14 (node --test 어법) · app/src/lib/api.ts :1-50 (`ApiError.status/code` 형상 — `mapPresignFailure` 입력)
    - app/src/constants/supplierCopy.ts (Task 2 산출 — `row.*` · `form.*` · `dialog`)
    - .planning/phases/38-supplier-link/38-UI-SPEC.md "### A-3 메인" 상태 표(리뷰판 — `expired` 행 포함) · "### A-5 업로드 진행 → 결과" · "### A-7" · "**form (A-4·A-5)**"
    - .planning/phases/38-supplier-link/38-PATTERNS.md "### 공급자 페이지 — 후보 (1) Expo Router 라우트 `app/src/app/supplier/index.tsx` · `guide.tsx`" 절(`normalize`·`validate` 발췌 + 플래너 메모) + "### 후보 (2) `web/supplier.html` (단일 정적 HTML)" 절(두 트랙이 같은 규칙을 쓰는 이유) + Shared Patterns §10·§11 + 관측 7
    - .planning/phases/38-supplier-link/38-REVIEWS.md "### R14" · "### R11" · "### R4"
  </read_first>
  <action>
    RED 먼저(두 테스트 파일, `node --test`, `.ts` import 명시, `firebase`/`react`/`expo` import 0) → 구현.
    `supplierFixtures.ts`(공용 fixture — **두 트랙·모든 규칙 테스트가 이 한 벌만 쓴다**, 38-12 `rules.test.mjs` 도 type-strip 된 같은 파일): `export const supplierFixtures = { rawDocs: { registering, queued, processing, activePending, activeOk(selfScore 97), activeLow(selfScore 61), failedLowConfidence(공개 doc; 실패 상세는 privateDocs 에), expired }, privateDocs: { failedLowConfidence: { registrationError: { code:'low_confidence', message, joints:['왼쪽 발목','오른쪽 발목'] }, isCombo:false, isSplit:true, hasHold:false, standingStart:true, techniqueRefId:null } }, apiFailures: [ {status:401,code:'unauthorized'}, {status:403,code:'forbidden'}, {status:0,code:null}, {status:500,code:'server_error'}, {status:400,code:'too_long'} ], files: [ {name:'a.mp4', sizeBytes: 10_000_000, durationSec: 4.9, isCombo:false} … 경계 8건 + `avi` + 101MB + durationSec null ], step1: { complete, missingTwo, standingNo }, prefillParams } as const` — 값은 UI-SPEC 상태 표와 계약 타입에서 그대로.
    `supplierRules.ts`(순수 — `firebase`·`react`·`expo` import 금지; 값 import 는 `supplierCopy` 하나, 타입은 전부 `import type`(38-12 의 type-strip → ESM 변환이 값 import 만 남기고 specifier 를 `./copy.js` 로 바꾸는 전제)): `SupplierMotion`·`SupplierMotionPrivate` 타입, `normalizeRegistration`, `normalizePrivate`, `LEVEL_LABEL_KO = {basic:'기본기', intermediate:'중급', advanced:'고급'}`, `export const SELF_SCORE_OK_MIN = 90;`(주석 `[ASSUMED RESEARCH A9 · 38-04 결정 절 selfScoreMin 이 바꾼다] 문구 분기점일 뿐, 목표 숫자 금지`), `rowSubtitle`, `selfCheckLine`, `selfCheckNote`, `failCopy(code, joints?)`, `expiredCopy()`, `hasDetail`, `sortNewestFirst`, `mapPresignFailure({status, code})`, `uploadOutcomeNext(outcome)`. 모든 문자열은 `supplierCopy` 참조 + `String.replace`(`{score}`·`{joints}`).
    `supplierForm.ts`(순수): `Step1State` 타입(`checkConfirmed`, `nameChoice: {kind:'dict', motionId, name} | {kind:'new', name} | null`, `athleteName`, `level: SkillLevel|null`, `isCombo`, `isSplit/hasHold/standingStart: 'yes'|'no'|null`, `file: {name, sizeBytes, durationSec|null, blob|uri, format}|null`), `ConsentState`, 위 behavior 함수들. 상수는 `models.py` 와 같은 값(5·30·60·100MB·이름 30자) — 주석에 lockstep.
    `supplierRules.test.ts` · `supplierForm.test.ts`: fixture 만 입력으로(리터럴 doc 을 테스트 안에 다시 쓰지 않는다). 각각 ≥ 10 / ≥ 12 테스트, 위 behavior 전부.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && node --test app/src/lib/supplierRules.test.ts app/src/lib/supplierForm.test.ts && cd app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `node --test app/src/lib/supplierRules.test.ts app/src/lib/supplierForm.test.ts` exits 0 (≥ 10 + ≥ 12 tests).
    - `cd app && npm run typecheck` exits 0.
    - `grep -c "from 'firebase\|from \"firebase\|from 'react\|from 'expo" app/src/lib/supplierRules.ts app/src/lib/supplierForm.ts app/src/lib/supplierFixtures.ts` → 0 for all; `grep -c "^import type\|import type {" app/src/lib/supplierRules.ts` ≥ 1 and `grep -c "^import { supplierCopy }" app/src/lib/supplierRules.ts` == 1 (값 import 는 supplierCopy 하나).
    - `grep -c "export const SELF_SCORE_OK_MIN = 90" app/src/lib/supplierRules.ts` == 1 with `ASSUMED` in the adjacent comment; `grep -rc "< 90\|<90\|>= 90\|>=90" app/src/lib/supplierRules.ts app/src/lib/supplierForm.ts` → 0.
    - `grep -c "export function mapPresignFailure\|export function uploadOutcomeNext\|export function normalizePrivate\|export function expiredCopy" app/src/lib/supplierRules.ts` == 4; `grep -c "supplierFixtures" app/src/lib/supplierRules.test.ts app/src/lib/supplierForm.test.ts` → each ≥ 1.
    - `grep -c "'expired'" app/src/lib/supplierRules.ts` ≥ 1 and `grep -c "too_large" app/src/lib/supplierFixtures.ts app/src/lib/supplierRules.test.ts` → ≥ 1 total.
  </acceptance_criteria>
  <done>두 호스팅 후보가 공유할 순수 규칙·폼 규칙이 한 fixture 벌로 테스트돼 존재하고, 화면 플랜은 import 만 한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 문구 상수 → 화면 | 데이터 전용, 입력 없음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-03-1 | Information disclosure | `docs/supplier-guide.md` | accept | 개인정보 0(정은지 이름은 공개 선수명, 기준 프레임은 이미 앱 자산) |
| T-38-03-2 | Tampering | `supplierCopy` 치환 자리 `{joints}` 등 | mitigate | 소비처(38-10/12)가 `String.replace` 만 쓰고 HTML/JSX 로 해석하지 않는다(단일 HTML 은 `textContent`) — 플랜 38-12 위협 표에 승계 |
| T-38-03-3 | Tampering | `supplierForm.validateFile` / `buildRequest` | mitigate | 클라이언트 검사(형식·100MB·길이·필수·동의)는 UX 용; 서버 `validate_reference_upload_request`(38-01) + 파이프라인 `head_object`/probe(38-07) 가 진짜 상한 — 규칙은 lockstep 주석으로 같은 값 |
| T-38-03-4 | Information disclosure | `normalizePrivate` | mitigate | 비공개 doc 은 규칙상 본인만 읽힌다(38-06 T3) — 규칙 함수는 받은 데이터만 정규화, 조회는 화면 훅(38-10/38-12)이 상세에서만 |
| T-38-SC | Tampering | npm/pip installs | accept | 설치 0 |
</threat_model>

<verification>
- `node --test app/src/constants/supplierCopy.test.ts app/src/lib/supplierRules.test.ts app/src/lib/supplierForm.test.ts` 통과 · `npm run typecheck` clean · md 게이트 통과 · 플랜 끝 push.
</verification>

<success_criteria>
- 가이드 정본 7항목이 있고 페이지 문구가 한 파일에 있으며 둘이 글자 단위로 같다.
- 실패 문구 7개가 `analysis.ts` `REGISTRATION_ERROR_MESSAGE` 와 조인 규칙으로 같음이 테스트로 잠긴다(no_human 예외 명시).
- 순수 규칙·폼 규칙이 공용 fixture 로 테스트돼 있고 어느 호스팅 후보도 그것을 import 만 한다(R14).
</success_criteria>

<output>
Create `.planning/phases/38-supplier-link/38-03-SUMMARY.md` when done.
</output>
