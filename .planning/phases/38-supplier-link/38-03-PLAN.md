---
phase: 38-supplier-link
plan: 03
type: execute
wave: 2
depends_on: ["38-01"]
files_modified:
  - docs/supplier-guide.md
  - app/src/constants/supplierCopy.ts
  - app/src/constants/supplierCopy.test.ts
autonomous: true
requirements: [REQ-38-8, REQ-38-4, REQ-38-6]
must_haves:
  truths:
    - "D-18: `docs/supplier-guide.md` 가 7항목(촬영 조건 = 정은지 영상 조건 · 동작 1개=영상 1개 · 선언 4 이유 · 등록 뒤 보는 것(재현성·실패 4형) · 권리/동의 · 내 코드 알리는 법 · 자주 틀리는 것) 을 쉬운 말로 담고 페이지 문구와 글자 단위로 같다"
    - "D-15/D-16: 가이드와 페이지 문구 어디에도 '45°'·'2~3m' 고정 각도/거리가 없고 '기준 영상처럼 찍으세요(폴 전체와 전신이 들어오는 거리, 세로, 고정)' 가 있다"
    - "D-09: 실패 4형 각각의 제목·본문·TIP 이 페이지 문구 단일점(`supplierCopy.row.fail.*`)에 있다"
    - "D-12/D-13: '내 코드' 카드 문구가 있고 크레딧·결제 문구는 없다"
    - "D-09 / 계약 3벌: `row.fail.<code>` 6개(no_human 제외)의 `title + '. ' + body` 가 `analysis.ts` `REGISTRATION_ERROR_MESSAGE`(= models.py = contract.md §5)와 글자 단위로 같다 — 실패 문구 출처 셋이 하나로 묶인다"
  artifacts:
    - path: "docs/supplier-guide.md"
      provides: "공급자 가이드 정본 7항목"
      min_lines: 60
    - path: "app/src/constants/supplierCopy.ts"
      provides: "supplierCopy — common·login·noAccess·home·row·form·dialog·guide 전 키"
      contains: "supplierCopy"
  key_links:
    - from: "app/src/constants/supplierCopy.test.ts"
      to: "docs/supplier-guide.md"
      via: "fs 로 md 를 읽어 guide.* 문자열 verbatim 포함 단언"
      pattern: "supplier-guide.md"
    - from: "app/src/constants/supplierCopy.test.ts"
      to: "app/src/types/analysis.ts"
      via: "fs 로 TS 텍스트를 읽어 row.fail 6코드의 title + '. ' + body 가 REGISTRATION_ERROR_MESSAGE 에 verbatim 존재(no_human 제외 — 의도된 예외)"
      pattern: "REGISTRATION_ERROR_MESSAGE"
---

<objective>
공급자 가이드 문서 정본(D-18, REQ-38-8)과 페이지 문구 단일점 `supplierCopy`(UI-SPEC §Copywriting 전 표 — 호스팅 결정과 무관한 **데이터**)를 먼저 만든다. 페이지 플랜(38-10·38-11 Expo / 38-12 단일 HTML)은 이 파일을 그대로 소비한다 — 문구를 화면 파일에 다시 쓰지 않는다.

Purpose: 문구가 두 곳에 있으면 갈린다(메모리 llm-tips: 문장 결함은 소비처 표부터). 가이드 md 와 화면 문구가 글자 단위로 같아야 belle 이 한 곳만 본다.
Output: `docs/supplier-guide.md`, `app/src/constants/supplierCopy.ts`, `supplierCopy.test.ts`(node --test).

wave 2(38-01 뒤)인 이유: `supplierCopy.test.ts` 가 38-01 이 쓴 `analysis.ts` `REGISTRATION_ERROR_MESSAGE` 를 텍스트로 대조한다(실패 문구 출처 셋 — models.py · analysis.ts · `row.fail` — 을 한 규칙으로 묶는다).

Phase 38 전제(38-01 머리말 1~5) 적용.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/38-supplier-link/38-CONTEXT.md
@.planning/phases/38-supplier-link/38-UI-SPEC.md
@.planning/phases/38-supplier-link/38-PATTERNS.md
@.planning/phases/38-supplier-link/38-01-SUMMARY.md
@design.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: `docs/supplier-guide.md` — UI-SPEC `guide.*` 표 7항목을 글자 단위로 옮긴 공급자 가이드 정본</name>
  <files>docs/supplier-guide.md</files>
  <read_first>
    - .planning/phases/38-supplier-link/38-UI-SPEC.md "**guide (A-6 = `docs/supplier-guide.md` 정본, D-18 7항목)**" 표 전체 + 표 아래 "D-18 ⑦ … `약 3m 안쪽`" 문장
    - .planning/phases/38-supplier-link/38-CONTEXT.md D-16 · D-18 · "Specific Ideas"(실물 프레임 조건)
    - docs/reference-capture-guide.md :1-19 (헤더 인용문·§0 어법 — 구조만; §6 운영자 절차는 그대로 두고 링크만)
    - app/assets/motion-thumbs/ (ref-kip-up.jpg · ref-power-spin.jpg 존재 확인 — 예시 사진 2장)
    - .planning/phases/38-supplier-link/38-PATTERNS.md "### `docs/supplier-guide.md` (신규) · docs 4종 정정" 절(헤더 인용문·§0·표 어법 발췌 + 쉬운 말 톤) + File Classification `docs/supplier-guide.md` 행
  </read_first>
  <action>
    새 문서 구조: 제목 `# 촬영과 등록 가이드 (공급자용)` → 인용 헤더 2줄(Phase 38 D-18 산출물 · 공급자(강사·선수)용 쉬운 말 · 운영자 절차는 `docs/reference-capture-guide.md` §6 · 페이지 화면 A-6 과 글자 단위로 같다 — 바꾸면 `app/src/constants/supplierCopy.ts` `guide.*` 도 같이) → `guide.sub` 문장 → 섹션 7: `## 1. 어떻게 찍나요` … `## 7. 자주 틀리는 것`, 각 섹션 제목 = `guide.s{n}.h` 문자열, 본문 = `guide.s{n}.items` 를 `- ` 목록으로 **한 글자도 바꾸지 않고**. §1 끝에 예시 사진 2장(`![정은지 선수 기준 영상 프레임 — kip-up](../app/assets/motion-thumbs/ref-kip-up.jpg)`, `![… power-spin](../app/assets/motion-thumbs/ref-power-spin.jpg)`) + 캡션 `guide.s1.caption`. §4 안에 TIP 블록: `**등록이 안 되는 4가지**` + `guide.s4.tip` 4줄. §7 의 "너무 가까이" 항목은 UI-SPEC 대로 `약 3m 안쪽`(게이트 `2~3m` 회피). 이모지 0, 기술 용어(RTMW·DTW·conf) 0, '~요' 체.
    금지: D-18 7항목 밖 내용 창작, 나쁜 예 사진 생성(자산 없음), 수치 목표(재현성 문턱 숫자) 기재 — "100에 가까워야 정상이에요" 문장만(UI-SPEC 원문).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && test "$(grep -c '^## ' docs/supplier-guide.md)" = "7" && test "$(grep -c '2~3m\|45°' docs/supplier-guide.md)" = "0" && grep -q '기준 영상처럼\|정은지 선수 기준 영상' docs/supplier-guide.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## ' docs/supplier-guide.md` == 7 with headings exactly `어떻게 찍나요`·`영상 하나에 동작 하나`·`올릴 때 왜 4가지를 묻나요`·`올리면 무엇이 보이나요`·`권리와 동의`·`내 코드를 수강생에게 알려주는 법`·`자주 틀리는 것` (numbered).
    - `grep -c '2~3m\|45°\|측면' docs/supplier-guide.md` == 0; `grep -c '약 3m 안쪽' docs/supplier-guide.md` == 1.
    - `grep -c 'ref-kip-up.jpg\|ref-power-spin.jpg' docs/supplier-guide.md` == 2 and both asset files exist (`ls app/assets/motion-thumbs/ref-kip-up.jpg app/assets/motion-thumbs/ref-power-spin.jpg`).
    - `grep -c '등록이 안 되는 4가지' docs/supplier-guide.md` == 1 and the 4 tip lines follow.
    - No emoji characters in the file (`grep -cP '[\x{1F300}-\x{1FAFF}]' docs/supplier-guide.md` == 0).
  </acceptance_criteria>
  <done>가이드 정본이 7항목·쉬운 말·예시 2장으로 존재한다.</done>
</task>

<task type="auto">
  <name>Task 2: `app/src/constants/supplierCopy.ts` — UI-SPEC §Copywriting 전 키(+UI-SPEC §Decisions ui-checker flags 반영) + md·analysis.ts 대조 node 테스트</name>
  <files>app/src/constants/supplierCopy.ts, app/src/constants/supplierCopy.test.ts</files>
  <read_first>
    - .planning/phases/38-supplier-link/38-UI-SPEC.md "## Copywriting Contract" 전체(common · login · noAccess · home · row · form · 검증 다이얼로그 · guide · B) + "## Decisions & Deviations Log" + "## Open for belle"
    - app/src/constants/authCopy.ts (전체 — `as const` 중첩 객체 규율, 헤더 주석 어법)
    - app/src/lib/pickerFailure.test.ts :1-14 (node --test 실행법)
    - docs/supplier-guide.md (Task 1 산출물)
    - .planning/phases/38-supplier-link/38-CONTEXT.md D-08(동의 문안 §6 그대로) · D-09 · D-12 · D-13 · D-15
    - app/src/types/analysis.ts (38-01 산출 — `REGISTRATION_ERROR_MESSAGE` 7줄 · `ReferenceRegistrationErrorCode`; 조인 규칙은 38-01 Task 2 action)
    - .planning/phases/38-supplier-link/38-PATTERNS.md "### 공급자 페이지 — 후보 (1) Expo Router 라우트 `app/src/app/supplier/index.tsx` · `guide.tsx`" 절(문구 소비처 어법) + Shared Patterns §11(문구는 단일점 + exhaustive 게이트)
  </read_first>
  <action>
    `supplierCopy.ts`: `export const supplierCopy = { common, login, noAccess, home, row, form, dialog, guide } as const;` 헤더 주석에 "Phase 38 UI-SPEC §Copywriting 글자 단위 정본 · 화면 파일에 리터럴 금지 · Figma 문자열은 그대로(D-22) · guide.* 는 docs/supplier-guide.md 와 동일" 인용. 키 이름은 UI-SPEC 표의 점 표기를 중첩 객체로(`row.fail.no_human.title` → `row.fail.no_human.title`; `form.sec1.items` 는 배열; `row.tip` 배열; `dialog` = 검증 다이얼로그 표 5 kind `{title, lines:[2]}`; `guide.s1..s7` = `{h, items[]}` + `guide.title`·`guide.sub`·`guide.s1.caption`·`guide.s4.tipHead`·`guide.s4.tip[]`). 치환 자리는 `{score}` `{joints}` `{name}` `{email}` `{n}` `{durationSec}` `{sizeMB}` `{pct}` `{sha}` 그대로 두고 소비처가 `String.replace` 한다.
    UI-SPEC §Decisions "ui-checker flags (2026-09-26)" 표의 적용값을 **값으로** 반영(그 표가 §Copywriting 원문보다 우선, 주석에 `// ui-checker flag #n` 표시): `common.copy` → `코드 복사`; `common.cancel` → `올리기 취소`(A-5 취소 링크); `home.codePending` → `코드 준비 중 · 운영팀이 코드를 넣으면 여기에 보여요`; `home.upload` 는 `동작 올리기 >` 유지. `SELF_SCORE_OK_MIN` 같은 숫자 상수는 이 파일에 두지 않는다(38-10 `lib/supplierMotions.ts`).
    `supplierCopy.test.ts`(node --test, `node:test`/`node:assert` 만, `.ts` 확장자 import): (1) `docs/supplier-guide.md` 를 `fs.readFileSync` 로 읽어 `guide.s1..s7.h` 7개와 모든 `items`·`tip`·`sub`·`caption` 문자열이 md 텍스트에 verbatim 포함 (2) 전 문자열을 재귀 순회해 `45°`·`2~3m`·`측면` 0건, 이모지 0건 (3) `row.tip[0]` 과 `form.sec4.pill[0]` 에 D-15 문장 `기준 영상처럼 찍으세요(폴 전체와 전신이 들어오는 거리, 세로, 고정)` 포함 (4) `row.fail` 의 7 코드(`no_human`·`multiple_people`·`no_standing_start`·`low_confidence`·`too_short`·`too_long`·`server_error`) 각각 `title`·`body` 비공백 (5) `form.sec5` 필수 3 문안이 기획안 §6 원문과 동일: `초상·성명 사용 동의(앱 내 표시·재생)` · `영상 이용 허락 — 분석 기준 사용 · 프레임 추출·표시 · 썸네일` · `무음 영상 확인(음악·안무 라이선스 회피)` · `학습 사용 동의` (6) `common.copy === '코드 복사'`, `common.cancel === '올리기 취소'`, `home.codePending` 에 `운영팀` 포함 (7) 크레딧·결제 단어(`크레딧`·`결제`·`원`) 가 값 어디에도 없다(D-13) (8) **실패 문구 3원 일치(no_human 제외):** `app/src/types/analysis.ts` 를 `fs.readFileSync` 로 읽어(import 금지 — 타입 파일) `multiple_people`·`no_standing_start`·`low_confidence`·`too_short`·`too_long`·`server_error` 각각 `row.fail.<code>.title + '. ' + row.fail.<code>.body` 문자열이 verbatim 으로 들어 있다(= 38-01 조인 규칙; `{joints}` 자리는 치환 전 원문 그대로). `no_human` 은 D-09 예외(기존 `ERROR_MESSAGE.no_human` 재사용, Figma `1:479` 문구와 다름)라 대조하지 않고 그 사실을 테스트 주석에 적는다.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && node --test app/src/constants/supplierCopy.test.ts && cd app && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `node --test app/src/constants/supplierCopy.test.ts` exits 0 (≥ 8 tests).
    - `grep -c "analysis.ts" app/src/constants/supplierCopy.test.ts` ≥ 1 and `grep -c "from '../types/analysis" app/src/constants/supplierCopy.test.ts` == 0 (텍스트 대조, import 아님).
    - `cd app && npm run typecheck` exits 0.
    - `grep -c "as const" app/src/constants/supplierCopy.ts` ≥ 1; `grep -c "코드 복사\|올리기 취소" app/src/constants/supplierCopy.ts` == 2.
    - `grep -c "no_human\|multiple_people\|no_standing_start\|low_confidence" app/src/constants/supplierCopy.ts` ≥ 4.
    - `grep -c "크레딧\|결제" app/src/constants/supplierCopy.ts` == 0.
  </acceptance_criteria>
  <done>페이지 문구 단일점이 존재하고 가이드 md 와 글자 단위로 같음이 테스트로 잠긴다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 문구 상수 → 화면 | 데이터 전용, 입력 없음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-03-1 | Information disclosure | `docs/supplier-guide.md` | accept | 개인정보 0(정은지 이름은 공개 선수명, 기준 프레임은 이미 앱 자산) |
| T-38-03-2 | Tampering | `supplierCopy` 치환 자리 `{joints}` 등 | mitigate | 소비처(38-10/12)가 `String.replace` 만 쓰고 HTML/JSX 로 해석하지 않는다(단일 HTML 은 `textContent`) — 플랜 38-12 위협 표에 승계 |
| T-38-SC | Tampering | npm/pip installs | accept | 설치 0 |
</threat_model>

<verification>
- `node --test app/src/constants/supplierCopy.test.ts` 통과 · `npm run typecheck` clean · md 게이트 통과 · 플랜 끝 push.
</verification>

<success_criteria>
- 가이드 정본 7항목이 있고 페이지 문구가 한 파일에 있으며 둘이 글자 단위로 같다.
- 실패 문구 6개가 `analysis.ts` `REGISTRATION_ERROR_MESSAGE` 와 조인 규칙으로 같음이 테스트로 잠긴다(no_human 예외 명시).
</success_criteria>

<output>
Create `.planning/phases/38-supplier-link/38-03-SUMMARY.md` when done.
</output>
