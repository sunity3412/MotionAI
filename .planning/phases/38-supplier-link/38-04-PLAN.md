---
phase: 38-supplier-link
plan: 04
type: execute
wave: 3
depends_on: ["38-01", "38-03"]
# 리뷰 실행전략 #3 (2026-09-26): wave 1 → 3 — 사람 체크포인트가 wave 1·2 의 순수 백엔드 작업을 막지 않게. 38-03 의 supplierRules.ts(SELF_SCORE_OK_MIN) 를 T3 결정이 고칠 수 있어 38-03 뒤.
files_modified:
  - app/package.json
  - app/package-lock.json
  - app/src/lib/socialAuth.web.ts
  - app/src/lib/firebase.web.ts
  - app/src/lib/supplierRules.ts
  - .planning/phases/38-supplier-link/38-04-probe/index.html
  - .planning/phases/38-supplier-link/38-04-probe/probe.js
  - .planning/phases/38-supplier-link/38-04-probe/README.md
  - .planning/phases/38-supplier-link/38-04-MEASUREMENT.md
  - .planning/phases/38-supplier-link/38-13-PLAN.md
  - .planning/phases/38-supplier-link/38-10-PLAN.md
  - .planning/phases/38-supplier-link/38-11-PLAN.md
  - .planning/phases/38-supplier-link/38-12-PLAN.md
  - .planning/phases/38-supplier-link/38-VALIDATION.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [REQ-38-3, REQ-38-5]
must_haves:
  truths:
    - "리뷰 R14: 호스팅 후보 (1) Expo Router web export 와 후보 (2) 정적 단일 페이지(작은 probe) 가 **같은 4가지 실험**(Google 팝업 로그인 · .mov 파일 선택 · presigned PUT · onSnapshot)으로 시뮬레이터 Mobile Safari 에서 각각 잰 값으로 기록된다 — 전체 앱 export 의 성패만으로 어느 후보의 가능/불가능을 추론하지 않는다 (D-01: 폰 브라우저에서 여는 같은 앱의 공급자 모드)"
    - "리뷰 R15d: 측정에 쓰는 계정은 belle 이 지정한 **테스트용 Google 계정**(belle 본계정 아님)이고, PUT 뒤 `uploads/{uid}/` 의 uid 가 그 계정의 로그인 uid 와 같음을 `aws s3 ls` 로 대조한다(익명 uid 라는 가정 폐기)"
    - "D-03: 웹 로그인은 같은 Firebase 프로젝트의 Google 팝업(`signInWithPopup`)이며 익명은 화이트리스트 재료로 쓰지 않는다"
    - "belle 이 채워진 결정표(측정값 · 두 후보의 남은 일 · 배포 경로 · 비용)를 보고 후보를 고른다 — 플래너·실행자가 대신 고르지 않는다"
    - "결정 뒤 미선택 트랙은 **파일로** 건너뛰어진다: 미선택 PLAN 에 `skipped: true`, 미선택 SUMMARY 에 `status: skipped-by-decision`, 38-13 `depends_on` 이 선택 트랙의 마지막 플랜(option-1 = 38-11, option-2 = 38-12)을 가리킨다 — execute-phase 는 SUMMARY 가 있는 플랜을 건너뛰므로 wave 4·5·6 에서 실행자가 판단할 것이 없다"
  artifacts:
    - path: ".planning/phases/38-supplier-link/38-04-MEASUREMENT.md"
      provides: "측정 로그 + 결정표(두 후보 × 4가지 같은 실험)"
      min_lines: 40
    - path: ".planning/phases/38-supplier-link/38-04-probe/probe.js"
      provides: "후보 (2) 최소 probe — Firebase 12.13.0 ESM 팝업 로그인 · file input · POST /upload-url → XHR PUT · onSnapshot (측정 전용, 페이지 구현 아님)"
      contains: "signInWithPopup"
    - path: "app/src/lib/socialAuth.web.ts"
      provides: "웹 Google 팝업 로그인(signInWithGoogle) — 네이티브 socialAuth.ts 와 같은 export 형상"
      contains: "signInWithPopup"
  key_links:
    - from: "app/src/lib/socialAuth.web.ts"
      to: "firebase/auth"
      via: "signInWithPopup / linkWithPopup"
      pattern: "signInWithPopup"
---

<objective>
페이지 호스팅은 **미결**이다(CONTEXT Claude's Discretion · RESEARCH §H "권장 없음 — 이걸 재야 답이 나온다"). 이 플랜은 두 후보를 **같은 작은 실험으로 잰다**(리뷰 R14) — 후보 (1) Expo Router 공급자 라우트 web export: 의존성 설치(승인 task) → export → 필요 시 `firebase.web.ts`/`socialAuth.web.ts` 분기; 후보 (2) 정적 단일 페이지: 화면 구현이 아니라 **probe 한 장**(`38-04-probe/`, Firebase ESM CDN + file input + 기존 `POST /upload-url` PUT + onSnapshot) — 둘 다 시뮬레이터 Mobile Safari 에서 4가지(Google 팝업 로그인 · `.mov` 선택 · presigned PUT · onSnapshot)를 belle 이 ○/× 로 잰다. 그리고 채워진 결정표로 `checkpoint:decision` 에서 belle 이 고른다. 전체 앱 export 의 성패만으로 "단일 HTML 은 되고/안 되고" 를 추론하지 않는다.

**wave 3(리뷰 실행전략 #3):** 사람 체크포인트 2개가 있는 이 플랜이 wave 1 을 막지 않게 wave 3 으로 옮겼다(38-01·38-03 뒤). 38-10/38-12(wave 4) 는 그대로 이 결정을 기다린다.

**이 플랜은 페이지를 구현하지 않는다.** 결정 뒤에 정확히 하나만 실행된다: 후보 (1) → 38-10 + 38-11 · 후보 (2) → 38-12. 그 "하나만" 은 Task 3 의 스킵 절차 (a)~(e) 가 파일로 강제한다(미선택 SUMMARY `status: skipped-by-decision` · 미선택 PLAN `skipped: true` · 38-13 `depends_on` 재작성 · VALIDATION 행 표시 · 한 커밋).

Purpose: 권장은 재고 나서(메모리 recommend-only-after-measuring). export 는 `react-native-web` 부재로 1.4초 만에 멈췄고(RESEARCH Q8 [확인]) 그 뒤는 전부 [미확인]이다.
Output: 측정 로그 `38-04-MEASUREMENT.md`(관측 절 = 잰 값, 진단 절 = 남은 일 추정), `socialAuth.web.ts`(+ 필요 시 `firebase.web.ts`), lockfile 변경.

Phase 38 전제(38-01 머리말) 적용. 추가 금지: `slopcheck install` 절대 금지(RESEARCH Q8 사고 — 홈 디렉터리에 설치됨), `npm` 을 `app/` 밖에서 실행 금지, `npx serve` 등 미감사 패키지 반입 금지(정적 서빙은 `npx expo serve` 또는 `python3 -m http.server`).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/38-supplier-link/38-CONTEXT.md
@.planning/phases/38-supplier-link/38-RESEARCH.md
@.planning/phases/38-supplier-link/38-PATTERNS.md
@.planning/phases/38-supplier-link/38-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: `react-native-web` 설치(승인 task) → `expo export --platform web` 실측 → 웹 로그인 분기 → 후보 (2) probe 한 장 → 시뮬레이터 Mobile Safari 에 둘 다 서빙 + 자동 스모크 → 측정 로그(테스트 계정 명시)</name>
  <files>app/package.json, app/package-lock.json, app/src/lib/socialAuth.web.ts, app/src/lib/firebase.web.ts, .planning/phases/38-supplier-link/38-04-probe/index.html, .planning/phases/38-supplier-link/38-04-probe/probe.js, .planning/phases/38-supplier-link/38-04-probe/README.md, .planning/phases/38-supplier-link/38-04-MEASUREMENT.md</files>
  <read_first>
    - .planning/phases/38-supplier-link/38-RESEARCH.md "### Q7. 공급자 인증 · 웹 로그인" · "### Q8. 페이지 호스팅 — 실측 결과" · "### H. 페이지 호스팅" 표 · "## Package Legitimacy Audit"(react-native-web = Approved, 설치는 승인 task) · "Pitfall 9"
    - .planning/phases/38-supplier-link/38-PATTERNS.md "### 후보 (1) 웹 분기 `app/src/lib/firebase.web.ts` · `socialAuth.web.ts`" 절(발췌 + 플래너 메모)
    - app/src/lib/firebase.ts (전체 — `resolveAuth` try/catch, exports `app, auth, db`) · app/src/lib/socialAuth.ts :1-60 (exports · `SocialAuthOutcome` · `SocialAuthResult`) · :95-160 (`signInWithGoogle` · `attachOrSignIn` 흐름) · app/src/lib/authUser.ts
    - app/src/app/auth/login.tsx :40-75 (Google 버튼이 `signInWithGoogle` 의 outcome 만 본다) · app/src/app/index.tsx (익명 로그인 진입) · app/src/app/(tabs)/analyze.tsx :420-480 (`pickFromLibrary` — 웹에서 File 이 어떻게 오는지 볼 자리)
    - app/package.json · app/app.json (`web` 키) · app/.gitignore (`dist/`)
    - app/scripts/verify-firebase.mjs :1-40 (`.env` 의 `EXPO_PUBLIC_FIREBASE_*` 7키 로드 어법 — probe `config.js` 생성에 재사용) · app/src/app/analysis/loading.tsx :159-178 (앱의 분석 doc 선작성 → PUT 순서 — probe 가 그대로 따른다) · .planning/phases/38-supplier-link/38-PATTERNS.md "### 후보 (2) `web/supplier.html` (단일 정적 HTML)" 절(CDN ESM·목업 선례) · .planning/phases/38-supplier-link/38-UI-SPEC.md "## Registry Safety"(gstatic 12.13.0 고정)
    - .planning/phases/38-supplier-link/38-REVIEWS.md "### R14" 마지막 문단 · "### R15" 넷째 항목(테스트 계정·uid)
  </read_first>
  <action>
    1. 설치(승인 task — RESEARCH 감사 Approved, 설치 전 `npm view react-native-web version` 이 `0.21.x` 인지·`repository.url` 이 `necolas/react-native-web` 인지 다시 본다): `cd /Users/kimtaesung/Dev/SunityMotion/app && npx expo install react-native-web`. 직후 `git status --porcelain` 이 `app/package.json`·`app/package-lock.json` 만 보이는지, `ls ~/package.json ~/node_modules 2>&1` 이 홈에 아무것도 새로 안 만들었는지 확인(사고 재발 방지). 커밋 `chore(38-04): install react-native-web for web-export measurement`.
    2. export 실측: `CI=1 npx expo export --platform web --output-dir <scratchpad>/web-export 2>&1 | tee <scratchpad>/web-export.log`; 기록 = exit code · 소요 초 · `du -sh` · 경고/오류 원문(파일:줄). 실패가 `firebase.ts`/`socialAuth.ts`/네이티브 모듈에서 나면 3 을 먼저 하고 재실측(최대 2회, 각 회 로그 보존).
    3. 웹 분기(`src/lib` 이라 `.web.ts` 허용 [CITED RESEARCH Q8]): `app/src/lib/socialAuth.web.ts` — `socialAuth.ts` 와 같은 export 이름·타입(`SocialAuthOutcome`, `SocialAuthResult`, `signInWithGoogle`, `signInWithApple`); `signInWithGoogle` 은 `firebase/auth` 의 `GoogleAuthProvider` + 현재 사용자가 익명이면 `linkWithPopup(auth.currentUser, provider)` → outcome `'linked'`, `auth/credential-already-in-use` 면 `signInWithCredential(auth, GoogleAuthProvider.credentialFromError(e))` → `'switched'`; 익명이 아니거나 없으면 `signInWithPopup(auth, provider)` → `'signed_in'`; `auth/popup-closed-by-user`·`auth/cancelled-popup-request` → `'cancelled'`; `auth/popup-blocked` 는 `Error` 에 `code` 를 보존해 throw(페이지가 `login.popupBlocked` 로 매핑). `signInWithRedirect` 금지(RESEARCH §D — S3/CloudFront 도메인에서 Safari 16.1+ 차단). `signInWithApple` 은 `throw new Error('apple_web_unsupported')`. `app/src/lib/firebase.web.ts` 는 export 또는 런타임에서 `getReactNativePersistence` 부재가 **문제로 확인될 때만**: `initializeApp` + `getAuth(app)`(웹 기본 persistence local [CITED]) + `getFirestore`, `app, auth, db` 같은 export. 만들지 않았으면 그 이유(`[확인]` 근거)를 로그에.
    3-b. **후보 (2) probe(리뷰 R14 — 같은 4가지를 같은 조건에서):** `.planning/phases/38-supplier-link/38-04-probe/index.html` + `probe.js`(ESM, `https://www.gstatic.com/firebasejs/12.13.0/firebase-{app,auth,firestore}.js` — 버전은 `app/package-lock.json` 의 firebase 와 같게) + `README.md`(측정 전용, 페이지 구현 아님, 지우지 않는 이유 = 결정 근거 재현). 화면은 버튼 4개와 로그 `<pre>` 뿐(디자인 0): ① `Google 로그인`(`signInWithPopup`, 익명 아님 — `auth/popup-blocked` 등 오류 코드를 로그에 그대로) ② `<input type="file" accept="video/mp4,video/quicktime,.mp4,.mov">` 선택 → `name/size/type` 로그 ③ `업로드`: 앱 순서 그대로 — `setDoc(users/{uid}/analyses/{analysisId})`(`analysisId` 는 `POST /upload-url` 응답값, `mode:'mode3'`, `status:'uploading'`, `fileName`, `createdAt`, `updatedAt`, `learningOptIn:false`) → XHR PUT(`Content-Type` = video/mp4|video/quicktime, 진행률 로그) ④ 자동: PUT 직후 그 doc 에 `onSnapshot` → `status` 변화를 로그(`queued` 가 오면 PUT+알림+구독 셋 다 산 것; Pod down 이라 뒤이어 `failed` 정상). `config.js` 는 `app/.env` 의 `EXPO_PUBLIC_FIREBASE_*` 7키 + `EXPO_PUBLIC_API_BASE_URL` 로 실행자가 `node -e` 한 줄로 생성하고 **커밋하지 않는다**(측정 끝나면 삭제; 값은 공개 웹 config 라 비밀은 아니다). `innerHTML` 금지(`textContent`), `signInWithRedirect` 금지.
    4. 서빙: 후보 (1) `npx expo serve <scratchpad>/web-export --port 8082` (없으면 `python3 -m http.server 8082 --directory <scratchpad>/web-export`, 이때 딥링크 404 는 SPA 폴백 부재로 기록); 후보 (2) `python3 -m http.server 8083 --directory .planning/phases/38-supplier-link/38-04-probe`. 자동 스모크: `curl -s http://localhost:8082/ | grep -c 'id="root"'` ≥ 1, `curl -s http://localhost:8083/ | grep -c 'id="log"'` ≥ 1; 부팅된 iOS 시뮬레이터가 없으면 `xcrun simctl boot "<iPhone 최신>"` 후 `xcrun simctl openurl booted http://localhost:8082/` → 10초 뒤 `xcrun simctl io booted screenshot <scratchpad>/38-04-web-root.png`; 한 장 더 `http://localhost:8082/auth/login`; probe 도 `http://localhost:8083/` 한 장. 스크린샷을 눈으로 보고 흰 화면/오류 배너 여부를 관측 절에 적는다(추측 금지).
    4-b. **테스트 계정(리뷰 R15d):** MEASUREMENT.md 머리에 `test_account:` 줄 — belle 이 지정한 테스트용 Google 계정(belle 본계정 아님; 없으면 Task 2 체크포인트 첫 줄에서 belle 이 하나 정한다). 로그인 뒤 uid 는 두 후보 모두 화면 로그/콘솔에 찍어 `[확인]` 으로 옮긴다(앞 4자 마스킹). PUT 뒤 실행자가 `AWS_PROFILE=sunity-motion aws s3 ls s3://sunity-motion-pilot-videos/uploads/<uid>/`(읽기 전용)로 그 uid 아래에 객체가 생겼는지 대조 — "익명 uid 에 남는다" 는 옛 문장은 쓰지 않는다(Google 로그인 뒤 PUT 은 로그인 uid 로 간다).
    5. `38-04-MEASUREMENT.md` 작성 — **관측(잰 값)** 절: 설치 버전·export exit/시간/크기·오류 원문·분기 파일 유무·probe 파일 존재·서빙 방식 2개·스모크 결과·스크린샷 경로·`test_account`; **미측정** 절: Task 2 가 잴 4가지 × 2 후보(로그인·파일·PUT·onSnapshot)와 실기기 HTTPS(Authorized domain 이 필요해 로컬에선 못 잰다 — `localhost` 만 기본 허용 [CITED firebase google-signin]); **진단** 절은 비워 두고 Task 3 에서 채운다.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && grep -q '"react-native-web"' package.json && npm run typecheck && test -f ../.planning/phases/38-supplier-link/38-04-MEASUREMENT.md && test -f src/lib/socialAuth.web.ts && test -f ../.planning/phases/38-supplier-link/38-04-probe/probe.js && grep -q 'signInWithPopup' ../.planning/phases/38-supplier-link/38-04-probe/probe.js && test "$(grep -c 'signInWithRedirect\|innerHTML' ../.planning/phases/38-supplier-link/38-04-probe/probe.js)" = "0" && grep -q 'test_account:' ../.planning/phases/38-supplier-link/38-04-MEASUREMENT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"react-native-web"' app/package.json` == 1 and `git status --porcelain` after install lists only `app/package.json` and `app/package-lock.json` (plus files this task creates).
    - `ls ~/package.json 2>/dev/null` shows nothing new created by this task (compare mtime before/after; record in MEASUREMENT.md).
    - `cd app && npm run typecheck` exits 0 with `socialAuth.web.ts` present (`grep -c "signInWithPopup\|linkWithPopup" app/src/lib/socialAuth.web.ts` ≥ 2, `grep -c "signInWithRedirect" app/src/lib/socialAuth.web.ts` == 0).
    - `38-04-MEASUREMENT.md` contains an `## 관측` section with the export exit code, elapsed seconds, `du -sh` size (or the verbatim failure), the screenshot paths (both candidates), and a `test_account:` line; and a `## 미측정` section listing the 4 human checks × 2 candidates.
    - `38-04-probe/probe.js` exists, imports `firebasejs/12.13.0` (`grep -c 'firebasejs/12.13.0' probe.js` ≥ 3), contains `signInWithPopup`, `XMLHttpRequest`, `onSnapshot`, `/upload-url`; `grep -c 'signInWithRedirect\|innerHTML' probe.js` == 0; `git status --porcelain` shows no `38-04-probe/config.js`.
  </acceptance_criteria>
  <done>export 결과·번들 크기·런타임 첫 화면이 잰 값으로 기록되고, 웹 Google 팝업 경로가 코드로 존재해 사람 검증이 가능하다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: belle 실측 — 시뮬레이터 Mobile Safari 에서 Google 팝업 로그인 · .mov 선택 · presigned PUT · onSnapshot</name>
  <files>none — 체크포인트(사람 손). 결과는 38-04-MEASUREMENT.md 관측 절에 기록</files>
  <action>
    실행자: Task 1 의 서버 두 개(`localhost:8082` 후보 (1) · `localhost:8083` 후보 (2) probe)와 시뮬레이터가 살아 있는지 확인하고 `xcrun simctl addmedia booted <짧은 .mov>` 로 테스트 영상을 넣은 뒤, 아래 how-to-verify(4항목 × 2 후보, 같은 계정·같은 영상·같은 순서)를 belle 에게 그대로 제시하고 멈춘다. `test_account:` 가 비어 있으면 첫 줄로 belle 에게 테스트용 Google 계정을 정해 달라고 한다(belle 본계정 금지 — 기록 오염). belle 의 ○/× 와 문구 원문을 `38-04-MEASUREMENT.md` 관측 절에 `[확인 belle]` 로 옮겨 적고, 4항목 뒤 실행자가 `aws s3 ls s3://sunity-motion-pilot-videos/uploads/<uid>/` 로 두 업로드가 그 계정 uid 아래에 있는지 대조해 `[확인]` 으로 적는다(해석·진단은 진단 절로). 자동화할 수 있는 부분(서버·시뮬·영상 주입·uid 대조)은 사람에게 시키지 않는다.
  </action>
  <verify>
    <automated>test -f /Users/kimtaesung/Dev/SunityMotion/.planning/phases/38-supplier-link/38-04-MEASUREMENT.md && grep -q '확인 belle' /Users/kimtaesung/Dev/SunityMotion/.planning/phases/38-supplier-link/38-04-MEASUREMENT.md</automated>
  </verify>
  <acceptance_criteria>
    - `38-04-MEASUREMENT.md` 관측 절에 **두 후보 각각** 4항목 ○/× 와 belle 가 본 문구 원문이 `[확인 belle]` 표식으로 적혀 있다(8줄).
    - `test_account:` 줄이 채워져 있고, 로그인 uid(앞 4자)와 `aws s3 ls …/uploads/<uid>/` 대조 결과가 `[확인]` 으로 있다(R15d).
    - 실행자가 추가한 해석은 관측 절이 아니라 진단 절에만 있다.
  </acceptance_criteria>
  <done>belle 의 4줄 ○/× 가 측정 로그에 원문으로 남았다.</done>
  <what-built>후보 (1) web export 번들이 `http://localhost:8082` 에, 후보 (2) 최소 probe(로그인 버튼·파일 선택·업로드·구독 로그) 가 `http://localhost:8083` 에 떠 있고(Task 1), iOS 시뮬레이터 Safari 로 열 수 있다. 웹 Google 로그인 분기(`socialAuth.web.ts`)가 들어 있다. 4가지 × 2 후보는 Google 계정과 사람 손이 필요해 자동으로 못 잰다. 계정 = MEASUREMENT.md `test_account:`(belle 지정 테스트 계정).</what-built>
  <how-to-verify>
    실행자가 먼저 시뮬레이터에 페이지를 띄워 둔다(`xcrun simctl openurl booted http://localhost:8082/`). belle 은 시뮬레이터 화면에서:
    1. 루트 `/` 가 열리는가(흰 화면·오류 배너 없이 인트로/홈). 익명 로그인이 자동으로 되는가(홈 진입).
    2. `http://localhost:8082/auth/login` → `Google로 시작하기` → 팝업이 뜨는가 → 로그인 뒤 화면이 넘어가는가. 팝업 차단이면 그 문구를 그대로 적어 준다. (localhost 는 Firebase 기본 Authorized domain — 실기기 LAN IP 는 안 된다, 실기기는 38-13 배포 뒤.)
    3. 분석 탭 → `앨범에서 선택` → 시뮬레이터에 넣어 둔 `.mov` 하나(실행자가 `xcrun simctl addmedia booted <mov>` 로 미리 넣는다) 를 고른다 → 형식 오류 다이얼로그가 뜨는지, 넘어가는지.
    4. 넘어가면 로딩 화면이 `queued` 로 바뀌는가(= upload-url 200 + S3 PUT + onSnapshot 셋 다 동작). Pod 가 꺼져 있으니 이후 `failed` 는 정상이다. 이 업로드는 **테스트 계정의 uid** 에 남는다(Google 로그인 뒤라 익명 uid 가 아니다 — 그래서 belle 본계정을 쓰지 않는다).
    **후보 (2) probe — `http://localhost:8083/` 에서 같은 4가지를 같은 순서로:** 5. `Google 로그인` 버튼 → 팝업 → 로그 `<pre>` 에 uid 가 찍히는가(차단이면 그 문구). 6. 파일 선택 → 같은 `.mov` → 로그에 이름/크기가 찍히는가. 7. `업로드` → 진행률 → `PUT 200`. 8. 로그에 `status: queued`(구독)가 찍히는가(뒤이어 `failed` 정상).
    각 항목 ○/× 와 본 그대로의 문구를 알려 주면 실행자가 MEASUREMENT.md 관측 절에 `[확인 belle]` 로 적는다(8줄).
  </how-to-verify>
  <resume-signal>8줄 ○/× (예: `1○ 2○ 3× 형식 오류 4- / 5○ 6○ 7○ 8○`) 또는 "approved" (+ 첫 줄에 `test_account: <계정>` 이 비어 있으면 그 지정)</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: 호스팅 결정 — 채워진 결정표로 belle 이 고른다 → 미선택 트랙 스킵을 파일로 박는다(SUMMARY skipped-by-decision · PLAN skipped · 38-13 depends_on · VALIDATION 행 · 한 커밋)</name>
  <files>.planning/phases/38-supplier-link/38-04-MEASUREMENT.md, .planning/phases/38-supplier-link/38-13-PLAN.md, .planning/phases/38-supplier-link/38-10-PLAN.md, .planning/phases/38-supplier-link/38-11-PLAN.md, .planning/phases/38-supplier-link/38-12-PLAN.md, .planning/phases/38-supplier-link/38-VALIDATION.md, .planning/STATE.md, .planning/ROADMAP.md, app/src/lib/supplierRules.ts (결정 뒤 실행자가 쓴다 — 미선택 SUMMARY 파일은 아래 (a) 에서 새로 만든다; supplierRules.ts 는 selfScoreMin 이 90 이 아닐 때만)</files>
  <action>
    실행자: Task 1·2 결과로 `38-04-MEASUREMENT.md` 의 결정표(context 절의 행 전부 — 잰 값 또는 `[미측정]`)를 **먼저 채운 뒤** 이 체크포인트를 띄운다. **결정표 규칙(리뷰 R14):** 두 후보의 4가지 행은 Task 2 의 같은 실험값으로만 채운다 — 후보 (2) 의 가능/불가능을 후보 (1) 전체 앱 export 결과에서 추론해 적지 않는다(export 실패는 후보 (1) 의 "남은 일" 행에만). belle 의 선택(option-1/option-2)을 결정 절에 belle 말 그대로 인용한다. 플래너·실행자가 대신 고르지 않는다.
    **결정 뒤 기계적 스킵(한 커밋, 순서 고정 — 이것이 없으면 wave 4 에서 두 트랙이 다 뜨고 option-2 면 wave 6 이 영영 안 풀린다):**
    (a) 미선택 플랜마다 `.planning/phases/38-supplier-link/38-NN-SUMMARY.md` 를 쓴다(option-1 → `38-12-SUMMARY.md`; option-2 → `38-10-SUMMARY.md` + `38-11-SUMMARY.md`). frontmatter: `phase: 38-supplier-link` · `plan: NN` · `status: skipped-by-decision` · `skipped_by: "38-04 Task 3"` · `decision: option-N` · `decided_at: <ISO 시각>`; 본문 한 단락 "belle 결정 `<원문>` 으로 미실행 — 코드 변경 0, 커밋 0, 이 트랙의 must_haves 는 선택 트랙이 대신 만족한다". (execute-phase 는 SUMMARY 가 있는 플랜을 완료로 보고 건너뛴다.)
    (b) 미선택 PLAN 파일 frontmatter 에 `skipped: true` 한 줄 추가(`autonomous:` 아래; 다른 필드 무접촉).
    (c) `38-13-PLAN.md` frontmatter `depends_on`: option-1 → `["38-09", "38-11"]`(현재값 유지, 확인만), option-2 → `["38-09", "38-12"]` 로 고쳐 쓴다(wave 6 그대로 — 38-09 가 wave 5).
    (d) `38-VALIDATION.md` Per-Task Verification Map 의 미선택 행(38-10-01~03 · 38-11-01~03 또는 38-12-01~03) Status 를 `⏭ skipped-by-decision` 으로.
    (e) `38-04-MEASUREMENT.md` 결정 절 + `.planning/STATE.md` `stopped_at` 한 줄 + ROADMAP 38 플랜 목록의 "option-1/option-2 전용" 표기에 확정 표시 → 위 (a)~(d) 와 함께 **한 커밋** `docs(38-04): hosting decision option-N — skip 38-XX by decision`.
    **한 줄 질문(같은 체크포인트에서, 별도 결정 아님):** "재현성 문구 분기 기본값 90(`SELF_SCORE_OK_MIN`, RESEARCH A9 [ASSUMED]) — 바꿀까요? 숫자는 belle 가 정합니다." 답이 있으면 결정 절에 `selfScoreMin: <숫자>` 로, 없으면 `selfScoreMin: 90 유지` 로 적는다. 값이 90 이 아니면 **같은 커밋에서** `app/src/lib/supplierRules.ts` 의 `export const SELF_SCORE_OK_MIN = 90` 한 줄만 그 값으로 고친다(38-03 이 만든 상수 — 38-10/38-12 는 이 이름만 import 한다, 리터럴 금지). 이 숫자는 통과 목표가 아니라 문구 분기점이다(목표 숫자 금지 메모리) — 어디에도 pass/fail 기준으로 쓰지 않는다.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/.planning/phases/38-supplier-link && grep -q 'option-1\|option-2' 38-04-MEASUREMENT.md && grep -q 'selfScoreMin:' 38-04-MEASUREMENT.md && { { test -f 38-12-SUMMARY.md && grep -q 'skipped-by-decision' 38-12-SUMMARY.md && grep -q 'skipped: true' 38-12-PLAN.md && grep -q '"38-11"' 38-13-PLAN.md; } || { test -f 38-10-SUMMARY.md && test -f 38-11-SUMMARY.md && grep -q 'skipped-by-decision' 38-10-SUMMARY.md && grep -q 'skipped: true' 38-11-PLAN.md && grep -q '"38-12"' 38-13-PLAN.md; }; }</automated>
  </verify>
  <acceptance_criteria>
    - `38-04-MEASUREMENT.md` 결정 절에 `option-1` 또는 `option-2` 와 belle 의 이유(있으면)가 인용돼 있다.
    - 결정표의 모든 행이 잰 값 또는 `[미측정]` 으로 채워져 있다(빈 칸 0).
    - Exactly one track is skipped on disk: either `38-12-SUMMARY.md` exists with `status: skipped-by-decision` (option-1) or both `38-10-SUMMARY.md` and `38-11-SUMMARY.md` do (option-2); the matching PLAN file(s) contain `skipped: true`; `grep -n depends_on 38-13-PLAN.md` shows `38-11` (option-1) or `38-12` (option-2); the unchosen rows in `38-VALIDATION.md` read `⏭ skipped-by-decision`; all of (a)~(e) are in one commit whose message starts `docs(38-04): hosting decision`.
    - 결정 절 has a `selfScoreMin:` line (belle 의 숫자 또는 `90 유지`), and nowhere in the phase is that number used as a pass/fail target; if ≠ 90, `grep -c "export const SELF_SCORE_OK_MIN = <값>" app/src/lib/supplierRules.ts` == 1 in the same commit.
    - 결정표의 후보 (2) 4가지 행 값은 Task 2 의 probe 실측(5~8번)에서만 왔다 — "export 실패라 불가" 같은 추론 문장이 그 행에 없다(R14).
  </acceptance_criteria>
  <done>belle 이 후보를 골랐고 그 선택이 측정 로그·STATE·ROADMAP 에 기록됐으며, 미선택 트랙이 SUMMARY/PLAN/38-13 depends_on/VALIDATION 으로 건너뛰어지게 파일에 박혔다.</done>
  <decision>공급자 페이지를 어느 후보로 만들고 어디에 올릴 것인가. 정확히 하나의 페이지 플랜 세트만 실행된다.</decision>
  <context>
    실행자는 Task 1·2 결과로 `38-04-MEASUREMENT.md` 의 결정표를 **채운 뒤** 이 체크포인트를 띄운다. 표의 행(전부 잰 값 또는 `[미측정]` 표시; **두 후보 열이 같은 4가지 실험값**): export 성공 여부·시간 · dist 크기 · 시뮬 Safari 첫 화면 · Google 팝업 ○/×(후보 1 · 후보 2 probe) · .mov 선택 ○/×(둘) · PUT ○/×(둘, uid 대조 포함) · onSnapshot ○/×(둘) · 후보 (1) 남은 일(38-10·38-11 = 라우트 3개 + 데이터 훅 + 웹 분기 마무리; 실측에서 깨진 것이 있으면 그 수리) · 후보 (2) 남은 일(38-12 = HTML/CSS/JS 3파일 + copy/rules 내보내기 스크립트 — probe 가 이미 잰 4가지는 "측정됨" 으로) · 배포 경로(둘 다 S3 정적 버킷 + CloudFront OAC, HTTPS 필수 — Google OAuth 팝업은 Authorized domain 등록 필요, belle 콘솔 1회) · 비용(S3 센트 단위; CloudFront 과금 방식은 38-13 이 계정에서 관측한 값만 — 여기서는 `[미확인]`, Free plan 단정 금지(리뷰 R12)) · "같은 앱" 문자 그대로인가((1) 예 / (2) 아니오 — 디자인 토큰은 CSS 로 재현).
    판정 기준(RESEARCH §H): "같은 앱"(D-01)을 문자 그대로 지키려면 (1); 실증 마감이 우선이고 (1) 이 실측에서 막혔으면 (2). 어느 쪽이든 로그인·PUT 실측 없이는 고르지 않는다.
    호스팅 도메인: 기본 = S3+CloudFront(스택 문서와 같다). Firebase Hosting 을 원하면 별도 플랜 수정이 필요하다(지금 플랜엔 없음).
    같은 자리에서 한 줄 더: "재현성 문구 분기 기본값 90(`SELF_SCORE_OK_MIN`) — 바꿀까요? 숫자는 belle 가 정합니다." (답이 없으면 90 유지. 문구 분기점일 뿐 통과 목표가 아니다.)
  </context>
  <options>
    <option id="option-1">
      <name>후보 (1) Expo Router 공급자 라우트 web export → S3+CloudFront (플랜 38-10 · 38-11 실행, 38-12 건너뜀)</name>
      <pros>같은 코드베이스 = D-01 "같은 앱" 문자 그대로; 테마 토큰·컴포넌트(PickErrorDialog 등) 재사용; 앱 안 공급자 모드로 이어진다</pros>
      <cons>번들 크기·web 호환 리스크(실측값 참조); expo-image-picker 웹 File 처리 확인 필요; 배포 산출물이 앱 전체 라우트</cons>
    </option>
    <option id="option-2">
      <name>후보 (2) 정적 단일 페이지(HTML+CSS+JS, Firebase JS SDK CDN 12.13.0) → S3+CloudFront (플랜 38-12 실행, 38-10 · 38-11 건너뜀)</name>
      <pros>의존성 0, 번들 리스크 0, 지금 당장 만들 수 있다; 같은 supplierCopy 문구·UI-SPEC 상태기계 재사용</pros>
      <cons>"같은 앱" 문자 그대로가 아니다(끝 그림 앱 안 공급자 모드는 별도 구현); 디자인 토큰을 CSS 로 재현; 4가지는 probe 로 잰 값(Task 2 5~8번)이 결정표에 있다</cons>
    </option>
  </options>
  <resume-signal>Select: option-1 또는 option-2 (원하면 이유 한 줄; 선택: `selfScoreMin: <숫자>`). 실행자는 선택을 `38-04-MEASUREMENT.md` 결정 절과 STATE.md 에 기록하고, 스킵 절차 (a)~(e) 를 한 커밋으로 실행한 뒤 ROADMAP 플랜 목록의 "조건부" 표기를 확정한다.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm 레지스트리 → app/node_modules | 새 패키지 1개 반입(react-native-web) |
| 브라우저 ↔ Firebase Auth 팝업 | OAuth 토큰이 브라우저에 저장된다(웹 persistence local) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-SC | Tampering | npm install react-native-web | mitigate | RESEARCH 감사 `[OK]`(npm 생태계, necolas repo, postinstall 없음) + 설치 직전 `npm view` 재확인 + `slopcheck install` 금지 + `git status` 로 lockfile 외 변경 0 확인 |
| T-38-04-1 | Spoofing | `socialAuth.web.ts` | mitigate | Firebase 공식 팝업 API 만(`signInWithPopup`/`linkWithPopup`), 리다이렉트 금지, 자체 OAuth 처리 없음 |
| T-38-04-2 | Information disclosure | 측정 중 업로드 | accept | belle 지정 **테스트 계정** uid 로 올린 무해한 클립 2건(후보별 1) — belle 본계정 무접촉, `uploads/` 에 남는 것은 영구 보관 정책과 같다; uid 는 `aws s3 ls` 로 대조(R15d) |
| T-38-04-4 | Tampering | probe `config.js` | mitigate | 공개 웹 config 라 비밀 아님이나 커밋 금지(`git status` 게이트) — 측정 뒤 삭제; probe 는 `textContent`·팝업 API 만 |
| T-38-04-3 | Elevation | localhost Authorized domain | accept | 로컬 측정 전용, 배포 도메인은 38-13 에서 belle 이 콘솔에 등록 |
</threat_model>

<verification>
- `38-04-MEASUREMENT.md` 관측 절에 잰 값(export·크기·스모크·belle 4줄), 결정 절에 belle 선택.
- `npm run typecheck` clean, lockfile 커밋, 플랜 끝 push.
</verification>

<success_criteria>
- 두 후보의 같은 4가지가 각각 ○/× 로 기록되고(테스트 계정·uid 대조 포함) belle 이 option-1/option-2 를 골랐다.
- 미선택 트랙이 파일로 건너뛰어진다(SUMMARY skipped-by-decision · PLAN skipped: true · 38-13 depends_on 확정 · VALIDATION 행) — 한 커밋.
- 웹 Google 팝업 로그인 코드가 존재한다.
</success_criteria>

<output>
Create `.planning/phases/38-supplier-link/38-04-SUMMARY.md` when done (결정 = belle 의 말 그대로 인용).
</output>
