---
phase: 38-supplier-link
plan: 05
type: execute
wave: 2
depends_on: ["38-01"]
files_modified:
  - backend/shared/python/sunity_shared/analysis/hold_height.py
  - backend/shared/python/sunity_shared/analysis/registration_checks.py
  - backend/tests/test_registration_checks.py
  - backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/rtmw_engine.py
  - backend/tests/test_rtmw_engine.py
autonomous: true
requirements: [REQ-38-4]
must_haves:
  truths:
    - "D-09 / Success ②: 실패 4형(사람 미검출 · 여러 명 · 서 있는 시작 없음 · 저신뢰)이 각각 **합성 입력**으로 강제되어 코드(`no_human`·`multiple_people`·`no_standing_start`·`low_confidence`)를 낸다 — Pod·Firestore·S3 0"
    - "여러 명은 엔진이 버리던 N 을 반환값으로 돌려주는 새 API(`estimate_with_person_counts`)로 잰다 — 사이드카 상태 0, `estimate()` 는 byte-동일"
    - "서 있는 시작은 nnt 바닥 규칙(창 낮은발 10% 분위 < −0.10 몸길이) 을 `hold_height` 의 같은 상수로 판정한다 — 상수 복제 0. 리뷰 R6: 이 규칙은 서 있음의 **PROXY**(웅크린 형상도 통과 — 리뷰 합성 재현)이고 자세 분류기가 아니다 — 한계를 코드 docstring·SUMMARY·가이드 ④ 문구에 적고 이 phase 에서 분류기를 더하지 않는다"
    - "리뷰 R6 판정 순서: `no_human`(엔진 예외, 호출측) → `low_confidence`(측정 불가: 관절 중앙값 < 문턱 **또는** 서 있는 창의 바닥 재료(발목·어깨)가 `hold_height.MIN_CONF` 미만) → `multiple_people` → `no_standing_start`(측정 가능할 때만). 발목 conf 0.2 는 `low_confidence`+부위 목록이지 `no_standing_start` 가 아니다"
    - "리뷰 R15b: 사람 수 비율은 순서 무관 — 2/10 통과 · 3/10 실패(≥0.30) · 4/10 실패, 같은 목록을 섞어도 같은 결과"
    - "저신뢰는 12관절 한국어 부위명(`KEYPOINT_LABEL_KO`) 목록을 낸다"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/registration_checks.py"
      provides: "check_registration · is_multiple_people · standing_start_measurable · standing_start_ok · low_confidence_joints · KEYPOINT_LABEL_KO · RegistrationVerdict"
      contains: "def check_registration"
    - path: "backend/shared/python/sunity_shared/analysis/hold_height.py"
      provides: "floor_reference_valid(report, window) 공개 함수"
      contains: "def floor_reference_valid"
    - path: "backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/rtmw_engine.py"
      provides: "estimate_with_person_counts(frames, pole_axis) -> (list[PoseFrame], list[int])"
      contains: "def estimate_with_person_counts"
  key_links:
    - from: "registration_checks.py"
      to: "hold_height.py"
      via: "floor_reference_valid + FLOOR_VIOLATION_BODY_LENGTH import (복제 금지)"
      pattern: "from .hold_height import\\|from . import hold_height\\|hold_height\\."
    - from: "registration_checks.py"
      to: "models.py"
      via: "REG_ERR_* 코드 상수"
      pattern: "REG_ERR_"
---

<objective>
등록 실패 4형의 **순수 판정**(REQ-38-4, D-09)과 그것을 먹이는 엔진 API(프레임별 사람 수)를 만든다. 넷 다 합성 입력으로 강제되는 pytest 가 Success ② 의 증거다.

Purpose: 원시 함수가 셋은 있고(NoHumanError · hold_height 바닥 규칙 · keypoint confidence) 하나는 없다(엔진이 `kps_batch[0]` 로 N 을 버린다, RESEARCH Q5). 판정을 순수 함수로 두면 Pod 없이 닫힌다(패턴 4).
Output: `registration_checks.py`(신규) · `hold_height.floor_reference_valid` · `rtmw_engine.estimate_with_person_counts` · 테스트 2파일.

Phase 38 전제(38-01 머리말) 적용. 문턱 3개(여러 명 비율 0.30 · 서 있는 구간 첫 1.0초 · 저신뢰 중앙값 0.5)는 RESEARCH A5~A7 **[ASSUMED]** — 상수 하나씩 두고 **시험 영상 2차로만 조정**한다(숫자 목표 금지). 채점 경로(`dimensions`·`assemble.build_mode1`·`_process`)는 무접촉.

**리뷰 반영(2026-09-26, R6·R15b):** 판정 순서를 "측정 불가(`low_confidence`) 먼저, 측정 결과 위반(`no_standing_start`) 나중" 으로 고쳤다 — `hold_height.MIN_CONF`(0.35) 미만 발목은 `_frame_series` 가 None 을 돌려 옛 순서로는 `no_standing_start` 로 오분류됐다(리뷰 로컬 재현 [확인]). 바닥 일관성 규칙은 서 있음의 **PROXY** — 웅크린 합성 좌표도 통과한다(리뷰 재현) — 이 phase 는 자세 분류기를 더하지 않고 한계를 문서·문구(가이드 ④ "서 있는 자세인지는 사람이 확인해 주세요")에 남긴다. 사람 수 fixture 는 2/10·3/10·4/10 경계로.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/38-supplier-link/38-CONTEXT.md
@.planning/phases/38-supplier-link/38-RESEARCH.md
@.planning/phases/38-supplier-link/38-PATTERNS.md
@.planning/phases/38-supplier-link/38-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `hold_height.floor_reference_valid` 공개 + `registration_checks.py`(저신뢰 → 여러 명 → 서 있는 시작 순서 · 측정 가능성 분리 · 12관절 라벨 · PROXY 명시) + 합성 강제 테스트(경계 2/10·3/10·4/10)</name>
  <files>backend/shared/python/sunity_shared/analysis/hold_height.py, backend/shared/python/sunity_shared/analysis/registration_checks.py, backend/tests/test_registration_checks.py</files>
  <behavior>
    - `multiple_people_ratio([1]*8 + [2]*2)` → 0.2; `is_multiple_people([1]*8 + [2]*2)` → False (2/10); `is_multiple_people([2]*3 + [1]*7)` → True (3/10 = 0.30, `>=` 경계); `is_multiple_people([2]*4 + [1]*6)` → True (4/10); `random.Random(0).shuffle` 한 같은 세 목록 → 같은 결과(순서 무관); `[]` → False; `[0,0,1]` → False(0 은 N≥2 아님)
    - `low_confidence_joints(report)`: 발목 두 관절 conf 0.2, 나머지 0.9 → `['left_ankle','right_ankle']`(report joints 순서); 전부 0.9 → `[]`
    - `standing_start_measurable(report, n_stand=10)` → `(True, [])` 정상; 발목 conf 0.2(서 있는 창) → `(False, ['left_ankle','right_ankle'])`; 어깨만 0.2 → `(False, ['left_shoulder','right_shoulder'])`; `n_stand=2`(`MIN_STAND_FRAMES` 미만) → `(True, [])`(신뢰도 문제가 아니라 창 문제 — 아래 `no_standing_start` 로 간다)
    - `standing_start_ok(report, n_stand=10)`: `test_hold_height._pose` 식 합성(앞 10프레임 서 있음: 어깨 0.5·발목 0.8, 창 발목 0.83) → True; 서 있는 프레임 발목을 0.6 으로 바꾼 변형(창 lowFoot ≈ (0.6−0.83)/0.3 < −0.10) → False; `n_stand=2` → False; 신뢰도 전부 0.1 → False(fail-closed — 단 `check_registration` 은 이 입력을 먼저 `low_confidence` 로 잡는다)
    - `joint_labels_ko(['left_ankle','right_hand'])` → `['왼쪽 발목','오른쪽 손목']`; `KEYPOINT_LABEL_KO` 키 집합 == `keypoint_frame._KEYPOINT_NAMES` 12개
    - `check_registration(person_counts, report, n_stand=10)` **순서(리뷰 R6):** ① `low_confidence_joints` 비어 있지 않거나 `standing_start_measurable` 이 False → `RegistrationVerdict(ok=False, reason='low_confidence', joints=<두 목록 합집합, report 순서>)`; ② 아니면 `is_multiple_people` → `multiple_people`; ③ 아니면 `standing_start_ok` False → `no_standing_start`(`detail` 에 `'floor_violation'` 또는 `'stand_window_too_short'`); ④ 전부 통과 → `RegistrationVerdict(ok=True, reason=None)`
    - 결정적 케이스: 발목 conf 0.2 + 바닥 위반 형상 + N=2 30% → `low_confidence`(joints 발목 2개) — 다른 두 사유보다 먼저; 전 관절 conf 0.9 + 바닥 위반 → `no_standing_start`; 전 관절 conf 0.9 + 정상 바닥 + N=2 40% → `multiple_people`
    - `hold_height.floor_reference_valid(report, (10, T))`: 정상 → True; 바닥 위반 → False; 형상 불량/창 부족/재료 신뢰도 미달 → None; 그리고 기존 `hold_window_heights` 결과가 리팩터 전후 동일(test_hold_height 전부 통과)
    - 리뷰 R6 재현 고정: 웅크린 합성 좌표(어깨~발목 세로 길이 양수, 발이 바닥 위로 안 올라감)도 `standing_start_ok` → True — 이 테스트의 이름을 `test_floor_rule_is_a_proxy_crouch_passes` 로 두고 docstring 에 "자세 분류기가 아니라는 반례 박제(리뷰 R6) — 이 테스트가 깨지면 규칙이 바뀐 것" 을 적는다
  </behavior>
  <read_first>
    - backend/shared/python/sunity_shared/analysis/hold_height.py :1-60 (헤더·상수 MIN_CONF/MIN_STAND_FRAMES/MIN_WINDOW_FRAMES/FLOOR_VIOLATION_BODY_LENGTH) · :63-95 (`_arrays` · `_y` — conf < MIN_CONF 는 NaN) · :96-135 (`_frame_series` — w0 < MIN_STAND_FRAMES → None :107, floor/body 비유한 → None) · :151-192 (`hold_window_heights` — :167-179 바닥 위반 판정 원문)
    - backend/tests/test_hold_height.py :1-60 (`_JOINTS` 12 · `_report` · `_pose` 합성 헬퍼 — 그대로 복제해 쓴다)
    - backend/shared/python/sunity_shared/analysis/keypoint_frame.py :55-80 (`_KEYPOINT_NAMES` 12 · `left_hand`/`right_hand` = wrist 매핑)
    - backend/shared/python/sunity_shared/analysis/skeleton.py :50-62 (`JOINT_KEYS` · `JOINT_LABEL_KO` 8개 어법)
    - backend/shared/python/sunity_shared/models.py (38-01 — `REG_ERR_*`)
    - .planning/phases/38-supplier-link/38-REVIEWS.md "### R6" (재현표: 발목 0.2 → 바닥 None · crouch 통과) · "### R15" 둘째 항목(2/10·3/10·4/10) · 38-REVIEW-EVIDENCE.json `reproduction_results`
    - .planning/phases/38-supplier-link/38-PATTERNS.md "### `…/registration_checks.py`" 절 + "## 관측 — 플래너가 알아야 할 코드 사실" 2·3 + RESEARCH "### B. 실패 4형" · "### Q5"
  </read_first>
  <action>
    RED 먼저: `backend/tests/test_registration_checks.py` 를 위 behavior 대로 작성(합성 `_report`/`_pose` 헬퍼는 `test_hold_height.py` 에서 복제, `sys.path` 주입 어법 동일; 사람 수 목록 세 개는 `[1]*8+[2]*2` · `[2]*3+[1]*7` · `[2]*4+[1]*6` 로 **비율이 서로 다르게**, 각 목록을 `random.Random(0).shuffle` 한 사본으로 한 번 더) → 실패 확인 → 구현.
    `hold_height.py`: `floor_reference_valid(report, window) -> bool | None` 신설 — `_frame_series(report, window)` 가 None 이면 None; 아니면 :171-179 와 **같은 통계**(양쪽 side 의 `lowFoot` 유한값 10% 분위 < −`FLOOR_VIOLATION_BODY_LENGTH` 이면 False, `v.size == 0` 도 False) → True. `hold_window_heights` 의 :167-179 블록을 이 함수 호출로 바꾸되 반환 동작(None) 은 그대로 — `test_hold_height.py` 가 회귀 게이트. 상수는 옮기지 않는다(hold_height 소유). docstring 에 "quick-260925-nnt 바닥 규칙의 두 소비처(카드 문장 · Phase 38 등록 판정)가 이 하나를 쓴다. **서 있음의 PROXY** — 어깨~발목 세로 길이 양수 + 창에서 발이 바닥 아래로 안 내려가면 통과하므로 웅크린 형상도 통과한다(2026-09-26 리뷰 R6 합성 재현); 자세 분류기가 아니다".
    `registration_checks.py`(신규, numpy·stdlib·`hold_height`·`keypoint_frame`·`models` 만 import — Pod/Firestore/boto3 0): 헤더 docstring 은 hold_height 어법(왜/어떻게/fail-closed/채점 무접촉 + Phase 38 D-09·Success ② 인용 + **판정 순서와 그 이유**: "측정 불가를 측정 결과 위반보다 먼저 — `MIN_CONF` 미만 발목은 바닥을 못 구해 옛 순서로는 `no_standing_start` 로 오분류됐다(리뷰 R6)" + PROXY 한계). 상수 `MULTI_PERSON_FRAME_RATIO = 0.30`, `STANDING_START_DEFAULT_SEC = 1.0`, `LOW_CONFIDENCE_MEDIAN_MIN = 0.5` — 각 상수 주석에 `[ASSUMED RESEARCH A6/A5/A7] 시험 영상 2차로만 조정, 목표 숫자 금지`; `LOW_CONFIDENCE_MEDIAN_MIN` 주석에 메모리 conf-05-gate("0.5 위아래 품질 차 없음, 의미는 0.6 위") 인용. `_FLOOR_JOINTS = ("left_ankle", "right_ankle", "left_shoulder", "right_shoulder")`(= `_frame_series` 가 바닥·몸길이에 쓰는 관절 — 주석에 :110-121 인용). `KEYPOINT_LABEL_KO: dict[str, str]` 12키(`skeleton.JOINT_LABEL_KO` 8개는 그 값 그대로 + `left_hand:'왼쪽 손목'`, `right_hand:'오른쪽 손목'`, `left_ankle:'왼쪽 발목'`, `right_ankle:'오른쪽 발목'`). `@dataclass(frozen=True) class RegistrationVerdict: ok: bool; reason: str | None = None; joints: tuple[str, ...] = (); detail: str = ""`. 함수: `multiple_people_ratio(person_counts) -> float`(N≥2 프레임 / 전체, 빈 목록 0.0), `is_multiple_people(person_counts, ratio=MULTI_PERSON_FRAME_RATIO) -> bool`(`>=` 비교, 순서 무관), `low_confidence_joints(report, min_conf=LOW_CONFIDENCE_MEDIAN_MIN) -> list[str]`(`hold_height._arrays` 재사용, 관절별 `np.nanmedian` < min_conf 인 이름을 report `joints` 순서로), `standing_start_measurable(report, n_stand) -> tuple[bool, list[str]]`(`_arrays` 로 C 를 얻어 `_FLOOR_JOINTS` 중 `C[:n_stand, j]` 중앙값 < `hold_height.MIN_CONF` 인 관절 목록; 창이 `MIN_STAND_FRAMES` 미만이면 `(True, [])` — 신뢰도 문제 아님), `standing_start_ok(report, n_stand: int) -> bool`(`_arrays` 로 T, `floor_reference_valid(report, (n_stand, T))` 가 True 일 때만 True — None/False 는 False, fail-closed), `joint_labels_ko(names) -> list[str]`, `check_registration(person_counts, report, *, n_stand: int) -> RegistrationVerdict`(위 behavior 순서 ①→④; reason 은 `models.REG_ERR_*` 상수; `no_standing_start` 의 `detail` 은 `'stand_window_too_short'`(n_stand < MIN_STAND_FRAMES) 또는 `'floor_violation'`). `no_human` 은 여기서 판정하지 않는다 — 엔진 `NoHumanError` 를 호출측(38-07)이 `REG_ERR_NO_HUMAN` 으로 매핑한다는 것을 docstring 에.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && .venv/bin/python -m pytest tests/test_registration_checks.py tests/test_hold_height.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && .venv/bin/python -m pytest tests/test_registration_checks.py tests/test_hold_height.py -q` exits 0; `test_registration_checks.py` collects ≥ 14 tests including `test_floor_rule_is_a_proxy_crouch_passes` and a shuffled-order multiple-people test.
    - `grep -c "def floor_reference_valid" backend/shared/python/sunity_shared/analysis/hold_height.py` == 1 and `grep -c "FLOOR_VIOLATION_BODY_LENGTH =" backend/shared/python/sunity_shared/analysis/registration_checks.py` == 0 (상수 복제 없음); `grep -c "PROXY" backend/shared/python/sunity_shared/analysis/hold_height.py backend/shared/python/sunity_shared/analysis/registration_checks.py` → each ≥ 1.
    - `grep -c "def standing_start_measurable" backend/shared/python/sunity_shared/analysis/registration_checks.py` == 1; inside `check_registration` the first `return` with a reason is `REG_ERR_LOW_CONFIDENCE` (sed the function body: `low_confidence` line number < `multiple_people` line number < `no_standing_start` line number).
    - `grep -c "import boto3\|firebase\|firestore" backend/shared/python/sunity_shared/analysis/registration_checks.py` == 0.
    - `python -c` check: `len(KEYPOINT_LABEL_KO) == 12` and keys equal `keypoint_frame._KEYPOINT_NAMES`.
  </acceptance_criteria>
  <done>세 판정이 순수 함수로 있고 측정 불가가 위반보다 먼저 잡히며, 합성 입력으로 각각 강제되고 hold_height 회귀가 0 이다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `RTMWPoseEngine.estimate_with_person_counts` — 프레임별 사람 수를 반환값으로(사이드카 0), `estimate()` byte-동일 + mock 테스트(N=0 → NoHumanError, N=2 → counts)</name>
  <files>backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/rtmw_engine.py, backend/tests/test_rtmw_engine.py</files>
  <behavior>
    - mock 이 매 프레임 `(np.zeros((2,133,2)), np.full((2,133),0.9))` 를 돌려주면 `estimate_with_person_counts(frames(5), pole)` → `counts == [2,2,2,2,2]`, `len(pose_frames) == 5`
    - mock 이 `(np.zeros((0,133,2)), np.zeros((0,133)))` 이면 `NoHumanError`(기존 `estimate` 와 같은 예외)
    - mock 이 프레임별로 `None`/1명/1명 을 섞어 주면 counts `[0,1,1]`
    - N=1 mock 에서 `estimate(frames, pole)` 와 `estimate_with_person_counts(frames, pole)[0]` 의 `keypoints_2d`·confidence 가 프레임마다 동일(np.array_equal)
    - `T == 0` → `([], [])`
  </behavior>
  <read_first>
    - backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/rtmw_engine.py :169-193 (`create_with_inferencer`) · :195-252 (`estimate` 본문 + 2-pass 디스패처 `_maybe_second_pass_rot180` / `_maybe_second_pass_inversion`) · :254-285 (`_infer_raw` — N 이 버려지는 :274-276) · :287-320 (`_build_pose_frames` local-return tuple)
    - backend/tests/test_rtmw_engine.py :1-100 (`default_pole_axis` · `mock_inferencer` · `mock_frames` · `real_manifest_path` fixture) — 이 파일이 로컬 `.venv` 에서 실제로 도는지 먼저 `pytest tests/test_rtmw_engine.py -q` 로 확인(rtmlib 미설치여도 `create_with_inferencer` 경로는 돌아야 한다; 안 돌면 원인을 SUMMARY 에 `[확인]`)
    - .planning/phases/38-supplier-link/38-PATTERNS.md "### `…/rtmw_engine.py` (adapter — `estimate_with_person_counts`)" 플래너 메모 · RESEARCH Q5 (b)
  </read_first>
  <action>
    RED 먼저(위 behavior 를 `test_rtmw_engine.py` 에 추가) → 구현:
    `_infer_raw` 를 복제하지 말고 `_infer_raw_with_counts(frames) -> tuple[list[tuple[np.ndarray, np.ndarray] | None], list[int]]` 로 분리 — 프레임마다 `n = 0 if kps_batch is None else int(len(kps_batch))` 를 `counts` 에 append, 나머지 로직(첫 사람 선택·형상 처리) 은 원문 그대로; `_infer_raw(frames)` 는 `return self._infer_raw_with_counts(frames)[0]` 한 줄(회귀 0).
    `estimate()` 본문을 `_estimate_impl(frames, pole_axis, *, with_counts: bool)` 로 옮기고 `estimate()` 는 `return self._estimate_impl(frames, pole_axis, with_counts=False)`; `estimate_with_person_counts(frames, pole_axis) -> tuple[list[PoseFrame], list[int]]` 는 `with_counts=True` 로 같은 impl(같은 `_build_pose_frames`, 같은 2-pass 디스패처 — 좌표가 `estimate()` 와 같아야 mode1 양쪽 원질이 같다). `T == 0` 이면 `([], [])`. NoHumanError 조건·메시지 원문 유지. docstring 에 "HIGH-1 v4 — local-return tuple, 인스턴스 사이드카 금지(Pod BackgroundTasks 동시 분석)" 와 Phase 38 D-09(여러 명) 인용.
    NEVER: `estimate()` 시그니처·예외·2-pass 순서 변경, 모듈 전역에 카운트 저장.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && .venv/bin/python -m pytest tests/test_rtmw_engine.py tests/test_rtmw_engine_rot180.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && .venv/bin/python -m pytest tests/test_rtmw_engine.py tests/test_rtmw_engine_rot180.py -q` exits 0 with ≥ 4 new tests.
    - `grep -c "def estimate_with_person_counts\|def _infer_raw_with_counts\|def _estimate_impl" backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/rtmw_engine.py` == 3.
    - `grep -c "self._person_counts\|self.person_counts" backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/rtmw_engine.py` == 0 (사이드카 없음).
    - The `estimate(` public signature line is unchanged in `git diff`.
  </acceptance_criteria>
  <done>프레임별 N 이 반환값으로 나오고 `estimate()` 는 같은 좌표를 낸다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 영상 프레임 → RTMW inferencer → 판정 | 입력 영상은 공급자가 올린 임의 파일(사람 수·조명 통제 불가) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-05-1 | Tampering | `check_registration` | mitigate | 순수 함수 fail-closed(못 재면 False/실패) — 형상 불량 report 로 통과 못 함 |
| T-38-05-2 | DoS | `estimate_with_person_counts` | accept | 프레임 수는 상류 100MB·5~30초 검사로 상한(38-01/38-07) |
| T-38-05-3 | Information disclosure | `KEYPOINT_LABEL_KO` joints 목록 | accept | 부위명만 — 개인정보 없음 |
| T-38-05-4 | Tampering(오분류) | `standing_start_ok` 바닥 규칙 | accept(한계 명시) | 리뷰 R6: 바닥 일관성은 서 있음의 PROXY — 웅크린 시작도 통과(위음성). 실영상 오분류율 [미확인]. 이 phase 에 분류기 없음; 가이드 ④ "서 있는 자세인지는 사람이 확인해 주세요"(D-16) + 시험 영상 2차로 실측 |
| T-38-SC | Tampering | npm/pip installs | accept | 설치 0 |
</threat_model>

<verification>
- `cd backend && .venv/bin/python -m pytest tests/test_registration_checks.py tests/test_hold_height.py tests/test_rtmw_engine.py tests/test_rtmw_engine_rot180.py -q` → 0 failed.
- 플랜 끝 push.
</verification>

<success_criteria>
- 실패 4형 중 셋(여러 명·서 있는 시작·저신뢰)이 순수 함수로, 사람 미검출이 엔진 예외로 각각 합성 강제된다(Success ② 의 재료).
- 엔진 `estimate()` 무회귀.
</success_criteria>

<output>
Create `.planning/phases/38-supplier-link/38-05-SUMMARY.md` when done (문턱 3개는 `[ASSUMED]` 로 명시; **한계 절 필수(리뷰 R6):** "바닥 일관성 규칙은 서 있음의 PROXY — 웅크린 합성 좌표 통과 [확인 test_floor_rule_is_a_proxy_crouch_passes], 실영상 오분류율 [미확인], 분류기 추가는 이 phase 범위 밖").
</output>
