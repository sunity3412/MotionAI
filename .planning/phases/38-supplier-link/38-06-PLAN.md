---
phase: 38-supplier-link
plan: 06
type: execute
wave: 2
depends_on: ["38-01"]
files_modified:
  - backend/shared/python/sunity_shared/firestore_admin.py
  - backend/tests/test_firestore_reference_writers.py
  - backend/functions/reference-upload-url/app.py
  - backend/functions/reference-upload-url/requirements.txt
  - backend/tests/test_reference_upload_url_handler.py
autonomous: true
requirements: [REQ-38-1, REQ-38-3, REQ-38-5]
must_haves:
  truths:
    - "D-04: `POST /reference/upload-url` 이 토큰 uid + 서버 생성 refId 로만 `reference/{uid}/{refId}.{ext}` presigned PUT(900s) 을 발급하고, 발급 **전에** `reference/{refId}` doc 을 `create()` 로 선작성한다(`registrationStatus:'registering'`, `isActive:false`, 폼 메타·선언 4·동의)"
    - "D-03: 공급자 화이트리스트 = SSM `/sunity/motion/supplier-uids`(런타임 읽기, 60초 캐시) ∪ `BELLE_UID`; 파라미터 없음/env 미설정 = 403(조용한 통과 금지)"
    - "D-05 / Success ④: angles 를 쓰는 유일한 writer `set_reference_angles` 는 legacy `ref-*` id 를 거부하고 길이 `frames × len(jointKeys)` 를 검사한다; 선작성은 `create()` 라 기존 11개를 구조적으로 못 건드린다"
    - "D-08: 동의는 서버 시각·버전·uid 와 함께 doc 에 남는다(부인 방지)"
    - "D-12: `{\"probe\": true}` 호출이 화이트리스트 판정 + `supplierCode` 를 돌려준다(페이지 '내 코드' 카드 재료)"
  artifacts:
    - path: "backend/shared/python/sunity_shared/firestore_admin.py"
      provides: "create_reference_registration · get_reference_registration · set_reference_registration_status · set_reference_angles · set_reference_self_check · create_analysis_doc · list_reference_registrations_by_status"
      contains: "def set_reference_angles"
    - path: "backend/functions/reference-upload-url/app.py"
      provides: "lambda_handler — 인증 → 화이트리스트 → (probe | 검증 → doc create → presign)"
      contains: "generate_presigned_url"
  key_links:
    - from: "backend/functions/reference-upload-url/app.py"
      to: "sunity_shared.s3keys.build_reference_upload_key"
      via: "키 구성은 토큰 uid + uuid4 hex 만"
      pattern: "build_reference_upload_key"
    - from: "backend/functions/reference-upload-url/app.py"
      to: "firestore_admin.create_reference_registration"
      via: "presign 전 doc.create()"
      pattern: "create_reference_registration"
---

<objective>
Firestore ADD-only writer 세트(D-05·D-08·D-19)와 `reference/` presigned PUT 입구 Lambda(D-03·D-04·REQ-38-1·REQ-38-3)를 만든다. 둘 다 AWS/Firestore 없이 monkeypatch 로 닫히는 pytest 가 게이트다.

Purpose: 지금 presigned PUT 은 `uploads/*` 만이고(`template.yaml:194`) `firestore.rules` 가 `reference/**` 쓰기를 막아 브라우저는 못 쓴다 — Admin SDK 를 가진 Lambda 만이 doc 을 선작성할 수 있다(RESEARCH 책임 지도). `create()` 선작성 + `ref-` 가드가 Success ④(기존 11개 무접촉)의 구조적 보장이다.
Output: `firestore_admin.py` 함수 7개 · 새 Lambda 디렉터리 · 테스트 2파일.

Phase 38 전제(38-01 머리말) 적용. 배포는 이 플랜에서 하지 않는다(38-09 체크포인트).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/38-supplier-link/38-CONTEXT.md
@.planning/phases/38-supplier-link/38-RESEARCH.md
@.planning/phases/38-supplier-link/38-PATTERNS.md
@.planning/phases/38-supplier-link/38-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `firestore_admin` 등록 writer 7종 — `create()` 선작성 · 상태 · angles(ADD-only, legacy 가드) · 자기 재현성 · 분석 doc 선작성 · queued 조회 + `_FakeDoc` 테스트</name>
  <files>backend/shared/python/sunity_shared/firestore_admin.py, backend/tests/test_firestore_reference_writers.py</files>
  <behavior>
    - `create_reference_registration("abc123…", supplier_uid="u1", form=<ReferenceUploadRequest>, video_s3_key="reference/u1/abc123….mp4", consent_at_ms=1700000000000, supplier_code="EUNJI")` → `_doc(path).create(payload)` 가 1회 호출(`set` 아님)되고 payload 에 `motionId`·`supplierUid`·`supplierCode`·`source:"supplier-link"`·`name`·`athleteName`·`level`·`techniqueRefId`·`isCombo`·`isSplit`·`hasHold`·`standingStart`·`clipRange`(None 이면 키 없음)·`consent{portrait:True,usage:True,silent:True,training,version:CONSENT_VERSION,at,uid}`·`videoS3Key`·`isActive:False`·`registrationStatus:"registering"`·`registrationUpdatedAt`·`createdAt`·`updatedAt` 가 있고 nested list 가 없다
    - 모든 writer 는 `ref_id` 가 빈 문자열이거나 `"ref-"` 로 시작하면 `ValueError`(legacy 11개 가드) — 7함수 parametrize
    - `set_reference_angles(ref_id, angles_flat=[…], joint_keys=[8개], frames=T, real_fps=10.0, keypoint_report={…flat…}, split_angle=None)` → `set(payload, merge=True)`, payload 키 정확히 `{angles, anglesJointKeys, anglesFrames, anglesUpdatedAt, anglesRealFps, referenceKeypointReport}`(+`referenceSplitAngle` 일 때만); `len(angles_flat) != frames*len(joint_keys)` → `ValueError`; keypoint_report 에 nested list → `TypeError`
    - `set_reference_registration_status(ref_id, "failed", error={"code":"low_confidence","message":"…","joints":["왼쪽 발목"]})` → payload `{registrationStatus:"failed", registrationError:{…}, registrationUpdatedAt, updatedAt}`; `"queued"` + `queued_reason="pod_down"` → `queuedReason` 포함; `"active"` → `extra={"isActive": True}` 병합; 미정의 status → `ValueError`
    - `set_reference_self_check(ref_id, status="done", analysis_id="a1", score=97.3)` → `{selfCheckStatus:"done", selfCheckAnalysisId:"a1", selfScore:97.3, selfScoreUpdatedAt}`; `status="pending"` 은 score 키 없음; 미정의 status → `ValueError`
    - `create_analysis_doc("u1","a1",{…})` → `users/u1/analyses/a1` 경로에 `create()` 1회, payload 에 `analysisId·mode·status` 없으면 `ValueError`
    - `get_reference_registration(ref_id)` → `_doc(path).get()` 의 dict(없으면 None) — 버전 포인터 로직 **없이** top-level 원본
    - `list_reference_registrations_by_status("queued")` → 컬렉션 `reference` 에 `where("registrationStatus","==","queued")` 단일 쿼리(전수 스캔 없음)
  </behavior>
  <read_first>
    - backend/shared/python/sunity_shared/firestore_admin.py :19-43 (`_db`/`_doc` seam · `update_analysis_status`) · :46-102 (`_validate_flat_dict_no_nested_array`) · :2173-2261 (`update_reference_body_data` ADD-only 골격 + "angles 절대 금지" 주석) · :2368-2443 (`set_reference_motion_with_gemini` 존재 분기) · :2446-2469 (`fail_analysis` · `get_analysis`) · :2558-2609 (`get_reference_motion` 포인터 폴백)
    - backend/tests/test_reference_auto_register_handler.py :364-392 (`_FakeDoc` + `_doc` monkeypatch) · backend/tests/test_analysis_provenance.py :308-325 (`_FakeDocStore`)
    - backend/shared/python/sunity_shared/models.py (38-01 — REGISTRATION_STATUSES · SELF_CHECK_STATUSES · CONSENT_VERSION · reference_motion_path · analysis_doc_path · REFERENCE_MOTIONS_COLLECTION)
    - backend/shared/python/sunity_shared/validation.py (38-01 — `ReferenceUploadRequest` 필드)
    - app/src/app/analysis/loading.tsx :159-178 (앱이 만드는 분석 doc 형상 — `create_analysis_doc` 정본)
    - .planning/phases/38-supplier-link/38-PATTERNS.md "### `…/firestore_admin.py`" 절 + Shared Patterns §5 · RESEARCH 코드 예시 C
  </read_first>
  <action>
    RED 먼저(`test_firestore_reference_writers.py`, `_FakeDoc` 은 `create`·`set`·`get` 을 기록; `_FakeCollection` 은 `where` 인자를 기록) → 구현. 파일 끝에 블록 주석 `# ── Phase 38 (D-04·D-05·D-08·D-10·D-19) — 공급자 링크 등록 writer ──` + "이 블록의 `set_reference_angles` 는 :2252-2261 'angles 절대 금지' 규칙의 **유일한 예외**이며 legacy `ref-*` id 를 거부한다" 문장.
    공통 가드 `_require_registration_ref_id(ref_id)`: 빈 값 또는 `startswith("ref-")` → `ValueError("ref_id invalid or legacy (ref-*)")`. 모든 payload 는 `_validate_flat_dict_no_nested_array(payload, path=...)` 를 지나고 `now_ms = int(time.time()*1000)` 로 `*UpdatedAt`·`updatedAt`.
    - `create_reference_registration(ref_id, *, supplier_uid, form, video_s3_key, consent_at_ms, supplier_code=None)` — `form` 은 `ReferenceUploadRequest`(속성 접근); `clip_range` 튜플은 `{"execStartS", "execEndS"}` dict 로; `doc.create(payload)`(firebase-admin: 존재하면 `AlreadyExists` 전파 — docstring 에 "원자적, `snap.exists` 분기와 달리 경합 없음, 리포 첫 `create()` 사용" 명시). 로그 `create_reference_registration ok ref_id=%s supplier_uid=%s`.
    - `get_reference_registration(ref_id) -> dict | None`.
    - `set_reference_registration_status(ref_id, status, *, error=None, queued_reason=None, extra=None)` — `status ∉ models.REGISTRATION_STATUSES` → `ValueError`; error dict 는 `{code, message}` 필수 + `joints` list[str] 선택.
    - `set_reference_angles(...)` — RESEARCH 코드 예시 C 그대로 + `anglesRealFps` float 필수(0/None 거부 — `_reference_angles_fps` 우선순위, 메모리 fps-label).
    - `set_reference_self_check(ref_id, *, status, analysis_id=None, score=None)` — `status ∉ SELF_CHECK_STATUSES` → `ValueError`; `done` 이면 `selfScore` float 필수.
    - `create_analysis_doc(uid, analysis_id, payload)` — `models.analysis_doc_path`; 필수 키 `analysisId`·`mode`·`status`; `doc.create(payload)`.
    - `list_reference_registrations_by_status(status) -> list[dict]` — `_db().collection(models.REFERENCE_MOTIONS_COLLECTION).where("registrationStatus", "==", status).stream()` (firebase-admin 의 `FieldFilter` 권장 형식이 있으면 그것; 읽기 수 = 대기 건수, Spark 5만/일 캡 주석).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && .venv/bin/python -m pytest tests/test_firestore_reference_writers.py tests/test_reference_auto_register_handler.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && .venv/bin/python -m pytest tests/test_firestore_reference_writers.py tests/test_reference_auto_register_handler.py -q` exits 0; new file collects ≥ 12 tests.
    - `grep -c "^def create_reference_registration\|^def get_reference_registration\|^def set_reference_registration_status\|^def set_reference_angles\|^def set_reference_self_check\|^def create_analysis_doc\|^def list_reference_registrations_by_status" backend/shared/python/sunity_shared/firestore_admin.py` == 7.
    - `grep -c '\.create(' backend/shared/python/sunity_shared/firestore_admin.py` ≥ 2 (registration + analysis doc).
    - `grep -c 'startswith("ref-")' backend/shared/python/sunity_shared/firestore_admin.py` ≥ 1.
    - `git diff` shows `update_reference_body_data` / `update_reference_downstream_data` / `get_reference_motion` untouched.
  </acceptance_criteria>
  <done>등록 doc 생명주기(registering→queued/processing→failed/active, selfCheck) 를 쓰는 writer 가 전부 있고 legacy 가드·flat 검증이 테스트로 잠긴다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `reference-upload-url` Lambda — 인증 → SSM 화이트리스트(∪ BELLE_UID) → probe | 검증 → doc `create()` → presigned PUT `reference/{uid}/{refId}.{ext}` + monkeypatch 테스트</name>
  <files>backend/functions/reference-upload-url/app.py, backend/functions/reference-upload-url/requirements.txt, backend/tests/test_reference_upload_url_handler.py</files>
  <behavior>
    - `verify_request` 가 `AuthError` → 401 `{error:{code:"unauthorized"}}`
    - 화이트리스트 map `{}` 이고 `BELLE_UID` 비어 있음 → 403 `forbidden` (env 미설정 = 거부)
    - uid 가 map 에도 BELLE_UID 에도 없음 → 403; BELLE_UID 와 같으면 통과
    - body `{"probe": true}` + 화이트리스트 통과 → 200 `{probe:true, uid:"u1", supplierCode:"EUNJI"}`(코드 없으면 `null`); doc create·presign 호출 0
    - 정상 body → `create_reference_registration` 가 `(ref_id, supplier_uid="u1"(토큰), form, video_s3_key="reference/u1/{ref_id}.mp4", consent_at_ms=<int>, supplier_code=...)` 로 1회, 그 **뒤** `_s3.generate_presigned_url(ClientMethod="put_object", Params={"Bucket","Key"}, ExpiresIn=900)` 1회, 응답 200 `{refId, uploadUrl, s3Key, expiresInSec:900}`; `refId` 는 `validate_analysis_id_format` 통과(uuid4 hex)
    - body 에 `"uid":"evil"` 또는 `"refId":"ref-kip-up"` 이 있어도 키·doc 은 토큰 uid·서버 refId 로만(`"evil"`/`"ref-kip-up"` 이 어떤 호출 인자에도 없다)
    - `validate_reference_upload_request` 가 `ValidationError("too_long", …)` → 400 `{error:{code:"too_long", message:REGISTRATION_ERROR_MESSAGE["too_long"]}}`
    - `create_reference_registration` 예외 → 500 `server_error`, presign 호출 0; presign 예외 → 500 `server_error`
    - SSM `get_parameter` 가 예외(ParameterNotFound/AccessDenied) → map `{}` + warning 로그(값 로그 금지) → BELLE_UID 외 전부 403
  </behavior>
  <read_first>
    - backend/functions/upload-url/app.py (72줄 전체 — 골격) · backend/functions/upload-url/requirements.txt
    - backend/functions/reference-auto-register/app.py :40-60 · :85-100 (화이트리스트 · env 미설정 거부) · :160-180 (Firestore 실패 매핑)
    - backend/shared/python/sunity_shared/auth.py :55-70 (SSM `get_parameter` 어법 — boto3 lazy import) · responses.py (`ok`/`error`/`parse_json_body`)
    - backend/tests/test_reference_auto_register_handler.py :25-56 (handler_module fixture) · :94-115 (`_FakeS3`) · :121-175 (happy path)
    - backend/shared/python/sunity_shared/models.py (38-01 — `parse_supplier_uids` · `SUPPLIER_UIDS_PARAM_DEFAULT` · `REGISTRATION_ERROR_MESSAGE`) · validation.py (38-01) · s3keys.py (38-01) · firestore_admin.py (Task 1)
    - .planning/phases/38-supplier-link/38-PATTERNS.md "### `backend/functions/reference-upload-url/app.py`" 절 + Shared Patterns §1·§2·§3·§4 · RESEARCH "## Security Domain"
  </read_first>
  <action>
    RED 먼저(`test_reference_upload_url_handler.py`: `_HANDLER_DIR` fixture 어법 복제, env `VIDEO_BUCKET`·`BELLE_UID`·`SUPPLIER_UIDS_PARAM`, `verify_request`·`_load_supplier_map`·`_s3`·`create_reference_registration` monkeypatch) → 구현.
    `requirements.txt` 는 `upload-url/requirements.txt` 복사(boto3 는 런타임 제공, firebase-admin 은 layer — 실제 내용은 원본 파일을 열어 그대로).
    `app.py` 모듈 상수: `_BUCKET = os.environ["VIDEO_BUCKET"]`, `_EXPIRES = 900`, `_BELLE_UID = os.environ.get("BELLE_UID","")`, `_SUPPLIER_UIDS_PARAM = os.environ.get("SUPPLIER_UIDS_PARAM", models.SUPPLIER_UIDS_PARAM_DEFAULT)`, `_SUPPLIER_CACHE_TTL_S = 60`, `_s3 = boto3.client("s3")`. `_load_supplier_map() -> dict[str, str | None]`: 모듈 캐시 `(value, loaded_at)` 60초; `boto3.client("ssm").get_parameter(Name=_SUPPLIER_UIDS_PARAM)["Parameter"]["Value"]` → `models.parse_supplier_uids`; 예외는 `log.warning("supplier-uids 조회 실패 param=%s", name)`(값·예외 본문 로그 금지) 후 `{}`. `_is_supplier(uid, m) -> bool` = `uid in m or (bool(_BELLE_UID) and uid == _BELLE_UID)`.
    `lambda_handler(event, _context)`: 1 `verify_request` → 401 · 2 `body = responses.parse_json_body(event)`; `m = _load_supplier_map()`; `not _is_supplier(uid, m)` → 403 `forbidden` "공급자로 등록된 계정만 쓸 수 있어요." + warning 로그(uid, `supplier_count=len(m)`, `belle_uid_set=bool(_BELLE_UID)`) · 3 `body.get("probe") is True` → `responses.ok({"probe": True, "uid": uid, "supplierCode": m.get(uid)})` · 4 `req = validate_reference_upload_request(body)` → `ValidationError` 매핑(`e.code`, `e.message`, `e.http_status`) · 5 `ref_id = uuid.uuid4().hex`; `s3_key = build_reference_upload_key(uid, ref_id, req.fmt)` · 6 `firestore_admin.create_reference_registration(ref_id, supplier_uid=uid, form=req, video_s3_key=s3_key, consent_at_ms=int(time.time()*1000), supplier_code=m.get(uid))` — 예외 `# noqa: BLE001` → `log.exception` + 500 `server_error` "등록 문서 생성에 실패했어요." · 7 presign(`upload-url` 과 동일 Params, ContentType 미포함 이유 주석 유지) — 예외 → 500 · 8 `log.info("reference-upload-url ok uid=%s ref_id=%s level=%s combo=%s", ...)` → `responses.ok({"refId", "uploadUrl", "s3Key", "expiresInSec"})`. 헤더 docstring: contract.md §2 `POST /reference/upload-url` · Phase 38 D-03/D-04/D-08 · "본문의 uid/refId 는 절대 읽지 않는다(V4)".
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && .venv/bin/python -m pytest tests/test_reference_upload_url_handler.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && .venv/bin/python -m pytest tests/test_reference_upload_url_handler.py -q` exits 0 with ≥ 10 tests.
    - `test -f backend/functions/reference-upload-url/app.py && test -f backend/functions/reference-upload-url/requirements.txt`.
    - `grep -c 'body.get("uid")\|body\["uid"\]\|body.get("refId")' backend/functions/reference-upload-url/app.py` == 0.
    - `grep -c "create_reference_registration" backend/functions/reference-upload-url/app.py` ≥ 1 and it appears **before** `generate_presigned_url` in file order (`grep -n` line numbers).
    - `grep -c 'ExpiresIn=_EXPIRES' backend/functions/reference-upload-url/app.py` == 1 and `_EXPIRES = 900`.
  </acceptance_criteria>
  <done>입구 Lambda 가 얇은 핸들러 규율로 존재하고 화이트리스트·선작성·서명 순서가 테스트로 잠긴다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 폰 브라우저 → API Gateway → Lambda | Bearer ID 토큰 + JSON 본문(신뢰 불가) |
| Lambda → Firestore Admin | 규칙 우회 쓰기(Admin SDK) — 서버만 |
| Lambda → SSM | 화이트리스트 원본 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-06-1 | Spoofing | `lambda_handler` uid | mitigate | `verify_request`(firebase-admin `verify_id_token`) 의 uid 만; 본문 uid 무시(테스트 "evil") |
| T-38-06-2 | Elevation | 화이트리스트 | mitigate | SSM map ∪ BELLE_UID, 파라미터 부재/env 미설정 = 403; 익명 uid 등록 금지는 운영 규칙(SUMMARY·supplier-guide 에 명시) |
| T-38-06-3 | Tampering | S3 키 | mitigate | `build_reference_upload_key(token_uid, uuid4hex, fmt)` 만; presign 정책은 `reference/*` PutObject 한정(38-09 template) |
| T-38-06-4 | Tampering | 기존 11개 doc | mitigate | `doc.create()`(존재 시 실패) + 모든 writer 의 `ref-` 가드 |
| T-38-06-5 | Repudiation | consent | mitigate | `consent.at`(서버 ms)·`version`·`uid` 를 Lambda 가 기록 |
| T-38-06-6 | Information disclosure | 로그 | mitigate | SSM 값·토큰·예외 본문 로그 금지, key=value 만 |
| T-38-06-7 | DoS | presign 남발 | accept | 화이트리스트 1~2 uid, 100MB·5~30초 검증, API GW 기본 스로틀 |
| T-38-SC | Tampering | pip installs | accept | requirements.txt 는 기존 파일 복사(신규 패키지 0) |
</threat_model>

<verification>
- `cd backend && .venv/bin/python -m pytest tests/test_firestore_reference_writers.py tests/test_reference_upload_url_handler.py tests/test_reference_auto_register_handler.py -q` → 0 failed.
- 플랜 끝 push.
</verification>

<success_criteria>
- writer 7종 + 입구 Lambda 가 순수 테스트로 닫혔고, 기존 11개 doc 은 구조적으로 못 건드린다.
</success_criteria>

<output>
Create `.planning/phases/38-supplier-link/38-06-SUMMARY.md` when done.
</output>
