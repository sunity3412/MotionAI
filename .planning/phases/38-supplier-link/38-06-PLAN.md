---
phase: 38-supplier-link
plan: 06
type: execute
wave: 2
depends_on: ["38-01"]
files_modified:
  - backend/shared/python/sunity_shared/firestore_admin.py
  - backend/tests/test_firestore_reference_writers.py
  - backend/functions/reference-upload-url/app.py
  - backend/functions/reference-upload-url/requirements.txt
  - backend/tests/test_reference_upload_url_handler.py
  - firestore.rules
  - backend/scripts/deploy_firestore_rules.py
  - backend/tests/test_deploy_firestore_rules.py
  - backend/tests/firestore_rules_cases.json
autonomous: true
requirements: [REQ-38-1, REQ-38-3, REQ-38-4, REQ-38-5]
must_haves:
  truths:
    - "D-04 (리뷰 R4·R5·R13): `POST /reference/upload-url` 이 토큰 uid + 서버 생성 refId 로만 `reference/{uid}/{refId}/upload.{ext}` presigned PUT(900s) 을 서명하고, 서명이 성공한 뒤 공개 `reference/{refId}` + 비공개 `reference/{refId}/private/registration` 두 doc 을 **한 batch `create()`** 로 선작성한다(`registrationStatus:'registering'`, `isActive:false`, `uploadKey`, `uploadExpiresAt` = 서명 만료와 같은 값; 동의·선언은 비공개 doc) — create 가 실패하면 URL 은 응답에 실리지 않는다(고아 객체 없음)"
    - "리뷰 R3: `claim_registration(ref_id, job_id, lease_sec)` 트랜잭션이 `registering|queued` 또는 lease 만료 `processing` 에서만 성공하고 `processing·jobId·leaseUntil` 을 쓴다; `set_registration_active/failed`·`set_reference_angles`·`begin_self_check` 는 `doc.jobId == job_id` 일 때만 쓴다(stale job = no-op + 로그) — 동시 이벤트 2·requeue 2회·늦은 stale 실패가 FakeFirestore(실제 CAS)로 잠긴다"
    - "리뷰 R2·R8: 자기 재현성은 기준 doc 이 권위 — `begin_self_check` 가 `selfCheckAnalysisId·selfCheckStatus=pending·selfCheckJobId` 를 **먼저** 쓰고, `self_check_authorized(ref_doc, uid, analysis_id, job_id)` 하나가 성공/실패 훅과 writer 를 지킨다(위조 표식·다른 공급자 refId·stale analysis id·pending 전 완료 훅 = 전부 스킵)"
    - "리뷰 R2·R13 규칙: `firestore.rules` 가 `reference/{refId}`(인증자 읽기)·`reference/{refId}/private/{doc}`(supplierUid 본인만 읽기)·`users/{uid}/analyses` 의 클라이언트 `selfCheckForReference·selfCheckJobId` create/update 거부를 담고, Rules REST `projects:test` 케이스 파일이 ALLOW/DENY 를 잠근다(배포는 38-09 체크포인트 안)"
    - "D-03: 공급자 화이트리스트 = SSM `/sunity/motion/supplier-uids`(런타임 읽기, 60초 캐시) ∪ `BELLE_UID`; 파라미터 없음/env 미설정 = 403(조용한 통과 금지)"
    - "D-05 / Success ④: angles 를 쓰는 유일한 writer `set_reference_angles` 는 legacy `ref-*` id 를 거부하고 길이 `frames × len(jointKeys)` 를 검사한다; 선작성은 `create()` 라 기존 11개를 구조적으로 못 건드린다"
    - "D-08: 동의는 서버 시각·버전·uid 와 함께 doc 에 남는다(부인 방지)"
    - "D-12: `{\"probe\": true}` 호출이 화이트리스트 판정 + `supplierCode` 를 돌려준다(페이지 '내 코드' 카드 재료)"
  artifacts:
    - path: "backend/shared/python/sunity_shared/firestore_admin.py"
      provides: "create_reference_registration(공개+비공개 batch create) · get_reference_registration · get_reference_registration_private · claim_registration · set_registration_queued · set_registration_expired · set_registration_active · set_registration_failed · set_reference_angles(job 가드) · begin_self_check · self_check_authorized(순수) · set_reference_self_check(가드) · create_analysis_doc · list_reference_registrations_by_status"
      contains: "def claim_registration"
    - path: "firestore.rules"
      provides: "reference 공개/비공개 분리 + selfCheck 표식 클라이언트 쓰기 거부(R2·R13)"
      contains: "private/{doc}"
    - path: "backend/scripts/deploy_firestore_rules.py"
      provides: "Rules REST — --test(projects:test) · --current · --release(rulesets+releases) · 순수 body 빌더"
      contains: "firebaserules.googleapis.com"
    - path: "backend/functions/reference-upload-url/app.py"
      provides: "lambda_handler — 인증 → 화이트리스트 → (probe | 검증 → doc create → presign)"
      contains: "generate_presigned_url"
  key_links:
    - from: "backend/functions/reference-upload-url/app.py"
      to: "sunity_shared.s3keys.build_reference_upload_key"
      via: "키 구성은 토큰 uid + uuid4 hex 만"
      pattern: "build_reference_upload_key"
    - from: "backend/functions/reference-upload-url/app.py"
      to: "firestore_admin.create_reference_registration"
      via: "presign 성공 뒤 batch create(공개+비공개) — create 실패면 URL 미응답"
      pattern: "create_reference_registration"
---

<objective>
Firestore ADD-only writer 세트(D-05·D-08·D-19)와 `reference/` presigned PUT 입구 Lambda(D-03·D-04·REQ-38-1·REQ-38-3), 그리고 **리뷰가 요구한 세 보장**을 만든다 — (R3) 트랜잭션 claim/lease 로 등록 작업 소유권, (R2·R8) 기준 doc 이 권위인 자기 재현성 가드·선기록, (R13) 공개/비공개 doc 분리 + `firestore.rules`(클라이언트 표식 쓰기 거부 포함, R2). 전부 AWS/Firestore 없이 FakeFirestore(실제 optimistic concurrency, `backend/tests/phase31/conftest.py`)·monkeypatch 로 닫히는 pytest 가 게이트다. 규칙의 실배포는 38-09 체크포인트 안(`deploy_firestore_rules.py --release`), 여기서는 파일·테스트 케이스·순수 스크립트까지.

Purpose: 지금 presigned PUT 은 `uploads/*` 만이고(`template.yaml:194`) `firestore.rules` 가 `reference/**` 쓰기를 막아 브라우저는 못 쓴다 — Admin SDK 를 가진 Lambda 만이 doc 을 선작성할 수 있다(RESEARCH 책임 지도). `create()` 선작성 + `ref-` 가드가 Success ④(기존 11개 무접촉)의 구조적 보장이다.
Output: `firestore_admin.py` 함수 14개(writer 13 + 순수 가드 1) · 새 Lambda 디렉터리 · `firestore.rules` · `deploy_firestore_rules.py` + 케이스 JSON · 테스트 3파일.

**리뷰 반영(2026-09-26):** R2(권위 가드 + 규칙) · R3(claim/lease/job 가드) · R4(presign 성공 뒤 create, `uploadExpiresAt`, `expired` 전이) · R5(upload 키만 서명, `videoS3Key` 는 활성화 때 v1) · R8(`begin_self_check` 선기록) · R13(비공개 서브문서 + 규칙 좁히기). 플래너 결정: 규칙은 "추가" 만으로는 무효(기존 `reference/{document=**}` 가 서브문서까지 인증자 전체 읽기) → 재귀 와일드카드를 좁힌다; firebase CLI 부재 → Rules REST API 를 기존 Admin SA 로 부른다(`google-auth`·`requests` 는 `.venv` 에 있음 [확인]).

Phase 38 전제(38-01 머리말) 적용. 배포는 이 플랜에서 하지 않는다(38-09 체크포인트).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/38-supplier-link/38-CONTEXT.md
@.planning/phases/38-supplier-link/38-RESEARCH.md
@.planning/phases/38-supplier-link/38-PATTERNS.md
@.planning/phases/38-supplier-link/38-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `firestore_admin` 등록 writer 13종 + 순수 가드 1 — 공개/비공개 두 doc batch `create()`(R13) · 트랜잭션 `claim_registration`/lease(R3) · job 가드 `set_registration_active/failed`·`set_reference_angles`(R3) · `set_registration_queued/expired`(R4) · `begin_self_check` 선기록 + `self_check_authorized`(R2·R8) · 분석 doc 선작성 · 상태 조회 + FakeFirestore(실제 CAS) 테스트</name>
  <files>backend/shared/python/sunity_shared/firestore_admin.py, backend/tests/test_firestore_reference_writers.py</files>
  <behavior>
    - `create_reference_registration("abc123…", supplier_uid="u1", form=<ReferenceUploadRequest>, upload_key="reference/u1/abc123…/upload.mp4", upload_expires_at_ms=<now+900000>, consent_at_ms=1700000000000, supplier_code="EUNJI")` → `_db().batch()` 에 `create` 2회(공개 `reference/{refId}` · 비공개 `reference/{refId}/private/registration`, `models.reference_private_path`) 뒤 `commit()` 1회(`set` 0회). 공개 payload 키 정확히: `motionId·supplierUid·supplierCode·source:"supplier-link"·name·athleteName·level·uploadKey·uploadExpiresAt·isActive:False·registrationStatus:"registering"·jobId:None·leaseUntil:None·registrationUpdatedAt·createdAt·updatedAt`(`videoS3Key` 없음 — 활성화 때 v1 로; `consent`·선언·`techniqueRefId`·`clipRange` 없음). 비공개 payload: `supplierUid·consent{portrait:True,usage:True,silent:True,training,version:CONSENT_VERSION,at,uid}·techniqueRefId·isCombo·isSplit·hasHold·standingStart·clipRange({execStartS,execEndS} 또는 키 없음)·updatedAt`. 둘 다 nested list 없음
    - 모든 writer 는 `ref_id` 가 빈 문자열이거나 `"ref-"` 로 시작하면 `ValueError`(legacy 11개 가드) — 13함수 parametrize
    - `claim_registration(ref_id, job_id="A", lease_sec=900, now_ms=T0)`: doc 없음 → False; `registering` → True 이고 doc 이 `{registrationStatus:"processing", jobId:"A", leaseUntil:T0+900000, registrationUpdatedAt, updatedAt}`; 곧바로 `claim_registration(ref_id, "B", now_ms=T0+1000)` → **False**(동시 이벤트 2 = 한 번만 실행); `now_ms=T0+900001` 로 "B" → True(lease 만료 재claim, 로그에 old jobId); `queued` 에서 → True; `active`/`failed`/`expired` 에서 → False. FakeFirestore 의 CAS 로 "같은 문서를 읽은 두 트랜잭션 중 하나만 성공" 을 실제 conflict 로 한 번 더(phase31 conftest 의 재시도 경로)
    - `set_registration_queued(ref_id, queued_reason="pod_down")` → `registering` 에서만 True(`queued`+`queuedReason`); `processing` 에서 False(no-op + warning)
    - `set_registration_expired(ref_id)` → `registering` 에서만 True(`expired`); `queued`/`processing` 에서 False
    - `set_registration_active(ref_id, "A", video_s3_key="reference/u1/…/v1.mp4", video_etag='"abc"')` → doc `jobId=="A"` ∧ `processing` 일 때만 True: `{registrationStatus:"active", isActive:True, videoS3Key, videoETag, leaseUntil:None, registrationUpdatedAt, updatedAt}`; doc `jobId=="B"` 면 False + warning, doc 무변경(**늦은 stale 작업이 활성 doc 을 못 뒤집는다**)
    - `set_registration_failed(ref_id, "A", error={"code":"low_confidence","message":"…","joints":["왼쪽 발목"]})` → 같은 가드; 성공 시 공개 `{registrationStatus:"failed", leaseUntil:None, …}` + **비공개** `{registrationError:{code,message,joints}, updatedAt}` 를 같은 트랜잭션에; error 에 `code`/`message` 없으면 `ValueError`; stale job 이 `active` doc 에 failed 를 쓰려 하면 False, doc 그대로
    - `set_reference_angles(ref_id, job_id="A", angles_flat=[…], joint_keys=[8개], frames=T, real_fps=10.0, keypoint_report={…flat…}, split_angle=None)` → 가드 통과 시 `transaction.set(merge=True)` payload 키 정확히 `{angles, anglesJointKeys, anglesFrames, anglesUpdatedAt, anglesRealFps, referenceKeypointReport}`(+`referenceSplitAngle` 일 때만); `len(angles_flat) != frames*len(joint_keys)` → `ValueError`; keypoint_report 에 nested list → `TypeError`; `real_fps` 0/None → `ValueError`; stale job → False, 무변경
    - `begin_self_check(ref_id, job_id="A", analysis_id="a1")` → `active` ∧ `jobId=="A"` 일 때만 True: `{selfCheckAnalysisId:"a1", selfCheckStatus:"pending", selfCheckJobId:"A", updatedAt}`; 아니면 False
    - `self_check_authorized(ref_doc, uid="u1", analysis_id="a1", job_id="A")`(순수): `ref_doc.supplierUid=="u1" ∧ selfCheckAnalysisId=="a1" ∧ selfCheckJobId=="A"` 전부 참일 때만 True; 케이스 — 위조 표식(정상 사용자 `u9` 의 분석이 남의 refId 를 가리킴: `supplierUid!="u9"`) False · 다른 공급자의 refId False · stale analysis id(`selfCheckAnalysisId=="a0"`) False · pending 기록 전(`selfCheckAnalysisId` 없음) False · None/빈 doc False
    - `set_reference_self_check(ref_id, status="done", uid="u1", analysis_id="a1", job_id="A", score=97.3)` → 트랜잭션 안에서 doc 을 읽어 `self_check_authorized` 통과 시만 `{selfCheckStatus:"done", selfScore:97.3, selfScoreUpdatedAt, updatedAt}`; `status="failed"` 는 `selfScore` 키 없음; 가드 실패 → False + warning, 무변경; 미정의 status → `ValueError`; `done` 인데 score None → `ValueError`
    - `create_analysis_doc("u1","a1",{…})` → `users/u1/analyses/a1` 경로에 `create()` 1회, payload 에 `analysisId·mode·status` 없으면 `ValueError`
    - `get_reference_registration(ref_id)` / `get_reference_registration_private(ref_id)` → dict(없으면 None) — 버전 포인터 로직 **없이** top-level 원본
    - `list_reference_registrations_by_status("queued")` → 컬렉션 `reference` 에 `where(filter=FieldFilter("registrationStatus","==","queued"))` 단일 쿼리(전수 스캔 없음)
  </behavior>
  <read_first>
    - backend/shared/python/sunity_shared/firestore_admin.py :19-43 (`_db`/`_doc` seam · `update_analysis_status`) · :46-102 (`_validate_flat_dict_no_nested_array`) · :2173-2261 (`update_reference_body_data` ADD-only 골격 + "angles 절대 금지" 주석) · :2368-2443 (`set_reference_motion_with_gemini` 존재 분기) · :2446-2469 (`fail_analysis` · `get_analysis`) · :2558-2609 (`get_reference_motion` 포인터 폴백) · **:2895-2945 (visual job 불변식 + Firestore seam `_collection`/`_field_filter`/`_run_in_transaction`/`_now_ms`) · :3015-3056 (`_tx(transaction)` 어법 — `obj.get(transaction=…)` → 검증 → `transaction.update/set` → `_run_in_transaction(_tx)`) — claim/가드 writer 의 원형**
    - backend/tests/phase31/conftest.py :1-20 (`fake_firestore` fixture — 4 seam 교체, 실제 optimistic concurrency) · :69-80 (`TransactionConflict`) · :222-302 (`_FakeTransaction` · `FakeFirestore.run_in_transaction` 재시도) · :351-364 (seam 바인딩) — **claim 동시성 테스트는 이 fixture 로**(`batch()` 가 없으면 이 테스트 파일 안에 `_FakeBatch`(create 기록·commit) 를 두고 `_db` seam 을 monkeypatch)
    - backend/tests/test_reference_auto_register_handler.py :364-392 (`_FakeDoc` + `_doc` monkeypatch) · backend/tests/test_analysis_provenance.py :308-325 (`_FakeDocStore`)
    - backend/shared/python/sunity_shared/models.py (38-01 — REGISTRATION_STATUSES(+expired) · SELF_CHECK_STATUSES · CONSENT_VERSION · REGISTRATION_LEASE_SEC · reference_motion_path · reference_private_path · analysis_doc_path · REFERENCE_MOTIONS_COLLECTION · ANALYSIS_FIELD_SELF_CHECK_*)
    - backend/shared/python/sunity_shared/validation.py (38-01 — `ReferenceUploadRequest` 필드)
    - app/src/app/analysis/loading.tsx :159-178 (앱이 만드는 분석 doc 형상 — `create_analysis_doc` 정본)
    - .planning/phases/38-supplier-link/38-REVIEWS.md "### R2" · "### R3" · "### R4" · "### R8" · "### R13"
    - .planning/phases/38-supplier-link/38-PATTERNS.md "### `…/firestore_admin.py`" 절 + Shared Patterns §5 · RESEARCH 코드 예시 C
  </read_first>
  <action>
    RED 먼저(`test_firestore_reference_writers.py` — phase31 `fake_firestore` fixture 재사용 + `_FakeBatch`; 동시 이벤트 2·requeue 2회·stale 실패·가드 4케이스·pending 전 완료 훅 을 각각 이름 있는 테스트로) → 구현. 파일 끝에 블록 주석 `# ── Phase 38 (D-04·D-05·D-08·D-10·D-19 + 리뷰 R2·R3·R4·R8·R13) — 공급자 링크 등록 writer ──` + "이 블록의 `set_reference_angles` 는 :2252-2261 'angles 절대 금지' 규칙의 **유일한 예외**이며 legacy `ref-*` id 를 거부한다" + 불변식 3줄(visual job 어법 :2899-2907 미러): "1. 상태 전이는 트랜잭션 안에서 현재 상태·jobId 를 검증한 뒤에만 쓴다 2. 외부 side-effect(위임·복사)는 `claim_registration` 이 True 를 준 뒤에만 3. 자기 재현성 완료/실패 기록은 `self_check_authorized` 를 지난 것만".
    공통 가드 `_require_registration_ref_id(ref_id)`: 빈 값 또는 `startswith("ref-")` → `ValueError("ref_id invalid or legacy (ref-*)")`. 모든 payload 는 `_validate_flat_dict_no_nested_array(payload, path=...)` 를 지나고 `_now_ms()` 로 `*UpdatedAt`·`updatedAt`. 트랜잭션은 기존 `_run_in_transaction` + `_tx(transaction)` 어법(:3020-3055) — nested `@transactional` 금지(:3070 규율).
    - `create_reference_registration(ref_id, *, supplier_uid, form, upload_key, upload_expires_at_ms, consent_at_ms, supplier_code=None)` — `form` 은 `ReferenceUploadRequest`(속성 접근); `clip_range` 튜플은 비공개 doc `{"execStartS","execEndS"}` 로; `batch = _db().batch(); batch.create(public_ref, public_payload); batch.create(private_ref, private_payload); batch.commit()` — docstring 에 "원자적 두 문서 create — 하나라도 존재하면 `AlreadyExists` 전파, `snap.exists` 분기와 달리 경합 없음, 리포 첫 `create()` 사용". 로그 `create_reference_registration ok ref_id=%s supplier_uid=%s`.
    - `get_reference_registration(ref_id) -> dict | None` · `get_reference_registration_private(ref_id) -> dict | None`.
    - `claim_registration(ref_id, job_id, *, lease_sec=models.REGISTRATION_LEASE_SEC, now_ms=None) -> bool` — 위 behavior; docstring 에 "R3: S3 이벤트 중복·순서 변경, requeue 재실행, Pod 죽음(lease 만료) 을 한 함수가 가른다".
    - `set_registration_queued(ref_id, *, queued_reason) -> bool` · `set_registration_expired(ref_id) -> bool` — `registering` 에서만.
    - `_guarded_update(ref_id, job_id, mutate)` 내부 헬퍼: 트랜잭션 안 `doc.get("jobId") != job_id` → `log.warning("stale job ref_id=%s job_id=%s current=%s")` + False.
    - `set_registration_active(ref_id, job_id, *, video_s3_key, video_etag) -> bool` · `set_registration_failed(ref_id, job_id, *, error) -> bool`(비공개 doc 의 `registrationError` 를 같은 트랜잭션에서 `transaction.set(private_ref, {...}, merge=True)`) · `set_reference_angles(ref_id, *, job_id, angles_flat, joint_keys, frames, real_fps, keypoint_report, split_angle=None) -> bool`(RESEARCH 코드 예시 C payload + `anglesRealFps` 필수, 메모리 fps-label) · `begin_self_check(ref_id, *, job_id, analysis_id) -> bool`.
    - `self_check_authorized(ref_doc, *, uid, analysis_id, job_id) -> bool` — 순수(I/O 0), 38-08 훅이 import.
    - `set_reference_self_check(ref_id, *, status, uid, analysis_id, job_id, score=None) -> bool`.
    - `create_analysis_doc(uid, analysis_id, payload)` — `models.analysis_doc_path`; 필수 키 `analysisId`·`mode`·`status`; `doc.create(payload)`.
    - `list_reference_registrations_by_status(status) -> list[dict]` — `_collection(models.REFERENCE_MOTIONS_COLLECTION).where(filter=_field_filter("registrationStatus", "==", status)).stream()`(:2926 seam 재사용; 읽기 수 = 건수, Spark 5만/일 캡 주석; 반환 dict 에 `motionId` 보장).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && .venv/bin/python -m pytest tests/test_firestore_reference_writers.py tests/test_reference_auto_register_handler.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && .venv/bin/python -m pytest tests/test_firestore_reference_writers.py tests/test_reference_auto_register_handler.py -q` exits 0; new file collects ≥ 24 tests including tests named for: two concurrent claims (second False), reclaim after lease expiry, stale-job failure on active doc (no change), forged marker, other supplier refId, stale analysis id, completion before pending.
    - `grep -c "^def create_reference_registration\|^def get_reference_registration\|^def get_reference_registration_private\|^def claim_registration\|^def set_registration_queued\|^def set_registration_expired\|^def set_registration_active\|^def set_registration_failed\|^def set_reference_angles\|^def begin_self_check\|^def self_check_authorized\|^def set_reference_self_check\|^def create_analysis_doc\|^def list_reference_registrations_by_status" backend/shared/python/sunity_shared/firestore_admin.py` == 14.
    - `grep -c 'batch.create(\|\.create(' backend/shared/python/sunity_shared/firestore_admin.py` ≥ 3 (public + private + analysis doc); `grep -c "_run_in_transaction(_tx)" backend/shared/python/sunity_shared/firestore_admin.py` increases by ≥ 7 vs `git show HEAD:…` (claim·queued·expired·active·failed·angles·begin/self_check).
    - `grep -c 'startswith("ref-")' backend/shared/python/sunity_shared/firestore_admin.py` ≥ 1.
    - `git diff` shows `update_reference_body_data` / `update_reference_downstream_data` / `get_reference_motion` untouched.
  </acceptance_criteria>
  <done>등록 doc 생명주기(registering→queued/processing/expired→failed/active, selfCheck)가 트랜잭션·job 가드·권위 가드로 쓰이고, 동시성·stale·위조 케이스가 실제 CAS fake 로 잠긴다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `reference-upload-url` Lambda — 인증 → SSM 화이트리스트(∪ BELLE_UID) → probe | 검증 → presigned PUT `reference/{uid}/{refId}/upload.{ext}` 서명 → 공개+비공개 doc batch `create()`(`uploadExpiresAt`) → 응답 + monkeypatch 테스트(create 실패 = URL 미응답)</name>
  <files>backend/functions/reference-upload-url/app.py, backend/functions/reference-upload-url/requirements.txt, backend/tests/test_reference_upload_url_handler.py</files>
  <behavior>
    - `verify_request` 가 `AuthError` → 401 `{error:{code:"unauthorized"}}`
    - 화이트리스트 map `{}` 이고 `BELLE_UID` 비어 있음 → 403 `forbidden` (env 미설정 = 거부)
    - uid 가 map 에도 BELLE_UID 에도 없음 → 403; BELLE_UID 와 같으면 통과
    - body `{"probe": true}` + 화이트리스트 통과 → 200 `{probe:true, uid:"u1", supplierCode:"EUNJI"}`(코드 없으면 `null`); doc create·presign 호출 0
    - 정상 body → `_s3.generate_presigned_url(ClientMethod="put_object", Params={"Bucket","Key":"reference/u1/{ref_id}/upload.mp4"}, ExpiresIn=900)` 1회가 **먼저**(순수 서명, 네트워크 0), 그 **뒤** `create_reference_registration(ref_id, supplier_uid="u1"(토큰), form, upload_key="reference/u1/{ref_id}/upload.mp4", upload_expires_at_ms=<now+900000, 서명 만료와 같은 상수>, consent_at_ms=<int>, supplier_code=...)` 1회, 응답 200 `{refId, uploadUrl, s3Key, expiresInSec:900}`; `refId` 는 `validate_analysis_id_format` 통과(uuid4 hex) (리뷰 R4: create 가 실패하면 URL 이 응답에 실리지 않아 고아 객체가 생길 수 없다)
    - body 에 `"uid":"evil"` 또는 `"refId":"ref-kip-up"` 이 있어도 키·doc 은 토큰 uid·서버 refId 로만(`"evil"`/`"ref-kip-up"` 이 어떤 호출 인자에도 없다)
    - `validate_reference_upload_request` 가 `ValidationError("too_long", …)` → 400 `{error:{code:"too_long", message:REGISTRATION_ERROR_MESSAGE["too_long"]}}`
    - presign 예외 → 500 `server_error`, `create_reference_registration` 호출 0; `create_reference_registration` 예외(`AlreadyExists` 포함) → 500 `server_error` 이고 응답 본문에 `uploadUrl` 없음
    - SSM `get_parameter` 가 예외(ParameterNotFound/AccessDenied) → map `{}` + warning 로그(값 로그 금지) → BELLE_UID 외 전부 403
  </behavior>
  <read_first>
    - backend/functions/upload-url/app.py (72줄 전체 — 골격) · backend/functions/upload-url/requirements.txt
    - backend/functions/reference-auto-register/app.py :40-60 · :85-100 (화이트리스트 · env 미설정 거부) · :160-180 (Firestore 실패 매핑)
    - backend/shared/python/sunity_shared/auth.py :55-70 (SSM `get_parameter` 어법 — boto3 lazy import) · responses.py (`ok`/`error`/`parse_json_body`)
    - backend/tests/test_reference_auto_register_handler.py :25-56 (handler_module fixture) · :94-115 (`_FakeS3`) · :121-175 (happy path)
    - backend/shared/python/sunity_shared/models.py (38-01 — `parse_supplier_uids` · `SUPPLIER_UIDS_PARAM_DEFAULT` · `REGISTRATION_ERROR_MESSAGE` · `REFERENCE_UPLOAD_EXPIRES_SEC`) · validation.py (38-01) · s3keys.py (38-01 — `build_reference_upload_key` = upload.{ext}) · firestore_admin.py (Task 1)
    - .planning/phases/38-supplier-link/38-REVIEWS.md "### R4"(presign 실패·고아 방지) · "### R5"(upload 키만 서명)
    - .planning/phases/38-supplier-link/38-PATTERNS.md "### `backend/functions/reference-upload-url/app.py`" 절 + Shared Patterns §1·§2·§3·§4 · RESEARCH "## Security Domain"
  </read_first>
  <action>
    RED 먼저(`test_reference_upload_url_handler.py`: `_HANDLER_DIR` fixture 어법 복제, env `VIDEO_BUCKET`·`BELLE_UID`·`SUPPLIER_UIDS_PARAM`, `verify_request`·`_load_supplier_map`·`_s3`·`create_reference_registration` monkeypatch) → 구현.
    `requirements.txt` 는 `upload-url/requirements.txt` 복사(boto3 는 런타임 제공, firebase-admin 은 layer — 실제 내용은 원본 파일을 열어 그대로).
    `app.py` 모듈 상수: `_BUCKET = os.environ["VIDEO_BUCKET"]`, `_EXPIRES = int(models.REFERENCE_UPLOAD_EXPIRES_SEC)`(= 900 — doc `uploadExpiresAt` 과 한 상수, R4), `_BELLE_UID = os.environ.get("BELLE_UID","")`, `_SUPPLIER_UIDS_PARAM = os.environ.get("SUPPLIER_UIDS_PARAM", models.SUPPLIER_UIDS_PARAM_DEFAULT)`, `_SUPPLIER_CACHE_TTL_S = 60`, `_s3 = boto3.client("s3")`. `_load_supplier_map() -> dict[str, str | None]`: 모듈 캐시 `(value, loaded_at)` 60초; `boto3.client("ssm").get_parameter(Name=_SUPPLIER_UIDS_PARAM)["Parameter"]["Value"]` → `models.parse_supplier_uids`; 예외는 `log.warning("supplier-uids 조회 실패 param=%s", name)`(값·예외 본문 로그 금지) 후 `{}`. `_is_supplier(uid, m) -> bool` = `uid in m or (bool(_BELLE_UID) and uid == _BELLE_UID)`.
    `lambda_handler(event, _context)`: 1 `verify_request` → 401 · 2 `body = responses.parse_json_body(event)`; `m = _load_supplier_map()`; `not _is_supplier(uid, m)` → 403 `forbidden` "공급자로 등록된 계정만 쓸 수 있어요." + warning 로그(uid, `supplier_count=len(m)`, `belle_uid_set=bool(_BELLE_UID)`) · 3 `body.get("probe") is True` → `responses.ok({"probe": True, "uid": uid, "supplierCode": m.get(uid)})` · 4 `req = validate_reference_upload_request(body)` → `ValidationError` 매핑(`e.code`, `e.message`, `e.http_status`) · 5 `ref_id = uuid.uuid4().hex`; `s3_key = build_reference_upload_key(uid, ref_id, req.fmt)`(= `reference/{uid}/{refId}/upload.{ext}`, R5) · 6 presign(`upload-url` 과 동일 Params, ContentType 미포함 이유 주석 유지) — 예외 → 500 `server_error`(doc 미생성) · 7 `now_ms = int(time.time()*1000)`; `firestore_admin.create_reference_registration(ref_id, supplier_uid=uid, form=req, upload_key=s3_key, upload_expires_at_ms=now_ms + _EXPIRES*1000, consent_at_ms=now_ms, supplier_code=m.get(uid))` — 예외 `# noqa: BLE001` → `log.exception` + 500 `server_error` "등록 문서 생성에 실패했어요."(**URL 은 버린다** — 응답에 싣지 않는다, R4 고아 방지 주석) · 8 `log.info("reference-upload-url ok uid=%s ref_id=%s level=%s combo=%s", ...)` → `responses.ok({"refId", "uploadUrl", "s3Key", "expiresInSec"})`. 헤더 docstring: contract.md §2 `POST /reference/upload-url` · Phase 38 D-03/D-04/D-08 · 리뷰 R4/R5 · "본문의 uid/refId 는 절대 읽지 않는다(V4)" · "서명 → create 순서인 이유: 서명은 순수 함수라 실패가 드물고, create 실패 시 URL 을 안 주면 객체가 생길 수 없다".
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && .venv/bin/python -m pytest tests/test_reference_upload_url_handler.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && .venv/bin/python -m pytest tests/test_reference_upload_url_handler.py -q` exits 0 with ≥ 10 tests.
    - `test -f backend/functions/reference-upload-url/app.py && test -f backend/functions/reference-upload-url/requirements.txt`.
    - `grep -c 'body.get("uid")\|body\["uid"\]\|body.get("refId")' backend/functions/reference-upload-url/app.py` == 0.
    - `grep -c "create_reference_registration" backend/functions/reference-upload-url/app.py` ≥ 1 and its call appears **after** `generate_presigned_url` in `lambda_handler` (`grep -n` line numbers; R4).
    - `grep -c 'ExpiresIn=_EXPIRES' backend/functions/reference-upload-url/app.py` == 1 and `grep -c 'REFERENCE_UPLOAD_EXPIRES_SEC' backend/functions/reference-upload-url/app.py` ≥ 1 (900 리터럴 대신 상수); `grep -c 'upload_expires_at_ms=' backend/functions/reference-upload-url/app.py` == 1.
    - A test asserts that when `create_reference_registration` raises, the 500 body contains no `uploadUrl`.
  </acceptance_criteria>
  <done>입구 Lambda 가 얇은 핸들러 규율로 존재하고 화이트리스트·서명→선작성 순서·고아 방지가 테스트로 잠긴다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: `firestore.rules` — `reference` 재귀 와일드카드 좁히기 + `private/{doc}` 본인 읽기(R13) + `users/**` 의 `selfCheckForReference·selfCheckJobId` 클라이언트 create/update 거부(R2) · Rules REST 스크립트 `deploy_firestore_rules.py`(`--test`/`--current`/`--release`, 순수 body 빌더) + `projects:test` 케이스 JSON + 단위 테스트(배포는 38-09)</name>
  <files>firestore.rules, backend/scripts/deploy_firestore_rules.py, backend/tests/test_deploy_firestore_rules.py, backend/tests/firestore_rules_cases.json</files>
  <behavior>
    - `build_test_body(rules_text, cases)` → `{"source":{"files":[{"name":"firestore.rules","content":rules_text}]},"testSuite":{"testCases":cases}}`; `build_ruleset_body(rules_text)` → `{"source":{"files":[…]}}`; `build_release_body(project, ruleset_name)` → `{"release":{"name":f"projects/{project}/releases/cloud.firestore","rulesetName":ruleset_name}}`
    - `summarize_test_results(resp_json, cases)` → `(passed:int, failed:list[str])` — `testResults[i].state == "SUCCESS"` 가 통과; `issues` 가 비어 있지 않으면 전부 실패로
    - `main(["--test", "--rules", path, "--cases", path, "--project", "p"])` 는 `http_post` seam 을 부르고 실패 건수를 exit code 로; `--release` 는 rulesets POST → releases PATCH 순서(두 호출 기록); `--current` 는 GET 만(쓰기 0); `--dry-run` 은 body 만 출력
    - `load_cases(path)` 가 JSON 의 `testCases` 를 그대로; 케이스 파일은 최소 10건: (1) uid A 가 `reference/r1/private/registration`(resource `{supplierUid:"A"}`) get → ALLOW (2) uid B 같은 doc → DENY (3) 무인증 → DENY (4) uid B 가 `reference/r1` get → ALLOW (5) uid B 가 `reference/r1/versions/v1` get → ALLOW (6) uid B 가 `reference/r1` update → DENY (7) uid A 가 `users/A/analyses/x` create with `{analysisId,mode,status,selfCheckForReference:"r1"}` → DENY (8) 같은 create without the key → ALLOW (9) uid A 가 기존 doc(resource `{…, selfCheckForReference:"r1"}`) 에 `coachReview` 만 바꾸는 update → ALLOW (10) 같은 doc 에서 `selfCheckForReference` 를 바꾸는 update → DENY (11) uid B 가 `users/A/analyses/x` get → DENY (12) uid A 가 `users/A/analyses/x` delete → ALLOW
  </behavior>
  <read_first>
    - firestore.rules (전체 24줄 — `users/{uid}/{document=**}` :9-11 · `reference/{document=**}` :14-17 · 기본 차단 :20-22)
    - backend/shared/python/sunity_shared/auth.py :40-70 (`_load_service_account_dict` — FIREBASE_SA_JSON/PATH/PARAM 3원 — 스크립트가 재사용) · backend/scripts/e2e_app_path.py :50-66 (SA json 경로 어법 · `http_json`) · backend/scripts/pod_teardown.py :1-30 (머리말 경고 어법) · backend/scripts/intake_clips.py :30-40 (`--dry-run` 관례)
    - app/src/lib/coachReview.ts :40-68 (앱이 분석 doc 에 `setDoc(…, {merge:true})` 로 `coachReview` 를 쓴다 — update 가드가 `affectedKeys()` 여야 하는 이유) · app/src/app/analysis/loading.tsx :159-178 (앱의 create payload — 표식 없음) · app/src/lib/referenceMotions.ts :208-242 (앱의 `reference` 읽기 — top-level doc 만; versions/private 는 클라이언트가 안 읽는다 [확인 grep])
    - backend/shared/python/sunity_shared/firestore_admin.py (`versions` 서브컬렉션 경로가 있는 함수 — `get_reference_motion` :2558-2609; 클라이언트가 읽는 다른 `reference/{id}/<sub>` 가 있으면 같은 읽기 블록을 하나씩 더한다)
    - .planning/phases/38-supplier-link/38-PATTERNS.md "### `backend/scripts/requeue_reference_registrations.py` (script, batch)" 절(스크립트 헤더·`main(argv)`·SSM/SA 어법) + Shared Patterns §3·§4 · "## No Analog Found"(운영 규칙 배포 스크립트 선례 0 — REST 문서를 따른다)
    - .planning/phases/38-supplier-link/38-REVIEWS.md "### R2" · "### R13"
    - 상류 문서: Firebase Security Rules REST — `projects.test`(`POST https://firebaserules.googleapis.com/v1/projects/{p}:test`, `TestCase{expectation, request{auth,path,method,resource}, resource}`) · `projects.rulesets.create` · `projects.releases.patch`(release name `cloud.firestore`); `google.oauth2.service_account.Credentials.from_service_account_info(sa, scopes=["https://www.googleapis.com/auth/firebase","https://www.googleapis.com/auth/cloud-platform"])` + `google.auth.transport.requests.Request` (`.venv` 에 google-auth 2.56·requests 2.34 [확인])
  </read_first>
  <action>
    RED 먼저(`test_deploy_firestore_rules.py` — `http_post`/`http_get`/`get_access_token` 을 monkeypatch, 네트워크 0) → 구현.
    `firestore.rules`(주석에 Phase 38 리뷰 R2·R13 + 이유 한 줄씩): (1) `match /users/{uid}/{document=**}` 를 `allow read, delete: if request.auth != null && request.auth.uid == uid;` + `allow create: if <owner> && !request.resource.data.keys().hasAny(['selfCheckForReference', 'selfCheckJobId']);` + `allow update: if <owner> && !request.resource.data.diff(resource.data).affectedKeys().hasAny(['selfCheckForReference', 'selfCheckJobId']);` 로(서버 Admin 은 규칙 무관; `affectedKeys()` 라야 서버가 표식을 쓴 doc 에 앱이 `coachReview` merge-set 을 계속 할 수 있다). (2) `match /reference/{document=**}` 블록을 **지우고** `match /reference/{refId} { allow read: if request.auth != null; allow write: if false; }` + `match /reference/{refId}/versions/{version} { 같은 읽기 }` + `match /reference/{refId}/private/{doc} { allow read: if request.auth != null && resource.data.supplierUid == request.auth.uid; allow write: if false; }` — 주석: "재귀 와일드카드는 서브문서까지 인증자 전체 읽기를 주므로(OR 결합) private 블록을 더하기만 해서는 무효 — 좁힌다. 앱은 top-level doc 만 읽는다(referenceMotions.ts)". (3) 기본 차단 블록 유지.
    `backend/tests/firestore_rules_cases.json`: `{"testCases":[…]}` 위 12 케이스(경로는 `/databases/(default)/documents/...` 절대 경로, `expectation` `ALLOW`/`DENY`, 읽기 케이스는 `resource.data` 로 기존 doc 상태, update 케이스는 `request.resource.data` 로 post-state).
    `backend/scripts/deploy_firestore_rules.py`(헤더: 왜 — firebase CLI 부재·감사 밖 패키지 금지 → Rules REST; 사용법 4줄 `--test` / `--current` / `--release` / `--dry-run`; **`--release` 는 38-09 belle 승인 뒤에만**; 롤백 = 이전 rules 파일로 `--release`): `main(argv) -> int`; 인자 `--rules`(기본 리포 루트 `firestore.rules`) · `--cases`(기본 `backend/tests/firestore_rules_cases.json`) · `--project`(기본 `.firebaserc` `projects.default` → 없으면 SA `project_id`) · `--test` · `--current` · `--release` · `--dry-run`; 토큰은 `sunity_shared.auth._load_service_account_dict()` → `google.oauth2.service_account.Credentials.from_service_account_info(..., scopes=[...])` → `refresh(Request())` → `credentials.token`(출력 금지); seam `http_post(url, body, token)` / `http_get(url, token)`(`requests`); 순수 함수 `build_test_body`·`build_ruleset_body`·`build_release_body`·`summarize_test_results`·`load_cases`. `--test` 출력: 케이스별 `ALLOW/DENY expected=… state=…` 한 줄, 실패 건수 exit code. `--current` 출력: 현재 release 의 rulesetName + 그 ruleset 의 `files[0].content` 를 `--out` 경로에 저장(38-09 가 롤백 원본으로 씀). `--release`: rulesets POST → 이름 → releases PATCH → 새 release 출력. 권한 거부(403) 는 원문 그대로 출력하고 exit 2(38-09 가 belle 콘솔 폴백으로 갈 신호).
    **라이브 `--test`(선택, 읽기 전용):** 리포 루트 SA(`FIREBASE_SA_PATH=sunity-ai-coach-firebase-adminsdk-fbsvc-7055d7d3d1.json`) 로 `backend/.venv/bin/python backend/scripts/deploy_firestore_rules.py --test` 를 한 번 돌려 12/12 를 SUMMARY 에 `[확인]`; 네트워크/권한으로 못 돌면 `[미확인 — 38-09 T3 F 에서 재시도]` 로 적고 이 task 의 게이트는 아래 자동 검증(단위 테스트 + grep)만이다. `--release` 는 여기서 절대 실행하지 않는다.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && backend/.venv/bin/python -m pytest backend/tests/test_deploy_firestore_rules.py -q && test "$(grep -c 'match /reference/{document=\*\*}' firestore.rules)" = "0" && grep -q 'match /reference/{refId}/private/{doc}' firestore.rules && grep -q "affectedKeys().hasAny(\['selfCheckForReference', 'selfCheckJobId'\])" firestore.rules && backend/.venv/bin/python -c "import json;c=json.load(open('backend/tests/firestore_rules_cases.json'))['testCases'];assert len(c)>=12"</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && .venv/bin/python -m pytest tests/test_deploy_firestore_rules.py -q` exits 0 (≥ 8 tests: 3 body builders, summarize pass/fail/issues, `--test` exit code, `--release` call order, `--current` write-0, `--dry-run`).
    - `grep -c 'match /reference/{document=\*\*}' firestore.rules` == 0; `grep -c 'match /reference/{refId}\b\|match /reference/{refId} ' firestore.rules` ≥ 1; `grep -c 'match /reference/{refId}/private/{doc}' firestore.rules` == 1; `grep -c 'resource.data.supplierUid == request.auth.uid' firestore.rules` == 1; `grep -c "hasAny(\['selfCheckForReference', 'selfCheckJobId'\])" firestore.rules` == 2 (create + update).
    - `backend/tests/firestore_rules_cases.json` has ≥ 12 `testCases`, each with `expectation` ∈ {ALLOW, DENY}; `grep -c '"DENY"' backend/tests/firestore_rules_cases.json` ≥ 6.
    - `grep -c "firebaserules.googleapis.com" backend/scripts/deploy_firestore_rules.py` ≥ 3 (test, rulesets, releases); `grep -c "print(.*token\|log.*token" backend/scripts/deploy_firestore_rules.py` == 0.
    - SUMMARY has either `projects:test 12/12 [확인]` or `[미확인 — 38-09 T3 F]`; `grep -c "\-\-release" .planning/phases/38-supplier-link/38-06-SUMMARY.md` mentions it was NOT executed.
  </acceptance_criteria>
  <done>규칙 파일이 비공개 서브문서·표식 쓰기 거부를 담고, 배포 없이도 REST 테스트 케이스와 순수 스크립트로 검증 가능하며, 실배포는 38-09 승인 뒤에만 가능하다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 폰 브라우저 → API Gateway → Lambda | Bearer ID 토큰 + JSON 본문(신뢰 불가) |
| Lambda → Firestore Admin | 규칙 우회 쓰기(Admin SDK) — 서버만 |
| Lambda → SSM | 화이트리스트 원본 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-06-1 | Spoofing | `lambda_handler` uid | mitigate | `verify_request`(firebase-admin `verify_id_token`) 의 uid 만; 본문 uid 무시(테스트 "evil") |
| T-38-06-2 | Elevation | 화이트리스트 | mitigate | SSM map ∪ BELLE_UID, 파라미터 부재/env 미설정 = 403; 익명 uid 등록 금지는 운영 규칙(SUMMARY·supplier-guide 에 명시) |
| T-38-06-3 | Tampering | S3 키 | mitigate | `build_reference_upload_key(token_uid, uuid4hex, fmt)` 만; presign 정책은 `reference/*` PutObject 한정(38-09 template) |
| T-38-06-4 | Tampering | 기존 11개 doc | mitigate | batch `create()`(존재 시 실패) + 모든 writer 의 `ref-` 가드 |
| T-38-06-8 | Tampering(중복 실행) | `claim_registration` | mitigate | 트랜잭션 CAS — registering/queued 또는 lease 만료 processing 만 claim; active/failed/angles/self-check 는 jobId 가드(R3) |
| T-38-06-9 | Spoofing | 자기 재현성 표식 | mitigate | 기준 doc 이 권위(`self_check_authorized` 3중 일치) + firestore.rules 가 클라이언트 표식 create/update 거부(R2) |
| T-38-06-10 | Information disclosure | 동의·실패 상세·선언 | mitigate | 비공개 서브문서 + 규칙 `supplierUid == request.auth.uid` 읽기(R13); 공개 doc 은 picker·상태·점수·작업 필드만 |
| T-38-06-11 | Elevation | `deploy_firestore_rules.py --release` | mitigate | 이 플랜에서 실행 금지(테스트만); 38-09 belle 승인 뒤, 이전 규칙 파일 보존 + `--current` 로 롤백 원본 |
| T-38-06-5 | Repudiation | consent | mitigate | `consent.at`(서버 ms)·`version`·`uid` 를 Lambda 가 기록 |
| T-38-06-6 | Information disclosure | 로그 | mitigate | SSM 값·토큰·예외 본문 로그 금지, key=value 만 |
| T-38-06-7 | DoS | presign 남발 | accept | 화이트리스트 1~2 uid, 100MB·5~30초 검증, API GW 기본 스로틀 |
| T-38-SC | Tampering | pip installs | accept | requirements.txt 는 기존 파일 복사(신규 패키지 0) |
</threat_model>

<verification>
- `cd backend && .venv/bin/python -m pytest tests/test_firestore_reference_writers.py tests/test_reference_upload_url_handler.py tests/test_reference_auto_register_handler.py tests/test_deploy_firestore_rules.py -q` → 0 failed.
- `firestore.rules` grep 게이트(Task 3) 통과; `--release` 미실행.
- 플랜 끝 push.
</verification>

<success_criteria>
- writer 13종 + 순수 가드 + 입구 Lambda 가 순수 테스트로 닫혔고, 기존 11개 doc 은 구조적으로 못 건드린다.
- 등록 작업 소유권(claim/lease/jobId)·자기 재현성 권위·공개/비공개 분리가 코드와 규칙 파일에 있고, 규칙은 REST 케이스로 검증 가능하다(배포는 38-09).
</success_criteria>

<output>
Create `.planning/phases/38-supplier-link/38-06-SUMMARY.md` when done.
</output>
