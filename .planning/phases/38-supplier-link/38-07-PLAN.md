---
phase: 38-supplier-link
plan: 07
type: execute
wave: 3
depends_on: ["38-05", "38-06"]
files_modified:
  - backend/functions/pipeline/app.py
  - backend/tests/test_pipeline_reference_dispatch.py
  - backend/tests/test_register_reference_pipeline.py
autonomous: true
requirements: [REQ-38-1, REQ-38-2, REQ-38-4]
must_haves:
  truths:
    - "D-05 / REQ-38-2: `reference/{uid}/{refId}.{ext}` 객체가 오면 파이프라인이 다운로드 → 프레임 추출(9fps/640) → RTMW(사람 수 포함) → **기준 11개와 같은 함수 순서**(estimate → measure_body_profile → to_coco17_array → compute_joint_angles → joint_uncertainty → temporal_fill) 로 angles(flat)·anglesRealFps·referenceKeypointReport·referenceSplitAngle 을 `reference/{refId}` 에 쓰고 `registrationStatus:'active'`·`isActive:true` 로 만든다 — mode1 `RuntimeError('기준 모션 또는 keyframe 데이터 없음')` 의 원인(angles 부재)이 사라진다"
    - "D-20: Pod 가 없으면(SSM `runpod-pod-expected` == down 또는 `/health` 12초 실패) doc 을 `queued`(queuedReason `pod_down`) 로 두고 **정상 반환**한다 — `server_error` 로 위장하지 않는다(메모리 pod-link)"
    - "D-09 / Success ②: 실패 4형 + 길이(too_short/too_long) 가 각각 doc `registrationStatus:'failed'` + `registrationError{code,message[,joints]}` 로 남는다 — 합성 입력으로 강제(pytest, Pod 0)"
    - "D-10: 등록 `active` 직후 같은 영상을 공급자 uid 의 `mode1` 분석 doc 으로 선작성하고 `uploads/{uid}/{newId}.{ext}` 로 S3 복사해 기존 경로가 자기 재현성을 잰다(doc 먼저 → 객체 뒤, 메모리 demo-only)"
    - "Success ④: `uploads/` 학생 경로는 byte-무접촉 — 기존 `test_pipeline_dispatch.py` 전부 통과"
  artifacts:
    - path: "backend/functions/pipeline/app.py"
      provides: "_handle_reference_upload · _pod_available · _register_reference · _trigger_self_check · lambda_handler 접두사 분기"
      contains: "def _register_reference"
  key_links:
    - from: "pipeline.lambda_handler"
      to: "s3keys.parse_reference_key"
      via: "for 루프 첫 줄에서 parse_upload_key 보다 먼저"
      pattern: "parse_reference_key"
    - from: "pipeline._register_reference"
      to: "firestore_admin.set_reference_angles"
      via: "angles flat + anglesRealFps + referenceKeypointReport"
      pattern: "set_reference_angles"
    - from: "pipeline._register_reference"
      to: "registration_checks.check_registration"
      via: "person_counts + keypointReport + n_stand"
      pattern: "check_registration"
    - from: "pipeline._trigger_self_check"
      to: "_s3.copy_object → uploads/{uid}/{newId}.{ext}"
      via: "Pod 자격증명의 uploads/* PutObject — 범위 [미확인]; 38-09 T3 (E) simulate-principal-policy / 38-14 T2 3-b probe 가 확인한다"
      pattern: "copy_object"
---

<objective>
파이프라인 Lambda 에 `reference/` 분기(REQ-38-1·D-20)와 등록 서비스 `_register_reference`(REQ-38-2·D-05·D-09·D-10)를 붙인다. 채점 경로(`_process`)는 부르지 않고 어댑터만 빌린다(RESEARCH 안티패턴 1). Pod 실행은 38-08 의 `/register-reference` 라우트가 같은 함수를 부른다.

Purpose: 지금 `reference/` 키는 알림에 안 오고(38-09 에서 고친다), 와도 `parse_upload_key → None → 스킵`(:10309-10312) 으로 죽는다(RESEARCH Q2). angles 를 쓰는 유일한 자동 경로가 이 함수다.
Output: `pipeline/app.py` 4개 함수 + 분기, 테스트 2파일(둘 다 AWS/Pod/Firestore 0).

Phase 38 전제(38-01 머리말) 적용. `pipeline/app.py` 는 10k 줄 — 필요한 범위만 Grep→Read 하고 같은 범위를 두 번 읽지 않는다.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/38-supplier-link/38-CONTEXT.md
@.planning/phases/38-supplier-link/38-RESEARCH.md
@.planning/phases/38-supplier-link/38-PATTERNS.md
@.planning/phases/38-supplier-link/38-05-SUMMARY.md
@.planning/phases/38-supplier-link/38-06-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `lambda_handler` 접두사 분기 + `_pod_available`(SSM `runpod-pod-expected` + `/health` 12s) + `_handle_reference_upload`(queued/delegated/failed, 예외 삼킴) + `_delegate_to_runpod(url=)` + dispatch 테스트</name>
  <files>backend/functions/pipeline/app.py, backend/tests/test_pipeline_reference_dispatch.py</files>
  <behavior>
    - SQS 이벤트 키 `reference/u1/{refId}.mp4` + `get_reference_registration` 가 `{registrationStatus:'registering', supplierUid:'u1', videoS3Key:...}` + `_pod_available` → (False,'pod_expected_down') ⇒ `set_reference_registration_status(refId,'queued', queued_reason='pod_down')` 1회, `_delegate_to_runpod` 0회, 예외 없음, 반환 `{"processed": 0}`
    - 같은 키 + `_pod_available` → (True,'healthy') ⇒ status `'processing'` 1회 → `_delegate_to_runpod(bucket, key, url=<…/register-reference>)` 1회 → `{"processed": 1}`
    - delegate 가 `RuntimeError` ⇒ status `'failed'` + error `{code:'server_error', message:REGISTRATION_ERROR_MESSAGE['server_error']}`; handler 는 정상 종료
    - doc 없음 / `registrationStatus != 'registering'` / `supplierUid != key uid` ⇒ 스킵(warning 로그, writer 호출 0)
    - `uploads/u3/a3.mp4` 키는 종전 그대로(기존 `test_pipeline_dispatch.py` 전부 통과) — 같은 이벤트에 uploads+reference 두 레코드가 섞여도 각자 처리
    - `_pod_available()`: `_runpod_enabled()` False → (False,'runpod_env_unset'); SSM 값 'down' → (False,'pod_expected_down'); SSM 예외(파라미터 없음/AccessDenied) → health 로 진행; health JSON `{"status":"ok","pipeline_loaded":true}` → (True,'healthy'); `pipeline_loaded:false` 또는 URLError/timeout → (False,'health_failed')
    - `_register_url()`: `https://x/analyze` → `https://x/register-reference`; trailing slash 없는 base 도 처리
  </behavior>
  <read_first>
    - backend/functions/pipeline/app.py :110-130 (s3keys import 블록) · :155-240 (`_s3`, `_s3_download`, `_RUNPOD_URL`/`_RUNPOD_TOKEN`/`_RUNPOD_TIMEOUT_S`, `_runpod_enabled`, `_delegate_to_runpod`) · :10297-10348 (`lambda_handler` 전체)
    - backend/infra/podwatch.yaml :130-170 (`pod_expected_up` · `health_url` · `probe` — 옮겨 올 원형) · backend/scripts/pod_teardown.py :120-128 (쓰기 측 SSM 이름 `/sunity/motion/runpod-pod-expected`) · backend/runpod_inference/start_server.sh :90-100
    - backend/tests/test_pipeline_dispatch.py (전체 — `_sqs_event` · `pipeline` fixture · 위임 실패·스킵 단언)
    - backend/shared/python/sunity_shared/firestore_admin.py (38-06 — `get_reference_registration` · `set_reference_registration_status`) · models.py (38-01) · s3keys.py (38-01)
    - .planning/phases/38-supplier-link/38-PATTERNS.md "### `backend/functions/pipeline/app.py`" 절(lambda_handler 원문 · `_delegate_to_runpod` 원문 · 플래너 메모) + "## 관측 — 플래너가 알아야 할 코드 사실" 1·4 · RESEARCH "### A. 등록 경로의 최소 분기점" 1~3 · "### Q13" · 코드 예시 B
  </read_first>
  <action>
    RED 먼저(`test_pipeline_reference_dispatch.py` — `test_pipeline_dispatch.py` 의 fixture 를 복제하되 추가로 `app.firestore_admin.get_reference_registration`·`set_reference_registration_status`·`app._pod_available`·`app._delegate_to_runpod` 를 monkeypatch; `_pod_available` 단위 테스트는 `app._ssm_get_parameter`·`urllib.request.urlopen` 를 monkeypatch) → 구현.
    `app.py`:
    - import 블록에 `parse_reference_key` 추가; `from sunity_shared.analysis import registration_checks` 는 Task 2 에서.
    - 상수: `_POD_EXPECTED_PARAM = os.environ.get("POD_EXPECTED_PARAM", "/sunity/motion/runpod-pod-expected").strip()` (주석: **쓰기 측** `start_server.sh:95`·`pod_teardown.py:125` 가 갱신하는 이름 — `podwatch.yaml` 기본값 `pod-expected` 는 읽기 측 다른 이름, PATTERNS 관측 1), `_HEALTH_TIMEOUT_S = 12`.
    - `_ssm_get_parameter(name) -> str | None`(boto3 ssm lazy import, 예외 → None + warning; 값 로그 금지) · `_health_url() -> str`(`_RUNPOD_URL` 의 trailing `/analyze` → `/health`, podwatch `health_url` 원문) · `_register_url() -> str`(`/analyze` → `/register-reference`) · `_pod_available() -> tuple[bool, str]`(위 behavior 순서; health 는 `urllib.request.Request(url, headers={"User-Agent": "sunity-motion-pilot/1.0 (+aws-lambda)"})`, timeout `_HEALTH_TIMEOUT_S`, `status=="ok" and pipeline_loaded`).
    - `_delegate_to_runpod(bucket, key, *, url: str | None = None)` — 본문에서 `_RUNPOD_URL` 대신 `url or _RUNPOD_URL`; 나머지 byte-동일(기존 호출부 무변경).
    - `_handle_reference_upload(bucket, key, ref) -> str`(반환 `'skipped'|'queued'|'delegated'|'failed'`, **절대 raise 하지 않는다** — 메시지 소비 = 재시도·DLQ 없음, D-20): (i) `doc = firestore_admin.get_reference_registration(ref.ref_id)`; None 이거나 `doc.get("registrationStatus") != models.REGISTRATION_STATUS_REGISTERING` 이거나 `doc.get("supplierUid") != ref.uid` → warning `스킵: 등록 대기 doc 아님 key=%s status=%s` → `'skipped'` (손 업로드·재전송 방어) (ii) `ok, why = _pod_available()`; not ok → `set_reference_registration_status(ref.ref_id, QUEUED, queued_reason="pod_down")` + info 로그 `why` → `'queued'` (iii) `set_reference_registration_status(ref.ref_id, PROCESSING)` → `_delegate_to_runpod(bucket, key, url=_register_url())` → `'delegated'`; 예외 `# noqa: BLE001` → `log.exception` + `set_reference_registration_status(ref.ref_id, FAILED, error={"code": models.REG_ERR_SERVER_ERROR, "message": models.REGISTRATION_ERROR_MESSAGE[...]})` → `'failed'`; writer 자체가 던지면 로그만.
    - `lambda_handler` for 루프 첫 줄: `ref = parse_reference_key(key)`; `if ref is not None: outcome = _handle_reference_upload(bucket, key, ref); processed += 1 if outcome == "delegated" else 0; continue` — 그 아래 기존 코드 byte-무접촉. 반환 형상 `{"processed": processed}` 유지(기존 테스트 단언).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && .venv/bin/python -m pytest tests/test_pipeline_reference_dispatch.py tests/test_pipeline_dispatch.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && .venv/bin/python -m pytest tests/test_pipeline_reference_dispatch.py tests/test_pipeline_dispatch.py -q` exits 0 (new file ≥ 9 tests; old file unchanged and green).
    - `grep -n "parse_reference_key(key)" backend/functions/pipeline/app.py` line number < `grep -n "parsed = parse_upload_key(key)"` line number inside `lambda_handler`.
    - `grep -c '"/sunity/motion/runpod-pod-expected"' backend/functions/pipeline/app.py` == 1 and `grep -c "_HEALTH_TIMEOUT_S = 12" backend/functions/pipeline/app.py` == 1.
    - `grep -c "def _handle_reference_upload\|def _pod_available\|def _register_url\|def _health_url\|def _ssm_get_parameter" backend/functions/pipeline/app.py` == 5.
    - `git diff backend/functions/pipeline/app.py` shows the existing `uploads/` branch body unchanged (only the inserted 4-line prefix branch inside the loop and the `url=` keyword in `_delegate_to_runpod`).
  </acceptance_criteria>
  <done>reference 키가 스킵되지 않고 Pod 유무에 따라 queued/delegated 로 갈리며 학생 경로는 무접촉이다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `_register_reference(bucket, key, uid, ref_id)` — 어댑터 재사용 · 실패 4형+길이 판정 · angles writer · `active` · 자기 재현성 트리거 + 합성 강제 테스트(6 코드 + happy path)</name>
  <files>backend/functions/pipeline/app.py, backend/tests/test_register_reference_pipeline.py</files>
  <behavior>
    (테스트는 `app._ensure_adapters` 를 no-op 으로, `app._s3` 를 가짜(download_file 은 빈 파일 생성 · copy_object 기록), `app._FRAME_EXTRACTOR` 를 가짜(`extract(path)` → `np.zeros((T,64,64,3),uint8)`, `effective_fps_for(path)` → 10.0), `app._POSE_ESTIMATOR` 를 `_engine = RTMWPoseEngine.create_with_inferencer(mock, real_manifest)` + `_default_pole` 를 가진 객체로, `firestore_admin` 6 writer 를 기록용 가짜로 바꾼다. mock inferencer 는 프레임별 `(N,133,2)`·`(N,133)` 를 `side_effect` 로 돌려주며 COCO-17 인덱스(어깨 5/6 · 팔꿈치 7/8 · 손목 9/10 · 고관절 11/12 · 무릎 13/14 · 발목 15/16)에 서 있는 자세/창 자세 픽셀 좌표를 넣는다 — 헬퍼 `_frames_133(T, *, n_people, stand, ankle_stand_y, ankle_window_y, ankle_conf)`.)
    - N=0 전 프레임 → writer `set_reference_registration_status(ref,'failed', error={code:'no_human', message:ERROR_MESSAGE['no_human']})`, angles writer 0회
    - N=2 전 프레임 → `failed` + `multiple_people` 문구
    - T=60(6.0s) 서 있는 10프레임 발목 y 가 창 발목보다 **위**(바닥 위반) → `no_standing_start`
    - 발목 conf 0.2 → `low_confidence` + `joints == ['왼쪽 발목','오른쪽 발목']` 이고 message 에 `잘 안 보인 부위: 왼쪽 발목 · 오른쪽 발목` 포함(`{joints}` 치환됨)
    - T=30(3.0s @10fps) → `too_short`; T=350(35s) → `too_long`; doc `isCombo:true` 면 T=350 통과·T=650 → `too_long`
    - happy path(T=60, N=1, 정상 자세, conf 0.9): 호출 순서 `set_reference_angles(ref, angles_flat=len T*8, joint_keys=list(skeleton.JOINT_KEYS), frames=T, real_fps=10.0, keypoint_report=<dict, frames==T>, split_angle=<float|None>)` → `set_reference_registration_status(ref,'active', extra={'isActive': True})` → `create_analysis_doc(uid, <32-hex>, payload{analysisId, mode:'mode1', referenceMotionId:ref, status:'uploading', fileName, createdAt, updatedAt, learningOptIn:<consent.training bool>, selfCheckForReference:ref — 이 키 이름은 `models.ANALYSIS_FIELD_SELF_CHECK_FOR_REFERENCE`(38-01) 로만 쓰고 테스트는 리터럴로 단언한다})` → `_s3.copy_object(Bucket=bucket, CopySource={'Bucket':bucket,'Key':key}, Key='uploads/{uid}/{newId}.mp4')` → `set_reference_self_check(ref, status='pending', analysis_id=newId)`; angles 값에 NaN 없음, 소수 2자리
    - 자기 재현성 트리거 단계에서 `copy_object` 가 예외 → `set_reference_self_check(ref, status='failed')` 호출, 등록은 `active` 그대로(예외 전파 없음)
    - 임시 파일은 성공·실패 모두 `finally` 에서 삭제(테스트: 다운로드 경로가 존재하지 않는다)
  </behavior>
  <read_first>
    - backend/functions/pipeline/app.py :1453-1512 (`_RTMWNlfCompat` — `_engine`·`_default_pole`·`estimate_with_profile` 의 measure_body_profile→replace→to_coco17_array 순서) · :1514-1565 (`_ensure_adapters`) · :1575-1590 (`_angles_from_video` 4줄) · :1666-1692 (`_download_analysis_video`) · :2210-2220 (`effective_fps_for` 사용 예) · :8901-8906 (mode1 이 요구하는 `angles`)
    - backend/scripts/extract_reference_angles.py :96-160 (`_rtmw_estimate_to_coco17` · `_extract_one` — compute_joint_angles → joint_uncertainty → occluded_mask → temporal_fill · round 2 · NaN→0.0 · `max_split(split_angle_series(keypoints))`) — **같은 함수·같은 순서**로
    - backend/shared/python/sunity_shared/analysis/assemble.py :901-990 (`build_keypoint_report(pose_frames, *, fps)`) · frame_extractor.py :30-75 · registration_checks.py (38-05) · skeleton.py (JOINT_KEYS) · firestore_admin.py (38-06 writer 시그니처) · s3keys.py (`build_upload_key`) · models.py (38-01 — `ANALYSIS_FIELD_SELF_CHECK_FOR_REFERENCE` · `REGISTRATION_ERROR_MESSAGE` · `REFERENCE_*_DURATION_SEC`)
    - backend/tests/test_mode3_fault_zoom_selection.py :100-115 (`app._FRAME_EXTRACTOR` 교체 어법) · backend/tests/test_s3_download_accelerate.py :45-60 (`app._s3` 교체) · backend/tests/test_rtmw_engine.py :38-80 (mock inferencer · `real_manifest_path`)
    - .planning/phases/38-supplier-link/38-RESEARCH.md "### A." 5 · "### C. 자기 재현성" · "Pitfall 3·6·7" · "Don't Hand-Roll" 표 · PATTERNS 플래너 메모(pipeline 절)
  </read_first>
  <action>
    RED 먼저 → 구현. `app.py` 에 `_register_reference(bucket, key, uid, ref_id) -> None`(Pod `/register-reference` 와 Lambda 폴백이 같은 함수 — "분기 0, 코드 1벌"):
    1. `_ensure_adapters()`; `doc = firestore_admin.get_reference_registration(ref_id)`(None 이면 `RuntimeError("등록 문서 없음")`); `is_combo = bool(doc.get("isCombo"))`; `clip = doc.get("clipRange")`.
    2. `path = _download_analysis_video(bucket, key, analysis_id=ref_id)`; 전체를 `try … finally: Path(path).unlink(missing_ok=True)`.
    3. `frames = _FRAME_EXTRACTOR.extract(path)`; `real_fps = _FRAME_EXTRACTOR.effective_fps_for(path) or _FRAME_EXTRACTOR.probe_effective_fps(path)`; `len(frames) == 0` 또는 `real_fps` None/0 → `failed(server_error)`(Pitfall 7 — 0프레임은 NoHumanError 가 아니다) 후 return.
    4. `dur = len(frames) / real_fps`; `< models.REFERENCE_MIN_DURATION_SEC` → `failed(too_short)`; `> (COMBO_MAX if is_combo else MAX)` → `failed(too_long)` (앱 `durationSec` 의 서버 2차 방어, RESEARCH A13).
    5. `try: pose_frames, counts = _POSE_ESTIMATOR._engine.estimate_with_person_counts(frames, _POSE_ESTIMATOR._default_pole) except NoHumanError: failed(no_human, message=models.ERROR_MESSAGE[models.ERR_NO_HUMAN])` (D-09 재사용).
    6. `profile = measure_body_profile(pose_frames)`; `pose_frames = [dataclasses.replace(pf, body_shape=profile) …]`; `keypoints = to_coco17_array(pose_frames)` — `_RTMWNlfCompat.estimate_with_profile` 와 같은 순서(lazy import 동일).
    7. `report = build_keypoint_report(pose_frames, fps=real_fps)`; `n_stand = int(round(clip["execStartS"] * real_fps)) if clip and clip.get("execStartS") else int(round(registration_checks.STANDING_START_DEFAULT_SEC * real_fps))`; `verdict = registration_checks.check_registration(counts, report, n_stand=n_stand)`; `not verdict.ok` → message = `REGISTRATION_ERROR_MESSAGE[verdict.reason]` 에 `{joints}` 가 있으면 `.replace("{joints}", " · ".join(labels))`; `failed(verdict.reason, joints=labels)` 후 return.
    8. `raw = compute_joint_angles(keypoints)`; `unc = joint_uncertainty(keypoints)`; `filled = temporal_fill(raw, unc)`; `angles = np.nan_to_num(np.round(filled.astype(np.float64), 2), nan=0.0, posinf=0.0, neginf=0.0)`; `split = max_split(split_angle_series(keypoints))` → `round(float, 2)` 또는 None(extract 스크립트 :148-155 그대로; import 는 그 스크립트가 쓰는 모듈에서).
    9. `firestore_admin.set_reference_angles(ref_id, angles_flat=angles.reshape(-1).tolist(), joint_keys=list(skeleton.JOINT_KEYS), frames=int(angles.shape[0]), real_fps=float(real_fps), keypoint_report=dataclasses.asdict(report) if dataclass else report, split_angle=split)` → `set_reference_registration_status(ref_id, ACTIVE, extra={"isActive": True})` → `log.info("register-reference ok ref_id=%s frames=%s fps=%.3f dur=%.1f", …)`.
    10. `_trigger_self_check(bucket, key, uid, ref_id, ext, training_opt_in)`: `new_id = uuid.uuid4().hex`; `now`; `firestore_admin.create_analysis_doc(uid, new_id, {…, models.ANALYSIS_FIELD_SELF_CHECK_FOR_REFERENCE: ref_id})`(리터럴 `"selfCheckForReference"` 금지 — 38-01 상수; 나머지 키는 behavior 의 payload) **먼저** → `_s3.copy_object(Bucket=bucket, CopySource={"Bucket": bucket, "Key": key}, Key=build_upload_key(uid, new_id, ext))` → `set_reference_self_check(ref_id, status="pending", analysis_id=new_id)`; 어느 단계든 예외 → `log.exception` + `set_reference_self_check(ref_id, status="failed")`(등록 active 유지). uid 는 공급자 uid(RESEARCH A8 — 정은지 앱 기록에서 눈으로 확인 가능; `[ASSUMED 선택]` 주석).
    헬퍼 `_fail_registration(ref_id, code, *, message=None, joints=None)` 하나로 `failed` 쓰기 통일. 채점 경로 함수(`_process`·recognizer·dimensions·assemble.build_mode1) 호출 0 — docstring 에 명시.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && .venv/bin/python -m pytest tests/test_register_reference_pipeline.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && .venv/bin/python -m pytest tests/test_register_reference_pipeline.py -q` exits 0 with ≥ 9 tests (6 failure codes + happy path + self-check failure + tmp cleanup).
    - `grep -c "def _register_reference\|def _trigger_self_check\|def _fail_registration" backend/functions/pipeline/app.py` == 3.
    - `grep -c "estimate_with_person_counts" backend/functions/pipeline/app.py` ≥ 1 and `grep -c "check_registration(" backend/functions/pipeline/app.py` ≥ 1.
    - Inside `_register_reference` (sed range), `grep -c "_process(\|build_mode1\|recognize(" ` == 0.
    - `grep -c "create_analysis_doc" backend/functions/pipeline/app.py` line number < `grep -n "copy_object"` line number within `_trigger_self_check`.
    - `grep -c '"selfCheckForReference"' backend/functions/pipeline/app.py` == 0 and `grep -c "ANALYSIS_FIELD_SELF_CHECK_FOR_REFERENCE" backend/functions/pipeline/app.py` ≥ 1 (상수만 — 3벌 계약의 단일 출처).
  </acceptance_criteria>
  <done>등록 서비스가 기준 11개와 같은 함수 순서로 angles 를 쓰고, 6개 실패 코드가 합성 입력으로 각각 doc 에 남으며, 자기 재현성이 기존 경로로 트리거된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| S3 이벤트 → Lambda | 누구든 `reference/` 에 손으로 올린 객체가 이벤트를 만든다 |
| Lambda → Pod HTTP | 위임 요청(토큰 헤더) |
| Pod → Firestore/S3 | Admin 쓰기 · 객체 복사 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigate Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-07-1 | Tampering | `_handle_reference_upload` | mitigate | doc `registering` + `supplierUid == key uid` 확인 없으면 스킵 — 손 업로드·재전송이 등록을 못 깨운다 |
| T-38-07-2 | DoS | `_register_reference` 길이 | mitigate | `T / real_fps` 로 5~30초(콤보 60) 서버 2차 방어 · 100MB 는 presign 정책 밖이라 Pod 다운로드 상한은 [미확인] — SUMMARY 에 기록 |
| T-38-07-3 | Tampering | 기존 11개 | mitigate | `set_reference_angles` legacy 가드(38-06) + `parse_reference_key` 가 평면 키 거부 |
| T-38-07-4 | Spoofing | 자기 재현성 분석 doc | mitigate | uid = 토큰에서 온 키 uid(공급자) 만, `selfCheckForReference` 는 서버가 쓴다 |
| T-38-07-5 | Information disclosure | 로그 | mitigate | 키·ref_id·수치만, 토큰/SSM 값 없음 |
| T-38-07-6 | Denial of service | Pod 부재 | mitigate | `queued` + 정상 반환(재시도·DLQ 폭주 없음), 재개는 38-08 requeue |
| T-38-07-7 | Denial of service | `_trigger_self_check` copy_object → `uploads/` | mitigate | Pod 자격증명(`/workspace/aws_env.sh`, 메모리 aws-keys: 2026-05-29 "Pod 은 GetObject 만" 으로 분리된 `sunity-motion` 키)의 `uploads/*` PutObject 범위는 **[미확인]** — 거부되면 `selfCheckStatus:'failed'` 로 조용히 강등돼 Success ③ 을 못 잰다. 확인 = 38-09 T3 (E) `iam simulate-principal-policy`(읽기 전용, 부작용 0) → 38-14 T2 3-b Pod 세션 probe(비매칭 키 put+delete). 이 플랜의 SUMMARY 에 `[미확인 — 38-09/38-14 가 잰다]` 로 남긴다 |
| T-38-SC | Tampering | pip installs | accept | 설치 0 |
</threat_model>

<verification>
- `cd backend && .venv/bin/python -m pytest tests/test_pipeline_reference_dispatch.py tests/test_register_reference_pipeline.py tests/test_pipeline_dispatch.py -q` → 0 failed.
- 플랜 끝 push(다음 플랜 38-08 도 pipeline/app.py 를 만진다 — 순서 보장).
</verification>

<success_criteria>
- reference 키가 파이프라인 입구를 지나 Pod 유무로 갈리고, 등록 서비스가 angles·실패 4형·자기 재현성 트리거를 낸다(전부 pytest 로 강제).
</success_criteria>

<output>
Create `.planning/phases/38-supplier-link/38-07-SUMMARY.md` when done (Pod 다운로드 상한 [미확인] · Pod 자격증명 `uploads/` PutObject 범위 [미확인 — 38-09 T3 (E)/38-14 T2 3-b 가 잰다] 명시).
</output>
