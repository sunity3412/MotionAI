---
phase: 38-supplier-link
plan: 07
type: execute
wave: 3
depends_on: ["38-05", "38-06"]
files_modified:
  - backend/functions/pipeline/app.py
  - backend/tests/test_pipeline_reference_dispatch.py
  - backend/tests/test_register_reference_pipeline.py
  - backend/shared/python/sunity_shared/analysis/frame_extractor.py
  - backend/tests/test_frame_extractor_probe_duration.py
autonomous: true
requirements: [REQ-38-1, REQ-38-2, REQ-38-4]
must_haves:
  truths:
    - "D-05 / REQ-38-2 (리뷰 R1·R5·R9): `reference/{uid}/{refId}/upload.{ext}` 객체가 오면 파이프라인이 `head_object`(크기 ≤ 100MB, ETag) → `copy_object(CopySourceIfMatch=ETag)` 로 불변 `.../v1.{ext}` → v1 다운로드 → `probe_duration_sec` 로 길이 거부(추출 전) → 프레임 추출(9fps/640, `end_s` 캡) → RTMW(사람 수 포함) → **기준 11개와 같은 함수 순서**(estimate → measure_body_profile → to_coco17_array → compute_joint_angles → joint_uncertainty → temporal_fill, `peak, peak_idx = max_split(...)`) 로 angles(flat)·anglesRealFps·referenceKeypointReport·referenceSplitAngle 을 `reference/{refId}` 에 쓰고 `registrationStatus:'active'`·`isActive:true`·`videoS3Key=v1`·`videoETag` 로 만든다 — mode1 `RuntimeError('기준 모션 또는 keyframe 데이터 없음')` 의 원인(angles 부재)이 사라지고, 같은 URL 로의 두 번째 PUT 은 기준 영상·angles 를 바꾸지 못한다"
    - "D-20 (리뷰 R4): Pod 가 없으면(SSM `runpod-pod-expected` == down 또는 `/health` 12초 실패) doc 을 `queued`(queuedReason `pod_down`) 로 두고 **그 쓰기가 성공했을 때만** 정상 반환한다 — 상태 쓰기 자체가 실패하면 raise 해서 SQS 가 재전달한다(`AnalysisQueue` BatchSize 1 · maxReceiveCount 3 → `AnalysisDLQ`); `server_error` 로 위장하지 않는다(메모리 pod-link)"
    - "리뷰 R3: 위임 전에 `claim_registration(ref_id, job_id)` 가 True 를 줘야만 Pod 에 보낸다(중복 S3 이벤트 2건 = 위임 1회); Pod 는 `jobId` 를 받아 모든 쓰기를 job 가드로 한다 — 늦게 끝난 stale 작업은 active doc 을 못 뒤집는다"
    - "리뷰 R2·R8: 자기 재현성 트리거 순서 = `begin_self_check`(기준 doc 에 analysisId·pending·jobId 선기록) → `create_analysis_doc`(표식 2개 = 상수) → `copy_object(v1 → uploads/)`; 어느 단계든 실패면 `set_reference_self_check(failed)`(가드 통과 시)"
    - "D-09 / Success ② (리뷰 R6·R9): 실패 4형 + 길이(too_short/too_long) + 크기(too_large) 가 각각 공개 doc `registrationStatus:'failed'` + 비공개 doc `registrationError{code,message[,joints]}` 로 남는다 — 순서 `no_human` → `low_confidence` → `multiple_people` → `no_standing_start`; 합성 입력으로 강제(pytest, Pod 0)"
    - "D-10: 등록 `active` 직후 같은 영상(**v1 객체**)을 공급자 uid 의 `mode1` 분석 doc 으로 선작성하고 `uploads/{uid}/{newId}.{ext}` 로 S3 복사해 기존 경로가 자기 재현성을 잰다(기준 doc 선기록 → 분석 doc → 객체, 메모리 demo-only + R8)"
    - "Success ④: `uploads/` 학생 경로는 byte-무접촉 — 기존 `test_pipeline_dispatch.py` 전부 통과"
  artifacts:
    - path: "backend/functions/pipeline/app.py"
      provides: "_handle_reference_upload(claim·re-raise) · _pod_available · _register_reference(head/copy-v1/probe/판정/angles/active) · _trigger_self_check(begin→doc→copy) · lambda_handler 접두사 분기"
      contains: "def _register_reference"
    - path: "backend/shared/python/sunity_shared/analysis/frame_extractor.py"
      provides: "probe_duration_sec(path) — 추출 없이 imageio 메타 duration (판정 불가 None)"
      contains: "def probe_duration_sec"
  key_links:
    - from: "pipeline.lambda_handler"
      to: "s3keys.parse_reference_key"
      via: "for 루프 첫 줄에서 parse_upload_key 보다 먼저 — kind upload 만 디스패치, v1 이벤트는 로그 스킵"
      pattern: "parse_reference_key"
    - from: "pipeline._handle_reference_upload"
      to: "firestore_admin.claim_registration"
      via: "위임 전 claim(True 일 때만 _delegate_to_runpod)"
      pattern: "claim_registration"
    - from: "pipeline._register_reference"
      to: "_s3.copy_object(upload → v1, CopySourceIfMatch=ETag)"
      via: "불변 확정 객체 — angles 는 v1 바이트에서"
      pattern: "CopySourceIfMatch"
    - from: "pipeline._register_reference"
      to: "firestore_admin.set_reference_angles"
      via: "angles flat + anglesRealFps + referenceKeypointReport"
      pattern: "set_reference_angles"
    - from: "pipeline._register_reference"
      to: "registration_checks.check_registration"
      via: "person_counts + keypointReport + n_stand"
      pattern: "check_registration"
    - from: "pipeline._trigger_self_check"
      to: "_s3.copy_object → uploads/{uid}/{newId}.{ext}"
      via: "Pod 자격증명의 uploads/* 와 reference/* PutObject(copy 목적지 2곳: v1 · uploads) — 범위 [미확인]; 38-09 T3 (E) simulate-principal-policy / 38-14 T2 3-b probe 가 확인한다"
      pattern: "copy_object"
---

<objective>
파이프라인 Lambda 에 `reference/` 분기(REQ-38-1·D-20)와 등록 서비스 `_register_reference`(REQ-38-2·D-05·D-09·D-10)를 붙인다. 채점 경로(`_process`)는 부르지 않고 어댑터만 빌린다(RESEARCH 안티패턴 1). Pod 실행은 38-08 의 `/register-reference` 라우트가 같은 함수를 부른다.

Purpose: 지금 `reference/` 키는 알림에 안 오고(38-09 에서 고친다), 와도 `parse_upload_key → None → 스킵`(:10309-10312) 으로 죽는다(RESEARCH Q2). angles 를 쓰는 유일한 자동 경로가 이 함수다.
Output: `pipeline/app.py` 함수 + 분기, `frame_extractor.probe_duration_sec`, 테스트 3파일(전부 AWS/Pod/Firestore 0).

**리뷰 반영(2026-09-26):** R1(`max_split` 튜플 — 실제 함수로 테스트) · R3(claim 뒤 위임, Pod 에 jobId 전달) · R4(queued 쓰기 실패 = raise → SQS 재전달; DLQ 존재) · R5(upload → v1 `copy_object(CopySourceIfMatch)`, angles 는 v1 바이트, `videoS3Key`=v1) · R6(판정 순서는 38-05 `check_registration` 이 갖고 여기서는 `no_human` 만 앞에) · R8(`begin_self_check` 선기록 → 분석 doc → 복사) · R9(`head_object` 크기 · `probe_duration_sec` 길이 · `extract(end_s)` 캡 — 전부 디코딩 **전**).

Phase 38 전제(38-01 머리말) 적용. `pipeline/app.py` 는 10k 줄 — 필요한 범위만 Grep→Read 하고 같은 범위를 두 번 읽지 않는다.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/38-supplier-link/38-CONTEXT.md
@.planning/phases/38-supplier-link/38-RESEARCH.md
@.planning/phases/38-supplier-link/38-PATTERNS.md
@.planning/phases/38-supplier-link/38-05-SUMMARY.md
@.planning/phases/38-supplier-link/38-06-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `lambda_handler` 접두사 분기(kind upload 만) + `_pod_available`(SSM `runpod-pod-expected` + `/health` 12s) + `_handle_reference_upload`(queued 쓰기 실패 = raise · claim 뒤 위임 · stale/중복 = 스킵) + `_delegate_to_runpod(url=, extra=)` + dispatch 테스트(동시 이벤트 2 · 쓰기 실패 재전달)</name>
  <files>backend/functions/pipeline/app.py, backend/tests/test_pipeline_reference_dispatch.py</files>
  <behavior>
    - SQS 이벤트 키 `reference/u1/{refId}/upload.mp4` + `get_reference_registration` 가 `{registrationStatus:'registering', supplierUid:'u1', uploadKey:...}` + `_pod_available` → (False,'pod_expected_down') ⇒ `set_registration_queued(refId, queued_reason='pod_down')` 1회(True), `claim_registration` 0회, `_delegate_to_runpod` 0회, 예외 없음, 반환 `{"processed": 0}`
    - 같은 조건에서 `set_registration_queued` 가 **예외**(Firestore 장애 모사) ⇒ `lambda_handler` 가 그 예외를 **전파**한다(리뷰 R4 — 메시지를 소비하지 않아 SQS 가 재전달; 테스트는 `pytest.raises`)
    - `_pod_available` → (True,'healthy') ⇒ `claim_registration(refId, <32-hex job_id>)` 1회 → True → `_delegate_to_runpod(bucket, key, url=<…/register-reference>, extra={"jobId": job_id})` 1회 → `{"processed": 1}`; claim 이 False(다른 전달자가 먼저) ⇒ 위임 0회, warning 로그 `중복 이벤트 스킵`, `{"processed": 0}` — **같은 이벤트 2건을 연달아 넣으면 위임은 정확히 1회**(claim fake 가 첫 호출만 True)
    - claim 자체가 예외 ⇒ 전파(R4 — 상태 쓰기 실패는 재전달)
    - delegate 가 `RuntimeError` ⇒ `set_registration_failed(refId, job_id, error={code:'server_error', message:REGISTRATION_ERROR_MESSAGE['server_error']})`; handler 는 정상 종료; 그 writer 마저 예외면 로그만(lease 만료 뒤 requeue `--reclaim-stale` 가 줍는다 — 주석)
    - 키 `reference/u1/{refId}/v1.mp4`(확정 복사 이벤트) ⇒ info 로그 `확정 키 이벤트 무시` + 스킵(writer·claim 0회) — R5
    - doc 없음 / `registrationStatus ∉ {registering}` / `supplierUid != key uid` ⇒ 스킵(warning 로그, writer 호출 0) — 활성화 뒤 같은 URL 로 두 번째 PUT 이 와도 여기서 끝난다(R5)
    - `uploads/u3/a3.mp4` 키는 종전 그대로(기존 `test_pipeline_dispatch.py` 전부 통과) — 같은 이벤트에 uploads+reference 두 레코드가 섞여도 각자 처리
    - `_pod_available()`: `_runpod_enabled()` False → (False,'runpod_env_unset'); SSM 값 'down' → (False,'pod_expected_down'); SSM 예외(파라미터 없음/AccessDenied) → health 로 진행; health JSON `{"status":"ok","pipeline_loaded":true}` → (True,'healthy'); `pipeline_loaded:false` 또는 URLError/timeout → (False,'health_failed')
    - `_register_url()`: `https://x/analyze` → `https://x/register-reference`; trailing slash 없는 base 도 처리
  </behavior>
  <read_first>
    - backend/functions/pipeline/app.py :110-130 (s3keys import 블록) · :155-240 (`_s3`, `_s3_download`, `_RUNPOD_URL`/`_RUNPOD_TOKEN`/`_RUNPOD_TIMEOUT_S`, `_runpod_enabled`, `_delegate_to_runpod`) · :10297-10348 (`lambda_handler` 전체 — 예외를 전부 삼키는 학생 경로; reference 분기는 **상태 쓰기 실패만** 전파한다)
    - backend/template.yaml :140-154 (`AnalysisQueue` VisibilityTimeout 960 · `RedrivePolicy maxReceiveCount 3` → `AnalysisDLQ`) · :390-394 (PipelineFunction SQS 이벤트 `BatchSize: 1` — raise 가 이 메시지 하나만 재전달한다; `ReportBatchItemFailures` 는 이 함수엔 없다)
    - backend/infra/podwatch.yaml :130-170 (`pod_expected_up` · `health_url` · `probe` — 옮겨 올 원형) · backend/scripts/pod_teardown.py :120-128 (쓰기 측 SSM 이름 `/sunity/motion/runpod-pod-expected`) · backend/runpod_inference/start_server.sh :90-100
    - backend/tests/test_pipeline_dispatch.py (전체 — `_sqs_event` · `pipeline` fixture · 위임 실패·스킵 단언)
    - backend/shared/python/sunity_shared/firestore_admin.py (38-06 — `get_reference_registration` · `claim_registration` · `set_registration_queued` · `set_registration_failed`) · models.py (38-01) · s3keys.py (38-01 — `ParsedReferenceKey.kind/is_upload`)
    - .planning/phases/38-supplier-link/38-REVIEWS.md "### R3" · "### R4" · "### R5"
    - .planning/phases/38-supplier-link/38-PATTERNS.md "### `backend/functions/pipeline/app.py`" 절(lambda_handler 원문 · `_delegate_to_runpod` 원문 · 플래너 메모) + "## 관측 — 플래너가 알아야 할 코드 사실" 1·4 · RESEARCH "### A. 등록 경로의 최소 분기점" 1~3 · "### Q13" · 코드 예시 B
  </read_first>
  <action>
    RED 먼저(`test_pipeline_reference_dispatch.py` — `test_pipeline_dispatch.py` 의 fixture 를 복제하되 추가로 `app.firestore_admin.get_reference_registration`·`claim_registration`·`set_registration_queued`·`set_registration_failed`·`app._pod_available`·`app._delegate_to_runpod` 를 monkeypatch(호출 기록 + 예외 주입 가능); `_pod_available` 단위 테스트는 `app._ssm_get_parameter`·`urllib.request.urlopen` 를 monkeypatch) → 구현.
    `app.py`:
    - import 블록에 `parse_reference_key` 추가; `from sunity_shared.analysis import registration_checks` 는 Task 2 에서.
    - 상수: `_POD_EXPECTED_PARAM = os.environ.get("POD_EXPECTED_PARAM", "/sunity/motion/runpod-pod-expected").strip()` (주석: **쓰기 측** `start_server.sh:95`·`pod_teardown.py:125` 가 갱신하는 이름 — `podwatch.yaml` 기본값 `pod-expected` 는 읽기 측 다른 이름, PATTERNS 관측 1), `_HEALTH_TIMEOUT_S = 12`.
    - `_ssm_get_parameter(name) -> str | None`(boto3 ssm lazy import, 예외 → None + warning; 값 로그 금지) · `_health_url() -> str`(`_RUNPOD_URL` 의 trailing `/analyze` → `/health`, podwatch `health_url` 원문) · `_register_url() -> str`(`/analyze` → `/register-reference`) · `_pod_available() -> tuple[bool, str]`(위 behavior 순서; health 는 `urllib.request.Request(url, headers={"User-Agent": "sunity-motion-pilot/1.0 (+aws-lambda)"})`, timeout `_HEALTH_TIMEOUT_S`, `status=="ok" and pipeline_loaded`).
    - `_delegate_to_runpod(bucket, key, *, url: str | None = None, extra: dict | None = None)` — 본문에서 `_RUNPOD_URL` 대신 `url or _RUNPOD_URL`, payload 는 `{"bucket","key", **(extra or {})}`; 나머지 byte-동일(기존 호출부 무변경).
    - `_handle_reference_upload(bucket, key, ref) -> str`(반환 `'skipped'|'queued'|'delegated'|'failed'`; **raise 하는 경우는 정확히 하나 — 상태 쓰기(queued/claim) 실패**, 리뷰 R4; docstring 에 "SQS BatchSize 1 · maxReceiveCount 3 → DLQ 가 재전달·격리를 담당한다, D-20"): (0) `not ref.is_upload` → info `확정 키 이벤트 무시 key=%s` → `'skipped'` (i) `doc = firestore_admin.get_reference_registration(ref.ref_id)`; None 이거나 `doc.get("registrationStatus") != models.REGISTRATION_STATUS_REGISTERING` 이거나 `doc.get("supplierUid") != ref.uid` → warning `스킵: 등록 대기 doc 아님 key=%s status=%s` → `'skipped'` (손 업로드·재전송·활성화 뒤 재PUT 방어) (ii) `ok, why = _pod_available()`; not ok → `firestore_admin.set_registration_queued(ref.ref_id, queued_reason="pod_down")`(예외는 **전파** — 주석 "쓰기 실패를 삼키면 registering 이 영구히 남는다(R4)") + info 로그 `why` → `'queued'` (iii) `job_id = uuid.uuid4().hex`; `claimed = firestore_admin.claim_registration(ref.ref_id, job_id)`(예외 전파); not claimed → warning `중복 이벤트 스킵 ref_id=%s` → `'skipped'` (iv) `_delegate_to_runpod(bucket, key, url=_register_url(), extra={"jobId": job_id})` → `'delegated'`; 위임 예외 `# noqa: BLE001` → `log.exception` + `set_registration_failed(ref.ref_id, job_id, error={"code": models.REG_ERR_SERVER_ERROR, "message": models.REGISTRATION_ERROR_MESSAGE[...]})` → `'failed'`; 이 writer 가 던지면 로그만(lease 만료 → requeue 가 재claim).
    - `lambda_handler` for 루프 첫 줄: `ref = parse_reference_key(key)`; `if ref is not None: outcome = _handle_reference_upload(bucket, key, ref); processed += 1 if outcome == "delegated" else 0; continue` — 그 아래 기존 코드 byte-무접촉(학생 경로의 삼킴 정책은 그대로). 반환 형상 `{"processed": processed}` 유지(기존 테스트 단언).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && .venv/bin/python -m pytest tests/test_pipeline_reference_dispatch.py tests/test_pipeline_dispatch.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && .venv/bin/python -m pytest tests/test_pipeline_reference_dispatch.py tests/test_pipeline_dispatch.py -q` exits 0 (new file ≥ 12 tests incl. `test_duplicate_events_delegate_once`, `test_queued_write_failure_reraises`, `test_v1_event_skipped`; old file unchanged and green).
    - `grep -n "parse_reference_key(key)" backend/functions/pipeline/app.py` line number < `grep -n "parsed = parse_upload_key(key)"` line number inside `lambda_handler`.
    - `grep -c '"/sunity/motion/runpod-pod-expected"' backend/functions/pipeline/app.py` == 1 and `grep -c "_HEALTH_TIMEOUT_S = 12" backend/functions/pipeline/app.py` == 1.
    - `grep -c "def _handle_reference_upload\|def _pod_available\|def _register_url\|def _health_url\|def _ssm_get_parameter" backend/functions/pipeline/app.py` == 5; `grep -c "claim_registration(" backend/functions/pipeline/app.py` ≥ 1 and its line is before the `_delegate_to_runpod(` call inside `_handle_reference_upload`.
    - `git diff backend/functions/pipeline/app.py` shows the existing `uploads/` branch body unchanged (only the inserted prefix branch inside the loop and the `url=`/`extra=` keywords in `_delegate_to_runpod`).
  </acceptance_criteria>
  <done>reference 업로드 키가 스킵되지 않고 Pod 유무에 따라 queued/delegated 로 갈리며, 위임은 claim 뒤 한 번만, 상태 쓰기 실패는 재전달되고, 학생 경로는 무접촉이다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `frame_extractor.probe_duration_sec` + `_register_reference(bucket, key, uid, ref_id, job_id)` — head_object 크기(R9) · copy → v1 with ETag(R5) · probe 길이 거부 + `extract(end_s)` 캡(R9) · 어댑터 재사용 · 실패 판정(R6 순서) · `peak, peak_idx = max_split` (R1) · job 가드 angles/active · 자기 재현성 begin → doc → copy(R8) + 합성 강제 테스트(7 코드 + happy path + 재PUT 불변 + stale job)</name>
  <files>backend/shared/python/sunity_shared/analysis/frame_extractor.py, backend/tests/test_frame_extractor_probe_duration.py, backend/functions/pipeline/app.py, backend/tests/test_register_reference_pipeline.py</files>
  <behavior>
    (테스트는 `app._ensure_adapters` 를 no-op 으로, `app._s3` 를 가짜 **객체 저장소**(`objects: dict[key → {"ETag","ContentLength","body"}]`; `head_object` 는 그 메타, `copy_object` 는 `CopySourceIfMatch` 가 현재 ETag 와 다르면 `ClientError(PreconditionFailed)` 를 던지고 같으면 목적지에 같은 ETag 로 복사, `download_file` 은 빈 파일 생성 + 어느 키를 받았는지 기록), `app._FRAME_EXTRACTOR` 를 가짜(`probe_duration_sec(path)` 설정 가능 · `extract(path, end_s=None)` 은 `end_s` 를 기록하고 `np.zeros((T,64,64,3),uint8)` · `effective_fps_for(path)` → 10.0), `app._POSE_ESTIMATOR` 를 `_engine = RTMWPoseEngine.create_with_inferencer(mock, real_manifest)` + `_default_pole` 를 가진 객체로, `firestore_admin` writer 를 기록용 가짜(`get_reference_registration` 는 `{registrationStatus:'processing', supplierUid:uid, jobId:'A', uploadKey:key}`, `get_reference_registration_private` 는 `{isCombo, consent:{training:False}, …}`) 로 바꾼다. mock inferencer 는 프레임별 `(N,133,2)`·`(N,133)` 를 `side_effect` 로 돌려주며 COCO-17 인덱스(어깨 5/6 · 팔꿈치 7/8 · 손목 9/10 · 고관절 11/12 · 무릎 13/14 · 발목 15/16)에 서 있는 자세/창 자세 픽셀 좌표를 넣는다 — 헬퍼 `_frames_133(T, *, n_people, stand, ankle_stand_y, ankle_window_y, ankle_conf)`.)
    - **R9 크기:** `head_object` ContentLength = 101MB → `set_registration_failed(ref,'A', error={code:'too_large', message:REGISTRATION_ERROR_MESSAGE['too_large']})`, `download_file` 0회, `copy_object` 0회
    - **R5 복사:** happy path 에서 호출 순서 `head_object(upload)` → `copy_object(Bucket, CopySource={'Bucket','Key': upload key}, Key=<v1 key>, CopySourceIfMatch=<upload ETag>, MetadataDirective='COPY')` → `head_object(v1)`(ETag 동일 확인) → `download_file(v1 key)`(**upload 키가 아니라 v1**); `copy_object` 가 `PreconditionFailed` 를 던지면(처리 중 원본 교체) → `failed(server_error)`, angles 0회
    - **R9 길이(추출 전):** `probe_duration_sec` → 35.0(비콤보) → `failed(too_long)` 이고 `extract` **0회**; → 3.0 → `failed(too_short)`, `extract` 0회; `isCombo:true` 면 35.0 통과·65.0 → `too_long`; `probe_duration_sec` → None(메타 못 읽음) → `extract(path, end_s=31.0)`(= 상한 30 + 1.0 캡; 콤보 61.0) 가 호출되고 `len(frames)/real_fps` 로 다시 검사(T=350 @10fps → `too_long`)
    - N=0 전 프레임 → `failed(no_human, message=ERROR_MESSAGE['no_human'])`, angles 0회
    - 발목 conf 0.2(+ 바닥 위반 형상이어도) → `failed(low_confidence)` + `joints == ['왼쪽 발목','오른쪽 발목']` 이고 message 에 `잘 안 보인 부위: 왼쪽 발목 · 오른쪽 발목` 포함(`{joints}` 치환됨) — R6: `no_standing_start` 가 아니다
    - N=2 전 프레임(conf 0.9) → `failed(multiple_people)`
    - T=60(6.0s) 전 관절 conf 0.9, 서 있는 10프레임 발목 y 가 창 발목보다 **위**(바닥 위반) → `failed(no_standing_start)`
    - **R1 happy path**(T=60, N=1, 정상 자세, conf 0.9): 호출 순서 `set_reference_angles(ref, job_id='A', angles_flat=len T*8, joint_keys=list(skeleton.JOINT_KEYS), frames=T, real_fps=10.0, keypoint_report=<dict, frames==T>, split_angle=<float|None>)` → `set_registration_active(ref, 'A', video_s3_key=<v1 key>, video_etag=<ETag>)` → `begin_self_check(ref, job_id='A', analysis_id=<32-hex>)` → `create_analysis_doc(uid, <같은 32-hex>, payload{analysisId, mode:'mode1', referenceMotionId:ref, status:'uploading', fileName, createdAt, updatedAt, learningOptIn:<consent.training bool>, selfCheckForReference:ref, selfCheckJobId:'A' — 두 키 이름은 `models.ANALYSIS_FIELD_*` 상수로만 쓰고 테스트는 리터럴로 단언})` → `_s3.copy_object(Bucket=bucket, CopySource={'Bucket':bucket,'Key':<v1 key>}, Key='uploads/{uid}/{newId}.mp4')`; angles 값에 NaN 없음, 소수 2자리; `split_angle` 은 `float` 또는 `None`(**실제 `max_split` 사용, mock 금지** — 이 테스트 파일에 `assert max_split(np.array([20.0, 90.0, 70.0])) == (90.0, 1)` 언패킹 계약 단언 1개 추가)
    - `begin_self_check` 가 False(가드 실패) → 분석 doc·복사 0회, 등록은 active 그대로, warning
    - 복사(`copy_object` → uploads) 예외 → `set_reference_self_check(ref, status='failed', uid, analysis_id=newId, job_id='A')` 호출, 등록은 `active` 그대로(예외 전파 없음)
    - **stale job:** `get_reference_registration` 의 `jobId:'B'` 인데 `_register_reference(..., job_id='A')` → 어떤 writer 도 호출 0회, `RuntimeError` 없이 warning 종료(또는 명시적 `StaleJobError` — 라우트가 로그만)
    - **재PUT 불변(R5):** happy path 뒤 가짜 저장소의 upload 키에 새 ETag 로 "PUT" → `_handle_reference_upload`(Task 1) 는 status active 라 스킵; v1 키 ETag 와 `set_reference_angles` 호출 수(1) 불변
    - 임시 파일은 성공·실패 모두 `finally` 에서 삭제(테스트: 다운로드 경로가 존재하지 않는다)
    - `probe_duration_sec`(단위 테스트, `imageio.get_reader` monkeypatch): meta `{"duration": 12.34, "fps": 30}` → 12.34; meta 에 duration 없음 → None; reader 예외 → None; `fps` 만 있고 `nframes` 있으면 `nframes/fps` 폴백
  </behavior>
  <read_first>
    - backend/shared/python/sunity_shared/analysis/frame_extractor.py :68-100 (`effective_fps_for` · `probe_effective_fps` — 같은 reader/meta 어법, None 규율) · :111-170 (`extract(start_s, end_s)` — `end_idx` 에서 break: 프레임 캡의 근거)
    - backend/functions/pipeline/app.py :1453-1512 (`_RTMWNlfCompat` — `_engine`·`_default_pole`·`estimate_with_profile` 의 measure_body_profile→replace→to_coco17_array 순서) · :1514-1565 (`_ensure_adapters`) · :1575-1590 (`_angles_from_video` 4줄) · :1666-1692 (`_download_analysis_video`) · :2210-2220 (`effective_fps_for` 사용 예) · :7321 (`_s3.head_object` 선례) · :8901-8906 (mode1 이 요구하는 `angles`) · :9205-9218 (mode1 이 `ref["videoS3Key"]` 로 기준 영상을 서명·다운로드한다 — v1 키가 여기로 들어간다)
    - backend/scripts/extract_reference_angles.py :96-160 (`_rtmw_estimate_to_coco17` · `_extract_one` — compute_joint_angles → joint_uncertainty → occluded_mask → temporal_fill · round 2 · NaN→0.0 · **:153-154 `ref_split, ref_split_idx = max_split(split_angle_series(keypoints))` 뒤 `isfinite(ref_split)` 일 때만 `round(float(ref_split), 2)`** — R1 원본) · backend/shared/python/sunity_shared/analysis/features.py :265-279 (`max_split` 반환 `(float, idx)` / `(nan, -1)`)
    - backend/shared/python/sunity_shared/analysis/assemble.py :901-990 (`build_keypoint_report(pose_frames, *, fps)`) · registration_checks.py (38-05 — `check_registration` 순서) · skeleton.py (JOINT_KEYS) · firestore_admin.py (38-06 — job 가드 writer 시그니처 · `begin_self_check` · `set_reference_self_check`) · s3keys.py (`build_upload_key` · `build_reference_final_key` · `ParsedReferenceKey`) · models.py (38-01 — `ANALYSIS_FIELD_SELF_CHECK_FOR_REFERENCE` · `ANALYSIS_FIELD_SELF_CHECK_JOB_ID` · `REGISTRATION_ERROR_MESSAGE` · `REFERENCE_*_DURATION_SEC` · `MAX_VIDEO_BYTES` · `REG_ERR_TOO_LARGE`)
    - backend/tests/test_mode3_fault_zoom_selection.py :100-115 (`app._FRAME_EXTRACTOR` 교체 어법) · backend/tests/test_s3_download_accelerate.py :45-60 (`app._s3` 교체) · backend/tests/test_rtmw_engine.py :38-80 (mock inferencer · `real_manifest_path`)
    - .planning/phases/38-supplier-link/38-REVIEWS.md "### R1" · "### R5" · "### R6" · "### R8" · "### R9" · 38-REVIEW-EVIDENCE.json(`max_split_actual` = `[90.0, 1]`)
    - .planning/phases/38-supplier-link/38-RESEARCH.md "### A." 5 · "### C. 자기 재현성" · "Pitfall 3·6·7" · "Don't Hand-Roll" 표 · PATTERNS 플래너 메모(pipeline 절)
  </read_first>
  <action>
    RED 먼저(위 behavior 전부; `test_frame_extractor_probe_duration.py` 는 별도 파일) → 구현.
    `frame_extractor.py`: `probe_duration_sec(self, path) -> float | None` — `probe_effective_fps` 와 같은 reader/meta 어법(`imageio.get_reader` → `get_meta_data()`), `meta.get("duration")` 유한·양수면 그 값, 없으면 `nframes/fps` 폴백(둘 다 양수일 때), 아니면 None(모듈 상단 규율 — 0/추정값으로 눙치지 않는다); docstring 에 "Phase 38 리뷰 R9 — 전체 디코딩 전에 길이를 거른다; 판정 불가는 None 이라 호출측이 `extract(end_s=캡)` 으로 2차 방어".
    `app.py` 에 `_register_reference(bucket, key, uid, ref_id, job_id) -> None`(Pod `/register-reference` 와 Lambda 폴백이 같은 함수 — "분기 0, 코드 1벌"; `key` 는 **upload 키**):
    1. `_ensure_adapters()`; `doc = firestore_admin.get_reference_registration(ref_id)`(None 이면 `RuntimeError("등록 문서 없음")`); `doc.get("jobId") != job_id` → warning `stale job` 후 return(쓰기 0, R3); `priv = firestore_admin.get_reference_registration_private(ref_id) or {}`; `is_combo = bool(priv.get("isCombo"))`; `clip = priv.get("clipRange")`; `training = bool((priv.get("consent") or {}).get("training"))`; `ref = parse_reference_key(key)`(None 이거나 `not ref.is_upload` → `RuntimeError`).
    2. **R9 크기(다운로드 전):** `head = _s3.head_object(Bucket=bucket, Key=key)`; `head["ContentLength"] > models.MAX_VIDEO_BYTES` → `_fail(REG_ERR_TOO_LARGE)` 후 return; `etag = head["ETag"]`.
    3. **R5 확정 복사:** `final_key = build_reference_final_key(uid, ref_id, ref.ext)`; `_s3.copy_object(Bucket=bucket, CopySource={"Bucket": bucket, "Key": key}, Key=final_key, CopySourceIfMatch=etag, MetadataDirective="COPY")` — `ClientError` 코드 `PreconditionFailed` → `_fail(server_error)`(로그 "처리 중 원본 교체") 후 return; `_s3.head_object(Bucket, Key=final_key)["ETag"] != etag` → `_fail(server_error)`(단일 PUT 객체라 ETag=MD5 로 같아야 한다 — 주석). 이후 모든 읽기는 **final_key**.
    4. `path = _download_analysis_video(bucket, final_key, analysis_id=ref_id)`; 전체를 `try … finally: Path(path).unlink(missing_ok=True)`.
    5. **R9 길이(추출 전):** `limit = models.REFERENCE_COMBO_MAX_DURATION_SEC if is_combo else models.REFERENCE_MAX_DURATION_SEC`; `dur = _FRAME_EXTRACTOR.probe_duration_sec(path)`; `dur is not None and dur < models.REFERENCE_MIN_DURATION_SEC` → `too_short`; `dur is not None and dur > limit` → `too_long` — **둘 다 `extract` 전**. `frames = _FRAME_EXTRACTOR.extract(path, end_s=limit + 1.0)`(프레임 캡 — 라벨이 틀린 긴 파일이 메모리를 못 채운다); `real_fps = _FRAME_EXTRACTOR.effective_fps_for(path) or _FRAME_EXTRACTOR.probe_effective_fps(path)`; `len(frames) == 0` 또는 `real_fps` None/0 → `server_error`(Pitfall 7 — 0프레임은 NoHumanError 가 아니다); `dur2 = len(frames) / real_fps` 로 같은 두 검사를 한 번 더(probe None 경로의 2차 방어).
    6. `try: pose_frames, counts = _POSE_ESTIMATOR._engine.estimate_with_person_counts(frames, _POSE_ESTIMATOR._default_pole) except NoHumanError: _fail(no_human, message=models.ERROR_MESSAGE[models.ERR_NO_HUMAN])` (D-09 재사용 — `no_human` 이 판정 순서의 첫째, R6).
    7. `profile = measure_body_profile(pose_frames)`; `pose_frames = [dataclasses.replace(pf, body_shape=profile) …]`; `keypoints = to_coco17_array(pose_frames)` — `_RTMWNlfCompat.estimate_with_profile` 와 같은 순서(lazy import 동일).
    8. `report = build_keypoint_report(pose_frames, fps=real_fps)`; `n_stand = int(round(clip["execStartS"] * real_fps)) if clip and clip.get("execStartS") else int(round(registration_checks.STANDING_START_DEFAULT_SEC * real_fps))`; `verdict = registration_checks.check_registration(counts, report, n_stand=n_stand)`(순서 low_confidence → multiple_people → no_standing_start 는 38-05 소유); `not verdict.ok` → message = `REGISTRATION_ERROR_MESSAGE[verdict.reason]` 에 `{joints}` 가 있으면 `.replace("{joints}", " · ".join(labels))`; `_fail(verdict.reason, joints=labels)` 후 return.
    9. `raw = compute_joint_angles(keypoints)`; `unc = joint_uncertainty(keypoints)`; `filled = temporal_fill(raw, unc)`; `angles = np.nan_to_num(np.round(filled.astype(np.float64), 2), nan=0.0, posinf=0.0, neginf=0.0)`; **R1:** `peak, peak_idx = max_split(split_angle_series(keypoints))`; `split = round(float(peak), 2) if np.isfinite(peak) else None`(extract 스크립트 :153-154 그대로; `float(max_split(...))` 금지 — TypeError, 리뷰 재현); 로그에 `peak_idx`.
    10. `ok = firestore_admin.set_reference_angles(ref_id, job_id=job_id, angles_flat=angles.reshape(-1).tolist(), joint_keys=list(skeleton.JOINT_KEYS), frames=int(angles.shape[0]), real_fps=float(real_fps), keypoint_report=dataclasses.asdict(report) if dataclass else report, split_angle=split)`; not ok → warning `stale job` return. `firestore_admin.set_registration_active(ref_id, job_id, video_s3_key=final_key, video_etag=etag)`; not ok → return. `log.info("register-reference ok ref_id=%s job_id=%s frames=%s fps=%.3f dur=%.1f etag=%s", …)`.
    11. `_trigger_self_check(bucket, final_key, uid, ref_id, ext, training_opt_in, job_id)`(**R8 순서**): `new_id = uuid.uuid4().hex`; ① `firestore_admin.begin_self_check(ref_id, job_id=job_id, analysis_id=new_id)` — False 면 warning 후 return(가드 실패) ② `firestore_admin.create_analysis_doc(uid, new_id, {…, models.ANALYSIS_FIELD_SELF_CHECK_FOR_REFERENCE: ref_id, models.ANALYSIS_FIELD_SELF_CHECK_JOB_ID: job_id})`(리터럴 금지 — 38-01 상수; 나머지 키는 behavior 의 payload) ③ `_s3.copy_object(Bucket=bucket, CopySource={"Bucket": bucket, "Key": final_key}, Key=build_upload_key(uid, new_id, ext))`(v1 에서 복사 — upload 키 아님, R5); ②·③ 예외 → `log.exception` + `firestore_admin.set_reference_self_check(ref_id, status=models.SELF_CHECK_STATUS_FAILED, uid=uid, analysis_id=new_id, job_id=job_id)`(등록 active 유지). uid 는 공급자 uid(RESEARCH A8; `[ASSUMED 선택]` 주석).
    헬퍼 `_fail_registration(ref_id, job_id, code, *, message=None, joints=None)` 하나로 `set_registration_failed(ref_id, job_id, error={...})` 통일(False 면 stale — 로그). 채점 경로 함수(`_process`·recognizer·dimensions·assemble.build_mode1) 호출 0 — docstring 에 명시.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && .venv/bin/python -m pytest tests/test_register_reference_pipeline.py tests/test_frame_extractor_probe_duration.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && .venv/bin/python -m pytest tests/test_register_reference_pipeline.py tests/test_frame_extractor_probe_duration.py -q` exits 0 with ≥ 16 tests (7 failure codes + happy path + max_split contract + precondition-failed + probe-None cap + stale job + re-PUT immutability + self-check begin-fails + copy-fails + tmp cleanup; probe file ≥ 4).
    - `grep -c "def _register_reference\|def _trigger_self_check\|def _fail_registration" backend/functions/pipeline/app.py` == 3; `grep -c "def probe_duration_sec" backend/shared/python/sunity_shared/analysis/frame_extractor.py` == 1.
    - `grep -c "peak, peak_idx = max_split(" backend/functions/pipeline/app.py` == 1 and `grep -c "float(max_split(" backend/functions/pipeline/app.py` == 0 (R1); `grep -c "max_split" backend/tests/test_register_reference_pipeline.py` ≥ 1 with no monkeypatch of it (`grep -c "monkeypatch.setattr(.*max_split" ` == 0).
    - `grep -c "CopySourceIfMatch=" backend/functions/pipeline/app.py` == 1 and `grep -c "build_reference_final_key(" backend/functions/pipeline/app.py` ≥ 1; inside `_register_reference` the `head_object` line number < `copy_object` < `_download_analysis_video` < `probe_duration_sec` < `extract(` (R5·R9 order, sed range).
    - `grep -c "estimate_with_person_counts" backend/functions/pipeline/app.py` ≥ 1 and `grep -c "check_registration(" backend/functions/pipeline/app.py` ≥ 1; inside `_register_reference`, `grep -c "_process(\|build_mode1\|recognize(" ` == 0.
    - Inside `_trigger_self_check`: `begin_self_check` line < `create_analysis_doc` line < `copy_object` line (R8).
    - `grep -c '"selfCheckForReference"\|"selfCheckJobId"' backend/functions/pipeline/app.py` == 0 and `grep -c "ANALYSIS_FIELD_SELF_CHECK_FOR_REFERENCE\|ANALYSIS_FIELD_SELF_CHECK_JOB_ID" backend/functions/pipeline/app.py` ≥ 2 (상수만 — 3벌 계약의 단일 출처).
  </acceptance_criteria>
  <done>등록 서비스가 크기·길이를 디코딩 전에 거르고, 불변 v1 객체에서 기준 11개와 같은 함수 순서로 angles 를 쓰며, 7개 실패 코드가 합성 입력으로 각각 doc 에 남고, 자기 재현성이 선기록→doc→복사 순서로 기존 경로에 트리거된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| S3 이벤트 → Lambda | 누구든 `reference/` 에 손으로 올린 객체가 이벤트를 만든다 |
| Lambda → Pod HTTP | 위임 요청(토큰 헤더) |
| Pod → Firestore/S3 | Admin 쓰기 · 객체 복사 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigate Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-07-1 | Tampering | `_handle_reference_upload` | mitigate | `kind == upload` + doc `registering` + `supplierUid == key uid` 확인 없으면 스킵 — 손 업로드·재전송·확정 키 이벤트·활성화 뒤 재PUT 이 등록을 못 깨운다; 위임은 `claim_registration` True 뒤 한 번(R3) |
| T-38-07-2 | DoS | `_register_reference` 크기·길이 | mitigate | 다운로드 전 `head_object` ContentLength ≤ 100MB(`too_large`) · 추출 전 `probe_duration_sec` 5~30초(콤보 60) · `extract(end_s=상한+1)` 프레임 캡 · 추출 뒤 `T / real_fps` 재검사(R9) — 라벨이 틀린 파일도 전체 배열을 못 만든다 |
| T-38-07-8 | Tampering | 업로드 객체 교체(R5) | mitigate | `copy_object(CopySourceIfMatch=ETag)` 로 불변 v1 확정 + v1 ETag 재확인; angles·재생·자기 재현성 전부 v1; upload 키 재PUT 은 status ≠ registering 이라 스킵(테스트 `재PUT 불변`) |
| T-38-07-9 | Tampering(stale) | 늦게 끝난 작업 | mitigate | `_register_reference` 입구에서 `doc.jobId == job_id` 확인 + 모든 writer 가 job 가드(38-06) — stale 작업은 쓰기 0 |
| T-38-07-10 | Denial of service | 상태 쓰기 실패(R4) | mitigate | queued/claim 쓰기 실패는 raise → SQS 재전달(BatchSize 1, maxReceiveCount 3 → DLQ); 위임 뒤 실패는 lease 만료 → requeue `--reclaim-stale` |
| T-38-07-3 | Tampering | 기존 11개 | mitigate | `set_reference_angles` legacy 가드(38-06) + `parse_reference_key` 가 평면 키 거부 |
| T-38-07-4 | Spoofing | 자기 재현성 분석 doc | mitigate | uid = 토큰에서 온 키 uid(공급자) 만, `selfCheckForReference` 는 서버가 쓴다 |
| T-38-07-5 | Information disclosure | 로그 | mitigate | 키·ref_id·수치만, 토큰/SSM 값 없음 |
| T-38-07-6 | Denial of service | Pod 부재 | mitigate | `queued` + 정상 반환(재시도·DLQ 폭주 없음), 재개는 38-08 requeue |
| T-38-07-7 | Denial of service | `copy_object` → `reference/{uid}/{refId}/v1` 과 `uploads/` | mitigate | Pod 자격증명(`/workspace/aws_env.sh`, 메모리 aws-keys: 2026-05-29 "Pod 은 GetObject 만" 으로 분리된 `sunity-motion` 키)의 `reference/*`·`uploads/*` PutObject 범위는 **[미확인]** — `reference/*` 가 거부되면 등록 자체가 `server_error`(R5 복사 단계), `uploads/*` 가 거부되면 `selfCheckStatus:'failed'` 로 강등돼 Success ③ 을 못 잰다. 확인 = 38-09 T3 (E) `iam simulate-principal-policy`(두 접두사, 읽기 전용) → 38-14 T2 3-b Pod 세션 probe(비매칭 키 put+delete, 두 접두사). 이 플랜의 SUMMARY 에 `[미확인 — 38-09/38-14 가 잰다]` 로 남긴다 |
| T-38-SC | Tampering | pip installs | accept | 설치 0 |
</threat_model>

<verification>
- `cd backend && .venv/bin/python -m pytest tests/test_pipeline_reference_dispatch.py tests/test_register_reference_pipeline.py tests/test_frame_extractor_probe_duration.py tests/test_pipeline_dispatch.py -q` → 0 failed.
- 플랜 끝 push(다음 플랜 38-08 도 pipeline/app.py 를 만진다 — 순서 보장).
</verification>

<success_criteria>
- reference 업로드 키가 파이프라인 입구를 지나 Pod 유무로 갈리고(claim 뒤 위임 1회, 상태 쓰기 실패는 재전달), 등록 서비스가 불변 v1 에서 angles·실패 7코드·자기 재현성 트리거(선기록→doc→복사)를 낸다(전부 pytest 로 강제).
</success_criteria>

<output>
Create `.planning/phases/38-supplier-link/38-07-SUMMARY.md` when done (Pod 자격증명 `reference/*`·`uploads/*` PutObject 범위 [미확인 — 38-09 T3 (E)/38-14 T2 3-b 가 잰다] 명시; `max_split` 언패킹 단언이 실제 함수로 통과했음을 `[확인]`).
</output>
