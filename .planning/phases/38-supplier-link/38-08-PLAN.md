---
phase: 38-supplier-link
plan: 08
type: execute
wave: 4
depends_on: ["38-07"]
files_modified:
  - backend/functions/pipeline/app.py
  - backend/runpod_inference/server.py
  - backend/tests/test_self_score_hook.py
  - backend/tests/test_runpod_server.py
  - backend/scripts/requeue_reference_registrations.py
  - backend/tests/test_requeue_reference_registrations.py
autonomous: true
requirements: [REQ-38-2, REQ-38-4]
must_haves:
  truths:
    - "D-10 (리뷰 R2·R8): 자기 재현성 분석(`selfCheckForReference`·`selfCheckJobId` 가 있는 분석 doc)이 `complete_analysis` 되면 **기준 doc 이 권위** — `self_check_authorized(ref_doc, uid, analysis_id, job_id)` 를 지난 경우에만 `reference/{refId}.selfScore` 에 `result.overallScore`(= deductionBreakdown.final, 화면 점수) 한 줄이 쓰이고, 그 분석이 실패하면 같은 가드 뒤 `selfCheckStatus:'failed'` 가 남는다 — 위조 표식·다른 공급자 refId·stale analysis id·pending 전 완료는 전부 로그+스킵; 채점 무접촉, 기록 실패는 분석을 막지 않는다"
    - "리뷰 R3: Pod `POST /register-reference {bucket,key,jobId?}` 가 `/analyze` 와 같은 토큰 검증·202·BackgroundTasks 로 `pipeline._register_reference(..., job_id)` 를 부른다 — `jobId` 가 오면 그대로(Lambda/requeue 가 이미 claim), 없으면 Pod 가 `claim_registration` 을 직접 시도해 실패면 409; upload 키만 받는다; `/analyze` 는 byte-무접촉"
    - "D-20 (리뷰 R3·R4): Pod 기동 뒤 `requeue_reference_registrations.py` 가 `registrationStatus == 'queued'` 단일 쿼리(Spark 5만/일 캡 — 전수 스캔 없음)로 대기 건마다 `claim_registration` 을 지나 Pod 에 `{bucket, key: uploadKey, jobId}` 를 순차 POST 하고(두 번 돌리면 두 번째는 claim False 로 0건), `--reclaim-stale` 이 lease 만료 `processing` 을, `--sweep-expired` 가 presign 만료 `registering` 을(객체 있으면 claim+POST, 없으면 `expired`) 처리한다 — Pod 기동 절차 7단계로 문서화"
  artifacts:
    - path: "backend/runpod_inference/server.py"
      provides: "POST /register-reference"
      contains: "/register-reference"
    - path: "backend/scripts/requeue_reference_registrations.py"
      provides: "queued 재개 + --reclaim-stale + --sweep-expired 스크립트 (--dry-run) — claim 경유"
      contains: "claim_registration"
    - path: "backend/functions/pipeline/app.py"
      provides: "_record_self_score_if_needed · _mark_self_check_failed_if_needed (둘 다 self_check_authorized 경유)"
      contains: "def _record_self_score_if_needed"
  key_links:
    - from: "pipeline._process (complete_analysis 직후)"
      to: "firestore_admin.set_reference_self_check(status='done', score=result['overallScore'])"
      via: "_record_self_score_if_needed(meta, result, uid, analysis_id) → get_reference_registration → self_check_authorized → set_reference_self_check(job 가드)"
      pattern: "self_check_authorized"
    - from: "server.py register_reference"
      to: "pipeline_app._register_reference"
      via: "background.add_task"
      pattern: "_register_reference"
---

<objective>
등록 경로의 나머지 세 조각: (1) selfScore 훅(D-10 — `complete_analysis` 직후, `meta`·`result` 스코프) + 자기 분석 실패 시 `selfCheckStatus:'failed'` — 둘 다 **기준 doc 권위 가드**(리뷰 R2·R8) 뒤에만 (2) Pod `POST /register-reference` 라우트 — `jobId` 전달/자체 claim(R3) (3) Pod 기동 시 재개 스크립트(D-20) — claim 경유 + lease 만료 재claim + presign 만료 스윕(R3·R4). 셋 다 Pod 0 으로 테스트한다.

**리뷰 반영(2026-09-26):** R2(훅 권위 검사 — 위조 표식 4케이스) · R3(라우트·requeue 가 같은 claim 경유, requeue 2회 = 1회 실행) · R4(`--sweep-expired`: `head_object` 로 객체 유무 → claim+처리 / `expired`) · R8(pending 전 완료 훅 = 스킵).

Purpose: 훅 없이는 `selfScore` 가 영영 안 채워지고(RESEARCH Q6), 라우트 없이는 Lambda 위임이 갈 곳이 없고, requeue 없이는 Pod 부재 중 올린 등록이 영영 `queued` 다(메모리 build-it-and-schedule-it).
Output: `pipeline/app.py` 훅 2개 + 배선, `server.py` 라우트, 스크립트, 테스트 3파일.

Phase 38 전제(38-01 머리말) 적용.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/38-supplier-link/38-CONTEXT.md
@.planning/phases/38-supplier-link/38-RESEARCH.md
@.planning/phases/38-supplier-link/38-PATTERNS.md
@.planning/phases/38-supplier-link/38-07-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: selfScore 훅 2개(`_record_self_score_if_needed` · `_mark_self_check_failed_if_needed` — 기준 doc 권위 가드 경유, R2·R8) + `_process`/`lambda_handler`/`server._process_in_background` 배선 + Pod `POST /register-reference {bucket,key,jobId?}`(claim 전달/자체, R3) + 테스트(위조 표식 4케이스 · pending 전 완료)</name>
  <files>backend/functions/pipeline/app.py, backend/runpod_inference/server.py, backend/tests/test_self_score_hook.py, backend/tests/test_runpod_server.py</files>
  <behavior>
    - `_record_self_score_if_needed({"selfCheckForReference":"r1","selfCheckJobId":"A"}, {"overallScore": 97.3}, "u1", "a1")` 와 `get_reference_registration("r1")` → `{supplierUid:"u1", selfCheckAnalysisId:"a1", selfCheckJobId:"A", registrationStatus:"active"}` ⇒ `firestore_admin.set_reference_self_check("r1", status="done", uid="u1", analysis_id="a1", job_id="A", score=97.3)` 1회; meta 에 키 없음 → 0회(기준 doc 읽기도 0회); `overallScore` 없음/비수치 → 0회 + warning; writer 예외 → 삼키고 `log.exception`(반환 정상)
    - **R2 가드 4케이스(둘 다 훅 공통):** (a) 위조 표식 — 정상 사용자 `u9` 의 분석 doc 이 `selfCheckForReference:"r1"` 을 가리키는데 `r1.supplierUid == "u1"` ⇒ writer 0회 + warning `자기 재현성 표식 불일치`; (b) 다른 공급자의 refId(`supplierUid` 다름) ⇒ 0회; (c) stale analysis id(`r1.selfCheckAnalysisId == "a0"`) ⇒ 0회; (d) **완료 훅이 pending 기록보다 먼저**(`r1` 에 `selfCheckAnalysisId` 없음) ⇒ 0회 — 순서 보호(R8); 기준 doc 없음 ⇒ 0회. 가드는 `firestore_admin.self_check_authorized` 하나를 부른다(훅 안에 비교식을 다시 쓰지 않는다)
    - `_mark_self_check_failed_if_needed("u1","a1")` → `firestore_admin.get_analysis` 로 meta 를 읽어 두 표식이 있으면 기준 doc 을 읽고 같은 가드를 지나 `set_reference_self_check(ref, status="failed", uid, analysis_id="a1", job_id)`, 표식 없으면 0회; 읽기 예외 삼킴
    - `_process` 안: `complete_analysis` 직후 `log.info("분석 완료 …")` 다음 줄에 훅 호출 1회(테스트: `_process` 를 직접 돌리지 않고 `grep`/AST 로 호출 위치 단언 + 훅 단위 테스트)
    - `lambda_handler` 의 세 except 블록과 `server._process_in_background` 의 세 except 블록에서 `fail_analysis` 다음 `_mark_self_check_failed_if_needed(uid, analysis_id)`(예외 삼킴)
    - `POST /register-reference` 토큰 없음 → 401; `uploads/u/a.mp4` · `reference/ref-kip-up.mp4` · `reference/u/r/v1.mp4`(확정 키) 키 → 400 `invalid reference key`; 정상 upload 키 + `jobId:"A"` → 202 `{status:"accepted", uid, refId, jobId:"A"}` + `FakeMod._register_reference(bucket, key, uid, ref_id, "A")` 1회(TestClient 동기 실행) — `claim_registration` 0회(전달자가 이미 claim); `jobId` 없음 → Pod 가 `FakeAdmin.claim_registration(ref_id, <new 32-hex>)` 1회 — True 면 202 + 그 job_id 로 실행, False 면 **409** `{error: "registration not claimable"}` + 실행 0회(R3: 손 curl 도 같은 claim); `/analyze` 기존 테스트 전부 통과
    - `_register_in_background` 는 `Exception` 하나만 잡아 `set_registration_failed(ref_id, job_id, error={code:'server_error', message})` + `log.exception`(NoHuman 은 `_register_reference` 안에서 이미 코드로 매핑됨; stale 은 `_register_reference` 가 쓰기 없이 끝낸다)
  </behavior>
  <read_first>
    - backend/functions/pipeline/app.py :10105-10142 (`complete_analysis` 호출 + `log.info("분석 완료 …")` + "실패해도 분석은 이미 complete" 어법) · :10297-10348 (`lambda_handler` except 블록 3개) · :8595-8610 (`meta` 획득)
    - backend/runpod_inference/server.py :48-56 (import) · :142-170 (`AnalyzeRequest`/`AnalyzeResponse`) · :173-215 (`_verify_token` · `_process_in_background`) · :433-456 (`/analyze`)
    - backend/tests/test_runpod_server.py (전체 — `server_mod` fixture `FakeMod` · 401/400 · 202 동기 실행 단언)
    - backend/shared/python/sunity_shared/firestore_admin.py (38-06 — `self_check_authorized`(순수) · `set_reference_self_check(uid, analysis_id, job_id)` · `get_reference_registration` · `claim_registration` · `set_registration_failed` · `get_analysis` :2469) · s3keys.py (`parse_reference_key` · `is_upload`) · models.py (38-01 — `ANALYSIS_FIELD_SELF_CHECK_FOR_REFERENCE` · `ANALYSIS_FIELD_SELF_CHECK_JOB_ID` · `SELF_CHECK_STATUS_*`)
    - .planning/phases/38-supplier-link/38-REVIEWS.md "### R2" · "### R3" · "### R8"
    - .planning/phases/38-supplier-link/38-PATTERNS.md "### `backend/runpod_inference/server.py`" 절 + pipeline 절의 "selfScore 훅 자리" 발췌 · RESEARCH 코드 예시 E · "### C."
  </read_first>
  <action>
    RED 먼저(`test_self_score_hook.py` 는 `test_pipeline_dispatch.py` fixture 어법으로 `app` 재로드 + `firestore_admin` monkeypatch; `test_runpod_server.py` 는 `FakeMod` 에 `_register_reference` 추가) → 구현.
    `app.py`: 공통 `_self_check_target(meta, uid, analysis_id) -> tuple[str, str] | None` — `ref_id = (meta or {}).get(models.ANALYSIS_FIELD_SELF_CHECK_FOR_REFERENCE)`, `job_id = (meta or {}).get(models.ANALYSIS_FIELD_SELF_CHECK_JOB_ID)`(리터럴 금지 — 38-01 상수, 38-07 writer 와 같은 출처); 둘 중 하나라도 없으면 None; `ref_doc = firestore_admin.get_reference_registration(ref_id)`; `firestore_admin.self_check_authorized(ref_doc, uid=uid, analysis_id=analysis_id, job_id=job_id)` 아니면 `log.warning("자기 재현성 표식 불일치 uid=%s analysis_id=%s ref=%s")` + None(R2 — 기준 doc 이 권위; 분석 doc 의 표식은 힌트일 뿐). `_record_self_score_if_needed(meta, result, uid, analysis_id) -> None` — target 없으면 return; `score = result.get("overallScore")`(화면 점수 = `deductionBreakdown.final` 과 같은 필드 — 주석에 "dimensionScores 아님, CLAUDE.md §7 ★ 소비처 추적" 명시); 수치 아니면 warning 후 return; `firestore_admin.set_reference_self_check(ref_id, status=models.SELF_CHECK_STATUS_DONE, uid=uid, analysis_id=analysis_id, job_id=job_id, score=float(score))`; 예외 `# noqa: BLE001` → `log.exception("selfScore 기록 실패 ref=%s analysis_id=%s")` (기록 실패가 분석을 막지 않는다 — :10137 어법). `_mark_self_check_failed_if_needed(uid, analysis_id)` — `firestore_admin.get_analysis` 로 meta, `_self_check_target` 통과 시 `set_reference_self_check(ref, status=SELF_CHECK_STATUS_FAILED, uid=uid, analysis_id=analysis_id, job_id=job_id)`; 모든 예외 삼킴. `_process` 의 `log.info("분석 완료 …")` 바로 다음 줄에 `_record_self_score_if_needed(meta, result, uid, analysis_id)` 한 줄(채점 코드 무접촉). `lambda_handler` except 3곳의 `fail_analysis(...)` 다음 줄에 `_mark_self_check_failed_if_needed(uid, analysis_id)`.
    `server.py`: import 에 `parse_reference_key` 추가; `class RegisterReferenceRequest(BaseModel): bucket: str; key: str; jobId: str | None = None`(description `reference/{uid}/{refId}/upload.{ext}` — upload 키만; `jobId` = 전달자(Lambda/requeue)가 claim 한 작업 id, 없으면 Pod 가 claim), `class RegisterReferenceResponse(BaseModel): status: str; uid: str; refId: str; jobId: str`; `_register_in_background(bucket, key, uid, ref_id, job_id)`(위 behavior); `@app.post("/register-reference", status_code=202, response_model=RegisterReferenceResponse)` — `_verify_token` Depends, `ref = parse_reference_key(req.key)`; `ref is None or not ref.is_upload` → 400 `invalid reference key: {key}`; `job_id = req.jobId or uuid.uuid4().hex`; `req.jobId is None` 이면 `pipeline_app.firestore_admin.claim_registration(ref.ref_id, job_id)` — False → 409 `registration not claimable`(R3: 어느 입구든 같은 claim); `log.info("/register-reference accepted bucket=%s uid=%s refId=%s jobId=%s")`, `background.add_task(_register_in_background, …, job_id)`. `_process_in_background` except 3곳에 `pipeline_app._mark_self_check_failed_if_needed(uid, analysis_id)` (pipeline 모듈은 이미 `_load_pipeline_module()` 로 잡혀 있다 — 없으면 try/except 로 감싼다). `/analyze` 본문 무접촉. `/health` 응답에 `"routes": ["analyze","register-reference"]` 를 **추가하지 않는다**(podwatch `probe()` 계약 유지) — 대신 SUMMARY 에 `curl -X POST …/register-reference` 무토큰 401/503 확인법을 적는다.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && .venv/bin/python -m pytest tests/test_self_score_hook.py tests/test_runpod_server.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && .venv/bin/python -m pytest tests/test_self_score_hook.py tests/test_runpod_server.py -q` exits 0 (hook file ≥ 10 tests; server file ≥ 5 new tests).
    - `grep -n "_record_self_score_if_needed(meta, result, uid, analysis_id)" backend/functions/pipeline/app.py` returns exactly one call line, and it is greater than the line of `log.info("분석 완료 uid=%s analysis_id=%s mode=%s"` by ≤ 3.
    - `grep -c "_mark_self_check_failed_if_needed(uid, analysis_id)" backend/functions/pipeline/app.py` == 3 and `grep -c "_mark_self_check_failed_if_needed" backend/runpod_inference/server.py` == 3; `grep -c "self_check_authorized(" backend/functions/pipeline/app.py` ≥ 1 and `grep -c "supplierUid" backend/functions/pipeline/app.py` inside the two hooks == 0 (비교식은 firestore_admin 의 가드 하나에만).
    - `test_self_score_hook.py` has ≥ 4 guard tests (forged marker · other supplier refId · stale analysis id · completion-before-pending) each asserting writer call count 0; `test_runpod_server.py` has a 409 test for `jobId` absent + claim False and a test that `jobId` present skips claim.
    - `grep -c '@app.post("/register-reference"' backend/runpod_inference/server.py` == 1; `git diff backend/runpod_inference/server.py` shows the `/analyze` function body unchanged.
    - `grep -c '"selfCheckForReference"' backend/functions/pipeline/app.py backend/runpod_inference/server.py` → 0 for both (상수 `ANALYSIS_FIELD_SELF_CHECK_FOR_REFERENCE` 만; 테스트 파일의 리터럴은 허용).
  </acceptance_criteria>
  <done>selfScore 가 기준 doc 권위 가드를 지난 경우에만 화면 점수와 같은 값으로 실리고, Pod 라우트가 claim 을 전달받거나 직접 지나 등록 서비스를 부르며, 자기 분석 실패가 같은 가드 뒤 `selfCheckStatus:'failed'` 로 남는다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `requeue_reference_registrations.py` — `queued` 단일 쿼리 → 건마다 `claim_registration` → Pod `/register-reference {bucket,key,jobId}` 순차 POST(`--dry-run`) + `--reclaim-stale`(lease 만료 processing) + `--sweep-expired`(presign 만료 registering: 객체 있으면 claim+POST / 없으면 expired) + 테스트 + Pod 기동 절차 7단계 문서화</name>
  <files>backend/scripts/requeue_reference_registrations.py, backend/tests/test_requeue_reference_registrations.py</files>
  <behavior>
    - `--dry-run` 이면 `list_reference_registrations_by_status("queued")` 결과를 표(ref_id · uploadKey · supplierUid · registrationUpdatedAt · 계획된 행동)로 출력하고 POST 0회·claim 0회·writer 0회
    - 실제 실행(R3): doc 마다 `job_id = uuid4().hex` → `firestore_admin.claim_registration(ref_id, job_id)` — False 면 그 건 스킵(로그 `claim 실패 — 다른 실행이 잡음`) → True 면 `POST {register_url} {"bucket": VIDEO_BUCKET, "key": doc["uploadKey"], "jobId": job_id}` 헤더 `Content-Type/Accept/User-Agent(sunity-motion-pilot/1.0 (+requeue))/X-RunPod-Token` 4개, 순차, 200/202 외 응답은 그 건만 실패로 기록하고 다음 건 진행; 종료 코드 = 실패 건수 > 0 이면 1. **같은 인자로 두 번 돌리면** 두 번째는 모든 건이 `processing`(lease 살아 있음) 이라 claim False → POST 0회(테스트 `test_requeue_twice_posts_once`)
    - `uploadKey` 가 `parse_reference_key` 를 못 지나거나 `kind != upload` 면 그 건은 스킵(경고) — legacy·확정 키 방어
    - URL/토큰은 인자 `--register-url`/`--token` 또는 SSM(`/sunity/motion/runpod-analyze-url` → `/analyze`→`/register-reference` 치환, `/sunity/motion/runpod-auth-token`) — 테스트는 인자 경로만
    - `--reclaim-stale`: `list_reference_registrations_by_status("processing")` 중 `leaseUntil < now` 인 건만 claim(재claim 은 `claim_registration` 의 lease 만료 규칙) → POST; lease 살아 있는 건은 스킵(테스트: 살아 있는 1건 + 만료 1건 → POST 1회)
    - `--sweep-expired`(R4): `list_reference_registrations_by_status("registering")` 중 `uploadExpiresAt < now` 인 건만(클라이언트 측 필터 — 복합 인덱스 불필요) `s3.head_object(uploadKey)`: 200 → claim → POST(이벤트 유실 뒤 객체 존재 케이스); 404 → `firestore_admin.set_registration_expired(ref_id)`(PUT 취소·미업로드 케이스); 그 외 오류 → 그 건 실패 기록. `uploadExpiresAt >= now` 인 건은 건드리지 않는다(아직 올릴 수 있다). 테스트: 만료+객체 없음 → `set_registration_expired` 1회·POST 0회; 만료+객체 있음 → claim 1회·POST 1회; 미만료 → 0회
  </behavior>
  <read_first>
    - backend/scripts/e2e_app_path.py :1-40 (헤더·사용법·`http_json`) · :56-61 (`firestore_client` — 리포 루트 SA json) · backend/scripts/intake_clips.py :30-40 (`--dry-run` 관례) · backend/scripts/pod_teardown.py :1-30 (머리말 경고 어법) · :120-128 (SSM 이름)
    - backend/functions/pipeline/app.py (38-07 — `_register_url` 변환 규칙 · `_delegate_to_runpod` 헤더 4개 · `extra={"jobId"}`)
    - backend/shared/python/sunity_shared/firestore_admin.py (38-06 — `list_reference_registrations_by_status` · `claim_registration` · `set_registration_expired`) · s3keys.py (`parse_reference_key` · `is_upload`) · models.py (`REGISTRATION_STATUS_*`)
    - .planning/phases/38-supplier-link/38-REVIEWS.md "### R3" · "### R4"(`registering` 만료·객체 존재/부재 복구)
    - backend/tests/test_intake_clips.py :1-40 (스크립트 모듈 import 어법 — conftest 가 `backend/scripts` 를 sys.path 에 넣는다)
    - /Users/kimtaesung/.claude/projects/-Users-kimtaesung-Dev-SunityMotion/memory/demo-only-pod-bring-up-procedure.md (기동 절차 1~6 — 여기에 "7. requeue" 를 붙일 문장)
    - .planning/phases/38-supplier-link/38-PATTERNS.md "### `backend/scripts/requeue_reference_registrations.py`" 절 · PATTERNS 관측 8(`e2e_app_path.py:110` 의 4값 종료 목록 복사 금지)
  </read_first>
  <action>
    RED 먼저(`test_requeue_reference_registrations.py`: 모듈 import 후 `firestore_admin.list_reference_registrations_by_status`·`claim_registration`(첫 호출만 True 인 fake, 호출 기록)·`set_registration_expired`·`boto3.client`(head_object 200/404)·`urllib.request.urlopen` 을 monkeypatch, `main(["--register-url", "https://x/register-reference", "--token", "t", "--bucket", "b"], now_ms=…)` 호출) → 구현.
    스크립트 헤더 docstring(e2e 어법): 왜(D-20 — Pod 부재 중 올린 등록은 `queued` 로 남고 SQS 재시도는 재개 수단이 아니다 RESEARCH Q13; 리뷰 R3·R4 — 재개도 claim 을 지나야 두 번 실행되지 않고, presign 만료 뒤 `registering` 은 스윕이 닫는다) · 사용법 4줄(`--dry-run` / 실제(queued) / `--reclaim-stale` / `--sweep-expired`) · **Pod 기동 절차의 7단계** 라는 문장 + "메모리 demo-only-pod-bring-up-procedure 갱신 대상". 인자: `--register-url`·`--token`(없으면 SSM, `AWS_PROFILE=sunity-motion` 필요 주석)·`--bucket`(기본 `sunity-motion-pilot-videos`)·`--dry-run`·`--reclaim-stale`·`--sweep-expired`. `main(argv, *, now_ms=None) -> int` 로 테스트 가능하게. Firestore 는 `firestore_admin` 함수 3개(`list_reference_registrations_by_status` · `claim_registration` · `set_registration_expired`)만(SA 는 `FIREBASE_SA_PATH` env 또는 리포 루트 json — `e2e_app_path.firestore_client` 어법을 `_ensure_firebase` 가 받아들이는 env 로) — 읽기 수 = 대기 건수(Spark 캡 주석). S3 는 `boto3.client("s3").head_object` 만(스윕). POST 는 `urllib` 표준 라이브러리만(위임 함수와 같은 헤더 + `jobId`). 출력은 `ref_id=… key=… job=… → 202|skip(claim)|expired` 한 줄씩. **스크립트가 쓰는 doc 필드는 claim(processing·jobId·leaseUntil)과 expired 뿐** — 나머지 상태 갱신은 Pod 가 한다.
    SUMMARY 에 Pod 기동 절차 7단계 문장(메모리 갱신은 오케스트레이터/belle 몫이라 문장만 제안).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && .venv/bin/python -m pytest tests/test_requeue_reference_registrations.py -q && .venv/bin/python scripts/requeue_reference_registrations.py --help >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && .venv/bin/python -m pytest tests/test_requeue_reference_registrations.py -q` exits 0 (≥ 9 tests: dry-run 0 POST/0 claim · claim 뒤 POST 헤더 4개 + `jobId` · `test_requeue_twice_posts_once` · 비202 실패 카운트 · legacy/v1 키 스킵 · exit code · reclaim-stale(살아 있는 lease 스킵) · sweep 만료+객체 없음 → expired · sweep 만료+객체 있음 → claim+POST · sweep 미만료 무접촉).
    - `backend/.venv/bin/python backend/scripts/requeue_reference_registrations.py --help` exits 0 and mentions `--dry-run`, `--register-url`, `--reclaim-stale`, `--sweep-expired`.
    - `grep -c "claim_registration\|set_registration_expired" backend/scripts/requeue_reference_registrations.py` ≥ 2; `grep -c "head_object" backend/scripts/requeue_reference_registrations.py` == 1.
    - `grep -c '"completed"\|"error"' backend/scripts/requeue_reference_registrations.py` == 0 (4값 종료 목록 미복사).
    - `grep -c "list_reference_registrations_by_status" backend/scripts/requeue_reference_registrations.py` ≥ 3 (queued·processing·registering) and `grep -c "stream()" backend/scripts/requeue_reference_registrations.py` == 0 (전수 스캔 없음 — 조회는 firestore_admin 경유).
  </acceptance_criteria>
  <done>Pod 기동 뒤 한 명령으로 대기 등록이 claim 을 지나 한 번씩만 재개되고, 죽은 작업과 미업로드 등록이 각각 재claim·expired 로 닫히며, 그 절차가 기동 절차 7단계로 문서화된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lambda/스크립트 → Pod HTTP | `X-RunPod-Token` 공유 비밀 |
| Pod → Firestore | Admin 쓰기 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-08-1 | Spoofing | `/register-reference` | mitigate | `/analyze` 와 같은 `_verify_token`(미설정 503, 불일치 401) |
| T-38-08-2 | Tampering | `/register-reference` 키 | mitigate | `parse_reference_key` 실패 400 — legacy·uploads 키로 등록 서비스를 못 부른다 |
| T-38-08-3 | Spoofing/Tampering | selfScore | mitigate | 기준 doc 이 권위 — `self_check_authorized`(supplierUid·selfCheckAnalysisId·selfCheckJobId 3중 일치) 뒤에만 기록(R2·R8); 분석 doc 표식은 힌트일 뿐(규칙이 클라이언트 쓰기 거부, 38-06 T3); 훅은 `overallScore` 수치 검증 |
| T-38-08-6 | Tampering(중복) | requeue / Pod 라우트 | mitigate | 두 입구 모두 `claim_registration` 경유(R3) — requeue 2회·손 curl·Lambda 동시 = 실행 1회 |
| T-38-08-7 | Denial of service | `--sweep-expired` 의 `expired` 쓰기 | mitigate | `registering` + `uploadExpiresAt < now` + `head_object` 404 일 때만(R4); 아직 올릴 수 있는 건은 무접촉 |
| T-38-08-4 | Information disclosure | requeue 출력 | mitigate | ref_id·키·상태만 출력, 토큰은 인자/SSM 에서 읽고 출력 금지 |
| T-38-08-5 | DoS | requeue 폭주 | accept | 순차 POST, 대기 건수 = 공급자 1~2명의 등록 수 |
| T-38-SC | Tampering | pip installs | accept | 설치 0 |
</threat_model>

<verification>
- `cd backend && .venv/bin/python -m pytest tests/test_self_score_hook.py tests/test_runpod_server.py tests/test_requeue_reference_registrations.py tests/test_pipeline_dispatch.py tests/test_pipeline_reference_dispatch.py tests/test_register_reference_pipeline.py -q` → 0 failed.
- 전체 suite: `cd backend && .venv/bin/python -m pytest tests -q` → 0 failed(소요 시간을 SUMMARY 에 `[확인]` — RESEARCH A15 미확인 해소).
- 플랜 끝 push(38-09 배포가 이 코드를 올린다).
</verification>

<success_criteria>
- 등록 경로 코드가 Lambda·Pod·재개 스크립트까지 닫혀 배포 가능 상태다(전 테스트 green).
</success_criteria>

<output>
Create `.planning/phases/38-supplier-link/38-08-SUMMARY.md` when done (전체 pytest 소요 시간 기록).
</output>
