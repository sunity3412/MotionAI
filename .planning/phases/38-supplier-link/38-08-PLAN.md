---
phase: 38-supplier-link
plan: 08
type: execute
wave: 4
depends_on: ["38-07"]
files_modified:
  - backend/functions/pipeline/app.py
  - backend/runpod_inference/server.py
  - backend/tests/test_self_score_hook.py
  - backend/tests/test_runpod_server.py
  - backend/scripts/requeue_reference_registrations.py
  - backend/tests/test_requeue_reference_registrations.py
autonomous: true
requirements: [REQ-38-2, REQ-38-4]
must_haves:
  truths:
    - "D-10: 자기 재현성 분석(`selfCheckForReference` 가 있는 분석 doc)이 `complete_analysis` 되면 `reference/{refId}.selfScore` 에 `result.overallScore`(= deductionBreakdown.final, 화면 점수) 한 줄이 쓰이고, 그 분석이 실패하면 `selfCheckStatus:'failed'` 가 남는다 — 채점 무접촉, 기록 실패는 분석을 막지 않는다"
    - "Pod `POST /register-reference {bucket,key}` 가 `/analyze` 와 같은 토큰 검증·202·BackgroundTasks 로 `pipeline._register_reference` 를 부르고, `/analyze` 는 byte-무접촉"
    - "D-20: Pod 기동 뒤 `requeue_reference_registrations.py` 가 `registrationStatus == 'queued'` 단일 쿼리(Spark 5만/일 캡 — 전수 스캔 없음)로 대기 건을 Pod 에 순차 POST 한다 — 만들어 놓고 스케줄에 안 올린 기능이 되지 않게 Pod 기동 절차 6단계로 문서화"
  artifacts:
    - path: "backend/runpod_inference/server.py"
      provides: "POST /register-reference"
      contains: "/register-reference"
    - path: "backend/scripts/requeue_reference_registrations.py"
      provides: "queued 등록 재개 스크립트 (--dry-run)"
      contains: "registrationStatus"
    - path: "backend/functions/pipeline/app.py"
      provides: "_record_self_score_if_needed · _mark_self_check_failed_if_needed"
      contains: "def _record_self_score_if_needed"
  key_links:
    - from: "pipeline._process (complete_analysis 직후)"
      to: "firestore_admin.set_reference_self_check(status='done', score=result['overallScore'])"
      via: "_record_self_score_if_needed(meta, result, uid, analysis_id)"
      pattern: "_record_self_score_if_needed"
    - from: "server.py register_reference"
      to: "pipeline_app._register_reference"
      via: "background.add_task"
      pattern: "_register_reference"
---

<objective>
등록 경로의 나머지 세 조각: (1) selfScore 훅(D-10 — `complete_analysis` 직후, `meta`·`result` 스코프) + 자기 분석 실패 시 `selfCheckStatus:'failed'` (2) Pod `POST /register-reference` 라우트 (3) Pod 기동 시 `queued` 재개 스크립트(D-20). 셋 다 Pod 0 으로 테스트한다.

Purpose: 훅 없이는 `selfScore` 가 영영 안 채워지고(RESEARCH Q6), 라우트 없이는 Lambda 위임이 갈 곳이 없고, requeue 없이는 Pod 부재 중 올린 등록이 영영 `queued` 다(메모리 build-it-and-schedule-it).
Output: `pipeline/app.py` 훅 2개 + 배선, `server.py` 라우트, 스크립트, 테스트 3파일.

Phase 38 전제(38-01 머리말) 적용.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/38-supplier-link/38-CONTEXT.md
@.planning/phases/38-supplier-link/38-RESEARCH.md
@.planning/phases/38-supplier-link/38-PATTERNS.md
@.planning/phases/38-supplier-link/38-07-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: selfScore 훅 2개(`_record_self_score_if_needed` · `_mark_self_check_failed_if_needed`) + `_process`/`lambda_handler`/`server._process_in_background` 배선 + Pod `POST /register-reference` + 테스트</name>
  <files>backend/functions/pipeline/app.py, backend/runpod_inference/server.py, backend/tests/test_self_score_hook.py, backend/tests/test_runpod_server.py</files>
  <behavior>
    - `_record_self_score_if_needed({"selfCheckForReference":"r1"}, {"overallScore": 97.3}, "u1", "a1")` → `firestore_admin.set_reference_self_check("r1", status="done", analysis_id="a1", score=97.3)` 1회; meta 에 키 없음 → 0회; `overallScore` 없음/비수치 → 0회 + warning; writer 예외 → 삼키고 `log.exception`(반환 정상)
    - `_mark_self_check_failed_if_needed("u1","a1")` → `firestore_admin.get_analysis` 로 meta 를 읽어 `selfCheckForReference` 가 있으면 `set_reference_self_check(ref, status="failed", analysis_id="a1")`, 없으면 0회; 읽기 예외 삼킴
    - `_process` 안: `complete_analysis` 직후 `log.info("분석 완료 …")` 다음 줄에 훅 호출 1회(테스트: `_process` 를 직접 돌리지 않고 `grep`/AST 로 호출 위치 단언 + 훅 단위 테스트)
    - `lambda_handler` 의 세 except 블록과 `server._process_in_background` 의 세 except 블록에서 `fail_analysis` 다음 `_mark_self_check_failed_if_needed(uid, analysis_id)`(예외 삼킴)
    - `POST /register-reference` 토큰 없음 → 401; `uploads/u/a.mp4` 또는 `reference/ref-kip-up.mp4` 키 → 400 `invalid reference key`; 정상 키 → 202 `{status:"accepted", uid, refId}` + `FakeMod._register_reference(bucket, key, uid, ref_id)` 1회(TestClient 동기 실행); `/analyze` 기존 테스트 전부 통과
    - `_register_in_background` 는 `Exception` 하나만 잡아 `set_reference_registration_status(ref_id, 'failed', error={code:'server_error', message})` + `log.exception`(NoHuman 은 `_register_reference` 안에서 이미 코드로 매핑됨)
  </behavior>
  <read_first>
    - backend/functions/pipeline/app.py :10105-10142 (`complete_analysis` 호출 + `log.info("분석 완료 …")` + "실패해도 분석은 이미 complete" 어법) · :10297-10348 (`lambda_handler` except 블록 3개) · :8595-8610 (`meta` 획득)
    - backend/runpod_inference/server.py :48-56 (import) · :142-170 (`AnalyzeRequest`/`AnalyzeResponse`) · :173-215 (`_verify_token` · `_process_in_background`) · :433-456 (`/analyze`)
    - backend/tests/test_runpod_server.py (전체 — `server_mod` fixture `FakeMod` · 401/400 · 202 동기 실행 단언)
    - backend/shared/python/sunity_shared/firestore_admin.py (38-06 — `set_reference_self_check` · `get_analysis` :2469) · s3keys.py (`parse_reference_key`) · models.py (38-01 — `ANALYSIS_FIELD_SELF_CHECK_FOR_REFERENCE` · `SELF_CHECK_STATUS_*`)
    - .planning/phases/38-supplier-link/38-PATTERNS.md "### `backend/runpod_inference/server.py`" 절 + pipeline 절의 "selfScore 훅 자리" 발췌 · RESEARCH 코드 예시 E · "### C."
  </read_first>
  <action>
    RED 먼저(`test_self_score_hook.py` 는 `test_pipeline_dispatch.py` fixture 어법으로 `app` 재로드 + `firestore_admin` monkeypatch; `test_runpod_server.py` 는 `FakeMod` 에 `_register_reference` 추가) → 구현.
    `app.py`: `_record_self_score_if_needed(meta, result, uid, analysis_id) -> None` — `ref_id = (meta or {}).get(models.ANALYSIS_FIELD_SELF_CHECK_FOR_REFERENCE)`(리터럴 금지 — 38-01 상수, 38-07 writer 와 같은 출처); 없으면 return; `score = result.get("overallScore")`(화면 점수 = `deductionBreakdown.final` 과 같은 필드 — 주석에 "dimensionScores 아님, CLAUDE.md §7 ★ 소비처 추적" 명시); 수치 아니면 warning 후 return; `firestore_admin.set_reference_self_check(ref_id, status=models.SELF_CHECK_STATUS_DONE, analysis_id=analysis_id, score=float(score))`; 예외 `# noqa: BLE001` → `log.exception("selfScore 기록 실패 ref=%s analysis_id=%s")` (기록 실패가 분석을 막지 않는다 — :10137 어법). `_mark_self_check_failed_if_needed(uid, analysis_id)` — `firestore_admin.get_analysis` 로 meta, `models.ANALYSIS_FIELD_SELF_CHECK_FOR_REFERENCE` 키가 있으면 `set_reference_self_check(ref, status=SELF_CHECK_STATUS_FAILED, analysis_id=analysis_id)`; 모든 예외 삼킴. `_process` 의 `log.info("분석 완료 …")` 바로 다음 줄에 `_record_self_score_if_needed(meta, result, uid, analysis_id)` 한 줄(채점 코드 무접촉). `lambda_handler` except 3곳의 `fail_analysis(...)` 다음 줄에 `_mark_self_check_failed_if_needed(uid, analysis_id)`.
    `server.py`: import 에 `parse_reference_key` 추가; `class RegisterReferenceRequest(BaseModel): bucket: str; key: str`(description `reference/{uid}/{refId}.{ext}`), `class RegisterReferenceResponse(BaseModel): status: str; uid: str; refId: str`; `_register_in_background(bucket, key, uid, ref_id)`(위 behavior); `@app.post("/register-reference", status_code=202, response_model=RegisterReferenceResponse)` — `_verify_token` Depends, `parse_reference_key(req.key)` None → 400 `invalid reference key: {key}`, `log.info("/register-reference accepted bucket=%s uid=%s refId=%s")`, `background.add_task(_register_in_background, …)`. `_process_in_background` except 3곳에 `pipeline_app._mark_self_check_failed_if_needed(uid, analysis_id)` (pipeline 모듈은 이미 `_load_pipeline_module()` 로 잡혀 있다 — 없으면 try/except 로 감싼다). `/analyze` 본문 무접촉. `/health` 응답에 `"routes": ["analyze","register-reference"]` 를 **추가하지 않는다**(podwatch `probe()` 계약 유지) — 대신 SUMMARY 에 `curl -X POST …/register-reference` 무토큰 401/503 확인법을 적는다.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && .venv/bin/python -m pytest tests/test_self_score_hook.py tests/test_runpod_server.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && .venv/bin/python -m pytest tests/test_self_score_hook.py tests/test_runpod_server.py -q` exits 0 (hook file ≥ 6 tests; server file ≥ 3 new tests).
    - `grep -n "_record_self_score_if_needed(meta, result, uid, analysis_id)" backend/functions/pipeline/app.py` returns exactly one call line, and it is greater than the line of `log.info("분석 완료 uid=%s analysis_id=%s mode=%s"` by ≤ 3.
    - `grep -c "_mark_self_check_failed_if_needed(uid, analysis_id)" backend/functions/pipeline/app.py` == 3 and `grep -c "_mark_self_check_failed_if_needed" backend/runpod_inference/server.py` == 3.
    - `grep -c '@app.post("/register-reference"' backend/runpod_inference/server.py` == 1; `git diff backend/runpod_inference/server.py` shows the `/analyze` function body unchanged.
    - `grep -c '"selfCheckForReference"' backend/functions/pipeline/app.py backend/runpod_inference/server.py` → 0 for both (상수 `ANALYSIS_FIELD_SELF_CHECK_FOR_REFERENCE` 만; 테스트 파일의 리터럴은 허용).
  </acceptance_criteria>
  <done>selfScore 가 화면 점수와 같은 값으로 기준 doc 에 실리고, Pod 라우트가 등록 서비스를 부르며, 자기 분석 실패가 `selfCheckStatus:'failed'` 로 남는다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `requeue_reference_registrations.py` — `queued` 단일 쿼리 → Pod `/register-reference` 순차 POST(`--dry-run`) + 테스트 + Pod 기동 절차 6단계 문서화</name>
  <files>backend/scripts/requeue_reference_registrations.py, backend/tests/test_requeue_reference_registrations.py</files>
  <behavior>
    - `--dry-run` 이면 `list_reference_registrations_by_status("queued")` 결과를 표(ref_id · videoS3Key · supplierUid · registrationUpdatedAt)로 출력하고 POST 0회
    - 실제 실행: doc 마다 `POST {register_url} {"bucket": VIDEO_BUCKET, "key": doc["videoS3Key"]}` 헤더 `Content-Type/Accept/User-Agent(sunity-motion-pilot/1.0 (+requeue))/X-RunPod-Token` 4개, 순차, 200/202 외 응답은 그 건만 실패로 기록하고 다음 건 진행; 종료 코드 = 실패 건수 > 0 이면 1
    - `videoS3Key` 가 `parse_reference_key` 를 못 지나면 그 건은 스킵(경고) — legacy 키 방어
    - URL/토큰은 인자 `--register-url`/`--token` 또는 SSM(`/sunity/motion/runpod-analyze-url` → `/analyze`→`/register-reference` 치환, `/sunity/motion/runpod-auth-token`) — 테스트는 인자 경로만
    - `--status processing --older-than-min 60` 옵션: `processing` 에 60분 넘게 머문 건(Pod 죽음)도 재개 대상(옵션 없으면 queued 만)
  </behavior>
  <read_first>
    - backend/scripts/e2e_app_path.py :1-40 (헤더·사용법·`http_json`) · :56-61 (`firestore_client` — 리포 루트 SA json) · backend/scripts/intake_clips.py :30-40 (`--dry-run` 관례) · backend/scripts/pod_teardown.py :1-30 (머리말 경고 어법) · :120-128 (SSM 이름)
    - backend/functions/pipeline/app.py (38-07 — `_register_url` 변환 규칙 · `_delegate_to_runpod` 헤더 4개)
    - backend/shared/python/sunity_shared/firestore_admin.py (38-06 — `list_reference_registrations_by_status`) · s3keys.py (`parse_reference_key`)
    - backend/tests/test_intake_clips.py :1-40 (스크립트 모듈 import 어법 — conftest 가 `backend/scripts` 를 sys.path 에 넣는다)
    - /Users/kimtaesung/.claude/projects/-Users-kimtaesung-Dev-SunityMotion/memory/demo-only-pod-bring-up-procedure.md (기동 절차 1~6 — 여기에 "7. requeue" 를 붙일 문장)
    - .planning/phases/38-supplier-link/38-PATTERNS.md "### `backend/scripts/requeue_reference_registrations.py`" 절 · PATTERNS 관측 8(`e2e_app_path.py:110` 의 4값 종료 목록 복사 금지)
  </read_first>
  <action>
    RED 먼저(`test_requeue_reference_registrations.py`: 모듈 import 후 `firestore_admin.list_reference_registrations_by_status` 와 `urllib.request.urlopen` 을 monkeypatch, `main(["--register-url", "https://x/register-reference", "--token", "t", "--bucket", "b"])` 호출) → 구현.
    스크립트 헤더 docstring(e2e 어법): 왜(D-20 — Pod 부재 중 올린 등록은 `queued` 로 남고 SQS 재시도는 재개 수단이 아니다 RESEARCH Q13) · 사용법 3줄(`--dry-run` / 실제 / `--status processing --older-than-min 60`) · **Pod 기동 절차의 7단계** 라는 문장 + "메모리 demo-only-pod-bring-up-procedure 갱신 대상". 인자: `--register-url`·`--token`(없으면 SSM, `AWS_PROFILE=sunity-motion` 필요 주석)·`--bucket`(기본 `sunity-motion-pilot-videos`)·`--dry-run`·`--status`(기본 queued)·`--older-than-min`. `main(argv) -> int` 로 테스트 가능하게. Firestore 는 `firestore_admin.list_reference_registrations_by_status`(SA 는 `FIREBASE_SA_PATH` env 또는 리포 루트 json — `e2e_app_path.firestore_client` 어법을 `_ensure_firebase` 가 받아들이는 env 로) — 읽기 수 = 대기 건수(Spark 캡 주석). POST 는 `urllib` 표준 라이브러리만(위임 함수와 같은 헤더). 출력은 `ref_id=… key=… → 202` 한 줄씩. 실행 뒤 상태 갱신은 Pod 가 한다(스크립트는 doc 을 쓰지 않는다).
    SUMMARY 에 Pod 기동 절차 7단계 문장(메모리 갱신은 오케스트레이터/belle 몫이라 문장만 제안).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && .venv/bin/python -m pytest tests/test_requeue_reference_registrations.py -q && .venv/bin/python scripts/requeue_reference_registrations.py --help >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && .venv/bin/python -m pytest tests/test_requeue_reference_registrations.py -q` exits 0 (≥ 5 tests: dry-run 0 POST · 순차 POST 헤더 4개 · 비202 실패 카운트 · legacy 키 스킵 · exit code).
    - `backend/.venv/bin/python backend/scripts/requeue_reference_registrations.py --help` exits 0 and mentions `--dry-run`, `--register-url`, `--status`.
    - `grep -c '"completed"\|"error"' backend/scripts/requeue_reference_registrations.py` == 0 (4값 종료 목록 미복사).
    - `grep -c "where\|list_reference_registrations_by_status" backend/scripts/requeue_reference_registrations.py` ≥ 1 and `grep -c "stream()" backend/scripts/requeue_reference_registrations.py` == 0 (전수 스캔 없음 — 조회는 firestore_admin 경유).
  </acceptance_criteria>
  <done>Pod 기동 뒤 한 명령으로 대기 등록이 재개되고, 그 절차가 기동 절차 7단계로 문서화된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lambda/스크립트 → Pod HTTP | `X-RunPod-Token` 공유 비밀 |
| Pod → Firestore | Admin 쓰기 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-08-1 | Spoofing | `/register-reference` | mitigate | `/analyze` 와 같은 `_verify_token`(미설정 503, 불일치 401) |
| T-38-08-2 | Tampering | `/register-reference` 키 | mitigate | `parse_reference_key` 실패 400 — legacy·uploads 키로 등록 서비스를 못 부른다 |
| T-38-08-3 | Tampering | selfScore | mitigate | `selfCheckForReference` 는 서버(38-07)가 쓴 meta 만; 훅은 `overallScore` 수치 검증 |
| T-38-08-4 | Information disclosure | requeue 출력 | mitigate | ref_id·키·상태만 출력, 토큰은 인자/SSM 에서 읽고 출력 금지 |
| T-38-08-5 | DoS | requeue 폭주 | accept | 순차 POST, 대기 건수 = 공급자 1~2명의 등록 수 |
| T-38-SC | Tampering | pip installs | accept | 설치 0 |
</threat_model>

<verification>
- `cd backend && .venv/bin/python -m pytest tests/test_self_score_hook.py tests/test_runpod_server.py tests/test_requeue_reference_registrations.py tests/test_pipeline_dispatch.py tests/test_pipeline_reference_dispatch.py tests/test_register_reference_pipeline.py -q` → 0 failed.
- 전체 suite: `cd backend && .venv/bin/python -m pytest tests -q` → 0 failed(소요 시간을 SUMMARY 에 `[확인]` — RESEARCH A15 미확인 해소).
- 플랜 끝 push(38-09 배포가 이 코드를 올린다).
</verification>

<success_criteria>
- 등록 경로 코드가 Lambda·Pod·재개 스크립트까지 닫혀 배포 가능 상태다(전 테스트 green).
</success_criteria>

<output>
Create `.planning/phases/38-supplier-link/38-08-SUMMARY.md` when done (전체 pytest 소요 시간 기록).
</output>
