---
phase: 38-supplier-link
plan: 09
type: execute
wave: 5
depends_on: ["38-08"]
files_modified:
  - backend/template.yaml
  - backend/README.md
  - backend/scripts/intake_clips.py
  - .planning/phases/38-supplier-link/infra/notification-before.json
  - .planning/phases/38-supplier-link/infra/lifecycle-before.json
  - .planning/phases/38-supplier-link/38-09-CHANGESET.md
  - .planning/phases/38-supplier-link/infra/legacy-reference-baseline.json
  - .planning/phases/38-supplier-link/infra/lambda-before.json
  - .planning/phases/38-supplier-link/infra/lambda-after.json
  - .planning/phases/38-supplier-link/infra/firestore-rules-before.rules
  - backend/scripts/snapshot_reference_baseline.py
  - backend/tests/test_snapshot_reference_baseline.py
autonomous: false
requirements: [REQ-38-1, REQ-38-7, REQ-38-2]
must_haves:
  truths:
    - "REQ-38-1 / D-04: 라이브 HTTP API 에 `POST /reference/upload-url` 라우트가 있고 무토큰 호출이 401 JSON 을 돌려준다; pipeline Lambda 가 `POD_EXPECTED_PARAM` 과 `ssm:GetParameter(/sunity/motion/runpod-pod-expected)` 를 가진다"
    - "D-05: 버킷 알림이 `uploads/` 와 `reference/` **두** QueueConfiguration 을 같은 큐로 가진다(교체-전체 put 을 한 번에) — 기존 uploads/ 알림이 사라지지 않았음을 get 으로 확인"
    - "REQ-38-7 / D-17: 버킷 수명주기 규칙이 없다(`NoSuchLifecycleConfiguration`) — uploads/ 영구 보관; README·intake_clips 주석에 '30일' 서술이 없고, template 의 lifecycle 주석에 '30일 만료' 서술이 없다(로그 보존 30일 주석 :723·:736 은 무관 — 손대지 않는다)"
    - "D-03: SSM `/sunity/motion/supplier-uids` 가 존재하고(초기값 = belle uid + 코드 BELLE) Lambda 가 런타임에 읽는다 — 정은지 uid 추가는 배포 없이 SSM put 만"
    - "belle 이 changeset(layer 재바인딩 5함수 + 새 함수/라우트 + pipeline env/정책)을 본 뒤에만 `sam deploy` 가 실행된다(메모리 lambda-code-update — sam deploy 금지의 유일한 예외 경로)"
    - "리뷰 R10 / Success ④: 배포·SSM put·첫 런타임 쓰기 **전에** 11 legacy `ref-*` doc 의 raw top-level 소비 필드(`angles` sha256·`anglesJointKeys`·`anglesFrames`·`anglesUpdatedAt`·`clipRange`·`isActive`·`updatedAt`·버전 포인터) + S3 `reference/ref-*.mp4` ETag + 저장 분석 회귀 표본 3건이 `infra/legacy-reference-baseline.json` 에 있고(읽기 ≈ 14, raw doc — 버전 overlay 아님), 배포 뒤 재diff 가 0 이다"
    - "리뷰 R12: 배포 전 5함수 `get-function-configuration`(CodeSha256·Layers·env 이름·Memory·Timeout) + 코드 zip 이 보존되고(`infra/lambda-before.json` + `/Users/Shared/sunity-motion-rollback/38-09/`), 배포 뒤 기존 API 회귀(upload-url·reference·playback-url 401 + 토큰 200 형상)와 설정 diff(layer 버전·pipeline env/정책 외 변경 0)가 CHANGESET.md 에 있으며 함수 단위 롤백 명령이 적혀 있다"
    - "리뷰 R2·R13: `firestore.rules`(38-06) 는 belle 승인 뒤 `deploy_firestore_rules.py --test`(12/12) → `--release` 로 올리고, 라이브 클라이언트 probe(비공개 doc 본인 ALLOW/타인 DENY, 표식 create DENY)가 통과하며, 이전 규칙 파일이 `infra/firestore-rules-before.rules` 로 보존된다(롤백 = 그 파일로 `--release`)"
  artifacts:
    - path: "backend/template.yaml"
      provides: "ReferenceUploadUrlFunction + LogGroup + PipelineFunction env/policy 추가"
      contains: "ReferenceUploadUrlFunction"
    - path: ".planning/phases/38-supplier-link/38-09-CHANGESET.md"
      provides: "changeset 요약(리소스별 Add/Modify) + 검증 결과 + 롤백 명령"
      min_lines: 30
    - path: ".planning/phases/38-supplier-link/infra/notification-before.json"
      provides: "알림 설정 원본(롤백용)"
      contains: "QueueConfigurations"
    - path: ".planning/phases/38-supplier-link/infra/legacy-reference-baseline.json"
      provides: "R10 baseline — 11 legacy doc raw 필드 해시 + S3 ETag + 회귀 표본 3건(배포 전)"
      contains: "ref-"
    - path: ".planning/phases/38-supplier-link/infra/lambda-before.json"
      provides: "R12 — 5함수 배포 전 설정(CodeSha256·Layers·env 이름·Memory·Timeout) + 코드 zip sha256"
      contains: "CodeSha256"
    - path: "backend/scripts/snapshot_reference_baseline.py"
      provides: "--out / --diff — raw doc 스냅샷·정규화 해시·diff(0 이면 exit 0)"
      contains: "sha256"
  key_links:
    - from: "backend/template.yaml ReferenceUploadUrlFunction"
      to: "functions/reference-upload-url/app.py"
      via: "CodeUri + Handler app.lambda_handler + HttpApi POST /reference/upload-url"
      pattern: "/reference/upload-url"
    - from: "S3 bucket notification (reference/ prefix)"
      to: "SQS sunity-motion-pilot-analysis"
      via: "put-bucket-notification-configuration 2 entries"
      pattern: "reference/"
---

<objective>
인프라 쓰기 — CFN(새 함수·라우트·정책·env) 배포는 belle 이 changeset 을 본 뒤에만, 버킷 알림/수명주기는 원본 저장 + 검증 읽기 + 정확한 롤백 명령을 갖고 실행한다(REQ-38-1·REQ-38-7·D-03·D-05·D-17). SSM `supplier-uids` 는 배포 **전에** 만든다.

Purpose: `sam deploy` 는 메모리상 금지("라이브 스택 낙후")지만 새 라우트·함수·권한은 CFN 밖에서 못 만든다(RESEARCH §I·Q11). 알림·수명주기 put 은 교체-전체라 한 항목만 쓰면 기존 `uploads/` 알림이 사라진다(RESEARCH Q1 [CITED]).
Output: template.yaml 변경, changeset 기록, 버킷 설정 원본 2파일, README/주석 정정, 라이브 검증 결과, **R10 baseline · R12 배포 전 설정/코드 보존 · 기존 API 회귀 · 함수 단위 롤백 · 규칙 배포(R2·R13)**.

**리뷰 반영(2026-09-26):** R10(baseline 을 배포·첫 쓰기 **전**으로, raw doc + angles 해시 + ETag + 저장 분석 표본) · R12(5함수 설정/코드 보존 → 회귀 → 설정 diff → 함수 단위 롤백; layer 재바인딩 범위 평가 기록) · R2/R13(규칙 배포는 이 체크포인트 안, 이전 규칙 보존, REST `projects:test` + 라이브 probe). CloudFront 과금은 38-13 에서 관측값만.

**모든 AWS 명령은 `AWS_PROFILE=sunity-motion`**(기본 프로파일 `sunity-api` 는 CFN/Lambda/SSM 권한 없음 — RESEARCH Q1). 쓰기 권한 범위는 [미확인] — AccessDenied 가 나면 그 명령·오류 원문을 적고 멈춘다(auth gate). Docker 데몬이 없으면 `sam build --use-container` 를 못 한다 — 네이티브 빌드로 대체 **금지**(메모리 sam-build-native-deps), 멈추고 보고.

Phase 38 전제(38-01 머리말) 적용. Pod 은 이 플랜에서 켜지 않는다.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/38-supplier-link/38-CONTEXT.md
@.planning/phases/38-supplier-link/38-RESEARCH.md
@.planning/phases/38-supplier-link/38-PATTERNS.md
@.planning/phases/38-supplier-link/38-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: (step 0, 읽기 전용 — 어떤 쓰기보다 먼저) R10 legacy baseline + R12 5함수 설정/코드 보존 + 이전 규칙 보존 → template.yaml(새 함수·로그그룹·라우트 · pipeline env/정책 · 주석 D-17) + SSM `supplier-uids` 생성 + `sam validate` + `sam build --use-container` + changeset 생성(미실행) + 요약 문서</name>
  <files>backend/scripts/snapshot_reference_baseline.py, backend/tests/test_snapshot_reference_baseline.py, .planning/phases/38-supplier-link/infra/legacy-reference-baseline.json, .planning/phases/38-supplier-link/infra/lambda-before.json, .planning/phases/38-supplier-link/infra/firestore-rules-before.rules, backend/template.yaml, .planning/phases/38-supplier-link/38-09-CHANGESET.md</files>
  <read_first>
    - backend/template.yaml :20-22 · :101-112 (Globals) · :128-137 (버킷 주석 — D-17 정정 자리) · :184-210 (`UploadUrlFunction` 정본 — PutObject 접두사 · SSM 명시 Statement 주석 · HttpApi 이벤트) · :255-299 (`ReferenceAutoRegisterFunction` — `BELLE_UID` 동적 참조) · :327-394 (`PipelineFunction` env·정책 :386-388 `ssm:GetParameter` 1개) · :720-745 (LogGroup 패턴 30일)
    - backend/samconfig.toml (stack · region · `confirm_changeset = true` · parameter_overrides)
    - backend/functions/reference-upload-url/app.py (38-06 — env 이름 `SUPPLIER_UIDS_PARAM`, `BELLE_UID`, `VIDEO_BUCKET`) · backend/functions/pipeline/app.py (38-07 — `POD_EXPECTED_PARAM`)
    - .planning/phases/38-supplier-link/38-RESEARCH.md "### Q1" · "### Q11" · "### I. 배포" · "Runtime State Inventory" · PATTERNS "### `backend/template.yaml`" 절 + 관측 1·4
    - /Users/kimtaesung/.claude/projects/-Users-kimtaesung-Dev-SunityMotion/memory/lambda-code-update-without-sam-deploy.md · sam-build-native-deps.md · firestore-spark-50k-read-cap-is-a-live-risk.md(읽기 ≈ 14 건 예산)
    - backend/shared/python/sunity_shared/firestore_admin.py (38-06 `get_reference_registration` — 버전 overlay **없는** raw top-level 읽기; `get_reference_motion` :2558-2609 는 overlay 라 baseline 에 쓰지 않는다) · backend/scripts/e2e_app_path.py :50-66 (SA 로 Firestore client · custom token → ID 토큰 발급 어법 — 회귀 테스트 토큰) · docs/reference-motions.md §3 (legacy 필드명) · backend/scripts/intake_clips.py :30-40 (`--dry-run` 관례)
    - .planning/phases/38-supplier-link/38-REVIEWS.md "### R10" · "### R12" · "### R2"(규칙 배포 위치)
    - .planning/phases/38-supplier-link/38-PATTERNS.md "### `backend/scripts/requeue_reference_registrations.py` (script, batch)" 절(스크립트 헤더·`main(argv)` 어법) + Shared Patterns §4
  </read_first>
  <action>
    **0. baseline·보존(읽기 전용, 이 플랜의 어떤 쓰기(SSM put·deploy)보다 먼저 — 리뷰 R10·R12):**
    (a) `backend/scripts/snapshot_reference_baseline.py`(헤더: 왜 — Success ④ 무접촉 증명은 **첫 쓰기 전** raw 값의 해시라야 의미가 있다; 사용법 `--out <json>` / `--diff <before.json> --out <after.json>`; 읽기 예산 ≈ 14, Spark 캡) + `test_snapshot_reference_baseline.py`(순수 함수 `canonical_doc_hash(doc)` — 키 정렬 JSON sha256, `angles` 는 별도 sha256 + 길이; `diff_snapshots(before, after) -> list[str]` — 필드별 차이 문자열; fixture 로 동일/변경/누락 3케이스). 스냅샷 내용: ① 11 legacy doc(`docs/reference-motions.md` §3 의 id 11개 — `ref-*`) 를 `firestore_admin.get_reference_registration`(raw top-level, overlay 아님 — 파일 머리에 `"source": "raw top-level doc (no version overlay)"` 명시) 로 읽어 `angles` sha256+len · `anglesJointKeys` · `anglesFrames` · `anglesUpdatedAt` · `clipRange` · `isActive` · `updatedAt` · 버전 포인터 필드(`activeVersion`/`versionPointer` 등 있는 것 그대로) · 전체 doc sha256; ② `aws s3api list-objects-v2 --bucket sunity-motion-pilot-videos --prefix reference/ref- --query 'Contents[].[Key,ETag,Size]' --profile sunity-motion` 11 키; ③ 저장 분석 회귀 표본 3건 — `aws s3api list-objects-v2 --prefix results/ --max-keys 300` 에서 `results/{uid}/{analysisId}/` 를 파싱해 서로 다른 (uid, analysisId) 3쌍(첫 3쌍, 고정) → 각 doc 을 `firestore_admin.get_analysis(uid, analysisId)` 로 1회 읽어 `status·mode·referenceMotionId·updatedAt·result.overallScore·result.deductionBreakdown.final` + doc sha256(읽기 3). 산출 `infra/legacy-reference-baseline.json`(커밋). 재실행 시 `--diff` 가 차이 0 이면 exit 0.
    (b) `infra/lambda-before.json`: 5 함수(`sunity-motion-pilot-{upload-url,reference,reference-auto-register,pipeline,playback-url}` — 실제 이름은 `aws lambda list-functions --query 'Functions[?starts_with(FunctionName, `sunity-motion-pilot`)].FunctionName'` 로 확정) 각각 `aws lambda get-function-configuration --profile sunity-motion` 의 `CodeSha256·CodeSize·Layers[].Arn·Environment.Variables 의 키 이름만(값 금지)·MemorySize·Timeout·Runtime·Handler·LastModified·Version` + `aws lambda get-function --query Code.Location` 으로 zip 을 `/Users/Shared/sunity-motion-rollback/38-09/<fn>.zip` 에 내려받고(리포 밖·홈 밖; 5개 zip 의 sha256 을 json 에) — 롤백 재료(R12). 현재 layer ARN(버전 번호)도 별도 키 `sharedLayerBefore` 로.
    (c) `git show debb86bf:firestore.rules > .planning/phases/38-supplier-link/infra/firestore-rules-before.rules`(리뷰 기준 커밋의 규칙 = 라이브에 있던 규칙; 38-06 이 리포 파일을 이미 바꿨으므로 여기서 보존) + `backend/.venv/bin/python backend/scripts/deploy_firestore_rules.py --current --out .planning/phases/38-supplier-link/infra/firestore-rules-live.rules`(라이브 release 의 실제 내용 — before 파일과 `diff` 해 같은지 CHANGESET.md 에; 권한 거부면 `[미확인]`). 이 둘이 규칙 롤백 원본이다.
    이 세 파일을 `docs(38-09): baseline + lambda-before + rules-before (read-only, pre-deploy)` 로 커밋한 뒤에만 1 로 간다.
    1. `template.yaml`:
       - `ReferenceUploadUrlFunction`(`UploadUrlFunction` 블록 복제 + 머리 주석 `# ── POST /reference/upload-url : 공급자 링크 presigned PUT + reference/{refId} doc 선작성 — Phase 38 D-03/D-04 ──`): `FunctionName: !Sub sunity-motion-${Stage}-reference-upload-url`, `CodeUri: functions/reference-upload-url/`, `Handler: app.lambda_handler`, `Timeout: 10`, `Environment.Variables`: `BELLE_UID: "{{resolve:ssm:/sunity/motion/belle-uid}}"`(auto-register 와 동일 어법) · `SUPPLIER_UIDS_PARAM: /sunity/motion/supplier-uids`(**런타임 읽기** — 동적 참조를 쓰지 않는 이유 주석: 정은지 uid 추가가 배포 없이 SSM put 으로 끝나야 한다, 닭-달걀). `Policies`: `s3:PutObject` on `arn:aws:s3:::${VideoBucketName}/reference/*` · `ssm:GetParameter` on `parameter${FirebaseSaParam}` **와** `parameter/sunity/motion/supplier-uids` (leading-slash 이중 슬래시 버그 주석 유지). `Events.Post`: HttpApi `POST /reference/upload-url`. `ReferenceUploadUrlLogGroup`(`/aws/lambda/sunity-motion-${Stage}-reference-upload-url`, RetentionInDays 30).
       - `PipelineFunction`: env `POD_EXPECTED_PARAM: /sunity/motion/runpod-pod-expected`(주석: 쓰기 측 이름, PATTERNS 관측 1); 정책 Statement 에 `ssm:GetParameter` on `parameter/sunity/motion/runpod-pod-expected` 추가(기존 FirebaseSaParam Statement 유지).
       - :133-135 주석의 `1) aws s3api put-bucket-lifecycle-configuration ... (uploads/ 30일 만료)` 를 `1) (해제됨 2026-09-26, Phase 38 D-17 — uploads/ 영구 보관: 수명주기 규칙 없음)` 로.
    2. SSM(배포 전): `BELLE=$(aws ssm get-parameter --name /sunity/motion/belle-uid --query Parameter.Value --output text --profile sunity-motion)`; `aws ssm put-parameter --name /sunity/motion/supplier-uids --type String --value "${BELLE}:BELLE" --no-overwrite --profile sunity-motion`(이미 있으면 `ParameterAlreadyExists` → 그대로 두고 값 형식만 확인); `aws ssm get-parameter --name /sunity/motion/supplier-uids --profile sunity-motion` 로 존재 확인(값은 CHANGESET.md 에 **uid 마스킹**해 기록: 앞 4자 + `…`).
    3. `cd backend && sam validate --lint` → 0; `docker info` 로 데몬 확인 → `sam build --use-container`(실패 시 멈춤). `sam deploy --no-execute-changeset --profile sunity-motion`(samconfig 기본값) → changeset ARN 을 받아 `aws cloudformation describe-change-set --change-set-name <arn> --profile sunity-motion` 결과를 `38-09-CHANGESET.md` 에 표(LogicalResourceId · Action · Replacement · 변경 속성)로. 기대: Add `ReferenceUploadUrlFunction`·Role·`…Permission`·HttpApi Route/Integration·LogGroup; Modify `SharedLayer`(새 버전) → **5개 함수 Layers 재바인딩**(upload-url·reference·reference-auto-register·pipeline·playback-url — 라이브 :16/:20 혼재가 전부 새 버전으로); Modify `PipelineFunction`(env·정책). **예상 밖 항목**(예: playback-url Timeout/Memory 변경, `RUNPOD_ANALYZE_URL` 값 변화 — SSM 현재값(`pod-down.invalid` 자리표시자 가능)으로 해석됨)이 있으면 굵게 표시. **layer 재바인딩 범위 평가(R12):** SAM 은 `SharedLayer` 를 5함수가 `!Ref` 로 공유하므로 새 버전이 전부에 붙는다. 4개 함수를 이전 layer ARN 리터럴로 고정하는 것은 template 드리프트를 영구화하므로 **하지 않는다** — 대신 (i) `sam build` 산출 zip 의 sha256 과 `lambda-before.json` 의 `CodeSha256` 을 함수별로 비교해 **코드가 실제로 바뀌는 함수 목록**을 표에 적고 (ii) 배포 뒤 회귀(T3 A)와 함수 단위 롤백으로 반경을 닫는다. 이 판단을 CHANGESET.md "layer 범위" 절에 한 단락으로. 롤백 절(함수 단위, R12): 미실행 시 `aws cloudformation delete-change-set`; 실행 뒤 문제 시 함수마다 `aws lambda update-function-configuration --function-name <fn> --layers <lambda-before.json 의 Layers[0].Arn> --profile sunity-motion` + `aws lambda update-function-code --function-name <fn> --zip-file fileb:///Users/Shared/sunity-motion-rollback/38-09/<fn>.zip --profile sunity-motion`(pipeline 은 env `POD_EXPECTED_PARAM` 제거까지) — 명령을 함수 5개 분 그대로 적는다(메모리 lambda-code-update 절차). 규칙 롤백: `deploy_firestore_rules.py --release --rules infra/firestore-rules-before.rules`.
    커밋 `feat(38-09): template — reference-upload-url function + pipeline pod-expected policy (changeset not executed)`.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && sam validate --lint && grep -c "ReferenceUploadUrlFunction\|ReferenceUploadUrlLogGroup" template.yaml | grep -qx 2 && grep -q "POD_EXPECTED_PARAM" template.yaml && test -f ../.planning/phases/38-supplier-link/38-09-CHANGESET.md</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && sam validate --lint` exits 0.
    - `grep -c "ReferenceUploadUrlFunction:\|ReferenceUploadUrlLogGroup:" backend/template.yaml` == 2; `grep -c "/reference/upload-url" backend/template.yaml` == 1; `grep -c "parameter/sunity/motion/supplier-uids\|parameter/sunity/motion/runpod-pod-expected" backend/template.yaml` == 2.
    - `grep -c 'uploads/ 30일\|30일 만료' backend/template.yaml` == 0 (로그 보존 주석 :723·:736 의 '30일' 은 범위 밖, 건드리지 않는다).
    - `AWS_PROFILE=sunity-motion aws ssm get-parameter --name /sunity/motion/supplier-uids --query Parameter.Name --output text` prints `/sunity/motion/supplier-uids`.
    - `38-09-CHANGESET.md` contains a table with ≥ 6 resource rows, a "예상 밖" section (possibly "없음"), a "layer 범위" section (함수별 코드 변경 여부 표), and a "롤백" section with per-function `update-function-configuration`/`update-function-code` commands + the rules rollback command; `sam build --use-container` exit code recorded.
    - `cd backend && .venv/bin/python -m pytest tests/test_snapshot_reference_baseline.py -q` exits 0 (≥ 3 tests); `infra/legacy-reference-baseline.json` exists with `"source": "raw top-level doc (no version overlay)"`, 11 `ref-` doc entries each with `anglesSha256`, 11 S3 keys with `ETag`, and a `regressionSamples` array of length 3; the commit containing it precedes any SSM put/deploy commit in `git log` (R10).
    - `infra/lambda-before.json` lists 5 functions with `CodeSha256`, `Layers`, env **key names only** (`grep -c 'RUNPOD_AUTH_TOKEN\|BEGIN\|secret' ` value-wise == 0), and zip sha256; `/Users/Shared/sunity-motion-rollback/38-09/` holds 5 zips (path recorded).
    - `infra/firestore-rules-before.rules` equals `git show debb86bf:firestore.rules` (`diff` empty); live `--current` result recorded or `[미확인]`.
  </acceptance_criteria>
  <done>첫 쓰기 전 baseline·설정·코드·규칙이 보존됐고, template 이 새 함수·정책을 담고 changeset 이 생성만 된 채 belle 검토용 요약(layer 범위·함수 단위 롤백 포함)이 있다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: belle — changeset 검토(layer 재바인딩 5함수 + 새 함수/라우트 + pipeline env/정책) 후 배포 승인</name>
  <files>none — 체크포인트(사람 검토). 결과는 38-09-CHANGESET.md 승인 절</files>
  <action>
    실행자: `38-09-CHANGESET.md` 의 표·예상 밖 절·롤백 절이 채워져 있는지 확인하고 how-to-verify 3항목을 belle 에게 제시한 뒤 멈춘다. `approved` 면 Task 3 으로, `hold` 면 `aws cloudformation delete-change-set --change-set-name <arn> --profile sunity-motion` 으로 changeset 을 지우고 그 사유를 CHANGESET.md 에 적고 플랜을 중단한다. belle 의 말을 CHANGESET.md 승인 절에 그대로 인용한다.
  </action>
  <verify>
    <automated>grep -q 'approved\|hold' /Users/kimtaesung/Dev/SunityMotion/.planning/phases/38-supplier-link/38-09-CHANGESET.md</automated>
  </verify>
  <acceptance_criteria>
    - `38-09-CHANGESET.md` 승인 절에 belle 의 응답(`approved` 또는 `hold: …`)이 원문으로 있다.
    - `hold` 인 경우 `delete-change-set` 출력이 기록돼 있고 Task 3 은 실행되지 않았다.
  </acceptance_criteria>
  <done>belle 이 changeset 을 보고 승인(또는 보류)했고 그 결정이 기록됐다.</done>
  <what-built>`sam deploy --no-execute-changeset` 로 changeset 만 만들었다. 요약은 `.planning/phases/38-supplier-link/38-09-CHANGESET.md`. 메모리 규칙("sam deploy 금지 — 라이브 스택 낙후")의 예외를 belle 이 직접 허락하는 자리다(RESEARCH §I).</what-built>
  <how-to-verify>
    1. `38-09-CHANGESET.md` 표를 본다 — Add 항목이 `reference-upload-url` 함수·역할·라우트·로그그룹뿐인가, Modify 가 `SharedLayer` 새 버전 + 5개 함수 Layers 재바인딩 + `PipelineFunction` env/정책뿐인가.
    2. "예상 밖" 절이 비어 있는가. 비어 있지 않으면(예: playback-url 설정, `RUNPOD_ANALYZE_URL` 값) 그 항목을 받아들일지 결정.
    3. 재바인딩되는 layer 새 버전은 리포 HEAD 의 `sunity_shared` 전체다 — 라이브 :16/:20 과 리포 사이의 드리프트를 한 번에 얹는다(RESEARCH Q11: 07-21 이후 template 커밋 2건 + 09-25 pipeline 손 갱신). "layer 범위" 절의 함수별 코드 변경 표와 함수 단위 롤백 명령을 보고 받아들일지(리뷰 R12).
    4. **Firestore 규칙 배포도 이 승인에 포함된다(리뷰 R2·R13):** `firestore.rules` diff(`git diff debb86bf -- firestore.rules`) — `reference/**` 읽기를 top-level+versions 로 좁히고 `private/` 는 본인만, 분석 doc 의 `selfCheckForReference·selfCheckJobId` 클라이언트 쓰기 거부. 이전 규칙은 `infra/firestore-rules-before.rules`. 받아들일지(거부면 규칙만 `hold-rules:` 로 따로 보류 가능).
  </how-to-verify>
  <resume-signal>"approved" (CFN 실행 + 규칙 배포) · "approved, hold-rules: <이유>" (CFN 만) · "hold: <이유>" (changeset 삭제 후 중단)</resume-signal>
</task>

<task type="auto">
  <name>Task 3: changeset 실행 + 라이브 검증(라우트·env·layer·401 스모크) + **기존 API 회귀 + 설정 diff(R12)** + **baseline 재diff(R10)** → 버킷 알림 2항목 put/검증 → 수명주기 해제/검증 → README·intake_clips 주석 정정 → IAM 읽기 확인(두 접두사) → **규칙 `--test`/`--release` + 라이브 probe(R2·R13)** (전부 원본 저장 + 롤백 명령)</name>
  <files>backend/README.md, backend/scripts/intake_clips.py, .planning/phases/38-supplier-link/infra/notification-before.json, .planning/phases/38-supplier-link/infra/lifecycle-before.json, .planning/phases/38-supplier-link/infra/lambda-after.json, .planning/phases/38-supplier-link/38-09-CHANGESET.md</files>
  <read_first>
    - .planning/phases/38-supplier-link/38-09-CHANGESET.md (Task 1 산출 — changeset ARN · 기대 변경)
    - backend/README.md :70-112 ("외부 버킷 노티 설정" — 3개 `put-*` 명령 원문) · backend/scripts/intake_clips.py :25-28 (`uploads/` 30일 주석)
    - .planning/phases/38-supplier-link/38-RESEARCH.md "### Q1"(현재 알림 Id·QueueArn·lifecycle 규칙 ID `expire-raw-uploads-30d`) · 코드 예시 F · "### G."
    - .planning/phases/38-supplier-link/38-PATTERNS.md "### `docs/supplier-guide.md` (신규) · docs 4종 정정" 절의 "README 버킷 명령 (교체-전체 주의)" 발췌 + File Classification `backend/README.md` 행 + "### `backend/scripts/requeue_reference_registrations.py`" 절(`intake_clips.py` `--dry-run`·주석 관례) + Shared Patterns §12(주석 어법)
    - .planning/phases/38-supplier-link/38-07-PLAN.md threat_model 표의 T-38-07-7 (copy_object 의 IAM 전제 — `reference/*` v1 확정 + `uploads/*` 자기 재현성, 아래 E 가 두 접두사를 읽기 전용으로 잰다)
    - backend/scripts/deploy_firestore_rules.py (38-06 — `--test`·`--release`·exit 2 = 권한 거부) · backend/tests/firestore_rules_cases.json · backend/scripts/e2e_app_path.py :44-56 (custom token → ID 토큰 — 회귀·probe 용 토큰 발급, uid 는 `regress38-*` 일회용) · app/scripts/verify-firebase.mjs :1-40 (웹 SDK 로 Firestore 읽기/쓰기 probe 어법 — 익명 대신 발급 토큰 `signInWithCustomToken`)
    - .planning/phases/38-supplier-link/38-REVIEWS.md "### R10" · "### R12" · "### R13"
  </read_first>
  <action>
    (belle "approved" 뒤에만) **A. 배포**: `aws cloudformation execute-change-set --change-set-name <arn> --profile sunity-motion` → `aws cloudformation wait stack-update-complete --stack-name sunity-motion-pilot --profile sunity-motion`. 검증 4가지를 CHANGESET.md "실행 결과" 절에 원문으로: ① `aws apigatewayv2 get-routes --api-id <ApiId from get-apis> --profile sunity-motion` 에 `POST /reference/upload-url` 존재 ② `aws lambda get-function-configuration --function-name sunity-motion-pilot-pipeline --profile sunity-motion --query 'Environment.Variables.[POD_EXPECTED_PARAM,RUNPOD_ANALYZE_URL]'` 에 `/sunity/motion/runpod-pod-expected` + 기존 URL(값 자체는 기록하되 토큰은 절대 출력 금지) ③ 5개 함수의 `Layers[0].Arn` 버전이 모두 같은 새 번호 ④ 스모크 `curl -s -o /dev/null -w '%{http_code}' -X POST https://2rbpecm4d8.execute-api.ap-northeast-2.amazonaws.com/pilot/reference/upload-url -H 'Content-Type: application/json' -d '{}'` → `401`, 본문 `{"error":{"code":"unauthorized"…}}`. 새 함수 `LastUpdateStatus == Successful` 폴링.
    **A-2. 설정 diff + 기존 API 회귀(R12):** 5함수 `get-function-configuration` 을 다시 `infra/lambda-after.json` 에(같은 필드) → before/after diff 를 CHANGESET.md 표로: 허용된 차이 = 모든 함수의 `Layers[0].Arn` 버전 + `CodeSha256`("layer 범위" 표에서 코드 변경으로 예고된 함수만) + pipeline 의 env `POD_EXPECTED_PARAM`·정책; **그 밖의 차이(Memory·Timeout·Runtime·Handler·다른 env 키)는 굵게 + belle 에게 보고**(T2 에서 예고되지 않았으면 롤백 후보). 회귀 — 무토큰: `POST /upload-url` · `GET /reference` · `POST /playback-url` 각각 `401` + `error.code == "unauthorized"`; 토큰(`e2e_app_path` 어법으로 SA custom token → ID 토큰, uid `regress38`, 발급 명령·uid 만 기록·토큰 출력 금지): `GET /reference` → 200 + 배열 길이 ≥ 11(legacy 11 이 그대로 보인다) · `POST /upload-url {mode:'mode3', format:'mp4', fileSizeBytes:1000}` → 200 + 키 `analysisId·uploadUrl`(PUT 은 하지 않는다 — 객체 0) · `POST /playback-url {referenceMotionId:'ref-kip-up'}` → 200 + `url`. 셋 다 응답 형상(키 이름)만 CHANGESET.md 에. 어느 하나라도 깨지면 **여기서 멈추고** 함수 단위 롤백 명령(T1)을 실행할지 belle 에게(가짜 통과 금지).
    **A-3. baseline 재diff(R10):** `backend/.venv/bin/python backend/scripts/snapshot_reference_baseline.py --diff .planning/phases/38-supplier-link/infra/legacy-reference-baseline.json --out /private/tmp/…/scratchpad/baseline-after-deploy.json` → 차이 0(exit 0) 을 CHANGESET.md 에 원문으로; 차이가 있으면 필드를 굵게(배포가 legacy 를 건드렸다 = 이 phase 의 결함, 멈추고 보고).
    **B. 알림(교체-전체)**: `aws s3api get-bucket-notification-configuration --bucket sunity-motion-pilot-videos --profile sunity-motion > .planning/phases/38-supplier-link/infra/notification-before.json`(원본 커밋). 새 설정 = 원본의 `uploads/` 항목(Id·QueueArn·Events·Filter 그대로) + 두 번째 항목 `{"Id":"reference-supplier-link","QueueArn":"arn:aws:sqs:ap-northeast-2:976369350031:sunity-motion-pilot-analysis","Events":["s3:ObjectCreated:*"],"Filter":{"Key":{"FilterRules":[{"Name":"prefix","Value":"reference/"}]}}}` 를 파일 `infra/notification-after.json` 로 만들어 **한 번의** `put-bucket-notification-configuration --notification-configuration file://…`. 검증 `get-…` 결과에 QueueConfigurations 길이 2, prefix 값 `uploads/`·`reference/`. 롤백 명령(CHANGESET.md 에 그대로): `aws s3api put-bucket-notification-configuration --bucket sunity-motion-pilot-videos --notification-configuration file://.planning/phases/38-supplier-link/infra/notification-before.json --profile sunity-motion`.
    **C. 수명주기(REQ-38-7)**: `get-bucket-lifecycle-configuration … > infra/lifecycle-before.json`(규칙 1개 `expire-raw-uploads-30d` 확인) → `aws s3api delete-bucket-lifecycle --bucket sunity-motion-pilot-videos --profile sunity-motion` → `get-bucket-lifecycle-configuration` 이 `NoSuchLifecycleConfiguration` 오류를 내는지 원문 기록. 롤백: `put-bucket-lifecycle-configuration --lifecycle-configuration file://infra/lifecycle-before.json`.
    **D. 문서**: README :78-82 의 lifecycle 블록을 `# 1) Lifecycle — 해제됨 (2026-09-26, Phase 38 D-17: uploads/ 영구 보관). 되돌리려면 .planning/phases/38-supplier-link/infra/lifecycle-before.json` 로 바꾸고 :101-109 의 알림 명령을 2항목 판(RESEARCH 코드 예시 F)으로 교체 + "교체-전체 — 항목 하나만 넣으면 다른 접두사 알림이 사라진다" 경고; `intake_clips.py:27` 주석 `앱 경로의 uploads/ 는 30일 뒤 지워진다(수명 규칙)` → `앱 경로의 uploads/ 도 영구 보관이다(2026-09-26 D-17) — fixtures/intake/ 는 분석 fixture 용 별도 사본`. 커밋 `docs(38-09): bucket notification 2 prefixes + lifecycle removed (D-17)`.
    **E. IAM 읽기 전용 확인(38-07 T-38-07-7 — Pod 의 `copy_object` 가 `uploads/` 에 쓸 수 있는지, 부작용 0):** `ARN=$(aws sts get-caller-identity --query Arn --output text --profile sunity-motion)` → `aws iam simulate-principal-policy --policy-source-arn "$ARN" --action-names s3:PutObject s3:GetObject --resource-arns arn:aws:s3:::sunity-motion-pilot-videos/uploads/u1/0123456789abcdef0123456789abcdef.mp4 arn:aws:s3:::sunity-motion-pilot-videos/reference/u1/0123456789abcdef0123456789abcdef/v1.mp4 arn:aws:s3:::sunity-motion-pilot-videos/reference/u1/0123456789abcdef0123456789abcdef/upload.mp4 --profile sunity-motion --query 'EvaluationResults[].[EvalActionName,EvalResourceName,EvalDecision]' --output table` → CHANGESET.md "IAM" 절에 표 원문 + ARN(계정 id 는 그대로, 키 없음) + `[확인]`. PutObject 가 `uploads/*` **와** `reference/*/v1` 둘 다 `allowed` 여야 Pod 의 두 copy_object(v1 확정 · 자기 재현성)가 산다(R5) — 단 Pod 의 키가 이 ARN 과 같을 때만 승계되므로 38-14 T2 3-b 가 Pod 에서 `get-caller-identity` 로 대조한다. `AccessDenied`(`iam:SimulatePrincipalPolicy` 없음) 면 `[미확인 — 38-14 T2 3-b probe 로]` 라고 적고 **멈추지 않는다**(읽기 전용이라 auth gate 대상이 아니다).
    **F. Firestore 규칙 배포(R2·R13 — belle 응답이 `hold-rules` 가 아닐 때만):** `backend/.venv/bin/python backend/scripts/deploy_firestore_rules.py --test`(`FIREBASE_SA_PATH` = 리포 루트 SA json) → 12/12 원문을 CHANGESET.md "규칙" 절에; 하나라도 실패면 배포하지 않고 멈춘다. 통과 시 `--release` → 새 ruleset 이름 기록 → **라이브 probe(웹 SDK, 발급 토큰)**: (i) Admin(firestore_admin) 으로 임시 doc `reference/_rulesprobe38` `{name:'probe',isActive:false}` + `reference/_rulesprobe38/private/registration {supplierUid:'regress38A'}` 를 만들고 (ii) `node` 스크립트(`app/node_modules/firebase`, `signInWithCustomToken` — 토큰은 `e2e_app_path` 어법으로 uid `regress38A`·`regress38B` 두 개) 로: A 가 private get → 성공, B 가 private get → `permission-denied`, B 가 public get → 성공, B 가 `users/regress38B/analyses/probe38` 에 `{analysisId,mode:'mode1',status:'uploading',selfCheckForReference:'x'}` set → `permission-denied`, 같은 doc 표식 없이 set → 성공 → 삭제 (iii) Admin 으로 임시 두 doc 삭제 + `users/regress38B/analyses/probe38` 삭제(읽기 3·쓰기 4 — 전부 임시, 캡 무관). 결과 5줄을 `[확인]` 으로. `--release` 가 exit 2(권한 거부) 면: CHANGESET.md 에 원문 + **belle 콘솔 폴백** — "Firebase 콘솔 → Firestore → 규칙 탭에 `firestore.rules` 내용을 붙여 넣고 게시" 를 이 체크포인트 안에서 belle 에게 요청하고 `done` 뒤 같은 probe 를 돌린다. 규칙 롤백 명령(`--release --rules infra/firestore-rules-before.rules` 또는 콘솔 붙여넣기)을 "롤백" 절에.
    AccessDenied 가 어느 단계에서든 나면 그 단계에서 멈추고 오류 원문·다음에 필요한 권한을 CHANGESET.md 에 적는다(가짜 성공 금지).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && export AWS_PROFILE=sunity-motion && test "$(aws s3api get-bucket-notification-configuration --bucket sunity-motion-pilot-videos --query 'length(QueueConfigurations)' --output text)" = "2" && aws s3api get-bucket-lifecycle-configuration --bucket sunity-motion-pilot-videos 2>&1 | grep -q NoSuchLifecycleConfiguration && test "$(curl -s -o /dev/null -w '%{http_code}' -X POST https://2rbpecm4d8.execute-api.ap-northeast-2.amazonaws.com/pilot/reference/upload-url -H 'Content-Type: application/json' -d '{}')" = "401" && test "$(grep -c '30일' backend/README.md backend/scripts/intake_clips.py | awk -F: '{s+=$2} END {print s}')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `aws s3api get-bucket-notification-configuration … --query 'QueueConfigurations[].Filter.Key.FilterRules[0].Value'` returns both `uploads/` and `reference/`, same QueueArn.
    - `aws s3api get-bucket-lifecycle-configuration …` stderr contains `NoSuchLifecycleConfiguration`.
    - `aws apigatewayv2 get-routes …` output contains `POST /reference/upload-url`; unauthenticated POST returns HTTP 401 with JSON `error.code == "unauthorized"`.
    - Pipeline function env contains `POD_EXPECTED_PARAM=/sunity/motion/runpod-pod-expected`; all 5 functions share the same new layer version (recorded in CHANGESET.md).
    - `infra/notification-before.json` and `infra/lifecycle-before.json` are committed; CHANGESET.md has a "롤백" section with both `put-*` restore commands and an "실행 결과" section with the 4 verification outputs.
    - `grep -c '30일' backend/README.md backend/scripts/intake_clips.py` → all 0; `grep -c 'uploads/ 30일\|30일 만료' backend/template.yaml` → 0 (로그 보존 주석 :723·:736 은 그대로).
    - CHANGESET.md has an "IAM" section with either the `simulate-principal-policy` table (`allowed`/`implicitDeny` per action × resource for `uploads/*`, `reference/*/v1`, `reference/*/upload`, caller ARN) or an explicit `[미확인 — 38-14 T2 3-b]`; no secret values.
    - `infra/lambda-after.json` exists; CHANGESET.md "설정 diff" table lists per-function differences and any unexpected difference is bolded with belle's decision; "회귀" section has 3 × 401 lines and (if a token was minted) 3 × 200 shape lines with key names only (R12).
    - CHANGESET.md "baseline" section pastes the `snapshot_reference_baseline.py --diff` output with 0 differences (R10) — or bold differences + stop.
    - CHANGESET.md "규칙" section has `--test` 12/12 output, the released ruleset name (or the belle console fallback record), the 5 probe lines (`permission-denied` for B private / B forged marker), and the temporary docs deletion confirmation; `grep -c 'permission-denied' 38-09-CHANGESET.md` ≥ 2 (R2·R13).
  </acceptance_criteria>
  <done>새 라우트가 살아 있고 기존 API 가 회귀 없이 돌며, legacy baseline diff 가 0 이고, reference/ 객체가 SQS 에 도착하며, uploads/ 가 영구 보관되고, 규칙이 배포·probe 됐으며, 전부 되돌릴 수 있는 원본이 커밋돼 있다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로컬 CLI(sunity-motion) → AWS 계정 | 스택·버킷·SSM 쓰기 |
| 인터넷 → HTTP API 새 라우트 | 무인증 호출 가능(401 로 막는다) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-09-1 | Tampering | CFN 배포 | mitigate | changeset 생성→belle 검토→실행; 배포 전 5함수 설정/코드 보존 + 배포 뒤 설정 diff + 기존 API 회귀 + 함수 단위 롤백 명령(R12) |
| T-38-09-6 | Tampering | legacy 11 doc·저장 분석 | mitigate | 첫 쓰기 전 raw baseline(angles 해시·ETag·회귀 표본 3) → 배포 뒤 재diff 0(R10); 차이 = 멈춤 |
| T-38-09-7 | Elevation/Information disclosure | Firestore 규칙 배포 | mitigate | belle 승인 안에서만 `--release`; `projects:test` 12/12 선행; 라이브 probe(타인 private DENY·위조 표식 DENY); 이전 규칙 보존 + 롤백 명령(R2·R13) |
| T-38-09-2 | DoS/Tampering | 버킷 알림 교체-전체 | mitigate | 원본 JSON 저장·2항목 한 번에 put·get 검증·롤백 명령 |
| T-38-09-3 | Information disclosure | 수명주기 해제 | accept | belle 결정 D-17(영구 보관, 비용 $0.7/월 급); 동의·철회 절차는 supplier-guide |
| T-38-09-4 | Elevation | 새 함수 IAM | mitigate | PutObject `reference/*` 와 SSM 파라미터 2개만(최소 권한) |
| T-38-09-5 | Information disclosure | CHANGESET.md | mitigate | uid 마스킹, 토큰·SSM 비밀 값 기록 금지 |
| T-38-SC | Tampering | pip installs (sam build 컨테이너) | accept | 기존 requirements.txt 만(신규 패키지 0), `--use-container` 로 Linux 바이너리 |
</threat_model>

<verification>
- Task 3 자동 검증 4가지 + CHANGESET.md 실행 결과 절 + 설정 diff/회귀/baseline 재diff/규칙 probe 절.
- `cd backend && .venv/bin/python -m pytest tests/test_snapshot_reference_baseline.py -q` → 0 failed.
- `git log --oneline origin/main..HEAD` 비어 있음.
</verification>

<success_criteria>
- 라이브에 `POST /reference/upload-url` 이 있고 reference/ 알림이 큐로 가며 uploads/ 수명주기가 없다 — 전부 읽기 명령 원문으로 증명, 롤백 원본 커밋.
</success_criteria>

<output>
Create `.planning/phases/38-supplier-link/38-09-SUMMARY.md` when done (AccessDenied 가 있었으면 어느 명령인지 `[확인]` 으로).
</output>
