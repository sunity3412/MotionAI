---
phase: 38-supplier-link
plan: 10
type: execute
wave: 4
depends_on: ["38-01", "38-03", "38-04"]
files_modified:
  - app/src/lib/api.ts
  - app/src/lib/supplierMotions.ts
  - app/src/lib/videoMeta.ts
  - app/src/lib/socialAuth.web.ts
  - app/src/components/SupplierUi.tsx
  - app/src/app/supplier/index.tsx
autonomous: true
# 38-04 Task 3 (b): belle 이 option-2 를 고르면 여기에 `skipped: true` 가 추가되고 38-10-SUMMARY.md 가 skipped-by-decision 으로 쓰인다
requirements: [REQ-38-3, REQ-38-4, REQ-38-5]
must_haves:
  truths:
    - "D-01/D-02: 폰 브라우저에서 `/supplier` 를 열면 같은 앱(Expo Router 라우트)의 공급자 첫 화면이 뜨고, 로그인 뒤 **내 동작(+올리기 CTA)** 와 **내 코드** 두 카드가 보인다"
    - "D-03: 로그인은 Google 팝업 1개(`socialAuth.web.ts`), 화이트리스트 밖이면 내 ID(uid) 를 보여주고 복사·재확인할 수 있다(닭-달걀 절차)"
    - "D-09/D-10 (리뷰 R4·R11·R13·R14): 행 부제가 `registrationStatus`/`selfScore` 에 따라 UI-SPEC 상태어로 바뀌고(`expired` 포함), 실패 행 → 실패 상세(제목·본문·TIP·다시 올리기 — 실패 상세는 **비공개 doc** `reference/{refId}/private/registration` 에서 읽는다), 만료 행 → 만료 패널(`row.expired.*` + 다시 올리기), 활성 행 → 등록됨 상세(자기 영상 재분석 한 줄 + 고지 `row.self.note`)가 보인다 — 규칙은 전부 38-03 `supplierRules` import(화면에 규칙 재작성 0)"
    - "D-12/D-13: 내 코드 카드는 코드 표시 + `코드 복사` 뿐 — 입력·귀속·크레딧 없음"
    - "D-20: Pod 가 꺼져 있어 `queued` 인 행이 있으면 안내 알약(`home.podDown`)이 이유를 준다"
    - "D-22: 모든 표면은 Figma 노드(1:960 · 1:977 · 1:419~424 · 1:717 · 1:743 · 1:646/663/713/709 · 1:428/479/482/483/486/458 · 1:498)를 `get_design_context` 로 실측한 값으로 만들고 Figma 문자열은 그대로 쓴다"
    - "Spark 5만/일: 페이지 구독은 `where('supplierUid','==',uid)` 하나뿐(전수 스캔 없음)"
  artifacts:
    - path: "app/src/app/supplier/index.tsx"
      provides: "A-1 로그인 · A-2 권한 없음 · A-3 홈(카드 2) · A-3b 실패 상세 · A-3c 완료 상세"
      min_lines: 200
    - path: "app/src/lib/supplierMotions.ts"
      provides: "useSupplierMotions(uid) — where supplierUid 단일 onSnapshot"
      contains: "supplierUid"
    - path: "app/src/lib/supplierMotions.ts"
      provides: "useSupplierRegistrationPrivate(refId) — 상세에서만 비공개 doc 1건 onSnapshot(R13)"
      contains: "private"
    - path: "app/src/lib/api.ts"
      provides: "probeSupplier · requestReferenceUploadUrl · uploadToS3WithProgress"
      contains: "/reference/upload-url"
  key_links:
    - from: "app/src/app/supplier/index.tsx"
      to: "app/src/constants/supplierCopy.ts"
      via: "모든 문구는 supplierCopy 에서(화면 리터럴 0)"
      pattern: "supplierCopy\\."
    - from: "app/src/lib/supplierMotions.ts"
      to: "firebase/firestore onSnapshot"
      via: "query(collection(db,'reference'), where('supplierUid','==',uid))"
      pattern: "onSnapshot"
    - from: "app/src/app/supplier/index.tsx"
      to: "app/src/lib/api.ts probeSupplier"
      via: "로그인 직후 화이트리스트 확인(403 → A-2)"
      pattern: "probeSupplier"
---

<objective>
**후보 (1) 전용 — 38-04 결정이 `option-1` 일 때만 실행한다(option-2 면 이 플랜과 38-11 을 건너뛰고 38-12 를 실행).** 공급자 페이지의 핵심(로그인 · 권한 없음 · 홈 두 카드 · 실패/완료 상세)을 Expo Router 라우트 `app/src/app/supplier/index.tsx` 로 만든다(D-01·D-02·D-03·D-09·D-10·D-12·D-20·D-22). 올리기 폼·가이드 화면은 38-11.

Purpose: 링크 = 같은 앱의 공급자 모드 첫 화면(belle Q1 ○). 페이지가 자기 doc 만 구독해 등록 상태·실패 문구·재현성을 보여주는 것이 "사람 손 없이" 의 사용자 쪽 절반이다.
Output: 데이터 층 3파일(api·motions·videoMeta — 순수 규칙 `supplierRules`/`supplierForm` 은 38-03 이 이미 만들었고 여기서는 import 만, 리뷰 R14) + 공용 UI 1파일 + 라우트 1파일. typecheck + 웹 export 스모크 + 시뮬 Safari 스크린샷.

**리뷰 반영(2026-09-26):** R4(`expired` 행·패널) · R11(재현성 문구 = 재분석 일관성 + 고지) · R13(실패 상세·선언 표는 비공개 doc 훅으로; 위협표 "개인정보 없음" 정정) · R14(규칙 import 만).

Phase 38 전제(38-01 머리말) 적용. UI 규칙: 테마 토큰만(`app/CLAUDE.md`), 문구는 `supplierCopy` 만, Figma 원격 이미지 URL 금지, 이모지 금지, 라이트 전용. **모든 UI task 는 시작 전에 `figma:figma-design-to-code` 스킬을 로드하고 해당 노드를 `get_design_context` 로 실측한다(D-22).**
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/38-supplier-link/38-CONTEXT.md
@.planning/phases/38-supplier-link/38-UI-SPEC.md
@.planning/phases/38-supplier-link/38-PATTERNS.md
@.planning/phases/38-supplier-link/38-04-MEASUREMENT.md
@.planning/phases/38-supplier-link/38-03-SUMMARY.md
@app/CLAUDE.md
@design.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 데이터 층 — `api.ts`(probe · reference presign · XHR 진행률 PUT) · `supplierMotions.ts`(where supplierUid 단일 구독 + 상세용 비공개 doc 훅, R13) · `videoMeta.ts`(웹 duration) — 규칙은 38-03 `supplierRules` import 만(R14)</name>
  <files>app/src/lib/api.ts, app/src/lib/supplierMotions.ts, app/src/lib/videoMeta.ts</files>
  <behavior>
    - `useSupplierMotions(uid)` 는 `normalizeRegistration`(38-03) 로 공개 doc 만 정규화한다 — `registrationError`·선언은 여기 없다(R13)
    - `useSupplierRegistrationPrivate(refId: string | null)` → `{ priv: SupplierMotionPrivate | null; loading; error }` — refId null 이면 비구독; `onSnapshot(doc(db,'reference',refId,'private','registration'))` 1건; `permission-denied`(남의 것) → `priv:null` + error 코드(화면은 `common.offline` 이 아니라 `row.fail.server_error` 로 강등하지 않고 목록으로 돌아간다); 정규화는 `normalizePrefix`… 아니라 38-03 `normalizePrivate`
    - `readVideoDurationSec(fileOrUrl)`: `document` 가 없으면 `null`; 있으면 `<video preload="metadata">` `loadedmetadata` 의 `duration`(유한·>0 아니면 null, 8초 타임아웃 null) — node 테스트는 `document` 부재 경로만
  </behavior>
  <read_first>
    - app/src/lib/api.ts :1-50 (ApiError · parseErrorCode) · :51-95 (`authedJson` · `requestUploadUrl`) · :318-343 (`uploadToS3` — Content-Type 필수 이유)
    - app/src/lib/referenceMotions.ts :1-35 · :60-80 (`normalize` · `LEVEL_ORDER`) · :208-242 (`useReferenceMotions` onSnapshot 어법) · :313-359 (`useReferenceMotionDoc`)
    - app/src/types/analysis.ts (38-01 — `ReferenceUploadUrlRequest/Response` · `SupplierProbeResponse` · `ReferenceRegistrationStatus` · `ReferenceRegistrationError` · `SkillLevel`)
    - app/src/constants/supplierCopy.ts (38-03 — `row.*` · `home.*`) · **app/src/lib/supplierRules.ts (38-03 — `normalizeRegistration` · `normalizePrivate` · `SupplierMotion`/`SupplierMotionPrivate` · `SELF_SCORE_OK_MIN`(38-04 결정값 반영됨) — import 만, 재정의 금지)**
    - .planning/phases/38-supplier-link/38-UI-SPEC.md "### A-3 메인" 상태 표(`registrationStatus` → 부제 · chevron, `expired` 행 포함) · "Open for belle 1"
    - .planning/phases/38-supplier-link/38-REVIEWS.md "### R13" · "### R14"
    - .planning/phases/38-supplier-link/38-PATTERNS.md "### 공급자 페이지 — 후보 (1)" 절(`authedJson`·`uploadToS3`·`useReferenceMotions`·`normalize` 발췌 + 플래너 메모) + 관측 7
  </read_first>
  <action>
    규칙 파일은 만들지 않는다 — `import { normalizeRegistration, normalizePrivate, type SupplierMotion, type SupplierMotionPrivate } from './supplierRules'`(38-03; 화면·훅 어디에도 `SELF_SCORE_OK_MIN`·상태어·실패 문구 재정의 0, R14).
    `supplierMotions.ts`: `useSupplierMotions(uid: string | null): { motions: SupplierMotion[]; loading; error; retry }` — uid null 이면 빈 상태; `query(collection(db,'reference'), where('supplierUid','==',uid))` + `onSnapshot`(에러 → `supplierCopy.home.listError`); 주석 "Spark 5만/일 캡 — 전수 스캔 금지, `useReferenceMotions().normalize` 는 등록 필드를 strip 하므로 재사용 불가(PATTERNS 관측 7)". `retry()` 는 재구독. `useSupplierRegistrationPrivate(refId)`(R13): 상세가 열릴 때만 `doc(db, 'reference', refId, 'private', 'registration')` 1건 `onSnapshot`, `normalizePrivate`; 주석 "규칙상 본인만 읽힌다(38-06 T3) — 목록에서는 구독하지 않는다(행 N 개 × 읽기 방지)".
    `api.ts` 추가: `probeSupplier(): Promise<SupplierProbeResponse>` = `authedJson('/reference/upload-url', {method:'POST', body:{probe:true}})`; `requestReferenceUploadUrl(req: ReferenceUploadUrlRequest): Promise<ReferenceUploadUrlResponse>`; `uploadToS3WithProgress(uploadUrl, body: Blob, format: VideoFormat, opts: { onProgress?: (pct:number)=>void; signal?: AbortSignal }): Promise<void>` — `XMLHttpRequest` PUT, `Content-Type` = `CONTENT_TYPE_BY_FORMAT[format]`(기존 상수 재사용), `upload.onprogress` → `Math.round(loaded/total*100)`, `signal.abort` → `xhr.abort()` + reject `Error('aborted')`, 2xx 외 reject `Error(\`s3 PUT ${status}\`)`. 기존 `uploadToS3` 무접촉.
    `videoMeta.ts`: `readVideoDurationSec(src: Blob | string): Promise<number | null>` — 위 behavior; Blob 이면 `URL.createObjectURL` 후 `revokeObjectURL`.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck && grep -q "from './supplierRules'" src/lib/supplierMotions.ts && grep -q "'private', 'registration'" src/lib/supplierMotions.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && npm run typecheck` exits 0.
    - `grep -c "supplierUid" app/src/lib/supplierMotions.ts` ≥ 1 with a `where(` call on it; `grep -c "export function useSupplierRegistrationPrivate" app/src/lib/supplierMotions.ts` == 1 and `grep -c "'private', 'registration'" app/src/lib/supplierMotions.ts` == 1 (R13).
    - `grep -c "from './supplierRules'" app/src/lib/supplierMotions.ts` == 1; `grep -rc "SELF_SCORE_OK_MIN = \|function rowSubtitle\|function failCopy" app/src/lib/supplierMotions.ts app/src/lib/api.ts app/src/app/supplier/` → 0 (규칙 재정의 없음, R14).
    - `grep -c "probeSupplier\|requestReferenceUploadUrl\|uploadToS3WithProgress" app/src/lib/api.ts` == 3 and `git diff app/src/lib/api.ts` leaves `uploadToS3` unchanged.
    - `grep -rc "< 90\|<90\|>= 90\|>=90" app/src/app/supplier/ app/src/lib/supplierMotions.ts` → 0 everywhere (named constant only, from supplierRules).
  </acceptance_criteria>
  <done>페이지가 쓸 데이터 층(공개 구독 + 상세용 비공개 훅 + API)이 typecheck 로 닫혔고 규칙은 38-03 import 만이다.</done>
</task>

<task type="auto">
  <name>Task 2: Figma 실측(D-22) → `SupplierUi.tsx` 공용 프리미티브 → `supplier/index.tsx` A-1 로그인 · A-2 권한 없음 · A-3 홈(내 동작 + 내 코드, podDown 알약, 빈 상태 STEP 카드, 토스트, 다른 계정 로그인)</name>
  <files>app/src/components/SupplierUi.tsx, app/src/app/supplier/index.tsx, app/src/lib/socialAuth.web.ts</files>
  <read_first>
    - .planning/phases/38-supplier-link/38-UI-SPEC.md "## Figma 참조 (필수)" 표(A-1·A-2·A-3 행) · "## Spacing Scale" · "## Typography" · "## Color" · "## Component Inventory" · "### A-1" · "### A-2" · "### A-3" · "### A-7" · "## Interaction & Accessibility" · "## Decisions & Deviations Log"(+ 그 안의 "ui-checker flags (2026-09-26)" 표 — 적용값 정본)
    - app/src/app/analysis/reference.tsx :100-181 (화면 골격 · 뒤로 · 목록 · CTA) · :183-219 (`MotionCard`) · :246-315 (탭 · 스타일 토큰) · app/src/app/(tabs)/index.tsx (홈 그라디언트 헤더 + 흰 시트 + 알약 CTA + STEP 카드 실물 — Figma 1:717 의 코드판) · app/src/app/auth/login.tsx :40-104 (busy/notice · Google 버튼 · `SocialIcon`) · app/src/components/PickErrorDialog.tsx (`AlertIcon` SVG) · app/src/theme/{colors,typography,index}.ts (토큰 이름 — `gradients.homeTop/homeCard/brandButton` · `radius.card/button/listItem` · `typography.headline/sectionTitle/listTitle/buttonSecondary/metricNumber/caption` · `layout.ctaHeight`)
    - app/src/lib/authUser.ts (`useAuthUser`) · app/src/lib/socialAuth.web.ts (38-04) · app/src/lib/supplierMotions.ts · supplierRules.ts · api.ts (Task 1) · app/src/constants/supplierCopy.ts (38-03)
    - .planning/phases/38-supplier-link/38-04-MEASUREMENT.md (웹에서 깨진 것이 있으면 여기 적혀 있다 — 그 수리를 먼저)
    - .planning/phases/38-supplier-link/38-PATTERNS.md "### 공급자 페이지 — 후보 (1)" 절 전체
  </read_first>
  <action>
    **0. Figma(D-22, 구현 전):** `figma:figma-design-to-code` 스킬 로드 → `get_design_context`(fileKey `jrdI7kp245HkPfLB0nclsz`) 를 노드 `1:960`, `1:977`, `1:419`, `1:420`, `1:423`, `1:424`, `1:717`, `1:743`, `1:646`, `1:663`, `1:713`, `1:709` 에 실행. UI-SPEC 의 `≈` 값(Google 버튼 높이 ≈48 · STEP 라벨 ≈13.8 · 테두리 버튼 48 · 안내 알약 반경 ≈15)을 실측값으로 바꾸고 SUMMARY 관측 절에 "노드 → 실측값" 표를 `[확인]` 으로. 노드는 자동생성명이라 텍스트로 찾는다(메모리 figma-search-by-text). Figma 문자열(`시작해 볼까요?` `Google로 시작하기` `전체보기` `새로 추가됨` `정은지 선수` 어법 `STEP 1`)은 그대로. 원격 이미지 URL 을 코드에 넣지 않는다. Figma 와 UI-SPEC 이 다르면 Figma(SUMMARY 에 기록).
    **1. `SupplierUi.tsx`**(토큰만, 리터럴 색·간격 0): `TopBar`(뒤로 chevron 44×44 + 오른쪽 링크), `GradientHeader`(`gradients.homeTop`, 워드마크 + Display 제목 — **UI-SPEC §Decisions ui-checker flags 7행: 링크·identity 텍스트는 헤더가 아니라 흰 시트 위**), `Sheet`(상단 반경 17, `gradients.homeCard`, 헤더 위 −24 겹침), `Card`(radius.card · 테두리 divider · 패딩 cardPadding), `SectionHeader`(Heading 20/700 + `전체보기` 링크), `ListRow`(48 썸네일 자리 `images-outline` · Title 18/700 · 부제 17 #5A5A5A → `colors.textMid` · chevron 조건부 · 최소 높이 72 · 행 사이 1px divider), `StepCard`(점선 브랜드 테두리 · `STEP 1` 라벨 · Title · Label · 알약 CTA), `PillCta`(높이 44 · 반경 22 · `gradients.brandButton` · `동작 올리기 >`), `PrimaryCta`(54/13, 비활성 `colors.brandButtonDisabled` + `accessibilityState.disabled`), `OutlineButton`(48/13 · 테두리 `inputBorder` · 글자 textPrimary 700), `GoogleButton`(`SocialIcon` 재사용), `TextLink`(이동 = brand 밑줄 · 로그아웃 = `infoTeal` · 취소 = textMid 밑줄), `NoticePill`(`AlertIcon` 24 + 2줄 — 1:399 문법, podDown 재사용), `TipCard`(헤더 `alert-circle` 16 + `촬영 TIP!` · 줄 간 4 · 헤더-줄 8 **(ui-checker flags 8행: 헤더-줄 8 · 줄 간 4)**), `GrayCard`, `Toast`(상단 safe+16 · 배경 `colors.textPrimary` 토큰(#0C0C0C) · 3초 · `accessibilityLiveRegion="polite"`), `IdBox`(softBg · 반경 13 · `selectable`), `CodeCard`(1:646 선수 카드: Google `photoURL` 있으면 사진 180 + 그라디언트 오버레이 + `폴스포츠`(밑줄 brand) + `{name} 선수`; 없으면 흰 카드 — 코드 Display 30/700 은 **`colors.textPrimary`(ui-checker flags 9행: 브랜드색 금지, 화면당 브랜드 앵커 = 알약 CTA 1개)** · `OutlineButton` `코드 복사` · 안내 1줄). 칩 패딩 4 8(ui-checker flags 10행).
    **2. `supplier/index.tsx`**(default export `SupplierHome`, 루트 Stack headerShown false 라 TopBar 직접): 상태기계 `phase: 'boot' | 'login' | 'checking' | 'noAccess' | 'home'`; `useAuthUser()` → user 없음/익명 → **A-1**(TopBar 워드마크 · Display `login.title` · Label `login.body` · `GoogleButton` → `signInWithGoogle()` 웹 분기 · 진행 `login.busy` · `auth/popup-blocked` → `login.popupBlocked` 인라인(`infoTeal`) · 그 외 → `login.failed` · 하단 `common.build`(`{sha}` = `expo-constants` `expoConfig.extra?.commitSha ?? 'dev'`)); 로그인됨 → `probeSupplier()`: 403 `forbidden` → **A-2**(`alert-circle` 44 brand · Title `noAccess.title` · Label 2줄 · `noAccess.idLabel` + `IdBox`(uid) · `OutlineButton` `noAccess.copyId`(`expo-clipboard` 가 이미 설치돼 있으면 그것, 없으면 `navigator.clipboard` — **신규 패키지 설치 금지**; 실패 시 `common.copyFallback`) · `OutlineButton` `noAccess.refresh`(재probe, 진행 `noAccess.checking`, 여전히 403 → `noAccess.stillNo`) · `TextLink` `common.signOut`(`signOut(auth)` → A-1, 확인 없음)); 401 → A-1 + `form.sessionExpired`; 200 → `supplierCode` 저장 → **A-3**: `GradientHeader`(워드마크 + Display `home.title`) → `Sheet`: 시트 맨 위 한 줄 = Label identity `home.identity`(`{name}`=displayName, `{email}`) + 오른쪽 `TextLink` `common.guideLink` → `router.push('/supplier/guide')`(38-11 이 만든다 — 링크만) → **카드 1** `SectionHeader(home.motionsTitle, 행 > 6 이면 전체보기 → 같은 행 형식 전체 목록 뷰 상태)` · Label `home.motionsSub` · `queued` 행이 하나라도 있으면 `NoticePill(home.podDown 2줄)` · 행이 있으면 `PillCta(home.upload)` → `router.push('/supplier/upload')` · `ListRow` 목록(`sortNewestFirst`, 부제 `rowSubtitle` — `expired` 는 `row.status.expired`(R4), chevron `hasDetail`(active/failed/expired), `onPress` → Task 3 상세) · 빈 상태 = `StepCard(home.emptyStep · home.emptyTitle · home.emptyBody · PillCta)`(CTA 는 화면에 한 번만) · 구독 오류 = `GrayCard(home.listError)` + `OutlineButton(common.retry)` → **카드 2** `CodeCard`(`home.codeTitle` 섹션 제목 · `supplierCode` 없으면 Label `home.codePending` · `코드 복사` → `Toast(common.copied)` + 버튼 라벨 2초 `common.copiedShort` · `home.codeHow`) → `TextLink common.signOut` → `common.build`. 방금 올린 행(라우트 param `?justUploaded=refId`) 테두리 brand 3초 후 원복 + 스크롤. 네트워크 오류(`ApiError.status 0`) → 인라인 `common.offline` + `common.retry`.
    **3. `socialAuth.web.ts`** 는 38-04 결과 그대로 쓰되 `auth/popup-blocked` 코드 보존이 없으면 보강.
    typecheck 후 웹 스모크: `cd app && CI=1 npx expo export --platform web --output-dir <scratchpad>/web-export-10` → 성공 · `npx expo serve <dir> --port 8082` → 시뮬레이터 Safari `http://localhost:8082/supplier` 스크린샷(A-1) → SUMMARY 에 경로. 로그인이 필요한 A-2/A-3 는 38-13 belle 검증(`[미확인]` 표기).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck && test "$(grep -c 'supplierCopy\.' src/app/supplier/index.tsx)" -ge 20 && test "$(grep -cE '#[0-9A-Fa-f]{6}' src/app/supplier/index.tsx src/components/SupplierUi.tsx | awk -F: '{s+=$2} END {print s}')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && npm run typecheck` exits 0.
    - `grep -c "supplierCopy\." app/src/app/supplier/index.tsx` ≥ 20 and `grep -cE "#[0-9A-Fa-f]{6}" app/src/app/supplier/index.tsx app/src/components/SupplierUi.tsx` → 0 for both (토큰만).
    - `grep -c "probeSupplier\|useSupplierMotions\|signInWithGoogle" app/src/app/supplier/index.tsx` ≥ 3; `grep -c "figma.com\|figmaalpha\|s3-alpha-sig" app/src/app/supplier/index.tsx app/src/components/SupplierUi.tsx` == 0 (원격 이미지 URL 없음).
    - `CodeCard` code text style references `colors.textPrimary` (not `colors.brand`) — `grep -n "CodeCard" -A40 app/src/components/SupplierUi.tsx | grep -c "colors.brand" ` counts only the underline/link usages, code text line uses `textPrimary`.
    - `CI=1 npx expo export --platform web` exits 0 and the SUMMARY lists the Figma node → measured value table plus ≥ 1 simulator screenshot path.
  </acceptance_criteria>
  <done>로그인·권한 없음·홈 두 카드가 Figma 실측값·토큰·supplierCopy 만으로 렌더되고 웹 export 가 통과한다.</done>
</task>

<task type="auto">
  <name>Task 3: A-3b 실패 상세(1:428 계열, 라이트 — 비공개 doc 훅, R13) · 만료 패널(R4) · A-3c 완료 상세(1:498 — 재분석 한 줄 + 고지, R11) · 행 → 상세 전환(URL param) · `다시 올리기` 프리필 링크(선언은 비공개 doc 에서)</name>
  <files>app/src/components/SupplierUi.tsx, app/src/app/supplier/index.tsx</files>
  <read_first>
    - .planning/phases/38-supplier-link/38-UI-SPEC.md "### A-3b 실패 상세" · "### A-3c 완료 상세" · Component Inventory "실패 패널" · "완료 패널" · Copywriting `row.*` 표 · Figma 참조 표 A-3b/A-3c 행
    - app/src/app/analysis/loading.tsx :470-560 (실패 상태 원형 — 원형 X · 제목 · 본문 · 오류코드 칩 · TIP 카드 · CTA — 1:428 의 코드판; **색은 다크 예외라 라이트 토큰으로 옮긴다**)
    - app/src/lib/supplierRules.ts (38-03 — `failCopy` · `expiredCopy` · `selfCheckLine` · `selfCheckNote` · `hasDetail` · `normalizePrivate`) · app/src/lib/supplierMotions.ts (Task 1 — `useSupplierRegistrationPrivate`) · app/src/constants/supplierCopy.ts (`row.fail.*` · `row.expired.*` · `row.self.*`(ok 새 문구 · note) · `row.tip` · `row.reupload` · `row.done.title`)
    - .planning/phases/38-supplier-link/38-REVIEWS.md "### R4" · "### R11" · "### R13"
  </read_first>
  <action>
    Figma 실측(D-22): `get_design_context` 를 `1:428`, `1:479`, `1:482`, `1:483`, `1:486`, `1:458`, `1:498` 에 실행 → 구조(원형 아이콘 지름·제목/본문 간격·TIP 카드·CTA)만 가져오고 색은 라이트 토큰(링 `colors.brand`, 체크 `colors.progressGreen`) — SUMMARY 에 실측표.
    `SupplierUi.tsx` 에 `FailurePanel({ title, body, code, tips, onReupload, onBack })`(원형 X 72/링 2 brand · Title · Label 2줄 · 코드 칩 `softBg` 반경 8 패딩 **4 8**(ui-checker flags 10행) `caption` · `TipCard(row.tipHead, row.tip)` · `PrimaryCta(row.reupload)` · `TextLink(common.toList)`), `DonePanel({ title, selfLine, note, info, low, onReupload, onBack })`(원형 체크 72 · Title `row.done.title` · 재현성 줄(`selfCheckLine` — ok 분기 문구는 R11 판 `자기 영상 재분석 N점 — 추출·저장이 일관돼요`) — 점수 숫자만 Heading 20/700 brand · 바로 아래 Label 17 `textMid` `note` = `selfCheckNote()`(`동작 정확도는 시험 영상으로 따로 봐요.`, D-11) · 정보 표 = 단순 2열(동작 이름 · 선수 · 레벨 · 등록일 `YYYY.MM.DD` · 스플릿/유지/서 있는 시작 `네`/`아니오` — 뒤 셋은 **비공개 doc**(`useSupplierRegistrationPrivate`) 값, 로딩 중 `–`) · `low` 면 TipCard + `PrimaryCta(row.reupload)` · `TextLink(common.toList)`).
    `index.tsx`: 상세는 URL 로 표현 — `useLocalSearchParams<{ detail?: string }>()`; `ListRow.onPress`(hasDetail 일 때만) → `router.push({ pathname: '/supplier', params: { detail: m.motionId } })`; `detail` 이 있으면 `useSupplierRegistrationPrivate(detail)` 로 비공개 doc 구독(R13) + TopBar 뒤로(= `router.back()`, 브라우저 뒤로가기와 동일) + `failed` → `FailurePanel(failCopy(priv?.registrationError?.code ?? 'server_error', priv?.registrationError?.joints))`(비공개 doc 로딩 중엔 스켈레톤 1줄, `permission-denied` 면 목록으로); `expired` → `FailurePanel(expiredCopy(), code 칩 없음, TipCard 없음, CTA row.reupload)`(R4); `active` → `DonePanel(selfCheckLine(m), selfCheckNote(), low = selfScore != null && selfScore < SELF_SCORE_OK_MIN)`; 그 외 상태는 목록으로. `다시 올리기` → `router.push({ pathname: '/supplier/upload', params: { name, athleteName, level, isSplit: '1'|'0', hasHold, standingStart } })`(선언 3 은 비공개 doc 에서; 38-11 이 읽는 파라미터 계약 — 이름 그대로).
    웹 스모크(플랜 끝 1회 — task automated 게이트 밖, VALIDATION 38s 예산): export 재실행 → 성공 + 소요 초. 상세 뷰는 로그인 데이터가 필요해 시뮬 스크린샷은 `[미확인 — 38-13 belle]` 로 남기고, `supplierRules.test.ts` 가 `failCopy`/`selfCheckLine` 을 잠근다는 사실을 인용.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck && test "$(grep -c 'FailurePanel\|DonePanel' src/app/supplier/index.tsx)" -ge 2 && test "$(grep -c 'export function FailurePanel\|export function DonePanel' src/components/SupplierUi.tsx)" = "2" && test "$(grep -cE '#[0-9A-Fa-f]{6}' src/components/SupplierUi.tsx)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && npm run typecheck` exits 0; `CI=1 npx expo export --platform web` exits 0 at plan-level verification (플랜 끝 1회) (소요 = 38-04 실측값과 비교, task 게이트 38s 예산 밖).
    - `grep -c "FailurePanel\|DonePanel" app/src/app/supplier/index.tsx` ≥ 2 and `grep -c "export function FailurePanel\|export function DonePanel" app/src/components/SupplierUi.tsx` == 2.
    - `grep -c "pathname: '/supplier/upload'" app/src/app/supplier/index.tsx` ≥ 1 with params `name`, `athleteName`, `level`, `isSplit`, `hasHold`, `standingStart` present in that call.
    - `grep -c "detail" app/src/app/supplier/index.tsx` ≥ 3 (URL param 로 상세 표현); `grep -c "useSupplierRegistrationPrivate\|expiredCopy\|selfCheckNote" app/src/app/supplier/index.tsx` ≥ 3 (R13·R4·R11).
    - `grep -cE "#[0-9A-Fa-f]{6}" app/src/components/SupplierUi.tsx` == 0.
  </acceptance_criteria>
  <done>실패 4형·등록됨 상세가 Figma 구조 + 라이트 토큰으로 있고 행 → 상세 → 다시 올리기 경로가 연결된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → Firebase Auth(팝업) / Firestore(onSnapshot) / HTTP API / S3 PUT | ID 토큰이 브라우저에 저장된다(웹 persistence local) |
| Firestore `reference/**` 읽기 | 인증 사용자 전체 읽기 규칙(파일럿 수용) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-10-1 | Spoofing | 로그인 | mitigate | Firebase 팝업 API 만, 리다이렉트 금지, 익명은 화이트리스트 재료 아님(A-1 은 Google 1종) |
| T-38-10-2 | Information disclosure | `where supplierUid` 구독 / 비공개 doc | mitigate | 공개 doc 은 인증자 전체 읽기(picker·상태·점수·작업 필드 — 동의·실패 상세·선언은 **없다**); 동의·실패 상세·선언은 `reference/{refId}/private/registration` 에 있고 규칙이 `supplierUid == request.auth.uid` 만 읽게 한다(38-06 T3·38-09 F, 리뷰 R13). 옛 "doc 에 개인정보 없음" 서술은 정정 |
| T-38-10-3 | Tampering | `{joints}` 치환 | mitigate | `String.replace` → RN `Text` 로만 렌더(HTML 해석 없음) |
| T-38-10-4 | Elevation | 페이지 쓰기 | mitigate | 브라우저는 Firestore 를 쓰지 않는다(규칙 write false) — 모든 쓰기는 Lambda/Pod |
| T-38-10-5 | DoS | 재probe 연타 | accept | 버튼 진행 상태로 중복 호출 차단, API GW 기본 스로틀 |
| T-38-SC | Tampering | npm installs | accept | 이 플랜 설치 0(`expo-clipboard` 미설치면 `navigator.clipboard`) |
</threat_model>

<verification>
- task 게이트: `node --test app/src/lib/supplierRules.test.ts` · `npm run typecheck` · grep 게이트(토큰만·리터럴 0·원격 URL 0).
- 플랜 끝 1회(task automated 게이트 밖 — VALIDATION 38s 예산): `cd app && CI=1 npx expo export --platform web --output-dir /private/tmp/claude-501/-Users-kimtaesung-Dev-SunityMotion/ea854566-c782-48be-b553-1ce36244b8ed/scratchpad/web-export-10` exit 0, 소요 초를 SUMMARY 에(38-04 실측과 비교).
- 플랜 끝 push.
</verification>

<success_criteria>
- `/supplier` 라우트가 로그인 → 권한 확인 → 홈 두 카드 → 상세까지 Figma 실측·토큰·supplierCopy 만으로 렌더되고 웹 번들이 만들어진다.
</success_criteria>

<output>
Create `.planning/phases/38-supplier-link/38-10-SUMMARY.md` when done (Figma 실측표 · 스크린샷 경로 · 미확인 항목).
</output>
