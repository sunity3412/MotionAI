---
phase: 38-supplier-link
plan: 11
type: execute
wave: 5
depends_on: ["38-10"]
files_modified:
  - app/src/components/SupplierUi.tsx
  - app/src/app/supplier/upload.tsx
  - app/src/app/supplier/guide.tsx
autonomous: true
# 38-04 Task 3 (b): belle 이 option-2 를 고르면 여기에 `skipped: true` 가 추가되고 38-11-SUMMARY.md 가 skipped-by-decision 으로 쓰인다
requirements: [REQ-38-3, REQ-38-6, REQ-38-8]
must_haves:
  truths:
    - "D-07/D-08: 올리기 폼이 STEP 01(촬영 전 체크 5 확인 · 동작 이름(사전 선택 + 새 이름) · 선수 이름 · 레벨 · 선언 3 네/아니오 · 영상 파일) → STEP 02(동의 필수 3 + 학습 사용 선택 기본 꺼짐 + 철회 고지) 순서로, 필수가 다 차기 전엔 `다음`/`올리기` 가 비활성이고 남은 개수를 글자로 준다"
    - "D-14: 5초 미만/30초 초과(콤보 60) 는 1:499 형식 다이얼로그로 즉시 막고, `<video>` metadata 를 못 읽으면 통과(fail-open, 서버 2차 방어)"
    - "D-09: 선언 '서 있는 자세에서 시작했나요' 아니오 → 제출 차단 문구"
    - "D-04: 제출 = `POST /reference/upload-url` → XHR PUT(진행률·취소) → 토스트 → 홈으로(방금 올린 행 강조); 401/403/5xx/PUT 실패 각각 UI-SPEC 문구"
    - "D-18: 상세 가이드 화면(A-6) 7섹션이 `supplierCopy.guide.*` = `docs/supplier-guide.md` 와 글자 단위로 같다"
    - "D-11 (리뷰 R7): 동작 이름 helper(`form.sec2.name.helper` 리뷰판)가 '이름과 선언은 등록 정보로 보관, 채점은 지금 기본 비교 방식만' 을 말한다 — 채점 규칙이 붙는다고 약속하지 않는다(`techniqueRefId` 소비 배선 없음)"
    - "리뷰 R14: 폼 규칙(`remainingRequired`·`validateFile`·`consentAllRequired`·`buildRequest`·`parsePrefill`)과 오류 매핑(`mapPresignFailure`)·전이(`uploadOutcomeNext`)는 38-03 `supplierForm`/`supplierRules` import 만 — 화면 파일에 규칙 재작성 0"
  artifacts:
    - path: "app/src/app/supplier/upload.tsx"
      provides: "A-4 STEP 01/02 폼 + A-5 업로드 진행/결과"
      min_lines: 250
    - path: "app/src/app/supplier/guide.tsx"
      provides: "A-6 상세 가이드 화면 7섹션 + 예시 사진 2장"
      min_lines: 60
    - path: "app/src/app/supplier/upload.tsx"
      provides: "38-03 supplierForm/supplierRules import (규칙 재정의 0)"
      contains: "from '../../lib/supplierForm'"
  key_links:
    - from: "app/src/app/supplier/upload.tsx"
      to: "app/src/lib/api.ts requestReferenceUploadUrl + uploadToS3WithProgress"
      via: "제출 → presign → XHR PUT"
      pattern: "requestReferenceUploadUrl"
    - from: "app/src/app/supplier/guide.tsx"
      to: "app/src/constants/supplierCopy.ts guide.*"
      via: "섹션 7 렌더"
      pattern: "supplierCopy.guide"
---

<objective>
**후보 (1) 전용(38-10 과 같은 조건).** 올리기 폼(A-4 STEP 01/02 · A-5 진행/결과, D-04·D-07·D-08·D-09·D-14)과 상세 가이드 화면(A-6, D-18)을 Expo Router 라우트 `supplier/upload.tsx` · `supplier/guide.tsx` 로 만든다. 폼 규칙·오류 매핑·전이는 38-03 의 순수 함수(`supplierForm.ts`·`supplierRules.ts`, 공용 fixture 로 이미 테스트됨)를 **import 만** 한다(리뷰 R14).

**리뷰 반영(2026-09-26):** R7(helper 문구 정직 — supplierCopy 리뷰판) · R14(규칙 import) · R4(취소·재시도 뒤 유령 등록: 재시도는 새 presign = 새 refId — 옛 `registering` 은 requeue 스윕이 `expired` 로 닫는다, 화면은 만료 행을 보여 준다(38-10)).

Purpose: 폼이 받는 선언 4·동의 4 가 뒤쪽 자동화의 입력이고(REQ-38-3), 길이 검사와 촬영 전 체크 5 가 실패 4형을 미리 줄인다(REQ-38-6·REQ-38-8).
Output: 라우트 2개 + `SupplierUi.tsx` 폼 프리미티브 추가(폼 규칙·테스트는 38-03 산출물을 import). 웹 export 스모크 + 시뮬 Safari 스크린샷.

Phase 38 전제(38-01 머리말) 적용. D-22 Figma 실측 선행(노드: `1:960` 입력/STEP/CTA · `1:1064` 동의 행 · `1:407` 파일 카드 · `1:399` 알약(문구는 정정본) · `1:499` 다이얼로그 · `1:482/483` TIP · `1:709` 가이드 카드). 신규 패키지 설치 금지.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/38-supplier-link/38-CONTEXT.md
@.planning/phases/38-supplier-link/38-UI-SPEC.md
@.planning/phases/38-supplier-link/38-PATTERNS.md
@.planning/phases/38-supplier-link/38-10-SUMMARY.md
@app/CLAUDE.md
@design.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Figma 실측 → `SupplierUi.tsx` 폼 프리미티브(Input · Select · Segment · CheckboxRow · AllAgreeBox · FileCard · ProgressBar · ValidationDialog 재사용) → `upload.tsx` STEP 01 — 규칙은 38-03 `supplierForm` import(R14)</name>
  <files>app/src/components/SupplierUi.tsx, app/src/app/supplier/upload.tsx</files>
  <read_first>
    - .planning/phases/38-supplier-link/38-UI-SPEC.md "### A-4 올리기 폼" 표(①~④ + STEP 02) · Component Inventory(Input · Select · 탭/세그먼트 · Checkbox row · 전체 동의 박스 · 선택 카드(파일) · 안내 알약 · 검증 다이얼로그 · Progress bar) · Copywriting `form.*` · `dialog` 표 · Figma 참조 표 A-4 행 · "## Interaction & Accessibility"
    - app/src/app/(tabs)/analyze.tsx :204-236 (`validate`/`checkLowQuality` — 종류 반환 규율) · :420-480 (`pickFromLibrary` — `ImagePicker.launchImageLibraryAsync`; 웹에서는 asset.`file`) · :900-960 (체크박스 행 실물 :918-938 — 1:1064 의 코드판) · app/src/components/PickErrorDialog.tsx (전체 — 1:499 토큰 그대로, `PickFailureCopy` 형상) · app/src/lib/pickerFailure.ts :30-50 (`PickFailureCopy` 타입)
    - app/src/app/analysis/reference.tsx :246-263 (탭 = 세그먼트 원형) · app/src/app/auth/signup.tsx (입력 필드 54/13 · 라벨 위 · 오류 틸 — 1:960 의 코드판)
    - app/src/lib/referenceMotions.ts (`useReferenceMotions` — 사전 옵션 = 활성 동작 이름 중복 제거 + motionId) · app/src/lib/videoMeta.ts (38-10) · **app/src/lib/supplierForm.ts · supplierRules.ts (38-03 — `Step1State`·`remainingRequired`·`validateFile`·`parsePrefill`·`buildRequest`; import 만)** · app/src/constants/supplierCopy.ts (38-03 — `form.sec2.name.helper` 리뷰판 R7)
    - .planning/phases/38-supplier-link/38-REVIEWS.md "### R7" · "### R14"
    - .planning/phases/38-supplier-link/38-PATTERNS.md "### 공급자 페이지 — 후보 (1)" · Shared Patterns §10·§11
  </read_first>
  <action>
    규칙 파일을 만들지 않는다 — `import { remainingRequired, validateFile, parsePrefill, type Step1State } from '../../lib/supplierForm'`(38-03). **Figma(D-22):** `get_design_context` 를 `1:960`, `1:1064`, `1:407`, `1:399`, `1:499`, `1:482`, `1:483` 에 실행 → 입력 높이/반경/테두리, STEP 라벨 크기, 동의 행 높이·체크박스 크기·`[필수]`/`[선택]` 색, 파일 카드 아이콘/간격(**UI-SPEC §Decisions ui-checker flags 11행: 파일 카드 아이콘-텍스트 gap 16**), 다이얼로그 값이 `PickErrorDialog` 토큰과 같은지 대조 → SUMMARY 실측표. `1:399` 문구는 복제 금지(정정본 `form.sec4.pill`).
    `SupplierUi.tsx` 추가: `StepLabel`(`STEP 01 / 02` brand 소문), `TextInput54`(높이 54/13, 테두리 `inputBorder` → 포커스 `brand` → 오류 `inputError`, 라벨 위 8, 오류 문구 아래 8 `infoTeal` `accessibilityLiveRegion`), `SelectField`(웹은 네이티브 `<select>` 를 RN `Picker` 대신 — `react-native-web` 에서 `select` 는 `createElement('select')`; 네이티브 빌드에서는 이 라우트를 쓰지 않으므로 `Platform.OS === 'web'` 분기 + 그 외는 옵션 목록 버튼), `Segment`(2~3 옵션 균등, 44/13, 선택 = brand 채움 흰 글자, `accessibilityRole="radio"`), `CheckboxRow`(최소 48, 박스 22/8, 태그 `[필수]` brand 700 / `[선택]` `resultTextSub` 700, 부연 12 `caption`, chevron 조건부, 행 전체 탭), `AllAgreeBox`(54/13, 테두리 divider), `FileCard`(1:407 — `images-outline` 32 brand · Title `form.sec4.card.title` · Label `form.sec4.card.sub` · chevron; 눌림 `cardDimmed`), `VideoPreview`(`expo-video` `VideoView` 9:16 최대 360 · muted · controls; 웹 blob URL), `ProgressBar`(트랙 8/4 `trackBg` · 채움 brand · `accessibilityRole="progressbar"`), `BottomBar`(흰 배경 · 위 1px divider · 패딩 16 20 · 위 Label `form.remaining`).
    `upload.tsx`(default export `SupplierUpload`, 인증 가드: 로그인/화이트리스트 아니면 `/supplier` 로 replace): `useLocalSearchParams` → `parsePrefill`; **STEP 01**: TopBar 뒤로 · `StepLabel(form.step1.label)` · Display `form.step1.title` · 섹션 카드 4(사이 24): ① `TipCard(form.sec1.title, form.sec1.items)` + `CheckboxRow(form.sec1.confirm)` + `TextLink(form.sec1.guideLink)` → `/supplier/guide` · 미체크 오류 `form.sec1.error`; ② `SelectField(form.sec2.name.label)` 옵션 = `useReferenceMotions()` 활성 동작 이름 중복 제거(값 = motionId) + 마지막 `form.sec2.name.newOption` → `TextInput54(form.sec2.name.newPlaceholder, 최대 30)` · helper `form.sec2.name.helper`(리뷰 R7 판 — "등록 정보로 보관 · 채점은 지금 기본 비교 방식" — supplierCopy 그대로, 창작 금지) · `CheckboxRow(form.sec2.combo.label, [선택])` · `TextInput54(form.sec2.athlete.label, 프리필 = Google displayName)` + helper · `Segment(form.sec2.level.options)` + helper; ③ 질문 3 × (`Label` + `Segment(네/아니오)` + helper) — `stand` 아니오 → 인라인 `form.sec3.stand.blocked` + CTA 비활성 유지; ④ `FileCard` → `ImagePicker.launchImageLibraryAsync({ mediaTypes: ['videos'] })` → asset(웹 `asset.file`, `fileName`, `fileSize`) → `readVideoDurationSec(asset.file ?? asset.uri)` → `validateFile` → kind 있으면 `PickErrorDialog` 에 `supplierCopy.dialog[kind]`(`{title, lines, primaryLabel: form.sec4.repick, primaryAction:'repick'}`) · 통과 → `VideoPreview` + `form.sec4.meta`(`{durationSec}`·`{sizeMB}`) + `form.sec4.previewHint` + `OutlineButton(form.sec4.repick)` · `NoticePill(form.sec4.pill)`; 하단 `BottomBar` + `PrimaryCta(form.next, disabled = remaining>0 || blocked)` + `form.remaining` `{n}`. STEP 01 상태는 세션 안에서 유지(`checkConfirmed` 포함).
    웹 스모크(플랜 끝 1회 — task automated 게이트 밖): export 성공 + 시뮬 Safari `http://localhost:8082/supplier/upload` 는 로그인 가드로 `/supplier` 로 돌아가는 것만 확인(스크린샷 1장), 폼 화면은 38-13 belle.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck && test "$(grep -c 'PickErrorDialog' src/app/supplier/upload.tsx)" -ge 1 && test "$(grep -c 'launchImageLibraryAsync' src/app/supplier/upload.tsx)" = "1" && grep -q "from '../../lib/supplierForm'" src/app/supplier/upload.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from '../../lib/supplierForm'" app/src/app/supplier/upload.tsx` == 1 and `grep -c "function validateFile\|function remainingRequired\|function buildRequest" app/src/app/supplier/upload.tsx` == 0 (규칙 재정의 0, R14).
    - `cd app && npm run typecheck` exits 0; `CI=1 npx expo export --platform web` exits 0 at plan-level verification (플랜 끝 1회) (task 게이트 38s 예산 밖).
    - `grep -c "supplierCopy\.form\." app/src/app/supplier/upload.tsx` ≥ 25; `grep -cE "#[0-9A-Fa-f]{6}" app/src/app/supplier/upload.tsx app/src/components/SupplierUi.tsx` → 0.
    - `grep -c "PickErrorDialog" app/src/app/supplier/upload.tsx` ≥ 1 and `grep -c "launchImageLibraryAsync" app/src/app/supplier/upload.tsx` == 1.
    - `grep -c "npm install\|expo install" .planning/phases/38-supplier-link/38-11-SUMMARY.md` == 0 (설치 0).
  </acceptance_criteria>
  <done>STEP 01 이 Figma 실측값과 38-03 순수 규칙 위에서 동작하고 길이 검사·서 있는 시작 차단이 코드로 있다.</done>
</task>

<task type="auto">
  <name>Task 2: `upload.tsx` STEP 02 동의(전체 동의 = 필수 3) · 철회 고지 · 제출 → presign → XHR PUT 진행률/취소 → 결과(토스트·실패 패널) · 오류 매핑(401/403/5xx/PUT)</name>
  <files>app/src/app/supplier/upload.tsx</files>
  <read_first>
    - .planning/phases/38-supplier-link/38-UI-SPEC.md "**STEP 02**" 문단 · "### A-5 업로드 진행 → 결과" · "### A-7" 표 · Copywriting `form.sec5.*` · `form.uploading.*` · `form.uploaded.toast` · `form.uploadFail.*` · `form.sessionExpired` · `form.presignFail` · "Open for belle 2·3" · Decisions 14
    - app/src/lib/api.ts (38-10 — `requestReferenceUploadUrl` · `uploadToS3WithProgress` · `ApiError.code/status`) · app/src/lib/supplierForm.ts (38-03 — `buildRequest` · `consentAllRequired`) · app/src/lib/supplierRules.ts (38-03 — `mapPresignFailure` · `uploadOutcomeNext`)
    - .planning/phases/38-supplier-link/38-REVIEWS.md "### R4"(취소·재시도 뒤 유령 등록) · "### R14"
    - app/src/app/analysis/loading.tsx :140-200 (앱의 presign → PUT 순서와 오류 문구 계층 — 여기서는 Firestore doc 을 **앱이 쓰지 않는다**(Lambda 가 선작성) 는 차이를 주석에)
    - .planning/phases/38-supplier-link/38-CONTEXT.md D-08 · D-04
    - .planning/phases/38-supplier-link/38-PATTERNS.md "### 공급자 페이지 — 후보 (1) Expo Router 라우트 `app/src/app/supplier/index.tsx` · `guide.tsx`" 절(`authedJson`·`uploadToS3` 발췌 + 플래너 메모) + Shared Patterns §10·§11
  </read_first>
  <action>
    **STEP 02**(`StepLabel(form.step2.label)` · Display `form.step2.title`): `AllAgreeBox(form.sec5.all)` → 탭하면 **필수 3 만** 토글(학습 사용은 건드리지 않는다 — D-08, UI-SPEC Decision 14; Figma 실측에서 전체 동의가 선택까지 켜는 것이 확인되면 SUMMARY 에 belle 판정 요청으로 기록하고 기본값 유지) · `CheckboxRow` 4: `[필수] form.sec5.portrait` · `[필수] form.sec5.usage` · `[필수] form.sec5.silent` · `[선택] form.sec5.training`(기본 꺼짐, 부연 `form.sec5.trainingNote`, chevron → `/supplier/guide#s5`) · Label `form.sec5.withdraw`(고지문, 버튼 없음) · `BottomBar` + `PrimaryCta(form.submit, disabled = !consentAllRequired)` + `form.remaining`. 뒤로 = STEP 01 로 입력 유지(`history.pushState` 대신 라우트 param `step=2` 로 브라우저 뒤로가기 동일).
    **제출**: `req = buildRequest(...)` → `requestReferenceUploadUrl(req)`: 실패는 `mapPresignFailure({status, code})`(38-03) 로 갈라 `'sessionExpired'` → `form.sessionExpired` + `/supplier` 로; `'forbidden'` → `/supplier`(A-2 가 보여준다); `'offline'` → 인라인 `common.offline` + `common.retry`; `'presignFail'` → 실패 패널(`form.presignFail` 본문, CTA `form.uploadFail.retry` = 재제출, 링크 `form.uploadFail.home`); 200 → **A-5 진행 패널**(Title `form.uploading.title` · `ProgressBar` + `올리는 중 · {pct}%` · `form.uploading.keepOpen` · `TextLink(common.cancel = '올리기 취소')` → `AbortController.abort()` → STEP 02 로 복귀 입력 유지) → `uploadToS3WithProgress(uploadUrl, blob, format, {onProgress, signal})`(blob = `asset.file` 또는 `fetch(uri).blob()`) → 결과는 `uploadOutcomeNext(outcome)`(38-03) 로: `'home'` → `router.replace({ pathname: '/supplier', params: { justUploaded: refId } })` + 토스트 `form.uploaded.toast`(홈이 param 으로 띄운다 — 38-10 계약); `'step2'`(aborted) → STEP 02 복귀 입력 유지; `'failPanel'` → 실패 패널(`form.uploadFail.title/body`, CTA `form.uploadFail.retry` = **새 presign 부터** 다시(= 새 refId; 옛 `registering` 은 900초 뒤 requeue 스윕이 `expired` 로 닫고 홈이 만료 행으로 보여 준다 — 리뷰 R4, 주석), 링크 `form.uploadFail.home`). 업로드 중 `beforeunload` 경고(웹) + 화면 전환 차단.
    Firestore 쓰기 0(규칙 write false — 선작성은 Lambda). 모든 문구 `supplierCopy` 만.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck && test "$(grep -c 'requestReferenceUploadUrl\|uploadToS3WithProgress\|AbortController' src/app/supplier/upload.tsx)" -ge 3 && test "$(grep -c "from 'firebase/firestore'" src/app/supplier/upload.tsx)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && npm run typecheck` exits 0.
    - `grep -c "requestReferenceUploadUrl\|uploadToS3WithProgress\|AbortController" app/src/app/supplier/upload.tsx` ≥ 3; `grep -c "from 'firebase/firestore'" app/src/app/supplier/upload.tsx` == 0 (페이지는 Firestore 를 쓰지 않는다); `grep -c "mapPresignFailure\|uploadOutcomeNext" app/src/app/supplier/upload.tsx` ≥ 2 (R14).
    - `grep -c "form.sec5.portrait\|form.sec5.usage\|form.sec5.silent\|form.sec5.training\|form.sec5.withdraw" app/src/app/supplier/upload.tsx` == 5.
    - `grep -c "justUploaded" app/src/app/supplier/upload.tsx` ≥ 1 (홈 강조 계약).
    - `grep -c "sessionExpired\|presignFail\|uploadFail" app/src/app/supplier/upload.tsx` ≥ 4.
  </acceptance_criteria>
  <done>동의 → 제출 → 업로드 진행/취소 → 결과가 UI-SPEC 문구·오류 계층대로 있고 페이지는 Firestore 를 쓰지 않는다.</done>
</task>

<task type="auto">
  <name>Task 3: `guide.tsx` A-6 상세 가이드(7섹션 · 예시 사진 2장 · 실패 4형 TIP · 앵커 `#s5`) + 홈/폼 링크 확인 + 웹 export/시뮬 스크린샷</name>
  <files>app/src/app/supplier/guide.tsx</files>
  <read_first>
    - .planning/phases/38-supplier-link/38-UI-SPEC.md "### A-6 상세 가이드 화면" · Component Inventory "Guide image" · Copywriting `guide.*` · Figma 참조 표 A-6 행
    - app/src/app/help.tsx (전체 — 카드 목록 화면 원형 · TopBar · styles) · app/src/constants/supplierCopy.ts `guide` · app/assets/motion-thumbs/ref-kip-up.jpg · ref-power-spin.jpg (예시 자산 — 정은지 기준 프레임)
    - docs/supplier-guide.md (38-03 — 글자 단위 동일 정본)
    - .planning/phases/38-supplier-link/38-PATTERNS.md "### 공급자 페이지 — 후보 (1) …" 절의 "가이드 화면 — 접힘 목록 + 쉬운 말 항목 데이터"(`help.tsx` 발췌) + File Classification `app/src/app/supplier/guide.tsx` 행(analog = `help.tsx` :20-58 · :109-138 · :140-196)
  </read_first>
  <action>
    Figma(D-22): `get_design_context` `1:709`(이력 카드 = 흰 목록 카드 + 닫기) · `1:482`/`1:483`(TIP 카드) 실측 → SUMMARY.
    `guide.tsx`(default export `SupplierGuide`, 로그인 불요 — 링크 공유 가능): TopBar 뒤로 · Display `guide.title` · Label `guide.sub` · 32 · 섹션 7 = `Card`(Title 18/700 `guide.sN.h` + 줄 Label 17/400 각 `· ` 접두, 사이 24; `ScrollView` + `useLocalSearchParams` 의 hash/`section` param 으로 `#s5` 스크롤) · ①: 끝에 `Image` 2장(`require('../../../assets/motion-thumbs/ref-kip-up.jpg')`, `ref-power-spin.jpg`; 9:16 각 50%−6, 반경 15) + 캡션 `guide.s1.caption` · ④: 안에 `TipCard(guide.s4.tipHead, guide.s4.tip)` · 나쁜 예 사진 없음. 문구는 `supplierCopy.guide` 만(리터럴 0) — `supplierCopy.test.ts` 가 md 와의 동일성을 이미 잠근다.
    홈(38-10) `common.guideLink` 와 폼 `form.sec1.guideLink`/학습 사용 chevron 이 이 라우트로 오는지 `grep` 으로 확인. 웹 export(플랜 끝 1회 — task automated 게이트 밖) → `npx expo serve` → 시뮬 Safari `http://localhost:8082/supplier/guide` 스크린샷(로그인 불요라 실물 확인 가능) → SUMMARY 경로.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck && test "$(grep -c 'supplierCopy.guide' src/app/supplier/guide.tsx)" -ge 7 && grep -q "ref-kip-up.jpg" src/app/supplier/guide.tsx && grep -q "ref-power-spin.jpg" src/app/supplier/guide.tsx && test "$(grep -c '/supplier/guide' src/app/supplier/index.tsx src/app/supplier/upload.tsx | awk -F: '{s+=$2} END {print s}')" -ge 2</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && npm run typecheck` exits 0; at plan-level verification (플랜 끝 1회) (task 게이트 밖) `CI=1 npx expo export --platform web` exits 0 and the export output lists routes `supplier/index`, `supplier/upload`, `supplier/guide` (or the dist contains `supplier/guide.html` when `web.output: static` — otherwise SPA index only; record which).
    - `grep -c "supplierCopy.guide" app/src/app/supplier/guide.tsx` ≥ 7; both example image requires present; `grep -cE "#[0-9A-Fa-f]{6}" app/src/app/supplier/guide.tsx` == 0.
    - `grep -c "/supplier/guide" app/src/app/supplier/index.tsx app/src/app/supplier/upload.tsx` → ≥ 1 each.
    - SUMMARY has the simulator screenshot path for `/supplier/guide` (`[확인]`).
  </acceptance_criteria>
  <done>가이드 화면이 md 정본과 같은 문구로 렌더되고 홈·폼에서 링크된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 파일 선택 → 페이지 | 임의 파일(형식·크기·길이 통제 불가) |
| 페이지 → HTTP API / S3 PUT | ID 토큰 · presigned URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-11-1 | Tampering | 폼 입력 | mitigate | 클라이언트 `validateFile`/필수 검사(38-03 규칙) + 서버 `validate_reference_upload_request`(38-01) + 파이프라인 `head_object`/probe(38-07) 삼중 |
| T-38-11-6 | Denial of service | 취소·재시도 유령 등록(R4) | mitigate | 재시도는 새 refId; 옛 `registering` 은 `uploadExpiresAt` 뒤 requeue `--sweep-expired` 가 `expired` 로 닫는다(38-08) — 화면은 만료 행(38-10) |
| T-38-11-2 | DoS | 큰/긴 영상 | mitigate | 100MB·5~30초(콤보 60) 즉시 차단, duration 못 읽으면 서버 2차(38-07) |
| T-38-11-3 | Repudiation | 동의 | mitigate | 필수 3 true 만 제출 가능, 서버가 시각·버전·uid 기록(38-06) |
| T-38-11-4 | Information disclosure | presigned URL | accept | 900초 만료, `reference/{uid}/…` 한 키 PutObject 만 |
| T-38-11-5 | Tampering | 페이지의 Firestore 쓰기 | mitigate | 0건(규칙 write false, 선작성은 Lambda) — acceptance grep |
| T-38-SC | Tampering | npm installs | accept | 설치 0 |
</threat_model>

<verification>
- task 게이트: `node --test app/src/lib/supplierForm.test.ts` · `npm run typecheck` · grep 게이트.
- 플랜 끝 1회(task automated 게이트 밖 — VALIDATION 38s 예산): `cd app && CI=1 npx expo export --platform web --output-dir /private/tmp/claude-501/-Users-kimtaesung-Dev-SunityMotion/ea854566-c782-48be-b553-1ce36244b8ed/scratchpad/web-export-11` exit 0 + 세 라우트(`supplier/index`·`upload`·`guide`) 포함 확인 + 소요 초 SUMMARY 기록 · 플랜 끝 push.
</verification>

<success_criteria>
- 폼 2단계·업로드·가이드가 Figma 실측·토큰·supplierCopy 로 완성되고 웹 번들이 세 라우트를 포함한다.
</success_criteria>

<output>
Create `.planning/phases/38-supplier-link/38-11-SUMMARY.md` when done (전체 동의 의미 Figma 실측 결과 = belle 판정 요청 여부 기록).
</output>
