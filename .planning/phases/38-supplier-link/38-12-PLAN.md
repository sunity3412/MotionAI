---
phase: 38-supplier-link
plan: 12
type: execute
wave: 4
depends_on: ["38-01", "38-03", "38-04"]
files_modified:
  - app/scripts/export-supplier-copy.mjs
  - app/scripts/build-supplier-web.mjs
  - web/supplier/index.html
  - web/supplier/supplier.css
  - web/supplier/supplier.js
  - web/supplier/.gitignore
autonomous: true
# 38-04 Task 3 (b): belle 이 option-1 을 고르면 여기에 `skipped: true` 가 추가되고 38-12-SUMMARY.md 가 skipped-by-decision 으로 쓰인다
requirements: [REQ-38-3, REQ-38-4, REQ-38-5, REQ-38-6, REQ-38-8]
must_haves:
  truths:
    - "**조건부 대안 — 38-04 결정이 `option-2` 일 때만 실행한다(option-1 이면 이 플랜은 건너뛰고 38-10 · 38-11 이 대신 실행된다). 정확히 한 쪽만 실행된다.**"
    - "D-01/D-02: 폰 브라우저 링크 하나가 공급자 첫 화면이며 로그인 뒤 **내 동작(+올리기)** 와 **내 코드** 두 카드가 보인다 — 같은 UI-SPEC 상태기계(A-1~A-7)와 같은 `supplierCopy` 문구를 쓴다"
    - "D-03: Google 팝업 로그인 1개(Firebase JS SDK 12.13.0 gstatic ESM — 앱 lockfile 과 같은 버전 고정), 화이트리스트 밖이면 uid 표시·복사·재확인"
    - "D-07/D-08/D-14/D-09/D-18: 폼 STEP 01/02 · 길이 5~30(콤보 60) 다이얼로그 · 서 있는 시작 차단 · 동의 필수 3/선택 1 · 가이드 7섹션 — 38-10/38-11 과 같은 규칙"
    - "D-22: 디자인 값은 UI-SPEC §Color 의 CSS 변수 블록(Figma 실측값 1:1) 로, 구조는 같은 Figma 노드를 `get_design_context` 로 실측해 만든다"
    - "D-13: 크레딧·결제 없음"
  artifacts:
    - path: "web/supplier/index.html"
      provides: "정적 단일 페이지(A-1~A-7 + A-6 가이드 뷰) 골격 + `<template>` 들"
      min_lines: 120
    - path: "web/supplier/supplier.js"
      provides: "ESM — Firebase auth/firestore · fetch+Bearer · XHR PUT · 상태기계 · 문구 치환"
      min_lines: 300
    - path: "app/scripts/export-supplier-copy.mjs"
      provides: "app/src/constants/supplierCopy.ts → web/supplier/copy.js (단일 출처)"
      contains: "supplierCopy"
  key_links:
    - from: "web/supplier/supplier.js"
      to: "web/supplier/copy.js (generated from supplierCopy.ts)"
      via: "import { supplierCopy } from './copy.js'"
      pattern: "copy.js"
    - from: "web/supplier/supplier.js"
      to: "POST /reference/upload-url"
      via: "fetch + Authorization: Bearer idToken"
      pattern: "/reference/upload-url"
---

<objective>
**조건부 대안(후보 2).** 38-04 에서 belle 이 `option-2` 를 고른 경우에만 실행한다. 공급자 페이지를 정적 단일 페이지(HTML + CSS + ESM JS 3파일, 의존성 0)로 만든다. 상태기계·문구·규칙은 UI-SPEC 과 `app/src/constants/supplierCopy.ts`(38-03) 를 **그대로 재사용**한다 — 문구 파일은 빌드 스크립트가 TS 에서 내보낸다(Node 24 type stripping, 신규 패키지 0). 배포는 38-13 이 `web/supplier/dist/` 를 올린다.

Purpose: RESEARCH §H 후보 (2) — 번들 리스크 0. "같은 앱" 문자 그대로는 아니므로(SUMMARY 에 명시) 디자인은 CSS 변수로 1:1 재현한다.
Output: `web/supplier/{index.html, supplier.css, supplier.js}` + 빌드 스크립트 2개(copy.js · config.js 생성 → `web/supplier/dist/`) + `.gitignore`(dist/·copy.js·config.js).

Phase 38 전제(38-01 머리말) 적용. D-22 Figma 실측은 38-10/38-11 Task 와 같은 노드 목록으로 각 task 시작에. `npx serve` 등 미감사 패키지 반입 금지(로컬 서빙은 `python3 -m http.server`).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/38-supplier-link/38-CONTEXT.md
@.planning/phases/38-supplier-link/38-UI-SPEC.md
@.planning/phases/38-supplier-link/38-PATTERNS.md
@.planning/phases/38-supplier-link/38-04-MEASUREMENT.md
@.planning/phases/38-supplier-link/38-03-SUMMARY.md
@design.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 빌드 스크립트(copy.js · config.js · dist) + `index.html` 골격/템플릿 + `supplier.css`(UI-SPEC CSS 변수 1:1) + `supplier.js` 로그인(A-1) · 권한 없음(A-2) · 홈 두 카드(A-3, onSnapshot where supplierUid)</name>
  <files>app/scripts/export-supplier-copy.mjs, app/scripts/build-supplier-web.mjs, web/supplier/index.html, web/supplier/supplier.css, web/supplier/supplier.js, web/supplier/.gitignore</files>
  <read_first>
    - .planning/phases/38-supplier-link/38-UI-SPEC.md "## Color" CSS 변수 블록 · "## Component Inventory" · "### A-1" · "### A-2" · "### A-3" · "### A-7" · "## Interaction & Accessibility" · "## Registry Safety"(gstatic 버전 고정) · "## Decisions & Deviations Log"(+ "ui-checker flags (2026-09-26)" 표 — 적용값 정본)
    - .planning/phases/38-supplier-link/38-PATTERNS.md "### 후보 (2) `web/supplier.html`" 절(목업 CSS 토큰 발췌 + 플래너 메모) · "### 공급자 페이지 — 후보 (1)" 의 `authedJson`·`uploadToS3`·`normalize`·`onSnapshot` 발췌(vanilla 로 옮길 원문)
    - app/src/constants/supplierCopy.ts (38-03) · app/src/lib/api.ts :51-95 · :318-343 · app/src/lib/referenceMotions.ts :60-80 · :208-242 · app/src/lib/firebase.ts (config 키 이름 7개 `EXPO_PUBLIC_FIREBASE_*`) · app/.env (키 이름만 — 값은 config.js 생성에만)
    - app/package-lock.json `node_modules/firebase` version(12.13.0 — CDN 경로 버전) · app/src/lib/pickerFailure.test.ts :1-6 (Node 24 type stripping 근거)
    - .planning/phases/33-result-trust-recovery/mockups/index.html :1-20 · :110-175 (Pretendard 로드 · `:root` · `.card` 선례)
    - .planning/phases/38-supplier-link/38-04-MEASUREMENT.md 결정 절(`option-2` 확인 · `selfScoreMin` — `SELF_SCORE_OK_MIN` 값 출처, 없으면 90)
  </read_first>
  <action>
    Figma(D-22): 38-10 Task 2 와 같은 노드(`1:960` `1:977` `1:419~424` `1:717` `1:743` `1:646/663/713/709`) `get_design_context` → CSS 값 확정(SUMMARY 실측표).
    `app/scripts/export-supplier-copy.mjs`: `import('../src/constants/supplierCopy.ts')`(Node ≥ 23 type stripping — `as const` 만 있어 erasable) → `web/supplier/copy.js` 에 `export const supplierCopy = <JSON>;` 기록 + 헤더 주석 "generated — do not edit; source app/src/constants/supplierCopy.ts". `app/scripts/build-supplier-web.mjs`: copy 내보내기 → `app/.env` 의 `EXPO_PUBLIC_FIREBASE_*` 7키 + `EXPO_PUBLIC_API_BASE_URL` 를 읽어 `web/supplier/config.js`(`export const firebaseConfig = {...}; export const apiBaseUrl = '...';`; 값은 공개 웹 config — 시크릿 아님, CONFIG 절) → `web/supplier/dist/` 에 html/css/js/copy/config + `app/assets/motion-thumbs/ref-kip-up.jpg`·`ref-power-spin.jpg` + Pretendard Regular/Bold TTF(`app/assets/fonts/` 의 실제 파일명 확인) 복사 → `dist/` 목록 출력. `web/supplier/.gitignore`: `dist/`, `copy.js`, `config.js`.
    `index.html`: `lang="ko"` · `viewport-fit=cover` · `<meta name="referrer" content="no-referrer">` · `@font-face` Pretendard 2종 self-host · `<div id="app">` + `<template id="tpl-login">` … `tpl-noaccess` `tpl-home` `tpl-row` `tpl-fail` `tpl-done` `tpl-upload-step1` `tpl-upload-step2` `tpl-uploading` `tpl-guide` `tpl-dialog` `tpl-toast` — 텍스트 노드는 전부 비워 두고 JS 가 `textContent` 로 채운다(HTML 해석 없음 — `{joints}` 치환 안전).
    `supplier.css`: UI-SPEC §Color 의 `:root` 블록 그대로 + 컴포넌트 클래스(`.card` 15/1px divider/16 · `.pill-cta` 44/22 그라디언트 · `.cta` 54/13 · `.cta[aria-disabled]` #FFB7AD · `.outline-btn` 48/13 · `.google-btn` · `.row` 72 · `.step-card` dashed brand · `.notice-pill` · `.tip-card`(헤더-줄 8, 줄 간 4 — UI-SPEC §Decisions ui-checker flags 8행) · `.chip`(4 8 — flags 10행) · `.toast` · `.id-box` · `.code`(**#0C0C0C — 브랜드색 아님, flags 9행**) · `.progress` 8/4 · `:focus-visible` outline brand · `prefers-reduced-motion`). `max-width: 430px` 중앙, safe-area env().
    `supplier.js`(ESM): `import { initializeApp } from 'https://www.gstatic.com/firebasejs/12.13.0/firebase-app.js'`, `firebase-auth.js`(`getAuth, GoogleAuthProvider, signInWithPopup, linkWithPopup, signInWithCredential, onAuthStateChanged, signOut`), `firebase-firestore.js`(`getFirestore, collection, query, where, onSnapshot`) — **버전은 lockfile 의 firebase 와 같게**(다르면 SUMMARY). `import { supplierCopy } from './copy.js'`, `import { firebaseConfig, apiBaseUrl } from './config.js'`. 상태기계 `render(state)`: `boot → login(A-1) → checking → noAccess(A-2) → home(A-3)`; `probe()` = `fetch(apiBaseUrl+'/reference/upload-url', {method:'POST', headers:{'Content-Type':'application/json', Authorization:'Bearer '+await user.getIdToken()}, body:'{"probe":true}'})` → 403 noAccess / 401 login+sessionExpired / 200 `{supplierCode}`; 로그인 = `signInWithPopup`(익명이면 `linkWithPopup` → credential-already-in-use → `signInWithCredential`), 리다이렉트 금지, `auth/popup-blocked` → `login.popupBlocked`; 홈 = `onSnapshot(query(collection(db,'reference'), where('supplierUid','==',uid)))` 하나(Spark 캡 주석) → `normalizeRegistration`(supplierRules 와 같은 규칙을 vanilla 로 — `const SELF_SCORE_OK_MIN = <38-04-MEASUREMENT.md 결정 절 selfScoreMin, 기본 90>` 주석 ASSUMED; 이 이름 하나만, 리터럴 90 을 다른 곳에 두지 않는다) → 행 렌더(부제 상태어 `rowSubtitle`) · `queued` 있으면 `home.podDown` 알약 · 빈 상태 STEP 카드 · 내 코드 카드(사진 `photoURL`, 코드 `.code`, `코드 복사` → `navigator.clipboard.writeText` → 토스트 `common.copied` + 2초 `common.copiedShort`, 실패 → `common.copyFallback` + `user-select: all`) · `common.signOut` · `common.build`(빌드 스크립트가 `git rev-parse --short HEAD` 를 config.js 에 넣는다). 라우팅은 `location.hash`(`#home` `#detail=refId` `#upload` `#guide`) — 브라우저 뒤로가기 동작.
    로컬 스모크: `node app/scripts/build-supplier-web.mjs` → `python3 -m http.server 8083 --directory web/supplier/dist` → `curl -s localhost:8083/ | grep -c 'id="app"'` → 시뮬 Safari 스크린샷(A-1) → SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && node app/scripts/build-supplier-web.mjs >/dev/null && test -f web/supplier/dist/index.html && test -f web/supplier/dist/copy.js && node --check web/supplier/supplier.js && grep -q 'firebasejs/12.13.0' web/supplier/supplier.js && test "$(grep -c 'signInWithRedirect' web/supplier/supplier.js)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `node app/scripts/build-supplier-web.mjs` exits 0 and `web/supplier/dist/` contains `index.html supplier.css supplier.js copy.js config.js` + 2 jpg + Pretendard TTFs.
    - `node --check web/supplier/supplier.js` exits 0; `grep -c "firebasejs/12.13.0" web/supplier/supplier.js` ≥ 3 (app/auth/firestore) and the version equals `node_modules/firebase` in `app/package-lock.json`.
    - `grep -c "signInWithRedirect" web/supplier/supplier.js` == 0; `grep -c "innerHTML" web/supplier/supplier.js` == 0 (textContent 만).
    - `git status --porcelain` shows no `web/supplier/dist/`, `copy.js`, `config.js` (gitignored).
    - `grep -c "where('supplierUid'" web/supplier/supplier.js` == 1.
  </acceptance_criteria>
  <done>단일 페이지가 로그인 → 권한 확인 → 홈 두 카드까지 supplierCopy·CSS 변수로 렌더되고 빌드 산출물이 만들어진다.</done>
</task>

<task type="auto">
  <name>Task 2: 폼 STEP 01/02(체크 5 · 동작 정보 · 선언 3 · 파일 + 검증 다이얼로그 · 동의 필수 3/선택 1 · 철회 고지) → presign → XHR PUT 진행률/취소 → 결과</name>
  <files>web/supplier/index.html, web/supplier/supplier.css, web/supplier/supplier.js</files>
  <read_first>
    - .planning/phases/38-supplier-link/38-UI-SPEC.md "### A-4" 표 · "**STEP 02**" · "### A-5" · Copywriting `form.*` · `dialog` 표 · Component Inventory(Input · Select · 탭/세그먼트 · Checkbox row · 전체 동의 박스 · 선택 카드 · 안내 알약 · 검증 다이얼로그 · Progress bar) · Decisions 12·13·14
    - app/src/lib/pickerFailure.ts :30-72 (다이얼로그 문구 형상) · app/src/components/PickErrorDialog.tsx (1:499 규격 값) · app/src/types/analysis.ts (38-01 — `ReferenceUploadUrlRequest` 필드명 정확히)
    - .planning/phases/38-supplier-link/38-CONTEXT.md D-06 · D-07 · D-08 · D-09 · D-14
    - .planning/phases/38-supplier-link/38-PATTERNS.md "### 후보 (2) `web/supplier.html` (단일 정적 HTML)" 절(목업 CSS 토큰 발췌 + 플래너 메모) + "### 공급자 페이지 — 후보 (1) …" 절의 `validate`·`uploadToS3` 발췌(vanilla 로 옮길 원문) + Shared Patterns §11
  </read_first>
  <action>
    Figma(D-22): `1:960` `1:1064` `1:407` `1:399` `1:499` `1:482` `1:483` 실측 → CSS 값. 파일 카드 아이콘-텍스트 gap 16(UI-SPEC §Decisions ui-checker flags 11행).
    STEP 01(`#upload`): TIP 카드 `form.sec1.*` + 확인 체크(필수, 세션 유지 `sessionStorage`) + 링크 `#guide` · 동작 이름 `<select>`(옵션 = `GET /reference` 활성 동작 이름 중복 제거 — `fetch(apiBaseUrl+'/reference')` 실패 시 Firestore `reference` `where('isActive','==',true)` 대신 **입력만 허용**, 전수 스캔 금지) + `새 동작 (직접 입력)` → `<input maxlength=30>` + helper(D-11) · 콤보 체크 · 선수 이름(프리필 displayName) · 레벨 세그먼트(`role=radiogroup`) · 선언 3 세그먼트 + helper · `stand` 아니오 → `form.sec3.stand.blocked` + 비활성 유지 · 파일 카드 = `<input type="file" accept="video/mp4,video/quicktime,.mp4,.mov">` → `validateFile`(확장자 · 100MB · `<video preload=metadata>` duration 5~30/콤보 60, 못 읽으면 통과) → 실패 = 검증 다이얼로그(`role=dialog aria-modal`, 포커스 트랩, ESC/백드롭 = 닫기, `닫기`/`다른 파일 선택`) · 통과 = `<video muted playsinline controls>` 미리보기 + `form.sec4.meta` + `form.sec4.previewHint` · 하단 고정 바 `다음`(비활성 #FFB7AD + `aria-disabled` + `form.remaining {n}` `aria-live`).
    STEP 02: 전체 동의(= 필수 3 만) · 행 4(`[필수]`×3 · `[선택]` 학습 사용 기본 꺼짐 + 부연 + chevron `#guide` s5) · 철회 고지 · `올리기`(필수 3 전 비활성). 제출: `POST /reference/upload-url` body = `ReferenceUploadUrlRequest`(camelCase 필드명 정확히: `name athleteName level techniqueRefId isCombo isSplit hasHold standingStart consent{portrait,usage,silent,training} format fileSizeBytes durationSec`) → 401 `form.sessionExpired` → login · 403 → noAccess · 네트워크 → `common.offline`+`common.retry` · 5xx → 실패 패널 `form.presignFail` · 200 → 진행 패널(`role=progressbar` · `올리는 중 · {pct}%` · `form.uploading.keepOpen` · `올리기 취소` = `xhr.abort()` → STEP 02 복귀) → XHR PUT(`Content-Type` video/mp4|video/quicktime) → 성공 → `#home` + 토스트 `form.uploaded.toast` + 방금 올린 행 3초 강조 → 실패 → 실패 패널(`form.uploadFail.*`, retry = 새 presign 부터). `beforeunload` 경고. Firestore 쓰기 0.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && node --check web/supplier/supplier.js && test "$(grep -c 'XMLHttpRequest\|/reference/upload-url\|accept="video/mp4,video/quicktime' web/supplier/supplier.js web/supplier/index.html | awk -F: '{s+=$2} END {print s}')" -ge 3 && test "$(grep -c 'setDoc\|addDoc\|updateDoc' web/supplier/supplier.js)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check web/supplier/supplier.js` exits 0.
    - `grep -c "XMLHttpRequest" web/supplier/supplier.js` ≥ 1; `grep -c "/reference/upload-url" web/supplier/supplier.js` ≥ 2 (probe + presign); `grep -c 'accept="video/mp4,video/quicktime' web/supplier/index.html` == 1.
    - `grep -c "setDoc\|addDoc\|updateDoc" web/supplier/supplier.js` == 0 (페이지 Firestore 쓰기 0).
    - `grep -c "form.sec5.portrait\|form.sec5.usage\|form.sec5.silent\|form.sec5.training\|form.sec5.withdraw" web/supplier/supplier.js` == 5.
    - `grep -c "role=\"dialog\"\|role='dialog'\|role=\"progressbar\"" web/supplier/index.html web/supplier/supplier.js | awk -F: '{s+=$2} END {print s}'` ≥ 2.
  </acceptance_criteria>
  <done>폼 2단계와 업로드가 UI-SPEC 규칙·문구·접근성으로 있고 계약 필드명이 정확하다.</done>
</task>

<task type="auto">
  <name>Task 3: 실패 상세(A-3b) · 완료 상세(A-3c) · 가이드 뷰(A-6, 7섹션 + 사진 2 + 실패 4형 TIP + `#guide` 앵커) · `다시 올리기` 프리필 · 로컬 스모크 + 시뮬 스크린샷</name>
  <files>web/supplier/index.html, web/supplier/supplier.css, web/supplier/supplier.js</files>
  <read_first>
    - .planning/phases/38-supplier-link/38-UI-SPEC.md "### A-3b" · "### A-3c" · "### A-6" · Component Inventory "실패 패널" "완료 패널" "Guide image" · Copywriting `row.*` · `guide.*`
    - docs/supplier-guide.md (38-03 정본) · app/src/constants/supplierCopy.ts `guide` `row`
    - .planning/phases/38-supplier-link/38-PATTERNS.md "### 후보 (2) `web/supplier.html` (단일 정적 HTML)" 절 + "### 공급자 페이지 — 후보 (1) …" 절의 "가이드 화면 — 접힘 목록 + 쉬운 말 항목 데이터"(`help.tsx` 발췌) + "### `docs/supplier-guide.md` (신규) · docs 4종 정정" 절
  </read_first>
  <action>
    Figma(D-22): `1:428` `1:479` `1:482` `1:483` `1:486` `1:458` `1:498` `1:709` 실측(색은 라이트로).
    `#detail=refId`: `failed` → 실패 패널(원형 X 72 링 brand · 제목/본문 = `row.fail.{code}` + `{joints}` ` · ` 치환(`textContent`) · 코드 칩(`.chip` 4 8) · TIP 카드 `row.tipHead`+`row.tip` · CTA `row.reupload` → `#upload?prefill=refId`(이름·선수·레벨·선언 3 프리필) · `common.toList`); `active` → 완료 패널(원형 체크 72 `--c-success` · `row.done.title` · 재현성 줄 `row.self.*`(`{score}` 반올림, 점수 숫자 `--c-brand` heading) · 정보 표 · `selfScore < SELF_SCORE_OK_MIN`(Task 1 의 상수 — 리터럴 금지) 면 TIP + `다시 올리기` · `common.toList`). `#guide`: `guide.title/sub` + 섹션 7 카드(`guide.sN.h` + items `· `) + ① 사진 2(`ref-kip-up.jpg` `ref-power-spin.jpg`, 9:16 50%−6, 캡션) + ④ TIP(`guide.s4.tipHead` + 4줄) + `#guide-s5` 앵커(학습 사용 chevron). 로그인 불요.
    로컬 스모크: 빌드 → `python3 -m http.server 8083 --directory web/supplier/dist` → 시뮬 Safari `http://localhost:8083/#guide` 스크린샷(로그인 불요) + `/` (A-1) → SUMMARY 경로. 로그인 필요 뷰는 38-13 belle(`[미확인]`).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && node app/scripts/build-supplier-web.mjs >/dev/null && node --check web/supplier/supplier.js && test "$(grep -c 'guide.s1\|guide.s7\|row.done.title\|row.reupload' web/supplier/supplier.js)" -ge 4 && test -f web/supplier/dist/ref-kip-up.jpg && test "$(grep -c '< 90\|<90\|>= 90\|>=90' web/supplier/supplier.js)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check web/supplier/supplier.js` exits 0; build exits 0 and `dist/` includes both example jpgs.
    - `grep -c "guide.s1\|guide.s7\|row.done.title\|row.reupload" web/supplier/supplier.js` ≥ 4; `grep -c "innerHTML" web/supplier/supplier.js` == 0.
    - `grep -c "#guide" web/supplier/supplier.js` ≥ 2 (링크 + 라우팅).
    - `grep -c "< 90\|<90\|>= 90\|>=90" web/supplier/supplier.js` == 0 and `grep -c "SELF_SCORE_OK_MIN" web/supplier/supplier.js` ≥ 2 (정의 + 사용 — 이름 하나, 값은 38-04 결정 절).
    - SUMMARY has the simulator screenshot path for `#guide` and `/` (`[확인]`) and states "같은 앱 문자 그대로 아님(후보 2)" explicitly.
  </acceptance_criteria>
  <done>상세 두 패널과 가이드 뷰까지 단일 페이지가 완성되고 로컬에서 렌더된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gstatic CDN → 브라우저 | Firebase SDK 원격 로드(SRI 미제공) |
| 브라우저 → API/S3/Firestore | 토큰 · presigned URL · 읽기 구독 |
| 사용자 파일 → 페이지 | 임의 파일 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-12-1 | Tampering | CDN ESM | accept | gstatic 고정 버전(12.13.0, lockfile 과 동일), SRI 미제공 [확인 UI-SPEC] — 자체 호스팅은 범위 밖, SUMMARY 에 기록 |
| T-38-12-2 | Tampering | 문구 치환 | mitigate | `textContent` 만(`innerHTML` 0건 acceptance) |
| T-38-12-3 | Spoofing | 로그인 | mitigate | Firebase 팝업 API 만, 리다이렉트 금지 |
| T-38-12-4 | Information disclosure | config.js | accept | Firebase 웹 config·API URL 은 공개값(보안은 규칙·토큰), gitignore 로 커밋 방지 |
| T-38-12-5 | Elevation | 페이지 쓰기 | mitigate | Firestore 쓰기 0(acceptance grep), 모든 쓰기는 Lambda/Pod |
| T-38-12-6 | DoS | 파일 | mitigate | 확장자·100MB·5~30초(콤보 60) 즉시 검사 + 서버 2차 |
| T-38-SC | Tampering | npm installs | accept | 설치 0 |
</threat_model>

<verification>
- 빌드 스크립트 exit 0 · `node --check` · acceptance grep 전부 · 플랜 끝 push.
</verification>

<success_criteria>
- 단일 페이지가 A-1~A-7 상태기계·supplierCopy·CSS 변수로 완성되고 `web/supplier/dist/` 가 38-13 배포 입력이 된다.
</success_criteria>

<output>
Create `.planning/phases/38-supplier-link/38-12-SUMMARY.md` when done.
</output>
