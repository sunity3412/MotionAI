---
phase: 38-supplier-link
plan: 13
type: execute
wave: 6
depends_on: ["38-09", "38-11"]
files_modified:
  - .planning/phases/38-supplier-link/infra/cloudfront-supplier.json
  - .planning/phases/38-supplier-link/infra/supplier-bucket-policy.json
  - .planning/phases/38-supplier-link/38-13-DEPLOY.md
  - app/app.json
autonomous: false
requirements: [REQ-38-3, REQ-38-5, REQ-38-8]
must_haves:
  truths:
    - "D-01: 링크 한 장(`https://<cloudfront-domain>/supplier` 또는 `/`)이 HTTPS 로 열리고 belle 의 실기기 폰 브라우저에서 Google 팝업 로그인 → 홈 두 카드 → 올리기 폼 → 가이드가 동작한다"
    - "D-03: 배포 도메인이 Firebase Authorized domains 에 등록돼 팝업 로그인이 된다(belle 콘솔 1회 — CLI 없음)"
    - "D-22: belle 이 Figma 스크린샷(`get_screenshot` 1:717 · 1:960 · 1:646 · 1:1064 · 1:499) 과 실기기 화면을 나란히 보고 승인한다"
    - "인프라(새 S3 버킷 + CloudFront OAC)는 명령·롤백이 기록되고 비용은 센트 단위 + CloudFront Free plan 이다"
  artifacts:
    - path: ".planning/phases/38-supplier-link/38-13-DEPLOY.md"
      provides: "배포 명령 원문 · 도메인 · 검증 · 롤백 · belle ○/× 표"
      min_lines: 40
    - path: ".planning/phases/38-supplier-link/infra/cloudfront-supplier.json"
      provides: "CloudFront distribution config (OAC · SPA 403/404→index.html · redirect-to-https)"
      contains: "DefaultRootObject"
  key_links:
    - from: "CloudFront distribution"
      to: "S3 sunity-motion-pilot-supplier-web (OAC)"
      via: "bucket policy AWS:SourceArn = distribution ARN"
      pattern: "cloudfront.amazonaws.com"
---

<objective>
공급자 페이지를 HTTPS 로 올리고(새 정적 버킷 + CloudFront OAC — 영상 버킷과 분리), Firebase Authorized domain 등록(사람만 가능) 뒤 belle 이 **실기기 폰 브라우저**로 A-1~A-6 을 검증한다(D-01·D-03·D-22). 배포 산출물은 38-04 결정에 따라 `app/dist`(후보 1, 38-10/38-11) 또는 `web/supplier/dist`(후보 2, 38-12) — **이 플랜은 어느 쪽이든 같다.** 이 플랜의 frontmatter `depends_on` 은 38-04 Task 3 (c) 가 belle 결정에 맞춰 확정한다(option-1 = `["38-09", "38-11"]`, option-2 = `["38-09", "38-12"]`) — 실행자는 frontmatter 를 그대로 믿으면 되고, 미선택 트랙은 `skipped-by-decision` SUMMARY 로 이미 닫혀 있다.

Purpose: Google OAuth 팝업은 HTTPS + Authorized domain 이 있어야 실기기에서 된다(RESEARCH §D·A3). 38-14 Pod E2E 는 이 링크로 정은지가 올리는 것을 전제한다.
Output: 배포 기록 `38-13-DEPLOY.md`(명령·도메인·검증·롤백·belle 표), CloudFront/버킷 정책 JSON(리포에 커밋), belle 승인.

Phase 38 전제(38-01 머리말) 적용. `AWS_PROFILE=sunity-motion`; CloudFront/S3 생성 권한은 [미확인] — AccessDenied 면 그 명령에서 멈추고 기록(auth gate). OTA/EAS 는 범위 밖(앱 네이티브 배포 없음).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/38-supplier-link/38-CONTEXT.md
@.planning/phases/38-supplier-link/38-RESEARCH.md
@.planning/phases/38-supplier-link/38-UI-SPEC.md
@.planning/phases/38-supplier-link/38-04-MEASUREMENT.md
@.planning/phases/38-supplier-link/38-09-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 산출물 빌드(결정에 따라) → 정적 버킷 + CloudFront(OAC · SPA 폴백 · HTTPS) 생성 → sync → 무효화 → 도메인·검증·롤백 기록</name>
  <files>.planning/phases/38-supplier-link/infra/cloudfront-supplier.json, .planning/phases/38-supplier-link/infra/supplier-bucket-policy.json, .planning/phases/38-supplier-link/38-13-DEPLOY.md, app/app.json</files>
  <read_first>
    - .planning/phases/38-supplier-link/38-04-MEASUREMENT.md "결정" 절(option-1/option-2) · 38-10/38-11 SUMMARY(후보 1) 또는 38-12 SUMMARY(후보 2)
    - .planning/phases/38-supplier-link/38-RESEARCH.md "### H." 표(배포 절차 열 · 비용) · "### Q1"(리전 · 계정 976369350031 · `sunity-motion` 프로파일) · "### Q8"(`web.output` 기본 single)
    - app/app.json (`web` 키 — 후보 1 이면 `"web": {"output": "single", "favicon": ...}` 명시 + `extra.commitSha` 주입 여부) · app/.gitignore (`dist/`)
    - backend/README.md :70-112 (버킷 명령 어법)
  </read_first>
  <action>
    1. 빌드: 후보 1 → `cd app && CI=1 npx expo export --platform web --output-dir dist`(`app.json` `web.output` 이 없으면 `single` 명시 — SPA 라 CloudFront 403/404 → `/index.html` 폴백 필수; 링크 = `/supplier`); 후보 2 → `node app/scripts/build-supplier-web.mjs`(링크 = `/`). 산출 디렉터리 크기 `du -sh` 기록.
    2. 버킷(읽기 먼저 — 멱등): `aws s3api head-bucket --bucket sunity-motion-pilot-supplier-web --profile sunity-motion` 이 200 이면 생성을 건너뛰고 "기존 버킷 재사용" 을 DEPLOY.md 에 적는다(404 일 때만 아래 create; 403 = 남의 소유 → 멈추고 기록). `aws s3api create-bucket --bucket sunity-motion-pilot-supplier-web --region ap-northeast-2 --create-bucket-configuration LocationConstraint=ap-northeast-2 --profile sunity-motion` → `put-public-access-block`(4개 전부 true) → 확인 `get-bucket-location`.
    3. CloudFront(읽기 먼저 — 멱등): `aws cloudfront list-origin-access-controls --query "OriginAccessControlList.Items[?Name=='sunity-supplier-oac'].Id" --output text --profile sunity-motion` 에 값이 있으면 그 Id 를 재사용하고, `aws cloudfront list-distributions --query "DistributionList.Items[?Comment=='Phase 38 supplier link'].[Id,DomainName]" --output text --profile sunity-motion` 에 값이 있으면 create-distribution 대신 그 배포를 쓴다(`get-distribution-config` 로 아래 설정과 같은지 대조, 다르면 `update-distribution --if-match`) — 둘 다 DEPLOY.md 에 "재사용" 표시. 없을 때만: `aws cloudfront create-origin-access-control`(name `sunity-supplier-oac`, S3 · sigv4 · always) → `infra/cloudfront-supplier.json` 작성(Origins = 버킷 REST 엔드포인트 `sunity-motion-pilot-supplier-web.s3.ap-northeast-2.amazonaws.com` + `OriginAccessControlId` · `DefaultRootObject: index.html` · `DefaultCacheBehavior`: `ViewerProtocolPolicy redirect-to-https`, `CachePolicyId 658327ea-f89d-4fab-a63d-7e88639e58f6`(CachingOptimized), `Compress true`, allowed GET/HEAD · `CustomErrorResponses`: 403→`/index.html` 200 · 404→`/index.html` 200 · `PriceClass_All` · `HttpVersion http2and3` · `Comment "Phase 38 supplier link"`) → `aws cloudfront create-distribution --distribution-config file://…` → `Id`·`ARN`·`DomainName` 기록 → `infra/supplier-bucket-policy.json`(Principal `cloudfront.amazonaws.com`, Action `s3:GetObject`, Resource `arn:aws:s3:::sunity-motion-pilot-supplier-web/*`, Condition `AWS:SourceArn` = distribution ARN) → `put-bucket-policy` → `aws s3 sync <dist> s3://sunity-motion-pilot-supplier-web --delete --profile sunity-motion` → `aws cloudfront wait distribution-deployed --id <Id>` → `create-invalidation --paths "/*"`.
    4. 검증: `curl -sI https://<DomainName>/` → 200 + `content-type: text/html`; `curl -sI https://<DomainName>/supplier`(후보 1) → 200(SPA 폴백); `curl -s https://<DomainName>/ | grep -c 'id="root"\|id="app"'` ≥ 1; 시뮬 Safari 로 열어 스크린샷(로그인 전 A-1 — Authorized domain 전이라 로그인은 안 된다).
    5. `38-13-DEPLOY.md`: 명령 원문 · 리소스 id/ARN/도메인 · 검증 출력 · 비용 근거(S3 저장 MB 단위 + CloudFront Free plan 1M req/100GB [CITED RESEARCH]) · **롤백**: `aws cloudfront get-distribution-config` → `Enabled:false` `update-distribution` → `wait distribution-deployed` → `delete-distribution --if-match`, `aws s3 rb s3://sunity-motion-pilot-supplier-web --force`, OAC 삭제. 다음 태스크가 belle 에게 보여 줄 "Authorized domains 에 넣을 도메인" 한 줄 굵게. `app/app.json` 변경이 있었으면 커밋. Figma `get_screenshot` 을 `1:717` `1:960` `1:646` `1:1064` `1:499` 에 실행해 scratchpad 에 저장(코드에 넣지 않는다) — Task 3 대조용 경로를 DEPLOY.md 에.
    AccessDenied → 그 단계에서 멈추고 필요한 IAM 액션 이름을 DEPLOY.md 에 적는다(가짜 성공 금지).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && test -f .planning/phases/38-supplier-link/38-13-DEPLOY.md && test -f .planning/phases/38-supplier-link/infra/cloudfront-supplier.json && grep -q 'DomainName\|cloudfront.net' .planning/phases/38-supplier-link/38-13-DEPLOY.md && D=$(grep -o '[a-z0-9]*\.cloudfront\.net' .planning/phases/38-supplier-link/38-13-DEPLOY.md | head -1) && test -n "$D" && curl -sI "https://$D/" | grep -qi '^HTTP/[0-9.]* 200'</automated>
  </verify>
  <acceptance_criteria>
    - `curl -sI https://<domain>/` returns HTTP 200 with `content-type: text/html`; for option-1 `curl -sI https://<domain>/supplier` also 200.
    - `aws s3api get-public-access-block --bucket sunity-motion-pilot-supplier-web` shows all four true; bucket policy principal is `cloudfront.amazonaws.com` with `AWS:SourceArn` condition.
    - `infra/cloudfront-supplier.json` contains `DefaultRootObject`, two `CustomErrorResponses` (403, 404 → `/index.html`), `redirect-to-https`.
    - `38-13-DEPLOY.md` has sections `명령` · `검증` · `롤백` · `비용` and the bold Authorized-domain line; Figma screenshot paths listed.
    - DEPLOY.md records the read-before results (`head-bucket` HTTP status, OAC lookup output, distribution lookup output) so a re-run creates nothing twice; after the task `list-distributions` shows exactly one distribution with Comment `Phase 38 supplier link`.
  </acceptance_criteria>
  <done>HTTPS 링크가 살아 있고 되돌릴 수 있는 기록이 있다.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: belle — Firebase 콘솔 Authorized domains 에 배포 도메인 추가(CLI 없음)</name>
  <files>none — 체크포인트(콘솔 전용 사람 작업). 결과는 38-13-DEPLOY.md</files>
  <action>
    실행자: `38-13-DEPLOY.md` 의 굵은 도메인 한 줄을 belle 에게 제시하고 멈춘다(CLI 로 할 수 없는 일 — Identity Toolkit REST 는 belle 계정 로그인이 필요). `done` 뒤 `curl -sI https://<domain>/` 200 을 다시 확인하고 DEPLOY.md 에 `Authorized domain 등록: [확인 belle] <시각>` 을 적는다.
  </action>
  <verify>
    <automated>grep -q 'Authorized domain 등록' /Users/kimtaesung/Dev/SunityMotion/.planning/phases/38-supplier-link/38-13-DEPLOY.md</automated>
  </verify>
  <acceptance_criteria>
    - `38-13-DEPLOY.md` 에 `Authorized domain 등록: [확인 belle]` 줄과 belle 의 응답 원문이 있다.
  </acceptance_criteria>
  <done>배포 도메인이 Firebase Authorized domains 에 등록됐다고 belle 이 확인했다.</done>
  <what-built>페이지가 `https://<cloudfront-domain>` 에 떠 있다(Task 1). Google 팝업 로그인은 Firebase 가 허용한 도메인에서만 된다 [CITED RESEARCH Q7 google-signin]. 이 설정은 콘솔 전용이라 belle 손이 필요하다(gcloud 로그인이 돼 있으면 Identity Toolkit `projects.updateConfig` REST 로도 되지만 그 로그인 자체가 belle 계정이다).</what-built>
  <how-to-verify>
    1. Firebase 콘솔(`sunity-ai-coach`, 계정 sunity3412) → Authentication → Settings → Authorized domains → "Add domain" → `38-13-DEPLOY.md` 의 굵은 도메인(`xxxx.cloudfront.net`) 추가.
    2. Google 제공자가 켜져 있는지(앱이 이미 쓴다) 확인.
    3. 추가 뒤 "done" 이라고 알려 주면 실행자가 `curl` 로 페이지를 다시 열고 Task 3 로 넘어간다.
  </how-to-verify>
  <resume-signal>"done" (도메인 추가 완료) 또는 "blocked: <이유>"</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: belle — 실기기 폰 브라우저에서 A-1 → A-3 → A-4/A-5 → A-6 검증 + Figma 대조(D-22)</name>
  <files>none — 체크포인트(실기기 검증). 결과는 38-13-DEPLOY.md belle ○/× 표</files>
  <action>
    실행자: `38-13-DEPLOY.md` 에 링크 · Figma `get_screenshot` 경로(1:717 · 1:960 · 1:646 · 1:1064 · 1:499) · 기대 화면 순서(how-to-verify 1~6)를 적어 두고 belle 에게 제시한 뒤 멈춘다. ×가 오면 그 항목을 고쳐 `aws s3 sync … --delete` + `create-invalidation` 으로 재배포하고 같은 항목만 다시 묻는다(재배포 횟수·커밋 sha 기록). belle 의 ○/× 와 문구 원문을 DEPLOY.md 표에 `[확인 belle]` 로 옮긴다. A-2(다른 계정) 는 belle 에게 두 번째 계정이 없으면 `[미확인 — 38-14]` 로 둔다.
  </action>
  <verify>
    <automated>grep -q '확인 belle' /Users/kimtaesung/Dev/SunityMotion/.planning/phases/38-supplier-link/38-13-DEPLOY.md</automated>
  </verify>
  <acceptance_criteria>
    - `38-13-DEPLOY.md` 에 6단계 ○/× 표가 `[확인 belle]` 표식과 belle 문구 원문으로 있다.
    - 재배포가 있었으면 횟수·sha·무효화 id 가 기록돼 있다.
    - belle 최종 응답이 `approved` 이거나, 미해결 항목이 SUMMARY 에 `[미확인]` 으로 명시돼 있다.
  </acceptance_criteria>
  <done>belle 이 실기기에서 페이지 전체를 보고 Figma 대조까지 승인했다.</done>
  <what-built>공급자 링크 페이지 전체(로그인 · 권한 확인 · 홈 두 카드 · 올리기 폼 2단계 · 업로드 진행 · 가이드) 가 HTTPS 도메인에 배포됐고 라이브 API(38-09)와 연결돼 있다. belle uid 는 `BELLE_UID` 로 화이트리스트라 홈이 바로 보인다(`내 코드` = `BELLE`, SSM 초기값). Pod 은 꺼져 있으므로 올린 동작은 `대기 중` 이 정상이다(D-20).</what-built>
  <how-to-verify>
    실행자가 먼저 `38-13-DEPLOY.md` 에 링크·Figma 스크린샷 경로·기대 화면 순서를 적어 둔다. belle 은 아이폰 Safari 로:
    1. 링크 열기 → `시작해 볼까요?` + `Google로 시작하기`(A-1). 팝업 → 로그인 → 홈(A-3): 상단 `공급자 페이지`, `내 동작` 카드에 `STEP 1 첫 동작을 올려보세요` + `동작 올리기 >` 알약, `내 코드` 카드에 `BELLE` + `코드 복사` → 토스트 `복사됐어요`.
    2. `촬영 가이드` 링크 → A-6 7섹션 + 정은지 프레임 2장 → 뒤로.
    3. `동작 올리기 >` → STEP 01: 체크 5 확인 · 동작 이름(사전 목록 + 새 동작) · 선수 이름 · 레벨 · 선언 3 · `앨범에서 선택` → 3초짜리 짧은 영상 → `영상이 너무 짧아요` 다이얼로그(`닫기`/`다른 파일 선택`) → 5~30초 영상 → 미리보기 → `다음` 활성.
    4. STEP 02: 필수 3 체크 전 `올리기` 비활성 + `필수 항목 N개가 남았어요` → 전체 동의 → `올리기` → 진행 막대 → 토스트 `동작을 올렸어요…` → 홈에 새 행 `올린 영상 확인 중` → 잠시 뒤 `대기 중` + `분석 서버가 꺼져 있어요.` 알약(Pod down 정상).
    5. 다른 Google 계정이 있으면 `다른 계정으로 로그인` → 그 계정 → `아직 등록된 공급자가 아니에요` + 내 ID 박스 + `ID 복사`(A-2). (없으면 "미확인"으로 남긴다 — 38-14 에서 정은지 계정이 이 화면을 본다.)
    6. Figma 대조: 실행자가 준 스크린샷(1:717 홈 · 1:960 폼 · 1:646 선수 카드 · 1:1064 동의 · 1:499 다이얼로그) 과 실기기 화면을 나란히 — 색·반경·타이포·문구가 Figma 와 같은가. 다르면 어느 화면 어느 요소인지.
    각 단계 ○/× + 본 문구 그대로. ×는 실행자가 고치고 재배포(sync + invalidation) 후 다시 묻는다.
  </how-to-verify>
  <resume-signal>"approved" 또는 단계별 ○/× 와 문제 목록</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 인터넷 → CloudFront → S3(OAC) | 공개 정적 페이지(무인증 읽기) |
| 페이지 → Firebase Auth(Authorized domain) | OAuth 팝업 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-13-1 | Information disclosure | 정적 버킷 | mitigate | Public access block 전부 true, OAC + SourceArn 조건 정책만 |
| T-38-13-2 | Spoofing | OAuth 도메인 | mitigate | belle 이 콘솔에 정확한 CloudFront 도메인만 추가(와일드카드 금지) |
| T-38-13-3 | Tampering | 배포 산출물 | mitigate | `aws s3 sync --delete` + invalidation, 산출 디렉터리 크기·커밋 sha 기록 |
| T-38-13-4 | DoS | CloudFront 비용 | accept | Free plan 1M req/100GB, 공급자 1~2명 |
| T-38-13-5 | Elevation | belle 테스트 업로드 | accept | 화이트리스트 uid 의 정상 등록(Pod down → queued), 정은지 계정과 분리 |
| T-38-SC | Tampering | npm installs | accept | 설치 0 |
</threat_model>

<verification>
- `curl -sI https://<domain>/` 200 · DEPLOY.md 롤백 절 · belle "approved".
- 플랜 끝 push.
</verification>

<success_criteria>
- belle 이 실기기에서 링크 → 로그인 → 두 카드 → 폼 → 대기 중 행 → 가이드를 보고 Figma 대조까지 승인했다.
</success_criteria>

<output>
Create `.planning/phases/38-supplier-link/38-13-SUMMARY.md` when done (belle ○/× 표 원문 인용, 미확인 = A-2 여부).
</output>
