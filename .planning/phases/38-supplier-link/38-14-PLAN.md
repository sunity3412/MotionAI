---
phase: 38-supplier-link
plan: 14
type: execute
wave: 7
depends_on: ["38-13"]
files_modified:
  - .planning/phases/38-supplier-link/38-14-E2E.md
  - .planning/phases/38-supplier-link/infra/legacy-reference-after-requeue.json
  - .planning/phases/38-supplier-link/infra/legacy-reference-after-e2e.json
autonomous: false
requirements: [REQ-38-2, REQ-38-4]
must_haves:
  truths:
    - "Success ① (리뷰 R15e): **belle 이 아닌 사람**(정은지 또는 다른 실제 사람)이 링크만으로 새 동작 1개를 올려 앱 picker 에 뜨고, 그 동작으로 mode1 분석이 `done` 으로 끝난다 — Pod 로그에 `기준 모션 또는 keyframe 데이터 없음` 0건. belle 의 다른 Google 계정(대역)은 **구조 검증**(`structural_check_stand_in`)만 채우고 Success ① 을 완료로 만들지 않는다 — E2E.md 에 두 줄이 따로 있다"
    - "Success ③ / D-10 (리뷰 R11): 등록 직후 자기 영상 재분석 `selfScore` 가 기준 doc 에 숫자로 남고 보고된다 — 의미는 '추출·저장·재분석이 일관되는가' 의 진단이지 동작 정확도가 아니다(D-11: 정확도는 시험 영상으로); 통과선은 belle 이 정한다(목표 숫자 금지; 페이지 문턱은 [ASSUMED]); 점수는 같은 v1 객체·같은 jobId 로 이어진 화면 점수(`result.overallScore`)만 보고한다"
    - "Success ④ / D-19 (리뷰 R10): 38-09 가 배포 **전**에 찍은 raw baseline(`infra/legacy-reference-baseline.json` — angles sha256·jointKeys·frames·clipRange·isActive·버전 포인터·S3 ETag·저장 분석 표본 3)과 첫 requeue 뒤·E2E 뒤 두 번의 재diff 가 모두 0 이고, 새 자기 재현성 분석 id 가 표본 3건과 겹치지 않는다"
    - "D-20 / D-03: Pod 기동 전에 올린 등록(`queued`)이 `requeue_reference_registrations.py` 로 처리되고, 정은지 uid:CODE 가 SSM 에 추가돼 배포 없이 화이트리스트가 확장된다"
    - "D-21: 사람 영상은 manifest.json 에 넣지 않는다; Pod 은 GPU 한 종류로(4090 우선, 없으면 L4 — 섞지 않는다) 켜고 끝나면 내린다(pod-expected=down)"
  artifacts:
    - path: ".planning/phases/38-supplier-link/38-14-E2E.md"
      provides: "Pod 종류·commitSha·링크 속도·requeue 결과·등록 doc 상태 전이·picker 캡처·mode1 분석 doc·selfScore·baseline diff·teardown — 전부 명령 원문 + [확인]"
      min_lines: 60
    - path: ".planning/phases/38-supplier-link/infra/legacy-reference-after-requeue.json"
      provides: "첫 requeue 뒤 재스냅샷(38-09 baseline 과 diff 0)"
      contains: "ref-"
    - path: ".planning/phases/38-supplier-link/infra/legacy-reference-after-e2e.json"
      provides: "E2E 뒤 재스냅샷(diff 0) + 새 selfCheck analysis id 비겹침"
      contains: "ref-"
  key_links:
    - from: "requeue_reference_registrations.py"
      to: "Pod /register-reference"
      via: "queued doc 마다 POST"
      pattern: "register-reference"
    - from: "reference/{refId}.selfScore"
      to: "users/{uid}/analyses/{selfCheckAnalysisId}.result.overallScore"
      via: "38-08 훅"
      pattern: "selfScore"
---

<objective>
Pod 를 켜서 끝까지 잰다 — 정은지(또는 대역) uid 화이트리스트 추가 → Pod 기동(GPU 한 종류) → `queued` 재개(claim 경유) → 새 동작 등록 → 앱 picker → mode1 완주(Success ①) → selfScore 보고(Success ③) → legacy 11개 무접촉 증명(Success ④ — 38-09 배포 전 baseline 대비 재diff 2회) → teardown. 이 플랜은 체크포인트 2개(사람 손) + 자동 2개로, 체크포인트는 문맥 비용이 거의 0 이라 4 task 로 둔다.

**리뷰 반영(2026-09-26):** R10(baseline 은 38-09 가 배포 전에 찍었다 — 여기서는 재diff 만, requeue 뒤 + E2E 뒤) · R11(selfScore 는 재분석 일관성 진단으로 보고, 같은 v1 객체·같은 jobId 로 이어진 화면 점수만) · R15e(대역 계정 = 구조 검증, Success ① 은 belle 아닌 사람 완주 때만 — E2E.md 두 줄 분리) · R5(재PUT 불변 실물 확인 1건).

**Pod 규칙(메모리):** belle 의 `pod:go <GPU>` 토큰(Task 1, 단가 조회값 포함) 없이는 켜지 않는다(공용 계정 · 유료 · 다른 프로젝트 Pod 무접촉) · 시연 때만 · 켜기 전 push 필수(`git log origin/main..HEAD` 비어 있어야) · 기동 직후 링크 속도(1MB/s 미만이면 교체; 가속 켜져 있음) · `RUNPOD_POD_ID` export 없이 ssh 기동하면 주소 동기가 안 된다 · 끝나면 `pod_teardown.py`(공용 계정 — 전체 종료 금지) · **GPU 종류 하나**(같은 영상도 종류가 다르면 각도가 ~20° 다르다) · Pod 로그는 재기동 전에 내려받는다.
**인터럽트:** 정은지 새 영상이 오면 이 플랜보다 `sealed_test.py`(시험 영상 2차)가 먼저다 — 같은 Pod 세션에서 먼저 돌린다. 사람 영상은 `manifest.json` 금지(D-21).
**보고:** 숫자(selfScore·점수)는 소비처(`result.overallScore` = 화면 점수)까지 따라가 적는다. 관측과 진단을 다른 절에.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/38-supplier-link/38-CONTEXT.md
@.planning/phases/38-supplier-link/38-RESEARCH.md
@.planning/phases/38-supplier-link/38-13-SUMMARY.md
@.planning/phases/38-supplier-link/38-08-SUMMARY.md
@/Users/kimtaesung/.claude/projects/-Users-kimtaesung-Dev-SunityMotion/memory/demo-only-pod-bring-up-procedure.md
@/Users/kimtaesung/.claude/projects/-Users-kimtaesung-Dev-SunityMotion/memory/pod-link-can-collapse-silently.md
@/Users/kimtaesung/.claude/projects/-Users-kimtaesung-Dev-SunityMotion/memory/gpu-type-changes-pose-angles.md
@/Users/kimtaesung/.claude/projects/-Users-kimtaesung-Dev-SunityMotion/memory/runpod-account-is-shared-across-projects.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: 정은지(또는 belle 이 아닌 대역 계정) 가 링크로 로그인 1회 → 내 ID 를 belle 에게 → belle 이 `uid:CODE` 를 준다(닭-달걀)</name>
  <files>none — 체크포인트(사람 손). 결과는 38-14-E2E.md 관측 절(uid 마스킹)</files>
  <action>
    실행자: 38-13 이 준 링크와 how-to-verify 3항목을 belle 에게 제시하고 멈춘다. 응답의 `uid:<uid> code:<CODE>` 를 파싱해 `CODE` 가 `^[A-Z0-9]{3,8}$` 인지 확인(아니면 다시 묻는다), `videos:` 가 있으면 Task 2 에서 시험 영상 2차를 먼저 돌리도록 E2E.md 머리에 적는다. 대역 계정이면 E2E.md 머리에 두 줄을 따로 둔다(리뷰 R15e): `structural_check_stand_in: <대역 uid 앞4자> (belle 의 다른 Google 계정)` 과 `success_1_independent_user: [미확인 — belle 아닌 사람 미완주]` — 정은지(또는 다른 실제 사람)가 완주하면 그때만 둘째 줄을 `[확인 — <사람>, <시각>]` 으로 바꾼다. uid 원문은 SSM 에만, 문서에는 앞 4자. 체크포인트를 띄우기 **전에** RunPod GraphQL(`gpuTypes { id displayName lowestPrice(input:{gpuCount:1}) { uninterruptablePrice } }`, UA 헤더 필수 — 메모리 runpod-graphql-blocks-urllib-user-agent)로 4090·L4 의 현재 온디맨드 단가를 조회해 how-to-verify 4 의 예상 비용 한 줄을 실값으로 채운다(조회 실패 = 콘솔 단가를 belle 에게 묻는다, 추측 금지). 응답의 `pod:go <4090|L4>` 를 E2E.md 머리에 `pod_consent: <GPU> (belle, <시각>, 단가 <조회값>/h × 예상 <n>h)` 로 적는다 — 이 토큰이 없으면 다시 묻는다(Pod 기동 동의 없이 Task 2 진행 금지).
  </action>
  <verify>
    <automated>test -f /Users/kimtaesung/Dev/SunityMotion/.planning/phases/38-supplier-link/38-14-E2E.md && grep -q 'code:' /Users/kimtaesung/Dev/SunityMotion/.planning/phases/38-supplier-link/38-14-E2E.md && grep -q 'pod_consent:' /Users/kimtaesung/Dev/SunityMotion/.planning/phases/38-supplier-link/38-14-E2E.md</automated>
  </verify>
  <acceptance_criteria>
    - `38-14-E2E.md` 에 uid(앞 4자 마스킹)·CODE·`videos:` 유무와 두 줄 `structural_check_stand_in:` / `success_1_independent_user:` 가 기록돼 있다(R15e).
    - CODE 가 `^[A-Z0-9]{3,8}$` 를 만족한다.
    - `38-14-E2E.md` has a `pod_consent:` line with the GPU type belle chose (`4090` or `L4`) and the quoted hourly price used for the estimate (조회 명령 원문 포함); without it Task 2 must not start a Pod.
  </acceptance_criteria>
  <done>정은지(대역) uid 와 코드가 belle 로부터 왔고 E2E 기록이 시작됐다.</done>
  <what-built>링크 페이지(38-13)와 라이브 API 가 있다. 화이트리스트는 SSM `/sunity/motion/supplier-uids`(런타임 읽기, 60초 캐시)라 배포 없이 uid 를 더할 수 있다. 화이트리스트 밖 계정은 A-2 화면에서 자기 uid 를 보고 복사할 수 있다.</what-built>
  <how-to-verify>
    1. 정은지에게 링크를 보낸다 → 폰 Safari → `Google로 시작하기` → `아직 등록된 공급자가 아니에요` 화면의 `내 ID` 를 `ID 복사` 로 복사해 belle 에게 보낸다.
    2. belle 이 코드를 정한다(예 `EUNJI`, 대문자·숫자 3~8자). 정은지가 지금 없으면 belle 의 **다른** Google 계정을 대역으로 쓴다 — 단 그것은 **구조 검증**일 뿐이다(같은 사람; Success ① "belle 이 아닌 사람이 링크만으로" 는 정은지 또는 다른 실제 사람이 완주해야 완료로 적는다, 리뷰 R15e).
    3. 새 영상이 이미 도착해 있으면 여기서 알려 준다 — 시험 영상 2차가 먼저다.
    4. **Pod 기동 승인(공용 RunPod 계정 — 유료):** 실행자가 적어 둔 "예상 비용 = <GPU> 단가 <조회값>/h × 예상 2~3시간(시험 영상 2차 포함 시 +1시간)" 을 보고 `pod:go 4090` 또는 `pod:go L4` 로 허락한다. 이 토큰 없이는 Pod 을 켜지 않는다. 다른 프로젝트의 Pod 은 절대 건드리지 않는다(메모리 runpod-account-is-shared-across-projects).
  </how-to-verify>
  <resume-signal>`uid:<uid> code:<CODE> pod:go <4090|L4>` (+ 있으면 `videos: <경로>`) 또는 "blocked" — `pod:go` 가 없으면 Task 2 는 Pod 을 켜지 않고 다시 묻는다</resume-signal>
</task>

<task type="auto">
  <name>Task 2: SSM 화이트리스트 추가(롤백 = get-parameter-history) → push 확인 → (시험 영상 2차 먼저) → Pod 기동(`pod_consent` 기록 후에만 · GPU 한 종류 · commitSha · 링크 속도 · 주소 동기) → IAM `uploads/`·`reference/` PutObject 확인 → requeue(claim 경유 + `--sweep-expired`) → 첫 requeue 뒤 legacy baseline 재diff(R10)</name>
  <files>.planning/phases/38-supplier-link/38-14-E2E.md, .planning/phases/38-supplier-link/infra/legacy-reference-after-requeue.json</files>
  <read_first>
    - /Users/kimtaesung/.claude/projects/-Users-kimtaesung-Dev-SunityMotion/memory/demo-only-pod-bring-up-procedure.md (전체 — 09-19·09-23·09-24 갱신 포함: API 생성 · nohup · `RUNPOD_POD_ID` · start_server.sh md5 · `--urls-only` 함정)
    - backend/runpod_inference/start_server.sh · backend/runpod_inference/bootstrap_full.sh · backend/scripts/pod_teardown.py :1-40 · backend/scripts/requeue_reference_registrations.py (38-08) · backend/scripts/sealed_test.py :1-25
    - backend/scripts/snapshot_reference_baseline.py (38-09 — `--diff <before> --out <after>`; raw doc, overlay 아님) · .planning/phases/38-supplier-link/infra/legacy-reference-baseline.json (38-09 가 배포 전에 찍은 정본) · docs/reference-motions.md §3 (legacy 필드명)
    - .planning/phases/38-supplier-link/38-REVIEWS.md "### R10" · "### R5" · "### R15" 다섯째 항목
    - .planning/phases/38-supplier-link/38-08-SUMMARY.md (Pod 기동 절차 7단계 문장) · 38-09-SUMMARY.md (라이브 라우트·env)
    - .planning/phases/38-supplier-link/38-09-CHANGESET.md "IAM" 절(simulate-principal-policy 결과 또는 `[미확인]`) · 38-07-PLAN.md threat_model 표의 T-38-07-7 · 38-14-E2E.md 머리의 `pod_consent:` 줄(Task 1)
  </read_first>
  <action>
    0. **SSM:** `CUR=$(aws ssm get-parameter --name /sunity/motion/supplier-uids --query Parameter.Value --output text --profile sunity-motion)` → `aws ssm put-parameter --name /sunity/motion/supplier-uids --type String --overwrite --value "${CUR},<uid>:<CODE>" --profile sunity-motion` → get 으로 확인(값은 E2E.md 에 uid 앞 4자만). **롤백 원본(덮어쓴 이전 값은 마스킹 로그가 아니라 SSM 이력에 있다):** `aws ssm get-parameter-history --name /sunity/motion/supplier-uids --profile sunity-motion --query 'Parameters[-2].[Version,LastModifiedDate]' --output text` 로 직전 버전 번호·시각을 E2E.md 에 적고, 복원 명령을 그대로 적어 둔다: `aws ssm put-parameter --name /sunity/motion/supplier-uids --type String --overwrite --value "$(aws ssm get-parameter-history --name /sunity/motion/supplier-uids --profile sunity-motion --query 'Parameters[-2].Value' --output text)" --profile sunity-motion`(값은 문서에 안 적는다 — 명령만). 60초 뒤 정은지(대역)가 `등록 확인하기` 를 누르면 홈이 보인다 — Task 3 에서 확인.
    1. **push:** `git log --oneline origin/main..HEAD` 가 비어 있지 않으면 push 먼저(Pod 부트스트랩은 origin 만 본다).
    2. **인터럽트:** Task 1 응답에 `videos:` 가 있으면 Pod 기동 뒤 **먼저** `sealed_test.py seal → run → grade`(사람 영상은 `intake_clips.py` 경로, manifest 금지) 를 돌리고 그 결과를 별도 절로.
    3. **Pod 기동(메모리 절차) — 전제: E2E.md 에 `pod_consent:` 줄(Task 1 의 `pod:go <GPU>`)이 없으면 여기서 멈춘다, Pod 을 켜지 않는다.** GraphQL `podFindAndDeployOnDemand`(networkVolumeId `a5z753defc`, GPU = `pod_consent` 의 종류 **하나만** — 그 종류가 없으면 L4 로 임의 대체하지 않고 belle 에게 다시 묻는다; 종류·podId·단가를 E2E.md 에; 다른 프로젝트 Pod 무접촉) → ssh → 볼륨 리포 `git fetch origin -q && git reset --hard origin/main -q` → `cp backend/runpod_inference/start_server.sh /workspace/start_server.sh` + md5 대조 → 부트스트랩(`nohup … < /dev/null &`) → `export RUNPOD_POD_ID=<podId>; source /workspace/aws_env.sh; nohup bash /workspace/start_server.sh > /tmp/runpod_server.log 2>&1 < /dev/null &` → `GET /health`: `pipeline_loaded:true`, `commitSha` == 로컬 `git rev-parse HEAD`(다르면 멈춤) → 링크 속도(presigned 10MB curl, ≥ 1MB/s; 미만이면 Pod 교체) → `aws lambda get-function-configuration --function-name sunity-motion-pilot-pipeline --profile sunity-motion --query 'Environment.Variables.RUNPOD_ANALYZE_URL'` 이 새 Pod 주소인지 + `aws ssm get-parameter --name /sunity/motion/runpod-pod-expected` == `up` → `curl -s -o /dev/null -w '%{http_code}' -X POST https://<pod>-8000.proxy.runpod.net/register-reference -H 'Content-Type: application/json' -d '{}'` → `401`(토큰 없음 = 라우트 존재).
    3-b. **자기 재현성 전제 확인(38-07 T-38-07-7 — copy_object 가 `uploads/` 에 쓸 수 있나):** Pod ssh 에서 `source /workspace/aws_env.sh && aws sts get-caller-identity --query Arn --output text` 를 38-09 CHANGESET.md "IAM" 절의 ARN 과 대조 — 같고 38-09 가 `s3:PutObject uploads/* = allowed` 를 기록했으면 승계 `[확인 38-09]`; 다르거나 38-09 가 `[미확인]` 이면 Pod 세션에서 probe: `aws s3api put-object --bucket sunity-motion-pilot-videos --key uploads/_iamprobe/38-14.txt --body /dev/null` **와** `--key reference/_iamprobe/38-14.txt`(둘 다 파이프라인 정규식에 안 맞는 키 — `parse_upload_key`/`parse_reference_key` None → "스킵" 로그 1줄씩, 분석 0) → 성공이면 각각 `delete-object` 로 지우고 `[확인 probe]`(delete 가 거부되면 그 0바이트 객체를 남겼다고 적는다); 어느 쪽이든 AccessDenied 면 **여기서 멈추고** belle 에게 보고 — `reference/*` 없이는 등록 자체(v1 확정 복사, R5)가, `uploads/*` 없이는 Success ③ 이 안 된다; 정책 추가는 belle 결정. 결과를 E2E.md 관측 절에.
    4. **requeue(claim 경유):** `backend/.venv/bin/python backend/scripts/requeue_reference_registrations.py --dry-run`(38-13 에서 belle 이 올린 대기 건이 보인다) → 실제 실행 → 각 건 `job=… → 202` → **곧바로 한 번 더 실행** → 전부 `skip(claim)` 0 POST(리뷰 R3 실물 확인) → `--sweep-expired --dry-run`(38-13 취소 실험이 있었으면 그 `registering` 이 `expired` 후보로 보인다) → 실제 → 몇 분 뒤 `reference/{refId}` 의 `registrationStatus`(active 또는 failed+code)·`videoS3Key`(v1 키)·`videoETag`·`jobId`·`selfCheckStatus`·`selfScore` 를 읽어 표로. belle 의 테스트 등록이 `failed` 면 코드·문구(비공개 doc `registrationError`)를 그대로 기록(실패 4형 실물 첫 관측).
    5. **baseline 재diff(Success ④, R10):** `backend/.venv/bin/python backend/scripts/snapshot_reference_baseline.py --diff .planning/phases/38-supplier-link/infra/legacy-reference-baseline.json --out .planning/phases/38-supplier-link/infra/legacy-reference-after-requeue.json` → 차이 0(exit 0) 원문을 E2E.md 에(첫 requeue 가 legacy 를 안 건드렸다); 차이가 있으면 굵게 + 멈추고 보고. 읽기 ≈ 14.
    E2E.md 관측 절에 전부 명령 원문 + 출력(토큰·비밀 제외).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && test -f .planning/phases/38-supplier-link/infra/legacy-reference-after-requeue.json && test "$(grep -c 'ref-' .planning/phases/38-supplier-link/infra/legacy-reference-after-requeue.json)" -ge 11 && backend/.venv/bin/python backend/scripts/snapshot_reference_baseline.py --diff .planning/phases/38-supplier-link/infra/legacy-reference-baseline.json --out /dev/null --offline .planning/phases/38-supplier-link/infra/legacy-reference-after-requeue.json && grep -q 'commitSha' .planning/phases/38-supplier-link/38-14-E2E.md && grep -q 'pod_consent:' .planning/phases/38-supplier-link/38-14-E2E.md && grep -q 'get-parameter-history' .planning/phases/38-supplier-link/38-14-E2E.md && grep -q 'skip(claim)' .planning/phases/38-supplier-link/38-14-E2E.md && test -z "$(git log --oneline origin/main..HEAD)"</automated>
  </verify>
  <acceptance_criteria>
    - `git log --oneline origin/main..HEAD` is empty before Pod bring-up (recorded).
    - E2E.md has the `pod_consent:` line **above** any Pod bring-up output, and the GPU type actually deployed equals the consented type (no silent substitution); no Pod of another project appears in any create/terminate output.
    - E2E.md records the SSM rollback source (previous `supplier-uids` version number from `get-parameter-history`) and the restore command verbatim (value masked).
    - E2E.md has the IAM PutObject-on-`uploads/` result: `[확인 38-09]`, `[확인 probe]`, or a stop with the AccessDenied text — a later `selfCheckStatus:'failed'` must not be the first place this is learned.
    - `38-14-E2E.md` records: GPU type (one), pod id, `/health` JSON with `pipeline_loaded:true` and `commitSha` equal to local HEAD, link speed number (≥ 1 MB/s), Lambda `RUNPOD_ANALYZE_URL` equals the pod URL, SSM `runpod-pod-expected` == `up`, `/register-reference` unauthenticated → 401.
    - requeue dry-run, real run, the immediate second run (all `skip(claim)`, 0 POST — R3) and `--sweep-expired` outputs are pasted; each previously `queued` doc has a final `registrationStatus`, `videoS3Key` (v1 key) and `videoETag` in the table.
    - `infra/legacy-reference-after-requeue.json` exists and `snapshot_reference_baseline.py --diff` against the 38-09 baseline prints 0 differences (R10) — (`--offline <after.json>` compares two files without reading Firestore; the script must support it).
    - E2E.md IAM probe line covers both `uploads/_iamprobe` and `reference/_iamprobe`.
    - SSM `supplier-uids` value contains the new `uid:CODE` (masked in the doc).
  </acceptance_criteria>
  <done>Pod 가 올바른 코드로 떠 있고 대기 등록이 claim 을 지나 한 번씩 처리됐으며, 첫 requeue 뒤 legacy 재diff 가 0 이다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Success ① · ③ — 정은지(대역)가 링크로 새 동작 1개 등록 → 앱 picker 에 뜸 → 그 동작으로 mode1 분석 완주 → 재현성 점수 보고</name>
  <files>none — 체크포인트(사람 검증). 결과는 38-14-E2E.md 판정 절</files>
  <action>
    실행자: Pod 가 떠 있고(Task 2) SSM 화이트리스트가 갱신됐음을 확인한 뒤 how-to-verify 1~4 를 belle/정은지에게 제시하고 멈춘다. 그 사이 Firestore `reference` 컬렉션에서 새 doc 이 생기면(`where supplierUid == <uid>` 단일 쿼리, 전수 스캔 금지) `registrationStatus` 전이와 Pod 로그를 실시간으로 E2E.md 관측 절에 적는다. `selfScore` 가 채워지면 그 숫자와 소비처(`users/{uid}/analyses/{selfCheckAnalysisId}.result.overallScore`)를 belle 에게 **숫자만** 알린다 — 통과선을 제안하지 않고, 의미도 "자기 영상 재분석 일관성" 이라고만(동작 정확도 판정이 아니다 — D-11, 리뷰 R11). 이 사람이 belle 의 대역 계정이면 판정 절의 `success_1_independent_user` 는 `[미확인]` 그대로 두고 `structural_check_stand_in` 만 채운다(R15e). belle 응답을 판정 절에 원문 인용.
  </action>
  <verify>
    <automated>grep -q 'selfScore' /Users/kimtaesung/Dev/SunityMotion/.planning/phases/38-supplier-link/38-14-E2E.md</automated>
  </verify>
  <acceptance_criteria>
    - `38-14-E2E.md` 판정 절에 belle/정은지의 단계별 ○/× 와 재현성 판정 한 줄이 원문으로 있고, `success_1_independent_user:` 줄이 완주자에 맞게(`[확인 — <사람>]` 또는 `[미확인]`) 적혀 있다(R15e).
    - selfScore 숫자와 그 소비처 경로가 관측 절에 있다(실행자 판정 문장 없음).
  </acceptance_criteria>
  <done>Success ①·③ 의 사람 판정이 원문으로 기록됐다.</done>
  <what-built>Pod 가 켜져 있고 화이트리스트에 정은지 uid 가 있다. 링크로 올리면 `올린 영상 확인 중 → 등록 중 → 새로 추가됨 → 재현성 N점` 으로 바뀌고 앱 기준 동작 목록에 새 동작이 나타나야 한다.</what-built>
  <how-to-verify>
    1. 정은지(대역): 링크 → `등록 확인하기` → 홈 → `동작 올리기 >` → 5~30초, 세로, 고정, 폴 전체+전신, 서 있는 자세에서 시작하는 영상 1개(가이드 조건) → 폼 → `올리기` → 홈 행이 `올린 영상 확인 중` → `등록 중 · 몇 분 걸려요` → `새로 추가됨` 또는 `재현성 N점` 으로 바뀌는가(실패면 `실패 · 눌러서 확인` → 상세의 제목·본문을 그대로 알려 준다).
    2. belle 아이폰 앱(TestFlight/시뮬) → 분석 → 기준 동작 선택 → 새 동작이 `{선수 이름}` 과 레벨 탭에 보이는가(썸네일은 회색 기본).
    3. 그 동작을 골라 **같은 영상**(또는 아무 연습 영상)으로 분석 → 결과 화면까지 오는가(`done`). 실패하면 화면 문구 그대로.
    4. 실행자가 알려 준 `selfScore` 숫자(`reference/{refId}.selfScore` = 자기 분석 `result.overallScore` 화면 점수 — 같은 v1 영상을 자기 기준에 다시 잰 값, 추출·저장이 일관되는지의 진단)를 보고 belle 이 "일관된다/다시 찍는다" 를 판정한다 — 통과선은 belle 이 정한다. 동작이 맞는지는 이 점수로 판정하지 않는다(시험 영상 2차가 잰다, D-11).
    5. **누가 완주했나:** 정은지(또는 belle 아닌 다른 사람)면 Success ① 완료; belle 의 다른 계정이면 구조 검증만 — 실행자가 E2E.md 두 줄을 그에 맞게 적는다(R15e).
    실행자는 그 사이 Pod 로그·doc 을 읽어 표를 채운다(Task 4).
  </how-to-verify>
  <resume-signal>"approved" 또는 단계별 ○/× + 문구 원문 + 재현성 판정 한 줄</resume-signal>
</task>

<task type="auto">
  <name>Task 4: 증거 수집(등록 doc 전이 · v1 키/ETag · 재PUT 불변 실물 1건(R5) · picker 노출 · mode1 분석 doc `done` · Pod 로그 RuntimeError 0 · selfScore 소비처·jobId 추적 · E2E 뒤 baseline 재diff = 0 + 표본 id 비겹침(R10)) → Pod teardown(pod-expected=down) → E2E.md(두 라벨 분리)/SUMMARY</name>
  <files>.planning/phases/38-supplier-link/38-14-E2E.md, .planning/phases/38-supplier-link/infra/legacy-reference-after-e2e.json</files>
  <read_first>
    - .planning/phases/38-supplier-link/38-14-E2E.md (Task 2 관측) · infra/legacy-reference-baseline.json
    - backend/scripts/pod_teardown.py (전체 — `--urls-only` 는 조회가 아니다 경고) · /Users/kimtaesung/.claude/projects/-Users-kimtaesung-Dev-SunityMotion/memory/pod-log-truncated-on-server-restart.md
    - backend/functions/pipeline/app.py :8901-8906 (RuntimeError 문자열 원문 `기준 모션 또는 keyframe 데이터 없음`)
  </read_first>
  <action>
    1. 등록 doc: `reference/{refId}` 를 읽어 `registrationStatus` 전이(`registrationUpdatedAt`)·`jobId`·`videoS3Key`(`…/v1.{ext}` 인지)·`videoETag`·`anglesFrames`·`anglesRealFps`·`isActive`·`selfCheckStatus`·`selfCheckAnalysisId`·`selfCheckJobId`·`selfScore` 표. picker 노출 조건(`name·athleteName·level·isActive`)이 전부 참인지 명시. **재PUT 불변 실물(R5, 1건):** 등록이 `active` 인 refId 의 `uploadKey` 에 900초 안이면 같은 presigned URL(없으면 Pod 세션 자격증명으로 `put-object`) 로 **다른 작은 mp4** 를 한 번 더 올린다 → `head_object(videoS3Key)` 의 ETag 가 `videoETag` 와 그대로인지, `reference/{refId}.anglesUpdatedAt` 이 안 바뀌었는지, Pod/Lambda 로그에 `스킵: 등록 대기 doc 아님` 1줄인지를 원문으로(등록 doc 무변경 = 통과). 실험한 refId 와 시각을 적는다.
    2. 자기 분석 doc: `users/{uid}/analyses/{selfCheckAnalysisId}` 의 `status`·`mode`·`referenceMotionId`·`selfCheckForReference`·`selfCheckJobId`(= 기준 doc `jobId`·`selfCheckJobId` 와 같은 값 — R2/R8 연결 확인)·`result.overallScore`·`result.deductionBreakdown.final` — **selfScore 와 같은 값인지**(소비처 추적, CLAUDE.md §7 ★). belle/정은지의 mode1 분석 doc 도 `status: done` 확인. 보고 문장은 "자기 영상 재분석 N점(추출·저장 일관성)" — 정확도 판정 문장 금지(R11).
    3. Pod 로그: `scp` 로 `/tmp/runpod_server.log` 를 scratchpad 에 먼저 내려받고(재기동 전 규율) `grep -c '기준 모션 또는 keyframe 데이터 없음'` == 0 · `grep -c 'register-reference ok'` ≥ 1 · `grep 'stage_timing' | grep rtmw` 로 GPU 판정(VRAM/시간) · 오류 줄 목록.
    4. Success ④(R10): `snapshot_reference_baseline.py --diff infra/legacy-reference-baseline.json --out infra/legacy-reference-after-e2e.json` — 11 doc raw 필드(angles sha256 포함)·11 ETag·저장 분석 표본 3건 전부 동일(0 diff) 을 명령 출력으로 + 새로 생긴 자기 재현성 `selfCheckAnalysisId` 들이 표본 3 id 와 겹치지 않음을 한 줄로. 다르면 그 필드를 굵게(무접촉 실패 = 이 phase 의 결함).
    5. teardown: `backend/.venv/bin/python backend/scripts/pod_teardown.py`(공용 계정 — 이 Pod 만) → `aws ssm get-parameter --name /sunity/motion/runpod-pod-expected` == `down` · Lambda `RUNPOD_ANALYZE_URL` 자리표시자 확인. 이후 올라오는 등록은 `queued` 가 정상(D-20) 임을 E2E.md 에.
    6. E2E.md: **관측** 절(위 1~5 원문) / **판정** 절(Success ①②③④ 각각 ○/×/반쪽 + belle 말 인용; ① 은 `success_1_independent_user` 줄이 `[확인 — <사람>]` 일 때만 ○, 대역만이면 "구조 검증 ○ · 독립 사용자 [미확인]" 두 줄(R15e); ② 는 38-05/38-07 pytest + 실물 실패가 있었으면 그 코드; ③ 은 숫자 + "재분석 일관성" 의미 + belle 판정(R11)) / **진단** 절(재검증 대상) / 비용(Pod 시간 × 단가). STATE.md `stopped_at` 갱신 문장을 SUMMARY 에 제안.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && export AWS_PROFILE=sunity-motion && test "$(aws ssm get-parameter --name /sunity/motion/runpod-pod-expected --query Parameter.Value --output text)" = "down" && grep -q 'selfScore' .planning/phases/38-supplier-link/38-14-E2E.md && grep -q '기준 모션 또는 keyframe 데이터 없음' .planning/phases/38-supplier-link/38-14-E2E.md && test -f .planning/phases/38-supplier-link/infra/legacy-reference-after-e2e.json && grep -q 'success_1_independent_user:' .planning/phases/38-supplier-link/38-14-E2E.md && grep -q 'structural_check_stand_in:' .planning/phases/38-supplier-link/38-14-E2E.md && grep -q 'videoETag' .planning/phases/38-supplier-link/38-14-E2E.md</automated>
  </verify>
  <acceptance_criteria>
    - E2E.md contains: the registration doc transition table with `registrationStatus == active`, `isActive == true`, `videoS3Key` ending in `/v1.<ext>` and `videoETag`; the re-PUT immutability record (ETag unchanged, `anglesUpdatedAt` unchanged, skip log line — R5); the self-check analysis doc `status == done`, `selfCheckJobId` equal to the reference `jobId`, and `result.overallScore` equal to `reference/{refId}.selfScore`; the Pod log grep line showing `0` for the RuntimeError string and ≥ 1 for `register-reference ok`; the `--diff` output against the 38-09 baseline (0 differences) plus the sample-id non-overlap line (R10); GPU type.
    - E2E.md has both labels `structural_check_stand_in:` and `success_1_independent_user:` and Success ① is marked ○ only when the latter is `[확인 — …]` (R15e); the ③ verdict line uses the words 재분석/일관 and contains no accuracy claim (R11).
    - `aws ssm get-parameter --name /sunity/motion/runpod-pod-expected` == `down` after teardown; `pod_teardown.py` output pasted.
    - Success ①~④ verdict lines exist with belle's words quoted for ① and ③.
    - The Pod server log file was downloaded (path recorded) before teardown.
  </acceptance_criteria>
  <done>네 Success 가 명령 원문·doc 값·로그로 판정되고 Pod 은 내려가 있다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로컬 → RunPod API / ssh | Pod 생성·기동(공용 계정) |
| Pod → S3/Firestore | 실제 쓰기(등록·분석) |
| 링크 → 정은지 계정 | 실제 사람 영상·동의 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-14-1 | Tampering | legacy 11 doc/S3/저장 분석 표본 | mitigate | 38-09 배포 전 raw baseline 대비 재diff 0 × 2(requeue 뒤·E2E 뒤) + 표본 id 비겹침(R10) |
| T-38-14-6 | Tampering | 재PUT 실험 객체 | accept | `upload.{ext}` 에만 쓰고(확정 v1·doc 무변경을 그 실험이 증명) 실험 refId·시각 기록; 실험 mp4 는 무해한 짧은 클립 |
| T-38-14-2 | DoS | 공용 RunPod 계정 | mitigate | belle `pod:go <GPU>` 동의(단가 조회값 포함) 뒤에만 생성, 이 Pod 만 생성·종료(`pod_teardown.py` 단일 대상), 전체 종료 금지 |
| T-38-14-3 | Information disclosure | E2E.md | mitigate | uid 마스킹, 토큰·SSM 값·SA json 기록 금지, 사람 영상 경로는 `/Users/Shared` 밖 금지 |
| T-38-14-4 | Repudiation | 정은지 동의 | mitigate | doc `consent{at,version,uid}` 를 E2E.md 에 확인(값 자체는 서버 기록) |
| T-38-14-5 | Tampering | 사람 영상 → manifest | mitigate | manifest.json 무접촉(D-21) — `git diff` 로 확인 |
| T-38-SC | Tampering | pip installs (Pod bootstrap) | accept | 리포 정본 `bootstrap_full.sh`/requirements 만 |
</threat_model>

<verification>
- E2E.md 관측 절 완비 · Pod down · baseline diff 0 · belle approved.
- 플랜 끝 push(E2E.md·baseline 커밋).
</verification>

<success_criteria>
- Success ① ② ③ ④ 가 각각 명령·doc·로그·pytest 로 판정됐고(① 은 belle 아닌 사람 완주 때만 완료, 대역은 구조 검증으로 따로), Pod 이 내려가 있으며 다음 등록은 queued 로 대기한다.
</success_criteria>

<output>
Create `.planning/phases/38-supplier-link/38-14-SUMMARY.md` when done (판정 먼저 · 관측/진단 분리 · `[확인]`/`[미확인]` · 대역이면 명시).
</output>
