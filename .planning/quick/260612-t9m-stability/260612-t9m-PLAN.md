---
phase: quick-260612-t9m
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/shared/python/sunity_shared/analysis/dimensions.py
  - app/src/app/analysis/result.tsx
autonomous: true
requirements:
  - Q-T9M-01  # stability tol 15° → 25° (self-test 95+ 회복)
  - Q-T9M-02  # 결과 화면 점수 안내 캡션 추가 (한국어, 라이트 톤)
  - Q-T9M-03  # 4 motion self-test 검증 + SAM 재배포 + Pod 재시작 + EAS update
user_setup: []

must_haves:
  truths:
    - "같은 영상 self-test 시 stability 점수가 95+ 회복 (이전 68-94 → 95+)"
    - "결과 화면 점수 게이지 아래에 '100점은 어렵습니다. 90+ 이면 정상' 톤의 한국어 안내가 노출"
    - "정은지 reference 4 motion (climb / elbow-twist-sister / foxtop / peter-pan) self-test stability 90+ 회복 확인"
    - "Lambda + Pod 양쪽 갱신 코드 사용 (Pod 재시작으로 sunity_shared 갱신 반영)"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/dimensions.py"
      provides: "_STABILITY_TOL_DEG = 25.0 + 주석 갱신 (정은지 wobble 6-16° + 90+ 정상 임계 + 30°+ 진짜 떨림)"
      contains: "_STABILITY_TOL_DEG = 25.0"
    - path: "app/src/app/analysis/result.tsx"
      provides: "OctagonScore 아래 안내 캡션 (한국어, 라이트 톤, design.md §5 정합)"
      contains: "촬영 노이즈"
  key_links:
    - from: "backend/shared/python/sunity_shared/analysis/dimensions.py"
      to: "stability_score / line_score 산식"
      via: "_STABILITY_TOL_DEG 상수"
      pattern: "_STABILITY_TOL_DEG\\s*=\\s*25"
    - from: "app/src/app/analysis/result.tsx"
      to: "OctagonScore 카드 영역"
      via: "score 게이지 카드 안 (gradeRow 다음) 캡션 Text"
      pattern: "촬영 노이즈|90점 이상"
---

<objective>
같은 영상 self-test stability 점수 보정 + 사용자 안내 캡션 추가.

Purpose:
  belle 가 self-test 시 stability 68-94 (만점 안 나옴) 의문 제기. 원인 = `_STABILITY_TOL_DEG = 15°` 가 정은지 reference wobble 측정 범위 (6-16°) 와 거의 같아 같은 영상조차 깎임 (RTMW noise + 자세 미세 변화 흡수 불충분).

  옵션 A (belle 채택): tol 15° → 25° 로 완화 → 같은 영상 95+ 회복, 30°+ 진짜 떨림만 감점. 동시에 결과 화면에 "100점은 어렵습니다, 90+ 면 정상" 안내 캡션 박제 → 사용자가 90+ = 정상 인지.

Output:
  - dimensions.py 의 _STABILITY_TOL_DEG 상수 25.0 + 주석 갱신
  - result.tsx 점수 게이지 아래 한국어 안내 캡션 (design.md §5 라이트 톤)
  - 4 motion self-test stability 90+ 회복 확인
  - SAM 재배포 + Pod 재시작 + EAS update preview 채널
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@app/CLAUDE.md
@app/AGENTS.md
@backend/shared/python/sunity_shared/analysis/dimensions.py
@app/src/app/analysis/result.tsx
@backend/scripts/verify_self_comparison.py

# 관련 메모리
# - [[scoring-dimensions-ipsf]] — IPSF 기반 점수 차원 (균형 제거)
# - [[feedback-analysis-first]] — 분석 정확도 우선
# - [[no-baekje-filler]] — "박제" 단어 자제
# - [[mvp-simple-pilot-quality]] — 시연 임박 가정 X
# - [[runpod-gpu-env]] — Pod env 박제
# - [[next-pod-use-network-storage]] — 새 Pod 만들기 X (기존 재시작)
</context>

<tasks>

<task type="auto">
  <name>Task 1: dimensions.py 의 _STABILITY_TOL_DEG 15° → 25° 보정</name>
  <files>backend/shared/python/sunity_shared/analysis/dimensions.py</files>
  <action>
    `backend/shared/python/sunity_shared/analysis/dimensions.py:159` 의 `_STABILITY_TOL_DEG = 15.0` 을 `_STABILITY_TOL_DEG = 25.0` 으로 변경 (Q-T9M-01).

    동시에 같은 줄 인라인 주석을 갱신하여 다음 결정 근거를 기록:
    (a) 측정 데이터: 정은지 reference 5영상 wobble inter-frame median 6~16° 범위.
    (b) 보정 의도: tol 15° 일 때 reference 자체가 z=0.4~1.0 → stability 68-94 (만점 X). tol 25° 면 reference wobble z<0.7 → 95+ 안정. 같은 영상 self-test 도 95+ 회복.
    (c) FAIL 임계: 진짜 떨림 (30°+) 만 stability < 70 깎임. 사용자 인지 임계 "90+ 정상" 정합.
    (d) 날짜·이유 박제: "2026-06-12 belle 결정: self-test 만점 미회복 의문 → RTMW noise + 자세 미세 변화 흡수 불충분 → tol 완화 (옵션 A)."

    주석은 기존 "Path T1 (2026-06-05)" 한 줄 헤더는 보존하고, 그 아래 추가 한 줄로 위 (a)~(d) 요약 (한국어, 60-100자 압축). Pretendard·이모지 금지·"박제" 단어 최소화 ([[no-baekje-filler]]).

    다른 상수 (_LINE_TOL_DEG=20.0) 는 절대 손대지 말 것 — 본 task scope 는 stability 전용.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && grep -n "_STABILITY_TOL_DEG\s*=\s*25\.0" shared/python/sunity_shared/analysis/dimensions.py && grep -c "_LINE_TOL_DEG\s*=\s*20\.0" shared/python/sunity_shared/analysis/dimensions.py | grep -q "^1$" && echo OK</automated>
  </verify>
  <done>
    - `_STABILITY_TOL_DEG = 25.0` 정확히 한 번 등장
    - `_LINE_TOL_DEG = 20.0` 보존 (변경 X)
    - 주석에 정은지 wobble 6-16° + 90+ 정상 + 30°+ 떨림 근거 한국어 기록
  </done>
</task>

<task type="auto">
  <name>Task 2: 결과 화면 점수 게이지 아래 안내 캡션 추가</name>
  <files>app/src/app/analysis/result.tsx</files>
  <action>
    `app/src/app/analysis/result.tsx` 의 점수 게이지 카드 (line 597-605 영역, `<View style={styles.card}>` 안의 `<OctagonScore>` + `gradeRow` + `LevelBenchmark` 블록) 안에 안내 캡션 한 줄을 추가 (Q-T9M-02).

    위치: `<LevelBenchmark score={result.overallScore} />` 바로 아래 (즉 점수 게이지 카드 가장 마지막 자식). 카드 안에 추가하여 시각적으로 점수 영역에 묶임. mode1/mode3 공통 노출 (분기 X — 두 모드 모두 stability 차원 노출).

    본문 (한국어, design.md §5 라이트 톤, Pretendard, 두 줄 이내):
    "촬영 노이즈와 측정 허용 범위가 있어 100점은 잘 나오지 않아요. 90점 이상이면 정상 자세에 가깝습니다."

    스타일: 새 `styles.scoreCaption` 항목 추가 — `...typography.caption`, `color: colors.textSecondary`, `textAlign: 'center'`, `marginTop: 12`, `lineHeight: 18`, `paddingHorizontal: 4`. 하드코딩 색·폰트 금지 — theme 토큰만 (app/CLAUDE.md 규칙).

    렌더: `<Text style={styles.scoreCaption}>촬영 노이즈와 측정 허용 범위가 있어 100점은 잘 나오지 않아요. 90점 이상이면 정상 자세에 가깝습니다.</Text>`

    접근성: 자동 — Text 컴포넌트라 별도 accessibilityRole 불필요. design.md §10 라이트 배경 보존 (다크 배경 금지 [[mvp-simple-pilot-quality]] 정합).

    이모지/스피너 X, "박제" 단어 X. 기존 `LevelBenchmark` 의 한국어 톤과 일관 (~24자/문장).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && grep -n "촬영 노이즈" src/app/analysis/result.tsx && grep -n "90점 이상" src/app/analysis/result.tsx && grep -n "scoreCaption" src/app/analysis/result.tsx && npm run typecheck</automated>
  </verify>
  <done>
    - 점수 카드 안 (`<View style={styles.card}>` 안, `LevelBenchmark` 다음) 캡션 Text 1개 추가
    - `styles.scoreCaption` 정의 (theme 토큰만, 하드코딩 색 0)
    - "촬영 노이즈" + "90점 이상" 문자열 둘 다 등장
    - `npm run typecheck` 통과 (tsc --noEmit 에러 0)
  </done>
</task>

<task type="auto">
  <name>Task 3: 4 motion self-test 검증 + SAM 재배포 + Pod 재시작 + EAS update</name>
  <files>
    backend (SAM stack sunity-motion-pilot, ap-northeast-2),
    RunPod pod (uvicorn runpod_inference.server),
    app (EAS preview 채널)
  </files>
  <action>
    Q-T9M-03 — 변경 검증 + 배포 3-단계. 순서대로 진행, 단계별 실패 시 즉시 정지하고 원인 보고.

    (1) backend quick self-test — 로컬에서 reference-angles.json 기반 self-DTW 실행:
        ```
        cd /Users/kimtaesung/Dev/SunityMotion/backend
        python scripts/verify_self_comparison.py \
          --reference scripts/reference-angles.json \
          --out scripts/self-comparison-quick.json \
          --quick
        ```
        결과 JSON 의 4 motion (ref-climb, ref-elbow-twist-sister, ref-foxtop, ref-peter-pan) stability 점수 (또는 partScores 안 해당 dim) 가 모두 90 이상인지 확인. 미만이면 dimensions.py 변경 누락 → 즉시 정지. 4 motion 모두 미포함 시 reference-angles.json 의 실제 motion id 로 대체 (예: ref-sideway-spin 등 reference-angles.json 의 키 모두 95+ 인지 확인하는 식으로 일반화) — verify 단계는 "reference 5+ motion 모두 stability/유사 차원 90+ " 로 환원.

    (2) backend 재배포 — Lambda 의 sunity_shared layer 갱신:
        ```
        cd /Users/kimtaesung/Dev/SunityMotion/backend
        sam build --use-container
        sam deploy --no-confirm-changeset --no-fail-on-empty-changeset
        ```
        [[sam-build-native-deps]] — Docker Desktop 켜져있어야 함. 실패 시 belle 에게 Docker 기동 요청.

    (3) RunPod 재시작 — Pod 의 sunity_shared 코드 동기화:
        Pod 는 sunity_shared 를 로컬 git clone 으로 보유. ssh 로 Pod 접속하여:
        ```
        cd /workspace/SunityMotion && git pull
        # uvicorn 재시작 (screen 기반 launch-uvicorn.sh)
        bash /root/launch-uvicorn.sh
        # 또는 screen -X -S uvicorn quit && bash /root/launch-uvicorn.sh
        ```
        [[runpod-gpu-env]] [[pod-ops-claude-runs]] — Pod ssh 정보는 .planning 또는 사용자 환경 변수 확인. Pod 재시작 후 `GET /health` 200 확인.

    (4) app 배포 — EAS preview 채널 update:
        ```
        cd /Users/kimtaesung/Dev/SunityMotion/app
        eas update --branch preview --message "stability tol 25° + 점수 안내 캡션"
        ```
        [[eas-build-gotchas]] — npm ci sync / git committed 확인. update 는 OTA 라 build 재실행 불필요.

    각 단계 산출물 (verify JSON, sam deploy outputs, eas update URL) 을 SUMMARY 에 기록.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && test -f scripts/self-comparison-quick.json && python -c "import json,sys; d=json.load(open('scripts/self-comparison-quick.json')); results=d.get('results',{}); fails=[k for k,v in results.items() if (v.get('partScores',{}).get('stability') or v.get('partScores',{}).get('안정성') or 90) < 90]; sys.exit(0 if not fails else (print('FAIL motions:',fails) or 1))"</automated>
  </verify>
  <done>
    - `scripts/self-comparison-quick.json` 의 stability (또는 동등 키) 모두 90+ — 미만 motion 0건
    - `sam deploy` 가 CREATE_COMPLETE / UPDATE_COMPLETE 로 종료 (CloudFormation stack sunity-motion-pilot)
    - Pod `GET /health` 200 + uvicorn 프로세스 재기동 확인
    - `eas update --branch preview` 가 update group URL 반환
    - SUMMARY 에 4 산출물 (self-test JSON 발췌 / sam outputs / pod health / eas URL) 기록
  </done>
</task>

</tasks>

<verification>
- `_STABILITY_TOL_DEG = 25.0` 정확히 한 번 등장, `_LINE_TOL_DEG = 20.0` 보존
- 결과 화면 점수 카드에 "촬영 노이즈" + "90점 이상" 한국어 캡션 노출 + `npm run typecheck` 통과
- self-comparison-quick.json 의 stability 차원 모두 90+
- SAM stack sunity-motion-pilot 배포 완료 + Pod 재시작 + EAS preview update 발행
</verification>

<success_criteria>
- belle 가 preview 채널 앱에서 자기 영상 재분석 시 stability 90+ 회복 확인 가능
- 점수 게이지 아래 한국어 안내 캡션이 라이트 톤으로 노출 (다크 배경 0)
- 정은지 reference 4 motion (또는 reference-angles.json 의 모든 motion) self-test stability 90+
- design.md §5 (카드/캡션 스타일) + app/CLAUDE.md 토큰 규칙 정합
</success_criteria>

<output>
Create `.planning/quick/260612-t9m-stability/260612-t9m-SUMMARY.md` when done. SUMMARY 안 다음 3 산출물 포함:
1. dimensions.py diff (변경 전후 _STABILITY_TOL_DEG + 주석)
2. self-comparison-quick.json 의 motion 별 stability 점수 표
3. SAM deploy outputs + Pod `/health` 응답 + EAS update group URL
</output>
