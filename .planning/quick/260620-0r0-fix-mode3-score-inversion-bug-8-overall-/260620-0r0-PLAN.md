---
phase: quick-260620-0r0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/functions/pipeline/app.py
  - backend/tests/test_pipeline_mode3.py
autonomous: true
requirements: [BUG-8-MODE3-INVERSION]
must_haves:
  truths:
    - "Mode3 두 번째+ 분석의 overall 은 절대 차원(line/stability)만으로 산출된다 (angle/유사도 제외)"
    - "이전 영상이 틀리고 새 영상이 더 잘 되어도 overall 이 내려가지 않는다 (역전 없음)"
    - "dimension_scores 반환값은 여전히 angle 포함(표시/일관성 지표용)"
    - "delta(deltaFromPrevious)는 기존대로 절대 차원만 사용 (미변경)"
  artifacts:
    - path: "backend/functions/pipeline/app.py"
      provides: "_mode3_comparison second+ overall = overall_from_dimensions(abs_dims)"
      contains: "overall_from_dimensions(abs_dims)"
    - path: "backend/tests/test_pipeline_mode3.py"
      provides: "회귀 테스트: 낮은 angle 유사도가 overall 을 끌어내리지 않음"
  key_links:
    - from: "backend/functions/pipeline/app.py:_mode3_comparison"
      to: "dimensions.overall_from_dimensions"
      via: "abs_dims 전달 (dim_scores 아님)"
      pattern: "overall = dimensions.overall_from_dimensions\\(abs_dims\\)"
---

<objective>
Mode3 점수 역전 버그(#8) 수정. `_mode3_comparison` 의 두 번째+ 분석 경로가 overall 을
angle 포함 dict(`dim_scores`)로 산출해, mode3 의 angle(=이전 영상 유사도)이 사용자 성장 시
오히려 떨어지면서 `overall_from_dimensions` 의 `min(CORE_DIMENSIONS)` 가 종합을 끌어내린다.
belle 기기 테스트에서 확인됨: 틀린 kip-up(98점) → 올바른 영상(88점, -10) 역전.

Purpose: overall 을 첫 분석과 동일하게 절대 차원(line/stability)만으로 산출해 세션 간 같은
척도 유지, 유사도에 의한 역전 제거. ([[mode3-progress-not-similarity]], [[mode3-overall-exclude-angle-similarity]])

Output: 수정된 call site 1줄 + stale 주석 갱신 + 회귀 테스트 1개. dimensions.py 의
CORE_DIMENSIONS / overall_from_dimensions 는 mode1 에서 올바르므로 변경 금지.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

@backend/functions/pipeline/app.py
@backend/shared/python/sunity_shared/analysis/dimensions.py
@backend/tests/test_pipeline_mode3.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix mode3 second+ overall source + 주석 갱신</name>
  <files>backend/functions/pipeline/app.py</files>
  <action>
`backend/functions/pipeline/app.py` 의 `_mode3_comparison` 두 번째+ 분석 경로(~line 1787)에서
overall 산출 source 를 `dim_scores`(angle 포함) → `abs_dims`(절대 line/stability)로 변경한다.

  변경: `overall = dimensions.overall_from_dimensions(dim_scores)` →
        `overall = dimensions.overall_from_dimensions(abs_dims)`

이로써 첫 분석(~line 1740 의 `overall_from_dimensions(abs_dims)`)과 두 번째+ 분석이 동일
source(절대 차원)를 써 세션 간 같은 척도가 되고, mode3 의 angle(=이전 영상 유사도)이 종합을
역전시키지 못한다.

`dim_scores` (= `{DIM_ANGLE: kismam.overall_score(...), **abs_dims}`, ~line 1781)는 그대로
함수의 dimension_scores 반환값으로 유지한다 (표시/일관성 지표 — return tuple 의 두 번째 요소).
오직 overall 계산만 바꾼다.

~line 1782-1786 의 stale/모순 주석 블록("박제 (2026-06-06 belle): mode3 second+ overall =
모든 차원 평균 ... (angle + line + stability) 평균")을 교정된 동작을 반영하도록 다시 쓴다.
새 주석 요지(한국어 주석 스타일 유지, 이모지 금지):
  - mode3 second+ overall = 절대 차원(line/stability)만 — angle 제외.
  - 이유: mode3 의 angle 은 "이전 영상 대비 유사도"라, 사용자가 발전(이전=틀림, 신규=올바름)
    하면 두 영상이 달라져 유사도(angle)가 떨어지고, `overall_from_dimensions` 의
    min(CORE_DIMENSIONS) 가 종합을 끌어내려 발전을 역전시킨다 (#8, belle 기기: 98→88).
  - 첫 분석 overall(abs_dims)과 동일 source → 같은 척도, 역전 제거.
  - 함수 docstring(~line 1721, 1727) 및 [[mode3-progress-not-similarity]] /
    [[mode3-overall-exclude-angle-similarity]] 정신과 정합.
  - delta(build_mode3 의 cur_dimension_scores=abs_dims, assemble.py:678 근방)는 이미 절대
    차원만 사용 → 변경 없음.

`delta` 산출(comparison/build_mode3 호출, ~line 1792-1797)과 dimensions.py 는 건드리지 않는다.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && grep -n "overall = dimensions.overall_from_dimensions(abs_dims)" functions/pipeline/app.py | wc -l | grep -q 2 && grep -vn '^\s*#' functions/pipeline/app.py | grep -c "overall_from_dimensions(dim_scores)" | grep -qx 0 && echo OK</automated>
  </verify>
  <done>두 번째+ 경로의 overall 이 abs_dims 로 산출됨(코드에 `overall_from_dimensions(abs_dims)` 2회, `overall_from_dimensions(dim_scores)` 0회). dim_scores 는 여전히 반환 tuple 에 포함. 주석 블록이 교정된 동작을 반영.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 회귀 테스트 — 낮은 유사도가 overall 을 역전시키지 않음</name>
  <files>backend/tests/test_pipeline_mode3.py</files>
  <behavior>
    - 케이스: mode3 두 번째 분석에서 이전 영상(seed=A)과 현재 영상(seed=B)이 충분히 달라
      angle(이전 대비 유사도) dim_scores["angle"] 이 절대 차원(line/stability) 최소값보다
      낮게 나오는 구성.
    - 단언 1: 반환된 overall == dimensions.overall_from_dimensions(abs_dims) 와 같다
      (abs_dims = dimensions.absolute_dimension_scores(현재 영상, profile)).
    - 단언 2: overall >= overall_from_dimensions(dim_scores) (angle 포함) — 즉 낮은 angle 이
      종합을 끌어내리지 않음(역전 방지). 두 값이 갈리도록 angle < min(line, stability) 구성을
      먼저 확인(전제 검증). angle 이 충분히 낮지 않으면 그 케이스로 단언이 무의미하므로,
      angle < min(line,stability) 가 성립하는 seed 조합을 고른다(여러 seed 시도).
    - 단언 3: dim_scores 에는 여전히 "angle" 이 포함된다(표시 지표 유지 — 회귀 방지).
  </behavior>
  <action>
`backend/tests/test_pipeline_mode3.py` 에 회귀 테스트 함수
`test_low_similarity_does_not_invert_overall()` 를 추가한다. 기존 헬퍼(`_video`, `_profile`,
`_as_prev`)와 `app`, `app.dimensions` 를 재사용한다 (새 fixture 만들지 말 것).

구성:
  - profile = _profile(); cur = _video(seed_cur); prev_angles = _video(seed_prev) 로 두 영상이
    달라 angle 유사도가 낮게 나오도록 한다. prev dim_scores 는 임의 절대값({"line": ..,
    "stability": ..}) — overall 산출엔 현재 영상 abs_dims 가 쓰이므로 무관.
  - abs_dims = app.dimensions.absolute_dimension_scores(cur, profile) 로 기대 overall 산출.
  - _, dims, overall, _ = app._mode3_comparison(cur, prev, profile) 호출.
  - 전제 가드: dims["angle"] < min(abs_dims.values()) 가 성립하는 seed 조합을 사용한다.
    (성립하지 않으면 이 회귀가 검증하려는 "역전 조건" 자체가 안 만들어지므로, 코드 작성 시
    여러 seed 를 직접 실행해 angle 이 절대 차원 최소값보다 낮게 나오는 조합을 박아둔다.
    seed 값은 결정적이어야 함.)
  - 단언:
      assert overall == app.dimensions.overall_from_dimensions(abs_dims)
      assert overall >= app.dimensions.overall_from_dimensions(dims)  # angle 포함 dict 보다 안 낮음
      assert "angle" in dims  # 표시 지표 유지
  - 한국어 주석으로 #8 역전 버그 / [[mode3-progress-not-similarity]] 출처를 짧게 표기.

주의: angle 이 낮게 나오는 seed 를 찾기 위해 `cd backend && python -m pytest
tests/test_pipeline_mode3.py -q` 로 반복 실행해 전제 가드(angle < min(abs_dims))가 통과하는
결정적 seed 를 확정한 뒤 커밋한다. 채점 수치를 임의로 채우지 말 것(의미있는 테스트만).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && python -m pytest tests/test_pipeline_mode3.py::test_low_similarity_does_not_invert_overall -x -q</automated>
  </verify>
  <done>새 테스트가 통과하고, 전제 가드(angle < min(line,stability))가 실제로 성립하는 결정적 seed 조합을 사용한다. overall == overall_from_dimensions(abs_dims) 이고 angle 포함 dict 의 overall 보다 낮지 않다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (없음 — 내부 순수 함수 변경) | `_mode3_comparison` 은 어댑터/S3/Firestore/네트워크 무관 순수 로직. 외부 입력 경계 미교차. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-01 | Tampering | overall 산출 로직 회귀 | mitigate | 회귀 테스트(Task 2)로 역전 방지 단언 + 기존 mode3 테스트(8개) 전체 통과 유지 |
| T-quick-02 | Information disclosure | dimension_scores 표시 | accept | angle 표시 지표는 기존대로 반환 — 동작 변경 없음, PII 무관 |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/test_pipeline_mode3.py -q` — 기존 8개 + 신규 회귀 전부 통과.
- `cd backend && python -m pytest tests/test_dimensions.py -q` — dimensions.py 미변경 확인(회귀 0).
- grep 가드: app.py 에 `overall_from_dimensions(dim_scores)` 0회, `overall_from_dimensions(abs_dims)` 2회.
</verification>

<success_criteria>
- mode3 두 번째+ overall 이 절대 차원(line/stability)만으로 산출됨 — angle/유사도 역전 제거.
- 이전=틀림·신규=올바름(angle 낮음) 케이스에서 overall 이 끌려 내려가지 않음(회귀 테스트 증명).
- dim_scores(angle 포함)는 반환값으로 유지, delta 및 dimensions.py 미변경.
- 모든 backend mode3/dimensions 테스트 통과.
</success_criteria>

<output>
Create `.planning/quick/260620-0r0-fix-mode3-score-inversion-bug-8-overall-/260620-0r0-SUMMARY.md` when done
</output>
