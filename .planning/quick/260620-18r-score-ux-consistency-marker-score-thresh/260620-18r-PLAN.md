---
phase: 260620-18r-score-ux-consistency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/app/analysis/result.tsx
  - app/src/components/KeypointOverlay.tsx
autonomous: true
requirements: [UX-SCORE-CONSISTENCY-02, UX-SCORE-CONSISTENCY-04]
must_haves:
  truths:
    - "세부 점수 섹션에 '안정성은 종합 점수에 직접 합산되지 않는 보조 지표'임을 알리는 캡션이 표시된다 (표시된 점수 값/계산 불변)"
    - "키포인트 마커는 IPSF 감점 허용오차(20°)를 넘은 관절에만 강조 표시된다 (감점 없는 관절에 빨강 마커 미표시)"
    - "npm run typecheck (tsc --noEmit) 가 clean 하게 통과한다"
  artifacts:
    - path: "app/src/app/analysis/result.tsx"
      provides: "세부 점수 보조지표 안내 캡션 (additive)"
      contains: "auxCaption"
    - path: "app/src/components/KeypointOverlay.tsx"
      provides: "마커 강조 임계 = IPSF tolerance 20°"
      contains: "KEYPOINT_DELTA_HIGHLIGHT_DEG = 20.0"
  key_links:
    - from: "app/src/components/KeypointOverlay.tsx KEYPOINT_DELTA_HIGHLIGHT_DEG"
      to: "backend dimensions.py _LINE_TOL_DEG = 20.0 (IPSF 허용오차)"
      via: "동일 상수 값 정합 (20.0)"
      pattern: "KEYPOINT_DELTA_HIGHLIGHT_DEG = 20.0"
---

<objective>
점수 UX 일관성 정리 — 사용자가 모순으로 읽는 두 표시를 정합한다. 점수 로직/표시 값은 전혀 바꾸지 않고 표시 일관성만 고친다.

- #4 (Task A): 세부 점수 섹션에서 "종합 100인데 동작 안정성 93?" 모순 인지를 없애기 위해, 안정성이 종합 점수에 직접 합산되지 않는 보조 지표임을 알리는 짧은 캡션을 추가만 한다 (Phase 19 D-01: overall = min-of-core(angle/line), stability 는 종합 입력에서 분리하되 표시는 유지).
- #2 (Task B): 키포인트 마커 강조 임계(KEYPOINT_DELTA_HIGHLIGHT_DEG = 10.0)가 IPSF 채점 허용오차(20°)보다 낮아, 감점 없는 관절(20° 이내)에도 빨강 마커가 떠 "100점인데 왜 빨강" 신뢰 모순이 발생한다. 임계를 IPSF tolerance 와 같은 값(20.0)으로 올려 마커가 실제 감점 기여 관절에만 뜨도록 정합한다.

Purpose: 분석 정확도 = 신뢰 (Core Value). 점수가 맞아도 표시가 모순이면 사용자는 결과를 불신한다. 로직은 이미 의도된 설계이므로 표시 일관성만 맞춘다.
Output: result.tsx 캡션 추가 + KeypointOverlay.tsx 상수/주석 변경. 두 개의 atomic commit.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kimtaesung/Dev/SunityMotion/CLAUDE.md
@/Users/kimtaesung/Dev/SunityMotion/app/CLAUDE.md

# 작업 대상 (정확히 이 두 파일만)
@/Users/kimtaesung/Dev/SunityMotion/app/src/app/analysis/result.tsx
@/Users/kimtaesung/Dev/SunityMotion/app/src/components/KeypointOverlay.tsx

# 근거 (read-only, 절대 수정 금지 — 상수 값/설계 확인용)
# backend/shared/python/sunity_shared/analysis/dimensions.py:
#   - _LINE_TOL_DEG = 20.0 (IPSF 각도 허용오차 20° — Task B 임계 정합 근거)
#   - 헤더: overall = min-of-core(angle/line), stability 종합 입력 분리·표시 유지 (Phase 19 D-01 — Task A 캡션 근거)
</context>

<constraints>
- 백엔드(dimensions.py / kismam.py / pipeline) 절대 수정 금지. 표시 점수 값/계산식 변경 금지. 두 task 모두 표시 일관성만.
- 한국어 user-facing 카피. 이모지 금지. 슬롭 금지.
- 테마 토큰만 사용 (하드코딩 컬러/spacing 금지). 기존 result.tsx 의 scoreCaption / segmentHintText 스타일 패턴(typography.* + colors.*)을 그대로 따른다.
- 주석에 근거 인용: design.md tone, D-12-C3 (Task B), Phase 19 D-01 (Task A).
- 검증 = npm run typecheck (tsc --noEmit) clean. JS test runner 없음(app/CLAUDE.md) → typecheck + grep 게이트가 verification.
- 가역성: Task B 는 한 상수 값 변경. 되돌리려면 KEYPOINT_DELTA_HIGHLIGHT_DEG 를 10.0 으로 복원하면 끝.
</constraints>

<tasks>

<task type="auto">
  <name>Task A: 세부 점수 보조지표 안내 캡션 추가 (#4, additive, 로직 0)</name>
  <files>app/src/app/analysis/result.tsx</files>
  <action>
세부 점수 섹션(영역 5, "세부 점수" sectionTitle 카드, 대략 876-897행)에 보조 지표 안내 캡션을 추가만 한다. 점수 값/계산/dims 매핑은 일절 손대지 않는다.

배치: dims.map(...) 으로 DimensionScoreRow 를 렌더하는 `<View style={styles.card}>` (대략 886-897행) 바로 아래에, 안정성(stability) 차원이 실제로 표시될 때만 `dims.includes('stability')` 조건으로 캡션을 조건부 렌더한다. mode3 first 처럼 stability 가 없으면 캡션을 띄우지 않아 불필요한 안내를 막는다.

카피(한국어, 이모지 금지, design.md tone — 기존 scoreCaption 745행 "촬영 노이즈와 측정 허용 범위가 있어..." 와 동일 톤): "동작 안정성은 자세 참고용 보조 지표예요. 종합 점수에는 직접 합산되지 않아요." (한 Text 로, 줄바꿈 강제 금지 — 자동 wrap.)

스타일: 새 스타일 토큰 auxCaption 을 StyleSheet 에 추가하되 기존 segmentHintText(typography.caption + colors.textSecondary + lineHeight 18) 패턴을 그대로 차용한다. 하드코딩 컬러 금지 — typography.* / colors.* 토큰만. marginTop 은 기존 파일이 sectionTitle 등에서 쓰는 8 리터럴 수준으로 맞춘다(컬러/radius 만 토큰 강제, 기존 파일 spacing 관례 따름).

주석: 캡션 위에 한 줄 — 근거가 Phase 19 D-01(overall = min-of-core(angle/line), stability 종합 입력 분리·표시 유지)임을 명시. 예: `{/* #4 표시 정합 — 안정성은 보조 지표(종합 입력 제외, 표시 유지). 근거 Phase 19 D-01 / dimensions.py 헤더 */}`

절대 금지: dimensionScores, OctagonScore(result.overallScore), DimensionScoreRow score prop, deltaFor, 어떤 점수 숫자도 변경 X. 새 state/effect/import 도입 X(dims, colors, typography 모두 이미 스코프 내). 캡션 Text + 스타일 1개 + 조건부 렌더만.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck 2>&1 | tail -5 && grep -q "auxCaption" src/app/analysis/result.tsx && grep -q "dims.includes('stability')" src/app/analysis/result.tsx && grep -vq "console.log" src/app/analysis/result.tsx && echo TASK-A-OK</automated>
  </verify>
  <done>typecheck clean. 세부 점수 카드 아래 stability 표시 시에만 보조지표 캡션이 렌더된다(auxCaption 스타일 + dims.includes('stability') 가드 존재). 점수 값/계산 코드 변경 0. atomic commit: docs/feat copy("display(result): 세부 점수 보조지표 안내 캡션 추가 (#4)").</done>
</task>

<task type="auto">
  <name>Task B: 키포인트 마커 임계 = IPSF tolerance 20° 정합 + 주석 갱신 (#2)</name>
  <files>app/src/components/KeypointOverlay.tsx</files>
  <action>
마커 강조 임계 상수 값과 그 근거 주석만 바꾼다. 강조 FEATURE / highlightedJoints 로직 / deltaThresholdDeg prop 메커니즘은 그대로 둔다.

1) 상수 값 변경 (32행): `export const KEYPOINT_DELTA_HIGHLIGHT_DEG = 10.0;` → `20.0`. 백엔드 dimensions.py 의 _LINE_TOL_DEG(20.0, IPSF 각도 허용오차 20°)와 명시적으로 같은 숫자로 맞춘다. 이로써 마커는 IPSF 감점 허용오차를 실제로 넘은(=감점에 기여하는) 관절에만 뜬다.

2) 31행 주석 갱신: 현재 "Phase 9 IPSF tolerance 20° 와 분리. UX 시각 강조 임계" 라는 분리 근거를 폐기하고, 새 rationale 로 교체 — 마커-점수 정합(marker-score consistency). 예: "D-12-C3 갱신 — IPSF 채점 허용오차(dimensions.py _LINE_TOL_DEG = 20°)와 동일 값으로 정합. 감점에 기여한 관절에만 강조 → '100점인데 빨강' 신뢰 모순 제거. (구 10° = early attention rationale 폐기)". design.md tone, 이모지 금지, 한국어 주석.

3) 83행 prop JSDoc 갱신: `/** default 10 (D-12-C3, KEYPOINT_DELTA_HIGHLIGHT_DEG). */` → default 값을 20 으로 정정하고 IPSF tolerance 정합 의도를 반영. 예: `/** default 20 (IPSF 허용오차 정합, KEYPOINT_DELTA_HIGHLIGHT_DEG). */`

가역성: 되돌리려면 상수를 10.0 으로 복원하면 끝(단일 값 변경). 주석은 코드 동작에 영향 없음.

절대 금지: highlightedJoints useMemo 로직(242행 Math.abs(cur - tgt) >= deltaThresholdDeg) 변경 X, prop 기본값 바인딩(162행 deltaThresholdDeg = KEYPOINT_DELTA_HIGHLIGHT_DEG) 구조 변경 X, 백엔드 X. 상수 값 1개 + 주석 2~3곳만.

테스트: app 에 이 상수를 참조하는 JS 테스트는 없다(grep 확인: node_modules 외 매치 0). 따라서 테스트 기대값 갱신 불필요 — 단, 실행자는 착수 시 `grep -rn KEYPOINT_DELTA_HIGHLIGHT_DEG app/src` 로 재확인하고, 혹 신규 테스트가 생겼다면 기대값을 20 으로 갱신한다.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck 2>&1 | tail -5 && grep -q "KEYPOINT_DELTA_HIGHLIGHT_DEG = 20.0" src/components/KeypointOverlay.tsx && ! grep -q "KEYPOINT_DELTA_HIGHLIGHT_DEG = 10.0" src/components/KeypointOverlay.tsx && grep -rn "KEYPOINT_DELTA_HIGHLIGHT_DEG" src/ | grep -v "node_modules" && echo TASK-B-OK</automated>
  </verify>
  <done>typecheck clean. KEYPOINT_DELTA_HIGHLIGHT_DEG === 20.0 (= 백엔드 _LINE_TOL_DEG). 31행/83행 주석이 마커-점수 정합 rationale 로 갱신됨. 강조 로직/prop 메커니즘 불변. 비-node_modules 테스트가 상수를 참조하면 기대값 20 으로 갱신됨(현재 없음). atomic commit: "fix(overlay): 마커 강조 임계를 IPSF 허용오차 20°와 정합 (#2)".</done>
</task>

</tasks>

<verification>
- npm run typecheck (tsc --noEmit) clean — 유일한 static gate (app 에 JS test runner 없음, app/CLAUDE.md).
- grep 게이트:
  - result.tsx 에 auxCaption 스타일 + dims.includes('stability') 가드 존재.
  - KeypointOverlay.tsx 에 `KEYPOINT_DELTA_HIGHLIGHT_DEG = 20.0` 존재, `= 10.0` 부재.
  - 비-node_modules 코드에서 상수 참조 시 모순 없음.
- 점수 로직 불변 확인: backend/ 변경 0 (git diff backend/ 비어있음). dimensionScores / overallScore 표시 값 변경 0.
</verification>

<success_criteria>
- 세부 점수 섹션이 안정성을 보조 지표로 명확히 안내(종합 직접 합산 아님) — 표시 점수 값/계산 불변.
- 키포인트 마커가 IPSF 허용오차(20°) 초과 관절에만 강조 — 감점 없는 관절 빨강 마커 제거, "100점인데 빨강" 모순 해소.
- typecheck clean. 백엔드 변경 0. 두 개의 atomic commit.
</success_criteria>

<output>
Create `.planning/quick/260620-18r-score-ux-consistency-marker-score-thresh/260620-18r-SUMMARY.md` when done.
</output>
