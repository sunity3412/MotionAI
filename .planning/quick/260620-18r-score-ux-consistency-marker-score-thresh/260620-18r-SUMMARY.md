---
phase: 260620-18r-score-ux-consistency
plan: 01
subsystem: app (React Native / TypeScript)
tags: [score-ux, display-consistency, keypoint-overlay, ipsf-tolerance]
requires: []
provides:
  - "세부 점수 보조지표 안내 캡션 (additive, 표시 정합)"
  - "마커 강조 임계 = IPSF 허용오차 20° 정합 (감점 기여 관절만 강조)"
affects:
  - app/src/app/analysis/result.tsx
  - app/src/components/KeypointOverlay.tsx
tech-stack:
  added: []
  patterns:
    - "표시 일관성 정리 — 점수 로직/값 불변, 캡션/임계만 조정"
key-files:
  created: []
  modified:
    - app/src/app/analysis/result.tsx
    - app/src/components/KeypointOverlay.tsx
decisions:
  - "마커 강조 임계를 백엔드 _LINE_TOL_DEG(20°)와 동일 값으로 정합 — 마커-점수 정합이 분리(early-attention 10°)보다 신뢰에 유리"
  - "result.tsx 인라인 주석의 '10°' 표현은 Task B 파일 스코프(KeypointOverlay.tsx) 밖이라 미수정 — additive-only 제약 준수"
metrics:
  duration: ~10m
  completed: 2026-06-20
---

# Phase 260620-18r Plan 01: Score UX Consistency (Marker-Score Threshold) Summary

점수 로직/표시 값을 전혀 바꾸지 않고, 사용자가 모순으로 읽던 두 표시(세부 점수 안정성 / 키포인트 마커)를 표시 일관성만 정합했다.

## What Was Done

### Task A — 세부 점수 보조지표 안내 캡션 추가 (#4, additive)
- `app/src/app/analysis/result.tsx`: 세부 점수 카드 바로 아래에 `dims.includes('stability')` 가드로 보조지표 캡션을 조건부 렌더(mode3 first 처럼 stability 미표시 시 캡션도 미표시).
- 카피(한국어, 이모지 없음): "동작 안정성은 자세 참고용 보조 지표예요. 종합 점수에는 직접 합산되지 않아요."
- 새 스타일 `auxCaption` 추가 — 기존 `segmentHintText` 패턴(typography.caption + colors.textSecondary + lineHeight 18) 차용, 하드코딩 컬러/spacing 없음.
- 점수 값/계산/dims 매핑/OctagonScore/DimensionScoreRow 전부 불변. 새 state/effect/import 없음.
- 근거 주석: Phase 19 D-01 (overall = min-of-core(angle/line), stability 종합 입력 분리·표시 유지).
- Commit: `5b5fb81` — display(result): 세부 점수 보조지표 안내 캡션 추가 (#4)

### Task B — 키포인트 마커 임계 = IPSF tolerance 20° 정합 (#2)
- `app/src/components/KeypointOverlay.tsx`: `KEYPOINT_DELTA_HIGHLIGHT_DEG` 10.0 → 20.0 (백엔드 dimensions.py `_LINE_TOL_DEG` = 20° IPSF 각도 허용오차와 동일 값).
- 31행 주석을 분리(early-attention) rationale 폐기 → marker-score consistency rationale 로 교체.
- prop JSDoc `default 10` → `default 20 (IPSF 허용오차 정합)` 정정.
- highlightedJoints useMemo 로직 / `deltaThresholdDeg` prop 기본값 바인딩 메커니즘 불변. 단일 상수 값 변경으로 가역.
- 비-node_modules 코드에서 이 상수 참조는 KeypointOverlay.tsx 내부뿐 — 갱신할 테스트 기대값 없음.
- Commit: `4675327` — fix(overlay): 마커 강조 임계를 IPSF 허용오차 20°와 정합 (#2)

## Verification

- `npm run typecheck` (tsc --noEmit) clean, exit 0 (양 task 모두).
  - 주: 워크트리에 `node_modules` 가 없어, 메인 체크아웃의 설치된 deps 를 가리키는 임시 심볼릭 링크로 typecheck 를 수행했다(패키지 설치 아님). 검증 후 심링크 제거, 커밋에 포함되지 않음.
- grep 게이트:
  - result.tsx 에 `auxCaption` + `dims.includes('stability')` 존재, `console.log` 부재.
  - KeypointOverlay.tsx 에 `KEYPOINT_DELTA_HIGHLIGHT_DEG = 20.0` 존재, `= 10.0` 부재.
  - 상수 참조는 KeypointOverlay.tsx 내부뿐 (모순 없음).
- `git diff --stat backend/` 비어 있음 — 백엔드 변경 0.

## Deviations from Plan

None — 계획대로 실행. result.tsx 영역 2의 인라인 주석에 있는 "delta ≥ 10°" 표현은 Task B 파일 스코프(KeypointOverlay.tsx) 밖이며 result.tsx 는 additive-only 제약 대상이라 의도적으로 미수정(표시/동작에 영향 없는 prose 주석).

## Known Stubs

None.

## Self-Check: PASSED
- FOUND: app/src/app/analysis/result.tsx (auxCaption, dims.includes('stability'))
- FOUND: app/src/components/KeypointOverlay.tsx (KEYPOINT_DELTA_HIGHLIGHT_DEG = 20.0)
- FOUND commit: 5b5fb81
- FOUND commit: 4675327
- backend diff: empty (no changes)
