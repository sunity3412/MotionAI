---
phase: 24-transparent-deduction-scoring
plan: 05-A
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/shared/python/sunity_shared/analysis/deduction_engine.py
  - backend/tests/test_deduction_engine.py
  - backend/tests/test_pipeline_deduction_seam.py
autonomous: true
requirements: [ND-01, ND-05, ND-06, ND-07]

must_haves:
  truths:
    - "quantification unavailable + 측정 각도 seed(md) 보유 시 granular 감점 record 가 산출된다 (dimension_overall_fallback 아님)"
    - "quantification unavailable + 측정 seed 전무(md={}) 시 dimension_overall_fallback 가 보존된다"
    - "legacy(quantification=None, md=None) 경로는 여전히 dimension_overall_fallback 로 폴백한다"
    - "low_alignment seam 테스트가 production wiring(_quant('unavailable'))과 일치하면서 leg_extension record 를 낸다"
    - "band grep(apply_downward_cap|SEVERITY_CAP|capApplied) = 0 (재calibration 없음)"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/deduction_engine.py"
      provides: "tally() — 폴백 결정을 criterion 선택 뒤로 이동, measured-seed 소비"
      contains: "reach_substrate_unavailable_low_alignment"
    - path: "backend/tests/test_deduction_engine.py"
      provides: "엔진 단위 회귀 가드 6종 + 기존 unavailable 테스트 reconcile"
    - path: "backend/tests/test_pipeline_deduction_seam.py"
      provides: "low_alignment seam false-green 교정 + reach-only 신규 테스트"
  key_links:
    - from: "deduction_engine.tally"
      to: "ipsf_criteria.criteria_from_measured_deviations"
      via: "criterion selection BEFORE fallback decision"
      pattern: "criteria_from_measured_deviations"
---

<objective>
`deduction_engine.tally()` 의 unavailable-fallback 게이트가 정렬-독립 measured-seed(RTMW 각도 편차)를
문 앞에서 폐기하는 결함을 수정한다. 폴백 결정을 criterion 선택 *뒤로* 옮겨, quantification 이
unavailable 이어도 측정 각도 seed 가 있으면 per-criterion granular 감점을 산출하게 한다. 폴백은
quant 불가 AND 활성 criterion 0(양쪽 substrate 빔)일 때만 발화.

Purpose: 24-04 가 apply seam 두 곳을 고쳐 low_alignment 를 tally-eligible 로 만들고
measured_deviations 를 전달했으나, 엔진 게이트 한 레이어가 빠져 fault clip 전량이
dimension_overall_fallback 로 붕괴했다(6/26 재-sweep). 이 plan 이 그 마지막 레이어를 닫는다.

Output:
- deduction_engine.tally 구조 변경 (단일 순수 함수, numpy 외 의존 0)
- coverage_gap 1종(reach_substrate_unavailable_low_alignment) 추가 — reach 칸 측정 불가를 투명 노출
- 엔진 단위 회귀 가드 6종 + 기존 unavailable 테스트 2종 reconcile
- low_alignment seam false-green fixture 교정 + reach-only 신규 seam 테스트

Scope 고정: 24-05-FIX-DESIGN 가 source of truth. slope/cap/tolerance 재calibration 금지,
collect-side / apply seam / assess_alignment_confidence / kip-up·(B) Gemini 경로 전부 OUT.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/24-transparent-deduction-scoring/24-05-FIX-DESIGN-measured-seed-engine-gate.md
@backend/shared/python/sunity_shared/analysis/deduction_engine.py
@backend/tests/test_deduction_engine.py
@backend/tests/test_pipeline_deduction_seam.py

# CLAUDE.md 준수: 이모지 금지, 순수 함수, 의미있는 테스트만, Korean 주석 + 스펙 섹션 인용(ND-01~07).
</context>

<tasks>

<task type="auto">
  <name>Task 1: tally() 폴백 게이트를 criterion 선택 뒤로 이동 + reach coverage_gap 추가</name>
  <files>backend/shared/python/sunity_shared/analysis/deduction_engine.py</files>
  <action>
24-05-FIX-DESIGN §3-2 를 정확히 구현한다 (재해석/확장 금지). `tally()` 의 현재 구조
`(1) unavailable-fallback → (2) criterion 선택 → (3) 감점` 을
`(2) criterion 선택 먼저 → (1') 조건부 폴백 → (3) 감점` 으로 재배치:

1. 함수 진입부에서 `quant_unavailable = quantification is None or status == "unavailable"` 를 계산하되,
   현재 line 162-184 의 early-return 블록을 제거하지 말고 *뒤로 이동*한다.
2. criterion 선택 블록(현재 line 186-201: `seeded`/`pointed`/`differences`/`activated` +
   leg·arm/line cross-exclusion)을 폴백 결정보다 *먼저* 실행한다.
3. 폴백 블록(현재 line 162-184 본문 그대로 — dimension_overall_fallback record,
   `_collect_coverage_gaps`, `fallback="quantification_unavailable"`)을 criterion 선택 뒤로 옮기고
   가드를 `if quant_unavailable and not activated:` 로 바꾼다. 본문 로직·record 필드·rule_id
   (`quantification_unavailable_dimension_overall`)·final 산식(`max(0, round(dim))`)은 UNCHANGED.
4. quant unavailable BUT activated(각도 seed 존재) 경로: 폴백을 건너뛰고, per-criterion 감점
   루프 진입 전에 coverage_gaps 에 reach 칸 측정 불가 entry 1개를 append 한다. dict 키는
   `_gap_to_dict` 와 동일: faultType="body_relative_reach", reason="quantification_unavailable",
   bodyPart="reach", faultState=None, keypointSet="body_relative_reach",
   ruleId="reach_substrate_unavailable_low_alignment" (ND-06/07 투명 추적성).
5. per-criterion 감점 루프(현재 line 203-232)와 final 산식(line 234-240)은 그대로 두고
   폴백 분기 뒤에 둔다. reach criterion 은 `_criterion_deduction`→`_notch_shortfall` 가 notches
   부재로 None 반환 → 자연 honest-0 (별도 처리 불필요, §3-3).

불변 가드: 순수 함수 유지 — numpy 외 import 금지(boto3/Gemini/network/firestore 없음). 결정적.
slope/cap/tolerance/baseline 상수 일절 손대지 않는다 (라우팅/소비 fix, NOT 재calibration).
모듈 상단 docstring 에 변경 의도를 Korean 1-2줄로 보강(ND-01/ND-06 인용). 코드 fence 없이 prose 주석만.
  </action>
  <verify>
    <automated>cd backend && PYTHONPATH=shared/python python -c "import ast,sys; ast.parse(open('shared/python/sunity_shared/analysis/deduction_engine.py').read()); print('parse ok')" && PYTHONPATH=shared/python python -c "import inspect; from sunity_shared.analysis import deduction_engine as d; src=inspect.getsource(d.tally); i_sel=src.index('criteria_from_measured_deviations'); i_fb=src.index('quant_unavailable and not activated'); assert i_sel < i_fb, 'fallback must come AFTER criterion selection'; assert 'reach_substrate_unavailable_low_alignment' in src; print('structure ok')"</automated>
  </verify>
  <done>tally() 가 criterion 선택을 먼저 수행하고, 폴백은 `quant_unavailable and not activated` 일 때만 발화하며, quant 불가+각도 seed 시 reach_substrate_unavailable_low_alignment coverage_gap 을 방출한다. import 추가 0, slope/cap/tol 상수 미변경.</done>
</task>

<task type="auto">
  <name>Task 2: 엔진 단위 회귀 가드 6종 추가 + 기존 unavailable 테스트 2종 reconcile</name>
  <files>backend/tests/test_deduction_engine.py</files>
  <action>
24-05-FIX-DESIGN §5-1 의 단위 테스트 6종을 추가한다 (기존 헬퍼 `_tally`, `_measured`,
`_unavailable_quant`, `_ctx`, `deduction_engine.tally` 재사용):

1. CORE 회귀 가드: `tally(quant=_unavailable_quant(), md={leg_extension:30})` →
   leg_extension record 존재 AND `breakdown.fallback != "quantification_unavailable"` AND final < 100.
2. 폴백 보존: `tally(quant=_unavailable_quant(), md={})` → dimension_overall_fallback record 1개 +
   fallback == "quantification_unavailable".
3. legacy 보존: `tally(quantification=None, measured_deviations=None)` → dimension_overall_fallback 유지.
4. 단조성: `md={leg:20}` 의 총 감점(Σ points 절대값) < `md={leg:40}` 의 총 감점 (둘 다 unavailable quant, ND-07).
5. 추적성: quant unavailable + 각도 seed(예: leg=30) → coverage_gaps 에 ruleId
   "reach_substrate_unavailable_low_alignment" 포함.
6. 결정성: 동일 입력 2회 호출 → `to_dict()` byte-동일 (records/coverageGaps/final 일치).

reconcile (필수 — 이게 빠지면 suite 가 red): 기존 `test_unavailable_falls_back_not_100`(현 line 354)과
`test_unavailable_emits_traceable_record`(현 line 362)는 `_measured(leg=40.0)` + unavailable 으로
폴백을 기대한다. fix 후 비-빈 각도 seed 는 granular 경로로 가므로 이 단언은 깨진다. 두 테스트의
measured 인자를 `_measured()`(빈 dict)로 바꿔 "양쪽 substrate 빔 → 폴백" 계약을 정확히 보존한다
(seam false-green 과 동일 class 의 engine-level 교정). 테스트 docstring 에 빈-seed 가 폴백 조건임을 1줄 명시.

CLAUDE.md: 의미있는 단언만(수치 채우기 금지), Korean 주석으로 ND-05/06/07 인용. 이모지 금지.
  </action>
  <verify>
    <automated>cd backend && PYTHONPATH=shared/python python -m pytest tests/test_deduction_engine.py -q</automated>
  </verify>
  <done>test_deduction_engine.py 전체 green. 신규 6 테스트가 measured-seed 소비/폴백 보존/단조성/추적성/결정성을 가드하고, 기존 두 unavailable 테스트가 빈-seed 폴백 계약으로 reconcile 됐다.</done>
</task>

<task type="auto">
  <name>Task 3: low_alignment seam false-green fixture 교정 + reach-only 신규 seam 테스트</name>
  <files>backend/tests/test_pipeline_deduction_seam.py</files>
  <action>
24-05-FIX-DESIGN §5-2 를 구현한다.

1. false-green 교정: production low_alignment 경로는 frame_pairs=[] → quantification UNAVAILABLE 인데,
   현 `test_low_alignment_measured_deduction_applied`(line 308)·`test_low_alignment_clean_geometry_not_applicable`(line 341)·
   `test_low_alignment_no_fabricated_gemini_faults`(line 357)·`test_low_alignment_does_not_regress_sibling_passthrough`(line 373)
   은 `_quant("available")` 를 넘겨 production wiring 과 갈라진다. 이들 low_alignment 테스트의
   `_quant("available")` → `_quant("unavailable")` 로 교정한다 (sibling passthrough 테스트도 동일 — 일관성).
   교정 후:
   - `test_low_alignment_measured_deduction_applied`: 여전히 leg_extension record 가 나와야 한다
     (= Task 1 fix 가 통과시키는 핵심). line 335 의 `assert "dimension_overall" not in sources` 단언은
     UNCHANGED 유지 — 각도 seed 가 살아 dimension_overall 폴백을 안 탄다.
   - `test_low_alignment_clean_geometry_not_applicable`(md={}): unavailable + 빈 seed → 폴백 경로.
     이 테스트의 단언이 unavailable-폴백 모양과 충돌하면(예: records == [] 기대) production wiring 에
     맞춰 조정하되, "측정 가능한 편차 없음 → 위양성 fabricate 0" 의 의도는 보존한다.
   - `test_low_alignment_no_fabricated_gemini_faults`(md={leg:30}): unavailable 에서도 leg_extension 만,
     split_angle 없음 — 객관성 단언 유지.
2. 신규 seam 테스트 추가: low_alignment + `_quant("unavailable")` + reach-only(measured_deviations={}) →
   `_apply_vision_veto_from_context` 결과가 dimension_overall_fallback(fallback=="quantification_unavailable",
   record 1개) + coverageGaps 에 reach_substrate_unavailable_low_alignment 포함(honest 한계 명시).

주의: 각 테스트의 단언을 production-shape 에 맞춰 최소 수정만. 새 점수 수치를 박지 말 것(curve-fit 금지).
Korean 주석으로 production wiring 일치 이유 명시(§2-4 false-green 교훈 인용). 이모지 금지.
  </action>
  <verify>
    <automated>cd backend && PYTHONPATH=shared/python python -m pytest tests/test_pipeline_deduction_seam.py -q</automated>
  </verify>
  <done>seam 테스트 전체 green. low_alignment 테스트가 production 과 동일하게 unavailable quant 를 쓰면서도 leg_extension record 를 내고(fix 검증), reach-only 신규 테스트가 폴백+coverage gap 을 가드한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | 순수 함수 내부 라우팅 변경. 신규 외부 입력 경계 없음 — quantification/measured_deviations 는 이미 검증된 upstream substrate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24A-01 | Tampering | tally() final 산식 | mitigate | final = max(0, round(100+Σpoints)) UNCHANGED; slope/cap/tol 상수 미변경 — band grep 게이트로 강제 |
| T-24A-02 | Information disclosure | coverage_gap 노출 | accept | reach 측정 불가를 honest 0 + coverage_gap 으로 투명 노출(ND-06) — 의도된 추적성, 민감정보 없음 |
| T-24A-SC | Tampering | 패키지 설치 | accept | 설치 task 0 (numpy 외 의존 추가 없음). 공급망 표면 변화 없음 |
</threat_model>

<verification>
로컬 게이트 (pod/GPU 불필요):

1. 엔진 단위: `cd backend && PYTHONPATH=shared/python python -m pytest tests/test_deduction_engine.py -q` green
2. seam 통합: `cd backend && PYTHONPATH=shared/python python -m pytest tests/test_pipeline_deduction_seam.py -q` green
3. Phase 24 + 20/23 타깃 suite green:
   `cd backend && PYTHONPATH=shared/python python -m pytest tests/test_deduction_engine.py tests/test_phase24_gates.py tests/test_pipeline_deduction_seam.py tests/test_vision_veto.py tests/test_assert_stillframe_veto_gate.py tests/test_pipeline_vision_gate.py -q`
4. band grep = 0 (재calibration·고정밴드 부재):
   `grep -rEn "apply_downward_cap|SEVERITY_CAP|capApplied" backend/shared/python backend/functions | wc -l` → 0
</verification>

<success_criteria>
- tally() 가 quant unavailable + 각도 seed 시 granular record 를 내고, quant unavailable + 빈 seed 와 legacy(None,None) 는 dimension_overall_fallback 를 보존한다.
- coverage_gap reach_substrate_unavailable_low_alignment 가 quant 불가 + 각도 seed 경로에서 방출된다.
- 엔진 단위 6종 + reconcile 2종, seam 교정 4종 + 신규 1종 전부 green.
- Phase 24 + 20/23 타깃 suite green. band grep = 0.
- deduction_engine.py 에 numpy 외 import 추가 0, slope/cap/tolerance 상수 미변경.
</success_criteria>

<output>
Create `.planning/quick/260626-e5k-phase-24-plan-05-a-fix-deduction-engine-/260626-e5k-SUMMARY.md` when done.
각 task 는 원자적 커밋 (Task 1 = engine fix, Task 2 = engine tests, Task 3 = seam fixture).
docs 커밋·ROADMAP 업데이트는 하지 않는다 (orchestrator 처리).
</output>
