---
phase: 24-transparent-deduction-scoring
plan: 260626-f3u
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/shared/python/sunity_shared/analysis/vision_veto.py
  - backend/functions/pipeline/app.py
  - backend/tests/test_vision_veto.py
  - backend/evals/phase24/run_sweep.py
  - backend/evals/phase24/probe_kip_up_gemini.py
autonomous: true
requirements: ["24-06-§3-DIAG"]

must_haves:
  truths:
    - "low_alignment_confidence bail 가 alignment 텔레메트리를 보존한다 (collect-side 에서 drop 안 함)"
    - "visionVeto audit 가 applied/not_applicable 양쪽에서 alignment 진단 sub-dict 를 노출한다 (관찰 전용, score 불변)"
    - "sweep 리포트가 clip 별 visionVeto(collectionStatus + alignment.adoption)를 캡처한다"
    - "kip-up still-pair 에 Gemini probe 를 alignment bail 우회로 직접 호출해 '결함을 볼 수 있나'를 확정한다 (pod 전용)"
    - "어떤 scoring/tally 출력도 변하지 않는다 (밴드 grep 0, 측정-감점 경로 무수정)"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/vision_veto.py"
      provides: "alignment_summary 순수 헬퍼 + to_audit_dict alignment 진단 방출"
      contains: "def alignment_summary"
    - path: "backend/evals/phase24/probe_kip_up_gemini.py"
      provides: "kip-up Gemini still-pair probe (bail 우회, pod-only)"
      contains: "assess_fault_context"
  key_links:
    - from: "app.py _collect_vision_fault_context low_alignment bail"
      to: "VisionFaultContext.alignment"
      via: "_ctx(..., alignment=alignment)"
      pattern: "low_alignment_confidence.*alignment=alignment"
    - from: "vision_veto.to_audit_dict"
      to: "audit['alignment']"
      via: "alignment_summary(self.alignment)"
      pattern: "alignment_summary"
    - from: "probe_kip_up_gemini.py"
      to: "gemini_vision_scorer.assess_fault_context"
      via: "still-pair direct call bypassing bail"
      pattern: "assess_fault_context"
---

<objective>
Phase 24 (B) 진단 계측. 24-06 DIRECTION §3 이 옵션 선택(B1/B2/B3)의 선결 조건으로 지목한
두 미지수를 다음 pod-run 에서 캡처한다: (1) kip-up 이 정확히 어느 조건으로
low_alignment_confidence 를 발화하나 (distance / visibility / localPathCount), (2) 선택된
still-pair 에서 Gemini 가 kip-up 결함을 실제로 짚을 수 있나 (support 게이트 통과 여부).

이 plan 은 **순수 추가 관찰 + standalone probe** 다. 채점/tally/score 출력은 한 바이트도
바꾸지 않는다 — alignment 데이터는 관찰 메타데이터일 뿐이다 (24-06 §3, §5).

Purpose: belle 의 옵션 확정을 위한 텔레메트리를 확보한다 — 추정 대신 측정.
Output: alignment 진단을 보존·방출하는 collect/apply/audit 경로 + sweep 의 clip 별 visionVeto
캡처 + kip-up Gemini probe 스크립트.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/24-transparent-deduction-scoring/24-06-DIRECTION-kip-up-gemini-path.md
@./CLAUDE.md

# 코드 진실 (이미 확인됨 — 재독 최소화):
# - app.py:1753-1894 _collect_vision_fault_context (bail @1829-1833, adoption returns @1870-1893)
# - app.py:2298-2312 _apply_vision_veto_from_context not_applicable early-return (ctx.alignment 가용)
# - app.py:1662-1709 _build_selected_frame_pair
# - vision_veto.py:572-648 to_trace_dict / to_audit_dict, :520-549 VisionFaultContext, :751-815 assess_alignment_confidence, :822 select_worst_frame_candidates
# - gemini_vision_scorer.py:894-924 assess_fault_context 시그니처, :107 VETO_PART_SCOPES
# - run_sweep.py:1-26 docstring(run command), :64-106 _run_member
@backend/functions/pipeline/app.py
@backend/shared/python/sunity_shared/analysis/vision_veto.py
@backend/evals/phase24/run_sweep.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: alignment 텔레메트리 보존·방출 (collect bail + apply not_applicable + audit)</name>
  <files>backend/shared/python/sunity_shared/analysis/vision_veto.py, backend/functions/pipeline/app.py, backend/tests/test_vision_veto.py</files>
  <action>
관찰 전용 alignment 진단을 3개 seam 에 추가한다. 모두 메타데이터 — tally/score 무수정 (24-06 §3, §5).

1) vision_veto.py — 순수 헬퍼 `alignment_summary(alignment: dict | None) -> dict | None` 를
to_audit_dict 위에 추가한다. alignment 가 비면 None (audit 에 키 미방출), 아니면 관찰 키만 추린
dict 반환: {adoption, distance, visibility, localPathCount, refFramePresent} — assess_alignment_confidence
반환 키(vision_veto.py:807-815)와 정합. Korean 도크스트링에 "관찰 전용 — 채점/tally 무관 (24-06 §3)"
명시. 새 import 금지 (dict-shaping 만 — analysis core 순수 보존).

2) vision_veto.py `to_audit_dict` — `audit["collectionStatus"] = self.collection_status` 직후,
`if final_status == "applied":` 분기 **이전에** alignment 진단을 붙인다:
collectionStatus 처럼 applied/not_applicable **둘 다** 방출하기 위해 분기 밖에 둔다.
`_align = alignment_summary(self.alignment)`; `if _align is not None: audit["alignment"] = _align`.
주석에 "24-06 §3 진단 — applied/not_applicable 양쪽, score 무관" 인용.

3) app.py `_collect_vision_fault_context` low_alignment bail (현재 ~1833
`return _ctx("low_alignment_confidence", frame_pairs=[])`): `alignment=alignment` 인자를 추가한다.
다른 adoption return(1870/1874/1877/1885)은 이미 alignment 를 넘긴다 — 이 bail 만 drop 했다.
_ctx 헬퍼는 이미 alignment kwarg 를 받는다(app.py:1784). 한 줄 보강.

4) app.py `_apply_vision_veto_from_context` not_applicable early-return (현재 ~2308
`return {**score_result, "deductionBreakdown": breakdown.to_dict(), "visionVeto": {"status": "not_applicable"}}`):
to_audit_dict 를 거치지 않는 직접 dict 이므로 동일 alignment 요약을 `ctx.alignment` 에서 직접 붙인다.
`na_audit = {"status": "not_applicable"}`; `_align = vision_veto.alignment_summary(getattr(ctx, "alignment", None))`;
`if _align is not None: na_audit["alignment"] = _align`; visionVeto 에 na_audit 사용.
vision_veto 는 이 함수 스코프에 이미 in-scope (app.py:2291 vision_veto.fault_joints_from_differences 사용).

5) test_vision_veto.py — 단위 테스트 2~3개 추가 (순수, pod 무관):
(a) alignment_summary 직접: 빈 dict/None → None, 채워진 dict → 관찰 키만 포함.
(b) to_audit_dict 가 ctx.alignment 존재 시 alignment sub-dict 방출 — final_status="not_applicable"
와 "applied" 양쪽에서 audit["alignment"]["adoption"] 확인 (low_alignment_confidence + distance/visibility).
(c) ctx.alignment={} 면 "alignment" 키 미방출.
기존 to_audit_dict shape assert(test_vision_veto.py:341, 367)는 alignment={} 라 무영향 — 깨지면
정확 dict-equality assert 를 superset assert 로 조정(키 추가는 비파괴). 정확-equality assert 가
다른 곳에 있으면 새 alignment 키를 반영해 reconcile.
  </action>
  <verify>
    <automated>cd backend && PYTHONPATH=shared/python:. python3 -m pytest tests/test_vision_veto.py tests/test_pipeline_vision_gate.py -q && grep -rEn "apply_downward_cap|SEVERITY_CAP|capApplied" shared/python functions | grep -v '^Binary' | wc -l | grep -qx 0 && python3 -c "import ast; ast.parse(open('functions/pipeline/app.py').read()); ast.parse(open('shared/python/sunity_shared/analysis/vision_veto.py').read())"</automated>
  </verify>
  <done>alignment_summary 헬퍼 존재 + to_audit_dict 가 alignment present 시 진단 sub-dict 를 applied/not_applicable 양쪽 방출 + collect bail 과 apply not_applicable 가 alignment 텔레메트리 surface + 신규 단위 테스트 green + 기존 비전 게이트 스위트 green + 밴드 grep 0.</done>
</task>

<task type="auto">
  <name>Task 2: eval-side 진단 계측 (sweep visionVeto 캡처 + kip-up Gemini probe)</name>
  <files>backend/evals/phase24/run_sweep.py, backend/evals/phase24/probe_kip_up_gemini.py</files>
  <action>
eval 하니스에 관찰 텔레메트리를 추가한다. 채점 무관 — 리포트/probe 출력만.

A) run_sweep.py `_run_member` (현재 ~86-106):
- `bd = r.get("deductionBreakdown")` 근처에 `vv = r.get("visionVeto")` 추가.
- rec dict 에 `"visionVeto": vv` 추가 (clip 별 리포트에 박제).
- print per-clip line 에 collectionStatus + alignment.adoption 포함:
  `cs = vv.get("collectionStatus") if isinstance(vv, dict) else None`;
  `aa = (vv.get("alignment") or {}).get("adoption") if isinstance(vv, dict) else None`;
  기존 print 의 끝에 `vv={cs}/{aa}` 토큰 추가. (Task 1 이후 not_applicable/applied audit 가
  alignment 를 담으므로 sweep 가 kip-up 의 정확한 발화 조건을 캡처한다.)
- 24-06 §3 인용 주석 1줄.

B) 신규 standalone 스크립트 `backend/evals/phase24/probe_kip_up_gemini.py`:
헤더 도크스트링에 run_sweep.py:18-25 와 **동일 스타일**의 정확한 pod run command 를 박제
(source aws_env.sh + CEREBRAS_KEY_PARAM/GEMINI_COACH_ENABLED/RTMW_ONNX_PATH/YOLOX_ONNX_PATH/
RTMW_DEVICE/FIREBASE_SA_PATH/RECOGNIZER_BACKEND + GEMINI_API_KEY SSM fetch +
`PYTHONPATH=shared/python:. python3 evals/phase24/probe_kip_up_gemini.py`). 도크스트링 첫 줄에
"pod 전용 — GPU/Gemini 로컬 부재 → 로컬 실행 금지" + 24-06 §3 인용 + 객관성
([[analysis-objectivity-no-human-scores]]) 명시.

스크립트 로직 (kip-up fault + correct 대비):
1. run_sweep.py 의 importlib `_load_pipeline()` 패턴으로 pipeline 모듈 로드 (+ _ensure_adapters).
   `from sunity_shared.analysis import vision_veto, gemini_vision_scorer`,
   `from sunity_shared import firestore_admin as fa, models`.
2. alignment bail 을 우회하기 위해 `pipeline._collect_vision_fault_context` 를 capturing wrapper 로
   monkeypatch 한다. wrapper(**kwargs)는 (a) 캡처된 kwargs(local_video_path/reference_video_path/
   reference_dtw_match/profile/pose_frames/reference_pose_frames)로 inline probe 를 수행하고
   (b) 원본 collect 를 호출해 _process 를 정상 종료시킨다. **inline 으로** 도는 이유: _process
   가 return 후 다운로드 영상 임시 파일을 unlink 하므로, 유효한 local path 가 살아있는 wrapper
   내부에서 probe 를 끝내야 한다.
3. inline probe (_do_probe) = app.py:1806-1850 의 selection→pair→alignment 를 **bail 없이** 재현:
   `selection = vision_veto.select_worst_frame_candidates(profile)`;
   `at = vision_veto.worst_pose_timestamp(profile)`; `user_frame_idx = int(round((at or 0.0)*9.0))`;
   `pair = pipeline._build_selected_frame_pair(user_video_path=local_video_path,
   reference_video_path=reference_video_path, reference_dtw_match=reference_dtw_match,
   user_frame_idx=user_frame_idx, pose_frames=pose_frames, reference_pose_frames=reference_pose_frames)`;
   visibility=pair.student_confidence; `alignment = vision_veto.assess_alignment_confidence(
   match=reference_dtw_match, selected_user_frame=user_frame_idx, keypoint_visibility=visibility)`.
4. pair is None 이거나 reference_dtw_match None 이면 graceful skip 출력 후 return.
5. Gemini 직접 호출 (bail 우회) — app.py:1842 와 **동일 시그니처**:
   `rich = gemini_vision_scorer.assess_fault_context(pair.student_frame_path,
   pair.reference_frame_path, at_seconds=at, part_scopes=list(gemini_vision_scorer.VETO_PART_SCOPES),
   frame_indices=[pair.user_frame_idx], reference_frame_indices=[pair.ref_frame_idx],
   selector_version=selection.get("selector_version"))`.
6. 출력 (label=fault/correct 별):
   (a) alignment dict 전체 (adoption/distance/visibility/localPathCount/refFramePresent) — 프로덕션이
       **왜** bail 하는지 확정.
   (b) Gemini differences 반환 여부 (rich.get("status"), verdict 유무).
   (c) support 게이트 통과 여부 = `supported = rich.get("supported_differences") or []`; non-empty 여부.
   (d) 각 difference 의 body_part / fault_state (supported 각 항목 + verdict.differences).
   (e) 명시 verdict 라인: supported non-empty 이고 fault_state 가 결함이면
       "Gemini CAN see kip-up fault from still-pair", 아니면 "Gemini CANNOT see kip-up fault from still-pair".
7. finally: `for p in pair.cleanup_paths: pipeline._safe_unlink_local_video(p)` (probe pair 는
   원본 collect pair 와 별개 임시 파일 — 이중 정리 없음).
8. main(): kip-up fault.mp4 / correct.mp4 각각 run_sweep.py:68-79 패턴으로 doc set
   (mode1/referenceMotionId=ref-kip-up/videoKey=fixtures/phase15/kip-up/{fault|correct}.mp4) 후
   `pipeline._process(BUCKET, key, UID, aid)` 호출 (wrapper 가 probe 트리거). 객관성: 점수 라벨 0,
   Gemini 는 측정대상만 짚음.

probe 스크립트는 단위 테스트 대상 아님 (pod-only) — import-parse 만 clean.
이모지 금지, 슬롭 금지, Korean 주석 + 24-06 § 인용 (CLAUDE.md §7).
  </action>
  <verify>
    <automated>cd backend && python3 -c "import ast; ast.parse(open('evals/phase24/probe_kip_up_gemini.py').read()); ast.parse(open('evals/phase24/run_sweep.py').read())" && grep -q "assess_fault_context" evals/phase24/probe_kip_up_gemini.py && grep -q "VETO_PART_SCOPES" evals/phase24/probe_kip_up_gemini.py && grep -q "rec\[.visionVeto.\]\|\"visionVeto\": vv\|'visionVeto': vv" evals/phase24/run_sweep.py</automated>
  </verify>
  <done>run_sweep _run_member 가 visionVeto(collectionStatus + alignment.adoption)를 rec 와 per-clip print 에 캡처 + probe_kip_up_gemini.py 가 alignment bail 우회로 assess_fault_context 를 동일 시그니처 호출하고 (a)~(e) 출력 + pod run command 도크스트링 박제 + ast-parse clean. probe/pod/GPU/network 미실행.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval harness → Gemini API | probe 가 still-pair 를 Gemini 에 업로드 (pod env 의 belle 키) |
| collect/apply → audit 직렬화 | alignment 메타데이터가 Firestore visionVeto 에 박제 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24B-01 | Tampering | to_audit_dict alignment 추가가 tally/score 를 오염 | mitigate | 분기 밖 dict-shaping 만, 측정-감점 경로 무수정; 밴드 grep 0 + 기존 비전 게이트 스위트 green 으로 증명 |
| T-24B-02 | Information disclosure | alignment 진단이 민감 데이터 누출 | accept | distance/visibility/path-count 는 기하 메트릭, PII 아님; 관찰 메타데이터 |
| T-24B-03 | Repudiation | probe Gemini fabrication 이 거짓 결함을 만들어 객관성 위반 | accept | probe 는 진단 출력만 (점수/라벨 0); support 게이트 통과 여부를 **보고**만 하고 채점 경로에 주입 안 함 ([[analysis-objectivity-no-human-scores]]) |
| T-24B-SC | Tampering | npm/pip 신규 install | mitigate | 신규 패키지 0 — 기존 numpy/gemini/RTMW 어댑터만 재사용; install 태스크 없음 |
</threat_model>

<verification>
- 밴드 grep (`apply_downward_cap|SEVERITY_CAP|capApplied`) over backend/shared/python + backend/functions = 0.
- 측정-감점/tally 경로 무수정 — `_apply_vision_veto_from_context` 의 tally 로직 변경 없음 (alignment 는 audit 직렬화에만).
- analysis core 순수 보존 — vision_veto.py 신규 import 0 (dict-shaping 만).
- 영향 단위 스위트 green: test_vision_veto.py, test_pipeline_vision_gate.py.
- probe/sweep ast-parse clean. probe/pod/GPU/Gemini/network **미실행** (로컬 금지).
</verification>

<success_criteria>
- low_alignment_confidence bail 이 alignment 텔레메트리를 보존한다.
- visionVeto audit 가 applied/not_applicable 양쪽에서 alignment 진단 sub-dict 를 노출한다 (score 불변).
- sweep 리포트가 clip 별 visionVeto(collectionStatus + alignment.adoption)를 캡처한다.
- kip-up Gemini probe 가 alignment bail 우회로 동일 시그니처 호출하며, 다음 pod-run 에서 24-06 §3 의 두 미지수(발화 조건 + Gemini 가시성)를 확정할 준비가 됐다.
- 어떤 scoring/tally/score 출력도 변하지 않는다.
</success_criteria>

<output>
Create `.planning/quick/260626-f3u-phase-24-b-diagnostic-instrumentation-al/260626-f3u-SUMMARY.md` when done.
각 task 는 atomic 커밋 (docs 커밋 금지 — orchestrator 처리; ROADMAP 무수정).
</output>
