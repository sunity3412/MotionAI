---
phase: quick-260630-l4e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/functions/pipeline/app.py
  - backend/tests/test_pipeline_deduction_seam.py
autonomous: true
requirements: [QT-260630-l4e]
must_haves:
  truths:
    - "Clean transparent tally (empty records, quant available) sets overallScore = breakdown.final, not the legacy min-of-core dimension."
    - "power-spin success (정은지) no longer pinned to 91 by the not_applicable passthrough."
    - "quant-unavailable+empty case is unaffected (still routes through the applied/fallback-record path)."
    - "ORCHESTRATOR-OWNED: full 6-pair Phase-24 pod sweep proves kip-up fault stays ≤ ~88 (no jump to 100) and every fault stays penalized."
  artifacts:
    - path: "backend/functions/pipeline/app.py"
      provides: "not_applicable branch overrides overallScore with breakdown.final"
      contains: "overallScore"
    - path: "backend/tests/test_pipeline_deduction_seam.py"
      provides: "clean-tally → overallScore == breakdown.final assertion (legacy 91/99 passthrough retired)"
      contains: "breakdown"
  key_links:
    - from: "backend/functions/pipeline/app.py::_apply_vision_veto_from_context (not_applicable branch)"
      to: "deduction_engine.tally(...).final"
      via: "overallScore = breakdown.final"
      pattern: "overallScore.*breakdown\\.final"
---

<objective>
Fix the power-spin success fallback so 정은지 success scores 95~100 instead of 91. The
investigation is DONE and pod-verified (see CONTEXT.md) — do NOT re-investigate.

Root cause (locked): in `pipeline/app.py::_apply_vision_veto_from_context`, when the Phase-24
transparent tally runs CLEAN (empty records — all measured deviations within the CITED 20° IPSF
tolerance, Gemini silent, quant available), `breakdown.final` = 100, but the `not_applicable`
branch (~app.py:2388-2396) returns `{**score_result, ...}` WITHOUT overriding `overallScore`,
so it keeps the legacy `score_result["overallScore"]` = `dimensions.overall_from_dimensions` =
`min(angle, line)` = 91. That continuous min-of-core penalty is exactly the philosophy the
transparent tally was built to retire ([[scoring-must-be-transparent-deduction-tally]]).

Fix (principled, NOT recalibration — no thresholds/constants/new code paths change, so it does
NOT violate [[calibration-source-hard-gate]]): make the `not_applicable` branch set
`overallScore = breakdown.final`. `final = max(0, round(100 + Σ points))` = 100 when records are
empty (deduction_engine.py:285), aligning the displayed score with the transparent tally that
already computed it. The quant-unavailable+empty case is unaffected — it routes through the
`applied` path via a `dimension_overall_fallback` RECORD (deduction_engine.py:213-233), not
`not_applicable` (BLOCKER A — the honest conservative fallback stays intact).

Purpose: retire the last legacy min-of-core passthrough so the transparent tally is authoritative
in every measured path; restore 정은지 success to spec ([[score-spec-95-100-elite-vision-fix]]).
Output: one-line behavioral change in the not_applicable branch + unit tests pinning it.

EXECUTOR SCOPE (no GPU): code change + local unit tests only. The 6-pair pod sweep regression
gate is owned by the orchestrator (Claude), NOT this executor — see <success_criteria>.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260630-l4e-power-spin-success-fallback-calibration-/260630-l4e-CONTEXT.md
@CLAUDE.md

# Fix site (read the not_applicable branch ~lines 2384-2396):
@backend/functions/pipeline/app.py

# Authoritative final = max(0, round(100 + Σ points)) — confirms final==100 when records empty:
@backend/shared/python/sunity_shared/analysis/deduction_engine.py

# Existing seam tests — extend, do not rewrite. The test that currently pins the legacy
# passthrough is test_gemini_silent_clean_geometry_not_applicable (line ~209):
@backend/tests/test_pipeline_deduction_seam.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set overallScore = breakdown.final in the not_applicable branch</name>
  <files>backend/functions/pipeline/app.py</files>
  <action>
In `_apply_vision_veto_from_context`, the `not_applicable` return (the block currently at ~lines
2388-2396, the path taken when `has_deduction` is False) returns a dict containing
`deductionBreakdown` and `visionVeto` but NOT `overallScore`, so it silently passes through the
legacy `score_result["overallScore"]` (= min-of-core dimension). Add a single key
`"overallScore": breakdown.final` to that returned dict, immediately alongside the existing
`"deductionBreakdown": breakdown.to_dict()` entry.

Do this ONLY in the `not_applicable` (has_deduction False) return — leave the `applied` branch
(~line 2378, which already sets `overallScore: breakdown.final`) and the non-tally passthrough
(~line 2335, the `{**score_result, "visionVeto": audit}` early return) untouched.

Add a Korean comment above the line citing Phase 24 and the principle, e.g.:
"# Phase 24 — not_applicable 도 breakdown.final 이 authoritative(측정 기하 clean → final=100).
# 레거시 min-of-core dimension passthrough 제거([[scoring-must-be-transparent-deduction-tally]],
# [[score-spec-95-100-elite-vision-fix]]). quant-unavailable 은 applied 경로(fallback record)라 불변."
No new constants, no threshold changes, no emojis.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_pipeline_deduction_seam.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>The not_applicable branch returns `overallScore: breakdown.final`. The applied branch and
  the non-tally passthrough are unchanged. No new constants introduced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin clean-tally → overallScore == breakdown.final and confirm suite green</name>
  <files>backend/tests/test_pipeline_deduction_seam.py</files>
  <behavior>
    - Update `test_gemini_silent_clean_geometry_not_applicable`: it currently asserts
      `out["overallScore"] == 99` (the retired legacy passthrough). Flip it to assert
      `out["overallScore"] == out["deductionBreakdown"]["final"]` AND `out["overallScore"] == 100`
      (clean tally with quant available → empty records → final 100), while keeping
      `out["visionVeto"]["status"] == "not_applicable"`. Update the Korean docstring to state the
      fix (not_applicable 도 breakdown.final 이 점수, 레거시 dimension passthrough 제거).
    - Add a dedicated test (e.g. `test_clean_not_applicable_uses_tally_final_not_legacy_dimension`)
      that seeds a CLEAN no_fault context (quant available, empty measured_deviations) with a
      legacy `score_result["overallScore"]` BELOW 100 (e.g. 91, the power-spin success number) and
      asserts the returned `overallScore` is `breakdown.final` (== 100), NOT the legacy 91 — i.e.
      the min-of-core dimension no longer leaks through. Reuse the existing `_ctx`, `_quant`,
      `_enable`, and `SelectedFramePair` helpers; mock-only, no Gemini/Pod/S3.
  </behavior>
  <action>
Edit only `backend/tests/test_pipeline_deduction_seam.py`. Mirror the existing test style in that
file (helpers `_ctx("no_fault", verdict=_verdict("none"), supported=[], frame_pairs=[pair])`,
`_quant("available")`, `_enable(monkeypatch)`, `measured_deviations={}`). Then run the full backend
unit suite to confirm deduction_engine + vision_veto + seam tests stay green (no regressions from
the app.py change). Do NOT touch deduction_engine.py, vision_veto.py, or dimensions.py.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_pipeline_deduction_seam.py tests/test_deduction_engine.py tests/test_vision_veto.py tests/test_pipeline_vision_gate.py -q 2>&1 | tail -25</automated>
  </verify>
  <done>The updated clean-geometry test asserts `overallScore == breakdown.final == 100`; the new
  test proves a sub-100 legacy `score_result.overallScore` no longer leaks through the
  not_applicable branch. All four named test files pass.</done>
</task>

</tasks>

<verification>
Local (executor-owned):
- `cd backend && python -m pytest tests/test_pipeline_deduction_seam.py tests/test_deduction_engine.py tests/test_vision_veto.py tests/test_pipeline_vision_gate.py -q` is green.
- `grep -n "overallScore.*breakdown.final" backend/functions/pipeline/app.py` shows the override in BOTH the applied and not_applicable branches.

Post-execution (ORCHESTRATOR-owned hard gate — Claude runs on pod, executor does NOT):
- Full 6-pair Phase-24 sweep `backend/evals/phase24/run_sweep.py`, SERIAL
  ([[pipeline-not-concurrency-safe-eval-serial]]), after the fix. PASS criteria:
  - power-spin success: 91 → 95~100
  - peter-pan / elbow-twist / pdshape success: stay 100
  - kip-up success: 95~100 (was 96)
  - kip-up fault: stays ≤ ~88 (NO jump to 100) ← HARD GATE; if it jumps, surface to belle as a
    decision, do not ship silently ([[kipup-fp-RESOLVED-phase24A]]).
  - all other faults stay penalized (power-spin 47, peter-pan 79, elbow 61, pdshape 60 ± noise).
- kip-up split-margin domain review: orchestrator authors a short written assessment of the
  `split_angle` reference-relative criterion's fault-vs-success separation against the fresh sweep
  numbers (documentation deliverable). Becomes an executor code task ONLY if the sweep reveals an
  inadequate margin.
</verification>

<success_criteria>
- not_applicable branch sets `overallScore = breakdown.final` (one-line change, no new constants).
- Clean-tally unit test asserts `overallScore == breakdown.final == 100` (legacy 91/99 passthrough
  retired); new test proves sub-100 legacy dimension no longer leaks.
- deduction_engine + vision_veto + seam + vision_gate suites green.
- ORCHESTRATOR gate (post-execution): 6-pair pod sweep confirms power-spin success 95~100 and
  kip-up fault stays ≤ ~88. If kip-up fault jumps to 100 → escalate to belle.
</success_criteria>

<output>
Create `.planning/quick/260630-l4e-power-spin-success-fallback-calibration-/260630-l4e-SUMMARY.md` when done.
</output>
