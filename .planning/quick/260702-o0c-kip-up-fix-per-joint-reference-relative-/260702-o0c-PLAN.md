---
phase: quick-260702-o0c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/functions/pipeline/app.py
  - backend/tests/test_pipeline_deduction_seam.py
autonomous: true
requirements: [KIPUP-UPPERBODY-DEDUCTION]
must_haves:
  truths:
    - "kip-up fault 영상의 상체(어깨/고관절) 편차가 감점으로 반영된다 (표시 40° → 감점)"
    - "표시되는 편차(windowMedianAngleDeltas)와 감점되는 편차(angle_vs_reference seed)가 동일 source (quant available 시)"
    - "quantification unavailable(low_alignment 등) 시 기존 DTW-median fallback seed 가 산출된다 (24-05/e5k 무회귀)"
    - "success/clean 영상은 여전히 감점 0 (window median < tol 20°)"
    - "self-compare(같은 영상)는 여전히 0"
    - "expects_extension 소유 관절은 여전히 reference_relative seed 미방출 (double-count 0)"
    - "seed source(window_median vs dtw_median_fallback)가 로그로 관찰 가능"
  artifacts:
    - path: "backend/functions/pipeline/app.py"
      provides: "2단 angle_vs_reference seed — worst-window median 우선 + DTW-median fallback"
      contains: "windowMedianAngleDeltas"
    - path: "backend/tests/test_pipeline_deduction_seam.py"
      provides: "window-median seed 단위 테스트 (국소결함/clean/self-compare/exclusion/graceful/fallback)"
  key_links:
    - from: "app._build_deduction_measured_deviations"
      to: "quantification.windowMedianAngleDeltas"
      via: "deltas 순회 → abs(delta_deg) seed (우선 경로)"
      pattern: "windowMedianAngleDeltas"
    - from: "app._build_deduction_measured_deviations"
      to: "motiondtw.per_joint_deviation"
      via: "quant-unavailable fallback (24-05 보존)"
      pattern: "per_joint_deviation"
    - from: "app._build_vision_quantification_result"
      to: "features.window_median_angle_deltas"
      via: "build_quantification_result (single source)"
      pattern: "window_median_angle_deltas"
---

<objective>
kip-up fault 영상의 상체 결함(왼어깨 Δ40.4°, 오른어깨 Δ31.0°, 오른고관절 Δ22.7° — 전부 kismam tol 20° 초과)이 감점 0 으로 새는 버그를 고친다.

근본원인(2026-07-02 실측 확정, 재조사 불필요): `_build_deduction_measured_deviations` 의
`angle_vs_reference__{jk}` seed 가 `motiondtw.per_joint_deviation` = **전체 DTW path median**
을 쓴다. worst-pose 부근의 국소 결함이 전체구간 median 에서 희석돼 tol 20° 미만으로 계산됨.
표시 레이어(visionVeto.windowMedianAngleDeltas)는 worst_pose_center ±2 median 을 쓰므로 40° 가
보인다 — **표시와 감점의 집계 불일치**가 결함.

Purpose: "표시되는 편차 = 감점되는 편차". belle 승인 의도 — 상체 결함도 측정대로 감점.
Output: seed 를 **2단**으로 정렬 — (1순위) 표시용과 동일한 worst-window median
(`quantification.windowMedianAngleDeltas`, single source = `features.window_median_angle_deltas`),
(fallback) quantification unavailable(low_alignment/measurement 결측) 시 기존 DTW-median
(`per_joint_deviation`) seed 유지 — 24-05(260626-e5k) "low_alignment 에서 정렬-독립 RTMW 각도
seed 가 granular 감점 산출" 보장 무회귀. 신규 상수 0, per_joint_deviation 함수 자체 불변.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

# 수정 대상 (읽고 시작)
@backend/functions/pipeline/app.py            # _build_deduction_measured_deviations 라인 2043-2158, 호출부 3400-3431
@backend/shared/python/sunity_shared/analysis/features.py   # window_median_angle_deltas 라인 229-280 (single source)
@backend/shared/python/sunity_shared/analysis/vision_veto.py # build_quantification_result 라인 452-515 (windowMedianAngleDeltas 채우는 곳)
@backend/shared/python/sunity_shared/analysis/ipsf_criteria.py # angle_vs_reference__{jk} criterion 라인 142-177
@backend/tests/test_pipeline_deduction_seam.py  # 기존 seed 테스트 라인 473-599 (확장 대상)
</context>

<interface_context>
현재 seed 로직 (app.py 2136-2156, **fallback 으로 보존**):
- `per_joint_deviation(path, user_seg, reference_angles)` → 전체 DTW path median (T,J)→(J,) 벡터.
- JOINT_KEYS 순회하며 `md[f"angle_vs_reference__{jk}"] = v` (v = 그 관절 full-path median deg).

표시 source (이미 존재, 우선 경로로 재사용):
- `quantification.windowMedianAngleDeltas` = `features.window_median_angle_deltas(...)` 반환 dict:
  `{deltas: [{joint, student_deg, reference_deg, delta_deg, direction, source}, ...],
    sourceFrameIndices, windowPolicy: "worst_pose_center_pm_2_median"}`.
- `delta_deg` = student_deg − reference_deg (SIGNED). seed 는 **abs(delta_deg)** 를 써야 함
  (criterion `angle_vs_reference_over_tol_linear` 은 비음수 deviation magnitude 를 기대).
- 이 quantification 은 호출부(app.py 3409)에서 이미 빌드돼 `_build_deduction_measured_deviations`
  의 `quantification` 파라미터로 넘어온다 → 우선 경로는 재계산 없이 그 field 만 읽으면 된다.

가용성 경계 (회귀 리스크 — 코디네이터 검토 반영):
- `build_quantification_result` 는 measurement(FramePairMeasurementContext) 결측 시
  `quantificationStatus="unavailable"` + `windowMedianAngleDeltas=None` 을 반환한다.
- `_apply_vision_veto_from_context` 는 `low_alignment_confidence` 도 tally-eligible 로 취급
  (24-04 Option A) — 이때 selected_frame_pairs 가 비어 quantification 이 unavailable 일 수 있다.
- 현행 DTW seed 는 `reference_dtw_match`+`reference_angles` 만 있으면 산출되므로, 이 파라미터/
  블록을 제거하면 quant-unavailable 케이스에서 granular seed 가 통째로 사라진다 (24-05/e5k 회귀).
  → **2단 구조**: window median 있으면 우선, 없으면 DTW-median fallback. 파라미터 유지.

criterion (ipsf_criteria.py): `angle_vs_reference__{jk}` 는 JOINT_KEYS 전체(어깨/고관절/무릎/팔꿈치
포함)에 대해 생성됨. tolerance=_ANGLE_TOLERANCE_DEG(20 [CITED] kismam), slope/cap 기존 재사용.
md value = scalar deg. 신구 모두 float — 계약 형상 불변.

per_joint_deviation 은 app.py:2646(Mode3/kismam core)에서도 쓰임. KISMAM/not_pole 게이트가 소비
→ **함수 자체 절대 수정 금지** (motiondtw.py docstring 박제 사유).
</interface_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: angle_vs_reference seed 를 2단(worst-window median 우선 + DTW-median fallback)으로 재배선</name>
  <files>backend/functions/pipeline/app.py</files>
  <behavior>
    - windowMedianAngleDeltas 에 어깨 Δ40°(> tol 20°) 존재 → md["angle_vs_reference__left_shoulder"] == 40.0 (abs) — DTW 전체 median 값이 아니라 window 값.
    - quantification.windowMedianAngleDeltas 가 None/부재 AND reference_dtw_match+reference_angles 보유 → 기존 per_joint_deviation(DTW-median) seed 산출 (24-05/e5k 무회귀).
    - 둘 다 없으면 → angle_vs_reference__* 미방출 (honest 0, graceful).
    - self-compare (양 경로 모두 편차 0) → angle_vs_reference__* 키 0개.
    - profile.expects_extension(jk) 관절 → 양 경로 모두 seed 미방출 (cross-exclusion 유지).
    - JOINT_KEYS 아닌 joint 문자열 → skip (spurious 키 금지).
    - 어느 경로가 쓰였는지 log.info 로 관찰 가능 (contract/Firestore 스키마 변경 0).
  </behavior>
  <action>
`_build_deduction_measured_deviations` (app.py ~2043) 의 24-07 ① 블록(라인 2129-2156)을
**2단 구조**로 재구성하라. 시그니처(`reference_dtw_match`/`reference_angles`)와 호출부
(app.py ~3420-3428)는 **변경하지 않는다** — fallback 이 계속 소비한다.

새 seed 로직 (공통 방출 규칙을 helper 또는 인라인으로 한 번만 작성해 중복 금지):
- 방출 규칙(양 경로 공통): `jk not in JOINT_KEYS` → skip. `v != v`(NaN) 또는 `v <= 0.0` → skip.
  `profile is not None and profile.expects_extension(jk)` → skip (seed-stage cross-exclusion —
  절대-신전 소유 관절은 ipsf_absolute leg/arm/line 이 이미 채점, double-count 금지).
  통과 시 `md[f"angle_vs_reference__{jk}"] = v`.
- **1순위 (window median — 표시=감점 정렬):**
  `wm = getattr(quantification, "windowMedianAngleDeltas", None)`. `wm` 이 dict 이고
  `wm.get("deltas")` 가 truthy list 이면, 각 entry 에서 `jk = entry.get("joint")`,
  `v = abs(float(entry.get("delta_deg", 0.0)))` 로 방출 규칙 적용. 이 경로를 탔으면
  seed_source = "window_median" 으로 기록하고 fallback 은 실행하지 않는다.
- **fallback (DTW 전체 median — 24-05/e5k 보존):** window median 을 못 탔을 때만
  (wm None/형상불량/deltas 빈 list), 기존 블록 그대로 —
  `reference_dtw_match is not None and reference_angles is not None and angles is not None` 이면
  path/start/end 를 뽑아 `per_joint_deviation(path, angles[start:end], reference_angles)` 산출,
  JOINT_KEYS 순회 + 동일 방출 규칙. seed_source = "dtw_median_fallback".
  예외는 기존과 동일하게 honest skip (`except Exception` → 미방출).
- **관찰 가능성 (코디네이터 요구 3):** angle_vs_reference__* 키를 1개 이상 방출했을 때 기존
  로깅 관례(구조적 key=value)로 1줄:
  `log.info("angle_vs_reference seed source=%s joints=%d", seed_source, count)`.
  contract/Firestore 스키마 변경 금지 — 로그만. md 에 마커 키 추가 금지 (엔진 md 계약은
  criterion id 키만 기대).

docstring 갱신: angle_vs_reference 항목을 "정은지 대비 per-joint 각도 편차 — (1순위) 표시용
windowMedianAngleDeltas(worst_pose_center ±2 median)와 **동일 source**(features.window_median_
angle_deltas). 국소 결함이 전체 DTW path median 에서 희석되던 버그를 표시=감점 정렬로 해소
(260702-o0c). (fallback) quantification unavailable(low_alignment 등, 24-04/24-05 정렬-독립
seed 보장) 시 기존 per_joint_deviation DTW-median 유지. expects_extension 미소유 관절만 방출."
로 교체. `24-07 ①` citation 유지 + 260702-o0c fix 사유를 왜(why) 중심 한국어 주석으로 병기.

제약 재확인 (위반 금지): `motiondtw.per_joint_deviation` 함수 자체 수정 금지 (KISMAM/not_pole
소비, app.py:2646 도 계속 사용). 신규 튜닝 상수 0 (tol/slope/cap 은 criterion 이 이미 kismam 값
재사용). kip-up 특정 분기 금지 — mode1 전 동작 공통 경로. 밴드(severity→고정감점) 재도입 금지.
이모지 금지.
  </action>
  <verify>
    <automated>cd backend && git diff --stat shared/python/sunity_shared/analysis/motiondtw.py | wc -l && grep -c "windowMedianAngleDeltas" functions/pipeline/app.py && grep -c "per_joint_deviation" functions/pipeline/app.py</automated>
  </verify>
  <done>
    seed 가 windowMedianAngleDeltas 우선 + per_joint_deviation fallback 2단 구조 (grep 양쪽 히트). motiondtw.py 무변경 (git diff 0줄). 시그니처/호출부 불변 (reference_dtw_match/reference_angles 유지). seed source log.info 존재. typecheck/import 무오류.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: seed 단위 테스트 — window-median 우선 + fallback(e5k 회귀 가드) 검증, 전체 회귀 0</name>
  <files>backend/tests/test_pipeline_deduction_seam.py</files>
  <behavior>
    - 국소결함(worst-window Δ40° 어깨) → angle_vs_reference__left_shoulder == 40.0 (희석 없이 감점 반영, window 값 우선).
    - window median 과 DTW median 이 다른 값일 때 → window 값이 채택됨 (우선순위 증명).
    - quantification unavailable(windowMedianAngleDeltas=None) + reference_dtw_match/reference_angles 보유 → DTW-median fallback seed 산출 (24-05/e5k 회귀 가드).
    - self-compare(양 경로 편차 0) → angle_vs_reference__* 0개.
    - 등록 profile 무릎(expects_extension) 제외 / 어깨(BENT_OK) 유지 — 양 경로 공통.
    - window median 도 없고 match/ref 도 None → angle_vs_reference__* 미방출 (graceful).
    - 결정성: 동일 입력 2회 == .
  </behavior>
  <action>
기존 "24-07 ① fix" 테스트 블록(test_pipeline_deduction_seam.py 라인 473-599)을 2단 구조에 맞게
확장·조정하라. 기존 fallback 계열 테스트(`test_reference_relative_unregistered_emits_per_joint_granular`
등, windowMedianAngleDeltas=None 인 `_quant` 로 호출)는 이제 **fallback 경로 검증**이 되므로
삭제하지 말고 docstring 만 "quant unavailable → DTW-median fallback (24-05/e5k 회귀 가드,
260702-o0c 2단 구조)" 로 갱신해 보존하라. `_identity_match`/`_ref_angles` 헬퍼도 유지.

추가 방침:
- `_quant(...)` 헬퍼(라인 76-83)에 `window_median=None` 인자를 추가해 `windowMedianAngleDeltas`
  주입 가능하게 하라. dict 형상은 features.window_median_angle_deltas 반환과 동일:
  `{"deltas": [{"joint": jk, "student_deg": s, "reference_deg": r, "delta_deg": s-r,
  "direction": ..., "source": "geometry"}, ...], "sourceFrameIndices": {...},
  "windowPolicy": "worst_pose_center_pm_2_median"}`. 실제 산출과 어긋나지 않도록 delta entry 는
  features._delta_entry 를 import 해 만들거나 최소 seed 가 읽는 키(joint, delta_deg)를 정확히 포함.
- 신규 테스트:
  1. `test_reference_relative_local_fault_from_window_median`: 어깨 delta_deg=-40 (abs 40) 인
     window_median 주입 → `md["angle_vs_reference__left_shoulder"] == pytest.approx(40.0)`.
     **국소-희석 회귀 문서화**: 주석에 "전체 DTW path median 이면 희석됐을 국소 결함이
     worst-window median 에서 보존됨(260702-o0c)" 명시.
  2. `test_reference_relative_window_median_takes_precedence_over_dtw`: window median 어깨 Δ40 +
     DTW 입력(reference_dtw_match/reference_angles, usr-ref 어깨 편차가 다른 값 예: 10°) 동시 제공
     → md 값 == 40.0 (window 채택, DTW 희석값 아님).
  3. `test_reference_relative_fallback_when_quant_unavailable`: window_median=None +
     match/ref 보유 + 어깨 40° 편차 → md 산출 (fallback 발동 — e5k 가드 명시 주석).
  4. window median 경로의 exclusion: 등록 profile(무릎 EXTEND) + window 에 무릎/어깨 둘 다 Δ40
     → `__left_knee` 부재, `__left_shoulder` == 40.0.
  5. window median self-compare: delta_deg 전부 0 → angle_vs_reference__* 0개.
  6. 결정성: 동일 window_median 2회 → md 동일.
- 기존 다른 테스트(test_score_not_deviation_regression 등)는 무회귀 — 손대지 말 것.

수치 라벨 금지(HIGH-3: deg 편차만, 0-100 score 금지). curve-fit 금지(보유 sweep 수치 타깃 아님).
주석 왜(why) 중심 한국어.
  </action>
  <verify>
    <automated>cd backend && PYTHONPATH="shared/python:functions/pipeline" python -m pytest tests/test_pipeline_deduction_seam.py -x -q && PYTHONPATH="shared/python:functions/pipeline" python -m pytest -q 2>&1 | tail -5</automated>
  </verify>
  <done>
    새 seed 테스트 통과 (국소결함/우선순위/fallback/clean/self-compare/exclusion/graceful/결정성). 기존 fallback 계열 테스트 보존·GREEN. backend 전체 pytest 회귀 0. 밴드 grep 0: `grep -rn -i "band\|밴드\|fixed_ceiling\|severity.*50" backend/functions/pipeline/app.py | grep -v '^#' | grep -vi "removed\|제거\|no.*band\|밴드 제거" | wc -l` 가 seed 변경으로 밴드 재도입 없음을 확인 (0).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Task 1-2 로 seed 를 2단(표시용 worst-window median 우선 + DTW-median fallback)으로 정렬.
    로컬 단위/회귀 GREEN. Pod sweep(6 페어 성공/실패)은 executor 가 실행하지 않는다 —
    orchestrator 가 커밋 후 별도 실행.
  </what-built>
  <how-to-verify>
    이 태스크는 실행하지 말 것. Pod sweep 검증 PENDING 마커.

    커밋 후 orchestrator/운영자가 실행할 sweep gate (기대 결과 박제):
    1. kip-up fault 영상 (analysis af8fb8c8 류, mode1) → 88 에서 **추가 하락** (상체 어깨/고관절
       감점 반영). 육안(belle) 상체 결함과 정합.
    2. 정은지 success 6영상 → 전부 **100 유지** (window median < tol 20° → 감점 0).
    3. 위 둘이 sweep 통과 기준. self-compare 0 재확인.
    4. 로그에서 seed source=window_median 발동 확인 (low_alignment 케이스면 dtw_median_fallback).

    실행 방법(운영자): 새 pod bootstrap → Lambda RUNPOD_ANALYZE_URL 동기화 → 6 페어 sweep.
    executor 는 SSH/pod 접근 금지.
  </how-to-verify>
  <resume-signal>sweep 결과 (kip-up<88 AND success 6/6==100) 확인 후 "approved" 또는 이슈 기술</resume-signal>
</task>

</tasks>

<verification>
- 로컬: `PYTHONPATH="shared/python:functions/pipeline" python -m pytest -q` (backend/ 에서) 회귀 0.
- seed source 정합: app.py seed 가 quantification.windowMedianAngleDeltas 우선 소비 +
  per_joint_deviation DTW-median fallback 보존 (24-05/e5k 무회귀). motiondtw.py 무변경.
- seed source 로그 관찰 가능 (window_median vs dtw_median_fallback).
- 밴드 재도입 0 (grep).
- Pod sweep = PENDING (Task 3 마커, executor 미실행).
</verification>

<success_criteria>
- quant available 시: angle_vs_reference__{jk} seed 가 표시용(windowMedianAngleDeltas)과
  **동일 값**을 감점에 사용 — 국소 상체 결함(어깨 Δ40°)이 전체구간 희석 없이 감점.
- quant unavailable(low_alignment 등) 시: 기존 DTW-median fallback seed 산출 (e5k 회귀 0).
- clean/success → 0, self-compare → 0, expects_extension 관절 제외 유지 (단위 테스트 증명).
- backend 전체 pytest 회귀 0, 밴드 grep 0, contract/Firestore 스키마 변경 0.
- 신규 튜닝 상수 0, per_joint_deviation 함수 불변, kip-up 특정 분기 0.
</success_criteria>

<output>
Create `.planning/quick/260702-o0c-kip-up-fix-per-joint-reference-relative-/260702-o0c-SUMMARY.md` when done
</output>
