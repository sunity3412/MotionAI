---
phase: quick-260702-q8q
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/types/analysis.ts
  - docs/contract.md
  - backend/shared/python/sunity_shared/models.py
  - backend/tests/test_deduction_engine.py
  - app/src/lib/userAnalyses.ts
  - app/src/lib/deductionLabels.ts
  - app/src/components/ScoreBreakdownSection.tsx
  - app/src/components/ForcePatternDetailModal.tsx
  - app/src/app/analysis/result.tsx
autonomous: true
requirements: [SCORE-BASIS-DISCLOSURE]
must_haves:
  truths:
    - "kip-up fault 88점 문서에서 앱이 '100(기준) − 12(다리 스플릿 각도: 기준 대비 30° 차이, 허용 20° 초과 10°) = 88' 형태의 감점 내역을 화면에 보여준다"
    - "감점 record 가 여러 개/신규 criterion(angle_vs_reference__{joint})이어도 일반 렌더된다 (관절별 한국어 라벨 자동)"
    - "실패 원인 상세 시트가 템플릿 문구 대신 실측 근거(측정 편차·허용오차·판정 일치 횟수·관절별 편차표)를 보여준다 (근거 데이터 있을 때)"
    - "신뢰도 라벨이 supportCount 와 모순되지 않는다 (4회 일치 → '신뢰도 낮음' 미표기)"
    - "구 분석 문서(deductionBreakdown 부재)와 mode3 문서에서 새 섹션이 안전하게 숨겨진다 (크래시 0)"
    - "점수 계산 로직/엔진 수치 불변 — 표시·계약 문서만 변경 (pipeline app.py 변경 0)"
    - "TS 계약이 실데이터와 일치한다 — DeductionRecord.source 에 'vision' 포함 (3-way lockstep)"
  artifacts:
    - path: "app/src/components/ScoreBreakdownSection.tsx"
      provides: "점수 계산 내역 섹션 (baseline − records − final 행 목록 + coverageGaps/fallback 각주)"
      min_lines: 60
    - path: "app/src/lib/deductionLabels.ts"
      provides: "criterion id → 한국어 라벨 + deviationSource 별 행 포맷터 (angle_vs_reference__{jk} generic 파싱)"
      contains: "angle_vs_reference__"
    - path: "app/src/types/analysis.ts"
      provides: "DeductionRecord.source 'geometry' | 'vision' 확장"
      contains: "'geometry' | 'vision'"
    - path: "app/src/lib/userAnalyses.ts"
      provides: "deductionBreakdown normalize null-guard"
      contains: "deductionBreakdown"
  key_links:
    - from: "app/src/app/analysis/result.tsx"
      to: "result.deductionBreakdown.records"
      via: "ScoreBreakdownSection 렌더 (mode1 + 존재 시)"
      pattern: "deductionBreakdown"
    - from: "app/src/components/ForcePatternDetailModal.tsx"
      to: "visionVeto.rootCauseHypotheses / windowMedianAngleDeltas / deductionBreakdown.records"
      via: "optional measuredEvidence prop (result.tsx 조립)"
      pattern: "measuredEvidence"
    - from: "docs/contract.md §10.2"
      to: "deduction_engine.DeductionRecord.source"
      via: "3-way lockstep (source='vision' split 경로 반영)"
      pattern: "vision"
---

<objective>
결과 화면 점수 근거 공개(B 작업) — "왜 이 점수인지"를 앱이 스스로 설명하게 한다.
belle 실기기 피드백(TestFlight #27, kip-up fault 88점): 점수는 투명 규칙(100 − 12 = 88)으로
계산되는데 앱이 계산 내역을 안 보여줘서 신뢰가 깎임. 채점 원칙 박제
([[scoring-must-be-transparent-deduction-tally]] — "보고서가 −X−Y−Z=50 내역 노출")의 UI 배선.

Purpose: 분석 정확도 = 신뢰. 감점 내역·실측 근거·판정 일치를 노출해 "따지는" 지점 제거.
Output: 점수 계산 내역 섹션 + 실측 근거 상세 시트 + 신뢰도 라벨 정합 + 계약 drift 수정.
</objective>

<planner_findings>
## 플래너 사전 검증 결과 (실측 — 재조사 불필요)

**1. "records 유실" 가설은 틀림 — 백엔드 방출은 이미 완료·검증됨.**
플래너가 Firebase Admin 으로 실 Firestore 를 read-only 조회한 결과 (2026-07-02):

- `users/csKWYvI3WCPYPysNQ9KkWecaUvq1/analyses/18796f8b350f48ccaf560f095e085fea`
  (mode1, 2026-07-02T08:04Z, overallScore 88, veto applied) 에
  `result.deductionBreakdown` 실존: `{baseline, records[1], final: 88, coverageGaps, fallback}`.
  record = `{criterion: "split_angle", measuredValue: 30, deviation: 10, points: -12,
  ruleId: "split_vs_reference_over_tol_linear", source: "vision", deviationSource:
  "reference_relative", baselineValue: 0, unit: "deg", ipsfAnchor: "expert_reference_deviation
  (정은지 대비 split 부족분)"}`. 다른 최근 문서들도 records 0~7개 정상 보유
  (예: devpdshapefault = 7 records, angle_vs_reference__left_elbow 등).
- 방출 경로 전부 배선됨: `deduction_engine.DeductionBreakdown.to_dict()` →
  `_apply_vision_veto_from_context` 가 `result["deductionBreakdown"]` 로 부착(app.py 2420/2438) →
  `firestore_admin.complete_analysis` 가 `_validate_deduction_breakdown` 후 persist (line 908).
- **따라서 스코프 항목 1(백엔드 방출)·2(계약 신설)는 작업 불필요** — 계약도 이미 3-way 존재
  (analysis.ts DeductionRecord/DeductionBreakdown, models.py DEDUCTION_RECORD_KEYS, contract.md §10).
- 2026-06-26 이전 문서(`hasBD: false`)는 legacy — 하위호환 방어 필요(스코프 항목 5 유효).

**2. 진짜 갭 = 앱이 records 를 안 그림.** result.tsx 는 `deductionBreakdown?.final` 을
캡션 비교(reframeVeto)에만 쓰고 records 목록은 어디서도 렌더하지 않음.

**3. 계약 drift 1건 발견 (수정 필요).** 엔진은 belle 2026-06-29 결정 A 이후 vision-측정
split 감점에 `source="vision"` 을 방출하는데(deduction_engine.py 281행, 실데이터 확인),
analysis.ts `source: 'geometry'` 하드 리터럴 + contract.md §10.2 "'geometry' 하드 리터럴" +
models.py 주석이 전부 구식. §10.2 criterion 카탈로그에도 `angle_vs_reference__{joint}`
(quick-260626-jwu 신설)가 빠져 있음.

**4. "신뢰도 낮음" 모순의 근원 = result.tsx 604-623행.** veto 결함 fallback finding 이
`confidence: 0` 하드코딩 → ForcePatternCard/Modal 의 `confidenceLabel(0)` = "신뢰도 낮음".
rootCauseHypotheses supportCount(4) 와 무관하게 항상 낮음으로 표기되던 것.

**5. "실패 원인 상세보기" 시트 = `ForcePatternDetailModal.tsx`.** 템플릿 문구 위치:
155행 "이 원인은 어떻게 측정됐나"(finding.reason 폴백 = "AI 가 동작 신호를 기반으로 추정한
가능성입니다"), 163행 "관련 부위 — 확인 필요", 170행 "확인하기 — 거울 보며 동작 직접 재현".

**6. supportCount 의 분모(총 샘플 수)는 Firestore 에 없음** (visionVeto.telemetry 는
resource_limited 전용). "4/4" 표기는 불가 — 절대 횟수 표기("분석 표본 4회 일치")로 정직하게.
백엔드 필드 추가는 Pod 재배포 필요 + 이 작업 스코프(표시만) 밖 — 하지 않는다.

**7. 관절 한국어 라벨 선례 = `FaultZoomCompare.tsx` 상단 JOINT KO 맵** (left_knee: '왼쪽 무릎' 등).

**8. tolerance 는 record 에 직접 없음 — 역산 가능.** record.deviation = 허용 초과분(over),
reference_relative: raw 편차 = measuredValue(baselineValue=0) → tol = measuredValue − deviation.
ipsf_absolute: raw 편차 = baselineValue − measuredValue → tol = (baselineValue − measuredValue) − deviation.
kip-up 예: measured 30 − deviation 10 = 허용 20. (단 split 160° 0-fail 불연속 record 는
역산이 성립 안 함 — over 가 cap 도출값. 역산 tol 이 음수/비정상이면 허용오차 표기 생략.)
</planner_findings>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@app/CLAUDE.md
@design.md
@app/src/types/analysis.ts
@app/src/app/analysis/result.tsx
@app/src/lib/userAnalyses.ts
@app/src/components/ForcePatternDetailModal.tsx
@app/src/components/ForcePatternCard.tsx
@app/src/components/FaultZoomCompare.tsx
@backend/shared/python/sunity_shared/analysis/deduction_engine.py
@backend/shared/python/sunity_shared/models.py
@docs/contract.md
</context>

<constraints>
- 점수 계산 로직 변경 금지 — 표시/계약 문서/테스트만. `deduction_engine.py` 수치·분기 불변.
- `backend/functions/pipeline/app.py` 는 절대 수정 금지 — 동시 진행 A 작업(quick-260702-o0c,
  f513587)이 `_build_deduction_measured_deviations` 를 수정했음. 이 플랜은 pipeline 무접촉으로 충돌 0.
- mode3 는 veto 미실행(mode3_held) — 새 섹션/근거는 mode1 전용. `cmp.mode === 'mode1'` +
  데이터 존재 이중 가드.
- UI: theme 토큰만(colors/radius/spacing — 하드코딩 금지), Pretendard 기본, 한국어 카피,
  이모지 금지, 라이트 전용. Figma 에 이 섹션 설계 없음 — 기존 result 화면 카드/시트 패턴 따라
  자체 구현 (미설계 화면도 멈추지 말 것, CLAUDE.md §4).
- 객관성: 점수/판정을 앱에서 재계산·재해석 금지 — 저장된 record 값을 그대로 표기.
  합계 검증(100 + Σpoints ≠ final)이 어긋나도 UI 가 숫자를 조작하지 않는다 (있는 그대로 + final 우선).
</constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 계약 drift 수정(3-way lockstep) + normalize 방어 + 백엔드 lockstep 테스트</name>
  <files>app/src/types/analysis.ts, docs/contract.md, backend/shared/python/sunity_shared/models.py, backend/tests/test_deduction_engine.py, app/src/lib/userAnalyses.ts</files>
  <behavior>
    - Test 1 (backend): vision-측정 split 경로(tally 에 supported_differences 로 split 주입,
      md 에 split_angle 부재)에서 방출된 record 의 to_dict() 키가 models.DEDUCTION_RECORD_KEYS 와
      정확히 일치하고 source == "vision" — 계약 값 집합 {geometry, vision} lockstep 가드.
      기존 test_deduction_engine.py 에 이미 유사 fixture 가 있으면 확장, 없으면 신규 함수 추가.
    - Test 2 (backend): geometry 경로 record 는 여전히 source == "geometry" (회귀 0).
  </behavior>
  <action>
    3-way lockstep 단일 커밋 (계약 변경 관례 — analysis.ts + models.py + contract.md 동시):

    1. `app/src/types/analysis.ts` DeductionRecord.source 를 `'geometry' | 'vision'` 으로 확장.
       주석에 근거 박제: belle 2026-06-29 결정 A — geometric 측정 불가 결함(split)은 Gemini
       vision-측정값으로 점수화하되 source='vision' 투명 표기 (deduction_engine.py 281행 정합).
    2. `docs/contract.md` §10.2: source 행을 `'geometry' | 'vision'` 로 갱신 (vision = geometric
       측정 불가 결함의 vision-측정 provenance, 점수 산식은 동일 규칙 tol×slope — Gemini 점수 아님
       ND-02 유지). criterion 카탈로그에 `angle_vs_reference__{joint}` (reference_relative,
       quick-260626-jwu) 추가. deviationSource 설명에 reference_relative 의
       angle_vs_reference/split vision 경로 한 줄 반영.
    3. `backend/shared/python/sunity_shared/models.py` DEDUCTION 주석 블록의 "source='geometry'
       하드 리터럴" 서술을 {'geometry','vision'} 로 갱신 (키 tuple 은 불변 — 값 집합 서술만).
    4. `backend/tests/test_deduction_engine.py` 에 behavior 의 lockstep 테스트 추가/확장.
       deduction_engine.py 본체는 절대 수정하지 않는다.
    5. `app/src/lib/userAnalyses.ts` normalize 에 deductionBreakdown null-guard 추가 —
       기존 safetyFlags/recommendedExercises 패턴 mirror: `result?.deductionBreakdown` 이
       객체이고 `records` 가 배열이면 통과(records 각 항목은 객체만 필터), malformed(비객체/
       records 비배열)면 필드를 undefined 로 두어 optional 유지 (구 doc 크래시 0).
       backend validator 가 flat 강제하므로 깊은 검증은 하지 않는다 (기존 관례).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_deduction_engine.py -q && cd ../app && npm run typecheck</automated>
  </verify>
  <done>analysis.ts/contract.md/models.py 의 source 서술이 실방출 값 {geometry, vision} 과 일치. angle_vs_reference__{joint} 가 §10.2 카탈로그에 존재. normalize 가 malformed deductionBreakdown 에서 undefined 로 접힘. backend 테스트 GREEN, tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: "점수 계산 내역" 섹션 — 감점 tally 를 화면에 노출</name>
  <files>app/src/lib/deductionLabels.ts, app/src/components/ScoreBreakdownSection.tsx, app/src/app/analysis/result.tsx</files>
  <action>
    1. `app/src/lib/deductionLabels.ts` 신설 (named export, lib 관례):
       - criterion id → 한국어 라벨: `split_angle` → '다리 스플릿 각도', `leg_extension` →
         '다리 신전(펴짐)', `arm_extension` → '팔 신전(펴짐)', `line` → '바디 라인',
         `body_relative_reach` → '리치(도달 거리)', `dimension_overall_fallback` →
         '측정 기하 종합(정량화 불가 폴백)'.
       - **generic 파싱 필수** (A 작업으로 관절별 record 증가 예정): `angle_vs_reference__{jk}`
         → `{관절 한국어}(정은지 대비 각도)` — 관절 KO 맵은 FaultZoomCompare.tsx 상단 맵을
         재사용 가능하게 추출하거나 동일 값으로 이 모듈에 두고 FaultZoomCompare 가 import 하도록
         정리 (중복 2벌 금지). 미등록 criterion id 는 id 그대로 노출 (숨기지 않음 — 투명성).
       - 행 포맷터: record → `{label, pointsText: '−12', detailText}` 형태.
         detailText 는 deviationSource 별:
         · reference_relative(deg): "기준 대비 {measuredValue}° 차이 (허용 {tol}° 초과 {deviation}°)",
           tol = measuredValue − deviation (planner_findings 8 — tol 이 0 미만/비유한이면 허용오차 구문 생략).
         · ipsf_absolute(deg): "측정 {measuredValue}° (기준 {baselineValue}°, 허용 초과 {deviation}°)".
         · unit==='notch': "도달 부족 {deviation}칸".
         · unit==='score_delta'(fallback): "측정 기하 종합 {measuredValue}점 기준 환산".
         · source==='vision' record 는 "(영상 비교 측정)" 꼬리 표기 — provenance 투명 노출.
       - 숫자는 저장값 그대로 (재계산 금지), 소수는 정수면 정수로, 아니면 1자리.
    2. `app/src/components/ScoreBreakdownSection.tsx` 신설 — 기존 result 카드 패턴
       (styles.card 상당: colors.bg 카드 + radius + border 토큰) mirror:
       - 헤더 행: "기준 점수 100".
       - record 행 목록: 왼쪽 라벨+detailText(2줄 허용), 오른쪽 pointsText 를 brand 계열
         강조 (감점). records 순서 = 저장 순서 그대로 (엔진이 결정적 정렬).
       - 합계 행: "= 종합 {final}점" (final 은 breakdown.final 사용 — overallScore 재사용 금지).
       - records 빈 배열(clean 100): "측정 감점 없음 — 기준 점수 그대로예요." 한 줄.
       - coverageGaps 존재 시 각주(작은 캡션): "측정하지 못해 점수에 반영하지 않은 항목이
         {N}건 있어요." (fabricate 금지 — 정직한 커버리지 노출, ND-06 정신).
       - fallback === 'quantification_unavailable' 시 캡션: "정밀 정량화가 불가해 측정 기하
         종합으로 환산했어요."
       - props: `{ breakdown: DeductionBreakdown }` 단순 유지 — 렌더 가드는 caller(result.tsx).
       - 접근성: 행 단위 accessibilityLabel (예: "다리 스플릿 각도 감점 12점").
    3. `app/src/app/analysis/result.tsx` 배선:
       - 배치: 영역 5 "세부 점수" 카드 + auxCaption 바로 아래, 섹션 타이틀 "점수 계산 내역"
         (styles.sectionTitle 재사용). 이유: belle 의 모순 인지("각도 100 인데 종합 88")가
         세부 점수 직후에 발생 — 그 자리에서 해소.
       - 렌더 가드: `cmp.mode === 'mode1' && result.deductionBreakdown != null` (mode3/legacy 숨김,
         isScoreSuppressed 경로 무관 — mode1 전용이라 자연 배타).
       - 기존 contextNote/reframeVeto 캡션(1050행 부근) 문구 사실 점검: "각도로 안 드러나는
         자세 결함" 서술은 vision-측정 split 케이스에 사실 정합(각도 차원은 DTW 유사도,
         감점은 vision 측정 — 거짓 아님) → 문구 유지하되, 새 섹션 존재 시 꼬리에
         "아래 '점수 계산 내역'에서 감점 근거를 확인할 수 있어요." 연결 문장 추가.
  </action>
  <verify>
    <automated>cd app && npm run typecheck && grep -c "deductionBreakdown" src/app/analysis/result.tsx</automated>
  </verify>
  <done>mode1 + breakdown 보유 doc 에서 100 − 각 감점 − … = final 행 목록이 렌더되고, 미보유/legacy/mode3 doc 에선 섹션 자체가 없음. 라벨은 criterion generic 매핑(관절별 포함). 토큰만 사용, 하드코딩 색상 0.</done>
</task>

<task type="auto">
  <name>Task 3: 실패 원인 상세 시트 실측 근거 교체 + 신뢰도 라벨 정합</name>
  <files>app/src/app/analysis/result.tsx, app/src/components/ForcePatternDetailModal.tsx</files>
  <action>
    1. `ForcePatternDetailModal.tsx` 에 optional prop `measuredEvidence` 추가 (기존 Phase 9
       finding 렌더는 무변경 — evidence 없으면 현행 템플릿 그대로, 무회귀):
       type 은 modal 파일 내 inline 선언 (관례): `{ measurementText: string;
       supportCount: number | null; jointDeltas: { jointLabel: string; studentDeg: number;
       referenceDeg: number; deltaDeg: number; overTolerance: boolean }[] }`.
       evidence 존재 시 교체 렌더:
       - "이 원인은 어떻게 측정됐나" 카드 본문 → measurementText (사실 기반 고정문구 —
         "내 영상과 정은지 선수 기준 영상을 프레임 단위로 비교해 관절 각도 편차를 측정하고,
         허용오차(20°)를 초과한 만큼 명시된 규칙으로 감점했어요." + record 실측 한 줄, 예:
         "측정 결과: 스플릿 각도가 기준 대비 30° 부족 (허용 20° 초과 10° → 12점 감점)."
         — record 값은 Task 2 의 deductionLabels 포맷터 재사용).
       - supportCount != null 이면 카드에 "분석 표본 {supportCount}회에서 같은 결함이
         확인됐어요." 추가 (분모 데이터 없음 — 절대 횟수만, planner_findings 6).
       - "관련 부위 — 확인 필요" 행 → jointDeltas 가 있으면 관절별 편차표로 교체:
         행마다 `{jointLabel} 내 {studentDeg}° / 기준 {referenceDeg}° (차이 {deltaDeg}°)`,
         overTolerance(|delta| > 20°) 행은 brand 색 강조. 표는 ScrollView 안 단순 View 행
         목록 (라이브러리 추가 금지). jointDeltas 비면 기존 jointHint 행 유지.
       - "확인하기 — 거울 보며 동작 직접 재현" 행은 evidence 존재 시 "확인하기 — 위
         '문제 부위 확대 비교'와 편차표의 강조 관절을 함께 확인" 으로 교체.
    2. `result.tsx` evidence 조립 (useMemo):
       - 소스: `result.visionVeto` (status==='applied' 일 때 rootCauseHypotheses 첫 항목
         supportCount, windowMedianAngleDeltas.deltas) + `result.deductionBreakdown.records`
         (dimension_overall_fallback 제외 첫 record → measurementText 실측 한 줄).
       - jointDeltas: windowMedianAngleDeltas.deltas 를 관절 KO 라벨로 변환, 소수 1자리,
         overTolerance = |delta_deg| > 20 (마커 강조 임계 20° 선례 — 260620-18r IPSF 허용오차
         정합; result.tsx/KeypointOverlay 에 기존 20 상수 있으면 재사용, 없으면
         deductionLabels.ts 에 IPSF_TOLERANCE_DEG=20 단일 선언 후 양쪽 import).
       - 가드: mode1 + applied + 해당 필드 존재 시에만 evidence 전달, 아니면 undefined
         (Phase 9 일반 finding 탭은 evidence 없이 열림 — 기존 동작).
    3. 신뢰도 라벨 정합 (모순 근원 = result.tsx 604-623행 fallback finding confidence: 0):
       veto fallback finding 생성 시 confidence 를 supportCount 기반으로 산출 —
       rootCauseHypotheses 최대 supportCount 가 3 이상 → 0.9 ('신뢰도 높음'), 2 → 0.6
       ('신뢰도 보통'), 1 이하/부재 → 현행 0 유지. 근거 주석 박제: 이 finding 은 신호 추정이
       아니라 vision 측정+판정 일치가 뒷받침하는 결함이므로 supportCount 가 신뢰도의 정직한
       소스 (fabricate 아님 — 저장된 판정 일치 횟수의 표시 변환). ForcePatternCard/
       confidenceLabel 함수 자체는 무변경 (Phase 9 findings 라벨 회귀 0).
  </action>
  <verify>
    <automated>cd app && npm run typecheck && grep -c "measuredEvidence" src/components/ForcePatternDetailModal.tsx</automated>
  </verify>
  <done>veto applied doc 에서 상세 시트가 실측 문구+표본 일치 횟수+관절별 편차표(허용오차 초과 강조)를 보여주고, 카드 신뢰도 라벨이 supportCount 4 에서 '높음'. evidence 없는 일반 Phase 9 finding/legacy doc 은 기존 템플릿 그대로 (무회귀). tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firestore doc → 앱 렌더 | 구버전/malformed 분석 문서가 새 렌더 경로로 유입 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-q8q-01 | Tampering/DoS | userAnalyses.normalize → ScoreBreakdownSection | mitigate | Task 1 normalize null-guard (malformed → undefined) + Task 2 caller 렌더 가드 — 크래시 대신 섹션 숨김 |
| T-q8q-02 | Information Disclosure | ScoreBreakdownSection / 상세 시트 | accept | 본인 문서(Firestore rules 소유자 read)만 표시, 신규 데이터 방출 0 — 표시 전용 |
| T-q8q-SC | Tampering | 패키지 설치 | accept | 신규 패키지 설치 0 (RN 기본 View/Text 만 사용, 라이브러리 추가 금지 명시) |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/test_deduction_engine.py -q` GREEN (lockstep + vision source)
- `cd app && npm run typecheck` clean
- grep 게이트: `git diff --stat` 에 `backend/functions/pipeline/app.py` 부재 (A 작업 충돌 0)
- 수동(선택, belle 실기기): kip-up fault 문서 열기 → "점수 계산 내역" 100 − 12 = 88 확인,
  실패 원인 상세보기 → 실측 근거/편차표/일치 횟수 확인, 신뢰도 라벨 '높음' 확인
</verification>

<success_criteria>
- kip-up 88점 문서에서 감점 내역(criterion 한국어 라벨 + 측정값 + 허용오차 + 감점점수)이 화면에 보임
- 상세 시트 템플릿 문구 3건("추정한 가능성/확인 필요/거울 보며") 이 실측 근거 보유 시 실데이터로 교체됨
- 신뢰도 라벨-supportCount 모순 해소
- legacy/mode3 doc 안전 (섹션 숨김, 크래시 0), Phase 9 일반 finding 무회귀
- 점수 로직/엔진/pipeline 무변경, 계약 3-way 정합
</success_criteria>

<output>
Create `.planning/quick/260702-q8q-x/260702-q8q-SUMMARY.md` when done
</output>
