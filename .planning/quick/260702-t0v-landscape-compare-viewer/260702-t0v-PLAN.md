---
phase: quick-260702-t0v-landscape-compare-viewer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/KeypointOverlay.tsx
  - app/src/components/VideoCompare.tsx
  - app/src/app/analysis/result.tsx
  - app/src/theme/colors.ts
autonomous: true
requirements: [QUICK-260702-T0V]

must_haves:
  truths:
    - "결과 화면 동작 비교 카드에 '가로로 크게 보기' 버튼이 보인다 (영상 1개 이상일 때)"
    - "버튼 탭 시 가로 전체화면 뷰어가 열리고 두 영상이 좌우로 크게 나란히 보인다 (portrait 고정 유지, 뷰 90° 회전)"
    - "전체화면에서 동기 재생/일시정지, 0.1s step, 타임라인 scrub, 처음으로, 스켈레톤 오버레이 토글이 전부 동작한다"
    - "전체화면 오버레이의 각도 라벨/마커가 세로 카드 대비 뚜렷하게 커져 판독 가능하다 (sizeScale 2.0)"
    - "닫기 버튼 또는 Android back 으로 세로 화면 복귀, 재생 위치/재생 상태 유지"
    - "세로 카드 기존 렌더는 무회귀 (sizeScale 기본값 1 = 기존과 픽셀 동일)"
    - "mode1 / mode3(두 번째+) 모두 동작"
  artifacts:
    - path: "app/src/components/VideoCompare.tsx"
      provides: "전체화면 가로 뷰어 (Modal + 90° rotate) + 진입 버튼 + 컨트롤 공유"
      contains: "rotate"
    - path: "app/src/components/KeypointOverlay.tsx"
      provides: "sizeScale prop (기본 1, 전체화면 2.0)"
      contains: "sizeScale"
    - path: "app/src/theme/colors.ts"
      provides: "전체화면 영상 뷰어 배경 토큰"
  key_links:
    - from: "app/src/app/analysis/result.tsx"
      to: "app/src/components/KeypointOverlay.tsx"
      via: "overlay render prop 2번째 인자 opts.sizeScale 전달"
      pattern: "sizeScale"
    - from: "app/src/components/VideoCompare.tsx"
      to: "expo-video VideoPlayer"
      via: "전체화면 VideoView 가 동일 leftPlayer/rightPlayer 인스턴스에 attach (다중 VideoView attach)"
      pattern: "player=\\{(left|right)Player\\}"
---

<objective>
결과 화면 "동작 비교" 섹션에 가로 전체화면 뷰어를 추가한다. belle 실기기 피드백(TestFlight #27): 스켈레톤 오버레이의 각도 라벨(빨간 말풍선 "158°" 등)이 세로 나란히 2개 레이아웃에서 너무 작아 전혀 안 읽힘. belle 제안 = "누르면 가로로 양 옆의 영상으로 크게".

Purpose: 각도 라벨 가독 확보 = 분석 근거 판독 가능 (분석 정확도 신뢰의 UI 측 마지막 구간).
Output: VideoCompare 내장 가로 전체화면 뷰어 + KeypointOverlay sizeScale prop + result.tsx 배선. OTA 배포 가능 (JS-only).

**회전 방식 결정 (사전 조사 완료):**
- app/package.json 에 expo-screen-orientation 없음 (grep 0건). app.json `orientation: "portrait"` 고정.
- expo-screen-orientation 설치 = native 모듈 추가 = native rebuild → 금지 (OTA 전제).
- 따라서 **portrait 고정 유지 + RN Modal 안에서 뷰 90° 회전(transform rotate)** 으로 가로 레이아웃 시뮬레이트 (검증된 패턴).

**오버레이 스케일 결정 (사전 조사 완료):**
- KeypointOverlay 는 viewBox "0 0 1 1" + 모든 크기(라벨 64×26, fontSize 14, 원 반지름 10/14)를 videoSize.height=1280 으로 나눠 정규화 → 렌더 크기에 **비례 축소**되는 구조. 세로 슬롯(높이 ~290pt)에선 유효 폰트 ~3pt 급 = "전혀 안 읽힘"의 근본 원인.
- 가로 전체화면 전환만으로는 영상 높이 290→~393pt = 라벨 ~1.35배뿐 → 가로 전환 + `sizeScale` prop(전체화면 2.0) 병행 필수.

**플레이어 재사용 결정:**
- expo-video 는 한 VideoPlayer 에 여러 VideoView attach 를 지원. 전체화면 뷰어는 새 player 를 만들지 않고 **기존 leftPlayer/rightPlayer 인스턴스에 두 번째 VideoView 를 attach** → 재생 위치/상태 연속, drift 보정 tick·togglePlay·seekBoth 등 기존 동기 로직이 그대로 양쪽 레이아웃을 제어 (로직 중복 0).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@app/CLAUDE.md
@app/src/components/VideoCompare.tsx
@app/src/components/KeypointOverlay.tsx
@app/src/components/KeypointOverlayToggle.tsx
@app/src/app/analysis/result.tsx
@app/src/theme/colors.ts
@app/src/theme/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: KeypointOverlay sizeScale prop + overlay render prop 시그니처 확장 + result.tsx 배선</name>
  <files>app/src/components/KeypointOverlay.tsx, app/src/components/VideoCompare.tsx, app/src/app/analysis/result.tsx</files>
  <action>
1. **KeypointOverlay.tsx** — `sizeScale?: number` prop 추가 (default 1). 화면상 크기를 갖는 모든 정규화 상수에 곱한다: RADIUS, RADIUS_HI, STROKE_BASE, STROKE_HI, STROKE_CIRCLE_OUTLINE, STROKE_CIRCLE_OUTLINE_HI, 라벨 labelW(64/W)·labelH(26/H)·rx/ry(13/H)·keypoint 우측 offset(14/W)·fontSize(14/H)·텍스트 stroke(0.6/H)·Rect 테두리(1.4/H), 저신뢰 dashArray(4/W). 좌표(positions/axis)는 그대로 — 크기만 스케일. default 1 이면 기존 렌더와 수치 동일 = 세로 카드 무회귀. JSDoc 주석에 근거 명시: "viewBox 정규화 구조라 렌더 크기에 비례 축소 → 전체화면 뷰어가 2.0 전달 (quick-260702-t0v)".

2. **VideoCompare.tsx** — overlay render prop 시그니처를 `(player: VideoPlayer | null, opts?: { sizeScale?: number }) => React.ReactNode` 로 확장 (VideoCompareProps.leftOverlay/rightOverlay + SlotProps.overlay 동일). 기존 세로 VideoSlot 은 opts 생략 호출 (하위호환 — 기존 caller 시그니처와 구조적 호환, 파라미터 추가는 TS 에서 안전).

3. **result.tsx** — leftOverlay/rightOverlay 콜백을 `(player, opts) =>` 로 바꾸고 KeypointOverlay 에 `sizeScale={opts?.sizeScale ?? 1}` 전달 (left 사용자 측 + mode1 right 정은지 측 둘 다). 다른 로직 변경 없음.

주의: KEYPOINT_DELTA_HIGHLIGHT_DEG 등 임계값/강조 로직 절대 무변경. 이모지·하드코딩 색 금지 (기존 '#FFFFFF' 계열 SVG 관례는 기존 그대로 유지).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck</automated>
  </verify>
  <done>sizeScale 기본 1 로 기존 세로 렌더 수치 동일(무회귀), render prop 2번째 인자로 sizeScale 이 KeypointOverlay 까지 전달되는 경로 완성, typecheck 통과.</done>
</task>

<task type="auto">
  <name>Task 2: VideoCompare 가로 전체화면 뷰어 (Modal + 90° rotate) + 진입 버튼 + 토글 유지</name>
  <files>app/src/components/VideoCompare.tsx, app/src/theme/colors.ts, app/src/app/analysis/result.tsx</files>
  <action>
1. **theme/colors.ts** — 전체화면 영상 뷰어 배경 토큰 추가 (예: `videoFullscreenBg: '#000000'`). 주석: 몰입형 영상 전체화면 예외 — 다크 배경 금지 원칙의 의도적 예외, 분석 로딩 navy 예외(loading.tsx) 선례 정합. 라이트 테마 일반 화면에 사용 금지.

2. **VideoCompare.tsx — 컨트롤 추출 (로직 중복 0):** 기존 controls JSX(재생/일시정지 + 0.1s step ×2 + 타임라인 track/thumb/시간라벨 + 처음으로)를 내부 render 함수 또는 내부 컴포넌트로 추출해 세로 카드와 전체화면이 **같은 핸들러(togglePlay/stepBy/seekBoth/restart/panResponder)와 같은 JSX** 를 공유. `dark?: boolean` 옵션으로 어두운 배경 위 색만 토큰 분기 (시간 라벨 = colors.textWhite 계열, step 버튼 배경 반투명). PanResponder 의 locationX 는 RN 이 transform 반영 로컬 좌표로 매핑하므로 회전 뷰에서도 동작 — trackWidthRef 는 onLayout 기준이라 그대로 유효.

3. **VideoCompare.tsx — 진입 버튼:** 영상 row 아래(자동 구간 맞춤 배지 근처)에 전체 너비 pill 버튼 "가로로 크게 보기" (Ionicons `expand` + 텍스트, brandTint 배경 + brand 텍스트, radius/spacing 토큰). hasAny 일 때만 렌더. accessibilityRole="button", accessibilityLabel="가로 전체화면으로 크게 보기", hitSlop.

4. **VideoCompare.tsx — 전체화면 Modal:** `const [fullscreen, setFullscreen] = useState(false)` + `{fullscreen && <Modal visible transparent={false} animationType="fade" statusBarTranslucent onRequestClose={() => setFullscreen(false)} supportedOrientations={['portrait']}>...}` (조건부 렌더 — 닫힌 동안 native 리소스 0). Modal 내부:
   - `useWindowDimensions()` 로 winW/winH. 배경 wrapper = flex 1, videoFullscreenBg. 그 안에 **회전 컨테이너**: `{ position: 'absolute', width: winH, height: winW, left: (winW - winH) / 2, top: (winH - winW) / 2, transform: [{ rotate: '90deg' }] }` — 검증된 portrait-고정 가로 시뮬레이트 패턴.
   - 회전 컨테이너 레이아웃: 상단 bar (좌 = slot 라벨 없이 여백, 우 = `headerExtra` 슬롯 + 닫기 버튼), 중앙 flexDirection row 로 두 영상 슬롯 (각 flex 1, 높이 최대), 하단 = 공유 컨트롤(dark). 슬롯 라벨("내 영상"/"정은지 선수")은 각 영상 하단에 작게 (textWhite 계열).
   - 각 슬롯 = `<VideoView player={leftPlayer|rightPlayer} contentFit="contain" nativeControls={false} allowsFullscreen={false} allowsPictureInPicture={false} />` — **기존 player 인스턴스 재사용** (expo-video 다중 VideoView attach). 새 useVideoPlayer 호출 금지. url 없는 슬롯은 기존 slotEmpty 스타일 준용 자리표시.
   - 오버레이: 슬롯 위 absolute 컨테이너(pointerEvents "none")에서 `overlay?.(player, { sizeScale: FULLSCREEN_OVERLAY_SCALE })` 호출. 파일 상단 상수 `const FULLSCREEN_OVERLAY_SCALE = 2.0;` + 근거 주석 (라벨 유효 크기 = 14 × 렌더높이/1280 이라 가로 전환 1.35배만으로 부족 → 2.0 배 병행, belle 실기기에서 미세조정 가능하도록 상수화).
   - 닫기 버튼: Ionicons `close`, accessibilityRole="button", accessibilityLabel="전체화면 닫기", hitSlop 12. 탭 → setFullscreen(false). Android back = onRequestClose 동일 처리.
   - StatusBar: Modal 내부에 expo-status-bar `<StatusBar hidden />` (모달 unmount 시 root \_layout 의 StatusBar 로 자연 복귀).
   - 재생 상태: 열고 닫을 때 pause/seek 조작 없음 — 같은 player 라 위치·재생 상태 자동 연속. drift 보정 tick effect 는 컴포넌트 단일 인스턴스라 그대로 양 레이아웃 제어.

5. **VideoCompare.tsx — `fullscreenHeaderExtra?: React.ReactNode` prop 추가:** 전체화면 상단 bar 에 렌더. **result.tsx** 가 `fullscreenHeaderExtra={<KeypointOverlayToggle value={overlayVisible} onValueChange={handleToggleOverlay} />}` 전달 → 전체화면에서도 오버레이 토글 유지 (state 단일 출처 = result.tsx, 토글 시 render prop 재실행으로 전체화면 오버레이 즉시 반영). mode3 second+ 도 동일 (left 오버레이만 있는 케이스 정상).

주의: 기존 세로 카드 렌더 경로(스타일 포함) 무변경 — 추출한 컨트롤이 세로에서 기존과 동일하게 렌더되는지 diff 로 확인. 한국어 카피만, 이모지 금지, 색/간격은 토큰만 (videoFullscreenBg 포함). 주석에 스펙 인용: "quick-260702-t0v (belle TestFlight #27 — 각도 라벨 가독)".
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck && grep -c "useVideoPlayer" src/components/VideoCompare.tsx | grep -qx "2" && grep -q "FULLSCREEN_OVERLAY_SCALE" src/components/VideoCompare.tsx && grep -q "videoFullscreenBg" src/theme/colors.ts</automated>
  </verify>
  <done>typecheck 통과. useVideoPlayer 호출이 기존 2개 그대로 (player 신규 생성 0 = 로직/자원 중복 0). 전체화면 Modal + 90° rotate 컨테이너 + 진입 버튼 + 닫기/back + 토글 유지 + sizeScale 2.0 오버레이 배선 완료. 세로 카드 렌더 무회귀.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (없음 — 신규 없음) | UI 전용 변경. 신규 네트워크 경로/입력 파싱/패키지 설치 0. 영상 URL 은 기존 S3 presigned 경로 그대로 소비. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-t0v-01 | DoS(자원) | 전체화면 Modal 다중 VideoView | mitigate | Modal 조건부 렌더(`{fullscreen && ...}`) — 닫힌 동안 추가 native 뷰 0. 신규 player 인스턴스 생성 금지(기존 2개 재사용). |
| T-t0v-SC | Tampering | npm installs | accept | 패키지 설치 없음 (JS-only, 기존 의존성만 사용). |
</threat_model>

<verification>
- `cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck` 통과 (유일한 정적 게이트 — 프로젝트에 lint/test runner 없음).
- app.json / package.json diff 0 (native rebuild 유발 변경 금지 확인).
- 세로 카드: sizeScale 미전달 경로가 기존 수치와 동일한지 코드 리뷰로 확인.
</verification>

<success_criteria>
- 동작 비교 카드에 "가로로 크게 보기" 버튼 → 탭 시 가로 전체화면 (portrait 고정 + 뷰 90° 회전).
- 전체화면: 두 영상 좌우 확대, 동기 재생/일시정지/0.1s step/scrub/처음으로/오버레이 토글 전부 동작, 각도 라벨 sizeScale 2.0 으로 판독 가능.
- 닫기/Android back → 세로 복귀 + 재생 위치·상태 연속.
- typecheck 통과, native 영향 변경 0 (OTA 배포 가능), mode1/mode3 둘 다 동작.
- SUMMARY 에 belle 실기기 체크리스트 포함:
  1. mode1 결과 → 가로로 크게 보기 → 각도 라벨(빨간 말풍선) 판독 가능한가 (부족하면 FULLSCREEN_OVERLAY_SCALE 상향)
  2. 전체화면 재생/일시정지/0.1s step/타임라인 드래그 동작 (드래그 방향이 회전 뷰에서 자연스러운가)
  3. 오버레이 토글 ON/OFF 즉시 반영
  4. 닫기 후 세로 복귀 + 재생 위치 유지
  5. mode3(두 번째+ 분석) 동일 확인
  6. Android 실기기: back 버튼 닫힘 + 레이아웃 정상 (edgeToEdge 환경)
</success_criteria>

<output>
Create `.planning/quick/260702-t0v-landscape-compare-viewer/260702-t0v-SUMMARY.md` when done
</output>
