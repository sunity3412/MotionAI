---
phase: quick-260704-fz4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/shared/python/sunity_shared/analysis/fault_zoom.py
  - backend/functions/pipeline/app.py
  - backend/tests/test_fault_zoom.py
  - app/src/types/analysis.ts
  - app/src/theme/colors.ts
  - app/src/lib/deductionLabels.ts
  - app/src/components/KeypointOverlay.tsx
  - app/src/components/FaultZoomCompare.tsx
  - app/src/components/ForcePatternDetailModal.tsx
  - app/src/app/analysis/result.tsx
autonomous: true
requirements: [QT-260704-fz4]
must_haves:
  truths:
    - "편차표에서 감점 근거 관절은 빨강(brand), |delta|>20° 인데 감점 없는 관절은 주황 + '감점 아님' 캡션으로 구분된다 (2단 시각 언어, CONTEXT locked)."
    - "스켈레톤 마커(KeypointOverlay)와 확대 카드에도 동일 2단 규칙이 적용된다 — 확정 결함 다리=빨강 유지 + 측정 초과 어깨=주황 추가 형태."
    - "측정 초과 관절에도 backend 가 '참고·확인 권장' 확대 카드를 생성하고 tier 필드로 확정 카드와 구분된다. 채점/veto/게이트 무접촉."
    - "편차표 강조 행을 탭하면 해당 부위 확대 비교 이미지가 시트 안에서 인라인으로 표시된다."
    - "편차표 각 행에 각도 의미 한 단어 라벨(팔꿈치 굽힘/겨드랑이 벌림 등 8관절 고정 사전)이 표시된다."
    - "legacy doc(tier/windowMedianAngleDeltas 부재)은 기존 동작 그대로 (하위호환 — tier 부재 = confirmed 취급, advisory 카드 미생성)."
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/fault_zoom.py"
      provides: "select_advisory_joints 순수 헬퍼 (측정 초과 관절 선별 — tol 초과·확정 제외·|delta| 내림차순·cap)"
      contains: "select_advisory_joints"
    - path: "backend/functions/pipeline/app.py"
      provides: "_render_fault_zoom tier 방출 + _attach_fault_zoom_comparisons advisory 카드 배선 (windowMedianAngleDeltas → _KISMAM_TO_KEYPOINT → advisory)"
      contains: "advisory"
    - path: "backend/tests/test_fault_zoom.py"
      provides: "select_advisory_joints 단위테스트 (임계/제외/cap/정렬)"
    - path: "app/src/types/analysis.ts"
      provides: "FaultZoomComparison.tier 옵션 scalar 필드 (lockstep)"
      contains: "tier"
    - path: "app/src/lib/deductionLabels.ts"
      provides: "ANGLE_MEANING_KO 8관절 의미 사전 + KEYPOINT_FROM_ANGLE_KEY 역매핑 + REGION_MEMBER_KEYPOINTS"
      contains: "ANGLE_MEANING_KO"
    - path: "app/src/components/KeypointOverlay.tsx"
      provides: "attentionKeypoints prop — 주황 마커/라벨 (빨강 우선)"
      contains: "attentionKeypoints"
    - path: "app/src/components/ForcePatternDetailModal.tsx"
      provides: "편차행 tier 색/캡션 + 의미 라벨 + 행 탭 인라인 확대 이미지"
  key_links:
    - from: "backend/functions/pipeline/app.py::_attach_fault_zoom_comparisons"
      to: "result.visionVeto.windowMedianAngleDeltas.deltas"
      via: "_KISMAM_TO_KEYPOINT 매핑 후 fault_zoom.select_advisory_joints → advisory 카드"
      pattern: "select_advisory_joints"
    - from: "app/src/app/analysis/result.tsx"
      to: "app/src/components/KeypointOverlay.tsx"
      via: "confirmed/attention keypoint set 단일 조립 후 highlightKeypoints/attentionKeypoints prop"
      pattern: "attentionKeypoints"
    - from: "app/src/components/ForcePatternDetailModal.tsx"
      to: "result.faultZoomComparisons"
      via: "행별 zoomCard 매칭(joint/region) → 탭 시 인라인 Image"
      pattern: "zoomCard"
---

<objective>
belle 실기기 피드백(TestFlight #27, kip-up fault 88) 3건 해소 — 결함 시각 언어 2단화(빨강=확정 감점 / 주황=측정 초과·확인 권장), 측정 초과 관절에도 "참고" 확대 카드 생성 + 편차행 탭 → 부위 확대 인라인 표시, 각도 의미 라벨(8관절 고정 사전).

Purpose: "스켈레톤은 다리 4개 빨강인데 텍스트는 어깨 얘기 → 어떻게 읽어야 하지?" 혼란 제거. 시스템이 아는 것(측정 초과)과 확신하는 것(감점 확정)을 구분해 말하는 것 자체가 신뢰 장치. 위양성 교훈([[window-median-silent-seed-fp-reverted]]) 존중 — 측정 초과는 감점이 아니라 "참고"임을 카피로 명시.
Output: backend advisory tier 카드 + 앱 2단 시각 언어(표/마커/카드) + 행 탭 인라인 확대 + 의미 라벨. 채점 무접촉.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260704-fz4-fault-visual-2tier-tap-zoom/260704-fz4-CONTEXT.md
@backend/shared/python/sunity_shared/analysis/fault_zoom.py
@backend/functions/pipeline/app.py (2409-2650 부근: _KISMAM_TO_KEYPOINT / _render_fault_zoom / _attach_fault_zoom_comparisons / _attach_mode3_fault_zoom)
@app/src/types/analysis.ts (FaultZoomComparison / VisionWindowMedianAngleDeltas / DeductionRecord)
@app/src/lib/deductionLabels.ts
@app/src/components/KeypointOverlay.tsx
@app/src/components/FaultZoomCompare.tsx
@app/src/components/ForcePatternDetailModal.tsx
@app/src/app/analysis/result.tsx (560-712: veto/measuredEvidence 조립, 1068-1076: FaultZoomCompare 박제)
@app/src/theme/colors.ts
</context>

<key_facts>
사전 조사로 확정된 사실 (executor 재탐색 불요):

- **관절 이름공간 2벌**: `windowMedianAngleDeltas.deltas[].joint` = kismam angle key (`left_elbow` 등, skeleton.JOINT_KEYS 8개). 확대 카드/`faultJoints` = keypoint 이름 (`left_hand` 등). 매핑은 pipeline `_KISMAM_TO_KEYPOINT` (app.py:2409, left_elbow→left_hand, 나머지 1:1). 앱 측 역방향은 KeypointOverlay `JOINT_KEY_TO_ANGLE_KEY` (keypoint→angle key)의 역매핑이 필요 — 신설 `KEYPOINT_FROM_ANGLE_KEY` 를 deductionLabels.ts 에 둔다.
- **20° 임계 기존 상수**: 앱 = `KEYPOINT_DELTA_HIGHLIGHT_DEG` (KeypointOverlay.tsx:34, export 됨). backend = `dimensions._LINE_TOL_DEG = 20.0` (dimensions.py:165). 신규 튜닝 상수 선언 금지 — 양쪽 다 기존 상수 import/참조 (정합 주석 필수).
- **편차표 위치**: ForcePatternDetailModal.tsx (실패 원인 상세 시트, Modal bottom-sheet)의 `measuredEvidence.jointDeltas` — result.tsx 664-711 useMemo 가 조립. 현재 행 tier 는 `overTolerance: boolean` 단일.
- **contract 문서화 범위**: faultZoomComparisons 는 docs/contract.md 에 섹션이 없다. `region` 필드 선례(quick-260702-sic)와 동일하게 TS 타입 주석 + Python 방출부 주석으로 lockstep — contract.md 수정 불요.
- **Firestore flat 제약**: 카드 dict 는 `_validate_dict_only_scalars` 통과 필요 — tier 는 scalar str 이라 통과. app normalize(userAnalyses.ts)는 faultZoomComparisons 를 무가공 통과시킴 — tier 자동 전달.
- **S3 키**: 확정 카드 `zoom_{joint}.png`. advisory 는 `zoom_adv_{joint}.png` 로 분리(충돌 원천 차단).
- **grouping 조건**: `_group_fault_joints` 는 kind 전원 동일 non-None 일 때만 좌+우 묶음. advisory 배치에 `joint_kinds={j:'deficit'}` 를 주면 양어깨 → arms region 1장 grouping 이 그대로 작동.
- **veto 없는 경로**: windowMedianAngleDeltas 는 visionVeto.status='applied' 에서만 존재 → advisory 카드는 Mode1 + veto applied 에서만 생성. Mode3(`_attach_mode3_fault_zoom`)는 무접촉.
- **pod 재기동**: backend 변경분은 pod 재기동+재분석 필요 (orchestrator 몫 — 이 plan 은 로컬 pytest 까지). 앱 변경은 OTA (JS-only).
</key_facts>

<tasks>

<task type="auto">
  <name>Task 1: backend — advisory tier 확대 카드 생성 (측정 초과 관절)</name>
  <files>backend/shared/python/sunity_shared/analysis/fault_zoom.py, backend/functions/pipeline/app.py, backend/tests/test_fault_zoom.py</files>
  <action>
CONTEXT locked 결정 "측정 초과 관절에도 확대 카드 생성 + 참고/확인 권장 티어" 구현. 채점/veto/게이트 무접촉 — 카드 생성·방출만.

1. **fault_zoom.py — 순수 헬퍼 신설** `select_advisory_joints(kp_deltas: dict[str, float], confirmed: set[str], tol_deg: float, max_items: int = 2) -> list[str]`:
   - 입력 kp_deltas 는 이미 keypoint 이름공간으로 매핑된 {keypoint: delta_deg(signed)} (매핑은 호출측 pipeline 책임 — fault_zoom 은 이름공간 무지 유지).
   - 반환: |delta| > tol_deg AND keypoint not in confirmed 인 것만, |delta| 내림차순, max_items cap. 비유한/None delta 는 skip (defensive).
   - docstring 에 위양성 교훈 명시: advisory 는 "측정 초과 = 참고"이지 결함 확정이 아님 ([[window-median-silent-seed-fp-reverted]] — window median 을 감점 seed 로 쓰면 FP, 표시 전용으로만 사용). 채점 입력으로 쓰지 말 것.

2. **pipeline app.py `_render_fault_zoom`** — tier 방출 확장:
   - 옵션 파라미터 추가: `advisory_joints: list[str] | None = None, advisory_deltas: dict[str, float] | None = None` (keyword-only 권장).
   - 프레임 추출(FfmpegFrameExtractor)은 기존 1회 그대로 두고, `build_fault_zoom_comparisons` 를 두 번 호출: (a) 기존 확정 배치 무수정, (b) advisory 배치 = advisory_joints + advisory_deltas + `joint_kinds={j: "deficit" for j in advisory_joints}` (grouping 활성 목적) + 동일 frame override/dtw_match. advisory_joints 가 비면 (b) 생략 — 기존 경로 100% 보존.
   - 방출 item: 확정 배치는 `item["tier"] = "confirmed"`, advisory 배치는 `item["tier"] = "advisory"` + S3 키 `zoom_adv_{joint}.png` (확정은 기존 `zoom_{joint}.png` 유지). advisory item 의 kind 는 방출하지 않는다 (kind='deficit' 은 grouping 용 내부 전달일 뿐 — 앱 캡션이 "부족해요" 확정 어조로 오인하지 않게 tier 가 캡션을 소유). 주석: scalar str 이라 _validate_dict_only_scalars 통과, TS lockstep FaultZoomComparison.tier.
   - max_items 총합이 캐러셀 과밀이 되지 않게 advisory 는 최대 2장 (select_advisory_joints cap).

3. **pipeline app.py `_attach_fault_zoom_comparisons`** (Mode1 전용) — advisory 배선:
   - vv.status 확인 후 `wmad = vv.get("windowMedianAngleDeltas") or {}`, `deltas = wmad.get("deltas") or []`. 각 d 의 joint(kismam angle key)를 `_KISMAM_TO_KEYPOINT` 로 keypoint 매핑 → kp_deltas dict.
   - confirmed set = fault_joints (keypoint 이름공간, veto faultJoints 또는 편차 top-2 폴백 결과).
   - tol = `dimensions._LINE_TOL_DEG` import 재사용 (신규 상수 0 — IPSF 허용오차 20° 단일 출처, 앱 KEYPOINT_DELTA_HIGHLIGHT_DEG 와 정합 주석).
   - `advisory = fault_zoom.select_advisory_joints(kp_deltas, set(fault_joints), tol)` → `_render_fault_zoom(..., advisory_joints=advisory, advisory_deltas={kp: abs(delta)})`. deltas 부재/legacy → advisory 빈 리스트 → 무변화.
   - `_attach_mode3_fault_zoom` 은 무수정 (advisory 파라미터 default None → 기존 동작).

4. **test_fault_zoom.py** — select_advisory_joints 단위테스트 추가 (기존 16개 무수정 PASS 유지):
   - 임계 초과만 선별 (21° 포함 / 19° 제외, tol=20)
   - confirmed 관절 제외
   - |delta| 내림차순 정렬 + max_items cap
   - 음수 delta 도 |delta| 로 판정
   - 비유한(nan)/미존재 graceful skip
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && python -m pytest tests/test_fault_zoom.py -q</automated>
  </verify>
  <done>select_advisory_joints 순수 헬퍼 + 테스트 GREEN(기존 16개 포함), _render_fault_zoom 이 tier(confirmed/advisory) scalar 방출, _attach_fault_zoom_comparisons 가 windowMedianAngleDeltas 초과 관절을 advisory 카드로 배선. 채점 코드 diff 0, Mode3 무접촉, legacy 입력 시 기존 산출 동일.</done>
</task>

<task type="auto">
  <name>Task 2: app — 2단 시각 언어(표·마커·카드) + 각도 의미 라벨</name>
  <files>app/src/types/analysis.ts, app/src/theme/colors.ts, app/src/lib/deductionLabels.ts, app/src/components/KeypointOverlay.tsx, app/src/components/FaultZoomCompare.tsx, app/src/app/analysis/result.tsx</files>
  <action>
CONTEXT locked "2단 시각 언어 — 표·스켈레톤 마커·확대 카드 전부 동일 규칙" + "각도 의미 라벨" 구현. Claude's Discretion 행사: 주황 톤 = 기존 warnAmber(#E6A300) 계열 alias (노랑보다 '주의' 의미론 정합, 기존 occlusion/부상경고 amber 와 일관).

1. **types/analysis.ts** — `FaultZoomComparison.tier?: 'confirmed' | 'advisory' | null` 추가. 주석: 부재(legacy doc) = confirmed 취급, Python lockstep pipeline `_render_fault_zoom` (region 선례와 동일 — contract.md 섹션 없음).

2. **theme/colors.ts** — alias 토큰 신설 (Phase 4 alias 선례 형식): `advisoryOrange: '#E6A300'` (warnAmber alias — 측정 초과·확인 권장 2단 시각 언어, 감점 아님 표시 전용. brand 빨강=확정 감점과 짝) + `advisoryOrangeBg: '#FFF6E5'` (warnAmberBg alias — advisory 배지/칩 배경). 정합 주석 필수.

3. **deductionLabels.ts** — 단일 출처 확장 (중복 2벌 금지 관례):
   - `ANGLE_MEANING_KO: Record<string, string>` — 8 angle key 고정 사전. left/right_elbow: '팔꿈치 굽힘', left/right_shoulder: '겨드랑이 벌림', left/right_hip: '다리 벌림', left/right_knee: '무릎 굽힘'. 주석: kismam 관절각 의미의 한 단어 설명 (CONTEXT locked — 8관절 고정 사전으로 충분). 구현 전 kismam.py/skeleton.py JOINT_ANGLES 정의를 열어 각도의 실제 기하(어느 세그먼트 사이 각인지)와 라벨이 맞는지 확인하고, 어긋나면 라벨 문구를 기하에 맞게 조정 (예: hip 각이 몸통-허벅지 각이면 '다리 벌림' 유지 적절성 판단).
   - `KEYPOINT_FROM_ANGLE_KEY: Record<string, KeypointName>` — angle key → keypoint (left_elbow→left_hand, 나머지 1:1). 주석: pipeline _KISMAM_TO_KEYPOINT / KeypointOverlay JOINT_KEY_TO_ANGLE_KEY 역방향 정합.
   - `REGION_MEMBER_KEYPOINTS: Record<'legs' | 'arms', readonly KeypointName[]>` — backend fault_zoom._REGION_JOINTS 의 8-keypoint 부분집합 미러 (legs: hips+knees, arms: shoulders+hands). 주석으로 backend 정합 명시.

4. **result.tsx — 2단 set 단일 조립** (표·마커·카드가 같은 소스를 쓰는 지점):
   - `confirmedKeypoints: Set<KeypointName>` useMemo = (vetoFaultJoints ?? []) ∪ deductionBreakdown.records 중 criterion 이 `angle_vs_reference__{jk}` 인 record 의 jk 를 KEYPOINT_FROM_ANGLE_KEY 로 매핑한 것. 주석: 빨강 = 확정 결함(감점 근거 deductionBreakdown records — faultJoints 는 vision record(split_angle 등 관절명 없는 criterion)의 관절 투영, CONTEXT locked).
   - `attentionKeypoints: KeypointName[]` useMemo = visionVeto applied 의 windowMedianAngleDeltas.deltas 중 |delta_deg| > KEYPOINT_DELTA_HIGHLIGHT_DEG 인 joint(angle key)를 keypoint 매핑, confirmedKeypoints 제외, dedup. legacy/부재 → 빈 배열.
   - KeypointOverlay(사용자 측)에 `attentionKeypoints` prop 전달.

5. **KeypointOverlay.tsx — attentionKeypoints prop** (주황 마커):
   - `attentionKeypoints?: readonly KeypointName[]` 신설. highlighted(빨강) set 계산은 무수정. attention set = prop 중 report.joints 에 존재하고 highlighted 에 없는 것 (빨강 우선 — 겹치면 확정이 이긴다).
   - 렌더: attention 관절 원 = `colors.advisoryOrange` fill + 흰 외곽선, RADIUS_HI/STROKE 는 강조와 동일 스케일 (가독 선례 Phase 20 A2 유지). bone 은 attention 만으로는 색 변경하지 않음 (빨강 bone 의 '확정' 의미 보존 — 마커 점만 주황). floating 각도 라벨: attention 관절도 표시하되 pill 배경 = advisoryOrange (기존 brand pill 로직 분기). 저신뢰(conf<0.5) 관절은 기존 estimateGray 규칙이 우선 (advisory 로 승격 금지).
   - prop 미전달 시 렌더 diff 0 (하위호환).

6. **FaultZoomCompare.tsx — advisory 카드 구분**:
   - `tier === 'advisory'` 카드: 이미지 위 또는 캡션 위에 배지 `참고 · 감점 아님` (advisoryOrangeBg 배경 + advisoryOrange 텍스트 칩). 캡션: `${label} · 기준과 ${Math.round(deficitDeg)}° 차이 — 감점은 아니지만 확인해 보세요` (deficitDeg 없으면 `${label} · 기준과 차이가 커요 — 감점은 아니에요`). "감점 아님" 문구는 CONTEXT locked 필수 — 사용자가 주황을 감점으로 오해하지 않게.
   - tier 부재/‘confirmed’ = 기존 캡션 로직 무수정 (legacy 무회귀). carousel key 는 tier 를 포함해 `${item.tier ?? 'confirmed'}-${item.joint}` 로 갱신 (확정/advisory joint 는 서로 배타지만 방어).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck</automated>
  </verify>
  <done>tier 타입/alias 토큰/의미사전/역매핑이 단일 출처에 존재. 마커: 확정=brand 빨강(무수정) + 측정 초과=advisoryOrange 추가, 겹침 시 빨강 우선, prop 미전달 렌더 diff 0. advisory 확대 카드에 "감점 아님" 배지+캡션. typecheck GREEN. 하드코딩 색 0 (토큰만).</done>
</task>

<task type="auto">
  <name>Task 3: app — 편차행 tier 표시 + 의미 라벨 + 행 탭 인라인 확대</name>
  <files>app/src/app/analysis/result.tsx, app/src/components/ForcePatternDetailModal.tsx</files>
  <action>
CONTEXT locked "편차행 탭 → 부위 확대" 구현. Claude's Discretion 행사: **시트 내 인라인 확장** 채택 (모달 시트 위에서 닫기→본문 스크롤→carousel 포커스 방식은 컨텍스트 단절 + 스크롤 좌표 취약 — 행 바로 아래 이미지가 belle 아이디어 "행을 탭하면 그 부위 확대 사진 + 각도"에 직접 부합). 원값(내 X°/기준 Y°)은 기본 노출 유지 (재량 — 측정값이 분석 시점 저장 결정 수치임을 보여주는 신뢰 장치, CONTEXT specifics).

1. **result.tsx — measuredEvidence 행 확장** (664-711 useMemo):
   - jointDeltas 각 행에 추가: `tier: 'confirmed' | 'advisory' | 'normal'` (joint 를 KEYPOINT_FROM_ANGLE_KEY 매핑 후 confirmedKeypoints 포함 → confirmed; 아니고 |delta| > KEYPOINT_DELTA_HIGHLIGHT_DEG → advisory; 그 외 normal — Task 2 의 set 재사용, 기존 overTolerance boolean 은 tier 로 대체), `meaningLabel: string` (ANGLE_MEANING_KO[d.joint] ?? '' — 한 단어 의미), `zoomCard?: { imageUrl: string; tier: 'confirmed' | 'advisory'; label: string }` — result.faultZoomComparisons 에서 매칭: 카드.joint === 매핑 keypoint OR (카드.region 있고 매핑 keypoint ∈ REGION_MEMBER_KEYPOINTS[region]). 첫 매치 사용, 없으면 undefined. label 은 FaultZoomCompare caption 관례 재사용 (region 우선 REGION_LABEL_KO, 아니면 JOINT_LABEL_KO).
   - MeasuredEvidence 타입(ForcePatternDetailModal.tsx 선언)을 함께 갱신 — overTolerance 제거·tier/meaningLabel/zoomCard 추가 시 accessibilityLabel 조립부도 동기 수정.

2. **ForcePatternDetailModal.tsx — 행 렌더 개편**:
   - 행 좌측: 관절 라벨 + 그 아래(또는 옆) 의미 라벨 작은 텍스트 (`meaningLabel`, textLo 톤) — "168.9° 가 무슨 각인지" 해소.
   - tier 스타일: confirmed → 기존 deltaOver(brand) 유지, advisory → advisoryOrange 텍스트 + 행 끝에 `감점 아님` 미니 칩 (advisoryOrangeBg 배경, CONTEXT locked 필수 카피), normal → 무강조.
   - 행 탭: `zoomCard` 있는 행만 Pressable (accessibilityRole="button", accessibilityLabel `${jointLabel} 부위 확대 비교 보기`, hitSlop). 우측에 chevron(Ionicons chevron-down/up) affordance. 탭 시 해당 행 바로 아래 인라인 확장: `<Image source={{uri: zoomCard.imageUrl}} resizeMode="contain">` aspectRatio 약 2:1 (합성 [내|기준] 2:1 비율, FaultZoomCompare 선례) + 좌/우 미니 라벨('내 영상'/'기준') + tier==='advisory' 면 그 아래 `측정값 참고용이에요 — 감점에 반영되지 않았어요` 캡션 (textLo). 확장 state = useState<number | null> (한 번에 한 행, 재탭 닫기). zoomCard 없는 행은 기존 정적 행 (탭 affordance 없음 — 빈 인터랙션 금지).
   - "확인하기 —" dot 행 카피를 인라인 확대 존재에 맞게 갱신: measuredEvidence 존재 시 `강조된 행을 탭하면 그 부위 확대 사진을 볼 수 있어요` 계열로 (기존 "위 '문제 부위 확대 비교'와 편차표…" 문구 대체 여부는 존재하는 zoomCard 유무로 분기).
   - Phase 9 일반 finding(비 fallback) 경로는 measuredEvidence undefined → 렌더 diff 0 (무회귀).

3. **게이트 실행** (plan 전체 검증): backend pytest fault_zoom + app typecheck. 커밋 분리: Task 1 backend / Task 2-3 app 각각 conventional commit.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck && cd /Users/kimtaesung/Dev/SunityMotion/backend && python -m pytest tests/test_fault_zoom.py -q</automated>
    <human-check>다음 pod 재분석 후 실기기: kip-up fault 화면에서 다리 행/마커=빨강 + 어깨 행/마커=주황('감점 아님'), 어깨 행 탭 → 어깨 확대 사진 인라인, 각 행 의미 라벨 표시</human-check>
  </verify>
  <done>편차표 행이 tier 색(빨강/주황/무강조) + 의미 라벨 + '감점 아님' 칩을 표시하고, zoomCard 매칭 행 탭 시 시트 안에서 해당 부위 확대 이미지가 인라인 표시된다. Phase 9 일반 finding 시트는 렌더 diff 0. typecheck + fault_zoom pytest GREEN.</done>
</task>

</tasks>

<verification>
- backend: `python -m pytest tests/test_fault_zoom.py -q` GREEN — 기존 16개 무수정 PASS + select_advisory_joints 신규 케이스.
- app: `npm run typecheck` GREEN.
- 채점 무접촉 증명: diff 가 dimensions.py/kismam.py/deduction_engine/vision_veto 판정 경로를 건드리지 않음 (fault_zoom/pipeline 카드 방출부 + 앱 표시 계층만).
- 하위호환: tier/windowMedianAngleDeltas 부재 legacy doc → advisory 카드 0장, 마커/표/카드 기존 렌더와 동일.
</verification>

<success_criteria>
- 2단 시각 언어가 표(ForcePatternDetailModal 편차표)·스켈레톤 마커(KeypointOverlay)·확대 카드(FaultZoomCompare) 3면에서 동일 데이터 소스(confirmedKeypoints/attentionKeypoints, result.tsx 단일 조립)로 작동.
- 측정 초과 관절 확대 카드가 tier='advisory' + "감점 아님" 카피로 생성·표시 (채점 0 영향).
- 편차행 탭 → 시트 내 인라인 부위 확대. 각도 의미 라벨 8관절 전부.
- 신규 튜닝 상수 0 (20° = KEYPOINT_DELTA_HIGHLIGHT_DEG / dimensions._LINE_TOL_DEG 재사용), 하드코딩 색 0.
</success_criteria>

<output>
Create `.planning/quick/260704-fz4-fault-visual-2tier-tap-zoom/260704-fz4-SUMMARY.md` when done.
백엔드 변경분은 pod 재기동+재분석 필요 (orchestrator 몫) — SUMMARY 에 명시.
</output>
