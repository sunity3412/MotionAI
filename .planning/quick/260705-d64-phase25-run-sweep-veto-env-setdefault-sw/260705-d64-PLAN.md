---
phase: quick-260705-d64
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/evals/phase25/run_sweep.py
  - backend/evals/phase25/README.md
autonomous: true
requirements: [QUICK-VETO-ENV]
must_haves:
  truths:
    - "새 pod 의 깨끗한 셸에서 run_sweep.py 를 실행해도 vision veto 경로가 기본 ON 이다 (2026-07-05 사고 재발 불가)"
    - "운영자가 GEMINI_VISION_VETO_ENABLED=0 을 명시 export 하면 setdefault 가 덮지 않는다 (A/B 비교 가능)"
    - "README/docstring 의 Pod sweep env 블록만 따라 해도 veto env 누락이 발생하지 않는다"
  artifacts:
    - path: "backend/evals/phase25/run_sweep.py"
      provides: "module-level GEMINI_VISION_VETO_ENABLED/GEMINI_MAX_VETO_WALL_S setdefault + docstring env 반영"
      contains: "GEMINI_VISION_VETO_ENABLED"
    - path: "backend/evals/phase25/README.md"
      provides: "Pod sweep 블록의 veto env 자동 setdefault 명기 + 함정 설명"
      contains: "GEMINI_VISION_VETO_ENABLED"
  key_links:
    - from: "backend/evals/phase25/run_sweep.py"
      to: "backend/functions/pipeline/app.py"
      via: "os.environ setdefault → pipeline 로드(_load_pipeline) 전 env 주입, app.py:259 가 소비"
      pattern: "os\\.environ\\.setdefault\\(\"GEMINI_VISION_VETO_ENABLED\""
---

<objective>
phase25 run_sweep.py 에 Gemini vision veto env 2종(`GEMINI_VISION_VETO_ENABLED=1`, `GEMINI_MAX_VETO_WALL_S=300`)을 module-level `os.environ.setdefault` 로 박제하고, 문서화된 Pod sweep env 블록(docstring + README)에 반영한다.

Purpose: 2026-07-05 새 pod(svn31pzja7uay0) sweep 무효화 사고 재발 방지 — 실행 셸에 veto env 가 없으면 veto 경로 전체가 조용히 OFF 되어 `visionVeto: disabled` + `deductionBreakdown: None` + 레거시 min-of-core 점수가 나온다. eval 은 production(`/workspace/start_server.sh` 영구 박제 구성, 2026-07-02 FP 사고 fix)을 항상 mirror 해야 한다. `RTMW_DETERMINISTIC` setdefault 와 정확히 같은 선례 패턴.

Output: run_sweep.py setdefault 블록 + docstring env 갱신, README Pod sweep 블록 갱신, 기존 게이트 테스트 GREEN 유지.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend/evals/phase25/run_sweep.py
@backend/evals/phase25/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: run_sweep.py 에 veto env setdefault 블록 추가 + docstring env 예시 갱신</name>
  <files>backend/evals/phase25/run_sweep.py</files>
  <action>
1) module-level `os.environ.setdefault("RTMW_DETERMINISTIC", "1")` (line 75) 바로 아래에 새 주석 블록 + setdefault 2줄 추가:
   - `os.environ.setdefault("GEMINI_VISION_VETO_ENABLED", "1")`
   - `os.environ.setdefault("GEMINI_MAX_VETO_WALL_S", "300")`
   - 주석(한국어 why, 이모지 금지, 기존 RTMW 블록 문체 승계): 2026-07-05 신규 pod sweep 무효화 사고 — 실행 셸에 env 미설정이면 veto 경로가 조용히 OFF (visionVeto disabled → deductionBreakdown 없음 + 레거시 min-of-core 점수) 로 sweep 전체가 무효. eval 은 production `/workspace/start_server.sh` 박제 구성(GEMINI_VISION_VETO_ENABLED=1 + GEMINI_MAX_VETO_WALL_S=300, 2026-07-02 FP 재발 사고 fix)을 mirror 한다. setdefault 이므로 운영자가 명시적으로 export 하면 그 값이 우선 (예: =0 으로 veto-off baseline A/B 재현). pipeline 로드(_load_pipeline → functions/pipeline/app.py 가 env 소비) 전에 설정돼야 하므로 module-level 주입 — RTMW_DETERMINISTIC 과 동일 패턴.
2) 모듈 docstring 의 "실행 (Pod, RTMW GPU + Gemini env 필요 ...)" 블록(line 41~50): export 나열을 비대화하지 말고, cold/warm 커맨드 아래에 1줄 명기 — veto env 2종(GEMINI_VISION_VETO_ENABLED=1 / GEMINI_MAX_VETO_WALL_S=300)은 run_sweep 이 스스로 setdefault 하므로 별도 export 불필요 (production start_server.sh mirror; 명시 export 시 그 값 우선).
3) 점수 리터럴/게이트 소스는 무접촉 — README locked 조항(점수 하드코딩 grep assert) 대상 파일(assert_gates.py, tests/)은 건드리지 않는다.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && GEMINI_VISION_VETO_ENABLED=0 PYTHONPATH=shared/python:. python3 -c "
import os, importlib.util
spec = importlib.util.spec_from_file_location('rs', 'evals/phase25/run_sweep.py')
m = importlib.util.module_from_spec(spec); spec.loader.exec_module(m)
assert os.environ['GEMINI_VISION_VETO_ENABLED'] == '0', 'setdefault 가 명시 export 를 덮음'
assert os.environ['GEMINI_MAX_VETO_WALL_S'] == '300', '미설정 env 기본값 미주입'
print('setdefault semantics OK')
"</automated>
  </verify>
  <done>깨끗한 env 에서 run_sweep 모듈 로드 시 GEMINI_VISION_VETO_ENABLED=1 / GEMINI_MAX_VETO_WALL_S=300 이 주입되고, 명시 export(=0)는 보존된다. docstring 실행 블록이 setdefault 사실을 명기한다. (run_sweep.py 는 `if __name__ == "__main__"` 가드(line 385)가 있어 import 는 module-level 코드만 실행 — 검증 안전 확인됨)</done>
</task>

<task type="auto">
  <name>Task 2: README Pod sweep 블록 갱신 + 게이트 테스트 회귀 확인</name>
  <files>backend/evals/phase25/README.md</files>
  <action>
1) README "### Pod sweep (RTMW GPU + Gemini env ...)" 섹션(line 81~94): bash 코드 블록은 그대로 두고(export 나열 비대화 방지), 블록 직후에 짧은 노트 문단 추가 — vision veto env 2종(`GEMINI_VISION_VETO_ENABLED=1`, `GEMINI_MAX_VETO_WALL_S=300`)은 run_sweep 이 module-level setdefault 로 자동 주입하므로 별도 export 불필요 (production `/workspace/start_server.sh` 박제 구성 mirror). 함정 1줄: 2026-07-05 신규 pod 에서 이 env 누락으로 veto 경로가 조용히 OFF 되어 sweep 1차가 통째로 무효(visionVeto disabled + deductionBreakdown 없음 + 레거시 min-of-core 점수)가 된 사고의 재발 방지. 명시 export 시 그 값 우선 ("RTMW 결정론 모드" 섹션의 setdefault 패턴과 동일).
2) README 의 locked 조항("특정 점수 assert 금지"), cold/warm 크레딧 섹션, Pod 운영 절차 섹션은 무접촉.
3) 회귀 확인: 기존 게이트 단위 테스트 GREEN 유지 (이 변경은 harness env 주입만 — 게이트/채점 로직 무접촉이므로 GREEN 이 기대값).
  </action>
  <verify>
    <automated>grep -q "GEMINI_VISION_VETO_ENABLED" /Users/kimtaesung/Dev/SunityMotion/backend/evals/phase25/README.md && grep -q "GEMINI_MAX_VETO_WALL_S" /Users/kimtaesung/Dev/SunityMotion/backend/evals/phase25/README.md && cd /Users/kimtaesung/Dev/SunityMotion/backend && PYTHONPATH=shared/python:. python3 -m pytest tests/test_phase25_eval_gates.py -q</automated>
  </verify>
  <done>README Pod sweep 섹션이 veto env 2종의 자동 setdefault 와 2026-07-05 함정을 명기하고, tests/test_phase25_eval_gates.py 가 GREEN 이다.</done>
</task>

</tasks>

<verification>
- `GEMINI_VISION_VETO_ENABLED=0` 명시 export 하에 run_sweep 모듈 import 시 0 이 보존되고, `GEMINI_MAX_VETO_WALL_S` 는 300 으로 주입된다 (setdefault semantics).
- env 전무 상태에서 run_sweep 모듈 import 시 두 env 가 각각 1 / 300 이다.
- `cd backend && PYTHONPATH=shared/python:. python3 -m pytest tests/test_phase25_eval_gates.py -q` GREEN.
- run_sweep.py docstring 실행 블록 + README Pod sweep 섹션 양쪽에 veto env 자동 주입이 명기돼 있다.
</verification>

<success_criteria>
- 새 pod 깨끗한 셸에서 README 대로만 실행해도 veto 경로가 ON — 2026-07-05 형태의 조용한 sweep 무효화가 구조적으로 불가능.
- 명시 export 우선(override 가능) — A/B 비교 경로 보존.
- 게이트/채점 로직·점수 리터럴 무접촉, 기존 테스트 회귀 0.
</success_criteria>

<output>
Create `.planning/quick/260705-d64-phase25-run-sweep-veto-env-setdefault-sw/260705-d64-SUMMARY.md` when done
</output>
