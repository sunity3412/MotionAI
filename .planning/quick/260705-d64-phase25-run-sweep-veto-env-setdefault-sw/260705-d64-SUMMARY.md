---
phase: quick-260705-d64
plan: 01
subsystem: eval-harness
tags: [phase25, sweep, gemini-vision-veto, env-setdefault, incident-fix]
requires: []
provides:
  - "run_sweep.py module-level veto env setdefault (GEMINI_VISION_VETO_ENABLED=1 / GEMINI_MAX_VETO_WALL_S=300)"
  - "README Pod sweep 블록 veto env 자동 주입 명기 + 2026-07-05 함정 설명"
affects: [backend/evals/phase25]
tech-stack:
  added: []
  patterns:
    - "os.environ.setdefault module-level env 주입 (RTMW_DETERMINISTIC 선례 승계)"
key-files:
  created: []
  modified:
    - backend/evals/phase25/run_sweep.py
    - backend/evals/phase25/README.md
key-decisions:
  - "veto env 는 setdefault (하드 덮어쓰기 아님) — 명시 export 시 그 값 우선, veto-off baseline A/B 재현 경로 보존"
  - "docstring/README 모두 export 나열 비대화 대신 자동 setdefault 사실을 1노트로 명기"
duration: 8m
completed: 2026-07-05
---

# Quick Task 260705-d64: phase25 run_sweep veto env setdefault Summary

phase25 run_sweep.py 가 Gemini vision veto env 2종을 module-level setdefault 로 스스로 주입해, 새 pod 깨끗한 셸에서 env 누락으로 veto 경로가 조용히 OFF 되던 2026-07-05 sweep 무효화 사고를 구조적으로 차단.

## What was done

### Task 1: run_sweep.py setdefault 블록 + docstring 갱신 (9e94530)

- `os.environ.setdefault("RTMW_DETERMINISTIC", "1")` 바로 아래에 동일 패턴으로 추가:
  - `os.environ.setdefault("GEMINI_VISION_VETO_ENABLED", "1")`
  - `os.environ.setdefault("GEMINI_MAX_VETO_WALL_S", "300")`
- 한국어 why 주석: 2026-07-05 신규 pod(svn31pzja7uay0) sweep 무효화 사고(visionVeto disabled → deductionBreakdown 없음 + 레거시 min-of-core 점수), production `/workspace/start_server.sh` 박제 구성(2026-07-02 FP 사고 fix) mirror, setdefault 라 명시 export 우선, pipeline 로드 전 module-level 주입 필요성 명시.
- 모듈 docstring 실행 블록에 1노트 추가 — veto env 2종은 run_sweep 이 스스로 setdefault 하므로 별도 export 불필요.

### Task 2: README Pod sweep 블록 갱신 (39df2f6)

- Pod sweep bash 블록 직후에 노트 문단 추가 — veto env 2종 자동 setdefault, production start_server.sh mirror, 2026-07-05 함정 1단락, 명시 export 우선("RTMW 결정론 모드" 섹션 패턴과 동일 참조).
- locked 조항("특정 점수 assert 금지"), cold/warm 크레딧 섹션, Pod 운영 절차 섹션 무접촉.

## Verification

- `GEMINI_VISION_VETO_ENABLED=0` 명시 export 하 모듈 import: 0 보존 + `GEMINI_MAX_VETO_WALL_S=300` 주입 — "setdefault semantics OK"
- env 전무(env -u 양쪽) 상태 모듈 import: 1 / 300 주입 — "clean-env defaults OK"
- README grep 2종 존재 확인 OK
- `PYTHONPATH=shared/python:. python3 -m pytest tests/test_phase25_eval_gates.py -q` — 25 passed (회귀 0)
- 게이트/채점 로직·점수 리터럴 무접촉 (assert_gates.py / tests/ diff 0)

## Deviations from Plan

None - plan executed exactly as written.

## Commits

| Task | Commit | Files |
|------|--------|-------|
| 1 | 9e94530 | backend/evals/phase25/run_sweep.py |
| 2 | 39df2f6 | backend/evals/phase25/README.md |

## Known Stubs

None.

## Self-Check: PASSED

- run_sweep.py / README.md / SUMMARY.md 존재 확인
- 커밋 9e94530 / 39df2f6 존재 확인
- 파일 삭제 0, setdefault 2줄 존재 확인
