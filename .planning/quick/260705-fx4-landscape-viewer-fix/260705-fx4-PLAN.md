---
phase: quick-260705-fx4-landscape-viewer-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/VideoCompare.tsx
autonomous: true
requirements: ["belle-device-feedback-2nd-2026-07-05"]

must_haves:
  truths:
    - "가로 전체화면에서 각 영상이 화면 짧은 변(세로축)을 100% 채운다 (68% 축소 렌더 소멸)"
    - "두 영상 박스가 화면 중앙에 인접 배치된다 (박스 사이 ~200pt 검은 간격 소멸)"
    - "스켈레톤 오버레이 마커가 영상 속 몸 위에 정확히 겹친다 (박스 밖 유령 마커 소멸)"
    - "슬롯 라벨이 각 영상 박스 내부 상단에 표시된다"
    - "단일 영상(hasLeft xor hasRight)이면 박스 1개가 화면 중앙에 표시된다"
    - "재생/일시정지/스텝/스크럽/닫기 등 기존 컨트롤이 그대로 동작한다"
  artifacts:
    - path: "app/src/components/VideoCompare.tsx"
      provides: "명시 숫자 치수 기반 가로 전체화면 뷰어 레이아웃"
      contains: "fsShort * VIDEO_ASPECT"
  key_links:
    - from: "VideoCompare render (fsBoxW/fsBoxH 계산)"
      to: "renderFullscreenSlot 박스 View style"
      via: "숫자 width/height 인라인 주입"
      pattern: "width: fsBoxW"
    - from: "fsVideoBox (명시 치수 박스)"
      to: "KeypointOverlay (viewBox 0 0 1 1)"
      via: "overlayContainer absoluteFill — 박스 치수가 정확하면 SVG 정합 자동 회복"
      pattern: "overlayContainer"
---

<objective>
가로 전체화면 뷰어(quick-260702-t0v 신설)의 실기기 레이아웃 붕괴 3종 수정 — belle 실기기 2차 피드백 (2026-07-05, TestFlight #27 + OTA): "가로 버전 수정 안 됐어. 영상이 꽉 차야 해."

증상 (belle 캡처 실측):
1. 두 영상이 기대 크기(fsShort 높이 100%)의 ~68%로만 렌더
2. 두 영상 사이 큰 검은 간격 (반쪽 426pt 슬롯 중앙에 221pt 박스 → 사이 ~200pt)
3. 스켈레톤 오버레이가 영상 박스 밖 위쪽 검은 영역에 유령처럼 렌더

근본 원인: fsVideoBox 의 `height: '100%'` + `maxWidth: '100%'` + `aspectRatio` 조합이
90° 회전(transform) absolute 컨테이너 + flex 반쪽 슬롯 안에서 의도대로 해석되지 않음
(퍼센트+aspectRatio 레이아웃 모호성). 이전 시도(t0v follow-up, 퍼센트 기반)가 실기기에서
실패 확인됨 → 이번엔 퍼센트/aspectRatio 를 버리고 **JS 명시 숫자 치수**로 전환.

Purpose: belle 시연 품질 — 가로 뷰어에서 영상 최대화 + 오버레이 정합은 각도 라벨 가독
기능(t0v 원 목적)의 전제 조건.
Output: app/src/components/VideoCompare.tsx 단일 파일 수정 (JS-only, OTA 배포 가능).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@app/src/components/VideoCompare.tsx
@app/src/components/KeypointOverlay.tsx (읽기 전용 참고 — 수정 금지. SVG 는 absoluteFill + viewBox "0 0 1 1" + preserveAspectRatio "none" 이라 박스 치수만 정확하면 정합 자동 회복)
@app/CLAUDE.md (테마 토큰 강제, 하드코딩 금지)
</context>

<interface_contract>
현재 구조 (수정 대상 좌표):
- `VIDEO_ASPECT = 9 / 16` (line ~160) — 유지, 치수 계산의 단일 출처
- `fsShort = Math.min(winW, winH)` / `fsLong = Math.max(winW, winH)` (line ~230-231) — 유지
- `renderFullscreenSlot(label, url, player, overlay)` (line ~590) — 내부 구조 변경
- Modal 내부: `fsRotated`(회전 컨테이너, render 에서 width/height/left/top 숫자 주입 — 검증됨, 유지) > `fsVideoRow`(absoluteFill row) > 슬롯 2개 + `fsTopBar`/`fsControlsWrap` overlay — 유지
- styles: `fsVideoRow`(absoluteFill+row), `fsSlot`(flex:1 반쪽 중앙 — **폐기 대상**), `fsVideoBox`(height '100%'+maxWidth '100%'+aspectRatio — **퍼센트 제거 대상**), `fsSlotLabel`(슬롯 기준 absolute — 박스 기준으로 이동)
- `overlayContainer` (absoluteFill) — 무수정, 새 박스 안에서 그대로 사용
- player 재사용(두 번째 VideoView attach)/컨트롤 공유(renderControls)/조건부 Modal 렌더 — t0v 검증분, 구조 무변경
</interface_contract>

<tasks>

<task type="auto">
  <name>Task 1: 가로 전체화면 뷰어를 명시 숫자 치수 레이아웃으로 전환</name>
  <files>app/src/components/VideoCompare.tsx</files>
  <action>
belle 실기기 2차 (2026-07-05) 피드백 반영. 퍼센트/aspectRatio 기반 박스 치수를 JS 계산
숫자로 교체하고, flex 반쪽 슬롯 구조를 중앙 인접 배치로 바꾼다. 변경은 전체화면 경로만 —
세로 카드(VideoSlot/slotFrame), player lifecycle, 컨트롤 공유, 조건부 Modal 렌더는 무수정.

1. **명시 치수 계산** (컴포넌트 본문, fsShort/fsLong 파생 직후):
   - `const fsBoxH = fsShort;` (세로영상이 화면 짧은 변을 100% 채움)
   - `const fsBoxW = Math.round(fsShort * VIDEO_ASPECT);` (9:16 고정 비율)
   - 한국어 why 주석: 퍼센트+aspectRatio 가 회전 absolute 컨테이너 안에서 ~68% 축소
     렌더된 실기기 증상(belle 실기기 2차 2026-07-05) → window 파생 숫자 치수로 전환.
     숫자 치수는 window 파생값이라 하드코딩 아님.

2. **renderFullscreenSlot 재구성** — 슬롯 = 박스 자체:
   - 바깥 View: `style={[styles.fsVideoBox, { width: fsBoxW, height: fsBoxH }]}`
     (fsSlot 래퍼 폐기 — flex:1 반쪽-중앙 구조가 간격의 원인)
   - 내부: 기존 VideoView(contentFit="contain" 유지 — 9:16 아닌 소스 letterbox 안전망)
     + overlayContainer(absoluteFill, sizeScale=FULLSCREEN_OVERLAY_SCALE 호출) 그대로
     + 빈 URL 케이스 slotEmpty 그대로
   - `fsSlotLabel` 을 박스 **내부** 마지막 child 로 이동 — 박스 기준 absolute
     (top 12, left 0/right 0 + textAlign center 또는 alignSelf center), pointerEvents
     "none" 유지. 라벨이 각 영상 상단 중앙에 부착됨.

3. **인접 배치** — `fsVideoRow` 스타일 수정:
   - absoluteFill + flexDirection 'row' 유지에 `justifyContent: 'center'`,
     `alignItems: 'center'`, `gap: 8` (기존 세로 카드 row 의 gap 8 과 동일 소량 간격) 추가
   - 결과 기하 (iPhone 393×852 예시): 박스 221×393 두 개 + gap 8 = 450pt ≤ fsLong 852
     — 중앙 인접 배치, 세로 꽉 참
   - 단일 영상(hasLeft xor hasRight): Modal 안 renderFullscreenSlot 호출을 url 존재
     여부로 조건부 렌더 (`{hasLeft && renderFullscreenSlot(...)}` /
     `{hasRight && renderFullscreenSlot(...)}`) → justifyContent center 가 1박스를
     자동 중앙 배치. 진입 버튼이 hasAny 가드라 0박스 케이스 없음.

4. **styles 정리**:
   - `fsVideoBox`: `height: '100%'` / `maxWidth: '100%'` / `aspectRatio` 전부 제거 —
     배경색(colors.videoFullscreenBg) 등 잔여 시각 속성만 유지하거나 빈 껍데기 유지
     (width/height 는 render 인라인 주입). 필요 시 `position: 'relative'` 명시 불필요
     (RN 기본값).
   - `fsSlot` 스타일 삭제 (사용처 소멸).
   - `fsSlotLabel`: `alignSelf: 'center'` 대신 박스 내부 absolute 로 좌표 재정의
     (left: 0, right: 0, top: 12, textAlign 'center' 유지). FULLSCREEN_TEXT_SCALE
     파생 fontSize 유지.
   - `fsTopBar` 주석 갱신: "9:16 박스는 반쪽 중앙 정렬이라 우측 끝과 겹치지 않음"
     rationale 이 더 이상 정확하지 않음 — 인접 중앙 배치 기준으로 문구 수정
     (기하상 393 기기에서 박스 우측 끝 x≈639 < 닫기 버튼 영역이므로 여전히 비겹침이나,
     근거 서술은 새 레이아웃 기준으로).

5. **금지 사항**: KeypointOverlay.tsx 수정 금지. expo-screen-orientation 등 native
   모듈 추가 금지 (OTA 전제). 색/치수 하드코딩 금지 (테마 토큰 + window 파생 숫자만).
   이모지 금지. 기존 t0v 검증분(player 재사용/renderControls 공유/fullscreenRef track
   폭 분리/조건부 Modal) 구조 변경 금지.

6. 파일 상단 또는 해당 블록 주석에 근거 인용: "belle 실기기 2차 2026-07-05".
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck</automated>
    <automated>grep -n "fsShort \* VIDEO_ASPECT" /Users/kimtaesung/Dev/SunityMotion/app/src/components/VideoCompare.tsx && ! grep -n "maxWidth: '100%'" /Users/kimtaesung/Dev/SunityMotion/app/src/components/VideoCompare.tsx</automated>
    <human-check>실기기(TestFlight OTA) 검증 — SUMMARY 에 아래 체크리스트 박제 후 belle 확인: (1) 영상이 세로(짧은 변)를 꽉 채움 (2) 두 영상 인접 배치·큰 검은 간격 없음 (3) 오버레이 마커가 몸 위에 정확히 겹침 (4) 라벨이 각 영상 상단에 표시 (5) 재생/스텝/스크럽/타임라인 동작 (6) 닫기 후 세로 카드 정상 복귀</human-check>
  </verify>
  <done>
typecheck GREEN. fsVideoBox 에 퍼센트/aspectRatio 치수 부재 + render 에서 fsBoxW/fsBoxH
숫자 주입. fsVideoRow 가 justifyContent center + gap 으로 두 박스 인접 배치. 라벨이 박스
내부 absolute. 단일 영상 시 1박스 중앙. KeypointOverlay 무수정. 세로 카드 렌더 diff 0.
SUMMARY 에 실기기 검증 체크리스트 6항목 기록.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 없음 | UI 레이아웃 전용 변경 — 신규 입력/네트워크/저장 경로 없음. 영상 URL/keypointReport 는 기존 신뢰 경로(presigned S3 + Firestore) 그대로 소비 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-fx4-01 | DoS | useWindowDimensions 비정상값(0) | accept | fsShort=Math.min 파생 + expo-video contain — 0 치수는 빈 렌더로 수렴, 크래시 경로 없음. 신규 패키지 설치 0 (공급망 게이트 해당 없음) |
</threat_model>

<verification>
- `cd app && npm run typecheck` GREEN (프로젝트 유일 정적 게이트 — 테스트 러너 없음)
- 코드 리뷰 수준 기하 검증: fsBoxH=fsShort, fsBoxW=round(fsShort*9/16), 두 박스+gap ≤ fsLong (iPhone 기준 450 ≤ 852)
- 세로 카드 경로(VideoSlot/slotFrame/row) diff 0 — 무회귀
- 실기기 검증 항목은 SUMMARY 체크리스트로 belle 에게 이관 (OTA 배포 후)
</verification>

<success_criteria>
- 가로 전체화면에서 영상이 화면 짧은 변을 100% 채우고, 두 영상이 중앙 인접 배치되며, 오버레이 마커가 몸 위에 정합
- 단일 영상 케이스 중앙 1박스 동작
- JS-only 변경 (OTA 가능), 테마 토큰/window 파생 치수만 사용
- typecheck GREEN + 기존 컨트롤/플레이어 구조 무변경
</success_criteria>

<output>
Create `.planning/quick/260705-fx4-landscape-viewer-fix/260705-fx4-SUMMARY.md` when done
(실기기 검증 체크리스트 6항목 포함 필수)
</output>
