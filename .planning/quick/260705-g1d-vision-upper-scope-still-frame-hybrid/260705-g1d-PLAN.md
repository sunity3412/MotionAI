---
phase: quick-260705-g1d-vision-upper-scope-still-frame-hybrid
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py
  - backend/functions/pipeline/app.py
  - backend/tests/test_gemini_vision_scorer.py
autonomous: true
requirements: ["phase25-residual-kipup-upper-vision"]

must_haves:
  truths:
    - "vision fanout 의 upper_body scope 호출만 학생/기준 worst-pose 정지프레임 이미지 페어를 입력으로 받고, lower_body/line 호출은 full-video 핸들을 그대로 쓴다"
    - "정지프레임 추출 또는 이미지 업로드 실패 시 upper_body scope 만 full-video 로 폴백하고 분석 흐름은 중단되지 않는다 (Pitfall 5)"
    - "하이브리드 캐시 키는 기존 whole_fanout 키 공간과 절대 충돌하지 않는다 — 옛 엔트리 stale-hit 0 (90d038f 재발 금지)"
    - "telemetry 에 upperGranularity('still_frame'|'video_fallback') 가 기록돼 eval 이 입력 형태를 관측할 수 있다"
    - "cold→warm 캐시 round-trip 이 동일 rich dict 를 반환한다 (결정론, 프레임 선택은 고정 시각·고정 인덱스)"
    - "supported_differences 이후 채점/집계/라우터 경로는 diff 0 (입력 granularity 만 변경, 새 튜닝 상수 0)"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py"
      provides: "하이브리드 granularity fanout — 정지프레임 추출 helper + upper scope 이미지 분기 + 캐시 키 bump"
      contains: "whole_fanout_hybrid"
    - path: "backend/functions/pipeline/app.py"
      provides: "_collect_vision_fault_context 가 worst_pose_timestamp 를 still_at_seconds 로 전달"
      contains: "still_at_seconds"
    - path: "backend/tests/test_gemini_vision_scorer.py"
      provides: "순수 인덱스 helper + upper 분기 + graceful 폴백 + 캐시 결정론 유닛 테스트"
  key_links:
    - from: "backend/functions/pipeline/app.py _collect_vision_fault_context"
      to: "assess_fault_context_video"
      via: "still_at_seconds=at (vision_veto.worst_pose_timestamp, line ~1847 기존 변수)"
      pattern: "still_at_seconds="
    - from: "assess_fault_context_video"
      to: "_run_part_frame_fanout"
      via: "upper_still_handles=(ref_img, student_img) | None"
      pattern: "upper_still_handles"
    - from: "_run_part_frame_fanout upper_body 분기"
      to: "_call_gemini_comparison"
      via: "media_kind='image' — contents 라벨을 이미지 정합으로 분기"
      pattern: "media_kind"
    - from: "assess_fault_context_video"
      to: "VisionVetoCache.build_key"
      via: "frame_indices=[student_idx, ref_idx] folding + INPUT_GRANULARITY_WHOLE_FANOUT 상수 bump"
      pattern: "frame_indices="
---

<objective>
vision fanout 하이브리드 granularity 배선 — upper_body scope 호출만 worst-pose 정지프레임 이미지 페어로 전환하고 lower_body/line 은 full-video 를 유지한다.

Purpose: Phase 25 잔여 — kip-up 상체(어깨 40° 기하 편차)가 vision 에 짚이지 않아 감점 0 → 앱에서 "참고·감점 아님" 강등 (belle 실기기: "딱봐도 힘들게 매달리는데 왜 감점 아님?"). 프롬프트 레버는 소진됐다: v11.1(집중 문구)은 upper scope 6회 중 상체 0회·하체 누수 4회, v11.2(배타 강제)는 누수 0 이 됐지만 상체 방출도 0/6 (2026-07-05 pod 진단 2회). 박제된 스파이크 실증(2026-06-22, spike-stillframe-recovers-upperbody): 전체영상 비교 = 다리만(상체 0) / 정지프레임 비교 = 팔 복구 — 레버는 granularity, NOT 프롬프트. 반대로 still-frame 단독은 동적 다리 결함(kip-up split 순간)을 놓쳐 위양성 이력(2026-06-29) → scope 별 하이브리드가 결론.

Output: gemini_vision_scorer.py 하이브리드 fanout + pipeline 배선 + 유닛 테스트. 실효 검증(상체 방출)은 pod 격리 진단 + sweep — SUMMARY 에 PENDING 박제.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py
@backend/functions/pipeline/app.py
@backend/tests/test_gemini_vision_scorer.py
@backend/shared/python/sunity_shared/analysis/fault_zoom.py
</context>

<interface_notes>
현재 계약 (executor 가 탐색 없이 쓸 수 있게 박제):

- `assess_fault_context_video(student_video_path, reference_video_path, *, at_seconds=None, part_scopes=None) -> dict` (line ~1130) — full-video 페어를 `_upload_video` 2회로 올리고 `_run_part_frame_fanout` 호출. 캐시 키 = `build_key(video_hash, model, input_granularity=INPUT_GRANULARITY_WHOLE_FANOUT, at_seconds, reference_hash)`. 반환 dict = {status, verdict, supported_differences, root_cause_hypotheses, telemetry}. skipped_error 는 캐시 미저장, 정상/resource_limited 는 store_rich.
- `_run_part_frame_fanout(client, ref_uploaded, student_uploaded, *, part_scopes, at_seconds, clock=None, wall_budget_s, max_calls) -> dict` (line ~1496) — scope 당 `_call_gemini_comparison(client, ref_uploaded, student_uploaded, at_seconds, part_scope=scope)` 1회. telemetry = {completedCalls, plannedCalls, uploadCount: 2 하드코딩, durationMs, samplingComplete}. 미완료 → fail-closed resource_limited.
- `_call_gemini_comparison(client, ref_uploaded, student_uploaded, at_seconds, part_scope=None) -> str` (line ~1630) — contents = ["기준(정타) 영상:", ref, "평가 대상(학생) 영상:", student, prompt]. part_scope 있으면 v11.2 배타 블록 suffix.
- `_upload_image(client, local_image_path)` (line ~829) — 존재 가드 + ACTIVE-wait, PNG/JPEG mime 은 `_mime` 이 이미 분기 (line ~766). 그대로 재사용, 수정 불필요.
- `VisionVetoCache.build_key(*, video_hash, model_name, input_granularity, at_seconds, reference_hash, selector_version=None, frame_indices=None, top_k=None, window=None)` (line ~479) — `frame_indices` 는 'fi' + '_'.join(int) 로 직렬화, None → 'fi-'. PROMPT/SCHEMA/AGGREGATION_VERSION 은 globals 경유로 키에 folding.
- pipeline 호출부 (backend/functions/pipeline/app.py line ~1904): `assess_fault_context_video(local_video_path, reference_video_path, at_seconds=None, part_scopes=list(VETO_PART_SCOPES))`. `at = vision_veto.worst_pose_timestamp(profile)` 는 line ~1847 에서 이미 계산돼 있음 (fault_zoom/frame-pair 용). at_seconds=None 은 Phase 24 close-out A 실측 결정 — video 호출에는 유지.
- 기준 프레임 시간비례 근사 선례: fault_zoom.py line ~512 `ratio = u_idx / max(1, u_n - 1); r_idx = round(ratio * (r_n - 1))` — DTW 는 이 레이어에 없음, 동일 접근.
- 테스트 fake 패턴: test_gemini_vision_scorer.py `_FakeClient/_FakeModels/_FakeFiles`, `_in_memory_cache(monkeypatch)`, `_patch_client_with_upload_spy`, `_run_part_frame_fanout` 직접 호출 + `_call_gemini_comparison` monkeypatch spy (line ~870, 시그니처 `(client, ref_uploaded, student_uploaded, at_seconds, part_scope=None)` — kw 추가 시 spy 들 시그니처 갱신 필요).
- test_pipeline_vision_gate.py line ~725 는 `assess_fault_context_video` 전체를 `_fake_video_fanout` 으로 monkeypatch — 새 kwarg 전달로 TypeError 나지 않게 fake 시그니처에 `**kwargs` 수용 확인/추가.
</interface_notes>

<tasks>

<task type="auto">
  <name>Task 1: gemini_vision_scorer 하이브리드 granularity — 정지프레임 helper + upper scope 분기 + 캐시 키 bump</name>
  <files>backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py</files>
  <action>
gemini_vision_scorer.py 에 하이브리드 입력 배선. 채점/집계(_filter_supported_differences 이후)/라우터 로직 무접촉 — 입력 granularity 와 telemetry 만 변경. 새 튜닝 상수 금지, severity/지지 규칙 불변. 이모지 금지, 한국어 why 주석에 근거 2건 인용: 2026-06-22 스파이크([[spike-stillframe-recovers-upperbody]] — 전체영상=상체 0/정지프레임=팔 복구, 레버=granularity) + 2026-07-05 pod 진단 2회(v11.1 상체 0·하체 누수 4, v11.2 누수 0·상체 0/6 — 프롬프트 레버 소진).

(1) 캐시 상수 bump: `INPUT_GRANULARITY_WHOLE_FANOUT = "whole_fanout_hybrid1"` 로 상수 값 자체를 갱신 (상수명 유지 — 참조부 무변경). 주석에 why: 하이브리드는 upper scope 산출 분포를 바꾸므로 옛 whole_fanout 키 공간 재사용 = stale-hit(90d038f kip-up FP 이력) 재발. PROMPT_VERSION/SCHEMA_VERSION/AGGREGATION_VERSION 은 bump 하지 않는다 — 공용 프롬프트 텍스트·스키마·집계 무변경이고, 이미지-모드 라벨/문구는 새 granularity 키 공간에서만 발생하므로 기존 키(frame_pair/whole 포함)의 결과와 절대 섞이지 않는다 (이 근거도 주석으로).

(2) 결정적 순수 helper 2개 (IO 없음, 유닛 테스트 대상):
  - `_still_frame_index(at_seconds, fps, n_frames) -> int`: at_seconds None → n_frames // 2 (영상 중앙), else round(at_seconds * fps) 를 [0, n_frames-1] 로 clamp. fault_zoom 의 _frame_index 와 같은 계약이지만 표시 모듈에 역의존하지 않게 어댑터 자체 helper 로 둔다 (docstring 에 명시).
  - `_ratio_ref_index(student_idx, student_n, ref_n) -> int`: fault_zoom line ~512 시간비례 근사와 동일 공식 (ratio = student_idx / max(1, student_n - 1); round(ratio * (ref_n - 1)), 경계 clamp). DTW 는 이 레이어에 없음 — 구조 변경 최소화 (docstring 인용).

(3) IO helper `_extract_comparison_stills(student_video_path, reference_video_path, at_seconds) -> dict`: imageio(기존 의존성) 로 두 영상에서 각 1프레임 → tempfile 임시 PNG 2장. 반환 {"student_png", "reference_png", "student_idx", "ref_idx"}. 학생 인덱스 = _still_frame_index(at_seconds, 학생 fps, 학생 n_frames); 기준 인덱스 = _ratio_ref_index. 프레임 수는 reader.count_frames() 우선, 실패/inf 시 메타데이터 폴백, 그것도 불가하면 예외 raise (호출자가 폴백 처리). 고정 시각·고정 인덱스 = 결정적 (D-06). 실패는 예외로 전파 — swallow 하지 않는다 (폴백 판단은 호출자 소관).

(4) `assess_fault_context_video` 에 kw 추가 `still_at_seconds: float | None = None` (기존 at_seconds 의미 불변 — full-video 호출용, Phase 24 close-out A):
  - hash 산출 직후 try 로 `_extract_comparison_stills(student, reference, still_at_seconds)` — 실패 시 stills=None + log.warning (graceful, 분석 중단 금지).
  - build_key 에 `frame_indices=[student_idx, ref_idx]` folding (stills 성공 시). 추출 실패 시 frame_indices=None → 'fi-' bucket — 폴백(전-scope video) 결과가 still-키 공간을 오염하지 않게 키가 구조적으로 분리된다 (주석에 why).
  - cache miss 경로: 기존 _upload_video 2회 + stills 있으면 _upload_image 2회 (학생/기준 PNG). 이미지 업로드는 별도 try 로 감싸 실패 시 stills 폐기(=video 폴백) + log.warning — 전체 skipped_error 로 떨어뜨리지 않는다 (요구: 해당 scope 만 폴백). MAX_VETO_UPLOADS=4 상한과 정합(2 video + 2 image = 4).
  - `_run_part_frame_fanout(..., upper_still_handles=(ref_img_handle, student_img_handle) 또는 None)` 전달.
  - finally: 업로드 핸들 4개 모두 files.delete (기존 루프에 이미지 핸들 추가) + 임시 PNG 2장 unlink (실패 graceful — _safe 스타일).
  - telemetry: fanout 결과 telemetry 를 그대로 실되, tel["inputGranularity"] 는 갱신된 상수 반영 (기존 라인 그대로면 자동).

(5) `_run_part_frame_fanout` 에 kw 추가 `upper_still_handles: tuple | None = None`: scope == "upper_body" 이고 handles 존재 → `_call_gemini_comparison(client, upper_still_handles[0], upper_still_handles[1], at_seconds, part_scope=scope, media_kind="image")`, 그 외 scope 는 기존 video 핸들 + media_kind 기본값. telemetry 에 `upperGranularity`: handles 사용 시 "still_frame", scopes 에 upper_body 가 포함됐는데 handles 가 None 이면 "video_fallback" (upper 미포함 scopes 면 미기록). uploadCount 는 handles 존재 시 4, 아니면 기존 2. fail-closed resource_limited/support 게이트/median severity 로직 무변경.

(6) `_call_gemini_comparison` 에 kw 추가 `media_kind: str = "video"`: media_kind == "image" 일 때만 contents 라벨을 "기준(정타) 정지 이미지:" / "평가 대상(학생) 정지 이미지:" 로 분기하고, 프롬프트 끝에 이미지-모드 정합 문구 1개를 덧붙인다 (이 입력은 각 영상에서 결함-의심 순간을 고른 정지 이미지 페어이며, 영상 구간 탐색 지시는 무시하고 이 자세끼리 비교하라는 취지 — _build_comparison_prompt 본문을 읽고 "영상" 전제 문구와 충돌하는 지점을 정합하게 커버할 것). default "video" 경로는 프롬프트/라벨 byte-동일 유지 — 기존 frame_pair 경로(assess_fault_context 는 이미지 핸들에 "영상" 라벨을 붙이는 기존 mismatch 가 있으나 이번 스코프 밖, 캐시 무효화 0 을 위해 무접촉). v11.2 배타 블록(part_scope suffix)은 그대로 적용된다.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && python -c "from shared.python.sunity_shared.analysis import gemini_vision_scorer as g; assert g.INPUT_GRANULARITY_WHOLE_FANOUT != 'whole_fanout'; assert g._still_frame_index(None, 9.0, 10) == 5; assert g._still_frame_index(100.0, 9.0, 10) == 9; assert g._ratio_ref_index(0, 10, 7) == 0" && python -m pytest tests/test_gemini_vision_scorer.py -x -q</automated>
  </verify>
  <done>INPUT_GRANULARITY_WHOLE_FANOUT 값 bump, 순수 helper 2개 + IO helper 1개 존재, upper_body scope 만 이미지 핸들 경유(media_kind="image"), 추출/업로드 실패 시 video 폴백으로 분석 계속, 기존 테스트 전부 GREEN (spy 시그니처 갱신 포함).</done>
</task>

<task type="auto">
  <name>Task 2: pipeline 배선 + 하이브리드 유닛 테스트</name>
  <files>backend/functions/pipeline/app.py, backend/tests/test_gemini_vision_scorer.py</files>
  <action>
(1) pipeline 배선: `_collect_vision_fault_context` 의 assess_fault_context_video 호출(line ~1904)에 `still_at_seconds=at` 추가 — `at = vision_veto.worst_pose_timestamp(profile)` 는 line ~1847 에서 이미 계산된 기존 변수 재사용 (None 이면 scorer 가 영상 중앙 폴백). `at_seconds=None` 은 유지하고, 기존 Phase 24 close-out A 주석에 1~2줄 보강: worst-pose hint 는 full-video 호출이 아니라 upper_body 정지프레임 **추출 시각**으로만 쓰인다 (2026-06-22 스파이크 근거 인용). runpod_inference/server.py 는 pipeline 모듈 _process 를 재사용하므로 별도 변경 없음 — 확인만.

(2) test_pipeline_vision_gate.py line ~725 의 `_fake_video_fanout` monkeypatch 시그니처가 새 kwarg(still_at_seconds)를 수용하는지 확인, 필요 시 `**kwargs` 추가 (TypeError 방지).

(3) test_gemini_vision_scorer.py 에 하이브리드 테스트 추가 — 기존 _FakeClient/_in_memory_cache/spy 패턴 재사용, 실 Gemini/실 영상 디코드 없음 (의미있는 테스트만, 수치 채우기 금지):
  - 순수 helper: _still_frame_index (None → 중앙, 초과 시각 → 마지막 인덱스 clamp, 0/1프레임 경계), _ratio_ref_index (0 → 0, 끝 → ref 끝, 중간 비례).
  - upper 분기 라우팅: `_extract_comparison_stills` 를 monkeypatch(가짜 PNG 경로 + 고정 인덱스), `_upload_video`/`_upload_image` spy, `_call_gemini_comparison` spy 로 (핸들, part_scope, media_kind) 캡처 → upper_body 호출만 이미지 핸들 + media_kind=="image", lower_body/line 은 video 핸들 + 기본 media_kind. telemetry upperGranularity=="still_frame", uploadCount==4.
  - graceful 폴백: `_extract_comparison_stills` 가 raise → 3 scope 전부 video 핸들, status 는 정상 산출(skipped_error 아님), telemetry upperGranularity=="video_fallback", 캐시 키에 'fi-' (frame_indices 미포함).
  - 캐시 결정론: cold 호출 → warm 호출 동일 rich dict + 2회째 Gemini 호출 0 (기존 test_determinism 패턴). 키 문자열에 "whole_fanout_hybrid" 포함 + still 성공 시 frame_indices 직렬화("fi{s}_{r}") 포함 → 옛 "whole_fanout" 키와 불일치 확인.
  - media_kind 라벨: _FakeModels 가 캡처한 contents 로 image 모드 라벨에 "정지 이미지" 포함·"영상:" 라벨 부재, video 모드는 기존 라벨 byte-동일.

(4) 전체 스위트: backend 테스트 전부 GREEN. SUMMARY 에 박제: 실효 검증(kip-up fault 페어에서 상체 방출 발생 여부)은 pod 격리 진단 + sweep PENDING — 유닛 테스트는 배선 정확성만 증명한다.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && python -m pytest tests/ -x -q</automated>
  </verify>
  <done>pipeline 이 still_at_seconds=at 전달, 신규 테스트(순수 helper/upper 분기/폴백/캐시 결정론/라벨) 포함 backend 전체 스위트 GREEN, SUMMARY 에 pod 실효 검증 PENDING 박제.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline → Gemini Files API | 기존 경계 그대로 — 업로드 대상이 영상 2 + 정지 이미지 2 로 늘 뿐, 새 신뢰 경계/새 패키지 설치 0 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-g1d-01 | Information Disclosure | Gemini File API 업로드 정지 이미지 | mitigate | 기존 finally files.delete 디시플린을 이미지 핸들 2개에도 확장 + 임시 PNG unlink (48h TTL 누수/429 방지, 기존 패턴 재사용) |
| T-g1d-02 | Tampering (cache poisoning) | VisionVetoCache whole_fanout 키 공간 | mitigate | INPUT_GRANULARITY_WHOLE_FANOUT 값 bump + frame_indices folding — 옛 분포 결과 stale-hit 구조적 차단 (90d038f 이력) |
</threat_model>

<verification>
- backend 전체 pytest GREEN (`python -m pytest tests/ -x -q`).
- upper_body scope 만 이미지 입력, lower/line full-video — spy 테스트로 증명.
- 캐시 키가 옛 "whole_fanout" 문자열과 불일치 + cold/warm round-trip 동일.
- 채점 경로 diff 0: _filter_supported_differences / deduction 라우팅 / severity 규칙 파일 변경 없음 (git diff 로 확인 — 변경 파일은 frontmatter files_modified 3개뿐).
</verification>

<success_criteria>
- 하이브리드 granularity 배선 완료 + 유닛 테스트 GREEN + 기존 계약(fail-closed, support 게이트, graceful) 회귀 0.
- 실효(kip-up 상체 방출)는 pod 진단/sweep 으로 후속 확인 — SUMMARY 에 PENDING 명시.
</success_criteria>

<output>
Create `.planning/quick/260705-g1d-vision-upper-scope-still-frame-hybrid/260705-g1d-SUMMARY.md` when done
</output>
