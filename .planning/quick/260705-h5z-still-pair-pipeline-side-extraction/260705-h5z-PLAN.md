---
phase: quick-260705-h5z
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py
  - backend/shared/python/sunity_shared/analysis/vision_veto.py
  - backend/functions/pipeline/app.py
  - backend/tests/test_gemini_vision_scorer.py
  - backend/tests/test_pipeline_vision_gate.py
autonomous: true
requirements: [RC-01, RC-02, RC-03, RC-04]

must_haves:
  truths:
    - "pipeline 이 자기 9fps 프레임 배열의 window/DTW-매칭 인덱스 페어로 만든 still PNG 2장을 scorer 에 경로+인덱스로 전달한다 (RC-01)"
    - "scorer 는 경로 제공 시 자체 추출(raw imageio + 시간비례 근사)을 하지 않고 제공된 PNG 를 업로드한다 (RC-02)"
    - "ref DTW 매칭 실패(시간비례 폴백) 시 still 은 scorer 에 전달되지 않고 upper scope 는 video-only 로 동작한다 (RC-01/RC-04)"
    - "upper_body scope 는 still 핸들 존재 시 동일 핸들 재사용 2-call 이 되어, 각도쌍 없는 상체 관측(그립/어깨)이 distinct-call K=2 를 충족할 수 있다 (RC-03)"
    - "캐시 키는 제공 인덱스 페어로 folding 되고 granularity 마커 bump 로 g1d(raw imageio) 엔트리와 구조적으로 분리된다 (stale-hit 금지)"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py"
      provides: "still 경로 주입 계약 + upper 2-call fanout (자체 추출 코드 제거)"
      contains: "still_student_png"
    - path: "backend/functions/pipeline/app.py"
      provides: "_collect_vision_fault_context 의 still 경로/인덱스 배선"
      contains: "still_student_png"
    - path: "backend/shared/python/sunity_shared/analysis/vision_veto.py"
      provides: "SelectedFramePair.ref_match_source (dtw|ratio) 판별 필드"
      contains: "ref_match_source"
  key_links:
    - from: "backend/functions/pipeline/app.py"
      to: "gemini_vision_scorer.assess_fault_context_video"
      via: "still_student_png/still_reference_png/still_frame_indices kwargs"
      pattern: "still_frame_indices"
    - from: "backend/functions/pipeline/app.py::_build_selected_frame_pair"
      to: "fault_zoom._matched_ref_frame"
      via: "DTW-매칭 ref 인덱스 (기존 코드 재사용, 재계산 0)"
      pattern: "_matched_ref_frame"
    - from: "gemini_vision_scorer._run_part_frame_fanout"
      to: "_filter_supported_differences"
      via: "per_call 에 upper 2 entries → distinct-call K=2"
      pattern: "upper_still_handles"
---

<objective>
g1d(quick 260705-g1d) 하이브리드 still 의 배선 교정: (1) still 페어 추출을 스코어러-측
(raw imageio + at_seconds + ref 시간비례 근사 — VFR 에서 파이프라인 9fps 배열과 다른
프레임/다른 동작 국면을 뽑아 위상 불일치)에서 **파이프라인-측**(자기 9fps 프레임 배열의
worst window median 인덱스 + fault_zoom._matched_ref_frame DTW-매칭 인덱스)으로 이동,
(2) upper scope 를 2-call 로 만들어 각도쌍 없는 비-각도 관측(그립/어깨)이 distinct-call
K=2 support 게이트에서 drop 되지 않게 한다.

근거 (2026-07-05 pod 진단 3회): 스코어러-측 추출 페어 = Gemini "편차 없음" 0/6 (위상
불일치의 정당한 판정) / 파이프라인 window·DTW 인덱스 페어(user 20, ref 37) = 6/6 발화
(왼팔 그립 major + 머리/목 moderate + 왼쪽 어깨), 내용 거의 결정적(6회 중 4회 동일).

Purpose: belle 이 지적한 왼팔 그립 등 상체 결함이 pointed set 에 들어가 window 감점을
받을 수 있는 구조 복원. 채점/라우터/support 규칙·PROMPT v11.2·SCHEMA v8.1·agg4 무변경.
Output: 교정된 배선 + 유닛 테스트, 4개 테스트 스위트 GREEN.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py
@backend/functions/pipeline/app.py
@backend/shared/python/sunity_shared/analysis/vision_veto.py
@backend/shared/python/sunity_shared/analysis/fault_zoom.py
@backend/tests/test_gemini_vision_scorer.py
@backend/tests/test_pipeline_vision_gate.py

핵심 사전 지식 (코드 읽기 결과):
- app.py `_build_selected_frame_pair`(line ~1688)가 이미 원하는 파이프라인-측 추출을
  전부 수행한다: FfmpegFrameExtractor(9fps/640px)로 user/ref 프레임 배열 추출, u_idx
  clamp, `fault_zoom._matched_ref_frame(dtw_match, u_idx, r_n)` 로 DTW-매칭 ref 인덱스
  (None 이면 시간비례 폴백), PNG 2장 임시 저장 + cleanup_paths. **새 추출 코드 불필요
  — 이 pair 를 scorer 에 넘기는 배선만 만들면 된다** (최소변경).
- `_collect_vision_fault_context`(app.py ~1792)에서 pair 는 `assess_fault_context_video`
  호출 **전에** 생성되고 PNG unlink 는 호출 **후** finally 에서 일어난다 → 호출 동안
  경로 유효. user_frame_idx = int(round(at * 9.0)) (worst window median × 9fps) 이미 존재.
- scorer 캐시 키 helper `_build_fanout_key(fi)` 에 frame_indices 슬롯 이미 존재
  ('fi{u}_{r}' 직렬화). 이미지 업로드 실패 시 fi=None 재키잉 폴백도 이미 존재.
- `_run_part_frame_fanout` 의 per_call 은 "call 당 1 entry" — upper 를 2회 호출하면
  자동으로 distinct-call 2 가 된다. `_filter_supported_differences` 는 무변경.
- `assess_fault_context`(frame_pair 경로)도 `_run_part_frame_fanout` 을 호출하지만
  upper_still_handles=None → 회귀 없음.
- test_pipeline_vision_gate.py 는 `_build_selected_frame_pair` 와
  `assess_fault_context_video` 를 monkeypatch 한다 (**k 흡수 stub) — 새 kwargs 는
  기존 stub 을 깨지 않으나, 새 배선 assert 테스트를 추가해야 한다.
</context>

<tasks>

<task type="auto">
  <name>Task 1: scorer — still 경로 주입 계약 + 자체 추출 제거 + 캐시 키 분리</name>
  <files>backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py, backend/tests/test_gemini_vision_scorer.py</files>
  <action>
RC-02. gemini_vision_scorer.py 수정:

1. `assess_fault_context_video` 시그니처 교체: `still_at_seconds` 파라미터 제거, 대신
   `still_student_png: str | None = None`, `still_reference_png: str | None = None`,
   `still_frame_indices: list | None = None` (2-원소 [user_idx, ref_idx]) 추가.
   세 값이 **모두** 제공될 때만 still 활성(stills dict 를 제공값으로 구성) — 일부만
   제공되면 미제공 취급(video-only). 결정적 계약, 어중간한 상태 금지.

2. 스코어러-측 추출 경로 **제거**: `_still_frame_index`, `_ratio_ref_index`,
   `_extract_comparison_stills` 3개 함수와 그 위 하이브리드 helper 주석 블록(line
   ~1140-1146) 삭제. 이유(한국어 why 주석으로 대체 박제): raw imageio 인덱싱은 VFR
   에서 파이프라인 frame_extractor(9fps 배열)와 다른 프레임을 반환하고, ref 시간비례
   근사는 측정 window(DTW-매칭 국면)와 다른 동작 국면을 뽑는다 — 2026-07-05 pod 진단:
   위상 불일치 페어 0/6 vs 파이프라인 window/DTW 인덱스 페어 6/6. 추출 책임은
   pipeline(_build_selected_frame_pair, 9fps 배열 + fault_zoom._matched_ref_frame)로
   이동. 폴백으로도 유지하지 않는다(잘못된 페어를 만드는 죽은-틀린 경로, 단순성 우선).

3. still PNG 소유권: 제공 경로는 호출자(pipeline pair.cleanup_paths finally) 소유 —
   scorer 의 `_cleanup_stills`(로컬 PNG unlink) 를 제거한다 (D-10 HIGH-2 "정리는
   생성처와 짝" 원칙). Gemini File API 업로드 핸들 delete(finally)는 그대로 유지.

4. 캐시 키: fi = list(still_frame_indices) (still 활성 시) / None (미제공·업로드 실패
   폴백 시). 이미지 업로드 실패 → fi=None 재키잉 + 'fi-' bucket 폴백 lookup 경로는
   그대로 유지. `INPUT_GRANULARITY_WHOLE_FANOUT` 를 "whole_fanout_hybrid1" →
   "whole_fanout_hybrid2" 로 bump — why 주석: 인덱스 folding 이 키를 분리하지만 g1d
   raw-imageio 인덱스와 파이프라인 9fps 인덱스가 **수치 충돌**하면 픽셀이 다른데 키가
   같아지는 stale-hit 가능(같은 fi ≠ 같은 픽셀) → 프레임 provenance 가 바뀌었으므로
   마커 bump 로 구세대 엔트리 전체와 구조 분리 (90d038f stale-hit 금지 원칙).
   AGGREGATION_VERSION 은 그룹핑 무변경이므로 bump 하지 않는다.

5. app.py:1914 의 `still_at_seconds=at` 호출은 Task 2 에서 새 kwargs 로 교체된다 —
   이 task 종료 시점의 중간 상태는 scorer 테스트만으로 검증 (pipeline gate 테스트는
   assess_fault_context_video 를 mock 하므로 영향 없음).

테스트 정합 (test_gemini_vision_scorer.py — g1d 7종 배선 교정, 계약 완화 아님):
- `test_still_frame_index_pure`, `test_ratio_ref_index_pure` 삭제 (helper 제거됨).
- `fake_stills` fixture: monkeypatch 반환값 대신 실제 임시 PNG 2장 생성 + kwargs
  (still_student_png/still_reference_png/still_frame_indices=[20, 37]) 로 전달하도록
  전환.
- `test_hybrid_upper_scope_gets_image_handles`: kwargs 제공 → upper scope 가 이미지
  핸들 수신 (기존 assert 유지) + **자체 추출 미수행**(imageio.get_reader 미호출 spy
  또는 함수 부재 확인).
- `test_hybrid_extraction_failure_falls_back_to_video` → "still kwargs 미제공 시
  video-only + 'fi-' bucket" 테스트로 재정의.
- `test_hybrid_cold_warm_determinism_and_key_space`: 키에 'fi20_37' +
  'whole_fanout_hybrid2' 포함 assert 로 갱신 (구 'whole_fanout_hybrid1' 키와 불일치).
- `test_hybrid_image_upload_failure_falls_back_and_rekeys`: kwargs 제공 + 업로드 실패
  → fi=None 재키잉 유지 확인 (경로 unlink 는 scorer 가 하지 않음도 assert — 제공 PNG
  파일이 호출 후에도 존재).
- `test_media_kind_image_labels_and_video_byte_identical` 등 프롬프트/스키마 테스트는
  무변경이어야 함 (PROMPT v11.2/SCHEMA v8.1 무변경 확인).

새 튜닝 상수 금지, 이모지 금지, 한국어 why 주석 + 근거 인용(2026-07-05 진단: 위상
불일치 0/6 → 정합 페어 6/6) 유지.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && PYTHONPATH=shared/python:. python3 -m pytest tests/test_gemini_vision_scorer.py -q</automated>
  </verify>
  <done>assess_fault_context_video 가 still 경로/인덱스 kwargs 를 받고, 자체 추출 3함수가 제거되고, 키가 whole_fanout_hybrid2 + 제공 인덱스로 folding 되며, scorer 테스트 GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: pipeline — pair 경로/인덱스 배선 + DTW 매칭 실패 video-only 폴백</name>
  <files>backend/functions/pipeline/app.py, backend/shared/python/sunity_shared/analysis/vision_veto.py, backend/tests/test_pipeline_vision_gate.py</files>
  <action>
RC-01/RC-04. 새 추출 코드를 만들지 않는다 — 기존 `_build_selected_frame_pair` 산출물
(pair)을 재사용한다 (최소변경 판단: ref 프레임 배열은 pair 빌드 시 frame_extractor 로
이미 추출되고 있으며, PNG 는 assess 호출 동안 유효하고 호출 후 기존 finally 가 unlink).

1. vision_veto.py `SelectedFramePair` 에 defaulted 필드 `ref_match_source: str = "ratio"`
   추가 (dataclass 뒤쪽 defaulted 필드라 기존 생성부 비파괴). docstring 에 의미 박제:
   "dtw"=fault_zoom._matched_ref_frame 성공(같은-pose 정합) / "ratio"=시간비례 근사 폴백.

2. app.py `_build_selected_frame_pair`: `r_matched is not None` 이면
   ref_match_source="dtw", 아니면 "ratio" 를 SelectedFramePair 에 세팅.

3. app.py `_collect_vision_fault_context` 의 assess_fault_context_video 호출(~1909):
   `still_at_seconds=at` 제거. 대신:
   - pair 가 존재하고 `pair.ref_match_source == "dtw"` 이면
     `still_student_png=pair.student_frame_path`,
     `still_reference_png=pair.reference_frame_path`,
     `still_frame_indices=[pair.user_frame_idx, pair.ref_frame_idx]` 전달.
   - 그 외(pair None 또는 ratio 폴백)는 still kwargs 미전달 = upper scope video-only.
   한국어 why 주석: 시간비례 근사 still 은 측정 window 와 다른 동작 국면 → 위상 불일치
   페어는 Gemini 가 정당하게 "편차 없음"(2026-07-05 진단 0/6) — DTW-매칭 페어만 still
   로 보낸다(6/6 발화). g1d 의 still_at_seconds 주석 블록(~1905)도 이 근거로 교체.
   pair PNG unlink 는 기존 finally 유지 (assess 호출 후 정리 — 순서 불변).

4. `at`/user_frame_idx 산출(worst window median × 9fps, ~1848)은 무변경 — 이미
   pair 빌드에 들어간다. 채점/라우터/support 규칙 무변경.

테스트 (test_pipeline_vision_gate.py):
- 기존 pair mock(~698)에 ref_match_source="dtw" 부여해 기존 계약 유지.
- 신규 1: pair.ref_match_source="dtw" → _fake_video_fanout 이 still_student_png/
  still_reference_png/still_frame_indices=[u_idx, r_idx] 를 수신하는지 assert.
- 신규 2: pair.ref_match_source="ratio" (또는 pair None) → still kwargs 미수신
  (video-only 폴백) assert.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && PYTHONPATH=shared/python:. python3 -m pytest tests/test_pipeline_vision_gate.py tests/test_gemini_vision_scorer.py -q</automated>
  </verify>
  <done>collect 가 DTW-매칭 pair 의 경로/인덱스를 scorer 에 전달하고, 매칭 실패 시 still 미전달(video-only)이며, gate+scorer 테스트 GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: upper scope 2-call fanout + telemetry 정합 + 전체 스위트</name>
  <files>backend/shared/python/sunity_shared/analysis/gemini_vision_scorer.py, backend/tests/test_gemini_vision_scorer.py</files>
  <action>
RC-03. `_run_part_frame_fanout` 수정:

1. scope 당 호출 수 산정: `upper_body` 이면서 upper_still_handles 존재 → 2 call
   (동일 이미지 핸들 재사용 — 업로드 증가 0, uploadCount=4 불변), 그 외 scope 는 1 call
   유지. planned = min(총 호출 수, max_calls). 3 scopes 기준 still 성공 시 planned=4
   (MAX_VETO_CALLS=9, wall 300s 내). 루프는 (scope, repeat) 평탄화로 재구성하되
   wall-clock 가드(호출 전 elapsed)·점수 누출 폐기·parse 실패 skip 로직 무변경.
   per_call 은 call 당 1 entry 그대로 → upper 2 entries = distinct-call K=2 성립.
   한국어 why 주석: upper scope 는 fanout 에서 1 call 뿐이라 각도쌍 없는 비-각도 관측
   (왼팔 그립/왼쪽 어깨)이 distinct-call K=2 미달로 support 게이트에서 drop → pointed
   set 진입 불가 = window 감점 불가. still 내용이 거의 결정적(2026-07-05 진단 6회 중
   4회 완전 동일)이라 같은 이미지 2-sample 로 교차확증이 성립한다. 기존 distinct-call
   의미론(WR-01)을 충족시키는 **입력 변경**이지 support 규칙 변경이 아니다 —
   `_filter_supported_differences`/라우터/agg4 무변경.

2. telemetry 정합: plannedCalls=planned(4), completedCalls, samplingComplete =
   completed >= planned 그대로. upperGranularity 마커 유지. resource_limited
   fail-closed(Option A) 의미 무변경 — upper 2번째 call 미완이면 resource_limited.

3. 신규 테스트 (test_gemini_vision_scorer.py):
   - upper_still_handles 제공 시: 호출 시퀀스가 upper 이미지 핸들 2회 + lower/line
     video 1회씩, telemetry plannedCalls==4, samplingComplete True.
   - upper_still_handles=None(video 폴백): planned==3 유지 (기존 의미 회귀 0).
   - e2e: 각도쌍 없는 upper 관측(그립)이 두 upper call 에 등장 → distinct-call K=2
     통과로 supported 에 생존 (_supportCount==2).
   - wall budget 소진으로 upper 2번째 call 미완 → resource_limited.
   기존 `test_emission_to_support_end_to_end_upper_body` 등 fanout 사용 테스트의
   planned 기대값 정합 수정 허용 (배선 교정, 계약 완화 아님).

4. INPUT_GRANULARITY 재bump 불필요 판단 박제: Task 1 의 hybrid2 bump 가 이 plan 의
   결과 형태 변화(2-call 집계 포함)까지 한 번에 커버 — 같은 plan 내 단일 bump 로 충분.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && PYTHONPATH=shared/python:. python3 -m pytest tests/test_gemini_vision_scorer.py tests/test_pipeline_vision_gate.py tests/test_deduction_engine.py tests/test_phase25_eval_gates.py -q</automated>
  </verify>
  <done>still 존재 시 upper 2-call(planned=4)·video 폴백 시 1-call(planned=3), 각도쌍 없는 upper 관측이 K=2 로 생존, 4개 스위트 전부 GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline→Gemini File API | still PNG/영상 업로드 (외부 서비스) — 기존 경계, 신규 없음 |
| Gemini 응답→support 게이트 | 비신뢰 LLM 출력이 감점 배선에 진입하는 지점 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-h5z-01 | Tampering | VisionVetoCache (whole_fanout 키) | mitigate | 프레임 provenance 변경 → INPUT_GRANULARITY_WHOLE_FANOUT bump 로 구세대(픽셀 상이) 엔트리 stale-hit 구조 차단 (Task 1) |
| T-h5z-02 | Spoofing/환각 | Gemini upper 2-call 출력 | mitigate | 기존 support 게이트(distinct-call K=2)·severity none 필터·점수 누출 폐기 무변경 — 입력만 교정, 규칙 완화 0 |
| T-h5z-03 | Information Disclosure | 임시 still PNG | accept | 로컬 임시 파일, 기존 pair.cleanup_paths finally unlink 유지 (PII 아님, 기존 수명과 동일) |
| T-h5z-SC | Tampering | 패키지 설치 | accept | 신규 의존성 설치 0 (기존 imageio/PIL/google-genai 만 사용) |
</threat_model>

<verification>
cd /Users/kimtaesung/Dev/SunityMotion/backend && PYTHONPATH=shared/python:. python3 -m pytest tests/test_gemini_vision_scorer.py tests/test_pipeline_vision_gate.py tests/test_deduction_engine.py tests/test_phase25_eval_gates.py -q

추가 확인:
- grep 으로 `_extract_comparison_stills|_still_frame_index|_ratio_ref_index|still_at_seconds` 가 backend 전역에서 0건 (제거 완전성).
- PROMPT_VERSION v11.2 / SCHEMA v8.1 문자열 무변경 (git diff 로 프롬프트/스키마 미접촉 확인).
- 새 튜닝 상수 0 (K=2, MAX_VETO_CALLS=9 등 기존 상수만).
</verification>

<success_criteria>
- pipeline 의 9fps window/DTW-매칭 still 페어가 경로+인덱스로 scorer 에 주입되고, 스코어러-측 추출(위상 불일치 경로)은 코드에서 사라짐.
- DTW 매칭 실패 시 still 미전달 = upper scope video-only 폴백.
- upper scope 2-call 로 각도쌍 없는 상체 관측이 distinct-call K=2 를 충족 가능 (support 규칙 자체는 무변경).
- 캐시 키: 인덱스 folding + whole_fanout_hybrid2 bump — g1d 엔트리 stale-hit 구조 불가.
- 4개 테스트 스위트 GREEN. 채점/라우터/support/PROMPT/SCHEMA/agg4 무변경.
</success_criteria>

<output>
Create `.planning/quick/260705-h5z-still-pair-pipeline-side-extraction/260705-h5z-SUMMARY.md` when done
</output>
