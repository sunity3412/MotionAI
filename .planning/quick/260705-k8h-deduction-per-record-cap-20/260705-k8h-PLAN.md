---
phase: quick-260705-k8h
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/shared/python/sunity_shared/analysis/deduction_engine.py
  - backend/shared/python/sunity_shared/models.py
  - app/src/types/analysis.ts
  - docs/contract.md
  - backend/tests/test_deduction_engine.py
autonomous: true
requirements: [QUICK-260705-K8H]

must_haves:
  truths:
    - "단일 criterion record 의 감점이 아무리 커도 -20 을 넘지 않는다 (kip-up 잘못된 시연 26점 -> 47점급)"
    - "상한이 적용된 record 는 원 감점(rawPoints)과 capApplied 마커를 함께 노출한다 (투명 내역 유지)"
    - "상한 미적용 record 는 기존과 byte-동일 형상이다 (구 앱/legacy doc 무영향)"
    - "breakdown.final == max(0, round(100 + capped points 합)) 정합이 유지된다"
    - "unavailable 폴백(dimension_overall_fallback) record 는 상한 비적용 — final == dimension_overall 불변식 보존"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/deduction_engine.py"
      provides: "PER_RECORD_DEDUCTION_CAP = 20.0 상수 + per-criterion loop 클램프 + DeductionRecord raw_points/cap_applied"
      contains: "PER_RECORD_DEDUCTION_CAP"
    - path: "backend/shared/python/sunity_shared/models.py"
      provides: "DEDUCTION_RECORD_OPTIONAL_KEYS = (rawPoints, capApplied)"
      contains: "DEDUCTION_RECORD_OPTIONAL_KEYS"
    - path: "app/src/types/analysis.ts"
      provides: "DeductionRecord 에 rawPoints?/capApplied? optional 필드"
      contains: "rawPoints"
    - path: "backend/tests/test_deduction_engine.py"
      provides: "cap 클램프/투명성/경계/final 정합 유닛 게이트 + 기존 strict 단언 cap-인지 갱신"
      contains: "PER_RECORD_DEDUCTION_CAP"
  key_links:
    - from: "backend/shared/python/sunity_shared/analysis/deduction_engine.py"
      to: "app/src/types/analysis.ts"
      via: "test_contract_lockstep (DEDUCTION_RECORD_KEYS | OPTIONAL_KEYS == TS 필드 set)"
      pattern: "DEDUCTION_RECORD_OPTIONAL_KEYS"
    - from: "backend/shared/python/sunity_shared/analysis/deduction_engine.py"
      to: "backend/evals/phase24/assert_gates.py"
      via: "check_monotonicity (비증가 게이트 — cap 포화는 동점 허용이므로 게이트 소스 무접촉)"
      pattern: "check_monotonicity"
---

<objective>
관절(criterion record)당 감점 상한 -20 도입 — 투명 내역 유지 (belle 승인 2026-07-05).

belle 실기기 관측: kip-up 잘못된 시연이 26점 ("너무 무지막지하게 깎인다").
내역 = split -36(dev 30) + 왼어깨 -24.4(dev 20.4) + 오른어깨 -13.2. run6 실데이터로
4개 누적 규칙 비교 결과 체감가중/RSS 는 작은-결함-다수 동작(elbow/pdshape)을 82~85점으로
역전시켜 변별 붕괴 — 탈락. 관절당 상한 -20 이 채택됨: kip-up 47 (100 - 20 - 20 - 13.2),
작은 결함 다수 동작 무영향(elbow 61/pdshape 54 유지), power-spin 22.9 record 만 20 으로 -> 57.
도메인 근거: IPSF 도 결함 유형별 감점 상한 구조. 원칙 정합: 고정 상한은 명시 규칙(밴드 아님,
severity 밴드 재도입 아님 — record 단위 클램프이지 final 밴드가 아님). 임계값 수치 belle
라벨 OK ([[analysis-objectivity-no-human-scores]]).

Purpose: 단일 대형 결함 record 의 폭주 감점을 상한으로 막되, 감점-합산 투명성
([[scoring-must-be-transparent-deduction-tally]])은 rawPoints/capApplied 노출로 보존.
Output: 엔진 클램프 + 계약 3-way(models.py/analysis.ts/contract.md) 동기 + 유닛 게이트.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@backend/shared/python/sunity_shared/analysis/deduction_engine.py
@backend/shared/python/sunity_shared/models.py  (DEDUCTION_RECORD_KEYS, line ~152)
@app/src/types/analysis.ts  (DeductionRecord interface, line ~471)
@docs/contract.md  (§10.1~§10.2, line ~1422)
@backend/tests/test_deduction_engine.py

**사전 조사 완료 사실 (재조사 불필요):**
- slope = kismam._PENALTY_PER_DEG = 1.2/deg, tol = 20.0deg, _ANGLE_CAP = 90.0.
  raw = over * 1.2 이므로 over > 16.67deg 부터 상한 발동.
- fallback record(dimension_overall_fallback, tally 상단 조기 return 경로)는 상한
  비적용이어야 함: test_unavailable_falls_back_not_100 이 final == 62 (points -38) 를
  요구 — final == dimension_overall 불변식 (contract.md §10.4).
- evals/phase24/assert_gates.py check_monotonicity 는 "finals[k] > finals[k-1]" 만
  실패 처리 (비증가 게이트) — cap 포화(동점)는 PASS. 게이트 소스 무접촉 (README locked,
  [[calibration-source-hard-gate]]).
- evals/phase25/assert_gates.py 도 무접촉. test_phase25_eval_gates.py 는 합성 fixture
  기반(sum(points) 로 final 자체 계산)이라 엔진 변경에 자동 정합.
- test_no_final_band 소스 가드: "min(100" / "min(final" 문자열이 엔진 소스에 없어야 함
  — min(capped, PER_RECORD_DEDUCTION_CAP) 는 저촉 없음.
- test_contract_lockstep 의 regex 는 `(\w+)\??:` 라 TS optional 필드도 ts_fields 에
  포함됨 — TS 에 optional 추가 시 이 테스트의 set 동등 단언도 함께 갱신 필수.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 엔진 per-record cap + 계약 3-way 동기</name>
  <files>backend/shared/python/sunity_shared/analysis/deduction_engine.py, backend/shared/python/sunity_shared/models.py, app/src/types/analysis.ts, docs/contract.md</files>
  <behavior>
    - 상한 초과 record: points == -20.0, to_dict() 에 rawPoints(상한 전 signed-negative)
      + capApplied: True 추가
    - 상한 이하 record: to_dict() 키 집합이 기존 DEDUCTION_RECORD_KEYS 와 정확히 동일
      (rawPoints/capApplied 생략 — byte-호환)
    - final = max(0, round(100 + Σ capped points))
    - fallback record(dimension_overall_fallback)는 클램프 대상 아님 (final == dim 불변)
  </behavior>
  <action>
    **deduction_engine.py:**
    1. `_BASELINE` 옆에 모듈 상수 추가:
       `PER_RECORD_DEDUCTION_CAP = 20.0` — 한국어 why 주석 필수: belle 승인 2026-07-05,
       run6 실데이터 4-규칙 비교(체감가중/RSS 는 작은-결함-다수 동작 역전으로 탈락),
       IPSF 결함 유형별 감점 상한 구조 정합, severity 밴드 아님(record 단위 명시 규칙,
       final 밴드 없음 그대로). 임계값 라벨 = belle OK.
    2. `DeductionRecord` frozen dataclass 에 default 필드 2개 추가 (기존 생성부 —
       fallback record 포함 — 무수정으로 호환):
       `raw_points: float | None = None`, `cap_applied: bool = False`.
       `to_dict()` 는 기존 dict 를 만들고 `if self.cap_applied:` 일 때만
       `"rawPoints": self.raw_points`, `"capApplied": True` 를 추가 (미적용 시 키 자체
       생략 — Firestore-flat scalar 유지).
    3. per-criterion loop 의 `capped = min(raw, crit["ipsf_cap"])` 직후에 클램프:
       ```
       capped_r = round(capped, 1)
       cap_hit = capped_r > PER_RECORD_DEDUCTION_CAP
       points_val = PER_RECORD_DEDUCTION_CAP if cap_hit else capped_r
       ```
       record 생성 시 `points=-points_val`, `raw_points=(-capped_r if cap_hit else None)`,
       `cap_applied=cap_hit`. 반올림(0.1 단위) 후 비교로 float epsilon 이 가짜
       capApplied(rawPoints == points 인 잡음 record)를 만들지 않게 한다. 경계
       (capped_r == 20.0 정확히)는 상한 이하 취급 — 필드 생략.
       fallback record 경로(조기 return)는 절대 건드리지 않는다 — 거긴 whole-score
       폴백이라 클램프하면 final == dimension_overall 불변식과 100+Σpoints==final
       추적성이 동시에 깨진다. 이 이유를 상수 주석에 명기.
    4. 모듈 docstring 6행("유일한 clamp 은 max(0,…)(상한 밴드 없음)")과
       DeductionBreakdown docstring 을 갱신: per-record 클램프 -20 존재를 명기하되
       "final 밴드/severity 밴드 없음" 은 그대로 참(record 단위 vs final 단위 구분).

    **models.py:** `DEDUCTION_RECORD_KEYS` 는 필수 11키 그대로 두고 바로 아래에
    `DEDUCTION_RECORD_OPTIONAL_KEYS = ("rawPoints", "capApplied")` 추가.
    주석: per-record 상한 -20 적용 record 에만 방출(additive optional — 구 doc/앱
    byte-호환), 3-way lockstep 명시 (analysis.ts / contract.md §10.2).

    **app/src/types/analysis.ts:** `DeductionRecord` 에 optional 필드 추가:
    ```
    rawPoints?: number;   // SIGNED NEGATIVE — per-record 상한(-20) 적용 전 원 감점. capApplied 시에만.
    capApplied?: true;    // 이 record 의 감점이 관절당 상한 -20 으로 클램프됨 (belle 2026-07-05)
    ```
    line ~484 의 "유일한 clamp 은 max(0,…)(상한 밴드 없음)" 주석도 per-record cap 을
    반영해 갱신 (final 밴드 없음은 유지). 앱 화면 코드는 무수정 (optional 이라 안전).

    **docs/contract.md:** §10.2 "DeductionRecord (11 필드)" -> 필수 11 + optional 2 로
    갱신, rawPoints/capApplied 행 추가(방출 조건 포함). §10.1 의 "유일한 clamp 은
    max(0,…)" 문장에 per-record 상한 -20(criterion record 단위, capApplied 시 rawPoints
    로 원 감점 투명 노출) 을 추가. final 산식 자체는 불변(points 가 이미 capped 값).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && PYTHONPATH=shared/python:. python3 -c "
from sunity_shared.analysis import deduction_engine as de
from sunity_shared import models
assert de.PER_RECORD_DEDUCTION_CAP == 20.0
assert models.DEDUCTION_RECORD_OPTIONAL_KEYS == ('rawPoints', 'capApplied')
b = de.tally(None, {'supported_differences': []}, dimension_overall=80,
             measured_deviations={'leg_extension': 60.0}, dimension_scores=None,
             baseline_kind='hip_line')
rec = [r for r in b.records if r.criterion == 'leg_extension'][0].to_dict()
assert rec['points'] == -20.0 and rec['rawPoints'] == -48.0 and rec['capApplied'] is True
assert b.final == 80
print('cap OK')"</automated>
  </verify>
  <done>상한 초과 record 가 points -20.0 + rawPoints/capApplied 로 방출, 상한 이하 record 는 기존 11키 그대로, 계약 4곳(engine/models/analysis.ts/contract.md) 동기. 이모지 0, 한국어 why 주석.</done>
</task>

<task type="auto">
  <name>Task 2: 유닛 게이트 신설 + cap-인지 기존 단언 갱신 + 전체 검증</name>
  <files>backend/tests/test_deduction_engine.py</files>
  <action>
    **기존 테스트 갱신 (test_deduction_engine.py — cap 도입으로 깨지는 5곳만, 나머지 무접촉):**
    1. `test_contract_lockstep`: TS regex 가 optional 필드도 잡으므로
       `assert set(models.DEDUCTION_RECORD_KEYS) | set(models.DEDUCTION_RECORD_OPTIONAL_KEYS) == ts_fields`
       로 갱신 + `assert not set(models.DEDUCTION_RECORD_KEYS) & set(models.DEDUCTION_RECORD_OPTIONAL_KEYS)`.
    2. `test_geometry_path_source_geometry` (leg=40 -> raw 24 -> capped): 키 set 동등 단언을
       "필수 ⊆ 키 ⊆ 필수∪optional" 로 갱신. 파일 상단에 공용 helper
       `_assert_record_keys(rec_dict)` 를 하나 추가해 재사용: 필수 키 전부 존재 +
       초과 키는 optional 만 + rawPoints/capApplied 는 함께만 존재(둘 다 있거나 둘 다 없음).
    3. `test_no_final_band` (leg/arm/split 500 -> 이제 final 0 아님): 상한 도입 후
       final == 100 - round(3 * de.PER_RECORD_DEDUCTION_CAP) == 40 (상수 파생 구조 단언,
       sweep 수치 타깃 아님) 으로 갱신 + 기존 소스 가드("min(100"/"min(final" 부재) 유지.
       주석: 밴드-부재 의미는 유지(50/75/90 floor 없음, 40 < 50) — record 단위 cap 은 밴드 아님.
    4. `test_deadzone_and_slope`: sweep devs 를 sub-cap 영역 (0.0, 25.0, 30.0, 35.0)
       (over 5/10/15 -> raw 6/12/18 < 20) 으로 바꿔 strict 단조 단언 보존. 주석에
       "cap 초과 영역 포화는 cap 전용 테스트에서 단언" 명기.
    5. `test_split_reference_relative_scored_and_monotonic`: finals sweep 도 동일하게
       sub-cap devs (0.0, 25.0, 30.0, 35.0) 로 교체 (앞부분 split=40 단언은 무수정 —
       deviation/unit/baseline 단언은 cap 과 무관하게 성립).
    6. `test_breakdown_serializes_flat` (leg=40 -> capped record 포함): per-record 키 단언을
       `_assert_record_keys` 로 교체. flat-scalar 루프(list/dict 금지)는 그대로 —
       rawPoints(float)/capApplied(bool) 는 scalar 라 자동 통과. final 정합 단언 유지.

    **신설 테스트 (새 섹션 `# ── per-record 감점 상한 -20 (quick-260705-k8h, belle 승인) ──`):**
    - `test_per_record_cap_clamps_and_exposes_raw`: leg=60 (over 40, raw 48) ->
      record points == -20.0(== -de.PER_RECORD_DEDUCTION_CAP), to_dict()["rawPoints"] == -48.0,
      capApplied is True, b.final == 80. 투명성 본체 게이트.
    - `test_per_record_cap_under_threshold_byte_compatible`: leg=30 (raw 12) ->
      `set(rec.to_dict()) == set(models.DEDUCTION_RECORD_KEYS)` 정확 동등 (rawPoints/capApplied
      키 자체 부재 — 구 형상 byte-호환).
    - `test_per_record_cap_boundary_exact_cap_not_flagged`: monkeypatch 로
      `deduction_engine.PER_RECORD_DEDUCTION_CAP = 12.0` 설정 후 leg=30 -> raw 정확히
      12.0(over 10 * slope 1.2) == cap -> points -12.0, rawPoints/capApplied 부재
      (경계 == 클램프 아님). 같은 테스트에서 cap 11.9 로 재패치 -> capApplied True 대비쌍.
      (slope 1.2 로는 raw 정확히 20.0 을 만드는 깔끔한 dev 가 없어 monkeypatch 가
      결정적 경계 검증 수단 — 튜닝 상수 변경 아님, 메커니즘 검증.)
    - `test_per_record_cap_final_sum_consistency`: leg=60 + arm=60 (둘 다 capped) ->
      b.final == max(0, round(100 + sum(r.points for r in b.records))) == 60,
      to_dict()["final"] 동일 — Σ 는 capped 값 기준.
    - `test_fallback_record_not_capped`: quant unavailable + 빈 seed + dim=62 ->
      fallback record points == -38.0 (< -20 허용), rawPoints/capApplied 부재,
      final == 62 — cap 은 criterion 감점 record 전용임을 고정.
    - `test_per_record_cap_deterministic`: leg=60 동일 입력 2회 -> to_dict() 동일
      (rawPoints/capApplied 포함 byte-identical).

    **주의:** evals/phase24/assert_gates.py, evals/phase25/assert_gates.py, README locked
    게이트 소스 전부 무접촉. 실영상 점수 리터럴(47 등) assert 금지 — 상수 파생 값만.
    severity 밴드 재도입 금지. 이모지 금지.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && PYTHONPATH=shared/python:. python3 -m pytest tests/test_deduction_engine.py tests/test_phase24_gates.py tests/test_phase25_eval_gates.py tests/test_deduction_seed_pointed_merge.py tests/test_pipeline_deduction_seam.py tests/test_pipeline_vision_gate.py tests/test_pipeline_mode3.py tests/test_gemini_vision_scorer.py tests/test_vision_veto.py tests/test_p1_objective_knee_decontamination.py -q && cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck</automated>
  </verify>
  <done>신설 6 게이트 + 갱신 5곳 전부 GREEN, deductionBreakdown 소비 테스트 10파일 회귀 0, app tsc --noEmit 통과. check_monotonicity 실엔진 PASS (cap 포화 = 동점 = 비증가 허용) 를 test_phase24_gates.py 로 확인.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 없음 (신규) | 순수 채점 수학 모듈 내부 변경 — 네트워크/입력 경계 신설 없음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-k8h-01 | Tampering | deduction_engine.tally points 산출 | mitigate | 기존 NaN/Inf guard (np.isfinite) 유지 + 클램프는 round 후 비교로 float epsilon 잡음 record 방지, 결정성 유닛 게이트 |
| T-k8h-02 | Repudiation | 감점 내역 투명성 (rawPoints 은폐) | mitigate | capApplied record 는 rawPoints 필수 동반 — 유닛 테스트가 쌍 존재를 고정 |
| T-k8h-SC | Tampering | 패키지 설치 | accept | 신규 의존성 0 (기존 numpy 만) — 설치 태스크 없음 |
</threat_model>

<verification>
- cd backend && PYTHONPATH=shared/python:. python3 -m pytest tests/test_deduction_engine.py tests/test_phase25_eval_gates.py -q  (요청 명시 게이트)
- 위 + phase24 gates/seam/pipeline/vision 소비 테스트 10파일 GREEN (Task 2 verify)
- cd app && npm run typecheck
- 게이트 소스(evals/*/assert_gates.py) diff 0
</verification>

<success_criteria>
- 단일 record 감점 상한 -20 클램프 + rawPoints/capApplied 투명 노출 (상한 이하 record 는 byte-호환)
- final = 100 - Σ capped points 정합, fallback record 비적용
- 계약 3-way (models.py / analysis.ts / docs/contract.md) 동기, 앱 화면 코드 무수정
- 전체 소비 테스트 회귀 0, locked 게이트 소스 무접촉
</success_criteria>

<output>
Create `.planning/quick/260705-k8h-deduction-per-record-cap-20/260705-k8h-SUMMARY.md` when done
</output>
