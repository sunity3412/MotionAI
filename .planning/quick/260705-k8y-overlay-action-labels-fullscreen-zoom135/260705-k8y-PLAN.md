---
phase: quick-260705-k8y
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/VideoCompare.tsx
  - app/src/lib/deductionLabels.ts
  - app/src/components/KeypointOverlay.tsx
  - app/src/app/analysis/result.tsx
autonomous: true
requirements: [QT-260705-k8y]
must_haves:
  truths:
    - "가로 전체화면 뷰어에서 두 영상 박스가 1.35배 확대되고 위아래(및 좌우 약간)가 클리핑 래퍼로 잘려 인물이 더 크게 보인다 (belle: '위아래 크롭하고 좀 더 키워야')."
    - "오버레이 마커가 확대된 영상과 계속 정합한다 (확대 박스 내부 absoluteFill 이므로 자동)."
    - "문제 관절(확정 감점 빨강 + 측정초과 주황)에만 행동 지시형 라벨('왼쪽 무릎 23° 더 펴야' 형태)이 표시되고, 정상 관절은 마커만 남는다 — 절대각 숫자 라벨(158° 등)은 전면 제거."
    - "라벨 문구는 실측 주입 데이터(관절 키·signed 편차·기하 의미 사전)로만 조립 — 방향 데이터가 없는 소스(faultJointDeficits)는 방향 생략 폴백('기준과 N° 차이') (거짓 구체성 금지, 260704-fwb)."
    - "Mode3(이전 영상 비교)에서 문제 관절 데이터가 없으면 라벨 없이 마커만 렌더 — 크래시/거짓 라벨 0 (안전 폴백)."
    - "세로 카드 경로(전체화면 줌)는 무접촉, 세로/전체화면 라벨 규칙은 동일 컴포넌트라 함께 전환."
  artifacts:
    - path: "app/src/components/VideoCompare.tsx"
      provides: "FULLSCREEN_ZOOM=1.35 모듈 상수 + renderFullscreenSlot 클리핑 래퍼(overflow hidden) + 내부 1.35배 확대 박스"
      contains: "FULLSCREEN_ZOOM"
    - path: "app/src/lib/deductionLabels.ts"
      provides: "composeActionLabelKo 순수 매핑 함수 (angle key 계열 + signed delta → 행동 지시 문구, 하드코딩 동작명 0)"
      contains: "composeActionLabelKo"
    - path: "app/src/components/KeypointOverlay.tsx"
      provides: "actionLabels prop — 문제 관절 한정 행동 지시 라벨 렌더 (절대각 라벨 대체, 동적 pill 폭, 우측 overflow 시 좌측 배치)"
      contains: "actionLabels"
    - path: "app/src/app/analysis/result.tsx"
      provides: "actionLabels memo 조립 (windowMedianAngleDeltas > faultJointDeficits > JointScore 우선순위) + 좌측 오버레이 배선"
      contains: "actionLabels"
  key_links:
    - from: "app/src/app/analysis/result.tsx"
      to: "app/src/lib/deductionLabels.ts"
      via: "composeActionLabelKo import + KEYPOINT_FROM_ANGLE_KEY 재사용"
      pattern: "composeActionLabelKo"
    - from: "app/src/app/analysis/result.tsx"
      to: "app/src/components/KeypointOverlay.tsx"
      via: "leftOverlay render prop 의 actionLabels prop"
      pattern: "actionLabels=\\{"
    - from: "app/src/components/VideoCompare.tsx"
      to: "app/src/components/KeypointOverlay.tsx"
      via: "확대 박스 내부 overlayContainer(absoluteFill) — 줌·클리핑 정합 자동"
      pattern: "overlayContainer"
---

<objective>
가로 전체화면 뷰어 개선 2건 (belle 실기기 2026-07-05, 승인 완료):
1. 고정 1.35배 줌 — 영상 박스를 1.35배 키우고 overflow hidden 클리핑 래퍼로 위아래를 잘라 인물을 크게 (천장 불필요).
2. 각도 라벨을 행동 지시형으로 교체 — "158°" 절대각 나열 대신 문제 관절에만 "왼쪽 무릎 23° 더 펴야" 형태 (편차+방향), 정상 관절은 마커만.

Purpose: belle 피드백 — 절대각 숫자는 수강생이 못 알아듣고, 라벨이 너무 작음. 수치는 보조·행동이 핵심 (PROJECT Core Value 정합).
Output: JS-only 변경 (OTA 가능). typecheck GREEN + SUMMARY 실기기 체크리스트.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@app/CLAUDE.md
@app/src/components/VideoCompare.tsx
@app/src/components/KeypointOverlay.tsx
@app/src/lib/deductionLabels.ts
@app/src/app/analysis/result.tsx (오버레이 배선부 560-620, 858-870, 1082-1150행)
@app/src/types/analysis.ts (VisionAngleDelta 362-384행, JointScore 151-173행)

planner_findings (실행 전 필독 — 코드 재탐색 불필요):
- 방향 데이터 소스 3벌 (전부 signed delta = student − reference 규약 통일):
  1. result.visionVeto.windowMedianAngleDeltas.deltas[] — { joint(kismam key), student_deg, reference_deg, delta_deg(signed), direction("더 굽음"|"더 펴짐") }. mode1 veto applied 시에만.
  2. result.visionVeto.faultJointDeficits — Partial<Record<KeypointName, number>>, Gemini 시각 추정 편차 크기(부호 없음). split_angle 등 windowMedian 에 없는 확정 관절 커버.
  3. JointScore(joints[]).deltaDeg(signed) + direction(enum) — kismam 평균, Mode3 커버. 단 "veto 결함을 못 잡아 과소" (fault_zoom 주석) → 우선순위 최하.
- 백엔드 direction 의미 확정 (features.py 169-170, kismam.py 95-108): delta<0 = 더 굽음(각 작음) → 펴야/벌려야, delta>0 = 더 펴짐 → 굽혀야/모아야. kismam JOINT_DIRECTION_PAIRS: elbow/knee=(extend,flex), hip=(open,close), shoulder=(raise,lower).
- 관절 의미 사전 (fz4): deductionLabels.ts 의 ANGLE_MEANING_KO(팔꿈치 굽힘/겨드랑이 벌림/다리 벌림/무릎 굽힘) + JOINT_LABEL_KO + KEYPOINT_FROM_ANGLE_KEY(elbow→hand 투영).
- KeypointOverlay 현 라벨: highlighted∪attention 관절에 절대각 `${Math.round(pair.current)}°` 를 64×26 고정 pill 로 렌더 (459-505행). viewBox 0 0 1 1 정규화 — 모든 크기 상수는 /W, /H 나눗셈 + sizeScale(S) 곱.
- 전체화면 박스: fsBoxH=fsShort, fsBoxW=fsShort*9/16 숫자 치수 (fx4 fix). renderFullscreenSlot 이 fsVideoBox 에 인라인 주입, overlayContainer=absoluteFill.
- FULLSCREEN_OVERLAY_SCALE=2.0 이 opts.sizeScale 로 전달됨 — 라벨 가독 메커니즘 기존 보유.
</context>

<tasks>

<task type="auto">
  <name>Task 1: VideoCompare 전체화면 고정 1.35배 줌 (클리핑 래퍼)</name>
  <files>app/src/components/VideoCompare.tsx</files>
  <action>
renderFullscreenSlot 의 영상 박스를 2겹 구조로 전환한다 (세로 카드 경로 무접촉):

1. 모듈 상수 신설: `const FULLSCREEN_ZOOM = 1.35;` — 주석에 why 박제: "belle 실기기 3차 2026-07-05 승인 — 세로영상 위아래 여백(천장 등)을 잘라내고 인물을 키운다. 클리핑 래퍼가 기존 박스 자리를 유지하므로 두 박스 row 레이아웃/컨트롤 배치 불변."
2. 클리핑 래퍼(바깥) = 기존 박스 자리 그대로: width fsBoxW, height fsBoxH(=fsShort), `overflow: 'hidden'`. 스타일 fsVideoBox 는 래퍼로 유지 (backgroundColor 포함).
3. 내부 확대 박스: width `Math.round(fsBoxW * FULLSCREEN_ZOOM)`, height `Math.round(fsBoxH * FULLSCREEN_ZOOM)`, position absolute + 상하좌우 중앙 정렬: left `-(확대W − fsBoxW)/2` (Math.round), top `-(확대H − fsBoxH)/2` (Math.round). VideoView(contentFit contain) + overlayContainer + overlay 호출을 전부 이 내부 박스 안으로 이동 — overlayContainer 가 absoluteFill 이라 오버레이가 영상과 함께 확대·클리핑돼 마커 정합 자동 유지 (fx4 정합 기준 박스 = 내부 확대 박스로 이동).
4. slotEmpty(준비 중) 분기와 fsSlotLabel(상단 라벨)은 래퍼(바깥) 직속 유지 — 라벨은 잘리면 안 됨.
5. 확대/클리핑 수치는 전부 fsBoxW/fsBoxH 파생 (하드코딩 픽셀 0). 세로 카드(slotFrame)와 VideoSlot 은 건드리지 않는다.

주의: 9:16 영상이 9:16 박스에 contain 이라 letterbox 없음 → 1.35배 확대 시 상하 각 ~17.5%, 좌우 각 ~17.5% 크롭. 인물은 통상 중앙이라 안전 — belle 승인 설계.
  </action>
  <verify>
    <automated>grep -c "FULLSCREEN_ZOOM" /Users/kimtaesung/Dev/SunityMotion/app/src/components/VideoCompare.tsx | awk '$1>=2' && cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck</automated>
  </verify>
  <done>전체화면 슬롯이 클리핑 래퍼(fsBoxW×fsBoxH, overflow hidden) + 내부 1.35배 확대 박스(중앙 정렬) 구조. VideoView·오버레이가 내부 박스 소속. 세로 카드 경로 diff 0. typecheck GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: composeActionLabelKo 순수 매핑 + KeypointOverlay actionLabels 렌더 전환</name>
  <files>app/src/lib/deductionLabels.ts, app/src/components/KeypointOverlay.tsx</files>
  <action>
**A. deductionLabels.ts — 행동 지시 문구 순수 매핑 함수 (단일 출처, UI 재계산 금지 원칙 유지 — 입력은 backend signed delta 그대로):**

`export function composeActionLabelKo(angleKey: string, signedDeltaDeg: number): string | null`

- `const n = Math.round(Math.abs(signedDeltaDeg));` — `!Number.isFinite(signedDeltaDeg) || n < 1` 이면 null (라벨 생략 = 마커만).
- 부위 라벨 = `JOINT_LABEL_KO[angleKey]` — 없으면 null.
- angle key 계열별 행동 문구 (kismam JOINT_DIRECTION_PAIRS + features._DIR_MORE_BENT/_EXTENDED 의미 미러 — 관절 기하 사전이지 동작명 하드코딩 아님. 주석에 근거 박제: delta<0 = 기준보다 더 굽음/닫힘):
  - `left_elbow|right_elbow|left_knee|right_knee` (굽힘 관절): delta<0 → `"${부위} ${n}° 더 펴야"` / delta>0 → `"${부위} ${n}° 더 굽혀야"`
  - `left_hip|right_hip` (다리 벌림 = 몸통-허벅지 개방각, ANGLE_MEANING_KO 정합): delta<0 → `"다리 ${n}° 더 벌려야"` / delta>0 → `"다리 ${n}° 더 모아야"` (belle 예시 "다리 더 올려야" 와 동의 — 사전 단어 '벌림' 과 정합 우선, 좌우 구분은 마커 위치가 전달)
  - `left_shoulder|right_shoulder` (겨드랑이 벌림): delta<0 → `"팔 ${n}° 더 벌려야"` / delta>0 → `"팔 ${n}° 더 모아야"`
  - delta===0 인데 n>=1 은 불가(라운딩) — 방어적으로 방향 폴백 처리.
- 방향 생략 폴백 export 추가: `export function composeDeviationOnlyLabelKo(angleKey: string, deviationDeg: number): string | null` — n 산출 동일, `"${부위} 기준과 ${n}° 차이"`. faultJointDeficits(부호 없음) 용 — 방향 조작(fabricate) 금지 (260704-fwb 거짓 구체성 금지).
- 두 함수 모두 순수 함수 (import 0 추가, 기존 JOINT_LABEL_KO 재사용).

**B. KeypointOverlay.tsx — 절대각 라벨을 행동 지시 라벨로 대체:**

1. prop 신설: `actionLabels?: Partial<Record<KeypointName, string>>` — jsdoc: "quick-260705-k8y — 문제 관절의 행동 지시 문구 (result.tsx 가 실측 데이터로 조립). 라벨은 highlighted(빨강)∪attention(주황) 관절 중 이 맵에 항목이 있는 것만 렌더 — 없으면 마커만 (안전 폴백, Mode3 데이터 부재 대응). 절대각 숫자 라벨은 본 prop 도입으로 전면 제거 (belle: '각도로는 무슨 말인지 못 알아듣는다')."
2. 기존 라벨 블록(459-505행) 수정:
   - 렌더 조건: `[...highlightedJoints, ...attentionJoints]` 중 `actionLabels?.[joint]` 가 truthy 인 관절만. 기존 `pair.current` 의존 제거 (jointAngles 는 강조 산출용으로만 유지).
   - 저신뢰(conf<0.5) 라벨 숨김 가드는 유지.
   - 텍스트 = `actionLabels[joint]` (절대각 `${...}°` 삭제).
   - pill 동적 폭: 순수 헬퍼 `labelTextWidth(text: string): number` — 문자별 한글(`/[가-힣]/`) 14 / 그 외(숫자·°·공백) 8 합산 + 좌우 패딩 16. `labelW = (labelTextWidth(text) * S) / W`. 높이 26·fontSize 14·rx 13·stroke 등 기존 상수 유지 (S 배율 메커니즘 그대로 — 전체화면 2.0 에서 판독).
   - 우측 overflow 클램프: `lx + labelW > 1` 이면 keypoint 왼쪽 배치 `lx = p.x − 14·S/W − labelW`, textAnchor middle 유지 (긴 문구가 화면 밖으로 나가는 것 방지 — 줌 클리핑과 중첩되는 전체화면에서 특히).
3. showAngleLabels prop 은 라벨 전체 on/off 게이트로 의미 유지 (이름 변경 금지 — 호출부 diff 최소화). 주석만 "행동 지시 라벨" 로 갱신.
4. 하드코딩 컬러 추가 금지 (기존 brand/advisoryOrange/#FFFFFF 유지), 이모지 금지, 한국어 why 주석.
  </action>
  <verify>
    <automated>grep -c "composeActionLabelKo" /Users/kimtaesung/Dev/SunityMotion/app/src/lib/deductionLabels.ts | awk '$1>=1' && grep -c "actionLabels" /Users/kimtaesung/Dev/SunityMotion/app/src/components/KeypointOverlay.tsx | awk '$1>=3' && ! grep -q 'Math.round(pair.current)}°' /Users/kimtaesung/Dev/SunityMotion/app/src/components/KeypointOverlay.tsx && cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck</automated>
  </verify>
  <done>composeActionLabelKo/composeDeviationOnlyLabelKo 순수 함수 존재 (계열별 결정적 매핑 + 방향 생략 폴백). KeypointOverlay 가 actionLabels 항목 있는 문제 관절만 동적 폭 pill 로 라벨 렌더, 절대각 라벨 코드 제거. typecheck GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: result.tsx actionLabels 조립·배선 + 검증</name>
  <files>app/src/app/analysis/result.tsx</files>
  <action>
**actionLabels memo 조립 (기존 confirmedKeypoints/attentionKeypoints memo 근처, 575-612행 패턴 따름):**

`const actionLabels = useMemo<Partial<Record<KeypointName, string>>>(...)` — 소스 우선순위 (높은 것이 이김, 이미 채운 keypoint 는 skip):

1. **windowMedianAngleDeltas (mode1 veto applied, signed·최우선):** `result.visionVeto?.status === 'applied'` 일 때 `windowMedianAngleDeltas?.deltas ?? []` 순회 — `Number.isFinite(d.delta_deg)` 필터 → `kp = KEYPOINT_FROM_ANGLE_KEY[d.joint]` → `composeActionLabelKo(d.joint, d.delta_deg)` 가 non-null 이면 등록. (direction 문자열 재파싱 금지 — signed delta 가 direction 의 원천, features.py 188행 정합.)
2. **faultJointDeficits (부호 없음 → 방향 생략 폴백):** applied 시 `faultJointDeficits ?? {}` 순회 — keypoint 키를 angle key 로 역변환할 필요 없이, KEYPOINT_FROM_ANGLE_KEY 의 역맵(로컬 상수 or Object.entries 탐색)으로 angle key 를 구해 `composeDeviationOnlyLabelKo(angleKey, deficit)` 등록. windowMedian 에 없는 확정 관절(split_angle 투영 등) 커버 — 방향 fabricate 금지.
3. **JointScore 폴백 (Mode3 커버):** `joints` 순회 — `Number.isFinite(j.deltaDeg)` 인 관절만 `composeActionLabelKo(j.key, j.deltaDeg)` 등록. deltaDeg 부재 시 currentAngle/targetAngle 로 재계산하지 않는다 (UI 재계산 금지 — 없으면 라벨 없음 = 마커만).

deps: result.visionVeto, joints. 라벨 대상 필터(문제 관절 한정)는 KeypointOverlay 내부 게이트(highlighted∪attention ∩ actionLabels)가 담당 — 여기선 데이터 있는 전 관절 조립 (오버레이 내부 폴백 강조 관절도 라벨을 받도록).

**배선:** leftOverlay 의 KeypointOverlay (세로 카드·전체화면 공용 render prop) 에 `actionLabels={actionLabels}` 추가 + 주석 "quick-260705-k8y — 행동 지시 라벨 (절대각 대체)". rightOverlay(정은지 측)는 jointAngles 미공급·강조 0 이라 무배선 (라벨 자연 미렌더 — diff 0).

**검증:** `cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck` GREEN 확인.

**SUMMARY 박제 (실기기 체크리스트 — belle 확인용):**
- [ ] 가로 뷰어: 인물이 이전보다 크게(1.35배), 천장/바닥 잘림, 마커 정합 유지
- [ ] 줌 크롭 정도 적절 (부족하면 FULLSCREEN_ZOOM 상수만 상향)
- [ ] 문제 관절 라벨 문구 예시 확인 (예: "왼쪽 무릎 23° 더 펴야" / "다리 30° 더 벌려야" / 방향 없으면 "기준과 N° 차이")
- [ ] 정상 관절 = 마커만 (라벨 없음), 절대각 숫자(158° 등) 미노출
- [ ] Mode3 결과 화면에서 크래시/거짓 라벨 없음 (데이터 없으면 마커만)
- [ ] 세로 카드도 같은 라벨 규칙 (문제 관절만)
  </action>
  <verify>
    <automated>grep -c "actionLabels" /Users/kimtaesung/Dev/SunityMotion/app/src/app/analysis/result.tsx | awk '$1>=2' && cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck</automated>
  </verify>
  <done>actionLabels memo 가 3-소스 우선순위로 조립되고 leftOverlay 에 배선됨. rightOverlay 무접촉. typecheck GREEN. SUMMARY 에 실기기 체크리스트 포함.</done>
</task>

</tasks>

<verification>
- `cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck` GREEN (유일한 정적 게이트 — JS 테스트 러너 없음).
- grep 게이트: VideoCompare 에 FULLSCREEN_ZOOM ≥2회, KeypointOverlay 에 actionLabels ≥3회 + 절대각 라벨 코드 부재, deductionLabels 에 composeActionLabelKo 존재, result.tsx 배선.
- 시각 검증은 belle 실기기 (OTA) — SUMMARY 체크리스트로 이관.
</verification>

<success_criteria>
- 전체화면 뷰어 1.35배 줌 + 클리핑 (세로 카드 무접촉), 오버레이 정합 자동 유지.
- 절대각 라벨 전면 제거, 문제 관절만 실측 데이터 기반 행동 지시 라벨 (편차 N° + 방향), 데이터 없으면 마커만.
- 거짓 구체성 0 (부호 없는 소스는 방향 생략 폴백), UI 단 각도 재계산 0, 테마 토큰만, JS-only(OTA), 이모지 0.
</success_criteria>

<output>
Create `.planning/quick/260705-k8y-overlay-action-labels-fullscreen-zoom135/260705-k8y-SUMMARY.md` when done (실기기 체크리스트 포함).
</output>
