---
phase: quick-260705-o0s
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/lib/deductionLabels.ts
  - app/src/types/analysis.ts
  - app/src/components/KeypointOverlay.tsx
  - app/src/components/ScoreBreakdownSection.tsx
  - app/src/app/analysis/result.tsx
autonomous: true
requirements: [QUICK-260705-O0S]

must_haves:
  truths:
    - "결과 화면 섹션 순서: 종합 점수 → 점수 계산 내역 → 영상 비교 → 문제 부위 확대 비교 → 코칭 팁 → 강사에게 확인할 점 → 참고 지표(맨 아래)"
    - "재생 중 오버레이: 감점 record 관절에만 빨간 번호 점(record 순서), 라벨은 각도 숫자 없는 짧은 행동구, 좌+우 동일 행동은 1개로 dedupe"
    - "점수 계산 내역 각 record 행에 영상 점과 같은 번호(①②③) 표기 + 상단에 채점 기준 1줄"
    - "'실패 원인 후보' 섹션이 화면에서 사라짐 (내용은 코칭 팁 '먼저 교정할 점'에 이미 존재)"
    - "참고 지표(구 세부 점수)에 '각도 유사도 (참고)' / '안정성 (참고)' 라벨, 종합점수 혼란 캡션 유지"
    - "감점 0(records 빈 배열) mode1 분석: '보완하면 더 올라가요/거의 다 왔어요' 류 카피 미노출, 축하·유지 카피로 대체"
    - "감점 0 mode1 분석: 문제 부위 확대 비교·'먼저 교정할 점' 미렌더(advisory 만 있어도 숨김), 성공 축하 1줄 섹션 노출"
    - "npm run typecheck GREEN"
  artifacts:
    - path: "app/src/lib/deductionLabels.ts"
      provides: "composeShortActionLabelKo / composeScoringBasisKo / buildDeductionMarkers / isCleanPass 순수 헬퍼"
    - path: "app/src/components/KeypointOverlay.tsx"
      provides: "markerNumbers prop — 빨간 점 내부 흰 숫자 렌더"
    - path: "app/src/components/ScoreBreakdownSection.tsx"
      provides: "recordNumbers/basisLine prop — 번호 표기 + 채점 기준 문구"
    - path: "app/src/app/analysis/result.tsx"
      provides: "섹션 재배치 + 실패원인후보 삭제 + 마커/번호/기준문구 배선 + 감점 0 게이트/축하 섹션 + 조건부 요약 카피"
  key_links:
    - from: "app/src/app/analysis/result.tsx"
      to: "app/src/lib/deductionLabels.ts"
      via: "buildDeductionMarkers 단일 소스 — 오버레이 점 번호와 내역 행 번호가 같은 결과물"
      pattern: "buildDeductionMarkers"
    - from: "app/src/app/analysis/result.tsx"
      to: "app/src/components/KeypointOverlay.tsx"
      via: "markerNumbers prop"
      pattern: "markerNumbers="
    - from: "app/src/app/analysis/result.tsx"
      to: "app/src/components/ScoreBreakdownSection.tsx"
      via: "recordNumbers + basisLine props"
      pattern: "recordNumbers="
    - from: "app/src/app/analysis/result.tsx"
      to: "app/src/lib/deductionLabels.ts"
      via: "isCleanPass 게이트 — 요약 카피·문제-계열 섹션·축하 섹션이 같은 신호 소비"
      pattern: "isCleanPass"
---

<objective>
belle 실기기 3차 피드백(2026-07-05, 캡처 4장, AskUserQuestion 승인) + 추가 피드백 2건 반영 — 결과 화면 정보 구조 재편.

원칙: 영상 위 = 최소 표시(번호 점 + 짧은 행동구), 정보는 화면 여백/보고서로. 47점의 공식 설명(감점 내역)이 첫 화면 주인공. 100점의 공식 설명(감점 0)은 축하가 주인공.

1. 오버레이: 문제 관절(감점 record 관절만)에 빨간 번호 점(①②③, record 순서). 라벨은 각도 숫자 없는 짧은 행동구("다리 더 모으기"). 좌+우 같은 행동 dedupe. 각도 수치는 점수 계산 내역이 담당.
2. 섹션 순서 재배치(승인): ① 종합 점수 → ② 점수 계산 내역(위로, 번호 + 채점 기준 문구) → ③ 영상 비교 → ④ 문제 부위 확대 비교 → ⑤ 코칭 팁 → ⑥ 강사에게 확인할 점 → ⑦ 참고 지표(구 세부점수, 맨 아래 강등).
3. '실패 원인 후보' 섹션 삭제 — 코칭 팁 '먼저 교정할 점'과 중복(실기기 캡처 확인).
4. 채점 기준 문구: 내역 상단 1줄, record 의 deviationSource 로 순수 helper 자동 조립.
5. 세부점수 혼란 해소: "각도 정확도"→"각도 유사도 (참고)", "동작 안정성"→"안정성 (참고)", 섹션명 "세부 점수"→"참고 지표".
6. 점수 조건부 요약 카피(추가 피드백): 감점 0(=100점 정타)이면 "보완하면 더 올라가요/거의 다 왔어요!" 류 카피 금지 → 축하·유지 카피("정은지 선수와 동일한 수준이에요" 급). 보완 카피는 감점 record 있을 때만. 순수 helper 조립.
7. 감점 0 게이트(추가 피드백): records 빈 배열이면 문제-계열 섹션(문제 부위 확대 비교 / '먼저 교정할 점') 전부 숨기고 성공 축하 1줄 섹션 노출. advisory(참고) 카드만 있는 경우도 숨김 — 2° 노이즈 카드가 정확히 그 사례 (실패원인후보는 #3 삭제로 자동 해소).

Purpose: 재생 중 라벨 5개가 영상을 뒤덮는 문제 + 감점 근거가 하단에 묻히는 문제 + 100점에 "보완하라"는 모순 카피 해소. 점수 신뢰 = 도입 (분석 정확도 우선 원칙).
Output: JS-only 표시 레이어 변경 (OTA 배포 가능). 백엔드/계약 무접촉.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@app/CLAUDE.md
@app/src/app/analysis/result.tsx
@app/src/components/KeypointOverlay.tsx
@app/src/components/ScoreBreakdownSection.tsx
@app/src/lib/deductionLabels.ts
@app/src/types/analysis.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 순수 헬퍼 4종 + 차원 라벨 개명 (deductionLabels.ts / types/analysis.ts)</name>
  <files>app/src/lib/deductionLabels.ts, app/src/types/analysis.ts</files>
  <action>
deductionLabels.ts 에 순수 함수 4개 추가 (전부 저장값만 읽음 — UI 재계산/재해석 금지 원칙 유지, 한국어 why 주석 + 근거 인용 관례 준수):

1. `composeShortActionLabelKo(angleKey: string, signedDeltaDeg: number): string | null` — 각도 숫자 없는 짧은 행동구. 기존 composeActionLabelKo 의 방향 의미(features.py _DIR_MORE_BENT/_EXTENDED + kismam 페어 미러)를 그대로 계승하되 `${n}°` 를 제거하고 명사형 종결:
   - elbow/knee: moreBent → `{JOINT_LABEL_KO[angleKey]} 더 펴기` / 아니면 `{...} 더 굽히기`
   - hip: `다리 더 벌리기` / `다리 더 모으기` (좌우 구분은 마커 위치가 전달 — 기존 관례)
   - shoulder: `팔 더 벌리기` / `팔 더 모으기` (겨드랑이 벌림 의미, ANGLE_MEANING_KO 정합)
   - 가드 유지: `Math.round(Math.abs(delta)) < 1` → null (1° 급 미미 편차 라벨 생략), 미등록 angleKey → null, 비유한 → null.
   belle 인용: "각도를 여기서 하기보단 '다리를 더 모으세요' 등으로만. 각도는 세부 점수에서 어차피 나옴."

2. `composeScoringBasisKo(records: DeductionRecord[]): string | null` — 점수 계산 내역 상단 채점 기준 1줄 자동 조립. 하드코딩 조건 분기를 이 순수 helper 한 곳에 격리:
   - records 중 deviationSource === 'reference_relative' 존재 → "세계챔피언 정은지 선수 시연 대비 편차" 파트
   - deviationSource === 'ipsf_absolute' 존재 → "IPSF 국제심사 기준" 파트
   - 둘 다 → " + " 로 병기, 하나만 → 단독, 둘 다 없음(records 빈 배열/fallback 만) → null (표기 생략).
   - 문장 마무리: "...기준으로 감점을 계산했어요." 톤 (기존 scoreCaption 관례). 작성 전 types/analysis.ts 의 DeductionRecord 필드를 확인하고 deviationSource 를 1차 신호로 사용 (ipsfAnchor 류 필드가 있으면 주석에 언급만 — 신호 이중화 금지).

3. `buildDeductionMarkers(records: DeductionRecord[], faultJoints: readonly KeypointName[] | undefined): { recordNumbers: (number | null)[]; keypointNumbers: Partial<Record<KeypointName, number>> }` — 영상 점 ↔ 내역 행 번호 매핑의 단일 출처 (quick-260704-fz4 confirmedKeypoints 와 같은 "표·마커 동일 소스" 패턴):
   - record 순회(저장 순서 그대로, 재정렬 금지). 각 record 의 관절 투영:
     a. criterion 이 ANGLE_VS_REFERENCE_PREFIX 시작 → KEYPOINT_FROM_ANGLE_KEY[jk] 단일 keypoint
     b. 관절명 없는 vision record(split_angle 등, source==='vision') → faultJoints 전체로 투영 (CONTEXT locked 선례 — fz4 confirmedKeypoints 와 동일 규칙)
     c. criterion === 'dimension_overall_fallback' 또는 unit === 'score_delta' → 투영 없음
   - 번호는 "새 keypoint 를 최소 1개 확보한 record" 에만 1부터 순차 부여. 이미 앞 record 가 번호를 붙인 keypoint 는 첫 번호가 이긴다(first-wins — 점 하나에 숫자 하나). 투영 실패/전부 선점된 record 는 recordNumbers[i] = null (내역 행에 번호 없이 표기 — 정직, fabricate 0).
   - 같은 record 가 여러 keypoint 로 투영되면(스플릿 → 양쪽 hip 등) 전부 같은 번호 (같은 감점의 시각 분산 — 거짓 아님).

4. `isCleanPass(breakdown: DeductionBreakdown | null | undefined): boolean` — 감점 0 게이트의 단일 신호 (belle 추가 피드백 #2). `breakdown != null && breakdown.records.length === 0` 일 때만 true. breakdown 부재(legacy doc/mode3) → false (게이트 미발동 = 기존 렌더 유지, 판단 불가 시 보수적). 주석에 명시: dimension_overall_fallback record 도 실감점이므로 records 존재 = clean 아님; advisory(주황, 감점 아님)는 records 에 없으므로 advisory-only 영상은 자동으로 clean — belle 의도(2° 노이즈 카드 숨김)와 정합.

types/analysis.ts: DIMENSION_LABEL_KO 의 angle 라벨 "각도 정확도" → "각도 유사도", stability "동작 안정성" → "안정성" 으로 개명 (측정 실체에 맞는 진실한 개명 — angle 차원은 DTW 유사도임). " (참고)" 접미는 결과 화면 렌더에서만 붙인다(Task 3) — 기록 탭/차트 등 다른 소비처가 접미로 오염되지 않게. 개명 전 `grep -rn "DIMENSION_LABEL_KO" app/src` 로 소비처 목록을 확보하고 SUMMARY 에 기록 (표시 문자열만 변경, 계약 키 무접촉 — contract.md 미러 대상 아님을 주석으로 명시).

기존 composeActionLabelKo / composeDeviationOnlyLabelKo 는 이 태스크에서 삭제하지 않는다 (소비처 전환은 Task 3, 삭제도 Task 3 grep 게이트 후).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck</automated>
  </verify>
  <done>4개 헬퍼가 export 되고, DIMENSION_LABEL_KO 개명 완료, typecheck GREEN. buildDeductionMarkers 는 record 순서·first-wins·vision 투영 규칙을, isCleanPass 는 advisory-only=clean 규칙을 주석으로 명문화.</done>
</task>

<task type="auto">
  <name>Task 2: KeypointOverlay 번호 점 + ScoreBreakdownSection 번호/기준문구 prop</name>
  <files>app/src/components/KeypointOverlay.tsx, app/src/components/ScoreBreakdownSection.tsx</files>
  <action>
KeypointOverlay.tsx:
- 신규 prop `markerNumbers?: Partial<Record<KeypointName, number>>` (result.tsx 의 buildDeductionMarkers.keypointNumbers). prop 미전달 시 렌더 diff 0 (하위호환 — Mode3/legacy doc 은 기존 highlightKeypoints/편차 폴백 그대로).
- 렌더: highlighted(빨강) 원 위에 markerNumbers[joint] 가 있으면 원 중앙에 흰색 bold 숫자 SvgText 를 그린다. fontSize ≈ (13 * S) / H, textAnchor middle, 세로 중앙 보정(y = cy + fontSize*0.35 근사 — 기존 pill 텍스트 0.68 배치 관례 참고). 원문자 유니코드 대신 SVG 숫자 렌더 (작은 원 안 가독 — 원 자체가 ① 역할). sizeScale(S) 곱으로 세로 카드/가로 전체화면 동일 규칙 자동 (quick-260702-t0v 메커니즘 재사용).
- 숫자 있는 관절은 RADIUS_HI 강조 크기 보장 (markerNumbers 키는 caller 가 highlightKeypoints 에도 포함해 전달하므로 기존 emphasized 분기로 충족 — 오버레이 내부에서 set 을 재구성하지 않는다, 이중 소스 금지).
- 라벨 렌더 게이트/pill 메커니즘 무변경 — actionLabels 내용이 짧아지는 것은 caller 책임. 절대각 라벨은 quick-260705-k8y 에서 이미 제거됨.

ScoreBreakdownSection.tsx:
- 신규 optional prop `recordNumbers?: (number | null)[]` (records 인덱스 정렬) + `basisLine?: string | null`.
- basisLine 이 있으면 카드 최상단(기준 점수 행 위)에 caption 톤 1줄 렌더 (typography.caption + textSecondary, 토큰만).
- record 행: recordNumbers[i] 가 숫자면 recordLabel 앞에 원문자 텍스트 접두 — 1..9 는 '①②③④⑤⑥⑦⑧⑨' 매핑, 초과분은 `(n)` 폴백 (constraints: UI 원문자 텍스트 OK, 이모지 아님). 접두 원문자는 brand 색 Text 로 분리해 "영상의 빨간 점과 같은 번호" 시각 연결. null 이면 접두 없음 (기존 표기 그대로).
- accessibilityLabel 에 번호 포함 (예: "1번 다리 스플릿 각도 감점 12점").
- record 목록 아래(합계 행 위)에 안내 각주 1줄: 번호가 1개 이상일 때만 "번호는 위 영상의 빨간 점 위치와 같아요." (styles.footnote 재사용). 왜: 번호 매핑은 설명 없이는 발견되지 않는 규칙.
- 두 prop 모두 미전달 시 렌더 diff 0 (다른 소비처/legacy 무회귀). records 빈 배열의 기존 "측정 감점 없음 — 기준 점수 그대로예요." 카피는 유지 (감점 0 케이스의 내역 설명 — Task 3 축하 섹션과 상보).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck</automated>
  </verify>
  <done>markerNumbers 로 빨간 점 안에 흰 숫자가 렌더되고(S 배율 반영), ScoreBreakdownSection 이 원문자 번호 접두 + 채점 기준 1줄 + 매핑 각주를 렌더. 두 컴포넌트 모두 prop 미전달 시 기존과 동일 렌더.</done>
</task>

<task type="auto">
  <name>Task 3: result.tsx 재배치 + 실패원인후보 삭제 + 감점 0 게이트/축하 + 조건부 카피 + 배선</name>
  <files>app/src/app/analysis/result.tsx, app/src/lib/deductionLabels.ts</files>
  <action>
result.tsx 를 다음 순서로 재구성 (승인된 7개 + 미언급 섹션의 상식 배치 — 재량 판단, SUMMARY 에 근거 기록):

  header + mode1 기준 모션 메타 카드(유지) → ① 종합 점수 카드(억제 분기 포함) → 부상 위험 InjuryRiskSection(안전 신호는 점수 직후 유지) → [감점 0 시: 성공 축하 섹션] → ② 점수 계산 내역(showBreakdownSection 가드 그대로, 위로 이동) → 구간별 점수(콤보 시, 점수 상세 계열이라 내역 직후) → ③ 동작 비교(영상 + 오버레이 토글) → ④ 문제 부위 확대 비교(FaultZoomCompare, 감점 0 시 숨김) → ⑤ 코칭 팁('먼저 교정할 점' lead 카드는 감점 0 시 숨김) → ⑥ 강사에게 확인할 점 → 보완 운동(행동 콘텐츠라 참고 지표 위 유지) → ⑦ 참고 지표(구 세부 점수, 맨 아래 강등) → CTA.

'실패 원인 후보' 섹션 완전 삭제:
- 섹션 JSX(sectionTitle '실패 원인 후보' + ForcePatternCard big/small row) 제거.
- 죽는 코드 연쇄 제거: forcePatternFindings memo, measuredEvidence memo, detailFinding state, ForcePatternDetailModal 렌더, ForcePatternCard/_FALLBACK_BODY/ForcePatternDetailModal/MeasuredEvidence import.
- 유지: openQuestionsForCoach 는 forcePatternInference.coachCommentHook 을 계속 소비 (다른 데이터 경로 — 삭제 금지).
- 흡수 점검: 자세히 시트 고유 정보였던 측정 방법 문구는 신규 채점 기준 문구 + 내역 detailText 가, 가능한 원인(rootCauseHypotheses)은 '먼저 교정할 점' vetoRootCauses 가, 관절별 현재→기준 각도는 코칭 팁 angleGuide 가 이미 커버 — 실제 유실 정보가 발견되면 코칭 팁 lead 카드로 흡수하고 SUMMARY 에 기록.
- `grep -rln "ForcePatternCard\|ForcePatternDetailModal" app/src` 로 잔여 소비처 확인 — result.tsx 가 유일 소비처였으면 두 컴포넌트 파일 삭제 (슬롭 잔존 금지).

감점 0 게이트 + 성공 축하 (belle 추가 피드백 #2 — 100점에 2° 노이즈 확대카드/"이슈 신호 없음" 카드 노출 모순):
- `cleanPass = isCleanPass(cmp.mode === 'mode1' ? result.deductionBreakdown : null)` 단일 신호 (Task 1 helper). mode3/legacy doc 은 false → 기존 렌더 무회귀.
- cleanPass 시 숨김: FaultZoomCompare 전체(advisory tier 카드 포함 — 감점 0 이면 참고 카드도 숨김이 belle 의도, 2° 카드가 정확히 그 사례), '먼저 교정할 점' veto lead 카드(감점 0 이면 veto applied 일 수 없지만 방어 게이트로 명시). 실패원인후보는 위 삭제로 자동 해소.
- cleanPass 시 노출: 성공 축하 1줄 섹션 — 점수 카드 바로 아래 brandTint 카드(refCard/vetoLeadCard 스타일 패턴 차용, 토큰만) 1~2줄: "감점 항목이 없어요 — 이 자세를 그대로 유지하세요." 톤. 시나리오 문서의 '+α 성공 순간 축하' 최소 구현. 이모지 0.
- 점수 계산 내역은 cleanPass 여도 렌더 유지 ("측정 감점 없음" 행이 100점의 공식 근거 — 투명성 원칙).

점수 조건부 요약 카피 (belle 추가 피드백 #1 — "100점인데 보완하면 더 올라가요는 너무하다"):
- mode1Summary: cleanPass 분기 추가 — cleanPass 면 "거의 다 왔어요!" 류 대신 축하·유지 카피("정은지 선수와 동일한 수준이에요. 이 자세를 유지하세요." 급). 감점 있을 때만 기존 티어 카피. 순수 함수 유지 (cleanPass 를 파라미터로 받음 — 컴포넌트 밖 로컬 helper 로 조립).
- ScoreContext: cleanPass prop 추가 — cleanPass 면 "...보완하면 더 올라가요" 조립 금지, correctionPoint 무시하고 "정은지 기준 {score}점 — 감점 항목 없이 통과했어요." 급 카피. "보완하면 더 올라가요" 는 감점 record 있을 때만 (belle: "텍스트는 분석마다 달라져야지").
- correctionPoint 소스(vetoPrimaryFault ?? tips[0]?.title)는 무변경 — 카피 조립 단계에서만 게이트.

번호/라벨 배선:
- `markers = useMemo(buildDeductionMarkers(result.deductionBreakdown?.records ?? [], vetoFaultJoints))`.
- KeypointOverlay(left): markerNumbers={markers.keypointNumbers} 추가. highlightKeypoints 는 기존 confirmedKeypointList 유지 (markers 키는 confirmedKeypoints 부분집합 — 규칙 동일 소스라 자동 정합).
- ScoreBreakdownSection: recordNumbers={markers.recordNumbers}, basisLine={composeScoringBasisKo(records)}.
- actionLabels 재조립 (핵심 변경 — 라벨 5개 영상 뒤덮기 해소):
  - mode1 + breakdown 보유: markers.keypointNumbers 에 있는 관절만 라벨 후보. 문구는 composeShortActionLabelKo — signed delta 소스 우선순위 기존 유지(windowMedianAngleDeltas → JointScore.deltaDeg). faultJointDeficits 밖에 없는 관절(부호 없음)은 라벨 없이 번호 점만 (방향 fabricate 금지, quick-260704-fwb — 번호가 내역 행으로 안내하므로 정보 손실 아님). composeDeviationOnlyLabelKo 경로 폐기.
  - dedupe: 조립 후 동일 문자열 라벨이 2개 관절에 붙으면(hip 좌우 "다리 더 모으기" 등) |delta| 큰 쪽만 라벨 유지, 나머지는 번호 점만.
  - attention(주황) 관절은 라벨 미부여 (감점 아님 — 점만, 영상 위 최소 표시 원칙).
  - mode3/legacy(breakdown 없음): 기존 소스 순서 유지하되 composeShortActionLabelKo 로 문구만 교체 (각도 숫자 전면 제거, dedupe 동일 적용).
  - cleanPass 시 markers/actionLabels 는 자연히 빈 결과 (records 빈 배열) → 오버레이는 뼈대만, 별도 분기 불요 — 확인만.
- 참고 지표 섹션: sectionTitle '세부 점수' → '참고 지표'. angle/stability 행 라벨에 ' (참고)' 접미 (DimensionScoreRow 에 접미 표시용 소품 추가 또는 렌더 시 라벨 조립 — 다른 화면 오염 0). auxCaption(안정성 보조 지표 설명)과 reframeVeto/contextNote 캡션 유지. occlusion badge 유지.
- 문구 정합 fix: contextNote 의 "아래 '점수 계산 내역'에서" → 재배치 후 내역이 참고 지표보다 위 → "위 '점수 계산 내역'에서" 로 수정.
- deductionLabels.ts 정리: 소비처 전환 후 `grep -rn "composeActionLabelKo\|composeDeviationOnlyLabelKo" app/src | grep -v deductionLabels.ts` 가 0줄이면 두 함수 삭제.

SUMMARY 필수 포함 (belle 질책 재발 방지 — constraints): 시뮬레이션 서술 2건 —
(a) 최악 케이스(record 4개 + advisory 2관절) 재생 중 화면: 세로 카드와 가로 전체화면(sizeScale 2) 각각에서 빨간 번호 점 몇 개(①~④, split 투영 시 같은 번호 복수 점 포함), dedupe 후 라벨 몇 개와 각 문구/pill 폭 추정(labelTextWidth 근사치), 주황 점(라벨 없음) 몇 개, 내역 행 ①~④ 대응 — 이전(라벨 5개 + 각도 숫자) 대비 정량 비교.
(b) 감점 0 케이스(정은지 정타 100점): 노출 섹션 목록(축하 섹션 + "측정 감점 없음" 내역 + 영상 비교 + 코칭 팁 잔여 + 참고 지표)과 숨김 섹션 목록(확대 비교/먼저 교정할 점), 요약 카피 실제 문자열 — "보완하면 더 올라가요/거의 다 왔어요" 미출현 확인.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck && test "$(grep -rl '실패 원인 후보' src | wc -l | tr -d ' ')" = "0" && test "$(grep -rn 'composeDeviationOnlyLabelKo' src | grep -v deductionLabels.ts | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <done>승인된 섹션 순서로 렌더 트리 재배치, '실패 원인 후보' 및 연쇄 죽은 코드 0, 오버레이 번호 점 + 짧은 행동구(dedupe) + 내역 번호/기준문구 배선 완료, 감점 0 시 문제-계열 섹션 숨김 + 축하 섹션 + 축하 카피(보완 카피 미출현), '참고 지표' 개명 + (참고) 접미 + 맨 아래 배치, typecheck GREEN, SUMMARY 에 최악 케이스·감점 0 케이스 시뮬레이션 서술 포함.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firestore doc → 앱 렌더 | 기존 경계 그대로 — normalize() 방어 정규화가 malformed 를 undefined 로 접음 (신규 필드 없음, 저장된 record/veto 값 읽기만) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-o0s-01 | Tampering | buildDeductionMarkers/isCleanPass 입력 (records/faultJoints) | mitigate | 저장값 read-only + Number.isFinite/키 존재 가드 — 재계산/재해석 0 (기존 원칙 유지) |
| T-o0s-02 | Information Disclosure | 오버레이/내역 표기 | accept | 표시 전용, 신규 데이터 수집·전송 0, 백엔드/계약 무접촉 |
| T-o0s-SC | Tampering | 패키지 설치 | accept | 신규 패키지 설치 없음 (기존 의존성만) |
</threat_model>

<verification>
- `cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck` GREEN (유일한 정적 게이트 — 앱에 JS 테스트 러너 없음)
- grep 게이트: '실패 원인 후보' 0건, degree 라벨 헬퍼 소비처 0건(삭제 후)
- 코드 정독 검증: 섹션 렌더 순서가 승인 순서와 일치, ScoreBreakdownSection/KeypointOverlay prop 미전달 경로(legacy doc/mode3)가 기존 렌더와 동일, cleanPass=false 경로(감점 있는 doc)의 렌더가 게이트 도입 전과 동일
- SUMMARY 시뮬레이션 서술 2건 존재: 최악 케이스(record 4 + advisory 2) 재생 화면 + 감점 0 케이스(100점 정타) 섹션/카피
</verification>

<success_criteria>
- belle 승인 5개 항목 + 추가 피드백 2건 전부 반영: 번호 점 + 각도 없는 행동구 + dedupe / 섹션 재배치 / 실패원인후보 삭제 / 채점 기준 문구 / 참고 지표 개명·강등 / 조건부 요약 카피 / 감점 0 게이트 + 축하 섹션
- 영상 점 번호 ↔ 점수 계산 내역 행 번호가 buildDeductionMarkers 단일 소스에서 나와 항상 일치
- 감점 0 판정이 isCleanPass 단일 소스 — 요약 카피·섹션 게이트·축하 섹션이 같은 신호 소비 (분기 산개 금지)
- 세로 카드/가로 전체화면 동일 규칙 (sizeScale 메커니즘 재사용)
- JS-only, 테마 토큰만, 이모지 0, 백엔드 무접촉, typecheck GREEN
</success_criteria>

<output>
Create `.planning/quick/260705-o0s-result-screen-reorder-numbered-markers/260705-o0s-SUMMARY.md` when done.
SUMMARY 에 반드시 포함: (1) 최악 케이스(record 4 + advisory 2, 재생 중) 세로/가로 화면 시뮬레이션 서술, (2) 감점 0 케이스(100점 정타) 노출/숨김 섹션 + 요약 카피 시뮬레이션, (3) DIMENSION_LABEL_KO 개명의 타 화면 소비처 목록, (4) 실패원인후보 시트 정보 흡수 점검 결과.
</output>
