---
phase: quick-260705-r6v
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/lib/deductionLabels.ts
  - app/src/components/KeypointOverlay.tsx
  - app/src/components/VideoCompare.tsx
  - app/src/components/ScoreBreakdownSection.tsx
  - app/src/components/DeductionDetailSheet.tsx
  - app/src/components/FaultZoomCompare.tsx
  - app/src/app/analysis/result.tsx
autonomous: true
requirements: [QUICK-260705-R6V]

must_haves:
  truths:
    - "가로 전체화면 뷰어 재생 중 영상 위에는 빨간 번호 점만 보인다 (텍스트 pill 0개)"
    - "스플릿(vision) 감점은 다리 4관절 각각이 아니라 다리 사이 중점 1개 번호 점으로 표시된다"
    - "가로 뷰어 위쪽 검은 여백에 '① 행동구 −감점' 고정 범례가 보인다"
    - "재생바에 결함 측정 시점 틱이 보이고, 탭하면 양쪽 영상이 그 시점으로 동기 seek 된다"
    - "메인 화면에서 '문제 부위 확대 비교' 섹션이 사라지고, 점수 계산 내역 행 탭 시 확대사진+수치+행동구 드릴다운 시트가 열린다"
    - "참고 지표 섹션이 숫자 카드 대신 감점 유무 x 지표값 구간 조건부 진단 문장 2줄을 보여준다 (mode1)"
    - "감점 0/legacy/mode3 doc 에서 크래시나 거짓 문장 없이 graceful 렌더된다"
  artifacts:
    - path: "app/src/lib/deductionLabels.ts"
      provides: "buildDeductionMarkers 그룹 마커 확장 + buildDeductionTicks + composeDimensionDiagnosisKo 순수 helper"
    - path: "app/src/components/DeductionDetailSheet.tsx"
      provides: "감점 record 드릴다운 시트 (확대사진 + 수치 + 행동구)"
      min_lines: 80
    - path: "app/src/components/VideoCompare.tsx"
      provides: "전체화면 범례 + 타임라인 틱 렌더/seek"
  key_links:
    - from: "app/src/app/analysis/result.tsx"
      to: "app/src/lib/deductionLabels.ts"
      via: "buildDeductionMarkers/buildDeductionTicks/composeDimensionDiagnosisKo import"
      pattern: "buildDeductionTicks|composeDimensionDiagnosisKo"
    - from: "app/src/components/VideoCompare.tsx"
      to: "seekBoth"
      via: "틱 탭 → 양쪽 동기 seek"
      pattern: "seekBoth"
    - from: "app/src/components/ScoreBreakdownSection.tsx"
      to: "DeductionDetailSheet"
      via: "onRecordPress(recordIndex) 콜백 → result.tsx 시트 오픈"
      pattern: "onRecordPress"
---

<objective>
belle 실기기 4차 피드백 + 해외 사례 리서치(Sportsbox/HomeCourt/Tempo/Frame.io 챕터 점프) 수렴 설계 3건을 결과 화면에 배선한다 (belle 승인 2026-07-05):

1. **가로 뷰어 v3** — 재생 중 영상 위 표시 최소화: 번호 점만(pill 전면 제거), 스플릿은 다리 사이 중점 1개 점, 행동 문구는 위 검은 여백 고정 범례, 재생바에 결함 측정 시점 틱(탭 = 동기 seek).
2. **확대사진 드릴다운 이동** — 메인 '문제 부위 확대 비교' 섹션 제거, 점수 계산 내역 행/범례/번호 점 탭 → 해당 감점의 [내 확대사진 | 정은지 확대사진] + 수치 + 행동구 시트.
3. **참고 지표 → 진단 문장 전환** — 숫자 카드 제거, isCleanPass x 지표값 구간 조건부 문장 2줄. 숫자는 '자세히' 모달로 이동.

Purpose: Tempo 공식 문서("폼 피드백 상시 노출은 demoralizing") 계열 UX — 재생 중엔 위치만, 설명은 여백/드릴다운으로. "번호 다 1" 혼란과 "각도 유사도 99 인데 47점" 모순 카피 해소.
Output: JS-only OTA 변경 (백엔드/계약 무접촉), typecheck GREEN.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@app/CLAUDE.md
@app/src/lib/deductionLabels.ts
@app/src/components/KeypointOverlay.tsx
@app/src/components/VideoCompare.tsx
@app/src/components/ScoreBreakdownSection.tsx
@app/src/components/FaultZoomCompare.tsx
@app/src/app/analysis/result.tsx
@app/src/types/analysis.ts
</context>

<planner_findings>
계획 시점에 소스에서 확정한 사실 — 실행자는 재탐색 없이 그대로 사용:

1. **"번호 다 1" 근본원인**: `buildDeductionMarkers` (deductionLabels.ts:166) 가 관절명 없는 vision record(split_angle)를 `faultJoints` 전체(다리 4관절)에 같은 번호로 투영 → 점 4개 전부 "1".
2. **오버레이 터치 차단**: VideoCompare 의 `overlayContainer` 가 `pointerEvents="none"` (styles:827, 전체화면은 renderFullscreenSlot 인라인) — 번호 점 탭에는 `box-none` 전환 + 히트 레이어 필요. 영상 본체엔 제스처 핸들러가 없어 box-none 무해.
3. **iOS 중첩 Modal 함정**: 전체화면 뷰어가 이미 Modal — 그 위에 sibling Modal(시트)을 띄우면 iOS 에서 표시 실패 가능. 전체화면에서의 드릴다운은 **closeFullscreen 후 시트 오픈** 시퀀스로 회피.
4. **actionLabels 게이트 연쇄**: result.tsx actionLabels memo(672행)가 `markers.keypointNumbers[kp] == null → skip` 게이트 — 스플릿 멤버 관절이 그룹 마커로 이동해 개별 번호를 잃으면 스플릿 행동구가 소멸. 게이트를 그룹 멤버까지 확장해야 범례/시트 행동구가 산다.
5. **틱 시점 데이터**: `VisionWindowMedianAngleDeltas.sourceFrameIndices.user: number[]` (analysis.ts:386) — window 공유 단일 시점(레코드별 아님). 초 환산 = KeypointOverlay Fix-A 와 동일 규칙 `sec = frameIndex * duration / keypointReport.frames` (라벨 fps 불신). 현 계약상 틱은 사실상 1개 — 구조는 record별 배열로 잡되 같은 시점은 번호 병합.
6. **확대 이미지는 좌우 합성 1장**: `FaultZoomComparison.imageUrl` = [학생|기준] 합성 PNG 1장 — 시트는 이미지 1장 + 좌/우 라벨 오버레이(FaultZoomCompare 의 halfLabel 패턴 이식)로 충분.
7. **record↔zoom 매칭 키**: record 투영 keypoint ∩ (zoom.joint 단일 또는 `REGION_MEMBER_KEYPOINTS[zoom.region]`). tier='confirmed' 우선, 없으면 advisory 도 허용하지 않음(감점 시트에 '감점 아님' 카드 오매칭 금지) — 매칭 실패 시 사진 없이 수치·문구만 (graceful).
8. **수치 문구 재사용**: "기준 대비 N° 차이 (허용 20° 초과 M°)" 는 `formatDeductionRecord(record).detailText` 가 이미 조립 — 시트에서 그대로 재사용, 신규 수치 조립 금지.
9. **FaultZoomCompare 소비처는 result.tsx 단 1곳** — 섹션 제거 후 컴포넌트 파일 자체를 삭제해도 안전 (자산은 시트로 이식).
10. **참고 지표 대상 차원**: 문장 전환은 angle/stability 만 (belle 예시 "각도 유사도 99/안정성 98"). line 차원(존재 시)과 mode3 경로(성장 델타 행)는 무변경 — mode3 는 cleanPass 신호 자체가 없고(breakdown=mode1 전용) 델타 표시가 성장 추적의 실체.
</planner_findings>

<tasks>

<task type="auto">
  <name>Task 1: deductionLabels 순수 helper 계층 — 그룹 마커 / 타임라인 틱 / 진단 문장</name>
  <files>app/src/lib/deductionLabels.ts</files>
  <action>
전부 순수 함수 (numpy/네트워크/React 무관, 저장값 read-only — 재계산·재해석 0). 한국어 why 주석 + 이 quick ID(quick-260705-r6v) 표기. 이모지 금지.

**1-A. `buildDeductionMarkers` 그룹 마커 확장** — 반환 shape 에 `groupMarkers: { number: number; keypoints: KeypointName[] }[]` 추가.
- 규칙 변경: 관절명 없는 vision record 가 **2개 이상** keypoint 로 투영되면(스플릿 → 다리 4관절) 멤버 각각에 `keypointNumbers` 를 찍지 않고 groupMarkers 에 1 entry(번호 1개 + 멤버 배열)로 방출. 멤버는 점유(first-wins) 처리해 뒤 record 가 재번호 못 붙이게 유지.
- 1개 keypoint 투영 record(angle_vs_reference__{jk} 등)는 기존 `keypointNumbers` 그대로 (무회귀).
- `recordNumbers` 의 번호 부여 순서/규칙은 불변 — 그룹 record 도 번호는 받는다 (내역 행 ①과 중점 점 ①이 일치해야 함).
- 기존 소비처(하위호환): groupMarkers 를 무시해도 컴파일 되는 additive 확장.

**1-B. `buildDeductionTicks(records, recordNumbers, visionVeto)` 신설** — 재생바 결함 틱 데이터.
- 반환: `{ numbers: number[]; frameIndex: number }[]` (frame 도메인 — 초 환산은 VideoCompare 가 duration 으로 수행, planner_findings 5).
- 시점 소스: `visionVeto.status === 'applied'` 이고 `windowMedianAngleDeltas.sourceFrameIndices.user` 가 비어있지 않을 때만 — user 배열의 **median** (정렬 후 중앙값, 짝수면 낮은 쪽 — 결정적). 그 median frame 1개에, 번호가 부여된(`recordNumbers[i] != null`) record 전부의 번호를 오름차순 병합해 틱 1개로 방출.
- 데이터 없으면(veto 미적용/legacy/mode3) 빈 배열 — 틱 생략 (fabricate 0).
- 구조를 배열로 잡는 이유(record별 시점이 백엔드에 생기면 틱 분리 확장) 주석 명기.

**1-C. `composeDimensionDiagnosisKo(dim, cleanPass, value)` 신설** — 참고 지표 진단 문장 (belle 승인 카피 그대로, 임의 수정 금지).
- `dim: 'angle' | 'stability'`, `cleanPass: boolean`, `value: number` (저장된 dimension 점수).
- 값 구간 상수 `DIMENSION_DIAGNOSIS_HIGH_THRESHOLD = 90` 선언 (belle "유사도 <90 이면 위 문장이 거짓" — 조건 분기 필수).
- angle (흐름):
  - value < 90: `'동작 흐름 자체가 기준과 차이가 있어요. 전체 동작을 기준 영상과 비교하며 따라가 보세요.'` (낮은 값에 "기준대로 타고 있어요" 거짓 문장 차단)
  - value >= 90 && !cleanPass: `'동작 전체 흐름은 기준대로 타고 있어요 — 감점은 특정 순간의 자세에서 나왔어요. 위 항목만 교정하면 됩니다.'`
  - value >= 90 && cleanPass: `'동작 흐름과 순간 자세 모두 기준과 일치해요.'`
- stability (안정성):
  - value < 90: `'버티는 구간에서 흔들림이 있어요. 자세 교정과 함께 버티는 힘을 길러 보세요.'`
  - value >= 90 && !cleanPass: `'떨림 없이 버티는 힘은 좋아요 — 자세를 교정하면 그대로 안정된 자세로 정착할 수 있어요.'` (틀린 자세로 안정 = 자산 재프레임, belle 승인)
  - value >= 90 && cleanPass: `'버티는 구간도 흔들림 없이 유지했어요.'`
- value 가 비유한이면 null 반환 (호출부가 행 생략).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck</automated>
  </verify>
  <done>buildDeductionMarkers 가 groupMarkers 를 방출하고(다중 keypoint vision record 는 keypointNumbers 미기록), buildDeductionTicks/composeDimensionDiagnosisKo 가 export 되며 typecheck GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: 뷰어 v3 — pill 제거 + 중점 점 + 여백 범례 + 타임라인 틱 + result.tsx 배선</name>
  <files>app/src/components/KeypointOverlay.tsx, app/src/components/VideoCompare.tsx, app/src/app/analysis/result.tsx</files>
  <action>
**2-A. KeypointOverlay — 텍스트 pill 전면 제거 + 그룹(중점) 마커 + 탭 히트 레이어.**
- 행동 지시 라벨 렌더 블록(`showAngleLabels && [...highlightedJoints, ...attentionJoints].map(...)` 의 Rect+SvgText pill)과 `labelTextWidth`, `actionLabels`/`showAngleLabels` prop 을 **삭제** (Tempo "폼 피드백 상시 노출 demoralizing" — 영상 위 = 점만). 행동구 데이터 자체는 result.tsx memo 로 존속 (범례/시트 소비).
- 신규 prop `groupMarkers?: { number: number; keypoints: KeypointName[] }[]`: 각 entry 를 멤버 keypoint 들의 현재 frame 위치 **산술 평균(centroid)** 1점에 강조 원(RADIUS_HI + 흰 외곽선, brand fill) + 중앙 흰 bold 숫자로 렌더 — 기존 markerNumbers 숫자 점과 동일 시각 규칙(sizeScale 곱 포함). centroid 는 표시 배치용 평균이지 좌표/각도 산출이 아님을 주석 명기 (D-12 §12 안티패턴 아님 — 백엔드 좌표의 시각 배치). 멤버 중 위치 없는 관절은 제외, 유효 멤버 0이면 미렌더. 저신뢰(conf<0.5) 멤버는 centroid 계산에서 제외하되 나머지로 진행.
- 스플릿 멤버 관절(다리 4관절)은 highlightKeypoints 로 계속 빨강 강조(bone 포함 — 결함 부위 시각화 유지)하되 개별 숫자는 없음: 숫자는 중점 점 1개만 ("번호 다 1" 해소, Claude 재량 — 번호 혼란이 문제였지 빨강 강조가 문제가 아니었음).
- 신규 prop `onMarkerPress?: (number: number) => void`: 전달 시 SVG 위에 형제 레이어(absolute fill, `pointerEvents="box-none"`)를 추가하고, 숫자 있는 마커(keypointNumbers 관절 + groupMarkers centroid) 위치에 `left/top` 퍼센트 문자열(정규화 좌표 x100)로 Pressable(고정 44pt 히트 박스, 중앙 정렬 마진 보정, `accessibilityLabel="{n}번 감점 상세 보기"`)을 배치. prop 미전달 시 렌더 diff 0. 최상위 View 의 `pointerEvents` 는 `onMarkerPress` 유무로 'box-none'/'none' 분기.

**2-B. VideoCompare — 오버레이 터치 개방 + 여백 범례 + 재생바 틱.**
- `overlayContainer` 및 전체화면 슬롯의 오버레이 래퍼 `pointerEvents` 를 `"none"` → `"box-none"` 전환 (planner_findings 2 — 영상 본체 제스처 없음, 무해. 이유 주석).
- 신규 prop `fullscreenLegend?: { number: number; text: string }[]`: 전체화면 위쪽 검은 여백(fsTopBar 좌측 flex:1 영역)에 고정 범례 렌더 — entry 를 가로 나열(row + flexWrap, 여백 좁으면 1줄 가로 나열 허용이 승인 사항). 각 entry = brand 원문자 번호 + 흰 텍스트 (FULLSCREEN_TEXT_SCALE 파생 크기, 토큰만). 원문자는 ScoreBreakdownSection 과 동일 규칙 — CIRCLED_DIGITS 로직을 deductionLabels 로 승격(export `circledNumberKo`)해 양쪽이 import (중복 2벌 금지).
- 신규 prop `onLegendPress?: (number: number) => void`: 범례 entry Pressable — 호출 전 `closeFullscreen()` 먼저 실행 후 콜백 (iOS 중첩 Modal 함정 회피, planner_findings 3. 주석 명기).
- 신규 prop `timelineTicks?: { numbers: number[]; frameIndex: number }[]` + `tickFrameCount?: number` (사용자 keypointReport.frames): renderControls 의 timeline 영역에서 `sec = frameIndex * duration / tickFrameCount` (Fix-A 실효 fps 규칙, tickFrameCount<=0 이면 틱 생략), `left = (sec/duration)*100%` 위치에 track 위쪽 틱 마커(brand 세로선 + 위 번호 미니 라벨) 렌더. 틱은 Pressable(hitSlop 8, `accessibilityLabel="{numbers}번 감점 시점으로 이동"`) — 탭 시 `seekBoth(sec)`. track 의 panResponder 와 겹치지 않게 틱 row 를 track 위 별도 줄(높이 ~16)로 분리. 세로 카드/전체화면 공용 (renderControls 공유 — 로직 중복 0).

**2-C. result.tsx 배선.**
- `markers` memo 는 Task 1 확장 반환을 그대로 사용. actionLabels memo 의 게이트를 `markers.keypointNumbers[kp] != null || groupMarkers 멤버 포함` 으로 확장 (planner_findings 4 — 스플릿 행동구 소멸 방지). memo 이름/역할 주석을 "영상 pill" → "범례·드릴다운 행동구" 로 갱신.
- 범례 조립 memo: 번호 있는 record 순서대로 `{ number, text: '{행동구 또는 criterionLabelKo(criterion)} −{|points|}' }` — 행동구는 해당 record 투영 keypoint(단일이면 그 관절, 그룹이면 멤버 중 actionLabels 보유 첫 관절)의 actionLabels 값, 없으면 criterionLabelKo 폴백 (fabricate 0). 감점 수치는 formatDeductionNumber(Math.abs(points)) — U+2212 표기 관례 유지.
- 틱 조립 memo: `buildDeductionTicks(records, markers.recordNumbers, result.visionVeto)`.
- KeypointOverlay 호출부: `actionLabels`/`showAngleLabels` prop 제거, `groupMarkers={markers.groupMarkers}` 전달 (사용자 측만 — 정은지 측 무변경), `onMarkerPress` 는 Task 3 시트 오픈 핸들러 (Task 3 에서 연결 — 본 task 시점엔 미전달로 컴파일 유지 가능).
- VideoCompare 호출부: `fullscreenLegend`/`timelineTicks`/`tickFrameCount={userKeypointReport?.frames ?? 0}` 전달. cleanPass/legacy 면 범례·틱 자연히 빈 배열 (별도 분기 불요).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck && ! grep -q "labelTextWidth" src/components/KeypointOverlay.tsx && ! grep -q "actionLabels" src/components/KeypointOverlay.tsx && grep -q "groupMarkers" src/components/KeypointOverlay.tsx && grep -q "timelineTicks" src/components/VideoCompare.tsx</automated>
  </verify>
  <done>오버레이에 텍스트 pill 렌더 경로가 없고(점만), 스플릿이 중점 1점+번호로 표시되는 데이터 경로가 배선되며, 전체화면 여백 범례와 탭-seek 틱이 렌더된다. typecheck GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: 확대사진 드릴다운 시트 + 참고 지표 진단 문장 전환</name>
  <files>app/src/components/DeductionDetailSheet.tsx, app/src/components/ScoreBreakdownSection.tsx, app/src/components/FaultZoomCompare.tsx, app/src/app/analysis/result.tsx</files>
  <action>
**3-A. `DeductionDetailSheet.tsx` 신설** (Modal, animationType slide, 하단 시트 스타일 — DimensionDetailModal 카드 패턴 준용, 토큰만, 라이트 전용).
Props: `visible`, `onClose`, `record: DeductionRecord | null`, `recordNumber: number | null`, `actionPhrase: string | null`, `zoom: FaultZoomComparison | null`, `rightLabel: string`.
콘텐츠 (위→아래):
- 헤더: 원문자 번호(circledNumberKo, brand) + criterionLabelKo(record.criterion) + 닫기 버튼.
- 확대 이미지: zoom 있으면 FaultZoomCompare 의 이미지 pane 자산 이식 — 합성 PNG 1장(aspect 2:1, resizeMode contain) + 좌 '내 영상'/우 rightLabel halfLabel 오버레이 (기존 스타일 그대로 이식). zoom 없으면 이 블록 생략 — 사진 없이 수치·문구만 (graceful, 빈 박스 금지).
- 수치 1줄: `formatDeductionRecord(record).detailText` 그대로 (신규 수치 조립 금지 — planner_findings 8) + pointsText.
- 행동구 1줄: actionPhrase 있으면 boxLabel 톤으로 (예: '다리 더 벌리기'), 없으면 생략.
파일 헤더에 quick-260705-r6v + "FaultZoomCompare 자산 이식(섹션 → 드릴다운)" why 주석.

**3-B. ScoreBreakdownSection — 행 탭 진입점.** 신규 prop `onRecordPress?: (recordIndex: number) => void`: 전달 시 record 행을 Pressable 로 감싸고(accessibilityRole button, accessibilityLabel "{label} 감점 상세 보기", hitSlop 4) 우측에 '자세히 ›' 힌트 캡션 없이 chevron 아이콘 미니 표기(행 밀도 유지). 미전달 시 렌더 diff 0 (다른 소비처 무회귀).

**3-C. result.tsx — 시트 state + 3 진입점 + 섹션 제거.**
- state: `detailRecordIndex: number | null`. 열기 helper: 번호 → recordIndex 역매핑(markers.recordNumbers 의 indexOf — 번호는 unique).
- 진입점 3개 배선: (1) ScoreBreakdownSection `onRecordPress={setDetailRecordIndex}`, (2) VideoCompare `onLegendPress`(번호→index — VideoCompare 가 closeFullscreen 선행), (3) KeypointOverlay `onMarkerPress`(번호→index; 전체화면 render prop 경로에서도 동일 핸들러 — 단 전체화면에선 시트가 중첩 Modal 이 되므로 opts?.sizeScale 존재(=전체화면 호출) 시 콜백 미전달, 세로 카드 오버레이에만 전달. 전체화면 점 탭 요구는 범례 탭이 대체 — iOS 중첩 Modal 함정 주석 명기).
- zoom 매칭 memo: 선택 record 의 투영 keypoint 집합(단일 관절 또는 groupMarkers 멤버) ∩ `faultZoomComparisons` 의 (joint 또는 REGION_MEMBER_KEYPOINTS[region]) — tier='confirmed'(또는 tier 부재 legacy)만 매칭, advisory 는 감점 시트에 오매칭 금지 (planner_findings 7). 매칭 없으면 null.
- actionPhrase: Task 2-C 행동구 resolver 재사용 (범례와 동일 소스 — 문구 이중화 금지).
- **'문제 부위 확대 비교' 섹션 제거**: `<FaultZoomCompare ...>` JSX + import 삭제 → **FaultZoomCompare.tsx 파일 삭제** (소비처 1곳뿐 — planner_findings 9. caption() 의 advisory/Mode3 카피는 시트가 confirmed 전용이라 이식 불요, 삭제 사유 SUMMARY 에 기록).
- 시트 렌더: ScrollView 밖 형제로 `<DeductionDetailSheet visible={detailRecordIndex != null} ... />`.

**3-D. 참고 지표 → 진단 문장 (mode1 한정).**
- mode1 + breakdown 보유 시: angle/stability 행을 DimensionScoreRow(숫자+트랙바) 대신 진단 문장 행으로 교체 — `composeDimensionDiagnosisKo(dim, cleanPass, dimensionScores[dim])` 문장 1줄(body 톤) + 라벨('동작 흐름'/'안정성' — 신규 미니 라벨, DIMENSION_LABEL_KO 원본 무접촉) + 기존 '자세히 ›' 링크 유지(setDetailDim — 숫자는 DimensionDetailModal 안에서 계속 노출 = "숫자는 자세히 모달 안으로 이동"). 문장 null(비유한 값)이면 행 생략.
- angle 행의 reframeVeto 콜아웃/contextNote 경로는 mode1 문장 전환으로 대체 — "감점은 특정 순간의 자세에서 나왔어요" 문장이 같은 역할(모순 차단)을 승계. 관련 조건식은 mode1 문장 분기에서 미사용 처리 (mode3 경로엔 원래 미발동).
- line 차원(존재 시)과 mode3 경로는 기존 DimensionScoreRow 그대로 (성장 델타 무접촉 — planner_findings 10). occlusion badge/auxCaption(안정성 보조 지표 각주) 유지 — 단 auxCaption 은 문장 전환 시에도 사실 정합(안정성 미합산)이므로 무변경.
- mode1 인데 breakdown 부재(legacy doc): cleanPass 판단 불가 → 기존 숫자 행 유지 (보수적, isCleanPass 규칙 정합).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck && ! grep -rq "FaultZoomCompare" src && grep -q "DeductionDetailSheet" src/app/analysis/result.tsx && grep -q "composeDimensionDiagnosisKo" src/app/analysis/result.tsx</automated>
  </verify>
  <done>메인 화면에서 확대 비교 섹션이 사라지고 내역 행/범례/(세로 카드) 번호 점 탭이 시트를 연다. 참고 지표가 mode1 에서 진단 문장 2줄로 렌더되고 숫자는 자세히 모달에 남는다. FaultZoomCompare 참조 0. typecheck GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firestore doc → UI | 저장된 분석 doc(legacy 포함)이 유일한 입력 — 신규 네트워크/입력 표면 없음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-r6v-01 | Tampering/DoS | deductionLabels 신규 helper | mitigate | 비유한/빈 배열/legacy 필드 부재 시 null·빈 배열 반환 (crash 0, fabricate 0) |
| T-r6v-02 | Spoofing(오정보) | 진단 문장 | mitigate | 값 구간(<90) 분기 필수 — 낮은 지표에 "기준대로 타고 있어요" 거짓 문장 차단 |
| T-r6v-SC | Tampering | npm installs | accept | 신규 패키지 설치 0 (기존 의존성만) |
</threat_model>

<verification>
- `cd app && npm run typecheck` GREEN (유일한 정적 게이트 — JS 테스트 러너 미구성).
- grep 게이트: KeypointOverlay 에 pill 렌더 잔재 0, FaultZoomCompare 참조 0, 신규 배선 존재.
- 코드 리뷰 셀프 체크: 하드코딩 색/간격 0 (토큰만), 이모지 0, 백엔드/계약 파일 무접촉(diff 가 app/src 안에만), 저장값 재계산 0.
</verification>

<success_criteria>
- 가로 뷰어 재생 중: 영상 위 = 번호 점만, 스플릿 = 중점 1점, 여백 범례 = "① 행동구 −감점" 나열, 재생바 틱 탭 = 동기 seek.
- 내역 행/범례/(세로) 번호 점 탭 → [내|정은지] 확대사진 + "기준 대비 N° (허용 …)" + 행동구 시트. zoom 미매칭 시 수치·문구만.
- 참고 지표(mode1) = 조건부 진단 문장 2줄, 숫자는 자세히 모달. 감점 0/legacy/mode3 graceful.
- typecheck GREEN, JS-only (OTA 가능).
</success_criteria>

<output>
Create `.planning/quick/260705-r6v-viewer-v3-legend-drilldown-diagnostics/260705-r6v-SUMMARY.md` when done.

SUMMARY 필수 포함 (constraints):
1. **최악 케이스 시뮬레이션 — 감점 4건 재생 중 화면**: 스플릿(그룹, 중점 ①) + 왼어깨 ② + 오른어깨 ③ + 팔꿈치 ④ 가정으로 (a) 영상 위 점 개수/위치, (b) 여백 범례 4-entry 가로 나열이 iPhone 393pt 가로(fsLong 852) 여백에 들어가는지 폭 추산, (c) 재생바 틱 병합 결과("①②③④" 1틱)를 서술.
2. **유사도 낮은 케이스 문장 시뮬레이션**: angle=72/감점 2건, stability=85/감점 2건, angle=99/감점 4건, angle=100/감점 0 — 4조합 각각 어떤 문장이 나오는지 표로 서술 (거짓 문장 0 확인).
3. FaultZoomCompare 삭제로 소멸한 카피(advisory/Mode3 caption)와 그 사유.
</output>
