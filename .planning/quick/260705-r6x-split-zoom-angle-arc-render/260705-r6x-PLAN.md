---
phase: quick-260705-r6x
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/shared/python/sunity_shared/analysis/fault_zoom.py
  - backend/tests/test_fault_zoom.py
  - backend/tests/test_fault_zoom_relaxed_crop.py
  - backend/functions/pipeline/app.py
autonomous: true
requirements: [QUICK-260705-r6x]

must_haves:
  truths:
    - "region='legs'(스플릿) 카드의 valid 측(학생/기준 각각)에 동그라미 대신 골반 중점→양 다리 선 2개 + 사이각 호(arc)가 그려진다"
    - "deductionBreakdown.records 에 criterion='split_angle' record 가 있으면 학생 측=measuredValue, 기준 측=baselineValue 각도 수치가 호 옆에 표기된다; 없으면 수치 생략(선+호만)"
    - "저신뢰(relaxed/full) 측은 각도 드로잉 없이 기존 렌더 폴백 (오인 방지 원칙 유지)"
    - "non-legs 카드(단일 관절/arms)와 legacy 호출 경로 산출은 기존과 동일 (circle+배지 규칙 불변)"
    - "채점/veto/게이트 무접촉 — 변경은 전부 display 렌더 경로"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/fault_zoom.py"
      provides: "_leg_line_pts / _draw_leg_angle / split_angle_degs_from_records + build_fault_zoom_comparisons split_angle_degs param"
    - path: "backend/tests/test_fault_zoom.py"
      provides: "legs 각도 드로잉 분기 / 수치 표기·생략 / non-legs 무변경 유닛테스트"
    - path: "backend/functions/pipeline/app.py"
      provides: "_render_fault_zoom passthrough + _attach_fault_zoom_comparisons split record 추출"
  key_links:
    - from: "backend/functions/pipeline/app.py"
      to: "fault_zoom.split_angle_degs_from_records"
      via: "result.deductionBreakdown.records 전달"
      pattern: "split_angle_degs_from_records"
    - from: "fault_zoom.build_fault_zoom_comparisons"
      to: "_draw_leg_angle"
      via: "region=='legs' AND 측 kind=='valid' AND _leg_line_pts 해석 성공 시에만"
      pattern: "_draw_leg_angle"
---

<objective>
스플릿(legs) 확대 카드의 시각 언어 교체 — 앵커 관절 동그라미 대신 "다리 사이각"을
직접 그린다: 골반 중점에서 왼/오른 다리 방향 선 2개 + 두 선 사이 호(arc) + 측정
각도 수치. 학생/기준(정은지) 양측에 동일하게 그려 호 크기 차이가 곧 결함으로 보이게
한다 (belle 실기기 2026-07-05: "다리에 동그라미가 아니라 다리와 다리 사이각을 표시").

Purpose: 스플릿 결함의 본질(두 다리 벌림 각도)이 현재 circle 마커로는 시각적으로 안
드러남. 분석 정확도=신뢰 (수치는 보조, 원인이 핵심 — 무엇이 부족한지 그림으로).
Output: fault_zoom 렌더러 확장 + pipeline 배선 + 유닛테스트. 채점 무접촉.
</objective>

<context>
@./CLAUDE.md
@backend/shared/python/sunity_shared/analysis/fault_zoom.py
@backend/functions/pipeline/app.py  (라인 2528-2735 — _render_fault_zoom / _attach_fault_zoom_comparisons)
@backend/tests/test_fault_zoom.py
@backend/tests/test_fault_zoom_relaxed_crop.py

**각도 수치의 저장 출처 (조사 완료 — 재조사 불필요):**
`result["deductionBreakdown"]["records"]` 의 `criterion == "split_angle"` record.
deduction_engine.py `_activated_over()` ipsf_absolute 경로(라인 393-411)가
`measuredValue = 180 − deficit` (추정 학생 각도), `baselineValue = 180.0`
(_IPSF_ABSOLUTE_BASELINE), `unit = "deg"` 로 방출. vision 이 split 을 짚으면
vision-측정 편차가 md["split_angle"] 로 주입돼 같은 record 형상으로 남는다
([[kipup-fp-RESOLVED-phase24A]] source='vision'). 이 record 의 수치를 그대로
표기하면 "점수가 쓴 측정값 = 화면에 보이는 수치" (측정-표시 정합,
[[scoring-must-be-transparent-deduction-tally]] 정신). record 부재(split 미발화 /
Mode3 / legacy) → 수치 생략, 선+호만 (요구 3).

**주의 — Gemini differences 의 student_angle_deg/reference_angle_deg 는 쓰지 말 것:**
result doc 에 저장되지 않음 (coach context 전용, vision_veto.to_coach_context).
windowMedianAngleDeltas 의 student_deg/reference_deg 도 쓰지 말 것 — kismam
관절(hip flexion) 각도라 inter-leg 사이각 호에 붙이면 의미 불일치.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: fault_zoom 렌더러 — legs 카드 사이각 시각화 (선 2개 + 호 + 수치)</name>
  <files>backend/shared/python/sunity_shared/analysis/fault_zoom.py, backend/tests/test_fault_zoom.py, backend/tests/test_fault_zoom_relaxed_crop.py</files>
  <behavior>
    새 테스트 (test_fault_zoom.py 신규 섹션 "quick-260705-r6x — legs 사이각 드로잉"):
    - Test 1 (_leg_line_pts 순수): hips+knees+ankles 전부 valid → (pelvis 중점,
      left_ankle, right_ankle) 정규화 좌표 반환. ankle 부재/저신뢰 → knee 폴백.
      hip 한쪽이라도 저신뢰/결측 → None.
    - Test 2 (드로잉 분기): legs 4관절(hips 0.4/0.6, knees 0.3/0.7 등 좌표 분산)
      valid + joint_kinds 전원 'deficit' → build 산출 1장. _draw_leg_angle 을
      monkeypatch spy (기존 _spy_mark 패턴 재사용) — user/ref 양측 각 1회 호출,
      _mark 는 circle=False 로 호출됨 (배지 유지, 원 생략).
    - Test 3 (수치 표기/생략): split_angle_degs=(130.0, 170.0) 전달 → spy 가
      user 측 130.0 / ref 측 170.0 을 받는다. split_angle_degs=None → 양측
      angle_deg=None (수치 생략, 선+호만).
    - Test 4 (저신뢰 폴백): ref 측 confidence 0.1 (relaxed) → ref 측
      _draw_leg_angle 미호출 (기존 relaxed crop 렌더 그대로), user 측만 호출.
    - Test 5 (non-legs 무변경): 단일 left_knee 카드 + arms grouped 카드 →
      _draw_leg_angle 0회 호출, _mark 는 기존 규칙(circle=True, anchor) 그대로.
    - Test 6 (split_angle_degs_from_records 순수): criterion='split_angle'/
      unit='deg' record → (measuredValue, baselineValue). record 부재/criterion
      불일치/비유한 값 → None 또는 해당 측 None (defensive). records=None graceful.
    - Test 7 (_draw_leg_angle 픽셀 smoke): 검정 캔버스에 pelvis(180,80),
      left(80,300), right(280,300), angle 130.0 → 두 선 위 픽셀이 _BRAND, 예외 없음.
      벡터 길이 < _MIN_LEG_VEC_PX 인 degenerate 입력 → 드로잉 없이 원본 반환(False).
  </behavior>
  <action>
    fault_zoom.py 에 legs(스플릿) 카드 전용 사이각 시각화를 추가한다. 전부 순수
    (PIL/numpy 외 의존 0), 채점 무접촉 — 모듈 docstring 의 "숫자 마커만" 원칙은
    유지 (한글 없음, 선/호/숫자만). 한국어 why 주석 + belle 2026-07-05 실기기 출처
    명기. 이모지 금지.

    1. `_to_crop_px(xy, left, top, side, w, h) -> tuple[int, int]` — 정규화 좌표
       → crop-내 출력 픽셀 변환을 _side_crop 의 anchor_px 산출 블록(라인 441-448)
       에서 추출해 단일 출처화. [0, _OUT-1] clamp 포함. _side_crop 의 anchor 산출이
       이 헬퍼를 쓰도록 교체 (산출 동일 — 기존 anchor 테스트 무수정 GREEN 가드).

    2. `_side_crop` 반환에 crop box `(left, top, side)` 를 4번째 원소로 추가
       (relaxed/full 은 해당 crop 의 box; full 은 box=None). 기존 테스트는 [0],
       [:2], [2] 인덱싱이라 하위호환 — 시그니처 주석에 명기.

    3. `_leg_line_pts(report, frame_idx) -> tuple[xy, xy, xy] | None` (순수) —
       사이각 드로잉에 필요한 3점(정규화):
       · pelvis = left_hip/right_hip 중점 — 양쪽 모두 _kp_xy finite AND _kp_conf
         게이트 통과(_KP_CONF_MIN 재사용, conf 부재=통과) 필수.
       · left 끝점 = left_ankle 우선, 게이트 실패 시 left_knee 폴백. right 동일.
       · 셋 중 하나라도 해석 불가 → None (호출측 기존 렌더 폴백).
       unit.members 가 아니라 report 에서 직접 조회 — fault_joints 에 knee 만
       있어도 hips 가 report 에 valid 하면 그린다 (드로잉 좌표 ≠ 결함 관절 목록).

    4. `_draw_leg_angle(img, pelvis_px, left_px, right_px, angle_deg) -> bool` —
       PIL 드로잉 (in-place, 성공 여부 반환):
       · 두 방향 벡터 길이가 `_MIN_LEG_VEC_PX = 8` 미만이면 False (겹친 좌표
         degenerate — 그리면 오히려 오인. display 전용 상수, 채점 무접촉 why 주석).
       · 선 2개: pelvis→left, pelvis→right, fill=_BRAND, width=4.
       · 호: 반지름 r = int(_OUT * 0.14), draw.arc(pelvis 중심 정사각 bbox,
         start=a1, end=a2, fill=_BRAND, width=3). a1/a2 = math.degrees(atan2(dy,dx))
         — 이미지 좌표(y down)에서 atan2 와 PIL arc(3시 기준 시계방향)가 정합.
         sweep = (a2−a1) % 360 이 180 초과면 a1/a2 스왑 (두 선 사이 minor arc).
       · 수치: angle_deg 유한하면 `_deficit_label` 재사용("130°" 포맷), 호 이등분
         방향 반지름 r+12 지점에 _BRAND 배경 소형 사각 + 흰 텍스트 (기존 배지
         스타일 재사용, 폭 추정 8px/글자 동일). angle_deg None/비유한 → 수치 생략.
       · 캔버스 밖 좌표는 _to_crop_px 가 이미 clamp — 추가 방어 불필요.

    5. `split_angle_degs_from_records(records) -> tuple[float | None, float | None] | None`
       (순수, boto3 무관) — deductionBreakdown.records 리스트에서
       criterion=='split_angle' AND unit=='deg' 첫 record 의
       (measuredValue=학생, baselineValue=기준) 추출. float 변환 실패/비유한은
       해당 측 None, 둘 다 None 이면 전체 None. records None/비리스트 graceful.
       docstring 에 "수치 출처 = 점수가 쓴 그 record (측정-표시 정합)" why 명기.

    6. `build_fault_zoom_comparisons` 에 keyword-only
       `split_angle_degs: tuple[float | None, float | None] | None = None` 추가.
       unit.region == "legs" 인 카드에서만, 측별 독립 적용:
       · user 측: u_kind=='valid' AND (pts := _leg_line_pts(user_report, u_kp_idx))
         AND _draw_leg_angle(...) True → 성공. 이때 _mark 는 circle=False 로 호출
         (deficit 배지 유지 — 우상단 배지와 호 수치는 역할 분리: 배지=부족분,
         호=측정 각도). 3점의 픽셀 변환은 _side_crop 이 반환한 box + _to_crop_px.
         실패(pts None / degenerate / kind != valid) → 기존 렌더 100% 그대로
         (circle=kind=='valid', anchor_px).
       · ref 측: r_kind=='valid' AND pts 해석 성공 시 ref crop 에 _draw_leg_angle
         (기존에 ref 측은 _mark 자체가 없음 — 배지/원 추가 금지, 선+호+수치만).
         실패 → 기존 그대로 (무마크).
       · split_angle_degs[0]=user 수치, [1]=ref 수치. None 이면 해당 측 수치 생략.
       · non-legs unit 은 코드 경로 완전 무접촉.

    7. 기존 테스트 조정 (형상 계약 이전, 삭제 금지):
       test_fault_zoom_relaxed_crop.py 의 `test_build_grouped_circle_at_max_deficit_joint`
       는 legs 카드에 circle 을 기대 — 이 계약(grouped 카드 circle=deficit 최대
       관절)은 non-legs grouped 카드에 여전히 유효하므로 fixture 를 arms 로 재표적
       (left/right_shoulder + left/right_elbow, 동일 0.3/0.7 좌표 그리드 →
       기대 anchor_px (280,280) 동일, joint_deltas 만 arms 키로 교체). 주석에
       "legs 는 quick-260705-r6x 사이각 드로잉으로 이전 — arms 로 계약 보존" 명기.
       test_fault_zoom.py 의 `test_grouping_legs_single_card` 는 전 좌표 (0.5,0.5)
       겹침 → degenerate 폴백으로 기존 산출 유지 — 무수정 GREEN 이어야 함 (폴백
       경로의 회귀 가드로 그대로 둠).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && PYTHONPATH=shared/python:. python3 -m pytest tests/test_fault_zoom.py tests/test_fault_zoom_relaxed_crop.py -q</automated>
  </verify>
  <done>신규 테스트 7건 GREEN + 기존 fault_zoom 테스트 전부 GREEN (재표적 1건 포함, 삭제 0). legs valid 측 = 선 2 + 호 + (수치|생략), 폴백/non-legs = 기존 산출 그대로.</done>
</task>

<task type="auto">
  <name>Task 2: pipeline 배선 — split_angle record 수치 전달 + 전체 회귀</name>
  <files>backend/functions/pipeline/app.py</files>
  <action>
    _render_fault_zoom (라인 2528) 에 keyword-only `split_angle_degs=None` 추가,
    confirmed/advisory 두 build_fault_zoom_comparisons 호출 모두에 그대로 전달
    (advisory legs 카드도 동일 시각 언어 — 배치 간 규칙 분기 없음).

    _attach_fault_zoom_comparisons (라인 2635) 에서 Mode1 수치 추출:
    `split_degs = _fz.split_angle_degs_from_records((result.get("deductionBreakdown") or {}).get("records"))`
    를 _render_fault_zoom 호출에 `split_angle_degs=split_degs` 로 전달. why 주석:
    "수치 = 점수가 쓴 split_angle record (measuredValue=추정 학생각, baselineValue=
    IPSF 180 기준) — 측정-표시 정합, record 부재 시 선+호만 (belle 2026-07-05)".

    _attach_mode3_fault_zoom 은 무접촉 (default None → Mode3 legs 카드는 수치 없이
    선+호만 — Mode3 는 split record 자체가 미방출, honest 생략).

    채점 경로 무접촉 확인: 변경이 _render_fault_zoom/_attach_* display 함수 밖으로
    새지 않는지 diff 로 확인. 새 튜닝 상수 0 (Task 1 의 _MIN_LEG_VEC_PX 하나가 전부,
    display 전용).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && PYTHONPATH=shared/python:. python3 -m pytest tests/ -q</automated>
  </verify>
  <done>backend 전체 테스트 GREEN (기존 pipeline 테스트 회귀 0). _attach_fault_zoom_comparisons 가 split_angle record 수치를 카드에 전달, record 부재 시 None graceful.</done>
</task>

</tasks>

<verification>
- cd backend && PYTHONPATH=shared/python:. python3 -m pytest tests/test_fault_zoom.py tests/test_fault_zoom_relaxed_crop.py -q → GREEN (요구 검증 명령)
- cd backend && PYTHONPATH=shared/python:. python3 -m pytest tests/ -q → 전체 GREEN
- grep -n "_draw_leg_angle" backend/shared/python/sunity_shared/analysis/fault_zoom.py → renderer 존재
- 채점 무접촉: dimensions/deduction_engine/vision_veto/kismam 파일 diff 0
- 실 PNG 검증(정은지 스플릿 영상 재분석)은 pod 재분석 PENDING — SUMMARY 에 박제
</verification>

<success_criteria>
- legs(스플릿) 확대 카드: valid 측에 골반 중점→양 다리 선 2개 + 사이각 호, split_angle record 있으면 학생/기준 각도 수치 각각 표기, 없으면 선+호만
- 저신뢰/결측 측 = 기존 relaxed/full 폴백 그대로 (각도 드로잉 생략 — 오인 방지)
- non-legs 카드 + legacy 호출 + Mode3 = 산출 형상 불변
- 채점/veto/게이트 무접촉, 신규 튜닝 상수 = _MIN_LEG_VEC_PX 1개 (display 전용)
</success_criteria>

<output>
Create `.planning/quick/260705-r6x-split-zoom-angle-arc-render/260705-r6x-SUMMARY.md` when done.
SUMMARY 에 "실 PNG 검증 = pod 재분석 PENDING" 박제 (요구사항).
</output>
