---
phase: quick-260707-je3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/22-custom-vlm-finetune/22-01-PLAN.md
  - .planning/phases/22-custom-vlm-finetune/22-02-PLAN.md
  - .planning/phases/22-custom-vlm-finetune/22-03-PLAN.md
  - .planning/phases/22-custom-vlm-finetune/22-04-PLAN.md
  - .planning/phases/22-custom-vlm-finetune/22-07-PLAN.md
  - .planning/phases/22-custom-vlm-finetune/22-08-PLAN.md
  - .planning/phases/22-custom-vlm-finetune/22-10-PLAN.md
autonomous: true
requirements: [DR-01, DR-02, DR-03, DR-04, DR-05, DR-06, DR-07, P2-2201, P2-2210, P2-LICENSE]
must_haves:
  truths:
    - "22-04에서 shadow 유래 샘플은 manifest.json video_hash join 없이는 영상/프레임을 참조할 수 없다 (DR-01)"
    - "22-03 Task 3, 22-08 Task 2/3의 production Pod 변형은 blocking checkpoint 뒤에 있고 canary-first + rollback 블록이 action에 명시된다 (DR-02)"
    - "22-07 게이트 verify에 'test $? -le 1' fail-open이 없고, post-Pod 게이트는 --require-pass exit 0을 요구한다 (DR-03)"
    - "train/val split 소유권이 build_jsonl.py(video_hash) 단일이며 22-07의 --split_dataset_ratio 0.02 이중 split 문구가 제거된다 (DR-04)"
    - "22-04 full distillation 앞에 blocking 비용 checkpoint가 존재한다 (DR-05)"
    - "build_jsonl 진입점이 _meta.collection_complete=true를 assert한다 — --partial 없이 false면 실패 (DR-06)"
    - "collect-only pytest, 정적 grep, prefix listing 등 실행하지 않는 verify가 실제 outcome assert로 교체된다 (DR-07)"
  artifacts:
    - path: ".planning/phases/22-custom-vlm-finetune/22-04-PLAN.md"
      provides: "DR-01/04/05/06/07 반영된 학습셋 조립 플랜 (autonomous: false)"
    - path: ".planning/phases/22-custom-vlm-finetune/22-03-PLAN.md"
      provides: "DR-02/07 반영된 shadow 배선 플랜 (autonomous: false)"
    - path: ".planning/phases/22-custom-vlm-finetune/22-08-PLAN.md"
      provides: "DR-02 반영된 vLLM 동거 플랜 (autonomous: false)"
    - path: ".planning/phases/22-custom-vlm-finetune/22-07-PLAN.md"
      provides: "DR-03/04 반영된 SFT 게이트 플랜"
  key_links:
    - from: ".planning/phases/22-custom-vlm-finetune/22-DIRECT-REVIEW.md"
      to: ".planning/phases/22-custom-vlm-finetune/22-0X-PLAN.md"
      via: "각 DR 항목의 'What I would do'가 그대로 편집 스펙"
      pattern: "DR-0[1-7]"
---

<objective>
22-DIRECT-REVIEW.md의 P0 3건(DR-01~03) + P1 4건(DR-04~07) + P2 3건을 Phase 22 플랜 문서 7개에 외과적으로 반영한다. 소스 코드는 건드리지 않는다 — 대상은 플랜 문서 자체다.

Purpose: 리뷰 verdict가 "BLOCK until P0/P1 patched"이므로, Phase 22 실행 전에 플랜 텍스트의 실행 경계 결함(production Pod 자율 변형, 고객영상 학습 유입 우회로, fail-open 게이트, 실행하지 않는 verify)을 닫는다.

Replan 금지: 플랜 구조·wave/DAG·태스크 순서는 유지한다. 각 태스크의 action/verify/acceptance/frontmatter만 수정하고, checkpoint 태스크 삽입은 기존 태스크 사이에만 한다. 기존 포맷(frontmatter 키, task XML 구조, 한국어 주석 스타일, [[메모리 링크]] 인용, D-XX/F1/F2 표기)을 그대로 따른다. 이모지 금지.

Output: 7개 PLAN.md 수정본 (22-01, 22-02, 22-03, 22-04, 22-07, 22-08, 22-10).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-custom-vlm-finetune/22-DIRECT-REVIEW.md
@.planning/phases/22-custom-vlm-finetune/22-01-PLAN.md
@.planning/phases/22-custom-vlm-finetune/22-02-PLAN.md
@.planning/phases/22-custom-vlm-finetune/22-03-PLAN.md
@.planning/phases/22-custom-vlm-finetune/22-04-PLAN.md
@.planning/phases/22-custom-vlm-finetune/22-07-PLAN.md
@.planning/phases/22-custom-vlm-finetune/22-08-PLAN.md
@.planning/phases/22-custom-vlm-finetune/22-10-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 22-04 + 22-02 — 데이터 유입/비용/split/verify 수정 (DR-01, DR-04, DR-05, DR-06, DR-07 일부, P2-LICENSE)</name>
  <files>.planning/phases/22-custom-vlm-finetune/22-04-PLAN.md, .planning/phases/22-custom-vlm-finetune/22-02-PLAN.md</files>
  <action>
    **22-04-PLAN.md 편집:**

    (1) DR-01 — shadow 유입 manifest join 강제. Task 2(build_jsonl) action의 "Firestore vlm_shadow 적재분(22-03 — firestore_admin 읽기, veto/coach 라벨화)" 입력 서술을 다음 규칙으로 교체/확장: 모든 shadow 유래 샘플은 manifest.json을 video_hash로 join해야 한다. manifest row가 없거나 anonymized != true면 **text-only verdict 라벨만** 허용(영상/프레임/media 참조 0). 프레임을 쓰려면 anonymized=true + manifest에 등록된 anonymized S3 key가 필수. _meta에 shadow_unregistered_dropped / shadow_text_only_count 카운터 방출(드롭률 은폐 불가). Task 2 behavior에 테스트 2건 추가: "Test 9: 미등록 shadow video_hash → video-referencing 샘플 0건 (text-only는 허용)", "Test 10: _meta.shadow_unregistered_dropped/_meta.shadow_text_only_count가 방출된다". must_haves truths의 "hard_negative_eval 격리 행과 미가명처리 고객 행이 학습 JSONL에 포함되지 않는다"에 "미등록/미가명 shadow는 text-only 라벨로 강등되고 media 참조 0 (DR-01)"을 덧붙인다.

    (2) DR-06 — collection_complete fail-closed. Task 2 action에 진입점 assert 추가: build_jsonl.py는 시작 시 manifest `_meta.collection_complete is true`를 assert하고, 명시적 `--partial` 플래그가 없으면 false/부재 시 즉시 실패. `--partial` run은 canonical prefix `training/phase22/jsonl/` 업로드 금지(별도 partial prefix 또는 업로드 자체 skip). behavior에 "Test 11: _meta.collection_complete=false manifest → build_jsonl 실패(--partial 없이), --partial run은 canonical S3 prefix에 업로드 0" 추가.

    (3) DR-04 — split 단일 소유. Task 2 action의 "(video_hash 단위 split 0.98/0.02 — D-06 split_dataset_ratio 정합)" 문구에서 "D-06 split_dataset_ratio 정합"을 삭제하고 다음으로 교체: "video_hash 단위 split의 소유자는 build_jsonl.py 단독이다. SFT(22-07)는 val.jsonl을 명시적 val 파일로 소비하며 내부 재분할을 하지 않는다. ms-swift가 explicit val 파일 인자를 지원하지 않으면 val.jsonl을 발행하지 않고 held-out eval gate(22-07 assert_gates)가 validation owner임을 _meta와 22-07 스크립트 헤더에 문서화한다 (DR-04)".

    (4) DR-05 — 증류 비용 blocking checkpoint. Task 2와 Task 3 사이에 새 태스크 삽입 (`<task type="checkpoint:human-verify" gate="blocking">`, 이름 "Task 3: 증류 비용 checkpoint — full batch 승인", 기존 Task 3은 "Task 4"로 번호 갱신): what-built = 자동 준비 자료 — manifest 증류 대상 행 수, 예상 teacher(gemini-3.1-pro-preview) call 수, 예상 judge(gemini-3.5-flash) call 수, 현재 quota/credit probe 결과(1건 시험 호출 + client.files.list 잔량), 첫 run max rows=10, abort threshold(예: 429 1회 또는 폐기율 임계) 제시. how-to-verify = 첫 배치 10 rows 실행 → 필터 통계(수락/폐기 사유별) + File API 잔여물(files.list) 검사 결과 확인 → "approved" 시 full batch 진행. resume-signal = "approved" 또는 조정 지시. frontmatter `autonomous: true` → `autonomous: false`로 변경. [[gemini-credits-depleted-2026-06-20]] 인용 유지.

    (5) DR-07 — File API delete-in-finally를 실제 테스트로. Task 1 verify가 소스 문자열 정적 assert뿐이므로, Task 1 action에 추가: fake Gemini client(업로드/생성/삭제 호출 기록 stub)로 delete-in-finally를 unit test — 생성 호출이 예외를 던져도 files.delete가 호출됨을 assert하는 테스트를 backend/tests/phase22/test_gemini_teacher.py에 작성. Task 1 files와 frontmatter files_modified에 backend/tests/phase22/test_gemini_teacher.py 추가. Task 1 verify에 `python3 -m pytest backend/tests/phase22/test_gemini_teacher.py -x -q` 추가(기존 정적 체크는 보조로 유지).

    (6) DR-07 — S3 JSONL prefix listing을 object key 존재 확인으로. Task 3(→Task 4) verify의 `aws s3 ls s3://sunity-motion-pilot-videos/training/phase22/jsonl/ --profile sunity-motion`을 명시적 object key 확인으로 교체: `aws s3api head-object --bucket sunity-motion-pilot-videos --key training/phase22/jsonl/train.jsonl --profile sunity-motion` (+ val.jsonl은 DR-04 결정에 따라 발행 시에만 동일 head-object — 미발행 옵션 선택 시 val 체크 대신 _meta의 validation-owner 문서화 grep). acceptance의 "train.jsonl + val.jsonl 존재"도 같은 조건부 문구로 정합.

    **22-02-PLAN.md 편집:**

    (7) DR-06 cross-ref — Task 1 action의 test_manifest_consistency (b) 균등 게이트 서술("_meta.collection_complete 플래그로 게이트 활성화 — silent 통과 금지")에 덧붙임: "플래그 false/부재 시의 fail-closed 강제는 22-04 build_jsonl 진입 assert가 담당 (DR-06) — 매니페스트 정합 테스트만으로 충분하다고 간주 금지".

    (8) P2-LICENSE — Task 3 action의 LICENSE-AUDIT.md 필수 내용에 절 추가: "이월 항목(A9)에 다음을 명시: 유튜브/공개영상의 학습 이용 법적 지위는 파일럿 리서치 용도 유예이며, 본 감사 문서는 법률 검토(counsel) 서명 전까지 release-clean을 함의하지 않는다 — 상업 출시 gate 별도". Task 3 acceptance에 `grep -c "release-clean" backend/training/LICENSE-AUDIT.md` ≥ 1 항목 추가.

    전 편집에서 frontmatter YAML 유효성, task XML 구조(name/files/action/verify/acceptance_criteria/done 또는 checkpoint 필드), 한국어 스타일 유지.
  </action>
  <verify>
    <automated>P=.planning/phases/22-custom-vlm-finetune; grep -q "shadow_unregistered_dropped" $P/22-04-PLAN.md && grep -q "shadow_text_only_count" $P/22-04-PLAN.md && grep -q "collection_complete" $P/22-04-PLAN.md && grep -q -- "--partial" $P/22-04-PLAN.md && grep -q "checkpoint:human-verify" $P/22-04-PLAN.md && grep -q "autonomous: false" $P/22-04-PLAN.md && ! grep -q "split_dataset_ratio" $P/22-04-PLAN.md && grep -q "head-object" $P/22-04-PLAN.md && grep -q "test_gemini_teacher" $P/22-04-PLAN.md && grep -q "release-clean" $P/22-02-PLAN.md && grep -q "DR-06" $P/22-02-PLAN.md && echo TASK1-OK</automated>
  </verify>
  <done>22-04가 manifest-join shadow 유입, fail-closed collection 게이트, 단일 split 소유, blocking 비용 checkpoint, 실제-outcome verify(delete-in-finally 테스트 + head-object)를 갖추고 autonomous: false가 된다. 22-02가 DR-06 cross-ref와 LICENSE-AUDIT release-clean 유예 문구를 갖춘다.</done>
</task>

<task type="auto">
  <name>Task 2: 22-03 + 22-08 — production Pod 변형 blocking checkpoint + canary/rollback (DR-02, DR-07 일부)</name>
  <files>.planning/phases/22-custom-vlm-finetune/22-03-PLAN.md, .planning/phases/22-custom-vlm-finetune/22-08-PLAN.md</files>
  <action>
    **22-03-PLAN.md 편집:**

    (1) DR-02 — frontmatter `autonomous: true` → `autonomous: false`. Task 2와 Task 3 사이에 blocking checkpoint 삽입 (`<task type="checkpoint:human-verify" gate="blocking">`, 이름 "Task 3: production Pod 변형 승인 — canary-first + rollback 블록", 기존 Task 3은 "Task 4"로 번호 갱신): what-built = Pod 변형 계획 제시 자료 — (a) clone/canary Pod 우선 절차(동일 이미지·Volume/스크립트 형상·라이브 트래픽 0)와 production 접근 전제 조건: canary에서 warmup OK + 분석 스모크 OK + Firestore write OK + nvidia-smi 예산 OK + rollback 커맨드 문서화 완료, (b) 현재 Pod(svn31pzja7uay0) 직접 변형이 불가피한 경우의 rollback 블록 — env revert 커맨드(VLM_SHADOW_LOG 제거), start_server.sh revert 경로(Volume 쪽 원본 백업), 서버 재기동 커맨드, /health 체크, Lambda RUNPOD_ANALYZE_URL/env sync 체크. resume-signal = "approved"(canary 경로) / "current-pod"(현재 Pod 직접, rollback 블록 전제) / 조정 지시.

    (2) DR-02 — 기존 Task 3(→Task 4) action 첫머리에 rollback 블록을 action 수행 **전에** 기록·검증하는 스텝으로 삽입: "(0) rollback 블록 선기록: env revert / start_server.sh revert 경로(수정 전 원본을 Volume에 백업) / 재기동 커맨드 / health check / Lambda URL·env sync check를 SUMMARY 초안에 먼저 적고 나서 변형 시작". 이후 기존 (1)~(5) 스텝 유지. acceptance에 "rollback 블록이 변형 전에 기록됨(SUMMARY 근거)" 추가.

    (3) DR-07 — Task 2 verify의 `python3 -m pytest backend/tests -q -x --co -q 2>/dev/null | tail -1` (collect-only)을 실제 full-suite 실행 + baseline diff 기록으로 교체: `python3 -m pytest backend/tests -q 2>&1 | tail -3` + acceptance에 "HEAD baseline과 FAILED/ERROR 목록 diff IDENTICAL을 SUMMARY에 기록(기존 54건 pre-existing failure 목록 대조)" 명시. `--co` 문자열은 플랜에서 완전 제거.

    **22-08-PLAN.md 편집:**

    (4) DR-02 — frontmatter `autonomous: true` → `autonomous: false`. Task 1과 Task 2 사이에 blocking checkpoint 삽입 (`<task type="checkpoint:human-verify" gate="blocking">`, 이름 "Task 2: 서빙 Pod vLLM 동거 승인 — canary-first + rollback 블록", 기존 Task 2/3은 Task 3/4로 번호 갱신): what-built = vLLM 동거 계획 제시 — 22-POD-VRAM.md 실측 예산, gpu-memory-utilization 제안값, (a) clone/canary Pod에서 먼저 vLLM 동거 검증(warmup OK + 분석 스모크 OK + Firestore write OK + nvidia-smi 예산 OK + rollback 커맨드 문서화) 후 production 접근, (b) 현재 Pod 직접 진행 시 rollback 블록 — vLLM 프로세스 중지/제거 커맨드, start_server.sh revert 경로, 재기동 커맨드, /health 체크(vllm_loaded 필드 회귀 무해 확인), Lambda RUNPOD_ANALYZE_URL/env sync 체크. resume-signal = "approved" / "current-pod" / 조정 지시.

    (5) DR-02 — 기존 Task 2(→Task 3) action 첫머리에 22-03과 동일한 "(0) rollback 블록 선기록" 스텝 삽입. 기존 Task 3(→Task 4)의 "판정: OOM 또는 지연 +20% 초과 → D-14 폴백" 서술은 유지(이미 수치 기준 있음)하되, 폴백 전환 시에도 production Pod 원복(rollback 블록 실행)을 선행함을 1문장 추가.

    두 파일 모두 must_haves truths에 "production Pod 변형은 blocking checkpoint 승인 뒤에만 실행되고 rollback 블록이 변형 전에 기록된다 (DR-02)"를 추가. 기존 wave/depends_on은 변경 금지.
  </action>
  <verify>
    <automated>P=.planning/phases/22-custom-vlm-finetune; grep -q "autonomous: false" $P/22-03-PLAN.md && grep -q "autonomous: false" $P/22-08-PLAN.md && grep -q "checkpoint:human-verify" $P/22-03-PLAN.md && grep -q "checkpoint:human-verify" $P/22-08-PLAN.md && ! grep -qF -- "--co -q" $P/22-03-PLAN.md && grep -qE "canary|카나리|클론" $P/22-03-PLAN.md && grep -qE "canary|카나리|클론" $P/22-08-PLAN.md && grep -q "RUNPOD_ANALYZE_URL" $P/22-03-PLAN.md && grep -q "RUNPOD_ANALYZE_URL" $P/22-08-PLAN.md && grep -c "rollback\|롤백" $P/22-03-PLAN.md >/dev/null && echo TASK2-OK</automated>
  </verify>
  <done>22-03/22-08의 production Pod 변형이 blocking checkpoint(canary-first 우선, 불가피 시 rollback 블록 선기록) 뒤로 이동하고 autonomous: false가 되며, 22-03의 collect-only 회귀 verify가 실제 full-suite + baseline diff로 교체된다.</done>
</task>

<task type="auto">
  <name>Task 3: 22-07 게이트 fail-open 제거 + split 소비 정합, 22-01/22-10 P2 (DR-03, DR-04, P2-2201, P2-2210)</name>
  <files>.planning/phases/22-custom-vlm-finetune/22-07-PLAN.md, .planning/phases/22-custom-vlm-finetune/22-01-PLAN.md, .planning/phases/22-custom-vlm-finetune/22-10-PLAN.md</files>
  <action>
    **22-07-PLAN.md 편집:**

    (1) DR-03 — Task 3 verify에서 `python3 backend/evals/phase22/assert_gates.py; test $? -le 1`을 제거하고 게이트 모드를 분리: verify(automated, 로컬 unit 모드)는 `python3 -m pytest backend/tests/phase22/test_assert_gates.py -x -q`만 (SKIPPED != FAIL 의미론은 이 테스트가 검증 — Pod artifact 부재 시 exit 0 + 경고). Task 3 action에 assert_gates.py `--require-pass` 플래그 스펙 추가: 기본 main() = SKIPPED-only면 exit 0 + 경고, FAIL 1건이라도 있으면 exit 1; `--require-pass` = 전 게이트 PASS가 아니면(SKIPPED 포함) exit 비0. acceptance에 "Pod eval artifact 생성 후 `python3 backend/evals/phase22/assert_gates.py --require-pass` exit 0 — post-Pod 게이트 모드에서 FAIL/SKIPPED 잔존 시 통과 불가" 추가. behavior Test 5(exit 코드 규약)에 --require-pass 모드 케이스를 덧붙임.

    (2) DR-03 — Wave 5 진입 조건 명문화: Task 3 action 말미의 "게이트 FAIL 시" 절에 "Wave 5(22-08 서빙) 진입은 --require-pass PASS 또는 belle의 명시적 hold/scope-down 결정만으로 가능 — FAIL 상태에서의 암묵 진행 금지"를 추가하고, must_haves truths의 게이트 항목에도 같은 문장을 반영. success_criteria의 "게이트 PASS 시 Wave 3(22-08 서빙) 진입 가능" 문구와 정합 확인(FAIL 시 belle 논의 경로는 기존 유지).

    (3) DR-04 — Task 2 action의 `--dataset train.jsonl(S3 training/phase22/jsonl/에서 sync) --split_dataset_ratio 0.02`에서 `--split_dataset_ratio 0.02`를 제거하고 교체: "`swift sft --help`로 explicit val 파일 인자(--val_dataset 또는 등가)를 확인해 val.jsonl을 명시 소비하고 내부 재분할 0으로 설정. explicit val 인자 미지원 시 22-04의 DR-04 결정과 정합: val.jsonl 미발행 + held-out eval gate(Task 3 assert_gates)가 validation owner임을 run_sft.sh 헤더 docstring에 문서화 — SFT 내부 split은 leakage 보장이 아님을 명시". 이중 split 문구가 플랜 어디에도 남지 않게 확인.

    **22-01-PLAN.md 편집:**

    (4) P2-2201 — Task 2 verify의 python -c 커맨드에서 `print('profile ok', p['_meta'].get('source_doc_count'))`를 threshold assert로 교체: `assert p['_meta'].get('source_doc_count', 0) >= 30, p['_meta'].get('source_doc_count')` 를 기존 assert 뒤에 추가하고 print는 유지. acceptance의 "30 미만이면 SUMMARY에 사유 기록 후 reference 데이터로 보강" 폴백은 유지하되 "보강 후 재측정으로 verify를 GREEN으로 만든 뒤 종료 — print-only 통과 금지"로 보강.

    **22-10-PLAN.md 편집:**

    (5) P2-2210 — "env 1개" 전 출현을 "역할당 env 1개(one env per role)"로 교체 (3개 role-specific env var 설계는 유지 — 설계 변경 아님, 문구 정정): must_haves truths "swap 토글은 env 1개(pipeline 단독 소유)로 즉시 롤백 가능하다" → "swap 토글은 역할당 env 1개(one env per role, pipeline 단독 소유 3개)로 역할별 즉시 롤백 가능하다"; objective Purpose의 "(env 1개)" → "(역할당 env 1개)"; Task 1 done "env 1개 토글로" → "역할당 env 1개 토글로"; T-22-31 "env 1개 즉시 롤백" → "역할당 env 1개 즉시 롤백". Task 1 behavior의 "역할별 독립 env 3개" 서술과 정합됨을 확인.

    세 파일 모두 frontmatter/task 구조 유지, wave/depends_on 변경 금지.
  </action>
  <verify>
    <automated>P=.planning/phases/22-custom-vlm-finetune; ! grep -qF 'test $? -le 1' $P/22-07-PLAN.md && grep -qF -- '--require-pass' $P/22-07-PLAN.md && ! grep -qF -- '--split_dataset_ratio 0.02' $P/22-07-PLAN.md && grep -qE "source_doc_count.*>= *30|>= *30.*source_doc_count" $P/22-01-PLAN.md && grep -q "역할당 env 1개" $P/22-10-PLAN.md && test "$(grep -o 'env 1개' $P/22-10-PLAN.md | wc -l | tr -d ' ')" -eq "$(grep -o '역할당 env 1개' $P/22-10-PLAN.md | wc -l | tr -d ' ')" && echo TASK3-OK</automated>
  </verify>
  <done>22-07 게이트가 로컬 unit 모드/post-Pod --require-pass 모드로 분리되어 fail-open이 사라지고 Wave 5 진입 조건이 명문화되며, SFT가 명시적 val 파일을 소비한다. 22-01 verify가 source_doc_count >= 30을 assert하고, 22-10의 롤백 원칙 문구가 역할당 env 1개로 정정된다.</done>
</task>

</tasks>

<verification>
- 3개 태스크 verify 체인 전부 GREEN (TASK1-OK / TASK2-OK / TASK3-OK)
- 수정 후 각 플랜의 frontmatter YAML이 유효(파싱 가능)하고 wave/depends_on/plan 번호 불변 — replan 0
- checkpoint가 삽입된 3개 플랜(22-03, 22-04, 22-08)이 전부 autonomous: false
- 소스 코드(.py/.ts/.sh) diff 0 — 편집 대상은 .planning 문서만 (`git status`로 확인)
</verification>

<success_criteria>
- 22-DIRECT-REVIEW.md의 P0 3건 + P1 4건 + P2 3건이 전부 해당 플랜 텍스트에 반영되어 BLOCK verdict의 편집 요건이 소진됨
- 플랜 구조·wave/DAG·태스크 의도 불변 (외과 수정만)
- 다음 단계: opus 재검증(22-DIRECT-REVIEW 재대조) → belle 외부 리뷰
</success_criteria>

<output>
Create `.planning/quick/260707-je3-phase-22-22-direct-review-dr-01-dr-07/260707-je3-SUMMARY.md` when done
</output>
