---
phase: quick-260707-k07
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/22-custom-vlm-finetune/22-03-PLAN.md
  - .planning/phases/22-custom-vlm-finetune/22-04-PLAN.md
  - .planning/phases/22-custom-vlm-finetune/22-07-PLAN.md
  - .planning/phases/22-custom-vlm-finetune/22-VALIDATION.md
autonomous: true
requirements: [FT-02, FT-03, FT-04, FT-05]
must_haves:
  truths:
    - "22-VALIDATION.md가 DR 반영 후의 현행 태스크 번호(22-03-4/22-04-4/22-08-4 + checkpoint 행)를 가리키고, pre-DR 번호 참조가 0건이다 (ITERATION2 P1 must-fix)"
    - "22-03의 full-suite baseline 검증이 tail -3 파이프라인(exit code 은폐)이 아니라 FAILED/ERROR 노드 ID 파싱 + baseline artifact diff로 판정된다 (ITERATION2 P1)"
    - "val.jsonl 유무 계약이 validation_owner = explicit_val_jsonl | phase22_eval_gate 단일 필드로 통일되고 22-04 검증·success criteria·22-07 loss 문구가 같은 계약을 쓴다 (ITERATION2 P2)"
    - "22-POD-VRAM.md 검증이 텍스트 존재 체크가 아니라 numeric peak_vram_gb 필드 regex로 판정된다 (ITERATION2 P2)"
  artifacts:
    - path: ".planning/phases/22-custom-vlm-finetune/22-VALIDATION.md"
      provides: "현행 태스크 그래프와 싱크된 검증 매트릭스 (checkpoint 행 + 신규 자동 검증 4종)"
      contains: "--require-pass"
    - path: ".planning/phases/22-custom-vlm-finetune/22-03-PLAN.md"
      provides: "baseline-diff verifier + peak_vram_gb 파싱 계약"
      contains: "peak_vram_gb"
    - path: ".planning/phases/22-custom-vlm-finetune/22-04-PLAN.md"
      provides: "validation_owner 단일 계약"
      contains: "validation_owner"
  key_links:
    - from: ".planning/phases/22-custom-vlm-finetune/22-VALIDATION.md"
      to: ".planning/phases/22-custom-vlm-finetune/22-03-PLAN.md"
      via: "태스크 번호/verify 커맨드 참조"
      pattern: "22-03-4"
    - from: ".planning/phases/22-custom-vlm-finetune/22-07-PLAN.md"
      to: ".planning/phases/22-custom-vlm-finetune/22-04-PLAN.md"
      via: "validation_owner 계약 공유 (manifest _meta)"
      pattern: "validation_owner"
---

<objective>
Phase 22 2차 직접리뷰(22-DIRECT-REVIEW-ITERATION2.md, verdict=Pass with fixups)의 "Suggested Next Action" 4항목을 phase 22 계획 문서에만 반영한다:

1. [must-fix, P1] 22-VALIDATION.md를 DR 반영 후 태스크 그래프에 싱크
2. [P1] 22-03 full-suite `tail -3` verifier를 baseline 비교로 교체
3. [P2] val.jsonl 계약을 `validation_owner` 단일 필드로 통일 (22-04 + 22-07)
4. [P2] Pod VRAM artifact 체크를 numeric `peak_vram_gb` 필드 요구로 교체

Purpose: 실행자/리뷰어가 pre-DR 태스크 번호나 fail-open verifier를 따라가 DR-02/03/05/06/07 방어가 실제로 검증되지 않는 false confidence를 제거한다. 이 패치 후 Phase 22는 실행 게이팅 준비 완료 (ITERATION2 결론).

Output: 편집된 계획 문서 4개. 소스 코드 무접촉, wave/DAG/플랜 번호 불변(Replan 금지).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-custom-vlm-finetune/22-DIRECT-REVIEW-ITERATION2.md
@.planning/phases/22-custom-vlm-finetune/22-VALIDATION.md
@.planning/phases/22-custom-vlm-finetune/22-03-PLAN.md
@.planning/phases/22-custom-vlm-finetune/22-04-PLAN.md
@.planning/phases/22-custom-vlm-finetune/22-07-PLAN.md
@.planning/phases/22-custom-vlm-finetune/22-08-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 22-03-PLAN.md — tail-3 verifier 교체 + peak_vram_gb 파싱 계약 (ITERATION2 P1-2·P2-4)</name>
  <files>.planning/phases/22-custom-vlm-finetune/22-03-PLAN.md</files>
  <action>
    22-03-PLAN.md 두 곳을 편집한다 (태스크 번호/구조/wave 불변, 기존 한국어 문체 유지, 이모지 금지).

    (A) Task 2 full-suite baseline verifier 교체 (ITERATION2 P1: pipefail 없는 `... | tail -3`은 tail의 exit code라 full-suite 실패를 은폐). Task 2 `<action>` 맨 앞에 baseline 캡처 step (0)을 추가:
    "(0) 코드 수정 전 clean HEAD에서 full-suite baseline 캡처: `OUT=$(mktemp); python3 -m pytest backend/tests -q > "$OUT" 2>&1 || true; grep -E '^(FAILED|ERROR)' "$OUT" | awk '{print $1, $2}' | sort > .planning/phases/22-custom-vlm-finetune/22-03-BASELINE-FAILED.txt` — FAILED/ERROR test node ID 목록을 baseline artifact로 커밋."
    그리고 Task 2 `<verify><automated>`를 다음으로 교체 (tail 파이프라인 완전 제거):
    `python3 -m pytest backend/tests/phase22/test_shadow_wiring.py -x -q && { OUT=$(mktemp); python3 -m pytest backend/tests -q > "$OUT" 2>&1 || true; grep -E '^(FAILED|ERROR)' "$OUT" | awk '{print $1, $2}' | sort | diff .planning/phases/22-custom-vlm-finetune/22-03-BASELINE-FAILED.txt -; }`
    (diff exit 0 = 실패 집합 IDENTICAL — 신규 Phase 22 실패 또는 실패 수 증가 시 diff 비0으로 FAIL. VALIDATION.md의 regression-0 규칙과 동일 의미론.) Task 2 acceptance_criteria의 "전체 backend 테스트 full-suite ..." 항목도 이 baseline artifact diff 방식으로 문구 갱신(collect-only 금지 문구는 유지). frontmatter `files_modified`에 `.planning/phases/22-custom-vlm-finetune/22-03-BASELINE-FAILED.txt` 추가.

    (B) Task 4 VRAM artifact 검증 수치화 (ITERATION2 P2: `grep -cE "peak|피크"`는 텍스트 존재 체크일 뿐 scope-down 판단에 쓸 수치를 보장하지 않음). Task 4 `<action>`의 (5)에 파싱 가능 필드 블록 요구를 추가: 22-POD-VRAM.md 상단에 라인 단위 key: value 블록 —
    `peak_vram_gb: <숫자, 예 12.4>` / `total_vram_gb: 24` / `model_variant: <측정 시 상주 스택, 예 NLF+RTMW+YOLO (pre-vLLM)>` / `pod_type: <예 RunPod RTX 4090>` / `pod_id:` / `measured_at:` / `vllm_gpu_mem_util_recommended: <숫자>`.
    Task 4 `<verify><automated>`를 다음으로 교체:
    `test -f .planning/phases/22-custom-vlm-finetune/22-POD-VRAM.md && grep -qE '^peak_vram_gb: *[0-9]+(\.[0-9]+)?$' .planning/phases/22-custom-vlm-finetune/22-POD-VRAM.md && grep -qE '^pod_type: *[^ ]' .planning/phases/22-custom-vlm-finetune/22-POD-VRAM.md && grep -qE '^model_variant: *[^ ]' .planning/phases/22-custom-vlm-finetune/22-POD-VRAM.md`
    (numeric GB 값 regex 추출 실패 시 FAIL). Task 4 acceptance_criteria의 "피크 VRAM 수치(GB)와 vLLM 예산 권고치가 숫자로 기록됨" 항목을 "peak_vram_gb 등 파싱 가능 필드로 기록됨(regex 검증)"으로 갱신.
  </action>
  <verify>
    <automated>! grep -qF 'tail -3' .planning/phases/22-custom-vlm-finetune/22-03-PLAN.md && grep -qF '22-03-BASELINE-FAILED.txt' .planning/phases/22-custom-vlm-finetune/22-03-PLAN.md && grep -qF 'peak_vram_gb' .planning/phases/22-custom-vlm-finetune/22-03-PLAN.md && ! grep -qF 'grep -cE "peak' .planning/phases/22-custom-vlm-finetune/22-03-PLAN.md</automated>
  </verify>
  <done>22-03의 full-suite 검증이 exit code를 은폐하지 않는 baseline-diff로 바뀌고, VRAM artifact가 numeric 필드 계약으로 기계 검증된다. 태스크 번호/wave 무변경.</done>
</task>

<task type="auto">
  <name>Task 2: 22-04 + 22-07 — validation_owner 단일 계약 통일 (ITERATION2 P2-3)</name>
  <files>.planning/phases/22-custom-vlm-finetune/22-04-PLAN.md, .planning/phases/22-custom-vlm-finetune/22-07-PLAN.md</files>
  <action>
    val.jsonl 유무 계약을 단일 필드로 정의하고 두 플랜의 acceptance surface를 통일한다 (ITERATION2 P2: 계약이 두 갈래로 갈라져 실행자가 검증 없이 val 생략하거나 리뷰어가 정상 no-val 분기를 오판 FAIL할 수 있음). 계약 정의:
    `validation_owner = explicit_val_jsonl | phase22_eval_gate` (정확히 둘 중 하나, manifest.json `_meta`와 JSONL `_meta` 양쪽에 기록).

    22-04-PLAN.md 편집:
    (a) Task 2 `<action>`의 DR-04 문장("ms-swift가 explicit val 파일 인자를 지원하지 않으면 ... 문서화한다 (DR-04)")을 계약 문장으로 교체: "build_jsonl은 항상 `_meta.validation_owner`를 방출한다 — `explicit_val_jsonl`(train.jsonl+val.jsonl 발행, video_hash 단위 split 0.98/0.02, SFT가 val.jsonl을 명시 소비하고 내부 재분할 0) 또는 `phase22_eval_gate`(val.jsonl 미발행, held-out eval gate(22-07 assert_gates)가 validation owner). 값과 발행물이 정합해야 한다 (DR-04, ITERATION2 P2)."
    (b) Task 2 `<behavior>`에 Test 12 추가: "_meta.validation_owner가 항상 방출되고 값이 {explicit_val_jsonl, phase22_eval_gate} 중 하나이며, explicit_val_jsonl이면 val.jsonl 출력 존재·phase22_eval_gate면 부재 (값-발행물 정합)".
    (c) Task 4 `<action>`에 manifest `_meta.validation_owner` 기록을 명시 ((3) 3자 정합 항목에 "manifest _meta에 validation_owner/jsonl_run_id/생성 시각 기록" 형태로 병합).
    (d) Task 4 `<verify><automated>`를 정확히-둘-중-하나 assert로 교체:
    `python3 -m pytest backend/tests/phase22/test_manifest_consistency.py backend/tests/phase22/test_build_jsonl.py -x -q && aws s3api head-object --bucket sunity-motion-pilot-videos --key training/phase22/jsonl/train.jsonl --profile sunity-motion && python3 -c "import json,subprocess; owner=json.load(open('backend/training/data/manifest.json'))['_meta']['validation_owner']; assert owner in ('explicit_val_jsonl','phase22_eval_gate'), owner; rc=subprocess.run(['aws','s3api','head-object','--bucket','sunity-motion-pilot-videos','--key','training/phase22/jsonl/val.jsonl','--profile','sunity-motion'],capture_output=True).returncode; assert (owner=='explicit_val_jsonl')==(rc==0), (owner,rc); print('validation_owner ok:', owner)"`
    (e) Task 4 acceptance_criteria 첫 항목의 val 문구를 계약으로 교체: "(a) train.jsonl+val.jsonl 둘 다 head-object exit 0 이고 validation_owner=explicit_val_jsonl, 또는 (b) train.jsonl head-object exit 0 이고 validation_owner=phase22_eval_gate — 정확히 둘 중 하나 (DR-04/DR-07)".
    (f) must_haves의 build_jsonl.py artifact provides("... train/val split")와 success_criteria("FT-02 학습셋이 train/val JSONL로 완성 — 22-07 SFT의 직접 입력")를 계약 문구로 갱신: "train.jsonl(+ validation_owner=explicit_val_jsonl 시 val.jsonl)로 완성 — validation_owner 계약이 22-07의 val 소비를 결정".

    22-07-PLAN.md 편집:
    (g) Task 2 `<action>`의 val 소비 문장(괄호 안 "val 소비는 DR-04 — ..." 전체)을 계약 기준 분기로 교체: "val 소비는 manifest `_meta.validation_owner`가 결정 — `explicit_val_jsonl`이면 `--val_dataset`(또는 `swift sft --help`로 확인한 등가 인자)로 val.jsonl 명시 소비 + 내부 재분할 0; `phase22_eval_gate`이면 val 미소비, run_sft.sh 헤더 docstring에 'validation_owner=phase22_eval_gate — held-out eval gate(assert_gates)가 validation owner, SFT 내부 split은 leakage 보장이 아님' 명시 (DR-04, ITERATION2 P2)".
    (h) Task 2 acceptance_criteria의 "최종 체크포인트 + train/val loss 수치가 SUMMARY에 기록"을 "최종 체크포인트 + train loss(+ validation_owner=explicit_val_jsonl인 경우에만 val loss) 수치가 SUMMARY에 기록 — no-val 분기(phase22_eval_gate)는 val loss 부재가 정상"으로 교체.

    기존 문서 포맷/한국어 스타일 유지, wave/depends_on/태스크 번호 불변.
  </action>
  <verify>
    <automated>test "$(grep -cF 'validation_owner' .planning/phases/22-custom-vlm-finetune/22-04-PLAN.md)" -ge 5 && grep -qF 'explicit_val_jsonl' .planning/phases/22-custom-vlm-finetune/22-04-PLAN.md && grep -qF 'phase22_eval_gate' .planning/phases/22-custom-vlm-finetune/22-04-PLAN.md && test "$(grep -cF 'validation_owner' .planning/phases/22-custom-vlm-finetune/22-07-PLAN.md)" -ge 2</automated>
  </verify>
  <done>val.jsonl 유무가 validation_owner 단일 필드로 판정되고, 22-04 검증/success criteria와 22-07 loss 문구가 같은 계약을 참조해 정상 no-val 분기의 오판 FAIL 여지가 없다.</done>
</task>

<task type="auto">
  <name>Task 3: 22-VALIDATION.md — DR 반영 태스크 그래프 싱크 (ITERATION2 P1 must-fix)</name>
  <files>.planning/phases/22-custom-vlm-finetune/22-VALIDATION.md</files>
  <action>
    Task 1·2 완료 후(갱신된 플랜을 근거로) 22-VALIDATION.md를 현행 태스크 그래프에 싱크한다. 현행 번호 근거: 22-03 = T1 helper / T2 배선 / T3 production 승인 checkpoint(DR-02) / T4 Pod deploy+smoke+VRAM. 22-04 = T1 gemini_teacher / T2 build_jsonl / T3 증류 비용 checkpoint(DR-05) / T4 증류 배치+JSONL. 22-08 = T1 vlm_judge / T2 동거 승인 checkpoint(DR-02) / T3 setup_vllm / T4 동거 판정.

    Per-Task Verification Map 편집:
    (a) 22-03-2 행 Automated Command를 baseline-diff 형태로 갱신: `pytest test_shadow_wiring.py -x -q && full-suite FAILED/ERROR → 22-03-BASELINE-FAILED.txt diff (exit 0)`.
    (b) 신규 행 삽입 — `| 22-03-3 | 03 | 1 | FT-05 | T-22-07 | production Pod 변형 승인 + rollback 블록 (DR-02) | **checkpoint** | (human gate) | n/a | ⬜ pending |`.
    (c) 기존 "Pod smoke + peak-VRAM" 행을 22-03-4로 재번호 + Automated Command를 `grep -E '^peak_vram_gb: [0-9.]+' 22-POD-VRAM.md` (numeric 필드)로 갱신.
    (d) 22-04-1 행 Automated Command에 `pytest backend/tests/phase22/test_gemini_teacher.py -x -q` 추가 (delete-in-finally unit test — DR-07).
    (e) 22-04-2 행 Secure Behavior에 "collection_complete fail-closed (DR-06)" 추가.
    (f) 신규 행 삽입 — `| 22-04-3 | 04 | 2 | FT-02 | T-22-12 | 증류 full-batch 비용 승인 (DR-05) | **checkpoint** | (human gate) | n/a | ⬜ pending |`.
    (g) 기존 distill batch 행(구 22-04-3)을 22-04-4로 재번호 + Automated Command를 `pytest ... && aws s3api head-object train.jsonl && validation_owner 계약 assert (DR-07/DR-04)`로 갱신 (구 `aws s3 ls …/jsonl/` 제거).
    (h) 22-07-3 행 Automated Command에서 fail-open `assert_gates.py; test $? -le 1`을 제거하고 `pytest test_assert_gates.py -x -q` (로컬 unit) + Secure Behavior에 "post-Pod 게이트 = --require-pass exit 0, FAIL/SKIPPED 잔존 시 통과 불가 (DR-03)" 반영.
    (i) 신규 행 삽입 — `| 22-08-2 | 08 | 5 | FT-05 | T-22-25 | vLLM 동거 승인 canary-first + rollback 블록 (DR-02) | **checkpoint** | (human gate) | n/a | ⬜ pending |`.
    (j) 구 22-08-2(setup_vllm) → 22-08-3, 구 22-08-3(동거 판정) → 22-08-4로 재번호 (내용 유지).

    Manual-Only Verifications 편집:
    (k) "Pod shadow smoke + peak VRAM (22-03 T3)" → "(22-03 T4)" + Test Instructions에 peak_vram_gb 등 파싱 필드 기록 명시.
    (l) "Distill batch execution (22-04 T3)" → "(22-04 T4)" + validation_owner 계약(train만 vs train+val) 확인 명시.
    (m) "vLLM cohabitation (22-08 T2/T3)" → "(22-08 T3/T4)".
    (n) "D-15 gate Pod eval (22-07 T3)" 행 Test Instructions에 "post-Pod `assert_gates.py --require-pass` exit 0 (DR-03)" 추가.
    (o) checkpoint 3종(22-03 T3 production 승인 / 22-04 T3 증류 비용 / 22-08 T2 동거 승인)의 manual 행 추가 — Why Manual = belle blocking gate, Test Instructions = 각 플랜의 what-built/resume-signal 요약.

    마지막으로 문서 상단 blockquote에 "2026-07-07 ITERATION2 fixup 반영 (태스크 그래프 싱크 — DR-02/03/05/06/07 검증 경로 현행화)" 한 줄 추가. 표 정렬/포맷/한국어 스타일 유지, pre-DR 번호 참조가 문서 전체에서 0건이 되도록 전수 확인.
  </action>
  <verify>
    <automated>V=.planning/phases/22-custom-vlm-finetune/22-VALIDATION.md; grep -qE '\| 22-03-4 ' "$V" && grep -qE '\| 22-04-4 ' "$V" && grep -qE '\| 22-08-4 ' "$V" && grep -qF 'test_gemini_teacher' "$V" && grep -qF 'collection_complete' "$V" && grep -qF 'head-object' "$V" && grep -qF -- '--require-pass' "$V" && grep -qF 'validation_owner' "$V" && grep -qF 'peak_vram_gb' "$V" && ! grep -qF 'test $? -le 1' "$V" && ! grep -E '\| 22-03-3 ' "$V" | grep -qiE 'vram|peak|피크|smoke' && ! grep -qF '22-03 T3' "$V" && ! grep -qF '22-04 T3' "$V" && ! grep -qF '22-08 T2/T3' "$V" && ! grep -qF 'aws s3 ls' "$V"</automated>
  </verify>
  <done>검증 매트릭스가 현행 태스크 ID만 가리키고(stale 번호 0), checkpoint 3종의 manual 행과 신규 자동 검증 4종(test_gemini_teacher / collection_complete / head-object+validation_owner / --require-pass)이 등재되어 DR-02/03/05/06/07이 검증 문서를 통해 실제로 exercised 된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (없음 — 계획 문서 편집만) | .planning/ 아래 마크다운 4개 편집, 소스 코드/인프라 무접촉 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-q07-01 | Tampering | 검증 문서가 약한 체크를 가리켜 false confidence 유발 | mitigate | 이 plan 자체가 그 완화 — stale 번호 0 grep 게이트 + fail-open 문자열 부재 게이트 |
| T-q07-SC | Tampering | 신규 패키지 설치 | accept | 설치 0 (문서 편집만) |
</threat_model>

<verification>
- Task 1~3 verify grep 체인 전부 exit 0
- `git diff --stat`이 .planning/phases/22-custom-vlm-finetune/ 아래 4개 문서만 표시 (소스 코드 무접촉)
- 22-03/04/07/08 플랜의 frontmatter wave/depends_on/plan 번호 diff 없음 (22-03 files_modified의 baseline artifact 추가 제외)
</verification>

<success_criteria>
- ITERATION2 "Suggested Next Action" 4항목 전부 반영 — 22-VALIDATION.md 싱크(must-fix), tail-3 교체, validation_owner 단일화, peak_vram_gb 수치화
- Phase 22 계획이 ITERATION2 기준 "실행 게이팅 준비 완료" 상태 도달 (재검증은 후속 opus 재검증 패스에서)
</success_criteria>

<output>
Create `.planning/quick/260707-k07-phase-22-2-iteration2-fixup-4-22-validat/260707-k07-SUMMARY.md` when done
</output>
