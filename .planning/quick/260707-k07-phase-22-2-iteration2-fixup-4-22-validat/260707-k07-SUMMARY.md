---
phase: quick-260707-k07
plan: 01
subsystem: planning-docs
tags: [phase-22, direct-review, iteration2, validation-sync]
requires: [22-DIRECT-REVIEW-ITERATION2.md]
provides:
  - "22-VALIDATION.md — DR 반영 태스크 그래프 싱크 (checkpoint 3행 + 신규 자동 검증 4종)"
  - "22-03-PLAN.md — baseline-diff verifier + peak_vram_gb 수치 계약"
  - "22-04-PLAN.md / 22-07-PLAN.md — validation_owner 단일 계약"
affects: [phase-22 execution gating]
tech-stack:
  added: []
  patterns: ["baseline FAILED/ERROR artifact diff (exit code 은폐 제거)", "validation_owner = explicit_val_jsonl | phase22_eval_gate 단일 계약", "라인 단위 key: value 파싱 필드 (peak_vram_gb regex)"]
key-files:
  created: []
  modified:
    - .planning/phases/22-custom-vlm-finetune/22-03-PLAN.md
    - .planning/phases/22-custom-vlm-finetune/22-04-PLAN.md
    - .planning/phases/22-custom-vlm-finetune/22-07-PLAN.md
    - .planning/phases/22-custom-vlm-finetune/22-VALIDATION.md
decisions:
  - "checkpoint manual 행 참조는 'NN-MM T?' 표기 대신 태스크 ID(22-03-3 등) 표기 — pre-DR 표기 grep 게이트와 충돌 방지"
  - "22-04 acceptance 'test_build_jsonl.py ≥ 11 tests'를 ≥ 12로 상향 (Test 12 validation_owner 정합 추가에 따른 일관성)"
metrics:
  duration: "~6분"
  completed: 2026-07-07
---

# Quick 260707-k07: Phase 22 ITERATION2 fixup 4항목 반영 Summary

22-DIRECT-REVIEW-ITERATION2.md "Suggested Next Action" 4항목을 phase 22 계획 문서 4개에만 외과적으로 반영 — 22-VALIDATION.md 태스크 그래프 싱크(P1 must-fix), tail-3 verifier baseline-diff 교체(P1), validation_owner 단일 계약(P2), peak_vram_gb 수치 계약(P2).

## Task Commits

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | 22-03 verifier fixup (tail-3 → baseline-diff + peak_vram_gb) | 58b674c | 22-03-PLAN.md |
| 2 | validation_owner 단일 계약 통일 | 947056e | 22-04-PLAN.md, 22-07-PLAN.md |
| 3 | 22-VALIDATION.md 태스크 그래프 싱크 | c318419 | 22-VALIDATION.md |

## 반영 내역

### 1. 22-03-PLAN.md (ITERATION2 P1-2 · P2-4)

- Task 2 action 맨 앞에 baseline 캡처 step (0) 추가 — full-suite FAILED/ERROR test node ID를 `22-03-BASELINE-FAILED.txt`로 커밋. frontmatter `files_modified`에 baseline artifact 등재.
- Task 2 `<verify>`의 `... | tail -3` 파이프라인(pipefail 부재 → pytest exit code 은폐)을 FAILED/ERROR 목록 baseline diff(exit 0 = IDENTICAL)로 완전 교체. acceptance_criteria도 동일 의미론으로 갱신.
- Task 4 action (5)에 라인 단위 key: value 파싱 필드 블록 요구 추가 (`peak_vram_gb` / `total_vram_gb` / `model_variant` / `pod_type` / `pod_id` / `measured_at` / `vllm_gpu_mem_util_recommended`).
- Task 4 `<verify>`의 텍스트 존재 체크(`grep -cE "peak|피크"`)를 numeric regex 3종(`^peak_vram_gb: [0-9.]+` 등)으로 교체.

### 2. 22-04 + 22-07 validation_owner 계약 (ITERATION2 P2-3)

- 22-04 Task 2: build_jsonl이 항상 `_meta.validation_owner`(`explicit_val_jsonl` | `phase22_eval_gate`)를 방출하고 값-발행물이 정합해야 한다는 계약 문장으로 DR-04 분기 서술 교체. behavior에 Test 12(정합 검증) 추가.
- 22-04 Task 4: verify에 정확히-둘-중-하나 python assert(manifest `_meta` 대조 + val.jsonl head-object rc 일치) 추가, acceptance/must_haves provides/success_criteria를 계약 문구로 갱신, manifest `_meta`에 validation_owner 기록 명시.
- 22-07 Task 2: val 소비를 `_meta.validation_owner` 분기로 교체(explicit_val_jsonl → `--val_dataset` 명시 소비; phase22_eval_gate → val 미소비 + run_sft.sh 헤더 docstring 명시). acceptance의 train/val loss 문구를 no-val 분기 정상 처리로 갱신.

### 3. 22-VALIDATION.md 싱크 (ITERATION2 P1 must-fix)

- Per-Task Map: checkpoint 행 3개 삽입(22-03-3 production 승인 / 22-04-3 증류 비용 / 22-08-2 vLLM 동거 승인), Pod 행 재번호(22-03-4 / 22-04-4 / 22-08-3·22-08-4).
- 신규 자동 검증 4종 등재: `test_gemini_teacher.py`(DR-07), collection_complete fail-closed(DR-06), `head-object` + validation_owner assert(DR-04/07), `assert_gates.py --require-pass`(DR-03).
- fail-open `assert_gates.py; test $? -le 1` 제거, VRAM 체크를 `^peak_vram_gb: [0-9.]+` numeric 필드로 교체, `aws s3 ls` prefix listing 제거.
- Manual-Only: checkpoint 3종 manual 행 추가(belle blocking gate + what-built/resume-signal 요약), 22-03 T4 / 22-04 T4 / 22-08 T3/T4 재번호, D-15 행에 post-Pod `--require-pass` exit 0 지침 반영.
- 상단 blockquote에 ITERATION2 fixup 반영 이력 1줄 추가. 전수 스캔으로 pre-DR 번호 참조 0건 확인.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - 일관성] 22-04 acceptance "≥ 11 tests" → "≥ 12 tests"**
- **Found during:** Task 2
- **Issue:** Test 12 추가 후 acceptance의 테스트 하한이 stale
- **Fix:** ≥ 12로 상향
- **Files modified:** 22-04-PLAN.md
- **Commit:** 947056e

**2. [Rule 1 - 일관성] 22-07 Task 2 action (2) "loss 곡선/val loss" 무조건 기록 문구**
- **Found during:** Task 2
- **Issue:** 신규 validation_owner 계약과 모순 (no-val 분기에서 val loss 부재가 정상)
- **Fix:** "train loss 곡선(validation_owner=explicit_val_jsonl인 경우 val loss 포함)"으로 갱신
- **Files modified:** 22-07-PLAN.md
- **Commit:** 947056e

## Verification

- Task 1~3 verify grep 체인 전부 exit 0 (실행 확인)
- 이 태스크의 3 커밋(58b674c~c318419) diff = .planning/phases/22-custom-vlm-finetune/ 아래 4개 문서만 — 소스 코드/22-PLAN-REVIEW.md 무접촉
- 22-03/04/07/08 frontmatter phase/plan/wave/depends_on/type 무변경 (22-03 files_modified baseline artifact 추가만 — plan 허용 범위)
- 22-VALIDATION.md 전수 스캔: 태스크 ID 참조 전부 현행 번호, train/val·tail stale 참조 0건

## Known Stubs

None — 계획 문서 편집만, 코드 stub 해당 없음.

## Threat Flags

None — 신규 보안 표면 없음 (문서 편집만, T-q07-01 완화가 이 plan 자체).

## Self-Check: PASSED

- 22-03-PLAN.md / 22-04-PLAN.md / 22-07-PLAN.md / 22-VALIDATION.md 편집 확인 — FOUND
- 커밋 58b674c / 947056e / c318419 — FOUND (git log)
