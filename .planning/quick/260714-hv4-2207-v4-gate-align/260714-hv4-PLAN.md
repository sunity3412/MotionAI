---
phase: quick-260714-hv4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/training/datagen/schema.py
  - backend/tests/phase22/test_schema.py
  - backend/evals/phase22/run_bakeoff.py
  - backend/evals/phase22/assert_gates.py
  - backend/tests/phase22/test_bakeoff_harness.py
  - backend/training/sft/run_sft_gates.sh
  - .planning/quick/260714-hv4-2207-v4-gate-align/POD-RECHECK.md
autonomous: true
requirements: [QUICK-260714-HV4-GATE-ALIGN]

must_haves:
  truths:
    - "aligned 모드에서 하네스 user 메시지 텍스트가 학습 JSONL user 양식과 문자 단위 동일 (RTMW_Data 직렬화 + _TASK_INSTRUCTION, 동작명 라인 없음, system 롤 없음)"
    - "기본(legacy) 모드의 메시지 조립·guided JSON·리포트 구조는 바이트 단위 불변 (opt-in 플래그 없이는 기존 동작)"
    - "자유생성 raw 에서 <thought> 프리앰블 스트립 후 첫 balanced JSON 추출이 run_bakeoff 와 assert_gates 양쪽에서 동일한 단일 함수로 수행됨 — 파싱 실패는 기존과 동일하게 실패 집계"
    - "assert_gates 판정 기준 4종(synthetic_holdout / eval18 / traceability / determinism)의 임계·비교식은 한 글자도 완화되지 않음"
    - "pytest backend/tests/phase22/ 전체 GREEN (기존 237 passed 유지 + 신규 테스트)"
  artifacts:
    - path: "backend/training/datagen/schema.py"
      provides: "extract_report_json(text) — thought 프리앰블 스트립 + raw_decode 기반 balanced JSON 추출 (단일 진실)"
      contains: "def extract_report_json"
    - path: "backend/evals/phase22/run_bakeoff.py"
      provides: "--prompt-mode aligned / --media / --repetition-penalty + build_aligned_report_messages + _meta.prompt_mode 기록"
      contains: "prompt_mode"
    - path: "backend/evals/phase22/assert_gates.py"
      provides: "_parsed_report 가 schema.extract_report_json 사용 (json.loads 직접 호출 제거)"
      contains: "extract_report_json"
    - path: "backend/training/sft/run_sft_gates.sh"
      provides: "PROMPT_MODE/REPETITION_PENALTY env 통과 배선 + aligned 시 vLLM video 서빙 플래그"
      contains: "PROMPT_MODE"
    - path: ".planning/quick/260714-hv4-2207-v4-gate-align/POD-RECHECK.md"
      provides: "Pod D-15 재계측 커맨드 시퀀스 (오케스트레이터가 SSH 로 직접 실행)"
  key_links:
    - from: "backend/evals/phase22/run_bakeoff.py"
      to: "backend/training/datagen/build_jsonl.py"
      via: "_TASK_INSTRUCTION / _rtmw_text import 재사용 (복사 금지)"
      pattern: "_TASK_INSTRUCTION"
    - from: "backend/evals/phase22/assert_gates.py"
      to: "backend/training/datagen/schema.py"
      via: "schema.extract_report_json 호출"
      pattern: "extract_report_json"
    - from: "backend/training/sft/run_sft_gates.sh"
      to: "backend/evals/phase22/run_bakeoff.py"
      via: "--prompt-mode 플래그 통과"
      pattern: "prompt-mode"
---

<objective>
22-07 v4 게이트의 계측-학습 분포 불일치 4겹(모달리티/지시문/시스템 프롬프트/디코딩)을 해소하는
"정렬(aligned) 프롬프트 모드"를 run_bakeoff 하네스에 opt-in 으로 추가한다. SFT v4 모델이
swift infer(학습 양식)에서는 라우팅 키를 갖춘 정상 리포트를 내는데 D-15 게이트에서는 라우팅 키
0건 + fault 스팸으로 전축 FAIL 하는 확정 진단(2026-07-14)에 대한 계측기 측 fix 다.

Purpose: 게이트가 모델의 실제 학습 분포에서의 능력을 재도록 프롬프트/디코딩 양식만 정렬 —
게이트 판정 기준(eval18 변별 / holdout 개선 / traceability 라우팅 키 / determinism)은 절대
불완화. repetition 대응(repetition_penalty 노출)은 별도 소규모 A/B 용이며 본판정은 rp=1.0.

Output: aligned 모드 하네스 + 공유 방어 파서 + 게이트 러너 배선 + Pod 재계측 커맨드 문서.
**Pod 실행(재계측)은 이 plan 범위 밖** — 오케스트레이터가 push 후 SSH 로 직접 수행한다.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend/evals/phase22/run_bakeoff.py
@backend/training/datagen/build_jsonl.py
@backend/training/datagen/schema.py
@backend/evals/phase22/assert_gates.py
@backend/training/sft/run_sft_gates.sh
@backend/tests/phase22/test_bakeoff_harness.py
@backend/tests/phase22/test_schema.py

## 확정 진단 (2026-07-14) — 계측-학습 분포 불일치 4겹

1. 모달리티: 학습 = `{"type":"video"}` 단일 비디오 vs 하네스 = image_url 프레임 64장
2. 지시문: 학습 = `build_jsonl._TASK_INSTRUCTION` vs 하네스 = 별개 `_REPORT_TASK_TEXT`
3. 시스템 프롬프트: 학습(swift 실주입) = 없음 vs 하네스 = `build_system_prompt` 장문
4. 디코딩: 학습 타겟 = `<thought>` 프리앰블 허용 자유생성 vs 하네스 = guided JSON 첫 토큰 `{` 강제

## 핵심 인터페이스 (읽기 결과 박제 — 재탐색 불요)

- 학습 user 텍스트 = `build_jsonl._rtmw_text(frames) + build_jsonl._TASK_INSTRUCTION`
  (`_rtmw_text` = `"RTMW_Data: " + json.dumps(frames, ensure_ascii=False, sort_keys=True)`).
  동작명 라인 없음. content 순서 = [video, text] (media 먼저).
- 하네스 좌표 행 포맷 = `[{'frame': N, '<joint>': [dx,dy,conf]|None}]` — 학습 프레임 행과
  동일 구조(`pod_coords` 캐시 → `extract_eval_coords.py` 산출).
- `gemini_teacher.build_rtmw_text` 와 `build_jsonl._rtmw_text` 는 출력 동일하나, 단일 진실
  원칙상 aligned 모드는 `build_jsonl` 쪽을 import 한다.
- `assert_gates._parsed_report` 가 현재 `json.loads(rec["raw"])` 직접 호출 — 자유생성
  `<thought>` 프리앰블이 오면 유효 JSON 이 뒤에 있어도 게이트가 파싱 실패 처리한다.
  **공유 방어 파서가 없으면 aligned 모드는 게이트에서 무의미** — 이것이 Task 1 이 선행인 이유.
- `_make_vllm_caller._call(messages, response_format=None)` 은 None 이면 guided 기본 적용 —
  aligned 는 "guided 해제"를 표현할 별도 경로 필요.
- 게이트 아티팩트 파일명 = `bakeoff_{model}_{run_tag}.json`, run_tag=run1/run2 —
  aligned 재계측도 같은 파일명 규약을 쓰므로 assert_gates 수정 불요(파서 교체만).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: schema.extract_report_json — thought 스트립 + balanced JSON 추출 단일 진실</name>
  <files>backend/training/datagen/schema.py, backend/tests/phase22/test_schema.py</files>
  <behavior>
    - Test 1: `<thought>\n...\n</thought>\n{"coaching": ...}` → 뒤쪽 JSON dict 반환 (키 순서 보존)
    - Test 2: 순수 JSON 문자열 (프리앰블 없음) → 그대로 dict 반환 (legacy guided 출력 하위호환)
    - Test 3: 앞뒤 산문 + 중간 balanced JSON + 뒤 잔여 텍스트 → 첫 balanced JSON dict 반환
    - Test 4: JSON 없음 / 중괄호 미완결(unbalanced) / 빈 문자열 / None → None 반환 (관대화 금지)
    - Test 5: JSON 문자열 값 안에 `{`/`}` 포함 (예: coaching 텍스트에 중괄호) → 올바른 경계로 추출
      (naive 중괄호 카운팅이 깨지는 케이스 — raw_decode 가 필요한 이유)
  </behavior>
  <action>
    backend/training/datagen/schema.py 에 순수 함수 `extract_report_json(text) -> dict | None`
    추가. 구현 규칙:
    - `</thought>` 마지막 등장 이후로 본문 절단 (build_jsonl.assistant_report 의 기존 스트립
      규칙과 동일 — `content.find("</thought>")` 대신 프리앰블 다중 대비 rfind 허용, 단
      테스트로 고정). 이어서 본문에서 첫 `{` 인덱스부터 `json.JSONDecoder().raw_decode` 로
      파싱 시도, 실패 시 다음 `{` 로 전진하는 스캔 루프 (22-02 File API raw_decode 전례 —
      문자열 리터럴 내 중괄호에 안전한 유일한 stdlib 경로. naive brace counting 금지).
    - dict 가 아닌 최상위(list/str 등)로 디코드되면 계속 스캔. 끝까지 없으면 None.
    - normalize_report 는 호출하지 않는다 — 추출과 정규화는 별개 관심사, 호출자가 조합.
    - 순수 함수 (numpy/네트워크 0), 모듈 내 기존 스타일(왜 중심 한국어 docstring, 22-07 v4
      게이트 정렬 fix 근거 명시). SCHEMA_VERSION bump 불요 — 프롬프트/리포트 구조 불변,
      파서 추가일 뿐.
    - 테스트는 backend/tests/phase22/test_schema.py 에 기존 컨벤션(순수 import, pod-free)으로
      추가. RED 커밋 → GREEN 커밋: `test(quick-260714): ...` / `fix(quick-260714): ...`
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && python3 -m pytest tests/phase22/test_schema.py -q</automated>
  </verify>
  <done>extract_report_json 5 케이스 전부 통과, 기존 test_schema.py 테스트 무회귀.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: run_bakeoff aligned 프롬프트 모드 + assert_gates 파서 교체</name>
  <files>backend/evals/phase22/run_bakeoff.py, backend/evals/phase22/assert_gates.py, backend/tests/phase22/test_bakeoff_harness.py</files>
  <behavior>
    - Test 1 (지시문 동일성): aligned 메시지의 user 텍스트가 `build_jsonl._rtmw_text(rows) +
      build_jsonl._TASK_INSTRUCTION` 과 문자 단위 동일 — import 비교 (복사 검출).
    - Test 2 (시스템 프롬프트 부재): aligned 메시지 리스트에 role=system 항목 0건,
      동작명 라인("분석 대상 동작") 미포함, content 순서 = media 파트 먼저 → text 마지막.
    - Test 3 (legacy 불변): 동일 입력으로 `build_report_messages` 출력이 기존과 동일
      (system 존재 + _REPORT_TASK_TEXT 포함) — opt-in 이 기본 동작을 못 건드림을 고정.
    - Test 4 (argparse 기본값): `--prompt-mode` 기본 legacy, `--repetition-penalty` 기본 1.0.
    - Test 5 (assert_gates 통합): `<thought>` 프리앰블 + 유효 리포트 JSON 인 raw 를 가진
      레코드가 `_parsed_report` 에서 normalize_report dict 로 파싱됨 / JSON 없는 산문 raw 는
      여전히 None (실패 집계 유지).
  </behavior>
  <action>
    **run_bakeoff.py — aligned 모드 (opt-in, 기본 legacy 불변):**
    - `build_aligned_report_messages(coords_rows, media_parts) -> list` 신설: system 롤 없음,
      user content = media_parts(있으면 먼저) + `{"type":"text", "text": _rtmw_text(coords_rows)
      + _TASK_INSTRUCTION}`. `from datagen.build_jsonl import _TASK_INSTRUCTION, _rtmw_text`
      (기존 `_coords_to_frames` import 라인 옆). 동작명 라인·few-shot 없음 — 학습 분포에
      해당 요소가 없기 때문.
    - CLI: `--prompt-mode {legacy,aligned}` (기본 legacy, env BAKEOFF_PROMPT_MODE 폴백),
      `--media {auto,video,frames}` (기본 auto, aligned 전용), `--repetition-penalty`
      (float, 기본 1.0). aligned 시 shots_modes 는 ["zero"] 로 강제 (few-shot 예시는 legacy
      양식이라 aligned 분포에 없음) — `--shots` 값과 무관, 로그로 명시.
    - 모달리티: `--media auto|video` 는 vLLM OpenAI video_url 파트
      `{"type":"video_url", "video_url": {"url": f"file://{local_path}"}}` 시도
      (서빙 측 --allowed-local-media-path 전제, Task 3). auto 모드에서 첫 video_url 요청이
      openai BadRequest/4xx 로 실패하면 그 항목부터 프레임(image_url 64장, 기존
      prepare_frame_images) 폴백으로 전환하고 이후 항목도 frames 유지 — 레코드마다
      `rec["modality"]` = "video"|"frames"|"none" 기록 (지시문/시스템/디코딩 3겹만 정렬해도
      판별 가치 있음). synthetic_grounding 트랙: 학습 perturb 트랙이 [실영상 + 교란좌표 →
      원좌표] 구조이므로, aligned 에서는 BASE_SYNTH_S3_KEY 로컬 영상이
      `_fixtures_dir()` 에 존재하면 같은 modality 규칙으로 media 첨부, 없으면 좌표 단독
      (modality="none") — 실패 아닌 정상 폴백.
    - 디코딩: `_make_vllm_caller` 에 aligned 경로 추가 — aligned 리포트 태스크는
      response_format 미전송(자유생성). repetition_penalty != 1.0 이면
      `extra_body={"repetition_penalty": rp}` 로 전달 (1.0 이면 요청 바디 불변 — legacy
      요청과 바이트 동일 유지). trap 트랙은 aligned 여부와 무관하게 기존 guided enum 유지
      (포맷 무관 트랙, 게이트 비대상).
    - 파싱: aligned 경로의 run_report_item/run_synth_item 은 `json.loads` 대신
      `schema.extract_report_json(raw)` 사용 — None 이면 기존과 동일하게 score_json 에
      비-dict 전달(parse 0.0 실패 집계, 관대화 금지). raw/raw_sha 는 원문 그대로 저장
      (determinism 비교 대상 불변). legacy 경로 파싱은 손대지 않는다.
    - 리포트: `_meta` 에 `prompt_mode`, `media`(요청값), `repetition_penalty` 기록,
      레코드에 `prompt_mode`/`modality` 기록 — 아티팩트 구분 가능. `--dry-run` 출력에
      prompt_mode 표기. 게이트 아티팩트 파일명 규약(`bakeoff_{model}_{run_tag}.json`)은
      불변 — assert_gates 경로 탐색 무수정.
    **assert_gates.py — 파서 교체 (판정 기준 무변경):**
    - `_parsed_report` 의 `json.loads(raw)` 를 `schema.extract_report_json(raw)` 로 교체.
      None → None (기존 실패 의미 동일), dict → 기존대로 `schema.normalize_report` 통과.
      legacy guided 출력(순수 JSON)은 Test 2(Task 1) 하위호환으로 동일 결과 — 4 게이트의
      임계값·비교식·SKIPPED 규칙은 한 글자도 수정 금지.
    - 테스트는 test_bakeoff_harness.py 에 기존 `_load_harness()` 패턴으로 추가 (pod-free,
      모델 호출 0 — 메시지 빌더/argparse/파서 통합만). assert_gates 통합 테스트는
      test_assert_gates.py 기존 픽스처 스타일 확인 후 그쪽에 두어도 좋다(파일 소유 존중).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && python3 -m pytest tests/phase22/ -q && PYTHONPATH=shared/python:training:. python3 evals/phase22/run_bakeoff.py --dry-run</automated>
  </verify>
  <done>phase22 스위트 전체 GREEN (기존 237 passed 무회귀 + 신규), dry-run ALLDONE, aligned 메시지 = 학습 양식 문자 동일이 테스트로 고정, assert_gates 가 thought 프리앰블 리포트를 파싱하되 판정 기준 불완화.</done>
</task>

<task type="auto">
  <name>Task 3: run_sft_gates.sh 배선 + Pod D-15 재계측 커맨드 문서</name>
  <files>backend/training/sft/run_sft_gates.sh, .planning/quick/260714-hv4-2207-v4-gate-align/POD-RECHECK.md</files>
  <action>
    **run_sft_gates.sh:**
    - env `PROMPT_MODE`(기본 legacy — 기존 호출 동작 불변)와 `REPETITION_PENALTY`(기본 1.0)
      수용, run_bakeoff 호출에 `--prompt-mode "$PROMPT_MODE" --repetition-penalty
      "$REPETITION_PENALTY"` 통과. run1/run2 둘 다 동일 적용 (determinism 비교는 동일 조건
      cold 2회여야 유효).
    - PROMPT_MODE=aligned 일 때 vLLM serve 인자에 `--allowed-local-media-path
      "$BAKEOFF_FIXTURES_DIR"` 추가 + `--limit-mm-per-prompt '{"image": 64, "video": 1}'`
      로 교체 (video_url 수용 — vLLM 버전이 video_url 미지원이면 하네스 auto 폴백이
      frames 로 처리하므로 서빙은 그대로 두어도 안전). legacy 일 때 서빙 인자 불변.
    - 헤더 주석의 "학습 입력 양식과는 다름(SUMMARY caveat)" 문구를 aligned 모드 존재로
      갱신 (caveat 해소 경로 명시).
    **POD-RECHECK.md (Pod 실행은 오케스트레이터 몫 — 커맨드만 박제):**
    - 전제: 로컬 커밋 push → Pod 에서 `cd /workspace/SunityMotion && git pull`.
    - 본판정 (rp=1.0 순수 정렬 효과 격리):
      `PROMPT_MODE=aligned nohup bash training/sft/run_sft_gates.sh
      /workspace/phase22_export/sft-run1/awq > /workspace/sft_gates_aligned.log 2>&1 &`
      — 완료 마커 `GATES ALLDONE`, 판정은 로그의 base/require_pass exit 코드.
    - 비교 근거: 직전 legacy FAIL 아티팩트와 aligned 아티팩트는 run_tag 동일(run1/run2)
      파일명이 같으므로, **재계측 전 기존 EVAL_OUT_DIR/phase22/ 아티팩트를
      `phase22_legacy_v4/` 로 mv 백업**하는 커맨드를 첫 단계로 명시 (덮어쓰기 소실 방지).
    - rp A/B (별도 소규모, 게이트 본판정 아님): `REPETITION_PENALTY=1.05` 로 val 4행
      수준 관찰 실행 커맨드 + "본판정에 rp!=1.0 사용 금지" 경고.
    - 현 Pod 좌표 명시: xkaejqz9u72osv A100 80GB, `ssh root@213.173.105.9 -p 43181`.
    커밋: `fix(quick-260714): 22-07 v4 게이트 계측-학습 양식 정렬 — aligned 프롬프트 모드`
    (Task 1~3 커밋 분리 유지, plan.md 갱신 규약은 quick 워크플로우 SUMMARY 로 갈음).
  </action>
  <verify>
    <automated>bash -n /Users/kimtaesung/Dev/SunityMotion/backend/training/sft/run_sft_gates.sh && grep -c "PROMPT_MODE" /Users/kimtaesung/Dev/SunityMotion/backend/training/sft/run_sft_gates.sh && test -f /Users/kimtaesung/Dev/SunityMotion/.planning/quick/260714-hv4-2207-v4-gate-align/POD-RECHECK.md</automated>
  </verify>
  <done>스크립트 문법 검증 통과, PROMPT_MODE/REPETITION_PENALTY 배선 존재, legacy 기본값으로 기존 호출 불변, POD-RECHECK.md 에 백업→본판정→rp A/B 시퀀스 완비.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 모델 자유생성 출력 → 파서 | guided JSON 해제로 untrusted 텍스트가 파서에 직접 도달 |
| 로컬 파일 경로 → vLLM 서빙 | --allowed-local-media-path 로 file:// URL 허용 범위 개방 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-hv4-01 | Tampering | extract_report_json | mitigate | raw_decode 스캔 + 추출 후 normalize_report 화이트리스트 통과(기존 T-22-02/T-22-14 방어 유지) — 스키마 밖 키/거대 배열은 감점 집계, 크래시 아님 |
| T-hv4-02 | Info Disclosure | vLLM --allowed-local-media-path | mitigate | 허용 경로를 BAKEOFF_FIXTURES_DIR 단일 디렉토리로 한정, 서빙은 127.0.0.1 바인딩 유지(외부 노출 0) |
| T-hv4-03 | DoS | 자유생성 무상한 출력 | accept | timeout 1800s + max_model_len 상한 기존 유지, eval 전용 오프라인 경로라 저위험 |
| T-hv4-SC | Tampering | 패키지 설치 | accept | 신규 패키지 설치 0 (기존 openai/vLLM/stdlib 만 사용) |
</threat_model>

<verification>
1. `cd backend && python3 -m pytest tests/phase22/ -q` — 전체 GREEN (기존 237 passed 무회귀 + 신규 테스트).
2. `PYTHONPATH=shared/python:training:. python3 evals/phase22/run_bakeoff.py --dry-run` — ALLDONE, prompt_mode 표기.
3. `bash -n backend/training/sft/run_sft_gates.sh` — 문법 오류 0.
4. `rtk git diff` 로 assert_gates 4 게이트(check_synthetic_holdout / check_eval18_no_regression / check_traceability_and_monotonicity / determinism)의 임계·비교식 변경이 `_parsed_report` 파서 교체 외 0건임을 육안 확인.
5. legacy 모드 무플래그 실행 경로(메시지 빌더/guided/파싱)가 diff 상 변경 없음 확인.
</verification>

<success_criteria>
- aligned 모드가 진단 4겹(모달리티/지시문/시스템/디코딩)을 전부 다루되 게이트 판정 기준은 불완화.
- _TASK_INSTRUCTION 은 build_jsonl 에서 import (grep 으로 문자열 복사본 0건).
- 방어 파서가 run_bakeoff/assert_gates 공유 단일 함수 — 파싱 실패는 실패로 집계.
- repetition_penalty 는 노출만 (기본 1.0, 본판정 rp=1.0 문서화).
- Pod 실행 없음 — POD-RECHECK.md 커맨드 시퀀스가 산출물.
- 커밋 3건 분리, 규약 `fix(quick-260714): ...` / 테스트 선행 커밋 `test(quick-260714): ...`.
</success_criteria>

<output>
Create `.planning/quick/260714-hv4-2207-v4-gate-align/260714-hv4-SUMMARY.md` when done.
</output>
