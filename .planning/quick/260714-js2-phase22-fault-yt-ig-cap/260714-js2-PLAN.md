---
phase: quick-260714-js2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/training/datagen/curate_vision.py
  - backend/scripts/phase22_sources.yaml
  - backend/scripts/collect_phase22_youtube.py
  - backend/scripts/collect_phase22_instagram.py
  - backend/tests/phase22/test_curate_vision.py
  - backend/tests/phase22/test_harvest_filter.py
  - backend/training/LICENSE-AUDIT.md
  - backend/training/data/manifest.json
  - .planning/quick/260714-js2-phase22-fault-yt-ig-cap/RUN-SHEET.md
autonomous: true
requirements: [QUICK-260714-JS2]

must_haves:
  truths:
    - "fault_demo 큐레이션 프로필에서 편집/자막이 있는 튜토리얼의 '잘못된 예시' 시연이 reject되지 않고 fault_demo=true + 시연 fault 설명과 함께 keep 판정된다 (순수 로직)"
    - "default 프로필(정타 트랙) 판정 기준은 문자 그대로 무변경 — 기존 phase22 테스트 스위트 GREEN (248 passed 기준)"
    - "fault_demo 프로필 verdict 캐시 키가 default 프로필과 분리되어, 22-02 당시 캐시된 reject가 fault 재큐레이션을 차단하지 않는다"
    - "phase22_sources.yaml에 fault/tutorial 검색쿼리 후보 + eunji.poledancer cap 상향이 등재되고 --dry-run exit 0"
    - "실제 수집 실행 커맨드(과금·다운로드)는 RUN-SHEET.md로 산출되며 이 플랜에서 실행되지 않는다"
  artifacts:
    - path: "backend/training/datagen/curate_vision.py"
      provides: "fault_demo 프로필 분기 (프롬프트/스키마/decide/캐시 키)"
      contains: "fault_demo"
    - path: "backend/scripts/phase22_sources.yaml"
      provides: "fault/tutorial 버킷 확장 + eunji cap 상향"
      contains: "curation_profile"
    - path: "backend/training/LICENSE-AUDIT.md"
      provides: "§7-2 fault 재수집 라운드 승인 박제"
      contains: "7-2"
    - path: ".planning/quick/260714-js2-phase22-fault-yt-ig-cap/RUN-SHEET.md"
      provides: "오케스트레이터 실행 커맨드 시퀀스"
  key_links:
    - from: "backend/scripts/collect_phase22_youtube.py"
      to: "curate_vision.VisionGate.gate"
      via: "curation_profile 채널 필드 → gate(profile=...) 전달"
      pattern: "curation_profile"
    - from: "backend/scripts/collect_phase22_instagram.py"
      to: "phase22_sources.yaml cap_per_account"
      via: "계정별 cap 오버라이드"
      pattern: "cap_per_account"
---

<objective>
Phase 22 fault 타겟 재수집 라운드의 **로컬 코드+문서 준비** (belle 승인 2026-07-14).

v4 aligned 게이트에서 eval18 fault 짚기 실패의 근본원인 = fault 학습신호 부족(실결함 라벨 10개).
22-02 수집에서 YT/IG fault=0이었던 이유는 큐레이션 게이트가 편집/자막/오버레이 영상을 자동
reject해서 튜토리얼(편집·자막이 일반적)이 통째로 걸러졌기 때문. 이번 라운드는:
① 튜토리얼 타겟 외부 재수집 (fault-demo 수용 큐레이션 프로필 신설)
② 정은지 IG(eunji.poledancer, 본인 동의 확보) cap 상향 수집

Purpose: v5 재학습용 결함 시연 영상 확충의 코드·레지스트리·문서 기반 완성.
Output: fault_demo 큐레이션 분기 + 소스 레지스트리 확장 + LICENSE-AUDIT §7-2 + RUN-SHEET.

**범위 밖 (절대 실행 금지)**: gallery-dl/yt-dlp 실 다운로드, Gemini 과금 호출, S3 적재.
전부 오케스트레이터가 RUN-SHEET로 별도 수행. 이 플랜의 verify는 순수 로직 테스트 +
--dry-run(네트워크 0)만.
</objective>

<context>
@.planning/STATE.md
@backend/scripts/phase22_sources.yaml
@backend/scripts/collect_phase22_youtube.py
@backend/scripts/collect_phase22_instagram.py
@backend/training/datagen/curate_vision.py
@backend/training/LICENSE-AUDIT.md
@backend/tests/phase22/test_curate_vision.py
@backend/tests/phase22/test_manifest_consistency.py

확정 사실 (2026-07-14 세션, 재조사 금지):
- 정은지 성공/실패 페어 6쌍 = eval18 시험지와 동일 자산 → **학습 투입 영구 금지** (train-on-test).
- 내부 파일럿 신규 0건 (Firestore 872 동일).
- 기존 수집은 `_meta.collection_complete=true`로 마감 (2026-07-10 belle 승인) — §7-1 처방 B가
  waiver로 내부 행 추가한 선례 있음.
- manifest 현재 154행 (131 마감본 + 내부 fault track 23행, 커밋 4a4515a).

코드 계약 확인 결과 (planner 검증 완료):
- `build_jsonl.assert_collection_complete`는 `_meta.collection_complete` boolean만 읽음 →
  **true 유지 + `_meta.recollection_rounds` 리스트 추가 등재**가 계약 비파괴 방식 (플래그 뒤집기 금지 —
  뒤집으면 build_jsonl fail-closed + test_manifest_consistency 균등게이트 gated 상태 assert가 깨짐).
- `test_manifest_consistency.test_balance_gate_active_only_when_collection_complete`는
  위반 ⊆ waiver.unmet 만 요구 → fault 행이 늘어 위반이 줄어드는 방향은 subset 조건 자동 충족.
- `vision_verdicts.json` 캐시는 video_id 단독 키 → 22-02 당시 default 프로필 reject가 박제되어
  있음. **프로필 스코프 캐시 키 없이는 fault 재큐레이션이 기존 reject에 영구 차단됨** (핵심 함정).
- `collect_phase22_youtube._run_collect`는 `gate._cache.get(vid)`로 캐시를 직접 조회 →
  프로필 스코프 키 도입 시 이 조회도 함께 갱신해야 함.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: curate_vision fault_demo 큐레이션 프로필 (순수 로직 + 프로필 스코프 캐시)</name>
  <files>backend/training/datagen/curate_vision.py, backend/tests/phase22/test_curate_vision.py</files>
  <behavior>
    - Test 1: VERDICT_KEYS에 fault_demo(bool)·fault_desc(str|None) 추가 후에도 score/severity 계열
      BANNED_VERDICT_FIELDS는 여전히 normalize에서 드롭된다 (불변식 가드 유지).
    - Test 2: normalize_verdict가 fault_demo 결측 → False, fault_desc 결측 → None 고정
      (기존 캐시 verdict — 두 키 부재 — 를 재정규화해도 크래시/오염 0).
    - Test 3: decide(verdict, profile="fault_demo")가 keep && single_person_pole &&
      bucket=="fault" && fault_demo==true 조합만 status=keep. bucket=="정타"나 fault_demo=false는
      fault 프로필에서 reject (fault 시연 없는 일반 튜토리얼 유입 차단).
    - Test 4: decide(verdict) 기본 호출(프로필 미지정)은 기존 판정과 문자 그대로 동일 —
      기존 6개 테스트 무수정 GREEN (default 트랙 기준 무변경 fence).
    - Test 5: VisionGate.gate(vid, url, profile="fault_demo")의 캐시 키가 default와 분리
      (예: f"{vid}::fault_demo") — default 키에 reject가 박제된 vid도 fault 프로필로 재판정 경로 진입.
      키 미설정 graceful unknown 동작은 프로필 무관 동일.
    - Test 6: _GATE_PROMPT_FAULT_DEMO 문자열에 score/severity/점수 산출 지시가 없고,
      후프·에어리얼·스트렝스·비폴 reject 지시가 포함된다 (프롬프트 정적 fence).
  </behavior>
  <action>
    curate_vision.py에 fault-demo 수용 분기를 신설한다. **default 프로필 코드 경로는 무변경**
    (분기 분리 — 정타 트랙 기준 보존이 belle 제약).

    1. verdict 스키마 확장: VERDICT_KEYS에 "fault_demo"(bool — 잘못된 예시 시연 여부),
       "fault_desc"(str|None — 시연된 fault 서술, 점수·severity 아님 서술만) 추가.
       normalize_verdict의 bool 처리 목록에 fault_demo, str|None 처리에 fault_desc 편입.
       BANNED_VERDICT_FIELDS 무변경 (score 계열 영구 금지 불변식 유지 —
       scoring-must-be-transparent-deduction-tally).
    2. 프롬프트 분기: _GATE_PROMPT_FAULT_DEMO 신설. default 프롬프트와의 차이(왜):
       · 수용: 편집/자막/오버레이/화면분할이 있어도 keep 가능 — 튜토리얼은 편집·자막이
         표준 형식이라 22-02에서 통째로 걸러진 근본원인. "잘못된 예시" 시연 구간이
         존재하면 fault_demo=true + fault_desc(어느 관절/동작이 어떻게 잘못됐는지 한 줄).
       · 유지(reject): 후프/에어리얼/실크, 폴 없는 스트렝스 훈련, 비폴, 시연자 전신이
         한 번도 안 보이는 클립. 시연 구간의 단일 시연자 요건 유지.
       · 점수/severity/숫자 등급 절대 금지 문구 유지 (짚기까지만).
    3. decide 프로필 분기: decide(verdict, profile="default") 시그니처 확장 (기본값으로
       기존 호출부 무수정 호환). profile=="fault_demo"일 때만 keep 조건 =
       keep && single_person_pole && bucket=="fault" && fault_demo. default 경로는 기존 로직
       그대로 (한 줄도 변경 금지).
    4. VisionGate.gate(video_id, url_or_path, profile="default") 확장:
       · 캐시 키 = video_id (default) / f"{video_id}::fault_demo" (fault 프로필) —
         22-02 캐시 reject가 fault 재큐레이션을 차단하지 않도록 프로필 스코프 분리.
       · _call_gemini에 프로필별 프롬프트 선택 전달. graceful unknown 경로는 프로필 무관 동일.
       · 캐시 키 빌더는 순수 헬퍼(예: cache_key(video_id, profile))로 분리해 collector가
         재사용 (Task 2의 _run_collect 캐시 조회와 계약 1벌).

    테스트는 기존 test_curate_vision.py에 추가 (Gemini 미호출 순수 로직만 — 기존 fence 전례).
    TDD: 실패 테스트 커밋 → 구현 커밋.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && python3 -m pytest tests/phase22/test_curate_vision.py -q</automated>
  </verify>
  <done>fault_demo 프로필 순수 로직 테스트 전부 GREEN + 기존 6개 테스트 무수정 GREEN. decide 기본 호출 시그니처 하위호환 (기존 호출부 grep 상 무변경 필요 확인).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 소스 레지스트리 확장 + 수집기 프로필/cap 배선</name>
  <files>backend/scripts/phase22_sources.yaml, backend/scripts/collect_phase22_youtube.py, backend/scripts/collect_phase22_instagram.py, backend/tests/phase22/test_harvest_filter.py</files>
  <behavior>
    - Test 1: load_registry가 curation_profile/cap_per_account/search 필드가 있는 엔트리를
      로드하고 기존 필터 함수(passes_filters)가 무변경 통과 (신규 필드는 필터에 불간섭).
    - Test 2: 검색쿼리 엔트리(channel_url 부재 + search 존재)에 대해 _enumerate_channel의
      URL 빌더가 ytsearch{N}: 스킴을 생성 (yt-dlp 미호출 — URL 빌드 로직을 순수 헬퍼로 분리해 테스트).
    - Test 3: IG 어댑터의 계정별 cap 해석 — cap_per_account 필드가 있으면 CLI 기본값(20)을
      오버라이드 (순수 헬퍼로 분리해 테스트).
    - Test 4: --dry-run이 신규 엔트리 포함 exit 0 + 키 스킴 self-check 통과
      (uploads/ prefix 0, fixtures/phase22/ 스킴 — HIGH 1 규율 유지).
  </behavior>
  <action>
    **phase22_sources.yaml** — fault/tutorial 재수집 라운드 섹션 추가 (belle 승인 2026-07-14 주석 명시):

    1. 기존 Tier-2 fault 채널 5개(BerryTV_FallInPole, PoleDanceLessonsPoleDream, PoleFreaks,
       becciedunnfitness, poleplace)에 `curation_profile: fault_demo` 부여 — 22-02에서 default
       프로필로 fault=0이었던 채널들의 재큐레이션 대상 지정 (주석: 재큐레이션 근거 =
       편집/자막 reject가 튜토리얼을 통째로 걸렀던 22-02 실측).
    2. 신규 검색쿼리 엔트리 5개 등재 (`platform: youtube` + `search:` 필드, channel_url 없음,
       `bucket: fault`, `curation_profile: fault_demo`, duration_window [30, 900]):
       · "폴댄스 흔한 실수" · "폴댄스 자세 교정 잘못된" · "pole dance common mistakes tutorial"
       · "pole dance beginner mistakes" · "pole invert mistakes tutorial"
       각 엔트리 주석에 선정 근거(잘못된 예시 시연 확률이 높은 교정형 튜토리얼 표적) +
       **실체 검증 절차**: 실행 단계에서 --dry-run 열거 → 제목 육안 스팟체크 → belle 확인 후
       --curate (신규 소스 실체 검증은 실행 단계 몫 — 이 플랜은 후보+절차만).
    3. eunji.poledancer: `cap_per_account: 60` 추가 (기존 IG CLI 기본 20 → 상향), notes 갱신 —
       "본인 동의 확보 2026-07-14 (belle), fault 라운드 cap 상향". enabled 플래그는 현행 유지
       (IG 어댑터는 enabled 무관하게 IG 트랙 처리 — 기존 계약).
    4. defaults·Tier-1 정타 채널 블록은 무변경 (정타 트랙 불간섭).

    **collect_phase22_youtube.py** — 프로필 pass-through + 검색 열거:
    1. _run_curate/_run_collect: `profile = ch.get("curation_profile", "default")` →
       `gate.gate(vid, url, profile=profile)` / `decide(verdict, profile=profile)` 전달.
       캐시 직조회(`gate._cache.get(vid)`)는 Task 1의 cache_key(vid, profile) 헬퍼로 교체
       (프로필 스코프 키 계약 1벌 — 직접 f-string 재작성 금지).
    2. _enumerate_channel: channel_url 부재 + search 존재 엔트리 → `ytsearch{limit}:{search}`
       URL로 열거 (limit = per_cap*4 상한 그대로). URL 빌드를 순수 헬퍼로 분리 (Test 2 대상).
       --dry-run의 채널 순회는 channel_url 부재 엔트리도 name 기반 sample_key 검증 통과하도록
       방어 (현행 ch["name"] 사용이라 변경 최소).
    3. label_bucket: fault_demo 프로필 keep은 dec.bucket=="fault" 강제라 기존
       build_manifest_row VALID_BUCKETS 계약 무변경. vision_verdict에 fault_demo/fault_desc가
       자동 동반 적재 (행 스키마 추가 필드 불필요 — verdict dict 통짜 보관 현행 유지).

    **collect_phase22_instagram.py** — cap 오버라이드 + 프로필:
    1. 계정별 `cap = c.get("cap_per_account", args.cap)` 해석 (순수 헬퍼 분리, Test 3).
    2. `profile = c.get("curation_profile", "default")` → gate/decide 전달 (YT와 동일 계약).
       eunji.poledancer는 정타 위주 본인 릴스라 curation_profile 미부여 = default 유지
       (cap 상향만 — fault 프로필 강제 아님).

    미성년 제외(polesportkids enabled=false + minor_privacy_flag)·anonymize 정책·
    PHASE22_BELLE_GREENLIGHT 게이트·uploads/ prefix 금지 전부 무변경 유지.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && python3 -m pytest tests/phase22/ -q && python3 scripts/collect_phase22_youtube.py --dry-run && python3 scripts/collect_phase22_instagram.py --dry-run</automated>
  </verify>
  <done>phase22 전체 스위트 GREEN (248 passed 기준 + 신규 테스트) + 두 수집기 --dry-run exit 0 (네트워크·과금 0). 정타 채널 defaults 블록 diff 0.</done>
</task>

<task type="auto">
  <name>Task 3: 재수집 라운드 문서화 — LICENSE-AUDIT §7-2 + manifest _meta + RUN-SHEET</name>
  <files>backend/training/LICENSE-AUDIT.md, backend/training/data/manifest.json, .planning/quick/260714-js2-phase22-fault-yt-ig-cap/RUN-SHEET.md</files>
  <action>
    **LICENSE-AUDIT.md** — §7-1 처방 B 양식을 선례로 §7-2 신설:

    "## 7-2. fault 타겟 재수집 라운드 (belle 승인 2026-07-14)" — 포함 항목:
    · (a) 결정 내용 — v4 aligned 게이트 fault 짚기 실패(실결함 라벨 10개)의 데이터 처방으로
      ① 튜토리얼 타겟 외부 재수집(fault_demo 큐레이션 프로필) ② 정은지 IG cap 상향(20→60,
      본인 동의 확보) 승인.
    · (b) 라이선스 등급 — 튜토리얼 YT 수집분 = fair-use 회색(기존 R1/A9-1 등급과 동일 취급,
      학습전용·비재배포·앱 미노출·info.json provenance 완화 요건 동일 적용). 정은지 IG 추가분 =
      본인 동의로 IG 중 최저 리스크(기존 §5-2 eunji 행 등급 승계).
    · (c) 게이트 유지 — 미성년 제외(연령 스크리닝 절차 A9-3)·후프/스트렝스/비폴 reject·
      점수 라벨 영구 금지·uploads/ prefix 금지 전부 현행 유지. fault_demo 프로필은 편집/자막
      reject만 완화.
    · (d) train-on-test fence — 정은지 성공/실패 페어 6쌍(eval18 동일 자산)은 학습 투입 영구
      금지, 이번 라운드 수집분과 무관함을 명시.
    · (e) 수집 실측 수치는 실행 후 기입 (본 개정 시점은 "라운드 개시 — 행 수 미정" 명시,
      §5 원장 실측 갱신은 수집 실행 후 후속).
    §8 belle 결정 이력 테이블에 2026-07-14 행 추가 (fault 재수집 라운드 승인).

    **manifest.json `_meta`** — collection_complete=true는 **유지** (계약 확인 완료:
    build_jsonl fail-closed와 test_manifest_consistency gated-assert가 boolean에 결합 —
    뒤집으면 양쪽 깨짐). 대신 `_meta.recollection_rounds` 리스트 신설:
    ```
    [{ "round": "fault-yt-ig-260714", "approved_by": "belle", "approved_at": "2026-07-14",
       "scope": "튜토리얼 fault_demo 재큐레이션 + eunji.poledancer cap 60", "status": "open",
       "license_audit": "LICENSE-AUDIT.md §7-2" }]
    ```
    rows는 건드리지 않는다 (행 추가는 실행 단계 collect가 수행). 편집 후
    test_manifest_consistency GREEN 확인 (waiver subset 조건은 fault 행 증가 방향에서 자동 충족).

    **RUN-SHEET.md** — 오케스트레이터 실행 시퀀스 (이 플랜에서 실행 금지, 문서만):
    1. 환경: `AWS_PROFILE=sunity-motion`, `GEMINI_KEY_PARAM=/sunity/motion/gemini-api-key`,
       Gemini 크레딧 잔여 확인 선행 (고갈 이력 2026-06-20), venv=scratch ytvenv.
    2. 안전 확인: 두 수집기 --dry-run exit 0.
    3. 신규 검색쿼리 실체 검증: `--curate` 전에 dry 열거로 제목 스팟체크 → belle 확인.
    4. YT fault 재큐레이션: `PHASE22_BELLE_GREENLIGHT=1 python3 scripts/collect_phase22_youtube.py
       --curate --only <fault채널/검색엔트리명> --max-candidates <상한>` (과금 안전장치 필수 명시,
       채널별 커맨드 나열). keep 집계 belle 확인 후 `--collect`.
    5. IG eunji: `PHASE22_BELLE_GREENLIGHT=1 python3 scripts/collect_phase22_instagram.py
       --collect --only eunji` (cap_per_account=60 레지스트리 적용).
    6. 사후: manifest 행 수·fault 버킷 집계 확인 → LICENSE-AUDIT §7-2 (e) 실측 기입 +
       recollection_rounds status="collected" 갱신 → phase22 스위트 GREEN → 커밋.
    각 단계에 예상 과금 지점(Gemini File API/URL 판정)과 중단 조건(unknown 폭증=키/크레딧
    문제) 명시.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && python3 -m pytest tests/phase22/test_manifest_consistency.py -q && python3 -c "import json; m=json.load(open('training/data/manifest.json')); assert m['_meta']['collection_complete'] is True; assert m['_meta']['recollection_rounds'][0]['round']=='fault-yt-ig-260714'; print('meta ok, rows', len(m['rows']))"</automated>
  </verify>
  <done>LICENSE-AUDIT §7-2 + §8 이력 행 존재, manifest _meta.recollection_rounds 등재(collection_complete=true 불변, rows 154 불변), RUN-SHEET.md에 dry-run→검증→curate→collect→사후 시퀀스와 과금 안전장치가 커맨드 수준으로 기재.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 외부 미디어 → 로컬/S3 | YT/IG 파일명·메타데이터 (기존 _ascii_safe T-22-06 유지) |
| Gemini 응답 → verdict | 모델 출력이 점수 필드 주입 시도 가능 (normalize 화이트리스트) |
| 수집기 → S3 | uploads/ prefix 발화 위험 (기존 assert_non_notified HIGH 1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-js2-01 | Tampering | normalize_verdict 신규 필드 | mitigate | fault_demo/fault_desc만 화이트리스트 추가, BANNED_VERDICT_FIELDS 불변 + 정적 프롬프트 fence 테스트 (Task 1 Test 1·6) |
| T-js2-02 | Elevation | 캐시 키 프로필 분리 | mitigate | cache_key 헬퍼 단일 소유 — collector 직조회도 헬퍼 경유 강제 (Task 1-4, Task 2-1) |
| T-js2-03 | Tampering | manifest _meta 편집 | mitigate | rows 무변경 + collection_complete 불변 assert 를 verify 커맨드에 포함 (Task 3) |
| T-js2-04 | D/E | 과금·다운로드 실행 | accept(transfer) | 이 플랜은 로컬만 — PHASE22_BELLE_GREENLIGHT 게이트 무변경, 실행은 RUN-SHEET 경유 오케스트레이터 몫 |
</threat_model>

<verification>
1. `cd backend && python3 -m pytest tests/phase22/ -q` — 전체 GREEN (기존 248 passed 기준 무회귀 + 신규 테스트).
2. `python3 scripts/collect_phase22_youtube.py --dry-run` / `collect_phase22_instagram.py --dry-run` — exit 0, 네트워크·과금 0.
3. `rtk git diff backend/scripts/phase22_sources.yaml` — Tier-1 정타 채널·defaults 블록 diff 0 (정타 트랙 불간섭 육안 확인).
4. manifest: collection_complete=true 불변 + rows 154 불변 + recollection_rounds 등재.
</verification>

<success_criteria>
- fault_demo 프로필이 순수 로직 레벨에서 튜토리얼 fault 시연을 keep하고, default 트랙은 무회귀.
- 프로필 스코프 캐시 키로 22-02 reject 박제가 재큐레이션을 차단하지 않음이 테스트로 고정.
- 레지스트리에 검색쿼리 후보 5개(검증 절차 주석 포함) + eunji cap 60 등재, dry-run 통과.
- LICENSE-AUDIT §7-2 + manifest recollection_rounds + RUN-SHEET 산출 — 실 수집은 0건 실행.
- 커밋 형식: `fix(quick-260714-js2): ...` (TDD 태스크는 test/feat 분리 커밋 허용, scope 동일).
</success_criteria>

<output>
Create `.planning/quick/260714-js2-phase22-fault-yt-ig-cap/260714-js2-SUMMARY.md` when done
</output>
