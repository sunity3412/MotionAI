---
phase: quick-260715-wq9
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: false            # Task 2 = checkpoint:decision (C descope 방향, belle)
files_modified:
  - backend/training/distill/gemini_teacher.py
  - backend/tests/phase22/test_gemini_teacher.py
  - backend/training/datagen/build_jsonl.py
  - backend/tests/phase22/test_build_jsonl.py
requirements: [PHASE22-V6-TEACHER-DENSITY, PHASE22-V6-MIX-REBALANCE, PHASE22-V6-COORDS-DESCOPE]

must_haves:
  truths:
    - "교사 프롬프트가 결함 영상에서 관찰되는 모든 결함을 빠짐없이 짚도록 지시한다 (하나만 짚지 않음)"
    - "교사 프롬프트가 정타 영상에는 여전히 빈 배열([]) 억지 결함 금지를 유지한다 (위양성 불변)"
    - "학습셋 조립이 faults>0 신호를 faults=[] 다수 행에 익사시키지 않도록 비율을 제한한다"
    - "corrected_coords 전체 프레임 배열 방출 학습이 기본 학습셋에서 제외된다 (belle 선택 방식대로)"
  artifacts:
    - path: backend/training/distill/gemini_teacher.py
      provides: "결함 다중 열거 지시 강화된 build_teacher_system_prompt"
    - path: backend/training/datagen/build_jsonl.py
      provides: "fault-ratio 재균형 캡 + corrected_coords descope 플래그"
  key_links:
    - from: build_jsonl.build_dataset
      to: fault-bearing / fault-free 미디어 비율 캡
      via: "_meta 관측치 방출"
      pattern: "fault_bearing_count|fault_free"
---

<objective>
Phase 22 v6 학습셋 조립 결함 3종을 코드+테스트로 교정한다 (재라벨/SFT 없음, scope 밖).

진단 (학습셋 363행 직접 분석):
- fault 라벨 품질은 이미 높다 (fault당 12필드). schema.py 계약(DEDUCTION_CONSUMED_KEYS)·
  fault 스키마·enum·motion 조건부 판정 로직은 갈아엎지 않는다.
- 진짜 문제 = fault 신호가 희박+불균형. fault 중앙값 1개/영상(교사가 가장 뚜렷한 하나만
  짚음), 363행 중 fault 보유 80행(22%)뿐, 나머지 = 빈 fault 리포트 69 + fault 신호 전무한
  perturb/text 214행. → 학생(Qwen)이 "faults:[] 가 정상"을 학습 → 결함 영상 칭찬 + faults 미방출.

교정 3축:
  A. 교사 프롬프트 밀도 강화 — 결함 영상의 모든 결함을 빠짐없이 짚기 (정타 빈 배열 불변).
  B. build_jsonl 믹스 재균형 — fault>0 학습 신호가 faults=[] 다수에 익사하지 않게 비율 캡.
  C. corrected_coords 방출 학습 descope — 모델이 5/5 빈 배열 거부 + 제품가치 아님.
     방식(완전 제거 vs 델타 한정)은 D-10a 설계 반전이라 belle 결정 (Task 2).

Purpose: v5 SFT 게이트 FAIL(v4 동일 두 축)의 데이터 근본원인 제거.
Output: gemini_teacher.py + build_jsonl.py 코드 변경 + 무회귀 테스트.

불변 (절대 접촉 금지):
- schema.py 계약(REPORT_KEYS/FAULT_ITEM_KEYS/DEDUCTION_CONSUMED_KEYS) 변경 금지.
- 채점 코어 backend/shared/python/sunity_shared/analysis/ 무접촉.
- 게이트(datagen assert_gates / run_bakeoff) 무접촉 — 학습셋 조립만.
- 정타 영상 억지 결함 금지 문장 보존 (위양성 방지 invariant, [[analysis-objectivity-no-human-scores]]).
- 재라벨(Gemini 과금)·SFT(GPU)는 이후 별도 단계 — 이 커밋에 넣지 않는다.
- 이모지 금지. TDD (테스트 먼저). backend/tests/phase22 무회귀.
</objective>

<context>
@backend/training/distill/gemini_teacher.py
@backend/training/datagen/build_jsonl.py
@backend/training/datagen/schema.py
@backend/tests/phase22/test_gemini_teacher.py
@backend/tests/phase22/test_build_jsonl.py

베이스라인: `cd backend && python3 -m pytest tests/phase22/ -q` (전체 267 pass 기준,
이 두 파일 64 pass). 테스트는 backend 디렉토리에서 실행 (conftest 가 training/ 를 path 주입,
`from datagen import ...` / `from distill import gemini_teacher`).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 교사 프롬프트 결함 다중 열거 지시 강화 (A)</name>
  <files>backend/training/distill/gemini_teacher.py, backend/tests/phase22/test_gemini_teacher.py</files>
  <behavior>
    - Test: build_teacher_system_prompt(joint_keys) 프롬프트에 "결함 영상에서 관찰되는
      모든 결함을 빠짐없이 짚어라 (가장 뚜렷한 하나만 짚지 말 것)" 취지 문장이 존재한다.
    - Test: 정타 억지 결함 금지 문장("빈 배열", "[]", "억지 결함")은 그대로 유지된다 (회귀 없음).
    - Test: motion 주입/미주입 graceful 계약 불변 (기존 test_teacher_prompt_* 전부 pass).
    - Test: 감점 계약 필드 요구(student/reference_angle_deg 등) + enum 나열 문장 회귀 없음.
  </behavior>
  <action>
    build_teacher_system_prompt 에 "결함 다중 열거" 지시 한 문장을 추가한다. 핵심: 교사가
    "가장 뚜렷한 하나"만 짚는 관성(진단상 fault 중앙값 1개/영상)을 깨고, 결함 영상에서
    관찰되는 모든 결함을 각각 별도 faults[] 항목으로 빠짐없이 산출하도록 명시한다.

    보존 (변경 금지):
    - "짚을 결함이 없으면 faults 는 빈 배열([])" + "정타 영상에 억지 결함을 만들지 않습니다"
      문장 그대로 유지 — 다중 열거 지시가 정타 영상 위양성으로 번지지 않게 두 지시를
      명확히 대비시킨다 (결함 영상=모두 짚기 / 정타 영상=빈 배열).
    - fault 필드 계약·fault_category enum 주입·motion 조건부 판정 문장·점수 금지 문장 전부 불변.
    - schema.py 접촉 금지. 동작별 지식 하드코딩 나열 금지 (기술조건부는 motion_line 이 담당).

    프롬프트 텍스트만 확장 — 함수 시그니처/반환 구조 불변. 새 문장은 한국어, 이모지 금지.
    문장 배치는 faults 계약 설명 직후 + 빈 배열 지시 직전(결함/정타 대비를 인접 배치).
  </action>
  <verify>
    <automated>cd backend && python3 -m pytest tests/phase22/test_gemini_teacher.py -q</automated>
  </verify>
  <done>
    새 다중 열거 테스트 + 기존 test_gemini_teacher.py 전부 pass. "빈 배열/억지 결함"
    금지 문장 회귀 0. motion graceful 불변.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>C descope 방식 — corrected_coords 전체 프레임 배열 방출 학습을 어떻게 처리할까</decision>
  <context>
    corrected_coords 전체 프레임 배열 방출을 모델이 5/5 빈 배열로 거부 + 제품가치 아님.
    현재 이 배열을 방출하는 유일한 트랙 = perturb (D-10a 자가 라벨). distill 트랙은 이미
    v4 에서 corrected_coords=None. 즉 perturb 트랙의 존재 이유가 곧 corrected_coords 감독이다.

    설계 반전 주의: corrected_coords / 좌표 CoT 보정은 belle NotebookLM 비전의 일부
    ("VLM이 영상+좌표JSON 받아 CoT로 좌표 보정+코칭", [[phase22-vlm-notebook-2026-guide]]).
    실증(5/5 거부 + 비제품)과 비전이 충돌 → belle 판단 필요.

    planner 분석 (코드 정독 결과):
    - perturb 행은 corrected_coords 외 다른 학습 신호가 전무하다 (faults=[], coaching=None).
      따라서 corrected_coords 만 None 으로 바꾸면 perturb 95행이 "순수 faults=[] 노이즈"가
      되어 B(익사) 문제를 오히려 악화시킨다. → "완전 제거"는 반드시 perturb 트랙 자체를
      기본 학습셋에서 빼는 것을 의미한다.
    - B(재균형)의 fault-ratio 캡은 어느 방식이든 동일하게 필요 (Task 3).
  </context>
  <options>
    <option id="C1-flag-remove">
      <name>완전 제거 (플래그 기본 off, 권장)</name>
      <pros>
        - 모델이 학습 불가능(5/5 거부)한 타겟을 제거 → 학습 신호 정직.
        - perturb 95행(전부 faults=[]) 제거 → B 익사 최대 기여자 즉시 해소.
        - build_dataset(include_perturb=False) 기본값 = 플래그라 코드 삭제 아님 → belle 가
          언제든 True 로 되살릴 수 있는 가역 descope (D-10a 구현·비전 보존).
      </pros>
      <cons>
        - quick-260715-fjw perturb 테스트 다수가 기본 경로에서 빠짐 → include_perturb=True
          로 명시 유지(도먼트 코드 커버) + 기본 제외 신규 테스트 추가 필요.
        - 좌표 CoT 보정 학습을 당분간 포기 (비전 지연).
      </cons>
    </option>
    <option id="C2-delta">
      <name>델타 한정 (perturb 유지, 교란 셀만 방출)</name>
      <pros>
        - 좌표 보정 목표를 축소된 형태로 유지 (비전 정렬).
        - 타겟 토큰 수 감소 (전체 프레임 echo → 교란 셀만).
      </pros>
      <cons>
        - perturb 95행(faults=[])은 그대로 남아 B 익사 기여 지속 → B 캡이 perturb 행까지
          강하게 눌러야 함 (추가 복잡도).
        - 모델의 좌표 배열 방출 거부는 "전체 vs 델타" 문제가 아닐 수 있음 (희소 배열도 거부
          위험 동일) → 비제품 타겟에 복잡도 투자.
        - _coords_to_frames / parse_corrected_coords 게이트 마스크 정합 재설계 필요.
      </cons>
    </option>
  </options>
  <resume-signal>Select: C1-flag-remove (권장) 또는 C2-delta. Task 3 이 선택대로 구현.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 3: build_jsonl fault-ratio 재균형(B) + corrected_coords descope(C, 선택대로)</name>
  <files>backend/training/datagen/build_jsonl.py, backend/tests/phase22/test_build_jsonl.py</files>
  <behavior>
    - Test (B): fault-bearing(faults>0) 미디어와 fault-free(faults=[]) 미디어를 구분하고,
      fault_free 가 캡 비율(FAULT_FREE_CAP_RATIO)을 초과하면 결정적(안정 순서) 트림된다.
    - Test (B): fault_bearing 개수 0 이면 캡 skip (전량 트림 방지 guard).
    - Test (B): _meta 에 fault_bearing_count / fault_free_count / fault_free_cap_ratio 관측치 방출.
    - Test (B): 정타 신호가 완전 소거되지 않는다 (fault_free_count > 0 유지 — 억지 결함 위양성
      역방향 실패 방지, invariant).
    - Test (C, 선택 반영): 기본 build_dataset 산출이 belle 결정 방식대로 corrected_coords
      전체 배열 감독을 제외한다 (C1: perturb 트랙 부재 / C2: 교란 셀만).
    - 무회귀: score-free, hard-negative 격리, video_hash split leakage 0, shadow 드롭 카운터,
      validation_owner/collection_complete 게이트 전부 pass.
  </behavior>
  <action>
    B (재균형) — 어느 C 방식이든 공통:
    - 각 미디어 샘플에 조립 시점 report 로 `_has_faults = bool(report["faults"])` 태깅
      (distill 은 faults>0 가능, shadow/perturb 는 항상 False).
    - build_dataset 에서 per-track _balance_media 이후, 미디어를 fault-bearing / fault-free 로
      분리. `len(fault_free) > FAULT_FREE_CAP_RATIO * len(fault_bearing)` 이면 fault_free 를
      결정적(_balance_media 와 동일한 안정 입력 순서)으로 상한까지 트림. fault_bearing==0 이면
      캡 skip. 오버샘플/중복 금지 (curve-fit·overfit 방지 [[scoring-redesign-must-generalize-no-overfit]]).
    - FAULT_FREE_CAP_RATIO 는 모듈 상수 (기본값 예: 1.5 — DEFAULT_MIX_RATIOS/_STAGE_CYCLE 과
      동일하게 ablation 축, 수치 자체가 근거 아님. 주석에 근거=방향(정타 다수 익사 차단) 명시).
    - _meta 에 fault_bearing_count / fault_free_count / fault_free_cap_ratio 방출 (드롭률 은폐 불가).
    - text 혼합은 이미 len(media) 비례 → 트림된 media 기준으로 자동 축소 (별도 변경 불필요).
    - 균등 게이트(max<=2*min) / video_hash split 계약 불변.

    C (descope) — belle 선택대로:
    - C1-flag-remove (권장 선택 시): build_dataset(..., include_perturb: bool = False) 파라미터
      추가. 기본 False → _build_perturb_samples 미호출. _build_perturb_samples 및 quick-260715-fjw
      로직·상수(_STAGE_CYCLE 등)는 코드 보존(가역). 기존 perturb 전용 테스트는 include_perturb=True
      로 명시 호출해 도먼트 경로 커버 유지 + "기본 include_perturb=False → perturb 트랙 부재"
      신규 테스트 추가. perturb_coords_only_count meta 는 include_perturb=False 시 0.
    - C2-delta (선택 시): _build_perturb_samples 의 D6 corrected_coords 를 전체 프레임 echo 대신
      교란된 셀만 방출로 변경. parse_corrected_coords 게이트 마스크 정합(미방출=NaN 제외)을
      깨지 않게 방출 규칙을 맞추고, test_perturb_corrected_coords_full_frame_rows /
      test_subsample_first_* 를 델타 계약으로 갱신. include_perturb 플래그는 불필요.

    schema.py·assert_gates·run_bakeoff·analysis/ 무접촉. 이모지 금지.
  </action>
  <verify>
    <automated>cd backend && python3 -m pytest tests/phase22/test_build_jsonl.py -q</automated>
  </verify>
  <done>
    B fault-ratio 캡 + _meta 관측치 신규 테스트 pass. C 선택 방식 반영 테스트 pass.
    무회귀: `cd backend && python3 -m pytest tests/phase22/ -q` 전체 pass (변경한 계약 테스트는
    갱신, 그 외 회귀 0). fault_free_count > 0 유지 (정타 신호 소거 아님).
  </done>
</task>

</tasks>

<verification>
- `cd backend && python3 -m pytest tests/phase22/ -q` 전체 pass (변경 계약 테스트 갱신분 외 회귀 0).
- schema.py / analysis/ / assert_gates / run_bakeoff diff 0 (git status 로 확인).
- 교사 프롬프트: 결함 다중 열거 지시 존재 + 정타 빈 배열 금지 보존.
- build_jsonl _meta: fault_bearing_count / fault_free_count / fault_free_cap_ratio 방출.
- corrected_coords 전체 배열 감독이 기본 산출에서 belle 선택대로 제외.
</verification>

<success_criteria>
- A/B/C 3축 교정이 코드+테스트로 반영되고 phase22 테스트 무회귀.
- 위양성 invariant 보존 (정타 억지 결함 금지 문장 + fault_free_count>0).
- overfit/오버샘플 없음 (fault_bearing 트림 아닌 fault_free 캡).
- 재라벨/SFT 미포함 (scope 밖).
</success_criteria>

<output>
Create `.planning/quick/260715-wq9-phase22-v6-gemini-teacher-build-jsonl-de/260715-wq9-SUMMARY.md` when done.
</output>
