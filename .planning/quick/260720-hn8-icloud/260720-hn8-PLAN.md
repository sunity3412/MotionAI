---
phase: quick-260720-hn8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/tsconfig.json
  - app/src/lib/pickerFailure.ts
  - app/src/lib/pickerFailure.test.ts
  - app/src/components/PickErrorSheet.tsx
  - app/src/app/(tabs)/analyze.tsx
autonomous: true
requirements: [HN8-01, HN8-02, HN8-03]
must_haves:
  truths:
    - "영상 선택/업로드가 실패하면 인라인 회색 캡션이 아니라 바텀시트 알림창이 뜬다"
    - "알림창은 원인 제목 + 왜 그런지 + 번호 없는 점 불릿 해결단계를 보여준다"
    - "권한 거부 / iCloud 의심 실패에서는 하단 풀폭 브랜드 버튼으로 설정 앱을 열 수 있다"
    - "picker 원본 오류 문자열이 회색 정보카드로 화면에 남아 belle 가 다음 실패 때 원인을 캡처할 수 있다"
    - "카톡 경고·저화질 경고·프로필 권유 모달과 알림창이 동시에 뜨지 않는다"
  artifacts:
    - path: "app/src/lib/pickerFailure.ts"
      provides: "실패 원인 → {제목, 원인설명, 해결단계[], 액션} 매핑 순수 함수"
      exports: ["PickFailureKind", "PickFailure", "describePickFailure"]
    - path: "app/src/lib/pickerFailure.test.ts"
      provides: "매핑 순수 함수 런타임 검증 (node --test, 의존성 0)"
    - path: "app/src/components/PickErrorSheet.tsx"
      provides: "Figma modal-sheet 패턴 알림창"
      exports: ["PickErrorSheet"]
  key_links:
    - from: "app/src/app/(tabs)/analyze.tsx"
      to: "app/src/lib/pickerFailure.ts"
      via: "describePickFailure import"
      pattern: "describePickFailure"
    - from: "app/src/app/(tabs)/analyze.tsx"
      to: "app/src/components/PickErrorSheet.tsx"
      via: "PickErrorSheet 렌더"
      pattern: "<PickErrorSheet"
---

<objective>
영상 선택·업로드 실패를 **인라인 캡션 → 바텀시트 알림창**으로 전환하고, 원인별로
"무엇을 하면 되는지" 해결단계를 제시한다.

Purpose: belle 요구 — "분석조차 안 되면 안 되니까 해결방안을 알려줘야지 고객들에게."
에러 표시가 목적이 아니라 사용자의 다음 행동 안내가 목적. 인라인 텍스트는 실기기에서
사실상 안 보인다.

Output: 순수 매핑 모듈 + Figma modal-sheet 알림창 + analyze.tsx 배선.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@app/CLAUDE.md
@app/src/app/(tabs)/analyze.tsx
@app/src/components/BodyProfilePromptModal.tsx
@app/src/theme/index.ts
@app/src/theme/colors.ts
</context>

<established_facts>
오케스트레이터 진단 완료 — 재조사 금지.

1. **이미 커밋된 것 (commit `1025f79`, 재작업 금지):** `errText()` 헬퍼, `Current` 실패 시
   `Automatic` 재시도 폴백, picker 호출과 `handleResult` 의 try 분리. 코드에 이미 있다.
2. **이번 플랜이 남은 일:** 그 실패들이 아직 `setError(문자열)` → 인라인 `<Text style={styles.error}>`
   로만 표시된다. 이걸 알림창 + 해결단계로 바꾼다.
3. **iCloud 오프로드는 확정이 아니라 가설.** 에어드랍(로컬 파일) 성공 / 앨범 원본 실패라는
   정황 증거뿐. 그래서 알림창은 (a) 해결단계와 (b) **원본 오류 문자열 노출**을 반드시 함께
   담아 다음 실패 때 원인이 드러나게 한다.
4. **Figma 근거:** 에러 전용 알림창 디자인은 없음. `modal-sheet`(node 87:506) 패턴을 따른다 —
   grab handle + 둥근 바텀시트 / 큰 볼드 제목 + 우측 X / 회색 설명문 / 회색 정보카드 /
   브랜드색 점 불릿 / 하단 풀폭 브랜드 버튼.
5. 분석 실패(`not_pole_motion` / `no_human`)는 `loading.tsx` 담당 — **이번 범위 밖**.
</established_facts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 실패 원인 → 해결안내 매핑 순수 모듈 + 런타임 테스트</name>
  <files>app/src/lib/pickerFailure.ts, app/src/lib/pickerFailure.test.ts, app/tsconfig.json</files>
  <behavior>
    - `describePickFailure('permissionLibrary')` → title 에 '사진', action.kind === 'openSettings', steps.length >= 2
    - `describePickFailure('format')` → steps 에 'mp4' 포함 문자열 존재, action 은 kind 'dismiss'
    - `describePickFailure('tooLarge')` → title/cause 에 '100MB' 등장
    - `describePickFailure('libraryOpen', '원본: ...')` → detail 이 반환 객체에 그대로 실림
    - detail 미전달 시 `detail` 은 undefined (빈 문자열 아님)
    - 모든 `PickFailureKind` 값에 대해 title/cause 비어있지 않고 steps.length >= 1
  </behavior>
  <action>
`app/src/lib/pickerFailure.ts` 를 신설한다. **react / react-native / expo import 0** —
순수 데이터 매핑만 (테스트 가능성 확보가 목적).

내보낼 것:
- `export type PickFailureKind` — 문자열 리터럴 union:
  `'permissionCamera' | 'permissionLibrary' | 'format' | 'tooLarge' | 'libraryOpen' | 'cameraOpen' | 'processFailed'`
- `export type PickFailureAction = { kind: 'openSettings'; label: string } | { kind: 'dismiss'; label: string }`
- `export interface PickFailure { kind: PickFailureKind; title: string; cause: string; steps: string[]; action: PickFailureAction; detail?: string }`
- `export function describePickFailure(kind: PickFailureKind, detail?: string): PickFailure`

`describePickFailure` 는 `switch (kind)` 로 각 케이스를 구성하고, `default` 절에서
`const _exhaustive: never = kind;` 를 선언해 **컴파일 타임 exhaustiveness** 를 강제한다
(kind 를 추가하고 매핑을 빠뜨리면 `npm run typecheck` 가 깨진다 — 이게 회귀 방지 게이트).

문구는 전부 한국어, 이모지 금지, 사용자 행동 중심. 케이스별 내용:

- `permissionLibrary` — title "사진 접근 권한이 꺼져 있어요" / cause: 앨범에서 영상을 가져오려면
  사진 접근 권한이 필요하다 / steps: 설정 앱 열기 → Sunity → 사진 → '모든 사진' 허용 →
  앱으로 돌아와 다시 시도 / action `openSettings` label "설정 열기"
- `permissionCamera` — 동일 구조, 카메라 권한 버전 / action `openSettings`
- `format` — title "지원하지 않는 형식이에요" / cause: mp4, mov 만 분석 가능 /
  steps: 다른 영상 고르기 / 앱에서 직접 촬영하면 항상 지원되는 형식으로 저장됨 /
  action `dismiss` label "다른 영상 선택"
- `tooLarge` — title "영상 용량이 너무 커요" / cause: 100MB 이하만 분석 가능 /
  steps: 더 짧게 자른 영상 올리기 / 앱에서 직접 촬영하기 / action `dismiss`
- `libraryOpen` — **핵심 케이스.** title "영상을 가져오지 못했어요" /
  cause: 영상 원본이 기기에 없고 iCloud 에 올라가 있으면 앨범에는 보여도 바로 못 가져올 수
  있다 (앨범에 보이는 것과 기기에 저장된 것은 다르다는 점을 사용자 언어로) /
  steps: (1) 사진 앱에서 그 영상을 끝까지 재생해 다운로드가 끝나면 다시 시도 (2) Wi-Fi 에
  연결된 상태에서 다시 시도 (3) 설정 → 사진 에서 'iPhone 저장 공간 최적화' 대신 '원본
  다운로드 및 보관' 선택 (4) 계속 안 되면 앱에서 직접 촬영 / action `openSettings`
- `cameraOpen` — title "카메라를 열지 못했어요" / steps: 다른 앱에서 카메라 사용 중인지 확인 /
  앱 껐다 다시 켜기 / 앨범에서 선택하기 / action `dismiss`
- `processFailed` — title "선택한 영상을 처리하지 못했어요" / cause: 영상은 골랐지만 앱이
  읽는 단계에서 실패 (앨범 여는 문제와 다른 단계임을 명시) / steps: 다른 영상으로 다시 시도 /
  앱에서 직접 촬영 / action `dismiss`

`detail` 은 가공하지 않고 그대로 실어 반환한다 (전달 안 되면 필드 미포함/undefined).

이어서 `app/src/lib/pickerFailure.test.ts` 를 신설한다. `node:test` + `node:assert/strict`
만 사용하고 `import { describePickFailure } from './pickerFailure.ts';` 처럼 **`.ts` 확장자
명시 import** 를 쓴다 (Node 24 type stripping 으로 트랜스파일 없이 실행됨 — 확인 완료).
`<behavior>` 6개 항목을 그대로 테스트로 옮기고, 마지막 항목은 모든 kind 를 담은 배열을
순회해 검증한다.

`app/tsconfig.json` 의 `compilerOptions` 에 `"allowImportingTsExtensions": true` 를 추가한다
(`noEmit: true` 이므로 허용됨). 이 플래그가 없으면 `.ts` 확장자 import 가 TS5097 로 깨진다.
Metro 는 테스트 파일을 import 하는 코드가 없어 번들에 포함하지 않는다.

**신규 npm 의존성 0** — 새 패키지를 설치하지 말 것.
  </action>
  <verify>
    <automated>node --test app/src/lib/pickerFailure.test.ts && npm --prefix app run typecheck</automated>
  </verify>
  <done>테스트 전건 통과 + typecheck 무오류. `pickerFailure.ts` 에 react/react-native/expo import 0건 (`grep -cE "from '(react|react-native|expo)" app/src/lib/pickerFailure.ts` == 0).</done>
</task>

<task type="auto">
  <name>Task 2: PickErrorSheet — Figma modal-sheet 패턴 알림창</name>
  <files>app/src/components/PickErrorSheet.tsx</files>
  <action>
`app/src/components/PickErrorSheet.tsx` 를 신설한다. 골격은 `BodyProfilePromptModal.tsx`
를 그대로 따른다 (`Modal transparent animationType="slide"` + `backdrop` + `backdropTop`
Pressable + `sheet`) — 검증된 바텀시트 패턴 재사용, 새 시각 언어 발명 금지.

Props:
```
interface Props {
  failure: PickFailure | null;   // null = 미표시
  onClose: () => void;           // X / 백드롭 / native back 3-way 수렴
  onAction: (action: PickFailureAction) => void;
}
```
`visible={failure != null}` 로 제어하고, `failure` 가 null 이면 시트 내부는 렌더하지 않는다.

시트 구성 (Figma modal-sheet node 87:506 순서 그대로):
1. grab handle (BodyProfilePromptModal `styles.handle` 동일 규격)
2. 제목 행 — `typography.heading` 볼드 제목 + 우측 X 버튼
   (`Ionicons name="close"`, `size={24}`, `color={colors.textSecondary}`,
   `accessibilityRole="button"`, `accessibilityLabel="닫기"`, `hitSlop={10}`)
3. `failure.cause` — 회색 설명문 (`typography.caption` / `colors.textMid`)
4. 해결단계 — "이렇게 해보세요" 소제목 + `failure.steps` 목록. 각 행은
   브랜드색 점 불릿(원형 View, `backgroundColor: colors.brand`) + 본문 Text.
   번호 매기지 말 것 (Figma 는 점 불릿).
5. `failure.detail` 이 있을 때만 회색 정보카드 — 배경 `colors.softBg`,
   `borderRadius: radius.listItem`. 소제목 "오류 정보" + 본문에 detail 원문.
   `selectable` 을 켜서 belle 가 길게 눌러 복사·캡처할 수 있게 한다.
   detail 이 없으면 카드 자체를 렌더하지 않는다.
6. 하단 풀폭 브랜드 버튼 — `height: layout.ctaHeight`, `borderRadius: radius.button`,
   `backgroundColor: colors.brand`, 라벨 `failure.action.label` (`typography.button` /
   `colors.textWhite`). `onPress={() => onAction(failure.action)}`.
   `accessibilityRole="button"`, `accessibilityLabel={failure.action.label}`.

제약:
- 색·간격·반경은 전부 `app/src/theme` 토큰. **인라인 hex 금지.**
- 라이트 테마 전용. 이모지 금지. 문구 한국어.
- 긴 steps 로 소형 기기에서 시트가 넘칠 수 있으므로 본문 영역을 `ScrollView` 로 감싸고
  `maxHeight` 를 화면 비율로 제한한다 (하단 버튼은 스크롤 밖에 고정).
- `StyleSheet.create` 는 파일 하단.
  </action>
  <verify>
    <automated>npm --prefix app run typecheck && test $(grep -vE "^\s*//" app/src/components/PickErrorSheet.tsx | grep -cE "#[0-9A-Fa-f]{3,8}") -eq 0</automated>
  </verify>
  <done>typecheck 무오류. 인라인 hex 0건. accessibilityRole/accessibilityLabel/hitSlop 이 X 버튼과 액션 버튼에 모두 존재.</done>
</task>

<task type="auto">
  <name>Task 3: analyze.tsx 배선 — 인라인 error 제거, 알림창 단일 경로로 수렴</name>
  <files>app/src/app/(tabs)/analyze.tsx</files>
  <action>
`app/src/app/(tabs)/analyze.tsx` 만 수정한다. 다른 화면(result.tsx, index.tsx,
loading.tsx)은 **건드리지 말 것** — 범위 밖.

1. **상태 교체.** `const [error, setError] = useState<string | null>(null)` 과
   `const [permissionBlocked, setPermissionBlocked] = useState(false)` 를 제거하고
   `const [failure, setFailure] = useState<PickFailure | null>(null)` 하나로 대체한다.
   `describePickFailure` / 타입은 `../../lib/pickerFailure` 에서 import.

2. **`validate()` 반환 타입 변경.** 지금은 한국어 문자열을 반환한다 →
   `PickFailureKind | null` 을 반환하도록 바꾼다 (`'format'` / `'tooLarge'`).
   문구 소유권을 `pickerFailure.ts` 단일점으로 옮기는 것이 목적. `handleResult` 는
   `setFailure(describePickFailure(problem))` 로 받는다.

3. **호출부 치환** (기존 `errText()` 는 그대로 활용 — detail 인자로 넘긴다):
   - 카메라 권한 거부 → `setFailure(describePickFailure('permissionCamera'))`
   - 사진 권한 거부 → `setFailure(describePickFailure('permissionLibrary'))`
   - `pickFromLibrary` 의 Current/Automatic 양쪽 실패 →
     `setFailure(describePickFailure('libraryOpen', \`원본: ${errText(primaryErr)} / 변환: ${errText(fallbackErr)}\`))`
   - `pickFromLibrary` 바깥 catch → `describePickFailure('libraryOpen', errText(err))`
   - `pickFromCamera` 바깥 catch → `describePickFailure('cameraOpen', errText(err))`
   - `handleResult` 처리 실패(양쪽 picker) → `describePickFailure('processFailed', errText(handleErr))`
   - 성공 경로의 `setError(null)` → `setFailure(null)`
   - `backToModeSelect` 의 리셋도 `setFailure(null)` 하나로 정리

4. **이중 모달 금지 (기존 직렬 게이트 규칙 유지).** 실패는 `talkvPicked` / `lowQualityPicked` /
   `promptVisible` / `formVisible` 로 가기 **전에** 발생하고 전부 early return 이므로
   구조상 배타적이다. 이를 무너뜨리지 않게, `setFailure(...)` 하는 자리에서 보류 영상 상태
   (`talkvPicked` / `lowQualityPicked` / `pendingPicked`)를 새로 세팅하지 말 것.
   방어적으로 `PickErrorSheet` 의 `visible` 조건에 `talkvPicked == null && lowQualityPicked == null && !promptVisible && !formVisible` 를 AND 로 걸어
   어떤 경우에도 두 모달이 겹치지 않게 한다. **기존 모달 2종(카톡 압축 경고, 저화질 경고)
   과 프로필 권유 게이트의 동작은 변경 금지.**

5. **렌더 치환.** `{error && <Text style={styles.error}>{error}</Text>}` 블록과
   `{permissionBlocked && <Pressable onPress={() => Linking.openSettings()}>...}` 블록을
   삭제하고, 기존 모달들과 같은 층에 `<PickErrorSheet />` 를 추가한다:
   - `onClose={() => setFailure(null)}`
   - `onAction={(action) => { setFailure(null); if (action.kind === 'openSettings') Linking.openSettings(); }}`
     (`Linking` 은 이미 react-native 에서 import 되어 있다 — 신규 패키지 금지)

6. **정리.** 더 이상 참조되지 않는 `styles.error` / `styles.link` 를 삭제한다.
   `Linking` import 는 `onAction` 에서 계속 쓰므로 **유지**.
   `errText` 는 계속 쓰이므로 유지 (export 도 유지).
  </action>
  <verify>
    <automated>npm --prefix app run typecheck && test $(grep -c "setError\|permissionBlocked" "app/src/app/(tabs)/analyze.tsx") -eq 0 && grep -q "PickErrorSheet" "app/src/app/(tabs)/analyze.tsx" && grep -q "talkvPicked" "app/src/app/(tabs)/analyze.tsx"</automated>
  </verify>
  <done>typecheck 무오류. `setError`/`permissionBlocked` 잔존 0건. `PickErrorSheet` 렌더 존재. 카톡/저화질/프로필 모달 3종 JSX 가 그대로 남아 있음.</done>
</task>

</tasks>

<verification>
1. `node --test app/src/lib/pickerFailure.test.ts` — 매핑 순수 함수 전건 통과
2. `npm --prefix app run typecheck` — 무오류 (exhaustiveness `never` 가드 포함)
3. `grep -vE "^\s*//" app/src/components/PickErrorSheet.tsx | grep -cE "#[0-9A-Fa-f]{3,8}"` == 0 — 하드코딩 색 없음
4. `rtk git diff --stat` — 변경 파일이 아래 5개를 벗어나지 않을 것:
   `app/tsconfig.json`, `app/src/lib/pickerFailure.ts`, `app/src/lib/pickerFailure.test.ts`,
   `app/src/components/PickErrorSheet.tsx`, `app/src/app/(tabs)/analyze.tsx`
5. `git diff` 상 `package.json` / `package-lock.json` 변경 0 — 신규 의존성 금지 확인
</verification>

<human_verification>
실기기 확인은 즉시 belle 에게 올리지 말고 이 quick 디렉터리의 HUMAN-UAT 항목으로 적립한다
(배치 UAT 규칙).

1. 분석 탭 → 앨범에서 선택 → 실패하는 그 영상 선택 → **바텀시트 알림창**이 뜨는가
2. 알림창에 원인 설명 + 점 불릿 해결단계 + 회색 "오류 정보" 카드(원문)가 보이는가
   → **이 원문을 캡처해 둘 것.** iCloud 가설의 확정/기각 증거가 된다.
3. [설정 열기] 버튼 → iOS 설정 앱이 열리는가
4. 회귀: 정상 영상 선택 → 알림창 없이 기존대로 진행되는가
5. 회귀: 카톡 압축본(`_talkv_`) 선택 → 기존 카톡 경고만 뜨고 알림창과 겹치지 않는가
</human_verification>

<success_criteria>
- 영상 선택·업로드 실패 6종이 전부 바텀시트 알림창으로 표시된다 (인라인 캡션 0)
- 각 실패가 최소 1개 이상의 구체적 해결단계를 제시한다
- 권한 거부 / iCloud 의심 실패에서 설정 앱을 열 수 있다
- picker 원본 오류 문자열이 화면에 노출·복사 가능하다
- 기존 모달 3종(카톡·저화질·프로필 권유) 동작 불변, 이중 모달 0
- 신규 npm 의존성 0
</success_criteria>

<output>
Create `.planning/quick/260720-hn8-icloud/260720-hn8-SUMMARY.md` when done
</output>
