---
phase: 260724-q6b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/types/analysis.ts
  - app/src/lib/userAnalyses.ts
  - app/src/components/ScoreBreakdownSection.tsx
  - app/src/components/DeductionDetailSheet.tsx
  - app/src/app/analysis/result.tsx
autonomous: true
requirements:
  - IN-01
must_haves:
  truths:
    - "When result.attributionReliability.unreliable === true, the video overlay shows at most one orange 예상 부위 marker (never 8 confirmed red/yellow dots)."
    - "When unreliable, the score breakdown shows baseline + one aggregate line + = 종합 N점 (N = deductionBreakdown.final, byte-unchanged) instead of per-joint deduction rows."
    - "When unreliable, no per-joint coaching tip and no joint-naming correction headline are shown."
    - "When unreliable, the 확대비교 crop is still shown but labeled 예상 부위 (estimated), not a definitive fault."
    - "When unreliable, NO other surface asserts a specific joint from deductionBreakdown.records: the topFix '오늘 고칠 것' card, the '다른 감점 항목' collapsed cards, the SummaryCard '오늘 고칠 것' headline + '다음 행동', and the '심사 정보 코너' per-joint rows are all suppressed or routed to the aggregate statement (no joint name)."
    - "The 'AI 공부 중' guidance line appears exactly once, in the 동작비교 area, mode-aware (mode1 / mode3 progress / mode3 first)."
    - "When attributionReliability is absent or unreliable=false, the result screen renders identically to before (no regression for normal analyses)."
    - "overallScore, deductionBreakdown.final, and deductionBreakdown.records are byte-for-byte unchanged (presentation only)."
  artifacts:
    - path: "app/src/types/analysis.ts"
      provides: "AttributionReliability type + optional attributionReliability field on AnalysisResult"
      contains: "interface AttributionReliability"
    - path: "app/src/components/ScoreBreakdownSection.tsx"
      provides: "aggregateMode/aggregateText props for aggregate presentation"
      contains: "aggregateMode"
    - path: "app/src/app/analysis/result.tsx"
      provides: "attributionUnreliable gate downgrading ALL 8 per-joint claim surfaces + single mode-aware guidance line + module-level copy constants"
      contains: "attributionUnreliable"
  key_links:
    - from: "app/src/app/analysis/result.tsx"
      to: "result.attributionReliability?.unreliable"
      via: "gate constant read"
      pattern: "attributionReliability\\?\\.unreliable"
    - from: "app/src/lib/userAnalyses.ts"
      to: "result.attributionReliability"
      via: "defensive normalize passthrough"
      pattern: "attributionReliability"
---

<objective>
IN-01 — 역립(inverted / self-occluded) 저신뢰 자세에서 백엔드가 이미 방출하는
`result.attributionReliability.unreliable === true` 마커를 앱이 소비하게 한다. 이 경우
per-joint 귀속(어느 관절이 틀렸는지)이 신뢰 불가이므로 결과 화면의 **모든 per-joint 단정
표면**을 "확정(confirmed)" 에서 "예상(estimated)/집계(aggregate)" 로 강등한다. 그리고
동작비교 영역에 "AI 공부 중" 안내 1줄을 **정확히 한 번** 추가한다.

강등 대상 표면 = 총 8개 (모두 동일 `deductionBreakdown.records`/`tips` 를 읽어 per-joint
단정을 만드는 자리):
  LOCKED 4 (belle 명시): (1) 영상 오버레이 마커, (2) 점수 계산 내역 행, (3) 코칭 팁,
  (4) 확대비교 라벨.
  ADDITIONAL 4 (코디네이터 감사 — result.tsx grep 로 records/tips 판독 전수 확인):
  (5) topFix "오늘 고칠 것" DeductionCard, (6) "다른 감점 항목" 접힘 카드,
  (7) SummaryCard "오늘 고칠 것" 헤드라인 + "다음 행동", (8) "심사 정보 코너"(심사 시뮬)
  per-joint 감점 행.

INVARIANT (belle + 코디네이터): unreliable 일 때 특정 관절 CLAIM 표면이 하나도 남지
않는다. "확정 per-joint" → 집계/예상 강등하거나 억제하며, 절대 특정 관절을 단정하지
않는다. 점수 값(overallScore/final/records)은 byte-불변.

Purpose: belle 확정 원칙 — 저신뢰 시 가치를 삭제하지 않고, 거짓 per-joint 단정도 하지
않는다. 확신하는 것(점수·비교·성장·잘한 점)을 앞세우고, per-joint 는 "예상" 으로 강등한다.
백엔드(main d5490a8)는 이미 마커를 방출하고 unreliable 시 per-joint 팁 재조립을 건너뛰지만
(assemble.rebuild_tips_for_vision_fault early-return), 앱이 아직 이 필드를 소비하지 않아
모든 per-joint 귀속이 사용자에게 그대로 샌다. 이 작업이 앱 소비를 배선한다.

Output: 계약 타입 1개, ScoreBreakdownSection aggregate 모드, result.tsx 게이팅 + 상수.

HARD INVARIANTS (belle LOCKED):
- overallScore / deductionBreakdown.final / deductionBreakdown.records = byte-for-byte 불변.
  이 작업은 **표현 전용** — 점수 로직 0 변경.
- 마커/카피 문자열은 전부 module-level SCREAMING_SNAKE_CASE 상수로 추출 (belle 가 로직
  무접촉으로 문구 재조정 가능). 추가 표면(Task 4)은 신규 카피를 만들지 않고 기존 aggregate
  상수/억제 경로로 라우팅한다.
- design.md 테마 토큰만 (src/theme). 색/여백 하드코딩 금지 (app/CLAUDE.md). 주황 "예상"
  톤은 신규 색 금지 — 기존 `colors.advisoryOrange` / `colors.advisoryOrangeBg`
  (attentionKeypoints 가 이미 쓰는 톤) 재사용.
- 라이트 테마 전용. 이모지 0. 한국어 사용자 카피 / 영어 식별자.
- attributionReliability 부재 또는 unreliable=false 인 일반 케이스 = 렌더 diff 0 (무회귀).

감사 결과 — 의도적으로 강등하지 않는 표면 (belle 확인용, per-joint "단정"이 아니라 코치/행동
라우팅이므로 유지):
- 강사 질문("강사님께 물어보기", 8번 섹션 combinedCoachQuestions) — CLAIM 이 아니라 코치에게
  가져갈 질문. 저신뢰 시 코치 안내는 오히려 바람직([[product-role-interpret-act-or-ask-coach]]).
- 보완 운동 이유(exerciseReason) — "이 운동을 하면 좋아져요" 행동 처방. per-joint 단정 아님.
  둘 다 유지가 원칙에 부합하나, belle 이 원하면 후속에서 확장 가능(스코프 밖).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@app/CLAUDE.md
@app/src/types/analysis.ts
@app/src/components/ScoreBreakdownSection.tsx
@app/src/components/DeductionDetailSheet.tsx
@app/src/components/SummaryCard.tsx
@app/src/lib/summarySource.ts
@app/src/app/analysis/result.tsx

# 백엔드 방출 shape (읽기 전용 — 변경 금지, 이미 배포됨 main d5490a8):
# backend/functions/pipeline/app.py:2282-2324 (_assess_attribution_reliability)
#   방출 dict = {unreliable: bool, geminiSilent: bool, overTolJointCount: int,
#                visibility: float|null, dtwDistance: float|null,
#                aggregateStatement?: str}  ← aggregateStatement 는 unreliable=True 시에만
#   aggregateStatement 실제 값 = "전체 자세가 정은지 선수보다 덜 정돈된 편이에요."
# backend/functions/pipeline/app.py:5696-5702 (result["attributionReliability"] = marker,
#   unreliable=True 일 때만 실림 — reliable/부재 doc 은 byte-동일)
# backend/shared/python/sunity_shared/analysis/assemble.py:817-819 (unreliable 시 per-joint
#   팁 재조립 skip — 앱 tips 는 이미 generic)
</context>

<tasks>

<task type="auto">
  <name>Task 1: 계약 타입 AttributionReliability + 방어 normalize passthrough</name>
  <files>app/src/types/analysis.ts, app/src/lib/userAnalyses.ts</files>
  <action>
app/src/types/analysis.ts 에 새 인터페이스 `AttributionReliability` 추가 — 파일의 manual-
mirror 관례(SpotCheck/CoachAudio 블록처럼 Python lockstep 을 주석에 명시)를 그대로 따른다.
필드는 백엔드 방출과 정확히 미러: `unreliable: boolean`, `geminiSilent: boolean`,
`overTolJointCount: number`, `visibility: number | null`, `dtwDistance: number | null`,
`aggregateStatement?: string` (unreliable=True 시에만 실림 — optional). 블록 상단 주석에
Python lockstep 경로를 명시: `backend/functions/pipeline/app.py _assess_attribution_reliability
(2282-2324) 방출 형상 + 5696-5702 result 부착 (unreliable=True 일 때만)`. 그리고 SpotCheck?
필드가 붙는 방식과 동일하게 `AnalysisResult` 타입 본문(730~ 라인의 교차 타입 객체)에
`attributionReliability?: AttributionReliability` 를 additive optional 로 추가 — 주석에
"OPTIONAL/legacy-doc 호환 (부재 = 정상 동작 — 역립 저신뢰에서만 방출). 앱 표현 강등 전용,
점수 무접촉" 명시.

app/src/lib/userAnalyses.ts 에 `normalizeSpotCheck`(297~) 패턴을 미러한
`normalizeAttributionReliability(value: unknown): AttributionReliability | undefined`
방어 파서 추가: 객체 아니거나 `unreliable`/`geminiSilent` 가 boolean 아니면 undefined 반환
(malformed doc 렌더 크래시 0). visibility/dtwDistance 는 finite number 아니면 null 로 정규화,
overTolJointCount 는 finite number 아니면 0, aggregateStatement 는 non-empty string 일 때만
보존. normalize() 본체(spotCheck/coachAudio 가드가 붙는 라인대 — deductionBreakdown 가드
근처)에서 `if (result?.attributionReliability !== undefined) { result = { ...result,
attributionReliability: normalizeAttributionReliability(result.attributionReliability) }; }`
로 배선. IN-01 per user LOCKED design.
  </action>
  <verify>
    <automated>cd app && npm run typecheck</automated>
  </verify>
  <done>AttributionReliability 타입이 계약에 존재하고 AnalysisResult.attributionReliability? 로 노출됨. normalize 가 malformed 를 undefined 로 접고 정상 shape 는 통과시킴. typecheck clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ScoreBreakdownSection aggregate 모드 prop</name>
  <files>app/src/components/ScoreBreakdownSection.tsx</files>
  <behavior>
    - aggregateMode 미전달(기존 caller): records.map(...) 그대로 렌더, 렌더 diff 0 (mode1/mode3 무회귀).
    - aggregateMode=true + aggregateText 전달: baseline 행 → 단일 aggregate 행(aggregateText) → "= 종합 {breakdown.final}점" 행. records.map 미실행, "번호는 위 영상의 빨간 점..." 각주 미표시.
    - aggregateMode=true 여도 breakdown.final 값은 그대로 표기 (점수 불변).
  </behavior>
  <action>
컴포넌트 props 에 `aggregateMode?: boolean` 과 `aggregateText?: string` 두 optional 추가
(limitNotice/onRecordPress 옆). aggregateMode 가 truthy 일 때: (1) `breakdown.records.map(...)`
블록과 그 empty-state 분기 전체를 `aggregateText` 한 줄 렌더로 치환 — 새 스타일
`aggregateLine`(기존 `basisLine` 토큰 패턴 mirror: typography.caption + colors.textSecondary,
색 하드코딩 금지)로 `<Text style={styles.aggregateLine}>{aggregateText}</Text>`. (2)
`hasAnyNumber` 번호-매핑 각주(`번호는 위 영상의 빨간 점 위치와 같아요.`)를 aggregateMode 시
숨김(번호 자체가 없으므로). baseline 헤더 행과 `= 종합 {breakdown.final}점` finalRow 는 두
분기 공통으로 유지. limitNotice footnote 는 aggregateMode 와 무관하게 기존대로 렌더. 기존
caller(result.tsx mode1/mode3 정상 경로)는 두 prop 미전달 → 렌더 diff 0. 토큰만, 이모지 0.
IN-01 per user LOCKED design.
  </action>
  <verify>
    <automated>cd app && npm run typecheck</automated>
  </verify>
  <done>aggregateMode/aggregateText 전달 시 records 행 대신 단일 aggregate 행 + baseline + 종합 final 렌더. 미전달 시 기존 records 렌더 그대로. typecheck clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: result.tsx attributionUnreliable 게이트 — LOCKED 4표면 강등 + 안내 1줄 + 확대비교 라벨</name>
  <files>app/src/app/analysis/result.tsx, app/src/components/DeductionDetailSheet.tsx</files>
  <behavior>
    - attributionUnreliable=true: leftOverlay KeypointOverlay 가 confirmed 빨강 점 0개, groupMarkers 0, markerNumbers {}, forceHighlightWorstCount 0, fullscreenLegend/timelineTicks 빈 배열; attentionKeypoints 는 최대 1개(최대편차 관절) 주황 점만.
    - attributionUnreliable=true: ScoreBreakdownSection 이 aggregateMode 로 렌더(record 행 억제, = 종합 final 유지).
    - attributionUnreliable=true: displayTips 에서 per-joint 팁(tip.joint != null) 제거, '먼저 교정할 점' vetoLead 카드 숨김, correctionPoint=null(헤드라인이 관절명 미포함), ScoreContext 아래 vetoPrimaryFault 표기 블록 숨김.
    - attributionUnreliable=true: 확대비교 크롭은 유지되 "예상 부위" 라벨(advisoryOrange)이 붙음.
    - 동작비교 영역에 mode-aware 안내 1줄이 정확히 1회 렌더(mode1/mode3-progress/mode3-first).
    - attributionUnreliable=false/부재: 위 전부 기존 값으로 렌더 → 렌더 diff 0.
    - overallScore/deductionBreakdown.final/records 표기 값 불변.
  </behavior>
  <action>
result.tsx 상단(다른 카피 상수들, 123~155 라인대)에 module-level SCREAMING_SNAKE 상수 추가:
`ATTR_GUIDANCE_MODE1 = '거꾸로 자세는 관절 하나하나까진 AI가 아직 공부 중이에요. 정은지 선수
영상을 자세히 비교해보세요.'`, `ATTR_GUIDANCE_MODE3_PROGRESS = '점수 기준으로 이전보다 발전
하고 있어요. 거꾸로 자세 세부 관절은 AI가 아직 공부 중이에요.'`, `ATTR_GUIDANCE_MODE3_FIRST =
'첫 분석이에요 — 다음부터 발전을 비교해드려요. 거꾸로 자세 세부는 AI가 공부 중이에요.'`,
`ATTR_SCORE_AGGREGATE_FALLBACK = '거꾸로 자세라 관절별 감점 위치는 추정이에요. 종합 점수는
그대로예요.'`, `ATTR_ZOOM_ESTIMATED_LABEL = '예상 부위'`.

(A) 게이트: 컴포넌트 본문(cleanPass/correctionPoint 인근, 1030~1172 라인대)에
`const attributionUnreliable = result.attributionReliability?.unreliable === true;` 추가.
(Task 4 도 이 동일 상수를 재사용 — 신규 게이트 금지.)

(B) 예상 부위 단일 관절: `const estimatedAreaKeypoints = useMemo<KeypointName[]>(() => {
if (!attributionUnreliable) return []; ... }, [...])` — deductionBreakdown.records 중
|points| 최대이면서 criterion 이 KEYPOINT_FROM_ANGLE_KEY 로 매핑되는 관절 1개를 고른다
(폴백: windowMedianAngleDeltas 최대 |delta_deg|). 매핑되는 관절이 없으면 빈 배열(점 0개).
기존 KEYPOINT_FROM_ANGLE_KEY(53 라인 import) 재사용, 신규 맵 금지.

(C) 오버레이 마커 강등 — leftOverlay 의 KeypointOverlay(2047~2080)에 넘기는 값을 게이트 파생으로
치환. attributionUnreliable 이면: highlightKeypoints=[] (confirmedKeypointList 대신),
attentionKeypoints=estimatedAreaKeypoints, groupMarkers=[], markerNumbers={},
forceHighlightWorstCount=0. 그리고 VideoCompare 의 fullscreenLegend/timelineTicks 도
attributionUnreliable 이면 빈 배열(번호가 사라졌으므로 범례/틱 모순 방지). 삼항으로 인라인
치환하거나 위에서 `const overlayHighlightKeypoints = attributionUnreliable ? [] :
confirmedKeypointList` 식 파생 상수 5~7개를 만들어 배선(가독). rightOverlay(정은지 측)는
무접촉. cueWindows/audio 는 이 작업 범위 밖 — 무접촉.

(D) 점수 계산 내역 aggregate — ScoreBreakdownSection 호출(2249~2255)에 `aggregateMode={
attributionUnreliable}` 와 `aggregateText={result.attributionReliability?.aggregateStatement
?? ATTR_SCORE_AGGREGATE_FALLBACK}` 추가. recordNumbers/basisLine/onRecordPress 는 유지(
aggregateMode 가 내부에서 record 렌더를 억제하므로 무해). breakdown prop 자체(final 포함)
불변.

(E) 코칭 팁 강등 — displayTips memo(1418~1425)에 attributionUnreliable 분기 추가:
unreliable 이면 per-joint 팁(tip.joint != null) 제거하고 generic 만 남긴다(기존 CONTRADICTORY
필터와 결합). '먼저 교정할 점' vetoLead 카드 게이트(2296 `!cleanPass && vetoApplied &&
vetoPrimaryFault ?`)에 `&& !attributionUnreliable` 추가로 숨김. correctionPoint(1171~1172)를
`const correctionPoint = attributionUnreliable ? null : (vetoPrimaryFault ?? result.tips[0]?.
title ?? null);` 로 변경(ScoreContext 헤드라인이 관절명 미포함). ScoreContext 아래
vetoPrimaryFault 표기 블록(2226~2230 `AI 영상 분석에서 발견한 점:`)도 attributionUnreliable
이면 숨김(관절 단정 방지) — 게이트에 `&& !attributionUnreliable` 추가.

(F) 확대비교 라벨 — DeductionDetailSheet(app/src/components/DeductionDetailSheet.tsx)에
`estimatedArea?: boolean` optional prop 추가: true 일 때 크롭 이미지 위/캡션에 기존 halfLabel/
caption 렌더 경로를 재사용해 `ATTR_ZOOM_ESTIMATED_LABEL`("예상 부위") 배지를
colors.advisoryOrange / colors.advisoryOrangeBg 토큰으로 표기(신규 색 금지, refMatchFailed
캡션 패턴 mirror). 이미지·수치·비교는 삭제하지 않음(크롭 유지). result.tsx 의
DeductionDetailSheet 호출(2733~2746)에 `estimatedArea={attributionUnreliable}` 전달. topFix
카드 "확대 비교 자세히 보기 ›" 링크 텍스트(1981)는 attributionUnreliable 이면 "예상 부위
확대 비교 ›" 로 치환(문구는 인라인 삼항, 상수화 불요 — 짧은 label). 시트가 실제 라벨 소유.

(G) 안내 1줄(유일 인스턴스) — 동작비교 그룹 종료 직후(mode3-first 안내 블록 2158~2162 다음,
2162 라인 뒤)에 단 하나의 렌더 지점 추가:
`{attributionUnreliable ? <Text style={styles.mode3LimitNotice}>{cmp.mode === 'mode1' ?
ATTR_GUIDANCE_MODE1 : cmp.isFirst ? ATTR_GUIDANCE_MODE3_FIRST : ATTR_GUIDANCE_MODE3_PROGRESS}
</Text> : null}`. 이 위치는 mode1/mode3-progress/mode3-first 세 경로 모두에서 정확히 1회
렌더된다(동작비교 header 게이트 밖). 기존 mode3LimitNotice 스타일 토큰 재사용(신규 스타일
불요). "AI 공부 중" 표현이 화면 전체에서 이 한 곳에만 존재하도록, 다른 곳에 추가 금지.

전체: 점수 값(overallScore/final/records) 표기 무접촉. 토큰만. 이모지 0. IN-01 per user
LOCKED design. attributionUnreliable=false/부재 경로는 모든 삼항이 else 가지로 기존 값 →
렌더 diff 0.
  </action>
  <verify>
    <automated>cd app && npm run typecheck</automated>
  </verify>
  <done>attributionUnreliable 게이트가 LOCKED 4표면(오버레이 마커/점수 내역/코칭 팁/확대비교 라벨)을 강등하고 동작비교에 mode-aware 안내 1줄을 정확히 1회 렌더. unreliable=false/부재 시 렌더 diff 0. 점수 표기 값 불변. typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 4: result.tsx — 나머지 per-joint 단정 표면 4개 강등 (동일 attributionUnreliable 게이트)</name>
  <files>app/src/app/analysis/result.tsx</files>
  <behavior>
    - attributionUnreliable=true: topFix "오늘 고칠 것" DeductionCard(per-joint statusLine 헤드라인) 미렌더. 잘못된 "감점 항목이 없어요" clean 카드로도 폴백하지 않음(records 있으니 cleanPass=false → clean 분기 미충족 → null).
    - attributionUnreliable=true: "다른 감점 항목" 접힘 카드 목록(records.map DeductionCard) 미렌더.
    - attributionUnreliable=true: SummaryCard "오늘 고칠 것" 헤드라인이 관절명을 담지 않음(집계 문장으로 라우팅), "다음 행동" pill 숨김. praise("잘한 점")·점수 배지는 유지(확신 표면, 리드).
    - attributionUnreliable=true: "심사 정보 코너"(심사 시뮬) per-joint 감점 행 섹션 전체 미렌더.
    - attributionUnreliable=false/부재: 위 네 곳 모두 기존 값 그대로 → 렌더 diff 0.
    - deductionBreakdown.records/final/overallScore 값 불변(표현만 억제).
  </behavior>
  <action>
Task 3 이 정의한 동일 `attributionUnreliable` 상수를 재사용한다(신규 게이트/상수 금지, 신규
카피 금지 — 기존 aggregate 문장으로 라우팅).

(A) topFix "오늘 고칠 것" 카드 — 렌더 가드(1953 `{isVisible('topFix') && topFixRecord ?`)에
`&& !attributionUnreliable` 추가. 이때 삼항 체인의 `: variantOf('summary') === 'clean' ?`
분기는 cleanPass=false(역립은 records 8개 보유 → isCleanPass=false)라 미충족 → 최종 null 로
렌더된다("감점 항목이 없어요" clean 카드 오표기 없음 — 검증필: cleanPass=isCleanPass(
deductionBreakdown)=false). topFix 를 완전 억제해도 점수는 아래 ScoreBreakdownSection
aggregate + 안내 1줄이 대신 전달하므로 정보 손실 아님.

(B) "다른 감점 항목" 접힘 카드 — 렌더 가드(2173 `{isVisible('collapsed') && records.length
> 1 && records.some(...) ?`)에 `&& !attributionUnreliable` 추가로 목록 전체 억제.

(C) SummaryCard todayFix/nextAction 라우팅 — SummaryCard 호출(1907~1914) prop 두 개 교체:
`todayFix={attributionUnreliable ? { headline: result.attributionReliability?.aggregateStatement
?? ATTR_SCORE_AGGREGATE_FALLBACK, criterion: '' } : summaryContent.todayFix}` 와
`nextAction={attributionUnreliable ? null : summaryContent.nextAction}`.
근거: SummaryCard 는 todayFix=null 시 `TODAY_NONE`('오늘 크게 고칠 점은 없어요')로 폴백하는데
이는 "고칠 것 없음=clean" 오인이라 사용 금지 → 대신 기존 백엔드 aggregateStatement(또는
ATTR_SCORE_AGGREGATE_FALLBACK) 를 SummaryTodayFix 형상 {headline, criterion:''} 으로 감싸
"오늘 고칠 것" 헤드라인을 관절명 없는 집계 문장으로 만든다(신규 카피 0). nextAction 은
selectNextAction 이 record cueLine(관절-행동)을 담으므로 null 로 전달 → SummaryCard 의 기존
`{nextAction ? ... : null}` 가드가 pill 을 숨긴다. praise/score 배지는 그대로 전달(잘한 점은
집계 표면 — 리드 유지). onPressTodayFix/onPressExpand 콜백은 그대로(점프 대상 topFixKey 가
억제된 카드라 no-op — 무해).

(D) "심사 정보 코너" 심사 시뮬 — 렌더 가드(2598 `{isVisible('judgeInfo') ?`)에
`&& !attributionUnreliable` 추가로 섹션(제목+카드+심사 환산 점수 행) 전체 억제. 이 섹션은
records.map 으로 `rec.statusLine ?? criterionLabelKo(rec.criterion)` per-joint 결함 행을
그리므로 unreliable 시 특정 관절 단정이 된다 → 억제가 원칙 정합.

전체: 네 가드 모두 `attributionUnreliable` 단일 상수 파생. 점수 값 무접촉. 토큰만. 이모지 0.
IN-01 per coordinator audit expansion. false/부재 경로는 `&& !attributionUnreliable` 가 true
로 붕괴 → 기존 렌더 그대로(diff 0).
  </action>
  <verify>
    <automated>cd app && npm run typecheck</automated>
  </verify>
  <done>unreliable 시 topFix 카드·"다른 감점 항목"·심사 정보 코너가 미렌더되고 SummaryCard "오늘 고칠 것"이 집계 문장(관절명 없음)+"다음 행동" 숨김. clean 카드 오표기 없음. false/부재 시 diff 0. 점수 값 불변. typecheck clean.</done>
</task>

</tasks>

<verification>
- `cd app && npm run typecheck` (tsc --noEmit) clean — 유일 static 게이트.
- grep 무회귀 확인: overallScore/deductionBreakdown.final/records 값 대입/변형 코드 0 추가
  (표현 전용 invariant). `grep -n "overallScore =" app/src/app/analysis/result.tsx` 신규 0.
- per-joint 표면 전수 게이트 확인 — 아래 8개 렌더 지점이 전부 `attributionUnreliable`(또는
  그 파생 displayTips/estimatedAreaKeypoints/aggregateMode)로 강등됨을 grep 로 확인:
  (1) leftOverlay KeypointOverlay highlight/marker props, (2) ScoreBreakdownSection aggregateMode,
  (3) displayTips 필터 + vetoLead 카드 + correctionPoint + vetoPrimaryFault 표기,
  (4) DeductionDetailSheet estimatedArea, (5) topFix 카드 가드, (6) 접힘 "다른 감점 항목" 가드,
  (7) SummaryCard todayFix/nextAction 라우팅, (8) judgeInfo 심사 시뮬 가드.
  `grep -c "!attributionUnreliable\|attributionUnreliable ?" app/src/app/analysis/result.tsx`
  로 게이트 사용처가 존재하는지 확인(정확 개수는 인라인 vs 파생 상수 구현 방식에 따라 가변).
- "AI 공부 중" 안내 렌더 지점이 정확히 1곳: ATTR_GUIDANCE_MODE* 를 소비하는 JSX 블록 1개.
- FINAL GATE(오케스트레이터 담당, 실행자 범위 아님): 시뮬레이터에서 unreliable 케이스 렌더
  확인([[verify-ui-on-simulator-before-ota]]) — 8점 대신 예상 주황 점 최대 1개; 점수 내역
  aggregate; per-joint 팁·먼저 교정할 점·topFix "오늘 고칠 것" 관절 단정·"다른 감점 항목"·
  심사 정보 코너 관절 행 없음; SummaryCard "오늘 고칠 것"=집계 문장; 확대비교 "예상 부위"
  라벨; 안내 1줄. OTA 는 belle 확인 후.
</verification>

<success_criteria>
- 계약에 AttributionReliability 타입 + AnalysisResult.attributionReliability? 존재.
- normalize 가 attributionReliability 를 방어적으로 통과(malformed → undefined).
- ScoreBreakdownSection aggregate 모드 동작(record 행 억제, final 유지), 미전달 무회귀.
- attributionUnreliable 게이트가 per-joint 단정 표면 8개(LOCKED 4 + ADDITIONAL 4) 전부를
  강등/억제 + 안내 1줄(정확히 1회) + 확대비교 "예상 부위" 라벨. 특정 관절 CLAIM 잔존 0.
- SummaryCard "오늘 고칠 것"이 unreliable 시 관절명 없는 집계 문장(신규 카피 0, 기존
  aggregateStatement/ATTR_SCORE_AGGREGATE_FALLBACK 재사용), "다음 행동" 숨김.
- unreliable 부재/false 인 일반 분석 = 렌더 diff 0.
- overallScore/deductionBreakdown.final/records 표기 값 byte-불변.
- 모든 신규 카피 = module-level SCREAMING_SNAKE 상수. 색/여백 = 테마 토큰(advisoryOrange 재사용).
- typecheck clean.
</success_criteria>

<output>
Create `.planning/quick/260724-q6b-in-01-attributionreliability-ai/260724-q6b-SUMMARY.md` when done
</output>
