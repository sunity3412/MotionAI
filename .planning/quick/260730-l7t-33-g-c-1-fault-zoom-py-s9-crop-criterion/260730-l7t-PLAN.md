---
phase: quick-260730-l7t
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/shared/python/sunity_shared/analysis/fault_zoom.py
  - backend/shared/python/sunity_shared/analysis/reference_anchors.py
  - backend/judging_data/reference_anchors/ref-power-spin.yaml
  - backend/judging_data/reference_anchors/README.md
  - backend/functions/pipeline/app.py
  - backend/tests/phase33/test_reference_anchors.py
  - backend/tests/phase33/test_criterion_vertex_crop.py
  - backend/tests/phase33/test_angle_bake.py
  - backend/tests/phase33/test_zoom_join_joint_exact.py
  - app/src/types/analysis.ts
  - docs/contract.md
autonomous: true
requirements: [S9, S8, F-3, M-1, M-2]
must_haves:
  truths:
    - "criterion 카드의 crop 중심 = 그 criterion 이 계측한 꼭짓점(관절/겨드랑이/골반중점)이고 두 패널 정중앙에 온다"
    - "두 패널의 crop 한 변 px 이 동일하다 (같은 배율)"
    - "팔꿈치 criterion 이 손(hand)을 짚지 않는다"
    - "어깨류·힙류 criterion 카드에 팔 선+옆구리 선+호 각도 표시가 두 패널 동일 기하로 베이크된다"
    - "기준측 각도/꼭짓점을 세울 수 없으면 두 패널 모두 각도를 그리지 않는다 (비대칭 마킹 0)"
    - "앱이 두 패널의 실영상 초를 백엔드 값으로 받는다 (참고코너 페어 = crop 과 같은 순간)"
    - "모든 규칙이 동작별 데이터(criterion·관절명·report.joints·anchor yaml)로 키잉된다 — 동작명 분기 0"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/reference_anchors.py"
      provides: "기준 앵커 주석 로더 (순수, fail-closed)"
      exports: ["load_reference_anchors", "resolve_anchor_joint_xy"]
    - path: "backend/judging_data/reference_anchors/ref-power-spin.yaml"
      provides: "승인 7R 기준 패널 근거의 첫 모션 주석"
      contains: "angle_vs_reference__left_shoulder"
    - path: "backend/judging_data/reference_anchors/README.md"
      provides: "10동작 주석 절차(§C-4 이관분) + 스키마 계약"
    - path: "backend/tests/phase33/test_criterion_vertex_crop.py"
      provides: "정중앙·동일배율·인접매핑금지 회귀 게이트"
    - path: "backend/tests/phase33/test_angle_bake.py"
      provides: "겨드랑이 t=0.15 known-answer + 대칭 게이트"
    - path: ".planning/quick/260730-l7t-33-g-c-1-fault-zoom-py-s9-crop-criterion/sweep_angle_crop.py"
      provides: "등재 10동작 일반화 스위프 하네스 (로컬, 산출 PNG 열람용)"
  key_links:
    - from: "backend/functions/pipeline/app.py"
      to: "fault_zoom.build_fault_zoom_comparisons"
      via: "motion_id + criterion_units 전달"
      pattern: "motion_id="
    - from: "backend/shared/python/sunity_shared/analysis/fault_zoom.py"
      to: "reference_anchors.load_reference_anchors"
      via: "기준측 관절 대입 해석"
      pattern: "reference_anchors"
    - from: "app/src/types/analysis.ts"
      to: "fault_zoom item 방출부"
      via: "userVideoSec/refVideoSec 3-way lockstep"
      pattern: "refVideoSec"
---

<objective>
33-G-MOCKUP-DIFF.md §C 수리 순서 **1번(백엔드)** 을 실행한다. 승인 목업 7R 대비 FAIL 판정
3건을 `fault_zoom.py` 산출 측에서 닫는다:

- **S9 / M-2** — crop 중심을 criterion 꼭짓점 관절 **정중앙**으로, 두 패널 **같은 배율**로.
  `CRITERION_REGION`/`REGION_MEMBERS` region 키잉은 **보조(캡션 grouping)** 로 강등하고,
  belle #7·#9 "팔꿈치인데 손을 집고 있음"의 실 원인인 인접 매핑
  (`_KISMAM_TO_KEYPOINT` elbow→hand)을 제거한다.
- **S8 / M-1** — 각도 표시 베이크(팔 선 + 옆구리 선 + 호). 학생 = kp 선-쌍 규칙
  (겨드랑이 = shoulder→hip t=0.15, 7R 승인 규칙을 무릎/팔꿈치/힙 계열로 확장 정의),
  기준 = **모션당 1회 수동 주석**(스키마·로더·소비 + 승인 7R 근거 1모션 시딩).
- **F-3** — 참고코너/paircap 페어가 crop 과 같은 순간을 가리키도록 **실영상 초**를
  백엔드가 방출(학생·기준 양측). 앱이 rep fps 로 추정하던 타임베이스 오독 제거.

Purpose: belle 확인 ② 반려의 근본원인은 "코드 기준 검증, 승인 목업 대비 대조 누락"(D-40).
이 플랜은 목업 스펙 표(S8·S9·F-3)를 **산출물(생성 PNG·JSON 값)** 로 되받는 것까지가 완료다.

Output: `fault_zoom.py` 수리 + 기준 앵커 주석 계약(`backend/judging_data/reference_anchors/`)
+ 등재 10동작 일반화 스위프 증거(생성 PNG 열람 기록).

## 수리 사이클 규칙 (위반 = blocking)

- **재논의 없음 (D-39)** — 스펙 = 승인 목업 7R + EVIDENCE §9. belle 에게 질문 금지.
  본 플랜의 `## 자체 도출 결정` 절이 "승인본이 정답 / 수리에 새 범위 금지" 원칙으로
  잔여 판단을 이미 확정했다. 실행 중 새 판단이 필요하면 같은 원칙으로 도출·SUMMARY 기록.
- **single-motion-fixation 금지 (D-41)** — 전 규칙은 criterion id·관절명 접미사·
  `report["joints"]` capability·anchor yaml 로 키잉. 동작명 문자열 분기 0. 완료 전
  등재 10동작 스위프 확인 (Task 3).
- **code-only-verification 금지 (D-40)** — 각 태스크 verify 는 pytest 통과 + **생성
  PNG 열람 / 로그 수치 확인**까지.
- **채점 무접촉 (D-44)** — 점수·산식·임계 byte 불변. 전 변경은 display 경로
  (`fault_zoom` = complete 이후 사후 렌더). `deductionBreakdown`/veto/게이트에 값 역류 0.
- **배포 없음 (D-45)** — OTA·belle 확인은 전 항목 수리 + Pod 재스위프 후 1회 일괄.

## 자체 도출 결정 (D-39 — belle 미질문, 승인본 근거 명시)

| # | 판단 | 근거 |
|---|---|---|
| L-1 | criterion 카드 crop 한 변 = `_CRITERION_CROP_FRAC = 220/360` (신규 상수, criterion 카드 전용). legacy/advisory/mode3 는 `_CROP_FRAC`(0.42) byte-불변 | 승인 7R 자산이 360×640 프레임의 **220px 크롭**(`mockups/index.html` 619-630행). 0.42 를 쓰면 승인본보다 좁아 팔 선(64px)·옆구리 선(85px)이 패널을 벗어남. display 전용 상수 = `_MIN_LEG_VEC_PX` 선례, calibration-source-hard-gate 대상 아님 |
| L-2 | "같은 배율" = 두 패널 **동일 px 한 변**. 값 = `round(min(두 프레임의 짧은 변) * _CRITERION_CROP_FRAC)` 1회 산출 후 양측 공용 | 4R#1 "두 패널 모두 꼭짓점 = 패널 정중앙(180,180)·같은 배율". 두 프레임 짧은 변의 min 을 쓰면 어느 프레임도 초과하지 않음 |
| L-3 | 꼭짓점이 프레임 경계 근처면 `_crop_box` 의 안쪽 shift 를 **적용하지 않고 흰 패딩**. shift 하면 꼭짓점이 정중앙을 벗어남 | S9 "정중앙"이 스펙. 흰 캔버스 = `_full_frame_fit` 선례 |
| L-4 | 각도 선 길이·호 반경 = _OUT 비율 고정 (팔 64/360 · 옆구리 85/360 · 호 16/360), 흰 halo + 브랜드 코어 | 7R 확정 기하("팔 선 64px·옆구리 선 85px 유지, 호 반경 27→16"). halo = 승인 자산 육안 + 목업 CSS `.legfx polyline.halo`. **선은 관절까지 잇지 않고 방향으로 고정 길이** — 7R 이 그렇게 그렸고, distal kp 의 crop-내 포함 여부에 둔감해져 두 패널 기하 통일이 성립 |
| L-5 | 기준 앵커 주석 = **정적 좌표가 아니라 "모션당 1회 관절 대입 선언"**(`joint_substitutions`) | 정적 좌표는 주석한 그 프레임에서만 유효한데 기준 표시 프레임은 DTW 실측 순간(S11 PASS, 학생마다 다름)이라 충돌한다. 대입 선언은 실 kp 를 따라가 어느 프레임에서도 유효 = 환각 드로잉 0. belle 요구("모션당 1회 수동 주석 저장, 런타임 자동화 불필요")를 문자 그대로 충족 |
| L-6 | 기준 report 가 criterion 꼭짓점 관절을 못 주고 대입 선언도 없으면 → **인접 관절로 대체하지 않는다.** 결과적으로 기존 D-12 ② (한 측 전신 폴백 → 카드 미방출) 가 그 카드를 떨군다 | 인접 대체가 정확히 belle #7·#9 의 원인. "틀린 조언보다 없는 게 낫다"(D-43·D-14 동원리)의 crop 판. 떨어진 카드는 §C-4 주석 채움으로 복귀 |
| L-7 | 각도 베이크 실제 커버리지 = 어깨류(대입 선언 필요) · 힙류(기준 8kp 자동 성립) · 다리 사이각(S10 기존). 무릎·팔꿈치류는 기준 report(phase4_v1 = legacy 8kp)에 ankle/elbow 부재라 **대입 선언이 채워질 때까지 원 마커 유지**(대칭) | 승인본은 "두 패널 동일" 기하가 조건. 한쪽만 그리면 M-4(비대칭) 재발. 기준 라이브러리 12kp 재처리는 별개 범위(수리에 새 범위 금지) |
| L-8 | 앱측 region-first 조인 강등(`deductionLabels.ts`)·paircap 초 렌더·시트 재구성은 이 플랜 밖 = §C-2(앱). 본 플랜은 앱이 필요한 **값**(criterion·userVideoSec·refVideoSec)까지 방출 | 오케스트레이터 제약("백엔드 산출 측만") + §C 순서 |
| L-9 | 10모션 × criterion 주석 **값 채우기**는 기준 프레임 실물 열람이 필요(S3/Pod) → §C-4 Pod 재스위프에 이관. 본 플랜은 스키마·로더·소비·절차 문서 + 승인 7R 근거 1모션 시딩까지 | 오케스트레이터 제약(Pod OFF, "로컬로 가능한 범위까지"). 미주석 모션은 fail-closed(L-6/L-7) — 조용한 누락 아님, README 에 잔여 목록 박제 |
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-result-trust-recovery/33-G-MOCKUP-DIFF.md
@.planning/phases/33-result-trust-recovery/.continue-here.md
@backend/shared/python/sunity_shared/analysis/fault_zoom.py
@backend/shared/python/sunity_shared/analysis/keypoint_frame.py
@backend/shared/python/sunity_shared/judging/loader.py
@backend/tests/phase33/test_zoom_join_joint_exact.py

# 승인 스펙 원본 (표적 열람 — 108KB 전독 금지)
# mockups/index.html: 36-115행(라운드 이력·7R 규칙), 610-630행(자산 근거표),
#   780-814행(기준 앵커 배치 이력 + A-5 구현 지시), 1049-1074행(shoulder DETAILS)
# mockups/assets/belle_shoulder_pair_dtwmatch_r7.png (승인 산출물 — 이미지 Read 로 열 것)

# 코드 좌표 (재탐색 금지)
# fault_zoom.py: CRITERION_REGION/REGION_MEMBERS/_REGION_JOINTS 76-112 ·
#   criterion_units_from_records 114-191 · _crop_box 945 · _render_crop 965 ·
#   _full_frame_fit 972 · _member_pts 989 · _anchor_xy 1016 · _to_crop_px 1038 ·
#   _pt_in_crop 1057 · _side_crop 1080 · _stamp_time 1156 · _mark 1188 ·
#   _draw_leg_angle 1213 · _draw_side_leg_angle 1252 · _leg_line_pts 914 ·
#   _gated_kp 894 · ARROW_JOINT_MAP 1397 · joint_inner_angle_deg 1407 ·
#   build_fault_zoom_comparisons 1850 (unit 루프 2039 · crop/마킹 2188-2346 ·
#   item 방출 2349-2378)
# pipeline/app.py: _KISMAM_TO_KEYPOINT 2866 · _render_fault_zoom 2898 ·
#   build 호출 2969 · advisory 호출 3004 · item 매퍼 3049-3090 ·
#   _build_fault_zoom_comparisons 3095 (criterion_units 3179, stamp_ref 3242)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 기준 앵커 주석 계약 + 팔꿈치 인접매핑 제거 + F-3 실영상 초 방출</name>
  <files>
backend/shared/python/sunity_shared/analysis/reference_anchors.py,
backend/judging_data/reference_anchors/ref-power-spin.yaml,
backend/judging_data/reference_anchors/README.md,
backend/shared/python/sunity_shared/analysis/fault_zoom.py,
backend/functions/pipeline/app.py,
backend/tests/phase33/test_reference_anchors.py,
backend/tests/phase33/test_zoom_join_joint_exact.py,
app/src/types/analysis.ts,
docs/contract.md
  </files>
  <behavior>
    - `load_reference_anchors("ref-power-spin")` → criterion 별 대입 선언 dict.
    - `load_reference_anchors("없는-모션")` → `{}` (예외 0, fail-closed).
    - yaml 이 스키마 위반(criteria 비-dict / 대입 대상이 미등재 관절명) → 그 항목만 드롭 + 경고 로그, 나머지 유지.
    - `resolve_anchor_joint_xy(report, frame_idx, joint, decl)`: 선언이 단일 관절이면 그 관절 좌표, `midpoint:[a,b]` 면 두 관절 중점. 소스 관절 중 하나라도 `_gated_kp` 미달 → None.
    - report 가 joint 를 직접 보유하면 선언보다 **report 우선**(대입은 부재 관절 전용).
    - `_KISMAM_TO_KEYPOINT["left_elbow"] == "left_elbow"` (hand 아님) — 우/좌 동일.
    - build 산출 item 에 `userVideoSec` = `u_idx_unit/frames_fps`, `refVideoSec` = `r_display_idx/frames_fps` (float, `_stamp_time` 에 이미 넘기는 값과 **동일 산출**). ref 대응 실패 카드는 `refVideoSec` 미방출.
    - 파이프라인 매퍼가 두 스칼라를 조건부 pass-through (region/refMatch 선례).
  </behavior>
  <action>
**(1) 앵커 로더 신설** — `reference_anchors.py` 를 `judging/loader.py` 관례(경로 =
`Path(__file__).resolve().parents[N] / "judging_data" / "reference_anchors"`, pyyaml
= 파이프라인 requirements 기보유)로 작성. 순수 모듈: boto3/네트워크 0, 모듈 docstring 에
"display 전용, 채점 무접촉(D-44) · 기준 라이브러리 11개 고정이라 모션당 1회 수동 주석으로
충분(승인 목업 A-5 구현 지시)" 명기. 공개 API 2개 = `load_reference_anchors(motion_id)`,
`resolve_anchor_joint_xy(report, frame_idx, joint, decl)`. 결과는 프로세스 캐시(모듈
dict)로 1회 로드 — reference 는 pinned 라 런타임 변동 없음. **모든 실패 경로는 예외 없이
빈 값** (fault_zoom 은 분석 흐름을 막지 않는 부가물 — 모듈 docstring 계약).

**(2) 스키마 = 관절 대입 선언** (L-5). 파일 = `reference_anchors/{motion_id}.yaml`,
최상위 키 = `motion`(필수, 파일명과 일치 검증) · `annotated`(날짜) · `source`(사람이
무엇을 열어 확인했는지) · `criteria`(criterion id → `{joint_substitutions: {부재관절:
대입선언}, note: str}`). 대입선언 = 관절명 문자열 또는 `{midpoint: [관절a, 관절b]}`.
허용 관절명 = `keypoint_frame._KEYPOINT_NAMES` 만(오타 방어). `note` 필수 — 근거 없는
주석 금지.

**(3) ref-power-spin 시딩** — 승인 7R 기준 패널(`belle_shoulder_pair_dtwmatch_r7.png`)을
**이미지 Read 로 직접 열어** 그립 팔이 채점 국면에서 곧게 펴져 있음을 확인한 뒤
`angle_vs_reference__left_shoulder` 에 `left_elbow: left_hand` 선언을 기록. `source`/`note`
에 근거(승인 7R 우 패널 육안, 기준 report = phase4_v1 8kp 로 elbow 부재, 기준 실 3.07s
DTW 실측 순간) 명기. 확인 결과가 "팔이 굽어 있다"면 선언을 넣지 말고 README 에 그 사실을
기록(fail-closed) — 관찰과 다른 주석 날조 금지.

**(4) README 절차 문서** — `reference_anchors/README.md`: 스키마 계약 + 왜 정적 좌표가
아니라 대입 선언인지(L-5) + **잔여 9모션 주석 절차**(기준 영상 S3 키 → 9fps 프레임 추출 →
criterion 채점 국면 프레임 열람 → 부재 관절(ankle/elbow)의 대입 유효성 판정 → yaml 기록)
+ "§C-4 Pod 재스위프에서 채움(L-9)" + 등재 10동작 체크리스트(`backend/judging_data/criteria/`
파일 목록에서 파생, 하드코딩 금지).

**(5) 팔꿈치 인접매핑 제거** — `pipeline/app.py` `_KISMAM_TO_KEYPOINT` 의 elbow 2행을
동명 관절로 교정. 주석에 "32-14 로 keypointReport 가 12관절이 되어 elbow 직접 표기 가능 —
hand 대입은 belle #7·#9 '팔꿈치인데 손을 집고 있음'의 원인이었다(33-G S9)" 근거 박제.
`_keypoint_deltas`/advisory 경로가 이 map 을 공유하므로, 이 map 이 **채점 경로에 없음**을
grep 으로 증명(감점 record 생성은 `deduction_engine`/`ipsf_criteria` 소유). 테스트 스텁
미러 `tests/phase33/test_zoom_join_joint_exact.py` `_ANGLE_MAP` 도 동일 교정 + "pipeline
미러" 주석 유지.

**(6) F-3 실영상 초 방출** — `build_fault_zoom_comparisons` item 에 `userVideoSec`,
`refVideoSec` 추가. 값은 `_stamp_time` 호출에 이미 쓰는 식과 **같은 표현식 재사용**(중복
계산 금지): 학생 = `u_idx_unit / frames_fps`, 기준 = `r_display_idx / frames_fps`
(`ref_display_frame_index` 타임베이스 보정분 — rep fps 로 나눈 값이 아니다. 이것이 F-3
"다른 순간"의 실 원인: 앱이 `refFrameIdx / rep.fps` 로 초를 추정해 rep(18fps 329f) ↔
video(9fps 220f) 불일치를 그대로 먹었다). `frames_fps <= 0` 또는 ref 대응 실패면 해당 키
미방출. 파이프라인 매퍼(3049-3090)에 `isinstance(..., (int, float))` 조건부 복사 추가.

**(7) 3-way 계약 lockstep** — `app/src/types/analysis.ts` `FaultZoomComparison` 에
`userVideoSec?: number; refVideoSec?: number;` + 주석(용도 = paircap 초 표기 S6 · 참고코너
페어 정합 F-3 · **rep 인덱스로 초를 재계산 금지** 경고 · 부재 = legacy doc), `docs/contract.md`
FaultZoomComparison 절에 같은 2필드. Python 방출부 주석에 TS/contract 상호 참조.
필드는 scalar 라 Firestore flat 제약(`_validate_dict_only_scalars`) 통과 — 검증 필요.
  </action>
  <verify>
    <automated>python3 -m pytest backend/tests/phase33/test_reference_anchors.py backend/tests/phase33/test_zoom_join_joint_exact.py backend/tests/test_fault_zoom.py backend/tests/test_fault_zoom_ref_match.py -q</automated>
    <automated>cd app && npx tsc --noEmit</automated>
    <automated>grep -rn "left_hand" backend/functions/pipeline/app.py | grep -v '^#' | grep -c "elbow" | grep -qx 0</automated>
    <manual>승인 자산 `mockups/assets/belle_shoulder_pair_dtwmatch_r7.png` 를 이미지 Read 로 열어 기준(우) 패널 그립 팔의 신전 여부를 판정하고, 그 판정이 시딩한 yaml `note` 와 일치함을 SUMMARY 에 기록</manual>
  </verify>
  <done>
`load_reference_anchors` 가 시딩 모션 1건을 읽고 미주석 모션에 `{}` 를 반환한다.
`_KISMAM_TO_KEYPOINT` 에 elbow→hand 잔재 0. item 이 `userVideoSec`/`refVideoSec` 를
`_stamp_time` 과 동일 값으로 방출하고 TS/contract 가 lockstep 이다. README 에 잔여 9모션
주석 절차와 §C-4 이관이 박제됐다.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: S9 — criterion 꼭짓점 정중앙 crop + 두 패널 동일 배율 (M-2)</name>
  <files>
backend/shared/python/sunity_shared/analysis/fault_zoom.py,
backend/tests/phase33/test_criterion_vertex_crop.py
  </files>
  <behavior>
    - `criterion_vertex_xy(criterion, members, report, frame_idx, deltas, resolver)` → 정규화 (x,y) | None:
      · `angle_vs_reference__*_shoulder` → 겨드랑이 근사 = shoulder→hip 선분 t=0.15
      · `angle_vs_reference__{jk}` (그 외) → jk kp 좌표
      · `split_angle` → left_hip/right_hip 중점 (= `_leg_line_pts[0]`)
      · 다관절 criterion(`leg_extension`/`arm_extension`) → `_anchor_xy` 최대편차 멤버
      · 소스 kp 게이트 미달/부재 → None
    - `_side_crop(..., center=(x,y), side_override=N)` → crop 박스 중심이 center, 한 변 = N, 경계 초과분은 **흰 패딩**(안쪽 shift 0).
    - 그 crop 에서 `_to_crop_px(center, ...)` == (_OUT//2, _OUT//2) ± 1.
    - criterion 카드의 user/ref 두 패널 `box[2]` 가 동일.
    - `center=None` 호출(legacy/advisory/mode3) 산출은 픽셀 byte-동일.
    - 꼭짓점을 못 세운 측은 기존 3단 강하 유지 → 한 측 `full` 이면 기존 D-12 ② 로 카드 미방출(L-6).
  </behavior>
  <action>
**(1) 꼭짓점 해석기 신설** — `criterion_vertex_xy` 순수 함수. 키잉은 criterion id +
**관절명 접미사**(`endswith("_shoulder")`)만 — 동작명 분기 0(D-41). 겨드랑이 상수
`_ARMPIT_T = 0.15` 를 모듈 상수로 두고 docstring 에 근거(승인 7R "겨드랑이 근사 =
shoulder→hip 선분 t=0.15 (일반화 규칙)")와 known-answer(shoulder(203,254)·hip(182,304)
→ (200,262), 360×640 좌표)를 박제. lerp 는 아핀이라 정규화 공간에서 계산해도 프레임 px
공간과 동일 지점임을 주석에 명기. `resolver` 인자 = Task 1 의 `resolve_anchor_joint_xy`
경유 관절 좌표 조회자(기본값 = `_gated_kp` 직접) — fault_zoom 이 anchor 이름공간을
모르는 채 주입받는 기존 관례(`angle_key_to_keypoint`/`select_advisory_joints`) 유지.

**(2) 정중앙 crop 경로** — `_crop_box` 는 **건드리지 않고**(legacy 경로 byte-보존)
`_crop_box_centered(h, w, cx, cy, side)` 를 신설: shift/clamp 없이 `left = round(cx*w -
side/2)`, `top = round(cy*h - side/2)` 를 그대로 반환(음수·초과 허용). `_render_crop` 도
그대로 두고 `_render_crop_padded(frame, left, top, side)` 신설: 흰 `(side, side)` 캔버스에
프레임과의 교집합 영역만 붙여 `_OUT` 리사이즈. `_side_crop` 에 keyword-only `center`,
`side_override` 추가 — 둘이 주어질 때만 새 경로, 미지정이면 현행 3단 강하 그대로.
`_to_crop_px`/`_pt_in_crop` 은 (left, top, side) 산술이라 무수정으로 새 박스에서도 성립.

**(3) 공용 배율 산출** — build 루프에서 카드당 1회:
`shared_side = round(min(min(uh,uw), min(rh,rw)) * _CRITERION_CROP_FRAC)`, 하한은
`_crop_box` 의 기존 하한(16)과 동일. `_CRITERION_CROP_FRAC = 220.0 / 360.0` 신규 상수 +
docstring 근거(L-1: 승인 7R 자산 220px/360px, display 전용·채점 무접촉). 두 패널에 같은
값을 넘긴다. 기존 `fault_zoom_crop` 로그에 `vertex_centered=` 와 `shared_side_px=` 를
추가해 32-03 parity 판정 재료를 유지.

**(4) region 강등** — `CRITERION_REGION`/`REGION_MEMBERS`/`_REGION_JOINTS` 는 **멤버
선정·캡션 grouping 용으로만 남기고** crop 중심 결정에서 제거. 세 상수 docstring 에
"33-G S9: crop 중심 결정에서 제외 — 중심은 `criterion_vertex_xy` 단일 출처. region 은
멤버 집합·앱 캡션 grouping 보조" 박제. `_anchor_xy` 는 다관절 criterion 의 꼭짓점
선택자로 계속 쓰되, 이제 그 반환값이 **crop 중심과 원 마커 중심 둘 다**가 되어 마커가
crop 중앙에 앉는다(승인본 정합).

**(5) criterion 카드에만 적용** — legacy fan-out(`unit.criterion is None`)·advisory·
mode3 경로는 `center` 미지정 = 산출 byte-불변. 이 무회귀는 baseline PNG 해시 선캡처로
증명한다(verify).
  </action>
  <verify>
    <automated>python3 -m pytest backend/tests/phase33/test_criterion_vertex_crop.py backend/tests/test_fault_zoom.py backend/tests/test_fault_zoom_relaxed_crop.py backend/tests/test_fault_zoom_ref_match.py backend/tests/test_fault_zoom_arrow.py backend/tests/phase32/test_fault_zoom_crop_parity.py backend/tests/test_mode3_fault_zoom_selection.py -q</automated>
    <automated>python3 - <<'PY'
# legacy 경로 무회귀 — 변경 전 해시를 baseline 파일에 남기고 변경 후 비교
import hashlib, json, pathlib, sys
p = pathlib.Path('.planning/quick/260730-l7t-33-g-c-1-fault-zoom-py-s9-crop-criterion/legacy_baseline.json')
sys.exit(0 if p.exists() and json.loads(p.read_text()).get('match') is True else 1)
PY</automated>
    <manual>스위프 하네스로 criterion 카드 PNG 를 생성해 **이미지 Read 로 열고**, 꼭짓점이 두 패널 정중앙인지 육안 확인 + 로그의 `user_side_px == ref_side_px` 수치 기록</manual>
  </verify>
  <done>
criterion 카드에서 꼭짓점 관절이 두 패널 정중앙(±1px)에 오고 두 패널 crop 한 변 px 이
동일하다. legacy/advisory/mode3 산출 PNG 해시가 변경 전과 동일하다. region 상수는 crop
중심 결정 경로에서 참조되지 않는다(grep 0).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: S8 — 각도 표시 베이크(선-쌍 규칙 + 기준 대입) + 10동작 일반화 스위프</name>
  <files>
backend/shared/python/sunity_shared/analysis/fault_zoom.py,
backend/tests/phase33/test_angle_bake.py,
.planning/quick/260730-l7t-33-g-c-1-fault-zoom-py-s9-crop-criterion/sweep_angle_crop.py
  </files>
  <behavior>
    - `ANGLE_BAKE_MAP` 선언(관절 계열 → 꼭짓점 규칙 + 사지측 방향 관절 + 몸통측 방향 관절):
      · `*_shoulder` → 꼭짓점 = 겨드랑이(t=0.15) · 사지 = elbow · 몸통 = hip
      · `*_hip` → 꼭짓점 = hip · 사지 = knee · 몸통 = shoulder
      · `*_knee` → 꼭짓점 = knee · 사지 = ankle · 몸통 = hip
      · `*_elbow` → 꼭짓점 = elbow · 사지 = hand(wrist) · 몸통 = shoulder
      · 미선언 관절 → 각도 0 (기존 원 마커)
    - `_draw_joint_angle(img, vertex_px, limb_dir_px, torso_dir_px)` → bool. 방향 벡터 정규화 후 고정 길이(사지 64/360·몸통 85/360 × _OUT)로 두 선 + 두 선 사이 minor arc(r = 16/360 × _OUT), 흰 halo 아래 + 브랜드 코어 위. 벡터 길이 < `_MIN_LEG_VEC_PX` → False.
    - 양측 게이트: user·ref **둘 다** 꼭짓점+두 방향점을 세울 수 있을 때만 두 패널에 그린다. 한쪽 실패 → 양쪽 미드로잉 → 기존 원 마커 폴백 (M-4 대칭).
    - 기준측 부재 관절은 Task 1 대입 선언으로 해석. 선언 없으면 그 카드는 각도 0(L-7).
    - 각도를 그린 카드는 원 마커·화살표를 생략(기존 legs 선례 = 시각 언어 충돌 방지).
    - 다리 사이각(`split_angle`) 카드는 기존 `_draw_side_leg_angle` 유지 — 각도 베이크와 이중 드로잉 금지.
    - 숫자 라벨 0 (belle 2026-07-28 "각도 배지 빼고 초 표기로").
  </behavior>
  <action>
**(1) 호 방향 공식 단일화** — `_draw_leg_angle` 안의 minor-arc 판정을 순수 helper
`_minor_arc_span_deg(vertex_px, a_px, b_px) -> (start, end)` 로 추출하고 `_draw_leg_angle`
이 그것을 호출하도록 교체(중복 공식 금지 — `_to_rep_idx` 선례). 다리 카드 산출은
byte-동일해야 한다(Task 2 의 baseline 해시 게이트로 확인).

**(2) 각도 베이크 렌더** — `_draw_joint_angle` 신설. 상수 3개(`_ANGLE_LIMB_LEN_FRAC =
64/360`, `_ANGLE_TORSO_LEN_FRAC = 85/360`, `_ANGLE_ARC_R_FRAC = 16/360`) + halo/코어
두께(`_ANGLE_HALO_W`, `_ANGLE_CORE_W`)를 승인 7R 근거와 함께 박제(L-4, display 전용 ·
채점 무접촉). **선을 관절까지 잇지 않는 이유**(방향만 쓰고 길이 고정 → 두 패널 기하 통일,
distal kp 의 crop-내 포함 여부에 둔감)를 docstring 에 명기. halo 를 먼저, 코어를 나중에
그려 어떤 배경에서도 읽히게(승인 자산 육안 정합).

**(3) 선-쌍 스펙 산출** — `build_angle_bake_spec(criterion, members, report, frame_idx,
resolver)` 순수 함수 → `(vertex_xy, limb_xy, torso_xy) | None`. 꼭짓점은 Task 2 의
`criterion_vertex_xy` 재사용(단일 출처). 방향점은 `ANGLE_BAKE_MAP` 선언 관절을 resolver 로
조회. `ARROW_JOINT_MAP`(무릎/팔꿈치/힙 3점)과 **어깨 포함 여부가 다른 이유**(화살표는
vertex 기하가 몸통-팔 혼합이라 어깨 제외 / 각도 베이크는 겨드랑이 꼭짓점 규칙이 승인돼
어깨 포함)를 두 맵 docstring 에 상호 명기 — 두 맵이 어긋난 게 아니라 목적이 다름을 박제.

**(4) build 루프 배선** — crop·마킹 블록(2246-2346)에서 다리 사이각 게이트 **뒤에**
각도 베이크 게이트를 둔다. 순서 = ① legs 사이각(기존, split 카드) → ② 각도 베이크
(양측 스펙 성립 시) → ③ 원 마커(폴백). ② 성립 시 user 원 마커·`draw_arrows` 화살표·ref
원 마커를 모두 생략. `u_try/r_try` copy-then-commit 패턴(기존 legs both-or-neither
2271-2283)을 그대로 따라 부분 드로잉이 남지 않게 한다. 로그에 `angle_bake=drawn|omitted:
{reason}` 추가(reason = `unmapped`/`user_gate`/`ref_gate`/`degenerate`) — §C-4 전수 대조
재료.

**(5) 10동작 일반화 스위프 하네스** — `sweep_angle_crop.py`(quick 디렉터리, 프로덕션
코드 아님). 등재 동작 목록은 `backend/judging_data/criteria/*.yaml` glob 에서 파생
(하드코딩 금지). 각 동작 × 그 동작 criteria 에서 유도한 criterion 카드에 대해:
로컬 실물 프레임(`.planning/phases/33-result-trust-recovery/33-S4-M8-crops/*.png`,
`mockups/assets/belle_still_f0*.png`) + 합성 keypointReport(user 12kp / ref 8kp =
실제 phase4_v1 형상)로 `build_fault_zoom_comparisons` 를 호출해 PNG 와 로그를 뽑는다.
출력 = `sweep_out/{motion}__{criterion}.png` + `sweep_out/summary.json`
(카드별 `vertex_centered`, `user_side_px`, `ref_side_px`, `angle_bake`, `refVideoSec`).
하네스가 스스로 검사하는 불변식 4개: 동작명 분기 0(코드 grep) · 정중앙 ±1px ·
두 패널 동일 배율 · 각도 드로잉 대칭(한쪽만 그려진 카드 0).

**(6) 33-G 표 재채점** — 스위프 산출을 근거로 33-G-MOCKUP-DIFF.md 의 S8·S9·F-3 행
판정을 갱신하고(FAIL → PASS 또는 부분 PASS + 잔여 근거), L-7/L-9 의 잔여분(무릎·팔꿈치
계열 각도, 9모션 주석)을 §C-4 이관 항목으로 같은 문서에 명시한다. 조용한 PASS 표기 금지.
  </action>
  <verify>
    <automated>python3 -m pytest backend/tests/phase33 backend/tests/test_fault_zoom.py backend/tests/test_fault_zoom_ref_match.py backend/tests/test_fault_zoom_relaxed_crop.py backend/tests/test_fault_zoom_arrow.py backend/tests/phase32 backend/tests/test_mode3_fault_zoom_selection.py -q</automated>
    <automated>python3 .planning/quick/260730-l7t-33-g-c-1-fault-zoom-py-s9-crop-criterion/sweep_angle_crop.py --assert</automated>
    <automated>python3 -m pytest backend/tests -q</automated>
    <manual>`sweep_out/` PNG 를 **최소 4장(어깨류·힙류·다리류·팔꿈치류 각 1)** 이미지 Read 로 직접 열어 승인 자산 `belle_shoulder_pair_dtwmatch_r7.png` 와 나란히 대조: 꼭짓점 = 겨드랑이/관절, 위 선 = 사지, 아래 선 = 몸통 방향, 호가 꼭짓점 안쪽에 작게, 두 패널 동일 기하. 대조 결과를 33-G 표 재채점에 반영</manual>
  </verify>
  <done>
어깨류·힙류 criterion 카드에 두 패널 동일 기하의 각도 표시(선 2 + 호 + halo)가 베이크되고,
기준측을 세울 수 없는 카드는 양측 모두 각도 없이 원 마커로 폴백한다(비대칭 0). 스위프
summary.json 이 등재 10동작 전 카드에서 불변식 4개를 통과하고, 생성 PNG 열람 기록과 33-G
표 재채점(잔여 §C-4 이관 명시 포함)이 남았다. `python3 -m pytest backend/tests` 회귀 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reference_anchors yaml → 렌더 경로 | 리포지토리 내 사람 작성 데이터가 드로잉 기하로 유입 |
| reference/user keypointReport → 드로잉 좌표 | 모델 산출(저신뢰·붕괴·환각 가능) 좌표가 시각 단정으로 유입 |
| fault_zoom item → Firestore doc → 앱 | 신규 스칼라 2개가 계약 표면을 넓힘 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-l7t-01 | Tampering | `reference_anchors/{motion}.yaml` | mitigate | 스키마 검증 = 관절명 화이트리스트(`keypoint_frame._KEYPOINT_NAMES`) + `motion` 필드 ↔ 파일명 일치 + 위반 항목 드롭. 좌표를 데이터로 받지 않으므로(대입 선언만) 임의 픽셀 지시 경로 자체가 없음 |
| T-l7t-02 | Information disclosure | crop PNG · 신규 초 스칼라 | mitigate | 신규 필드는 초(float) 2개뿐 — PII·경로·내부 id 0. crop provenance(criterion·프레임)는 기존대로 **로그 전용**, 화면 라벨 미방출 |
| T-l7t-03 | Denial of service | 앵커 yaml 로딩 · 패딩 crop | mitigate | 로더는 모듈 캐시 1회 + 예외 전면 흡수(빈 dict). `_render_crop_padded` 는 `side` 를 두 프레임 짧은 변 min 파생으로 상한 → 메모리 폭주 경로 없음 |
| T-l7t-04 | Repudiation | 채점 무접촉 주장 | mitigate | `deductionBreakdown`/veto/게이트 입력에 신규 값 역류 0 을 grep + 전 스위트 회귀 0 으로 증명(D-44). 점수 관련 테스트 통과가 증거 |
| T-l7t-05 | Spoofing | 환각 keypoint 로 그린 각도 = 거짓 단정 | mitigate | 드로잉은 `_gated_kp`(conf ≥ 0.5) AND 양측 성립 AND degenerate 아님일 때만. 미달 = 미드로잉(원 폴백) — 추정 좌표로 선을 긋지 않음 |
| T-l7t-SC | Tampering | 패키지 설치 | accept | 신규 의존성 0 (pyyaml·numpy·PIL 기보유). 설치 태스크 없음 → 정합성 게이트 비해당 |
</threat_model>

<verification>
## 33-G 표 항목별 재채점 (D-40 — 이 절이 완료 판정)

| 행 | 스펙 | 재채점 근거 |
|---|---|---|
| S9 | crop 중심 = criterion 꼭짓점 관절 정중앙, 같은 배율, region 인접 매핑 금지 | 스위프 summary.json 의 `vertex_centered` 전건 true + `user_side_px == ref_side_px` + region 상수 grep 이 crop 중심 경로 0 + elbow→hand 잔재 0 |
| S8 | 각도 베이크 — 팔 선 + 옆구리 선 + 호 r16, 꼭짓점 = 겨드랑이, 두 패널 동일 | 생성 PNG 4장 이상을 승인 자산과 나란히 육안 대조 + `_ARMPIT_T` known-answer((200,262)) 테스트 + 대칭 게이트 |
| S10/S11 | 다리 사이각 · DTW 실측 순간 | baseline PNG 해시 동일(무회귀) + 기존 ref_match 테스트 통과 |
| S15/M-4 | 페어 대칭(두 패널 동일 마킹) | 각도 both-or-neither 게이트 + "한쪽만 그려진 카드 0" 불변식 |
| F-3 | 참고코너 페어 = DTW 매칭 프레임 | `refVideoSec` 가 `ref_display_frame_index` 보정 경유 값과 동일함을 테스트로 고정 + summary.json 수치 확인 |

## 일반화 게이트 (single-motion-fixation blocking)

- 등재 10동작 목록은 `backend/judging_data/criteria/*.yaml` glob 파생 — 목록 하드코딩 0.
- 프로덕션 코드에 동작명(`ref-*`) 문자열 분기 0: `grep -rn "ref-" backend/shared/python/sunity_shared/analysis/fault_zoom.py` 결과 없음(주석 예시 제외).
- 스위프가 10동작 전건에서 불변식 4개 통과.

## 채점 무접촉 게이트 (D-44)

- `python3 -m pytest backend/tests` 회귀 0 (점수·감점 스위트 포함).
- `fault_zoom`·신규 모듈이 `dimensions`/`kismam`/`deduction_engine` 를 import 하지 않음(grep).
- 신규 상수는 전부 display 전용 표기 + 채점 임계 재사용/불변.

## 잔여 이관 (조용한 누락 금지)

| 항목 | 이관 대상 | 근거 |
|---|---|---|
| 9모션 × criterion 앵커 주석 값 | §C-4 (Pod 재스위프) | L-9 — 기준 프레임 실물 열람 필요, Pod OFF |
| 무릎·팔꿈치 계열 각도 베이크 활성화 | §C-4 (주석 채움 후 자동 활성) | L-7 — 기준 report 8kp 로 ankle/elbow 부재, 대칭 게이트가 fail-closed |
| 앱 region-first 조인 강등 · paircap 초 렌더 · 시트 재구성 | §C-2 (앱) | L-8 — 본 플랜은 값 방출까지 |
| crop 전수 재생성 · OTA · belle 확인 ③ | §C-4 / D-45 | 일괄 1회 원칙 |
</verification>

<success_criteria>
- criterion 카드 crop 중심 = 꼭짓점(관절/겨드랑이/골반중점), 두 패널 정중앙 ±1px.
- 두 패널 crop 한 변 px 동일 (스위프 summary.json 전건).
- 팔꿈치 criterion 이 손을 짚지 않음 (`_KISMAM_TO_KEYPOINT` 교정 + 테스트).
- 어깨류·힙류 카드에 두 패널 동일 기하 각도 베이크(선 2 + 호 + halo), 숫자 라벨 0.
- 기준측 미성립 카드 = 양측 각도 미드로잉(비대칭 0).
- `userVideoSec`/`refVideoSec` 방출 + TS/contract 3-way lockstep + tsc clean.
- 기준 앵커 주석 스키마·로더·README(잔여 9모션 절차) + 승인 7R 근거 1모션 시딩.
- legacy/advisory/mode3 산출 PNG 해시 무변경, `python3 -m pytest backend/tests` 회귀 0.
- 생성 PNG 4장 이상 직접 열람 + 33-G 표 S8·S9·F-3 재채점 기록.
</success_criteria>

<output>
Create `.planning/quick/260730-l7t-33-g-c-1-fault-zoom-py-s9-crop-criterion/260730-l7t-SUMMARY.md` when done.

SUMMARY 필수 포함:
- 자체 도출 결정 L-1~L-9 중 실행 중 추가·수정된 것 (D-39 — belle 미질문 원칙 유지 증거)
- 스위프 summary.json 핵심 수치 + 열람한 PNG 목록과 육안 판정
- 33-G 표 재채점 결과 + §C-4/§C-2 이관 목록
</output>
