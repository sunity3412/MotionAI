---
phase: quick-260730-py1
plan: 01
type: execute
wave: 1
depends_on: [quick-260730-l7t]
files_modified:
  - app/src/lib/deductionSheet.ts
  - app/src/lib/__tests__/deductionSheet.test.ts
  - app/src/lib/deductionLabels.ts
  - app/src/lib/visualCards.ts
  - app/src/components/DeductionDetailSheet.tsx
  - app/src/app/analysis/result.tsx
  - app/src/theme/colors.ts
  - .planning/quick/260730-py1-33-g-c-2-1-s7-basis-method-proof-facing-/sweep_sheet_blocks.test.ts
autonomous: true
requirements: [S6, S7, F-3]
user_setup: []

must_haves:
  truths:
    - "한 부위에 감점 2건이 있으면 시트가 1개 열리고 그 안에 결함 블록이 2개 보인다 (record 단위 시트 2개가 아니다)"
    - "확대 크롭 바로 아래에 '내 자세 · 실 N초' / '기준 (정은지) · 실 N초' 가 좌우로 보인다 (기준측 초 필수)"
    - "각 결함 블록 맨 위에 '고칠 것 N — 항목 (−N점)' 번호 헤더가 보이고, 그 N 은 영상 위 마커 번호와 같다"
    - "각 블록에 '어디서 재나요' 근거 줄과 '측정 방법' 정직 라벨이 데이터가 성립할 때 보이고, 수치는 블록 맨 뒤 작은 회색 줄로 내려간다"
    - "기준 정렬로 잰 항목이 있는 부위 시트에는 '두 사진이 달라 보이는 이유' 안내가 보인다"
    - "참고하세요 > 자세 비교 실프레임 페어가 확대 크롭과 같은 순간을 가리킨다 (기준측이 대표 프레임이 아니다)"
    - "데이터가 없으면 basis·method·facing·onecap·proof 는 문구를 만들지 않고 생략된다"
  artifacts:
    - path: "app/src/lib/deductionSheet.ts"
      provides: "부위 단위 시트 뷰모델 + 블록 카피 조립 (순수 함수, 동작명 분기 0)"
      exports: ["buildRegionSheetView", "regionPartKeyForRecord", "formatVideoSecKo"]
      min_lines: 120
    - path: "app/src/lib/__tests__/deductionSheet.test.ts"
      provides: "그룹핑·번호·fail-closed 단위 검증 (node --test)"
    - path: ".planning/quick/260730-py1-33-g-c-2-1-s7-basis-method-proof-facing-/sweep_sheet_blocks.test.ts"
      provides: "등재 10동작 criteria yaml 파생 일반화 스위프"
    - path: "app/src/components/DeductionDetailSheet.tsx"
      provides: "승인 목업 ② 구조 렌더 (chip → crop → paircap → onecap → 블록 N → facing → illust)"
      contains: "buildRegionSheetView"
  key_links:
    - from: "app/src/components/DeductionDetailSheet.tsx"
      to: "app/src/lib/deductionSheet.ts"
      via: "뷰모델 소비 (조판 로직 사본 0)"
      pattern: "from '\\.\\./lib/deductionSheet'"
    - from: "app/src/app/analysis/result.tsx"
      to: "FaultZoomComparison.userVideoSec / refVideoSec"
      via: "poseFrames.timeSec (rep 인덱스 나눗셈 제거)"
      pattern: "userVideoSec|refVideoSec"
    - from: "app/src/lib/deductionSheet.ts"
      to: "deductionLabels.buildDeductionMarkers"
      via: "블록 번호 = 전역 마커 번호 단일 출처"
      pattern: "recordNumbers"
---

<objective>
33-G §C-2 앱 수리 1단위 — 승인 목업 7R ② 부위 상세 시트를 **record 단위 → 부위 단위**로
재구성하고(S6), 승인본에 있으나 앱에 **전무한 블록 요소**(번호 헤더·basis·method·numnote·
facing)를 신설하며(S7), 참고하세요 자세 비교 페어를 백엔드가 방출하는 실영상 초로 정합한다
(F-3 앱분).

Purpose: belle 확인 ② 반려의 남은 축. §C-1 이 백엔드 산출(crop 정중앙·각도 베이크·초 방출)을
닫았고, 이 단위는 **앱 표면**이 승인본과 어긋난 부분을 닫는다. 시트가 결함 하나만 보여주고
근거·측정방법이 없으면 "왜 감점인지"가 전달되지 않는다 (Core value = 분석 정확도의 전달).

Output:
- `app/src/lib/deductionSheet.ts` (신규, 순수 뷰모델 + 카피 조립)
- `DeductionDetailSheet.tsx` 승인 구조 렌더 / `result.tsx` 배선 + F-3 초 소비
- 자체 도출 결정 M-1~M-14 (D-39 재논의 없음 — belle 미질문)
- 등재 10동작 일반화 스위프 + 시뮬 렌더 대조 재채점
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-result-trust-recovery/33-G-MOCKUP-DIFF.md
@.planning/phases/33-result-trust-recovery/.continue-here.md
@.planning/quick/260730-l7t-33-g-c-1-fault-zoom-py-s9-crop-criterion/260730-l7t-SUMMARY.md
@app/src/components/DeductionDetailSheet.tsx
@app/src/lib/deductionLabels.ts
@app/src/lib/visualCards.ts

승인 스펙 원본 (표적 열람 — 이미 추출된 스펙은 아래 <approved_spec> 에 있으므로 재열람은
불일치 의심 시에만):
- `.planning/phases/33-result-trust-recovery/mockups/index.html` 1002-1117행 (`DETAILS` +
  `renderDetail`) · 161-176행/198-254행 (블록 CSS) · 41-101행 (4R#2·5R#2·5R#3·6R#3·7R 근거)
</context>

<approved_spec>
## 승인 목업 ② 부위 상세 시트 — 구조 (mockups/index.html `renderDetail`, 1086-1117)

```
card                                  # 크롭 카드
  chip.brand "오늘 고칠 것"           | chip.gray "참고 — 감점은 아니지만 회전·힘에 영향"
  cropimg  <img>                       # 부위당 1쌍
  paircap  [내 자세 · 실 1.7초] ←→ [기준 (정은지) · 실 3.07초]   # space-between, 6R 기준측 필수
  onecap   마킹 설명 1줄 (center)
card.fault × N                        # 결함 블록 (부위에 감점 N건 → 블록 N개)
  fault-head  "고칠 것 2 — 무릎 펴기 (−20점)"   # 4R#2, brand-tint 헤더 바
  headline    record.statusLine
  why         record.whyLine
  basis       "<b>어디서 재나요:</b> …"          # 5R#2, 회색 박스 (옵셔널)
  cue         record.cueLine                     # inline-block brand-tint 박스
  methodline  "측정 방법 — …"                    # 5R#3, 틸 박스 (옵셔널)
  numnote     "측정 수치(참고) — … → −20점"      # 2R, 블록 맨 뒤 작은 회색 (옵셔널)
  proofrow    증거 3컷 (img + 초 캡션 + pnote)   # 5R (옵셔널)
facing        "두 사진이 달라 보이는 이유 — …"    # 틸 박스, 블록 전부 뒤 (옵셔널)
illust-slot   일러스트                            # 33-14 기존 슬롯
```

**번호 헤더 형태:** 부위에 record 2건 → `고칠 것 1 — …` / `고칠 것 2 — …`.
record 1건 → 번호 없이 `고칠 것 — 어깨 각도 (−17.4점)` (목업 shoulder 케이스).

**CSS 요지:** `.fault-head` 15/800 brand on brand-tint + 하단 보더 / `.headline` bodyLg 800 /
`.why` bodySm ink-2 / `.basis` #F7F8F9 + line 보더, `b`=진한 잉크 / `.cue` bodyMd 800 brand on
brand-tint, radius 12 / `.methodline` 12.5 #2C7C8C on #F4FBFC + #CFE9EF 보더 / `.numnote`
12.5 ink-3, `b`=ink-2 700 / `.paircap` 12/800 #6b6e76 space-between / `.onecap` caption ink-2
center / `.facing` #F4FBFC + #CFE9EF 보더, bodySm #2C7C8C / `.pnote` brand 700.

**확정 문구 (원문 유지 대상):**
- paircap 좌 = `내 자세 · 실 {N}초` / 우 = `기준 ({athleteName}) · 실 {N}초`
- method(vision) = `측정 방법 — 이 항목은 특정 순간을 잰 프레임 측정이 아니라, 두 영상 전체를
  견준 AI 시각 판단이에요.`
- method(정렬) = `측정 방법 — 이 항목은 한 순간이 아니라 두 영상의 전 구간을 정렬해 견준
  값이에요. 기준 사진은 그 정렬이 실제로 짝지은 순간(기준 실 {N}초)이에요.`
- basis 문두 = `어디서 재나요:` (b) / facing 문두 = `두 사진이 달라 보이는 이유 — `
- numnote 문두 = `측정 수치(참고) — `
- advisory onecap = `참고 부위예요 — 점수 감점은 되지 않지만 회전·힘 같은 전체 동작에 영향을
  줄 수 있어요. 눈에 띈 차이만 보여드려요`
</approved_spec>

<derived_decisions>
D-39 = 재논의 없음. 승인본이 답을 직접 주지 않는 지점을 "승인본이 정답 / 수리에 새 범위 금지"
원칙으로 자체 도출했다. belle 에게 묻지 않는다. (§C-1 의 L-1~L-11 에 이어 **M-1 부터** 부여.)

- **M-1 (초 표기의 정본 = paircap 텍스트).** belle 원문 요구는 "확대사진 **아래에** 몇 초인지
  기입해주면 해결"(2026-07-28, `fault_zoom._stamp_time` docstring 인용)이고 승인 7R 은 그 초를
  paircap 텍스트로 렌더한다. 승인 자산 `belle_shoulder_pair_dtwmatch_r7.png`·
  `belle_zoom_left_hip.png` 를 열어 실측 = **패널 내 베이크 타임스탬프 없음**(hip 카드에는 구
  각도 배지 `30°` 만). → 앱은 paircap 을 정본으로 두고, 사진 속 초를 지칭하는 기존 안내
  `TIME_STAMP_NOTE_MAIN/PAREN` 을 **제거**한다. 백엔드 `_stamp_time` 제거는 **§C-4 crop 전수
  재생성**에 이관 (이 플랜은 백엔드 무접촉 — 코드만 바꾸면 기존 crop 과 불일치). 이중 표기는
  재생성 시점에 자동 해소되고, 그 사이에도 두 표기가 **같은 값**이라 모순이 아니다.
- **M-2 (초 포맷).** `toFixed(1)` + `실 {N}초`. 백엔드 `_timestamp_label` = `f"{s:.1f}s"` 와
  동일 자릿수 (두 표기 공존 구간에 불일치 0). 목업의 `3.07초` 는 33-A4 수동 기입값이고 앱의
  단일 출처는 백엔드 방출값이다. 비유한/음수 → 초 절 생략(라벨만).
- **M-3 (부위 그룹 키).** `projectDeductionRecordKeypoints(record, faultJoints)` → keypoint →
  부위 토큰 집합. 토큰 = `shoulder`(어깨) / `arm`(팔: elbow·hand) / `leg`(다리: hip·knee·ankle).
  **좌우 미분할** (승인본 '다리' 그룹 = hips+knees 통합). 투영 공집합(`line`·
  `dimension_overall_fallback`) → `criterion:{id}` 단독 그룹, 제목 = `criterionLabelKo`
  (숨기지 않음 — 투명성 관례).
- **M-4 (블록 번호 = 전역 마커 번호).** `buildDeductionMarkers().recordNumbers[i]` 를 그대로
  쓴다. "영상 위 점 안 숫자 = 내역 행 원문자 = 블록 번호"가 한 소스에서 나와야 하는 기존
  불변식(deductionLabels 191-214 주석)을 승인본이 깨지 않는다 — 목업 legs 의 1·2 도 그 doc 의
  전역 번호와 같다. 그룹 record 가 1건이면 번호 절 생략(승인본 shoulder 형식).
  `estimatedArea`(IN-01) 이면 번호 억제(기존 게이트 계승 — 오버레이 번호도 억제되어 대응 점이
  없다). `recordNumbers[i] == null` 도 번호 없이 렌더.
- **M-5 (그룹 크롭 1쌍 + 블록 순서).** 그룹 크롭 = 그룹 내 **저장순 첫 매치** 카드
  (`matchZoomForDeductionRecord`) 1쌍. 블록 순서 = 그 record 를 먼저, 나머지는 저장순 —
  승인본 주석의 규칙("사진의 마킹이 가리키는 결함 먼저", 1015-1016)을 불변식으로 옮긴 것이다.
  같은 부위의 **다른** record 가 다른 카드를 가지면 그 크롭을 **그 블록 안**에 렌더한다
  (proof 자리, M-10 으로 비어 있음): 기존에 보이던 증거를 조용히 잃지 않는다. primary 와 동일
  카드면 중복 렌더 금지.
- **M-6 (onecap = 마킹 기하 단정 금지).** 어떤 마킹이 실제 베이크됐는지는 doc 에 **없다**
  (`fault_zoom` item 방출 필드에 marking 종류 부재 — `u_drew_angle`/`r_drew_legs` 는 렌더
  지역변수). 각도 베이크는 힙류 자동 + 주석 완료 모션만 성립하므로(§C-1 스위프: 21/110 drawn)
  "빨간 두 줄 / 꼭짓점 = 겨드랑이" 를 상시 문구로 넣으면 **대부분 카드에서 거짓 지칭**이 된다.
  같은 컴포넌트의 선례(`TIME_STAMP_NOTE` = "구 PNG 에 없는 배지를 지칭하는 거짓 라벨 방지 —
  데이터 키잉 게이트")를 따라 **무엇을 견주는지만** 말한다:
  `이 사진은 {무엇}을 기준 자세와 견줘요` — {무엇} = `angle_vs_reference__{jk}` →
  `ANGLE_MEANING_KO[jk]`(겨드랑이 벌림·무릎 굽힘…) / `split_angle` → `두 다리를 벌린 정도` /
  그 외 → `criterionLabelKo`. advisory 카드 onecap 은 기하 무단정이라 **승인 문구 원문 유지**.
  마킹 종류 방출(`markingKind`)은 §C-4 후보로 이관.
- **M-7 (basis = 성립하는 축만).** 렌더 = `어디서 재나요: 이 항목은 {무엇}을 재요. 위 사진은
  그 값을 잰 순간(실 {N}초)이에요.` ({무엇} = M-6 과 동일 출처, N = `userVideoSec`).
  승인본의 **구간 축**("실제 재생 6.3~8.2초 구간", "7.3~7.7초에 도로 접힘")은 이 단위에서
  성립하지 않는다 — 계약상 측정창은 **record별이 아니라 공유 단일 창**이고
  (`visionVeto.windowMedianAngleDeltas` — deductionLabels 350-355 주석), 창 인덱스→초 변환에
  필요한 프레임 배열 fps 가 앱에 없다. 앱이 인덱스를 나눠 초를 추정한 것이 **바로 F-3 의
  근본원인**이므로 같은 실수를 반복하지 않는다. → 구간 축 = 백엔드 record별
  `measureWindowStartSec`/`measureWindowEndSec` 방출을 조건으로 **§C-4/후속 이관**.
  `userVideoSec` 부재면 뒷문장 생략, 둘 다 불성립이면 basis 전체 생략.
- **M-8 (method = source × deviationSource 키잉).** `source==='vision'` → 승인 vision 문형.
  `source==='geometry' && deviationSource==='reference_relative'` → 승인 정렬 문형
  (`refVideoSec` 부재 시 두 번째 문장 생략). `ipsf_absolute` → **미방출** (승인본에 해당 문형이
  없다 — 없는 문구를 창작하지 않는다. 그 역할은 basis 가 한다).
- **M-9 (facing 조건 = reference_relative 존재 + 두 초 보유).** 승인본 분포(어깨 = 있음 /
  다리 = 없음)를 **데이터로** 재현한다: 어깨 블록만 `deviationSource==='reference_relative'`
  (기준 대비 34° 차이)이고, 다리 두 블록은 vision + ipsf_absolute 다. 문구 = 승인 문두 유지 +
  데이터로 참인 절만 —
  `두 사진이 달라 보이는 이유 — 두 장면은 같은 동작의 서로 다른 순간이에요(내 {u}초 ↔ 기준
  {r}초). 분석이 같은 구간으로 짝지은 순간이라, 몸 방향이 달라 보여도 {부위} 각도만 견줘 보세요.`
  승인본의 "정은지는 무릎을 모아 폴에 붙인 고정 회전 자세" 절은 **그 동작·그 순간의 관찰**이라
  제외한다(single-motion-fixation 금지). 두 초가 같으면(동일 순간) 생략.
- **M-10 (proof 증거 3컷 = 미구현·fail-closed).** 백엔드는 카드당 **합성 PNG 1장**만 방출한다
  (`fault_zoom.py` item = `png` 1개, 2879-2924 실측). 3컷의 "좋았던 / 감점 / 마무리" 분류는
  **측정 판단**이고 doc 에 없다 — 앱이 각도 시계열을 뒤져 정하면 판정 재해석(=fabricate,
  deductionLabels 헤더 "앱에서 점수/판정 재계산·재해석 금지")이다. → 블록에 **자리도 두지 않고**
  생략하고, 백엔드 3컷 방출(초 + 캡션 분류)을 §C-4 로 이관한다. S7 은 이 축만 미달로 남는다.
- **M-11 (F-3 앱분 = 같은 카드에서 초를 함께 읽는다).** `pickCompareFrames` 가 이미 고른
  `comparisons[0]` 에서 `userVideoSec`/`refVideoSec` 를 **같이** 반환하고 `poseFrames.timeSec`
  이 그 값을 쓴다 (별 조인·나눗셈 0). `frameIdx` 는 rep 공간 그대로 유지 — keypoint 오버레이
  좌표계라 바뀌면 안 된다. `refVideoSec` 부재 → `reference.url` 미지정 → 기존
  `framesReady=false` 경로로 스켈레톤 뷰어 폴백(신규 분기 0). `userVideoSec` 부재 → 페어 전체
  미렌더. legacy doc 은 실프레임 페어를 잃지만 **틀린 순간을 보여주지 않는다**(D-14 원리).
- **M-12 (S12 어휘는 이 단위 범위 밖).** 번호 헤더가 `criterionLabelKo` 단일 출처를 소비하므로
  `다리 신전(펴짐)` → 다음 단위의 라벨 수정이 자동 전파된다. 여기서 라벨 사전을 손대지 않는다
  (과잉 일반화로 다른 PASS 표면을 흔들지 않는다 — §C-1 L-10 교훈).
- **M-13 (승인본에 없는 기존 요소의 처분).** 시트 하단 `evidenceBox`("이 원인은 어떻게
  측정됐나")는 **numnote 로 대체**한다 — 수치의 승인 거처가 "블록 맨 뒤 작은 회색 줄"이다
  (2R 수치 강등). 반면 `bullets`(용어줄·확인하기) · `coachConnect` · `aiNoteBox` 는 **gate ⑤
  승인 원형(belle Figma)** 산물이므로 **유지**하고 위치만 illust 뒤로 옮긴다 — 다른 승인 자산을
  깨는 삭제는 하지 않는다.
- **M-14 (조판 = 앱 토큰 우선, 목업 px 는 스케일 참조).** 목업 12/12.5/13.5px 는 데스크톱
  스케일이다. 본문급(headline/why/basis/cue)은 E2 토큰(bodyLg/bodySm/bodyMdBold, D-05 하한 17)
  으로, 소형 주석급(paircap/onecap/method/numnote)은 같은 컴포넌트의 기존 `caption`(12) 선례로
  매핑한다. 틸 정보 톤은 신규 토큰 3개(`infoTeal`/`infoTealBg`/`infoTealBorder`)로 추가하고
  출처를 승인 목업 CSS 로 박제한다 — **하드코딩 금지**(CLAUDE.md §4). basis 박스는 기존
  `softBg`+`border` 재사용(신규 토큰 최소화).
</derived_decisions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 부위 단위 시트 뷰모델 + 블록 카피 조립 (순수 함수)</name>
  <files>app/src/lib/deductionSheet.ts, app/src/lib/__tests__/deductionSheet.test.ts, app/src/lib/deductionLabels.ts</files>
  <behavior>
    - 부위 그룹핑: left_hip + left_knee record 2건 → 그룹 1개(`leg`), 블록 2개. left_shoulder
      record 1건 → 별 그룹(`shoulder`), 블록 1개. (M-3)
    - 투영 공집합(`line`, `dimension_overall_fallback`) → `criterion:{id}` 단독 그룹, 제목 =
      criterionLabelKo.
    - 번호 헤더: 그룹 2건 → `고칠 것 1 — {라벨} (−12점)` / `고칠 것 2 — …`, 번호는 전역
      `recordNumbers` 값. 그룹 1건 → `고칠 것 — {라벨} (−17.4점)` (번호 절 없음). (M-4)
    - estimatedArea=true → 모든 블록 헤더에 번호 없음 + numnote = 관절별 수치 대신 IN-01 안내.
    - 블록 순서: 그룹 크롭을 낳은 record 가 첫 블록, 나머지는 저장순. (M-5)
    - paircap: userVideoSec=1.74 → `내 자세 · 실 1.7초`, refVideoSec=3.07 + athlete='정은지' →
      `기준 (정은지) · 실 3.1초`. refVideoSec 부재 → 우측 = `기준 (정은지)` (초 절 생략). (M-2)
    - method: source='vision' → vision 문형 / geometry+reference_relative → 정렬 문형(+기준 초) /
      ipsf_absolute → null. (M-8)
    - basis: userVideoSec 보유 → `어디서 재나요: …잰 순간(실 N초)이에요.` / 부재 → 뒷문장 생략 /
      {무엇} 미상 + 초 부재 → null. (M-7)
    - facing: 그룹에 reference_relative record 존재 + 두 초 보유 + 두 초 상이 → 문장, 그 외 null.
      (M-9)
    - onecap: `angle_vs_reference__left_shoulder` → `이 사진은 겨드랑이 벌림을 기준 자세와
      견줘요` / advisory 카드 → 승인 원문. 마킹 기하 문구 0. (M-6)
    - proof 필드 자체가 뷰모델에 없다 (M-10 — 빈 배열도 두지 않는다).
    - records 빈 배열 / breakdown 부재 / 잘못된 index → null 반환 (크래시 0).
  </behavior>
  <action>
`app/src/lib/deductionSheet.ts` 를 신설한다 — 시트 조판 로직 전부를 순수 함수로 격리해
`DeductionDetailSheet.tsx` 가 렌더만 하게 만든다 (`resultSections.ts` 선례: "2,700줄 대배선은
typecheck 만으로 보증 못 한다").

`deductionLabels.ts` 에는 **부위 사전만** 추가한다 (라벨 단일 출처 소관):
`BODY_PART_OF_KEYPOINT: Record<KeypointName, 'shoulder'|'arm'|'leg'>` (shoulder→shoulder,
elbow·hand→arm, hip·knee·ankle→leg — 12 keypoint 전건) + `BODY_PART_LABEL_KO`
(shoulder '어깨' / arm '팔' / leg '다리'). `REGION_LABEL_KO`(양다리/양팔)와 **키 공간이 다르다**는
주석을 달아 혼용을 막는다. 기존 export 는 수정 0 (M-12).

`deductionSheet.ts` API:
- `regionPartKeyForRecord(record, faultJoints): string` — M-3.
- `formatVideoSecKo(sec): string | null` — `실 {toFixed(1)}초`, 비유한/음수 → null (M-2).
- `buildRegionSheetView(input): RegionSheetView | null` — input =
  `{ records, recordNumbers, actionPhrases, zooms, selectedRecordIndex, rightPairLabel,
  estimatedArea }`. `zooms[i]` = 그 record 의 matched `FaultZoomComparison | null`
  (조인은 result.tsx 가 기존 `matchZoomForDeductionRecord` 로 수행 — 조인 규칙 사본 금지).
- 반환 = `{ partKey, title, primaryRecordIndex, blocks, pairCapLeft, pairCapRight, oneCap,
  facingLine, isAdvisoryOnly }`, `blocks[i]` =
  `{ recordIndex, header, statusLine, whyLine, basisLine, cueLine, methodLine, numNote,
  blockRecordIndexForCrop }`.

카피 조립은 <approved_spec> 확정 문구를 **원문 그대로** 상수로 박고 자리표시자만 치환한다
(Figma/목업 확정문구 원문 유지 원칙). `<b>` 같은 HTML 은 문자열에 넣지 말 것 — 강조 구간은
`{ text, bold }` 세그먼트 배열로 방출해 렌더가 중첩 `<Text>` 로 그린다 (RN 은 마크업을 해석하지
않아 태그가 그대로 노출된다 — T-33G2-01).

numNote = `측정 수치(참고) — ${formatDeductionRecord(record).detailText} → ${pointsText}점`.
`formatDeductionRecord` 는 **수정하지 않는다** (ScoreBreakdownSection 이 공유 — PASS 표면 보호).

동작명 분기 0: 키잉은 `record.criterion` / `record.source` / `record.deviationSource` /
`projectDeductionRecordKeypoints` / `zoom.tier` 만 사용한다. 모션 id 리터럴 금지.

테스트는 `node --test` + `node:assert/strict`, `.ts` 확장자 import (신규 의존성 0 —
`resultSections.test.ts` 선례 헤더 형식을 따를 것).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && node --test app/src/lib/__tests__/deductionSheet.test.ts && cd app && npm run typecheck && grep -Ev '^[[:space:]]*//' src/lib/deductionSheet.ts | grep -c "ref-" | grep -qx 0 && echo GATE_OK</automated>
  </verify>
  <done>behavior 12축 전부 통과 · typecheck clean · 신규 모듈에 동작명 리터럴 0 · `deductionLabels.ts` 기존 export 시그니처 변경 0 (git diff 로 추가분만 확인)</done>
</task>

<task type="auto">
  <name>Task 2: DeductionDetailSheet 승인 구조 렌더 + result.tsx 배선</name>
  <files>app/src/components/DeductionDetailSheet.tsx, app/src/app/analysis/result.tsx, app/src/theme/colors.ts</files>
  <action>
`DeductionDetailSheet.tsx` 를 <approved_spec> 구조로 재구성한다. Props 를 record 단위 →
**뷰 단위**로 교체: `view: RegionSheetView | null` + `primaryZoom` + `blockZooms` (recordIndex →
`FaultZoomComparison`) + 기존 `zoomPending`/`refMatchFailed`/`estimatedArea`/`rightLabel`/
`illustrationSlot`.

렌더 순서 (승인본 그대로):
1. 시트 제목 = `${view.title} 부위 상세` (estimatedArea → 기존 `ESTIMATED_AREA_TITLE` 유지 —
   IN-01 = S17 PASS 보존).
2. 크롭 카드: chip(`오늘 고칠 것` brand / advisory-only 는 gray 승인 문구) → 크롭 이미지
   (기존 `imageWrap`/`halfLabel` 자산 재사용) → **paircap**(좌우 `space-between`) → **onecap**
   (center). `estimatedBadge`·`refMatchNote` 게이트는 그대로.
   **`TIME_STAMP_NOTE_MAIN`/`TIME_STAMP_NOTE_PAREN` 및 그 렌더 블록을 제거**한다 (M-1).
3. 블록 N개: `fault-head`(brand-tint 바 + 하단 보더) → headline → why → basis(softBg 박스,
   bold 세그먼트 중첩 Text) → cue(brandTint 박스, brand 텍스트, 라벨 없음 — 승인본) →
   methodline(infoTeal 박스) → numnote(작은 회색) → **블록 크롭**(`blockRecordIndexForCrop` 이
   있을 때만, M-5).
4. facing(infoTeal 박스) → `illustrationSlot` → 기존 `bullets`·`coachConnect`·`aiNoteBox`
   (M-13 — 삭제 금지, 위치만 이동) → 닫기 CTA.

`colors.ts` 에 토큰 3개 추가: `infoTeal: '#2C7C8C'`, `infoTealBg: '#F4FBFC'`,
`infoTealBorder: '#CFE9EF'`. 주석에 출처(승인 목업 `.methodline`/`.facing` CSS)와 기존
`inputError:#54B8CD` 와 다른 값인 이유(본문 대비용 어두운 톤)를 박제한다. 하드코딩 hex 0,
새 폰트 크기 리터럴 0 — 조판은 M-14 매핑표대로 기존 typography 토큰만 조합한다.

`result.tsx` 배선 (진입점 7곳은 전부 `detailRecordIndex` 로 모이므로 **수정 불필요**):
- `selectedRecord`/`selectedRecordNumber`/`selectedActionPhrase`/`selectedZoom` 를 대체하는
  `sheetView` useMemo 를 만든다 — `records` 전건에 대해 기존 `matchZoomForDeductionRecord` 로
  `zooms[]` 를 조립하고, `markers.recordNumbers`·`actionPhraseForRecord` 결과와 함께
  `buildRegionSheetView({ ..., selectedRecordIndex: detailRecordIndex, rightPairLabel })` 를 호출.
- `rightPairLabel` = mode1 → `기준 (${cmp.athleteName})`, mode3 → `지난 영상`
  (기존 `rightLabel` 은 halfLabel 용으로 유지).
- `refMatchFailed` 는 primary zoom 기준으로 계산.
- 삭제되는 변수를 참조하는 다른 소비처가 없는지 grep 으로 확인한 뒤 정리한다.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck && grep -c "TIME_STAMP_NOTE" src/components/DeductionDetailSheet.tsx | grep -qx 0 && grep -Ec "#[0-9A-Fa-f]{6}" src/components/DeductionDetailSheet.tsx | grep -qx 0 && grep -q "infoTeal" src/theme/colors.ts && echo GATE_OK</automated>
  </verify>
  <done>typecheck clean · 시트가 뷰모델만 소비(조판 분기 사본 0) · `TIME_STAMP_NOTE` 잔재 0 · 컴포넌트 내 hex 리터럴 0 · `bullets`/`coachConnect`/`aiNoteBox`/`estimatedBadge`/`refMatchNote` 5개 요소 존속 확인 · `app/package.json` diff 0</done>
</task>

<task type="auto">
  <name>Task 3: F-3 앱분 (참고코너 페어 초) + 10동작 일반화 스위프 + 시뮬 렌더 대조</name>
  <files>app/src/lib/visualCards.ts, app/src/app/analysis/result.tsx, .planning/quick/260730-py1-33-g-c-2-1-s7-basis-method-proof-facing-/sweep_sheet_blocks.test.ts</files>
  <action>
**(1) F-3 앱분 (M-11).** `visualCards.ts` `CompareFrames` 에 `userSec?: number` /
`refSec?: number` 를 추가하고 `pickCompareFrames` 가 **이미 고른 같은 카드**(`comparisons[0]`)
에서 `userVideoSec`/`refVideoSec` 를 읽어 유한값일 때만 담는다. 타입 파라미터도 두 필드를
받도록 넓힌다 (현재 구조적 타입).
`result.tsx` `ReferenceCornerSection` `poseFrames`(3048-3079행):
- `user.timeSec` = `compareFrames.userSec`, `reference.timeSec` = `compareFrames.refSec`.
- **`compareFrames.userIdx / (result.keypointReport?.fps || 9)` 와
  `compareFrames.refIdx / (referenceKeypointReport?.fps || 18)` 나눗셈을 삭제**한다 — 이 두 줄이
  F-3 의 근본원인이다. `frameIdx`/`report` 는 오버레이 좌표계라 **그대로 유지**.
- `refSec` 부재 → `reference.url` 을 `undefined` 로 두어 기존 `framesReady=false` 스켈레톤 폴백
  경로로 떨어뜨린다(신규 분기 0). `userSec` 부재 → `poseFrames` 전체 `null`.
- 주석에 "초는 백엔드 방출값만 — rep 인덱스 나눗셈 금지(F-3)" 를 박제한다.

**범위 울타리 (over-generalize 금지).** `result.tsx` 에 같은 fps 환산이 2곳 더 있으나
**둘 다 이 단위 밖**이다: (a) `legacyStartOffsetSec`(1467행, `legacyOffsetFromCompareFrames`)
= VideoCompare 정렬 시작 오프셋 — 동작 비교 정렬 거동이라 건드리면 이미 PASS 인 표면이 흔들린다.
단 그 주석(1464행)이 "poseFrames 정본과 동일 환산"이라 선언하므로 **주석만** 갱신해 다음 독자가
폐기된 규칙을 재복제하지 않게 한다(거동 변경 0). (b) `buildCueWindows`(1815행) = 음성 큐 창
타이밍 — 별 서브시스템. 두 곳의 초 정합은 백엔드 초 방출 범위가 넓어질 때 별 단위로 판정한다.

**(2) 등재 10동작 일반화 스위프.** `sweep_sheet_blocks.test.ts` 를 quick 디렉터리에 만든다
(`node --test` 로 실행, 신규 의존성 0). `backend/judging_data/criteria/*.yaml` 10개를
`node:fs` + 정규식으로 읽어 `motion` + `joint` 목록을 뽑고, 각 모션마다 합성 record 집합을
만든다: `angle_vs_reference__{joint}`(reference_relative) + `leg_extension`/`arm_extension`
(ipsf_absolute) + `split_angle`(vision) + `line` + `dimension_overall_fallback`.
각 모션에서 `buildDeductionMarkers` → `buildRegionSheetView` 를 전 record 에 대해 돌리고 불변식을
assert 한다:
- 파일 10개 발견 (하드코딩 목록 금지 — glob 파생, 0건이면 FAIL)
- 모든 record 가 정확히 1개 그룹에 귀속 (Σ 블록 수 == record 수, 소실·중복 0)
- 블록 헤더의 번호 == 전역 `recordNumbers` 값 (불일치 0)
- `ipsf_absolute` record 의 methodLine == null · vision record 의 methodLine != null
- zoom 없는 record 의 pairCap/oneCap/basis/facing == null (fabricate 0)
- 어떤 모션에서도 예외 0 · 반환 null 0 (선택 index 유효 시)
- 결과 요약(모션·그룹·블록·널 카운트)을 stdout 표로 출력해 SUMMARY 에 인용한다.

**(3) 시뮬 렌더 대조 (D-40 — 코드 통과 ≠ 완료).** `mcp__ios-simulator__*` 로 앱을 띄우고
검증 doc(파워스핀 80 · 킵업 79 · pdshape 100, `uat-33-16-verification`) 결과 화면에서
**부위 상세 시트를 실제로 열어** 스크린샷을 캡처한다. 캡처를 승인 목업 ② 스크린샷과 나란히 놓고
**항목별 표**로 대조한다 (S6 8항목: 부위 단위·크롭 1쌍·paircap 좌우·기준측 초·onecap /
S7 6항목: 번호 헤더·basis·method·numnote 위치·facing·proof / F-3 1항목: 페어 순간 일치).
- 캡처 파일은 quick 디렉터리에 남긴다 (증거 사슬 — §C-1 의 결손 1건을 반복하지 않는다).
- 최소 3케이스: 부위에 감점 2건 있는 케이스 · 감점 1건 케이스 · advisory/estimatedArea 케이스.
- 33-G 표의 S6·S7·F-3 행을 재채점해 근거와 함께 갱신한다 (proof 축은 M-10 로 미달 명시).
- **OTA 발행 금지** (D-45 — 전 항목 수리 후 1회 일괄).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && node --test .planning/quick/260730-py1-33-g-c-2-1-s7-basis-method-proof-facing-/sweep_sheet_blocks.test.ts && node --test app/src/lib/__tests__/deductionSheet.test.ts && cd app && npm run typecheck && grep -Ec "compareFrames\.(userIdx|refIdx) /" src/app/analysis/result.tsx | grep -qx 0 && echo GATE_OK</automated>
    <human-check>belle 확인 ③ 은 D-45 로 이관 — 이 플랜에서는 요청하지 않는다. Claude 가 캡처한 시뮬 스크린샷과 승인 목업 대조 표가 완료 근거다.</human-check>
  </verify>
  <done>스위프 10동작 불변식 6종 PASS(요약 표 출력) · rep-인덱스 초 추정 2곳 제거 확인 · 시뮬 스크린샷 3케이스 이상 quick 디렉터리에 존재 · 33-G 표 S6/S7/F-3 재채점 갱신(근거 인용) · OTA 미발행</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firestore doc → 앱 렌더 | 백엔드가 쓴 문자열/수치(statusLine·whyLine·criterion·초)가 조판에 들어온다 |
| S3 presigned URL → Image | 크롭 PNG·영상 URL (만료·교체 가능) |
| repo yaml → 테스트 하네스 | 스위프가 `backend/judging_data/criteria/*.yaml` 을 읽는다 (테스트 전용, 런타임 아님) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33G2-01 | Tampering | `deductionSheet.ts` 카피 조립 | mitigate | 목업의 `<b>` HTML 을 문자열로 옮기지 않고 `{text,bold}` 세그먼트 → 중첩 `<Text>` 로 렌더. RN 은 마크업을 해석하지 않으므로 태그 문자열은 화면에 그대로 노출된다(표시 결함) — 문자열에 `<`/`>` 금지 게이트를 테스트로 고정 |
| T-33G2-02 | Spoofing | basis/method/facing/onecap | mitigate | 데이터 미충족 시 문구 생성 금지(fail-closed). 스위프가 "zoom 없는 record → 4필드 null" 을 assert. 거짓 근거는 신뢰 붕괴(반려 2회 이력)이므로 공백이 정답 |
| T-33G2-03 | Information disclosure | paircap/basis 초 노출 | accept | 노출값은 사용자 자신의 영상 시각 + 기준 영상 시각으로, 이미 crop PNG 에 베이크돼 있던 값이다. PII 확대 0 |
| T-33G2-04 | Denial of service | 블록 N개 렌더 | accept | 블록 수는 `deductionBreakdown.records` 유한 집합(엔진 상한)이고 기존 ScrollView 안이다. 부위 그룹은 최대 3~4개 — 가상화 불필요 |
| T-33G2-05 | Repudiation | 시뮬 대조 증거 | mitigate | 스크린샷을 quick 디렉터리에 파일로 남긴다(§C-1 증거 사슬 결손 1건 재발 방지) |
| T-33G2-SC | Tampering | 공급망 | mitigate | npm/pip 설치 0 — 신규 의존성 금지. `app/package.json`·`package-lock.json` diff 0 을 Task 2 done 에 게이트로 둔다. 패키지 설치가 없으므로 legitimacy 감사 대상 아님 |
</threat_model>

<verification>
1. `node --test app/src/lib/__tests__/deductionSheet.test.ts` — 그룹핑·번호·fail-closed 12축
2. `node --test .planning/quick/260730-py1-.../sweep_sheet_blocks.test.ts` — 등재 10동작 일반화
3. `cd app && npm run typecheck` — clean
4. grep 게이트: 신규 모듈 동작명 리터럴 0 · `TIME_STAMP_NOTE` 잔재 0 · 컴포넌트 hex 0 ·
   `compareFrames.(userIdx|refIdx) /` 나눗셈 2건 → 0 (다른 fps 환산 2곳은 범위 밖 — 불변 확인)
5. `git diff --stat -- app/package.json app/package-lock.json` — 빈 출력
6. **시뮬 렌더 + 승인 목업 항목별 대조 표** (D-40 — 이것이 완료 판정이다. 코드 통과는 아니다)
7. 33-G 표 S6·S7·F-3 재채점 (proof 축 미달 = M-10 근거 명시)
8. 채점 무접촉 증명 (D-44): `git diff` 에 `backend/` 변경 0 · 앱 점수 산출 코드 무수정
</verification>

<success_criteria>
- 부위에 감점 2건이면 시트 1개 · 블록 2개 (시뮬 스크린샷으로 확인)
- 크롭 아래 paircap 좌우 초 표기(기준측 포함) + onecap 1줄
- 블록마다 번호 헤더(전역 마커 번호와 일치) · basis · method · 맨 뒤 numnote
- reference_relative 항목이 있는 부위에 facing 안내
- 참고하세요 자세 비교 페어가 크롭과 같은 순간 (rep 인덱스 나눗셈 0)
- 데이터 미충족 시 문구 창작 0 (스위프가 강제)
- 등재 10동작 전건 불변식 PASS · 동작명 분기 0
- 백엔드 변경 0 · 신규 의존성 0 · OTA 미발행 (D-44/D-45)
</success_criteria>

<output>
Create `.planning/quick/260730-py1-33-g-c-2-1-s7-basis-method-proof-facing-/260730-py1-SUMMARY.md`
when done. 필수 기록: 자체 도출 결정 M-1~M-14 적용 결과(신규 도출분은 M-15+ 로 추가) ·
스위프 수치 표 · 열람한 스크린샷 목록과 육안 판정 · 33-G 재채점 표 · §C-4 이관 항목
(`_stamp_time` 제거 · `markingKind` 방출 · `measureWindow*Sec` 방출 · proof 3컷 방출).
</output>
