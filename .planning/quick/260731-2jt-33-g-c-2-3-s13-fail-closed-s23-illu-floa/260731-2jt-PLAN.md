---
phase: quick-260731-2jt
plan: 01
type: execute
wave: 1
depends_on: [quick-260730-l7t, quick-260730-py1, quick-260730-szk]
files_modified:
  - app/src/lib/illustrationScene.ts
  - app/src/lib/__tests__/illustrationScene.test.ts
  - app/src/components/DefectIllustration.tsx
  - app/src/components/VideoCompare.tsx
  - app/src/components/KeypointOverlay.tsx
  - app/src/app/analysis/result.tsx
  - .planning/quick/260731-2jt-33-g-c-2-3-s13-fail-closed-s23-illu-floa/sweep_illustration_scene.test.ts
  - .planning/quick/260731-2jt-33-g-c-2-3-s13-fail-closed-s23-illu-floa/sweep_illustration_scene.json
autonomous: true
requirements: [S13, S25, S23, S26]
user_setup: []

must_haves:
  truths:
    - "어깨 항목 상세 시트에 다리 일러스트가 붙지 않는다 — 항목 부위와 그림 장면이 어긋나면 그 자리에 아무것도 렌더되지 않는다 (빈 카드·점선 박스·안내 문구 0)"
    - "그림 장면과 부위가 일치하는 항목에서는 종전대로 일러스트가 보인다 — 일괄 제거가 아니다"
    - "mode3 · 미등재 동작 · 에셋 미보유 4동작 · 투영 부위 없는 항목은 종전대로 일러스트가 없다 (회귀 0)"
    - "음성 안내가 읽는 동안 영상 카드 우상단에 그 항목의 일러스트가 함께 뜨고, 음성이 끝나면 사라진다"
    - "음성 중 일러스트도 시트와 같은 장면일치 판정을 통과할 때만 뜬다 — 어긋나면 흰 카드조차 뜨지 않는다"
    - "일러스트는 시트에서도 음성 중에도 3:4 원본 비율로 나온다 (잘림·빈 배경 프레임 0)"
    - "일러스트에 캡션·라벨 문장이 붙지 않는다 (그림이 말을 대체 — D-05)"
    - "등재 10동작 전건에서 어느 항목에 붙고 어느 항목에 안 붙는지가 동작명 코드 분기 없이 데이터만으로 갈린다"
  artifacts:
    - path: "app/src/lib/illustrationScene.ts"
      provides: "S13/S25 장면일치 판정 순수 모듈 — 에셋 장면 메타(부위 토큰 + provenance) + 매칭 규칙"
      exports: ["ILLUSTRATION_SCENES", "illustrationMotionForPart", "hasIllustrationFor"]
      min_lines: 70
    - path: "app/src/lib/__tests__/illustrationScene.test.ts"
      provides: "매칭 규칙·fail-closed 단위 검증 (node --test, 신규 의존성 0)"
    - path: "app/src/components/DefectIllustration.tsx"
      provides: "partKey 판정 통과 시에만 렌더하는 일러스트 (require 에셋 맵 소유)"
      contains: "illustrationMotionForPart"
    - path: ".planning/quick/260731-2jt-33-g-c-2-3-s13-fail-closed-s23-illu-floa/sweep_illustration_scene.test.ts"
      provides: "등재 10동작 × 부위 키 일반화 스위프 (criteria yaml glob 파생)"
  key_links:
    - from: "app/src/components/DefectIllustration.tsx"
      to: "app/src/lib/illustrationScene.ts"
      via: "장면일치 판정 소비 (판정 규칙 사본 0)"
      pattern: "from '\\.\\./lib/illustrationScene'"
    - from: "app/src/app/analysis/result.tsx"
      to: "sheetView.partKey"
      via: "시트 일러스트 부착 판정 입력 = 1단위 부위 키"
      pattern: "partKey=\\{sheetView"
    - from: "app/src/components/VideoCompare.tsx"
      to: "renderCueIllustration"
      via: "S23 illu-float — 자리는 VideoCompare, 매핑은 caller (33-14 illustrationSlot 선례)"
      pattern: "renderCueIllustration"
    - from: "app/src/app/analysis/result.tsx"
      to: "regionPartKeyForRecord"
      via: "큐 record 부위 = 시트 부위 = 칩 부위 단일 출처 (두 번째 그룹핑 규칙 금지)"
      pattern: "regionPartKeyForRecord"
---

<objective>
33-G §C-2 앱 수리 3단위 — 승인 목업 7R 대비 FAIL 2건 + PARTIAL 1건을 닫는다.

- **S13 / S25 (FAIL = M-5)**: 일러스트를 **그 항목의 부위·장면과 일치할 때만** 부착한다.
  현재 `DefectIllustration.tsx:26` `VERIFIED_ILLUSTRATIONS` 가 `motionId` 만 키로 써서
  동작당 1장을 그 동작의 **모든 항목**에 공통 부착한다 → 어깨 항목에 다리 일러스트
  (belle #8·#9·#11). 적합성 판정 코드가 0줄이다.
- **S23 (FAIL)**: 음성 중 우상단 **일러스트 동반**(illu-float, 3R 확정 B안). `VideoCompare` 에
  일러스트 코드 0건.
- **S26 (PARTIAL)**: belle #11 "빈 배경 프레임"의 **실체 재현** — 코드상 3:4 정합인데 화면에서
  빈 배경으로 보인 원인이 에셋 구도인지 다른 표면인지 확정한다.

Purpose: 틀린 그림은 없는 것보다 나쁘다(D-43 · D-14 동원리). 어깨를 말하면서 다리 그림을
붙이는 것은 "말하는 부위 = 가리키는 부위" 원리(M-4)의 일러스트 축 위반이고 belle 반려의
직접 사유다. 적합한 에셋이 없으면 **아무것도 안 붙이는 게 정답**이다.

Output: 장면일치 판정 순수 모듈 + 세 표면(시트 · 음성 중 illu-float · 향후 소비처) 단일 규칙
배선 + 등재 10동작 일반화 스위프 산출 + S26 재현 판정 기록.

**범위 밖 (새 범위 금지 — D-43)**: 부위별 일러스트 **신규 생성**(어깨·상체용 세트), 미완
4동작 재시도. 둘 다 새 생성·검수 라운드가 필요해 수리 사이클 밖 deferred 다. 이번 수리의
귀결로 **부착 건수가 줄어드는 것은 결함이 아니라 목적**이다.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-result-trust-recovery/33-G-MOCKUP-DIFF.md
@.planning/phases/33-result-trust-recovery/.continue-here.md
@.planning/phases/33-result-trust-recovery/33-14-SUMMARY.md
@app/src/components/DefectIllustration.tsx
@app/src/lib/deductionSheet.ts
</context>

<spec_extract>
승인 목업 `mockups/index.html` 에서 이 단위가 구현할 스펙 (표적 재확인용 — 실행자는 아래
줄번호를 직접 열어 대조할 것. D-40 "코드 기준 검증 금지").

| 스펙 | 목업 근거 | 원문 요지 |
|---|---|---|
| 일러스트는 **항목(부위)별** 데이터다 | `:1047` `:1073` `:1081` | `legs.illust` = "파워스핀 위·아래 일자 스플릿" / `shoulder.illust` = "그립 어깨 견갑 고정" / `refonly.illust` = **`null`** — 세 시트가 **서로 다른 값**을 갖는다. 동작당 1장 공통이 아니다 |
| 불일치 = 자리 자체가 없음 | `:1114` | `if(d.illust) h += '<div class="illust-slot">…'` — `null` 이면 슬롯 DOM 자체가 안 생긴다 (빈 박스·플레이스홀더 아님) |
| 일러스트 자리 = 시트 최하단 | `:1113-1114` | `facing` 다음, 시트의 마지막 요소 |
| illu-float 기하 | `:215-218` | `position:absolute; right:10px; top:10px; width:104px; background:rgba(255,255,255,.94); border-radius:12px; padding:6px; box-shadow:0 4px 14px rgba(0,0,0,.25)` · `img{width:100%; height:auto}` (= 원본 비율) |
| illu-float 발동 조건 | `:518-538` | `.player` 안에서 `dim` + 부위 강조(`legfx pulse`) + 자막 + `음성 중 — 잠시 멈춤` 과 **같은 상태**에 함께 뜬다 |
| illu-float 좌표계 | `:524` | `legfx viewBox="0 0 360 640"` — `.player` = 360px 폭 화면 1장. 104/360 = **28.9%**, inset 10/360 = **2.8%** |
| M-6 렌더 | `:217` | `width:100%; height:auto` = 원본 3:4 그대로. 특정 비율 크롭 금지 |
</spec_extract>

<self_derived_decisions>
D-39 = 재논의 없음. 승인본이 답을 직접 주지 않는 지점은 "승인본이 정답 / 수리에 새 범위
금지" 원칙으로 자체 도출한다. 1단위 M-, 2단위 N- 를 썼으므로 이번 단위는 **P-** 를 쓴다.
실행자는 적용/변경 여부를 SUMMARY 표에 근거와 함께 적는다.

| # | 결정 | 근거 |
|---|---|---|
| **P-1** | 적합성 판정 단위 = `deductionSheet.regionPartKeyForRecord` 부위 키 (`leg` / `shoulder` / `arm` / `shoulder+arm` / `criterion:{id}`) | 1·2단위가 마커 그룹·칩·시트를 이 키 하나로 통일했다. 일러스트에 두 번째 그룹핑 규칙을 만들면 "칩은 어깨인데 그림 판정은 다른 단위"가 되어 N-16 이 막은 사본이 되살아난다 |
| **P-2** | 매칭 규칙 = **항목 부위 토큰 ⊆ 에셋 장면 토큰** (전부 포함). 부분 겹침(항목 `shoulder+arm` × 에셋 `arm`)은 **불일치** | 승인본 `shoulder.illust` 는 "그립 **어깨** 견갑 고정" = 그 시트가 말하는 부위 전부를 담은 그림이다. 일부만 담은 그림은 나머지 부위에 대해 틀린 그림이고, D-43 "불일치는 숨김이 정답" |
| **P-3** | 토큰 공집합(`criterion:*` 그룹 = 투영 keypoint 0) → **무조건 미부착** | 공집합은 `⊆` 가 vacuously 참이라 모든 에셋과 매칭된다. fail-closed 를 뒤집는 버그이므로 규칙 앞에 명시 게이트를 둔다 |
| **P-4** | 장면 토큰 출처 = 33-14-SUMMARY 「입력 프레임 선정 기록」 + 「검수 게이트 4종 판정 표」 + **에셋 실물 열람**. 문서에 없는 부위는 부여 금지 | 날조 금지. 확신이 안 서면 토큰을 **빼는** 쪽(= 덜 붙는 쪽)이 정답이다 — 안 붙는 것은 D-43 이 허용한 결과이고, 잘못 붙는 것은 belle 반려 사유다 |
| **P-5** | 장면 메타 + 판정 규칙 = 순수 모듈 `lib/illustrationScene.ts` 소유 / `require()` 에셋 맵 = `DefectIllustration.tsx` 소유. 두 표의 키 목록 일치는 **grep 게이트로 봉인** | `require('*.jpg')` 가 있는 모듈은 `node --test` 로 실행 불가(Node 가 jpg 를 못 읽는다). 규칙만 떼어내야 검증 가능. 표가 둘로 갈리면 "메타는 있는데 그림이 없는" 조용한 실패가 나므로 게이트가 한 쌍이다(D-18) |
| **P-6** | illu-float 부착 레벨 = `VideoCompare` 의 `styles.row`(두 슬롯을 감싸는 행) | 앱은 이미 `cueSubtitleWrap`(자막 + "음성 중 — 잠시 멈춤" pill)을 `styles.row` 레벨에 두어 **승인본 `.player` ↔ 앱 `row`** 대응을 확립했다(`VideoCompare.tsx:1466-1483`). 같은 상태에서 함께 뜨는 요소를 다른 레벨에 두면 기준이 갈린다 |
| **P-7** | illu-float 기하 = 승인본 비율 그대로. width = row 폭 × `104/360`, inset = row 폭 × `10/360`, padding·radius 는 테마 토큰(승인본 6/12 에 대응하는 값), 배경·그림자는 승인본 값 | 승인본 `.player` 는 360px 폭 화면 1장이고 앱의 row 가 그 대응(P-6). 절대 px 를 그대로 옮기면 기기마다 비율이 깨진다 |
| **P-8** | illu-float **캡션 없음** (`.illu-float .t` 미이식) | 그 줄("현 채택본 — 후보 선택 시 교체…")은 목업 내부의 **belle 후보-선택 안내**이지 제품 카피가 아니다. D-05(일러스트는 말을 대체) + 33-14 배선 규칙(캡션/라벨 0)과 정면 충돌 |
| **P-9** | illu-float 도 **같은 장면일치 판정**을 통과할 때만 뜬다. 불일치·미등재·mode3 → 흰 카드조차 렌더 0 | 시트에서 숨긴 그림을 영상 위에서 보여주면 같은 결함이 표면만 옮긴 것이다. 세 표면 한 규칙 |
| **P-10** | 시트 안 일러스트의 **자리·순서 불변**(facing 다음, 최하단) | 이번 변경은 "붙일지 말지"만 바꾼다. 1단위에서 렌더 확인된 승인 순서를 건드리면 over-generalize-breaks-approved |
| **P-11** | S26 = **재현 조사**. 렌더 경로가 3:4 정합으로 실증되면 잔여 원인은 에셋 구도이고 재생성은 D-43 로 범위 밖 → 정직 기록 후 deferred. `resizeMode` 를 `contain` 으로 바꾸는 등 레터박스를 만드는 "억지 수정" 금지 | 못 고치는 것을 고친 척하지 않는다. 승인본 M-6 요구는 "원본 그대로"이고 `contain` 은 빈 띠를 만들어 오히려 #11 을 재생산한다 |
| **P-12** | 2단위 이월 정리 — `KeypointOverlay.tsx` 의 `#FFFFFF` 리터럴 11곳을 `colors.textWhite` 로 기계 교체. `rgba(255,255,255,…)` 알파 halo 계열은 대응 토큰이 없어 **신설 없이 유지** | 값이 토큰과 문자 그대로 같아 렌더 결과 불변(2단위가 다음 단위로 넘긴 항목). 알파 토큰 신설은 새 범위이므로 SUMMARY 에 잔여로 명시 |
</self_derived_decisions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 장면일치 판정 순수 모듈 + 등재 10동작 일반화 스위프</name>
  <files>app/src/lib/illustrationScene.ts, app/src/lib/__tests__/illustrationScene.test.ts, .planning/quick/260731-2jt-33-g-c-2-3-s13-fail-closed-s23-illu-floa/sweep_illustration_scene.test.ts, .planning/quick/260731-2jt-33-g-c-2-3-s13-fail-closed-s23-illu-floa/sweep_illustration_scene.json</files>
  <behavior>
    - 장면 토큰이 항목 토큰을 전부 덮으면 `illustrationMotionForPart` 가 그 motionId 를 돌려준다
    - 덮지 못하면 `null` (= 어깨 항목에 다리 그림 부착 차단 — M-5 의 직접 해소)
    - 부분 겹침(항목 `shoulder+arm` × 장면 `['arm']`) → `null` (P-2)
    - 토큰 공집합 partKey(`criterion:...`) → 항상 `null` (P-3 — vacuous ⊆ 차단)
    - `motionId` 가 null/undefined/미등재/에셋 미보유 4동작 → `null` (mode3 fail-closed 회귀 0)
    - `partKey` 가 null/빈 문자열 → `null`
    - `hasIllustrationFor` 판정이 `illustrationMotionForPart` 와 항상 일치 (규칙 사본 0)
    - `ILLUSTRATION_SCENES` 전 항목이 비어있지 않은 `parts` 와 `provenance` 를 갖는다 (근거 없는 등재 차단)
    - `parts` 의 모든 토큰이 `BODY_PART_OF_KEYPOINT` 의 치역(`shoulder`/`arm`/`leg`)에 속한다 (오타 토큰이 조용히 미매칭되는 것 차단)
  </behavior>
  <action>
`app/src/lib/illustrationScene.ts` 를 신설한다 — `deductionSheet.ts` 헤더 규격(상단 결정
주석 + 근거 인용 + 불변식 목록 + 미구현 명시)을 따른다.

**(a) 장면 메타 표 `ILLUSTRATION_SCENES`.** 키 = reference motionId(동작명 코드 분기가
아니라 **데이터 맵** — 33-14 `VERIFIED_ILLUSTRATIONS` 키잉 규약 계승). 값 =
`{ parts: readonly string[]; provenance: string }`.

토큰 부여 절차 (P-4 — 순서대로, 건너뛰기 금지):
  1. `.planning/phases/33-result-trust-recovery/33-14-SUMMARY.md` 의 「입력 프레임 선정
     기록」표(동작별 선정 t 와 국면 근거)와 「검수 게이트 4종 전수 판정 표」의 PASS 행
     세부(그 그림이 실제로 무엇을 그렸는지 + 정직 노트)를 읽는다.
  2. `app/assets/illustrations/{motionId}.jpg` **6장을 Read 로 직접 열어본다**
     (D-40/D-19 — 문서만 보고 토큰을 정하지 않는다). 확인 축 = 가이드 표시(곧은 선 /
     부위 원)가 **어느 부위 위에** 얹혀 있는가, 그림이 강조하는 신체 부위가 무엇인가.
  3. 문서 근거와 실물이 가리키는 부위가 일치하는 토큰만 `parts` 에 넣는다. 어긋나거나
     애매하면 **그 토큰을 뺀다**(덜 붙는 쪽이 정답 — P-4).
  4. `provenance` 에 근거를 1줄로 박제한다 (예: `"33-14 입력 8.50s · 두 다리 폴 축 한 줄 ·
     가이드 = 다리 라인 곧은 선 (실물 열람 확인)"`). 화면 미노출 — 내부 검수용.

⚠ `parts` 는 **관찰 결과**이지 목표가 아니다. 6장이 전부 같은 계열로 수렴하더라도 그것이
정상 귀결이다(어깨용 세트는 D-43 deferred). 반대로 "붙게 하려고" 토큰을 부여하는 것은 날조다.

**(b) 판정 함수.**
  - `illustrationMotionForPart(motionId: string | null | undefined, partKey: string | null |
    undefined): string | null` — ① 입력 유효성 ② partKey 를 `+` 로 쪼개 토큰 집합 산출,
    **공집합이거나 `criterion:` 접두면 즉시 null**(P-3) ③ 등재 장면 조회 ④ 모든 항목
    토큰이 장면 토큰에 포함될 때만 motionId 반환.
  - `hasIllustrationFor(motionId, partKey): boolean` = 위 함수 위임 (독립 구현 금지).
  - 부위 토큰 유효값은 `deductionLabels.BODY_PART_OF_KEYPOINT` 의 치역을 재사용해 검증한다
    (새 부위 사전 신설 금지 — P-1).

**(c) 단위 테스트** `app/src/lib/__tests__/illustrationScene.test.ts` — `deductionSheet.test.ts`
헤더 규격 그대로(실행 방법 · 존재 이유 · 검증 축 목록). `node:test` + `node:assert/strict`,
신규 npm 의존성 0. 위 `<behavior>` 9축 전건 커버.

**(d) 10동작 일반화 스위프** — 2단위 `sweep_markers_focus.test.ts` 를 하네스 선례로
`sweep_illustration_scene.test.ts` 를 만든다. 동작 목록은 `backend/judging_data/criteria/*.yaml`
**glob 파생**(하드코딩 목록 0건, 발견 수 10 미만이면 FAIL). 불변식:
  - INV-1 criteria yaml 10개 발견 · 스위프/모듈 소스에 동작명 조건 분기 0
  - INV-2 동작 × 부위 키(`leg` / `shoulder` / `arm` / `shoulder+arm` / `criterion:split_angle`)
    전 조합의 부착·미부착 판정을 산출해 `sweep_illustration_scene.json` 에 표로 남긴다
  - INV-3 에셋 미보유 4동작(peter-pan · elbow-twist-sister · pdshape · sideway-spin)은 전
    부위 미부착 (기존 fail-closed 회귀 0)
  - INV-4 부착 판정 전건이 "항목 토큰 ⊆ 장면 토큰"을 만족 (반례 0)
  - INV-5 `criterion:*` 부착 0 · `motionId=null`(mode3) 부착 0
  - INV-6 부착 케이스가 **최소 1건 이상** 존재 — 전멸이면 배선이 죽은 코드라는 뜻이므로
    FAIL 로 두고 SUMMARY 에 사실대로 보고한다 (억지 매칭으로 통과시키지 말 것)
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && node --test app/src/lib/__tests__/illustrationScene.test.ts && node --test .planning/quick/260731-2jt-33-g-c-2-3-s13-fail-closed-s23-illu-floa/sweep_illustration_scene.test.ts && test "$(grep -v '^\s*//' app/src/lib/illustrationScene.ts | grep -cE "if[[:space:]]*\(.*'ref-")" = 0 && echo GATE_OK</automated>
  </verify>
  <done>단위 테스트·스위프 전건 통과 · `sweep_illustration_scene.json` 에 10동작 × 부위 판정 표 존재 · 6개 에셋 전부 `provenance` 에 실물 열람 근거 명시 · 동작명 조건 분기 0</done>
</task>

<task type="auto">
  <name>Task 2: S13/S25 시트 배선(fail-closed) + S26 빈 배경 프레임 재현 판정</name>
  <files>app/src/components/DefectIllustration.tsx, app/src/app/analysis/result.tsx</files>
  <action>
**(a) `DefectIllustration.tsx` — 판정 소비.** props 에 `partKey: string | null | undefined`
를 추가한다. 렌더 게이트를 `illustrationMotionForPart(motionId, partKey)` 결과로 바꾼다 —
`null` 이면 종전과 동일하게 `return null`(조용한 hidden, Alert/Toast/배너 0).
`VERIFIED_ILLUSTRATIONS`(require 맵)는 이 파일이 계속 소유하고, 조회 키는 판정 함수가
돌려준 motionId 를 쓴다. 헤더 주석에 D-43 / S13 / P-2 / P-3 근거를 추가하되 기존 D-15 ·
D-18 · D-05 주석은 지우지 말 것(등재 게이트 근거는 여전히 유효).

3:4 렌더(`aspectRatio: 3/4`)와 캡션 0 규칙은 **그대로 유지**한다(M-6 / D-05).

**(b) `result.tsx` 시트 배선.** `illustrationSlot` 의 `<DefectIllustration>` 에
`partKey={sheetView?.partKey ?? null}` 을 넘긴다. `sheetView` 는 이미 이 화면이 들고 있는
1단위 뷰모델(`buildRegionSheetView`)이라 신규 상태·신규 계산 0. `motionId` 배선
(`cmp.mode === 'mode1' ? cmp.referenceMotionId : null`)은 무변경. 슬롯 위치·순서도 무변경(P-10).

**(c) 두 표 drift 봉인 (P-5).** `illustrationScene.ILLUSTRATION_SCENES` 키 목록과
`DefectIllustration.VERIFIED_ILLUSTRATIONS` 키 목록이 완전히 같아야 한다. 아래 verify 의
`diff` 게이트로 확인하고, 어긋나면 **메타 쪽이 아니라 사실 쪽**(에셋 존재 여부)에 맞춘다.

**(d) S26 재현 판정 (P-11).** 아래를 순서대로 수행하고 결과를 SUMMARY 에 사실대로 적는다.
결론이 "재현 안 됨"이어도 그것이 산출이다 — 억지 수정 금지.
  1. 에셋 실물 6장의 **원본 픽셀 크기**를 확인한다(720×964 = 0.7469 vs `3/4` = 0.75 →
     `cover` 크롭 ≈ 0.4%). 크롭률이 실제로 무시할 수준인지 수치로 남긴다.
  2. Task 1 (a)-2 에서 연 그림들이 **구도상 빈 배경 비중이 큰지**(인물 원경 + 여백)
     기록한다 — belle #11 이 "이 일러스트는 뭘까"였고, 어깨 항목에 전신 다리 그림이
     붙으면 그 자체가 "빈 배경 프레임"으로 읽힌다는 가설을 실물로 검증/반증한다.
  3. 일러스트를 렌더하는 **다른 표면이 있는지** 전수 확인한다
     (`grep -rn "DefectIllustration\|assets/illustrations" app/src`). 시트 외 소비처가
     있으면 그쪽 비율 규칙도 함께 점검한다.
  4. 판정을 3분기로 적는다: ① 렌더 경로 결함(수정함) ② 에셋 구도 귀결(§C-4/deferred 이관,
     D-43 근거 명시) ③ 재현 불가(무엇을 못 봤는지 명시 — 시뮬 렌더 필요 축 포함).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck && cd /Users/kimtaesung/Dev/SunityMotion && diff <(grep -v '^\s*//' app/src/lib/illustrationScene.ts | grep -oE "'ref-[a-z0-9-]+'" | sort -u) <(grep -v '^\s*//' app/src/components/DefectIllustration.tsx | grep -oE "'ref-[a-z0-9-]+'" | sort -u) && test "$(grep -v '^\s*//' app/src/components/DefectIllustration.tsx | grep -c '<Text')" = 0 && grep -q 'aspectRatio: 3 / 4' app/src/components/DefectIllustration.tsx && echo GATE_OK</automated>
  </verify>
  <done>typecheck clean · 두 표 키 목록 diff 0 · 시트 일러스트가 `partKey` 판정을 통과할 때만 렌더 · 캡션 `<Text>` 0 · 3:4 규칙 생존 · S26 판정이 3분기 중 하나로 SUMMARY 에 기록됨</done>
</task>

<task type="auto">
  <name>Task 3: S23 illu-float(음성 중 일러스트 동반) + P-12 토큰 정리 + 시뮬 확인 요청</name>
  <files>app/src/components/VideoCompare.tsx, app/src/app/analysis/result.tsx, app/src/components/KeypointOverlay.tsx</files>
  <action>
**(a) `VideoCompare.tsx` — illu-float 자리.** 옵셔널 prop
`renderCueIllustration?: (recordId: string) => React.ReactNode` 를 추가한다(미전달 시 기존
소비처 렌더 diff 0 — `illustrationSlot`(33-14) · `overlay`(OverlayRenderProp) 선례).

렌더 위치 = `styles.row` 안, 자막(`cueSubtitleWrap`)과 **같은 레벨**(P-6). 발동 조건 =
`voiceCueRecordId != null`(승인본 `.player` 의 dim + 강조 + 자막 + "잠시 멈춤"과 같은 상태).

fail-closed 배선(P-9 — 중요): 콜백 결과를 **먼저 변수에 받고, 그 값이 null 일 때는 흰
카드 프레임 자체를 렌더하지 않는다.** `<View style={float}>{render(id)}</View>` 형태로
감싸면 콘텐츠가 null 이어도 빈 흰 카드가 남아 "빈 배경 프레임"을 새로 만든다.

기하(P-7): `useWindowDimensions` 대신 이미 이 컴포넌트가 쓰는 폭 산출 경로를 재사용해
row 폭 기준 비율로 둔다 — width = row 폭 × `104/360`, top/right inset = row 폭 × `10/360`.
배경 `rgba(255,255,255,0.94)` · radius 12 · padding 6 · 그림자는 승인본 값. 색/간격/반경은
가능한 한 `src/theme` 토큰을 쓰고, 토큰이 없는 값(알파 배경·그림자)만 승인본 수치를
주석과 함께 남긴다. `pointerEvents="none"` — 재생바·틱 탭을 가리지 않는다(S20 보호).

⚠ **S18 무접촉**: `voiceCueRecordId` 를 세우고 내리는 tick 로직(`:635-700`)·`speakCue` ·
`stopCue` · 정지/재개 분기는 **한 줄도 건드리지 않는다**. 이미 PASS 인 항목이다.

**(b) `result.tsx` 배선.** `VideoCompare` 에 `renderCueIllustration` 을 넘긴다. 콜백 본문:
recordId → `records` 에서 해당 record 조회 → `regionPartKeyForRecord(rec, vetoFaultJoints)`
로 부위 키 산출(1·2단위 단일 출처 재사용, 새 그룹핑 금지) →
`hasIllustrationFor(motionId, partKey)` 가 false 면 `null` 반환 → true 면
`<DefectIllustration motionId={...} partKey={...} />` 반환. motionId 는 시트와 동일 규칙
(`cmp.mode === 'mode1' ? cmp.referenceMotionId : null`) — mode3 는 자동 null.

**(c) P-12 토큰 정리.** `KeypointOverlay.tsx` 의 `#FFFFFF` 리터럴 11곳을 `colors.textWhite`
로 교체한다(값 동일 = 렌더 결과 불변, `colors` 는 이미 import 되어 있다). `rgba(255,255,255,…)`
알파 계열은 대응 토큰이 없으므로 **건드리지 않고** SUMMARY 잔여로 적는다. 이 파일의 형태·
pulse·게이트 로직은 무접촉(S19/S1/S3/F-8 보호).

**(d) SUMMARY 마무리.** 다음을 반드시 포함한다.
  - `## 시뮬 확인 요청 (오케스트레이터)` 표 — 항목 / 도달 경로 / 무엇을 보면 PASS /
    선결 조건. 33-G 표는 **갱신하지 말고** 재채점 제안만 적는다.
  - **S23 렌더 확인의 선결 조건 (정직 고지)**: `voiceCueRecordId` 는 `speakCue` 가
    `started=true` 를 돌려줄 때만 세워진다(33-13 D-18 고아 가드). 즉 **재생 구동 +
    coachAudio 준비 doc + 음성 안내 토글 ON** 이 전부 성립해야 illu-float 가 화면에
    나타난다. 재생 없이 이 상태를 만드는 진입점은 코드에 없다(디버그 진입점 신설은 새
    범위라 만들지 않는다). 2단위에서 시뮬 재생이 구동되지 않아 S19·S2 가 미검증으로
    남았으므로, S23 도 **재생 구동이 선결**임을 표에 명시하고 구동 실패 시 미검증으로
    남긴다 — PASS 주장 금지.
  - 실행자가 확인한 것 / 못 한 것 구분 (D-18 "무엇을 못 봤는지 명시").
  - P-1~P-12 적용 결과 표 + 신규 도출분(P-13~) 이 있으면 근거와 함께 추가.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck && cd /Users/kimtaesung/Dev/SunityMotion && grep -q 'renderCueIllustration' app/src/components/VideoCompare.tsx && grep -q 'renderCueIllustration' app/src/app/analysis/result.tsx && test "$(grep -v '^\s*//' app/src/components/KeypointOverlay.tsx | grep -c '#FFFFFF')" = 0 && test "$(grep -v '^\s*//' app/src/app/analysis/result.tsx | grep -cE "if[[:space:]]*\(.*'ref-")" = 0 && node --test app/src/lib/__tests__/deductionSheet.test.ts && node --test app/src/lib/__tests__/focusShape.test.ts && node --test app/src/lib/__tests__/cueTrack.test.ts && echo GATE_OK</automated>
  </verify>
  <done>typecheck clean · illu-float 이 음성 큐 상태에서만·판정 통과 시에만 렌더(불일치 시 흰 카드 0) · S18/S20 로직 diff 0 · KeypointOverlay 흰색 하드코딩 0 · 1·2단위 테스트 회귀 0 · SUMMARY 에 시뮬 확인 요청 표 + S23 선결 조건 명시</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firestore doc → 앱 표시 계층 | 저장된 record·zoom 값이 화면 문구·그림 부착 판정의 입력. 앱은 read-only 소비자 |
| 번들 에셋 ↔ 장면 메타 | `require()` 맵과 순수 메타 표가 서로 다른 파일에 산다 (drift 가능) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33G4-01 | Spoofing (거짓 근거 표시) | `illustrationScene.ILLUSTRATION_SCENES` | mitigate | 장면 토큰은 33-14 기록 + 에셋 실물 열람에서만 부여(P-4). `provenance` 필수 필드 + 단위 테스트가 빈 값·미등록 토큰을 FAIL |
| T-33G4-02 | Elevation (fail-closed 우회) | 매칭 규칙 공집합 경로 | mitigate | 토큰 공집합 / `criterion:` 접두는 규칙 앞 명시 게이트로 즉시 null (P-3). 전용 단위 테스트 + 스위프 INV-5 |
| T-33G4-03 | Tampering (두 표 drift) | `ILLUSTRATION_SCENES` ↔ `VERIFIED_ILLUSTRATIONS` | mitigate | Task 2 verify 의 `diff` 키 목록 게이트 (한쪽만 늘면 즉시 FAIL) |
| T-33G4-04 | Denial (승인 표면 파괴) | `VideoCompare` 음성 큐 상태전이(S18) · `KeypointOverlay`(S19/S1/S3/F-8) | mitigate | 상태전이 로직 무접촉 명시 + `pointerEvents="none"` + 1·2단위 테스트 회귀 게이트를 Task 3 verify 에 포함 |
| T-33G4-05 | Information (빈 카드 노출) | illu-float 프레임 | mitigate | 콘텐츠 null 이면 프레임 자체 미렌더 (P-9, Task 3 (a) 명시) |
| T-33G4-SC | Tampering | npm/pip 설치 | accept | 신규 패키지 0 — `package.json` 무변경이 게이트 |
</threat_model>

<verification>
1. **자동**: Task 1~3 의 `<automated>` 게이트 전건 통과 (단위 테스트 · 스위프 · typecheck ·
   grep/diff 게이트 · 1·2단위 회귀 테스트).
2. **산출물 직접 열람 (D-19/D-40)**: 에셋 6장을 실제로 열어 장면 토큰을 정했다는 근거가
   `provenance` 와 SUMMARY 에 남아 있을 것. "문서만 보고 정함"은 미완료다.
3. **일반화 (blocking)**: 등재 10동작 스위프 산출 JSON 이 존재하고, 동작명 조건 분기 0.
   어느 동작 · 어느 부위에 붙고 안 붙는지가 표로 읽힐 것.
4. **오케스트레이터 몫 (실행자 금지)**: 시뮬 실렌더 + 승인 목업 대조 + 33-G 표 재채점.
   실행자는 33-G 표를 갱신하지 않고 제안만 적는다.

**이 산출물이 틀렸다면 어떻게 알았을까 (D-18)**
- 어깨 항목에 다리 그림이 남아 있다 → 스위프 INV-4 반례 + 시뮬에서 어깨 시트 최하단 확인.
- 그림이 전멸했다(부착 0건) → 스위프 INV-6 FAIL.
- 메타는 있는데 에셋이 없다 / 그 반대 → Task 2 키 목록 `diff` 게이트 FAIL.
- illu-float 이 빈 흰 카드로 뜬다 → P-9 배선 규칙 위반, 시뮬 렌더에서 즉시 보임.
- 음성 상태전이가 깨졌다 → S18 표면(정지·dim·"잠시 멈춤")이 시뮬에서 사라짐 + cueTrack 테스트.
</verification>

<success_criteria>
- S13/S25: 항목 부위와 그림 장면이 일치할 때만 부착, 불일치는 자리 자체 없음 (fail-closed).
- S23: 음성 큐 상태에서 row 우상단에 승인본 비율의 illu-float, 같은 판정 통과 시에만.
- S26: 빈 배경 프레임의 실체가 3분기 중 하나로 판정·기록됨 (억지 수정 0).
- 등재 10동작 일반화 스위프 산출 존재, 동작명 코드 분기 0.
- 이미 PASS 인 S18 · S20 · S24 · 1단위(시트 구조) · 2단위(F-8/S1/S3) 무회귀.
- 채점 무접촉(D-44) · 배포 없음(D-45) · 신규 npm 의존성 0 · 새 RN 경고 0.
</success_criteria>

<output>
Create `.planning/quick/260731-2jt-33-g-c-2-3-s13-fail-closed-s23-illu-floa/260731-2jt-SUMMARY.md` when done
</output>
