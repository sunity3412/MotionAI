---
phase: quick-260801-f77
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/lib/api.ts
  - app/src/lib/audioCue.ts
autonomous: true
requirements: [quick-260801-f77]

must_haves:
  truths:
    - "prefetch 는 캐시에 엔트리가 있어도 만료가 임박했으면 URL 을 재발급한다"
    - "분석 문서가 바뀌면 이전 분석의 recordId 캐시가 재사용되지 않는다"
    - "재생이 로드에 실패하면 URL 을 재발급해 다시 시도한다 — 재시도는 1회로 상한"
    - "재시도까지 실패하면 isCueSpeaking() 이 즉시 false 가 되어 다음 tick(100ms)에 양쪽 영상이 재개된다 (15초 대기 없음)"
    - "어떤 실패 경로도 예외를 던져 자막·영상·분석 흐름을 막지 않는다"
    - "큐 하나가 실패해도 다음 큐는 정상 발화한다 (실패 상태가 모듈에 눌러붙지 않는다)"
  artifacts:
    - path: "app/src/lib/api.ts"
      provides: "fetchCoachAudioUrl 이 playbackUrl + expiresInSec 동반 반환"
      contains: "expiresInSec"
    - path: "app/src/lib/audioCue.ts"
      provides: "만료 인지 캐시 + 로드 감시(watchdog) + 1회 재발급 재시도"
      contains: "expiresAtMs"
  key_links:
    - from: "app/src/lib/audioCue.ts"
      to: "app/src/lib/api.ts"
      via: "fetchCoachAudioUrl (prefetch + 실패 시 재발급)"
      pattern: "fetchCoachAudioUrl\\("
    - from: "app/src/lib/audioCue.ts (speechActive)"
      to: "app/src/components/VideoCompare.tsx tick"
      via: "isCueSpeaking() 폴링 — 재개 판정"
      pattern: "speechActive"
---

<objective>
코치 음성 큐가 앱 실행 1시간 뒤 무음이 되고, 무음일 때 양쪽 영상이 15초 멈추는 결함을 수리한다.

Purpose: 결함 3종(A 만료 URL 영구 캐시 / B 실패 복구 경로 부재 / C 실패 시 15초 정지)이
하나의 원인 사슬로 묶여 있다 — 만료된 URL 을 계속 쓰고, 실패를 관측하지 않으니,
`speechActive` 가 true 에 갇혀 `CUE_PAUSE_MAX_MS` 상한까지 영상이 멈춘다.
belle 실기기 관찰 "멈추는 구간도 뭔가 이상해짐" 과 일치한다.

Output: `api.ts` 가 만료 시각을 함께 반환하고, `audioCue.ts` 가 만료를 인지하며
로드 실패를 감시해 1회 재발급 재시도 후 즉시 발화 플래그를 푼다.

**무접촉(수정 금지):** 백엔드 `playback-url` (TTL 3600 은 의도된 보안 설계),
`VideoCompare.tsx`, `fetchVisualAssetUrl`. 신규 의존성 0.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@app/CLAUDE.md
@app/src/lib/audioCue.ts
@app/src/lib/api.ts
</context>

<established_facts>
아래는 소스를 직접 열어 확정한 사실이다. 다시 조사하지 말고 전제로 쓸 것.

**계약 (백엔드 — 무접촉)**
- `backend/functions/playback-url/app.py:52` `_ASSET_EXPIRES = 3600` → coachAudio mp3 presigned URL 수명 1시간.
- 같은 파일 182행: 응답은 이미 `{"playbackUrl": url, "expiresInSec": _ASSET_EXPIRES}` — **만료 정보는 서버가 이미 주고 있다.** 앱이 버리고 있을 뿐이다(`api.ts:190-199`).
- `recordId` 형식 = `^r[0-9]{2}:[A-Za-z0-9_]{1,64}$` (app.py:59, contract.md §12.3 `r{index:02d}:{criterion}`). **분석 간 전역 고유가 아니다** — `r00:...` 는 대부분의 분석에 존재한다.

**결함 A — 만료 URL 영구 캐시**
- `audioCue.ts:47` `urlCache = new Map<string, string>()` — 만료 개념 없음, 어디서도 비워지지 않음.
- `prefetchCueAudio`(158-182)가 `!urlCache.has(id)` 로 필터 → 이미 캐시된 id 는 **재발급조차 하지 않는다.**
- `api.ts:178-179` 주석은 "URL 은 문서에 저장하지 않으므로 재생 시점마다 여기서 재서명한다" 라고 적혀 있다. 캐시가 이 파일 자신의 설계 의도를 무력화했다.
- **추가 발견(같은 캐시의 두 번째 결함):** 캐시 키가 `recordId` 단독이라 분석 A 의 `r00:x` 와 분석 B 의 `r00:x` 가 **같은 칸을 쓴다** → 다른 분석의 음성이 재생될 수 있다. 결과를 연속으로 열람하면 재현 가능. 캐시를 고치는 김에 같이 닫는다.

**결함 B — 재생 실패 복구 경로 없음**
- `speakCue`(194-221)의 try/catch 는 동기 throw 만 잡는다. AVPlayer 의 비동기 로드 실패(403 등)는 catch 되지 않는다.
- ★ **`playbackStatusUpdate` 는 로드 실패 시 아예 발화(emit)되지 않는다.** 근거:
  - `AudioPlayer.swift:147-163` `setupPublisher()` 가 `currentItem?.status` 를 구독하지만 `if status == .readyToPlay` 일 때만 `updateStatus` 를 호출한다. `.failed` 는 **조용히 return** 한다.
  - `addPlaybackEndNotification`(264-290) 은 `AVPlayerItemDidPlayToEndTime` 에만 반응 — 실패 시 오지 않는다.
  - `registerTimeObserver`(292-309) 는 재생 시각이 전진할 때만 emit — 실패하면 전진하지 않는다.
- ★ `AudioStatus` 타입(`expo-audio/build/Audio.types.d.ts:137-169`)에 **error 필드는 없다.** 존재하는 필드는 `currentTime` / `playbackState` / `timeControlStatus` / `reasonForWaitingToPlay` / `mute` / `duration` / `playing` / `loop` / `didJustFinish` / `isBuffering` / `isLoaded` / `playbackRate` / `shouldCorrectPitch` 뿐이다.
- ★ `playbackState` 는 `AVPlayer.status` 문자열화("readyToPlay"/"failed"/"unknown", `StringConversions.swift:3-14`)로 **아이템이 아니라 플레이어** 상태다 — 개별 아이템 403 은 여기 안 잡힌다.
- ★ `isLoaded` 는 `ref.currentItem?.status == .readyToPlay`(`AudioPlayer.swift:51-53`) — **아이템 단위 성공 신호로 유효하다.**
- 결론: 실패는 **이벤트로 관측 불가**하고 오직 "성공 신호(`isLoaded`)의 부재"로만 판정된다 → **감시 타이머(watchdog)가 유일한 수단.** 없는 필드를 지어내지 말 것.
- `updateStatus`(120-130)는 매 emit 마다 `currentStatus()` 전체를 실어 보내므로, 이벤트가 오기만 하면 `isLoaded` 는 항상 읽을 수 있다.

**결함 C — 실패 시 영상 15초 멈춤**
- 로드 실패면 `didJustFinish` 가 안 와서 `speechActive`(58행)가 true 에 갇힌다.
- `VideoCompare.tsx:720-737` 재개 블록이 `!isCueSpeaking() || overMax` 를 기다리므로 `CUE_PAUSE_MAX_MS = 15000`(313행) 까지 양쪽 영상이 멈춘다.
- ★ **따라서 C 는 B 를 고치면 VideoCompare 무수정으로 함께 닫힌다** — 재개 경로가 이미 `isCueSpeaking()` 을 100ms 폴링하고 있다. 우리가 할 일은 그 플래그를 **정직하게** 만드는 것뿐이다.

**blast radius (grep 실측)**
- `fetchCoachAudioUrl` 호출처 = `audioCue.ts` 단 하나. 반환 타입 변경이 밖으로 새지 않는다.
- `audioCue.ts` 의 외부 소비자 = `VideoCompare.tsx` 뿐이고, 공개 API 시그니처는 이 플랜에서 바뀌지 않는다.

**하지 말 것**
- expo-audio 네이티브 우회 금지. `play()` 는 매번 종료 알림을 재등록(63-65)하고, `replaceCurrentSource`(177-195)는 새 `AVPlayerItem` 을 만들어 위치 0 에서 시작하며(`seekTo(0)` 불필요), `isPlaying`(55-57)은 실시간 조회다. 이 3건은 이미 소스로 반증된 오진이다.
- `CUE_PAUSE_MAX_MS` 축소로 때우기 금지. 안전망으로 **그대로 유지**한다.
- `urlCache` 제거 금지 — "재생 시점 네트워크 0 → 자막·음성 동기 유지"가 존재 이유다(158-161행 주석). 만료 인지로 **고치는** 것이지 없애는 게 아니다.
</established_facts>

<tasks>

<task type="auto">
  <name>Task 1: 만료 인지 캐시 — expiresInSec 수용 + 분석 단위 무효화</name>
  <files>app/src/lib/api.ts, app/src/lib/audioCue.ts</files>
  <action>
서버가 이미 주고 있는 `expiresInSec` 를 앱이 받아 쓰게 만들고, `urlCache` 를 만료 인지 캐시로 교체한다.

**api.ts — `fetchCoachAudioUrl`(183-200)만 수정. `fetchVisualAssetUrl`(157-174)은 무접촉.**
- 모듈 상수 `COACH_AUDIO_TTL_FALLBACK_SEC` 를 신설한다. 값은 300 (5분). 근거 주석: 서버가 `expiresInSec` 를 안 주거나 형식이 깨졌을 때 쓰는 보수적 기본값이다. 과다 재발급의 대가는 authed POST 1회지만, 과소 추정의 대가는 무음이므로 짧은 쪽으로 실패한다.
- 반환 타입을 `Promise<string>` 에서 `Promise<CoachAudioUrl>` 로 넓힌다. `export type CoachAudioUrl = { url: string; expiresInSec: number }`.
- `authedJson` 의 제네릭에 `expiresInSec?: unknown` 을 추가한다. `playbackUrl` 검증(문자열·비어있지 않음 → 아니면 기존 `ApiError('malformed_response')`)은 **지금 그대로 유지**한다.
- `expiresInSec` 는 **검증 실패해도 throw 하지 않는다**: `typeof === 'number'` + `Number.isFinite` + `> 0` 을 모두 만족할 때만 채택하고, 아니면 `COACH_AUDIO_TTL_FALLBACK_SEC`. 구 Lambda 가 떠 있어도 앱이 죽지 않게 하는 하위호환 장치다.
- 178-179행 주석 아래에 이번 수리 근거를 덧붙인다: 그 주석이 선언한 "재생 시점마다 재서명" 의도를 앱측 무기한 캐시가 무력화했고(quick-260801-f77), 이제 만료 시각을 함께 넘겨 캐시가 스스로 갱신하게 한다.

**audioCue.ts — 캐시 구조 교체**
- 모듈 타입 `type CachedAudio = { url: string; expiresAtMs: number }` 를 정의하고 `urlCache` 를 `Map<string, CachedAudio>` 로 바꾼다.
- 모듈 상수 `URL_EXPIRY_MARGIN_MS = 120_000` 신설. 근거 주석: 경계에서 재생 도중 만료되면 안 되므로 2분 여유를 둔다. prefetch 와 실제 발화 사이 간격은 사용자의 화면 체류 시간만큼 벌어질 수 있고, 기기 시계 오차도 흡수한다.
- **만료 판정은 단일 순수 함수 하나만 둔다**: `function isFresh(entry: CachedAudio | undefined, nowMs: number): boolean` — `!!entry && entry.expiresAtMs - URL_EXPIRY_MARGIN_MS > nowMs`. prefetch 와 speakCue 가 **모두 이 함수만** 호출한다(판정 로직 중복 금지 — 경계값이 두 군데로 갈라지면 한쪽만 고쳐지는 사고가 난다).
- 모듈 변수 `cachedAnalysisId: string | null = null` 을 추가한다.
- `prefetchCueAudio`(158-182) 수정:
  - `hydrate()` 직후, `cachedAnalysisId !== analysisId` 면 `urlCache.clear()` 하고 `cachedAnalysisId = analysisId` 로 갱신한다. 주석에 이유를 남긴다: `recordId` 는 `r{index:02d}:{criterion}` 이라 분석 간 고유하지 않아서(contract.md §12.3), 무효화 없이는 이전 분석의 mp3 가 재생된다. **주의: `enabled` 검사보다 먼저 해야 한다** — off 상태로 분석을 갈아탄 뒤 켜면 이전 분석 캐시가 살아남는다.
  - 필터 조건을 `!urlCache.has(id)` 에서 `!isFresh(urlCache.get(id), now)` 로 바꾼다. `now` 는 루프 전에 `Date.now()` 로 한 번 잡아 재사용한다. 이것이 "미보유 **또는 만료 임박**" 재발급의 본체다.
  - `.then(...)` 안에서 `{ url, expiresInSec }` 를 받아 `urlCache.set(recordId, { url, expiresAtMs: Date.now() + expiresInSec * 1000 })` 로 저장한다. 개별 실패는 지금처럼 조용히 삼킨다(자막만).
- `speakCue`(194-221) 의 캐시 조회를 만료 인지로 바꾼다. `const entry = urlCache.get(id)` 후 `isFresh(entry, Date.now())` 가 false 면 **재생하지 않고 false 를 반환**한다(자막만 — VideoCompare 가 영상을 멈추지 않으므로 이 큐에서는 정지 자체가 발생하지 않는다). 죽은 게 확실한 URL 로 영상을 멈추는 것이 가장 나쁜 선택이다. 동시에 만료 엔트리를 `urlCache.delete(id)` 하고 fire-and-forget 으로 재발급을 걸어 **다음 큐가 정상 동작**하게 한다. 재발급 헬퍼는 Task 2 와 공유할 것이므로 `async function reissue(recordId: string): Promise<CachedAudio | null>` 로 분리한다 — `cachedAnalysisId` 가 null 이면 즉시 null, 성공 시 캐시에 넣고 엔트리를 반환, 실패는 삼키고 null. 호출부는 `void reissue(id)` (await 없음 — `speakCue` 는 동기 계약).
  - `speakCue` 의 boolean 계약("발화를 시작했는가")과 `player.replace(url)` 사용은 그대로 둔다. Task 2 가 이 자리에 감시를 붙인다.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck</automated>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && grep -v '^\s*\(//\|\*\|/\*\)' src/lib/audioCue.ts | grep -q 'URL_EXPIRY_MARGIN_MS' && grep -v '^\s*\(//\|\*\|/\*\)' src/lib/audioCue.ts | grep -q 'cachedAnalysisId' && grep -v '^\s*\(//\|\*\|/\*\)' src/lib/api.ts | grep -q 'COACH_AUDIO_TTL_FALLBACK_SEC'</automated>
  </verify>
  <done>
- `npm run typecheck` 통과 (반환 타입 확장이 `audioCue.ts` 유일 호출처에 정합).
- 만료 판정이 `isFresh` 단일 함수에만 있고, prefetch·speakCue 두 호출부가 그 함수를 쓴다 (경계 로직 중복 0).
- `prefetchCueAudio` 의 재발급 대상 조건이 "미보유 또는 만료 임박"이다 — `urlCache.has` 단독 필터가 코드에 남아 있지 않다.
- `analysisId` 가 바뀌면 `urlCache` 가 비워지며, 그 판정이 `enabled` 게이트보다 먼저 실행된다.
- `expiresInSec` 부재/형식오류가 예외를 던지지 않고 fallback 상수로 흡수된다.
- `fetchVisualAssetUrl` 과 백엔드는 무접촉.
  </done>
</task>

<task type="auto">
  <name>Task 2: 로드 실패 감시 → 1회 재발급 재시도 → 실패 시 즉시 발화 플래그 해제</name>
  <files>app/src/lib/audioCue.ts</files>
  <action>
만료가 아닌 실패(네트워크 단절·403·S3 객체 삭제)까지 복구하고, 복구 불가일 때 영상이 15초 갇히지 않게 한다.

**설계 전제(established_facts 재확인):** expo-audio 는 로드 실패 시 `playbackStatusUpdate` 를 **보내지 않고**, `AudioStatus` 에 error 필드도 **없다**. 그러므로 실패는 "성공 신호의 부재"로만 판정한다 — 존재하는 필드 `isLoaded` 를 성공 신호로 쓰고, 정해진 시간 안에 그것이 오지 않으면 실패로 간주하는 **감시 타이머**를 둔다. 다른 필드를 실패 판정에 쓰지 말 것(`playbackState` 는 아이템이 아닌 플레이어 상태라 403 을 못 잡는다).

**신규 모듈 상수**
- `SPEECH_LOAD_TIMEOUT_MS = 3000`. 근거 주석에 **파생 관계**를 남긴다: 최대 2회 시도(최초 + 재시도 1회) × 3000ms = 6000ms 로, `VideoCompare` 의 안전망 `CUE_PAUSE_MAX_MS`(15000ms)보다 확실히 짧아야 우리 복구가 안전망보다 먼저 작동한다. URL 은 prefetch 로 이미 확보돼 있고 큐 mp3 는 수 초 분량이라 정상 경로는 1초 내 `isLoaded` 다 — 3초는 학원 wifi 여유분이다.
- `MAX_SPEECH_RETRY = 1`. 재발급 재시도 상한(무한 루프 금지).

**신규 모듈 상태**
- `speechSeq = 0` — 발화 세대 번호. `speakCue` 진입 시와 `stopCue` 에서 증가시킨다. 지연 콜백(감시 타이머·재발급 응답)이 **자기 세대가 아직 유효할 때만** 상태를 건드리게 하는 유일한 장치다. 큐 B 가 큐 A 를 밀어낸 뒤 A 의 타이머가 깨어나 B 를 죽이는 사고를 막는다.
- `loadWatchdog: ReturnType<typeof setTimeout> | null = null`
- `speechRetryCount = 0` — 현재 발화에 한정. `speakCue` 진입 시 0 으로 리셋한다(그래야 큐 하나가 실패해도 다음 큐가 온전한 예산을 받는다).

**헬퍼**
- `clearLoadWatchdog()` — 타이머가 있으면 `clearTimeout` 하고 null.
- `pausePlayerSafely()` — `try { if (player?.playing) player.pause(); } catch {}`. `stopCue` 의 기존 로직과 동일 형태로 뽑아 공유한다.
- `failSpeech(seq: number)` — `seq !== speechSeq` 면 즉시 return(만료된 세대). 아니면 `clearLoadWatchdog()`, `playingCueId = null`, `speechActive = false`, `pausePlayerSafely()`. **`speechActive = false` 가 결함 C 의 수리 본체다** — `VideoCompare.tsx:720-737` 가 100ms tick 으로 `isCueSpeaking()` 을 폴링하고 있으므로 다음 tick 에 양쪽 영상이 재개된다. `pausePlayerSafely()` 를 같이 부르는 이유도 주석에 남긴다: 뒤늦게(예: 10초 뒤) 로드가 성공하면 이미 재개된 영상 위로 음성이 튀어나온다.
- `armLoadWatchdog(seq: number)` — `clearLoadWatchdog()` 후 `setTimeout(() => onLoadTimeout(seq), SPEECH_LOAD_TIMEOUT_MS)`.

**`onLoadTimeout(seq)` — 감시 만료 처리**
1. `seq !== speechSeq` 면 return.
2. `player?.isLoaded` 가 true 면 이벤트를 놓쳤을 뿐 로드는 된 것이므로 `clearLoadWatchdog()` 만 하고 return (오탐 방지 — 이벤트가 아닌 실시간 프로퍼티로 재확인한다).
3. `speechRetryCount >= MAX_SPEECH_RETRY` 이거나 `playingCueId` 가 null 이면 `failSpeech(seq)`.
4. 그 외 = 재시도 1회: `speechRetryCount += 1`, 대상 `recordId = playingCueId` 를 캡처하고 `urlCache.delete(recordId)` (죽은 엔트리 축출), Task 1 의 `reissue(recordId)` 를 호출한다. 응답 시점에 `seq !== speechSeq` 이거나 결과가 null 이면 `failSpeech(seq)`. 성공이면 `player.replace(entry.url)` → `declareAudioMode()` → `player.play()` → `armLoadWatchdog(seq)` 로 감시를 다시 건다. 이 블록 전체를 try/catch 로 감싸고 catch 에서 `failSpeech(seq)` (동기 throw 도 무음+정지로 남지 않게). **catch 나 어디에서도 presigned URL 문자열을 로그·에러 메시지에 싣지 말 것**(서명 노출 — T-f77-02).

**`attachStatusListener`(62-71) 확장**
- 기존 `didJustFinish` 분기에 `clearLoadWatchdog()` 를 추가한다.
- `if (status.isLoaded) clearLoadWatchdog()` 분기를 추가한다. 로드가 확인되면 403 계열 실패는 배제된 것이므로 감시를 내린다. 로드 후 중간 정지(stall)는 `CUE_PAUSE_MAX_MS` 안전망이 계속 담당한다 — 이 플랜에서 그 상수는 건드리지 않는다.
- 리스너는 지금처럼 1회만 부착되고 `replace` 를 관통해 유지된다(`play()` 가 종료 알림을 매번 재등록하므로 이벤트 유실 없음 — 이미 반증된 오진을 되살리지 말 것).

**`speakCue` 확장 (성공 경로만)**
- `playingCueId = id; speechActive = true;` 세팅 자리에서 `speechSeq += 1`, `const mySeq = speechSeq`, `speechRetryCount = 0` 을 함께 처리한다.
- `player.play()` 직후 `armLoadWatchdog(mySeq)`.
- 기존 catch 블록(215-220)에도 `clearLoadWatchdog()` 를 넣어 동기 실패 시 타이머가 남지 않게 한다.
- **반환 계약은 그대로 true** 다. 아래 주석을 코드에 남긴다: `speakCue` 의 boolean 은 "지금 발화를 시작했다(= VideoCompare 가 영상을 멈출 근거)" 라는 뜻이지 "끝까지 성공한다" 는 약속이 아니다. 비동기 실패는 반환값을 뒤집는 대신 **VideoCompare 가 이미 폴링 중인 같은 채널**(`isCueSpeaking()`)로 드러난다 — `failSpeech` 가 `speechActive` 를 내리면 다음 tick(100ms)에 영상이 재개된다. 그래서 `VideoCompare.tsx` 는 이 수리에서 **한 줄도 바뀌지 않는다.**

**`stopCue`(224-233) 확장**
- 첫 줄에서 `speechSeq += 1` 로 세대를 올리고 `clearLoadWatchdog()` 를 호출한다. 사용자가 일시정지·seek·화면 이탈했는데 지연 콜백이 깨어나 재생을 되살리는 경로를 구조적으로 차단한다. 기존 pause 로직은 `pausePlayerSafely()` 로 대체(동작 동일).

**교차 확인:** `stopCue` 는 VideoCompare 의 pause(904)·언마운트(582, 1011, 1131)·overMax(726)·토글 off 에서 호출된다. 세대 증가가 이 전 경로의 잔여 타이머를 무효화한다.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck</automated>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && grep -v '^\s*\(//\|\*\|/\*\)' src/lib/audioCue.ts | grep -q 'MAX_SPEECH_RETRY' && grep -v '^\s*\(//\|\*\|/\*\)' src/lib/audioCue.ts | grep -q 'SPEECH_LOAD_TIMEOUT_MS' && grep -v '^\s*\(//\|\*\|/\*\)' src/lib/audioCue.ts | grep -q 'speechSeq'</automated>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && test -z "$(git status --porcelain -- app/src/components/VideoCompare.tsx backend/)"</automated>
    <human-check>belle 실기기(TestFlight) 확인 — 시뮬레이터는 오디오 실측 불가라 코드 게이트로 대체 불가. SUMMARY 의 "belle 확인 필요" 로 분리해 기록할 것: (1) 앱을 1시간 이상 띄워둔 뒤 결과 화면 재진입 → 음성이 여전히 들리는가. (2) 기내모드로 음성 로드를 강제 실패시켰을 때 영상이 약 6초 안에 재개되는가(15초 정지 없음). (3) 실패한 큐 다음 큐가 정상 발화하는가. (4) 결과 A 를 본 뒤 결과 B 로 이동했을 때 A 의 음성이 새어나오지 않는가.</human-check>
  </verify>
  <done>
- `npm run typecheck` 통과.
- 재시도가 `MAX_SPEECH_RETRY` 로 상한 1회다 — 실패 후 재진입 경로에서 `speechRetryCount` 를 다시 0 으로 만드는 곳은 `speakCue` 진입뿐이다(무한 재발급 루프 불가).
- 지연 콜백(감시 타이머·재발급 응답)이 상태를 쓰기 전에 전부 `seq !== speechSeq` 가드를 통과한다 — 가드 없는 지연 쓰기 0.
- 복구 불가 판정 시 `speechActive` 가 즉시 false 가 되고 플레이어가 pause 된다.
- 최악 경로 총 지연이 `2 × SPEECH_LOAD_TIMEOUT_MS = 6000ms` 로 `CUE_PAUSE_MAX_MS`(15000ms) 안전망보다 짧다. `CUE_PAUSE_MAX_MS` 는 값·위치 모두 무변경.
- 실패 판정에 `isLoaded` 외의 필드를 쓰지 않는다 (존재하지 않는 error 필드 가정 0).
- `VideoCompare.tsx` 와 `backend/` diff 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 앱 → `POST /playback-url` | 인증된 호출. 클라이언트는 `analysisId` + `recordId` 만 넘기고 S3 key 선택은 전적으로 서버 몫 |
| 앱 → S3 presigned GET | 서명된 URL 이 앱 메모리에 상주. 유효기간 1시간 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-f77-01 | Denial of Service (자해) | `onLoadTimeout` 재발급 루프 | mitigate | `MAX_SPEECH_RETRY = 1` + `speechRetryCount` 리셋 지점을 `speakCue` 진입 한 곳으로 제한 + 세대(`speechSeq`) 가드. 실패가 authed POST 난사로 번지지 않음 |
| T-f77-02 | Information disclosure | 실패 처리 catch 블록 | mitigate | presigned URL(서명 포함) 을 로그·에러 문자열에 싣지 않는다. 기존 조용한 폴백 관례 유지 (`audioCue.ts` 전 catch 블록) |
| T-f77-03 | Tampering | 재발급 경로의 S3 key 선택 | transfer | 재발급도 동일한 `fetchCoachAudioUrl(analysisId, recordId)` 만 경유 — 서버 canonical key 구성 + 저장 key exact 비교 규율(playback-url `_handle_coach_audio`) 불변. 앱이 key 를 만드는 경로는 신설되지 않음 |
| T-f77-04 | Information disclosure | 분석 간 캐시 키 충돌 | mitigate | `recordId` 는 분석 간 고유하지 않음 → `cachedAnalysisId` 불일치 시 `urlCache.clear()` (Task 1). 타 분석 음성 재생 차단 |
| T-f77-SC | Tampering (supply chain) | 패키지 설치 | N/A | 신규 의존성 0 — `npm install` 없음. 패키지 정당성 게이트 비해당 |
</threat_model>

<verification>
1. `cd app && npm run typecheck` — 이 앱의 유일한 정적 게이트 (JS 테스트 러너 없음).
2. `git diff --stat` — 변경 파일이 `app/src/lib/api.ts`, `app/src/lib/audioCue.ts` 두 개뿐인지 확인. `VideoCompare.tsx`·`backend/` 가 목록에 있으면 실패.
3. `git diff` 육안 검토 — 만료 판정이 `isFresh` 한 곳, 재시도 상한이 `MAX_SPEECH_RETRY` 한 곳, 지연 콜백 전부에 세대 가드.
4. 기기 확인은 belle 몫 (Task 2 `<human-check>` 4항목). **done 조건에 "음성이 들린다" 를 넣지 않는다** — 시뮬레이터로 실측 불가.
</verification>

<success_criteria>
- 만료 임박 URL 이 prefetch 시점에 재발급된다 (`urlCache.has` 단독 필터 소멸).
- 분석 전환 시 캐시가 무효화된다.
- 로드 실패가 감시 타이머로 관측되고 1회 재발급 재시도 후, 복구 불가면 `speechActive` 가 즉시 풀린다.
- 최악 지연 6초 < 안전망 15초. `CUE_PAUSE_MAX_MS` 무변경.
- `VideoCompare.tsx`·백엔드·`fetchVisualAssetUrl` 무접촉, 신규 의존성 0.
- `npm run typecheck` 통과.
</success_criteria>

<output>
`.planning/quick/260801-f77-presigned-url-15/260801-f77-SUMMARY.md` 작성.

SUMMARY 에 반드시 포함할 것:
- **belle 확인 필요** 섹션 — Task 2 `<human-check>` 4항목을 그대로 옮긴다. 코드 게이트로 대체 불가임을 명시.
- **OTA 필요** — belle 가 기기에서 확인하려면 OTA 발행이 선행되어야 한다. 발행 자체는 이 플랜 범위 밖(별도 일괄 OTA 절차).
- **미검증으로 남는 것** — 실기기 음성 재생은 이 사이클에서 검증되지 않았다. 코드 수준 근거(타입체크·재시도 상한·세대 가드)와 기기 확인 대기를 구분해 적을 것. 확인하지 않은 것을 확인한 것처럼 쓰지 말 것.
- **F-6(음성 무음 별건)** 은 이 플랜의 스코프가 아니다 — 이미 아는 미해결 건이며 이번 수리로 닫혔다고 주장하지 말 것.
</output>
