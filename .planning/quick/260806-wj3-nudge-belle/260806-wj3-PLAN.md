---
phase: quick-260806-wj3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/lib/audioCue.ts
  - app/src/app/analysis/result.tsx
  - app/src/components/VideoCompare.tsx
autonomous: true
requirements: [belle-①, belle-②, belle-④, D-13, IN-01]
must_haves:
  truths:
    - "큐 N 발화 시 재생되는 오디오 아이템은 큐 N 의 URL 로 만들어진 것뿐이다 — 다른 큐의 mp3 가 재생될 수 있는 경로가 코드에 존재하지 않는다"
    - "오디오 로드가 실패하면 결과는 무음(자막만)이며, 절대 이전 큐 음성으로 대체되지 않는다 (fail-closed)"
    - "attributionReliability.unreliable doc 에서도 진행 바에 감점 시점 틱이 렌더된다 (탭 = 그 시점 seek + 해당 감점 시트 열기)"
    - "저신뢰 doc 의 나머지 IN-01 강등(영상 위 마커/번호/그룹/전체화면 범례/헤드라인)은 그대로 억제된다 — 틱만 복원"
    - "재개 관찰창에서 **마지막** play() 재시도 직전, 안 도는 쪽에 제자리 seek 가 1회 발사된다"
    - "converge-pause 최종 폴백과 판정 순수 함수의 계약은 변하지 않는다 (playbackInvariant.ts diff 0)"
  artifacts:
    - path: "app/src/lib/audioCue.ts"
      provides: "큐마다 플레이어 재생성 — 스테일 아이템 구조적 배제 (releasePlayer / activatePlayer)"
      contains: "activatePlayer"
    - path: "app/src/app/analysis/result.tsx"
      provides: "저신뢰 doc 타임라인 틱 복원 (IN-01 강등 범위에서 틱만 제외)"
      contains: "overlayTimelineTicks = timelineTicks"
    - path: "app/src/components/VideoCompare.tsx"
      provides: "마지막 재시도 직전 제자리 seek nudge (적용측 — 판정 함수 무접촉)"
      contains: "RESUME_PLAY_RETRIES"
  key_links:
    - from: "speakCue / onLoadTimeout 재발급"
      to: "createAudioPlayer(url)"
      via: "activatePlayer 단일 경유 지점 (replace 경로 소멸)"
      pattern: "activatePlayer\\("
    - from: "VideoCompare tick 집행 블록 (retry-play)"
      to: "leftPlayer/rightPlayer.currentTime 제자리 대입"
      via: "resumeRetriesRef.current === RESUME_PLAY_RETRIES - 1 일 때만"
      pattern: "RESUME_PLAY_RETRIES - 1"
---

<objective>
belle 실기기 관측 3건(①②④)을 수리한다. 셋 다 **오늘(08-06) OTA `ac79c72e` 적용 상태**에서
belle 이 엘보 doc 으로 직접 본 것이다.

- **①** 큐2(오른어깨, 6.7s) 시점에 자막은 큐2인데 **음성은 큐1(왼엉덩)이 처음부터 다시** 났다.
  → 플레이어 **재사용(replace)** 이 실패했을 때 이전 큐의 끝난 아이템이 남아 있고, `play()` 가
  그 끝난 아이템을 처음부터 재재생하는 구조. **fail-closed 로 뒤집는다**: 큐마다 플레이어를
  새로 만들어 "남의 음성"이 재생될 경로 자체를 없앤다. 실패의 결과는 무음(자막만)이어야 한다.
- **②** 진행 바에 감점 마커(①②③) 표시가 없다 — belle 은 이것을 "틀림"으로 읽는다.
  → IN-01(저신뢰) 강등이 타임라인 틱까지 비우고 있다. **틱만** 복원한다.
- **④** 큐2 음성 종료 후 둘 다 멈췄다 — 이는 오늘 넣은 불변식의 converge-pause 가
  **설계대로** 작동한 것이다(편측 대신 대칭 정지). 불변식은 유지하되 **회복력**을 올린다:
  마지막 재시도 직전에 제자리 seek 를 1회 넣는다.

**③(큐1 후 정상 재개)은 수리 대상이 아니라 오늘 불변식 집행이 작동한 증거다 — 건드리지 않는다.**

Purpose: ①은 "AI가 엉뚱한 말을 한다"로 읽히는 신뢰 파괴이고, ②는 "분석이 틀렸다"로 읽힌다.
둘 다 분석 정확도가 아니라 **전달 경로**의 결함이다.
Output: 파일 3개 수정, 커밋 3개(task 단위 atomic). 판정 순수 함수·채점·계약은 무접촉.
</objective>

<source_audit>

| 출처 | 항목 | 상태 | 담는 곳 |
|---|---|---|---|
| GOAL | belle ① 자막·음성 불일치(이전 큐 재재생) | COVERED | Task 1 |
| GOAL | belle ② 진행 바 감점 마커 부재 | COVERED | Task 2 |
| GOAL | belle ④ 종료 후 양쪽 정지 — 회복력 개선 | COVERED | Task 3 |
| CONTEXT | fail-closed: 실패 결과는 무음, 남의 음성 금지 | COVERED | Task 1 (구조적 배제 + 게이트) |
| CONTEXT | IN-01 승인 설계 존중 — 틱 외 강등 무접촉 | COVERED | Task 2 (잔존 6건 카운트 게이트) |
| CONTEXT | 불변식 불변 — converge-pause 최종 폴백 유지 | COVERED | Task 3 (playbackInvariant.ts diff 0 게이트) |
| CONTEXT | f77 세대(seq) 무효화 설계와 모순 없는 주석 갱신 | COVERED | Task 1 action |
| CONTEXT | 12축·6·7 테스트 기준선 유지 | COVERED | 전 task verify (25 pass) |

**범위 밖(고의)**: belle ③(정상 동작 — 무접촉) · EAS OTA 발행(오케스트레이터) · 시뮬 렌더 확인
(오케스트레이터) · ① 의 실기기 실효 확인(기기 네트워크 실패가 전제라 시뮬 재현 불가) ·
음성 무음(F-6) 원인 규명 · V-2(시작 음성) — belle 결정 대기 건이라 이번에 손대지 않는다.
</source_audit>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@app/CLAUDE.md
@app/src/lib/audioCue.ts
@app/src/lib/playbackInvariant.ts
@.planning/quick/260806-usc-v-1/260806-usc-PLAN.md
</context>

<interface_contract>
읽고 시작할 것 — 실행자가 코드베이스를 뒤지지 않게 여기에 좌표와 계약을 박는다.
**현 HEAD 기준 실측 행번호**이며, 편집으로 밀리므로 각 task 착수 시 앵커 문자열로 재확인한다.

**A. `app/src/lib/audioCue.ts` (463행)**

| 위치 | 내용 |
|---|---|
| 19-21 | 모듈 헤더 — "단일 모듈 플레이어를 **재사용(replace)** 해 리소스를 아낀다" (**이번에 철회되는 전제**) |
| 79-80 | `let player: AudioPlayer \| null = null;` / `let playingCueId: string \| null = null;` |
| 87-88 | `let speechActive = false;` / `let statusListenerAttached = false;` |
| 109-122 | `SPEECH_LOAD_TIMEOUT_MS=3000` / `MAX_SPEECH_RETRY=1` / `speechSeq` / `speechRetryCount` |
| 132-138 | `pausePlayerSafely()` — `try { if (player?.playing) player.pause(); } catch {}` |
| 151-157 | `failSpeech(seq)` — 세대 가드 + 감시 해제 + `speechActive=false` + pause |
| 168-208 | `onLoadTimeout(seq)` — 재발급 후 **`player?.replace(entry.url)`** → `declareAudioMode()` → `player?.play()` → `armLoadWatchdog(seq)` |
| 212-225 | `attachStatusListener()` — `didJustFinish` 시 `speechActive=false; playingCueId=null` (**아이템은 비우지 않는다** = ① 의 빈틈) |
| 396-447 | `speakCue(cue)` — 418 동일 큐 재요청 조기 return / 422-424 세대·예산 / 425-430 **`createAudioPlayer(url)` 또는 `player.replace(url)`** / 431-438 리스너·플래그·모드선언·play·감시 |
| 457-463 | `stopCue()` — 세대 +1, 감시 해제, 플래그 내림, `pausePlayerSafely()` (리소스 유지) |

**expo-audio 실측 API** (`app/node_modules/expo-audio/build/AudioModule.types.d.ts`):
`export declare class AudioPlayer extends SharedObject<AudioEvents>`(15) ·
`replace(source)`(134) · `remove(): void`(176, 문서 원문 "Remove the player from memory to
free up resources") · `createAudioPlayer(source?, options?): AudioPlayer`(ExpoAudio.d.ts:183).
**teardown 은 문서화된 `remove()` 를 쓴다** (base 의 `release()` 아님).

**B. `app/src/app/analysis/result.tsx` (3933행)**

| 위치 | 내용 |
|---|---|
| 1308-1316 | `timelineTicks = useMemo(buildDeductionTicks(records, markers.recordNumbers, result.visionVeto))` |
| 1337-1340 | IN-01 강등 주석 — "번호가 사라졌으므로 범례/**틱도 빈 배열**로 두어 모순 방지" (**이번에 갱신 대상**) |
| 1341-1376 | `overlayHighlightKeypoints` / `overlayAttentionKeypoints` / `overlayGroupMarkers` / `overlayMarkerNumbers` / `overlayForceHighlightWorstCount` — **전부 무접촉** |
| 1377 | `overlayFullscreenLegend = attributionUnreliable ? [] : fullscreenLegend;` — **무접촉** |
| 1378 | `overlayTimelineTicks = attributionUnreliable ? [] : timelineTicks;` — **이 한 줄만 수리** |
| 1426-1429 | `openRecordByNumber(n)` — `markers.recordNumbers.indexOf(n)` → 시트 열기. **저신뢰 게이트 없음** |
| 2581-2588 | `<VideoCompare timelineTicks={overlayTimelineTicks} tickFrameCount={anglesFrames ?? 0} … onTickPress={openRecordByNumber} />` |

**틱이 고아가 아니라는 근거(= 복원해도 모순이 아닌 이유)**: 틱 번호는 `markers.recordNumbers`
로 만들어지고, 그 번호는 저신뢰 doc 에서도 **점수 계산 내역 행/시트**에 그대로 렌더된다
(belle 실기기: 저신뢰 0.49 인데 감점 카드 5장 확정 출력 — 이번에 바꾸지 않는 기존 사실).
반면 **영상 위** 번호·그룹·전체화면 범례는 계속 숨긴다 — 비대칭은 의도된 것이고 SUMMARY 에 적는다.

`buildDeductionTicks` (`app/src/lib/deductionLabels.ts:404-423`) 는
`visionVeto.status !== 'applied'` 또는 `sourceFrameIndices.user` 가 비면 `[]` 를 돌려주고,
성립하면 **번호를 전부 병합한 틱 1개**(window median 프레임)를 돌려준다 — 즉 복원의 정상 결과는
"①②③④⑤ 라벨이 붙은 틱 1개"이지 틱 5개가 아니다. 이건 기존 승인 동작(r6v)이며 변경 대상 아님.

**C. `app/src/components/VideoCompare.tsx` (2449행)**

| 위치 | 내용 |
|---|---|
| 32-35 | `import { decidePlaybackInvariant, RESUME_WATCH_TICKS } from '../lib/playbackInvariant';` (**RESUME_PLAY_RETRIES 미수입**) |
| 567-568 | `resumeWatchTicksRef` / `resumeRetriesRef` |
| 792-798 | 관찰창 진행(+1/닫기) + `startHoldActive` 산출 |
| 801-811 | `decidePlaybackInvariant({...})` 호출 |
| 812-827 | 집행 — `left/right` 커맨드 적용 → `consumeRetry` → `closeWatch` → `setPlaying(false)` → 조기 `return` (**이 블록 안에 nudge 를 넣는다**) |
| 651/658-659 | `player.currentTime = ...` 대입 선례(expo-video) |

**판정 계약(`app/src/lib/playbackInvariant.ts`)은 이번에 한 글자도 바꾸지 않는다** — 12축
테스트 동반 갱신을 피하고, 불변식(R7 3회 → R8 converge-pause)을 그대로 보존하기 위해서다.

**실측 기준선 (이 계획 착수 시점, 전부 직접 실행해 확인함)**

```
npm --prefix app run typecheck                      → GREEN (무출력)
node --test <playbackInvariant|manualOffset|cueTrack>.test.ts → 25 pass / 0 fail
audioCue.ts   'player?.replace('  (주석 제외) = 2   → 수리 후 0
audioCue.ts   'createAudioPlayer(' (주석 제외) = 1  → 수리 후 1 (activatePlayer 안 단 하나)
audioCue.ts   'player = null'      (주석 제외) = 0  → 수리 후 1 (releasePlayer 안 단 하나)
result.tsx    'overlayX = attributionUnreliable' = 7 → 수리 후 6 (틱 1건만 이탈)
VideoCompare  'RESUME_PLAY_RETRIES'            = 0 → 수리 후 >= 2 (import + 사용)
VideoCompare  'setInterval('       (주석 제외) = 1  → 불변 (신규 타이머 0)
```
</interface_contract>

<tasks>

<task type="auto">
  <name>Task 1: 큐마다 플레이어 재생성 — 스테일 아이템 재재생 차단 (belle ①)</name>
  <files>app/src/lib/audioCue.ts</files>
  <action>
    **원리**: `replace()` 가 이번 큐의 URL 로드에 실패하면 플레이어에는 **이전 큐의 아이템**이
    남고, 직후 `play()` 가 그 아이템을 재생한다(끝난 아이템이면 처음부터). 즉 "남의 음성"은
    replace 라는 단 하나의 벡터로만 들어온다. 그 벡터를 없앤다 — **모든 재생 아이템은
    `createAudioPlayer(url)` 로만 태어난다.** 신규 생성은 요청한 URL 로만 만들어지므로 로드
    실패의 결과는 구조적으로 무음(자막만)이다.

    (1) **`releasePlayer()` 신설** (`pausePlayerSafely` 아래):
    지역 변수로 현재 player 를 잡고 → `player = null; statusListenerAttached = false;` 를 **먼저**
    수행(재진입 시 죽은 참조를 보지 않게) → 지역 변수가 있으면 `try { if (p.playing) p.pause(); } catch {}`
    → `try { p.remove(); } catch {}`. `remove()` 는 expo-audio 문서화 teardown API(위 계약 표).
    두 try/catch 는 released 객체·플랫폼 예외 무해화 — 기존 `pausePlayerSafely` 관례와 동일.

    (2) **`activatePlayer(url: string): AudioPlayer` 신설**:
    `releasePlayer()` → `const p = createAudioPlayer(url); player = p; attachStatusListener();`
    → `return p`. **`createAudioPlayer(` 호출은 이 함수 안 단 하나여야 한다**(생명주기 단일 소유자).
    반환값을 쓰는 이유는 TS 좁히기 — 호출부에서 `player?.` 옵셔널 체이닝 없이 `p.play()` 한다.

    (3) **`speakCue` 교체** (418행 동일 큐 조기 return 은 **그대로 앞에 남긴다** — 같은 큐
    재요청이 재시작 stutter 를 만들면 안 된다): 425-430 의 `if (!player) createAudioPlayer(url)
    else player.replace(url)` 분기와 431 의 `attachStatusListener()` 를 `const p = activatePlayer(url);`
    한 줄로 대체하고, 437 의 `player.play()` 를 `p.play()` 로 바꾼다. `speechSeq` 증가·
    `speechRetryCount=0`·`playingCueId`·`speechActive`·`declareAudioMode()`·`armLoadWatchdog(mySeq)`·
    catch 블록은 **순서·내용 그대로**.

    (4) **`onLoadTimeout` 재발급 경로 교체**: `player?.replace(entry.url)` → `const p = activatePlayer(entry.url);`,
    `player?.play()` → `p.play()`. 같은 큐라도 그 시점 플레이어에는 **로드 실패한 아이템**이
    남아 있으므로 동일 규칙을 적용한다. `declareAudioMode()` / `armLoadWatchdog(seq)` / 세대 가드 /
    catch→`failSpeech(seq)` 는 불변.

    (5) **`stopCue` 에 정리 적용**: `pausePlayerSafely()` → `releasePlayer()`. 정지 이후에는
    어떤 아이템도 남지 않는다(정지·seek·언마운트·토글 off 전 경로가 이 무효화를 받는다).
    `speechSeq += 1` 등 나머지 줄은 불변. **`failSpeech` 는 `pausePlayerSafely()` 를 유지한다** —
    거기서 release 하면 뒤늦게 도착하는 재발급 then 콜백이 죽은 참조를 만지게 되고, 세대 가드가
    이미 유령 음성을 막고 있어 추가 이득이 없다.

    (6) **`didJustFinish` 핸들러(212-225)는 내용 무변경** — 대신 **왜 여기서 release 하지 않는가**를
    주석으로 남긴다: (a) 네이티브 이벤트 콜백 안에서 공유 객체를 해제하는 재진입을 피하고,
    (b) 다음 발화가 어차피 `activatePlayer` 로 이전 플레이어를 해제하며, (c) 큐 종료 시
    `VideoCompare` 가 `stopCue()` 를 부르는 경로도 있어 메모리도 실제로 회수된다.

    (7) **주석 정합 (필수)**: 모듈 헤더 19-21 의 "재사용(replace)해 리소스를 아낀다" 문장을
    새 전제로 갱신한다 — "큐마다 새 플레이어를 만든다. 리소스 절약보다 **아이템 정합**이
    우선이다(스테일 아이템 재재생 = 자막과 다른 음성 = 신뢰 파괴)". `speakCue` docstring
    (380-395)의 "새 큐는 replace 로 이전 발화를 자동 중단" 도 "새 큐는 이전 플레이어를
    해제하고 새로 만든다(이전 발화 중단은 그대로 성립)"로 고친다. **f77 의 세대(seq) 무효화
    설계는 유지된다는 점을 명시** — 이번에 철회되는 것은 "플레이어 재사용" 전제뿐이다.
    모든 신규/수정 주석은 왜-주석 + 출처 `260806-wj3` 인용. 이모지 금지.

    (8) **무접촉**: `isFresh`/`URL_EXPIRY_MARGIN_MS`/`urlCache`/`cachedAnalysisId`/`prefetchCueAudio`/
    `reissue`/`SPEECH_LOAD_TIMEOUT_MS`/`MAX_SPEECH_RETRY`/`declareAudioMode`/`hydrate`/
    `setAudioCueEnabled`/`isCueSpeaking` — 한 줄도 바꾸지 않는다. 신규 npm 의존성 0.
  </action>
  <verify>
    <automated>npm --prefix app run typecheck</automated>
    <automated>test $(grep -vE '^\s*(//|\*)' /Users/kimtaesung/Dev/SunityMotion/app/src/lib/audioCue.ts | grep -c 'player?*\.replace(') -eq 0</automated>
    <automated>test $(grep -vE '^\s*(//|\*)' /Users/kimtaesung/Dev/SunityMotion/app/src/lib/audioCue.ts | grep -c 'createAudioPlayer(') -eq 1</automated>
    <automated>test $(grep -vE '^\s*(//|\*)' /Users/kimtaesung/Dev/SunityMotion/app/src/lib/audioCue.ts | grep -c 'player = null') -eq 1</automated>
    <automated>test $(grep -vE '^\s*(//|\*)' /Users/kimtaesung/Dev/SunityMotion/app/src/lib/audioCue.ts | grep -c '\.remove()') -eq 1</automated>
    <automated>node --test /Users/kimtaesung/Dev/SunityMotion/app/src/lib/__tests__/cueTrack.test.ts</automated>
  </verify>
  <done>
    typecheck GREEN. replace 경로 0건 / `createAudioPlayer(` 1건 / `player = null` 1건 /
    `remove()` 1건 — 즉 **재생 아이템의 출생지가 한 곳뿐**임이 게이트로 고정됨. cueTrack 7 pass.
    자동 테스트 없음(아래 사유를 SUMMARY 에 그대로 박제):
    `audioCue.ts` 는 expo-audio/AsyncStorage 를 직접 import 하는 어댑터라 `node --test` 로
    로드 불가이고, 이 수리의 본질은 **호출 순서·생명주기**여서 순수 함수로 떼어내면 실제 결함이
    아닌 껍데기를 검증하게 된다(수치 채우기 금지). 대신 위 4개 구조 게이트 + 코드 판독 논증
    ("play() 이전에 이 큐의 URL 로 만들어지지 않은 아이템이 플레이어에 존재할 수 있는 경로가
    없다")를 SUMMARY 에 적는다.
  </done>
</task>

<task type="auto">
  <name>Task 2: 저신뢰 doc 에서도 진행 바 감점 틱 표시 (belle ②)</name>
  <files>app/src/app/analysis/result.tsx</files>
  <action>
    **한 줄 수리 + 주석 정합.** 1378행
    `const overlayTimelineTicks = attributionUnreliable ? [] : timelineTicks;`
    → `const overlayTimelineTicks = timelineTicks;`

    바로 위(1337-1340)의 IN-01 주석에서 "범례/**틱도** 빈 배열로 두어 모순 방지" 부분을
    갱신하고, 틱 줄 위에 `260806-wj3` 출처의 왜-주석을 새로 붙인다. 주석에 담을 것:
    - belle 실기기 ②: 진행 바에 표시가 없으면 사용자는 "분석이 틀렸다"로 읽는다.
    - 틱 번호는 `markers.recordNumbers` 파생이고, 그 번호는 저신뢰 doc 에서도 점수 계산 내역
      행·시트에 렌더된다 → 틱은 고아가 아니다(탭 = seek + `openRecordByNumber` 로 그 항목 열기).
    - **영상 위** 마커/번호/그룹/전체화면 범례는 계속 억제한다 — 저신뢰에서 관절 단정을 영상
      위에 찍지 않는 것이 IN-01 승인 설계의 핵심이고, 진행 바 틱은 그 단정이 아니라 **시점**
      안내다. 비대칭은 의도된 것.

    **무접촉 (과잉 일반화 금지 — 승인 PASS 항목을 깨뜨리지 말 것)**:
    `overlayHighlightKeypoints` / `overlayAttentionKeypoints` / `overlayGroupMarkers` /
    `overlayMarkerNumbers` / `overlayForceHighlightWorstCount` / `overlayFullscreenLegend`
    6건은 `attributionUnreliable` 분기를 **그대로 둔다**. `focusKeypointsForRecordId`(1852),
    헤드라인/todayFix/요약(1700/2314/2325/2375) 등 다른 강등도 무접촉.
    `buildDeductionTicks` / `deductionLabels.ts` / `VideoCompare` 렌더부도 무접촉
    (프롭 이름 `overlayTimelineTicks` 를 유지하므로 2581행은 변경 0).
  </action>
  <verify>
    <automated>npm --prefix app run typecheck</automated>
    <automated>test $(grep -vE '^\s*(//|\*)' /Users/kimtaesung/Dev/SunityMotion/app/src/app/analysis/result.tsx | grep -cE 'overlay[A-Za-z]+ = attributionUnreliable') -eq 6</automated>
    <automated>test $(grep -vE '^\s*(//|\*)' /Users/kimtaesung/Dev/SunityMotion/app/src/app/analysis/result.tsx | grep -c 'overlayTimelineTicks = timelineTicks') -eq 1</automated>
    <automated>test $(grep -vE '^\s*(//|\*)' /Users/kimtaesung/Dev/SunityMotion/app/src/app/analysis/result.tsx | grep -c 'attributionUnreliable ? \[\] : fullscreenLegend') -eq 1</automated>
    <automated>test -z "$(git -C /Users/kimtaesung/Dev/SunityMotion diff HEAD --stat -- app/src/lib/deductionLabels.ts app/src/components/VideoCompare.tsx)"</automated>
    <human-check>
    **오케스트레이터 시뮬 확인** (실행자는 시뮬 도구 없음):
    저신뢰 doc(엘보)을 열어 (a) 진행 바에 감점 틱이 **보이는가** (라벨은 번호 병합 1개가 정상),
    (b) 틱 탭 → 그 시점으로 양쪽 seek + 해당 감점 시트가 열리는가,
    (c) **영상 위**에는 여전히 빨강 번호·그룹 배지·전체화면 범례가 없는가(IN-01 유지).
    ※ (a) 가 실패하면 원인이 둘로 갈린다 — 이 게이트(코드)가 아니라 **데이터**
    (`visionVeto.status !== 'applied'` 또는 `windowMedianAngleDeltas.sourceFrameIndices.user`
    가 빔)일 수 있다. 어느 쪽인지 확인해 SUMMARY 에 적을 것. 추측으로 코드를 더 고치지 말 것.
    </human-check>
  </verify>
  <done>
    typecheck GREEN. 잔존 강등 6건 정확히 유지(7→6, 이탈은 틱 1건뿐) · 범례 억제 유지 ·
    deductionLabels/VideoCompare diff 0. 오케스트레이터 시뮬 (a)(b)(c) 결과가 SUMMARY 에
    기록됨(미도달·미확인이면 그렇게 적는다).
  </done>
</task>

<task type="auto">
  <name>Task 3: 재개 마지막 재시도 직전 제자리 seek nudge (belle ④)</name>
  <files>app/src/components/VideoCompare.tsx</files>
  <action>
    **불변식은 그대로 두고 회복 확률만 올린다.** belle ④ 는 결함이 아니라 오늘 넣은
    converge-pause 가 설계대로 작동한 결과다(편측 대신 대칭 정지). 최종 폴백은 유지한다.

    (1) import 확장 — 32-35행 `import { decidePlaybackInvariant, RESUME_WATCH_TICKS }` 에
    `RESUME_PLAY_RETRIES` 를 추가한다.

    (2) 집행 블록(812-827) 안, **커맨드 적용 직전**에 nudge 를 넣는다:
    - 조건: `decision.action === 'retry-play' && resumeRetriesRef.current === RESUME_PLAY_RETRIES - 1`
      (= 마지막 재시도. 재시도 카운터는 관찰창 안에서 단조 증가하고 소진 시 창이 닫히므로
      **한 관찰창당 최대 1회**가 구조적으로 보장된다 — 별도 플래그 불필요).
    - 동작: 커맨드가 `'play'` 인 쪽에만 제자리 seek. `if (decision.left === 'play' && leftPlayer)
      leftPlayer.currentTime = leftPlayer.currentTime;` / right 도 동일 형태.
    - 그 다음 기존 `left/right` 커맨드 적용 → `consumeRetry` → `closeWatch` → `setPlaying(false)`
      → 조기 `return` 순서는 **그대로**.

    (3) 주석에 담을 것(왜-주석, 출처 `260806-wj3`, 이모지 금지):
    - 제자리 seek 는 iOS AVPlayer 버퍼 스톨 회복 관용구다. **이것이 belle ④ 의 원인이라는
      증명은 없다** — 기기 실패 기제는 여전히 미규명이고(F-6 원칙), 재시도가 실패해 어차피
      멈출 자리에서 한 번 더 두드려 보는 것뿐이다.
    - 제자리(= 같은 시각) 인 이유: 정렬을 건드리지 않기 위해서다. 오프셋/워프 정합은
      converge-pause 가 지키려는 바로 그 자산이므로 nudge 가 시각을 옮기면 안 된다.
    - 마지막 재시도에만 거는 이유: 앞선 재시도는 그냥 성공할 수 있고, seek 는 그 자체가
      파이프라인을 다시 흔들어 정상 회복을 방해할 수 있다. 마지막은 어차피 잃을 것이 없다.
    - 실효 없으면 다음 지렛대는 epsilon seek(수십 ms 전진) — **지금은 넣지 않는다**(정렬 위험).

    (4) **무접촉**: `app/src/lib/playbackInvariant.ts` 는 **한 글자도 바꾸지 않는다**
    (판정 계약·12축 테스트 동반 갱신 회피). 관찰창 진행/`startHoldActive`/제스처 4곳 창 닫기/
    F-1·F-2 조기 return/follow·drift 블록도 무접촉. 신규 타이머 0.
  </action>
  <verify>
    <automated>npm --prefix app run typecheck</automated>
    <automated>test -z "$(git -C /Users/kimtaesung/Dev/SunityMotion diff HEAD --stat -- app/src/lib/playbackInvariant.ts)"</automated>
    <automated>node --test /Users/kimtaesung/Dev/SunityMotion/app/src/lib/__tests__/playbackInvariant.test.ts /Users/kimtaesung/Dev/SunityMotion/app/src/lib/__tests__/manualOffset.test.ts /Users/kimtaesung/Dev/SunityMotion/app/src/lib/__tests__/cueTrack.test.ts</automated>
    <automated>test $(grep -vE '^\s*(//|\*)' /Users/kimtaesung/Dev/SunityMotion/app/src/components/VideoCompare.tsx | grep -c 'RESUME_PLAY_RETRIES - 1') -eq 1</automated>
    <automated>test $(grep -vE '^\s*(//|\*)' /Users/kimtaesung/Dev/SunityMotion/app/src/components/VideoCompare.tsx | grep -c 'setInterval(') -eq 1</automated>
    <human-check>
    **오케스트레이터 시뮬 회귀 확인**: 큐 4사이클을 태워 (a) 큐 시점 양쪽 동시 정지,
    (b) 음성 종료 후 양쪽 동시 재개, (c) 최종까지 동기 주행이 260806-usc 실측 궤적
    (4.1/4.2 → 6.7/6.7 → 13.3/13.5)과 같은 모양인지 확인. 시뮬은 원 스톨을 재현하지 못하므로
    여기서 증명되는 것은 **회귀 없음**뿐이다(nudge 자체가 정상 재생을 끊지 않는지).
    </human-check>
  </verify>
  <done>
    typecheck GREEN. `playbackInvariant.ts` diff 0(계약 무접촉 증명) · 25 pass 기준선 유지 ·
    nudge 조건 1곳 · 타이머 1개 불변. 오케스트레이터 시뮬 (a)(b)(c) 회귀 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 앱 → S3 presigned mp3 | 기존 경계(무변경). 이번 수리는 URL 발급/캐시를 건드리지 않고 **플레이어 생명주기만** 바꾼다. |
| 사용자 화면 ← 분석 doc | 저신뢰 doc 의 표시 범위를 1건 넓힌다(틱). 새 데이터 소스·네트워크 경계 0. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-wj3-01 | Spoofing | audioCue 재생 아이템 | mitigate | 재생 아이템 출생지를 `activatePlayer` 안 `createAudioPlayer(url)` 하나로 고정 + replace 경로 0건 게이트 → **다른 큐의 음성이 이 큐의 자막으로 위장되는 경로 소멸**(belle ① 본체). |
| T-wj3-02 | Denial of Service | 큐마다 플레이어 재생성 | accept | 큐는 분석당 수 개이고 mp3 는 수 초 분량. 생성 비용보다 아이템 정합이 우선(재사용 전제 철회는 의도된 트레이드오프). 누수는 `releasePlayer`→`remove()` 단일 소유자가 담당. |
| T-wj3-03 | Information Disclosure | 저신뢰 doc 틱 복원 | accept | 틱은 **시점**만 노출하고 관절 단정은 노출하지 않는다. 영상 위 마커/번호/범례 억제는 유지(잔존 6건 카운트 게이트로 고정). |
| T-wj3-04 | Denial of Service | nudge × 재생 파이프라인 | mitigate | 관찰창당 최대 1회(재시도 카운터 단조 증가 + 소진 시 창 닫힘) + 제자리 seek(시각 이동 0) + converge-pause 최종 폴백 유지 → seek 폭주·정렬 붕괴 구조적 차단. |
| T-wj3-SC | Tampering | npm/pip/cargo installs | mitigate | **신규 의존성 0** (설치 태스크 없음 — 기존 expo-audio/expo-video API 만 사용). |
</threat_model>

<verification>
1. `npm --prefix app run typecheck` — GREEN(무출력, 기준선 동일)
2. `node --test` 3파일 — 25 pass / 0 fail (12축 + 6 + 7 기준선 유지)
3. 구조 게이트: audioCue replace 0 / createAudioPlayer 1 / player=null 1 / remove 1
4. 구조 게이트: result.tsx 잔존 강등 6 / 틱 복원 1 / 범례 억제 1
5. 구조 게이트: playbackInvariant.ts diff 0 / nudge 조건 1 / setInterval 1
6. 오케스트레이터 시뮬 — Task 2 (a)(b)(c), Task 3 (a)(b)(c)
7. 커밋 3개(task 단위 atomic), EAS OTA 는 범위 밖
</verification>

<success_criteria>
- 자막이 큐 N 을 가리키는데 큐 M(M≠N)의 음성이 나는 경로가 코드에 **존재하지 않는다** — 구조 게이트로 증명.
- 오디오 실패의 결과는 항상 무음(자막만)이다 — fail-closed.
- 저신뢰 doc 진행 바에 감점 틱이 렌더되고, 나머지 IN-01 강등 6건은 그대로다.
- 재개 실패 시 마지막 재시도 직전 제자리 seek 1회 → 그래도 안 되면 종전대로 대칭 정지.
- 판정 순수 함수·채점·계약·테마·카피·의존성 변경 0. 신규 타이머 0.
</success_criteria>

<honesty_gate>
SUMMARY 에 **반드시** 다음을 그대로 박제한다 (문장을 부드럽게 바꾸지 말 것):

- **① 실기기 실효 = UNVERIFIED, 시뮬로 재현 불가.** 이 결함의 전제가 **기기 네트워크에서의
  로드 실패**라 시뮬(정상 네트워크)에서는 애초에 발생하지 않는다. 시뮬로 증명되는 것은
  회귀 없음뿐이다. "①을 고쳤다"가 아니라 "① 이 발생할 수 있는 **경로를 없앴다**(구조 게이트로
  증명) / 실기기 실효 미확인"이 정확한 표현이다.
- **① 의 기제는 코드 판독 논증이지 계측이 아니다.** replace 실패 시 이전 아이템이 남는다는
  것을 실기기에서 계측한 것이 아니다 — expo-audio API 형태와 belle 관측("큐1이 처음부터 다시")의
  정합으로 세운 가설이다. 확정된 사실처럼 쓰지 말 것.
- **② 가 belle 화면에서 안 보이면 원인이 둘이다.** 이 게이트(코드) 아니면 데이터
  (`visionVeto.status !== 'applied'` / `sourceFrameIndices.user` 빔). 시뮬에서 어느 쪽인지
  확인한 결과를 적고, 미확인이면 "미확인"이라고 적을 것.
- **② 는 비대칭을 남긴다.** 진행 바에는 번호 틱이 뜨는데 영상 위에는 번호가 없다 — IN-01
  승인 설계를 지키기 위한 **의도된** 비대칭임을 적는다. belle 이 "영상 위에도 번호를"이라고
  하면 그건 IN-01 재논의 건이지 이번 수리의 결함이 아니다.
- **④ nudge 는 효과 미검증 지렛대다.** 제자리 seek 가 실제로 스톨을 푸는지 확인한 바 없다.
  belle 이 여전히 "음성 끝나고 둘 다 멈춘다"고 하면 다음 후보는 (a) epsilon seek,
  (b) `RESUME_PLAY_RETRIES`/`RESUME_WATCH_TICKS` 상향임을 적어 둔다.
- **③(큐1 후 정상 재개)은 무접촉**이며 오늘 불변식이 작동한 증거라는 것을 적는다.
- belle 실기기 확인 항목: ① 큐2 에서 자막과 음성이 같은 부위를 말하는가(아니면 **무음**인가 —
  무음은 이번 설계상 정상 실패), ② 진행 바에 감점 표시가 보이는가, ④ 음성 종료 후 재개가
  되는가(안 되면 여전히 **양쪽이 같이** 멈춰 있는가).
</honesty_gate>

<output>
Create `.planning/quick/260806-wj3-nudge-belle/260806-wj3-SUMMARY.md` when done.
커밋 3개(task 단위). EAS OTA 발행은 이 계획 범위 밖 — 오케스트레이터가 시뮬 확인 후 수행한다.
</output>
