---
phase: quick-260807-k70
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/VideoCompare.tsx
  - app/src/app/analysis/result.tsx
  - app/src/lib/replaySettle.ts
  - app/src/lib/__tests__/replaySettle.test.ts
autonomous: true
requirements: [BELLE-0807-8, BELLE-0807-9, BELLE-0807-10]
user_setup: []

must_haves:
  truths:
    - "가로 전체보기(전체화면 Modal)에서도 자막 배너(activeCueText)·'음성 중 — 잠시 멈춤' pill·'일시정지됨 — 탭하여 계속' 배지가 세로와 같은 조건·문구·상태 소스로 렌더된다 — 기존 #8 해소 (BELLE-0807-8)"
    - "세로(인라인) 큐 오버레이의 렌더 출력은 불변 — 조건·문구·상태는 단일 소스(공유 헬퍼, renderControls 선례)이고 분기 사본 0 (BELLE-0807-8)"
    - "재생 세션(재생 중 + 음성 정지 + 정지 중 발화) 동안 학생 패널은 활성 음성 큐 record 의 투영 관절만 빨강(+기존 emphasis·dim)으로 표시하고, 큐가 없으면 점 0. 기준(우) 패널 스켈레톤도 세션 중 숨김. 세로·가로 동일 정책 (BELLE-0807-9)"
    - "재생 세션 밖(사용자 정지/탐색)의 번호 마커·그룹 경계·주황 참고 점·스켈레톤 토글 렌더는 현행 값 그대로 — 각 prop 삼항의 세션-밖 가지가 기존 표현식과 동치. emphasis 강도(PLAYBACK_EMPHASIS_SCALE=1.3)는 belle '진하기 적당' 로 무변경 — KeypointOverlay.tsx diff 0 (BELLE-0807-9)"
    - "종료 후 재재생 시 stale 종료 위치로 인한 즉시 재-pause 가 settle 가드로 차단되고, 양쪽 seek 직후 드리프트 보정이 기존 0.8s 히스테리시스 간격으로 유예되며, 발화 이력(chainSpoken)이 새 런 시작에서 명시 리셋된다 (BELLE-0807-10)"
    - "근거 없는 수리 0 — 플레이어 재생성·재생 관찰창 replay 무장 등 판독 근거 없는/리스크 변경 미도입. 미해결 잔여는 SUMMARY 에 belle 관측 요청 항목과 함께 박제 (BELLE-0807-10)"
    - "채점·doc·계약 무접촉: backend/ 와 app/src/types/ diff 0. playbackInvariant.ts·cueTrack.ts·voiceSnap.ts·driftHysteresis.ts diff 0 (HEAD 기준)"
  artifacts:
    - path: "app/src/lib/replaySettle.ts"
      provides: "decideReplaySettle 순수 판정 + REPLAY_SETTLE_MAX_TICKS 상수 — 재재생 직후 stale 종료판정 유예"
    - path: "app/src/lib/__tests__/replaySettle.test.ts"
      provides: "settled/hold/expired 분기 + duration 미산정 + NaN 방어 + 상수 박제 테스트"
    - path: "app/src/components/VideoCompare.tsx"
      provides: "공유 큐 오버레이 헬퍼(세로+전체화면 양쪽 렌더) + 우측 슬롯 세션 신호 passthrough + 재재생 settle 가드/드리프트 유예/발화 이력 리셋"
    - path: "app/src/app/analysis/result.tsx"
      provides: "재생 세션 판정(inPlaybackSession) 기반 '말하는 지점만' 오버레이 prop 파생 (좌/우 패널)"
  key_links:
    - from: "app/src/components/VideoCompare.tsx"
      to: "app/src/components/VideoCompare.tsx"
      via: "renderCueOverlays 헬퍼를 세로 row 와 전체화면 fsLandscape/fsRotated 컨테이너가 공유 호출"
      pattern: "renderCueOverlays"
    - from: "app/src/app/analysis/result.tsx"
      to: "app/src/components/VideoCompare.tsx"
      via: "leftOverlay/rightOverlay 가 opts.isPlaying·voiceCueRecordId·activeCueRecordId 로 세션 판정 (신규 opts 필드 0)"
      pattern: "inPlaybackSession"
    - from: "app/src/components/VideoCompare.tsx"
      to: "app/src/lib/replaySettle.ts"
      via: "tick 종료판정 직전 decideReplaySettle 호출 + togglePlay(isAtEnd)/restart 에서 가드 무장"
      pattern: "decideReplaySettle"
---

<objective>
belle 08-07 저녁 실기기 피드백 3건 (iwp OTA 확인 후, 가로 전체보기 스크린샷 2장 기반) — 전부 앱 전용, 채점·doc·백엔드 무접촉.

1. (BELLE-0807-8) 가로 전체보기 자막·정지 배지 — belle "가로 크게 보기는 자막이 안 나오기도 하고". 기존 알려진 #8: 자막 오버레이('음성 중 — 잠시 멈춤' pill + 검은 자막 배너)가 세로 row 절대 자식으로만 있고 전체화면 Modal 내부엔 렌더 자리가 없다. 같은 상태를 전체화면 레이아웃 안에도 렌더.
2. (BELLE-0807-9) 재생 세션 중 "말하는 지점만" 점 표기 — belle "가로에선 되려 동그라미가 너무 크니까 보기가 너무 힘들어. 지점을 말해줄 때만 표기하는 건 어떨까?" 정책 확정으로 접수. 재생 세션 동안 기본 관절 점·스켈레톤을 숨기고 활성 음성 큐 record 부위만. fpw 의 "기본 흰 점" 접근을 이 정책으로 대체(belle 재정의 우선). 단 "크게 보니까 진하기는 적당" — emphasis 강도(1.3배)는 유지.
3. (BELLE-0807-10) n번째 재생 안정성 (보수적) — belle "두번째/n번째 재생이 매번 깔끔하다고 보기 힘듦. 어떨 땐 잘 나오고 어떨 땐 끊기고". 원인 확정 전 추측 수리 금지 — 코드 판독으로 근거가 선 3가지만: (a) 재재생 stale 종료판정 유예(settle 가드), (b) 양쪽 seek 직후 드리프트 보정 유예(기존 히스테리시스 재사용), (c) 발화 이력 명시 리셋. 근거가 안 서는 나머지는 SUMMARY 에 미해결 박제 + belle 관측 요청.

Purpose: 가로 전체보기가 세로와 같은 정보(자막·정지 상태)를 주고, 재생 화면이 "말하는 순간에만 지점이 보이는" 깨끗한 표면이 되며, 재재생이 코드로 증명 가능한 결함 경로만큼 안정된다.
Output: 위 3건 반영 앱 코드 + 신규 순수 모듈 1개(replaySettle) + node --test 신규 케이스 + 전 스위트(기준선 208+신규)/typecheck GREEN. 시뮬·OTA 는 오케스트레이터가 사이클 후 수행 (계획 범위 밖).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@app/src/components/VideoCompare.tsx
@app/src/components/KeypointOverlay.tsx
@app/src/app/analysis/result.tsx
@app/src/lib/playbackInvariant.ts
@app/src/lib/driftHysteresis.ts
@app/src/lib/__tests__/driftHysteresis.test.ts
</context>

<planner_findings>
플래너가 실측 판독·전수 grep 으로 확정한 사실 (실행자는 재조사 불요, 인용해 사용. 행 번호는 HEAD 30c1bcb4 기준):

1. **#8 원인 구조 확정**: 세로 큐 오버레이는 `styles.row` 절대 자식 — 자막 블록 VideoCompare:1913~1930(`cueSubtitleWrap` 안에 `voiceCueRecordId != null && !playing` 조건 pill + `activeCueText` 배너), 재개 배지 1937~1952(`resumeNotice && !playing`). 전체화면 Modal(2114~2220)의 `fsLandscape`/`fsRotated` 컨테이너(2138~2151) 안에는 `fsVideoRow`(2156)+`fsTopBar`(2178)+`fsControlsWrap`(2216)만 있고 큐 상태 렌더가 없다. 상태(activeCueText 601, voiceCueRecordId 618, playing, resumeNotice 687, handleResumeNoticePress 1315)는 전부 같은 컴포넌트 스코프라 Modal 안에서 그대로 읽힌다 — **추가 배선 0, 렌더 위치만 부재**.
2. **공유 헬퍼 선례 = `renderControls(dark)`(1609~1746)**: 같은 핸들러·같은 JSX 를 세로/전체화면이 공유(로직 중복 0). 큐 오버레이도 같은 방식이 "분기 사본 금지" 요건의 정본 실현이다. 전체화면 텍스트 배율 선례 = `FULLSCREEN_TEXT_SCALE`(387)을 fontSize 에 곱하는 `fsLegendText`(2709~2713) 패턴. `fsControlsWrap` 은 absolute bottom 0(2742~2749) — 자막 zone 은 그 위. 세로 배지 paddingBottom 산식 주석 관례 = 2361~2364.
3. **재생 세션 판정은 기존 opts 4필드로 충분 (신규 opts 필드 0)**: `inPlaybackSession = opts?.isPlaying === true || opts?.voiceCueRecordId != null`. 음성 정지 중 `playing` 은 false(873/953)지만 `voiceCueRecordId` 는 발화 시작~종료 동안 non-null(869/926 설정, 945 해제). "정지 상태 중 발화"(멈춤 없는 발화, 969~976 해제 경로)도 이 식으로 세션에 편입된다 — 표시-발화 일치 원칙상 의도된 포함으로 문서화한다.
4. **세션 중 빨강 대상 record 는 voiceCueRecordId 우선**: 체인 발화 중 활성 윈도우(activeCueWindowRecordId)는 멈춘 cL 기준이라 이전 큐를 가리킬 수 있다(846~847 주석이 실증한 함정). 재생 중(발화 없음)엔 activeCueRecordId. 즉 `sessionCueRecordId = opts?.voiceCueRecordId ?? (opts?.isPlaying === true ? (opts?.activeCueRecordId ?? null) : null)`.
5. **"큐 없으면 점 0" 메커니즘은 이미 실증**: KeypointOverlay 762 게이트(`!skeletonVisible` 이면 isHi/isAttn 만 렌더) + highlightedJoints 파생(434~440: 빈 highlightKeypoints + jointAngles undefined + forceHighlightWorstCount 0 → 빈 set). fpw 가 배포한 조합과 동일 — KeypointOverlay **코드 변경 불요**, prop 값 파생(result.tsx)만 바꾸면 된다.
6. **세션-밖 byte 동치 확인**: markersVisible 현행식(result.tsx 2578~2583 `overlayVisible || voiceCue != null || (isPlaying && activeCue != null)`)은 세션 밖(isPlaying false·voiceCue null)에서 `overlayVisible` 로 축약된다 — `inPlaybackSession ? (sessionCueRecordId != null) : overlayVisible` 재작성은 세션 밖 값 동일. 다른 prop 삼항(2589~2640)도 else 가지를 현행 표현식 그대로 두면 세션 밖 렌더 불변.
7. **우측 패널 세션 신호는 전달만 추가**: VideoSlot(131~140)과 renderFullscreenSlot(1764~1774)은 이미 `cueRecordId`/`isPlaying` 파라미터를 갖고 있고, 우측 호출부(세로 1874~1880, 전체화면 2170~2176)가 안 넘길 뿐이다. rightOverlay(result.tsx 2662~2673)는 `visible={overlayVisible}` 단독 — 세션 중 숨김은 이 값 분기 하나. mode3 는 rightOverlay null 이라 자동 무접촉.
8. **IN-01 저신뢰 doc 상속**: `focusKeypointsForRecordId`(result.tsx 1884~1888)는 fpw 가 IN-01 조기 반환을 제거(1878~1883 주석)해 음성/큐 표면의 부위 표시를 허용한다 — 세션 정책이 같은 함수를 타므로 "음성 중 그 부위만" fpw 정책이 자동 계승된다. 정지 표면 강등(overlayHighlightKeypoints 등 별도 파생)은 세션-밖 가지라 무접촉.
9. **재재생 stale 종료판정 레이스 (BELLE-0807-10 의 코드 증명 경로)**: togglePlay isAtEnd 분기(1266~1284)는 양쪽 seek(0) 후 `REPLAY_SEEK_DELAY_MS=200ms` 뒤 play() 한다. 200ms 는 경험 상수이고 상향 이력 자체(1279 주석 "60→200ms — 정은지 S3 buffer reset")가 seek 적용 지연이 실재함의 선행 증거다. 적용이 200ms 를 넘기면 tick 의 종료판정(1161~1186)이 stale cR(=end)로 `eitherReachedOwnEnd`(followTick 활성 시 한쪽 stale 만으로 참) → `shouldPauseAtEnd` → 방금 재개한 양쪽을 즉시 재-pause. 사용자가 다시 탭하면 그땐 seek 이 적용돼 있어 성공 — belle "어떨 땐 잘 나오고 어떨 땐 끊기고" 와 부합하는 결정론적 경로다.
10. **관찰창(백오프) replay 무장은 이번 사이클 금지 — 판독 근거**: `RESUME_WATCH_TICKS = 75`(7.5s, playbackInvariant.ts 87~89). 재재생에 관찰창을 무장하면 7.5초보다 짧은 영상의 자연 종료 pause 가 열린 창 안에 들어와 R7'(157~166)가 끝에서 retry-play 로 재생을 되살리고, 소진 후 converge-pause 배지("일시정지됨")가 자연 종료에 오표시된다. playbackInvariant 에 종료-상태 규칙 신설이 선행돼야 하는 변경 — 이번엔 미적용하고 SUMMARY 에 다음 지렛대로 박제한다. (음성 재개 경로의 기존 무장은 무접촉.)
11. **chainSpokenRef 리셋의 유일 경로 = tick 837~839 (activeCue null 관측)**: 종료 프레임이 마지막 큐 윈도우 안이고(윈도우 endSec 이 영상 끝까지) 첫 큐 윈도우가 0 부근에서 시작하면, 재재생 사이에 null 관측 tick 이 없어 이전 런의 발화 이력이 새 런의 speakCue 를 차단(865 가드)하는 경로가 실재한다 — 새 런 시작(명시 되감기)에서의 명시 clear 는 "되감아 재진입하면 다시 발화"(694 주석) 의도의 복원이다.
12. **드리프트 보정 유예는 기존 메커니즘 재사용**: `lastDriftSeekAtRef` 스탬프(1113~1116/1136~1144 소비)를 양쪽 seek 직후에 `Date.now()` 로 찍으면 driftHysteresis 의 `DRIFT_SEEK_MIN_INTERVAL_MS(0.8s)` 간격이 그대로 보정 유예로 작동한다 — 신규 상수 0, driftHysteresis.ts 무접촉, 간격 경과 후 잔존 drift 는 반드시 보정(수렴 보장 불변). settle 가드 hold 중 우측이 end 에 붙어 있으면 0.8s 뒤 follow 보정이 목표시각(≈0) 재-seek 을 쏘는 것이 회복 지렛대로 작동한다(버퍼 안정 후 재-seek — wj3 nudge 계열).
13. **재생 세션 밖 유일 정지-중-스냅 경로인 voiceSnap 복원(unsnapRight 644~649)은 togglePlay isAtEnd 에서 항상 no-op**: 스냅은 음성 정지 중에만 걸리고 그 상태로 영상이 끝에 도달할 수 없으며 사용자 정지 시 이미 복원된다 — isAtEnd 분기의 이중 seek 우려 없음, 무접촉.
14. **테스트 러너**: Node type stripping, `node --test <파일>` 파일 단위 루프(디렉터리 일괄 실행 깨짐), node:test/node:assert 만, `.ts` 확장자 import. 기준선 = 14파일 + pickerFailure.test.ts, 208 tests (iwp 종료 시점). 전체 루프에 `app/src/lib/__tests__/*.test.mjs` 와 `app/src/lib/pickerFailure.test.ts` 포함.
</planner_findings>

<tasks>

<task type="auto">
  <name>Task 1: 가로 전체보기에 자막·음성중 pill·재개 배지 렌더 — #8 해소 (BELLE-0807-8)</name>
  <files>app/src/components/VideoCompare.tsx</files>
  <action>
    VideoCompare.tsx — 공유 헬퍼 `renderCueOverlays(fs: boolean)` 신설 (renderControls(dark) 선례, planner_findings 2). 반환 fragment 에 두 블록:

    (a) 자막 블록 — 현행 세로 JSX(1913~1930)를 헬퍼로 이동: 조건 `activeCueText` 존재, 내부에 `voiceCueRecordId != null && !playing` 이면 '음성 중 — 잠시 멈춤' pill(pause 아이콘 + voicePauseText), 이어 `activeCueText` 자막(numberOfLines 3, accessibilityRole text). wrap 스타일 = fs ? 신규 `fsCueSubtitleWrap` : 기존 `cueSubtitleWrap`, 텍스트/pill 은 기존 스타일에 fs 배율 스타일을 배열 합성(예: `[styles.cueSubtitle, fs && styles.fsCueText]` 계열).

    (b) 재개 배지 블록 — 현행 세로 JSX(1937~1952) 이동: 조건 `resumeNotice && !playing`, Pressable onPress=`handleResumeNoticePress`(동일 핸들러 — 분기 사본 0). wrap = fs ? 신규 `fsResumeNoticeWrap` : 기존 `resumeNoticeWrap`. 재개 배지를 전체화면에도 두는 근거: 같은 상태의 렌더 위치 추가일 뿐이며, Task 3 의 잔여 진단(belle 이 가로에서 스톨을 보면 배지 유무가 관측 신호)에 직접 기여한다 — 주석에 명기.

    호출 2곳: 세로 row 안 기존 두 블록 자리(1913~1952)를 `{renderCueOverlays(false)}` 로 치환(렌더 출력 동일 — 조건·문구·순서·스타일 불변). 전체화면은 fsLandscape/fsRotated 컨테이너 안 `fsVideoRow` 닫힘(2177) 직후에 `{renderCueOverlays(true)}` 삽입 — fsTopBar/fsControlsWrap 이 뒤에 렌더되므로 겹침 시 컨트롤이 위에 그려진다. 90° 회전 폴백(fsRotated)도 컨테이너 내부라 자동 회전.

    신규 스타일 (토큰만, 하드코딩 색 0 — videoBg/textWhite 기존 사용 계승):
    - `fsCueSubtitleWrap`: absolute left/right `spacing.cardPadding`, top 0 bottom 0, alignItems center, justifyContent flex-end, paddingBottom ≈ 70 (fsControlsWrap 하단 zone: paddingBottom 10 + 컨트롤 행 높이 ≈ 60 위에 앉힘 — 산식을 주석으로 박제, 2361~2364 관례).
    - `fsCueText`/`fsVoicePauseText`(필요 시)/`fsResumeNoticeText`: fontSize(및 lineHeight)에 `FULLSCREEN_TEXT_SCALE` 곱 (fsLegendText 선례 — planner_findings 2). 신규 절대 px 최소화, 기존 스타일 상속 + 배율만.
    - `fsResumeNoticeWrap`: fsCueSubtitleWrap 위치 계열, paddingBottom 은 fs 자막 zone(3줄 × 배율 lineHeight + 패딩) 위 — 세로 resumeNoticeWrap 96 산식 관례로 주석에 계산 명기.

    헬퍼 정의 위치는 renderControls 인접. 상태·핸들러 참조만 하므로 추가 배선 0 (planner_findings 1). 주석 한국어 + 출처(belle 08-07 저녁 "가로 크게 보기는 자막이 안 나오기도 하고", quick-260807-k70, 기존 #8). ⚠ 주석에 `renderCueOverlays(` 호출형 문자열을 쓰지 말 것(아래 grep 카운트 게이트 자기무효화 방지).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && (cd app && npm run typecheck) && [ "$(grep -c 'renderCueOverlays(' app/src/components/VideoCompare.tsx)" = "3" ] && [ "$(grep -c '>음성 중 — 잠시 멈춤<' app/src/components/VideoCompare.tsx)" = "1" ] && [ "$(grep -c 'onPress={handleResumeNoticePress}' app/src/components/VideoCompare.tsx)" = "1" ] && [ "$(git diff HEAD -- app/src | grep -cE '^\+.*#[0-9a-fA-F]{6}')" = "0" ]</automated>
  </verify>
  <done>전체화면(진짜 가로 + 90° 폴백 모두)에서 자막·'음성 중 — 잠시 멈춤' pill·재개 배지가 세로와 같은 조건·같은 문구·같은 상태로 렌더되고, 텍스트는 FULLSCREEN_TEXT_SCALE 배율로 읽힌다. 세로 렌더 출력 불변(공유 헬퍼 — 문구 리터럴 각 1곳 유지가 단일 소스 증명). typecheck GREEN + 신규 hex 리터럴 0.</done>
</task>

<task type="auto">
  <name>Task 2: 재생 세션 "말하는 지점만" 점 표기 — belle 정책 확정 (BELLE-0807-9)</name>
  <files>app/src/app/analysis/result.tsx, app/src/components/VideoCompare.tsx</files>
  <action>
    (a) result.tsx leftOverlay(2550~2661) — 세션 판정 파생 신설 (신규 opts 필드 0, planner_findings 3·4):
    - `inPlaybackSession` = opts?.isPlaying === true || opts?.voiceCueRecordId != null
    - `sessionCueRecordId` = opts?.voiceCueRecordId ?? (opts?.isPlaying === true ? (opts?.activeCueRecordId ?? null) : null)
    - `sessionCueKeypoints` = inPlaybackSession && sessionCueRecordId ? focusKeypointsForRecordId(sessionCueRecordId) : null
    - 기존 `playingInversion`(isPlaying===true)은 playbackEmphasis 전달용으로 유지 — emphasis 배율은 실재생 중만(음성 정지 중 승인 dim·펄스 렌더에 배율 미적용, belle "진하기 적당" = 강도 무변경).

    prop 치환 — **각 삼항의 세션-밖 가지는 현행 표현식 그대로**(byte 동치, planner_findings 6):
    - skeletonVisible: inPlaybackSession ? false : overlayVisible — 세션 중 축 폴리라인·본·흰 점·회색 점 전부 숨김(토글 ON 이어도. 토글은 정지 상태 표시 담당으로 강등 — 주석 명기).
    - markersVisible: inPlaybackSession ? sessionCueRecordId != null : overlayVisible
    - jointAngles: inPlaybackSession ? undefined : (hasBreakdownRecords ? undefined : userJointAngles)
    - highlightKeypoints: inPlaybackSession ? (sessionCueKeypoints ?? []) : overlayHighlightKeypoints
    - attentionKeypoints: inPlaybackSession ? [] : overlayAttentionKeypoints
    - groupMarkers: inPlaybackSession ? [] : overlayGroupMarkers
    - markerNumbers: inPlaybackSession ? {} : overlayMarkerNumbers
    - forceHighlightWorstCount: inPlaybackSession ? 0 : overlayForceHighlightWorstCount
    - focusKeypoints(2654~2658)·playbackEmphasis(2648)·onMarkerPress·sizeScale 무변경.
    효과: 재생 중 큐 없음 = 점 0(planner_findings 5), 큐 있음 = 그 record 투영 관절만 빨강(+PE 배율), 음성 정지 중 = dim·펄스·그 부위 빨강만(번호·주황·그룹·스켈레톤 숨김). "정지 상태 중 발화" 편입은 표시-발화 일치로 문서화(planner_findings 3). IN-01 저신뢰 doc 은 focusKeypointsForRecordId 경유로 fpw 정책 자동 계승(planner_findings 8).

    (b) result.tsx rightOverlay(2662~2673) — 같은 세션 판정식으로 `visible={inSession ? false : overlayVisible}` (mode1 만 — mode3 는 null 그대로). 근거: belle "동그라미가 너무 크니까 보기 힘들다"의 표면은 양 패널이고, 정책 문언 "기본 관절 점·스켈레톤 표시를 숨기고"는 무한정 — 기준 패널은 세션 중 점 0 (큐 record 투영은 학생 결함이라 좌측만 빨강, fpw 관례 유지). 판정식은 leftOverlay 와 동일 식 사용(규칙 두 벌 금지 — 인라인 재계산 시 같은 식임을 주석으로 못박기).

    (c) VideoCompare.tsx — 우측 슬롯에 세션 신호 passthrough 만 추가(planner_findings 7): 세로 우측 VideoSlot(1874~1880)에 `cueRecordId={voiceCueRecordId}` + `isPlaying={playing}`, 전체화면 우측 renderFullscreenSlot 호출(2170~2176)에 5번째(voiceCueRecordId)·6번째(playing) 인자 전달. 좌측 배선·opts 타입·VideoSlot 시그니처 무변경. 1867~1872 주석의 "학생 측만" 서술을 갱신(우측은 세션 판정 신호용 — 빨강 강조는 여전히 좌측만).

    주석 한국어 + 출처(belle 08-07 저녁 "지점을 말해줄 때만 표기", quick-260807-k70, fpw #4 대체 명기). KeypointOverlay.tsx 는 무접촉(diff 0 이 게이트).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && (cd app && npm run typecheck) && [ "$(grep -c 'inPlaybackSession' app/src/app/analysis/result.tsx)" -ge "3" ] && [ "$(grep -c 'sessionCueRecordId' app/src/app/analysis/result.tsx)" -ge "3" ] && git diff HEAD --exit-code -- app/src/components/KeypointOverlay.tsx</automated>
  </verify>
  <done>재생 세션(재생·음성 정지·정지 중 발화) 동안: 좌 패널 = 활성 큐 record 투영 관절만 빨강(큐 없으면 점 0, 스켈레톤·번호·주황·그룹 숨김), 우 패널 = 오버레이 숨김. 세션 밖 = 현행 번호 마커·토글 렌더 그대로(각 삼항 else 가지 동치). 세로·가로 동일(같은 render prop). emphasis 강도 무변경 — KeypointOverlay.tsx diff 0. typecheck GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: n번째 재생 안정성 — settle 가드 + 드리프트 유예 + 발화 이력 리셋 (보수적, BELLE-0807-10)</name>
  <files>app/src/lib/replaySettle.ts, app/src/lib/__tests__/replaySettle.test.ts, app/src/components/VideoCompare.tsx</files>
  <behavior>
    replaySettle.test.ts (node:test, driftHysteresis.test.ts 관례):
    - 양쪽 현재시각이 자기 duration 끝 임계 미만 → 'settled' (seek 적용 관측 = 즉시 정상 종료판정 복원)
    - 우측만 stale end(cR ≥ dR−0.05) → 'hold'
    - 좌측만 stale end → 'hold'
    - ticksElapsed ≥ REPLAY_SETTLE_MAX_TICKS → 'expired' (stale 지속에도 종료판정 복원 — 가드 영구화 금지)
    - duration 미산정(dR ≤ 0) 쪽은 settled 취급 (그쪽 종료판정 자체가 안 돎)
    - hasRight=false → 좌측만으로 판정 (단일 패널)
    - NaN/비유한 current → 그쪽 not settled ('hold', expiry 가 안전망)
    - 상수 박제: REPLAY_SETTLE_MAX_TICKS === 20
  </behavior>
  <action>
    원칙(task_spec): 원인 확정 전 추측 수리 금지 — 아래 3건은 각각 planner_findings 9·12·11 의 코드 판독 근거가 있는 것만이다. 플레이어 재생성·관찰창 replay 무장은 금지(무장 금지의 판독 근거 = planner_findings 10 — 7.5s 미만 영상 자연 종료 오개입·배지 오표시).

    (a) `app/src/lib/replaySettle.ts` 신규 — 순수 함수만 (driftHysteresis.ts 관례: 상수를 순수 모듈이 소유). export: `REPLAY_SETTLE_MAX_TICKS = 20`(tick 100ms × 20 = 2.0s — REPLAY_SEEK_DELAY_MS 200ms 의 10배 상한, seek 적용이 그때까지 관측 안 되면 정상 종료판정 복원), `decideReplaySettle(input: { ticksElapsed; hasLeft; hasRight; cL; cR; dL; dR }): 'hold' | 'settled' | 'expired'`. 판정: 존재하는 각 패널이 settled(= duration ≤ 0 이거나 유한 current < duration − 0.05 — tick 종료판정과 같은 끝 임계, 값 사본임을 주석 명기)면 'settled'; ticksElapsed ≥ MAX 면 'expired'; 아니면 'hold'. 헤더에 출처(belle 08-07 저녁 "n번째 재생이 매번 깔끔하지 않다", quick-260807-k70)와 근거 서술: 재재생은 양쪽 seek(0) 후 고정 200ms 뒤 play 인데(경험 상수 — 60→200 상향 이력이 지연 실재의 증거), 적용 지연 시 tick 종료판정이 stale 종료 위치로 방금 재개한 재생을 즉시 재-pause 한다 — 이 가드는 seek 적용 관측(또는 2s 상한)까지 종료판정만 유예한다.

    (b) VideoCompare.tsx — `replaySettleTicksRef = useRef<number | null>(null)` 신설. tick 의 종료판정 블록(1161~1186) 직전에서: ref 가 non-null 이면 decideReplaySettle 호출(ticksElapsed=ref, cL/cR/dL/dR/hasLeft/hasRight) — 'hold' 면 ref += 1 하고 `shouldPauseAtEnd` pause 실행을 건너뜀(판정식 계산은 그대로 두고 실행만 가드하거나 블록 전체를 조건으로 감싸기 — 어느 쪽이든 가드 비활성 시 byte 동일 경로), 'settled'/'expired' 면 ref = null 하고 그 tick 부터 정상 종료판정. drift 보정·불변식 판정·큐 블록은 무접촉(가드는 종료판정 하나만 유예 — 최소 개입).

    (c) 무장/해제 지점:
    - togglePlay isAtEnd 분기(1275~1284): 양쪽 seek 직후 `replaySettleTicksRef.current = 0` + `lastDriftSeekAtRef.current = Date.now()`(양쪽 방금 seek — 0.8s 히스테리시스 간격이 보정 유예로 작동, 미정착 버퍼 위 3중 seek pile-up 차단, planner_findings 12) + `chainSpokenRef.current.clear()`(새 런 발화 이력 리셋 — planner_findings 11 의 차단 경로 해소, "되감아 재진입 = 다시 발화" 의도 복원).
    - restart()(1322~1329): 양쪽 seek 직후 동일 3종(settle 무장 + 드리프트 스탬프 + chainSpoken clear) — 재생 중 끝 부근 restart 도 같은 stale 경로라 동일 근거.
    - togglePlay needsStartSync 분기(1285~1302): 동기 seek 직후 `lastDriftSeekAtRef.current = Date.now()` 만(종료 stale 아님 — settle 무장·chainSpoken clear 불요).
    - 해제(사용자 제스처 = 새 의도): togglePlay pause 분기(1227~1240)와 seekBoth(1333~1374)에서 `replaySettleTicksRef.current = null` (scrub 은 seekBoth 경유라 자동 포함).
    voiceSnap 복원(unsnapRight)은 isAtEnd 에서 항상 no-op — 무접촉(planner_findings 13).

    (d) SUMMARY 박제 요건 (실행자가 SUMMARY 작성 시 필수 포함): (1) 이번 수리는 코드 판독으로 증명된 3개 기제(stale 종료 재-pause / seek pile-up / 발화 이력 잔존)에 한정되며 belle 의 비결정 스터터 전체의 해소 증명이 아님을 명시("재봤다" 없는 단정 금지), (2) 미해결 잔여 = "재현 조건 미확보 — 기기 play()/seek 실효성은 코드로 증명 불가" 박제, (3) belle 관측 요청 4항목: ① 끊길 때 어느 패널인지(내 영상/정은지/둘 다) ② '일시정지됨 — 탭하여 계속' 배지가 떴는지(이번부터 가로에서도 보임) ③ 몇 번째 재생·음성 안내 ON/OFF 여부 ④ 세로/가로 어디였는지, (4) 다음 지렛대 = 사용자 재생 개시에도 백오프 관찰창 무장(선행 조건: playbackInvariant 에 종료-상태 규칙 신설 — planner_findings 10 리스크 서술 인용).

    주석 한국어 + 출처(belle 08-07 저녁, quick-260807-k70). ⚠ 주석에 `chainSpokenRef.current.clear()`·`decideReplaySettle(` 문자열을 그대로 쓰지 말 것(grep 카운트 게이트 자기무효화 방지 — 근거 서술은 호출부 옆 일반 문장으로).

    (e) 마감 게이트 겸 전체 회귀: 전 스위트 파일 단위 루프 + typecheck + 보호 파일 diff 0 일괄 실행.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && node --test app/src/lib/__tests__/replaySettle.test.ts && (cd app && npm run typecheck) && for f in app/src/lib/__tests__/*.test.ts app/src/lib/__tests__/*.test.mjs app/src/lib/pickerFailure.test.ts; do node --test "$f" || exit 1; done && [ "$(grep -c 'decideReplaySettle(' app/src/components/VideoCompare.tsx)" = "1" ] && [ "$(grep -c 'chainSpokenRef.current.clear()' app/src/components/VideoCompare.tsx)" = "3" ] && git diff HEAD --exit-code -- app/src/lib/playbackInvariant.ts app/src/lib/cueTrack.ts app/src/lib/voiceSnap.ts app/src/lib/driftHysteresis.ts && git diff HEAD --stat -- backend/ app/src/types/ | wc -l | grep -q '^ *0$'</automated>
  </verify>
  <done>재재생/처음으로 직후 stale 종료 위치가 재-pause 를 일으키지 않고(settle 가드 — seek 적용 관측 또는 2s 상한에 정상 복원), 양쪽 seek 직후 드리프트 보정이 0.8s 유예되며(기존 히스테리시스 재사용, 신규 상수 0), 발화 이력이 새 런에서 리셋된다(clear 3곳: 기존 tick null 관측 + isAtEnd + restart). 관찰창·플레이어 재생성 무접촉. replaySettle 테스트 + 전 스위트(기준선 208 + 신규) PASS + typecheck GREEN + 보호 파일 4종·backend/·types/ diff 0. SUMMARY 에 미해결 박제 + belle 관측 4항목 + 다음 지렛대 명기.</done>
</task>

</tasks>

<verification>
전 태스크 공통 게이트 (계획 범위 = 코드 + node 테스트 + typecheck. 시뮬 렌더·OTA·belle 실기기 확인은 오케스트레이터가 사이클 후 수행):

1. 전체 스위트: `for f in app/src/lib/__tests__/*.test.ts app/src/lib/__tests__/*.test.mjs app/src/lib/pickerFailure.test.ts; do node --test "$f" || exit 1; done` — 전 파일 PASS (기준선 208 tests + replaySettle 신규. 파일 단위 루프 — 디렉터리 일괄 실행 깨짐).
2. `cd app && npm run typecheck` GREEN.
3. 무접촉 증명 (HEAD 기준 — `git add` 무력화 방지): `git diff HEAD --exit-code -- app/src/lib/playbackInvariant.ts app/src/lib/cueTrack.ts app/src/lib/voiceSnap.ts app/src/lib/driftHysteresis.ts app/src/components/KeypointOverlay.tsx` 통과 + `git diff HEAD --stat -- backend/ app/src/types/` 빈 출력.
4. 관례: 주석 한국어 + 출처 인용(belle 08-07 저녁, quick-260807-k70), 이모지 0, 신규 hex 색 리터럴 0 (토큰·기존 스타일 배율 합성만).
</verification>

<success_criteria>
- BELLE-0807-8: 가로 전체보기에서 자막·'음성 중 — 잠시 멈춤'·'일시정지됨' 배지가 세로와 같은 조건·문구·상태로 렌더 (#8 해소). 세로 렌더 출력 불변, 조건·문구 단일 소스(공유 헬퍼).
- BELLE-0807-9: 재생 세션 동안 "말하는 지점만" — 좌 패널 활성 큐 record 부위만 빨강(큐 없으면 점 0), 우 패널 오버레이 숨김, 세션 밖 현행 유지, emphasis 강도 무변경(KeypointOverlay diff 0). 세로·가로 동일.
- BELLE-0807-10: 판독 근거 있는 3개 기제만 수리(settle 가드/드리프트 유예/발화 이력 리셋), 무거운 변경 0, SUMMARY 에 미해결 박제 + belle 관측 요청 + 다음 지렛대.
- 전 스위트 + typecheck GREEN, 채점·doc·계약 무접촉, 보호 파일 4종 + KeypointOverlay.tsx diff 0.
</success_criteria>

<output>
완료 시 `.planning/quick/260807-k70-belle-08-07-3-n/260807-k70-SUMMARY.md` 생성 (summary.md 템플릿).
</output>
