---
phase: quick-260807-k70
plan: 01
subsystem: ui
tags: [react-native, expo-video, video-compare, keypoint-overlay, playback, tdd]

requires:
  - phase: quick-260807-fpw
    provides: "재개 백오프 배지(resumeNotice)·활성 큐 윈도우 신호(activeCueRecordId)·재생 중 색 반전 opts 배선"
  - phase: quick-260807-iwp
    provides: "driftHysteresis 순수 모듈(0.8s 간격)·voiceSnap 짝 프레임 스냅·playbackEmphasis 1.3배"
provides:
  - "가로 전체보기(전체화면 Modal)에 자막·'음성 중 — 잠시 멈춤' pill·'일시정지됨' 배지 렌더 (기존 #8 해소) — renderCueOverlays 공유 헬퍼 단일 소스"
  - "재생 세션(재생+음성 정지+정지 중 발화) '말하는 지점만' 오버레이 정책 — 좌 패널 활성 큐 record 투영 관절만 빨강(큐 없으면 점 0), 우 패널 숨김, 세로·가로 동일"
  - "lib/replaySettle.ts — 재재생 직후 stale 종료판정 유예 순수 판정 (REPLAY_SETTLE_MAX_TICKS=20)"
  - "재재생 안정화 3종: settle 가드 + 양쪽 seek 직후 드리프트 보정 0.8s 유예(기존 히스테리시스 재사용) + 발화 이력(chainSpoken) 명시 리셋"
affects: [video-compare, result-screen, playback-stability, ota]

tech-stack:
  added: []
  patterns:
    - "renderCueOverlays(fs) 공유 헬퍼 — 세로/전체화면 큐 상태 렌더 단일 소스 (renderControls 선례)"
    - "inPlaybackSession 세션 판정 — 기존 opts 4필드만으로 파생 (신규 opts 필드 0)"
    - "settle 가드 — 순수 모듈이 상수·판정 소유, tick 은 pause 실행만 가드 (driftHysteresis 관례)"

key-files:
  created:
    - app/src/lib/replaySettle.ts
    - app/src/lib/__tests__/replaySettle.test.ts
  modified:
    - app/src/components/VideoCompare.tsx
    - app/src/app/analysis/result.tsx

key-decisions:
  - "재개 배지를 전체화면에도 렌더 — 같은 상태의 렌더 위치 추가일 뿐이며, belle 가 가로에서 스톨을 보면 배지 유무가 BELLE-0807-10 잔여 진단의 관측 신호"
  - "세션 판정에 '정지 상태 중 발화' 편입 — 표시-발화 일치 원칙상 의도된 포함으로 문서화"
  - "세션 중 빨강 대상은 voiceCueRecordId 우선 (체인 발화 중 activeCueWindowRecordId 는 멈춘 cL 기준이라 이전 큐 오귀속 가능)"
  - "관찰창(백오프) replay 무장 미도입 — 7.5s 미만 영상 자연 종료 오개입·배지 오표시 리스크 (다음 지렛대로 박제)"

patterns-established:
  - "전체화면 텍스트 배율: 기존 스타일 + fs 배율 스타일 배열 합성 (fsLegendText 선례, 신규 절대 px 최소화)"

requirements-completed: [BELLE-0807-8, BELLE-0807-9, BELLE-0807-10]

duration: 16min
completed: 2026-08-07
---

# Quick 260807-k70: belle 08-07 저녁 3건 (가로 자막·지점만 표기·재생 안정성) Summary

**가로 전체보기 자막·정지 배지 렌더(#8 해소) + 재생 세션 "말하는 지점만" 오버레이 정책 + 재재생 settle 가드 3종 (전 스위트 216 GREEN, 채점·doc·계약 무접촉)**

## Performance

- **Duration:** 16 min
- **Started:** 2026-08-07T05:51:47Z
- **Completed:** 2026-08-07T06:07:37Z
- **Tasks:** 3 (Task 3 = TDD RED/GREEN)
- **Files modified:** 4 (신규 2 + 수정 2)

## Accomplishments

- **BELLE-0807-8**: `renderCueOverlays(fs)` 공유 헬퍼 신설 — 자막 배너·'음성 중 — 잠시 멈춤' pill·'일시정지됨 — 탭하여 계속' 배지가 전체화면(진짜 가로 + 90도 회전 폴백)에서도 세로와 같은 조건·문구·상태 소스로 렌더. 문구 리터럴 각 1곳 유지가 단일 소스 증명(grep 게이트). 세로 렌더 출력 불변.
- **BELLE-0807-9**: 재생 세션(`inPlaybackSession` = 재생 중 OR 발화 중) 동안 좌 패널은 활성 음성 큐 record 투영 관절만 빨강(큐 없으면 점 0, 스켈레톤·번호·주황·그룹·폴백 전부 숨김), 우 패널 오버레이 숨김. 각 prop 삼항의 세션-밖 가지는 현행 표현식 그대로(세션 밖 렌더 동치). emphasis 강도(1.3배) 무변경 — KeypointOverlay.tsx diff 0. 세로·가로 동일(같은 render prop).
- **BELLE-0807-10**: 코드 판독 근거가 선 3개 기제만 수리 — (a) settle 가드(stale 종료 위치 재-pause 차단, 2.0s 상한), (b) 양쪽 seek 직후 드리프트 보정 0.8s 유예(기존 히스테리시스 스탬프 재사용, 신규 상수 0), (c) 발화 이력 명시 리셋(clear 3곳: 기존 tick null 관측 + isAtEnd + restart).

## Task Commits

1. **Task 1: 가로 전체보기 자막·pill·재개 배지 (BELLE-0807-8)** - `10141a99` (fix)
2. **Task 2: 재생 세션 "말하는 지점만" (BELLE-0807-9)** - `3b16bcae` (fix)
3. **Task 3: settle 가드 RED 테스트 (BELLE-0807-10)** - `69b4b1c9` (test)
4. **Task 3: settle 가드 + 드리프트 유예 + 발화 이력 리셋 GREEN (BELLE-0807-10)** - `6a6f6234` (feat)

## Files Created/Modified

- `app/src/lib/replaySettle.ts` - decideReplaySettle 순수 판정 + REPLAY_SETTLE_MAX_TICKS=20 (재재생 직후 stale 종료판정 유예, 근거 서술 헤더)
- `app/src/lib/__tests__/replaySettle.test.ts` - settled/hold/expired 분기 + 끝 임계 경계 + duration 미산정 + 단일 패널 + NaN 방어 + 상수 박제 (8 tests)
- `app/src/components/VideoCompare.tsx` - renderCueOverlays 공유 헬퍼 + fs 스타일 4종, 우측 슬롯 세션 신호 passthrough, settle 가드 tick 배선 + 3종 무장/해제
- `app/src/app/analysis/result.tsx` - inPlaybackSession/sessionCueRecordId 파생 + leftOverlay 8개 prop 세션 분기, rightOverlay 세션 중 숨김 (mode1 만)

## Decisions Made

- 재개 배지를 전체화면에도 렌더: 같은 상태의 렌더 위치 추가일 뿐이며, belle 가 가로에서 스톨을 보면 배지 유무가 BELLE-0807-10 잔여 진단의 관측 신호가 된다 (코드 주석에도 명기).
- "정지 상태 중 발화"(멈춤 없는 발화)도 세션에 편입 — 표시-발화 일치 원칙상 의도된 포함으로 문서화.
- 세션 중 빨강 대상 record 는 voiceCueRecordId 우선, 재생 중(발화 없음)엔 activeCueRecordId — 체인 발화 중 활성 윈도우가 이전 큐를 가리키는 함정 회피 (VideoCompare 기존 주석이 실증).
- fsResumeNoticeWrap paddingBottom 166 = fs 자막 zone 70 + 3줄 × 배율 lineHeight 25.5 + 패딩 12 + 간격 7 (세로 96 산식 관례로 주석 박제).

## Deviations from Plan

None - plan executed exactly as written.

## TDD Gate Compliance

- RED: `69b4b1c9` (test) — lib/replaySettle.ts 미구현 상태에서 스위트 FAIL 확인 후 커밋.
- GREEN: `6a6f6234` (feat) — 순수 모듈 + VideoCompare 배선, 8 tests PASS.
- REFACTOR: 불필요 (첫 구현이 최종형).

## Verification Results

- 전 스위트 파일 단위 루프: **16 files, 216 tests ALL GREEN** (기준선 208 + replaySettle 신규 8).
- `npm run typecheck` GREEN.
- 무접촉 증명 (plan 시작 기준 03f76d4b 대비 diff 0): `playbackInvariant.ts`·`cueTrack.ts`·`voiceSnap.ts`·`driftHysteresis.ts`·`KeypointOverlay.tsx` + `backend/` + `app/src/types/`.
- grep 게이트: renderCueOverlays 3곳(정의+호출 2)·pill 문구 1곳·handleResumeNoticePress 1곳·decideReplaySettle 호출 1곳·발화 이력 clear 3곳·신규 hex 리터럴 0.

## BELLE-0807-10 한정·미해결 박제 (task_spec 필수)

**이번 수리의 한계 (단정 금지):** 이번 수리는 코드 판독으로 증명된 3개 기제 — (1) 재재생 직후 stale 종료 위치가 tick 종료판정을 참으로 만들어 방금 재개한 재생을 즉시 재-pause, (2) 미정착 버퍼 위 seek pile-up(재재생 seek 직후 드리프트 보정이 겹침), (3) 이전 런 발화 이력 잔존이 새 런의 발화 차단 — 에 한정된다. **belle 이 관측한 비결정 스터터 전체의 해소 증명이 아니다** (기기 재현을 재보지 않았다 — 코드 판독만 했다).

**미해결 잔여:** 재현 조건 미확보 — 기기에서 play()/seek 호출이 실제로 실효했는지는 코드로 증명 불가 (expo-video 는 동기 확인 수단이 없고, 기존 백오프 관찰창·nudge 도 이 불확실성 때문에 존재한다). 스터터의 다른 원인(재버퍼 스톨, S3 스트리밍 지연, 플레이어 내부 레이스)은 열려 있다.

**belle 관측 요청 4항목 (다음 끊김 목격 시):**
1. 끊길 때 어느 패널인지 — 내 영상 / 정은지 / 둘 다.
2. '일시정지됨 — 탭하여 계속' 배지가 떴는지 (이번부터 **가로에서도** 보인다).
3. 몇 번째 재생이었는지 + 음성 안내 ON/OFF 여부.
4. 세로 화면이었는지 가로 전체보기였는지.

**다음 지렛대 (이번 사이클 의도적 미적용):** 사용자 재생 개시(togglePlay/재재생)에도 백오프 관찰창(RESUME_WATCH_TICKS)을 무장하는 것. 선행 조건 = playbackInvariant 에 종료-상태 규칙 신설 — 지금 무장하면 관찰창 7.5s(75 tick)보다 짧은 영상의 자연 종료 pause 가 열린 창 안에 들어와 R7' 가 끝에서 retry-play 로 재생을 되살리고, 소진 후 converge-pause 배지가 자연 종료에 오표시된다 (planner_findings 10 판독 근거). 플레이어 재생성도 근거 없는 리스크 변경이라 미도입.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- 시뮬 렌더 확인·OTA 발행은 오케스트레이터가 사이클 후 수행 (계획 범위 밖).
- belle 실기기 확인 항목: 가로 전체보기 자막/pill/배지 노출, 재생 중 "말하는 지점만" 점 표기(세로·가로), 재재생 n회 반복 안정성 (+위 관측 요청 4항목).

## Self-Check: PASSED

- 파일 존재 확인: replaySettle.ts / replaySettle.test.ts / VideoCompare.tsx / result.tsx / 본 SUMMARY — 전건 FOUND.
- 커밋 존재 확인: 10141a99 / 3b16bcae / 69b4b1c9 / 6a6f6234 — 전건 브랜치에 FOUND.

---
*Phase: quick-260807-k70*
*Completed: 2026-08-07*
