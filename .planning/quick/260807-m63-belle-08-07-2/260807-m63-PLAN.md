---
phase: quick-260807-m63
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/VideoCompare.tsx
  - app/src/lib/cueTrack.ts
  - app/src/lib/__tests__/cueTrack.test.ts
autonomous: true
requirements: [M63-1, M63-2]
user_setup: []

must_haves:
  truths:
    - "스크럽(PanResponder grant~release/terminate) 중에는 tick 의 큐 진입 판정·발화 블록이 완전 정지 — 바를 끌어 여러 큐 윈도우를 통과해도 speakCue 도달 0, 오디오 큐잉/중첩 0 (M63-1, belle ①)"
    - "드래그 릴리스 시 릴리스 지점에 이미 열려 있는 윈도우(겹침이면 전부)는 '이미 통과한 것' — 즉시 발화 0(자막 갱신은 유지), 릴리스 지점 이후 새로 진입하는 윈도우부터 정상 발화 (M63-1)"
    - "릴리스 시 발화 이력(chainSpokenRef)이 릴리스 지점의 열린-윈도우 집합으로 교체돼, 스크럽으로 되돌아간 경우 릴리스 지점 이전 큐들은 재생이 그 순간을 다시 지나면 정상 발화 — replay 의미 보존 (M63-1, k70 발화 이력 리셋 관례)"
    - "CUE_CHAIN_HORIZON_SEC = 0.3 — 파워스핀(윈도우 시작 간격 0.11s, 겹침)은 계속 한 멈춤에서 연속 발화, 엘보 ③9.35s→④10.36s(간격 1.01s)는 체인하지 않고 재개 후 ④의 제 순간까지 재생해 정상 정지·발화 (M63-2, belle ④)"
    - "채점·doc·계약 무접촉: backend/ 와 app/src/types/ diff 0. playbackInvariant.ts·voiceSnap.ts·driftHysteresis.ts·replaySettle.ts diff 0 (git diff HEAD 기준)"
    - "전 스위트 파일 단위 루프 GREEN(직전 기준선 216 + 신규/갱신) + `npm run typecheck` GREEN"
  artifacts:
    - path: "app/src/lib/cueTrack.ts"
      provides: "openCueRecordIds 순수 헬퍼(릴리스 지점 열린-윈도우 recordId 집합) + CUE_CHAIN_HORIZON_SEC=0.3 (belle 08-07 밤 근거 주석)"
      exports: ["openCueRecordIds", "CUE_CHAIN_HORIZON_SEC"]
    - path: "app/src/lib/__tests__/cueTrack.test.ts"
      provides: "openCueRecordIds 테스트(겹침 전부 수집·반개구간·orphan 제외·방어) + 지평 경계 0.3 갱신 + 엘보 ③④ 실형상(체인 금지) + 상수 박제"
    - path: "app/src/components/VideoCompare.tsx"
      provides: "tick 큐 진입 블록 scrubbingRef 게이트 + PanResponder release/terminate 발화 이력 교체(openCueRecordIds 시드)"
  key_links:
    - from: "app/src/components/VideoCompare.tsx"
      to: "app/src/components/VideoCompare.tsx"
      via: "tick 큐 진입 블록이 scrubbingRef 를 게이트 조건으로 (기존 917 재개 판정 게이트와 대칭)"
      pattern: "!voicePauseRef\\.current && !scrubbingRef\\.current"
    - from: "app/src/components/VideoCompare.tsx"
      to: "app/src/lib/cueTrack.ts"
      via: "onPanResponderRelease/Terminate 가 openCueRecordIds(cueWindowsRef.current, 릴리스 시각)로 chainSpokenRef 교체"
      pattern: "openCueRecordIds"
    - from: "app/src/components/VideoCompare.tsx"
      to: "app/src/lib/cueTrack.ts"
      via: "tick 체인 블록의 기존 CUE_CHAIN_HORIZON_SEC 배선 그대로 — 상수 값만 1.0→0.3 (VideoCompare diff 는 게이트·릴리스 훅만)"
      pattern: "CUE_CHAIN_HORIZON_SEC"
---

<objective>
belle 08-07 밤 실기기 피드백 2건 (k70 OTA a8874040 확인 후). 앱 전용 — 채점·doc·백엔드·계약 무접촉.

1. (M63-1, belle ①) 스크럽 중 음성 발화 게이트 — "다 본 다음 새로고침 말고 바를 끌어서 처음에 갖다 놓으면 지나가는 포인트에서 음성이 한꺼번에 나와버림". 바 드래그 중 큐 진입 판정·발화를 완전 정지하고, 릴리스 지점에 이미 열려 있는 윈도우는 통과한 것으로 취급(요란한 반중간 발화 금지), 이력은 릴리스 지점 기준으로 교체해 되감기 replay 의미를 보존한다.
2. (M63-2, belle ④) 체인 지평 축소 — "④번은 영상이 재생된 다음이 아니라 그 지점으로 바로 점프하듯이 끊겨서 옮겨지는 경향". CUE_CHAIN_HORIZON_SEC 1.0→0.3. 엘보 ③무릎 9.35s→④팔꿈치 10.36s(간격 1.01s)가 1.0 지평에 걸려 체인되던 것을 끊고 제 순간까지 재생하게 한다. 파워스핀(0.11s 간격 겹침)의 연속 발화는 유지.

Purpose: 스크럽이라는 탐색 제스처가 코칭 음성을 오발사하지 않고, 체인은 "재생 시간이 사실상 0인 겹침 전용"이라는 원칙으로 좁혀져 각 감점의 제 순간을 영상으로 보게 된다.
Output: VideoCompare 게이트+릴리스 훅, cueTrack 순수 헬퍼+상수 변경, node --test 신규/갱신 케이스. 전 스위트(기준선 216+신규)/typecheck GREEN. 시뮬·OTA 는 오케스트레이터가 사이클 후 수행(계획 범위 밖).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@app/src/components/VideoCompare.tsx
@app/src/lib/cueTrack.ts
@app/src/lib/__tests__/cueTrack.test.ts
</context>

<planner_findings>
플래너가 실측 판독으로 확정한 사실 — 실행자는 재조사 불요, 인용해 사용 (행 번호 = HEAD 8407d3c7 기준, app/src 워킹트리 clean):

1. **① 증상 기전 확정**: 스크럽 경로는 PanResponder(1497~1531) grant→`scrubbingRef=true`+pause, move 마다 `scrubAtX`→`seekBoth`(1401~1445). `seekBoth` 가 매 move 마다 `stopCue()`+`voicePauseRef.current=false`+`setVoiceCueRecordId(null)` 을 실행하므로 **tick(100ms)의 큐 진입 블록 862 `if (!voicePauseRef.current)` 가 드래그 중에도 계속 돈다** — 플레이헤드가 지나는 윈도우마다 자막 전환→`speakCue`(880) 발화. 스크럽 중엔 `leftPlaying=false` 라 pause 분기(884)에 안 들어가 발화가 끊기지 않고 연달아 겹친다(belle "한꺼번에"). 발화 site 는 880·934 두 곳뿐이고, 체인 site(934)는 이미 917 `voicePauseRef.current && !scrubbingRef.current` 로 scrub 게이트됨("scrub 중에는 판정 보류" 주석 915~916). **862 에 `!scrubbingRef.current` 를 더하면 진입측 대칭이 완성된다.**
2. **릴리스 현재 코드**: `onPanResponderRelease`(1515)는 `scrubbingRef=false` + wasPlaying 이면 200ms 뒤 play 뿐. `onPanResponderTerminate`(1526)는 `scrubbingRef=false` 뿐. 릴리스 지점 윈도우 처리·이력 정리는 전무 — 릴리스 직후 첫 tick 이 자막 전환으로 그 윈도우를 즉시 발화한다(반중간 요란 시작).
3. **chainSpokenRef(709) = recordId 발화 가드의 정본**: speak 성공 시 add(882·939), `activeCue===null` 관측 tick 에서 clear(851~853), 재재생/처음으로 리셋에서 clear(1337·1394, k70 관례). **이력은 speak 만 차단하고 자막 갱신은 유지**(재발화 함정 b, 876~879 주석) — 릴리스 지점 윈도우를 이 Set 에 넣으면 "자막은 나오되 발화 없음"이 기존 관례 그대로 성립한다. 릴리스 핸들러에서 자막 텍스트 동기화는 불요(다음 tick 의 텍스트 전환이 처리).
4. **릴리스 시드는 activeCue 승자 하나가 아니라 열린 윈도우 전부여야 한다**: activeCue 는 겹침에서 시작 늦은 것 하나만 반환(128~142). 겹침 구간(파워스핀 형상)에 릴리스하면 두 윈도우 다 "새로 진입"이 아니므로 둘 다 통과 취급 — 하나만 넣으면 나머지가 진행 중 전환·체인에서 발화한다. 균일 폭 윈도우라도 buildCueWindows 의 0-클램프(96)로 시작 동률 형상이 가능해 승자-단일 마킹은 불충분.
5. **④ 증상 기전 확정**: 프로덕션 윈도우 `CUE_WINDOW_SEC=1.6`(result.tsx:174, half 0.8). 엘보 ③ center 9.35 → [8.55, 10.15), ④ center 10.36 → [9.56, 11.16). 멈춤은 100ms tick 이 윈도우 진입을 관측한 시각이라 8.55 정각이 아닌 ~8.6. 체인 판정 `startSec <= cL + horizon`: 1.0 지평에서 9.56 <= 9.6 → 체인(belle 의 "점프하듯"). 0.3 지평에서 9.56 > 8.9 → 체인 안 됨 → 기존 재개 경로(958~981)로 재생 → 9.56 진입 시 activeCue 전환 → 정상 정지·발화. **파워스핀은 0.3 에서도 유지**: 큐2 startSec 4.31 은 멈춤 시각(~4.2-4.3)에 이미 열려 있어 4.31 <= cL+0.3 성립.
6. **기존 테스트 영향**: cueTrack.test.ts 의 체인 테스트 중 상수를 쓰는 것은 파워스핀 테스트(167~177, cL 5.0 에 큐2 startSec 4.31 = 이미 열린 윈도우)뿐이라 0.3 에서도 GREEN — 수정 금지. 지평 경계 테스트(179~188)는 리터럴 1.0 인자라 함수 semantics 상 GREEN 이지만 프로덕션 지평을 검증하지 않음 — 상수 기준으로 갱신 대상. 나머지 리터럴-인자 테스트(190~235)는 인자 semantics 검증이라 유지.
7. **게이트 비대상 (의도된 무변경)**: `activeCueWindowRecordId` 추적(840~848)은 "발화·멈춤 여부와 무관하게 항상" 주석의 표시 전용 신호(belle 08-07 #4 색 반전) — 게이트 밖 유지. 851~853 null-clear 도 유지(릴리스 교체가 결정적으로 덮음). `stepBy`/`seekBoth` 단발 점프(0.1s 스텝 버튼)는 belle 지적 범위 밖 — 기존 동작 유지, diff 최소.
8. **전 스위트 관례 (k70 SUMMARY)**: 16 파일 216 tests, 파일 단위 루프 — `app/src/lib/__tests__/*.test.*`(15개, visualCards 는 .mjs) + `app/src/lib/pickerFailure.test.ts`. Node 24 type stripping 으로 신규 npm 의존성 0.
</planner_findings>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 스크럽 중 발화 게이트 + 릴리스 이력 교체 (M63-1)</name>
  <files>app/src/lib/cueTrack.ts, app/src/lib/__tests__/cueTrack.test.ts, app/src/components/VideoCompare.tsx</files>
  <behavior>
    openCueRecordIds(windows, currentSec): string[] — cueTrack.ts 신규 순수 함수 (node --test 로 선검증):
    - 반개구간 [startSec, endSec) 로 currentSec 을 포함하는 **모든** 윈도우의 recordId 수집 (activeCue 와 달리 승자 1개가 아님 — planner_findings 4). 경계: currentSec == startSec 포함, currentSec == endSec 제외.
    - 파워스핀 겹침 형상([4.2,5.8) r1 + [4.31,5.91) r2, currentSec 5.0) → ['r1','r2'] 둘 다.
    - recordId 없는 윈도우(고아 큐)는 스킵. 윈도우 사이 공백 시각 → []. 비유한 currentSec·windows 부재/빈 배열 → [] (크래시 0, activeCue 방어 관례).
    - 반환 순서는 입력 순 (Set 시드 용도라 순서 무관하나 결정성 유지).
  </behavior>
  <action>
    (a) `app/src/lib/cueTrack.ts` 에 `openCueRecordIds` 를 nextChainedCue 아래에 추가. 헤더 주석(한국어): belle 08-07 밤 ① (quick-260807-m63) — 스크럽 릴리스 지점에 **이미 열려 있는** 윈도우는 "통과한 것"으로 취급하는 발화 이력 시드. 겹침이면 전부 수집하는 이유(activeCue 승자 1개만 마킹하면 나머지 겹침 윈도우가 진행 중 전환·체인에서 발화 — 릴리스 이후 "새로 진입"한 윈도우만 발화한다는 원칙) 명기.

    (b) `app/src/lib/__tests__/cueTrack.test.ts` 에 behavior 블록의 케이스로 Test 6 블록 추가(파일 헤더의 검증 축 목록에 6번 항목 추가). RED 확인(함수 부재로 실패) 후 (a) 구현 → GREEN 순서 권장 — 단일 커밋 가능.

    (c) `app/src/components/VideoCompare.tsx` 배선 2곳 (게이트 대상은 PanResponder 스크럽만 — planner_findings 7):
    - **tick 진입 게이트**: 862 `if (!voicePauseRef.current) {` → `if (!voicePauseRef.current && !scrubbingRef.current) {`. 직전 주석에 추가(한국어): belle 08-07 밤 ① (quick-260807-m63) — 스크럽 중 큐 진입 판정·발화 완전 정지. 드래그로 플레이헤드가 윈도우들을 훑을 때 seekBoth 가 voicePauseRef 를 매 move 풀어 이 블록이 돌며 통과 윈도우마다 발화하던 경로 차단. 재개 판정(아래 917)의 기존 scrub 게이트("판정 보류")와 대칭. 840~848 의 activeCueWindowRecordId 추적(표시 전용, "항상" 주석)은 게이트 밖 유지.
    - **릴리스 이력 교체**: `onPanResponderRelease`(1515)와 `onPanResponderTerminate`(1526) 양쪽에서 `scrubbingRef.current = false` 직전에 `chainSpokenRef.current = new Set(openCueRecordIds(cueWindowsRef.current, leftPlayer?.currentTime ?? 0))` 수행 (릴리스 시각 소스는 tick 의 cL 과 동일한 `leftPlayer?.currentTime ?? 0`). 중복 코드는 지역 함수 하나로 묶어 두 핸들러가 공유. 주석(한국어): belle 08-07 밤 ① — (1) 릴리스 지점에 열려 있는 윈도우 = 통과 취급 → 첫 tick 이 자막만 갱신하고 발화하지 않음(이력은 speak 만 차단, 재발화 함정 b 관례), (2) 교체 = 그 외 이력 전부 소거 → 되감았으면 릴리스 지점 이전 큐들이 재생 재통과 시 정상 발화(k70 발화 이력 리셋 관례 — replay 의미 보존). cueTrack import 에 `openCueRecordIds` 추가. panResponder useMemo 의존성 배열은 ref·player 만 추가로 읽으므로 기존 그대로(`leftPlayer` 이미 포함).

    금지: playbackInvariant.ts·voiceSnap.ts·driftHysteresis.ts·replaySettle.ts·backend/·app/src/types/ 접촉 금지(diff 0 펜스). seekBoth/stepBy 내부 변경 금지(이미 stopCue+상태 정리 완비 — planner_findings 1). 이모지 금지, 주석 한국어+출처(belle 08-07 밤, quick-260807-m63).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && node --test src/lib/__tests__/cueTrack.test.ts && npm run typecheck && grep -c 'voicePauseRef.current && !scrubbingRef.current' src/components/VideoCompare.tsx | grep -qx '2' && grep -c 'openCueRecordIds' src/components/VideoCompare.tsx | grep -q '[2-9]' && cd /Users/kimtaesung/Dev/SunityMotion && git diff HEAD --stat -- backend/ app/src/types/ app/src/lib/playbackInvariant.ts app/src/lib/voiceSnap.ts app/src/lib/driftHysteresis.ts app/src/lib/replaySettle.ts | wc -l | grep -qx '0'</automated>
  </verify>
  <done>openCueRecordIds 테스트 GREEN(겹침 전부·반개구간·고아 스킵·방어). tick 진입 블록이 scrubbingRef 로 게이트돼 드래그 중 speakCue 도달 경로 0 (발화 site 880 은 게이트 안, 934 는 기존 917 게이트 안 — 스크럽 중 발화 site 0). release/terminate 가 이력을 릴리스 지점 열린-윈도우 집합으로 교체. 펜스 파일 diff 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 체인 지평 1.0→0.3 + 경계·엘보 형상 테스트 (M63-2)</name>
  <files>app/src/lib/cueTrack.ts, app/src/lib/__tests__/cueTrack.test.ts</files>
  <behavior>
    - CUE_CHAIN_HORIZON_SEC === 0.3 (상수 박제 — 재확대 회귀 가드).
    - 엘보 실형상(CUE_WINDOW_SEC 1.6 파생, planner_findings 5): ③ [8.55,10.15) r3 + ④ [9.56,11.16) r4, 멈춤 cL=8.6(100ms tick 진입 관측 시각), spoken={r3} → nextChainedCue(…, CUE_CHAIN_HORIZON_SEC) === null (체인 안 됨 → 재개 후 제 순간까지 재생). 대조: 같은 형상에 legacy 1.0 지평이면 r4 반환 (이 대조가 지평 재확대 회귀를 행동으로 잡는다).
    - 파워스핀 기존 테스트(167~177, CUE_CHAIN_HORIZON_SEC 사용) 무수정 GREEN — 0.3 에서도 체인 유지(startSec 4.31 이 cL 5.0 에 이미 열린 윈도우).
    - 지평 경계 갱신: 포함 경계는 startSec 을 `5.0 + CUE_CHAIN_HORIZON_SEC` 로 테스트 내 계산해 구성(부동소수 리터럴 불일치 회피) → 후보 반환, 초과(예: startSec 5.35, currentSec 5.0) → null.
  </behavior>
  <action>
    (a) `app/src/lib/cueTrack.ts` 의 `CUE_CHAIN_HORIZON_SEC` 을 1.0 → 0.3 으로 변경하고 상수 주석을 재작성(한국어): 기존 파워스핀 근거(quick-260807-fpw, 0.11초 간격 끊김 해소)는 유지하되, belle 08-07 밤 ④ (quick-260807-m63) 근거 추가 — "④가 점프하듯 옮겨짐 — 제 순간까지 재생하는 것이 원칙, 체인은 재생 시간이 사실상 0인 겹침 전용". 수치 근거 명기: 엘보 ③9.35s→④10.36s(간격 1.01s, 윈도우 1.6s 기준 ④ 시작 9.56s — 멈춤 ~8.6s 에서 1.0 지평이면 체인)는 제외되고, 파워스핀(시작 간격 0.11s — 멈춤 시각에 이미 열린 윈도우)은 유지되는 값.

    (b) `app/src/lib/__tests__/cueTrack.test.ts` 갱신: (i) 지평 경계 테스트(179~188)를 behavior 블록대로 프로덕션 상수 기준으로 재작성 — 리터럴 1.0 대신 CUE_CHAIN_HORIZON_SEC 를 사용하고 포함 경계는 `5.0 + CUE_CHAIN_HORIZON_SEC` 계산값으로 구성. (ii) 엘보 ③④ 실형상 테스트 신규 추가 — 윈도우는 리터럴 객체([8.55,10.15) r3 / [9.56,11.16) r4)로 직접 구성(기존 체인 테스트 관례), cL=8.6 주석에 "멈춤은 100ms tick 의 윈도우 진입 관측 시각(8.55 정각 아님)" 명기, 상수 지평 → null 단언 + legacy 1.0 지평 → r4 대조 단언. (iii) 상수 박제 단언 `assert.equal(CUE_CHAIN_HORIZON_SEC, 0.3)` (k70 REPLAY_SETTLE_MAX_TICKS 박제 관례). (iv) 파일 헤더 검증 축 5번 문구에 0.3 지평·엘보 형상 반영. 파워스핀 테스트(167~177)와 리터럴-인자 semantics 테스트(190~235)는 무수정(planner_findings 6).

    금지: nextChainedCue 함수 본문 변경 금지(semantics 불변 — 상수만 변경). VideoCompare 의 상수 배선(931) 무접촉.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && for f in src/lib/__tests__/*.test.* src/lib/pickerFailure.test.ts; do node --test "$f" || exit 1; done && npm run typecheck && grep -q 'CUE_CHAIN_HORIZON_SEC = 0.3' src/lib/cueTrack.ts && cd /Users/kimtaesung/Dev/SunityMotion && git diff HEAD --stat -- backend/ app/src/types/ app/src/lib/playbackInvariant.ts app/src/lib/voiceSnap.ts app/src/lib/driftHysteresis.ts app/src/lib/replaySettle.ts | wc -l | grep -qx '0'</automated>
  </verify>
  <done>상수 0.3 + 근거 주석. 엘보 형상 체인 금지·legacy 대조·경계 0.3·상수 박제 테스트 GREEN. 파워스핀 테스트 무수정 GREEN. 전 스위트 파일 단위 루프 ALL GREEN(기준선 216 + Task 1/2 신규 — fail 0), typecheck GREEN, 펜스 diff 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (없음) | 앱 내부 재생 UI 순수 로직만 — 신규 입력 표면·네트워크·패키지 설치 0 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-m63-01 | DoS(오디오 폭주) | VideoCompare tick 발화 경로 | mitigate | 스크럽 중 진입 게이트 + 릴리스 이력 교체로 발화 site 2곳 전부 scrub 게이트 하에 둠 (본 작업 자체가 완화) |
| T-m63-SC | Tampering | npm/pip 설치 | accept | 신규 의존성 0 (node:test 표준 모듈만 — 설치 태스크 없음) |
</threat_model>

<verification>
- 전 스위트: `cd app && for f in src/lib/__tests__/*.test.* src/lib/pickerFailure.test.ts; do node --test "$f" || exit 1; done` — 16+ 파일 전건 pass (기준선 216 + 신규, fail 0).
- 타입: `cd app && npm run typecheck` clean.
- 무접촉 펜스: `git diff HEAD --stat -- backend/ app/src/types/ app/src/lib/playbackInvariant.ts app/src/lib/voiceSnap.ts app/src/lib/driftHysteresis.ts app/src/lib/replaySettle.ts` 출력 0줄. (Task 1 이 이 파일들을 접촉해야 할 정당한 근거는 발견되지 않았음 — planner_findings 1·2: 스크럽 상태·발화 이력·릴리스 훅 전부 VideoCompare 스코프.)
- 코드 형태: VideoCompare 에 `!voicePauseRef.current && !scrubbingRef.current` 2곳(진입 862 + 기존 재개 917), `openCueRecordIds` import+호출.
- 시뮬 실기 확인(스크럽 왕복 무발화·엘보 ④ 제 순간 정지)·OTA 는 오케스트레이터가 사이클 후 수행 — 계획 범위 밖. SUMMARY 에 미검증 항목으로 명기할 것.
</verification>

<success_criteria>
- belle ① 재현 시나리오(끝까지 본 뒤 바를 처음으로 드래그)에서 코드상 발화 경로 0: 드래그 중 = 진입 게이트, 릴리스 순간 = 이력 시드, 릴리스 후 재생 = 새로 진입 윈도우부터 발화.
- belle ④ 재현 시나리오(엘보 ③→④)에서 체인 판정 null → 기존 재개 경로로 ④ 윈도우까지 재생 후 정상 정지·발화. 파워스핀 연속 발화 불변.
- 전 스위트+typecheck GREEN, 펜스 diff 0, 채점·doc 무접촉.
</success_criteria>

<output>
`.planning/quick/260807-m63-belle-08-07-2/260807-m63-SUMMARY.md` 생성 — 시뮬 미검증 항목(스크럽 왕복 무발화·엘보 제 순간 정지 실기 확인)을 명시 박제.
</output>
