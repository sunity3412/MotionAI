---
phase: quick-260807-m63
plan: 01
subsystem: ui
tags: [react-native, expo-video, pan-responder, audio-cue, cue-track, node-test]

requires:
  - phase: quick-260807-fpw
    provides: 인접 큐 체이닝(nextChainedCue + CUE_CHAIN_HORIZON_SEC) + chainSpokenRef 발화 이력
  - phase: quick-260807-k70
    provides: 발화 이력 리셋 관례(재재생/처음으로 clear) + 전 스위트 파일 단위 루프 기준선 216
provides:
  - 스크럽(PanResponder) 중 tick 큐 진입 판정·발화 완전 정지 게이트 (M63-1)
  - openCueRecordIds 순수 헬퍼 — 릴리스 지점 열린-윈도우 recordId 집합(겹침 전부)
  - release/terminate 발화 이력 교체(통과 취급 + replay 의미 보존)
  - CUE_CHAIN_HORIZON_SEC 1.0 → 0.3 (체인 = 겹침 전용, M63-2)
affects: [VideoCompare 재생/발화 후속, 음성 큐 UX]

tech-stack:
  added: []
  patterns:
    - "스크럽 발화 게이트 대칭: 진입(862)·재개(917) 두 판정 모두 !scrubbingRef 게이트 — 발화 site 2곳 전부 scrub 하에서 도달 0"
    - "릴리스 이력 시드: 열린 윈도우 = 통과 취급, 교체 = 이전 이력 소거(replay 보존) — 승자 1개 아닌 겹침 전부 수집"
    - "체인 지평 = 겹침 전용(0.3): 멈춤 시각에 이미 열린 윈도우만 체인, 간격 있는 큐는 제 순간까지 재생"

key-files:
  created: []
  modified:
    - app/src/lib/cueTrack.ts
    - app/src/lib/__tests__/cueTrack.test.ts
    - app/src/components/VideoCompare.tsx

key-decisions:
  - "릴리스 시드는 activeCue 승자 1개가 아니라 열린 윈도우 전부 — 하나만 마킹하면 나머지 겹침 윈도우가 진행 중 전환·체인에서 발화 (planner_findings 4)"
  - "seedSpokenAtRelease 는 panResponder useMemo 팩토리 내부 지역 함수 — ref·leftPlayer(기존 dep)만 읽어 의존성 배열 무변경"
  - "0.3 지평: 엘보 ③④(간격 1.01s) 제외 + 파워스핀(0.11s 겹침) 유지를 동시에 만족하는 값 — 수치 근거 상수 주석에 박제"

patterns-established:
  - "발화 이력(chainSpokenRef)은 speak 만 차단하고 자막 갱신은 유지 — 릴리스 시드도 같은 관례 위에서 성립"

requirements-completed: [M63-1, M63-2]

duration: 9min
completed: 2026-08-07
---

# quick-260807-m63: 스크럽 발화 게이트 + 체인 지평 축소 Summary

**스크럽 드래그 중 큐 발화 완전 정지(tick 진입 게이트 + 릴리스 열린-윈도우 이력 교체) + CUE_CHAIN_HORIZON_SEC 1.0→0.3 으로 체인을 겹침 전용으로 축소 — belle 08-07 밤 ①·④ 앱 전용 수리**

## Performance

- **Duration:** 9 min
- **Started:** 2026-08-07T07:08:40Z
- **Completed:** 2026-08-07T07:17:29Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments

- **M63-1 (belle ①)**: "바를 끌어서 처음에 갖다 놓으면 지나가는 포인트에서 음성이 한꺼번에" 차단 — 3겹:
  - tick 큐 진입 블록에 `!scrubbingRef.current` 게이트 추가 — 드래그 중 seekBoth 가 voicePauseRef 를 매 move 풀어 자막 전환→speakCue(880) 가 연발하던 경로 차단. 체인 site(934)는 기존 917 게이트 안 — 스크럽 중 발화 site 0.
  - `openCueRecordIds` 순수 헬퍼 신규 (cueTrack.ts) — 릴리스 지점에 열려 있는 윈도우 recordId **전부** 수집(반개구간, 겹침 전부, 고아 스킵, 방어). node --test 4케이스 선검증(TDD RED→GREEN).
  - release/terminate 가 `chainSpokenRef` 를 릴리스 지점 열린-윈도우 집합으로 **교체** — 릴리스 지점 윈도우는 즉시 발화 0(자막은 다음 tick 이 갱신), 되감았으면 릴리스 이후 새로 진입하는 윈도우부터 정상 발화(replay 의미 보존).
- **M63-2 (belle ④)**: "④번은 그 지점으로 점프하듯 끊겨서 옮겨짐" 해소 — CUE_CHAIN_HORIZON_SEC 1.0→0.3. 엘보 ③9.35s→④10.36s(간격 1.01s, ④ 윈도우 시작 9.56s)가 멈춤 ~8.6s 에서 1.0 지평(9.56<=9.6)에 걸려 체인되던 것을 끊음 → 기존 재개 경로로 ④ 제 순간까지 재생 후 정상 정지·발화. 파워스핀(0.11s 간격 — 멈춤 시각에 이미 열린 윈도우)은 0.3 에서도 체인 유지(기존 테스트 무수정 GREEN).
- 전 스위트 16파일 **222 pass / fail 0** (기준선 216 + 신규 6: openCueRecordIds 4 + 엘보 실형상 1 + 상수 박제 1), `npm run typecheck` GREEN.

## Task Commits

Each task was committed atomically (code changes only):

1. **Task 1: 스크럽 중 발화 게이트 + 릴리스 이력 교체 (M63-1)** - `d2832572` (fix, TDD: RED 확인 후 구현·단일 커밋 — 계획 허용)
2. **Task 2: 체인 지평 1.0→0.3 + 경계·엘보 형상 테스트 (M63-2)** - `ab9ddcc4` (fix, TDD: RED 3건 확인 후 상수 변경)

## Files Created/Modified

- `app/src/lib/cueTrack.ts` - `openCueRecordIds` 신규(nextChainedCue 아래, 겹침 전부 수집 근거 주석) + `CUE_CHAIN_HORIZON_SEC` 0.3 (fpw 파워스핀 근거 유지 + m63 수치 근거 추가)
- `app/src/lib/__tests__/cueTrack.test.ts` - 검증 축 6개로 확장. Test 6 블록(openCueRecordIds 4건) + 지평 경계 상수 기준 재작성(`5.0 + CUE_CHAIN_HORIZON_SEC` 계산 구성) + 엘보 ③④ 실형상(0.3 체인 금지 + legacy 1.0 대조 = 재확대 회귀 가드) + 상수 0.3 박제
- `app/src/components/VideoCompare.tsx` - tick 진입 게이트(`!voicePauseRef.current && !scrubbingRef.current`, 재개 판정 917 게이트와 대칭) + panResponder useMemo 내 `seedSpokenAtRelease` 지역 함수(release/terminate 공유, `scrubbingRef=false` 직전 실행)

## Decisions Made

None - followed plan as specified (planner_findings 를 그대로 인용해 사용, 재조사 0).

## Deviations from Plan

None - plan executed exactly as written.

## Verification Evidence

- 전 스위트: 16파일 222 tests / 222 pass / 0 fail (직접 실행 — node --test 16파일 일괄, 파일별 서브프로세스)
- typecheck: `tsc --noEmit` clean (직접 실행)
- 코드 형태: `voicePauseRef.current && !scrubbingRef.current` grep 2곳(진입 + 기존 재개), `openCueRecordIds` VideoCompare 내 2곳(import + 호출) (직접 grep)
- 무접촉 펜스: `git diff c3cf8a2..HEAD --stat -- backend/ app/src/types/ playbackInvariant.ts voiceSnap.ts driftHysteresis.ts replaySettle.ts` = 0줄 (직접 실행)
- 파워스핀 체인 테스트(167~177) 무수정 GREEN — 0.3 에서도 유지 실증
- nextChainedCue 함수 본문·VideoCompare 상수 배선(931) 무접촉 — 상수 값만 변경

## Known Stubs

None - 스텁 0 (전 경로 실배선 + 테스트).

## Threat Flags

None - 신규 표면 0 (threat register T-m63-01 의 mitigate 가 본 작업 자체 — 발화 site 2곳 전부 scrub 게이트 하).

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## 미검증 항목 (실기/시뮬 — 계획 범위 밖, 오케스트레이터 사이클 후 수행)

- **스크럽 왕복 무발화**: 끝까지 본 뒤 바를 처음으로 드래그 → 드래그 중·릴리스 직후 발화 0, 이후 재생에서 큐마다 정상 발화 — 시뮬/실기기 미확인 (코드·테스트 근거만)
- **엘보 ④ 제 순간 정지**: ③ 발화 종료 후 체인 없이 재생 재개 → 9.56s 진입 시 정상 정지·발화 — 시뮬/실기기 미확인
- **OTA**: 미발행 (오케스트레이터 소관)

## Self-Check: PASSED

- 수정 3파일 + SUMMARY 존재 확인 (ls 직접 실행)
- 커밋 `d2832572`·`ab9ddcc4` git log 존재 확인 (직접 실행)

---
*Phase: quick-260807-m63*
*Completed: 2026-08-07*
