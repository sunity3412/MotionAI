---
phase: quick-260808-im8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/scripts/p35_observe_screen.py
  - .planning/quick/260808-im8-v1-r03/260808-im8-RESULT.md
  - .planning/quick/260808-im8-v1-r03/260808-im8-report.json
autonomous: true
requirements: [QUICK-260808-IM8]

must_haves:
  truths:
    - "자율 스크린이 리포에 커밋된 스크립트로 재실행 가능 — v0 이 세션 인라인이라 재부팅 소실 위험이던 것 해소 (v0 실측 지식은 V0_REGRESSION 게이트로 코드에 박제)."
    - "전구간 스크린이 v0 인라인 실측을 재현: powerspin left_ankle +0.56±0.05·벌림각 −24.5°±3(u 27.7 vs r 98.1), elbow 전 피처 약함(scaled ≤0.5), 미러 5동작 전부 동측 — `--regress-v0` exit 0."
    - "홀드 스크린이 user·ref 각자의 저에너지 구간(<p40, 이동평균 k=5, 최소 1s)을 추출하고, 짝 프레임이 양쪽 모두 홀드인 짝만으로 별도 집계해 전구간과 나란히 출력한다."
    - "elbow 홀드 스크린의 r03 블라인드 재발견 여부(몸라인-폴 또는 hip_mid 가 top 3 + user>ref 방향)가 기본 파라미터 1회 판정으로 기록 — 성공이든 실패든 수치 근거와 함께 정직 기록, 성공을 위한 임계 튜닝 0."
    - "과검출 가드(pdshapefault 홀드 bodyline·hip_mid scaled < 1.0)와 유지 가드(powerspin 스플릿 신호 전구간 top 3 잔존)가 함께 기계 판정된다."
    - "RESULT.md 에 LLM 학습 기여 1절 — 발견 목록을 라벨 후보로, 사람검증(belle 승인)과 기계단독 발견을 구분 (belle 08-08 '항상 언급' 지시)."
  artifacts:
    - path: "backend/scripts/p35_observe_screen.py"
      provides: "v0+v1 통합 자율 스크린 (전구간+홀드), POLE_X_CACHE, V0_REGRESSION 게이트, r03 verdict 하네스"
      min_lines: 200
    - path: ".planning/quick/260808-im8-v1-r03/260808-im8-RESULT.md"
      provides: "전구간/홀드 표 5동작 + r03 재발견 verdict + v0 회귀 대조 + LLM 학습 기여 절"
    - path: ".planning/quick/260808-im8-v1-r03/260808-im8-report.json"
      provides: "표 수치의 출처 데이터 (스크립트 --json-out 원본 박제)"
  key_links:
    - from: "backend/scripts/p35_observe_screen.py"
      to: ".planning/phases/35-server-rendered-comparison-video/data/{motion}/align.json"
      via: "--data-dir 로딩 (리포 영구화 데이터 d266cb8d)"
      pattern: "align\\.json"
    - from: "backend/scripts/p35_observe_screen.py"
      to: "backend/scripts/render_compare_prototype.py"
      via: "폴 재계산 폴백 경로에서만 lazy import (_detect_pole 재사용, 렌더러 무수정)"
      pattern: "from render_compare_prototype import"
    - from: ".planning/quick/260808-im8-v1-r03/260808-im8-RESULT.md"
      to: ".planning/quick/260808-im8-v1-r03/260808-im8-report.json"
      via: "표 수치 인용 출처"
      pattern: "260808-im8-report\\.json"
---

<objective>
자율 발견 스크린 v1 — belle 방향("내가 바라는 건 너가 자체적으로 찾는 것")의 v0 알고리즘을
리포 스크립트로 영구화하고, 홀드 구간 인식을 추가해 **belle 승인 r03(엘보 몸-폴 편차)을
블라인드로 재발견하는지** 검증한다.

Purpose: v0 은 오케스트레이터가 세션 인라인으로 실행해 검증했으나 파일로 저장되지 않아
재부팅 소실 위험(08-08 scratchpad 소실 사건 재발 방지 대상). v1 의 검증 질문은 "사람이
짚어준 결함(r03 몸-폴)을 기계가 스스로 찾아내는가" — 성공하면 자율 스크린이 라벨 후보
생성기로 승격 가능, 실패하면 실패 자체가 v1 한계 데이터.

Output: backend/scripts/p35_observe_screen.py (커밋) + 260808-im8-RESULT.md +
260808-im8-report.json 박제. 채점·렌더러 무접촉 (신규 스크립트 1개 + 결과 문서만).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend/scripts/render_compare_prototype.py  (271~390행만: _column_coverage/_detect_pole/_pole_gap_series/_hipmid_gap_series/_med3 — 폴 감지·gap 시계열 관례. 전체 재독 불필요)
@.planning/phases/35-server-rendered-comparison-video/data/README.md  (데이터 출처·검증 이력)
@.planning/quick/260808-epy-phase-35-2-p35-pdshape-r01-5-v7/260808-epy-SUMMARY.md  (임계 관례 + r03 실측 user 0.629 / ref 0.316)

**align.json 스키마 (플래너가 실측 확인 완료 — 재탐색 불필요):**
`motion`(str) / `fps`(15.0) / `userSize`·`refSize`([W,H]) / `userFrames`·`refFrames`(int) /
`curveRefSec`(list[userFrames] — user 프레임 i → ref 초) / `pairs`(rid → {atVideoSec,
refVideoSec, joint, ...}) / `userKp`·`refKp`(list[F], 각 행 flat 34 = 17관절×2 정규화 좌표 →
reshape (F,17,2)) / `userScore`·`refScore`(list[F]×17 conf) / `joints17`(COCO 이름: nose,
left/right_eye, left/right_ear, left/right_shoulder, left/right_elbow, left/right_wrist,
left/right_hip, left/right_knee, left/right_ankle).

**실행 인터프리터:** `backend/.venv/bin/python` (numpy·PIL·imageio_ffmpeg 존재 실측 완료.
`render_compare_prototype` 는 backend/scripts 를 sys.path 에 두고 import OK 실측 — 단
module top 에서 PIL·imageio_ffmpeg 를 당기므로 반드시 폴 재계산 함수 안에서만 lazy import).
</context>

<tasks>

<task type="auto">
  <name>Task 1: p35_observe_screen.py 작성 — v0 재현 스펙 + v1 홀드 스크린 통합, v0 회귀 게이트 내장</name>
  <files>backend/scripts/p35_observe_screen.py</files>
  <action>
backend/scripts/p35_observe_screen.py 신규 작성. 순수 numpy+stdlib(argparse/json/pathlib).
PIL·렌더러 import 는 폴 재계산 폴백 함수 내부에서만 lazy. 한국어 why 주석, 이모지 금지.
모듈 docstring 에 belle 방향 원문 인용: "내가 바라는 건 너가 자체적으로 찾는 것" — 자율
발견 스크린. v0(08-08 세션 인라인 검증)의 재현 스펙 + v1 홀드 구간 인식임을 명기.

**상수 (전부 스펙 출처 주석 — r03 재발견을 위한 튜닝 금지를 주석으로 명문):**
CONF_GATE=0.35 (v0 재현 스펙 — 렌더러 폴 문법의 0.5 와 다름을 주석 명시), VALID_MIN=0.40,
SCALE_ANGLE=30.0, SCALE_GAP=0.15, HOLD_SMOOTH_K=5, HOLD_PCT=40, HOLD_MIN_S=1.0,
HOLD_PAIR_MIN=10, MIRROR_IMPROVE=0.03.

**POLE_X_CACHE (모듈 dict — 08-08 프로브 실측·스틸 눈검증 출처를 주석 명기):**
elbow u/r 0.4992/0.5003, powerspin 0.502/0.502, kipup 0.498/0.501,
pdshapefault 0.4996/0.4993, peterpan 0.501/0.513.
캐시 우선. `--pole-frames-dir ROOT`(ROOT/{motion}/{side}/*.jpg) 지정 시에만 재계산 경로:
함수 내부에서 `from render_compare_prototype import _detect_pole` lazy import 재사용
(렌더러 무수정 — import 는 무접촉. 영상 원본이 scratchpad 세션 의존이라 캐시가 기본).
캐시·frames 둘 다 없는 동작은 폴 파생 피처 제외 + 사유 로그(fail-closed).
동작명은 이 캐시의 데이터 키로만 존재 — 스크린 피처·집계 로직에 동작명 분기 0
(motion-routing 일반화 원칙). V0_REGRESSION·verdict 하네스(실측 대조 목적)만 동작명 허용.

**피처 14종 (프레임별, 패널별. torso px = ||어깨중점−힙중점||×[W,H] per-frame, 무효(≤1e-3)는
패널 중앙값 대체 — 렌더러 _pole_gap_series 관례 준용):**
- 폴간격 10: |x_norm − pole_x|×W/torso — L/R elbow·wrist·knee·ankle(관절 conf≥0.35) +
  hip_mid·sho_mid(구성 두 관절 min conf≥0.35). 게이트 미달 프레임 = NaN.
- 몸라인-폴 편차(bodyline) 1: 프레임별 max(sho_mid gap, hip_mid gap) — 둘 다 유효할 때만.
- 다리 벌림각 1: hip_mid→left_ankle 와 hip_mid→right_ankle 의 **px 벡터**(kp×[W,H]) 사이각
  (도) — 구성 관절(양 힙+양 발목) conf 게이트. 정규화 좌표 각도는 W≠H 왜곡이라 px 가 정본.
- 무릎각 2: knee 꼭짓점, knee→hip·knee→ankle px 벡터 사이각(도), L/R 각각.

**짝·집계 (v0 스펙):** user 프레임 i ↔ ref 프레임 j=round(curveRefSec[i]×fps),
[0, refFrames−1] 클립. diff[i] = user[i] − ref[j] (양수 = user 가 큼). 유효 짝 = 양쪽
non-NaN. 유효율 < VALID_MIN 피처는 집계 제외로 표기. 집계 = 유효 diff 의 median.
scaled = |median| / (각도 SCALE_ANGLE, 간격 SCALE_GAP). 표는 scaled 내림차순.
패널별 중앙값(user_med/ref_med)도 산출 — 벌림각 "u 27.7 vs r 98.1" 류 대조용.

**미러 체크 (v0 스펙):** body12(양 어깨·팔꿈치·손목·힙·무릎·발목) 힙중심·몸통정규 pose
feature(conf≥0.35 미달 관절 0 처리 — 렌더러 feat 방식 준용). 짝 프레임 평균 L2 를 정상
ref vs 스왑 ref(L/R 라벨 스왑 + 힙중심 상대 x 부호 반전 = 물리 거울)로 비교, 개선률
(d_normal−d_swap)/d_normal ≥ MIRROR_IMPROVE 일 때만 "거울상", 아니면 "동측".

**홀드 구간 인식 (v1 신규):** 패널별 energy[t] = body12 중 양 프레임 conf≥0.35 관절의
||kp[t]−kp[t−1]||px/torso 평균(유효 관절 <4 → NaN) → 이동평균 k=HOLD_SMOOTH_K → 유효
energy 의 p{HOLD_PCT} 미만 연속 run ≥ HOLD_MIN_S(15fps 기준 15프레임)만 홀드 구간.
user·ref 독립 추출. 홀드 스크린 = 짝 (i,j) 가 **양쪽 모두** 홀드에 속한 짝만으로 동일 집계.
홀드 짝 < HOLD_PAIR_MIN → "홀드 판정 불가" 정직 출력(수치 날조 금지). 홀드 구간 초
목록·짝 수를 리포트에 포함.

**V0_REGRESSION 내장 게이트 (`--regress-v0` — v0 인라인 실측을 코드에 박제, 영구화의 본체):**
- powerspin 전구간: left_ankle 폴간격 median diff ∈ [0.51, 0.61] (v0 +0.56) AND 벌림각
  diff ∈ [−27.5, −21.5] (v0 −24.5) AND 벌림각 user_med 27.7±3 / ref_med 98.1±3.
- elbow 전구간: 유효 전 피처 scaled ≤ 0.5 (v0 "전 피처 ≤0.4x" + 여유 0.1).
- 미러: 5동작 전부 동측.
- kipup·peterpan: hard gate 없음(v0 서술이 정성적 "진폭 부족") — 음수 diff 다수 여부
  참고 출력만.
게이트 실패 시 exit 1 + 실측치 출력. **px 각도 관례로 벌림각 재현 실패 시에 한해**
정규화 좌표 관례 1회 교차 시도 허용 — v0 값을 재현하는 쪽 채택 + 채택 근거 주석 박제.
그래도 실패 → STOP 하고 실측치 그대로 보고(숫자 맞추기 금지, Task 2 진입 금지).

**r03 verdict 하네스 (`--verdict-r03` — 기계 판정으로 서사 왜곡 차단. 3게이트 각각
PASS/FAIL + overall 출력, JSON 리포트에도 포함):**
- G1 재발견: elbow **홀드 스크린**에서 bodyline 또는 hip_mid 폴간격이 scaled top 3 AND
  해당 median diff > 0 (user 가 폴에서 더 멂 — r03 "몸이 폴에서 떨어져 있어요" 방향 일치).
- G2 과검출 없음: pdshapefault 홀드 스크린의 bodyline·hip_mid scaled < 1.0 (1.0 = 렌더러
  유의미 마진 POLE_MARGIN 0.15 몸통 / SCALE_GAP 0.15 — 구조 유도, 신규 상수 아님.
  epy 실측: pdshapefault r03 마진 0.061 → 올바른 침묵이었음).
- G3 유지: powerspin 스플릿 신호(벌림각 또는 ankle 폴간격)가 **전구간 스크린** top 3 잔존.
overall = G1∧G2∧G3. 어떤 게이트든 FAIL 이면 FAIL 로 출력 — 홀드 파라미터(HOLD_PCT 등)를
verdict 통과 목적으로 바꾸는 것 금지(curve-fit 금지, 판정은 기본값 1회).

**CLI:** --data-dir(기본 .planning/phases/35-server-rendered-comparison-video/data),
--motions(콤마, 기본 elbow,powerspin,kipup,pdshapefault,peterpan), --full/--hold(플래그 —
기본 둘 다, 지정 시 해당 스크린만), --json-out PATH(전구간+홀드+홀드구간+미러+패널중앙값
+verdict 전수 수치), --pole-frames-dir, --regress-v0, --verdict-r03. 표는 stdout.

작성 후 `--regress-v0` PASS 확인하고 스크립트만 먼저 커밋:
`feat(quick-260808-im8): 자율 스크린 v1 — v0 리포 영구화 + 홀드 구간 인식`
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && backend/.venv/bin/python -c "import ast; ast.parse(open('backend/scripts/p35_observe_screen.py').read()); print('parse OK')" && backend/.venv/bin/python backend/scripts/p35_observe_screen.py --regress-v0 && ! grep -q "import cv2" backend/scripts/p35_observe_screen.py && ! grep -n "^from PIL\|^import PIL\|^from render_compare_prototype\|^import render_compare_prototype" backend/scripts/p35_observe_screen.py</automated>
  </verify>
  <done>
스크립트가 리포에 존재·커밋되고 `--regress-v0` exit 0 (powerspin left_ankle·벌림각·패널
중앙값, elbow 전 피처 약함, 미러 5/5 동측 전부 v0 재현). cv2 import 0, PIL·렌더러
top-level import 0 (lazy 만). 홀드 스크린·verdict 하네스 코드 경로 포함.
  </done>
</task>

<task type="auto">
  <name>Task 2: 5동작 전구간+홀드 실행 → r03 재발견 verdict → RESULT.md 박제 + 커밋</name>
  <files>.planning/quick/260808-im8-v1-r03/260808-im8-RESULT.md, .planning/quick/260808-im8-v1-r03/260808-im8-report.json</files>
  <action>
실행: `backend/.venv/bin/python backend/scripts/p35_observe_screen.py --verdict-r03
--json-out .planning/quick/260808-im8-v1-r03/260808-im8-report.json` (기본 5동작, 전구간+홀드).
verdict 는 기본 파라미터 1회 판정 — G1 실패해도 재실행·파라미터 변경 금지.

260808-im8-RESULT.md 를 Write 도구로 작성 (모든 수치는 report JSON 에서 인용 — 출처 명기):
1. **한 줄**: verdict overall + G1/G2/G3 요약. 실패면 실패를 헤드라인에 그대로
   ("성공으로 꾸미지 말 것" — 실패 자체가 v1 한계 데이터).
2. **홀드 구간 실측**: 5동작 × user/ref 홀드 구간(초 범위 목록)·홀드 짝 수. "홀드 판정
   불가" 동작은 사유와 함께 기재.
3. **전구간 스크린 표** (5동작): 피처 / median(u−r) / scaled / 유효율 / 순위.
4. **홀드 스크린 표** (5동작): 동일 컬럼.
5. **r03 블라인드 재발견 verdict**: G1/G2/G3 각각 근거 수치. 참고 대조 — 렌더러 bodyViz
   실측(elbow r03 user 0.629 / ref 0.316, epy SUMMARY)과 홀드 스크린 bodyline 패널
   중앙값이 같은 방향·자릿수인지 (일치 강제 아님 — 측정 창이 다름을 명기: 렌더러는
   정지 순간 창, 스크린은 홀드 전 구간).
6. **v0 회귀 대조 표**: v0 인라인 실측치 vs 이 스크립트 실측치 (powerspin left_ankle
   +0.56 / 벌림각 −24.5°·u 27.7·r 98.1 / elbow ≤0.4x / kipup·peterpan 진폭 부족 /
   미러 5동측) + `--regress-v0` 판정.
7. **미러 판정** 5동작 (개선률 수치 포함).
8. **LLM 학습 기여 관점** (belle 08-08 "항상 언급" 지시): 이번 발견 목록을 라벨 후보로
   정리 — (a) 사람검증 완료: elbow 몸-폴(belle 승인 r03, 재발견 성공 시 "자율 스크린이
   사람 라벨을 재현" = 라벨 생성기 후보 근거) (b) 기계단독(미검증): powerspin
   left_ankle·벌림각, 홀드 구간 경계 자체(홀드/전환 구간 라벨), 기타 이번 실행에서 top
   에 오른 신규 신호. 각 항목에 어떤 감독 신호로 쓰일 수 있는지 1줄(결함 부위 라벨 /
   홀드 세그먼트 라벨 / 미러 정합 라벨).
9. **한계**: 파일럿 고정 카메라·수직 폴 가정(폴 캐시), x-투영 원리 한계(pdshape r01
   이력 인용), 5동작 표본, 홀드 파라미터 민감도 — 부록으로 HOLD_PCT 30/50 참고 수치
   허용(관찰만, verdict 는 p40 고정임을 명기).

무접촉 확인 후 커밋: `docs(quick-260808-im8): 자율 스크린 v1 실행 결과 — r03 재발견
verdict + v0 회귀 대조 박제` (RESULT.md + report.json).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && backend/.venv/bin/python -c "import json; r=json.load(open('.planning/quick/260808-im8-v1-r03/260808-im8-report.json')); assert 'verdict' in r and all(k in r['verdict'] for k in ('G1','G2','G3','overall')), r.get('verdict'); print('verdict:', r['verdict'])" && for s in "홀드 구간 실측" "전구간 스크린" "홀드 스크린" "재발견 verdict" "v0 회귀 대조" "미러 판정" "LLM 학습 기여" "한계"; do grep -q "$s" .planning/quick/260808-im8-v1-r03/260808-im8-RESULT.md || { echo "MISSING: $s"; exit 1; }; done && test -z "$(git diff HEAD -- backend/shared backend/scripts/render_compare_prototype.py)" && echo ALL-CHECKS-PASS</automated>
  </verify>
  <done>
report JSON 에 verdict(G1/G2/G3/overall) 존재, RESULT.md 에 9절 전부 존재(섹션 grep PASS),
verdict 가 표 수치와 정합(성공이든 실패든 근거 수치 병기), 채점·렌더러 무접촉
(`git diff HEAD` 빈 출력 — staged 포함 검사), 두 파일 커밋 완료. 이모지 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (없음) | 로컬 read-only 분석 스크립트 — 리포 내 JSON 읽기만, 네트워크 0, 신규 패키지 설치 0, 프로덕션 경로 무접촉 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-q-im8-01 | Tampering | 채점·렌더러 코드 | mitigate | Task 2 verify 의 `git diff HEAD -- backend/shared backend/scripts/render_compare_prototype.py` 빈 출력 게이트 (staged 포함 — `git add` 무력화 함정 회피) |
| T-q-im8-02 | Repudiation | verdict 수치 | mitigate | verdict 를 스크립트 기계 판정(--verdict-r03)으로 산출 + report JSON 원본 박제 — 서사로 성공을 꾸밀 경로 차단 |
</threat_model>

<verification>
- `backend/.venv/bin/python backend/scripts/p35_observe_screen.py --regress-v0` exit 0 (v0 재현).
- `--verdict-r03 --json-out ...` 이 G1/G2/G3/overall 을 기계 출력, report JSON 박제.
- RESULT.md 9절 grep 전건 존재, 수치는 report JSON 과 정합.
- 채점·렌더러 무접촉: `git diff HEAD -- backend/shared backend/scripts/render_compare_prototype.py` 빈 출력.
- 신규 파일 3개(스크립트+RESULT+report) 커밋 존재.
</verification>

<success_criteria>
- v0 알고리즘이 리포 스크립트로 영구화되고 v0 인라인 실측(powerspin left_ankle +0.56±0.05·
  벌림각 −24.5±3°·u 27.7/r 98.1±3, elbow 전 피처 scaled ≤0.5, 미러 5/5 동측)을 재현한다.
- 홀드 스크린이 5동작에서 동작하고(불가 동작은 정직 표기), elbow r03 재발견 여부가
  기본 파라미터 1회 기계 판정(G1∧G2∧G3)으로 기록된다 — 성공/실패 무관하게 수치 근거 병기.
- RESULT.md 가 LLM 학습 기여 절(사람검증/기계단독 구분)을 포함해 quick dir 에 박제된다.
- 채점 엔진·렌더러 diff 0.
</success_criteria>

<output>
`.planning/quick/260808-im8-v1-r03/260808-im8-SUMMARY.md` 작성 (실행 완료 시, summary 템플릿).
verdict 결과(성공/실패)와 편차·미검증 항목을 SUMMARY 에 그대로 — 실패의 산출물은 문서가
아니라 재시도이므로, G1 FAIL 시 SUMMARY 에 "v1 한계 + 다음 가설" 1줄 필수.
</output>
