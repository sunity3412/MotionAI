---
phase: quick-260808-r82
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/shared/python/sunity_shared/analysis/motiondtw.py
  - backend/shared/python/sunity_shared/analysis/side_match.py
  - backend/shared/python/sunity_shared/analysis/compare_align.py
  - backend/functions/pipeline/app.py
  - backend/tests/phase33/test_m3_alignment_only.py
  - backend/tests/phase34/__init__.py
  - backend/tests/phase34/test_measurement_window.py
  - backend/tests/phase34/test_side_match.py
  - backend/tests/phase35/test_compare_render_stage.py
  - backend/tests/phase35/test_align_quality_calibration.py
  - backend/scripts/phase34_rescore_harness.py
  - .planning/quick/260808-r82-phase-34-3/data/
  - .planning/quick/260808-r82-phase-34-3/POD-VERIFY.md
autonomous: true
requirements: [QUICK-260808-R82]

must_haves:
  truths:
    - "수술 ②: 방출된 angle_vs_reference record 의 측정 순간(대표 프레임)이 ref-경계 제외 구간의 정렬 스텝에서 나오지 않는다 — 7 doc 하네스 전건 + pdshapefault right_elbow(구 1.22s) 이동 관측 박제"
    - "self-compare(동일 각도 행렬) median 0 · 무감점 불변식이 제외 창 도입 후에도 pytest 로 고정"
    - "fail-open 불변식: 경계 제외 후 잔여 스텝이 하한 미만이면 종전 전-경로 산출과 동일 (극소 표본 급변 금지)"
    - "수술 ③: 그립측 일치(비미러) 입력은 스왑 미발동 = 종전 byte-동일. 판별 신뢰 미달 = 미발동(fail-closed)"
    - "수술 ①: 렌더 스테이지가 tier 프록시 대신 build_align 산출 자체 품질(align_quality)로 판정 — 승인 5편 align.json 전건 PASS, tier=trim_only doc 이라도 품질 PASS 면 부착 경로 생존"
    - "산식 상수(tol 20°/slope 1.2/cap −20·−40/floor)·deduction_engine.py·dimensions.py·kismam.py·compare_render.py·cue_text.py 무접촉 (git diff 0)"
    - "pytest 기준선 59 failed 대비 신규 실패 0 (FAILED/ERROR node-ID diff IDENTICAL)"
    - "7 doc 재채점 전/후 점수 이동 회계 표(관절별 편차 diff + 제외 스텝 수) 리포 커밋 — 이동 허용, 무근거 급변 감시"
    - "belle-FAIL 측(doc 127a2a90) 최종 검증은 Pod 스윕으로 명시 이월 — POD-VERIFY.md 절차 박제"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/motiondtw.py"
      provides: "수술 ② — ref_boundary_step_mask 공용 헬퍼 + per_joint_deviation/per_joint_representative_frames ref-경계 제외 창"
      contains: "REF_BOUNDARY_EXCLUDE_S"
    - path: "backend/shared/python/sunity_shared/analysis/side_match.py"
      provides: "수술 ③ — 그립측 판별 순수 함수 + 팔 L/R 열 스왑 (numpy-only)"
      exports: ["grip_side", "swap_lr_arm_columns"]
    - path: "backend/shared/python/sunity_shared/analysis/compare_align.py"
      provides: "수술 ① — align_quality(align) 순수 판정 함수 (build_align/select_pairs 본체 무변경)"
      exports: ["align_quality"]
    - path: "backend/scripts/phase34_rescore_harness.py"
      provides: "7 doc 로컬 재채점 하네스 — before/after diff 표 + 불변식 1 전건 검사"
    - path: "backend/tests/phase34/test_measurement_window.py"
      provides: "불변식 1·2·4 (측정 순간·self-compare·fail-open)"
    - path: "backend/tests/phase34/test_side_match.py"
      provides: "불변식 3 (비미러 byte-동일) + 판별/스왑 단위 성질"
    - path: ".planning/quick/260808-r82-phase-34-3/POD-VERIFY.md"
      provides: "belle-FAIL 측 캘리브레이션 Pod 검증 절차 (명시 이월)"
  key_links:
    - from: "backend/shared/python/sunity_shared/analysis/motiondtw.py"
      to: "backend/functions/pipeline/app.py"
      via: "per_joint_deviation/per_joint_representative_frames 소비처 4곳(2687/2707/4901 + _angles_to_dtw_median_dicts)이 동일 ref_fps 로 동일 마스크를 공유"
      pattern: "ref_fps"
    - from: "backend/functions/pipeline/app.py"
      to: "backend/shared/python/sunity_shared/analysis/side_match.py"
      via: "_deviation_against(swap_lr_arms=...) mode1 call site 배선"
      pattern: "swap_lr_arm"
    - from: "backend/functions/pipeline/app.py"
      to: "backend/shared/python/sunity_shared/analysis/compare_align.py"
      via: "_run_deferred_compare_render — build_align 성공 직후 align_quality 게이트"
      pattern: "align_quality"
    - from: "backend/shared/python/sunity_shared/analysis/motiondtw.py"
      to: "backend/shared/python/sunity_shared/analysis/compare_verify.py"
      via: "REF_BOUNDARY_EXCLUDE_S == REF_BOUNDARY_PIN_S lockstep assert (테스트로 단일 출처 강제, 채점 코어→리그 import 0)"
      pattern: "REF_BOUNDARY"
---

<objective>
Phase 34 수술 3건 (보드 착수 블록 승인분, belle 08-08 pdshape 반려 doc 127a2a90 후속):

- 수술 ② (최우선 — 반려의 뿌리): 측정창 = ref-경계 마진 제외. per_joint_deviation 의
  median 집계와 per_joint_representative_frames 의 표시 순간이 DTW 종점 강제 정렬
  아티팩트 구간(진입/이탈 국면)의 스텝을 소비하지 않게 한다. 렌더층 리그 G 핀
  (compare_verify.REF_BOUNDARY_PIN_S=0.5s)과 동일 원리의 채점층 대응.
- 수술 ③: 좌우 기능 짝맞춤. 미러 수행(그립팔 좌우 거울상) 시 팔 관절쌍만 열 스왑
  비교. 판별 신뢰 미달 = 미발동 fail-closed.
- 수술 ①: 렌더 스테이지의 tier 프록시 게이트(리포 7 doc 전부 trim_only → 렌더 부착 0,
  가치 0)를 제거하고 build_align 산출 자체의 품질 판정(align_quality)으로 교체.

무접촉 봉인: 산식·tol(20°)·slope(1.2)·cap(−20/−40)·baseline·deduction_engine.py·
dimensions.py·kismam.py·compare_render.py·cue_text.py. 동작명 분기 0, fixture별 임계 0.

Purpose: 완료 정의 = "belle 이 아무 영상을 올려도 리그 선판정 v7급". 채점이 이탈 국면을
재고(17.56/17.78s record), 표시가 자세 완성 전(1.22s)을 가리키는 구조를 뿌리에서 수리.
Output: 수술 3건 코드 + 불변식 스위트(Phase 34 겹 1의 시작) + 7 doc 재채점 회계 표 +
Pod 검증 절차 문서.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@backend/shared/python/sunity_shared/analysis/motiondtw.py
@backend/shared/python/sunity_shared/analysis/compare_align.py
@backend/shared/python/sunity_shared/analysis/compare_verify.py   # 60-106행 G 핀 절만
@backend/shared/python/sunity_shared/analysis/skeleton.py
@backend/tests/phase33/test_m3_alignment_only.py
@.planning/phases/35-server-rendered-comparison-video/data/README.md

# app.py 는 거대 — 전체 Read 금지. 아래 앵커 구간만 Read(offset/limit):
#   2600-2760  _build_deduction_measured_deviations 의 DTW fallback + 대표 프레임 배선
#   4006-4130  _run_deferred_compare_render 게이트 + build_align 호출부
#   4880-4903  _deviation_against 공용 코어
#   5090-5130  mode3 second+ call site
#   5985-6030  mode1 call site (ref dict·reference_kp_fps 스코프)
# _angles_to_dtw_median_dicts 정의부는 grep 으로 위치 확인 후 그 구간만 Read.
</context>

<tasks>

<task type="auto">
  <name>Task 1: 재채점 하네스 + 수술 ② 측정창(ref-경계 마진 제외) + 불변식 1·2·4</name>
  <files>backend/scripts/phase34_rescore_harness.py, backend/shared/python/sunity_shared/analysis/motiondtw.py, backend/functions/pipeline/app.py, backend/tests/phase33/test_m3_alignment_only.py, backend/tests/phase34/__init__.py, backend/tests/phase34/test_measurement_window.py, .planning/quick/260808-r82-phase-34-3/data/</files>
  <action>
**Step 0 — pytest 기준선 캡처 (수술 전, 필수 선행).**
`PYTHONPATH=backend/tests backend/.venv/bin/python -m pytest -q backend/tests 2>&1 | tail -5`
를 실행하고 전체 출력에서 FAILED/ERROR node-ID 목록을
`.planning/quick/260808-r82-phase-34-3/data/pytest_baseline.txt` 로 저장 (기대: 59 failed
계열 — 기존 기준선. `git diff --exit-code` 는 게이트로 쓰지 말 것, add 로 무력화 이력).

**Step 1 — 기준 각도 캐시 (1회 조회, 재현성 박제).**
`backend/scripts/phase34_rescore_harness.py` 신설. `--fetch` 서브커맨드:
7 doc(`.planning/phases/35-server-rendered-comparison-video/data/{elbow,powerspin,kipup,pdshapefault,peterpan,pdshape,realupload}/doc.json`)
의 top-level `referenceMotionId` 를 수집(중복 제거 — ref-elbow-twist-sister/ref-power-spin/
ref-kip-up/ref-pdshape/ref-peter-pan 예상)하고 Firestore `reference` 컬렉션에서 해당 doc 의
top-level `angles`(flat)·`anglesJointKeys`·`anglesFrames`·`keypointReport.fps`·`joints3d`·
`joints3dKeys`(Wave 5 top-level mirror 11필드 — 존재를 assert, 없으면 그 필드만 결측 기록)를
1회 조회해 `.planning/quick/260808-r82-phase-34-3/data/reference_angles/{motionId}.json` 으로
캐시(커밋 대상). 인증 = repo 루트 `firebase-sa.json` 서비스 계정(firebase_admin — backend/.venv
에 없으면 venv 에 pip 설치, requirements 파일 무변경). Firestore 컬렉션 경로는
`backend/shared/python/sunity_shared/firestore_admin.py` 의 reference 조회 코드를 grep 해
실제 경로를 따른다 (추측 금지).

**Step 2 — 하네스 본체 (before 기록).**
`--run` 서브커맨드: 각 doc 에 대해 top-level `angles`(flat, anglesFrames×8) reshape →
캐시된 ref angles reshape → `_deviation_against` 와 동일 배선으로 재채점:
`motion_dtw(feature_vector(user), feature_vector(ref))` → `per_joint_deviation(path,
user_seg, a_ref_win)` + `per_joint_representative_frames(...)`. import 는
`backend/tests/conftest.py` 가 쓰는 sys.path 방식 재사용(sunity_shared 직접 import —
pipeline app.py import 불요, 순수 계층만). 산출(JSON): doc×관절별
`{deviation, rep_frame, rep_video_sec(frame/9.0), n_path}` + doc 의 기존 records
(recordId/criterion/atVideoSec) 대조. mode1 ref_boundary 는 하네스에선 None(콤보 아님 —
7 doc 전부 단일 기술, doc 의 referenceMotionId 로 확인). **수술 전 HEAD 에서 실행**해
`data/rescore_before.json` 저장. 첫 원자 커밋: `feat(34): 재채점 하네스 + 기준 각도 캐시 + before 기록`.

**Step 3 — 수술 ② motiondtw.py.**
(a) 모듈 상수 + 공용 헬퍼:
```
REF_BOUNDARY_EXCLUDE_S = 0.5
def ref_boundary_step_mask(path, ref_len, ref_fps): -> np.ndarray[bool]  # True=집계 사용
```
스텝 (u,r) 에서 r < margin 또는 r >= ref_len − margin 이면 False.
margin = int(np.ceil(REF_BOUNDARY_EXCLUDE_S * ref_fps)) (9fps→5프레임, 18fps→9프레임 —
초→프레임 환산을 docstring 에 명시). why-주석: 렌더층 G 핀
compare_verify.REF_BOUNDARY_PIN_S(0.5s, "DTW 종점 강제 정렬 아티팩트")과 동일 원리의
채점층 대응 — belle doc 127a2a90 실측(records 2건 atVideoSec 17.56/17.78s = 이탈 국면,
짝 ref 16.4~16.6/16s = 종점 아티팩트) 인용. 값은 compare_verify 와 lockstep(단일 출처) —
채점 코어가 리그 모듈을 import 하지 않도록(계층 방향: 코어→리그 금지) 상수를 여기 두고
**테스트에서 동등 assert 로 강제**한다(Step 5).
(b) `per_joint_deviation(path, A_user_seg, A_ref, *, ref_fps: float | None = None)`:
ref_fps=None → 종전 byte-동일(제외 0 — 기존 호출·테스트 보존). ref_fps 지정 시 마스크
적용해 살아남은 스텝만으로 diffs 구축. **fail-open 불변식**: 잔여 스텝 <
max(3, ceil(0.5 * len(path))) 이면 전 경로 사용(종전과 동일 산출). 근거 주석: 잔여가
과반 미달 = 정렬이 경계에 뭉갠 것 — 수술 ①·리그 소관이고 채점이 극소 표본으로 급변하면
안 됨. 3 = 의미 있는 median 최소 표본(홀수 중심). fixture 유도 아님(구조 유도) 명시.
(c) `per_joint_representative_frames(..., *, ref_fps: float | None = None)`: sibling
순회에 **동일 마스크·동일 fail-open** 복제(기존 sibling 패턴 유지 — docstring 의 SHA 박제
사유 절은 핀 갱신 사실로 갱신). 마스크로 제외된 스텝은 대표 프레임 후보에서 inf 로
밀어냄 — 표시 순간이 제외 구간 스텝에서 절대 나오지 않게.
(d) self-compare: 두 분기 모두 |Δ|=0 이므로 median 0 보장 구조 유지(테스트로 고정).

**Step 4 — 소비처 배선 (한 곳 수정이 전 소비자에 미치는지 추적).**
`grep -n "per_joint_deviation\|per_joint_representative_frames\|np.median" backend/functions/pipeline/app.py`
로 전 소비처 확정 후:
- `_deviation_against`(4880): `ref_fps: float | None = None` param 추가 → 4901 호출에 전달.
- mode1 call site(6002 부근): `reference_kp_fps` 읽기(현 6009)를 `_deviation_against`
  호출 **앞으로 이동**해 `ref_fps=reference_kp_fps or None` 전달(0.0 → None = 제외 미적용
  fail-open, log.info 1줄). 28-01 실측: 11 ref 전부 18.0.
- mode3 second+ call site(5102 부근): `ref_fps=9.0` 상수 + 주석(frame_extractor 9fps 축 —
  이전 영상도 같은 파이프라인 산출).
- `_build_deduction_measured_deviations`(2687 dev / 2707 _reps): 같은 ref_fps 를 param 으로
  스레딩(호출부 grep 으로 caller 추적해 mode1 값 전달) — 점수 경로와 순간 경로가 다른
  창을 쓰면 "쟀다" 계약이 깨진다는 주석.
- `_angles_to_dtw_median_dicts`(정의부 grep — 1756-1779 주석 근방): 내부가
  per_joint_deviation 호출이면 ref_fps 전달, 자체 np.median 순회면 `ref_boundary_step_mask`
  동일 헬퍼 적용(표시 median = 점수 median 동일 창 — Phase 19 TRUST-01 표시·점수 source
  통일 원칙 인용). 6017 호출부에 ref_fps 스레딩.
- measurement_error.per_joint_median_ci(2694)는 신뢰구간 보조 지표 — 이번 수술 범위 밖
  (무접촉, 주석으로 창 불일치 알려짐 박제) 또는 같은 마스크 적용이 1줄이면 적용. 산식
  모듈이 아니므로 적용 권장하되 diff 최소 우선.

**Step 5 — M3 SHA 핀 갱신 + 불변식 테스트.**
`backend/tests/phase33/test_m3_alignment_only.py` 39행 `PER_JOINT_DEVIATION_SHA256` 을 새
본문 해시로 갱신 + 주석: "Phase 34 수술 ② (quick-260808-r82, 보드 착수 블록 승인) —
ref-경계 마진 제외 추가. tol 20°/slope 1.2 불변 assert 는 그대로 유지". I1/I2 는
ref_fps 미지정 경로라 무변경 통과해야 함.
`backend/tests/phase34/__init__.py` + `test_measurement_window.py` 신설 (fixture 기대점수
curve-fit 금지 — fixture-무관 합성 입력):
- 불변식 2: 동일 각도 행렬 self-compare, ref_fps=9.0 → median 전관절 0.
- 불변식 4: 경계 제외로 잔여 < 하한이 되는 짧은 합성 입력 → ref_fps=9.0 산출 ==
  ref_fps=None 산출 (np.array_equal).
- 마스크 성질: 경계 스텝만 False, margin 환산(9→5, 18→9), 대표 프레임이 제외 스텝의
  user 프레임 집합에서 나오지 않음(경계에만 큰 편차를 심은 합성 입력으로 — 종전이면
  경계를 고르고 수술 후엔 내부를 고르는 형상).
- lockstep: `from sunity_shared.analysis import compare_verify` 후
  `motiondtw.REF_BOUNDARY_EXCLUDE_S == compare_verify.REF_BOUNDARY_PIN_S` assert
  (상수 중복 정의의 단일 출처 강제 — 리그 쪽이 바뀌면 여기가 트립).

**Step 6 — after 기록 + 회계 표.**
하네스 `--run` 재실행(ref_fps: mode1 은 캐시된 keypointReport.fps, 없으면 None) →
`data/rescore_after.json` + `--diff` 로 `data/rescore_diff.md` 생성: doc×관절
`dev_before/dev_after/Δ/n_path/n_excluded/n_used/rep_frame before→after(video_sec)` 표 +
**불변식 1 전건 검사**(after 대표 프레임이 제외 스텝 user 프레임 집합 밖 — 위반 시 exit 1).
pdshapefault right_elbow(구 1.22s)·pdshape 계열의 이동을 표에서 관측 박제(기대치 박제
금지 — 관측만). 둘째 원자 커밋: `feat(34): 수술 ② 측정창 ref-경계 제외 + 불변식 + M3 핀 갱신 + 회계 표`.
  </action>
  <verify>
    <automated>PYTHONPATH=backend/tests backend/.venv/bin/python -m pytest -q backend/tests/phase34/test_measurement_window.py backend/tests/phase33/test_m3_alignment_only.py backend/tests/test_motiondtw.py backend/tests/test_record_measured_at.py && backend/.venv/bin/python backend/scripts/phase34_rescore_harness.py --run --diff --data .planning/phases/35-server-rendered-comparison-video/data --out .planning/quick/260808-r82-phase-34-3/data</automated>
  </verify>
  <done>불변식 1(하네스 exit 0)·2·4 + 마스크 성질 + lockstep PASS. M3 I1~I5 PASS(핀 갱신 반영). rescore_before/after/diff 3파일 커밋. deduction_engine.py·dimensions.py·kismam.py diff 0.</done>
</task>

<task type="auto">
  <name>Task 2: 수술 ③ 좌우 기능 짝맞춤(side_match) + 불변식 3</name>
  <files>backend/shared/python/sunity_shared/analysis/side_match.py, backend/functions/pipeline/app.py, backend/tests/phase34/test_side_match.py, backend/scripts/phase34_rescore_harness.py</files>
  <action>
**`side_match.py` 신설 (numpy-only 순수 모듈 — 네트워크/영상/모델 금지).**

(a) `grip_side(joints3d, joint_keys, confidence=None) -> "left" | "right" | None`:
(T,17,3) 위치 배열(또는 flat+keys reshape)에서 left_wrist/right_wrist 궤적의 공간 분산
비교. 그립 손목 = 폴을 잡아 정지(잔여 이동 = 몸통 흔들림 스케일) vs 자유 손목 = 팔
길이 스케일 스윕 → 표준편차 비 분리로 판별. 상수 `GRIP_STD_RATIO_MIN = 2.0`(std 비 —
분산 4배): 구조 유도 — 고정점(폴 접점) 대 스윕 말단의 운동 스케일이 한 자릿수 배수로
갈린다는 역학 구조에서 유도, fixture curve-fit 아님(주석 박제 + 하네스 관측 표가 증거
의무). 판별식: std_min * GRIP_STD_RATIO_MIN <= std_max 일 때만 작은 쪽을 그립으로 반환,
미달 = None. NaN-aware(nanstd), confidence 주어지면 conf >= 0.5 프레임만(fault-zoom
마커 게이트와 동일 임계 인용), 유효 프레임이 어느 손목이든 max(9, T//4) 미만이면 None
(9프레임 = 9fps 1초 — 1초 미만 관측으로 그립 판정 금지, 구조 유도).
(b) `swap_lr_arm_columns(A, joint_keys) -> np.ndarray`: (T,J) 각도 행렬의
(left_elbow,right_elbow)·(left_shoulder,right_shoulder) 열만 교환(skeleton.JOINT_KEYS
의 L/R 짝). 다리(hip/knee) 무접촉 = 기본. 모듈 docstring 에 박제: **스크린 v0 실측 —
전신 미러 스왑은 위치 특징 공간에서 5동작 전부 미개선으로 기각. 단 v0 은 위치 공간이었고
이 스왑은 각도 공간의 국소(팔) L/R 열 교환 — 다른 연산**(memory
mirror-performance-side-matching-phase34 인용). 근거 실측: 엘보 미러 수행 — user
오른손목-폴 x간격 0.20 vs ref 왼손목-폴 0.20 (거울상).

**배선 (mode1 만 — 최소 폭발반경).**
- `_deviation_against(..., swap_lr_arms: bool = False)`: reshape 직후 True 면
  `a_ref = side_match.swap_lr_arm_columns(a_ref, JOINT_KEYS)` — feature_vector/DTW/편차/
  반환 a_ref 전부 스왑본 사용 → 하류 소비(6012 reference_angles_for_veto, 6017 표시
  median, 2687 seed)가 자동 일관(표시=점수 동일 source, TRUST-01). False 기본 =
  byte-동일.
- mode1 call site(6002 앞): user 그립 = 파이프라인이 보유한 user (T,17,3)(`grep -n
  "joints3d" backend/functions/pipeline/app.py` 로 result.joints3d 를 만드는 원천 변수
  추적 — 추측 금지, confidence 있으면 전달) / ref 그립 = ref doc top-level
  `joints3d`+`joints3dKeys`(Task 1 캐시로 존재 실측 확인된 필드 — 프로덕션은 Firestore
  ref dict 에서 직접). 둘 다 non-None 이고 다를 때만 `swap_lr_arms=True` +
  `log.info("side_match 발동 user_grip=%s ref_grip=%s ...")`. 어느 쪽이든 None(판별
  불가·필드 결측) = 미발동 fail-closed. record 계약 무변경(3-way lockstep 회피 — 관측은
  log 만).
- mode3 call site(5102) 무접촉 + 주석(같은 사용자 이전 영상 비교 — 미러 판별은 이번 범위
  밖, Phase 34 후속).

**불변식 3 + 단위 테스트 `test_side_match.py`** (합성 입력만):
- 정지 왼손목 + 스윕 오른손목 → "left". 대칭 → "right". 비슷한 분산 → None.
  NaN 과다/짧은 관측 → None.
- swap involution: swap(swap(A)) == A (np.array_equal). 다리 열 불변.
- 불변식 3: `_deviation_against` 급 배선을 순수 계층에서 재현 — 그립측 일치(스왑
  미발동) 경로의 per_joint_deviation 산출이 side_match 도입 전과 np.array_equal
  (swap_lr_arms=False 기본값 byte-동일을 직접 assert).

**하네스 확장 + 회계.**
`--side` 옵션: 7 doc × user/ref 그립 판별 결과(std 비·유효 프레임 수 관측 표 — 증거
의무) + 발동 doc 의 팔만 스왑 vs 팔+다리 스왑 편차 비교 표. **다리 포함 여부는 이 실측으로
결정**(기본 = 팔만. 다리 확장은 발동 doc 에서 다리 편차가 일관 개선될 때만 — 결과와
근거를 SUMMARY 에 박제, 개선 없으면 팔만 유지). elbow doc(실측 거울상)이 발동 후보,
비미러 doc 은 미발동 = before 대비 byte-동일이 표에서 보여야 함.
원자 커밋 1~2개: `feat(34): 수술 ③ 좌우 기능 짝맞춤 side_match + 불변식 3`.
  </action>
  <verify>
    <automated>PYTHONPATH=backend/tests backend/.venv/bin/python -m pytest -q backend/tests/phase34/ && backend/.venv/bin/python backend/scripts/phase34_rescore_harness.py --run --diff --side --data .planning/phases/35-server-rendered-comparison-video/data --out .planning/quick/260808-r82-phase-34-3/data</automated>
  </verify>
  <done>판별/스왑 단위 성질 + 불변식 3 PASS. 하네스 side 표(그립 판별 관측치 + 발동/미발동 + 팔vs팔다리 비교) 커밋. 비미러 doc 산출 byte-동일 관측. mode3 경로·record 계약 무변경.</done>
</task>

<task type="auto">
  <name>Task 3: 수술 ① 렌더측 정렬 품질 게이트(align_quality) + 캘리브레이션 + Pod 절차 박제</name>
  <files>backend/shared/python/sunity_shared/analysis/compare_align.py, backend/functions/pipeline/app.py, backend/tests/phase35/test_compare_render_stage.py, backend/tests/phase35/test_align_quality_calibration.py, .planning/quick/260808-r82-phase-34-3/POD-VERIFY.md</files>
  <action>
**`compare_align.align_quality(align: dict) -> tuple[bool, list[str]]` 신설** —
build_align 산출 dict 만 소비하는 순수 함수(리그 verify 라인 형식 `"지표=값 임계=값
PASS|FAIL"` 재사용). **build_align/select_pairs/pose_feature 본체 무변경**(승인 렌더
재현성 — pairs 선정 로직 byte 불변).

지표 2축 (보드 명시):
- 신뢰 커버리지: `userScore`/`refScore`(T,17) → BODY12 열 → 전 (프레임×관절) 중
  conf >= CONF_MIN(0.3, 기존 상수 재사용) 비율. user·ref 각각 `COVERAGE_MIN` 이상.
- 자세거리 프로파일: `userKp`/`refKp` reshape (T,17,2) → 기존 `pose_feature` 재사용
  (단일 출처) → 곡선 따라 d_t = ||fu[t] − fr[clip(round(curveRefSec[t]*fps))]|| →
  median <= `POSE_DIST_MED_MAX` ∧ p85 <= `POSE_DIST_P85_MAX`.

**캘리브레이션 (PASS 측 — 로컬 가능 범위).** 승인 5편
`.planning/phases/35-server-rendered-comparison-video/data/{elbow,kipup,pdshapefault,peterpan,powerspin}/align.json`
(GPU 산출 실물)에서 3지표 실측 → 임계 = 승인 코퍼스 최악값 대비 구조적 마진(커버리지:
승인 최솟값에서 여유 하향 / 자세거리: 승인 최댓값에서 여유 상향 — 배수·근거를 상수
주석에 박제, "임계가 5편 유도"라는 일반화 한계도 명시). belle-FAIL 측은 로컬 불가:
belle 08-08 영상의 render-side align 은 GPU 에서만 생산(CPU align ≠ GPU align 실측 —
E 13% vs 28%) → **Pod 스윕 명시 이월**.

**게이트 교체 (app.py `_run_deferred_compare_render`).**
- 4022-4036 tier 프록시 블록(`_ma`/`_ma_tier != "warped"` skip) 삭제 — 근거 주석 이관:
  리포 7 doc 전부 tier=trim_only(승인 픽스처 포함) 실측 → 렌더 부착 0, 프록시 가치 0.
- build_align 성공 + align.json 진단 기록(~4081) 직후, doc_like 조립(4085) **앞**에:
  `ok, qlines = compare_align.align_quality(align)`; FAIL → `log.info`(qlines 포함) +
  return (**필드 미기록 스킵** — 방어 2와 같은 UX = 앱 듀얼 플레이어 폴백, failed 마킹
  아님). PASS → 진행. docstring 의 "방어 2" 절도 새 게이트 서술로 갱신.

**테스트 갱신 (`backend/tests/phase35/test_compare_render_stage.py`).**
- `test_gate_alignment_tier_skips_non_warped`(188-211, 4형상 parametrize) 삭제 → 대체:
  (a) align_quality FAIL 형상(저신뢰/원거리 스텁 align — 기존 stage_env 의 build_align
  스텁 경로 재사용) → 필드 미기록 스킵 + doc 무접촉, (b) **tier=trim_only doc 이라도
  품질 PASS align 이면 부착 경로 진행**(가치 복원 핵심 — 성공 픽스처의
  `"motionAlignment": {"tier": "warped"}`(139행)를 trim_only 로 바꿔도 성공하는 형상).
  기존 성공/실패/rig 테스트는 통과 유지.
- `backend/tests/phase35/test_align_quality_calibration.py` 신설: repo-relative 로 승인
  5편 align.json glob(부재 시 pytest.skip) → 전건 align_quality PASS assert + 합성 FAIL
  2형상(전 관절 conf 0 → 커버리지 FAIL / 무관 좌표 → 자세거리 FAIL). 구버전 포맷 벤치
  슬롯(pdshape/realupload — refKp 없음)은 대상 외(README 실측: 운영 스테이지는 항상
  신포맷 생산).

**렌더러 byte 불변 게이트**: `git diff --stat HEAD -- backend/shared/python/sunity_shared/analysis/compare_render.py backend/shared/python/sunity_shared/analysis/cue_text.py`
= 0줄 (렌더 재실행은 범위 밖 — 무접촉이면 diff 0 로 갈음, 보드 승인).

**`POD-VERIFY.md` 박제 (belle-FAIL 측 최종 검증 — 명시 이월).** 내용: (1) 다음 Pod
부트스트랩(memory 절차, GPU 명시) 후 리포 pull, (2) belle doc 127a2a90c1d74c62ad61270eb3fe5625
의 myVideoKey(S3 uploads/{uid}/...)와 ref 영상 다운로드, (3) GPU 로
`compare_align.build_align` 실행(records 는 그 doc 의 deductionBreakdown.records), (4)
`align_quality` → **기대 FAIL**(반려 실측 근거: records 이탈 국면 + 종점 아티팩트 짝 +
low_global_confidence), (5) PASS 로 나오면 임계 재캘리브레이션 라운드(승인 5편 PASS
유지 조건 하에서만 조정)를 열고 결과를 이 파일에 추기, (6) CPU 대체 금지 근거(E 13% vs
28% 실측) 재인용. 원자 커밋 1~2개: `feat(34): 수술 ① align_quality 게이트 교체 + 캘리브레이션 + Pod 절차`.
  </action>
  <verify>
    <automated>PYTHONPATH=backend/tests backend/.venv/bin/python -m pytest -q backend/tests/phase35/test_compare_render_stage.py backend/tests/phase35/test_align_quality_calibration.py && git diff --stat HEAD -- backend/shared/python/sunity_shared/analysis/compare_render.py backend/shared/python/sunity_shared/analysis/cue_text.py | wc -l | grep -qx 0</automated>
  </verify>
  <done>새 게이트 형상(FAIL 스킵·trim_only+양품 부착) + 승인 5편 캘리브레이션 전건 PASS. 렌더러 2파일 diff 0. build_align/select_pairs 본체 diff 없음(compare_align.py diff 가 함수 추가·상수만). POD-VERIFY.md 커밋.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firestore ref doc → 채점 코어 | 캐시/조회된 기준 각도·joints3d 를 numpy reshape (형상 assert 로 방어, 기존 _as_tj 스타일) |
| align dict → 게이트 판정 | build_align 산출만 소비하는 순수 함수 — 외부 입력 표면 신설 없음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Tampering | 결측/기형 ref 필드(joints3d·fps) | mitigate | fail-closed(스왑 미발동)·fail-open(제외 미적용) + log — 채점 급변 금지 불변식이 방어 |
| T-34-02 | DoS | align_quality FAIL 남발 → 렌더 부착 소멸 | mitigate | 승인 5편 전건 PASS 캘리브레이션 게이트가 릴리스 조건, FAIL 사유 log 라인 |
| T-34-SC | Tampering | 신규 패키지 설치 | accept | 신규 의존 0 (firebase-admin 은 기존 프로젝트 의존의 로컬 venv 반영뿐, requirements 무변경) |
</threat_model>

<verification>
전체 게이트 (SUMMARY 에 "동사 = 재봤다" 실측 표로 각 행위 명기):

1. 전체 pytest 기준선 diff: `PYTHONPATH=backend/tests backend/.venv/bin/python -m pytest -q backend/tests`
   → FAILED/ERROR node-ID 집합이 `data/pytest_baseline.txt`(Step 0)와 IDENTICAL (신규 실패 0).
   `diff` 명령 결과를 SUMMARY 에 박제.
2. 산식 무접촉: `git diff --stat HEAD~N -- backend/shared/python/sunity_shared/analysis/deduction_engine.py backend/shared/python/sunity_shared/analysis/dimensions.py backend/shared/python/sunity_shared/analysis/kismam.py backend/shared/python/sunity_shared/analysis/compare_render.py backend/shared/python/sunity_shared/analysis/cue_text.py` = 0줄.
3. 하네스 회계 표(rescore_diff.md)를 **직접 열어** pdshapefault right_elbow 이동·비미러 byte-동일·
   제외 스텝 수를 눈으로 확인(산출물 직접 열기 원칙).
4. 금지 스캔: 신규/변경 파일에 이모지 0, 동작명 분기 0(`grep -n "kip\|elbow\|pdshape\|peterpan\|powerspin"` 이
   프로덕션 신규 코드에 0건 — 주석 실측 인용 제외), fixture별 임계 0.
5. 미검증 항목 박제: belle-FAIL 측 align_quality(Pod 필요)·실 doc 재분석(Pod 없음 상태)은
   "안 재봤다 + 이유 + POD-VERIFY.md 절차" 로 SUMMARY 에 명시.
</verification>

<success_criteria>
- 수술 ②③① 코드 + 불변식 스위트(backend/tests/phase34/ 신설 = Phase 34 겹 1 시작) 커밋.
- must_haves.truths 전건 성립: 측정 순간 제외구간 밖(하네스 전건)·self-compare 0·fail-open·
  비미러 byte-동일·승인 5편 align PASS·산식 diff 0·pytest 신규 실패 0.
- M3 SHA 핀 갱신에 사유 주석(Phase 34 수술 ②, 보드 착수 블록 승인) 박제.
- 7 doc 회계 표 + side 판별 관측 표 + POD-VERIFY.md 리포 커밋(scratchpad 휘발 금지 원칙).
- 커밋은 feat(34): 원자 단위 5~7개.
</success_criteria>

<output>
.planning/quick/260808-r82-phase-34-3/260808-r82-SUMMARY.md 생성 — 실측 표(각 게이트에 실행한
검증 행위 동사 명기), 점수 이동 회계 요약, 미검증 항목(이유 포함), Pod 이월 절차 링크.
</output>
