---
phase: quick-260808-r82
plan: 01
subsystem: ml-scoring
tags: [dtw, motiondtw, side-match, align-quality, phase34, measurement-window]

requires:
  - phase: 33-result-trust-recovery
    provides: M3 정렬 substrate(paired window)·감점 측정 순간(measured_at)·SHA 핀 게이트
  - phase: 35-server-rendered-comparison-video
    provides: compare_align/compare_verify 리그 G 핀(REF_BOUNDARY_PIN_S)·승인 5편 align.json
provides:
  - 수술 ② 측정창 ref-경계 마진 제외 (motiondtw.REF_BOUNDARY_EXCLUDE_S=0.5s, ref_fps opt-in + fail-open)
  - 수술 ③ side_match — 라운드 2 에서 스왑 채점 실측 기각·무장해제 (그립측 판별 = 관측 로그 전용, data/swap_hypothesis.md)
  - 수술 ① align_quality 게이트 (tier 프록시 삭제 → build_align 산출 자체 품질 판정)
  - Phase 34 불변식 스위트 시작 (backend/tests/phase34/ — 20 테스트)
  - 7 doc 재채점 하네스 + before/after 회계 표 + Pod 검증 절차
affects: [phase-34, phase-35, pipeline, compare-render, pod-verify]

tech-stack:
  added: []
  patterns:
    - "ref-경계 제외 창: 채점층 상수를 코어에 두고 리그 상수와 테스트 lockstep assert 로 단일 출처 강제 (코어→리그 import 금지 계층 유지)"
    - "opt-in kwarg(ref_fps/swap_lr_arms) + None/False 기본 = byte-동일 — 기존 호출·테스트 전건 무회귀"
    - "게이트는 프록시가 아니라 판정 대상 산출물 자체를 본다 (tier→align_quality 교체 근거)"

key-files:
  created:
    - backend/shared/python/sunity_shared/analysis/side_match.py
    - backend/scripts/phase34_rescore_harness.py
    - backend/tests/phase34/test_measurement_window.py
    - backend/tests/phase34/test_side_match.py
    - backend/tests/phase35/test_align_quality_calibration.py
    - .planning/quick/260808-r82-phase-34-3/POD-VERIFY.md
    - .planning/quick/260808-r82-phase-34-3/data/ (기준 각도 캐시 5건 + rescore before/after/diff + side 표 + pytest 기준선)
  modified:
    - backend/shared/python/sunity_shared/analysis/motiondtw.py
    - backend/shared/python/sunity_shared/analysis/compare_align.py
    - backend/functions/pipeline/app.py
    - backend/tests/phase33/test_m3_alignment_only.py
    - backend/tests/phase35/test_compare_render_stage.py

key-decisions:
  - "fail-open 하한 = max(3, ceil(0.5*len(path))) — 잔여 과반 미달이면 전 경로 유지 (극소 표본 급변 금지, 구조 유도)"
  - "align_quality 임계 = 승인 5편 최악값의 2.0배 마진 (커버리지 0.88 / d_med 6.3 / d_p85 11.0) — 5편 유도 한계 명시 + belle-FAIL 측은 Pod 이월"
  - "side_match 다리 확장 보류 — 발동 실측 0건이라 근거 없음, 팔만 유지 (기본값)"
  - "mode3 ref_fps 는 리터럴 9.0 대신 _pipeline_frame_fps() 단일 출처 (app.py 기존 규율)"

patterns-established:
  - "측정창 원칙: 점수 median·표시 median·대표 프레임(순간)이 항상 같은 스텝 집합을 소비한다 (TRUST-01 확장)"

requirements-completed: [QUICK-260808-R82]

duration: 35min
completed: 2026-08-08
---

# quick-260808-r82: Phase 34 수술 3건 Summary

**측정창 ref-경계 0.5s 제외(DTW 종점 아티팩트 차단) + 그립측 판별 팔 스왑(fail-closed) + 렌더 게이트 tier 프록시→align_quality 교체 — 산식 무접촉, pytest 기준선 IDENTICAL**

## Performance

- **Duration:** 35min
- **Started:** 2026-08-08T10:54:17Z
- **Completed:** 2026-08-08T11:29:55Z
- **Tasks:** 3
- **Files modified:** 5 modified + 6 created (+ data 아티팩트 10)

## Accomplishments

- **수술 ② (반려의 뿌리):** `per_joint_deviation`/`per_joint_representative_frames`/`_angles_to_dtw_median_dicts` 가 ref-경계 마진(0.5s) 스텝을 집계·순간 선택에서 제외 — 점수/표시/순간이 동일 창. belle doc 127a2a90 유형(이탈 국면 측정·종점 아티팩트 짝)의 채점층 대응.
- **수술 ③:** `side_match.py` — 손목 궤적 std 비(>=2.0 구조 유도) 그립 판별 + 팔 관절쌍만 L/R 열 스왑. 판별 미달 = 미발동 fail-closed. mode1 만 배선, record 계약 무변경(log 관측만).
- **수술 ①:** 렌더 스테이지 tier 프록시 게이트(7 doc 전부 trim_only → 부착 0 = 가치 0) 삭제 → `compare_align.align_quality` (커버리지 + 곡선-자세거리, 승인 5편 캘리브레이션). trim_only doc 이라도 품질 PASS 면 부착 경로 생존.
- **회계:** 7 doc 재채점 하네스 — before 가 doc records 를 정확 재현(배선 검증), after 에서 측정 순간 이동 관측 박제.

## Task Commits

1. **Task 1a: 재채점 하네스 + 기준 각도 캐시 + before 기록** - `630907c7` (feat)
2. **Task 1b: 수술 ② 측정창 ref-경계 제외 + 불변식 + M3 핀 갱신 + 회계 표** - `fd4674e6` (feat)
3. **Task 2: 수술 ③ side_match + 불변식 3** - `a04ba850` (feat)
4. **Task 3: 수술 ① align_quality 게이트 교체 + 캘리브레이션 + Pod 절차** - `5e139cf0` (feat)

## 실측 표 (게이트별 검증 행위 — 동사 명기)

| # | 게이트 | 행위 (동사) | 결과 |
|---|---|---|---|
| 1 | pytest 기준선 diff | 수술 전/후 전체 스위트를 **돌렸고** FAILED/ERROR node-ID 를 `diff` 로 **대조했다** | IDENTICAL — 59 failed 불변, passed 4027→4050 (+23 신규 전건 통과) |
| 2 | 산식 무접촉 | `git diff --stat HEAD~4` 5파일(deduction_engine/dimensions/kismam/compare_render/cue_text)을 **재봤다** | 0줄 |
| 3 | 회계 표 | `rescore_diff.md` 를 **직접 열어 읽었다** | pdshapefault right_elbow 측정 순간 1.22s→9.56s 이동, 7 doc 전건 마스크 발동(fail_open 0, 제외 18~29스텝), 편차 이동 최대 5.84° (peterpan right_elbow 23.25→17.41) |
| 4 | 하네스 불변식 1 | `--run --diff` 를 **실행했다** (exit 0) | after 대표 프레임 전건이 제외-전용 스텝 프레임 집합 밖 |
| 5 | before 배선 검증 | before rep_sec 를 doc records atVideoSec 와 **대조했다** | pdshapefault 4 record 전건 일치 (1.2222/8.5556/3.2222/3.6667s) — 하네스 = 프로덕션 동일 배선 실증 |
| 6 | side 관측 표 | `side_report.md` 를 **직접 열어 읽었다** | 발동 0건 — 비미러 4 doc(powerspin/kipup/peterpan/realupload) 그립 일치 미발동, elbow/pdshape 계열 ratio 1.14~1.25 판별 미달 fail-closed |
| 7 | 캘리브레이션 실측 | 승인 5편 align.json 지표를 **계산했다** | 최악 ucov 0.9468 / rcov 0.9441 / d_med 3.125 / d_p85 5.490 → 임계 0.88/6.3/11.0 (2.0배 마진), pytest 로 전건 PASS **고정했다** |
| 8 | 렌더러 byte 불변 | compare_render.py/cue_text.py `git diff` 를 **재봤다** | 0줄. compare_align.py 는 93 insertions만(본체 무변경) |
| 9 | 금지 스캔 | 이모지 정규식 + 동작명 grep 을 **돌렸다** | 이모지 0, 동작명 코드 분기 0 (매치는 관절명 left_elbow 계열·주석 실측 인용뿐), fixture별 임계 0 |
| 10 | M3 게이트 | phase33 M3 스위트를 **돌렸다** | I1~I5 + 12 테스트 PASS (핀 갱신 반영, tol 20°/slope 1.2 assert 유지) |

## 점수 이동 회계 요약

정본 = `data/rescore_diff.md` (doc×관절 전표). 요지:

- 편차(delta) 이동은 전건 |5.84°| 이내 — 무근거 급변 0. tol(20°) 경계 교차 2건: peterpan right_elbow 23.25→17.41(감점권 이탈 — 이탈 국면 아티팩트가 부풀리던 편차), kipup left_shoulder 18.98→20.09(+0.09 진입).
- 측정 순간(rep_frame) 이동 다수 — 의도된 효과. 대표: pdshapefault right_elbow 1.22s(자세 완성 전)→9.56s, left_shoulder 3.22→11.78s.
- 회계 단위: md(관절별 편차) 1차 + over(tol 초과분) 환산 열. `deduction_engine.tally` 재실행은 입력 재구성(profile/vision 컨텍스트/quantification)이 순수 계층 범위를 넘어 생략 — 표 하단 명시(오케스트레이터 검수분 §3 준수). vision-pointed 관절의 wm 경로는 수술 무접촉이라 최종 record 반영은 별도.

## Decisions Made

- **mode3 ref_fps = `_pipeline_frame_fps()`** (플랜의 "9.0 상수" 대신) — app.py 2447 "리터럴 fps 금지" 기존 규율 준수. 값 동일(9.0), 단일 출처만 다름.
- **side_match 다리 확장 = 보류(팔만 유지)** — 플랜이 실측 조건부로 위임한 결정. 발동 doc 0건이라 다리 편차 개선 근거 자체가 없음.
- **불변식 1 검사 = "제외-전용 user 프레임 집합 밖"** — 한 user 프레임이 kept/excluded 스텝에 동시 등장할 수 있어(DTW 수직 런) 순수 "제외 스텝 프레임 집합 밖"은 거짓 위반 가능. 선택 주체는 항상 kept 스텝이므로 excluded-only 집합이 정확한 판정 (하네스 주석 박제).

## Deviations from Plan

**1. [Rule 1 - 검증 커맨드 이식성] Task 3 verify 의 `wc -l | grep -qx 0` macOS 공백 패딩 실패**
- **Found during:** Task 3 verify
- **Issue:** macOS `wc -l` 출력의 선행 공백으로 `grep -qx 0` 이 항상 실패 (게이트 자체는 통과 상태)
- **Fix:** `tr -d ' '` 로 정규화해 동일 게이트 실행 — 결과 0줄 확인
- **Files modified:** 없음 (검증 방법만)

**2. [Rule 2 - 계층 규율] mode3 ref_fps 리터럴 회피** — 위 Decisions 참조. 플랜 문구(상수 9.0)와 다르나 플랜이 인용한 원칙(단일 출처) 자체를 따름.

---

**Total deviations:** 2 (경미 — 코드 산출 영향 0)
**Impact on plan:** 스코프 변화 없음.

## 미검증 항목 (안 재봤다 — 이유 + 이월 절차)

| 항목 | 왜 안 재봤나 | 이월 |
|---|---|---|
| belle-FAIL 측(doc 127a2a90) align_quality **기대 FAIL** | render-side align 은 GPU 에서만 생산 (CPU align != GPU align, 리그 E 13% vs 28% 실측). 현재 Pod 없음 | [POD-VERIFY.md](./POD-VERIFY.md) 절차 박제 (기대 FAIL, PASS 시 재캘리브레이션 라운드) |
| 실 doc 재분석 (수술 ②③ 반영 프로덕션 records/순간) | 앱 업로드 분석 불가 상태 (Pod 전멸 — memory current-pod). 하네스는 순수 계층 재현일 뿐 프로덕션 경로 산출물이 아님 | 다음 Pod 세션에서 재분석 → 하네스 표와 대조 (POD-VERIFY.md §6) |
| side_match 발동 케이스의 실효 (미러 doc 편차 개선) | 발동 실측 0건 — elbow(memory 실측 거울상)조차 std 비 1.14~1.16 으로 판별 미달. 역립 동작에서 양 손목 운동 스케일이 갈리지 않는 원리적 한계로 보임 (짜맞춤 금지 — 임계 하향으로 강제 발동시키지 않았다) | Phase 34 후속 — 폴-근접 기반 판별 검토 (단 260808-epy r01 의 x-투영 한계 선례 유의) |
| mode3 second+ ref_fps 경로 실 데이터 | mode3 doc 픽스처 리포 부재 (합성 테스트 + 기존 mode3 스위트 무회귀만 확인) | Pod 재분석 사이클에 mode3 케이스 포함 |

## 라운드 2 (오케스트레이터) — 수술 ③ 스왑 가설 기계검증 → 기각·무장해제

커밋 `3796b17`. 실행자 산출(side 관측 표: 발동 0건, elbow ratio 1.14 판별 미달)을
열어본 뒤, 검출기 개선 투자 전에 **가설 자체**를 기계검증했다
(`swap_hypothesis_probe.py`, 표 = `data/swap_hypothesis.md`):

| 변형 | elbow(유일 실측 거울상) arm mean | 판정 |
|---|---|---|
| 스왑 없음 | 24.15° (DTW 63.02) | 기준 |
| 팔 스왑+재정렬 (프로덕션 배선 형상) | 29.83° (DTW 70.35) | **악화 — 기각** |
| 전신 스왑+재정렬 | 29.25° | 악화 |
| 짝만 스왑(정렬 원본) | 38.95° | **악화 — 기각** |

전 7 doc 일관 악화(kipup 12.4→107.6 등, 개선 0건). 각도 공간 L/R 열 교환은 그립
미러를 보정하지 못한다 — 스크린 v0(위치 공간) 기각에 이어 각도 공간도 기각.

**처분 (동사 = 재봤다/고쳤다/돌렸다):**
- app.py mode1 call site 스왑 발동 제거 — 그립 거울상 감지는 관측 로그만
  ("스왑 미적용(실측 기각)" 문구). `_deviation_against(swap_lr_arms=)` 파라미터는
  하네스·불변식 3 재검용 존치, 프로덕션 항상 기본값 False.
- side_match.py docstring 의 "각도는 미러-불변 스칼라라 열 교환으로 충분" 이론
  문단을 실측 기각 절로 교체 (실측 > 이론).
- 방치 위험 제거가 실익: 검출이 잘 되는 스핀 계열(ratio 2.4~5.0)에서 미러 수행자가
  나오면 기각된 변환이 발동해 점수를 악화시켰을 것.
- phase34 32 테스트 + pipeline 16 테스트 재실행 GREEN (돌렸다). N=1 한계와 재개
  조건(미래 거울상 doc 에서 일관 개선 실측 시)은 swap_hypothesis.md 에 박제.
- belle 반려 "좌우 혼동"의 표시 계층 대응(attributionReliability 강등 후보)은
  제품 표면 결정 — belle 판단 대상으로 이월, 무단 구현 안 함.

## LLM 학습 영향

없음 — 채점 기하·렌더 게이트 수리로 학습 원장/데이터 무접촉 (보드 "우리 LLM" 토글 변화 0).

## Next Phase Readiness

- Phase 34 겹 1(불변식 스위트) 시작됨 — `backend/tests/phase34/` 20 테스트.
- 다음 Pod 세션 1순위: POD-VERIFY.md (belle-FAIL align_quality + 실 doc 재분석 대조).
- 렌더 부착 경로가 tier 에서 해방됨 — 다음 신선 doc E2E 에서 renderedCompare 부착 실측 가능.

## Self-Check: PASSED

- 신설 파일 10건 존재 확인 (`[ -f ]` 전건 FOUND)
- 커밋 4건(630907c7/fd4674e6/a04ba850/5e139cf0) git log 존재 확인
- 잔여 미커밋 = PLAN/SUMMARY(오케스트레이터 docs 커밋 소관) + 이 태스크 무관 기존 변경(.planning/config.json, spikes README)

---
*Phase: quick-260808-r82*
*Completed: 2026-08-08*
