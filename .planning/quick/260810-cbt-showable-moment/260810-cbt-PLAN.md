---
id: 260810-cbt
title: 표시 순간 조건부 측정 + 편차÷요동 지표의 belle 판정 대조
date: 2026-08-10
status: executing
mode: 측정만 — 채점·표시·렌더 산출 무접촉
---

# 260810-cbt 계획

착수 근거 = `.planning/MORNING-BRIEF-2026-08-10.md` §3 "할 것" 1·2.

## 무엇을 하나

### Task 1 — 조건부 표시 순간이 클립에 있는가 (`probes/showable_moment.py`)

세 조건을 **동시에** 만족하는 프레임이 있는지 클립 전수로 센다.

| 조건 | 판정 기준 | 어디서 온 수치인가 |
|---|---|---|
| ① 차이 큼 | \|학생각 − 기준각\| 곡선 전수 + 20° / 2×현행 표시 | 20° = 기존 마커 강조 임계(IPSF 허용오차 정합, 260620-18r) |
| ② 양쪽 포즈 신뢰 | 표시에 쓰이는 keypoint 3점이 양쪽 다 conf ≥ 0.5 <br> + 붕괴 아님(몸통 ≥ 0.4×클립중앙, 가로/세로 ≥ 0.25) | 0.5 = `fault_zoom._KP_CONF_MIN` / 0.4·0.25 = `collapse_scan.py` 08-09 실측 신호 |
| ③ 정렬 신뢰 | 짝 자세거리 `_pose_dist` ≤ **그 카드가 이미 승인받은 정지 순간의 짝 거리** | 승인 코퍼스 수준 = 통과선 (memory `approved-corpus-level-is-the-bar-not-the-danger-zone`) |

**새 임계를 만들지 않는다** — 셋 다 기존 게이트/승인본에서 되읽는다. ③이 자기 카드
기준이므로 "정렬을 더 나쁘게 만드는 순간"은 원리적으로 후보가 못 된다 —
폐기된 "최대 차이 순간"(150~177도가 전부 다른 국면)이 재발할 수 없는 자리.

산출 = record 별 1행: 현행 표시 순간의 차이 / 후보 수 / 최선 후보의 차이·시각·짝거리 /
그 후보에서의 `편차÷요동`. 게이트별 탈락 census 동반(무엇이 묶는지).

- **후보 있음** → 표시 순간 규칙만 교체 가능(채점 무접촉·점수 불변, 렌더만 다시).
- **후보 없음** → 그 지적은 사진으로 말할 수 없다 → 근거 트랙(피처 뱅크)으로.

### Task 2 — 지표 자체 검증 (`probes/metric_vs_verdict.py`)

`편차 ÷ 그 순간 요동` 이 **belle 이 반려한 카드와 통과시킨 카드를 가르는가**.

- 갈리면 임계 자리(0.08~0.14 빈 구간)가 유효.
- **안 갈리면 이 지표를 버린다.** (내 지표 17/20 vs belle 체감 "한두 개" — 어긋난다)
- belle 판정 라벨은 리포 문서에서 되읽고, 출처 없는 건은 `unknown` 으로 남긴다.
  라벨을 추측으로 채우지 않는다.

## 하지 않는 것

- 채점 코드·표시 코드·렌더 산출물 변경 0. `deduction_engine`·`compare_render` diff 0.
- 임계를 데이터에 맞춰 고르는 것(curve-fit). 이 사이클은 **측정만**이고 정책은 belle 결정.
- GPU / Pod / Firestore 쓰기.

## 검증

- `git diff --stat backend/` = 0 (probe 는 `.planning/` 아래)
- probe 2종이 로컬 픽스처 7건에서 실행되고 표를 낸다 (GPU 불필요)
- 표를 직접 열어 값을 읽고 SUMMARY 에 옮긴다 (memory `open-the-artifact-before-claiming-done`)
