---
id: 260810-e4v
title: fps 실효 rate 단일 출처 교정 — U1·U2·U3 코드 완료, 백필·U4 대기
date: 2026-08-10
status: 코드 4단위 + 백필 완료 · Pod 실검증 U2 PASS / U3 캡 포화로 미검증
commits:
  - b0dbe3c5 U1 실효 rate 산출·기록 (산출 배열 무접촉)
  - cfb3a447 U2 표시 앵커 초를 실효 rate 로 (점수 무접촉)
  - c0b7488b U3 기준 실측 rate 읽기 경로 + 백필 스크립트(미실행)
---

# 260810-e4v 요약

근거 = quick 260810-cbt 실측 · belle 08-10 결정("전역 + 점수표 게이트, 점수 변동은
정당하면 괜찮다").

## 무엇이 들어갔나

| 단위 | 무엇 | 점수 | 게이트 |
|---|---|---|---|
| **U1** | `effective_fps(src_fps,target)` + `extract()` 가 **경로별** 실효 rate 기록 | 무접촉 | 신규 13 pass · 산출 배열 불변 assert |
| **U2** | `_pipeline_frame_fps(video_path)` · `_moment_video_sec()` → `atVideoSec` 가 실효 rate 로 | **불변**(tally 종료 후 각인) | 신규 7 pass · 산식 5파일 diff 0 |
| **U3** | `_reference_angles_fps(ref)` — `anglesRealFps` 우선, 없으면 라벨 fail-open | 백필 전까지 **byte-동일** | 신규 11 pass |

전 구간 pytest **59 failed IDENTICAL(기준선) / 4096 passed**. 채점 산식 5파일
(`deduction_engine`·`dimensions`·`kismam`·**`motiondtw`**·`assemble`) diff 0 —
08-08 게이트가 목록에서 빠뜨렸던 `motiondtw` 를 명시 포함해 확인했다.

## 새로 알게 된 것

- **기준 11건 전수** 실제 rate = 14.88~15.00fps, 저장 라벨은 전부 18.0
  (아침 260810-cbt 는 5건 표본이었다). 마진 `ceil(0.5s×fps)` 가 전 동작에서 9 → 8.
- **경로별 기록이 필요한 이유**가 실측으로 확인됨: 한 분석에서 사용자·기준·이전 영상을
  같은 싱글턴으로 추출하고, 사용자는 target 9(→step 3), 기준 트랙은 target 18(→step 2)
  이라 rate 가 다르다. 단일 속성이면 나중 추출이 앞 기록을 덮는다.
- ★**fail-closed 게이트가 내 유도식을 잡았다** — 백필 교차검증을 `anglesFrames/길이` 로
  했더니 **강제 마지막 프레임 1장**(12-deferred §12-B) 때문에 전 건이 조금 크고, 짧은
  클립(peter-pan 8.6s)에서 1.56% 로 불일치 판정됐다. `(n−1)/길이` 로 교정하고 기록값은
  원리 정본(`src_fps/step`)으로 → 11/11 통과. 게이트가 없었으면 편향된 값을 심었다.

## 미완 (박제)

- **백필 미실행** — `--dry-run` 만 돌렸다(Firestore 쓰기 0). 이 실행이 **점수가 움직이는
  스위치**다. 로컬 근사 점수표(260810-cbt: 편차 이동 최대 0.150/0.571/0.597/0.290/0.294도)
  는 있고, **허용오차 20도 경계에 걸린 record 의 감점 유무 뒤집힘은 아직 안 쟀다.**
- **U4 미착수** — `fault_zoom:852` 4/3 보정 제거 + 승인본 카드 대조(belle 승인분),
  교정으로 표시 미성립이 되는 카드를 링·맥락으로 남기기(belle 승인분).
- **실 파이프라인 미검증** — U2 의 앵커 이동, U3 의 점수 이동 모두 Pod 재분석이 확정
  수단이다. 지금 것은 단위 테스트 + 로컬 근사다.
- U2 의 `pose_fps` 는 mode1/mode3 공용 call site 1곳에만 배선했다. `dtw_ref_fps`
  (mode3 fault_zoom)·Gemini 프롬프트 초 라벨은 여전히 `target_fps` 를 쓴다 — 각각
  표시·프롬프트 문자열이라 점수 무영향이지만 남은 자리로 기록한다.


## Pod 실검증 (2026-08-10, Pod `vaovfyw4pttnv1` 4090)

부트스트랩 → 기동 → `/health` 4항목 PASS (commitSha `548d9673` = U1~U4 포함 ·
`RTMW_DETERMINISTIC` true · `PR_INVERSION_ENABLED` true · modelLoaded).
기동 스크립트 md5 `e7f224d6` = 리포 정본과 동일, `start_p15_server.sh` 는 위임 스텁 확인.

`phase34_fresh_reanalysis.py` 로 belle 반려 영상(`127a2a90…`) 신선 재분석 —
**status=done score=60**, 08-08 과 같은 스크립트·같은 영상·같은 5 record.

### ✅ U2 검증됨 — 앵커가 실효 rate 로 환산된다

| record | 08-08 (라벨 9.0) | 08-10 (실효 rate) | 비율 |
|---|---|---|---|
| r00 left_elbow | 5.89s | **5.301767s** | 0.90013 |
| r01 right_elbow | 6.78s | **6.102034s** | 0.90001 |
| r04 left_knee | 11.67s | **10.503501s** | 0.90004 |

예측 `9 / 9.9967 = 0.90030` 과 소수 3~4자리 일치(08-08 값이 2자리 보고라 그 반올림 안).
`at` 값이 1/9 배수(`17.7777…`)에서 비배수(`5.301767…`)로 바뀐 것 자체가 라이브 증거이고,
`53 / 5.301767 = 9.9967` 이며 `fault_zoom` 로그의 `user9=53` 과 일치한다.

### ⚠ U3 는 이 실행으로 검증되지 않았다 (캡 포화)

Σ실행 감점 = 45.0 → `min(40, Σ)` 캡 포화 → final 60. 편차 ≤0.6도 이동이 **최종 점수에
나타날 수 없는 조건**이었다. U3 의 점수 효과 확정은 **Σ < 40 인 doc** 이 필요하다.

### ✅ 결정론 확인 + 인과 분해 (2회차 재실행)

2회차가 1회차와 **소수 15자리까지 동일**(at 5건·points 5건·score 60 전건). 채점·표시
순간은 완전 결정론이므로, 08-08 → 08-10 의 변화는 run-to-run 노이즈가 아니라 **코드
변경**이다. 프레임 인덱스로 되돌리면 원인이 깨끗이 갈린다:

| record | 08-08 | 프레임 | 08-10 | 프레임 | 원인 |
|---|---|---|---|---|---|
| r00 left_elbow | 5.89s | 53 | 5.3018s | **53** | 프레임 불변 → **U2만**(초만 ×0.9) |
| r01 right_elbow | 6.78s | 61 | 6.1020s | **61** | 프레임 불변 → **U2만** |
| r04 left_knee | 11.67s | 105 | 10.5035s | **105** | 프레임 불변 → **U2만** |
| r02 right_shoulder | 9.11s | 82 | 13.8046s | **138** | 프레임 이동 → **U3(median 변화)** |
| r03 left_hip | 14.89s | 134 | 4.7016s | **47** | 프레임 이동 → **U3** |

U2 는 초 환산만 바꾸므로 `atFrameIdx` 를 건드릴 수 없다 — 3건이 그것을 증명한다.
2건의 프레임 이동은 U3 가 median 을 0.04~0.18도 옮겨 "집계값 최근접 프레임" 선택이
바뀐 것이다. **즉 U3 는 점수에는(캡 때문에) 안 나타났지만 표시 순간에는 라이브로
작동했다.** left_hip 은 14.89s(20.2s 영상의 종점 근처) → 4.70s 로 왔다.

### ★ 남은 관찰: 표시 순간 규칙이 median 에 이산적으로 민감하다

0.04~0.18도의 median 이동이 표시 순간을 **4.7~5.7초** 옮긴다(`moment.select_moment_index`
= 집계값 최근접, `TIE_EPS=0.01`). 개선/악화 판정은 belle 이 사진을 봐야 한다 —
수치로는 못 가린다. (이전 항목의 "가설"은 결정론 확인으로 기전이 특정됐다.)

### (구) 가설 항목 — 위에서 해소됨

같은 5 record 중 **2건이 비례하지 않고 점프**했다:
`r02 right_shoulder 9.11s → 13.80s` · `r03 left_hip 14.89s → 4.70s` (4.7~5.7초).
셋은 0.9001 로 정확히 스케일되고 둘만 점프했다.

가설: `moment.select_moment_index` 는 "집계값에 가장 가까운 프레임"이고 동점 허용오차가
`TIE_EPS=0.01` 이다. U3 로 median 이 0.04~0.18도 움직이면 근접 후보 순위가 바뀌어
표시 순간이 통째로 다른 국면으로 뛴다. **이것이 belle 이 느낀 "할 때마다 화면이
달라진다"의 후보 기전이다.**

★**아직 재보지 않았다** — 확정 방법 = 같은 분석 1회 재실행(결정론 확인: 재현되면 U3 의
median 이동이 원인, 흔들리면 잔여 비결정성). 재실행 전에는 기전을 단정하지 않는다.

### 미완

- SSM/Lambda `RUNPOD_ANALYZE_URL` 새 proxy 동기 **미실행**(belle 앱 업로드 경로 필요) —
  프로덕션 config 쓰기
- U4-2(빠진 카드 링 폴백) 미착수 · noise-floor 억제 경계 미측정

## LLM 학습 영향

코드 자체는 없음(채점 신호 무접촉). 단 백필 후에는 **표시 순간과 기준 경계 마진이 바뀌므로**
Phase 22 교사 라벨이 프레임·초를 인용하는 경우 재라벨 대상이 된다 — 인용 여부 미확인(별건).
