---
quick_id: 260811-xa1
slug: mark-grammar-round-ufb-freeze-2-belle
date: 2026-08-12
type: execute
wave: 1
depends_on: []
autonomous: true
files_modified:
  - .planning/quick/260811-xa1-mark-grammar-round-ufb-freeze-2-belle/grammar_round.py
  - .planning/quick/260811-xa1-mark-grammar-round-ufb-freeze-2-belle/out/ (baseline + 후보 PNG)
  - .planning/quick/260811-xa1-mark-grammar-round-ufb-freeze-2-belle/JUDGMENT.md
  - .planning/quick/260811-xa1-mark-grammar-round-ufb-freeze-2-belle/260811-xa1-SUMMARY.md
must_haves:
  truths:
    - "무패치 베이스라인 렌더 md5 == ufb 인증 md5 2건 (left_elbow 9891d2811eb9dcb7925bcf229831f19b / left_hip 8e1472097f8a7c2acf3834dfdaf2c78f) — 반려 실물과 같은 경로 증명"
    - "왼골반 후보 3안에 '그냥 선' 계열 포함 (LD-3, belle 힌트)"
    - "왼팔꿈치 후보 3안 전부 마크가 얼굴/머리 영역 비관통 — 개별 원본 육안 확인 (LD-3, LD-5)"
    - "각 후보에 근거 1줄 + 캡션 1줄 (수치 미노출·신뢰 티어 표기·이모지 금지·코칭 화법, 왼팔꿈치 = 폴 이탈 화법) (LD-4)"
    - "판정지에 카드별 내 추천 1개 + 실물 관찰 근거 1줄 + bz5 관계 1줄 (LD-6, LD-7)"
    - "운영 코드 diff 0 (backend/ 무접촉) + Gemini 호출 0 (machine_eye 스텁)"
  artifacts:
    - path: ".planning/quick/260811-xa1-mark-grammar-round-ufb-freeze-2-belle/grammar_round.py"
      provides: "베이스라인 md5 게이트 + 후보 렌더 드라이버 (ufb verify_local.run 랩)"
    - path: ".planning/quick/260811-xa1-mark-grammar-round-ufb-freeze-2-belle/out/baseline/"
      provides: "무패치 재현 카드 2장 (md5 게이트 대상)"
    - path: ".planning/quick/260811-xa1-mark-grammar-round-ufb-freeze-2-belle/out/candidates/"
      provides: "후보 PNG 6장 (left_hip P1~P3 / left_elbow E1~E3)"
    - path: ".planning/quick/260811-xa1-mark-grammar-round-ufb-freeze-2-belle/JUDGMENT.md"
      provides: "belle 판정지 — 카드별 후보 나란히 + 캡션 + 추천"
  key_links:
    - from: "grammar_round.py"
      to: ".planning/quick/260811-ufb-freeze-only/verify_local.py"
      via: "importlib 로드 후 vl.EV 리다이렉트 + vl.run() 호출 (경로 재발명 금지)"
      pattern: "spec_from_file_location.*verify_local"
    - from: "grammar_round.py"
      to: "sunity_shared.analysis.card_gates.machine_eye"
      via: "드라이버 프로세스 한정 스텁 (Gemini 0회 — ufb --zero-card 의 hold_gate 스텁과 같은 방식)"
      pattern: "machine_eye"
    - from: "grammar_round.py"
      to: "sunity_shared.analysis.fault_zoom._draw_joint_angle"
      via: "2-pass 패치 (bz5 _GrammarPatch 패턴 — record → candidate)"
      pattern: "_draw_joint_angle"
---

<objective>
마크 문법 후보 라운드 — belle 반려 2카드(왼골반 freeze 16.7s "마크 반려" · 왼팔꿈치
freeze 5.3s "부위 특정 불가")의 표시 문법 후보를 **같은 freeze 순간에서** 로컬 실물로
산출하고, 캡션 문구를 동반한 판정지 1장을 belle 께 낸다. 운영 코드 무접촉,
Gemini/Pod 0회. 채택은 belle 몫 — 이 라운드는 판정 재료 생산이다.

Purpose: 08-11 심야 belle 판정에서 순간·짝·크롭 축은 안정화("어째 점차 좋아지는거
같긴 허다")됐고 잔여 축 = 마크 문법 + 설명 문구. r03 보드 그림 반려("선이 너무
가려서")와 같은 뿌리이므로 이 라운드가 풀면 두 곳이 같이 풀린다.
Output: grammar_round.py 하네스 + out/ 실물 PNG(베이스라인 2 + 후보 6) + JUDGMENT.md
</objective>

<context>
@.planning/quick/260811-ufb-freeze-only/evidence/belle-verdict-2026-08-11.md   # 반려 스펙 원문
@.planning/quick/260811-ufb-freeze-only/evidence/pod_cards/zoom_angle_vs_reference__left_elbow.png   # 반려 실물 (역립 — 각도선 2가닥이 얼굴·머리카락 관통)
@.planning/quick/260811-ufb-freeze-only/evidence/pod_cards/zoom_angle_vs_reference__left_hip.png     # 마크 반려 실물 (장면 PASS)
@.planning/quick/260811-ufb-freeze-only/verify_local.py    # 재사용 원천 ① — run() 이 반려 카드를 로컬 재현 (md5 인증·결정론 2회 증명)
@.planning/quick/260811-bz5-mark-grammar/render_harness.py # 재사용 원천 ② — _GrammarPatch 2-pass drawing 패치 + 고스트/쐐기/하이브리드 드로잉 함수
@.planning/quick/260811-bz5-mark-grammar/260811-bz5-SUMMARY.md # bz5 실측 (left_hip "쐐기가 읽힘" / 승인 시각 언어 제약)
</context>

<locked_decisions_map>
- LD-1 산출만, 배선 없음 — 채점·운영·렌더러(fault_zoom.py 포함) 코드 무접촉. 드로잉 교체는 하네스 프로세스 안 monkeypatch 만 (bz5 방식).
- LD-2 같은 freeze 프레임 — 왼골반 u16.667/r15.20, 왼팔꿈치 u5.302/r5.13 (ufb run_verdict.json survivors 그대로). 순간 변경 금지.
- LD-3 후보 구성 — 왼골반 3안("그냥 선" 계열 필수) + 왼팔꿈치 3안(가림 회피 규칙), 각 안에 근거 1줄.
- LD-4 캡션 — 각 안에 1줄 동반. 기결론 존중: 각도 수치 미노출, 신뢰(티어) 표기만, 이모지 금지, 코칭 화법. 왼팔꿈치 캡션 = 폴 이탈/근접 화법(P35 기결론 "엘보 = 폴 근접도", 재질문 금지).
- LD-5 몽타주 축소본 검수 금지 — 산출 후 개별 원본을 직접 열어 확인.
- LD-6 판정지 = 마크다운 1장 — 카드별 후보 나란히 + 내 추천 1개와 근거 1줄(실물 관찰 근거만).
- LD-7 bz5 관계 1줄 명기 — 중복 발명 금지, 재사용 가능하면 재사용.
</locked_decisions_map>

<tasks>

<task type="auto">
  <name>Task 1: grammar_round.py 하네스 — 베이스라인 md5 게이트 + 후보 6안 렌더</name>
  <files>.planning/quick/260811-xa1-mark-grammar-round-ufb-freeze-2-belle/grammar_round.py, .planning/quick/260811-xa1-mark-grammar-round-ufb-freeze-2-belle/out/</files>
  <action>
xa1 디렉터리에 `grammar_round.py` 를 만든다. 경로를 재발명하지 말고 ufb 드라이버를
그대로 감싼다 (LD-1):

**골격 (importlib 랩):**
1. `importlib.util.spec_from_file_location` 로
   `.planning/quick/260811-ufb-freeze-only/verify_local.py` 를 `vl` 로 로드
   (verify_local 이 import 시점에 backend sys.path 를 넣어줌).
2. **EV 리다이렉트 필수·선행**: `vl.EV = <xa1>/out/_ev` 로 바꾼 뒤에만 `vl.run()`
   호출. run() 은 EV 하위 cards/·eye_ledger/ 를 비우므로 리다이렉트 없이는 ufb
   evidence 가 파괴된다 — 호출 전 `assert "260811-xa1" in str(vl.EV)` 가드 삽입.
3. **Gemini 0회**: `os.environ.setdefault("GEMINI_API_KEY", "xa1-stub")` (SSM 미호출)
   + `card_gates.machine_eye` 를 드라이버 프로세스 한정 스텁으로 교체 — 반환
   `{"observed": ..., "limb": ..., "match": True, "confidence": 1.0, "reason":
   "xa1-stub (ufb 인증 verdict 재사용)", "crop": cg.mark_crop(frame, xy)[0]}`
   (app.py:4544 가 `res["crop"].save` 를 부르므로 PIL crop 필수 — `cg.mark_crop`
   재사용, 네트워크 0). ufb --zero-card 의 hold_gate 스텁과 같은 계열의 관찰 실험
   패치다. 홀드/짝정합 게이트는 순수 로컬이라 그대로 돈다.
4. 캐시 = 세션 scratchpad `fresh/` (현재 살아 있음 — doc.json/user.mp4/ref.mp4/
   align_work/render 확인 완료). 죽어 있으면
   `python3 ../260811-ufb-freeze-only/verify_local.py --fetch` 로 재구성
   (AWS_PROFILE=sunity-motion 필요) — 하네스가 캐시 부재 시 이 안내를 출력.

**`--baseline` 스테이지 (게이트):** 드로잉 무패치로 vl.run() 1회 →
out/baseline/ 에 카드 2장. 판정:
  (a) PNG md5 == ufb 인증값 — left_elbow `9891d2811eb9dcb7925bcf229831f19b`,
      left_hip `8e1472097f8a7c2acf3834dfdaf2c78f` (evidence/run_verdict.json
      pngMd5 정본). 일치 = machine_eye 스텁이 verdict 를 안 바꿨고 이 경로가
      belle 이 반려한 실물과 동일 경로라는 증명.
  (b) survivors == `['r03:inherit@u16.667/r15.20', 'r00:inherit@u5.302/r5.13']`
      (LD-2 — 순간 무변경의 기계 증명).
불일치 시 후보 렌더로 넘어가지 않는다 (기준선 없는 비교는 무의미 — bz5 규율).
베이스라인 실행 중 `fault_zoom._draw_joint_angle` 에 record 래퍼(원본 위임 +
호출별 vertex/limb/torso 사이각 기록 — bz5 `_inner_deg` 재사용)를 걸어 후보
패스용 각도 시퀀스를 1회로 확보한다. 렌더 호출 순서는 결정론 (ufb 2회 md5 동일
증명) 이므로 호출 인덱스 키가 안전하다. 어느 build 호출이 어느 관절인지는
`fz.build_fault_zoom_comparisons` 랩으로 joints 인자를 함께 기록.

**`--candidates` 스테이지:** 후보별로 `fz._draw_joint_angle` 만 교체하고 vl.run()
→ out/candidates/{안}/ 에서 해당 카드 PNG 만 수확 (다른 카드는 원본 드로잉 유지 —
bz5 _GrammarPatch 의 candidate 패스 패턴). 승인 시각 언어 유지: 브랜드 #FF4B33 +
흰 halo, 수치 배지 0, 이미지 내 한글 텍스트 렌더 금지 (bz5 폰트 회피).

후보 6안 (LD-3 — 각 안의 근거는 아래 1줄이 정본, JUDGMENT.md 에 그대로 옮김):

왼골반 (r03, freeze u16.667/r15.20, deficit 20.0도 — 장면 PASS·마크만 반려):
- **P1 단일선 ("그냥 선")**: 결함 사지(대퇴) 방향 선 1가닥 + vertex 소형 링만.
  몸통 선·사이각 호 제거. 근거: belle 원문 "그냥 선으로 되거나 해도" 직역 —
  V자 2가닥의 방향 혼동("뭔가 좀 이상한디")이 반려 원인 후보.
- **P2 단일선 + 쐐기**: P1 + 기준각 이식 쐐기(반투명 브랜드) + 교정 방향 화살촉
  (bz5 `_draw_candidate` 의 쐐기·카이럴리티 로직 재사용, 기준각 = record 패스
  기록값). 근거: bz5 left_hip 실측 "쐐기가 읽힘" — 검증된 요소의 재사용으로
  "어느 쪽으로 얼마나"를 면적으로 답한다.
- **P3 bz5 하이브리드 그대로**: 기존 선 2 + 쐐기 + 화살촉 + 고스트(델타>=8도만).
  근거: bz5 후보군과의 직접 대조 기준점 — 재사용 판정을 belle 이 같은 프레임에서
  내릴 수 있게 (LD-7, 중복 발명 금지).

왼팔꿈치 (r00, freeze u5.302/r5.13, deficit 29.1도 — 역립, 팔꿈치가 머리 옆):
**가림 회피 규칙 (전 안 공통)**: 마크 픽셀이 얼굴/머리 영역에 진입 금지.
머리 영역 추정 — 12관절 리포트에 머리 kp 가 없으므로 ① align 17-kp 트랙
(fresh/align_work, nose/eyes/ears = COCO 0~4)을 카드 크롭 변환으로 패널 좌표에
투영하거나 ② 어깨 중점에서 골반 반대 방향 연장 휴리스틱 중 실행자 선택.
최종 판정은 산출 PNG 육안 (Task 2 — 휴리스틱이 빗나가면 여기서 잡힌다).
- **E1 짧은 선**: 현행 2가닥 유지하되 머리 원반 경계에서 클리핑(진입 전 중단).
  근거: 문법 변경 최소 수정 — "가림만 제거하면 현행 마크가 유효한가"를 분리 검증.
- **E2 폴 간격 브래킷**: 팔꿈치 vertex 링 + 팔꿈치→폴 축선 간격 브래킷
  (폴 x 좌표 = 운영 pole 캐시 `render/pole_user.json` xNorm 재사용), 경로가 머리
  원반을 우회하도록 브래킷 위치 선정. 근거: P35 기결론 "엘보 = 폴 근접도" 이식 —
  이 부위의 확정 설명 문법은 각도가 아니라 폴 이탈이다 (LD-4 기결론 정합).
- **E3 스포트라이트**: 팔꿈치 중심 원 바깥을 약하게 감광(반투명 dim 오버레이)
  + vertex 링, 선 0가닥. 근거: 밀집 국면(역립·머리 옆)에서 관통 마크가 원리적으로
  불가능한 유일 계열 — 가림 회피의 극한값을 belle 판정 스펙트럼에 넣는다.

운영 코드·ufb/bz5 디렉터리 무접촉 (LD-1). 렌더 파라미터·크롭은 run() 경로가
전부 결정하므로 손대지 않는다.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/.planning/quick/260811-xa1-mark-grammar-round-ufb-freeze-2-belle && python3 grammar_round.py --baseline && python3 grammar_round.py --candidates && ls out/candidates/*/zoom_angle_vs_reference__left_hip.png out/candidates/*/zoom_angle_vs_reference__left_elbow.png | grep -c png | grep -qx 6 && [ -z "$(git -C /Users/kimtaesung/Dev/SunityMotion status --porcelain -- backend/)" ] && echo GATE-PASS</automated>
  </verify>
  <done>--baseline 이 md5 2/2 + survivors 일치로 PASS 출력. out/candidates/ 에 P1~P3(left_hip) + E1~E3(left_elbow) PNG 6장. backend/ 워킹트리 변경 0 (staged 포함). Gemini 실호출 0 (machine_eye 스텁 — 하네스 출력에 eye 스텁 카운트 명기).</done>
</task>

<task type="auto">
  <name>Task 2: 개별 원본 육안 확인 + JUDGMENT.md 판정지 + 커밋</name>
  <files>.planning/quick/260811-xa1-mark-grammar-round-ufb-freeze-2-belle/JUDGMENT.md, .planning/quick/260811-xa1-mark-grammar-round-ufb-freeze-2-belle/260811-xa1-SUMMARY.md</files>
  <action>
**① 실물 확인 (LD-5 — 몽타주 금지)**: 후보 PNG 6장 + 베이스라인 2장을 Read 도구로
**한 장씩 개별로 연다**. 확인 항목:
- E1~E3: 마크 픽셀이 얼굴/머리카락 위를 지나는가 — 지나면 해당 안을 수정 후
  재렌더하고 다시 연다 (수정 없이 판정지에 올리지 않는다).
- P1~P3: 마크가 카드에서 읽히는가 (선/쐐기가 크롭 안에 실제로 보이는가),
  vertex 가 왼골반 위에 있는가.
- 관찰 결과를 카드×안 표로 기록 (추천 근거의 원천 — recommend-only-after-measuring).

**② JUDGMENT.md (LD-6)**: 마크다운 1장, 구조:
- 카드별 섹션 2개 (왼골반 / 왼팔꿈치). 각 섹션: 반려 실물(pod_cards 상대 링크,
  belle 판정 원문 인용) → 후보 3안 나란히 (상대 링크 + **근거 1줄** — Task 1 의
  정본 그대로 + **캡션 1줄**).
- 캡션 규칙 (LD-4): 코칭 화법, 각도 수치 미노출, 신뢰(티어) 표기 포함
  (예: "신뢰 높음" — confirmed 티어), 이모지 금지. 왼팔꿈치 캡션은 폴 이탈/근접
  화법 (예: "왼팔꿈치가 폴에서 떨어져 있어요. 폴 쪽으로 붙여 보세요 · 신뢰 높음"),
  왼골반 캡션은 골반-상체 정렬 화법. 캡션은 판정지 텍스트로만 — 이미지 안 렌더
  금지 (bz5 폰트 회피 유지). 안마다 마크가 말하는 몫이 다르므로 캡션도 안별로
  쓴다 (P1 단일선은 캡션이 설명을 더 지고, P2 쐐기는 방향을 마크가 진다).
- 카드별 **내 추천 1개 + 근거 1줄** — 근거는 ①의 실물 관찰만 (측정 없는 단정
  금지). 관찰이 갈라주지 않으면 "추천 없음, belle 판정이 가른다"로 정직하게.
- **bz5 관계 1줄** (LD-7): 고스트/쐐기/하이브리드 중 재사용분(P2·P3)과 신규분
  (P1·E1~E3)의 구분, bz5 판정 페이지와의 관계.
- **LLM 학습 영향 1줄**: 이번 라운드 Gemini 호출 0 (machine_eye 스텁) — 학습
  전송 0, 눈 원장 신규 0 (스텁 산출물은 원장 아님 명기).

**③ SUMMARY + 커밋**: `260811-xa1-SUMMARY.md` (quick 관례 — 기계 판정 한 줄 +
베이스라인 게이트 결과 + 후보 6안 요약 + belle 판정 대기 명기). 커밋 (한국어
conventional commits, 이모지 금지): grammar_round.py + out/ PNG (리포 보존 —
scratchpad 휘발 규율) + JUDGMENT.md + PLAN + SUMMARY.
예: `feat(quick-260811-xa1): 마크 문법 후보 라운드 — ufb freeze 2카드 후보 6안 + 판정지`
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/.planning/quick/260811-xa1-mark-grammar-round-ufb-freeze-2-belle && python3 -c "
import pathlib,sys
j=pathlib.Path('JUDGMENT.md').read_text()
pngs=sorted(p for p in pathlib.Path('out/candidates').rglob('*.png'))
missing=[str(p) for p in pngs if (p.parent.name+'/'+p.name) not in j and str(p) not in j]
assert pngs, 'candidates 없음'
assert not missing, f'판정지 미링크: {missing}'
assert 'pod_cards' in j, '반려 실물 링크 없음'
for tok in ('추천','bz5','신뢰'): assert tok in j, f'{tok} 누락'
import re; assert not re.search(r'[\U0001F300-\U0001FAFF☀-➿]', j), '이모지 검출'
print('JUDGMENT-GATE PASS')" && git -C /Users/kimtaesung/Dev/SunityMotion log --oneline -1 | grep -q "xa1" && echo COMMIT-OK</automated>
  </verify>
  <done>JUDGMENT.md 가 후보 6장 전부 + 반려 실물을 링크하고, 카드별 추천 1개 + 근거 1줄 + 캡션(안별 1줄) + bz5 관계 1줄 + LLM 학습 영향 1줄을 담는다. 후보 8장(베이스라인 포함) 전부 개별 Read 로 열어 확인했고 E1~E3 얼굴/머리 비관통이 육안 성립. 커밋 완료. belle 판정은 다음 단계 (이 라운드의 완료 정의 아님).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로컬 하네스 → AWS (캐시 부재 시에만) | S3/Firestore fetch — SA 키·AWS 키 사용 |
| (차단됨) 하네스 → Gemini | machine_eye 스텁으로 0회 — 크롭 이미지 외부 전송 0 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-xa1-01 | Information Disclosure | grammar_round.py 로그/출력 | mitigate | GEMINI_API_KEY="xa1-stub" 더미값 — 실키 SSM 미조회, 키·SA 값 로그 금지 (ufb 규율 상속) |
| T-xa1-02 | Tampering | ufb evidence/ (반려 증거 정본) | mitigate | vl.EV 리다이렉트 선행 + `assert "260811-xa1" in str(vl.EV)` 가드 — run() 의 디렉터리 클리어가 ufb 증거를 지우지 못하게 |
| T-xa1-SC | Tampering | 패키지 설치 | accept | 신규 설치 0 — 기존 backend 의존성만 import |
</threat_model>

<verification>
- 베이스라인 게이트: 무패치 렌더 md5 2/2 == ufb 인증값 + survivors 문자열 일치 (LD-2 기계 증명).
- 운영 무접촉: `git status --porcelain -- backend/` 빈 출력 (staged 포함 — git add 무력화 함정 회피).
- 후보 6장 존재 + 판정지 전량 링크 + 이모지 0 (Task 2 automated).
- 실물 게이트: 8장 개별 Read (frames-before-numbers — 판정지는 근거 프레임 눈 확인 후에만).
</verification>

<success_criteria>
- belle 이 JUDGMENT.md 하나로 카드 2장 × 후보 3안씩을 캡션과 함께 판정할 수 있다.
- 모든 후보가 반려된 그 freeze 순간의 실물이다 (순간 발명 0 — md5/survivors 게이트).
- 운영 코드 diff 0 · Gemini 호출 0 · ufb/bz5 증거 무접촉.
- 내 추천이 실물 관찰 근거와 함께 있고, 관찰이 못 가르면 그렇게 적혀 있다.
</success_criteria>

<output>
완료 시 `.planning/quick/260811-xa1-mark-grammar-round-ufb-freeze-2-belle/260811-xa1-SUMMARY.md` 작성 (quick 관례).
</output>
