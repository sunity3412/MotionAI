---
phase: quick-260813-m0k
quick_id: 260813-m0k
slug: v-bake-align-ab-pair-recovery-coverage
date: 2026-08-13
status: planned
description: 미세조정 1차 재료 — ① V 베이크 스펙 align 단일 출처 이관 A/B 렌더(전/후 시트, 운영 코드 무접촉) ② 반려 2건(피디쉐입 왼무릎·왼팔꿈치) 짝 국면 회복 후보 탐색 + 추천 사전 박제 ③ 침묵·드랍 3건 회복 가능성 실측 + 커버리지 표(belle 커버리지 질문 실측 답)
wave: 1
depends_on: []
type: execute
plan: 01
autonomous: true
requirements: [QUICK-260813-M0K]
files_modified:
  - .planning/quick/260813-m0k-1-v-bake-align-a-b-2/ab_render.py
  - .planning/quick/260813-m0k-1-v-bake-align-a-b-2/evidence/
  - .planning/quick/260813-m0k-1-v-bake-align-a-b-2/AB-EYE-VERDICT.md
  - .planning/quick/260813-m0k-1-v-bake-align-a-b-2/PAIR-CANDIDATES.md
  - .planning/quick/260813-m0k-1-v-bake-align-a-b-2/COVERAGE.md
  - .planning/quick/260813-m0k-1-v-bake-align-a-b-2/260813-m0k-SUMMARY.md
must_haves:
  truths:
    - "A 런(무패치)은 ivs 스윕과 동일하다 — zoom_*.png md5 가 ivs sweep_verdict.json pngMd5 정본과 전건 일치 (불일치 = BLOCKER 박제, 진행 금지)"
    - "B 런(align 유도 스펙)은 하네스 monkeypatch 만 — backend/ 및 기존 하네스 원본(sweep_render.py, verify_wiring.py, verify_local.py, grammar_round.py) diff 0"
    - "B 는 fail-closed — align conf < 0.5 관절은 정직한 침묵 유지 + conf 실측값 기록 (환각 좌표로 V 발명 금지, frames-before-numbers). B 에서 살아난 V 전부 Read 로 열어 '옳게 살아났는가(관절 위·방향 맞음)' 사전 박제"
    - "survivors/dropped 는 ivs == A == B 전건 동일 (게이트·채점 무접촉의 기계 증거 — B 는 카드 build 단계만 변형)"
    - "반려 2건(피디쉐입 왼무릎 u3.667/r2.4 · 왼팔꿈치 u8.556/r9.4) 각각 ref 후보 2~3개(시간 분산 확보) + 현행 짝 baseline 거리 + 내 추천 1안 사전 박제 (포즈 거리 수치 + 육안)"
    - "짝 후보는 재정박 부활이 아니다 — 운영 코드 무접촉, belle 장면 확정용 판정 재료. 확정 시 다음 사이클에서 r01 선례(pairSrc=override, 명시 짝 지정)와 같은 pair-override 경로로 반영 — PAIR-CANDIDATES.md 와 SUMMARY 에 이 구분 명기"
    - "커버리지 표: 승인 5동작 감점 record 전수(13 rid) x (방출/침묵 사유/B 회복 가능/회복 불가 사유) — belle '모든 확대 비교를 해주는 거지?' 질문의 실측 답"
    - "Gemini 실호출 0 — grammar_round machine_eye 스텁 상속, 학습 전송 0"
    - "/Users/Shared/sunity-finetune-260813/ 한글 파일명 사본 + SUMMARY 보드 게시 재료 (캡션에 각도 도 수치 금지)"
  artifacts:
    - path: ".planning/quick/260813-m0k-1-v-bake-align-a-b-2/ab_render.py"
      provides: "m0k 신설 드라이버 — ivs sweep_render 상속 구조 + B monkeypatch(2 seam) + 짝 후보 스캔"
    - path: ".planning/quick/260813-m0k-1-v-bake-align-a-b-2/evidence/ab_verdict.json"
      provides: "카드별 A/B 베이크 상태·md5·align conf 실측·survivors 동일성 기계 판정"
    - path: ".planning/quick/260813-m0k-1-v-bake-align-a-b-2/evidence/ab_cards/"
      provides: "A/ B/ 카드 PNG + sheets/ 전/후 나란히 시트"
    - path: ".planning/quick/260813-m0k-1-v-bake-align-a-b-2/evidence/pair_candidates.json"
      provides: "반려 2건 포즈 거리 랭킹 + 후보 좌표/초 + baseline"
    - path: ".planning/quick/260813-m0k-1-v-bake-align-a-b-2/AB-EYE-VERDICT.md"
      provides: "B 소생 V 전수 육안 사전 박제 (옳게 살아났는가)"
    - path: ".planning/quick/260813-m0k-1-v-bake-align-a-b-2/PAIR-CANDIDATES.md"
      provides: "후보별 스틸 + 추천 1안 사전 박제 + 재정박 아님 구분 명기"
    - path: ".planning/quick/260813-m0k-1-v-bake-align-a-b-2/COVERAGE.md"
      provides: "record 전수 커버리지 표 (belle 질문 답)"
    - path: ".planning/quick/260813-m0k-1-v-bake-align-a-b-2/260813-m0k-SUMMARY.md"
      provides: "보드 게시 재료 + LLM 학습 영향 + 한계 박제"
  key_links:
    - from: ".planning/quick/260813-m0k-1-v-bake-align-a-b-2/ab_render.py"
      to: ".planning/quick/260811-xa1-mark-grammar-round-ufb-freeze-2-belle/grammar_round.py"
      via: "importlib 로드 (machine_eye 스텁·더미 키·backend sys.path 상속 — ivs sweep_render 62-68행 패턴)"
      pattern: "grammar_round"
    - from: ".planning/quick/260813-m0k-1-v-bake-align-a-b-2/ab_render.py"
      to: "backend/shared/python/sunity_shared/analysis/fault_zoom.py"
      via: "B seam monkeypatch — build_angle_bake_spec(1959행)·_member_pts (원본 파일 무수정)"
      pattern: "build_angle_bake_spec"
    - from: ".planning/quick/260813-m0k-1-v-bake-align-a-b-2/evidence/ab_verdict.json"
      to: ".planning/quick/260813-ivs-5/evidence/sweep_verdict.json"
      via: "A 런 md5 == ivs pngMd5 정본 대조 (무패치 동치 게이트)"
      pattern: "pngMd5"
---

<objective>
마크 미세조정 1차 라운드의 판정 재료 3종을 한 상에 올린다: ① V 베이크 스펙을
rep12 대신 align 단일 출처에서 유도하면 V 미베이크 5/8 이 옳게 살아나는가의
A/B 실물 (belle 08-13 locked: "어차피 둘 다 해야 하잖아" — V 회복 + 반려 회복
같은 라운드), ② 반려 2건(피디쉐입 왼무릎·왼팔꿈치 — "두 패널이 너무나 다른
화면")의 짝 국면 회복 후보와 내 추천 사전 박제, ③ 침묵·드랍 3건의 회복 가능성
실측 + record 전수 커버리지 표 (belle "영상별로 모든 확대 비교를 해주는 거지?"
질문의 실측 답).

**살리는 것 자체가 목표가 아니라 옳게 살아나는지가 판정 대상** — 환각 좌표로
그린 V 는 더 나쁨 (frames-before-numbers). align conf 미달은 정직한 침묵 유지가
옳다는 증거로 박제한다.

Purpose: belle 이 V 이관 채택 여부·반려 2건 장면·커버리지 처분을 실물과 실측으로
판정할 수 있게 한다. 운영 코드 무접촉 — 채택 시 배선은 다음 사이클.
Output: A/B 카드 + 전/후 시트 + ab_verdict.json + 짝 후보 스틸 + 커버리지 표 +
육안 사전 박제 2건 + /Users/Shared 한글 사본 + SUMMARY 보드 재료.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260813-ivs-5/sweep_render.py
@.planning/quick/260813-ivs-5/STATUS.md
@.planning/quick/260813-ivs-5/evidence/sweep_verdict.json
@.planning/quick/260813-fxx-belle-3-p3r1-pass-v-p3-align-fps-5-pytes/260813-fxx-SUMMARY.md

사전 확인된 사실 (플래너 실측 — 재조사 불요):
- V 베이크 게이트 자리 = `fault_zoom.py` 3425-3453행. 진입 조건이 양측 crop
  `valid`+box 요구 (`ref_crop_relaxed` = 진입 자체 실패), 진입 후
  `build_angle_bake_spec`(1959행) 이 rep12 트랙 + `_gated_kp` conf 게이트로
  3점(꼭짓점·사지점·몸통점)을 만들고 None 이면 `user_gate`/`ref_gate`.
  방향 관절 선언 = `ANGLE_BAKE_MAP`(1951행): shoulder→(elbow,hip),
  hip→(knee,shoulder), knee→(ankle,hip), elbow→(hand,shoulder).
- align 단일 출처 공식 (app.py 4730-4752 정본): `ai = clamp(round(sec*afps))`,
  `afps = float(align["fps"])`(4450행), 좌표 = `cg.kp(u/rrep15, ai, joint,
  conf_min=_fz._KP_CONF_MIN)` — None(conf<0.5) 이면 fail-closed drop.
  `urep15/rrep15 = cg.align_to_report(align, side)`.
- `card_gates.NAME_ALT`(71-72행)가 wrist↔hand 명명을 이미 처리 — align 17-kp
  에서 elbow 사지점(`{side}_hand`) 조회 시 별도 매핑 발명 금지, cg.kp 에 맡길 것.
- `_da_applied` 카드의 V 꼭짓점은 어차피 display_anchor(= align crit 관절 좌표)
  로 shift 된다(3455-3460행 + `shift_bake_spec` 2002행) — B 스펙 규칙은
  "꼭짓점 = align crit 관절 좌표, 사지·몸통점 = align 의 ANGLE_BAKE_MAP 선언
  관절 좌표" 로 승인 문법과 정합.
- 침묵 2건(elbow r03, powerspin r02)의 자리 = `_member_pts` nValid 양측 0 →
  build 내 무로그 skip (ivs measure.json memberPts 실측). display_anchor 로그는
  성립했으므로 align 좌표는 존재 — B 회복 가설 대상.
- elbow r01 은 display_anchor drop side=user (align conf 자체 미달) — B 도
  align 출처라 회복 불가가 예상값. 실측 conf 값 기록이 이 케이스의 산출물.
- 반려 2건의 freeze 정본 = sweep_verdict.json: left_knee u3.667/r2.4,
  left_elbow u8.556/r9.4. 둘 다 ref 측 crop relaxed(무마크) 카드였음.
- 명시 짝 지정 선례 = pdshapefault r01 `pairSrc: "override"` refSec 0.8 —
  회복 후보 확정 시 이 경로로 다음 사이클 반영 (이 사이클은 판정 재료까지만).
- ivs 캐시는 구세션 scratchpad 라 사망 — 영상 6편 S3 재fetch + refmotion
  Firestore 재fetch 1회 발생. env: `AWS_PROFILE=sunity-motion` +
  `FIREBASE_SA_PATH=<repo>/firebase-sa.json`. A/B 사이는 같은 캐시 재사용.
- ivs pngMd5 정본에는 눈 스텁 PNG(`NN_user_*.png`)도 섞여 있음 — A 대조는
  `zoom_*.png` 키만.
</context>

<tasks>

<task type="auto">
  <name>Task 1: A/B 드라이버 신설 + B(align 유도 스펙) 재렌더 + 전/후 시트 + 소생 V 육안 박제</name>
  <files>.planning/quick/260813-m0k-1-v-bake-align-a-b-2/ab_render.py, .planning/quick/260813-m0k-1-v-bake-align-a-b-2/evidence/, .planning/quick/260813-m0k-1-v-bake-align-a-b-2/AB-EYE-VERDICT.md</files>
  <action>
    `ab_render.py` 신설 (backend/·기존 하네스 원본 sweep_render.py·
    verify_wiring.py·verify_local.py·grammar_round.py 전부 무수정 — locked).
    ivs `sweep_render.py` 의 구조(grammar_round importlib 상속 + vl.EV/SPA
    재지정 + `_guard_ev` "260813-m0k" assert + 관찰 래퍼 + BLOCKER 처리)를
    이 파일 안에 복제·확장한다.

    `--ab` 스테이지 = A 런 → B 런 순차 (같은 프로세스, 캐시 공유):

    A 런 (무패치 동치 게이트): vl.sweep() 그대로 1회, 카드를
    `evidence/ab_cards/A/{motion}/` 에. `zoom_*.png` md5 를 ivs
    sweep_verdict.json pngMd5 정본과 전건 대조 — **불일치 시 evidence/
    BLOCKER.md 박제 + 비영 종료** (기반이 흔들린 채 B 를 재면 무의미).

    B 런 (하네스 monkeypatch 2 seam — 원본 파일 무수정, 함수 속성 교체만):
    1) `fz.build_angle_bake_spec` 래핑: 원본 호출 결과가 None 이고 criterion
       접미사가 ANGLE_BAKE_MAP 선언이면, align 유도 스펙 시도 — 현재 동작의
       align 데이터에서 `rep15 = cg.align_to_report(align, side)`,
       `ai = clamp(round(freeze_sec * afps))` (app.py 4733-4737 공식 그대로,
       side 판별은 report 객체 identity 로 user/ref 매핑 — 드라이버가 동작별
       user_report/ref_report 참조를 잡아둔다), 3점 = cg.kp(rep15, ai,
       crit관절/사지관절/몸통관절, conf_min=0.5). **한 점이라도 None 이면
       None 유지 (정직한 침묵) + 그 관절별 conf 실측값을 관측 기록**
       (cg.kp 를 conf_min=0.0 으로 재조회해 실값 채록). 원본이 스펙을 준
       카드는 무개입 (기존 성립 경로 보존).
    2) `fz._member_pts` 래핑: rep12 valid 0 인 호출에서 align 유도 점들
       (같은 freeze ai, conf>=0.5)로 폴백 — ref crop relaxed 2건(pdshape
       r00/r03)과 침묵 2건(elbow r03, powerspin r02)의 crop 성립 회복 가설.
       members 의 각 관절을 cg.kp 로 조회, conf 미달 관절은 제외 (발명 금지).
       ※ 실행 전 `_member_pts` 원본(fault_zoom.py)을 열어 반환 계약
       (valid, relaxed) 을 확인하고 폴백 점을 valid 자리에 넣을 것 —
       relaxed 자리 주입은 의미가 다르다.
    B 카드는 `evidence/ab_cards/B/{motion}/` 에. 관측: 카드별 bake 로그
    (`fault_zoom_angle_bake` drawn/omitted:사유), drawCalls, display_anchor
    args, seam 발화 여부(1/2 어느 쪽), 관절별 align conf 실측값.

    산출 `evidence/ab_verdict.json`: 동작 x rid 별 {A 상태(bake 사유),
    B 상태, 발화 seam, alignConf(관절별 u/r 실값), mdA, mdB, 회복 여부} +
    survivors/dropped 3원 대조(ivs==A==B). `evidence/ab_cards/sheets/` 에
    카드별 A|B 나란히 시트 PNG 합성 (전/후 시트 — 시트는 대조용 합성일 뿐,
    육안 판정은 원본 카드 Read 로).

    AB-EYE-VERDICT.md: **B 에서 새로 살아난 V 카드 전부 + A 대비 변화가 있는
    카드 전부를 Read 도구로 원본 크기로 열어** (몽타주/축소본 검수 금지)
    카드별 사전 박제 — V 꼭짓점이 관절 위인가, 사지/몸통 가닥 방향이 실물
    포즈와 맞는가, ref 무마크였던 카드에서 ref V 가 성립했는가, 살았지만
    틀린 좌표(환각)인가. **옳게 살아났는지가 판정 대상** — 틀리게 살아난
    케이스는 그대로 박제 (수정·튜닝 0, belle 판정 재료). 각도 도 수치 금지
    (저각/가림 등 범주 표현만). 마크 길이·위치 튜닝 금지 (별건).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && AWS_PROFILE=sunity-motion FIREBASE_SA_PATH=$PWD/firebase-sa.json python3 .planning/quick/260813-m0k-1-v-bake-align-a-b-2/ab_render.py --ab && python3 -c "
import json
d='.planning/quick/260813-m0k-1-v-bake-align-a-b-2/evidence/'
ab=json.load(open(d+'ab_verdict.json'))
iv=json.load(open('.planning/quick/260813-ivs-5/evidence/sweep_verdict.json'))
assert ab['aMatchesIvs'] is True, 'A != ivs 정본'
for m in iv:
    assert ab['survivors'][m]['ivs']==ab['survivors'][m]['A']==ab['survivors'][m]['B'], m
unrec=[c for c in ab['cards'] if c.get('bStatus','').startswith('omitted') and c.get('seamTried')]
assert all(c.get('alignConf') for c in unrec), 'B 미회복 케이스 conf 실측 누락'
print('AB PASS cards=%d recovered=%d'%(len(ab['cards']),sum(1 for c in ab['cards'] if c.get('recovered'))))" && git diff --stat --exit-code backend/ .planning/quick/260813-ivs-5/sweep_render.py .planning/quick/260813-fxx-belle-3-p3r1-pass-v-p3-align-fps-5-pytes/verify_wiring.py .planning/quick/260811-ufb-freeze-only/verify_local.py .planning/quick/260811-xa1-mark-grammar-round-ufb-freeze-2-belle/grammar_round.py</automated>
  </verify>
  <done>A 런 zoom md5 == ivs 정본 전건, survivors/dropped ivs==A==B, B 카드·전/후 시트·ab_verdict.json(관절별 align conf 실측 포함) 존재, B 소생/변화 카드 전수 육안 사전 박제(AB-EYE-VERDICT.md), backend/·하네스 원본 diff 0, Gemini 실호출 0.</done>
</task>

<task type="auto">
  <name>Task 2: 반려 2건 짝 국면 회복 후보 탐색 + 스틸 렌더 + 추천 사전 박제</name>
  <files>.planning/quick/260813-m0k-1-v-bake-align-a-b-2/evidence/pair_candidates.json, .planning/quick/260813-m0k-1-v-bake-align-a-b-2/evidence/pair_stills/, .planning/quick/260813-m0k-1-v-bake-align-a-b-2/PAIR-CANDIDATES.md</files>
  <action>
    ab_render.py 에 `--pairs` 스테이지 추가 (같은 파일 — 데이터 로드·캐시
    공유). 대상 = 피디쉐입 왼무릎(user freeze u3.667s)·왼팔꿈치(u8.556s).

    후보 탐색: user freeze 순간의 align 포즈(u_ai = round(3.667*afps) 등)를
    기준으로 **ref align 시퀀스 전 프레임**과 포즈 거리 랭킹.
    거리 규칙 (1줄 명기해 재현 가능하게): 양측 conf>=0.5 공통 관절만,
    root = 양 hip 중점 중심화, scale = torso 길이(hip 중점↔shoulder 중점)
    정규화, 전신 = 공통 관절 L2 평균. 부위 국소 거리(crit 관절 +
    ANGLE_BAKE_MAP 사지·몸통 관절만)도 같이 기록 — 랭킹은 전신, 국소는
    참고 열. 후보 = 전신 랭킹 상위에서 **상호 0.5s 이상 시간 분산**을
    지키며 2~3개 greedy 선별. baseline 행 = 현행 반려 짝(r2.4 / r9.4)의
    같은 거리 — "얼마나 멀었길래 반려됐나"의 수치 맥락.
    산출 `evidence/pair_candidates.json`: joint 별 {baseline, candidates:
    [{refSec, refAi, fullBody, limb}], recommend}.

    스틸 렌더: 후보별 user freeze 프레임 | ref 후보 프레임 **전신 나란히**
    합성 PNG → `evidence/pair_stills/` (l0u 전신 프레임 짝 선례 — 장면
    화면감 판정용이므로 크롭 아님). 프레임은 vl 캐시 영상에서 초→프레임
    환산으로 추출. 추가 관측: 각 후보 순간에 Task 1 의 B 스펙(align 유도)
    으로 ref V 가 성립하는지 — cg.kp conf 판정만 기록 (렌더 재실행 불요,
    반려 2건이 ref 무마크였던 것의 회복 여부 판정 재료).

    PAIR-CANDIDATES.md: 후보 스틸 전부 Read 로 열어 육안 대조 후 joint 별
    **내 추천 1안 사전 박제** (근거 = 포즈 거리 수치 + 육안 소견 — 포즈
    거리 수치는 내부 판정 재료라 기록 OK, 각도 도 수치는 금지). 그리고
    **구조 구분 명기 (필수)**: 이 산출물은 재정박 부활이 아니다 — 운영
    코드 무접촉, belle 이 장면을 확정하기 위한 판정 재료이며, 확정 시
    다음 사이클에서 r01 선례(pairSrc=override, 0.8s 명시 지정)와 같은
    **명시 짝 지정(pair-override) 경로**로 반영한다. 후보가 baseline 보다
    못하거나 전 구간에 정합 순간이 없으면 그 사실 그대로 박제 (억지 추천
    금지 — recommend-only-after-measuring).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && python3 -c "
import json,pathlib
d=pathlib.Path('.planning/quick/260813-m0k-1-v-bake-align-a-b-2')
pc=json.load(open(d/'evidence/pair_candidates.json'))
assert set(pc)=={'left_knee','left_elbow'}, sorted(pc)
for j,v in pc.items():
    cs=v['candidates']; assert 2<=len(cs)<=3, (j,len(cs))
    secs=sorted(c['refSec'] for c in cs)
    assert all(b-a>=0.5 for a,b in zip(secs,secs[1:])), (j,'시간 분산 미달')
    assert 'baseline' in v and v.get('recommend') is not None, j
stills=list((d/'evidence/pair_stills').glob('*.png'))
n=sum(len(v['candidates']) for v in pc.values())
assert len(stills)>=n>0, (len(stills),n)
t=(d/'PAIR-CANDIDATES.md').read_text()
assert '재정박' in t and 'override' in t, '구조 구분 명기 누락'
print('PAIRS PASS candidates=%d stills=%d'%(n,len(stills)))"</automated>
  </verify>
  <done>joint 2건 각각 후보 2~3개(상호 0.5s 이상 분산) + baseline 거리 + 전신 나란히 스틸 실물 + 후보 순간 ref V 성립 여부 관측 + 추천 1안 육안 사전 박제, "재정박 아님 — pair-override 경로" 구분이 PAIR-CANDIDATES.md 에 명기.</done>
</task>

<task type="auto">
  <name>Task 3: 커버리지 표 + /Users/Shared 한글 사본 + SUMMARY 보드 재료 + 커밋</name>
  <files>.planning/quick/260813-m0k-1-v-bake-align-a-b-2/COVERAGE.md, /Users/Shared/sunity-finetune-260813/, .planning/quick/260813-m0k-1-v-bake-align-a-b-2/260813-m0k-SUMMARY.md</files>
  <action>
    코드 실행 없음 — Task 1·2 산출물만 출처 (수치 손 재유도·발명 금지).

    COVERAGE.md — 승인 5동작 감점 record 전수 표 (13 rid: elbow r00~r03,
    kipup r00, pdshapefault r00~r03, peterpan r00, powerspin r00~r02):
    rid 별 [현행 상태(방출/침묵+사유 — ivs 정본 인용), B 회복 여부(ab_verdict
    실측), 회복 불가 시 사유(align conf 실값 포함 — "좌표가 진짜 없는 프레임
    = 정직한 침묵 유지가 옳다"의 증거), 반려 2건은 짝 후보 상태 링크].
    침묵 3건(elbow r01 display_anchor drop / elbow r03·powerspin r02 rep12
    양측 신뢰 0)의 행에는 conf 실측값과 B 결과를 반드시 채운다 — belle
    커버리지 질문("일부만 나타낸 것 같아서")의 실측 답이 이 표다.

    /Users/Shared/sunity-finetune-260813/ 에 한글 파일명 사본: B 소생/변화
    카드 + 전/후 시트 + 짝 후보 스틸 (`{동작한글}_{관절한글}_{구분}.png`
    형식, 동작한글 매핑 ivs 선례: elbow=엘보트위스트, pdshapefault=피디쉐입,
    peterpan=피터팬, powerspin=파워스핀, kipup=킵업). 눈 스텁 산출물 제외.

    260813-m0k-SUMMARY.md (quick 템플릿):
    - 한 줄 + Task 별 핵심 실측 (V 소생 수/옳게 소생 수, 반려 2건 추천,
      커버리지 집계)
    - 보드 게시 재료 절: [이미지 절대경로(/Users/Shared 사본) + 캡션] —
      캡션에 각도 도 수치 금지·이모지 금지, 게시는 오케스트레이터 몫
    - **구조 구분 절 (필수)**: Task 2 는 재정박 부활 아님 — 판정 재료,
      확정 시 pair-override 경로 다음 사이클 / Task 1 B 는 하네스 A/B 재료,
      운영 배선은 belle 채택 후 별도 사이클
    - LLM 학습 영향 절 (필수): Gemini 실호출 0 — machine_eye 스텁, 학습
      전송 0, 눈 원장 신규 적재 0
    - 한계 박제: 눈 스텁이라 08-11 실눈 기각 이력 record 2건이 이번에도
      방출됨(문법 재료로만), 마크 길이·위치 튜닝 0(별건), ÷9.0 표기 잔존
      (kpo 유보), Pod 실증 범위 밖, 채점 무접촉(survivors/dropped 3원 동일이
      증거)
    커밋: `gsd-sdk query commit "docs(quick-260813-m0k): V 베이크 align A/B + 반려 짝 후보 + 커버리지 실측" --files .planning/quick/260813-m0k-1-v-bake-align-a-b-2`
    (/Users/Shared 는 리포 밖 — 커밋 대상 아님, scratchpad 캐시 커밋 금지).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && python3 -c "
import json,pathlib,re
d=pathlib.Path('.planning/quick/260813-m0k-1-v-bake-align-a-b-2')
cov=(d/'COVERAGE.md').read_text()
rids=[('elbow',4),('kipup',1),('pdshapefault',4),('peterpan',1),('powerspin',3)]
for m,n in rids:
    assert len(re.findall(rf'{m}.*r0\d', cov))>=n, (m,n)
for tok in ['display_anchor','conf']:
    assert tok in cov, tok
shared=list(pathlib.Path('/Users/Shared/sunity-finetune-260813').glob('*.png'))
assert len(shared)>0
s=(d/'260813-m0k-SUMMARY.md').read_text()
for tok in ['/Users/Shared/sunity-finetune-260813','LLM 학습','재정박','override']:
    assert tok in s, tok
print('COVERAGE PASS shared=%d'%len(shared))" && git log --oneline -5 | grep -q "quick-260813-m0k"</automated>
  </verify>
  <done>COVERAGE.md 에 13 rid 전수 행(침묵 3건 conf 실값 + B 결과 포함), /Users/Shared/sunity-finetune-260813/ 한글 사본 존재, SUMMARY 에 보드 재료·구조 구분·LLM 학습 영향·한계 박제, m0k 디렉터리 커밋 완료.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로컬 드라이버 -> S3/Firestore | 승인 코퍼스 영상·refmotion 읽기 (읽기 전용 자격) |
| 로컬 드라이버 -> Gemini | 경계 자체를 스텁으로 차단 (실호출 0) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-m0k-01 | Information Disclosure | AWS/Firebase 자격 | mitigate | env 주입만, 키 값 로그·커밋 금지 (ivs T-ivs-01 선례) |
| T-m0k-02 | Tampering | ivs/ufb/xa1 evidence | mitigate | vl.EV 재지정 + "260813-m0k" assert 가드 — 타 사이클 evidence 파괴 fail-closed |
| T-m0k-03 | Spoofing | Gemini 호출 | mitigate | grammar_round machine_eye 스텁 + 더미 키 상속 — eyeStubCalls 기계 확인 |
| T-m0k-04 | Tampering | 운영 코드/기존 하네스 | mitigate | monkeypatch 는 드라이버 프로세스 내 함수 속성 교체만 — git diff --exit-code 게이트 (backend/ + 하네스 원본 4파일) |
| T-m0k-SC | Tampering | 패키지 설치 | accept | 신규 설치 0 (기존 backend venv/의존만) |
</threat_model>

<verification>
- `ab_render.py --ab` exit 0: A md5 == ivs 정본(zoom_*.png 전건) +
  survivors/dropped ivs==A==B + B 미회복 케이스 conf 실측 전건.
- `ab_render.py --pairs` 산출: 후보 2~3 x 2 joint + 0.5s 분산 + baseline +
  recommend + 스틸 실물.
- git diff 0: backend/ + sweep_render.py + verify_wiring.py + verify_local.py
  + grammar_round.py.
- 육안 사전 박제 2건 (AB-EYE-VERDICT.md, PAIR-CANDIDATES.md) — 원본 카드/스틸
  Read, 몽타주 검수 금지.
- Gemini 실호출 0 (스텁 calls 기록만). 채점 무접촉 (게이트 판정 3원 동일).
</verification>

<success_criteria>
- V 미베이크 5/8 각각에 대해 "B 에서 살았는가 + 옳게 살았는가(육안) + 못 살면
  conf 실값" 이 실물과 수치로 존재 — belle V 이관 채택 판정 재료 완비.
- 반려 2건 각각 짝 후보 2~3개 스틸 + 내 추천 1안 사전 박제 + 재정박 아님
  구분 명기 — belle 장면 확정 재료 완비.
- 커버리지 표 13 rid 전수 — belle "모든 확대 비교?" 질문에 실측으로 답 가능.
- /Users/Shared/sunity-finetune-260813/ 한글 사본 + SUMMARY 보드 재료, 커밋 완료.
- 운영 코드·채점·마크 튜닝 전부 무접촉, Gemini 실호출 0.
</success_criteria>

<output>
Create `.planning/quick/260813-m0k-1-v-bake-align-a-b-2/260813-m0k-SUMMARY.md` when done (Task 3 산출물이 곧 SUMMARY).
</output>
