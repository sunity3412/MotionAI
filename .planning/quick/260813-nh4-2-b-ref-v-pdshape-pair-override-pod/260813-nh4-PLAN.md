---
phase: quick-260813-nh4
quick_id: 260813-nh4
slug: b-spec-port-ref-v-fix-pair-override-pod
date: 2026-08-13
status: planned
description: 운영 배선 2차 — belle 판정 박제(B 채택·왼팔꿈치 ref V 보정 지시·왼무릎 3.9s 반려) + V 베이크 align 스펙(B) 운영 이식 + 왼팔꿈치 ref V 앵커 진단·보정 + 왼무릎 확정 장면 content-match·pair override 로컬 재렌더(S3 쓰기 0) + Pod 실증(B 스펙)
wave: 1
depends_on: []
type: execute
plan: 01
autonomous: true
requirements: [QUICK-260813-NH4]
files_modified:
  - .planning/quick/260811-xa1-mark-grammar-round-ufb-freeze-2-belle/JUDGMENT.md
  - backend/shared/python/sunity_shared/analysis/fault_zoom.py
  - backend/functions/pipeline/app.py
  - backend/tests/test_fault_zoom_display_repair.py
  - .planning/quick/260813-nh4-2-b-ref-v-pdshape-pair-override-pod/verify_port.py
  - .planning/quick/260813-nh4-2-b-ref-v-pdshape-pair-override-pod/evidence/
  - .planning/quick/260813-nh4-2-b-ref-v-pdshape-pair-override-pod/staging/
  - .planning/quick/260813-nh4-2-b-ref-v-pdshape-pair-override-pod/260813-nh4-SUMMARY.md
must_haves:
  truths:
    - "belle 08-13 판정 3건이 xa1 JUDGMENT.md 에 원문 인용 그대로 append (삭제 라인 0): ① B 스펙 채택('나머진 오케이다') ② 왼팔꿈치 ref V 위치 보정 지시('오른쪽(정은지) 각도 표기의 위치가 좀 애매하고') ③ 왼무릎 3.867s 추천 반려 원문 전체 + 사전 박제 대조 '불일치' 기록 + 교훈 박제(짝은 기술 요소 정체성/시퀀스 순서가 우선, 포즈 유사도는 보조 — 포즈 거리 랭킹이 닮은 다른 요소를 골랐다)"
    - "B 스펙이 운영 코드에 이식됐다 — fault_zoom V 베이크 스펙이 rep12 미성립 시 게이트 freeze 순간의 align 17-kp 에서 유도(fail-closed conf>=0.5, 미달 = 정직한 침묵 + conf 실값 로그), 미지정/기본값 경로는 byte-동일, 동작명·분석 ID 분기 0, 채점 무접촉(산식 5파일 diff 0)"
    - "운영 이식이 m0k B 인증값을 재현한다 — 승인 5동작 스윕(monkeypatch 없이 운영 경로)에서 소생 6/6(elbow r00·r03, pdshape r00·r02·r03, powerspin r02) + elbow r01 drop 유지(align conf 0.229~0.429) + survivors/dropped ivs 정본 전건 동일 + 카드 8→10 + 소생 카드 육안 전수(환각 0)"
    - "왼팔꿈치(피디쉐입 r9.4s) ref V 진단이 실측으로 판정됐다 — ref left_elbow align 좌표·conf 실값 + 실제 팔꿈치 픽셀 육안 대조 → (a) 좌표 어긋남이면 원인 수리 (b) 좌표 정확 + 시각 모호면 기존 문법 요소 내 최소 보정. 새 문법 발명 0, 마크 길이·크기 튜닝 0, 보정 전/후 렌더 실물 존재"
    - "왼무릎 확정 장면 refSec 이 content-match 로 확정됐다(belle-confirmed-scene-8.png 표시 초 00:04 불신 — 픽셀 대조가 판정자) — 확정 시 pair override 로컬 재렌더 + 리그 ALL PASS + diff 왼무릎 짝 국한 + 재렌더 mp4·확정 짝 스틸 스테이징. top-1 분리 불명확 또는 2초대/4초대 판정 갈림이면 해석 금지 — 양쪽 후보 스틸 스테이징 + 명기"
    - "S3 쓰기 0 (스테이징만 — belle 복귀 확인 후 업로드는 별도 1단계), AWS 인프라 무변경"
    - "Pod mddy6gsqmt24ud 실증 — push 후 git pull + 서버 재기동(setsid nohup 표준), /health commitSha == 새 HEAD, fresh 재분석 1회 = 점수 60 유지 + display_anchor·angle_bake 운영 로그 실물 + 카드 회수 육안(왼팔꿈치 카드 B 스펙 V 반영)"
    - "pytest 기준선 59 failed 동일(신규 실패 0) + 승인 무회귀(joint-scope hold 9/9 + pair 9/9) + 로컬 검증 Gemini 실호출 0(스텁), Pod 재분석 운영 경로만 실호출 — SUMMARY 에 LLM 학습 영향 필수 기재"
  artifacts:
    - path: ".planning/quick/260811-xa1-mark-grammar-round-ufb-freeze-2-belle/JUDGMENT.md"
      provides: "belle 08-13 판정 3건 append (원문 인용 + 사전 박제 대조 + 교훈)"
    - path: "backend/shared/python/sunity_shared/analysis/fault_zoom.py"
      provides: "B 스펙 운영판 — align 유도 V 베이크 스펙 + valid-0 align 폴백 (기본값 byte-동일)"
    - path: "backend/functions/pipeline/app.py"
      provides: "_run_gated_card_inherit 에서 align freeze 좌표 산출·전달 확장 (기존 u_ai/r_ai 공식 재사용)"
    - path: ".planning/quick/260813-nh4-2-b-ref-v-pdshape-pair-override-pod/verify_port.py"
      provides: "운영 이식 재현 게이트 — 승인 5동작 스윕(운영 경로) vs m0k B 인증값 기계 대조"
    - path: ".planning/quick/260813-nh4-2-b-ref-v-pdshape-pair-override-pod/evidence/elbow_ref_v_diagnosis.json"
      provides: "왼팔꿈치 ref V 진단 실측 — align 좌표·conf·육안 판정·(a)/(b) 분기 근거"
    - path: ".planning/quick/260813-nh4-2-b-ref-v-pdshape-pair-override-pod/evidence/frame_match.json"
      provides: "왼무릎 확정 장면 content-match 결과 — 후보 diff 전 구간 + top-1 분리 판정"
    - path: ".planning/quick/260813-nh4-2-b-ref-v-pdshape-pair-override-pod/staging/"
      provides: "재렌더 mp4 + 확정 짝 스틸 (또는 모호 시 양쪽 후보 스틸 + 명기) — S3 업로드 보류분"
    - path: ".planning/quick/260813-nh4-2-b-ref-v-pdshape-pair-override-pod/evidence/pod/"
      provides: "Pod 실증 — /health·재분석 로그·회수 카드·점수 대조"
    - path: ".planning/quick/260813-nh4-2-b-ref-v-pdshape-pair-override-pod/260813-nh4-SUMMARY.md"
      provides: "보드 게시 재료 + LLM 학습 영향 + 한계 박제"
  key_links:
    - from: "backend/functions/pipeline/app.py"
      to: "backend/shared/python/sunity_shared/analysis/fault_zoom.py"
      via: "build_fault_zoom_comparisons keyword-only align 좌표 전달 (display_anchor 선례 확장, 기본 None = byte-동일)"
      pattern: "build_fault_zoom_comparisons"
    - from: "backend/shared/python/sunity_shared/analysis/fault_zoom.py"
      to: ".planning/quick/260813-m0k-1-v-bake-align-a-b-2/ab_render.py"
      via: "_BPatch seam 2개(_bake 313행 / _members 361행)의 운영판 — 로직 동치, monkeypatch 아님"
      pattern: "build_angle_bake_spec"
    - from: ".planning/quick/260813-nh4-2-b-ref-v-pdshape-pair-override-pod/staging/"
      to: "backend/scripts/render_compare_prototype.py"
      via: "--pair-override-json rid→{refVideoSec, note} (260808-epy r01 refSec 0.8 선례, pairSrc=override)"
      pattern: "pair-override-json"
---

<objective>
belle 08-13 판정 3건을 장부에 박제하고, m0k 하네스가 인증한 B 스펙(V 베이크
align 단일 출처)을 운영 코드로 이식하며, belle 이 지시한 왼팔꿈치 ref V 위치
진단·보정과 왼무릎 확정 장면 pair override 로컬 재렌더(스테이징)를 수행한 뒤,
가동 중인 Pod 에서 B 스펙 배선을 실증한다.

핵심 규율: ① 장부는 원문 인용 append-only — 내 3.867s 추천 반려는 불일치로
그대로 남긴다(교훈 = 짝은 기술 요소 정체성 우선). ② 운영 이식의 완료 기준은
"m0k B 인증값 재현"이지 새 판정이 아니다 — 소생 6/6·elbow r01 정직한 침묵·
survivors 불변이 그대로 나와야 한다. ③ 왼무릎 확정 초는 스크린샷 표시 초가
아니라 content-match 픽셀 대조가 판정자다 — 모호하면 해석하지 말고 양쪽 후보를
재료로 스테이징한다. ④ S3 쓰기 0 — 업로드는 belle 복귀 확인 후 별도 1단계.

Purpose: belle 채택분(B 스펙)을 운영 경로에 성립시키고(Pod 로그+카드 실물이
증거), 반려분(왼무릎 짝)은 belle 확정 장면 그대로 반영한 실물을 판정대에 올린다.
Output: JUDGMENT append + fault_zoom/app.py B 배선 + 재현 게이트 기계 판정 +
왼팔꿈치 진단·보정 전/후 렌더 + 왼무릎 스테이징(mp4+스틸) + Pod 실증 로그·카드.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260813-m0k-1-v-bake-align-a-b-2/260813-m0k-SUMMARY.md
@.planning/quick/260813-m0k-1-v-bake-align-a-b-2/ab_render.py
@.planning/quick/260813-fxx-belle-3-p3r1-pass-v-p3-align-fps-5-pytes/260813-fxx-SUMMARY.md
@.planning/quick/260811-xa1-mark-grammar-round-ufb-freeze-2-belle/JUDGMENT.md
@.planning/quick/260813-l0u-sweep-verdict-lshoulder-frames/recover_frames.py
@.planning/quick/260808-epy-phase-35-2-p35-pdshape-r01-5-v7/260808-epy-SUMMARY.md
@backend/shared/python/sunity_shared/analysis/fault_zoom.py
@backend/functions/pipeline/app.py
@backend/scripts/render_compare_prototype.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: belle 판정 장부 박제 + B 스펙 운영 이식 + m0k 재현 게이트</name>
  <files>.planning/quick/260811-xa1-mark-grammar-round-ufb-freeze-2-belle/JUDGMENT.md, backend/shared/python/sunity_shared/analysis/fault_zoom.py, backend/functions/pipeline/app.py, backend/tests/test_fault_zoom_display_repair.py, .planning/quick/260813-nh4-2-b-ref-v-pdshape-pair-override-pod/verify_port.py, .planning/quick/260813-nh4-2-b-ref-v-pdshape-pair-override-pod/evidence/</files>
  <action>
    (1) 장부 박제 — xa1 JUDGMENT.md 에 "미세조정 1차 판정 — belle 08-13 (재료 =
    quick-260813-m0k)" 절을 append-only 로 추가 (기존 라인 삭제 0). 내용:
    ① B 스펙(V 베이크 align 단일 출처) 채택 — A/B 6/6 소생 승인, belle 원문
    "나머진 오케이다" ② 왼팔꿈치 ref V 위치 보정 지시 — belle 원문 "오른쪽(정은지)
    각도 표기의 위치가 좀 애매하고" (대상 = 피디쉐입 왼팔꿈치 r9.4s ref 패널 V,
    진단 먼저·새 문법 발명 금지) ③ 왼무릎 짝 3.867s 추천 = 반려 — belle 교정
    원문 전체 인용("실제 영상 기준에서 동작이 다르다. 후보3처럼 다리를 접는
    순간은(정은지 기준) 4초 정도임(다리를 뻗고 한두바퀴 돈 다음에 하는 동작).
    후보 1이 2초에 나오는 동작. 학생 영상보면 2초대에 나오는 동작 같아서 후보3은
    어울리지 않음. 어울리는 건 이때임 [스크린샷 = 본 사이클 dir
    belle-confirmed-scene-8.png]"). 사전 박제 대조 누적에 "왼무릎 3.867s 추천 =
    불일치(기각)" 기록 + 교훈 박제: 짝은 기술 요소 정체성(시퀀스 순서)이 우선,
    포즈 유사도는 보조 — 포즈 거리 랭킹이 닮은 다른 요소를 골랐다. 커밋 1
    (docs(quick-260813-nh4)).

    (2) B 스펙 운영 이식 — m0k ab_render.py `_BPatch` 2 seam(313행 `_bake` /
    361행 `_members`)의 로직을 monkeypatch 가 아니라 운영 코드로 이관.
    구현 계약 (형태는 fxx display_anchor 선례를 따르되 실물 대조로 결정):
    - fault_zoom: `build_fault_zoom_comparisons` 에 keyword-only 파라미터로
      align freeze 좌표 payload 를 받는다 (기본 None = 전 경로 byte-동일 —
      기존 fault_zoom 테스트 무접촉 PASS 가 증명해야 함). 지정 시:
      (a) `build_angle_bake_spec` 이 None 이고 vertex 접미사가 `ANGLE_BAKE_MAP`
      선언에 있으면 align 유도 3점 스펙(꼭짓점 = crit 관절, 사지·몸통 =
      ANGLE_BAKE_MAP 선언 관절)으로 V 를 그린다. (b) `_member_pts` valid 0 인
      패널은 align 점들을 valid 자리에 폴백(relaxed 자리 주입 금지 — 의미가
      다름, m0k docstring 근거)해 ref 전신 무마크를 부위 크롭+V 로 회복한다.
      align 유도 스펙은 이미 align 좌표라 display_anchor `shift_bake_spec`
      이중 적용 금지 (꼭짓점==앵커라 shift 가 0 이 되는 구조를 대조로 확인).
    - app.py `_run_gated_card_inherit`: 기존 display_anchor 블록(4730~4759행)의
      u_ai/r_ai 공식과 `cg.kp(conf_min=_fz._KP_CONF_MIN)` 을 재사용해 vertex +
      ANGLE_BAKE_MAP 선언 관절 + unit member 관절의 양측 align 좌표를 산출·전달.
      NAME_ALT 는 wrist→hand(rep12) 단방향 — align17 조회는 역방향(hand→wrist)
      정규화 (m0k Deviations 실측, 신규 매핑 발명 0). 한 점 미달 = 그 점만 결측
      (V 는 3점 전부 성립 시만, 미성립 = 정직한 침묵 + conf 실값 로그 —
      기존 `fault_zoom_angle_bake` 로그 규약 유지).
    - 금지: 동작명/분석 ID 리터럴 분기 0, 마크 길이·크기 튜닝 상수 신설 0,
      채점 산식 5파일(deduction_engine/dimensions/kismam/motiondtw/assemble)
      무접촉, rep12 성립 카드 무개입 (원본 스펙 우선 — seam 1 은 None 일 때만).
    - backend/tests/test_fault_zoom_display_repair.py 에 신규 테스트 추가:
      기본값 byte-동일 / align 유도 스펙 성립 / 3점 중 1점 미달 = V 생략 /
      valid-0 align 폴백 성립·relaxed 미주입. 커밋 2 (feat), 커밋 3 (test).

    (3) m0k 재현 게이트 — verify_port.py 신설: ivs/m0k 스윕 하네스 구조(m0k
    ab_render.py 의 모듈 로드·스텁·캐시 경로 상속 — 같은 세션 scratchpad
    /private/tmp/claude-501/.../ae166167.../scratchpad 재사용)로 승인 5동작
    스윕을 **monkeypatch 없이 운영 경로 그대로** 실행하고 m0k B 인증값과 기계
    대조: 소생 6건(elbow r00·r03, pdshape r00·r02·r03, powerspin r02) 카드
    성립 + elbow r01 drop 유지(align conf 실값 0.229~0.429 로그) + powerspin
    r00·kipup r00 침묵 유지(unmapped/legs_owned) + survivors/dropped == ivs
    정본 전건 + 비대상 카드 무변동 + 카드 총수 8→10. 1차 대조 = zoom md5 ==
    m0k evidence/sweep_verdict_B.json(로직 동치면 픽셀 동일 기대); 불일치 시
    숨기지 말고 좌표·bake outcome 동일성으로 판정 + 원인 실측 명기. 소생 카드
    6장 전수 Read 육안(꼭짓점 관절 위·환각 0 — frames-before-numbers).
    Gemini 실호출 0 (grammar_round machine_eye 스텁 + 더미 키 상속).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && backend/.venv/bin/python .planning/quick/260813-nh4-2-b-ref-v-pdshape-pair-override-pod/verify_port.py (exit 0 = m0k B 재현 전건 PASS) && PYTHONPATH=backend/tests backend/.venv/bin/python -m pytest -q backend/tests (59 failed 기준선 동일) && git diff --stat HEAD~3 -- backend/shared/python/sunity_shared/analysis/deduction_engine.py backend/shared/python/sunity_shared/analysis/dimensions.py backend/shared/python/sunity_shared/analysis/kismam.py backend/shared/python/sunity_shared/analysis/motiondtw.py backend/shared/python/sunity_shared/analysis/assemble.py (빈 출력 = 산식 무접촉)</automated>
  </verify>
  <done>JUDGMENT.md append 완료(원문 3건 + 불일치 + 교훈, 삭제 라인 0). B 스펙 운영 배선 커밋 존재. verify_port.py 기계 판정 PASS: 소생 6/6 + elbow r01 침묵 유지 + survivors/dropped ivs 전건 동일 + 카드 8→10 + 소생 카드 육안 전수 기록(evidence/). pytest 59 failed 동일. 산식 5파일 diff 0. 배선 추가 라인 동작명/분석 ID 리터럴 grep 0. 승인 무회귀(fxx verify_wiring.py 의 vl.approved 위임 경로 재사용 — hold 9/9 + pair 9/9).</done>
</task>

<task type="auto">
  <name>Task 2: 왼팔꿈치 ref V 진단·보정 + 왼무릎 content-match·pair override 스테이징</name>
  <files>.planning/quick/260813-nh4-2-b-ref-v-pdshape-pair-override-pod/evidence/elbow_ref_v_diagnosis.json, .planning/quick/260813-nh4-2-b-ref-v-pdshape-pair-override-pod/evidence/frame_match.json, .planning/quick/260813-nh4-2-b-ref-v-pdshape-pair-override-pod/evidence/pair_overrides.json, .planning/quick/260813-nh4-2-b-ref-v-pdshape-pair-override-pod/staging/, backend/shared/python/sunity_shared/analysis/fault_zoom.py</files>
  <action>
    (A) 왼팔꿈치 ref V 진단 먼저, 보정은 분기 — 대상 = 피디쉐입 왼팔꿈치 record
    (freeze u8.556/r9.4) ref 패널 V (Task 1 배선 후 운영 렌더 실물 기준).
    진단: ref left_elbow(+ V 3점 전부)의 align 좌표·conf 실값을 freeze ai 에서
    실측하고, ref 원본 프레임의 실제 팔꿈치 픽셀 위치와 확대 크롭 육안 대조 —
    elbow_ref_v_diagnosis.json 에 좌표·conf·육안 판정·분기 근거 기록. 분기:
    (a) 좌표가 실제 팔꿈치에서 어긋나면 원인을 수리한다(align 사슬/인덱스/명명 —
    원인 실측 후 수리, 증상 덮기 금지). (b) 좌표는 맞는데 머리카락 가림·시각
    모호면 기존 문법 요소 내 최소 보정만(예: 이미 존재하는 요소의 적용 조건 —
    새 마크 종류·오프셋 상수·길이 튜닝 신설 금지. belle 판정 ② "새 문법 발명
    금지"). 어느 분기든 보정 전/후 렌더 실물(같은 freeze, 같은 크롭)을
    evidence/ 에 남기고 육안 대조 기록. 보정이 코드 변경이면 verify_port.py 재실행
    으로 의도-변경-국한(왼팔꿈치 ref 패널 외 카드 무변동) 확인. 커밋 (feat 또는
    docs — (b) 에서 코드 무변경 판정이 나면 진단 기록만 커밋하고 보정 없음을 명기).

    (B) 왼무릎 확정 장면 content-match → pair override 스테이징 — 대상 =
    피디쉐입 왼무릎 record (freeze u3.667/현행 r2.4, 반려됨). 절차:
    1. belle-confirmed-scene-8.png (본 사이클 dir) 을 먼저 열어 구도 파악 —
       ref(정은지) 내용이 있는 영역(우측 패널 또는 단일 화면)을 특정해 크롭.
       플레이어 표시 00:04/00:16 은 신뢰하지 않는다 (어느 타임라인인지 모호).
    2. l0u recover_frames.py 선례로 content-match: 크롭 픽셀 vs pdshape ref
       원본 영상 **전 구간** 프레임 diff 스캔(리사이즈 후 최소 diff) →
       frame_match.json (프레임별 diff + top-1 + 분리도). ref 영상은 기존 캐시
       (scratchpad approved_sweep) 우선, 없으면 S3 read-only fetch.
    3. 판정 분기: top-1 이 명확히 분리되고 매칭 초가 단일 해석이면 → refSec
       확정. top-1 분리 불명확이거나 매칭 초가 belle 서사(2초대 요소)와 4초대
       표시 사이에서 판정이 갈리면 → **해석 금지**: 양쪽 후보 스틸을 staging/ 에
       두고 frame_match.json + SUMMARY 에 모호성 명기, override 재렌더는 하지
       않는다 (belle 판정 재료로 종료).
    4. 확정 시: pair_overrides.json 작성 — 왼무릎 rid→{refVideoSec: 확정 초,
       note: "belle 08-13 확정 장면 content-match"} (260808-epy
       pdshape_pair_overrides.json r01 선례 형식, pairSrc=override). rid 는
       pdshapefault 승인 doc 의 angle_vs_reference__left_knee record 에서 특정.
    5. 로컬 재렌더: backend/scripts/render_compare_prototype.py 에
       --pair-override-json 지정 (epy 선례 인자 구성 — doc json + user/ref 영상 +
       audio-dir + align-json, 캐시 재사용). 리그 게이트 =
       backend/scripts/verify_render_prototype.py ALL PASS (exit 0).
    6. diff 국한 assert: 재렌더 report 를 v7 승인본과 대조(epy diff_reports.py
       선례) — 의도 변경 = 왼무릎 ref 짝(refSec/pairSrc)에 국한, 나머지 record
       불변. 주의: 카드(사진) 경로의 B 스펙과 비교 영상(compare_render)은 별개
       모듈이라 영상 diff 에 B 영향이 없어야 함 — 있으면 불일치로 보고.
    7. 스테이징: 재렌더 mp4 + 확정 짝 스틸(user freeze + 확정 ref 순간 나란히)을
       staging/ 에 배치. **S3 업로드 금지** — belle 복귀 확인 후 업로드는 별도
       1단계임을 staging/README 격 텍스트와 SUMMARY 에 명기. 커밋 (docs —
       evidence/staging 경로, 대용량 mp4 는 커밋 제외하고 절대경로 명기).
  </action>
  <verify>
    <automated>test -f .planning/quick/260813-nh4-2-b-ref-v-pdshape-pair-override-pod/evidence/elbow_ref_v_diagnosis.json && test -f .planning/quick/260813-nh4-2-b-ref-v-pdshape-pair-override-pod/evidence/frame_match.json && ls .planning/quick/260813-nh4-2-b-ref-v-pdshape-pair-override-pod/staging/ (스틸 존재; 확정 경로면 재렌더 mp4 + 리그 exit 0 로그, 모호 경로면 양쪽 후보 스틸 + 명기) && aws s3 ls 계열 쓰기 명령 실행 이력 0 (S3 put/cp 미사용 — 실행 로그로 확인)</automated>
  </verify>
  <done>왼팔꿈치: 진단 실측(좌표·conf·육안) 기록 + (a)/(b) 분기 판정 + 보정 전/후 렌더 실물 + 의도-변경-국한 확인(코드 변경 시). 왼무릎: frame_match.json 에 전 구간 스캔 + top-1 분리 판정 기록, 확정 시 pair_overrides.json + 재렌더 리그 ALL PASS + diff 왼무릎 국한 + staging/ 에 mp4·스틸, 모호 시 양쪽 후보 스틸 + 명기(재렌더 생략). S3 쓰기 0.</done>
</task>

<task type="auto">
  <name>Task 3: Pod 실증 (B 스펙) + SUMMARY</name>
  <files>.planning/quick/260813-nh4-2-b-ref-v-pdshape-pair-override-pod/evidence/pod/, .planning/quick/260813-nh4-2-b-ref-v-pdshape-pair-override-pod/260813-nh4-SUMMARY.md</files>
  <action>
    전제: Task 1·2 커밋 전부 push (rtk git push). Pod mddy6gsqmt24ud 가동 중 —
    SSH `ssh -p 20152 -i ~/.ssh/id_ed25519 root@213.173.110.207`, proxy URL 동기
    완료 상태(재갱신 불필요, memory current-pod-mddy6gsqmt24ud).
    1. Pod 에서 git pull → **서버 재기동 필수** (FastAPI 는 옛 모듈을 문다 —
       memory 실증): 기존 uvicorn 종료 후 start_server.sh 의 프로덕션 env 로
       `source <(sed -n '3,34p' start_server.sh)` + setsid nohup ... < /dev/null
       & disown 표준 기동.
    2. /health 4항목 확인 — **commitSha == 로컬 새 HEAD** (rtk git rev-parse
       HEAD 대조). 불일치 = 재기동 실패로 간주, 진행 금지.
    3. fresh 재분석 1회 — backend/scripts/phase34_fresh_reanalysis.py (hlv
       p34fresh 선례, 승인 elbow 영상·belle 계정 경로). 운영 경로만 Gemini
       실호출 허용(눈 게이트) — 호출 건수 채록.
    4. 판정: 점수 60 유지(감점 -45.0, records 값 hlv 인증값과 대조) +
       `display_anchor`·`fault_zoom_angle_bake` 운영 로그 실물 회수
       (wiring-claims-need-log-evidence — 커밋·테스트는 호출 증거가 아니다) +
       카드 실물 S3 read-only 회수 → 육안: 왼팔꿈치 카드에 B 스펙 V 반영
       (V 미베이크였던 자리에 align 유도 V 성립). B 소생으로 fresh doc 에서
       신규 카드가 늘어나면 그대로 기록(예상 밖 변동 숨기지 말 것 — 점수·
       survivors 는 불변이어야 하고, 변하면 불일치 보고 + 진행 중단).
    5. SUMMARY 작성 — 보드 게시 재료(캡션 각도 수치·이모지 0), LLM 학습 영향
       필수 기재(로컬 = 스텁 0건 / Pod = 실호출 N건·학습 전송 0·눈 원장),
       한계 박제(÷9.0 표기 잔존, 마크 미세조정 별건, S3 업로드 보류 = belle
       복귀 후 1단계), 왼무릎 모호 시 판정 재료 요약. Pod 는 터미네이트하지
       않는다 (memory: 종료 제안 먼저 하지 말 것).
  </action>
  <verify>
    <automated>curl -s (Pod proxy)/health 응답에서 commitSha == git rev-parse HEAD && grep "display_anchor\|fault_zoom_angle_bake" 재분석 서버 로그 (성립 라인 존재) && 재분석 doc 점수 == 60 (hlv 인증값 대조 스크립트 또는 dump_analysis_doc.py) && test -d .planning/quick/260813-nh4-2-b-ref-v-pdshape-pair-override-pod/evidence/pod/</automated>
  </verify>
  <done>push 완료, Pod /health commitSha == 새 HEAD, fresh 재분석 1회 = 점수 60 유지 + 운영 로그 실물(display_anchor·angle_bake) + 회수 카드 육안 PASS(왼팔꿈치 B 스펙 V). 예상 밖 변동 0 (또는 불일치 보고로 중단). SUMMARY 존재 — LLM 학습 영향·한계 박제·보드 재료 포함. 전 커밋 push 됨.</done>
</task>

</tasks>

<verification>
- 장부: JUDGMENT.md append-only (git diff 에 삭제 라인 0), belle 원문 3건 + 불일치 + 교훈.
- 배선: 기본값 경로 byte-동일(기존 fault_zoom 테스트 무접촉 PASS), m0k B 재현 전건(verify_port.py exit 0), 소생 카드 육안 전수, 분기 0 grep, 산식 5파일 diff 0.
- 회귀: pytest 59 failed 기준선 동일, 승인 무회귀 hold 9/9 + pair 9/9, freeze 순간·survivors·점수 불변(의도 변경 = B 스펙 V 소생 국한).
- 왼무릎: content-match 가 판정자(표시 초 불신), 모호 시 해석 금지 + 양쪽 스테이징.
- S3 쓰기 0, AWS 인프라 무변경, 로컬 Gemini 스텁·Pod 운영 경로만 실호출.
- Pod: commitSha 대조 + 점수 60 + 운영 로그 + 카드 육안 — 넷 다 있어야 실증.
</verification>

<success_criteria>
- belle 판정 3건이 원문 인용으로 장부에 남고, 내 추천 반려가 불일치로 기록됐다.
- B 스펙이 운영 코드에서 m0k 인증값 그대로 재현된다 (소생 6/6, elbow r01 정직한 침묵, survivors/점수 불변, 카드 8→10).
- 왼팔꿈치 ref V 가 진단 실측 위에서 (a) 원인 수리 또는 (b) 최소 보정으로 처리되고 전/후 실물이 있다.
- 왼무릎 확정 장면이 content-match 로 초 확정돼 pair override 재렌더가 스테이징됐다 (또는 모호성이 정직하게 판정 재료로 남았다). S3 쓰기 0.
- Pod fresh 재분석이 B 스펙 배선을 운영 로그 + 카드 실물로 증명하고 점수 60 이 유지됐다.
</success_criteria>

<output>
완료 시 `.planning/quick/260813-nh4-2-b-ref-v-pdshape-pair-override-pod/260813-nh4-SUMMARY.md` 작성
(보드 게시 재료 + LLM 학습 영향 + 한계 박제 + Self-Check).
</output>
