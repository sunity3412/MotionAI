---
phase: quick-260813-u8i
quick_id: 260813-u8i
slug: fps-fps-pod
date: 2026-08-13
status: planned
description: 카드 초 표기 수리 — fault_zoom 확대 카드의 초 라벨(÷9.0 fps 라벨 사슬, 실제보다 ~10% 큼)을 실효 fps 환산으로 교정 (표시 전용·채점 무접촉) + 승인 코퍼스 무회귀(의도 변경 = 라벨만) + pytest 기준선 + Pod 실증(fresh 재분석 라벨 실측)
wave: 1
depends_on: []
type: execute
plan: 01
autonomous: true
requirements: [QUICK-260813-U8I]
files_modified:
  - backend/shared/python/sunity_shared/analysis/fault_zoom.py
  - backend/functions/pipeline/app.py
  - backend/tests/test_fault_zoom_label_fps.py
  - docs/contract.md
  - .planning/quick/260813-u8i-fps-fps-pod/verify_label.py
  - .planning/quick/260813-u8i-fps-fps-pod/evidence/
  - .planning/quick/260813-u8i-fps-fps-pod/260813-u8i-SUMMARY.md
must_haves:
  truths:
    - "확대 카드에 그려지는 초 라벨이 실초와 일치한다 — 게이트 상속 카드는 freeze 실초와 측별 <=1.5프레임/eff(≈0.15s, ref 측 이중 반올림 경로 포함) 안에서 일치하고 표시 0.1s 정밀도에서 같은 값으로 읽힌다. 알려진 앵커 예 = Pod fresh doc(p34fresh1786613939) left_elbow r00 카드 라벨 5.9s → 5.3s (freeze u≈5.302s — 승인 스윕이 아니라 fresh doc 소속, Task 3 에서 단언). ÷9.0 잔존 소멸"
    - "수리는 표시 전용 — freeze 순간·survivors/dropped·마크 좌표(display_anchor)·프레임 선택 사슬(_override_idx)·채점(산식 5파일 diff 0)·records 전부 무변경. 의도 변경 = 초 라벨 픽셀 + 같은 산출의 userVideoSec/refVideoSec 필드 값 + contract.md §11.8 환산 서술 1줄뿐 (F-3 단일 산출이라 픽셀과 필드는 한 변수)"
    - "label_fps 미지정(기본 None) 경로는 byte-동일 — 기존 fault_zoom 테스트 전건 무접촉 PASS + 명시 하위호환 테스트"
    - "승인 5동작 스윕 재현 게이트 — survivors/dropped nh4 정본 전건 동일 + display_anchor 좌표 로그 동일 + 대조 런(label_fps 만 9.0 강제 shim)이 nh4 정본 md5 전건 재현 = 변경 픽셀이 라벨뿐임을 기계 증명 + 카드별 라벨 전/후 표(구 라벨 → 신 라벨 vs freeze 실초, 앵커 하드코딩 없이 nh4 정본 freezes 에서 기계 유도) 산출"
    - "pytest 기준선 59 failed 동일(신규 실패 0) + 신규 라벨 환산 유닛 테스트 PASS + diff 추가 라인 동작명/분석 ID 리터럴 0"
    - "Pod mddy6gsqmt24ud 실증 — push 후 git pull + 서버 재기동(setsid 표준), /health commitSha == 새 HEAD, fresh 재분석 1회 = 점수 60 유지 + 회수 카드의 초 라벨이 실초와 일치(기계: doc userVideoSec vs card_gates survivors freeze 초 / 육안: 카드 PNG Read — fresh doc left_elbow r00 라벨 5.9→5.3s 변화가 앵커 증거)"
    - "제약 준수 — 로컬 검증 Gemini 스텁(실호출 0)·로컬 S3 업로드 0·AWS 인프라 무변경(SSM/Lambda URL 재갱신 안내 금지 — 이미 동기)·Pod 터미네이트/스톱 제안 금지. Pod 재분석은 프로덕션 파이프라인 자체 쓰기(신규 analysis id 하위)만 — 승인 코퍼스 키 무접촉. SUMMARY 에 LLM 학습 영향 필수 기재"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/fault_zoom.py"
      provides: "keyword-only label_fps=(user|None, ref|None) — 초 라벨 환산 분모만 측별 교체, None/비양수 = frames_fps 폴백(byte-동일)"
      contains: "label_fps"
    - path: "backend/functions/pipeline/app.py"
      provides: "두 경로 배선 — _run_gated_card_inherit(기존 eff dict 재사용) + _render_fault_zoom(probe_effective_fps fail-open, confirmed/advisory 두 호출)"
    - path: "backend/tests/test_fault_zoom_label_fps.py"
      provides: "라벨 환산 유닛 — 하위호환 byte-동일 / 측별 환산 / 측별 폴백 / 비양수 fail-open"
    - path: "docs/contract.md"
      provides: "§11.8 userVideoSec/refVideoSec 환산 서술 1줄 갱신 — '(9fps 프레임 인덱스 / fps)' → 실효 fps (3-way 동시 수정 규칙, 필드 형태 무변경)"
    - path: ".planning/quick/260813-u8i-fps-fps-pod/verify_label.py"
      provides: "승인 5동작 스윕 재현 게이트 + 라벨 전/후 표 + 대조 런(shim) md5 재현 — nh4 verify_port 구조 상속"
    - path: ".planning/quick/260813-u8i-fps-fps-pod/evidence/"
      provides: "label_check.json(게이트 판정+라벨 표) + 카드 실물 + EYE 기록 + pod/(health·재분석 로그·회수 카드·점수 대조)"
    - path: ".planning/quick/260813-u8i-fps-fps-pod/260813-u8i-SUMMARY.md"
      provides: "보드 게시 재료 + LLM 학습 영향 + 한계 박제"
  key_links:
    - from: "backend/functions/pipeline/app.py"
      to: "backend/shared/python/sunity_shared/analysis/fault_zoom.py"
      via: "build_fault_zoom_comparisons(label_fps=(eff['user'], eff['ref'])) — 게이트 경로는 기존 eff dict(4473-4485) 재사용, probe 신규 0"
      pattern: "label_fps="
    - from: "backend/shared/python/sunity_shared/analysis/fault_zoom.py 라벨 사이트(3669-3670)"
      to: "_stamp_time + item userVideoSec/refVideoSec(3721-3730)"
      via: "u_video_sec/r_video_sec 변수 1개가 픽셀과 필드 양쪽에 흐름 — F-3 단일 산출 유지(중복 계산 금지), 수리도 이 변수 하나만"
      pattern: "u_video_sec"
    - from: ".planning/quick/260813-u8i-fps-fps-pod/verify_label.py"
      to: ".planning/quick/260813-nh4-2-b-ref-v-pdshape-pair-override-pod/evidence/"
      via: "nh4 B 인증값(survivors/dropped/md5/display_anchor 로그) 기계 대조 — 대조 런 md5 재현이 '변경=라벨뿐' 증명"
      pattern: "verify_port"
---

<objective>
fault_zoom 확대 카드 위에 그려지는 초 라벨의 fps 라벨 사슬(÷9.0) 잔존 버그를
실효 fps 환산으로 수리한다 — 표시 전용, 채점·records·freeze 선정·마크 좌표 무접촉.

버그 정본 (기지 실측 — 재조사 불요): frame_extractor 가 정수 step 솎음(30fps → 실제
9.997fps)인데 초 환산은 요청값 9.0 → 라벨이 실제 순간보다 ~10% 크게 적힌다
(memory fps-label-vs-actual-decimation-rate). 실례 (Pod fresh doc — l0u frame_match
실측) = left_elbow 카드 라벨 5.9s ↔ 실초 5.30s / 피디쉐입 왼어깨 라벨 3.2s ↔ 실초
2.87s. 트랙 백필(anglesRealFps)·atVideoSec 교정은 08-10 완료 — 잔존 =
fault_zoom.py:3669-3670 의 `u_video_sec = u_idx_unit / frames_fps` /
`r_video_sec = r_display_idx / frames_fps` 한 자리다. 이 변수는 `_stamp_time` 픽셀과
item `userVideoSec`/`refVideoSec` 필드 양쪽에 흐른다 (F-3 단일 산출 — 3721-3730).

수리 방향 (locked): 라벨 초를 실효 fps 로 환산. freeze/align 좌표 사슬은 fxx 에서 이미
단일 출처 — 게이트 상속 카드의 라벨은 그 출처의 실초(freeze sec)와 일치하게 된다
(u9 = round(u_sec x eff) 이므로 u9/eff ≈ u_sec, 측별 반올림 오차 <=1.5프레임/eff).

belle 방침 (locked): Pod 켠 김에 Pod 필요 작업 우선 — 이 사이클은 라벨 수리 +
Pod 재분석 실측까지. 실기기 통합 검증은 파트 전부 합쳐진 뒤 (별건).

Purpose: 카드가 가리키는 순간을 사용자가 영상에서 찾을 수 있게 — 10% 어긋난 초는
틀린 자리를 가리키는 표지판이다.
Output: label_fps 배선 + 유닛 테스트 + 재현 게이트 증거 + Pod 실측.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend/shared/python/sunity_shared/analysis/fault_zoom.py
@backend/functions/pipeline/app.py
@.planning/quick/260813-nh4-2-b-ref-v-pdshape-pair-override-pod/verify_port.py
@.planning/quick/260813-nh4-2-b-ref-v-pdshape-pair-override-pod/260813-nh4-SUMMARY.md
@.planning/quick/260813-fxx-belle-3-p3r1-pass-v-p3-align-fps-5-pytes/260813-fxx-SUMMARY.md

핵심 코드 좌표 (착수 즉시 실물 대조 — verify-the-target):
- fault_zoom.py 3669-3670 = 라벨 환산 사이트 (÷frames_fps). 3721-3730 = 같은 변수의
  userVideoSec/refVideoSec 방출. 2766-2790 = build_fault_zoom_comparisons 시그니처
  (keyword-only 블록, display_anchor/align_bake 선례). 1629-1643 = _stamp_time
  (docstring 의 "frames_fps 프레임 인덱스/fps" 문구도 갱신 대상).
- app.py 4473-4485 = 게이트 경로 eff dict (probe_effective_fps, 이미 존재).
  4696-4715 = _override_idx (무접촉 — 프레임 선택 사슬). 4816-4847 = 게이트 경로
  build 호출 (frames_fps=9.0 자리). 3370-3404 confirmed / 3411-3440 advisory =
  스테이지 경로 build 호출 2곳 (_render_fault_zoom — Mode1/Mode3 공용 코어).
- docs/contract.md 1896 부근 (§11.8) = "(9fps 프레임 인덱스 / fps)" 환산 서술 —
  수리 후 거짓이 되므로 갱신 대상.
- Pod 좌표 = memory current-pod-mddy6gsqmt24ud.md (가동 중). ★서버 코드 실측 =
  96b4e07b (nh4 POD-VERDICT) — memory 파일 기재 0f999619 는 스테일, /health
  commitSha 실측이 정본.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: label_fps 배선 — fault_zoom 라벨 환산 + app.py 두 경로 + 유닛 테스트</name>
  <files>backend/shared/python/sunity_shared/analysis/fault_zoom.py, backend/functions/pipeline/app.py, docs/contract.md, backend/tests/test_fault_zoom_label_fps.py</files>
  <behavior>
    - 하위호환: label_fps 미지정(None) 호출은 결과 dict(userVideoSec/refVideoSec 포함)이 종전과 동일 — 기존 fault_zoom 테스트 전건(test_fault_zoom*.py) 무접촉 PASS 가 큰 증거 + 명시 테스트 1건
    - 측별 환산: label_fps=(9.997, 15.0) 이면 userVideoSec == u_idx/9.997, refVideoSec == r_display_idx/15.0 (frames_fps=9.0 은 프레임 선택에만 관여)
    - 측별 폴백: label_fps=(None, 15.0) 이면 user 측만 frames_fps 로 환산, ref 측은 15.0
    - fail-open: 0/음수/비유한 값은 그 측 frames_fps 폴백 (라벨 때문에 카드를 죽이지 않는다 — 라벨 게이트는 fail-open, 좌표 게이트(display_anchor)만 fail-closed 라는 기존 층위 유지)
  </behavior>
  <action>
    fault_zoom.build_fault_zoom_comparisons 에 keyword-only
    `label_fps: tuple[float | None, float | None] | None = None` 추가 (display_anchor/
    align_bake 선례 자리, 기본 None = 전 경로 byte-동일). 라벨 사이트(3669-3670)에서
    측별 분모를 계산해 교체: 값이 유한하고 >0 이면 그 값, 아니면 frames_fps.
    u_video_sec/r_video_sec 변수 하나만 바꾼다 — _stamp_time 픽셀과 userVideoSec/
    refVideoSec 필드가 같은 산출을 공유하는 F-3 구조(중복 계산 금지)를 그대로 탄다.
    _stamp_time/빌더 docstring 의 초 환산 서술도 실효 fps 로 갱신. 프레임 선택 사슬
    (worst_seconds/_frame_index/_override_idx/candidates)은 무접촉 — frames_fps 인자의
    다른 용처를 건드리지 않는다.

    app.py 배선 2곳:
    (1) _run_gated_card_inherit 의 build 호출(4816-4847)에
    `label_fps=(eff["user"], eff["ref"])` — eff dict 는 4473-4485 에서 이미 산출
    (probe 신규 0). 기대 라벨 = freeze 실초 (u9 = round(u_sec x eff x rep9 보정)이고
    user 측 rep9 보정은 identity, ref 측은 ref_display_frame_index 역변환으로 상쇄 —
    _override_idx docstring 참조. 최종 표시 인덱스/eff ≈ freeze sec, 측별 반올림
    누적 <=1.5프레임/eff — ref 측은 이중 반올림 경로가 실존).
    (2) _render_fault_zoom(3351 부근)에서 ext.probe_effective_fps 를 측별 try/except
    로 probe (실패 = None = 그 측 종전 ÷9.0 유지 — fail-open, native provider 의
    fail 정책과 별개로 라벨은 카드를 죽이지 않음), confirmed(3370)·advisory(3411)
    두 호출에 동일 전달. Mode1/Mode3 은 이 공용 코어를 타므로 분기 0. 동작명·분석 ID
    분기 0 (추가 라인 리터럴 grep 이 게이트).

    계약 갱신 (3-way 동시 수정 규칙): userVideoSec/refVideoSec 은 기존 optional
    scalar 필드 — 값만 교정되므로 analysis.ts/models.py 의 타입/키는 무접촉이 맞다.
    단, docs/contract.md 1896 부근(§11.8)의 "(9fps 프레임 인덱스 / fps)" 환산 서술은
    수리 후 거짓이 되므로 1줄을 실효 fps 환산으로 갱신한다 (서술 정정만 — 필드
    형태·키 무변경, analysis.ts 주석에 같은 서술이 있으면 함께 정정).

    테스트는 backend/tests/test_fault_zoom_label_fps.py 신규 — 기존
    test_fault_zoom_display_repair.py 의 픽스처 구성 방식(합성 프레임 + 최소 report)을
    상속해 behavior 4건을 RED 먼저 작성 후 구현.
  </action>
  <verify>
    <automated>PYTHONPATH=backend/tests backend/.venv/bin/python -m pytest -q backend/tests/test_fault_zoom_label_fps.py backend/tests/test_fault_zoom.py backend/tests/test_fault_zoom_display_repair.py backend/tests/test_fault_zoom_record_moment.py</automated>
  </verify>
  <done>신규 4+ 테스트 PASS, 기존 fault_zoom 테스트 전건 무접촉 PASS, label_fps 미지정 경로 byte-동일, app.py 두 경로에 label_fps 전달 라인 존재 (rtk grep "label_fps=" backend/functions/pipeline/app.py 로 게이트·스테이지 확인), contract.md §11.8 환산 서술 갱신 반영</done>
</task>

<task type="auto">
  <name>Task 2: 기계 검증 사다리 — 승인 코퍼스 재현 게이트 + 라벨 전/후 표 + 기준선</name>
  <files>.planning/quick/260813-u8i-fps-fps-pod/verify_label.py, .planning/quick/260813-u8i-fps-fps-pod/evidence/</files>
  <action>
    nh4 verify_port.py 구조 상속(같은 세션 scratchpad 캐시 + grammar_round Gemini
    스텁/더미 키 — 실호출 0, 로컬 S3 업로드 0)으로 verify_label.py 작성, 승인 5동작
    스윕을 운영 경로로 실행해 게이트 전건 PASS 를 기계 판정 (하나라도 실패 = exit 1,
    결과 = evidence/label_check.json):

    1. 무회귀 핵: survivors/dropped == nh4 정본 전건 + display_anchor 로그 좌표
       (u_ai/r_ai/user/ref xy) == nh4 정본 — freeze 순간·게이트·마크 좌표 무접촉 증거.
    2. 변경=라벨뿐 증명 (대조 런): _fz.build_fault_zoom_comparisons 를 shim 으로 감싸
       label_fps 만 (9.0, 9.0) 강제한 대조 런의 카드 md5 == nh4 B 정본 md5 전건 —
       라벨 분모를 종전값으로 되돌리면 픽셀이 완전 재현된다 = 이 수리의 픽셀 변경원이
       라벨뿐임의 기계 증명. shim 은 대조 런 한정 하네스 장치(운영 코드 무패치),
       본 런(무패치)은 다음 게이트가 판정.
    3. 라벨 라운드트립 게이트 (본 런): 카드별 (motion, rid, freeze 실초 u/r,
       구 라벨 = 표시 인덱스/9.0, 신 라벨 = item userVideoSec/refVideoSec) 표를
       기계 산출 — freeze 실초와 구/신 라벨의 기대값은 전부 nh4 정본 freezes 에서
       기계 유도한다 (예: elbow r00 = right_elbow u=11.111/r=12.067 — 승인 스윕에는
       5.9/5.302 페어가 존재하지 않는다: 그 값은 Pod fresh doc 소속, Task 3 의
       판정 재료). 앵커 값 하드코딩 없이 전건 라운드트립만 게이트:
       |신 라벨 − freeze 실초| <= 측별 1.5프레임/eff (≈0.15s). 허용치 근거를
       verify_label.py 주석으로 박제 — ref 측은 rep9 보정(k<1 rep 메타 실존
       케이스)의 이중 반올림 경로라 최악 ≈0.117s 로 1프레임(0.12s) 게이트는
       경계선 플레이크. 구 라벨은 같은 런의 표시 인덱스에서 ÷9.0 독립 계산
       (인덱스 사슬 불변이 게이트 1 로 이미 증명된 상태).
    4. pytest 기준선: `PYTHONPATH=backend/tests backend/.venv/bin/python -m pytest -q
       backend/tests` — failed 59 동일 (신규 실패 0, passed 는 신규 테스트만큼 증가).
    5. 채점 무접촉: 산식 5파일(deduction_engine/dimensions/kismam/motiondtw/assemble)
       diff 0 (수리 커밋 범위 git diff 빈 출력).
    6. 분기 0: 수리 diff 추가 라인에 동작명/분석 ID 리터럴 0 (fxx W-1 방식 —
       추가 라인만 grep).
    7. Gemini 실호출 0 (스텁 계수).

    카드 실물 Read 육안 (frames-before-numbers): 본 런 카드 최소 2장을 Read 로 열어
    라벨 픽셀이 표의 신 라벨 값과 일치하는지 눈으로 확인 — 기계 산출값(필드)만
    믿고 픽셀을 안 본 채 제시 금지. 판정을 evidence/EYE-LABEL.md 에 기록.
    evidence/ 에 카드 사본과 label_check.json 커밋 (대용량 중간물은 .gitignore).
  </action>
  <verify>
    <automated>backend/.venv/bin/python .planning/quick/260813-u8i-fps-fps-pod/verify_label.py && rtk grep -c "\"pass\": true" .planning/quick/260813-u8i-fps-fps-pod/evidence/label_check.json</automated>
  </verify>
  <done>verify_label.py exit 0 (게이트 7종 전건 PASS), label_check.json 에 라벨 전/후 표(nh4 정본 freezes 기계 유도, 하드코딩 앵커 0) + 대조 런 md5 재현 기록, EYE-LABEL.md 육안 판정 존재, pytest 59 failed 동일 로그 채록</done>
</task>

<task type="auto">
  <name>Task 3: Pod 실증 — push + 재기동 + fresh 재분석 라벨 실측</name>
  <files>.planning/quick/260813-u8i-fps-fps-pod/evidence/pod/, .planning/quick/260813-u8i-fps-fps-pod/260813-u8i-SUMMARY.md</files>
  <action>
    수리 커밋을 push (GSD Pod 작업 = push 한 단위) 후 Pod mddy6gsqmt24ud 에서 실증
    (memory current-pod-mddy6gsqmt24ud.md 좌표 — SSH TCP `ssh root@213.173.110.207
    -p 20152 -i ~/.ssh/id_ed25519` 는 재시작마다 변동 가능, 접속 실패 시 콘솔
    Connect 탭 재확인. HTTP proxy = https://mddy6gsqmt24ud-8000.proxy.runpod.net.
    현 서버 코드 실측 = 96b4e07b — nh4 POD-VERDICT, /health commitSha 가 정본):

    1. `git -C /workspace/SunityMotion fetch && git -C /workspace/SunityMotion merge
       --ff-only origin/main` — 새 HEAD 확인.
    2. 서버 재기동 (코드 당긴 뒤 필수 — FastAPI 는 옛 모듈을 문다): 기존 서버 프로세스
       종료 후 `cd /workspace && source aws_env.sh && setsid nohup bash
       start_server.sh > /workspace/_server.log 2>&1 < /dev/null & disown`
       (setsid 완전 분리 표준, source aws_env.sh 누락 = 조용한 실패).
    3. /health 폴링: commitSha == 새 HEAD + RTMW_DETERMINISTIC + PR_INVERSION_ENABLED
       + modelLoaded 4항목.
    4. fresh 재분석 1회 (프로덕션 env 그대로): `source /workspace/aws_env.sh && source
       <(sed -n '3,34p' /workspace/start_server.sh)` 후
       `cd /workspace/SunityMotion/backend && python3 scripts/phase34_fresh_reanalysis.py`.
    5. 판정 (기계): 점수 60 유지 + records 값 무변경(소수점 15자리 대조 선례) +
       card_gates survivors 의 freeze @u/r 값이 종전 fresh(nh4 p34fresh1786613939)와
       동일 + Firestore doc comparisons[].userVideoSec/refVideoSec ≈ survivors freeze
       초 (측별 <=1.5프레임/eff ≈0.15s — Task 2 게이트 3 과 같은 허용치·같은 근거) —
       구 라벨(x1.11)이면 FAIL. ★앵커 단언 (체커 1 귀속): 종전 fresh doc
       p34fresh1786613939 의 left_elbow r00 카드가 라벨 5.9s (freeze u≈5.302) 였다 —
       새 fresh 의 같은 자리 카드 라벨이 5.3s 로 나오는 것이 이 수리의 대표 증거.
    6. 판정 (육안): 카드 PNG SCP 회수 → Read — 초 라벨 픽셀이 실초와 일치하는지
       (left_elbow 5.9→5.3s 급 변화). 근거를 evidence/pod/ 에 채록 (health·재분석
       로그·회수 카드·점수 대조).

    제약: Pod 터미네이트/스톱 제안 금지(가동 유지). AWS 인프라 무변경 — SSM/Lambda
    URL 은 이미 동기 완료라 재갱신 안내 금지 (memory 명시). S3 는 파이프라인 자체
    신규 analysis id 하위 쓰기만 — 승인 코퍼스(v7) 키 무접촉. Pod 재분석의 기계 눈
    호출은 운영 경로 실호출(건수 채록) — 추론만, 학습 전송 0.

    SUMMARY 작성: 라벨 전/후 표 + Pod 실측 + LLM 학습 영향(필수) + 한계 박제
    (이 수리가 다루지 않는 것 — freeze 장면 선정·마크 가독성 등 미세조정 의제는
    별건 유지). 커밋·push 로 종결.
  </action>
  <verify>
    <automated>rtk curl -s https://mddy6gsqmt24ud-8000.proxy.runpod.net/health && ls .planning/quick/260813-u8i-fps-fps-pod/evidence/pod/</automated>
  </verify>
  <done>/health commitSha == push 된 HEAD, fresh 재분석 점수 60 + records 무변경 + survivors freeze 동일, doc userVideoSec ≈ freeze 초 기계 대조 PASS (측별 0.15s), left_elbow 5.9→5.3s 앵커 실측 + 회수 카드 육안 라벨 기록, SUMMARY 에 LLM 학습 영향 기재, 전 산출물 커밋·push</done>
</task>

</tasks>

<verification>
- 라벨 수리: 승인 스윕 본 런 전건 |신 라벨 − freeze 실초| <= 측별 1.5프레임/eff(≈0.15s, ref 이중 반올림 근거). 앵커 5.9→5.3 단언은 Pod fresh doc 판정(Task 3) — 승인 스윕 소속 아님
- 무회귀: survivors/dropped/display_anchor == nh4 정본 + 대조 런(label 9.0 강제) md5 전건 재현
- 기준선: pytest 59 failed 동일 + 산식 5파일 diff 0 + 추가 라인 리터럴 grep 0 + contract.md §11.8 서술 갱신
- Pod: 점수 60 + records 15자리 동일 + doc 초 필드 ≈ freeze + left_elbow 5.9→5.3s + 카드 육안
- 제약: 로컬 Gemini 스텁·로컬 S3 업로드 0·AWS 무변경·Pod 유지
</verification>

<success_criteria>
- fault_zoom 카드 초 라벨의 ÷9.0 잔존이 소멸하고 실초를 가리킨다 (기계 표 + 육안 + Pod 실측 3중)
- 라벨 외 픽셀·순간·좌표·채점이 변하지 않았음이 기계로 증명된다 (대조 런 md5 재현이 핵)
- 미지정 경로 byte-동일 하위호환 + pytest 기준선 보존 + contract.md 서술 정합
</success_criteria>

<output>
완료 시 `.planning/quick/260813-u8i-fps-fps-pod/260813-u8i-SUMMARY.md` 작성
</output>
