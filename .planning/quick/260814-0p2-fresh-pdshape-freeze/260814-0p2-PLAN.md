---
phase: quick-260814-0p2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/quick/260814-0p2-fresh-pdshape-freeze/inject_freeze.py
  - .planning/quick/260814-0p2-fresh-pdshape-freeze/evidence/
  - .planning/quick/260814-0p2-fresh-pdshape-freeze/FINDINGS.md
autonomous: true
requirements: [QUICK-260814-0P2]

must_haves:
  truths:
    - "재렌더 영상의 정지 목록 = 기존 freeze 전건 보존 + 왼무릎 채택 순간(u12.87/r12.40) 정지 1개 삽입 — 삽입 외 정지의 순간·짝·자막·마크 무변경"
    - "리그 게이트(compare_verify.verify) 전 항목 PASS — 기존 freeze 는 운영 판정기 무수정 기준 그대로, 신규 freeze 의 H2 면제는 사본 delta 1값(pairSrc 신설 라벨)으로 명기 박제"
    - "diff = 의도 변경(신규 정지 1) 국한 기계 증명 — 기존 정지 5건 프레임 내용 pixel/md5 동일 + 재생 구간 표본 동일"
    - "무릎 카드가 신규 freeze 를 상속해 방출 (운영 헬퍼 _run_gated_card_inherit 호출만 — 운영 코드 무접촉), 카드 초 라벨 = 실효 fps(u8i label_fps) 환산 약 12.9s"
    - "backend/ diff 0 · S3 쓰기 0(GET 만) · Firestore 쓰기 0 · Gemini 실호출 0(wif 원장 replay 스텁)"
    - "belle 로컬 확인 재료 = /Users/Shared/sunity-freeze-inject-260814/ 한글 사본 (S3 업로드 보류 명기)"
  artifacts:
    - path: ".planning/quick/260814-0p2-fresh-pdshape-freeze/inject_freeze.py"
      provides: "하네스 사본 확장 — freeze 주입 레이어 + 리그 사본-면제 delta (원본 스크립트 무수정)"
    - path: ".planning/quick/260814-0p2-fresh-pdshape-freeze/evidence/SUPPORT-SURFACE.md"
      provides: "하네스 freeze 주입 지원 범위 실측 박제 (착수 전 선행)"
    - path: ".planning/quick/260814-0p2-fresh-pdshape-freeze/evidence/inject_verdict.json"
      provides: "주입 재렌더 리그 판정 + 결정론 + diff 국한 기계 증명"
    - path: ".planning/quick/260814-0p2-fresh-pdshape-freeze/evidence/cards/"
      provides: "신규 freeze 상속 카드 실물"
    - path: ".planning/quick/260814-0p2-fresh-pdshape-freeze/evidence/VISUAL-REVIEW.md"
      provides: "frames-before-numbers 육안 전수 기록"
    - path: "/Users/Shared/sunity-freeze-inject-260814/"
      provides: "belle 확인용 한글 사본 (카드·스틸·재렌더 영상)"
  key_links:
    - from: "inject_freeze.py"
      to: "sunity_shared.analysis.compare_render.render/build_timeline"
      via: "임포트 + 사본 내 monkeypatch 주입 (원본 파일 무수정)"
      pattern: "from sunity_shared.analysis import compare_render"
    - from: "inject_freeze.py"
      to: "sunity_shared.analysis.compare_verify.verify"
      via: "운영 판정기 호출 (면제 튜플만 사본 delta — 판정 로직 무수정)"
      pattern: "compare_verify"
    - from: "inject_freeze.py"
      to: "backend/functions/pipeline/app.py::_run_gated_card_inherit"
      via: "S3/Firestore 스텁 + 눈 replay 스텁으로 호출 (wif/ufb 선례)"
      pattern: "_run_gated_card_inherit"
    - from: "신규 freeze text/음성"
      to: "기존 r04 record 코칭 문구 + coach_audio_r04 mp3"
      via: "speech_text(rec) 재사용 — 새 문구 발명 0"
      pattern: "coach_audio_r04"
---

<objective>
채택 발굴 순간의 영상 반영 실증 — belle 채택 순간 cand13b(user 12.87s / ref
12.40s, 왼무릎; DISCOVERY-LEDGER 판정 "후보 1 채택", 승격 장부 1행 = 일치 1/1)를
fresh pdshape 비교 영상(doc p34fresh1786628533 계열)의 정지 목록에 **추가**하고
로컬 재렌더하여, 무릎 카드가 그 freeze 를 상속해 방출됨을 실증한다. belle 확정
(locked): 표현 경로 = "비교 영상 freezes 에 발굴 순간 추가 -> 재렌더 -> 카드
상속" (카드 단독 방출 금지 — 영상이라는 승인 틀 원칙, r01 명시 지정 pair
override 메커니즘의 확장). S3 업로드 보류 — 이 사이클은 로컬 재렌더 실물(belle
확인 재료)까지이고, 반영(업로드·doc 갱신)은 belle 실물 확인 후 별도 1단계.

Purpose: "스스로 잘 찾는다는 전제하에 영상도 바뀌는 것" = 승격의 정의(belle
상호 확인) — 발굴 채택 순간이 실제 영상·카드로 표현되는 경로의 첫 실증.
Output: 재렌더 영상 + 상속 카드 + 리그 ALL PASS + diff 국한 기계 증명 +
/Users/Shared 한글 사본 (belle 확인 재료).

**착수 전 실측 정본 (planner 가 이미 열어 확인한 사실 — 재조사 불요):**

1. **freeze 주입은 현행 하네스 미지원.** `compare_render.build_timeline`
   (compare_render.py:1119-1321)의 freezes 는 record-driven(레코드당 1건,
   moment+mp3 필수)이고, `--pair-override-json`(rid→{refVideoSec, note},
   :1214-1222)은 **기존 record freeze 의 rt(기준측)만** 덮어쓴다 — ut 무접촉,
   정지 추가 경로 부재. 따라서 하네스 확장은 확정 (0p2 사본에서만).
2. **리그 게이트 H2 는 외부 삽입을 설계상 검출한다.** `compare_verify.verify`
   (compare_verify.py:313) 의 H2(:225-249)는 freeze 마다 userSec 을 doc record
   atVideoSec 과 ±0.2s 대조하고, 면제 튜플 `_H2_UT_DISPLACING_SRC =
   ("align-peak", "align-pole")` (:173) 뿐이다. 삽입 freeze(u12.87 != doc r04
   atVideoSec 10.5035)는 운영 판정기 그대로는 **구조적으로 H2 FAIL** — 이것이
   H2 의 존재 이유("외부 삽입 의심" 검출)다. 해소는 **정직한 사본 delta 1값**:
   신규 freeze 에 pairSrc 신설 라벨(예: "discover")을 붙이고 리그 실행 시
   면제 튜플에 그 1값만 사본 수준으로 추가(+박제). **align-peak/align-pole
   사칭 절대 금지** (게이트 속이기 = 금지 사항). H1(rid 집합 동일성 — 중복
   rid 는 set 붕괴로 무해)·H3(speech_text 재사용이면 일치)·H4(r04 는
   coachAudio 존재)는 운영 그대로 PASS 가능.
3. **직전 계열 doc 의 freeze 5건** (ufb evidence/run_verdict.json — 동일 사용자
   영상 계열): r03 left_hip u16.667/r15.2(align-peak), r00 left_elbow
   u5.302/r5.13, r01 right_elbow u6.102/r4.53, **r04 left_knee
   u10.5035/r9.4(align — 카드는 hold=moving 침묵)**, r02 right_shoulder
   u13.805/r10.73(align-w). 현행 u8i doc(p34fresh1786628533)의 실제 목록은
   Task 1 베이스라인이 확정한다 (가정 금지 — 5건과 다르면 실측 그대로 박제).
4. **채택 순간 정확값** = ut 12.8667s / rt 12.40s (wif cand13b 카드 파일명
   `cand13b_u12.8667s_r12.4s_...` 이 정본, align 15fps 인덱스 u193/r186).
   게이트 수치(wif candidates.json): hold 양측 PASS(학생 21.7도/초) · pose
   0.417 · poleDiff 0.235 · 눈 user bent/leg 0.9 + ref extended/leg 0.95.
5. **카드 상속 경로** = `app._run_gated_card_inherit`(app.py:4382) 가
   `report["freezes"]` 를 받아 그 freeze 자리에서 방출 판정 — 신규 report(정지
   6건)를 넣으면 신규 freeze 가 게이트 판정 대상이 된다. wif discover_knee.py
   render 스테이지(:761-)가 S3/Firestore 스텁 호출 선례.
6. **캐시 재수화 선례** = wif discover_knee.py --fetch (Firestore doc +
   refmotion + S3 GET 영상 + align = build_align + P35 replay, 프레임 수
   272/237 정확 일치 = 영상 정체성 게이트) + ufb verify_local.py --fetch
   (rid 별 mp3: `results/{UID}/{AID}/coach_audio_r04:angle_vs_reference__left_knee.mp3`).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260813-wif-knee-discovery/DISCOVERY-LEDGER.md
@.planning/quick/260813-wif-knee-discovery/discover_knee.py
@.planning/quick/260811-ufb-freeze-only/verify_local.py
@backend/shared/python/sunity_shared/analysis/compare_render.py
@backend/shared/python/sunity_shared/analysis/compare_verify.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 지원 범위 실측 박제 + 하네스 사본 확장 + 베이스라인 재현</name>
  <files>.planning/quick/260814-0p2-fresh-pdshape-freeze/inject_freeze.py, .planning/quick/260814-0p2-fresh-pdshape-freeze/evidence/SUPPORT-SURFACE.md, .planning/quick/260814-0p2-fresh-pdshape-freeze/evidence/baseline_verdict.json</files>
  <action>
  (1) **SUPPORT-SURFACE.md 선행 박제** (locked 제약: "하네스가 freeze 주입을
  지원하지 않으면 지원 범위 실측을 먼저 박제") — objective 실측 정본 1·2 항을
  file:line 인용으로 기록: pair-override = rt 전용(추가 경로 부재), H2 면제
  튜플 = ("align-peak","align-pole") 이라 삽입 freeze 는 운영 판정기 그대로
  H2 구조 FAIL(게이트가 설계대로 작동하는 것), 따라서 확장 = 0p2 사본에서만 +
  리그 면제 사본 delta 1값. 운영 반영(별도 사이클) 시 필요한 backend 변경
  목록도 여기 박제: build_timeline 주입 레이어 + _H2_UT_DISPLACING_SRC 라벨
  추가.
  (2) **캐시 재수화** — wif discover_knee.py fetch 패턴 그대로 (동일 좌표:
  UID fvcNXzEqKjgqVxRPVSj1iwFnIpn2 / AID p34fresh1786628533 / ref-pdshape,
  Firestore 읽기 전용 + S3 GET 만): doc.json / refmotion.json / user·ref
  영상 / align(build_align + P35 replay — 프레임 수 272/237 정확 일치 게이트)
  + ufb verify_local fetch 패턴으로 **전 rid mp3** (coach_audio_{rid}:...mp3,
  S3 GET). wif 세션 scratchpad 캐시(wif_fresh/)가 살아 있으면 재사용, 없으면
  재수화. r04 mp3 부재 시: 캡션만·음성 무 추가로 진행하고 그 사실을
  SUPPORT-SURFACE.md 와 카드·FINDINGS 에 명기 (locked: "없으면 캡션만·음성 무
  추가 명기" — freeze dur 은 캡션 표시 최소치로 하고 그 값을 박제).
  (3) **inject_freeze.py 작성** — 원본 스크립트 무수정 (backend/ diff 0):
  compare_render 를 임포트하고 build_timeline 을 **사본 내 래퍼로 monkeypatch**
  (m0k/nh4 하네스 monkeypatch 선례): 원본 build_timeline 호출 후 freezes 에
  신규 1건 append — rid "r04", ut 12.8667, rt 12.40, pair_src "discover"
  (신설 라벨 — align-peak/pole 사칭 금지), mp3 = r04 mp3, dur = 운영 규칙
  동일(mp3_duration_s + FREEZE_TAIL_S), text = 운영 speech_text(r04 record)
  재사용(새 문구 발명 0 — H3 진품 일치 조건), 마크 = 운영 viz 헬퍼를 같은
  방식으로 호출(_align_markers + left_knee 이므로 _body_line_viz — 새 문법
  발명 0, compare_render.py:1256-1259 미러). 주입은 플래그로 on/off (베이스
  라인 = off).
  (4) **베이스라인 렌더 (주입 off) 2회** — render() 를 doc/align dict 입력으로
  호출(render_compare_prototype.py 얇은 래퍼와 동일 경로), 같은 입력 2회 md5
  동일(결정론, ufb/wif 선례) + freeze 목록 실측 박제(현행 doc 의 정지 전건 —
  rid/joint/ut/rt/pairSrc). 운영 compare_verify.verify(mp4, report, workdir,
  align=, doc=) **무수정 그대로** ALL PASS 확인 → baseline_verdict.json
  (freezes 목록 + 리그 판정 라인 + md5 2회).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && backend/.venv/bin/python .planning/quick/260814-0p2-fresh-pdshape-freeze/inject_freeze.py --check-baseline (baseline_verdict.json 존재 + 리그 ALL PASS + md5 2회 동일 + freeze 전건 기록 assert) && rtk git diff --stat backend/ (0줄) && git status --porcelain backend/ (빈 출력)</automated>
  </verify>
  <done>SUPPORT-SURFACE.md 에 미지원 실측 + 사본 확장 근거 + 반영 시 backend 변경 목록 박제. 베이스라인 재렌더가 운영 리그 무수정 ALL PASS + 결정론 md5 2회 동일. backend/ diff 0.</done>
</task>

<task type="auto">
  <name>Task 2: 주입 재렌더 + 리그 ALL PASS + diff 국한 증명 + 카드 상속 실증</name>
  <files>.planning/quick/260814-0p2-fresh-pdshape-freeze/inject_freeze.py, .planning/quick/260814-0p2-fresh-pdshape-freeze/evidence/inject_verdict.json, .planning/quick/260814-0p2-fresh-pdshape-freeze/evidence/cards/</files>
  <action>
  (1) **주입 렌더 (주입 on) 2회** — 정지 목록 = 베이스라인 전건 + 신규 1건
  (ut 순 정렬로 삽입 위치는 자동). 같은 입력 2회 md5 동일(결정론).
  expectedFreezes 는 주입 반영 수로 report 에 정합 (A2 게이트 정합).
  (2) **리그 게이트** — 운영 compare_verify.verify 를 doc/align 제공 호출.
  기존 freeze 전건 = 운영 기준 무수정 PASS. 신규 freeze 의 H2 는 사본 delta:
  verify 호출 전 compare_verify._H2_UT_DISPLACING_SRC 에 "discover" 1값만
  추가(monkeypatch — 판정 로직·타 항목 무접촉), 이 delta 를 inject_verdict.json
  에 "운영 반영 시 compare_verify 면제 튜플 확장 필요" 로 명기. 전 항목
  (A/A2/B/C/D/E/F/G/H1~H4) PASS 아니면 FAIL 그대로 박제하고 통과 조작 금지.
  (3) **diff 국한 기계 증명** — baseline vs injected: (a) report 수준 —
  기존 정지 전건의 (rid, ut, rt, pairSrc, text) 필드 동일 + 신규 정지 정확히
  1건 assert. (b) 내용 수준 — 각 report 의 자체 출력 타임라인 매핑으로 기존
  정지 각각의 중앙 프레임을 양쪽 mp4 에서 추출해 pixel/md5 동일 + 재생 구간
  표본 프레임 동일. 삽입점(u12.87) 이후 출력 초가 신규 정지 길이만큼 이동하는
  것은 의도 변경으로 명기(무변경 주장 금지 — 내용 동일성으로 증명한다).
  (4) **카드 상속 실증** — wif discover_knee.py render 스테이지 패턴으로
  app._run_gated_card_inherit 를 신규 report(정지 6건)로 호출: S3/Firestore
  로컬 스텁(put 캡처만), **기계 눈 = wif evidence/eye_ledger + 렌더 헬퍼 원장
  replay 스텁 (Gemini 실호출 0** — locked). replay 매핑 불가 시 실호출로
  대체하지 말고 그 지점을 박제 후 보고. 확인: 왼무릎 카드가 신규 freeze
  (u12.8667/r12.40) 를 그대로 상속해 방출 + 카드 초 라벨 = 실효 fps 환산
  약 12.9s (u8i label_fps — 9.0/18.0 분모 사용 0) + 기존 생존 카드(왼팔꿈치
  등) 무회귀. wif 선례 주석: cand13b 카드의 V 베이크는 user left_ankle conf
  0.489 < 0.5 게이트로 생략될 수 있음(링 대조 카드 = belle 이 채택한 그
  실물) — 강제 베이크 금지, 게이트 산출 그대로.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && backend/.venv/bin/python .planning/quick/260814-0p2-fresh-pdshape-freeze/inject_freeze.py --check-inject (inject_verdict.json: 리그 전 항목 PASS + 결정론 md5 2회 + 기존 정지 전건 필드·프레임 동일 + 신규 정지 정확 1건 + 카드 상속 userSec==12.8667/refSec==12.40 + 눈 실호출 0 assert) && git status --porcelain backend/ (빈 출력)</automated>
  </verify>
  <done>주입 재렌더 리그 ALL PASS(사본 delta 1값 명기) + diff = 신규 정지 1 국한 기계 증명 + 무릎 카드 freeze 상속 방출 + 라벨 실효 fps 확인 + Gemini 실호출 0. backend/ diff 0 유지.</done>
</task>

<task type="auto">
  <name>Task 3: 실물 육안 전수 + 한글 사본 + 박제 커밋</name>
  <files>.planning/quick/260814-0p2-fresh-pdshape-freeze/evidence/stills/, .planning/quick/260814-0p2-fresh-pdshape-freeze/evidence/VISUAL-REVIEW.md, .planning/quick/260814-0p2-fresh-pdshape-freeze/FINDINGS.md</files>
  <action>
  (1) **실물 산출** — 재렌더 영상에서 신규 정지 구간 스틸(정지 중앙 프레임,
  양 패널 + 자막 보이게) + 기존 정지 전건 대조 스틸(baseline/injected 짝) 추출
  → evidence/stills/. 상속 카드 PNG → evidence/cards/ (Task 2 산출 확인).
  (2) **frames-before-numbers 육안 전수** — 실행자가 스틸·카드 전건을 Read 로
  직접 열어 확인(왼무릎 정지 = 학생 접힘 vs 기준 신전 장면인가, 마크·자막·
  초 라벨이 카드와 정합인가, 기존 정지가 눈으로도 무변경인가) →
  VISUAL-REVIEW.md 에 장별 기록. 수치 통과만으로 완료 주장 금지.
  (3) **belle 확인 재료 한글 사본** — /Users/Shared/sunity-freeze-inject-260814/
  에 복사: 재렌더영상_신규정지포함.mp4, 신규정지_왼무릎_12.9s_스틸.png,
  상속카드_왼무릎_12.9s.png, 기존정지_무변경_대조 스틸들, 안내.md(무엇이
  바뀌었고 무엇이 안 바뀌었는지 + S3 업로드 보류 상태 명시). 이미지 전달
  정본은 보드 embed 이므로 파일 절대경로 목록을 FINDINGS 에 병기.
  (4) **FINDINGS.md 박제** — 리그 판정 요약 + 사본 delta 1값(반영 시 backend
  필요 변경) + 제약 준수 증빙(backend/ diff 0 porcelain, S3 put 0, Firestore
  쓰기 0, Gemini 실호출 0) + LLM 학습 영향 1행(실호출 0, replay 만) +
  보존/휘발 구분(mp4 는 /Users/Shared 사본 = 휘발 가능, 보존은 belle 확인 후
  반영 단계의 S3 업로드가 담당 — scratchpad/Users/Shared 를 "보존"으로 주장
  금지) + 다음 1단계 = belle 실물 확인 -> 반영(업로드 + doc 갱신 + backend
  pairSrc 면제 확장) 별도 사이클. evidence 전건 + FINDINGS 커밋.
  </action>
  <verify>
    <automated>ls /Users/Shared/sunity-freeze-inject-260814/ | grep -c mp4 (>=1) && ls .planning/quick/260814-0p2-fresh-pdshape-freeze/evidence/stills/ | wc -l (>=6) && test -s .planning/quick/260814-0p2-fresh-pdshape-freeze/evidence/VISUAL-REVIEW.md && test -s .planning/quick/260814-0p2-fresh-pdshape-freeze/FINDINGS.md && git log --oneline -1 (0p2 커밋 존재)</automated>
  </verify>
  <done>스틸·카드 전건 육안 기록 완료(VISUAL-REVIEW.md), /Users/Shared 한글 사본 + 안내 존재, FINDINGS 에 S3 업로드 보류·반영 요건·제약 준수 증빙 박제, evidence 커밋 완료.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로컬 하네스 -> S3 | read-only GET 만 (영상·mp3 회수). put 호출 = 스텁 캡처 |
| 로컬 하네스 -> Firestore | doc/refmotion 읽기 전용. 쓰기 API 호출 0 |
| 리그 게이트 사본 delta | 판정기 신뢰 경계 — 면제 1값 외 판정 로직 무접촉이 게이트 무결성 조건 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0p2-01 | Tampering | compare_verify 사본 면제 | mitigate | delta = "_H2_UT_DISPLACING_SRC + 1값" 만, verdict 에 명기 박제 + 기존 freeze 는 무수정 판정기 기준 PASS 를 별도 assert |
| T-0p2-02 | Spoofing | pairSrc 라벨 | mitigate | align-peak/align-pole 사칭 금지 — 신설 라벨 "discover" 로만 주입 |
| T-0p2-03 | Info Disclosure | AWS/SSM 자격 | mitigate | --profile sunity-motion 사용, 키 값 로그 금지 (wif 선례), Gemini 키 미사용(실호출 0) |
| T-0p2-04 | Tampering | backend 운영 코드 | mitigate | 전 Task verify 에 `git status --porcelain backend/` 빈 출력 게이트 |
</threat_model>

<verification>
- backend/ diff 0 (porcelain 빈 출력) — 전 Task 공통 게이트.
- 베이스라인: 운영 리그 무수정 ALL PASS + md5 결정론 2회.
- 주입: 리그 전 항목 PASS(사본 delta 1값 명기) + 기존 정지 전건 필드·프레임
  내용 동일 + 신규 정지 정확 1건 + 카드 상속(u12.8667/r12.40) + 라벨 실효 fps.
- S3 put 0 / Firestore 쓰기 0 / Gemini 실호출 0 — 스텁 캡처 로그로 증빙.
- frames-before-numbers: 스틸·카드 전건 실행자 Read 육안 기록 존재.
</verification>

<success_criteria>
- 재렌더 영상 실물 + 상속 카드 실물 + /Users/Shared 한글 사본이 존재하고,
  belle 이 열어볼 수 있다 (완료 = 열어볼 수 있는 물건 + 기계 판정 PASS).
- 기계 증명 3종 성립: 리그 ALL PASS, diff 국한, 결정론.
- S3 업로드·doc 갱신은 하지 않았고 그 사실이 FINDINGS 에 명기돼 있다.
- 반영 단계에 필요한 backend 변경(주입 레이어 + H2 면제 라벨)이 목록으로
  박제돼 다음 사이클이 재조사 없이 착수 가능하다.
</success_criteria>

<output>
Create `.planning/quick/260814-0p2-fresh-pdshape-freeze/260814-0p2-SUMMARY.md` when done
</output>
