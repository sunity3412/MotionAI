# VISUAL-REVIEW — frames-before-numbers 육안 전수 기록 (quick-260814-0p2)

실행자가 스틸 11장 + 카드 3장 전건을 Read 로 직접 열어 확인한 기록이다.
수치 게이트(리그/md5)와 별도로, 눈으로 본 것만 적는다.

## 1. 신규 정지 (주입 freeze — 판정 대상의 핵심)

### stills/new_r04_left_knee_inj_idx1409.png (재렌더 영상 out 46.97s, 정지 중앙)

- **학생 패널(좌)**: 역립, 폴측 **왼무릎이 깊게 접혀** 폴을 감고 있고 반대
  다리는 수평 신전. 접힌 무릎 위에 빨간 실선 링 1개 (solid — align conf
  게이트 통과 좌표). 링이 접힌 무릎 관절 위에 정확히 앉아 있다.
- **기준 패널(우)**: 같은 역립 국면, 폴측 다리가 **폴을 따라 완전 신전**.
- **자막**: "왼쪽 무릎 각도가 기준 셰이프와 차이가 있어요. 무릎을 펴는 게
  아니라, 깊게 접은 다리 모양 그대로 기준 자세에 겹쳐 맞춰보세요" — r04
  record 운영 코칭 문구 그대로 (H3 진품 일치와 정합).
- **판정: PASS** — belle 채택 cand13b 의 "접힘 vs 신전" 대조가 영상 정지로
  그대로 성립. wif 전신 짝 스틸(cand13b_user_12.8667s / ref_12.4s)과 같은
  장면이다.

### cards/zoom_angle_vs_reference__left_knee.png (상속 카드)

- 좌 = 학생 접힌 왼무릎 확대 + 링 / 우 = 기준 신전 다리 확대 + 링.
- 좌하단 초 라벨 **12.9s** — 실효 fps 환산 (doc 값 12.9043s, ÷9.0 분모 0).
- V 베이크 없음 — user left_ankle conf 0.489 < 0.5 게이트 생략
  (`align_bake miss ... left_ankle=0.489` 로그, wif 와 동일). 강제 베이크 안 함.
- **md5 = wif cand13b 카드와 byte-동일** (e891e7ae…) — belle 이 채택한 그
  실물이 6정지 report 상속 경로에서도 똑같이 방출된다.
- **판정: PASS**.

## 2. 기존 정지 5건 — baseline / injected 대조 (눈으로도 무변경인가)

각 정지의 중앙 프레임을 두 mp4 에서 같은 방법(select=eq 인덱스 정확 추출)으로
뽑아 좌우로 비교했다.

| rid | 장면 (육안) | base | injected | 육안 판정 |
|-----|------------|------|----------|-----------|
| r00 | 왼팔꿈치 — 역립, 폴 아래 그립, 팔꿈치 링(solid), 왼팔꿈치 자막 | idx303 | idx303 | **동일** (기계: cross md5 동일) |
| r01 | 오른팔꿈치 — 역립 그립, 점선(est) 링, 오른팔꿈치 자막 | idx618 | idx618 | **동일** (기계: cross md5 동일) |
| r04(구) | 왼무릎 — 양쪽 다 접힘 국면(전환 장면), 무릎 링, 왼무릎 자막 | idx1045 | idx1045 | **동일** (기계: cross md5 동일) |
| r02 | 오른어깨 — 역립 어깨 장면, 어깨 링, 겨드랑이 자막 | idx1470 | idx1763 | **동일하게 보임** (기계: 코덱 노이즈 crossMax 69/101px·mean 0.0115 — 구조 차 육안 0) |
| r03 | 왼골반 — 벌림 장면, 양 패널 사이각 선 2줄+호, 왼다리 벌림 자막 | idx1907 | idx2200 | **동일하게 보임** (기계: crossMax 3 — 디코드 노이즈) |

- r04(구) 정지가 그대로 살아 있는 것 확인 — 삽입은 **추가**이지 교체가 아니다.
  구 freeze(10.5s)는 카드 게이트에서 hold=moving 으로 계속 침묵 (기존과 동일).
- r02/r03 은 삽입점(42.07s) 이후라 H.264 재인코드 노이즈가 있으나 (verdict 에
  수치 박제) 눈으로는 차이를 찾을 수 없었다 — 마크·자막·자세·크롭 전부 동일.

## 3. 기존 생존 카드 2장 — 무회귀

- **cards/zoom_angle_vs_reference__left_elbow.png**: 좌 = 학생 왼팔꿈치 V
  (관절 위 꺾인 두 선) + 5.3s 라벨 / 우 = 기준 왼팔꿈치 V. u8i Pod 실증
  카드와 같은 문법·같은 라벨 (5.3s). **PASS**.
- **cards/zoom_angle_vs_reference__left_hip.png**: 좌 = 학생 골반 P3
  하이브리드(화살표 계열 마크) + 16.7s 라벨 / 우 = 기준 골반 동일 문법.
  u8i 실증 카드와 같은 문법·같은 라벨 (16.7s). **PASS**.

## 4. 종합

- 신규 정지 1건 = 채택 발굴 순간이 영상·카드로 표현됨 (접힘 vs 신전 대조 성립).
- 기존 정지 5건 = 눈으로도 무변경 (기계 증명과 정합).
- 카드 3장 = 신규 1(wif 채택 실물과 byte-동일) + 기존 2(무회귀).
- 수치 통과만으로 완료 주장하지 않음 — 이 문서가 육안 근거다.
