---
phase: quick-260814-di7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/shared/python/sunity_shared/models.py
  - backend/shared/python/sunity_shared/s3keys.py
  - backend/shared/python/sunity_shared/firestore_admin.py
  - backend/shared/python/sunity_shared/analysis/compare_render.py
  - backend/shared/python/sunity_shared/analysis/compare_verify.py
  - backend/tests/phase35/test_discovery_freeze.py
  - docs/contract.md
  - .planning/quick/260814-di7-s3-doc-freeze-discover/wire_discover.py
  - .planning/quick/260814-di7-s3-doc-freeze-discover/evidence/
autonomous: true
requirements: [QUICK-260814-DI7]

must_haves:
  truths:
    - "compare_render.build_timeline 이 doc result.discovery.items 를 읽어 discover freeze 를 추가한다 — 운영 코드 정식 경로 (monkeypatch 0), 실행 로그 '[discover]' 실물 캡처"
    - "무수정 compare_verify 가 discover freeze 포함 ALL PASS — 단 doc discovery 에 그 순간(±0.2s)이 없으면 H2 FAIL (fail-closed, 외부 삽입 검출 유지 — 음성 대조 실증 포함)"
    - "배선 경로 재렌더의 compose 프레임 md5 사슬 == chd 승인본 (repo 고정 frames_md5_injected.json 전건)"
    - "프로덕션 반영 실행 로그 박제: chd 승인본 mp4 byte-보존 업로드(md5 77cdcd436472438f3580cbb8d48683f3), discover mp3 canonical 키 업로드, doc discovery 기입 + renderedCompare.freezes 갱신"
    - "doc 반영 후 live doc 재fetch → 재렌더 → 무수정 verify ALL PASS (영속화 필드가 운영 경로를 실제 구동)"
    - "기존 record freeze 경로 무회귀: discovery 부재 doc = byte-동일 (베이스라인 사슬 == chd frames_md5_baseline.json) + pytest 59 failed 기준선 동일 + 산식 5파일 diff 0"
    - "backend diff 에 pdshape/p34fresh/좌표 리터럴 0 — 일반 경로 (belle: 다른 영상들도 이런식으로)"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/compare_render.py"
      provides: "build_timeline discovery 주입 레이어 (record 루프 뒤, 공식 경로)"
    - path: "backend/shared/python/sunity_shared/analysis/compare_verify.py"
      provides: "H1~H4 discover 정식 지원 (단일 매칭 헬퍼, fail-closed)"
    - path: "backend/shared/python/sunity_shared/firestore_admin.py"
      provides: "_validate_discovery + update_analysis_discovery (coachAudio 뼈대)"
    - path: "backend/shared/python/sunity_shared/models.py"
      provides: "DISCOVERY_* 계약 블록"
    - path: "backend/shared/python/sunity_shared/s3keys.py"
      provides: "build_discover_audio_key canonical 단일 출처"
    - path: "backend/tests/phase35/test_discovery_freeze.py"
      provides: "주입 레이어 + verify discover + validator 유닛 (합성 fixture, 실좌표 0)"
    - path: ".planning/quick/260814-di7-s3-doc-freeze-discover/wire_discover.py"
      provides: "배선 재현 + 프로덕션 반영 + live 검증 드라이버"
    - path: ".planning/quick/260814-di7-s3-doc-freeze-discover/evidence/"
      provides: "wire_verdict.json / production_log.json / live_verdict.json"
  key_links:
    - from: "compare_render.build_timeline"
      to: "doc result.discovery.items"
      via: "record 루프 뒤 주입 레이어"
      pattern: "discovery"
    - from: "compare_render.build_timeline"
      to: "audio_dir/<basename(mp3Key)>"
      via: "mp3 조인 규약 (r{NN}.mp3 비충돌)"
      pattern: "mp3Key"
    - from: "compare_verify H2/H3/H4"
      to: "doc result.discovery.items"
      via: "_discovery_item_for 단일 매칭 헬퍼 (rid + |Δut|<=0.2s)"
      pattern: "_discovery_item_for"
    - from: "firestore_admin.update_analysis_discovery"
      to: "result.discovery 단일 field-path"
      via: "coachAudio 선례 부분 갱신"
      pattern: "result\\.discovery"
    - from: "wire_discover.py --live"
      to: "Firestore doc (반영 후 재fetch)"
      via: "영속화 필드 → 렌더 구동 증명"
      pattern: "live_verdict"
---

<objective>
발굴 채택 freeze 를 사본 delta(monkeypatch)에서 **doc 영속화 + 운영 코드 정식 경로**로 승격하고, belle 승인본(chd 재렌더)을 프로덕션 S3/doc 에 반영한다.

Purpose: chd 까지는 discover freeze 가 드라이버 수준 사본 delta 였다. 이번 배선 후에는 무수정 compare_verify 가 discover freeze 포함 ALL PASS 해야 하고(면제 monkeypatch 0), belle 의 "다른 영상들도 이런식으로" 지시대로 향후 다른 동작의 발굴 채택에도 그대로 재사용되는 일반 경로여야 한다 (pdshape 전용 하드코딩 금지).

Output: backend 배선 5파일 + 계약 문서 + 유닛 테스트 + 배선 재현 게이트(프레임 md5 사슬 == chd 승인본) + 프로덕션 반영 실행 로그(S3 2건 + Firestore 2건) + live doc 재렌더 검증 + push.
</objective>

<context>
@.planning/quick/260814-0p2-fresh-pdshape-freeze/evidence/SUPPORT-SURFACE.md   (§5 = 변경 목록 뼈대, §1~§3 = H 게이트 실측)
@.planning/quick/260814-chd-freeze-belle-ok-0p2/260814-chd-SUMMARY.md          (승인본 생산 스펙)
@.planning/quick/260814-chd-freeze-belle-ok-0p2/inject_freeze.py               (주입 스펙 실증본 — _install_injection 이 승격 대상 스펙)
@.planning/quick/260814-chd-freeze-belle-ok-0p2/evidence/inject_verdict.json   (freeze 6건 정본값 + mp4Md5 77cdcd43…)
@backend/shared/python/sunity_shared/analysis/compare_render.py                (build_timeline :1119-1321)
@backend/shared/python/sunity_shared/analysis/compare_verify.py                (:166-282 H1~H4)
@backend/shared/python/sunity_shared/firestore_admin.py                        (:1441-1646 coachAudio/renderedCompare 선례)
@backend/shared/python/sunity_shared/models.py                                 (:327-374 계약 블록 선례)
@backend/shared/python/sunity_shared/s3keys.py

## 좌표 (locked — chd/wif 정본)

- uid `fvcNXzEqKjgqVxRPVSj1iwFnIpn2` / aid `p34fresh1786628533` / motion `ref-pdshape` / bucket `sunity-motion-pilot-videos`
- 채택 순간: rid `r04` / joint `left_knee` / userSec `12.8667` / refSec `12.40` / pairSrc `discover`
- text: chd `inject_freeze.py` `DISCOVER_TEXT` 원문 문자 단위 그대로 — **기계 대조 필수**: chd `evidence/inject_verdict.json` `meta.inject.text` 와 == 비교로 전사 drift 차단
- mp3: `.planning/quick/260814-chd-freeze-belle-ok-0p2/evidence/discover_left_knee.mp3` (64,700 bytes, repo 고정 — 재합성 금지, Polly 0회)
- 승인본 mp4 (md5 `77cdcd436472438f3580cbb8d48683f3` — planner 실측 일치 확인 완료):
  1순위 `/Users/Shared/sunity-freeze-inject-260814/재렌더영상_신규정지포함.mp4`
  2순위 구 세션 scratchpad `/private/tmp/claude-501/-Users-kimtaesung-Dev-SunityMotion/ae166167-1abf-4754-9fe8-336a719ef9e2/scratchpad/chd_out/inject_run1.mp4` (휘발 — 있으면 md5 재확인)
  md5 불일치/전멸 = STOP (belle 승인은 이 바이트에 붙어 있다)
- chd 대조 사슬 (repo 고정): `.planning/quick/260814-chd-freeze-belle-ok-0p2/evidence/frames_md5_baseline.json` / `frames_md5_injected.json`
- chd freeze 6건 정본 (voiceStartOutS): r00 5.33 / r01 15.67 / r04 29.93 / **r04(discover) 42.07** / r02 54.17 / r03 69.0, discover freezeS 11.18
- wif_fresh 캐시: 구 세션 scratchpad (위 경로) `wif_fresh/` 생존 확인됨(planner 실측 08-14 오전) — 있으면 현 세션 scratchpad 로 복사 재사용, 없으면 chd `fetch()` 패턴 재수화 (S3 GET/Firestore 읽기 전용). 영상 정체성 게이트 = align userFrames 272 / refFrames 237

## Planner 설계 결정 (실측 근거 포함)

- **D-di7-01 doc 스키마**: `result.discovery = {"items": [...]}`, item 키 = `("rid", "joint", "userSec", "refSec", "pairSrc", "text", "mp3Key", "adoptedAt")` 정확히. pairSrc enum `("discover",)`. 배열 안 scalar-only map (Firestore 중첩 배열 금지 정합 — `_validate_dict_only_scalars` 라우팅, coachAudio 선례). 통째 교체 `update_analysis_discovery(uid, analysis_id, items)`.
- **D-di7-02 mp3 조인 규약**: s3keys `build_discover_audio_key(uid, analysis_id, rid, joint)` → `results/{uid}/{analysis_id}/discover_audio_{rid}_{joint}.mp3` (단일 출처 — build_coach_audio_key 선례). audio_dir 조인 = **mp3Key 의 basename** (`discover_` 접두라 `r{NN}.mp3` 와 구조적 비충돌). validator 가 items 간 mp3Key 중복을 거부 (같은 rid+joint 복수 채택 미래 케이스는 키 규약 확장 의제 — 지금은 fail-closed).
- **D-di7-03 coachAudio 무접촉**: discovery mp3 는 coachAudio.items 에 넣지 **않는다**. 근거 실측 = app.py:4203-4208 mp3 회수 루프가 rid 로 파일명을 만들므로(`audio_dir/r04.mp3`) 같은 rid 항목이 원본 record mp3 를 덮어쓴다 + 앱 audioCue prefetch 가 coachAudio 를 cueId 로 조인. H4 는 discover 분기(discovery 필드 자체 조인)로 해소 — scope 의 "discovery 필드 자체 조인" 안 채택. app.py 는 이번 사이클 무접촉 (fresh 파이프라인은 discovery 를 가질 수 없고, 재렌더는 드라이버 경로 — Pod 실증 사이클에서 재검).
- **D-di7-04 H2 fail-closed (튜플 미확장)**: SUPPORT-SURFACE §5-2 의 `_H2_UT_DISPLACING_SRC` 튜플 추가 제안은 **scope 의 fail-closed 요구가 대체** — 튜플 blanket 면제는 임의 순간 이동이 discover 를 사칭할 수 있다(T-di7-01). `"discover"` 는 rec-부재 guard **앞**에서 자체 분기: doc discovery 목록에 rid+순간(±`_H2_TOL_S` 0.2s) 매칭이 있을 때만 PASS, 없으면 FAIL "doc discovery 에 없는 순간 (외부 삽입 의심)". 튜플은 `("align-peak", "align-pole")` 불변. 코드 주석에 대체 사유 명기.
- **D-di7-05 doc freezes rid 구분**: renderedCompare.freezes 의 discover 항목 rid = `"r04:discover"` (콜론 접미 — §12.3 recordId 콜론 규약 정합). 근거 실측 = 앱 `RenderedComparePlayer.tsx:251` 이 `key={f.rid}` 라 rid `r04` 중복 = React duplicate key. `freezeNumber(":discover" 접미)` 는 null → 번호 없는 틱 = 앱이 설계한 fail-open 분기(:81 주석 "형식이 달라지면 번호 없이 마커만"). 앱 코드 무접촉.
- **D-di7-06 표시 스펙 = chd 실증 그대로**: discover freeze 의 표시 = `_align_markers(align, rec, ut)` + knee 관절(`left_knee`/`right_knee`)만 `_body_line_viz(align, ut, rt, poles)` (성립 시 markers=[] — compare_render.py:1261 미러), legs_viz/viz_kind/viz_side/pole_viz = None. 짝 재선정(피크/폴/그립/가중) **미실행** — 채택이 이미 ut/rt 를 확정했다. 비-knee 관절 발굴의 표시 문법 확장은 실물이 생길 때 (과잉 일반화 금지 — 승인 PASS 보호). rt 는 record 경로와 같은 `REF_BOUNDARY_PIN_S` 경계 제외 적용 (12.40 은 경계 밖 = 이번 결과 불변).
- **주의**: `git add` 가 `git diff --exit-code` 를 무력화한 이력 — 무접촉 게이트는 커밋 범위 diff(`git diff <base>..HEAD -- <파일>`)로 판정. 파일 생성은 Write 도구만 (heredoc 금지). 이모지 금지.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: backend 배선 — discovery 영속화 규약 + 주입 레이어 + verify discover 정식 지원 + 유닛</name>
  <files>backend/shared/python/sunity_shared/models.py, backend/shared/python/sunity_shared/s3keys.py, backend/shared/python/sunity_shared/firestore_admin.py, backend/shared/python/sunity_shared/analysis/compare_render.py, backend/shared/python/sunity_shared/analysis/compare_verify.py, backend/tests/phase35/test_discovery_freeze.py, docs/contract.md</files>
  <behavior>
    합성 fixture 만 사용 (실좌표/동작명/분석 ID 리터럴 0 — 예: rid "r07", 임의 초). backend/tests/phase35/test_discovery_freeze.py:
    - build_timeline: discovery items 1건 → freezes 에 pair_src "discover" 1건 추가, 필드(text/dur=mp3_duration_s+FREEZE_TAIL_S/joint/ut/rt) 정확. knee 관절은 body_viz 시도 + 성립 시 markers 비움, 비-knee 관절은 markers 만 (legs/pole/viz_kind/viz_side None)
    - build_timeline 하위호환: result 에 discovery 부재/빈 items → freezes/excluded 가 종전과 완전 동일 (기존 record freeze 경로 byte-동일의 유닛판)
    - build_timeline fail-closed: audio_dir 에 basename(mp3Key) 부재 → excluded 에 reason "discover_no_mp3" 회계 (freeze 미추가, 예외 0)
    - authenticity_checks H2: discover freeze + doc discovery 순간 일치(±0.2s) → PASS 라벨 "H2 순간 {rid}[discover]" / 순간 0.5s 비틀기 → FAIL / doc 에 discovery 목록 자체 부재 → FAIL (blanket 면제 없음 검증)
    - authenticity_checks H3: discover freeze expected = discovery item 의 text 문자 일치 (라벨 "[discover]"). 같은 rid 의 원본 record freeze H3 는 종전 로직 그대로 (rid 공유 케이스 — chd stock 2FAIL 의 구조 원인 해소 검증)
    - authenticity_checks H4: discover freeze = discovery item mp3Key "results/" prefix 로 조인 PASS (라벨 "[discover]"), coachAudio 에 없는 신규 rid 도 PASS / 매칭 부재 → FAIL
    - authenticity_checks H1: **신규 rid** discovery(doc records 에 없는 rid) 렌더 시 eligible 에 합류해 PASS — 미래 케이스 게이트 (scope 3 의 H1 요구)
    - authenticity_checks 하위호환: discovery 부재 doc → 체크 목록이 종전과 완전 동일
    - _validate_discovery: 정상 payload 통과 / 키 여분·누락 거부 / pairSrc enum 거부 / mp3Key prefix·suffix 거부 / items 간 mp3Key 중복 거부 / item 내 list 값(중첩 배열) 거부
    - build_discover_audio_key: 키 문자열 규약 정확 + basename "discover_" 접두 (r{NN}.mp3 비충돌)
  </behavior>
  <action>
    RED(실패 테스트 커밋) 후 구현. 전부 context 의 D-di7-01~06 스펙대로:

    **models.py** (:374 `PLAYBACK_ASSET_RENDERED_COMPARE` 뒤): DISCOVERY 계약 블록 신설 — `DISCOVERY_KEYS = ("items",)`, `DISCOVERY_ITEM_KEYS = ("rid", "joint", "userSec", "refSec", "pairSrc", "text", "mp3Key", "adoptedAt")`, `DISCOVERY_PAIR_SRC = "discover"`. 주석: 발굴 채택 freeze 의 doc 영속화 (quick-260814-di7) — 분석 **사후** 채택 산출물, 앱 미독(계약 문서화만 — TS 무변경), 소비자 = compare_render.build_timeline 주입 + compare_verify H 게이트, mp3Key = s3keys.build_discover_audio_key 단일 출처, docs/contract.md §12.10 lockstep. renderedCompare freezes 주석(:360-364)에 D-di7-05 규칙 1줄 추가 (discover 정지 틱은 rid 에 ':discover' 접미 — 앱 freezeNumber fail-open 무번호 틱 + React key 중복 회피).

    **s3keys.py**: `build_discover_audio_key(uid, analysis_id, rid, joint)` → `results/{uid}/{analysis_id}/discover_audio_{rid}_{joint}.mp3`. docstring 에 단일 출처 원칙 + basename 조인 규약(D-di7-02) 명기.

    **firestore_admin.py**: `_validate_discovery(payload)` — `_validate_coach_audio` 뼈대 복제: dict + 최상위 키 정확히 models.DISCOVERY_KEYS, items list, 각 item `_validate_dict_only_scalars` 라우팅 + 키 정확히 DISCOVERY_ITEM_KEYS + rid/joint/text/adoptedAt 비어있지 않은 str + userSec/refSec 유한 수 >=0 + pairSrc == models.DISCOVERY_PAIR_SRC + mp3Key "results/" prefix + ".mp3" suffix + items 간 mp3Key 중복 거부. `update_analysis_discovery(uid, analysis_id, items)` — `result.discovery` **단일 field-path** `.update()` + updatedAt (update_analysis_coach_audio 선례 그대로, 통째 교체 — merge 배열 병합 모호성 회피).

    **compare_render.py build_timeline**: record 루프(:1188-1320) **뒤**에 discovery 주입 레이어 — `(r.get("discovery") or {}).get("items")` 순회: mp3 = audio_dir / basename(mp3Key) (PurePosixPath.name), 부재 → print warn + excluded reason "discover_no_mp3" + continue. align 부재 → excluded reason "discover_no_align" + continue (운영 스테이지는 항상 align 보유 — 방어). rt 경계 = record 경로와 같은 REF_BOUNDARY_PIN_S 제외(reason "ref_boundary_pin"). rec = records by-rid 조회(부재 시 markers=[] — 신규 rid fail-open). 표시 = D-di7-06 스펙. freeze dict = record 경로와 같은 내부 키 형상(rid/ut/rt/pair_src="discover"/dur=mp3_duration_s(mp3)+FREEZE_TAIL_S/mp3/joint/markers/legs_viz/viz_kind/viz_side/pole_viz/body_viz/text=item 의 text). render() 가 ut 순 정렬하므로 append 순서 무관. **배선 로그**: 주입 성립마다 "[discover] rid=… joint=… ut=… rt=… mp3=…" print — 실행 로그 게이트의 증거 지점. 주석에 스펙 실증본 = chd inject_freeze.py `_install_injection` 인용.

    **compare_verify.py**: `_discovery_item_for(fz, doc)` 단일 매칭 헬퍼 — doc result.discovery.items 중 rid 동일 + |fz.userSec − item.userSec| 이 최소이면서 <= `_H2_TOL_S` 인 item, 없으면 None. H2/H3/H4 가 공유(매칭 규칙 drift 0). H2: `fz.get("pairSrc") == "discover"` 분기를 rec-부재 guard **앞**에 — 매칭 성립 PASS "(doc discovery 순간 일치)", 미성립 FAIL "doc discovery 에 없는 순간 (외부 삽입 의심)", 라벨 "H2 순간 {rid}[discover]". `_H2_UT_DISPLACING_SRC` 는 불변 유지 + D-di7-04 대체 사유 주석(SUPPORT-SURFACE §5-2 튜플안을 fail-closed 요구가 대체). H3: discover freeze 는 expected = 매칭 item 의 text (라벨 "[discover]", 매칭 None → FAIL), 비-discover freeze 무접촉 (rid 공유 원본 정지는 종전 cue_text 로직 그대로). H4: discover freeze 는 매칭 item 의 mp3Key "results/" prefix 판정 (라벨 "[discover]"), 비-discover 무접촉. H1: eligible 에 discovery items rid 합집합 (discovery 부재 doc = 종전과 완전 동일). "discover" 리터럴은 models.DISCOVERY_PAIR_SRC 참조 또는 리터럴+상수 언급 주석 (compare_verify 의 기존 import 스타일 존중).

    **docs/contract.md**: §12.10 신설 (짧게) — result.discovery 형상 표 + 목적(발굴 채택 freeze 영속화, 서버 전용/앱 미독 — TS 무변경 사유 명기) + models.py DISCOVERY 블록 lockstep + §12.9 freezes 절에 ':discover' rid 접미 규칙 1줄.

    금지: 산식 5파일(deduction_engine/dimensions/kismam/motiondtw/assemble) 접촉, app.py 접촉, app/ 접촉, 동작명·분석 ID·실좌표 리터럴(테스트 포함), 이모지, heredoc. GREEN 후 원자 커밋 2개: `test(quick-260814-di7): discovery freeze 배선 실패 테스트` → `feat(quick-260814-di7): 발굴 채택 freeze doc 영속화 + 주입 레이어 + verify discover`.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && PYTHONPATH=backend/tests backend/.venv/bin/python -m pytest -q backend/tests/phase35/test_discovery_freeze.py && PYTHONPATH=backend/tests backend/.venv/bin/python -m pytest -q backend/tests 2>&1 | tail -3 && git diff HEAD~2..HEAD -- backend/shared/python/sunity_shared/analysis/deduction_engine.py backend/shared/python/sunity_shared/analysis/dimensions.py backend/shared/python/sunity_shared/analysis/kismam.py backend/shared/python/sunity_shared/analysis/motiondtw.py backend/shared/python/sunity_shared/analysis/assemble.py --stat && git diff HEAD~2..HEAD -- backend/ | grep -cE 'pdshape|p34fresh|12\.8667|fvcNXzEqK' | grep -qx 0 && echo GATES-OK</automated>
  </verify>
  <done>신규 테스트 전부 PASS. 전체 pytest = 59 failed 기준선 동일 (신규 FAIL 0). 산식 5파일 diff 빈 출력. backend diff 좌표/동작 리터럴 0 (GATES-OK 출력). discovery 부재 doc 하위호환 유닛 PASS. 원자 커밋 2개 존재.</done>
</task>

<task type="auto">
  <name>Task 2: 배선 재현 게이트 — 운영 경로(monkeypatch 0) 재렌더 == chd 승인본</name>
  <files>.planning/quick/260814-di7-s3-doc-freeze-discover/wire_discover.py, .planning/quick/260814-di7-s3-doc-freeze-discover/evidence/wire_verdict.json</files>
  <action>
    드라이버 wire_discover.py 작성 — chd inject_freeze.py 의 fetch / _render_once / 사슬 대조 패턴을 미러하되 **주입·판정 monkeypatch 전면 제거** (compare_render.build_timeline / compare_verify 원본 그대로 호출 — 이것이 승격의 정의). SP = 현 세션 scratchpad 하위 `di7_fresh/`. 스테이지:

    `--fetch`: 구 세션 wif_fresh 생존 시 현 세션 SP 로 복사 재사용, 아니면 chd fetch() 패턴 재수화 (Firestore 읽기 + S3 GET 만 — 쓰기 0). 영상 정체성 게이트 = align userFrames 272 / refFrames 237 assert. discover mp3 를 chd evidence 에서 audio_dir 로 복사하되 파일명 = basename(build_discover_audio_key(uid, aid, "r04", "left_knee")) — D-di7-02 조인 규약의 실사용 (s3keys 함수를 import 해 단일 출처 확인).

    `--baseline`: discovery **없는** doc 으로 render 1회 → compose 프레임 md5 사슬 == chd frames_md5_baseline.json 전건 assert (배선이 기존 record freeze 경로를 1비트도 안 바꿨다는 실렌더 증명) + 무수정 compare_verify ALL PASS.

    `--inject`: discovery payload 조립 — items 1건 {rid "r04", joint "left_knee", userSec 12.8667, refSec 12.40, pairSrc "discover", text=DISCOVER_TEXT(chd inject_freeze.py 원문 복사 후 chd inject_verdict.json meta.inject.text 와 == assert), mp3Key=build_discover_audio_key(...), adoptedAt "2026-08-14"} — **이 dict 가 Task 3 update_analysis_discovery 입력과 단일 소스** (드라이버 상수 1곳). `_validate_discovery` 직접 호출로 사전 통과 확인. doc 사본 result.discovery 에 얹어 render 1회 — stdout 캡처에서 "[discover] rid=" 라인 존재 assert (배선했다는 주장은 실행 로그로). 게이트: (a) compose 사슬 == chd frames_md5_injected.json 전건 (b) report freezes 6건의 rid/joint/userSec/refSec/pairSrc/text/freezeS/voiceStartOutS == chd inject_verdict.json freezes (±0.01) (c) **무수정 compare_verify.verify(align+doc 제공) ALL PASS — 면제 delta 0** (d) 음성 게이트: doc 사본 discovery userSec 을 +0.5s 비튼 doc 으로 같은 mp4/report 에 verify 재실행 → "H2 순간 r04[discover]" FAIL 정확 발생 assert (fail-closed 실증 — verify 는 mp4+report 입력이라 재렌더 불필요).

    `--check-wire`: 위 전부를 evidence/wire_verdict.json 에 박제 + exit code 게이트 (chd check_inject 패턴 미러).

    제약: S3/Firestore 쓰기 0 (이 Task 는 읽기 전용), Gemini/Polly 호출 0, 카드 경로 재실행 불요 (chd 가 카드 3장 md5 == 0p2 로 캡션 비종속을 이미 기계 증명 — 재검 대상 아님). 렌더 결정론 규율 = chd _render_once 형식 미러. 커밋: `feat(quick-260814-di7): 배선 재현 드라이버 + wire_verdict (운영 경로 == chd 승인본)`.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && backend/.venv/bin/python .planning/quick/260814-di7-s3-doc-freeze-discover/wire_discover.py --check-wire</automated>
  </verify>
  <done>wire_verdict.json: baselineChainSame=true + injectedChainSame=true + reportFreezesMatch=true + rigStockAllPass=true (무수정 verify, 면제 delta 0) + discoverLogSeen=true + perturbH2Fail=true. exit 0. 커밋 존재.</done>
</task>

<task type="auto">
  <name>Task 3: 프로덕션 반영 실행 (S3 2건 + doc 2필드) + live doc 재렌더 검증 + push</name>
  <files>.planning/quick/260814-di7-s3-doc-freeze-discover/wire_discover.py, .planning/quick/260814-di7-s3-doc-freeze-discover/evidence/production_log.json, .planning/quick/260814-di7-s3-doc-freeze-discover/evidence/live_verdict.json</files>
  <action>
    belle 승인 완료된 반영 실행 (task_background — "좋다 다음 단계로 넘어가자"). wire_discover.py 에 스테이지 추가:

    `--apply` (프로덕션 쓰기 — 전 단계 로그 박제, AWS_PROFILE sunity-motion):
    (1) 승인본 mp4 소스 확보 — context 의 1순위/2순위 경로에서 md5 == 77cdcd436472438f3580cbb8d48683f3 인 파일. 불일치/전멸 = **STOP** (업로드 없이 보고 — belle 승인은 이 바이트에 붙어 있다). (2) doc 현행 result.renderedCompare.key 를 읽어 build_rendered_compare_key(uid, aid) 와 exact 일치 assert 후 그 키에 put_object(ContentType video/mp4) — 같은 키 덮어쓰기 = byte-보존 반영. 업로드 후 S3 GET md5 재확인 로그. (3) discover mp3 를 build_discover_audio_key(uid, aid, "r04", "left_knee") 키에 put_object(audio/mpeg) + 재확인. (4) update_analysis_discovery(uid, aid, items) — Task 2 와 단일 소스 payload. (5) update_analysis_rendered_compare(uid, aid, key, status done, freezes 6건) — freezes 는 Task 2 재현 report 의 [{rid, outSec=voiceStartOutS}], 단 discover 항목 rid 는 D-di7-05 규칙으로 "r04:discover" (나머지 5건 rid 그대로). 값이 chd 정본(5.33/15.67/29.93/42.07/54.17/69.0)과 ±0.01 일치 assert 후 기록. 전 쓰기(키/바이트/md5/타임스탬프/함수 인자) → evidence/production_log.json.

    `--live` (반영 검증 — 영속화 필드가 운영 경로를 구동한다는 최종 증명): Firestore 에서 doc **재fetch** (로컬 캐시 미사용) → result.discovery / renderedCompare 가 방금 쓴 payload 와 == assert → 그 live doc 으로 render 1회. 이때 discover mp3 는 **S3 의 방금 쓴 키에서 GET** 해 basename 규약으로 audio_dir 조인 (키 왕복 증명). 게이트: compose 사슬 == chd frames_md5_injected.json + 무수정 verify ALL PASS + "[discover]" 로그 존재 → evidence/live_verdict.json 박제.

    `--check-apply`: production_log + live_verdict 기계 게이트 (exit code).

    마무리: SUMMARY 작성 (output 절), 커밋 `feat(quick-260814-di7): 프로덕션 반영 실행 (S3 mp4/mp3 + doc discovery/renderedCompare) + live 검증` 후 **push** (Pod 이 origin 에서 당김 — 후속 Pod 실증 전제). LLM 학습 영향 보고 필수: 이 사이클 LLM 추론 호출 0 (Gemini 0 / Polly 0 — 렌더·업로드·doc 쓰기만). SUMMARY 에 후속 대기 = Pod 실증 사이클(새 Pod 재진입 + 운영 경로 재현 + `_run_deferred_compare_render` 의 discovery mp3 회수 배선은 그때 의제) 명기.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && backend/.venv/bin/python .planning/quick/260814-di7-s3-doc-freeze-discover/wire_discover.py --check-apply && git status --porcelain backend/ | wc -l && git log origin/main..HEAD --oneline | wc -l</automated>
  </verify>
  <done>production_log.json: mp4 md5 77cdcd43… 업로드 + 재확인 / mp3 canonical 키 업로드 / doc discovery + renderedCompare(freezes 6건, discover rid "r04:discover") 갱신 로그 전건. live_verdict.json: liveDocFieldsMatch=true + injectedChainSame=true + rigStockAllPass=true + discoverLogSeen=true. check-apply exit 0. push 완료 (origin/main..HEAD 커밋 0). SUMMARY 존재.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doc(Firestore) → 렌더/판정 | discovery 필드가 렌더 순간·자막·음성을 구동 — 오염 doc = 오염 영상 |
| 로컬 드라이버 → 프로덕션 S3/doc | belle 승인 바이트/값만 반영되어야 함 |
| freeze report → compare_verify | 외부 삽입 freeze 검출이 belle 신뢰 장치 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-di7-01 | Tampering | compare_verify H2 discover | mitigate | blanket 면제 금지 — doc discovery rid+순간(±0.2s) 매칭만 PASS, 유닛 + 드라이버 음성 게이트(순간 비틀기 FAIL) 실증 |
| T-di7-02 | Spoofing | H3/H4 discover (자막/음성 진품) | mitigate | expected = doc 영속화 원문 문자 일치 + mp3Key results/ prefix 조인 — 단일 매칭 헬퍼로 drift 0 |
| T-di7-03 | Tampering | 프로덕션 mp4 업로드 | mitigate | 소스 md5 == 77cdcd43… 사전 assert (불일치 STOP) + 업로드 후 S3 재확인 로그 |
| T-di7-04 | Tampering | doc 쓰기 (discovery/renderedCompare) | mitigate | scoped validator (키 화이트리스트 + enum + prefix + 중첩 배열 거부) + 단일 field-path update + live 재fetch == 대조 |
| T-di7-05 | DoS(회귀) | 기존 record freeze 경로 / 승인 5동작 | mitigate | discovery 부재 doc byte-동일 유닛 + 베이스라인 실렌더 사슬 == chd + pytest 59 기준선 + 산식 5파일 diff 0 |
| T-di7-06 | Info Disclosure | 로그/evidence | accept | uid/aid 는 기존 quick evidence 관례와 동일 범위 (PII 아님, 리포 내 기존 박제 다수) — 신규 노출 없음 |
| T-di7-SC | Tampering | 패키지 설치 | n/a | 신규 패키지 설치 0 (기존 backend/.venv 그대로) — 공급망 변화 없음 |
</threat_model>

<verification>
- 승격 완성: Task 2/3 의 verify 가 **무수정 compare_verify** (면제 monkeypatch 0) ALL PASS — chd 의 사본 delta 2축이 더 이상 필요 없음을 기계 증명
- 승인본 동등: 배선 경로 재렌더 compose 사슬 == chd frames_md5_injected.json (repo 고정) + report freeze 6건 값 일치
- 무회귀 3층: 유닛(discovery 부재 byte-동일) + 실렌더(베이스라인 사슬 == chd frames_md5_baseline.json) + pytest 전체 59 failed 기준선 동일
- 채점 무접촉: 산식 5파일(deduction_engine/dimensions/kismam/motiondtw/assemble) 커밋 범위 diff 빈 출력
- 일반 경로: backend diff 에 pdshape/p34fresh/12.8667/uid 리터럴 0 (grep 게이트)
- 프로덕션 반영: production_log.json (S3 2건 md5 재확인 + doc 2필드) + live_verdict.json (재fetch doc 이 렌더를 구동)
- 실행 로그: build_timeline "[discover]" 라인 캡처 (wire/live 양쪽)
- LLM 학습 영향: 추론 호출 0 보고 (SUMMARY 명기)
</verification>

<success_criteria>
- discover freeze 가 doc 영속화 + 운영 코드 정식 경로로 성립 — 무수정 verify ALL PASS (면제 monkeypatch 0), fail-closed 외부 삽입 검출 유지 (음성 게이트 실증)
- 프로덕션 반영 완료: belle 승인본 mp4 byte-보존 업로드 + discover mp3 canonical 키 + doc discovery/renderedCompare 갱신 — 전부 실행 로그 박제
- live doc 재렌더가 chd 승인본과 프레임 md5 사슬 동등 (영속화 → 렌더 구동의 왕복 증명)
- pytest 기준선 무회귀 + 산식 무접촉 + pdshape 하드코딩 0 (belle "다른 영상들도 이런식으로" 재사용 가능 일반 경로)
- push 완료 (후속 Pod 실증 사이클 전제 충족)
</success_criteria>

<output>
완료 시 `.planning/quick/260814-di7-s3-doc-freeze-discover/260814-di7-SUMMARY.md` 작성 — 기계 판정 한 줄(무수정 verify ALL PASS + 사슬 동등 + 프로덕션 반영 로그) + 성립한 것 / 제약 준수(채점 무접촉·이모지 0·LLM 추론 호출 0) / Deviations / 다음 1단계(= Pod 실증 사이클: 새 Pod 재진입 + 운영 경로 재현 + `_run_deferred_compare_render` discovery mp3 회수 배선 의제, 그리고 발굴 일반화 스윕 대기).
</output>
