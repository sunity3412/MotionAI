---
phase: 260814-j24-eye-s3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/training/datagen/harvest_eye.py
  - backend/tests/phase22/test_harvest_eye.py
  - backend/training/datagen/build_jsonl.py
  - backend/tests/phase22/test_eye_track.py
  - backend/training/distill/full_batch.py
  - backend/training/sft/run_retrain_cycle.sh
  - backend/training/data/eye_manifest.json
  - backend/training/data/manifest.json
  - backend/training/LICENSE-AUDIT.md
  - .planning/quick/260814-j24-eye-s3/EYE-HARVEST-REPORT.md
autonomous: true
requirements: [EYE-01, EYE-02, EYE-03, EYE-04, EYE-05, EYE-06]

must_haves:
  truths:
    - "기계 눈 원장 행이 학습 JSONL 행으로 실제 변환되어 파일로 존재한다"
    - "트랙-눈 불일치(match=false) 행이 버려지지 않고 샘플에서 식별 가능하다"
    - "eye 샘플에 score/severity 계열 필드가 구조적으로 부재한다"
    - "eye_loader 미주입 시 기존 3트랙 train/val JSONL 이 byte-동일하다"
    - "프라이버시 판정이 코드·문서 실측으로 내려져 산출물에 박제되고, 미판정분은 적재 0 으로 보류된다"
    - "수확 행에 uid/analysisId 등 사용자 식별자가 0 개다"
    - "재학습 사이클 assemble 스테이지가 eye 트랙을 태울 수 있다"
  artifacts:
    - path: "backend/training/datagen/harvest_eye.py"
      provides: "눈 원장 → 학습 원장 수확기(순수 변환 + dry-run/run 껍데기)"
      contains: "def consent_disposition"
    - path: "backend/tests/phase22/test_harvest_eye.py"
      provides: "수확기 순수층 TDD"
    - path: "backend/tests/phase22/test_eye_track.py"
      provides: "eye 트랙 + 기존 3트랙 무회귀 TDD"
    - path: "backend/training/data/eye_manifest.json"
      provides: "눈 크롭 수확 원장(provenance + disposition)"
    - path: ".planning/quick/260814-j24-eye-s3/EYE-HARVEST-REPORT.md"
      provides: "규모 실측 + 프라이버시 판정 + belle 결정 항목"
  key_links:
    - from: "backend/training/sft/run_retrain_cycle.sh"
      to: "training.distill.full_batch --with-eye"
      via: "assemble 스테이지 플래그"
      pattern: "with-eye"
    - from: "backend/training/distill/full_batch.py"
      to: "build_jsonl.build_dataset(eye_loader=...)"
      via: "make_eye_loader 주입"
      pattern: "eye_loader"
    - from: "backend/training/datagen/build_jsonl.py"
      to: "eye_manifest.json admit 행"
      via: "_build_eye_samples"
      pattern: "_build_eye_samples"
---

<objective>
기계 눈(machine_eye) 원장이 계속 쌓이는데 학습으로 가는 경로가 없다 — 그 경로를 뚫는다.
belle 지시(2026-08-14): "학습으로 흘러들어가게 해내야 한다."

Purpose: 눈 원장 = 마킹 크롭 + "이 관절이 접혔나 폈나" 판정 + 근거 + 신뢰도. 특히
trackAngleDeg 와 observed 가 어긋난 행은 keypoint 환각(belle 08-11 지목 근본 결함)의
라벨된 실례이며, 현 파이프라인보다 나아질 수 있는 감독 축이다.

Output: (1) build_jsonl 4번째 트랙 eye, (2) 눈 원장 수확기 + 수확 원장 eye_manifest.json,
(3) 재학습 사이클 배선, (4) 학습 JSONL 실물, (5) 규모 실측 + 프라이버시 판정 박제.

스코프 밖(이번 사이클 금지): 실제 SFT 학습 실행, IG 재수집 실행, 모델 승격,
학습셋·크롭 S3 업로드, Firestore 접촉.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@backend/training/datagen/build_jsonl.py
@backend/training/datagen/schema.py
@backend/training/distill/full_batch.py
@backend/scripts/phase22_watch.py
@backend/training/LICENSE-AUDIT.md
@backend/training/sft/run_retrain_cycle.sh
@backend/tests/phase22/conftest.py
</context>

<measured_facts>
플래너가 착수 전 실측한 값. 실행자는 이 값을 재측정해 리포트에 최종 수치를 박는다
(여기 값과 어긋나면 어긋남 자체를 리포트에 기록 — 조용히 맞추지 말 것).

리포 evidence 눈 원장 (.planning 아래 eye_ledger 디렉토리의 JSON):
- 파일 69 (ledger.json 8 + flat per-entry 61) / PNG 159 / entry 157
- observed == "error" 0건
- match=false 95건 (user 64 / ref 31) — 불일치 보존 대상
- side: user 112 / ref 45
- joint: left_hip 40, right_elbow 36, left_knee 26, right_shoulder 24,
  left_elbow 13, left_shoulder 13, right_knee 5
- claim 화살표 observed: bent→bent 60, bent→extended 34, extended→extended 23,
  extended→bent 18, bent→unclear 12, bent→off_body 4, extended→off_body 4,
  extended→unclear 2
- PNG 해결률 157/157 (entry key 의 basename, 또는 flat shape 는 JSON 과 같은 stem 의 .png)

원장 JSON 3형태 (수확기가 전부 흡수해야 함):
1. analysisId + entries 배열 — 운영/Pod 형 (8 파일)
2. flat 단일 entry, 키 = cand/claim/confidence/frameIdx/joint/limb/match/observed/reason/sec/side (8 파일)
3. flat 단일 entry + motion/rid/trackAngleDeg (53 파일) — ehz 스윕 형, motion 필드 보유

운영 S3: 버킷 sunity-motion-pilot-videos, 키 results/{uid}/{aid}/eye/ledger.json 및 크롭 PNG.
생산자 = backend/functions/pipeline/app.py 4979~5003행 업로드 블록, 필드 계약 단일 출처 =
app.py 4644행 eye_ledger.append(...) + card_gates.machine_eye 반환 dict.

기존 계약 실측 (재발명 금지):
- build_jsonl._is_customer_source(source) = source 에 customer/pilot/user 포함 시 고객 트랙
- build_jsonl._eligible_media_row = 고객 트랙이면 anonymized is True 필수
- manifest rows[0] = source "internal", anonymized false → internal seed 17행은
  현재도 anonymized=false 로 학습 소비 중 (_is_customer_source("internal") 은 False)
- manifest _meta.collection_complete = true, collection_batches = [] (한 번도 실행 안 됨)
- 배치 등재 규약 소유자 = backend/scripts/phase22_watch.py
  (make_batch_entry / register_batch / compute_batch_id / assert_ledger_invariants)
- assemble 사슬 = run_retrain_cycle.sh 91행 → full_batch --assemble --with-perturb --upload
  → full_batch.assemble_jsonl → build_jsonl.build_dataset
</measured_facts>

<privacy_verdict>
스코프 3(선행 게이트)의 판정. 이 판정은 임의 완화가 아니라 기존 정책의 기계적 적용이며,
실행자는 이를 그대로 코드 fence 로 구현하고 리포트에 근거와 함께 박제한다.

| ID | 대상 | 판정 | 근거(실측 인용) |
|----|------|------|------------------|
| P-1 | user 측 크롭 (수강생/내부 실증 업로드 유래) | hold — 적재 0 | LICENSE-AUDIT 7-1(c) "anonymize 강제 불변, 적재 전 강제, 소급 불가" + 7-1(d) 컷오프 2026-07-13 이후 문서는 learningOptIn=true 엄격. 눈 원장은 2026-08 생성 = 컷오프 이후. 이번 사이클 Firestore 무접촉이라 optIn 확인 불가 → fail-safe 보류 |
| P-2 | ref 측 크롭 (정은지 reference 영상 유래) | admit | LICENSE-AUDIT 5-1 internal seed 17행 = "자사 촬영 + 파일럿 참가 동의서(D-12 1겹)". _is_customer_source("internal") = False → D-12 가명처리 요건 비대상. 같은 영상 통째가 이미 anonymized=false 로 학습 소비 중 = 선례. 크롭이 원본 영상보다 엄격할 근거 없음 |
| P-3 | 운영 S3 results/{uid}/{aid}/eye/ 전량 | hold — 적재 0, 수확 원장에만 기록 | P-1 과 동일 사유 + S3 키 자체가 uid 포함. 읽기만 하고 disposition=hold 로 등재 |
| P-4 | 수확 행의 식별자 | uid·analysisId 금지 | LICENSE-AUDIT 119행 "매니페스트에 uid·사용자 식별자 필드 금지(테스트 fence)" + 7-1(f) "uid/analysisId 비파생, video_hash 기반만" → eye 행 식별자는 크롭 PNG content hash 단독 |
| P-5 | motion 미해결 행 | hold | motion 없는 샘플은 _balance_media 에서 무조건 통과(m is None 이면 kept) → 균등 규율 우회 = dump-all. 제약 위반이므로 fail-closed |

belle 결정 대기 항목 (리포트에 명기, 이번 사이클 임의 진행 금지):
- B-1: user 측 크롭 112건(불일치 64건 포함 — 최고가치 elbow 5건이 여기 포함될 수 있음)을
  얼굴 블러 가명처리해 살릴 것인가. 크롭은 관절 중심이라 얼굴 포함 사례가 실측 존재
  (xa1 라운드 "원이 얼굴 둘러쌈" 박제). 원장에 keypoint 좌표가 없어 크롭 단위 얼굴 위치를
  알 수 없음 → 별도 검출 재추론 필요 = 별건 사이클.
- B-2: 운영 S3 수확분의 learningOptIn 확인을 위한 Firestore read 승인 여부.
- B-3: ref 크롭에 정은지 얼굴이 포함될 수 있음(어깨/팔꿈치 크롭). 기존 정책상 비대상이나
  본인 관련이므로 통지 항목.
</privacy_verdict>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 눈 원장 수확기 순수층 + 동의 게이트 (EYE-02, EYE-03, EYE-06)</name>
  <files>backend/training/datagen/harvest_eye.py, backend/tests/phase22/test_harvest_eye.py</files>
  <behavior>
    - iter_ledger_entries: analysisId+entries 형은 entries 리스트로, flat 단일 dict 형은 1원소 리스트로, 그 외(list·손상)는 빈 리스트로. 세 형태 전부 흡수.
    - content_hash(png_bytes): sha256 hexdigest 앞 16자. 같은 바이트면 같은 해시(멱등 키).
    - media_key(sha16): training/phase22/eye/{sha16}.png 반환. 다른 prefix 금지.
    - consent_disposition: P-1~P-5 표를 그대로 반환. side=="ref" 이고 source_kind=="repo_evidence" 이고 motion 이 진리값이면 admit/internal_seed_ref. side=="user" 면 hold/customer_anonymize_required. source_kind=="s3_operational" 이면 hold/optin_unverified_post_cutoff. motion 미해결이면 hold/motion_unknown. 미분류면 hold/unclassified. hold 사유 우선순위는 테스트로 고정.
    - entry_to_row: observed=="error" 면 None(라벨 없음, 실측 0건이지만 fail-closed). match 는 track_claim_agrees 로 이름 보존하며 false 행도 반드시 행 생성(버리지 않음).
    - assert_no_identifier_keys: uid/uidHash/analysisId/analysis_id/email/user_id 키가 행에 있으면 ValueError. 값도 스캔 — 20자 이상 영숫자 연속 토큰이 Firebase uid 패턴(실측 fvcNXzEqKjgqVxRPVSj1iwFnIpn2)과 일치하면 ValueError.
    - merge_rows: eye_id(= media_sha16) 기준 멱등. 같은 원장 2회 수확 시 added 0 / skipped N, 기존 행 무변형.
    - assert_ledger_invariants 재사용: manifest _meta.collection_complete 불변, collection_closed 와 balance_waiver 무변형, rows append-only.
  </behavior>
  <action>
backend/training/datagen/harvest_eye.py 를 신설한다. 모듈 docstring 에 목적(눈 원장에서
학습 원장으로), privacy_verdict P-1~P-5 판정과 각 근거의 LICENSE-AUDIT 절 번호, 그리고
"식별자는 크롭 content hash 단독"(P-4) 규약을 명기한다.

순수층(numpy/boto3/네트워크 0):
EYE_ROW_KEYS 상수로 행 계약을 고정한다 — eye_id, motion, side, joint, claim, observed,
track_claim_agrees, track_angle_deg, confidence, reason, sec, frame_idx, media_sha16,
media_key, uploaded, source, source_kind, source_ref, motion_source, collected_at, usage,
disposition, disposition_reason. analysis_id/uid 계열 키는 이 튜플에 넣지 않는다(P-4).
usage 는 manifest 관례 그대로 training-only-no-redistribution.

함수: iter_ledger_entries(doc), content_hash(png_bytes), media_key(sha16),
consent_disposition(side, motion, source_kind), entry_to_row(entry, source_kind,
source_ref, motion, motion_source, png_bytes, collected_at), assert_no_identifier_keys(row),
merge_rows(existing, new), summarize(rows).

summarize 는 규모 실측 리포트의 단일 출처다 — 총계, disposition 별, 불일치
(track_claim_agrees is False), joint 별, observed 별, side 별 카운트를 반환한다.

source_ref 는 uid 를 절대 담지 않는다: 리포 evidence 는 원장 파일의 리포 상대경로,
운영 S3 는 s3-operational 접두 + sha16 형태(내용 파생 단독).

motion_source 는 provenance 다 — entry 에 motion 필드가 있으면 entry, CLI --motion 으로
주입했으면 operator 접두 뒤에 근거 문자열. 근거 없는 motion 주입 금지.

assert_ledger_invariants 는 재발명하지 말고 backend/scripts/phase22_watch.py 의 것을
import 해 재사용한다(conftest 가 backend/scripts 를 sys.path 에 주입한다).

I/O 껍데기(같은 파일 하단, lazy import): scan_repo_evidence(root, motion=None) 은 rglob 로
eye_ledger 디렉토리의 JSON 을 훑고 PNG 를 basename 으로, 없으면 JSON 과 같은 stem 의 .png
로 해결한다(실측 157/157). PNG 미해결은 fail-closed(행 생성 안 함 + 카운터).
scan_s3(bucket, prefix, s3_client) 는 읽기 전용(list_objects_v2 + get_object)이며 전 행이
hold(P-3)다.

CLI 2단(22-02/22-11 관례 상속): --dry-run 기본은 스캔 + summarize 출력이고 파일 쓰기 0.
--run 은 backend/training/data/eye_manifest.json 에 merge_rows 로 멱등 병합 기록 +
manifest.json 의 _meta.collection_batches 에 배치 등재. batch_id 접두는 watch 규약과
충돌하지 않도록 eye- 로 자체 산출하고, 같은 날 2회차부터 -2 접미는 compute_batch_id 관례를
준용한다. manifest rows 는 절대 건드리지 않는다 — 눈 크롭은 영상 행이 아니므로 별도 원장
(eye_manifest.json)이 소유한다. 이것이 기존 3트랙 무회귀의 구조적 보장이다.
등재 전후로 assert_ledger_invariants 를 돌려 위반 시 저장을 중단한다.

backend/tests/phase22/test_harvest_eye.py 를 먼저 작성해 RED 를 확인한 뒤 구현한다.
behavior 각 항목당 최소 1 테스트 + 실 리포 원장 3형태를 fixture 로 파싱하는 통합 테스트
1개(파일 쓰기 0). 네트워크/boto3 의존 0.
  </action>
  <verify>
    <automated>PYTHONPATH=backend/tests backend/.venv/bin/python -m pytest -q backend/tests/phase22/test_harvest_eye.py</automated>
  </verify>
  <done>test_harvest_eye.py 전량 통과. 3형태 원장 파싱, disposition 표 P-1~P-5 일치, 식별자 fence(uid 패턴 값 거부), 멱등 merge(재수확 added 0), match=false 행 보존이 각각 테스트로 고정됨.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: build_jsonl eye 트랙 + 사이클 배선 + 무회귀 게이트 (EYE-01, EYE-04, EYE-05)</name>
  <files>backend/training/datagen/build_jsonl.py, backend/tests/phase22/test_eye_track.py, backend/training/distill/full_batch.py, backend/training/sft/run_retrain_cycle.sh</files>
  <behavior>
    - eye_loader=None(기본)이면 build_dataset 의 train/val 샘플 리스트가 기존과 완전 동일. _meta 는 track_counts.eye 등 additive 키만 추가. 동일 입력 대조 테스트로 고정.
    - eye 샘플 assistant JSON 에 score/severity/overall/points 계열 키가 하나도 없다(재귀 스캔 테스트).
    - track_claim_agrees 가 False 인 행이 샘플로 방출되고, assistant JSON 에서 그 사실이 식별 가능하다.
    - eye 샘플은 _balance_media 트랙 독립 균등을 통과한다(motion 별 max 가 2배 min 이하). motion 없는 행은 loader 단계에서 이미 제외.
    - disposition 이 admit 이 아닌 행은 eye_loader 가 방출하지 않는다(hold 행 JSONL 유입 0).
    - val 로 뽑힌 video_hash 의 motion 과 같은 motion 의 eye 샘플은 드롭된다(leakage 0) — _meta.eye_leakage_dropped 카운터로 노출.
    - _build_text_samples 분모가 eye 제외 media 수로 고정되어 텍스트 혼합 행 수가 불변이다.
  </behavior>
  <action>
build_jsonl.py 를 additive 로만 수정한다. 기존 함수 시그니처와 본문 변경은 최소화한다.

신규 상수와 함수: EYE_TASK_KEYS 는 joint, observed, reason, side, track_claim,
track_claim_agrees 를 알파벳 정렬로 고정한다 — score/severity 의 구조적 부재가 D-01
불변식의 eye 판이다. _EYE_SYSTEM 시스템 프롬프트, _EYE_TASK_INSTRUCTION 고정 지시문,
normalize_eye_report(raw) 는 EYE_TASK_KEYS 화이트리스트 + 결측 None 고정 + 알파벳 정렬을
수행한다(schema.normalize_report 는 REPORT_KEYS 전용이라 재사용하지 않는다).
_user_image_msg(media_key, joint, side) 는 content 항목의 type 을 image 로 두고 image 값에
s3 URI 를 넣으며 텍스트는 _EYE_TASK_INSTRUCTION 을 재사용한다(신규 지시문 난립 금지).
_build_eye_samples(eye_loader) 는 samples 와 counters 를 반환한다.

왜 별도 리포트 스키마인가: 눈 판정은 결함이 아니라 관절 상태 관측이다. 이를 faults 배열에
담으면 정상 관측(match=true)까지 결함으로 사칭돼 학습셋이 오염된다. v4 자유생성 붕괴
fix 의 교훈(_TASK_INSTRUCTION — 출력 계약을 입력에 명시)을 그대로 적용해 eye 태스크는
자체 지시문과 자체 6키 스키마를 갖는다. 이 근거를 코드 주석에 남긴다.

eye 샘플 필드: _track 은 eye, _motion, _video_hash 는 None, _has_faults 는 False,
_eye_agrees, _media_pending_upload.

build_dataset 수정(순서가 무회귀의 핵심): 파라미터 eye_loader=None 을 추가하고 기존 계산을
다음 순서로 유지한다.
(1) media_core 는 기존 3트랙 balance 합이다.
(2) _cap_fault_free(media_core, ...) 를 eye 를 넣기 전에 적용한다. eye 는 전부 fault-free
    라 캡을 잡아먹어 기존 distill 행을 밀어낸다 — 이 순서를 어기면 1급 게이트 위반이다.
(3) text_samples 는 _build_text_samples(len(media_core), ...) 로 분모를 media_core 에
    고정한다. eye 는 다른 태스크라 언어 감퇴 방지 비율의 분모가 아니다 — 근거를 주석에 남긴다.
(4) val_hashes 산출 후 val 에 속한 media 샘플들의 motion 집합을 만들고, 그 motion 을 가진
    eye 샘플을 드롭한다(leakage 0).
(5) media 는 media_core 뒤에 _balance_media(eye_samples) 를 이어붙인 것이다.

_meta 에 추가할 키: track_counts.eye, eye_admitted_count, eye_mismatch_count,
eye_media_pending_upload, eye_leakage_dropped, eye_observed_counts.

full_batch.py 에는 make_eye_loader(eye_manifest_path) 를 추가한다 — eye_manifest 를 읽어
disposition 이 admit 이고 motion 이 진리값인 행만 방출한다(fail-closed). assemble_jsonl 에
eye_loader=None 파라미터를 추가해 build_dataset 으로 전달한다. argparse 에 --with-eye 를
추가하고 --eye-manifest 기본값을 backend/training/data/eye_manifest.json 으로 둔다.
--with-perturb 의 loader 주입 관례를 그대로 따른다(미주입 시 기존 동작 불변).

업로드 fail-closed: --upload 와 --with-eye 가 동시에 켜졌는데 _meta.eye_media_pending_upload
가 0 보다 크면 SystemExit 로 차단한다. 이유는 이번 사이클이 크롭 PNG 의 S3 업로드를 스코프
밖으로 두어 JSONL 이 아직 없는 키를 가리키기 때문이다. 로컬 조립(업로드 없음)은 허용해
실물을 만들되, canonical 업로드는 크롭 업로드가 선행된 다음 사이클에서만 통과한다.
이 근거를 에러 메시지에 담는다.

run_retrain_cycle.sh 는 assemble 스테이지의 full_batch 호출에 --with-eye 를 추가한다.
그 외 스테이지(preflight/label/train/gates/promote)와 run_sft/assert_gates/promotion 은
무접촉이다.

backend/tests/phase22/test_eye_track.py 를 먼저 작성(RED)한 뒤 구현한다. 무회귀 테스트는
동일 manifest/loader 입력에 대해 eye_loader 미주입 산출과 eye_loader 주입 산출의 비-eye
부분이 동등함을 대조한다. 기존 backend/tests/phase22/test_build_jsonl.py 는 수정하지
않는다 — 그것이 무회귀의 증인이다.
  </action>
  <verify>
    <automated>PYTHONPATH=backend/tests backend/.venv/bin/python -m pytest -q backend/tests/phase22 && grep -c "with-eye" backend/training/sft/run_retrain_cycle.sh</automated>
  </verify>
  <done>phase22 테스트 전량 통과(test_build_jsonl.py 무수정 통과 포함). eye_loader 미주입 무회귀, score 필드 부재, 불일치 식별, hold 행 차단, leakage 드롭, 균등 게이트 참여가 각각 테스트로 고정됨. run_retrain_cycle.sh assemble 에 --with-eye 존재.</done>
</task>

<task type="auto">
  <name>Task 3: 수확 실행 + JSONL 실물 + 규모 실측 + 판정 박제</name>
  <files>backend/training/data/eye_manifest.json, backend/training/data/manifest.json, backend/training/LICENSE-AUDIT.md, .planning/quick/260814-j24-eye-s3/EYE-HARVEST-REPORT.md</files>
  <action>
(a) dry-run 수확 — harvest_eye 를 --dry-run 으로 돌려 리포 evidence 전량을 스캔하고
summarize 결과를 얻는다. measured_facts 의 157 / 95 / user 112 / ref 45 와 대조하고,
어긋나면 어긋난 사실을 리포트에 기록한다(조용히 맞추지 말 것).

(b) 운영 S3 스캔(읽기 전용) — 버킷 sunity-motion-pilot-videos 의 results 접두 아래
eye/ledger.json 을 열거·읽어 disposition 을 hold(P-3)로 등재한다. 자격증명 실패 시
--profile sunity-motion 으로 1회 재시도하고, 그래도 실패하면 스킵하고 그 사실을 리포트에
명기한 뒤 진행한다(S3 분은 전량 hold 라 결과물에 영향 없음). S3 쓰기는 절대 금지.

(c) motion 해결 — entry 에 motion 이 있는 원장(ehz 스윕형 53 파일)은 그대로 쓴다. motion
필드가 없는 원장(kpo/ufb/xa1/fxx/wif 계열)은 그 원장이 들어 있는 quick 디렉토리의 자체
문서에서 동작명을 직접 읽어 확인한 뒤에만 --motion 으로 주입하고 근거 문자열을
motion_source 에 남긴다. 확인 불가면 주입하지 말고 hold(P-5)로 둔다 — 추정 금지.

(d) --run 실행 — eye_manifest.json 생성 + manifest.json 의 _meta.collection_batches 에
배치 1건 등재. 등재 직후 같은 명령을 1회 더 실행해 멱등 실증(added 0)을 리포트에 기록한다.
git diff 로 manifest.json 변경이 _meta.collection_batches 한 곳뿐임을 확인한다.

(e) JSONL 실물 방출 — 로컬 조립을 돌린다. --assemble 과 --with-eye 만 켜고 --upload 는
켜지 않는다. out-dir 는 이 quick 디렉토리 아래로 잡는다. accepted 디렉토리가 로컬에
없으므로 distill 트랙은 0행이고 eye 트랙만 든 train.jsonl 이 나온다 — 그게 이번 사이클의
실물이다. 산출된 train.jsonl 을 직접 열어 첫 행과 track_claim_agrees 가 false 인 행 최소
1건을 리포트에 원문 인용한다. 열어보지 않고 완료 선언 금지.

(f) 리포트 작성 — .planning/quick/260814-j24-eye-s3/EYE-HARVEST-REPORT.md 에 다음을 담는다.
1) 규모 실측 표: 총 entry, disposition(admit 과 hold 사유별), 불일치 수, joint 별,
observed 별, side 별. 2) admit 되어 JSONL 로 나간 행 수와 그중 불일치 행 수.
3) 고가치 표본: elbow 에서 트랙이 5~7도 극단 굽힘을 주장했는데 눈이 펴짐으로 뒤집은 행이
admit 과 hold 중 어디에 속하는지 실측으로 밝힌다(hold 라면 그것이 B-1 의 비용이다).
4) 프라이버시 판정 P-1~P-5 전문과 근거 절 인용. 5) belle 결정 항목 B-1/B-2/B-3.
6) 다음 사이클 필요 작업(크롭 S3 업로드 후 --upload 게이트 해제, 그 뒤 실 학습 사이클).
7) LLM 학습 영향: 이번 작업의 Gemini/LLM 실호출 0, 학습 전송 0, 다만 경로가 뚫려 다음
사이클부터 눈 원장이 학습 코퍼스로 들어간다는 사실을 명시한다.

(g) LICENSE-AUDIT.md 에 7-3 절 "기계 눈 크롭 학습 적재 판정 (2026-08-14)" 을 신설해
P-1~P-5 판정과 근거, admit/hold 실측 수, B-1~B-3 belle 결정 대기 항목을 박제한다.
8절 결정 이력 표에도 1행 추가한다. 기존 7-1 과 7-2 문구는 수정하지 않는다.

(h) 게이트 — 전체 pytest 기준선(59 failed 유지) 확인, git diff --stat 으로 변경 파일이
files_modified 목록 밖으로 새지 않았는지 확인, backend/shared/python/sunity_shared/analysis
디렉토리 diff 0(채점·운영 경로 무접촉)을 명시적으로 확인한다.
  </action>
  <verify>
    <automated>PYTHONPATH=backend/tests backend/.venv/bin/python -m pytest -q backend/tests 2>&1 | tail -3; ls -l .planning/quick/260814-j24-eye-s3/EYE-HARVEST-REPORT.md backend/training/data/eye_manifest.json; git diff --stat backend/shared/python/sunity_shared/analysis | wc -l</automated>
  </verify>
  <done>pytest 기준선 59 failed 유지. eye_manifest.json 에 admit/hold 행 존재하고 uid·analysisId 키 0. train.jsonl 에 eye 트랙 행 존재하며 불일치 행 최소 1건이 리포트에 원문 인용됨. LICENSE-AUDIT 7-3 절 존재. analysis 디렉토리 diff 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 운영 S3 → 학습 원장 | 수강생 실업로드 유래 이미지·경로(uid 포함)가 학습 코퍼스로 건너오는 지점 |
| 눈 원장 JSON → JSONL | 외부(Gemini) 생성 자유텍스트(reason)가 학습 타겟으로 들어가는 지점 |
| 수확기 → manifest.json | 마감된 수집 원장(collection_complete)의 무결성 경계 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-j24-01 | Information Disclosure | eye_manifest.json 행 | mitigate | assert_no_identifier_keys 로 uid/analysisId 키·값(Firebase uid 패턴) 차단, 식별자는 content hash 단독(P-4). 테스트 fence |
| T-j24-02 | Information Disclosure | 크롭 PNG(얼굴 포함 가능) | mitigate | user 측 전량 hold(P-1), 운영 S3 전량 hold(P-3). admit 은 internal seed ref 만(P-2). belle 결정 B-1 전까지 적재 0 |
| T-j24-03 | Tampering | manifest.json 마감 무결성 | mitigate | phase22_watch.assert_ledger_invariants 재사용(collection_complete 불변, rows append-only), rows 무접촉 + 별도 eye_manifest 소유 |
| T-j24-04 | Tampering | 기존 3트랙 학습셋 | mitigate | eye_loader 미주입 시 코드 경로 동일 + _cap_fault_free/text 분모를 media_core 로 고정. test_build_jsonl.py 무수정 통과가 게이트 |
| T-j24-05 | Elevation of Privilege | 점수 라벨 유입 | mitigate | EYE_TASK_KEYS 화이트리스트로 score/severity/overall/points 구조적 부재 + 재귀 스캔 테스트 |
| T-j24-06 | Denial of Service | 존재하지 않는 S3 키 참조로 학습 실패 | mitigate | eye_media_pending_upload > 0 이면 --upload 차단(fail-closed). 로컬 조립만 허용 |
| T-j24-07 | Repudiation | 수확 행 출처 불명 | mitigate | source_kind/source_ref/motion_source/collected_at/usage provenance 필수 기록. motion 추정 금지 |
| T-j24-SC | Tampering | 신규 패키지 설치 | accept | 신규 pip 설치 0 — 표준 라이브러리(hashlib/json/pathlib) + 기존 boto3 만 사용 |
</threat_model>

<verification>
1. 기존 3트랙 무회귀 — test_build_jsonl.py 무수정 전량 통과 + eye_loader 미주입 산출 동등성 테스트
2. pytest 기준선 — 전체 59 failed 유지(증가 0)
3. 불변식 — eye 샘플 재귀 키 스캔에서 score/severity/overall/points 0건
4. 불일치 보존 — 원장 match=false 행 수와 수확 원장 track_claim_agrees=false 행 수 일치
5. 식별자 — eye_manifest.json 전 행에 uid/analysisId 키 0, Firebase uid 패턴 값 0
6. 멱등 — 같은 수확 2회 실행 시 added 0
7. 마감 무결성 — manifest.json diff 가 _meta.collection_batches 단일 지점
8. 운영 경로 무접촉 — backend/shared/python/sunity_shared/analysis diff 0, Firestore 접촉 0, S3 쓰기 0
9. 실물 — train.jsonl 을 직접 열어 eye 행 원문 확인(코드 통과로 대체 금지)
</verification>

<success_criteria>
- 눈 원장 행이 학습 JSONL 행으로 변환된 파일이 존재하고 그 원문이 리포트에 인용됨
- 불일치 행이 보존되고 샘플에서 식별 가능함
- 프라이버시 판정 P-1~P-5 가 코드 fence + LICENSE-AUDIT 7-3 + 리포트 3중으로 박제되고,
  미판정분(B-1/B-2)은 적재 0 으로 보류됨
- 재학습 사이클 assemble 이 --with-eye 로 새 트랙을 태울 수 있음
- 기존 3트랙 출력과 pytest 기준선 무회귀
- 규모 실측(총/불일치/관절별/disposition별)이 산출물에 박제되어 belle 의 학습 실행 결정 재료가 됨
</success_criteria>

<output>
Create `.planning/quick/260814-j24-eye-s3/260814-j24-SUMMARY.md` when done
</output>
