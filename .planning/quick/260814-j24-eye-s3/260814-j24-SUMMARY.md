---
phase: 260814-j24-eye-s3
plan: 01
subsystem: training-data-pipeline
tags: [phase22, eye-ledger, harvest, privacy, jsonl, keypoint-hallucination]
requires:
  - backend/training/datagen/build_jsonl.py (3트랙 조립기)
  - backend/scripts/phase22_watch.py (배치 등재/불변식 규약)
  - backend/training/LICENSE-AUDIT.md (D-12 동의 3겹 · 7-1 컷오프)
provides:
  - backend/training/datagen/harvest_eye.py (눈 원장 → 학습 원장 수확기)
  - backend/training/data/eye_manifest.json (수확 원장 141행)
  - build_jsonl eye 트랙(4번째 트랙) + full_batch --with-eye 배선
  - LICENSE-AUDIT 7-3 (기계 눈 크롭 적재 판정)
affects:
  - backend/training/sft/run_retrain_cycle.sh (assemble 스테이지)
  - backend/training/data/manifest.json (_meta.collection_batches 1행)
tech-stack:
  added: []
  patterns: [content-hash-identity, fail-closed-consent-gate, additive-meta-namespace]
key-files:
  created:
    - backend/training/datagen/harvest_eye.py
    - backend/tests/phase22/test_harvest_eye.py
    - backend/tests/phase22/test_eye_track.py
    - backend/training/data/eye_manifest.json
    - .planning/quick/260814-j24-eye-s3/EYE-HARVEST-REPORT.md
    - .planning/quick/260814-j24-eye-s3/assemble_out/jsonl/train.jsonl
  modified:
    - backend/training/datagen/build_jsonl.py
    - backend/training/distill/full_batch.py
    - backend/training/sft/run_retrain_cycle.sh
    - backend/training/data/manifest.json
    - backend/training/LICENSE-AUDIT.md
decisions:
  - "눈 크롭은 영상 행이 아니므로 manifest.rows 가 아니라 eye_manifest.json 이 소유 — 기존 3트랙 무회귀의 구조적 보장"
  - "eye 는 faults[] 가 아니라 자체 7키 스키마 — 정상 관측을 결함으로 사칭하면 감점 엔진까지 오염"
  - "동의 실측 결과 learningOptIn 6/6 부재 + 컷오프 이후 → P-1/P-3 hold 확정(추정 아닌 측정)"
  - "user 크롭은 optIn=true 라도 hold — 동의와 가명처리는 다른 층, 얼굴 블러(B-1)는 belle 미결"
  - "motion 어휘를 근거 문서 기반으로 manifest 어휘(ref-*)로 정규화 — 안 하면 leakage 게이트 미발화"
metrics:
  duration: "약 2시간"
  completed: 2026-08-14
---

# quick 260814-j24: 기계 눈 원장 학습 유입 경로 개통 Summary

기계 눈 원장 205행을 수확해 141행 학습 원장을 만들고, build_jsonl 4번째 트랙(eye)으로
7행을 실제 학습 JSONL 로 방출했다 — 경로가 코드가 아니라 **파일로** 뚫렸다.

## 무엇이 생겼나

**1. 수확기** `backend/training/datagen/harvest_eye.py` (800줄)
원장 JSON 3형태(analysisId+entries / flat / flat+motion) 전부 흡수, PNG content hash 를
유일 식별자로 쓰는 멱등 수확. 프라이버시 판정 P-1~P-5 가 `consent_disposition` 한 함수에
집행되고, `assert_no_identifier_keys` 가 키와 값(Firebase uid 패턴) 양쪽을 막는다.
S3·Firestore 는 읽기 전용 경로만 존재한다(쓰기 API 호출이 코드에 없다).

**2. 수확 원장** `backend/training/data/eye_manifest.json` (141행)
스캔 205(리포 evidence 157 + 운영 S3 48) → content-hash 병합 141.
**admit 41 / hold 100**, 트랙-눈 불일치 90(admit 29 / hold 61).

**3. eye 트랙** `build_jsonl` 에 4번째 트랙 추가. 7키 자체 스키마
(joint/limb/observed/reason/side/track_claim/track_claim_agrees) — score·severity 계열
구조적 부재. `full_batch --with-eye` + `run_retrain_cycle.sh assemble` 배선 완료.

**4. 실물** `.planning/quick/260814-j24-eye-s3/assemble_out/jsonl/train.jsonl`
9행(eye 7 + text 2). eye 7행 중 **6행이 트랙-눈 불일치**. 첫 행 원문은 리포트 §5-1 에 인용.

**5. 판정 박제** LICENSE-AUDIT §7-3 신설 + 8절 결정 이력 1행 + EYE-HARVEST-REPORT.md.

## 실측 (플래너 `<measured_facts>` 대조)

| 항목 | 플래너 값 | 실측 | 결과 |
|------|----------|------|------|
| 원장 파일 / PNG / entry | 69 / 159 / 157 | 69 / 159 / 157 | **일치** |
| `observed=="error"` | 0 | 0 | 일치 |
| `match=false` | 95 (user 64 / ref 31) | 95 (user 64 / ref 31) | 일치 |
| side | user 112 / ref 45 | user 112 / ref 45 | 일치 |
| joint 7종 분포 | 40·36·26·24·13·13·5 | 40·36·26·24·13·13·5 | 일치 |
| claim 화살표 8종 | bent→bent 60 … | 동일 | 일치 |
| PNG 해결률 | 157/157 | 157/157 | 일치 |

어긋난 항목 0. 보강 1건: 디렉토리 매칭을 정확히 `eye_ledger` 로 하면 `pod_eye_ledger`(40 entry)가
빠져 117 이 나온다 — 수확기는 이름에 `eye_ledger` 를 **포함**하는 디렉토리를 훑는다.

운영 S3 신규 실측: `results/` 2063 객체 중 eye ledger 5 / 크롭 PNG 48, 전량 단일 uid.
크롭 48장 중 **47장이 리포 evidence 사본과 byte-동일**(S3 고유 생존 1행).

## 동의 실측 (오케스트레이터 B-2 해제분)

플랜은 "Firestore 무접촉이라 optIn 확인 불가"를 이유로 hold 했다. 읽기가 해제돼 **측정**했다.

분석 doc 6건 전수 read-only: **6/6 에서 `learningOptIn` 필드가 부재**(false 도 아닌 미기록),
**6/6 이 belle 일괄승인 컷오프(2026-07-13) 이후 생성**. `consent_allows` 계약의 기계적 적용으로
**P-1/P-3 은 hold 로 확정**. "확인 못 했다"가 아니라 "**플래그가 없다는 것이 측정 결과**"다.
각 행의 `consent_flag: null` 이 이 측정을 보존한다. Firestore 쓰기 0.

부수 소득: 같은 읽기의 `referenceMotionId` 6/6 전건 `ref-pdshape` 로 motion 미해결(P-5) 88행을
추정 없이 해소했다.

## belle 결정 대기

| ID | 항목 | 비용/효과 |
|----|------|-----------|
| **B-1** | user 크롭 100행 가명처리 여부 | 열면 적재 41→최대 141(3.4배), 불일치 29→90. **트랙이 5~7도 극단 굽힘을 주장한 순간을 눈이 뒤집은 최고가치 8행이 전부 여기 있다**(8/8 user = 8/8 hold) |
| **B-3** | ref 크롭에 정은지 얼굴 포함 가능(어깨 크롭 admit 10행) | 정책상 비대상이나 본인 관련 통지 |
| **B-4**(신규) | eye 균등 축을 motion → (joint × observed)로 볼 것인가 | 현재 균등 트림 34/41. motion 별 최소가 1(peter-pan)이라 상한이 2로 내려앉음 |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] `track_claim_agrees` 2단 판정을 1단으로 가르치고 있었다**
- **Found during:** Task 3 (실물 JSONL 을 열어보다가)
- **Issue:** 원장의 `match` 는 `card_gates._eye_verdict` 의 **2단 판정**(상태 일치 AND 기대
  사지 일치)인데, 수확 행에 `limb` 을 안 옮기고 지시문에 "상태가 같으면 true"라고 썼다.
  실물 7행 중 3행이 "claim==observed 인데 agrees=false" 라 **라벨과 지시문이 정면 충돌** —
  그대로 학습시키면 모델이 잡음을 배운다. 하필 이 부류(마크가 다른 사지에 얹힘)가
  keypoint 환각의 핵심 증거다(원장 전체 20행).
- **Fix:** `EYE_ROW_KEYS`·`EYE_TASK_KEYS` 에 `limb` 추가(6키→7키), 지시문을 2단 규칙으로
  정정, `card_gates._eye_verdict` 단일 owner 를 근거로 주석 인용. 회귀 테스트 1건 추가.
- **Files:** harvest_eye.py, build_jsonl.py, test_eye_track.py
- **Commit:** cd68378c

**2. [Rule 2 - Missing critical] motion 어휘 불일치로 leakage 게이트가 장식이었다**
- **Found during:** Task 3 (c) motion 해결
- **Issue:** 하네스 motion 이름(`pdshapefault`)과 manifest 어휘(`ref-pdshape`)가 달라
  `build_jsonl` 의 val-motion leakage 대조가 **원리적으로 발화하지 않았다**. 게이트가 있는데
  절대 안 걸리는 상태.
- **Fix:** `resolve_motion` 신설 — entry 값 / analysisId 맵 / 디렉토리 맵 3경로, 전부
  **근거 문자열(evidence) 없으면 주입 거부**. 하네스 이름은 근거 문서 기반 alias 로
  manifest 어휘 정규화(`discover_sweep.py:73-84`, `discover_knee.py:63`, Firestore
  `referenceMotionId`). 각 행 `motion_source` 에 근거가 박힌다. 테스트 5건 추가.
- **Files:** harvest_eye.py, test_harvest_eye.py
- **Commit:** cd68378c

**3. [Rule 2 - Missing critical] 균등 트림 손실이 은폐되고 있었다**
- **Found during:** Task 3 (e) JSONL 방출
- **Issue:** admit 41 → 방출 7 인데 그 34행 손실이 어디에도 안 나왔다. 리포지토리 관례는
  드롭률 노출(`shadow_unregistered_dropped` 선례).
- **Fix:** `_meta.eye_balance_trimmed` 추가 + 테스트 고정. 손실 원인이 코드가 아니라
  motion 커버리지임을 주석·리포트에 명시(완화 아님).
- **Commit:** cd68378c

**4. [Rule 2] 배치 entry 수치 혼동 제거**
- 초기 `eye_admit`/`eye_hold` 가 스캔 총계(205 기준)인데 `eye_rows_after` 는 병합 후(141)라
  같은 entry 안에서 기준이 섞였다 → `scanned_*` / `ledger_*_after` 로 분리하고 원장을
  되돌린 뒤 재수확. **Commit:** cd68378c

**5. [Rule 3] 손상 원장 오해석**
- `{"entries": "not-a-list"}` 가 flat 단일 entry 로 해석돼 쓰레기 행이 생겼다 →
  `ledger_shape` 에서 `entries` 키 보유 시 list 아니면 `other`(fail-closed). **Commit:** 4af3858d

### 계획 대비 추가 산출물

`assemble_out/jsonl/{train.jsonl,val.jsonl,_meta.json}` 3파일은 frontmatter
`files_modified` 목록에는 없지만 Task 3 (e) 가 "out-dir 는 이 quick 디렉토리 아래로 잡는다"고
지시한 산출물이다. 이것이 이번 사이클의 **실물**이라 리포에 남긴다.

수확 재현용 맵 4종은 scratchpad(휘발)에서 만들었으므로 EYE-HARVEST-REPORT 부록 A 에 원문을
박제했다(재료는 리포에만 보존).

## 게이트

| 게이트 | 결과 |
|--------|------|
| pytest 전체 | **59 failed / 4289 passed / 26 skipped** — failed 기준선(59) 동일 |
| phase22 | 380 passed / 1 skipped. `test_build_jsonl.py` **무수정** 통과(무회귀 독립 증인) |
| 채점·운영 경로 | `backend/functions` + `backend/shared` diff **0줄** (analysis 포함) |
| manifest.json | 단일 hunk `@@ -52 +52,46 @@` = `_meta.collection_batches` 뿐, rows 239 무접촉 |
| 멱등 | `--run` 2회차 added **0** / skipped 205 / rows_after 141 |
| 식별자 | 141/141 fence 통과. 원문에 uid·`results/`·`p34fresh` 문자열 **0** |
| 쓰기 | S3 쓰기 0 · Firestore 쓰기 0 · Pod 무접촉 |
| 변경 파일 | 계획 10 + 실물 3, 목록 밖 유출 0 |

## Known Stubs

없음. eye 트랙은 loader 주입식이고 원장·JSONL 실물이 모두 존재한다.

단, **의도된 fail-closed 정지**가 하나 있다: 크롭 PNG 가 S3 에 없으므로
`--upload` + `--with-eye` 조합은 `SystemExit` 로 차단된다(`eye_media_pending_upload > 0`).
`run_retrain_cycle.sh assemble` 도 여기서 멈춘다 — 크롭 업로드 사이클 전에 재학습을 돌리려면
`--with-eye` 를 빼야 한다. 이 조건과 해제 경로는 에러 메시지·스크립트 주석·리포트 §8 에 명시.

## LLM 학습 영향

- **이번 작업의 Gemini/LLM 실호출 0** — 이미 쌓인 원장을 읽었을 뿐. 과금 0.
- **학습 전송 0** — 외부 전송 없음. JSONL 은 리포 안 로컬 파일이고 canonical 업로드는 차단.
- **다만 경로가 뚫렸다.** 다음 사이클부터 `run_retrain_cycle.sh assemble` 이 `--with-eye` 로
  눈 원장을 학습 코퍼스에 태운다. 들어가는 것 = ref 측 크롭 41행(현재 균등 통과 7행),
  사람 점수 라벨 0, score/severity 구조적 부재.
- **신호의 성격** — 이 트랙이 가르치는 것은 "각도 트랙의 주장이 그림과 맞는가"이고,
  불일치 라벨 29/41 이라 모델이 배우는 것은 정답 맞히기가 아니라 **파이프라인의 오답을
  지적하는 능력**이다. 위험도 같은 자리에 있다 — 눈(Gemini) 자체가 틀린 행이 섞이면 그
  오답도 학습된다. 이번 원장은 belle 육안 검수를 거친 사이클 산출이지만 **행 단위 인증은
  아니며**, 규모가 커지면 행 단위 검수 규약이 필요하다.

## 다음

1. 크롭 PNG S3 업로드(`training/phase22/eye/{sha16}.png`) → `--upload` 게이트 해제
2. 수확 범위 확대(동작별 눈 호출 균등) → 41→7 병목 해소
3. B-1 결정 시 얼굴 검출 재추론 → 블러 → 재수확(블러하면 바이트가 바뀌어 새 eye_id 가
   생기므로 구 행 supersede 규약 설계 필요)
4. 실 학습 사이클(위 1~2 이후)

## Self-Check: PASSED

- 생성 파일 6/6 존재 확인
- 커밋 7/7 존재 확인 (194e5c40 / 4af3858d / 01998124 / 83da09be / cd68378c / 070a3775 / 787a11ca)
- `run_retrain_cycle.sh` assemble 스테이지 `--with-eye` 존재 확인
- LICENSE-AUDIT §7-3 절 존재 확인
