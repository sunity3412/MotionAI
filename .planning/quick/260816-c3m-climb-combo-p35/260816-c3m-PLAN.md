---
phase: quick-260816-c3m
quick_id: 260816-c3m
slug: climb-combo-p35-input-data
date: 2026-08-16
status: planned
description: climb·combo 를 발굴 스윕 대상에 올리기 위한 P35 렌더 입력 데이터(doc.json+align.json) 생성 — SIM_UID 직접 _process(phase34_fresh_reanalysis.py 선례) + Pod RTMW 재추출(p35_extract_align.py JOBS 확장) + discover_sweep.py 소스 게이트 실증. foxtop 계열 4동작은 학생 영상 부재로 이번 범위 밖(README 에 명기).
wave: 1
depends_on: []
type: execute
plan: 01
autonomous: true
requirements: [QUICK-260816-C3M]
files_modified:
  - backend/scripts/p35_new_motion_docs.py
  - backend/scripts/p35_extract_align.py
  - .planning/phases/35-server-rendered-comparison-video/data/climb/doc.json
  - .planning/phases/35-server-rendered-comparison-video/data/climb/align.json
  - .planning/phases/35-server-rendered-comparison-video/data/climbfault/doc.json
  - .planning/phases/35-server-rendered-comparison-video/data/climbfault/align.json
  - .planning/phases/35-server-rendered-comparison-video/data/combo/doc.json
  - .planning/phases/35-server-rendered-comparison-video/data/combo/align.json
  - .planning/phases/35-server-rendered-comparison-video/data/README.md
  - .planning/quick/260816-c3m-climb-combo-p35/verify_source_gate.py
  - .planning/quick/260816-c3m-climb-combo-p35/source_gate_result.json
  - .planning/quick/260816-c3m-climb-combo-p35/260816-c3m-SUMMARY.md
must_haves:
  truths:
    - "climb(correct)·climb(fault)·combo(correct) 3건의 신선 분석 doc 이 SIM_UID(fvcNXzEqKjgqVxRPVSj1iwFnIpn2) 아래 direct-process 로 생성되고 status=done 으로 완료된다 — belle 실계정(csKWYvI3WCPYPysNQ9KkWecaUvq1)은 전혀 등장하지 않는다"
    - "그 3건의 align.json 이 Pod GPU 에서 rtmlib RTMW 15fps 재추출로 생성되어 기존 7동작과 동일 스키마(userKp/refKp/userScore/refScore/curveRefSec/fps/userSize/refSize/joints17)를 갖춘다"
    - "3건 모두 .planning/phases/35-server-rendered-comparison-video/data/{climb,climbfault,combo}/ 에 영구 커밋되어 Pod 소실과 무관하게 재현 가능하고, README.md 의 파일 구성·S3 키 표가 10동작 22파일로 갱신된다"
    - "discover_sweep.py(quick-260814-ehz-5) 의 source_gate() 가 이 3건에 대해 PASS 를 반환한다(align 스키마 필드 + fps 교차검증 포함) — 원본 discover_sweep.py 파일 자체는 무편집(다음 발굴 사이클에서 SWEEP_JOBS 정식 등재)"
    - "foxtop·foxtop-split·invert·sideway-spin 은 fixtures/phase15/ 에 학생(정답/오답) 영상이 없어 이번 사이클에서 처리되지 않았음이 README 에 명시된다 — belle 촬영 계획의 근거"
    - "기존 5 활성 렌더 슬롯(elbow/powerspin/kipup/pdshapefault/peterpan) + 2 벤치 슬롯(pdshape/realupload) 데이터는 git status 기준 바이트 단위로 무변경이고, backend/shared·backend/functions(운영 코드)는 전혀 손대지 않는다"
  artifacts:
    - path: "backend/scripts/p35_new_motion_docs.py"
      provides: "SIM_UID 직접 _process 드라이버 — ITEMS 데이터 표(slot→sourceS3Key,referenceMotionId) 순회, 동작명 분기 0, --dry-run 지원"
      contains: "SIM_UID = \"fvcNXzEqKjgqVxRPVSj1iwFnIpn2\""
    - path: "backend/scripts/p35_extract_align.py"
      provides: "기존 7동작 JOBS 무변경 + climb/climbfault/combo 3건 추가 (2-tuple: sourceS3Key, refS3Key)"
      contains: "\"combo\": (\"fixtures/phase15/combo/correct.mp4\", \"reference/ref-combo.mp4\")"
    - path: ".planning/phases/35-server-rendered-comparison-video/data/climb/doc.json"
      provides: "climb correct.mp4 vs ref-climb 신선 분석 결과(SIM_UID, status=done)"
    - path: ".planning/phases/35-server-rendered-comparison-video/data/climbfault/doc.json"
      provides: "climb fault.mp4 vs ref-climb 신선 분석 결과(SIM_UID, status=done)"
    - path: ".planning/phases/35-server-rendered-comparison-video/data/combo/doc.json"
      provides: "combo correct.mp4 vs ref-combo 신선 분석 결과(SIM_UID, status=done)"
    - path: ".planning/phases/35-server-rendered-comparison-video/data/README.md"
      provides: "10동작 22파일 표 갱신 + climb/climbfault/combo 는 렌더 슬롯 아님(발굴 스윕 후보) 명기 + foxtop 계열 불가 사유"
      contains: "발굴 스윕 후보"
    - path: ".planning/quick/260816-c3m-climb-combo-p35/verify_source_gate.py"
      provides: "discover_sweep.py::source_gate() 를 원본 무편집으로 재사용해 climb/climbfault/combo 3건 게이트 실증"
      contains: "source_gate"
    - path: ".planning/quick/260816-c3m-climb-combo-p35/source_gate_result.json"
      provides: "3건 게이트 원본 판정 dict (passed/checks/reasons) — belle 대조 재료"
  key_links:
    - from: "backend/scripts/p35_new_motion_docs.py"
      to: "backend/functions/pipeline/app.py::_process"
      via: "importlib.util.spec_from_file_location 경로 임포트 후 직접 호출 (phase34_fresh_reanalysis.py/sweep_phase15.py 와 동일 패턴 — 분기 0, 코드 1벌)"
      pattern: "pipeline\\._process\\(args\\.bucket"
    - from: "backend/scripts/p35_extract_align.py"
      to: ".planning/phases/35-server-rendered-comparison-video/data/{climb,climbfault,combo}/align.json"
      via: "python3 scripts/p35_extract_align.py --workdir /workspace/p35 --motions climb,climbfault,combo (Pod, RTMW_DEVICE=cuda)"
      pattern: "JOBS\\[motion\\]"
    - from: ".planning/quick/260816-c3m-climb-combo-p35/verify_source_gate.py"
      to: ".planning/quick/260814-ehz-5/discover_sweep.py"
      via: "importlib 로 원본 무편집 로드 후 SWEEP_JOBS 를 런타임 in-memory 확장(디스크 파일 변경 0) — source_gate() 만 재사용, scan/eye/render 는 호출하지 않음(다음 사이클 범위)"
      pattern: "source_gate"
    - from: ".planning/phases/35-server-rendered-comparison-video/data/climb/doc.json"
      to: "Firestore reference/ref-climb (isActive=true, referenceKeypointReport 확인됨)"
      via: "referenceMotionId 필드"
      pattern: "\"referenceMotionId\": \"ref-climb\""
---

<objective>
climb·combo 를 다음 발굴 스윕 사이클의 대상으로 올리기 위한 선행 조건 — P35 렌더
입력 데이터(doc.json+align.json)를 신선 생성하고, discover_sweep.py 의 소스
게이트가 그 데이터를 실제로 통과함을 기계로 증명한다. belle 질문("pdshape,
파워스핀, 엘보하고있고, 피터팬 하고 있고, 킵업이랑 콤보 남은건가?")에 대한 답 —
실측 결과 climb·combo 만 fixtures/phase15/ 에 학생 영상이 있어 이번에 늘릴 수
있고, foxtop 계열 4동작은 학생 영상 자체가 없어 촬영 전엔 불가하다(README 에 명기).

Purpose: 발굴 스윕(quick-260814-ehz-5 discover_sweep.py) 이 지금은 5동작에서만
도는 이유가 "P35 로컬 데이터 부재"라는 순수 인프라 갭이었음을 climb/combo 로
증명하고, 다음 belle 판정 사이클이 즉시 시작할 수 있도록 재료를 갖춘다. 이번
사이클은 재료 생산 + 게이트 실증까지만 — 실제 발굴 스캔(record 전수 홀드/짝/눈)은
belle 이 이 산출물을 본 뒤 별도 사이클(SWEEP_JOBS 정식 등재 포함)에서 진행한다.

Output: backend/scripts/p35_new_motion_docs.py(신규 SIM_UID 드라이버) +
p35_extract_align.py JOBS +3 + data/{climb,climbfault,combo}/{doc,align}.json
(리포 영구본) + README.md 갱신(10동작 22파일 + foxtop 계열 불가 사유) +
verify_source_gate.py 게이트 실증 산출.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend/scripts/phase34_fresh_reanalysis.py
@backend/scripts/sweep_phase15.py
@backend/scripts/phase15_keys.json
@backend/scripts/p35_extract_align.py
@.planning/phases/35-server-rendered-comparison-video/data/README.md
@.planning/quick/260814-ehz-5/discover_sweep.py

배경 (플래너 2026-08-16 실측 — 재파생 불필요, 그대로 사용):

- **doc.json 생성 경로 결정 (scope 1 이행):** 기존 7동작 doc.json 은 전부
  `pipeline._process(bucket, key, uid, analysisId)` 직접 호출로 만들어졌다 —
  `sweep_phase15.py --trigger direct-process`(fixtures/ 원본, uploads/ COPY 0,
  HIGH1 불변식) 와 `phase34_fresh_reanalysis.py`(SIM_UID, 원 doc 무접촉) 둘 다
  이 방식이다. belle 제약이 "phase34_fresh_reanalysis.py 의 SIM_UID 선례를
  따를 것"으로 명시했으므로, uid 는 sweep_phase15 의 per-run 합성 uid
  (`phase15_mode1_<runId>`) 대신 **SIM_UID 고정값**
  `fvcNXzEqKjgqVxRPVSj1iwFnIpn2`(phase34_fresh_reanalysis.py 리터럴 그대로)을
  쓴다. source key 는 fixtures/phase15/ 원본을 그대로 `_process` 에 넘긴다
  (S3 copy 0 — direct-process 와 동일 정신).
- **S3 실물 확인(2026-08-16, head-object):** `fixtures/phase15/climb/correct.mp4`
  40.9MB, `fixtures/phase15/climb/fault.mp4` 31.6MB, `fixtures/phase15/combo/correct.mp4`
  318MB(2160x3840 30fps **61.98초** — ffprobe 실측, 다른 픽스처의 약 2배 길이라
  9fps 프레임 추출 시 ~558프레임, Task 2 Pod 처리 시간이 다른 동작보다 길 수
  있음). `reference/ref-climb.mp4` 4.2MB, `reference/ref-combo.mp4` 18.4MB.
  5개 키 전부 S3 에 존재 확인됨. combo 는 fault.mp4 가 S3 에 없음(phase15_keys.json
  에도 mode1 success 항목만 존재) — correct.mp4 1건만 처리한다.
- **Firestore reference 문서 확인(2026-08-16, firestore_admin.get_reference_motion):**
  `ref-climb`·`ref-combo` 둘 다 `isActive=true`, `referenceKeypointReport`
  (fps=18.0, axisData/axisMask/confidence/data/frames/joints/reliability/version/warnings
  10필드) 존재, `checkpoints`/`clipRange`/`meanAngles`/`techniqueProfile`/
  `bodyNormalizationProfile` 등 downstream 필드 완비(`.planning/phases/
  14-reference-motion-registration/14-BACKFILL-RUN-SUMMARY.json` 의 11동작
  downstreamComplete:true 와 일치). `ref-combo` 문서엔 과거 비활성 사유
  (`inactiveReason`, UAT 2026-06-12 NLF↔RTMW 호환 finding) 필드가 잔존하지만
  현재 `isActive=true` 로 재활성 완료 상태 — MODE_EXPERT(_process) 실행에
  걸림돌 없음.
- **discover_sweep.py 원본 편집하지 않는 이유 (scope 5 이행):** SWEEP_JOBS 에
  climb/climbfault/combo 를 영구 등재하면 그 파일의 다른 상수(RECORD_INVENTORY,
  SWEEP_CATEGORY)와 --check 게이트(evidence/VISUAL-REVIEW 커버리지)가 즉시
  깨진다 — 이 3건에 대한 candidates.json/VISUAL-REVIEW 는 아직 없고(발굴 실행은
  scope 에서 명시적으로 별도 사이클로 이연). 대신 이번 사이클은 새 quick 디렉터리
  전용 `verify_source_gate.py` 에서 discover_sweep 모듈을 importlib 로 읽기만
  하고 `SWEEP_JOBS` 딕셔너리를 **런타임 in-memory** 로만 확장해 `source_gate()`
  함수만 재사용한다 — ehz 디렉터리의 실물 파일은 diff 0 로 남는다. SWEEP_JOBS
  정식 등재(+RECORD_INVENTORY/VISUAL-REVIEW 갱신)는 belle 이 이 산출물을 승인한
  뒤 발굴 실행 사이클의 몫이다.
- **Pod 정체성:** `t4i1p9k2hnvb0q` — 오늘(08-16) 실서비스 분석 경로(S3→SQS→
  Lambda→RunPod)를 복구·실증한 **살아있는 추론 서빙 Pod**(uvicorn
  `runpod_inference.server:app`, 단일 워커, NLF 모델을 VRAM 에 상주). 우리
  작업은 이 서버 프로세스를 재기동/종료하지 않고 **별도 1회성 python 프로세스**로
  같은 GPU 를 잠깐 나눠 쓴다 — 작업 전후 `nvidia-smi` 로 여유만 확인.
  SSH: `ssh root@213.173.102.172 -p 15167 -i ~/.ssh/id_ed25519`.
- **NLF vs RTMW 디바이스 환경변수 구분:** `_process()`(doc.json 생성, Task 2 1단계)
  가 쓰는 운영 pose adapter(`pose_estimator.py`)는 NLF 이고 디바이스는
  `torch.cuda.is_available()` 자동 감지 — 환경변수 불필요. `p35_extract_align.py`
  (align.json 생성, Task 2 2단계)는 별도로 `rtmlib` RTMW 모델을 직접 띄우며
  `RTMW_DEVICE=cuda` 를 **명시로 요구**(README 커맨드 템플릿 그대로) — 두 커맨드에
  이 차이를 그대로 반영할 것(첫 커맨드엔 안 넣고, 두 번째에만 넣는다).
- **push 선행 필수 (gsd-pod-work-push-first 원칙):** Task 1 산출물은 Task 2 가
  Pod 에서 `git pull` 로 받아야 한다 — GSD executor 의 태스크별 커밋은 자동으로
  origin 에 push 되지 않는다. Task 1 완료 후 반드시 `git push origin main`
  까지 실행하고, Task 2 시작 시 로컬 `git log -1 --format=%H` 와
  `git ls-remote origin main` 이 일치하는지 먼저 확인한다.
- **AWS 자격증명:** 로컬 기본 프로파일로 이미 이 버킷 read 확인됨(head-object
  실측) — `discover_sweep.py::_s3_client()` 는 `AWS_PROFILE` 미설정 시
  `sunity-motion` 프로파일을 쓰는데(기존 5동작 스윕에서 이미 검증된 경로),
  Task 3 에서 별도 설정 없이 그대로 두면 된다.
- 이모지 금지, 슬롭 금지. 새 코드의 주석은 한국어로 "왜"를 남기고 근거(quick-
  260816-c3m, phase34_fresh_reanalysis.py/sweep_phase15.py 선례, belle 인용)를
  인용할 것 (CLAUDE.md §7).
</context>

<tasks>

<task type="auto">
  <name>Task 1: SIM_UID 신선 doc 드라이버 작성 + P35 align JOBS 확장 (로컬, GPU 불요)</name>
  <files>backend/scripts/p35_new_motion_docs.py, backend/scripts/p35_extract_align.py</files>
  <action>
    `backend/scripts/p35_new_motion_docs.py` 를 신설한다. 상단 docstring 에 목적
    (quick-260816-c3m, belle 질문 인용, "phase34_fresh_reanalysis.py 의 SIM_UID·
    direct-process 선례 재사용" 명기) + Pod 실행 커맨드 예시를 적는다. 모듈
    상수: `SIM_UID = "fvcNXzEqKjgqVxRPVSj1iwFnIpn2"`(belle 실계정 아님, 리터럴
    그대로), `DEFAULT_BUCKET = "sunity-motion-pilot-videos"`, 그리고
    `ITEMS: dict[str, tuple[str, str]]` — slot(문자열, p35_extract_align.py 의
    JOBS 키와 반드시 동일)→(sourceS3Key, referenceMotionId) 3행: `"climb"` →
    (`"fixtures/phase15/climb/correct.mp4"`, `"ref-climb"`), `"climbfault"` →
    (`"fixtures/phase15/climb/fault.mp4"`, `"ref-climb"`), `"combo"` →
    (`"fixtures/phase15/combo/correct.mp4"`, `"ref-combo"`). 동작명을 리터럴로
    분기하는 코드(if slot == "climb": ...)는 0 — 전부 `ITEMS`/`--slots` 순회로만
    처리한다.

    `_load_pipeline_module()` 헬퍼 — `importlib.util.spec_from_file_location` 로
    `backend/functions/pipeline/app.py` 를 경로 임포트(phase34_fresh_reanalysis.py
    `_load_pipeline_module`/sweep_phase15.py `_load_pipeline` 와 동일 패턴, 새
    분석 로직 0 — 기존 `_process` 재사용).

    `main()`: argparse `--outdir`(필수, Path), `--bucket`(기본
    `DEFAULT_BUCKET`), `--slots`(기본 `ITEMS` 전체 키 콤마 join, 부분집합
    override 가능), `--dry-run`(플래그). `sunity_shared.firestore_admin`,
    `sunity_shared.models` 를 임포트한다(`from sunity_shared import
    firestore_admin, models`). dry-run 이면: 각 slot 에 대해
    `analysisId = f"p35new{slot}{int(time.time())}"` 를 조립하고
    `re.match(r"^[A-Za-z0-9]+$", analysisId)` 로 alnum 전용 self-check(sweep_phase15.py
    `_self_check_identities` 의 alnum 규율과 동일 취지 — 위반 시 RuntimeError) 후
    slot/analysisId/sourceS3Key/referenceMotionId 를 한 줄씩 출력하고 종료(Firestore
    쓰기·`_process` 호출·S3 접근 0). dry-run 아니면: `firestore_admin._db()`
    (phase34_fresh_reanalysis.py 선례 그대로, `# noqa: SLF001` 주석 동반)로 각
    slot 마다 analysis 문서를 `users/{SIM_UID}/analyses/{analysisId}` 에 쓴다 —
    필드는 `analysisId, uid=SIM_UID, mode=models.MODE_EXPERT,
    status=models.STATUS_QUEUED, videoKey=sourceS3Key, videoFormat="mp4",
    fileSizeBytes=0, createdAt=now_ms, updatedAt=now_ms,
    referenceMotionId=refMotionId, sourceLabel=f"p35_new_motion_docs:{slot}"`.
    쓰기 직후 `[{slot}] fresh doc uid={SIM_UID} analysisId={analysisId}
    ref={refMotionId} source={sourceS3Key} (원본 무접촉, uploads copy 0)` 를
    출력한다. 이어서 `pipeline._process(args.bucket, sourceS3Key, SIM_UID,
    analysisId)` 를 호출한다 — 개별 slot 이 예외를 던지면 `try/except
    Exception`(`# noqa: BLE001`)으로 잡아 실패 사유를 출력하고 **다음 slot 으로
    계속 진행**(한 slot 실패로 전체 중단 금지, 성공/실패 목록을 마지막에 요약).
    `_process` 성공 후 `firestore_admin.get_analysis(SIM_UID, analysisId)` 로
    재조회해 `status == models.STATUS_DONE` 확인 — 아니면 실패로 기록하고 계속.
    성공한 slot 은 `{outdir}/{slot}/doc.json` 을 `mkdir(parents=True,
    exist_ok=True)` 후 `json.dumps(out, ensure_ascii=False, default=str)`(Firestore
    Timestamp 대비 `default=str`)로 저장하고, `[{slot}] _process 완료 {dt:.1f}s
    status=done records={n}` 를 출력한다(n = `result.deductionBreakdown.records`
    길이). 실패 slot 이 하나라도 있으면 마지막에 실패 목록을 JSON 으로 찍고
    `main()` 이 1 을 반환, 전부 성공이면 0.

    `backend/scripts/p35_extract_align.py` 의 `JOBS` 딕셔너리(현재 7행:
    elbow/powerspin/pdshape/kipup/realupload/pdshapefault/peterpan)에 정확히 3행을
    추가한다 — `"climb": ("fixtures/phase15/climb/correct.mp4",
    "reference/ref-climb.mp4")`, `"climbfault": ("fixtures/phase15/climb/fault.mp4",
    "reference/ref-climb.mp4")`, `"combo": ("fixtures/phase15/combo/correct.mp4",
    "reference/ref-combo.mp4")`. 기존 7행과 파일의 다른 어떤 줄도 변경하지 않는다
    (`process()`/`main()` 은 이미 임의 JOBS 키를 `--motions` 로 처리하므로 로직
    변경 불요). JOBS 딕셔너리 바로 위 주석에 climb/climbfault/combo 추가 근거
    한 줄(quick-260816-c3m 인용)을 남긴다.

    완료 후 실행자 표준 태스크 커밋에 이어 **반드시 `git push origin main`**
    까지 실행한다 — Task 2 가 Pod 에서 `git pull` 로 이 코드를 받아야 하는데
    로컬 커밋만으로는 Pod 에 전달되지 않는다(gsd-pod-work-push-first 원칙).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && backend/.venv/bin/python backend/scripts/p35_new_motion_docs.py --outdir .planning/quick/260816-c3m-climb-combo-p35/.dryrun --dry-run && grep -q '"climb": ("fixtures/phase15/climb/correct.mp4", "reference/ref-climb.mp4")' backend/scripts/p35_extract_align.py && grep -q '"climbfault": ("fixtures/phase15/climb/fault.mp4", "reference/ref-climb.mp4")' backend/scripts/p35_extract_align.py && grep -q '"combo": ("fixtures/phase15/combo/correct.mp4", "reference/ref-combo.mp4")' backend/scripts/p35_extract_align.py && python3 -c "txt=open('backend/scripts/p35_extract_align.py').read(); assert txt.count('fixtures/phase15/elbow-twist-sister/fault.mp4')==1, 'existing JOBS row mutated'" && PYTHONPATH=backend/tests backend/.venv/bin/python -m pytest -q backend/tests 2>&1 | tail -3 && test -z "$(git status --porcelain backend/shared backend/functions)" && git log -1 --format=%H && git ls-remote origin main</automated>
  </verify>
  <done>
    `p35_new_motion_docs.py` 신설 + `--dry-run` 이 Firestore/S3/GPU 접촉 없이
    3개 alnum-safe identity 를 출력; `p35_extract_align.py` JOBS 가 기존 7행
    무변경 + climb/climbfault/combo 3행 정확히 추가; pytest 실패 59 이하;
    backend/shared·backend/functions 무변경; 커밋 후 `git push origin main`
    완료(로컬 HEAD == origin/main HEAD).
  </done>
</task>

<task type="auto">
  <name>Task 2: Pod 에서 신선 doc 3건 생성 + RTMW align 재추출 (GPU, 살아있는 서빙 Pod)</name>
  <files>/workspace/p35/climb/doc.json (Pod), /workspace/p35/climbfault/doc.json (Pod), /workspace/p35/combo/doc.json (Pod), /workspace/p35/climb/align.json (Pod), /workspace/p35/climbfault/align.json (Pod), /workspace/p35/combo/align.json (Pod)</files>
  <action>
    (a) push 확인 — 로컬에서 `git log -1 --format=%H` 와 `git ls-remote origin
    main`(필요 시 먼저 `git fetch origin main`)의 해시가 같은지 재확인. 다르면
    Task 1 이 push 되지 않은 것이므로 `git push origin main` 을 먼저 실행하고
    재확인한다 — 일치 전엔 Pod 작업을 시작하지 않는다.

    (b) `ssh root@213.173.102.172 -p 15167 -i ~/.ssh/id_ed25519` 로 Pod
    `t4i1p9k2hnvb0q` 에 접속한다. 이 Pod 은 오늘 복구한 **살아있는 추론 서빙
    Pod**(uvicorn `runpod_inference.server:app` 단일 워커) — 이 프로세스를
    죽이거나 재시작하지 않는다. 우리 작업은 전부 별도 1회성 python 프로세스로
    수행한다.

    (c) Pod 안(`/workspace/SunityMotion`)에서 `bash backend/scripts/pod_doctor.sh`
    를 실행한다 — 오늘 이미 bootstrap 했으므로 exit 0(추론 준비 완료) 또는 2
    (추론은 되나 학습 불가, 이번 작업엔 무해)를 기대한다. `[결손]` 줄이 있으면
    그 줄이 출력한 복구 커맨드를 **그대로** 실행한 뒤 doctor 를 재실행해
    결손이 해소됐는지 확인한다 — 진단기가 주지 않은 다른 복구를 임의로 시도
    하지 않는다.

    (d) `cd /workspace/SunityMotion && git fetch origin && git pull --ff-only
    origin main` 을 실행하고, `git rev-parse HEAD` 가 (a)에서 확인한 로컬
    push 커밋 해시와 일치하는지, `git log --oneline -1 -- backend/scripts/
    p35_new_motion_docs.py` 로 신규 파일이 실제로 받아졌는지 확인한다.

    (e) `nvidia-smi --query-gpu=memory.used,memory.total --format=csv` 를 1회
    실행해 기록한다(정보용 — 살아있는 서버가 NLF 모델을 이미 물고 있는 것은
    정상. used/total 비율이 명백히 90% 를 넘는 등 여유가 없어 보이면 진행을
    멈추고 그 사실을 보고한다).

    (f) doc.json 3건: `cd /workspace/SunityMotion/backend && source
    /workspace/aws_env.sh && python3 scripts/p35_new_motion_docs.py --outdir
    /workspace/p35` (venv 아닌 시스템 python3 — README 커맨드 템플릿과 동일
    관례. NLF 는 `torch.cuda.is_available()` 자동 감지라 `RTMW_DEVICE` 불필요).
    slot 마다 `status=done records=N` 로그를 확인한다 — 하나라도 `_process
    FAILED` 또는 done 이 아니면 그 slot 을 지어내지 말고 실패 사유를 그대로
    다음 태스크에 넘긴다(재시도는 원인 파악 후 최대 1회, 같은 오류가 재발하면
    중단하고 보고 — 특히 combo 는 61.98초/318MB 라 다른 슬롯보다 오래 걸릴 수
    있음을 감안해 섣불리 실패로 단정하지 말 것).

    (g) align.json 3건: `cd /workspace/SunityMotion/backend && source
    /workspace/aws_env.sh && RTMW_DEVICE=cuda python3 scripts/p35_extract_align.py
    --workdir /workspace/p35 --motions climb,climbfault,combo` (`--motions` 는
    기존 스크립트 인자, Task 1 이 늘린 JOBS 3행만 지정 — 기존 7동작은 재추출
    하지 않는다). `[motion] frames u=... r=... pairs=[...] -> align.json` 줄
    3개 + 마지막 `ALL_DONE` 을 확인한다.

    (h) `ls -la /workspace/p35/{climb,climbfault,combo}/{doc.json,align.json}`
    로 6개 파일 크기를 확인한다. SSH 세션은 종료해도 무방(파일은 Pod 볼륨에
    남는다) — Task 3 가 별도 SSH/scp 로 회수한다.
  </action>
  <verify>
    <automated>ssh -p 15167 -i ~/.ssh/id_ed25519 root@213.173.102.172 "test -s /workspace/p35/climb/doc.json && test -s /workspace/p35/climbfault/doc.json && test -s /workspace/p35/combo/doc.json && test -s /workspace/p35/climb/align.json && test -s /workspace/p35/climbfault/align.json && test -s /workspace/p35/combo/align.json && python3 -c \"import json;n=sum(1 for m in ('climb','climbfault','combo') if json.load(open(f'/workspace/p35/{m}/doc.json')).get('status')=='done');import sys;sys.exit(0 if n==3 else 1)\" && echo POD-FILES-OK"</automated>
  </verify>
  <done>
    Pod 상 6개 파일(doc.json/align.json ×3슬롯) 실물 존재 + 3건 모두
    doc.json status=done + align 추출기가 ALL_DONE 출력 + 살아있는 uvicorn
    서버 프로세스 무변경(재기동/종료 0) + Pod git pull 이 Task 1 의 push
    커밋과 일치.
  </done>
</task>

<task type="auto">
  <name>Task 3: 리포 영구본 회수 + README 갱신 + 발굴 게이트 실증 + 전체 검증</name>
  <files>.planning/phases/35-server-rendered-comparison-video/data/climb/doc.json, .planning/phases/35-server-rendered-comparison-video/data/climb/align.json, .planning/phases/35-server-rendered-comparison-video/data/climbfault/doc.json, .planning/phases/35-server-rendered-comparison-video/data/climbfault/align.json, .planning/phases/35-server-rendered-comparison-video/data/combo/doc.json, .planning/phases/35-server-rendered-comparison-video/data/combo/align.json, .planning/phases/35-server-rendered-comparison-video/data/README.md, .planning/quick/260816-c3m-climb-combo-p35/verify_source_gate.py, .planning/quick/260816-c3m-climb-combo-p35/source_gate_result.json</files>
  <action>
    (a) 회수: 로컬에서 `mkdir -p .planning/quick/260816-c3m-climb-combo-p35/.podfetch`
    후 `ssh -p 15167 -i ~/.ssh/id_ed25519 root@213.173.102.172 "tar -C
    /workspace/p35 -cf - climb climbfault combo" | tar -C .planning/quick/
    260816-c3m-climb-combo-p35/.podfetch -xf -` (단일 스트림 회수 — scp 3회
    반복보다 원자적). 이어서 리포의 최종 3개 디렉터리를 만들고(`mkdir -p
    .planning/phases/35-server-rendered-comparison-video/data/{climb,
    climbfault,combo}`) `.podfetch/{motion}/doc.json`·`align.json` 을 각각
    `cp` 로 복사한다(이동 아님). `.podfetch/` 는 `files_modified` 목록 밖이라
    표준 커밋 절차가 자동으로 제외한다 — 별도로 `git add` 하지 않는다.

    (b) README 갱신: `.planning/phases/35-server-rendered-comparison-video/data/
    README.md` 의 "파일 구성" 표 헤더를 "7동작 16파일" → "10동작 22파일"로
    바꾸고, climb/climbfault/combo 3행을 추가한다 — 비고 칸은 기존 두 카테고리
    ("활성 렌더 슬롯"/"벤치 슬롯")와 다른 새 문구 "발굴 스윕 후보 (렌더 슬롯
    아님, quick-260816-c3m — P35 렌더 미실행)"를 쓴다. "영상 원본 S3 키" 표에
    climb/climbfault/combo 3행을 Task 1 의 JOBS 값 그대로 추가한다("산출 mp4
    키" 절엔 추가하지 않는다 — 렌더를 만들지 않았으므로). "검증 결과" 절
    아래에 2026-08-16 날짜의 새 불릿 두 개를 추가한다 — (i) 이 3건은
    `proto/phase35/{motion}_v3.mp4` 산출이나 `render_compare_prototype.py`
    실행이 없다(발굴 스윕 소스 게이트 통과만 목적), (ii) `foxtop`/
    `foxtop-split`/`invert`/`sideway-spin` 은 `fixtures/phase15/` 에 학생
    영상이 전혀 없어(정답/오답 둘 다 부재, 기준 영상만 S3 에 존재) 촬영 전엔
    발굴 대상이 될 수 없다 — belle 촬영 계획 근거로 명기.

    (c) `.planning/quick/260816-c3m-climb-combo-p35/verify_source_gate.py` 를
    신설한다. 상단 docstring 에 quick-260816-c3m 인용 + "discover_sweep.py
    원본은 절대 편집하지 않는다"는 규율을 남긴다. `importlib.util.
    spec_from_file_location` 으로 `.planning/quick/260814-ehz-5/
    discover_sweep.py` 를 모듈로 로드한다 — 경로는 이 스크립트 파일 기준
    상대 계산(`Path(__file__).resolve().parents[2] / ".planning/quick/
    260814-ehz-5/discover_sweep.py"`, 하드코딩 절대경로 금지). 로드한 모듈
    객체의 `SWEEP_JOBS` 딕셔너리에(원본 파일은 그대로 두고 **메모리상의 dict
    객체만**) 3개 키를 추가한다 — `"climb": ("fixtures/phase15/climb/
    correct.mp4", "reference/ref-climb.mp4", "ref-climb")`, `"climbfault":
    ("fixtures/phase15/climb/fault.mp4", "reference/ref-climb.mp4",
    "ref-climb")`, `"combo": ("fixtures/phase15/combo/correct.mp4",
    "reference/ref-combo.mp4", "ref-combo")` — discover_sweep.py 의
    SWEEP_JOBS 스키마는 (userKey, refKey, motionId) 3-tuple 이니
    p35_extract_align.py 의 2-tuple JOBS 와 혼동하지 말 것. `--cache-root`
    (옵션, 기본값 `Path(__file__).resolve().parent / ".cache"`)를 로드한
    모듈의 `_CACHE_ROOT` 전역에 대입한다(모듈의 `_cr_root()` 가드가 요구하는
    값 — `.cache/` 는 커밋 대상 아님). 이어서 3개 슬롯 각각에 대해 로드한
    모듈의 `source_gate(m, download=True)` 를 호출해 `passed`/`reasons` 를
    한 줄씩 출력하고, 3건의 반환 dict 를 `{"climb": {...}, "climbfault":
    {...}, "combo": {...}}` 형태로 `.planning/quick/260816-c3m-climb-combo-p35/
    source_gate_result.json` 에 저장한다(`json.dumps(..., ensure_ascii=False,
    indent=1, default=str)`). 셋 중 하나라도 `passed` 가 false 면 실패 사유를
    출력하고 exit 1, 전부 true 면 exit 0.

    (d) `.cache/`(verify_source_gate.py 의 영상 다운로드 캐시)와 `.podfetch/`
    (Pod 회수 스테이징)는 둘 다 `files_modified` 목록 밖이므로 최종 커밋에
    포함하지 않는다 — 표준 커밋 절차가 명시된 `files_modified` 경로만
    스테이징한다면 자동으로 제외된다.

    (e) 아래 검증 커맨드 체인 전체를 실행해 전 게이트를 통과시킨다(Task 3 검증 블록 그대로).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && backend/.venv/bin/python .planning/quick/260816-c3m-climb-combo-p35/verify_source_gate.py && backend/.venv/bin/python -c "import json,sys; d=json.load(open('.planning/quick/260816-c3m-climb-combo-p35/source_gate_result.json')); sys.exit(0 if all(v.get('passed') for v in d.values()) else 1)" && ST=$(git status --porcelain .planning/phases/35-server-rendered-comparison-video/data/) && ! echo "$ST" | grep -qE '(^|/)(elbow|powerspin|kipup|pdshapefault|peterpan|pdshape|realupload)/' && echo "$ST" | grep -qE '(^|/)climb/' && echo "$ST" | grep -qE '(^|/)climbfault/' && echo "$ST" | grep -qE '(^|/)combo/' && ! grep -rq "csKWYvI3WCPYPysNQ9KkWecaUvq1" backend/scripts/p35_new_motion_docs.py .planning/phases/35-server-rendered-comparison-video/data/climb .planning/phases/35-server-rendered-comparison-video/data/climbfault .planning/phases/35-server-rendered-comparison-video/data/combo && test -z "$(git status --porcelain backend/shared backend/functions)" && PS=$(PYTHONPATH=backend/tests backend/.venv/bin/python -m pytest -q backend/tests 2>&1 | tail -1) && echo "$PS" && F=$(echo "$PS" | grep -oE '[0-9]+ failed' | grep -oE '[0-9]+') && { [ -z "$F" ] || [ "$F" -le 59 ]; }</automated>
  </verify>
  <done>
    climb/climbfault/combo 의 doc.json+align.json 이 리포에 영구 커밋되고,
    README 가 10동작 22파일 + foxtop 계열 불가 사유로 갱신되며,
    verify_source_gate.py 가 3건 모두 PASS(source_gate_result.json 기록)하고,
    기존 7동작(5 활성+2 벤치) 데이터는 git status 상 무변경, belle 실uid
    문자열이 신규 산출물에 등장하지 않고, backend/shared·backend/functions
    무접촉, pytest 실패 59 이하.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 드라이버 스크립트 → AWS S3 | fixtures/phase15 원본 read-only 다운로드(_process 내부 + align 재추출) |
| 드라이버 스크립트 → Firestore | SIM_UID 아래 신규 analyses 문서 쓰기 + reference 문서 읽기 |
| 드라이버 스크립트 → _process (운영 코드) | 실서비스와 동일 코드 경로 재사용 — Gemini/Cerebras 실호출 트리거 |
| 실행자 로컬 → RunPod SSH | 살아있는 실서빙 Pod 에 원격 명령 실행 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-c3m-01 | Spoofing | SIM_UID 분석 doc | mitigate | uid 하드코딩 리터럴(fvcNXzEqKjgqVxRPVSj1iwFnIpn2) — belle 실uid 등장 0, Task 3 verify 에서 grep 으로 기계 검증 |
| T-c3m-02 | Tampering | S3 업로드 경로 | mitigate | direct-process 만 사용, uploads/ copy_object 호출 0 — source key 는 fixtures/ read-only 그대로 _process 에 전달 |
| T-c3m-03 | Tampering | 운영 코드(backend/shared, backend/functions) | mitigate | 두 디렉터리 무편집 — Task 1·Task 3 양쪽에서 git status --porcelain 기계 검증 |
| T-c3m-04 | Repudiation | Pod 에서 어떤 코드가 실행됐는지 | mitigate | Task 2 전 로컬 push 완료 확인 + Pod git pull 후 HEAD 대조, 실행 로그에 slot 별 status/records 명시 |
| T-c3m-05 | Information Disclosure | doc.json 커밋(SIM_UID + presigned 필드 포함) | accept | 기존 관례(quick-260808-epy T-35q-02 와 동일 판단 — 파일럿 내부 sim 계정, presigned URL 은 7일 만료, belle 실uid 는 등장하지 않음을 별도 검증) |
| T-c3m-06 | Information Disclosure | Gemini/Cerebras 실호출(기술 인식/코칭 문장) | accept | 운영과 동일한 추론 호출(학습 데이터 전송 아님) — SUMMARY 에 호출 규모·비용 추정 필수 기재 (belle 원칙: LLM 학습 영향 항상 보고) |
| T-c3m-07 | Denial of Service | 살아있는 서빙 Pod GPU VRAM 경합 | mitigate | 3슬롯을 한 프로세스 내 순차 처리(모델 1회 로드 재사용, `_ensure_adapters` 캐시) + 작업 전 nvidia-smi 확인 + uvicorn 서버 프로세스 재기동/종료 금지 |
| T-c3m-SC | Tampering | 패키지 설치 | accept | 신규 pip 설치 0 — 로컬 backend/.venv, Pod 기존 시스템 python3 만 재사용(오늘 이미 bootstrap 완료) |
</threat_model>

<verification>
- 운영 코드 무접촉: `git status --porcelain backend/shared backend/functions` 빈 출력 (Task 1·Task 3 공통 게이트)
- 동작별 분기 0: `p35_new_motion_docs.py`/`p35_extract_align.py` 양쪽 다 ITEMS/JOBS 딕셔너리 순회만 — `if slot ==`/`if motion ==` 패턴 부재
- discover_sweep.py(quick-260814-ehz-5) 원본 무편집: `git diff --stat .planning/quick/260814-ehz-5/` 0줄
- SIM_UID 전용: belle 실uid(csKWYvI3WCPYPysNQ9KkWecaUvq1) 문자열이 신규 산출물(p35_new_motion_docs.py, data/{climb,climbfault,combo}/)에 등장 0
- 기존 P35 데이터 무변경: `git status --porcelain .planning/phases/35-server-rendered-comparison-video/data/` 에 elbow/powerspin/kipup/pdshapefault/peterpan/pdshape/realupload 미등장
- pytest 기준선: `PYTHONPATH=backend/tests backend/.venv/bin/python -m pytest -q backend/tests` failed <= 59
- foxtop 계열 4동작 불가 사유가 README.md 에 명시 (임의 판단으로 처리 시도 금지 — 학생 영상 부재는 계획 권한 밖 사유)
</verification>

<success_criteria>
- climb(correct)·climb(fault)·combo(correct) 3건의 doc.json+align.json 이 `.planning/phases/35-server-rendered-comparison-video/data/` 에 영구 커밋되어 Pod 소실과 무관하게 재현 가능
- `discover_sweep.py::source_gate()` 가 이 3건 모두에 대해 PASS(원본 파일 무편집, verify_source_gate.py 의 in-memory 확장으로 실증)
- README.md 가 10동작 22파일로 갱신되고, foxtop·foxtop-split·invert·sideway-spin 의 불가 사유(학생 영상 부재)가 belle 촬영 계획 근거로 명시
- 기존 5 활성 렌더 슬롯 + 2 벤치 슬롯 데이터 무변경, backend/shared·backend/functions 무접촉, SIM_UID 전용(belle 실계정 무접촉)
- pytest 기준선(59 실패 이하) 유지, git push 완료로 Pod 작업 재현 가능
</success_criteria>

<output>
Create `.planning/quick/260816-c3m-climb-combo-p35/260816-c3m-SUMMARY.md` when done — LLM 학습 영향(Gemini/Cerebras 실호출 규모·비용 추정, 추론만·학습 전송 0) 필수 기재, foxtop 계열 4동작 불가 사유 재확인, discover_sweep.py 소스 게이트 판정 결과 요약, 다음 단계(발굴 실행 사이클에서 SWEEP_JOBS 정식 등재 + RECORD_INVENTORY/VISUAL-REVIEW 갱신 필요) 명기.
</output>
