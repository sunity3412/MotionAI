---
phase: quick-260816-e26
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/training/datagen/schema.py
  - backend/tests/phase22/test_schema.py
  - .planning/quick/260816-e26-gate-parser-runaway/audit_reparse.py
autonomous: true
requirements: [QUICK-260816-E26-GATE-PARSER-RUNAWAY]

must_haves:
  truths:
    - "extract_report_json 은 REPORT_KEYS 부분집합이 아닌 후보 dict(리포트가 아닌 다른 구조체의 조각)를 리포트로 반환하지 않는다"
    - "정상 형식의 완결 리포트(빈 골격 포함)는 수리 후에도 그대로 dict 로 인정된다 — 회귀 없음"
    - "기존 test_schema.py 의 extract_report_json 테스트 5개 함수가 그대로 통과한다"
    - "v29 run1 실물 raw 29건(trap/hard_negative 제외) 중 폭주로 확인된 15건이 전부 None(파싱 실패)으로 재집계된다"
    - "backend/tests 전체 스위트에서 새 회귀가 0건이다 — 기존 무관 실패 59건은 그대로, 통과 카운트만 신규 테스트 1개만큼 증가"
  artifacts:
    - path: "backend/training/datagen/schema.py"
      provides: "extract_report_json 의 후보 dict 인정 조건이 '키 전부가 REPORT_KEYS 부분집합'으로 강화됨 + 2026-08-16 실측 근거 docstring"
      contains: "def extract_report_json"
    - path: "backend/tests/phase22/test_schema.py"
      provides: "폭주 산출 None 반환 회귀 테스트 — 원 진단(무관 조각) + 키 이름 우연 충돌(부분일치 조각) 2케이스"
      contains: "def test_extract_report_json_truncated_runaway_returns_none"
    - path: ".planning/quick/260816-e26-gate-parser-runaway/audit_reparse.py"
      provides: "v29 run1 실물 29건 재파싱 감사 — 수리 전/후 dict/None 분류를 사전 검증된 기대치와 대조, 불일치 시 non-zero exit"
      contains: "extract_report_json"
  key_links:
    - from: "backend/evals/phase22/run_bakeoff.py (run_report_item/run_synth_item, aligned 경로)"
      to: "backend/training/datagen/schema.py::extract_report_json"
      via: "schema.extract_report_json(raw) 호출 — 단일 진실 함수, 호출부 무변경"
      pattern: "extract_report_json"
    - from: ".planning/quick/260816-e26-gate-parser-runaway/audit_reparse.py"
      to: "backend/training/datagen/schema.py"
      via: "수리된 extract_report_json 을 v29 실물 raw 에 재적용"
      pattern: "extract_report_json"
---

<objective>
게이트 파서 `schema.extract_report_json` 이 폭주(토큰 상한까지 반복 생성되어 바깥 리포트
객체가 안 닫힌) 산출에서, 스캔 도중 우연히 balanced 로 닫히는 안쪽 조각(예: `segments[]`
항목 하나, 또는 `faults[]` 항목이 hallucinate 한 `corrected_coords` 필드)을 리포트로
오인해 "파싱 성공"으로 집계하던 결함을 수리한다. v29 게이트 원문 회수분 실측(2026-08-16):
29건(trap/hard_negative 제외) 중 15건이 실제로는 바깥 객체가 안 닫힌 폭주인데 24건이
`parse=1.0` 으로 집계됐다.

Purpose: 게이트가 "아무 말 안 함(빈 골격)"과 "40KB 폭주"를 구분하지 못하면 v29 같은 재학습
판정이 오염된다(재료 붕괴를 파싱 성공으로 가리는 셈). 이 수리는 그 오염원 하나를 좁게
제거한다 — score_json/normalize_report/assert_gates 의 임계·비교식, `</thought>` 스트립,
raw_decode 스캔 방식은 전부 무변경.

Output: 강화된 `extract_report_json` + 회귀 테스트 1개 + v29 실물 29건 재집계 감사 결과
(SUMMARY.md 에 수치 기록).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend/training/datagen/schema.py
@backend/tests/phase22/test_schema.py

## 계획 단계 실측 (2026-08-16) — 재조사 불필요, 이 사실을 전제로 구현할 것

v29 run1 회수분(`v29-20260815-124030_checkpoint-64-merged_run1.json`, records 37건 —
type real=21/hard_negative=2/synthetic_grounding=8/trap=6)에서 real+synthetic_grounding
29건에 **현재(미수리) `extract_report_json`** 을 그대로 재적용해 정밀 분류한 결과:

- **before**: dict=24, None=5 (브리프의 "24/29 가 parse=1.0" 과 일치).
- 29건 전부에 대해 "바깥 객체(맨 첫 `{`)가 raw_decode 로 즉시 완결되는가"를 직접 측정한
  **정답 기준**: outer_ok=True(진짜 완결, 폭주 아님) 14건 / outer_ok=False(폭주, 바깥이 안
  닫힘) 15건. 이 14/15 분할이 이 수리가 맞춰야 할 정답이다.
- 브리프의 "폭주 15건·빈 골격 9건" 중 15는 이 정답과 일치하지만, "9"는 느슨한 눈대중
  추정이었다 — 실측 정답은 14건(완결 리포트 중 다수가 "빈 골격"이 아니라 실질 콘텐츠를
  가진 큰 리포트, 예: `real-combo-correct` 15490byte, `real-pdshape-fault` 8107byte). Task
  2 의 SUMMARY.md 는 실측값 14/15 를 그대로 적을 것 — 브리프의 "9" 에 억지로 맞추지 말 것.

### fix_policy 원문("REPORT_KEYS 중 최소 1개를 가진 dict 만 인정")은 이 데이터로 검증하면 불충분

원문 그대로("후보 dict 의 키 중 하나라도 REPORT_KEYS 멤버면 인정")를 실제로 시뮬레이션하면
before dict=24 -> after dict=16 (15건이 아니라 8건만 None 으로 전환, `real-powerspin-fault`/
`real-sideway-spin`/synth-ground 6건은 정상 전환되지만 **2건이 여전히 오탐으로 남는다**:

- `real-peterpan-correct`, `real-invert` — 둘 다 바깥 객체는 안 닫혔는데(폭주), 스캔이
  `faults[]` 항목 하나(`{"body_part": "...", "fault_category": "...", "corrected_coords":
  [...]}`)에서 멈춘다. 이 항목은 FAULT_ITEM_KEYS 스키마 밖의 `corrected_coords` 필드를
  모델이 hallucinate 한 것인데, 그 이름이 REPORT_KEYS 최상위 키 이름과 우연히 겹쳐서 "최소
  1개 일치" 조건을 통과해버린다.

따라서 Task 1 은 fix_policy 의 문구를 "후보 dict 의 키 **전부**가 REPORT_KEYS 의 부분집합
(그리고 비어있지 않음)"으로 구현한다 — "최소 1개 일치"를 포함하는 엄격화된 특수 사례이므로
fix_policy 의도(관대화 금지, 좁은 수정)에서 벗어나지 않으며, `</thought>` 처리·raw_decode
스캔 루프·score_json/normalize_report/assert_gates 는 전혀 건드리지 않는다. 이 조건으로
시뮬레이션하면 after dict=14 / None=15 — 위 "정답 기준"과 정확히 일치하고, 기존
`backend/tests/phase22` 411건(스킵 1건 별도) 전체가 무회귀임을 in-process 몽키패치로
사전 확인했다.

### 회귀 방지용 fixture 구성 요령 (Task 1 새 테스트, 정확히 검증된 구조)

두 케이스 모두 "바깥 최상위 객체가 안 닫힌 채 텍스트가 끝남"을 만족해야 한다 — 구체적으로,
최상위 여는 중괄호로 시작해 `"coaching"` 키·문자열 값을 두고, 그 다음 두 번째 키(아래
케이스별로 다름)를 완전히 닫힌 형태로 두고, 이어서 `"svg_spec"` 키의 값으로 시작하는
문자열 리터럴이 따옴표로 닫히지 않은 채 텍스트가 끊기게 만든다(= 바깥 객체의
`raw_decode` 가 "Unterminated string" 으로 실패).

- 케이스 1(원 진단 재현, REPORT_KEYS 와 전혀 안 겹침): 두 번째 키는 `"segments"`, 값은
  `[{"start_frame": 0, "end_frame": 8, "description": "설명"}]` 하나짜리 배열. 이 안쪽
  객체의 키(start_frame/end_frame/description)는 REPORT_KEYS 밖 → 새 조건에서도, 구
  "any" 조건에서도 걸러져야 한다(원래도 회귀 테스트로 유효).
- 케이스 2(신규 발견, 부분일치 회귀 방지 — **핵심**): 두 번째 키는 `"faults"`, 값은
  `[{"body_part": "right_knee", "fault_category": "alignment", "corrected_coords":
  [{"frame": 2, "x": 538}]}]` 하나짜리 배열. 이 안쪽 객체는 `raw_decode` 로 정상 완결되고
  `corrected_coords` 라는 이름이 REPORT_KEYS 와 우연히 겹친다 — "최소 1개 일치" 조건이면
  여기서 **잘못 통과**하지만(실측: real-peterpan-correct/real-invert), "키 전부가 부분집합"
  조건에서는 `body_part`/`fault_category` 가 REPORT_KEYS 밖이므로 정확히 걸러진다. 이
  케이스가 빠지면 미래에 누군가 "any" 로 되돌려도 pytest 가 못 잡는다.

두 케이스 모두 최종 기대값은 `schema.extract_report_json(raw) is None`.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: extract_report_json 폭주 방어 강화 + 회귀 테스트</name>
  <files>backend/training/datagen/schema.py, backend/tests/phase22/test_schema.py</files>
  <behavior>
    기존 5케이스(파일 상단 컨텍스트에서 이미 읽음)는 무회귀로 유지한다. 신규 1케이스:
    - Test 6 (`test_extract_report_json_truncated_runaway_returns_none`): 위 컨텍스트의
      "회귀 방지용 fixture 구성 요령" 케이스 1·케이스 2 를 각각 별도 raw 문자열로 구성해
      `schema.extract_report_json(raw) is None` 을 두 케이스 모두 assert. docstring 에
      2026-08-16 v29 실측 근거(원 진단 record id + 부분일치로는 불충분했던 키 충돌 record
      id)를 한국어로 명시.
  </behavior>
  <action>
    `extract_report_json` 의 후보 dict 인정 조건을 `isinstance(obj, dict)` 단독에서
    `isinstance(obj, dict) and obj and set(obj) <= set(REPORT_KEYS)` 로 강화한다(빈 dict
    `{}` 도 제외 — "최소 1개" 요건). REPORT_KEYS 는 모듈에 이미 정의된 튜플을 그대로
    재사용하고, 새 모듈 상수는 추가하지 말 것(diff 최소화). `</thought>` rfind 스트립,
    `body.find("{")` 스캔 루프, 실패/거부 시 `pos = body.find("{", pos + 1)` 로 전진하는
    구조, 끝까지 없으면 `None` 반환 — 이 전부는 한 글자도 바꾸지 말 것. 바뀌는 것은 accept
    조건문 한 줄뿐이다.

    함수 docstring 에 "2026-08-16 v29 게이트 실측" 단락을 추가한다. 내용: (1) 폭주 산출은
    바깥 리포트 객체가 안 닫혀 raw_decode 가 실패하고, 스캔이 안쪽의 우연히 balanced 인
    조각을 리포트로 오인했다(실측: `real-powerspin-fault`/`real-sideway-spin`, raw
    40KB대가 `segments[]` 항목 하나로 집계됨). (2) "REPORT_KEYS 중 최소 1개 키 일치"만으론
    불충분함이 실측으로 드러남 — `faults[]` 항목이 자체적으로 hallucinate 한
    `corrected_coords` 필드가 REPORT_KEYS 이름과 우연히 겹쳐(`real-peterpan-correct`/
    `real-invert`) 부분일치 조건도 통과시킨다. 그래서 "후보 dict 의 키 전부가 REPORT_KEYS
    부분집합"으로 강화했다. (3) v29 run1 회수분 29건(trap/hard_negative 제외) 재적용 결과
    = 폭주 15건 전부 None, 완결 리포트 14건은 그대로 dict(감사 스크립트 = quick-260816-e26
    `audit_reparse.py`).

    새 테스트 함수는 `test_schema.py` 의 extract_report_json 테스트 블록 끝
    (`test_extract_report_json_braces_inside_string_values` 함수 뒤,
    `test_normalize_segments_span_dict_coercion` 함수 앞)에 추가 — 블록 내 로컬 번호
    규약(Test 1~5 가 이미 있음)을 따라 "Test 6" 으로 표기.

    RED 커밋 후 GREEN 커밋 (TDD 규약): `test(quick-260816-e26): 폭주 산출 None 반환 회귀
    테스트 추가` → `fix(quick-260816-e26): extract_report_json 폭주 방어 — REPORT_KEYS
    부분집합 검증`.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && PYTHONPATH=backend/tests backend/.venv/bin/python -m pytest -q backend/tests/phase22/test_schema.py && PYTHONPATH=backend/tests backend/.venv/bin/python -m pytest -q backend/tests</automated>
  </verify>
  <done>
    test_schema.py: 20 passed(기존 19 + 신규 1, 계획 단계 측정 기준 — 19는 `grep -c "^def
    test_" backend/tests/phase22/test_schema.py` 로 재확인 가능). backend/tests 전체:
    passed 카운트가 계획 단계 측정값(4370)에서 정확히 신규 테스트 수만큼(+1 = 4371)
    증가하고, failed=59(전부 test_pipeline_geminic_wiring.py/test_pipeline_geminid_wiring.py/
    test_spike_gemini_moment_smoke.py 소속 — schema.py 와 무관, 계획 단계 기존 실패와
    동일해야 함) · skipped=26 은 변하지 않는다. 이 셋 중 하나라도 계획 단계 측정치와
    어긋나면(새 실패 발생, 또는 무관 실패 59건 중 하나가 사라지거나 늘어남) 수리 방침이
    틀렸거나 다른 무언가를 건드린 것이니 멈추고 정확한 수치와 함께 보고할 것 — 억지로
    맞추지 말 것.
  </done>
</task>

<task type="auto">
  <name>Task 2: v29 실물 29건 재집계 감사 + SUMMARY 수치 기록</name>
  <files>.planning/quick/260816-e26-gate-parser-runaway/audit_reparse.py</files>
  <action>
    `.planning/quick/260816-e26-gate-parser-runaway/audit_reparse.py` 를 신설한다(Write
    도구 사용). 스크립트는:
    (1) `Path(__file__).resolve().parents[3]` 로 repo root 를 구해 `backend/training` 을
        `sys.path` 에 주입한 뒤 `from datagen import schema` import(schema.py 자신이
        `backend/shared/python` 주입은 이미 처리함 — 추가 불필요).
    (2) 고정 경로
        `/private/tmp/claude-501/-Users-kimtaesung-Dev-SunityMotion/4cd70dc3-9461-4b35-bf25-b2405ed6c7e7/scratchpad/v29-20260815-124030_checkpoint-64-merged_run1.json`
        를 로드해 `records` 중 `type` 이 `"real"` 또는 `"synthetic_grounding"` 인 29건만
        순회한다(`trap`/`hard_negative` 제외).
    (3) 각 레코드의 `raw` 필드에 (Task 1 로 수리된) `schema.extract_report_json` 을 적용해
        `None` 이면 "none", `dict` 면 "dict" 로 분류한다.
    (4) 아래 두 집합(계획 단계에 이미 정밀 검증된 기대치)과 정확히 일치하는지 대조한다 —
        기대 "none" 15건: `real-kipup-fault`, `real-powerspin-correct`,
        `real-powerspin-fault`, `real-peterpan-correct`, `real-climb-fault`,
        `real-invert`, `real-sideway-spin`, `real-cocoon`, `real-jadesplit`,
        `synth-ground-stage1-s11`, `synth-ground-stage1-s12`, `synth-ground-stage2-s21`,
        `synth-ground-stage2-s22`, `synth-ground-stage3-s32`, `synth-ground-stage1-s13`.
        기대 "dict" 14건: `real-kipup-correct`, `real-peterpan-fault`,
        `real-elbowtwist-correct`, `real-elbowtwist-fault`, `real-pdshape-correct`,
        `real-pdshape-fault`, `real-climb-correct`, `real-combo-correct`, `real-foxtop`,
        `real-ironx`, `real-angle-corner`, `real-various-poleorg`,
        `synth-ground-stage3-s31`, `synth-ground-stage2-s23`.
    (5) 레코드별 한 줄(id, raw 길이, 분류, 기대와 일치 여부)과 집계 요약(dict 카운트/none
        카운트/불일치 카운트)을 stdout 에 출력하고, 불일치가 1건이라도 있으면 `sys.exit(1)`
        (그 record id 를 명시), 전부 일치하면 `sys.exit(0)`.
    598KB 짜리 원본 JSON 을 Read 도구로 직접 읽지 말 것 — 스크립트(Bash 실행)가 처리하고
    stdout 요약만 확인한다.

    실행 후, 스크립트가 출력한 실제 dict/none 카운트와 불일치 여부를
    `.planning/quick/260816-e26-gate-parser-runaway/260816-e26-SUMMARY.md` 에 수치로
    기록한다(표 또는 목록) — 최소한 다음을 포함: 수리 전(계획 단계 측정, before dict=24/
    none=5) vs 수리 후(이 태스크 실측) 대비표, `real-peterpan-correct`/`real-invert` 키
    충돌 사례를 부분집합 조건으로 해결했다는 서술, 브리프의 "빈 골격 9건" 추정치와 실측
    14건의 차이 및 이유(일부는 실질 콘텐츠를 가진 대형 완결 리포트) 1~2문장.

    커밋: `fix(quick-260816-e26): v29 실물 29건 재집계 감사 스크립트 + 결과 기록`.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && backend/.venv/bin/python .planning/quick/260816-e26-gate-parser-runaway/audit_reparse.py</automated>
  </verify>
  <done>
    스크립트가 exit 0(29건 전부 기대 분류와 일치: none 15건/dict 14건, 불일치 0건)으로
    종료하고, SUMMARY.md 에 수리 전/후 수치와 두 키-충돌 레코드 해결 서술이 기록되어 있다.
    스크립트가 불일치를 보고하면(exit 1) 억지로 SUMMARY 를 맞추지 말고 실제 불일치 record
    id·raw 길이·현재 분류를 그대로 SUMMARY 에 남기고 보고할 것.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 모델 자유생성 출력 → extract_report_json | guided JSON 해제 상태의 untrusted 텍스트(토큰 상한 도달 시 임의 절단 포함)가 파서에 직접 도달 — 기존 T-hv4-01 과 동일 경계, 이 plan 은 그 경계의 방어를 강화 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-e26-01 | Tampering | schema.extract_report_json | mitigate | 후보 dict 의 키 전부가 REPORT_KEYS 부분집합일 때만 인정(부분일치 아닌 전체 부분집합) — 폭주 조각이 REPORT_KEYS 이름을 우연히 재사용해도(실측: corrected_coords 충돌) 나머지 키가 스키마 밖이면 거부됨. normalize_report 화이트리스트(T-22-02/T-hv4-01)가 이중 방어로 유지됨 |
| T-e26-02 | Repudiation | 게이트 집계(parse=1.0/0.0) | mitigate | Task 2 감사 스크립트가 v29 실물 29건의 수리 전/후 분류를 SUMMARY.md 에 수치로 고정 — 향후 "그때 왜 24/29 로 집계됐는지" 재조사 불필요 |
| T-e26-SC | Tampering | 패키지 설치 | accept | 신규 패키지 설치 0건(표준 라이브러리 json/pathlib/sys 만 사용, 기존 스택 무변경) |
</threat_model>

<verification>
1. `cd /Users/kimtaesung/Dev/SunityMotion && PYTHONPATH=backend/tests backend/.venv/bin/python -m pytest -q backend/tests` — passed 는 계획 단계 측정치(4370)에서 신규 테스트 수만큼만 증가, failed=59(무관 사전 실패, 불변), skipped=26(불변).
2. `cd /Users/kimtaesung/Dev/SunityMotion && backend/.venv/bin/python .planning/quick/260816-e26-gate-parser-runaway/audit_reparse.py` — v29 run1 실물 29건이 기대 분류(None 15/dict 14)와 정확히 일치, exit 0.
3. `rtk git diff -- backend/evals/phase22/run_bakeoff.py backend/evals/phase22/assert_gates.py` — 두 파일 모두 diff 0건(score_json/normalize_report/assert_gates 임계·비교식 무변경 확인).
</verification>

<success_criteria>
- extract_report_json 이 REPORT_KEYS 부분집합이 아닌 후보를 리포트로 반환하지 않는다(원 진단 + 키 충돌 케이스 모두).
- 기존 test_schema.py 5개 extract_report_json 테스트 + phase22 스위트 411건이 무회귀.
- backend/tests 전체에서 새 회귀 0건, 무관 사전 실패 59건은 그대로.
- v29 run1 실물 29건 재집계가 SUMMARY.md 에 정확한 수치(before/after, 키 충돌 2건 해결)로 기록됨.
- score_json/normalize_report/assert_gates 임계·비교식 diff 0건.
</success_criteria>

<output>
Create `.planning/quick/260816-e26-gate-parser-runaway/260816-e26-SUMMARY.md` when done.
</output>
