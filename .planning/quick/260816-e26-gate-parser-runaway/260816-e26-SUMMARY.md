---
phase: quick-260816-e26
plan: 01
subsystem: ml-eval-gate
tags: [json-parsing, gate-parser, extract_report_json, v29, schema, deduction-engine]

# Dependency graph
requires: []
provides:
  - "extract_report_json 폭주(바깥 리포트 객체 미완결) 오탐 방어 — 후보 dict 인정 조건이 REPORT_KEYS 부분집합으로 강화"
  - "회귀 테스트(test_schema.py Test 6) — 원 진단 재현 + 키 이름 우연 충돌 케이스"
  - "v29 run1 실물 29건 재집계 감사 스크립트(audit_reparse.py) + 정확한 before/after 수치"
affects: [22-07 v4 게이트, run_bakeoff.py, assert_gates.py, 다음 재학습 판정 파이프라인]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "게이트 파서 인정 조건 = 후보 dict 의 키 전부가 스키마 화이트리스트(REPORT_KEYS)의 부분집합(전체 일치, 부분일치 아님) + 비어있지 않음"

key-files:
  created:
    - .planning/quick/260816-e26-gate-parser-runaway/audit_reparse.py
  modified:
    - backend/training/datagen/schema.py
    - backend/tests/phase22/test_schema.py

key-decisions:
  - "fix_policy 원문(REPORT_KEYS 중 최소 1개 키 일치)은 실측으로 불충분함이 드러나(real-peterpan-correct/real-invert 가 corrected_coords 필드명 우연 충돌로 여전히 오탐) 키 전부가 REPORT_KEYS 부분집합 조건으로 강화"
  - "빈 dict {} 도 거부 대상에 포함(obj and ...) — 부분집합 조건만으로는 빈 dict 도 통과하므로 최소 1개 키 요건을 명시적으로 유지"

patterns-established:
  - "화이트리스트 파서는 부분일치가 아니라 완전 부분집합으로 검증 — 필드명 우연 충돌에 의한 오탐을 막는 일반 원칙"

requirements-completed: [QUICK-260816-E26-GATE-PARSER-RUNAWAY]

# Metrics
duration: 9min
completed: 2026-08-16
---

# Quick 260816-e26: 게이트 파서 폭주 오탐 수리 Summary

**extract_report_json 인정 조건을 "최소 1개 키 일치"가 아닌 "후보 dict 키 전부가 REPORT_KEYS 부분집합"으로 강화해, v29 게이트가 폭주(바깥 리포트 객체 미완결) 산출 15건을 parse=1.0 으로 오집계하던 결함을 제거**

## Performance

- **Duration:** 9 min
- **Started:** 2026-08-16T01:29:38Z
- **Completed:** 2026-08-16T01:38:22Z
- **Tasks:** 2/2
- **Files modified:** 3 (1 created, 2 modified)

## Accomplishments

- `extract_report_json` (backend/training/datagen/schema.py) 후보 dict 인정 조건을 `isinstance(obj, dict)` 단독에서 `isinstance(obj, dict) and obj and set(obj) <= set(REPORT_KEYS)` 로 강화 — diff 1줄 + docstring 실측 근거 추가, `</thought>` 스트립·raw_decode 스캔 루프는 전부 무변경.
- 회귀 테스트(test_schema.py Test 6) 2케이스 추가 — 원 진단 재현(REPORT_KEYS 와 전혀 안 겹치는 segments[] 항목) + 키 이름 우연 충돌(faults[] 항목이 hallucinate 한 corrected_coords 필드명이 REPORT_KEYS 최상위 키와 겹치는 사례). RED 단계에서 두 케이스 모두 실패를 직접 확인 후 GREEN.
- v29 run1 실물 29건 재집계 감사 스크립트(audit_reparse.py) 신설·실행 — 29건 전부 사전 검증된 기대 분류(none 15/dict 14)와 정확히 일치, exit 0.

## Task Commits

Each task was committed atomically:

1. **Task 1 (RED): extract_report_json 폭주 방어 회귀 테스트** - `fd283a58` (test)
2. **Task 1 (GREEN): extract_report_json 폭주 방어 강화** - `14377c0f` (fix)
3. **Task 2: v29 실물 29건 재집계 감사 스크립트 + 결과** - `34097ae3` (fix)

_Task 1 은 tdd="true" — RED(test) → GREEN(fix) 순서로 커밋. REFACTOR 커밋 없음(조건문 1줄 변경, 리팩터링 대상 없음)._

## Files Created/Modified

- `backend/training/datagen/schema.py` - `extract_report_json` 인정 조건 강화(REPORT_KEYS 부분집합) + docstring 에 2026-08-16 v29 실측 근거 단락 추가
- `backend/tests/phase22/test_schema.py` - `test_extract_report_json_truncated_runaway_returns_none` (Test 6) 추가 — 폭주 산출 None 반환 회귀 테스트 2케이스
- `.planning/quick/260816-e26-gate-parser-runaway/audit_reparse.py` - v29 run1 실물 29건 재집계 감사 스크립트(신설) — 수리된 파서를 raw 에 재적용해 사전 검증된 기대치와 대조, 불일치 시 non-zero exit

## Decisions Made

- **부분일치 대신 완전 부분집합 조건 채택**: PLAN.md 컨텍스트에서 계획 단계에 이미 실측으로 결정되어 있었다 — "REPORT_KEYS 중 최소 1개 키 일치" 정책을 실제 시뮬레이션하면 before dict=24 → after dict=16 (15건이 아니라 8건만 전환)이고, `real-peterpan-correct`/`real-invert` 2건이 corrected_coords 필드명 우연 충돌로 여전히 오탐된다. "키 전부가 부분집합" 조건은 이 2건까지 정확히 걸러 after dict=14/None=15 로 정답 기준과 정확히 일치한다. 실행 단계에서는 이 결정을 그대로 구현하고 독립적으로 재검증했다.
- **빈 dict `{}` 명시적 거부**: `set(obj) <= set(REPORT_KEYS)` 만으로는 빈 dict(`{}`, 부분집합의 자명한 경우)도 통과하므로 `obj and ...` 를 앞에 추가해 "최소 1개 키" 요건을 유지.

## Deviations from Plan

None - plan executed exactly as written. 모든 검증 수치가 계획 단계 실측치와 정확히 일치해 추가 조사나 방침 변경이 필요하지 않았다.

추가 자체 검증(계획에 명시되지 않았으나 신뢰도를 높이기 위해 수행): 수리 전(구 "any dict" 로직)의 before dict=24/none=5 주장을 scratchpad 전용 스크립트로 로직을 그대로 복제해 독립 재현 — PLAN.md 브리프 수치와 일치 확인. 이 스크립트는 scratchpad 에만 존재하며 커밋 대상이 아니다(휘발성, memory 원칙 — scratchpad 는 보존 주장 금지).

## Before/After 재집계 수치 (v29 run1, real+synthetic_grounding 29건)

| 구분 | dict (parse=1.0 오집계) | None (파싱 실패로 정확 집계) |
|------|------|------|
| 수리 전 (구 "any dict" 로직 — 계획 단계 실측 + 실행 단계 독립 재확인) | 24 | 5 |
| "REPORT_KEYS 중 최소 1개 일치" 정책 시뮬레이션 (채택 안 함, 계획 단계 실측) | 16 | 13 |
| 수리 후 (이번 태스크 실측, `audit_reparse.py` exit 0) | 14 | 15 |
| 정답 기준 (바깥 객체 raw_decode 즉시 완결 여부 직접 측정) | 14 | 15 |

수리 후 수치가 정답 기준과 정확히 일치(불일치 0건, `audit_reparse.py` exit 0). 키 충돌 2건(`real-peterpan-correct`, `real-invert`)은 faults[] 항목이 hallucinate 한 `corrected_coords` 필드명이 REPORT_KEYS 최상위 키와 우연히 겹쳐 "최소 1개 일치" 조건이면 여전히 오탐되지만(위 표의 시뮬레이션 행), "키 전부가 부분집합" 조건에서는 같은 항목의 `body_part`/`fault_category` 가 REPORT_KEYS 밖이라 정확히 거부된다.

브리프의 "폭주 15건·빈 골격 9건" 추정 중 "9"는 느슨한 눈대중이었다 — 실측 정답은 완결 리포트 14건이며, 그중 다수는 "빈 골격"이 아니라 실질 콘텐츠를 가진 대형 완결 리포트다(예: `real-combo-correct` 15490byte, `real-pdshape-fault` 8107byte). "폭주 15건" 추정치는 정답과 정확히 일치했다.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `extract_report_json` 은 `run_bakeoff.py`(aligned 경로)와 `assert_gates.py`(`_parsed_report`)가 공유하는 단일 진실 함수이므로, 이 수리는 호출부 변경 없이 다음 게이트 실행부터 자동 적용된다.
- `score_json`/`normalize_report`/`assert_gates` 의 임계·비교식은 전혀 건드리지 않았다(diff 0 확인) — 게이트 판정 기준 자체는 무변경, 오직 "폭주를 파싱 성공으로 오인하던 계측 결함"만 제거됨.
- 이 수리 자체가 v29 게이트의 FAIL 판정을 뒤집지는 않는다 — v29 는 이미 재료(붕괴) 문제로 FAIL 이고, 이 수리는 다음 재학습 게이트부터 parse=1.0/0.0 집계를 정확하게 만드는 계측 정합성 수리다.

## TDD Gate Compliance

Task 1 gate sequence 확인(git log): `test(quick-260816-e26): 폭주 산출 None 반환 회귀 테스트 추가` (`fd283a58`, RED) → `fix(quick-260816-e26): extract_report_json 폭주 방어 — REPORT_KEYS 부분집합 검증` (`14377c0f`, GREEN). RED 단계에서 두 케이스 중 케이스 1(segments[] 항목)부터 즉시 AssertionError 로 실패 확인(수리 전 로직이 안쪽 조각을 dict 로 반환). REFACTOR 커밋 없음 — diff 가 조건문 1줄 + docstring 뿐이라 리팩터링 대상이 없다.

## Verification Results

- `PYTHONPATH=backend/tests backend/.venv/bin/python -m pytest -q backend/tests/phase22/test_schema.py` → **20 passed** (기존 19 + 신규 1).
- `PYTHONPATH=backend/tests backend/.venv/bin/python -m pytest -q backend/tests/phase22` → **412 passed, 1 skipped** (기존 411+1 skip, 무회귀).
- `PYTHONPATH=backend/tests backend/.venv/bin/python -m pytest -q backend/tests` → **4371 passed, 59 failed, 26 skipped** — 계획 단계 기준선(4370/59/26)에서 passed 만 신규 테스트 수만큼(+1) 증가, failed/skipped 불변. 실패 59건은 test_pipeline_geminic_wiring.py/test_pipeline_geminid_wiring.py/test_spike_gemini_moment_smoke.py 를 포함해 phase04/phase06/phase08/phase31/pipeline/gemini 관련 여러 파일에 분산되어 있으며(schema.py 와 무관한 기존 실패), `test_schema.py`/`test_assert_gates.py`(extract_report_json 의 유일한 간접 소비 테스트)는 실패 목록에 없음 — 회귀 0건 확인.
- `backend/.venv/bin/python .planning/quick/260816-e26-gate-parser-runaway/audit_reparse.py` → **exit 0**, 29건 전부 기대 분류(none 15/dict 14) 일치, mismatches=0.
- `rtk git diff -- backend/evals/phase22/run_bakeoff.py backend/evals/phase22/assert_gates.py` → **diff 0건**.

---
*Task: quick-260816-e26*
*Completed: 2026-08-16*

## Self-Check: PASSED

- FOUND: backend/training/datagen/schema.py
- FOUND: backend/tests/phase22/test_schema.py
- FOUND: .planning/quick/260816-e26-gate-parser-runaway/audit_reparse.py
- FOUND: .planning/quick/260816-e26-gate-parser-runaway/260816-e26-SUMMARY.md
- FOUND commit: fd283a58 (test — RED)
- FOUND commit: 14377c0f (fix — GREEN)
- FOUND commit: 34097ae3 (fix — audit script + result)
