---
phase: quick-260816-ill2
quick_id: 260816-ill2
slug: illustration-framing-wiring
date: 2026-08-16
status: planned
description: "일러스트 crop/orientation 배선 누락 수리 — build_prompt() 가 targets.json 의 cropBox·cropNote·orientation 을 0회 참조하던 것을 FRAMING/POSE FIDELITY 문단에 반영 + 08-16 기계 9항목 게이트를 재생성 루프에 통합(재시도 상한 3, Gemini 호출 ≤60회) + 반려 10장(구도 5·자세 5) 잃어버린 입력 프레임을 소스 영상에서 재확보·재생성 + belle 판정 재료 산출. 통과 9장(app/assets/illustrations)·backend/·app/src 전부 무접촉, 운영 반영은 belle 판정 후 별도 사이클"
wave: 1
depends_on: []
type: execute
plan: 01
autonomous: true
requirements: [QUICK-260816-ILL2]
files_modified:
  - .planning/quick/260809-ill-missing-illustrations/generate.py
  - .planning/quick/260809-ill-missing-illustrations/targets.json
  - .planning/quick/260809-ill-missing-illustrations/gen/
  - .planning/quick/260816-ill2-illustration-framing-wiring/extract_inputs.py
  - .planning/quick/260816-ill2-illustration-framing-wiring/regenerate_gated.py
  - .planning/quick/260816-ill2-illustration-framing-wiring/regen_verdicts.json
  - .planning/quick/260816-ill2-illustration-framing-wiring/board.html
  - .planning/quick/260816-ill2-illustration-framing-wiring/260816-ill2-SUMMARY.md
must_haves:
  truths:
    - "generate.py 의 build_prompt() 가 cropBox 또는 cropNote 가 있는 타깃에는 부분 프레이밍 문장을 보내고, 둘 다 없는 타깃에는 기존 'FILL the frame' 문구를 글자 단위 그대로 보낸다 — API 호출 0 인 순수함수 검증으로 증명된다"
    - "orientation 필드가 '도립'/'직립' 으로 시작하는 타깃은 POSE FIDELITY 블록에 INVERTED/UPRIGHT 단언 문장이 추가되고, 필드가 없는 타깃은 문장이 전혀 삽입되지 않는다"
    - "08-16 반려 10장(ref-combo--leg, ref-elbow-twist-sister--shoulder, ref-kip-up, ref-kip-up--shoulder, ref-pdshape--arm, ref-pdshape--leg, ref-peter-pan--arm, ref-peter-pan--leg, ref-peter-pan--shoulder, ref-power-spin) 전부 소실된 입력 프레임이 소스 영상에서 재추출되고, 새 배선으로 재생성이 시도되며, 오늘과 동일한 9항목 기계 게이트로 재판정된다(자산당 재시도 상한 3회, 통과 즉시 종료)"
    - "게이트를 끝내 통과하지 못한 자산은 마지막 시도 그대로 '미통과' 로 재료에 남는다 — 덮어쓰거나 통과로 기록하지 않는다"
    - "통과 9장(ref-climb, ref-elbow-twist-sister--arm, ref-elbow-twist-sister--leg, ref-foxtop-split, ref-foxtop, ref-invert, ref-pdshape--shoulder, ref-power-spin--shoulder, ref-sideway-spin--leg)의 app/assets/illustrations/ 파일이 sha256 기준 완전히 무변경이다"
    - "belle 이 /Users/Shared/ 의 한글 자료만 열어 재생성 10장의 이전/이후 이미지와 9항목 게이트 판정 원문(항목별 pass/fail+사유)을 대조할 수 있다 — 반영 여부는 이 사이클에서 결정되지 않는다"
    - "backend/shared·backend/functions·app/src 는 git status 기준 전혀 손대지 않고, pytest 기준선(4398 passed/59 failed/26 skipped, failed<=59) 이 유지된다"
  artifacts:
    - path: ".planning/quick/260809-ill-missing-illustrations/generate.py"
      provides: "build_prompt() 가 cropBox/cropNote/orientation 을 FRAMING/POSE FIDELITY 에 반영 — 동작명 분기 0, 신규 패키지 0"
      contains: "FRAMING_PARTIAL"
    - path: ".planning/quick/260809-ill-missing-illustrations/targets.json"
      provides: "반려 10장의 inputFrame 재확보 경로 + cropNote 2건/orientation 1건 신설 + 10건 sourceVideo·t 백필, 전부 quick-260816-ill2 인용"
      contains: "quick-260816-ill2"
    - path: ".planning/quick/260816-ill2-illustration-framing-wiring/extract_inputs.py"
      provides: "S3 read-only 다운로드 + 원본 해상도 프레임 추출 + 기록된 cropBox 적용 — 동작명 분기 0(RAW_FRAMES/OUTPUTS/TARGET_MAP 딕셔너리 순회만)"
    - path: ".planning/quick/260816-ill2-illustration-framing-wiring/regenerate_gated.py"
      provides: "generate.py 재사용 재생성 + gemini-3.5-flash 9항목 게이트 내장 재시도 루프(상한 3) — 호출 ≤60회"
      contains: "MAX_ATTEMPTS"
    - path: ".planning/quick/260816-ill2-illustration-framing-wiring/regen_verdicts.json"
      provides: "10 자산 × attempt 별 원 게이트 응답 전문 + finalStatus"
    - path: ".planning/quick/260816-ill2-illustration-framing-wiring/board.html"
      provides: "10 자산 이전/이후 base64 embed 비교 + 게이트 판정 원문"
    - path: "/Users/Shared/sunity-illustration-260816-ill2/"
      provides: "belle 열람용 한글 파일명 이전/이후 자료 + 판정요청.md"
    - path: ".planning/quick/260816-ill2-illustration-framing-wiring/260816-ill2-SUMMARY.md"
      provides: "기계 판정 요약 + LLM 학습 영향(호출 건수) + 미통과 자산 정직 보고 + 다음 단계"
  key_links:
    - from: ".planning/quick/260809-ill-missing-illustrations/generate.py"
      to: ".planning/quick/260809-ill-missing-illustrations/targets.json"
      via: "build_prompt() 의 row.get('cropBox')/row.get('cropNote')/row.get('orientation') 읽기"
      pattern: "row\\.get\\(.(cropBox|cropNote|orientation)."
    - from: ".planning/quick/260816-ill2-illustration-framing-wiring/regenerate_gated.py"
      to: ".planning/quick/260809-ill-missing-illustrations/generate.py"
      via: "importlib.util.spec_from_file_location 로 build_prompt/generate/load_targets/asset_name 재사용"
      pattern: "spec_from_file_location"
    - from: ".planning/quick/260816-ill2-illustration-framing-wiring/regenerate_gated.py"
      to: "Gemini generateContent API"
      via: "gemini-3-pro-image(이미지 생성, generate.py 기존 경로) + gemini-3.5-flash(9항목 판정, raw urllib REST 신규 작성)"
      pattern: "generateContent"
    - from: ".planning/quick/260816-ill2-illustration-framing-wiring/extract_inputs.py"
      to: ".planning/quick/260814-ehz-5/discover_sweep.py"
      via: "importlib 로 _s3_client() 재사용(sys.path 삽입) — S3 read-only"
      pattern: "_s3_client"
---

<objective>
belle 2026-08-16: "일러스트는 실패해서 못 쓴 것이다. 수리분 또한 마지막까지 엉망이었다."
+ "이제 해내야만 한다." 오늘 실측(재조사 불요, 이 사이클은 그대로 사용) — 현 자산 19장을
9항목 기계 게이트로 전수 선별한 결과 **9장 통과 / 10장 반려**, 반려 전건이 ②"자세 충실"
한 항목에서만 걸렸다. 근본 원인은 모델 품질이 아니라 배선 누락이다:
`build_prompt()` 가 실제로 치환하는 것은 `pose`·`guide` 둘뿐이고, `targets.json` 에 있는
`cropBox`/`cropNote`(장별 부분 프레이밍 의도)와 `orientation`(도립/직립)은 generate.py
에 0회 등장한다. 대신 모든 생성에 고정 문구 "FRAMING: ... The figure must FILL the
frame"가 나가, 부분 클로즈업을 요구하는 promptPose 의도와 정면으로 모순된다 — 구도 실패
5건의 직접 원인. orientation 미배선은 자세 실패 5건 중 방위(도립) 축 실패의 자리다.

이 사이클은 그 배선을 잇고, 사람 눈이 아니라 오늘 확립한 9항목 기계 게이트를 재생성
루프에 내장해 반려 10장을 재시도한다(belle: "사람 눈 대신 이 게이트를 통과선으로
쓴다"). 계획 단계에서 추가로 발견한 사실 — 반려 10장의 원본 입력 프레임(실사 인물
사진)은 전부 지난 세션의 scratchpad(session `6d09763a-...`)에 있었는데 그 디렉터리
자체가 통째로 사라졌다(scratchpad = 휘발, memory `feedback-scratchpad-volatile-
never-claim-preserved`). 따라서 재생성 전에 **입력 프레임을 소스 영상에서 재확보하는
단계**가 선행돼야 한다 — 이것도 이 계획의 범위에 포함한다(빠지면 재생성 자체가 불가능).

Purpose: 사람이 몽타주로 훑어보고 통과시켰던 08-09 라운드의 실패를 반복하지 않는다 —
①크롭/방위 배선이라는 뿌리 원인을 코드로 고치고, ②판정을 기계 게이트로 일관되게
적용해, ③belle 이 최종 반영 여부를 판단할 정직한 재료(통과/미통과 그대로)를 만든다.

Output: generate.py 배선 수리 + targets.json 데이터 보강(3건 신규 필드 + 10건 백필) +
extract_inputs.py(입력 프레임 재확보) + regenerate_gated.py(게이트 내장 재생성, 반려
10장 실행) + regen_verdicts.json + board.html + /Users/Shared 한글 재료 + SUMMARY.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260809-ill-missing-illustrations/generate.py
@.planning/quick/260809-ill-missing-illustrations/targets.json
@.planning/quick/260809-ill-missing-illustrations/260809-ill-SUMMARY.md
@.planning/quick/260814-ehz-5/discover_sweep.py

배경 (플래너 2026-08-16 실측 — 재파생 불필요, 그대로 사용):

- **오늘의 9항목 게이트 원문과 판정 결과가 이 사이클의 지반이다.** 게이트 소스 =
  `/private/tmp/claude-501/-Users-kimtaesung-Dev-SunityMotion/4cd70dc3-9461-4b35-bf25-
  b2405ed6c7e7/scratchpad/ill_screen.py`(참고 구현, scratchpad 라 휘발 — 아래 원문을
  regenerate_gated.py 에 그대로 옮겨 레포에 박제할 것), 결과 =
  같은 디렉터리 `ill_screen.json`(19 자산 전수 판정, 이미 이 세션에서 열람 완료).
  게이트 프롬프트 원문(한 글자도 바꾸지 말 것 — 오늘 결과와의 연속성이 근거):
  "이 그림은 폴스포츠 수강생에게 \"이렇게 되어야 한다\"는 목표 자세를 보여주는
  일러스트입니다.\n아래 9개 항목을 각각 pass/fail 로 판정하고, fail 이면 이유를 한
  문장으로. 추측 말고 보이는 것만.\n\n1 익명: 얼굴 이목구비가 드러나지 않는가(머리카락은
  있어야 함)\n2 자세 충실: 아래 의도한 자세와 방위(도립/직립)·형태가 맞는가   <- 가장
  중요\n3 가이드 표시(빨간 원/선)가 의도한 부위에 있는가\n4 사지 개수: 팔 2·다리 1~2 로
  셀 수 있는가(여분 팔다리 없음)\n5 머리카락이 있는가(민머리 아님)\n6 착의: 상의·하의를
  입고 있는가\n7 폴 1개·인물 1명인가\n8 목 방향이 자연스러운가\n9 사지 길이·비례가
  자연스러운가\n\nJSON 만 출력: {\"items\":{\"1\":{\"v\":\"pass|fail\",\"why\":\"\"},...,
  \"9\":{...}},\"overall\":\"pass|fail\",\"worst\":\"번호\"}" — 이 문자열 뒤에
  "\n\n의도한 자세: {promptPose}\n의도한 방위: {orientation 값 또는 '(미기재)'}\n가이드
  대상: {guideTarget 값 또는 '(미기재)'}" 를 이어붙여 이미지와 함께 전송한다(오늘
  ill_screen.py 와 동일 조합 방식).
- **반려 10장과 사유 (오늘 ill_screen.json 그대로, 재판정 불요 — 이 사이클의 출발점)**:
  | asset(targets.json motionId--part) | 배포 파일명(app/assets/illustrations) | 게이트 사유(② 원문) | 분류 |
  |---|---|---|---|
  | ref-combo--leg | ref-combo--leg.jpg | "머리가 프레임 안에 완전히 노출" | 구도 |
  | ref-pdshape--arm | ref-pdshape--arm.jpg | "몸통과 머리가 프레임 안에 드러남" | 구도 |
  | ref-peter-pan--arm | ref-peter-pan--arm.jpg | "상체와 엉덩이 등 다른 신체 부위 포함" | 구도 |
  | ref-peter-pan--leg | ref-peter-pan--leg.jpg | "전신이 모두 프레임 안에 포함" | 구도 |
  | ref-pdshape--leg | ref-pdshape--leg.jpg | "도립이 아닌 직립, 클로즈업 구도 아님" | 구도 |
  | ref-elbow-twist-sister--shoulder | ref-elbow-twist-sister--shoulder.jpg | "직립으로 서 있음, 머리가 프레임 가장자리 아님" | 자세(방위) |
  | ref-kip-up (bare) | ref-kip-up.jpg | "왼손이 폴을 못 잡고 공중으로" | 자세(그립) |
  | ref-kip-up--shoulder | ref-kip-up--shoulder.jpg | "한쪽 팔만 위로 뻗음" | 자세(그립) |
  | ref-peter-pan--shoulder | ref-peter-pan--shoulder.jpg | "매달림이 아니라 바닥에 직립" | 자세(행잉) |
  | ref-power-spin (bare, part=leg) | ref-power-spin.jpg | "수직 스플릿이 아닌 대각선" | 자세(각도) |
  이 중 **cropBox/cropNote 배선으로 직접 해소를 노리는 것 = 구도 5건 전부**,
  **orientation 배선으로 직접 해소를 노리는 것 = elbow-twist-sister--shoulder 1건뿐**
  (나머지 자세 4건은 그립/행잉/스플릿각 문제로 이번 배선 축과 무관 — 데이터를 억지로
  끼워맞추지 않는다. 배선 고도화 후에도 재시도 상한 안에서 통과 못 하면 "배선으로 해소
  안 됨"으로 정직하게 보고할 것, 무한 재시도·조작 금지).
- **통과 9장 — 절대 재생성하지 않는다** (`git status --porcelain app/assets/
  illustrations` 가 이 계획 종료 시점까지 빈 출력이어야 한다). sha256 기준선(오늘 실측):
  ref-climb=780af0bad53b8c5854c897dbd3813fa75c268d62f5047a4a110f3ee8782e2df4 ·
  ref-elbow-twist-sister--arm=d5ac75c14337a80f98dfef317a3a6bfe40262d1eb579f530a1ec3502
  8169c97a · ref-elbow-twist-sister--leg=9b2ae0e25896b9361cd325cfa86997056f89ef3d2c193
  c35e285fa1eded4db47 · ref-foxtop-split=1360c09fd4c44d28f764f6d370b196a9bf81c0a7549e871
  3959d692b067cb2ce · ref-foxtop=dd441904c7d361e3dc6afd48e23018ef85a8621cda266bba350ac5
  4238d34235 · ref-invert=241e924b6a55a2ac9da0a7fe043226a0a2c49eba7651b111c76abf1e29ce35
  94 · ref-pdshape--shoulder=4e0b7586e3daaa0ca726e4d22f3f9c9963d936001470bb654dfdddafd5
  ea8429 · ref-power-spin--shoulder=967a42076454bfb2822cb840f0688ebcabb5683463f838807146
  8950f567eefc · ref-sideway-spin--leg=20177bd5c547b0aeb96c49650a76d6f8d3561d56ba6c627a8
  c76df4349de1e50.
- **입력 프레임 소실 확인(오늘 실측)** — 10장이 참조하던 `inputFrame` 경로는 전부
  세션 `6d09763a-6cbc-4cbd-8d1c-208ec9f04c3e` 의 scratchpad 아래였고, 그 세션 디렉터리
  자체가 지금 존재하지 않는다(`ls` 로 확인, "No such file or directory"). 재현 정보는
  targets.json 의 `sourceVideo`/`t`/`cropBox` 뿐이며(scratchpad_note 규율), 아래 표가
  각 자산의 재확보 경로를 전부 확정한다 — **지어낸 값 0**:
  | targets.json (motionId, part) | 최종 t(s) | sourceVideo(S3, reference/{motionId}.mp4) | cropBox(있으면 그대로) | 근거 |
  |---|---|---|---|---|
  | ref-combo, leg | 33.0 | reference/ref-combo.mp4 | [180,620,740,1320] | 기존 기록 그대로(무변경 재추출) |
  | ref-peter-pan, leg | 2.4 | reference/ref-peter-pan.mp4 | [300,700,820,1340] | 기존 기록 그대로 |
  | ref-pdshape, leg | 5.0 | reference/ref-pdshape.mp4 | [360,400,780,920] | 기존 기록 그대로 |
  | ref-kip-up, leg(배포명 bare ref-kip-up.jpg) | 3.75 | reference/ref-kip-up.mp4 | 없음 | t 는 기존 기록, sourceVideo 는 `reference/{motionId}.mp4` 패턴 파생(4건의 기존 sourceVideo 기록이 전부 이 패턴 — 예외 0) |
  | ref-kip-up, shoulder | 3.75 | reference/ref-kip-up.mp4 | 없음 | leg 항목과 **동일 프레임 재사용**(같은 t, 크롭만 다름 — 실제로는 크롭도 없음) |
  | ref-power-spin, leg(배포명 bare ref-power-spin.jpg) | 8.5 | reference/ref-power-spin.mp4 | 없음 | t 는 기존 기록, sourceVideo 는 패턴 파생 |
  | ref-elbow-twist-sister, shoulder | 13.0 | reference/ref-elbow-twist-sister.mp4 | 없음 | **같은 동작의 이미 통과한 ref-elbow-twist-sister--arm 항목이 쓰는 바로 그 t=13.0 재사용**(그 항목은 오늘 게이트 pass). 오늘 `firestore_admin.get_reference_motion('ref-elbow-twist-sister')` 로 grounding 확인: `clipRange.execPeakS=13`, `checkpoints` 에 `left_shoulder`/`right_shoulder` 둘 다 weight 0.15 로 "hold 8초 핵심"/"도립+트위스트 유지의 주 지지점" — 이 순간이 양쪽 어깨가 가장 잘 보이는 순간이라는 근거가 이미 있다. **이 행에만 orientation="도립(등 뒤에서 본 두 견갑 — elbow-twist-sister--arm 과 동일 순간 t=13.0 재사용, clipRange.execPeakS=13/checkpoints 양쪽 어깨 weight 0.15 로 grounding, quick-260816-ill2)" 를 신설한다** |
  | ref-pdshape, arm | 5.0(1차) | reference/ref-pdshape.mp4 | 없음(무크롭 — 부분 프레이밍은 이제 FRAMING_PARTIAL 문장으로 전달) | **같은 동작 같은 hold 의 ref-pdshape--leg 항목과 동일 순간 t=5.0 재사용**(같은 정적 유지 구간). **이 행에 cropNote 신설**(아래 참조) |
  | ref-peter-pan, arm | 2.4(1차) | reference/ref-peter-pan.mp4 | 없음 | **ref-peter-pan--leg 항목과 동일 순간 t=2.4 재사용**. **이 행에 cropNote 신설** |
  | ref-peter-pan, shoulder | 2.4(1차) | reference/ref-peter-pan.mp4 | 없음 | ref-peter-pan--leg 항목과 동일 순간 t=2.4 재사용(자세 실패 원인은 행잉 vs 직립이지 프레이밍이 아니므로 cropNote 신설 없음) |
  cropNote 신설 2건 문구(그대로 사용): pdshape/arm — "quick-260816-ill2 — 08-16 게이트
  fail(② 몸통·머리 노출): promptPose 가 이미 '어깨는 프레임 가장자리, 나머지 몸은
  프레임 밖'을 지시하는데 generate.py 의 FRAMING 고정문(fill the frame)이 모순돼 매
  생성마다 전신이 그려졌다. 별도 pixel cropBox 는 없음 — 부분 프레이밍은 이제
  FRAMING_PARTIAL 문장으로 전달." / peter-pan/arm — 같은 문장에서 "promptPose 가 이미
  '한 팔만 프레임에, 다른 사지 없음'을 지시하는데"로 바꾼 것.
  "1차" 로 표시된 t 는 **재사용 후보** — Task 2 에서 추출한 프레임을 Read 로 열람해
  promptPose 서술과 맞지 않으면(전환 중 블러, 대상 관절 프레임 밖 등) 같은 창 안에서
  ±0.5s/±1.0s(pdshape 창 3.5~11.5s, peter-pan 창 2.0~6.0s 로 clamp)를 추가 추출해
  재선정한다(frames-before-numbers — 반드시 눈으로 확인 후 확정, memory
  `frames-before-numbers-gate`). 최종 선택 t 와 사유는 각 행에 `note10` 으로 남긴다.
- **generate.py 는 33-14/L-4 로 이미 승인된 레시피다 — GUIDE_SENTENCE 딕셔너리와
  ANONYMISATION/CLOTHING/SCENE/ANATOMY/STYLE 문단은 손대지 않는다.** 이번 수리는
  FRAMING 문단과 POSE FIDELITY 문단의 딱 한 문장에만 국한한다. `표준 라이브러리만
  사용 — 신규 패키지 0`, `동작명 조건 분기 0` 두 규율은 이번 수리에도 그대로 적용된다.
- **인증/환경**: `GEMINI_API_KEY` = SSM `/sunity/motion/gemini-api-key`(profile
  sunity-motion, region ap-northeast-2) — `discover_sweep.py::_ensure_gemini_key()`
  재사용(importlib, 값은 절대 로그하지 않음, 길이만 출력). S3 = `boto3.Session(
  profile_name="sunity-motion")`, region ap-northeast-2, 버킷
  `sunity-motion-pilot-videos` — `discover_sweep.py::_s3_client()` 재사용. 파이썬 =
  `backend/.venv/bin/python`(오늘 boto3/imageio/PIL/numpy 임포트 확인 완료, 신규 설치
  0). 생성 모델 = 기존 `gemini-3-pro-image`(generate.py 무변경 유지). 판정 모델 =
  `gemini-3.5-flash`, raw urllib REST(`generativelanguage.googleapis.com/v1beta/
  models/{MODEL}:generateContent`) — **google-genai SDK 는 쓰지 않는다**(오늘
  scratchpad 스크립트는 SDK 를 썼지만 그건 1회성 조사용이었고, 이 레포에 커밋되는
  신규 Gemini 호출 코드는 전부 raw urllib 관례를 따른다 — generate.py 자신의
  `generate()`, 그리고 오늘 커밋된 `.planning/quick/260816-p1x-pole-distance-axis/
  eyefirst_verify.py::eye_propose` 둘 다 이 관례).
- **호출 예산(명시)**: Task 3 = 반려 10장 × 재시도 상한 3 × (이미지 생성 1 + 게이트
  판정 1) = **이미지 생성 ≤30회 + 판정 ≤30회, 합계 ≤60회**. 통과 즉시 그 자산의 재시도는
  중단한다(상한까지 다 채우지 않음) — 실사용은 이보다 적을 가능성이 높다. 신규 pip
  설치 0, 신규 S3 업로드 0, Firestore 쓰기 0.
- 이모지 금지, 슬롭 금지. 새 코드 주석은 한국어로 "왜"를 남기고 quick-260816-ill2 와
  오늘 실측을 인용할 것(CLAUDE.md §7).
</context>

<tasks>

<task type="auto">
  <name>Task 1: FRAMING/POSE FIDELITY 배선 수리 + 순수함수 회귀 검증 (API 호출 0)</name>
  <files>.planning/quick/260809-ill-missing-illustrations/generate.py</files>
  <action>
    `generate.py` 를 수리한다. 모듈 docstring(1~19행) 끝에 짧은 날짜 부기 한 줄을
    추가한다 — "업데이트 2026-08-16 (quick-260816-ill2): cropBox/cropNote/orientation
    배선 추가. GUIDE_SENTENCE 및 그 외 문단은 무변경 — L-4 승인 레시피 유지." 기존
    문장은 한 글자도 지우지 않는다.

    GUIDE_SENTENCE 딕셔너리(45~74행) 바로 아래, PROMPT 상수(76행) 바로 위에 두 상수를
    신설한다. `FRAMING_FULL` = 현재 PROMPT 안의 FRAMING 문단 본문과 **글자·공백·줄바꿈
    단위로 완전히 동일한** 문자열 — "Portrait 3:4. The figure must FILL the frame -
    crop in close so the body occupies most of the image. Avoid large empty
    background areas; leave only a small margin around the figure." (지금 112~113행에
    있는 바로 그 문장). `FRAMING_PARTIAL` = "Portrait 3:4. This is an intentional
    PARTIAL crop showing only the body part described above in POSE FIDELITY. Only
    that part needs to be fully visible and readable. It is correct and expected
    for other body parts (head, opposite arm or leg, torso, etc.) to extend beyond
    the frame edges exactly as described above - do NOT pull back the camera or
    add empty background just to fit the whole body into the frame." 이어서
    `ORIENTATION_SENTENCE` 딕셔너리 — 키 `"inverted"` 값 "The body is INVERTED - the
    head is BELOW the hips, hanging upside-down from the pole. "(끝에 공백 1개), 키
    `"upright"` 값 "The body is UPRIGHT - the head is ABOVE the hips, not
    upside-down. "(끝에 공백 1개).

    두 헬퍼 함수를 신설한다(위치: PROMPT 상수 다음, build_prompt 앞). `_orientation_hint
    (raw: str | None) -> str` — `raw` 가 falsy(None 또는 빈 문자열)면 빈 문자열
    반환(회귀 0). `raw.startswith("도립")` 이면 `ORIENTATION_SENTENCE["inverted"]`,
    `raw.startswith("직립")` 이면 `ORIENTATION_SENTENCE["upright"]` 반환. 그 외
    접두(알 수 없는 값)는 빈 문자열(fail-closed — 지어내지 않는다). 동작명이 아니라
    문자열 접두사로만 갈린다 — `row['motionId']` 를 참조하는 분기를 넣지 않는다.
    `_framing_block(row: dict) -> str` — `row.get("cropBox") or row.get("cropNote")`
    가 truthy 면 `FRAMING_PARTIAL`, 아니면 `FRAMING_FULL` 반환.

    PROMPT 템플릿(76~117행)을 두 곳만 고친다. (1) POSE FIDELITY 문단(78~79행) —
    현재 "...FIRST image: {pose}. Keep the pole..." 를 "...FIRST image: {pose}.
    {orientation}Keep the pole..." 로(placeholder `{orientation}` 을 "{pose}. " 바로
    뒤, "Keep" 바로 앞에 공백 없이 삽입 — `_orientation_hint` 가 이미 필요한 앞뒤
    공백/문장부호를 포함해서 반환하므로 빈 문자열일 때 원문과 완전히 동일해야 한다).
    (2) FRAMING 문단(112~113행) — 현재 "FRAMING:\nPortrait 3:4. ..." 전체 본문을
    "FRAMING:\n{framing}" 으로 교체(고정 문구를 지우고 플레이스홀더만 남긴다).
    GUIDE_SENTENCE 딕셔너리, ANONYMISATION/CLOTHING/SCENE/ANATOMY/STYLE 문단은
    한 글자도 건드리지 않는다.

    `build_prompt(row: dict) -> str`(140~146행)을 갱신한다 — 시그니처는 그대로 유지.
    `PROMPT.format(...)` 호출에 기존 `pose=row["promptPose"]`,
    `guide=guide_tpl.format(target=row["promptTarget"])` 두 인자에 더해
    `orientation=_orientation_hint(row.get("orientation"))` 와
    `framing=_framing_block(row)` 두 인자를 추가한다.

    새 import 는 추가하지 않는다(표준 라이브러리 0 순증 — 기존 import 문 8줄
    무변경). `main()`/`generate()`/`resolve()`/`asset_name()`/`inline_part()`/
    `load_targets()` 는 전혀 건드리지 않는다.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && backend/.venv/bin/python -c "
import importlib.util
spec = importlib.util.spec_from_file_location('generate_recipe', '.planning/quick/260809-ill-missing-illustrations/generate.py')
gen = importlib.util.module_from_spec(spec)
spec.loader.exec_module(gen)

FRAMING_ORIG = ('Portrait 3:4. The figure must FILL the frame - crop in close so the body '
                'occupies most of the image. Avoid large empty background areas; leave only '
                'a small margin around the figure.')
base = {'guideType': 'angle', 'promptPose': 'TESTPOSE', 'promptTarget': 'TESTTARGET'}

p1 = gen.build_prompt(dict(base))
assert FRAMING_ORIG in p1, 'REGRESSION: 무크롭 타깃의 FRAMING 문구가 원문과 다름'
assert 'INVERTED' not in p1 and 'UPRIGHT' not in p1, 'orientation 미기재인데 단언 문장 삽입됨'

p2 = gen.build_prompt(dict(base, cropNote='why'))
assert FRAMING_ORIG not in p2, 'cropNote 신호가 FRAMING 에 반영되지 않음'
assert 'PARTIAL crop' in p2, 'cropNote 있는데 부분 프레이밍 문장이 없음'

p3 = gen.build_prompt(dict(base, cropBox=[0, 0, 1, 1]))
assert FRAMING_ORIG not in p3 and 'PARTIAL crop' in p3, 'cropBox 단독 신호가 반영되지 않음'

p4 = gen.build_prompt(dict(base, orientation='도립(설명)'))
assert 'INVERTED' in p4, '도립 orientation 이 단언 문장으로 반영되지 않음'
assert FRAMING_ORIG in p4, 'orientation 만 있는 타깃의 FRAMING 이 회귀되지 않음(무크롭인데 변형됨)'

p5 = gen.build_prompt(dict(base, orientation='직립(설명)'))
assert 'UPRIGHT' in p5, '직립 orientation 이 단언 문장으로 반영되지 않음'

p6 = gen.build_prompt(dict(base, cropBox=[0, 0, 1, 1], orientation='도립(설명)'))
assert 'PARTIAL crop' in p6 and 'INVERTED' in p6, '크롭+방위 동시 신호가 함께 반영되지 않음(독립성 실패)'

p7 = gen.build_prompt(dict(base, guideType='circle'))
assert 'ONE coral-red open ring' in p7, 'GUIDE_SENTENCE(승인 레시피) 가 훼손됨'

src = open('.planning/quick/260809-ill-missing-illustrations/generate.py').read()
import re
roots = set()
for m in re.finditer(r'^(?:import|from)\s+([A-Za-z_][A-Za-z0-9_]*)', src, re.M):
    roots.add(m.group(1))
allowed = {'__future__', 'argparse', 'base64', 'json', 'mimetypes', 'os', 'sys', 'urllib', 'pathlib'}
assert roots <= allowed, f'신규 패키지 의존 발견(신규 패키지 0 위반): {roots - allowed}'

print('TASK1 WIRING GATE PASS')
" && test -z "$(git status --porcelain backend/shared backend/functions app/src)"</automated>
  </verify>
  <done>
    build_prompt() 가 cropBox/cropNote 존재 여부로 FRAMING 문장을 바이트 단위로 갈라
    보내고(무존재=회귀 0), orientation 접두(도립/직립)로 POSE FIDELITY 단언 문장을
    독립적으로 삽입/생략한다. GUIDE_SENTENCE 등 나머지 승인 문단 무변경, 신규 import 0,
    금지 디렉터리(backend/shared·backend/functions·app/src) diff 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: 반려 10장 입력 프레임 재확보 + targets.json 데이터 보강</name>
  <files>.planning/quick/260816-ill2-illustration-framing-wiring/extract_inputs.py, .planning/quick/260809-ill-missing-illustrations/targets.json</files>
  <action>
    `extract_inputs.py` 를 신설한다. 상단에서 `sys.path.insert(0, str(Path(__file__)
    .resolve().parents[2] / ".planning/quick/260814-ehz-5"))` 후 `import
    discover_sweep as ds` 로 `ds._s3_client()`(및 존재하면 `ds.BUCKET`, 없으면 리터럴
    `"sunity-motion-pilot-videos"`)를 재사용한다 — S3 다운로드 로직을 새로 작성하지
    않는다. 실행 세션 scratchpad 하위에 `ill2/videos/`(원본 mp4 6편)와 `ill2/crop/`
    (추출된 입력 프레임)를 새로 만든다(`--cache-root` CLI 인자, 기본값 =
    스크립트 파일 기준 상대 `.cache` — discover_sweep.py `--cache-root` 관례와 동일,
    **이전 세션 UUID 하드코딩 금지**).

    데이터는 전부 딕셔너리 3개로 표현한다(동작명 리터럴 분기 금지 — `if motion ==`
    패턴 없이 순회만). `RAW_FRAMES`(6행) — 키=motionId, 값=(S3 키
    `f"reference/{motionId}.mp4"`, t초): ref-combo→33.0, ref-peter-pan→2.4,
    ref-pdshape→5.0, ref-kip-up→3.75, ref-power-spin→8.5,
    ref-elbow-twist-sister→13.0(§Context 표 그대로, 지어낸 값 0). `OUTPUTS`(8행) —
    키=산출 파일 스텀, 값=(RAW_FRAMES 키, cropBox 또는 None): "combo_leg"→
    (ref-combo,[180,620,740,1320]), "peterpan_leg"→(ref-peter-pan,[300,700,820,1340]),
    "peterpan_full"→(ref-peter-pan,None), "pdshape_leg"→(ref-pdshape,[360,400,780,920]),
    "pdshape_full"→(ref-pdshape,None), "kipup_full"→(ref-kip-up,None),
    "powerspin_full"→(ref-power-spin,None), "elbowtwist_shoulder_full"→
    (ref-elbow-twist-sister,None). `TARGET_MAP`(10행) — 키=(motionId,part), 값=OUTPUTS
    스텀: (ref-combo,leg)→combo_leg, (ref-peter-pan,leg)→peterpan_leg,
    (ref-peter-pan,arm)→peterpan_full, (ref-peter-pan,shoulder)→peterpan_full,
    (ref-pdshape,leg)→pdshape_leg, (ref-pdshape,arm)→pdshape_full,
    (ref-kip-up,leg)→kipup_full, (ref-kip-up,shoulder)→kipup_full,
    (ref-power-spin,leg)→powerspin_full, (ref-elbow-twist-sister,shoulder)→
    elbowtwist_shoulder_full.

    실행 순서: (1) `RAW_FRAMES` 의 6개 motionId 마다 S3 다운로드(캐시 히트 시 재다운로드
    생략) → `ill2/videos/{motionId}.mp4`. (2) `OUTPUTS` 8행마다: `imageio.get_reader
    (video_path)` 로 열어 `reader.get_meta_data()['fps']`(원본 네이티브 fps — 9fps
    솎음 파이프라인의 `effective_fps` 트랩과 무관, 이 프레임 추출은 원본 그대로
    단일 프레임을 집는 것이라 `frame_extractor.py` 의 디시메이션 로직을 재사용하지
    않는다)를 얻어 `frame_idx = round(t * fps)`, `reader.get_data(frame_idx)` 로
    RGB ndarray 를 얻고 `PIL.Image.fromarray(...)` 로 변환한다. cropBox 가 있으면
    `.crop(tuple(cropBox))`(순서 = left,top,right,bottom, PIL 표준)를 적용한다.
    JPEG quality 92 로 `ill2/crop/{스텀}.jpg` 저장, 콘솔에 `[{스텀}] t={t}s
    frame_idx={idx} fps={fps:.2f} size={img.size} → {경로}` 출력.

    (3) `pdshape_full`/`peterpan_full`(peter-pan 은 arm 과 shoulder 양쪽에 쓰이므로
    특히) 는 형제 항목(leg)의 hold 순간을 그대로 재사용한 **1차 후보**다 — 실행자는
    Read 도구로 `pdshape_full.jpg`·`peterpan_full.jpg`·`elbowtwist_shoulder_full.jpg`
    3장을 열람해 각 타깃의 promptTarget/promptPose(§Context 표 인용)와 맞는지 확인한다
    (frames-before-numbers, memory `frames-before-numbers-gate` — 반드시 프레임을
    본 뒤에만 확정). 명백히 안 맞으면(전환 중 블러, 대상 관절이 프레임 밖 등) 그 대상만
    `extract_inputs.py --window {motionId} --center {t} --span 1.5 --step 0.5`
    (신설 옵션 — 창을 clamp: pdshape [3.5,11.5], peter-pan [2.0,6.0])로 후보를 추가
    추출해 다시 Read 로 비교 후 최종 t 를 확정한다. 변경이 없으면(1차 후보로 확정)
    그 사실도 그대로 기록한다 — 억지로 바꾸지 않는다.

    (4) `targets.json` 을 갱신한다. 대상 10개 행(motionId,part 로 특정) 각각의
    `inputFrame` 을 `TARGET_MAP` 이 가리키는(3단계에서 재선정했다면 그 파일의)
    절대경로로 바꾼다. 10개 행 전부에 `sourceVideo`(§Context 표의 S3 URI, 형식은
    기존 4건과 동일하게 `"s3://sunity-motion-pilot-videos/reference/{motionId}.mp4"`)
    와 `t`(최종 확정값)를 없으면 신설, 있으면(이미 기록된 4행) 그대로 둔다. 다음
    2건에 `cropNote` 를 신설한다(§Context 의 정확한 문구 그대로) — (ref-pdshape,arm),
    (ref-peter-pan,arm). 다음 1건에 `orientation` 을 신설한다(§Context 문구 그대로) —
    (ref-elbow-twist-sister,shoulder). 수정한 10개 행 전부에 `note10` 을 추가해 이번
    사이클 인용 + 최종 확정 t 와 재사용/재선정 사유 한 줄을 남긴다(quick-260816-ill2
    인용 필수). **cropBox 는 기존 4건(combo/leg, peter-pan/leg, pdshape/leg — 이미
    3건 있음) 외에 새로 만들지 않는다** — 나머지 6건은 무크롭으로 남긴다(§Context
    설계 그대로). 다른 12개 행(반려 10장 밖)은 전혀 건드리지 않는다.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && backend/.venv/bin/python -c "
import json, pathlib
t = json.loads(pathlib.Path('.planning/quick/260809-ill-missing-illustrations/targets.json').read_text())
rows = {(r['motionId'], r['part']): r for r in t['targets']}
targets10 = [('ref-combo','leg'),('ref-elbow-twist-sister','shoulder'),('ref-kip-up','leg'),
             ('ref-kip-up','shoulder'),('ref-pdshape','arm'),('ref-pdshape','leg'),
             ('ref-peter-pan','arm'),('ref-peter-pan','leg'),('ref-peter-pan','shoulder'),
             ('ref-power-spin','leg')]
fails = []
for key in targets10:
    r = rows.get(key)
    if r is None:
        fails.append(f'{key} 행 부재'); continue
    p = pathlib.Path(r.get('inputFrame') or '')
    if not p.is_file():
        fails.append(f'{key} inputFrame 실물 부재: {p}')
    if not r.get('sourceVideo') or 't' not in r:
        fails.append(f'{key} sourceVideo/t 백필 누락')
    if 'quick-260816-ill2' not in json.dumps(r, ensure_ascii=False):
        fails.append(f'{key} quick-260816-ill2 인용 없음(note10 누락)')
cn = rows[('ref-pdshape','arm')].get('cropNote') and rows[('ref-peter-pan','arm')].get('cropNote')
if not cn:
    fails.append('cropNote 2건 신설 누락')
if not (rows[('ref-elbow-twist-sister','shoulder')].get('orientation') or '').startswith('도립'):
    fails.append('elbow-twist-sister--shoulder orientation 신설 누락 또는 형식 오류')
other12 = [k for k in rows if k not in targets10]
if len(other12) != 9:
    fails.append(f'targets.json 총 행 수 이상(반려10 밖 행 = {len(other12)}, 기대 9)')
msg = 'FAIL: ' + '; '.join(fails) if fails else 'TASK2 DATA GATE PASS'
print(msg)
raise SystemExit(1 if fails else 0)
" && test -z "$(git status --porcelain backend/shared backend/functions app/src app/assets/illustrations)"</automated>
  </verify>
  <done>
    반려 10장 전부 inputFrame 이 소스 영상 재추출 실물을 가리키고, sourceVideo/t 가
    10건 백필되고, cropNote 2건(pdshape/arm, peter-pan/arm)·orientation 1건
    (elbow-twist-sister/shoulder)이 신설되며, 나머지 9행은 무변경, 금지 디렉터리
    (backend/shared·backend/functions·app/src·app/assets/illustrations) diff 0.
  </done>
</task>

<task type="auto">
  <name>Task 3: 게이트 내장 재생성 드라이버 + 반려 10장 실행 (재시도 상한 3, Gemini 호출 ≤60회)</name>
  <files>.planning/quick/260816-ill2-illustration-framing-wiring/regenerate_gated.py, .planning/quick/260809-ill-missing-illustrations/gen/, .planning/quick/260816-ill2-illustration-framing-wiring/regen_verdicts.json</files>
  <action>
    `regenerate_gated.py` 를 신설한다. 상단에서 `importlib.util.spec_from_file_location`
    로 `.planning/quick/260809-ill-missing-illustrations/generate.py` 를 모듈
    `gen` 으로 로드해 `gen.load_targets`/`gen.generate`/`gen.asset_name`/
    `gen.inline_part`/`gen.MODEL`/`gen.ENDPOINT` 를 재사용한다(새 생성 로직을
    작성하지 않는다 — Task 1 이 고친 build_prompt() 를 `gen.generate()` 내부에서
    그대로 타므로 이 파일은 build_prompt 를 직접 호출하지 않는다). 이어서
    `sys.path.insert(0, ...)` 로 `.planning/quick/260814-ehz-5/discover_sweep.py`
    를 `ds` 로 임포트해 `ds._ensure_gemini_key()` 를 재사용한다(SSM 조회 로직을
    새로 작성하지 않는다, 키 값은 절대 출력하지 않는다).

    모듈 상수: `TARGET_KEYS`(순서 고정 10행, Task 2 검증에 쓴 동일 리스트) =
    (ref-combo,leg), (ref-elbow-twist-sister,shoulder), (ref-kip-up,leg),
    (ref-kip-up,shoulder), (ref-pdshape,arm), (ref-pdshape,leg),
    (ref-peter-pan,arm), (ref-peter-pan,leg), (ref-peter-pan,shoulder),
    (ref-power-spin,leg). `MAX_ATTEMPTS = 3`. `ATTEMPT_BASE = 900`(gen/ 안의
    기존 try1~21 이력과 충돌하지 않는 네임스페이스 — try901~903 형태로 저장되게).
    `GATE_MODEL = "gemini-3.5-flash"`. `GATE_ENDPOINT` 는 `gen.ENDPOINT` 와 같은
    패턴으로 모델명만 `GATE_MODEL` 로 바꿔 구성한다. `GATE_PROMPT` = §Context 에
    인용한 9항목 게이트 원문을 **한 글자도 바꾸지 않고** 그대로 옮긴다(오늘
    ill_screen.py 와 동일 문자열이어야 판정 연속성이 성립한다).

    `gate_judge(image_path: Path, row: dict) -> dict` 신설 — intent 문자열을
    `f"의도한 자세: {row['promptPose'][:400]}\n의도한 방위: {row.get('orientation') or '(미기재)'}\n가이드 대상: {row.get('guideTarget') or '(미기재)'}"`
    로 조립하고 `GATE_PROMPT + "\n\n" + intent` 를 텍스트 파트로, `gen.inline_part
    (image_path)` 를 이미지 파트로 담아 `urllib.request` 로 POST 한다(
    `generate()`(153~201행)와 같은 요청/타임아웃/에러 처리 패턴, `generationConfig`
    에 `responseMimeType: "application/json"` 추가). 응답 텍스트에서
    `re.search(r'\{.*\}', text, re.S)` 로 JSON 블록을 추출해 `json.loads` (오늘
    ill_screen.py 와 동일 파싱). **어떤 예외(HTTPError/URLError/JSONDecodeError/
    기타)도 이 함수 밖으로 내보내지 않는다** — 잡아서
    `{"overall": "fail", "worst": "?", "error": f"{type(e).__name__}: {e}"}` 를
    반환한다(배치 전체를 죽이지 않기 위함, generate() 의 `sys.exit` 와 달리 이
    함수는 절대 `sys.exit` 를 호출하지 않는다).

    메인 루프: `gen.load_targets()` 로 최신 targets.json(Task 2 반영분)을 읽어
    `(motionId,part)` 로 인덱싱한다. `TARGET_KEYS` 순서대로 각 자산을 처리한다.
    자산마다 `attempts=[]`, `finalStatus="still_failing"`, `chosenFile=None` 으로
    시작해 `n=1..MAX_ATTEMPTS`: `attempt_num = ATTEMPT_BASE + n`. `gen.generate
    (row, attempt_num, None)` 을 **`try: ... except (SystemExit, Exception) as e:`**
    로 감싼다 — `generate()` 는 실패 시 `sys.exit(msg)` 를 호출하므로(153~201행),
    `SystemExit` 를 명시적으로 잡지 않으면 배치 전체가 죽는다(bare
    `except Exception` 만으로는 `SystemExit` 를 못 잡는다). 실패 시
    `attempts.append({"attempt":attempt_num,"status":"generate_error",
    "error":str(e)})` 후 다음 attempt 로. 성공하면 `gate_judge(outpath, row)`
    호출 → `attempts.append({"attempt":attempt_num,"file":str(outpath.relative_to
    (REPO)),"gate":gate_result})` → 콘솔에
    `f"[{motionId}--{part}] try{attempt_num} → {gate_result.get('overall','?')}"`
    출력(flush=True). `gate_result.get("overall") == "pass"` 면 `finalStatus="pass"`,
    `chosenFile=` 이 파일, **그 자산의 재시도 루프를 즉시 중단**(다음 attempt 로
    넘어가지 않는다 — 상한을 다 채우지 않는다). 자산 처리가 끝날 때마다(성공이든
    3회 소진이든) 누적 결과를 `regen_verdicts.json` 에 즉시 저장한다(자산 하나
    끝날 때마다 덮어쓰기 — 중간에 죽어도 이전 자산 결과가 남게, ill_screen.py 의
    incremental-save 관례와 동일). 스키마: `{"meta":{"generatedAt":ISO시각,
    "maxAttempts":3,"attemptBase":900,"genModel":gen.MODEL,"gateModel":GATE_MODEL},
    "assets":{"{motionId}--{part}":{"finalStatus":...,"chosenFile":...,
    "attempts":[...]}}}`. 전부 끝나면 콘솔에 `f"DONE pass={n_pass}
    still_failing={n_fail} totalAttempts={n_attempts}"` 출력.

    CLI: `--asset` 옵션(선택, `"{motionId}--{part}"` 형식 1건만 재실행 — 상한 소진
    후 재시도할 때 전체를 다시 돌리지 않기 위함, 기본값은 `TARGET_KEYS` 전체).

    금지: `build_prompt()` 재구현(반드시 `gen.generate()` 경유 — build_prompt 를
    직접 호출하면 Task 1 수리분과 별개의 경로가 생겨 "분기 0" 원칙 위반), 동작명
    리터럴 분기, `gate_judge` 내부에서의 `sys.exit` 호출, 통과한 자산의 재시도
    계속(즉시 break 안 하면 예산 초과).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && backend/.venv/bin/python -c "
import json, pathlib
v = json.loads(pathlib.Path('.planning/quick/260816-ill2-illustration-framing-wiring/regen_verdicts.json').read_text())
assets = v.get('assets') or {}
expected = {'ref-combo--leg','ref-elbow-twist-sister--shoulder','ref-kip-up--leg',
            'ref-kip-up--shoulder','ref-pdshape--arm','ref-pdshape--leg',
            'ref-peter-pan--arm','ref-peter-pan--leg','ref-peter-pan--shoulder',
            'ref-power-spin--leg'}
fails = []
missing = expected - set(assets.keys())
if missing:
    fails.append(f'자산 누락: {missing}')
total_attempts = 0
n_pass = 0
for key, rec in assets.items():
    atts = rec.get('attempts') or []
    total_attempts += len(atts)
    if not (1 <= len(atts) <= 3):
        fails.append(f'{key} attempt 수 이상: {len(atts)}')
    fs = rec.get('finalStatus')
    if fs not in ('pass', 'still_failing'):
        fails.append(f'{key} finalStatus 이상: {fs}')
    if fs == 'pass':
        n_pass += 1
        last = atts[-1] if atts else {}
        if (last.get('gate') or {}).get('overall') != 'pass':
            fails.append(f'{key} finalStatus=pass 인데 마지막 attempt 게이트가 pass 아님')
        if not rec.get('chosenFile'):
            fails.append(f'{key} pass 인데 chosenFile 없음')
if total_attempts > 30:
    fails.append(f'생성 시도 수 {total_attempts} > 30 상한')
msg = 'FAIL: ' + '; '.join(fails) if fails else f'TASK3 GATE PASS (pass={n_pass}/10, totalAttempts={total_attempts})'
print(msg)
raise SystemExit(1 if fails else 0)
" && test -z "$(git status --porcelain backend/shared backend/functions app/src app/assets/illustrations)"</automated>
  </verify>
  <done>
    regen_verdicts.json 에 10 자산 전부 attempt별 원 게이트 응답(1~3회, 합계 ≤30 생성
    +≤30 판정) 이 기록되고, pass 로 표시된 자산은 마지막 attempt 의 게이트가 실제로
    pass 이며 chosenFile 이 존재한다. 통과 못 한 자산은 still_failing 으로 정직하게
    남는다. 금지 디렉터리 diff 0.
  </done>
</task>

<task type="auto">
  <name>Task 4: belle 판정 재료 산출 + 검증 스위트 + SUMMARY</name>
  <files>.planning/quick/260816-ill2-illustration-framing-wiring/board.html, .planning/quick/260816-ill2-illustration-framing-wiring/260816-ill2-SUMMARY.md</files>
  <action>
    (1) `board.html` 신설 — 10 자산 각각 이전(§Context "배포 파일명" 표가 가리키는
    `app/assets/illustrations/{X}.jpg`, base64 data URI embed) / 이후
    (regen_verdicts.json 의 `chosenFile` — pass 면 그 파일, still_failing 이면 마지막
    attempt 의 파일, base64 data URI embed) 를 좌우로 배치한다(memory
    `이미지 전달은 보드 embed 만 확실` — 링크가 아니라 반드시 data URI 로 인라인).
    자산마다: motionId--part, 배포 파일명, 최종 상태(통과/미통과 — 한글, 이모지
    금지), 오늘 08-16 게이트 사유(§Context "게이트 사유(② 원문)" 표 인용, 이전
    이미지 아래), 재생성 게이트 원문(regen_verdicts.json 의 마지막 attempt
    `gate.items` 중 `v:"fail"` 인 항목의 `why` 전부, 이후 이미지 아래 — 전부
    pass 면 "9항목 전부 pass"). 미통과 자산은 카드 상단에 눈에 띄게
    "미통과 — 이 배선으로 해소되지 않음" 을 표기한다(숨기지 않는다).

    (2) `/Users/Shared/sunity-illustration-260816-ill2/` 신설 — 10 자산의 이전/이후
    jpg 를 한글 파일명으로 복사한다(예: "콤보_다리_이전.jpg"/"콤보_다리_이후.jpg",
    "피터팬_어깨_이전.jpg"/"..._이후.jpg" 형태 — 동작명 한글 매핑: combo=콤보,
    pdshape=피디셰이프, peter-pan=피터팬, kip-up=킵업, elbow-twist-sister=엘보트위스트,
    power-spin=파워스핀; 부위 한글: leg=다리, arm=팔, shoulder=어깨). 숫자 각도
    라벨은 파일명·캡션 어디에도 넣지 않는다(D-09 계열 규율 승계). `판정요청.md`
    신설(한국어, 이모지 0) — 10 자산 표(상태/게이트 사유 원문 인용) + belle 에게
    묻는 질문 한 줄("이 중 통과한 자산을 app/assets/illustrations/ 에 반영해도
    좋은지 — 미통과분은 별도 처리 방안이 필요합니다") + 열람 순서 안내.

    (3) 검증 스위트 실행 — `PYTHONPATH=backend/tests backend/.venv/bin/python -m
    pytest -q backend/tests` 로 실패 수 확인(기준선 59 이하), 통과 9장 sha256 가
    §Context 기준선 표와 전부 일치하는지 확인, `git status --porcelain
    backend/shared backend/functions app/src app/assets/illustrations` 빈 출력
    확인. 이 세 게이트 전부 통과해야 다음 단계로 진행한다 — 하나라도 실패하면
    STOP 하고 원인을 그대로 보고한다(조용히 다음 태스크로 넘어가지 않는다).

    (4) `260816-ill2-SUMMARY.md` 신설 — 기계 판정 요약 표(10 자산: 이전 상태(전부
    fail로 시작) / 이후 finalStatus / 사용한 attempt 수), **LLM 학습 영향 필수
    기재**(Gemini 실호출 총 수 = regen_verdicts.json 실측치 그대로, 이미지 생성
    ≤30 + 판정 ≤30, 모델 gemini-3-pro-image/gemini-3.5-flash, 추론 호출만·학습
    데이터 전송 0), 미통과 자산이 있으면 정직하게 나열 + 사유(배선으로 해소되는
    축이 아니었음을 §Context 분류 인용해 명시), pytest/해시 게이트 결과, board.html
    과 /Users/Shared 절대경로, 다음 단계("belle 판정 → 승인분만 app/assets/
    illustrations 반영 + VERIFIED_ILLUSTRATIONS/ILLUSTRATION_SCENES 배선은 별도
    사이클 — 이 사이클은 app/assets/illustrations·app/src 무접촉으로 종료").
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && python3 -c "
import hashlib, pathlib
baseline = {
 'ref-climb': '780af0bad53b8c5854c897dbd3813fa75c268d62f5047a4a110f3ee8782e2df4',
 'ref-elbow-twist-sister--arm': 'd5ac75c14337a80f98dfef317a3a6bfe40262d1eb579f530a1ec35028169c97a',
 'ref-elbow-twist-sister--leg': '9b2ae0e25896b9361cd325cfa86997056f89ef3d2c193c35e285fa1eded4db47',
 'ref-foxtop-split': '1360c09fd4c44d28f764f6d370b196a9bf81c0a7549e8713959d692b067cb2ce',
 'ref-foxtop': 'dd441904c7d361e3dc6afd48e23018ef85a8621cda266bba350ac54238d34235',
 'ref-invert': '241e924b6a55a2ac9da0a7fe043226a0a2c49eba7651b111c76abf1e29ce3594',
 'ref-pdshape--shoulder': '4e0b7586e3daaa0ca726e4d22f3f9c9963d936001470bb654dfdddafd5ea8429',
 'ref-power-spin--shoulder': '967a42076454bfb2822cb840f0688ebcabb5683463f8388071468950f567eefc',
 'ref-sideway-spin--leg': '20177bd5c547b0aeb96c49650a76d6f8d3561d56ba6c627a8c76df4349de1e50',
}
fails = []
for name, want in baseline.items():
    p = pathlib.Path(f'app/assets/illustrations/{name}.jpg')
    got = hashlib.sha256(p.read_bytes()).hexdigest() if p.is_file() else 'MISSING'
    if got != want:
        fails.append(f'{name}: {got} != {want}')
if fails:
    print('FAIL: 통과 9장 무변경 위반 —', '; '.join(fails)); raise SystemExit(1)
print('PASS-9 HASH GATE OK (9/9 byte-identical)')
" && test -f .planning/quick/260816-ill2-illustration-framing-wiring/board.html && test -f .planning/quick/260816-ill2-illustration-framing-wiring/260816-ill2-SUMMARY.md && grep -qi "LLM 학습" .planning/quick/260816-ill2-illustration-framing-wiring/260816-ill2-SUMMARY.md && ls /Users/Shared/sunity-illustration-260816-ill2/ | wc -l | awk '{exit !($1>=3)}' && FAILCOUNT=$(PYTHONPATH=backend/tests backend/.venv/bin/python -m pytest -q backend/tests 2>&1 | grep -oE '[0-9]+ failed' | grep -oE '[0-9]+' || echo 0) && echo "pytest failed=$FAILCOUNT (기준선 <=59)" && [ "$FAILCOUNT" -le 59 ] && test -z "$(git status --porcelain backend/shared backend/functions app/src app/assets/illustrations)"</automated>
  </verify>
  <done>
    board.html 에 10 자산 이전/이후 embed + 게이트 사유 원문, /Users/Shared 에 한글
    재료 + 판정요청.md, SUMMARY 에 LLM 학습 영향 + 미통과 정직 보고 + 다음 단계 명시,
    통과 9장 sha256 9/9 무변경, pytest failed&lt;=59, 금지 디렉터리 diff 0 확인 후
    커밋 완료.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 실행자 → Gemini API(gemini-3-pro-image) | reference 영상에서 추출한 얼굴 없는 전문 선수 정지 프레임을 이미지 생성 요청 첫 참조로 전송 — 33-14/L-4 로 이미 승인된 것과 동일 성격의 입력, 신규 위험 아님 |
| 실행자 → Gemini API(gemini-3.5-flash) | 생성 시도 결과(익명화 시도된 일러스트)를 9항목 판정 요청에 전송 |
| 실행자 → S3 | reference 영상 6편 read-only 다운로드(`sunity-motion-pilot-videos` 버킷) |
| 실행자 → 로컬 공유 레시피 | `generate.py`/`targets.json` 편집 — 33-14 승인 레시피의 비-잠금 구역(FRAMING/POSE FIDELITY 문단, 데이터 필드)만 |
| 실행자 → /Users/Shared | belle 판정용 자료 쓰기(레포 밖, PII 아닌 익명 일러스트만) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ill2-01 | Information Disclosure | Gemini generateContent(이미지 생성 입력) | accept | 33-14/L-4 로 이미 승인된 것과 동일 성격(얼굴 없는 선수 정지 프레임) — 이 사이클이 새로 만든 위험이 아님, 추론 호출만·학습 데이터 전송 0, SUMMARY 에 호출 규모 명기 |
| T-ill2-02 | Tampering | S3 다운로드 | mitigate | `discover_sweep.py::_s3_client()` 재사용, `download_file` 만 호출 — 이 계획 어디에도 `put_object`/`upload_file` 코드 없음 |
| T-ill2-03 | Tampering | app/assets/illustrations, backend/shared, backend/functions, app/src | mitigate | 전 태스크 공통 게이트 `git status --porcelain` 로 매 태스크 종료 시 기계 검증, Task 4 에서 sha256 9/9 별도 재확인 |
| T-ill2-04 | Repudiation | 재생성 attempt 이력 | mitigate | regen_verdicts.json 에 attempt 별 원 게이트 응답 전문을 자산 처리 직후 즉시 저장(incremental save) — 사후 재현·감사 가능 |
| T-ill2-05 | Information Disclosure | GEMINI_API_KEY(SSM) | mitigate | `discover_sweep.py::_ensure_gemini_key()` 재사용 — 키 값은 절대 로그하지 않음(길이만 출력) |
| T-ill2-SC | Tampering | 패키지 설치 | accept | 신규 pip 설치 0 — boto3/imageio/PIL/numpy 전부 backend/.venv 기존 패키지, google-genai SDK 미사용(raw urllib REST 로 generate.py 관례 승계) |
</threat_model>

<verification>
- 금지 디렉터리 무접촉: 전 태스크 공통 `test -z "$(git status --porcelain backend/shared backend/functions app/src app/assets/illustrations)"`
- Task 1 순수함수 게이트: cropBox/cropNote 유무 → FRAMING 문구 분기(무신호=원문 바이트 동일), orientation 접두 → POSE FIDELITY 단언 문장 독립 삽입, 신규 import 0
- Task 2 데이터 게이트: 반려 10장 전부 inputFrame 실물 존재 + sourceVideo/t 백필 + cropNote 2건/orientation 1건 신설(quick-260816-ill2 인용), 나머지 9행 무변경
- Task 3 실행 게이트: 10 자산 전부 attempt 기록(합계 ≤30 생성 호출), pass 로 표시된 자산은 마지막 attempt 게이트가 실제 pass, 미통과는 still_failing 으로 정직하게 보존
- Task 4 산출 게이트: 통과 9장 sha256 9/9 무변경, board.html/SUMMARY/판정요청.md 존재, SUMMARY 에 LLM 학습 영향 명시
- pytest 기준선: `PYTHONPATH=backend/tests backend/.venv/bin/python -m pytest -q backend/tests` failed&lt;=59 (전 태스크 공통 기준선 4398 passed/59 failed/26 skipped)
- Gemini 호출 예산: 이미지 생성 ≤30회 + 9항목 판정 ≤30회, 합계 ≤60회(regen_verdicts.json 실측치로 SUMMARY 에 기재)
</verification>

<success_criteria>
- generate.py 의 build_prompt() 가 cropBox/cropNote/orientation 신호를 FRAMING/POSE
  FIDELITY 에 반영하고, 신호 없는 타깃은 기존 문구를 바이트 단위로 유지(순수함수
  테스트로 증명, API 호출 0)
- 반려 10장 전부 소실된 입력 프레임이 소스 영상에서 근거 있게(지어낸 값 0) 재확보되고,
  새 배선으로 재생성이 시도되며, 오늘과 동일한 9항목 기계 게이트로 재시도 상한 3회
  안에서 재판정된다
- 게이트를 통과하지 못한 자산은 미통과로 정직하게 belle 판정 재료에 남는다(덮어쓰기·
  은폐 없음)
- 통과 9장(app/assets/illustrations)·backend/shared·backend/functions·app/src 전부
  git status/sha256 기준 완전히 무접촉
- board.html + /Users/Shared 한글 재료 + SUMMARY(LLM 학습 영향 필수 기재)가 belle
  판정에 필요한 모든 것(이전/이후, 게이트 원문, 미통과 사유)을 담아 완비된다
- pytest 기준선(failed&lt;=59) 유지, Gemini 호출 총량이 명시된 예산(≤60회) 안에 든다
</success_criteria>

<output>
Create `.planning/quick/260816-ill2-illustration-framing-wiring/260816-ill2-SUMMARY.md` when
done — 기계 판정 요약(10 자산 이전/이후 finalStatus) + LLM 학습 영향(Gemini 호출 실측
건수, 모델, 추론만·학습 전송 0) + 미통과 자산 정직 보고(있다면 사유) + board.html/
/Users/Shared 절대경로 + 다음 단계(belle 판정 대기 — 승인분만 app/assets/illustrations
+ VERIFIED_ILLUSTRATIONS/ILLUSTRATION_SCENES 반영은 별도 사이클) 명기.
</output>
