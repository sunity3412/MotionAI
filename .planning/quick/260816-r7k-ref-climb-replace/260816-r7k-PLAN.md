---
phase: 260816-r7k-ref-climb-replace
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/scripts/mirror_reference_candidate_top_level.py
  - backend/tests/test_mirror_reference_candidate_top_level.py
  - .planning/phases/35-server-rendered-comparison-video/data/climb/doc.json
  - .planning/phases/35-server-rendered-comparison-video/data/climb/align.json
  - .planning/phases/35-server-rendered-comparison-video/data/climbfault/doc.json
  - .planning/phases/35-server-rendered-comparison-video/data/climbfault/align.json
  - .planning/phases/35-server-rendered-comparison-video/data/README.md
autonomous: true
requirements: ["belle-2026-08-16-ref-climb-replace"]

must_haves:
  truths:
    - "reference/ref-climb 문서의 videoS3Key 가 가리키는 S3 영상이 06-17 정은지 성공 클라임(40,928,589 B)이다 — 구 영상(4,223,747 B, 06-11 업로드)이 아니다"
    - "구 Firestore reference 11-doc 스냅샷과 구 S3 영상이 각각 독립적으로 byte-검증된 복구 가능 백업으로 존재한다 (파괴되지 않음)"
    - "ref-climb 의 angles/joints3d/keypointReport/meanAngles/techniqueProfile/bodyNormalizationProfile/forceDirectionPattern/bodyComparisonSourcePose/referenceKeypointReport/anglesRealFps 가 모두 새 영상 기준으로 재계산되어 있다 (구 영상 잔재 필드 없음)"
    - "다른 기준 모션 10개는 문서·영상 모두 교체 전후 완전히 동일하다 (canonical SHA-256 대조로 증명)"
    - "fixtures/phase15/climb/fault.mp4 를 새 기준과 비교했을 때의 angle 유사도 점수가 수치로 기록되어 있다 (NotPoleMotionError 해소 여부와 무관하게 결과를 있는 그대로 보고)"
    - "P35 data/climb 의 doc.json/align.json 이 새 기준으로 재생성됐거나, 불가하면 README 에 무효 사실이 명시돼 있다"
    - "pytest 기준선(4371 passed / 59 failed / 26 skipped)이 초과 실패 없이 유지된다"
  artifacts:
    - path: "s3://sunity-motion-pilot-videos/backups/reference-11-preC-<timestamp>.json"
      provides: "reference 11-doc 전량의 byte-검증·S3-업로드·복원-리허설 완료 백업 (canonical 복구 원천)"
    - path: "s3://sunity-motion-pilot-videos/reference/_archive/ref-climb-pre-quick260816r7k.mp4"
      provides: "교체 전 ref-climb.mp4 원본의 서버사이드 복사본 (byte-동일, ETag 대조 완료)"
    - path: "reference/ref-climb/versions/quick-260816-r7k"
      provides: "새 영상 기반 candidate 버전 전체 (base 11필드 + downstream 7필드) — 불변 스냅샷, phase4_v1/pre_phase4/phase33-cm3-run1/run2 는 그대로 보존"
    - path: "backend/scripts/mirror_reference_candidate_top_level.py"
      provides: "candidate 버전 → top-level 선택적 미러 (reference/_release 무접촉, ref-climb 단일 문서만 write)"
      min_lines: 40
    - path: "backend/tests/test_mirror_reference_candidate_top_level.py"
      provides: "_build_mirror_fields 순수 함수 behavior 테스트"
    - path: ".planning/phases/35-server-rendered-comparison-video/data/climb/doc.json"
      provides: "새 ref-climb 기준으로 재생성된 mode1 분석 doc (구 버전 대체)"
    - path: ".planning/phases/35-server-rendered-comparison-video/data/README.md"
      provides: "2026-08-16 quick-260816-r7k 교체 반영 + climbfault 실증 결과 기록"
  key_links:
    - from: "backend/scripts/mirror_reference_candidate_top_level.py"
      to: "reference/ref-climb (top-level doc)"
      via: "_build_mirror_fields() 산출을 .set(merge=True) 로 write, activeVersion=quick-260816-r7k"
      pattern: "activeVersion"
    - from: "backend/scripts/mirror_reference_candidate_top_level.py"
      to: "reference/_release"
      via: "의도적으로 미접촉 — _flip_active_pointer 재사용 안 함(전역 포인터가 다른 10개 모션 의미론과 얽힘)"
      pattern: "_release"
    - from: ".planning/phases/35-server-rendered-comparison-video/data/climb/doc.json"
      to: "reference/ref-climb (새 영상)"
      via: "p35_new_motion_docs.py 가 pipeline._process() 직접 호출 — 실 운영 코드 경로 그대로"
      pattern: "\"referenceMotionId\": \"ref-climb\""
---

<objective>
ref-climb 기준 모션을 05-22 극초반 배치(2026-06-11 재업로드분, 1080x1920, 17.07초)에서
06-17 정은지 성공 클라임 세트(`fixtures/phase15/climb/correct.mp4`, 2160x3840, 7.93초)로
교체한다. belle 2026-08-16 결정: "걍 따지지말고 교체하자" — 촬영 스튜디오·형태 불일치를
오늘 실측하고 내린 판단이며, **2026-06-21 결정("하나만 쓴다면 이전 것 우선")을 의도적으로
뒤집는 것**이다. belle 은 두 클라임이 같은 동작인지 나중에 다시 물어볼 수 있다고 했으므로
구 자산(S3 영상 + Firestore 문서 전체 스냅샷)은 절대 파괴하지 않고 byte-검증된 형태로
보존한다.

Purpose: mode1(정은지 기준 비교) 이 실제 촬영 형태와 맞는 기준으로 채점되게 하고, 지금은
angle 26점(간신히 통과)/fault 는 NotPoleMotionError 로 완전 차단인 짝 어긋남을 해소한다.
Output: 새 영상이 반영된 `reference/ref-climb` (Firestore top-level + 새 candidate 버전
서브문서), byte-검증된 구 자산 백업(S3 + 로컬), climbfault 실증 결과, 갱신된 P35 데이터.

## 무엇을 왜 하는가 (다운스트림 백필 필요 여부 판단 — scope 항목 5)

ref-climb 의 현재 top-level 문서는 `angles`/`joints3d`/`keypointReport` 뿐 아니라
`meanAngles`/`techniqueProfile`/`bodyNormalizationProfile`/`forceDirectionPattern`/
`bodyComparisonSourcePose`/`referenceKeypointReport`/`captureViews` 까지 전부 채워져
있다(2026-08-16 실측, 아래 <context> 참조). 이 필드들은 mode1 채점의 각 축(각도/라인/힘
방향/체형정규화)과 비교 화면(줌 비교의 소스 프레임, 키포인트 오버레이)이 실제로 소비한다.
`angles`/`joints3d` 만 새 영상으로 갈아끼우고 이 7개 필드를 그대로 두면, 같은 문서 안에서
일부는 새 영상, 일부는 구 영상을 가리키는 내부 불일치 상태가 된다 — 교체 전보다 더 나쁘다
(예: bodyComparisonSourcePose 가 새 영상과 무관한 구 영상의 한 프레임을 계속 보여줌).
따라서 `backend/scripts/backfill_reference_downstream.py --write-candidate` 로 이 7개
필드 전부를 새 영상 기준으로 재산출하는 것은 선택이 아니라 필수다.

## fps 라벨 선택 — 왜 18.0 을 유지하는가

`reprocess_reference_motions_phase4.py --target-fps` 기본값은 18.0, 반면
`extract_reference_keypoint_reports.py` 는 "학생 경로(9fps)와 기질 비대칭"을 이유로
9.0 으로 바꿨다(그 파일 docstring 참조). 하지만 **다른 기준 모션 10개는 전부 여전히
18.0** 이다(2026-08-16 실측, 전량 확인). ref-climb 하나만 9.0 으로 바꾸면 belle 이
요청하지 않은 "기준 모션 전역 fps 컨벤션 교정"이라는 별개의 더 큰 결정을 이 quick 태스크가
암묵적으로 떠안게 되고, 10개와 새로운 불일치를 만든다. 이 plan 은 **18.0 을 그대로
유지**해 10개 형제 문서와의 정합을 지킨다 — 사이트 전역 fps 컨벤션 교정은 belle 이 별도로
결정할 사안.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
CLAUDE.md 필독(프로젝트 규칙, §3 시크릿 하드코딩 금지·§7 코드 품질). STATE.md 는 크므로
읽지 말 것(이미 planning_context 에서 제외 지시됨).

@.planning/phases/35-server-rendered-comparison-video/data/README.md

# 2026-08-16 실측 (재조사 불필요 — 아래 값을 그대로 신뢰하되, 매 write 직전 fail-closed
# 재확인은 각 Task 에 명시)

**Firestore `reference/ref-climb` 현재 필드 (top-level, 실측 전량)**:
activeVersion=phase4_v1, angles(len=2056), anglesJointKeys(len=8, skeleton.JOINT_KEYS),
anglesFrames=257, anglesRealFps=15.0, anglesBackbone="rtmw-x-384-bukuroo-2026-06-06"
(어떤 현재 스크립트도 이 필드를 쓰지 않음 — 코드베이스 전체 grep 0건, 재현 시도하지
않음, harmless 하게 stale 로 남김), bodyComparisonSourcePose(dict),
bodyNormalizationProfile(dict), captureViews=1, coordDim=3, forceDirectionPattern(dict),
isActive=True, joints3d(len=13107), joints3dFrames=257, joints3dKeys(len=17, COCO-17),
keypointReport(dict, fps=18.0, frames=257), level="basic", meanAngles(dict),
motionId="ref-climb", name="클라임", pipelineVersion=phase4_v1,
referenceKeypointReport(dict, fps=18.0, frames=257, keypointReport 와 동일 — 별도 9fps
산출 아님), reprocessedAt="2026-06-14T10:42:34...", space="pole_aligned",
techniqueProfile(dict), videoS3Key="reference/ref-climb.mp4".

`reference/ref-climb/versions/` 서브컬렉션 실측: `phase4_v1`(11키, base 필드만, 현재
active), `pre_phase4`(25키, 5월 마이그레이션 이전 preimage), `phase33-cm3-run1`(28키),
`phase33-cm3-run2`(11키) — 전부 존재, 전부 무접촉 유지(새 버전만 추가). `reference/_release`
문서는 존재하지 않음(활성 아님 — 이 plan 도 건드리지 않는다).

**다른 기준 모션 10개**: 전부 activeVersion=phase4_v1, anglesFrames==joints3dFrames==
keypointReport.frames, len(angles)==anglesFrames×8, len(joints3d)==joints3dFrames×17×3
— `backup_reference_docs.py` 4-check 게이트가 통과할 조건을 이미 만족(실측 확인 완료).

**S3 head-object 실측**: `reference/ref-climb.mp4` ContentLength=4223747,
ETag="4c4dfdbf48c30d80789186e4e8080b72", LastModified=2026-06-11T13:10:51Z.
`fixtures/phase15/climb/correct.mp4` ContentLength=40928589,
ETag="8dd30a35761c5f8d50a9412b3832dd59". `fixtures/phase15/climb/fault.mp4`
ContentLength=31560025. AWS profile `sunity-motion`(Account 976369350031) 로컬에서
S3 접근 확인됨. FIREBASE_SA_PATH=../firebase-sa.json 로컬에서 Firestore 읽기 확인됨.

**pytest 기준선 재확인(2026-08-16, 이 plan 작성 중 직접 실행)**: 정확히
`59 failed, 4371 passed, 26 skipped, 42 warnings in 41.94s`
(명령: `PYTHONPATH=backend/tests backend/.venv/bin/python -m pytest -q backend/tests`,
레포 루트에서 실행).

**주요 도구 스크립트 요약** (전부 `backend/scripts/`, 이미 git 커밋됨 — Pod 에 존재):
- `backup_reference_docs.py` — reference 11-doc 읽기전용 백업, 4-check 무결성 게이트 +
  S3 업로드/재다운로드 byte 비교 + 격리 컬렉션 복원 리허설. **로컬 실행 가능**(GPU 불요).
- `reprocess_reference_motions_phase4.py` — S3 영상 → RTMW → angles/joints3d/keypointReport
  → `reference/{id}/versions/{version}` write(refuse-overwrite) → `--no-flip` 없으면
  `_flip_active_pointer` 로 top-level mirror + `reference/_release` 전역 포인터까지 write.
  **이 plan 은 `--no-flip` 만 쓰고 flip 은 자체 스크립트로 대체**(아래 이유).
- `backfill_reference_downstream.py` — candidate 버전 문서에서 angles 를 읽어(재추론
  아님) meanAngles/techniqueProfile 산출 + live RTMW 1회 재추론으로
  bodyNormalizationProfile/forceDirectionPattern/bodyComparisonSourcePose/keypointReport/
  referenceKeypointReport 산출 → **같은 candidate 버전 문서에 MERGE**(top-level 무접촉).
  `--write-candidate` 필요, `--reference-version` 필수.
- `backfill_reference_real_fps.py` — 저장된 fps 라벨이 아니라 영상을 ffprobe 로 실측해
  진짜 decimation rate 를 유도, 1% 이내 교차검증 통과 시에만 top-level `anglesRealFps`
  하나만 write(fail-closed). **로컬 실행 가능**(GPU 불요, ffprobe 만 필요).
- `p35_new_motion_docs.py` — SIM_UID(fvcNXzEqKjgqVxRPVSj1iwFnIpn2, 파일럿 내부 sim
  계정, belle 실계정 아님) 로 `pipeline._process()` 를 **직접 호출**(S3 업로드/SQS 없이
  fixtures/ 원본을 그대로 분석) → `users/{SIM_UID}/analyses/{id}` 에 실 분석 doc 생성.
  `ITEMS` 딕셔너리에 이미 `climb`/`climbfault`/`combo` 3개 슬롯 존재(quick-260816-c3m
  에서 오늘 추가됨) — 스크립트 수정 불필요, `--slots climb,climbfault` 로 바로 실행.
- `p35_extract_align.py` — `{workdir}/{motion}/doc.json` 필수 전제(위 스크립트가 생성) →
  RTMW 15fps 재추출 + DTW 정렬 → `align.json`. `JOBS` 딕셔너리에도 `climbfault` 이미
  존재. `--motions climb,climbfault` 로 부분 실행 가능.

**의도적으로 피하는 것**: `reprocess_reference_motions_phase4.py` 의 `_flip_active_pointer`
전체 실행. 이유 두 가지 — (1) `reference/_release.activeCandidate` 전역 필드에
"quick-260816-r7k" 를 쓰면 이 값은 ref-climb 1개에만 의미가 있는데 필드 자체는 11개
모션 전체에 걸친 collection-level 값이라 향후 다른 모션 승격 로직이 이 값을 신뢰하면
혼란을 만든다(현재 아무 코드도 `_release` 를 읽지 않음 — 확인됨 — 그래도 새로 심지
않는다). (2) `_flip_active_pointer` 의 `pre_phase4` 백업은 "존재하면 스킵" 가드라서
이미 존재하는 `pre_phase4`(5월 preimage) 를 보존하는 대신 **지금 시점의 top-level
전체 상태**(6/14 재처리 + 6/15 다운스트림 병합분, downstream 7필드 포함)는 백업하지
않는다 — Task 1 의 `backup_reference_docs.py` 전체 스냅샷이 이 gap 을 메운다(11필드가
아니라 top-level 전 필드를 백업).
</context>

<tasks>

<task type="auto">
  <name>Task 1: 백업(Firestore 11-doc + S3 영상 아카이브) 후 신규 영상 업로드</name>
  <files>없음 (레포 파일 변경 없음 — S3/Firestore 쓰기 + 로컬 백업 아티팩트만)</files>
  <action>
  모두 레포 루트에서, 로컬 `backend/.venv`(GPU 불요) 로 실행한다.

  1) Firestore 11-doc 백업 (읽기전용 + S3 업로드/재다운로드 byte 비교 +
  격리 컬렉션 복원 리허설 — `backup_reference_docs.py` 기본값이 이미 전체 11개이므로
  `--motions` 생략):
  `cd backend && PYTHONPATH=shared/python:. FIREBASE_SA_PATH=../firebase-sa.json AWS_PROFILE=sunity-motion .venv/bin/python3 scripts/backup_reference_docs.py --out ../.planning/debug/backups/reference-11-preC-$(date +%Y%m%d-%H%M%S).json --rehearse-restore`
  stdout 에서 `GATE PASS` 와 `REHEARSAL PASS — 11/11 round-trip 바이트 일치` 둘 다
  확인. 둘 중 하나라도 안 나오면 (`GATE FAIL`/`REHEARSAL FAIL`) 이후 어떤 write 도
  하지 말고 STOP, 사유를 그대로 보고. `.planning/debug/backups/` 는 `.gitignore` 로
  git 추적 제외되므로(확인됨) **진짜 durable 백업은 stdout 에 출력되는 S3 URI**
  (`s3://sunity-motion-pilot-videos/backups/reference-11-preC-<ts>.json`) 다 — 이
  URI 와 whole-file SHA-256 을 SUMMARY.md 에 그대로 옮겨 적을 것 (로컬 파일은 재현
  가능한 캐시일 뿐).

  2) 교체 전 S3 영상이 측정값과 정확히 일치하는지 재확인(드리프트 감지, fail-closed):
  `aws s3api head-object --bucket sunity-motion-pilot-videos --key reference/ref-climb.mp4 --profile sunity-motion`
  → `ContentLength` 이 정확히 4223747, `ETag` 이 정확히
  `"4c4dfdbf48c30d80789186e4e8080b72"` 인지 확인. 다르면 누군가 이미 영상을 바꾼 것 —
  STOP, belle 에게 보고(추측으로 진행 금지).

  3) 구 영상을 아카이브 키로 서버사이드 복사(다운로드 없이, byte 그대로):
  `aws s3 cp s3://sunity-motion-pilot-videos/reference/ref-climb.mp4 s3://sunity-motion-pilot-videos/reference/_archive/ref-climb-pre-quick260816r7k.mp4 --profile sunity-motion`
  이어서 head-object 로 아카이브가 원본과 byte-동일한지 확인
  (`ContentLength=4223747`, 같은 `ETag`).

  4) 아카이브 확인 후에만 새 영상으로 덮어쓰기:
  `aws s3 cp s3://sunity-motion-pilot-videos/fixtures/phase15/climb/correct.mp4 s3://sunity-motion-pilot-videos/reference/ref-climb.mp4 --profile sunity-motion`
  이어서 head-object 로 새 영상이 자리잡았는지 확인
  (`ContentLength=40928589`, `ETag="8dd30a35761c5f8d50a9412b3832dd59"`).

  이 Task 는 Firestore `reference/ref-climb` **문서 자체는 아직 쓰지 않는다** —
  S3 영상만 바뀐 상태이고 문서는 여전히 구 영상을 가리키는 메타데이터를 담고 있다
  (Task 2/3 이 뒤이어 갱신). 다른 10개 기준 모션의 S3 키/Firestore 문서는 이 Task
  에서 어떤 명령도 건드리지 않는다(전부 `ref-climb` 로 명시 scope).
  </action>
  <verify>
    <automated>
      set -e
      LATEST=$(ls -t .planning/debug/backups/reference-11-preC-*.json 2>/dev/null | grep -v FAILED | head -1)
      test -n "$LATEST" && jq -e '.header.integrityFailures == []' "$LATEST" >/dev/null
      jq -e '.header.count == 11' "$LATEST" >/dev/null
      ARCH=$(aws s3api head-object --bucket sunity-motion-pilot-videos --key reference/_archive/ref-climb-pre-quick260816r7k.mp4 --profile sunity-motion)
      echo "$ARCH" | jq -e '.ContentLength == 4223747' >/dev/null
      NEW=$(aws s3api head-object --bucket sunity-motion-pilot-videos --key reference/ref-climb.mp4 --profile sunity-motion)
      echo "$NEW" | jq -e '.ContentLength == 40928589' >/dev/null
      echo "Task 1 verify PASS"
    </automated>
  </verify>
  <done>
  로컬 백업 JSON(+ MANIFEST) 존재, stdout 에 GATE PASS + REHEARSAL PASS(11/11) 기록,
  S3 durable 백업 URI 확보. `reference/_archive/ref-climb-pre-quick260816r7k.mp4` 가
  구 영상과 byte-동일. `reference/ref-climb.mp4` 가 이제 40,928,589 B 새 영상.
  Firestore 문서는 아직 미변경.
  </done>
</task>

<task type="auto">
  <name>Task 2: Pod GPU 재처리 — 새 영상 기준 candidate 버전 생성(base + downstream)</name>
  <files>없음 (레포 파일 변경 없음 — Pod 에서 이미 커밋된 스크립트 실행, Firestore
  candidate 서브문서만 write)</files>
  <action>
  fail-closed 선행 체크: Task 1 의 로컬 백업 파일이 존재하고 `integrityFailures==[]`
  임을, 그리고 `reference/ref-climb.mp4` head-object 가 이미 40928589 B 임을 재확인한
  뒤에만 아래로 진행한다. 어느 하나라도 아니면 STOP.

  Pod 접속: `ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -i ~/.ssh/id_ed25519 -p 15167 root@213.173.102.172`
  레포는 `/workspace/SunityMotion`. **추론 서버(PID 4823)는 재기동하지 않는다** — 아래
  스크립트들은 서버 프로세스와 무관한 독립 1회성 Python 프로세스로, 같은 GPU 를 순차
  공유할 뿐이다.

  1) 진단: `bash backend/scripts/pod_doctor.sh`. exit 1(필수 결손) 이면 STOP, 임의로
  pip install 등 즉흥 수리 시도하지 말고 결손 내역 그대로 보고(CLAUDE.md §7 "막히면
  질문 먼저"). exit 2(경고만, 추론은 가능)면 진행. §1 "리포 뒤처짐" 경고가 뜨면
  `git -C /workspace/SunityMotion pull --ff-only` 먼저 실행.

  2) `cd /workspace/SunityMotion/backend && export PYTHONPATH=$PWD:$PWD/shared/python`
  이어서 pod_doctor.sh §5 가 확인한 production env 파일을 source 한다
  (`/workspace/aws_env.sh` 또는 `pod.env` — pod_doctor.sh 출력에서 실제 존재하는
  쪽을 확인 후 사용, FIREBASE_SA_PATH/AWS_ACCESS_KEY_ID/AWS_SECRET_ACCESS_KEY/
  AWS_DEFAULT_REGION 이 export 됐는지 `env | grep -E 'FIREBASE_SA|AWS_'` 로 확인).

  3) Base 필드 재처리 — **`--no-flip` 필수**(top-level 은 Task 3 이 별도 스크립트로
  갱신, 이유는 <context> 참조), `--motions ref-climb` 필수(생략하면 원본 5개 전부
  건드림), `--target-fps 18.0` 명시(10개 형제와 정합, <objective> 참조), 버전 id 는
  `quick-260816-r7k`(기존 phase4_v1/pre_phase4/phase33-cm3-run1/run2 와 충돌 없음
  확인됨):
  `python3 scripts/reprocess_reference_motions_phase4.py --motions ref-climb --version quick-260816-r7k --target-fps 18.0 --no-flip --out /workspace/reprocess-ref-climb-quick260816r7k.json`
  stdout 에 `[ref-climb] reprocess OK`, `[ref-climb] schema gate PASS`,
  `[ref-climb] versioned write 완료 → reference/ref-climb/versions/quick-260816-r7k`
  가 전부 있어야 한다. 실패 시 STOP, 원인 보고(재시도 전 원인 파악 — belle 판단
  기다림).

  4) Downstream 필드 백필 — 같은 candidate 버전에 MERGE, `--target-fps` 는 생략(candidate
  의 `keypointReport.fps`=18.0 을 자동으로 읽음):
  `python3 scripts/backfill_reference_downstream.py --reference-version quick-260816-r7k --bucket sunity-motion-pilot-videos --motions ref-climb --write-candidate --output /workspace/backfill-ref-climb-quick260816r7k.json`
  stdout 에 `all-or-nothing gate 실패` 가 없어야 하고 exit code 0, 그리고
  `real-run 완료 — 1/1 motion candidate versions/quick-260816-r7k 에 MERGE` 로 끝나야
  한다. angle 게이트(candidate vs 이 스크립트의 live 재추론 각도 systematic shift)가
  걸리면(=같은 영상인데 mean/p99 delta 가 임계 초과) 이는 RTMW 비결정성 조사가 필요한
  신호다 — 임계를 올려서 통과시키지 말고(D-29 원칙) STOP, 원인 그대로 보고.
  </action>
  <verify>
    <automated>
      cd backend && PYTHONPATH=shared/python:. FIREBASE_SA_PATH=../firebase-sa.json .venv/bin/python3 -c "
import sys; sys.path.insert(0,'shared/python'); sys.path.insert(0,'.')
from sunity_shared import firestore_admin as fa
snap = fa._doc('reference/ref-climb/versions/quick-260816-r7k').get()
assert snap.exists, 'candidate version 문서 없음'
d = snap.to_dict()
need = ['angles','anglesJointKeys','anglesFrames','joints3d','joints3dKeys','joints3dFrames',
        'coordDim','space','pipelineVersion','reprocessedAt','keypointReport',
        'meanAngles','techniqueProfile','bodyNormalizationProfile','forceDirectionPattern',
        'captureViews','bodyComparisonSourcePose','referenceKeypointReport']
missing = [k for k in need if k not in d]
assert not missing, f'candidate 문서 필드 누락: {missing}'
assert d['anglesFrames'] == d['keypointReport']['frames'], 'frame 수 불일치'
assert d['pipelineVersion'] == 'quick-260816-r7k'
print('Task 2 verify PASS — candidate 문서', len(d), '키, frames=', d['anglesFrames'])
"
    </automated>
  </verify>
  <done>
  `reference/ref-climb/versions/quick-260816-r7k` 서브문서가 base 11필드 + downstream
  7필드를 전부 갖추고 있다(frame-count 내부 정합). `reference/ref-climb` top-level 과
  `reference/_release` 는 여전히 미변경. `phase4_v1`/`pre_phase4`/`phase33-cm3-run1`/
  `run2` 서브문서도 무변경(추가만 했을 뿐 수정/삭제 없음).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: top-level 미러 스크립트 작성+테스트 → 실행 → 실측 fps 백필 → 전체 검증</name>
  <files>backend/scripts/mirror_reference_candidate_top_level.py, backend/tests/test_mirror_reference_candidate_top_level.py</files>
  <behavior>
  `_build_mirror_fields(candidate_doc: dict, version: str, now_ms: int) -> dict` 순수
  함수(Firestore/네트워크 무관, `validation.py` 의 "순수 함수" 컨벤션과 동일 스타일):
  - Test 1: candidate_doc 이 BASE_FIELDS = ["angles","anglesJointKeys","anglesFrames",
    "keypointReport","joints3d","joints3dKeys","joints3dFrames","coordDim","space",
    "pipelineVersion","reprocessedAt"] 와 DOWNSTREAM_FIELDS = ["meanAngles",
    "techniqueProfile","bodyNormalizationProfile","forceDirectionPattern",
    "captureViews","bodyComparisonSourcePose","referenceKeypointReport"] 를 전부 가진
    fixture 를 넣으면, 반환 dict 는 이 18개 필드 + `activeVersion == version` +
    DOWNSTREAM_FIELDS 각각에 대한 `{field}UpdatedAt == now_ms` (7개) 를 전부 포함한다
    (합계 18+1+7=26개 top-level 키).
  - Test 2: 반환 dict 에 `downstreamBackfillVersion`/`downstreamBackfilledAt`/
    `keypointReportUpdatedAt`(base 필드는 UpdatedAt 컴패니언이 없음) 등 candidate-only
    부기 필드가 하나도 섞여 있지 않다(정확히 26개 키, 그 이상도 이하도 아님).
  - Test 3: BASE_FIELDS 중 하나가 candidate_doc 에서 빠져 있으면 `ValueError` 를 던진다
    (부분 데이터를 조용히 write 하지 않는다 — fail-closed).
  </behavior>
  <action>
  `backend/scripts/mirror_reference_candidate_top_level.py` 작성. 한국어 모듈
  docstring 으로 목적과 이 스크립트가 왜 `reprocess_reference_motions_phase4.py` 의
  `_flip_active_pointer` 를 재사용하지 않는지 근거(<context> 인용)를 남긴다
  (CLAUDE.md §7 "주석은 왜를 남기고 근거 인용"). CLI: `--motion-id`(기본 필수),
  `--version`(기본 필수). 동작: `reference/{motion_id}/versions/{version}` 을 읽고
  (없으면 에러) → write 전에 현재 top-level `activeVersion` 을 stdout 에 출력
  (`rollback_reference_motions_phase4.py` 의 T-04-W5-05 감사 로그 패턴과 동일 —
  "누구든 실행 로그만 보고 이전 상태를 알 수 있게") → `_build_mirror_fields` 호출 →
  `reference/{motion_id}` top-level 에 `.set(mirror_fields, merge=True)` **만** 실행
  (`reference/_release` 는 이 스크립트 어디에서도 참조하지 않는다 — grep 으로
  자기증명 가능하게).

  로컬에서 실행(GPU 불요, Firestore write 만):
  `cd backend && PYTHONPATH=shared/python:. FIREBASE_SA_PATH=../firebase-sa.json .venv/bin/python3 -m pytest -q tests/test_mirror_reference_candidate_top_level.py`
  통과 확인 후:
  `PYTHONPATH=shared/python:. FIREBASE_SA_PATH=../firebase-sa.json .venv/bin/python3 scripts/mirror_reference_candidate_top_level.py --motion-id ref-climb --version quick-260816-r7k`
  stdout 에 `현재 activeVersion=phase4_v1 → quick-260816-r7k` 류의 로그와 병합 필드
  개수가 찍혀야 한다.

  실측 fps 백필(로컬, GPU 불요 — ffprobe 로 새 영상을 실측):
  `FIREBASE_SA_PATH=../firebase-sa.json .venv/bin/python3 scripts/backfill_reference_real_fps.py --dry-run --motions ref-climb`
  판정 열이 `기록` 이면:
  `FIREBASE_SA_PATH=../firebase-sa.json .venv/bin/python3 scripts/backfill_reference_real_fps.py --write --motions ref-climb`
  판정이 `★불일치 — 미기록` 이면 강제로 쓰지 말고(fail-closed 는 이 스크립트의 설계다)
  SUMMARY.md 에 "anglesRealFps 미기록 — 파이프라인이 라벨(18.0)로 fail-open, 2026-08-10
  이전 동작과 동일 수준" 이라고 있는 그대로 남긴다.

  전체 검증(문서 필드 + 다른 10개 무변경 + pytest 기준선) — verify 블록의 python 을
  그대로 실행.
  </action>
  <verify>
    <automated>
      cd backend && PYTHONPATH=shared/python:. FIREBASE_SA_PATH=../firebase-sa.json .venv/bin/python3 -c "
import sys, json, hashlib, glob
sys.path.insert(0,'shared/python'); sys.path.insert(0,'.')
from sunity_shared import firestore_admin as fa

d = fa.get_reference_motion('ref-climb') or {}
assert d.get('isActive') is True
assert d.get('name') == '클라임'
assert d.get('level') == 'basic'
assert d.get('videoS3Key') == 'reference/ref-climb.mp4'
assert d.get('activeVersion') == 'quick-260816-r7k'
af = d.get('anglesFrames'); kr = d.get('keypointReport') or {}; rkr = d.get('referenceKeypointReport') or {}
assert af == kr.get('frames') == rkr.get('frames'), (af, kr.get('frames'), rkr.get('frames'))
assert len(d.get('angles') or []) == af * len(d.get('anglesJointKeys') or [])
assert d.get('bodyComparisonSourcePose'), 'bodyComparisonSourcePose 비어있음'
print('ref-climb top-level 필드 검증 PASS — activeVersion=', d['activeVersion'], 'anglesFrames=', af)

def _canon(obj):
    return json.dumps(obj, ensure_ascii=False, sort_keys=True, separators=(',',':'), default=str).encode('utf-8')

others = ['ref-foxtop','ref-foxtop-split','ref-invert','ref-sideway-spin','ref-combo',
          'ref-elbow-twist-sister','ref-kip-up','ref-pdshape','ref-peter-pan','ref-power-spin']
latest = sorted(g for g in glob.glob('../.planning/debug/backups/reference-11-preC-*.json') if not g.endswith('FAILED'))[-1]
backup = json.load(open(latest))
mismatch = []
for mid in others:
    now_hash = hashlib.sha256(_canon(fa.get_reference_motion(mid))).hexdigest()
    before_hash = backup['header']['perDocSha256'][mid]
    if now_hash != before_hash:
        mismatch.append(mid)
assert not mismatch, f'다른 기준 모션 변경 감지: {mismatch}'
print('다른 기준 모션 10개 무변경 확인 PASS (SHA-256 대조,', latest, ')')
"
      cd .. && PYTHONPATH=backend/tests backend/.venv/bin/python -m pytest -q backend/tests 2>&1 | tail -3
    </automated>
  </verify>
  <done>
  `reference/ref-climb` top-level 문서가 새 영상 기준 필드로 전부 채워짐(activeVersion=
  quick-260816-r7k), 다른 10개 기준 모션 문서는 Task 1 백업 시점과 SHA-256 완전 일치,
  pytest 가 정확히 `59 failed, 4371 passed, 26 skipped` (그 이상 실패 없음). anglesRealFps
  기록 여부(성공/미기록)가 SUMMARY.md 에 있는 그대로 남아 있음.
  </done>
</task>

<task type="auto">
  <name>Task 4: 교체 효과 실증(climbfault) + P35 데이터 재생성 + README 갱신</name>
  <files>.planning/phases/35-server-rendered-comparison-video/data/climb/doc.json,
  .planning/phases/35-server-rendered-comparison-video/data/climb/align.json,
  .planning/phases/35-server-rendered-comparison-video/data/climbfault/doc.json (조건부),
  .planning/phases/35-server-rendered-comparison-video/data/climbfault/align.json (조건부),
  .planning/phases/35-server-rendered-comparison-video/data/README.md</files>
  <action>
  Task 3 까지 Firestore 의 `reference/ref-climb` 가 새 영상 기준으로 갱신된 뒤에만
  실행한다(위 verify 가 PASS 했어야 함).

  Pod 접속(Task 2 세션이 끝났으면 재접속, env 재source 필요):
  `ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -i ~/.ssh/id_ed25519 -p 15167 root@213.173.102.172`
  새 세션이면 `bash /workspace/SunityMotion/backend/scripts/pod_doctor.sh` 먼저.

  1) 실증 — SIM_UID 로 `pipeline._process()` 직접 호출(운영 코드 그대로, uploads/ 복사
  없음). `climb`/`climbfault` 슬롯은 `ITEMS` 딕셔너리에 이미 있음(오늘 quick-260816-c3m
  에서 추가, 스크립트 수정 불필요):
  `cd /workspace/SunityMotion/backend && mkdir -p /workspace/p35 && python3 scripts/p35_new_motion_docs.py --outdir /workspace/p35 --slots climb,climbfault`
  결과를 있는 그대로 기록한다(어느 쪽이든 "성공"으로 간주 — belle "덮지 말 것"):
  - `climb`(correct.mp4 vs 새 ref-climb): `status=done` 기대. 실패하면 원인 그대로 보고.
  - `climbfault`(fault.mp4 vs 새 ref-climb): 교체 전 angle=0 으로 `NotPoleMotionError`
    차단이었다. 이번에 (a) `successes` 에 들어가면 doc.json 을 열어 각도 유사도 점수
    (`result` 안의 dimensionScores/deductionBreakdown 부근, `dimensions.DIM_ANGLE`
    관련 값 — grep 으로 찾을 것)를 정확한 숫자로 기록한다. (b) 여전히 `failures` 에
    `NotPoleMotionError: angle {N} < 25` 형태로 남으면 그 N 값을 그대로 (0 이었던 것이
    바뀌었는지 아닌지) 기록한다. 둘 중 어느 쪽이 나와도 Task 실패가 아니다 — 숫자를
    수치로 남기는 것이 목적.

  2) P35 align 재생성 — doc.json 이 생성된 슬롯만:
  `python3 scripts/p35_extract_align.py --workdir /workspace/p35 --motions climb`
  (climbfault 도 doc.json 이 생겼으면 `--motions climb,climbfault` 로 같이). stdout 에
  `[climb] frames u=... r=... pairs=[...] -> align.json` 확인, traceback 없어야 함.

  3) 로컬로 회수(Pod 의 /workspace 는 휘발성 — 레포에 반영해야 "보존"):
  `scp -P 15167 -i ~/.ssh/id_ed25519 root@213.173.102.172:/workspace/p35/climb/doc.json .planning/phases/35-server-rendered-comparison-video/data/climb/doc.json`
  `scp -P 15167 -i ~/.ssh/id_ed25519 root@213.173.102.172:/workspace/p35/climb/align.json .planning/phases/35-server-rendered-comparison-video/data/climb/align.json`
  climbfault 도 생성됐으면 `mkdir -p .planning/phases/35-server-rendered-comparison-video/data/climbfault` 후 동일하게 scp.

  4) `.planning/phases/35-server-rendered-comparison-video/data/README.md` 갱신 —
  기존 "2026-08-16, quick-260816-c3m" 섹션 아래에 새 날짜 구분된 섹션 추가
  (기존 내용을 지우지 않고 append): (a) climb 의 doc.json/align.json 이
  quick-260816-r7k 로 새 ref-climb(06-17 영상) 기준으로 재생성됐고, c3m 이 만든
  이전 쌍은 구 영상(05-22/06-11 배치) 기준이라 무효였다는 사실 명기. (b) climbfault
  실증 결과를 있는 그대로(전/후 angle 값, 통과/차단 여부) 기록. (c) climbfault 결과가
  이제 doc.json 을 갖게 됐다면 "S3 키" 표와 "동작" 표에 climbfault 행을 추가.

  5) pytest 기준선 재확인(레포 루트):
  `PYTHONPATH=backend/tests backend/.venv/bin/python -m pytest -q backend/tests`
  `59 failed, 4371 passed, 26 skipped` 정확히 일치해야 함(그 이상 실패는 STOP 후 보고).
  </action>
  <verify>
    <automated>
      test -f .planning/phases/35-server-rendered-comparison-video/data/climb/doc.json
      test -f .planning/phases/35-server-rendered-comparison-video/data/climb/align.json
      python3 -c "
import json
d = json.load(open('.planning/phases/35-server-rendered-comparison-video/data/climb/doc.json'))
assert d.get('referenceMotionId') == 'ref-climb'
assert d.get('status') == 'done', d.get('status')
print('climb doc.json 검증 PASS — status=', d['status'])
"
      grep -q "quick-260816-r7k" .planning/phases/35-server-rendered-comparison-video/data/README.md
      grep -qi "climbfault" .planning/phases/35-server-rendered-comparison-video/data/README.md
      PYTHONPATH=backend/tests backend/.venv/bin/python -m pytest -q backend/tests 2>&1 | tail -3
        </automated>
  </verify>
  <done>
  `data/climb/{doc.json,align.json}` 이 새 ref-climb 기준으로 재생성됨(status=done,
  referenceMotionId=ref-climb). climbfault 의 NotPoleMotionError 해소 여부가 수치와
  함께 README 에 기록됨(성공/실패 어느 쪽이든). pytest 기준선 유지.
  </done>
</task>

</tasks>

<rollback_procedure>
## 문제 발생 시 되돌리는 법 (SUMMARY.md 에 그대로 복사할 것)

1. **S3 영상 되돌리기**: `aws s3 cp s3://sunity-motion-pilot-videos/reference/_archive/ref-climb-pre-quick260816r7k.mp4 s3://sunity-motion-pilot-videos/reference/ref-climb.mp4 --profile sunity-motion`
   (head-object 로 ContentLength=4223747, ETag="4c4dfdbf48c30d80789186e4e8080b72" 확인)

2. **Firestore 문서 되돌리기** (Task 1 의 백업 JSON 이 유일한 완전 복원 원천 — `versions/phase4_v1`
   서브문서는 downstream 7필드가 없어 불완전하다, 반드시 백업 JSON 사용):
   ```python
   import json, sys
   sys.path.insert(0, 'backend/shared/python'); sys.path.insert(0, 'backend')
   from sunity_shared import firestore_admin as fa
   backup = json.load(open('<Task 1 이 출력한 로컬 백업 경로, 또는 S3 URI 에서 재다운로드>'))
   fa._doc('reference/ref-climb').set(backup['docs']['ref-climb'], merge=False)
   ```
   (`merge=False` — 새로 추가된 필드까지 포함해 백업 시점 상태로 완전히 되돌림)

3. `reference/ref-climb/versions/quick-260816-r7k` 서브문서는 지우지 않아도 무해(참조되지
   않음 — activeVersion 이 phase4_v1 로 되돌아가면 아무도 이 서브문서를 읽지 않는다).

4. 다른 10개 기준 모션과 `reference/_release` 는 애초에 이 plan 이 건드리지 않았으므로
   롤백 대상 아님.
</rollback_procedure>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Executor(Claude/Bash) → AWS S3 `sunity-motion-pilot-videos` | `AWS_PROFILE=sunity-motion` 크리덴셜로 프로덕션 비디오 버킷에 쓰기 |
| Executor → Firestore `reference` 컬렉션 | `firebase-sa.json`(Admin SDK, 보안규칙 우회)로 모든 사용자 mode1 비교가 직접 읽는 문서에 쓰기 |
| Executor → RunPod Pod `t4i1p9k2hnvb0q`(SSH root) | 운영 추론 서버(PID 4823)와 같은 GPU/프로세스 공간 공유 |
| `fixtures/phase15/climb/{correct,fault}.mp4` → 신규 기준 콘텐츠 | 로컬 fixture 가 모든 향후 climb mode1 비교의 "정답"으로 승격 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-r7k-01 | Tampering | S3 `reference/ref-climb.mp4` 덮어쓰기 | mitigate | Task 1 이 덮어쓰기 전 `_archive/` 로 서버사이드 복사 + head-object ETag 대조, Task 2/3 진입 전 fail-closed 재확인 |
| T-r7k-02 | Tampering | Firestore `reference/ref-climb` top-level 문서 덮어쓰기 | mitigate | Task 1 `backup_reference_docs.py --rehearse-restore` 로 byte-해시 + S3 검증 + 격리 컬렉션 round-trip 복원까지 증명된 백업, 롤백 레시피 명문화 |
| T-r7k-03 | Tampering | `_flip_active_pointer` 의 전역 `reference/_release.activeCandidate` | mitigate | 전혀 호출하지 않음 — Task 3 의 커스텀 미러 스크립트는 `reference/ref-climb` 단일 문서만 write, grep 으로 `_release` 무접촉 자기증명 |
| T-r7k-04 | Tampering/Integrity | angles/joints3d 는 새 영상, meanAngles 등 downstream 7필드는 구 영상 — 내부 불일치 문서 | mitigate | Task 2 downstream 백필 필수화(<objective> 근거) + Task 3 이 frame-count 정합·필드 존재 전량 검증 |
| T-r7k-05 | Tampering | 다른 기준 모션 10개 문서/영상 collateral 변경 | mitigate | 모든 S3/Firestore 명령이 `ref-climb` 로 명시 scope, Task 3 이 SHA-256 대조로 10개 무변경 기계 검증 |
| T-r7k-06 | Elevation of Privilege | Admin AWS IAM(`sunity-motion`) + Firebase Admin SA 크리덴셜 사용 | accept | 이 프로젝트의 모든 자매 reference 스크립트가 이미 쓰는 표준 admin-ops 크리덴셜, 신규 노출 없음 |
| T-r7k-07 | Denial of Service | 재처리 스크립트가 운영 추론 서버(PID 4823)와 같은 Pod GPU 공유 | accept | `reprocess_reference_motions_phase4.py`/`p35_new_motion_docs.py` 는 서버 프로세스와 무관한 독립 1회성 파이썬 프로세스(HTTP 위임 아님), 서버 재기동 절대 금지를 각 Task 에 명시 |
| T-r7k-SC | Tampering | npm/pip 신규 설치 | N/A | 이 plan 에 설치 태스크 없음 — boto3/firebase_admin/numpy/rtmlib/onnxruntime 전부 기설치분 재사용(pod_doctor.sh/로컬 venv 로 기확인). Package Legitimacy Gate 비적용 |
</threat_model>

<verification>
- Task 1~4 의 `<verify><automated>` 전부 통과.
- `PYTHONPATH=backend/tests backend/.venv/bin/python -m pytest -q backend/tests` 가
  정확히 `59 failed, 4371 passed, 26 skipped` (더 많은 실패면 회귀 — STOP).
- `reference/ref-climb` top-level 문서 재조회: isActive=True, name="클라임",
  level="basic", videoS3Key="reference/ref-climb.mp4", activeVersion=
  "quick-260816-r7k", referenceKeypointReport 채워짐.
- 다른 기준 모션 10개 문서의 canonical SHA-256 이 Task 1 백업 시점과 완전 일치.
- climbfault(fault.mp4 vs 새 ref-climb) 의 결과(성공 시 angle 점수, 실패 시
  NotPoleMotionError 의 새 angle 값)가 README.md 와 SUMMARY.md 에 수치로 남아있음.
</verification>

<success_criteria>
- ref-climb 의 S3 영상과 Firestore 파생 필드 전부가 06-17 정은지 성공 클라임 기준으로
  교체됨.
- 구 S3 영상(`_archive/`)과 구 Firestore 문서 전체(백업 JSON, S3 URI)가 byte-검증된
  형태로 보존되고 롤백 절차가 SUMMARY.md 에 명문화됨.
- 다른 기준 모션 10개는 문서·영상 모두 완전히 무변경(SHA-256 증명).
- climbfault 의 NotPoleMotionError 해소 여부가 숫자로(덮지 않고) 기록됨.
- P35 `data/climb` 가 새 기준으로 재생성되고 README 가 갱신됨.
- pytest 기준선(59 failed / 4371 passed / 26 skipped) 초과 실패 없이 유지.
- SUMMARY.md 에 "이 교체가 2026-06-21 결정('이전 것 우선')을 belle 08-16 실측 근거로
  뒤집은 것"이라는 반전 사실이 명시됨.
</success_criteria>

<output>
Create `.planning/quick/260816-r7k-ref-climb-replace/260816-r7k-SUMMARY.md` when done.
SUMMARY.md 는 반드시 다음을 포함할 것: (1) 위 `<rollback_procedure>` 전문 복사,
(2) Task 1 이 출력한 S3 백업 URI + whole-file SHA-256, (3) climbfault 실증 전/후
angle 점수, (4) "2026-06-21 결정을 뒤집었다"는 반전 명시, (5) LLM 학습에 미치는 영향
(기준 모션 데이터가 바뀌므로 향후 재학습 재료 구성에 영향 있음 — memory
`feedback-always-report-llm-learning-impact` 원칙).
</output>
