---
phase: quick-260818-nc2
quick_id: 260818-nc2
slug: illustration-goal-card
date: 2026-08-18
status: planned
description: >
  일러스트 "목표 자세" 카드 — 그림과 목표 문장을 한 덩어리로 (belle 2026-08-18: "그림처럼
  이렇게 자세가 되어야 한다고, 혹은 참고하라고 써주면 조합이 좋을 듯. 캡션이 잘 들어가지면" +
  "보기 좋게 깔끔한 UI로"). 실물 확인(시뮬): 문장(goalLine)은 시트 맨 위, 그림은 두 화면 아래
  — 둘 다 있는데 떨어져 있었다. 새 문장을 만들지 않는다 — goalLine 은 이미 cueLine 에서
  잘라낸 것이라 자리만 옮긴다. 승인 목업 ②의 크롭 카드와 같은 문법(카드 → 칩 → 이미지 →
  캡션)으로 목표 카드를 하나 만든다.
wave: 1
depends_on: []
type: execute
plan: 01
autonomous: true
requirements: [QUICK-260818-NC2]
files_modified:
  - app/src/components/DeductionDetailSheet.tsx
must_haves:
  truths:
    - "그림 바로 위에 '목표 자세' 칩, 그림 바로 아래에 goalLine 문장이 같은 카드 안에 있다"
    - "goalLine 이 없으면 시트 맨 위 goalBox 도, 카드 캡션도 그리지 않는다 (없는 문장을 만들지 않는다). 그림만 있으면 그림+칩만"
    - "일러스트가 없으면(silent hidden) 카드 자체를 만들지 않는다 — 빈 카드 0"
    - "goalLine 은 한 곳에만 — 그림이 있으면 카드 캡션에, 없으면 종전대로 시트 맨 위. 같은 문장이 두 번 보이지 않는다"
    - "하드코딩 색·간격·반경 0, 테마 토큰만. 이모지 0. 새 문장 0"
    - "typecheck 통과 + 시뮬레이터에서 실제 렌더 캡처로 확인 (typecheck 는 렌더 크래시를 못 잡는다)"
  artifacts:
    - path: "app/src/components/DeductionDetailSheet.tsx"
      provides: "illustCard(칩 goalChip + 슬롯 + illustCap) + goalLine 위치 분기"
      contains: "illustCard"
---

# quick-260818-nc2 — 일러스트 목표 카드

## 왜

08-18 골격 A/B 판정에서 belle: "그림처럼 이렇게 자세가 되어야 한다고 써주면 조합이 좋을 듯".
시뮬로 실물을 열어보니 문장과 그림이 **둘 다 있는데 두 화면 떨어져** 있었다. D-05("그림 안에
글자 없음")는 이걸 막는 규칙이 아니다 — 33-A3:216 "탭-상세의 감점근거 글이 캡션 역할".

## 하지 않는 것

- 새 문장 만들기 (goalLine 재사용만)
- 일러스트 에셋 교체 (별도 사이클 — belle 판정 필요)
