---
phase: quick-260821-fe9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/quick/260821-fe9-20-a-vs-b/PREDICTION.md
  - .planning/quick/260821-fe9-20-a-vs-b/generate_arrow20.py
  - .planning/quick/260821-fe9-20-a-vs-b/compose_b.py
  - .planning/quick/260821-fe9-20-a-vs-b/meta.json
  - .planning/quick/260821-fe9-20-a-vs-b/out/
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05]

must_haves:
  truths:
    - "예측 커밋이 생성물 커밋보다 git 이력에서 앞선다 (예측 박제 먼저 — 장부 6전째)"
    - "belle 08-21 판정 원문이 PREDICTION.md 에 그대로 박제되어 있다"
    - "A(그림에 굽기) 2장 + B(앱 오버레이 합성) 1~2장이 quick dir out/ 에 존재한다"
    - "B 의 발 좌표 4점(잔상 좌/우 발, 실선 좌/우 발)이 문서에 기록되어 있다 (나중 배선 앵커 메타 재료)"
    - "모든 산출물을 Read 도구로 직접 열어본 자평이 PREDICTION.md 에 박제되어 있다 (실물 게이트)"
    - "app/ 아래 변경 0, 직전 라운드(260821-exq) dir 변경 0 — 40/60 은 아카이브 그대로 (D-01, D-05)"
  artifacts:
    - path: ".planning/quick/260821-fe9-20-a-vs-b/PREDICTION.md"
      provides: "belle 판정 원문 박제 + 생성 전 예측(축 3개, 장부 5전 1승·6전째) + 생성 후 자평"
      contains: "절대 못 쓰는건"
    - path: ".planning/quick/260821-fe9-20-a-vs-b/generate_arrow20.py"
      provides: "A 길 하네스 — HOW_GUIDE['ghost'] 화살표 굽기 프롬프트 + 잔상 20 수준 서술 (표준 라이브러리만)"
      min_lines: 60
    - path: ".planning/quick/260821-fe9-20-a-vs-b/compose_b.py"
      provides: "B 길 합성 — stage20-1 위 PIL 코랄 화살표 2개 + 소형 표기 1곳"
      min_lines: 40
    - path: ".planning/quick/260821-fe9-20-a-vs-b/meta.json"
      provides: "A/B 파일명 + B 발 좌표 4점 + 소스 이미지 경로 (나중 배선 재료)"
      contains: "coords"
    - path: ".planning/quick/260821-fe9-20-a-vs-b/out/"
      provides: "A 2장(jpg) + B 1~2장(png) + prompt_A_arrow20.txt"
  key_links:
    - from: "generate_arrow20.py"
      to: ".planning/quick/260809-ill-missing-illustrations/generate.py"
      via: "importlib.util.spec_from_file_location 로 모듈 로드 후 PROMPT 골격·헬퍼 재사용 (승인 레시피 무변경)"
      pattern: "spec_from_file_location"
    - from: "generate_arrow20.py 의 GUIDE 문단"
      to: "260818-nnm generate_how.py HOW_GUIDE['ghost'] (08-18 belle '가' 통과 변형)"
      via: "코랄 곡선 화살표 절 유지, 잔상 다리 서술만 20 수준으로 치환, 분리 절 추가"
      pattern: "coral-red curved arrows"
    - from: "compose_b.py"
      to: ".planning/quick/260821-exq-3-ghost-noarrow-20-40-60-belle/out/ref-kip-up--leg__ghost-stage20-1.jpg"
      via: "읽기 전용 소스 — 출력은 fe9 out/ 에만, exq dir 무변경"
      pattern: "ghost-stage20-1"
---

<objective>
킵업 "어떻게" 화살표 라운드 — belle 판정(08-21) 반영: 3단계 기각(40·60 절대 불가), 20 종류 채택
+ "화살표가 필요". 화살표를 넣는 두 길을 **둘 다 실물로** 만들어 belle 이 고른다. 배선 금지.

belle 판정 원문 (08-21, 이 라운드의 스펙 — PREDICTION.md 에 그대로 박제할 것):
> "절대 못 쓰는건 스테이지 60 / 스테이지 40 / 20과 같은 종류에서 화살표가 필요하겠군.
>  만약 학생이 60처럼 다리를 아예 안 벌리거나 40처럼 한쪽만 벌리면 쓸수도 있긴한데... 정해진게 없으니.
>  지금 기준으로만 봐씅ㄹ 떈 20 종류가 맞고 화살표가 필요"

확정 결정 (재논의 금지):
- D-01: 3단계 세트 기각. stage40·stage60 에셋은 절대 불가 — 260821-exq out/ 그대로 아카이브
  (삭제 금지, 무변경), 신규 제작 없음.
- D-02: 채택 계열 = 20 종류 (잔상이 실선보다 약간만 좁음). 기준 후보 =
  `260821-exq/out/ref-kip-up--leg__ghost-stage20-1.jpg`.
- D-03: 화살표 필요. 두 길을 둘 다 실물로: **A (그림에 굽기)** = Gemini 재생성, 08-18
  HOW_GUIDE["ghost"] 프롬프트 기반 + 잔상 다리를 20 수준으로 서술 수정, 2장.
  **B (앱 오버레이 합성)** = stage20-1 위에 PIL 로 코랄(#FF4B33) 곡선 화살표 2개 +
  소형 표기 1곳, 1~2장.
- D-04: 화살표 문법 (08-18 belle 고정): 시작점 = 반드시 잔상의 발. 폴 가운데 출발 금지.
  값 표기는 방향 문장만 ("20° 정도 더 벌리세요" — "좁다" 금지). 표기는 소형 1곳, 그림이
  지저분해지지 않게.
- D-05: 배선 금지 — app/ 아래 무변경. 생성물은 quick dir 안에만.

Purpose: belle 판정용 A/B 실물을 만들어 장부 6전째를 건다 (5전 1승 — 직전 판 불일치 포함).
Output: PREDICTION.md(판정 원문+예측+자평), generate_arrow20.py, compose_b.py, out/ A 2장 + B 1~2장, meta.json.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/quick/260821-exq-3-ghost-noarrow-20-40-60-belle/PREDICTION.md
@.planning/quick/260821-exq-3-ghost-noarrow-20-40-60-belle/generate_ghost3.py
@.planning/quick/260818-nnm-illustration-how-first/generate_how.py
@.planning/quick/260809-ill-missing-illustrations/generate.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: belle 판정 원문 + 예측 박제 커밋 (생성 전)</name>
  <files>.planning/quick/260821-fe9-20-a-vs-b/PREDICTION.md</files>
  <action>
    PREDICTION.md 작성 — 260821-exq PREDICTION.md 의 패턴(예측 → 자평)을 따르되 이번엔
    **판정 원문 절이 먼저** 온다. 섹션 구성:
    (1) `## belle 판정 원문 (08-21)` — objective 의 인용 3줄을 오타 포함 그대로 박제 (D-01~D-04 의 출처).
    (2) `## 그리려는 것` — A/B 두 길 요약: A = HOW_GUIDE["ghost"](08-18 "가" 통과 변형) 기반
    재생성 + 잔상 20 수준, 2장. B = exq stage20-1 위 PIL 오버레이(코랄 화살표 2개, 잔상 발 →
    같은 쪽 실선 발, 소형 표기 "20° 정도 더 벌리세요" 1곳), 1~2장.
    (3) `## 예측` — 축 3개를 반드시 숫자/명제로 박제:
      * **A 화살표 시작점이 또 폴 가운데로 갈 확률** — 08-18 1차에서 4장 전부 화살표가 폴
        가운데서 출발해 반려된 전례. 이번엔 잔상이 20 수준(실선과 거의 겹침)이라 화살표가
        짧아야 하는데, 모델이 짧은 화살표 대신 임의 위치에서 크게 그릴 위험. 2장 중 몇 장이
        위반할지 숫자로.
      * **A/B 중 belle 픽 예측** — 어느 쪽을 고를지, 근거 한 줄과 함께 박제.
      * **B 합성의 발 좌표 정확도** — 내 눈으로 잡은 픽셀 좌표 4점이 실제 발 위에 앉을지
        (자평 때 실물 대조로 판정).
    (4) `## 자를 미리 정한다` — 통과 기준 = belle 이 A/B 를 나란히 보고 화살표 문법(잔상 발
    출발·머리 분명·표기 소형 1곳·방향 문장)이 지켜진 쪽을 "쓸 수 있다"고 하는가. 개별 장의
    미려함이 아니라 화살표가 "지금 → 목표"로 읽히는가가 판정 대상.
    (5) `## 장부` — belle 눈 예측 **5전 1승, 이번이 6전째** 명기. 직전 판(260821-exq) 불일치
    내용을 그대로 적는다: 내 자평은 3단계 세트(스테이지별 추천 1장)를 냈으나 belle 은 3단계
    자체를 기각(40·60 절대 불가)하고 20 계열만 채택 + "화살표가 필요"를 냈다 — 예측 축에 없던
    판정 축이었다.
    작성 후 **생성 실행 전에** 커밋: `docs(quick-260821-fe9): A/B 화살표 예측 박제 + belle 판정 원문 (생성 전)`.
    out/ 이미지가 생기기 전에 예측이 git 이력에 박혀야 한다. 이모지 금지.
  </action>
  <verify>
    <automated>rtk git log --oneline -1 -- .planning/quick/260821-fe9-20-a-vs-b/ | grep -q "예측 박제" && grep -q "절대 못 쓰는건" /Users/kimtaesung/Dev/SunityMotion/.planning/quick/260821-fe9-20-a-vs-b/PREDICTION.md && grep -q "6전째" /Users/kimtaesung/Dev/SunityMotion/.planning/quick/260821-fe9-20-a-vs-b/PREDICTION.md && test ! -d /Users/kimtaesung/Dev/SunityMotion/.planning/quick/260821-fe9-20-a-vs-b/out</automated>
  </verify>
  <done>PREDICTION.md 에 belle 판정 원문·예측 축 3개·자·장부(5전 1승, 6전째, 직전 판 불일치 명기)가 있고, out/ 이미지가 존재하기 전에 커밋되어 있다.</done>
</task>

<task type="auto">
  <name>Task 2: A 길 — 화살표 굽기 재생성 2장 (generate_arrow20.py)</name>
  <files>.planning/quick/260821-fe9-20-a-vs-b/generate_arrow20.py, .planning/quick/260821-fe9-20-a-vs-b/out/</files>
  <action>
    (1) `generate_arrow20.py` 작성 — 260821-exq `generate_ghost3.py` 의 구조를 복사한다
    (importlib 로 260809 generate.py 로드, `G.PROMPT`/`G._orientation_hint`/`G._framing_block`/
    `G.inline_part`/`G.load_targets`/`G.asset_name`/`G.resolve`/`G.ENDPOINT` 재사용, 승인 레시피
    무변경, skip-existing 재시도, 키는 환경변수로만·로그 금지, 표준 라이브러리만). GUIDE 문단은
    260818-nnm `generate_how.py` 의 `HOW_GUIDE["ghost"]`(08-18 belle "가" 통과 변형)를 기반으로
    3가지만 수정한 단일 문단 (D-03, D-04):
      a. 잔상 다리 서술 치환: "hanging closer together and slightly bent" → exq stage20 서술
         "only slightly narrower than the solid wide straddle, the ghost legs almost as wide as
         the solid legs, just a little less open" (D-02 — 20 수준).
      b. exq 라운드에서 일을 한 분리 절 추가: "the two ghost legs must be clearly separate,
         one on each side of the pole, never merged into one".
      c. 화살표 절 유지 + 강화: "TWO coral-red curved arrows, one per leg, each starting
         exactly at the ghost foot and sweeping outward to the solid foot on the same side.
         Arrow heads must be clear. The arrows must NEVER start at the pole or the center of
         the body" — 08-18 반려 사유(폴 가운데 출발)를 명시적 금지로 박는다. 잔상이 실선과
         가까우므로 "the arrows may be short - that is correct" 절도 넣어 모델이 화살표를
         키우려고 시작점을 옮기는 것을 막는다. "NO angle marker, NO straight measurement line,
         NO number, NO text" 절 유지 — A 는 화살표만 굽고 표기는 없다 (표기는 B 만).
    CLI: `--asset` 기본 `ref-kip-up--leg`, `--n` 기본 2, `--out` 기본 `out/`. 프롬프트는
    `out/prompt_A_arrow20.txt` 로 저장, 출력 파일명 `ref-kip-up--leg__A-arrow20-{i}.jpg`.
    (2) 실행 — 키는 SSM 에서 환경변수로만 주입 (파일·로그·stdout 금지):
    `GEMINI_API_KEY=$(aws ssm get-parameter --name /sunity/motion/gemini-api-key --with-decryption
    --profile sunity-motion --region ap-northeast-2 --query 'Parameter.Value' --output text)
    python3 generate_arrow20.py --n 2 --out out/` (cwd = quick dir, 경로는 절대경로로).
    HTTP 오류(쿼터·크레딧 고갈 이력) 시 오류 원문(키 제외)을 보고하고 중단. 부분 성공이면 같은
    커맨드 재실행(기존 성공분은 건너뜀) — 그래도 미달이면 몇 장인지 사실대로 기록하고 진행.
    (3) app/ 및 260821-exq dir 은 아무것도 건드리지 않는다 (D-01, D-05).
  </action>
  <verify>
    <automated>ls /Users/kimtaesung/Dev/SunityMotion/.planning/quick/260821-fe9-20-a-vs-b/out/ref-kip-up--leg__A-arrow20-*.jpg | wc -l | grep -q 2 && grep -q "coral-red curved arrows" /Users/kimtaesung/Dev/SunityMotion/.planning/quick/260821-fe9-20-a-vs-b/out/prompt_A_arrow20.txt && grep -q "NEVER start at the pole" /Users/kimtaesung/Dev/SunityMotion/.planning/quick/260821-fe9-20-a-vs-b/out/prompt_A_arrow20.txt && python3 -c "import ast; ast.parse(open('/Users/kimtaesung/Dev/SunityMotion/.planning/quick/260821-fe9-20-a-vs-b/generate_arrow20.py').read())"</automated>
  </verify>
  <done>A 이미지 2장 + prompt_A_arrow20.txt 가 out/ 에 있고, 프롬프트에 잔상 20 서술·분리 절·잔상 발 출발·폴 출발 금지·NO text 절이 전부 들어 있으며, exq dir·app/ 변경이 0이다.</done>
</task>

<task type="auto">
  <name>Task 3: B 길 — PIL 오버레이 합성 + 실물 게이트 자평 + 전체 커밋</name>
  <files>.planning/quick/260821-fe9-20-a-vs-b/compose_b.py, .planning/quick/260821-fe9-20-a-vs-b/meta.json, .planning/quick/260821-fe9-20-a-vs-b/PREDICTION.md, .planning/quick/260821-fe9-20-a-vs-b/out/</files>
  <action>
    (1) **좌표 잡기** — 소스 `260821-exq/out/ref-kip-up--leg__ghost-stage20-1.jpg` 를 Read 도구로
    직접 열어 눈으로 픽셀 좌표 4점을 잡는다: 잔상 왼발·오른발, 실선 왼발·오른발. PIL 로 이미지
    크기를 확인해 좌표를 절대 픽셀로 기록. 이 좌표 4점은 나중 배선 시 앵커 메타 재료 —
    meta.json 과 PREDICTION.md 자평에 기록한다.
    (2) `compose_b.py` 작성 + 실행 — Pillow 는 로컬 python3 에 **이미 설치 확인됨(12.2.0)**,
    한글 폰트 `/System/Library/Fonts/AppleSDGothicNeo.ttc` 로드 **확인됨** — 신규 설치 없음.
    소스는 읽기 전용(exq dir 무변경, D-01), 출력은 `out/ref-kip-up--leg__B-overlay20-{1,2}.png`:
      * 코랄 `#FF4B33` 곡선 화살표 2개 — 각 잔상 발 → 같은 쪽 실선 발 (D-04: 잔상 발 출발,
        폴 가운데 출발 금지). quadratic bezier 를 선분 폴리라인으로 그리고(제어점은 다리가
        벌어지는 바깥 방향으로 휘게), 끝에 삼각 화살촉 폴리곤 — 머리 분명. 선 굵기는 이미지
        폭의 ~0.6%, 화살촉은 ~2.5% 수준에서 실물 보고 조정. 계단 현상이 거슬리면 2배
        슈퍼샘플 후 축소.
      * 소형 표기 1곳: `20° 정도 더 벌리세요` — 방향 문장만, "좁다" 계열 금지 (D-04).
        AppleSDGothicNeo, 소형(이미지 폭의 ~3% 폰트 크기), 가독을 위해 흰 반투명 패드 허용.
        2장은 표기 위치 변형: 1안 = 하단 중앙, 2안 = 한쪽 화살표 근처 — 실물 보고 조정 가능.
      * 좌표 4점은 스크립트 상수로 박고 주석에 "눈으로 잡음, 배선 시 앵커 메타로 대체" 명기.
    (3) `meta.json` 작성 — asset, 채택 계열(`stage20`), A/B 실존 파일명 배열, B 의
    `coords`(4점: ghostL/ghostR/solidL/solidR, 픽셀), 소스 이미지 상대 경로, 표기 문구.
    (4) **실물 게이트** — 생성·합성된 모든 이미지(A 2장 + B 1~2장)를 Read 도구로 직접 열어
    관찰을 적는다. 스크린 축: A 화살표가 정말 잔상 발에서 출발하는가(폴 가운데 위반 여부,
    장별로) / A 에 수치·텍스트 유입이 없는가 / A 잔상이 20 수준(약간만 좁게)으로 그려졌는가 /
    B 화살표 시작·끝이 실제 발 위에 앉았는가(내 좌표의 명중 여부) / B 한글 표기가 깨짐 없이
    렌더됐는가 / B 표기가 소형 1곳·방향 문장인가.
    (5) PREDICTION.md 에 `## 내 자평 (belle 판정 전, 실물 전부 열어본 뒤)` 섹션 추가 — 예측
    축 3개 대비 결과 표, 장별 관찰, A/B 중 내 픽과 근거. "★단 이건 내 눈이다 — belle 판정과
    다를 수 있다. 장부 5전 1승 — 내 자평은 답안지가 아니다" 단서 유지.
    (6) 전부 커밋: `feat(quick-260821-fe9): 20 계열 화살표 A(굽기) 2장 + B(오버레이) 합성 + 자평 박제 (belle 판정 전)`.
    belle 판정용 아티팩트 게시는 하지 않는다 — 오케스트레이터 몫 (범위 밖).
  </action>
  <verify>
    <automated>ls /Users/kimtaesung/Dev/SunityMotion/.planning/quick/260821-fe9-20-a-vs-b/out/ref-kip-up--leg__B-overlay20-*.png | wc -l | grep -qE "^ *[12]$" && python3 -c "import json; m=json.load(open('/Users/kimtaesung/Dev/SunityMotion/.planning/quick/260821-fe9-20-a-vs-b/meta.json')); assert len(m['coords'])==4" && grep -q "내 자평" /Users/kimtaesung/Dev/SunityMotion/.planning/quick/260821-fe9-20-a-vs-b/PREDICTION.md && rtk git status --porcelain -- .planning/quick/260821-fe9-20-a-vs-b/ app/ .planning/quick/260821-exq-3-ghost-noarrow-20-40-60-belle/ | wc -l | grep -q "^ *0$"</automated>
  </verify>
  <done>B 오버레이 1~2장이 out/ 에 있고, meta.json 에 발 좌표 4점이 기록되어 있으며, PREDICTION.md 에 실물 전부의 자평(예측 대조표 + A/B 내 픽)이 있고, quick dir 생성물 전체가 커밋되어 app/ 와 exq dir 변경이 0이다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로컬 → Gemini API | SSM 키가 HTTP 요청에 실림 (환경변수 경유) |
| Gemini 응답 → 로컬 파일 | 생성 이미지가 quick dir 에만 저장됨 |
| PIL 합성 입력 | 직전 라운드 아카이브 이미지를 읽기 전용으로 소비 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-fe9-01 | Information Disclosure | GEMINI_API_KEY | mitigate | SSM→환경변수로만 주입, 파일·로그·stdout·커밋 어디에도 키 문자열 금지 (exq T-exq-01 관행 승계) |
| T-fe9-02 | Tampering | 생성물 경로 | mitigate | 출력은 fe9 quick dir 안으로 한정. app/ 변경 0 + exq dir(40/60 아카이브) 변경 0 을 Task 2·3 verify 에서 기계 확인 (D-01, D-05) |
| T-fe9-SC | Tampering | 패키지 설치 | accept | 신규 패키지 0 — Pillow 12.2.0 로컬 설치·한글 폰트 로드 사전 확인 완료, 설치 태스크 없음. A 하네스는 표준 라이브러리만 |
</threat_model>

<verification>
- 예측 커밋(Task 1)이 생성물 커밋(Task 3)보다 앞선다: `rtk git log --oneline -- .planning/quick/260821-fe9-20-a-vs-b/` 에서 "예측 박제" 커밋이 "자평 박제" 커밋보다 아래(먼저)에 있다.
- `rtk git status --porcelain -- app/ .planning/quick/260821-exq-3-ghost-noarrow-20-40-60-belle/` 출력 0행 (배선 금지 + 40/60 아카이브 무변경).
- A 프롬프트에 화살표 문법이 전부 존재: `grep -c "ghost foot" out/prompt_A_arrow20.txt` ≥ 1, "NEVER start at the pole" 존재, "NO text" 존재.
- belle 판정 원문("절대 못 쓰는건")이 PREDICTION.md 에 그대로 있다.
</verification>

<success_criteria>
- A(그림에 굽기) 2장 + B(앱 오버레이 합성) 1~2장이 quick dir 에 커밋되어 belle 이 나란히 놓고 고를 수 있다 (D-02, D-03).
- 화살표 문법(잔상 발 출발·폴 가운데 금지·방향 문장·소형 표기 1곳)이 A 프롬프트와 B 합성 코드 양쪽에 박혀 있다 (D-04).
- 예측(축 3개, 장부 6전째, 직전 판 불일치 명기)이 생성 전에, 자평(실물 전부 Read 확인 + 발 좌표 명중 여부)이 생성 후에 PREDICTION.md 에 박제되어 있다.
- meta.json 이 B 발 좌표 4점을 담아 나중 배선 시 앵커 메타 재료로 쓸 수 있다.
- app/ 변경 0, exq dir(40/60 아카이브) 변경 0 — belle 판정·게시·배선·40/60 재제작은 범위 밖 (D-01, D-05).
</success_criteria>

<output>
Create `.planning/quick/260821-fe9-20-a-vs-b/260821-fe9-SUMMARY.md` when done
</output>
