---
phase: quick-260821-gb7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/assets/illustrations/ref-kip-up--leg.jpg
  - app/src/lib/illustrationHow.ts
  - app/src/lib/illustrationScene.ts
  - app/src/lib/__tests__/illustrationHow.test.ts
  - app/src/components/DefectIllustration.tsx
autonomous: true
requirements: [BELLE-0821-1, BELLE-0821-2, BELLE-0821-3, BELLE-0821-4]

must_haves:
  truths:
    - "킵업 다리 부위 상세 시트에 잔상 포함 승인 그림(stage20-1)이 뜬다 — 08-09 검수본이 아니라"
    - "학생 측정값이 있으면 코랄 곡선 화살표 2개(잔상 발 → 같은 쪽 실선 발)와 'N° 정도 더 벌리세요' 문장이 하단 중앙에 그려진다"
    - "측정값이 없거나 unit이 deg가 아니거나 차이<3° 면 화살표·문장은 안 그려지지만 그림 자체는 표시된다 (잔상이 구워져 있어 그림만으로 방법이 읽힘)"
    - "kip-up 외 다른 동작 장면·에셋은 바이트 단위로 무변경 (pdshape 등 승인 5동작 회귀 0)"
  artifacts:
    - path: "app/assets/illustrations/ref-kip-up--leg.jpg"
      provides: "belle 08-21 승인 잔상 그림 (896x1200, exq stage20-1 과 md5 동일)"
    - path: "app/src/lib/illustrationHow.ts"
      provides: "baked-ghost 앵커형(kind 판별 union) + buildHowOverlay baked 분기"
      contains: "baked"
    - path: "app/src/lib/__tests__/illustrationHow.test.ts"
      provides: "baked 오버레이 산출 + fail-closed 축 단위 테스트"
    - path: "app/src/components/DefectIllustration.tsx"
      provides: "baked 렌더 분기 (bezier 화살표 + 하단 중앙 문장 pill, 회전복사·지금 pill 생략) + 에셋별 종횡비"
  key_links:
    - from: "app/src/lib/illustrationScene.ts (ref-kip-up × leg 장면)"
      to: "asset 'ref-kip-up--leg'"
      via: "ILLUSTRATION_SCENES asset 필드"
      pattern: "asset: 'ref-kip-up--leg'"
    - from: "app/src/components/DefectIllustration.tsx HowOverlayLayer"
      to: "buildHowOverlay baked 반환형"
      via: "overlay.kind === 'baked' 분기"
      pattern: "kind"
---

<objective>
belle 08-21 승인 실행: 잔상 포함 킵업 그림 20-1 을 앱에 걸고(BELLE-0821-1), 화살표·수치
문장은 앱이 그린다 — B 방식(BELLE-0821-2). 표기 = "N° 정도 더 벌리세요" 하단 중앙 소형
1곳, N = 학생 측정값(BELLE-0821-3). 잔상은 그림에 구워져 있으므로 이 에셋에서 회전복사
잔상 경로는 비활성(BELLE-0821-4).

Purpose: "어떻게" 일러스트를 belle 승인 실물과 동일한 겉모습으로 처음 실배선한다.
문법 승인 ≠ 그림 승인 원칙에 따라 **승인된 그 바이트**(exq stage20-1)를 무재인코딩으로
싣고, 화살표 기하는 승인 실물을 만든 compose_b.py 를 그대로 옮긴다.

Output: 에셋 1장 교체 + illustrationHow baked 형 + DefectIllustration baked 분기 +
단위 테스트 + 시뮬 캡처(가능 시). OTA 발행 금지 — belle 이 시뮬 확인을 본 뒤에만 별도 결정.

이연(범위 밖 — 플랜에 넣지 않음): 발전 캡션("저번보다 더 벌어졌어요") = 직전 분석 조회
+ 노이즈 문턱 측정 필요.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@app/CLAUDE.md
@app/src/lib/illustrationScene.ts
@app/src/lib/illustrationHow.ts
@app/src/components/DefectIllustration.tsx
@app/src/components/DeductionDetailSheet.tsx
@.planning/quick/260821-fe9-20-a-vs-b/meta.json
@.planning/quick/260821-fe9-20-a-vs-b/compose_b.py
</context>

<interface_notes>
확정 수치 (플래너 실측 완료 — 재계산 금지):

- 승인 원본: `.planning/quick/260821-exq-3-ghost-noarrow-20-40-60-belle/out/ref-kip-up--leg__ghost-stage20-1.jpg`
  = **896x1200** (sips 실측). 기존 번들 에셋 20장은 전부 720x964 → `ASSET_H_OVER_W = 964/720`
  단일 상수가 이 에셋에는 안 맞는다(1200/896 = 1.33929 vs 1.33889). DefectIllustration 헤더
  주석이 이미 경고한 전제("비율이 다르면 ASSET_H_OVER_W 와 cover 를 함께 다시 봐야 한다").
- 발 좌표 (meta.json, 896x1200 절대 픽셀 → 0~1 비율 환산값):
  - ghostL (213,1043) → [0.2377, 0.8692] / solidL (110,1015) → [0.1228, 0.8458]
  - ghostR (645,1035) → [0.7199, 0.8625] / solidR (742, 990) → [0.8281, 0.8250]
- 화살표 기하 (compose_b.py — 승인 실물 `out/ref-kip-up--leg__B-overlay20-1.png` 의 생성 코드):
  - quadratic bezier, 제어점 = 두 끝점 중점 + offset (좌: −8px, 우: +8px, 공통 +46px)
    → 비율 offset: 좌 [-0.0089, +0.0383], 우 [+0.0089, +0.0383]
  - 선 굵기 ≈ 폭의 0.6%, 화살촉 ≈ 폭의 2.5%, 촉 반각 0.45 rad, 촉 방향 = 제어점→끝점 접선
  - 표기 = 하단 중앙 1곳 (variant 1 채택본): 텍스트 상단 y ≈ (1200−62)/1200 = 0.948
    → 앱에서는 기존 `dirPill` 스타일(cardBg 채움 + brand 테두리·글자) 재사용, `bottom`
    기준 하단 중앙 배치. "지금" pill·잔상 점(Circle)은 **그리지 않는다** (승인 실물에 없음).
- delta 의미 (illustrationHow 기존 docstring 계승): kip-up split_angle record 는
  기준 대비 차이형(measured=deficit, target=0)이 온다. delta = |target − measured| 로
  두 모양 다 성립. N = Math.round(delta).
- 캡션(actionLine) 배선은 DeductionDetailSheet 08-18 산물 그대로 — 이번 무접촉.
- 테스트 실행: `node --test app/src/lib/__tests__/*.test.ts` (Node 24 type stripping,
  illustrationScene.test.ts 헤더 선례). 기존 1건 기지 실패(pdshape/arm provenance)는
  이 작업 전부터 실패 — 회귀 아님으로 인정.
</interface_notes>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 승인 에셋 교체 + illustrationHow baked-ghost 형 + 장면 표 배선</name>
  <files>app/assets/illustrations/ref-kip-up--leg.jpg, app/src/lib/illustrationHow.ts, app/src/lib/illustrationScene.ts, app/src/lib/__tests__/illustrationHow.test.ts</files>
  <behavior>
    - buildHowOverlay('ref-kip-up--leg', 20, 0, 'deg', aspect) → kind 'baked' 오버레이:
      arrows 2개 (from=잔상 발 비율좌표, to=같은 쪽 실선 발, ctrl=중점+offset),
      directionText === '20° 정도 더 벌리세요', deltaDeg === 20
    - 절대각형 입력 (measured 94, target 71) → deltaDeg 23 (두 record 모양 다 성립)
    - fail-closed 유지: 미등록 asset → null / unit 'cm' → null / measured 없음 → null /
      delta 2.9 → null / NaN → null
    - illustrationAssetForPart('ref-kip-up', 'leg') === 'ref-kip-up--leg' (장면 표 배선)
  </behavior>
  <action>
    1. 에셋 교체 (BELLE-0821-1): `cp` 로 exq 원본 `ref-kip-up--leg__ghost-stage20-1.jpg` 를
       `app/assets/illustrations/ref-kip-up--leg.jpg` 에 덮어쓴다 — **재인코딩·리사이즈 금지**
       (문법 승인 ≠ 그림 승인: belle 이 보드에서 3회 확인한 그 바이트가 실려야 한다).
       교체 후 md5 로 원본과 동일 확인. 종전 08-18 solid1 본은 belle 반려품("그림이 이게
       아닌데")이고 장면 표가 가리킨 적 없으며 git 이력(52d88c95)에 남으므로 교체가 맞다.
       다른 19장 에셋 무접촉.
    2. illustrationHow.ts (BELLE-0821-2/-4): HowAnchors 를 판별 union 으로 확장 —
       기존 회전복사형에 `kind: 'rotate'`, 신규 `kind: 'baked'` 형은
       `{ arrows: readonly { from: Frac; to: Frac; ctrlOffset: Frac }[], directionSentence }`.
       clip/pivot/inwardSign/shares/frontClip/nowLabelOffsetY 는 baked 형에 없다(잔상이
       그림에 구워져 있어 회전복사 불필요 — 헤더 주석에 belle 08-21 결정 박제).
       `HOW_ANCHORS['ref-kip-up--leg']` 의 기존 rotate 데이터를 baked 형으로 **대체**
       (한 에셋에 앵커 두 벌 금지, 구 좌표는 720x964 solid1 실측이라 새 그림에 무효):
       interface_notes 의 비율 좌표 4점 + ctrlOffset 2벌 + directionSentence
       '{n}° 정도 더 벌리세요' ("좁다" 금지, BELLE-0821-3).
       HowOverlay 도 union 으로: baked 반환형 = `{ kind: 'baked', deltaDeg, directionText,
       arrows: readonly { from: Frac; ctrl: Frac; to: Frac }[] }` — ctrl 계산(중점+offset)은
       순수 lib 이 소유해 node --test 로 고정한다. fail-closed 게이트(asset/unit/finite/
       delta<3)는 두 kind 공통 경로에 그대로 유지. rotate 기계(rotateFrac, 기존 반환 필드)는
       타입에 kind 태그만 붙이고 로직 무변경 보존.
    3. illustrationScene.ts: ref-kip-up × ['leg'] 장면의 `asset` 을 'ref-kip-up' →
       'ref-kip-up--leg' 로 변경. provenance 갱신 — "belle 08-21 승인(보드 실물 3회 확인):
       exq stage20-1 잔상 포함 그림, 좌·우 다리 잔상→실선 벌림이 주제, 화살표·수치 문장은
       앱 렌더(fe9 B-overlay20-1 겉모습)" 취지 1줄. 기존 테스트 축 13 이 parts 토큰의 부위
       어휘를 provenance 에서 찾으므로 '다리' 어휘 포함 필수. 08-18 미승인 주석(114~116행)은
       승인 사실로 대체. 다른 장면 항목 무변경.
    4. 신규 `app/src/lib/__tests__/illustrationHow.test.ts`: behavior 블록의 축만 —
       node:test + node:assert/strict + `.ts` 확장자 import (illustrationScene.test.ts
       선례·헤더 규약 동일). 수치 채우기 금지, 이모지 금지.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && md5 -q app/assets/illustrations/ref-kip-up--leg.jpg .planning/quick/260821-exq-3-ghost-noarrow-20-40-60-belle/out/ref-kip-up--leg__ghost-stage20-1.jpg | sort -u | wc -l | grep -qx ' *1' && node --test app/src/lib/__tests__/illustrationHow.test.ts app/src/lib/__tests__/illustrationScene.test.ts</automated>
  </verify>
  <done>에셋 md5 = 승인 원본 md5. buildHowOverlay 가 baked 오버레이를 정확한 좌표·문장으로
  반환하고 fail-closed 5축이 전부 null. 장면 표가 새 에셋을 가리키며 illustrationScene 기존
  테스트 통과 (asset 중복 0 · provenance 부위 어휘 게이트 포함). 다른 동작 장면 diff 0.</done>
</task>

<task type="auto">
  <name>Task 2: DefectIllustration baked 렌더 분기 — 승인 실물과 같은 겉모습</name>
  <files>app/src/components/DefectIllustration.tsx</files>
  <action>
    1. 에셋별 종횡비: `ASSET_H_OVER_W` 단일 상수 옆에 override 맵(데이터)
       `{ 'ref-kip-up--leg': 1200/896 }` 추가, 기본값 964/720. 카드 높이 계산
       (capW·h)과 buildHowOverlay 의 aspect 인자 모두 매치된 에셋의 값을 쓴다.
       헤더의 "720x964 전제" 주석에 이 예외를 1줄 추기.
    2. HowOverlayLayer 에 overlay.kind 분기:
       - 'rotate' → 기존 렌더 그대로 (잔상 클립·회전, frontClip, 직선 화살표, 지금 pill).
       - 'baked' (BELLE-0821-2/-4) → 잔상 SvgImage·frontClip·지금 pill·잔상 점 **생략**.
         화살표 2개는 react-native-svg `Path` 로 `M from Q ctrl to` (compose_b 와 같은
         quadratic bezier), stroke = colors.brand, strokeWidth ≈ Math.max(2, w*0.006),
         strokeLinecap round, fill none. 화살촉 = Polygon, 크기 ≈ w*0.025, 반각 0.45 rad,
         방향 = 제어점→끝점 접선 (compose_b `_arrow` 이식 — 촉이 앉는 만큼 몸통 끝을
         살짝 줄이는 처리 포함, SVG 라 stroke 가 촉 아래 깔려도 무방하면 생략 가능).
       - 수치 문장 (BELLE-0821-3): 기존 `dirPill`/`dirText` 스타일 재사용하되 baked 에서는
         상단(top: h*0.06)이 아니라 **하단 중앙** — `bottom` ≈ h*0.035 (승인 실물의
         y≈0.948 텍스트 상단과 등가). 텍스트 = overlay.directionText.
    3. 색·간격 전부 theme 토큰 (colors.brand 등) — hex/수치 하드코딩 금지 (app/CLAUDE.md).
       이모지 금지. 주석에 belle 08-21 결정과 fe9 승인 실물 경로 박제.
    4. VERIFIED_ILLUSTRATIONS['ref-kip-up--leg'] require 경로는 파일명 불변이라 무변경 —
       64~66행의 "미승인" 주석만 승인 사실로 갱신. DeductionDetailSheet(actionLine 캡션)
       무접촉.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck</automated>
  </verify>
  <done>typecheck 통과. baked 분기가 곡선 화살표 2개 + 하단 중앙 문장 pill 만 그리고
  회전복사·frontClip·지금 pill 코드는 rotate 분기에만 남는다. 신규 하드코딩 색 0.</done>
</task>

<task type="auto">
  <name>Task 3: 전량 회귀 + 시뮬 실렌더 확인 (정직 폴백)</name>
  <files>.planning/quick/260821-gb7-20-1-belle-08-21/how_card_20-1.png</files>
  <action>
    1. 전량 게이트: `npm run typecheck` (app/) + `node --test app/src/lib/__tests__/*.test.ts`.
       기지 실패 1건(pdshape/arm provenance — 08-18 이전부터)만 허용, 그 외 회귀 0.
       git diff 로 kip-up 외 에셋·장면 무변경 확인 (`git status app/assets/illustrations/`
       에 ref-kip-up--leg.jpg 만).
    2. 시뮬 실렌더 (08-18 how_card_real.png 선례): iOS 시뮬레이터에서 앱을 띄워 킵업 다리
       부위 상세 시트를 열고, 잔상 그림 + 화살표 2개 + 하단 중앙 수치 문장이 승인 실물
       (fe9 out/ref-kip-up--leg__B-overlay20-1.png)과 같은 겉모습인지 캡처해
       `.planning/quick/260821-gb7-20-1-belle-08-21/how_card_20-1.png` 로 저장.
       UI 변경은 시뮬 확인이 원칙(typecheck 는 렌더 크래시를 못 잡음).
    3. 시뮬 기동 불가 시 **"미실증" 을 SUMMARY 에 명기** — 거짓 실증 금지. 오케스트레이터가
       이어서 한다.
    4. **OTA 발행 금지** — belle 이 시뮬 확인을 본 뒤에만 별도 결정. eas update 실행 0.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck && node --test src/lib/__tests__/*.test.ts 2>&1 | tail -20</automated>
    <human-check>캡처(또는 미실증 명기)가 SUMMARY 에 있고, 겉모습이 fe9 승인 실물과 일치</human-check>
  </verify>
  <done>typecheck 통과 + 테스트 기지 실패 1건 외 회귀 0 + kip-up 외 파일 diff 0.
  캡처 저장 또는 SUMMARY 에 "미실증" 명기. OTA 미발행.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firestore doc → 앱 렌더 | measured/target/unit 은 백엔드 산출값 — 기존 normalize 경로로 이미 방어적 파싱됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-gb7-01 | Tampering | buildHowOverlay 입력 | mitigate | 기존 fail-closed 게이트 유지 (non-finite/unit/delta 검증 — Task 1 테스트로 봉인) |
| T-gb7-SC | Tampering | 패키지 설치 | accept | 신규 패키지 설치 0 (react-native-svg 기설치분만 사용) — 공급망 표면 무변화 |
</threat_model>

<verification>
- `npm run typecheck` (app/) 통과
- `node --test app/src/lib/__tests__/*.test.ts` — 기지 실패 1건(pdshape/arm provenance) 외 전부 통과
- 번들 에셋 md5 = 승인 원본 md5 (재인코딩 0)
- `git diff` 범위 = files_modified 5개뿐 (다른 동작 에셋·장면 무접촉)
- 시뮬 캡처 저장 또는 SUMMARY "미실증" 명기 · OTA 미발행
</verification>

<success_criteria>
- belle 승인 4항목 전부 구현: 승인 그림 배선(1) + 앱 렌더 화살표·표기 B 방식(2) +
  수치 문장 하단 중앙(3) + 회전복사 비활성·메타 좌표 앵커(4)
- fail-closed 유지: 값 없음/<3° → 오버레이 없음, 그림은 표시
- pdshape 등 다른 동작 회귀 0 · 캡션(actionLine) 배선 유지
</success_criteria>

<output>
`.planning/quick/260821-gb7-20-1-belle-08-21/260821-gb7-SUMMARY.md` 작성 — 시뮬 실증
여부(캡처 경로 또는 "미실증")와 이연 항목(발전 캡션) 명기.
</output>
