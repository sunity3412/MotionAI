---
phase: quick-260821-ls0
quick_id: 260821-ls0
slug: climb-combo-ehz-sweep
date: 2026-08-21
status: planned
description: climb·climbfault·combo 발굴 스윕 — ehz discover_sweep.py 하네스에 3동작을 정식 등재(c3m SUMMARY 가 박제한 다음 사이클 절차)하고 스윕을 돌려 판정 재료를 생산한다. 재료 생산만 — 프로덕션(S3 쓰기/Firestore 쓰기/backend) 무접촉, belle 판정 요청 없음.
wave: 1
depends_on: []
type: execute
plan: 01
autonomous: true
requirements: [QUICK-260821-LS0]
tags: [discovery, sweep, climb, combo, machine-eye, gemini, silence-proof]
files_modified:
  - .planning/quick/260814-ehz-5/discover_sweep.py
  - .planning/quick/260814-ehz-5/evidence/climb/
  - .planning/quick/260814-ehz-5/evidence/climbfault/
  - .planning/quick/260814-ehz-5/evidence/combo/
  - .planning/quick/260814-ehz-5/evidence/VISUAL-REVIEW.md
  - .planning/quick/260813-wif-knee-discovery/DISCOVERY-LEDGER.md
  - .planning/quick/260821-ls0-climb-combo-ehz-0/DISCOVERY-SHEET.md
  - .planning/quick/260821-ls0-climb-combo-ehz-0/260821-ls0-SUMMARY.md
must_haves:
  truths:
    - "climb·climbfault·combo 3동작이 discover_sweep.py SWEEP_JOBS/RECORD_INVENTORY 에 정식 등재되고, 등재 후 기존 5동작 결과가 무회귀임이 기계적으로 확인된다 (확정 1)"
    - "3동작 각각 소스 게이트 판정 + P35 doc 전 record 스캔 결과가 evidence/{m}/candidates.json 실물로 남는다 — 발굴이든 침묵이든 실행 수치·사유가 기록된다 (확정 2, 침묵도 증명)"
    - "동작별 사전 추천 3행(침묵 = '발굴 0 — 추천 없음' 도 1건)이 belle 에게 보이기 전에 DISCOVERY-LEDGER 에 append 커밋된다 (게이트: 예측 박제)"
    - "판정 재료가 /Users/Shared/sunity-discovery-sweep-260821/ 에 안내.md 와 함께 준비되고, belle 판정 요청·재촉은 하지 않는다 (확정 5)"
    - "S3 는 GET only, Firestore 쓰기 0, backend/ diff 0 (확정 6)"
    - "Gemini 호출 회수가 로그 실물로 남고 SUMMARY 에 LLM 사용·학습 영향 절이 있다 (확정 3) — 호출 0 이어도 0 을 명기"
  artifacts:
    - path: ".planning/quick/260814-ehz-5/discover_sweep.py"
      provides: "SWEEP_JOBS/RECORD_INVENTORY 에 climb·climbfault·combo 추가 (기존 행 무수정, 순수 추가)"
      contains: "fixtures/phase15/climb/fault.mp4"
    - path: ".planning/quick/260814-ehz-5/evidence/climb/candidates.json"
      provides: "climb 소스 게이트 + record 전수 스캔 산출 (침묵이면 recordCount 와 사유)"
    - path: ".planning/quick/260814-ehz-5/evidence/climbfault/candidates.json"
      provides: "climbfault 소스 게이트 + 스캔 산출"
    - path: ".planning/quick/260814-ehz-5/evidence/combo/candidates.json"
      provides: "combo 소스 게이트 + 스캔 산출"
    - path: ".planning/quick/260821-ls0-climb-combo-ehz-0/DISCOVERY-SHEET.md"
      provides: "ehz 형식 발굴/침묵 시트 — 기계 증명 요지 / 동작별 1행 요약 / 발굴·침묵 절 / 한계 / 판정 대기 항목 (요청 아님)"
    - path: ".planning/quick/260813-wif-knee-discovery/DISCOVERY-LEDGER.md"
      provides: "새 절 append — 동작별 사전 추천 3행 + belle 판정 기입란(공란) + 승격 실적 집계 행 9~11"
    - path: "/Users/Shared/sunity-discovery-sweep-260821/안내.md"
      provides: "belle 판정 재료 안내 (ehz 선례 — 한국어 파일명, 카드 없으면 시트 사본과 결과 요약)"
  key_links:
    - from: ".planning/quick/260814-ehz-5/discover_sweep.py"
      to: "backend/scripts/p35_extract_align.py"
      via: "S3 키/motion_id 정본 — climb(correct.mp4/ref-climb), climbfault(fault.mp4/ref-climb), combo(correct.mp4/ref-combo)"
      pattern: "fixtures/phase15/combo/correct.mp4"
    - from: ".planning/quick/260813-wif-knee-discovery/DISCOVERY-LEDGER.md"
      to: "git 커밋 (belle 판정 전)"
      via: "눈 PASS 산출 직후 append 커밋 — /Users/Shared 복사·보고보다 먼저"
    - from: ".planning/quick/260814-ehz-5/evidence/{m}/candidates.json"
      to: ".planning/phases/35-server-rendered-comparison-video/data/{m}/doc.json"
      via: "scan() 이 result.deductionBreakdown.records 를 읽는다 — 스냅샷이 곧 재료"
      pattern: "deductionBreakdown"
---

<objective>
belle 08-21 지시("climb·combo 스윕 돌려놔") 이행 — 08-14 ehz 발굴 일반화 하네스
(`discover_sweep.py`)를 그대로 climb·climbfault·combo 에 돌려 판정 재료를 생산한다.
c3m SUMMARY 가 박제한 다음 사이클 절차(SWEEP_JOBS/RECORD_INVENTORY 정식 등재 +
evidence 커버리지 + `--check` PASS)를 그대로 따른다. 새 하네스 발명 0.

재료 생산만이다: S3 GET only · Firestore 쓰기 0 · backend/ 무접촉 · 채점 무접촉.
판정 재료는 준비만 하고 belle 판정 요청·재촉은 하지 않는다 (belle 이 볼 때 봄).

**플래너 실측 (2026-08-21, 커밋된 P35 doc.json 직접 판독):** 3동작 모두
`result.deductionBreakdown.records` 가 **0 건**이다 —
- climb (r7k 재생성본, score 100): deductionBreakdown 존재, `records` 키 부재
- combo (c3m 생성본, score 100): `records: []`
- climbfault (r7k 재생성본, score 86): **deductionBreakdown 키 자체 부재** —
  faultZoomComparisons 2건(right_shoulder 19.8°, right_knee 15.3°)은 있는데
  감점 record 가 없다. r7k 회수 스냅샷의 필드 결손 가능성이 있어 Firestore
  원본 읽기 대조가 선행돼야 침묵을 주장할 수 있다 (Task 1).

따라서 예상 산출은 "3동작 침묵 증명"일 가능성이 높다 — 그래도 스윕은 정직하게
돌린다 (침묵도 실행 수치로 증명, ehz elbow/peterpan 선례). 실행 시점 재실측이
플래너 실측과 다르면 (record > 0) 그 값으로 등재하고 발굴 전체 플로우
(육안→눈→렌더)를 그대로 탄다. **faultZoomComparisons 를 새 record 소스로
발명하는 것은 금지** — 하네스의 record 소스는 deductionBreakdown.records 뿐이다.

Output: 하네스 등재 커밋 + evidence 3동작 + DISCOVERY-SHEET + LEDGER 사전 박제
커밋 + /Users/Shared 판정 재료 + SUMMARY.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/quick/260814-ehz-5/discover_sweep.py
@.planning/quick/260814-ehz-5/260814-ehz-SUMMARY.md
@.planning/quick/260814-ehz-5/DISCOVERY-SHEET.md
@.planning/quick/260816-c3m-climb-combo-p35/260816-c3m-SUMMARY.md
@.planning/quick/260813-wif-knee-discovery/DISCOVERY-LEDGER.md
@backend/scripts/p35_extract_align.py

인터페이스 (실행자가 탐색 없이 쓰는 확정값):
- SWEEP_JOBS 추가 3행 (정본 = p35_extract_align.py JOBS + c3m verify_source_gate.py):
  - climb: ("fixtures/phase15/climb/correct.mp4", "reference/ref-climb.mp4", "ref-climb")
  - climbfault: ("fixtures/phase15/climb/fault.mp4", "reference/ref-climb.mp4", "ref-climb")
  - combo: ("fixtures/phase15/combo/correct.mp4", "reference/ref-combo.mp4", "ref-combo")
- RECORD_INVENTORY 추가 3행: 실행 시점에 커밋된 doc.json 의
  result.deductionBreakdown.records 길이를 재어 그 값을 등재 (플래너 실측 0/0/0).
- SWEEP_CATEGORY 는 미등재 (렌더 스탬프에만 영향 — ehz 주석 근거, 발명 금지).
- 하네스 스테이지: --fetch --scan --pairsheet --eye --render --check,
  --motions climb,climbfault,combo 로 범위 한정, --cache-root 는 실행 세션
  scratchpad 하위 (구 세션 UUID 하드코딩 금지).
- check() 는 SWEEP_JOBS 전체(8동작)를 검증한다 — 새 3동작 candidates.json 이
  생기기 전(Task 1 시점)의 --check FAIL 은 정상이고, 최종 PASS 는 Task 2 종료 게이트.
- ehz evidence/VISUAL-REVIEW.md 는 append only. wif LEDGER 도 append only
  (기존 행 1~8, 4'/6' 무수정 — 새 절 + 행 9~11 추가만).
- Gemini 키 = SSM --profile sunity-motion (하네스 _ensure_gemini_key 그대로,
  키 값 로그 금지). Firestore 읽기 = 하네스 _ensure_firebase_env 그대로.
</context>

<tasks>

<task type="auto">
  <name>Task 1: 하네스 3동작 등재 + 기존 5동작 무회귀 게이트 + climbfault 스냅샷 대조</name>
  <files>.planning/quick/260814-ehz-5/discover_sweep.py</files>
  <action>
1. **재실측 (verify-the-target 규율):** 커밋된 P35 doc.json 3건
   (.planning/phases/35-server-rendered-comparison-video/data/{climb,climbfault,combo}/doc.json)
   에서 result.deductionBreakdown.records 길이를 python 으로 직접 재어 출력을 박제한다.
   플래너 실측(0/0/0)과 다르면 측정값을 쓴다 — 플래너 수치를 믿고 건너뛰지 않는다.

2. **climbfault Firestore 원본 대조 (읽기만):** climbfault doc.json 의
   analysisId/videoKey 에서 uid(SIM_UID fvcNXzEqKjgqVxRPVSj1iwFnIpn2)·analysisId 를
   파싱해, 하네스의 _ensure_firebase_env() + sunity_shared.firestore_admin 내부
   클라이언트로 users/{uid}/analyses/{analysisId} 문서의 result.deductionBreakdown
   존재 여부와 records 길이만 읽는다 (쓰기 0). 판정 분기:
   - Firestore 도 부재/0건 → 진짜 침묵. 시트 재료로 박제.
   - Firestore 에 records 가 있는데 스냅샷에 없다 → **스냅샷 결손**. 이 경우
     climbfault 는 "침묵"이 아니라 "재료 결손 — doc 재회수는 별건(c3m/r7k 계보)"으로
     시트·SUMMARY 에 기록하고, doc.json 재회수는 이 사이클에서 하지 않는다
     (P35 data 변경은 범위 밖). 스윕 자체는 커밋된 스냅샷 기준으로 계속 돈다.
   결과(존재 여부·길이·판정)를 텍스트로 박제해 Task 2 시트에 넣는다.

3. **등재 (순수 추가만):** discover_sweep.py 의 SWEEP_JOBS 에 위 인터페이스의
   3행, RECORD_INVENTORY 에 1번 측정값 3행을 추가한다. 기존 5동작 행·임계 상수·
   로직은 한 줄도 수정/삭제하지 않는다. 허용되는 유일한 로직 외 수정 =
   check()/모듈 docstring 의 "5 motions"/"13/13" 하드코딩 문자열을 동적 값
   (len(SWEEP_JOBS), sum(RECORD_INVENTORY.values()))으로 바꾸는 것 — 이것도
   생략 가능하면 생략 (check 의 총 커버리지 식은 이미 sum() 기반이라 동작에 지장 없음).

4. **무회귀 게이트 (확정 1 — 채택본 재현, 비파괴):**
   a. rtk git diff 로 discover_sweep.py 변경이 순수 추가(+행)뿐임을 확인 —
      삭제/변경(-행)이 기존 코드에 걸리면 STOP.
   b. 채택본 스캔 재현: --fetch --scan --motions pdshapefault --cache-root
      <scratchpad>/ehz_cache 실행 → 재생성된 evidence/pdshapefault/candidates.json 을
      HEAD 커밋본과 비교 — meta.generated(타임스탬프) 필드만 제외하고 dict 동등,
      stills *.jpg 는 md5 전건 동일해야 PASS. 특히 belle 채택 순간 r00/cand17B
      (u16.4667/r15.1333)와 r03/cand13B 행이 동일해야 한다.
   c. 비교 후 git restore 로 .planning/quick/260814-ehz-5/evidence/ 를 원복하고
      git status --porcelain .planning/quick/260814-ehz-5/ 가 discover_sweep.py
      변경 외 빈 출력임을 확인 (ehz 채택 evidence 를 덮어쓴 채 남기지 않는다).
      Gemini 를 부르는 --render 재현은 하지 않는다 — 새 세션에서는 눈 memo 가
      비어 실호출이 발생하고 채택 카드 실물을 덮어쓸 위험이 있어, 스캔 층
      byte-재현 + --check(Task 2)로 갈음한다 ("가능한 것" 범위, 확정 1).
   d. 무회귀 확인 불가/불일치 시 STOP — 등재 커밋을 밀어붙이지 않고 보고.

5. 커밋: "feat(quick-260821-ls0): discover_sweep 에 climb·climbfault·combo 등재 (기존 5동작 무회귀 확인)"
  </action>
  <verify>
    <automated>rtk git diff HEAD~1 -- .planning/quick/260814-ehz-5/discover_sweep.py | grep -c '^-[^-]' 가 0 (순수 추가) && python3 -c "import importlib.util,pathlib; s=importlib.util.spec_from_file_location('ds', '.planning/quick/260814-ehz-5/discover_sweep.py'); m=importlib.util.module_from_spec(s); s.loader.exec_module(m); assert set(['climb','climbfault','combo']) <= set(m.SWEEP_JOBS) and set(['climb','climbfault','combo']) <= set(m.RECORD_INVENTORY); print('REGISTERED')"</automated>
  </verify>
  <done>3동작 등재 커밋 존재, diff 는 순수 추가, pdshapefault 스캔 byte-재현 PASS + evidence 원복 확인, climbfault Firestore 대조 결과 박제.</done>
</task>

<task type="auto">
  <name>Task 2: 스윕 실행 + 실물 게이트 + DISCOVERY-SHEET + 사전 추천 LEDGER 박제 커밋</name>
  <files>.planning/quick/260814-ehz-5/evidence/climb/, .planning/quick/260814-ehz-5/evidence/climbfault/, .planning/quick/260814-ehz-5/evidence/combo/, .planning/quick/260814-ehz-5/evidence/VISUAL-REVIEW.md, .planning/quick/260821-ls0-climb-combo-ehz-0/DISCOVERY-SHEET.md, .planning/quick/260813-wif-knee-discovery/DISCOVERY-LEDGER.md</files>
  <action>
1. **스윕 실행:** discover_sweep.py 를 --motions climb,climbfault,combo 로
   --fetch → --scan → (후보 있으면) --pairsheet → --eye → --render 순으로 실행.
   --cache-root 는 실행 세션 scratchpad 하위. AWS_PROFILE=sunity-motion.
   combo 는 62s/318MB 영상이라 30fps 프레임 추출이 무겁다 — scratchpad 용량만
   유의 (휘발 캐시, 보존 아님).
   - 소스 게이트 FAIL 동작은 하네스가 "로컬 불가 — Pod 필요"로 정직 박제하고
     스캔을 생략한다 — 억지 우회 금지 (확정 2).
   - record 0 동작은 candidates.json 에 recordCount=0/inventory 대조가 남는다 —
     그것이 침묵 증명의 실행 수치다. 조작·보충 발명 0.
   - **Gemini 게이트 (확정 게이트):** 눈 호출 실패/키 문제 시 STOP — 부분 결과로
     "스윕 완료" 선언 금지, 어디까지 갔는지 명기. 호출 회수는 evidence/{m}/
     eye_calls.log 실물 (record 당 상한 16 코드 강제). 후보 0 이면 호출 0 —
     0 도 로그·SUMMARY 에 명기.

2. **실물 게이트 (확정 게이트):** 생성된 stills/짝시트/카드 이미지를 **전건
   Read 도구로 직접 열어** 자평한다 — 깨진 렌더(판독 불가/국면 상이/경계)를
   판정 재료로 내보내지 않는다. 압축 후보가 생기면 ehz 규율대로 육안 판정을
   evidence/VISUAL-REVIEW.md 에 append (visual_verdicts.json 경로 포함,
   기존 내용 무수정). 후보 0 이면 이 게이트는 "열 실물 없음"을 명기.

3. **DISCOVERY-SHEET.md 작성** (이 quick 디렉터리, ehz 형식 그대로):
   §1 기계 증명 요지(소스 게이트 3/3 판정, record 커버리지, 눈 호출 수) /
   §2 동작별 1행 요약 / §3+ 동작별 발굴 성립 또는 침묵 증명(탈락·침묵 사유 분포 —
   record 0 이면 "P35 doc 스냅샷 감점 record 0" + Task 1 의 Firestore 대조 결과,
   faultZoomComparisons 는 별개 층이라 record 소스가 아님을 명기) /
   한계 정직 박제 / 마지막 절은 "belle 판정 **대기** 항목" — 요청·재촉 문구 금지.

4. **사전 추천 박제 (게이트 — belle 에게 보이기 전 커밋):** wif DISCOVERY-LEDGER.md 에
   새 절 append — 사이클 260821-ls0 헤더 + 기계 요지 + 동작별 실행자 추천 표 3행
   (침묵 = "발굴 0 — 추천 없음"도 추천 1건으로 계상, 눈 PASS 있으면 정확히 1안 추천) +
   belle 판정 기입란(공란 — 선기입 금지) + 승격 실적 집계 행 9(climb)/10(climbfault)/11(combo)
   ("(판정 대기)"). 기존 행·절 무수정.

5. 커밋 (LEDGER + 시트 + evidence 3동작 + VISUAL-REVIEW append 를 하나로):
   "feat(quick-260821-ls0): climb·climbfault·combo 스윕 — 사전 추천 박제 (belle 판정 전)"
   그 뒤 python3 .planning/quick/260814-ehz-5/discover_sweep.py --check 로
   8동작 전체 게이트 PASS 확인 (FAIL 이면 evidence 보완 후 재확인 — check 를
   통과시키려고 RECORD_INVENTORY 를 측정값과 다르게 고치는 것은 금지).
  </action>
  <verify>
    <automated>python3 .planning/quick/260814-ehz-5/discover_sweep.py --check && ls .planning/quick/260814-ehz-5/evidence/climb/candidates.json .planning/quick/260814-ehz-5/evidence/climbfault/candidates.json .planning/quick/260814-ehz-5/evidence/combo/candidates.json && rtk grep -c "260821-ls0" .planning/quick/260813-wif-knee-discovery/DISCOVERY-LEDGER.md</automated>
  </verify>
  <done>--check PASS(8동작), 3동작 candidates.json 실물, DISCOVERY-SHEET 존재, LEDGER 에 사전 추천 3행+집계 행 9~11 append 커밋이 belle 노출 전에 존재. 눈 호출 수(0 포함)가 로그로 남음.</done>
</task>

<task type="auto">
  <name>Task 3: 판정 재료 /Users/Shared 준비 + 무접촉 검증 + SUMMARY</name>
  <files>.planning/quick/260821-ls0-climb-combo-ehz-0/260821-ls0-SUMMARY.md</files>
  <action>
1. **판정 재료 복사 (ehz 선례):** /Users/Shared/sunity-discovery-sweep-260821/ 에
   안내.md + 판정 재료를 한국어 파일명으로 복사.
   - 눈 PASS 카드가 있으면: 동작·추천·관절·시각이 드러나는 파일명으로 카드 PNG +
     전신 짝 JPG (ehz 명명 예: 파워스핀_추천_왼어깨_카드_0.5초.png).
   - 침묵뿐이면: DISCOVERY-SHEET.md 사본 + 안내.md 에 동작별 침묵 사유 요약
     (record 0 의 의미 — "결함 감점이 안 잡힌 doc 이라 발굴 재료가 없다" +
     climbfault 스냅샷 대조 결과). 없는 카드를 만들어 채우지 않는다.
   - 안내.md 는 관측만 적는다 (진단을 관측처럼 적지 않는다) — 판정 요청·재촉
     문구 금지, "belle 이 볼 때 보면 된다"까지만.

2. **무접촉 검증 (확정 6):** rtk git diff --stat backend/ 가 0줄,
   git status --porcelain backend/ 빈 출력, evidence/render 로그에서 S3 put 0
   (하네스 _S3Stub 경유 확인), Firestore 쓰기 0 (하네스는 읽기 전용 경로만 사용).
   위반이 불가피했다면 deviation 으로 박제.

3. **SUMMARY 작성** (.planning/quick/260821-ls0-climb-combo-ehz-0/260821-ls0-SUMMARY.md):
   - 기계 판정 한 줄 (소스 게이트 → record → 후보 → 눈 → 카드 깔때기 수치)
   - **LLM 사용·학습 영향 절 (확정 3, 필수):** Gemini 실호출 회수(0 이면 0)
     record 별 분해 + 모델명 + 비용 추산 + "학습 전송 0(추론만)" 명기 +
     원장 보존 위치 (evidence/{m}/eye_ledger, 해당 시)
   - Known Limitations 정직 박제 (record 0 의 의미, climbfault 스냅샷 결손 여부,
     운영 방출 아님 — 반영은 belle 판정 후 별건)
   - 다음 = "belle 판정 대기 — 요청하지 않음. 판정은 wif LEDGER 기입란에"
   - Self-Check: 산출물 실물 확인 목록 + 커밋 해시.

4. 커밋: "docs(quick-260821-ls0): 스윕 SUMMARY + 판정 재료 준비 (판정 요청 없음)"
  </action>
  <verify>
    <automated>ls /Users/Shared/sunity-discovery-sweep-260821/안내.md && test -f .planning/quick/260821-ls0-climb-combo-ehz-0/260821-ls0-SUMMARY.md && rtk git diff --stat HEAD -- backend/ | wc -l | grep -q '^ *0$' && rtk grep -c "LLM" .planning/quick/260821-ls0-climb-combo-ehz-0/260821-ls0-SUMMARY.md</automated>
  </verify>
  <done>/Users/Shared 재료 + 안내.md 실물 존재, backend/ diff 0, SUMMARY 에 LLM 절·깔때기 수치·판정 대기 명기, 최종 커밋 존재. belle 판정 요청 문구 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로컬 → Gemini API | 관절 크롭 이미지 + claim 질문이 외부로 나감 (추론만) |
| 로컬 → S3 | 영상 다운로드 (GET only — put 은 하네스 스텁이 차단) |
| 로컬 → Firestore | refmotion·doc 대조 읽기만 (쓰기 0) |
| 리포 → /Users/Shared | 인물 스틸/카드 이미지 반출 (belle 판정용) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ls0-01 | Information Disclosure | Gemini 키 (SSM) | mitigate | 하네스 _ensure_gemini_key 그대로 — env 주입만, 키 값 로그·커밋 금지 |
| T-ls0-02 | Tampering | ehz 채택 evidence | mitigate | Task 1 무회귀 게이트 후 git restore 원복 + porcelain 빈 출력 확인, --render 재현 금지 |
| T-ls0-03 | Tampering | 프로덕션 (S3/Firestore/backend) | mitigate | S3 put 스텁 확인 + Firestore 쓰기 0 + backend diff 0 을 Task 3 에서 기계 검증 |
| T-ls0-04 | Information Disclosure | 인물 이미지 반출 | mitigate | /Users/Shared 만 사용 (홈디렉터리 git 저장소 금지 규율), scratchpad 캐시는 휘발 |
| T-ls0-05 | Repudiation | 사전 추천 vs belle 판정 | mitigate | LEDGER append 를 belle 노출 전에 커밋 (커밋 해시 = 박제 증거), 판정란 선기입 금지 |
| T-ls0-SC | Tampering | 패키지 설치 | accept | 신규 설치 0 — 기존 환경(numpy/boto3/firebase-admin/imageio)만 사용, npm/pip 설치 태스크 없음 |
</threat_model>

<verification>
- rtk git diff HEAD~3 -- .planning/quick/260814-ehz-5/discover_sweep.py: 순수 추가만
- python3 .planning/quick/260814-ehz-5/discover_sweep.py --check: PASS (8동작)
- .planning/quick/260813-wif-knee-discovery/DISCOVERY-LEDGER.md: 새 절 + 행 9~11, 기존 행 무수정 (diff 로 확인)
- rtk git diff --stat backend/: 0줄 · git status --porcelain backend/: 빈 출력
- /Users/Shared/sunity-discovery-sweep-260821/: 안내.md + 재료 실물
- Gemini 호출 로그 (evidence/{m}/eye_calls.log 또는 "호출 0" 명기) 존재
</verification>

<success_criteria>
- climb·climbfault·combo 가 ehz 하네스에 정식 등재되고 기존 5동작 무회귀가 기계 확인됨
- 3동작 전 record(측정 인벤토리 기준) 스캔 결과가 evidence 실물로 남음 — 발굴이면 눈 PASS 카드까지, 침묵이면 실행 수치·사유로 증명
- 사전 추천 3행이 belle 판정 전에 LEDGER 에 커밋됨 (침묵도 1건 계상)
- 판정 재료가 /Users/Shared 에 준비되고 belle 판정 요청은 발생하지 않음
- 프로덕션 무접촉 (S3 GET only / Firestore 쓰기 0 / backend diff 0), Gemini 사용량이 SUMMARY 에 박제됨
</success_criteria>

<output>
Create `.planning/quick/260821-ls0-climb-combo-ehz-0/260821-ls0-SUMMARY.md` when done
</output>
