---
phase: quick-260821-o3m
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/training/sft/setup_train_venv.sh
  - backend/training/sft/run_sft_gates.sh
  - backend/training/sft/pins/cu124-driver550.txt
autonomous: true
requirements: [QUICK-260821-O3M]

must_haves:
  truths:
    - "A100 80GB Pod(w340kaemere1po, 드라이버 550.127.05)에서 새 venv의 torch가 CUDA를 잡는다 (torch.cuda.is_available() == True, cu124 계열)"
    - "v30을 죽인 transformers 조합이 실측 근거로 교체되어, swift sft가 어댑터 부착 검증(validate_quantization_for_training)을 통과하고 첫 optimizer step의 loss가 로그에 찍힌다"
    - "재과금 0 — label/assemble 미실행, Gemini/LLM API 호출 0 (로그에서 확인)"
    - "venv 레시피(핀 파일 + 스크립트 수정)가 리포에 커밋되어 다음 Pod에서 재현 가능하다"
    - "train_venv312 / ab_venv_cu129 무접촉 (삭제·수정 없음)"
  artifacts:
    - path: "backend/training/sft/setup_train_venv.sh"
      provides: "TRAIN_VENV_SKIP_VLLM 노브 (기본 0 = 기존 동작 무회귀)"
      contains: "TRAIN_VENV_SKIP_VLLM"
    - path: "backend/training/sft/run_sft_gates.sh"
      provides: "Blackwell 우회 가드의 compute_cap 조건 (비-Blackwell 발화 차단)"
      contains: "compute_cap"
    - path: "backend/training/sft/pins/cu124-driver550.txt"
      provides: "cu124 핀 레시피 박제 (torch/transformers 실측 핀 + 사유 헤더 + 인터프리터 경로)"
  key_links:
    - from: "backend/training/sft/run_sft.sh"
      to: "/workspace/train_venv_cu124"
      via: "TRAIN_VENV env 주입"
      pattern: "TRAIN_VENV"
    - from: "backend/training/sft/pins/cu124-driver550.txt"
      to: "setup_train_venv.sh의 모든 pip 호출"
      via: "PIP_CONSTRAINT env (pip 네이티브 — 스크립트 수정 불요)"
      pattern: "PIP_CONSTRAINT"
---

<objective>
재학습 v30을 train 단계에서 재개한다. belle이 띄운 A100 80GB Pod(w340kaemere1po, 드라이버 550.127.05 = CUDA 12.4대)에서 볼륨의 train_venv312(torch cu130)는 못 쓰므로, 드라이버 맞춤 venv(/workspace/train_venv_cu124)를 새로 구축하고, v30을 죽인 transformers/swift QLoRA 조합을 실측 근거로 수리한 뒤, `run_retrain_cycle.sh train`을 nohup으로 발사해 첫 optimizer step(loss 로그) 성립까지 확인한다. 라벨·조립은 08-18에 완료 — 재과금 0.

Purpose: 플라이휠 v30 사이클 복구. v30 사인은 코드가 아니라 환경(transformers 5.12의 validate_quantization_for_training이 swift의 QLoRA 어댑터 부착 순서와 어긋남 — CONTINUE-2026-08-19 §2).
Output: 발사된 학습 프로세스 + 첫 loss 로그 + 리포에 박제된 venv 레시피 + SUMMARY(모니터링 방법 명기). 학습 완주 감시는 다음 세션 몫.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend/training/sft/setup_train_venv.sh
@backend/training/sft/run_sft.sh
@backend/training/sft/run_retrain_cycle.sh
@backend/training/sft/run_sft_gates.sh
@.planning/CONTINUE-2026-08-19.md

**Pod 사실관계 (2026-08-21 실측, planning context 원문):**
- SSH: `ssh root@213.173.105.5 -p 30279 -i ~/.ssh/id_ed25519` (Pod w340kaemere1po)
- GPU: A100 80GB PCIe (sm_80), 드라이버 550.127.05 → torch는 **cu124 계열** (cu130 불가, cu129는 minor-compat 경계라 피함 — 이 결정은 locked)
- 볼륨: /workspace/train_venv312 (cu130 — 못 씀, **삭제 금지**), /workspace/ab_venv_cu129 (**무접촉**), /workspace/phase22_sft_out/v30-20260818-122707/ (args.json만 있는 빈 껍데기)
- 추론 서버 가동 중 (:8000, RTMW onnx VRAM 소량) — 80GB라 공존 가능. OOM 시 고려사항으로만
- env: /workspace/aws_env.sh (run_sft.sh가 자체 source). 학습 데이터 = s3://sunity-motion-pilot-videos/training/phase22/jsonl/ (08-18 assemble 완료본)
- 리포: /workspace/SunityMotion (git pull로 동기화)

**v30 사인 원문:** `ValueError: You cannot perform fine-tuning on purely quantized models. Please attach trainable adapters` — run_sft.sh 인자는 정상(`--tuner_type lora --lora_rank 64` 존재, 212행). 수리 축 = transformers 버전 (ms-swift 4.4.0 상향 금지 — SftArguments 필드명 변동으로 조용한 실패, setup 스크립트 78행).

**게이트 (전 태스크 공통):**
- 프로덕션 무접촉: S3는 training/phase22/ 하위 GET + cycle_logs PUT(사이클 스크립트 자체 동작)만. Firestore 접근 0
- Gemini/LLM 호출 0 — train 단계가 LLM API를 부르면 즉시 STOP + 원인 보고
- OOM 시: "OOM→설정 축소" 전에 **데이터 크기부터 잰다**(메모리 원칙). run_sft.sh 168-204행 OOM 이력 주석 참조해 축소 1회 시도까지, 그 이상 STOP + 보고
- 시크릿 값 echo 금지 (env 이름만 로그)
- Bash에서 gsd-sdk 외 관찰 커맨드는 rtk 접두 (프로젝트 규칙)
</context>

<tasks>

<task type="auto">
  <name>Task 1: 리포 수리 — gates 가드 arch 조건 + setup vllm 스킵 노브 (커밋·푸시)</name>
  <files>backend/training/sft/run_sft_gates.sh, backend/training/sft/setup_train_venv.sh</files>
  <action>
두 스크립트에 최소 수정. 기본 동작은 그대로(무회귀) — 새 경로는 명시적으로 켜는 쪽만.

(1) `run_sft_gates.sh` 43-62행 드라이버 가드에 compute_cap 조건 추가:
현재 조건은 "번들 ptxas-blackwell CUDA > 드라이버 CUDA"만 봐서, 이 Pod(A100 sm_80, 드라이버 CUDA 12.4, triton 번들 ptxas 13.x)에서도 발화해 3종 우회(ptxas 교체 + FlashInfer off + `--attention-backend TRITON_ATTN`/`TORCH_SDPA`)를 A100에 강제한다. 3종 우회는 전부 Blackwell(sm_100+) 전용 증상이다: ptxas-blackwell은 triton이 arch>=100에서만 쓰고, FA2 PTX 거부와 FlashInfer 아키 파서 문제도 sm_120 실측이다. A100에 백엔드 강제는 불필요·유해 (run_retrain_cycle.sh 174-183행의 T-22-12-06 선례와 같은 정신).
수정: 가드 if 조건에 `nvidia-smi --query-gpu=compute_cap --format=csv,noheader`로 얻은 cap이 `>= 10`일 때만 발화하도록 추가 (awk 비교는 run_retrain_cycle.sh 177행 패턴 재사용). cap 미검출이면 발화 안 함(보수적). 주석에 2026-08-21 사유(A100+550 실측, cu129 우회 3종은 비-Blackwell에 해당 없음) 기록 — 프로젝트 관례대로 왜를 남긴다.

(2) `setup_train_venv.sh` 104-114행 vLLM 설치에 스킵 노브:
`TRAIN_VENV_SKIP_VLLM=1`이면 `pip install vllm uvloop`을 건너뛰고 명시 경고 출력: "이 venv로는 게이트 단계 불가 — gates는 vllm 가용 venv(TRAIN_VENV 교체)로 별도 실행". 기본 0 = 기존 설치 유지. 131-143행 torch 재검증 블록은 **무조건 실행 유지**(torch 게이트가 핵심 — 스킵 시 vllm import는 기존 관용 메시지로 떨어진다). 사유 주석: 드라이버 550(cu124 상한) Pod에서 vllm 최신판이 torch를 cu13x로 끌어올려 스크립트가 4단계에서 hard-fail하는 것을 막는다. Qwen3-VL을 서빙할 만큼 새로운 vllm과 cu124 torch는 양립 가능성이 낮아, 게이트는 별도 venv 축으로 분리한다(다음 세션 몫 — SUMMARY에 flag).

수정 후 `bash -n` 양쪽 통과 확인, 커밋 + push (Task 2에서 Pod가 pull 하므로 push까지가 이 태스크).
  </action>
  <verify>
    <automated>bash -n backend/training/sft/run_sft_gates.sh && bash -n backend/training/sft/setup_train_venv.sh && grep -q "TRAIN_VENV_SKIP_VLLM" backend/training/sft/setup_train_venv.sh && grep -q "compute_cap" backend/training/sft/run_sft_gates.sh && git log --oneline -1 && git status --porcelain backend/training/sft/ | wc -l | grep -q "^0$"</automated>
  </verify>
  <done>두 스크립트 수정이 문법 통과 + 기본 경로 무회귀(노브 기본값 0, cap 조건은 Blackwell에서만 참) + 커밋이 origin/main에 push됨</done>
</task>

<task type="auto">
  <name>Task 2: Pod에서 cu124 train venv 구축 + 진입점 import 게이트</name>
  <files>(Pod측: /workspace/train_venv_cu124, /workspace/train_pins_cu124.txt — 리포 파일 없음)</files>
  <action>
전부 `ssh root@213.173.105.5 -p 30279 -i ~/.ssh/id_ed25519`로 실행. train_venv312 / ab_venv_cu129 무접촉.

(1) 사전 실측 (추측 금지 — 핀은 전부 측정으로 결정):
- `nvidia-smi`로 드라이버 550.127.05 / A100 80GB 확인, `python3 -V`로 베이스 인터프리터 버전·경로 기록 (SUMMARY에 버전 박힌 경로로 박제)
- **v29 실증 조합 발굴**: `/workspace/train_venv`(v29가 08-15에 완주한 구 venv)가 볼륨에 남아 있으면 `/workspace/train_venv/bin/pip list`에서 transformers/torch/peft/accelerate/bitsandbytes/ms-swift 버전 채록 (pip 불능 시 `ls .../site-packages/*.dist-info`로 대체). 이것이 최우선 근거.
- **swift 메타데이터 실측**: `curl -s https://pypi.org/pypi/ms-swift/4.4.0/json`에서 requires_dist의 transformers 범위 추출 (Pod curl 불가 시 로컬 WebFetch)
- **cu124 최종 torch 실측**: `https://download.pytorch.org/whl/cu124/torch/` 인덱스에서 베이스 python에 맞는 최신 cu124 빌드 확인 (2.6.0+cu124 예상 — 확인으로 확정)

(2) transformers 핀 결정 규칙: (a) v29 채록값이 swift requires 범위 내이고 < 5.12(v30 사망 버전)이면 그 값. (b) 구 venv 부재 시: swift requires 상한과 5.12 사이에서, Qwen3-VL 지원이 시작된 transformers 버전(필요시 WebFetch로 릴리스노트 확인) 이상인 최신 안정판. (c) 어느 쪽도 확정 불가하거나 그 핀의 torch 하한이 cu124 최종 torch를 초과하면 — **STOP + 선택지 보고** (cu124 locked 결정을 임의로 벗어나지 않는다).

(3) 핀 파일 작성 (Pod에 scp 또는 ssh로 생성) `/workspace/train_pins_cu124.txt`:
torch==<실측 cu124 최신>+cu124 / torchvision==<짝 버전>+cu124 (설치 강제 아님 — constraint는 끌려올 때만 발동, cu 불일치 torchvision이 torch를 덮는 사고 방지) / transformers==<(2)의 결정값>. 헤더 주석에 사유(드라이버 550.127.05, v30 사인, swift requires 실측값) 기록.

(4) venv 구축 — Task 1이 push한 코드를 pull 후 setup 재사용 (스크립트 수정 없이 pip 네이티브 env로 핀 주입):
`cd /workspace/SunityMotion && git pull --ff-only`, 이어서
`TRAIN_VENV=/workspace/train_venv_cu124 TRAIN_VENV_ISOLATED=1 TRAIN_VENV_SKIP_VLLM=1 PIP_CONSTRAINT=/workspace/train_pins_cu124.txt PIP_EXTRA_INDEX_URL=https://download.pytorch.org/whl/cu124 bash backend/training/sft/setup_train_venv.sh`
(pip은 PIP_CONSTRAINT/PIP_EXTRA_INDEX_URL env를 네이티브로 읽는다 — 스크립트의 모든 pip 호출에 일괄 적용. 메인 인덱스는 PyPI 유지라 nvidia-* 의존성이 끊기는 08-14 libcudnn 사건은 재현 안 됨. 설치가 길다 — run_in_background + 폴링.) pip 해소 충돌(swift↔transformers 핀)이 나면 그것이 곧 측정 결과 — STOP + 보고.

(5) 진입점 import 게이트 (패키지 목록이 아니라 **호출되는 진입점** — 메모리 invariant). `V=/workspace/train_venv_cu124`:
- `$V/bin/python -c "import torch; assert torch.cuda.is_available(); assert hasattr(torch.nn.Module,'set_submodule'); print(torch.__version__, torch.version.cuda, torch.cuda.get_device_name(0))"` — cu124 계열 + A100 확인
- `$V/bin/python -c "import transformers, peft, accelerate, bitsandbytes; print(transformers.__version__)"` — 핀 일치 확인
- `$V/bin/swift sft --help > /dev/null` — 학습이 실제로 부르는 CLI 진입점 (`swift --version`은 쓰지 말 것 — 4.4 라우터 KeyError, setup 21행)
- `$V/bin/python -c "import decord"` + `$V/bin/python -c "import boto3, imageio_ffmpeg"` (run_sft [1/3] 로컬라이즈가 직접 import) + `$V/bin/python -c "import liger_kernel"` (미설치면 run_sft가 liger=false로 조용히 감 — 80GB라 치명 아님, 상태만 기록)
  </action>
  <verify>
    <automated>ssh root@213.173.105.5 -p 30279 -i ~/.ssh/id_ed25519 'V=/workspace/train_venv_cu124; $V/bin/python -c "import torch; assert torch.cuda.is_available(); assert hasattr(torch.nn.Module,\"set_submodule\"); print(torch.__version__)" && $V/bin/python -c "import transformers, peft, accelerate, bitsandbytes; print(transformers.__version__)" && $V/bin/swift sft --help > /dev/null && $V/bin/python -c "import decord, boto3, imageio_ffmpeg" && echo GATE-OK'</automated>
  </verify>
  <done>새 venv가 격리 모드로 존재, torch cu124 계열이 A100에서 CUDA 가용, transformers가 실측 근거 핀으로 설치됨, swift sft CLI·decord·boto3 진입점 import 전건 통과, 기존 venv 2종 무접촉</done>
</task>

<task type="auto">
  <name>Task 3: train 발사 + 첫 optimizer step 확인 + 레시피 리포 박제</name>
  <files>backend/training/sft/pins/cu124-driver550.txt</files>
  <action>
(1) 발사 전 점검 (preflight 단계는 train-only 실행에서 스킵되므로 수동으로):
- `df -BG /workspace` 여유 >= 30GB
- `pgrep -f "swift sft|vllm serve|full_batch"` 비어 있음 (추론 서버 uvicorn은 패턴 밖 — 계속 떠 있어도 됨)
- `ls -dt /workspace/phase22_sft_out/*/checkpoint-*` 결과 채록. **구 사이클(v29 이하) 체크포인트가 남아 있으면 SFT_RESUME=auto가 그걸 물어 스테일 재개가 된다** — 그래서 아래 발사는 `SFT_RESUME=none` 명시(신규 사이클 = 08-18 canonical 데이터로 베이스 모델부터). v30-20260818-122707 빈 껍데기는 checkpoint-* 가 없어 어차피 안 잡힌다 — 삭제하지 말고 그대로 둔다.

(2) 발사 (nohup detached — ssh가 즉시 복귀하도록 stdin 닫기):
`ssh root@213.173.105.5 -p 30279 -i ~/.ssh/id_ed25519 'cd /workspace/SunityMotion/backend && TRAIN_VENV=/workspace/train_venv_cu124 SFT_RESUME=none nohup bash training/sft/run_retrain_cycle.sh train > /workspace/cycle_260821.log 2>&1 < /dev/null &'`
(run_retrain_cycle.sh train은 run_sft.sh만 부르고 gates/promote로 이어지지 않는다 — 완주 후 gates/promote는 다음 세션 몫, SUMMARY에 명기.)

(3) 첫 optimizer step 확인 — [1/3] 로컬라이즈(신규 행 다운로드+재인코딩)가 수십 분 걸릴 수 있다. 수 분 간격으로 `tail /workspace/cycle_260821.log` 폴링 (Bash run_in_background 활용, 최대 ~90분 예산):
- 성공 신호 = swift 학습 로그의 첫 `loss` 라인 (logging_steps 1이라 step 1부터 찍힘) + `ls -dt /workspace/phase22_sft_out/*/` 에 새 run dir 생성
- **v30 사인 재발**(`purely quantized models` ValueError) 시 = transformers 핀이 틀린 것 — STOP + 로그 원문과 함께 보고 (재핀은 다음 판단)
- **OOM 시**: 축소 전에 데이터 크기부터 잰다(로그의 시퀀스 길이/max_length 관측 채록). 그 후 run_sft.sh 168-204행 이력대로 1회 시도(liger 상태 확인, `FPS_MAX_FRAMES` 축소 등) — 그 이상은 STOP + 보고. 추론 서버 VRAM 공존도 이때 고려사항으로 보고
- **Gemini 게이트**: `grep -ci gemini /workspace/cycle_260821.log` 가 0이어야 함 — 아니면 STOP

(4) 레시피 박제: Pod의 `/workspace/train_pins_cu124.txt`를 리포 `backend/training/sft/pins/cu124-driver550.txt`로 미러 (헤더에 Pod w340kaemere1po / 드라이버 550.127.05 / A100 80GB / 베이스 인터프리터 버전·경로 / swift requires 실측값 / 발사 커맨드 원문 기록). 커밋 + push.

(5) SUMMARY에 모니터링 방법 명기 (다음 세션 인계 — 관측만 적고 진단을 관측처럼 적지 말 것):
- 로그: `ssh root@213.173.105.5 -p 30279 -i ~/.ssh/id_ed25519 'tail -30 /workspace/cycle_260821.log'`
- 진행: `ssh ... 'ls -dt /workspace/phase22_sft_out/*/checkpoint-* | head -3'` + `pgrep -f "swift sft"`
- 완주 후 다음 수: `TRAIN_VENV`를 vllm 가용 venv로 바꿔 `run_retrain_cycle.sh gates` → `promote` (이 venv는 TRAIN_VENV_SKIP_VLLM=1로 만들어 게이트 서빙 불가 — 해소 방안은 다음 세션 결정 사항으로 flag)
- 중단 시 재개: 같은 발사 커맨드에서 `SFT_RESUME=auto` (새 run의 checkpoint-*가 최신이 되므로 auto가 올바르게 잡는다)
  </action>
  <verify>
    <automated>ssh root@213.173.105.5 -p 30279 -i ~/.ssh/id_ed25519 'grep -m1 -E "loss.:|.loss." /workspace/cycle_260821.log && [ "$(grep -ci gemini /workspace/cycle_260821.log)" = "0" ] && pgrep -f "swift sft" > /dev/null && echo LAUNCH-OK' && test -f backend/training/sft/pins/cu124-driver550.txt</automated>
  </verify>
  <done>학습 프로세스가 detached로 살아 있고 첫 loss 라인이 로그에 존재, 새 run dir 생성 확인, Gemini 호출 0, 핀 레시피가 리포에 커밋·push됨, SUMMARY에 모니터링·재개·다음 수(gates venv gap 포함) 명기</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로컬 → Pod SSH | 운영 GPU Pod에 명령 주입 — 오타·경로 실수가 볼륨 자산(train_venv312, 구 체크포인트)을 훼손할 수 있음 |
| Pod → S3 | AWS 키(aws_env.sh) 사용 — training/phase22/ 밖 접근은 스코프 위반 |
| pip → PyPI/pytorch index | 패키지 공급망 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-o3m-01 | Tampering | /workspace 기존 venv·체크포인트 | mitigate | 신규 경로(train_venv_cu124)만 생성, 기존 venv 2종·v30 dir 무접촉을 done 조건에 명시, rm 계열 커맨드 금지 |
| T-o3m-02 | Information Disclosure | aws_env.sh / SSH 로그 | mitigate | 시크릿 값 echo 금지(env 이름만), 로그·SUMMARY에 키 값 미기재 (T-22-22 관례) |
| T-o3m-03 | Elevation of Privilege | S3 쓰기 스코프 | mitigate | train 단계는 jsonl GET + cycle_logs PUT만 — 그 외 S3 쓰기·Firestore 접근 발견 시 STOP |
| T-o3m-SC | Tampering | pip installs (torch/transformers/ms-swift 등) | mitigate | 전부 프로젝트가 이미 쓰는 기성 패키지의 버전 재핀(신규 패키지 0) + PIP_CONSTRAINT 핀으로 해소 고정. 신규 패키지가 필요해지면 STOP + 보고 |
</threat_model>

<verification>
- Task 1 수정 2건이 기본 경로 무회귀(노브 off·cap 조건은 Blackwell에서만 참)로 커밋·push됨
- 새 venv 진입점 게이트 전건 통과 (torch CUDA/A100, swift sft CLI, transformers 핀, decord/boto3)
- /workspace/cycle_260821.log에 첫 loss 라인 + 새 run dir + `grep -ci gemini` == 0
- 기존 venv 2종·프로덕션(Firestore, training/ 외 S3) 무접촉
- 핀 레시피 리포 박제 + SUMMARY에 모니터링·다음 수 명기
</verification>

<success_criteria>
- v30 train 단계가 A100/드라이버 550 Pod에서 재발사되어 첫 optimizer step이 성립 (loss 로그 실물 확인 — 코드 통과 아님)
- v30 사인(transformers 어댑터 검증)이 실측 핀으로 해소됐음이 그 첫 스텝으로 입증됨
- 재과금 0 (label/assemble 스킵, Gemini 0), 볼륨 기존 자산 무손상
- 레시피가 리포에 있어 Pod가 또 바뀌어도 재현 가능
- 다음 세션이 로그 경로·확인 커맨드·gates venv gap을 SUMMARY만 읽고 이어받을 수 있음
</success_criteria>

<output>
Create `.planning/quick/260821-o3m-v30-train-a100-550-train-venv-transforme/260821-o3m-SUMMARY.md` when done
</output>
