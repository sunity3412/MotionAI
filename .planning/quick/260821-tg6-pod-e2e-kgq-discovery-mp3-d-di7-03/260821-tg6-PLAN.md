---
phase: quick-260821-tg6
quick_id: 260821-tg6
slug: pod-e2e-kgq-discovery-mp3-d-di7-03
date: 2026-08-21
status: planned
description: 새 Pod(w340kaemere1po) 운영 경로 E2E 실증(SIM_UID 신선 분석 1건) + kgq 반영 발굴 정지의 운영 렌더 상속 검증(no-write 재구동) + D-di7-03(discover mp3 회수 배선) 운영 실행 증거 확보. 수리 금지 — 실증/관측 전용. 이 Pod의 마지막 대기열 항목.
wave: 1
depends_on: []
type: execute
plan: 01
autonomous: true
requirements: [QUICK-260821-TG6]
files_modified:
  - .planning/quick/260821-tg6-pod-e2e-kgq-discovery-mp3-d-di7-03/evidence/code_facts.md
  - .planning/quick/260821-tg6-pod-e2e-kgq-discovery-mp3-d-di7-03/evidence/fresh_analysis.log
  - .planning/quick/260821-tg6-pod-e2e-kgq-discovery-mp3-d-di7-03/evidence/fresh_doc_fields.json
  - .planning/quick/260821-tg6-pod-e2e-kgq-discovery-mp3-d-di7-03/evidence/rerun_pdshape.log
  - .planning/quick/260821-tg6-pod-e2e-kgq-discovery-mp3-d-di7-03/evidence/rerun_powerspin.log
  - .planning/quick/260821-tg6-pod-e2e-kgq-discovery-mp3-d-di7-03/evidence/rerun_pdshape_report.json
  - .planning/quick/260821-tg6-pod-e2e-kgq-discovery-mp3-d-di7-03/evidence/rerun_powerspin_report.json
  - .planning/quick/260821-tg6-pod-e2e-kgq-discovery-mp3-d-di7-03/evidence/stills/
  - .planning/quick/260821-tg6-pod-e2e-kgq-discovery-mp3-d-di7-03/260821-tg6-SUMMARY.md
must_haves:
  truths:
    - "SIM_UID(fvcNXzEqKjgqVxRPVSj1iwFnIpn2) 신선 분석 1건(피디쉐입 코퍼스 영상)이 이 Pod에서 서버 동일 env의 direct-process로 status=done 완료된다 — 실사용자 doc 쓰기 0"
    - "kgq 반영 피디쉐입 doc(p34fresh1786628533)의 발굴 정지 2건(왼무릎 r04 + 왼팔꿈치 r00)이 운영 렌더 스테이지(_run_deferred_compare_render) no-write 재구동에서 [discover] 주입으로 freezes에 포함되고, discover mp3 2건이 S3에서 audio_dir로 실회수된다"
    - "kgq 반영 파워스핀 doc(powerspinFault1785373695)의 발굴 정지 1건(왼어깨 r02)이 같은 경로에서 동일하게 성립한다"
    - "D-di7-03 재검 결론이 코드+실행 로그로 박제된다 — 배선은 quick-260814-ghs가 이미 구현(pipeline/app.py:4245-4286), 운영 실행 증거(compare_render discovery 조달 n=… log.info)는 이번 Pod 실행이 최초"
    - "운영 doc 2건(피디쉐입·파워스핀)에 대한 쓰기 0 — rerun 로그에 [no-write] S3 put_object 차단 / [no-write] doc 마킹 차단 라인이 존재한다"
    - "발굴 정지 실물 스틸(피디쉐입 42.07s 무릎 / 68.8s 팔꿈치, 파워스핀 0.5s 어깨)이 재구동 산출 mp4에서 추출되어 육안 확인된다"
  artifacts:
    - path: ".planning/quick/260821-tg6-pod-e2e-kgq-discovery-mp3-d-di7-03/evidence/code_facts.md"
      provides: "상속 원천 코드 확정(get_analysis_discovery = doc 자체 result.discovery, 사후 채택물 — 코퍼스 전파 아님) + D-di7-03 배선 이력(ghs 구현, 운영 미실증) + Pod repo/env 파리티 확인"
    - path: ".planning/quick/260821-tg6-pod-e2e-kgq-discovery-mp3-d-di7-03/evidence/fresh_analysis.log"
      provides: "신선 분석 stdout/stderr 원문 — status=done, records 표, renderedCompare 관측, Gemini 호출 집계 근거"
    - path: ".planning/quick/260821-tg6-pod-e2e-kgq-discovery-mp3-d-di7-03/evidence/rerun_pdshape.log"
      provides: "운영 렌더 재구동 원문 — discovery 조달 n=2, [discover] 주입, 리그 판정, no-write 차단 라인"
    - path: ".planning/quick/260821-tg6-pod-e2e-kgq-discovery-mp3-d-di7-03/evidence/stills/"
      provides: "발굴 정지 실물 스틸 3장(육안 확인 재료)"
    - path: ".planning/quick/260821-tg6-pod-e2e-kgq-discovery-mp3-d-di7-03/260821-tg6-SUMMARY.md"
      provides: "명제 3건 판정 + 관측/진단 구분 박제 + LLM 학습 영향 + belle Pod 종료 판단 재료"
  key_links:
    - from: "backend/scripts/phase34_fresh_reanalysis.py"
      to: "backend/functions/pipeline/app.py::_process"
      via: "경로 import 직접 호출 (SIM_UID 신선 doc, SOURCE 키/identity 분리 — 분기 0, 코드 1벌)"
      pattern: "pipeline\\._process"
    - from: "backend/scripts/rerun_compare_stage.py"
      to: "backend/functions/pipeline/app.py::_run_deferred_compare_render"
      via: "no-write 재구동 (S3 put 차단 + doc 마킹 차단 — 운영 doc 읽기만)"
      pattern: "_run_deferred_compare_render"
    - from: "backend/functions/pipeline/app.py::_run_deferred_compare_render"
      to: "Firestore result.discovery + S3 discover_audio_*.mp3"
      via: "firestore_admin.get_analysis_discovery + _s3.download_file(audio_dir) — quick-260814-ghs 배선(D-di7-03 해소분)"
      pattern: "get_analysis_discovery"
---

<objective>
새 Pod(w340kaemere1po)에서 세 명제를 실증/관측한다 — 수리 금지, 이 Pod의 마지막
대기열 항목(이후 belle이 Pod 종료 판단):

1. **운영 경로 E2E**: SIM_UID 신선 분석 1건(피디쉐입 코퍼스 영상)이 서버 동일
   env의 direct-process로 끝까지 done — Pod 복구 후 E2E 실증.
2. **kgq 반영분 상속**: 오늘(08-21 kgq) 반영한 발굴 정지(피디쉐입 왼무릎+왼팔꿈치,
   파워스핀 왼어깨)가 운영 렌더 경로(_run_deferred_compare_render)에서 상속되는가.
3. **D-di7-03 재검**: 렌더 스테이지의 S3 discover mp3 → audio_dir 회수 배선.

플래닝 중 코드 사실 확정 (집행자는 Pod HEAD 기준 재확인 후 박제만):
- **명제 2의 상속 원천은 코드로 이미 확정** — `_run_deferred_compare_render`는
  `firestore_admin.get_analysis_discovery(uid, analysis_id)`로 **그 doc 자체의
  result.discovery**를 조달한다(app.py:4254-4258). 발굴은 분석 사후 belle 채택물
  — 승인 코퍼스 doc에서 신선 doc으로 전파되는 설계가 아니다. 따라서 신선 분석의
  렌더에 발굴 정지가 **없는 것이 설계상 정상**이고(관측으로 박제), kgq 반영분
  상속 검증은 **발굴을 보유한 운영 doc 2건의 no-write 재구동**으로 한다.
- **D-di7-03은 quick-260814-ghs가 이미 배선** (app.py:4245-4286: discovery 조달
  + mp3 basename 회수 + doc_like 탑재, 전 경로 fail-open). 단 ghs는
  FakeS3+monkeypatch 유닛만 — 프로덕션 쓰기 0, Pod 무접촉. ghs SUMMARY:128이
  "조달 성립 시 compare_render discovery 조달 로그가 Pod 실증 때 실행 증거"라고
  이연 명기. **이번 재구동이 그 운영 실행 증거의 최초 확보다**
  ([[wiring-claims-need-log-evidence]]).

Purpose: 재구동 로그의 조달/주입/리그 라인 + 실물 스틸로 "배선이 운영 경로에서
돈다"를 로그 증거로 종결하고, E2E done으로 belle의 Pod 종료 판단 재료를 준다.
Output: evidence 로그/스틸/판정 + SUMMARY (수리 0, backend/ 변경 0).

게이트 해석 (planning context 원문 준수):
- "분석은 1건만" = 전체 _process(NLF+채점+Gemini) 1회 = Task 2. Task 3의 렌더
  스테이지 단독 재구동은 분석이 아니다(NLF 채점 경로 0, Gemini 0 — align GPU
  재추출만). 두 doc 재구동 비용은 영상 길이 합 ~2분 분량의 15fps 재추출로 소액.
- "오늘 반영한 두 운영 doc은 읽기만" = rerun 기본 no-write 모드가 정확히 충족
  (S3 put 차단 + doc 마킹 차단, 차단 로그가 증거). --write 절대 금지.
- 수리 금지 — 결함 발견 시 관측(로그/실물 원문)과 진단(추정 기전)을 구분해
  SUMMARY에 박제 ([[handoff-observation-not-diagnosis]]).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/quick/260814-di7-s3-doc-freeze-discover/260814-di7-SUMMARY.md
@.planning/quick/260821-kgq-3-pdshape-powerspin-s3-doc/260821-kgq-SUMMARY.md
@backend/scripts/phase34_fresh_reanalysis.py
@backend/scripts/rerun_compare_stage.py

Pod 사실관계 (이 세션 실측 — 착수 시 재검증):
- SSH: `ssh -o ConnectTimeout=15 root@213.173.105.5 -p 30279 -i ~/.ssh/id_ed25519`
- 추론 서버 :8000 가동 중 (proxy health 200, start_server.sh env = veto ON + Gemini 키)
- v32 SFT 학습 완주(ALLDONE) — GPU 여유. 볼륨 venv 2종 인터프리터 깨짐(train gates 불가 — 범위 밖)
- repo 위치: /workspace/SunityMotion. 기동 정본 = /workspace/start_server.sh
  (repo 사본 backend/runpod_inference/start_server.sh 와 md5 대조 — [[pod-start-script-canonical-and-versioned]])

운영 doc 2건 (읽기 전용 — 쓰기 절대 금지):
- 피디쉐입: uid `fvcNXzEqKjgqVxRPVSj1iwFnIpn2` / aid `p34fresh1786628533`
  — discovery 2건(r04 왼무릎 + r00 왼팔꿈치), freezes 정본 7건:
  r00 5.33 / r01 15.67 / r04 29.93 / r04:discover 42.07 / r02 54.17 / r00:discover 68.8 / r03 83.0
  — mp3: discover_audio_r00_left_elbow.mp3 (md5 c0bab5c9…) + discover_audio_r04_left_knee.mp3
- 파워스핀: uid `csKWYvI3WCPYPysNQ9KkWecaUvq1` (**belle 실계정 — 읽기만**) / aid `powerspinFault1785373695`
  — discovery 1건(r02 왼어깨), freezes 정본 3건: r02:discover 0.5 / r02 16.47 / r00 32.03
  — mp3: discover_audio_r02_left_shoulder.mp3 (md5 909728c5…)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pod 준비 확인 + 코드 사실 박제 (code_facts.md)</name>
  <files>.planning/quick/260821-tg6-pod-e2e-kgq-discovery-mp3-d-di7-03/evidence/code_facts.md</files>
  <action>
    착수 즉시 실물 대조 ([[verify-the-target-before-touching-it]]) — 전부 SSH 원격
    커맨드로 실행하고 원문을 로컬 evidence에 남긴다. 수리/설치/서버 재기동 금지.

    1. Pod 생존+서버: SSH 경유
       `curl -s -o /dev/null -w '%{http_code}' http://localhost:8000/health` == 200.
       실패 시 여기서 중단하고 관측만 SUMMARY에 기록
       ([[pod-terminate-recreate-is-normal-not-death]] — 404/refused는 belle 종료와
       장애가 구분 안 되는 관측이므로 "죽었다" 진단 금지).
    2. Pod repo 파리티: `cd /workspace/SunityMotion && git fetch origin &&
       git rev-parse HEAD origin/main`. 불일치면 `git log --oneline HEAD..origin/main`
       기록 후 `git pull --ff-only` (Pod 로컬 수정으로 ff 불가면 pull 하지 말고
       관측 기록 후 현 HEAD로 진행). 핵심 확인:
       `grep -n "discovery 조달" backend/functions/pipeline/app.py` 히트 존재 —
       ghs 배선이 Pod 코드에 실재한다는 전제. 없으면 D-di7-03 판정이 "Pod 코드
       미반영"으로 갈리므로 반드시 먼저 확인하고, 이 경우 Task 3의 기대 로그
       라인이 안 나오는 것을 결함이 아닌 코드 부재로 판정한다.
    3. 서버 env/인터프리터: `ps -eo command | grep -m1 uvicorn`으로 실행 인터프리터
       절대경로 확보, `md5sum /workspace/start_server.sh` vs repo 사본
       `backend/runpod_inference/start_server.sh` md5 대조 결과 기록. Task 2/3에서
       쓸 env 소싱 방법을 확정 — start_server.sh 상단 export 블록과 동일 계열
       (RTMW_ONNX_PATH/YOLOX_ONNX_PATH/FIREBASE_SA_*/AWS_*/GEMINI/RECOGNIZER 등,
       rerun_compare_stage.py 독스트링 지침 그대로). **env 값(키/시크릿)은 절대
       로그/evidence에 남기지 않는다 — 변수명만.**
    4. code_facts.md 작성 (Write 도구, 이모지 0): (a) 상속 원천 =
       get_analysis_discovery(uid, analysis_id) — doc 자체 result.discovery,
       사후 채택물, 코퍼스→신선 전파 아님 (Pod HEAD 기준 라인 번호 인용)
       (b) D-di7-03 배선 이력 = di7 이연 → quick-260814-ghs 구현(유닛만, 운영 실행
       증거 없음 — ghs SUMMARY:128 인용) → 운영 증거는 이번 Task 3이 최초
       (c) Pod 파리티(HEAD sha, grep 히트 라인, md5 대조, 인터프리터 경로).
  </action>
  <verify>
    <automated>test -s .planning/quick/260821-tg6-pod-e2e-kgq-discovery-mp3-d-di7-03/evidence/code_facts.md && grep -q "get_analysis_discovery" .planning/quick/260821-tg6-pod-e2e-kgq-discovery-mp3-d-di7-03/evidence/code_facts.md && grep -q "ghs" .planning/quick/260821-tg6-pod-e2e-kgq-discovery-mp3-d-di7-03/evidence/code_facts.md</automated>
  </verify>
  <done>code_facts.md에 상속 원천/D-di7-03 이력/Pod 파리티(HEAD sha + discovery 조달 grep 히트 + 인터프리터 경로)가 박제되고, 서버 health 200 확인. 시크릿 값 0.</done>
</task>

<task type="auto">
  <name>Task 2: SIM_UID 신선 분석 1건 E2E (피디쉐입 코퍼스 영상)</name>
  <files>.planning/quick/260821-tg6-pod-e2e-kgq-discovery-mp3-d-di7-03/evidence/fresh_analysis.log, .planning/quick/260821-tg6-pod-e2e-kgq-discovery-mp3-d-di7-03/evidence/fresh_doc_fields.json</files>
  <action>
    Pod에서 `backend/scripts/phase34_fresh_reanalysis.py`를 인자 기본값 그대로 실행
    — 기본값이 정확히 피디쉐입 코퍼스 케이스다 (SOURCE = belle 원본 업로드 키
    127a2a90…, 읽기만 / 신선 doc = SIM_UID `fvcNXzEqKjgqVxRPVSj1iwFnIpn2` 아래
    `p34fresh{ts}` 신규 — 실사용자 doc 쓰기 0). 실행 env = Task 1에서 확정한 서버
    동일 env + 인터프리터 (planning context "운영 서버가 쓰는 것과 동일한 env의
    direct-process" 요건). stdout/stderr 전체를 Pod 파일로 tee 후 scp로
    evidence/fresh_analysis.log 회수. 분석은 이 1건이 전부 — 재시도 전 로그를 먼저
    읽고, 환경 원인(모델 경로/자격 등)이 명백할 때만 1회 재시도 허용(그 외 관측
    기록 후 중단).

    완료 후 판정 재료 수집 (Pod에서 서버 인터프리터로 짧은 인라인 스크립트,
    firestore_admin.get_analysis 읽기만):
    - status / overallScore / records (recordId, criterion, atVideoSec, points)
    - renderedCompare (status/key/freezes) — **freezes에 :discover 항목이 없어야
      설계상 정상** (Task 1 코드 사실의 실측 대응 — 신선 doc은 discovery 부재).
      있거나 renderedCompare가 skip/failed 계열이면 그대로 관측 기록 (수리 금지,
      스킵 사유 로그 라인 원문 포함).
    - result.discovery 부재 확인
    위를 fresh_doc_fields.json으로 저장 (scp 회수).

    renderedCompare.status == done이면 실물 확인: S3에서 해당 compare mp4를
    scratchpad로 GET → 임의 freeze 시각 스틸 1장을 backend/.venv의
    imageio/imageio_ffmpeg로 추출(kgq 스틸 선례) → 육안 확인 소견을 SUMMARY 초안에
    기록 (스틸은 evidence/stills/에 fresh_*.png로 저장 가능 — 선택).

    로그에서 Gemini 호출 흔적(recognizer/veto 라인)을 세어 SUMMARY의 LLM 학습 영향
    절 재료로 박제 ([[feedback-always-report-llm-learning-impact]]).
  </action>
  <verify>
    <automated>grep -q "status=done" .planning/quick/260821-tg6-pod-e2e-kgq-discovery-mp3-d-di7-03/evidence/fresh_analysis.log && test -s .planning/quick/260821-tg6-pod-e2e-kgq-discovery-mp3-d-di7-03/evidence/fresh_doc_fields.json</automated>
  </verify>
  <done>신선 SIM 분석 1건 status=done (드라이버 exit 0), fresh_analysis.log + fresh_doc_fields.json 회수. 신선 doc의 discovery 부재/renderedCompare 관측 박제. 실사용자 doc 쓰기 0 (SIM_UID 외 쓰기 경로 없음 — 드라이버 구조상 보장, 로그로 재확인).</done>
</task>

<task type="auto">
  <name>Task 3: 운영 doc 2건 no-write 렌더 재구동 — kgq 상속 + D-di7-03 실행 증거 + 실물 스틸</name>
  <files>.planning/quick/260821-tg6-pod-e2e-kgq-discovery-mp3-d-di7-03/evidence/rerun_pdshape.log, .planning/quick/260821-tg6-pod-e2e-kgq-discovery-mp3-d-di7-03/evidence/rerun_powerspin.log, .planning/quick/260821-tg6-pod-e2e-kgq-discovery-mp3-d-di7-03/evidence/rerun_pdshape_report.json, .planning/quick/260821-tg6-pod-e2e-kgq-discovery-mp3-d-di7-03/evidence/rerun_powerspin_report.json, .planning/quick/260821-tg6-pod-e2e-kgq-discovery-mp3-d-di7-03/evidence/stills/, .planning/quick/260821-tg6-pod-e2e-kgq-discovery-mp3-d-di7-03/260821-tg6-SUMMARY.md</files>
  <action>
    Pod에서 `backend/scripts/rerun_compare_stage.py`를 **--write 없이** (기본
    no-write) 순차 2회 실행 — GPU 직렬
    ([[pipeline-not-concurrency-safe-eval-serial]]), Task 2 종료 후:

    1. `--uid fvcNXzEqKjgqVxRPVSj1iwFnIpn2 --analysis-id p34fresh1786628533` (피디쉐입)
    2. `--uid csKWYvI3WCPYPysNQ9KkWecaUvq1 --analysis-id powerspinFault1785373695` (파워스핀)

    env = Task 1 확정본 (rerun 독스트링: RTMW/YOLOX ONNX 경로 실파일이어야 능력
    프로브 통과, FIREBASE_SA_* + aws_env 필요, torch 프리로드로 ORT CUDA 파리티).
    각 실행 stdout/stderr 전체 tee → scp로 rerun_pdshape.log / rerun_powerspin.log.

    각 로그에서 판정할 라인 (원문 인용으로 SUMMARY 박제):
    - `compare_render discovery 조달 n=2 rids=['r00','r04']` (피디쉐입) /
      `n=1 rids=['r02']` (파워스핀) — **D-di7-03 운영 실행 증거의 핵심 라인**
      (ghs SUMMARY:128이 지정한 그 로그).
    - `[discover]` 주입 로그 (build_timeline) — 피디쉐입 2건 / 파워스핀 1건.
      discover mp3 회수 실패 시 `discover_no_mp3` 회계 라인 — 나오면 관측 기록.
    - 리그 판정 (`리그: ALL PASS` 또는 FAIL 라인 전체) — FAIL이어도 수리 금지,
      FAIL 라인 + 보존 경로(/tmp/compare_fail_*)를 관측으로 박제.
    - `[no-write] S3 put_object 차단` + `[no-write] doc 마킹 차단` — 쓰기 0 증거.
    - 차단된 마킹의 freezes 목록 vs kgq 정본 (피디쉐입 7건: r00 5.33 / r01 15.67 /
      r04 29.93 / r04:discover 42.07 / r02 54.17 / r00:discover 68.8 / r03 83.0,
      파워스핀 3건: r02:discover 0.5 / r02 16.47 / r00 32.03). 재구동은 align을
      실 GPU로 재생성하므로 outSec 수치가 정본과 다를 수 있다 — rid 집합/순서의
      일치(특히 :discover 2+1건 존재)를 1차 판정으로, 수치 차이는 관측으로 기록
      (kgq powerspin record 2건 선례처럼 live 값 우선).

    실물 확인 ([[open-the-artifact-before-claiming-done]] +
    [[frames-before-numbers-gate]]): 드라이버 outdir(/tmp/compare_rerun_{aid})의
    compare.mp4 + report.json 회수 — report.json은 evidence/rerun_*_report.json으로
    커밋, mp4는 scratchpad로만 scp (repo 커밋 금지 — 대용량 바이너리). mp4에서
    발굴 정지 3장 스틸 추출 (report.json의 실제 freeze 출력 시각 기준 — 피디쉐입
    r04:discover 무릎 / r00:discover 팔꿈치, 파워스핀 r02:discover 어깨) →
    evidence/stills/*.png 저장 + 육안 소견(정지+마커+캡션 구움 여부) 기록.
    추출은 backend/.venv 인터프리터의 imageio 사용 (kgq 선례 — 시스템 python3에
    imageio_ffmpeg 부재).

    SUMMARY 작성 (summary.md 템플릿, 이모지 0): 명제 1/2/3 각각의 판정 + 근거
    (로그 원문 라인/스틸/fresh_doc_fields), 관측 vs 진단 구분
    ([[handoff-observation-not-diagnosis]]), 쓰기 회계 (운영 doc 2건 쓰기 0 증거
    라인 / SIM doc 쓰기 목록), LLM 학습 영향 (Task 2 Gemini 호출 수 + Task 3
    Gemini 0), belle Pod 종료 판단 재료 한 줄 (이 Pod 대기열 소진 여부). 커밋:
    `docs(quick-260821-tg6): Pod E2E + kgq 발굴 상속 + D-di7-03 운영 실증` —
    backend/ 변경 0 확인(`git status --porcelain backend/` 빈 출력) 후 planning
    파일만 커밋.
  </action>
  <verify>
    <automated>grep -q "discovery 조달 n=2" .planning/quick/260821-tg6-pod-e2e-kgq-discovery-mp3-d-di7-03/evidence/rerun_pdshape.log && grep -q "discovery 조달 n=1" .planning/quick/260821-tg6-pod-e2e-kgq-discovery-mp3-d-di7-03/evidence/rerun_powerspin.log && grep -q "no-write" .planning/quick/260821-tg6-pod-e2e-kgq-discovery-mp3-d-di7-03/evidence/rerun_pdshape.log && ls .planning/quick/260821-tg6-pod-e2e-kgq-discovery-mp3-d-di7-03/evidence/stills/ | grep -c png && test -s .planning/quick/260821-tg6-pod-e2e-kgq-discovery-mp3-d-di7-03/260821-tg6-SUMMARY.md</automated>
  </verify>
  <done>두 재구동 로그에 discovery 조달 n=2/n=1 + [discover] 주입 + no-write 차단 라인 존재, freezes rid 집합 판정 박제 (:discover 2+1건), report.json 2건 + 발굴 정지 스틸 3장 육안 확인, SUMMARY에 명제 3건 판정 + 관측/진단 구분 + 쓰기 회계 + LLM 영향 박제, backend/ porcelain 빈 출력 상태로 커밋 완료. 리그 FAIL 등 결함 발견 시에도 관측 박제로 done (수리는 다음 태스크 재료).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로컬 → Pod SSH | 원격 명령 실행 (실측 좌표 213.173.105.5:30279, 키 인증) |
| Pod → Firestore | 운영 doc 읽기 (belle 실계정 doc 포함 — 읽기 전용 요구) |
| Pod → S3 | 영상/mp3 GET (no-write 프록시가 put 차단) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-tg6-01 | Tampering | 운영 doc 2건 (pdshape/powerspin) | mitigate | rerun --write 절대 금지 (기본 no-write), `[no-write] 차단` 로그 존재를 verify 게이트로 강제 |
| T-tg6-02 | Tampering | 실사용자 doc | mitigate | 신선 분석은 SIM_UID 드라이버 기본값만 — belle uid로의 쓰기 경로 없음, 로그로 uid 재확인 |
| T-tg6-03 | Information Disclosure | env/시크릿 (Gemini/AWS/Firebase 키) | mitigate | env 값 출력 금지 — 변수명만 기록, 로그 scp 전 키 문자열 grep 스크린 |
| T-tg6-04 | Information Disclosure | Gemini 송신 (분석 내부 recognizer/veto) | accept | 게이트 허용 범위 (자연 발생분만, 호출 수 SUMMARY 집계) |
| T-tg6-SC | Tampering | npm/pip/cargo installs | accept | 패키지 설치 0 (기존 Pod env + backend/.venv 재사용만 — 설치 발생 시 중단이 규칙) |
</threat_model>

<verification>
- 서버 health 200 + Pod repo에 discovery 조달 배선 실재 (Task 1)
- SIM 신선 분석 status=done, exit 0 (Task 2) — 실사용자 doc 무접촉
- 재구동 2건: discovery 조달 로그(n=2/n=1) + [discover] 주입 + no-write 차단 라인 + freezes rid 집합 (Task 3)
- 실물: 발굴 정지 스틸 3장 육안 + report.json 2건
- `rtk git status --porcelain backend/` 빈 출력 (수리 금지 준수)
- SUMMARY: 명제 3건 판정, 관측/진단 구분, 쓰기 회계, LLM 학습 영향
</verification>

<success_criteria>
- 명제 1: 이 Pod 운영 경로 E2E 1건 done — 로그 원문으로 성립
- 명제 2: kgq 반영 발굴 정지 3건(무릎/팔꿈치/어깨)이 운영 렌더 재구동 freezes에 포함 — 로그 + 스틸로 성립 (상속 원천 = doc 자체 discovery, 코드 사실로 확정)
- 명제 3: D-di7-03 = "ghs가 배선 완료, 운영 실행 증거 이번 최초 확보" — 조달 로그 라인으로 종결 (또는 결함 시 관측 박제)
- 운영 doc 쓰기 0, backend/ 변경 0, 분석 1건, 이모지 0
- SUMMARY 커밋 완료 — belle Pod 종료 판단 재료 제공
</success_criteria>

<output>
Create `.planning/quick/260821-tg6-pod-e2e-kgq-discovery-mp3-d-di7-03/260821-tg6-SUMMARY.md` when done
</output>
