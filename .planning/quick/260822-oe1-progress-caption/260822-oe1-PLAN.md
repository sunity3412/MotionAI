---
phase: quick-260822-oe1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/quick/260822-oe1-progress-caption/NOISE-MEASUREMENT.md
  - .planning/quick/260822-oe1-progress-caption/measure_noise.mjs
  - app/src/lib/progressCaption.ts
  - app/src/lib/illustrationHow.ts
  - app/src/lib/__tests__/progressCaption.test.ts
  - app/src/components/DefectIllustration.tsx
  - app/src/app/analysis/result.tsx
autonomous: true
requirements: [BELLE-0821-P1, BELLE-0821-P2, BELLE-0821-P3, BELLE-0821-P4]
tags: [progress-caption, how-overlay, kip-up, noise-threshold, measure-first]

must_haves:
  truths:
    - "같은 동작(mode1 referenceMotionId 동일)의 직전 done 분석이 있고, 이번 편차가 직전보다 노이즈 문턱 이상 줄었으면 킵업 다리 상세 시트의 수치 문장 아래에 '저번보다 더 벌어졌어요'(belle 원문)가 붙는다"
    - "직전 분석 없음 / 같은 criterion record 없음 / 값 비유한 / 단위 불일치 / 개선폭이 문턱 미만 / 악화 → 캡션 없음, 기존 오버레이·수치 문장은 바이트 동일 겉모습 유지"
    - "노이즈 문턱값은 실데이터 측정 산출물(NOISE-MEASUREMENT.md)에서만 온다 — 측정 전에 정한 수치 0. 측정 불충분이면 문턱 null = 캡션 전면 비활성(fail-closed)"
    - "기존 fail-closed 규율 불변: 값 없음/delta<3° → 오버레이 없음 → 수치 문장 없음 → 캡션도 없음 (수치 문장이 없는 곳에 캡션만 뜨는 일 없음)"
  artifacts:
    - path: ".planning/quick/260822-oe1-progress-caption/NOISE-MEASUREMENT.md"
      provides: "사전 박제 판정 규칙 + 실데이터 요동 분포 + threshold_deg 기계 판독 행"
      contains: "threshold_deg:"
    - path: "app/src/lib/progressCaption.ts"
      provides: "직전 분석 선택 + record 추출 + 캡션 판정 순수 함수 3종 + 문턱 상수(출처 주석)"
      contains: "PROGRESS_NOISE_THRESHOLD_DEG"
    - path: "app/src/lib/__tests__/progressCaption.test.ts"
      provides: "fail-closed 전 축 + 개선 판정 단위 테스트 (node --test)"
    - path: "app/src/components/DefectIllustration.tsx"
      provides: "prevHow prop + 캡션 렌더(카드 아래, overlay 있을 때만)"
  key_links:
    - from: "app/src/app/analysis/result.tsx illustrationSlot 클로저"
      to: "progressCaption.findPreviousComparable / extractCriterionMeasure"
      via: "useMyAnalyses({doneOnly:true}) 목록 + sheetView.primaryCriterion"
      pattern: "findPreviousComparable"
    - from: "app/src/components/DefectIllustration.tsx"
      to: "buildProgressCaption"
      via: "overlay != null 게이트 뒤에서만 호출"
      pattern: "buildProgressCaption"
    - from: "app/src/lib/progressCaption.ts PROGRESS_NOISE_THRESHOLD_DEG"
      to: ".planning/quick/260822-oe1-progress-caption/NOISE-MEASUREMENT.md"
      via: "출처 주석 (측정 산출값 소비 — measure-first)"
      pattern: "NOISE-MEASUREMENT"
---

<objective>
발전 캡션 배선 — 킵업 "어떻게" 오버레이(quick-260821-gb7)의 수치 문장("N° 정도 더
벌리세요")에 발전 추적 캡션을 잇는다.

belle 승인 근거 (2026-08-21 원문): "수치 하면 체감은 못하지만 다음 검증때 20도→18도로
줄면 발전적인 부분이 보인다. 캡션에 '저번보다 더 벌어졌어요' 담으면 된다."

- BELLE-0821-P1: 같은 동작(mode1 referenceMotionId 기준)의 직전 done 분석을 찾아
  같은 criterion 의 편차(delta = |baseline − measured|)를 이번 값과 비교.
- BELLE-0821-P2: 캡션 문구 = belle 화법 원문 "저번보다 더 벌어졌어요" 그대로.
  변형·수치 삽입·악화 문구 발명 금지 (문법 승인 ≠ 그림 승인 원칙의 문구판 —
  승인된 그 문장만 나간다).
- BELLE-0821-P3: 노이즈 문턱은 **측정 전에 정하지 않는다** (measure-first).
  Task 1 이 실데이터로 요동 분포를 재고, Task 2 는 그 산출값만 소비한다.
- BELLE-0821-P4: fail-closed — 직전 분석 없음 / 값 없음 / 문턱 이하 / 악화 →
  캡션 없음. 기존 오버레이 fail-closed(<3°/값 없음 → 오버레이 없음)는 불변.

Purpose: 수치의 역할 = 발전 추적 (kipup-how-asset-approved-20-1 박제). 한 번 본
수치는 체감이 안 되지만 다음 검증에서 줄어든 것이 보이면 수치가 의미를 갖는다.
Output: 측정 산출물 1건 + 순수 lib + 컴포넌트 캡션 렌더 + result.tsx 배선 + 단위
테스트 + 시뮬 캡처. **OTA 발행 금지** — belle 시뮬 확인 후 별도 결정
(verify-ui-on-simulator-before-ota).
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@app/CLAUDE.md
@app/src/lib/illustrationHow.ts
@app/src/components/DefectIllustration.tsx
@app/src/lib/userAnalyses.ts
@app/src/types/analysis.ts
@.planning/quick/260821-gb7-20-1-belle-08-21/260821-gb7-SUMMARY.md
</context>

<interface_notes>
플래너 실측 완료 — 아래는 검증된 배선 사실이다. 재탐색 금지, 그대로 소비할 것.

**수치 문장의 현재 배선 (gb7 산물):**
- 수치 문장 = `DefectIllustration.tsx` `BakedHowLayer` 하단 pill (`overlay.directionText`,
  스타일 `dirPill`+`bakedDirPill`). 오버레이 산출 = `illustrationHow.buildHowOverlay(asset,
  measured, target, unit, aspect)` — fail-closed: 미등록 asset / unit≠'deg' / 값 없음 /
  non-finite / delta<3° → null (오버레이 없음, 그림은 표시).
- `how` 값의 흐름: `deductionSheet.buildRegionSheetView` 가
  `view.primaryMeasure = { measured: primaryRec.measuredValue, target: primaryRec.baselineValue,
  unit: primaryRec.unit }` 를 만들고 (deductionSheet.ts:905-908), DeductionDetailSheet 이
  `illustrationSlot(maxHeight, howInput)` render prop 으로 넘긴다 (DeductionDetailSheet.tsx:410).
  슬롯 클로저는 result.tsx:3540-3547 — 이 스코프에 `sheetView`(`.primaryCriterion`,
  `.partKey`)와 `cmp`(`.mode`, `.referenceMotionId`)가 이미 있다.
  **DeductionDetailSheet.tsx 는 이번에 무접촉** — prev 데이터는 시트 계약을 안 거치고
  caller 클로저에서 DefectIllustration 의 새 prop 으로 직접 들어간다.
- 앵커 데이터: `illustrationHow.HOW_ANCHORS['ref-kip-up--leg']` kind 'baked'
  (`directionSentence: '{n}° 정도 더 벌리세요'`). `HowAnchorsBaked` interface 에 optional
  `progressSentence` 를 더하면 다른 에셋은 자동 미대상 (데이터로 opt-in, 코드 분기 0).

**직전 분석 재료 (계약 확인 완료):**
- `AnalysisDoc`: `analysisId`, `mode`, `status`, `createdAt`(epoch ms), `result?`.
  mode1 이면 `result.comparison.referenceMotionId` (Mode1Comparison, types/analysis.ts:243-256).
- 감점 record: `result.deductionBreakdown.records[]` — `criterion`, `measuredValue`,
  `baselineValue`, `unit: 'deg'|'notch'|'score_delta'` (types/analysis.ts:747-755).
  킵업 다리 = criterion 'split_angle', 차이형(measured=deficit, target=0)이 실측 배지.
  delta = |baselineValue − measuredValue| 는 절대각형·차이형 두 모양 다 성립
  (illustrationHow.ts:158-163 docstring 계승).
- 목록 훅: `userAnalyses.useMyAnalyses({doneOnly:true})` — `orderBy('createdAt','desc')`
  단일 필드 쿼리 (기록 탭과 동일 쿼리, 신규 인덱스 0 — analyses 인덱스 면제 설정 무접촉).
  **신규 Firestore 쿼리를 만들지 말 것** — 이 훅 재사용이 제약이다.

**측정 스크립트 (Task 1) 실행 환경:**
- firebase-admin 은 `app/node_modules` 에 있다 (devDependency ^13.10.0). 스크립트를
  quick 디렉터리에 두려면 ESM 에서 `createRequire` 로 app 쪽 resolution 을 빌린다:
  `import { createRequire } from 'node:module';`
  `const require = createRequire(new URL('../../../app/scripts/x.mjs', import.meta.url));`
  `const admin = require('firebase-admin');`
- 인증: `GOOGLE_APPLICATION_CREDENTIALS=/Users/kimtaesung/Dev/SunityMotion/firebase-sa.json`
  + `applicationDefault()` (seed-reference-motions.mjs 선례 — ADC 브라우저 로그인 없이
  키 파일로 동작). Admin SDK 는 rules 우회 — **읽기 전용으로만 쓸 것**.
- Firestore 구조: `users/{uid}/analyses/{analysisId}`. uid 열람 = `listDocuments()` 또는
  collectionGroup('analyses') (admin 은 인덱스 없이도 collectionGroup 단순 조회 가능하나,
  실패하면 users 순회 폴백).

**테스트 규약:** `node --test app/src/lib/__tests__/*.test.ts` (Node 24 type stripping,
`.ts` 확장자 import, node:test + node:assert/strict — illustrationScene.test.ts 헤더 선례).
현 기준선 = 198 중 197 통과, 기지 실패 1건(illustrationScene test 8, ref-pdshape/arm
provenance — 08-18 이전부터). `import type` 은 런타임 소거라 types/analysis.ts 를
type-only 로 import 해도 node --test 에서 안전하다.

**시뮬:** gb7 이 iPhone 16 Pro 시뮬(UDID 873D7CB3) + Metro :8081 를 belle 확인용으로
켜 둔 상태였다 — 죽어 있으면 gb7 Task 3 절차(Metro 신선 번들 → 기록 탭 → 킵업 →
다리 부위 상세 시트)로 재기동. 시뮬 uid 계열 = `fvcNXz…`, belle 실계정 = `csKWYvI3…`
(quick-260801-f77 박제) — **belle uid 에 쓰기 절대 금지**.
</interface_notes>

<tasks>

<task type="auto">
  <name>Task 1: 노이즈 문턱 측정 — 판정 규칙 사전 박제 후 실데이터 요동 분포 산출</name>
  <files>.planning/quick/260822-oe1-progress-caption/measure_noise.mjs, .planning/quick/260822-oe1-progress-caption/NOISE-MEASUREMENT.md</files>
  <action>
    measure-first (BELLE-0821-P3). 순서가 규율이다 — 규칙을 먼저 커밋하고 수치를 잰다
    (fe9 PREDICTION.md 예측 박제 게이트 선례, recommend-only-after-measuring 준수).

    1. **판정 규칙 사전 박제 (측정 실행 전 커밋):** NOISE-MEASUREMENT.md 에
       "## 판정 규칙 (측정 전 박제)" 섹션을 쓰고 먼저 커밋한다. 규칙:
       - 노이즈 표본 = (a) 같은 uid + 같은 영상 재분석 페어 (fileName 동일 + anglesFrames
         동일을 같은-영상 후보로 간주 — 08-09 결정론 ON 이후 페어는 Δ≈0 예상, 이전
         페어는 역사적 지터), (b) 같은 (uid, referenceMotionId) 의 48시간 이내 연속
         done mode1 분석 페어 (세션 내 실력 변화 ≈ 소, 나머지 = 촬영·RTMW·측정창 요동).
       - 각 페어에서 unit 'deg' record 를 criterion 별로 짝 맞춰
         |Δdelta| = ||base−meas|_i − |base−meas|_j| 를 수집.
       - threshold_deg = max(1, ceil(풀링된 |Δdelta| 분포의 P95)).
       - **최소 표본 요건 = 페어 5개.** 미달이면 threshold_deg: null 로 박제 —
         수치를 지어내지 않는다. null 이면 Task 2 배선은 캡션 전면 비활성(fail-closed)
         으로 나가고 SUMMARY 가 belle 결정 항목으로 올린다.
    2. **스카우트 + 측정:** measure_noise.mjs 작성 (interface_notes 의 createRequire +
       ADC 키 파일 인증). Firestore **읽기 전용** — users/*/analyses 를 순회해
       status done + result.deductionBreakdown.records 보유 doc 만 수집, (uid,
       mode, referenceMotionId, criterion, delta, createdAt, fileName, anglesFrames)
       튜플로 축약. **PII 방어 (T-oe1-01):** bodyProfile·영상 URL·키는 읽지도 출력하지도
       않는다. 산출물의 uid 는 앞 6자 + '…' 로 절단.
    3. **결과 박제:** NOISE-MEASUREMENT.md 에 "## 측정 결과" 섹션 — 전체 doc 수,
       같은-영상 페어 수 / 48h 페어 수, criterion 별 |Δdelta| 분포 (min/median/P95/max),
       그리고 기계 판독 행 하나를 정확히 이 형식으로:
       `threshold_deg: <정수>` 또는 `threshold_deg: null`
       규칙 적용 산출 과정(어느 분포에서 P95 가 얼마)을 수치로 남긴다. 예측 대비
       실측 자평 1줄 (결정론 ON 페어가 정말 Δ≈0 이었는지) 포함.
    4. 스크립트·산출물 커밋. 스크립트는 재실행 가능하게 남긴다 (다음 캡션 대상
       동작 추가 시 재측정 도구).
  </action>
  <verify>
    <automated>test -f /Users/kimtaesung/Dev/SunityMotion/.planning/quick/260822-oe1-progress-caption/NOISE-MEASUREMENT.md && grep -qE '^threshold_deg: ([0-9]+|null)$' /Users/kimtaesung/Dev/SunityMotion/.planning/quick/260822-oe1-progress-caption/NOISE-MEASUREMENT.md && git -C /Users/kimtaesung/Dev/SunityMotion log --oneline -- .planning/quick/260822-oe1-progress-caption/NOISE-MEASUREMENT.md | tail -1 | grep -q .</automated>
  </verify>
  <done>판정 규칙 커밋이 측정 결과 커밋보다 먼저 존재 (git log 2건). threshold_deg 행이
  기계 판독 형식으로 존재 — 값이면 규칙 산출 과정이 수치로 추적되고, null 이면 표본
  미달이 페어 수로 증명된다. Firestore 쓰기 0. PII 산출물 유입 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 측정값 소비하는 발전 캡션 배선 — 순수 lib + 렌더 + result.tsx</name>
  <files>app/src/lib/progressCaption.ts, app/src/lib/illustrationHow.ts, app/src/lib/__tests__/progressCaption.test.ts, app/src/components/DefectIllustration.tsx, app/src/app/analysis/result.tsx</files>
  <behavior>
    - findPreviousComparable(analyses, { analysisId, createdAt, referenceMotionId }) →
      createdAt 이 현재보다 작은 것 중 가장 최신의 done mode1 + 같은 referenceMotionId
      doc. 자기 자신(analysisId 동일) 제외. 없으면 null. mode3/타 동작/미완료 doc 무시.
    - extractCriterionMeasure(doc, 'split_angle') → 그 doc 의 records 에서 criterion
      정확 일치 첫 record 의 { measured, target, unit }. records 없음/불일치 → null.
    - buildProgressCaption(asset, current, prev):
      · 개선 = prevDelta − curDelta (delta = |target − measured|). 개선 ≥ 문턱이고
        두 쪽 다 unit 'deg' + 유한값이고 앵커에 progressSentence 있으면 → 그 문장.
      · fail-closed 전 축 각각 null: 문턱 null / prev null / unit 'deg' 아님 /
        non-finite / 개선 < 문턱 / 악화(개선 음수) / 미등록 asset / progressSentence 없는 앵커.
    - buildHowOverlay 기존 축 전부 불변 (illustrationHow.test.ts 197 기준선 유지).
  </behavior>
  <action>
    1. illustrationHow.ts: `HowAnchorsBaked` 에 optional `progressSentence?: string` 추가,
       `HOW_ANCHORS['ref-kip-up--leg']` 에 `progressSentence: '저번보다 더 벌어졌어요'`
       (BELLE-0821-P2 — belle 08-21 화법 원문 그대로. 수치 삽입·어미 변형·악화 문구
       발명 금지. 헤더 주석에 belle 08-21 원문 인용 박제). buildHowOverlay 로직 무접촉.
    2. 신규 app/src/lib/progressCaption.ts — 순수 함수만 (RN/firebase import 0,
       types/analysis.ts 는 `import type` 만 → node --test 가능):
       - `PROGRESS_NOISE_THRESHOLD_DEG: number | null` = **Task 1 NOISE-MEASUREMENT.md 의
         threshold_deg 값 그대로** (BELLE-0821-P3). 주석에 산출물 경로 + 측정일 +
         페어 수 박제 — 이 상수의 유일한 출처는 그 측정이다. null 이면 캡션 전면
         비활성이라는 의미도 주석에 명기.
       - behavior 블록의 함수 3종. buildProgressCaption 은 HOW_ANCHORS 를 import 해
         progressSentence 를 읽는다 (문장 사본 금지 — 단일 소스).
    3. 신규 __tests__/progressCaption.test.ts: behavior 블록 축 전부 (node:test +
       node:assert/strict + `.ts` import, 기존 규약). RED 커밋 → GREEN 커밋 순서.
       문턱값 축 테스트는 상수를 직접 참조하지 말고 함수에 문턱을 주입할 수 있게
       설계하거나(마지막 인자 default = 상수), 상수 null/수치 양쪽 분기를 커버.
    4. DefectIllustration.tsx: prop `prevHow?: { measured?, target?, unit? } | null` 추가.
       `overlay != null && prevHow` 일 때만 `buildProgressCaption(matched, how, prevHow)`
       호출 — **수치 문장이 있는 곳에만 캡션이 붙는다** (BELLE-0821-P4, 오버레이 게이트는
       컴포넌트 소유·캡션 게이트는 lib 소유로 이중 소유 없이 분리). 캡션은 카드 **아래**
       Text 로 렌더 (gb7 시뮬 실측 — 시트 카드 약 189pt 에서 오버레이 pill 이 화살표를
       가렸던 전례가 있어 오버레이 안에 두 번째 pill 을 넣지 않는다). 스타일 = 기존
       typography.caption + colors.textMid 계열 theme 토큰만, 신규 hex/수치 0. 주석에
       belle 08-21 판정 날짜 + 이 플랜 인용.
    5. result.tsx: `useMyAnalyses({ doneOnly: true })` 를 AnalysisResultContent 최상위에
       무조건 호출로 추가 (조건부 훅 금지 — 기존 M-04 규율). illustrationSlot 클로저에서
       mode1 일 때만 `findPreviousComparable(analyses, { analysisId, createdAt, referenceMotionId:
       cmp.referenceMotionId })` → `extractCriterionMeasure(prevDoc, sheetView.primaryCriterion)`
       → `<DefectIllustration ... prevHow={...} />`. mode3/직전 없음 → prevHow null.
       DeductionDetailSheet.tsx 무접촉 (시트 계약 불변). 신규 Firestore 쿼리 0 —
       기록 탭과 같은 구독 재사용 (인덱스 면제 설정 무접촉).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck && node --test src/lib/__tests__/progressCaption.test.ts src/lib/__tests__/illustrationHow.test.ts</automated>
  </verify>
  <done>typecheck 통과. progressCaption 신규 축 전부 통과 + illustrationHow 기존 축 회귀 0.
  캡션 문구는 HOW_ANCHORS 단일 소스의 belle 원문뿐. 문턱 상수의 출처 주석이
  NOISE-MEASUREMENT.md 를 가리킨다. DeductionDetailSheet diff 0.</done>
</task>

<task type="auto">
  <name>Task 3: 전량 회귀 + 시뮬 실증 (캡션 표시/억제 양면) — OTA 금지</name>
  <files>.planning/quick/260822-oe1-progress-caption/sim_progress_caption.png</files>
  <action>
    1. 전량 게이트: `npm run typecheck` + `node --test app/src/lib/__tests__/*.test.ts` —
       기지 실패 1건(pdshape/arm provenance) 외 회귀 0. `git diff --stat` 으로 변경
       범위가 files_modified 목록뿐임을 확인.
    2. 시뮬 데이터 스카우트: measure_noise.mjs 재사용(읽기 전용)으로 시뮬 uid 계열에
       done mode1 킵업(split_angle record 보유) 분석이 2건 이상인지 확인.
       부족하면 기존 시뮬 킵업 doc 을 **시뮬 uid 아래에만** 복사해 직전 doc 을 시드
       (createdAt 을 과거로, split_angle deficit 을 문턱 초과만큼 크게, fileName 에
       'test-progress' 표기 — quick-260801-f77 doc 복사 선례). **belle uid 대상 쓰기
       절대 금지 (T-oe1-02): 스크립트에 대상 uid 가 시뮬 uid 인지 assert 를 넣는다.**
       Task 1 이 threshold_deg: null 이었으면 시드 없이 "캡션 미표시가 정답" 확인만.
    3. 시뮬 실렌더 (gb7 Task 3 절차): 기록 탭 → 킵업 → 다리 부위 상세 시트.
       (a) 개선 ≥ 문턱 케이스 — 그림 + 화살표 + 수치 문장 + 그 아래
       '저번보다 더 벌어졌어요' 캡처 → sim_progress_caption.png 저장.
       (b) 직전 없음/문턱 미만 케이스 — 캡션이 없고 gb7 승인 겉모습(how_card_20-1.png)
       과 동일함을 육안 대조 (기존 수치 문장·화살표 불변 확인).
    4. 시드 doc 을 만들었으면 캡처 후 삭제해 시뮬 계정 상태 복원 (삭제도 시뮬 uid
       assert 통과 후에만).
    5. 시뮬 기동 불가 시 SUMMARY 에 "미실증" 명기 — 거짓 실증 금지.
    6. **OTA 발행 금지** (verify-ui-on-simulator-before-ota) — `eas update` 실행 0.
       belle 이 시뮬 확인을 본 뒤에만 별도 결정. 시뮬·Metro 는 belle 확인용으로 켜 둔다.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/app && npm run typecheck && node --test src/lib/__tests__/*.test.ts 2>&1 | tail -5</automated>
    <human-check>캡처(또는 미실증 명기)가 SUMMARY 에 있고, 캡션 표시 케이스와 미표시 케이스 양면이 실증됐다. OTA 미발행.</human-check>
  </verify>
  <done>typecheck 통과 + 테스트 기지 실패 1건 외 회귀 0 + 변경 범위 = files_modified 뿐.
  캡처 저장(양면) 또는 SUMMARY "미실증" 명기. 시드 doc 잔류 0. belle uid 쓰기 0. OTA 미발행.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firestore doc → 앱 렌더 | prev doc 의 record 값도 백엔드 산출값 — normalize 경로로 방어적 파싱된 AnalysisDoc 만 소비 |
| Admin SDK → Firestore | 측정·시드 스크립트가 rules 를 우회한다 — 읽기 전용 원칙 + uid allowlist |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-oe1-01 | Information Disclosure | measure_noise.mjs 산출물 | mitigate | bodyProfile·영상 URL·키 미수집, uid 6자 절단 — 산출물은 수치 분포만 (홈 디렉터리=git 저장소 PII 규율) |
| T-oe1-02 | Tampering | 시뮬 시드 스크립트 | mitigate | 쓰기 전 대상 uid 가 시뮬 uid 인지 assert, belle uid(csKWYvI3…) 거부. 캡처 후 시드 삭제 |
| T-oe1-03 | Tampering | buildProgressCaption 입력 | mitigate | fail-closed 전 축 (unit/finite/문턱/악화/앵커) — Task 2 테스트로 봉인 |
| T-oe1-SC | Tampering | 패키지 설치 | accept | 신규 패키지 설치 0 (firebase-admin 기설치 devDependency 재사용) — 공급망 표면 무변화 |
</threat_model>

<verification>
- NOISE-MEASUREMENT.md: 판정 규칙 커밋 → 측정 결과 커밋 순서 (git log 로 증명),
  `threshold_deg:` 기계 판독 행 존재
- `npm run typecheck` 통과 + `node --test app/src/lib/__tests__/*.test.ts` 기지 실패
  1건 외 전부 통과 (기준선 198/197+1 에서 신규 축만 증가, 회귀 0)
- DeductionDetailSheet.tsx diff 0 · buildHowOverlay 로직 diff 0 (캡션 없는 경로의
  겉모습 불변)
- 시뮬 캡처(표시/미표시 양면) 또는 "미실증" 명기 · OTA 미발행 · 시드 doc 잔류 0
</verification>

<success_criteria>
- belle 08-21 승인 실현: 같은 동작 직전 분석 대비 문턱 이상 개선 시에만
  "저번보다 더 벌어졌어요"(원문) 캡션이 수치 문장에 이어진다
- 문턱값의 유일한 출처 = Task 1 실측 (측정 전 확정 수치 0, 표본 미달이면 정직한
  전면 비활성 + belle 결정 항목 승격)
- fail-closed 불변: 기존 오버레이·수치 문장 규율(<3°/값 없음)과 캡션 규율(직전
  없음/문턱 미만/악화)이 각각 독립으로 닫힌다
- OTA 는 belle 시뮬 확인 후 별도 — 이 플랜 범위 밖
</success_criteria>

<output>
`.planning/quick/260822-oe1-progress-caption/260822-oe1-SUMMARY.md` 작성 — threshold_deg
실측값(또는 null 과 페어 수), 시뮬 실증 여부(캡처 경로 또는 "미실증"), belle 결정
대기 항목(문턱 null 시 캡션 활성화 여부 / OTA 발행) 명기.
</output>
