---
phase: quick-260824-gt1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/quick/260824-gt1-ghost-noarrow-gemini-belle/measure_powerspin_leg.mjs
  - .planning/quick/260824-gt1-ghost-noarrow-gemini-belle/PREDICTION.md
  - .planning/quick/260824-gt1-ghost-noarrow-gemini-belle/generate_ghost_powerspin.py
  - .planning/quick/260824-gt1-ghost-noarrow-gemini-belle/meta.json
  - .planning/quick/260824-gt1-ghost-noarrow-gemini-belle/out/
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05]

must_haves:
  truths:
    - "잔상(오류) 자세 서술이 Firestore 실측(leg_extension·split_angle 방향·크기 분포)에서 도출되어 PREDICTION.md 에 박제되어 있다 — 지어낸 자세 0 (D-01)"
    - "예측 커밋이 out/ 생성물 커밋보다 git 이력에서 앞선다 (예측 사전 박제 — exq 게이트 승계)"
    - "ghost-noarrow 문법(화살표·수치·텍스트·빨간 표시 0)의 파워스핀 다리 잔상 후보 4장 내외가 quick dir 에 존재한다 (D-03, D-04)"
    - "전 장을 Read 도구로 직접 열어본 D-03 스크린 + 자평이 PREDICTION.md 에 박제되어 있다 (실물 게이트)"
    - "app/ 아래 파일 변경 0, Firestore/S3 쓰기 0 (D-05 배선 금지)"
  artifacts:
    - path: ".planning/quick/260824-gt1-ghost-noarrow-gemini-belle/measure_powerspin_leg.mjs"
      provides: "Firestore 읽기 전용 파워스핀 다리 record 분포 측정 (코퍼스 제외 + PII 마스크)"
      min_lines: 80
    - path: ".planning/quick/260824-gt1-ghost-noarrow-gemini-belle/PREDICTION.md"
      provides: "실측 절(분포 표 + 전형 오류 자세 서술 1~2문장) + 예측 절 + 자평 절"
      contains: "전형 오류 자세"
    - path: ".planning/quick/260824-gt1-ghost-noarrow-gemini-belle/generate_ghost_powerspin.py"
      provides: "승인 레시피(260809 generate.py) importlib 재사용 + 파워스핀 잔상 GUIDE 치환 하네스"
      min_lines: 60
    - path: ".planning/quick/260824-gt1-ghost-noarrow-gemini-belle/meta.json"
      provides: "파일명 -> 오류유형(criterion) -> 실측 deficit 매핑 + Gemini 모델·호출 수 (배선 재료 + LLM 사용 보고)"
      contains: "criterion"
    - path: ".planning/quick/260824-gt1-ghost-noarrow-gemini-belle/out/"
      provides: "이미지 4장 내외 + prompt_*.txt 사본"
  key_links:
    - from: "measure_powerspin_leg.mjs 실측 분포"
      to: "PREDICTION.md 전형 오류 자세 서술"
      via: "부호(measured-baseline) 방향 + median/P95 크기에서 서술 도출 — 실측 없이 자세 지어내기 금지"
      pattern: "leg_extension"
    - from: "generate_ghost_powerspin.py"
      to: ".planning/quick/260809-ill-missing-illustrations/generate.py"
      via: "importlib.util.spec_from_file_location 로 모듈 로드 후 PROMPT 골격·헬퍼 재사용 (L-4 무변경)"
      pattern: "spec_from_file_location"
    - from: "generate_ghost_powerspin.py 잔상 GUIDE"
      to: "260818-nnm generate_how.py HOW_GUIDE['ghost-noarrow']"
      via: "잔상 다리 서술 절만 치환, NO arrows/text/marker 절과 '같은 사람의 모션 트레일' 절은 공통 유지"
      pattern: "NO arrows"
---

<objective>
파워스핀 다리(`ref-power-spin--leg`) ghost-noarrow 잔상 후보 생성 — 실사용 감점 doc 미커버 1위
(leg_extension 10건 + split_angle 9건, 오케스트레이터 08-24 실측)의 "어떻게" 일러스트 재료를
belle 판정용 실물로 만든다. 잔상 자세는 Firestore 실측에서 도출하고, 승인 레시피를 재사용하며,
생성 전 예측을 커밋으로 박제하고, 전 장을 직접 열어 자평한다. 배선은 하지 않는다.

이 태스크의 잠긴 결정:
- D-01: 잔상(오류) 자세 서술은 Task 1 실측(record 의 baselineValue/measuredValue 방향·크기 분포)에서
  도출한다. 실측 없이 잔상 자세를 지어내지 않는다.
- D-02: 승인 레시피 재사용 — 260809 generate.py 의 PROMPT 골격(익명·의상·SCENE·ANATOMY·STYLE·프레이밍)을
  importlib 로 로드해 바이트 무변경으로 쓰고, GUIDE 문단만 파워스핀 잔상 절로 치환한다 (L-4).
- D-03: 그림 자체에 화살표·수치·텍스트·빨간 표시 0 (ghost-noarrow 승인 문법, belle 08-18) —
  화살표·수치는 나중에 앱이 렌더한다. "좁다" 류 부정 서술 금지는 앱 문구 규율이므로 이 태스크 무관.
- D-04: 대상 = `ref-power-spin--leg` 1종만. 다른 동작 확산은 belle 판정 후.
- D-05: 배선 금지 — app/ 디렉터리 무접촉, Firestore/S3 쓰기 0. 그림 승인 전 배선 금지
  (메모리 grammar-approval-is-not-asset-approval, 08-18 원복 전례). belle 판정 요청 문구도 쓰지 않는다
  (판정은 오케스트레이터가 대화로).

Purpose: 감점은 나가는데 그림이 없는 최다 부위(파워스핀 다리)에 "어떻게" 잔상 에셋 후보를 만든다.
Output: measure_powerspin_leg.mjs + PREDICTION.md(실측·예측·자평) + generate_ghost_powerspin.py +
out/ 이미지 4장 내외 + prompt 사본 + meta.json.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/quick/260824-bxf-criterion-split-angle-12-split-21/measure_split_flip.mjs
@.planning/quick/260821-exq-3-ghost-noarrow-20-40-60-belle/generate_ghost3.py
@.planning/quick/260821-exq-3-ghost-noarrow-20-40-60-belle/PREDICTION.md
@.planning/quick/260818-nnm-illustration-how-first/generate_how.py
@.planning/quick/260809-ill-missing-illustrations/generate.py
</context>

<interface_notes>
사전 확인 완료(플래너 실측) — 실행 시 재탐색 불요:
- targets.json 에 `ref-power-spin--leg` row 존재: guideType "angle", orientation null,
  cropBox/cropNote null(= FRAMING_FULL), promptPose = "hanging inverted from a single vertical
  pole, one leg extended straight up alongside the pole and the other extended straight down
  in a vertical split, one arm gripping the pole", inputFrame =
  `/Users/Shared/sunity-motion-260816-ill2-cache/ill2/crop/powerspin_full.jpg` (존재 확인),
  anchor = `app/assets/illustrations/ref-invert.jpg` (존재 확인). row 데이터는 손대지 않는다.
- 260809 generate.py 노출 심볼: `G.PROMPT`(guide/pose/orientation/framing 4 슬롯),
  `G._orientation_hint`, `G._framing_block`, `G.inline_part`, `G.load_targets`, `G.asset_name`,
  `G.resolve`, `G.ENDPOINT`, `G.MODEL`(= "gemini-3-pro-image").
- Firestore 읽기 앵커: `createRequire(new URL('../../../app/scripts/measure_noise_anchor.mjs',
  import.meta.url))` — 이 파일은 존재하지 않으며 app/node_modules 해석 기준 경로로만 쓰인다
  (bxf 선례 그대로, app/ 무접촉). 실행 env: `GOOGLE_APPLICATION_CREDENTIALS=
  /Users/kimtaesung/Dev/SunityMotion/firebase-sa.json`.
- record 스키마(bxf splitDelta 와 동일): `result.deductionBreakdown.records[]` 의
  `{criterion, unit:'deg', baselineValue, measuredValue}`. 파워스핀 판별 =
  `result.comparison.referenceMotionId === 'ref-power-spin'`.
</interface_notes>

<tasks>

<task type="auto">
  <name>Task 1: 실측 — 파워스핀 다리 record 방향·크기 분포 (Firestore 읽기 전용)</name>
  <files>.planning/quick/260824-gt1-ghost-noarrow-gemini-belle/measure_powerspin_leg.mjs, .planning/quick/260824-gt1-ghost-noarrow-gemini-belle/PREDICTION.md</files>
  <action>
    (1) `measure_powerspin_leg.mjs` 작성 — bxf `measure_split_flip.mjs` 의 골격(앵커 createRequire,
    listDocuments 순회, select() 필드 마스크)을 재사용하되 페어 로직은 버리고 단일-doc 분포 측정으로 단순화 (D-01):
    - select 필드: `mode`, `status`, `createdAt`, `result.comparison.referenceMotionId`,
      `result.deductionBreakdown.records` 만. bodyProfile·영상 URL·키는 읽지도 않는다 (PII 마스크).
    - 실사용 필터 (코퍼스 제외 — 정확히 이 목록):
      uid 접두 제외 `phase* / mock* / genpod* / e2e* / sweep* / eval* / test*` + uid 정확 일치 제외
      `det / kipdia / devdia / solo / pode2e`, doc id 접두 제외 `p34* / p35* / kipupFault* / testprogress*`.
    - 표본: status done + referenceMotionId === 'ref-power-spin' 인 doc 의 records 중
      unit 'deg' 이고 criterion 이 `leg_extension` 또는 `split_angle` 인 유효 record 전건
      (비유한값 skip — bxf 선택 규칙 승계, 단 여기는 첫 record 가 아니라 해당 criterion 전 record).
    - criterion 별 산출: n, 부호별 건수(measured < baseline = 부족 방향 vs measured > baseline),
      signed delta(measured - baseline)의 min/median/P95/max, |delta| median. 출력은 markdown 표,
      uid 는 앞 6자 + '…' 절단만 노출.
    - 쓰기 API 호출 0: get/listDocuments 만. 집계 컨테이너는 평범한 객체/배열만 사용 —
      Set/Map 금지 (`.add(`/`.set(` 가 쓰기 grep 게이트와 표기가 겹친다, bxf 관행).
    (2) 실행: `GOOGLE_APPLICATION_CREDENTIALS=/Users/kimtaesung/Dev/SunityMotion/firebase-sa.json
    node .planning/quick/260824-gt1-ghost-noarrow-gemini-belle/measure_powerspin_leg.mjs`
    (repo 루트에서). 출력을 PREDICTION.md `## 실측 (생성 전)` 절에 그대로 붙인다.
    (3) 실측에서 "전형 오류 자세" 서술 1~2문장을 도출해 같은 절에 박제한다 — 예:
    leg_extension 부족이 median N° 면 "무릎이 기준 대비 약 N° 굽음", split_angle 부족이 median M° 면
    "수직 스플릿 벌림이 기준 대비 약 M° 좁음(윗다리가 덜 올라가거나 아랫다리가 덜 내려감)".
    문장은 반드시 측정된 방향·크기에서만 나온다 — 방향이 예상과 다르면(예: 초과 방향 우세)
    그 관측을 그대로 적는다 (관측만, 진단 승격 금지).
    (4) 오케스트레이터 08-24 관측(leg_extension 10건 / split_angle 9건)과 건수를 대조해
    크게 어긋나면 강제로 맞추지 말고 차이를 그대로 병기한다.
  </action>
  <verify>
    <automated>node --check /Users/kimtaesung/Dev/SunityMotion/.planning/quick/260824-gt1-ghost-noarrow-gemini-belle/measure_powerspin_leg.mjs && ! grep -qE "\.(set|update|delete|add|create|batch|recursiveDelete)\(" /Users/kimtaesung/Dev/SunityMotion/.planning/quick/260824-gt1-ghost-noarrow-gemini-belle/measure_powerspin_leg.mjs && grep -q "전형 오류 자세" /Users/kimtaesung/Dev/SunityMotion/.planning/quick/260824-gt1-ghost-noarrow-gemini-belle/PREDICTION.md && grep -q "leg_extension" /Users/kimtaesung/Dev/SunityMotion/.planning/quick/260824-gt1-ghost-noarrow-gemini-belle/PREDICTION.md</automated>
  </verify>
  <done>스크립트가 문법 유효 + Firestore 쓰기 메서드 호출 0 이고, PREDICTION.md 실측 절에 criterion 별 분포 표(방향·크기)와 실측 유래 "전형 오류 자세" 서술 1~2문장이 있다. uid 는 6자 절단, bodyProfile·URL 미수집.</done>
</task>

<task type="auto">
  <name>Task 2: 예측 박제 커밋 (생성 전) → Gemini 후보 생성 + meta.json</name>
  <files>.planning/quick/260824-gt1-ghost-noarrow-gemini-belle/PREDICTION.md, .planning/quick/260824-gt1-ghost-noarrow-gemini-belle/generate_ghost_powerspin.py, .planning/quick/260824-gt1-ghost-noarrow-gemini-belle/out/, .planning/quick/260824-gt1-ghost-noarrow-gemini-belle/meta.json</files>
  <action>
    (1) PREDICTION.md 에 `## 예측 (생성 전)` 절 추가 — exq PREDICTION.md 형식 승계. 반드시 포함:
    오류 유형별(실측이 가른 1~2개) 잔상이 실선과 구분되게 그려질지 / 파워스핀 수직 스플릿에서
    잔상 다리가 두 번째 사람·폴과 혼동될 위험(도립 자세라 잔상 윗다리가 폴과 평행 — 신규 위험 축) /
    총 몇 장이 쓸 만할지 숫자 박제. 자를 미리 정한다: 통과 기준 = 잔상(내 위치)→실선(정은지 목표)
    방향이 한눈에 읽히고 표기 0. 장부 최신 전적(belle 눈 08-21 기준 6전 2승, 이번이 7전째) 명기.
    (2) `generate_ghost_powerspin.py` 작성 — exq `generate_ghost3.py` 구조 승계 (D-02):
    - 260809 generate.py 를 importlib 로드, `G.PROMPT`/`G._orientation_hint`/`G._framing_block`/
      `G.inline_part`/`G.load_targets`/`G.asset_name`/`G.resolve`/`G.ENDPOINT` 재사용. targets.json 의
      `ref-power-spin--leg` row 를 그대로 쓴다 (--asset 기본값, row 데이터 무변경).
    - GUIDE 치환: HOW_GUIDE["ghost-noarrow"] 의 구조를 파워스핀에 맞춰 쓴다. 공통 유지 절(의도 불변):
      잔상 = 다리 두 개만, 몸통·팔·머리·폴은 실선 1회 / "Draw NO arrows, NO angle marker,
      NO measurement line, NO number, NO text, NO red mark of any kind" (D-03, 문자 그대로 유지) /
      "The ghost must read as a motion trail of the same person, never as a second person" /
      잔상 두 다리는 각각 별개 윤곽으로 읽혀야 함. 킵업 전용 절("one on each side of the pole",
      "spread wide")은 파워스핀 수직 스플릿 기하에 맞게 고쳐 쓴다 — 실선 after 는 row promptPose 의
      수직 스플릿(윗다리 폴 따라 위로 곧게, 아랫다리 아래로 곧게) 그대로.
    - 잔상 다리 서술(before)은 **Task 1 의 전형 오류 자세 서술에서 영역**한다: 오류 유형별로 1개
      프롬프트 — leg_extension 유형이면 무릎 굽음(굽은 정도는 실측 median 크기감을 반영한 정성 표현,
      수치 문자열은 프롬프트에 넣지 않음 — 그림에 숫자가 새는 것을 원천 차단), split_angle 유형이면
      스플릿 덜 벌어짐. 실측에 없는 오류 자세를 창작하지 않는다 (D-01).
    - CLI: `--asset` 기본 `ref-power-spin--leg` (D-04), `--types` = 실측이 가른 오류 유형(1~2개),
      `--n` 기본 2, `--out` 기본 out/. 총 4장 내외 — 유형 2개면 유형당 2장, 유형 1개면 그 유형 3~4장.
      출력 파일명 `ref-power-spin--leg__ghost-{type}-{i}.jpg`, 프롬프트 사본 `out/prompt_{type}.txt`.
      기존 출력 파일은 건너뜀(부분 실패 시 같은 커맨드 재실행 = 재시도). 표준 라이브러리만, 이모지 금지.
    (3) **생성 실행 전에** PREDICTION.md(실측+예측) + measure_powerspin_leg.mjs +
    generate_ghost_powerspin.py 를 커밋한다: `docs(quick-260824-gt1): 파워스핀 잔상 실측+예측 박제 (생성 전)`.
    이 시점에 out/ 이 존재하지 않아야 한다 — 예측 박제의 git 증명.
    (4) 생성 실행 — 키는 SSM 에서 환경변수로만 (파일·로그·stdout 에 키 값 0):
    `GEMINI_API_KEY=$(aws ssm get-parameter --name /sunity/motion/gemini-api-key --with-decryption
    --profile sunity-motion --region ap-northeast-2 --query 'Parameter.Value' --output text)
    python3 generate_ghost_powerspin.py --out out/`. HTTP 오류 시 오류 원문(키 제외)을 보고하고 중단.
    부분 성공이면 재실행 1회, 그래도 미달이면 몇 장 나왔는지 사실대로 기록하고 진행 (있는 실물만 자평).
    (5) `meta.json` 작성: asset, grammar("ghost-noarrow"), model(G.MODEL 값), geminiCallCount(실제 호출 수),
    errorTypes 맵 — 유형별 `{criterion, measuredMedianDeg(실측값), files(실존 파일명 배열)}`.
    모델·호출 수는 SUMMARY 에도 기록한다 (LLM 사용 보고 규율).
  </action>
  <verify>
    <automated>rtk git log --oneline -- .planning/quick/260824-gt1-ghost-noarrow-gemini-belle/ | grep -q "생성 전" && test $(ls /Users/kimtaesung/Dev/SunityMotion/.planning/quick/260824-gt1-ghost-noarrow-gemini-belle/out/*.jpg | wc -l) -ge 4 && grep -l "NO arrows" /Users/kimtaesung/Dev/SunityMotion/.planning/quick/260824-gt1-ghost-noarrow-gemini-belle/out/prompt_*.txt >/dev/null && python3 -c "
import json, os
d='/Users/kimtaesung/Dev/SunityMotion/.planning/quick/260824-gt1-ghost-noarrow-gemini-belle'
m=json.load(open(d+'/meta.json'))
assert m['model'] and m['geminiCallCount']>=1
for t in m['errorTypes'].values():
    assert t['criterion'] in ('leg_extension','split_angle')
    for f in t['files']: assert os.path.exists(d+'/out/'+f), f
"</automated>
  </verify>
  <done>예측 커밋이 out/ 생성 전에 존재하고(git 이력), out/ 에 이미지 4장 내외 + 오류 유형별 prompt 사본이 있으며(프롬프트에 D-03 절 포함), meta.json 이 파일명→criterion→실측 deficit 매핑과 Gemini 모델·호출 수를 담는다.</done>
</task>

<task type="auto">
  <name>Task 3: 실물 게이트 — 전 장 Read 스크린(D-03) + 자평 박제 + 전체 커밋</name>
  <files>.planning/quick/260824-gt1-ghost-noarrow-gemini-belle/PREDICTION.md</files>
  <action>
    (1) **out/ 의 생성 이미지 전부를 Read 도구로 직접 연다** (실물 게이트 — 안 열어보고 제시 금지).
    장별 D-03 스크린: 화살표·수치·텍스트·빨간 표시 0 인가 / 잔상이 같은 사람의 모션 트레일로
    읽히는가(두 번째 사람 오독 0) / 잔상 다리가 실선·폴과 구분되는가 / before(오류)→after(목표)
    방향이 맞는가 — exq 에서 발견된 방향 역전(잔상이 실선보다 목표에 가까움)을 명시적으로 확인.
    (2) PREDICTION.md 에 `## 내 자평 (belle 판정 전, 전 장을 직접 열어본 뒤)` 절 추가 —
    예측 축별 대조표(예측 vs 결과) + 유형별 추천/탈락과 사유. D-03 위반 장은 즉시 탈락 처리.
    "★단 이건 내 눈이다 — belle 판정과 다를 수 있다" 단서와 장부 전적 유지.
    belle 판정란은 비워 둔다 — 판정 요청 문구를 쓰지 않는다 (D-05, 판정은 오케스트레이터가 대화로).
    (3) 전부 커밋: 이미지 + prompt 사본 + meta.json + PREDICTION.md 갱신 + PLAN.md.
    커밋 메시지: `feat(quick-260824-gt1): 파워스핀 다리 ghost-noarrow 후보 + 자평 박제 (belle 판정 전)`.
    app/ 아래는 아무것도 건드리지 않았음을 확인한다 (D-05).
  </action>
  <verify>
    <automated>grep -q "내 자평" /Users/kimtaesung/Dev/SunityMotion/.planning/quick/260824-gt1-ghost-noarrow-gemini-belle/PREDICTION.md && test $(rtk git status --porcelain -- .planning/quick/260824-gt1-ghost-noarrow-gemini-belle/ | wc -l) -eq 0 && test $(rtk git status --porcelain -- app/ | wc -l) -eq 0</automated>
  </verify>
  <done>PREDICTION.md 에 전 장의 D-03 스크린 결과와 예측 대조 자평(추천/탈락+사유)이 있고 판정란은 비어 있으며, quick dir 전체가 커밋되어 미커밋 잔여 0, app/ 변경 0 이다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로컬 → Firestore | 서비스 계정 자격으로 실사용 분석 doc 을 읽음 (PII 인접) |
| 로컬 → Gemini API | SSM 키가 HTTP 요청에 실림 (환경변수 경유) |
| Gemini 응답 → 로컬 파일 | 생성 이미지가 quick dir 에만 저장됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-gt1-01 | Information Disclosure | 실사용 doc PII | mitigate | select() 필드 마스크(bodyProfile·URL·키 미수집) + 출력 uid 6자 절단 (bxf T-bxf-01 승계) |
| T-gt1-02 | Tampering | Firestore | mitigate | 읽기 API(get/listDocuments)만 — Task 1 grep 게이트가 쓰기 메서드 호출 부재를 기계 검증 (bxf T-bxf-02 승계) |
| T-gt1-03 | Information Disclosure | GEMINI_API_KEY | mitigate | SSM→환경변수로만 주입, 파일·로그·stdout·커밋 어디에도 키 문자열 금지 (T-33C4-04 관행 승계) |
| T-gt1-04 | Tampering | 생성물 경로 | mitigate | 출력은 quick dir 안으로 한정, app/ 변경 0 을 Task 3 verify 에서 기계 확인 (D-05) |
| T-gt1-SC | Tampering | 패키지 설치 | accept | 신규 패키지 0 — 표준 라이브러리 + 기존 app/node_modules firebase-admin(해석만) 사용, 설치 태스크 없음 |
</threat_model>

<verification>
- 커밋 순서: `rtk git log --oneline -- .planning/quick/260824-gt1-ghost-noarrow-gemini-belle/` 에서
  "생성 전" 커밋이 "자평 박제" 커밋보다 아래(먼저)에 있다.
- 전 프롬프트 사본에 "NO arrows" · "NO text" 절 존재: `grep -L "NO arrows" out/prompt_*.txt` 출력 0행.
- `rtk git status --porcelain -- app/` 출력 0행 (D-05).
- measure_powerspin_leg.mjs 에 Firestore 쓰기 메서드 호출 0 (Task 1 grep 게이트).
- PREDICTION.md 실측 절의 전형 오류 자세 서술이 분포 표의 방향·크기와 정합한다 (지어낸 자세 0).
</verification>

<success_criteria>
- 파워스핀 다리 잔상 후보 4장 내외가 ghost-noarrow 문법(표기 0)으로 quick dir 에 커밋되어 있고,
  잔상 자세는 실사용 실측(leg_extension·split_angle 방향·크기)에서 도출되었다 (D-01~D-04).
- 실측·예측이 생성 전에, D-03 스크린 + 자평(전 장 실물 확인)이 생성 후에 PREDICTION.md 에 박제되어 있다.
- meta.json 이 파일명→오류유형(criterion)→실측 deficit 매핑 + Gemini 모델·호출 수를 담아
  승인 후 배선 재료 + LLM 사용 보고로 쓸 수 있다.
- app/ 변경 0, Firestore/S3 쓰기 0 — belle 판정·배선·게시는 이 플랜 범위 밖 (D-05).
</success_criteria>

<output>
Create `.planning/quick/260824-gt1-ghost-noarrow-gemini-belle/260824-gt1-SUMMARY.md` when done.
SUMMARY 에 Gemini 모델·호출 수(LLM 사용 보고)와 실측 요지(criterion 별 n·방향·median)를 반드시 포함.
</output>
