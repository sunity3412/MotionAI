---
phase: quick-260824-q6p
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/shared/python/sunity_shared/s3keys.py
  - backend/shared/python/sunity_shared/models.py
  - backend/functions/pipeline/app.py
  - backend/functions/playback-url/app.py
  - backend/tests/test_playback_url_fault_zoom.py
  - docs/contract.md
  - app/src/types/analysis.ts
  - app/src/lib/api.ts
  - app/src/lib/faultZoomUrls.ts
  - app/src/lib/__tests__/faultZoomUrls.test.ts
  - app/src/app/analysis/result.tsx
  - app/src/components/DeductionDetailSheet.tsx
autonomous: true
requirements: [QUICK-260824-Q6P]

must_haves:
  truths:
    - "7일 넘은 분석 doc 의 확대비교(faultZoomComparisons) 이미지가 열람 시점 재발급 URL 로 표시된다 — 기존 doc 소급 (imageKey 없는 doc 도 저장 imageUrl 의 key 를 서버가 파싱해 canonical exact 비교 후 재서명, 백필 0)"
    - "타 사용자 키 재서명 불가 — 토큰 uid 로 canonical key 를 구성하므로 타 uid 키는 exact 비교에서 불일치, 서명 0건이면 404 (클라이언트는 key 를 절대 보내지 않는다, H-05)"
    - "재발급 실패(네트워크·미배포 Lambda 400·404) 시 기존 저장 imageUrl 폴백 유지 — 현행 회색 fail-closed 그대로, 앱 크래시 0"
    - "신규 분석 doc 의 faultZoomComparisons[] 에 imageKey(S3 key)가 함께 저장된다 — 향후 재발급은 URL 파싱 없이 key exact 비교"
    - "asset 미지정/기존 asset 종류의 POST /playback-url 응답은 바이트 불변 (coachAudio/renderedCompare 선례 — 기존 test_playback_url_reference.py 무회귀)"
  artifacts:
    - path: "backend/shared/python/sunity_shared/s3keys.py"
      provides: "build_fault_zoom_key(uid, analysis_id, tier, key_base) 단일 출처 + parse_result_key_from_presigned_url(url) 순수 파서"
      exports: ["build_fault_zoom_key", "parse_result_key_from_presigned_url"]
    - path: "backend/functions/playback-url/app.py"
      provides: "_handle_fault_zoom — asset 'faultZoom' 배치 재서명 (done 게이트 + canonical exact 비교)"
      contains: "_handle_fault_zoom"
    - path: "backend/tests/test_playback_url_fault_zoom.py"
      provides: "재서명 가드 pytest (소급 URL 파싱·cross-uid 차단·advisory prefix·무회귀)"
    - path: "app/src/lib/faultZoomUrls.ts"
      provides: "zoomCardKey/buildFreshZoomUrlMap 순수 함수 + useFreshFaultZoomUrls 훅"
    - path: "app/src/lib/__tests__/faultZoomUrls.test.ts"
      provides: "순수 함수 node --test"
  key_links:
    - from: "backend/functions/pipeline/app.py"
      to: "backend/shared/python/sunity_shared/s3keys.py"
      via: "_fault_zoom_upload_items 가 build_fault_zoom_key 로 skey 구성 (인라인 f-string 제거 — 단일 출처)"
      pattern: "build_fault_zoom_key"
    - from: "backend/functions/playback-url/app.py"
      to: "backend/shared/python/sunity_shared/s3keys.py"
      via: "_handle_fault_zoom 이 같은 build_fault_zoom_key 로 canonical 구성 (저장측과 drift 0)"
      pattern: "build_fault_zoom_key"
    - from: "app/src/lib/api.ts"
      to: "POST /playback-url"
      via: "fetchFaultZoomUrls — body {analysisId, asset: 'faultZoom'}"
      pattern: "asset: 'faultZoom'"
    - from: "app/src/components/DeductionDetailSheet.tsx"
      to: "app/src/lib/faultZoomUrls.ts"
      via: "renderCrop: freshZoomUrls?.[zoomCardKey(zoom)] ?? zoom.imageUrl + Image onError → 재발급 트리거"
      pattern: "zoomCardKey"
---

<objective>
확대비교 신뢰 수리 (belle 08-24 "확대비교랑 모션 분석에 집중") — 분석 doc 의 faultZoomComparisons[].imageUrl 이 presigned 7일 TTL 로 죽어 비교 패널이 전부 회색이 되는 결함을 열람 시점 재발급 구조로 수리.

실측 원인 (착수 전 검증 완료):
- pipeline `_fault_zoom_upload_items`(app.py:3494-3501)가 `imageUrl = _signed_get(bucket, skey)` 로 **7일 서명 URL 을 doc 에 박제** (`_PLAYBACK_EXPIRES = 7*24*3600`, app.py:1275 부근). S3 key 는 doc 에 미저장.
- 영상 링크는 **이미 해결돼 있다** — result.tsx 의 freshMyUrl(:962)/freshPrevUrl(:943)/freshRefUrl 이 POST /playback-url 로 재발급 중. 죽는 것은 zoom PNG 만.
- 배경 제안의 "새 HTTP API POST /media-urls" 는 불요 — **기존 POST /playback-url 의 asset 확장 선례 3건**(correctedPose/rotation §31, coachAudio §12.7, renderedCompare §12.9)이 정확히 이 패턴(H-02: URL 비저장·열람 시점 재서명·server-selected key·exact 비교)이다. 4번째 asset 종류 'faultZoom' 추가가 정답.
- 소급(기존 doc): zoom S3 key = `results/{uid}/{analysisId}/{zoom_|zoom_adv_}{criterion or joint}.png` — **doc 의 tier/criterion/joint 필드만으로 canonical 재구성 가능**. 클라이언트 key 전송도, 앱측 URL 파싱도 불요. 저장 imageUrl 에서 key 를 **서버가** 파싱해 canonical 과 exact 비교(M2-01 stale-key 가드)만 한다. 백필 0.

Purpose: belle 이 08-24 직접 겪은 결함(7-30 분석 비교 패널 전부 회색) 제거 — 확대비교가 서비스의 중심 표면이 된 지금(일러스트 제거 직후) 7일 뒤 죽는 카드로는 신뢰 불가.
Output: 백엔드 asset 'faultZoom' 재서명 경로 + 앱 재발급 훅 + 게이트 GREEN. 배포(sam deploy)·OTA 는 범위 밖 — SUMMARY 에 커맨드만 명기.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kimtaesung/Dev/SunityMotion/CLAUDE.md
@/Users/kimtaesung/Dev/SunityMotion/backend/functions/playback-url/app.py
@/Users/kimtaesung/Dev/SunityMotion/backend/shared/python/sunity_shared/s3keys.py
@/Users/kimtaesung/Dev/SunityMotion/backend/tests/test_playback_url_reference.py
@/Users/kimtaesung/Dev/SunityMotion/app/src/lib/api.ts
@/Users/kimtaesung/Dev/SunityMotion/app/src/components/DeductionDetailSheet.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 백엔드 — key 단일 출처 + asset 'faultZoom' 재서명 + 신규 방출분 imageKey (계약 3중 동시)</name>
  <files>backend/shared/python/sunity_shared/s3keys.py, backend/shared/python/sunity_shared/models.py, backend/functions/pipeline/app.py, backend/functions/playback-url/app.py, backend/tests/test_playback_url_fault_zoom.py, docs/contract.md, app/src/types/analysis.ts</files>
  <action>
1. `s3keys.py` — 순수 함수 2개 추가 (boto3/네트워크 0, build_coach_audio_key 선례 docstring 규율 — "단일 출처" 명기):
   - `build_fault_zoom_key(uid: str, analysis_id: str, tier: str | None, key_base: str) -> str` — tier == "advisory" 면 `zoom_adv_`, 그 외(confirmed/None/legacy) `zoom_` prefix 로 `results/{uid}/{analysis_id}/{prefix}{key_base}.png` 반환. **기존 pipeline 인라인 규칙과 byte-동일해야 한다** (pipeline app.py:3461-3494 실측: confirmed→"zoom_", advisory→"zoom_adv_", key_base = criterion or joint).
   - `parse_result_key_from_presigned_url(url: str) -> str | None` — urllib.parse(urlparse+unquote)로 presigned URL path 에서 S3 key 추출. virtual-hosted(`{bucket}.s3.{region}...`/path=`/{key}`)와 path-style(netloc 이 `s3.`/`s3-` 시작/path=`/{bucket}/{key}`) 둘 다 처리. 비-str·빈 path·파싱 실패는 None. 파서는 관대해도 안전 — 서명 게이트는 caller 의 canonical exact 비교가 전담 (M2-01).
2. `models.py` — `PLAYBACK_ASSET_FAULT_ZOOM = "faultZoom"` 상수 추가 (PLAYBACK_ASSET_RENDERED_COMPARE :381 인접, 동일 주석 양식).
3. pipeline `functions/pipeline/app.py` — `_fault_zoom_upload_items`(:3473)의 인라인 `skey = f"results/{uid}/{analysis_id}/{key_prefix}{key_base}.png"` 를 `build_fault_zoom_key(uid, analysis_id, tier, key_base)` 호출로 교체하고 `key_prefix` 파라미터 제거 (호출측은 grep `_fault_zoom_upload_items(` 로 전수 확인 — 실측 2곳: `_render_fault_zoom` 말미 :3461-3468 루프와 `_run_gated_card_inherit`(quick-260811-kpo 공용화) — 둘 다 시그니처 갱신). item dict 에 `"imageKey": skey` 방출 추가 (scalar str — `_validate_dict_only_scalars` flat 제약 통과, update_analysis_fault_zoom 은 키 화이트리스트가 없어 validator 무수정). 교체 후 게이트: `grep -rn "zoom_adv_" backend/functions backend/shared --include="*.py" | grep -v "^.*#" | grep -v s3keys` 로 prefix 리터럴이 s3keys 밖에 남지 않았는지 확인 (주석 제외).
4. `functions/playback-url/app.py` — `_handle_fault_zoom(uid: str, analysis_id: str) -> dict` 신설 (`_handle_coach_audio`/`_handle_rendered_compare` 규율 복제):
   - doc = firestore_admin.get_analysis → result dict 방어 파싱 → `result.get("faultZoomStatus") == models.FAULT_ZOOM_STATUS_DONE` 아니면 404 not_found "확대 비교 이미지를 찾을 수 없어요." (가드 위반 전부 동일 404 — leak 0 선례).
   - `result.get("faultZoomComparisons")` list 순회, item 별: dict 아님/joint 비-str-빈값 → skip. key_base = criterion(비어있지 않은 str 일 때) or joint. canonical = build_fault_zoom_key(uid, analysis_id, item.get("tier"), key_base). stored = item.get("imageKey")(비어있지 않은 str) 우선, 부재 시 parse_result_key_from_presigned_url(item.get("imageUrl")) — **소급 경로**. `stored == canonical` exact 일 때만 `_sign_get(canonical, expires=_ASSET_EXPIRES, content_type=_ASSET_CONTENT_TYPE["png"])`. 서명 성공 item 만 응답에 `{"joint", "playbackUrl"}` + tier/criterion 이 str 이면 echo (앱 join 키 재료 — 서버 echo 필드와 앱 zoomCardKey 입력이 동일해야 함).
   - 서명 0건이면 404, 1건 이상이면 `responses.ok({"items": out, "expiresInSec": _ASSET_EXPIRES})`.
   - lambda_handler asset 분기: coachAudio/renderedCompare 분기 뒤·VISUAL_JOB_KINDS 검사 앞에 `if asset == models.PLAYBACK_ASSET_FAULT_ZOOM: return _handle_fault_zoom(uid, analysis_id)` — 기존 asset 종류 응답 바이트 불변 (선례 주석 양식 유지).
5. 계약 3중 동시 (CLAUDE.md 규율):
   - `docs/contract.md` — renderedCompare 절(:148 부근) 뒤에 `#### POST /playback-url — asset: 'faultZoom' 확장` 절 신설: 요청/응답 형상, done+exact 이중 가드, 소급(URL 파싱) 경로, expiresInSec 3600. FaultZoomComparison 필드 절(§11 계열)에 `imageKey` optional 추가 — 부재=legacy doc(서버가 imageUrl 파싱 폴백).
   - `app/src/types/analysis.ts` — `FaultZoomComparison` 에 `imageKey?: string` 추가, lockstep 주석 (Python 방출부 + contract.md 인용 — refMatch 선례 양식). userAnalyses.ts normalize 는 `...c` spread 라 통과 확인만 (실측 :699 — 수정 불요).
6. `backend/tests/test_playback_url_fault_zoom.py` 신설 — test_playback_url_reference.py 의 fixture 패턴 그대로 (sys.path 동적 import + VIDEO_BUCKET env + _FakeS3 + verify_request/firestore_admin monkeypatch, 외부 호출 0). 케이스:
   (a) done doc + imageKey exact → 200, 서명 Key == canonical, ExpiresIn 3600, ResponseContentType image/png
   (b) 소급: imageKey 없음 + virtual-hosted presigned imageUrl → 파싱 key exact → 200
   (c) path-style URL 도 파싱 → 200
   (d) imageKey 가 canonical 과 불일치(stale) → 해당 item 제외, 전 item 불일치 → 404
   (e) faultZoomStatus pending/failed/부재 → 404
   (f) cross-uid: 토큰 uid-001 인데 doc item imageKey 가 uid-002 경로 → canonical 불일치 → 404 (타 사용자 키 재서명 차단)
   (g) advisory tier → zoom_adv_ canonical 로 서명 / criterion 카드 → criterion 키·legacy joint 카드 → joint 키
   (h) 순수 함수 직접: build_fault_zoom_key prefix 매핑 2종 + parse_result_key_from_presigned_url virtual-hosted/path-style/None 케이스
   (i) 무회귀 sanity: asset 미지정 analysisId 경로 200 (기존 응답 형상)
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && python3 -m pytest tests/test_playback_url_fault_zoom.py tests/test_playback_url_reference.py -q 2>&1 | tail -3</automated>
  </verify>
  <done>신규 pytest 전 케이스 PASS + 기존 test_playback_url_reference.py 무회귀. zoom prefix 리터럴 단일 출처 grep 확인. contract.md/analysis.ts/백엔드 방출부 3면 동시 갱신.</done>
</task>

<task type="auto">
  <name>Task 2: 앱 — fetchFaultZoomUrls + 재발급 훅 + 시트 배선 (fail-closed 폴백 유지)</name>
  <files>app/src/lib/api.ts, app/src/lib/faultZoomUrls.ts, app/src/lib/__tests__/faultZoomUrls.test.ts, app/src/app/analysis/result.tsx, app/src/components/DeductionDetailSheet.tsx</files>
  <action>
1. `api.ts` — `fetchFaultZoomUrls(analysisId: string)` 추가: POST /playback-url `{analysisId, asset: 'faultZoom'}` → `{items, expiresInSec}`. 스키마 방어는 fetchVisualAssetUrl 선례: items 가 array 아니면 malformed_response throw; item 은 joint/playbackUrl 이 비어있지 않은 str 인 것만 통과(불량 item 은 조용히 filter — 배치라 부분 성공 보존). export type `FaultZoomUrlItem = { joint: string; tier?: string; criterion?: string; playbackUrl: string }`.
2. `app/src/lib/faultZoomUrls.ts` 신설:
   - 순수 `zoomCardKey(z: { tier?: string | null; criterion?: string; joint: string }): string` — `(z.tier === 'advisory' ? 'adv:' : 'conf:') + (z.criterion || z.joint)`. 서버 canonical key 유일성 축(tier×key_base)과 동형 — doc item 과 서버 echo item 양쪽에 같은 함수를 적용해 join. 주석에 s3keys.build_fault_zoom_key lockstep 명기.
   - 순수 `buildFreshZoomUrlMap(items: FaultZoomUrlItem[]): Record<string, string>` — zoomCardKey → playbackUrl 맵.
   - 훅 `useFreshFaultZoomUrls(args: { analysisId: string; createdAt: number | undefined; comparisons: FaultZoomComparison[] | null | undefined })` → `{ freshZoomUrls: Record<string, string>; onZoomImageError: () => void }`:
     · 자동 재발급 effect — comparisons 비어있지 않고 `Date.now() - (createdAt || 0) >= SAFE_TTL_MS`(6일 — result.tsx freshMyUrl :974 선례와 동일 마진·동일 근거 주석)일 때 1회 fetch, cancelled 플래그로 unmount 방어.
     · `onZoomImageError` — Image 로드 실패 시 나이 무관 재발급 (시계 오차·조기 만료 커버). ref 로 single-flight — mount 당 최대 1회 (무한 onError 루프 차단).
     · 실패(네트워크·미배포 백엔드의 400 bad_request 포함)는 맵 비움 유지 + `__DEV__` warn 만 — 저장 imageUrl 폴백 = 현행 회색 fail-closed 그대로. **백엔드 배포 전 앱이 먼저 나가도 회귀 0 인 순서 독립성이 이 폴백의 존재 이유다.**
3. `result.tsx` — freshMyUrl 블록(:962 부근) 인접에서 훅 호출 (createdAt·result.faultZoomComparisons 재료 이미 스코프에 있음). DeductionDetailSheet 렌더 지점에 `freshZoomUrls`/`onZoomImageError` props 전달.
4. `DeductionDetailSheet.tsx` — Props 에 optional `freshZoomUrls?: Record<string, string>`, `onZoomImageError?: () => void` 추가. `renderCrop`(:144)의 Image 를 `source={{ uri: freshZoomUrls?.[zoomCardKey(zoom)] ?? zoom.imageUrl }}` + `onError={onZoomImageError}` 로. props 부재 = 종전 동작 (하위호환). 배선 전 렌더 지점 전수 확인: `grep -rn "imageUrl" app/src --include="*.tsx"` (실측 1곳 :146 — 추가 발견 시 같은 맵 조회 배선). 주의: `deductionSheet.ts:838` 의 imageUrl 동일성 비교는 doc 저장값끼리라 무접촉 — 훅 맵은 렌더 경계에서만 조회하고 doc item 을 변형하지 않는다.
5. `app/src/lib/__tests__/faultZoomUrls.test.ts` 신설 (node --test, 순수 함수만 — 훅은 RN 런타임이라 제외): zoomCardKey advisory/confirmed prefix·criterion 우선·joint 폴백·tier null 처리, buildFreshZoomUrlMap 매핑·불량 item 무시.
  </action>
  <verify>
    <automated>npm --prefix /Users/kimtaesung/Dev/SunityMotion/app run typecheck && cd /Users/kimtaesung/Dev/SunityMotion/app && node --test src/lib/__tests__/faultZoomUrls.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>typecheck GREEN + 신규 node 테스트 PASS. 시트 이미지가 fresh 맵 우선·저장 URL 폴백으로 렌더. onError 재발급 single-flight. doc item 무변형(맵은 렌더 경계 전용).</done>
</task>

<task type="auto">
  <name>Task 3: 전량 게이트 — pytest 대상 모듈 + typecheck + node --test 실패 0</name>
  <files>(변경 없음 — 검증 전용)</files>
  <action>
1. 백엔드 대상 모듈 pytest: 신규 파일 + 무회귀 대상 — `tests/test_playback_url_fault_zoom.py tests/test_playback_url_reference.py tests/test_fault_zoom_deferred.py`. 추가로 `grep -rln "_fault_zoom_upload_items\|build_fault_zoom_key" backend/tests` 로 pipeline 매퍼를 참조하는 테스트를 찾아 함께 실행 (시그니처 변경 여파 전수 — 전체 스위트는 기지 실패 기준선이 있어 대상 모듈만).
2. 앱: `npm run typecheck` + `node --test src/lib/__tests__/*.test.ts src/lib/__tests__/*.test.mjs` 전량 — 기준선 201 pass 대비 실패 0 (신규 테스트만큼 증가).
3. SUMMARY 에 배포 준비 절 명기 (실행은 범위 밖 — belle/오케스트레이터 확인 후):
   - 백엔드: `cd backend && sam build --use-container && sam deploy` (Mac 네이티브 함정 — --use-container 필수. 템플릿 변경 0, 함수 코드 + SharedLayer 만)
   - 앱: OTA 발행은 belle 결정 대기 (verify-ui-on-simulator-before-ota — 시뮬 실증은 오케스트레이터 후속)
   - 순서 독립: 앱 먼저 나가도 400 → 폴백, 백엔드 먼저 나가도 구 앱 무영향 (asset 미지정 바이트 불변)
   - 소급 실증 항목: 배포 후 belle 의 7-30 분석(비교 패널 회색이던 doc)을 열어 이미지 표시 확인
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && python3 -m pytest tests/test_playback_url_fault_zoom.py tests/test_playback_url_reference.py tests/test_fault_zoom_deferred.py -q 2>&1 | tail -3 && npm --prefix /Users/kimtaesung/Dev/SunityMotion/app run typecheck && cd /Users/kimtaesung/Dev/SunityMotion/app && node --test src/lib/__tests__/*.test.ts src/lib/__tests__/*.test.mjs 2>&1 | tail -6</automated>
  </verify>
  <done>대상 모듈 pytest 실패 0 + typecheck GREEN + node --test 전량 실패 0 (기준선 201 + 신규분). SUMMARY 에 배포 커맨드·순서 독립·소급 실증 항목 명기.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 앱 → POST /playback-url | 미신뢰 입력 (analysisId, asset) — Firebase 토큰 필수 |
| doc 저장값 → 서명기 | doc 의 imageKey/imageUrl 은 과거 파이프라인 산출 — stale/오염 가능 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-q6p-01 | E (EoP) | _handle_fault_zoom | mitigate | 클라이언트 key 미수용 (asset 종류만) — canonical 은 토큰 uid 로 서버 구성, 타 uid 키는 exact 불일치로 서명 0 (H-05/M2-01 선례) |
| T-q6p-02 | I (Info disclosure) | 가드 실패 응답 | mitigate | 전부 동일 404 — status/파싱/불일치 단계 비구분 (leak 0 선례) |
| T-q6p-03 | T (Tampering) | 저장 imageUrl 파싱 경로 | mitigate | 파서 출력을 신뢰하지 않음 — canonical exact 비교 통과분만 서명 (파서는 후보 추출 전용) |
| T-q6p-04 | D (DoS) | 배치 서명 | accept | item 수 = doc 의 카드 수(한 자릿수)·본인 doc 한정 — 파일럿 트래픽에서 무시 가능 |
| T-q6p-SC | Tampering | 신규 패키지 설치 | accept | 신규 의존성 0 — stdlib urllib.parse 만 추가 사용 |
</threat_model>

<verification>
- 신규 pytest: done+exact 서명 / 소급 URL 파싱 / cross-uid 차단 / advisory prefix / status 게이트 / 기존 경로 무회귀 전부 PASS
- prefix 리터럴 단일 출처: zoom_adv_ 가 s3keys 밖 코드(주석 제외)에 0
- typecheck + node --test 전량 실패 0 (기준선 201 pass 유지 + 신규분)
- 계약 3면(contract.md / analysis.ts / 백엔드 방출·재서명) 동시 갱신 — drift 0
</verification>

<success_criteria>
- 7일 지난 doc 열람 시 확대비교 이미지 재발급 경로 성립 (기존 doc 소급 — 백필 0, 클라이언트 key 전송 0)
- 재발급 실패 시 현행 회색 폴백 그대로 (fail-closed, 배포 순서 독립)
- 신규 방출분 imageKey 저장 — 이후 재발급은 key exact 비교
- 배포·OTA 는 미실행 — SUMMARY 에 커맨드·소급 실증 항목(belle 7-30 doc) 명기
</success_criteria>

<output>
Create `.planning/quick/260824-q6p-7/260824-q6p-SUMMARY.md` when done
</output>
