---
phase: quick-260903-f2w
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/DeductionCard.tsx
  - app/src/app/analysis/result.tsx
  - app/src/components/DeductionDetailSheet.tsx
  - app/src/lib/faultZoomUrls.ts
  - app/src/lib/__tests__/faultZoomUrls.test.ts
autonomous: true
requirements: [quick-260903-f2w]
must_haves:
  truths:
    - "topFix(오늘 고칠 것) 카드에 확정 확대비교 합성 PNG([내 영상|정은지])가 인라인으로 보인다 — 링크를 누르지 않아도"
    - "faultZoomStatus='pending' 동안은 합성 이미지와 같은 모양의 placeholder 1개(스피너+캡션)가 보이고, done 도착 시 onSnapshot 으로 이미지로 바뀐다 (유령 스피너 쌍 소멸)"
    - "'다른 감점 항목' 접힌 행마다 확대 사진 유무가 보인다 (사진 있는 행에 '확대 사진' 표시)"
    - "7일 넘은 doc: 카드 인라인 이미지도 fresh 재발급 맵을 우선 쓰고, 로드 실패 시 onZoomImageError 로 재발급을 트리거한다 (시트와 동일 규칙, 규칙 사본 0)"
    - "advisory tier 카드는 종전대로 감점 카드에 붙지 않는다 (matchZoomForDeductionRecord 무접촉)"
    - "typecheck 0 오류, node --test 기준선 212 → 신규 테스트 포함 전부 pass"
  artifacts:
    - path: "app/src/components/DeductionCard.tsx"
      provides: "합성 PNG 1장 모델 zoom prop + hasZoom 접힘 표시 + 재발급 onError"
      contains: "ZOOM_COMPOSITE_ASPECT"
    - path: "app/src/lib/faultZoomUrls.ts"
      provides: "resolveZoomImageUrl(zoom, freshMap) — fresh 우선·저장 imageUrl 폴백 단일 출처"
      contains: "export function resolveZoomImageUrl"
    - path: "app/src/app/analysis/result.tsx"
      provides: "topFix 카드 zoom 배선 + 접힌 행 hasZoom 배선"
      contains: "resolveZoomImageUrl"
  key_links:
    - from: "app/src/app/analysis/result.tsx (topFix DeductionCard)"
      to: "result.faultZoomComparisons"
      via: "matchZoomForRecord(topFixRecord) → resolveZoomImageUrl(zoom, freshZoomUrls)"
      pattern: "zoom={"
    - from: "app/src/components/DeductionCard.tsx"
      to: "backend fault_zoom._compose"
      via: "합성 PNG 종횡비 (_OUT*2+gap) x _OUT = 726x360 lockstep 상수"
      pattern: "726"
---

<objective>
확대비교 앱 배선 완성 — topFix 카드에 합성 PNG 인라인 + 다른 감점 항목 행마다 사진 유무 표시.

belle 09-02: "왜 확대비교 사진이 하나밖에 없어" / 09-01: "확대비교 하는 칸 자체가 없어졌다".
검토 표 = `.planning/research/report-restructure-review-2026-09-02.md` 표 3 (확대비교 문제 5건) 중
**앱 쪽 결함 2건**만 이 작업이 닫는다:

1. (표 3 행 4, 결함) topFix 카드의 유령 스피너 — `DeductionCard` 가 `zoomPair {userUri, refUri}` (사진 2장 분리 URI) 를
   기다리는데 백엔드 `fault_zoom._compose` 는 **[내|정은지] 합성 PNG 1장**(`imageUrl`, 726x360) 만 낸다. 모양이 안 맞아
   히스토리 전체에서 한 번도 배선된 적 없음 → pending 동안 스피너 쌍만 떴다 사라지고 done 이 돼도 이미지로 못 바뀜.
   사진은 "확대 비교 자세히 보기 ›" 를 눌러 시트(`DeductionDetailSheet`)에서만 보였다.
2. (표 3 행 1, 구조) pdshape 60점: 감점 5건 중 확정 사진 2장인데 화면은 top-1 만 노출, 두 번째(왼엉덩이)는
   "다른 감점 항목 → 카드 → 확대 비교" 3탭 뒤. 접힌 행에 사진 유무 표시가 없어 "사진이 하나뿐"으로 체감.

**손대지 않는 것 (belle 판정 대기·별건):** advisory 카드 노출 여부(표 3 행 3), 클라임 eye 위양성(행 2), 사후 스테이지
순서(행 5), 결과 화면 순서·문구 재배치(표 1·2 — belle ○× 전 금지), `matchZoomForDeductionRecord` 규칙, 백엔드 전부.
</objective>

<context>
읽을 것 (순서대로):
- `.planning/research/report-restructure-review-2026-09-02.md` 표 3
- `app/src/components/DeductionCard.tsx` 전체 (415줄) — zoomPair/zoomPending 렌더부 (~150-230줄), 접힘 모드 (~100-120줄)
- `app/src/app/analysis/result.tsx` 1981-1997 (topFixRecord/toCardRecord), 1999-2007 (matchZoomForRecord),
  1067-1076 (useFreshFaultZoomUrls → freshZoomUrls/onZoomImageError), 2612-2646 (topFix DeductionCard + 링크),
  3085-3110 (다른 감점 항목 접힌 행), 3645-3665 (DeductionDetailSheet 배선)
- `app/src/components/DeductionDetailSheet.tsx` 143-172 (imgH = imgW/2, renderCrop: fresh 맵 우선 + onError 재발급 + halfLabel 좌/우)
- `app/src/lib/faultZoomUrls.ts` (zoomCardKey / useFreshFaultZoomUrls) + `__tests__/faultZoomUrls.test.ts` (node:test 관례)
- `backend/shared/python/sunity_shared/analysis/fault_zoom.py` 2314-2324 `_compose`: `_OUT=360`, `gap=6` → 캔버스 (726, 360)

규율:
- 토큰만 (colors/spacing/radius/typography) — 하드코딩 색·간격 금지. 이모지 0. 라이트 전용.
- 주석은 프로젝트 관례(한국어, 출처 인용: `quick-260903-f2w`, `fault_zoom._compose`, 표 3 행 번호).
- 계약(`app/src/types/analysis.ts`) 무접촉 — doc 형상은 그대로, 앱 표시 계층만.
- 앱 문구는 OTA 불가(네이티브 모듈) → 빌드는 이 작업 범위 밖 (버전 범프 금지 — belle 확정분 모아 1.2.4 한 빌드).
</context>

<tasks>

<task id="1" name="faultZoomUrls: resolveZoomImageUrl 단일 출처 + 테스트">
files: app/src/lib/faultZoomUrls.ts, app/src/lib/__tests__/faultZoomUrls.test.ts, app/src/components/DeductionDetailSheet.tsx
action:
- `faultZoomUrls.ts` 에 순수 함수 추가:
  `export function resolveZoomImageUrl(zoom: { tier?: string | null; criterion?: string; joint: string; imageUrl: string }, freshMap: Record<string, string> | null | undefined): string`
  → `freshMap?.[zoomCardKey(zoom)] ?? zoom.imageUrl`. 빈 문자열 fresh 값은 무시(저장값 폴백). 주석: 시트(`renderCrop`)와 카드 인라인이 같은 규칙을 쓰기 위한 단일 출처 (quick-260824-q6p 규칙 승계, 사본 0).
- `DeductionDetailSheet.tsx` `renderCrop` 의 `freshZoomUrls?.[zoomCardKey(zoom)] ?? zoom.imageUrl` 을 `resolveZoomImageUrl(zoom, freshZoomUrls)` 로 교체 (동작 동일). `zoomCardKey` import 가 더 이상 안 쓰이면 제거.
- 테스트 3건 추가 (기존 파일, node:test 관례): fresh 맵 우선 / 맵 부재·미등재 키 = 저장 imageUrl / advisory 와 confirmed 가 같은 joint 여도 키가 갈려 서로의 fresh URL 을 안 가져감.
verify: `cd app && node --test "src/lib/__tests__/*.test.*"` pass 215 이상 fail 0
done: resolveZoomImageUrl export + 시트가 그것을 소비 + 테스트 green
</task>

<task id="2" name="DeductionCard: 합성 PNG 1장 모델 + 접힘 행 사진 표시">
files: app/src/components/DeductionCard.tsx
action:
- `DeductionCardZoomPair {userUri, refUri}` 를 **삭제**하고 `export interface DeductionCardZoom { imageUrl: string }` 로 교체. props: `zoomPair?` → `zoom?: DeductionCardZoom`, 신규 `onZoomImageError?: () => void`, 신규 `hasZoom?: boolean`.
  (zoomPair 는 리포 어디서도 넘겨진 적 없음 — grep 으로 확인 후 제거. 모듈 헤더 주석의 "결함 확대쌍(내 vs 정은지 crop)" 서술을 "합성 PNG 1장([내|정은지], 백엔드 fault_zoom._compose)" 로 정정.)
- 상수 `const ZOOM_COMPOSITE_ASPECT = (360 * 2 + 6) / 360;` — 주석에 lockstep 출처(`fault_zoom._compose`: `_OUT=360`, `gap=6`, 캔버스 726x360). 시트는 `imgW/2` 근사를 쓰지만 카드는 정확 비율(레터박스 0).
- 펼침 렌더 3) 블록: `zoom` 있으면 컨테이너(`width:'100%'`, `aspectRatio: ZOOM_COMPOSITE_ASPECT`, `borderRadius: radius.listItem`, `overflow:'hidden'`, `backgroundColor: colors.softBg`) 안에 `Image` 1장 `resizeMode="contain"` + 로딩 skeleton 오버레이(기존 zoomSkeleton 재사용) + onError 시 실패 캡션(IMG_FAILED_CAPTION) 렌더 **및** `onZoomImageError?.()` 호출(재발급 트리거 — 시트와 동일). 좌상단 태그 "내 영상", 우상단 태그 `rightLabel` (기존 zoomTag 스타일 + right 변형). accessibilityLabel "내 영상과 {rightLabel} 확대 비교 이미지".
- 이미지 상태 훅은 1쌍(loading/failed)으로 줄이고, `zoom?.imageUrl` 변경 시 `useEffect` 로 리셋 (재발급으로 URL 이 바뀌면 실패 캡션이 남지 않게).
- `zoomPending` placeholder: 스피너 2개 정사각 → **같은 종횡비 컨테이너 1개** + ActivityIndicator + `ZOOM_PENDING_CAPTION` 텍스트(시트 pending 블록 미러). accessibilityRole progressbar 유지.
- 접힘 모드(`expanded === false`): `hasZoom` 이면 헤드라인과 chevron 사이에 pill 1개 — `Ionicons name="image-outline" size={14}` + 텍스트 "확대 사진" (`typography.caption`, `colors.textSecondary`, 배경 `colors.softBg`, `radius.listItem`). `numberOfLines` 헤드라인은 그대로 flex:1. accessibilityLabel 에 " — 확대 사진 있음" 접미 추가.
- 스타일은 토큰만. 기존 `zoomRow/zoomHalf` 중 안 쓰게 된 스타일은 제거(죽은 스타일 0).
verify: `cd app && npx tsc --noEmit` 0 오류 (result.tsx 미배선 상태에서도 prop 이 optional 이라 통과해야 함)
done: 카드가 `zoom.imageUrl` 1장을 정확 비율로 그리고, pending 이 1개 placeholder, 접힘 행이 hasZoom 표시
</task>

<task id="3" name="result.tsx: topFix 카드 zoom 배선 + 접힌 행 hasZoom 배선">
files: app/src/app/analysis/result.tsx
action:
- import 에 `resolveZoomImageUrl` (from '../../lib/faultZoomUrls' — 기존 `useFreshFaultZoomUrls` import 줄에 합침).
- `topFixRecord` 직후(1981 부근)에 `const topFixZoom = topFixRecord ? matchZoomForRecord(topFixRecord) : null;` 는 **불가** — `matchZoomForRecord` 가 2002 에서 정의되므로 그 아래(2008 부근)에 둔다. useMemo 불필요(다른 호출부와 동일하게 렌더마다 계산 — 비용 미미).
- topFix `<DeductionCard>` (2617 부근): `zoom={topFixZoom ? { imageUrl: resolveZoomImageUrl(topFixZoom, freshZoomUrls) } : undefined}` + `onZoomImageError={onZoomImageError}` 추가. `zoomPending={zoomPending}` 유지(카드가 zoom 우선). 주석: 표 3 행 4 결함 — 히스토리 전체 무배선이던 인라인 사진을 여기서 처음 연결 (quick-260903-f2w).
- 링크 `matchZoomForRecord(topFixRecord) || zoomPending` 조건은 `topFixZoom || zoomPending` 으로 치환(동일 값, 중복 호출 제거). 링크 문구·시트 진입은 **그대로** (시트는 실초·수치·블록별 크롭을 더 준다 — 문구 변경은 표 1 belle ○× 영역).
- '다른 감점 항목' 접힌 행 `<DeductionCard expanded={false}>` (3095 부근): `hasZoom={matchZoomForRecord(rec) != null}` 추가. 주석: 표 3 행 1 — "5개 틀렸는데 사진 1장" 의 출구, 사진 있는 행을 행에서 보이게.
- 그 외 무접촉: recordMaps/cueWindows/시트 배선/estimatedArea 진입점/문구.
verify: `cd app && npx tsc --noEmit` 0 오류; `node --test "src/lib/__tests__/*.test.*"` fail 0; `grep -n "zoomPair" app/src/app/analysis/result.tsx app/src/components/DeductionCard.tsx` 는 result.tsx 의 recordMaps 주석/필드(resultSections 타입)만 남고 DeductionCard 에는 0
done: topFix 카드가 확정 사진을 인라인, 접힌 행이 사진 유무를 표시, 게이트 전부 green
</task>

</tasks>

<verification>
- `cd app && npx tsc --noEmit` → 0 오류
- `cd app && node --test "src/lib/__tests__/*.test.*"` → 기준선 212 + 신규 3 = 215 pass, fail 0
- `grep -rn "DeductionCardZoomPair\|userUri\|refUri" app/src` → 0건
- 시뮬레이터 실물 확인은 오케스트레이터 후속(개발 빌드 셸 + Metro, 확대 카드가 있는 doc 필요) — 실행자는 코드 게이트까지.
</verification>

<success_criteria>
- topFix 카드 인라인 합성 PNG 배선 (유령 스피너 소멸), 접힌 행 사진 유무 표시, fresh 재발급 규칙 단일 출처화.
- 계약·백엔드·매칭 규칙·문구 무접촉. 버전 범프 없음.
- 커밋 3개(태스크당 1개, 코드만) — docs 커밋은 오케스트레이터.
</success_criteria>
