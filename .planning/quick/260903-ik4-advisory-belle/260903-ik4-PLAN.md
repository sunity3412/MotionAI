---
phase: quick-260903-ik4
plan: 01
type: execute
wave: 1
depends_on: [quick-260903-ftg]
files_modified:
  - app/src/lib/resultSections.ts
  - app/src/lib/__tests__/resultSections.test.ts
  - app/src/app/analysis/result.tsx
autonomous: true
requirements: [quick-260903-ik4]
must_haves:
  truths:
    - "doc 에 tier='advisory' 확대 카드가 있으면 결과 화면에 '참고 부위' 섹션이 뜨고, 카드마다 합성 PNG 가 인라인으로 보인다 — 저신뢰·일반 두 경로 모두"
    - "카드에는 승인 문구만 쓴다: 칩 ADVISORY_CHIP_KO('참고 — 감점은 아니지만 회전·힘에 영향'), 안내 ADVISORY_NOTE_KO. 감점 수치·statusLine 없음. 색은 중립(빨강·주황 아님 — quick-260831-lcc 빨강 규율, PartChipsRow 선례)"
    - "advisory 카드가 0장이면 섹션 미렌더(diff 0). 확정 카드 표면(topFix·예상 부위·접힌 행·시트)은 무접촉"
    - "매칭 규칙(matchZoomForDeductionRecord 의 advisory 제외) 무접촉 — 이 섹션은 record 와 조인하지 않고 카드 자체를 보여준다"
    - "typecheck 0, node --test 기준선 219 → 신규 포함 전부 pass"
  artifacts:
    - path: "app/src/lib/resultSections.ts"
      provides: "selectAdvisoryZooms(comparisons) — tier==='advisory' 만, doc 순서 보존, 불량 항목 제외"
      contains: "export function selectAdvisoryZooms"
    - path: "app/src/app/analysis/result.tsx"
      provides: "'참고 부위' 섹션 — '다른 감점 항목' 블록 뒤, 기준 모션 메타 카드 앞"
      contains: "selectAdvisoryZooms"
  key_links:
    - from: "app/src/app/analysis/result.tsx (참고 부위 섹션)"
      to: "result.faultZoomComparisons[tier=advisory]"
      via: "selectAdvisoryZooms → ZoomCompositeImage(resolveZoomImageUrl, onZoomImageError)"
      pattern: "ZoomCompositeImage"
---

<objective>
참고(advisory) 확대비교 카드를 앱에 노출 — "감점 아님" 라벨로 사진 인라인. **belle ○× 대기 항목**(검토 표 3 행 3):
belle 09-03 "1번부터 정확하고 신중하게" → 실제 앱 화면 스크린샷으로 ○× 를 받기 위해 구현한다. × 면 이 작업 커밋을 revert.

배경(코드로 확인): 백엔드 `fault_zoom.select_advisory_joints` — 확정 결함이 아닌 관절 중 |편차| > tol 인 것을 최대 2개
골라 "참고" 카드를 만든다(표시 전용, 채점 무관). belle 계정 최근 6동작에 참고 카드 8장이 만들어졌지만 앱은 어디에도
사진을 보여주지 않는다(부위 칩 행이 이름만 보여줌). 클라임 92점은 확정 0장·참고 2장이라 이 섹션이 유일한 사진이 된다.
</objective>

<context>
읽을 것:
- `app/src/lib/deductionSheet.ts` 60-85: `ADVISORY_NOTE_KO`, `ADVISORY_CHIP_KO` (승인 원문 — 사본 금지, import)
- `app/src/components/PartChipsRow.tsx` 헤더 주석 + 스타일: advisory 는 **중립색**(quick-260831-lcc 빨강 규율로 advisoryOrange 제거). 이 섹션도 중립: 카드 배경 colors.cardBg + divider 테두리(styles.card), 칩은 softBg/textSecondary
- `app/src/lib/deductionLabels.ts` 28-60: `JOINT_LABEL_KO`(keypoint 키 공간 포함), `REGION_LABEL_KO`
- `app/src/components/ZoomCompositeImage.tsx` (quick-260903-ftg) — props imageUrl/rightLabel/onError/accessibilityLabel
- `app/src/lib/faultZoomUrls.ts` — `resolveZoomImageUrl`, `zoomCardKey`
- `app/src/app/analysis/result.tsx`: 3060-3130 (IN-01 예상 부위 카드 — 렌더 관례), 3130-3213 ('다른 감점 항목' 블록), **3215 `{/* mode1 전용: 기준 모션 메타 카드` 직전이 삽입 지점**, `isVisible(` 사용부와 `resultSections.ts` 의 섹션 키 정의(있으면 관례를 따르고, 섹션 키 체계가 이 화면 순서를 소유하면 'advisory' 키를 추가 — 없으면 게이트 없이 렌더)
- `.planning/quick/260903-ftg-in-01-zoomcompositeimage/260903-ftg-SUMMARY.md`

규율: 토큰만, 이모지 0, 한국어 주석에 출처(quick-260903-ik4, quick-260704-fz4, quick-260831-lcc). 계약·백엔드·매칭 규칙 무접촉. 버전 범프 금지.
</context>

<tasks>

<task id="1" name="selectAdvisoryZooms 순수 함수 + 테스트">
files: app/src/lib/resultSections.ts, app/src/lib/__tests__/resultSections.test.ts
action:
- `export function selectAdvisoryZooms<Z extends { tier?: string | null; imageUrl?: string; joint?: string }>(comparisons: readonly Z[] | null | undefined): Z[]` — tier === 'advisory' 만, doc 순서 보존, imageUrl 이 비어 있거나 joint 없는 항목 제외. null/undefined → []. 주석: quick-260903-ik4, 확정 카드 매칭(matchZoomForDeductionRecord)이 advisory 를 제외하는 것과 짝 — 이쪽은 record 조인 없이 카드 자체.
- 테스트 3건: advisory 만 남고 순서 보존 / confirmed·legacy(tier 부재) 제외 / 빈 imageUrl·null 입력 → [].
verify: `cd app && node --test "src/lib/__tests__/*.test.*"` 222 pass 이상 fail 0
done: export + 테스트 green
</task>

<task id="2" name="result.tsx '참고 부위' 섹션">
files: app/src/app/analysis/result.tsx
action:
- import: `selectAdvisoryZooms`(resultSections 기존 import 줄), `ADVISORY_CHIP_KO`, `ADVISORY_NOTE_KO`(deductionSheet — 이미 import 중이면 합침), `JOINT_LABEL_KO`, `REGION_LABEL_KO`(deductionLabels 기존 import 줄).
- 상수: `const ADVISORY_SECTION_TITLE = '참고 부위';` (ADVISORY_NOTE_KO 첫 어절과 같은 말). 주석: 승인 문구 2종은 deductionSheet 단일 소스.
- `const advisoryZooms = useMemo(() => selectAdvisoryZooms(result.faultZoomComparisons), [result.faultZoomComparisons]);`
- 3215 직전에 섹션: `advisoryZooms.length > 0 ? (<> <Text style={styles.sectionTitle}>{ADVISORY_SECTION_TITLE}</Text> {advisoryZooms.map(z => <View key={zoomCardKey(z)} style={[styles.card, styles.advisoryCard]}> 칩(ADVISORY_CHIP_KO, 중립 pill) + 부위 1줄(`z.region ? REGION_LABEL_KO[z.region] : JOINT_LABEL_KO[z.joint]` 폴백 z.joint, typography.bodyMdBold) + <ZoomCompositeImage imageUrl={resolveZoomImageUrl(z, freshZoomUrls)} rightLabel={cmp.mode === 'mode1' ? `${cmp.athleteName} 선수` : '지난 영상'} onError={onZoomImageError} accessibilityLabel={`${부위} 참고 확대 비교 이미지`} /> </View>)} <Text style={styles.advisoryNote}>{ADVISORY_NOTE_KO}</Text> </>) : null`.
  · 탭 없음(Pressable 아님) — 시트는 record 기반이라 advisory 는 열 대상이 없다.
  · 감점 수치·statusLine·초 표기 없음. 색 중립(softBg 칩, textSecondary). 빨강·주황 0.
  · `isVisible` 섹션 체계가 있으면 그 관례대로 키 추가(순서 = '다른 감점 항목' 뒤). 없으면 게이트 없이.
  · 주석: quick-260903-ik4 + belle ○× 대기 + 왜 여기(감점 표면 뒤, 참고는 마지막) + 클라임처럼 확정 0장일 때 유일한 사진.
- 스타일: `advisoryCard { gap: 10 }`, `advisoryChip`(softBg, radius.listItem, padding 3/8, alignSelf flex-start), `advisoryChipText`(typography.caption, textSecondary), `advisoryJoint`(typography.bodyMdBold, textPrimary), `advisoryNote`(typography.bodySm, textSecondary). 토큰만.
verify: `cd app && npx tsc --noEmit` 0; `node --test` fail 0; topFix·IN-01·다른 감점 항목 블록 diff 0
done: advisory 있는 doc 에서 섹션 렌더, 없는 doc diff 0
</task>

</tasks>

<verification>
- tsc 0 · node --test ≥222/0
- 시뮬(오케스트레이터): pdshape 복사본(참고 1장 왼쪽 어깨) → '예상 부위' 카드 2장 아래 '참고 부위' 섹션 1장. 클라임(참고 2장)은 belle 이 doc 복사 후.
</verification>

<success_criteria>
- 커밋 2개(코드만). 메시지에 "belle ○× 대기" 명시. docs 커밋은 오케스트레이터.
</success_criteria>
