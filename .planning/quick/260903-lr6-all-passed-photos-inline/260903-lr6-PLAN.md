---
phase: quick-260903-lr6
plan: 01
type: execute
wave: 1
depends_on: [quick-260903-ftg]
files_modified:
  - app/src/components/DeductionCard.tsx
  - app/src/app/analysis/result.tsx
autonomous: true
requirements: [quick-260903-lr6]
must_haves:
  truths:
    - "일반 경로에서 확정 확대 사진이 있는 감점 행은 접혀 있어도 사진이 바로 보인다(헤드라인 + 합성 PNG). 사진 없는 행은 종전 한 줄 그대로"
    - "topFix 카드 인라인 사진(f2w)·저신뢰 예상 부위 카드(ftg)·시트 무접촉. 사진 있는 행 탭 = 종전대로 시트"
    - "확정 카드 매칭 규칙·계약·백엔드 무접촉. typecheck 0, node --test 219/0 유지"
  artifacts:
    - path: "app/src/components/DeductionCard.tsx"
      provides: "접힘 모드에 zoom 이 오면 헤드라인 아래 ZoomCompositeImage 인라인 (pill 대신 사진)"
      contains: "collapsedZoom"
    - path: "app/src/app/analysis/result.tsx"
      provides: "'다른 감점 항목' 행에 zoom={resolveZoomImageUrl(...)} 전달"
      contains: "zoom={"
---

<objective>
통과한 확정 확대 사진은 동작별로 전부 인라인.

belle 09-03: "이래선 끝이 안나겠는데 동작별로 확장 사진에 들어갈 동작들 통과한거 다 넣고" — 사진마다 ○× 받는 절차를 끝내고,
게이트(정지·짝 프레임·기계 눈)를 통과한 확정 사진은 결과 화면에서 **탭 없이 전부** 보이게 한다. 현재: 일반 경로는 topFix
카드 1장만 인라인(f2w), 나머지 확정 사진은 '다른 감점 항목' 접힌 행의 "확대 사진" pill 뒤(탭 → 시트). 저신뢰 경로는 이미
전부 인라인(ftg). 이 작업은 일반 경로의 접힌 행에 사진을 펼친다.

예: 파워스핀 60점(확정 3장) → topFix(무릎 펴짐) 인라인 + 다른 감점 항목 2행(다리 벌림·왼어깨) 각각 사진 인라인.
</objective>

<context>
읽을 것: `DeductionCard.tsx`(접힘 모드 블록 — hasZoom pill, f2w), `ZoomCompositeImage.tsx`, `result.tsx` '다른 감점 항목' 블록(3130-3213 부근: `<DeductionCard expanded={false} hasZoom=…>`), `resolveZoomImageUrl`/`freshZoomUrls`/`onZoomImageError` 사용부(topFix 블록 선례).
규율: 토큰만, 이모지 0, 문구 신규 0(pill 문구 '확대 사진'은 사진이 대신하므로 사진 있는 행에서는 pill 미렌더). 버전 범프 금지.
</context>

<tasks>

<task id="1" name="DeductionCard 접힘 모드 사진 인라인">
files: app/src/components/DeductionCard.tsx
action:
- 접힘 모드(`expanded === false`)에서 `zoom` prop 이 있으면: 헤드라인 행(헤드라인 + chevron) 아래에 `<ZoomCompositeImage imageUrl={zoom.imageUrl} rightLabel={rightLabel} onError={onZoomImageError} accessibilityLabel={`내 영상과 ${rightLabel} 확대 비교 이미지`} />` 를 렌더. 이때 `hasZoom` pill 은 렌더하지 않는다(사진이 있으니 중복). `zoom` 없고 `hasZoom` 이면 종전 pill(하위호환).
- Pressable 전체가 탭 대상 그대로(시트 진입). 접힘 카드 스타일에 `gap: 10`(사진 간격) — styles.card 의 gap 12 면 그대로 써도 됨.
- 헤더 주석 1줄: quick-260903-lr6 belle "통과한거 다 넣고" — 접힌 행도 사진 인라인.
verify: `cd app && npx tsc --noEmit` 0
done: 접힘 행이 zoom 을 받으면 사진 인라인
</task>

<task id="2" name="result.tsx 다른 감점 항목 행에 zoom 전달">
files: app/src/app/analysis/result.tsx
action:
- '다른 감점 항목' `records.map` 안에서 `const rowZoom = matchZoomForRecord(rec);` → `<DeductionCard … expanded={false} hasZoom={rowZoom != null} zoom={rowZoom ? { imageUrl: resolveZoomImageUrl(rowZoom, freshZoomUrls) } : undefined} onZoomImageError={onZoomImageError} …>` (topFix 블록과 같은 규칙). 주석 1줄(quick-260903-lr6).
- topFix·IN-01·시트·다른 블록 무접촉.
verify: `cd app && npx tsc --noEmit` 0; `node --test "src/lib/__tests__/*.test.*"` 219/0
done: 일반 경로 확정 사진 전부 인라인
</task>

</tasks>

<verification>
- tsc 0 · node --test 219/0
- 시뮬(오케스트레이터): 파워스핀 60 복사본(확정 3) → topFix 1 + 접힌 행 2 사진 인라인. 클라임 복사본(확정 1) diff 0(행 없음).
</verification>
