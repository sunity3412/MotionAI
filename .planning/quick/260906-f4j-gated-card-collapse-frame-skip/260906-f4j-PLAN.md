---
phase: quick-260906-f4j
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/shared/python/sunity_shared/analysis/fault_zoom.py
  - backend/tests/test_gated_frame_skips_collapse.py
  - backend/functions/pipeline/app.py
autonomous: true
requirements: [quick-260906-f4j]
must_haves:
  truths:
    - "gated(멈춤 상속) 카드에서 앵커 프레임(멈춤 초 → _override_idx 산출 u9/r9)이 붕괴(pose_collapse 단일 출처)면, 표시 프레임이 9fps 격자 ±4(_MOMENT_ANCHOR_RADIUS_WIDE 재사용) 안에서 가장 가까운 성한 프레임으로 옮겨진다 — 학생·기준 각각 독립"
    - "성한 = 비붕괴 AND 그 카드 멤버 좌표로 그릴 수 있음(_member_pts valid 비어있지 않음). 이 판정은 fault_zoom 창 승급 블록이 쓰는 것과 같은 함수 한 개(_frame_usable)다"
    - "앵커가 붕괴가 아니면 프레임·좌표·산출이 byte-동일(09-06 실측 비붕괴 28패널 무변화). 붕괴인데 반경 안에 성한 프레임이 없으면 그대로(fail-open) — 사진 장수·어느 멈춤인지 불변"
    - "옮김 또는 옮기지 못함(stuck)은 로그 1행 `fault_zoom_gated_frame_shift analysis_id=… rid=… user=A->B(status) ref=C->D(status) display_anchor=…` 로 남는다"
    - "stage-1(app.py 3488)·advisory(3531) 호출부, 채점(deductionBreakdown/veto/게이트), Firestore 방출 필드, conf 임계, 어깨 각도 문법 무접촉. pytest 4655 passed/0 failed/20 skipped → 신규 포함 전부 pass, failed 0"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/fault_zoom.py"
      provides: "성한 프레임 판정 단일 출처 + 붕괴 앵커 이동 순수 함수"
      contains: "def nearest_usable_frame"
    - path: "backend/shared/python/sunity_shared/analysis/fault_zoom.py"
      provides: "창 승급과 gated 이동이 공유하는 판정"
      contains: "def _frame_usable"
    - path: "backend/functions/pipeline/app.py"
      provides: "gated 호출부 배선 — u9/r9 를 build_fault_zoom_comparisons 에 넘기기 전에 옮긴다"
      contains: "_fz.nearest_usable_frame("
    - path: "backend/tests/test_gated_frame_skips_collapse.py"
      provides: "실측 fixture 기반 양방향 테스트(옮긴다/그대로) + 배선 source 단언"
      min_lines: 80
  key_links:
    - from: "backend/functions/pipeline/app.py (_run_gated_card_inherit)"
      to: "fault_zoom.nearest_usable_frame"
      via: "u9/r9 산출 직후, build_fault_zoom_comparisons 호출 직전"
      pattern: "_fz\\.nearest_usable_frame\\("
    - from: "fault_zoom.nearest_usable_frame"
      to: "fault_zoom._frame_usable → is_collapsed_frame + _member_pts"
      via: "목적지 판정(성한 프레임)"
      pattern: "_frame_usable\\("
    - from: "fault_zoom 창 승급 블록(구 3194-3205행)"
      to: "fault_zoom._frame_usable"
      via: "any(_frame_usable(...) for _c in _u_syn) — 중복 공식 제거"
      pattern: "any\\(\\s*_frame_usable\\("
---

<objective>
멈춤 상속(gated) 카드 경로에 붕괴 프레임 배제를 붙인다 — 앵커(멈춤 초 프레임)가 붕괴한 순간이면
멈춤 초 주변 9fps 후보 ±4 안에서 가장 가까운 성한 프레임으로 표시 프레임을 옮긴다. 학생·기준 양측 독립.

**진단은 실측으로 확정됨 — 재조사 금지. 이 위에 구현한다.**

- 2026-09-06 실측(09-03 라이브 6문서 21장 = 42패널): 앵커 붕괴 패널 14(학생 5 · 정은지 9). conf<0.5 겹침 8 →
  신뢰도는 붕괴의 부분집합. 14개 **전부 gated 카드**(경로별 gated 12 / stage1 3 / advisory 6, stage1·advisory 붕괴 0).
- 구제 가능성(코드가 실제 고르는 9fps 격자): ±2 에 10/14, ±4 에 11/14, ±8 에 14/14. 최대 이동 0.78초, 대부분 0.11초.
- 반사실(학생 불건전 5패널 전수를 옮겨 기계 눈 재질문): 1장 불일치→통과, 4장 통과 유지, **악화 0**. 5/5 정중앙 판독이 제목 부위로 이동.
- 기전(코드로 확인): `app.py:5334` gated 호출부가 `dtw_match=None`, `criterion_units=[{..."at_frame_idx": None}]`,
  `user_frame_idx=u9`/`ref_frame_idx=r9` 로 프레임을 고정해 호출한다. `build_fault_zoom_comparisons` 의 후보 합성 블록은
  `if unit.at_frame_idx is not None and dtw_match is not None:` 일 때만 돌기 때문에 `_drop_collapsed` · `select_confident_frame` ·
  `_ring(±2→±4)` 승급이 **이 경로에서 구조적으로 도달 불가**. 08-10(ms2/e4v)에 이 목적으로 만든 붕괴 방어가 정작 필요한 카드에서 안 돈다.

**하는 일:** (1) `fault_zoom.py` 에 "성한 프레임" 판정을 창 승급 블록과 **공유하는 헬퍼 한 개**로 뽑고, 그 위에
`nearest_usable_frame` 순수 함수를 만든다. (2) gated 호출부에서 u9/r9 를 그 함수로 옮긴 뒤 넘긴다. 옮겼으면 로그 1행.
(3) 옮긴 측의 freeze-순간 좌표 payload(`display_anchor`/`align_bake`)는 정의상 무효이므로 떨군다(아래 Task 2 근거).

**하지 않는 일:** 감점·채점 무접촉(complete 이후 표시 렌더). 사진 장수·어느 멈춤인지 불변(바뀌는 것은 그 멈춤 안 몇 번째 프레임뿐).
stage-1(3488)·advisory(3531) 호출부 무접촉. 어깨 각도 문법(`_ARMPIT_T`, `ANGLE_BAKE_MAP["shoulder"]`) 무접촉. Firestore 방출 필드 무변경.
conf 임계 무변경. 새 임계·새 상수 0 — 반경은 `_MOMENT_ANCHOR_RADIUS_WIDE`(=4) 재사용, 판정은 `pose_collapse` 단일 출처.

Purpose: belle 09-05 지적("팔꿈치 카드가 다른 부위를 보여준다")의 직접 원인 하나를 구조적으로 닫는다 — 08-10 붕괴 방어가 실제로 필요한 경로에서 돌게 한다.
Output: `fault_zoom._frame_usable`, `fault_zoom.nearest_usable_frame`, gated 배선 + 로그, 테스트 파일 1개.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kimtaesung/Dev/SunityMotion/CLAUDE.md
@/Users/kimtaesung/Dev/SunityMotion/.planning/STATE.md

읽을 것(한 번씩, 범위만):
- `backend/shared/python/sunity_shared/analysis/fault_zoom.py`
  - 372-416 `_to_rep_idx` / `is_collapsed_frame` / `_drop_collapsed` (판정 단일 출처, 변환 공식 단일 출처)
  - 418-472 `select_confident_frame` (rep_fps/rep_frames 를 report 메타에서 읽는 관례, tie-break = 오름차순 첫 인덱스 429-432행)
  - 540-567 `_MOMENT_ANCHOR_RADIUS` / `_MOMENT_ANCHOR_RADIUS_WIDE` 주석(ms2 "항상 넓히면 악화" 교훈)
  - 1308-1332 `_member_pts` (valid = finite AND (conf 부재 | conf ≥ _KP_CONF_MIN))
  - 3011-3016, 3066-3069 override 경로(u_idx/r_idx 클램프 공식 `max(0, min(int(idx), max(0, n-1)))`)
  - 3162-3216 후보 합성 블록 — 특히 3193-3206 창 승급 조건(이번에 헬퍼로 뽑는 대상)
  - 3083-3092 `_da_user/_da_ref` both-or-neither, 3420-3429 display_anchor 가 **크롭 중심·원 앵커·V 꼭짓점**의 단일 출처
- `backend/shared/python/sunity_shared/analysis/pose_collapse.py` (전체 87행 — 임계는 여기 것만)
- `backend/functions/pipeline/app.py` 5195-5375 `_run_gated_card_inherit` 안의 unit 루프:
  5203-5222 `_override_idx`, 5264-5290 `align_bake` 산출, 5298-5327 `display_anchor` 산출, 5328-5333 u9/r9, 5334-5375 호출
- 테스트 fixture 관례: `backend/tests/test_select_frame_skips_collapse.py`(`_report`/`_mk` 헬퍼, 8관절, MEMBERS),
  source 단언 선례: `backend/tests/test_fault_zoom_ref_marked.py:305-323`

규율: 이모지 금지. 한국어 주석에 **왜**와 실측 근거(날짜·수치)를 박제. 순수 함수는 fault_zoom, 파이프라인은 얇게.
의미있는 테스트만(CLAUDE.md §7). 백엔드 게이트 = `cd backend && .venv/bin/python -m pytest -q` 기준선 **4655 passed / 0 failed / 20 skipped**(2026-09-06).
(주의: 전체 게이트는 rtk 래퍼 없이 위 커맨드 그대로 — 부분 배치에서 `test_fault_zoom_ref_marked.py` 를 순수 파일들과 섞어 돌리면
`imageio_ffmpeg` import 순서 아티팩트로 4건이 실패해 보인다. 단독/전체 실행에서는 통과한다.)
Pod·Gemini·Firestore 호출 없음 — 전부 pod-free.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: fault_zoom — 성한 프레임 판정 단일화(_frame_usable) + nearest_usable_frame 순수 함수 + 테스트</name>
  <files>backend/shared/python/sunity_shared/analysis/fault_zoom.py, backend/tests/test_gated_frame_skips_collapse.py</files>
  <behavior>
    새 파일 `backend/tests/test_gated_frame_skips_collapse.py`. fixture 는 `test_select_frame_skips_collapse.py` 의
    `_report`/`_mk`(8관절 JOINTS, MEMBERS=("right_elbow","right_shoulder"), "x"=폴 위 한 세로선 붕괴, "o"=흩어진 정상) 를 그대로 복사해 쓴다.
    모듈 docstring 에 09-06 실측 수치(42패널/붕괴 14 전부 gated/±2 10 · ±4 11 · ±8 14/반사실 악화 0)와 기전을 적는다.
    - 옮긴다: kinds ["o","o","x","x","x","o","o"], anchor=3 → (1, "moved") (3±1 붕괴, 3-2=1 이 가장 가까운 성한 프레임)
    - 동점 규칙: kinds ["o","x","o"], anchor=1 → (0, "moved") — 같은 거리면 작은 인덱스(select_confident_frame 429-432행 관례)
    - 대조군(무회귀): 같은 데이터에서 anchor 를 성한 프레임(예: 0)에 두면 (0, "kept") — 이웃 conf 가 더 높아도 재선택하지 않는다
    - 반경 ±4 도달/미도달: 11프레임, anchor=5, index 1 만 "o" → (1, "moved"); index 0 만 "o"(=5-5) → (5, "stuck") (실측: ±4 가 11/14, belle 지목 카드가 ±4 에서 구제)
    - 목적지는 두 조건 모두: anchor=2 "x"; frame 1 은 "o" 좌표지만 전 멤버 conf 0.3(_member_pts valid 빈 리스트 = relaxed 만); frame 3 "o" conf 0.9 → (3, "moved") (p34fresh1786349646 교훈 — 붕괴만 보면 그릴 수 없는 프레임으로 간다)
    - 기준측 18fps rep: fps=18.0, frames=2N 리포트. `_to_rep_idx(idx, 9.0, 18.0, 2N)` = round(2·idx). rep 6..9 "x", rep 10 "o", rep 4 "o"; anchor(9fps)=4 → (5, "moved") — 판정이 rep 인덱스 변환을 거친다(정은지 doc 18fps, 붕괴 9/14 가 기준측)
    - legacy(joints 메타 없음): rep.pop("joints") → is_collapsed_frame False → (anchor, "kept") fail-open
    - 경계: anchor=0 "x", frame 1 "o" → (1, "moved"); n_frames=1 이면 (0, "stuck") — 범위 밖 후보는 클램프 중복이 아니라 스킵
    - 새 임계 금지: `inspect.signature(fz.nearest_usable_frame).parameters["radius"].default is fz._MOMENT_ANCHOR_RADIUS_WIDE` 그리고 == 4
    - `_frame_usable` 직접: 붕괴 프레임 False / 비붕괴·conf 0.3 프레임 False / 비붕괴·conf 0.9 프레임 True
  </behavior>
  <action>
    1) `_frame_usable(report, idx, members, frames_fps, rep_fps, rep_frames) -> bool` 를 `_drop_collapsed` 바로 아래(416행 뒤)에 추가한다.
       본문 = `rep_idx = _to_rep_idx(idx, frames_fps, rep_fps, rep_frames)` 후 `bool(_member_pts(report, rep_idx, members)[0]) and not is_collapsed_frame(report, rep_idx)`.
       docstring(한국어): "쓸 수 있는 프레임 = 그릴 수 있음(_member_pts valid) AND 비붕괴(pose_collapse 단일 출처). 창 승급(ms2)과 gated 이동(f4j)이
       **같은 판정**을 써야 한다 — 붕괴만 보면 그릴 수 없는 프레임으로 간다(실측 p34fresh1786349646). `_to_rep_idx` 선례의 중복 공식 금지."
       `_member_pts` 는 파일 뒤(1308행)에 정의돼 있지만 호출 시점 해석이라 문제 없다(기존 `_drop_collapsed`→`is_collapsed_frame` 순서와 같은 관례).
    2) 창 승급 블록(3193-3206행)의 인라인 `any(_member_pts(...)[0] and not is_collapsed_frame(...) for _c in _u_syn)` 를
       `any(_frame_usable(user_report, _c, unit.members, frames_fps, u_rep_fps, u_rep_frames) for _c in _u_syn)` 로 바꾼다. 동작 보존(순수 추출) —
       바로 위 ms2 주석 블록(3179-3192행)은 그대로 두고 마지막에 "판정 함수 = `_frame_usable` (quick-260906-f4j 가 gated 이동과 공유)" 한 줄만 덧붙인다.
       기존 테스트(test_fault_zoom_record_moment.py::test_collapsed_anchor_falls_back_inside_the_window_without_certifying, test_pose_pair_skips_collapse.py,
       test_fault_zoom_display_repair.py)가 무회귀를 지킨다.
    3) `nearest_usable_frame(report, frame_idx, members, *, n_frames, frames_fps=9.0, radius=_MOMENT_ANCHOR_RADIUS_WIDE) -> tuple[int, str]` 를
       `_MOMENT_ANCHOR_RADIUS_WIDE = 4`(567행) 바로 뒤, `_POSE_MIN_COMMON_JOINTS` 주석 블록 앞에 정의한다(기본 인자가 그 상수를 참조하므로 뒤에 와야 한다).
       알고리즘(전부 순수, report 메타는 select_confident_frame 관례대로 `rep_fps = float(report.get("fps") or frames_fps)`, `rep_frames = int(report.get("frames") or 0)`):
         a. `n_frames <= 0` 이면 `(int(frame_idx), "kept")`. 아니면 anchor = build_fault_zoom_comparisons 와 같은 클램프 `max(0, min(int(frame_idx), max(0, n_frames-1)))`.
         b. 트리거 = **붕괴만**: `is_collapsed_frame(report, _to_rep_idx(anchor, ...))` 가 False 면 `(anchor, "kept")`. (비붕괴인데 그릴 수 없는 앵커는 align_bake 폴백(nh4)이
            소유하는 경우라 건드리지 않는다 — 트리거를 넓히면 m0k A/B 소생 6/6 인증 경로가 바뀐다.)
         c. `for d in range(1, radius+1): for cand in (anchor-d, anchor+d):` 범위 `0 <= cand < n_frames` 인 것만, `_frame_usable(...)` 이면 즉시 `(cand, "moved")`.
            −d 를 +d 보다 먼저 보므로 같은 거리는 작은 인덱스가 이긴다(select_confident_frame 의 오름차순 tie-break 관례와 정합 — 실측상 방향 선호 근거 없음, 결정론이 목적).
         d. 못 찾으면 `(anchor, "stuck")` — fail-open(카드가 사라지는 것보다 낫다. 사진 장수 불변).
       docstring(한국어)에 박제: 09-06 실측(42패널/붕괴 14 전부 gated · 학생 5 정은지 9 / conf<0.5 겹침 8 = 신뢰도는 붕괴의 부분집합 / ±2 10 · ±4 11 · ±8 14 /
       최대 이동 0.78초, 대부분 0.11초 / 반사실 5패널 악화 0), 기전(gated 호출부 dtw_match=None+at_frame_idx=None 고정 → 합성 블록·_drop_collapsed·창 승급 미도달),
       왜 ±4 인가(±8 이 14/14 이지만 ms2 가 박제한 "항상 넓히면 악화" 교훈 + belle 지목 카드가 ±4 에서 구제 — 새 상수 0, `_MOMENT_ANCHOR_RADIUS_WIDE` 재사용),
       반환 status 3종(kept/moved/stuck)은 호출측 로그용, 채점 무접촉(complete 이후 표시 렌더, D-20).
    4) 테스트 파일을 `<behavior>` 대로 작성한다(파일 상단 sys.path 삽입은 선례 그대로). 각 테스트 docstring 에 한 줄 "왜".
    커밋: `fix(f4j): 붕괴 앵커에서 가장 가까운 성한 프레임으로 — 순수 함수 + 창 승급과 판정 공유`.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && .venv/bin/python -m pytest -q tests/test_gated_frame_skips_collapse.py tests/test_fault_zoom_record_moment.py tests/test_pose_pair_skips_collapse.py tests/test_fault_zoom_display_repair.py tests/test_select_frame_skips_collapse.py tests/test_pose_collapse.py && grep -v '^\s*#' shared/python/sunity_shared/analysis/fault_zoom.py | grep -c "_frame_usable(" | xargs test 3 -le && grep -v '^\s*#' shared/python/sunity_shared/analysis/fault_zoom.py | grep -c "^_MOMENT_ANCHOR_RADIUS_WIDE" | xargs test 1 -eq</automated>
  </verify>
  <done>
    `fz.nearest_usable_frame` 이 붕괴 앵커에서 ±4 안 가장 가까운 성한 프레임을 (idx, "moved") 로, 비붕괴 앵커를 (anchor, "kept") 로, 반경 안 전건 불가를 (anchor, "stuck") 으로 돌려준다.
    `_frame_usable` 호출이 3곳(정의·창 승급·nearest_usable_frame) — 창 승급 블록에 인라인 `_member_pts(...)[0] and not is_collapsed_frame` 가 남아 있지 않다.
    새 상수·새 임계 0. 위 6개 테스트 파일 전부 pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: gated 호출부 배선 — u9/r9 이동 + 로그 1행 + freeze-순간 payload 정합 + 배선 source 단언 + 전체 게이트</name>
  <files>backend/functions/pipeline/app.py, backend/tests/test_gated_frame_skips_collapse.py</files>
  <action>
    `_run_gated_card_inherit` 의 unit 루프에서 `r9 = _override_idx(...)`(5331-5333행) 직후, `comps = _fz.build_fault_zoom_comparisons(`(5334행) 직전에 끼운다. 얇게 — 판정은 전부 Task 1 순수 함수:
    1) `_members = tuple(str(j) for j in (cu.get("joints") or ()) if j)` (fault_zoom 이 `_CropUnit.members` 를 만드는 것과 같은 식 — 3106행).
       `u9_new, u_why = _fz.nearest_usable_frame(user_report, u9, _members, n_frames=int(user_frames.shape[0]))`,
       `r9_new, r_why = _fz.nearest_usable_frame(ref_report, r9, _members, n_frames=int(ref_frames.shape[0]))`. frames_fps 는 기본 9.0(호출부의 `frames_fps=9.0` 과 동일).
    2) freeze-순간 payload 정합: `display_anchor` 와 `align_bake` 는 quick-260813-fxx/nh4 정의상 **게이트 freeze 순간(u_ai/r_ai)의 align 17-kp 좌표**다.
       프레임이 그 순간을 떠나면 그 좌표는 정의상 그 프레임의 것이 아니다 — `display_anchor` 는 적용되면 **크롭 중심·원 앵커·V 꼭짓점 전부**의 단일 출처라(fault_zoom 3420-3429행)
       그대로 두면 옮긴 사진 위에 옛 순간의 중심이 찍힌다(이번 결함과 같은 종류의 "제목과 다른 부위"). 그래서:
       `u9_new != u9 or r9_new != r9` 이면 `display_anchor = None`(fault_zoom 3427행 both-or-neither 라 한 측만 옮겨도 쌍이 무효 — 양측 rep12 vertex 경로로 복귀, fxx 이전 동작),
       옮긴 측만 `align_bake[side] = {}`(옮긴 프레임은 `_frame_usable` 로 rep12 valid 가 보장되므로 폴백이 필요 없고, 안 옮긴 측의 폴백은 그대로 산다).
       `suppress` 는 무접촉(눈 불일치·align conf 미달의 "표시 생략"은 게이트 판정이지 프레임 문제가 아니다 — 옮겨도 그 측 표시는 계속 생략, userMarked/refMarked 가 사실을 말한다).
    3) 로그 1행 — `u_why != "kept" or r_why != "kept"` 일 때만(둘 다 kept 면 침묵 = 종전 로그 byte-동일):
       `log.info("fault_zoom_gated_frame_shift analysis_id=%s rid=%s user=%d->%d(%s) ref=%d->%d(%s) display_anchor=%s", analysis_id, rid, u9, u9_new, u_why, r9, r9_new, r_why, tag)`
       rid = `str(rec.get("recordId") or "").split(":")[0]`(인접 로그 관례), tag = "dropped"(원래 있었는데 떨굼) / "kept"(안 옮김 또는 원래 없음 — 구분 필요하면 "absent").
       stuck 도 남긴다: "앵커 붕괴인데 ±4 안에 성한 프레임 없음"이 사후 판별의 핵심 흔적이다(지금은 어느 경로가 프레임을 골랐는지 흔적 0).
    4) `u9, r9 = u9_new, r9_new` 후 기존 호출은 **그대로**(`user_frame_idx=u9`, `ref_frame_idx=r9`, `at_frame_idx: None`, `dtw_match=None` 유지 — 순간은 여전히 override 가 소유).
       주석(한국어): quick-260906-f4j, 09-06 실측 요지 1-2줄, "이 경로는 dtw_match=None 이라 fault_zoom 의 창 승급이 구조적으로 미도달 — 그래서 여기서 옮긴다", 그리고 2)의 정합 근거.
       stage-1(3488)·advisory(3531) 호출부는 건드리지 않는다(실측 붕괴 0).
    5) 배선 source 단언을 Task 1 테스트 파일에 추가(선례 test_fault_zoom_ref_marked.py:305-323 방식 — app.py 를 텍스트로 읽는다. app.py import 는 env 의존이라 금지):
       - `def _run_gated_card_inherit(` 부터 다음 컬럼0 `\ndef ` 까지를 body 로 잘라 `body.count("_fz.nearest_usable_frame(") == 2`,
         `body.index("_fz.nearest_usable_frame(") < body.index("comps = _fz.build_fault_zoom_comparisons(")`, `"user_frame_idx=u9"` 와 `"ref_frame_idx=r9"` 와 `"fault_zoom_gated_frame_shift"` 가 body 안에 있음.
       - 범위 가드: app.py 전체에서 주석 줄(`lstrip().startswith("#")`) 제외 `nearest_usable_frame(` 등장 == 2 (stage-1·advisory 무접촉 증명).
       - 로그 형식 lockstep: `"fault_zoom_gated_frame_shift analysis_id=%s rid=%s user=%d->%d(%s) ref=%d->%d(%s)"` 부분 문자열이 app.py 에 있음(사후 grep 재료 고정).
    6) 전체 게이트를 rtk 없이 그대로 돈다: `cd backend && .venv/bin/python -m pytest -q` → passed ≥ 4655 + 신규 테스트 수, failed 0, skipped 20.
    커밋: `fix(f4j): gated 카드 경로에 붕괴 프레임 배제 배선 — 로그 1행, freeze-순간 좌표 payload 정합`.
    SUMMARY 에 반드시: 라이브 미검증(wiring-claims-need-log-evidence) — 다음 Pod 때 09-03 fixture 재분석에서 `fault_zoom_gated_frame_shift` 로그 회수 +
    `backend/scripts/audit_card_photos.py` 재감사(mismatched 7 → 감소, 신규 불일치 0)가 실증이다. 이 quick 의 게이트는 pod-free 테스트까지.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && .venv/bin/python -m py_compile functions/pipeline/app.py && grep -v '^\s*#' functions/pipeline/app.py | grep -c "nearest_usable_frame(" | xargs test 2 -eq && .venv/bin/python -m pytest -q tests/test_gated_frame_skips_collapse.py && .venv/bin/python -m pytest -q 2>&1 | tail -3</automated>
  </verify>
  <done>
    app.py 의 `nearest_usable_frame(` 호출이 정확히 2곳(학생·기준), 둘 다 `_run_gated_card_inherit` 안, `build_fault_zoom_comparisons` 직전.
    옮김/stuck 이 `fault_zoom_gated_frame_shift` 1행으로 남고, 옮긴 카드는 `display_anchor=None` + 옮긴 측 `align_bake[side]={}` 로 좌표와 프레임이 같은 순간을 가리킨다.
    stage-1·advisory 호출부·채점·방출 필드·conf 임계 무접촉. 전체 pytest failed 0, passed ≥ 4655(신규분 가산), skipped 20.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firestore keypointReport → fault_zoom 순수 함수 | 백엔드가 쓴 데이터만 읽는다(클라이언트 입력 아님). 새 입력 표면 0 |
| 표시 렌더 → 앱 | 방출 필드 무변경(userFrameIdx/refFrameIdx/userVideoSec 는 옮긴 프레임을 그대로 반영 — 계약 형상 불변) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-f4j-01 | Tampering | nearest_usable_frame 입력(keypointReport) | accept | 서버측 생성 데이터. 결측/legacy 는 기존 fail-open(is_collapsed_frame False, `_member_pts` 결측 스킵) 그대로 |
| T-f4j-02 | DoS | 탐색 루프 | accept | 상한 = 2×radius(8) 후보 × 멤버 관절 수, 카드당 상수 시간. 반경은 기존 상수 재사용 |
| T-f4j-03 | Information disclosure | `fault_zoom_gated_frame_shift` 로그 | accept | analysis_id·recordId·정수 인덱스만. PII·좌표 없음(인접 로그 관례와 동일) |
| T-f4j-SC | Tampering | npm/pip 설치 | n/a | 패키지 설치 0 — 순수 Python 변경만 |
</threat_model>

<verification>
- Task 1 타깃 테스트 6파일 pass + `_frame_usable` 3곳 + `_MOMENT_ANCHOR_RADIUS_WIDE` 정의 1곳(새 상수 0).
- Task 2: `py_compile`, app.py 비주석 `nearest_usable_frame(` == 2, source 단언 테스트 pass.
- 전체: `cd backend && .venv/bin/python -m pytest -q` → failed 0, passed ≥ 4655 + 신규, skipped 20 (rtk 래퍼 없이).
- 무접촉 확인(grep): `rtk git diff --stat` 에 파일 3개만(fault_zoom.py / app.py / 새 테스트). app.py diff 가 5328-5375 구간 밖을 건드리지 않음.
- 라이브 실증(이 quick 범위 밖, SUMMARY 에 "미검증"으로 박제): 다음 Pod 세션에서 09-03 6문서 재분석 → 로그 `fault_zoom_gated_frame_shift` 회수(붕괴 14패널 중 ≥11 moved 기대),
  `audit_card_photos.py --pairs …` 재감사에서 mismatched 7 이하·신규 불일치 0. Pod 는 시연 때만(belle 08-28) — 이 quick 에서 띄우지 않는다.
</verification>

<success_criteria>
- gated 카드의 앵커가 붕괴면 표시 프레임이 ±4 안 가장 가까운 성한 프레임으로 옮겨진다(양측 독립, fail-open, 로그 1행).
- 비붕괴 앵커 카드는 byte-동일. 사진 장수·어느 멈춤인지 불변. 채점·계약·stage-1·advisory·conf 임계·어깨 문법 무접촉.
- 판정 출처 단일(pose_collapse), 성한 판정 함수 단일(_frame_usable — 창 승급과 공유), 반경 상수 단일(_MOMENT_ANCHOR_RADIUS_WIDE).
- 백엔드 pytest failed 0, passed 4655 이상.
</success_criteria>

<output>
Create `.planning/quick/260906-f4j-gated-card-collapse-frame-skip/260906-f4j-SUMMARY.md` when done — 라이브 미검증 명시 + 다음 Pod 실증 절차(로그 회수 → 재감사) 포함.
</output>
