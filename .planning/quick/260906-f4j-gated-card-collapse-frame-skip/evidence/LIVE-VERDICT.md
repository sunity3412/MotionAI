# f4j 라이브 실증 — 2026-09-06 (Pod re55fphu175viu L4, commitSha 93aeec29)

## 판정: 수리는 설계대로 돌았다. 그런데 **눈 불일치율은 안 움직였다.**

### 된 것 (수리가 목표한 것)

- `fault_zoom_gated_frame_shift` **8행 회수** — 코드가 프로덕션에서 실제로 돌았다.
  moved 10패널(학생 6·정은지 4) / stuck 2(정은지, ±4 안에 성한 프레임 없음) / `display_anchor=dropped` 4.
  powerspin r01 은 학생·정은지 **양쪽** 이동(`user=8->6, ref=26->25`).
  climb r00 은 `user=30->29` 로 09-06 오프라인 예측표와 **정확히 일치**.
- 출하 카드의 앵커 붕괴: **학생 5/21 → 0/18, 정은지 9/21 → 4/18** (합계 14/42 → 4/36).
  남은 4 = gated stuck 2 + advisory 2(수리 범위 밖 경로).
- `stuck` 이 로그로 남는다 = 사후 판별 불가 문제 해소.

### 안 된 것 (정직하게)

`audit_card_photos.py --rounds 5` (프로젝트 규율인 5회 반복, 지금까지 지켜진 적 없음):

```
card_photo_audit total=18 mismatched=8 unresolved=1
```

| | 09-05 (3라운드, 21장) | 09-06 라이브 (5라운드, 18장) |
|---|---|---|
| 전체 불일치 | 7 / 21 (33%) | 8 / 18 (44%) |
| confirmed(감점) | 4 / 15 (27%) | **4 / 13 (31%)** |
| advisory(참고) | 3 / 6 | **4 / 5** |

**감점 카드는 사실상 제자리(4장 → 4장)이고, 악화분은 참고 카드에 몰려 있다.**
참고 카드는 이번 수리가 손대지 않는 경로이고 belle 이 09-03 에 이미 보류시킨 계열이다.

불일치 8장이 난 패널: 학생만 2 · 정은지만 4 · 양쪽 2 — **정은지 쪽이 여전히 두 배.**

### 이게 뜻하는 것

**붕괴는 "제목과 다른 부위"의 지배적 원인이 아니었다.** 09-06 오프라인 측정이 이미 그렇게 말했다
(앵커 불건전 10장 중 눈 불일치는 3장뿐 = 정밀도 3/10). 이번 라이브가 그것을 규모로 확인했다 —
붕괴 앵커를 14패널에서 4패널로 줄였는데 불일치율은 안 내려갔다.

남은 원인으로 이번 런이 지목하는 것:
- **powerspin 왼어깨 정은지 패널이 `knee` 로 읽힌다** — belle 09-05 원문("어깨 잡는데 다리를 잰다")
  그대로인데, 이번엔 정은지 측이고 **앵커는 성하다**. 어깨 각도 문법(꼭짓점=겨드랑이 `_ARMPIT_T=0.15`,
  두 선=`ANGLE_BAKE_MAP["shoulder"]=("elbow","hip")`) 이 원인 후보. 승인 문법(7R#2)이라 belle 판정 필요.
- **pdshape 오른팔꿈치 학생 패널이 `neck` 으로 읽힌다** — 앵커 성함. 붕괴와 무관한 별개 기전.
- **참고 카드 4/5 불일치** — 이번 수리 범위 밖.

### 비교의 한계 (반드시 같이 읽을 것)

- Gemini 단계가 비결정이라 재분석은 **재현이 아니라 새 분석**이다. pdshape 60→63점, 감점 항목도 다르다
  (09-03 right_shoulder/left_hip → 09-06 right_knee). **카드 대 카드 비교 불가**, 집계로만 비교했다.
- 라운드 수가 다르다(3 → 5). 표본도 21 → 18. n 이 작아 33% vs 44% 차이는 유의하지 않다.
  정직한 표현은 "나빠졌다"가 아니라 **"지표가 안 움직였다"**.
- 자가검출 1건: 기준 리포트를 `db.document('referenceMotions/…')` 로 읽으면 빈 dict 가 오고
  `is_collapsed_frame` 이 fail-open(False) 이라 **"정은지 붕괴 0"이라는 가짜 통과**가 나왔다.
  `fa.get_reference_motion(rid)['referenceKeypointReport']` 로 고쳐 4/18 이 실제 값.

### 게이트

backend pytest 4669 passed / 0 failed / 20 skipped. Pod 종료·`pod-expected=down`.
