---
phase: quick-260906-j8g
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/shared/python/sunity_shared/analysis/card_photo_audit.py
  - backend/shared/python/sunity_shared/analysis/card_gates.py
  - backend/scripts/audit_card_photos.py
  - backend/tests/test_anchor_part_check.py
  - backend/functions/pipeline/app.py
  - backend/tests/test_gated_frame_skips_collapse.py
autonomous: true
requirements: [quick-260906-j8g]
must_haves:
  truths:
    - "gated(멈춤 상속) 카드마다 학생·기준 **양측** 앵커 좌표를 마킹 없는 좁은 크롭(짧은 변 × 0.18)으로 기계 눈에 물어 '제목의 부위인가'를 확인한다 — 기존 _eye_check(bent/extended, 학생만)와 별개이고 그 게이트는 무접촉"
    - "확인 실패(눈이 읽은 부위 ∉ expected_parts)면 fault_zoom 의 건전 후보(_frame_usable = 비붕괴 AND 그릴 수 있음) 중 가까운 것부터 최대 2프레임 재확인(ANCHOR_CHECK_MAX_RETRY) → 통과하면 그 측 프레임을 옮기고, 그래도 실패면 그 측을 suppress 에 넣어 표시만 생략한다. 사진은 남는다(belle 09-03 규칙 1)"
    - "expected 가 빈 집합·눈이 못 읽음(unclear/error/어휘 밖)·좌표나 프레임 부재·API 키 부재 = 비구속(표시 유지) — 감사 불가는 불일치가 아니다(card_photo_audit 관례)"
    - "사진 장수·어느 멈춤인지 불변 — 확인은 표시(suppress_marks)와 그 멈춤 안 프레임만 정한다. 카드 제거·Firestore 방출 필드 추가·채점·hold/pair/_eye_check·stage-1(3488)·advisory(3531)·어깨 각도 문법 무접촉"
    - "눈 호출은 (측, 프레임, 관절, 좌표) 캐시로 중복 억제되고 eye_calls 에 합산되며, 카드당 측별 로그 1행 `fault_zoom_anchor_check …` + 문서당 `fault_zoom_anchor_check_summary …` 로 호출 수·통과/이동/생략이 남는다"
    - "backend pytest: 4669 passed / 0 failed / 20 skipped 기준선에서 신규 포함 전부 pass, failed 0"
  artifacts:
    - path: "backend/shared/python/sunity_shared/analysis/card_photo_audit.py"
      provides: "순수 판정 — 토큰 최빈(modal_token) + 앵커 부위 판정(anchor_verdict: ok|mismatch|unreadable|no_expectation)"
      contains: "def anchor_verdict"
    - path: "backend/shared/python/sunity_shared/analysis/card_gates.py"
      provides: "상수 3개(ANCHOR_CHECK_CROP_FRAC=0.18 / MAX_RETRY=2 / ROUNDS=3) + part_crop(무마킹) + eye_part_token(조기 종료 최빈) + anchor_retry_frames + verify_anchor_side 상태기계"
      contains: "def verify_anchor_side"
    - path: "backend/functions/pipeline/app.py"
      provides: "gated 호출부 배선 — f4j 이동 직후·build_fault_zoom_comparisons 직전에 양측 확인, 이동/suppress, 로그 2종"
      contains: "cg.verify_anchor_side("
    - path: "backend/tests/test_anchor_part_check.py"
      provides: "오프라인 테스트(눈 가로채기) — 통과/재확인 후 이동/생략/상한/비구속/장수 불변 + 배선 source 단언"
      min_lines: 150
  key_links:
    - from: "backend/functions/pipeline/app.py (_run_gated_card_inherit)"
      to: "card_gates.verify_anchor_side"
      via: "unit 루프, `r9 = r9_new` 직후 · `comps = _fz.build_fault_zoom_comparisons(` 직전"
      pattern: "cg\\.verify_anchor_side\\("
    - from: "card_gates.verify_anchor_side 결과 action=suppressed"
      to: "fault_zoom.build_fault_zoom_comparisons(suppress_marks=…)"
      via: "기존 suppress 집합 — 표시 생략 경로 재사용 (새 방출 필드 0)"
      pattern: "suppress\\.add\\("
    - from: "card_gates.eye_part_token"
      to: "card_gates.eye_judge(crop, \"part\")"
      via: "운영 질문·스키마 그대로, 라운드만 여기서 센다"
      pattern: "eye_judge\\(crop, \"part\""
---

<objective>
카드를 내보내기 전에 **앵커 좌표가 실제로 제목의 부위인지 기계 눈으로 확인**한다. 실패하면 창 안 다른
건전 프레임(최대 2)으로 옮기고, 그래도 실패하면 **사진은 남기고 표시만 생략**한다(belle 09-03 규칙 1).

Purpose: 2026-09-06 실측(09-03 라이브 6문서 36패널, 짧은 변 18% 좁은 크롭 → 기계 눈): **좌표 OK 15 ·
좌표 틀림 14 · 못 읽음 7**. 신뢰도(conf 0.872 판독불가, 0.749 무릎 자리에 손)도 붕괴 검사(f4j 가 붕괴
14→4 로 줄였는데 감점 카드 눈 불일치 4→4)도 이것을 예측하지 못한다. 카드 크롭(짧은 변 42%)은 몸
절반이 들어와 틀린 좌표를 가린다(클라임 오른무릎 `ok` 인데 좌표는 팔). 그래서 **보는 수밖에 없다** —
`.planning/quick/260906-f4j-gated-card-collapse-frame-skip/evidence/ROOT-CAUSE.md` (진단 확정본, 재조사 금지).

Output: 순수 판정 2함수(card_photo_audit) + 확인 부품 4함수·상수 3개(card_gates) + gated 배선(app.py) +
오프라인 테스트 파일 1개. 채점·방출 필드·기존 게이트 무접촉.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kimtaesung/Dev/SunityMotion/CLAUDE.md
@/Users/kimtaesung/Dev/SunityMotion/.planning/quick/260906-f4j-gated-card-collapse-frame-skip/evidence/ROOT-CAUSE.md
@/Users/kimtaesung/Dev/SunityMotion/backend/shared/python/sunity_shared/analysis/card_photo_audit.py
@/Users/kimtaesung/Dev/SunityMotion/backend/shared/python/sunity_shared/analysis/card_gates.py
@/Users/kimtaesung/Dev/SunityMotion/backend/functions/pipeline/app.py
@/Users/kimtaesung/Dev/SunityMotion/backend/tests/test_gated_frame_skips_collapse.py
@/Users/kimtaesung/Dev/SunityMotion/backend/tests/test_card_gates.py
@/Users/kimtaesung/Dev/SunityMotion/backend/scripts/audit_card_photos.py

<interfaces>
아래는 실물에서 읽은 계약이다 — 탐색 없이 그대로 쓴다.

card_gates.py (894행):
  · `eye_judge(crop, claim, *, api_key, expected_limb=None, joint_kind=None, model=DEFAULT_C_MODEL, timeout_s=60.0) -> dict`
    반환 {observed, limb, match, confidence, reason}. claim "part" 는 `_CLAIM_QUESTION["part"]` 그대로(힌트·기대 0),
    `_eye_verdict` 는 "읽어냈는가"만 — 대조는 호출측. 네트워크 실패 = observed "error". (691행)
  · `eye_judge_majority(..., max_rounds=3)` (755행) — **match 표**를 센다. part claim 에서는 match=읽어냈다 라
    부위 최빈에 못 쓴다(audit_card_photos.py 24-30행 주석) → 토큰 최빈은 새 함수로.
  · `mark_crop(frame_rgb, joint_xy_px, *, crop_px=360, ring_frac=0.10)` (672행) — 링을 **그린다**. 이번 크롭은 이걸 쓰지 않는다.
  · `crit_joint(joint_or_crit)` (91행) / `joint_kind_ko` (114행) / `eye_applicable` (128행) / `NAME_ALT` (82행).
  · 모듈은 `from . import fault_zoom as fz` 를 이미 import (`PAIR_MIN_JOINTS = fz._POSE_MIN_COMMON_JOINTS`, 62행 선례 — noqa: SLF001).
  · `DEFAULT_C_MODEL` 은 `..gemini.config` 에서 온다 — 모델 문자열을 어디에도 박지 않는다.
card_photo_audit.py (270행, numpy/네트워크 0, card_gates 를 import 하지 않음):
  · `PART_VOCAB`, `UNREAD = {"unclear","error"}`, `_readable(token)` (186행), `expected_parts(criterion, joint) -> frozenset` (107행; 빈 집합 = 감사 불가),
    `_KIND_PARTS`(elbow/shoulder/hip/knee), `_CRIT_PARTS`(split_angle/leg_extension/arm_extension), `__all__` (256행).
fault_zoom.py:
  · `_to_rep_idx(idx, frames_fps, rep_fps, rep_frames) -> int` (372행) — 9fps 배열 인덱스 → rep 인덱스.
  · `_frame_usable(report, idx, members, frames_fps, rep_fps, rep_frames) -> bool` (418행) — 비붕괴 AND 그릴 수 있음.
  · `_MOMENT_ANCHOR_RADIUS_WIDE = 4` (585행), `nearest_usable_frame(...)` (588행) 순회 관례: d=1..radius, (anchor-d, anchor+d) 순.
  · `ref_display_frame_index(r9_idx, ref_video_n, ref_rep_frames, ref_rep_fps, frames_fps=9.0) -> int` (1042행) — ref 9fps 인덱스 → ref 비디오 배열 인덱스.
  · `_member_pts(report, frame_idx, members) -> (valid[(name,xy)], relaxed[xy])` (1387행), `_anchor_xy(valid, deltas) -> xy` (1414행),
    `_gated_kp(report, frame_idx, joint)` (1163행, conf>=0.5), `make_reference_anchor_resolver(motion_id, criterion, *, anchors=None)` (1436행),
    `criterion_vertex_xy(criterion, members, report, frame_idx, deltas=None, resolver=None) -> xy|None` (1487행) — crop 중심 단일 출처.
  · `native_or_downscaled(provider, which, idx, frames)` (1257행) — 카드가 실제로 자르는 프레임(원본 우선, 축소본 폴백).
  · build_fault_zoom_comparisons 가 실제로 그리는 좌표의 우선순위 (3474-3505행): display_anchor(양측 모두 있을 때만) → criterion_vertex_xy(rep12) →
    `_side_crop(center=None, anchor=_anchor_xy(valid))` (valid 가 비면 seam 2 가 align_bake 점을 valid 로 주입, 3411-3421행) → relaxed/전신(표시 0).
    학생 프레임 = `user_frames[u_idx_unit]`, 기준 프레임 = `ref_frames[ref_display_frame_index(r_idx_unit, …)]` (3442-3457행).
  · `suppress_marks` (2923행, docstring 3039행): {"user","ref"} 부분집합 — 그 측 원·선·호·화살표를 그리지 않고 userMarked/refMarked=False. 크롭은 그대로.
app.py `_run_gated_card_inherit` (4840-5537행):
  · unit 루프 지역 변수(이 시점에 살아 있음): `crit`, `cu`, `rec`, `decision`, `suppress: set[str]`, `u_ai/r_ai`, `align_bake: {"user":{}, "ref":{}}`,
    `display_anchor: dict|None`, `u9/r9`(f4j 이동 반영 후), `_members: tuple[str,...]`, `user_frames/ref_frames` (T,H,W,3), `native_at`,
    `user_report/ref_report`(rep12), `deficits`, `eff`, `api_key`, `eye_calls`(nonlocal 카운터), `eye_ledger`(S3 eye/ 원장 — 각 항목 `side`,`joint` 키 필수, `png` 는 업로드 뒤 pop).
  · f4j 블록 끝 = 5383-5385행 `if u_why == "moved": u9 = u9_new` / `if r_why == "moved": r9 = r9_new`. 그 다음 5386행 `comps = _fz.build_fault_zoom_comparisons(`.
  · 기존 `_eye_check(side, rep, idx, gate_joint)` (4991행) 는 align 15fps 트랙·`_frame_at(side, sec)`(render 30fps 프레임)·claim bent/extended — **무접촉**.
  · 부착 완료 로그 5475행 `"card_gates 대체 부착 완료 analysis_id=%s expected_units=%d emitted=%d …"` — 이 문자열은 바꾸지 않는다(사후 grep 재료). 새 요약 로그는 그 **앞**에 별도 1행.
tests:
  · `test_gated_frame_skips_collapse.py::_gated_body()` — app.py 를 텍스트로 잘라 단언(app.py import 는 env 의존이라 금지). fixture `_mk(kinds, confs)`(8관절, "x"=붕괴, "o"=정상), `MEMBERS`.
  · `test_card_gates.py::_scripted_eye_judge(monkeypatch, script)` (332행) — `cg.eye_judge` 를 대본으로 대체. `test_eye_judge_part_sends_part_question_and_schema` (609행) — urlopen 가로채기 선례.
  · `test_audit_card_photos_script.py::test_modal_token_majority_and_ties` — 스크립트의 `acp.modal_token` 의미(동률 unclear) 고정.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 순수 층 — card_photo_audit(modal_token·anchor_verdict) + card_gates(상수 3·part_crop·eye_part_token·anchor_retry_frames·verify_anchor_side) + 오프라인 테스트</name>
  <files>backend/shared/python/sunity_shared/analysis/card_photo_audit.py, backend/shared/python/sunity_shared/analysis/card_gates.py, backend/scripts/audit_card_photos.py, backend/tests/test_anchor_part_check.py</files>
  <behavior>
    새 파일 `backend/tests/test_anchor_part_check.py`(sys.path 삽입·fixture 는 test_gated_frame_skips_collapse.py 의 `_mk`/`MEMBERS` 를 복사). 모듈 docstring 에 09-06 실측(36패널: OK 15 / 틀림 14 / 못읽음 7, conf 0.872→판독불가·0.749→손·0.743→허벅지·0.637→얼굴, f4j 붕괴 14→4 인데 감점 카드 불일치 4→4, 카드 크롭 42% 가 가림)과 belle 09-03 규칙 1 을 적는다. 각 테스트 docstring 1줄 "왜".
    card_photo_audit:
    - modal_token: ["knee","knee","elbow"]→"knee" / ["knee","elbow"]→"unclear"(동률) / ["unclear","error"]→"unclear" / 어휘 밖 토큰은 표 아님 / `vocab=MARK_VOCAB` 이면 "no_mark" 도 표 — 스크립트 test_audit_card_photos_script 와 동일 의미
    - anchor_verdict: ("knee", {"knee","thigh","foot"})→"ok" / ("elbow", 같은 집합)→"mismatch" / ("unclear"|"error"|None|"zzz", 집합)→"unreadable" / (아무거나, frozenset())→"no_expectation"; `ANCHOR_VERDICTS == ("ok","mismatch","unreadable","no_expectation")`
    card_gates 상수: `ANCHOR_CHECK_CROP_FRAC == 0.18`, `ANCHOR_CHECK_MAX_RETRY == 2`, `ANCHOR_CHECK_ROUNDS == 3`; `inspect.signature(cg.part_crop).parameters["frac"].default is cg.ANCHOR_CHECK_CROP_FRAC`; `anchor_retry_frames` 의 `radius` 기본값 `is fz._MOMENT_ANCHOR_RADIUS_WIDE`, `limit` 기본값 `is cg.ANCHOR_CHECK_MAX_RETRY`
    - part_crop: 200x300(H x W) 프레임, 좌표 (150,100) → PIL RGB 정사각, 한 변 == round(200*0.18)=36, 픽셀이 원본 해당 영역과 **완전 동일**(np.array_equal — 마킹 0); 좌표 (0,0)·(299,199) 는 경계 클램프로 변이 줄지 않는다; 아주 작은 프레임(10x10)도 예외 없이 변 >= 1
    - eye_part_token (monkeypatch `cg.eye_judge` 를 대본으로 — `_scripted_eye_judge` 방식, 각 호출의 claim=="part"·expected_limb None·joint_kind None 을 박제):
      ["knee","knee"]→observed "knee", calls 2 (조기 종료: 2표면 3회 최빈과 결과 동일) / ["knee","elbow","knee"]→"knee", 3 / ["knee","elbow","thigh"]→"unclear", 3 / ["unclear","unclear","unclear"]→"unclear", 3 (못 읽음은 표가 아니라 조기 종료 안 됨) / ["error","knee","knee"]→"knee", 3 / rounds=2 → ValueError / 반환에 `tokens`(대본 그대로)·`calls`·`reason`(최빈 첫 응답)
    - anchor_retry_frames(`_mk` fixture): kinds ["o","o","x","x","x","o","o"] conf 0.9, anchor 3 → [1, 5] (d=2 에서 −d 먼저; 앵커 3±1 붕괴 제외) / kinds 전부 "o", anchor 3 → [2, 4] (앵커 자신 제외, limit 2 로 잘림 — 후보 5+ 있어도 2) / anchor 0 → [1, 2] (범위 밖 스킵) / 전부 "x" → [] / 그릴 수 없는 프레임(conf 0.3)은 제외 (_frame_usable 과 같은 판정)
    - verify_anchor_side (ask 는 dict 대본 `{frame: token}`·호출 횟수 기록, 반환 {"observed": tok, "calls": 1}; 대본에 없는 프레임은 None):
      · 좌표가 맞으면 통과: anchor 30 → "knee", expected {knee,thigh,foot} → action "pass", frame_idx 30, trail ((30,"knee","ok"),), eye_calls 1, ask 1회
      · 틀리면 재확인 후 이동: 30→"elbow", 29→"knee", retry [29,31] → "moved", frame_idx 29, ask 2회(31 은 안 물음)
      · 그래도 실패면 생략: 30→"elbow", 29→"hand", 31→"elbow" → "suppressed", frame_idx **30**(사진은 앵커 프레임 그대로), ask 3회
      · 재확인 상한: retry [29,31,28,32,27] 전부 "elbow" → ask 정확히 3회(앵커+2), "suppressed"
      · expected 빈 집합 → "unbound", ask 0회 / ask(anchor) 가 None(좌표·프레임·키 부재) → "unbound", 0회
      · 앵커 "unclear" → "pass"(눈이 못 본 것은 틀린 게 아니다), trail 에 (30,"unclear","unreadable") / 앵커 mismatch·후보 29 "unclear"·후보 31 "knee" → "moved" 31
      · 후보 ask None(좌표 없음) 은 호출 0 으로 스킵하되 상한을 소모한다: retry [29,31,28], 29→None, 31→"elbow", 28→"knee" 여도 → "suppressed" (28 은 안 물음)
      · 사진 장수 불변: `cg.ANCHOR_ACTIONS == ("pass","moved","suppressed","unbound")`, `AnchorCheckOutcome` 필드 == {action, frame_idx, trail, eye_calls} (emit/drop 류 필드 없음)
    스크립트: `test_audit_card_photos_script.py` 기존 테스트가 그대로 통과(alias 후에도 `acp.modal_token` 유지).
  </behavior>
  <action>
    **card_photo_audit.py** (numpy 0 유지, card_gates import 금지 — 순환 방지):
    1) `modal_token(observed, vocab=PART_VOCAB) -> str` 를 `_readable` 근처에 추가 — 본문은 `backend/scripts/audit_card_photos.py:136-150` 과 **동일 의미**(vocab 안 토큰만 표, 최빈, 동률·표 0 = "unclear"). docstring 에 "스크립트에서 이관(quick-260906-j8g) — 파이프라인 앵커 확인과 감사 스크립트가 같은 최빈 규칙을 쓴다".
    2) `ANCHOR_VERDICTS = ("ok", "mismatch", "unreadable", "no_expectation")` + `anchor_verdict(observed, expected) -> str`: expected 비면 "no_expectation"; `_readable(observed)` 아니면 "unreadable"; ∈ expected 면 "ok" 아니면 "mismatch". docstring(한국어): 왜 unreadable 이 불일치가 아닌지(눈이 못 본 것은 틀린 게 아니다 — card_gates.eye_mismatch 의미론), 왜 판정만 하고 처분(이동/생략)은 안 하는지(순수·비용 결정은 호출측). `__all__` 에 둘 추가.
    3) 모듈 docstring 끝에 절 추가: "앵커 확인 (quick-260906-j8g)" — 09-06 36패널 실측 요지 3줄 + "이 모듈은 판정, card_gates 가 눈·프레임, pipeline 이 처분".

    **card_gates.py** — `machine_eye` 뒤·"카드 결정" 절 앞에 새 절 `# ── 앵커 부위 확인 (quick-260906-j8g) ──`:
    4) 상수 3개, 각각 한국어 주석으로 **근거 박제**(재튜닝 금지 문구 포함):
       `ANCHOR_CHECK_CROP_FRAC = 0.18` — "2026-09-06 36패널 측정에 쓴 값 그대로(좌표 OK 15/틀림 14/못읽음 7). 카드 크롭 ≈0.42 는 몸 절반이 들어와 틀린 좌표를 가린다(클라임 오른무릎 audit ok·좌표는 팔). 새 튜닝값 아님".
       `ANCHOR_CHECK_MAX_RETRY = 2` — "창 안 재확인 프레임 상한 = 비용 상한(무한 재시도 금지). 후보는 fault_zoom._frame_usable 건전 프레임뿐".
       `ANCHOR_CHECK_ROUNDS = 3` — "측정 프로토콜 '3회 최빈' 그대로. 홀수(동률 방지, eye_judge_majority 관례)".
    5) `part_crop(frame_rgb: np.ndarray, joint_xy_px, *, frac: float = ANCHOR_CHECK_CROP_FRAC)` → PIL RGB. side = `max(1, int(round(min(H, W) * frac)))` 를 `min(side, H, W)` 로 클램프, x0/y0 클램프는 `mark_crop` 과 같은 식, **그리지 않는다**. docstring: "링을 그리면 눈이 링을 보고 답할 수 있다 — 측정 계측기는 마킹 없는 크롭으로 쟀다. mark_crop 무접촉(운영 게이트 크롭)".
    6) `eye_part_token(crop, *, api_key, model=DEFAULT_C_MODEL, rounds=ANCHOR_CHECK_ROUNDS, timeout_s=60.0) -> dict`: rounds 가 양의 홀수 아니면 ValueError. 루프 i<rounds: `eye_judge(crop, "part", api_key=api_key, model=model, timeout_s=timeout_s)` 호출(expected_limb/joint_kind 넘기지 않는다 — 기대가 새면 감사 전제가 깨진다, `_claim_question` part 분기 참조), tokens 에 observed 추가; **조기 종료**: vocab(`card_photo_audit.PART_VOCAB`) 안 토큰 중 어느 것의 표가 `rounds // 2 + 1` 에 이르면 중단(3회 최빈과 결과 동일 — 나머지 표가 뒤집을 수 없다). 반환 `{"observed": card_photo_audit.modal_token(tokens), "tokens": tokens, "calls": len(tokens), "reason": 최빈 토큰 첫 응답 reason 또는 ""}`. import 는 모듈 상단 `from . import card_photo_audit as cpa`(cpa 는 numpy·card_gates 를 안 끌어와 순환 0).
    7) `anchor_retry_frames(report, anchor_idx, members, *, n_frames, frames_fps=9.0, radius=fz._MOMENT_ANCHOR_RADIUS_WIDE, limit=ANCHOR_CHECK_MAX_RETRY) -> list[int]`: `nearest_usable_frame` 과 같은 순회(d=1..radius, `(anchor-d, anchor+d)` 순 — −d 먼저, 결정론), `0 <= cand < n_frames`, `fz._frame_usable(rep, cand, members, frames_fps, rep_fps, rep_frames)`(rep_fps/rep_frames 는 select_confident_frame 관례 `float(rep.get("fps") or frames_fps)` / `int(rep.get("frames") or 0)`) 인 것만, `len == limit` 이면 중단. 앵커 자신은 제외. docstring: "f4j 이동은 붕괴만 트리거·목적지 1개; 이건 눈 실패 트리거·후보 목록. 판정 함수는 같은 `_frame_usable` 한 개(중복 판정 금지)".
    8) `ANCHOR_ACTIONS = ("pass", "moved", "suppressed", "unbound")`, `@dataclass(frozen=True) class AnchorCheckOutcome: action: str; frame_idx: int; trail: tuple[tuple[int, str, str], ...]; eye_calls: int`.
       `verify_anchor_side(*, anchor_idx: int, expected, retry_frames, ask) -> AnchorCheckOutcome` — 순수 상태기계, 눈·프레임·좌표는 전부 `ask(frame_idx) -> {"observed": str, "calls": int} | None` 주입(None = 그 프레임 판정 불가: 좌표/프레임/키 부재):
         a. `exp = frozenset(expected or ())`; 비면 `("unbound", anchor_idx, (), 0)`.
         b. 앵커: `r = ask(anchor_idx)`; None → "unbound". verdict = `cpa.anchor_verdict(r["observed"], exp)`; trail 에 (anchor_idx, observed, verdict). "ok"|"unreadable" → "pass"(앵커 프레임). "mismatch" → c.
         c. 후보: `for cand in list(retry_frames)[:ANCHOR_CHECK_MAX_RETRY]:`(상한은 **함수가 소유** — 호출측이 더 줘도 2) `r = ask(cand)`; None → 스킵(호출 0, 상한 소모); verdict "ok" → `("moved", cand, trail, calls)`; 그 외 → 다음.
         d. 소진 → `("suppressed", anchor_idx, trail, calls)` — frame_idx 는 앵커(사진은 그 멈춤 그대로, 표시만 생략).
       docstring(한국어): belle 09-03 규칙 1 인용, "이 함수는 카드를 없앨 수 없다 — action 4종 어느 것도 방출을 바꾸지 않는다", 비용 상한 산식(호출측 라운드 r 일 때 측당 최대 (1+MAX_RETRY)·r).
    9) `backend/scripts/audit_card_photos.py`: `modal_token` 본문(136-150행)을 `modal_token = cpa.modal_token` 한 줄(주석: "정본은 card_photo_audit — quick-260906-j8g 이관")로 바꾼다. 시그니처·기본 vocab 동일이라 `judge_panel`·기존 테스트 무변경.
    10) 테스트 파일을 `<behavior>` 대로 작성. `verify_anchor_side` 테스트의 `ask` 는 `calls: list[int]` 에 프레임을 append 하는 클로저로 호출 횟수를 센다. 눈 네트워크 0: `eye_part_token` 은 `monkeypatch.setattr(cg, "eye_judge", fake)`.
    커밋: `feat(j8g): 앵커 부위 확인 순수 층 — 좁은 크롭·토큰 최빈·재확인 상태기계 (36패널 실측 근거)`.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && .venv/bin/python -m pytest -q tests/test_anchor_part_check.py tests/test_card_photo_audit.py tests/test_card_gates.py tests/test_audit_card_photos_script.py && grep -v '^\s*#' shared/python/sunity_shared/analysis/card_gates.py | grep -c "^ANCHOR_CHECK_CROP_FRAC = 0.18" | xargs test 1 -eq && grep -v '^\s*#' shared/python/sunity_shared/analysis/card_gates.py | grep -c "^ANCHOR_CHECK_MAX_RETRY = 2" | xargs test 1 -eq && grep -v '^\s*#' scripts/audit_card_photos.py | grep -c "^def modal_token" | xargs test 0 -eq && grep -v '^\s*#' shared/python/sunity_shared/analysis/card_photo_audit.py | grep -c "^import numpy\|^from numpy\|card_gates" | xargs test 0 -eq</automated>
  </verify>
  <done>
    `cpa.anchor_verdict`/`cpa.modal_token`(numpy 0), `cg.part_crop`(무마킹, 0.18)·`cg.eye_part_token`(조기 종료 최빈)·`cg.anchor_retry_frames`(≤2, `_frame_usable` 공유)·`cg.verify_anchor_side`(pass/moved/suppressed/unbound) 가 `<behavior>` 전 항목을 통과한다. 새 튜닝값 = 측정값 0.18 하나(근거 주석), 모델 문자열 하드코딩 0. 스크립트의 `modal_token` 중복 정의 0. 4개 테스트 파일 전부 pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: gated 호출부 배선 — 양측 앵커 확인·이동/생략·캐시·로그 2종 + 배선 source 단언 + f4j 슬라이스 조정 + 전체 게이트</name>
  <files>backend/functions/pipeline/app.py, backend/tests/test_anchor_part_check.py, backend/tests/test_gated_frame_skips_collapse.py</files>
  <action>
    `_run_gated_card_inherit` 만 건드린다. import 블록(4899행 근방 `from sunity_shared.analysis import card_gates as cg` 옆)에 `from sunity_shared.analysis import card_photo_audit as cpa` 추가.
    1) 카운터: `eye_calls = 0` (4968행) 옆에 `anchor_stats = {"sides": 0, "eye_calls": 0, "pass": 0, "moved": 0, "suppressed": 0, "unbound": 0, "recheck": 0}` 와 `_anchor_cache: dict[tuple, str] = {}` 를 둔다(캐시 키 = (측, 9fps 프레임, 관절 라벨, round(x,4), round(y,4)) — 같은 프레임·관절이라도 좌표 출처(display_anchor=align vs rep12)가 다르면 다른 질문이라 좌표를 키에 넣는다).
    2) unit 루프 안, f4j 블록 끝(5385행 `r9 = r9_new`) **직후**·`comps = _fz.build_fault_zoom_comparisons(` **직전**에 마커 주석 `# quick-260906-j8g — 앵커 부위 확인 (양측). 사진은 남기고 표시만 정한다 (belle 09-03 규칙 1).` 로 시작하는 블록을 넣는다. 얇게 — 판정·상한·상태기계는 전부 Task 1 순수 함수. 주석(한국어)에 09-06 실측 요지(36패널 OK 15/틀림 14/못읽음 7, conf·붕괴가 못 거른다, f4j 4→4)와 "기존 _eye_check 는 학생만·claim bent/extended 라 이 결함을 못 잡는다 — 별개 확인, 그 게이트 무접촉"을 적는다.
       지역 정의:
       - `_is_angle = crit.startswith(_fz.ANGLE_VS_REFERENCE_PREFIX)`; `_check_joint = cg.crit_joint(crit.split("__")[-1]) if _is_angle else crit` (로그·캐시·원장 라벨. 좌우 이름은 로그에만 — 눈 질문에는 안 간다).
       - `_expected = cpa.expected_parts(crit, _members[0] if len(_members) == 1 else None)`.
       - `_frames9 = {"user": user_frames, "ref": ref_frames}`, `_reps = {"user": user_report, "ref": ref_report}`, `_moved = {"user": False, "ref": False}`, `_used_da = {"user": False, "ref": False}`,
         `_mid = profile.motion_id if isinstance(getattr(profile, "motion_id", None), str) else None`.
       - `_rep_idx(side, idx9)`: `_fz._to_rep_idx(idx9, 9.0, float(rep.get("fps") or 9.0), int(rep.get("frames") or 0))`  # noqa: SLF001 (align_bake 블록의 `_fz._kp_conf` 선례).
       - `_video_idx(side, idx9)`: user → `max(0, min(idx9, n-1))`; ref → `_fz.ref_display_frame_index(idx9, n, int(ref_report.get("frames") or 0), float(ref_report.get("fps") or 9.0), 9.0)` — fault_zoom 3448-3457행과 같은 프레임.
       - `_xy_for_check(side, idx9) -> xy|None` — **그릴 좌표의 우선순위를 fault_zoom(3474-3505행) 그대로** 미러: ① `not _moved[side] and display_anchor is not None` → `_used_da[side] = True; return display_anchor[side]`; ② `_fz.criterion_vertex_xy(crit, _members, rep, ri, deficits, _fz._gated_kp if side == "user" else _fz.make_reference_anchor_resolver(_mid, crit))`; ③ `valid = _fz._member_pts(rep, ri, _members)[0]`, 비고 `not _moved[side]` 면 `valid = [(m, xy) for m, xy in align_bake[side].items() if m in _members]`(seam 2 주입 미러); valid 있으면 `_fz._anchor_xy(valid, deficits)`; ④ None(좌표가 없으면 fault_zoom 도 relaxed/전신 → 표시 0 — 검사할 표시가 없다 = 비구속).
       - `_ask_part(side, idx9) -> dict|None`: xy 없음 → None; `frame = _fz.native_or_downscaled(native_at, side, _video_idx(side, idx9), _frames9[side])`(카드와 **같은 픽셀 원천**); `H, W = frame.shape[:2]`; 캐시 적중 → `{"observed": cached, "calls": 0}`; `not api_key` → None(기존 no_api_key 비차단 관례); `crop = cg.part_crop(frame, (xy[0]*W, xy[1]*H))`; `res = cg.eye_part_token(crop, api_key=api_key)`; `eye_calls += res["calls"]`(nonlocal), `anchor_stats["eye_calls"] += res["calls"]`; 캐시 저장; `eye_ledger.append({"side": side, "joint": _check_joint, "frameIdx": int(idx9), "frameSpace": "video9", "sec": round(idx9 / eff[side], 3), "claim": "part", "observed": res["observed"], "expected": sorted(_expected), "rounds": res["calls"], "reason": res["reason"], "png": crop PNG bytes})`(기존 원장 업로드 블록이 `side`/`joint`/`png` 키만 요구 — additive, 실패는 try 로 비차단); return `{"observed": res["observed"], "calls": res["calls"]}`.
       확인 루프 `for _side in ("user", "ref")`: 이미 suppress 에 든 측은 건너뛴다(표시가 이미 생략된 측 — 검사할 표시가 없다, 호출 0). 건너뛰기는 `if _side not in suppress:` 블록으로 감싸 구현하고 **`continue` 토큰은 쓰지 않는다** — 아래 source 단언이 이 블록 안 `continue` 0 으로 "unit 을 버릴 수 없다"를 증명한다. 블록 안: `idx9 = u9 if _side == "user" else r9`; `retry = cg.anchor_retry_frames(_reps[_side], idx9, _members, n_frames=int(_frames9[_side].shape[0]))`; `out = cg.verify_anchor_side(anchor_idx=idx9, expected=_expected, retry_frames=retry, ask=(lambda i, s=_side: _ask_part(s, i)))  # 새 import 0 — app.py 에 functools 없음, 루프 변수는 기본 인자로 고정`; `anchor_stats["sides"] += 1; anchor_stats[out.action] += 1`.
         · "moved": `u9 = out.frame_idx` 또는 `r9 = out.frame_idx`; `_moved[_side] = True`; `display_anchor = None`(f4j 와 같은 이유 — both-or-neither, 옮긴 프레임에 옛 순간 좌표를 찍지 않는다); `align_bake[_side] = {}`.
         · "suppressed": `suppress.add(_side)` — 기존 suppress_marks 경로가 그 측 표시를 생략하고 userMarked/refMarked 가 False 를 말한다. 새 필드 0.
         · 로그 1행(항상): `log.info("fault_zoom_anchor_check analysis_id=%s rid=%s side=%s joint=%s expected=%s action=%s frame=%d->%d trail=%s eye_calls=%d", analysis_id, rid, _side, _check_joint, "|".join(sorted(_expected)) or "-", out.action, idx9, out.frame_idx, ",".join(f"{f}:{o}/{v}" for f, o, v in out.trail) or "-", out.eye_calls)` (rid = `str(rec.get("recordId") or "").split(":")[0]` 인접 관례).
       교차 재확인(1회, 재확인 프레임 0): 루프 뒤 `for _side in ("user","ref")`: `_used_da[_side] and display_anchor is None and not _moved[_side] and _side not in suppress` 이면 — 그 측은 display_anchor(align) 좌표로 통과했는데 다른 측 이동으로 display_anchor 가 떨어져 이제 rep12 좌표로 그려진다 — `out2 = cg.verify_anchor_side(anchor_idx=idx9, expected=_expected, retry_frames=[], ask=…)`; `anchor_stats["recheck"] += 1`; "suppressed" 면 `suppress.add(_side)`; 같은 형식 로그에 `action=recheck:{out2.action}`.
    3) 문서당 요약 로그 — 5475행 `"card_gates 대체 부착 완료 …"` **직전**에 별도 1행: `log.info("fault_zoom_anchor_check_summary analysis_id=%s cards=%d sides=%d eye_calls=%d pass=%d moved=%d suppressed=%d unbound=%d recheck=%d", analysis_id, len(gated_raw), …)`. 기존 verdict 로그(5147행)·부착 완료 로그 문자열은 **무변경**.
    4) `test_gated_frame_skips_collapse.py::test_moved_side_drops_freeze_moment_payload` 의 슬라이스 끝을 `body.index("comps = _fz.build_fault_zoom_comparisons(")` → `body.index("r9 = r9_new") + len("r9 = r9_new")` 로 좁힌다(단언 의도 "f4j 이동 블록은 suppress 무접촉" 유지 — 새 블록은 f4j 블록 밖). docstring 에 한 줄 이유(quick-260906-j8g 블록이 그 뒤에 suppress 를 쓴다). 나머지 f4j 단언(`_fz.nearest_usable_frame(` == 2, app.py 전체 비주석 `nearest_usable_frame(` == 2)이 깨지지 않도록 **새 블록의 코드 줄에 `nearest_usable_frame(` 문자열을 쓰지 않는다**(주석은 무관).
    5) `test_anchor_part_check.py` 에 배선 source 단언 추가(`_gated_body()` 를 복사):
       - `body.count("cg.verify_anchor_side(") == 2`(본확인 + 교차 재확인), `body.count("cg.anchor_retry_frames(") == 1`, `body.count("cg.part_crop(") == 1`, `body.count("cg.eye_part_token(") == 1`, `body.count("cpa.expected_parts(") == 1`.
       - 순서: `body.index("r9 = r9_new") < body.index("cg.verify_anchor_side(") < body.index("comps = _fz.build_fault_zoom_comparisons(")`.
       - block = `body[body.index("r9 = r9_new"):body.index("comps = _fz.build_fault_zoom_comparisons(")]`: `"suppress.add(" in block`(표시 생략 = 기존 경로), `"display_anchor = None" in block`, `"continue" not in block` 과 `"gated_raw" not in block` 과 `"units.remove" not in block`(사진 장수 불변 — 이 블록은 unit 을 못 버린다), `"suppress_marks=frozenset(suppress)" in body`(호출 형태 불변), `"user_frame_idx=u9" in body and "ref_frame_idx=r9" in body`.
       - 무접촉: `_eye_check` 본문(`body[body.index("def _eye_check("):body.index("def _pair_at(")]`)에 `"part"`·`anchor`·`part_crop` 없음, `body.count("cg.machine_eye(") == 1`. app.py 전체 비주석 `verify_anchor_side(` == 2 이고 둘 다 `_gated_body()` 안(stage-1 3488·advisory 3531 무접촉).
       - 로그 형식 lockstep: `"fault_zoom_anchor_check analysis_id=%s rid=%s side=%s joint=%s expected=%s action=%s frame=%d->%d trail=%s eye_calls=%d"` 와 `"fault_zoom_anchor_check_summary analysis_id=%s cards=%d sides=%d eye_calls=%d"` 가 app.py 에 있음(다음 Pod 때 이 문자열로 회수).
       - 부착 완료 로그 불변: `"card_gates 대체 부착 완료 analysis_id=%s expected_units=%d emitted=%d"` 가 app.py 에 그대로 있음.
    6) 전체 게이트 rtk 없이: `cd backend && .venv/bin/python -m pytest -q` → failed 0, skipped 20, passed ≥ 4669 + 신규.
    커밋: `feat(j8g): gated 카드 양측 앵커 부위 확인 배선 — 실패 시 창 안 2프레임 재확인 후 표시만 생략, 로그 2종`.
    SUMMARY 에 반드시 적을 것(belle 보고 수치): (a) 눈 호출 산식 — 측당 통상 2회(2표 조기 종료)·카드당 통상 4회, 최악 측당 (1+2)×3=9·교차 재확인 +3 → 카드당 최대 21회, 문서당(카드 ≤5) 통상 ≈20회·최악 ≈105회; 지연은 호출당 실측 없음 → "미실측, 다음 Pod 에서 `fault_zoom_anchor_check` 의 eye_calls 와 타임스탬프로 잰다"라고 정직하게; (b) stage-1(3488)·advisory(3531) 호출부를 이번에 안 건드린 이유 — gated 경로가 belle 이 보는 감점 카드이고 f4j 실측에서 붕괴·이동은 전부 gated 였다, 나머지는 다음 단위; (c) 측정 스크립트가 리포에 없고(scratchpad 휘발) 크롭의 프레임 원천(원본/축소본)이 미기록이라 이번 구현은 "카드와 같은 픽셀 원천(native_or_downscaled)"으로 정했다 — 축소본 폴백(640px, 크롭 ≈65px)에서는 못 읽음이 늘 수 있고 그것은 비구속; (d) 라이브 검증은 이 quick 밖 — 다음 Pod 때 09-03 6영상 재분석 → `fault_zoom_anchor_check`/`_summary` 로그 회수 + `backend/scripts/audit_card_photos.py --rounds 5` 재감사(감점 카드 불일치 4장이 줄어드는지, 신규 불일치 0); (e) 모델 문자열은 DEFAULT_C_MODEL(런타임 config) — 측정과 다를 수 있음을 명시.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion/backend && .venv/bin/python -m py_compile functions/pipeline/app.py && grep -v '^\s*#' functions/pipeline/app.py | grep -c "verify_anchor_side(" | xargs test 2 -eq && grep -v '^\s*#' functions/pipeline/app.py | grep -c "nearest_usable_frame(" | xargs test 2 -eq && grep -c "card_gates 대체 부착 완료 analysis_id=%s expected_units=%d emitted=%d" functions/pipeline/app.py | xargs test 1 -eq && .venv/bin/python -m pytest -q tests/test_anchor_part_check.py tests/test_gated_frame_skips_collapse.py && .venv/bin/python -m pytest -q 2>&1 | tail -3</automated>
  </verify>
  <done>
    app.py 의 `cg.verify_anchor_side(` 호출이 정확히 2곳(본확인·교차 재확인), 둘 다 `_run_gated_card_inherit` unit 루프 안 `r9 = r9_new` 뒤·`build_fault_zoom_comparisons` 앞. 실패 측은 `suppress` 로만 처리(새 필드 0, unit 제거 0), 이동 측은 `display_anchor=None`+`align_bake[side]={}` 로 좌표와 프레임이 같은 순간. 카드당 측별 `fault_zoom_anchor_check` 1행 + 문서당 `_summary` 1행. `_eye_check`·hold·pair·stage-1·advisory·채점·방출 필드·부착 완료 로그 문자열 무접촉. 전체 pytest failed 0, skipped 20, passed ≥ 4669 + 신규.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 파이프라인 → Gemini(generateContent) | 좁은 크롭 JPEG 만 전송 — 기존 기계 눈(T-kpo-01)과 같은 경계, 새 데이터 종류 0 |
| Firestore/S3 리포트 → 순수 함수 | 서버가 쓴 keypointReport·align 만 읽음(클라이언트 입력 아님) |
| 표시 렌더 → 앱 | 방출 필드 무변경 — 표시 생략은 기존 userMarked/refMarked 로만 드러남 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-j8g-01 | Information disclosure | `_ask_part` → Gemini | accept | 크롭 픽셀만 전송(기존 eye 와 동일 범위), 개인정보 필드 0, 원장은 기존 S3 eye/ 키 규칙 하위(T-kpo-02 보존) |
| T-j8g-02 | DoS (비용/지연) | 눈 호출 루프 | mitigate | 상한을 순수 함수가 소유: `ANCHOR_CHECK_MAX_RETRY=2`·`ROUNDS=3`·조기 종료·(측,프레임,관절,좌표) 캐시·이미 suppress 된 측 0회 — 테스트가 "후보 5개 줘도 3회"를 고정. 카드당 ≤21회 산식을 SUMMARY 에 박제 |
| T-j8g-03 | Tampering | 판정 입력(keypointReport/align) | accept | 서버측 생성 데이터. 좌표 부재·legacy 는 비구속(unbound)으로 fail-open — 카드는 항상 남는다 |
| T-j8g-04 | Repudiation | 판정 흔적 | mitigate | 카드당 측별 로그 1행(trail 포함) + 문서당 요약 1행 + S3 eye 원장(claim=part, expected 동반) — 침묵하면 사후 판별 불가(이번 조사의 교훈) |
| T-j8g-SC | Tampering | npm/pip 설치 | n/a | 패키지 설치 0 — 순수 Python 변경만 |
</threat_model>

<verification>
- Task 1: 4개 테스트 파일 pass + grep 게이트(상수 0.18·2 정의 각 1, 스크립트 `def modal_token` 0, card_photo_audit 에 numpy/card_gates import 0).
- Task 2: `py_compile`, app.py 비주석 `verify_anchor_side(` == 2 / `nearest_usable_frame(` == 2(f4j 불변), 부착 완료 로그 문자열 1, source 단언 테스트 pass.
- 전체: `cd backend && .venv/bin/python -m pytest -q` → failed 0, skipped 20, passed ≥ 4669 + 신규 (rtk 래퍼 없이).
- 무접촉 확인: `rtk git diff --stat` 에 파일 6개만(card_photo_audit / card_gates / audit_card_photos / app.py / 새 테스트 / test_gated_frame_skips_collapse). app.py diff 가 `_run_gated_card_inherit` 밖(3488 stage-1·3531 advisory·`_eye_check`·hold/pair)을 건드리지 않음. `_ARMPIT_T`·`ANGLE_BAKE_MAP`·fault_zoom.py 무변경.
- 라이브 실증(이 quick 범위 밖 — SUMMARY 에 "미검증" 박제): 다음 Pod 세션에서 09-03 6영상 재분석 → `fault_zoom_anchor_check`/`fault_zoom_anchor_check_summary` 로그 회수(호출 수·지연 실측) + `audit_card_photos.py --rounds 5` 재감사(감점 카드 불일치 4장 감소 여부, 신규 불일치 0). Pod 는 시연 때만(belle 08-28) — 이 quick 에서 띄우지 않는다.
</verification>

<success_criteria>
- gated 카드마다 양측 앵커가 좁은 무마킹 크롭(0.18)으로 확인되고, 틀리면 건전 후보 ≤2 프레임 재확인 → 이동, 그래도 틀리면 표시만 생략(사진·멈춤 불변). 못 읽음·expected 없음·좌표/프레임/키 부재는 비구속.
- 눈 호출은 캐시·조기 종료·상한으로 bounded 이고 eye_calls 에 합산되며, 로그 2종으로 사후 판별 가능.
- 채점·Firestore 방출 필드·기존 _eye_check/hold/pair·stage-1·advisory·어깨 문법·fault_zoom.py 무접촉. 순수 로직은 shared 모듈에, app.py 배선은 얇다.
- 새 튜닝값 = 측정값 0.18 하나(근거 주석), 모델 문자열 하드코딩 0, 이모지 0.
- backend pytest failed 0, passed ≥ 4669 + 신규, skipped 20.
</success_criteria>

<output>
Create `.planning/quick/260906-j8g-anchor-part-verification-gate/260906-j8g-SUMMARY.md` when done — Task 2 action 의 SUMMARY 항목 (a)~(e)(호출 수 산식·미실측 지연·stage-1/advisory 보류 이유·프레임 원천 결정·다음 Pod 실증 절차·모델 문자열 주의) 를 반드시 포함.
</output>
