---
quick_id: 260906-n2j
slug: stage-1-advisory
date: 2026-09-06
status: complete
---

# stage-1·advisory 확대 카드에도 앵커 부위 확인 게이트 — 완료

## 한 일

카드를 만드는 세 경로 중 게이트가 하나(gated)에만 있었다. 나머지 둘에 같은 게이트를 걸었다.

| 커밋 | 내용 |
|---|---|
| `7e4322b3` | `fault_zoom.build_fault_zoom_comparisons` 에 카드별 `anchor_check` 콜백 (테스트 7) |
| `105fa15e` | `_make_card_anchor_check` 공용 팩토리 + stage-1·advisory 두 호출부 배선 (테스트 8, 범위 가드 갱신) |

- 콜백이 받는 크롭 중심 = `_side_crop` 에 실제로 넘긴 값(vertex 우선, 없으면 anchor).
  게이트가 검사하는 자리와 카드가 보여주는 자리를 **하나로 묶는 단일 출처**.
- 실패 측은 기존 `suppress_marks` 경로로 **표시만 생략** — 사진 장수·프레임 선택·크롭 폭·
  채점 무접촉 (belle 09-03 규칙 1).
- 콜백 예외·키 부재·전신 폴백·기대 부위 파생 불가 = 호출 0, 표시 유지 (fail-open).
- 재확인 프레임 미사용 — 09-06 라이브 `moved=0/15`.

## 왜 (측정)

라이브 j8g 4문서 28패널을 **배달된 카드로** 다시 쟀다 (`evidence/GATE-VERDICT.md`):

- 중심이 딴 부위인 패널 **9/28**
- 그중 게이트가 잡은 것 3 · 게이트가 놓친 것 3 · **게이트가 없던 경로 3**
- 게이트가 표시를 생략한 3면은 전부 실제 불일치 — **오판 0**

마지막 줄이 이 수리를 정당화한다: 실패 방향이 안전하고(맞는 표시를 안 지운다), 못 잡은
것의 절반이 단순히 게이트가 닿지 않는 경로였다.

## 남은 것

- 게이트가 통과시킨 불일치 3 — 원본 프레임에선 허용 부위, 배달 패널에선 아님. 다음 Pod
  라이브에서 배달 패널로 재측정하는 것이 판정법. 원인 단정 금지.
- 감사(`card_photo_audit`)는 "틀린 표시"와 "틀린 사진"을 구분하지 않아 이 수리를 재는
  계기가 아니다. 열 분리는 별개 단위.
- 배포 안 됨 — 다음 Pod 기동 때 번들에 실려 라이브 검증.

## 게이트

- 백엔드 `4717 passed / 0 failed / 20 skipped` (기준선 4701 → +16)
