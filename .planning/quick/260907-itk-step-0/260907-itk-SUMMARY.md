---
id: 260907-itk
slug: step-0
description: Step 0 관절 좌표 학습 필요 판정 — 사전 기준 박제 후 두 측정
date: 2026-09-07
status: complete
commits:
  - bdaebd1c  사전 기준 박제 + 명부 정정 (불일치 8 → 5)
  - 3798bf0f  게이트 확인분 실측 — 게이트는 사진을 못 없앤다
  - 653dd7b7  Step 0 판정 — 좌표 학습 시작하지 않는다
---

# Step 0 요약

**판정: 관절 좌표 학습을 시작하지 않는다.** 근거는 `evidence/STEP0-VERDICT.md`.

## 들어간 것

| | 산출물 |
|---|---|
| 사전 기준 | `evidence/STEP0-PREREGISTRATION.md` (커밋 `bdaebd1c`, 측정 이전) |
| 명부 정정 | `evidence/fix_roster.py` → `roster_corrected.json` |
| 측정 A | `evidence/measure_a_mediapipe.py` → `.json` (18행) + 프레임 17장 |
| 측정 B | `evidence/measure_b_vote.py` → `.json` (18행) |
| 게이트 실측 | `evidence/GATE-COVERAGE.md` |
| 판정 | `evidence/STEP0-VERDICT.md` |

## 이 날 바뀐 사실 4개

1. **"불일치 8면"은 5면이었다.** 3면은 09-06 계기 자신의 크롭로그 조회 실패
   (advisory 카드가 묶음 region 으로 로깅되는데 관절 이름으로 찾았다).
   내가 먼저 잡아 정정했다. 눈에게 재질의 없음.

2. **게이트는 사진을 못 없앤다** — 설계상 그렇다(`card_gates.py:972`, belle 09-03 규칙).
   승인된 방향 1번("자동 사진은 게이트 확인분만")을 인계서 문구대로 구현하면
   belle 09-03 규칙을 깬다. **belle 결정 필요.**
   덤: 사진 0장 되는 동작 없음(최소 1카드). 그리고 방향 1번은 불일치 5면 중 3면을 통과시킨다.

3. **좌표가 틀렸다는 근거가 안 나왔다.** MediaPipe 가 대상 5면 전부에서 우리 좌표와
   임계 안(1.17~12.5 px vs 36~49.5 px)에 있다. 다중 프레임 다수결도 5면 전부 HOLDS.

4. **★내 측정 B 는 빈 계기였다.** 대칭 창 중앙값은 쓸려가는 관절에서 구조적으로 중심을
   돌려준다 — 생존 표본이 임계의 1.2~2.1배로 흩어져 있는데도 이동량은 임계 밑.
   적대 검증이 잡았고, 직접 재서 확인했다. **교차표의 두 번째 다리로 쓸 수 없다.**

## 사전 예측 대 실제

- **A 적중** — "4면 이상 AGREE, DISAGREE 0" 예측 → 5/5. 박제한 표본 예측 5.5 px 이 5.54 px 로 재현.
- **B 빗나감** — 학생측 2면 MOVES 예측 → 0면. (그런데 §3 이 그 결과밖에 못 내는 계기였다.)

## 벌지 못한 것 (말하면 안 되는 것)

"좌표는 맞다" — 못 번다. 진짜 근거는 A 한 다리이고, 그 안에서 **오늘 파이프라인에 해당하는
것은 대상 2면**뿐이다(나머지 3면은 2026-06 기준 좌표라 학습으로 못 고친다).
심판(MediaPipe)도 오늘 파이프라인 대조군 7면 중 2면에서 우리와 다르다.

## 다음

`skel_overlay_measure.py` (이름표 골격 오버레이) — 좌표 오류 / 크롭 폭 / 눈 어휘를 가른다.
GPU 불필요. 같은 방식으로 사전 박제할 것.
