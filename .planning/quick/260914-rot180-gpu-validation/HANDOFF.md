---
title: 인수인계서 — 2026-09-14 세션
date: 2026-09-14
next_section: belle 판정 4건 + 가시성 조항(치명 트랙 유일 현실 후보)
status: 회전 GPU 실증 완료 · 기준 결함 2건 발견(1건 수리) · 폴 계기 유효범위 확정 · IPSF 원문 확보
---

# 인수인계서 — 2026-09-14

**§0 → §6 순서로 읽어라.** 관측과 진단을 구분해 적었다.
★ 오늘 **내가 틀렸다가 고친 것이 5건**이다 — §7 에 모아뒀다. 같은 함정을 밟지 말 것.

---

## 0. 30초 요약

- **회전(rot180)은 GPU 에서 재현됐다.** 붕괴 6→0, 뼈위반 −49%, 못 본 관절 1→0.
  로컬 CPU 예측과 1.3% 이내.
- **그런데 회전보다 큰 걸 찾았다 — 저장된 기준 모션이 허용오차(20°)의 최대 2.6배 틀려 있다.**
  정은지 자기비교가 회전을 켜자 **100점 → 60점**. 감점이 기준이 가장 틀린 관절에 정확히 떨어졌다.
  **지금 mode1 점수가 그럴듯한 건 학생과 기준을 같은 고장난 자로 재기 때문이다.**
  > ★ **2026-09-17 정정** — 이 문장은 **pdshape 1편**에서만 참이다. 전수 측정 결과 저장
  > 라이브러리는 오늘 파이프라인(회전 OFF)과 88쌍 중 허용오차 초과 **0개**로 일치하고,
  > 20° 초과는 회전을 켤 때만 9/88 생기며 **그중 5개가 pdshape 하나**다. 5편은 회전을
  > 켜도 전혀 안 변한다. 근거 = `.planning/quick/260917-hjy-reference-drift-all11/`
- ~~**기준 라이브러리 결함 2건** — (a) 26~52° 드리프트(미수리, 판정 필요)~~ **(a)는 2026-09-17 전수 측정으로 정정됨** — 그 드리프트는 pdshape 1편의 성질이고, 저장 라이브러리는 오늘 파이프라인(회전 OFF)과 88쌍 중 초과 0개로 일치한다. 재추출 대상은 11편이 아니라 **6편**. 근거 = `.planning/quick/260917-hjy-reference-drift-all11/`. (b) 4편 좌표계 불일치는 **수리 완료** 그대로.
- **폴을 자로 쓸 수 있다** — 1cm = 4.7px, 그 자로 잰 몸통이 해부학적으로 맞는다.
  유효 범위 = **세로 촬영 + 가로 1080px 이상**(= 우리 입력 분포).
- **IPSF Code of Points 2025-2027 원문을 확보**했다(`evidence/ipsf_cop_2025_2027_v2.txt`).
  belle 09-13 판정의 1차 출처 근거가 거기 있다.
- **운영 코드 무접촉.** 회전 플래그 여전히 0. 게이트 4827 passed / 0 failed.

---

## 1. 지금 상태 (실측)

```
브랜치   main   HEAD b5455136   (origin/main 보다 앞섬 — 미푸시)
게이트   pytest 4827 passed / 20 skipped / 0 failed   (backend/.venv 로 직접 실행)
플래그   ROT180_INVERSION_ENABLED=0   (start_server.sh:24, 리포 무접촉)
Pod      0대 · SSM pod-expected=down · 잔액 $6.06 (오늘 $0.85)
Firestore★ Blaze 전환됨 (belle, 오늘) — Spark 5만/일 캡 해소
```

★ **Firestore 는 오늘 Blaze 로 전환됐다.** 그 전에 내가 병렬 에이전트 전수 스캔으로
하루 읽기 한도를 소진시켜 **Pod 분석 1건이 죽고 앱이 몇 시간 멈췄다.**
메모리 `firestore-spark-50k-read-cap-is-a-live-risk` 참조 — **코퍼스를 여러 에이전트가
각자 스캔하게 하지 말 것.** 한 번 읽어 로컬 파일로 떨구고 그걸 읽게 한다.

---

## 2. 회전 GPU 실증 (근거 = `evidence/MEASUREMENTS.md`)

Pod 3회 기동(RTX PRO 4500 Blackwell, ORT-gpu 1.19.2 CUDA EP), 커밋 `e3936dfb` 번들 반입.

**잡음 바닥 = 0** — off 두 런의 `joints3d` 가 byte 동일. 다른 Pod·다른 시각 재실행도
불일치 계기가 `p50 0.109 / p90 1.608 / max 4.764 / torso 67.5` 로 완전히 같았다.

| | belle pdshape | 정은지 pdshape |
|---|---|---|
| 붕괴 프레임 | 6 → **0** | 2 → **0** |
| 뼈위반 p90 | 1.502 → **0.763** | 7.171 → **0.774** (−89%) |
| 못 본 관절 | 1 → **0** | 1 → 1 (관절만 바뀜) |
| **점수** | 60 → 60 (상한 포화) | **100 → 60** |

- 회전 2패스가 PR 워프보다 **싸다**(8.1s vs 10.9s). `pr_warp_skipped=true` 확인.
- 채택 182/182, fail-safe 기각 0.
- **렌더·확대카드·음성 정상 생성**(ON 런 산출물 15개). 아침의 "0개"는 내가 Pod 을 일찍 끈 탓.

## 3. ★ 기준 모션이 틀렸다 (근거 = `evidence/REFERENCE-IS-BROKEN.md`)

정은지 기준 영상을 학생으로 올려 자기 자신과 비교. 회전을 켜니 100 → 60.
기준 각도를 회전 켜고 재추출해 저장본과 대조:

| 관절 | 저장본−재추출 차 | 학생이 받은 감점 |
|---|---|---|
| left_elbow | **+0.1°** | **0** |
| right_elbow | **+52.3°** | **−20.0** |
| left_knee | **+44.5°** | **−14.1** |
| left_hip | **+43.2°** | **−15.0** |

**기준이 맞는 관절은 감점 0, 가장 틀린 세 관절이 감점 3건 전부다.** 인과가 닫혔다.
허용오차 20° 대비 8관절 중 5개가 26~52° 어긋나 있다.

곁가지: 저장본 237프레임(≈15fps) vs 현재 재추출 159프레임(≈10fps) — **회전 이전에
이미 드리프트가 있다.**

### 3-1. 기준 4편 좌표계 불일치 — **수리 완료** (커밋 `cdb40158`)

`ref-foxtop` / `ref-foxtop-split` / `ref-invert` / `ref-sideway-spin` 이 세로 좌표를
z 슬롯에 담고 있었다. **y 가 ~1e-13 (회전행렬 잔여값)인 것이 단서** — 그 4편은 정렬이
실제로 돌아간 편이고, 나머지 7편은 scipy 부재로 정렬이 실패해 원본 좌표가 남은 것이다.

이미지 좌표로 통일했다(7/11 → **11/11**). 몸통 14.9~20.6px → 55.9~59.5px.
**채점 영향 0** — y↔z 스왑은 평면 반사라 관절각이 불변이다(검증함).
수리 도구 = `backend/scripts/repair_reference_joints3d_axis.py` (멱등, 백업 후 적용).

## 4. 폴을 계기로 (근거 = `evidence/POLE-AS-INSTRUMENT.md`)

- **기존 `HoughPoleDetector` 는 롤을 못 잰다** — `VERTICAL_TOLERANCE_DEG=5.0` 이 수직 ±5°
  밖 선을 버려서, 10° 롤을 줘도 1° 만 움직이고 부호도 틀린다. 그런데 `detected` 를 보고한다.
  **`detected` 는 폴을 찾았다는 증거가 아니다.** 그립 프라이어도 없다(창틀·거울이 동등 채택).
- **prior 를 빼면 잡힌다** — 평균 오차 0.7°. 다만 **롤 보정의 값어치는 작다**(§7-2).
- ★ **스케일이 나온다** — 폴 폭 21.5px ÷ 45mm → 1cm = 4.7px → 몸통 47.4cm(해부학적으로 맞음).
- **유효 범위** (플라이휠 코퍼스 64편, 채널 32개):

  | 구분 | 표본 | 검출률 | 폭 퍼짐 |
  |---|---|---|---|
  | **세로 + 가로≥1080** | 6 | **83%** | **1.2배** |
  | 가로 촬영 | 50 | 38% | 27.5배 |

  **그 유효 범위가 곧 우리 입력 분포다**(업로드는 세로 고해상도). 단 조건 미달이면
  **측정을 내지 말고 기권해야 한다.**
- ★ **롤 보정 코드가 이미 있고 도달만 못 한다** — `force_signals.py:1003 _shoulder_tilt_2d`
  가 어깨선에서 폴 선 방향을 빼는 함수인데, `:1119` 가 `has_pole_aligned` 를 먼저 보고
  3D 분기로 간다. scipy ImportError 폴백이 그 dict 를 항상 채우므로 `arcsin(0)=0`.
  ★ **scipy 를 깔면 해결되지 않는다** — 평면 데이터를 3D 회전시키면 의미 없는 z 가 생긴다.
  올바른 수리는 **z 가 비면 `has_pole_aligned` 를 False 로** 해 2D 분기로 떨어뜨리는 것.

## 5. 치명 트랙 — IPSF 원문 확보 (근거 = 커밋 `b5455136`)

설계는 3단(정은지 95~100 / 실행결함 60~95 / **치명결함 25~60**)인데 **치명 트랙이 한 번도
안 돌았다.** 유일한 트리거가 split 0-fail 인데 그 필드를 가진 criterion 이 0개다.
**버그가 아니라 의도적 보류** — 켜면 kip-up 위양성이 재발한다고 코드가 두 곳에 적어뒀다.

★ **IPSF Pole Sports CoP 2025-2027 V2 원문을 확보**했다 → `evidence/ipsf_cop_2025_2027_v2.txt`

| 후보 | 판정 |
|---|---|
| split | **막힘** — 2D 투영 포화(반대 방향 허벅지도 180°), 깊이 필요 |
| 완전신전 | **부분** — 규칙은 기하학적으로 안전하나 `EXTEND` 선언이 power-spin 무릎 2개뿐 |
| 2초 홀드 | **측정기 완성, 검증 불가** — 코퍼스에 2초 홀드 영상이 없다 |
| **가시성/입증책임** | **가장 유망** ↓ |

### ★ 가시성 조항이 belle 09-13 판정의 1차 출처 근거다

```
p.14-15 "It is up to the athlete to ensure that all minimum criteria are visible
         to the judges. If a compulsory element is on the border, it will not be awarded."
원문 전수 grep: "benefit of" 0건 — IPSF 에는 의심시 선수 유리 조항이 없다.
```

(반대 조항은 POSA §9.3.1 이고 `judging_data/README.md` 가 타 연맹 기준을 금지한다.)
즉 *"명백히 틀린 영상이 100점은 말이 안 된다"* 가 **IPSF 규정과 정확히 일치**한다.
깊이 불필요 · 위양성 구조적 불가(기하 주장을 아예 안 함) · 신뢰도 신호는 이미 있다.

## 6. belle 판정 대기 — 4건

| # | 판정 | 왜 belle 이어야 하나 |
|---|---|---|
| **1** | **회전 + 기준 11편 재추출을 한 묶음으로** | 회전만 켜면 PROJECT.md 핵심 우려("정은지 41점")를 우리 손으로 만든다 — 오늘 60점으로 재현했다. 재추출하면 **과거 분석과 점수 연속성이 끊긴다** |
| **2** | 재학습 게이트 의미론 | 설계상 SKIPPED(kip-up/climb)를 PASS 로 볼 것인가. 데이터를 더 모아도 안 풀린다 |
| **3** | Omni 영상 합성 spike 비용 | Video output $17.50/영상 |
| **4** | 강사 교정 루프 | 앱이 교정을 **0건** 수집한다(`coachComment`/`reviewedBy` 자리만 있고 쓰는 코드 없음) |

★ **감점 상한(40)은 판정 대기가 아니다** — belle 2026-07-24 에 이미 정했고, 40 의 근거도
`deduction_engine.py:49-54` 에 박제돼 있다(IPSF 실행 총합상한 −25.0 → 0~100 스케일 비례환산).
바닥 60 도 의도된 설계다. **다시 묻지 말 것.**

### 내가 먼저 할 것 (판정 불필요)

- ~~기준 11편 드리프트 전수 측정~~ **2026-09-17 완료** → `.planning/quick/260917-hjy-reference-drift-all11/`
- 가시성 조항 배선 설계(신뢰도 신호 → 요소 미인정)
- 코드 출처 태그 정정 2건(§7-5)

---

## 7. ★ 오늘 내가 틀렸다가 고친 것 5건 — 같은 함정 반복 금지

1. **폴 폭 22.76px → 실제 6.0px (3.8배 오차)**
   쌍 검출이 폴 실루엣이 아니라 폴+배경 엣지를 짝지었다. **belle 이 45mm 를 알려줘서
   물리 검산이 가능해진 뒤에야 잡혔다**(몸통이 13cm 로 나왔다).
   → **교훈: 새 계기는 독립된 물리량으로 반드시 교차검증하라.**

2. **"롤이 모든 라인 점수를 오염시킨다" — 과장이었다**
   **평면 회전은 두 사지 사이 각도를 바꾸지 않는다.** 오늘 점수를 만드는 유일한 감점
   계열(`angle_vs_reference` 8종)은 롤에 **무영향**이다. 롤이 망치는 건 세계 수직과
   비교하는 양(`axisMetrics`)뿐인데 그건 이미 상수 0.0 으로 죽어 있다.

3. **"회전이 렌더를 깬다" — 오진이었다**
   ON 런 S3 산출물 0개로 보였는데, 확대카드·음성은 `status=done` **이후** 생성된다
   (후처리 110초~220초). **내가 Pod 을 완료 3분 만에 껐다.**

4. **"기준 4편의 y 가 전부 0" — 현상만 맞고 원인이 틀렸다**
   y 는 0 이 아니라 ~1e-13(회전행렬 잔여값)이었다. **수리 게이트(`y != 0`)가 작동을
   거부해서 잡아냈다.** → **안전 게이트는 오진도 잡는다. 넣어라.**

5. **"keypointReport.fps 가 2배 틀렸다" — 틀렸다**
   버그가 아니라 두 표현이다. 분석은 9fps(182/20.2s=9.01), keypointReport 는 앱
   오버레이용 18fps 보간본(364=182×2). 둘 다 라벨이 맞다.

6. **"B(완전신전)부터 하자" — 성급했다**
   권하기 전에 `EXTEND` 선언 현황을 안 봤다. 전수로 보니 **power-spin 무릎 2개뿐**이었다.

7. **감점 상한을 다시 물었다** — 이미 2026-07-24 에 정해졌고 근거도 코드에 있었다.
   → **"정해주세요" 하기 전에 메모리와 코드 주석부터 읽어라.** 오늘 두 번 다 거기 답이 있었다.

## 8. 함정 (밟으면 시간을 태운다)

1. **Firestore 코퍼스를 여러 에이전트가 각자 스캔하게 하지 말 것.** 오늘 앱을 멈췄다.
   한 번 읽어 로컬 파일로 떨구고 그걸 읽게 한다. Blaze 라 이제 캡은 없지만 비용은 든다.
2. **`start_server.sh:51` 의 `>` 는 재기동마다 로그를 truncate 한다.** 로그는 **종료 전이
   아니라 재기동 전**에 내려받아라.
3. **Pod 은 분석 완료 후 3~5분 더 돌려라** — 확대카드·음성이 그때 만들어진다.
4. **`runpod/pytorch:2.4.0-py3.11-cuda12.4.1-devel` 태그는 사라졌다.** 정확한 이름은
   **`...-devel-ubuntu22.04`**(템플릿 `runpod-torch-v240`).
5. **리포에 ORT 핀이 두 개다** — `bootstrap_full.sh:23` 1.19.2 vs 09-06 런북 1.22.
   `ort_determinism.py` 옵션은 **1.19.2 기준**이다. A/B 실측에는 1.19.2 를 쓸 것.
   "Blackwell 금지" 메모는 1.22 전제였다 — 1.19.2 에서는 정상 동작한다.
6. **Blackwell cold JIT ≈ 125초.** 멈춘 게 아니다. 컨테이너당 1회.
7. **Pod `/analyze` 인증 헤더는 `X-RunPod-Token`** 이다. `Authorization: Bearer` 아님.
8. **Lambda `RUNPOD_ANALYZE_URL` 은 여전히 죽은 Pod 주소다.** 오늘은 Pod 직접 위임으로
   우회했다(감사로 산출 동일성 확정). **앱 E2E 를 돌리려면 이 env 를 먼저 고쳐야 한다.**
9. 09-13 인계서 §7 함정 1~9 는 그대로 유효하다. 특히 **서브에이전트 게이트 수치 불신**.

## 9. 산출물

```
.planning/quick/260914-rot180-gpu-validation/
  HANDOFF.md                    이 문서
  evidence/
    MEASUREMENTS.md             회전 GPU 실측
    REFERENCE-IS-BROKEN.md      기준 드리프트 + 4편 좌표계(수리 완료)
    GEMINI-C-FIELDS.md          Gemini 3필드 전수 941건
    POLE-AS-INSTRUMENT.md       폴 계기 스파이크 1~4
    ipsf_cop_2025_2027_v2.txt   ★ IPSF 원문 추출본 (1MB, 1차 자료)
    pole_probe.py · pole_batch.py · hold_duration.py
    joints3d_metrics.py · upload_only.py · poll_doc.py · enroll_s3_video.py
    rtmw_error_profile_975docs_20260914.json
    docs/*.json (분석 5벌) · ref_backup/ (기준 4편 원본) · 서버 로그
backend/scripts/repair_reference_joints3d_axis.py   ★ 운영 스크립트(멱등)

역량 지도 페이지 → https://claude.ai/code/artifact/07f9074f-c42e-4410-85d5-56090c076b3e
  역량 142개 5분류 + 외부 전략 조사 78건
```

**belle 계정** = `csKWYvI3WCPYPysNQ9KkWecaUvq1` · **폴 지름** = 45mm(belle 확인, 간혹 40)
