---
quick_id: 260918-fyf
slug: rotbundle-other-motions
date: 2026-09-18
status: complete
verdict: "나빠진 동작 없음 — kip-up 99->99, power-spin 73->73. 4편은 미측정(Pod 필요)"
---

# 회전 묶음 배포 — pdshape 외 동작 측정 결과

## 판정

**나빠진 동작은 안 나왔다.** 인계서가 걱정한 **kip-up 위양성 재발은 이 표본에서
일어나지 않았다**(99 → 99). power-spin 도 73 → 73 으로 0 이동.

기준 11편 중 **5편은 기준이 사실상 안 움직였고**(허용오차 이내), 6편이 초과했으나
그중 pdshape 는 어제 E2E 로 이미 확인됐다(60 → 87).

**Pod 없이 여기까지가 한계다.** 남은 4편은 학생측 회전 ON 분석본이 필요하다.

수치 전문 = `evidence/MEASUREMENTS.md`.

## 한 일

1. 배포 전/후 기준 각도 Firestore 1회 pull (읽기 34회, 로컬 박제 후 재조회 0)
2. 어제 채점 seam 으로 **계기 검증** — belle pdshape 60→87 · 정은지 60→100 재현
3. 학생 4편 × 기준 2벌 채점 + 기준 이동 11편 전수
4. belle 눈 판정용 확대 카드 판정지 작성(`evidence/JUDGE-zoom-card.png`)

## 덤으로 드러난 것 2건

**(1) ★ ref-climb 의 옛 기준이 틀려 있었다**
배포 최대 변화(68.28°)인데 **회전 기여가 0.00°** 다. `rot180_v1`(지금 라이브),
`quick-260816-r7k`(8월 판), 어제 재추출본 **셋이 서로 일치**하고 옛 `phase4_v1` 만
혼자 다르다(엉덩이·무릎 125~137° vs 57~89°). 배포가 갈아끼운 것으로 보이나
**학생 doc 이 없어 점수 영향 미측정**.
인계서 §7-3 이 걱정한 "climb 부분 미러 상태"는 **아니다** — 플립 정상 완료 확인.

**(2) 게이트 fail-open**
`zoom_adv_left_shoulder` 를 눈이 `unclear/unreadable` 로 판독했는데 `action=pass`.
**원 위치가 맞든 틀리든 이건 결함이다.** 판정과 별개로 수리 대상.

## ★ 내가 틀렸다가 고친 것 1건

**`pre_phase4` 를 "배포 전 기준"으로 썼다 — 틀렸다.**
pdshape −54, climb 70° 같은 엉뚱한 수치가 나왔고 하마터면 *"배포가 점수를
망가뜨렸다"* 고 보고할 뻔했다. **계획에 박아둔 계기 검증(§함정 1)이 막았다.**

판별: 어제 박제한 *"저장본 vs 오늘 파이프라인(OFF) = 최대 15.6°"* 와 대조하니
`phase4_v1` 이 **15.58°** 로 일치. → 배포 직전 라이브는 **`phase4_v1`**.

**교훈: 백업 이름이 `pre_` 로 시작한다고 직전 상태인 것이 아니다.**

## 다음에 Pod 을 띄울 근거

| 동작 | 필요한 것 |
|---|---|
| ref-elbow-twist-sister | 학생측 회전 ON 재분석 1편 |
| ref-climb · ref-combo · ref-foxtop · ref-invert | 학생 영상 + ON 분석 4편 |

**비대칭이 왜 무효인가:** 학생 OFF × 기준 ON 은 한쪽만 교정된 비교라 구조적으로
나빠진다(본 측정에서 pdshape 100→60 으로 재현, 실제 배포는 60→87).
회전이 안 건드린 동작에는 이 문제가 없어 수치를 그대로 읽을 수 있다.
