# Quick Task 260918-k06: 착수점 정리 — Context

**Gathered:** 2026-09-18
**Status:** Ready for planning

<domain>
## Task Boundary

belle 이 "뭐 하나 정리가 안 되고 있는 것 같다. 뭔가 딱 정리된 순간이 필요하다"고 요청했다.
학원 실증이 약 한 달 뒤(2026-10 중순, 날짜 미확정)로 밀렸고, 그 사이 문서 원장이 흩어졌다.

전 트랙 실측(에이전트 10대, 근거 파일:줄번호 첨부) 결과 "정리가 안 된다"의 진원지는 **셋**으로
좁혀졌다. 이 작업은 그 셋을 실제로 닫는 **문서 정리 전용** 작업이다.

**코드 무접촉.** `app/`, `backend/` 의 소스는 한 줄도 건드리지 않는다.
건드리는 것은 `.planning/`, `CLAUDE.md`, `.gitignore`, `.planning/TRAINING-DUE.md` 뿐이다.

</domain>

<decisions>
## Implementation Decisions — 실측으로 확정된 사실 (재조사 불필요)

### 진원지 1 — 착수점이 4개다

지금 서로 다른 네 곳이 "여기서 시작하라"고 말한다:

| 위치 | 무엇이라 말하나 | 실제 |
|---|---|---|
| `CLAUDE.md:3`, `CLAUDE.md:83` | "현재 할 일 = /plan.md, 매 세션 반드시 확인" | `plan.md` 는 2026-08-30 이 마지막 커밋 (`1e631b84`) |
| `.planning/STATE.md:6` | `stopped_at: Phase 36 36-02 완료` | 2026-08-30 기준 |
| `.planning/CONTINUE-2026-09-07.md` | 루트에 착수점 파일로 존재 | 09-07 시점 |
| `.planning/quick/260918-day-closeout/HANDOFF.md` | 실제 최신 착수점 | 2026-09-18 |

프로젝트 자체 규칙(memory `section-start-housekeeping`)이 **"착수점 1개"**를 요구한다. 지금 4개다.

**결정:** 착수점은 **`.planning/quick/260918-day-closeout/HANDOFF.md`** 하나로 한다.
- `CLAUDE.md` §5 의 "현재 할 일 → /plan.md" 줄을 최신 HANDOFF 를 가리키도록 고친다.
  단 `plan.md` 파일 자체는 지우지 않는다 — 상단에 "이 파일은 이력이다. 착수점은 X 다" 배너만 붙인다.
- `.planning/CONTINUE-2026-09-07.md` 도 지우지 않고 상단에 같은 배너를 붙인다.
- `.planning/STATE.md` 의 frontmatter `last_updated` / `stopped_at` 을 오늘 실측 상태로 갱신한다.

### 진원지 2 — STATE.md 원장에 quick 트랙 45개가 빠졌다

- `ls -1 .planning/quick | wc -l` → **183**
- `grep -oE '\]\(\./quick/[0-9a-z-]+/\)' .planning/STATE.md | sort -u | wc -l` → **138**
- 차집합 **45개** 누락. 빠진 것은 전부 최근 2주 작업(09-10 이후 20개 포함).
- 원장 행이 `.planning/STATE.md` 안에서 **두 군데(약 229행대, 약 1053행대)** 로 쪼개져 있고 날짜순도 아니다.

**결정:** 누락 45건을 원장에 채우고, 원장을 **한 곳으로 합쳐 날짜 내림차순**으로 정렬한다.
각 행의 설명은 해당 디렉터리의 SUMMARY.md / HANDOFF.md / FINDINGS.md 첫 제목에서 뽑는다.
SUMMARY 가 없는 디렉터리는 설명란에 `(SUMMARY 없음)` 을 적어 미종결임이 드러나게 한다.

### 진원지 3 — belle 이 이미 답한 "유령" 8건이 문서 4곳에 미결로 남아 있다

전부 실측으로 확인된 유령이다. 각 문서의 해당 표/절에 **정정 주석**을 붙인다(삭제가 아니라 정정).

| # | 유령 항목 | 어디에 미결로 남아 있나 | 실제로 닫힌 근거 |
|---|---|---|---|
| 1 | 회전+기준 재추출 묶음 판정 | `.planning/quick/260914-rot180-gpu-validation/HANDOFF.md` §6 | 09-17 rot180_v1 승격 + 플래그 ON 배포, 09-18 kip-up 99→99 안전 확인 |
| 2 | 재학습 게이트 의미론 판정 | 같은 §6 | belle "게이트부터 고쳐" → 커밋 `222e9c87` (2026-09-18 00:57) |
| 3 | Omni 비용 승인 | 같은 §6 | 09-17 승인, 09-18 트랙 자체 종결 |
| 4 | 강사 교정 루프 | 같은 §6 | 09-17 구현, 09-18 OTA `0dddb65f` 발행 |
| 5 | 확대 카드 `zoom_adv_left_shoulder` 원 위치 | `.planning/quick/260917-day-closeout/HANDOFF.md` §4 | belle 09-18 11:56 "어깨 맞음" — 커밋 `5d6deef1` 제목에 그대로 |
| 6 | 강사 교정 OTA 발행 | 같은 §4 | 09-18 `0dddb65f` preview/1.2.4 발행 + belle 승인 |
| 7 | "남은 belle 판정 1건" | `.planning/STATE.md:103` (커밋 `d3afd4e9`, 09-18 11:53) | belle 판정이 **3분 뒤** 11:56 에 들어왔다 |
| 8 | "회전 플래그를 운영에 켤 것인가(리포는 아직 0)" / "재학습(Task B)은 착수 금지" | MEMORY.md 인덱스 (메모리 파일은 이 작업 범위 밖 — 아래 참조) | 플래그는 09-17 에 켜짐(`start_server.sh:24` = 1), 착수 금지 사유는 게이트 수리로 해소 |

**메모리 파일 주의:** `/Users/kimtaesung/.claude/projects/.../memory/` 는 이 저장소 밖이다.
executor 는 **건드리지 않는다.** 메인 세션이 별도로 갱신한다. 8번은 표에 기록만 남긴다.

### 추가 1 — TRAINING-DUE.md 의 성적표가 낡았다

`.planning/TRAINING-DUE.md` 는 아직 git 에 안 올라가 있고(`?? .planning/TRAINING-DUE.md`), 내용 세 곳이 실측과 어긋난다:

| 지금 적힌 것 | 실측 |
|---|---|
| "직전 판(v29) 성적 … 빈 골격 9/29" | 실제 최신은 **v38 (2026-08-28)**. `promotion_ledger.json` entries 5건(v28·v29·v35·v36·v38) 전부 `promoted=false`, `current: null` — 한 번도 승격된 적 없음 |
| "belle 이 **5090 이상** Pod 추가" | `run_sft.sh:169-190` 실측 주석: 4090 24GB OOM, **5090 32GB 도 OOM**. 완주한 v36·v38 은 **A100 PCIe 80GB $1.39/hr** |
| (비용 언급 없음) | 신규 라벨 **142편 × 약 9분 ≈ 21시간** + 학습 2h49m + 게이트 1h → **약 $33~38**. 현재 RunPod 잔액 $18.90 로는 한 사이클 불가 |
| (학습률 언급 없음) | ★ 리포 기본 `SFT_LR=1e-5` (`run_sft.sh:180`) 는 08-26 실험에서 **"침묵의 진범"으로 실측된 값**이다. 1e-4 는 리포 본체가 아니라 `.planning/quick/260828-v34-targeted-data/v34_cycle.sh:45` 일회성 래퍼에만 있다. 문서대로 돌리면 침묵을 만든 설정으로 학습한다 |
| (파일럿 연결 언급 없음) | ★ 승격해도 **앱에 꽂을 배선이 0건** — 운영 코드에 `promotion_ledger`/`phase22_sft`/`SFT_MODEL` 참조 0. 22-08/09/10 은 PLAN 만 있고 미실행. 이번 실증에는 안 닿는다 |

**결정:** 위 5건을 반영해 TRAINING-DUE.md 를 고쳐 쓰고 git 에 추가한다.

### 추가 2 — .gitignore

`git status` 미추적 7건 중 저장소에 들어가면 안 되는 것이 있다:

- `외주용역 제출 서류/` — **22MB, 32개 파일.** 사업자등록증·전자세금계산서·용역계약서·견적서 등
  사업 문서다. 지금 `.gitignore` 에 없어서 `git add -A` 한 번이면 그대로 커밋된다.
  (memory `home-dir-is-git-repo-pii-hazard` 와 같은 종류의 위험)
- `_cap/` — 2.7MB 스크린샷 13장, 작업용 임시물
- `skills-lock.json`, `.agents/` — 도구 산출물

**결정:** 위 항목을 `.gitignore` 에 추가한다. **파일은 지우거나 옮기지 않는다** — belle 의 문서다.
`docs/openapi.yaml` 과 `reference-downstream-backfill.json` 은 판단 보류(무시하지도, 커밋하지도 않는다).

### Claude's Discretion

- 원장 표의 정확한 열 구성과 배너 문구
- 45건의 설명 문장을 뽑는 방식(SUMMARY 제목 우선, 없으면 디렉터리명)
- TRAINING-DUE.md 의 최종 문장 구성

</decisions>

<specifics>
## Specific Ideas

- 정정은 **삭제가 아니라 취소선/정정 주석**으로 한다. 이 프로젝트는 "왜 그렇게 판단했나"의 이력을
  중요하게 다룬다(memory `handoff-observation-not-diagnosis`). 지운 흔적이 남아야 한다.
- 배너 문구는 다음 형태로 통일한다:
  `> **이 파일은 이력이다.** 착수점은 `.planning/quick/260918-day-closeout/HANDOFF.md` 하나다. (2026-09-18 정리)`
- 이모지 금지 (CLAUDE.md §7).
- 커밋은 태스크별 원자 커밋.

</specifics>

<canonical_refs>
## Canonical References

- `CLAUDE.md` §5 (세부 컨텍스트 파일 위치), §7 (코드 품질 원칙 — 이모지 금지)
- `.planning/STATE.md` (원장 정본)
- `.planning/quick/260918-day-closeout/HANDOFF.md` (새 단일 착수점)
- 실측 근거 전문: 이 세션의 워크플로 산출물 (에이전트 10대, 파일:줄번호 첨부)

</canonical_refs>
