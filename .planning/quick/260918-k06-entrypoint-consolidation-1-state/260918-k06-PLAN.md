---
phase: quick-260918-k06
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - plan.md
  - .planning/CONTINUE-2026-09-07.md
  - .planning/STATE.md
  - .planning/quick/260914-rot180-gpu-validation/HANDOFF.md
  - .planning/quick/260917-day-closeout/HANDOFF.md
  - .planning/quick/260918-day-closeout/HANDOFF.md
  - .planning/TRAINING-DUE.md
  - .gitignore
autonomous: true
requirements: [QUICK-260918-k06]

must_haves:
  truths:
    - "새 세션이 CLAUDE.md 만 읽어도 착수점이 `.planning/quick/260918-day-closeout/HANDOFF.md` 하나라는 것을 안다 (plan.md·CONTINUE-2026-09-07.md 는 상단 배너로 '이력'임이 드러난다)"
    - "STATE.md frontmatter 가 2026-09-18 상태를 말한다 (stopped_at / last_updated / last_activity)"
    - "STATE.md 의 quick 트랙 원장이 `.planning/quick` 디렉터리 전부를 한 표에 날짜 내림차순으로 담고 있다 (차집합 0)"
    - "belle 이 이미 답한 유령 판정 8건이 09-14 §6 · 09-17 §4 · STATE:103 에서 정정 주석으로 닫혀 있고, 원문 표는 삭제되지 않았다"
    - "TRAINING-DUE.md 가 실측(v38·A100·비용·LR·배선 0)을 말하고 git 에 추적된다"
    - "`외주용역 제출 서류/`·`_cap/`·`skills-lock.json`·`.agents/` 가 git status 에서 사라지되 파일은 그대로 있다"
    - "app/ 와 backend/ 의 diff 가 0 이다"
  artifacts:
    - path: "CLAUDE.md"
      provides: "§5 현재 할 일 -> 단일 착수점"
      contains: "260918-day-closeout/HANDOFF.md"
    - path: "plan.md"
      provides: "이력 배너 (1행)"
      contains: "이 파일은 이력이다"
    - path: ".planning/CONTINUE-2026-09-07.md"
      provides: "이력 배너 (1행)"
      contains: "이 파일은 이력이다"
    - path: ".planning/STATE.md"
      provides: "frontmatter 갱신 + 원장 단일 표 + :103 정정 주석"
      contains: "2026-09-18"
    - path: ".planning/quick/260914-rot180-gpu-validation/HANDOFF.md"
      provides: "§6 정정 주석 (4건 종결)"
      contains: "정정 (2026-09-18"
    - path: ".planning/quick/260917-day-closeout/HANDOFF.md"
      provides: "§4 정정 주석 (2건 종결)"
      contains: "정정 (2026-09-18"
    - path: ".planning/quick/260918-day-closeout/HANDOFF.md"
      provides: "§8 유령 판정 정정 원장 (8행, 8번은 메모리 범위 밖 기록만)"
      contains: "유령 판정 정정 원장"
    - path: ".planning/TRAINING-DUE.md"
      provides: "정정 5건 반영, git 추적"
      contains: "A100"
    - path: ".gitignore"
      provides: "사업 문서·임시물·도구 산출물 무시"
      contains: "외주용역 제출 서류/"
  key_links:
    - from: "CLAUDE.md"
      to: ".planning/quick/260918-day-closeout/HANDOFF.md"
      via: "§5 현재 할 일 행 + §1/§6 읽기 순서"
      pattern: "260918-day-closeout/HANDOFF\\.md"
    - from: ".planning/STATE.md"
      to: ".planning/quick/*/"
      via: "원장 Directory 열 링크"
      pattern: "\\]\\(\\./quick/[0-9a-z-]+/\\)"
    - from: ".planning/quick/260918-day-closeout/HANDOFF.md §8"
      to: "09-14 §6 / 09-17 §4 / STATE:103"
      via: "정정 위치 열"
      pattern: "5d6deef1"
---

<objective>
착수점 정리 — "정리가 안 된다"의 진원지 3건을 문서만으로 닫는다 (CONTEXT.md 잠긴 결정 전부).

1. 착수점 4개 -> 1개 (`.planning/quick/260918-day-closeout/HANDOFF.md`). CLAUDE.md §5 수정, plan.md·CONTINUE-2026-09-07.md 는 지우지 않고 배너, STATE.md frontmatter 갱신.
2. STATE.md quick 원장 누락분 보충 + 두 군데로 쪼개진 표를 한 표로 병합, 날짜 내림차순.
3. belle 이 이미 답한 유령 판정 8건에 정정 주석 (삭제 금지, 이력 보존).
4. 추가: TRAINING-DUE.md 정정 5건 + git 추적. `.gitignore` 에 사업 문서·임시물·도구 산출물 추가.

Purpose: 학원 실증(2026-10 중순)까지 한 달, 다음 세션이 어디서 시작할지 3초 안에 알게 한다. memory `section-start-housekeeping` 이 요구하는 "착수점 1개·STATE 실측"을 실제로 만족시킨다.
Output: 위 9개 파일 수정, 태스크당 원자 커밋 1개 (총 5커밋). **코드 무접촉** — `app/`, `backend/` diff 0.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260918-k06-entrypoint-consolidation-1-state/260918-k06-CONTEXT.md
@CLAUDE.md
@.planning/quick/260918-day-closeout/HANDOFF.md

STATE.md 는 1067행이다. 전체를 읽지 말고 아래 구간만 읽어라 (Task 1 착수 시점의 줄번호):
- 1-15 frontmatter (`stopped_at` 6행, `last_updated` 7행, `last_activity` 8행)
- 28-31 `## Current Position` 헤더 + 08-30 정리 주석
- 103 `Last activity: 2026-09-18 - ...` (한 줄짜리 긴 문단, 유령 #7 이 여기 있다)
- 149-153 `### Quick Tasks Completed` 헤더 + 표 머리 2행
- 1043-1058 `### 이전 Pod 이력` 표 끝에 quick 행 4개(260907-itk·260909-bhc·260909-ji1·260917-hjy)가 잘못 끼어 있다
Task 1 이 Current Position 아래에 2행을 넣으므로 그 뒤 태스크는 줄번호가 아니라 **앵커 문자열**로 위치를 찾아라.

시작 전에 기준 커밋을 박아둔다 (마지막 코드 무접촉 게이트에 쓴다):
`BASE=$(git rev-parse HEAD); echo $BASE > /tmp/k06-base.txt`

프로젝트 규칙: 관찰용 git/grep/ls 는 `rtk` 접두, `gsd-sdk` 는 rtk 로 감싸지 말 것. 이모지 금지 (CLAUDE.md §7). 정정은 삭제가 아니라 취소선·정정 주석.
</context>

<tasks>

<task type="auto">
  <name>Task 1: 착수점 1개 통합 — CLAUDE.md §5 + plan.md/CONTINUE 배너 + STATE frontmatter (진원지 1)</name>
  <files>CLAUDE.md, plan.md, .planning/CONTINUE-2026-09-07.md, .planning/STATE.md</files>
  <action>
CONTEXT.md 진원지 1 결정을 그대로 집행한다. 착수점은 `.planning/quick/260918-day-closeout/HANDOFF.md` 하나다.

**CLAUDE.md** (잠긴 결정 = §5 행. 나머지 plan.md 언급 4곳은 discretion — 두면 plan.md 가 5번째 착수점으로 남으므로 같이 바꾼다):
- 83행 `현재 할 일          → /plan.md         ← 매 세션 반드시 확인` 을 `현재 할 일          → .planning/quick/260918-day-closeout/HANDOFF.md  ← 착수점(유일). 매 세션 반드시 확인. plan.md 는 이력` 로 바꾼다. 정렬 공백은 표 모양이 깨지지 않을 정도로 맞춘다.
- 3행 읽기 순서 `CLAUDE.md → design.md → plan.md` 를 `CLAUDE.md → design.md → 착수점(.planning/quick/260918-day-closeout/HANDOFF.md)` 로.
- 103행 (§6 새 플랫폼 세션 시작) 의 `plan.md 읽기` 도 같은 착수점 경로로.
- 99행 `plan.md 업데이트 (완료/진행중/다음 할 것)` 과 124행 `작업 완료 시 plan.md 업데이트.` 는 `착수점 HANDOFF + .planning/STATE.md 갱신` 으로. (프로젝트 실제 관행이 일자별 HANDOFF 이고 plan.md 는 08-30 이후 죽어 있다.)
- 그 외 CLAUDE.md 내용은 한 글자도 바꾸지 않는다.

**plan.md** 와 **.planning/CONTINUE-2026-09-07.md**: 파일을 지우지 않는다. 각 파일의 **1행 앞**에 아래 배너 1행 + 빈 줄 1행을 삽입한다 (CONTEXT.md specifics 의 통일 문구 그대로):
`> **이 파일은 이력이다.** 착수점은 \`.planning/quick/260918-day-closeout/HANDOFF.md\` 하나다. (2026-09-18 정리)`
CONTINUE 파일 3행의 `> 착수점 (유일본)` 은 지우지 않는다 — 위 배너가 정정한다.

**.planning/STATE.md**:
- 6행 `stopped_at` 을 `"2026-09-18 착수점 정리(quick-260918-k06) 완료 — 다음 첫 일 = belle 학원 실증 결과 듣기. 착수점 = .planning/quick/260918-day-closeout/HANDOFF.md"` 로.
- 7행 `last_updated` 를 `date -u +%Y-%m-%dT%H:%M:%S.000Z` 실행값으로, 8행 `last_activity: 2026-09-18` 로.
- `## Current Position` 헤딩(28행) 바로 아래, 기존 `> 2026-08-30 정리...` 주석 **위에** 다음 2행(주석 1행 + 빈 줄)을 넣는다:
  `> **2026-09-18 정리 (quick-260918-k06):** 착수점은 \`.planning/quick/260918-day-closeout/HANDOFF.md\` 하나다. 아래 페이즈 수치(38 페이즈, 완료 30/진행 4/미착수 4, plan 118/108)는 2026-08-30 실측 그대로이며 이번 정리에서 다시 재지 않았다.`
  progress 숫자는 손대지 않는다 (재측정 금지).

커밋: `rtk git add CLAUDE.md plan.md .planning/CONTINUE-2026-09-07.md .planning/STATE.md && rtk git commit -m "docs(quick-260918-k06): 착수점 1개 통합 — CLAUDE.md §5 + plan.md/CONTINUE 배너 + STATE frontmatter"` (Co-Authored-By 트레일러는 환경 규칙대로).
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && test "$(grep -c '260918-day-closeout/HANDOFF.md' CLAUDE.md)" -ge 3 && test "$(grep -c '현재 할 일.*/plan.md' CLAUDE.md)" -eq 0 && test "$(head -n 1 plan.md | grep -c '이 파일은 이력이다')" -eq 1 && test "$(head -n 1 .planning/CONTINUE-2026-09-07.md | grep -c '이 파일은 이력이다')" -eq 1 && sed -n '1,15p' .planning/STATE.md | grep -q '^stopped_at: "2026-09-18' && sed -n '1,15p' .planning/STATE.md | grep -q '^last_activity: 2026-09-18' && sed -n '1,15p' .planning/STATE.md | grep -q '^last_updated: "2026-09-18T' && grep -q '2026-09-18 정리 (quick-260918-k06)' .planning/STATE.md && echo TASK1-OK</automated>
  </verify>
  <done>CLAUDE.md 가 plan.md 대신 HANDOFF 를 착수점으로 가리키고(§5·§1·§6), plan.md·CONTINUE 1행이 배너, STATE.md frontmatter 3필드가 2026-09-18, Current Position 에 09-18 정리 주석. 커밋 1개.</done>
</task>

<task type="auto">
  <name>Task 2: STATE.md 원장 복구 — 누락 quick 트랙 보충 + 한 표로 병합·날짜 내림차순 (진원지 2)</name>
  <files>.planning/STATE.md</files>
  <commands>
# (a) 차집합 재계산 — CONTEXT 실측은 183/138/45 였고, 그 뒤 k06 디렉터리가 생겨 지금은 184 이므로 46 이 나와야 정상
cd /Users/kimtaesung/Dev/SunityMotion
ls -1 .planning/quick > /tmp/all.txt
grep -oE '\]\(\./quick/[0-9a-z-]+/\)' .planning/STATE.md | sed 's|](\./quick/||; s|/)||' | sort -u > /tmp/linked.txt
comm -13 /tmp/linked.txt <(sort /tmp/all.txt) > /tmp/missing.txt
wc -l /tmp/all.txt /tmp/linked.txt /tmp/missing.txt

# (b) 누락분 행 생성 — 설명 = SUMMARY > HANDOFF > FINDINGS 첫 H1, 없으면 "(SUMMARY 없음)". 파일명은 접두형(260918-fyf-SUMMARY.md)이라 glob 필수
while read -r d; do
  dir=".planning/quick/$d"; doc=""
  for pat in '*SUMMARY.md' '*HANDOFF.md' '*FINDINGS.md'; do
    doc=$(ls "$dir"/$pat 2>/dev/null | head -n 1); [ -n "$doc" ] && break
  done
  desc=""; date="20${d:0:2}-${d:2:2}-${d:4:2}"
  if [ -n "$doc" ]; then
    desc=$(grep -m1 -E '^# ' "$doc" | sed 's/^# *//')
    fd=$(grep -m1 -E '^date: ' "$doc" | sed 's/^date: *//; s/"//g'); [ -n "$fd" ] && date="$fd"
  fi
  [ -z "$desc" ] && desc="(SUMMARY 없음)"
  desc=${desc//|/\\|}
  commit=$(git log -1 --format=%h -- "$dir"); [ -z "$commit" ] && commit="(미커밋)"
  echo "| ${d:0:10} | $desc | $date | $commit | [$d](./quick/$d/) |"
done < /tmp/missing.txt > /tmp/newrows.txt
wc -l /tmp/newrows.txt

# (c) 기존 행 전부 수거 (두 군데 합쳐 143행) 후 병합·정렬 — 키 = # 열(quick id, YYMMDD 접두) 내림차순
grep -E '^\| 26[0-9]{4}-' .planning/STATE.md > /tmp/oldrows.txt
cat /tmp/oldrows.txt /tmp/newrows.txt | sort -t'|' -k2,2r > /tmp/ledger.txt
wc -l /tmp/oldrows.txt /tmp/ledger.txt
  </commands>
  <action>
CONTEXT.md 진원지 2 결정: 누락분을 원장에 채우고, 원장을 **한 곳(`### Quick Tasks Completed` 아래)으로 합쳐 날짜 내림차순**으로 만든다. 열 구성은 기존 그대로 `| # | Description | Date | Commit | Directory |` (discretion: 열 추가 없음).

1. `<commands>` (a)~(c) 를 순서대로 실행한다. (a) 의 missing 은 46행이어야 한다 (183+k06). 45 도 46 도 아니면 멈추고 보고.
2. `260918-k06-entrypoint-consolidation-1-state` 행은 SUMMARY 가 아직 없어 `(SUMMARY 없음)` 이 되는데, 이 작업 자신이므로 설명을 `착수점 정리 — 진원지 3건 해소 (이 작업, SUMMARY 는 완료 시 작성)` 로 손으로 바꾼다. 나머지 `(SUMMARY 없음)` 행은 그대로 둔다 — 미종결이 드러나는 것이 결정의 목적이다.
3. `/tmp/newrows.txt` 를 눈으로 훑어 H1 이 아닌 것(예: frontmatter 만 있고 제목이 없어 빈 설명)이 있으면 그 디렉터리의 SUMMARY 본문 첫 문장으로 한 줄 채운다. 재측정이 아니라 설명 문장 보정이다.
4. STATE.md 를 python3 스크립트(scratchpad 에 두고 커밋하지 않음)로 고친다. 알고리즘:
   - 파일을 행 단위로 읽는다.
   - `^\| 26[0-9]{4}-` 에 맞는 행을 **파일 전체에서** 제거한다 (Quick Tasks 구간의 139행 + `### 이전 Pod 이력` 표 끝의 4행 모두). 그 외 행은 건드리지 않는다 — 특히 Pod 표의 `| 항목 | 상태 |` 행들과 `### 남은 작업` 이하는 그대로.
   - `### Quick Tasks Completed` 헤딩 뒤의 빈 줄 + 표 머리 2행(`| # | Description ...`, `|---|...`)은 유지하고, 그 직후에 `/tmp/ledger.txt` 전체를 빈 줄 없이 붙인다.
   - 표 끝과 다음 `### ` 헤딩(`### Plan 09-01 close-out`) 사이의 빈 줄은 정확히 1개만 남긴다. 기존 구간 안에 있던 빈 줄 6개(표를 쪼개던 것)는 없앤다.
   - 다시 쓴다.
5. 결과에서 링크 차집합이 0 인지, 행이 전부 한 구간 안에 있는지, 내림차순인지 `<verify>` 로 확인한다.

커밋: `rtk git add .planning/STATE.md && rtk git commit -m "docs(quick-260918-k06): STATE 원장 복구 — quick 트랙 누락 46건 보충 + 한 표로 병합·날짜 내림차순"`.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && ls -1 .planning/quick | sort > /tmp/all2.txt && grep -oE '\]\(\./quick/[0-9a-z-]+/\)' .planning/STATE.md | sed 's|](\./quick/||; s|/)||' | sort -u > /tmp/linked2.txt && test "$(comm -13 /tmp/linked2.txt /tmp/all2.txt | wc -l | tr -d ' ')" -eq 0 && test "$(wc -l < /tmp/linked2.txt | tr -d ' ')" -eq "$(wc -l < /tmp/all2.txt | tr -d ' ')" && test "$(awk '/^### Quick Tasks Completed/{f=1;next} /^### /{f=0} f && /^\| 26[0-9]{4}-/{n++} END{print n+0}' .planning/STATE.md)" -eq "$(grep -cE '^\| 26[0-9]{4}-' .planning/STATE.md)" && grep -E '^\| 26[0-9]{4}-' .planning/STATE.md | cut -d'|' -f2 | sort -cr && test "$(sed -n '/^### Quick Tasks Completed/,/^### Plan 09-01/p' .planning/STATE.md | grep -c '^$')" -eq 2 && test "$(sed -n '/^### 이전 Pod 이력/,/^### 남은 작업/p' .planning/STATE.md | grep -cE '^\| 26[0-9]{4}-')" -eq 0 && echo TASK2-OK</automated>
  </verify>
  <done>`.planning/quick` 전체(184)가 STATE.md 원장에 링크되어 차집합 0. 행 전부가 `### Quick Tasks Completed` 한 표에 빈 줄 없이 연속, # 열 내림차순, Pod 이력 표에는 quick 행 0. SUMMARY 없는 디렉터리는 `(SUMMARY 없음)` 으로 보인다. 커밋 1개.</done>
</task>

<task type="auto">
  <name>Task 3: 유령 판정 8건 정정 주석 — 09-14 §6 · 09-17 §4 · STATE:103 + 정정 원장 (진원지 3)</name>
  <files>.planning/quick/260914-rot180-gpu-validation/HANDOFF.md, .planning/quick/260917-day-closeout/HANDOFF.md, .planning/STATE.md, .planning/quick/260918-day-closeout/HANDOFF.md</files>
  <action>
CONTEXT.md 진원지 3 의 표(8건)를 그대로 옮긴다. **삭제 금지** — 원문 표·문장은 남기고 취소선 + 정정 블록을 붙인다. 근거 커밋/시각은 CONTEXT 표의 값을 그대로 쓴다(재조사 금지). 메모리 파일(`~/.claude/projects/.../memory/`)은 **건드리지 않는다**.

**A. `.planning/quick/260914-rot180-gpu-validation/HANDOFF.md` §6** (헤딩 `## 6. belle 판정 대기 — 4건`, 147행 부근):
- 헤딩을 `## 6. ~~belle 판정 대기 — 4건~~ (2026-09-18 정정: 4건 전부 종결)` 로 바꾼다.
- 헤딩 바로 아래(표 위)에 빈 줄 + 정정 블록을 넣는다. 첫 줄 `> **정정 (2026-09-18, quick-260918-k06):** 아래 4건은 전부 닫혔다. 이 절은 이력이다. 다시 묻지 말 것.` 이어서 인용 항목 4개:
  1. 회전+기준 재추출 묶음 — 09-17 rot180_v1 승격 + 플래그 ON 배포(quick-260917-hjy), 09-18 kip-up 99→99·power-spin 73→73 안전 확인(quick-260918-fyf).
  2. 재학습 게이트 의미론 — belle "게이트부터 고쳐" → 커밋 `222e9c87` (2026-09-18 00:57), SKIPPED/DESCOPED 분리.
  3. Omni 비용 — 09-17 승인, 09-18 트랙 자체 종결(Omni 1.1 은 카메라를 안 돌린다, quick-260918-et1).
  4. 강사 교정 루프 — 09-17 구현, 09-18 OTA `0dddb65f` preview/1.2.4 발행 + belle 승인.
- 표 4행과 아래 "감점 상한은 판정 대기가 아니다" 문단은 그대로 둔다.

**B. `.planning/quick/260917-day-closeout/HANDOFF.md` §4** (헤딩 `## 4. belle 판정 대기 — 2건`, 137행 부근):
- 헤딩을 `## 4. ~~belle 판정 대기 — 2건~~ (2026-09-18 정정: 2건 전부 종결)` 로.
- 헤딩 아래 같은 형식의 정정 블록, 항목 2개:
  1. 확대 카드 `zoom_adv_left_shoulder` 원 위치 — belle 09-18 11:56 "어깨 맞음"(커밋 `5d6deef1` 제목). 게이트는 2단 판정으로 수리(`8c64b941`).
  2. 강사 교정 OTA 발행 — 09-18 `0dddb65f` preview/1.2.4 발행 + belle 승인.
- §4-1 본문(게이트 fail-open 분석)은 그대로 둔다 — 09-18 의 2단 판정 수리 근거가 된 관측이다.

**C. `.planning/STATE.md` 의 `Last activity: 2026-09-18 - ...` 한 줄 문단** (유령 #7): 문자열 `**남은 belle 판정 1건 = 확대 카드 \`zoom_adv_left_shoulder\` 원 위치**(판정지 = quick-260918-fyf/evidence/JUDGE-zoom-card.png, 내 예측 박제: 원 중심이 몸 왼쪽 바깥·실제 어깨는 원 오른쪽 가장자리)` 를 찾아, 닫는 괄호 `)` 바로 뒤에 ` **[정정 2026-09-18: 이 문장(커밋 d3afd4e9, 11:53) 3분 뒤 11:56 belle "어깨 맞음" 판정 수신 — 커밋 5d6deef1. 미결 0건]**` 을 삽입한다. 문장 자체는 지우지 않는다. 한 줄 문단이므로 줄바꿈을 넣지 않는다.

**D. `.planning/quick/260918-day-closeout/HANDOFF.md`** (새 단일 착수점, 현재 131행, §7 함정 8번으로 끝남) 파일 끝에 구분선 `---` 과 새 절을 붙인다:
`## 8. 유령 판정 정정 원장 (2026-09-18 정리, quick-260918-k06)` — 한 줄 설명 "belle 이 이미 답했는데 문서에 미결로 남아 있던 8건. **전부 판정 완료 — 다시 묻지 말 것.**" 다음 표 `| # | 항목 | 미결로 남아 있던 곳 | 닫힌 근거 | 정정 위치 |`, 행 1~8 = CONTEXT.md 진원지 3 표의 항목·위치·근거를 그대로, 정정 위치 열은 1~4 = "09-14 HANDOFF §6 정정 블록", 5~6 = "09-17 HANDOFF §4 정정 블록", 7 = "STATE.md Last activity 2026-09-18 문단 인라인", 8 = "**메모리 파일 — 리포 밖, 이 작업 무접촉. 메인 세션이 별도 갱신** (플래그는 09-17 ON = `start_server.sh:24`, 착수 금지 사유는 게이트 수리로 해소)". 8번은 기록만 남기는 것이 결정이다.

커밋: `rtk git add .planning/quick/260914-rot180-gpu-validation/HANDOFF.md .planning/quick/260917-day-closeout/HANDOFF.md .planning/STATE.md .planning/quick/260918-day-closeout/HANDOFF.md && rtk git commit -m "docs(quick-260918-k06): 유령 판정 8건 정정 주석 — 09-14 §6·09-17 §4·STATE:103 + 정정 원장"`.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && A=.planning/quick/260914-rot180-gpu-validation/HANDOFF.md && B=.planning/quick/260917-day-closeout/HANDOFF.md && E=.planning/quick/260918-day-closeout/HANDOFF.md && grep -q '^## 6\. ~~belle 판정 대기 — 4건~~' "$A" && test "$(grep -c '정정 (2026-09-18, quick-260918-k06)' "$A")" -eq 1 && grep -q '222e9c87' "$A" && test "$(grep -c '^| \*\*1\*\* | \*\*회전 + 기준 11편 재추출' "$A")" -eq 1 && grep -q '^## 4\. ~~belle 판정 대기 — 2건~~' "$B" && test "$(grep -c '정정 (2026-09-18, quick-260918-k06)' "$B")" -eq 1 && grep -q '5d6deef1' "$B" && test "$(grep -c '^| \*\*1\*\* | \*\*확대 카드' "$B")" -eq 1 && test "$(grep '^Last activity: 2026-09-18' .planning/STATE.md | grep -c '정정 2026-09-18: 이 문장(커밋 d3afd4e9')" -eq 1 && grep -q '^## 8\. 유령 판정 정정 원장' "$E" && test "$(sed -n '/^## 8\. 유령 판정 정정 원장/,$p' "$E" | grep -cE '^\| [1-8] \|')" -eq 8 && grep -q '메모리 파일 — 리포 밖' "$E" && echo TASK3-OK</automated>
  </verify>
  <done>09-14 §6 와 09-17 §4 헤딩이 취소선 + 정정 블록을 갖고 원문 표는 남아 있다. STATE.md Last activity 문단에 #7 인라인 정정. 착수점 HANDOFF §8 에 8행 정정 원장(8번 = 메모리 범위 밖 기록). 커밋 1개.</done>
</task>

<task type="auto">
  <name>Task 4: TRAINING-DUE.md 정정 5건 + git 추적 (추가 1)</name>
  <files>.planning/TRAINING-DUE.md</files>
  <action>
CONTEXT.md 추가 1 의 표 5건을 반영해 `.planning/TRAINING-DUE.md` 를 고쳐 쓴다 (Write 도구). 이 파일은 한 번도 커밋된 적이 없으므로 git 이력은 없지만, 프로젝트 원칙대로 낡은 문장은 **취소선으로 남기고** 정정을 옆에 둔다. 수치·커밋·경로는 CONTEXT 표 값을 그대로 (재측정 금지). 최종 문장 구성은 discretion. 구조:

- 제목·"플라이휠이 자동 판정했다" 문장·수집 513/400·admit 334/60 표는 유지.
- 상단에 `> **정정 (2026-09-18, quick-260918-k06):** 아래 성적표·Pod·비용·학습률·파일럿 연결 5곳을 실측으로 고쳤다.` 1행.
- `## 돌리는 법` 1번: `~~belle 이 5090 이상 Pod 추가~~` → **A100 PCIe 80GB ($1.39/hr)** — `run_sft.sh:169-190` 실측 주석: 4090 24GB OOM, 5090 32GB 도 OOM. 완주한 v36·v38 은 A100. EU-RO-1, 기존 볼륨은 유지.
- 새 항목 `## 학습률 — 돌리기 전에 반드시 정할 것`: 리포 기본 `SFT_LR=1e-5` (`run_sft.sh:180`) 는 08-26 실험에서 "침묵의 진범"으로 실측된 값. `1e-4` 는 리포 본체가 아니라 `.planning/quick/260828-v34-targeted-data/v34_cycle.sh:45` 일회성 래퍼에만 있다. 문서대로 돌리면 침묵을 만든 설정으로 학습한다 — LR 을 명시하고 시작할 것.
- 새 항목 `## 비용 (한 사이클)`: 신규 라벨 142편 × 약 9분 ≈ 21시간 + 학습 2h49m + 게이트 1h → **약 $33~38**. 현재 RunPod 잔액 **$18.90** 으로는 한 사이클 불가 → 충전이 선행.
- `## 직전 판(v29) 성적` 절: 제목을 `## 직전 판 성적 — ~~v29~~ v38 (2026-08-28)` 로, 본문 `빈 골격 9/29 · faults 2 · 4동작 중 1동작만 짚음 · 게이트 FAIL` 은 취소선으로 남기고 정정: `promotion_ledger.json` entries 5건(v28·v29·v35·v36·v38) 전부 `promoted=false`, `current: null` — **한 번도 승격된 적 없음**. 넘어야 할 선 = 게이트 통과 + 승격(hard 게이트 4+4).
- 새 항목 `## 파일럿 연결 — 없음`: 승격해도 **앱에 꽂을 배선이 0건** — 운영 코드에 `promotion_ledger`/`phase22_sft`/`SFT_MODEL` 참조 0. 22-08/09/10 은 PLAN 만 있고 미실행. **이번 실증(2026-10 중순)에는 안 닿는다.** 재학습은 실증 뒤 우선순위로 belle 에게 물을 것.
- 이모지 금지.

`rtk git add .planning/TRAINING-DUE.md && rtk git commit -m "docs(quick-260918-k06): TRAINING-DUE 정정 5건 — v38·A100·비용·LR·앱 배선 0"`.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && T=.planning/TRAINING-DUE.md && grep -q 'A100 PCIe 80GB' "$T" && grep -q '~~belle 이 5090 이상' "$T" && grep -q 'SFT_LR=1e-5' "$T" && grep -q 'v34_cycle.sh:45' "$T" && grep -qE '\$33~38' "$T" && grep -q '18.90' "$T" && grep -q 'v38' "$T" && grep -q '~~v29~~' "$T" && grep -q 'promoted=false' "$T" && grep -q '배선이 0건' "$T" && grep -q '513' "$T" && git ls-files --error-unmatch "$T" >/dev/null && test "$(git status --porcelain "$T" | wc -l | tr -d ' ')" -eq 0 && echo TASK4-OK</automated>
  </verify>
  <done>TRAINING-DUE.md 가 v38·A100·비용 $33~38/잔액 $18.90·SFT_LR 함정·앱 배선 0 을 말하고 낡은 문장은 취소선으로 남아 있다. 파일이 git 에 추적되고 워킹트리 clean. 커밋 1개.</done>
</task>

<task type="auto">
  <name>Task 5: .gitignore — 사업 문서·임시 스크린샷·도구 산출물 (추가 2)</name>
  <files>.gitignore</files>
  <action>
CONTEXT.md 추가 2 결정. `.gitignore` **끝에** 아래 블록을 덧붙인다 (기존 항목은 손대지 않는다). 파일은 지우거나 옮기지 않는다 — belle 의 문서다. `docs/openapi.yaml` 과 `reference-downstream-backfill.json` 은 판단 보류이므로 **넣지 않는다**(무시도 커밋도 하지 않는다).

블록 내용 (주석은 기존 파일의 한국어 주석 톤을 따른다):
- 주석: `# 사업 문서 — belle 의 외주용역 제출 서류(사업자등록증·전자세금계산서·용역계약서·견적서, 22MB/32파일). git 추적 금지. 파일은 지우지 말 것.` 다음 줄 `외주용역 제출 서류/`
- 주석: `# 작업용 임시 스크린샷 (2026-09-18 정리)` 다음 줄 `_cap/`
- 주석: `# 도구 산출물 — 다른 머신에서 다시 생성` 다음 줄 `skills-lock.json` 과 `.agents/`

확인 후 `rtk git add .gitignore && rtk git commit -m "chore(quick-260918-k06): .gitignore — 외주용역 제출 서류·_cap·skills-lock·.agents 무시"`.

마지막으로 코드 무접촉 게이트: `git diff --name-only $(cat /tmp/k06-base.txt)..HEAD | grep -cE '^(app|backend)/'` 가 0 이어야 한다. 0 이 아니면 어떤 파일인지 보고하고 멈춘다.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && git check-ignore -q '외주용역 제출 서류/' && git check-ignore -q '_cap/' && git check-ignore -q 'skills-lock.json' && git check-ignore -q '.agents/' && test "$(git status --porcelain | grep -cE '외주용역|_cap/|skills-lock\.json|\.agents/')" -eq 0 && test -d '외주용역 제출 서류' && test -d _cap && test "$(git status --porcelain | grep -c 'docs/openapi.yaml')" -eq 1 && test "$(git status --porcelain | grep -c 'reference-downstream-backfill.json')" -eq 1 && test "$(git diff --name-only $(cat /tmp/k06-base.txt)..HEAD | grep -cE '^(app|backend)/')" -eq 0 && echo TASK5-OK</automated>
  </verify>
  <done>4항목이 무시되어 git status 에서 사라지고 디렉터리는 디스크에 그대로. 보류 2건(openapi.yaml·backfill.json)은 여전히 `??`. BASE..HEAD 에 app/·backend/ 변경 0. 커밋 1개.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로컬 워킹트리 → git 원격 | `git add -A` 한 번으로 사업 문서(사업자등록증·계약서)가 공개 이력에 들어갈 수 있는 경계 |
| 문서 이력 | 판정 근거를 지우면 다음 세션이 다시 묻거나 틀린 판단을 승계한다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-k06-01 | Information Disclosure | `외주용역 제출 서류/` (22MB PII·계약) | mitigate | Task 5 `.gitignore` + `git check-ignore` 게이트. 파일 이동·삭제 금지 |
| T-k06-02 | Tampering (이력 소실) | 09-14 §6 · 09-17 §4 · STATE:103 · TRAINING-DUE | mitigate | 삭제 대신 취소선 + 정정 블록. Task 3/4 verify 가 원문 행 잔존을 grep 으로 확인 |
| T-k06-03 | Tampering (코드 오접촉) | `app/`, `backend/` | mitigate | Task 5 마지막 `git diff BASE..HEAD` 게이트 = 0 |
| T-k06-SC | Tampering | npm/pip/cargo installs | accept | 이 작업에 패키지 설치 없음 (문서 전용) |
</threat_model>

<verification>
- 착수점: `grep -c '260918-day-closeout/HANDOFF.md' CLAUDE.md` ≥ 3, plan.md·CONTINUE 1행 배너.
- 원장: `.planning/quick` 디렉터리 수 == STATE.md 고유 링크 수, 차집합 0, 행 전부 한 구간·내림차순, Pod 표에 quick 행 0.
- 유령: 09-14 §6 / 09-17 §4 취소선 헤딩 + 정정 블록, 원문 표 잔존, STATE:103 인라인 정정, 착수점 HANDOFF §8 8행.
- TRAINING-DUE: 5건 문자열 존재 + `git ls-files` 추적.
- .gitignore: 4항목 check-ignore PASS, 보류 2건 `??` 유지.
- 코드 무접촉: `git diff --name-only BASE..HEAD | grep -cE '^(app|backend)/'` == 0.
- 커밋 5개 (`rtk git log --oneline BASE..HEAD | wc -l` == 5), 이모지 0.
</verification>

<success_criteria>
- 새 세션이 CLAUDE.md → 착수점 HANDOFF 순으로 읽으면 "첫 일 = belle 학원 실증 결과 듣기"에 3초 안에 도달한다.
- STATE.md 가 2026-09-18 을 말하고 quick 원장이 완전하다 (184/184).
- belle 이 이미 답한 8건이 어디에서도 미결로 읽히지 않으며, 왜 닫혔는지 근거(커밋·시각)가 문서에 남는다.
- TRAINING-DUE.md 대로 돌리면 침묵 LR·OOM Pod 로 가지 않는다.
- 사업 문서가 `git add -A` 로 커밋될 경로가 막혀 있다.
- `app/`·`backend/` 무접촉.
</success_criteria>

<output>
Create `.planning/quick/260918-k06-entrypoint-consolidation-1-state/260918-k06-SUMMARY.md` when done. SUMMARY 에는 재계산된 차집합 수(46 예상)와 `(SUMMARY 없음)` 행 수를 실측으로 적는다.
</output>
