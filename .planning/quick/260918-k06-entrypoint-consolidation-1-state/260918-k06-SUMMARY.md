---
phase: quick-260918-k06
plan: 01
subsystem: docs
tags: [entrypoint, state-ledger, handoff, gitignore, training-due]

# Dependency graph
requires:
  - phase: quick-260918-day-closeout
    provides: 2026-09-18 인계서 (새 단일 착수점)
provides:
  - 착수점 1개 (CLAUDE.md §1/§5/§6 -> .planning/quick/260918-day-closeout/HANDOFF.md), plan.md·CONTINUE 는 이력 배너
  - STATE.md quick 원장 완전화 (184/184, 한 표, 내림차순) + frontmatter 2026-09-18
  - 유령 판정 8건 정정 주석 (09-14 §6 · 09-17 §4 · STATE Last activity) + 착수점 HANDOFF §8 정정 원장
  - TRAINING-DUE.md 정정 5건 + git 추적
  - .gitignore 사업 문서·임시물·도구 산출물 차단
affects: [session-start-housekeeping, state-ledger, retrain-trigger]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "정정은 삭제가 아니라 취소선 + 정정 블록 (이력 보존)"
    - "quick 원장 행 설명 = SUMMARY frontmatter title: > 첫 H1 > (SUMMARY 없음)"

key-files:
  created:
    - .planning/TRAINING-DUE.md (최초 git 추적)
  modified:
    - CLAUDE.md
    - plan.md
    - .planning/CONTINUE-2026-09-07.md
    - .planning/STATE.md (워킹트리만 — 오케스트레이터가 커밋)
    - .planning/quick/260914-rot180-gpu-validation/HANDOFF.md
    - .planning/quick/260917-day-closeout/HANDOFF.md
    - .planning/quick/260918-day-closeout/HANDOFF.md
    - .gitignore

key-decisions:
  - "quick 원장 # 열 = 디렉터리명 앞 두 토큰 (260831-apple-signin -> 260831-apple). 플랜의 ${d:0:10} 은 코드 없는 디렉터리에서 '260831-my-' 같은 끊긴 id 를 만들어 보정"
  - "원장 설명은 SUMMARY frontmatter title: 을 H1 보다 우선 — H1 이 '260809-jnb 요약'/'판정 먼저'/'Dependency graph' 류인 문서가 5건"
  - "기존 원장의 중복 행 3건(260714-hv4 x2, 260824-gt1 x2, 260814-l5i / 260814-l5i(b))은 손대지 않음 — 재측정 금지, 범위 밖"
  - "Task 2 는 STATE.md 단독 변경이라 별도 커밋 없음 — 오케스트레이터 제약(STATE.md 커밋 금지)이 플랜의 '태스크당 커밋 1개' 보다 우선"

patterns-established:
  - "착수점 = 일자별 HANDOFF 1개, CLAUDE.md §5 가 가리킨다. plan.md 는 이력"

requirements-completed: [QUICK-260918-k06]

# Metrics
duration: 8min
completed: 2026-09-18
---

# Quick 260918-k06: 착수점 정리 — 진원지 3건 해소 Summary

**착수점 4개 -> 1개(`.planning/quick/260918-day-closeout/HANDOFF.md`), STATE.md quick 원장 138 -> 184 링크(차집합 46 -> 0)를 한 표로 병합, belle 이 이미 답한 유령 판정 8건을 취소선+정정 블록으로 닫고, TRAINING-DUE.md 를 실측(v38·A100·$33~38·SFT_LR·배선 0)으로 고쳐 git 에 올리고, 사업 문서 22MB 의 `git add -A` 유출 경로를 .gitignore 로 막았다. 코드 무접촉 (app/·backend/ diff 0).**

## Performance

- **Duration:** 8 min
- **Started:** 2026-09-18T05:37:23Z
- **Completed:** 2026-09-18T05:46:22Z
- **Tasks:** 5/5
- **Files modified:** 9 (커밋 8 + STATE.md 워킹트리)

## Accomplishments

- 새 세션이 CLAUDE.md 만 읽어도 착수점이 HANDOFF 하나임을 안다 (§1 읽기 순서, §5 현재 할 일, §6 새 플랫폼 세션 시작 3곳 모두 교체). plan.md·CONTINUE-2026-09-07.md 는 1행 배너로 이력임이 드러난다.
- STATE.md quick 원장: `.planning/quick` 184 디렉터리 전부 링크 (누락 46 보충), 두 군데(139행 + Pod 표 끝 4행)로 쪼개져 있던 143행을 `### Quick Tasks Completed` 한 표 189행으로 병합, # 열 내림차순. `(SUMMARY 없음)` 10행이 미종결로 드러난다.
- 유령 판정 8건: 09-14 §6(4건) · 09-17 §4(2건) 헤딩 취소선 + 정정 블록(커밋·시각 근거), STATE Last activity 문단 인라인 정정(#7), 착수점 HANDOFF §8 에 8행 정정 원장(8번 = 메모리 범위 밖, 기록만).
- TRAINING-DUE.md: Pod 5090 -> A100 PCIe 80GB(OOM 실측), SFT_LR=1e-5 함정, 한 사이클 $33~38 vs 잔액 $18.90, v29 -> v38(승격 0회), 앱 배선 0건. 낡은 문장은 취소선.
- .gitignore: `외주용역 제출 서류/`·`_cap/`·`skills-lock.json`·`.agents/` 차단. 파일은 디스크에 그대로. 보류 2건(openapi.yaml·backfill.json)은 `??` 유지.

## Task Commits

1. **Task 1: 착수점 1개 통합** - `141aa35b` (docs) — CLAUDE.md, plan.md, CONTINUE-2026-09-07.md (STATE.md 변경은 워킹트리)
2. **Task 2: STATE.md 원장 복구** - 커밋 없음 (STATE.md 단독 변경, 오케스트레이터 일괄 커밋 대상)
3. **Task 3: 유령 판정 8건 정정 주석** - `8a6a1afc` (docs) — 09-14/09-17/09-18 HANDOFF (STATE.md #7 인라인은 워킹트리)
4. **Task 4: TRAINING-DUE 정정 5건** - `994fd341` (docs)
5. **Task 5: .gitignore** - `b47e53b5` (chore)

BASE = `d96d18e5` (`/tmp/k06-base.txt`). `git log --oneline d96d18e5..HEAD | wc -l` = **4** (플랜 예상 5 — Task 2 커밋 제외 사유는 위).

## 실측 수치 (플랜 output 요구)

| 항목 | 값 |
|---|---|
| `.planning/quick` 디렉터리 수 | 184 |
| 작업 전 STATE.md 고유 링크 | 138 |
| 차집합 (누락) | **46** (플래너 예고대로 45 가 아니라 46 — k06 자신 포함) |
| 작업 후 고유 링크 | 184 (차집합 0) |
| 원장 행 수 | 189 (기존 143 + 신규 46; 기존 중복 3건 보존) |
| `(SUMMARY 없음)` 행 | **10** (신규 46 중. 260818-lik·muy·nc2·nnm, 260831-apple·kipup·my·pair·posture·review) |
| 누락분 중 6~7월 것 | 5 (260629-cej, 260705-r6v·r6x·wbs, 260715-wq9) — "전부 최근 2주" 아님 |
| STATE.md 워킹트리 diff | 190 insertions, 147 deletions |

## Verification — 명령과 출력

### Task 1

```
$ test "$(grep -c '260918-day-closeout/HANDOFF.md' CLAUDE.md)" -ge 3 && test "$(grep -c '현재 할 일.*/plan.md' CLAUDE.md)" -eq 0 && test "$(head -n 1 plan.md | grep -c '이 파일은 이력이다')" -eq 1 && test "$(head -n 1 .planning/CONTINUE-2026-09-07.md | grep -c '이 파일은 이력이다')" -eq 1 && sed -n '1,15p' .planning/STATE.md | grep -q '^stopped_at: "2026-09-18' && sed -n '1,15p' .planning/STATE.md | grep -q '^last_activity: 2026-09-18' && sed -n '1,15p' .planning/STATE.md | grep -q '^last_updated: "2026-09-18T' && grep -q '2026-09-18 정리 (quick-260918-k06)' .planning/STATE.md && echo TASK1-OK
TASK1-OK
```

### Task 2

(a)~(c) 실행 결과 (중간 파일은 scratchpad):
```
$ wc -l all.txt linked.txt missing.txt
     184 all.txt
     138 linked.txt
      46 missing.txt
$ wc -l oldrows.txt ledger.txt
     143 oldrows.txt
     189 ledger.txt
```
게이트:
```
$ ls -1 .planning/quick | sort > all2.txt && grep -oE '\]\(\./quick/[0-9a-z-]+/\)' .planning/STATE.md | sed 's|](\./quick/||; s|/)||' | sort -u > linked2.txt && test "$(comm -13 linked2.txt all2.txt | wc -l | tr -d ' ')" -eq 0 && test "$(wc -l < linked2.txt | tr -d ' ')" -eq "$(wc -l < all2.txt | tr -d ' ')" && test "$(awk '/^### Quick Tasks Completed/{f=1;next} /^### /{f=0} f && /^\| 26[0-9]{4}-/{n++} END{print n+0}' .planning/STATE.md)" -eq "$(grep -cE '^\| 26[0-9]{4}-' .planning/STATE.md)" && grep -E '^\| 26[0-9]{4}-' .planning/STATE.md | cut -d'|' -f2 | sort -cr && test "$(sed -n '/^### Quick Tasks Completed/,/^### Plan 09-01/p' .planning/STATE.md | grep -c '^$')" -eq 2 && test "$(sed -n '/^### 이전 Pod 이력/,/^### 남은 작업/p' .planning/STATE.md | grep -cE '^\| 26[0-9]{4}-')" -eq 0 && echo TASK2-OK
TASK2-OK
all=184 linked=184 rows=189 no_summary=10
```
(260913-vqr 행 설명 보정 뒤 재실행에서도 `TASK2-OK`.)

### Task 3

```
$ A=.planning/quick/260914-rot180-gpu-validation/HANDOFF.md && B=.planning/quick/260917-day-closeout/HANDOFF.md && E=.planning/quick/260918-day-closeout/HANDOFF.md && grep -q '^## 6\. ~~belle 판정 대기 — 4건~~' "$A" && test "$(grep -c '정정 (2026-09-18, quick-260918-k06)' "$A")" -eq 1 && grep -q '222e9c87' "$A" && test "$(grep -c '^| \*\*1\*\* | \*\*회전 + 기준 11편 재추출' "$A")" -eq 1 && grep -q '^## 4\. ~~belle 판정 대기 — 2건~~' "$B" && test "$(grep -c '정정 (2026-09-18, quick-260918-k06)' "$B")" -eq 1 && grep -q '5d6deef1' "$B" && test "$(grep -c '^| \*\*1\*\* | \*\*확대 카드' "$B")" -eq 1 && test "$(grep '^Last activity: 2026-09-18' .planning/STATE.md | grep -c '정정 2026-09-18: 이 문장(커밋 d3afd4e9')" -eq 1 && grep -q '^## 8\. 유령 판정 정정 원장' "$E" && test "$(sed -n '/^## 8\. 유령 판정 정정 원장/,$p' "$E" | grep -cE '^\| [1-8] \|')" -eq 8 && grep -q '메모리 파일 — 리포 밖' "$E" && echo TASK3-OK
TASK3-OK
```

### Task 4

```
$ T=.planning/TRAINING-DUE.md && grep -q 'A100 PCIe 80GB' "$T" && grep -q '~~belle 이 5090 이상' "$T" && grep -q 'SFT_LR=1e-5' "$T" && grep -q 'v34_cycle.sh:45' "$T" && grep -qE '\$33~38' "$T" && grep -q '18.90' "$T" && grep -q 'v38' "$T" && grep -q '~~v29~~' "$T" && grep -q 'promoted=false' "$T" && grep -q '배선이 0건' "$T" && grep -q '513' "$T" && git ls-files --error-unmatch "$T" >/dev/null && test "$(git status --porcelain "$T" | wc -l | tr -d ' ')" -eq 0 && echo TASK4-OK
TASK4-OK
```

### Task 5 + 코드 무접촉 게이트

```
$ git check-ignore -q '외주용역 제출 서류/' && git check-ignore -q '_cap/' && git check-ignore -q 'skills-lock.json' && git check-ignore -q '.agents/' && test "$(git status --porcelain | grep -cE '외주용역|_cap/|skills-lock\.json|\.agents/')" -eq 0 && test -d '외주용역 제출 서류' && test -d _cap && test "$(git status --porcelain | grep -c 'docs/openapi.yaml')" -eq 1 && test "$(git status --porcelain | grep -c 'reference-downstream-backfill.json')" -eq 1 && test "$(git diff --name-only $(cat /tmp/k06-base.txt)..HEAD | grep -cE '^(app|backend)/')" -eq 0 && echo TASK5-OK
TASK5-OK
$ echo "BASE=$(cat /tmp/k06-base.txt)"; git diff --name-only $(cat /tmp/k06-base.txt)..HEAD | grep -cE '^(app|backend)/'
BASE=d96d18e5dde73b33d90de65ca90840f7cb6843d4
0
$ git diff --diff-filter=D --name-only d96d18e5..HEAD
(없음)
$ git status --short
 M .planning/STATE.md
?? .planning/quick/260918-k06-entrypoint-consolidation-1-state/
?? docs/openapi.yaml
?? reference-downstream-backfill.json
```

### 이모지 검사 (내가 쓴 줄만)

```
committed_added_lines=84 hits=0
new_rows=46 hits=0
state_authored_lines=5 hits=0
```
넓은 검사에서 잡힌 19줄은 전부 기존 원장 행·Last activity 문단이 자리만 옮긴 것(`↔` U+2194 x14, `⚠` U+26A0 x16 — 원문에 이미 있던 문자). 내가 추가한 문자 아님.

## Decisions Made

- 원장 # 열: 디렉터리명 앞 두 토큰. 플랜의 `${d:0:10}` 은 `260831-my-tab-login-entry` 를 `260831-my-` 로 끊어 보정 (discretion 범위: 열 구성).
- 원장 설명: frontmatter `title:` > 첫 H1 > `(SUMMARY 없음)`. `260913-vqr` 는 title 도 없고 H1 이 "Dependency graph"라 SUMMARY 본문 첫 문장으로 채움 (플랜 3단계 그대로).
- 260918-0q8·260918-gate 행의 Date 는 frontmatter `date: 2026-09-17` 을 따름 (플랜 (b) 규칙).
- 기존 원장 중복 행 3건 보존 (재측정·정리 범위 밖).
- SDK `state.advance-plan`/`record-metric`/`roadmap.update-plan-progress`/`requirements.mark-complete` 는 실행하지 않음 — quick 작업이고 STATE.md·ROADMAP.md 는 오케스트레이터 관할(제약). STATE.md 본문은 Task 1~3 이 직접 고쳤다.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] 플랜 (b) 셸 루프가 zsh 에서 glob 을 안 펼쳐 46행 전부 `(SUMMARY 없음)` 이 됨**
- **Found during:** Task 2 (b)
- **Issue:** Bash 도구 셸이 zsh 라 `ls "$dir"/$pat` 의 `$pat`(`*SUMMARY.md`) 이 글롭되지 않아 문서 탐지가 항상 실패했다 (`ls: .../*HANDOFF.md: No such file or directory`).
- **Fix:** (b) 를 같은 알고리즘의 python3 (`glob.glob`) 로 다시 돌렸다. 결과 46행 중 `(SUMMARY 없음)` 10행(실제로 SUMMARY/HANDOFF/FINDINGS 가 없는 디렉터리).
- **Files modified:** scratchpad 스크립트만 (리포 무접촉)
- **Verification:** TASK2-OK
- **Committed in:** 해당 없음 (STATE.md 워킹트리)

**2. [Rule 1 - Bug] `260913-vqr` 행 설명이 "Performance" (frontmatter 안 `# Dependency graph` 다음 `##` 헤딩)**
- **Found during:** Task 2 step 3 (newrows 눈으로 훑기)
- **Fix:** SUMMARY 본문 첫 문장으로 교체. 재검증 TASK2-OK.

### 제약에 따른 조정 (오케스트레이터 지시)

- **Task 2 커밋 없음.** 오케스트레이터 제약 "STATE.md 는 커밋하지 마라" 에 따라 Task 1·3 의 STATE.md 변경도 커밋에서 뺐다. 결과 커밋 4개 (플랜 verification 의 "커밋 5개" 와 1 차이). STATE.md 는 `M` 상태로 워킹트리에 있다 (190+/147-).

---

**Total deviations:** 2 auto-fixed (Rule 3 x1, Rule 1 x1) + 제약 조정 1
**Impact on plan:** 결과물은 플랜과 동일. 코드 무접촉 유지.

## Issues Encountered

- 기존 원장에 같은 디렉터리를 두 번 가리키는 행 3쌍이 있었다 (143행 vs 고유 링크 138 의 차이 원인 중 3). 범위 밖이라 보존. 다음 정리 때 판단.

## 정리하지 않은 것 (의도)

- 메모리 디렉터리(`~/.claude/projects/.../memory/`) 무접촉 — 유령 #8 은 착수점 HANDOFF §8 에 기록만.
- `docs/openapi.yaml`, `reference-downstream-backfill.json` — 판단 보류, `??` 그대로.
- STATE.md progress 수치(38/30/4/4, 118/108) — 재측정 안 함, Current Position 주석에 명시.

## Next Steps

- 오케스트레이터: STATE.md + 이 SUMMARY/PLAN/CONTEXT 일괄 커밋.
- 다음 세션 첫 일 = belle 학원 실증 결과 듣기 (착수점 HANDOFF §5).

## Self-Check: PASSED

- 파일 10개 존재 확인 (위 목록)
- 커밋 4개 존재 확인: 141aa35b · 8a6a1afc · 994fd341 · b47e53b5
