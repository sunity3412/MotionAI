---
phase: quick-260918-qm2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - .planning/PROJECT.md
  - .planning/codebase/STACK.md
  - .planning/codebase/ARCHITECTURE.md
  - app/src/types/analysis.ts
  - ml/ml_CLAUDE.md
  - docs/contract.md
  - backend/shared/python/sunity_shared/analysis/pose_engines/__init__.py
  - docs/reference-motions.md
  - .planning/quick/260918-gpx-gate-false-suppress/DEMO-CARD.md
  - .claude/commands/start.md
  - .planning/quick/260906-n2j-stage-1-advisory/evidence/POD-RUNBOOK-2026-09-06.md
  - backend/runpod_inference/README.md
  - docs/runpod-fast-restart.md
  - design.md
  - app/CLAUDE.md
  - .planning/STATE.md
  - .planning/debug/illustration-slot-crop.md
  - .planning/debug/viewer-axis-flat-skeleton.md
  - .planning/debug/kipup-split-injection-lost.md
  - .planning/debug/recognizer-ipsf-fallback.md
  - .planning/debug/inversion-joint-attribution.md
  - .planning/debug/ref-student-substrate-gap.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/quick/260918-qm2-doc-truth-repair-18/260918-qm2-BELLE-JUDGMENT.md
  - .planning/quick/260918-day-closeout/HANDOFF.md
autonomous: true
requirements: [QUICK-260918-qm2]

must_haves:
  truths:
    - "새 세션이 CLAUDE.md·ml_CLAUDE.md·contract.md·analysis.ts 를 읽으면 운영 포즈 엔진이 YOLOX-m + RTMW-x 133(COCO-17 변환)이고 NLF/ViTPose 는 운영 경로에 없다는 것을 안다 (A-1·A-2·A-11·A-12·A-13·A-18)"
    - "docs/reference-motions.md §6 을 그대로 따르면 신규 기준 모션이 rot180_v1 과 같은 기질(ROT180_INVERSION_ENABLED=1)로 들어가고, 절차가 잠정이며 Phase 21 이 자동화 대상이라는 것과 /reference/auto-register 의 한계가 적혀 있다 (A-4 반려 반영)"
    - "docs/reference-motions.md §3·§5 가 camelCase 11편이고, §4 는 clipRange 의 실제 소비처(공유 베이스 경계)만 남기며 checkpoints 가중평균·heroFrameUrl 규칙이 없다 (A-3·A-5·A-6·A-7)"
    - "Pod 종료 절차 3곳이 'Lambda RUNPOD_ANALYZE_URL 을 자리표시자로 되돌린다(값 삭제·SSM 삭제 금지) + Pod 없으면 분석은 어차피 실패(CPU 폴백 ImportError)' 라고 말한다 (A-9 반려 반영)"
    - "DEMO-CARD 가 죽은 Pod 을 가리키지 않고 다운로드 시간이 156초 하나로 통일돼 있다 (A-8)"
    - "runpod README 가 '시연 때만 기동 / RTMW / ROT180 은 2026-09-17 이후 운영 Pod 에서 ON(코드 기본 off, start_server.sh 한 곳)' 을 말한다 (A-16, '기본 ON' 표현 없음)"
    - "STATE.md 에 미종결 트랙 판정 원장이 있고 22-06·36-01 은 '완료(원장 표기만 누락)' 이지 '사문' 이 아니다 (묶음 B)"
    - "ROADMAP 에서 SUMMARY 가 실재하는 미체크 플랜이 0 이고, REQUIREMENTS 의 SCORE-10 이 Complete, 나머지 Pending 이 '재판정 대상' 으로 표시돼 있다"
    - "belle 판정 대기 5건이 BELLE-JUDGMENT.md 한 파일에 있고 HANDOFF 가 그것을 가리킨다 (묶음 C)"
    - "app/·backend/ 의 diff 는 주석/docstring 3파일(analysis.ts, runpod_inference/README.md, pose_engines/__init__.py)뿐이고 backend/template.yaml 은 무접촉이다"
  artifacts:
    - path: "docs/reference-motions.md"
      provides: "§3 camelCase 스키마 · §4 정정 · §5 11편 · §6 잠정 절차(ROT180 env 포함) · §7 ViTPose 항목 제거"
      contains: "ROT180_INVERSION_ENABLED=1"
    - path: ".planning/STATE.md"
      provides: "미종결 트랙 판정 원장 (2026-09-18, quick-260918-qm2)"
      contains: "미종결 트랙 판정 원장"
    - path: ".planning/quick/260918-qm2-doc-truth-repair-18/260918-qm2-BELLE-JUDGMENT.md"
      provides: "belle 판정 대기 5건 (기록만)"
      min_lines: 40
    - path: "backend/runpod_inference/README.md"
      provides: "시연 때만 기동 / RTMW / ROT180 운영 ON 문구"
      contains: "2026-09-17"
    - path: "CLAUDE.md"
      provides: "§3 ML 스택 RTMW 표기 + §5 실재 파일 경로"
      contains: "RTMW"
  key_links:
    - from: "docs/reference-motions.md §6"
      to: "backend/runpod_inference/start_server.sh:24"
      via: "ROT180_INVERSION_ENABLED=1 을 reprocess 명령줄에 명시"
      pattern: "ROT180_INVERSION_ENABLED=1 python backend/scripts/reprocess_reference_motions_phase4.py"
    - from: ".planning/quick/260918-day-closeout/HANDOFF.md"
      to: "260918-qm2-BELLE-JUDGMENT.md"
      via: "§0 요약 + §5 후보 표 링크"
      pattern: "260918-qm2-BELLE-JUDGMENT.md"
    - from: ".planning/STATE.md 판정 원장"
      to: ".planning/debug/*.md"
      via: "각 debug 문서 상단 정정 블록이 원장을 가리킴"
      pattern: "quick-260918-qm2"
---

<objective>
문서 18곳을 코드와 일치시키고(묶음 A, A-4·A-9 는 반려 반영 문구로), 미종결 트랙 판정을 STATE·ROADMAP·REQUIREMENTS 원장에 박고(묶음 B, "완료"는 "사문"이 아니다), belle 판정 대기 5건을 한 파일에 모아 인계서에서 가리킨다(묶음 C).

Purpose: 새 세션이 첫 3분에 읽는 문서(CLAUDE.md·ml_CLAUDE·contract·analysis.ts)가 폐기된 백본(NLF/ViTPose/YOLO11)을 가르치고, 기준 모션 등록 런북이 그대로 따르면 기질 혼합(정은지 자기비교 100 -> 60)을 만들며, Pod 종료 절차가 Lambda 를 죽은 주소로 남긴다. 판정이 끝난 트랙이 원장에서 미결로 보여 다시 조사된다. 이 세 가지를 코드 무접촉으로 닫는다.

Output: 6개 원자 커밋. 소스 diff 는 주석/docstring 3파일뿐.
</objective>

<execution_context>
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kimtaesung/Dev/SunityMotion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260918-qm2-doc-truth-repair-18/260918-qm2-CONTEXT.md
@.planning/quick/260918-qm2-doc-truth-repair-18/260918-qm2-FINDINGS.md
@CLAUDE.md

읽는 규칙: CONTEXT 가 잠긴 결정이다. FINDINGS 는 실측 원문 — 각 태스크가 지정한 절(줄 범위)만 읽는다. 재조사·재판정 금지.

전역 규칙 (모든 태스크 공통):
- 이모지 금지 (CLAUDE.md §7). 새로 쓰는 텍스트에 ⚠/✅/❌/⭐ 류 기호도 넣지 않는다. 배너는 `> **주의 (2026-09-18, quick-260918-qm2):** ...` 형식. FINDINGS 의 제안 문구에 들어 있는 ⚠·✅ 는 옮길 때 뺀다. 기존 문서에 이미 있는 기호는 건드리지 않는다.
- 정정은 삭제가 아니라 정정으로. 사문 문서는 지우지 말고 상단 배너. 변경 이력(`*vN:*` 줄 등)은 지우지 않는다.
- `backend/template.yaml` 무접촉. `app/`·`backend/` 소스는 Task 1 의 analysis.ts 주석 4곳·pose_engines/__init__.py docstring 1곳, Task 3 의 runpod_inference/README.md 외에 손대지 않는다. 각 태스크 verify 가 `git diff --stat -- app backend` 로 이를 검사한다.
- 태스크 1개 = 커밋 1개. 커밋 메시지 접두 `docs(quick-260918-qm2):`. 커밋은 `gsd-sdk query commit "<msg>" --files <paths>` 로 (rtk 로 감싸지 말 것).
- 이모지 검사(각 태스크 verify 에 포함): 스테이징 전 작업 트리 diff 의 추가 줄에서 U+1F300-U+1FAFF·U+2705·U+274C·U+26A0·U+2B50 이 0 이어야 한다.
- STATE.md `### Quick Tasks Completed` 표의 260918-qm2 행은 이 플랜에서 쓰지 않는다 — 오케스트레이터 마감 단계가 커밋 해시와 함께 추가한다.
</context>

<tasks>

<task type="auto">
  <name>Task 1: 포즈 엔진 표기 전수 정정 — CLAUDE.md + 재생성 원본 3 + analysis.ts + ml_CLAUDE + contract + pose_engines docstring + CLAUDE.md §5 경로 (A-1·A-2·A-11·A-12·A-13·A-14·A-18)</name>
  <files>CLAUDE.md, .planning/PROJECT.md, .planning/codebase/STACK.md, .planning/codebase/ARCHITECTURE.md, app/src/types/analysis.ts, ml/ml_CLAUDE.md, docs/contract.md, backend/shared/python/sunity_shared/analysis/pose_engines/__init__.py</files>
  <read_first>
    - FINDINGS `## w1:문서어긋남` 항목 1·2·11·12·13·14·18 (줄 145-158, 215-240, 264-270) — before/after 원문
    - backend/runpod_inference/start_server.sh:24-31 (실제 env 이름: RTMW_ONNX_PATH·YOLOX_ONNX_PATH·RTMW_DEVICE·ROT180_INVERSION_ENABLED·PR_INVERSION_ENABLED·RTMW_DETERMINISTIC)
    - backend/functions/pipeline/requirements.txt:1-4 (CPU 폴백이 ImportError 로 차단된 근거)
  </read_first>
  <action>
현행 사실(FINDINGS #1 근거): 운영 엔진 = `RTMWPoseEngine`(pipeline/app.py:1272-1348 `_RTMWNlfCompat`), 검출기 = YOLOX-m onnx(rtmw_engine.py:51-54,157), 기본값 `_DEFAULT_POSE_ENGINE = "RTMW"`(config.py:27), 운영 가중치 = `rtmw-x-384x288`(commercial_ok) 1건. NLF 는 라이선스 차단(Max Planck 비상업) + backend/research·verify_nlf_pipeline.py 잔재, 클래스 `NlfPoseEngine` 은 존재하지 않고 실물은 pose_estimator.py 의 `NlfPoseEstimator`(interfaces.py:6 DEPRECATED).

1. **CLAUDE.md 손수 쓴 부분** (GSD 마커 128행 이전):
   - :48 `ML        : YOLO11 → ViTPose-S → MotionDTW (FastDTW)` → `ML        : YOLOX-m(검출) → RTMW-x 133 wholebody → COCO-17 → MotionDTW (Sakoe-Chiba band)`.
   - 코드펜스가 닫히는 줄(:53) 바로 뒤, 기존 `> ⚠️ 서니티에는...` 인용 앞에 한 줄 추가: `> NLF/ViTPose 는 운영 경로에 없다 — NLF 는 backend/research/ 와 backend/scripts/verify_nlf_pipeline.py 에만 남은 R&D 잔재이고 상업 라이선스가 막혀 있다. 운영 백본 정본 = backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/.`
   - §5 (:80-85, A-14): `/ml/CLAUDE.md` → `/ml/ml_CLAUDE.md`; `/backend/CLAUDE.md` → `/backend/runpod_inference/README.md + /backend/template.yaml (전용 CLAUDE.md 없음)`; `/docs/principles.md` 줄은 `개발 원칙/에이전트   → CLAUDE.md §7 (docs/principles.md 는 없음)` 로 바꾼다. 열 정렬은 주변 줄과 맞춘다.
2. **재생성 원본 3파일** (A-2) — 다음 `/gsd:docs-update` 가 되돌리지 않도록 원본을 먼저 고친다:
   - `.planning/PROJECT.md` :21 `RunPod NLF 3D 실분석 end-to-end (YOLO11n → NLF 3D → band-constrained DTW)` → `RunPod GPU 실분석 end-to-end (YOLOX-m → RTMW-x 133 → COCO-17 → band-constrained DTW; 2026-05-29 NLF 로 최초 통과, 이후 RTMW 로 교체)`; :74 `현 포즈 = NLF 3D, COCO-17 8관절각 기반` → `현 포즈 = RTMW-x 133 wholebody → COCO-17 변환, 8관절각(JOINT_KEYS) 기반`; :82 `YOLO11→NLF 3D→MotionDTW` → `YOLOX→RTMW-x 133→MotionDTW`; :86 `NLF 3D는 CUDA 필수 (CPU에서 NaN)` → `RTMW ONNX 추론은 CUDA 필요(onnxruntime-gpu)`.
   - `.planning/codebase/STACK.md` :38 → `CUDA required — RTMW ONNX 추론은 onnxruntime-gpu 로 돈다 (backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/rtmw_engine.py). NLF CPU-NaN 서술은 폐기된 백본 이야기`; :93 `ultralytics ... YOLO11n person bounding-box detection (pose_estimator.py)` → `YOLOX-m ONNX (rtmlib) - person bounding-box detection (rtmw_engine.py). ultralytics 는 requirements 에 남아 있으나 운영 경로 호출 0`; :94 NLF 줄 → `RTMW-x 133 wholebody (rtmlib ONNX, Apache-2.0) - 포즈 백본, 133 → COCO-17 변환; 가중치 /workspace/rtmw_weights/rtmw-x-384.onnx. NLF TorchScript 는 backend/research 전용 잔재`; :123 `optional NLF_MODEL_PATH/YOLO_WEIGHTS_PATH` → `RTMW_ONNX_PATH / YOLOX_ONNX_PATH / RTMW_DEVICE / ROT180_INVERSION_ENABLED / PR_INVERSION_ENABLED / RTMW_DETERMINISTIC (정본 backend/runpod_inference/start_server.sh)`; :141 `NLF model loaded into VRAM` → `RTMW-x ONNX loaded into VRAM`; :151 → `ViTPose-S / NLF - 운영 경로에 없음. 백본은 RTMW-x 133 (pose_engines/rtmw). NLF 는 backend/research + pose_estimator.py(DEPRECATED) 잔재, 상업 라이선스 차단`.
   - `.planning/codebase/ARCHITECTURE.md` :37 `NLF 3D pose` → `RTMW-x pose`; :56 `(NLF 3D pose)` → `(RTMW-x 133 pose)`; :58 `ffmpeg / NLF / Cerebras` → `ffmpeg / RTMW / Cerebras`; :71 `torch/NLF` → `rtmlib+onnxruntime`; :114 `PoseEstimator (NLF 3D)` → `PoseEstimator (RTMW-x 133 via _RTMWNlfCompat)`; :115 `torch/NLF` → `rtmlib/onnxruntime-gpu`; :128 `Fallback path (dev): Lambda runs _process directly` 끝에 ` — 배포 환경에선 ImportError 로 의도적 차단(pipeline/requirements.txt:1-4), 폴백 없음` 추가; :129 `NLF 3D keypoints` → `RTMW COCO-17 keypoints`; :195 `(NLF holds GPU VRAM)` → `(RTMW ONNX holds GPU VRAM)`; :200 → `**GPU requirement:** RTMW ONNX inference requires CUDA; the Lambda CPU fallback path is blocked by ImportError in deploy (pipeline/requirements.txt) — there is no fallback`; :225 `ffmpeg/torch/NLF` → `ffmpeg/onnxruntime/rtmlib`.
3. **CLAUDE.md 자동생성 블록 미러** (마커 128-148 · 150-258 · 393-535 안): 위 원본과 같은 문장이 CLAUDE.md 에 복제돼 있다(:140·:144 ← PROJECT; :177·:214·:215·:235·:248·:256 ← STACK; :414·:416·:426·:458·:459·:506·:511·(폴백 줄) ← ARCHITECTURE). 각 줄을 원본과 **동일 문구**로 맞춘다. `<!-- GSD:...-->` 마커 자체는 건드리지 않는다.
4. **app/src/types/analysis.ts 주석 4곳** (A-11, 주석만): :98 `// YOLO11 + ViTPose-S` → `// YOLOX 사람검출 + RTMW-x 133 wholebody → COCO-17`; :156 `// ViTPose 17 keypoint 이름` → `// COCO-17 keypoint 이름`; :1202 동일 치환; :1226 `// NLF 추출 시퀀스` → `// RTMW 추출 시퀀스`. 타입·값 변경 0.
5. **ml/ml_CLAUDE.md** (A-12): :19-21 의 `2D 포즈 추정(ViTPose 포함)은 ... NLF 로 전환 ...` 문단을 `2D 포즈 추정 단독은 접힌 인버트에서 천장. 운영 백본 = RTMW-x 133 wholebody(rtmlib ONNX, Apache-2.0) + YOLOX-m 검출; 역립은 ROT180 회전 2-pass(ROT180_INVERSION_ENABLED, 2026-09-17 운영 ON)와 시간축 보간으로 메운다. NLF 3D HMR 은 라이선스(Max Planck 비상업)로 폐기, backend/research 전용.` 로; :29-31 표를 `| YOLOX-m (onnx, rtmlib) | 인체 bbox 탐지 (RTMW 입력 박스, Apache-2.0) | 현행 |` / `| RTMW-x 133 wholebody (rtmlib ONNX) | 포즈 백본 — 133 → COCO-17 변환 + score | 현행 |` / `| NLF / ViTPose-S/H | 라이선스·정확도 사유로 폐기 — NLF 는 backend/research 전용 | - |` 로; :38-39 도식을 `→ YOLOX-m (인체 bbox 탐지)` / `→ RTMW-x 133 → COCO-17 어댑터 (17 keypoint + score)` 로, :40 `3D 관절각 계산 (compute_joint_angles — 투영 왜곡 자유)` 는 `관절각 계산 (compute_joint_angles; 저장 joints3d 의 z 는 전부 0 — 사실상 2D)` 로.
6. **docs/contract.md:38** (A-13): `YOLO11 → ViTPose-S → MotionDTW → KISMAM → Cerebras` → `YOLOX-m → RTMW-x 133 → COCO-17 → MotionDTW → KISMAM → Cerebras`.
7. **pose_engines/__init__.py:6** (A-18, docstring 한 줄): `  - NlfPoseEngine: R&D 격리 (backend/research/pose_engines/nlf/ — 제품 import 경로 밖)` → `  - NLF: 클래스는 없다. sunity_shared/analysis/pose_estimator.py 의 NlfPoseEstimator 가 DEPRECATED 로 남아 있고(interfaces.py:6), 호출처는 backend/research/ 와 backend/scripts/verify_nlf_pipeline.py 뿐 — 제품 파이프라인 호출 0건.` 코드 변경 0.

커밋: `docs(quick-260918-qm2): 포즈 엔진 표기 전수 정정 — NLF/ViTPose/YOLO11 → YOLOX-m + RTMW-x 133 (CLAUDE.md·재생성 원본·analysis.ts·ml_CLAUDE·contract·docstring)`.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && test "$(grep -n 'YOLO11 → ViTPose-S\|YOLO11→NLF 3D\|NLF 3D는 CUDA\|YOLO11n → NLF 3D\|현 포즈 = NLF 3D\|YOLO11n person\|superseded by NLF\|NLF 3D pose\|NLF 3D keypoints\|NLF holds GPU\|NLF 3D pose inference\|/ml/CLAUDE.md\|/backend/CLAUDE.md\|/docs/principles.md' CLAUDE.md .planning/PROJECT.md .planning/codebase/STACK.md .planning/codebase/ARCHITECTURE.md | wc -l | tr -d ' ')" -eq 0 && test "$(grep -c 'RTMW' CLAUDE.md)" -ge 4 && test "$(grep -n 'ViTPose 17\|NLF 추출\|YOLO11 + ViTPose' app/src/types/analysis.ts | wc -l | tr -d ' ')" -eq 0 && test "$(grep -c 'YOLO11 → ViTPose-S' docs/contract.md)" -eq 0 && test "$(grep -c 'backend/research/pose_engines/nlf' backend/shared/python/sunity_shared/analysis/pose_engines/__init__.py)" -eq 0 && test "$(grep -c 'NLF.*| 현행 |\|YOLO11n.*| 현행 |' ml/ml_CLAUDE.md)" -eq 0 && test "$(grep -c 'RTMW-x 133.*| 현행 |' ml/ml_CLAUDE.md)" -eq 1 && python3 -c "import ast,sys; ast.parse(open('backend/shared/python/sunity_shared/analysis/pose_engines/__init__.py').read())" && test "$(git diff --stat -- app backend | grep -c '|')" -eq 2 && git diff --stat -- app backend | grep -q 'app/src/types/analysis.ts' && git diff --stat -- app backend | grep -q 'pose_engines/__init__.py' && test "$(git diff -U0 | grep '^+' | python3 -c "import sys,re; print(len(re.findall(r'[\U0001F300-\U0001FAFF✅❌⚠⭐]', sys.stdin.read())))")" -eq 0 && (cd app && rtk npm run typecheck) && echo TASK1-OK</automated>
  </verify>
  <done>4개 문서 + CLAUDE.md 미러에 폐기 백본 표기 0, §5 가 실재 파일만 가리킴, analysis.ts 주석 4곳·docstring 1곳만 소스 diff, tsc clean, 이모지 추가 0. 커밋 1개.</done>
</task>

<task type="auto">
  <name>Task 2: docs/reference-motions.md 전면 수리 — §2·§3 camelCase, §4 정정, §5 11편, §6 잠정 절차(A-4 반려 반영), §7 ViTPose 삭제, 이력 v7 (A-3·A-4·A-5·A-6·A-7)</name>
  <files>docs/reference-motions.md</files>
  <read_first>
    - FINDINGS `## w1:문서어긋남` 항목 3·4·5·6·7 (줄 159-193) + `## 비평가 반려` 의 "문서 수정안 #4"(줄 317-323)·"놀란것: §4 clipRange 살아 있다"(파일 끝 단락)
    - CONTEXT `### ★ A-4 반려` 전체 + A-6 정정(clipRange 살아 있음)
    - app/src/types/analysis.ts 1180-1290 (`ClipRange`·`Checkpoint`·`ReferenceMotion` 인터페이스 — §3 의 정본)
    - app/scripts/seed-reference-motions.mjs 274-429 (6블록 원값) + 473-499 (Firestore 에 실제로 쓰는 키)
    - backend/scripts/reprocess_reference_motions_phase4.py·backfill_reference_downstream.py·promote_reference_version.py 의 argparse 절 (`grep -n "add_argument" <파일>`) — §6 에 적을 플래그 이름 확인
    - backend/shared/python/sunity_shared/analysis/pose_engines/rtmw/rtmw_engine.py:60-68 (ROT180 env 읽는 자리)
  </read_first>
  <action>
한 파일을 §2→§3→§4→§5→§6→§7→이력 순서로 고친다. 기존 5블록·§8·이력은 삭제하지 않는다.

1. **키 표기 통일 (A-5)**: 문서 전체에서 스키마 키를 camelCase 로 바꾼다 — `entry_type`→`entryType`, `entry_description`→`entryDescription`, `clip_range`→`clipRange`, `prep_start_s`→`prepStartS`, `exec_start_s`→`execStartS`, `exec_peak_s`→`execPeakS`, `land_end_s`→`landEndS`, `recommended_record_s`→`recommendedRecordS`, `shared_base_motion_id`→`sharedBaseMotionId`, `base_until_s`→`baseUntilS`. §2 등록 규칙·§3·§5 기존 5블록·§7 항목까지 전부. 단 `*vN:*` 이력 줄(문서 끝)은 그대로 둔다(이력). sed 로 하되 diff 를 눈으로 훑어 이력 줄이 안 바뀌었는지 본다.
2. **§3 데이터 스키마**: TypeScript 블록을 analysis.ts 의 `ClipRange`·`Checkpoint`·`ReferenceMotion` 을 정본으로 다시 적는다. 반드시 들어갈 실사용 필드: `entryType?`, `entryDescription?`, `clipRange?`, `checkpoints?`, `sharedBaseMotionId?`, `baseUntilS?`, `updatedAt?`, `isActive`, `videoS3Key`, `videoUrlExpiresAt?`, `angles?`(flat, T*J), `anglesJointKeys?`, `anglesFrames?`, `meanAngles?`, `bodyNormalizationProfile?`, `bodyComparisonSourcePose?`, `referenceKeypointReport?`. 각 필드에 한 줄 주석(어느 스크립트가 채우는지: seed / reprocess / backfill). 블록 위에 `정본 = app/src/types/analysis.ts ReferenceMotion (contract.md §3 와 동기). Firestore 실제 키 = camelCase (seed-reference-motions.mjs:473-499).` 한 줄. "체크포인트 weight 합 = 1.0" 문장은 유지하되 뒤에 `(문서 규약 — 채점은 이 weight 를 읽지 않는다, §4)` 를 붙인다.
3. **§4 런타임 추출 규칙 (A-6, 비평가 정정 반영)**: 규칙 1 을 삭제하지 말고 정정 — `clipRange 의 실제 소비처는 공유 베이스 경계 한 곳이다: backend/functions/pipeline/app.py:7989-7993 (segments.ref_boundary_frame) + 앱 referenceMotions.ts:178 정규화·analysis.ts:1217 타입. 기준 angles 는 영상 전체 프레임에서 추출된다 (extract_reference_angles.py·reprocess_reference_motions_phase4.py 에 clipRange 참조 0).` 규칙 2(사용자 영상 전체 추출 + subsequence 정렬)는 유지. 규칙 3(checkpoints weight 가중평균)과 규칙 4(heroFrameUrl)는 소비처 0 이므로 지우고, 대신 `실제 채점 관절 = backend/shared/python/sunity_shared/analysis/skeleton.py JOINT_KEYS 8개(elbow·shoulder·hip·knee 좌우) 고정. checkpoints[].weight 는 채점에 쓰이지 않고 spine_mid 는 이 집합에 없다. heroFrameUrl 은 리포 전체 참조 0.` 을 적는다. `prepStartS 는 런타임 분석엔 안 쓰임` 문장 유지.
4. **§5 등록된 모션 (A-3)**: §5 제목 바로 아래에 한 줄 — `11편 (2026-09-18). 메타의 문서 정본은 여기지만 Firestore 반영은 app/scripts/seed-reference-motions.mjs MOTIONS 배열을 통해서만 일어난다 — 두 곳이 어긋나면 앱이 보는 값은 Firestore 라이브(= seed 결과)다. 활성 버전 포인터 = reference/_release.activeCandidate (2026-09-18 현재 rot180_v1).` 그리고 기존 ref-foxtop-split 블록 끝(§6 앞 `---` 앞)에 6블록을 append — ref-kip-up · ref-peter-pan · ref-power-spin · ref-elbow-twist-sister · ref-pdshape · ref-combo. 각 블록 형식은 기존 블록과 같게 `### {motionId}` + yaml(motionId/name/athleteName: 정은지/level/entryType/entryDescription/description/videoUrl) + `**clipRange**` yaml + `**채점 체크포인트**` yaml(checkpoints 7행). 원값은 seed 274-429행을 그대로 옮기고(문장 수정 금지), "원본 영상 분석" 소절은 seed 에 값이 없으므로 넣지 않는다. 각 블록 머리에 `(원값 = seed-reference-motions.mjs, 2026-06-12 belle 결정)` 한 줄.
5. **§6 새 모션 등록 절차 (A-4 반려 반영 — CONTEXT 의 수정 문구가 정본)**: §6 본문(6단계 + "Claude Code 명령 템플릿")을 통째로 교체하고 "선수 사전 검증 (최소)" 문단은 유지. 새 §6 은 다음 순서:
   - 머리 배너 한 단락: `잠정 절차 (2026-09-18). 이 수동 런북은 Phase 21(프로 셀프서비스 기준 등록, belle 2026-08-30 요구사항 — 파일럿 Step 2 직결)이 없애려는 대상이다. Phase 21 이 살아 있는 동안 이 절차를 정본으로 고착시키지 말 것.`
   - auto-register 사실 한 단락: `이미 배포된 POST /reference/auto-register (backend/functions/reference-auto-register/app.py, template.yaml 라우트) 는 Gemini A 로 메타(motionId/clipRange/checkpointJoints)를 자동 생성해 upsert 한다. 그러나 name·athleteName·level 을 쓰지 않아 앱 선택 화면에 뜨지 않고, angles 도 만들지 않는다 — 이것만으로 등록이 완결되지 않으므로 아래 [0]~[7] 이 전부 필요하다.`
   - 단계 [0]~[7] (코드펜스 안, 각 한 줄 명령):
     [0] 활성 버전 확인 — Firestore `reference/_release.activeCandidate` 를 읽어 그 이름을 `<활성버전>` 으로 쓴다(2026-09-18 현재 `rot180_v1`).
     [1] 영상 분석 + §5 블록 확정 → 이 파일 §5 에 append(camelCase).
     [2] `aws s3 cp {파일}.mp4 s3://sunity-motion-pilot-videos/reference/{motionId}.mp4`
     [3] seed-reference-motions.mjs MOTIONS 에 블록 추가 → `cd app && npm run seed:reference` (메타·presigned URL·isActive 만 쓴다, angles 는 안 쓴다).
     [4] Pod GPU(CUDA 필수, 기동은 `/start`): `ROT180_INVERSION_ENABLED=1 python backend/scripts/reprocess_reference_motions_phase4.py --version <활성버전> --no-flip --motions {motionId}` — 바로 아래 주석 한 줄: `# ROT180_INVERSION_ENABLED=1 필수. RTMW 엔진은 이 플래그를 런타임 env 로만 읽고 코드 기본은 off 다(rtmw_engine.py:62-66). 활성 기질 rot180_v1 은 이 플래그 ON 으로 추출됐다 — 안 켜면 신규 1편만 비회전 각도로 들어가 11편과 기질이 섞이고, 실측 결과가 정은지 자기비교 100 -> 60 이다(start_server.sh:24 의 경고와 같은 건).` → `reference/{motionId}/versions/<활성버전>` 에 angles·joints3d·keypointReport 생성.
     [5] Pod GPU: `python backend/scripts/backfill_reference_downstream.py --reference-version <활성버전> --write-candidate --motions {motionId}` → meanAngles·techniqueProfile·bodyNormalizationProfile·forceDirectionPattern·bodyComparisonSourcePose·captureViews 백필.
     [6] `FIREBASE_SA_PATH=firebase-sa.json python backend/scripts/promote_reference_version.py --version <활성버전> --motions <기존 11편 + 신규> --dry-run` 으로 먼저, 이상 없으면 `--dry-run` 없이 → `_release.activeCandidate` flip + top-level 미러 + 해시 검증.
     [7] 검수: 새 모션으로 mode1 분석 1건 완주(status=done) 확인.
     플래그 이름은 read_first 의 argparse 실측과 맞춘다 — FINDINGS 의 이름과 다르면 코드 쪽 이름을 쓰고 그 사실을 SUMMARY 에 적는다.
   - 주의 3줄: (a) [4][5] 를 Pod 밖 CPU 에서 돌리지 말 것. (b) mode1 파이프라인은 angles 없는 기준 doc 에 `RuntimeError("기준 모션 또는 keyframe 데이터 없음")`(pipeline/app.py:7896-7898) — [3] 까지만 하면 등록된 것처럼 보이고 분석만 실패한다. (c) ref-combo top-level doc 은 1013KB 로 Firestore 1MB 한도의 99% — 콤보보다 긴 모션은 한도에 걸린다.
6. **§7 (A-7)**: `joint 이름 ↔ ViTPose-S 출력 매핑` 항목과 `5개 reference 영상 ViTPose 시각 검증` 항목 2개를 지우고 그 자리에 한 줄 `(2026-09-18 정정: ViTPose 매핑·overlay 검증 2항목은 백본이 RTMW 로 바뀌어 영구 미실행 — 삭제. spine_mid 는 JOINT_KEYS 8개에 없어 채점에 닿지 않는다.)` MotionDTW sanity·clipRange 시각 검증 항목은 유지(camelCase).
7. **이력**: 문서 끝 이력에 `*v7 (2026-09-18, quick-260918-qm2): §3 camelCase + 실사용 필드 · §4 소비처 실측 정정(clipRange 는 공유 베이스 경계만, checkpoints 가중평균·heroFrameUrl 삭제) · §5 11편 · §6 잠정 절차(ROT180_INVERSION_ENABLED=1 명시, auto-register 한계, Phase 21 이 자동화 대상) · §7 ViTPose 2항목 삭제*` 추가. 문서 머리 `> 새 영상 추가 시 §6 등록 절차만 따르면 됨.` 은 `> 새 영상 추가 시 §6 절차(잠정 — Phase 21 이 자동화 대상)를 따른다.` 로.

커밋: `docs(quick-260918-qm2): reference-motions.md 수리 — camelCase·§4 실측·11편·§6 잠정 절차(ROT180 env 명시)·ViTPose 항목 삭제`.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && F=docs/reference-motions.md && test "$(grep -c '^### ref-' $F)" -eq 11 && for m in ref-kip-up ref-peter-pan ref-power-spin ref-elbow-twist-sister ref-pdshape ref-combo; do grep -q "^### $m\$" $F || exit 1; done && test "$(grep -v '^\*v' $F | grep -c 'entry_type\|clip_range\|prep_start_s\|exec_start_s\|exec_peak_s\|land_end_s\|recommended_record_s\|shared_base_motion_id\|heroFrameUrl\|ViTPose\|ml/CLAUDE.md')" -eq 0 && grep -q 'ROT180_INVERSION_ENABLED=1 python backend/scripts/reprocess_reference_motions_phase4.py' $F && grep -q 'reference/auto-register' $F && grep -q 'Phase 21' $F && grep -q 'segments.ref_boundary_frame' $F && grep -q 'JOINT_KEYS' $F && grep -q 'anglesJointKeys' $F && grep -q 'videoS3Key' $F && grep -q '^\*v7 (2026-09-18' $F && test "$(grep -c '^\*v4.3' $F)" -eq 1 && test "$(git diff --stat -- app backend | wc -l | tr -d ' ')" -eq 0 && test "$(git diff -U0 | grep '^+' | python3 -c "import sys,re; print(len(re.findall(r'[\U0001F300-\U0001FAFF✅❌⚠⭐]', sys.stdin.read())))")" -eq 0 && echo TASK2-OK</automated>
  </verify>
  <done>§5 11블록, 이력 외 snake_case 키·ViTPose·heroFrameUrl 0, §6 에 ROT180 env 가 박힌 reprocess 명령·auto-register 한계·Phase 21 잠정 배너, §4 에 실제 소비처, §3 에 실사용 필드, v7 이력. 소스 diff 0. 커밋 1개.</done>
</task>

<task type="auto">
  <name>Task 3: 운영 문서 — DEMO-CARD 빈칸화 + Pod 종료 절차 3곳(A-9 반려 반영) + runpod README + runpod-fast-restart 배너 + design.md 배너 + app/CLAUDE.md 차트 (A-8·A-9·A-10·A-15·A-16·A-17)</name>
  <files>.planning/quick/260918-gpx-gate-false-suppress/DEMO-CARD.md, .claude/commands/start.md, .planning/quick/260906-n2j-stage-1-advisory/evidence/POD-RUNBOOK-2026-09-06.md, backend/runpod_inference/README.md, docs/runpod-fast-restart.md, design.md, app/CLAUDE.md</files>
  <read_first>
    - FINDINGS 항목 8·9·10·15·16·17 (줄 194-213, 243-263) + `## 비평가 반려` 의 "문서 수정안 #9"(줄 324-330)·"runpod README:36-37"(줄 345-351)
    - CONTEXT `### ★ A-9 반려` (정본 문구) + A-16 ("기본 ON" 금지)
    - backend/functions/pipeline/requirements.txt:1-4, backend/template.yaml:342 과 :481 (읽기만 — 수정 금지)
  </read_first>
  <action>
1. **DEMO-CARD.md (A-8)**: :7 `| Pod | \`elevev58iv4mox\` · L4 · 루마니아(RO) · $0.49/hr |` → `| Pod | \`{podId}\` · GPU · 리전 · 시간당 비용 — 기동 후 채워넣기 |`; :8 서버 → `https://{podId}-8000.proxy.runpod.net`; :18 curl 도 `{podId}`; :46 `(오늘 실측: 88MB 가 25초 미만 — 종전 기록 752초보다 훨씬 빨랐습니다.)` → `(2026-09-18 실측: 88.7MB 가 156초 — 아래 단계별 표와 같은 값입니다. 종전 기록 752초는 재현되지 않았습니다.)`. "지금 상태 (기동 완료)" 제목은 `지금 상태 (기동 후 기입)` 로. 하단 E2E 기록(analysisId·단계별 표)은 그날의 관측이므로 그대로 둔다. "끝나면 반드시" 절에 아래 A-9 문구를 한 줄 추가.
2. **Pod 종료 절차 3곳 (A-9 — CONTEXT 의 수정 문구만)**: `.claude/commands/start.md:99`, `POD-RUNBOOK-2026-09-06.md:59`, DEMO-CARD "끝나면 반드시" 절. 세 곳에 같은 뜻의 한 줄을 추가한다(원문 `terminate + pod-expected=down` 은 유지): `+ Lambda \`RUNPOD_ANALYZE_URL\` 을 자리표시자 \`https://pod-down.invalid/analyze\` 로 되돌린다 (\`aws lambda update-function-configuration\` 은 병합이 아니라 치환 — 나머지 변수를 통째로 다시 넣는다). 값을 지우거나 SSM \`/sunity/motion/runpod-analyze-url\` 을 삭제하지 말 것 — SSM 은 Type=String 이라 빈 값이 안 들어가고, template.yaml:342/481 이 \`{{resolve:ssm:...}}\` 로 이 이름을 해석하므로 삭제하면 다음 sam deploy 가 그 자리에서 깨진다. 그리고 Pod 이 없으면 분석은 어차피 실패한다 — URL 을 비워도 CPU 폴백은 배포 환경에서 ImportError 로 막혀 있다(pipeline/requirements.txt:1-4). 이 되돌리기는 '죽은 주소로 위임' 대신 '즉시 실패'로 만드는 것이지 분석을 살리는 것이 아니다.` 자리표시자 값은 Claude 재량으로 `.invalid`(RFC 2606 예약 TLD — DNS 즉시 실패, 이름만 봐도 내려감을 안다) 를 쓴다 — 이 선택을 start.md 의 그 줄에 괄호로 적는다. **AWS 를 실제로 호출하지 않는다** — 문서만.
3. **backend/runpod_inference/README.md (A-16)**: :3 → `... RTMW GPU 추론 위임 서버. **시연·실증 때만 기동**(belle 2026-08-28 결정), RTMW-x ONNX 모델 VRAM 상주.`; :18 도식 `NLF 추출` → `RTMW 추출`; :36-37 두 줄 → `> \`ROT180_INVERSION_ENABLED\` 는 코드 기본이 off 이고 켜는 곳은 start_server.sh:24 한 곳뿐이다. 2026-09-17 belle 승인 이후 운영 Pod 에서는 ON 이며 기준 라이브러리 rot180_v1 승격과 한 묶음이다 — 맨손 기동에서 이 값을 누락하면 학생만 비회전인 비대칭 비교가 되어 정은지 자기비교 100 -> 60 으로 떨어진다(조용한 OFF 함정).` "기본 ON" 이라는 표현은 쓰지 않는다. 추가로 제목 바로 아래(:2) 배너 한 줄: `> **주의 (2026-09-18):** 아래 "한 번에 띄우기 2)" 의 setup.sh/NLF 모델 다운로드, 환경변수 표의 NLF_MODEL_PATH·MOTIONBERT_*, "운영 메모"의 NLF 서술은 2026-06 NLF 시절 것이다. 서빙 부트스트랩 정본 = backend/runpod_inference/bootstrap_full.sh, 기동 정본 = start_server.sh, 절차 = /start.` 그 외 줄은 건드리지 않는다.
4. **docs/runpod-fast-restart.md (A-17)**: 1행 제목 아래 배너 `> **사문 (2026-08-31 이후).** 서빙 Pod 부트스트랩 정본은 backend/runpod_inference/bootstrap_full.sh (rtmlib + onnxruntime-gpu) 이고 기동 절차는 /start 다. 본 문서의 mmcv/mmpose 절차는 backend/research/spikes/spike_rtmpose.py 를 돌릴 때만 쓴다 — 서빙에 쓰면 15~45분을 태운다.` 본문 무접촉.
5. **design.md (A-10)**: (a) :5 뒤·`---` 앞에 배너 `> **주의 (2026-09-18):** 본 문서는 2026-05-28 이 마지막 갱신이다. 분석 결과 화면은 2026-09-09 에 피그마 시안으로 전면 재디자인됐다(곡선 빨강 헤더 + 흰 점수 원 + 상단 4탭: 요약/동작비교/교정포인트/보완운동). §7·§8·§10-5 는 그 이전 서술이다 — 결과 화면 작업은 app/src/app/analysis/result.tsx 와 app/src/components/result/ 를 정본으로 볼 것.` (b) :427 `→ [미완성] 분석 결과 화면` → `→ 분석 결과 화면 (4탭: 요약/동작비교/교정포인트/보완운동)`. (c) :432 제목 → `## 8. 미완성 화면 (2026-05-28 시점 — 결과/기록/마이는 이후 구현 완료)`, :437·:445·:449 의 `❌ 분석 결과 화면`·`❌ 기록 탭`·`❌ 마이페이지` 를 `[구현됨 — app/src/app/analysis/result.tsx] 분석 결과 화면` / `[구현됨 — app/src/app/(tabs)/history.tsx] 기록 탭` / `[구현됨 — app/src/app/(tabs)/profile.tsx] 마이페이지` 로(기호 대신 대괄호). `❌ 게이미피케이션 UI` 는 그대로. (d) :493 `(30~60초)` → `(실측 3~6분 — 88MB 영상 352초, 2026-09-18)`. (e) :484 `react-native-linear-gradient 설치 필요` → `expo-linear-gradient (설치됨, app/package.json)`.
6. **app/CLAUDE.md (A-15)**: `## 차트 라이브러리` 코드블록(54-64행)을 `react-native-svg : 성장 그래프·점수 위젯을 직접 그린다 (GrowthChart.tsx / OctagonScore.tsx / ResultScoreDial.tsx)` / `차트 전용 라이브러리는 쓰지 않는다 — Victory Native / react-native-gifted-charts 는 도입된 적 없음(package.json 에 없음). 설치 지시 삭제 2026-09-18.` 두 줄로 교체.

커밋: `docs(quick-260918-qm2): 운영 문서 — DEMO-CARD 빈칸화 · Pod 종료 시 Lambda URL 자리표시자 복귀(3곳) · runpod README 시연기동/RTMW/ROT180 · fast-restart 사문 배너 · design.md 재디자인 배너 · app 차트 라이브러리`.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && D=.planning/quick/260918-gpx-gate-false-suppress/DEMO-CARD.md && test "$(grep -c 'elevev58iv4mox' $D)" -eq 0 && test "$(grep -c '25초' $D)" -eq 0 && grep -q '156초' $D && for f in .claude/commands/start.md .planning/quick/260906-n2j-stage-1-advisory/evidence/POD-RUNBOOK-2026-09-06.md $D; do grep -q 'pod-down.invalid' "$f" || exit 1; grep -q 'ImportError' "$f" || exit 1; done && test "$(grep -c '삭제하지 말 것\|SSM 파라미터를 삭제' .claude/commands/start.md)" -ge 1 && R=backend/runpod_inference/README.md && test "$(grep -c '24/7\|NLF 모델 메모리\|off 가 기본이라 지금은 누락돼도\|기본 ON' $R)" -eq 0 && grep -q '2026-09-17' $R && grep -q '시연' $R && grep -q 'RTMW 추출' $R && test "$(sed -n '1,4p' docs/runpod-fast-restart.md | grep -c '사문')" -eq 1 && test "$(sed -n '1,8p' design.md | grep -c '2026-09-09')" -eq 1 && test "$(grep -c '\[미완성\] 분석 결과 화면\|react-native-linear-gradient\|30~60초' design.md)" -eq 0 && test "$(grep -c '❌' design.md)" -eq 1 && test "$(grep -c 'npm install victory-native\|npm install react-native-gifted-charts' app/CLAUDE.md)" -eq 0 && grep -q 'ResultScoreDial' app/CLAUDE.md && test "$(git diff --stat -- app backend | grep -c '|')" -eq 1 && git diff --stat -- app backend | grep -q 'runpod_inference/README.md' && git diff --quiet -- backend/template.yaml && test "$(git diff -U0 | grep '^+' | python3 -c "import sys,re; print(len(re.findall(r'[\U0001F300-\U0001FAFF✅❌⚠⭐]', sys.stdin.read())))")" -eq 0 && echo TASK3-OK</automated>
  </verify>
  <done>DEMO-CARD 에 죽은 Pod 0·시간 156초 통일, 종료 절차 3곳에 자리표시자 복귀 + ImportError 사실, README 에 24/7·NLF·"기본 ON" 0, 두 사문/낡음 배너, design.md 3지점 정정, 차트 설치 지시 0. 소스 diff = README.md 1파일, template.yaml 무접촉. 커밋 1개.</done>
</task>

<task type="auto">
  <name>Task 4: STATE.md 원장 — Current Position 정정 + 미종결 트랙 판정 원장 신설 + debug 문서 6건 정정 블록 (묶음 B, "완료"는 "사문"이 아니다)</name>
  <files>.planning/STATE.md, .planning/debug/illustration-slot-crop.md, .planning/debug/viewer-axis-flat-skeleton.md, .planning/debug/kipup-split-injection-lost.md, .planning/debug/recognizer-ipsf-fallback.md, .planning/debug/inversion-joint-attribution.md, .planning/debug/ref-student-substrate-gap.md</files>
  <read_first>
    - CONTEXT `## 묶음 B` 두 표 전체 (판정·원장 표기 문구의 정본)
    - FINDINGS `## w1:debug원장`·`## w1:phase22-31`·`## w1:phase33-36` 의 각 `**원장 한 줄:**` (줄 4-141) + `## 비평가 반려` 의 `**원장:**` 줄(플라이휠·REQUIREMENTS·ROADMAP·Phase 35/18/21·worktree)
    - .planning/STATE.md 28-56 (Current Position — 진행중/미착수 블록)
  </read_first>
  <action>
1. **STATE.md Current Position 인라인 정정** (:37-42 진행중 코드블록, :47-52 미착수 코드블록 — 숫자 열은 08-30 실측이므로 그대로, 설명 열만 09-18 판정으로):
   - 22 → `22-06 완료(SUMMARY 미작성) · 22-08/09/10 보류(재개 = promotion_ledger.current 가 null 을 벗어남 + RunPod 충전)`
   - 31 → `31-12 사문(belle 2026-07-20 축소마감 + CALIBRATION blocked + 09-09 소비처 제거)`
   - 33 → `33-07/16/21 사문(2026-09-18 판정 — flip 은 09-17 rot180_v1 로 실행됨·게이트 화면 소멸·no-op)`
   - 36 → `36-01 완료(SUMMARY 미작성) · 36-02 살았다(belle 실계정 로그인 1건, 파일럿 비차단) · 카카오·네이버 보류(belle 08-31 "출시 준비 때")`
   - 18 → `← 보류: baseline·assert 실재(backend/evals/phase18). 남은 것 = Pod live sweep + sensitivity 셋(= SCORE-09 잔여)`
   - 21 → 기존 문구 뒤 ` · 살았다(2026-09-18): reference-motions.md §6 수동 절차가 이 페이즈의 제거 대상`
   - 35 → `← 사문(표기 낡음): 합성 mp4 경로는 quick 트랙으로 배선·운영 중(result.tsx:1720 RenderedComparePlayer)`
   블록 위 인용문에 한 줄 추가: `> **2026-09-18 판정 (quick-260918-qm2):** 아래 설명 열은 09-18 판정, 숫자 열은 08-30 실측. 판정 전문 = 이 절 아래 "미종결 트랙 판정 원장".`
2. **판정 원장 신설**: 미착수 코드블록(:52)과 `**현재 우선순위 = 분석**` 사이에 `### 미종결 트랙 판정 원장 (2026-09-18, quick-260918-qm2)` 절. 머리에 라벨 정의 한 단락 — `사문 = 상위결정 기각·기능제거·소비처 0 / 완료 = 실행이 끝났고 원장 표기만 누락 / 살았다 = 남은 일이 있다 / 보류 = 재개 조건이 있다. 완료는 사문이 아니다(22-06 의 Qwen3-VL-8B 백본 결정은 기각된 적 없다). 근거 = 260918-qm2-FINDINGS.md(파일:줄번호 실측). 다시 조사하지 말 것.` 그 다음 표 — 열 = `대상 | 판정 | 근거(한 줄, 커밋·파일:줄) | 남은 것 / 재개 조건`. 행은 CONTEXT 묶음 B 의 두 표 전부(debug 6 · 22-06 · 22-08/09/10 · 31-12 · 33-07 · 33-16 · 33-21 · 36-01 · 36-02 · 36 카카오·네이버 · ROADMAP 미체크 · Phase 35 · Phase 18 · Phase 21 · REQUIREMENTS Pending · 플라이휠 크롭 반출) + 기록만 1행 `유령 worktree .claude/worktrees/agent-a572524a5cb0c0b23 | 기록만(CONTEXT 범위 밖) | 2026-08-01 RED 커밋 c0d662de 에 물린 worktree+브랜치, .planning 사본이 전수 검색 오염 | 제거는 별도 판정`. 문구는 CONTEXT 의 "원장 표기" 열을 그대로 쓰고 근거 열은 FINDINGS 의 `원장 한 줄` 에서 커밋 해시·파일:줄만 뽑는다. 22-06·36-01 판정 열은 정확히 `완료(원장 표기만 누락)`. ROADMAP 행에는 Task 5 의 실측(SUMMARY 실재 16건 = CONTEXT 의 14 + 25-02·25-03) 을 적는다. REQUIREMENTS 행에는 `Pending 행 실측 19(CONTEXT 는 17) — SCORE-10 Complete, SCORE-09 belle 지시로 열림, 나머지 17 재판정 대상` 을 적는다.
3. **debug 문서 6건 정정 블록**: 각 파일의 frontmatter 닫는 `---` 바로 다음 줄에 빈 줄 + 한 단락 인용문을 넣는다. frontmatter(`status:` 포함)는 건드리지 않는다(도구 계약 — 판정은 배너와 원장으로). 형식: `> **판정 정정 (2026-09-18, quick-260918-qm2): {판정}** — {CONTEXT 원장 표기 문구}. 원장 = .planning/STATE.md "미종결 트랙 판정 원장". {사문/해소면 "다시 열지 말 것." / 살았다·보류면 "남은 것 = ..."}` 판정 값: illustration-slot-crop 사문 · viewer-axis-flat-skeleton 사문 · kipup-split-injection-lost 해소 · recognizer-ipsf-fallback 해소 · inversion-joint-attribution 살았다(남은 것 = 회전 ON 이후 발화 재측정, 초과 관절 8→2·임계 5) · ref-student-substrate-gap 보류(남은 것 = elbow-twist 학생 ON 재분석 1편, Pod 필요).

커밋: `docs(quick-260918-qm2): 미종결 트랙 판정 원장 — STATE.md 신설 + debug 6건 정정 블록 (완료≠사문)`.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && S=.planning/STATE.md && test "$(grep -c '^### 미종결 트랙 판정 원장 (2026-09-18, quick-260918-qm2)' $S)" -eq 1 && test "$(sed -n '/^### 미종결 트랙 판정 원장/,/^\*\*현재 우선순위/p' $S | grep -c '^| ')" -ge 23 && sed -n '/^### 미종결 트랙 판정 원장/,/^\*\*현재 우선순위/p' $S | grep -q '^| 22-06 .*| 완료(원장 표기만 누락) |' && sed -n '/^### 미종결 트랙 판정 원장/,/^\*\*현재 우선순위/p' $S | grep -q '^| 36-01 .*| 완료(원장 표기만 누락) |' && test "$(grep -c '22-06[^|]*죽었\|36-01[^|]*죽었\|22-06[^|]*사문\|36-01[^|]*사문' $S)" -eq 0 && sed -n '28,60p' $S | grep -q '2026-09-18 판정 (quick-260918-qm2)' && for f in illustration-slot-crop viewer-axis-flat-skeleton kipup-split-injection-lost recognizer-ipsf-fallback inversion-joint-attribution ref-student-substrate-gap; do p=.planning/debug/$f.md; test "$(head -1 $p)" = '---' || exit 1; test "$(grep -c '판정 정정 (2026-09-18, quick-260918-qm2)' $p)" -eq 1 || exit 1; test "$(git diff -U0 -- $p | grep -c '^-[a-z_]*:')" -eq 0 || exit 1; done && test "$(git diff --stat -- app backend | wc -l | tr -d ' ')" -eq 0 && test "$(git diff -U0 | grep '^+' | python3 -c "import sys,re; print(len(re.findall(r'[\U0001F300-\U0001FAFF✅❌⚠⭐]', sys.stdin.read())))")" -eq 0 && echo TASK4-OK</automated>
  </verify>
  <done>STATE.md 에 라벨 정의 + 23행 이상 판정 표, 22-06/36-01 이 "완료(원장 표기만 누락)", 진행중/미착수 블록 설명 열 정정, debug 6건 각 1개 정정 블록(frontmatter 키 삭제 0). 소스 diff 0. 커밋 1개.</done>
</task>

<task type="auto">
  <name>Task 5: ROADMAP 체크박스(SUMMARY 실재 = [x]) + 미종결 11줄 판정 표기 + Phase 18/21/35/36 정정 + REQUIREMENTS SCORE-10 Complete·재판정 표시</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - CONTEXT `### ★ 비평가가 새로 찾은 것` 표 (ROADMAP 14건 · Phase 35/18/21 · REQUIREMENTS)
    - .planning/ROADMAP.md 미체크 줄 (`grep -n '^\s*- \[ \]' .planning/ROADMAP.md` — 29줄) · Phase 18 블록(583-592) · Phase 21(686-716) · Phase 35(1277-1287) · Phase 36(1288-1330)
    - .planning/REQUIREMENTS.md :42-43 (SCORE-09/10 본문) · :170-232 (Traceability 표)
  </read_first>
  <action>
1. **체크박스 — 규칙 = "SUMMARY 가 디스크에 실재하면 [x]"** (Claude 재량: 고정 목록 대신 실측 규칙, 한 커밋으로 묶음). 미체크 `NN-MM-PLAN.md` 줄 각각에 대해 `ls .planning/phases/*/NN-MM-SUMMARY.md` 가 있으면 `- [ ]` → `- [x]`. 실측상 16건이다: CONTEXT 의 14건(01-23/24/25 · 05-03/04/05 · 12-01/02/03 · 23-03 · 24-03 · 25-01 · 25-04 · 32-15) + 25-02 · 25-03(SUMMARY 실재, 비평가 목록에서 빠짐 — 관측으로 SUMMARY 에 적는다). 줄의 나머지 텍스트는 무접촉.
2. **미종결 11줄 표기** (CONTEXT 묶음 B 문구, "완료"≠"사문"):
   - :754 22-06 → `- [x] 22-06-PLAN.md — ... (원문 유지) — **완료(원장 표기만 누락, 2026-09-18 판정)**: bake-off 실행 완료, Qwen3-VL-8B CONFIRMED(belle 2026-07-13), 산출물 2/2 커밋(ab0f5124·2cdc76e0). SUMMARY 만 미작성`
   - :762/:766/:770 22-08/09/10 → `[ ]` 유지, 줄 끝에 ` — **보류(2026-09-18)**: 미착수(artifact 부재, 운영 참조 0). 재개 = promotion_ledger.current 가 null 을 벗어남 + RunPod 충전`
   - :1079 31-12 → `- [x] ~~31-12-PLAN.md~~ — ... — **사문(2026-09-18)**: belle 2026-07-20 축소마감(31-CLOSEOUT)이 원 플랜 기각 + CALIBRATION blocked:true + 09-09 재디자인이 앱 소비처 제거(aadf0375)`
   - :1188 33-07 → `[x] ~~...~~ — **사문(2026-09-18)**: flip 은 09-17 rot180_v1 로 실행됨(02b1155a). 잔여 = 기준 18fps vs 학생 9fps → Phase 34`
   - :1228 33-16 → `[x] ~~...~~ — **사문(2026-09-18)**: 게이트 대상 화면이 08-24 일러스트 제거 + 09-09 4탭 재디자인으로 소멸`
   - :1174 33-21 → `[x] ~~...~~ — **사문(no-op, 2026-09-18)**: 33-06 elbow-twist 여유 +3.10 ≥ +2.0 이라 플랜 자신이 규정한 no-op`
   사문 표기는 `[x]` + 제목 취소선 + "사문" 라벨 — 완료와 구분되게(완료 줄에는 취소선 없음).
3. **Phase 36 (1288-1330)**: `**Plans:** 0 plans` → `**Plans:** 2 plans (36-01 완료 · 36-02 살았다)`, `- [ ] TBD (run /gsd-plan-phase 36 to break down)` 을 세 줄로 교체: `- [x] 36-01-PLAN.md — 계정 화면 3종 + 라우팅 — **완료(원장 표기만 누락, 2026-09-18)**: f957bad9. 전제 "시작하기=게스트 직행"은 belle 2026-09-01 로그인 게이트 결정으로 대체(app/src/app/index.tsx:12-18)` / `- [ ] 36-02-PLAN.md — Google 로그인 + 게스트 승계 — **살았다**: 코드 배포됨(41fa1abd). 남은 것 1건 = belle 실계정 로그인으로 linked/uid 유지 실측(Success #4). 파일럿 비차단` / `- [ ] 카카오·네이버 — **보류**: belle 2026-08-31 "출시 준비 때". WIRED_PROVIDER_IDS=['google','apple'] 이라 버튼 미렌더, POST /auth/social 배포 라우트 0`.
4. **Phase 18 (583-592)**: `**Plans:**` 줄 아래 인용문 `> **2026-09-18 판정 (quick-260918-qm2): 보류.** baseline·assert 실재(backend/evals/phase18). 남은 것 = Pod live sweep + sensitivity 셋(미보유+above-cutoff) — REQUIREMENTS SCORE-09 잔여와 같은 물건, 재개 조건 = Pod.`
5. **Phase 21 (686-716)**: `**Plans:** 0 plans` 아래 인용문 `> **2026-09-18 판정: 살았다 · 파일럿 Step 2 직결.** belle 2026-08-30 요구사항. docs/reference-motions.md §6 의 수동 절차(S3→seed→Pod GPU reprocess(ROT180 env)→backfill→promote)가 이 페이즈가 없애려는 것이다. POST /reference/auto-register 는 배포돼 있으나 name·athleteName·level 미기록 + angles 미생성이라 등록이 완결되지 않는다.` TBD 줄은 유지.
6. **Phase 35 (1277-1287)**: `**Plans:** 0 plans` 아래 인용문 `> **2026-09-18 판정: 사문(표기 낡음).** 핵심 산출물(단일 합성 mp4 재생 경로)은 quick 트랙으로 이미 배선·운영 중 — app/src/app/analysis/result.tsx:1720 RenderedComparePlayer, s3keys.build_rendered_compare_key, playback-url 재서명. 플랜 트랙으로 재개할 대상이 아니다. 세부 목표(전 동작 프로토타입 배치 → belle 느낌 평가) 전수 마감 여부는 미확인.` TBD 줄은 `[ ]` 유지하되 끝에 ` (2026-09-18: 플랜 트랙 재개 대상 아님 — 위 판정)`.
7. **REQUIREMENTS.md**: :43 `- [ ] **SCORE-10**` → `- [x] **SCORE-10**`, 문단 끝에 ` **(2026-09-18 판정, quick-260918-qm2: 구현 완료 — deduction_engine.tally 실재(:268), vision_veto.SEVERITY_CAP·apply_downward_cap 코드에서 소멸. Pending 표기는 누락이었다.)**`. Traceability 표 :204 `| SCORE-10 | Phase 24 | Pending |` → `| SCORE-10 | Phase 24 | Complete (2026-09-18 코드 실측) |`. :203 SCORE-09 → `| SCORE-09 | ... | Pending — belle 지시로 열림(STATE 20-04/SCORE-09 ownership 노트). 잔여 = Phase 18 sensitivity 셋 |`. 나머지 `| Pending |` 행 전부(실측 17행) → `| Pending — 재판정 대상(2026-09-18, 미검토) |`. 표 바로 위에 한 줄 `> **2026-09-18 (quick-260918-qm2):** Pending 행은 아무도 열어보지 않았다. SCORE-10 만 코드 실측으로 Complete 정정, 나머지는 "재판정 대상" 표시만 — 전수 판정은 범위 밖.` 파일 끝 `*Updated ...*` 이력에 같은 취지 한 줄 추가.

커밋: `docs(quick-260918-qm2): ROADMAP 체크박스 16건(SUMMARY 실재) + 미종결 11줄 판정 + Phase 18/21/35/36 · REQUIREMENTS SCORE-10 Complete + 재판정 표시`.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && R=.planning/ROADMAP.md && test "$(grep -oE '^\s*- \[ \] [0-9]{2}(\.[0-9])?-[0-9]{2}-PLAN\.md' $R | sed -E 's/.*\] //; s/-PLAN\.md//' | while read p; do ls .planning/phases/*/"$p"-SUMMARY.md >/dev/null 2>&1 && echo "$p"; done | wc -l | tr -d ' ')" -eq 0 && test "$(grep -c '^\s*- \[ \]' $R)" -eq 9 && grep -q '^\s*- \[x\] 22-06-PLAN.md.*완료(원장 표기만 누락' $R && grep -q '^\s*- \[x\] 36-01-PLAN.md.*완료(원장 표기만 누락' $R && grep -q '^\s*- \[ \] 36-02-PLAN.md.*살았다' $R && test "$(grep -c 'run /gsd-plan-phase 36' $R)" -eq 0 && for p in 31-12 33-07 33-16 33-21; do grep -q "^\s*- \[x\] ~~$p-PLAN.md~~.*사문" $R || exit 1; done && test "$(grep -c '22-06-PLAN.md.*사문\|22-06-PLAN.md.*죽었\|36-01-PLAN.md.*사문\|36-01-PLAN.md.*죽었' $R)" -eq 0 && test "$(grep -c '2026-09-18 판정' $R)" -ge 3 && Q=.planning/REQUIREMENTS.md && test "$(grep -c '| Pending |' $Q)" -eq 0 && sed -n '43p' $Q | grep -q '^- \[x\] \*\*SCORE-10\*\*' && grep -q '^| SCORE-10 | Phase 24 | Complete' $Q && test "$(grep -c '재판정 대상' $Q)" -ge 17 && grep -q '^| SCORE-09 .*belle 지시로 열림' $Q && test "$(git diff --stat -- app backend | wc -l | tr -d ' ')" -eq 0 && test "$(git diff -U0 | grep '^+' | python3 -c "import sys,re; print(len(re.findall(r'[\U0001F300-\U0001FAFF✅❌⚠⭐]', sys.stdin.read())))")" -eq 0 && echo TASK5-OK</automated>
  </verify>
  <done>ROADMAP 에 SUMMARY 실재 미체크 0, 남은 `[ ]` 9줄(Phase 22 상위 · 22-08/09/10 · TBD 21/34/35 · 36-02 · 카카오·네이버), 22-06/36-01 완료 표기, 사문 4건 취소선, Phase 18/21/35 판정 인용문, Phase 36 플랜 목록. REQUIREMENTS `| Pending |` 0, SCORE-10 Complete, 재판정 표시 17. 커밋 1개.</done>
</task>

<task type="auto">
  <name>Task 6: BELLE-JUDGMENT.md 신설(belle 판정 대기 5건, 기록만) + 인계서 HANDOFF 연결 (묶음 C)</name>
  <files>.planning/quick/260918-qm2-doc-truth-repair-18/260918-qm2-BELLE-JUDGMENT.md, .planning/quick/260918-day-closeout/HANDOFF.md</files>
  <read_first>
    - CONTEXT `## 묶음 C` 5건 전체 (정본)
    - FINDINGS `## 비평가 반려` 의 template.yaml 두 항목(줄 331-344) + "놀란것: 배포 스택과 실환경이 서로 다른 Pod 세대" 단락 + `## w1:phase33-36` 의 resultSections.ts 항목(줄 134-141)
    - backend/template.yaml:29-40 · backend/samconfig.toml:18 (인용만, 수정 금지)
    - .planning/quick/260918-day-closeout/HANDOFF.md §0(14-26) · §5(90-100)
  </read_first>
  <action>
1. **`260918-qm2-BELLE-JUDGMENT.md` 신설** (Write 도구). 구조:
   - 제목 `# belle 판정 대기 5건 — 2026-09-18 (quick-260918-qm2)` + 머리말 `고치지 않았다 — 기록만이다. 각 건 = 관측(파일:줄, 실측 명령) → 선택지 → 판정 요청 한 줄. 수치는 물을 때만(belle 보고 형식).`
   - `## 0. 판정 요청 (한 줄씩)` — 5건을 각 한 줄로: (1) sam deploy 를 살릴 것인가 — 죽은 Phase 31 파라미터 5개에 Default/override 를 줄지, 파라미터를 걷어낼지 (2) template.yaml RunpodAnalyzeUrl/RunpodAuthToken 사문 파라미터를 걷어낼 것인가(설명문이 NLF 폴백을 두 번 틀리게 가르친다 — A-9 오해의 근원) (3) 배포 스택 1커밋 낙후(polly:SynthesizeSpeech 미배포)를 언제 재배포로 닫을 것인가 (4) 스택 파라미터·Lambda env·SSM 이 서로 다른 Pod 세대 — sam deploy 전 정본을 어느 것으로 할지 (5) 죽은 코드 — 제거 범위(OTA 가능분 / 네이티브 재빌드분) + DeductionCard 판정('강사님께 물어보기' 기능 소멸의 물증).
   - `## 1`~`## 5` 각 건: **관측**(CONTEXT 묶음 C 문구 그대로 + FINDINGS 근거의 파일:줄·실행 명령 인용 — template.yaml:29-40 설명문 원문, samconfig.toml:18 parameter_overrides 3개, describe-stacks 결과 p56qusi8cgc91z vs elevev58iv4mox, LastUpdatedTime 2026-07-21T16:17Z) / **왜 지금 판정이 필요한가**(실증 한 달 뒤, Lambda 코드를 고칠 일이 생기면 (1)에서 멈춘다) / **선택지**(2~3개, 각 한 줄 결과) / **판정 요청**. 처방을 단정하지 않는다 — 인계에는 관측만.
   - 5번 건에 관측 한 줄 추가(누락 사실을 숨기지 않는다): `FINDINGS 에는 "죽은코드" 절이 없다. 이 절의 수치(앱 고아 16파일 3,707줄, 즉시 제거 후보 2,991줄, 3D 스택 제거 = 네이티브 재빌드)는 CONTEXT §C-5 에서 옮긴 것이고 파일별 목록은 리포 안에 없다 — FINDINGS 에 있는 파일별 항목은 app/src/lib/resultSections.ts(+테스트 348줄, 32-11 산물, 소비처 0) 1건뿐. 파일별 목록은 메인 세션이 첨부해야 판정이 가능하다.`
   - 끝에 `## 관련` — CONTEXT·FINDINGS 경로, STATE 판정 원장 절 이름, "backend/template.yaml 은 이 판정 전까지 무접촉".
2. **HANDOFF.md 연결**: §0 30초 요약 마지막 불릿 앞에 `- ★ **belle 판정 대기 5건** = \`.planning/quick/260918-qm2-doc-truth-repair-18/260918-qm2-BELLE-JUDGMENT.md\` (sam deploy 불가 · template 사문 파라미터 · 스택 1커밋 낙후 · Pod 세대 불일치 · 죽은 코드). 문서 18곳 정정 + 판정 원장 = STATE.md "미종결 트랙 판정 원장".` 추가. §5 표에 행 `| 6 | belle 판정 대기 5건 | 위 BELLE-JUDGMENT.md — 실증 결과 들은 뒤 (1) sam deploy 부터 |` 추가. §1 의 `Lambda RUNPOD_ANALYZE_URL = https://elevev58iv4mox-...` 줄 끝에 ` (2026-09-18 실측: 이 주소는 404 — 종료 절차의 자리표시자 복귀는 /start 1-6 참조)` 를 덧붙인다(관측만).

커밋: `docs(quick-260918-qm2): belle 판정 대기 5건 — BELLE-JUDGMENT.md 신설 + 인계서 연결`.
  </action>
  <verify>
    <automated>cd /Users/kimtaesung/Dev/SunityMotion && B=.planning/quick/260918-qm2-doc-truth-repair-18/260918-qm2-BELLE-JUDGMENT.md && test -f $B && test "$(wc -l < $B | tr -d ' ')" -ge 40 && test "$(grep -c '^## ' $B)" -ge 6 && grep -q 'VisualInputBucketName' $B && grep -q 'RunpodAnalyzeUrl' $B && grep -q 'polly' $B && grep -q 'p56qusi8cgc91z' $B && grep -q 'DeductionCard' $B && grep -q '죽은코드" 절이 없다\|죽은코드. 절이 없다' $B && H=.planning/quick/260918-day-closeout/HANDOFF.md && test "$(grep -c '260918-qm2-BELLE-JUDGMENT.md' $H)" -ge 2 && sed -n '14,30p' $H | grep -q 'belle 판정 대기 5건' && git diff --quiet -- backend/template.yaml && test "$(git diff --stat -- app backend | wc -l | tr -d ' ')" -eq 0 && test "$( (git diff -U0; git diff -U0 --no-index /dev/null $B 2>/dev/null) | grep '^+' | python3 -c "import sys,re; print(len(re.findall(r'[\U0001F300-\U0001FAFF✅❌⚠⭐]', sys.stdin.read())))")" -eq 0 && echo TASK6-OK</automated>
  </verify>
  <done>BELLE-JUDGMENT.md 에 판정 요청 5줄 + 건별 관측/선택지, 죽은코드 목록 부재 명시; HANDOFF §0·§5 가 파일을 가리킴. template.yaml·소스 무접촉, 이모지 0. 커밋 1개.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 문서 → 운영자 행위 | 런북(§6 등록 절차·Pod 종료 절차)을 사람이 그대로 실행한다 — 문서가 틀리면 기준 라이브러리·Lambda 설정이 실제로 망가진다 |
| 문서 → 다음 세션 판단 | 원장(STATE/ROADMAP/REQUIREMENTS)이 미결/완료를 잘못 말하면 재조사·재작업이 발생한다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-qm2-01 | Tampering (data integrity) | docs/reference-motions.md §6 → Firestore reference 기질 | mitigate | Task 2: reprocess 명령줄에 `ROT180_INVERSION_ENABLED=1` 명시 + 왜 필요한지 주석 + verify 가 그 문자열을 강제 |
| T-qm2-02 | Denial of Service (배포 파손) | Pod 종료 절차 → SSM 파라미터/`{{resolve:ssm}}` | mitigate | Task 3: "값 삭제·SSM 삭제 금지, 자리표시자로 복귀" 문구 + verify 가 `pod-down.invalid` 와 금지 문구를 확인 |
| T-qm2-03 | Information Disclosure | BELLE-JUDGMENT.md 에 자격증명 인용 | mitigate | 토큰·키 값은 적지 않는다(파라미터 이름·Pod ID·URL 만). RunpodAuthToken 은 이름만 |
| T-qm2-04 | Repudiation (판정 추적성) | STATE 판정 원장 | mitigate | 각 행에 커밋 해시·파일:줄 근거, 라벨 정의 명시, "완료≠사문" |
| T-qm2-05 | Tampering (소스 오염) | app/·backend/ 소스 | mitigate | 모든 태스크 verify 가 `git diff --stat -- app backend` 로 허용 3파일 외 diff 0 + template.yaml `git diff --quiet` |
| T-qm2-SC | Tampering | npm/pip/cargo installs | accept | 설치 0건 — 문서·주석만 (패키지 게이트 대상 없음) |
</threat_model>

<verification>
전체 마감 확인(모든 태스크 커밋 후, 메인 세션):
- `git log --oneline -6` 에 `docs(quick-260918-qm2):` 커밋 6개.
- `git diff d96d18e5..HEAD --stat -- app backend` = analysis.ts · pose_engines/__init__.py · runpod_inference/README.md 3파일만(첫 커밋 기준이 b47e53b5 이후면 그 해시로).
- `git diff d96d18e5..HEAD --quiet -- backend/template.yaml` 통과.
- 게이트 무회귀(문서·주석만이라 깨질 이유 없음, 확인만): `cd app && rtk npm run typecheck`; `cd backend && rtk pytest tests -x -q`(4835 passed 기준선).
- 이모지 추가 0: `git diff d96d18e5..HEAD -U0 | grep '^+' | python3 -c "..."` (각 태스크 verify 의 정규식) = 0.
</verification>

<success_criteria>
- 묶음 A 18건이 FINDINGS 원장 줄대로 적용됐고, A-4(ROT180 env·auto-register 한계·Phase 21 잠정)·A-9(자리표시자 복귀·삭제 금지·ImportError)·A-6(clipRange 살아 있음)·A-16("기본 ON" 금지)은 CONTEXT 의 반려 반영 문구로 적용됐다.
- 묶음 B: STATE.md 판정 원장 + ROADMAP/REQUIREMENTS 표기가 CONTEXT 표와 일치하고 22-06·36-01 은 "완료(원장 표기만 누락)".
- 묶음 C: BELLE-JUDGMENT.md 5건 + HANDOFF 링크. template.yaml 무접촉.
- 소스 diff = 주석/docstring 3파일. 커밋 6개. 이모지 0.
</success_criteria>

<output>
Create `.planning/quick/260918-qm2-doc-truth-repair-18/260918-qm2-SUMMARY.md` when done. SUMMARY 에 반드시 적을 관측 3건: (1) ROADMAP SUMMARY 실재 미체크는 14 가 아니라 16(25-02·25-03 추가) (2) REQUIREMENTS Pending 행은 17 이 아니라 19 (3) FINDINGS 에 `죽은코드` 절이 없어 BELLE-JUDGMENT 5번 건은 CONTEXT 수치만 옮겼다 — 파일별 목록은 메인 세션이 첨부해야 한다. §6 플래그 이름이 argparse 실측과 달랐다면 그것도.
</output>
