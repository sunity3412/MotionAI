---
id: 260920-k4p
title: 인수인계서 — 2026-09-20
date: 2026-09-20
status: complete
---

# 인수인계서 — 2026-09-20

> 앞 인계서: `.planning/quick/260919-cls-day-closeout/260919-cls-HANDOFF.md`
> 오늘 커밋 31건 · 게이트 **4989 passed / 20 skipped / 0 failed**(직접 실측) · 앱 tsc clean

---

## 0. 30초 요약

오늘은 **mode3 의 점수가 자세를 안 본다**는 것을 확인하고, 그 자리에 기준 선수 각도를
고정 잣대로 걸었다. **플래그 뒤에 두고 켜지 않았다.**

```
아침  동시 분석 오염 수리 (싱글턴 → 인자)
낮    mode3 종합 = 떨림 단독 확인 · Gemini 캐시 키 수리
오후  §11~§13 판정 붕괴 — 표본에 학생이 없었다, 기준 버전이 섞였다
저녁  Pod 실측: 정은지 파워스핀 정타 80점 → EXTEND 창 수리 · D-08 억제 표시 복원
밤    mode3 기준 축 배선(OFF) + 발전 읽기 신설 + yaml 이 빈 이유 규명
```

**내일 첫 일은 정해져 있다 → §7.**

---

## 1. ★ 지금 켜져 있는 것 / 꺼져 있는 것

```
RunPod Pod           0 대            (볼륨 보관료만 $0.015/hr)
RunPod 잔액          $8.58           [확인] 09-20 API 실측
Lambda RUNPOD_URL    죽은 Pod 을 가리킴  ← 지금 앱에서 영상 올리면 '분석 실패' 화면
MODE3_REFERENCE_RELATIVE_ENABLED   OFF (기동 스크립트에 주석으로 대기 중)
ROT180_INVERSION_ENABLED           ON  (기준 rot180_v1 승격과 한 묶음 — 한쪽만 되돌리지 말 것)
```

★ **Pod 0대 = 조용한 대기가 아니다.** 위임 요청이 실패하면 doc 이 `failed/server_error`
로 확정된다(`pipeline/app.py` 위임 분기). belle 이 앱을 열면 곧바로 실패 화면을 본다.
Pod 을 띄울 계획이 없는 날이면 belle 에게 먼저 알려야 한다.

---

## 2. ★★★★ 내일 Pod 작업 전에 **반드시** — 푸시

```
origin/main  =  a5ec1b08   (오늘 아침 14:08 마감본)
로컬 HEAD    =  17 커밋 앞
```

`scripts/pod_bootstrap_full.sh:19` 가 **`origin/main` 에서만 당긴다.**
푸시하지 않으면 내일 Pod 을 띄우고 bootstrap 을 돌려도 볼륨은 여전히 `a5ec1b08` 이고,
오늘 고친 것이 **하나도 안 들어간다**:

| 안 들어가는 것 | 무엇이 틀리게 측정되나 |
|---|---|
| `97a451f8` EXTEND 기하 창 | 정은지 정타가 여전히 80점 — 수리 확인 자체가 불가능 |
| `37da77fc` mode3 기준 축 | 플래그를 켜도 읽을 코드가 없다 |
| `5c29f91f` D-08 억제 | (앱측이라 Pod 무관, OTA 사안 — §3) |

→ **푸시는 belle 승인 사항이다.** 내가 임의로 하지 않았다. 내일 첫 질문에 포함(§8).

---

## 2-1. belle 승인 3건 — 집행 결과 (09-20 밤)

| # | belle 답 | 집행 |
|---|---|---|
| ① 푸시 | "오케이" | **완료** — `origin/main` = `2b8b2d23`, 미푸시 0 |
| ② 학생 영상 | "요청해놔서 몇 일 내로" | **대기** — 오면 관문 3개를 잰다(§8-1) |
| ③ OTA | "응" | **완료** — preview / runtime 1.2.4 / group `b59f97ae` |

**OTA 세부** — 09-09 이후 11일치 앱 작업 29파일(3,010줄)이 처음 기기로 나갔다.

- `[확인]` **네이티브 변경 0건** — `package.json`/`app.json`/`eas.json`/`ios`/`android`
  전부 09-09 이후 무변경. `version` 도 1.2.4 그대로라 설치본과 런타임이 맞는다.
- `[확인]` **발행 전 시뮬레이터 실물 확인**(typecheck 는 렌더 크래시를 못 잡는다):
  로그인 → 홈 → 기록 → 결과(요약 / 교정포인트 / 보완운동) 전부 렌더 정상,
  Metro 런타임 오류 **0**. 앱 시험 **286/0**, tsc clean.
- `[확인]` 옛 분석의 보완운동 이름이 **영문으로 보이는 것은 정상**이다 — doc 이
  분석 당시 이름을 스냅샷으로 들고 있기 때문(`legacyExerciseNames.ts` 머리말이 명시).
  백엔드가 새 분석에 싣는 이름은 **한글 43/43**이라 앞으로 올리는 분석은 한글이다.

★ **이제 §3 의 상황이 바뀌었다** — 아래는 OTA 전 상태 기록으로 남긴다.

## 3. (해소됨) 앱에 깔린 것은 09-09 판이었다

`[확인]` 마지막 OTA = **2026-09-09**(`e7023625`, preview/1.2.4).
그 뒤 `app/src` 에 커밋 **28건**이 쌓였다.

즉 belle 폰에 깔린 것은 **D-08 게이트가 쓸려나간 바로 그 판**이다.
→ 지금 기기에서는 기준 미보유 분석이 **여전히 confident 숫자를 띄운다.**
→ 거꾸로, belle 판정 대기 항목인 *"mode3 에 점수가 안 뜨는 상태로 실증 가능한가"* 는
   **현재 배포본에서는 아직 발생하지 않는 상황**이다. OTA 를 쏘는 순간 실물이 된다.

`[미확인]` belle 의 TestFlight 빌드가 preview 채널인지 production 채널인지 못 봤다.

---

## 4. 오늘 한 일 — 근거 위치

운영 동작을 바꾼 것은 **6건**, 나머지 25건은 문서·시험·도구다.

| 커밋 | 무엇을 | 플래그 | 되돌리는 법 |
|---|---|---|---|
| `27576cef` | 동시 분석 오염 — 분석별 값을 싱글턴에서 인자로 | 없음 | revert(구조 가드 시험이 재발 차단) |
| `22e6ede9` | Gemini 캐시 키에 동작 질의 추가 | 없음 | **되돌리지 말 것** — 옛 키는 이력 의존 결함 |
| `97a451f8` | EXTEND 채점이 기하 hold 창을 본다 | 없음 | revert 시 4파일 동반 + **저장 doc 은 재분석해야 반영** |
| `5c29f91f` | 앱 D-08 억제 표시 복원 | 없음 | revert + **OTA 재발행까지 해야 기기 반영** |
| `37da77fc` | mode3 기준 축 배선 | **OFF** | 플래그 주석 처리 + 재기동 |
| `7a3f7d36` | `referenceApproach` 발전 읽기 | 축과 동반 | 동상 |

상세 = `.planning/quick/260920-{cac,ra8,ruj,stn,m3r}-*/`

### 오늘의 핵심 발견 3가지

**① mode3 종합점수는 자세를 안 본다** (`4a0320c1`)
동작을 모르니 `line` 차원이 안 생기고, 이전 영상 대비 유사도는 **일부러 종합에서 뺐다**
(발전하면 과거의 나와 덜 비슷해져 점수가 역전되니까 — 그 판단은 옳다).
결과: **움직이지 않는 잣대가 하나도 없었다.** 같은 자세를 mode3 100점 / mode1 0점.

**② 정은지 본인 영상이 100점을 못 받는다** (`1ac2b6a0`, `a750f071`)
저장 분석 358건 중 46건(12.8%)이 100점 미만, 최저 10점. Pod 실측으로 범인을 잡았다 —
Gemini 가 10.6초 클립의 hold 를 **1.0초(진입부)**라 답했다. 같은 영상 5벌에서 8.0초/1.0초
이봉, **2/5 오답**. 기하 창으로 바꿔 위양성 4건 제거 + 실수 8건 신규 검출(양방향).

**③ criteria yaml 이 빈 것은 방치가 아니다** (`e9708d84`)
2026-06-27 belle 결정의 **의도적 제거**다. 양 무릎 EXTEND 를 걸었더니 **정타가 위양성**을
맞았다(peter-pan 정타 116° < 결함 121.6°). "굽힘 form 에 신전기준 강요 금지."
★ **그리고 파일 네 편이 후임으로 `reference_relative` 를 지목해 놨다** — mode1 엔 있었고
mode3 에만 없었다. 오늘 배선은 그 06-27 결정의 mode3 이행이다.

---

## 5. ★★ 내가 틀렸다가 고친 것 — 같은 함정 반복 금지

**다음 세션이 승계하면 안 되는 목록.** 오늘만 13건이고, 전부 같은 병이다 —
**숫자를 소비처까지 안 따라가고 이름표부터 붙였다.**

| 내가 한 말 | 사실 |
|---|---|
| "80점 중 stability 절반" | stability 는 **종합 입력 밖**. 기여 0 |
| "mode3 에 line 이 **구조적으로** 안 생긴다" | 과장 — 뒷문 2개(FallbackRecognizer / 캐시 상속) |
| "여유 2.0~2.3도" | **옛 판 값.** 현재 판은 12.1~17.2도 |
| "D-08 과 09-09 시안이 **충돌**" | 내 오판 — D-08 이 UX 강도를 재량으로 열어놨다 |
| "kip-up 은 이 축으로 **안 고쳐진다**(split 포화)" | 축은 **정확히 본다**(어깨 3.5→20.6). 막힌 건 문턱+신뢰구간 |
| "criteria yaml 은 **belle 라벨링 필요**" | 정반대 — 의도적 제거 + 후임 지목됨 |
| "`profile.hold_window` **소비처 0**" | ★**아직 읽는다** — §6-1 참조 |

★ **`[확인]`/`[미확인]` 을 문장마다 붙이는 규칙을 오늘 CLAUDE.md §7 에 넣었다**(`ed8cfa18`).
인용하기 전에 그 표시부터 볼 것.

---

## 6. ★ 내일 이어받을 미결 — 파일럿 성공기준별

### 6-1. ★★★★ Mode3(성공기준 1번)을 막는 것

**(a) Gemini 국면 힌트가 아직 mode3 점수에 닿는다** `[확인, 오늘 밤 정정]`

```
_select_stability_window (dimensions.py:366)  ← profile.hold_window 를 아직 읽는다
  → stability_score
  → mode3 는 core 차원(angle/line)이 없다
  → overall_from_dimensions 가 stability 단독으로 종합을 낸다
```

`97a451f8` 이 고친 것은 **EXTEND 경로뿐**이다. 내가 "소비처 0"이라 적은 것은 틀렸고
4곳을 정정했다. 같은 파일 `:357` 이 이미 별도 불일치를 경고해 둔 자리다 —
창 선택자는 *위치 분산 최소*로 고르는데 stability 는 *프레임간 흔들림*으로 채점한다
(스윕 12건에서 창을 바꾸면 **−15~+5점 양방향 이동**).
`[미확인]` 점수 이동 폭은 안 쟀다. 관측은 "경로가 이어져 있다"이지 "몇 점 틀린다"가 아니다.

**(b) 성장 그래프에 점이 0개다** `[확인]`
플래그 OFF + 캐시 뒷문이 닫힌 상태에서 mode3 는 항상 미보유 → 항상 억제 →
`growthSelectors` 가 집계에서 뺀다 → 홈에 **"주별 데이터가 아직 부족해요"** 만 뜬다.
수강생이 본인 영상 2개를 올려도 그렇다. **이것이 성공기준 1번 그 자체다.**
`[미확인]` 그 빈 화면을 실물로 본 기록이 없다 — stn 의 시뮬 검증은 결과화면이었다.

**(c) mode3 기준 축을 켤 수 있나** — 관문 3개(§8-1)

### 6-2. ★★★ Mode1(성공기준 2번)을 막는 것

- **ra8 수리를 Pod 에서 확인한 적이 없다** `[미확인]`. 원 결함은 Pod 실측에서 났는데
  검증은 전부 오프라인 재계산이다. 닫는 측정은 하나 — `fixtures/phase15/power-spin/
  correct.mp4` 를 앱과 같은 경로로 1회 분석해 `leg_extension` 감점 0 / 100점 확인.
- `ref-elbow-twist-sister` 등 6동작 바닥 미측정(`reference_axis_floors.json` 의 `_missing`).

### 6-3. 둘 다 막는 것

★★★★ **학생 영상 0편.** 오늘의 모든 수치가 정은지 fixture 재분석이다.
→ 다만 **관문 3개 중 둘은 '정은지가 아닌 사람'이면 잰다**(§8-1).

---

## 7. ★ 내일 첫 30분

```
1. (푸시는 어젯밤 완료) Pod 기동 + bootstrap + 볼륨 동기
2. §7-1 의 측정 7건을 한 번에 — Pod 재기동 반복이 잔액을 태운다
3. Terminate
```
★ **belle 이 앱을 열 수 있다** — OTA 가 나갔고 Pod 은 0대다. 영상을 올리면 실패 화면이니
Pod 을 먼저 띄우거나 belle 에게 먼저 알릴 것.

### 7-1. Pod 1회에 묶을 측정 7건

| # | 무엇을 | 재료 | 왜 지금 |
|---|---|---|---|
| 1 | ra8 수리 실경로 확인 | `fixtures/phase15/power-spin/correct.mp4` | 성공기준 2번 직결 |
| 2 | 6동작 바닥 측정 | `measure_reference_axis.py` (elbow-twist-sister 우선) | 문턱까지 여유를 모른다 |
| 3 | 같은 영상 10회 재분석 | 현행 판 재현성 | 12.8% 위양성의 재현성 |
| 4 | 안전 경고 국면 실물 대조 | ra8 §5 | 창을 바꿨는데 안 봤다 |
| 5 | oxg 로 새로 살아난 카드 21장 | 기계 눈 먼저 | ★§7-2 주의 |
| 6 | `angle_bake=omitted:*_crop_relaxed` 집계 | 라이브 로그 세기만 | 새 코드 0, 판정이 한 번에 끝난다 |
| 7 | (켤 경우) mode3 기준 축 발화 로그 | `scoringBasis` 라벨 | 배선 주장에 실행 증거가 없다 |

### 7-2. ★ 주의 — 오늘 바꾼 창이 **카드 사진의 순간**을 움직인다

`_select_window` 는 점수뿐 아니라 **대표 프레임**도 고르고, 그 인덱스를 확대카드가
앵커로 쓴다(`fault_zoom.py` `atFrameIdx`). 즉 `97a451f8` 이후 **같은 분석이라도 다른
사진이 나온다.**

→ belle 판정 대기 중인 사진들(oxg 21장 · mhl 3장 · 8.1초 판정판)은 **전부 옛 창에서
렌더된 것**이다. 옛 사진에 받은 ○× 를 새 카드에 물려받으면 안 되고
([[grammar-approval-is-not-asset-approval]]), 옛 사진으로 새로 물으면
belle 의 *"이걸 지금 몇 번째 하능거여"* 가 재현된다.
→ **새 창으로 다시 렌더한 뒤 묻는다.**

---

## 8. belle 에게 물을 것 — 짧은 순서

`[정리됨]` 아래 7건은 **이미 belle 이 답했다. 다시 묻지 말 것.**
§17-9 EXTEND 수리안(오늘 실행 완료) · 09-14 §6 4건 · hjy 기준 반영 ·
08-31 power-spin ○× · 08-31 kip-up 수리 · **감점 상한 40**(세 번째 금지) ·
확대 사진 규칙 09-18 승인분.

### 8-1. 오늘 열린 것 — 이것부터

1. ~~푸시해도 되나~~ — **belle 승인, 완료**(§2-1). `origin/main` = `2b8b2d23`.
2. ~~영상~~ — belle: *"요청해놔서 일단.. 몇 일 내로 받아볼게"*. **오면 관문 3개를 잰다.**
   그때까지 참고: 관문 ①③ 은 '수강생'이 아니라 **'정은지가 아닌 한 사람'**이면 잰다 — 관문 3개 중 ①같은 기준에 일관되게
   붙나 ③카메라 각도 내성은 **'수강생'이 아니라 '정은지가 아닌 한 사람'이면 잰다.**
   belle 본인이 아무 동작 2편만 찍어도 오늘 막힌 항목의 절반이 열린다.
   ②강사 ○× 만 도메인이라 belle 이 답해야 한다.
3. ~~OTA 를 쏠까~~ — **belle 승인, 발행 완료**(§2-1).

### 8-2. 실증 전에 닫아야 하는 것

4. **100점인데 "오늘은 이 부분에 집중해봐요"** — 감점 0이면 mission 이 소멸해 헤드라인이
   그렇게 바뀐다. 버전을 맞추면 정은지 영상은 11/11 100점이라 **실증에서 곧바로 뜬다.**
5. **보완운동 한국어 이름 5건** — `매달려 어깨 내리기` 는 **내가 지어낸 말**이다.
   학원에서 부르는 이름이 정답. 수강생 화면에 나가는 문자열.
6. **약관이 영상·자세 데이터를 덮나** (법무) — 수강생 영상이 들어가기 전에.

### 8-3. 안 급한 것

7. ★**mode3 점수 없이 실증 가능한가 — OTA 를 쐈으므로 이제 실물이 됐다.**
   선택지를 글로 적어두면: (A) 점수 없이 간다 (B) 관문 통과 후 플래그 ON (C) OTA 보류.
8. 죽은 `gemini_cache` 170건 처분 — 급하지 않다. 다만 **캐시가 사실상 비어서
   실증 당일 Gemini 비용·지연이 늘어난다**는 사실이 더 중요하다.
9. 브랜드색 대비 미달 2건 · 눈 게이트 엉덩이/어깨 · 붕괴 게이트 문구 · Phase 34 범위 2건.

---

## 9. 함정

- **서브에이전트 게이트 수치를 믿지 말 것** — worktree 에 `backend/.venv` 가 없다.
  오늘 4989/0 은 내가 직접 쟀다.
- **Firestore 전수 스캔 금지** — Spark 무료, 읽기 5만/일 하드 캡.
- **Pod 로그는 재기동 *전에* 내려받아라** — `start_server.sh` 의 `>` 가 truncate 한다.
- **볼륨 사본은 자동으로 안 바뀐다** — 기동 전 리포에서 복사.
- **ROT180 은 기준 라이브러리 승격과 한 묶음** — 한쪽만 되돌리면 지금보다 나빠진다.
- **착수점은 하나다** — `.planning/quick/260920-cac-.../260920-cac-SUMMARY.md`.
  ★**§14 부터 읽을 것.** §0~§13 은 판정이 뒤집혔고 맨 위에 배너가 있다.

---

## 10. 미커밋으로 남긴 것

없다. 작업트리 clean.
origin/main 동기 완료(`2b8b2d23`).
