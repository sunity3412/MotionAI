---
quick_id: 260923-t57
slug: 2026-09-23-night
date: 2026-09-23
status: complete
---

# 인수인계 — 2026-09-23 (하루 마감, 새 섹션 첫날)

> **다음 세션은 여기부터.** 이전 착수점 = `260923-m49-HANDOFF.md`(이력 — §1·§2 규율은 승계).
> belle 마감 지시(09-23 밤): *"mode1의 안정화를 위해 최선을 다하자. 좀 끝내자 인제… 두달 째다."* — 다음 세션의 기준은 이것 하나.
> 새 기능 없음. **Mode 1 이 같은 영상에 같은 답을 내고, 틀린 자세를 틀렸다고 말하게 만드는 일**만.

## 0. 30초 요약

```
정리   착수점 1개 — CLAUDE.md 는 이제 경로 대신 STATE stopped_at 을 가리킨다
       빈 phases/37- 제거(find-phase 37 이 그걸 돌려주던 고장) · STATE 페이즈 실측
수리1  감점 재료가 기준 영상의 엉뚱한 구간과 비교되던 결함 — 학생 영상이 기준의 1.2~1.5배 길이일 때
       재현: 정은지 영상 앞 15% 만 자른 것 → 84~89점 (수리 후 100). 과거 저장 점수는 불변
수리2  ★Gemini 에 보내는 정은지 still 이 다른 순간이던 결함 — **모든 Mode 1 분석**(u2q)
       같은 테이크 픽셀 대조: 같은 순간 0/10 → 9/10. 점수 영향은 미확인 → §3 의 0단계
도구   영상 받는 도구(intake_clips.py) + 짝 비교(measure_reference_axis.py --pairs)
정정   kip-up "20.1도 · 0.12점 반올림"은 운영 계산이 아니었다 — 저장값 20.62도, −0.7점 측정오차 억제
       판정 "배선 고장 아님"은 그대로
검증   ★belle 충전 뒤 Pod 띄워 fixture 10편 재분석 완료(weq): 정타 5/5=100 · 실수 변별 4/5 — 어제와 같다
       다운로드 멎음(30분)은 S3 가속 엔드포인트로 우회(1~3초). ★GPU 종류가 바뀌면 각도가 일부 프레임에서 바뀐다(8/8)
인프라 Pod 0(종료) · Lambda 자리표시자 · 잔액 $23.12 · 게이트 5010/0 · origin 동기
다음   belle 영상 오면 §3 절차(0단계는 끝났다). Success 0 은 belle 확인 대기(§4)
```

## 1. 다시 하지 말 것 (승계 + 추가)

- **허용오차 20도 축은 죽었다**(m49 §1). 오늘도 재지 않았다 — 저장된 감점 기록을 읽었을 뿐이다.
- **"할 수 있는 것"으로 우선순위를 정하지 말 것**(m49 §2).
- ★추가: **짝·관절 편차를 손으로 계산하지 말 것.** `measure_reference_axis.py --pairs` 를 쓴다
  (편차 = 운영 `_deviation_against`, 점수·감점 = 저장값). 09-23 손 계산이 원시 각도로 DTW 를
  돌려 20.1도를 냈고 그게 인계서·게이트 표·메모리까지 번졌다.

## 2. 관측 — 오늘 한 것 (quick 5개, 커밋 전부 push)

### 2-1. 260923-smt 섹션 착수 정리 (`95b5dfac`)
- CLAUDE.md 착수점 경로가 09-20 문서를 가리키고 있었다(그 뒤 3번 바뀜) → STATE `stopped_at` 간접참조.
- `.planning/phases/37-/`(빈 .gitkeep) 제거 — `gsd-sdk query find-phase 37` 이 이 빈 디렉터리를
  돌려줬다. 이제 `37-continuous-learning`.
- STATE 페이즈 실측: 39 · 완료 30 · 진행 4(22·31·33·36) · 미착수 5(18·21·34·35·37) · plan 205/195.
  frontmatter `progress` 는 GSD 관리값이라 본문과 다르다 — 본문이 실측.
- kip-up 정정 `[확인]`: 저장된 doc `e83811d3` 의 `suppressedRecords` = 왼어깨 20.62도,
  wouldBePoints −0.7, 구간 15.89~24.33(허용 20.0 포함) → 억제 → 100점.

### 2-2. 260923-sqt 감점 seed 기준 창 결함 수리 (`3348a8e4`)
- `[확인: 코드]` `_build_deduction_measured_deviations` 의 DTW fallback 이 window-local path 로
  **전체** 기준 배열을 인덱싱했다. 점수 경로·표시·segment 는 잘라 쓰는데 이 builder 만 빠졌다.
- `[확인: 재현]` 운영 함수 + 실제 기준 doc, 학생 = 정은지 기준 영상 앞 15% 절단:
  kip-up 89 · power-spin 85 · climb 84 (점수 경로 편차 0도인데 감점 재료 29~33도). 수리 후 100.
  합성 입력에선 진짜 25도 차이가 19.7도로 읽혀 감점이 사라지는 반대 방향도 나왔다.
- `[확인]` 2026-09-22 운영 11건은 전부 창 [0, 기준 전체) — 이 결함 밖, 과거 점수 불변.
- `[확인: 산식]` 걸리는 조건 = 학생/기준 프레임 0.80~1.00 = **학생 영상이 기준의 1.2~1.5배 길이**
  (학생 분석 ~10fps, 기준 ~15fps). kip-up 9.4~11.8초 · power-spin 12.7~15.9초 · climb 9.6~12.0초.
- ★**정은지 fixture 는 전부 이 밖(0.52~0.77)** — fixture 로만 재면 창 미끄럼 경로를 한 번도 안 탄다.
- 회귀 테스트 3건(수정 전 3 failed). 09-23 밤 Pod(86fa0f89)에서 실행 확인 — fixture 는 이 경로를 안 타서 점수 변화 0.

### 2-3. 260923-swh 영상 받는 도구 + 짝 비교 (`be7d0c7d`)
- `intake_clips.py register` — 메모 시트 → 내용 해시 → S3 영구 사본 `fixtures/intake/` → 원장
  (`clips.jsonl` · `subjects.jsonl` · `pairs.jsonl`). 한 줄이라도 틀리면 아무것도 안 쓴다.
- `intake_clips.py analyze` — Pod 있을 때만 · 직렬 · 시간 초과 시 배치 중단 → `analysis_runs.jsonl`
  (video_hash → uid·analysisId·analysisVersion).
- `measure_reference_axis.py --pairs/--pair/--clips` — 새 계산 없음.
- `[확인: 코드]` **사람 영상은 manifest.json 에 넣지 않는다** — `eligible_for_distill` 이 동의를 안 보고
  s3_key 행을 증류 후보로 자동 선택한다. 37-DATA-SPEC §2-2 개정.
- 소급: phase15 fixture 12편 + 09-22 분석 11건(S3 ETag 대조 11/11). kip-up 짝이 저장값 재현.
- Pod 분석 경로(`analyze`)는 **미실행** — Pod 없어서 거절 경로만 확인했다.

### 2-4. 260923-u2q Gemini 정은지 still 시각 수리 (`e347911e`) — belle *"이제 시작이야 뭐하지?"* 뒤
- `[확인: 코드]` veto still 경로가 기준 **각도**(~15fps) 번호를 기준 **영상 프레임**(~10fps)에 그대로 꽂았다.
  확대 카드는 28-05(07-08)에 같은 변환을 넣었고 이 경로(23-01, 06-23)만 빠졌다. `_matched_ref_frame` 도 ref_start 누락.
- `[확인: 실영상]` kip-up 정타 = 기준과 같은 테이크 → 픽셀 대조로 같은 순간 **0/10 → 9/10**.
  수리 전: 학생 2.0초에 정은지 3.0초, 후반 1/3 은 기준 마지막 프레임. 정량화도 후반이 5.2초에 눌렸다.
- `[확인: 저장 인덱스]` 09-22 Gemini 판정 실행된 실수 4건 중 3건의 still 이 1.1~3.1초 어긋나 있었다.
- 캐시 키 = 보낸 이미지 번호로 바꿨다 — 안 바꾸면 이미지가 바뀌어도 어긋난 예전 판정이 캐시로 되살아난다.
- `[미확인]` **점수 영향.** Gemini 는 두 영상 전체 + still 을 같이 본다. 7월 "어긋난 still 이면 Gemini 가
  편차 없음"(0/6 vs 6/6) 기록은 그때의 "맞은 쌍"도 이 결함이 있었을 수 있어 재검증 대상.
- 확대 카드 **표시** 경로는 안 건드렸다(비교 영상 정지 순간 상속 — belle 승인 문법).

### 2-5. 260923-weq Pod 재분석 + 다운로드 우회 (`86fa0f89`) — belle 충전 뒤
- `[확인]` EU-RO Pod ↔ 서울 S3 직결은 연결 단위로 멎는다(30MB 4회 중 3회 5~7KB/s, 두 호스트 모두).
  **S3 Transfer Acceleration** 으로 다운로드만 우회 → 10편 전부 1~3.5초. 버킷 가속 ON, `S3_USE_ACCELERATE=1`.
- `[확인]` fixture 10편 재분석: 정타 100×5 · 실수 73/100/86/80/87 (09-22: 66/100/89/85/87). veto 상태 10/10 불변.
- `[확인: 2/2 vs 8/8]` **같은 GPU 종류면 각도 완전 동일, 다른 종류(4090↔L4)면 일부 프레임(3~16%)에서 달라진다** —
  연속 구간에 몰린 약 20° 차이. 실수 점수가 ±3~7 움직이는 원인. → 실증 Pod 은 **GPU 한 종류로 고정**.
- `[추정]` power-spin 정타 stability 84→96 은 각도 동일인데 움직였다 — Gemini recognizer 국면 창 차이(신규 호출).
- 오늘 수리 2건의 점수 효과는 GPU 변수와 섞여 `[미확인]`. 확실한 것 = 나빠지지 않았다.
- 내 운영 실수 2건: `pod_teardown.py --urls-only` 는 조회가 아니다(주소 지움) · ssh 백그라운드는 `< /dev/null`.

## 3. ★ belle 영상 받는 절차 (2026-09-24 경)

> **0단계 — 2026-09-23 밤 완료(§2-5).** 다시 할 필요 없다. 아래는 기록용:
> `intake_clips.py analyze --hash 83ee8fc690dd 8c206887023c 8a6f9f8f7f7c 574a774c7d19 a73d4fff1bdd 45090cede718 2199c10603b6 cc5dc7ad03d7 f499f163c03a 1f7667694e14`
> (fixture 5쌍) → `measure_reference_axis.py --pairs` → 09-22 기록(정타 100 / 실수 66·85·87·89·100)과 대조.
> ★첫 분석의 `analysisVersion.commitSha` 가 e347911e 이후인지 먼저 확인.

```
1. 파일마다 확인: 동작 이름 · 누가(학생은 "학생 1" 식) · 잘한 것/일부러 틀린 것(+무엇을) ·
   촬영 방향(옆/앞/뒤) · 학생·강사면 학습 사용 동의 여부(모르면 "모름")
2. 파일 → /Users/Shared/sunity-intake/<날짜>/   ★홈 폴더는 git 저장소 — 사람 영상 두지 말 것
3. 시트(JSONL) 작성 — 형식은 intake_clips.py 머리말. 학생 = sub_s01… 이름 금지, note 에도 이름 금지
4. backend/.venv/bin/python backend/scripts/intake_clips.py register --sheet <시트> --dry-run
   → 확인 → --dry-run 빼고 본 등록
5. Pod 기동 ([[demo-only-pod-bring-up-procedure]] 09-23 절 = API 생성·nohup 규율) — ★GPU 는 한 종류로(4090 우선, 없으면 L4 —
   섞으면 같은 영상의 각도가 바뀐다). 다운로드는 가속 엔드포인트라 링크 시험은 참고만
   ★첫 분석의 analysisVersion.commitSha 가 86fa0f89 이후인지 확인(09-23 밤 검증한 판)
6. intake_clips.py analyze --pending          (직렬 — 편당 수 분)
7. measure_reference_axis.py --pairs          (짝)
   measure_reference_axis.py --clips --floors backend/scripts/reference_axis_floors.json  (학생 단독)
8. Pod 종료: pod_teardown.py <podId>          ★무인자 금지 — 계정이 다른 프로젝트와 공용
9. belle 보고: 된다 / 반쪽 / 안 된다 먼저
```
학생 영상 = **정은지가 아닌 첫 사람.** 재학습 게이트 4(인물·세션 분리 평가셋)의 첫 조각이고,
오늘 고친 창 미끄럼 경로가 처음으로 실제 데이터에서 돌 수 있는 자리다.

## 4. belle 판단 대기

- **Success 0** (감점 0건을 정상/미검사/판정불가로 나눠 표시) — 0순위로 적혀 있으나 표시 계층을
  건드린다. "실증까지 분석만" 과의 경계라 착수 전 belle 확인 필요(m49 §9 보류 승계).

## 5. 재학습 게이트 — 7개 중 1개 (변동 없음)

`.planning/TRAINING-DUE.md` 정본. 1 건수 ✅ · 2 원인 🔶 · 3~7 ❌. 잔액 $23.12(사이클 $33~38 에 아직 못 미침).
게이트 4 는 오늘 **도구**가 생겼다(clips 에 subject_id·capture.session) — 데이터는 아직 0.

## 6. 미확인 — 진단 아님, 다음 세션 재검증 대상

- **Gemini still 수리(u2q)의 점수 영향** — 0단계를 돌렸으나 GPU 변수와 섞여 못 갈랐다(§2-5). 같은 GPU 로 09-22 판을 다시 돌려야 갈린다.
- **기준 각도(rot180_v1)를 뽑은 GPU 종류** — 모른다. 학생 Pod 과 다르면 편차에 GPU 잡음이 얹힌다.
- kip-up 실수에 Gemini 가 "차이 거의 없음"을 낸 것과 still 어긋남(u2q)의 관계 — 가설일 뿐이다. 오늘 still 을 고친 뒤에도
  kip-up 실수 veto 는 not_applicable 그대로였다(§2-5).

- 창 미끄럼 결함이 실제 학생 영상에서 몇 %나 걸리는가 — 학생 영상 0편이라 모른다.
  DTW 창이 모호하면 전체 기준으로 떨어지므로(fail-closed) 산식 구간보다 적게 걸릴 수 있다.
- ~~`fixtures/phase15/<동작>/correct.mp4` 와 기준 사본 같은 테이크인지 미확인~~ → **닫힘 (09-24 c3m)**: kip-up 은 같은 테이크
  (79/79 프레임 최근접 오프셋 0, 픽셀차 0.2 vs 무작위 7.0). 다른 동작은 미대조.
- ~~kip-up 국면 정렬 품질 — 미착수~~ → **닫힘 (09-24 c3m, `260924-c3m-SUMMARY.md`)**: 측정은 맞다(실수 왼어깨 스텝 절반이 30~60도),
  집계가 지운다 — 영상 전체 중앙값이 두 봉우리 사이 틈(20.6)에 떨어지고, 기준이 한 바퀴 더 도는 동안 학생 한 프레임에 **정체**(22스텝)한 것이 낮은 표본을 공급.
  정체 제외만이 CI 하한을 20 위로 올린다(27.6, 정타 무영향). 수리 방향은 belle 결정 대기(SUMMARY §5).

## 7. 인프라

```
분석 Pod      0대
Lambda        RUNPOD_ANALYZE_URL = https://pod-down.invalid/analyze
SSM           pod-expected = down
잔액          $23.12 (belle 09-23 밤 충전 +$20, 오늘 Pod 2대 ≈ $1.1). 다른 프로젝트(Sunityfunding) Pod 이 같은 잔액을 쓴다
origin/main   이 인계서 커밋까지 동기
상태 보드     아티팩트 733f91d2 는 9월 초 이후 갱신 안 됨 — 인계서가 사실상 정본
```

## 8. 오늘 셈 — 고친 것 / 안 것 / 안 된 것

```
고친 것 (코드, push 완료)   감점 창 어긋남(sqt) · Gemini still 시각(u2q) · 다운로드 멎음 우회(weq) · 영상 받는 도구(swh)
안 것 (관측)               같은 GPU 면 같은 영상 = 같은 각도(2/2), GPU 가 다르면 일부 프레임이 바뀐다(8/8)
                            fixture 는 창 미끄럼 경로를 한 번도 안 탄다 · 09-23 손 계산은 운영 계산이 아니었다
안 된 것                   kip-up 실수 여전히 100 · 오늘 수리 2건의 점수 효과는 GPU 변수와 섞여 못 갈랐다
                            Success 0 미착수(belle 결정 대기)
```

## 9. 미커밋

없다. origin/main 동기.
