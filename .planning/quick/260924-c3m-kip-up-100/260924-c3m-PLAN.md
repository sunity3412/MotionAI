---
quick_id: 260924-c3m
slug: kip-up-100
date: 2026-09-24
mode: quick (inline — 서브에이전트 미사용, 아래 사유)
---

# Quick 260924-c3m — kip-up 실수 영상이 100점인 이유: 국면 정렬 품질

belle 2026-09-24: *"넌 왜 자꾸 이걸 100점이라고 치는거야? 이유가 있을 거 아냐"* / *"시작해 … 최선을 다해주게"*.
착수점 t57 §6 의 `[미확인]` "kip-up 국면 정렬 품질(DTW distance 정타 6.51 vs 실수 26.94)" 을 닫는다.
코드 변경 0 · Pod 0 · 크레딧 0. 저장된 각도 + 운영 순수 함수 + 영상 정지컷만.

**인라인 실행 사유:** 최근 quick(260922-gnj · 260923-k02/m49/smt/sqt/swh/u2q/weq)이 전부 인라인.
worktree 서브에이전트는 `backend/.venv` 가 없어 다른 인터프리터로 돈다([[dont-trust-subagent-gate-numbers]]).
`use_worktrees=false` 확인. 이 과제는 숫자를 소비처까지 따라가는 일이라 맥락 단절이 곧 오측이다(09-23 k02 의 손 계산 재발 방지).

## 규율 (m49 §1·§2 + t57 §1 승계)
- 허용오차 20도 축은 죽은 축 — 문턱을 옮기지 않는다. 재지도 않는다.
- 짝·편차는 손 계산 금지 — `measure_reference_axis.deviate` = 운영 `app._deviation_against` 를 부르고,
  스텝별 |Δ| 는 `per_joint_deviation` 과 **같은 diffs·같은 마스크**로 재구성한다. 게이트: 재구성 median == 운영 dev.
- 정지컷은 운영 `FfmpegFrameExtractor` 로 뽑는다(각도 프레임 번호 ↔ 정지컷 1:1). 게이트: 프레임 수 == 저장 각도 프레임 수.
- 관측과 진단을 다른 절에. 판정은 사진 + ○× 로 belle 에게, 내 예측을 먼저 박제.

## Task 1 — 스텝별 편차의 시간 분포 (어깨·팔꿈치가 어디에 몰려 있나)
- 입력: doc e83811d3(실수 09-22) · 9fbe0cab(정타 09-22) · 36b98f5c(실수 09-23, 다른 GPU) + 기준 ref-kip-up(rot180_v1).
- 운영 path 위에서 관절별 signed Δ · |Δ| 를 스텝마다 내고 학생 시간축 구간별 median, LS>20도 스텝 비율, 정체(한 학생 프레임에 기준 여러 프레임) 목록.
- **done:** 게이트 3/3 통과(median 재현) · 구간표 · 정체표가 SUMMARY 에 실린다.

## Task 2 — 짝 검증 (DTW 가 실수 영상의 어느 순간을 정은지의 어느 순간과 짝지었나)
- 운영 extractor 로 fault 68 · correct 79 · ref 118 프레임 추출(게이트: 수 일치).
- path 를 따라 (학생 u | 기준 r) 정지컷을 나란히 붙인 대조표 3장: 실수 전구간 · 실수 큰편차+정체 구간 · 정타 통제군.
- 내 ○× 예측을 먼저 적고 belle 확인용 사진으로 남긴다. 좁은 크롭 판정은 내가 하면 틀린다([[my-tight-crop-eye-is-unreliable-twice-wrong]]) — 전신 국면만 본다.
- 덤: correct.mp4 와 ref-kip-up.mp4 가 같은 테이크인지(t57 §6 미확인) 프레임 픽셀 대조.
- **done:** 대조표 PNG 가 quick 디렉터리에 있고 SUMMARY 에 예측(○×)이 적혀 있다.

## Task 3 — 관측/미확인 분리 보고 + 인계
- SUMMARY(관측 절 / 진단 절 분리) · STATE `stopped_at` · 착수점 인계서 t57 §6 갱신 · 메모리 1건.
- **done:** commit + push, origin 동기.
