---
quick_id: 260924-ig3
slug: window-constant
date: 2026-09-24
mode: quick (inline — 같은 사유: backend/.venv 필요, 채점 seed 는 맥락 단절이 곧 오채점)
---

# Quick 260924-ig3 — reference_relative seed 에 "유지 구간 상수" 경로 배선 (kip-up 수리 2단계)

belle 09-24 *"응 진행해"* (i38 측정 통과 뒤). 정본 설계 = `260924-i38-SUMMARY.md` §4.

## 불변
- 허용오차 20 · slope 1.2 · record 문법 · 표시층 **무접촉**. Gemini 가 짚은 관절의 window 경로 우선순위도 그대로.
- 새 경로는 **DTW-fallback 자리만** 대체한다: pointed∩wm → window / 기준 exec 창 있음 → **constant** / 그 외 → DTW(종전 byte-동일).
- fail-closed: 기준 doc 에 `clipRange` 없음 · 창이 너무 짧음 · 표본 부족 · NaN → 그 관절은 DTW 로.
- 플래그 `REFERENCE_CONSTANT_WINDOW_ENABLED`(default 1, "0" 이면 OFF) — 호출측에서 읽는다(빌더는 순수).

## Task 1 — 순수 헬퍼 3개 (app.py, 빌더 밖)
`_reference_exec_window(ref_doc, ref_fps, n_ref)` → (r0, r1)|None · `_student_window_from_match(match, r0, r1)` → (u0, u1)|None ·
`_window_constant_samples(angles, ref_angles, u_win, r_win)` → {joint: signed 표본(학생 각도 − 기준 창 median)}.
i38 의 `hold5_measure.py` 와 **같은 산식**(그 표가 통과 근거).

## Task 2 — 빌더 배선
`_build_deduction_measured_deviations(..., ref_exec_window=None)`: constant 경로 = |median(표본)| 방출 + 순간(창 안에서 학생 median 에 가장 가까운 프레임, 동점은 신뢰도) + measurement_error(표본 순서통계 CI, signed → |·| 구간) + seed_audit `constant_joints` + 로그.
호출측(`_process`, 8894 부근): mode1 기준 doc 의 clipRange → `ref_exec_window`(플래그 ON 일 때만).

## Task 3 — 테스트 + 재현
- 새 테스트: 창 없음 = byte-동일 · 합성(창 안 +30° 오프셋 → 값 30, 희석 0) · 창 짧음 → DTW · expects_extension 게이트 · CI 부호 변환.
- 기존 seed 테스트 전부 통과(`backend/.venv` 로 직접).
- 재현 게이트: 실제 kip-up doc(e83811d3)+기준으로 빌더 호출 → left_shoulder ≈ 33.9 (i38 표).

## Task 4 — Pod 검증 (같은 GPU, 1회) + 인계
fixture 10편 재분석: kip-up 실수 < 100 · 정타 5/5 = 100 · 4 실수 점수 하락 방향. `--pairs` 표. Pod 종료(podId 지정). SUMMARY/STATE/메모리.
