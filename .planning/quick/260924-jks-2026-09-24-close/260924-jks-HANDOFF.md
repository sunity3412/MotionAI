---
quick_id: 260924-jks
slug: 2026-09-24-close
date: 2026-09-24
status: complete
---

# 인수인계 — 2026-09-24 (하루 마감) — 다음 세션은 여기부터

> **착수점은 이 문서 하나다.** 이전 착수점 `260923-t57-HANDOFF.md` 는 이력(§1 규율은 승계).
> belle 마감 지시(09-24): *"인계서 작성하고 새 섹션에서 시작하자. 새 섹션에서 헷갈리지 않게 확실히 정리해."*
> 새 세션이 할 일은 **§4 하나**다. §3 에 적힌 것은 끝난 일이고 다시 조사하지 않는다.

## 0. 30초 요약

```
닫힘   kip-up 실수가 왜 100점인지 — 측정은 맞았고(어깨 30~60도) 영상 전체 중앙값이 두 봉우리 사이 틈(20.6)에 떨어졌다 (c3m)
닫힘   그 수리(기준 clipRange 창 안 짝 없는 각도 상수)가 5동작 전부에서 성립 — 사람이 구간을 짚지 않는다 (i38)
닫힘   배선 + Pod 검증 — kip-up 실수 100→83, 정타 5/5=100 유지, climb 60·peter-pan 60·pdshape 80·power-spin 80→(구간 제거 뒤 산식 68) (ig3)
남음   ★kip-up 한 편에서 "문장"과 "사진"이 belle 이 본 것(왼팔 덜 굽음 → 몸이 낮게 매달림)과 같은 말을 하게 만드는 것 — §4
belle  지쳤다. "같은 사람 틀린 것도 못 찾으면서" · "이거라도 완벽하게 하고 해야 할 말" · "Mode1 도 매번 짚어줘야 하는디" — 학생 영상·다음 단계 얘기 금지
인프라 Pod 0 · Lambda 자리표시자 · pod-expected=down · 잔액 $22.74 · origin 동기 · 새 경로 플래그 default ON(main)
```

## 1. 다시 하지 말 것 (t57 §1 승계 + 오늘 추가)

- **허용오차 20도 축은 죽었다.** 오늘도 안 건드렸다. 새 경로도 20·slope 1.2 그대로다.
- **짝·편차 손 계산 금지** — `measure_reference_axis.py --pairs`. 오늘 숫자는 전부 운영 함수 호출(게이트 = 재구성 median == 운영 dev).
- ★**유지 구간 상수에 순서통계 신뢰구간을 붙이지 말 것.** 창 안 표본의 구간은 측정 잡음이 아니라 **관절 움직임 폭**이라 진짜 편차를 억제한다(power-spin 어깨 26.4/23.9 억제 → 점수 ↑7). 이미 제거했다(커밋 e45d325f). 되살리지 말 것.
- ★**"조절"이라는 말을 쓰지 말 것.** belle: *"새 영상 주면 또 조절해야 함?"* 영상마다 상수를 만지는 일은 없다. 안 맞으면 구조에서 원인을 찾고, 못 찾으면 "고장"이라 보고한다.
- ★**학생 영상·다음 단계를 먼저 꺼내지 말 것.** belle: *"이거라도 완벽하게 하고 해야 할 말이지."* kip-up 한 편이 끝날 때까지.
- **좁은 크롭 표식 위치는 내 눈으로 판정하지 말 것**(2연속 오판 이력). 카드 후보는 사진으로 belle ○×.
- **kip-up 100점 이유·정렬 품질·일반화를 다시 재지 말 것** — §3 에 닫혀 있다. 다리 기울기 각도 안 갈린다(§3-4)는 것도 이미 쟀다.

## 2. belle 발언 원문 (오늘, 순서대로) — 관측, 정정 대상 아님

1. *"학생 영상에 너무 매몰되지 말고 지금 할 수 있는 걸 하자. 3번부터 갈까? 넌 왜 자꾸 이걸 100점이라고 치는거야? 이유가 있을 거 아냐"*
2. *"고민이군… 틀린 게 보이긴 한데.. 잡아내야 할 터인데. 어쨌든 최선을 다해주게"*
3. 대조표 판독: *"맨 윗줄은 시작의 동작이 다를 뿐 틀리다고 보진 않아. 우리 시작점도 맞추지 않아?"* → *"sheet1 k24 여기서부터 왼팔, 다리 뜨임의 틀림 시작"* · *"발의 높이의 영향이 왼팔의 굽힘 때문"* · *"kip-up 은 동일한 자세로 계속 도는 게 끝이라 어긋난 동작이 끝까지 연속"*
4. *"응 진행해"* (일반화 측정) → *"응 진행해"* (배선 + Pod 검증)
5. 확대 카드: *"각도 표기가 어긋났는데… 이 사진에서 뭘 말하고 싶은거지? 그냥 동그라미나 다른 표시를 해도 괜찮을걸"*
6. *"이제 지친다. 새로운 영상 주면 또 이런 식으로 결과 낼 거 아냐. 그럼 또 조절해야 함?"* → *"같은 사람 틀린 것도 못 찾으면서"* → *"이거라도 완벽하게 하고 해야 할 말이지"*
7. *"냉정하게 이 앱 너가 못 만들 것 같아? 안 될 것 같지?"* → *"근데 지금 Mode1 도 내가 매번 짚어줘야 하는디… 새로운 영상이라고 뭐 다를까"* → *"쩝.. 그래서 다음 뭐할건데"* → §4 제안 → *"인계서 작성하고 새 섹션에서 시작하자"*

## 3. 관측 — 오늘 한 것 (quick 3개, 커밋 전부 push)

### 3-1. 260924-c3m — kip-up 실수 100점 규명 (`65440ce3` `79a5e447` `b42c6a92`)
- `[확인]` 운영 경로 재구성 = 저장값(왼어깨 20.62). 왼어깨 |Δ| 는 두 봉우리(<15도 36스텝 / ≥25도 41스텝), 중앙값이 틈 20.6 에 떨어진다 = 허용오차 자리. GPU 다른 판 19.0.
- `[확인]` 낮은 표본의 절반 = **정체**: 기준이 3.67~4.6s 에 한 바퀴 더 도는 동안 학생 f33 한 장(22스텝). 정타는 정체 0.
- `[확인]` belle 판독 k24(학생 1.0s 점프)부터 끝까지: 학생 발이 키의 21% 낮음(짝의 94%), 정체 구간이 최대(−30%). 짝 없이 낮은발 5% vs 정은지 25%. 왼팔꿈치 덜 굽음(med 65 vs 59, 최소 41 vs 21). **왼어깨 raw 59.6 vs 25.1.**
- `[확인]` correct.mp4 == ref-kip-up.mp4 같은 테이크. 대조표 3장·발 높이 그림 = `.planning/quick/260924-c3m-kip-up-100/`.
- 내 ○× 예측 틀린 것 2건(정체 구간 "편차 작음", 높이가 어깨각으로 찍힌다) — 기록됨.

### 3-2. 260924-i38 — 유지 구간 상수, 5동작 일반화 측정 (`0d8f645a` `93b60fa5`)
- 창 = 기준 doc `clipRange.execStartS~landEndS`(5편 전부 등록돼 있다) → 운영 DTW 로 **가장자리만** 학생 축에. 창 안은 짝 없이 median.
- `[확인]` 기준 3/3 통과: 정타−정은지 높이 ≤1.9·각도 대부분 ≤7(예외 −10/−16/+13, 허용 안) · 4동작 계속 갈림(DTW 보다 크게) · **kip-up 왼어깨 +33.9**(DTW 19.0). 판 간(09-22/23) 상수 ≤5 vs DTW 12.1.
- 높이 축(발·엉덩이)은 kip-up·power-spin·peter-pan 만 갈린다. 표 = `.planning/quick/260924-i38-5-exec/hold5_table_0923.txt`.

### 3-3. 260924-ig3 — 배선 + Pod 검증 (`1d9a41e9` `e45d325f` `cbbed9f5`)
- 코드: `app.py` 순수 헬퍼 3개 + 빌더 `ref_exec_window` (pointed > **constant** > DTW), 호출측 플래그 `REFERENCE_CONSTANT_WINDOW_ENABLED`(default **ON**). 테스트 10 신규 · 5019 통과.
- `[확인: Pod L4, 앱 경로]` kip-up 실수 **100→83**(왼어깨 33.9 −16.6) · 정타 5/5=100 dims 동일 · climb 86→60 · peter-pan 80→60 · pdshape 87→80 · power-spin 73→80.
- `[확인]` power-spin ↑7 = 상수 경로 CI 가 어깨 2건 억제 → CI 제거(e45d325f). 산식 재계산 68, **Pod 재검증은 안 했다.**
- `[관측]` 60 = 관절당 −20 캡 × 2~3 + 합산 캡 −40 → 실수 크기가 60 에서 뭉친다(캡은 belle 원칙, 무접촉).
- 정본 `.planning/quick/260924-ig3-window-constant/260924-ig3-SUMMARY.md` + `score_compare_0923_vs_ig3.txt` + `kipup_fault_zoom_card_ig3.png`.

### 3-4. 마감 직전 추가 관측 (인계서에만 있음)
- `[확인]` **다리 기울기 각(엉덩이→발목 vs 수직)은 kip-up 을 못 가른다**(정타 +1.4/−0.4, 실수 −1.0/−4.4). belle 의 "다리 뜨임"은 다리 각도가 아니라 **몸 전체가 폴에서 낮게 매달린 것**(엉덩이 −17%, 발 −21%)이고, 어깨 34도와 같은 현상이다. 시스템은 못 본 게 아니라 **말을 못 하는 것**.
- `[확인]` 오늘 kip-up 실수 doc(dacc4467)의 문장: 어깨·추진력 방향은 맞다(기준 doc `checkpoints[left_shoulder]` "어깨 처지면 다리 스윙 추진력 손실"에서 온 whyLine). 그러나 tips 는 팔꿈치를 belle 판독과 **반대**("완전히 펴지 못하게")로 적고 "6도" 같은 근거 없는 숫자를 넣는다 — LLM 생성 층.
- `[관측]` 확대 카드는 record 의 순간(f14, 1.4s)이 아니라 DTW 짝 순간(userFrameIdx 28 / ref 3.6s)을 쓴다(표시 문법 상속). 빨간 각도 선이 어깨를 안 가리킨다(belle 5번 발언).
- `[미확인]` 발 높이 −21%(바닥) vs −5%(손 기준) 차 → 그립 손 자체가 낮다는 뜻일 수 있다. 안 쟀다.

## 4. ★ 다음 — 새 세션이 할 일 (belle "시작" 확인 뒤 착수)

**kip-up 실수 한 편(doc dacc4467, 영상 `fixtures/phase15/kip-up/fault.mp4`)에서 문장과 사진이 belle 판독과 같은 말을 하게 만든다.** 점수는 됐다(83). 학생·다른 동작·다음 단계는 꺼내지 않는다.

```
1. 문장 후보 — 잰 사실로만: "몸이 정은지보다 폴에서 낮게 매달렸어요(엉덩이 17% 아래). 왼팔꿈치가 덜 굽었고, 왼어깨가 34도 열렸어요."
   + 기준 doc 코치 한 줄(checkpoints). 없는 숫자·없는 관절 금지. LLM 이 채우던 자리가 어디인지(phrasebook / coach_writer / tips 생성기) 먼저 확인.
2. 사진 후보 — 발 높이 차 최대 순간(학생 3.3s 언저리 = 정체 구간)의 학생 | 정은지 나란히, 발과 왼팔에 **동그라미**, 각도 선 없음.
   재료: c3m 의 frames/(휘발) → `FfmpegFrameExtractor` 로 다시 뽑는다(프레임 수 게이트 68/118). 기존 카드 렌더 경로(compare_render / fault_zoom)는 읽기만.
3. belle 에게 사진 + 문장을 ○× 로. ○ 받은 것만 배선. 배선 범위 = kip-up 카드 하나(belle "이거라도 완벽하게" — 표시층 예외 승인은 이 한 건).
4. belle 한 줄 확인: "왼팔이 덜 굽어서 다리 높이가 낮아진다"를 kip-up 기준 doc checkpoints 에 belle 말 그대로 넣어도 되는지 — 동작당 한 줄, 영상마다 짚는 일을 없애는 재료.
```
규율: Pod 불필요 · 코드는 표시/문장 층만 · 점수 경로 무접촉 · 후보를 먼저 보여주고 승인 뒤 배선(문법 승인 ≠ 그림 승인).

> **진행 (09-24 밤, quick 260924-uff):** 1·2단계 후보 완료 → 판정지 `.planning/quick/260924-uff-kip-up-belle/judge_kipup_card.png`, **belle ○× 대기.**
> 재 보고 바뀐 것 `[확인]`: ① 왼팔꿈치는 운영 창 중앙값이 정은지와 **같다**(+1.1°) → 1번 초안의 "왼팔꿈치가 덜 굽었고"는 뺐다(다른 건 겨드랑이 +33.9).
> ② 그립 손 높이도 **같다**(−0.02 몸길이, 정타 −0.01) → §3-4 `[미확인]` "그립 손이 낮다?"는 아니다. "−21% vs −5%"는 단위가 섞인 비교(몸길이 비 vs 프레임 %).
> ③ 화면의 kip-up 문장은 전부 문구집 고정 문구이고 LLM `tips` 는 앱 소비처 0 — "LLM 이 채우던 자리"는 화면 밖이다.
> **belle 판정(09-24 밤): 사진·제목·설명·교정 4/4 ○** — *"괜찮네. 이게 내가 말해서 짜맞춰서 이렇게 된거지?"* → 답 = uff SUMMARY §8(출처 분리).
> 다음 = 3단계 배선(설계 = uff SUMMARY §6) — belle 진행 확인 대기. 배선하면 짜맞춤 여부를 기계로 가른다(정타에는 안 나오고 kip-up 실수에는 이 카드 그대로).
> belle 이어서 *"짜맞추는거면 이게 무슨 소용일지는 싶네 앞으로"* → 권고 = 같은 코드를 수정 없이 belle 이 짚지 않은 영상 9편(다른 동작 실수 4 + 정타 5)에 돌려 예측 박제 → belle ○×(uff SUMMARY §8). belle 확인 대기.
> 이후: belle *"아무도 짚은 적 없는 실수가 뭐냐"* → 못 재는 곳(코드 COVERAGE_GAP) 설명 · *"영구적으로 벗어날 다음 작업"* → **봉인 시험지**(새 테이크 + 결과 보기 전 한 줄 정답) 권고, belle 정은지 추가 촬영 부탁함(도착 대기) · *"지금 할거 하자"*.
> **3단계 문장 배선 완료 = quick 260924-vj1 (`33ffae38`)** — 잰 값 네 조건 전부일 때만 승인 문장, 저장 10편 게이트 = kip-up 실수만. **남은 것 = 사진**(영상 멈춤 순간 상속 규칙 때문에 record 순간부터) → Pod E2E.

## 5. belle 판단 대기

- §4 착수 "시작" (제안만 했고 belle 는 인계서부터 쓰라고 했다).
- §4-4 kip-up 코치 한 줄.
- 60 바닥에서 실수 크기가 뭉치는 것(캡) — belle 원칙이라 무접촉, 꺼낼 때만.
- 높이 축(발·엉덩이) 별건 여부 — 단위·IPSF 앵커 미결.
- Success 0(감점 0건 표시 분류) — t57 승계.

## 6. 미확인 — 진단 아님, 재검증 대상

- CI 제거 판(e45d325f)의 Pod 재검증 — 다음 Pod 기동 때 같은 10편(`intake_clips.py analyze --hash …` t57 §3 0단계 명령).
- 정타의 여유 적은 관절(power-spin 왼팔꿈치 −10·오른무릎 −16, peter-pan 왼어깨 +13; 판 간 불변) — 테이크 차이인지 창 가장자리인지.
- 다른 4동작의 문장 품질 — 안 봤다.
- Gemini still 수리(u2q)·기준 GPU 종류·창 미끄럼 비율 — t57 §6 승계.

## 7. 인프라 · 코드 상태

```
분석 Pod      0대 (eqmfk0nwgaxset 종료, 23분 $0.16)
Lambda        RUNPOD_ANALYZE_URL = https://pod-down.invalid/analyze   SSM pod-expected = down
잔액          $22.74 (Sunityfunding 과 공용)
main          새 경로 플래그 REFERENCE_CONSTANT_WINDOW_ENABLED default ON — Pod 은 기동 때 origin/main 을 당기므로 다음 Pod 부터 자동 적용. OFF = "0"
★ssh 기동     RUNPOD_POD_ID 가 없어 주소 자동 동기 SKIP → `export RUNPOD_POD_ID=<id>` 먼저 (메모리 갱신됨)
origin/main   이 인계서 커밋까지 동기
재학습 게이트  7개 중 1개, 변동 없음 (TRAINING-DUE.md)
```

## 8. 오늘 셈

```
고친 것 (코드, push)   유지 구간 상수 경로 배선(1d9a41e9) · 상수 경로 CI 제거(e45d325f)
안 것 (관측)          kip-up 100 = 중앙값이 두 봉우리 틈에 · 정체가 낮은 표본 공급 · 발 높이는 저장 키포인트에 있다 · 창 상수는 5동작 일반화
                       · 상수 경로 CI 는 움직임 폭이다 · 다리 기울기 각은 안 갈린다 · 문장은 LLM 이 지어낸다
안 된 것              문장·사진이 belle 이 본 것을 말하지 못한다(§4) · CI 제거 판 Pod 미재검증
belle 상태            지쳤다. 답은 짧게, 표는 하나만, 학생 얘기 금지
```

## 9. 미커밋
없다. origin/main 동기.
