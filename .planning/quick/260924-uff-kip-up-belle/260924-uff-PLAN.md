---
quick_id: 260924-uff
slug: kip-up-belle
date: 2026-09-24
mode: quick (inline — 서브에이전트 미사용, c3m/i38/ig3 와 같은 사유: backend/.venv 필요, 숫자를 소비처까지 따라가는 일)
---

# Quick 260924-uff — kip-up 실수 카드의 문장·사진 후보 (belle 판독과 같은 말)

착수점 = `260924-jks-HANDOFF.md` §4. belle *"이거 읽고 할 것 진행하자"*(= §4 "시작").
대상 = kip-up 실수 한 편: doc `dacc4467`(uid TbXjwbLp…, 1d9a41e9 · rot180_v1 · L4, 점수 83), 영상 `fixtures/phase15/kip-up/fault.mp4`.
belle 판독(09-24, 정정 대상 아님): *"k24(학생 1.0s)부터 왼팔, 다리 뜨임의 틀림 시작 … 발의 높이의 영향이 왼팔의 굽힘 때문 … 끝까지 연속"*.

## 불변
- **후보만 만든다.** 배선(문구집·파이프라인·앱)은 belle ○× 뒤. 이 quick 의 코드 변경 = 0 (scratch 스크립트는 운영 함수를 읽기만).
- 점수 경로 무접촉 · 허용 20 · 상수 경로 CI 금지 · "조절" 금지 · 학생 영상/다음 단계 안 꺼냄 · Pod 0.
- 문장은 **이 영상에서 잰 사실로만**. 없는 숫자·없는 관절 금지. 문구집 금지 규칙(`\d+%`, "N도만큼")도 지킨다.
- 좁은 크롭 표식 위치는 내 눈으로 닫지 않는다 — 사진은 기계 점검 결과를 붙여 belle ○×.

## Task 1 — 문장 출처 지도 (화면에 실제로 뜨는 것만)
- 화면 자리별 지금 문장과 출처(문구집 / 앱 포맷터 / LLM)를 코드로 추적. LLM `tips` 가 화면에 닿는지 확인.
- **done:** SUMMARY §1 표. 각 행 `[확인]` 근거(파일:행).

## Task 2 — 이 영상에서 잰 사실 (운영 창, 저장값)
- 창 = 운영 `_reference_exec_window` + `_student_window_from_match`(운영 `_deviation_against` 의 match). 창 안 짝 없는 중앙값.
- 각도 8관절 부호 있는 상수 · 높이(바닥 기준·손 기준, 서 있을 때 몸길이로 정규화) · 창을 셋으로 나눈 구간별 높이(“끝까지”인지).
- 게이트: 왼어깨 상수 == 저장 measuredValue 33.86 · 키포인트 프레임 == 각도 프레임 × 2 · 정타(9fbe0cab 계열 최신 정타 doc)는 같은 계산에서 0 근처.
- **done:** SUMMARY §2 표 + 문장 후보(각 절에 근거 값).

## Task 3 — 사진 후보 (발·왼팔 동그라미, 각도 선 없음)
- 운영 `FfmpegFrameExtractor` 로 정지컷(게이트: 프레임 수 68/118). 창 안 운영 DTW 짝 중 대표 짝(높이 차가 창 상수에 가까움 + 표식 관절 신뢰도).
- 두 패널을 같은 몸 크기·같은 바닥선으로 맞춰 나란히. 표식 = 왼팔·발 동그라미만.
- 기계 점검(넘기기 전): 표식 관절 신뢰도 · 왼손이 위 손(풀업 그립)인지 · 원이 패널 안인지. 걸린 것은 판정지 첫 줄에.
- **done:** PNG 가 이 디렉터리에 있고 SUMMARY 에 점검 결과.

## Task 4 — belle 판정지 + 인계
- 사진 1장 + 문장 3줄, 줄마다 ○×. 내 예측을 먼저 박제.
- SUMMARY(관측/진단 분리) · STATE stopped_at · 인계서 §4 진행 표시.
- **done:** commit + push.
