---
quick_id: 260924-vj1
slug: kip-up-card-text
date: 2026-09-24
mode: quick (inline — c3m/i38/ig3/uff 와 같은 사유: backend/.venv, 숫자를 소비처까지 따라가는 일)
---

# Quick 260924-vj1 — kip-up 카드 **문장** 배선 (belle 4/4 ○, uff 판정지)

belle 09-24 밤: 판정지 4칸 ○ → *"지금 할거 하자"*. 설계 = `260924-uff-SUMMARY.md` §6.
이 quick 은 **문장만** 건다. 사진은 영상 정지 순간 상속 규칙([[zoom-compare-canonical-do-not-reinvestigate]] 08-09)
때문에 합성 비교 영상의 멈춤 순간과 같이 바꿔야 해서 다음 quick 으로 분리한다.

## 불변
- 점수 경로 무접촉(허용 20·slope·캡·seed 값·순간). 바뀌는 것 = 해당 record 의 statusLine/whyLine/cueLine 뿐.
- 승인 문장은 **데이터**(phrasebook.json 새 절 `measuredVariants`) — 코드에 문장 0.
- **분석마다 잰 값이 패턴을 만족할 때만** 쓴다. 하나라도 못 재거나 어긋나면 지금 문구(byte-동일, fail-closed).
- 패턴 문턱은 잡음 폭에서 유도하고, 판정이 그 값에 둔감함을 테스트로 박제한다(짜맞춤 방지).
- 범위 = kip-up × `angle_vs_reference__left_shoulder` 한 항목(belle "이거라도 완벽하게"). 높이 측정 자체는 mode1 전부에서 로그만.

## Task 1 — 순수 측정 `sunity_shared/analysis/hold_height.py`
- `hold_window_heights(student_report, reference_report, u_win, r_win)` → 엉덩이·낮은발·그립 손 높이(바닥 기준)와 손 기준 높이,
  창 앞/중/뒤 1/3, 학생·정은지·차. 단위 = 서 있을 때 몸길이(창 이전 프레임), 바닥 = 창 이전 발목 y. 못 재면 None.
- `body_low_arm_open(heights, arm_signed_deg)` → 세 1/3 모두 엉덩이·낮은발이 잡음 폭 넘게 낮음 · 그립 손 같음 · 손 기준 엉덩이 낮음 · 겨드랑이 부호 +.
- 테스트: 합성 키포인트 정확값 · fail-closed 5종 · 판정의 문턱 둔감성(uff 실측값으로 0.03~0.12 동일).

## Task 2 — 문구 데이터 + 배선
- phrasebook.json `measuredVariants["ref-kip-up.angle_vs_reference__left_shoulder"]["body_low_arm_open"]` = uff 판정지 ○ 문장 3줄 그대로 + `_meta` 출처.
  `rendered_copy_strings()` 에 포함(금지어·숫자 게이트가 새 문장도 검사).
- app.py `_measured_phrase_variants(...)`(mode1, 상수 경로가 그 관절을 잰 경우만) → `_attach_translation_emission(measured_phrases=)` 가
  문구집보다 **먼저** 그 슬롯을 채운다(나머지 슬롯은 문구집 그대로). 높이 사실은 로그 한 줄(E2E 호출 증거).
- 테스트: 변형 있음/없음 byte-동일 · 슬롯 병합 · 로그.

## Task 3 — 오프라인 게이트(저장 doc) + 기록
- 운영 헬퍼로 저장 doc 재구성: kip-up 실수(dacc4467) → 승인 문장 3줄 · 정타(e6264535) → 변형 없음. 나머지 4동작 10편은 높이 로그만(짜맞춤 점검 재료).
- 기존 테스트 전부(backend/.venv). SUMMARY · STATE · 인계서.
- **done:** commit + push. Pod E2E 는 사진 quick 과 한 번에(구간 제거 판 재검증 포함).
