---
quick_id: 260924-vw2
slug: kip-up-card-photo
date: 2026-09-24
mode: quick (inline — 같은 사유: backend/.venv, 표시 순간 단일 출처 불변식을 따라가는 일)
---

# Quick 260924-vw2 — kip-up 카드 **사진** 배선 (belle 4/4 ○, uff 판정지)

선행 = vj1(문장, 33ffae38). 사진은 belle 08-09 규칙 **"카드 순간 = 합성 비교 영상 멈춤 순간"**
([[zoom-compare-canonical-do-not-reinvestigate]]) 때문에 한 곳만 못 바꾼다 — 순간의 출처(record)부터 바꾸고
영상 멈춤·카드가 그것을 물려받게 한다.

## 불변
- 점수 경로 무접촉. 다른 record·다른 동작의 순간·짝·표시 byte-동일 (새 필드 부재 = 종전).
- 순간을 고르는 곳은 한 곳(record). 영상·카드는 물려받기만 한다(재선정 0).
- 표시 = 왼팔·낮은발 **동그라미**, 각도 선 0 (belle 09-24 "각도 표기 어긋남 … 동그라미").
- 영상 리그(compare_verify) 전 항목 PASS 유지 — H2(멈춤 순간 == record 순간)는 구조로 성립.

## Task 1 — 대표 짝 (순수, hold_height.py) + record 필드
- `representative_pair(...)`: 창 안 운영 DTW 짝 중 1:1(정체 아님) · 폴(그립 손) 같은 쪽 · 표식 관절 신뢰도 ≥ MIN_CONF ·
  짝 높이 차가 창 상수(엉덩이·낮은발)에 가장 가까운 짝. 없으면 None → 문장 대체도 하지 않는다(카드 전체 종전, 반쪽 금지).
- record 새 필드(additive, 3-way 계약): `measuredPattern`, `atRefVideoSec`. 같은 record 의 `atFrameIdx/atVideoSec` = 대표 짝 학생 프레임.
- 테스트: 순수 규칙 · 방출 · 계약 lockstep.

## Task 2 — 영상 멈춤 (compare_align / compare_render)
- `select_pairs`: record 에 `atRefVideoSec` 가 있으면 그 기준 프레임 그대로(자세거리 재선정 0).
- `build_timeline`: `measuredPattern` record 는 ①폴 ②그립 ③가중 재선정·겨드랑이 각도선 생략, `circle_viz`(양 패널 왼팔·낮은발 원) 방출.
- `render`: circle_viz 그리기(표시 없는 판은 종전대로 표시 전 캔버스).
- 테스트: 짝 고정 · 문법 전환 · 다른 record 무변화.

## Task 3 — 카드 (게이트 상속)
- `_run_gated_card_inherit`: `measuredPattern` record 는 fault_zoom 좁은 크롭 대신 **영상 멈춤 프레임 두 장 + 같은 circle_viz**
  를 정사각 두 패널(앱 종횡비 726×360)로 합성, 표시 없는 판 동반. 카드 dict 키는 종전 계약 그대로.
- 테스트: 합성 이미지 형상·원 위치·plain 판.

## Task 4 — 검증 + belle
- 로컬: 저장 doc + 로컬 영상으로 카드 합성 함수 미리보기(운영 함수). 전체 테스트.
- Pod E2E 1회(같은 GPU 종류): kip-up 실수·정타 + fixture 10편(구간 제거 판 e45d325f 재검증 겸) → 로그(`hold heights`,
  `measured variant applied`, card_gates verdict) · 카드 PNG · 영상 멈춤 프레임을 **운영 산출물 그대로** 판정지로 → belle ○×.
- 앱 화면(시뮬레이터) 캡처로 최종 확인(문법 승인 ≠ 그림 승인).
