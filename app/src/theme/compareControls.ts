/**
 * 동작비교 영상 카드의 컨트롤 치수 (시안 2 실측 — 260909-ji1).
 *
 * 이 값들은 **두 재생 경로가 공유한다**: 합성 단일 mp4(RenderedComparePlayer)와
 * 듀얼 라이브 동기(VideoCompare). doc 이 어느 가지를 타든 belle 에게는 같은
 * 화면으로 읽혀야 하는데, 두 파일이 같은 숫자를 각자 박아 두면 한쪽만 손볼 때
 * 조용히 어긋난다 (09-09 sweep 지적). 그래서 한 곳에서만 센다.
 *
 * 가로 전체화면은 이 값을 쓰지 않는다 — 어두운 배경 위 종전 치수(재생 원 36 ·
 * 트랙 4 · 손잡이 14)를 그대로 둔다.
 */
export const compareCardControls = {
  /** 시안 재생 원 지름 */
  playDiameter: 22.4,
  /** 20 × 22.4/36 — 전체화면 아이콘을 같은 비로 줄인 값 */
  playIcon: 12,
  /** 시안 진행바 두께 */
  trackHeight: 4.8,
  /** 시안엔 손잡이가 없다 — 끌 수 있음을 알리는 용도로만 줄여서 남긴다 */
  thumbDiameter: 10,
  /** 트랙 터치 행 높이 (손잡이가 잘리지 않는 최소치) */
  trackRowHeight: 14,
  /** 카드 바깥변에서 컨트롤 행까지 (시안) */
  controlsInset: 42.8,
  /** 시안 영상 블록의 좌우 여백 */
  blockInset: 20.9,
} as const;
