# cu124 핀 레시피 — 드라이버 550 계열(CUDA 12.4 상한) Pod 용 train venv 재현 (quick-260821-o3m)
#
# 실측 환경 (2026-08-21):
#   Pod          : w340kaemere1po (RunPod, A100 80GB PCIe, sm_80)
#   드라이버      : 550.127.05 = CUDA 12.4 상한 — 볼륨 train_venv312(torch 2.13.0+cu130) 사용 불가.
#                  cu129 는 minor-compat 경계라 피함(locked 결정) → cu124 계열.
#   베이스 파이썬 : /usr/bin/python3.11 (Python 3.11.10) — 격리 venv(TRAIN_VENV_ISOLATED=1)
#
# v30 사인 (재발 방지의 핵심):
#   train_venv312 의 transformers 5.12.1 이 ms-swift 4.4.0 의 QLoRA 어댑터 부착 순서와 어긋나
#   validate_quantization_for_training 이 "You cannot perform fine-tuning on purely quantized
#   models" ValueError 로 train 즉사 (cycle_260818.log). 수리 축 = transformers 하향 핀.
#   (ms-swift 4.4.0 상향 금지 — SftArguments 필드명 변동, setup_train_venv.sh 참조.)
#
# 핀 근거 (전부 실측 — 추측 핀 금지):
#   - ms-swift==4.4.0 requires_dist (PyPI): transformers<5.13.0,>=4.33 / peft<0.20,>=0.11 / trl<1.0,>=0.15
#   - transformers 5.11.0 = 5.12(사망 버전) 미만 최신 안정판(PyPI, 2026-06-10 릴리스).
#     Qwen3-VL 지원(v5.11.0 태그에 models/qwen3_vl/modeling_qwen3_vl.py 존재 확인).
#     torch>=2.4 하한 → 2.6.0 충족. requires_python>=3.10 → 3.11.10 충족.
#     v29 실증 venv(/workspace/train_venv)는 볼륨에 부재 — 핀 규칙 (b) 적용.
#   - torch 2.6.0+cu124 = download.pytorch.org/whl/cu124 인덱스의 cp311 최종 빌드(실측).
#     torchvision 0.21.0 이 짝. ★torchvision 은 필수 설치다 (2026-08-21 실측): qwen_vl_utils
#     가 모듈 최상단에서 import torchvision — TRAIN_VENV_SKIP_VLLM=1 이면 vllm 이 안 끌어와
#     swift sft 가 [2/3] 에서 ModuleNotFoundError 즉사(v31 발사 1차). setup 스크립트의
#     스킵 분기가 명시 설치하고, 이 constraint 가 cu124 짝을 고정한다.
#
# venv 구축 커맨드 원문 (Pod, 2026-08-21 실행분):
#   cd /workspace/SunityMotion && \
#   TRAIN_VENV=/workspace/train_venv_cu124 TRAIN_VENV_ISOLATED=1 TRAIN_VENV_SKIP_VLLM=1 \
#   PIP_CONSTRAINT=/workspace/train_pins_cu124.txt \
#   PIP_EXTRA_INDEX_URL=https://download.pytorch.org/whl/cu124 \
#   bash backend/training/sft/setup_train_venv.sh
#   (이 파일을 Pod 의 /workspace/train_pins_cu124.txt 로 복사해 PIP_CONSTRAINT 로 주입.
#    메인 인덱스는 PyPI 유지 — nvidia-* 의존성 단절(08-14 libcudnn 사건) 재현 없음.
#    TRAIN_VENV_SKIP_VLLM=1 이라 이 venv 로는 gates 불가 — gates 는 vllm 가용 venv 로 별도.)
#
# 결과 실측 (2026-08-21): torch 2.6.0+cu124 / transformers 5.11.0 / peft 0.19.1 /
#   accelerate 1.14.0 / bitsandbytes 0.50.1 / ms-swift 4.4.0 / decord 0.6.0 / liger_kernel OK
#
# train 발사 커맨드 원문 (Pod):
#   cd /workspace/SunityMotion/backend && \
#   TRAIN_VENV=/workspace/train_venv_cu124 SFT_RESUME=none \
#   nohup bash training/sft/run_retrain_cycle.sh train > /workspace/cycle_260821.log 2>&1 < /dev/null &
#   (SFT_RESUME=none 필수 — 볼륨에 v29 checkpoint-* 가 남아 있어 auto 는 스테일 재개가 된다.)
torch==2.6.0+cu124
torchvision==0.21.0+cu124
transformers==5.11.0
