# Sunity AI Coach — design.md
> Figma 분석 기반 디자인 레퍼런스. Claude Code 개발 시 항상 참조.
> 피그마 파일: https://www.figma.com/design/npL3Iq2wYvTDHszDGrjG9l/

---

## 0. 미설계 컴포넌트 자율 설계 원칙

> **이 섹션이 최우선.** 피그마에 없는 UI가 나와도 절대 멈추지 말 것.

### 기본 원칙
```
피그마에 정의되지 않은 버튼, 화면, 팝업, 알림창, 아이콘 등
모든 미설계 요소는 아래 규칙을 따라 자체 판단으로 구현한다.
"디자인이 없습니다" "확인이 필요합니다"는 허용되지 않는다.
```

### 자율 설계 시 따를 결정 트리

```
1. 비슷한 컴포넌트가 이 문서에 있는가?
   YES → 그 패턴을 최대한 따라 확장
   NO  → 아래 기본값 사용

2. 버튼인가?
   주요 액션  → CTA 버튼 패턴 (5-3 참조)
   보조 액션  → 테두리 1px #B1B6BE, 배경 투명, 텍스트 #0C0C0C, radius 13pt
   위험 액션  → 텍스트만, 색상 #54B8CD (틸)
   텍스트 링크 → #FF4B33, underline

3. 팝업/모달인가?
   배경: #FFFFFF, radius 20pt
   오버레이: rgba(0,0,0,0.4)
   하단 시트 방식 권장 (아래서 올라오는 형태)

4. 알림/토스트인가?
   배경: #0C0C0C, radius 12pt, 텍스트 흰색 15pt
   상단 SafeArea 아래 위치, 3초 후 자동 사라짐

5. 빈 상태(Empty State)인가?
   아이콘 + 설명 텍스트 #ACACAC + CTA 버튼 구조
   배경: 화면 그대로 유지

6. 로딩인가?
   스피너 금지. 브랜드 컬러(#FF4B33) 계열 애니메이션 또는 단계별 텍스트 메시지
```

### 색상 기본값 (미정의 요소에 적용)

```
배경      : #FFFFFF (서브 화면) 또는 화면 테마에 맞게
텍스트    : #0C0C0C (기본), #ACACAC (보조), #FF4B33 (강조)
테두리    : #D9D9D9 (일반), #FF4B33 (활성/선택), #54B8CD (오류)
radius    : 13pt (버튼/입력), 15pt (카드), 20pt (모달)
패딩      : 좌우 20pt (화면), 16pt (카드 내부)
```

### 통일성 체크리스트 (구현 전 자가 검토)

```
□ 폰트가 Pretendard인가?
□ 브랜드 컬러 #FF4B33이 포인트 컬러로 사용됐는가?
□ 라이트 배경 기반인가? (다크 배경 사용 금지)
□ 이미 구현된 다른 화면과 radius, 패딩, 타이포가 일치하는가?
□ 보라색 계열이 없는가?
```

---

## 1. 디자인 방향성

> ✅ 피그마 실제 분석 완료. 이전 추정값 전면 수정.

**콘셉트**: 활기차고 따뜻한 AI 스포츠 코치. 무겁지 않고 에너제틱한 라이트 톤.

**테마**: 라이트 + 브랜드 컬러 그라디언트 혼합 구조.
- 가입/로그인/서브 화면 → 흰색 배경
- 홈 메인 화면 → 상단 브랜드 컬러 그라디언트 + 하단 흰색으로 페이드
- 전체적으로 밝고 따뜻한 살몬-코랄 계열

**절대 피할 것**:
- 다크 블랙 배경 (설계 의도 아님)
- 보라색 계열 AI 앱 클리셰

---

## 2. 타이포그래피

피그마에서 확인된 텍스트 스케일. 단위는 pt (React Native에서 그대로 사용).

```
Heading    30pt  Bold      대형 제목, 화면 메인 카피
Body       25pt  Regular   본문 설명
           25pt  Bold      강조 본문
Button     20pt  Bold      주요 CTA 버튼
           17pt  Regular   보조 버튼, 탭 레이블
Box        15pt  Bold      카드 내 레이블, 섹션 타이틀
           12pt  Regular   보조 설명, 메타 정보
           10pt  Regular   캡션, 최소 단위 텍스트
```

**폰트 패밀리**: ✅ Pretendard 확정 (한글/영문 모두)

**폰트 파일 위치**: `/pretendard.css` (프로젝트 루트)
→ 모든 컴포넌트에서 이 파일 기준으로 폰트 적용할 것.

**레터스페이싱 패턴** (fontSize × -0.04):
```
30pt → tracking: -1.2px (-1.2)
25pt → tracking: -1px   (-1)
20pt → tracking: -0.8px (-0.8)
18pt → tracking: -0.72px
17pt → tracking: -0.68px
15pt → tracking: -0.6px
12pt → tracking: -0.48px
```

**추가 확인된 텍스트 스타일**:
```
박스 18pt Bold  : 동작 리스트 타이틀
박스 20pt Bold  : 섹션 타이틀, 최근분석 종목명 (#FF4B33)
바디 25pt Bold  : 홈 종목명 (흰색)
버튼 50pt Bold  : 점수 숫자 (#FF4B33)
버튼 13.8pt Bold: NEW 뱃지 텍스트
```

---

## 3. 레이아웃 & 그리드

```
기준 프레임: 390 x 844 pt  (iPhone 14 기준)
Safe Area 상단: 59pt
Safe Area 하단: 34pt
바텀 탭 높이: 83pt (Safe Area 포함)
기본 좌우 패딩: 20pt
카드 내 패딩: 16pt
```

---

## 4. 컬러 시스템

> ✅ 피그마 실제 코드 추출 완료. 모든 값 확정.

```
/* ── 브랜드 ── */
메인                     : #FF4B33  ← Sunity 공식 브랜드 컬러
브랜드 그라디언트 시작   : #FFB5AB  (rgb(255,181,171))
브랜드 그라디언트 끝     : #FF604B  (rgb(255,96,75))
브랜드 버튼 그라디언트   : linear-gradient(111.41°, #FFB1A6 11.93%, #FF6651 89.96%)
브랜드 비활성 버튼       : #FFB7AD
브랜드 오버레이          : rgba(255,75,51,0.4)
브랜드 틴트              : rgba(255,75,51,0.15)

/* ── 배경 ── */
배경 기본 (가입/로그인)  : #FFFFFF
홈 상단 그라디언트       : linear-gradient(94.36°, #FFB5AB 1.74%, #FF604B 90.6%)
홈 하단 카드 배경        : linear-gradient(to-bottom, #FFFFFF, #FFF0EE)
카드 배경                : #FFFFFF
카드 대시 테두리         : #FF4B33 + border-dashed (종목 미설정 카드)

/* ── 텍스트 ── */
텍스트 주                : #0C0C0C  (거의 검정, 순수 #000000 아님)
텍스트 흰색              : #FFFFFF
텍스트 보조              : #ACACAC
텍스트 비활성            : #B1B6BE
텍스트 브랜드 강조       : #FF4B33

/* ── 상태 색상 ── */
성공/유효 (입력 테두리)  : #FF4B33  (브랜드 컬러 그대로 사용)
오류/경고 (입력 테두리)  : #54B8CD  (틸/청록색! 레드 아님 — 브랜드 컬러 충돌 방지)
[필수] 레이블            : #54B8CD
[선택] 레이블            : #B1B6BE

/* ── UI 요소 ── */
구분선                   : #D9D9D9
입력 테두리 기본         : #B1B6BE
바텀탭 활성              : #FF4B33
바텀탭 비활성            : #A5A5A5
Highlight                : #006FFD  (링크, 특수 강조)
Neutral Dark             : #1F2024

/* ── SNS 버튼 ── */
카카오                   : #FEE500  (텍스트: #0C0C0C)
네이버                   : #03C75A  (텍스트: #FFFFFF)
구글                     : #FFFFFF  (테두리: #B1B6BE, 텍스트: #0C0C0C)
애플                     : #000000  (텍스트: #FFFFFF)
```

> ℹ️ 오류 색상이 #54B8CD(틸)인 이유: 브랜드 컬러(#FF4B33)가 레드-오렌지라
> 일반적인 빨간 오류색 쓰면 혼동. 의도적으로 보색에 가까운 틸 계열 선택.

---

## 5. 핵심 컴포넌트 패턴

### 5-1. 화면 배경 (확정)

**가입/로그인/서브 화면**: 단순 흰색 배경
```
background: #FFFFFF
```

**홈 메인 화면**: 2레이어 구조
```
레이어 1 (상단 447px):
  linear-gradient(94.36°, #FFB5AB 1.74%, #FF604B 90.6%)

레이어 2 (하단 콘텐츠 카드 영역):
  border-radius: 17.16px (상단 모서리)
  background: linear-gradient(to-bottom, #FFFFFF, #FFF0EE)
```

**종목 미설정 홈**: 브랜드 그라디언트 전체 화면
```
linear-gradient(95.9°, #FFB5AB 1.74%, #FF604B 90.6%)
```

### 5-2. 바텀 네비게이션 (4탭)
```
홈 | 분석 | 기록 | 마이
```
- 고정 하단 배치
- 활성 탭: 텍스트 #FF4B33 + 활성 아이콘
- 비활성 탭: 텍스트 #A5A5A5 + 비활성 아이콘
- 배경: 흰색 (#FFFFFF)

### 5-3. CTA 버튼 (주요) ✅ 확정
- 전체 너비: width 330pt (좌우 30pt 패딩)
- 높이: 54pt
- 모서리: 13pt radius
- 내부 그림자: inset 0px 0px 6.6px rgba(255,255,255,0.25)
- 활성 배경: #FF4B33 또는 그라디언트 (linear-gradient(111.41°, #FFB1A6, #FF6651))
- 비활성 배경: #FFB7AD (연한 살몬)
- 텍스트: 20pt Regular 흰색

### 5-3-1. 입력 필드 ✅ 확정
- 높이: 54pt, 모서리: 13pt radius
- 배경: #FFFFFF
- 내부 그림자: inset 0px 0px 6.6px rgba(255,255,255,0.25)
- 기본 테두리: 1px solid #B1B6BE
- 포커스/유효 테두리: 1px solid #FF4B33 (브랜드 컬러)
- 오류 테두리: 1px solid #54B8CD (틸)
- 라벨: 17pt Regular #0C0C0C, 위치: 필드 위
- 플레이스홀더: 12pt Regular #B1B6BE
- 성공 메시지: 12pt Regular #FF4B33
- 오류 메시지: 12pt Regular #54B8CD
- 우측 유효성 아이콘: 체크마크 또는 X (10.4×7.4pt)

### 5-4. 카드 컴포넌트 ✅ 확정
- 배경: #FFFFFF
- 테두리: 0.858px solid #D9D9D9
- 모서리: 15pt radius (일반) / 8.58pt radius (리스트 아이템)
- 그림자: 없음

### 5-5. 점수 위젯 (OBJECTS 컴포넌트) ✅ 확정 — 옥타곤
크기: 95.43px × 95.43px. **정팔각형(8각형) 외곽선** + 숫자 오버레이.
```
외곽선: 두꺼운 8각형 stroke #FF4B33 (두께 3px)
내부 채움: 흰색 #FFFFFF
중앙 텍스트: 50pt Bold #FF4B33
위치: 홈 최근 분석 카드 우측 (223px, 310px)
```
→ 구현: components/OctagonScore.tsx (react-native-svg `<Polygon>` + 절대위치 텍스트).
정점 좌표 = viewBox 100 기준 정팔각형, inset 4(stroke 잘림 방지).
> 정정 이력: 이전엔 "원형 OBJECTS 이미지"로 적혀 있었으나 Figma 1:719 재확인 결과 옥타곤이 맞음 (2026-05-20).

### 5-6. 선수 프로필 카드 (종목 선택)
- 선수 이미지 풀블리드 배경
- 하단 이름 + 타이틀 오버레이
- 탭 시 이력 팝업 (소속/경력/수상이력)

### 5-7. 레벨 선택 — 원형 다이얼 UI
피그마에서 확인된 독특한 인터랙션.
```
중앙 텍스트 (선택된 레벨명)
  + 원형 슬라이더 (드래그로 레벨 이동)
  + 하단 기간 텍스트 ("1개월", "6개월")
```
React Native에서는 PanResponder 또는 react-native-gesture-handler로 구현.

### 5-8. 튜토리얼 슬라이드
- 전체 화면 슬라이드 (가로 스와이프)
- 하단 점(dot) 인디케이터 3개
- 건너뛰기 버튼 (우상단)
- 마지막 슬라이드: "시작하기" CTA

### 5-9. 분석 로딩 화면
단계별 진행 메시지 순차 표시.
```
단계 1: "영상 프레임 추출 중..."
단계 2: "포즈 데이터 분석 중..."
단계 3: "기준 모션과 비교 중..."
완료:   "분석이 완료되었어요!"
```

### 5-10. 플랜 페이월 트리거
- 고급 기능 접근 시 자연스럽게 노출
- 팝업이 아니라 전체 화면으로 플랜 비교
- 카피: "고급 트릭 분석은 Basic 플랜부터 가능해요"

---

## 6. 화면별 UI 핵심 노트

### 인트로 / 스플래시
- 로고 중앙 배치 + 슬로건
- 배경: 스포츠 이미지 또는 다크 그라디언트
- "시작하기" 주요 CTA
- "이미 계정이 있으신가요? 로그인하기" 보조 텍스트 링크

### 회원가입
- SNS 가입: 카카오 / 네이버 / Google / Apple (4개 버튼 세로 나열)
- "이메일로 가입하기" 텍스트 링크 하단
- 이메일 가입 STEP 01: 이메일 + 비밀번호
- STEP 02: 약관 동의 (전체동의 + 개별동의)
- 진행률 표시: 상단 "STEP 01 / 02"

### 로그인
- SNS 로그인 버튼 4개
- 이메일 + 비밀번호 폼
- 자동 로그인 토글
- 비밀번호 찾기 텍스트 링크
- Face ID 옵션 (선택)

### 알림 설정
- "코칭 알림을 받으시겠어요?" 헤드라인
- 3가지 토글: 운동 리마인더 / 분석 완료 / 주간 리포트
- 기본값: 모두 ON 권장

### 레벨 선택
- 원형 다이얼 UI (핵심 차별화 포인트)
- 입문 / 중급 / 고급 3단계
- 하단에 레벨별 설명 텍스트

### 종목 선택
- 폴스포츠 카드 (정은지 선수 이미지)
- [출시 예정] 종목: 테니스, 족구 등 비활성 표시
- 선수 이름 탭 시 이력 팝업 (하단 시트)
- 완료 화면: "[이름]님의 폴스포츠 여정을 응원합니다."

### 플랜 안내
- 3개 플랜 카드: FREE / BASIC(₩9,900) / PRO(₩19,900)
- 기능 체크리스트로 비교
- 추천 플랜 강조 표시

### 기준 모션 선택
- "어떤 동작과 비교할까요?" 헤드라인
- 탭: 기본기 / 중급 / 고급
- 동작 리스트 카드 (동작명 + 설명)
- "분석하기" 하단 CTA

### 영상 업로드 소스 선택
- "어떻게 영상을 가져올까요?" 헤드라인
- 2개 옵션 카드: 즉석 촬영 / 앨범에서 선택
- 카메라 권한 요청 화면 (별도)

### AI 분석 로딩
- 전체 화면 로딩 (단계별 메시지)
- 오류 상태 4종:
  - 서버 오류: "분석 중 문제가 발생했어요"
  - 인체 미감지: "사람을 찾지 못했어요"
  - 용량 초과: 100MB 초과 안내
  - 지원 불가 형식: mp4, mov만 가능 안내

### 메인 홈 (4가지 상태) ✅ 확정

**상태 A: 기존 사용자 (분석 기록 있음)**
```
상단 그라디언트 영역 (브랜드 컬러):
  - 종목명 25pt Bold 흰색 (중앙)
  - 마지막 접속일 + 평균 점수 12pt 흰색 (중앙)
  - 공지 배너: #FF4B33 rounded pill (NEW 뱃지 포함)

카드 영역 (흰→#FFF0EE 그라디언트, 상단 모서리 17.16px):
  - 최근 분석 카드 (bordered)
      좌: 종목명 20pt Bold #FF4B33 + 동작명 17pt + 날짜 12pt #ACACAC
      우: 점수 위젯 95px (50pt Bold #FF4B33)
  - "오늘 도전해볼 동작" 섹션 타이틀 20pt Bold
  - 동작 리스트 (썸네일 51.48px + 동작명 18pt Bold + 난이도 17pt + 화살표)
  - 성장 그래프 카드 (흰색 bordered, 주간 꺾은선)
```

**상태 B: 신규 사용자 (첫 분석 전)**
```
동일 레이아웃이나 최근 분석 카드 대신:
  - "프로와 얼마나 가까운지 확인해봐요!" 18pt Bold
  - "첫 분석하기" 그라디언트 버튼 (pill 형태, #FFB1A6→#FF6651)
  - AI 그래프 카드: 점선 테두리 + "분석을 2번 이상 하면 AI 그래프가 보여요"
```

**상태 C: 종목 전환 (족구 등)**
```
동일 구조, 종목명만 변경
종목명 텍스트: 20pt Bold #FF4B33 (카드 내)
```

**상태 D: 종목 미설정**
```
브랜드 그라디언트 전체 화면
  - "[이름]님, 안녕하세요" 25pt Bold 흰색
  - "STEP 1 / 종목을 골라주세요" 카드 (흰색, #FF4B33 점선 테두리)
  - "종목 선택하기 >" 그라디언트 버튼
  - 하단 잠금 카드 (rgba(255,75,51,0.4) 오버레이 + 잠금 아이콘)
```

---

## 7. 네비게이션 구조

```
[온보딩 플로우] (로그인 전)
인트로 → 시작화면
  ├── 로그인 → (Face ID) → 홈
  └── 회원가입 → 알림설정 → 레벨선택 → 종목선택 → 플랜안내 → 홈

[메인 앱] (로그인 후)
바텀탭: 홈 / 분석 / 기록 / 마이

홈 → 분석 탭 진입
  → 영상 소스 선택
  → 카메라 / 갤러리
  → AI 분석 로딩
  → 기준 모션 선택 (또는 플랜 페이월)
  → [미완성] 분석 결과 화면
```

---

## 8. 미완성 화면 (개발 시 설계 필요)

피그마에 아직 없는 화면들. 개발 전 IA 참조해서 설계 필요.

```
❌ 분석 결과 화면
   - 유사도 점수 (원형 게이지)
   - 파트별 세부 점수 (상체/하체/코어)
   - 기준 모션과 나란히 비교 (좌: 내 영상 / 우: 프로)
   - 틀린 관절 하이라이트
   - 코칭 팁 카드 3개

❌ 기록 탭
   - 분석 기록 리스트
   - 성장 그래프 (꺾은선)
   - 전후 비교 기능

❌ 마이페이지
   - 프로필 / 구독 / 알림 / 계정

❌ 게이미피케이션 UI
   - 레벨 / 배지 / 포인트 (MVP 이후)
```

---

## 9. 인터랙션 컨벤션

```
화면 전환     : 기본 슬라이드 (좌→우)
모달/시트     : 하단에서 올라오는 Bottom Sheet
오류 피드백   : 인라인 텍스트 (토스트 최소화)
로딩          : 전체 화면 (스피너 X, 단계별 메시지)
성공 피드백   : 상단 배너 또는 화면 전환으로 대체
버튼 비활성   : opacity 0.4
탭 피드백     : 즉각 반응 (haptic 권장)
```

---

## 10. 개발 시 주의사항

```
1. 라이트 테마 기반. 다크모드 대응 불필요 (MVP 기준)
   → 이전 다크 추정 완전 폐기
   → ⚠️ 예외: 분석 로딩 화면(§5-9, Figma 1:429/436/445)은 다크 네이비 +
     파랑→보라 그라디언트 링이 최신 결정. 임팩트 화면 단독 예외.
     본 결정 2026-05-20 — 다른 화면은 라이트 유지.

2. 홈 배경은 LinearGradient 컴포넌트로 구현
   → 이미지 에셋 아님! CSS/RN linear-gradient 사용
   → react-native-linear-gradient 설치 필요

3. 원형 다이얼 UI (레벨 선택)
   → 직접 구현 필요. 라이브러리 없음
   → react-native-gesture-handler 사용 권장

4. 폴스포츠 특성상 세로 영상이 많을 수 있음
   → 영상 플레이어는 가로/세로 모두 대응

5. AI 분석 대기 시간 (30~60초)
   → 단순 스피너 금지. 단계별 메시지로 체류감 유지

6. 피그마 색상값은 반드시 직접 확인 후 하드코딩
   → 이 문서의 추정 hex값 그대로 쓰지 말 것
```

---

*최종 업데이트: 피그마 MCP 실제 코드 추출 완료. 모든 색상/레이아웃 확정.*
*참조 IA: Sunity_AI_Coach_IA.xlsx (6시트)*
